---
context: default
phase: ad-hoc-profile-settings-impersonate
task: 2
total_tasks: 3
status: paused
last_updated: 2026-04-09T09:16:19.437Z
---

<current_state>
Sesi ini mengerjakan 2 task ad-hoc pada halaman Profile dan Settings, plus investigasi fitur Impersonate.
Task 1 dan 2 selesai (belum di-commit). Task 3 (Impersonate actions) belum dimulai.
</current_state>

<completed_work>

- Task 1: Tambah RoleLevel + Preview P-Sign ke Profile page - Done
  - `Models/ProfileViewModel.cs` — tambah property `RoleLevel` dan `PSign`
  - `Controllers/AccountController.cs` — map RoleLevel dan PSignViewModel di method Profile()
  - `Views/Account/Profile.cshtml` — tambah row Role Level (label deskriptif per level 1-6) dan section Preview P-Sign via partial `_PSign`
- Task 2: Hapus Ubah Password dari Settings page - Done
  - `Views/Account/Settings.cshtml` — hapus seluruh section Ubah Password + `@inject IConfiguration`
  - `Controllers/AccountController.cs` — hapus ChangePassword action + referensi di Settings()
  - `Models/SettingsViewModel.cs` — hapus property ChangePassword dan class ChangePasswordViewModel
</completed_work>

<remaining_work>

- Task 3: Implementasi 3 controller actions di AdminController untuk fitur Impersonate
  - `StartImpersonation()` — POST, panggil ImpersonationService.StartRole() atau StartUser()
  - `StopImpersonation()` — POST, panggil ImpersonationService.Stop() + audit log
  - `SearchUsersApi()` — GET, AJAX endpoint untuk search user by query
  - Infrastructure sudah ada: ImpersonationService, ImpersonationMiddleware, Impersonate.cshtml, _ImpersonationBanner.cshtml, _Layout integration
</remaining_work>

<decisions_made>

- RoleLevel ditampilkan sebagai label deskriptif (Level 1 — Admin, dst) bukan angka saja
- Reuse partial `_PSign` di Profile (konsisten dengan Settings)
- Hapus ChangePasswordViewModel sepenuhnya (user tidak butuh fitur ubah password)
- Hapus `@inject IConfiguration` dari Settings.cshtml (hanya dipakai untuk cek AD terkait password)
</decisions_made>

<blockers>
- Build: HcPortal.exe locked (app sedang jalan) — tidak ada compilation error, hanya MSB3027 copy error. Restart app untuk verifikasi.
</blockers>

## Required Reading (in order)
1. `Services/ImpersonationService.cs` — session management untuk impersonate (30 menit auto-expire)
2. `Middleware/ImpersonationMiddleware.cs` — read-only enforcement, whitelist paths
3. `Views/Admin/Impersonate.cshtml` — UI page dengan search + "View As" cards
4. `Views/Shared/_ImpersonationBanner.cshtml` — red banner saat impersonating

## Infrastructure State
- App berjalan di localhost:5277 (HcPortal.exe PID 19776)
- Perubahan Profile/Settings belum di-commit

<context>
Fokus sesi: perbaikan UX halaman Profile dan Settings. Profile ditambah info yang kurang (RoleLevel, P-Sign). Settings dibersihkan dari fitur yang tidak dipakai (Ubah Password). Investigasi Impersonate menunjukkan infrastructure sudah lengkap, tinggal 3 action methods.
</context>

<next_action>
Start with: Commit perubahan Profile & Settings, lalu implementasi StartImpersonation, StopImpersonation, dan SearchUsersApi di AdminController.
</next_action>
