---
gsd_state_version: 1.0
milestone: v32.7
milestone_name: Perbaikan Menyeluruh Sistem Pre-Test/Post-Test
status: executing
stopped_at: Phase 423 context gathered
last_updated: "2026-06-24T01:28:42.615Z"
last_activity: 2026-06-24 -- Phase 423 planning complete
progress:
  total_phases: 39
  completed_phases: 3
  total_plans: 12
  completed_plans: 9
  percent: 75
---

# Project State: Portal HC KPB

## Project Reference

See: .planning/PROJECT.md

**Core value:** Evidence-based competency tracking with automated assessment-to-CPDP integration
**Current focus:** Phase 423 — Certificate Issuance Consistency

## Current Position

Milestone: **v32.7 Perbaikan Menyeluruh Sistem Pre-Test/Post-Test** — 🚧 STARTED 2026-06-22 (branch ITHandoff)
Phase: 423 (Certificate Issuance Consistency) — NOT STARTED (next: discuss)
Plan: 0 of TBD
Status: Ready to execute
Last activity: 2026-06-24 -- Phase 423 planning complete

**Predecessor v32.4 Ujian Ulang — ✅ CLOSED 2026-06-22** (audit PASSED 14/14; tag lokal `v32.4`; migration=TRUE Fase 405; NOT pushed). v32.1+v32.3+v32.4 closed di ITHandoff (deploy bundle). v32.7 menyusul di bundle yang sama.

## Next Action

**Phase 422 ✅ COMPLETE (all 5 gates PASS) — next `/gsd-discuss-phase 423`.** Progres: 420 ✅ + 421 ✅ + 422 ✅ (3/6). Sumber: audit `docs/prepost-audit/2026-06-22-evaluasi-pretest-posttest.md` (~60 temuan, 4 High). 6 fase 420-425 (1:1 dgn kategori REQ), urutan eksekusi **sekuensial by file-overlap** 420 → 421 → 422 → 423 → 424 → 425:

1. **Phase 420 (P1) FORM** — Form Create/Edit persistensi field (E-01 🔴) + UX Pre-Post. FORM-01..11. migration=FALSE.
2. **Phase 421 (P2) RTH** — Retake lifecycle hardening (RTK-LOGIC-02 🔴). RTH-01..05. migration=FALSE.
3. **Phase 422 (P3) SHFX** — SamePackage & Shuffle integrity (SHUF-ISS-03 🔴). SHFX-01..07. **migration=KEMUNGKINAN TRUE** (toggle SamePackage). ⚠️ overlap v32.6 main — rekonsiliasi saat merge.
4. **Phase 423 (P4) CERT** — Certificate issuance consistency. CERT-01..07. migration=FALSE.
5. **Phase 424 (P5) GRDF** — Grading de-dup + Flow/Linking + Gating Pre→Post (FLOW-04 🔴). GRDF-01..07. migration=FALSE.
6. **Phase 425 (P6) CLN** — Cosmetic/Naming/Tech-Debt. CLN-01..05. **migration=KEMUNGKINAN TRUE** (CLN-03 drop `AssessmentPhase`).

**Coverage:** 42/42 v1 REQ mapped, 0 orphan, 0 duplikat. Keputusan bisnis terkonfirmasi: (a) Pre wajib selesai dulu (GRDF-01); (b) SamePackage fleksibel (SHFX-02); (c) soal Post=Pre sudah ada (SamePackage). Inject (VAL-01) & Section/ScopedShuffle (v32.6) OUT-of-scope.

**NEXT:** `/gsd-discuss-phase 423` (Phase 422 ✅ done — 5 gerbang lulus, HEAD `a8d10ce8`, migration=TRUE `AddPackageNumberUniqueIndex` applied lokal, NOT pushed). 423 CERT (Certificate Issuance Consistency, CERT-01..07, migration=FALSE) belum punya CONTEXT.md → discuss dulu. Fase mulai 420 (main pakai 409-419: v32.5=409-414, v32.6=415-419) — hindari integer collision saat bundle deploy.

**Deploy bundle (user-owned):** v32.1 + v32.3 + v32.4 (+v32.7 saat siap) → 1 push `origin/ITHandoff` → notify IT **migration=TRUE** (Fase 399 `AddUserUnitsTable` + Fase 405 retake cols + v32.7 migration P3/P6 TBD; carry lama 360 `PendingProtonBypass` + 372 `ShuffleToggles`).

**v32.0** CLOSED manual 2026-06-19 di branch `main` (lihat MEMORY `project_v32_0_close_deferred`).

Backlog tracked: 999.6/9/10/11/12.

## Tag Git

- `v24.0`..`v31.0` — ✅ tag dibuat. v29/v30 PUSHED `origin/ITHandoff`; v31.0 MERGED→main + PUSHED `origin/main` 2026-06-16 (merge `7ea6c81e`). v32.1 CLOSED (archive-only, tag lokal, NOT pushed — deploy bareng v32.3).

## Deferred Items

> ✅ **ACCEPTED OK 2026-06-14** (keputusan user): semua carry-over v11.2/v13/v14/v15 di bawah = phase lama, dianggap OK / non-blocking (kode ship + jalan; tak ada bug report v16-v32). Histori, bukan TODO. Buka lagi hanya bila muncul bug/kebutuhan nyata.

### Carry-over ACCEPTED OK (ringkas)

| Item | Status |
|------|--------|
| EPRV-01 Preview Essay rubrik (v15.0) | accepted-OK |
| Phase 303 Coach Workload 12-langkah UAT | accepted-OK (kode ship+jalan) |
| Phase 235 (5 items) + Phase 247 (2 TODO) UAT | accepted-OK |
| Phase 297 Pre-Post Renewal behavior + Phase 298 essay char limit | accepted-OK (undecided, non-blocking) |
| Phase 293 org Level 2+ support | accepted-OK (org 2-level cukup) |
| v11.2 Phase 281 (System Settings) + 285 (Impersonation Page) | accepted-OK (closed-early) |

### Backlog aktif (belum dipromote)

| Item | Reason |
|------|--------|
| 999.9 label residu "Backfill/Restore" di UI BulkBackfill | kosmetik (LOW) |
| 999.11 WR-01 PendingGrading guard (parked dari v32.0) | tech-debt parked |
| 999.12 391 test WebApplicationFactory (parked dari v32.0) | tech-debt parked |
| 43 quick-task/todo (audit-open) | ✅ RESOLVED 2026-06-21 (pre-close v32.3) — 42 quick-task ditandai `status: complete`; 1 todo legacy-session-cleanup di-promote ke backlog 999.12 |

### v2 / future (dari REQUIREMENTS v32.3)

| Item | Reason |
|------|--------|
| Cert/analytics atribusi per-unit akurat (kolom unit-at-issue + backfill) | deferred — D1=b primary; buka bila compliance per-unit butuh (migration ke-2) |
| PROTON paralel (2 track aktif konkuren) | deferred — sekuensial dikonfirmasi; perlu relax unique index + kolom Unit `ProtonTrackAssignment` + re-key ~21 site |

### Push IT

| Item | Status |
|------|--------|
| Notify IT — 2 migration carry lama (`PendingProtonBypass`+index/360, `ShuffleToggles`/372) | ⏳ PENDING (carry lama) |
| **v32.1** — 0 migration, 0 backend; deploy ditunda (close bareng v32.3) | ⏳ pending (bundle dgn v32.3) |
| **v32.3** — **migration=TRUE** (`AddUserUnitsTable` Phase 399 + backfill); 1 push → notify IT migration=TRUE (commit hash) | ✅ CLOSED 2026-06-21 (tag lokal v32.3); ⏳ pending deploy (push + notify IT) |

## Accumulated Context

### Decisions (persist across milestones)

- [422-01 / SHFX-05 + SHFX-07 backend — Phase 422 (v32.7) Wave 1 KEYSTONE, plan 1/3 COMPLETE]: **migration=TRUE `AddPackageNumberUniqueIndex` (commit `31ef3e1d`).** Task 1 (carried `0ed5983f`): 3 pure-helper kill-drift — `PackageSizeAnalysis.Compute` (satu sumber mismatch/refCount/withQ), `SessionEditLockRules.IsSessionEditLocked` (PostTest && SamePackage; dipakai Wave 2 guard + Wave 3 view), `ShuffleToggleRules.ShouldShowKMinTruncationWarning` ON-path (D-04) + 33 helper-test. Task 2: migration Up() dedup ROW_NUMBER OVER (PARTITION BY AssessmentSessionId ORDER BY PackageNumber, Id) renumber SEBELUM CreateIndex unique `(AssessmentSessionId, PackageNumber)` (Pitfall 1); Down() DropIndex only; index PLAIN tanpa filter (Pitfall 2 — PackageNumber non-nullable; EF tak generate filter); applied lokal SQLEXPRESS OK, dups==0, is_unique=1. Task 3: CreatePackage `existingCount+1`→`MaxAsync (maxNumber ?? 0)+1` (anti-bentrok pasca delete tengah); 5× `OrderBy(PackageNumber).ThenBy(Id)` deterministik (:5447/5527/5572/5764/5895, 0 bare tersisa); 2 integration test (MAX+1 anti-clash, unique reject DbUpdateException, dedup renumber gap-free) hijau. **v32.6 MERGE:** 5× ThenBy + CreatePackage MAX+1 + ShuffleToggleRules extend = titik rekonsiliasi (lihat 422-01-SUMMARY §Merge). **IT NOTIFY: migration=TRUE AddPackageNumberUniqueIndex** (bundle v32.7). NOT pushed. Build 0-err. **NEXT 422-02 (Wave 2):** guard 5 endpoint POST + ToggleSamePackage + SyncToLinkedPostIfSamePackageAsync.
- [420-03 / FORM-07..11 + FORM-10 rename UX Pre-Post — Phase 420 (v32.7) Wave 3, plan 3/3 — 3/4 task auto SHIPPED, **Task 4 UAT live @5270 AWAITING** (checkpoint:human-verify, plan BELUM complete)]: **migration=FALSE.** Redesign tata-letak form Create mode Pre-Post; mode Standard WAJIB tak berubah perilaku (backward-compat = risiko utama). **FORM-10** rename atomik `AssessmentTypeInput`→`CreationMode` (6 ref ctrl: param POST CreateAssessment + ViewBag + 2 compare + isPrePostMode + validasi `ModelState.AddModelError("CreationMode")`; 9 ref view: select `name="CreationMode"` `id="creationMode"` + 8 JS `getElementById('creationMode')`; XML-doc model 4-nilai PreTest/PostTest/Standard/Manual + catatan beda CreationMode). **Kolom DB `AssessmentType` TIDAK disentuh**; grep produksi (Controllers/+Views/) 0 sisa. **FORM-07/L-4** SamePackage pindah dari kartu Post → `#samePackageHeaderWrapper` (header section, di atas `#ppt-jadwal-section`, di LUAR kartu Post), default `d-none` tampil hanya Pre-Post. **FORM-08/L-2** dua sub-kartu Pre-Post `#prePostSettingsCards` (default `d-none`): "Setelan Post-Test" (`bi-clock-history`: PassPercentage+Sertifikat) + "Setelan Bersama Pre & Post" (`bi-arrow-left-right`: Shuffle+AllowAnswerReview+Token) — **via DOM-relokasi SINGLE-SOURCE** (`appendChild` move-bukan-clone + `homeMap` rekam parent+nextSibling untuk restore persis) → hanya 1 set `<input name>` → payload Standard identik. **FORM-09/L-5** `#standard-jadwal-section` inputs + hidden combiner `#schedHidden`+`#ewcdHidden` di-`disabled` saat Pre-Post (Pitfall #4/#5: disabled bukan d-none → tak ter-POST); aksi-balik enable saat Standard. **FORM-11/L-3** `#retakeBlockCreate` `d-none`+input retake disabled saat Pre-Post (Pre baseline); PassPercentage relabel "Nilai Lulus Post-Test (%)" via JS. **JS toggle diperluas** `applyPrePostLayout()`/`applyStandardLayout()` — cabang `else` (Standard) AKSI-BALIK SEMUA (Pitfall #2): kembalikan field ke Group B/C/D, retake visible+enabled, std input enabled, sub-kartu+header hidden. Group B/C/D dapat `id` (`groupBCard`/`groupCCard`/`groupDCard`) untuk di-`d-none` saat Pre-Post. **2 spec e2e BARU** (`tests/e2e/`): `form-persistence-420.spec.ts` (FORM-01 lifecycle: create shuffle ON via wizard → buka Edit assert `#ShuffleQuestions`/`#ShuffleOptions` checked → submit → reopen → MASIH checked, E-01 closed) + `form-prepost-ux-420.spec.ts` (5 skenario per-mode: FORM-08/10 sub-kartu+binding, FORM-07 SamePackage header, FORM-11 retake hidden+disabled, FORM-09 std input+combiner disabled, regresi Standard layout tunggal+round-trip). Idiom `shuffle.spec.ts` (serial + db.backup/restore SEED_WORKFLOW + login admin + `--workers=1` @ E2E_BASE_URL). **`--list` 7 tests/3 files parse OK; executor TIDAK live-run** (delegasi checkpoint Task 4). **Deviasi [Rule 3]×1**: rename DOM `id` memecah regresi `assessment.spec.ts` 8.2-8.4 (RESEARCH grep melewatkan 2 file e2e `wizardSelectors.ts`+`assessment.spec.ts` yg ref `#assessmentTypeInput`) → fix nilai selector `wizardSelectors.ts` `#assessmentTypeInput`→`#creationMode` (key dipertahankan, pemanggil tak disentuh; Views/ grep tetap bersih). **build 0 err** (25 warning pre-existing); 0 `Html.Raw` baru (1 existing line 901). `[ValidateAntiForgeryToken]`+`[Authorize(Admin,HC)]` POST CreateAssessment DIPERTAHANKAN. Commits `1ddfc952`(feat rename)+`3bdc29f8`(feat view+JS)+`2c558a75`(test e2e). Files: `Controllers/AssessmentAdminController.cs`, `Views/Admin/CreateAssessment.cshtml`, `Models/AssessmentSession.cs`, `tests/e2e/helpers/wizardSelectors.ts`, `tests/e2e/form-persistence-420.spec.ts`, `tests/e2e/form-prepost-ux-420.spec.ts`. **SISA (checkpoint Task 4 = orchestrator/user UAT gate): live Playwright @5270 `form-persistence-420 form-prepost-ux-420 shuffle assessment --workers=1` (per-mode baru + regresi Standard hijau, 0 pageerror) + `dotnet test` full @SQLEXPRESS + browser manual @5270 (default Standard layout tunggal; Pre-Post SamePackage header+2 sub-kartu+retake hilang+std input disabled; DevTools payload Pre-Post TANPA Schedule/EWCD/retake; Edit shuffle persist FORM-01; redirect manual FORM-06; lock Completed FORM-05) + SEED snapshot→restore (SEED_JOURNAL cleaned).** Plan BELUM ditandai complete. Pre-existing `tsc` error di `manage-org-label.spec.ts`/`proton-bypass.spec.ts` → `deferred-items.md` (out-of-scope). Lesson: rename DOM id WAJIB sapu e2e selector helper (grep RESEARCH bisa lewatkan `tests/`); backward-compat sub-kartu paling aman via DOM-relokasi move single-source (bukan duplikasi markup yg bisa double-POST).
- [420-02 / FORM-05 lock + FORM-06 redirect SHIPPED — Phase 420 (v32.7) Wave 2, plan 2/3]: **migration=FALSE.** Tutup 2 celah server-authoritative form Edit assessment tanpa ubah perilaku sesi standard. **FORM-05 (E-04)**: ANGKAT guard `Status=="Completed"` ke ATAS cabang Pre-Post di POST `EditAssessment` (setelah null-check, **sebelum** `if (AssessmentType=="PreTest"||"PostTest")` :1854) — bug asal: cabang Pre-Post `return RedirectToAction("ManageAssessment")` :2010 mendahului guard lama single-mode :2014 → guard tak terjangkau Pre-Post (metadata pasca-Completed bisa dimutasi). **GROUP-AWARE PENUH (Open Q#1)**: `isCompleted` via `AnyAsync(a=>a.LinkedGroupId==assessment.LinkedGroupId && a.Status=="Completed")` bila `LinkedGroupId.HasValue`, else `Status=="Completed"` (identik guard lama); blokir SELURUH metadata grup bila ADA satu sesi Completed → redirect ManageAssessment + `TempData["Error"]`, metadata TAK dimutasi. Guard lama :2014-2019 **DIBIARKAN** (defense-in-depth jalur standard; identik utk non-grup, harmless, default-safe per plan). **TIDAK reuse** `AssessmentEditEligibility.IsEditableAsync` (semantik TERBALIK: `true` HANYA bila Completed). **FORM-06 (E-08)**: GET `EditAssessment` +`if(assessment.IsManualEntry) RedirectToAction("EditManualAssessment","TrainingAdmin",new{id})` tepat setelah null-check :1705 (sebelum `bool isPrePost`) — mirror filter `TrainingAdminController` EditManualAssessment GET. **Test BARU** `HcPortal.Tests/EditGuardRedirect420Tests.cs` (5 Fact real-SQL `IClassFixture<RetakeServiceFixture>`, MakeController pola `AssessmentWindowRemovalTests` null!-substitute deps): Test1 group-aware lock (Pre Open + Post Completed → POST Edit ditolak + Title tak berubah), Test2 standard lock (Completed tunggal ditolak+non-mutasi), Test3 negatif all-Open (replika-body `AnyAsync(...Completed)==false`), Test4 redirect manual (`RedirectToActionResult` EditManualAssessment/TrainingAdmin/route id), Test5 negatif online (replika-body `IsManualEntry==false`). Campuran **action-invoke** (Test 1/2/4 return-early → assert `RedirectToActionResult` aktual) + **replika-body guard** (Test 3/5 jalur lanjut, hindari deps null!/render `View()` custom-override). **5/5 GREEN @SQLEXPRESS; build 0 err; non-integration suite 448/0/2** (identik baseline 420-01, no regresi). `[Authorize(Admin,HC)]` GET+POST + `[ValidateAntiForgeryToken]` POST DIPERTAHANKAN. **0 deviasi** (anchor struktural; nomor baris geser ~+6..18 dari literal plan akibat 420-01 — terverifikasi). Commits `b09c8301`(test)+`e043983c`(feat). Files: `HcPortal.Tests/EditGuardRedirect420Tests.cs`, `Controllers/AssessmentAdminController.cs`. **NEXT 420-03** (UX Pre-Post FORM-07..11 + rename `AssessmentTypeInput`→`CreationMode` FORM-10 + e2e FORM-01 lifecycle). Lesson: guard akses-kontrol WAJIB diangkat ke atas SEMUA cabang mutasi (cabang `return` mendahului guard = guard mati); test guard return-early via action-invoke, jalur-lanjut via replika-body (hindari deps null!/render View).
- [420-01 / FORM-01..04 persistensi field SHIPPED — Phase 420 (v32.7) Wave 1, plan 1/3]: **migration=FALSE.** Tutup pola "field dirender/ditulis tapi tak konsisten" tanpa ubah perilaku mode Standard. **FORM-01** (akar E-01 🔴): render `asp-for="ShuffleQuestions"`/`asp-for="ShuffleOptions"` di `EditAssessment.cshtml` (Group "Pengaturan Assessment" :384, berdampingan kartu Ujian Ulang) — bug = view TAK merender shuffle → POST bind `false` → reset OFF tiap Edit; write controller (:2084-2085) SUDAH benar, murni RENDER. Copy idiom+text dari `CreateAssessment.cshtml:536-551` (label `fw-semibold` ikut Edit). **A1 VERIFIED**: Group settings shared single+Pre-Post (conditional `@if(isPrePostGroup)` :213 hanya bungkus tab jadwal :230-380; render shuffle SEKALI cukup). `asp-for` emit hidden fallback `value="false"` → unchecked bind benar (bukan data-loss); `checked` terisi dari `Model` (entity). **FORM-02**: penyalinan retake EKSPLISIT di 3 jalur build Create (Pitfall #1 checklist tuntas) — std (:1467) `AllowRetake=model`+clamp; **Pre (:1243) `AllowRetake=false` EKSPLISIT (D-03 baseline murni)** + MaxAttempts/RetakeCooldownHours disalin grup (clamp); Post (:1279) `AllowRetake=model`+clamp. **FORM-03+04** (Edit std loop :2072): +`sibling.ValidUntil=model.ValidUntil` (FORM-04, sebelumnya absent) +`AllowRetake/MaxAttempts/RetakeCooldownHours` clamp (FORM-03, sebelumnya no-op; satu-satunya writer = `UpdateRetakeSettings`). Semua tulis retake pakai `Math.Clamp(MaxAttempts,1,5)`/`(RetakeCooldownHours,0,168)` (V5 / T-420-01 defense-in-depth). **Test BARU** `HcPortal.Tests/FormPersistence420Tests.cs` (5 Fact real-SQL `IClassFixture<RetakeServiceFixture>`, template `ShuffleCreatePersistenceTests`+`RetakeSettingsEndpointTests`, replika SHAPE object-init/sibling-loop VERBATIM) — Test1 std retake persist(4/12), Test2 Pre baseline `AllowRetake==false` walau model true, Test3 Post clamp outlier(9→5,999→168), Test4 Edit std loop ValidUntil+retake×3 ke 3 sibling clamp(9→5,200→168), Test5 FORM-01 invariant shuffle write benar. **5/5 GREEN @SQLEXPRESS** (`HcPortalDB_Dev` live di env executor). **build 0 err; non-integration suite 448/0/2** (no regresi baseline v32.4). **Deviasi [Rule 3]×1**: `ValidUntil` aktual = `DateOnly?` (bukan `DateTime?` seperti RESEARCH `<interfaces>` — TZ-01 v19.0 DateOnly refactor); fix test `new DateOnly(2027,1,31)` (controller type-aman, model VM=entity DateOnly?). backward-compat std: hanya tambah field, 0 existing diubah/dihapus; `[Authorize(Admin,HC)]`+`[ValidateAntiForgeryToken]` (:1806-1807) UTUH. Commits `8bda9e5f`(test)+`d72b304a`(feat view+Edit-loop)+`9ec4d657`(feat Create×3). Files: `HcPortal.Tests/FormPersistence420Tests.cs`, `Views/Admin/EditAssessment.cshtml`, `Controllers/AssessmentAdminController.cs`. **NEXT 420-02** (FORM-05 guard lock Completed group-aware + FORM-06 redirect manual GET) + **420-03** (UX Pre-Post FORM-07..11 + rename `AssessmentTypeInput`→`CreationMode` FORM-10; e2e FORM-01 lifecycle penuh). Lesson: field yang ditulis controller WAJIB dirender view (anti silent reset E-01); RESEARCH interface bisa keliru tipe (verify ke source — DateOnly bukan DateTime).
- [408-02 / RTK-14 GAP-3 e2e lifecycle SHIPPED (ARTEFAK) — Phase 408 Wave 2, plans 2/2]: 2 file BARU PURELY ADDITIVE (0 test/spec/helper/seed existing tersentuh, 0 production, 0 migration). **`tests/sql/retake-lifecycle-408-seed.sql`** ([RETAKE408] 1 sesi gagal lifecycle: Completed IsPassed=0 Score 40, **PassPercentage=50** [all-correct→Score 100≥50→LULUS], **GenerateCertificate=1** [Pitfall 2 — beda 407=0], **RetakeCooldownHours=0** [Pitfall 5 — tombol aktif langsung], AllowRetake=1 MaxAttempts=3, **AccessToken=''/IsTokenRequired=0** [hindari token gate StartExam re-entry], CompletedAt -2 hari, ShuffleOptions=1; paket 3 SA opsi-benar **deterministik** `BENAR408_Q1_AsamHF`/`BENAR408_Q2_Alkylate`/`BENAR408_Q3_Isobutana` [spec pilih by-TEXT shuffle-safe] + opsi salah `SALAH408_*` + current responses pilih SALAH [prior failed → ✗ verdict pra-retake]; UPA ShuffledQuestionIds; **0 arsip era-retake** currentAttempt=1<3 eligible single-pass; idempotent FK-safe WIPE-AND-INSERT cleanup `'[[]RETAKE408%'` + THROW 51408 guard). **`tests/e2e/retake-lifecycle-408.spec.ts`** (1 happy-path real-browser RTK-14: jahit harness 407 [serial + db.backup/seed/restore SEED_WORKFLOW + login coachee + pageerror lesson 413] + exam-taking Flow A [answer-by-text shuffle-safe + `submitExamTwoStep` helper]; langkah: login→Results(gagal)→assert pra-retake leak-safe [`.card` Tinjauan Jawaban + `[role=status]` Kunci jawaban disembunyikan + `not.toContain('(Jawaban Benar)')` + `not.toContain('BENAR408_')` + `.list-group-item-success` count 0 + pageerror empty]→klik `#btnRetake`→`#retakeConfirmModal` + `__RequestVerificationToken` count 1→`Promise.all([waitForURL **/CMP/StartExam/**, submit])`→defensive resume-modal dismiss→loop `[id^=qcard_]` baca `h6,.fw-bold` map marker Qn→correctText→`label[id^=lbl_]` filter hasText→`#answeredProgress` `${q}/${q}`→`submitExamTwoStep`→assert `.badge.text-bg-success` "LULUS" + body "Nomor Sertifikat" + regex `/KPB\/\d+\/[IVX]+\/\d{4}/` + pageerror empty). **Selector dibaca dari markup aktual** (anti-deviasi): StartExam `#qcard_`/`#lbl_`/`#answeredProgress` text "0/N answered"→`toContainText`, question text di `<p class=fw-bold>`→selector `h6,.fw-bold` valid, `#reviewSubmitBtn`; Results badge LULUS=`.badge.text-bg-success` (BUKAN body toContainText 'LULUS' — anti salah-match `.text-bg-danger` "TIDAK LULUS"), cert "Nomor Sertifikat: {NomorSertifikat}", `#btnRetake`. **`dotnet build` 0 Error** (25 warning pre-existing OOS); **spec `--list` 1 test parse OK** (executor TIDAK live-run); grep acceptance: RETAKE408 seed 16 / submitExamTwoStep 4 / waitForURL StartExam 1 / pageerror 9 / KPB 2 / LULUS 6 / list-group-item-success 2 / GenerateCertificate+cooldown+BENAR408+51408 hadir. **0 deviasi** (budget 3 unused — selector pre-read). Commits `5159e7d0`(seed+journal) + `0497c18b`(spec). **Plan 408-02 = migration=FALSE.** Files: `tests/sql/retake-lifecycle-408-seed.sql`, `tests/e2e/retake-lifecycle-408.spec.ts`, `docs/SEED_JOURNAL.md`. **Task 3 = checkpoint:human-verify (blocking)** — live UAT lifecycle penuh + full regresi (dotnet test @SQLEXPRESS + `retake-lifecycle-408 retake-worker-407 retake-config-406 riwayat-hc-406 --workers=1` @5270, 0 pageerror) + DB count `[RETAKE408]`=0 pasca-restore + journal `cleaned` = **DELEGASI ORCHESTRATOR autopilot UAT gate** (executor TIDAK menjalankan live run per instruksi kritis). **Phase 408 plans 2/2 (status ready_for_verification). NEXT: orchestrator live UAT → `gsd-secure-phase 408` (D-03 formal, `<threat_model>` konsolidasi 406+407+T-408-cert di 408-02-PLAN.md) → `/gsd-verify-work`.** SEED_JOURNAL [RETAKE408] status `planned` (→`cleaned` setelah orchestrator afterAll RESTORE).
- [408-01 / RTK-14 GAP-1 capstone SHIPPED — Phase 408 Wave 1]: **`HcPortal.Tests/RetakeThenPassCertTests.cs`** (1 file BARU, PURELY ADDITIVE — 0 test hijau tersentuh, 0 production, 0 migration). Capstone invariant v32.4 **DIBUKTIKAN via SQL riil @SQLEXPRESS**: sesi GAGAL (IsPassed=false) → **`RetakeService.ExecuteAsync`** (reset-ONLY: hapus responses/assignment + arsip snapshot, Status→Open, **TAK PERNAH issue cert**) → re-seed paket+assignment+respons-benar → muat ulang sesi → **`GradingService.GradeAndCompleteAsync`** (grade-dari-DB, all-correct Score 100 ≥ PassPercentage 70 → isPassed=true → step 6 issue NomorSertifikat) → assert **TEPAT 1 NomorSertifikat** (anti-double-cert guard `GradingService.cs:287-312` retry-3x + filtered `WHERE NomorSertifikat==null` di bawah unique index `IX_AssessmentSessions_NomorSertifikat`) + format **`^KPB/\d{3}/[IVX]+/\d{4}$`** (CertNumberHelper). **REUSE total**: `IClassFixture<RetakeServiceFixture>` (disposable DB full MigrateAsync chain) + `NoOpHubContext` (same assembly RetakeServiceTests.cs) + ctor recipe `GradingService` VERBATIM `SubmitResurrectionTests.cs:68-76` (FakeNotificationService/FakeWorkerDataService/NullLogger; no Moq) + seed helpers disalin (SeedUserAsync/SeedSessionAsync/SeedPackageWithResponsesAsync). **SeedSessionAsync DIPERLUAS**: param baru `generateCertificate`(default false→**Pitfall 2** set true) + `passPercentage`(70). **Pitfall 1** dihormati: TIDAK assert cert==null pasca-ExecuteAsync (ExecuteAsync tak pernah issue cert → assert itu lulus-menyesatkan). **A2** dikonfirmasi: pasca-reset Status="Open" **TIDAK** di set terminal-reject GradeAndCompleteAsync (Completed/Abandoned/Cancelled/PendingGrading) → grade lanjut TANPA set Status manual. **+1 nilai-tambah** `Assert.False(CanRetakeAsync(sid))` pasca-lulus (D-02 — BUKAN duplikasi counting; (UserId,Title,Category) Pre/Post no-conflate tetap di `RetakeServiceTests.Counting_PrePostSameTitle_NoConflate`). build **0 err** (25 warning pre-existing unrelated, out-of-scope); **`dotnet test --filter ~RetakeThenPassCert` GREEN 1/0/0 (1s) @SQLEXPRESS**. grep acceptance: ExecuteAsync=6, GradeAndCompleteAsync=6, Assert.Equal(1,certCount)=1, IClassFixture=1, Trait Integration=1. **0 deviasi** (budget 3 unused; skeleton plan compile+pass first-run). `<threat_model>` konsolidasi 406+407+T-408-cert tersedia di 408-01-PLAN.md untuk `gsd-secure-phase 408`. Commit `39e3ef46` (test). **Plan 408-01 = migration=FALSE.** Files: `HcPortal.Tests/RetakeThenPassCertTests.cs`. **NEXT 408-02** (Playwright e2e lifecycle `retake-lifecycle-408.spec.ts` + `tests/sql/retake-lifecycle-408-seed.sql` — buktikan invariant sama in-browser, cert exactly-1 ditegaskan-ulang oleh xUnit GAP-1 ini).
- [407-03 / RTK-10/11/12 worker UI SHIPPED — Phase 407 Wave 3, LENGKAP 3/3]: **`Views/CMP/Results.cshtml` `@switch(Model.RetakeMode)` 3-state** (tambah `@using HcPortal.Helpers`): **`ShowFullReview`** = markup full-review existing **VERBATIM** (option-loop + leak-site + essay CorrectAnswer utuh, kunci tampil saat lulus/exhausted); **`ShowWrongFlagsOnly`** = **BRANCH BARU LEAK-SAFE** (card Tinjauan + `alert-info role=status` `bi-eye-slash` note kunci-disembunyikan + loop QuestionReviews render HANYA `Soal N: QuestionText` + badge verdict tri-state [IsEssayPending→Menunggu/IsCorrect→Benar/else→Salah] + `!IsNullOrEmpty(q.UserAnswer)`→"Jawaban Anda: @q.UserAnswer"; **TIDAK render `list-group-item-success`/icon-kunci/"(Jawaban Benar)"/`CorrectAnswer` essay**); **`ShowScoreOnly`** = notice `alert-info` "Tinjauan jawaban tidak tersedia" VERBATIM. **Retake control RTK-10** (sebelum action row): `IsCapReached`→`alert-warning role=alert` `bi-lock-fill` "Batas percobaan tercapai (@MaxAttempts dari @MaxAttempts). Hubungi HC..." (no tombol); `CanRetake`→`#btnRetake` (cooldown→`disabled`+`data-cooldown-until=@...ToString("o")` / eligible→`data-bs-toggle=modal data-bs-target=#retakeConfirmModal`) + counter `Percobaan ke-@CurrentAttempt dari @MaxAttempts`. **Modal `#retakeConfirmModal`** (centered, aria-labelledby, `btn-close aria-label=Tutup`) → footer "Batal" + `<form method=post asp-action=RetakeExam asp-controller=CMP asp-route-id=@Model.AssessmentId>`+**`@Html.AntiForgeryToken()`**+submit "Ya, Ujian Ulang" (RTK-09/D-02). **Countdown JS guard-safe** (lesson 413): IIFE `if(!btn)return`+`if(!iso)return`+`isNaN` guard; `setInterval` 1s tulis `#retakeCountdown` `HH:MM:SS` via `.textContent`; saat `<=0`→`clearInterval`+`removeAttribute(disabled)`+`setAttribute(data-bs-toggle/target modal)`+relabel "Ujian Ulang". **Partial baru `Views/CMP/_RiwayatPekerja.cshtml`** (copy `Admin/_RiwayatPercobaan`, `@model List<RiwayatAttemptViewModel>`, 3 worker-delta: "Gagal"→**"Tidak Lulus"** `text-bg-danger`, "Jawaban Peserta"→**"Jawaban Saya"**, **gating `ViewData["HideDetail"]` bool**→true render daftar attempt TANPA tabel per-soal [notice "Tinjauan jawaban tidak tersedia"]; tri-state cell ✓/✗/— + `visually-hidden` + badge `bg-info` "Percobaan saat ini" [`attempt.IsCurrent`] + empty-states VERBATIM; **ZERO raw-output**) dirender di Results card "Riwayat Percobaan Saya" via `PartialAsync("_RiwayatPekerja", Model.RiwayatAttempts, new ViewDataDictionary(ViewData){["HideDetail"]=(RetakeMode==ShowScoreOnly)})` (empty-state bila null/kosong). **`Records.cshtml` SENGAJA tak disentuh** (D-04 default: andalkan "Lihat Hasil" route ke Results tempat riwayat hidup). **Playwright `tests/e2e/retake-worker-407.spec.ts`** (mirror retake-config-406: mode serial + per-spec `db.backup/restore` SEED_WORKFLOW; login worker=`coachee`) **6 skenario**: leak-safety (sesi A ShowWrongFlagsOnly: card+notice+badge Benar/Salah+"Jawaban Anda:"; DOM `not.toContain("(Jawaban Benar)")`/`"KUNCIBENAR_A1"`/`"KUNCIBENAR_A2"`+`.list-group-item-success` count 0; no-pageerror) / control eligible (#btnRetake+counter) / modal (#retakeConfirmModal+`__RequestVerificationToken` count 1+"Ya, Ujian Ulang"; no-pageerror) / riwayat (card+#riwayatPekerjaAccordion+"Percobaan saat ini") / cap-reached (sesi B alert "Batas percobaan tercapai"+#btnRetake count 0) / cooldown (sesi C disabled+#retakeCountdown `HH:MM:SS` ticking, conditional non-flaky). **`tests/sql/retake-worker-407-seed.sql`** 3 sesi `[RETAKE407]` idempotent WIPE-AND-INSERT+THROW 51407 (A=LeakSafe+Eligible Completed IsPassed=0 AllowRetake=1 MaxAttempts=3 cooldown=0 +1 arsip→currentAttempt=2+package chain 2SA q1-benar/q2-salah+opsi-kunci `KUNCIBENAR_*` / B=CapReached MaxAttempts=2 +2 arsip→currentAttempt=3 / C=CooldownActive CompletedAt=now RetakeCooldownHours=24). **build 0 err; unit Category!=Integration 448/0/2 no-regresi** (baseline 407-02 identik); spec parse `--list` 6 tests OK (**live run = orchestrator UAT gate @5270, TIDAK dijalankan executor**). **Deviasi [Rule 1]×2**: comment di view (ShowWrongFlagsOnly) + partial di-rephrase agar gate grep-by-string bersih (leak-token `list-group-item-success`/"(Jawaban Benar)"/`CorrectAnswer` + `Html.Raw`/"Jawaban Peserta" keluar dari komentar; markup ter-render TIDAK berubah; 2 fix < limit 3). Commits `b57fdc6b`(feat tier/control/modal/JS)+`810ffb60`(feat partial+render)+`0bd3c1ac`(test spec+seed+journal). **Plan 407-03 = migration=FALSE** (Razor view+partial+spec/seed murni). Files: `Views/CMP/Results.cshtml`, `Views/CMP/_RiwayatPekerja.cshtml`, `tests/e2e/retake-worker-407.spec.ts`, `tests/sql/retake-worker-407-seed.sql`, `docs/SEED_JOURNAL.md`. **Phase 407 LENGKAP 3/3 (RTK-09/10/11/12/13). SISA: live Playwright UAT @5270 (orchestrator gate) → tandai SEED_JOURNAL `cleaned` → `/gsd-verify-work` (verifier 407) → Phase 408 (Test&UAT terakhir).**
- [407-02 / RTK-09/10/12/13 controller SHIPPED — Phase 407 Wave 2]: DI `RetakeService` ke `CMPController` (field/ctor param-15/assign, mirror triple `_gradingService`; AddScoped Program.cs:63 no-reg-baru) + action POST `RetakeExam(int id)` (cermin HC ResetAssessment :4244-4327; beda actor=worker effective-user/guard=ownership Forbid+CanRetakeAsync/redirect=StartExam): `[HttpPost]`+`[ValidateAntiForgeryToken]` (RTK-09 CSRF) → load sesi → `GetCurrentUserRoleLevelAsync` (impersonation-aware) → **`if(assessment.UserId!=user.Id) return Forbid();`** (IDOR T-407-idor SEBELUM mutasi) → **`if(!await _retakeService.CanRetakeAsync(id))`** server-authoritative re-check (T-407-bypass; countdown JS non-authoritative) → not-eligible redirect Results+`TempData[Error]` → actorName format mirror :4298 → `ExecuteAsync(id,user.Id,actorName,"RetakeAssessment","worker_retake")` → **`TempData.Remove($"TokenVerified_{id}")`** (must-fix #1 T-407-token, StartExam pakai Peek non-consume) → `RedirectToAction("StartExam")`. `Results` perluas (SEBELUM return View): counting eraRetakeArchives mirror CanRetakeAsync :237-242 → currentAttempt; `attemptsRemaining = AllowRetake && !ShouldHideRetakeToggle(...) && currentAttempt<MaxAttempts` (non-retake/PreTest/Manual → ShowFullReview no-regresi); set 7 VM field (CurrentAttempt/MaxAttempts/CanRetake/RetakeMode/CooldownUntilUtc/IsCapReached/RiwayatAttempts); **RetakeMode=ResolveReviewMode(AllowAnswerReview, `assessment.IsPassed` bool? BUKAN viewModel.IsPassed bool — Pitfall 5)**; riwayat load cermin RiwayatPercobaan :3493-3522 (`RetakeArchiveBuilder.Build(0,...)`+`RiwayatUnifier.Build`, currentRows only-if Status==Completed); **branch QuestionReviews/AllowAnswerReview UTUH** (tier ShowWrongFlagsOnly reuse). build **0 err**; **RetakeExamEndpointTests 3/3 green** (FakeUserStore IUserStore+IUserRoleStore empty-roles + StubSession no-impersonation→UseRealUser + real-SQL RetakeServiceFixture/NoOpHubContext: non-owner→`ForbidResult`+sesi tetap Completed / LULUS→CanRetake false→RedirectToAction Results+`TempData[Error]`+no-mutasi / failed+eligible→`TempData[TokenVerified]` TERHAPUS+RedirectToAction StartExam+sesi→Open); **unit 448/0/2** no-regresi (baseline 407-01 identik). **Deviasi [Rule 3]**: FakeUserStore WAJIB implement `IUserRoleStore` (GetCurrentUserRoleLevelAsync panggil `GetRolesAsync` SETELAH `GetUserAsync` → NotSupportedException) — fixed inline sebelum commit Task 3. Commits `f77d7710`(feat DI+action)+`120d6286`(feat Results VM populate)+`af19a643`(test 3 RTK-09). **Plan 407-02 = migration=FALSE.** Files: `Controllers/CMPController.cs`, `HcPortal.Tests/RetakeExamEndpointTests.cs`. **NEXT 407-03** (VIEW: Results.cshtml `@switch(Model.RetakeMode)` suppress leak-site `:366/:367/:386-389/:403` di ShowWrongFlagsOnly + retake control block btnRetake+countdown `data-cooldown-until`+IsCapReached lock RTK-10 + confirm modal POST RetakeExam+`@Html.AntiForgeryToken` + `_RiwayatPekerja` partial render `Model.RiwayatAttempts` RTK-12). 7 VM field+RiwayatAttempts sudah terisi server.
- [407-01 / RTK-11+RTK-12 tier helper + VM SHIPPED — Phase 407 Wave 1]: **`enum RetakeReviewMode {ShowFullReview, ShowWrongFlagsOnly, ShowScoreOnly}`** (adjacent class `RetakeRules`) + **`RetakeRules.ResolveReviewMode(allowAnswerReview, isPassed bool?, attemptsRemaining)`** PURE 3-state **leak-safe A1 (orchestrator-locked)**: `if(!allowAnswerReview)→ShowScoreOnly`; **`if(isPassed != true && attemptsRemaining)→ShowWrongFlagsOnly`** (pending null diperlakukan SAMA dengan failed — tahan kunci selama retake MASIH MUNGKIN, **BUKAN `isPassed == false`**); `else→ShowFullReview` (passed | belum-lulus-exhausted, retake tak mungkin lagi). `CanRetake`/`ShouldHideRetakeToggle` existing UTUH. **6 Fact truth-table** (Tier_ScoreOnly_WhenReviewDisabled / Tier_WrongFlagsOnly_WhenFailedWithAttemptsLeft / **Tier_WrongFlagsOnly_WhenPendingNullWithAttemptsLeft** / Tier_FullReview_WhenFailedExhausted / **Tier_FullReview_WhenPendingNullExhausted** / Tier_FullReview_WhenPassed) — menyimpang dari PATTERNS.md asli (5-Fact `isPassed==false` + `Tier_FullReview_WhenPendingNull`), ikut A1 di PLAN.md verbatim. **`AssessmentResultsViewModel` +7 field** (`RetakeMode` default ShowFullReview / `CanRetake` / `CurrentAttempt` / `MaxAttempts` / `CooldownUntilUtc` / `IsCapReached` / `RiwayatAttempts`=`List<RiwayatAttemptViewModel>?`); **`AllWorkersHistoryRow` +`IsCurrentAttempt`**. **0 field existing terhapus** (AllowAnswerReview/IsPassed/IsPendingGrading utuh). build **0 err**; **RetakeRulesTests 22/22** (16 existing + 6 tier); **unit suite 448/0/2** (Category!=Integration, 2 skip SQLEXPRESS-gated, no regresi). TDD: RED verified **12 compile-err (CS0103/CS0117)** sebelum impl → GREEN combined 1 feat commit (C# RED tak compile bersih isolasi). Commits `c4ad2fe4`(helper+test) + `b45e5123`(VM). **Plan 407-01 = migration=FALSE.** Files: `Helpers/RetakeRules.cs`, `HcPortal.Tests/RetakeRulesTests.cs`, `Models/AssessmentResultsViewModel.cs`, `Models/AllWorkersHistoryRow.cs`. **NEXT 407-02** (CMPController.Results isi 7 field + `RetakeExam` action CSRF+ownership+`CanRetakeAsync`/`ExecuteAsync`; **Pitfall 5: pakai `assessment.IsPassed` bool? BUKAN `viewModel.IsPassed` bool**) + **407-03** (Results.cshtml `@switch(Model.RetakeMode)` suppress leak-site `:366/:367/:386-389/:403` di ShowWrongFlagsOnly + `_RiwayatPekerja` partial render RiwayatAttempts).
- [406-03 / RTK-08 riwayat HC modal mount SHIPPED — Phase 406 LENGKAP 3/3]: `AssessmentMonitoringDetail.cshtml` +**dropdown `<li>` `.btn-riwayat-percobaan`** (Completed-gated, `data-session-id`+`data-worker-name`+`aria-label`) sebelum `</ul>` dropdown ⋮ baris peserta + **ONE shared `#riwayatPercobaanModal`** (`modal-lg modal-dialog-scrollable`, `#riwayatBody`) ditaruh setelah `#activityLogModal` **DI LUAR `@if(Model.IsPackageMode)`** (selalu render; reshuffle-modals `:481` di-DALAM if, akhiriSemua/activityLog di-LUAR) + **lazy-fetch JS dalam main IIFE (`:804-1031`)**: delegate `click` on `.btn-riwayat-percobaan` → `#riwayatModalLabel.textContent='Riwayat Percobaan — '+wname` (**XSS-safe `.textContent` T-406-10, BUKAN innerHTML**) → spinner "Memuat riwayat..." → `new bootstrap.Modal(...).show()` → `fetch(appUrl('/Admin/RiwayatPercobaan?sessionId='+encodeURIComponent(sid)))` (**PathBase-aware T-406-13, Dev /KPB-PortalHC**) → `body.innerHTML = server @-encoded partial` (T-406-11) → `.catch` alert-warning "Gagal memuat riwayat. Coba lagi.". **Konsumsi endpoint+partial 406-01** (`RiwayatPercobaan` GET `:3485` [Authorize Admin,HC] + `_RiwayatPercobaan.cshtml`) — TIDAK re-implement partial; mirror EditHistoryPartial lazy-load (`:986-1001`) + appUrl fetch (`:1013/:1080`). **Deviasi**: Rule3 escape bare `@`→`@@` di 2 JS-comment (`@-encoded` picu CS1501 Razor `Write()` 0-arg); Rule1 e2e helper buka dropdown ⋮ (`button[aria-haspopup]`) DULU sebelum klik dropdown-item (Bootstrap collapse default-hidden). build **0 err**; **e2e `riwayat-hc-406.spec.ts` 5/5 GREEN @5270 `--workers=1`** (open=modal+lazy accordion 3 attempt / per-soal=kolom No/Soal/Jawaban/Status/Skor+nilai seeded / current=badge "Percobaan saat ini" paling atas ke-3 / pending=essay `IsCorrect=NULL`→`—`/Menunggu title bukan ✗ / **xss=`window.__riwayatXss406` undefined + literal `<script>` ter-encode teks**); seed `tests/sql/riwayat-hc-406-seed.sql` `[RIWAYAT406]` Completed session+package chain (current attempt LIVE)+2 `AssessmentAttemptHistory`+5 `AssessmentAttemptResponseArchive` (benar/salah/XSS di A1, benar/essay-pending di A2; idempotent WIPE-AND-INSERT+THROW 51406; tambah `MaxCharacters` NOT NULL saat dry-run); **SEED_WORKFLOW snapshot→seed→RESTORE Layer 4 RIWAYAT406+matrix leftovers=0** (journal `cleaned`); **full xUnit 604/0/2 no regresi** (2 skip = SQLEXPRESS-gated 404). Commits `83054ba8`(feat view)+`a4cea6b0`(test spec+seed+journal). **Plan 406-03 = migration=FALSE** (Razor view + e2e murni). Files: `Views/Admin/AssessmentMonitoringDetail.cshtml`, `tests/e2e/riwayat-hc-406.spec.ts`, `tests/sql/riwayat-hc-406-seed.sql`, `docs/SEED_JOURNAL.md`. **Phase 406 LENGKAP 3/3 (RTK-05+RTK-08, migration=FALSE seluruh fase — 405 bawa satu-satunya migration v32.4) → ready `/gsd-verify-work`.** NEXT: verify 406 → **407** (worker CMP `RetakeExam`/`CanRetakeAsync`/`ExecuteAsync`, paralel depends 405) + **408** (Test&UAT terakhir depends 406+407).
- [406-01 / RTK-08 riwayat backend SHIPPED — Phase 406 Wave 1]: **`RiwayatUnifier`** (Helpers/, PURE EF-free sync, mirror RetakeArchiveBuilder/RetakeRules) satukan **arsip** (group STRICT by `AttemptHistoryId`, Pitfall 3; history by `(UserId,Title,Category)` anti-konflasi Pre/Post) + **attempt LIVE saat ini** (via `RetakeArchiveBuilder.Build(0,...)` sentinel id=0, HANYA bila `Status=="Completed"` Pitfall 2/5 anti partial-leak) → `List<RiwayatAttemptViewModel>` ordered `AttemptNumber DESC` (current floats top via max+1, `IsCurrent=true`). **VM** (`Models/RiwayatAttemptViewModel.cs`): `AttemptNumber`/`ScorePercent`/`IsPassed`/`CompletedAt`/`IsCurrent`/`Rows`(=`List<AssessmentAttemptResponseArchive>` shape identik arsip). **Endpoint `RiwayatPercobaan(int sessionId)`** disisip setelah `AssessmentMonitoringDetail` (`:3477`): `[HttpGet]`+`[Authorize(Roles="Admin, HC")]` (mirror :3290, IDOR/answer-leak T-406-01; **read-only GET → TANPA `[ValidateAntiForgeryToken]`**) → `PartialView("_RiwayatPercobaan", vm)`, `ViewBag.WorkerName=session.User?.FullName`. **`Views/Admin/_RiwayatPercobaan.cshtml`**: `@model List<RiwayatAttemptViewModel>`, accordion newest-first (first expanded `show`) + per-soal table No/Soal/Jawaban/Status/Skor, **tri-state** `IsCorrect` ✓Benar/✗Salah/—Menunggu (null≠false Pitfall 4) + `visually-hidden` label + badge "Percobaan saat ini"; **XSS D-02** semua user-content via Razor `@` default-encode, **ZERO `Html.Raw`** (gate grep=0), NO answer-key leak (cuma `AnswerText`+verdict glyph+`AwardedScore`); 2 empty-state. **xUnit 6/6 GREEN** (no DB: ordering DESC, IsCurrent max+1 floats-first, empty→empty, strict AttemptHistoryId grouping, unmatched-row not-attached, score/pass/date provenance); build **0 err**; full suite **604/0/2** (2 skip=`UserUnitsBackfillIntegrationTests` SQLEXPRESS-gated 404, no regresi). **Deviasi**: Rule3 hapus literal "Html.Raw" dari komentar partial (gate conformance) + Rule1 hapus unused `using System;`. Commits `53ed0e37`(helper+VM+xUnit) + `6b47820a`(endpoint) + `c0199ee3`(partial). **Plan 406-01 = migration=FALSE** (backend+partial murni; TIDAK sentuh ManagePackages/Create/Edit/AssessmentMonitoringDetail). Files: `Models/RiwayatAttemptViewModel.cs`, `Helpers/RiwayatUnifier.cs`, `HcPortal.Tests/RiwayatUnifierTests.cs`, `Controllers/AssessmentAdminController.cs`, `Views/Admin/_RiwayatPercobaan.cshtml`. **NEXT 406-02** (config card ManagePackages mirror shuffle + binding Create/Edit) + **406-03** (wire trigger+modal-shell+fetch JS di AssessmentMonitoringDetail konsumsi endpoint ini + Playwright RUNTIME-verify partial @5270 — Lesson 354 build+grep tak cukup utk Razor).
- [405-04 / controller wire SHIPPED — Wave 4 FINAL, Phase 405 LENGKAP 4/4]: Wiring HC retake ke `AssessmentAdminController` (1 file, 3 commit atomik). **(1) `ResetAssessment` (RTK-06)** — inject `RetakeService` (param ke-13 ctor + field `_retakeService`); GANTI blok inline `:4238-4323` (archive→delete→ET-cleanup→ExecuteUpdateAsync-reset→audit→SignalR, 78 baris) dgn `await _retakeService.ExecuteAsync(id, rsUser?.Id, rsActorName, actionType:"ResetAssessment", reason:"hc_reset")` + `if(!rsResult.Success)` error-redirect + **`TempData.Remove($"TokenVerified_{id}")`** (must-fix #1 — StartExam pakai `TempData.Peek` non-consume). Guard HC (`IsResettable` :4193 TETAP `public static` + Pre-Post block + status guard) + trailing `TempData["Success"]`+redirect DIPERTAHANKAN di controller. `SendAsync("sessionReset")` count di controller = **0** (dipindah ke service). **(2) Endpoint `UpdateRetakeSettings` (RTK-04)** — sisip setelah `UpdateShuffleSettings`: `[HttpPost]`+`[Authorize(Admin,HC)]`+`[ValidateAntiForgeryToken]`, params `(assessmentId, allowRetake, maxAttempts, retakeCooldownHours)`; guard `RetakeRules.ShouldHideRetakeToggle` reject PreTest/Manual; `Math.Clamp(maxAttempts,1,5)`+`Math.Clamp(retakeCooldownHours,0,168)`; sibling-propagation key identik shuffle (`Title==&&Category==&&Schedule.Date==`) foreach set 3 field+`UpdatedAt`→SaveChanges; audit `"UpdateRetakeSettings"` warn-only; PRG redirect ManagePackages. **TANPA lock-guard `IsShuffleLocked`** (retake config bisa diubah kapan saja — D-02 retroaktif, beda dari shuffle). **(3) Bulk-add (RTK-01)** — standard add-users `newSessions` (:2166) tambah `AllowRetake/MaxAttempts/RetakeCooldownHours = savedAssessment.*` (pekerja baru mewarisi policy sibling, anti silent EF-default). Pre/Post add (`:1944`/`:1965`) **SENGAJA tak disentuh** (copy dari `model` ViewModel tanpa field retake → compile-error; EF default false/2/24 benar untuk Pre D6 + Post default-off). **(4) ManagePackages ViewBag** — `AllowRetake`/`MaxAttempts`/`RetakeCooldownHours`/`HideRetakeToggle`/`RetakeMaxAttemptsUsedInGroup` (untuk card UI Phase 406). **build 0 error; `ResetGuardTests` 2/2 (regresi RTK-06 hijau, IsResettable static); unit suite 436/438 (2 skip, no regresi, baseline 405-03 identik); `RetakeServiceTests` integration 5/5 (service masih jalan pasca-delegasi controller, SQLEXPRESS).** Deviasi **[Rule 3]**: `AssessmentWindowRemovalTests:48` ctor manual break (CS7036) → tambah `retakeService: null!` (pola existing file, action tak pakai dep). Commits `e1d5defc` (refactor RTK-06) + `7e3fc4aa` (feat RTK-04) + `e6abb938` (feat RTK-01). **Plan 405-04 = migration=FALSE** (controller murni). Files: `Controllers/AssessmentAdminController.cs`. **Phase 405 = 4/4 plan COMPLETE → ready `/gsd-verify-work`.** NEXT: verify 405 → plan 406+407 paralel (406=Views card konsumsi ViewBag+form; 407=worker CMP `CanRetakeAsync`/`ExecuteAsync(RetakeAssessment)`).
- [405-01 / data-model + migration SHIPPED — Wave 1 BLOCKING COMPLETE]: Fondasi data ujian ulang v32.4 di chain. **Entity `AssessmentAttemptResponseArchive`** (`Models/`, snapshot per-soal beku: `Id`/`AttemptHistoryId`(FK)/`AttemptHistory`(nav)/`PackageQuestionId`(plain int no-FK)/`QuestionText`/`AnswerText`(null)/`IsCorrect`(bool? null=essay-pending)/`AwardedScore`/`ArchivedAt`). **3 kolom config `AssessmentSession`** (sisip setelah `ShuffleOptions:42`): `AllowRetake`(false)/`MaxAttempts`(2, `[Range(1,5)]`)/`RetakeCooldownHours`(24, `[Range(0,168)]`). **DbSet `AssessmentAttemptResponseArchives`** + entity config (`builder.Entity<...>`, BUKAN `modelBuilder` — param aktual = `builder`): `HasIndex(AttemptHistoryId)` + FK `OnDelete(Cascade)`→`AssessmentAttemptHistory`. **Migration `AddRetakeColumnsAndArchive`** (`20260621065918`, D-03 single-migration pola 399) digenerate via global `dotnet ef` 10.0.3 (env Development) + **applied LOKAL `HcPortalDB_Dev` saja**; snapshot ProductVersion tetap **8.0.0** (no drift 10.x); `has-pending-model-changes`=none. **DEVIASI [Rule 1]**: EF generate `MaxAttempts`/`RetakeCooldownHours` `defaultValue: 0` (C# property init `=2`/`=24` TIDAK ditarik ke backfill `AddColumn` baris existing) → hand-fix migration `Up()` → `2`/`24` (verified sqlcmd default constraint `((2))`/`((24))`). **sqlcmd verified**: 3 kolom+default benar, tabel archive (8 kolom, ObjectId `436196604`), FK `CASCADE`, index `IX_..._AttemptHistoryId`, **5 baris legacy `AssessmentAttemptHistory` retained (D-04)** + archive table lahir KOSONG (0 rows → D-01 snapshot-presence diskriminator natural-works). build 0 error. Commits `2d04f216` (entity) + `11fad1ef` (kolom+DbSet+config) + `69db727a` (migration, **migration=TRUE notify IT**). **Plan 405-01 = migration=TRUE** (BARU v32.4, applied lokal; IT promosi Dev/Prod, deploy bundle v32.4). RTK-01/02 BELUM 100% (RTK-01 sisa bulk-add→405-04; RTK-02 sisa `RetakeArchiveBuilder`→405-02) — TIDAK di-mark-complete prematur. Files: `Models/AssessmentAttemptResponseArchive.cs`, `Models/AssessmentSession.cs`, `Data/ApplicationDbContext.cs`, `Migrations/*AddRetakeColumnsAndArchive*`. **Chain `MigrateAsync`-ready untuk integration test 405-03.**
- [400-01 / MU-06 set-aware listing SHIPPED — Phase 400 COMPLETE]: filter unit listing pekerja diubah dari scalar `u.Unit==unitFilter` (primary-only) → **set-aware correlated EXISTS** `_context.UserUnits.Any(uu=>uu.UserId==u.Id && uu.Unit==unitFilter && uu.IsActive)` di **3 lokasi** (`WorkerDataService.GetWorkersInSection` + `WorkerController.ManageWorkers` + `ExportWorkers`). Pekerja {X,Y} 1-Bagian muncul saat difilter **tiap** unit (termasuk non-primary); **dedup by-construction** (`.Any()` boolean subquery = 1 row/user, no fan-out — **TANPA `.Distinct()`**). Kolom **Unit kontekstual D-02** (filtered→matched unit / unfiltered→all-active primary-first comma-join `string.Join(", ", uList)` / 0-unit→fallback `user.Unit` D-05); **batch-load dict `unitsByUser` D-04** (1 query active-only primary-first, no N+1). **PITFALL #1 dihindari**: `_context.UserUnits` correlated, BUKAN nav-prop `u.UserUnits` (CS1061). **Anomali-backfill check `HcPortalDB_Dev`=0 → `.Any()` MURNI final** (NO OR-scalar-fallback; backfill 399 lengkap, RESEARCH Open Q2 resolved). **Consumer #4 `AssessmentAdminController:278`** (ManageAssessmentTab) mewarisi set-aware OTOMATIS (no code change). **No-drift D1=b (SC#3)**: analytics `CMPController:2581/:2589` scalar mirror + Team View `:543` no-filter call **UTUH** (diff bersih). Markup `_RecordsTeamBody.cshtml` byte-stable (value-driven). suite **507/0/3** (3 skip = `UserUnitsBackfillIntegrationTests` SQLEXPRESS-gated milik Phase 404; baseline ≥366 terlampaui, 0 regresi); filter `~WorkerDataServiceSearchTests` 17/17. **UAT lokal APPROVED** (Playwright+SQL snapshot→seed→RESTORE, journal cleaned `e203c9ad`: user Iwan GAST primary "Alkylation Unit (065)" + sekunder aktif "RFCC NHT (053)" → no-filter 1-baris dedup + kolom "Alkylation Unit (065), RFCC NHT (053)" / filter primary→match kolom "Alkylation Unit (065)" / **filter NON-primary RFCC→TETAP match** (set-aware ✓) kolom "RFCC NHT (053)" / EXISTS translation benar di SQL Server riil). 6 test MU-06 (set-aware both-units, dedup `Count==1`, IsActive D-03, kontekstual unfiltered/filtered D-02, fallback D-05) + regresi `Scope_Null_NoFilter_BackwardCompat` (SC#3). Commits RED `24a71b7f` + GREEN `520058b8`. **Plan 400-01 = 0 migration.** Files: `Services/WorkerDataService.cs`, `Controllers/WorkerController.cs`, `HcPortal.Tests/WorkerDataServiceSearchTests.cs`. **Phase 400 = 1/1 plan COMPLETE → ready `/gsd-verify-work`.**
- [399-04 / display semua unit SHIPPED — Phase 399 LENGKAP]: 7 surface tampil SEMUA unit pekerja primary ditandai (**MU-03 done**). 5 HTML (Profile/Settings/WorkerDetail/ManageWorkers/Home hero) badge primary-first via `Units.OrderByDescending(x=>x==PrimaryUnit).ThenBy(x=>x)`: primary=`bg-success bg-opacity-10 text-success`+`bi-star-fill`+"Utama"(+visually-hidden), sekunder=`bg-secondary bg-opacity-25 text-dark` (UI-SPEC §B PERSIS). **`_PSign` cetak all-units primary-first comma-join (D-07 LOCKED, BUKAN primary-only, teks polos no-badge)**; Excel kolom 7 primary-first (399-02). VM **Profile/Settings/PSign/DashboardHome** diperluas `List<string> Units`+`string? PrimaryUnit` (Section TETAP scalar; PSign scalar `Unit` fallback dipertahankan). **AccountController inject `ApplicationDbContext` BARU** (sebelumnya TIDAK ada — VERIFIED) + Profile/Settings GET populate UserUnits primary-first → VM + nested PSign; **WorkerDetail GET `ViewBag.WorkerUnits`/`PrimaryUnit`** (read-only view-binding, @model=ApplicationUser bukan VM — Rule 3, TIDAK sentuh write-logic 399-02); **HomeController** populate `CurrentUserUnits`/`PrimaryUnit` (mode-role null-safe). Fallback D-09 ("Belum diisi" panel / "-" cell / no-row print). Mirror `user.Unit` dipertahankan (pembaca belum-migrasi). Spec `tests/e2e/multiunit-display-399.spec.ts` **8/8 hijau** headless `--workers=1` (D-01..D-08: WorkerDetail 2 badge+bintang+Utama, ordering primary-first, ManageWorkers cell, 0-unit fallback, Profile smoke, **_PSign D-07 login-as-pekerja-2-unit baca `.psign-label`**, **Excel D-08 JSZip `xl/sharedStrings.xml` primary-first comma**). **build 0 error**; **suite 366/366** (0 skip, no regresi); app boot localhost:5277 HTTP 200; DB snapshot→RESTORE baseline `UserUnits`=6 (SEED_JOURNAL cleaned). 3 deviasi auto-fix (Rule 3 WorkerDetail ViewBag view-binding; Rule 1 ×2 spec: createWorker re-select Bagian + Excel assert via JSZip ganti exceljs fragile). Commits `24e0f6f2`/`781c2bf2`/`87e3ad7d`/`79dadd33`. **Plan 04 = 0 migration.** **Phase 399 Foundation LENGKAP → ready `/gsd-verify-work`.**
- [399-03 / multi-select widget SHIPPED]: `initSectionUnitMultiCascade(opts)` ditambah di `wwwroot/js/shared-cascade.js` (EXTEND — `initSectionUnitCascade`+`togglePassword` utuh) — render checkbox-list Unit `name="Units"` + radio `name="PrimaryUnit"` per baris client-side dari `ViewBag.SectionUnitsJson` (no AJAX, D-01). State machine **UI-SPEC §A 8-state WIRED**: Bagian kosong→placeholder; pilih Bagian→checkbox-list; centang→radio enabled; **default primary=first checked (D-02)**; uncheck primary→promote ke checked berikutnya; uncheck→radio disabled+clear; ganti Bagian→reset (invariant #1); HTML-escape nama unit (**T-399-03-04 mitigate**, bukan accept). Widget di **CreateWorker+EditWorker** (`#unitMultiContainer` role=group+aria-label; Bagian TETAP single `<select id=sectionSelect>`; idiom form + "Simpan" tak berubah, no inline font magic DSN-05). **MU-07 modal EditWorker** (`ViewBag.NeedConfirm`→modal "Konfirmasi Penghapusan {Unit}" + tombol "Ya, Hapus & Nonaktifkan" submit `form=editWorkerForm name=ConfirmedDeactivate value=true`; PROTON hard-block D-11 via validation-summary merah). **EditWorker GET pre-fill** `Model.Units`/`Model.PrimaryUnit` dari junction (**Rule 3 blocking round-trip**, view-binding necessity — no logic baru, tak sentuh write-through/guard 399-02). Spec `tests/e2e/multiunit-widget-399.spec.ts` 9 widget test test-first (data-driven `pickSectionWithUnits`, no hardcode Bagian) → **Playwright RUNTIME 9/9 hijau** (W-06 round-trip 2-unit Create→Edit + W-04 default-primary + W-05 promote + W-08 a11y; W-09 MU-07 skip fixture — logika server GREEN unit test 399-02). **build 0 error/0 warning**; app boot localhost:5277 HTTP 200; DB snapshot(setup)→RESTORE(teardown) baseline `UserUnits`=6 (worker round-trip temporary bersih, SEED_JOURNAL cleaned); suite 366/366. Scope display surfaces/`_PSign`/AccountController UTUH (= Plan 04). Commits `2a2767aa`/`b3756903`/`60aad1ab`/`03a775c6`. **Plan 03 = 0 migration.** **REQ MU-01/MU-02 done.**
- [399-02 / write-through SyncUserUnitsAsync SHIPPED]: kontrak write-through primary-mirror ter-implement di `WorkerController` sebagai PUBLIC STATIC helper testable (`SyncUserUnitsAsync(ctx,user,units,primary)` + `ParseUnitCell` + `ValidateUnitsInSection` + `EvaluateRemoveUnitGuardAsync` + `WorkerUnitsView` record) — pola `AdminBaseController.FindTitleDuplicatesAsync` (testable seam, no InternalsVisibleTo). Single-source junction `UserUnits` + mirror `ApplicationUser.Unit` (replace-set DELETE-before-INSERT, no window 2-primary) + audit set-diff (D-12, hapus anti-pattern `if user.Unit != model.Unit`). Wire Create/Edit/Import; **EditWorker 1-tx atomic** (`BeginTransactionAsync`: UpdateAsync + UserUnits + deactivate, **Open Q3**). **MU-07 asimetris**: PTA aktif → HARD-BLOCK (`protonUnit = AssignmentUnit ?? oldPrimary`, **Open Q1** kedua cabang + **Open Q2** kosong-semua-unit ter-cover) (D-11); coach-mapping aktif tanpa PTA → confirm (NeedConfirm re-prompt) → auto-deactivate IsActive/EndDate 1 tx (D-10). Validasi `Unit∈Bagian` + `primary∈set` tiap junction-write (MU-05, mass-assignment guard). Import **pipe** "UnitA|UnitB" first=primary dedup backward-compat (MU-04) + per-unit validasi; Export kolom 7 primary-first comma-join (D-08); template help-text + contoh pipe (D-06); ManageWorkers `ViewBag.UserUnitsDict` (untuk display Plan 04, unitFilter TETAP scalar = Phase 400 MU-06). **6 test logic Wave 0 GREEN (19 fakta)**; suite **366/366** (347+19, 0 skip, no regresi); build 0 error; app boot localhost:5277 HTTP 200; **round-trip 2-unit SQL lokal MATCH** (2 baris/1 IsPrimary/mirror==primary-row) + filtered-unique tolak 2nd primary + RESTORE baseline (SEED_JOURNAL cleaned). authz `[Authorize(Admin,HC)]` 12 + `[ValidateAntiForgeryToken]` 6 utuh; bind hanya Units/PrimaryUnit. Commits 862003b7/facc0df6/23fb5033/dadca0cc. **Plan 02 = 0 migration.** Interface siap Plan 03 (widget konsumsi Units/PrimaryUnit/SectionUnitsJson) + Plan 04 (display konsumsi UserUnitsDict/VM/PSign).
- [399-01 / junction UserUnits APPLIED]: tabel `UserUnits` ada di DB lokal (`HcPortalDB_Dev`) — filtered-unique `IX_UserUnits_UserId_PrimaryUnique` WHERE [IsPrimary]=1 (enforce invariant #3) + unique `IX_UserUnits_UserId_Unit_Unique` + backfill idempotent (6 baris IsPrimary=1 == 6 Users Unit-non-null; Unit-null=0 baris; re-run backfill=0 dobel). Migration scaffold via **global `dotnet ef` CLI v10.0.3** (backward-compat OK dengan project EF 8.0.0; snapshot tetap ProductVersion 8.0.0) — fallback pin-tool/hand-author tak perlu. Index UserId polos dibuang (EF dedup ke filtered-unique; has-pending-model-changes=none). `Users.Unit` scalar DIPERTAHANKAN (mirror). **migration=TRUE commit `fc015f4d`** (`AddUserUnitsTable`) — SATU-SATUNYA migration milestone v32.3, notify IT saat push. Interface siap untuk plan 02 (SyncUserUnitsAsync write-through) + plan 03/04 (UI/display). 7 test scaffold Wave 0 RED skip-with-reason (suite 347 pass/0 fail/16 skip).
- [v32.3 roadmap / phases 399-404]: 6 fase derived dari spec §5/§6 (mapping fase→REQ + dependency TERKUNCI, bukan re-derive) — **399** Foundation junction `UserUnits`+mirror+multi-select UI (MU-01/02/03/04/05/07, **migration=TRUE**), **400** listing set-aware+dedup (MU-06), **401** PROTON unit-resolution hardening (PSU-01/02/03/04/05/07), **402** coaching cross-unit (CXU-01..05), **403** Org cascade/guard UserUnits-aware (ORG-01/02), **404** test SQL riil+UAT+docs (QA-01..04). 24/24 REQ mapped, 0 orphan/duplicate. Dependency: 400/401/403→399; 402→401; 404→semua. Wave 1 {400,401,403} PARALEL (cluster file disjoint), 402 serial setelah 401. Critical path 399→401→402→404. Phase numbering mulai 399 (391-398 reserved di branch main: v32.0=391-392, v32.2=393-398).
- [v32.3 invariant (spec §7)]: (1) Section scalar 1 Bagian/akun; (2) PROTON single-active (index `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique` + E8 dipertahankan); (3) primary mirror `ApplicationUser.Unit`=baris `UserUnits.IsPrimary` write-through; (4) `AssignmentUnit ∈ coachee.UserUnits` (pasca-401); (5) `ProtonKompetensi.Unit` 1:1 per deliverable. Junction `UserUnits.Unit` = NAME-string (konsisten `AssignmentUnit`/`ProtonKompetensi.Unit`), validasi via `GetUnitsForSectionAsync(user.Section)`. D1=b cert/analytics atribusi primary (no kolom unit-at-issue, no migration ke-2). De-risk: authz Section 100% scalar → 0 perubahan.
- [v32.1 / 389-01 spec parity]: `tests/e2e/coachcoacheemapping-389.spec.ts` 14-test (V-01..V-14) test-first (Nyquist safeguard, Phase 354 lesson). Closed PASSED 7/7.
- [v31.0 Hotfix Pra-Ujian Lisensor / phases 385-387]: 14/14 PXF closed, 0 migration. Pattern: shared display helper `AssessmentScoreAggregator.IsQuestionCorrect`+`BuildAnswerCell` (kill-drift); essay PathBase-aware sub-path. MERGED→main 7ea6c81e, UAT Dev full-lifecycle PASS.
- [v30.0 / ECG]: helper terpusat `AssessmentScoreAggregator.IsQuestionCorrect` (essay `>0`=Benar) `CMPController.Results` 4 site + PDF (kill-drift).
- [v29.0 / 382]: SAVE-01 dedupe last-write-wins in-memory, NO migration.
- [v25.0 / A-4]: Penanda kelulusan Proton lewat 1 helper bersama `ProtonCompletionService` (Origin).
- [v24.0 / spec §9]: Hapus file gambar pola Phase 333/335 — kumpul path SEBELUM tx, File.Delete SETELAH commit, warn-only.
- [v24.0 / AF-1..7]: eligibility coachee per-unit (CoacheeEligibilityCalculator) — **relevan v32.3** (multi-unit memperluas eligibility ke lintas-unit dalam Bagian, fase 402).
- [v22.0 cross-milestone]: `AssessmentConstants.AssessmentStatus.PendingGrading` single source of truth label.
- [v21.0 / org labels]: tier label org (Bagian/Unit/Sub-unit) configurable via `IOrgLabelService` + global `@inject OrgLabels` (110 calls / 26 views) — **relevan v32.3** (display unit + multi-select Bagian/Unit pakai `@OrgLabels.GetLabel(0/1)`).
- [v13.0]: org tree `OrganizationUnit` self-FK `ParentId` (Level0=Bagian, Level1=Unit), user nyambung via Name-string bukan Id — **fondasi v32.3** (`UserUnits.Unit` NAME-string anak Bagian; `GetSectionUnitsDictAsync`/`GetUnitsForSectionAsync` primitif siap dipakai multi-select + validasi).
- [v12.0]: AdminController dipecah jadi 8 controller per domain; URL tetap via [Route]. (Worker/CoachMapping/CDP/Organization/AssessmentAdmin = controller terpisah → fondasi cluster file disjoint paralelisme Wave 1 v32.3.)

### Open Blockers/Concerns

- [push] Carry migration lama (`PendingProtonBypass`+index/360, `ShuffleToggles`/372) — notify IT flag. **v32.3 = migration BARU TRUE** (`AddUserUnitsTable` Phase 399 + backfill).
- [v32.3 risiko utama (spec §10)]: (a) `CleanupCoachCoacheeMappingOrg` reset `AssignmentUnit`→primary = data-loss multi-unit → Fase 401 jadikan UserUnits-aware/gated SEBELUM data multi-unit produksi; (b) reparent unit lintas-Bagian = split-brain Section → Fase 403 hard-block; (c) primary-mirror desync → kontrak write-through terpusat + test; (d) EF-InMemory tak enforce filtered-unique-index → test palsu hijau → Fase 404 WAJIB SQL riil SQLEXPRESS; (e) atribusi cert primary bikin cert unit-Y muncul di laporan unit-X → diterima (D1=b), didokumentasikan.
- [v32.0 sudah CLOSED]: v32.0 (Manajemen Peserta 391-392) di-close MANUAL non-destruktif 2026-06-19 di branch `main` (tag `v32.0` @`423a2e76` + archive `milestones/v32.0-*` + entry MILESTONES — di main). Dulu deferred krn `/gsd-complete-milestone v32.0` standar destruktif (file REQUIREMENTS/STATE/PROJECT live sudah pindah ke milestone lain) → diselesaikan manual via snapshot git. BUKAN pending close; sisa cuma ikut push deploy. Lihat MEMORY `project_v32_0_close_deferred`.

## Session Continuity

Last activity: 2026-06-23

Stopped at: Phase 423 context gathered

Next action: **`/gsd-discuss-phase 421`** (Retake Lifecycle Hardening; RTH-01..05; RTK-LOGIC-02 🔴 retake dead-end). Phase 420 done. Urutan eksekusi **sekuensial by file-overlap** 420 -> 421 -> 422 -> 423 -> 424 -> 425. Domain 420: `Views/Admin/CreateAssessment.cshtml`/`EditAssessment.cshtml` + `AssessmentAdminController` binding. migration=FALSE (420). ⚠️ migration TBD-confirm saat plan-phase 422 (SamePackage toggle editable) & 425 (CLN-03 drop `AssessmentPhase`). App port **5270** (branch ITHandoff; 5277 dipakai worktree main). NOT pushed (deploy bundle v32.1+v32.3+v32.4+v32.7). ⚠️ 422 (+420) overlap v32.6 (branch main, Section+ScopedShuffle) — rekonsiliasi saat merge, JANGAN duplikasi.
