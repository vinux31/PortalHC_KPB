# Codebase Concerns

> Auto-generated by codebase mapper — 2026-04-02

## Tech Debt

**Oversized Controllers:**
- Issue: Several controllers exceed 2000+ lines with dozens of action methods each, mixing business logic with HTTP concerns
- Files: `Controllers/CDPController.cs` (4013 lines, ~35 actions), `Controllers/AssessmentAdminController.cs` (3791 lines, ~37 actions), `Controllers/CMPController.cs` (2402 lines, ~26 actions), `Controllers/ProtonDataController.cs` (1607 lines)
- Impact: High cognitive load, difficult to test, merge conflict risk. A single controller handles CRUD for multiple unrelated entities.
- Fix approach: Extract business logic into service classes. The AdminController refactoring (now 108 lines, delegating to AdminBaseController + 7 sub-controllers) is the correct pattern. CDPController is the prime candidate — split Proton coaching, IDP, dashboard, and export logic into separate controllers or services.

**Near-Zero AsNoTracking Usage:**
- Issue: Only 1 `AsNoTracking()` call across all controllers despite hundreds of read-only queries
- Files: All controllers, especially `Controllers/CDPController.cs` (173+ `ToListAsync` calls), `Controllers/AssessmentAdminController.cs`
- Impact: Unnecessary EF Core change tracking overhead on read-only pages (dashboards, lists, exports). Increased memory pressure under load.
- Fix approach: Add `.AsNoTracking()` to all queries in GET actions that only display data. Keep tracking only on queries followed by `SaveChangesAsync`.

**Bare Catch Blocks:**
- Issue: Three bare `catch` blocks (no exception type) used for transaction rollback-and-rethrow
- Files: `Controllers/AssessmentAdminController.cs` lines 1093, 1462, 3607
- Impact: Low — these do `RollbackAsync()` then `throw`, so errors propagate correctly. But bare catch captures non-CLS-compliant exceptions and obscures intent.
- Fix approach: Replace `catch` with `catch (Exception)` for clarity. Extract the transaction-wrap-rollback pattern into a reusable helper method.

**Repetitive Audit Log Try-Catch Wrapping:**
- Issue: 20+ audit log calls are individually wrapped in identical try-catch blocks: `try { auditLog... } catch (Exception ex) { _logger.LogWarning(ex, "Audit log failed..."); }`
- Files: Throughout `Controllers/AdminBaseController.cs` subclasses and `Controllers/AssessmentAdminController.cs`
- Impact: Massive code duplication. Inconsistent handling if pattern is forgotten on new audit calls.
- Fix approach: Make `Services/AuditLogService.cs` (44 lines) internally fault-tolerant — catch and log within the service itself so callers never need try-catch wrappers.

**View Override Pattern Duplication:**
- Issue: 6 admin sub-controllers duplicate identical `View()` override boilerplate to redirect to `~/Views/Admin/`
- Files: `Controllers/AssessmentAdminController.cs:49-50`, `Controllers/CoachMappingController.cs:35-36`, `Controllers/DocumentAdminController.cs:29-30`, `Controllers/OrganizationController.cs:25-26`, `Controllers/RenewalController.cs:26`, `Controllers/TrainingAdminController.cs`, `Controllers/WorkerController.cs`
- Impact: Easy to forget when adding new sub-controllers
- Fix approach: Move the `View()` override into `Controllers/AdminBaseController.cs` so all sub-controllers inherit it.

**Stale Test Artifacts in Project Root:**
- Issue: 19+ UAT debug scripts, screenshots, and log files polluting the project root
- Files: `uat-265-*.js`, `uat-266-*.js`, `uat-267-*.js`, `uat-error-console.log`, `cookies.txt`, `server.log`, `277-uat-*.png`, `phase276-*.png`, `uat-testcase1-ui-table.png`, `uat-266-screenshots/`
- Impact: Pollutes repository, confuses developers, inflates repo size
- Fix approach: Delete all stale files. Add patterns to `.gitignore` (some `.png` and `.js` patterns already ignored). Move reusable test scripts to `tests/` directory.

**153 Database Migrations:**
- Issue: Large migration history accumulation
- Files: `Migrations/` directory (153 files)
- Impact: Slow `dotnet ef` operations, confusing history
- Fix approach: Low priority — squash migrations into a single baseline at next major version.

**Heavy ViewBag/TempData Usage:**
- Issue: Controllers pass data via ViewBag and TempData (magic strings, no compile-time safety)
- Files: All controllers in `Controllers/`
- Impact: Typos in ViewBag property names cause silent null values. No IntelliSense for view data.
- Fix approach: Low priority — migrate complex data passing to strongly-typed ViewModels. Current pattern works.

## Security Considerations

**CSRF Protection — Complete:**
- Risk: None. All 83 `[HttpPost]` actions have matching `[ValidateAntiForgeryToken]` attributes.
- Recommendations: Maintain this discipline. Consider `[AutoValidateAntiforgeryToken]` at controller level.

**Authorization — Consistently Applied:**
- Risk: Low. All controllers have class-level `[Authorize]` or role-based authorization. Individual actions add further role restrictions.
- Files: `Controllers/AdminBaseController.cs`, `Controllers/ProtonDataController.cs`, all sub-controllers
- Recommendations: Consider resource-level authorization (verifying users can only access their own unit's data, not just role checks).

**Raw Form Access:**
- Risk: Direct `Request.Form` access bypasses model binding validation
- Files: `Controllers/AssessmentAdminController.cs:2012`
- Current mitigation: Uses `int.TryParse` for type safety
- Recommendations: Replace with model binding where possible.

**Seed Data Passwords:**
- Risk: `Data/SeedData.cs` seeds development users with weak passwords. If seed runs in production, accounts are exploitable.
- Files: `Data/SeedData.cs`
- Recommendations: Gate seed data behind environment check (`if (env.IsDevelopment())`).

## Performance Bottlenecks

**Deep Include Chains Without Projection:**
- Problem: Queries chain 4+ levels of `.Include().ThenInclude()` loading entire object graphs
- Files: `Controllers/CDPController.cs:657-660` (4 levels), `Controllers/CDPController.cs:806-808`
- Cause: EF Core eager loading pulls entire related entity trees into memory
- Improvement path: Use projection (`Select`) to load only needed fields for list/dashboard views. Reserve deep includes for detail pages.

**No Server-Side Pagination:**
- Problem: List endpoints load all matching records with `ToListAsync()` without `Skip()`/`Take()`
- Files: Various controller list actions across all controllers
- Cause: All matching records loaded into memory; pagination handled client-side
- Improvement path: Implement server-side pagination using `Helpers/PaginationHelper.cs` (15 lines, already exists) consistently.

**SQLite Write Contention:**
- Problem: SQLite allows only one concurrent writer. Simultaneous POST requests queue.
- Files: `appsettings.json`, `Program.cs`
- Improvement path: Migrate to PostgreSQL/SQL Server for production. Short-term: minimize transaction duration.

## Fragile Areas

**CDPController — Proton Coaching Flow:**
- Files: `Controllers/CDPController.cs` (lines ~400-2800)
- Why fragile: Single 4000-line file handles coaching sessions, deliverables, evaluations, exports (Excel + PDF). Complex nested Include chains. Multiple coach/coachee permission checks inline.
- Safe modification: Test all coaching flows end-to-end after any change.
- Test coverage: Manual UAT only. 488 JS test scripts in `tests/` are ad-hoc, not a maintained suite.

**Assessment Lifecycle (Create/Edit/Grade/Certify):**
- Files: `Controllers/AssessmentAdminController.cs` (3791 lines), `Controllers/CMPController.cs` (exam flow), `Hubs/AssessmentHub.cs`
- Why fragile: Multi-step workflows with nested transactions, package shuffling algorithms, certificate number generation with duplicate-key retry loops. CreateAssessment spans ~200 lines with nested try-catch-finally.
- Safe modification: Always test full lifecycle. Do not remove `throw` statements in bare catch blocks (lines 1093, 1462, 3607).

**Certificate Number Generation:**
- Files: `Controllers/AssessmentAdminController.cs` (lines 1032, 2306, 2400), `Controllers/CMPController.cs` (line 1611), `Helpers/CertNumberHelper.cs`
- Why fragile: Uses retry loop on `DbUpdateException` for duplicate key conflicts. If retry logic fails after max attempts, operation throws.
- Safe modification: Ensure `maxCertAttempts` is sufficient for concurrent generation.

## Scaling Limits

**SQLite Database:**
- Current capacity: Adequate for < 50 concurrent users
- Limit: Write contention at ~10+ concurrent writers. Performance degrades past a few GB.
- Scaling path: Migrate to PostgreSQL or SQL Server (connection string + provider change).

**In-Memory Cache:**
- Current capacity: `AddDistributedMemoryCache()` in `Program.cs` — process-local only
- Limit: Cannot share across server instances
- Scaling path: Replace with Redis for multi-server deployment.

## Dependencies at Risk

**QuestPDF Licensing:**
- Risk: QuestPDF changed licensing (free for revenue < $1M, paid otherwise)
- Impact: PDF generation in `Controllers/CDPController.cs` (coaching reports)
- Migration plan: Monitor licensing. Alternative: iTextSharp or html-to-pdf.

## Missing Critical Features

**No Automated Test Suite:**
- Problem: Zero C# unit tests. E2E specs exist only in `.claude/worktrees/` branches, not committed to main.
- Blocks: All testing is manual UAT. Regressions only caught by human verification.

**No CI/CD Pipeline:**
- Problem: No GitHub Actions, Azure Pipelines, or other CI configuration detected.
- Blocks: No automated build verification, test runs, or deployment automation.

**No Rate Limiting:**
- Problem: No request rate limiting on authentication or data mutation endpoints.
- Blocks: Production hardening (noted in deferred v9.0 requirements).

**No Centralized Logging/Monitoring:**
- Problem: `ILogger` used consistently but no aggregation (Sentry, Application Insights) or health check endpoints.
- Blocks: Production debugging requires direct server access.

## Test Coverage Gaps

**Zero Unit Tests:**
- What's not tested: All business logic — assessment grading, certificate generation, approval chains, role-based filtering, package shuffling
- Files: `Controllers/`, `Services/`, `Helpers/`
- Risk: Any refactoring can introduce regressions undetected
- Priority: High — start with assessment grading, cert number generation, approval state transitions

**E2E Tests Not Merged:**
- What's not tested: E2E specs exist only in worktree branches
- Files: `tests/e2e/` (worktrees only), `tests/` (488 ad-hoc JS files on main)
- Risk: Tests drift from actual code
- Priority: Medium — merge to main and set up CI runner

**No Authorization Integration Tests:**
- What's not tested: IDOR (accessing other users' data via ID manipulation), role escalation
- Files: All controllers with user-specific data access
- Risk: Authorization is role-based but resource-level checks may have gaps
- Priority: Medium

---

*Concerns audit: 2026-04-02*
