---
phase: 304-ui-label-polish-login-wib
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/CreateAssessment.cshtml
autonomous: false
requirements:
  - WIZ-02
  - WIZ-03
user_setup: []

must_haves:
  truths:
    - "Step 3 wizard menampilkan helper text '(WIB)' di bawah setiap input date/time/datetime-local"
    - "Standard mode: 4 input (Tanggal Jadwal, Waktu Jadwal, Tanggal Tutup, Waktu Tutup) memiliki helper '(WIB)'"
    - "PrePost mode: 4 input datetime-local (PreSchedule, PreEWCD, PostSchedule, PostEWCD) memiliki helper '(WIB)'"
    - "Step 4 summary Standard: baris Jam Tutup menampilkan suffix ' WIB' (konsisten dengan Jam Mulai existing)"
    - "Step 4 summary PrePost: 4 baris datetime (pre-schedule, pre-ewcd, post-schedule, post-ewcd) menampilkan suffix ' WIB'"
    - "Existing helper text '1–480 menit (maks 8 jam)' (line 371, Durasi field) TIDAK diubah"
    - "DOM ID semua input dan summary span TIDAK berubah (model binding intact)"
    - "Tidak ada regresi: wizard 4-step tetap berfungsi, validation tetap jalan, submit Standard & PrePost tetap berhasil"
  artifacts:
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "8 helper text WIB + 5 JS suffix WIB"
      contains: "form-text text-muted"
  key_links:
    - from: "<input type='date'> (line 358, 378)"
      to: "<div class='form-text text-muted'>Tanggal (WIB)</div>"
      via: "Razor markup sibling element"
      pattern: "form-text text-muted.>Tanggal \\(WIB\\)"
    - from: "<input type='time'> (line 363, 385)"
      to: "<div class='form-text text-muted'>Waktu (WIB)</div>"
      via: "Razor markup sibling element"
      pattern: "form-text text-muted.>Waktu \\(WIB\\)"
    - from: "<input type='datetime-local'> (line 405, 413, 426, 434)"
      to: "<div class='form-text text-muted'>Tanggal & Waktu (WIB)</div>"
      via: "Razor markup sibling element"
      pattern: "form-text text-muted.>Tanggal &amp; Waktu \\(WIB\\)"
    - from: "populateSummary() JS lines 1126, 1130, 1132, 1136, 1177"
      to: "DOM textContent dengan suffix ' WIB'"
      via: "string concatenation `+ ' WIB'`"
      pattern: "\\+ ' WIB'"
---

<objective>
Menambahkan label "(WIB)" eksplisit di seluruh input date/time/datetime-local pada Step 3 wizard `CreateAssessment.cshtml` (WIZ-02, 8 input), dan suffix " WIB" di Step 4 summary JS (WIZ-03, 5 lokasi). Pattern helper text mengikuti precedent existing di `EditAssessment.cshtml`. JS suffix mengikuti precedent existing di line 1164 file yang sama.

Purpose: Memenuhi audit findings 5 & 6 — eliminasi ambiguitas zona waktu untuk admin/HC yang membuat assessment. Konsisten dengan EditAssessment yang sudah punya pattern ini.

Output: `Views/Admin/CreateAssessment.cshtml` dimodifikasi (8 `<div class="form-text text-muted">…</div>` baru di Step 3 + 5 string concatenation `+ ' WIB'` di JS Step 4). Tidak ada file baru, tidak ada dependency baru.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-ui-label-polish-login-wib/304-CONTEXT.md
@.planning/phases/304-ui-label-polish-login-wib/304-PATTERNS.md
@Views/Admin/CreateAssessment.cshtml
@Views/Admin/EditAssessment.cshtml

<interfaces>
<!-- Existing markup (Step 3) di CreateAssessment.cshtml lines 355-444 yang akan dimodifikasi.   -->
<!-- HARUS dipertahankan strukturnya (DOM ID, name attribute) per D-18 D-19.                    -->
<!-- Source: Views/Admin/CreateAssessment.cshtml                                                 -->

```html
<!-- Standard mode (lines 355-388) -->
<input type="date" id="schedDateInput" name="ScheduleDate" class="form-control" />        <!-- line 358 -->
<input type="time" id="schedTimeInput" name="ScheduleTime" class="form-control" value="08:00" />  <!-- line 363 -->
<input type="date" id="ewcdDateInput" class="form-control" />                              <!-- line 378 -->
<input type="time" id="ewcdTimeInput" class="form-control" value="23:59" />               <!-- line 385 -->

<!-- PrePost mode (lines 396-447) -->
<input type="datetime-local" name="PreSchedule" id="preSchedule" class="form-control" />                       <!-- line 405 -->
<input type="datetime-local" name="PreExamWindowCloseDate" id="preExamWindowCloseDate" class="form-control" /> <!-- line 413 -->
<input type="datetime-local" name="PostSchedule" id="postSchedule" class="form-control" />                     <!-- line 426 -->
<input type="datetime-local" name="PostExamWindowCloseDate" id="postExamWindowCloseDate" class="form-control"/><!-- line 434 -->
```

Pattern reference (HARUS REPLICATE PERSIS) dari `Views/Admin/EditAssessment.cshtml`:
```html
<div class="form-text text-muted">Tanggal (WIB)</div>     <!-- untuk type=date -->
<div class="form-text text-muted">Waktu (WIB)</div>       <!-- untuk type=time -->
<div class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>  <!-- untuk type=datetime-local -->
```

Existing JS precedent (line 1164 — TIDAK DIUBAH, hanya dijadikan referensi konsistensi):
```javascript
if (summSchedule) summSchedule.textContent = schedVal ? (schedVal + ' ' + timeVal + ' WIB') : '-';
```

DOM ID kontrak (TIDAK BOLEH berubah — model binding & JS handler bergantung):
- Inputs: `schedDateInput`, `schedTimeInput`, `ewcdDateInput`, `ewcdTimeInput`, `preSchedule`, `preExamWindowCloseDate`, `postSchedule`, `postExamWindowCloseDate`
- Summary spans: `summary-schedule`, `summary-ewcd`, `summary-pre-schedule`, `summary-pre-ewcd`, `summary-post-schedule`, `summary-post-ewcd`
- Hidden combiners: `schedHidden`, `ewcdHidden`
- Razor tag helper: `asp-for="Schedule"`, `asp-for="ExamWindowCloseDate"`, `asp-for="DurationMinutes"`

Existing helper text yang TIDAK boleh diubah: line 371 `<div class="form-text text-muted">1–480 menit (maks 8 jam)</div>` (Durasi field).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tambah 4 helper text "(WIB)" di Standard mode Step 3 (lines 358, 363, 378, 385)</name>

  <read_first>
    - `Views/Admin/CreateAssessment.cshtml` lines 350-395 (Standard mode date/time block)
    - `Views/Admin/EditAssessment.cshtml` lines 360-366 (pattern reference Standard mode date+time pair)
    - `.planning/phases/304-ui-label-polish-login-wib/304-CONTEXT.md` §"WIZ-02" (D-10 D-11 D-12)
    - `.planning/phases/304-ui-label-polish-login-wib/304-PATTERNS.md` §"Apply ke CreateAssessment 8 input"
  </read_first>

  <files>Views/Admin/CreateAssessment.cshtml</files>

  <action>
Sisipkan 4 helper text `<div class="form-text text-muted">…</div>` di Standard mode. Posisi: **SETELAH `<input>`** dan **SEBELUM `<div class="invalid-feedback">`** existing (per 304-PATTERNS.md — invalid-feedback tetap behave normal di state `:invalid`).

**Edit 1 — Tanggal Jadwal (line 358):**

Old (lines 357-359):
```html
                                        <label class="form-label fw-bold">Tanggal Jadwal <span class="text-danger">*</span></label>
                                        <input type="date" id="schedDateInput" name="ScheduleDate" class="form-control" />
                                        <div class="invalid-feedback" id="schedDateError">Tanggal jadwal wajib diisi.</div>
```

New (sisipkan helper text persis di antara input dan invalid-feedback):
```html
                                        <label class="form-label fw-bold">Tanggal Jadwal <span class="text-danger">*</span></label>
                                        <input type="date" id="schedDateInput" name="ScheduleDate" class="form-control" />
                                        <div class="form-text text-muted">Tanggal (WIB)</div>
                                        <div class="invalid-feedback" id="schedDateError">Tanggal jadwal wajib diisi.</div>
```

**Edit 2 — Waktu Jadwal (line 363):**

Old (lines 362-364):
```html
                                        <label class="form-label fw-bold">Waktu Jadwal <span class="text-danger">*</span></label>
                                        <input type="time" id="schedTimeInput" name="ScheduleTime" class="form-control" value="08:00" />
                                        <div class="invalid-feedback" id="schedTimeError">Waktu jadwal wajib diisi.</div>
```

New:
```html
                                        <label class="form-label fw-bold">Waktu Jadwal <span class="text-danger">*</span></label>
                                        <input type="time" id="schedTimeInput" name="ScheduleTime" class="form-control" value="08:00" />
                                        <div class="form-text text-muted">Waktu (WIB)</div>
                                        <div class="invalid-feedback" id="schedTimeError">Waktu jadwal wajib diisi.</div>
```

**Edit 3 — Tanggal Tutup Ujian (line 378):**

Old (lines 374-380):
```html
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold">
                                            <i class="bi bi-calendar-x text-danger me-1"></i>Tanggal Tutup Ujian <span class="text-danger">*</span>
                                        </label>
                                        <input type="date" id="ewcdDateInput" class="form-control" />
                                        <div class="invalid-feedback" id="ewcdDateError">Tanggal tutup ujian wajib diisi.</div>
                                    </div>
```

New:
```html
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold">
                                            <i class="bi bi-calendar-x text-danger me-1"></i>Tanggal Tutup Ujian <span class="text-danger">*</span>
                                        </label>
                                        <input type="date" id="ewcdDateInput" class="form-control" />
                                        <div class="form-text text-muted">Tanggal (WIB)</div>
                                        <div class="invalid-feedback" id="ewcdDateError">Tanggal tutup ujian wajib diisi.</div>
                                    </div>
```

**Edit 4 — Waktu Tutup Ujian (line 385):**

Old (lines 381-387):
```html
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold">
                                            <i class="bi bi-clock text-danger me-1"></i>Waktu Tutup Ujian <span class="text-danger">*</span>
                                        </label>
                                        <input type="time" id="ewcdTimeInput" class="form-control" value="23:59" />
                                        <div class="invalid-feedback" id="ewcdTimeError">Waktu tutup ujian wajib diisi.</div>
                                    </div>
```

New:
```html
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold">
                                            <i class="bi bi-clock text-danger me-1"></i>Waktu Tutup Ujian <span class="text-danger">*</span>
                                        </label>
                                        <input type="time" id="ewcdTimeInput" class="form-control" value="23:59" />
                                        <div class="form-text text-muted">Waktu (WIB)</div>
                                        <div class="invalid-feedback" id="ewcdTimeError">Waktu tutup ujian wajib diisi.</div>
                                    </div>
```

**Yang TIDAK boleh diubah** (per D-18 D-19):
- DOM ID `schedDateInput`, `schedTimeInput`, `ewcdDateInput`, `ewcdTimeInput` TIDAK berubah
- `name="ScheduleDate"`, `name="ScheduleTime"` TIDAK berubah (Standard mode binding)
- Hidden combiner `schedHidden`, `ewcdHidden` (lines 392-393) TIDAK diubah
- Existing line 371 `<div class="form-text text-muted">1–480 menit (maks 8 jam)</div>` (Durasi field) TIDAK diubah
- `invalid-feedback` div TIDAK diubah, hanya helper text disisipkan SEBELUMnya
- `asp-for="DurationMinutes"` (line 367, 370) TIDAK diubah

Pakai `Edit` tool 4x (atau MultiEdit jika tersedia) — old_string match exact context (label + input + invalid-feedback) untuk setiap edit, new_string sisipkan helper text di antara input dan invalid-feedback.
  </action>

  <verify>
    <automated>grep -c 'class="form-text text-muted">Tanggal (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"</automated>
    <!-- Expected: 2 (Tanggal Jadwal + Tanggal Tutup Ujian) -->
  </verify>

  <acceptance_criteria>
    - `grep -c 'class="form-text text-muted">Tanggal (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"` returns `2` (2 input type=date Standard)
    - `grep -c 'class="form-text text-muted">Waktu (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"` returns `2` (2 input type=time Standard)
    - `grep -c 'id="schedDateInput"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'id="schedTimeInput"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'id="ewcdDateInput"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'id="ewcdTimeInput"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'name="ScheduleDate"' "Views/Admin/CreateAssessment.cshtml"` returns `1` (binding intact)
    - `grep -c 'name="ScheduleTime"' "Views/Admin/CreateAssessment.cshtml"` returns `1` (binding intact)
    - `grep -c '1–480 menit (maks 8 jam)' "Views/Admin/CreateAssessment.cshtml"` returns `1` (existing helper Durasi TIDAK berubah)
  </acceptance_criteria>

  <done>
    - 4 helper text WIB ditambahkan di Standard mode (Tanggal Jadwal, Waktu Jadwal, Tanggal Tutup Ujian, Waktu Tutup Ujian)
    - Posisi: setelah `<input>` dan sebelum `<div class="invalid-feedback">`
    - DOM ID, name attribute, dan hidden combiner intact
    - Existing helper text Durasi (line 371) tidak terganggu
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tambah 4 helper text "(WIB)" di PrePost mode Step 3 (lines 405, 413, 426, 434)</name>

  <read_first>
    - `Views/Admin/CreateAssessment.cshtml` lines 395-450 (PrePost mode datetime-local block)
    - `Views/Admin/EditAssessment.cshtml` lines 241-247, 299-305 (pattern reference PrePost datetime-local jika ada — fallback ke pattern Tanggal+Waktu pair yang sama struktur)
    - `.planning/phases/304-ui-label-polish-login-wib/304-CONTEXT.md` §"WIZ-02" (D-11 — 4 entry datetime-local: 405, 413, 426, 434)
    - `.planning/phases/304-ui-label-polish-login-wib/304-PATTERNS.md` §"Apply ke CreateAssessment 8 input" (4 baris dengan helper "Tanggal & Waktu (WIB)")
  </read_first>

  <files>Views/Admin/CreateAssessment.cshtml</files>

  <action>
Sisipkan 4 helper text `<div class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>` di PrePost mode (lines 405, 413, 426, 434). Posisi: **SETELAH `<input type="datetime-local" ...>`** dan **SEBELUM `</div>`** penutup `col-md-4`.

**Catatan:** Di PrePost section TIDAK ada `<div class="invalid-feedback">` di sekitar setiap input datetime-local (validation handled oleh JS handler atau jQuery validate generik). Helper text disisipkan langsung setelah input.

**Edit 1 — PreSchedule (line 405):**

Old (lines 403-406):
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Tanggal &amp; Waktu <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PreSchedule" id="preSchedule" class="form-control" />
                                            </div>
```

New:
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Tanggal &amp; Waktu <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PreSchedule" id="preSchedule" class="form-control" />
                                                <div class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>
                                            </div>
```

**Edit 2 — PreExamWindowCloseDate (line 413):**

Old (lines 411-414):
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Batas Waktu Pengerjaan <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PreExamWindowCloseDate" id="preExamWindowCloseDate" class="form-control" />
                                            </div>
```

New:
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Batas Waktu Pengerjaan <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PreExamWindowCloseDate" id="preExamWindowCloseDate" class="form-control" />
                                                <div class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>
                                            </div>
```

**Edit 3 — PostSchedule (line 426):**

Old (lines 424-427):
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Tanggal &amp; Waktu <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PostSchedule" id="postSchedule" class="form-control" />
                                            </div>
```

New:
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Tanggal &amp; Waktu <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PostSchedule" id="postSchedule" class="form-control" />
                                                <div class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>
                                            </div>
```

**Edit 4 — PostExamWindowCloseDate (line 434):**

Old (lines 432-435):
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Batas Waktu Pengerjaan <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PostExamWindowCloseDate" id="postExamWindowCloseDate" class="form-control" />
                                            </div>
```

New:
```html
                                            <div class="col-md-4">
                                                <label class="form-label small fw-semibold text-muted">Batas Waktu Pengerjaan <span class="text-danger">*</span></label>
                                                <input type="datetime-local" name="PostExamWindowCloseDate" id="postExamWindowCloseDate" class="form-control" />
                                                <div class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>
                                            </div>
```

**Penting tentang HTML entity:** Pakai `&amp;` (HTML entity) untuk ampersand di Razor view, BUKAN `&` literal. Pattern existing di label (`Tanggal &amp; Waktu`) sudah pakai entity — replikasi format yang sama untuk konsistensi.

**Yang TIDAK boleh diubah** (per D-18 D-19):
- DOM ID `preSchedule`, `preExamWindowCloseDate`, `postSchedule`, `postExamWindowCloseDate` TIDAK berubah
- `name="PreSchedule"`, `name="PreExamWindowCloseDate"`, `name="PostSchedule"`, `name="PostExamWindowCloseDate"` TIDAK berubah
- Field `id="preDurationMinutes"` (line 409) dan `id="postDurationMinutes"` (line 430) TIDAK ditambah helper text (per D-11 — hanya 8 input date/time/datetime-local; durasi out of scope)
- Checkbox `samePackageCheck` (line 439) dan badge (line 442-444) TIDAK diubah
- Card header & label existing TIDAK diubah

Pakai `Edit` tool 4x — old_string match exact context untuk setiap edit, new_string sisipkan helper text sebelum `</div>` penutup col-md-4.
  </action>

  <verify>
    <automated>grep -c 'class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"</automated>
    <!-- Expected: 4 (preSchedule, preEWCD, postSchedule, postEWCD) -->
  </verify>

  <acceptance_criteria>
    - `grep -c 'class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"` returns `4` (4 input datetime-local PrePost)
    - `grep -c 'id="preSchedule"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'id="preExamWindowCloseDate"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'id="postSchedule"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'id="postExamWindowCloseDate"' "Views/Admin/CreateAssessment.cshtml"` returns `>=1` (DOM ID intact)
    - `grep -c 'name="PreSchedule"' "Views/Admin/CreateAssessment.cshtml"` returns `1` (binding intact)
    - `grep -c 'name="PreExamWindowCloseDate"' "Views/Admin/CreateAssessment.cshtml"` returns `1` (binding intact)
    - `grep -c 'name="PostSchedule"' "Views/Admin/CreateAssessment.cshtml"` returns `1` (binding intact)
    - `grep -c 'name="PostExamWindowCloseDate"' "Views/Admin/CreateAssessment.cshtml"` returns `1` (binding intact)
    - **Combined audit (Task 1 + Task 2):** `grep -c 'class="form-text text-muted">' "Views/Admin/CreateAssessment.cshtml"` returns `9` (4 dari Task 1 + 4 dari Task 2 + 1 existing line 371 Durasi)
  </acceptance_criteria>

  <done>
    - 4 helper text "Tanggal &amp; Waktu (WIB)" ditambahkan di PrePost mode (preSchedule, preEWCD, postSchedule, postEWCD)
    - Posisi: setelah `<input type="datetime-local">` dan sebelum `</div>` penutup col-md-4
    - HTML entity `&amp;` digunakan (bukan `&` literal) — konsisten dengan label existing
    - DOM ID & name attribute intact
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tambah suffix " WIB" di 5 lokasi populateSummary JS Step 4 (lines 1126, 1130, 1132, 1136, 1177)</name>

  <read_first>
    - `Views/Admin/CreateAssessment.cshtml` lines 1110-1192 (populateSummary function lengkap)
    - `.planning/phases/304-ui-label-polish-login-wib/304-CONTEXT.md` §"WIZ-03" (D-13 D-14 D-15)
    - `.planning/phases/304-ui-label-polish-login-wib/304-PATTERNS.md` §"Analog 2 (JS suffix WIB)" (precedent line 1164)
  </read_first>

  <files>Views/Admin/CreateAssessment.cshtml</files>

  <action>
Tambah suffix `+ ' WIB'` di 5 baris JS dalam `populateSummary` function. Pattern ini mengikuti precedent existing line 1164 yang sudah pakai format `(schedVal + ' ' + timeVal + ' WIB')`.

**Edit 1 — line 1126 (summary-pre-schedule, PrePost mode):**

Old:
```javascript
            if (el) el.textContent = preSched && preSched.value ? preSched.value.replace('T', ' ') : '-';
```

New:
```javascript
            if (el) el.textContent = preSched && preSched.value ? preSched.value.replace('T', ' ') + ' WIB' : '-';
```

**Edit 2 — line 1130 (summary-pre-ewcd, PrePost mode):**

Old:
```javascript
            if (el) el.textContent = preEwcd && preEwcd.value ? preEwcd.value.replace('T', ' ') : '-';
```

New:
```javascript
            if (el) el.textContent = preEwcd && preEwcd.value ? preEwcd.value.replace('T', ' ') + ' WIB' : '-';
```

**Edit 3 — line 1132 (summary-post-schedule, PrePost mode):**

Old:
```javascript
            if (el) el.textContent = postSched && postSched.value ? postSched.value.replace('T', ' ') : '-';
```

New:
```javascript
            if (el) el.textContent = postSched && postSched.value ? postSched.value.replace('T', ' ') + ' WIB' : '-';
```

**Edit 4 — line 1136 (summary-post-ewcd, PrePost mode):**

Old:
```javascript
            if (el) el.textContent = postEwcd && postEwcd.value ? postEwcd.value.replace('T', ' ') : '-';
```

New:
```javascript
            if (el) el.textContent = postEwcd && postEwcd.value ? postEwcd.value.replace('T', ' ') + ' WIB' : '-';
```

**Edit 5 — line 1177 (summary-ewcd, Standard mode):**

Old:
```javascript
            if (summEwcd) summEwcd.textContent = (ewcdDateEl && ewcdDateEl.value) ? (ewcdDateEl.value + ' ' + (ewcdTimeEl ? ewcdTimeEl.value : '23:59')) : '-';
```

New:
```javascript
            if (summEwcd) summEwcd.textContent = (ewcdDateEl && ewcdDateEl.value) ? (ewcdDateEl.value + ' ' + (ewcdTimeEl ? ewcdTimeEl.value : '23:59') + ' WIB') : '-';
```

**Catatan posisi `+ ' WIB'` di Edit 5:** Dimasukkan **di dalam** parentheses ternary `( ... )` (yaitu sebelum `:`), agar suffix hanya muncul ketika date ada. Saat date kosong, `'-'` ditampilkan tanpa suffix. Ini konsisten dengan logic line 1164 existing.

**Yang TIDAK boleh diubah** (per D-13 D-14 D-15 D-18):
- DOM ID summary span (`summary-pre-schedule`, `summary-pre-ewcd`, `summary-post-schedule`, `summary-post-ewcd`, `summary-ewcd`, `summary-schedule`) TIDAK berubah
- Variable JS (`preSched`, `preEwcd`, `postSched`, `postEwcd`, `ewcdDateEl`, `ewcdTimeEl`) TIDAK direname
- Line 1164 (`summary-schedule`) yang sudah punya `+ ' WIB'` TIDAK diubah (sudah benar, jadi precedent)
- Line 1128 (`summary-pre-duration`), 1134 (`summary-post-duration`), 1168 (`summary-duration`) — Durasi dengan suffix ' menit' — TIDAK diubah (out of scope per D-11/D-13)
- Line 1141, 1145, 1149, 1153 (token, pass, samepackage, validuntil) TIDAK diubah
- Tidak parse ke Date object (per D-14 — minimal change, pakai `replace('T', ' ')` existing)
- Tidak extract function ke external JS (per D-15 — edit inline)

Pakai `Edit` tool 5x — old_string match exact line per edit (gunakan context unik agar tidak ambiguous; jika perlu sertakan baris atas/bawah).
  </action>

  <verify>
    <automated>grep -c "+ ' WIB'" "Views/Admin/CreateAssessment.cshtml"</automated>
    <!-- Expected: 5 (5 lokasi baru). Line 1164 pakai format ' WIB' inside string concat dengan format slightly berbeda — verify via individual greps below. -->
  </verify>

  <acceptance_criteria>
    - `grep -c "+ ' WIB'" "Views/Admin/CreateAssessment.cshtml"` returns `5` (5 lokasi baru: 1126, 1130, 1132, 1136, 1177)
    - `grep -c "' WIB'" "Views/Admin/CreateAssessment.cshtml"` returns `>=6` (5 baru + 1 existing line 1164 yang pakai `' WIB'` di akhir string concat)
    - `grep -n "preSched.value.replace('T', ' ') + ' WIB'" "Views/Admin/CreateAssessment.cshtml"` matches exactly 1 line (line 1126 setelah edit)
    - `grep -n "preEwcd.value.replace('T', ' ') + ' WIB'" "Views/Admin/CreateAssessment.cshtml"` matches exactly 1 line (line 1130)
    - `grep -n "postSched.value.replace('T', ' ') + ' WIB'" "Views/Admin/CreateAssessment.cshtml"` matches exactly 1 line (line 1132)
    - `grep -n "postEwcd.value.replace('T', ' ') + ' WIB'" "Views/Admin/CreateAssessment.cshtml"` matches exactly 1 line (line 1136)
    - `grep -c "ewcdTimeEl.value : '23:59') + ' WIB'" "Views/Admin/CreateAssessment.cshtml"` returns `1` (line 1177 dengan suffix di-inside ternary)
    - `grep -c "schedVal + ' ' + timeVal + ' WIB'" "Views/Admin/CreateAssessment.cshtml"` returns `1` (line 1164 existing TIDAK BERUBAH — precedent)
    - DOM ID intact: `grep -c 'id="summary-pre-schedule"\|id="summary-pre-ewcd"\|id="summary-post-schedule"\|id="summary-post-ewcd"\|id="summary-ewcd"\|id="summary-schedule"' "Views/Admin/CreateAssessment.cshtml"` returns `>=6`
  </acceptance_criteria>

  <done>
    - 5 lokasi JS populateSummary memiliki suffix `+ ' WIB'` (lines 1126, 1130, 1132, 1136, 1177)
    - Line 1164 (Standard summary-schedule, "Jam Mulai") tidak berubah dan tetap menjadi precedent format
    - Empty value tetap menampilkan `'-'` tanpa suffix WIB (logic ternary intact)
    - DOM ID summary span tidak berubah
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Build verification + grep audit gabungan</name>

  <read_first>
    - `Views/Admin/CreateAssessment.cshtml` (state final setelah Task 1, 2, 3)
  </read_first>

  <files>(no modifications — verification only)</files>

  <action>
Jalankan build dan combined grep audit untuk memverifikasi semua 8 helper text + 5 JS suffix berada di posisi yang benar dan tidak ada regresi pada DOM contract.

**Step 1 — Build:**
```bash
dotnet build -c Debug --nologo --verbosity minimal
```
Expected: exit 0, tidak ada error/warning baru pada `CreateAssessment.cshtml`.

**Step 2 — Combined grep audit (Step 3 helper text):**
```bash
# Total helper text WIB (Tanggal/Waktu/Tanggal & Waktu) — expect 8
grep -c 'form-text text-muted">Tanggal (WIB)</div>\|form-text text-muted">Waktu (WIB)</div>\|form-text text-muted">Tanggal &amp; Waktu (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"

# Existing helper Durasi (line 371) tetap — expect 1
grep -c '1–480 menit (maks 8 jam)' "Views/Admin/CreateAssessment.cshtml"

# Total form-text text-muted — expect 9 (8 baru + 1 existing Durasi)
grep -c 'class="form-text text-muted">' "Views/Admin/CreateAssessment.cshtml"
```

**Step 3 — Combined grep audit (Step 4 JS suffix):**
```bash
# 5 lokasi baru dengan suffix ' WIB' inline
grep -c "+ ' WIB'" "Views/Admin/CreateAssessment.cshtml"          # = 5
# Total ' WIB' termasuk precedent line 1164
grep -c "' WIB'" "Views/Admin/CreateAssessment.cshtml"            # >= 6
```

**Step 4 — DOM contract regression check:**
```bash
# Semua DOM ID input Step 3 intact
grep -c 'id="schedDateInput"\|id="schedTimeInput"\|id="ewcdDateInput"\|id="ewcdTimeInput"\|id="preSchedule"\|id="preExamWindowCloseDate"\|id="postSchedule"\|id="postExamWindowCloseDate"' "Views/Admin/CreateAssessment.cshtml"
# Expected: >= 8 (1 per ID)

# Semua DOM ID summary span Step 4 intact
grep -c 'id="summary-pre-schedule"\|id="summary-pre-ewcd"\|id="summary-post-schedule"\|id="summary-post-ewcd"\|id="summary-ewcd"\|id="summary-schedule"' "Views/Admin/CreateAssessment.cshtml"
# Expected: >= 6
```

**Output yang wajib dilaporkan ke human verifier (Task 5):**
- Build exit code 0
- 9 form-text text-muted (8 baru + 1 existing Durasi) — list count breakdown
- 5 occurrence `+ ' WIB'` di JS
- Semua DOM ID intact
- Lokasi file modified: `Views/Admin/CreateAssessment.cshtml`
  </action>

  <verify>
    <automated>dotnet build -c Debug --nologo --verbosity minimal && grep -c 'class="form-text text-muted">' "Views/Admin/CreateAssessment.cshtml"</automated>
    <!-- Expected: build exit 0, grep returns 9 -->
  </verify>

  <acceptance_criteria>
    - `dotnet build -c Debug --nologo --verbosity minimal` exit code = 0
    - `grep -c 'class="form-text text-muted">' "Views/Admin/CreateAssessment.cshtml"` returns `9` (4 Standard helpers + 4 PrePost helpers + 1 existing Durasi)
    - `grep -c "+ ' WIB'" "Views/Admin/CreateAssessment.cshtml"` returns `5` (5 lokasi JS suffix)
    - `grep -c "' WIB'" "Views/Admin/CreateAssessment.cshtml"` returns `>=6` (5 baru + 1 precedent line 1164)
    - `grep -c '1–480 menit (maks 8 jam)' "Views/Admin/CreateAssessment.cshtml"` returns `1` (Durasi helper TIDAK terganggu)
    - File `Views/Admin/CreateAssessment.cshtml` di-track di git status sebagai `modified`
    - Tidak ada error/warning Razor baru terkait file ini
  </acceptance_criteria>

  <done>
    - Build sukses, view Razor compile tanpa error
    - 9 form-text text-muted total (8 WIB baru + 1 Durasi existing) terkonfirmasi
    - 5 JS suffix `' WIB'` baru terkonfirmasi (line 1164 existing tetap)
    - Semua DOM ID kontrak intact
    - File CreateAssessment.cshtml siap untuk human verification (Task 5)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification — wizard CreateAssessment Step 3 & Step 4</name>

  <files>Views/Admin/CreateAssessment.cshtml (verification target — no modifications)</files>

  <read_first>
    - `Views/Admin/CreateAssessment.cshtml` (state final setelah Task 1, 2, 3, 4)
    - `.planning/phases/304-ui-label-polish-login-wib/304-CONTEXT.md` §"WIZ-02" §"WIZ-03" (D-10 sampai D-19 sebagai checklist functional)
  </read_first>

  <what-built>
    - **Step 3 Standard mode:** 4 input (Tanggal Jadwal, Waktu Jadwal, Tanggal Tutup Ujian, Waktu Tutup Ujian) menampilkan helper text "(WIB)" di bawahnya
    - **Step 3 PrePost mode:** 4 input datetime-local (PreSchedule, PreEWCD, PostSchedule, PostEWCD) menampilkan helper text "Tanggal & Waktu (WIB)"
    - **Step 4 summary Standard:** "Jam Tutup" menampilkan suffix " WIB" (konsisten dengan "Jam Mulai" yang sudah ada)
    - **Step 4 summary PrePost:** Pre Schedule, Pre EWCD, Post Schedule, Post EWCD menampilkan suffix " WIB"
    - Existing helper Durasi "1–480 menit (maks 8 jam)" TIDAK terganggu
    - Submit Standard & PrePost tetap berfungsi (DOM ID intact, validation tetap jalan)
  </what-built>

  <action>
Pause execution dan tampilkan instructions kepada human verifier. Tunggu user mengetik "approved" atau melaporkan masalah. Jangan lanjutkan task berikutnya sampai resume signal diterima.

Ikuti 12 langkah verifikasi di bawah secara berurutan, laporkan hasil tiap langkah ke user.
  </action>

  <how-to-verify>
1. Jalankan aplikasi: `dotnet run` atau via IIS Express / Visual Studio Run.
2. Login sebagai admin/HC, buka `/Admin/CreateAssessment` (Step 1).

3. **Step 1 → Step 2 → Step 3 — navigate ke wizard step 3:**
   - Pilih kategori, paket soal, peserta sampai bisa lanjut ke "Jadwal & Waktu".

4. **Step 3 Standard mode visual check (default mode):**
   - "Tanggal Jadwal" input → di bawahnya helper kecil abu-abu "Tanggal (WIB)"
   - "Waktu Jadwal" input → helper "Waktu (WIB)"
   - "Durasi (Menit)" → helper "1–480 menit (maks 8 jam)" (existing TETAP, jangan terganggu)
   - "Tanggal Tutup Ujian" → helper "Tanggal (WIB)"
   - "Waktu Tutup Ujian" → helper "Waktu (WIB)"
   - Total: 4 helper "(WIB)" + 1 helper Durasi terlihat

5. **Step 3 PrePost mode visual check (toggle ke Pre-Post Test):**
   - Pilih "Pre-Post Test" di field tipe (sesuai struktur wizard existing)
   - Standard section hide, PrePost section show
   - Card "Jadwal Pre-Test":
     - "Tanggal & Waktu" → helper "Tanggal & Waktu (WIB)"
     - "Durasi (menit)" → no new helper (out of scope)
     - "Batas Waktu Pengerjaan" → helper "Tanggal & Waktu (WIB)"
   - Card "Jadwal Post-Test": sama pattern (3 field, 2 helper WIB)
   - Total PrePost: 4 helper "Tanggal & Waktu (WIB)"

6. **Step 4 Standard mode summary check:**
   - Isi field date/time Standard, lanjut ke Step 4
   - "Jam Mulai" → tampil "{tanggal} {waktu} WIB" (existing — TIDAK BERUBAH)
   - "Jam Tutup" → tampil "{tanggal} {waktu} WIB" (BARU — sebelumnya tanpa WIB)
   - Konsistensi visual: keduanya berakhir dengan "WIB"

7. **Step 4 PrePost mode summary check:**
   - Toggle ke Pre-Post Test, isi 4 field datetime-local Pre/Post, lanjut ke Step 4
   - Pre Schedule → "{YYYY-MM-DD} {HH:mm} WIB"
   - Pre EWCD → "{YYYY-MM-DD} {HH:mm} WIB"
   - Post Schedule → "{YYYY-MM-DD} {HH:mm} WIB"
   - Post EWCD → "{YYYY-MM-DD} {HH:mm} WIB"
   - Format: "T" replaced with space, suffix " WIB"

8. **Empty value behavior:**
   - Kembali Step 4 tanpa isi field (mis. validasi gagal di Step 3, atau test path)
   - Empty datetime → tampil "-" tanpa suffix WIB (logic ternary intact)

9. **Submit regression Standard:**
   - Isi semua field Standard valid, klik "Buat Assessment" / submit
   - Assessment ter-create di DB normal (tidak ada error binding)
   - Cek `/Admin/ManageAssessment` — entry baru muncul

10. **Submit regression PrePost:**
    - Isi semua field PrePost valid, klik submit
    - Assessment Pre-Post ter-create normal
    - Cek list — Pre-Test dan Post-Test session muncul

11. **EditAssessment (regression check):**
    - Buka entry yang baru dibuat → klik Edit
    - EditAssessment.cshtml TIDAK BERUBAH oleh phase ini, helper "(WIB)" yang sudah ada di sana TETAP tampil
    - Verifikasi visual: pattern Edit dan Create sekarang IDENTIK

12. **Validation regression:**
    - Coba submit Step 3 dengan Tanggal kosong → invalid-feedback "Tanggal jadwal wajib diisi" muncul (di bawah input, di antara helper WIB dan field berikutnya)
    - Helper "(WIB)" tetap visible (warna abu-abu) sementara invalid-feedback merah hanya muncul saat invalid
  </how-to-verify>

  <verify>
    <automated>echo "MANUAL VERIFICATION — wait for human resume-signal 'approved'"</automated>
    <!-- Checkpoint: blocking until user types 'approved' -->
  </verify>

  <acceptance_criteria>
    - User mengetik "approved" setelah menyelesaikan 12 functional checks di browser
    - Atau, user melaporkan issue spesifik untuk perbaikan (planner re-engages)
  </acceptance_criteria>

  <done>
    - Human verifier konfirmasi 12 checks pass
    - Helper text WIB visually appropriate di Standard mode dan PrePost mode
    - Summary suffix WIB konsisten di Step 4 (Standard & PrePost)
    - Tidak ada regresi: submit Standard & PrePost berhasil, EditAssessment tidak berubah, validation tetap jalan
  </done>

  <resume-signal>
    Type "approved" jika semua 12 checks lolos. Jika ada masalah, jelaskan: (a) langkah berapa yang gagal, (b) actual vs expected, (c) screenshot UI jika diperlukan, (d) error message lengkap jika submit gagal.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM (admin user) ↔ Razor view | Helper text rendered server-side via Razor markup statis — no user input flows here |
| Browser JS ↔ Browser DOM | populateSummary mengakses `document.getElementById(...)` lalu set `textContent` (auto-escape, XSS-safe) |
| Browser ↔ Server (POST /Admin/CreateAssessment) | Form submit — TIDAK terpengaruh helper text/JS suffix; DOM ID & name attribute intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-304-07 | Tampering | DOM ID berubah → server-side model binding gagal | mitigate | DOM ID semua input (`schedDateInput`, `preSchedule`, dst) DAN summary span (`summary-*`) eksplisit dipertahankan per D-18 D-19. Verified via grep di acceptance_criteria Task 4 (`>=8` input ID + `>=6` summary span ID). |
| T-304-08 | Tampering | Razor `&` literal merusak HTML structure | mitigate | Pakai `&amp;` HTML entity di "Tanggal &amp; Waktu (WIB)" — konsisten dengan label existing pattern. Build .NET (Task 4) akan catch syntax error. |
| T-304-09 | Information Disclosure | Helper text mengekspos timezone server config | accept | "WIB" adalah label statik public-facing (Indonesia Western Time, UTC+7). Tidak menambah info attacker yang belum tahu. Server already publishes timezone via DateTime serialization (existing). |
| T-304-10 | DoS | XSS via `el.textContent = ...` di JS | mitigate | `textContent` auto-escapes (vs `innerHTML`). Source value (`preSched.value`) adalah `<input type="datetime-local">` browser-controlled string format `YYYY-MM-DDTHH:mm`. Suffix `' WIB'` adalah string literal hardcoded — tidak ada user-controlled string concat. Compliant ASVS V5.3.4. |
| T-304-11 | Repudiation | Audit log impact dari label change | n/a | UI cosmetic only. Tidak ada server-side state change. |
| T-304-12 | Elevation of Privilege | Bypass validation via DOM manipulation | accept | Helper text TIDAK affect server-side validation. Server tetap validate `ModelState.IsValid` dan range constraints. invalid-feedback divs (lines 359, 364, 379, 386) intact. |

**ASVS L1 alignment:**
- V5.2.1 (output encoding): `textContent` auto-escapes — compliant
- V5.3.4 (no string concatenation user input → HTML): Suffix `' WIB'` hardcoded literal, value adalah datetime browser-controlled — compliant
- V11.1.1 (no security logic in client): Helper text & JS suffix murni cosmetic — compliant

**No high-risk threat — security_enforcement gate: PASS.**
</threat_model>

<verification>
## Phase-Level Verification

Setelah semua 5 task selesai:

1. **Static checks** (otomatis):
   ```bash
   # Step 3 helper text
   grep -c 'class="form-text text-muted">Tanggal (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"           # = 2
   grep -c 'class="form-text text-muted">Waktu (WIB)</div>' "Views/Admin/CreateAssessment.cshtml"             # = 2
   grep -c 'class="form-text text-muted">Tanggal &amp; Waktu (WIB)</div>' "Views/Admin/CreateAssessment.cshtml" # = 4
   grep -c 'class="form-text text-muted">' "Views/Admin/CreateAssessment.cshtml"                              # = 9 (8 + Durasi existing)

   # Step 4 JS suffix
   grep -c "+ ' WIB'" "Views/Admin/CreateAssessment.cshtml"                                                    # = 5
   grep -c "' WIB'" "Views/Admin/CreateAssessment.cshtml"                                                      # >= 6

   # DOM ID intact (regression guard)
   grep -c 'id="schedDateInput"\|id="schedTimeInput"\|id="ewcdDateInput"\|id="ewcdTimeInput"\|id="preSchedule"\|id="preExamWindowCloseDate"\|id="postSchedule"\|id="postExamWindowCloseDate"' "Views/Admin/CreateAssessment.cshtml"  # >= 8
   grep -c 'id="summary-pre-schedule"\|id="summary-pre-ewcd"\|id="summary-post-schedule"\|id="summary-post-ewcd"\|id="summary-ewcd"\|id="summary-schedule"' "Views/Admin/CreateAssessment.cshtml"  # >= 6

   # Existing helper Durasi tidak terganggu
   grep -c '1–480 menit (maks 8 jam)' "Views/Admin/CreateAssessment.cshtml"                                   # = 1

   # Existing line 1164 precedent tidak diubah
   grep -c "schedVal + ' ' + timeVal + ' WIB'" "Views/Admin/CreateAssessment.cshtml"                          # = 1
   ```

2. **Build check:**
   ```bash
   dotnet build -c Debug --nologo --verbosity minimal                                                          # exit 0
   ```

3. **Human verification** — Task 5 checkpoint (12 functional checks termasuk submit regression Standard & PrePost).
</verification>

<success_criteria>
- [x] WIZ-02 covered: 8 input date/time/datetime-local Step 3 punya helper "(WIB)"
- [x] WIZ-03 covered: 5 JS suffix `' WIB'` di populateSummary (4 PrePost + 1 Standard ewcd)
- [x] D-10 honored: pakai pattern `<div class="form-text text-muted">` (form-text helper, bukan inline label)
- [x] D-11 honored: 8 input target (4 Standard + 4 PrePost) — scope extension dari ROADMAP
- [x] D-12 honored: tidak ada banner top-section "WIB", helper per-field saja
- [x] D-13 honored: 5 lokasi JS suffix WIB (1126, 1130, 1132, 1136, 1177); line 1164 precedent tetap
- [x] D-14 honored: pakai `replace('T', ' ') + ' WIB'`, tidak parse Date object
- [x] D-15 honored: edit inline, tidak extract ke external JS
- [x] D-16 honored: invalid-feedback messages tidak diubah
- [x] D-17 honored: tidak interfere jQuery validate
- [x] D-18 honored: DOM ID semua input dan summary span intact
- [x] D-19 honored: tag helper `asp-for` dan hidden input intact
- [x] Existing helper Durasi (line 371) tidak terganggu
- [x] Build .NET sukses
- [x] Human verification (Task 5) approved — submit Standard & PrePost regress-free
</success_criteria>

<output>
Setelah completion, buat `.planning/phases/304-ui-label-polish-login-wib/304-02-SUMMARY.md` dengan template summary standar:
- Files modified: `Views/Admin/CreateAssessment.cshtml`
- Tasks completed: 5 (4 auto + 1 checkpoint)
- WIZ-02 status: implemented + human-verified (8 helper text)
- WIZ-03 status: implemented + human-verified (5 JS suffix)
- Linkages: D-10 sampai D-19 (canonical refs in CONTEXT.md), pattern source EditAssessment.cshtml lines 241/247/299/305/360/366/435/441
- Notes: tidak ada file baru, tidak ada dependency baru, tidak ada DB change, tidak ada controller change
</output>
