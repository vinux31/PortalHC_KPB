---
phase: 305-question-type-naming-clarity
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html
  - wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md
  - wwwroot/documents/TKI/generate_bab_x.py
  - wwwroot/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html
  - wwwroot/documents/guides/Release-Notes-HC-Portal-KPB.html
  - wwwroot/documents/guides/Penjelasan-Halaman-PortalHC-KPB.html
  - wwwroot/documents/guides/Struktur-Website-PortalHC-KPB.html
  - docs/Persiapan-Test-Manual-Assessment.html
autonomous: false
requirements:
  - LBL-01
user_setup: []

must_haves:
  truths:
    - "Semua 8 file dokumentasi (7 di wwwroot/documents/, 1 di docs/) diaudit per occurrence dan label tipe MC/MA di-update ke 'Single Choice'/'Multiple Answers' sesuai konteks"
    - "Occurrence yang merupakan label tipe (capitalized, dalam tabel/list/header) DIGANTI ke wording baru"
    - "Occurrence yang merupakan deskripsi generic ('pilihan ganda' lowercase, 'pilihan ganda kompleks' AKM Kemendikbud context, atau enum value `<code>MultipleAnswer</code>` dalam bullet schema doc) DIPERTAHANKAN atau di-rephrase context-aware"
    - "Tidak ada residual 'Pilihan Ganda' (capitalized, label tipe context) di 8 file dokumentasi setelah update"
    - "Tidak ada residual 'Multi Jawaban' di 8 file dokumentasi"
    - "'Multiple Answer' (singular) → 'Multiple Answers' (plural) di setiap occurrence yang merupakan label tipe"
    - "'Multiple Choice' di context label tipe MC → 'Single Choice'"
    - "DB query verifikasi: `SELECT DISTINCT QuestionType FROM PackageQuestions` returns hanya 'MultipleChoice', 'MultipleAnswer', 'Essay' (D-17 D-20 — enum value DB tidak berubah)"
    - "E2E Playwright test directory `tests/e2e/` ZERO match label baru OR label lama (D-15 verified — tidak perlu update)"
    - "PDF panduan + screenshot training di-flag sebagai deferred manual user task di SUMMARY (D-14)"
  artifacts:
    - path: "wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html"
      provides: "TKI HTML dengan label tipe updated"
    - path: "wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md"
      provides: "TKI markdown outline dengan label tipe updated"
    - path: "wwwroot/documents/TKI/generate_bab_x.py"
      provides: "Python source untuk regen TKI HTML — string literal updated"
    - path: "wwwroot/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html"
      provides: "Panduan website dengan label tipe updated di tabel"
    - path: "wwwroot/documents/guides/Release-Notes-HC-Portal-KPB.html"
      provides: "Release notes — label tipe + caption screenshot updated"
    - path: "wwwroot/documents/guides/Penjelasan-Halaman-PortalHC-KPB.html"
      provides: "Penjelasan halaman — label tipe di description fitur updated"
    - path: "wwwroot/documents/guides/Struktur-Website-PortalHC-KPB.html"
      provides: "Struktur website — label tipe di description scoring updated"
    - path: "docs/Persiapan-Test-Manual-Assessment.html"
      provides: "Persiapan test manual — label tipe di tabel contoh updated"
  key_links:
    - from: "wwwroot/documents/* (static HTML/MD/PY)"
      to: "Browser/User documentation viewer"
      via: "ASP.NET Core StaticFiles middleware (server langsung dari folder, no cache)"
      pattern: "wwwroot/documents/(TKI|guides)/"
    - from: "DB column PackageQuestions.QuestionType"
      to: "Enum literal validation D-17"
      via: "SELECT DISTINCT post-deploy verification (D-20)"
      pattern: "QuestionType IN \\('MultipleChoice', 'MultipleAnswer', 'Essay'\\)"
---

<objective>
Update 8 file dokumentasi (7 di `wwwroot/documents/`, 1 di `docs/`) untuk menggantikan label tipe MC/MA lama ("Pilihan Ganda", "Multi Jawaban", "Multiple Answer", "Multiple Choice") dengan wording baru ("Single Choice", "Multiple Answers") secara **context-aware per occurrence** — bukan blind sed. Kemudian verifikasi DB enum value tidak berubah dan grep audit final memastikan tidak ada residual label lama di kode.

Purpose: Memenuhi LBL-01 success criteria #4 ("Documentation cross-cutting") + #5 ("DB query verifikasi enum unchanged"). Plan 01 sudah ship view + controller; Plan 02 menutup gap dokumentasi + verifikasi schema lock.

Output:
- 8 file dokumentasi dengan label tipe konsisten label baru di context label, dan generic-context "pilihan ganda" lowercase / `<code>MultipleAnswer</code>` enum value tetap (D-13 manual review).
- DB query screenshot/output verifikasi (Task 2 manual checkpoint).
- E2E Playwright grep audit (Task 3 — verified ZERO match baseline, no edit needed).
- PDF panduan + screenshot training di-flag deferred manual task di SUMMARY (D-14).

Konteks **17+ occurrences nyata** (RESEARCH.md Pitfall #2 — CONTEXT.md klaim 14 incomplete; grep menyeluruh menunjukkan ~17 occurrences across 5 patterns: "Pilihan Ganda", "Pilihan ganda", "Multi Jawaban", "Multiple Answer", "Multiple Choice"). Plan 02 wajib pakai grep pattern lengkap saat eksekusi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/305-question-type-naming-clarity/305-CONTEXT.md
@.planning/phases/305-question-type-naming-clarity/305-RESEARCH.md
@.planning/phases/305-question-type-naming-clarity/305-PATTERNS.md
@.planning/phases/305-question-type-naming-clarity/305-VALIDATION.md
@wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html
@wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md
@wwwroot/documents/TKI/generate_bab_x.py
@wwwroot/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html
@wwwroot/documents/guides/Release-Notes-HC-Portal-KPB.html
@wwwroot/documents/guides/Penjelasan-Halaman-PortalHC-KPB.html
@wwwroot/documents/guides/Struktur-Website-PortalHC-KPB.html
@docs/Persiapan-Test-Manual-Assessment.html

<interfaces>
Hasil grep menyeluruh 2026-04-28 (gunakan ini sebagai baseline expected occurrences):

File: wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md
- Line 63: "1) Pilihan ganda (single answer)" — generic deskripsi numbered list, REVIEW (kemungkinan rephrase ke "Single Choice (single answer)" atau "Soal pilihan tunggal (single answer)")
- Line 195: "Download template sesuai tipe: Essay / Multiple Answer / Mix" — label tipe context, GANTI ke "Multiple Answers"
- Line 320: "Lampiran I — Template Excel Import Soal (Essay / Multiple Answer / Mix)" — label tipe context, GANTI ke "Multiple Answers"

File: wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html
- Line 216: "Tipe soal yang tersedia: (1) Pilihan ganda; (2) Multiple answer; (3) Essay; (4) Mix." — label tipe enumerasi, GANTI "Pilihan ganda" → "Single Choice", GANTI "Multiple answer" → "Multiple Answers"
- Line 337: "Download template sesuai tipe: Essay / Multiple Answer / Mix (lihat Lampiran I)." — label tipe, GANTI "Multiple Answer" → "Multiple Answers"
- Line 442: "Lampiran I — Template Excel Import Soal (Essay / Multiple Answer / Mix)" — label tipe, GANTI

File: wwwroot/documents/TKI/generate_bab_x.py (Python source, regenerator)
- Line 56: `("USER", "Tipe soal yang tersedia: (1) Pilihan ganda; (2) Multiple answer; (3) Essay; (4) Mix.", None),` — string literal untuk regen HTML, GANTI sama dengan line 216 .html
- Line 181: `("HC", "Download template sesuai tipe: Essay / Multiple Answer / Mix (lihat Lampiran I).", None),` — GANTI sama dengan line 337 .html
- Line 305: `"Lampiran I — Template Excel Import Soal (Essay / Multiple Answer / Mix)",` — GANTI sama dengan line 442 .html

File: wwwroot/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html
- Line 373: `<td><strong>Pilihan Ganda</strong></td>` — label tipe di tabel, GANTI ke "Single Choice"

File: wwwroot/documents/guides/Release-Notes-HC-Portal-KPB.html
- Line 327: `<td><strong>Multiple Choice (MC)</strong></td>` — label tipe header tabel, GANTI ke "Single Choice (MC)" — keep "(MC)" sebagai legacy abbrev
- Line 328: `<td>Pilihan ganda, satu jawaban benar. Peserta memilih dengan radio button.</td>` — DESKRIPSI generic; setelah label baru "Single Choice", deskripsi ini redundant. REPHRASE ke "Soal dengan satu jawaban benar. Peserta memilih dengan radio button."
- Line 332: `<td><strong>Multiple Answer (MA)</strong></td>` — label tipe header, GANTI ke "Multiple Answers (MA)" — plural + keep "(MA)"
- Line 333: `<td>Pilihan ganda, lebih dari satu jawaban benar. Label "Pilih semua yang benar" ditampilkan.</td>` — DESKRIPSI generic; REPHRASE ke 'Soal dengan lebih dari satu jawaban benar. Label "Pilih semua yang benar" ditampilkan.'
- Line 359: `<div class="caption">Halaman Manage Questions — daftar soal dengan badge tipe, dan form Tambah Soal dengan dropdown Pilihan Ganda (MC) / Multiple Answer (MA) / Essay</div>` — caption screenshot, GANTI: "Pilihan Ganda (MC)" → "Single Choice (MC)", "Multiple Answer (MA)" → "Multiple Answers (MA)"
- Line 359 alt attribute: `alt="Manage Questions — daftar soal dan form tambah soal dengan pilihan tipe MC/MA/Essay"` — keep "MC/MA/Essay" abbreviation TETAP (informal short)

File: wwwroot/documents/guides/Penjelasan-Halaman-PortalHC-KPB.html
- Line 315: `Mendukung tipe soal Pilihan Ganda, Multiple Answer, dan Essay.` — label tipe enumerasi (worker exam description), GANTI "Pilihan Ganda" → "Single Choice", "Multiple Answer" → "Multiple Answers"
- Line 532: `Mendukung tipe Multiple Choice, Multiple Answer, dan Essay.` — label tipe enumerasi (admin Manage Questions description), GANTI "Multiple Choice" → "Single Choice", "Multiple Answer" → "Multiple Answers"
- Line 538: `Upload soal massal melalui template Excel (format berbeda untuk MC, Multiple Answer, Essay, dan Mix).` — abbrev "MC" + label tipe; KEEP "MC" abbrev tapi GANTI "Multiple Answer" → "Multiple Answers"

File: wwwroot/documents/guides/Struktur-Website-PortalHC-KPB.html
- Line 272: `<li>Import soal format Essay / Multiple Answer / Mix</li>` — label tipe, GANTI "Multiple Answer" → "Multiple Answers"
- Line 430: `<div class="desc">Menghitung skor ujian otomatis: Multiple Choice, Multiple Answer, Essay.</div>` — label tipe enumerasi (scoring description), GANTI "Multiple Choice" → "Single Choice", "Multiple Answer" → "Multiple Answers"

File: docs/Persiapan-Test-Manual-Assessment.html
- Line 396: `<tr><td>1</td><td>Multiple Choice</td><td>"Warna bendera Indonesia?"</td>...` — label tipe di tabel contoh, GANTI "Multiple Choice" → "Single Choice"
- Line 397: `<tr><td>2</td><td>Multiple Answer</td><td>"Pilih alat safety"</td>...` — label tipe, GANTI "Multiple Answer" → "Multiple Answers"

Pattern grep lengkap (gunakan saat task execute untuk verifikasi):
```bash
grep -rn "Pilihan Ganda\|Pilihan ganda\|Multi Jawaban\|Multiple Answer\|Multiple Choice" wwwroot/documents/ docs/ 2>/dev/null
```

Total expected occurrences setelah update: ~21 changed/reviewed (sebagian rephrase, sebagian replace) di 8 file. Plan 01 sudah handle 5 file kode (4 view + 1 controller).

Files yang TIDAK diubah Plan 02:
- `wwwroot/documents/*.pdf` — binary, deferred manual user (D-14)
- `wwwroot/documents/*.xlsx` — Excel template binary, kolom QuestionType pakai enum value (D-18)
- Screenshot training material (lokasi tidak ter-track Git) — deferred manual user (D-14)
- `Views/`, `Models/`, `Controllers/` — sudah handle Plan 01
- `tests/e2e/` — D-15 verified ZERO match label lama, no edit needed (Task 3 audit only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update 8 file dokumentasi context-aware (sed-replace per occurrence)</name>

  <read_first>
    - SEMUA 8 file di `files_modified` (read full files dulu sebelum edit untuk konfirmasi current line + surrounding context — wajib per D-13 "context-by-context review, bukan blind sed")
    - `.planning/phases/305-question-type-naming-clarity/305-CONTEXT.md` D-13 D-14 D-15 (8 file scope, deferred PDF + screenshot, E2E zero match)
    - `.planning/phases/305-question-type-naming-clarity/305-RESEARCH.md` Pitfall #1 (Pilihan Ganda lowercase ambiguous) + Pitfall #2 (Multiple Choice juga ada — total 17 occurrences nyata, bukan 14)
    - `.planning/phases/305-question-type-naming-clarity/305-PATTERNS.md` section 8 (file occurrence breakdown + replace pattern table)
    - `.planning/phases/305-question-type-naming-clarity/305-VALIDATION.md` task 305-02-01 (grep audit baseline)
  </read_first>

  <files>
wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html
wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md
wwwroot/documents/TKI/generate_bab_x.py
wwwroot/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html
wwwroot/documents/guides/Release-Notes-HC-Portal-KPB.html
wwwroot/documents/guides/Penjelasan-Halaman-PortalHC-KPB.html
wwwroot/documents/guides/Struktur-Website-PortalHC-KPB.html
docs/Persiapan-Test-Manual-Assessment.html
  </files>

  <action>
PROCEDURE per file (urutan tidak penting — bisa parallel atau sequential):

Step 1: Run grep audit pre-edit untuk konfirmasi baseline:
```bash
grep -rn "Pilihan Ganda\|Pilihan ganda\|Multi Jawaban\|Multiple Answer\|Multiple Choice" wwwroot/documents/ docs/ 2>/dev/null
```
Expected: ~17-21 occurrences across 8 file (sesuai daftar di `<interfaces>`).

Step 2: Untuk SETIAP occurrence, pakai `Read` + `Edit` workflow:
1. Read 2-3 baris context atas + bawah dari hit
2. Klasifikasi sebagai:
   - **A. Label tipe (capitalized, dalam tabel/list/header/dropdown caption):** REPLACE ke wording baru
   - **B. Generic deskripsi (lowercase "pilihan ganda" = "soal dengan pilihan", AKM Kemendikbud term):** REPHRASE jika sekarang redundant dengan label baru, atau KEEP
   - **C. Enum value (`<code>MultipleAnswer</code>` di schema doc bullet, atau Python string literal yang regen HTML B):** REVIEW — kalau Python source untuk regen, ikuti perubahan HTML target
3. `Edit` dengan old_string match exact + 2-3 baris context unik agar tidak ambiguous

DETAIL PER FILE — APPLY EDITS PERSIS SEPERTI DI BAWAH:

═══════════════════════════════════════════════════════════════
File 1A: wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md
═══════════════════════════════════════════════════════════════

EDIT 1A.1 — line 63 (klasifikasi B/A — generic deskripsi tetapi konteks numbered list tipe soal, label tipe):
OLD: `  - 1) Pilihan ganda (single answer)`
NEW: `  - 1) Single Choice (single answer)`

EDIT 1A.2 — line 195 (klasifikasi A — label tipe template):
OLD: `- b. \`[HC]\` Download template sesuai tipe: Essay / Multiple Answer / Mix (lihat Lampiran I).`
NEW: `- b. \`[HC]\` Download template sesuai tipe: Essay / Multiple Answers / Mix (lihat Lampiran I).`

EDIT 1A.3 — line 320 (klasifikasi A — label lampiran):
OLD: `- **Lampiran I** — Template Excel Import Soal (Essay / Multiple Answer / Mix)`
NEW: `- **Lampiran I** — Template Excel Import Soal (Essay / Multiple Answers / Mix)`

═══════════════════════════════════════════════════════════════
File 1B: wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html
═══════════════════════════════════════════════════════════════

EDIT 1B.1 — line 216 (klasifikasi A — label tipe enumerasi):
OLD: `<div class="step">f. <span class="role USER">USER</span>Tipe soal yang tersedia: (1) Pilihan ganda; (2) Multiple answer; (3) Essay; (4) Mix.</div>`
NEW: `<div class="step">f. <span class="role USER">USER</span>Tipe soal yang tersedia: (1) Single Choice; (2) Multiple Answers; (3) Essay; (4) Mix.</div>`

EDIT 1B.2 — line 337 (klasifikasi A):
OLD: `<div class="step">b. <span class="role HC">HC</span>Download template sesuai tipe: Essay / Multiple Answer / Mix (lihat Lampiran I).</div>`
NEW: `<div class="step">b. <span class="role HC">HC</span>Download template sesuai tipe: Essay / Multiple Answers / Mix (lihat Lampiran I).</div>`

EDIT 1B.3 — line 442 (klasifikasi A):
OLD: `<li>Lampiran I — Template Excel Import Soal (Essay / Multiple Answer / Mix)</li>`
NEW: `<li>Lampiran I — Template Excel Import Soal (Essay / Multiple Answers / Mix)</li>`

═══════════════════════════════════════════════════════════════
File 1C: wwwroot/documents/TKI/generate_bab_x.py
═══════════════════════════════════════════════════════════════
(Python source untuk regen HTML — string literal harus match HTML target)

EDIT 1C.1 — line 56 (sync dengan EDIT 1B.1):
OLD: `                ("USER", "Tipe soal yang tersedia: (1) Pilihan ganda; (2) Multiple answer; (3) Essay; (4) Mix.", None),`
NEW: `                ("USER", "Tipe soal yang tersedia: (1) Single Choice; (2) Multiple Answers; (3) Essay; (4) Mix.", None),`

EDIT 1C.2 — line 181 (sync dengan EDIT 1B.2):
OLD: `                ("HC", "Download template sesuai tipe: Essay / Multiple Answer / Mix (lihat Lampiran I).", None),`
NEW: `                ("HC", "Download template sesuai tipe: Essay / Multiple Answers / Mix (lihat Lampiran I).", None),`

EDIT 1C.3 — line 305 (sync dengan EDIT 1B.3):
OLD: `    "Lampiran I — Template Excel Import Soal (Essay / Multiple Answer / Mix)",`
NEW: `    "Lampiran I — Template Excel Import Soal (Essay / Multiple Answers / Mix)",`

═══════════════════════════════════════════════════════════════
File 1D: wwwroot/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html
═══════════════════════════════════════════════════════════════

EDIT 1D.1 — line 373 (klasifikasi A — label tipe di tabel):
OLD: `    <td><strong>Pilihan Ganda</strong></td>`
NEW: `    <td><strong>Single Choice</strong></td>`

(Catatan: jika ada deskripsi di line ~374 yang generic "Soal pilihan ganda dengan satu jawaban benar", REVIEW — bisa stay atau rephrase ke "Soal dengan satu jawaban benar." — gunakan judgement context.)

═══════════════════════════════════════════════════════════════
File 1E: wwwroot/documents/guides/Release-Notes-HC-Portal-KPB.html
═══════════════════════════════════════════════════════════════

EDIT 1E.1 — line 327 (klasifikasi A — header tabel tipe):
OLD: `    <td><strong>Multiple Choice (MC)</strong></td>`
NEW: `    <td><strong>Single Choice (MC)</strong></td>`

EDIT 1E.2 — line 328 (klasifikasi B — deskripsi generic, redundant setelah label baru):
OLD: `    <td>Pilihan ganda, satu jawaban benar. Peserta memilih dengan radio button.</td>`
NEW: `    <td>Soal dengan satu jawaban benar. Peserta memilih dengan radio button.</td>`

EDIT 1E.3 — line 332 (klasifikasi A — header tabel tipe):
OLD: `    <td><strong>Multiple Answer (MA)</strong></td>`
NEW: `    <td><strong>Multiple Answers (MA)</strong></td>`

EDIT 1E.4 — line 333 (klasifikasi B — deskripsi generic, redundant):
OLD: `    <td>Pilihan ganda, lebih dari satu jawaban benar. Label "Pilih semua yang benar" ditampilkan.</td>`
NEW: `    <td>Soal dengan lebih dari satu jawaban benar. Label "Pilih semua yang benar" ditampilkan.</td>`

EDIT 1E.5 — line 359 (klasifikasi A — caption screenshot):
OLD: `<div class="screenshot"><img src="screenshots/rn-package-questions.png" alt="Manage Questions — daftar soal dan form tambah soal dengan pilihan tipe MC/MA/Essay"><div class="caption">Halaman Manage Questions — daftar soal dengan badge tipe, dan form Tambah Soal dengan dropdown Pilihan Ganda (MC) / Multiple Answer (MA) / Essay</div></div>`
NEW: `<div class="screenshot"><img src="screenshots/rn-package-questions.png" alt="Manage Questions — daftar soal dan form tambah soal dengan pilihan tipe MC/MA/Essay"><div class="caption">Halaman Manage Questions — daftar soal dengan badge tipe, dan form Tambah Soal dengan dropdown Single Choice (MC) / Multiple Answers (MA) / Essay</div></div>`

(Catatan: alt attribute "MC/MA/Essay" abbreviation TETAP — informal short, bukan label penuh.)

═══════════════════════════════════════════════════════════════
File 1F: wwwroot/documents/guides/Penjelasan-Halaman-PortalHC-KPB.html
═══════════════════════════════════════════════════════════════

EDIT 1F.1 — line 315 (klasifikasi A — label tipe enumerasi worker exam description):
OLD: `  <div class="fdesc">Halaman pengerjaan ujian. Soal ditampilkan satu per satu atau per page, dengan timer berjalan. Mendukung tipe soal Pilihan Ganda, Multiple Answer, dan Essay. Sistem mencatat aktivitas mencurigakan (keluar fullscreen, pindah tab) ke log.</div>`
NEW: `  <div class="fdesc">Halaman pengerjaan ujian. Soal ditampilkan satu per satu atau per page, dengan timer berjalan. Mendukung tipe soal Single Choice, Multiple Answers, dan Essay. Sistem mencatat aktivitas mencurigakan (keluar fullscreen, pindah tab) ke log.</div>`

EDIT 1F.2 — line 532 (klasifikasi A — label tipe admin Manage Questions description):
OLD: `  <div class="fdesc">Menambah/edit/hapus soal dalam satu paket. Mendukung tipe Multiple Choice, Multiple Answer, dan Essay. Soal dapat dilengkapi gambar pendukung.</div>`
NEW: `  <div class="fdesc">Menambah/edit/hapus soal dalam satu paket. Mendukung tipe Single Choice, Multiple Answers, dan Essay. Soal dapat dilengkapi gambar pendukung.</div>`

EDIT 1F.3 — line 538 (klasifikasi A — abbrev MC keep, label tipe MA update):
OLD: `  <div class="fdesc">Upload soal massal melalui template Excel (format berbeda untuk MC, Multiple Answer, Essay, dan Mix). Validasi dilakukan otomatis sebelum commit.</div>`
NEW: `  <div class="fdesc">Upload soal massal melalui template Excel (format berbeda untuk MC, Multiple Answers, Essay, dan Mix). Validasi dilakukan otomatis sebelum commit.</div>`

(Catatan: "MC" abbreviation di line 538 KEEP — informal short konsisten dengan template button query param `type=MC`.)

═══════════════════════════════════════════════════════════════
File 1G: wwwroot/documents/guides/Struktur-Website-PortalHC-KPB.html
═══════════════════════════════════════════════════════════════

EDIT 1G.1 — line 272 (klasifikasi A — label tipe import format):
OLD: `      <li>Import soal format Essay / Multiple Answer / Mix</li>`
NEW: `      <li>Import soal format Essay / Multiple Answers / Mix</li>`

EDIT 1G.2 — line 430 (klasifikasi A — label tipe scoring description):
OLD: `    <div class="desc">Menghitung skor ujian otomatis: Multiple Choice, Multiple Answer, Essay. Menentukan pass/fail berdasarkan passing grade.</div>`
NEW: `    <div class="desc">Menghitung skor ujian otomatis: Single Choice, Multiple Answers, Essay. Menentukan pass/fail berdasarkan passing grade.</div>`

═══════════════════════════════════════════════════════════════
File 1H: docs/Persiapan-Test-Manual-Assessment.html
═══════════════════════════════════════════════════════════════

EDIT 1H.1 — line 396 (klasifikasi A — label tipe contoh tabel):
OLD: `    <tr><td>1</td><td>Multiple Choice</td><td>"Warna bendera Indonesia?"</td><td>A. Merah-Putih (✓), B. Hijau, C. Biru, D. Kuning</td><td>30</td></tr>`
NEW: `    <tr><td>1</td><td>Single Choice</td><td>"Warna bendera Indonesia?"</td><td>A. Merah-Putih (✓), B. Hijau, C. Biru, D. Kuning</td><td>30</td></tr>`

EDIT 1H.2 — line 397 (klasifikasi A):
OLD: `    <tr><td>2</td><td>Multiple Answer</td><td>"Pilih alat safety"</td><td>A. Helm (✓), B. Sarung tangan (✓), C. Sepatu (✓), D. Kaos</td><td>30</td></tr>`
NEW: `    <tr><td>2</td><td>Multiple Answers</td><td>"Pilih alat safety"</td><td>A. Helm (✓), B. Sarung tangan (✓), C. Sepatu (✓), D. Kaos</td><td>30</td></tr>`

═══════════════════════════════════════════════════════════════

Step 3: Setelah semua edit, run grep audit POST-edit:
```bash
grep -rn "Pilihan Ganda\|Multi Jawaban" wwwroot/documents/ docs/ 2>/dev/null
# Expected: 0 hits (capitalized label tipe lama hilang dari 8 file)

grep -rn "Multiple Answer[^s]" wwwroot/documents/ docs/ 2>/dev/null
# Expected: 0 hits (singular "Multiple Answer" tanpa trailing 's' hilang)

grep -rn "Multiple Choice" wwwroot/documents/ docs/ 2>/dev/null
# Expected: 0 hits (capitalized label tipe MC lama hilang)

grep -rn "Pilihan ganda" wwwroot/documents/ docs/ 2>/dev/null
# Expected: minimal residu — review tiap hit:
#   - Jika hit di context label tipe → SHOULD BE 0 (sudah di-edit di EDIT 1A.1)
#   - Jika hit di context generic deskripsi yang dibiarkan → REVIEW boleh ada
```

Step 4: Untuk setiap residual hit di Step 3, review individu — kalau true generic deskripsi (bukan label tipe), boleh tetap. Contoh: "fitur soal pilihan ganda" (deskripsi feature genericnya). Tapi pattern dasar: kalau capitalized atau dalam tabel/list label tipe, sudah harus hilang.

Yang TIDAK boleh dilakukan:
- TIDAK pakai `sed -i` blind — Pitfall #1 dilarang. Selalu Read + Edit per occurrence.
- TIDAK ubah file `.pdf` (binary, deferred D-14)
- TIDAK ubah `.xlsx` template (binary, kolom QuestionType pakai enum value D-18)
- TIDAK ubah `tests/e2e/` (Task 3 audit only — D-15 zero match baseline)
- TIDAK ubah Excel template config / mapping logic di controller `DownloadQuestionTemplate` — query param `type=MC`/`type=MA` tetap (Plan 01 ImportPackageQuestions edit sudah handle button text)
- TIDAK ubah alt attribute screenshot (informal abbreviation) di EDIT 1E.5 — keep "MC/MA/Essay"
- TIDAK ubah caption "(MC)" / "(MA)" suffix — keep legacy abbrev sebagai pendamping label baru per pattern industry (e.g., Canvas "Multiple Choice (MC)")

Tools: ~22 sequential `Edit` calls (atau MultiEdit per file jika tersedia). Per edit harus pakai context unik agar match tidak ambiguous. Total durasi estimate: 5-10 menit eksekusi otomatis.
  </action>

  <verify>
    <automated>grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Choice" wwwroot/documents/ docs/ 2>/dev/null | wc -l</automated>
    <!-- Expected: 0 (capitalized label tipe lama hilang dari semua 8 file dokumentasi) -->
  </verify>

  <acceptance_criteria>
    - `grep -rn "Pilihan Ganda" wwwroot/documents/ docs/ 2>/dev/null | wc -l` returns `0` (capitalized label tipe MC lama hilang)
    - `grep -rn "Multi Jawaban" wwwroot/documents/ docs/ 2>/dev/null | wc -l` returns `0` (label tipe MA lama hilang)
    - `grep -rn "Multiple Choice" wwwroot/documents/ docs/ 2>/dev/null | wc -l` returns `0` (label tipe English lama hilang)
    - `grep -rn "Multiple Answer[^s]" wwwroot/documents/ docs/ 2>/dev/null | wc -l` returns `0` (singular "Multiple Answer" — match diikuti karakter NON-`s` — hilang; "Multiple Answers" plural OK)
    - `grep -rn "Single Choice" wwwroot/documents/ docs/ 2>/dev/null | wc -l` returns `>=8` (label baru muncul di minimal 8 location across 8 file)
    - `grep -rn "Multiple Answers" wwwroot/documents/ docs/ 2>/dev/null | wc -l` returns `>=10` (label MA baru di banyak location)
    - `grep -rn "Pilihan ganda" wwwroot/documents/ docs/ 2>/dev/null | wc -l` ≤ 1 (Pitfall #1 — generic lowercase boleh tetap di context true generic, tapi maksimal 1-2 residual setelah review; 0 lebih ideal)
    - File `wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html` di-track sebagai modified
    - File `wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md` di-track sebagai modified
    - File `wwwroot/documents/TKI/generate_bab_x.py` di-track sebagai modified
    - File `wwwroot/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html` di-track sebagai modified
    - File `wwwroot/documents/guides/Release-Notes-HC-Portal-KPB.html` di-track sebagai modified
    - File `wwwroot/documents/guides/Penjelasan-Halaman-PortalHC-KPB.html` di-track sebagai modified
    - File `wwwroot/documents/guides/Struktur-Website-PortalHC-KPB.html` di-track sebagai modified
    - File `docs/Persiapan-Test-Manual-Assessment.html` di-track sebagai modified
    - Tidak ada file di luar 8 daftar ini yang di-modified Plan 02
    - `dotnet build -c Debug --nologo --verbosity minimal` exit code = 0 (build tidak break karena edit static files — sanity check)
  </acceptance_criteria>

  <done>
    - 8 file dokumentasi di-update context-aware per occurrence (D-13 manual review)
    - Label tipe lama capitalized ("Pilihan Ganda" / "Multi Jawaban" / "Multiple Answer" / "Multiple Choice") hilang dari semua 8 file
    - Label baru ("Single Choice" / "Multiple Answers") muncul di context label tipe
    - Generic deskripsi yang redundant di-rephrase ("Pilihan ganda, satu jawaban benar" → "Soal dengan satu jawaban benar")
    - Python source `generate_bab_x.py` sync dengan HTML target — regen HTML akan konsisten
    - Excel template binary, PDF, screenshot — TIDAK diubah (deferred D-14, D-18)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: DB query verifikasi enum value tidak berubah (D-17 D-20)</name>

  <files>(verification target — no file modifications)</files>

  <read_first>
    - `.planning/phases/305-question-type-naming-clarity/305-CONTEXT.md` D-17 D-20
    - `.planning/phases/305-question-type-naming-clarity/305-VALIDATION.md` task 305-02-02 (DB query post-deploy)
    - `appsettings.json` atau `appsettings.Development.json` — verifikasi connection string DB target (untuk konteks user runtime)
  </read_first>

  <what-built>
    Verifikasi runtime DB schema bahwa Plan 01 view + controller edits TIDAK secara tidak sengaja mengubah DB enum value `PackageQuestions.QuestionType`. Satu-satunya cara verify schema lock D-17 adalah dengan query DB sebenarnya (post-deploy / dev DB) — tidak bisa di-automate via dotnet build atau grep karena nilai DB hidup di runtime store, bukan source code.
  </what-built>

  <action>
Pause execution, instruksikan user untuk run query DB dan laporkan output. Tunggu user mengetik "approved" plus paste query result, atau melaporkan masalah.

User options untuk run query (pilih salah satu sesuai DB target):

**Option A — SQL Server Management Studio (SSMS) atau Azure Data Studio:**
1. Connect ke connection string yang sama dengan `appsettings.json` / `appsettings.Development.json` `ConnectionStrings:DefaultConnection`
2. New Query, paste:
   ```sql
   SELECT DISTINCT QuestionType
   FROM PackageQuestions
   ORDER BY QuestionType;
   ```
3. Execute, screenshot/copy result.

**Option B — sqlcmd CLI (SQL Server):**
```bash
sqlcmd -S {server} -d {database} -U {user} -P {password} -Q "SELECT DISTINCT QuestionType FROM PackageQuestions ORDER BY QuestionType;"
```

**Option C — dotnet ef (alternatif via EF Core dbcontext, jika user prefer):**
```bash
# (Optional, requires custom command — skip jika SSMS atau sqlcmd available)
```

**Option D — SQLite (jika dev DB SQLite):**
```bash
sqlite3 {path-to-db.db} "SELECT DISTINCT QuestionType FROM PackageQuestions ORDER BY QuestionType;"
```

Expected output (D-17 lock):
```
QuestionType
------------
Essay
MultipleAnswer
MultipleChoice
```
(3 rows, atau ≤3 jika tipe tertentu belum dipakai. URUTAN bisa berbeda — yang penting set value adalah subset dari `{Essay, MultipleAnswer, MultipleChoice}`.)

**FAIL conditions** (report ke user, halt phase):
- Ada value selain 3 di atas (misal "SingleChoice", "MultipleAnswers" plural, dll) — D-17 BREACHED, harus rollback Plan 01 controller edits (validation literal mungkin terketuk)
- Query error (table tidak ada / column tidak ada / connection refused) — verify DB target benar dengan user

Catatan: Jika dev environment fresh tanpa data, query bisa return 0 rows. Itu OK juga (no breach), tapi tidak conclusive — minta user buat 1 sample question per tipe untuk konfirmasi (atau approve sebagai "no data, schema column DataType `nvarchar` tetap").
  </action>

  <verify>
    <automated>echo "MANUAL DB QUERY VERIFICATION — wait for human resume-signal 'approved' with query output"</automated>
  </verify>

  <acceptance_criteria>
    - User mengeksekusi query `SELECT DISTINCT QuestionType FROM PackageQuestions` di DB target
    - User report query result: distinct values adalah subset dari `{"Essay", "MultipleAnswer", "MultipleChoice"}` (atau empty jika fresh DB)
    - Tidak ada value baru/asing yang muncul (e.g., "SingleChoice", "MultipleAnswers" plural)
    - User mengetik "approved" plus paste output query
  </acceptance_criteria>

  <done>
    - DB enum value verified tetap `Essay`/`MultipleAnswer`/`MultipleChoice` (D-17 lock confirmed)
    - D-20 task fulfilled — query post-implementation done
    - Schema lock confirmed — Plan 01 view+controller rename label hanya touch presentation layer, tidak ada side-effect ke DB
  </done>

  <resume-signal>
    Type "approved" plus paste output `SELECT DISTINCT QuestionType FROM PackageQuestions`. Jika output mengandung value asing atau query error, jelaskan: (a) DB target (server/db name), (b) actual output / error message, (c) connection method (SSMS/sqlcmd/sqlite).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Grep audit final + E2E test directory check (D-15) + flag deferred PDF/screenshot (D-14)</name>

  <read_first>
    - All 7 file Plan 01 (view + controller + helper) — state final setelah Plan 01 selesai
    - All 8 file Plan 02 — state final setelah Task 1 selesai
    - `tests/e2e/` directory — verify ZERO match label tipe baru atau lama (D-15 baseline)
    - `.planning/phases/305-question-type-naming-clarity/305-CONTEXT.md` D-14 D-15
    - `.planning/phases/305-question-type-naming-clarity/305-VALIDATION.md` task 305-02-03 (E2E grep audit)
  </read_first>

  <files>(no modifications — verification only, generates report for SUMMARY)</files>

  <action>
Combined audit untuk verifikasi:
1. Plan 01 + Plan 02 cross-cut — semua label lama hilang dari kode + dokumentasi
2. E2E test directory: D-15 verify ZERO match
3. PDF + screenshot deferred items di-flag explicit untuk SUMMARY

Step 1 — Plan 01 + Plan 02 combined grep audit:
```bash
# Total residual label lama di seluruh tree (kecuali .planning/, tests/e2e/, .git/, bin/, obj/, node_modules/)
grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Choice" \
  --include="*.cs" --include="*.cshtml" --include="*.html" --include="*.md" --include="*.py" \
  --exclude-dir=".planning" --exclude-dir=".git" --exclude-dir="bin" --exclude-dir="obj" --exclude-dir="node_modules" \
  Models/ Views/ Controllers/ wwwroot/documents/ docs/ 2>/dev/null

# Expected output: 0 lines (semua surface kode + dokumentasi sudah ter-update)
# Boleh ada hit di .planning/ (CONTEXT.md, RESEARCH.md, PATTERNS.md merupakan documentation phase) — exclude itu

# Singular "Multiple Answer" (tanpa trailing 's')
grep -rn "Multiple Answer[^s]" \
  --include="*.cs" --include="*.cshtml" --include="*.html" --include="*.md" --include="*.py" \
  --exclude-dir=".planning" --exclude-dir=".git" --exclude-dir="bin" --exclude-dir="obj" --exclude-dir="node_modules" \
  Models/ Views/ Controllers/ wwwroot/documents/ docs/ 2>/dev/null

# Expected: 0 lines (kecuali untuk JS string literal `'MultipleAnswer'` tanpa space — tapi pattern grep `"Multiple Answer"` punya space, jadi tidak match enum identifier)
```

Step 2 — Konfirmasi label baru muncul di kode + dokumentasi:
```bash
grep -rn "Single Choice\|Multiple Answers" \
  --include="*.cs" --include="*.cshtml" --include="*.html" --include="*.md" --include="*.py" \
  Models/ Views/ Controllers/ wwwroot/documents/ docs/ 2>/dev/null | wc -l

# Expected: ≥20 occurrences (helper class 4 arms × 2 patterns + 4 view × 2 caller + controller 4 × pattern + 8 dokumentasi × ~2 occurrence = ~30+)
```

Step 3 — DB binding integrity (cross-check Plan 01):
```bash
# Enum literal di kode tetap (D-17, D-19)
grep -rn "MultipleChoice\|MultipleAnswer\|Essay" \
  --include="*.cs" --include="*.cshtml" \
  Models/ Views/ Controllers/ 2>/dev/null | wc -l

# Expected: banyak hits — enum value/literal masih ada di switch helper, JS literal, controller validation, view value attr, model property default. Ini SEHAT — D-17 lock.
```

Step 4 — E2E test directory audit (D-15):
```bash
# Verify ZERO match label TIPE (lama OR baru) di tests/e2e/ — kalau ada hits, perlu update tests
ls tests/e2e/ 2>/dev/null  # konfirmasi directory ada
grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Answer\|Multiple Choice\|Single Choice\|Multiple Answers" tests/e2e/ 2>/dev/null
# Expected: 0 hits (D-15 verified — E2E tidak reference label tipe, hanya enum value via dropdown.value selector)

# Sanity check: tests/e2e/ directory exists?
ls tests/e2e/*.spec.ts tests/e2e/*.spec.js 2>/dev/null | head -5
# Expected: file list (atau empty jika no E2E setup); kalau directory tidak ada, D-15 trivially holds
```

Step 5 — Flag deferred items untuk SUMMARY (D-14):
```bash
# List PDF panduan files yang masih reference label lama (deferred manual user)
ls -la wwwroot/documents/*.pdf wwwroot/documents/**/*.pdf 2>/dev/null

# Catatan untuk SUMMARY: file PDF binary tidak di-edit phase 305 — user harus regen manual setelah deployment
```

Step 6 — Generate audit report (untuk dimasukkan ke 305-02-SUMMARY.md):
```
=== PHASE 305 AUDIT REPORT ===

Plan 01 + 02 verification:
- Label lama "Pilihan Ganda"/"Multi Jawaban"/"Multiple Choice" residual di kode+docs: {count} (target: 0)
- Label baru "Single Choice"/"Multiple Answers" usage: {count} (target: ≥20)
- Enum value DB literal di kode: {count} (target: ≥10, sebagai indikator D-17 D-19 lock)

E2E (D-15):
- tests/e2e/ residual label tipe: {count} (target: 0 — verified pre-phase, no edit needed)

Deferred (D-14):
- PDF panduan yang reference label lama: {list} — user harus regen manual setelah deployment
- Screenshot training material — lokasi tidak ter-track Git, user harus screenshot ulang setelah deployment

Build status: dotnet build exit {code}
```

Yang TIDAK boleh dilakukan:
- TIDAK update file PDF (deferred D-14)
- TIDAK update screenshot (deferred D-14)
- TIDAK edit file di `tests/e2e/` (D-15 ZERO match baseline — kalau ada hit, halt dan investigate)
- TIDAK ubah `.planning/` files saat audit (CONTEXT.md/RESEARCH.md/PATTERNS.md adalah documentation phase, harus tetap reflect history)
  </action>

  <verify>
    <automated>grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Choice" --include="*.cs" --include="*.cshtml" --include="*.html" --include="*.md" --include="*.py" --exclude-dir=".planning" --exclude-dir=".git" --exclude-dir="bin" --exclude-dir="obj" --exclude-dir="node_modules" Models/ Views/ Controllers/ wwwroot/documents/ docs/ 2>/dev/null | wc -l</automated>
    <!-- Expected: 0 -->
  </verify>

  <acceptance_criteria>
    - Combined grep label lama (`"Pilihan Ganda"\|"Multi Jawaban"\|"Multiple Choice"`) di Models/+Views/+Controllers/+wwwroot/documents/+docs/ returns `0` (excluding `.planning/`)
    - `grep -rn "Multiple Answer[^s]" Models/ Views/ Controllers/ wwwroot/documents/ docs/ --include="*.cs" --include="*.cshtml" --include="*.html" --include="*.md" --include="*.py" 2>/dev/null | wc -l` returns `0` (singular form hilang dari prose; enum identifier `MultipleAnswer` tanpa space tidak match karena grep pattern punya space sebelum `[^s]`)
    - `grep -rn "Single Choice\|Multiple Answers" Models/ Views/ Controllers/ wwwroot/documents/ docs/ 2>/dev/null | wc -l` returns `>=20` (label baru distributed di code + docs)
    - `grep -rn "MultipleChoice\|MultipleAnswer" Models/ Views/ Controllers/ --include="*.cs" --include="*.cshtml" 2>/dev/null | wc -l` returns `>=10` (enum value/literal D-17 D-19 lock)
    - `grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Answer\|Multiple Choice\|Single Choice\|Multiple Answers" tests/e2e/ 2>/dev/null | wc -l` returns `0` (D-15 baseline verified ZERO match — kalau directory tidak ada, trivially `0`)
    - PDF list captured untuk SUMMARY (boleh empty)
    - `dotnet build -c Debug --nologo --verbosity minimal` exit code = 0
  </acceptance_criteria>

  <done>
    - Cross-cut audit Plan 01 + Plan 02 — semua label lama hilang dari kode + dokumentasi
    - E2E test directory ZERO match (D-15 verified)
    - PDF + screenshot di-flag deferred manual user (D-14) untuk dimasukkan ke SUMMARY
    - Build sukses
    - Audit report siap di-include di 305-02-SUMMARY.md
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — dokumentasi browse + sign-off final phase 305</name>

  <files>(verification target — no modifications)</files>

  <read_first>
    - All 8 file Plan 02 (state final)
    - 305-CONTEXT.md D-13 D-14 D-15 (deferred items)
    - 305-VALIDATION.md (Manual-Only Verifications section)
  </read_first>

  <what-built>
    - 8 file dokumentasi (7 di wwwroot/documents/, 1 di docs/) di-update label tipe ke "Single Choice"/"Multiple Answers" sesuai konteks
    - DB enum value verified tetap `MultipleChoice`/`MultipleAnswer`/`Essay` (Task 2 sign-off)
    - E2E test ZERO match label tipe (Task 3 audit)
    - PDF panduan + screenshot training di-flag deferred manual user (D-14)
    - Plan 01 (helper + view + controller) sudah human-verified (305-01 Task 9)
    - Phase 305 LBL-01 COMPLETE
  </what-built>

  <action>
Pause execution, instruksikan user untuk verifikasi visual dokumentasi di browser dan sign-off final phase 305. Tunggu user mengetik "approved" atau melaporkan masalah.

Ikuti 6 langkah verifikasi visual + 1 dokumentasi step:
  </action>

  <how-to-verify>
1. Jalankan aplikasi: `dotnet run` atau via IIS Express. Aplikasi harus serve `wwwroot/documents/` via StaticFiles middleware.

2. **Browse dokumentasi panduan website** (`Panduan-Penggunaan-Website-HC-Portal-KPB.html`):
   - Buka `https://localhost:{port}/documents/guides/Panduan-Penggunaan-Website-HC-Portal-KPB.html` (atau path StaticFiles serve)
   - Cari section tabel tipe soal — verify `<strong>Single Choice</strong>` (bukan "Pilihan Ganda")
   - Spot check: tabel masih readable, formatting OK, label baru muncul

3. **Browse Release Notes** (`Release-Notes-HC-Portal-KPB.html`):
   - Buka URL serupa
   - Section header tabel tipe: "Single Choice (MC)" + "Multiple Answers (MA)" + "Essay"
   - Deskripsi cell sudah di-rephrase: "Soal dengan satu jawaban benar..." (bukan "Pilihan ganda, satu jawaban benar")
   - Caption screenshot: "...dropdown Single Choice (MC) / Multiple Answers (MA) / Essay" (bukan label lama)

4. **Browse Penjelasan Halaman** (`Penjelasan-Halaman-PortalHC-KPB.html`):
   - Section worker exam description (line 315): "Mendukung tipe soal Single Choice, Multiple Answers, dan Essay."
   - Section admin Manage Questions (line 532): "Mendukung tipe Single Choice, Multiple Answers, dan Essay."
   - Section Excel import (line 538): "format berbeda untuk MC, Multiple Answers, Essay, dan Mix" (MC abbrev keep, Multiple Answers updated)

5. **Browse Struktur Website** (`Struktur-Website-PortalHC-KPB.html`):
   - Section import format: "Import soal format Essay / Multiple Answers / Mix"
   - Section scoring description: "Menghitung skor ujian otomatis: Single Choice, Multiple Answers, Essay."

6. **Browse TKI Draft** (`Draft-BAB-X-INSTRUKSI-KERJA.html`):
   - Step 216: "Tipe soal yang tersedia: (1) Single Choice; (2) Multiple Answers; (3) Essay; (4) Mix."
   - Step 337: "Download template sesuai tipe: Essay / Multiple Answers / Mix..."
   - Lampiran I: "Template Excel Import Soal (Essay / Multiple Answers / Mix)"

7. **Buka docs Persiapan Test** (`docs/Persiapan-Test-Manual-Assessment.html` — note: di `docs/` bukan `wwwroot/documents/`, akses via file:// URL atau via VS Code preview):
   - Tabel contoh: row 1 "Single Choice" (bukan "Multiple Choice"), row 2 "Multiple Answers" (bukan "Multiple Answer")

**Final sign-off checks (D-14 deferred):**
- Konfirmasi user paham bahwa **PDF panduan** (file `.pdf` di `wwwroot/documents/`) belum regen — manual task user setelah deployment
- Konfirmasi user paham bahwa **screenshot training material** (di lokasi terpisah, tidak ter-track Git) — manual screenshot ulang setelah deployment

**Phase 305 final summary:**
- LBL-01 fully implemented (Plan 01 + Plan 02)
- 6 surface UI menampilkan label baru (Plan 01)
- 4 controller flash error pakai helper (Plan 01)
- 8 dokumentasi konsisten label baru (Plan 02)
- DB enum value tetap (Plan 02 Task 2 verified)
- E2E test ZERO match (D-15 baseline)
- PDF + screenshot regen = deferred manual user task (D-14)
  </how-to-verify>

  <verify>
    <automated>echo "MANUAL DOC VERIFICATION — wait for human resume-signal 'approved' for phase 305 final sign-off"</automated>
  </verify>

  <acceptance_criteria>
    - User mengetik "approved" setelah browse 6 file dokumentasi + acknowledge 2 deferred items (PDF + screenshot)
    - Atau user melaporkan issue spesifik untuk perbaikan
  </acceptance_criteria>

  <done>
    - Human verifier konfirmasi 6 file dokumentasi visual OK
    - User acknowledge PDF + screenshot deferred manual task (D-14)
    - Phase 305 LBL-01 COMPLETE — siap untuk RETROSPECTIVE / next phase 306
  </done>

  <resume-signal>
    Type "approved" jika 6 file dokumentasi browse OK dan 2 deferred items acknowledged. Jika ada masalah, jelaskan: (a) file mana, (b) section mana, (c) actual vs expected text, (d) screenshot kalau perlu.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ ASP.NET Core StaticFiles middleware | Serve `wwwroot/documents/*.html`, `*.md`, `*.py` direct dari folder. No template rendering, no Razor evaluation, no user input parsing. |
| User filesystem ↔ DB (manual query Task 2) | Read-only `SELECT DISTINCT` query — no data mutation, no INSERT/UPDATE/DELETE. |
| Editor ↔ Static doc files | Plain text file edit — XSS risk dalam content yang akhirnya rendered di browser sebagai HTML doc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-305-09 | Tampering | Static HTML doc edit memasukkan unsafe HTML/script | mitigate | Edit hanya plain text label rename ("Pilihan Ganda" → "Single Choice"), tidak menambah `<script>` / `<iframe>` / inline event handler. Browser render sebagai static HTML — tidak ada eval. Reviewer wajib visual verify Task 4 (no broken markup). |
| T-305-10 | Information Disclosure | Documentation expose internal taxonomy | accept | "Single Choice"/"Multiple Answers" adalah label LMS-standard public-facing. Documentation memang dimaksud untuk user training — tidak ada secret. |
| T-305-11 | Tampering | Python source `generate_bab_x.py` jika di-eksekusi otomatis akan regen HTML | accept | File `generate_bab_x.py` adalah developer regenerator (offline tool), bukan deployed application code. ASP.NET Core tidak execute Python. Edit string literal aman — tidak menambah malicious code. |
| T-305-12 | Tampering | DB query Task 2 melalui SSMS dengan elevated credentials | accept | User explicit menjalankan read-only `SELECT DISTINCT` — no data mutation. Auth via SSMS connection string, tetap requires DB user privilege existing. Tidak ada credential leak melalui plan. |
| T-305-13 | Repudiation | Audit log impact dari label rename docs | n/a | Documentation cosmetic edit. Tidak ada server-side state change. |

**ASVS L1 alignment:**
- V5.2.1 (output encoding di static HTML): Static HTML serve via StaticFiles — browser parse standard, label string adalah plain text content. Compliant.
- V11.1.1 (no security logic in client): Documentation cosmetic — compliant.

**No high-risk threat — security_enforcement gate: PASS.**

**Phase 305 Plan 02 risk profile:** Documentation rename + DB read-only verify. No new attack surface. No code execution path baru. DB query Task 2 adalah read-only `SELECT` — no privilege escalation, no data mutation.
</threat_model>

<verification>
## Phase-Level Verification

Setelah semua 4 task selesai:

1. **Static checks (otomatis):**
   ```bash
   # Label lama hilang dari 8 file dokumentasi
   grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Choice" wwwroot/documents/ docs/ 2>/dev/null | wc -l   # = 0
   grep -rn "Multiple Answer[^s]" wwwroot/documents/ docs/ 2>/dev/null | wc -l                            # = 0

   # Label baru ada
   grep -rn "Single Choice" wwwroot/documents/ docs/ 2>/dev/null | wc -l                                  # >= 8
   grep -rn "Multiple Answers" wwwroot/documents/ docs/ 2>/dev/null | wc -l                               # >= 10

   # Cross-cut Plan 01 + Plan 02 — full tree sweep
   grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Choice" \
     --include="*.cs" --include="*.cshtml" --include="*.html" --include="*.md" --include="*.py" \
     --exclude-dir=".planning" --exclude-dir=".git" --exclude-dir="bin" --exclude-dir="obj" --exclude-dir="node_modules" \
     Models/ Views/ Controllers/ wwwroot/documents/ docs/ 2>/dev/null | wc -l   # = 0

   # E2E (D-15)
   grep -rn "Pilihan Ganda\|Multi Jawaban\|Multiple Answer\|Multiple Choice\|Single Choice\|Multiple Answers" tests/e2e/ 2>/dev/null | wc -l   # = 0

   # Build check (sanity)
   dotnet build -c Debug --nologo --verbosity minimal   # exit 0
   ```

2. **DB query verification** — Task 2 checkpoint (manual SQL).

3. **Visual documentation verification** — Task 4 checkpoint (browse 6 file).

4. **Deferred items tracked:**
   - PDF panduan binary regen — user manual task (D-14)
   - Screenshot training material regen — user manual task (D-14)
   - Excel template binary content — backward compat dengan enum value (D-18, no change)
</verification>

<success_criteria>
- [x] LBL-01 success criteria #4 covered: Documentation cross-cutting (8 file updated)
- [x] LBL-01 success criteria #5 covered: DB query verifikasi enum unchanged (Task 2)
- [x] D-13 honored: 8 file context-aware sed-replace (manual review per occurrence, bukan blind sed)
- [x] D-14 honored: PDF + screenshot di-flag deferred manual user task
- [x] D-15 honored: E2E test ZERO match verified (no edit needed)
- [x] D-17 honored: DB enum value `MultipleChoice`/`MultipleAnswer`/`Essay` tetap (Task 2)
- [x] D-18 honored: Excel template binary tidak diubah, bullet helper enum value tetap (handled di Plan 01 Task 6)
- [x] D-20 honored: post-implementation DB query run + result captured
- [x] Phase 305 Plan 01 + Plan 02 cross-cut: zero residual label lama di kode + dokumentasi
- [x] Build .NET sukses (sanity check static file edits)
- [x] Human verification (Task 4) approved — phase 305 LBL-01 COMPLETE
- [x] threat_model: T-305-09..13, ASVS L1 compliant, no high-risk
</success_criteria>

<output>
Setelah completion, buat `.planning/phases/305-question-type-naming-clarity/305-02-SUMMARY.md` dengan template summary standar:
- Files modified: 8 EDIT (`wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA.html`, `wwwroot/documents/TKI/Draft-BAB-X-INSTRUKSI-KERJA-outline.md`, `wwwroot/documents/TKI/generate_bab_x.py`, 4 file di `wwwroot/documents/guides/`, 1 file di `docs/`)
- Tasks completed: 4 (2 auto + 2 checkpoint)
- LBL-01 status: 8 file dokumentasi updated + DB enum verified + E2E zero match — phase 305 COMPLETE
- Linkages: D-13 sampai D-20 (CONTEXT.md), Pitfall #1 #2 (RESEARCH.md), section 8 (PATTERNS.md), task 305-02-01..03 (VALIDATION.md)
- Threats covered: T-305-09..13 (mitigate/accept/n.a.)
- Audit report inline (output Task 3 Step 6)
- Deferred manual user tasks (D-14):
  - PDF panduan regenerasi (`wwwroot/documents/*.pdf`) — user generate ulang dari source dokumen setelah deployment label baru live
  - Screenshot training material regenerasi — user screenshot ulang halaman /Admin/ManagePackageQuestions, /CMP/StartExam, /CMP/ExamSummary, /Admin/ImportPackageQuestions setelah deployment
- Notes: 8 file edit (semua static doc, no code execution path), 0 dependency baru, 0 DB schema change (verified via Task 2 query), 0 Excel binary change (D-18 backward compat).
- Hand-off: Phase 305 LBL-01 COMPLETE. Update ROADMAP.md status `Phase 305 [x]`, STATE.md milestone progress 50% → 75%. Next phase 306 (QSCR-01 Score Editable).
</output>
