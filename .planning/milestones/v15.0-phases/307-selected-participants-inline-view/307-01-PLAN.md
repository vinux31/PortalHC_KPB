---
phase: 307-selected-participants-inline-view
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/assessment.spec.ts
  - tests/e2e/helpers/wizardSelectors.ts
  - .planning/phases/307-selected-participants-inline-view/307-UAT.md
autonomous: true
requirements:
  - WIZ-01
tags:
  - testing
  - e2e
  - playwright
  - wave-0
  - wizard
user_setup: []

must_haves:
  truths:
    - "E2E test file `tests/e2e/assessment.spec.ts` punya describe block `Phase 307 Selected Participants Panel` dengan minimal 4 test cases (initial empty state, real-time toggle, expand-collapse, reset)"
    - "Selector module `tests/e2e/helpers/wizardSelectors.ts` mengekspor 5 konstanta selector untuk panel Phase 307 (panelWrapper, panelBody, panelCount, summaryListContainer, summaryCount)"
    - "Pre-existing rot di `tests/e2e/assessment.spec.ts` line 45 (`'2 selected'`) di-fix menjadi `'2 terpilih'` (sesuai literal text Bahasa Indonesia di production)"
    - "Manual UAT script `.planning/phases/307-selected-participants-inline-view/307-UAT.md` ada dengan 5 step Bahasa Indonesia yang cover 5 success criteria phase (initial render + real-time toggle + bulk operations + performance budget + reset/Proton)"
    - "`npx playwright test e2e/assessment.spec.ts --grep 'Phase 307' --list` mengembalikan tepat 4 test names (smoke verification — actual run depend pada implementasi Wave 1)"
  artifacts:
    - path: "tests/e2e/helpers/wizardSelectors.ts"
      provides: "Phase 307 selector constants module"
      exports: ["selectors"]
    - path: "tests/e2e/assessment.spec.ts"
      provides: "Phase 307 E2E test scaffold + opportunistic rot fix"
      contains: "test.describe('Assessment - Phase 307 Selected Participants Panel'"
    - path: ".planning/phases/307-selected-participants-inline-view/307-UAT.md"
      provides: "Manual UAT 5-step Bahasa Indonesia"
      min_lines: 50
  key_links:
    - from: "tests/e2e/assessment.spec.ts (Phase 307 describe block)"
      to: "tests/e2e/helpers/wizardSelectors.ts"
      via: "import { selectors } from './helpers/wizardSelectors'"
      pattern: "import.*wizardSelectors"
    - from: "tests/e2e/assessment.spec.ts line 45"
      to: "production text di CreateAssessment.cshtml line 289 default '0 terpilih'"
      via: "string literal '2 terpilih' (post-fix)"
      pattern: "'2 terpilih'"
---

<objective>
Wave 0: Buat test infrastructure scaffold sebelum implementasi Wave 1 dimulai. Plan ini meletakkan kontrak verifikasi (E2E describe block + selector constants + manual UAT script) sehingga executor Wave 1 dapat langsung jalankan smoke test setelah setiap edit, dan reviewer punya checklist konkret untuk verify success criteria.

Purpose: Implementasi panel "Peserta Terpilih" Step 2 memerlukan validasi behavior real-time (success criteria #2), parity Step 2/Step 4 (success criteria #3, #5), dan performance budget < 200ms (success criteria #4). Tanpa test scaffold yang siap, executor harus improvisasi assertion → risiko coverage gap. Wave 0 menutup gap ini.

Output: 1 file baru (selectors helper), 1 file di-extend (assessment.spec.ts dengan Phase 307 describe + opportunistic rot fix line 45), 1 file baru (307-UAT.md manual UAT script Bahasa Indonesia).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/307-selected-participants-inline-view/307-CONTEXT.md
@.planning/phases/307-selected-participants-inline-view/307-RESEARCH.md
@.planning/phases/307-selected-participants-inline-view/307-PATTERNS.md
@.planning/phases/307-selected-participants-inline-view/307-VALIDATION.md
@CLAUDE.md
@tests/e2e/assessment.spec.ts
@tests/helpers/auth.ts

<interfaces>
<!-- Konvensi existing yang harus diikuti — extracted dari codebase. -->
<!-- Executor TIDAK perlu eksplorasi codebase untuk dapat informasi ini. -->

Existing test pattern di tests/e2e/assessment.spec.ts (FLOW 1 line 21-92):
```typescript
test.describe.configure({ mode: 'serial' });

test.describe('Assessment - Admin Creates & Manages', () => {
  test('1.2 - HC can create a new assessment for workers', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');
    const rinoCheckbox = page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input');
    await rinoCheckbox.click({ force: true });
    await expect(page.locator('#selectedCountBadge')).toContainText('2 selected');  // ← LINE 45 ROT
    // ...
  });
});
```

Existing helper modules (folder tests/helpers/, FLAT — bukan tests/e2e/helpers/):
- tests/helpers/auth.ts — `export async function login(page: Page, account: AccountKey)`
- tests/helpers/utils.ts — `export function uniqueTitle(prefix: string): string`, `export function today(): string`
- tests/helpers/accounts.ts — `export const accounts`, `export type AccountKey`

Phase 307 selectors yang akan dipakai (DOM IDs dari Wave 1 markup):
- `#selected-participants-panel-wrapper` (root container, always visible)
- `#selected-participants-panel` (body — helper render target)
- `#selected-participants-count` (header badge — count "N peserta")
- `#summary-peserta-list-container` (Step 4 body, post-refactor Wave 1 Option A)
- `#summary-peserta-count` (Step 4 header badge — UNCHANGED)
- `#selectedCountBadge` (filter bar — UNCHANGED, "N terpilih" format)

Login helper signature (tests/helpers/auth.ts):
```typescript
import { login } from '../helpers/auth';
await login(page, 'hc');  // navigates ke /Account/Login dan submit, lalu wait redirect ke /Home/**
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat selector helper module untuk Phase 307</name>
  <files>tests/e2e/helpers/wizardSelectors.ts</files>
  <read_first>
    - tests/helpers/auth.ts (pattern existing helper module — `export async function`, simple top-level export)
    - tests/helpers/utils.ts (pattern function exports)
    - tests/helpers/accounts.ts (pattern const + type exports)
    - .planning/phases/307-selected-participants-inline-view/307-PATTERNS.md (item 9 — selector helper section)
    - .planning/phases/307-selected-participants-inline-view/307-CONTEXT.md (D-13 ID prefix `selected-participants-*`, Phase 304 D-18 stability untuk existing IDs)
  </read_first>
  <action>
Buat folder baru `tests/e2e/helpers/` (folder belum ada — verifikasi via `ls tests/e2e/helpers` returns 'No such file or directory') lalu tulis file `tests/e2e/helpers/wizardSelectors.ts` dengan VERBATIM content berikut:

```typescript
// Phase 307 — DOM ID selectors untuk Step 2 panel "Peserta Terpilih"
// Centralized selector constants — single source of truth untuk Playwright tests.
// IDs match production markup di Views/Admin/CreateAssessment.cshtml.

export const selectors = {
  // Phase 307 panel (Wave 1 markup — sesudah line 309 #userCheckboxContainer)
  panelWrapper: '#selected-participants-panel-wrapper',
  panelBody: '#selected-participants-panel',
  panelCount: '#selected-participants-count',

  // Step 4 summary (parity check target — Phase 307 markup refactor Option A)
  summaryListContainer: '#summary-peserta-list-container',
  summaryCount: '#summary-peserta-count',

  // Existing Step 2 (Phase 304 D-18 stability — TIDAK di-touch)
  filterBarBadge: '#selectedCountBadge',
  userContainer: '#userCheckboxContainer',
  protonContainer: '#protonUserCheckboxContainer',
} as const;
```

Catatan implementasi:
- Folder `tests/e2e/helpers/` BARU — buat folder dulu (mkdir / Write akan auto-create parent).
- Pakai `as const` di akhir agar TypeScript treat properties sebagai literal type (better autocomplete + type safety).
- Tidak perlu import — pure const export.
- TIDAK pakai `default export` — match pattern existing helpers (named export only).
  </action>
  <verify>
    <automated>cd tests &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/helpers/wizardSelectors.ts` exists (verify: `ls tests/e2e/helpers/wizardSelectors.ts` returns the path, no error)
    - `grep -n "export const selectors" tests/e2e/helpers/wizardSelectors.ts` returns line 5 (or similar early line, top-level export)
    - `grep -c "panelWrapper\|panelBody\|panelCount\|summaryListContainer\|summaryCount\|filterBarBadge\|userContainer\|protonContainer" tests/e2e/helpers/wizardSelectors.ts` returns count >= 8 (semua 8 selector keys present)
    - `grep -n "'#selected-participants-panel-wrapper'" tests/e2e/helpers/wizardSelectors.ts` returns satu line
    - `grep -n "'#selected-participants-panel'" tests/e2e/helpers/wizardSelectors.ts` returns satu line
    - `grep -n "'#summary-peserta-list-container'" tests/e2e/helpers/wizardSelectors.ts` returns satu line
    - `grep -n "as const" tests/e2e/helpers/wizardSelectors.ts` returns satu line di akhir block
    - `cd tests && npx tsc --noEmit -p tsconfig.json` exit code 0 (TypeScript compile success — file berkonform dengan tsconfig)
  </acceptance_criteria>
  <done>
File `tests/e2e/helpers/wizardSelectors.ts` exists, contain 8 selector constants dengan literal IDs match Phase 307 spec, TypeScript compile lulus tanpa error, tidak ada implicit-any atau syntax error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/e2e/assessment.spec.ts dengan Phase 307 describe block + opportunistic rot fix</name>
  <files>tests/e2e/assessment.spec.ts</files>
  <read_first>
    - tests/e2e/assessment.spec.ts (full file — pahami pattern describe + login + locator + expect, terutama FLOW 1 line 21-92)
    - tests/helpers/auth.ts (verify login signature)
    - tests/helpers/utils.ts (verify uniqueTitle/today/autoConfirm signature)
    - .planning/phases/307-selected-participants-inline-view/307-PATTERNS.md (item 8 — E2E test scaffold section, full target pattern)
    - .planning/phases/307-selected-participants-inline-view/307-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - .planning/phases/307-selected-participants-inline-view/307-RESEARCH.md (line 555-557 confirm rot di line 45)
  </read_first>
  <action>
DUA edit di file `tests/e2e/assessment.spec.ts`:

**Edit 1 (opportunistic rot fix — line 45):**
Locate string literal `'2 selected'` di line 45 (verify dengan `grep -n "'2 selected'" tests/e2e/assessment.spec.ts` returns line 45). Replace dengan `'2 terpilih'` agar match literal Bahasa Indonesia text di production (`Views/Admin/CreateAssessment.cshtml` line 289 default `0 terpilih`, dan `updateSelectedCount` line 1446 format `count + ' terpilih'`).

BEFORE (line 45):
```typescript
await expect(page.locator('#selectedCountBadge')).toContainText('2 selected');
```

AFTER:
```typescript
await expect(page.locator('#selectedCountBadge')).toContainText('2 terpilih');
```

**Edit 2 (extend dengan Phase 307 describe block — APPEND setelah test.describe FLOW 1 close `});` line 92):**
Tambahkan import baru dan describe block. Pastikan import statement `import { selectors } from './helpers/wizardSelectors';` ditambahkan di BAGIAN ATAS file setelah import existing (after line 3 `import { uniqueTitle, today, autoConfirm } from '../helpers/utils';`).

INSERT setelah line 92 (close `});` describe FLOW 1, sebelum `// ============================================================` FLOW 2 line 94):

```typescript
// ============================================================
// FLOW 7: Phase 307 — Selected Participants Inline Panel (Step 2)
// REQ: WIZ-01 (5 success criteria)
// ============================================================
test.describe('Assessment - Phase 307 Selected Participants Panel', () => {

  test('7.1 - Panel renders with empty state on initial load (success criteria #1)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    // Panel always-visible (D-02), empty state default
    await expect(page.locator(selectors.panelWrapper)).toBeVisible();
    await expect(page.locator(selectors.panelCount)).toContainText('0 peserta');
    await expect(page.locator(selectors.panelBody)).toContainText('Belum ada peserta dipilih');
  });

  test('7.2 - Panel updates real-time when checkbox toggled (success criteria #2)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    const rinoCheckbox = page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input');
    await rinoCheckbox.click({ force: true });

    // Count badge IMMEDIATE update (D-11 — count immediate)
    await expect(page.locator(selectors.panelCount)).toContainText('1 peserta');

    // Panel body update setelah debounce 100ms window
    await page.waitForTimeout(150);
    await expect(page.locator(selectors.panelBody)).not.toContainText('Belum ada peserta dipilih');
    await expect(page.locator(selectors.panelBody)).toContainText(/rino|prasetyo/i);
  });

  test('7.3 - Step 4 summary parity dengan Step 2 panel (success criteria #3, #5 DRY)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    const rinoCheckbox = page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input');
    const iwanCheckbox = page.locator('.user-check-item', { hasText: 'iwan3' }).locator('input');
    await rinoCheckbox.click({ force: true });
    await iwanCheckbox.click({ force: true });
    await page.waitForTimeout(150);

    // Capture Step 2 panel text — extract names only (strip "Belum ada" placeholder kalau ada)
    const step2Text = (await page.locator(selectors.panelBody).textContent())?.trim() ?? '';
    expect(step2Text).toMatch(/rino|prasetyo/i);
    expect(step2Text).toMatch(/iwan/i);

    // Verify count badge format — Step 2 panel header
    await expect(page.locator(selectors.panelCount)).toContainText('2 peserta');

    // Verify filter bar badge format — UNCHANGED (Phase 304 D-18 stability)
    await expect(page.locator(selectors.filterBarBadge)).toContainText('2 terpilih');

    // Note: Full Step 2 vs Step 4 visual parity assertion (advance wizard ke Step 4) di-defer ke manual UAT
    // karena wizard navigation flow + form validation memerlukan setup tambahan yang akan diisi
    // setelah Wave 1 implementasi merged. Manual UAT 307-UAT.md Step 5 cover ini.
  });

  test('7.4 - Reset clears panel ke empty state (success criteria #2 reset path)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    const rinoCheckbox = page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input');
    await rinoCheckbox.click({ force: true });
    await page.waitForTimeout(150);
    await expect(page.locator(selectors.panelBody)).toContainText(/rino|prasetyo/i);

    // Click "Batalkan Semua" untuk uncheck semua (deselectAllBtn — line 286 Bahasa Indonesia)
    await page.click('#deselectAllBtn');
    await page.waitForTimeout(150);

    // Panel kembali ke empty state, count 0
    await expect(page.locator(selectors.panelCount)).toContainText('0 peserta');
    await expect(page.locator(selectors.panelBody)).toContainText('Belum ada peserta dipilih');
    await expect(page.locator(selectors.filterBarBadge)).toContainText('0 terpilih');
  });
});
```

Catatan implementasi:
- TOTAL 4 test cases — cover 5 success criteria (test 7.3 cover #3 + #5 sekaligus, performance #4 di-defer ke manual UAT karena flaky di CI per VALIDATION.md `Manual-Only Verifications`).
- Pakai `selectors.X` dari import — single source of truth.
- `page.waitForTimeout(150)` setelah click — wait debounce 100ms window + slack 50ms.
- Test 7.4 dependent pada Wave 1 item 7 (reset handler edit) — kalau executor Wave 1 lupa edit reset handler, test 7.4 akan FAIL → guard yang tepat.
- TIDAK menambahkan test untuk performance budget #4 — manual UAT step 4 cover.
- TIDAK menambahkan test full Step 2 vs Step 4 advance-wizard parity — manual UAT step 5 cover (E2E advance wizard butuh fill form lengkap, scope creep dari Wave 0).
  </action>
  <verify>
    <automated>cd tests &amp;&amp; npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --list 2>&amp;1 | grep -E "(7\.[1-4]|tests in)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'2 terpilih'" tests/e2e/assessment.spec.ts` returns line 45 (rot fix completed)
    - `grep -n "'2 selected'" tests/e2e/assessment.spec.ts` returns ZERO matches (rot fix complete, no leftover)
    - `grep -n "import { selectors } from './helpers/wizardSelectors'" tests/e2e/assessment.spec.ts` returns ONE line di top of file (between line 1 and 5)
    - `grep -n "test.describe('Assessment - Phase 307 Selected Participants Panel'" tests/e2e/assessment.spec.ts` returns ONE line
    - `grep -c "test('7\.[1-4]" tests/e2e/assessment.spec.ts` returns 4 (tepat 4 test cases dalam Phase 307 describe)
    - `grep -n "selectors.panelWrapper\|selectors.panelBody\|selectors.panelCount" tests/e2e/assessment.spec.ts` returns minimum 5 references (tests menggunakan helper selectors)
    - `grep -n "Belum ada peserta dipilih" tests/e2e/assessment.spec.ts` returns minimum 2 lines (test 7.1 + test 7.4 verify empty state)
    - `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --list` exit code 0 dan output contain `4 tests` atau `7.1`, `7.2`, `7.3`, `7.4` test names
    - `cd tests && npx tsc --noEmit -p tsconfig.json` exit code 0 (TypeScript compile lulus, import baru valid)
  </acceptance_criteria>
  <done>
File `tests/e2e/assessment.spec.ts` extend dengan: (a) opportunistic rot fix line 45 `'2 selected'` → `'2 terpilih'`, (b) import statement untuk `selectors` dari `./helpers/wizardSelectors`, (c) describe block `Phase 307 Selected Participants Panel` dengan 4 test cases (7.1 initial empty, 7.2 real-time toggle, 7.3 parity & format, 7.4 reset). Playwright `--list` lulus, TypeScript compile lulus.
  </done>
</task>

<task type="auto">
  <name>Task 3: Buat manual UAT script Bahasa Indonesia 5-step</name>
  <files>.planning/phases/307-selected-participants-inline-view/307-UAT.md</files>
  <read_first>
    - .planning/phases/307-selected-participants-inline-view/307-PATTERNS.md (item 10 — Manual UAT Script section dengan target pattern verbatim)
    - .planning/phases/307-selected-participants-inline-view/307-VALIDATION.md (Wave 0 Requirements list 5-step structure + Manual-Only Verifications table)
    - .planning/phases/307-selected-participants-inline-view/307-CONTEXT.md (5 success criteria mapping + D-XX/CD-XX wording references)
    - .planning/phases/306-score-editable-per-question-type/ (kalau ada UAT.md / VALIDATION.md untuk mirror style — Phase 306 punya 10-step UAT pattern per VALIDATION.md reference)
  </read_first>
  <action>
Tulis file BARU `.planning/phases/307-selected-participants-inline-view/307-UAT.md` dengan content berikut (Bahasa Indonesia, 5 step format action → expect mirror Phase 306 style):

```markdown
# Phase 307 — Manual UAT Script

**Phase:** 307-selected-participants-inline-view
**REQ:** WIZ-01 (Selected Participants Inline View Step 2)
**Created:** 2026-04-29
**Mandatory before:** `/gsd-verify-work` Phase 307

> Manual UAT cover 5 success criteria Phase 307 yang tidak praktis di-automate via E2E (performance budget, visual parity Step 2 vs Step 4, Proton mode, reset modal flow). E2E `tests/e2e/assessment.spec.ts` Phase 307 describe (test 7.1-7.4) cover automated subset.

## Pre-conditions

- App di-build & running (development atau staging)
- Login sebagai HC atau Admin (kredensial lihat `tests/helpers/accounts.ts`)
- Browser modern: Chrome / Edge / Firefox versi terbaru (≥ 86 untuk `Element.replaceChildren` support — fallback `innerHTML = ''; appendChild(frag)` ada di helper kalau legacy)
- DevTools terbuka untuk Step 4 (Console + Performance tab)
- Database seed minimal 50+ user untuk test bulk operations Step 4

---

## Step 1 — Initial render & empty state (success criteria #1, #2)

**Action:**
1. Navigasi ke `/Admin/CreateAssessment` (Create mode, fresh form)
2. Pilih Category "Assessment OJT" (atau category non-Proton lainnya)
3. Scroll ke section Step 2 ("Pilih Peserta")
4. Inspeksi area di bawah `#userCheckboxContainer` (di atas `#protonEligibleSection`)

**Expect:**
- Panel `#selected-participants-panel-wrapper` tampil dengan border + rounded + padding (Bootstrap mini-card style)
- Header panel menampilkan ikon people + teks **"Peserta Terpilih"** + badge `bg-primary` dengan text **"0 peserta"**
- Body panel `#selected-participants-panel` menampilkan placeholder italic muted **"Belum ada peserta dipilih"**
- Filter bar badge `#selectedCountBadge` (line 289 existing) tetap menampilkan **"0 terpilih"** (Phase 304 D-18 stability — tidak di-touch)
- Tidak ada layout shift saat page load (panel always-visible default)

---

## Step 2 — Real-time toggle (success criteria #2)

**Action:**
1. Centang 1 peserta (random pilih nama yang ada di list)
2. Observasi badge `#selected-participants-count` dan body `#selected-participants-panel`
3. Centang 4 peserta tambahan (total 5 dipilih)
4. Centang 1 peserta lagi (total 6 dipilih)
5. Klik tombol "...dan 1 lainnya" yang muncul
6. Klik tombol "Sembunyikan" untuk collapse

**Expect:**
- Saat centang 1: count badge **immediate** ke "1 peserta", body update dalam ≤ 150ms tampilkan nama peserta
- Saat 5 dipilih: body tampil 5 nama joined dengan `', '`, **tidak ada** tombol expand
- Saat 6 dipilih: body tampil 5 nama + tombol `btn-link` text **"...dan 1 lainnya"**
- Klik tombol expand: nama ke-6 muncul di-append ke list, tombol berubah text jadi **"Sembunyikan"**
- Klik Sembunyikan: nama ke-6 hilang, tombol kembali ke **"...dan 1 lainnya"**
- Filter bar badge `#selectedCountBadge` parallel update ke "6 terpilih" (existing behavior preserved)

---

## Step 3 — Bulk operations Pilih Semua / Batalkan Semua (success criteria #2)

**Action:**
1. Refresh page (clean state)
2. Pilih Category "Assessment OJT"
3. Klik tombol **"Pilih Semua"** (id `#selectAllBtn`)
4. Observasi panel render — should batch single-render (debounce 100ms window absorb 50+ sequential `cb.checked = true`)
5. Klik tombol **"Batalkan Semua"** (id `#deselectAllBtn`)

**Expect:**
- Setelah Pilih Semua: count badge ke "N peserta" (N = jumlah user visible), body render single batch (NO flicker, NO multiple re-render visible)
- Visual: panel body tampil 5 nama + tombol "...dan (N-5) lainnya"
- Filter bar badge update ke "N terpilih"
- Setelah Batalkan Semua: panel kembali ke empty state **"Belum ada peserta dipilih"**, count "0 peserta", filter badge "0 terpilih"

---

## Step 4 — Performance budget instrumentation 50+ peserta (success criteria #4)

**Action:**
1. Pre-condition: pastikan list memiliki ≥ 50 user visible (kalau database kurang, tambah seed atau filter section yang besar)
2. Buka DevTools → Console
3. Refresh page, pilih Category "Assessment OJT"
4. Run di Console:
   ```javascript
   performance.mark('p307-start');
   document.getElementById('selectAllBtn').click();
   setTimeout(() => {
       performance.mark('p307-end');
       performance.measure('p307-render', 'p307-start', 'p307-end');
       const measure = performance.getEntriesByName('p307-render')[0];
       console.log('[Phase 307] selectAll → panel render delta:', measure.duration.toFixed(2), 'ms');
   }, 200);
   ```
5. Capture screenshot Console output

**Expect:**
- Output console: `[Phase 307] selectAll → panel render delta: X.XX ms`
- **X < 200** (success criteria #4 — < 200ms untuk 50+ peserta)
- Tidak ada error / warning di Console terkait Phase 307 namespace
- Catatan: 100ms dari debounce + render time aktual. Untuk 50 peserta expected render < 10ms, total budget 100-150ms typical.

**Evidence to capture:**
- Screenshot Console output dengan delta value
- Browser version + OS info untuk reproducibility

---

## Step 5 — Reset modal "Buat lagi" + Proton mode parity (success criteria #2 reset path, #3, #5)

**Action:**
1. Refresh page, pilih Category "Assessment OJT"
2. Pilih 3 peserta
3. Fill required fields (Title, Schedule, Duration, PassPercentage)
4. Submit form (`#submitBtn`)
5. Tunggu modal sukses + tombol "Buat lagi" muncul
6. Klik **"Buat lagi"** di modal
7. **Expect (reset path):** Panel `#selected-participants-panel` revert ke "Belum ada peserta dipilih", `#selected-participants-count` ke "0 peserta", `#selectedCountBadge` ke "0 terpilih"
8. Pilih Category **"Assessment Proton"**
9. Pilih track Proton dari dropdown (`#protonTrackSelect`)
10. Tunggu AJAX load checkbox eligible coachee
11. Centang 2 coachee
12. **Expect (Proton mode):** Panel update real-time menampilkan nama coachee — sumber data switch dari `#userCheckboxContainer` ke `#protonUserCheckboxContainer` (D-03 single panel switch by isProton)

**Expect (visual parity Step 2 vs Step 4 — manual side-by-side):**
13. Lanjut wizard ke Step 4 (klik tombol Next/Confirm sampai Step 4 summary)
14. Side-by-side compare: Step 2 panel `#selected-participants-panel` vs Step 4 summary `#summary-peserta-list-container`
15. **Expect:** Same nama, same urutan (assuming `:checked` query order deterministic), same format (joined `', '`), same expand button behavior
16. Capture side-by-side screenshot sebagai evidence

---

## Sign-off Checklist

- [ ] Step 1: Initial empty state + always-visible verified
- [ ] Step 2: Real-time toggle 1/5/6 peserta + expand/collapse verified
- [ ] Step 3: Pilih Semua / Batalkan Semua single-batch render verified
- [ ] Step 4: Performance delta < 200ms captured + screenshot
- [ ] Step 5: Reset "Buat lagi" + Proton mode + Step 2/Step 4 parity verified

**Tester name:** ____________________
**Tested at:** ____________________ (timestamp)
**Browser/version:** ____________________
**Result:** PASS / FAIL (jika FAIL — note step number + observed vs expected di section bawah)

**Notes / observed deviations:**
_____________________________________________________________
_____________________________________________________________

---

*Phase: 307-selected-participants-inline-view*
*UAT script created: 2026-04-29*
```

Catatan implementasi:
- 5 step cover 5 success criteria (Step 1=#1, Step 2=#2, Step 3=#2 bulk, Step 4=#4 perf, Step 5=#2 reset + #3/#5 parity)
- Format action → expect mirror Phase 306 prose style yang Bahasa Indonesia
- Step 4 `performance.now()` instrumentation script verbatim — copy-paste-able ke Console
- Sign-off checklist + tester field di akhir untuk audit trail
  </action>
  <verify>
    <automated>test -f .planning/phases/307-selected-participants-inline-view/307-UAT.md &amp;&amp; wc -l .planning/phases/307-selected-participants-inline-view/307-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/307-selected-participants-inline-view/307-UAT.md` exists
    - `wc -l .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns ≥ 50 lines (substantive UAT script, bukan stub)
    - `grep -n "## Step [1-5]" .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns tepat 5 lines (Step 1 sampai Step 5)
    - `grep -n "Belum ada peserta dipilih" .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns minimum 2 lines (Step 1 + Step 3 verify empty state)
    - `grep -n "performance.mark\|performance.measure" .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns minimum 2 lines (Step 4 instrumentation)
    - `grep -n "200ms\|< 200" .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns minimum 1 line (Step 4 budget threshold)
    - `grep -n "Buat lagi\|Pilih Semua\|Batalkan Semua" .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns minimum 3 lines (Bahasa Indonesia button label references)
    - `grep -n "Assessment Proton" .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns minimum 1 line (Step 5 Proton scenario)
    - `grep -n "## Sign-off Checklist" .planning/phases/307-selected-participants-inline-view/307-UAT.md` returns 1 line (audit trail section ada)
  </acceptance_criteria>
  <done>
File `307-UAT.md` ada di phase directory, mengandung 5 step Bahasa Indonesia (Initial empty / Real-time toggle / Bulk ops / Performance / Reset+Proton+parity), Step 4 punya `performance.now()` instrumentation script copy-paste-able, sign-off checklist dengan tester field di akhir.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → DevServer | Playwright login dengan test credentials → trust boundary, namun scope Wave 0 hanya test scaffold (tidak menyentuh production credential management) |
| Test code → CI/CD pipeline | Test file di-commit ke repo → harus tidak mengandung secret atau hardcoded production URL |

## STRIDE Threat Register

Phase 307 attack surface terbatas pada client-side DOM rendering. Wave 0 (this plan) HANYA membuat test infrastructure — tidak menambah attack surface baru. Threat model untuk Wave 1 implementasi di-handle di plan 02.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-307-W0-01 | Information Disclosure | tests/e2e/assessment.spec.ts (Phase 307 tests) | accept | Test pakai login helper existing yang baca dari `tests/helpers/accounts.ts` (test-only credentials, non-production). Tidak ada secret hardcoded di Phase 307 test code. Selectors helper pure DOM ID constants — no sensitive data. |
| T-307-W0-02 | Tampering | tests/e2e/helpers/wizardSelectors.ts | accept | Pure const export, no input handling. Risk profile zero — tidak ada user input atau dynamic state. |
| T-307-W0-03 | Repudiation | .planning/phases/307-.../307-UAT.md | accept | Manual UAT script dengan sign-off section memiliki tester name + timestamp field — audit trail intentionally part of design. |

**Catatan:** Wave 1 plan akan punya threat model lengkap untuk T-307-01 (XSS via FullName), T-307-02 (DOM clobbering), T-307-03 (listener leak) — semua relevant pada implementasi helper render aktual.
</threat_model>

<verification>
**Phase-level verification untuk Wave 0:**

1. Folder `tests/e2e/helpers/` exists (created by Task 1).
2. `tests/e2e/helpers/wizardSelectors.ts` exists, TypeScript compile lulus.
3. `tests/e2e/assessment.spec.ts` di-extend: rot fix line 45 + Phase 307 describe (4 tests) + import statement.
4. `cd tests && npx playwright test e2e/assessment.spec.ts --list` exit 0 dan list 4 Phase 307 tests.
5. `cd tests && npx tsc --noEmit -p tsconfig.json` exit 0.
6. `.planning/phases/307-selected-participants-inline-view/307-UAT.md` exists, ≥ 50 lines, 5 step Bahasa Indonesia.
7. **Smoke run (optional, Wave 1 dependent):** Phase 307 test akan FAIL saat run sebelum Wave 1 implementasi (selector `#selected-participants-panel-wrapper` belum ada) — itu EXPECTED. Failure → indicates test infrastructure ready untuk RED→GREEN cycle pada Wave 1.

**Smoke command sequence post-Wave-0:**
```bash
cd tests && npx tsc --noEmit -p tsconfig.json   # compile clean
cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --list   # 4 tests listed
cd tests && npx playwright test e2e/assessment.spec.ts --grep "1\\.2"   # FLOW 1 test still pass (rot fix didn't break)
```
</verification>

<success_criteria>
Wave 0 complete saat:
- [ ] Task 1: `tests/e2e/helpers/wizardSelectors.ts` exists dengan 8 selector constants, TypeScript compile lulus
- [ ] Task 2: `tests/e2e/assessment.spec.ts` extend dengan import + Phase 307 describe (4 tests) + line 45 rot fix `'2 selected'` → `'2 terpilih'`, Playwright `--list` lulus, TypeScript compile lulus
- [ ] Task 3: `.planning/phases/307-selected-participants-inline-view/307-UAT.md` exists, ≥ 50 lines, 5 step Bahasa Indonesia (cover 5 success criteria), Step 4 punya `performance.now()` script
- [ ] All `<acceptance_criteria>` per task lulus
- [ ] No regression di FLOW 1 test (`1.2 - HC can create...`) — rot fix harus benar-benar match production text
- [ ] Phase 307 test cases READY untuk RED state (akan fail karena Wave 1 belum implement panel — itu EXPECTED dan benar)
</success_criteria>

<output>
After completion, create `.planning/phases/307-selected-participants-inline-view/307-01-SUMMARY.md` containing:
- Files created (count + paths)
- Files modified (count + paths)
- Test names registered (4 Phase 307 + 1 rot fix)
- UAT structure overview
- Verification commands run + exit codes
- Handoff notes untuk Wave 1 executor (selector names ready, helper module path, expected test failures pre-implementation)
</output>
