---
phase: 308-prepost-wizard-validation-fix
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/assessment.spec.ts
  - tests/e2e/helpers/wizardSelectors.ts
  - .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md
autonomous: true
requirements:
  - WIZ-04
tags:
  - testing
  - e2e
  - playwright
  - wave-0
  - wizard
  - validation
  - prepost
user_setup: []

must_haves:
  truths:
    - "E2E test file `tests/e2e/assessment.spec.ts` punya describe block `Phase 308 PrePost Wizard Validation` dengan tepat 4 test cases (8.1 Standard saja, 8.2 S→PP→S, 8.3 PP saja, 8.4 PP→S→PP) yang cover test matrix D-10"
    - "Selector module `tests/e2e/helpers/wizardSelectors.ts` di-extend dengan 4 selector baru untuk Phase 308 (`assessmentTypeInput`, `statusFieldWrapper`, `statusSelect`, `submitBtn`) plus 1 reference selector (`createForm` = `#createAssessmentForm` per RESEARCH form ID correction) — TIDAK menghapus 8 selector existing Phase 307"
    - "Manual UAT script `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` ada dengan 4 step Bahasa Indonesia (mirror Phase 307 5-step pattern) yang cover 4 kombinasi test matrix D-10 + sign-off section dengan tester name + browser version + result PASS/FAIL"
    - "`cd tests && npx tsc --noEmit -p tsconfig.json` exit 0 (TypeScript compile lulus untuk import baru + selector additions)"
    - "`cd tests && npx playwright test e2e/assessment.spec.ts --grep \"Phase 308\" --list` mengembalikan tepat 4 test names (8.1, 8.2, 8.3, 8.4) — actual run dependent pada Wave 1 (RED state expected pre-implementation)"
  artifacts:
    - path: "tests/e2e/helpers/wizardSelectors.ts"
      provides: "Extended selector constants module dengan Phase 308 additions"
      exports: ["selectors"]
    - path: "tests/e2e/assessment.spec.ts"
      provides: "Phase 308 E2E test scaffold (FLOW 8 describe block, 4 tests)"
      contains: "test.describe('Assessment - Phase 308 PrePost Wizard Validation'"
    - path: ".planning/phases/308-prepost-wizard-validation-fix/308-UAT.md"
      provides: "Manual UAT 4-step Bahasa Indonesia"
      min_lines: 50
  key_links:
    - from: "tests/e2e/assessment.spec.ts (Phase 308 describe block)"
      to: "tests/e2e/helpers/wizardSelectors.ts"
      via: "import { selectors } from './helpers/wizardSelectors' (existing — Phase 307 added)"
      pattern: "import.*wizardSelectors"
    - from: "Phase 308 tests selector reference"
      to: "Production form ID `#createAssessmentForm` (CreateAssessment.cshtml line 102)"
      via: "selectors.createForm constant"
      pattern: "'#createAssessmentForm'"
    - from: "Phase 308 tests 8.3 + 8.4 (PrePost path)"
      to: "Wave 1 implementation (D-01 JS value assignment + D-04 server ModelState.Remove)"
      via: "RED → GREEN cycle (test fail saat run pre-Wave-1, pass post-implementasi)"
      pattern: "test\\('8\\.[34]"
---

<objective>
Wave 0: Buat test infrastructure scaffold untuk Phase 308 sebelum implementasi Wave 1 dimulai. Plan ini meletakkan kontrak verifikasi (E2E describe block FLOW 8 + 4 selector tambahan + manual UAT 4-step Bahasa Indonesia) sehingga executor Wave 1 dapat langsung jalankan smoke test setelah setiap edit, dan reviewer punya checklist konkret untuk verify 4 kombinasi test matrix D-10.

Purpose: REQ WIZ-04 fix two-layer (JS value assignment + server ModelState.Remove) memerlukan validasi 4 kombinasi mode-switching (Standard saja, S→PP→S, PP saja, PP→S→PP) per success criteria #4. Tanpa test scaffold yang siap, executor harus improvisasi assertion → risiko coverage gap, terutama untuk regression guard #5 (Standard mode tetap require Status). Wave 0 menutup gap ini dan establish RED→GREEN cycle hygiene mirror precedent Phase 307.

Output: 1 file di-extend (selector module — APPEND 4-5 selector baru, TIDAK replace), 1 file di-extend (assessment.spec.ts — APPEND FLOW 8 describe block setelah FLOW 7 line 171), 1 file baru (308-UAT.md manual UAT 4-step Bahasa Indonesia dengan sign-off section).

Catatan: ROADMAP success criteria #3 ("jQuery validate re-parse") di-supersede oleh existing custom `validateStep(n)` visibility guard (CreateAssessment.cshtml line 996-1004) — RESEARCH.md Pitfall 2 verified `_ValidationScriptsPartial` 0 matches di file ini, jQuery validate plugin tidak loaded. Wave 0 test scaffold tidak perlu cover #3 secara automated; manual UAT akan note bahwa Status field correctly hidden via existing validateStep guard, tanpa jQuery re-parse code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/308-prepost-wizard-validation-fix/308-CONTEXT.md
@.planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md
@.planning/phases/308-prepost-wizard-validation-fix/308-VALIDATION.md
@.planning/phases/307-selected-participants-inline-view/307-01-PLAN.md
@CLAUDE.md
@tests/e2e/assessment.spec.ts
@tests/e2e/helpers/wizardSelectors.ts
@tests/helpers/auth.ts

<interfaces>
<!-- Verbatim DOM IDs, function signatures, dan struktur test existing yang executor butuh — extracted dari verifikasi langsung file. -->
<!-- Executor TIDAK perlu eksplorasi codebase untuk dapat informasi konkret. -->

EXISTING DOM IDs (production — VERIFIED via grep 2026-04-29):
- `#createAssessmentForm` — CreateAssessment.cshtml line 102 (form root, attribute `novalidate`) — **NOTE:** RESEARCH-corrected dari CONTEXT D-07/CD-01 yang menulis `#createForm` (TIDAK ADA — 0 matches)
- `#assessmentTypeInput` — Step 1 type selector dropdown (verified anchor di line 1862 — handler init)
- `#statusFieldWrapper` — Status field container div line 479 (toggled `d-none` oleh handler line 1880/1886)
- `#Status` — Status field `<select>` line 481 (asp-for binding)
- `#submitBtn` atau `button[type="submit"]` di dalam form — final wizard step submit trigger

EXISTING TEST INFRASTRUCTURE (verified post-Phase 307):
- `tests/e2e/assessment.spec.ts` (175 lines, 7 FLOW describe blocks: FLOW 1, 2, 3, 4, 5, 6, 7) — last describe close `});` di line 171 (Phase 307 FLOW 7 close)
- `tests/e2e/helpers/wizardSelectors.ts` (20 lines, 8 selector keys per Phase 307 D-15) — current state: `panelWrapper`, `panelBody`, `panelCount`, `summaryListContainer`, `summaryCount`, `filterBarBadge`, `userContainer`, `protonContainer`
- Import existing di assessment.spec.ts line 4: `import { selectors } from './helpers/wizardSelectors';` — ALREADY available, Phase 308 tinggal extend

EXISTING TEST PATTERNS (assessment.spec.ts FLOW 7 line 96-171 — Phase 307 precedent):
```typescript
test.describe('Assessment - Phase 307 Selected Participants Panel', () => {
  test('7.1 - Panel renders with empty state on initial load (success criteria #1)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');
    await expect(page.locator(selectors.panelWrapper)).toBeVisible();
    // ...
  });
});
```

EXISTING wizardSelectors.ts struktur (verbatim — APPEND, TIDAK REPLACE):
```typescript
export const selectors = {
  panelWrapper: '#selected-participants-panel-wrapper',
  panelBody: '#selected-participants-panel',
  panelCount: '#selected-participants-count',
  summaryListContainer: '#summary-peserta-list-container',
  summaryCount: '#summary-peserta-count',
  filterBarBadge: '#selectedCountBadge',
  userContainer: '#userCheckboxContainer',
  protonContainer: '#protonUserCheckboxContainer',
} as const;
```

LOGIN HELPER (tests/helpers/auth.ts — existing, no change):
```typescript
import { login } from '../helpers/auth';
await login(page, 'hc');  // navigates ke /Account/Login dan submit, lalu wait redirect ke /Home/**
```

FORM ID CORRECTION (RESEARCH-verified, OVERRIDE CONTEXT.md):
- CONTEXT D-07/CD-01 menulis `#createForm` — TIDAK ADA di production
- RESEARCH grep `id="createAssessmentForm"` → 1 match line 102 — INI yang correct
- Plan 308-01 Task 1 WAJIB pakai `#createAssessmentForm` di selector `submitBtn` dan `createForm` reference
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/e2e/helpers/wizardSelectors.ts dengan 4 selector baru Phase 308 + 1 form reference selector</name>
  <files>tests/e2e/helpers/wizardSelectors.ts</files>
  <read_first>
    - tests/e2e/helpers/wizardSelectors.ts (full file — verify struktur 8 selectors existing Phase 307 dengan `as const` di akhir)
    - .planning/phases/308-prepost-wizard-validation-fix/308-CONTEXT.md (D-13 selector list, CD-03 wave 0 scope decision)
    - .planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md (Verified Line Numbers table — anchor `#createAssessmentForm` line 102, `#statusFieldWrapper` line 479, `#Status` line 481, `#assessmentTypeInput` line 1862; Pitfall 1 form ID correction)
    - .planning/phases/307-selected-participants-inline-view/307-01-PLAN.md (Task 1 — pattern existing untuk extend selectors module)
    - .planning/phases/308-prepost-wizard-validation-fix/308-VALIDATION.md (Wave 0 Requirements — 4 selector baru required)
  </read_first>
  <action>
EXTEND file `tests/e2e/helpers/wizardSelectors.ts` (APPEND, TIDAK REPLACE — 8 selectors Phase 307 existing tetap, tambahkan 5 selectors baru di akhir block sebelum `} as const`).

BEFORE (current state — line 5-19, 8 selectors Phase 307):

```typescript
export const selectors = {
  // Phase 307 panel (Wave 1 markup — sesudah line 309 #userCheckboxContainer)
  panelWrapper: '#selected-participants-panel-wrapper',
  panelBody: '#selected-participants-panel',
  panelCount: '#selected-participants-count',

  // Step 4 summary (parity check target — Phase 307 markup refactor Option A)
  summaryListContainer: '#summary-peserta-list-container',
  summaryCount: '#summary-peserta-count',

  // Existing Step 2 (Phase 304 D-18 stability — TIDAK di-touch)
  filterBarBadge: '#selectedCountBadge',
  userContainer: '#userCheckboxContainer',
  protonContainer: '#protonUserCheckboxContainer',
} as const;
```

AFTER (extend dengan Phase 308 additions — INSERT 7 lines sebelum `} as const`):

```typescript
export const selectors = {
  // Phase 307 panel (Wave 1 markup — sesudah line 309 #userCheckboxContainer)
  panelWrapper: '#selected-participants-panel-wrapper',
  panelBody: '#selected-participants-panel',
  panelCount: '#selected-participants-count',

  // Step 4 summary (parity check target — Phase 307 markup refactor Option A)
  summaryListContainer: '#summary-peserta-list-container',
  summaryCount: '#summary-peserta-count',

  // Existing Step 2 (Phase 304 D-18 stability — TIDAK di-touch)
  filterBarBadge: '#selectedCountBadge',
  userContainer: '#userCheckboxContainer',
  protonContainer: '#protonUserCheckboxContainer',

  // Phase 308 — PrePost Wizard Validation Fix (D-13 selectors per CONTEXT)
  // Form ID correction per RESEARCH: '#createAssessmentForm' (BUKAN '#createForm' yang ada di CONTEXT.md)
  createForm: '#createAssessmentForm',
  assessmentTypeInput: '#assessmentTypeInput',
  statusFieldWrapper: '#statusFieldWrapper',
  statusSelect: '#Status',
  submitBtn: '#createAssessmentForm button[type="submit"]',
} as const;
```

Catatan implementasi:
- TIDAK menghapus atau memodifikasi 8 selector Phase 307 existing — Phase 304 D-18 stability principle.
- TIDAK pakai `default export` — match pattern existing (named export `selectors` only).
- `as const` di akhir tetap dipertahankan — TypeScript literal type safety.
- Comment `Phase 308 — PrePost Wizard Validation Fix` dengan note RESEARCH form ID correction — dokumentasi inline untuk maintainer future.
- Selector key `statusSelect` (BUKAN `Status`) untuk avoid TypeScript reserved name collision dan memperjelas semantic ("ini adalah `<select>` element, bukan status string").
- Selector `submitBtn` pakai compound CSS selector `#createAssessmentForm button[type="submit"]` agar match button submit DI DALAM form ini saja (avoid match button di header/nav lain).
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/helpers/wizardSelectors.ts` exists dan TIDAK kosong (verify: `wc -l tests/e2e/helpers/wizardSelectors.ts` returns minimum 25 lines, naik dari 20 baseline)
    - `grep -c "panelWrapper\|panelBody\|panelCount\|summaryListContainer\|summaryCount\|filterBarBadge\|userContainer\|protonContainer" tests/e2e/helpers/wizardSelectors.ts` returns minimum 8 (8 selectors Phase 307 tetap ada — Phase 304 D-18 stability)
    - `grep -n "createForm: '#createAssessmentForm'" tests/e2e/helpers/wizardSelectors.ts` returns 1 line (form ID correction per RESEARCH)
    - `grep -n "assessmentTypeInput: '#assessmentTypeInput'" tests/e2e/helpers/wizardSelectors.ts` returns 1 line
    - `grep -n "statusFieldWrapper: '#statusFieldWrapper'" tests/e2e/helpers/wizardSelectors.ts` returns 1 line
    - `grep -n "statusSelect: '#Status'" tests/e2e/helpers/wizardSelectors.ts` returns 1 line
    - `grep -n "submitBtn: '#createAssessmentForm button\\[type=\"submit\"\\]'" tests/e2e/helpers/wizardSelectors.ts` returns 1 line
    - `grep -c "'#createForm'" tests/e2e/helpers/wizardSelectors.ts` returns 0 (RESEARCH correction enforced — TIDAK ada `#createForm` reference, hanya `#createAssessmentForm`)
    - `grep -n "as const;" tests/e2e/helpers/wizardSelectors.ts` returns 1 line di akhir block
    - `grep -n "Phase 308" tests/e2e/helpers/wizardSelectors.ts` returns minimum 1 line (comment marker untuk maintainer)
    - `cd tests && npx tsc --noEmit -p tsconfig.json` exit code 0 (TypeScript compile lulus tanpa error)
  </acceptance_criteria>
  <done>
File `tests/e2e/helpers/wizardSelectors.ts` di-extend dengan 5 selector baru Phase 308 (`createForm`, `assessmentTypeInput`, `statusFieldWrapper`, `statusSelect`, `submitBtn`) — total 13 selector keys (8 Phase 307 + 5 Phase 308). Form ID dikoreksi pakai `#createAssessmentForm` (BUKAN `#createForm`) sesuai RESEARCH Pitfall 1. TypeScript compile lulus tanpa error, `as const` literal type safety preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/e2e/assessment.spec.ts dengan FLOW 8 describe block (4 test cases — test matrix D-10)</name>
  <files>tests/e2e/assessment.spec.ts</files>
  <read_first>
    - tests/e2e/assessment.spec.ts lines 1-50 (verify import statements + FLOW 1 pattern)
    - tests/e2e/assessment.spec.ts lines 96-171 (verify FLOW 7 Phase 307 describe block — pattern reference untuk FLOW 8)
    - .planning/phases/308-prepost-wizard-validation-fix/308-CONTEXT.md (D-10 test matrix 4 kombinasi, D-13 test scaffold scope)
    - .planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md (Code Examples Example 4 FLOW 8 test scaffold target — verbatim pattern; Verified Line Numbers anchor `#assessmentTypeInput`, `#statusFieldWrapper`, `#Status`)
    - .planning/phases/308-prepost-wizard-validation-fix/308-VALIDATION.md (Per-Task Verification Map — 4 tests 8.1-8.4 expected)
    - tests/helpers/utils.ts (verify uniqueTitle/today signature untuk fill Title field di test)
  </read_first>
  <action>
EXTEND file `tests/e2e/assessment.spec.ts` dengan SATU edit (APPEND describe block FLOW 8) — TIDAK perlu update import statement karena Phase 307 sudah added `import { selectors } from './helpers/wizardSelectors';` di line 4.

LOKASI INSERTION: tepat setelah line 171 (close `});` Phase 307 FLOW 7 describe block), sebelum line 173 (`// ============================================================` FLOW 2 separator yang sebenarnya).

INSERT block VERBATIM ini (FLOW 8 Phase 308 — 4 test cases mirror Phase 307 pattern):

```typescript

// ============================================================
// FLOW 8: Phase 308 — PrePost Wizard Validation Fix
// REQ: WIZ-04 (5 success criteria, 4-combination test matrix per D-10)
// Test scaffold WAVE 0 — RED state expected sebelum Wave 1 implementation merged
// ============================================================
test.describe('Assessment - Phase 308 PrePost Wizard Validation', () => {

  test('8.1 - Standard saja submit sukses (regression guard success criteria #5)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    // Standard mode default — Status field visible (NOT d-none)
    await expect(page.locator(selectors.statusFieldWrapper)).toBeVisible();
    await expect(page.locator(selectors.statusFieldWrapper)).not.toHaveClass(/d-none/);

    // Pilih 1 user
    const rinoCheckbox = page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input');
    await rinoCheckbox.click({ force: true });

    // Fill Step 1: Title + Category + assessmentType (default Standard)
    await page.fill('#Title', uniqueTitle('Phase 308 Standard'));
    await page.selectOption('#Category', 'OJT');

    // Fill Status (Standard mode WAJIB pilih — D-11 regression guard)
    await page.selectOption(selectors.statusSelect, 'Open');

    // Verify Status value ke-set
    await expect(page.locator(selectors.statusSelect)).toHaveValue('Open');

    // NOTE: Test SCAFFOLD wave 0 — full wizard navigation (Step 2/3/4 + submit) di-defer ke Wave 1
    // expect partial verification: Status field interactable + value persistence
  });

  test('8.2 - Switch S→PP→S Status field clear (mode-switch state cleanup D-02)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    // Initial: Standard mode → fill Status
    await expect(page.locator(selectors.statusFieldWrapper)).toBeVisible();
    await page.selectOption(selectors.statusSelect, 'Open');
    await expect(page.locator(selectors.statusSelect)).toHaveValue('Open');

    // Switch ke PrePost — D-01 expected: Status auto-set 'Upcoming', wrapper hidden
    await page.selectOption(selectors.assessmentTypeInput, 'PrePostTest');
    await expect(page.locator(selectors.statusFieldWrapper)).toHaveClass(/d-none/);
    // D-01 verification: Status value programmatically set ke 'Upcoming' meskipun field hidden
    await expect(page.locator(selectors.statusSelect)).toHaveValue('Upcoming');

    // Switch back ke Standard — D-02 expected: Status value clear (''), wrapper visible
    await page.selectOption(selectors.assessmentTypeInput, 'Standard');
    await expect(page.locator(selectors.statusFieldWrapper)).not.toHaveClass(/d-none/);
    // D-02 verification: Status value cleared, force user re-pick
    await expect(page.locator(selectors.statusSelect)).toHaveValue('');
  });

  test('8.3 - PP saja Status auto-set Upcoming + wrapper hidden (D-01 main path success criteria #1)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    // Switch ke PrePostTest dari default Standard
    await page.selectOption(selectors.assessmentTypeInput, 'PrePostTest');

    // D-01: Status value auto-set 'Upcoming' meskipun field hidden
    await expect(page.locator(selectors.statusSelect)).toHaveValue('Upcoming');

    // statusFieldWrapper hidden (existing handler behavior — D-03)
    await expect(page.locator(selectors.statusFieldWrapper)).toHaveClass(/d-none/);

    // Form ID verification: createForm selector resolves ke #createAssessmentForm (RESEARCH Pitfall 1)
    await expect(page.locator(selectors.createForm)).toBeVisible();

    // NOTE: Full submit flow (fill PreSchedule + PostSchedule + durations + EWCD + submit) di-defer ke manual UAT Step 3
    // E2E scaffold wave 0 fokus ke Status value assertion (D-01) + wrapper visibility (D-03)
    // Server-side ModelState.Remove (D-04) verified via successful submit di manual UAT
  });

  test('8.4 - Switch PP→S→PP Status auto-set Upcoming kembali (idempotency D-01 re-fire)', async ({ page }) => {
    await login(page, 'hc');
    await page.goto('/Admin/CreateAssessment');

    // Pertama: switch ke PrePost
    await page.selectOption(selectors.assessmentTypeInput, 'PrePostTest');
    await expect(page.locator(selectors.statusSelect)).toHaveValue('Upcoming');
    await expect(page.locator(selectors.statusFieldWrapper)).toHaveClass(/d-none/);

    // Kedua: switch back Standard — Status clear, wrapper visible
    await page.selectOption(selectors.assessmentTypeInput, 'Standard');
    await expect(page.locator(selectors.statusSelect)).toHaveValue('');
    await expect(page.locator(selectors.statusFieldWrapper)).not.toHaveClass(/d-none/);

    // Ketiga: switch lagi ke PrePost — D-01 re-fire idempotently
    await page.selectOption(selectors.assessmentTypeInput, 'PrePostTest');
    await expect(page.locator(selectors.statusSelect)).toHaveValue('Upcoming');
    await expect(page.locator(selectors.statusFieldWrapper)).toHaveClass(/d-none/);
  });

});
```

Catatan implementasi:
- TOTAL 4 test cases — cover test matrix D-10 dan 4 dari 5 success criteria phase (#1, #2 implicit, #4, #5). Success criteria #3 (jQuery validate re-parse) di-supersede oleh existing `validateStep` visibility guard per RESEARCH Pitfall 2 — tidak perlu test automated, manual UAT akan note.
- Pakai `selectors.X` dari import existing (Phase 307 import statement line 4) — single source of truth.
- Test 8.3 dan 8.4 fokus ke **client-side** state (Status value + wrapper d-none class) — server-side ModelState.Remove verified via manual UAT submit success path (Wave 1 Task 4 checkpoint).
- Test 8.2 cover mode-switch state cleanup (D-02): Standard → PrePost (D-01 set 'Upcoming') → Standard (D-02 clear ''). Critical untuk verify state-machine correctness.
- TIDAK include full wizard submit flow di test scaffold — wizard navigation memerlukan fill Step 2/3 dengan banyak field (Schedule, Duration, EWCD, PassPercentage, dll), risk flaky di CI. Manual UAT Step 3 + Step 4 cover full submit → server validation success path.
- Test 8.1 (Standard regression guard) hanya verify **partial state** (Status field interactable + value persistence). Full submit success path manual UAT-only.
- `uniqueTitle('Phase 308 ...')` digunakan untuk generate Title agar tidak collision dengan FLOW 1 test 1.2 yang juga create assessment.
- Test akan **RED** saat run sebelum Wave 1 implementasi merged — itu EXPECTED dan benar (RED → GREEN cycle).
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --list 2>&1 | grep -cE "8\\.[1-4]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "test.describe('Assessment - Phase 308 PrePost Wizard Validation'" tests/e2e/assessment.spec.ts` returns ONE line setelah line 171
    - `grep -c "test('8\\.[1-4]" tests/e2e/assessment.spec.ts` returns 4 (tepat 4 test cases dalam Phase 308 describe — 8.1, 8.2, 8.3, 8.4)
    - `grep -n "test('8.1 - Standard saja" tests/e2e/assessment.spec.ts` returns 1 line
    - `grep -n "test('8.2 - Switch S→PP→S" tests/e2e/assessment.spec.ts` returns 1 line
    - `grep -n "test('8.3 - PP saja" tests/e2e/assessment.spec.ts` returns 1 line
    - `grep -n "test('8.4 - Switch PP→S→PP" tests/e2e/assessment.spec.ts` returns 1 line
    - `grep -n "selectors.assessmentTypeInput\|selectors.statusFieldWrapper\|selectors.statusSelect\|selectors.createForm" tests/e2e/assessment.spec.ts` returns minimum 8 references (4 selectors baru × minimum 2 test usages each)
    - `grep -n "toHaveValue('Upcoming')" tests/e2e/assessment.spec.ts` returns minimum 3 lines (test 8.2 setelah switch PP, test 8.3 main path, test 8.4 first dan third switch)
    - `grep -n "toHaveValue('')" tests/e2e/assessment.spec.ts` returns minimum 2 lines (test 8.2 setelah switch back Standard, test 8.4 setelah switch back Standard)
    - `grep -n "toHaveClass(/d-none/)" tests/e2e/assessment.spec.ts` returns minimum 4 lines (test 8.2 + 8.3 + 8.4 verify wrapper hide)
    - `grep -n "PrePostTest" tests/e2e/assessment.spec.ts` returns minimum 4 lines (4 tests pakai value 'PrePostTest' untuk selectOption)
    - `cd tests && npx tsc --noEmit -p tsconfig.json` exit code 0 (TypeScript compile lulus, selector reference baru valid)
    - `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --list` exit code 0 dan output mengandung 4 test names (8.1, 8.2, 8.3, 8.4)
  </acceptance_criteria>
  <done>
File `tests/e2e/assessment.spec.ts` di-extend dengan FLOW 8 describe block "Phase 308 PrePost Wizard Validation" yang berisi tepat 4 test cases (8.1 Standard saja, 8.2 S→PP→S, 8.3 PP saja, 8.4 PP→S→PP) cover test matrix D-10. Tests pakai 4 selector baru Phase 308 dari wizardSelectors module. TypeScript compile lulus, Playwright `--list` lulus dengan 4 test names listed. Tests akan RED state sampai Wave 1 implementasi merge.
  </done>
</task>

<task type="auto">
  <name>Task 3: Buat manual UAT script Bahasa Indonesia 4-step (308-UAT.md)</name>
  <files>.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md</files>
  <read_first>
    - .planning/phases/307-selected-participants-inline-view/307-UAT.md (kalau exists — pattern reference 5-step format Bahasa Indonesia + sign-off section)
    - .planning/phases/308-prepost-wizard-validation-fix/308-CONTEXT.md (D-10 test matrix 4 kombinasi, D-18 UAT structure, D-19 sign-off requirement)
    - .planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md (Pitfall 4 mode-switch state UX note, Pitfall 6 programmatic value assignment, Manual-Only Verifications dari VALIDATION.md)
    - .planning/phases/308-prepost-wizard-validation-fix/308-VALIDATION.md (Manual-Only Verifications table — 4 behaviors yang manual-only)
    - .planning/REQUIREMENTS.md (REQ WIZ-04 full text + Out of Scope T11 differentiator excluded)
    - CLAUDE.md (Bahasa Indonesia mandate)
  </read_first>
  <action>
Tulis file BARU `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` dengan content berikut (Bahasa Indonesia, 4 step format action → expect mirror Phase 307 5-step pattern, dengan sign-off section):

```markdown
# Phase 308 — Manual UAT Script

**Phase:** 308-prepost-wizard-validation-fix
**REQ:** WIZ-04 (PrePost Wizard Validation Fix — Status field tidak reset wizard ke Step 1)
**Created:** 2026-04-29
**Mandatory before:** `/gsd-verify-work` Phase 308

> Manual UAT cover 5 success criteria Phase 308 dengan test matrix 4 kombinasi (D-10): Standard saja, Switch S→PP→S, PP saja, Switch PP→S→PP. E2E `tests/e2e/assessment.spec.ts` Phase 308 describe (test 8.1-8.4) cover **client-side state assertion** (Status value + wrapper d-none class). Manual UAT cover **server-side submit success path** (ModelState.Remove("Status") D-04) yang memerlukan full wizard navigation + browser observation.
>
> **Catatan RESEARCH:** ROADMAP success criteria #3 ("jQuery validate re-parse") di-supersede oleh existing custom `validateStep(n)` visibility guard di `Views/Admin/CreateAssessment.cshtml` line 996-1004. jQuery validate plugin **TIDAK** loaded di form ini (`_ValidationScriptsPartial` 0 matches). Manual UAT verifikasi behavior equivalent via observasi: tidak ada error "Status field is required" muncul saat submit PrePost.

## Pre-conditions

- App di-build & running (development atau staging)
- Login sebagai HC atau Admin (kredensial lihat `tests/helpers/accounts.ts`)
- Browser modern: Chrome / Edge / Firefox versi terbaru
- DevTools terbuka (Console tab — capture JS errors selama testing)
- Database seed minimal 5+ user untuk pilih peserta + 1+ Package soal Standard + 1+ Package soal PrePost (Pre-Test + Post-Test)

---

## Step 1 — Standard saja: pilih tipe Standard, fill semua field termasuk Status, submit, verify success (D-10 #1, success criteria #5 regression guard)

**Action:**
1. Navigasi ke `/Admin/CreateAssessment` (Create mode, fresh form)
2. Inspeksi Step 1: type selector `#assessmentTypeInput` default `Standard`, Status field `#Status` visible (wrapper `#statusFieldWrapper` TIDAK punya class `d-none`)
3. Pilih 1-2 peserta di Step 2 (centang checkbox `.user-checkbox`)
4. Lanjut ke Step 3: fill Title, Category "Assessment OJT", Schedule (≥ today), Duration ≥ 30 menit, ExamWindowCloseDate (> Schedule)
5. Pilih **Status = "Open"** (atau Closed) di Step 1 form (kalau Step 1 sebelumnya skip)
6. Lanjut ke Step 4 review summary
7. Klik **Submit** (`#createAssessmentForm button[type="submit"]`)
8. Observasi page response

**Expect:**
- Status field di Step 1 visible saat Standard mode (default) — wrapper TIDAK d-none
- Submit sukses → redirect ke `/Admin/ManageAssessment` atau success TempData muncul ("Assessment berhasil dibuat" atau equivalent)
- **TIDAK** ada error "Status field is required" di summary atau Step 1 reset
- Wizard **TIDAK** reset ke Step 1
- DevTools Console: tidak ada JS error baru (Phase 307 helper warnings boleh, errors tidak)

**Regression check (sub-step 1a — wajib):**
1. Refresh page, ulangi Step 1 sampai Step 4 **TANPA** pilih Status (biarkan default `-- Pilih Status --`)
2. Klik Submit
3. **Expect:** Error "Status wajib dipilih" muncul ATAU client-side validation block submit. Wizard tetap di Step yang sama (atau reset ke Step 1 dengan TempData error — itu acceptable untuk Standard mode error real per REQUIREMENTS Out of Scope T11 differentiator).

---

## Step 2 — Switch S→PP→S: switch tipe Standard → PrePost → kembali Standard, observe Status field re-shown empty, submit, verify success (D-10 #2, D-02 mode-switch cleanup)

**Action:**
1. Navigasi ke `/Admin/CreateAssessment` fresh
2. Step 1: pilih Status = "Open" sebelum switch tipe (untuk verify D-02 clear)
3. Switch type selector ke "Pre-Post Test"
4. Observasi: `#statusFieldWrapper` jadi hidden (`d-none` class added). Inspect element `#Status` value via DevTools Console: `document.getElementById('Status').value` — should return `'Upcoming'` (D-01 auto-set)
5. Switch type selector kembali ke "Standard"
6. Observasi: `#statusFieldWrapper` re-shown (d-none class removed). Inspect `#Status` value: should return `''` (empty — D-02 clear)
7. Pilih ulang Status = "Open" (D-11 — force user re-pick setelah switch back)
8. Pilih 1-2 peserta, fill Title + Category + Schedule + Duration + EWCD
9. Klik Submit

**Expect:**
- Switch ke PrePost: wrapper hide, Status value programmatically `'Upcoming'`
- Switch back Standard: wrapper visible, Status value `''` (empty)
- Submit sukses (Standard mode dengan Status re-picked) — TIDAK ada error reset
- DevTools Console: tidak ada JS error
- Sub-step UX note (Pitfall 4): kalau user sebelumnya pilih "Open" di Step 1, lalu switch ke PrePost, value 'Open' di-overwrite ke 'Upcoming' — ini intentional (state cleanup), TIDAK bug

---

## Step 3 — PrePost saja: pilih PrePost, fill PreSchedule + PostSchedule + durations + EWCD, submit, verify NO error "Status field is required" + NO reset ke Step 1 (D-10 #3, success criteria #1, #2, #4)

**Action:**
1. Navigasi ke `/Admin/CreateAssessment` fresh
2. Step 1: switch type selector ke "Pre-Post Test"
3. Observasi: `#statusFieldWrapper` jadi hidden, prePostCertNote tampil
4. Inspect `#Status` value via DevTools Console: should return `'Upcoming'` (D-01)
5. Step 2: pilih 1-2 peserta
6. Step 3: fill Title (e.g. "Phase 308 UAT Step 3"), Category "Assessment OJT", PreSchedule (≥ today + 1 jam), PreDurationMinutes = 60, PreExamWindowCloseDate (> PreSchedule), PostSchedule (> PreExamWindowCloseDate), PostDurationMinutes = 60, PostExamWindowCloseDate (> PostSchedule), pilih Pre-Test package + Post-Test package
7. Lanjut ke Step 4 review summary — verify Pre + Post details muncul, TIDAK ada Status di summary (PrePost mode)
8. Klik Submit
9. Observasi page response

**Expect:**
- Status field hidden saat PrePost mode (tidak terlihat di Step 1)
- Submit sukses → redirect / TempData success ("Assessment Pre-Post berhasil dibuat" atau equivalent)
- **TIDAK** ada error "Status field is required" di Step 1 atau Step 4 summary
- Wizard **TIDAK** reset ke Step 1 setelah submit (success criteria #4 main behavior)
- AssessmentSession terbuat dengan `Status="Upcoming"` (verify via /Admin/ManageAssessment list — record Pre-Post baru muncul dengan status "Upcoming")
- DevTools Console: tidak ada JS error

---

## Step 4 — Switch PP→S→PP: switch ke PrePost → Standard → kembali PrePost, observe Status field hidden + auto-set "Upcoming", submit, verify success (D-10 #4, D-01 idempotency)

**Action:**
1. Navigasi ke `/Admin/CreateAssessment` fresh
2. Switch type selector ke "Pre-Post Test"
3. Inspect `#Status` value Console: `'Upcoming'` (D-01 first fire)
4. Switch type selector kembali ke "Standard"
5. Inspect `#Status` value: `''` (D-02 clear)
6. Switch type selector lagi ke "Pre-Post Test"
7. Inspect `#Status` value: `'Upcoming'` (D-01 idempotent re-fire — KEY assertion Step 4)
8. Observasi: `#statusFieldWrapper` masih hidden (d-none class re-applied)
9. Step 2: pilih 1-2 peserta
10. Step 3: fill Title + Category + PreSchedule + PreDuration + PreEWCD + PostSchedule + PostDuration + PostEWCD + Pre/Post packages
11. Submit

**Expect:**
- Setelah switch PP→S→PP, Status value re-set ke 'Upcoming' setiap kali ke PP (idempotent)
- Wrapper hidden konsisten setiap PP mode
- Submit sukses, TIDAK ada error "Status field is required", TIDAK reset ke Step 1
- DevTools Console: tidak ada JS error baru per cycle switch

---

## Sign-off Checklist

- [ ] Step 1: Standard saja submit sukses + regression sub-step 1a (Standard tanpa Status tetap require pick)
- [ ] Step 2: Switch S→PP→S — Status auto-'Upcoming' saat PP, clear '' saat back Standard, submit sukses
- [ ] Step 3: PrePost saja — Status hidden, auto-'Upcoming', submit sukses tanpa error/reset (key acceptance success criteria #4)
- [ ] Step 4: Switch PP→S→PP — Status idempotent re-set 'Upcoming' setiap PP, submit sukses

**Tester name:** ____________________
**Tested at:** ____________________ (timestamp ISO 8601 atau readable date)
**Browser/version:** ____________________ (e.g. Chrome 120.0.6099.130 / Edge 120.0.2210.91 / Firefox 121.0)
**OS:** ____________________ (e.g. Windows 11 / macOS 14 / Ubuntu 22.04)
**Result:** PASS / FAIL (jika FAIL — note step number + observed vs expected di section bawah)

**Notes / observed deviations:**
_____________________________________________________________
_____________________________________________________________
_____________________________________________________________

**DevTools Console errors observed (kalau ada):**
_____________________________________________________________
_____________________________________________________________

---

*Phase: 308-prepost-wizard-validation-fix*
*UAT script created: 2026-04-29*
*REQ: WIZ-04 (Audit Temuan 11) — JS handler set Status='Upcoming' saat PrePost + server ModelState.Remove("Status") + mode-switch state cleanup*
```

Catatan implementasi:
- 4 step cover 4 kombinasi test matrix D-10 + sub-step Step 1a (regression Standard tanpa Status — success criteria #5).
- Format action → expect mirror Phase 307 5-step UAT pattern Bahasa Indonesia.
- Step 3 adalah **key acceptance** untuk success criteria #4 (TIDAK reset ke Step 1 setelah submit PrePost — exact bug di REQ WIZ-04).
- DevTools Console inspection (`document.getElementById('Status').value`) dimasukkan di setiap step yang melibatkan switch — verify D-01 / D-02 client-side state programmatically.
- Sign-off section: tester name, timestamp, browser version, OS, result PASS/FAIL, notes, DevTools console errors observed — pattern existing 307-UAT.md.
- TIDAK include section "performance budget" (#4 di Phase 307) karena Phase 308 tidak punya perf success criteria.
- TIDAK include section "side-by-side parity" karena Phase 308 tidak ada comparison view.
- TIDAK include test untuk jQuery validate re-parse (success criteria #3) — RESEARCH Pitfall 2 verified plugin tidak loaded; existing `validateStep` visibility guard line 996-1004 sudah cover behavior. Note explicit di intro paragraph.
  </action>
  <verify>
    <automated>test -f .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md && wc -l .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` exists
    - `wc -l .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns ≥ 50 lines (substantive UAT script)
    - `grep -c "## Step [1-4]" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns 4 (tepat 4 step — Step 1, 2, 3, 4 cover test matrix D-10)
    - `grep -n "Standard saja" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 1 line (Step 1 title)
    - `grep -n "Switch S→PP→S\|S→PP→S" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 1 line (Step 2)
    - `grep -n "PrePost saja\|PP saja" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 1 line (Step 3)
    - `grep -n "Switch PP→S→PP\|PP→S→PP" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 1 line (Step 4)
    - `grep -n "Status field is required" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 2 lines (negative assertion — error TIDAK boleh muncul di Step 3 dan Step 4)
    - `grep -n "Upcoming" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 4 lines (D-01 mention di multiple steps)
    - `grep -n "## Sign-off Checklist" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns 1 line (sign-off section — D-19)
    - `grep -n "Tester name:\|Tested at:\|Browser/version:\|Result:" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 4 lines (D-19 sign-off fields)
    - `grep -n "validateStep\|jQuery validate" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 1 line (RESEARCH Pitfall 2 acknowledgement — success criteria #3 N/A note)
    - `grep -n "regression\|Standard tanpa Status\|Status wajib dipilih" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns minimum 1 line (Step 1 sub-step 1a regression guard — success criteria #5)
  </acceptance_criteria>
  <done>
File `308-UAT.md` ada di phase directory, mengandung 4 step Bahasa Indonesia (Standard saja, Switch S→PP→S, PP saja, Switch PP→S→PP), Step 1 punya sub-step regression guard (success criteria #5), sign-off section dengan tester/timestamp/browser/OS/result fields. Note explicit di intro tentang RESEARCH Pitfall 2 (jQuery validate N/A — superseded oleh existing validateStep visibility guard).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → DevServer | Playwright login dengan test credentials → trust boundary, namun scope Wave 0 hanya test scaffold (tidak menyentuh production credential management) |
| Test code → CI/CD pipeline | Test file di-commit ke repo → harus tidak mengandung secret atau hardcoded production URL |
| Selector module → Production DOM | Selector constants reference production DOM IDs (e.g. `#createAssessmentForm`) — risk minimal: pure const, no dynamic input |

## STRIDE Threat Register

Phase 308 attack surface terbatas pada server-side ModelState removal (scoped ke field `Status` saja) + client-side JS value assignment (read-only side-effect tanpa user input). Wave 0 (this plan) HANYA membuat test infrastructure — TIDAK menambah attack surface baru. Threat model untuk Wave 1 implementasi di-handle di plan 308-02.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-308-W0-01 | Information Disclosure | tests/e2e/assessment.spec.ts (Phase 308 tests) | accept | Test pakai login helper existing yang baca dari `tests/helpers/accounts.ts` (test-only credentials, non-production). Tidak ada secret hardcoded di Phase 308 test code. Selectors helper pure DOM ID constants — no sensitive data. |
| T-308-W0-02 | Tampering | tests/e2e/helpers/wizardSelectors.ts (extended) | accept | Pure const export, no input handling. Risk profile zero — tidak ada user input atau dynamic state. Form ID correction (`#createAssessmentForm` per RESEARCH) adalah static literal, no injection vector. |
| T-308-W0-03 | Repudiation | .planning/phases/308-.../308-UAT.md | accept | Manual UAT script dengan sign-off section memiliki tester name + timestamp + browser version field — audit trail intentionally part of design (D-19). |

**Catatan:** Wave 1 plan (308-02) akan punya threat model untuk:
- Server-side `ModelState.Remove("Status")` scope — risk Tampering kalau remove field lain (mitigated via exact-string `"Status"` match, no glob).
- Client-side `statusEl.value = 'Upcoming'` programmatic — risk Tampering minimal karena server hardcode `Status = "Upcoming"` di line 1078/1112/1170 (defense-in-depth, JS value tidak authoritative).
- Cross-IIFE scope (Phase 307 helpers di line 1469-1614 vs Phase 308 edits di line 1872-1889) — risk DoS via listener leak / scope collision (mitigated via D-17 — Phase 308 TIDAK touch helper area).
</threat_model>

<verification>
**Phase-level verification untuk Wave 0:**

1. `tests/e2e/helpers/wizardSelectors.ts` extended dengan 5 selector baru Phase 308 (Task 1).
2. `tests/e2e/assessment.spec.ts` extended dengan FLOW 8 describe block (4 tests — Task 2).
3. `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --list` exit 0 dan list 4 Phase 308 tests.
4. `cd tests && npx tsc --noEmit -p tsconfig.json` exit 0.
5. `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` exists, ≥ 50 lines, 4 step Bahasa Indonesia + sign-off section (Task 3).
6. **Smoke run (optional, Wave 1 dependent):** Phase 308 test akan FAIL saat run sebelum Wave 1 implementasi (D-01 JS value assignment belum applied, `#Status` value tetap `''` saat switch PrePost) — itu EXPECTED. Failure → indicates test infrastructure ready untuk RED→GREEN cycle pada Wave 1.

**Smoke command sequence post-Wave-0:**
```bash
cd tests && npx tsc --noEmit -p tsconfig.json   # compile clean
cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --list   # 4 tests listed
cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307|1\\.2" --list   # FLOW 1 + FLOW 7 still listed (no scaffold regression)
```
</verification>

<success_criteria>
Wave 0 complete saat:
- [ ] Task 1: `tests/e2e/helpers/wizardSelectors.ts` extended dengan 5 selector baru Phase 308 (`createForm`, `assessmentTypeInput`, `statusFieldWrapper`, `statusSelect`, `submitBtn`) — total 13 keys (8 Phase 307 + 5 Phase 308), TypeScript compile lulus, form ID correction `#createAssessmentForm` (BUKAN `#createForm`)
- [ ] Task 2: `tests/e2e/assessment.spec.ts` extended dengan FLOW 8 describe block "Phase 308 PrePost Wizard Validation" (4 test cases 8.1-8.4 — cover test matrix D-10), Playwright `--list` lulus dengan 4 test names listed, TypeScript compile lulus
- [ ] Task 3: `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` exists, ≥ 50 lines, 4 step Bahasa Indonesia (cover test matrix D-10 + sub-step regression #5), sign-off section dengan 5 fields (tester/timestamp/browser/OS/result)
- [ ] All `<acceptance_criteria>` per task lulus
- [ ] No regression di FLOW 1, 2, 3, 4, 5, 6, 7 — selector module extension TIDAK break existing tests (verify via `npx playwright test --list` count >= existing baseline)
- [ ] Phase 308 test cases READY untuk RED state (akan fail karena Wave 1 belum implement D-01/D-02/D-04 — itu EXPECTED dan benar)
</success_criteria>

<output>
After completion, create `.planning/phases/308-prepost-wizard-validation-fix/308-01-SUMMARY.md` containing:
- Files modified (count + paths)
- Files created (count + paths — minimum 308-UAT.md)
- Selector additions count (5 baru: createForm, assessmentTypeInput, statusFieldWrapper, statusSelect, submitBtn)
- Test names registered (4 Phase 308: 8.1, 8.2, 8.3, 8.4)
- UAT structure overview (4 step + sub-step 1a regression)
- Verification commands run + exit codes
- RESEARCH-corrected items: form ID `#createAssessmentForm` (BUKAN `#createForm`), jQuery validate N/A note
- Handoff notes untuk Wave 1 executor (selector names ready, test scaffold ready, expected RED state pre-implementation)
</output>
