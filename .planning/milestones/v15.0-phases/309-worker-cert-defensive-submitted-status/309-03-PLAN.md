---
phase: 309
slug: worker-cert-defensive-submitted-status
plan: 03
type: execute
wave: 2
depends_on:
  - 309-02
files_modified:
  - Services/GradingService.cs
autonomous: true
requirements:
  - SUB-01
threat_model_summary:
  asvs_level: 1
  primary_category: "V14 Configuration"
  threat_count: 1
user_setup: []

must_haves:
  truths:
    - "Services/GradingService.cs line 196, 199 menggunakan AssessmentConstants.AssessmentStatus.PendingGrading constant alih-alih literal \"Menunggu Penilaian\""
    - "Literal \"Menunggu Penilaian\" eliminated dari Services/GradingService.cs (0 hits post-refactor)"
    - "Existing logic interim score calculation + IsPassed null projection + Progress=100 + CompletedAt=DateTime.UtcNow preserved (regression-free)"
    - "EF Core ExecuteUpdateAsync translation IDENTIK pre/post refactor (compile-time const equivalent dengan literal string — no runtime behavior change)"
  artifacts:
    - path: "Services/GradingService.cs"
      provides: "Refactor literal \"Menunggu Penilaian\" line 196 (Where clause) + line 199 (SetProperty) ke AssessmentConstants.AssessmentStatus.PendingGrading constant — opportunistic fix per OQ#2 (split dari Plan 309-02 Task 5 di iter-1 revision per scope_sanity warning)"
      contains: "AssessmentConstants.AssessmentStatus.PendingGrading"
  key_links:
    - from: "Services/GradingService.cs line 196 Where clause"
      to: "AssessmentConstants.AssessmentStatus.PendingGrading + AssessmentConstants.AssessmentStatus.Completed"
      via: "Refactor 2 literal string ke constants — eliminate typo risk + drift dengan Plan 309-02 PendingGrading constant introduction"
      pattern: "s\\.Status\\s*!=\\s*AssessmentConstants\\.AssessmentStatus\\.(Completed|PendingGrading)"
    - from: "Services/GradingService.cs line 199 SetProperty"
      to: "AssessmentConstants.AssessmentStatus.PendingGrading"
      via: "Refactor literal SetProperty value ke constant — semua call site internal pakai konstanta yang sama"
      pattern: "\\.SetProperty\\(r => r\\.Status, AssessmentConstants\\.AssessmentStatus\\.PendingGrading\\)"
---

<objective>
Plan 309-03 melakukan **opportunistic refactor** literal `"Menunggu Penilaian"` di `Services/GradingService.cs` line 196 (Where clause guard) dan line 199 (SetProperty value) ke constant `AssessmentConstants.AssessmentStatus.PendingGrading` yang di-introduce di Plan 309-02 Task 1. Plan ini di-SPLIT dari Plan 309-02 Task 5 di iter-1 revision per scope_sanity warning (Plan 309-02 sebelumnya 6 task melebihi threshold; sekarang Plan 309-02 = 5 task acceptable, Plan 309-03 = 1 auto task tematik separate).

**Purpose:** Eliminate typo risk untuk milestone berikutnya — saat constant baru di-introduce, konsisten kalau langsung di-pakai di SEMUA call site internal (cegah drift literal vs constant). Resolved per OQ#2 RESEARCH.md (post-iter-1 split).

**Output:** 1 file edit — `Services/GradingService.cs` line 196 + 199. Single-file scope, minimal diff (4 literal swap: 2 di Where clause `"Completed"` + `"Menunggu Penilaian"` → constant; 2 di SetProperty `"Menunggu Penilaian"` → constant). Tidak ada signature/contract yang berubah — non-breaking, compile-time constant equivalent.

**Decision yang ditutup planner:**
- **Plan split rationale:** GradingService refactor secara tematik berbeda dari certificate flow (Plan 309-02 fokus CMPController + Views certificate flow; Plan 309-03 fokus grading service). Wave 2 sequential setelah 309-02 karena `AssessmentConstants.AssessmentStatus.PendingGrading` constant baru di-introduce di Plan 309-02 Task 1 — Plan 309-03 depends_on=[309-02] untuk constant availability.
- **Refactor scope strict:** HANYA literal di line 196 dan 199 (yang berkaitan dengan PendingGrading flow). Literal `"Completed"` di lokasi LAIN GradingService.cs (e.g., baris 232 jika ada) **TIDAK** ikut di-refactor — out of scope; CONTEXT D-06 strict 3-lokasi rollout, opportunistic refactor terbatas pada line yang BERSAMAAN dengan PendingGrading literal saja.
- **Refactor literal `"Completed"` di line 196 IKUT swap** ke constant — line 196 Where clause punya 2 literal (`"Completed"` dan `"Menunggu Penilaian"`); konsisten kalau keduanya pakai constant (eliminate typo + drift risk). Line 196 menjadi: `s.Status != AssessmentConstants.AssessmentStatus.Completed && s.Status != AssessmentConstants.AssessmentStatus.PendingGrading`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md
@.planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md

<interfaces>
<!-- Key contracts diekstrak dari codebase. Executor pakai langsung — tidak perlu explore codebase. -->

From `Models/AssessmentConstants.cs` (post-Plan-309-02 Task 1):
```csharp
public static class AssessmentStatus
{
    public const string Open = "Open";
    public const string Upcoming = "Upcoming";
    public const string Completed = "Completed";
    public const string PendingGrading = "Menunggu Penilaian"; // Phase 309 D-04
}
```

From `Services/GradingService.cs` line 190-204 (current state — refactor target):
```csharp
if (hasEssay)
{
    // Interim score = hanya dari MC + MA (Essay skor 0)
    int interimPercentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;

    var essayRowsAffected = await _context.AssessmentSessions
        .Where(s => s.Id == session.Id && s.Status != "Completed" && s.Status != "Menunggu Penilaian")
        .ExecuteUpdateAsync(s => s
            .SetProperty(r => r.Score, interimPercentage)
            .SetProperty(r => r.Status, "Menunggu Penilaian")
            .SetProperty(r => r.HasManualGrading, true)
            .SetProperty(r => r.IsPassed, (bool?)null)
            .SetProperty(r => r.Progress, 100)
            .SetProperty(r => r.CompletedAt, DateTime.UtcNow)
        );
    // ...
}
```

From `Services/GradingService.cs` line 1-30 (using directives — verify `using HcPortal.Models;` ada untuk akses AssessmentConstants):
- Service file kemungkinan besar sudah punya `using HcPortal.Models;` karena akses model classes (AssessmentSession, dll). Executor verify pre-edit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: GradingService.cs opportunistic refactor literal "Menunggu Penilaian" → constant</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs line 1-30 (using directives — verify `using HcPortal.Models;` ada untuk akses AssessmentConstants)
    - Services/GradingService.cs line 189-227 (essay flow yang set status PendingGrading — 2 literal target)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md §"Deferred Ideas" + §"Specifics" (planner discretion include atau defer — DECISION iter-1: include sebagai opportunistic fix di Plan 309-03 split)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Open Questions (RESOLVED)" #2 (recommendation include + resolved iter-1 split decision)
    - Models/AssessmentConstants.cs (post-Plan-309-02 Task 1 — verify `AssessmentStatus.PendingGrading` constant available; jika 309-02 belum complete, BLOCK execution dan tunggu wave 2 dependency)
  </read_first>
  <action>
EDIT `Services/GradingService.cs` — refactor 2 literal `"Menunggu Penilaian"` line 196 dan 199 ke constant `AssessmentConstants.AssessmentStatus.PendingGrading`. Sekaligus refactor literal `"Completed"` line 196 ke `AssessmentConstants.AssessmentStatus.Completed` untuk konsistensi.

**Step 1 — Verify Plan 309-02 dependency (constant availability):**

Verify file `Models/AssessmentConstants.cs` contains `public const string PendingGrading = "Menunggu Penilaian";` (introduced di Plan 309-02 Task 1). Jika belum ada, ABORT — Plan 309-03 wave 2 sequential dependency belum terpenuhi (depends_on=[309-02]).

```bash
grep -F 'public const string PendingGrading = "Menunggu Penilaian";' Models/AssessmentConstants.cs
```

Expected: 1 hit. Jika 0 hit, ABORT execution (Plan 309-02 Task 1 belum complete).

**Step 2 — Verify using directive ada di GradingService.cs:**

Read line 1-30 file. Verify ada `using HcPortal.Models;`. Jika ada (kemungkinan besar — service akses model classes), skip ke Step 3. Jika TIDAK ada, ADD di top file setelah existing using directives:

```csharp
using HcPortal.Models;
```

**Step 3 — Locate dan replace literal di line 196 (Where clause):**

Locate via grep `s.Status != "Completed" && s.Status != "Menunggu Penilaian"` di file `Services/GradingService.cs` — expect 1 hit di line 196.

REPLACE:
```csharp
.Where(s => s.Id == session.Id && s.Status != "Completed" && s.Status != "Menunggu Penilaian")
```

DENGAN:
```csharp
.Where(s => s.Id == session.Id && s.Status != AssessmentConstants.AssessmentStatus.Completed && s.Status != AssessmentConstants.AssessmentStatus.PendingGrading)
```

**Step 4 — Locate dan replace literal di line 199 (SetProperty):**

Locate via grep `.SetProperty(r => r.Status, "Menunggu Penilaian")` di file — expect 1 hit di line 199.

REPLACE:
```csharp
.SetProperty(r => r.Status, "Menunggu Penilaian")
```

DENGAN:
```csharp
.SetProperty(r => r.Status, AssessmentConstants.AssessmentStatus.PendingGrading)
```

**Specific instruksi exec:**

1. **Refactor literal `"Completed"` di line 196 JUGA** ke constant — konsisten dengan refactor PendingGrading (eliminate typo risk + drift) — Step 3 sudah include kedua literal.
2. **JANGAN refactor literal `"Completed"` di lokasi LAIN GradingService.cs** (e.g., line 232 jika ada `s.Status == "Completed"` guard di flow yang berbeda) — out of scope; CONTEXT D-06 strict 3-lokasi rollout, opportunistic refactor ini terbatas pada line yang BERSAMAAN dengan PendingGrading literal saja (line 196 & 199). Audit grep `"Menunggu Penilaian"` returns expected 0 hits POST-refactor; `"Completed"` literal lain di file biarkan untuk milestone next.
3. **JANGAN refactor literal Status di file LAIN** (CMPController, AssessmentAdminController) — CONTEXT D-06 explicit out of scope. CMPController refactor di Plan 309-02 Task 2 sudah cover 3 lokasi target SUB-01.
4. **Verify EF Core ExecuteUpdateAsync compatibility:** `AssessmentConstants.AssessmentStatus.PendingGrading` adalah `const string` — compile-time constant equivalent dengan literal string. EF Core SetProperty/Where translation IDENTIK — no runtime behavior change. Compiler will inline constant value to identical IL.
5. **Comment trail Phase 309:** opsional tambah inline comment `// Phase 309 OQ#2 — opportunistic constant refactor` di atas Where clause atau SetProperty untuk traceability (planner discretion — terlalu banyak comment juga noise).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0; warning count ≤ 92
    - `grep -F '"Menunggu Penilaian"' Services/GradingService.cs` returns 0 hit (literal eliminated dari file ini)
    - `grep -c 'AssessmentConstants.AssessmentStatus.PendingGrading' Services/GradingService.cs` returns ≥ 2 (2 lokasi swap: Where clause + SetProperty)
    - `grep -F 'AssessmentConstants.AssessmentStatus.Completed' Services/GradingService.cs` returns ≥ 1 (line 196 Where clause juga ikut refactor)
    - Existing logic line 195-204 functional preserved:
      - `grep -F 'ExecuteUpdateAsync' Services/GradingService.cs` returns ≥ 1 hit (still uses ExecuteUpdateAsync)
      - `grep -c 'interimPercentage' Services/GradingService.cs` returns ≥ 1 (interim score calc preserved)
      - `grep -F 'IsPassed, (bool?)null' Services/GradingService.cs` returns ≥ 1 (IsPassed null projection preserved)
      - `grep -F 'Progress, 100' Services/GradingService.cs` returns ≥ 1 (Progress 100 preserved)
      - `grep -F 'CompletedAt, DateTime.UtcNow' Services/GradingService.cs` returns ≥ 1 (CompletedAt preserved)
    - `grep -F 'using HcPortal.Models;' Services/GradingService.cs` returns ≥ 1 hit (constant accessible)
  </acceptance_criteria>
  <done>`Services/GradingService.cs` line 196 dan 199 menggunakan `AssessmentConstants.AssessmentStatus.PendingGrading` constant alih-alih literal `"Menunggu Penilaian"`; line 196 juga `AssessmentConstants.AssessmentStatus.Completed` konsisten; literal di file ini eliminated; existing logic interim score + IsPassed null + Progress 100 + CompletedAt preserved; dotnet build pass 0 error; warning count ≤ 92.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GradingService → DbContext (EF Core ExecuteUpdateAsync) | Internal service to DB write — guarded oleh Where clause status check. Refactor literal ke constant TIDAK ubah translation behavior (compile-time constant equivalent dengan literal string). |
| Services/GradingService → Models/AssessmentConstants | Compile-time dependency — `using HcPortal.Models;` directive imports namespace; constant value resolved at compile time, not runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-309-08 | Tampering | `GradingService.ExecuteUpdateAsync` Where clause status guard — refactor literal ke constant | accept | Compile-time constant `AssessmentConstants.AssessmentStatus.PendingGrading` equivalent dengan literal string `"Menunggu Penilaian"` di IL bytecode. EF Core LINQ-to-SQL translation IDENTIK pre/post refactor — produces same WHERE clause SQL. NO runtime behavior change, NO race condition surface introduced. Acceptance criteria verifies via grep + build success. Wave 2 sequential setelah 309-02 ensures constant availability di codebase. |
</threat_model>

<verification>
**Build & Compile:**
- `dotnet build` exits 0; warning count ≤ 92 (Phase 308 baseline maintained)

**Code Pattern Verification:**
- `Services/GradingService.cs`: 0 hits literal `"Menunggu Penilaian"`, ≥ 2 hits `AssessmentConstants.AssessmentStatus.PendingGrading`, ≥ 1 hit `AssessmentConstants.AssessmentStatus.Completed`
- Existing logic preserved: ExecuteUpdateAsync, interimPercentage, IsPassed null, Progress 100, CompletedAt DateTime.UtcNow

**Manual Verification (no checkpoint — autonomous plan):**
- Optional: post-deploy smoke test — submit assessment ber-essay → DB Status='Menunggu Penilaian' (regression: refactor TIDAK break GradingService set status)
- Sufficient via Plan 309-02 Task 5 manual UAT Step 1 (covers GradingService set status correctly)
</verification>

<success_criteria>
**Mapped to Plan 309-02 OQ#2 RESOLVED + iter-1 scope_sanity split:**

- [x] Bonus (opportunistic per planner OQ#2 decision): `Services/GradingService.cs` line 196, 199 menggunakan constant `AssessmentConstants.AssessmentStatus.PendingGrading` (Task 1 — eliminate typo risk untuk milestone berikutnya)
- [x] Iter-1 split rationale: Plan 309-02 task count berkurang dari 6 ke 5 (acceptable scope_sanity threshold); Plan 309-03 tematik separate (grading flow, bukan certificate flow)
</success_criteria>

<output>
After completion, create `.planning/phases/309-worker-cert-defensive-submitted-status/309-03-SUMMARY.md` capturing:
- Decision yang ditutup planner: Refactor literal `"Menunggu Penilaian"` line 196, 199 ke constant + literal `"Completed"` line 196 juga ikut konsisten (per OQ#2 RESOLVED iter-1)
- Compile result + warning count
- Files changed: 1 file (`Services/GradingService.cs`)
- Commits: 1 (Task 1 commit)
- Phase 309 progress: 3/3 plan complete; Phase 309 ready untuk `/gsd-verify-work` closure
- Phase 310 dependency: `AssessmentConstants.AssessmentStatus.PendingGrading` constant available + GradingService sudah pakai constant — Phase 310 FinalizeEssayGrading tinggal reference constant tanpa risk drift
</output>
