---
phase: 310-essay-finalize-idempotency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentMonitoringViewModel.cs
  - Controllers/AssessmentAdminController.cs
  - Services/WorkerDataService.cs
autonomous: true
requirements: [ESCG-01]

must_haves:
  truths:
    - "Admin klik Finalize 2x pada session yang sudah Completed → kedua response sukses (alreadyFinalized:true), TIDAK ada error toast, TIDAK ada duplicate row TrainingRecord/NomorSertifikat/AuditLog"
    - "Admin klik Finalize pada session Status=Open/InProgress/Cancelled → response berisi pesan spesifik per status (Bahasa Indonesia)"
    - "MonitoringSessionViewModel expose Status + NomorSertifikat sehingga Razor view bisa gate button (D-02)"
    - "NotifyIfGroupCompleted dipanggil 2x untuk group yang sama → recipient HC/Admin hanya dapat 1 row UserNotification per session (dedup via AnyAsync)"
    - "AuditLog distinct per session: 2x klik finalize → 1 entry AuditLog dengan Action='FinalizeEssayGrading' (gated by rowsAffected > 0)"
  artifacts:
    - path: "Models/AssessmentMonitoringViewModel.cs"
      provides: "MonitoringSessionViewModel.Status + NomorSertifikat properties"
      contains: "public string Status"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "FinalizeEssayGrading idempotent + ViewModel mapper extended"
      contains: "alreadyFinalized"
    - path: "Services/WorkerDataService.cs"
      provides: "NotifyIfGroupCompleted dedup via UserNotifications.AnyAsync"
      contains: "alreadySent"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs FinalizeEssayGrading"
      to: "AssessmentConstants.AssessmentStatus.PendingGrading constant"
      via: "WHERE-clause guard di ExecuteUpdateAsync"
      pattern: "Status == AssessmentConstants.AssessmentStatus.PendingGrading"
    - from: "Controllers/AssessmentAdminController.cs FinalizeEssayGrading"
      to: "AuditLogService.LogAsync"
      via: "gated by if (rowsAffected > 0) — di dalam scope post-update"
      pattern: "_auditLog\\.LogAsync.*FinalizeEssayGrading"
    - from: "Services/WorkerDataService.cs NotifyIfGroupCompleted"
      to: "_context.UserNotifications"
      via: "AnyAsync dedup query sebelum SendAsync"
      pattern: "AnyAsync.*ASMT_ALL_COMPLETED"
    - from: "Controllers/AssessmentAdminController.cs ViewModel mapper L2573-2587"
      to: "MonitoringSessionViewModel.Status + NomorSertifikat"
      via: "projection assignment"
      pattern: "Status\\s*=\\s*a\\.Status"
---

<objective>
Tambahkan idempotency layer ke Essay finalize flow di backend: (1) FinalizeEssayGrading capture rowsAffected dari ExecuteUpdateAsync + gate semua side-effect (audit, cert, notif) dengan rowsAffected > 0, (2) friendly no-op response saat Status sudah Completed (D-03) + per-status BI rejection messages saat Status non-PendingGrading (D-04), (3) NotifyIfGroupCompleted dedup via UserNotifications.AnyAsync lookup (D-05), (4) AuditLog entry baru "FinalizeEssayGrading" gated by rowsAffected > 0 (D-07), (5) extend MonitoringSessionViewModel dengan Status + NomorSertifikat sebagai prerequisite untuk Plan 02 UI gate.

Purpose: Klik 2x tombol Finalize tidak menduplikasi TrainingRecord, NomorSertifikat, atau notifikasi grup; AuditLog distinct (tidak spam) per session. Goal-backward: tanpa idempotency layer ini, UI gate Plan 02 hanya kosmetik — server tetap rentan race condition. Pattern 100% adopsi dari GradingService.cs L195-212 (canonical Phase 309-03).

Output: 3 file modified — Models/AssessmentMonitoringViewModel.cs (ViewModel extend), Controllers/AssessmentAdminController.cs (FinalizeEssayGrading refactor + mapper extend), Services/WorkerDataService.cs (NotifyIfGroupCompleted dedup). Build pass dengan warning ≤ 92 (Phase 309 baseline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/310-essay-finalize-idempotency/310-CONTEXT.md
@.planning/phases/310-essay-finalize-idempotency/310-RESEARCH.md
@.planning/phases/310-essay-finalize-idempotency/310-PATTERNS.md
@.planning/phases/310-essay-finalize-idempotency/310-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Locked constants & helpers WAJIB dipakai (Phase 309) -->

From Models/AssessmentConstants.cs L13-19, L43-44:
```csharp
public static class AssessmentStatus
{
    public const string Open = "Open";
    public const string Upcoming = "Upcoming";
    public const string Completed = "Completed";
    public const string PendingGrading = "Menunggu Penilaian"; // Phase 309 D-04
}

public static bool IsAssessmentSubmitted(string? status) =>
    status == AssessmentStatus.Completed || status == AssessmentStatus.PendingGrading;
```

From Services/GradingService.cs L195-212 (CANONICAL pattern — copy semantik):
```csharp
var essayRowsAffected = await _context.AssessmentSessions
    .Where(s => s.Id == session.Id
                && s.Status != AssessmentConstants.AssessmentStatus.Completed
                && s.Status != AssessmentConstants.AssessmentStatus.PendingGrading)
    .ExecuteUpdateAsync(s => s.SetProperty(...));

if (essayRowsAffected == 0)
{
    _logger.LogWarning("GradingService: race condition session {SessionId}...", session.Id);
    return false;
}
```

From Services/AuditLogService.cs L21-42:
```csharp
public async Task LogAsync(
    string actorUserId, string actorName,
    string actionType, string description,
    int? targetId = null, string? targetType = null)
```

From Models/UserNotification.cs L11-65 (verified field availability):
```csharp
public string UserId { get; set; }    // [Required]
public string Type { get; set; }      // [Required, MaxLength 50] — "ASMT_ALL_COMPLETED"
public string Title { get; set; }     // [Required, MaxLength 200] — "Assessment Selesai"
public string Message { get; set; }   // [Required] — full text
public DateTime CreatedAt { get; set; }
// NOT AVAILABLE: SourceTitle, SourceDate, AssessmentSessionId
```

From Controllers/AssessmentAdminController.cs L322-328 (audit pattern reference):
```csharp
var currentUser = await _userManager.GetUserAsync(User);
var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
    ? (currentUser?.FullName ?? "Unknown")
    : $"{currentUser.NIP} - {currentUser.FullName}";
await _auditLog.LogAsync(currentUser?.Id ?? "", actorName, "AddCategory",
    $"Added assessment category '{category.Name}'...", category.Id, "AssessmentCategory");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend MonitoringSessionViewModel + mapper untuk Status + NomorSertifikat (D-02 prerequisite)</name>
  <files>Models/AssessmentMonitoringViewModel.cs, Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Models/AssessmentMonitoringViewModel.cs L47-63 (existing MonitoringSessionViewModel — append properti baru di akhir class, BUKAN replace block)
    - Controllers/AssessmentAdminController.cs L2555-2591 (LINQ projection mapper — block `sessions.Select(a => { ... return new MonitoringSessionViewModel { ... }; })` — append 2 baris di trailing comma sebelum `}`)
    - .planning/phases/310-essay-finalize-idempotency/310-PATTERNS.md L421-473 (Pattern Map section 5 — exact diff format ViewModel extension + mapper extension)
    - .planning/phases/310-essay-finalize-idempotency/310-UI-SPEC.md L171-176 (locked ViewModel field signature)
    - .planning/phases/310-essay-finalize-idempotency/310-CONTEXT.md L29-32 (D-02 — gate by Status==Completed; NomorSertifikat tetap di-expose untuk forward-compat dengan UI tooltip text)
  </read_first>
  <action>
    **Edit 1 — Models/AssessmentMonitoringViewModel.cs:** Setelah baris 62 `public int EssayPendingCount { get; set; }`, sebelum closing brace `}` baris 63, append 2 properti baru (literal copy):

    ```csharp

            // Phase 310 D-02 — gate button finalize berdasarkan Status assessment session
            public string Status { get; set; } = "";               // mirror AssessmentSession.Status (raw value, BUKAN UserStatus yang sudah remap ke "Not started"/"InProgress"/"Completed"/"Dibatalkan")
            public string? NomorSertifikat { get; set; }           // mirror AssessmentSession.NomorSertifikat (nullable)
    ```

    **Edit 2 — Controllers/AssessmentAdminController.cs L2573-2587 mapper:** Di dalam `return new MonitoringSessionViewModel { ... }` block, setelah baris `EssayPendingCount = essayPendingCountMap.TryGetValue(a.Id, out var ep) ? ep : 0` (L2586), tambah koma trailing dan append 2 baris baru sebelum closing brace `};`:

    ```csharp
                    // Phase 310 D-02 — append untuk button gate
                    Status          = a.Status ?? "",
                    NomorSertifikat = a.NomorSertifikat
    ```

    **PENTING:**
    - JANGAN replace existing `UserStatus` field — itu remap "Completed"/"Dibatalkan"/"InProgress"/"Not started" untuk display, BEDA dengan raw `Status` yang dipakai gate D-02.
    - Field `Status` raw + `NomorSertifikat` HARUS dari source `a` (AssessmentSession entity), bukan dari `userStatus` local variable.
    - `a.Status` adalah `string?` (nullable di entity) — pakai `?? ""` fallback agar field non-null di ViewModel.

    **Verify:** `dotnet build` exit 0 (warning ≤ 92).
  </action>
  <acceptance_criteria>
    - `grep -c "public string Status { get; set; } = \"\";" Models/AssessmentMonitoringViewModel.cs` returns 1 (atau lebih jika sudah ada di class lain — minimum 1 di scope MonitoringSessionViewModel)
    - `grep -n "public string? NomorSertifikat" Models/AssessmentMonitoringViewModel.cs` returns line antara 60-70 (di dalam MonitoringSessionViewModel block)
    - `grep -n "Status\s*=\s*a\.Status" Controllers/AssessmentAdminController.cs` returns line antara 2580-2595 (di dalam mapper block)
    - `grep -n "NomorSertifikat\s*=\s*a\.NomorSertifikat" Controllers/AssessmentAdminController.cs` returns line antara 2580-2595
    - `dotnet build` exit code = 0
    - `dotnet build 2>&1 | grep -c "Warning"` returns ≤ 92 (Phase 309 baseline)
    - `grep "EssayPendingCount = essayPendingCountMap" Controllers/AssessmentAdminController.cs` masih ada (regression guard — existing field tidak terhapus)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &&       dotnet build 2>&1 | tail -3 &&       grep -c 'public string Status' Models/AssessmentMonitoringViewModel.cs &&       grep -c 'public string? NomorSertifikat' Models/AssessmentMonitoringViewModel.cs &&       grep -c 'Status = a.Status' Controllers/AssessmentAdminController.cs &&       grep -c 'NomorSertifikat = a.NomorSertifikat' Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <done>ViewModel extended dengan 2 properti baru, mapper populate kedua field dari AssessmentSession entity, build pass 0 errors, warning count ≤ 92, behavior literals sampled via grep ≥ 1 untuk semua 4 patterns.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor FinalizeEssayGrading — capture rowsAffected + D-03 friendly no-op + D-04 per-status BI + D-07 audit gated</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs L2712-2833 (current FinalizeEssayGrading — full method body, ini target refactor utama)
    - Controllers/AssessmentAdminController.cs L322-328 (AddCategory audit log pattern — copy verbatim untuk currentUser/actorName/LogAsync structure)
    - Services/GradingService.cs L189-227 (CANONICAL Phase 309-03 capture-rowsAffected + race log + return false pattern — adopt semantik untuk return Json no-op)
    - Models/AssessmentConstants.cs L13-19 (status string constants — WAJIB pakai, BUKAN literal)
    - .planning/phases/310-essay-finalize-idempotency/310-PATTERNS.md L25-203 (Pattern Map section 1 — exact code blocks D-03/D-04/audit/rowsAffected gate)
    - .planning/phases/310-essay-finalize-idempotency/310-CONTEXT.md L36-78 (D-03, D-04, D-06, D-07 locked decisions + JSON contract field names)
    - .planning/phases/310-essay-finalize-idempotency/310-RESEARCH.md L475-610 (Example 1 full proposed FinalizeEssayGrading skeleton — reference)
  </read_first>
  <action>
    Refactor `Controllers/AssessmentAdminController.cs` `FinalizeEssayGrading` method (L2716-2833) dalam 4 langkah edit. JANGAN re-write full method dari scratch — apply targeted edits.

    **Edit A — Replace L2719 status check (single-line) dengan branching D-03 + D-04:**

    Existing L2719-2720:
    ```csharp
    if (session == null || session.Status != "Menunggu Penilaian")
        return Json(new { success = false, message = "Session tidak dalam status Menunggu Penilaian" });
    ```

    Replace dengan:
    ```csharp
    if (session == null)
        return Json(new { success = false, message = "Session tidak ditemukan." });

    // D-03 LOCKED — friendly no-op kalau sudah Completed (alreadyFinalized response)
    if (session.Status == AssessmentConstants.AssessmentStatus.Completed)
    {
        return Json(new
        {
            success = true,
            alreadyFinalized = true,
            message = $"Penilaian sudah diselesaikan sebelumnya pada {session.CompletedAt:dd MMM yyyy HH:mm} WIB",
            score = session.Score,
            isPassed = session.IsPassed,
            nomorSertifikat = session.NomorSertifikat
        });
    }

    // D-04 LOCKED — pesan spesifik per status non-PendingGrading (Bahasa Indonesia, copy literal dari CONTEXT.md D-04)
    if (session.Status != AssessmentConstants.AssessmentStatus.PendingGrading)
    {
        var statusMsg = session.Status switch
        {
            AssessmentConstants.AssessmentStatus.Open => "Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian.",
            "InProgress" => "Belum bisa di-finalize. Peserta sedang mengerjakan ujian.",
            "Cancelled"  => "Tidak bisa di-finalize. Session sudah dibatalkan.",
            _            => $"Tidak bisa di-finalize. Status saat ini: {session.Status}."
        };
        return Json(new { success = false, message = statusMsg });
    }
    ```

    **Edit B — Replace L2784-2790 ExecuteUpdateAsync (capture rowsAffected, gate side-effects):**

    Existing L2784-2790:
    ```csharp
    await _context.AssessmentSessions
        .Where(s => s.Id == sessionId && s.Status == "Menunggu Penilaian")
        .ExecuteUpdateAsync(s => s
            .SetProperty(r => r.Score, finalPercentage)
            .SetProperty(r => r.Status, "Completed")
            .SetProperty(r => r.IsPassed, isPassed)
            .SetProperty(r => r.CompletedAt, DateTime.UtcNow));
    ```

    Replace dengan:
    ```csharp
    // D-06 / D-07 — capture rowsAffected, gate semua side-effect (audit + cert + notif)
    var rowsAffected = await _context.AssessmentSessions
        .Where(s => s.Id == sessionId && s.Status == AssessmentConstants.AssessmentStatus.PendingGrading)
        .ExecuteUpdateAsync(s => s
            .SetProperty(r => r.Score, finalPercentage)
            .SetProperty(r => r.Status, AssessmentConstants.AssessmentStatus.Completed)
            .SetProperty(r => r.IsPassed, isPassed)
            .SetProperty(r => r.CompletedAt, DateTime.UtcNow));

    if (rowsAffected == 0)
    {
        // Race lost — thread lain sudah finalize duluan. Read current state, return friendly no-op.
        var current = await _context.AssessmentSessions.AsNoTracking()
            .FirstOrDefaultAsync(s => s.Id == sessionId);

        _logger.LogInformation(
            "FinalizeEssayGrading: race condition session {SessionId} — skip side-effects (already finalized).",
            sessionId);

        return Json(new
        {
            success = true,
            alreadyFinalized = true,
            message = $"Penilaian sudah diselesaikan sebelumnya pada {current?.CompletedAt:dd MMM yyyy HH:mm} WIB",
            score = current?.Score,
            isPassed = current?.IsPassed,
            nomorSertifikat = current?.NomorSertifikat
        });
    }
    ```

    **Edit C — Tambah audit log block setelah cert generation (after L2825 `catch (DbUpdateException) { ... }`), sebelum L2827 reload comment:**

    Insert block baru:
    ```csharp

                // 5b. Audit log (Phase 310 D-07) — gated by rowsAffected > 0 (di-skip otomatis kalau race lost karena early return di atas)
                var currentUser = await _userManager.GetUserAsync(User);
                var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
                    ? (currentUser?.FullName ?? "Unknown")
                    : $"{currentUser.NIP} - {currentUser.FullName}";
                try
                {
                    await _auditLog.LogAsync(
                        currentUser?.Id ?? "",
                        actorName,
                        "FinalizeEssayGrading",
                        $"Session {sessionId} ({session.Title}) finalized: score={finalPercentage}%, isPassed={isPassed}",
                        sessionId,
                        "AssessmentSession");
                }
                catch (Exception ex)
                {
                    // Audit failure tidak boleh break primary flow (precedent Phase 306 D-10)
                    _logger.LogWarning(ex, "FinalizeEssayGrading: audit log failed for session {SessionId}", sessionId);
                }
    ```

    **Edit D — Update existing return statement L2832 untuk include nomorSertifikat field (D-03 contract consistency):**

    Existing L2832:
    ```csharp
    return Json(new { success = true, score = finalPercentage, isPassed });
    ```

    Replace dengan:
    ```csharp
    return Json(new
    {
        success = true,
        score = finalPercentage,
        isPassed,
        nomorSertifikat = updatedSession?.NomorSertifikat
    });
    ```

    **Constraints:**
    - **L2827-2830 PRESERVATION (Warning #10):** Existing reload + NotifyIfGroupCompleted block (`var updatedSession = await _context.AssessmentSessions.FindAsync(sessionId);` + `await _workerDataService.NotifyIfGroupCompleted(updatedSession);`) WAJIB di-preserve as-is — variable `updatedSession` dibutuhkan oleh Edit D return statement (`nomorSertifikat = updatedSession?.NomorSertifikat`). JANGAN delete atau move block ini.
    - WAJIB pakai `AssessmentConstants.AssessmentStatus.{Completed, PendingGrading, Open}` constant. JANGAN literal `"Completed"`, `"Menunggu Penilaian"`, `"Open"` di kode baru. Existing literal yang TIDAK di-touch (mis. di kode lain di file ini) tidak perlu di-refactor — itu out of scope.
    - JANGAN pakai `TempData["Info"]` untuk D-03 — handler AJAX tidak trigger reload (RESEARCH Pitfall #8). Friendly message di-render JS-side oleh Plan 02.
    - Audit log Description tetap English (consistent existing convention — RESEARCH Open Question #4 recommendation). User-facing message D-03/D-04 wajib Bahasa Indonesia.
    - Existing TrainingRecord AnyAsync guard L2794-2796 + Cert WHERE-clause guard L2819-2822 PRESERVE as-is (sudah idempotent).
    - `_logger` field sudah ada di controller (verified RESEARCH A6 — pakai existing).

    **Verify:** `dotnet build` exit 0; grep all 5 acceptance patterns ditemukan.
  </action>
  <acceptance_criteria>
    - `grep -c "alreadyFinalized" Controllers/AssessmentAdminController.cs` returns ≥ 2 (early-Completed branch + race-lost branch)
    - `grep -F "AssessmentConstants.AssessmentStatus.PendingGrading" Controllers/AssessmentAdminController.cs` returns ≥ 1 line di FinalizeEssayGrading area
    - `grep -F "AssessmentConstants.AssessmentStatus.Completed" Controllers/AssessmentAdminController.cs` returns ≥ 1
    - `grep -F "var rowsAffected = await _context.AssessmentSessions" Controllers/AssessmentAdminController.cs` returns ≥ 1
    - `grep -F "if (rowsAffected == 0)" Controllers/AssessmentAdminController.cs` returns ≥ 1
    - `grep -F "_auditLog.LogAsync" Controllers/AssessmentAdminController.cs` returns ≥ 2 (existing AddCategory + new FinalizeEssayGrading)
    - `grep -F "\"FinalizeEssayGrading\"" Controllers/AssessmentAdminController.cs` returns ≥ 2 (XML doc/comment + audit Action string)
    - `grep -F "Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian." Controllers/AssessmentAdminController.cs` returns 1 (D-04 Open literal)
    - `grep -F "Belum bisa di-finalize. Peserta sedang mengerjakan ujian." Controllers/AssessmentAdminController.cs` returns 1 (D-04 InProgress literal)
    - `grep -F "Tidak bisa di-finalize. Session sudah dibatalkan." Controllers/AssessmentAdminController.cs` returns 1 (D-04 Cancelled literal)
    - `grep -F "Tidak bisa di-finalize. Status saat ini:" Controllers/AssessmentAdminController.cs` returns 1 (D-04 fallback literal — Warning #7 fix)
    - `grep -F "Penilaian sudah diselesaikan sebelumnya pada" Controllers/AssessmentAdminController.cs` returns ≥ 2 (early Completed branch + race-lost branch)
    - `dotnet build` exit code = 0
    - `dotnet build 2>&1 | grep -c "Warning"` returns ≤ 92
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &&       dotnet build 2>&1 | tail -3 &&       grep -c 'var rowsAffected = await' Controllers/AssessmentAdminController.cs &&       grep -c 'if (rowsAffected == 0)' Controllers/AssessmentAdminController.cs &&       grep -c 'AssessmentConstants.AssessmentStatus.PendingGrading' Controllers/AssessmentAdminController.cs &&       grep -c 'alreadyFinalized' Controllers/AssessmentAdminController.cs &&       grep -F 'Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian.' Controllers/AssessmentAdminController.cs &&       grep -F 'Tidak bisa di-finalize. Status saat ini:' Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <done>FinalizeEssayGrading capture rowsAffected, D-03 friendly no-op response 2 branches (early Completed + race-lost), D-04 per-status BI rejection (4 messages termasuk fallback "Status saat ini:"), D-07 audit gated rowsAffected > 0 dengan try-catch. Build pass 0 errors. Behavior literals sampled via grep untuk semua 6 acceptance patterns.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tambah dedup AnyAsync di NotifyIfGroupCompleted (D-05)</name>
  <files>Services/WorkerDataService.cs</files>
  <read_first>
    - Services/WorkerDataService.cs L313-345 (current NotifyIfGroupCompleted method body — target refactor)
    - Services/WorkerDataService.cs L1-25 (imports + DI fields — verify `_context` dan `_logger` injected)
    - Models/UserNotification.cs L11-65 (schema — Type, Title, Message, UserId, CreatedAt fields ONLY; SourceTitle/SourceDate TIDAK ada)
    - .planning/phases/310-essay-finalize-idempotency/310-PATTERNS.md L207-289 (Pattern Map section 2 — exact AnyAsync dedup query dengan time-window guard)
    - .planning/phases/310-essay-finalize-idempotency/310-CONTEXT.md L57-66 (D-05 locked — lookup UserNotifications dengan Title exact + Message.Contains + CreatedAt time-window)
    - .planning/phases/310-essay-finalize-idempotency/310-RESEARCH.md L437-444 (Pitfall #4 — false-positive risk Message.Contains, mitigation time-window guard)
  </read_first>
  <action>
    Refactor `Services/WorkerDataService.cs` method `NotifyIfGroupCompleted` (L313-345) — tambah dedup query SEBELUM `_notificationService.SendAsync`. Target line: dalam `foreach (var recipientId in recipientIds)` loop body L331-344.

    **Existing L331-344:**
    ```csharp
    foreach (var recipientId in recipientIds)
    {
        try
        {
            await _notificationService.SendAsync(
                recipientId,
                "ASMT_ALL_COMPLETED",
                "Assessment Selesai",
                $"Semua peserta assessment \"{completedSession.Title}\" telah menyelesaikan ujian",
                "/CMP/Assessment"
            );
        }
        catch (Exception ex) { _logger.LogWarning(ex, "Notification send failed"); }
    }
    ```

    **Replace dengan (D-05 dedup baru di awal foreach body):**
    ```csharp
    foreach (var recipientId in recipientIds)
    {
        // Phase 310 D-05 — dedup via UserNotifications.AnyAsync sebelum SendAsync
        // Schema: UserNotifications TIDAK punya SourceTitle/SourceDate, jadi pakai
        // Type + Title exact + Message.Contains(sessionTitle) + CreatedAt time-window guard
        bool alreadySent = await _context.UserNotifications.AnyAsync(n =>
            n.UserId == recipientId
            && n.Type == "ASMT_ALL_COMPLETED"
            && n.Title == "Assessment Selesai"
            && n.Message.Contains(completedSession.Title)
            && n.CreatedAt >= completedSession.Schedule.Date);

        if (alreadySent)
        {
            _logger.LogInformation(
                "NotifyIfGroupCompleted: skip recipient {RecipientId} — sudah ada notif untuk session \"{Title}\"",
                recipientId, completedSession.Title);
            continue;
        }

        try
        {
            await _notificationService.SendAsync(
                recipientId,
                "ASMT_ALL_COMPLETED",
                "Assessment Selesai",
                $"Semua peserta assessment \"{completedSession.Title}\" telah menyelesaikan ujian",
                "/CMP/Assessment"
            );
        }
        catch (Exception ex) { _logger.LogWarning(ex, "Notification send failed"); }
    }
    ```

    **Constraints:**
    - JANGAN ubah signature method `NotifyIfGroupCompleted(AssessmentSession completedSession)`.
    - JANGAN ubah logic L324 `allSiblings.All(s => s.Status == "Completed" || s.Status == "Cancelled")` — itu out of scope.
    - JANGAN ubah literal `"ASMT_ALL_COMPLETED"` dan `"Assessment Selesai"` — wajib match exact untuk dedup query work.
    - Time-window guard `CreatedAt >= completedSession.Schedule.Date` cegah false-positive cross-day collision.
    - `_context` (ApplicationDbContext) sudah injected — VERIFIED via existing usage L316 `_context.AssessmentSessions.AsNoTracking()`.
    - Pakai `using Microsoft.EntityFrameworkCore;` untuk `AnyAsync` extension — sudah di-import L1-4 (verify).

    **Verify:** `dotnet build` exit 0.
  </action>
  <acceptance_criteria>
    - `grep -F "bool alreadySent = await _context.UserNotifications.AnyAsync" Services/WorkerDataService.cs` returns 1
    - `grep -F "n.Type == \"ASMT_ALL_COMPLETED\"" Services/WorkerDataService.cs` returns 1 (di dedup query)
    - `grep -F "n.Title == \"Assessment Selesai\"" Services/WorkerDataService.cs` returns 1
    - `grep -F "n.Message.Contains(completedSession.Title)" Services/WorkerDataService.cs` returns 1
    - `grep -F "n.CreatedAt >= completedSession.Schedule.Date" Services/WorkerDataService.cs` returns 1 (time-window guard)
    - `grep -F "if (alreadySent)" Services/WorkerDataService.cs` returns 1
    - `grep -c "_notificationService.SendAsync" Services/WorkerDataService.cs` returns 1 (existing call preserved, no duplication)
    - `dotnet build` exit code = 0
    - `dotnet build 2>&1 | grep -c "Warning"` returns ≤ 92
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &&       dotnet build 2>&1 | tail -3 &&       grep -c 'AnyAsync' Services/WorkerDataService.cs &&       grep -F 'Title == "Assessment Selesai"' Services/WorkerDataService.cs &&       grep -F '.Message.Contains(' Services/WorkerDataService.cs &&       grep -F 'CreatedAt >=' Services/WorkerDataService.cs</automated>
  </verify>
  <done>NotifyIfGroupCompleted dedup via AnyAsync 4-field query (UserId + Type + Title + Message.Contains + CreatedAt window). Skip continue saat alreadySent dengan structured log. Build pass. Behavior literals sampled via grep untuk 4 dedup query field patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Admin/HC → /Admin/FinalizeEssayGrading POST | AntiForgeryToken required + Authorize(Roles="Admin,HC") existing — preserved |
| Concurrent finalize (parallel browser tabs / dual admin) | Race condition window — diatasi via EF WHERE-clause atomic guard |
| WorkerDataService → DB UserNotifications | Service-level dedup query sebelum insert (D-05) |
| AuditLogService → DB AuditLogs | Audit gated by rowsAffected > 0 (D-07) — natural dedup via DB constraint state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-310-01 | Tampering | FinalizeEssayGrading concurrent klik | mitigate | EF `ExecuteUpdateAsync` dengan WHERE clause `Status == PendingGrading` — atomic, thread ke-2 dapat 0 rows affected, skip side-effects. Pattern verified di GradingService L195-212 |
| T-310-02 | Repudiation | AuditLog spam (2x klik = 2 entries) merusak forensic clarity | mitigate | Audit `_auditLog.LogAsync` gated dalam if-block setelah `rowsAffected > 0` — race lost branch return early sebelum reach audit (D-07) |
| T-310-03 | Information Disclosure | Friendly no-op message D-03 leak `nomorSertifikat` ke user yang seharusnya tidak akses | accept | Endpoint sudah `[Authorize(Roles="Admin,HC")]` — Admin/HC memang diizinkan lihat NomorSertifikat. Tidak ada PII leak. |
| T-310-04 | Denial of Service | Spam klik finalize → flood AuditLog/Notif | mitigate | rowsAffected guard cap audit pada 1 entry per session lifetime (Status transition PendingGrading→Completed irreversible). Notification dedup via AnyAsync cap notif pada 1 per recipient per session. |
| T-310-05 | Elevation of Privilege | Worker bypass auth via direct POST | accept | Existing `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` preserved — no change to auth surface |
| T-310-06 | Information Disclosure | UserNotifications.Message.Contains LIKE query rentan SQL injection | accept | EF Core parameterizes `Contains` ke `LIKE @p1` parameterized query — input dari `completedSession.Title` (DB-sourced, not user input). No injection vector. |
</threat_model>

<verification>
**Per-task automated:** `dotnet build` exit 0 + warning ≤ 92 setelah setiap task commit.

**Plan-level integration (post Task 3):**
- `dotnet build` final pass.
- Manual smoke (developer Bash, optional): start app `dotnet run`, login Admin, navigate ke session ber-essay yang sudah Completed, klik Finalize via DevTools (`fetch /Admin/FinalizeEssayGrading`) → response harus contain `alreadyFinalized:true` field. Full UAT happens di Plan 02.
- DB query post-test (untuk SC #4): `SELECT COUNT(*) FROM AuditLogs WHERE Action='FinalizeEssayGrading' AND TargetId={sessionId}` returns 1 setelah klik 2x — manual UAT step di Plan 02.

**Required reading verification:** Tasks 1, 2, 3 setiap punya `<read_first>` minimum 5 sources covering: (a) target file current state, (b) PATTERNS.md analog, (c) CONTEXT.md decision, (d) RESEARCH.md pitfall, (e) UI-SPEC contract.
</verification>

<success_criteria>
- Build pass 0 errors warning ≤ 92.
- All 11+ grep acceptance patterns return ≥ 1 (per task).
- ViewModel exposes Status + NomorSertifikat (Plan 02 prerequisite met).
- FinalizeEssayGrading idempotent: 2x klik concurrent → 1 audit entry, 1 cert, 1 training record (proven via WHERE-clause guard pattern from Phase 309-03).
- NotifyIfGroupCompleted dedup: 2x trigger same group → 1 notification per recipient.
- Bahasa Indonesia mandatory di user-facing strings (D-03/D-04 messages); audit Description English machine-readable.
- All locked decisions implemented: D-02 (ViewModel prep) ✓, D-03 (friendly response) ✓, D-04 (per-status BI) ✓, D-05 (notif dedup) ✓, D-06 (no new lock) ✓, D-07 (audit gated) ✓.
</success_criteria>

<output>
After completion, create `.planning/phases/310-essay-finalize-idempotency/310-01-SUMMARY.md` containing:
- Files modified dengan line ranges
- Decisions implemented (D-02 prep, D-03, D-04, D-05, D-07) — link tiap decision ke commit hash
- Build output (warning count diff vs Phase 309 baseline 92)
- Patterns reused (GradingService L195-212, AddCategory audit L322-328)
- Open items untuk Plan 02 (UI gate Razor + JS handler upgrade + tests + UAT)
- Threat mitigations applied (T-310-01..T-310-06)
</output>
