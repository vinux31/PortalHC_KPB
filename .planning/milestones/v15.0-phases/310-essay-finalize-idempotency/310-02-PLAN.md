---
phase: 310-essay-finalize-idempotency
plan: 02
type: execute
wave: 2
depends_on: ["310-01"]
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - tests/e2e/assessment.spec.ts
  - .planning/phases/310-essay-finalize-idempotency/310-UAT.md
autonomous: false
requirements: [ESCG-01]

must_haves:
  truths:
    - "Saat session.Status == AssessmentConstants.AssessmentStatus.Completed, tombol Selesaikan Penilaian visually disabled (greyed out, opacity 0.65, cursor not-allowed) dengan Bootstrap tooltip BI"
    - "Saat klik tombol Finalize pada session non-Completed → server response success → location.reload(); pada response alreadyFinalized:true → render alert-info biru dengan auto-dismiss 5s, BUKAN reload, button tetap disabled"
    - "Saat klik tombol Finalize pada session Status=Open/InProgress/Cancelled → render alert-danger merah dengan pesan spesifik per status (BI), button re-enable agar admin troubleshoot"
    - "Playwright spec FLOW 9 'Phase 310 Essay Finalize Idempotency' contain ≥3 test cases: SC #1 alreadyFinalized branch, SC #2 disabled state inspection, SC #3+#4+#5 stub deferred ke manual UAT"
    - "310-UAT.md draft contain manual concurrent dual-tab test scenario + 4 SQL verify queries (TrainingRecord, AssessmentSessions.NomorSertifikat, AuditLogs, UserNotifications)"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Razor button gate D-02 + JS handler D-03/D-04 + showAlert helper + Bootstrap tooltip activation"
      contains: "data-bs-toggle=\"tooltip\""
    - path: "tests/e2e/assessment.spec.ts"
      provides: "FLOW 9 Phase 310 describe block dengan ≥3 test"
      contains: "Phase 310 Essay Finalize Idempotency"
    - path: ".planning/phases/310-essay-finalize-idempotency/310-UAT.md"
      provides: "Manual concurrent UAT script Bahasa Indonesia"
      contains: "Manual UAT — Phase 310"
  key_links:
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml button L416-418"
      to: "session.Status == AssessmentConstants.AssessmentStatus.Completed conditional"
      via: "Razor inline @if expression dengan disabled attr + tooltip"
      pattern: "disabled.*Status.*Completed"
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml JS handler L1331-1359"
      to: "showAlert helper function"
      via: "data.alreadyFinalized branch invokes showAlert('info', ...)"
      pattern: "alreadyFinalized.*showAlert"
    - from: "tests/e2e/assessment.spec.ts FLOW 9"
      to: "/Admin/AssessmentMonitoringDetail/{id} session detail page"
      via: "Playwright page.goto + locator assertion on disabled button"
      pattern: "Phase 310"
---

<objective>
Wire frontend UI gate dan validation infrastructure: (1) Razor conditional disable + Bootstrap tooltip BI di tombol "Selesaikan Penilaian" saat session.Status==Completed (D-02), (2) JS handler upgrade untuk konsumsi response baru — branch alreadyFinalized:true render alert-info biru, branch error D-04 render alert-danger merah (D-03/D-04), (3) extract showAlert helper dari existing pattern L1383-1389 dengan auto-dismiss 5s/7s, (4) Bootstrap tooltip activation snippet, (5) Playwright FLOW 9 spec scaffold (Phase 310) dengan ≥3 test, (6) 310-UAT.md draft manual concurrent test Bahasa Indonesia + 4 SQL verify queries.

Purpose: Tanpa UI gate D-02 + handler upgrade D-03/D-04, idempotency backend dari Plan 01 invisible ke admin — admin tetap bisa klik tombol pada Completed session dan dapat raw success/no feedback. Plan ini delivers SC #2 (UI hide) + SC #1 visible UX (friendly toast biru bukan error merah). Manual UAT cover SC #5 parallel finalize karena no .NET integration test project (RESEARCH finding #3).

Output: 1 view modified (Razor + JS), 1 test spec extended (FLOW 9 with ≥3 tests), 1 UAT.md draft. Build pass + Playwright list returns 4+4+1+(≥3)=12+ tests. Manual UAT checkpoint approved by user (Bahasa Indonesia).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/310-essay-finalize-idempotency/310-CONTEXT.md
@.planning/phases/310-essay-finalize-idempotency/310-RESEARCH.md
@.planning/phases/310-essay-finalize-idempotency/310-PATTERNS.md
@.planning/phases/310-essay-finalize-idempotency/310-VALIDATION.md
@.planning/phases/310-essay-finalize-idempotency/310-UI-SPEC.md
@.planning/phases/310-essay-finalize-idempotency/310-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Backend contract dari Plan 01 yang HARUS di-konsumsi UI -->

JSON response shape FinalizeEssayGrading (Plan 01 Edit B + Edit D):
```typescript
// Success normal (rowsAffected > 0):
{ success: true, score: number, isPassed: boolean, nomorSertifikat: string | null }

// Success alreadyFinalized (early Completed branch ATAU race-lost branch):
{ success: true, alreadyFinalized: true,
  message: "Penilaian sudah diselesaikan sebelumnya pada {dd MMM yyyy HH:mm} WIB",
  score: number, isPassed: boolean, nomorSertifikat: string | null }

// Error D-04 (status non-PendingGrading):
{ success: false, message: string }  // BI per status (Open/InProgress/Cancelled/fallback)
```

ViewModel field signature (Plan 01 Task 1):
```csharp
public string Status { get; set; } = "";              // raw AssessmentSession.Status
public string? NomorSertifikat { get; set; }
```

Bootstrap tooltip activation pattern (canonical Views/Admin/AddTraining.cshtml L394-397):
```js
document.addEventListener('DOMContentLoaded', function() {
    var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
    tooltipTriggerList.forEach(function(el) { new bootstrap.Tooltip(el); });
});
```

Inline alert injection pattern (CANONICAL Views/Admin/AssessmentMonitoringDetail.cshtml L1383-1389 — same file Phase 302):
```js
var alertClass = data.success ? 'alert-success' : 'alert-danger';
var iconClass = data.success ? 'bi-check-circle-fill' : 'bi-exclamation-triangle-fill';
var alertHtml = '<div class="alert ' + alertClass + ' alert-dismissible fade show mb-3" role="alert">'
    + '<i class="bi ' + iconClass + ' me-2"></i>' + data.message
    + '<button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>';
var alertContainer = document.querySelector('.container-fluid');
alertContainer.insertAdjacentHTML('afterbegin', alertHtml);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Razor button gate D-02 + JS handler D-03/D-04 + showAlert helper + tooltip activation</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml L408-422 (current button block — target Edit A)
    - Views/Admin/AssessmentMonitoringDetail.cshtml L1330-1360 (current JS finalize handler — target Edit B)
    - Views/Admin/AssessmentMonitoringDetail.cshtml L1380-1397 (existing inline alert pattern Phase 302 — extract sebagai showAlert helper)
    - Views/Admin/AddTraining.cshtml L394-397 (canonical Bootstrap tooltip activation snippet)
    - .planning/phases/310-essay-finalize-idempotency/310-PATTERNS.md L291-417 (Pattern Map sections 3+4 — exact Razor + JS diff format)
    - .planning/phases/310-essay-finalize-idempotency/310-UI-SPEC.md L93-160 (locked copy/color/spacing/interaction contract)
    - .planning/phases/310-essay-finalize-idempotency/310-CONTEXT.md L29-56 (D-02, D-03, D-04 locked)
    - .planning/phases/310-essay-finalize-idempotency/310-RESEARCH.md L420-470 (Pitfall #6 disabled tooltip + Pitfall #8 TempData anti-pattern)
    - .planning/phases/310-essay-finalize-idempotency/310-01-SUMMARY.md (verify Plan 01 ViewModel.Status + NomorSertifikat sudah ter-deploy)
  </read_first>
  <action>
    Empat edit di file `Views/Admin/AssessmentMonitoringDetail.cshtml`. JANGAN re-write file — apply targeted edits.

    **Edit A — L414-419 button gate (D-02 disabled + tooltip wrapper untuk Pitfall #6):**

    Existing L414-419:
    ```cshtml
    <div id="finalizeSection_@session.Id"
         style="display: @(session.EssayPendingCount == 0 ? "block" : "none")">
        <button class="btn btn-success btn-finalize-grading" data-session-id="@session.Id">
            <i class="bi bi-check-circle me-1"></i>Selesaikan Penilaian
        </button>
    </div>
    ```

    Replace dengan (D-02 LOCKED — wrap dengan `<span>` parent untuk solve Pitfall #6 disabled mouseenter skip):
    ```cshtml
    <div id="finalizeSection_@session.Id"
         style="display: @(session.EssayPendingCount == 0 ? "block" : "none")">
        @{
            // Phase 310 D-02 — gate by Status==Completed && NomorSertifikat!=null (LOCKED CONTEXT.md D-02 dual-criterion)
            var isFinalized = session.Status == AssessmentConstants.AssessmentStatus.Completed
                              && !string.IsNullOrEmpty(session.NomorSertifikat);
            var tooltipText = session.CompletedAt.HasValue
                ? $"Sudah selesai pada {session.CompletedAt.Value:dd MMM yyyy HH:mm} WIB"
                : "Penilaian sudah diselesaikan";
        }
        @if (isFinalized)
        {
            // Pitfall #6 mitigation — wrap disabled button dalam <span> agar tooltip tetap fire mouseenter
            <span data-bs-toggle="tooltip" title="@tooltipText" style="display:inline-block;">
                <button class="btn btn-success btn-finalize-grading"
                        data-session-id="@session.Id"
                        disabled
                        style="pointer-events: none;">
                    <i class="bi bi-check-circle me-1"></i>Selesaikan Penilaian
                </button>
            </span>
        }
        else
        {
            <button class="btn btn-success btn-finalize-grading" data-session-id="@session.Id">
                <i class="bi bi-check-circle me-1"></i>Selesaikan Penilaian
            </button>
        }
    </div>
    ```

    **Edit B — Tambah `showAlert(type, icon, message)` helper di top of `<script>` block.** Cari `<script>` opening tag di view (existing block ~L1230-1398). **Insertion point:** tepat setelah `<script>` opening tag di L1230 (sebelum existing code di L1231+) — helper deklarasi WAJIB di scope script function declarations agar reachable dari finalize handler L1347+. Insert helper SEBELUM existing code (top of script body):

    ```js
        // Phase 310 — extracted dari existing Phase 302 inline alert pattern (L1383-1389)
        // Render alert dismissible di top of .container-fluid dengan auto-dismiss
        function showAlert(type, icon, message) {
            var html = '<div class="alert alert-' + type + ' alert-dismissible fade show mb-3" role="alert">'
                + '<i class="bi ' + icon + ' me-2"></i>' + message
                + '<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>'
                + '</div>';
            var container = document.querySelector('.container-fluid');
            container.insertAdjacentHTML('afterbegin', html);

            // Auto-dismiss: 5s untuk info/success, 7s untuk danger (UI-SPEC contract)
            var dismissMs = type === 'danger' ? 7000 : 5000;
            setTimeout(function() {
                var alertEl = container.querySelector('.alert.alert-' + type);
                if (alertEl) {
                    var closeBtn = alertEl.querySelector('.btn-close');
                    if (closeBtn) closeBtn.click();
                }
            }, dismissMs);
        }

    ```

    **Edit C — L1347-1357 JS handler upgrade (D-03 + D-04 branching):**

    Existing L1347-1357 (di dalam `.btn-finalize-grading` click handler):
    ```js
    const data = await res.json();
    if (data.success) {
        location.reload();
    } else {
        alert(data.message);
        this.disabled = false;
    }
    ```

    Replace dengan:
    ```js
    const data = await res.json();
    if (data.success && data.alreadyFinalized) {
        // Phase 310 D-03 — friendly no-op, render alert-info biru, JANGAN reload (state sudah final)
        var msg = data.message;
        if (data.nomorSertifikat) {
            msg += ' Nomor sertifikat: ' + data.nomorSertifikat + '.';
        }
        showAlert('info', 'bi-info-circle-fill', '<strong>Info:</strong> ' + msg);
        this.disabled = true;   // tetap disabled — sudah final
    } else if (data.success) {
        // Path normal: finalize sukses pertama kali → reload untuk refresh state
        location.reload();
    } else {
        // Phase 310 D-04 — error spesifik per status (Open/InProgress/Cancelled), inline alert BUKAN browser native alert()
        showAlert('danger', 'bi-x-circle-fill', '<strong>Error:</strong> ' + data.message);
        this.disabled = false;
    }
    ```

    **Edit D — Tooltip activation snippet di akhir `<script>` block (sebelum closing `</script>` ~L1398):**

    Insert (di akhir script body, di luar function declarations):
    ```js
        // Phase 310 D-02 — Bootstrap tooltip activation untuk button disabled state
        // Canonical pattern dari Views/Admin/AddTraining.cshtml L394-397
        document.addEventListener('DOMContentLoaded', function() {
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
            tooltipTriggerList.forEach(function(el) { new bootstrap.Tooltip(el); });
        });
    ```

    **Constraints:**
    - JANGAN ubah existing confirm dialog L1333 — preserve as-is.
    - JANGAN ubah existing fetch URL `/Admin/FinalizeEssayGrading` ATAU body params — backwards-compat dengan controller.
    - JANGAN pakai `TempData["Info"]` / `TempData["Error"]` untuk D-03/D-04 — Pitfall #8 (handler AJAX tidak trigger reload, TempData hanya render saat full server-side reload).
    - WAJIB pakai `<span data-bs-toggle="tooltip">` wrapper untuk button disabled — Pitfall #6.
    - Bahasa Indonesia mandatory: tooltip text + alert messages + label `Info:` / `Error:` prefix.
    - Format tanggal: `dd MMM yyyy HH:mm` consistent dengan Phase 304 WIB convention (server interpolasi sudah BI-formatted dari Plan 01 D-03 message).
    - Existing fetch error catch L1354-1357 (`alert('Gagal menyelesaikan penilaian...')`) PRESERVE as-is — itu network/exception fallback, bukan response handling.

    **Verify:** Open file di browser dev mode + dotnet build pass (Razor compile).
  </action>
  <acceptance_criteria>
    - `grep -c 'data-bs-toggle="tooltip"' Views/Admin/AssessmentMonitoringDetail.cshtml` returns ≥ 1
    - `grep -E 'Status == AssessmentConstants\.AssessmentStatus\.Completed[\s\S]*NomorSertifikat' Views/Admin/AssessmentMonitoringDetail.cshtml` returns ≥ 1 (dual-criterion D-02 gate per CONTEXT.md)
    - `grep -F "!string.IsNullOrEmpty(session.NomorSertifikat)" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1
    - `grep -F "Sudah selesai pada" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1 (tooltip text)
    - `grep -F "Penilaian sudah diselesaikan" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1 (tooltip fallback)
    - `grep -F "function showAlert(type, icon, message)" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1 (helper extracted)
    - `grep -F "data.alreadyFinalized" Views/Admin/AssessmentMonitoringDetail.cshtml` returns ≥ 1
    - `grep -F "showAlert('info', 'bi-info-circle-fill'" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1 (D-03)
    - `grep -F "showAlert('danger', 'bi-x-circle-fill'" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1 (D-04)
    - `grep -F "new bootstrap.Tooltip(el)" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1 (activation)
    - `grep -F "<strong>Info:</strong>" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1 (label prefix per UI-SPEC)
    - `grep -F "<strong>Error:</strong>" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 1
    - `grep -c 'btn-finalize-grading' Views/Admin/AssessmentMonitoringDetail.cshtml` returns ≥ 3 (one in each branch isFinalized true/false + JS selector)
    - `grep -F "TempData[\"Info\"]" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 0 (anti-pattern absent)
    - `dotnet build` exit code = 0 (Razor compile pass)
    - `dotnet build 2>&1 | grep -c "Warning"` returns ≤ 92
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>Razor button gate dengan 2 branches (isFinalized: span+disabled+tooltip; non-isFinalized: existing button), JS handler upgrade 3-way branching (alreadyFinalized → info / success → reload / error → danger), showAlert helper extracted dengan auto-dismiss, tooltip activation snippet di-tambahkan. Build pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Playwright FLOW 9 Phase 310 spec scaffold + 310-UAT.md draft</name>
  <files>tests/e2e/assessment.spec.ts, .planning/phases/310-essay-finalize-idempotency/310-UAT.md</files>
  <read_first>
    - tests/e2e/assessment.spec.ts L173-263 (existing FLOW 8 Phase 308 describe block — copy structure as template)
    - tests/e2e/assessment.spec.ts L1-20 (imports + helpers — selectors, login, uniqueTitle convention)
    - .planning/phases/310-essay-finalize-idempotency/310-VALIDATION.md L42-90 (validation map per task ID + manual UAT scenarios — exact SQL queries)
    - .planning/phases/310-essay-finalize-idempotency/310-CONTEXT.md L29-78 (D-01..D-07 — UAT scenarios derive from these)
    - .planning/phases/310-essay-finalize-idempotency/310-UI-SPEC.md L92-156 (copy locked — UAT must verify literal strings)
    - .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md (reference template — IF EXISTS, untuk format consistency)
  </read_first>
  <action>
    Dua file deliverable — Playwright spec scaffold (RED state expected pre-Plan 01 merge, GREEN setelah) + UAT.md manual concurrent script.

    **File 1 — `tests/e2e/assessment.spec.ts` — append FLOW 9 setelah FLOW 8 closing brace L263 (sebelum FLOW 2 comment block L265):**

    ```typescript

    // ============================================================
    // FLOW 9: Phase 310 — Essay Finalize Idempotency
    // REQ: ESCG-01 (5 success criteria)
    // SC #1 alreadyFinalized branch + SC #2 disabled state — auto E2E
    // SC #3 (notif dedup) + SC #4 (audit dedup) + SC #5 (parallel finalize) — manual UAT (RESEARCH finding #3, no .NET integration test project)
    // ============================================================
    test.describe('Assessment - Phase 310 Essay Finalize Idempotency', () => {

      test.beforeEach(async ({ page }) => {
        await login(page, 'hc');
      });

      test('9.1 - SC #2: Tombol Selesaikan Penilaian disabled + tooltip wrapper saat session Status=Completed', async ({ page }) => {
        // PRECONDITION: Seed data — session ber-essay yang sudah Completed (manual seed atau pakai fixture existing)
        // Test ini scaffold WAVE — assertion target struktur DOM, bukan flow create-finalize end-to-end
        // Untuk Wave 1 fill: developer pilih sessionId Completed dari dev DB, navigate ke detail page

        // PLACEHOLDER target session ID — Wave 1 fill dengan actual seeded ID
        const completedSessionTitle = 'Phase 310 Completed Fixture';

        // Navigate ke ManageAssessment, find seeded session, klik detail
        await page.goto('/Admin/ManageAssessment');
        const groupRow = page.locator('tr', { hasText: completedSessionTitle }).first();

        // Test akan SKIP otomatis kalau seed belum ada (RED state pre-fixture)
        if (await groupRow.count() === 0) {
          test.skip(true, 'Seed session "Phase 310 Completed Fixture" not found — Wave 1 manual seed required');
        }

        // Click ke detail
        await groupRow.locator('a[href*="AssessmentMonitoringDetail"]').first().click();
        await page.waitForLoadState('networkidle');

        // Assertion D-02: tombol .btn-finalize-grading wrapped dalam <span> dengan tooltip
        const finalizeBtn = page.locator('.btn-finalize-grading').first();
        await expect(finalizeBtn).toBeDisabled();

        const tooltipWrapper = page.locator('span[data-bs-toggle="tooltip"]').filter({ has: finalizeBtn });
        await expect(tooltipWrapper).toBeVisible();

        // Tooltip text contains "Sudah selesai pada"
        const tooltipTitle = await tooltipWrapper.getAttribute('title');
        expect(tooltipTitle).toContain('Sudah selesai pada');
        expect(tooltipTitle).toContain('WIB');
      });

      test('9.2 - SC #1: Klik Finalize 2x → response ke-2 alreadyFinalized:true render alert-info biru (NOT alert-danger)', async ({ page }) => {
        // PRECONDITION: Session dengan Status=PendingGrading (essay sudah di-grade semua, belum di-finalize)
        // Test ini scaffold — Wave 1 fill dengan seeded sessionId yang ready untuk finalize

        const pendingSessionTitle = 'Phase 310 PendingGrading Fixture';
        await page.goto('/Admin/ManageAssessment');
        const groupRow = page.locator('tr', { hasText: pendingSessionTitle }).first();

        if (await groupRow.count() === 0) {
          test.skip(true, 'Seed session "Phase 310 PendingGrading Fixture" not found — Wave 1 manual seed required');
        }

        await groupRow.locator('a[href*="AssessmentMonitoringDetail"]').first().click();
        await page.waitForLoadState('networkidle');

        // Klik 1x — confirm dialog accept
        page.on('dialog', dialog => dialog.accept());

        const finalizeBtn = page.locator('.btn-finalize-grading').first();
        await finalizeBtn.click();

        // Wait reload — first click sukses normal
        await page.waitForLoadState('networkidle');

        // Sekarang button gated (Status=Completed) — tapi ini test 2x klik via fetch JS langsung
        // Re-trigger via JS fetch (bypass UI gate untuk simulate concurrent dual-tab)
        const response = await page.evaluate(async () => {
          const token = (document.querySelector('input[name="__RequestVerificationToken"]') as HTMLInputElement).value;
          const sessionId = parseInt((document.querySelector('.btn-finalize-grading') as HTMLElement)?.dataset?.sessionId || '0');
          const res = await fetch('/Admin/FinalizeEssayGrading', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/x-www-form-urlencoded',
              'X-Requested-With': 'XMLHttpRequest'
            },
            body: 'sessionId=' + sessionId + '&__RequestVerificationToken=' + encodeURIComponent(token)
          });
          return await res.json();
        });

        // Assertion D-03: response ke-2 = success + alreadyFinalized:true + message contain "sudah diselesaikan"
        expect(response.success).toBe(true);
        expect(response.alreadyFinalized).toBe(true);
        expect(response.message).toContain('Penilaian sudah diselesaikan sebelumnya pada');
        expect(response.message).toContain('WIB');
      });

      test('9.3 - SC #1 alt: Klik Finalize pada session Open → error toast spesifik (D-04 BI literal)', async ({ page }) => {
        // PRECONDITION: Session Status=Open (belum mulai mengerjakan)
        const openSessionTitle = 'Phase 310 Open Fixture';
        await page.goto('/Admin/ManageAssessment');
        const groupRow = page.locator('tr', { hasText: openSessionTitle }).first();

        if (await groupRow.count() === 0) {
          test.skip(true, 'Seed session "Phase 310 Open Fixture" not found — Wave 1 manual seed required');
        }

        await groupRow.locator('a[href*="AssessmentMonitoringDetail"]').first().click();
        await page.waitForLoadState('networkidle');

        // Trigger via JS fetch (UI mungkin sudah hide button via existing EssayPendingCount guard)
        const response = await page.evaluate(async () => {
          const token = (document.querySelector('input[name="__RequestVerificationToken"]') as HTMLInputElement).value;
          // Find sessionId dari data-session-id atau URL
          const sessionId = parseInt(window.location.pathname.split('/').pop() || '0');
          const res = await fetch('/Admin/FinalizeEssayGrading', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/x-www-form-urlencoded',
              'X-Requested-With': 'XMLHttpRequest'
            },
            body: 'sessionId=' + sessionId + '&__RequestVerificationToken=' + encodeURIComponent(token)
          });
          return await res.json();
        });

        // Assertion D-04: response BI literal "Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian."
        expect(response.success).toBe(false);
        expect(response.message).toContain('Belum bisa di-finalize');
      });

    });

    ```

    **File 2 — Create `.planning/phases/310-essay-finalize-idempotency/310-UAT.md`:**

    ```markdown
    # Manual UAT — Phase 310 Essay Finalize Idempotency

    **REQ:** ESCG-01
    **Phase:** 310-essay-finalize-idempotency
    **Bahasa:** Bahasa Indonesia (mandatory user-facing)
    **Created:** 2026-05-02
    **Status:** Draft (Wave 0 stub — fill saat Plan 02 implementasi merged)

    ## Prerequisite Setup

    1. Login Admin (atau HC) ke aplikasi `http://localhost:5277/Account/Login`
    2. Pastikan ada minimum 3 session ber-essay di DB:
       - Session A: Status=`PendingGrading` (essay sudah di-grade semua, belum di-finalize). Pakai existing flow: Buat assessment → Worker submit → HC grade essay sampai `EssayPendingCount=0`.
       - Session B: Status=`Completed` (sudah ter-finalize sebelumnya). Lanjutan dari Session A setelah klik Finalize.
       - Session C: Status=`Open` (worker belum mulai). Buat assessment baru tanpa worker submit.

    ## Step 1 — SC #1 (D-03 friendly no-op): Klik Finalize 2x sequential

    1. Login Admin di tab #1
    2. Navigate ke `/Admin/AssessmentMonitoringDetail/{sessionId_A}` (session Status=PendingGrading)
    3. Klik tombol **Selesaikan Penilaian**
    4. Confirm dialog → klik OK
    5. Expected: page reload, status sekarang Completed, tombol disabled
    6. Buka DevTools Console, run:
       ```js
       const token = document.querySelector('input[name="__RequestVerificationToken"]').value;
       const sessionId = {sessionId_A};
       fetch('/Admin/FinalizeEssayGrading', {
           method: 'POST',
           headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
           body: `sessionId=${sessionId}&__RequestVerificationToken=${encodeURIComponent(token)}`
       }).then(r => r.json()).then(console.log);
       ```
    7. **Expected console output:**
       ```json
       {
         "success": true,
         "alreadyFinalized": true,
         "message": "Penilaian sudah diselesaikan sebelumnya pada {tanggal} WIB",
         "score": <int>,
         "isPassed": <bool>,
         "nomorSertifikat": "<string|null>"
       }
       ```
    8. **Result:** [ ] PASS / [ ] FAIL — _isi setelah test_

    ## Step 2 — SC #2 (D-02 UI gate): Tombol disabled + tooltip BI

    1. Refresh halaman `/Admin/AssessmentMonitoringDetail/{sessionId_A}` setelah Step 1
    2. Locate tombol **Selesaikan Penilaian** di bagian Essay grading section
    3. **Expected visual:**
       - Tombol greyed out (opacity ~0.65)
       - Cursor `not-allowed` saat hover
       - Bootstrap `disabled` attribute set di DOM
       - Tombol di-wrap dalam `<span data-bs-toggle="tooltip">`
    4. Hover tombol selama 1 detik
    5. **Expected tooltip:** `Sudah selesai pada {dd MMM yyyy HH:mm} WIB` (BI format Phase 304 convention)
    6. **Result:** [ ] PASS / [ ] FAIL

    ## Step 3 — SC #3 (notif dedup): UserNotifications distinct per recipient

    1. Trigger Step 1 (klik Finalize 2x sequential)
    2. Buka SQL Server Management Studio atau Azure Data Studio
    3. Execute query:
       ```sql
       SELECT UserId, COUNT(*) AS NotifCount
       FROM UserNotifications
       WHERE Type = 'ASMT_ALL_COMPLETED'
         AND Title = 'Assessment Selesai'
         AND Message LIKE '%' + (SELECT Title FROM AssessmentSessions WHERE Id = {sessionId_A}) + '%'
         AND CreatedAt >= (SELECT CAST(Schedule AS DATE) FROM AssessmentSessions WHERE Id = {sessionId_A})
       GROUP BY UserId;
       ```
    4. **Expected:** Setiap UserId (HC + Admin recipients) memiliki `NotifCount = 1` (NO duplicate per recipient)
    5. **Result:** [ ] PASS / [ ] FAIL

    ## Step 4 — SC #4 (audit dedup): AuditLogs distinct per session

    1. Trigger Step 1 (klik Finalize 2x sequential)
    2. Execute query:
       ```sql
       SELECT COUNT(*) AS AuditCount
       FROM AuditLogs
       WHERE Action = 'FinalizeEssayGrading'
         AND TargetId = {sessionId_A}
         AND TargetType = 'AssessmentSession';
       ```
    3. **Expected:** `AuditCount = 1` (audit only logged saat rowsAffected > 0; thread ke-2 race-lost tidak log)
    4. **Result:** [ ] PASS / [ ] FAIL

    ## Step 5 — SC #5 (concurrent parallel finalize): Dual-tab simultaneous click

    **Setup baru:** Buat session E baru dengan Status=PendingGrading (essay all graded).

    1. Login Admin di **2 browser tab** (atau 2 browser window berbeda)
    2. Kedua tab navigate ke `/Admin/AssessmentMonitoringDetail/{sessionId_E}`
    3. Pre-stage: Posisikan kursor di tombol **Selesaikan Penilaian** di kedua tab
    4. Click as simultan as possible (target window ≤ 1 detik)
    5. Confirm dialog → OK di kedua tab
    6. **Expected UI:** kedua tab tampak success (NO error toast merah)
       - Tab #1: page reload (winner thread, normal success path)
       - Tab #2: render `alert-info` biru "Penilaian sudah diselesaikan sebelumnya pada..." (loser thread, alreadyFinalized response)
    7. Execute SQL queries:
       ```sql
       -- TrainingRecord — distinct
       SELECT COUNT(*) FROM TrainingRecords
       WHERE UserId = (SELECT UserId FROM AssessmentSessions WHERE Id = {sessionId_E})
         AND Judul = 'Assessment: ' + (SELECT Title FROM AssessmentSessions WHERE Id = {sessionId_E})
         AND Tanggal = (SELECT Schedule FROM AssessmentSessions WHERE Id = {sessionId_E});
       -- Expected: 1

       -- NomorSertifikat — distinct value
       SELECT COUNT(DISTINCT NomorSertifikat) FROM AssessmentSessions WHERE Id = {sessionId_E};
       -- Expected: 1 (single value, not null IF GenerateCertificate=true && isPassed)

       -- AuditLog — distinct
       SELECT COUNT(*) FROM AuditLogs WHERE Action = 'FinalizeEssayGrading' AND TargetId = {sessionId_E};
       -- Expected: 1

       -- UserNotifications — distinct per recipient
       SELECT UserId, COUNT(*) FROM UserNotifications
       WHERE Type = 'ASMT_ALL_COMPLETED' AND Message LIKE '%' + (SELECT Title FROM AssessmentSessions WHERE Id = {sessionId_E}) + '%'
       GROUP BY UserId;
       -- Expected: setiap UserId punya count = 1
       ```
    8. **Result:** [ ] PASS / [ ] FAIL

    ## Step 6 — D-04 status branching: Per-status BI message

    1. Navigate ke `/Admin/AssessmentMonitoringDetail/{sessionId_C}` (Status=Open)
    2. Buka DevTools Console, run fetch finalize (Step 1 #6 snippet dengan sessionId_C)
    3. **Expected response:**
       ```json
       { "success": false, "message": "Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian." }
       ```
    4. Repeat untuk Status=Cancelled (jika tersedia di test DB):
       Expected message: `"Tidak bisa di-finalize. Session sudah dibatalkan."`
    5. **Result:** [ ] PASS / [ ] FAIL

    ## Sign-Off

    | Criterion | Verifier | Date | Result |
    |-----------|----------|------|--------|
    | SC #1 friendly no-op | _________ | ____-__-__ | [ ] PASS [ ] FAIL |
    | SC #2 disabled + tooltip | _________ | ____-__-__ | [ ] PASS [ ] FAIL |
    | SC #3 notif dedup | _________ | ____-__-__ | [ ] PASS [ ] FAIL |
    | SC #4 audit dedup | _________ | ____-__-__ | [ ] PASS [ ] FAIL |
    | SC #5 parallel finalize | _________ | ____-__-__ | [ ] PASS [ ] FAIL |
    | D-04 status branching | _________ | ____-__-__ | [ ] PASS [ ] FAIL |

    **Overall:** [ ] PASS / [ ] FAIL — _semua criterion harus PASS untuk close phase_

    ---
    *UAT script: 310-UAT.md — Phase 310 Essay Finalize Idempotency*
    *Created: 2026-05-02 | Filled: ____*
    ```

    **Constraints:**
    - Playwright spec WAJIB pakai `test.skip(true, '...')` saat seed fixture tidak ada — RED state expected pre-Wave 1 manual seed.
    - UAT.md WAJIB Bahasa Indonesia di semua user-facing instruksi (consistent dengan 308-UAT.md precedent).
    - SQL queries di UAT.md pakai SQL Server syntax (`SELECT ... FROM ... WHERE ... + ... LIKE '%' + (SELECT ...)`).
    - JANGAN tambah `.spec.ts` test yang require fixture seed otomatis di Wave 0 — itu out of scope (no .NET seed runner). Test ini SCAFFOLD, manual seed deferred.
    - File path Windows: pakai forward slash di task description (Bash compatibility), tapi Write tool path absolute Windows pakai backslash.

    **Verify:**
    - `cd tests && npx playwright test --list` returns ≥ 12 tests (4 FLOW 1 existing + 4 FLOW 7 + 4 FLOW 8 + 3 FLOW 9 baru ≈ 15+, varies dengan FLOW 2-6).
    - `cd tests && npx playwright test --grep "Phase 310" --list` returns 3 tests.
    - 310-UAT.md exists and contains 6 step sections + sign-off table.
  </action>
  <acceptance_criteria>
    - `grep -F "FLOW 9: Phase 310" tests/e2e/assessment.spec.ts` returns 1
    - `grep -F "Assessment - Phase 310 Essay Finalize Idempotency" tests/e2e/assessment.spec.ts` returns 1
    - `grep -c "test('9\\." tests/e2e/assessment.spec.ts` returns 3 (test 9.1, 9.2, 9.3)
    - `grep -F "alreadyFinalized" tests/e2e/assessment.spec.ts` returns ≥ 1
    - `grep -F "Sudah selesai pada" tests/e2e/assessment.spec.ts` returns ≥ 1 (assertion tooltip)
    - `grep -F "Belum bisa di-finalize" tests/e2e/assessment.spec.ts` returns ≥ 1 (D-04 assertion)
    - File `.planning/phases/310-essay-finalize-idempotency/310-UAT.md` exists
    - `grep -c "## Step" .planning/phases/310-essay-finalize-idempotency/310-UAT.md` returns ≥ 6
    - `grep -F "Manual UAT — Phase 310" .planning/phases/310-essay-finalize-idempotency/310-UAT.md` returns 1
    - `grep -F "ASMT_ALL_COMPLETED" .planning/phases/310-essay-finalize-idempotency/310-UAT.md` returns ≥ 1 (SQL query SC #3)
    - `grep -F "FinalizeEssayGrading" .planning/phases/310-essay-finalize-idempotency/310-UAT.md` returns ≥ 1 (SQL query SC #4)
    - `cd tests && npx playwright test --list` exit code = 0 (no syntax error)
    - `cd tests && npx playwright test --grep "Phase 310" --list 2>&1 | grep -c "9\\."` returns 3
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test --grep "Phase 310" --list 2>&1 | tail -10</automated>
  </verify>
  <done>FLOW 9 Playwright describe block dengan 3 test (9.1 disabled state, 9.2 alreadyFinalized branch, 9.3 D-04 status open) appended ke spec file dengan test.skip fallback untuk pre-seed RED state. 310-UAT.md draft dengan 6 step section + sign-off table BI. Both files lint pass (no TS syntax error, markdown well-formed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT 6-step Bahasa Indonesia + sign-off</name>
  <what-built>
    Plan 01: Backend FinalizeEssayGrading idempotent + NotifyIfGroupCompleted dedup + AuditLog gated + ViewModel extended.
    Plan 02 Tasks 1-2: UI gate D-02 (button disabled + tooltip wrapped span), JS handler D-03/D-04 (alreadyFinalized → alert-info, status non-terminal → alert-danger), showAlert helper extracted, Bootstrap tooltip activation, Playwright FLOW 9 scaffold + 310-UAT.md draft.

    All 5 ROADMAP success criteria addressed:
    - SC #1 friendly no-op (D-03)
    - SC #2 UI hide via disabled (D-02)
    - SC #3 notif dedup (D-05)
    - SC #4 audit distinct (D-07)
    - SC #5 concurrent parallel (D-06 — proven via WHERE-clause guard pattern adopt dari Phase 309-03 GradingService)
  </what-built>
  <how-to-verify>
    Buka file `.planning/phases/310-essay-finalize-idempotency/310-UAT.md` dan jalankan 6 step UAT secara berurutan:

    1. **Step 1 (SC #1 friendly no-op)** — Login Admin, klik Finalize pada session PendingGrading, verify reload sukses; trigger 2nd fetch via DevTools Console, verify response `alreadyFinalized:true`.

    2. **Step 2 (SC #2 UI gate)** — Refresh page, verify tombol greyed out + hover tooltip "Sudah selesai pada {tanggal} WIB".

    3. **Step 3 (SC #3 notif dedup)** — Run SQL query `UserNotifications` GROUP BY UserId, verify count = 1 per recipient.

    4. **Step 4 (SC #4 audit dedup)** — Run SQL `AuditLogs WHERE Action='FinalizeEssayGrading'`, verify count = 1.

    5. **Step 5 (SC #5 concurrent)** — Buka 2 tab, sync click, run 4 SQL verify queries; expected 1 TrainingRecord, 1 NomorSertifikat, 1 audit, 1 notif/recipient.

    6. **Step 6 (D-04 status)** — DevTools Console fetch pada session Status=Open, verify response message `"Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian."`.

    Setiap step memiliki Result row [ ] PASS / [ ] FAIL — fill semuanya. Sign-off table di akhir UAT.md.

    **Bahasa Indonesia mandatory.** Jika ada copy yang masih English atau salah format, FAIL dan report.
  </how-to-verify>
  <resume-signal>Type "approved" jika semua 6 step PASS dan sign-off table sudah ter-fill. Atau describe issues (step nomor + observed vs expected) jika FAIL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Admin/HC → Razor view rendering | ViewModel.Status + NomorSertifikat dari Plan 01 — server-side render conditional D-02 |
| Browser fetch → /Admin/FinalizeEssayGrading | AntiForgeryToken existing — preserved |
| JS DOM injection (showAlert) | Inline HTML construction dengan user data (data.message dari server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-310-07 | Information Disclosure | Tooltip leak NomorSertifikat ke unauthorized viewer | accept | Endpoint `[Authorize(Roles="Admin,HC")]` — viewer memang authorized. Tidak ada PII di tooltip text (hanya tanggal CompletedAt). |
| T-310-08 | XSS | showAlert injects raw HTML termasuk data.message dari server | mitigate | Server controls D-03/D-04 message content — tidak ada user input pass-through. Pesan literal dari Plan 01 hard-coded BI strings. Future risk: kalau message dynamic dari user, wajib HTML-encode. Document di SUMMARY. |
| T-310-09 | Tampering | Client-side bypass disabled button via DevTools | accept | Client-side disabled cosmetic — Plan 01 backend WHERE-clause guard adalah real protection (D-06). Bypass via DevTools tetap di-handle dengan alreadyFinalized friendly response. |
| T-310-10 | Denial of Service | Spam fetch from console flood server | accept | Existing rate limit ASP.NET Core middleware — out of phase scope. AuditLog/Notif dedup cap downstream side-effects. |
| T-310-11 | Repudiation | Admin claim "saya tidak klik 2x" | mitigate | AuditLog distinct per session (D-07) — race-lost thread tidak append ke audit, audit count = jumlah session lifecycle Pending→Completed transition (1). |
</threat_model>

<verification>
**Per-task automated:**
- Task 1: `dotnet build` exit 0 + 13+ grep patterns match.
- Task 2: `npx playwright test --grep "Phase 310" --list` returns 3 tests; UAT.md grep patterns match.
- Task 3: Manual checkpoint — user run 6-step UAT, fills sign-off table.

**Plan-level integration:**
- Smoke test: navigate ke seeded session Completed → verify visual disabled state + tooltip hover.
- Network tab: trigger fetch → verify response shape match contract Plan 01 (alreadyFinalized field present).
- Cross-browser: Chrome + Firefox + Edge tooltip activation consistent.

**Required reading verification:** Tasks 1-2 setiap punya `<read_first>` minimum 5 sources covering: target file current state, PATTERNS.md analog, UI-SPEC contract, CONTEXT.md decision, RESEARCH.md pitfall (#6 disabled tooltip + #8 TempData anti-pattern).

**Phase gate:** Plan 02 done = build pass + Playwright list ≥ 3 Phase 310 tests + 310-UAT.md filled with sign-off PASS.
</verification>

<success_criteria>
- Razor view button gate dengan 2 branches (isFinalized vs not), span tooltip wrapper untuk Pitfall #6 mitigation.
- JS handler 3-way branching: alreadyFinalized → showAlert info biru, success → reload, error → showAlert danger merah.
- showAlert helper extracted dari L1383-1389 pattern, auto-dismiss 5s/7s.
- Bootstrap tooltip activation snippet via DOMContentLoaded.
- Playwright FLOW 9 dengan 3 test scaffold (9.1 disabled, 9.2 alreadyFinalized, 9.3 D-04 status open) — RED state expected pre-seed, GREEN saat fixture available.
- 310-UAT.md draft 6 step Bahasa Indonesia + sign-off table.
- Manual UAT PASS user-approved: SC #1, SC #2, SC #3, SC #4, SC #5, D-04 all verified.
- Bahasa Indonesia di semua user-facing copy: tooltip text, alert messages, label prefixes, UAT instructions.
- All locked decisions implemented: D-02 (UI gate disabled+tooltip) ✓, D-03 (UI render alert-info) ✓, D-04 (UI render alert-danger BI) ✓.
</success_criteria>

<output>
After completion, create `.planning/phases/310-essay-finalize-idempotency/310-02-SUMMARY.md` containing:
- Files modified dengan line ranges (Razor edits + JS edits)
- Patterns reused (showAlert extract from L1383-1389, tooltip activation from AddTraining L394-397)
- Pitfall mitigations applied (#6 span wrapper, #8 inline alert vs TempData)
- Playwright FLOW 9 test count + grep verification
- 310-UAT.md sign-off summary (PASS/FAIL per step + dates + verifiers)
- Threat mitigations applied (T-310-07..T-310-11)
- 5 ROADMAP success criteria coverage matrix (SC #1..#5 → which artifact addresses each)
- Phase closure recommendation (ready for `/gsd-verify-work` or remaining issues)
</output>
