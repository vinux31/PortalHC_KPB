---
phase: 311-manageassessment-performance
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - .planning/phases/311-manageassessment-performance/311-BASELINE.md
autonomous: false
requirements: [PERF-01]
tags: [perf, ef-core, instrumentation, baseline, measurement-driven]
must_haves:
  truths:
    - "Action ManageAssessment menulis log entry 'ManageAssessment perf breakdown: t1=...ms t2=...ms t3=...ms t4=...ms t5=...ms total=...ms tab=... search_present=... page=...' setiap kali diakses (D-11 + D-16)"
    - "5 segment Stopwatch (T1=Assessment query L66-110+grouping, T2=GetWorkersInSection L210, T3=GetAllWorkersHistory L212, T4=GetAllSectionsAsync+GetUnitsForSectionAsync L220-223, T5=Distinct Categories L172-176) ter-instrumentasi terpisah (D-16)"
    - "Build berhasil 0 errors, jumlah warnings ≤ 92 (Phase 309 baseline preserved)"
    - "Baseline 5x cold run terdokumentasi di 311-BASELINE.md dengan median per segment (skip first run sebagai JIT warmup per D-12)"
    - "User sudah commit decision Skenario A (T1 dominan >60%), Skenario B (T2/T3 dominan >50%), atau Skenario C (mixed) di 311-BASELINE.md sign-off section"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Per-segment Stopwatch instrumentation di action ManageAssessment (T1..T5 + Total) + structured logging via _logger.LogInformation"
      contains: "ManageAssessment perf breakdown:"
    - path: ".planning/phases/311-manageassessment-performance/311-BASELINE.md"
      provides: "Baseline measurement record (5x cold runs, median per segment, Skenario A/B/C decision sign-off)"
      contains: "Skenario A"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (action ManageAssessment)"
      to: "Serilog/Console log output"
      via: "_logger.LogInformation structured template 'ManageAssessment perf breakdown: t1={T1}ms ...'"
      pattern: "ManageAssessment perf breakdown:"
    - from: ".planning/phases/311-manageassessment-performance/311-BASELINE.md"
      to: "User decision gate"
      via: "STOP checkpoint Skenario A/B/C — orchestrator pause execute"
      pattern: "Skenario [ABC]"
---

<objective>
Instrumentasi `ManageAssessment` action dengan per-segment Stopwatch (T1..T5) supaya kita bisa ukur secara konkret bottleneck dominan SEBELUM apply patch optimasi apapun. Ini Wave 0 measurement-driven gate per D-16: tanpa breakdown per-query, kita tidak tahu apakah optimasi Assessment query saja (D-14 default scope) cukup mencapai ≥30% improvement, atau Training/History queries (D-16 disclosed) yang sebenarnya dominan.

**Purpose:** Hindari risiko Phase 311 gagal capai SC #6 karena salah optimasi target. Per D-16, baseline numbers menentukan keputusan scope: Skenario A (proceed Wave 1 sesuai rencana), Skenario B (STOP — expand scope), atau Skenario C (user decide explicit dengan data nyata). Pattern philosophy ini mirror Phase 310 ExecuteUpdate rowsAffected gate (PATTERNS.md Cross-cutting Pattern 5).

**Output:**
- Permanent Stopwatch instrumentation di `Controllers/AssessmentAdminController.cs` action `ManageAssessment` (D-13 — tidak dihapus post-validation, value untuk ongoing perf monitoring)
- `.planning/phases/311-manageassessment-performance/311-BASELINE.md` artifact berisi 5x cold runs, median per segment, dan user sign-off Skenario A/B/C
- Decision gate yang menentukan apakah Plan 02 (patches) di-eksekusi sesuai rencana (Skenario A) atau di-pause untuk re-discuss (Skenario B/C)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/311-manageassessment-performance/311-CONTEXT.md
@.planning/phases/311-manageassessment-performance/311-RESEARCH.md
@.planning/phases/311-manageassessment-performance/311-PATTERNS.md
@.planning/phases/311-manageassessment-performance/311-VALIDATION.md
@Controllers/AssessmentAdminController.cs
@Services/WorkerDataService.cs
@Program.cs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase verbatim. -->
<!-- No exploration needed — these are the exact patterns to copy. -->

**Action signature (Controllers/AssessmentAdminController.cs:60-62) — DO NOT CHANGE:**
```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ManageAssessment(string? search, int page = 1, int pageSize = 20,
    string? tab = null, string? section = null, string? unit = null,
    string? category = null, string? statusFilter = null, string? isFiltered = null)
```

**Constructor injection (already present at L22-23, L34-35, L42-43):**
```csharp
private readonly IMemoryCache _cache;
private readonly ILogger<AssessmentAdminController> _logger;
```

**Existing structured logging convention (PATTERNS.md Cross-cutting Pattern 1, source L2839-2841):**
```csharp
_logger.LogInformation(
    "FinalizeEssayGrading: race condition session {SessionId} — skip side-effects (already finalized).",
    sessionId);
```
Konvensi: prefix message dengan action name; placeholder PascalCase; NO interpolation `$"..."` (pakai positional args supaya Serilog bisa index field).

**Imports needed:** `System.Diagnostics` untuk Stopwatch — gunakan fully-qualified `System.Diagnostics.Stopwatch.StartNew()` SEPERTI di RESEARCH Pattern 4 supaya tidak perlu tambah `using` baru (verify file header sudah ada `using System.Diagnostics` atau tidak; kalau belum, tambahkan).

**5 segment yang harus diukur (per D-16 + RESEARCH §"Implementation Sequence" Wave 0):**
| Segment | Range | Description |
|---------|-------|-------------|
| T1 | L66-169 | managementQuery + filter + projection + grouping + statusFilter |
| T5 | L172-176 | Distinct Categories query (akan di-cache di Wave 1) |
| T2 | L210 | `_workerDataService.GetWorkersInSection(...)` |
| T3 | L212 | `_workerDataService.GetAllWorkersHistory()` |
| T4 | L220-223 | `_context.GetAllSectionsAsync()` + `_context.GetUnitsForSectionAsync(section)` |
| Total | full action | swTotal dari L64 awal sampai sebelum `return View()` L226 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah per-segment Stopwatch instrumentasi di action ManageAssessment</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs L1-50 (imports + constructor) — verifikasi `using System.Diagnostics;` ada atau tidak; kalau belum, tambahkan setelah `using Microsoft.EntityFrameworkCore;`
    - Controllers/AssessmentAdminController.cs L60-227 (full body action ManageAssessment) — target instrumentation
    - Controllers/AssessmentAdminController.cs L2839-2841 (analog structured logging) — copy konvensi message format + placeholder naming
    - .planning/phases/311-manageassessment-performance/311-PATTERNS.md §"Core pattern E — Per-segment Stopwatch + structured logging" L194-258 (verbatim code template)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-11 + D-13 + D-16 (decisions: Stopwatch lokasi, permanent, 5 segments)
  </read_first>
  <action>
**Insertion plan (NO query/projection logic changes — purely instrumentation):**

1. **L64 (top of action body, after `var sevenDaysAgo = ...`)** — declare swTotal + per-segment slot vars:
```csharp
var swTotal = System.Diagnostics.Stopwatch.StartNew();
long t1 = 0, t2 = 0, t3 = 0, t4 = 0, t5 = 0;
```

2. **WRAP T1 (L66-169 = managementQuery chain + projection + grouping + statusFilter)** dengan swT1:
```csharp
var swT1 = System.Diagnostics.Stopwatch.StartNew();
// ... existing L66-169 unchanged (managementQuery, search filter, category filter, allSessions ToListAsync, prePostGrouped, standardGrouped, grouped, statusFilter) ...
swT1.Stop();
t1 = swT1.ElapsedMilliseconds;
```

3. **WRAP T5 (L172-176 = ViewBag.Categories distinct query)** dengan swT5:
```csharp
var swT5 = System.Diagnostics.Stopwatch.StartNew();
ViewBag.Categories = await _context.AssessmentSessions
    .Select(a => a.Category)
    .Distinct()
    .OrderBy(c => c)
    .ToListAsync();
swT5.Stop();
t5 = swT5.ElapsedMilliseconds;
```

4. **WRAP T2 (L210 = GetWorkersInSection call)** dengan swT2 — instrumentasi DI DALAM blok `{ ... }` L198-224 yang sudah ada, JANGAN restructure block scope:
```csharp
List<WorkerTrainingStatus> workers;
if (isInitialState)
    workers = new List<WorkerTrainingStatus>();
else
{
    var swT2 = System.Diagnostics.Stopwatch.StartNew();
    workers = await _workerDataService.GetWorkersInSection(section, unit, category, search, statusFilter);
    swT2.Stop();
    t2 = swT2.ElapsedMilliseconds;
}
```

5. **WRAP T3 (L212 = GetAllWorkersHistory call):**
```csharp
var swT3 = System.Diagnostics.Stopwatch.StartNew();
var (assessmentHistory, trainingHistory) = await _workerDataService.GetAllWorkersHistory();
swT3.Stop();
t3 = swT3.ElapsedMilliseconds;
```

6. **WRAP T4 (L220-223 = GetAllSectionsAsync + GetUnitsForSectionAsync):**
```csharp
var swT4 = System.Diagnostics.Stopwatch.StartNew();
ViewBag.TrainingSections = await _context.GetAllSectionsAsync();
ViewBag.TrainingUnits = !string.IsNullOrEmpty(section)
    ? await _context.GetUnitsForSectionAsync(section)
    : new List<string>();
swT4.Stop();
t4 = swT4.ElapsedMilliseconds;
```

7. **TEPAT SEBELUM `return View();` L226** — stop swTotal + emit log:
```csharp
swTotal.Stop();
_logger.LogInformation(
    "ManageAssessment perf breakdown: t1={T1}ms t2={T2}ms t3={T3}ms t4={T4}ms t5={T5}ms total={Total}ms tab={Tab} search_present={SearchPresent} page={Page}",
    t1, t2, t3, t4, t5, swTotal.ElapsedMilliseconds, activeTab,
    !string.IsNullOrEmpty(search), page);

return View();
```

**KRITIS — preserve as-is:**
- TIDAK mengubah signature method (D-14 backward compat).
- TIDAK menambah `.AsNoTracking()`, TIDAK menghapus `.Include(...)`, TIDAK mengubah cache pattern. Semua optimization patches DEFER ke Plan 02 (Wave 1) setelah Skenario A approval.
- TIDAK mengubah ViewBag shape (D-15 smoke parity hard requirement).
- TIDAK menambah try/catch — pattern controller ini "let exceptions bubble" (PATTERNS.md §"Error handling pattern").
- Kalau `using System.Diagnostics;` belum di-import di L1-15, gunakan fully-qualified `System.Diagnostics.Stopwatch` SEPERTI contoh di atas (verbose tapi explicit, no import needed). Kalau planner pilih untuk add `using System.Diagnostics;`, konsisten pakai `Stopwatch.StartNew()` saja.

**Konvensi field naming (ditetapkan dari analog L2839-2841):**
- Message prefix: `"ManageAssessment perf breakdown: "` (bukan `"ManageAssessment: perf "` — match PATTERNS.md Pattern 4)
- Placeholders: `{T1}` `{T2}` `{T3}` `{T4}` `{T5}` `{Total}` `{Tab}` `{SearchPresent}` `{Page}` (PascalCase per cross-cutting pattern 1)
- Positional args order match placeholder order
  </action>
  <verify>
    <automated>dotnet build --nologo -v q 2>&1 | grep -E "(Build succeeded|Build FAILED|Error|0 Error)"</automated>
    Expected: "Build succeeded" + 0 errors. Warnings ≤ 92 (Phase 309 baseline preserved).
  </verify>
  <acceptance_criteria>
    - `grep -c "perf breakdown:" Controllers/AssessmentAdminController.cs` = 1 (single log call)
    - `grep -c "Stopwatch.StartNew" Controllers/AssessmentAdminController.cs` ≥ 6 (1 swTotal + 5 per-segment) di action ManageAssessment area
    - `grep -nE "swT[1-5]\.Stop|swTotal\.Stop" Controllers/AssessmentAdminController.cs` = 6 (5 segments + total)
    - `dotnet build --nologo -v q` exit 0, 0 errors, warnings ≤ 92
    - Action signature L60-62 unchanged (grep diff vs HEAD): `git diff HEAD Controllers/AssessmentAdminController.cs | grep "^-.*public async Task<IActionResult> ManageAssessment"` = 0 hits (signature line tidak boleh dimodifikasi)
    - ViewBag assignments unchanged: `git diff HEAD Controllers/AssessmentAdminController.cs | grep -E "^\-\s+ViewBag\." | wc -l` = 0 (tidak ada ViewBag assignment yang dihapus)
  </acceptance_criteria>
  <done>
    Build hijau (0 errors, ≤ 92 warnings). Action ManageAssessment punya 5 per-segment Stopwatch + 1 swTotal + 1 structured log call dengan format yang match PATTERNS.md Pattern 4. Tidak ada perubahan signature, ViewBag shape, query logic, atau caching pattern. Commit message: `feat(311): add per-segment Stopwatch instrumentation to ManageAssessment (D-11, D-13, D-16)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture baseline 5x cold + dokumentasi T1..T5 median di 311-BASELINE.md</name>
  <files>.planning/phases/311-manageassessment-performance/311-BASELINE.md</files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-12 (5x cold protocol, skip first run sebagai JIT warmup, median p95)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-16 (Skenario A/B/C decision matrix)
    - .planning/phases/311-manageassessment-performance/311-RESEARCH.md §"Implementation Sequence" Wave 0 Task 0.2..0.4 (snapshot DB + run protocol)
    - .planning/phases/311-manageassessment-performance/311-RESEARCH.md Pitfall 9 (Baseline Reproducibility — snapshot Dev DB sebelum run supaya post-patch run pakai dataset yang sama)
    - .planning/phases/311-manageassessment-performance/311-VALIDATION.md §"Manual-Only Verifications" row 1 (5-step manual procedure: dotnet run, login admin, curl/browser hit endpoint 5x, grep log, record median)
  </read_first>
  <action>
**Step 1 — Snapshot Dev DB sebelum baseline run (Pitfall 9 mitigation):**
```bash
# Identify Dev DB dari appsettings.Development.json: Server=localhost\SQLEXPRESS;Database=HcPortalDB_Dev
# Snapshot via SSMS GUI (Tasks → Back Up...) ATAU sqlcmd:
sqlcmd -S "localhost\SQLEXPRESS" -E -Q "BACKUP DATABASE [HcPortalDB_Dev] TO DISK = 'C:\temp\HcPortalDB_Dev_311_baseline.bak' WITH INIT;"
```
Catat lokasi backup di 311-BASELINE.md untuk reference post-patch (Plan 02 Task 8 akan restore dataset SAMA supaya delta murni dari patches).

**Step 2 — Start app:**
```bash
dotnet run --no-launch-profile --project HcPortal.csproj > baseline-311.log 2>&1 &
# wait sampai "Now listening on: http://localhost:5000" muncul di baseline-311.log
```

**Step 3 — Login + run 5x cold filter (browser ATAU curl):**

Browser route: login `https://localhost:5001/Account/Login` sebagai admin (UseActiveDirectory=false untuk dev), navigate `/Admin/ManageAssessment?tab=assessment&page=1&pageSize=20` 5x dengan hard refresh (Ctrl+F5) untuk bypass HTTP cache.

Curl route (preferred — reproducible):
```bash
# 1. Get login session cookie
curl -c cookies.txt -X POST https://localhost:5001/Account/Login \
  -d "Email=admin@pertamina.com&Password=YOUR_DEV_PASSWORD&__RequestVerificationToken=..." \
  -k

# 2. Hit endpoint 5x cold (sleep 3s antar run untuk reset connection pool warmth)
for i in 1 2 3 4 5; do
  curl -b cookies.txt "https://localhost:5001/Admin/ManageAssessment?tab=assessment&page=1&pageSize=20" \
    -o /dev/null -k -s -w "Run $i: %{time_total}s HTTP %{http_code}\n"
  sleep 3
done
```

**Step 4 — Extract log entries:**
```bash
grep "ManageAssessment perf breakdown" baseline-311.log
# Expected output 5 lines, e.g.:
# info: HcPortal.Controllers.AssessmentAdminController[0]
#       ManageAssessment perf breakdown: t1=420ms t2=180ms t3=95ms t4=15ms t5=120ms total=830ms tab=assessment search_present=False page=1
```

**Step 5 — Compute median (skip Run 1 sebagai JIT warmup per D-12):**
- Ambil run 2..5 (4 samples)
- Median per segment = average dari 2 nilai tengah (untuk 4 samples, median = (sample[1] + sample[2]) / 2 setelah sort)

**Step 6 — Tulis 311-BASELINE.md dengan struktur:**

```markdown
---
phase: 311
artifact: baseline
captured: <ISO date>
captured_by: <user>
db_snapshot: C:\temp\HcPortalDB_Dev_311_baseline.bak
captured_runs: 5
samples_used: runs 2-5 (run 1 dropped per D-12 JIT warmup)
---

# Phase 311 — Baseline Measurement (Pre-Patch)

## Test Configuration

| Property | Value |
|----------|-------|
| Endpoint | `GET /Admin/ManageAssessment?tab=assessment&page=1&pageSize=20` |
| Filter | no search, no category, no statusFilter |
| Cold runs | 5 (3s sleep between) |
| Drop run | 1 (JIT warmup per D-12) |
| Samples used | runs 2-5 |
| Dataset snapshot | `C:\temp\HcPortalDB_Dev_311_baseline.bak` |
| Build commit | <git rev-parse HEAD output> |

## Raw Log Entries

```
<paste 5 raw log lines from `grep "ManageAssessment perf breakdown" baseline-311.log`>
```

## Per-Segment Numbers (ms)

| Run | T1 | T2 | T3 | T4 | T5 | Total | curl time_total |
|-----|----|----|----|----|----|-------|------------------|
| 1 (drop) | ... | ... | ... | ... | ... | ... | ... |
| 2 | ... | ... | ... | ... | ... | ... | ... |
| 3 | ... | ... | ... | ... | ... | ... | ... |
| 4 | ... | ... | ... | ... | ... | ... | ... |
| 5 | ... | ... | ... | ... | ... | ... | ... |
| **Median (2-5)** | **...** | **...** | **...** | **...** | **...** | **...** | **...** |

## Bottleneck Analysis

| Segment | Median (ms) | % of Total | Notes |
|---------|-------------|------------|-------|
| T1 (Assessment query L66-169) | ... | ...% | scope D-08+D-09 patches target ini |
| T2 (GetWorkersInSection L210) | ... | ...% | OUT OF SCOPE per D-14 |
| T3 (GetAllWorkersHistory L212) | ... | ...% | OUT OF SCOPE per D-14 |
| T4 (Sections+Units L220-223) | ... | ...% | OUT OF SCOPE per D-14 |
| T5 (Distinct Categories L172-176) | ... | ...% | scope D-04 cache target ini |

## Decision Gate (D-16)

**Skenario:** ☐ A (T1 dominan >60%) | ☐ B (T2/T3 dominan >50% combined) | ☐ C (mixed)

**User sign-off:** ___________________  **Date:** ___________

**Rationale:**
> <user fills 1-2 paragraph rationale based on numbers above>

**Decision:**
- ☐ **PROCEED** to Plan 02 Wave 1 (apply patches as planned per D-04..D-09)
- ☐ **STOP** — return to /gsd-discuss-phase to revise D-14 scope (expand to T2/T3) atau spawn new lazy-load phase

## Next Step

If PROCEED → Plan 02 (`311-02-PLAN.md`) eksekusi.
If STOP → orchestrator pause, surface decision ke user, /gsd-discuss-phase iteration.
```

**Tulis file dengan placeholder `...` di numbers cells — Task 3 akan fill setelah user execute baseline runs.**

**Tidak commit yet — wait sampai Task 3 (human checkpoint) approve.**
  </action>
  <verify>
    <automated>test -f .planning/phases/311-manageassessment-performance/311-BASELINE.md && grep -q "Decision Gate (D-16)" .planning/phases/311-manageassessment-performance/311-BASELINE.md && echo "OK"</automated>
    Expected: "OK" — file exists dengan struktur Decision Gate section.
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/311-manageassessment-performance/311-BASELINE.md` exists
    - File mengandung header section: "Test Configuration", "Raw Log Entries", "Per-Segment Numbers (ms)", "Bottleneck Analysis", "Decision Gate (D-16)"
    - File punya placeholder cells (`...`) untuk T1..T5 numbers per run + median row
    - File punya checkbox markdown untuk Skenario A/B/C selection
    - File punya signature line untuk user sign-off
    - File mention `db_snapshot` path di frontmatter (Pitfall 9 mitigation)
  </acceptance_criteria>
  <done>
    `311-BASELINE.md` scaffold tertulis lengkap dengan 5-section struktur dan placeholder untuk numbers. File belum commit (wait Task 3 checkpoint). Tidak ada baseline numbers yang fabricated — semua cell placeholder `...` menunggu user execute manual run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: STOP gate — user execute baseline runs + decide Skenario A/B/C</name>
  <what-built>
    Per-segment Stopwatch instrumentasi di action ManageAssessment (Task 1) sudah compile + scaffold artifact `311-BASELINE.md` dengan struktur Decision Gate (Task 2). Sekarang user yang execute manual baseline runs di environment Dev mereka karena Claude tidak punya akses SQL Server lokal + browser session admin login.
  </what-built>
  <how-to-verify>
**Langkah-langkah (user execute, ~15 menit):**

1. **Snapshot DB Dev** sesuai instruksi Task 2 Step 1 (sqlcmd BACKUP DATABASE atau SSMS GUI). Konfirmasi `.bak` file tersimpan di `C:\temp\HcPortalDB_Dev_311_baseline.bak` (atau path alternatif — catat di 311-BASELINE.md).

2. **Start app fresh** (cold start untuk JIT warmup di run 1):
   ```bash
   dotnet run --no-launch-profile --project HcPortal.csproj > baseline-311.log 2>&1 &
   ```
   Tunggu sampai "Now listening on: http://localhost:5000" (atau https://localhost:5001) muncul di `baseline-311.log`.

3. **Login admin** via browser (atau curl, lihat Task 2 Step 3 untuk command).

4. **Hit endpoint 5x cold** dengan filter: `tab=assessment&page=1&pageSize=20` (NO search, NO category, NO statusFilter). Sleep 3s antar request.

5. **Extract log entries:**
   ```bash
   grep "ManageAssessment perf breakdown" baseline-311.log | tee baseline-runs.txt
   ```
   Expected 5 baris dengan format `t1=...ms t2=...ms ... total=...ms tab=assessment search_present=False page=1`.

6. **Compute median (skip run 1 per D-12):** ambil run 2-5, untuk tiap segment T1..T5+Total = average dari 2 nilai tengah.

7. **Edit `311-BASELINE.md`** — fill semua `...` placeholder cells (Run 1-5 + Median row + % of Total + Bottleneck Analysis), paste raw log entries di section "Raw Log Entries".

8. **Decide Skenario:** evaluasi % of Total per segment:
   - **Skenario A:** T1 > 60% total → patches D-04..D-09 valid scope, **PROCEED Plan 02**
   - **Skenario B:** T2 + T3 > 50% total combined → **STOP**, expand scope discuss
   - **Skenario C:** Mixed (T1 ~ T2/T3) → **user explicit decide** dengan numbers di tangan
   Tick checkbox `Decision Gate` section, tulis rationale 1-2 paragraf, sign-off (nama + date).

9. **Resume signal ke Claude:**
   - Kalau Skenario A → reply "approved skenario A" + paste median row dari Bottleneck Analysis
   - Kalau Skenario B/C → reply "stop skenario B" atau "stop skenario C" + paste median row + jelaskan kebutuhan scope expansion atau decision yang diambil

**Expected outcome:**
- 5 raw log entries tertulis di 311-BASELINE.md
- Median row terisi numerik
- Skenario checkbox ticked + signature ada
- Decision recorded explicit di file (audit trail untuk SUMMARY.md kelak)

**KRITIS — Skenario B/C path:**
Kalau user pilih Skenario B (T2/T3 dominan), Plan 02 sebagaimana ditulis TIDAK akan capai SC #6 ≥30% improvement karena hanya optimasi T1+T5. User harus decide:
- (a) Expand Plan 02 dengan tambah `.AsNoTracking()` di `Services/WorkerDataService.cs` GetWorkersInSection + GetAllWorkersHistory (revisi D-14 in-place), atau
- (b) Skip Phase 311 perf gain target, document hasil sebagai informational baseline + defer real optimization ke phase lazy-load architecture future.
Orchestrator akan pause, return ke /gsd-discuss-phase untuk re-decide D-14 scope dengan data konkret.
  </how-to-verify>
  <resume-signal>Reply "approved skenario A" + median row paste, ATAU "stop skenario B" / "stop skenario C" + median row paste + decision direction</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit baseline artifacts (after user sign-off Skenario A only)</name>
  <files>
    Controllers/AssessmentAdminController.cs
    .planning/phases/311-manageassessment-performance/311-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-BASELINE.md (verify user filled numbers + signed off Skenario A)
  </read_first>
  <action>
**Hanya execute kalau Task 3 = approved Skenario A.** Kalau Skenario B/C, SKIP task ini — orchestrator akan pause + re-discuss.

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "feat(311): add per-segment Stopwatch instrumentation + capture baseline (D-11, D-13, D-16, Skenario A approved)" \
  --files Controllers/AssessmentAdminController.cs \
          .planning/phases/311-manageassessment-performance/311-BASELINE.md
```

Verify commit landed:
```bash
git log -1 --oneline
git show --stat HEAD | grep -E "AssessmentAdminController|BASELINE"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | grep -q "Skenario A approved" && echo "OK"</automated>
    Expected: "OK" — commit message records Skenario A decision.
  </verify>
  <acceptance_criteria>
    - `git log -1` menunjukkan commit dengan files `Controllers/AssessmentAdminController.cs` + `311-BASELINE.md`
    - Commit message mention `Skenario A approved` (audit trail)
    - `git status` clean (no uncommitted changes)
  </acceptance_criteria>
  <done>
    Wave 0 sealed dengan 1 commit yang berisi instrumentation + baseline artifact + Skenario A sign-off. Ready handoff ke Plan 02 Wave 1+2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Admin → ASP.NET Core route | Authenticated request enters `ManageAssessment` action; existing `[Authorize(Roles = "Admin, HC")]` gate aktif |
| Action body → ILogger sink | Structured log fields ditulis ke Serilog/Console/File; logs accessible to dev ops sesuai existing logging policy |
| Action body → DB (read) | EF Core query path TIDAK BERUBAH di Plan 01 (instrumentation only), no new injection surface |
| Manual SQL backup → filesystem | User execute `BACKUP DATABASE` ke `C:\temp\` lokal; backup file mungkin berisi PII jika dataset Dev mirror Prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-311-01 | Information Disclosure | `_logger.LogInformation` "ManageAssessment perf breakdown" structured fields | accept | Log fields HANYA berisi: `t1..t5` (int ms), `total` (int ms), `tab` (string enum), `search_present` (bool), `page` (int). NO user identifier, NO PII, NO query content. Acceptable per existing logging convention (PATTERNS.md Cross-cutting Pattern 1 analog L2839 logs `{SessionId}` int — identifier saja, tidak sensitif). Grep-verifiable: `grep "ManageAssessment perf breakdown" Controllers/AssessmentAdminController.cs` shows only ms numbers + tab enum + bool. |
| T-311-02 | Denial of Service | Stopwatch overhead di hot-path action ManageAssessment | accept | Stopwatch BCL primitive, microsecond-level overhead per Start/Stop call. 6 instances × 2 calls (Start+Stop) = 12 ops, total <1ms overhead per request — negligible vs T1 baseline >100ms. D-13 explicit: permanent untuk ongoing perf monitoring. |
| T-311-03 | Information Disclosure | DB backup file `C:\temp\HcPortalDB_Dev_311_baseline.bak` di filesystem lokal | accept | Backup di Dev environment lokal user, no production data; user responsible untuk delete post-Phase-311 close. Document di SUMMARY.md sebagai cleanup todo. NO automated mitigation karena Dev workflow standard. |

**No HIGH/CRITICAL threats di Plan 01.** Instrumentation adalah pure observability addition tanpa expand attack surface.
</threat_model>

<verification>
**Phase-level checks (Plan 01 scope):**

1. **Compile gate (after Task 1):**
   ```bash
   dotnet build --nologo -v q
   ```
   Expected: 0 errors, ≤ 92 warnings (Phase 309 baseline preserved).

2. **Instrumentation grep (after Task 1):**
   ```bash
   grep -c "perf breakdown:" Controllers/AssessmentAdminController.cs   # = 1
   grep -c "Stopwatch.StartNew" Controllers/AssessmentAdminController.cs  # ≥ 6
   ```

3. **ViewBag shape preservation (after Task 1):**
   ```bash
   git diff HEAD~1 HEAD Controllers/AssessmentAdminController.cs | grep -E "^\-\s+ViewBag\." | wc -l
   ```
   Expected: 0 (no ViewBag assignment removed).

4. **Baseline artifact exists (after Task 2):**
   ```bash
   test -f .planning/phases/311-manageassessment-performance/311-BASELINE.md && \
     grep -q "Decision Gate (D-16)" .planning/phases/311-manageassessment-performance/311-BASELINE.md
   ```

5. **Decision recorded (after Task 3 user sign-off):**
   ```bash
   grep -E "^\- \[x\] \*\*(PROCEED|STOP)\*\*" .planning/phases/311-manageassessment-performance/311-BASELINE.md
   ```
   Expected: 1 hit (one of PROCEED or STOP ticked).

6. **Commit landed (after Task 4):**
   ```bash
   git log -1 --pretty=format:"%s" | grep "Skenario A approved"
   ```
</verification>

<success_criteria>
**Plan 01 complete when:**

- [x] Action `ManageAssessment` punya 5 per-segment Stopwatch + 1 swTotal + 1 structured log call dengan format `"ManageAssessment perf breakdown: t1={T1}ms ... total={Total}ms tab={Tab} search_present={SearchPresent} page={Page}"` (D-11, D-13, D-16)
- [x] `dotnet build --nologo -v q` returns 0 errors, ≤ 92 warnings
- [x] Action signature L60-62 unchanged (D-14 backward compat)
- [x] Tidak ada ViewBag assignment yang dihapus (D-15 smoke parity gate)
- [x] `.planning/phases/311-manageassessment-performance/311-BASELINE.md` exists dengan 5-section struktur (Test Configuration, Raw Log Entries, Per-Segment Numbers, Bottleneck Analysis, Decision Gate)
- [x] User sudah execute 5x cold runs + fill numbers + sign-off Skenario decision di 311-BASELINE.md
- [x] Skenario A approved → 1 git commit dengan message `feat(311): ... Skenario A approved` (Plan 02 unblocked)
- [x] OR Skenario B/C → orchestrator pause, return ke `/gsd-discuss-phase` (Plan 02 NOT executed sebagaimana drafted)
</success_criteria>

<output>
After completion, create `.planning/phases/311-manageassessment-performance/311-01-SUMMARY.md` dengan:
- Baseline T1..T5 median numbers (copy dari 311-BASELINE.md)
- Skenario decision (A/B/C) + rationale
- Build status (errors/warnings count)
- Commit hash
- Handoff note ke Plan 02 (kalau Skenario A) atau ke /gsd-discuss-phase (kalau Skenario B/C)
</output>
