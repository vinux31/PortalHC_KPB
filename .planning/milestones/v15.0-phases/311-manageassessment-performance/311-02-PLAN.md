---
phase: 311-manageassessment-performance
plan: 02
type: execute
wave: 1
depends_on: [311-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - wwwroot/lib/htmx/htmx.min.js
  - wwwroot/css/site.css
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ManageAssessment.cshtml
autonomous: false
requirements: [PERF-01]
tags: [perf, htmx, lazy-load, partial-action, frontend, backend]
must_haves:
  truths:
    - "User loading /Admin/ManageAssessment menerima shell document <14 KB (TCP first roundtrip) — verified via curl content-length"
    - "Tab aktif (default Assessment) auto-fetch konten via HTMX hx-trigger=load setelah HTMX init"
    - "Tab Training & History fetch on first activation via Bootstrap shown.bs.tab event (D-01) — sekali per session per tab"
    - "Filter form change (search debounce 500ms / dropdown instant per D-03) re-fetch tab aktif TANPA full page reload"
    - "Filter change invalidate cached HTML pada tab non-aktif (D-05) supaya next activation lazy-fetch dengan filter terbaru"
    - "Pagination klik via HTMX AJAX (D-04) swap pane content TANPA full page reload"
    - "3 partial endpoints (ManageAssessmentTab_Assessment/_Training/_History) accept filter+page params identik dengan signature ManageAssessment, return PartialView, [Authorize(Roles=\"Admin, HC\")], [ResponseCache(NoStore=true)] per D-06"
    - "Stopwatch instrumentation dipindah dari single-action ke per-action context (D-09) — 1 Stopwatch per partial action, format log 'ManageAssessment perf [tab={Tab}]: elapsed={Ms}ms search_present={SearchPresent} page={Page}'"
    - "ViewBag contract identik dengan rendering inline lama (D-10) — partial views existing tidak butuh modifikasi major"
    - "Smoke test parity per tab: kolom, row count, ordering identik pre/post (manual UAT 4-step Bahasa Indonesia)"
    - "Build hijau: 0 errors, ≤ 92 warnings (Phase 309 baseline preserved)"
    - "Dokumen REQUIREMENTS.md §PERF-01 ter-update dengan strategy + criteria HTMX lazy load (D-08)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated PERF-01 acceptance criteria + strategy untuk HTMX lazy load architecture"
      contains: "HTMX lazy load"
    - path: "wwwroot/lib/htmx/htmx.min.js"
      provides: "Vendored HTMX 2.0.x library (~14 KB minified, NOT CDN)"
      min_lines: 1
    - path: "wwwroot/css/site.css"
      provides: ".htmx-request CSS rules untuk filter form + tab pane loading state"
      contains: ".htmx-request"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Refactored ManageAssessment shell action (no data fetch except cached Categories) + 3 NEW partial actions ManageAssessmentTab_Assessment/_Training/_History"
      contains: "ManageAssessmentTab_Assessment"
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Shell view dengan HTMX attributes + skeleton placeholders 3 varian + filter form id=filter-form + script src htmx.min.js defer + error template handler"
      contains: "hx-trigger=\"shown.bs.tab"
  key_links:
    - from: "Views/Admin/ManageAssessment.cshtml (filter form + tab pane wrappers)"
      to: "Controllers/AssessmentAdminController.cs (3 partial actions)"
      via: "HTMX hx-get attribute → @Url.Action(...) Razor helper (PathBase /KPB-PortalHC auto-resolved)"
      pattern: "hx-get=\"@Url.Action\\(\"ManageAssessmentTab_"
    - from: "Filter form input change (search debounce 500ms / dropdown instant)"
      to: "Tab aktif partial endpoint"
      via: "HTMX hx-trigger='input changed delay:500ms' / 'change' + hx-target='#pane-{activeTab}' + hx-include='#filter-form'"
      pattern: "hx-include=\"#filter-form\""
    - from: "3 partial actions (ManageAssessmentTab_*)"
      to: "_logger structured log sink"
      via: "_logger.LogInformation('ManageAssessment perf [tab={Tab}]: elapsed={Ms}ms search_present={SearchPresent} page={Page}', ...)"
      pattern: "ManageAssessment perf \\[tab="
    - from: "Tab non-aktif (Training/History) wrappers"
      to: "Bootstrap tab activation lifecycle"
      via: "hx-trigger='shown.bs.tab from:closest button.nav-link once' (D-01)"
      pattern: "shown\\.bs\\.tab"
---

<objective>
Refactor `Controllers/AssessmentAdminController.cs` action `ManageAssessment` (L60-262) menjadi shell-only (no data fetch except cached Categories untuk dropdown filter) plus tiga partial actions baru (`ManageAssessmentTab_Assessment`, `ManageAssessmentTab_Training`, `ManageAssessmentTab_History`) yang masing-masing eksekusi T1/(T2+T4)/T3 logic dan return `PartialView`. Refactor `Views/Admin/ManageAssessment.cshtml` menjadi HTMX-driven: skeleton placeholder per tab, lazy fetch tab aktif via `hx-trigger="load"` (D-01), tab non-aktif via Bootstrap `shown.bs.tab` event (D-01 — D-02 vendor HTMX 2.0.x lokal di `wwwroot/lib/htmx/htmx.min.js`). Filter form pakai HTMX live (search debounce 500ms input, dropdown instant change per D-03), pagination AJAX (D-04), cross-tab cache invalidation pada filter change (D-05), partial endpoint `[ResponseCache(NoStore=true)]` (D-06), error template + retry (UI-SPEC §2). Pindah Stopwatch instrumentation dari single-action (current state post-`a4ce556e`) ke per-action context (D-09). Update REQUIREMENTS.md §PERF-01 sebagai task awal (D-08).

**Purpose:** Backend bukan bottleneck (baseline 11-27ms warm, TTFB 281ms). Wifi kantor proxy throttle content download ke ~40 byte/detik untuk 3.4 KB shell document → 1.4 menit total wall-clock. Strategi yang bekerja: minimize HTML payload via lazy load per tab. Kirim shell ringan dulu, fetch data per tab on demand.

**Output:**
- REQUIREMENTS.md §PERF-01 updated (strategy + acceptance criteria baru)
- HTMX 2.0.x vendored ke `wwwroot/lib/htmx/htmx.min.js`
- 1 controller action refactored (ManageAssessment shell-only) + 3 NEW partial actions
- 1 shell view refactored (HTMX attrs + 3 skeleton variants per UI-SPEC + filter form + error template + script tag)
- CSS rules `.htmx-request` di `wwwroot/css/site.css`
- Stopwatch per-partial-action telemetry (preserve a4ce556e pattern)
- Smoke test parity manual UAT verified
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/311-manageassessment-performance/311-CONTEXT.md
@.planning/phases/311-manageassessment-performance/311-DESIGN.md
@.planning/phases/311-manageassessment-performance/311-UI-SPEC.md
@.planning/phases/311-manageassessment-performance/311-RESEARCH.md
@.planning/phases/311-manageassessment-performance/311-PATTERNS.md
@.planning/phases/311-manageassessment-performance/311-VALIDATION.md
@Controllers/AssessmentAdminController.cs
@Views/Admin/ManageAssessment.cshtml
@Views/Admin/Shared/_AssessmentGroupsTab.cshtml
@Views/Admin/Shared/_TrainingRecordsTab.cshtml
@Views/Admin/Shared/_HistoryTab.cshtml
@appsettings.json
@Program.cs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase verbatim. -->

**Existing controller signature (Controllers/AssessmentAdminController.cs:58-62) — DO NOT change parameter list:**
```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ManageAssessment(string? search, int page = 1, int pageSize = 20,
    string? tab = null, string? section = null, string? unit = null,
    string? category = null, string? statusFilter = null, string? isFiltered = null)
```

**Constructor injection (already present at L22-48):**
```csharp
private readonly IMemoryCache _cache;
private readonly ILogger<AssessmentAdminController> _logger;
private readonly IWorkerDataService _workerDataService;
```

**ViewBag contract yang HARUS preserved (per partial view consumer):**

`_AssessmentGroupsTab.cshtml` membaca:
- `ViewBag.ManagementData` (List<dynamic>), `ViewBag.CurrentPage`, `ViewBag.TotalPages`, `ViewBag.TotalCount`
- `ViewBag.SearchTerm`, `ViewBag.Categories` (List<string>), `ViewBag.SelectedCategory`, `ViewBag.SelectedStatus`

`_TrainingRecordsTab.cshtml` membaca:
- `ViewBag.Workers` (List<WorkerTrainingStatus>), `ViewBag.IsInitialState` (bool)
- `ViewBag.SelectedSection`, `ViewBag.SelectedUnit`, `ViewBag.SelectedCategory`, `ViewBag.SelectedStatus`, `ViewBag.SearchTerm`
- `ViewBag.TrainingSections` (List<string>), `ViewBag.TrainingUnits` (List<string>)

`_HistoryTab.cshtml` membaca:
- `ViewBag.AssessmentHistory` (List<AllWorkersHistoryRow>), `ViewBag.TrainingHistory` (List<AllWorkersHistoryRow>)
- `ViewBag.AssessmentTitles` (List<string>)

**PartialView helper (existing override L55):**
```csharp
protected new PartialViewResult PartialView(string viewName, object? model)
    => base.PartialView(viewName.StartsWith("~/") ? viewName : "~/Views/Admin/" + viewName + ".cshtml", model);
```

**PathBase configuration (appsettings.json:9):** `"PathBase": "/KPB-PortalHC"` — semua HTMX `hx-get` URL HARUS pakai `@Url.Action(...)` Razor helper untuk auto-resolve prefix.

**Existing Bootstrap 5 nav-tabs structure (Views/Admin/ManageAssessment.cshtml:54-93):**
- Tab buttons: `#tab-assessment`, `#tab-training`, `#tab-history` (each `data-bs-toggle="tab" data-bs-target="#pane-..."`)
- Tab panes: `#pane-assessment`, `#pane-training`, `#pane-history` (each `class="tab-pane fade"`)
- Active tab class managed by Bootstrap JS via `new bootstrap.Tab(tabEl).show()` di `DOMContentLoaded`

**Stopwatch logging convention dari Phase 311 Plan 01 (commit a4ce556e):**
- Prefix: `"ManageAssessment perf "` (literal)
- Placeholders PascalCase: `{T1}`, `{Tab}`, `{Total}`
- Positional args, NO interpolation `$"..."`
- Pattern: `_logger.LogInformation("ManageAssessment perf breakdown: t1={T1}ms ...", t1, ...);`

**HTMX 2.0.x official source (D-02 vendor target):**
- Download from: `https://unpkg.com/htmx.org@2.0/dist/htmx.min.js`
- Save to: `wwwroot/lib/htmx/htmx.min.js`
- Approximate size: ~14 KB minified
- SHA verification recommended: cross-check `https://htmx.org/docs/#installing` published hash atau npm registry checksum

**Existing partial view patterns yang sudah established (untuk preserve):**
- Filter form di `_AssessmentGroupsTab.cshtml:14-32` — ada `<form method="get" asp-action="ManageAssessment">` (tetap GET, tapi WRAP di shell jadi `id="filter-form"` agar HTMX bisa target)
- Filter category/status dropdown (L36-83) — current pakai `onchange="applyAssessmentFilters()"` server redirect; refactor ke HTMX `hx-trigger="change"`
- Filter di `_TrainingRecordsTab.cshtml:30-...` — pakai `id="filterFormTraining"` form submit; preserve form tapi change submit handler ke HTMX

**Bootstrap 5 native event integration (D-01):**
- Bootstrap fires `shown.bs.tab` di tab button (`<button data-bs-toggle="tab">`) setelah transisi complete
- HTMX selector: `hx-trigger='shown.bs.tab from:closest button.nav-link once'`
- `from:closest button.nav-link` traverses dari tab pane wrapper KE button.nav-link sibling — verify Bootstrap structure: button di `<li>` dalam `<ul.nav-tabs>`, pane di sibling `<div class="tab-content">` — actually `closest` won't reach the button. **Use `from:#tab-{name}` selector instead** (explicit ID match).

**Active tab from query param (existing pattern):**
- `var activeTab = tab switch { "training" => "training", "history" => "history", _ => "assessment" };` (L203)
- `ViewBag.ActiveTab = activeTab` (L204)
- Razor view conditional render skeleton based on `activeTab`

**MemoryCache pattern (existing precedents L3234, L3314):**
- `_cache.TryGetValue` / `_cache.Set` style sudah ada
- `_cache.GetOrCreateAsync` pattern baru, OK to introduce

**ResponseCache attribute (existing import via Microsoft.AspNetCore.Mvc):**
```csharp
[ResponseCache(NoStore = true, Location = ResponseCacheLocation.None)]
```
Apply per D-06 untuk 3 partial actions (no browser cache, HTMX manage tab cache JS-side).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md §PERF-01 + vendor HTMX 2.0.x + add CSS .htmx-request rules</name>
  <files>
    .planning/REQUIREMENTS.md
    wwwroot/lib/htmx/htmx.min.js
    wwwroot/css/site.css
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md L33-35 (existing PERF-01 entry)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-08 (REQUIREMENTS update spec)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-02 (HTMX vendor location + version)
    - .planning/phases/311-manageassessment-performance/311-UI-SPEC.md §3 "Loading Indicator (htmx-request CSS class)" L266-294 (CSS verbatim)
    - .planning/phases/311-manageassessment-performance/311-DESIGN.md §3.2 "Library (HTMX)" L142-149 (vendor instructions)
    - https://htmx.org/docs/#installing (HTMX 2.0.x install reference)
  </read_first>
  <action>
**Step 1 — Update REQUIREMENTS.md §PERF-01 (per D-08):**

Find existing entry (line 35 area):
```markdown
- [ ] **PERF-01**: Halaman `/Admin/ManageAssessment` memuat ≥30% lebih cepat dari baseline pada dataset produksi (target p95 ≤ baseline × 0.7). Strategi: `AsNoTracking()` di chain query baris 66, hapus redundant `.Include(a => a.User)` baris 88, tambah DB index `IX_AssessmentSessions_Schedule_ExamWindowCloseDate` & `IX_LinkedGroupId` jika belum ada (cek migration history dulu), `IMemoryCache` (TTL 5 menit) untuk distinct Categories. *(maps Temuan 3)*
```

Replace dengan (verbatim):
```markdown
- [ ] **PERF-01**: Halaman `/Admin/ManageAssessment` memuat lebih cepat di wifi kantor (yang dimediasi proxy Pertamina) dengan target end-to-end ≤40 detik (≥50% reduction dari baseline ~1.4 menit). Strategi: HTMX lazy load architecture — refactor `ManageAssessment` action menjadi shell-only (no data fetch kecuali cached Categories) + 3 partial actions (`ManageAssessmentTab_Assessment`, `_Training`, `_History`) yang return PartialView per tab. Shell view `Views/Admin/ManageAssessment.cshtml` pakai HTMX attributes (vendored `wwwroot/lib/htmx/htmx.min.js` v2.0.x, ~14 KB) untuk lazy fetch per tab. Plus opportunistic backend wins di Plan 03 (AsNoTracking + IX_AssessmentSessions_LinkedGroupId + IX_AssessmentSessions_ExamWindowCloseDate + IMemoryCache TTL 5min untuk distinct Categories). Acceptance: initial response document <14 KB, end-to-end load wifi kantor ≤40 detik, tab switching ≤2 detik post-initial, smoke test parity (kolom, row count, ordering identik pre/post per tab), TTFB ≤500ms (no regression backend), filter+pagination+ViewBag contract preserved. Source: `.planning/phases/311-manageassessment-performance/311-DESIGN.md` (approved 2026-05-07). *(maps Temuan 3)*
```

**Step 2 — Vendor HTMX 2.0.x:**

```bash
mkdir -p wwwroot/lib/htmx
curl -fsSL "https://unpkg.com/htmx.org@2.0/dist/htmx.min.js" -o wwwroot/lib/htmx/htmx.min.js
# Verify size ~14 KB minified
wc -c < wwwroot/lib/htmx/htmx.min.js
# Expected: between 12000 and 18000 bytes
# Verify file mulai dengan HTMX header comment atau IIFE
head -c 200 wwwroot/lib/htmx/htmx.min.js
# Expected: starts with `(function(e,t){"use strict";` atau similar minified IIFE; mengandung "htmx" identifier
```

**SHA verification (best effort — HTMX docs tidak publish SRI hash resmi tapi npm tarball checksum bisa cross-check):**
```bash
sha256sum wwwroot/lib/htmx/htmx.min.js > wwwroot/lib/htmx/htmx.min.js.sha256
# Document hash di commit message untuk audit trail
```

Kalau download gagal (proxy block unpkg), fallback: download manual dari https://htmx.org/dist/htmx.min.js atau https://cdn.jsdelivr.net/npm/htmx.org@2.0/dist/htmx.min.js — pilih satu mirror yang accessible.

**Step 3 — Append CSS rules ke `wwwroot/css/site.css` (verbatim dari UI-SPEC §3):**

Tambahkan di akhir file `wwwroot/css/site.css`:

```css

/* === Phase 311: HTMX loading state (UI-SPEC §3) === */

/* Filter form: subtle disabled visual selama re-fetch */
#filter-form.htmx-request {
  opacity: 0.7;
  pointer-events: none;
  transition: opacity 0.15s ease-in-out;
}

/* Tab pane: skeleton sudah handle visual selama load awal.
   Untuk re-fetch (filter change, pagination), tambah subtle opacity. */
.tab-pane.htmx-request {
  opacity: 0.85;
  transition: opacity 0.15s ease-in-out;
}

/* Cursor feedback global */
.htmx-request {
  cursor: progress;
}
```

JANGAN modifikasi atau hapus rule CSS existing.
  </action>
  <verify>
    <automated>grep -q "HTMX lazy load architecture" .planning/REQUIREMENTS.md && test -f wwwroot/lib/htmx/htmx.min.js && grep -q "htmx-request" wwwroot/css/site.css && echo "OK"</automated>
    Expected: "OK" — REQUIREMENTS update applied + HTMX vendored + CSS rules appended.
  </verify>
  <acceptance_criteria>
    **Konvensi:** Semua `grep` di acceptance harus exit 0 = success. Untuk negative assertion (text harus ABSEN), pakai `! grep -q ...` (negation) supaya exit 0 kalau no match.

    - `grep -q "HTMX lazy load architecture" .planning/REQUIREMENTS.md` exit 0 (PERF-01 strategy ter-update — POSITIVE assertion)
    - `grep -q "≤40 detik" .planning/REQUIREMENTS.md` exit 0 (acceptance criteria baru — POSITIVE)
    - `grep -q "≤2 detik" .planning/REQUIREMENTS.md` exit 0 (tab switch criteria locked di D-08 — POSITIVE)
    - `grep -qE "<14\s*KB" .planning/REQUIREMENTS.md` exit 0 (initial doc size locked di D-08 — POSITIVE; regex tolerate optional whitespace)
    - `! grep -q "≥30% lebih cepat dari baseline" .planning/REQUIREMENTS.md` exit 0 (NEGATIVE assertion — old text removed; bash `!` negation, exit 0 kalau no match)
    - `test -f wwwroot/lib/htmx/htmx.min.js` exit 0 (file exists)
    - `SIZE=$(wc -c < wwwroot/lib/htmx/htmx.min.js); [ "$SIZE" -ge 8000 ] && [ "$SIZE" -le 25000 ]` exit 0 (sanity bounds; spec says ~14 KB)
    - `grep -q "htmx" wwwroot/lib/htmx/htmx.min.js` exit 0 (file mengandung HTMX identifier — POSITIVE)
    - `[ "$(grep -c '.htmx-request' wwwroot/css/site.css)" -ge 3 ]` exit 0 (≥ 3 CSS rules: #filter-form, .tab-pane, global cursor)
    - `grep -q "Phase 311: HTMX loading state" wwwroot/css/site.css` exit 0 (comment marker present — POSITIVE)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md §PERF-01 ter-update dengan strategy HTMX lazy load + acceptance criteria baru per D-08. HTMX 2.0.x ter-vendored di `wwwroot/lib/htmx/htmx.min.js` (~14 KB) dengan SHA hash dicatat di commit message. CSS `.htmx-request` rules appended ke `wwwroot/css/site.css` per UI-SPEC §3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor ManageAssessment action menjadi shell-only + tambah 3 partial actions baru di AssessmentAdminController.cs</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs L1-262 (full ManageAssessment action — current state post-a4ce556e dengan Stopwatch instrumentasi)
    - Controllers/AssessmentAdminController.cs L50-55 (View/PartialView override helpers)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-06 (ResponseCache NoStore)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-09 (Stopwatch per-action format)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-10 (ViewBag preserved)
    - .planning/phases/311-manageassessment-performance/311-DESIGN.md §3.2 "Backend" L110-117 (action breakdown spec)
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml (verify ViewBag fields consumed)
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml (verify ViewBag fields consumed)
    - Views/Admin/Shared/_HistoryTab.cshtml (verify ViewBag fields consumed)
    - **P-2 fix — partial action analogs di codebase (file:line):**
      - Controllers/CDPController.cs:306 — `return PartialView("Shared/_CoachingProtonContentPartial", model)` (PartialView return + Shared/ subfolder pattern)
      - Controllers/CDPController.cs:3623 — `return PartialView("Shared/_CertificationManagementTablePartial", vm)` (analog terdekat: Authorize controller + Shared partial)
      - Controllers/CMPController.cs:758 — `return PartialView("_RecordsTeamBody", workerList)` (root partial naming variant)
      - Controllers/CMPController.cs:3666 — `return PartialView("Shared/_SertifikatGroupTablePartial", vm)` (Authorize + Shared partial — analog struktural)
      - Controllers/RenewalController.cs:307 — `return PartialView("~/Views/Admin/Shared/_RenewalGroupedPartial.cshtml", gvm)` (absolute path resolution variant — gunakan kalau override `PartialView` helper L55 tidak applicable)
      - Pakai variant `PartialView("Shared/_...", null)` (relative + null model) sesuai existing override helper di AssessmentAdminController.cs:L55 yang resolve ke `~/Views/Admin/Shared/...cshtml`
  </read_first>
  <action>
**Tujuan:** Pecah action `ManageAssessment` menjadi 1 shell action (no data fetch except cached Categories) + 3 partial actions yang masing-masing eksekusi T1/(T2+T4)/T3 logic. Pertahankan ViewBag contract per partial view consumer (D-10). ResponseCache NoStore di partial actions (D-06). Stopwatch per-action (D-09).

**KRITIS — preserve as-is:**
- Method signature `ManageAssessment(...)` parameter list TIDAK BOLEH diubah (URL bookmarks tetap valid)
- ViewBag fields per partial consumer harus identik dengan rendering inline lama
- Authorization `[Authorize(Roles = "Admin, HC")]` di setiap action

**Step 1 — Refactor `ManageAssessment` action menjadi shell-only (L60-262):**

Replace seluruh body method `ManageAssessment` (L63 sampai sebelum closing `}` di L262) dengan:

```csharp
{
    // Phase 311 Plan 02: shell-only refactor (D-01..D-10).
    // Per D-10 backward compat: action signature + Categories ViewBag dipertahankan.
    // Data fetch per tab dipindah ke 3 partial actions (lazy load via HTMX).

    var swShell = System.Diagnostics.Stopwatch.StartNew();

    // Tab routing — default to "assessment" (preserved dari logika lama)
    var activeTab = tab switch { "training" => "training", "history" => "history", _ => "assessment" };
    ViewBag.ActiveTab = activeTab;

    // Filter values yang HARUS preserved untuk pre-populate filter form di shell view
    ViewBag.SearchTerm = search;
    ViewBag.SelectedCategory = category ?? "";
    ViewBag.SelectedStatus = statusFilter ?? "";
    ViewBag.SelectedSection = section;
    ViewBag.SelectedUnit = unit;
    ViewBag.CurrentPage = page;
    ViewBag.PageSize = pageSize;

    // Categories dropdown — dibutuhkan di shell untuk filter form. Plan 03 akan wrap dengan IMemoryCache.GetOrCreateAsync.
    // Plan 02 add .AsNoTracking() (zero-cost change, read-only path — independent dari Plan 03 cache wrapping).
    ViewBag.Categories = await _context.AssessmentSessions
        .AsNoTracking()
        .Select(a => a.Category)
        .Distinct()
        .OrderBy(c => c)
        .ToListAsync();

    swShell.Stop();
    _logger.LogInformation(
        "ManageAssessment perf [tab=shell]: elapsed={Ms}ms search_present={SearchPresent} page={Page}",
        swShell.ElapsedMilliseconds, !string.IsNullOrEmpty(search), page);

    return View();
}
```

**Step 2 — Tambah 3 partial actions BARU tepat SETELAH closing `}` action `ManageAssessment` (sebelum `// --- CATEGORY MANAGEMENT ---` block L264):**

```csharp
// =====================================================================
// Phase 311 Plan 02: Partial actions untuk HTMX lazy load (D-01..D-10).
// Setiap partial action mirror parameter signature shell + return PartialView.
// ResponseCache NoStore (D-06) — browser tidak cache, HTMX manage tab cache JS-side.
// Stopwatch per-action (D-09 — preserve a4ce556e telemetry pattern).
// =====================================================================

[HttpGet]
[Authorize(Roles = "Admin, HC")]
[ResponseCache(NoStore = true, Location = ResponseCacheLocation.None)]
public async Task<IActionResult> ManageAssessmentTab_Assessment(string? search, int page = 1, int pageSize = 20,
    string? category = null, string? statusFilter = null)
{
    var sw = System.Diagnostics.Stopwatch.StartNew();
    var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);

    var managementQuery = _context.AssessmentSessions
        .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)
        .AsQueryable();

    if (!string.IsNullOrEmpty(search))
    {
        var lowerSearch = search.ToLower();
        managementQuery = managementQuery.Where(a =>
            a.Title.ToLower().Contains(lowerSearch) ||
            a.Category.ToLower().Contains(lowerSearch) ||
            (a.User != null && (
                a.User.FullName.ToLower().Contains(lowerSearch) ||
                (a.User.NIP != null && a.User.NIP.Contains(lowerSearch))
            ))
        );
    }

    if (!string.IsNullOrEmpty(category))
        managementQuery = managementQuery.Where(a => a.Category == category);

    var allSessions = await managementQuery
        .Include(a => a.User)
        .OrderByDescending(a => a.Schedule)
        .Select(a => new
        {
            a.Id, a.Title, a.Category, a.Schedule, a.ExamWindowCloseDate, a.DurationMinutes,
            a.Status, a.IsTokenRequired, a.AccessToken, a.PassPercentage, a.AllowAnswerReview,
            a.CreatedAt, a.AssessmentType, a.LinkedGroupId,
            UserFullName = a.User != null ? a.User.FullName : "Unknown",
            UserEmail = a.User != null ? a.User.Email : "",
            UserId = a.User != null ? a.User.Id : ""
        })
        .ToListAsync();

    var mgPrePostSessions = allSessions.Where(a => a.LinkedGroupId != null).ToList();
    var mgStandardSessions = allSessions.Where(a => a.LinkedGroupId == null).ToList();

    var prePostGrouped = mgPrePostSessions
        .GroupBy(a => a.LinkedGroupId)
        .Select(g => {
            var rep = g.Where(a => a.AssessmentType == "PreTest").OrderBy(a => a.CreatedAt).FirstOrDefault() ?? g.OrderBy(a => a.CreatedAt).First();
            string groupStatus;
            if (g.Any(a => a.Status == "Open" || a.Status == "InProgress")) groupStatus = "Open";
            else if (g.Any(a => a.Status == "Upcoming")) groupStatus = "Upcoming";
            else groupStatus = "Closed";
            return new {
                rep.Title, rep.Category, rep.Schedule, rep.ExamWindowCloseDate, rep.DurationMinutes,
                rep.Status, rep.IsTokenRequired, rep.AccessToken, rep.PassPercentage, rep.AllowAnswerReview,
                RepresentativeId = rep.Id,
                Users = g.Where(a => a.AssessmentType == "PreTest").Select(a => new { a.UserFullName, a.UserEmail, a.UserId }).ToList<dynamic>(),
                AllIds = g.Select(a => a.Id).ToList(),
                UserCount = g.Where(a => a.AssessmentType == "PreTest").Count(),
                GroupStatus = groupStatus,
                IsPrePostGroup = true,
                LinkedGroupId = g.Key
            };
        }).ToList<dynamic>();

    var standardGrouped = mgStandardSessions
        .GroupBy(a => (a.Title, a.Category, a.Schedule.Date))
        .Select(g => {
            var rep = g.OrderBy(a => a.CreatedAt).First();
            string groupStatus;
            if (g.Any(a => a.Status == "Open" || a.Status == "InProgress")) groupStatus = "Open";
            else if (g.Any(a => a.Status == "Upcoming")) groupStatus = "Upcoming";
            else groupStatus = "Closed";
            return new {
                rep.Title, rep.Category, rep.Schedule, rep.ExamWindowCloseDate, rep.DurationMinutes,
                rep.Status, rep.IsTokenRequired, rep.AccessToken, rep.PassPercentage, rep.AllowAnswerReview,
                RepresentativeId = rep.Id,
                Users = g.Select(a => new { a.UserFullName, a.UserEmail, a.UserId }).ToList<dynamic>(),
                AllIds = g.Select(a => a.Id).ToList(),
                UserCount = g.Count(),
                GroupStatus = groupStatus,
                IsPrePostGroup = false,
                LinkedGroupId = (int?)null
            };
        }).ToList<dynamic>();

    var grouped = prePostGrouped.Concat(standardGrouped)
        .OrderByDescending(g => g.Schedule)
        .ToList();

    if (string.IsNullOrEmpty(statusFilter))
        grouped = grouped.Where(g => g.GroupStatus != "Closed").ToList();
    else if (statusFilter == "Open" || statusFilter == "Upcoming" || statusFilter == "Closed")
        grouped = grouped.Where(g => g.GroupStatus == statusFilter).ToList();

    var paging = PaginationHelper.Calculate(grouped.Count, page, pageSize);

    ViewBag.ManagementData = grouped.Skip(paging.Skip).Take(paging.Take).ToList();
    ViewBag.CurrentPage = paging.CurrentPage;
    ViewBag.TotalPages = paging.TotalPages;
    ViewBag.TotalCount = paging.TotalCount;
    ViewBag.SearchTerm = search;
    ViewBag.SelectedCategory = category ?? "";
    ViewBag.SelectedStatus = statusFilter ?? "";

    // Categories juga dibutuhkan oleh partial view (filter dropdown render). Plan 03 wrap dengan GetOrCreateAsync.
    // Plan 02 add .AsNoTracking() (zero-cost change, read-only path — independent dari Plan 03 cache wrapping).
    ViewBag.Categories = await _context.AssessmentSessions
        .AsNoTracking()
        .Select(a => a.Category)
        .Distinct()
        .OrderBy(c => c)
        .ToListAsync();

    sw.Stop();
    _logger.LogInformation(
        "ManageAssessment perf [tab=assessment]: elapsed={Ms}ms search_present={SearchPresent} page={Page}",
        sw.ElapsedMilliseconds, !string.IsNullOrEmpty(search), page);

    return PartialView("Shared/_AssessmentGroupsTab", null);
}

[HttpGet]
[Authorize(Roles = "Admin, HC")]
[ResponseCache(NoStore = true, Location = ResponseCacheLocation.None)]
public async Task<IActionResult> ManageAssessmentTab_Training(string? search, int page = 1, int pageSize = 20,
    string? section = null, string? unit = null, string? category = null, string? statusFilter = null,
    string? isFiltered = null)
{
    var sw = System.Diagnostics.Stopwatch.StartNew();

    bool isInitialState = false;
    ViewBag.IsInitialState = isInitialState;
    ViewBag.SelectedSection = section;
    ViewBag.SelectedUnit = unit;
    ViewBag.SelectedCategory = category;
    ViewBag.SelectedStatus = statusFilter;
    ViewBag.SearchTerm = search;

    List<WorkerTrainingStatus> workers;
    if (isInitialState)
        workers = new List<WorkerTrainingStatus>();
    else
        workers = await _workerDataService.GetWorkersInSection(section, unit, category, search, statusFilter);

    ViewBag.Workers = workers;

    // T4 (Sections + Units) — required untuk dropdown filter render
    ViewBag.TrainingSections = await _context.GetAllSectionsAsync();
    ViewBag.TrainingUnits = !string.IsNullOrEmpty(section)
        ? await _context.GetUnitsForSectionAsync(section)
        : new List<string>();

    sw.Stop();
    _logger.LogInformation(
        "ManageAssessment perf [tab=training]: elapsed={Ms}ms search_present={SearchPresent} page={Page}",
        sw.ElapsedMilliseconds, !string.IsNullOrEmpty(search), page);

    return PartialView("Shared/_TrainingRecordsTab", null);
}

[HttpGet]
[Authorize(Roles = "Admin, HC")]
[ResponseCache(NoStore = true, Location = ResponseCacheLocation.None)]
public async Task<IActionResult> ManageAssessmentTab_History(string? search, int page = 1, int pageSize = 20,
    string? statusFilter = null)
{
    var sw = System.Diagnostics.Stopwatch.StartNew();

    var (assessmentHistory, trainingHistory) = await _workerDataService.GetAllWorkersHistory();

    ViewBag.AssessmentHistory = assessmentHistory;
    ViewBag.TrainingHistory = trainingHistory;
    ViewBag.AssessmentTitles = assessmentHistory
        .Select(r => r.Title)
        .Where(t => !string.IsNullOrEmpty(t))
        .Distinct().OrderBy(t => t).ToList();
    ViewBag.SearchTerm = search;

    sw.Stop();
    _logger.LogInformation(
        "ManageAssessment perf [tab=history]: elapsed={Ms}ms search_present={SearchPresent} page={Page}",
        sw.ElapsedMilliseconds, !string.IsNullOrEmpty(search), page);

    return PartialView("Shared/_HistoryTab", null);
}
```

**Step 3 — Verify build:**

```bash
dotnet build --nologo -v q
# Expected: Build succeeded, 0 errors, ≤ 92 warnings
```

**KRITIS notes:**
- `PartialView("Shared/_AssessmentGroupsTab", null)` — pakai existing `PartialView` override yang resolve ke `~/Views/Admin/Shared/_AssessmentGroupsTab.cshtml`
- Partial views existing tidak butuh modifikasi karena baca dari `ViewBag` (D-10 contract)
- Stopwatch instrumentasi lama (5 segments T1..T5 di shell action) DIPINDAH ke per-action — single Stopwatch per partial endpoint
- Action `ManageAssessment` shell TIDAK panggil `_workerDataService.*` lagi — semua data fetch terjadi di partial action saat HTMX trigger
  </action>
  <verify>
    <automated>dotnet build --nologo -v q 2>&1 | grep -E "(Build succeeded|Build FAILED)"</automated>
    Expected: "Build succeeded" + 0 errors. Warnings ≤ 92.
  </verify>
  <acceptance_criteria>
    **Konvensi:** Semua `grep`/`test` di acceptance harus exit 0 = success. Untuk negative assertion, pakai `!` negation.

    - `[ "$(grep -c 'public async Task<IActionResult> ManageAssessmentTab_' Controllers/AssessmentAdminController.cs)" -eq 3 ]` exit 0 (3 partial actions baru)
    - `[ "$(grep -c 'ResponseCache(NoStore = true' Controllers/AssessmentAdminController.cs)" -ge 3 ]` exit 0 (D-06 applied to 3 partial actions)
    - `[ "$(grep -c 'ManageAssessment perf [tab=' Controllers/AssessmentAdminController.cs)" -eq 4 ]` exit 0 (1 shell + 3 partial — D-09 format)
    - `grep -q 'PartialView("Shared/_AssessmentGroupsTab"' Controllers/AssessmentAdminController.cs` exit 0 (POSITIVE)
    - `grep -q 'PartialView("Shared/_TrainingRecordsTab"' Controllers/AssessmentAdminController.cs` exit 0 (POSITIVE)
    - `grep -q 'PartialView("Shared/_HistoryTab"' Controllers/AssessmentAdminController.cs` exit 0 (POSITIVE)
    - **C-1 fix:** `[ "$(grep -c 'AsNoTracking' Controllers/AssessmentAdminController.cs)" -ge 2 ]` exit 0 — minimal 2 occurrence di Plan 02 stage (shell Categories query + partial Categories query). Plan 03 akan tambah lagi di 3 partial main chains (target ≥ 5 post-Plan-03).
    - Action signature `ManageAssessment` unchanged: `! git diff HEAD Controllers/AssessmentAdminController.cs | grep -E '^-.*public async Task<IActionResult> ManageAssessment('` exit 0 (NEGATIVE — no removed signature line)
    - Shell action body sudah TIDAK panggil `_workerDataService`: `[ "$(awk '/public async Task<IActionResult> ManageAssessment(/,/^        }/' Controllers/AssessmentAdminController.cs | grep -c '_workerDataService')" -eq 0 ]` exit 0
    - `dotnet build --nologo -v q` exit 0, 0 errors, warnings ≤ 92
  </acceptance_criteria>
  <done>
    Action `ManageAssessment` ter-refactor jadi shell-only (no data fetch except Categories untuk filter dropdown). 3 partial actions baru (`ManageAssessmentTab_Assessment`, `_Training`, `_History`) ter-tambah dengan `[ResponseCache(NoStore=true)]` (D-06), Stopwatch per-action (D-09), `[Authorize(Roles="Admin, HC")]`, return PartialView ke partial view existing. Build hijau. ViewBag contract preserved sesuai partial view consumer (D-10).
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor Views/Admin/ManageAssessment.cshtml shell — HTMX attrs + skeleton 3 varian + filter form id + script tag + error template handler</name>
  <files>Views/Admin/ManageAssessment.cshtml</files>
  <read_first>
    - Views/Admin/ManageAssessment.cshtml (full file — current shell+inline partial render)
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml L1-90 (existing filter form L14-32 + dropdown L36-83 yang akan jadi shell-level)
    - .planning/phases/311-manageassessment-performance/311-UI-SPEC.md §1 Skeleton Placeholder L107-227 (3 varian skeleton verbatim)
    - .planning/phases/311-manageassessment-performance/311-UI-SPEC.md §2 Error Template L229-261 (error HTML + retry button verbatim)
    - .planning/phases/311-manageassessment-performance/311-UI-SPEC.md §4 Tab Transition + race condition L298-353 (hx-sync semantic)
    - .planning/phases/311-manageassessment-performance/311-UI-SPEC.md §5 Filter Form Interaction Feedback L356-377
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-01..D-06 (HTMX trigger semantics, vendoring, filter form, cache invalidation, no-store)
    - .planning/phases/311-manageassessment-performance/311-DESIGN.md §3.4 Data Flow Detail L161-186 (lifecycle reference)
    - https://htmx.org/docs/#triggers (HTMX 2.0 trigger syntax reference)
    - Views/Shared/_Layout.cshtml L54 (S-1 verify: confirms `function appUrl(path)` global helper definition — wrapper berbasis `basePath` PathBase)
    - wwwroot/js/site.js (S-1 confirm: BACKUP location to verify — kalau `appUrl` di-define di sini juga, dokumentasikan)
  </read_first>
  <action>
**Tujuan:** Replace inline `<partial name="...">` dengan HTMX-driven tab pane wrappers (skeleton + hx-* attrs). Filter form di-promote ke shell level (id=filter-form) sehingga shared antar 3 tab via `hx-include`. Tambah error template + retry handler. Vendor HTMX script tag dengan `defer`.

**REPLACE seluruh isi `Views/Admin/ManageAssessment.cshtml`** dengan:

```cshtml
@{
    ViewData["Title"] = "Manage Assessment & Training";
    ViewData["ContainerClass"] = "container-fluid";

    var activeTab = ViewBag.ActiveTab as string ?? "assessment";
    var searchTerm = ViewBag.SearchTerm as string;
    var assessmentCategories = ViewBag.Categories as List<string> ?? new List<string>();
    var selectedCategory = ViewBag.SelectedCategory as string ?? "";
    var selectedStatus = ViewBag.SelectedStatus as string ?? "";

    // Phase 311 Plan 02: HTMX endpoint URLs (PathBase auto-resolved via @Url.Action).
    var urlAssessment = Url.Action("ManageAssessmentTab_Assessment", "AssessmentAdmin");
    var urlTraining = Url.Action("ManageAssessmentTab_Training", "AssessmentAdmin");
    var urlHistory = Url.Action("ManageAssessmentTab_History", "AssessmentAdmin");
}

@Html.AntiForgeryToken()

<!-- Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Admin")">Kelola Data</a></li>
        <li class="breadcrumb-item active" aria-current="page">Manage Assessment & Training</li>
    </ol>
</nav>

<!-- Flash Messages -->
@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle me-2"></i>@TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle me-2"></i>@TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="fw-bold text-dark mb-0">
        <i class="bi bi-sliders me-2 text-primary"></i>Manage Assessment & Training
    </h2>
    <div class="d-flex gap-2" id="header-assessment-btns"
         style="@(activeTab != "assessment" ? "display:none" : "")">
        <a href="@Url.Action("CreateAssessment", "AssessmentAdmin")" class="btn btn-primary">
            <i class="bi bi-plus-circle me-1"></i> Buat Assessment
        </a>
        <a href="@Url.Action("AssessmentMonitoring", "AssessmentAdmin")" class="btn btn-outline-success">
            <i class="bi bi-binoculars me-1"></i> Monitoring
        </a>
        <a href="@Url.Action("AuditLog", "AssessmentAdmin")" class="btn btn-outline-secondary">
            <i class="bi bi-clock-history me-1"></i> Audit Log
        </a>
    </div>
</div>

<!-- Tab Navigation -->
<ul class="nav nav-tabs mb-0" id="manageAssessmentTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <button class="nav-link" id="tab-assessment" data-bs-toggle="tab" data-bs-target="#pane-assessment"
                type="button" role="tab" aria-controls="pane-assessment" aria-selected="false">
            <i class="bi bi-sliders me-1"></i> Assessment Groups
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button class="nav-link" id="tab-training" data-bs-toggle="tab" data-bs-target="#pane-training"
                type="button" role="tab" aria-controls="pane-training" aria-selected="false">
            <i class="bi bi-people me-1"></i> Input Records
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button class="nav-link" id="tab-history" data-bs-toggle="tab" data-bs-target="#pane-history"
                type="button" role="tab" aria-controls="pane-history" aria-selected="false">
            <i class="bi bi-clock-history me-1"></i> History
        </button>
    </li>
</ul>

<!-- ============================================================ -->
<!-- Phase 311 Plan 02: Shared filter form di shell level         -->
<!-- (HTMX hx-include="#filter-form" target tab pane aktif).      -->
<!-- ============================================================ -->
<form id="filter-form" method="get" action="@Url.Action("ManageAssessment", "AssessmentAdmin")"
      class="row g-2 align-items-end my-3"
      onsubmit="return false;">

    <input type="hidden" name="tab" value="@activeTab" id="filter-tab" />
    <input type="hidden" name="page" value="1" id="filter-page" />
    <input type="hidden" name="pageSize" value="20" />

    <!-- Search input (D-03 debounce 500ms input) -->
    <div class="col-md-4">
        <div class="input-group">
            <span class="input-group-text bg-white border-end-0">
                <i class="bi bi-search text-muted"></i>
            </span>
            <input type="text" class="form-control border-start-0 ps-0" name="search"
                   id="filter-search"
                   placeholder="Cari berdasarkan judul, kategori, nama, atau NIP..."
                   value="@searchTerm"
                   hx-get="@urlAssessment"
                   hx-trigger="input changed delay:500ms, keyup[key=='Enter']"
                   hx-target="#pane-@activeTab > div.htmx-tab-wrapper"
                   hx-swap="innerHTML"
                   hx-include="#filter-form"
                   hx-sync="this:replace" />
            <button class="btn btn-primary" type="button"
                    hx-get="@urlAssessment"
                    hx-target="#pane-@activeTab > div.htmx-tab-wrapper"
                    hx-swap="innerHTML"
                    hx-include="#filter-form"
                    hx-sync="closest form:replace">Cari</button>
            @if (!string.IsNullOrEmpty(searchTerm))
            {
                <button type="button" class="btn btn-outline-secondary" title="Hapus pencarian"
                        onclick="document.getElementById('filter-search').value=''; htmx.trigger('#filter-search','input')">
                    <i class="bi bi-x-lg"></i>
                </button>
            }
        </div>
    </div>

    <!-- Category dropdown (D-03 instant change) -->
    <div class="col-md-3">
        <select id="filter-category" name="category" class="form-select form-select-sm"
                hx-get="@urlAssessment"
                hx-trigger="change"
                hx-target="#pane-@activeTab > div.htmx-tab-wrapper"
                hx-swap="innerHTML"
                hx-include="#filter-form"
                hx-sync="this:replace">
            <option value="">Semua Kategori</option>
            @foreach (var cat in assessmentCategories)
            {
                if (selectedCategory == cat)
                { <option value="@cat" selected="selected">@cat</option> }
                else
                { <option value="@cat">@cat</option> }
            }
        </select>
    </div>

    <!-- Status dropdown (D-03 instant change) -->
    <div class="col-md-3">
        <select id="filter-status" name="statusFilter" class="form-select form-select-sm"
                hx-get="@urlAssessment"
                hx-trigger="change"
                hx-target="#pane-@activeTab > div.htmx-tab-wrapper"
                hx-swap="innerHTML"
                hx-include="#filter-form"
                hx-sync="this:replace">
            @if (string.IsNullOrEmpty(selectedStatus))
            { <option value="" selected="selected">Aktif (Open/Upcoming)</option> }
            else
            { <option value="">Aktif (Open/Upcoming)</option> }
            @if (selectedStatus == "Open")
            { <option value="Open" selected="selected">Open</option> }
            else
            { <option value="Open">Open</option> }
            @if (selectedStatus == "Upcoming")
            { <option value="Upcoming" selected="selected">Upcoming</option> }
            else
            { <option value="Upcoming">Upcoming</option> }
            @if (selectedStatus == "Closed")
            { <option value="Closed" selected="selected">Closed</option> }
            else
            { <option value="Closed">Closed</option> }
            @if (selectedStatus == "All")
            { <option value="All" selected="selected">Semua Status</option> }
            else
            { <option value="All">Semua Status</option> }
        </select>
    </div>

    <!-- Reset filter -->
    @if (!string.IsNullOrEmpty(selectedCategory) || !string.IsNullOrEmpty(selectedStatus) || !string.IsNullOrEmpty(searchTerm))
    {
        <div class="col-md-2">
            <a href="@Url.Action("ManageAssessment", "AssessmentAdmin", new { tab = activeTab })"
               class="btn btn-sm btn-outline-secondary">
                <i class="bi bi-x-lg me-1"></i>Reset Filter
            </a>
        </div>
    }
</form>

<!-- Tab content -->
<div class="tab-content pt-2" id="manageAssessmentTabsContent">

    <!-- ===== TAB 1: ASSESSMENT GROUPS (active tab — hx-trigger=load) ===== -->
    <div class="tab-pane fade @(activeTab == "assessment" ? "show active" : "")"
         id="pane-assessment" role="tabpanel" aria-labelledby="tab-assessment">
        <div class="htmx-tab-wrapper"
             hx-get="@urlAssessment"
             hx-trigger="@(activeTab == "assessment" ? "load" : "shown.bs.tab from:#tab-assessment once")"
             hx-target="this"
             hx-swap="innerHTML"
             hx-include="#filter-form"
             hx-sync="this:replace"
             data-tab-name="assessment">
            <!-- Skeleton Assessment (UI-SPEC §1a) -->
            <div aria-busy="true" aria-label="Memuat data assessment" class="placeholder-glow">
                <div class="mb-3"><span class="placeholder col-2" style="height:24px"></span></div>
                <div class="card shadow-sm border-0">
                    <div class="card-body">
                        @for (int i = 0; i < 5; i++)
                        {
                            <div class="d-flex gap-2 mb-2 align-items-center" style="height:48px">
                                <span class="placeholder col-1"></span>
                                <span class="placeholder col-3"></span>
                                <span class="placeholder col-2"></span>
                                <span class="placeholder col-2"></span>
                                <span class="placeholder col-1"></span>
                                <span class="placeholder col-1"></span>
                                <span class="placeholder col-2"></span>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- ===== TAB 2: TRAINING RECORDS ===== -->
    <div class="tab-pane fade @(activeTab == "training" ? "show active" : "")"
         id="pane-training" role="tabpanel" aria-labelledby="tab-training">
        <div class="htmx-tab-wrapper"
             hx-get="@urlTraining"
             hx-trigger="@(activeTab == "training" ? "load" : "shown.bs.tab from:#tab-training once")"
             hx-target="this"
             hx-swap="innerHTML"
             hx-include="#filter-form"
             hx-sync="this:replace"
             data-tab-name="training">
            <!-- Skeleton Training (UI-SPEC §1b) -->
            <div aria-busy="true" aria-label="Memuat data training" class="placeholder-glow">
                <div class="d-flex justify-content-between mb-4">
                    <span class="placeholder col-2" style="height:24px"></span>
                    <div class="d-flex gap-2">
                        <span class="placeholder" style="width:140px;height:38px"></span>
                        <span class="placeholder" style="width:140px;height:38px"></span>
                    </div>
                </div>
                <div class="card bg-light border-0 mb-4">
                    <div class="card-body">
                        <div class="row g-3">
                            @for (int i = 0; i < 4; i++)
                            {
                                <div class="col-md-2"><span class="placeholder col-12" style="height:31px"></span></div>
                            }
                        </div>
                    </div>
                </div>
                @for (int i = 0; i < 5; i++)
                {
                    <div class="d-flex gap-2 mb-2" style="height:48px">
                        <span class="placeholder col-3"></span>
                        <span class="placeholder col-2"></span>
                        <span class="placeholder col-2"></span>
                        <span class="placeholder col-2"></span>
                        <span class="placeholder col-1"></span>
                    </div>
                }
            </div>
        </div>
    </div>

    <!-- ===== TAB 3: HISTORY ===== -->
    <div class="tab-pane fade @(activeTab == "history" ? "show active" : "")"
         id="pane-history" role="tabpanel" aria-labelledby="tab-history">
        <div class="htmx-tab-wrapper"
             hx-get="@urlHistory"
             hx-trigger="@(activeTab == "history" ? "load" : "shown.bs.tab from:#tab-history once")"
             hx-target="this"
             hx-swap="innerHTML"
             hx-include="#filter-form"
             hx-sync="this:replace"
             data-tab-name="history">
            <!-- Skeleton History (UI-SPEC §1c) -->
            <div aria-busy="true" aria-label="Memuat riwayat" class="placeholder-glow">
                <div class="d-flex gap-3 mb-3 border-bottom pb-2">
                    <span class="placeholder col-2" style="height:32px"></span>
                    <span class="placeholder col-2" style="height:32px"></span>
                </div>
                <div class="row g-2 mb-3">
                    <div class="col-md-4"><span class="placeholder col-12" style="height:31px"></span></div>
                    <div class="col-md-4"><span class="placeholder col-12" style="height:31px"></span></div>
                </div>
                @for (int i = 0; i < 4; i++)
                {
                    <div class="d-flex gap-2 mb-2" style="height:40px">
                        <span class="placeholder col-3"></span>
                        <span class="placeholder col-3"></span>
                        <span class="placeholder col-2"></span>
                        <span class="placeholder col-2"></span>
                    </div>
                }
            </div>
        </div>
    </div>

</div>

<!-- Phase 311 Plan 02: HTMX 2.0.x vendored locally (D-02) -->
<script src="~/lib/htmx/htmx.min.js" defer></script>

@section Scripts {
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Activate the correct tab based on ViewBag.ActiveTab
    var activeTab = '@activeTab';
    var tabId = activeTab === 'training' ? 'tab-training'
              : activeTab === 'history' ? 'tab-history'
              : 'tab-assessment';
    var tabEl = document.getElementById(tabId);
    if (tabEl) {
        new bootstrap.Tab(tabEl).show();
    }

    // Show/hide header action buttons based on active tab
    var headerAssessmentBtns = document.getElementById('header-assessment-btns');
    var manageAssessmentTabs = document.getElementById('manageAssessmentTabs');

    if (manageAssessmentTabs && headerAssessmentBtns) {
        manageAssessmentTabs.querySelectorAll('button[data-bs-toggle="tab"]').forEach(function(btn) {
            btn.addEventListener('shown.bs.tab', function(e) {
                var target = e.target.getAttribute('data-bs-target');
                if (target === '#pane-assessment') {
                    headerAssessmentBtns.style.display = '';
                } else {
                    headerAssessmentBtns.style.display = 'none';
                }
                // Phase 311 Plan 02: update hidden tab name di filter form supaya
                // filter trigger fetch tab yang aktif sekarang.
                var newTab = target.replace('#pane-','');
                var hiddenTab = document.getElementById('filter-tab');
                if (hiddenTab) hiddenTab.value = newTab;

                // Update hx-get filter input + dropdowns ke endpoint tab aktif (D-05 mendukung
                // filter affects active tab, cross-tab invalidation di-handle di bawah).
                var endpointMap = {
                    'assessment': '@urlAssessment',
                    'training': '@urlTraining',
                    'history': '@urlHistory'
                };
                var endpoint = endpointMap[newTab];
                if (endpoint) {
                    document.querySelectorAll('#filter-form [hx-get]').forEach(function(el){
                        el.setAttribute('hx-get', endpoint);
                        el.setAttribute('hx-target', '#pane-' + newTab + ' > div.htmx-tab-wrapper');
                    });
                    // Re-process attributes karena htmx tidak observe atribut runtime change
                    if (window.htmx) htmx.process(document.getElementById('filter-form'));
                }
            });
        });
    }
});

// Phase 311 Plan 02: Cross-tab cache invalidation (D-05).
// Saat filter form fetch sukses pada tab aktif, reset tab non-aktif "loaded" flag
// supaya next activation lazy-fetch ulang dengan filter terbaru.
document.body.addEventListener('htmx:afterSwap', function(evt) {
    var target = evt.detail.target;
    if (!target || !target.classList || !target.classList.contains('htmx-tab-wrapper')) return;

    // Saat afterSwap di tab aktif (hasil filter fetch), reset wrapper tab lain
    // ke skeleton state + restore hx-trigger 'shown.bs.tab once' supaya next click fetch ulang.
    var activeTabName = target.getAttribute('data-tab-name');
    document.querySelectorAll('.htmx-tab-wrapper').forEach(function(wrapper){
        var tabName = wrapper.getAttribute('data-tab-name');
        if (tabName === activeTabName) return;
        // Reset wrapper konten ke skeleton placeholder (re-render via clone dari template kalau ada,
        // atau strip innerHTML supaya next 'shown.bs.tab' fetch fire). Trigger 'load' lagi acceptable
        // karena tab non-aktif tidak visible sampai user click.
        wrapper.innerHTML = '<div aria-busy="true" aria-label="Memuat data" class="placeholder-glow"><div class="row g-2 mb-2"><span class="placeholder col-12" style="height:48px"></span></div><div class="row g-2 mb-2"><span class="placeholder col-12" style="height:48px"></span></div></div>';
        // Reset 'once' flag dengan reprocess
        wrapper.setAttribute('hx-trigger', 'shown.bs.tab from:#tab-' + tabName + ' once');
        if (window.htmx) htmx.process(wrapper);
    });
});

// Phase 311 Plan 02: Error template (UI-SPEC §2).
// Saat partial endpoint return 4xx/5xx atau network fail, swap target dengan error template + retry button.
function renderHtmxError(targetEl) {
    targetEl.innerHTML = '<div role="alert" class="alert alert-danger d-flex align-items-start gap-2 my-3">' +
        '<i class="bi bi-exclamation-triangle-fill flex-shrink-0" style="font-size:1.25rem"></i>' +
        '<div class="flex-grow-1">' +
        '<div class="fw-semibold mb-1">Gagal memuat data</div>' +
        '<div class="small mb-2">Periksa koneksi jaringan Anda lalu coba lagi. Jika masalah berlanjut, hubungi administrator.</div>' +
        '<button type="button" class="btn btn-sm btn-outline-danger" data-htmx-retry>' +
        '<i class="bi bi-arrow-clockwise me-1"></i>Coba Lagi' +
        '</button>' +
        '</div>' +
        '</div>';
}
document.body.addEventListener('htmx:responseError', function(evt) {
    var target = evt.detail.target || evt.target;
    if (target && target.classList && target.classList.contains('htmx-tab-wrapper')) {
        renderHtmxError(target);
    }
});
document.body.addEventListener('htmx:sendError', function(evt) {
    var target = evt.detail.target || evt.target;
    if (target && target.classList && target.classList.contains('htmx-tab-wrapper')) {
        renderHtmxError(target);
    }
});
// Retry button delegation
document.body.addEventListener('click', function(evt) {
    var btn = evt.target.closest('[data-htmx-retry]');
    if (!btn) return;
    var wrapper = btn.closest('.htmx-tab-wrapper');
    if (wrapper && window.htmx) {
        htmx.trigger(wrapper, 'load');
    }
});

// Regenerate Token handler — preserved dari versi lama
document.body.addEventListener('click', function(evt) {
    var btn = evt.target.closest('.btn-regenerate-token');
    if (!btn) return;
    (async function() {
        var id = btn.dataset.id;
        var token = document.querySelector('input[name="__RequestVerificationToken"]')?.value
            || document.querySelector('[name="__RequestVerificationToken"]')?.value;
        try {
            var res = await fetch(appUrl('/Admin/RegenerateToken/' + id), {
                method: 'POST',
                headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                body: '__RequestVerificationToken=' + encodeURIComponent(token)
            });
            var data = await res.json();
            if (data.success) {
                alert('Token baru: ' + data.token);
            } else {
                alert('Error: ' + data.message);
            }
        } catch (e) {
            alert('Gagal regenerate token.');
        }
    })();
});

// History tab: Riwayat Assessment client-side filter (preserved dari versi lama)
function filterAssessmentRows() {
    var workerFilter = document.getElementById('assessmentWorkerFilter')?.value.toLowerCase() || '';
    var titleFilter = document.getElementById('assessmentTitleFilter')?.value.toLowerCase() || '';
    var rows = document.querySelectorAll('#assessmentHistoryTable .assessment-history-row');
    rows.forEach(function(row) {
        var worker = row.getAttribute('data-worker') || '';
        var title = row.getAttribute('data-title') || '';
        var workerMatch = !workerFilter || worker.includes(workerFilter);
        var titleMatch = !titleFilter || title.toLowerCase().includes(titleFilter);
        row.style.display = (workerMatch && titleMatch) ? '' : 'none';
    });
}
</script>
}
```

**Catatan implementasi penting:**
1. **Filter form di-promote ke shell level** — partial views existing (`_AssessmentGroupsTab.cshtml`) masih punya filter form internal mereka; partial views TIDAK dimodifikasi di Plan 02 (D-10 backward compat). Filter form duplicate di partial Assessment akan tetap render saat partial swap, tapi shell-level form yang dipakai HTMX trigger. Kalau muncul redundansi visual, future cleanup phase boleh consolidate. Plan 02 prioritas: tidak break partial view contract.
2. **`hx-target="#pane-{activeTab} > div.htmx-tab-wrapper"`** — selektor wrapper di dalam pane, bukan pane langsung, supaya skeleton dapat di-replace clean.
3. **`hx-trigger` active tab pakai `"load"`** (Claude's Discretion locked di UI-SPEC).
4. **`hx-trigger` non-active pakai `"shown.bs.tab from:#tab-{name} once"`** (D-01 — explicit ID match, bukan `closest button.nav-link` karena DOM structure tab button di sibling tree).
5. **`hx-sync="this:replace"`** (D-Discretion locked) — race condition handling.
6. **`hx-include="#filter-form"`** — semua filter values dikirim setiap fetch.
7. **Cross-tab invalidation (D-05)** — `htmx:afterSwap` event listener reset wrapper tab non-aktif ke skeleton + restore `shown.bs.tab once` trigger. Acceptable behavior: 1 round trip extra saat user pindah tab post-filter.
8. **Error template (UI-SPEC §2)** — `htmx:responseError` + `htmx:sendError` handler render alert-danger dengan retry button. Retry button delegation re-trigger `load`.
9. **`<script src="~/lib/htmx/htmx.min.js" defer>`** — tag bukan di `@section Scripts` supaya HTMX init sebelum DOMContentLoaded handlers di scripts section.
10. **Bootstrap fade transition di tab pane** — preserved (`fade show active` classes).
  </action>
  <verify>
    <automated>grep -q 'hx-get="@urlAssessment"' Views/Admin/ManageAssessment.cshtml && grep -q 'hx-trigger="@(activeTab' Views/Admin/ManageAssessment.cshtml && grep -q 'src="~/lib/htmx/htmx.min.js"' Views/Admin/ManageAssessment.cshtml && grep -q 'id="filter-form"' Views/Admin/ManageAssessment.cshtml && echo "OK"</automated>
    Expected: "OK" — semua HTMX attrs + filter form id + script tag present.
  </verify>
  <acceptance_criteria>
    - `grep -c "hx-get=\"@url" Views/Admin/ManageAssessment.cshtml` ≥ 6 (3 tab wrappers + 3 filter inputs)
    - `grep -q "id=\"filter-form\"" Views/Admin/ManageAssessment.cshtml` returns 0
    - `grep -q "shown.bs.tab from:#tab-" Views/Admin/ManageAssessment.cshtml` returns 0 (D-01 trigger)
    - `grep -q "hx-trigger=\"input changed delay:500ms" Views/Admin/ManageAssessment.cshtml` returns 0 (D-03 debounce)
    - `grep -q "hx-include=\"#filter-form\"" Views/Admin/ManageAssessment.cshtml` returns 0
    - `grep -q "hx-sync=\"this:replace\"" Views/Admin/ManageAssessment.cshtml` returns 0 (race condition handling)
    - `grep -c "placeholder-glow" Views/Admin/ManageAssessment.cshtml` ≥ 3 (3 skeleton variants)
    - `grep -q "aria-label=\"Memuat data assessment\"" Views/Admin/ManageAssessment.cshtml` returns 0 (UI-SPEC copywriting)
    - `grep -q "aria-label=\"Memuat data training\"" Views/Admin/ManageAssessment.cshtml` returns 0
    - `grep -q "aria-label=\"Memuat riwayat\"" Views/Admin/ManageAssessment.cshtml` returns 0
    - `grep -q "Gagal memuat data" Views/Admin/ManageAssessment.cshtml` returns 0 (error template copy)
    - `grep -q "Coba Lagi" Views/Admin/ManageAssessment.cshtml` returns 0 (retry button copy)
    - `grep -q "src=\"~/lib/htmx/htmx.min.js\" defer" Views/Admin/ManageAssessment.cshtml` returns 0
    - `grep -q "htmx:afterSwap" Views/Admin/ManageAssessment.cshtml` returns 0 (cross-tab invalidation)
    - `grep -q "htmx:responseError" Views/Admin/ManageAssessment.cshtml` returns 0 (error handler)
    - `dotnet build --nologo -v q` exit 0, 0 errors, warnings ≤ 92
    - **S-1 fix:** `appUrl()` helper reachable post-refactor: `grep -rE "function appUrl|window.appUrl|const appUrl|let appUrl|var appUrl" Views/Shared/ wwwroot/js/ | grep -q "."` exit 0. Verified: `Views/Shared/_Layout.cshtml:54` defines `function appUrl(path) { return basePath + (path.startsWith('/') ? path : '/' + path); }` — global scope, available di setiap view yang inherit `_Layout.cshtml`. Catatan: kalau hasil grep null (helper hilang akibat upstream change), executor MUST flag dan re-include `appUrl` definition inline di `<script>` block shell view.
  </acceptance_criteria>
  <done>
    Shell view `ManageAssessment.cshtml` ter-refactor: tab pane wrappers pakai HTMX `hx-get` + skeleton 3 varian + `hx-trigger` per D-01 (active load, inactive shown.bs.tab once); filter form id=filter-form di shell level dengan search debounce 500ms (D-03), dropdown instant change, `hx-sync="this:replace"`, `hx-include="#filter-form"`. Script tag HTMX vendored dengan defer. Cross-tab invalidation handler (D-05) + error template handler + retry button delegation. Build hijau, partial views existing tidak dimodifikasi (D-10).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT smoke test parity 4-step Bahasa Indonesia + perf measurement wifi kantor</name>
  <files>(human checkpoint — no files modified by Claude; user verifies browser-side smoke parity + perf metrics)</files>
  <action>Surface the verification steps in <how-to-verify> below to the user; pause execution until user replies with resume-signal. Claude does not execute browser actions — user runs the 5-step manual UAT in their environment.</action>
  <verify><automated>N/A — checkpoint:human-verify (no automated gate per GSD convention; resume blocked sampai user reply "uat passed" / "uat failed")</automated></verify>
  <done>User replies "uat passed" + perf metrics paste OR "uat failed" + gap explanation. Plan 02 commit (Task 5) only proceeds on "uat passed".</done>
  <what-built>
    Plan 02 selesai: REQUIREMENTS.md updated (Task 1), HTMX vendored + CSS (Task 1), `ManageAssessment` refactored ke shell + 3 partial actions (Task 2), shell view ManageAssessment.cshtml refactored ke HTMX-driven (Task 3). Build hijau ≤92 warnings, 0 errors. Sekarang user verify smoke parity per tab + measure perf di wifi kantor.
  </what-built>
  <how-to-verify>
**Pre-requisite:** Server `10.55.3.3` ter-deploy commit terbaru (koordinasi IT — lihat DESIGN.md §6.1). Atau test lokal dulu dengan `dotnet run` lalu test di wifi kantor setelah deploy.

**Step 1 — Smoke parity Tab Assessment (~3 menit):**
1. Buka browser di wifi kantor, navigate ke `https://10.55.3.3/KPB-PortalHC/Admin/ManageAssessment` (atau localhost equivalent untuk pre-deploy test)
2. Login admin (`admin@pertamina.com / 123456` jika UseActiveDirectory=false)
3. Tab Assessment otomatis aktif. Verifikasi:
   - Skeleton placeholder visible instant (~< 1 detik shell document received)
   - Setelah HTMX init + GET partial, skeleton di-swap dengan tabel real
   - Kolom tabel: Title, Category, Schedule, Duration, Status, Token, Pass %, Actions (atau seperti pre-refactor — bandingkan dengan baseline dari git history `git show 381b36cd:Views/Admin/Shared/_AssessmentGroupsTab.cshtml` jika ragu)
   - Row count match dataset Dev (cek pre-refactor screenshot kalau ada)
   - Pagination footer benar (CurrentPage/TotalPages)
4. Test filter:
   - Ketik "test" di search box → debounce 500ms → tabel re-fetch dengan filter
   - Pilih Category dropdown → instant fetch
   - Pilih Status "Open" → instant fetch
   - Klik Reset Filter → pane re-fetch tanpa filter
5. Test pagination:
   - Klik halaman 2 di pagination links → pane swap dengan halaman 2 (TIDAK full reload)

**Step 2 — Smoke parity Tab Training (~3 menit):**
1. Klik tab "Input Records"
2. Verifikasi:
   - Skeleton training visible immediately (sebelum data load)
   - Setelah HTMX shown.bs.tab fire + fetch, skeleton di-swap dengan tabel + filter row
   - Header buttons "Tambah Training", "Tambah Assessment Manual", "Import Excel" present
   - Filter dropdown Bagian, Kategori Training, Unit, Status render
   - Worker rows tampil (kalau ada data)
3. Klik tab Training lagi → instant (NO re-fetch karena `once` modifier)
4. Filter form (search/category/status) re-fetch tab aktif (Training sekarang)

**Step 3 — Smoke parity Tab History (~3 menit):**
1. Klik tab "History"
2. Verifikasi:
   - Skeleton history visible
   - Setelah fetch, sub-tabs "Riwayat Assessment" + "Riwayat Training" render
   - Tabel Riwayat Assessment kolom: Pekerja, NIP, Title, Score, Status, Date
   - Sub-tab "Riwayat Training" switchable, render kolom Pekerja, NIP, Title, Date

**Step 4 — Performance measurement wifi kantor (~10 menit):**
1. Open Chrome DevTools → Network tab → check "Disable cache"
2. Hard reload (Ctrl+F5) `https://10.55.3.3/KPB-PortalHC/Admin/ManageAssessment`
3. Capture metrics di Network tab:
   - Shell document: TTFB, content download, total time, **size dalam KB**
   - Active tab partial (`ManageAssessmentTab_Assessment`): TTFB, content download, total time, size
   - Total page finish time
4. Run 3x cold (close+reopen tab antar run). Skip Run 1 (warmup), median Run 2-3.
5. **Acceptance per CONTEXT:**
   - [ ] Initial response document <14 KB
   - [ ] End-to-end load wifi kantor ≤40 detik (vs baseline ~1.4 menit)
   - [ ] Tab switching post-initial ≤2 detik
   - [ ] TTFB ≤500ms
6. Tail log (Serilog Console output dari `dotnet run`) untuk verify Stopwatch entries:
   ```bash
   grep "ManageAssessment perf \[tab=" baseline-311.log | tail -10
   ```
   Expected entries (4 entries per page load — 1 shell + 3 partial saat user klik tab semua):
   ```
   ManageAssessment perf [tab=shell]: elapsed=...ms search_present=False page=1
   ManageAssessment perf [tab=assessment]: elapsed=...ms search_present=False page=1
   ManageAssessment perf [tab=training]: elapsed=...ms search_present=False page=1
   ManageAssessment perf [tab=history]: elapsed=...ms search_present=False page=1
   ```

**Step 5 — Race condition + error handling (~3 menit):**
1. Cepat klik antar tab Assessment → Training → History → Assessment dalam 1 detik
2. Verifikasi: tidak ada flicker glitch, hx-sync auto-cancel previous request, pane terakhir yang diklik yang tampil real content
3. Simulate error: stop dotnet process di tengah fetch (atau force network offline di DevTools), klik tab → expect error template "Gagal memuat data" + tombol "Coba Lagi"
4. Klik "Coba Lagi" → re-trigger fetch (kalau service back up, swap dengan content)

**Resume signal ke Claude:**
- Kalau semua 5 step PASS → reply "uat passed" + paste perf metrics (shell size KB, total wifi kantor seconds, median Stopwatch elapsed per tab)
- Kalau ada gap → reply "uat failed" + jelaskan tab mana, kolom hilang/extra, atau perf gap mana yang miss

**Expected outcome dokumentasi:** Hasil paste ke `311-02-SUMMARY.md` di Task 5 (commit task) supaya audit trail.
  </how-to-verify>
  <resume-signal>Reply "uat passed" + perf metrics paste ATAU "uat failed" + gap explanation</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit Plan 02 changes setelah UAT passed</name>
  <files>
    .planning/REQUIREMENTS.md
    wwwroot/lib/htmx/htmx.min.js
    wwwroot/css/site.css
    Controllers/AssessmentAdminController.cs
    Views/Admin/ManageAssessment.cshtml
  </files>
  <read_first>
    - User reply dari Task 4 (verify "uat passed" + metrics paste)
  </read_first>
  <action>
**Hanya execute kalau Task 4 = "uat passed".** Kalau "uat failed", SKIP — return ke Task 2 atau Task 3 untuk fix gap.

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "feat(311-02): HTMX lazy load refactor — shell + 3 partial actions + skeleton (D-01..D-10, PERF-01)" \
  --files .planning/REQUIREMENTS.md \
          wwwroot/lib/htmx/htmx.min.js \
          wwwroot/css/site.css \
          Controllers/AssessmentAdminController.cs \
          Views/Admin/ManageAssessment.cshtml
```

Verify commit landed:
```bash
git log -1 --oneline
git show --stat HEAD | grep -E "(REQUIREMENTS|htmx.min.js|site.css|AssessmentAdminController|ManageAssessment.cshtml)"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | grep -q "311-02" && echo "OK"</automated>
    Expected: "OK" — commit dengan tag 311-02 landed.
  </verify>
  <acceptance_criteria>
    - `git log -1` menunjukkan commit dengan 5 files modified (REQUIREMENTS, htmx.min.js, site.css, controller, shell view)
    - Commit message mention `311-02` + `HTMX lazy load`
    - `git status` clean (no uncommitted changes)
  </acceptance_criteria>
  <done>
    Plan 02 (HTMX lazy load) ter-commit. UAT passed dengan smoke parity 3 tab + perf metrics dokumentasi. Ready handoff ke Plan 03 (backend opportunistic).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Admin → ASP.NET Core route | Authenticated request enters `ManageAssessment` shell + 3 partial endpoints; existing `[Authorize(Roles = "Admin, HC")]` gate aktif di setiap action |
| Browser ← HTMX JS → 3 partial endpoints | Client-side HTMX library 2.0.x (vendored lokal `wwwroot/lib/htmx/htmx.min.js`) mengirim XHR/fetch dengan filter params dari form `#filter-form`; response berisi HTML partial yang di-swap ke DOM |
| HTMX vendor file → filesystem | Vendored library file tetap di repo, served sebagai static asset oleh ASP.NET Core; integrity verified via SHA hash di commit message |
| Action body → ILogger sink | Structured log fields ditulis ke Serilog/Console; log fields hanya berisi tab name (string enum), elapsed ms (int), search_present (bool), page (int) |
| Action body → DB (read) | EF Core query path identical dengan logic pre-refactor (just dipindah ke 3 partial actions); no new injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-311-02-01 | Tampering / Spoofing | HTMX vendor file `wwwroot/lib/htmx/htmx.min.js` integrity | mitigate | Vendor lokal (NOT CDN) supaya proxy/MITM tidak bisa swap library. SHA-256 hash dicatat di commit message Task 5 (`sha256sum wwwroot/lib/htmx/htmx.min.js`). Cross-check terhadap upstream npm registry checksum saat Task 1 download. Static asset diserve via ASP.NET Core static middleware (existing). Future re-vendor wajib re-verify hash. |
| T-311-02-02 | Spoofing / Authorization Bypass | 3 NEW partial endpoints (`ManageAssessmentTab_*`) | mitigate | Setiap partial action di-decorate `[Authorize(Roles = "Admin, HC")]` — verified via grep di acceptance_criteria Task 2. Anonymous request return 401, role bukan Admin/HC return 403. URL pattern di-resolve via PathBase `/KPB-PortalHC` route. |
| T-311-02-03 | XSS via partial response output | Partial views render `_AssessmentGroupsTab.cshtml`, `_TrainingRecordsTab.cshtml`, `_HistoryTab.cshtml` dengan ViewBag data dari DB | accept | Razor encoding default (`@field`) auto HTML-encode. Existing partial views sudah pakai `@model` properties dengan encoding default. No raw HTML output (`@Html.Raw`) di filter form atau skeleton. HTMX swap pakai `innerHTML` yang receive HTML server-side, JS tidak inject string user input. Inherited safety dari Razor model. |
| T-311-02-04 | Cross-Site Request Forgery (CSRF) | 3 partial endpoints (read-only GET) | accept | Endpoint adalah `[HttpGet]` read-only — tidak modify state. CSRF threat applicable hanya untuk state-changing actions (POST/PUT/DELETE). `[ValidateAntiForgeryToken]` tidak dibutuhkan per ASP.NET Core best practice. Existing AntiForgeryToken di shell `@Html.AntiForgeryToken()` preserved untuk POST endpoints lain (RegenerateToken handler). |
| T-311-02-05 | Information Disclosure via cache | `[ResponseCache(NoStore = true)]` di partial endpoints | mitigate | D-06 explicit: `NoStore = true, Location = ResponseCacheLocation.None` cegah browser/proxy/CDN cache response yang mungkin berisi data filter-spesifik. HTMX manage tab cache JS-side (DOM retention sampai user filter atau pindah tab). Verified via grep di Task 2 acceptance. |
| T-311-02-06 | Denial of Service | Race condition tab switching cepat (multiple in-flight fetch) | mitigate | `hx-sync="this:replace"` di setiap tab wrapper dan filter form input — auto-cancel previous in-flight request saat trigger fire. Server tidak terbebani >1 concurrent request per tab per user. Verified visual di Task 4 Step 5. |
| T-311-02-07 | Denial of Service | Stopwatch overhead di hot-path 3 partial actions | accept | Stopwatch BCL primitive, microsecond-level overhead per Start/Stop. 1 instance × 2 calls per partial action = 2 ops, total <1ms overhead per request — negligible vs baseline >10ms. D-09 explicit permanent untuk ongoing perf monitoring. |
| T-311-02-08 | Information Disclosure | `_logger.LogInformation` "ManageAssessment perf [tab=...]" structured fields | accept | Log fields hanya: `Tab` (string enum: shell/assessment/training/history), `Ms` (int elapsed), `SearchPresent` (bool flag), `Page` (int). NO user identifier, NO query content, NO PII. Acceptable per existing logging convention (PATTERNS.md Cross-cutting Pattern 1). |

**No HIGH/CRITICAL threats di Plan 02.** Refactor preserves auth + encoding semantics; new attack surface (3 GET endpoints) di-mitigasi dengan Authorize + ResponseCache + read-only nature.
</threat_model>

<verification>
**Phase-level checks:**

1. **REQUIREMENTS update (after Task 1):**
   ```bash
   grep -q "HTMX lazy load architecture" .planning/REQUIREMENTS.md
   grep -q "≤40 detik" .planning/REQUIREMENTS.md
   ```

2. **HTMX vendored (after Task 1):**
   ```bash
   test -f wwwroot/lib/htmx/htmx.min.js && wc -c < wwwroot/lib/htmx/htmx.min.js
   # Expected: 8000-25000 bytes
   ```

3. **CSS rules (after Task 1):**
   ```bash
   grep -c ".htmx-request" wwwroot/css/site.css
   # Expected: ≥ 3
   ```

4. **Controller refactor (after Task 2):**
   ```bash
   grep -c "public async Task<IActionResult> ManageAssessmentTab_" Controllers/AssessmentAdminController.cs
   # Expected: 3
   grep -c "ResponseCache(NoStore = true" Controllers/AssessmentAdminController.cs
   # Expected: ≥ 3
   grep -c "ManageAssessment perf \[tab=" Controllers/AssessmentAdminController.cs
   # Expected: 4 (shell + 3 partial)
   ```

5. **Shell view refactor (after Task 3):**
   ```bash
   grep -q "hx-trigger=\"input changed delay:500ms" Views/Admin/ManageAssessment.cshtml
   grep -q "hx-include=\"#filter-form\"" Views/Admin/ManageAssessment.cshtml
   grep -q "src=\"~/lib/htmx/htmx.min.js\" defer" Views/Admin/ManageAssessment.cshtml
   grep -c "placeholder-glow" Views/Admin/ManageAssessment.cshtml
   # Expected: ≥ 3
   ```

6. **Build gate (after each task):**
   ```bash
   dotnet build --nologo -v q
   # Expected: 0 errors, ≤ 92 warnings
   ```

7. **Manual UAT (Task 4):**
   - 3 tab smoke parity passed (kolom, row count, ordering identik)
   - Perf metrics: shell <14 KB, total wifi kantor ≤40 detik, tab switch ≤2 detik, TTFB ≤500ms
   - Race condition + error template + retry button verified

8. **Commit (Task 5):**
   ```bash
   git log -1 --pretty=format:"%s" | grep "311-02"
   ```
</verification>

<success_criteria>
**Plan 02 complete when:**

- [ ] REQUIREMENTS.md §PERF-01 ter-update dengan strategy "HTMX lazy load architecture" + acceptance "≤40 detik wifi kantor" + "<14 KB initial doc" + "≤2s tab switch" (D-08)
- [ ] HTMX 2.0.x ter-vendored di `wwwroot/lib/htmx/htmx.min.js` (~14 KB) dengan SHA hash dicatat (D-02)
- [ ] CSS `.htmx-request` rules appended ke `wwwroot/css/site.css` per UI-SPEC §3
- [ ] Action `ManageAssessment` ter-refactor ke shell-only — no `_workerDataService` calls, no `ManagementData` query (Categories tetap untuk dropdown filter)
- [ ] 3 partial actions baru: `ManageAssessmentTab_Assessment`, `_Training`, `_History` — masing-masing `[Authorize(Roles="Admin, HC")]` + `[ResponseCache(NoStore=true)]` (D-06) + return `PartialView("Shared/_...")`
- [ ] Stopwatch instrumentation dipindah ke per-action context (D-09) — 4 log entries (1 shell + 3 partial) format "ManageAssessment perf [tab={Tab}]: elapsed={Ms}ms search_present={SearchPresent} page={Page}"
- [ ] ViewBag contract preserved per partial view consumer (D-10)
- [ ] Shell view `ManageAssessment.cshtml` punya: filter form id=filter-form di shell level, 3 tab pane wrappers `.htmx-tab-wrapper` dengan hx-* attrs, 3 skeleton variants per UI-SPEC §1, hx-trigger="load" untuk active tab + "shown.bs.tab from:#tab-... once" untuk inactive (D-01), hx-trigger="input changed delay:500ms" search + "change" dropdown (D-03), hx-sync="this:replace", hx-include="#filter-form", error template handler (UI-SPEC §2), retry button delegation, cross-tab cache invalidation handler (D-05)
- [ ] Script tag `<script src="~/lib/htmx/htmx.min.js" defer>` di shell view
- [ ] `dotnet build --nologo -v q` returns 0 errors, ≤ 92 warnings
- [ ] Manual UAT 4-step BI passed: tab smoke parity per 3 tab + perf metrics ≤40 detik wifi kantor + tab switch ≤2s + TTFB ≤500ms + race condition + error template
- [ ] 1 git commit dengan message tagged `311-02` + `HTMX lazy load`
</success_criteria>

<output>
Note: SUMMARY.md tidak ditulis manual — `/gsd-execute-phase` membuatnya post-hoc dari template. Checklist konten yang diharapkan ada di SUMMARY (untuk execute-phase reference):
- Brief Plan 02 outcome (HTMX vendored, action refactored, shell view updated)
- Stopwatch metrics (median elapsed per partial action dari Task 4 perf measurement)
- Shell document size KB + end-to-end wifi kantor seconds + tab switch seconds (Task 4 measurements)
- Smoke parity verdict per tab (PASS/FAIL)
- Build status (errors/warnings count)
- Commit hash (Task 5)
- Handoff note ke Plan 03 (backend opportunistic AsNoTracking + 2 indexes + Categories cache)
- Memory transition note: update memory `project_311_wave0_checkpoint.md` Plan 02 complete
</output>
