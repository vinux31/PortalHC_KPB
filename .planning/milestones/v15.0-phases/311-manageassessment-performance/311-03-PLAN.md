---
phase: 311-manageassessment-performance
plan: 03
type: execute
wave: 2
depends_on: [311-02]
files_modified:
  - Data/ApplicationDbContext.cs
  - Migrations/{ts}_AddManageAssessmentPerfIndexes.cs
  - Migrations/{ts}_AddManageAssessmentPerfIndexes.Designer.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PERF-01]
tags: [perf, ef-core, migration, indexes, cache, asnotracking]
must_haves:
  truths:
    - "EF Core migration `AddManageAssessmentPerfIndexes` membuat 2 single-column index pada AssessmentSessions: IX_AssessmentSessions_LinkedGroupId + IX_AssessmentSessions_ExamWindowCloseDate (D-07)"
    - "Migration ter-apply ke Dev DB; verified via dotnet ef migrations list shows applied + sys.indexes query shows indexes ada"
    - "3 partial actions (ManageAssessmentTab_Assessment/_Training/_History dari Plan 02) pakai .AsNoTracking() di chain query read-only"
    - "Redundant .Include(a => a.User) di partial action Assessment di-hapus — projection sudah handle null guard `a.User != null ? a.User.FullName : \"Unknown\"`"
    - "Distinct Categories query di-wrap dengan _cache.GetOrCreateAsync(\"assessment_categories_distinct\", ...) TTL 5 menit absolute expiration (D-04)"
    - "Cache invalidation _cache.Remove(\"assessment_categories_distinct\") di AddCategory L320, EditCategory L372, DeleteCategory L401 (3 lokasi setelah SaveChangesAsync)"
    - "Build hijau: 0 errors, ≤ 92 warnings"
    - "Smoke test parity: 3 tab tetap render konten identik pre/post Plan 03 (kolom, row count, ordering)"
  artifacts:
    - path: "Data/ApplicationDbContext.cs"
      provides: "Fluent API entity.HasIndex untuk LinkedGroupId + ExamWindowCloseDate appended di AssessmentSession config block"
      contains: "ExamWindowCloseDate"
    - path: "Migrations/{ts}_AddManageAssessmentPerfIndexes.cs"
      provides: "EF Core migration Up/Down dengan CreateIndex IX_AssessmentSessions_LinkedGroupId + IX_AssessmentSessions_ExamWindowCloseDate"
      contains: "AddManageAssessmentPerfIndexes"
    - path: "Migrations/ApplicationDbContextModelSnapshot.cs"
      provides: "Auto-updated snapshot dengan 2 b.HasIndex baru (auto-generated by dotnet ef)"
      contains: "ExamWindowCloseDate"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "AsNoTracking di 3 partial actions + redundant Include hapus + GetOrCreateAsync Categories cache + 3 cache invalidation hooks di Add/Edit/DeleteCategory"
      contains: "assessment_categories_distinct"
  key_links:
    - from: "Migration AddManageAssessmentPerfIndexes Up()"
      to: "SQL Server AssessmentSessions table"
      via: "migrationBuilder.CreateIndex(name: \"IX_AssessmentSessions_LinkedGroupId\", ..., column: \"LinkedGroupId\")"
      pattern: "IX_AssessmentSessions_(LinkedGroupId|ExamWindowCloseDate)"
    - from: "ManageAssessmentTab_Assessment partial action"
      to: "_cache (IMemoryCache)"
      via: "_cache.GetOrCreateAsync(\"assessment_categories_distinct\", ...)"
      pattern: "GetOrCreateAsync\\(\"assessment_categories_distinct\""
    - from: "AddCategory/EditCategory/DeleteCategory POST actions"
      to: "_cache invalidation"
      via: "_cache.Remove(\"assessment_categories_distinct\") setelah SaveChangesAsync"
      pattern: "_cache\\.Remove\\(\"assessment_categories_distinct\""
---

<objective>
Apply opportunistic backend optimizations sebagai Plan 03 (parallel-safe SETELAH Plan 02 HTMX di-merge): tambah `.AsNoTracking()` di 3 partial actions read-only chain (`ManageAssessmentTab_Assessment`, `_Training`, `_History`), hapus redundant `.Include(a => a.User)` di partial Assessment (projection sudah punya null guard), wrap distinct Categories query dengan `_cache.GetOrCreateAsync` TTL 5 menit (D-04), tambah cache invalidation `_cache.Remove("assessment_categories_distinct")` di `AddCategory`/`EditCategory`/`DeleteCategory` setelah `SaveChangesAsync` (3 lokasi), tambah 2 single-column index pada `AssessmentSessions` via EF Core migration (`IX_AssessmentSessions_LinkedGroupId` + `IX_AssessmentSessions_ExamWindowCloseDate` per D-05/D-06/D-07).

**Purpose:** Plan 02 (HTMX lazy load) adalah primary lever untuk capai SC #2 (≥50% reduction wifi kantor). Plan 03 ini opportunistic — low-cost (~50 baris kode + 1 EF migration) dengan small wins backend (10-20% per partial action), resilience untuk scaling future. Tidak block Plan 02. Per D-07: "Plan 03 paralel/setelah Plan 02 HTMX". PERF-01 traceable.

**Output:**
- 1 EF Core migration class file + Designer.cs + auto-updated ModelSnapshot.cs
- 2 lines tambahan di `Data/ApplicationDbContext.cs` Fluent API
- AsNoTracking insertion di 3 partial actions chain start
- 1 Include line hapus di partial Assessment (projection preserved)
- IMemoryCache wrap di Categories query (1 location setelah Plan 02 selesai = di shell action ManageAssessment + di partial action Assessment, both query Categories)
- 3 cache invalidation hooks di Add/Edit/DeleteCategory setelah SaveChangesAsync
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/311-manageassessment-performance/311-CONTEXT.md
@.planning/phases/311-manageassessment-performance/311-DESIGN.md
@.planning/phases/311-manageassessment-performance/311-RESEARCH.md
@.planning/phases/311-manageassessment-performance/311-PATTERNS.md
@.planning/phases/311-manageassessment-performance/311-02-PLAN.md
@Controllers/AssessmentAdminController.cs
@Data/ApplicationDbContext.cs
@Migrations/ApplicationDbContextModelSnapshot.cs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase verbatim. -->

**Existing analog Fluent HasIndex (Data/ApplicationDbContext.cs:170-181):**
```csharp
builder.Entity<AssessmentSession>(entity =>
{
    entity.HasOne(a => a.User)
        .WithMany()
        .HasForeignKey(a => a.UserId)
        .OnDelete(DeleteBehavior.Cascade);

    // Indexes for performance
    entity.HasIndex(a => a.UserId);
    entity.HasIndex(a => new { a.UserId, a.Status });
    entity.HasIndex(a => a.Schedule);
    entity.HasIndex(a => a.AccessToken); // ← LAST existing index line, append after this
```

**Existing migration analog (Migrations/20260324030227_ChangeUniqueIndexToComposite.cs:115-127):**
```csharp
migrationBuilder.CreateIndex(
    name: "IX_AssessmentCategories_ParentId_Name",
    table: "AssessmentCategories",
    columns: new[] { "ParentId", "Name" },
    unique: true,
    filter: "[ParentId] IS NOT NULL");
```

**Existing cache.Remove pattern (Controllers/AssessmentAdminController.cs:3234, 3314):**
```csharp
_cache.Remove($"exam-status-{id}");
```

**Existing cache constructor injection (already present L22-48):**
```csharp
private readonly IMemoryCache _cache;
```

**Categories query yang harus di-cache (post-Plan 02 — exists di 2 lokasi):**

Lokasi 1: shell action `ManageAssessment` (post-Plan 02 Task 2, L~85 area):
```csharp
ViewBag.Categories = await _context.AssessmentSessions
    .Select(a => a.Category)
    .Distinct()
    .OrderBy(c => c)
    .ToListAsync();
```

Lokasi 2: partial action `ManageAssessmentTab_Assessment` (post-Plan 02 Task 2):
```csharp
ViewBag.Categories = await _context.AssessmentSessions
    .Select(a => a.Category)
    .Distinct()
    .OrderBy(c => c)
    .ToListAsync();
```

**Cache invalidation insertion points (Controllers/AssessmentAdminController.cs):**
- `AddCategory` action: setelah `await _context.SaveChangesAsync();` di L355 (sebelum `var currentUser = await _userManager.GetUserAsync(User);`)
- `EditCategory` POST action: setelah `await _context.SaveChangesAsync();` di L407 (sebelum `var currentUser = await _userManager.GetUserAsync(User);`)
- `DeleteCategory` action: setelah `await _context.SaveChangesAsync();` di L436 (sebelum `var currentUser = await _userManager.GetUserAsync(User);`)

**Migration generation command (executor runs):**
```bash
dotnet ef migrations add AddManageAssessmentPerfIndexes --project HcPortal.csproj
```
Auto-generate timestamped file `Migrations/{YYYYMMDDhhmmss}_AddManageAssessmentPerfIndexes.cs` + `.Designer.cs` + auto-update `ApplicationDbContextModelSnapshot.cs`.

**Migration apply command:**
```bash
dotnet ef database update --project HcPortal.csproj
```

**Migration verification queries (SQL Server):**
```sql
-- Via SSMS atau sqlcmd:
SELECT i.name, t.name AS table_name
FROM sys.indexes i
JOIN sys.tables t ON i.object_id = t.object_id
WHERE t.name = 'AssessmentSessions'
  AND i.name IN ('IX_AssessmentSessions_LinkedGroupId', 'IX_AssessmentSessions_ExamWindowCloseDate');
-- Expected: 2 rows
```

**Cache key constant (introduce sebagai const di top of class atau inline):**
- Per D-04 + Claude's Discretion: literal string sufficient untuk single use case. Recommended pakai `private const string CategoriesCacheKey = "assessment_categories_distinct";` di top of `AssessmentAdminController` class untuk DRY (1 const, 5 references: 2 GetOrCreateAsync + 3 Remove).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah 2 entity.HasIndex di ApplicationDbContext.cs + generate + apply migration AddManageAssessmentPerfIndexes</name>
  <files>
    Data/ApplicationDbContext.cs
    Migrations/{ts}_AddManageAssessmentPerfIndexes.cs
    Migrations/{ts}_AddManageAssessmentPerfIndexes.Designer.cs
    Migrations/ApplicationDbContextModelSnapshot.cs
  </files>
  <read_first>
    - Data/ApplicationDbContext.cs L170-200 (existing AssessmentSession entity config)
    - Migrations/ApplicationDbContextModelSnapshot.cs L425-475 (existing snapshot for AssessmentSessions)
    - Migrations/20260324030227_ChangeUniqueIndexToComposite.cs L1-130 (analog migration pattern)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-05 + D-06 + D-07 (index naming + migration name)
    - .planning/phases/311-manageassessment-performance/311-PATTERNS.md "Cross-cutting Pattern 4: Single-Column Fluent HasIndex"
  </read_first>
  <action>
**Step 1 — Tambah 2 baris `entity.HasIndex` di `Data/ApplicationDbContext.cs`:**

Find existing block (L170-181):
```csharp
builder.Entity<AssessmentSession>(entity =>
{
    entity.HasOne(a => a.User)
        .WithMany()
        .HasForeignKey(a => a.UserId)
        .OnDelete(DeleteBehavior.Cascade);

    // Indexes for performance
    entity.HasIndex(a => a.UserId);
    entity.HasIndex(a => new { a.UserId, a.Status });
    entity.HasIndex(a => a.Schedule);
    entity.HasIndex(a => a.AccessToken); // Removed .IsUnique() to allow shared tokens
```

Tambahkan 2 baris BARU tepat setelah `entity.HasIndex(a => a.AccessToken);` (line 181):
```csharp
    // Phase 311 Plan 03: ManageAssessment perf indexes (D-05, D-06)
    entity.HasIndex(a => a.ExamWindowCloseDate);
    entity.HasIndex(a => a.LinkedGroupId);
```

JANGAN modifikasi index existing (UserId, Schedule, AccessToken, dll). JANGAN ubah HasOne/WithMany config. JANGAN ubah HasCheckConstraint atau Property defaults.

**Step 2 — Generate migration via `dotnet ef`:**

```bash
dotnet ef migrations add AddManageAssessmentPerfIndexes --project HcPortal.csproj
```

Expected output:
- File baru `Migrations/{YYYYMMDDhhmmss}_AddManageAssessmentPerfIndexes.cs`
- File baru `Migrations/{YYYYMMDDhhmmss}_AddManageAssessmentPerfIndexes.Designer.cs`
- Auto-update `Migrations/ApplicationDbContextModelSnapshot.cs` (2 baris baru `b.HasIndex("ExamWindowCloseDate")` + `b.HasIndex("LinkedGroupId")` di blok AssessmentSession entity).

**Step 3 — Verify migration content:**

```bash
# Find migration file
ls Migrations/*_AddManageAssessmentPerfIndexes.cs

# Inspect Up() body — expected 2 CreateIndex calls
cat Migrations/*_AddManageAssessmentPerfIndexes.cs | head -40
```

Expected Up() body (auto-generated):
```csharp
protected override void Up(MigrationBuilder migrationBuilder)
{
    migrationBuilder.CreateIndex(
        name: "IX_AssessmentSessions_ExamWindowCloseDate",
        table: "AssessmentSessions",
        column: "ExamWindowCloseDate");

    migrationBuilder.CreateIndex(
        name: "IX_AssessmentSessions_LinkedGroupId",
        table: "AssessmentSessions",
        column: "LinkedGroupId");
}
```

Kalau index name TIDAK match exactly `IX_AssessmentSessions_ExamWindowCloseDate` atau `IX_AssessmentSessions_LinkedGroupId`, edit manual file `_AddManageAssessmentPerfIndexes.cs` rename. (EF Core convention `IX_{Table}_{Column}` harus auto-match — rare to need manual edit.)

**Step 4 — Apply migration ke Dev DB:**

```bash
dotnet ef database update --project HcPortal.csproj
```

Expected: "Done." atau "Applying migration '{ts}_AddManageAssessmentPerfIndexes'." tanpa error.

**Step 5 — Verify indexes ada di SQL Server (kalau SSMS available, atau via sqlcmd):**

```bash
sqlcmd -S "localhost\SQLEXPRESS" -E -Q "SELECT i.name FROM sys.indexes i JOIN sys.tables t ON i.object_id = t.object_id WHERE t.name = 'AssessmentSessions' AND i.name IN ('IX_AssessmentSessions_LinkedGroupId', 'IX_AssessmentSessions_ExamWindowCloseDate')"
```

Expected output: 2 rows.

Kalau SQL Server tidak available di environment executor (mis. CI/Linux), skip Step 5 — manual verification di Dev environment user.

**Step 6 — Verify build:**
```bash
dotnet build --nologo -v q
```
  </action>
  <verify>
    <automated>ls Migrations/*_AddManageAssessmentPerfIndexes.cs && grep -q "IX_AssessmentSessions_LinkedGroupId" Migrations/*_AddManageAssessmentPerfIndexes.cs && grep -q "IX_AssessmentSessions_ExamWindowCloseDate" Migrations/*_AddManageAssessmentPerfIndexes.cs && dotnet ef migrations list --project HcPortal.csproj 2>&1 | grep -q "AddManageAssessmentPerfIndexes" && echo "OK"</automated>
    Expected: "OK" — migration file exists dengan 2 CreateIndex + ter-apply.
  </verify>
  <acceptance_criteria>
    - File `Migrations/*_AddManageAssessmentPerfIndexes.cs` exists
    - `grep -c "CreateIndex" Migrations/*_AddManageAssessmentPerfIndexes.cs` = 2 (di Up()) + 2 DropIndex di Down() = total 4 jika cek both
    - `grep -q "IX_AssessmentSessions_LinkedGroupId" Migrations/*_AddManageAssessmentPerfIndexes.cs` returns 0
    - `grep -q "IX_AssessmentSessions_ExamWindowCloseDate" Migrations/*_AddManageAssessmentPerfIndexes.cs` returns 0
    - `dotnet ef migrations list --project HcPortal.csproj | grep -q "AddManageAssessmentPerfIndexes"` returns 0 (migration listed)
    - `grep -c "entity.HasIndex(a => a.ExamWindowCloseDate)" Data/ApplicationDbContext.cs` = 1
    - `grep -c "entity.HasIndex(a => a.LinkedGroupId)" Data/ApplicationDbContext.cs` = 1
    - `grep -q "b.HasIndex(\"ExamWindowCloseDate\")" Migrations/ApplicationDbContextModelSnapshot.cs` returns 0 (auto-updated)
    - `grep -q "b.HasIndex(\"LinkedGroupId\")" Migrations/ApplicationDbContextModelSnapshot.cs` returns 0
    - `dotnet build --nologo -v q` exit 0, 0 errors, warnings ≤ 92
  </acceptance_criteria>
  <done>
    2 entity.HasIndex baru di ApplicationDbContext.cs (preserve index existing). Migration `AddManageAssessmentPerfIndexes` ter-generate dengan 2 CreateIndex (Up) + 2 DropIndex (Down). ModelSnapshot.cs auto-updated. Migration ter-apply ke Dev DB (verified via `dotnet ef migrations list` shows applied). Build hijau.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tambah AsNoTracking + hapus redundant Include + IMemoryCache Categories + 3 cache invalidation hooks di AssessmentAdminController.cs</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs L20-50 (constructor — verify _cache injected) + cache key location decision
    - Controllers/AssessmentAdminController.cs (post-Plan 02 state) — find ManageAssessment shell action + 3 partial actions ManageAssessmentTab_*
    - Controllers/AssessmentAdminController.cs L296-413 (3 Category CRUD POST actions: AddCategory, EditCategory, DeleteCategory)
    - Controllers/AssessmentAdminController.cs L3234 + L3314 (existing _cache.Remove pattern analog)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-04 (cache GetOrCreateAsync TTL 5min)
    - .planning/phases/311-manageassessment-performance/311-PATTERNS.md "Core pattern C — IMemoryCache.GetOrCreateAsync" + "Core pattern D — Cache invalidation hook"
    - .planning/phases/311-manageassessment-performance/311-RESEARCH.md "Pitfall 4" (cache stampede acceptable per D-04 — admin endpoint low concurrency)
  </read_first>
  <action>
**Step 1 — Tambah cache key constant di top of `AssessmentAdminController` class:**

Find class declaration (L20):
```csharp
public class AssessmentAdminController : AdminBaseController
{
    private readonly IMemoryCache _cache;
```

Tambahkan const baru tepat setelah `private readonly IMemoryCache _cache;` (L22):
```csharp
    // Phase 311 Plan 03: cache key untuk distinct Categories list (D-04)
    private const string CategoriesCacheKey = "assessment_categories_distinct";
```

**Step 2 — Wrap Categories query di shell action `ManageAssessment` (post-Plan 02 — Categories di-fetch untuk filter dropdown):**

Find di shell action body (post-Plan 02 Task 2 state):
```csharp
ViewBag.Categories = await _context.AssessmentSessions
    .Select(a => a.Category)
    .Distinct()
    .OrderBy(c => c)
    .ToListAsync();
```

Replace dengan:
```csharp
ViewBag.Categories = await _cache.GetOrCreateAsync(CategoriesCacheKey, async entry =>
{
    entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5);
    return await _context.AssessmentSessions
        .AsNoTracking()
        .Select(a => a.Category)
        .Distinct()
        .OrderBy(c => c)
        .ToListAsync();
});
```

**Step 3 — Wrap Categories query di partial action `ManageAssessmentTab_Assessment` (post-Plan 02 Task 2):**

Find di partial action body:
```csharp
ViewBag.Categories = await _context.AssessmentSessions
    .Select(a => a.Category)
    .Distinct()
    .OrderBy(c => c)
    .ToListAsync();
```

Replace dengan SAMA persis dengan Step 2 (cache lookup pakai constant key — kedua lokasi share cache entry, single TTL).

**Step 4 — Tambah `.AsNoTracking()` di 3 partial actions read-only chain:**

Di partial action `ManageAssessmentTab_Assessment`, find:
```csharp
var managementQuery = _context.AssessmentSessions
    .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)
    .AsQueryable();
```

Replace dengan (insert `.AsNoTracking()` SEBELUM `.Where(...)` per PATTERNS.md convention):
```csharp
var managementQuery = _context.AssessmentSessions
    .AsNoTracking()
    .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)
    .AsQueryable();
```


**Catatan I-2 (clarify `_Training`/`_History`):** Kedua partial action ini **TIDAK** query `_context.AssessmentSessions` langsung — mereka delegasi ke `IWorkerDataService` (`GetWorkersInSection`, `GetAllWorkersHistory`). Verifikasi service layer sudah pakai AsNoTracking:

```bash
grep -c "AsNoTracking" Services/WorkerDataService.cs
# Expected: ≥ 9 (verified saat planning — service layer sudah AsNoTracking di semua read-only chain)
```

Jadi Plan 03 Step 4 hanya tambah `.AsNoTracking()` di **1 lokasi controller**: `ManageAssessmentTab_Assessment` `managementQuery` chain (yang query langsung `_context.AssessmentSessions`). Partial `_Training` dan `_History` tidak butuh patch di controller karena dependensi sudah AsNoTracking di service layer. Kalau verifikasi service layer FAIL (count < 9 atau ada read-only chain tanpa AsNoTracking), executor MUST flag dan tambah AsNoTracking ke chain yang missing — atau eskalasi ke gap-closure phase.

**Step 5 — Hapus redundant `.Include(a => a.User)` di partial action Assessment:**

Find di partial action `ManageAssessmentTab_Assessment`:
```csharp
var allSessions = await managementQuery
    .Include(a => a.User)
    .OrderByDescending(a => a.Schedule)
    .Select(a => new
    {
        ...
        UserFullName = a.User != null ? a.User.FullName : "Unknown",
        ...
    })
    .ToListAsync();
```

Hapus baris `.Include(a => a.User)` saja (line 1 di chain projection). Projection body TIDAK berubah — null guard `a.User != null ? a.User.FullName : "Unknown"` sudah handle nav property access. EF Core 8 auto-emit `LEFT JOIN AspNetUsers` dari projection.

**Step 6 — Tambah cache invalidation di 3 Category CRUD actions:**

Find `AddCategory` (L355 area, after `await _context.SaveChangesAsync();`):
```csharp
_context.AssessmentCategories.Add(category);
await _context.SaveChangesAsync();

var currentUser = await _userManager.GetUserAsync(User);
```

Insert SEBELUM `var currentUser = await _userManager.GetUserAsync(User);`:
```csharp
_context.AssessmentCategories.Add(category);
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);  // Phase 311 Plan 03 (D-04 invalidation)

var currentUser = await _userManager.GetUserAsync(User);
```

Find `EditCategory POST` (L407 area, after `await _context.SaveChangesAsync();`):
```csharp
category.SignatoryUserId = string.IsNullOrWhiteSpace(signatoryUserId) ? null : signatoryUserId;
await _context.SaveChangesAsync();

var currentUser = await _userManager.GetUserAsync(User);
```

Insert SEBELUM `var currentUser = ...`:
```csharp
category.SignatoryUserId = string.IsNullOrWhiteSpace(signatoryUserId) ? null : signatoryUserId;
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);  // Phase 311 Plan 03 (D-04 invalidation)

var currentUser = await _userManager.GetUserAsync(User);
```

Find `DeleteCategory` (L436 area, after `await _context.SaveChangesAsync();`):
```csharp
_context.AssessmentCategories.Remove(category);
await _context.SaveChangesAsync();

var currentUser = await _userManager.GetUserAsync(User);
```

Insert SEBELUM `var currentUser = ...`:
```csharp
_context.AssessmentCategories.Remove(category);
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);  // Phase 311 Plan 03 (D-04 invalidation)

var currentUser = await _userManager.GetUserAsync(User);
```

**Step 7 — Verify build:**
```bash
dotnet build --nologo -v q
```

**KRITIS notes:**
- AsNoTracking placement: SEBELUM `.Where(...)` per codebase convention (PATTERNS.md Cross-cutting Pattern 2)
- `.Include(a => a.User)` removal aman karena projection sudah punya null guard — EF Core emit equivalent SQL `LEFT JOIN AspNetUsers`
- Cache invalidation di 3 Category CRUD: setelah `SaveChangesAsync` + sebelum audit log/redirect (PATTERNS.md Cross-cutting Pattern 3)
- `_cache.Remove(CategoriesCacheKey)` aman dipanggil multiple times (idempotent)
- TTL 5 menit absolute expiration (D-04) — pure time-based, no sliding/LRU
- Cache key static `"assessment_categories_distinct"` (D-04) — single global key, no per-user variant
  </action>
  <verify>
    <automated>grep -c "AsNoTracking" Controllers/AssessmentAdminController.cs && grep -c "GetOrCreateAsync(CategoriesCacheKey" Controllers/AssessmentAdminController.cs && grep -c "_cache.Remove(CategoriesCacheKey)" Controllers/AssessmentAdminController.cs && dotnet build --nologo -v q 2>&1 | grep -E "(Build succeeded|Build FAILED)"</automated>
    Expected: AsNoTracking ≥ 3, GetOrCreateAsync ≥ 2, Remove ≥ 3, Build succeeded.
  </verify>
  <acceptance_criteria>
    - `grep -q "private const string CategoriesCacheKey" Controllers/AssessmentAdminController.cs` returns 0
    - `grep -c "GetOrCreateAsync(CategoriesCacheKey" Controllers/AssessmentAdminController.cs` ≥ 2 (shell + partial Assessment)
    - `grep -c "AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5)" Controllers/AssessmentAdminController.cs` ≥ 2
    - `grep -c "_cache.Remove(CategoriesCacheKey)" Controllers/AssessmentAdminController.cs` ≥ 3 (Add/Edit/Delete invalidation)
    - `grep -c "AsNoTracking" Controllers/AssessmentAdminController.cs` ≥ 3 (di 3 partial actions chain — bisa lebih kalau cache factory delegate juga pakai)
    - `awk '/public async Task<IActionResult> ManageAssessmentTab_Assessment/,/return PartialView/' Controllers/AssessmentAdminController.cs | grep -c "Include(a => a.User)"` = 0 (Include hapus di partial Assessment)
    - `dotnet build --nologo -v q` exit 0, 0 errors, warnings ≤ 92
  </acceptance_criteria>
  <done>
    Cache key constant `CategoriesCacheKey` di-introduce. Distinct Categories di-cache via `GetOrCreateAsync` TTL 5min di shell + partial Assessment. AsNoTracking added di 3 partial actions read-only chain. Redundant `.Include(a => a.User)` di partial Assessment hapus. 3 cache invalidation hooks di Add/Edit/DeleteCategory setelah SaveChangesAsync. Build hijau.
  </done>
</task>

<task type="auto">
  <name>Task 3: Smoke test parity post-Plan 03 + commit</name>
  <files>
    Data/ApplicationDbContext.cs
    Migrations/{ts}_AddManageAssessmentPerfIndexes.cs
    Migrations/{ts}_AddManageAssessmentPerfIndexes.Designer.cs
    Migrations/ApplicationDbContextModelSnapshot.cs
    Controllers/AssessmentAdminController.cs
  </files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (verify Task 2 changes intact)
    - Data/ApplicationDbContext.cs (verify Task 1 changes intact)
    - Migrations/*_AddManageAssessmentPerfIndexes.cs (verify migration intact + applied)
  </read_first>
  <action>
**Step 1 — Build + smoke test parity automation:**

```bash
dotnet build --nologo -v q
# Expected: 0 errors, ≤ 92 warnings
```

**Step 2 — Optional smoke runtime test (kalau dotnet run available di executor environment):**

```bash
# Start app di background
dotnet run --no-launch-profile --project HcPortal.csproj > plan03-smoke.log 2>&1 &
APP_PID=$!
sleep 15  # wait sampai ASP.NET Core listening

# Hit 3 partial endpoints + verify 200 OK + check log Stopwatch entries
for tab in assessment training history; do
  curl -k -s -o /dev/null -w "%{http_code}" "https://localhost:5001/KPB-PortalHC/Admin/ManageAssessmentTab_${tab^}?page=1&pageSize=20" || echo "fail"
done

# Stop app
kill $APP_PID 2>/dev/null

# Verify Stopwatch log entries present
grep "ManageAssessment perf \[tab=" plan03-smoke.log | head -5
```

Note: Kalau auth required (login session cookie), smoke runtime butuh manual UAT. Skip Step 2 dan rely pada manual UAT yang sama dengan Plan 02 Task 4 — user verify smoke parity belum berubah pre/post Plan 03.

**Step 3 — Verify migration applied di Dev DB (kalau SQL Server reachable):**

```bash
sqlcmd -S "localhost\SQLEXPRESS" -E -Q "SELECT i.name FROM sys.indexes i JOIN sys.tables t ON i.object_id = t.object_id WHERE t.name = 'AssessmentSessions' AND i.name IN ('IX_AssessmentSessions_LinkedGroupId', 'IX_AssessmentSessions_ExamWindowCloseDate')" 2>&1 | tee plan03-indexes.txt

# Expected: 2 rows dengan name kedua index
```

Kalau SQL Server tidak available, skip — user manual verify di Dev environment.

**Step 4 — Commit:**

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "feat(311-03): backend opportunistic — AsNoTracking + 2 indexes + Categories cache TTL 5min (D-04..D-07, PERF-01)" \
  --files Data/ApplicationDbContext.cs \
          Migrations/*_AddManageAssessmentPerfIndexes.cs \
          Migrations/*_AddManageAssessmentPerfIndexes.Designer.cs \
          Migrations/ApplicationDbContextModelSnapshot.cs \
          Controllers/AssessmentAdminController.cs
```

Verify commit landed:
```bash
git log -1 --oneline
git show --stat HEAD | grep -E "(ApplicationDbContext|AddManageAssessmentPerfIndexes|ApplicationDbContextModelSnapshot|AssessmentAdminController)"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | grep -q "311-03" && echo "OK"</automated>
    Expected: "OK" — commit dengan tag 311-03 landed.
  </verify>
  <acceptance_criteria>
    - `dotnet build --nologo -v q` exit 0, 0 errors, warnings ≤ 92
    - `git log -1 --oneline` mention `311-03` + `AsNoTracking` + `indexes` + `cache`
    - Files in commit: ApplicationDbContext.cs, migration file (.cs + .Designer.cs), ModelSnapshot.cs, AssessmentAdminController.cs
    - `git status` clean
    - (Manual / optional automated) — smoke parity 3 tab tetap render konten identik dengan post-Plan 02 (kolom, row count, ordering)
  </acceptance_criteria>
  <done>
    Plan 03 backend opportunistic optimizations ter-commit. Build hijau. Migration ter-apply. AsNoTracking di 3 partial actions, Include redundant hapus, Categories cache TTL 5min dengan 3 invalidation hooks. Smoke parity preserved. Phase 311 ready untuk verify-work + close.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller action → DB (read) | EF Core query path dengan AsNoTracking — read-only, no state change |
| Controller action → IMemoryCache | In-memory cache di same process, no external dependency |
| Migration tooling → SQL Server | DDL executed via EF Core migration framework dengan elevated DB user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-311-03-01 | Tampering | EF Core migration `AddManageAssessmentPerfIndexes` | accept | DDL hanya CreateIndex (read-only optimization, no schema modification beyond index addition). Down() menyediakan DropIndex untuk rollback. Migration class auto-generated by `dotnet ef` tooling — no manual SQL injection vector. |
| T-311-03-02 | Information Disclosure | Cache poisoning via cache key | mitigate | Cache key fixed string `"assessment_categories_distinct"` literal const, NO user input in key construction. Cache value hanya berisi public Category names (`a.Category` field — public domain entity). Existing precedent L3234 pakai user-derived key (`exam-status-{id}`) — Phase 311 Plan 03 strictly stricter (static key). |
| T-311-03-03 | Denial of Service | Cache stampede (multiple concurrent miss → multiple factory delegate execute) | accept | Per RESEARCH Pitfall 4 + D-04 explicit acceptance: admin endpoint low concurrency (Admin/HC role only, ~5-10 concurrent users max). .NET 8 IMemoryCache TIDAK punya native stampede protection (HybridCache .NET 9+ adds it). Worst case stampede: 5-10 redundant DB queries selama TTL window — acceptable cost vs implementation complexity. Documented sebagai accepted risk. |
| T-311-03-04 | Information Disclosure | AsNoTracking impact pada lazy-loading exception | accept | AsNoTracking + projection (Select new) NOT trigger lazy loading karena query terminal di `ToListAsync()`. Projection access nav property `a.User.FullName` di-handle EF Core via SQL JOIN, bukan runtime navigation. No exception risk. |
| T-311-03-05 | Information Disclosure | Cache TTL 5min mungkin serve stale Categories saat CRUD baru | mitigate | 3 cache invalidation hooks di AddCategory L355, EditCategory L407, DeleteCategory L436 setelah SaveChangesAsync — invalidate immediately on mutation. Worst case stale window: kalau invalidation gagal (cache.Remove throw — unlikely) sampai TTL expire = 5 menit. Acceptable per D-04. ToggleCategoryActive (L450-471) TIDAK di-invalidate karena query `AssessmentSessions.Category` (string column) tidak depend pada `IsActive` flag di `AssessmentCategories` table — dataset different. |
| T-311-03-06 | Tampering | DB index addition mempengaruhi query plan | accept | Indexes are non-unique single-column. SQL Server query optimizer auto-pick best index — backward compat preserved. Worst case: query plan slightly different post-index, performance same or better. Down() migration menyediakan rollback path kalau diperlukan. |

**No HIGH/CRITICAL threats di Plan 03.** Optimization changes preserve semantics; no new attack surface beyond static-key cache.
</threat_model>

<verification>
**Phase-level checks:**

1. **Migration generated + applied (after Task 1):**
   ```bash
   ls Migrations/*_AddManageAssessmentPerfIndexes.cs
   dotnet ef migrations list --project HcPortal.csproj | grep "AddManageAssessmentPerfIndexes"
   ```

2. **Index Fluent API (after Task 1):**
   ```bash
   grep "entity.HasIndex(a => a.ExamWindowCloseDate)" Data/ApplicationDbContext.cs
   grep "entity.HasIndex(a => a.LinkedGroupId)" Data/ApplicationDbContext.cs
   ```

3. **AsNoTracking + Include removal (after Task 2):**
   ```bash
   grep -c "AsNoTracking" Controllers/AssessmentAdminController.cs
   # Expected: ≥ 3 (3 partial actions + 2 cache factory delegates)
   awk '/ManageAssessmentTab_Assessment/,/return PartialView/' Controllers/AssessmentAdminController.cs | grep -c "Include(a => a.User)"
   # Expected: 0
   ```

4. **Cache pattern (after Task 2):**
   ```bash
   grep -c "GetOrCreateAsync(CategoriesCacheKey" Controllers/AssessmentAdminController.cs
   # Expected: ≥ 2 (shell + partial Assessment)
   grep -c "_cache.Remove(CategoriesCacheKey)" Controllers/AssessmentAdminController.cs
   # Expected: ≥ 3 (Add/Edit/Delete invalidation)
   ```

5. **Build (after each task):**
   ```bash
   dotnet build --nologo -v q
   ```

6. **Commit (after Task 3):**
   ```bash
   git log -1 --pretty=format:"%s" | grep "311-03"
   ```
</verification>

<success_criteria>
**Plan 03 complete when:**

- [ ] 2 baris `entity.HasIndex(a => a.ExamWindowCloseDate)` + `entity.HasIndex(a => a.LinkedGroupId)` appended di `Data/ApplicationDbContext.cs` AssessmentSession config block (D-05, D-06)
- [ ] EF Core migration `AddManageAssessmentPerfIndexes` ter-generate dengan 2 CreateIndex (Up) + 2 DropIndex (Down) — index names `IX_AssessmentSessions_LinkedGroupId` + `IX_AssessmentSessions_ExamWindowCloseDate` (D-07)
- [ ] `Migrations/ApplicationDbContextModelSnapshot.cs` auto-updated dengan 2 baris `b.HasIndex` baru
- [ ] Migration ter-apply ke Dev DB (`dotnet ef database update` sukses; verified via `dotnet ef migrations list` shows applied)
- [ ] Cache key constant `private const string CategoriesCacheKey = "assessment_categories_distinct"` di top of `AssessmentAdminController` class
- [ ] `_cache.GetOrCreateAsync(CategoriesCacheKey, ...)` dengan `AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5)` + `.AsNoTracking()` factory di shell action `ManageAssessment` + partial action `ManageAssessmentTab_Assessment` (D-04)
- [ ] `.AsNoTracking()` ditambah di chain start partial action `ManageAssessmentTab_Assessment` `managementQuery` (sebelum `.Where(...)`)
- [ ] `.AsNoTracking()` ditambah di chain start partial actions `ManageAssessmentTab_Training` (untuk read-only chains internal kalau ada — verify per partial body)
- [ ] `.AsNoTracking()` ditambah di chain start partial action `ManageAssessmentTab_History` (untuk read-only chains internal)
- [ ] Redundant `.Include(a => a.User)` di partial action `ManageAssessmentTab_Assessment` di-hapus (projection null guard preserved)
- [ ] 3 cache invalidation hooks `_cache.Remove(CategoriesCacheKey)` di `AddCategory`, `EditCategory POST`, `DeleteCategory` — setelah `SaveChangesAsync` + sebelum audit log
- [ ] `dotnet build --nologo -v q` returns 0 errors, ≤ 92 warnings
- [ ] Smoke parity 3 tab preserved (rendered konten identik pre/post Plan 03 — manual UAT optional, automated grep sufficient untuk no-regression confidence)
- [ ] 1 git commit dengan message tagged `311-03` + `backend opportunistic`
</success_criteria>

<output>
After completion, create `.planning/phases/311-manageassessment-performance/311-03-SUMMARY.md` dengan:
- Brief Plan 03 outcome (2 indexes, AsNoTracking, Categories cache, invalidation hooks)
- Migration name + applied timestamp
- File changes count + key grep counts (AsNoTracking ≥3, GetOrCreateAsync ≥2, Remove ≥3)
- Build status (errors/warnings)
- Smoke parity verdict (no regression)
- Commit hash
- Phase 311 close note: ROADMAP entry update + STATE.md update — Phase 311 complete dengan 3 plans (01 baseline preserved, 02 HTMX, 03 backend)
- Cross-reference Plan 02 SUMMARY (perf metrics dari wifi kantor — Plan 03 small wins backend additive)
</output>
