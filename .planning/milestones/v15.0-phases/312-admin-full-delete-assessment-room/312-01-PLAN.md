---
phase: 312
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements:
  - DEL-01
threat_refs:
  - T-312-01
  - T-312-02
  - T-312-03
  - T-312-04

must_haves:
  truths:
    - "Admin role bisa delete assessment regardless Status atau ResponseCount (override)"
    - "HC role di-block untuk delete assessment dengan Status=Completed atau responseCount > 0"
    - "Block reject di body method (bukan attribute), tulis AuditLog blocked entry, set TempData[Error], redirect ManageAssessment"
    - "Delete sukses tulis AuditLog success entry dengan Status= dan ResponseCount= di description"
    - "GetDeleteImpact JSON endpoint kembalikan {status, responseCount, certCount, packageCount, attemptCount, sessionCount, prePostBreakdown?} untuk modal impact preview"
    - "Cascade delete utuh — guard sisip SEBELUM cascade block, bukan menggantikannya"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "EnsureCanDeleteAsync helper + 3 guarded delete methods + GetDeleteImpact AJAX endpoint"
      contains: "private async Task<IActionResult?> EnsureCanDeleteAsync"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "GetDeleteImpact HttpGet AJAX endpoint"
      contains: "public async Task<IActionResult> GetDeleteImpact"
  key_links:
    - from: "DeleteAssessment(int id) line 2022"
      to: "EnsureCanDeleteAsync helper"
      via: "guard call setelah PrePost block (line ~2046), pre-cascade"
      pattern: "var blockResult = await EnsureCanDeleteAsync"
    - from: "DeleteAssessmentGroup(int id) line 2127"
      to: "EnsureCanDeleteAsync helper"
      via: "guard call setelah siblings load (line ~2152)"
      pattern: "var blockResult = await EnsureCanDeleteAsync"
    - from: "DeletePrePostGroup(int linkedGroupId) line 2232"
      to: "EnsureCanDeleteAsync helper"
      via: "guard call setelah groupSessions load (line ~2247)"
      pattern: "var blockResult = await EnsureCanDeleteAsync"
    - from: "EnsureCanDeleteAsync"
      to: "_auditLog.LogAsync"
      via: "blocked entry dengan ActionType suffix Blocked"
      pattern: "\\\"DeleteAssessment.*Blocked\\\""
---

<objective>
Tambah role-tier guard di body method 3 delete actions (`DeleteAssessment`, `DeleteAssessmentGroup`, `DeletePrePostGroup`) di `Controllers/AssessmentAdminController.cs` supaya akun Admin bisa full-delete regardless Status/responseCount, sementara akun HC di-block untuk Status=Completed atau ada response peserta. Plus tambah `GetDeleteImpact` HttpGet AJAX endpoint untuk modal impact preview di Plan 02.

Purpose: Menutup celah security HC-bypass tanpa menggangu Authorize attribute existing (per D-04, T-312-01 PRIMARY mitigation di server-side body guard, bukan UI). Plus persiapkan endpoint AJAX untuk modal 2-step impact preview (D-02).

Output:
- `EnsureCanDeleteAsync` private helper (Admin override → null; HC + (Completed atau responseCount>0) → AuditLog blocked entry + TempData["Error"] + RedirectToAction("ManageAssessment")).
- 3 method delete dengan guard injection + extended AuditLog success description (`Status=... ResponseCount=...` per UI-SPEC line 167-169).
- `GetDeleteImpact(int id, string type)` HttpGet AJAX endpoint return JSON {status, responseCount, certCount, packageCount, attemptCount, sessionCount, prePostBreakdown?}.
- `dotnet build` pass dengan zero new warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/312-admin-full-delete-assessment-room/312-CONTEXT.md
@.planning/phases/312-admin-full-delete-assessment-room/312-RESEARCH.md
@.planning/phases/312-admin-full-delete-assessment-room/312-PATTERNS.md
@.planning/phases/312-admin-full-delete-assessment-room/312-VALIDATION.md
@.planning/phases/312-admin-full-delete-assessment-room/312-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key existing types/methods executor butuhkan — NO codebase exploration needed -->

From Services/AuditLogService.cs:21-42 (signature):
```csharp
public async Task LogAsync(
    string actorUserId,
    string actorName,
    string actionType,
    string description,
    int targetId,
    string targetType)
```

From Controllers/AssessmentAdminController.cs (existing usages):
```csharp
// _userManager (UserManager<ApplicationUser>) — DI injected
// _context (ApplicationDbContext) — DI injected
// _auditLog (AuditLogService) — DI injected

// Action attribute pattern preserved verbatim (lines 2019-2022, 2124-2127, 2229-2232):
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> DeleteAssessment(int id)

// AuditLog success call pattern (lines 2094-2109):
var deleteUser = await _userManager.GetUserAsync(User);
var deleteActorName = string.IsNullOrWhiteSpace(deleteUser?.NIP)
    ? (deleteUser?.FullName ?? "Unknown")
    : $"{deleteUser.NIP} - {deleteUser.FullName}";
await _auditLog.LogAsync(
    deleteUser?.Id ?? "",
    deleteActorName,
    "DeleteAssessment",
    $"Deleted assessment '{assessmentTitle}' [ID={id}]",
    id,
    "AssessmentSession");

// AssessmentSession entity fields used:
// - Id (int), Status (string), Title (string), LinkedGroupId (int?), NomorSertifikat (string?)
// - GenerateCertificate (bool — for cert count proxy)
```

From GetAkhiriSemuaCounts (Controllers/AssessmentAdminController.cs:3419-3435) — analog blueprint:
```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> GetAkhiriSemuaCounts(string title, string category, DateTime scheduleDate)
{
    var sessions = await _context.AssessmentSessions
        .Where(a => a.Title == title && a.Category == category && a.Schedule.Date == scheduleDate.Date
                 && (a.Status == "Open" || a.Status == "InProgress"))
        .ToListAsync();
    int inProgressCount = sessions.Count(s => s.StartedAt != null && s.CompletedAt == null && s.Score == null);
    int notStartedCount = sessions.Count(s => s.StartedAt == null);
    return Json(new { inProgressCount, notStartedCount });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add EnsureCanDeleteAsync private helper + GetDeleteImpact AJAX endpoint</name>
  <files>Controllers/AssessmentAdminController.cs</files>

  <read_first>
    - Controllers/AssessmentAdminController.cs (the file being modified — read lines 2018-2317 untuk 3 delete bodies + 3419-3435 untuk GetAkhiriSemuaCounts analog)
    - Services/AuditLogService.cs:21-42 (LogAsync signature confirmation)
    - .planning/phases/312-admin-full-delete-assessment-room/312-PATTERNS.md (section 1.D `EnsureCanDeleteAsync` skeleton + 1.E GetDeleteImpact analog)
    - .planning/phases/312-admin-full-delete-assessment-room/312-UI-SPEC.md (section "Audit Log Description Format" line 163-174 untuk blocked description format)
  </read_first>

  <action>
Buat 2 anggota baru di class `AssessmentAdminController` (file `Controllers/AssessmentAdminController.cs`):

**A. Private helper `EnsureCanDeleteAsync`** — tempatkan di akhir class (sebelum closing brace) atau dekat existing helpers. Signature literal:

```csharp
// PHASE 312: Role-tier guard untuk 3 delete methods (DEL-01, T-312-01 PRIMARY mitigation)
private async Task<IActionResult?> EnsureCanDeleteAsync(
    string actionPrefix,                       // "DeleteAssessment" | "DeleteAssessmentGroup" | "DeletePrePostGroup"
    int targetId,
    string entityType,                         // "AssessmentSession"
    IList<AssessmentSession> sessions)
{
    // Admin override: lewati semua cek (D-04 Admin tier)
    if (User.IsInRole("Admin")) return null;

    // HC tier: cek Status==Completed atau ada response peserta
    var sessionIds = sessions.Select(s => s.Id).ToList();
    int responseCount = await _context.PackageUserResponses
        .CountAsync(r => sessionIds.Contains(r.AssessmentSessionId));
    bool anyCompleted = sessions.Any(s => s.Status == "Completed");

    if (anyCompleted || responseCount > 0)
    {
        // AuditLog blocked entry (D-03)
        try
        {
            var blockUser = await _userManager.GetUserAsync(User);
            var blockActor = string.IsNullOrWhiteSpace(blockUser?.NIP)
                ? (blockUser?.FullName ?? "Unknown")
                : $"{blockUser.NIP} - {blockUser.FullName}";

            // Per UI-SPEC line 170-172: format description per action type
            string statusSummary;
            if (sessions.Count == 1)
            {
                statusSummary = sessions[0].Status;
            }
            else if (actionPrefix == "DeletePrePostGroup")
            {
                // Per Q3 RESOLVED — explicit per-session "PreTest:X,PostTest:Y"
                // Field discriminator: AssessmentSession.AssessmentType (string nullable: "PreTest" | "PostTest" | null)
                // Confirmed dari Models/AssessmentSession.cs:154 (AssessmentType field, v14.0 Assessment Enhancement columns)
                var pre = sessions.FirstOrDefault(s => s.AssessmentType == "PreTest");
                var post = sessions.FirstOrDefault(s => s.AssessmentType == "PostTest");
                statusSummary = $"PreTest:{pre?.Status ?? "?"},PostTest:{post?.Status ?? "?"}";
            }
            else
            {
                // Group: aggregate distinct status counts
                statusSummary = string.Join("/", sessions.GroupBy(s => s.Status)
                    .Select(g => $"{g.Count()} {g.Key}"));
            }

            await _auditLog.LogAsync(
                blockUser?.Id ?? "",
                blockActor,
                $"{actionPrefix}Blocked",
                $"HC role blocked from {actionPrefix} [TargetId={targetId}]: Status={statusSummary}, ResponseCount={responseCount}",
                targetId,
                entityType);
        }
        catch (Exception auditEx)
        {
            var blockLogger = HttpContext.RequestServices.GetRequiredService<ILogger<AssessmentAdminController>>();
            blockLogger.LogWarning(auditEx, "Audit log write failed for {Action}Blocked TargetId={TargetId}", actionPrefix, targetId);
        }

        TempData["Error"] = "Anda tidak memiliki izin untuk menghapus assessment yang sudah Completed atau memiliki jawaban peserta.";
        return RedirectToAction("ManageAssessment");
    }

    return null;  // pass — caller lanjut cascade
}
```

LOCKED FIELDS (no executor decision needed):
- Field discriminator Pre/Post: `AssessmentSession.AssessmentType` (string nullable, values `"PreTest"` | `"PostTest"` | null) — confirmed dari `Models/AssessmentSession.cs:154` (v14.0 Assessment Enhancement columns).
- Cert proxy field: `AssessmentSession.NomorSertifikat` (string nullable) — confirmed dari `Models/AssessmentSession.cs:72` (Phase 192 cert number field).
- Format string output: `PreTest:{preStatus},PostTest:{postStatus}` (per UI-SPEC + Q3 RESOLVED).
- Cascade tables: `PackageUserResponses` → `AssessmentAttemptHistory` → `AssessmentPackages` → `AssessmentSessions` (verified existing pattern di line 2061-2089).

**B. New HttpGet `GetDeleteImpact`** — tempatkan SETELAH `GetAkhiriSemuaCounts` (~line 3435) untuk grouping locality dengan AJAX endpoint lain:

```csharp
// PHASE 312: AJAX impact preview untuk modal delete (D-02, T-312-04 mitigated by Authorize)
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> GetDeleteImpact(int id, string type)
{
    if (string.IsNullOrEmpty(type) || (type != "single" && type != "group" && type != "prepost"))
        return BadRequest(new { error = "Invalid type. Must be 'single', 'group', or 'prepost'." });

    try
    {
        List<AssessmentSession> sessions;

        if (type == "single")
        {
            var sess = await _context.AssessmentSessions
                .AsNoTracking()
                .FirstOrDefaultAsync(a => a.Id == id);
            if (sess == null) return NotFound(new { error = "Assessment not found." });
            sessions = new List<AssessmentSession> { sess };
        }
        else if (type == "group")
        {
            // Load representative + siblings by Title+Category+Schedule.Date (existing pattern dari DeleteAssessmentGroup)
            var rep = await _context.AssessmentSessions
                .AsNoTracking()
                .FirstOrDefaultAsync(a => a.Id == id);
            if (rep == null) return NotFound(new { error = "Group representative not found." });
            sessions = await _context.AssessmentSessions
                .AsNoTracking()
                .Where(a => a.Title == rep.Title && a.Category == rep.Category && a.Schedule == rep.Schedule)
                .ToListAsync();
        }
        else // prepost
        {
            sessions = await _context.AssessmentSessions
                .AsNoTracking()
                .Where(a => a.LinkedGroupId == id)
                .ToListAsync();
            if (sessions.Count == 0) return NotFound(new { error = "PrePost group not found." });
        }

        var sessionIds = sessions.Select(s => s.Id).ToList();
        int responseCount = await _context.PackageUserResponses
            .AsNoTracking()
            .CountAsync(r => sessionIds.Contains(r.AssessmentSessionId));
        int attemptCount = await _context.AssessmentAttemptHistory
            .AsNoTracking()
            .CountAsync(a => sessionIds.Contains(a.AssessmentSessionId));
        int packageCount = await _context.AssessmentPackages
            .AsNoTracking()
            .CountAsync(p => sessionIds.Contains(p.AssessmentSessionId));
        int certCount = sessions.Count(s => !string.IsNullOrEmpty(s.NomorSertifikat));

        string statusSummary;
        object? prePostBreakdown = null;
        if (type == "prepost")
        {
            // Per Q3 RESOLVED — per-session breakdown (field: AssessmentType per Models/AssessmentSession.cs:154)
            var pre = sessions.FirstOrDefault(s => s.AssessmentType == "PreTest");
            var post = sessions.FirstOrDefault(s => s.AssessmentType == "PostTest");
            int preResp = pre != null ? await _context.PackageUserResponses.AsNoTracking().CountAsync(r => r.AssessmentSessionId == pre.Id) : 0;
            int postResp = post != null ? await _context.PackageUserResponses.AsNoTracking().CountAsync(r => r.AssessmentSessionId == post.Id) : 0;
            statusSummary = $"PreTest:{pre?.Status ?? "-"},PostTest:{post?.Status ?? "-"}";
            prePostBreakdown = new
            {
                pre = new { status = pre?.Status ?? "-", responseCount = preResp },
                post = new { status = post?.Status ?? "-", responseCount = postResp }
            };
        }
        else if (sessions.Count == 1)
        {
            statusSummary = sessions[0].Status;
        }
        else
        {
            statusSummary = string.Join(" / ", sessions.GroupBy(s => s.Status).Select(g => $"{g.Count()} {g.Key}"));
        }

        return Json(new
        {
            status = statusSummary,
            responseCount,
            certCount,
            packageCount,
            attemptCount,
            sessionCount = sessions.Count,
            prePostBreakdown
        });
    }
    catch (Exception ex)
    {
        var logger = HttpContext.RequestServices.GetRequiredService<ILogger<AssessmentAdminController>>();
        logger.LogError(ex, "GetDeleteImpact failed for type={Type} id={Id}", type, id);
        return StatusCode(500, new { error = "Gagal mengambil data dampak penghapusan." });
    }
}
```

FIELDS LOCKED (verified dari source artifacts — no runtime decision needed):
- `AssessmentSession.NomorSertifikat` (string nullable) — `Models/AssessmentSession.cs:72`. Used directly untuk certCount via `sessions.Count(s => !string.IsNullOrEmpty(s.NomorSertifikat))`.
- `AssessmentSession.AssessmentType` (string nullable: `"PreTest"` | `"PostTest"`) — `Models/AssessmentSession.cs:154`. Used untuk discriminator di prepost branch.
- `AssessmentAttemptHistory` cascade pattern verified — line 2061-2065 (DeleteAssessment).
- `AssessmentPackages` field `AssessmentSessionId` verified — line 2068-2087 cascade.

Build verify: `dotnet build` (zero new warnings — Phase 311 baseline 92 warnings).
  </action>

  <verify>
    <automated>dotnet build 2>&amp;1 | grep -E "(error|Build succeeded)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "private async Task<IActionResult\?> EnsureCanDeleteAsync" Controllers/AssessmentAdminController.cs` returns 1 match
    - `grep -n "public async Task<IActionResult> GetDeleteImpact" Controllers/AssessmentAdminController.cs` returns 1 match
    - `grep -c "User.IsInRole(\"Admin\")" Controllers/AssessmentAdminController.cs` returns >= 1 (in EnsureCanDeleteAsync)
    - `grep -n "Blocked" Controllers/AssessmentAdminController.cs` shows ActionType "{actionPrefix}Blocked" usage in helper
    - `grep -c "AsNoTracking" Controllers/AssessmentAdminController.cs` shows GetDeleteImpact uses AsNoTracking on all reads (>= 5 calls in new endpoint)
    - `grep -c "\\[Authorize(Roles = \"Admin, HC\")\\]" Controllers/AssessmentAdminController.cs` returns >= 4 (3 existing delete + 1 new GetDeleteImpact)
    - `dotnet build` exits code 0; "Build succeeded" line present; zero NEW warnings vs Phase 311 baseline (92 warnings allowed)
    - Existing Authorize attributes line 2020/2125/2230 unchanged: `grep -n "Authorize(Roles = \"Admin, HC\")" Controllers/AssessmentAdminController.cs | head -5` masih menampilkan 3 entries di area delete methods (lines 2020/2125/2230) — TIDAK boleh hilang
  </acceptance_criteria>

  <done>
EnsureCanDeleteAsync helper + GetDeleteImpact endpoint exist, build pass, AuditLog blocked entry pattern instantiated, zero new compile warnings.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inject guard call + extend AuditLog success description di 3 delete methods</name>
  <files>Controllers/AssessmentAdminController.cs</files>

  <read_first>
    - Controllers/AssessmentAdminController.cs lines 2018-2121 (DeleteAssessment current state)
    - Controllers/AssessmentAdminController.cs lines 2123-2226 (DeleteAssessmentGroup current state)
    - Controllers/AssessmentAdminController.cs lines 2228-2317 (DeletePrePostGroup current state)
    - .planning/phases/312-admin-full-delete-assessment-room/312-PATTERNS.md (section 1.B insert points + 1.C AuditLog success format)
    - .planning/phases/312-admin-full-delete-assessment-room/312-UI-SPEC.md line 163-174 (Audit Log Description Format)
  </read_first>

  <action>
Edit 3 method delete di `Controllers/AssessmentAdminController.cs`. Pertahankan: action attributes (line 2019-2022, 2124-2127, 2229-2232), try/catch wrapper, cascade order, TempData["Success"]/["Error"] convention, AuditLog try/catch swallow pattern.

**A. `DeleteAssessment(int id)` — line 2022:**

1. Setelah `assessment == null` early return + setelah existing PrePost block (line ~2046, sebelum cascade), insert:

```csharp
// PHASE 312: role-tier guard (D-04, T-312-01)
var blockResult = await EnsureCanDeleteAsync(
    "DeleteAssessment",
    id,
    "AssessmentSession",
    new List<AssessmentSession> { assessment });
if (blockResult != null) return blockResult;

// PHASE 312: capture pre-delete snapshot SEBELUM cascade (line 2089 SaveChangesAsync hapus assessment)
string preDeleteStatus = assessment.Status;
int preDeleteResponseCount = await _context.PackageUserResponses
    .CountAsync(r => r.AssessmentSessionId == id);
```

2. Edit AuditLog success description (line ~2102) per UI-SPEC line 167:

```csharp
// FROM:
//   $"Deleted assessment '{assessmentTitle}' [ID={id}]",
// TO:
$"Deleted assessment '{assessmentTitle}' [ID={id}] Status={preDeleteStatus} ResponseCount={preDeleteResponseCount}",
```

**B. `DeleteAssessmentGroup(int id)` — line 2127:**

1. Setelah `siblings = await _context.AssessmentSessions.Where(...).ToListAsync()` line ~2152, insert:

```csharp
// PHASE 312: role-tier guard (D-04, T-312-01)
var blockResult = await EnsureCanDeleteAsync(
    "DeleteAssessmentGroup",
    id,
    "AssessmentSession",
    siblings);
if (blockResult != null) return blockResult;

// PHASE 312: capture pre-delete aggregated snapshot
var siblingIds = siblings.Select(s => s.Id).ToList();
string preDeleteStatusSummary = string.Join(" / ", siblings.GroupBy(s => s.Status).Select(g => $"{g.Count()} {g.Key}"));
int preDeleteResponseCount = await _context.PackageUserResponses
    .CountAsync(r => siblingIds.Contains(r.AssessmentSessionId));
int preDeleteSessionCount = siblings.Count;
```

2. Edit AuditLog success description (line ~2207) per UI-SPEC line 168:

```csharp
// FROM equivalent:
//   $"Deleted assessment group '{rep.Title}' [RepId={id}]",
// TO:
$"Deleted assessment group '{rep.Title}' [RepId={id}] SessionCount={preDeleteSessionCount} Status={preDeleteStatusSummary} ResponseCount={preDeleteResponseCount}",
```

(`rep` adalah representative session yang sudah di-load existing — verifikasi nama variable di context line ~2150).

**C. `DeletePrePostGroup(int linkedGroupId)` — line 2232:**

1. Setelah `groupSessions = await _context.AssessmentSessions.Where(a => a.LinkedGroupId == linkedGroupId).ToListAsync()` line ~2247, insert:

```csharp
// PHASE 312: role-tier guard (D-04, T-312-01)
var blockResult = await EnsureCanDeleteAsync(
    "DeletePrePostGroup",
    linkedGroupId,
    "AssessmentSession",
    groupSessions);
if (blockResult != null) return blockResult;

// PHASE 312: capture per-session breakdown SEBELUM cascade (Q3 RESOLVED)
// Field: AssessmentType per Models/AssessmentSession.cs:154 (values "PreTest" | "PostTest" | null)
var groupSessionIds = groupSessions.Select(s => s.Id).ToList();
var preSession = groupSessions.FirstOrDefault(s => s.AssessmentType == "PreTest");
var postSession = groupSessions.FirstOrDefault(s => s.AssessmentType == "PostTest");
string preDeleteStatusSummary = $"PreTest:{preSession?.Status ?? "-"},PostTest:{postSession?.Status ?? "-"}";
int preDeleteResponseCount = await _context.PackageUserResponses
    .CountAsync(r => groupSessionIds.Contains(r.AssessmentSessionId));
string groupTitle = preSession?.Title ?? postSession?.Title ?? $"LinkedGroup-{linkedGroupId}";
```

2. Edit AuditLog success description (line ~2299) per UI-SPEC line 169:

```csharp
// FROM equivalent:
//   $"Deleted Pre-Post group [LinkedGroupId={linkedGroupId}]",
// TO:
$"Deleted Pre-Post group '{groupTitle}' [LinkedGroupId={linkedGroupId}] Status={preDeleteStatusSummary} ResponseCount={preDeleteResponseCount}",
```

LOCKED (no executor decision):
- Field discriminator: `AssessmentSession.AssessmentType == "PreTest" / "PostTest"` per `Models/AssessmentSession.cs:154`. Existing `DeletePrePostGroup` body (line 2247-2287) sudah pakai field ini untuk grouping.
- Output format: `PreTest:{status},PostTest:{status}` (Q3 RESOLVED).

**Aturan ketat:**
- Cascade block (existing line 2048-2089 / 2158-2196 / 2254-2287) JANGAN diubah.
- Try/catch outer JANGAN diubah.
- TempData["Success"] message JANGAN diubah.
- Authorize attributes JANGAN diubah.
- Hanya: insert guard call + snapshot vars + extend description string.

Build verify: `dotnet build` (zero new warnings).
  </action>

  <verify>
    <automated>dotnet build 2>&amp;1 | grep -E "(error|Build succeeded)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "EnsureCanDeleteAsync" Controllers/AssessmentAdminController.cs` returns >= 4 (1 declaration + 3 call sites)
    - `grep -c "preDeleteStatus" Controllers/AssessmentAdminController.cs` returns >= 3 (single + group summary + prepost summary)
    - `grep -c "preDeleteResponseCount" Controllers/AssessmentAdminController.cs` returns >= 3
    - `grep -c "Status=.*ResponseCount=" Controllers/AssessmentAdminController.cs` returns >= 3 (3 success AuditLog descriptions extended)
    - `grep -n "Authorize(Roles = \"Admin, HC\")" Controllers/AssessmentAdminController.cs` masih menampilkan attribute di delete methods (lines 2020/2125/2230 area unchanged)
    - `grep -c "_context.PackageUserResponses\\.RemoveRange\\|_context.AssessmentSessions\\.Remove" Controllers/AssessmentAdminController.cs` cascade calls preserved (>= 6: 3 PackageUserResponses + 3 AssessmentSessions removes — atau jumlah existing baseline)
    - `dotnet build` exits 0, "Build succeeded", zero new warnings vs Phase 311 baseline (92 allowed)
    - **Cascade order grep verify (W3 — automated, no manual review):** untuk setiap delete method body, urutan `PackageUserResponses` → `AssessmentAttemptHistory` → `AssessmentPackages` → `AssessmentSessions.Remove` masih in-order. Run:
      ```bash
      # Per-method cascade order check — extract method body via awk, grep cascade lines, verify urutan ascending
      for METHOD in "DeleteAssessment" "DeleteAssessmentGroup" "DeletePrePostGroup"; do
        echo "=== $METHOD ==="
        awk "/public async Task<IActionResult> $METHOD\(/,/^    \}$/" Controllers/AssessmentAdminController.cs \
          | grep -nE "PackageUserResponses\.|AssessmentAttemptHistory\.|AssessmentPackages\.|AssessmentSessions\.Remove" \
          | head -20
      done
      ```
      Expect: setiap method menampilkan output dengan urutan baris ascending: `PackageUserResponses` line < `AssessmentAttemptHistory` line < `AssessmentPackages` line < `AssessmentSessions.Remove` line. Tidak ada cascade line yang muncul SEBELUM `EnsureCanDeleteAsync` call (guard harus pre-cascade).
    - **Cascade count sanity (W3 quick check):** `grep -c "_context\.PackageUserResponses\.RemoveRange\|_context\.PackageUserResponses\.Remove" Controllers/AssessmentAdminController.cs` returns >= 3 (1 per delete method).
    - **Final remove count:** `grep -c "_context\.AssessmentSessions\.Remove\|_context\.AssessmentSessions\.RemoveRange" Controllers/AssessmentAdminController.cs` returns >= 3 (final cascade step per method).
    - **Guard pre-cascade ordering:** `grep -nE "EnsureCanDeleteAsync|PackageUserResponses\.(Remove|RemoveRange)" Controllers/AssessmentAdminController.cs` — verifikasi visual: untuk tiap method body, line `EnsureCanDeleteAsync` muncul SEBELUM line `PackageUserResponses.Remove*` (guard harus return early jika reject, sebelum cascade dieksekusi).
  </acceptance_criteria>

  <done>
3 method delete punya guard injection di awal try block (sebelum cascade), AuditLog success description sertakan Status= dan ResponseCount=, cascade preserved, build pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → MVC route (HttpPost delete actions) | Untrusted user (HC role bisa craft direct POST bypass UI) |
| Browser → MVC route (HttpGet GetDeleteImpact) | Authenticated Admin/HC; less sensitive (read-only impact preview) |
| Controller body → AuditLogService | Internal trust; service writes to AuditLogs table |
| Controller body → ApplicationDbContext (PackageUserResponses, AssessmentAttemptHistory, AssessmentPackages, AssessmentSessions) | Internal trust; cascade RemoveRange operates on session-scoped IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-312-01 | Elevation of Privilege | DeleteAssessment / DeleteAssessmentGroup / DeletePrePostGroup HttpPost endpoints — HC user crafts direct POST tanpa UI lewat existing dropdown | mitigate | **PRIMARY** — `EnsureCanDeleteAsync` body guard (Task 1). Admin → null pass; HC + (Status=Completed atau responseCount>0) → AuditLog blocked + TempData error + RedirectToAction. Sisip SEBELUM cascade RemoveRange. UI hide layer = bonus defense-in-depth (Plan 02), bukan primary. |
| T-312-02 | Tampering | CSRF on destructive POST endpoints (attacker forces logged-in HC/Admin to delete via cross-site form) | mitigate | Existing `[ValidateAntiForgeryToken]` attribute on 3 delete methods preserved verbatim (no edit per CONTEXT line 10). Plan 02 modal form WAJIB carry `@Html.AntiForgeryToken()`. |
| T-312-03 | Repudiation | Audit log gaps — failed HC delete attempts (blocked) tidak ter-log → operator denial of attempt | mitigate | `EnsureCanDeleteAsync` writes blocked entry dengan ActionType `{actionPrefix}Blocked` + description sertakan reason (Status, ResponseCount, actor NIP+name). Wrapped try/catch dengan logger.LogWarning fallback (audit failure jangan break user flow). Per D-03 lock. |
| T-312-04 | Information Disclosure | `GetDeleteImpact` JSON endpoint reveal session data (responseCount, certCount, packageCount, attemptCount) ke HC role, walaupun HC dilarang delete | accept | `[Authorize(Roles = "Admin, HC")]` apply ke endpoint. HC sudah bisa lihat row di `ManageAssessment` (Title, Status, schedule) — endpoint cuma surface aggregated counts, bukan PII. Per A5 di RESEARCH §"Assumptions". Worker/Coach role tidak bisa akses (tier sudah dibatasi). |

</threat_model>

<verification>
Wave 1 verification (after both tasks):

1. **Build:** `dotnet build` → exit 0, zero new warnings (Phase 311 baseline 92 allowed).
2. **Grep audit:**
   - `grep -n "EnsureCanDeleteAsync" Controllers/AssessmentAdminController.cs` → 4 matches (1 decl + 3 calls) min.
   - `grep -n "GetDeleteImpact" Controllers/AssessmentAdminController.cs` → 1 match.
   - `grep -c "Status=.*ResponseCount=" Controllers/AssessmentAdminController.cs` → 3+ (success AuditLog descriptions extended).
   - `grep -n "Authorize(Roles = \"Admin, HC\")" Controllers/AssessmentAdminController.cs` → 4 matches (3 existing delete + 1 new GetDeleteImpact).
3. **Smoke API check (manual via curl/Postman atau via Playwright Plan 02):**
   - `curl http://localhost:5277/Admin/GetDeleteImpact?type=single&id={existingId}` (logged-in admin) → 200 + valid JSON shape.
4. **DB AuditLog query** (post Wave 2 E2E run, verifikasi at Phase 312 closure):
   ```sql
   SELECT TOP 10 ActionType, Description, CreatedAt FROM AuditLogs
   WHERE ActionType LIKE 'Delete%' ORDER BY CreatedAt DESC;
   ```
   Expect: success rows match `Status=... ResponseCount=...`; blocked rows match `*Blocked` ActionType + reason text.
</verification>

<success_criteria>
- [ ] `EnsureCanDeleteAsync` helper exists dengan Admin override + HC blocked branch + AuditLog blocked entry + TempData error + RedirectToAction.
- [ ] `GetDeleteImpact` HttpGet endpoint exists dengan branch type ∈ {single, group, prepost} + AsNoTracking + JSON response shape `{status, responseCount, certCount, packageCount, attemptCount, sessionCount, prePostBreakdown?}`.
- [ ] 3 method delete punya guard call sebelum cascade + snapshot pre-delete vars + extended AuditLog success description.
- [ ] Existing Authorize attributes line 2020/2125/2230 unchanged.
- [ ] Cascade order PackageUserResponses → AssessmentAttemptHistory → AssessmentPackages → AssessmentSessions preserved.
- [ ] `dotnet build` pass dengan zero new warnings.
- [ ] No regression di endpoint lain (smoke check via existing Playwright FLOW 1-9 PASS — verify di Plan 02 closure).
</success_criteria>

<output>
After Plan 01 completion, create `.planning/phases/312-admin-full-delete-assessment-room/312-01-SUMMARY.md` documenting:
- Helper signature + insertion location
- 3 delete method edits (line numbers actual vs predicted)
- GetDeleteImpact endpoint location + JSON shape contract
- Field discrimination decisions (e.g., Type vs IsPreTest untuk PrePost)
- Build warnings delta (should be 0)
- Any deviation from plan dengan rationale
</output>
