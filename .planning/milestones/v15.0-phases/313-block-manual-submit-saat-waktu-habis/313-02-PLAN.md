---
phase: 313-block-manual-submit-saat-waktu-habis
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - Controllers/CMPController.cs
autonomous: true
requirements: [TMR-01]
must_haves:
  truths:
    - "Worker yang klik manual Submit setelah `elapsed > Duration+ExtraTime` (Online/PreTest/PostTest) di-reject server-side dengan TempData D-01 + redirect StartExam"
    - "Worker yang fire auto-submit (`isAutoSubmit=true`) dalam grace 2min sukses submit (Tier-2 grace preserved)"
    - "Worker yang fire auto-submit setelah grace 2min di-reject dengan existing tier-2 message (D-06 preserved)"
    - "Worker yang submit assessment AssessmentType=Manual TIDAK di-block oleh guard (D-15 exclude)"
    - "AuditLog `SubmitExamBlocked` entry tertulis untuk setiap Tier-1 reject dengan Description berisi `Type=`, `ElapsedMin=`, `AllowedMin=`, `SessionId=` key=value"
    - "Audit write failure (DB exception) TIDAK block primary action — di-log via `_logger.LogWarning` lalu swallow"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "EnsureCanSubmitExamAsync helper + WriteSubmitBlockedAuditAsync helper + SubmitExam call site refactor"
      contains: "EnsureCanSubmitExamAsync"
      min_lines: 80
  key_links:
    - from: "Controllers/CMPController.cs SubmitExam (~line 1619)"
      to: "EnsureCanSubmitExamAsync helper"
      via: "var timerBlockResult = await EnsureCanSubmitExamAsync(assessment, isAutoSubmit); if (timerBlockResult != null) return timerBlockResult;"
      pattern: "EnsureCanSubmitExamAsync\\(assessment, isAutoSubmit\\)"
    - from: "EnsureCanSubmitExamAsync Tier-1 reject branch"
      to: "WriteSubmitBlockedAuditAsync"
      via: "await WriteSubmitBlockedAuditAsync(assessment, elapsed, allowedMinutes);"
      pattern: "WriteSubmitBlockedAuditAsync"
    - from: "WriteSubmitBlockedAuditAsync"
      to: "_auditLog.LogAsync"
      via: "actionType: \"SubmitExamBlocked\""
      pattern: "SubmitExamBlocked"
user_setup: []
---

<objective>
Backend implementation — modify `Controllers/CMPController.cs` `SubmitExam()` action: extract 2-tier timer guard ke helper privat `EnsureCanSubmitExamAsync` + AuditLog blocked entry helper `WriteSubmitBlockedAuditAsync`. Replace existing single-tier LIFE-03 block (line 1616-1628) dengan helper invocation.

Purpose: Close TMR-01 server-side enforcement gap — worker yang klik manual submit setelah waktu habis di-reject immediately tanpa 2min grace (yang saat ini bisa di-bypass). Auto-submit fallback tetap aman dengan grace 2min untuk network latency. Mirror Phase 312 D-04 pattern (`EnsureCanDeleteAsync` precedent) untuk testability + readability.

Output:
- `Controllers/CMPController.cs` di-modify:
  - 2 helper privat baru (`EnsureCanSubmitExamAsync` + `WriteSubmitBlockedAuditAsync`) di akhir class sebelum closing brace
  - SubmitExam line 1619-1628 (existing LIFE-03 block) replaced dengan helper call (3-line invocation)
  - DI fields existing (`_userManager`, `_auditLog`, `_logger`) reused — no constructor modification
- `dotnet build` exit 0 (compile gate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-RESEARCH.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-01-PLAN.md
@.planning/phases/312-admin-full-delete-assessment-room/312-01-SUMMARY.md
@Controllers/CMPController.cs
@Controllers/AssessmentAdminController.cs
@Models/AssessmentSession.cs
@Models/AssessmentConstants.cs
@Models/ApplicationUser.cs
@Services/AuditLogService.cs
@CLAUDE.md

<interfaces>
<!-- Critical contracts — executor pakai langsung, no codebase scavenger hunt -->

From Models/AssessmentConstants.cs:5-11 [VERIFIED]:
```csharp
public static class AssessmentType
{
    public const string Manual = "Manual";
    public const string Online = "Online";
    public const string PreTest = "PreTest";
    public const string PostTest = "PostTest";
}
```

From Models/AssessmentSession.cs (verified field names — C-01 OVERRIDE):
- `int Id` (PK)
- `string AssessmentType` (line 154 — NOT `Type`)
- `DateTime? StartedAt` (line 40)
- `int DurationMinutes` (line 19)
- `int? ExtraTimeMinutes` (line 192)
- `string Status`

From Services/AuditLogService.cs:21-43 [VERIFIED signature]:
```csharp
public async Task LogAsync(
    string actorUserId,
    string actorName,
    string actionType,
    string description,
    int? targetId = null,
    string? targetType = null)
```

From Controllers/CMPController.cs constructor (line 27-72) [VERIFIED DI fields available]:
- `private readonly UserManager<ApplicationUser> _userManager;` (line 27)
- `private readonly ApplicationDbContext _context;` (line 30)
- `private readonly AuditLogService _auditLog;` (line 32)
- `private readonly ILogger<CMPController> _logger;` (line 34)

From Controllers/CMPController.cs:1554-1556 [VERIFIED SubmitExam signature — preserved]:
```csharp
[HttpPost]
[ValidateAntiForgeryToken]
public async Task<IActionResult> SubmitExam(int id, Dictionary<int, int> answers, bool isAutoSubmit = false)
```

From Controllers/CMPController.cs:1616-1628 [VERIFIED — TARGET REPLACEMENT block]:
```csharp
// ---- Server-side timer enforcement (LIFE-03) ----
// Grace period: 2 minutes to account for network latency and slow connections.
// Skip check if StartedAt is null (legacy sessions that existed before Phase 21).
if (assessment.StartedAt.HasValue)
{
    var elapsed = DateTime.UtcNow - assessment.StartedAt.Value;
    int allowedMinutes = assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0) + 2; // 2-minute grace
    if (elapsed.TotalMinutes > allowedMinutes)
    {
        TempData["Error"] = "Waktu ujian Anda telah habis. Pengiriman jawaban tidak dapat diproses.";
        return RedirectToAction("StartExam", new { id });
    }
}
```

From Controllers/AssessmentAdminController.cs:5535-5612 [VERIFIED Phase 312 EnsureCanDeleteAsync pattern]:
- Helper privat di akhir class, signature `private async Task<IActionResult?>` (nullable return — null = pass, IActionResult = redirect/reject)
- Try/catch swallow audit write (line 5563-5605) dengan `_logger.LogWarning` fallback

From Models/ApplicationUser.cs (line 13 area) [VERIFIED]:
- `string FullName` (property)
- `string NIP` (property)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add EnsureCanSubmitExamAsync + WriteSubmitBlockedAuditAsync helpers di akhir CMPController class</name>

  <files>Controllers/CMPController.cs</files>

  <read_first>
    - Controllers/CMPController.cs (full file — confirm class closing brace location, existing using directives, DI field names, no helper name collision)
    - Controllers/AssessmentAdminController.cs:5474-5612 (Phase 312 EnsureCanDeleteAsync template — direct pattern reference)
    - Models/AssessmentConstants.cs (AssessmentType constants)
    - Models/AssessmentSession.cs:154 (verify AssessmentType field name)
    - Models/ApplicationUser.cs (verify FullName + NIP property names)
    - Services/AuditLogService.cs:21-43 (LogAsync signature)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md (section 1.C + 1.D — exact code skeleton)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md (D-01, D-05, D-06, D-15, C-01)
  </read_first>

  <behavior>
    - 2 helper privat baru ditambahkan **di akhir class CMPController, sebelum closing brace `}`** (mirror Phase 312 placement convention)
    - `EnsureCanSubmitExamAsync` returns `Task<IActionResult?>` — null = pass (caller lanjut), non-null = redirect/reject (caller `return` value)
    - Manual type exclude (D-15 / C-01) — pakai `AssessmentConstants.AssessmentType.{Online|PreTest|PostTest}` constants, JANGAN magic string
    - Tier-1 (NEW Phase 313): `!isAutoSubmit && elapsed > allowedMinutes` → reject manual + AuditLog blocked + TempData D-01 + redirect
    - Tier-2 (existing LIFE-03 preserved D-06): `elapsed > graceLimitMinutes` (allowed + 2) → reject + TempData existing message + redirect (NO AuditLog)
    - Legacy session guard: `StartedAt == null` → return null (skip check, existing convention preserved)
    - `WriteSubmitBlockedAuditAsync` wrap di try/catch dengan `_logger.LogWarning` fallback (Phase 312 T-306-02 precedent — audit failure jangan break submit reject path)
    - NO transaction wrap (intentional deviation — guard read-only check + audit write only, no state mutation)
    - Description format D-05 EXACT: `"HC/User role manual submit blocked after timeup. Type={X} ElapsedMin={Y} AllowedMin={Z} SessionId={W}"`
    - ActionType D-05 EXACT: `"SubmitExamBlocked"`
    - Bahasa Indonesia hanya untuk TempData["Error"] user-facing string. Log message + comment boleh English.
  </behavior>

  <action>
    Buka `Controllers/CMPController.cs` full read terlebih dahulu untuk:
    - Konfirmasi closing brace location (last `}` before EOF — class scope)
    - Konfirmasi existing using directives di top (jika perlu add `using HcPortal.Models;` untuk AssessmentConstants — cek namespace exact via Models/AssessmentConstants.cs)
    - Konfirmasi DI field names exact (`_userManager`, `_auditLog`, `_logger`) per line 27-72

    Insert 2 helper sebelum closing brace class `CMPController`:

    ```csharp
        // ============================================================
        // Phase 313 — Block Manual Submit Saat Waktu Habis (TMR-01)
        // 2-tier server-side timer enforcement helper (analog Phase 312 EnsureCanDeleteAsync)
        // ============================================================

        /// <summary>
        /// Enforces 2-tier timer rules pada SubmitExam:
        ///  - Tier 1 (Phase 313 NEW, D-09): manual submit (!isAutoSubmit) reject tanpa grace bila elapsed > Duration+ExtraTime.
        ///  - Tier 2 (existing LIFE-03 preserved, D-06): auto submit reject bila elapsed > Duration+ExtraTime+2min grace.
        /// AssessmentType Manual / null di-skip (D-15 defense-in-depth).
        /// Returns IActionResult kalau reject (caller `return` value), atau null kalau pass (caller lanjut).
        /// </summary>
        private async Task<IActionResult?> EnsureCanSubmitExamAsync(
            AssessmentSession assessment,
            bool isAutoSubmit)
        {
            // D-15 / C-01: Manual type exclude via explicit field check (defense-in-depth).
            // Field di model adalah `AssessmentType` (Models/AssessmentSession.cs:154 verified).
            if (assessment.AssessmentType != AssessmentConstants.AssessmentType.Online &&
                assessment.AssessmentType != AssessmentConstants.AssessmentType.PreTest &&
                assessment.AssessmentType != AssessmentConstants.AssessmentType.PostTest)
            {
                return null; // Manual / null AssessmentType — skip guard
            }

            // Legacy session: StartedAt null (sessions sebelum Phase 21) — skip check (existing convention preserved).
            if (!assessment.StartedAt.HasValue) return null;

            var elapsed = DateTime.UtcNow - assessment.StartedAt.Value;
            int allowedMinutes = assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0);
            int graceLimitMinutes = allowedMinutes + 2; // 2-minute grace untuk network latency (existing tier-2)

            // Tier 1 (Phase 313 NEW): manual reject tanpa grace (D-09 strict 0-grace).
            if (!isAutoSubmit && elapsed.TotalMinutes > allowedMinutes)
            {
                await WriteSubmitBlockedAuditAsync(assessment, elapsed, allowedMinutes);
                // D-01 explanatory message — Bahasa Indonesia per CLAUDE.md.
                TempData["Error"] = "Waktu ujian Anda sudah habis. Sistem akan otomatis mengirim jawaban Anda dalam beberapa detik. Mohon tunggu, jangan refresh halaman.";
                return RedirectToAction("StartExam", new { id = assessment.Id });
            }

            // Tier 2 (existing LIFE-03 preserved): auto reject setelah grace.
            // D-06: TIDAK tulis AuditLog Blocked entry untuk Tier 2 (scope minimal, hanya tier-1 yang baru).
            if (elapsed.TotalMinutes > graceLimitMinutes)
            {
                TempData["Error"] = "Waktu ujian Anda telah habis. Pengiriman jawaban tidak dapat diproses.";
                return RedirectToAction("StartExam", new { id = assessment.Id });
            }

            return null; // Pass — caller lanjut grading flow.
        }

        /// <summary>
        /// Tulis AuditLog `SubmitExamBlocked` entry (D-05) untuk Tier-1 manual reject.
        /// Try/catch swallow — audit failure jangan block primary action (Phase 312 T-306-02 precedent).
        /// </summary>
        private async Task WriteSubmitBlockedAuditAsync(
            AssessmentSession assessment,
            TimeSpan elapsed,
            int allowedMinutes)
        {
            try
            {
                var blockUser = await _userManager.GetUserAsync(User);
                // Actor name pattern: NIP - FullName (Phase 312 EnsureCanDeleteAsync line 5567-5570 reference).
                var blockActor = string.IsNullOrWhiteSpace(blockUser?.NIP)
                    ? (blockUser?.FullName ?? "Unknown")
                    : $"{blockUser.NIP} - {blockUser.FullName}";

                // D-05 Description format EXACT — parsing-friendly key=value:
                // "HC/User role manual submit blocked after timeup. Type={...} ElapsedMin={X} AllowedMin={Y} SessionId={id}"
                var description =
                    $"HC/User role manual submit blocked after timeup. " +
                    $"Type={assessment.AssessmentType} " +
                    $"ElapsedMin={(int)elapsed.TotalMinutes} " +
                    $"AllowedMin={allowedMinutes} " +
                    $"SessionId={assessment.Id}";

                await _auditLog.LogAsync(
                    actorUserId: blockUser?.Id ?? "",
                    actorName: blockActor,
                    actionType: "SubmitExamBlocked", // D-05 — Phase 312 {Action}Blocked convention
                    description: description,
                    targetId: assessment.Id,
                    targetType: "AssessmentSession");
            }
            catch (Exception auditEx)
            {
                // Swallow — audit failure tidak boleh block primary action (Phase 312 T-306-02 precedent).
                _logger.LogWarning(auditEx,
                    "AuditLog SubmitExamBlocked write failed for SessionId={SessionId}",
                    assessment.Id);
            }
        }
    ```

    NB importante:
    - Cek namespace `AssessmentConstants` di `Models/AssessmentConstants.cs`. Kalau namespace `HcPortal.Models` (atau equivalent) belum di `using` directives top file, tambahkan.
    - JANGAN ubah signature `SubmitExam` (line 1554-1556) — preserved.
    - JANGAN ubah constructor — semua DI fields sudah tersedia.
  </action>

  <verify>
    <automated>dotnet build --nologo --verbosity minimal 2>&1 | tail -20 && grep -c "EnsureCanSubmitExamAsync" Controllers/CMPController.cs && grep -c "WriteSubmitBlockedAuditAsync" Controllers/CMPController.cs</automated>
  </verify>

  <acceptance_criteria>
    - `Controllers/CMPController.cs` berisi `private async Task<IActionResult?> EnsureCanSubmitExamAsync(`
    - `Controllers/CMPController.cs` berisi `private async Task WriteSubmitBlockedAuditAsync(`
    - `Controllers/CMPController.cs` berisi `AssessmentConstants.AssessmentType.Online` (constant usage, NOT magic string `"Online"` di guard)
    - `Controllers/CMPController.cs` berisi `AssessmentConstants.AssessmentType.PreTest`
    - `Controllers/CMPController.cs` berisi `AssessmentConstants.AssessmentType.PostTest`
    - `Controllers/CMPController.cs` berisi exact string `"SubmitExamBlocked"` (ActionType D-05)
    - `Controllers/CMPController.cs` berisi exact substring `HC/User role manual submit blocked after timeup. Type=` (Description format D-05)
    - `Controllers/CMPController.cs` berisi exact substring `Waktu ujian Anda sudah habis. Sistem akan otomatis mengirim jawaban Anda dalam beberapa detik. Mohon tunggu, jangan refresh halaman.` (D-01 verbatim)
    - `Controllers/CMPController.cs` berisi `_logger.LogWarning(auditEx,` (try/catch swallow pattern)
    - `Controllers/CMPController.cs` TIDAK berisi `assessment.Type ==` (anti-pattern C-01 — should be `AssessmentType`)
    - `Controllers/CMPController.cs` TIDAK berisi `BeginTransactionAsync` di area helper (intentional deviation — no transaction wrap)
    - `dotnet build --nologo --verbosity minimal` exit 0 (CS0103/CS1061/CS0246 errors absent)
    - Grep `EnsureCanSubmitExamAsync` count == 1 (declaration only — call site di Task 2)
    - Grep `WriteSubmitBlockedAuditAsync` count == 2 (declaration + 1 call from EnsureCanSubmitExamAsync Tier-1 branch)
  </acceptance_criteria>

  <done>
    2 helper privat `EnsureCanSubmitExamAsync` + `WriteSubmitBlockedAuditAsync` ada di akhir class `CMPController`. `dotnet build` sukses (0 errors). Helpers belum di-call dari SubmitExam — itu di Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace existing LIFE-03 block (line 1616-1628) di SubmitExam dengan EnsureCanSubmitExamAsync invocation</name>

  <files>Controllers/CMPController.cs</files>

  <read_first>
    - Controllers/CMPController.cs:1554-1640 (SubmitExam method — full context: signature, ownership check, serverTimerExpired calc, LIFE-03 block, packageAssignment query)
    - Controllers/CMPController.cs (Task 1 output — verify EnsureCanSubmitExamAsync helper sudah ada)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md (section 1.B — replacement skeleton)
  </read_first>

  <behavior>
    - Replace existing LIFE-03 block (line 1616-1628 — `if (assessment.StartedAt.HasValue) { ... }` 13 lines) dengan helper invocation (4-5 lines)
    - Position invocation: SETELAH `serverTimerExpired` calculation (line 1580-1614 area, preserve unchanged) dan SEBELUM `packageAssignment` query (line ~1631 area, preserve unchanged)
    - Comment header preserved (LIFE-03 + Phase 313 context)
    - Existing tier-2 message variable + return preserved di dalam helper (Task 1 output) — tidak di-duplicate di SubmitExam
    - 4-line invocation:
      ```csharp
      var timerBlockResult = await EnsureCanSubmitExamAsync(assessment, isAutoSubmit);
      if (timerBlockResult != null) return timerBlockResult;
      ```
  </behavior>

  <action>
    Read `Controllers/CMPController.cs` line 1554-1640 untuk konfirmasi exact context. Identify line range untuk LIFE-03 existing block (line 1616-1628 area) — confirm via grep substring `Server-side timer enforcement (LIFE-03)`.

    Replace block (current line 1616-1628):
    ```csharp
    // ---- Server-side timer enforcement (LIFE-03) ----
    // Grace period: 2 minutes to account for network latency and slow connections.
    // Skip check if StartedAt is null (legacy sessions that existed before Phase 21).
    if (assessment.StartedAt.HasValue)
    {
        var elapsed = DateTime.UtcNow - assessment.StartedAt.Value;
        int allowedMinutes = assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0) + 2; // 2-minute grace
        if (elapsed.TotalMinutes > allowedMinutes)
        {
            TempData["Error"] = "Waktu ujian Anda telah habis. Pengiriman jawaban tidak dapat diproses.";
            return RedirectToAction("StartExam", new { id });
        }
    }
    ```

    Dengan:
    ```csharp
    // ---- Server-side timer enforcement (LIFE-03 + Phase 313 2-tier TMR-01) ----
    // Phase 313: 2-tier branching — manual reject tanpa grace (D-09), auto reject setelah grace (existing).
    // Helper extraction mirror Phase 312 EnsureCanDeleteAsync pattern (D-04 lock, body-method placement).
    // AssessmentType Manual exclude di-handle dalam helper (D-15 defense-in-depth).
    var timerBlockResult = await EnsureCanSubmitExamAsync(assessment, isAutoSubmit);
    if (timerBlockResult != null) return timerBlockResult;
    ```

    NB:
    - Posisi exact: SETELAH blok `serverTimerExpired` (yang compute boolean ViewBag — preserve unchanged), SEBELUM `packageAssignment = await _context.UserPackageAssignments.FirstOrDefaultAsync(...)` query.
    - Indentation match existing (4 spaces inside method body).
    - `id` parameter sudah tersedia di scope SubmitExam (line 1556 signature).
    - JANGAN modify line lain (serverTimerExpired logic, ownership check, etc.) — out of scope.
  </action>

  <verify>
    <automated>dotnet build --nologo --verbosity minimal 2>&1 | tail -20 && grep -c "EnsureCanSubmitExamAsync" Controllers/CMPController.cs && grep -c "Server-side timer enforcement (LIFE-03 + Phase 313 2-tier TMR-01)" Controllers/CMPController.cs</automated>
  </verify>

  <acceptance_criteria>
    - `Controllers/CMPController.cs` berisi `var timerBlockResult = await EnsureCanSubmitExamAsync(assessment, isAutoSubmit);`
    - `Controllers/CMPController.cs` berisi `if (timerBlockResult != null) return timerBlockResult;`
    - Grep `EnsureCanSubmitExamAsync` count == 2 (1 declaration di Task 1 helper + 1 call site di SubmitExam)
    - `Controllers/CMPController.cs` berisi comment substring `Phase 313 2-tier TMR-01` (anchor untuk traceability)
    - `Controllers/CMPController.cs` TIDAK lagi berisi original LIFE-03 inline block: `int allowedMinutes = assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0) + 2; // 2-minute grace` (single-tier replaced)
    - `Controllers/CMPController.cs` TIDAK berisi duplicate `TempData["Error"] = "Waktu ujian Anda telah habis. Pengiriman jawaban tidak dapat diproses.";` di SubmitExam method body (sekarang ada di helper Task 1 only — grep count 1 untuk file)
    - `dotnet build --nologo --verbosity minimal` exit 0
    - Grep `Server-side timer enforcement (LIFE-03)` di SubmitExam method context — comment header updated atau replaced (verify via grep `LIFE-03 + Phase 313 2-tier`)
  </acceptance_criteria>

  <done>
    SubmitExam (line ~1616 area) sekarang panggil `EnsureCanSubmitExamAsync` helper. `dotnet build` sukses. SubmitExam method shorter (~9 lines removed, 5 lines added). Backend TMR-01 enforcement complete — siap untuk frontend Plan 03 + UAT verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker browser → CMPController.SubmitExam (POST) | Untrusted form input crosses here. `isAutoSubmit` field controllable via DevTools. |
| EnsureCanSubmitExamAsync → AuditLogService.LogAsync | Internal C# call — trusted. Description string contains user-controlled `assessment.Id` (int — type-safe). |
| AuditLogService → AuditLogs DB table | Trusted internal write. SQL injection N/A (parameterized via EF Core / DbContext). |
| WriteSubmitBlockedAuditAsync → _logger.LogWarning | Internal exception swallow — no user data leak via log (only SessionId int). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313-01 | Tampering / Elevation | Manual submit bypass via `isAutoSubmit=false` after timeup | mitigate | **PRIMARY MITIGATION (this plan):** Tier-1 strict 0-grace reject di `EnsureCanSubmitExamAsync` line `if (!isAutoSubmit && elapsed.TotalMinutes > allowedMinutes) { ... return RedirectToAction(...); }`. Server adalah source-of-truth `DateTime.UtcNow`, tidak rely pada client clock. |
| T-313-02 | Tampering / Elevation | DevTools force `isAutoSubmit=true` to skip Tier-1 | mitigate | Tier-2 enforces hard cap `Duration+ExtraTime+2min` regardless of `isAutoSubmit` flag (`if (elapsed.TotalMinutes > graceLimitMinutes) reject`). Defense-in-depth dengan 2-tier guard. |
| T-313-03 | Repudiation | Audit gap untuk blocked attempts (tidak ada traceability) | mitigate | AuditLog `SubmitExamBlocked` entry per Tier-1 reject dengan `{Type, ElapsedMin, AllowedMin, SessionId, ActorUserId}` di Description (D-05). TargetId + TargetType set untuk indexed query. |
| T-313-04 | Denial of Service / Bypass | AuditLog DB exception block primary action | mitigate | Try/catch swallow di `WriteSubmitBlockedAuditAsync` dengan `_logger.LogWarning` fallback (Phase 312 T-306-02 precedent). Audit failure → primary reject still proceeds. |
| T-313-05 | Tampering | TOCTOU race (multi-tab parallel submit) | accept | Phase 313 guard read-only check + audit write only — no state mutation di guard itself. Multiple Blocked entries dari concurrent attempts = informational noise (acceptable per RESEARCH.md Pitfall 6). Existing GradingService idempotency cover write path (Phase 309 precedent). |
| T-313-06 | Elevation of Privilege | Manual type assessment incorrectly blocked | mitigate | D-15 defense-in-depth: `assessment.AssessmentType != AssessmentConstants.AssessmentType.Online && != PreTest && != PostTest → return null` (skip guard). Manual type bypass entirely. Constants from `Models/AssessmentConstants.cs:5-11` (anti-magic-string). |
| T-313-07 | Information Disclosure | TempData["Error"] message exposes internal state | accept | D-01 message generic — no SessionId, no UserId, no internal numeric values. Safe for user display. |
| T-313-08 | Tampering | CSRF replay POST `/CMP/SubmitExam` via stale form | mitigate | `[ValidateAntiForgeryToken]` attribute already applied (line 1555 — preserved unchanged). |
</threat_model>

<verification>
- [ ] Task 1: 2 helper privat ada di akhir CMPController class
- [ ] Task 1: AssessmentConstants constants used (no magic string)
- [ ] Task 1: D-01 + D-05 EXACT strings present
- [ ] Task 1: Try/catch swallow pattern present
- [ ] Task 2: Existing LIFE-03 inline block replaced dengan helper invocation
- [ ] Task 2: Comment trail updated (LIFE-03 + Phase 313 2-tier TMR-01)
- [ ] **Aggregate:** `dotnet build --nologo` exit 0, no warnings baru di CMPController.cs
- [ ] **Aggregate:** `grep -c "EnsureCanSubmitExamAsync" Controllers/CMPController.cs` = 2 (declaration + call)
- [ ] **Aggregate:** `grep -c "WriteSubmitBlockedAuditAsync" Controllers/CMPController.cs` = 2 (declaration + 1 call dari helper)
- [ ] **Aggregate:** `grep -c "SubmitExamBlocked" Controllers/CMPController.cs` = 1 (single ActionType literal)
</verification>

<success_criteria>
**Phase SC mapping (per ROADMAP):**
- SC #1: Modify LIFE-03 block jadi 2-tier branching `isAutoSubmit` ✓ (Task 2 invocation + Task 1 helper body 2-tier branches)
- SC #2: Tier 1 `!isAutoSubmit && elapsed > allowed` reject manual + TempData + redirect StartExam ✓ (Task 1 helper Tier-1 branch)
- SC #3: Tier 2 `elapsed > allowed + 2min grace` reject auto-submit (existing preserved) ✓ (Task 1 helper Tier-2 branch + D-06 message preserved)
- SC #5: AuditLog entry `SubmitExamBlocked` dengan `{UserId, SessionId, ElapsedMin, AllowedMin}` ✓ (Task 1 WriteSubmitBlockedAuditAsync + targetId/targetType)
- SC #6: Verifikasi 3 tipe ber-timer Online/PreTest/PostTest, Manual exclude ✓ (Task 1 helper AssessmentType field check using constants)

**Plan-level SC (executor close criteria):**
1. `dotnet build` exit 0
2. 2 helper privat ada di CMPController class
3. SubmitExam call site refactored — single-tier inline block replaced dengan helper invocation
4. AssessmentConstants used (anti-magic-string per C-01)
5. D-01, D-05 string literals match verbatim
6. Try/catch swallow pattern present (T-313-04 mitigation)
7. NO transaction wrap di helper (intentional deviation per RESEARCH.md)

**Out of scope (Plan 03 / UAT closure):**
- Frontend disable button + retry handler → Plan 03
- Modal text adjustment + auto-submit timing → Plan 03
- DB seed run + Playwright FLOW 313 GREEN transition → Plan 01 + UAT
- AuditLog spot-check via SSMS → 313-UAT.md (Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-02-SUMMARY.md` mencakup:
- Task 1-2 outcomes (line ranges modified, exact strings inserted)
- `dotnet build` final output (errors=0, warnings count)
- D-01, D-05 verbatim string verification (grep results)
- C-01 compliance: zero `assessment.Type` occurrence (only `assessment.AssessmentType`)
- D-15 compliance: AssessmentConstants used in guard
- Threat T-313-01..T-313-04 mitigation status
- Note untuk Plan 03 executor: backend ready, frontend Razor + JS modifications next
- Note untuk UAT operator: jalankan `.planning/seeds/313-timer-fixtures.sql` + FLOW 313 untuk verify GREEN transition
</output>
