---
phase: 313-block-manual-submit-saat-waktu-habis
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - Views/CMP/ExamSummary.cshtml
  - Views/CMP/StartExam.cshtml
autonomous: true
requirements: [TMR-01]
must_haves:
  truths:
    - "Saat `ViewBag.TimerExpired == true` di ExamSummary, tombol Submit utama di-render disabled greyed-out dengan label `Waktu Habis - Submit Otomatis Berjalan...` + spinner (D-03 / C-02)"
    - "Saat countdown timer di StartExam reach 0, modal `timeUpWarningModal` muncul info-only (no OK button, spinner indicator) DAN form `examForm` submit fire PARALEL langsung tanpa setTimeout(10000) delay (C-03)"
    - "Saat ExamSummary auto-submit POST gagal (network 5xx/timeout), JS retry 3x exponential backoff (1s, 2s, 4s) dengan logging console (D-10)"
    - "Setelah retry 3x exhausted, banner permanent muncul dengan pesan D-11 'Submit gagal karena masalah jaringan...'"
    - "Modal `timeUpWarningModal` tetap visible during POST → ExamSummary chain (data-bs-backdrop=static + keyboard=false preserved)"
  artifacts:
    - path: "Views/CMP/ExamSummary.cshtml"
      provides: "Submit button conditional render (timerExpired branch disabled+spinner) + @section Scripts retry handler"
      contains: "manualSubmitDisabledBtn"
      min_lines: 30
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Modal timeUpWarningModal modified (no OK button, spinner indicator) + JS timer flow modified (no setTimeout 10s, fire paralel)"
      contains: "Phase 313 C-03"
      min_lines: 10
  key_links:
    - from: "Views/CMP/ExamSummary.cshtml `@if (timerExpired)` branch"
      to: "ViewBag.TimerExpired (set by ExamSummary action server-side)"
      via: "Razor conditional render"
      pattern: "@if \\(timerExpired\\)"
    - from: "Views/CMP/ExamSummary.cshtml @section Scripts retry handler"
      to: "form.action (POST /CMP/SubmitExam endpoint via DOM property)"
      via: "fetch(form.action, { method: 'POST', body: fd })"
      pattern: "fetch\\(form\\.action"
    - from: "Views/CMP/StartExam.cshtml updateTimer() countdown=0 handler"
      to: "examForm.submit() (POST → ExamSummary action)"
      via: "modal show + immediate form submit (no setTimeout)"
      pattern: "examForm.*submit\\(\\)"
user_setup: []
---

<objective>
Frontend implementation — modify `Views/CMP/ExamSummary.cshtml` (Submit button conditional + retry JS) dan `Views/CMP/StartExam.cshtml` (modal text + timer flow). Defense-in-depth dengan backend Plan 02 — UI prevent klik manual submit setelah timeup, JS handle network resilience untuk auto-submit.

Purpose: Close TMR-01 user-facing UX:
- D-03 / C-02: Tombol Submit utama di **ExamSummary.cshtml** (BUKAN StartExam.cshtml — Pitfall 2 RESEARCH.md) disabled saat `timerExpired=true`. Existing `unanswered>0` branch preserved.
- C-03: Modal `timeUpWarningModal` tetap muncul untuk awareness, **TAPI** hapus button "OK — Kirim Jawaban" → ganti spinner indicator. JS hapus `setTimeout(10000)` → fire `examForm.submit()` immediate paralel.
- D-10: Auto-submit retry 3x exponential backoff [1s, 2s, 4s] via fetch chain di ExamSummary.cshtml @section Scripts.
- D-11: Banner permanent fallback kalau retry exhausted.

Output:
- `Views/CMP/ExamSummary.cshtml`: button conditional refine + @section Scripts new retry handler
- `Views/CMP/StartExam.cshtml`: modal markup modify + JS countdown flow modify
- `dotnet build` exit 0 (Razor compile gate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-RESEARCH.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-01-PLAN.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-02-PLAN.md
@Views/CMP/ExamSummary.cshtml
@Views/CMP/StartExam.cshtml
@Views/Shared/_Layout.cshtml
@CLAUDE.md

<interfaces>
<!-- Critical contracts — executor pakai langsung, no codebase scavenger hunt -->

From Views/CMP/ExamSummary.cshtml:107-145 [VERIFIED — TARGET REFINE area]:
```razor
<form asp-action="SubmitExam" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" name="id" value="@assessmentId" />
    <input type="hidden" name="isAutoSubmit" value="@(timerExpired ? "true" : "false")" id="autoSubmitFlag" />
    @foreach (var kvp in answers)
    {
        <input type="hidden" name="answers[@kvp.Key]" value="@kvp.Value" />
    }

    <div class="d-flex justify-content-between align-items-center">
        @if (timerExpired) { <span class="text-danger">Waktu habis...</span> }
        else { <a asp-action="StartExam">Kembali ke Ujian</a> }

        @if (unanswered > 0 && !timerExpired)
        {
            <button type="button" class="btn btn-success btn-lg fw-bold" disabled
                    title="Jawab semua soal terlebih dahulu">
                <i class="bi bi-check-circle-fill me-2"></i>Kumpulkan Ujian
            </button>
        }
        else
        {
            <button type="submit" class="btn btn-success btn-lg fw-bold"
                    onclick="return confirm('Kumpulkan ujian? Tindakan ini tidak dapat dibatalkan.')">
                <i class="bi bi-check-circle-fill me-2"></i>Kumpulkan Ujian
            </button>
        }
    </div>
</form>
```

From Views/CMP/StartExam.cshtml:376-391 [VERIFIED — TARGET MODIFY modal]:
```razor
<div class="modal fade" id="timeUpWarningModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-hidden="true"
     style="z-index: 9999;">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-danger">
            <div class="modal-body text-center p-4">
                <i class="bi bi-clock-history text-danger" style="font-size:3rem"></i>
                <h5 class="mt-3 fw-bold text-danger">Waktu Habis!</h5>
                <p class="text-muted">Waktu pengerjaan ujian telah habis. Jawaban Anda akan dikirimkan secara otomatis.</p>
                <button type="button" class="btn btn-danger px-4 fw-bold" id="timeUpOkBtn">
                    <i class="bi bi-send me-1"></i>OK — Kirim Jawaban
                </button>
            </div>
        </div>
    </div>
</div>
```

From Views/CMP/StartExam.cshtml:462-478 [VERIFIED — TARGET MODIFY JS timer flow]:
```javascript
if (remaining <= 0) {
    clearInterval(timerInterval);
    clearInterval(saveInterval);
    window.onbeforeunload = null;
    if (!submitted) {
        var timeupModal = new bootstrap.Modal(document.getElementById('timeUpWarningModal'));
        timeupModal.show();
        // Auto-submit setelah 10 detik jika user tidak klik OK
        setTimeout(function() {
            if (!submitted) {
                submitted = true;
                document.getElementById('examForm').submit();
            }
        }, 10000);
    }
}
```

From Views/CMP/StartExam.cshtml:71 [VERIFIED]:
```razor
<form id="examForm" asp-action="ExamSummary" asp-route-id="@Model.Id" method="post">
```
NB: examForm POST ke `ExamSummary` (review page), BUKAN langsung SubmitExam. ExamSummary action menerima `isAutoSubmit` dari ViewBag.TimerExpired calculated server-side, render hidden field tied (line 110), lalu Submit button user (atau JS auto-fire dari Plan 03 retry handler) POST ke SubmitExam.

From Views/Shared/_Layout.cshtml:199-208 [VERIFIED]:
```razor
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show">
        <strong>Error:</strong> @TempData["Error"]
        ...
    </div>
}
```
NB: TempData["Error"] auto-render global oleh layout — Pitfall 3 mitigated, tidak perlu add `@if` block manual.

From .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md anti-pattern reminders:
- C-02 (HIGH): Submit utama di ExamSummary.cshtml, NOT StartExam.cshtml
- C-03 (HIGH): Hapus setTimeout(10000), fire submit paralel
- WR-02 (MEDIUM): Path-prefix bug — pakai `form.action` DOM property atau `data-url` via `@Url.Action`. JANGAN hardcode `/CMP/SubmitExam`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refine ExamSummary.cshtml Submit button conditional + add retry JS @section Scripts</name>

  <files>Views/CMP/ExamSummary.cshtml</files>

  <read_first>
    - Views/CMP/ExamSummary.cshtml (full file — confirm existing structure, ViewBag references, end of file location, existing @section Scripts presence)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md (section 2 line 204-368 — exact replacement skeleton)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md (D-03, D-10, D-11, D-13, C-02 + C-03)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-02-PLAN.md (backend semantics — `isAutoSubmit` hidden field already tied to timerExpired ViewBag, preserved)
  </read_first>

  <behavior>
    - Refine button conditional: split current 2-branch (`unanswered>0 && !timerExpired` / `else`) jadi 3-branch (`timerExpired` / `unanswered>0` / `else submit`)
    - `timerExpired==true` branch: button disabled greyed-out + spinner + label `Waktu Habis - Submit Otomatis Berjalan...` + tooltip
    - `unanswered>0` branch (when !timerExpired): preserved existing
    - else (happy path): preserved existing submit + confirm
    - Hidden field `isAutoSubmit` (line 110) PRESERVED unchanged — backend Plan 02 expects this binding
    - @section Scripts NEW (file currently has no @section Scripts — verify): retry handler fetch chain D-10 + showRetryFailBanner D-11
    - Retry handler: only auto-fire kalau `timerExpired == true` (happy path tetap pakai native form submit + confirm)
    - Optional: D-13 banner info di top form kalau timerExpired (Claude discretion — implementer pilih untuk include)
  </behavior>

  <action>
    Buka `Views/CMP/ExamSummary.cshtml` full read. Konfirmasi:
    - Line range existing form (~107-145)
    - `timerExpired` variable atau ViewBag reference (existing — JANGAN reinvent, gunakan yang ada)
    - Existing `@section Scripts` block (jika ada — append ke dalam, bukan duplicate)
    - End-of-file marker

    ## A. Replace 3-branch button conditional (line ~117-143 area):

    Current pattern (verify via Read):
    ```razor
    @if (unanswered > 0 && !timerExpired)
    {
        <button type="button" class="btn btn-success btn-lg fw-bold" disabled
                title="Jawab semua soal terlebih dahulu">
            <i class="bi bi-check-circle-fill me-2"></i>Kumpulkan Ujian
        </button>
    }
    else
    {
        <button type="submit" class="btn btn-success btn-lg fw-bold"
                onclick="return confirm('Kumpulkan ujian? Tindakan ini tidak dapat dibatalkan.')">
            <i class="bi bi-check-circle-fill me-2"></i>Kumpulkan Ujian
        </button>
    }
    ```

    Replace dengan 3-branch:
    ```razor
    @* Phase 313 D-03 / C-02: 3-branch button — timerExpired (disabled + spinner), unanswered (disabled), else submit *@
    @if (timerExpired)
    {
        <button type="button" class="btn btn-secondary btn-lg fw-bold" disabled
                id="manualSubmitDisabledBtn"
                title="Auto-submit sedang berjalan, mohon tunggu">
            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
            Waktu Habis - Submit Otomatis Berjalan...
        </button>
    }
    else if (unanswered > 0)
    {
        <button type="button" class="btn btn-success btn-lg fw-bold" disabled
                title="Jawab semua soal terlebih dahulu">
            <i class="bi bi-check-circle-fill me-2"></i>Kumpulkan Ujian
        </button>
    }
    else
    {
        <button type="submit" class="btn btn-success btn-lg fw-bold"
                onclick="return confirm('Kumpulkan ujian? Tindakan ini tidak dapat dibatalkan.')">
            <i class="bi bi-check-circle-fill me-2"></i>Kumpulkan Ujian
        </button>
    }
    ```

    ## B. (Optional D-13 — Claude discretion) Add banner info di atas form kalau timerExpired:

    Insert sebelum `<form asp-action="SubmitExam" method="post">` (line ~107):
    ```razor
    @if (timerExpired)
    {
        @* D-13: Banner info untuk reload-after-timeup awareness *@
        <div class="alert alert-info" role="alert">
            <i class="bi bi-info-circle me-2"></i>
            Waktu ujian sudah habis. Submit otomatis sudah/sedang berjalan.
        </div>
    }
    ```

    ## C. Add (atau append ke existing) `@section Scripts { ... }` di akhir file:

    Cek end-of-file. Kalau `@section Scripts` belum ada di file ini, tambah blok baru. Kalau sudah ada (jarang di view-only), append script tag dalam.

    ```razor
    @section Scripts {
    <script>
        (function () {
            // Phase 313 D-10: auto-submit retry 3x exponential backoff [1s, 2s, 4s].
            // Hanya aktif kalau timerExpired (form auto-fired dari StartExam.cshtml redirect chain).
            var timerExpired = @Json.Serialize((bool?)(ViewBag.TimerExpired) ?? false);
            if (!timerExpired) return; // Happy path — pakai native form submit + confirm.

            var form = document.querySelector('form[action$="/SubmitExam"]');
            if (!form) return;

            var maxAttempts = 3;
            var delays = [1000, 2000, 4000]; // D-10 exponential backoff
            var attempt = 0;

            function attemptSubmit() {
                attempt++;
                var fd = new FormData(form);
                return fetch(form.action, { method: 'POST', body: fd, redirect: 'follow' })
                    .then(function (r) {
                        if (r.redirected) { window.location.href = r.url; return; }
                        if (!r.ok) throw new Error('HTTP ' + r.status);
                        // Server returned non-redirect 2xx — fallback: reload to current URL (server akan re-render dengan TempData jika ada).
                        window.location.reload();
                    })
                    .catch(function (err) {
                        console.error('[Phase 313] Submit attempt ' + attempt + ' failed', err);
                        if (attempt < maxAttempts) {
                            setTimeout(attemptSubmit, delays[attempt - 1]);
                        } else {
                            showRetryFailBanner(); // D-11 banner permanent
                        }
                    });
            }

            function showRetryFailBanner() {
                // D-11: banner permanent — server-side last-resort save deferred ke v16.0+
                var container = document.querySelector('.container.py-4') || document.querySelector('main') || document.body;
                if (!container) return;
                var banner = document.createElement('div');
                banner.className = 'alert alert-warning fw-semibold';
                banner.setAttribute('role', 'alert');
                banner.innerHTML =
                    '<i class="bi bi-exclamation-triangle-fill me-2"></i>' +
                    'Submit gagal karena masalah jaringan. Hubungi admin. ' +
                    'Jawaban Anda tersimpan di tab ini, jangan tutup browser.';
                container.prepend(banner);
            }

            // Auto-fire on page-ready kalau timerExpired (chain dari StartExam timer-up flow).
            if (document.readyState === 'complete') {
                attemptSubmit();
            } else {
                window.addEventListener('load', attemptSubmit);
            }
        })();
    </script>
    }
    ```

    NB:
    - WR-02 mitigation: pakai `form.action` (DOM property — auto-prefix base path safe). JANGAN hardcode `/CMP/SubmitExam` atau pakai relative path tanpa base.
    - `form[action$="/SubmitExam"]` selector pakai suffix-match — robust terhadap base path prefix.
    - `submitted` state tidak perlu — `attemptSubmit` self-guard via `attempt` counter + `if (attempt < maxAttempts)` exit branch.
    - Bahasa user-facing string Bahasa Indonesia (per CLAUDE.md). Comment + log message English boleh.
  </action>

  <verify>
    <automated>dotnet build --nologo --verbosity minimal 2>&1 | tail -20 && grep -c "manualSubmitDisabledBtn" Views/CMP/ExamSummary.cshtml; grep -c "Waktu Habis - Submit Otomatis Berjalan" Views/CMP/ExamSummary.cshtml; grep -c "Submit gagal karena masalah jaringan" Views/CMP/ExamSummary.cshtml</automated>
  </verify>

  <acceptance_criteria>
    - `Views/CMP/ExamSummary.cshtml` berisi `id="manualSubmitDisabledBtn"`
    - `Views/CMP/ExamSummary.cshtml` berisi exact text `Waktu Habis - Submit Otomatis Berjalan...`
    - `Views/CMP/ExamSummary.cshtml` berisi `spinner-border spinner-border-sm me-2` (spinner indicator)
    - `Views/CMP/ExamSummary.cshtml` berisi `@if (timerExpired)` (3-branch conditional first branch)
    - `Views/CMP/ExamSummary.cshtml` berisi `else if (unanswered > 0)` (3-branch second branch — note: NO `&& !timerExpired` lagi, sudah handled di first branch)
    - `Views/CMP/ExamSummary.cshtml` berisi `@section Scripts {`
    - `Views/CMP/ExamSummary.cshtml` berisi `fetch(form.action,` (D-10 retry pattern, no hardcoded URL — WR-02 mitigation)
    - `Views/CMP/ExamSummary.cshtml` berisi `delays = [1000, 2000, 4000]` (exponential backoff)
    - `Views/CMP/ExamSummary.cshtml` berisi exact text `Submit gagal karena masalah jaringan. Hubungi admin. Jawaban Anda tersimpan di tab ini, jangan tutup browser.` (D-11 verbatim)
    - `Views/CMP/ExamSummary.cshtml` berisi `[Phase 313]` log marker (console.error prefix)
    - `Views/CMP/ExamSummary.cshtml` TIDAK berisi `'/CMP/SubmitExam'` literal hardcode (WR-02 anti-pattern)
    - Hidden field `<input type="hidden" name="isAutoSubmit" value="@(timerExpired ? "true" : "false")"` PRESERVED (grep confirms still present)
    - `dotnet build --nologo --verbosity minimal` exit 0 (Razor compile gate)
  </acceptance_criteria>

  <done>
    `Views/CMP/ExamSummary.cshtml` punya 3-branch button conditional (timerExpired/unanswered/else), retry handler @section Scripts, banner D-11 fallback, dotnet build sukses. Task 2 (StartExam.cshtml modal + JS timer) next.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modify StartExam.cshtml — modal text (no OK button, spinner) + JS timer flow (no setTimeout 10s)</name>

  <files>Views/CMP/StartExam.cshtml</files>

  <read_first>
    - Views/CMP/StartExam.cshtml (full file — verify modal location line 376-391, JS timer flow line 462-478, examForm declaration line 71)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md (section 3 line 371-491 — exact replacement skeletons + C-03 rules)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md (C-03 post-research correction VERBATIM)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-RESEARCH.md (Pitfall 7 — visibilitychange listener line 487 PRESERVED unchanged)
  </read_first>

  <behavior>
    - Modal `timeUpWarningModal` markup modify: hapus button "OK — Kirim Jawaban" (id="timeUpOkBtn"), ganti dengan spinner indicator + label "Mengirim..."
    - Modal text body update: ganti `Waktu pengerjaan ujian telah habis. Jawaban Anda akan dikirimkan secara otomatis.` -> `Jawaban Anda sedang dikirim otomatis. Mohon tunggu, jangan refresh halaman.`
    - `data-bs-backdrop="static"` + `data-bs-keyboard="false"` PRESERVED (user tidak bisa close modal)
    - JS timer flow (line 462-478): hapus `setTimeout(function() { ... }, 10000);` wrapper. Set `submitted = true` SEBELUM modal show, fire `examForm.submit()` immediate setelah modal show — no delay
    - Existing `submitted` flag pattern PRESERVED (guard double-submit via visibilitychange listener line 487-498 — JANGAN modify)
    - `clearInterval(timerInterval)` + `clearInterval(saveInterval)` + `window.onbeforeunload = null` PRESERVED
    - `timeUpOkBtn` event listener (jika ada di luar block ini) — verify dan hapus jika orphan (kemungkinan ada di line 480-490 area; cek via grep)
  </behavior>

  <action>
    Buka `Views/CMP/StartExam.cshtml` full read. Konfirmasi exact line ranges:
    - Modal markup: line 376-391 area (grep `timeUpWarningModal`)
    - JS timer flow: line 462-478 area (grep `if (remaining <= 0)`)
    - `timeUpOkBtn` references: grep semua occurrence (modal + event listener handler — keduanya perlu update)

    ## A. Replace modal markup (line ~376-391):

    Replace block `<div class="modal fade" id="timeUpWarningModal" ...>` ... `</div>` (full modal markup) dengan versi info-only:
    - Header: `<h5 class="mt-3 fw-bold text-danger">Waktu Habis!</h5>` (PRESERVED)
    - Body text: `<p class="text-muted mb-3">Jawaban Anda sedang dikirim otomatis. Mohon tunggu, jangan refresh halaman.</p>`
    - Replace button block dengan div spinner:
      ```razor
      <div class="d-flex justify-content-center align-items-center gap-2">
          <span class="spinner-border text-danger" role="status" aria-hidden="true"></span>
          <span class="fw-semibold text-danger">Mengirim...</span>
      </div>
      ```
    - Add comment header `@* Phase 313 C-03: modal info-only (tetap muncul untuk awareness, button OK diganti spinner indicator). *@`

    Full replacement skeleton sudah ada di `313-PATTERNS.md` section 3.A line 410-426 — copy verbatim.

    ## B. Replace JS timer flow (line ~462-478):

    Block target: `if (remaining <= 0) { ... }` di dalam `updateTimer()` (atau wherever existing).

    Replace dengan:
    ```javascript
    if (remaining <= 0) {
        clearInterval(timerInterval);
        clearInterval(saveInterval);
        window.onbeforeunload = null;
        // Phase 313 C-03: modal muncul (info awareness) + submit fire PARALEL langsung (no setTimeout 10s delay).
        if (!submitted) {
            submitted = true; // set flag SEBELUM modal show — prevent visibilitychange listener race re-trigger.
            var timeupModal = new bootstrap.Modal(document.getElementById('timeUpWarningModal'));
            timeupModal.show();
            // Fire submit immediate — modal tetap visible during POST -> ExamSummary chain.
            document.getElementById('examForm').submit();
        }
    }
    ```

    ## C. Cleanup orphan `timeUpOkBtn` event listener (jika ada):

    Grep file untuk semua occurrence `timeUpOkBtn`. Setelah modal markup di-replace, button ini tidak ada lagi. Kalau ada event listener `document.getElementById('timeUpOkBtn').addEventListener(...)` di JS section file (kemungkinan setelah block timer-up handler), hapus listener tersebut (dia tidak akan find element, throw error null reference saat page load).

    Search pattern: `timeUpOkBtn` — count harus jadi 0 setelah cleanup.

    NB:
    - JANGAN modify `examForm` declaration line 71 (action="ExamSummary" — backend depends on this routing)
    - JANGAN modify `visibilitychange` listener line 487-498 (Pitfall 7 — wall-clock anchor preserved)
    - JANGAN modify `submitted` flag declaration (line 441 area — preserved as before-block flag)
    - Bahasa Indonesia user-facing (h5 + p text), comment boleh English
  </action>

  <verify>
    <automated>dotnet build --nologo --verbosity minimal 2>&1 | tail -20 && grep -c "timeUpOkBtn" Views/CMP/StartExam.cshtml; grep -c "Mengirim" Views/CMP/StartExam.cshtml; grep -c "Phase 313 C-03" Views/CMP/StartExam.cshtml</automated>
  </verify>

  <acceptance_criteria>
    - `Views/CMP/StartExam.cshtml` berisi `Phase 313 C-03` (anchor untuk traceability)
    - `Views/CMP/StartExam.cshtml` berisi exact text `Jawaban Anda sedang dikirim otomatis. Mohon tunggu, jangan refresh halaman.` (modal body copy update)
    - `Views/CMP/StartExam.cshtml` berisi `Mengirim...` (spinner label)
    - `Views/CMP/StartExam.cshtml` berisi `<span class="spinner-border text-danger"` (spinner indicator)
    - `Views/CMP/StartExam.cshtml` TIDAK berisi `id="timeUpOkBtn"` (button removed) — grep count == 0
    - `Views/CMP/StartExam.cshtml` TIDAK berisi `OK — Kirim Jawaban` (button label removed) — grep count == 0
    - `Views/CMP/StartExam.cshtml` TIDAK berisi `}, 10000)` di dalam timer-up block (10s setTimeout removed) — grep `}, 10000\\)` count == 0 di file (atau di area sekitar block)
    - `Views/CMP/StartExam.cshtml` TIDAK berisi `Waktu pengerjaan ujian telah habis. Jawaban Anda akan dikirimkan secara otomatis.` (old modal copy replaced) — grep count == 0
    - `Views/CMP/StartExam.cshtml` berisi `data-bs-backdrop="static"` (preserved)
    - `Views/CMP/StartExam.cshtml` berisi `data-bs-keyboard="false"` (preserved)
    - `Views/CMP/StartExam.cshtml` berisi `submitted = true;` SEBELUM `timeupModal.show()` (set flag pre-modal — prevent race)
    - `Views/CMP/StartExam.cshtml` berisi `document.getElementById('examForm').submit();` (fire paralel — preserved at correct location)
    - `Views/CMP/StartExam.cshtml` berisi `clearInterval(timerInterval);` dan `clearInterval(saveInterval);` (preserved)
    - `dotnet build --nologo --verbosity minimal` exit 0
  </acceptance_criteria>

  <done>
    Modal `timeUpWarningModal` info-only (no OK button, spinner indicator). JS timer flow fire submit paralel langsung tanpa setTimeout(10000). visibilitychange listener + examForm declaration preserved. dotnet build sukses. Frontend C-03 user override honored. Phase 313 frontend complete — backend Plan 02 + frontend Plan 03 + tests Plan 01 ready for UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker browser DOM -> JS retry handler | Untrusted DOM state — mitigated dengan `form[action$="/SubmitExam"]` selector + `form.action` DOM property (no hardcode URL) |
| JS retry handler -> CMPController.SubmitExam (POST) | Repeated POSTs (3x backoff) — server tier-2 grace cap absorb retry latency, idempotency via existing `assessment.Status == "Completed"` check (line 1572 preserved unchanged) |
| Modal markup -> user keyboard/click | `data-bs-backdrop="static"` + `data-bs-keyboard="false"` prevent user dismiss — info-only display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313-09 | Tampering | DevTools `removeAttribute('disabled')` pada `manualSubmitDisabledBtn` untuk klik manual submit setelah timeup | mitigate | **Defense-in-depth dengan backend Plan 02:** Even if user remove disabled attribute + klik, server-side `EnsureCanSubmitExamAsync` Tier-1 reject manual after timeup. UI disable adalah primary deterrent, server adalah authoritative gate. |
| T-313-10 | Denial of Service | Network 5xx storm cause infinite retry | accept | Hard cap `maxAttempts = 3` + exponential backoff `[1s, 2s, 4s]` (total 7s recovery window). After exhausted -> `showRetryFailBanner` D-11. No infinite loop. |
| T-313-11 | Information Disclosure | console.error log leak via `[Phase 313] Submit attempt N failed` | accept | Console log generic — no PII, no SessionId, no UserId. Useful untuk DevTools telemetry tanpa expose internal state. |
| T-313-12 | Tampering | Hardcoded URL `/CMP/SubmitExam` di JS (path-prefix bug Phase 312 WR-02) | mitigate | Pakai `form.action` DOM property (auto-prefix base path safe) + `form[action$="/SubmitExam"]` suffix selector. JANGAN hardcode literal path. |
| T-313-13 | Repudiation | User refresh halaman ExamSummary saat retry in-progress -> submit hilang | accept | D-11 banner explicit instruct: "Jawaban Anda tersimpan di tab ini, jangan tutup browser." Behavioral mitigation. Server-side last-resort save deferred ke v16.0+ per CONTEXT.md `## Deferred Ideas`. |
| T-313-14 | Tampering | XSS via TempData["Error"] message | accept | Razor auto-escape via `@TempData["Error"]` di `_Layout.cshtml:202` (verified). D-01 message static literal Bahasa Indonesia, no user-controlled interpolation. |
| T-313-15 | Race | Modal show + form submit fire paralel race | mitigate | `submitted = true` set SEBELUM `timeupModal.show()` + `examForm.submit()` — prevent `visibilitychange` listener (line 487-498) re-trigger duplicate submit. |
</threat_model>

<verification>
- [ ] Task 1: ExamSummary.cshtml 3-branch button conditional present (timerExpired/unanswered/else)
- [ ] Task 1: Spinner + label `Waktu Habis - Submit Otomatis Berjalan...` present
- [ ] Task 1: @section Scripts retry handler (D-10 backoff + D-11 banner) present
- [ ] Task 1: No hardcoded `/CMP/SubmitExam` URL (WR-02 mitigation)
- [ ] Task 2: StartExam.cshtml modal markup: no OK button, spinner indicator present
- [ ] Task 2: JS timer flow: no setTimeout 10000, fire submit paralel
- [ ] Task 2: timeUpOkBtn occurrences = 0 (cleanup)
- [ ] Task 2: visibilitychange listener preserved (Pitfall 7)
- [ ] **Aggregate:** `dotnet build --nologo --verbosity minimal` exit 0 (no Razor compile errors)
- [ ] **Aggregate:** No new build warnings di ExamSummary.cshtml or StartExam.cshtml beyond Phase 312 baseline (92 warnings)
</verification>

<success_criteria>
**Phase SC mapping (per ROADMAP):**
- SC #4: Frontend `StartExam.cshtml`: countdown=0 disable Submit manual; auto-submit handler tetap aktif
  - Adaptasi C-02: Submit utama di **ExamSummary.cshtml** (Pitfall 2 mitigated). StartExam.cshtml: hanya modal + auto-fire flow. Kombinasi Task 1 + Task 2 satisfy SC #4.

**Plan-level SC (executor close criteria):**
1. `dotnet build` exit 0
2. ExamSummary.cshtml: 3-branch button (timerExpired/unanswered/else) + retry @section Scripts + D-11 banner pattern
3. StartExam.cshtml: modal info-only (no OK button) + fire submit paralel (no setTimeout 10s)
4. C-02 honored: Submit primary di ExamSummary (NOT StartExam)
5. C-03 honored: modal tetap muncul + submit paralel langsung
6. WR-02 mitigated: form.action DOM property usage (no hardcode URL)
7. Pitfall 7 preserved: visibilitychange listener intact

**Out of scope (Plan 01 + UAT closure):**
- Playwright FLOW 313 GREEN transition -> Plan 01 + DB seed run
- Manual UAT 7-step verification -> 313-UAT.md (Plan 01)
- AuditLog SQL spot-check -> 313-UAT.md
</success_criteria>

<output>
After completion, create `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-03-SUMMARY.md` mencakup:
- Task 1-2 outcomes (line ranges modified, exact strings inserted)
- `dotnet build` final output (errors=0, warnings count delta)
- Acceptance criteria verification (grep results)
- C-02 + C-03 + D-03 + D-10 + D-11 compliance status
- WR-02 mitigation status (form.action DOM property usage)
- Pitfall 2 + Pitfall 7 preservation status
- Note untuk UAT operator: jalankan `.planning/seeds/313-timer-fixtures.sql` + login coachee + jalankan 313-UAT.md 7 step + spot-check AuditLog
- Note untuk Plan 01 closure: setelah backend (Plan 02) + frontend (Plan 03) merged + DB seed run, FLOW 313 Playwright tests transisi RED -> GREEN. Tester finalisasi assertion bodies sesuai UAT result.
</output>
