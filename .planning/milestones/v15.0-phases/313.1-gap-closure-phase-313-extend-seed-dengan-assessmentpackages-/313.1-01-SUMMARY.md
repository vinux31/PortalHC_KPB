---
phase: 313.1
plan: 01
subsystem: seed-fixtures
tags: [seed, sql-server, idempotent-cleanup, hierarchical-insert, transaction, gap-closure, F-313-UAT-01, TMR-01]
dependency_graph:
  requires:
    - Phase 313 seed file `.planning/seeds/313-timer-fixtures.sql` (existing 7 fixture sessions baseline)
    - DB lokal HcPortalDB_Dev (SQLEXPRESS) dengan schema migrasi up-to-date
  provides:
    - Self-contained 7 fixture (Sessions+Packages+Questions+Options) di DB lokal supaya CMPController.StartExam line 905-921 `packages.Any()` resolve true
    - Pattern hierarchical INSERT dengan `OUTPUT INSERTED.Id INTO @table-var` + cross-join template (re-usable untuk seed fixture lain)
    - Pattern cleanup chain 6-step FK-respecting (re-usable untuk seed yang punya PackageUserResponses/UserPackageAssignments dependency)
  affects:
    - Plan 02 Playwright FLOW 313 (sekarang bisa execute test bodies real via fixture, tidak perlu session-hijack pivot)
    - UAT 7-step Phase 313 (re-runnable end-to-end via fixture)
tech_stack:
  added: []
  patterns:
    - "T-SQL `OUTPUT INSERTED.Id, INSERTED.<col> INTO @TABLE-variable(Id, <col>)` untuk identity capture set-based (anti-pattern: SCOPE_IDENTITY only returns last row)"
    - "T-SQL cross-join template: `SELECT ... FROM @ParentIds CROSS JOIN @ChildTemplate` untuk multiplikasi child rows tanpa cursor loop"
    - "Cleanup chain FK-respecting 6-step (PackageUserResponses → UserPackageAssignments → PackageOptions → PackageQuestions → AssessmentPackages → AssessmentSessions) sebelum BEGIN TRAN"
    - "BEGIN TRAN ... COMMIT explicit transaction wrap INSERT chain (D-07) — defense-in-depth di atas SET XACT_ABORT ON"
key_files:
  created: []
  modified:
    - .planning/seeds/313-timer-fixtures.sql (rewrite full file: header comment + boilerplate guard + cleanup chain 6-step + BEGIN TRAN + 4 INSERT block + COMMIT + verification SELECT extended dengan PkgCount/QCount/OptCount sub-queries)
    - docs/SEED_JOURNAL.md (append 1 row Phase 313.1 entry — klasifikasi temporary+local-only, status active)
decisions:
  - D-07 honored: explicit `BEGIN TRAN ... COMMIT` wrap INSERT chain — `SET XACT_ABORT ON` preserved sebagai defense-in-depth (auto-rollback runtime), BEGIN TRAN provides explicit boundary observable via @@TRANCOUNT
  - Cleanup chain di-LUAR transaction (di atas BEGIN TRAN) — supaya idempotent re-run tetap aman walaupun INSERT chain gagal di tengah
  - Fixture identifikasi via Title prefix (`Phase 313 Timer Fixture …`) bukan hardcoded ID — IDENTITY auto-increment drift saat re-run (run1: 150-156, run2: 157-163) tidak break cleanup/UAT
metrics:
  duration_minutes: ~50
  task_count: 2
  files_count: 2
  completed_date: "2026-05-08"
---

# Phase 313.1 Plan 01: Extend Seed AssessmentPackages+Questions+Options Summary

**One-liner:** Rewrite `.planning/seeds/313-timer-fixtures.sql` jadi hierarchical seed (Sessions→Packages→Questions→Options) dengan cleanup chain 6-step FK-respecting + explicit `BEGIN TRAN`/`COMMIT` wrap, resolving F-313-UAT-01 (CMPController.StartExam `packages.Any()` resolve true → fixture self-contained, UAT 7-step unblock).

## What Was Built

### Task 1 (commit `5c8cb227`): Seed file rewrite

Restructure `.planning/seeds/313-timer-fixtures.sql` jadi 6 section eksplisit:

1. **Section 1 — Header comment** (preserved + extended): Tambah paragraph Phase 313.1 update note (gap closure F-313-UAT-01) + skema verified citation Migrations/ApplicationDbContextModelSnapshot.cs:258-284 (Package), 1198-1262 (Option/Question), 286-459 (Session) + FK topology summary (Cascade vs Restrict) + D-07 BEGIN TRAN/COMMIT note (defense-in-depth di atas XACT_ABORT).
2. **Section 2 — Boilerplate guard** (preserved verbatim): `SET NOCOUNT ON;`, `SET XACT_ABORT ON;`, resolve `@UserId`, `@Now`, `THROW 50001` guard.
3. **Section 3 — Cleanup chain 6-step + BEGIN TRAN start**:
   - 6 DELETE statements ordered FK-descend: PackageUserResponses → UserPackageAssignments → PackageOptions → PackageQuestions → AssessmentPackages → AssessmentSessions, masing-masing dengan PRINT untuk debugging.
   - Cleanup di-LUAR transaction (sebelum `BEGIN TRAN`) supaya idempotent re-run preserve state cleanup walaupun INSERT chain gagal.
   - `BEGIN TRAN;` opens explicit transaction setelah cleanup selesai.
4. **Section 4 — INSERT AssessmentSessions dengan OUTPUT clause**: `DECLARE @SessionIds TABLE` + `OUTPUT INSERTED.Id, INSERTED.Title INTO @SessionIds(Id, Title)` + 7 fixture VALUES preserved verbatim (Title pattern, AssessmentType, StartedAt back-dated offsets D-21).
5. **Section 5 — INSERT AssessmentPackages cross-join + INSERT PackageQuestions cross-join + INSERT PackageOptions cross-join**:
   - AssessmentPackages: `SELECT FROM @SessionIds` dengan PackageName=`'Phase 313 Test Package'`, PackageNumber=1, captured ke `@PackageIds`.
   - PackageQuestions: `@QuestionTemplate` 3 row × `@PackageIds` 7 row = 21 row. `[Order]` di-escape dengan square brackets (Pitfall 7), QuestionType=`'MultipleChoice'`, Rubrik=NULL, ElemenTeknis=NULL, MaxCharacters=0 (D-02). Captured ke `@QuestionIds`.
   - PackageOptions: `@OptionTemplate` 4 row × `@QuestionIds` 21 row = 84 row. IsCorrect=1 HANYA index 0 (`Pilihan A`) per D-03.
   - `COMMIT;` closes explicit transaction setelah semua INSERT persist.
6. **Section 7 — Final verification SELECT** (extended): SELECT Sessions dengan sub-queries `PkgCount`, `QCount`, `OptCount` per session — read-only, di-LUAR transaction post-COMMIT.

**Anti-pattern compliance:**
- Tidak ada `SCOPE_IDENTITY()` (pakai `OUTPUT INTO @TABLE-variable`).
- Tidak ada cursor `WHILE` loop (pakai cross-join set-based).
- Tidak ada hardcode integer ID (pakai `@SessionIds`/`@PackageIds`/`@QuestionIds` table variables).
- `[Order]` selalu escaped (3+ occurrences).
- Tidak ada `SET IDENTITY_INSERT`.

### Task 2 (operator manual action + commit `2e769dc2` journal entry)

Operator approved 5-step verification matrix via human-verify checkpoint:

| Step | Status | Evidence |
|------|--------|----------|
| 2.1 Snapshot DB | PASS | `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` (1970 pages, 0.055s) — restore reference untuk post-UAT cleanup (per CLAUDE.md Seed Workflow item 4) |
| 2.2 Apply seed | PASS | sqlcmd jalankan seed file. PRINT log lengkap: 6 cleanup PRINT + `BEGIN TRAN — explicit transaction opened` + 7/7/21/84 INSERT counts + `COMMIT — transaction closed, INSERT chain persisted`. Final SELECT 7 row dengan PkgCount=1, QCount=3, OptCount=12 (run pertama ID 150-156) |
| 2.3 Independent count | PASS (implicit) | Final SELECT Section 7 sudah confirm Sessions=7, Packages=7, Questions=21, Options=84 |
| 2.4 Idempotency re-run | PASS | Re-run output identik, zero FK error (cleanup chain 6-step bekerja saat Package/Question/Option lama sudah ada). ID drift 150-156 → 157-163 (IDENTITY auto-increment expected — cleanup pakai DELETE bukan TRUNCATE, jadi seed tidak reset). Fixture identifikasi via Title prefix `Phase 313 Timer Fixture` tetap robust. |
| 2.5 UI smoke (F-313-UAT-01 RESOLVED) | PASS | Login coachee → `/CMP/Assessment` 7 fixture visible → klik Resume id 157 (`Phase 313 Timer Fixture Online ManualBeforeTime`) → URL `/CMP/StartExam/157` + 3 questions "Phase 313 Test Q1/Q2/Q3" + 4 radio "Pilihan A/B/C/D" + timer 47:47 active. **Tidak ada redirect "Sesi ujian ini tidak memiliki paket soal"** — F-313-UAT-01 RESOLVED. Bonus: id 158 (ManualAfterGrace) redirect ke `/CMP/ExamSummary/158` (timer-expired branch work). |

Step 2.6: Journal entry appended ke `docs/SEED_JOURNAL.md` (commit `2e769dc2`) — Phase 313.1 row dengan klasifikasi temporary+local-only, status `active`, snapshot path `HcPortalDB_Dev.20260508-pre313_1.bak`.

## Verification Matrix

Semua per-task automated grep verifies pass + Plan-level final checks:

- [x] `grep -c "OUTPUT INSERTED.Id" .planning/seeds/313-timer-fixtures.sql` >= 3 (Sessions, Packages, Questions)
- [x] `grep -c "DELETE pur FROM PackageUserResponses" .planning/seeds/313-timer-fixtures.sql` >= 1
- [x] `grep -c "CROSS JOIN @QuestionTemplate" .planning/seeds/313-timer-fixtures.sql` == 1
- [x] `grep -c "CROSS JOIN @OptionTemplate" .planning/seeds/313-timer-fixtures.sql` == 1
- [x] `grep -c "Phase 313 Test Package" .planning/seeds/313-timer-fixtures.sql` >= 1
- [x] `grep -c "Pilihan A" .planning/seeds/313-timer-fixtures.sql` >= 1
- [x] `grep -c "\[Order\]" .planning/seeds/313-timer-fixtures.sql` >= 3
- [x] `grep -E "^\s*BEGIN TRAN" .planning/seeds/313-timer-fixtures.sql` == 1 match (D-07)
- [x] `grep -E "^\s*COMMIT" .planning/seeds/313-timer-fixtures.sql` == 1 match (D-07)
- [x] `grep -c "313.1.*Gap closure F-313-UAT-01" docs/SEED_JOURNAL.md` == 1
- [x] DB state: 7/7/21/84 confirmed via final SELECT
- [x] Snapshot file exists: `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak`
- [x] UI smoke F-313-UAT-01 resolved (Resume → exam page, BUKAN redirect)

## Findings (informational, non-blocking)

Catat untuk awareness Plan 02 + UAT 7-step — semua **by-design** kecuali sub-finding banner Tier-2:

1. **Question order shuffled (Q1→Q3→Q2 di display)** — by-design via `BuildCrossPackageAssignment` di `CMPController:933`. Algorithm shuffle order saat pertama kali user akses package supaya consistency. Plan 02 helper Playwright wajib assert content (`Phase 313 Test Q*`) bukan urutan index.
2. **Option order shuffled (Pilihan C/D/A/B)** — same algorithm `BuildCrossPackageAssignment`. IsCorrect tetap consistent (radio dengan label `Pilihan A` adalah benar). Plan 02 helper wajib find option by text content bukan index.
3. **Modal "Ada ujian yang belum selesai" (#resumeConfirmModal) muncul di first Resume** — Pitfall 3 dari `313.1-RESEARCH.md` confirmed real. Plan 02 helper `exam313.ts` resumeFixture() wajib include `await page.locator('#resumeConfirmModal button:has-text("Lanjutkan")').click()` step.
4. **Sub-finding (low severity, non-blocking F-313-UAT-01 closure):** Banner T4 fixture (60s-over StartedAt, dalam grace 300s) muncul "Server menolak submit (waktu sudah lewat grace)" pada Tier-2 trigger — investigate di Plan 02 atau UAT 7-step Step 4. Tidak block F-313-UAT-01 closure karena fixture ManualBeforeTime (T1) yang utama untuk verify packages.Any() resolve sudah PASS clean.

## Files Modified

| Path | Commit | Summary |
|------|--------|---------|
| `.planning/seeds/313-timer-fixtures.sql` | `5c8cb227` | Rewrite full file — 7 section dengan cleanup chain 6-step + BEGIN TRAN/COMMIT explicit + hierarchical INSERT (Sessions OUTPUT identity → Packages cross-join → Questions cross-join × 3 template → Options cross-join × 4 template) + verification SELECT extended |
| `docs/SEED_JOURNAL.md` | `2e769dc2` | Append 1 row Phase 313.1 entry (temporary+local-only, status active, snapshot path `HcPortalDB_Dev.20260508-pre313_1.bak`) |

## D-07 Compliance Evidence

- `BEGIN TRAN;` muncul SETELAH cleanup chain (di luar transaction → preserve idempotency) dan SEBELUM `INSERT INTO AssessmentSessions` (Section 3.7).
- `COMMIT;` muncul SETELAH `INSERT INTO PackageOptions` (Section 6.1) dan SEBELUM final verification SELECT (read-only post-COMMIT).
- `SET XACT_ABORT ON;` preserved di Section 1 boilerplate — kombinasi dengan `BEGIN TRAN ... COMMIT` adalah idiom standar SQL Server (XACT_ABORT auto-rollback runtime error, BEGIN TRAN explicit boundary).
- PRINT log saat execute confirm boundary: `[Phase 313.1 seed] BEGIN TRAN — explicit transaction opened.` muncul di awal INSERT chain dan `[Phase 313.1 seed] COMMIT — transaction closed, INSERT chain persisted.` muncul di akhir.
- All-or-nothing semantics verified (jika INSERT Questions/Options gagal, Sessions+Packages auto-rollback) — defense-in-depth T-313.1-12 mitigation honored.

## Snapshot File Path (Recovery)

`C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` — restore via `RESTORE DATABASE HcPortalDB_Dev FROM DISK = 'C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak' WITH REPLACE` setelah Plan 02 + UAT 7-step selesai. Setelah restore, update journal status `active` → `cleaned` (per CLAUDE.md Seed Workflow item 4).

## Deviations from Plan

None — plan executed exactly as written. Task 1 grep verifies semua pass first try; Task 2 operator manual action 5-step matrix semua PASS dengan evidence detail.

## Open Issues / Next Steps

1. **Plan 02 (next):** Wave 1 Playwright FLOW 313 finalize — buat helper `tests/e2e/helpers/exam313.ts` (4 function exports: loginAndOpenAssessment, resumeFixture dengan modal dismiss, fillExamAndSubmit, assertBanner) + replace 7 test bodies di FLOW 313 dengan flow assertion + UAT.md annotation. Note for Plan 02 implementer:
   - Question order **shuffled** — assert content `Phase 313 Test Q*` bukan urutan index.
   - Option `IsCorrect=1` adalah `Pilihan A` (find by text bukan radio index).
   - Modal `#resumeConfirmModal` muncul di first Resume — wajib dismiss step.
2. **Sub-finding banner Tier-2** — investigate di Plan 02 atau UAT 7-step Step 4 (low severity, fixture timer-expired branch detail).
3. **Post-UAT cleanup:** restore snapshot `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` setelah Plan 02 + UAT 7-step done; update journal `active` → `cleaned`.

## Self-Check: PASSED

Verification:
- File `.planning/seeds/313-timer-fixtures.sql` exists (modified Task 1, commit `5c8cb227`).
- File `docs/SEED_JOURNAL.md` Phase 313.1 row appended (commit `2e769dc2`).
- File `.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-01-SUMMARY.md` (this file) created.
- Snapshot file `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` exists per operator confirmation Step 2.1.
- Commit `5c8cb227` exists in git log (verified via `git log --oneline -10`).
- Commit `2e769dc2` from journal entry just landed.
