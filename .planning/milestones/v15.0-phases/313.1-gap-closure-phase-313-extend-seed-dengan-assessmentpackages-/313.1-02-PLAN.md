---
phase: 313.1
plan: 02
type: execute
wave: 1
depends_on:
  - 313.1-01
files_modified:
  - tests/e2e/helpers/exam313.ts
  - tests/e2e/exam-taking.spec.ts
  - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md
autonomous: false
requirements:
  - F-313-UAT-01
  - TMR-01
gap_closure: true

must_haves:
  truths:
    - "7 test FLOW 313 di tests/e2e/exam-taking.spec.ts (313.1-313.7) eksekusi flow lengkap (click Resume → navigate → assert outcome banner/URL), bukan placeholder `targetRow.toBeVisible()`"
    - "Helper module `tests/e2e/helpers/exam313.ts` ada dengan 4 function exports (clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess)"
    - "TypeScript compile (`npx tsc --noEmit`) zero error"
    - "Playwright list (`npx playwright test --grep \"Phase 313\" --list`) returns 7 tests (bukan 0/SKIP)"
    - "313-UAT.md pre-conditions block ditandai dengan annotation Phase 313.1 update yang menjelaskan seed sekarang self-contained"
    - "Import block di tests/e2e/exam-taking.spec.ts hardcode 4 helper sekaligus di Task 2 (single source of truth, Task 3-5 tidak modify import block)"
  artifacts:
    - path: "tests/e2e/helpers/exam313.ts"
      provides: "Helper functions untuk FLOW 313 fixture interaction + outcome assertion (D-17)"
      exports: ["clickResumeForFixture", "assertTier1Reject", "assertTier2Reject", "assertSubmitSuccess"]
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "7 test FLOW 313 (313.1-313.7) dengan flow assertion lengkap (D-10 s/d D-16)"
      contains: "import.*helpers/exam313"
    - path: ".planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md"
      provides: "Pre-conditions annotation Phase 313.1 update + verification SQL extended (PkgCount/QCount)"
      contains: "Phase 313.1 update"
  key_links:
    - from: "tests/e2e/exam-taking.spec.ts FLOW 313 describe block"
      to: "tests/e2e/helpers/exam313.ts"
      via: "import { clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess } from './helpers/exam313'"
      pattern: "import.*from.*helpers/exam313"
    - from: "test 313.1 (Manual+BeforeTime)"
      to: "Submit success → /CMP/Results/{id}"
      via: "clickResumeForFixture → fill radio first → #reviewSubmitBtn → confirm dialog accept → Kumpulkan Ujian → assertSubmitSuccess"
      pattern: "assertSubmitSuccess"
    - from: "test 313.2/313.5/313.6 (Manual+AfterGrace Tier-1)"
      to: "/CMP/StartExam/{id} dengan banner D-01 'Waktu ujian Anda sudah habis'"
      via: "clickResumeForFixture → assertTier1Reject (waitForURL StartExam + .alert-danger contains text)"
      pattern: "assertTier1Reject"
    - from: "test 313.4 (Auto+AfterGrace Tier-2)"
      to: "/CMP/StartExam/{id} dengan banner Tier-2 'Server menolak submit' atau 'Waktu ujian Anda telah habis'"
      via: "clickResumeForFixture → assertTier2Reject (waitForURL StartExam + .alert-warning|.alert-danger contains text regex)"
      pattern: "assertTier2Reject"

user_setup: []
---

<objective>
Finalize Playwright FLOW 313 (`tests/e2e/exam-taking.spec.ts` line 1607-1689) — replace 7 placeholder body `await expect(targetRow).toBeVisible()` dengan flow assertion lengkap sesuai D-10 s/d D-16 di CONTEXT.md. Buat helper module baru `tests/e2e/helpers/exam313.ts` (D-17) dengan 4 function exports yang centralize fixture interaction + outcome assertion. Annotate `313-UAT.md` pre-conditions block dengan note Phase 313.1 update + verification SQL extended (PkgCount/QCount).

Banner literals di-extract verbatim dari source (RESEARCH.md "Banner Text Literals" line 523-583): D-01 Tier-1 = `'Waktu ujian Anda sudah habis'` (substring match), Tier-2 = `/Server menolak submit|Waktu ujian Anda telah habis/` (regex). Selectors di-extract verbatim dari `Views/CMP/StartExam.cshtml` + `ExamSummary.cshtml` (RESEARCH.md "CMP Exam Page Selectors" line 586-664).

Purpose: Tutup gap UAT verification Phase 313 (per `313-UAT-FINDINGS.md` SC#7 BLOCKED — "Playwright FLOW 313 GREEN state"). Setelah Plan 02 landed dan seed Plan 01 di-apply, 7 tests harus PASS (bukan SKIP, bukan placeholder visible-only) — FLOW 313 jadi regression net real untuk Phase 313 logic (Tier-1/Tier-2 reject + Manual exclude D-15).

Output:
- New file `tests/e2e/helpers/exam313.ts` mengikuti pattern modul `tests/e2e/helpers/wizardSelectors.ts` (Phase 307 D-01 analog).
- Modified `tests/e2e/exam-taking.spec.ts` — preserve existing `escapeRegex` helper line 1692-1694, preserve test name + `login(page, 'coachee')` + `fixtureTitle` constants, replace placeholder bodies dengan flow assertion. Import 4 helper sekaligus di Task 2 (hardcode konsolidasi).
- Modified `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md` pre-conditions block (annotation Phase 313.1 + extended verification SQL).
</objective>

<execution_context>
@.claude/agents/gsd-execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-RESEARCH.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-PATTERNS.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-VALIDATION.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md
@tests/e2e/exam-taking.spec.ts
@tests/e2e/helpers/wizardSelectors.ts

<interfaces>
<!-- Selector + banner literal contracts (extracted verbatim dari Views/CMP/* + Controllers/CMPController.cs via RESEARCH.md citations). Executor wajib copy-paste, tidak boleh paraphrase. -->

Banner Tier-1 (D-01) — substring match (Controllers/CMPController.cs:4599):
  'Waktu ujian Anda sudah habis'

Banner Tier-2 (313.4) — regex match (Controllers/CMPController.cs:4607 OR ExamSummary.cshtml:206):
  /Server menolak submit|Waktu ujian Anda telah habis/

Selectors (Views/CMP/StartExam.cshtml + ExamSummary.cshtml):
  - Resume link di Assessment list: `a:has-text("Resume")` di dalam `tr` row
  - Resume confirmation modal: `#resumeConfirmModal`, button `#resumeConfirmBtn` (text "Lanjutkan")
  - Question card container: `[id^="qcard_"]`
  - Multiple-choice radio: `input.exam-radio[type="radio"]` (no data-testid; use locator role + .first())
  - Review-submit button (StartExam): `#reviewSubmitBtn`
  - Submit button (ExamSummary): `button:has-text("Kumpulkan Ujian")` (with onclick="return confirm(...)" — register dialog handler before click)
  - Banner danger (TempData Error): `.alert-danger`
  - Banner warning (custom JS retry warning): `.alert-warning`
  - Form action: `form[action$="/SubmitExam"]`

Login helper (existing): import { login } from '../helpers/auth'; await login(page, 'coachee');
  Credential: rino.prasetyo@pertamina.com / 123456 (per memory reference_dev_credentials.md)

Existing escapeRegex helper (exam-taking.spec.ts:1692-1694):
  function escapeRegex(s: string): string { return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'); }

Timeout (D-22):
  - Navigation default: 10_000 ms (10s)
  - SubmitExam POST + retry chain: 30_000 ms (30s, accommodates 1+2+4=7s backoff + processing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create helper module `tests/e2e/helpers/exam313.ts` dengan 4 function exports (clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess)</name>
  <files>tests/e2e/helpers/exam313.ts</files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md (D-17 helper structure, D-18 selector strategy, D-22 timeout)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-RESEARCH.md (Section "Banner Text Literals" line 523-583, Section "CMP Exam Page Selectors" line 586-664, Section "Pattern 4: Test Helper Module" line 332-380)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-PATTERNS.md (Section 3 line 304-385 — verbatim helper file template)
    - tests/e2e/helpers/wizardSelectors.ts (analog: comment block + module structure pattern, Phase 307 D-01)
  </read_first>
  <action>
Create new file `tests/e2e/helpers/exam313.ts` dengan structure berikut (verbatim dari `313.1-PATTERNS.md` line 320-385 — selaraskan banner literal + selector dengan `<interfaces>` block di atas):

```typescript
// Phase 313.1 — Helpers untuk FLOW 313 timer-enforcement E2E tests
// Centralized fixture interactions: locate row, click Resume, assert outcome banners.
// Banner literals match Controllers/CMPController.cs:4599 (Tier-1) + 4607 (Tier-2)
// + Views/CMP/ExamSummary.cshtml:206 (custom JS retry warning).

import { Page, expect, test } from '@playwright/test';

function escapeRegex(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

/**
 * Locate fixture row by Title, click Resume link, wait for navigation to either
 * StartExam (alive session) or ExamSummary (server-side redirect timer-expired).
 * Returns sessionId parsed from URL.
 *
 * Auto-skips test if fixture not found (seed Wave 0 not yet applied).
 */
export async function clickResumeForFixture(
  page: Page,
  fixtureTitle: string
): Promise<number> {
  await page.goto('/CMP/Assessment');
  const targetRow = page.locator('tr', {
    hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm')
  }).first();
  if (await targetRow.count() === 0) {
    test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — jalankan .planning/seeds/313-timer-fixtures.sql`);
  }
  await targetRow.locator('a:has-text("Resume")').click();
  // Lands at StartExam (alive) atau ExamSummary (server-side redirect timer-expired)
  await page.waitForURL(/\/CMP\/(StartExam|ExamSummary)\/\d+/, { timeout: 10_000 });
  const m = page.url().match(/\/(?:StartExam|ExamSummary)\/(\d+)/);
  return m ? parseInt(m[1], 10) : 0;
}

/**
 * Assert Tier-1 reject outcome (used by 313.2/313.5/313.6 — Manual+AfterGrace).
 * Path: ExamSummary auto-fire JS retry → server 302 StartExam + TempData["Error"]
 * Banner D-01 verbatim from Controllers/CMPController.cs:4599 (substring match).
 */
export async function assertTier1Reject(page: Page, sessionId: number) {
  await page.waitForURL(new RegExp(`/CMP/StartExam/${sessionId}\\b`), { timeout: 30_000 });
  await expect(page.locator('.alert-danger')).toContainText(
    'Waktu ujian Anda sudah habis'
  );
}

/**
 * Assert Tier-2 reject outcome (used by 313.4 — Auto+AfterGrace).
 * Path: server 302 StartExam, JS retry handler intercept → custom .alert-warning
 * ATAU server-side .alert-danger dari TempData (jika JS tidak intercept).
 * Banner regex covers both branches per RESEARCH.md "Banner Text Literals" Tier-2 section.
 */
export async function assertTier2Reject(page: Page, sessionId: number) {
  await page.waitForURL(new RegExp(`/CMP/StartExam/${sessionId}\\b`), { timeout: 30_000 });
  const banner = page.locator('.alert-warning, .alert-danger').first();
  await expect(banner).toContainText(
    /Server menolak submit|Waktu ujian Anda telah habis/
  );
}

/**
 * Assert submit success outcome (used by 313.1 — Manual+BeforeTime,
 * 313.3 — Auto+InGrace, 313.7 — Manual type exclude D-15).
 * Path: SubmitExam POST → 302 Results.
 */
export async function assertSubmitSuccess(page: Page, sessionId: number) {
  await page.waitForURL(new RegExp(`/CMP/Results/${sessionId}\\b`), { timeout: 30_000 });
}
```

**Naming convention compliance (per CONTEXT.md D-17):**
- Verb-prefixed function names (`click...`, `assert...`).
- Module exports flat (no `export default`).
- Private helper (`escapeRegex`) tidak di-export.
- Comment block style mengikuti `wizardSelectors.ts` (Phase 307 D-01).

**Anti-pattern avoidance (per RESEARCH.md "Anti-Patterns" + Common Pitfalls):**
- TIDAK pakai `tr:has-text("plain")` — pakai regex anchor + escapeRegex (Phase 312 WR-03 mitigation, Pitfall 5).
- TIDAK hardcode session ID `/CMP/StartExam/150` — parse dari URL.
- TIDAK polling tight loop — pakai `waitForURL` dengan timeout 30s (D-22).
- TIDAK exact-string banner match — pakai substring `toContainText` atau regex (lebih robust dari punctuation drift).
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
    Expected: zero errors. (TypeScript compile sanity — fail kalau import path salah, type signature mismatch, atau syntax error.)

    Spot-checks:
    - `grep -c "export function clickResumeForFixture\|export async function clickResumeForFixture" tests/e2e/helpers/exam313.ts` returns 1
    - `grep -c "export function assertTier1Reject\|export async function assertTier1Reject" tests/e2e/helpers/exam313.ts` returns 1
    - `grep -c "export function assertTier2Reject\|export async function assertTier2Reject" tests/e2e/helpers/exam313.ts` returns 1
    - `grep -c "export function assertSubmitSuccess\|export async function assertSubmitSuccess" tests/e2e/helpers/exam313.ts` returns 1
    - `grep -c "Waktu ujian Anda sudah habis" tests/e2e/helpers/exam313.ts` returns 1
    - `grep -c "Server menolak submit" tests/e2e/helpers/exam313.ts` returns 1
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/helpers/exam313.ts` exists.
    - 4 function exports terverifikasi via grep (clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess).
    - Banner literal D-01 'Waktu ujian Anda sudah habis' (Tier-1) appears di assertTier1Reject.
    - Banner regex 'Server menolak submit|Waktu ujian Anda telah habis' (Tier-2) appears di assertTier2Reject.
    - `import { Page, expect, test } from '@playwright/test'` di-import.
    - `escapeRegex` helper inline (private, tidak di-export).
    - Tidak ada `export default`.
    - TypeScript compile (`npx tsc --noEmit` di `tests/`) zero error.
  </acceptance_criteria>
  <done>
    Helper module landed; TS compile pass; ready untuk konsumsi di Task 2-5.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace test 313.1 body (Manual+BeforeTime — submit OK regression) — D-10 + hardcode import 4 helper sekaligus (single source of truth)</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md (D-10 spec)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-RESEARCH.md (Section "CMP Exam Page Selectors" line 586-664: #qcard_, .exam-radio, #reviewSubmitBtn, "Kumpulkan Ujian" button)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-PATTERNS.md (Section 2 line 225-301 — Resume modal dismiss + radio click + review-submit + Kumpulkan-Ujian + WaitForURL pattern)
    - tests/e2e/exam-taking.spec.ts line 1595-1619 (existing 313.1 test placeholder + Flow A6/A7 line 125-200 sebagai analog)
    - tests/e2e/helpers/exam313.ts (Task 1 output — import functions)
  </read_first>
  <action>
Edit `tests/e2e/exam-taking.spec.ts`. Tambah import statement di bagian atas file (line ~1-3, gabung dengan existing imports). **Hardcode import 4 helper sekaligus** supaya Task 3-5 tidak perlu modify import block lagi (TS unused-import allowed sementara karena helper akan dipakai Task 3-5 di file yang sama):

```typescript
import { clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess } from './helpers/exam313';
```

**Replace test 313.1** (line 1609-1619 existing) dengan body lengkap:

```typescript
test('313.1 - Manual + before-time + Online → submit OK (regression)', async ({ page }) => {
  await login(page, 'coachee');
  const fixtureTitle = 'Phase 313 Timer Fixture Online ManualBeforeTime';
  const sessionId = await clickResumeForFixture(page, fixtureTitle);
  // Manual+BeforeTime: Resume → StartExam (alive). Modal dismiss kalau ada.
  const resumeModal = page.locator('#resumeConfirmModal');
  if (await resumeModal.isVisible({ timeout: 2_000 }).catch(() => false)) {
    await page.locator('#resumeConfirmBtn').click();
    await page.waitForTimeout(500);
  }
  // Fill Q1 first option (= "Pilihan A" per D-03 IsCorrect=true index 0).
  // No data-testid in StartExam.cshtml — use class selector .exam-radio (per D-18).
  const firstQuestion = page.locator('[id^="qcard_"]').first();
  await firstQuestion.locator('input.exam-radio[type="radio"]').first().check({ force: true });
  // Trigger Review and Submit (POST → ExamSummary).
  await page.locator('#reviewSubmitBtn').click();
  await page.waitForURL(/\/CMP\/ExamSummary\/\d+/, { timeout: 10_000 });
  // Confirm dialog handler (Pitfall 4) MUST register before click.
  page.once('dialog', dialog => dialog.accept());
  await page.locator('button:has-text("Kumpulkan Ujian")').click();
  await assertSubmitSuccess(page, sessionId);
});
```

**Preserve existing structure:**
- Test name string identik dengan baseline (jangan ubah hyphen/punctuation).
- `login(page, 'coachee')` line 1 body (existing pattern).
- `fixtureTitle` constant identik literal (jangan ubah case atau spasi).

**Hapus existing placeholder lines** (1612-1618):
- `await page.goto('/CMP/Assessment');` — sudah dipanggil di `clickResumeForFixture` helper.
- `const targetRow = page.locator(...)` + `if (await targetRow.count() === 0) test.skip(...)` — sudah di-handle helper.
- `await expect(targetRow).toBeVisible();` — placeholder, hapus.

**Catatan import konsolidasi:** Walaupun Task 2 cuma pakai `clickResumeForFixture` + `assertSubmitSuccess`, import 4 helper sekaligus (`assertTier1Reject` + `assertTier2Reject` juga di-import). Helper-helper itu akan dipakai Task 3-5 di file yang sama, jadi unused-import sementara di akhir Task 2 — Task 3-5 tidak perlu modify import block lagi. Setelah Task 3-5 selesai, semua 4 helper akan terpakai. TypeScript `noUnusedLocals` config di `tests/tsconfig.json` (kalau aktif) tidak akan complain selama unused-import phase intermediate antara Task 2 dan Task 5; kalau strict mode complain, snapshot TS check di akhir Wave 1 (Task 7) saja yang mandatory.
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
    Expected: zero errors.

    Spot-checks:
    - `grep -c "import.*helpers/exam313" tests/e2e/exam-taking.spec.ts` returns 1
    - `grep -c "clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess" tests/e2e/exam-taking.spec.ts` returns 1 (verifikasi konsolidasi 4 helper di-import sekaligus)
    - `grep -c "313.1 - Manual + before-time" tests/e2e/exam-taking.spec.ts` returns 1
    - `grep -c "assertSubmitSuccess(page, sessionId)" tests/e2e/exam-taking.spec.ts` returns >= 1
    - `grep -c "Kumpulkan Ujian" tests/e2e/exam-taking.spec.ts` returns >= 1 (test 313.1 body) — note: ini grep generic, akan ada match dari test lain di file juga, expected >= 2 setelah Task 2-5 (Flow A7 existing + 313.1 + 313.3 + 313.7)
    - `cd tests && npx playwright test --grep "313.1" --list` returns "1 test" (313.1 enumerated)
  </verify>
  <acceptance_criteria>
    - Test 313.1 body diganti dengan flow lengkap (Resume → StartExam → fill radio → reviewSubmitBtn → ExamSummary → confirm dialog → Kumpulkan Ujian → assertSubmitSuccess).
    - Resume modal dismiss handled (Pitfall 3).
    - Confirm dialog handler registered sebelum click (Pitfall 4).
    - Import statement `helpers/exam313` ditambahkan dengan **4 helper sekaligus** (clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess) — single source of truth.
    - TS compile zero error (atau warn-only kalau noUnusedLocals strict; resolved by Task 5).
    - Placeholder `await expect(targetRow).toBeVisible()` removed dari test 313.1.
  </acceptance_criteria>
  <done>
    Test 313.1 fully migrated; import block hardcoded 4 helper; siap eksekusi via Playwright runner setelah Task 6.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace test 313.2 + 313.5 + 313.6 body (Manual+AfterGrace Tier-1 reject) — D-11/D-14/D-15</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md (D-11/D-14/D-15: Tier-1 reject pattern shared)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-RESEARCH.md (Section "Playwright Redirect Verification Pattern" line 668-707, Section "Banner Text Literals" line 527-541)
    - tests/e2e/exam-taking.spec.ts line 1621-1632, 1656-1675 (existing 313.2/5/6 placeholders)
    - tests/e2e/helpers/exam313.ts (assertTier1Reject)
  </read_first>
  <action>
Asumsi import sudah ada dari Task 2 (4 helper di-import sekaligus: clickResumeForFixture, assertTier1Reject, assertTier2Reject, assertSubmitSuccess). Task 3 cuma replace test bodies — TIDAK modify import block lagi.

**Replace test 313.2** (existing line 1621-1632):

```typescript
test('313.2 - Manual + after-time (in grace) + Online → BLOCKED + AuditLog SubmitExamBlocked', async ({ page }) => {
  await login(page, 'coachee');
  const fixtureTitle = 'Phase 313 Timer Fixture Online ManualAfterGrace';
  const sessionId = await clickResumeForFixture(page, fixtureTitle);
  // Server detects timer expired → redirect StartExam → ExamSummary.
  // JS auto-fire retry handler → POST SubmitExam → server Tier-1 reject 302 StartExam.
  // assertTier1Reject covers final waitForURL StartExam + banner D-01 'Waktu ujian Anda sudah habis'.
  await assertTier1Reject(page, sessionId);
  // AuditLog SubmitExamBlocked verify TETAP MANUAL SQL spot-check di 313-UAT.md (Playwright tidak query DB direct).
});
```

**Replace test 313.5** (existing line 1656-1665):

```typescript
test('313.5 - Manual + after-time + PreTest → BLOCKED (Tier-1)', async ({ page }) => {
  await login(page, 'coachee');
  const fixtureTitle = 'Phase 313 Timer Fixture PreTest ManualAfterGrace';
  const sessionId = await clickResumeForFixture(page, fixtureTitle);
  await assertTier1Reject(page, sessionId);
});
```

**Replace test 313.6** (existing line 1667-1676):

```typescript
test('313.6 - Manual + after-time + PostTest → BLOCKED (Tier-1)', async ({ page }) => {
  await login(page, 'coachee');
  const fixtureTitle = 'Phase 313 Timer Fixture PostTest ManualAfterGrace';
  const sessionId = await clickResumeForFixture(page, fixtureTitle);
  await assertTier1Reject(page, sessionId);
});
```

**Preserve test names** verbatim (jangan ubah punctuation/dash). **Hapus existing placeholder lines** (page.goto + targetRow + test.skip + toBeVisible) — semua di-handle helper.

**Note:** Untuk PreTest/PostTest (313.5/313.6), AssessmentType di seed memang `PreTest`/`PostTest` — backend Tier-1 logic identik untuk Online/PreTest/PostTest (D-15 cuma exclude `Manual` type). Helper `assertTier1Reject` tidak perlu type-aware.
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
    Expected: zero errors.

    Spot-checks:
    - `grep -c "assertTier1Reject(page, sessionId)" tests/e2e/exam-taking.spec.ts` returns 3 (313.2 + 313.5 + 313.6)
    - `grep -c "import.*helpers/exam313" tests/e2e/exam-taking.spec.ts` returns 1 (TIDAK bertambah dari Task 2 — single import block)
    - `cd tests && npx playwright test --grep "313\\.[256]" --list` returns "3 tests"
  </verify>
  <acceptance_criteria>
    - 3 test bodies (313.2, 313.5, 313.6) diganti dengan flow `clickResumeForFixture → assertTier1Reject`.
    - Test names + fixtureTitle constants identik dengan baseline.
    - Comment di 313.2 menjelaskan AuditLog verify tetap manual.
    - **Import block TIDAK di-modify** — hanya 1 import statement dari Task 2 (single source of truth).
    - TS compile zero error.
  </acceptance_criteria>
  <done>
    3 Tier-1 tests migrated; siap eksekusi setelah Task 6.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Replace test 313.3 (Auto+InGrace grace pass) + 313.4 (Auto+AfterGrace Tier-2 BLOCK) — D-12/D-13</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md (D-12 grace pass + D-13 Tier-2 reject)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-RESEARCH.md (Section "Banner Text Literals" Tier-2 line 553-562 — JS custom warning OR server alert-danger)
    - tests/e2e/exam-taking.spec.ts line 1634-1654 (existing 313.3/4 placeholders)
    - tests/e2e/helpers/exam313.ts (assertSubmitSuccess + assertTier2Reject)
  </read_first>
  <action>
Asumsi import sudah ada dari Task 2 (4 helper di-import sekaligus). Task 4 cuma replace test bodies.

**Replace test 313.3** (existing line 1634-1643):

```typescript
test('313.3 - Auto + after-time (in grace) + Online → submit OK (Tier 2 grace covers)', async ({ page }) => {
  await login(page, 'coachee');
  const fixtureTitle = 'Phase 313 Timer Fixture Online AutoInGrace';
  const sessionId = await clickResumeForFixture(page, fixtureTitle);
  // Server timer-expired → redirect ExamSummary. JS auto-fire retry POST SubmitExam.
  // isAutoSubmit=true + elapsed within grace (< allowed + 2min) → server Tier-2 ACCEPT → 302 Results.
  await assertSubmitSuccess(page, sessionId);
});
```

**Replace test 313.4** (existing line 1645-1654):

```typescript
test('313.4 - Auto + after-grace + Online → BLOCKED Tier 2 (existing preserved)', async ({ page }) => {
  await login(page, 'coachee');
  const fixtureTitle = 'Phase 313 Timer Fixture Online AutoAfterGrace';
  const sessionId = await clickResumeForFixture(page, fixtureTitle);
  // isAutoSubmit=true + elapsed > allowed + 2min grace → server Tier-2 REJECT.
  // JS retry handler intercept redirect StartExam → custom .alert-warning 'Server menolak submit'
  // ATAU server-side .alert-danger 'Waktu ujian Anda telah habis' (kalau JS tidak intercept).
  // assertTier2Reject covers both branches via regex match.
  await assertTier2Reject(page, sessionId);
});
```

**Preserve test names** verbatim. **Hapus existing placeholder body**.

**Note:** D-12 (313.3) berbeda dari D-10 (313.1) — 313.3 tidak butuh manual fill radio + click reviewSubmitBtn karena auto-submit fire dari ExamSummary timer-expired branch tanpa user interaction. Cuma `clickResumeForFixture → assertSubmitSuccess` cukup.

**Race condition note (per RESEARCH.md line 705-707):** Karena JS retry handler ada di `window.addEventListener('load', attemptSubmit)`, ada window kecil dimana Playwright reach ExamSummary tapi retry belum fire. `assertSubmitSuccess` (waitForURL Results timeout 30s) cover ini — Playwright wait sampai second URL transition terjadi.
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
    Expected: zero errors.

    Spot-checks:
    - `grep -c "assertSubmitSuccess(page, sessionId)" tests/e2e/exam-taking.spec.ts` returns >= 2 (313.1 + 313.3, plus 313.7 setelah Task 5)
    - `grep -c "assertTier2Reject(page, sessionId)" tests/e2e/exam-taking.spec.ts` returns 1 (313.4)
    - `cd tests && npx playwright test --grep "313\\.[34]" --list` returns "2 tests"
  </verify>
  <acceptance_criteria>
    - Test 313.3 body diganti dengan `clickResumeForFixture → assertSubmitSuccess`.
    - Test 313.4 body diganti dengan `clickResumeForFixture → assertTier2Reject`.
    - Test names + fixtureTitle constants identik.
    - Comment menjelaskan Tier-2 grace logic vs Tier-2 reject logic.
    - Import block TIDAK di-modify (single source of truth dari Task 2).
    - TS compile zero error.
  </acceptance_criteria>
  <done>
    2 Tier-2 tests migrated.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Replace test 313.7 body (Manual ExcludeVerify D-15 — submit OK walaupun StartedAt NOW-161min)</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md (D-16 spec — Manual type bypass guard)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md (D-15 Manual exclude rule sumber)
    - tests/e2e/exam-taking.spec.ts line 1678-1688 (existing 313.7 placeholder)
    - tests/e2e/helpers/exam313.ts (assertSubmitSuccess)
  </read_first>
  <action>
Asumsi import sudah ada dari Task 2 (4 helper di-import sekaligus). Task 5 cuma replace test body 313.7.

**Replace test 313.7** (existing line 1678-1688):

```typescript
test('313.7 - Manual + after-time + Manual type → submit OK (D-15 exclude verify)', async ({ page }) => {
  await login(page, 'coachee');
  const fixtureTitle = 'Phase 313 Timer Fixture Manual ExcludeVerify';
  const sessionId = await clickResumeForFixture(page, fixtureTitle);
  // AssessmentType=Manual + StartedAt=NOW-161min: walaupun "after-time" jauh,
  // backend EnsureCanSubmitExamAsync skip Tier-1/Tier-2 untuk Manual type (D-15 exclude).
  // User reach exam page normal, fill answer, submit OK → /CMP/Results/{id}.
  const resumeModal = page.locator('#resumeConfirmModal');
  if (await resumeModal.isVisible({ timeout: 2_000 }).catch(() => false)) {
    await page.locator('#resumeConfirmBtn').click();
    await page.waitForTimeout(500);
  }
  const firstQuestion = page.locator('[id^="qcard_"]').first();
  await firstQuestion.locator('input.exam-radio[type="radio"]').first().check({ force: true });
  await page.locator('#reviewSubmitBtn').click();
  await page.waitForURL(/\/CMP\/ExamSummary\/\d+/, { timeout: 10_000 });
  page.once('dialog', dialog => dialog.accept());
  await page.locator('button:has-text("Kumpulkan Ujian")').click();
  await assertSubmitSuccess(page, sessionId);
  // AuditLog "TIDAK ada SubmitExamBlocked row untuk fixture id 156" verify tetap MANUAL SQL spot-check
  // di 313-UAT.md (Playwright tidak query DB direct, deferred per CONTEXT.md).
});
```

**Preserve test name** verbatim. **Hapus existing placeholder body**.

**Note:** Test 313.7 punya body mirror 313.1 (manual fill + reviewSubmitBtn + Kumpulkan Ujian → assertSubmitSuccess) — perbedaan adalah fixture title pakai `Manual ExcludeVerify` (AssessmentType=Manual) dan StartedAt=NOW-161min (jauh after-time). Yang di-test adalah backend D-15 exclude rule: Manual type harus PASS guard.
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
    Expected: zero errors.

    Spot-checks:
    - `grep -c "313.7 - Manual + after-time + Manual type" tests/e2e/exam-taking.spec.ts` returns 1
    - `grep -c "assertSubmitSuccess(page, sessionId)" tests/e2e/exam-taking.spec.ts` returns 3 (313.1 + 313.3 + 313.7)
    - `cd tests && npx playwright test --grep "313.7" --list` returns "1 test"
  </verify>
  <acceptance_criteria>
    - Test 313.7 body diganti dengan flow: clickResumeForFixture → modal dismiss → fill radio → reviewSubmitBtn → ExamSummary → confirm dialog → Kumpulkan Ujian → assertSubmitSuccess.
    - Comment menjelaskan D-15 exclude semantics + AuditLog manual verify.
    - Import block TIDAK di-modify (single source of truth dari Task 2).
    - TS compile zero error.
    - Placeholder removed.
    - Setelah Task 5 selesai, semua 4 helper yang di-import di Task 2 terpakai (no unused imports remaining).
  </acceptance_criteria>
  <done>
    Test 313.7 fully migrated. Semua 7 test FLOW 313 sekarang punya body lengkap. Semua 4 helper import terpakai → no TS unused-import warning.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Annotate `313-UAT.md` pre-conditions block dengan Phase 313.1 update note + extended verification SQL</name>
  <files>.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md</files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-PATTERNS.md (Section 5 line 405-442 — verbatim annotation block)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md line 22-42 (existing pre-conditions block)
  </read_first>
  <action>
Buka `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md`. Locate pre-conditions block (sekitar line 22-33 — bullet "Wave 0 fixtures: jalankan ...").

**Replace SQL block existing** (yang cuma SELECT Sessions) dengan SQL extended yang juga verify PkgCount + QCount. Verbatim dari `313.1-PATTERNS.md` line 428-441:

```markdown
- Wave 0 fixtures: jalankan `.planning/seeds/313-timer-fixtures.sql` di SSMS / sqlcmd terhadap DB lokal — verify 7 row dengan title prefix `Phase 313 Timer Fixture` muncul:
  ```sql
  SELECT s.Id, s.Title, s.AssessmentType, s.StartedAt,
         DATEDIFF(MINUTE, s.StartedAt, SYSUTCDATETIME()) AS ElapsedMinutes,
         (SELECT COUNT(*) FROM AssessmentPackages WHERE AssessmentSessionId = s.Id) AS PkgCount,
         (SELECT COUNT(*) FROM PackageQuestions pq INNER JOIN AssessmentPackages ap
              ON pq.AssessmentPackageId=ap.Id WHERE ap.AssessmentSessionId=s.Id) AS QCount
  FROM AssessmentSessions s
  WHERE s.Title LIKE 'Phase 313 Timer Fixture%'
  ORDER BY s.Title;
  ```
  Expected: 7 row, semua dengan `PkgCount=1` dan `QCount=3` (Phase 313.1 gap closure).

> **Phase 313.1 update (2026-05-08):** Seed sekarang self-contained (Package+Question+Option auto-INSERT). Sebelumnya UAT 7-step blocked karena fixture tidak punya AssessmentPackages → `/CMP/StartExam` redirect ke `/CMP/Assessment` dengan TempData "Sesi ujian ini tidak memiliki paket soal". Re-run seed setelah pull commit 313.1.
```

**Preserve** semua bullet pre-conditions lainnya (login fixture credential, app build status, dll). Cuma modify SQL block + tambah blockquote update.
  </action>
  <verify>
    <automated>grep -c "Phase 313.1 update (2026-05-08)" .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md</automated>
    Expected: returns 1.

    Spot-checks:
    - `grep -c "PkgCount" .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md` returns >= 2 (di SQL + di "Expected" text)
    - `grep -c "QCount" .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md` returns >= 2
  </verify>
  <acceptance_criteria>
    - Pre-conditions block di `313-UAT.md` di-update dengan SQL verification extended (PkgCount + QCount).
    - Blockquote `> Phase 313.1 update (2026-05-08)` ditambahkan setelah SQL block.
    - Expected text "7 row, semua dengan PkgCount=1 dan QCount=3" ditambahkan.
    - Bullet pre-conditions lain (credential, app build) preserved.
  </acceptance_criteria>
  <done>
    UAT script di-annotate; UAT operator manual tahu seed sekarang self-contained.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: TS compile final + Playwright list verify 7 tests + headed smoke run subset (313.1 + 313.2)</name>
  <files></files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-VALIDATION.md (Sampling Rate "After Wave 1 (Playwright finalize)")
    - tests/e2e/exam-taking.spec.ts (Tasks 2-5 output)
    - tests/e2e/helpers/exam313.ts (Task 1 output)
  </read_first>
  <what-built>
    Tasks 1-6 sudah:
    - Created `tests/e2e/helpers/exam313.ts` dengan 4 function exports.
    - Updated 7 test bodies di `tests/e2e/exam-taking.spec.ts` (313.1-313.7) dengan flow assertion lengkap.
    - Single import block (Task 2) hardcode 4 helper sekaligus — Task 3-5 tidak modify import.
    - Annotated `313-UAT.md` pre-conditions dengan Phase 313.1 update.
  </what-built>
  <how-to-verify>
**Step 7.1 — TypeScript compile final:**

```
cd tests && npx tsc --noEmit
```

Expected: zero errors. Kalau error, fix sebelum lanjut.

**Step 7.2 — Playwright test enumeration:**

```
cd tests && npx playwright test --grep "Phase 313" --list
```

Expected output: 7 tests listed:
- 313.1 - Manual + before-time + Online → submit OK (regression)
- 313.2 - Manual + after-time (in grace) + Online → BLOCKED + AuditLog SubmitExamBlocked
- 313.3 - Auto + after-time (in grace) + Online → submit OK (Tier 2 grace covers)
- 313.4 - Auto + after-grace + Online → BLOCKED Tier 2 (existing preserved)
- 313.5 - Manual + after-time + PreTest → BLOCKED (Tier-1)
- 313.6 - Manual + after-time + PostTest → BLOCKED (Tier-1)
- 313.7 - Manual + after-time + Manual type → submit OK (D-15 exclude verify)

**Step 7.3 — Pre-flight: pastikan dotnet running + DB lokal punya seed extended:**

Terminal terpisah:
```
dotnet build && dotnet run
```

Expected: app listening di `http://localhost:5277`.

Verify seed sudah di-apply (Plan 01 sudah selesai):
```
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -Q "SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE 'Phase 313 Timer Fixture%';"
```
Expected: 7.

**Step 7.4 — Headed smoke run subset (313.1 + 313.2 — happy path + Tier-1 reject):**

```
cd tests && npx playwright test --grep "313\\.[12]" --reporter=list --headed
```

Expected: 2 tests PASS. (Headed mode — tester bisa observe browser untuk catch flake atau timeout issue.)

Bila pass, test 313.1 menunjukkan:
- Login → Assessment list → click Resume → masuk StartExam → fill radio Pilihan A → Review → ExamSummary → Kumpulkan Ujian (confirm dialog accept) → Results.

Test 313.2 menunjukkan:
- Login → Assessment list → click Resume → server redirect StartExam → ExamSummary → JS retry POST → server Tier-1 reject 302 → StartExam dengan banner `.alert-danger` "Waktu ujian Anda sudah habis...".

**Step 7.5 — Optional full FLOW 313 run (kalau Step 7.4 PASS):**

```
cd tests && npx playwright test --grep "Phase 313" --reporter=list
```

Expected: 7/7 PASS. Runtime estimasi 30-60 detik (per VALIDATION.md).

**Tunggu user konfirmasi:** balas "approved" kalau Step 7.1-7.5 semua PASS, atau jelaskan issue (mis. "test 313.4 timeout di waitForURL — banner tidak muncul").

**Catatan:** Kalau test 313.2/313.5/313.6 fail karena banner tidak match, double-check banner literal di Controllers/CMPController.cs:4599 — kemungkinan tester pull commit terbaru yang ubah copy. Update `tests/e2e/helpers/exam313.ts` `assertTier1Reject` dengan literal terbaru (substring tetap robust).

Kalau test 313.4 fail karena Tier-2 banner tidak muncul:
1. Cek di browser headed: apakah JS retry handler intercept (custom warning) atau server side banner muncul?
2. Update regex di `assertTier2Reject` kalau perlu.
3. Atau switch ke `assertSubmitSuccess` kalau Plan 03 Phase 313 logic ternyata accept Tier-2 grace pada fixture ini (verify dengan SQL ElapsedSeconds vs allowed+grace).
  </how-to-verify>
  <action>Lihat blok <how-to-verify> — eksekusi 5 langkah berurutan: (7.1) `cd tests && npx tsc --noEmit` zero error; (7.2) `cd tests && npx playwright test --grep "Phase 313" --list` returns 7 tests; (7.3) pre-flight `dotnet run` di port 5277 + verifikasi seed sudah di-apply (Plan 01 dependency, COUNT(*) AssessmentSessions WHERE Title LIKE 'Phase 313 Timer Fixture%' = 7); (7.4) headed smoke run subset `cd tests && npx playwright test --grep "313\.[12]" --reporter=list --headed` — 313.1 + 313.2 PASS; (7.5) optional full FLOW 313 run, target 7/7 PASS.</action>
  <verify>
    <automated>cd tests && npx tsc --noEmit && npx playwright test --grep "Phase 313" --list</automated>
    Expected: TS compile zero error + 7 tests enumerated.
  </verify>
  <done>
    TS compile pass; Playwright list returns 7 tests (bukan SKIP); headed smoke run 313.1 + 313.2 PASS dengan banner D-01 verified; (optional) full FLOW 313 7/7 PASS; user approve untuk close Plan 02.
  </done>
  <resume-signal>Type "approved" untuk close Plan 02, atau jelaskan failure detail untuk debug.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner → app lokal (localhost:5277) | Test driver simulate browser, kirim form POST + redirect chain. Trust boundary terhadap server: server harus enforce Tier-1/Tier-2 logic correctly. |
| Test assertion → banner DOM | Banner literal di-render server-side via TempData → Razor → DOM; brittleness risk kalau copy diganti tanpa update test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313.1-06 | T (Tampering) | Banner copy drift di future PR (mis. UX team rename) | mitigate | Pakai `toContainText` substring match (bukan exact full-string). Substring `'Waktu ujian Anda sudah habis'` tahan punctuation/tail variation. Documented di RESEARCH.md "Banner Text Literals". |
| T-313.1-07 | D (Denial of Service) | Test flaky karena timing race (JS retry vs Playwright assertion) | mitigate | Timeout bumped 30s untuk SubmitExam (D-22), accommodates 1+2+4=7s backoff + processing. `waitForURL` chain instead of `waitForTimeout` polling. |
| T-313.1-08 | I | Test data isolation — test sequence stateful (313.1 submit creates Result row → test re-run dependency) | accept (per D-19) | Rely on existing seed wipe-and-insert idempotency. Re-run seed sebelum next test session = standard workflow per 313-UAT.md. Tidak tambah `test.afterEach` cleanup (D-20). |
| T-313.1-09 | T | Modal `data-bs-backdrop="static"` block radio click (Pitfall 3) | mitigate | Modal dismiss pattern (`#resumeConfirmBtn` click + waitForTimeout) di test 313.1 + 313.7. |
| T-313.1-10 | D | Confirm dialog stuck Playwright test (Pitfall 4) | mitigate | `page.once('dialog', dialog => dialog.accept())` registered SEBELUM click di test 313.1 + 313.7. |
| T-313.1-11 | T | Selector substring match cross-pollutes (Phase 312 WR-03) | mitigate | regex anchor `^\\s*${escapeRegex(...)}\\s*` di `clickResumeForFixture` helper. |
| T-313.1-13 | T | Import drift (Task 3-5 modify import berbeda dari Task 2 → conflict) | mitigate | Konsolidasi 4 helper import di Task 2 (single source of truth). Task 3-5 explicit asumsi import sudah ada — tidak modify import block. Review verify `grep -c "import.*helpers/exam313"` returns 1 setelah semua task selesai. |
</threat_model>

<verification>
**Per-task automated verifies sudah ada di setiap task. Plan-level final check (Task 7):**

- TS compile pass: `cd tests && npx tsc --noEmit` exit 0.
- Playwright list returns 7 tests untuk grep "Phase 313".
- Headed smoke run subset (313.1 + 313.2) PASS dengan banner D-01 verified.
- Optional full FLOW 313 run 7/7 PASS.
- `git diff tests/e2e/exam-taking.spec.ts` shows 7 test body replacements + 1 import statement added (4 helper sekaligus, single block).
- `git diff tests/e2e/helpers/exam313.ts` shows new file created (4 function exports).
- `git diff .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md` shows pre-conditions block annotation + extended SQL.
</verification>

<success_criteria>
1. Helper module `tests/e2e/helpers/exam313.ts` exists dengan 4 function exports + TS compile clean (Task 1).
2. 7 test bodies (313.1-313.7) di `exam-taking.spec.ts` migrated dari placeholder ke flow assertion lengkap (Tasks 2-5).
3. Banner literal D-01 (Tier-1 substring) + Tier-2 regex match copy verbatim dari Controllers/CMPController.cs (Tasks 1, 3, 4).
4. Pitfall mitigations applied: regex anchor (Pitfall 5), modal dismiss (Pitfall 3), dialog handler (Pitfall 4), waitForURL bukan polling.
5. `313-UAT.md` annotated dengan Phase 313.1 update + extended SQL (Task 6).
6. TS compile zero error final (Task 7 step 7.1).
7. Playwright list returns 7 tests (Task 7 step 7.2).
8. Headed smoke run 313.1 + 313.2 PASS (Task 7 step 7.4) — bukti banner D-01 + Tier-1 reject end-to-end working.
9. (Optional) Full FLOW 313 run 7/7 PASS (Task 7 step 7.5).
10. **Single import block** (Task 2 hardcode 4 helper) — `grep -c "import.*helpers/exam313" tests/e2e/exam-taking.spec.ts` returns exactly 1 di setiap stage Task 2-5.
</success_criteria>

<output>
After completion, create `.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-02-SUMMARY.md` dengan:
- Files modified (path + before/after summary).
- Helper file structure (4 function exports + escapeRegex private).
- Test migration table (313.1-313.7 → helper function used).
- Banner literal verification (D-01 + Tier-2 source citation).
- Pitfall mitigation evidence.
- Playwright run result (7/7 PASS atau detail kegagalan).
- Single import block evidence (`grep -c "import.*helpers/exam313"` = 1).
- Open issues (e.g., AuditLog Playwright assertion deferred per D-19).
- Ready untuk: re-run manual UAT 7-step di 313-UAT.md + restore snapshot DB → mark journal entry `cleaned`.
</output>
