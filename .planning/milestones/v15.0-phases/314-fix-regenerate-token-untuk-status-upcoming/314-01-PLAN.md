---
phase: 314-fix-regenerate-token-untuk-status-upcoming
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md
  - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md
autonomous: false
requirements: [TKN-01]
must_haves:
  truths:
    - "Bug RegenerateToken untuk Status=Upcoming + IsTokenRequired=true + 0 worker terbukti reproducible di Dev environment"
    - "Stacktrace exception dari server log Dev tertangkap dan terdokumentasi"
    - "5 query DB sample (D-39) dieksekusi dan hasilnya tertulis sebagai data shape baseline"
    - "4 hipotesis (NRE Schedule.Date / AuditLog FK / Concurrency / Frontend handler) di-verifikasi sebagai CONFIRMED, RULED OUT, atau INCONCLUSIVE berdasarkan stacktrace + data shape"
    - "Plan 02 tahu apakah perlu D-38 Schedule MinValue guard (berdasarkan Query (a) result)"
    - "CONTEXT.md D-23 wording sudah direvisi sesuai RESEARCH.md D-24 finding (token TIDAK invalidate worker session aktif)"
  artifacts:
    - path: ".planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md"
      provides: "Reproduction Evidence + Stacktrace Evidence + Data Shape Baseline + Root Cause section"
      contains: "## Reproduction Evidence"
    - path: ".planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md"
      provides: "Hipotesis tabel finalized (CONFIRMED/RULED OUT/INCONCLUSIVE)"
      contains: "## Root Cause"
    - path: ".planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md"
      provides: "D-23 wording revised sesuai RESEARCH D-24 finding"
      contains: "TIDAK akan invalidate sesi"
  key_links:
    - from: "RESEARCH.md § Root Cause"
      to: "Plan 02 backend patch decision (Schedule MinValue guard, primary fix)"
      via: "evidence-based hipotesis verdict"
      pattern: "CONFIRMED|RULED OUT|INCONCLUSIVE"
    - from: "RESEARCH.md § Data Shape Baseline Query (a)"
      to: "Plan 02 D-37/D-38 conditional guard decision"
      via: "COUNT > 0 → include guard, COUNT = 0 → skip guard"
      pattern: "Schedule = '0001-01-01'"
---

<objective>
Plan 01 = investigative phase Phase 314. Reproduce bug RegenerateToken di environment Dev (URL `http://10.55.3.3/KPB-PortalHC` per D-01) sesuai trigger condition `Status='Upcoming' + IsTokenRequired=true + 0 worker yang sudah masuk ujian`, capture stacktrace dari server log Dev (D-02), eksekusi 5 SQL sample queries (D-39 a-e) untuk validate data shape baseline, finalize tabel 4 hipotesis di RESEARCH.md (CONFIRMED/RULED OUT/INCONCLUSIVE) dengan root cause + fix proposal mapping. Sebagai bagian closure investigasi, revisi wording D-23 di CONTEXT.md sesuai RESEARCH §D-24 finding (token TIDAK invalidate sesi worker yang sudah berjalan) dan finalize keputusan conditional D-37/D-38 berdasarkan Query (a) result.

Purpose: Stacktrace + data baseline = ground truth yang menggantikan tebakan. Plan 02 baru boleh patch root cause CONFIRMED + tambah defensive guards untuk hipotesis lain. Tanpa output Plan 01, Plan 02 buta tentang mana 4 hipotesis yang harus di-fix vs mana yang sudah RULED OUT.

Output: RESEARCH.md updated dengan 4 section baru (Reproduction Evidence + Stacktrace Evidence + Data Shape Baseline + Root Cause finalized) + CONTEXT.md D-23 wording revised + screenshot/log artifacts di phase folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-VALIDATION.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md
@docs/SEED_WORKFLOW.md
@Controllers/AssessmentAdminController.cs
@Models/AssessmentSession.cs
@Models/AuditLog.cs

<interfaces>
<!-- Existing endpoint signature (TIDAK diubah di Plan 01 — investigation only) -->

From Controllers/AssessmentAdminController.cs:2424-2475:
```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> RegenerateToken(int id)
{
    var assessment = await _context.AssessmentSessions.FindAsync(id);
    if (assessment == null)
        return Json(new { success = false, message = "Assessment not found." });
    if (!assessment.IsTokenRequired)
        return Json(new { success = false, message = "This assessment does not require a token." });

    try
    {
        var newToken = GenerateSecureToken();
        var siblings = await _context.AssessmentSessions
            .Where(a => a.Title == assessment.Title
                     && a.Category == assessment.Category
                     && a.Schedule.Date == assessment.Schedule.Date)
            .ToListAsync();
        foreach (var sibling in siblings) {
            sibling.AccessToken = newToken;
            sibling.UpdatedAt = DateTime.UtcNow;
        }
        await _context.SaveChangesAsync();
        // ... AuditLog write
        return Json(new { success = true, token = newToken, message = "Token regenerated successfully." });
    }
    catch (Exception ex) {
        var logger = HttpContext.RequestServices.GetRequiredService<ILogger<AssessmentAdminController>>();
        logger.LogError(ex, "Error regenerating token");
        return Json(new { success = false, message = "Gagal regenerate token. Silakan coba lagi." });
    }
}
```

From Models/AssessmentSession.cs:
```csharp
public int Id { get; set; }
public string Title { get; set; }
public string Category { get; set; }
public DateTime Schedule { get; set; }       // line 18 — non-nullable
public DateTime? StartedAt { get; set; }     // line 40 — used for hasStarted (D-19)
public bool IsTokenRequired { get; set; }
public string? AccessToken { get; set; }
public string Status { get; set; }            // values: Upcoming / Open / Completed / Cancelled
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Repro bug di URL Dev (D-01) + capture exception alert + Network response</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md (§ Reproduction Steps + § Hipotesis Tabel — pre-repro)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-01, D-02)
    - CLAUDE.md (Develop Workflow — Lokal → Dev → Prod, repro di Dev URL `http://10.55.3.3/KPB-PortalHC`)
    - C:\Users\Administrator\.claude\projects\C--Users-Administrator-OneDrive---PT-Pertamina--Persero--Desktop-PortalHC-KPB\memory\reference_dev_credentials.md (admin@pertamina.com fixture)
  </read_first>
  <what-built>
    Investigation manual — repro bug RegenerateToken di environment Dev sesuai trigger condition `Status=Upcoming + IsTokenRequired=true + 0 worker yang sudah masuk ujian`. Capture: (a) exception alert text dari browser, (b) DevTools Network tab response body untuk POST `/Admin/RegenerateToken/{id}`, (c) screenshot UI alert + Network panel.
  </what-built>
  <how-to-verify>
    1. Buka browser, navigate ke `http://10.55.3.3/KPB-PortalHC` (Dev environment)
    2. Login sebagai admin@pertamina.com (kredensial lihat reference_dev_credentials.md)
    3. Navigate ke `/Admin/CreateAssessment` (atau `/KPB-PortalHC/Admin/CreateAssessment`)
    4. Buat assessment baru dengan field:
       - Title: `Phase 314 Repro Token Bug`
       - Category: `Assessment OJT`
       - Type: `Standard` (atau `Online` — sesuai existing type yang punya IsTokenRequired option)
       - Schedule: H+1 atau H+2 dari today (pastikan Status=Upcoming setelah save)
       - Duration: 30 menit
       - ExamWindowCloseDate: H+3 (lebih jauh dari Schedule)
       - **IsTokenRequired: TRUE** (centang checkbox)
       - Peserta: pilih hanya 1 user (admin@pertamina.com sendiri kalau eligible, atau worker test 1 orang) — pastikan SETELAH save peserta TIDAK login dan TIDAK masuk ujian (StartedAt = null)
    5. Save assessment, verify Status = `Upcoming` + IsTokenRequired = true + 0 StartedAt
    6. Navigate ke `/Admin/AssessmentMonitoring`, cari row dengan title `Phase 314 Repro Token Bug`
    7. Buka DevTools → Network tab → filter `RegenerateToken`
    8. Klik tombol `Regenerate Token` (`.btn-regenerate-token`) di row tersebut
    9. Konfirm dialog `Regenerate token untuk assessment ini?` → klik OK
    10. CAPTURE:
        - **Screenshot** browser alert window (text alert) → save ke `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/01-alert.png`
        - **Screenshot** DevTools Network panel `RegenerateToken` request: status code, response headers, response body → save ke `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/02-network.png`
        - **Copy text** response body (kalau JSON: `{success, message}`; kalau HTML 5xx page: full HTML <title>) → simpan ke `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/03-response-body.txt`
    11. Append ke RESEARCH.md section baru `## Reproduction Evidence` dengan struktur:
        ```markdown
        ## Reproduction Evidence

        **Environment:** Dev (`http://10.55.3.3/KPB-PortalHC`)
        **Date:** 2026-05-08
        **Tester:** {nama}
        **Trigger condition:** Status=Upcoming + IsTokenRequired=true + 0 StartedAt

        ### Steps executed
        1. ... (numbered steps actual yang dijalankan)

        ### Captured artifacts
        - Alert text: `{verbatim alert text}` (e.g. "Gagal regenerate token. Periksa koneksi jaringan.")
        - HTTP status: `{200|400|500|...}`
        - Response body (raw): `{verbatim text/json/html}`
        - Screenshots: `repro-evidence/01-alert.png`, `repro-evidence/02-network.png`

        ### Observations
        - Apakah alert text sesuai existing handler generic catch (pre-D-07)?
        - Apakah response body JSON `{success:false, message:...}` atau HTML 5xx page?
        - Apakah Status code 200 (controller catch) atau 500 (unhandled)?
        ```
    Acceptance: section `## Reproduction Evidence` exists di RESEARCH.md dengan minimal 3 captured artifact + observations bullet ≥ 3.
  </how-to-verify>
  <action>
    Manual investigation. JANGAN edit kode di Dev (CLAUDE.md). Hanya capture + dokumentasikan.

    Kalau alert text di Dev sudah berubah (someone applied partial fix) — note di Observations bahwa pre-D-07 generic handler maybe sudah replaced. Tetap lanjut.

    Kalau bug TIDAK reproduce (success di Dev) — PENTING: ini SIGNAL bahwa root cause environmental (e.g. data spesifik di-fix via script IT, atau bug muncul hanya untuk subset assessments tertentu). Buat fixture baru dengan variation: (a) PrePost type (multi-sibling), (b) Online type (single-sibling), (c) tanpa peserta, (d) dengan 1 peserta belum start. Iterate sampai bug muncul ATAU 4 variation pass dengan reasonable confidence.

    Output ke RESEARCH.md `## Reproduction Evidence` section.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/01-alert.png` ada (file size > 0)
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/02-network.png` ada
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/03-response-body.txt` ada
    - `grep "## Reproduction Evidence" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` returns ≥ 1 match
    - Section berisi minimal: Steps executed list, Captured artifacts bullet (3 file path), Observations bullet (≥ 3 item)
    - Verify: `dotnet build` lokal masih hijau (no code change yet) — `dotnet build` exit 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^## Reproduction Evidence" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md &amp;&amp; ls .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/ &amp;&amp; dotnet build --nologo --verbosity quiet</automated>
  </verify>
  <done>RESEARCH.md `## Reproduction Evidence` section present dengan repro steps + captured artifacts (3 file) + observations. dotnet build pass.</done>
  <resume-signal>Type "approved" atau describe issues (bug tidak reproduce / akses Dev tidak ada / dst)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture server log Dev exception (D-02) — full stacktrace</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md (§ Reproduction Evidence dari Task 1 — timestamp + session ID dari URL)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-02 — capture exception dari server log Dev)
    - CLAUDE.md (Develop Workflow — promosi Dev = tanggung jawab Team IT, jadi log access via koordinasi IT)
  </read_first>
  <what-built>
    Stacktrace ground truth dari server log Dev untuk session repro Task 1. Pinpoint exception type + line number + inner exception. Output: section `## Stacktrace Evidence` di RESEARCH.md.
  </what-built>
  <how-to-verify>
    1. Identifikasi timestamp Task 1 repro (lihat Network panel screenshot atau RESEARCH `## Reproduction Evidence` section)
    2. Identifikasi session ID dari URL `/Admin/RegenerateToken/{id}` (id = AssessmentSessions.Id integer)
    3. Akses server log Dev — opsi:
       - **Opsi A:** Kalau ASP.NET Core logging file-based (`Logs/app-*.log` atau path custom), koordinasi dengan Team IT untuk akses path log file Dev (read-only)
       - **Opsi B:** Kalau Application Insights aktif, query via Azure Portal dengan filter timestamp ± 1 menit dari Task 1 + filter `RoleName = KPB-PortalHC` + severity Error
       - **Opsi C:** Kalau Event Viewer Windows (IIS hosted), Application log filter Source = ASP.NET Core 8.0
       - **Opsi D (fallback):** Kalau log Dev tidak accessible dalam 30 menit, request Team IT export log file Dev untuk window 5 menit sekitar Task 1 timestamp via support ticket
    4. Cari log entry dengan keyword `RegenerateToken` AND session ID dari Task 1. Existing logging line 2471-2472:
       ```
       _logger.LogError(ex, "Error regenerating token");
       ```
       Note: existing log message generic — stacktrace adalah signal yang penting.
    5. CAPTURE full stacktrace:
       - Exception type (e.g. `System.NullReferenceException`, `Microsoft.EntityFrameworkCore.DbUpdateException`, `Microsoft.Data.SqlClient.SqlException`)
       - Exception message
       - **Stacktrace lengkap** (semua frame, dari atas ke `RegenerateToken` method)
       - Inner exception (kalau ada — penting untuk DbUpdateException → unwrap SqlException)
       - Save raw text ke `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/04-stacktrace.txt`
    6. Append ke RESEARCH.md `## Stacktrace Evidence` section:
       ```markdown
       ## Stacktrace Evidence

       **Source:** {Opsi A/B/C/D — path/Application Insights query/Event log}
       **Timestamp:** {ISO timestamp dari log entry}
       **Session ID matched:** {id integer dari URL}

       ### Exception type & message
       ```
       {ExceptionType}: {Message}
       ```

       ### Stacktrace
       ```
       {full stacktrace verbatim}
       ```

       ### Inner exception (if any)
       ```
       {InnerExceptionType}: {InnerMessage}
       {inner stacktrace}
       ```

       ### Analysis
       - Top frame mengarah ke line: `{file}:{line}` (e.g. `Controllers/AssessmentAdminController.cs:2447` — Schedule.Date access)
       - Hipotesis matched (preliminary): {1 / 2 / 3 / 4 dari pre-repro tabel}
       ```
    7. Kalau Opsi D (request IT) — note di RESEARCH bahwa Task 2 *partially blocked pending IT response* dan tunggu file log dari IT; SET status `## Stacktrace Evidence: PARTIAL` dengan timestamp permintaan IT.
  </how-to-verify>
  <action>
    Investigation manual via koordinasi Team IT. JANGAN edit log file atau kode di Dev.

    **Penting (anti-bias dari RESEARCH):** Pre-repro tabel memprediksi root cause = Hipotesis 4 (Frontend handler generic .catch() menyembunyikan server message). Stacktrace di sini akan validate prediksi atau falsify-nya. JANGAN bias narrative ke Hipotesis 4 — record verbatim apa yang log tunjukkan.

    Kalau stacktrace **TIDAK ada** (tidak ada error log untuk timestamp Task 1) — itu sendiri adalah evidence kuat untuk Hipotesis 4 (server return 200 dengan `{success:false, message:...}`, frontend yang fail propagate). Note `## Stacktrace Evidence: NONE — server tidak throw exception, root cause client-side` di RESEARCH dan lanjut.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/04-stacktrace.txt` ada (file size > 0 kalau ada exception, atau berisi catatan "no exception found" kalau Hipotesis 4 confirmed)
    - `grep "## Stacktrace Evidence" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` returns ≥ 1 match
    - Section berisi: Source, Timestamp, Exception type & message OR explicit "NONE — server tidak throw exception" note, Analysis bullet (≥ 2 item)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^## Stacktrace Evidence" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md &amp;&amp; test -f .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/04-stacktrace.txt</automated>
  </verify>
  <done>RESEARCH.md `## Stacktrace Evidence` section present dengan exception type + stacktrace OR explicit "NONE" finding. File 04-stacktrace.txt ada.</done>
  <resume-signal>Type "approved" atau describe issues (log tidak accessible / IT belum respond / dst)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Eksekusi 5 SQL D-39 sample queries di DB Dev — Data Shape Baseline</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-39 — 5 queries a-e)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md (§ Data Shape Baseline — placeholder section kalau ada, kalau tidak akan dibuat)
    - Models/AssessmentSession.cs (Title/Category/Schedule/Status/IsTokenRequired/StartedAt schema)
    - CLAUDE.md (JANGAN edit DB Dev — read-only queries only)
  </read_first>
  <what-built>
    5 SQL sample queries dieksekusi di DB Dev (READ-ONLY) untuk validate matching/Schedule/Status assumptions. Output: section `## Data Shape Baseline (D-39)` di RESEARCH.md dengan tabel hasil.
  </what-built>
  <how-to-verify>
    1. Akses DB Dev via SSMS atau DBeaver dengan **read-only** credential (koordinasi Team IT). JANGAN gunakan write credential.
    2. Eksekusi 5 query verbatim — copy-paste ke `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/05-d39-queries.sql`:
       ```sql
       -- (a) Schedule MinValue check (validate D-37/D-38 conditional guard)
       SELECT COUNT(*) AS MinValueCount
       FROM AssessmentSessions
       WHERE Schedule = '0001-01-01';

       -- (b) Title duplicate sample (case/whitespace test — validate D-29 SQL Server CI_AS sufficient)
       SELECT TOP 10 Title, Category, COUNT(*) AS Cnt
       FROM AssessmentSessions
       GROUP BY Title, Category
       HAVING COUNT(*) > 1
       ORDER BY Cnt DESC;

       -- (c) Category NULL check (validate D-31 [Required] non-null assumption)
       SELECT COUNT(*) AS CategoryNullCount
       FROM AssessmentSessions
       WHERE Category IS NULL;

       -- (d) Trigger condition distribution (Status='Upcoming' + IsTokenRequired=1 + 0 StartedAt)
       SELECT
         a.Status,
         a.IsTokenRequired,
         COUNT(*) AS RowCount,
         SUM(CASE WHEN EXISTS (
           SELECT 1 FROM UserAssessmentSessions u
           WHERE u.AssessmentSessionId = a.Id AND u.StartedAt IS NOT NULL
         ) THEN 1 ELSE 0 END) AS WithStartedCount
       FROM AssessmentSessions a
       WHERE a.Status = 'Upcoming' AND a.IsTokenRequired = 1
       GROUP BY a.Status, a.IsTokenRequired;

       -- (e) Sibling group size statistics
       SELECT
         AVG(GroupSize) AS AvgGroupSize,
         MIN(GroupSize) AS MinGroupSize,
         MAX(GroupSize) AS MaxGroupSize
       FROM (
         SELECT Title, Category, CAST(Schedule AS DATE) AS ScheduleDate, COUNT(*) AS GroupSize
         FROM AssessmentSessions
         GROUP BY Title, Category, CAST(Schedule AS DATE)
       ) AS GroupStats;
       ```
    3. Capture output TABULAR untuk setiap query. Save raw text ke `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/06-d39-results.txt`.
    4. Append ke RESEARCH.md `## Data Shape Baseline (D-39)` section:
       ```markdown
       ## Data Shape Baseline (D-39)

       **Environment:** Dev DB (read-only)
       **Date:** 2026-05-08
       **SQL file:** repro-evidence/05-d39-queries.sql
       **Raw output:** repro-evidence/06-d39-results.txt

       ### Query (a) — Schedule MinValue
       | MinValueCount |
       |---------------|
       | {N}           |

       **Verdict:** {Kalau N > 0 → "D-37/D-38 guard MANDATORY di Plan 02"; kalau N = 0 → "D-37/D-38 guard SKIP — minimal diff"}

       ### Query (b) — Title duplicates
       | Title | Category | Count |
       |-------|----------|-------|
       | ...   | ...      | ...   |

       **Verdict:** {Kalau duplicates dengan whitespace/case variation → "D-29 risk LOW — CI_AS handles, no change"; kalau zero duplicates → "D-29 risk NONE"}

       ### Query (c) — Category NULL
       | CategoryNullCount |
       |-------------------|
       | {N}               |

       **Verdict:** {Kalau N > 0 → "D-31 risk: existing schema ada NULL Category, defensive matching needed"; kalau N = 0 → "D-31 [Required] enforced"}

       ### Query (d) — Trigger condition distribution
       | Status | IsTokenRequired | RowCount | WithStartedCount |
       |--------|-----------------|----------|------------------|
       | Upcoming | 1 | {N} | {M} |

       **Verdict:** Trigger condition population = (RowCount - WithStartedCount) rows match `Upcoming + IsTokenRequired + 0 StartedAt`. Kalau > 0, fixture seed feasible di Dev. Kalau = 0, repro Task 1 harus create fixture sendiri.

       ### Query (e) — Sibling group size statistics
       | AvgGroupSize | MinGroupSize | MaxGroupSize |
       |--------------|--------------|--------------|
       | {avg}        | {min}        | {max}        |

       **Verdict:** Min=0 → invariant violation (D-33 0-row guard MANDATORY). Min=1 → single-sibling normal (Online). Max > 5 → multi-sibling (PrePost groups), test harus cover > 1 sibling case.
       ```
    5. Verify mathematical sense: query (a) result type INT, (b) max 10 rows, (c) INT, (d) 1-2 row, (e) 1 row 3 cols.
  </how-to-verify>
  <action>
    Read-only investigation. JANGAN UPDATE/DELETE/INSERT di DB Dev (CLAUDE.md). Kalau IT request use staging clone — switch ke staging DB dengan same schema.

    **Hasil shape baseline drives 3 keputusan downstream:**
    1. Query (a) → Plan 02 D-37/D-38 conditional inclusion (CONTEXT.md sudah lock conditional)
    2. Query (c) → Plan 02 D-31 risk assessment (kalau Category NULL exists, suggest defensive null-coalesce — tetapi per CONTEXT D-31 audit-only di RESEARCH, tidak code change)
    3. Query (e) → Plan 02 D-33 0-row guard mandatory check (Min=0 tabular)
  </action>
  <acceptance_criteria>
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/05-d39-queries.sql` ada
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/06-d39-results.txt` ada
    - `grep "## Data Shape Baseline" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` returns ≥ 1 match
    - Section berisi 5 sub-section (Query (a)-(e)) masing-masing dengan tabel hasil + Verdict
    - 5 verdict explicit (no `{N}` placeholder remaining)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^## Data Shape Baseline" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md &amp;&amp; grep -c "### Query (" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md | awk '$1 &gt;= 5 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>RESEARCH.md `## Data Shape Baseline (D-39)` present dengan 5 query result + 5 verdict no placeholder. Files 05/06 di repro-evidence/ exists.</done>
  <resume-signal>Type "approved" atau describe issues (DB Dev access denied / query timeout / dst)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Finalize RESEARCH.md hipotesis tabel — CONFIRMED/RULED OUT/INCONCLUSIVE per evidence</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md (§ Reproduction Evidence dari Task 1 + § Stacktrace Evidence dari Task 2 + § Data Shape Baseline dari Task 3 + § Hipotesis Tabel pre-repro)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-03 format Standard, D-05 defensive coverage philosophy, D-12 specific exception types)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#H composite full method template — adjust kalau root cause di luar prediksi)
  </read_first>
  <action>
    Edit `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md`:

    1. **Locate § Hipotesis Tabel section** (pre-repro tabel 4 hipotesis dengan kolom Evidence/Status preliminary).
    2. **Update tabel** dengan finalize verdict berdasarkan evidence dari Task 1+2+3:

       Format tabel finalized (**WAJIB pakai row label `H1`/`H2`/`H3`/`H4`** untuk acceptance grep `^\| H[1-4] `):
       ```markdown
       | # | Hipotesis | Pre-repro Status | Evidence (Task 1+2+3) | Final Verdict |
       |---|-----------|------------------|------------------------|---------------|
       | H1 | NRE pada `assessment.Schedule.Date` (line 2447) — Schedule null/MinValue | PROBABLE RULED OUT | Stacktrace Top frame: `<actual frame text>`. Query (a) Schedule MinValue count = `<actual N>`. | CONFIRMED |
       | H2 | AuditLog FK violation (`ActorUserId` empty/invalid) | PROBABLE RULED OUT | Existing line 2460: `regenUser?.Id ?? ""`. Stacktrace exception type: `<actual type>`. | RULED OUT |
       | H3 | Concurrency / TOCTOU (admin regen sambil worker login) | PROBABLE RULED OUT | Stacktrace exception type: `<actual>` (kalau `DbUpdateConcurrencyException` → CONFIRMED). Query (e) sibling stats: max=`<actual max>`. | INCONCLUSIVE |
       | H4 | Frontend handler generic `.catch()` menyembunyikan server message asli | PROBABLE CONFIRMED | Reproduction alert text: `<verbatim>`. Network response: HTTP `<actual status>`, body `<actual snippet>`. Stacktrace: `<NONE atau exception summary>`. | CONFIRMED |
       ```

       **CRITICAL:** Replace SEMUA placeholder `{...}` / `{ALL_CAPS}` dengan actual evidence text dari Task 1-3. Final Verdict cell HARUS satu kata literal (`CONFIRMED`, `RULED OUT`, atau `INCONCLUSIVE`) — JANGAN biarkan bentuk `CONFIRMED / RULED OUT / INCONCLUSIVE` (multiple choice) atau `{CONFIRMED}` (placeholder). Acceptance grep `! grep -E "\| \{[A-Z]+\} \|"` akan FAIL kalau ada `{...}` placeholder tersisa.
    3. **Tambah § Root Cause section** (atau update existing):
       ```markdown
       ## Root Cause (Finalized 2026-05-08)

       **Primary:** {Hipotesis # X CONFIRMED — {brief description e.g. "Frontend `.catch()` swallow server message Y dengan generic alert"}.

       **Evidence chain:**
       1. Reproduction (Task 1): {alert text}
       2. Stacktrace (Task 2): {NONE / Exception type at line N}
       3. Data shape (Task 3): {Query (a) count, Query (e) min/max — relevant baseline}

       **Secondary (defensive coverage per D-05):**
       - Hipotesis # Y INCONCLUSIVE — tetap pasang guard di Plan 02 (D-25 status block, D-33 0-row guard, D-17 transaction wrap, D-06 audit try-catch swallow, D-12 specific exception types).
       - Hipotesis # Z RULED OUT — guard tetap pasang sebagai defense-in-depth (cumulative philosophy CONTEXT.md D-05).

       ## Fix Proposal Mapping (untuk Plan 02)

       | CONTEXT.md Decision | Apply at | Justification |
       |---------------------|----------|---------------|
       | D-25 (Status block Cancelled+Completed) | Backend pre-condition | Lock per CONTEXT, regardless root cause |
       | D-33 (Sibling 0-row guard) | Backend post-query guard | {Mandatory kalau Query (e) min=0 / Defensive kalau min=1} |
       | D-17 (Transaction wrap) | Backend siblings UPDATE block | Lock per CONTEXT |
       | D-06 (Audit try-catch swallow + LogWarning) | Backend audit call | {Mandatory kalau Hipotesis 2 CONFIRMED / Defensive kalau RULED OUT} |
       | D-12 (Specific exception types) | Backend catch block | Lock per CONTEXT |
       | D-20/D-21 (Extended structured logging) | Backend catch + success | Lock per CONTEXT |
       | D-37/D-38 (Schedule MinValue guard) | Backend pre-condition | {INCLUDE kalau Query (a) > 0 / SKIP kalau 0} |
       | D-07/D-09/D-11 (Frontend error propagation) | 3 view handlers | {Mandatory kalau Hipotesis 4 CONFIRMED — primary fix; Defensive kalau RULED OUT} |
       | D-22/D-23 revised wording | 3 view handlers + warn dialog | Lock per CONTEXT, REVISED per RESEARCH §D-24 finding |
       ```
    4. **Final review:** verify masing-masing 4 hipotesis punya Final Verdict NON-NULL (CONFIRMED, RULED OUT, atau INCONCLUSIVE), dan Root Cause section ada Primary + Secondary.

    Save file. Verify dengan grep: `grep -c "Final Verdict" RESEARCH.md` ≥ 4 (per row tabel) atau header line. Verify `grep -q "## Root Cause (Finalized" RESEARCH.md`.
  </action>
  <acceptance_criteria>
    - `grep -q "## Root Cause (Finalized" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` returns 0 (match found)
    - `grep -q "## Fix Proposal Mapping" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` returns 0
    - 4 baris Hipotesis tabel di RESEARCH.md tidak punya placeholder `{...}` di kolom Final Verdict
    - **Placeholder absence (no `{...}` verdict in hipotesis tabel):** `! grep -E "\| \{[A-Z]+\} \|" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` returns 0 (NO `{ALL_CAPS}` placeholder in any tabel verdict cell)
    - **Hipotesis row count present:** `grep -cE "^\| H[1-4] " .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` returns ≥ 4 (4 hipotesis rows H1/H2/H3/H4 exist) — **NOTE:** kalau current tabel pakai format `| 1 | Hipotesis |` rename row labels ke `| H1 |` ... `| H4 |` saat finalize untuk acceptance pass; alternative: also accept format `^\| [1-4] \|.*Hipotesis` jika strukturalnya `| # | Hipotesis | ... |` — flag explicit di Task 4 action.
    - File line count RESEARCH.md tetap > 500 (no accidental delete besar) — kalau menurun > 50 lines, kemungkinan delete content lama secara tidak sengaja
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^## Root Cause (Finalized" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md &amp;&amp; grep -q "^## Fix Proposal Mapping" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md</automated>
  </verify>
  <done>RESEARCH.md hipotesis tabel finalized (4 verdict explicit), Root Cause section + Fix Proposal Mapping table present.</done>
</task>

<task type="auto">
  <name>Task 5: Update CONTEXT.md D-23 wording revision sesuai RESEARCH §D-24 finding</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-23 baseline wording line 104)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md (§D-24 Open Question Recommendation 1 — token TIDAK invalidate sesi worker yang sudah berjalan, HIGH confidence)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (S5 — BI text revised wording row D-23)
  </read_first>
  <action>
    Edit `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md`:

    1. **Locate D-23 line** (`<decisions>` section, sub-section "Open + Active Worker Behavior", around line 104):
       ```markdown
       - **D-23:** **Warning wording:** `"PERINGATAN: {N} worker sudah masuk ujian. Regenerate token akan invalidate session token mereka — mereka harus login ulang dengan token baru. Jawaban yang sudah disimpan tidak hilang. Lanjutkan?"`. Angka {N} dari sibling count `StartedAt != null`. Frontend butuh data ini — endpoint GET ringan atau inline `data-started-count` attribute saat render Monitoring view.
       ```
    2. **Replace** dengan revised wording (sesuai RESEARCH §D-24 Recommendation 1 + PATTERNS.md S5):
       ```markdown
       - **D-23 (REVISED 2026-05-08 per RESEARCH §D-24):** **Warning wording:** `"PERINGATAN: {N} worker sudah masuk ujian. Regenerate token TIDAK akan invalidate sesi mereka yang sudah berjalan — mereka tetap bisa lanjut. Tapi worker lain yang belum masuk dan punya token lama akan ditolak login. Lanjutkan?"`. Angka {N} dari sibling count `StartedAt != null`. Frontend butuh data ini — inline `data-started-count` attribute saat render Monitoring view (Claude's Discretion: GET endpoint alternatif kalau attribute tidak feasible). **Revisi alasan:** Pre-repro RESEARCH §D-24 Open Question CONFIRMED dengan HIGH confidence bahwa ASP.NET Core Identity session worker established via cookie/identity ticket — token only gate-keep initial entry via `CMPController.VerifyToken`. Wording lama (claim "akan invalidate session token mereka — login ulang") salah secara teknis dan misleading admin.
       ```
    3. **Tambah catatan di header file** (di line 4-5, setelah `**Updated:** 2026-05-08 (4 area baru, +15 decisions)`):
       ```markdown
       **Updated:** 2026-05-08 (4 area baru, +15 decisions)
       **Revised:** 2026-05-08 (D-23 wording corrected per RESEARCH §D-24 finding — token TIDAK invalidate active worker session)
       ```

    Save file. Verify dengan grep:
    - `grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" CONTEXT.md` returns 0
    - `grep -q "REVISED 2026-05-08" CONTEXT.md` returns 0
    - `grep -q "akan invalidate session token mereka — mereka harus login ulang" CONTEXT.md` returns 1 (tidak ada — wording lama replaced)
  </action>
  <acceptance_criteria>
    - `grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md` returns 0 (match found)
    - `grep -q "REVISED 2026-05-08" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md` returns 0
    - `grep -c "akan invalidate session token mereka — mereka harus login ulang" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md` returns 0 (wording lama TIDAK lagi exist)
    - **D-23 struktural verification (revisi wording end-to-end):**
      - `grep -A 1 "D-23 (REVISED" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md | grep -qE "TIDAK akan invalidate|tetap aktif|tetap bisa lanjut|cookie session"` → exit 0 (revisi marker langsung followed by accurate worker session description)
      - `grep -c "^- \*\*D-23" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md` returns 1 (only ONE D-23 row di CONTEXT.md, not duplicated — revisi REPLACE bukan APPEND)
    - **PATTERNS.md S5 reference verification (downstream consistency):**
      - `grep -q "^### S5" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md` → exit 0 (S5 section exists — verified saat revision iteration 2)
      - `grep -A 2 "Confirm warning (D-23 revised)" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md | grep -q "TIDAK akan invalidate sesi"` → exit 0 (S5 row D-23 BI text aligned dengan CONTEXT.md revisi)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md &amp;&amp; ! grep -q "akan invalidate session token mereka — mereka harus login ulang" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md</automated>
  </verify>
  <done>CONTEXT.md D-23 wording revised dengan "TIDAK akan invalidate sesi" phrasing. Wording lama removed. Header catatan revisi added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Dev server (10.55.3.3) | Investigative HTTP traffic (POST RegenerateToken). Read-only observasi, no destructive action. |
| Tester → DB Dev | Read-only SQL queries via SSMS/DBeaver. Credential read-only via koordinasi IT. |
| Tester → Server log Dev | Read-only access koordinasi IT. No log modification. |

## STRIDE Threat Register (Plan 01 — investigative)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-314-01-01 | Information Disclosure | Server log Dev (stacktrace export ke local repro-evidence/) | accept | Stacktrace umumnya tidak berisi PII (hanya internal class/method names + DB error messages). Repro-evidence/ folder adalah project-local dan committed ke git private repo. Kalau stacktrace berisi connection string (rare di .NET 8 — EF Core sanitize), redact sebelum commit. |
| T-314-01-02 | Tampering | DB Dev — accidental write via SSMS query | mitigate | Use read-only credential dari Team IT. Verify connection string Read-Only di SSMS Property panel. Query verbatim SELECT only — no UPDATE/DELETE/INSERT. |
| T-314-01-03 | Repudiation | RESEARCH.md edit history | accept | Git commit setelah Task 5 = audit trail edit. Tester record nama di RESEARCH.md `## Reproduction Evidence` section. |
</threat_model>

<verification>
- Task 1+2+3 = manual investigation, output verified via file existence + grep section header di RESEARCH.md
- Task 4+5 = file edit, output verified via grep specific text
- Plan 01 closure: `dotnet build` lokal masih hijau (no code change yet) + RESEARCH.md punya 4 section baru (Reproduction Evidence + Stacktrace Evidence + Data Shape Baseline + Root Cause finalized + Fix Proposal Mapping) + CONTEXT.md D-23 revised
</verification>

<success_criteria>
- [ ] `## Reproduction Evidence` ada di RESEARCH.md
- [ ] `## Stacktrace Evidence` ada di RESEARCH.md
- [ ] `## Data Shape Baseline (D-39)` ada di RESEARCH.md dengan 5 query verdict
- [ ] `## Root Cause (Finalized` ada di RESEARCH.md
- [ ] `## Fix Proposal Mapping` ada di RESEARCH.md
- [ ] CONTEXT.md D-23 wording = "TIDAK akan invalidate sesi mereka yang sudah berjalan"
- [ ] CONTEXT.md D-23 wording lama removed (no `mereka harus login ulang`)
- [ ] Files repro-evidence/01-alert.png, 02-network.png, 03-response-body.txt, 04-stacktrace.txt, 05-d39-queries.sql, 06-d39-results.txt all exist
- [ ] `dotnet build` lokal exit 0 (no code change yet)
</success_criteria>

<output>
After completion, create `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-01-SUMMARY.md` dengan:
- Reproduction outcome ringkas (alert text + HTTP status + stacktrace summary)
- 5 query verdict ringkas (boolean per query)
- Root cause Primary verdict (Hipotesis # X CONFIRMED)
- D-37/D-38 conditional decision (INCLUDE / SKIP per Query (a) result)
- D-23 wording revision diff
- File artifacts list (6 files di repro-evidence/)
</output>
