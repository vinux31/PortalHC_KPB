---
status: partial
phase: 315-assessment-matrix-test
source:
  - 315-01-SUMMARY.md
  - 315-02-SUMMARY.md
  - 315-03-SUMMARY.md
  - 315-04-SUMMARY.md
  - 315-05-SUMMARY.md
started: 2026-05-11T05:30:00Z
updated: 2026-05-11T06:25:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test (test infra fresh boot)
expected: Dari DB bersih, smoke run S5 selesai end-to-end. globalSetup BACKUP+seed+Layer 1 pass, globalTeardown RESTORE+Layer 4=0 pass, exit code reported.
result: pass
notes: |
  Fresh smoke run 2026-05-11T06:14:36Z. globalSetup BACKUP+seed+Layer 1 (18/18/54/144) OK; .matrix-state.json written; journal active. S5 failed (real bug page-closed, expected per Plan 04 finding). globalTeardown report flush (2 findings) + RESTORE + Layer 4 (0 row) + journal cleaned + cleanup state.json+.bak. Auto-screenshots Playwright captured di `test-results/assessment-matrix-Scenario-30ab3-.../test-failed-{1,2,3}.png`.

### 2. Report markdown shape
expected: `docs/test-reports/2026-05-11-assessment-matrix.md` punya section `## Summary`, `## Discovery findings`, `## Meta-validation results`. Per finding: severity tag + screenshot path + Expected/Actual + Hypothesis konkret. Tidak ada `TBD` placeholder.
result: pass
notes: |
  Report regenerated post smoke run. Section lengkap (Summary + Discovery findings + Meta-validation results). Per finding: Severity / Expected / Actual / Screenshot / Hypothesis. No TBD.

### 3. Hypothesis actionable quality
expected: Hypothesis tiap finding cite file:line (`Hubs/AssessmentHub.cs:134-182`, `Controllers/CMPController.cs:1569+`) + fix proposal konkret (e.g., `Promise.all([waitForURL, click])` snippet). Cukup spesifik untuk developer reproduce + mulai debug.
result: pass
notes: |
  Finding 1 cite `Controllers/CMPController.cs` SaveAnswer line 348-417. Finding 2 cite `Controllers/CMPController.cs:1569+` + fix snippet `Promise.all([page.waitForURL("**/CMP/Results/**"), page.click(...)])`. Cross-reference Plan 04 finding recurrence.

### 4. DB cleanup automated (Layer 4)
expected: Post-run `SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[[]MATRIX_TEST_2026_05_11]%'` returns 0. Teardown throw kalau != 0 (T-315-03 mitigation).
result: pass
notes: |
  Verified post-smoke: 0 rows. Teardown log `[teardown] Layer 4 OK: 0 matrix rows post-RESTORE`.

### 5. SEED_JOURNAL active→cleaned regex
expected: Tiap run tambah entry baru ke `docs/SEED_JOURNAL.md`. Status `active` saat globalSetup, regex-update ke `cleaned` saat globalTeardown sukses. 5/5 runs di Phase 315 selesai dgn `cleaned`.
result: pass
notes: |
  6/6 runs Phase 315 selesai dgn `cleaned` (5 prior + 1 fresh smoke). 1 `active` = Plan 03 baseline template (committed pre-first-run, not corresponding to actual run).

### 6. Sentinel S8/S9/S10 behavior
expected: S8 [META-AllCorrect] = peserta jawab semua benar → expect 100/100. S9 [META-AllWrong] = jawab semua salah → expect 0/0. S10 [META-CollectorCheck] = `test.fail()` annotation → sengaja gagal verify, collector wajib catat finding (sentinel-rusak kalau S10 mendadak pass).
result: blocked
blocked_by: prior-phase
reason: |
  Sentinel S8/S9/S10 belum ter-exercise di any run karena S1 atau S5 cascade fail (real app bug submit-exam page-closed). Plan 05 collector singleton lifecycle fix + spec wired correctly — tunggu Phase 316 fix bug app → re-run matrix test → sentinel akan ter-exercise. Test infra structure correct (test.fail annotation di line 362 spec.ts verified).

### 7. Continue-on-fail behavior
expected: Spec utama pakai `matrixReport.softAssert()` wrapper. Critical failure → throw `SkipScenarioError` skip sisa step skenario tapi lanjut ke skenario berikutnya. Bukan stop-on-first-fail.
result: pass
notes: |
  Smoke run S5 menampilkan `[S5] Critical fail — skip sisa step scenario` lalu test infra continue ke teardown. softAssert mechanism works. Full inter-scenario continue (S5 fail → S6 mulai) belum demonstrated end-to-end karena smoke run S5-only — deferred ke Phase 316 full run post bug fix.

### 8. ID range collision pre-check
expected: globalSetup throw kalau AssessmentSessions Id range 9001-9018 sudah terpakai sebelum seed. Defense mitigation T-315-04 + RESEARCH Q4.
result: pass
notes: |
  Code present di `tests/e2e/global.setup.ts:55-67`. Pre-check via `db.queryScalar` + `expect().toBe(0)` throw kalau collision count > 0. Smoke run 6 kali tidak trigger throw karena DB always clean (Layer 4 teardown bersih).

## Summary

total: 8
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

[none — blocked test S6 sentinel = upstream dependency (real bug app), bukan code gap]

## Acknowledged Gaps (carried forward to Phase 316)

1. **Sentinel S8/S9/S10 verification deferred** — Layer 2/3 meta-validation blocked by real app bug (S1/S5 submit-exam page-closed cascade). Test infra ready; will exercise post Phase 316 bug fix.
2. **Screenshot path mismatch** — `softAssert()` writes path `test-results/matrix-s{id}-{step}.png` to report but Playwright auto-capture writes to `test-results/{spec-dirname}/test-failed-{N}.png`. Both paths exist post run (auto-capture works; custom-path silent-fail due to page closed). Phase 316: refine softAssert defensive screenshot OR update report renderer to use Playwright default path.
3. **Full inter-scenario continue-on-fail** — softAssert mechanism verified per-scenario; full 10-block run (S1→S10) belum demonstrated end-to-end karena real bug cascade. Will validate post Phase 316.
