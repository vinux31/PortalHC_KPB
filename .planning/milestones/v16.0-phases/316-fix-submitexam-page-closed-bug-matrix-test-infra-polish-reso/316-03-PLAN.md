---
phase: 316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso
plan: 03
type: execute
wave: 0
depends_on: ["316-01", "316-02"]
files_modified:
  - tests/e2e/_throwaway-probe.spec.ts
autonomous: true
gap_closure: true
requirements: [GAP-316-1, GAP-316-2, GAP-316-3]
tags: [playwright, e2e, gap-closure, wave-0, probe, validation]

must_haves:
  truths:
    - "Probe spec demonstrasi 2 test() di 2 test.describe() terpisah — block A always-fail, block B should-run"
    - "Run probe produces output '1 failed, 1 passed' (NOT '1 failed, 1 did not run') — A2 verified"
    - "Probe spec includes synthetic 'always-fail-fast' (no page.* operation, throw immediately) untuk validate A2 boundary tanpa A1 ambiguity"
    - "Wave 0 outcome di-record di /tmp/316-w0-probe.log + summary di 316-VALIDATION.md untuk decision gate ke Wave 1"
  artifacts:
    - path: "tests/e2e/_throwaway-probe.spec.ts"
      provides: "2-block dummy spec untuk validate test.describe() boundary failure isolation di fullyParallel:false"
      contains: "test.describe('Block A'"
    - path: "/tmp/316-w0-probe.log"
      provides: "Empirical evidence run output — assert '1 failed, 1 passed' summary line"
  key_links:
    - from: "tests/e2e/_throwaway-probe.spec.ts Block A"
      to: "tests/e2e/_throwaway-probe.spec.ts Block B (independent describe)"
      via: "Playwright test.describe() boundary"
      pattern: "test.describe"
    - from: "Block A always-fail throw"
      to: "Playwright runner failure isolation"
      via: "fullyParallel: false + no spec-level mode:'serial'"
      pattern: "fullyParallel"
---

<objective>
Wave 0 validasi research assumption A2 (test.describe() boundary isolate failure di mode `fullyParallel: false`) SEBELUM commit ke (d-partial) describe restructure di Plan 05. Lakukan via throwaway 2-block probe spec — jika A2 invalid (output "1 failed, 1 did not run") → STOP, fallback ke (a-revised) alone path; jika A2 valid (output "1 failed, 1 passed") → proceed ke Wave 1+.

**Purpose:** De-risk Plan 05 restructure (terbesar LOC) dengan empirical proof di codebase ini. Per 316-RESEARCH-GAP-316-2.md Open Question Q2 + Wave 0 Gaps line 433-436. Probe spec adalah **throwaway** (NO commit kalau invalid + langsung delete setelah probe selesai sukses).

**Output:** 1 throwaway probe spec + 1 log file di /tmp + 1 decision record di 316-VALIDATION.md Wave 0 section. Probe spec di-delete setelah wave 1 commit (cleanup di Plan 06 final task).

**Critical:** Plan 03 TIDAK edit kode produksi. Pure infra probe untuk Wave 0 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH-GAP-316-2.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md
@tests/playwright.config.ts
@CLAUDE.md

<interfaces>
<!-- Playwright API surface yang dipakai di probe spec — minimal -->
From @playwright/test:
```typescript
export function test(title: string, fn: (args: any) => Promise<void>): void;
export namespace test {
  export function describe(title: string, fn: () => void): void;
}
```

From tests/playwright.config.ts (key relevant config):
- `fullyParallel: false` (line 8) — sequential within file di 1 worker
- `retries: 0` (line 9) — no retry → real failure visible
- `timeout: 60_000` (line 6) — per-test default
- `projects[chromium].dependencies: ['setup']` — probe spec akan trigger globalSetup; **HARUS pakai testMatch exclusion atau standalone command**

**Critical Concern:** Probe spec berada di `tests/e2e/` directory → akan ter-pick oleh chromium project default match. globalSetup akan execute (DB BACKUP/RESTORE Phase 315 layer). Untuk hindari overhead, **run probe via `--project=chromium --grep "_PROBE_316"` + UNSET dependencies via inline override**. Atau: jalankan probe via standalone project tanpa dependencies via `npx playwright test _throwaway-probe.spec.ts --project=chromium` dengan TEMPORARY skip global.setup via `--project=chromium` direct (akan tetap trigger setup).

**Solusi terpilih:** Jalankan probe SEBELUM globalSetup hits matrix scenarios — gunakan workaround `npx playwright test tests/e2e/_throwaway-probe.spec.ts --project=chromium --reporter=list` setelah backup `.matrix-state.json` (jika ada). Probe akan trigger globalSetup (DB backup) — penerimaan ini karena setup IDempotent dan kita restore lewat globalTeardown otomatis. Alternative: probe spec pakai `test.describe.serial.configure({mode:'default'})` skip pattern — tidak applicable karena point adalah validate NO serial.

**Final approach:** Place probe di path TERPISAH `tests/e2e/_throwaway-probe.spec.ts` (prefix `_` untuk konvensi temporary file). Override execution via custom command — bypass globalSetup via `--project=chromium` dengan SEBELUMnya verify state file exists. Probe execution overhead acceptable karena 1x run < 30s.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tulis probe spec 2-block + verifikasi describe boundary isolation</name>
  <files>tests/e2e/_throwaway-probe.spec.ts</files>
  <action>
Buat file `tests/e2e/_throwaway-probe.spec.ts` dengan content berikut. **JANGAN edit file lain. JANGAN modify playwright.config.ts. JANGAN modify global.setup.ts atau global.teardown.ts.**

```typescript
/**
 * Phase 316 Plan 03 Wave 0 — THROWAWAY probe spec.
 *
 * Tujuan: Validate research assumption A2 (316-RESEARCH-GAP-316-2.md line 470-471):
 *   "test.describe() boundary mengisolasi failure di mode `fullyParallel: false`"
 *
 * Expected output (jika A2 valid):
 *   1 failed, 1 passed
 *
 * Anti-expected (jika A2 invalid, fallback path needed):
 *   1 failed, 1 did not run
 *
 * Cleanup: File ini dihapus di Plan 06 final task. JANGAN commit jika hasil "did not run".
 * Sumber: 316-RESEARCH-GAP-316-2.md Open Question Q2 + Wave 0 Gaps line 433-436.
 */

import { test } from '@playwright/test';

// NO test.describe.configure({ mode: 'serial' }) di sini — point adalah test default mode
// dengan fullyParallel: false (config-level).

test.describe('_PROBE_316 Block A: always-fail-fast', () => {
  test('synthetic throw — no page operation', async () => {
    // Throw langsung, tidak ada Playwright wait yang bisa accumulate timer.
    // Pure synthetic untuk isolate A2 dari A1 (page.check timeout question).
    throw new Error('Phase 316 Wave 0 probe — intentional fail to test describe boundary');
  });
});

test.describe('_PROBE_316 Block B: should-run-after-A-fails', () => {
  test('synthetic pass — confirm block B executed', async () => {
    // Kalau test ini EXECUTE dan PASS → A2 VALID (describe boundary isolate failure).
    // Kalau test ini "did not run" di output summary → A2 INVALID (full halt-on-first-fail).
    console.log('[_PROBE_316] Block B executed — A2 validation: PASS');
  });
});
```

**Catatan implementasi:**
- File prefix `_throwaway-probe` untuk visibility (lihat sekilas ini bukan permanent spec)
- Komentar header EKSPLISIT bahwa file ini throwaway dan akan dihapus
- 2 describe blocks dengan name prefix `_PROBE_316` supaya grep-able
- Block A: synthetic throw (NO page.* operation) — eliminate A1 (page.check timeout) dari equation, isolate A2 saja
- Block B: synthetic pass dengan console.log marker
- NO import dari helpers (probe murni untuk Playwright runner behavior validation)

**Pastikan:**
- File path EXACT `tests/e2e/_throwaway-probe.spec.ts` (under-tests-e2e, NOT tests/_throwaway-probe.spec.ts)
- TypeScript compile clean (`cd tests && npx tsc --noEmit` exit 0)
- No edit ke playwright.config.ts atau file lain di Wave 0 ini
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit 2>&1 | tee /tmp/316-w0-tsc.log && test -f tests/e2e/_throwaway-probe.spec.ts && echo "FILE_OK"</automated>
  </verify>
  <done>File `tests/e2e/_throwaway-probe.spec.ts` exists dengan 2 test.describe() blocks (`_PROBE_316 Block A` + `_PROBE_316 Block B`). TS compile clean (exit 0). Tidak ada edit di file lain.</done>
</task>

<task type="auto">
  <name>Task 2: Jalankan probe + capture output + decision gate A2</name>
  <files>/tmp/316-w0-probe.log</files>
  <action>
Jalankan probe spec dan capture output ke log file. Lalu inspect untuk decision A2 valid/invalid.

**Step 1 — Run probe (filter ke 2 probe blocks saja via grep):**

```bash
cd tests && npx playwright test e2e/_throwaway-probe.spec.ts --project=chromium --reporter=list 2>&1 | tee /tmp/316-w0-probe.log
echo "EXIT_CODE: $?" | tee -a /tmp/316-w0-probe.log
```

**Catatan:** Run akan trigger globalSetup (DB BACKUP+seed Phase 315 matrix state file). Acceptable karena setup idempotent. globalTeardown akan RESTORE — DB tidak terdampak. Total runtime ~30-60s.

**Alternative kalau globalSetup overhead unacceptable:** Skip via `--project=chromium` dengan `dependencies: []` inline tidak tersedia di Playwright CLI. Pakai workaround: rename probe ke `tests/_probe-only.spec.ts` (OUTSIDE testDir) lalu `cd tests && npx playwright test --config=playwright.config.ts _probe-only.spec.ts`. **Tapi rekomendasi:** Terima overhead globalSetup demi simplicity — Wave 0 hanya 1x run.

**Step 2 — Parse output, deteksi pattern:**

```bash
# Cari summary line — Playwright format: "  1 failed\n  1 passed" atau "  1 failed\n  1 did not run"
grep -E "(failed|passed|did not run)" /tmp/316-w0-probe.log | tail -20
```

**Decision gate:**
- **A2 VALID** (proceed Wave 1+): Output mengandung "1 failed" + "1 passed" untuk 2 probe tests. Block B `_PROBE_316` console.log "Block B executed" muncul di log.
- **A2 INVALID** (STOP — fallback path): Output mengandung "1 failed" + "1 did not run". STOP plan execution. Lapor ke user dengan fallback recommendation: implement (a-revised) saja di Plan 04, skip Plan 05 describe restructure.

**Step 3 — Record decision di 316-VALIDATION.md:**

Append ke Wave 0 Requirements section di `316-VALIDATION.md` (jika belum ada):

```markdown
### Plan 03 Wave 0 — A2 Probe Result (2026-05-11)

- **Outcome:** [A2 VALID | A2 INVALID]
- **Log:** /tmp/316-w0-probe.log
- **Evidence summary line:** {grep result paste}
- **Decision:** [Proceed Wave 1+ | STOP — fallback ke (a-revised) alone]
- **Probe spec:** tests/e2e/_throwaway-probe.spec.ts (cleanup di Plan 06)
```

**Step 4 — Conditional commit:**

- **Jika A2 VALID:** Commit probe spec sebagai checkpoint Wave 0:
  ```bash
  git add tests/e2e/_throwaway-probe.spec.ts .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md
  git commit -m "test(316-03): wave 0 probe spec — A2 verified valid

  Phase 316 Plan 03 Wave 0 — empirical proof test.describe() boundary
  isolate failure di mode fullyParallel: false. Output: 1 failed, 1 passed.

  Decision gate PASS — proceed Plan 04 (Wave 1 helper edits) + Plan 05
  (Wave 2 describe restructure). Probe spec throwaway — cleanup di Plan 06.

  Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  ```

- **Jika A2 INVALID:** **JANGAN commit probe spec.** Delete file:
  ```bash
  rm tests/e2e/_throwaway-probe.spec.ts
  ```
  Lalu commit VALIDATION.md update saja dengan decision STOP:
  ```bash
  git add .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md
  git commit -m "docs(316-03): wave 0 probe — A2 INVALID, fallback path

  Decision STOP Plan 05 describe restructure. Fallback: implement
  (a-revised) softAssert cascade catch + waitForURL regex widen
  saja di Plan 04. Skip Plan 05.

  Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  ```

**Important:** Step ini autonomous untuk decision tracking + commit, TIDAK butuh user interaction. Plan 04+ harus baca outcome di 316-VALIDATION.md untuk tahu lanjut atau skip.
  </action>
  <verify>
    <automated>test -f /tmp/316-w0-probe.log && grep -qE "(passed|did not run)" /tmp/316-w0-probe.log && grep -q "Plan 03 Wave 0" .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md && echo "WAVE0_GATE_RECORDED"</automated>
  </verify>
  <done>Probe run executed dengan log di `/tmp/316-w0-probe.log`. Summary line "1 failed" + (passed|did not run) captured. Decision A2 valid/invalid di-record di `316-VALIDATION.md` Plan 03 Wave 0 section. Commit done (probe spec + VALIDATION.md jika A2 valid; VALIDATION.md saja + delete probe jika A2 invalid).</done>
</task>

</tasks>

<verification>
**Plan 03 Wave 0 sukses jika:**
1. File `tests/e2e/_throwaway-probe.spec.ts` exists dengan 2 test.describe() blocks (Task 1)
2. TS compile clean
3. Probe run terdokumentasi di `/tmp/316-w0-probe.log` dengan summary line jelas (failed + passed/did-not-run)
4. Decision A2 valid/invalid di-record di `316-VALIDATION.md`
5. Commit atomic (probe spec + VALIDATION.md, atau VALIDATION.md + cleanup-by-delete)
6. Plan 04+ (downstream) bisa baca outcome dari 316-VALIDATION.md

**Jika A2 INVALID:** Eskalasi ke checkpoint:decision ke user untuk konfirmasi fallback path (skip Plan 05). Plan 04 tetap valid (Sub-task 1 + 2 surgical helper edits).
</verification>

<success_criteria>
- A2 probe outcome captured + decision gate documented
- TS compile clean throughout
- No edit ke kode produksi (examMatrix.ts, matrixReport.ts, assessment-matrix.spec.ts, playwright.config.ts) di Plan 03
- VALIDATION.md updated dengan Wave 0 A2 result
- Commit atomic, audit trail clean
</success_criteria>

<output>
After completion, create `.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-03-SUMMARY.md`
</output>
