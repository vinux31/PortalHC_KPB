---
phase: 318
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/exam-taking.spec.ts
  - docs/test-reports/2026-05-12-surf-317-a1-patch.md
autonomous: true
requirements: [QA-08]
tags: [e2e, playwright, surf-317-a1, regression, exam-taking, selector-fix]

must_haves:
  truths:
    - "tests/e2e/exam-taking.spec.ts line 40 selector dipatch dari `.user-check-item input` legacy ke pola Phase 304+ form-check (`.user-check-item[data-email]` + `input.user-checkbox`) dengan Playwright `.check()` semantic action"
    - "FLOW A1 (exam-taking.spec.ts test name 'A1 - HC creates assessment for coachee') HIJAU setelah patch — selector tidak lagi cascade-abort file-level serial mode"
    - "Phase 317 regression suite tetap HIJAU end-to-end (28/28 sub-tests via `npx playwright test exam-types.spec.ts`)"
    - "Test file lain (exam-types.spec.ts, assessment-matrix.spec.ts) NOT modified — patch scope strictly 1 file 1 line"
    - "Backlog catalog SURF-317-{n} ter-rekam di docs/test-reports/2026-05-12-surf-317-a1-patch.md (delta vs baseline 2026-05-11) — A2+ failures (cascade lain) tetap exist, di-anchor untuk Phase 320 wholesale refresh"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "Line 40 patched — Phase 304+ form-check compatible selector"
      contains: "input.user-checkbox"
    - path: "docs/test-reports/2026-05-12-surf-317-a1-patch.md"
      provides: "Post-patch report — A1 status PASS, A2+ cascade (jika ada) di-anchor SURF-317-{n}"
      contains: "SURF-317-A1"
  key_links:
    - from: "tests/e2e/exam-taking.spec.ts"
      to: "Views/Admin/CreateAssessment.cshtml"
      via: "selector `.user-check-item[data-email] input.user-checkbox` match Phase 304+ Bootstrap 5 form-check markup"
      pattern: "user-check-item\\[data-email"
    - from: "docs/test-reports/2026-05-12-surf-317-a1-patch.md"
      to: "docs/test-reports/2026-05-11-flow-a-j-regression.md"
      via: "Compare delta — A1 pre-patch CASCADE-ABORT → post-patch PASS"
      pattern: "2026-05-11-flow-a-j-regression"
---

<objective>
Tutup SURF-317-A1 (test fixture carryover) dari Phase 317: patch single-file `tests/e2e/exam-taking.spec.ts` line 40 selector legacy `.user-check-item input` (Phase 303 Bootstrap 4 markup, force-click pattern) ke selector Phase 304+ form-check `.user-check-item[data-email="..."] input.user-checkbox` + Playwright `.check()` semantic action — pola yang sudah proven di `tests/e2e/helpers/examTypes.ts:62-74` (Phase 317 Plan 01).

Purpose: Unblock visibility cascade abort di file `exam-taking.spec.ts` (file-level serial mode). FLOW A1 PASS membuat A2+ failure jadi visible per-test (bukan single cascade abort). Phase 318 Plan 01 SCOPE = patch terbatas line 40 saja per D-318-01 + scope guardrail CONTEXT.md ("DO NOT extend SURF-317-A1 patch beyond line 40 — Phase 320 territory").

Output:
- `tests/e2e/exam-taking.spec.ts` line 40 patched (~1-3 LOC delta)
- `docs/test-reports/2026-05-12-surf-317-a1-patch.md` post-patch delta report dengan SURF-317-{n} anchor catalog untuk cascade lain yang surface
- Phase 317 regression suite (exam-types.spec.ts 28/28) tetap HIJAU sebagai gate post-patch
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-CONTEXT.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md
@docs/test-reports/2026-05-11-flow-a-j-regression.md
@tests/e2e/exam-taking.spec.ts
@tests/e2e/helpers/examTypes.ts
@tests/e2e/helpers/wizardSelectors.ts
@Views/Admin/CreateAssessment.cshtml

<interfaces>
<!-- Phase 317 Plan 01 deliverables yang Plan 01 Phase 318 consume sebagai REFERENCE pattern. -->

From `tests/e2e/helpers/examTypes.ts:60-74` (Phase 317 Plan 01 — proven pattern):
```typescript
// STEP 2 — Peserta selection
await page.locator(wizardSelectors.step2).waitFor({ state: 'visible', timeout: 5_000 });
for (const email of opts.participantEmails) {
  const attrSelector = `${wizardSelectors.userCheckItem}[data-email="${email}"] ${wizardSelectors.userCheckbox}`;
  try {
    await page.locator(attrSelector).check({ timeout: 3_000 });
  } catch {
    // Fallback A7 mitigation — text-based selector dengan email local-part
    const localPart = email.split('@')[0];
    const fallback = page.locator(`${wizardSelectors.userCheckItem}:has-text("${localPart}") ${wizardSelectors.userCheckbox}`);
    await fallback.first().check();
  }
}
```

From `tests/e2e/helpers/wizardSelectors.ts:51-52`:
```typescript
userCheckItem: '.user-check-item',
userCheckbox: 'input.user-checkbox',
```

From `Views/Admin/CreateAssessment.cshtml` (verified 2026-05-12, Phase 304+ markup):
- `.user-check-item[data-email="{email}"]` wrapper Bootstrap 5 form-check pattern
- `input.user-checkbox` actual input element (visually hidden behind label styling — `.click({force:true})` di legacy Bootstrap 4 tidak lagi reliable)

Current code at `tests/e2e/exam-taking.spec.ts:40` (BUG site — to be patched):
```typescript
await page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input').click({ force: true });
```

From `docs/test-reports/2026-05-11-flow-a-j-regression.md` (Phase 317 Plan 02 baseline):
- SURF-317-A1 anchor recorded — file-level serial mode cascade abort dari A1
- Phase 318 mandate: unblock A1 only, A2+ deferred ke Phase 320
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch `exam-taking.spec.ts:40` selector — `.user-check-item input` → `.user-check-item[data-email] input.user-checkbox` + `.check()`</name>
  <files>
    tests/e2e/exam-taking.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts (lines 25-60 — FLOW A1 full describe block, especially line 40 target)
    - tests/e2e/helpers/examTypes.ts (lines 60-74 — Phase 317 proven `.check()` pattern reference)
    - tests/e2e/helpers/wizardSelectors.ts (lines 50-52 — userCheckItem + userCheckbox constants)
    - Views/Admin/CreateAssessment.cshtml (lines 280-320 — confirm `.user-check-item[data-email]` markup + `input.user-checkbox` co-existence)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 547-599 — SURF-317-A1 fix approach + Section "SURF-317-A1 Test Fix Approach")
  </read_first>
  <behavior>
    - Sebelum patch: identify exact line 40 BEFORE state — `.user-check-item` + `.locator('input')` + `.click({ force: true })` chain
    - Setelah patch: line 40 AFTER state — `.user-check-item[data-email="rino.prasetyo@pertamina.com"] input.user-checkbox` + `.check()` semantic action
    - Patch tidak ubah behavior FLOW A1 lain (no test logic move, no helper extraction, no surrounding selector touched)
    - File diff size: max 3 LOC modified (1 LOC primary + ≤2 LOC adjustment kalau ada surrounding waitFor needed)
    - Other tests A2..J di file SAMA tidak di-patch (out of scope per D-318-01 + scope guardrail)
  </behavior>
  <action>
    **STEP 1 — Open `tests/e2e/exam-taking.spec.ts` dan locate line 40:**

    Current line 40 (verified 2026-05-12):
    ```typescript
    await page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input').click({ force: true });
    ```

    **STEP 2 — Replace dengan Phase 304+ form-check compatible selector:**

    Patch line 40 menjadi:
    ```typescript
    await page.locator('.user-check-item[data-email="rino.prasetyo@pertamina.com"] input.user-checkbox').check();
    ```

    **Rationale (cross-reference 318-RESEARCH.md lines 547-577):**
    - `[data-email="..."]` attribute selector targets DOM uniquely (verified Phase 317 attribute present di CreateAssessment.cshtml Step 2 user list rendering)
    - `input.user-checkbox` matches actual input class (Phase 304+ form-check Bootstrap 5)
    - `.check()` Playwright checkbox-aware action — handles label-wrap, visibility, aria-checked, force-needed transparently
    - **JANGAN** ubah selector lain di file ini (A2 ManageQuestions, A3 question_text, dll) — scope guard per D-318-01

    **STEP 3 — Verify surrounding lines tetap kompatibel (line 41-58):**

    Line 41 (post-patch — TIDAK di-touch, hanya verifikasi compat):
    ```typescript
    await expect(page.locator('#selectedCountBadge')).toContainText('1 selected');
    ```

    `.check()` lebih reliable trigger event change handler dibanding `.click({force:true})` — `#selectedCountBadge` update via JS listener `change` event. Verifikasi flow tidak terganggu.

    **STEP 4 — Save file. JANGAN modify line 43-58 (Title, Category, ScheduleDate, dll).** Sekalipun selector lain di line 43+ obsolete (e.g. `#ScheduleDate` legacy → `#schedDateInput`), per scope guard A1 only.

    **STEP 5 — Run FLOW A1 isolated dulu (verify A1 PASS):**
    ```bash
    cd tests && npx playwright test exam-taking.spec.ts --grep "A1 - HC creates" --reporter=list
    ```
    Expected output: 1 test PASS (atau dengan A2+ cascade kalau serial mode file-level — A1 sub-test sendiri PASS).

    Jika A1 RED: verify selector match via `npx playwright codegen http://localhost:5277` untuk inspect actual markup, lalu adjust ke fallback text-based pattern Phase 317:
    ```typescript
    await page.locator('.user-check-item:has-text("rino.prasetyo") input.user-checkbox').first().check();
    ```
    Document deviation di SUMMARY kalau jadi pakai fallback.
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-taking.spec.ts --grep "A1 - HC creates" --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - Grep `tests/e2e/exam-taking.spec.ts` for `input.user-checkbox` returns ≥1 line
    - Grep `tests/e2e/exam-taking.spec.ts` for `\.click\(\{ force: true \}\)` di context user-check-item returns 0 line (force-click pattern di line 40 dihapus)
    - `cd tests && npx playwright test exam-taking.spec.ts --grep "A1 - HC creates"` exit 0 (A1 PASS)
    - `git diff tests/e2e/exam-taking.spec.ts` shows ≤3 LOC modified, semua di neighborhood line 40 (tidak boleh ada perubahan di FLOW B..J)
    - File `tests/e2e/exam-types.spec.ts`, `tests/e2e/assessment-matrix.spec.ts`, `tests/e2e/helpers/*.ts` semua UNCHANGED (`git diff` returns empty untuk file-file ini)
  </acceptance_criteria>
  <done>
    Line 40 `tests/e2e/exam-taking.spec.ts` patched ke Phase 304+ form-check selector. FLOW A1 sub-test PASS isolated run. Scope guard preserved — tidak ada perubahan di luar line 40 neighborhood.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression gate Phase 317 + SURF-317-{n} backlog catalog + delta report</name>
  <files>
    docs/test-reports/2026-05-12-surf-317-a1-patch.md
  </files>
  <read_first>
    - docs/test-reports/2026-05-11-flow-a-j-regression.md (full file — baseline pre-patch untuk delta comparison)
    - tests/e2e/exam-taking.spec.ts (full file — list semua describe FLOW A..J + per-test names untuk per-FLOW breakdown)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md (lines 60-80 — Phase 317 verification stats untuk reference 28/28 baseline)
  </read_first>
  <behavior>
    - Run `cd tests && npx playwright test exam-types.spec.ts --reporter=list` — verify Phase 317 suite tetap HIJAU 28/28 setelah Task 1 patch (regression gate)
    - Run `cd tests && npx playwright test exam-taking.spec.ts --reporter=list 2>&1 | tee /tmp/post-patch-exam-taking.txt` — capture full output post-patch
    - Generate `docs/test-reports/2026-05-12-surf-317-a1-patch.md` dengan:
      - Header: Phase 318 Plan 01 SURF-317-A1 patch report
      - Sub-section "Patch Detail" — before/after line 40 diff
      - Sub-section "FLOW A1 Status" — PASS confirmation
      - Sub-section "Cascade Delta" — list test A2..J yang sebelumnya cascade-abort, sekarang execute, list yang FAIL dengan brief reason, anchor SURF-317-{2..N}
      - Sub-section "Phase 317 Regression Gate" — exam-types.spec.ts 28/28 confirmation
      - Sub-section "Out of Scope (Phase 320 territory)" — list obsolete selectors lain (#ScheduleDate, name="question_text", #submitBtn, dll) yang surface tapi BUKAN target Phase 318
    - TypeScript compile gate: `cd tests && npx tsc --noEmit` exit 0
  </behavior>
  <action>
    **STEP 1 — Run regression gate Phase 317:**

    ```bash
    cd tests && npx playwright test exam-types.spec.ts --reporter=list
    ```

    Expected: 28/28 passed (1 setup + 27 sub-tests) — Phase 317 baseline preserved. JIKA RED: rollback Task 1 patch, investigate selector regression, re-attempt dengan fallback pattern. JANGAN proceed Task 2 step berikutnya sampai 28/28.

    **STEP 2 — Run exam-taking.spec.ts post-patch + capture output:**

    ```bash
    cd tests && npx playwright test exam-taking.spec.ts --reporter=list 2>&1 | tee /tmp/post-patch-exam-taking.txt
    ```

    Capture: total tests, passed, failed, skipped, list failed test names. Compare dengan baseline `docs/test-reports/2026-05-11-flow-a-j-regression.md`.

    **STEP 3 — Create `docs/test-reports/2026-05-12-surf-317-a1-patch.md`:**

    Template:
    ```markdown
    # Phase 318 Plan 01 — SURF-317-A1 Patch Report

    **Date:** 2026-05-12
    **Phase:** 318
    **Plan:** 01
    **Scope:** SURF-317-A1 single-file selector patch — `tests/e2e/exam-taking.spec.ts:40`
    **Baseline:** `docs/test-reports/2026-05-11-flow-a-j-regression.md`

    ## Patch Detail

    **File:** `tests/e2e/exam-taking.spec.ts` line 40

    BEFORE:
    ```typescript
    await page.locator('.user-check-item', { hasText: 'rino.prasetyo' }).locator('input').click({ force: true });
    ```

    AFTER:
    ```typescript
    await page.locator('.user-check-item[data-email="rino.prasetyo@pertamina.com"] input.user-checkbox').check();
    ```

    **LOC delta:** 1 line modified.

    ## FLOW A1 Status

    - Pre-patch: CASCADE-ABORT (file-level serial mode block)
    - Post-patch: **PASS** ({duration_ms}ms)

    ## Phase 317 Regression Gate

    - Command: `cd tests && npx playwright test exam-types.spec.ts --reporter=list`
    - Result: **28/28 PASS** ({runtime}m)
    - Baseline preserved. Tidak ada regression.

    ## Cascade Delta (exam-taking.spec.ts)

    | Metric | Baseline (2026-05-11) | Post-patch (2026-05-12) |
    |--------|----------------------|-------------------------|
    | Total tests | {N_baseline} | {N_post} |
    | Passed | {P_baseline} | {P_post} |
    | Failed | {F_baseline} | {F_post} |
    | Skipped (cascade) | {S_baseline} | {S_post} |

    ### Tests yang sekarang execute (visibility unblocked)

    | Test | Pre-patch | Post-patch | Anchor |
    |------|-----------|------------|--------|
    | A1 - HC creates assessment for coachee | CASCADE-ABORT | PASS | — |
    | A2 - HC navigates to ManageQuestions | CASCADE-ABORT | {PASS/FAIL} | {SURF-317-{n} if FAIL} |
    | A3 - HC adds 3 questions | CASCADE-ABORT | {PASS/FAIL} | {SURF-317-{n} if FAIL} |
    | ... | ... | ... | ... |

    ### New SURF-317-{n} Anchors (Phase 320 backlog)

    {Untuk setiap A2..J yang FAIL post-patch:}

    #### SURF-317-{N+1} — `{test name}`
    - **FLOW:** {A/B/.../J}
    - **Failure:** {1-line reason from output}
    - **Suspected obsolete selector:** {e.g., `#ScheduleDate` legacy single-input, `name="question_text"`, `#submitBtn` single-page submit, etc — cross-reference 318-RESEARCH.md Section SURF-317-A1 "Surrounding Adjustments"}
    - **Target phase:** Phase 320 (proposed FLOW A-J wholesale refresh)

    ## Out of Scope (Phase 320 territory)

    Per CONTEXT.md D-318-01 + scope guardrail, patch terbatas line 40 saja. Obsolete selectors lain yang DETECTED tapi BUKAN target Phase 318:
    - `#ScheduleDate` legacy single-input → wizard `#schedDateInput` + `#schedTimeInput` dual-input (Phase 304+)
    - `#submitBtn` single-page submit → wizard `#btnSubmit` 4-step (Phase 304+)
    - `input[name="correct_option_index"][value="N"]` → `#correct_A/B/C/D` (Phase 298+)
    - `name="question_text"` + `name="options[]"` → `name="questionText"` + `name="optionA..D"` (Phase 298+)
    - `/Admin/ManageQuestions` route → `/Admin/ManagePackageQuestions?packageId={N}` (Phase 298+)
    - Wizard auto-create package → manual `createDefaultPackage` required (Phase 317 W0.1 discovery)

    ## TypeScript Compile Gate

    - Command: `cd tests && npx tsc --noEmit`
    - Result: exit 0

    ## Next Steps

    - Phase 318 Plan 02 (parallel Wave 1) — SURF-317-A production fix (CMPController.cs:2190 ToLookup refactor + Razor view update + Phase 317 MA regression rerun gate)
    - Phase 320 (proposed) — wholesale FLOW A-J refresh menggunakan helpers `examTypes.ts` Phase 317
    ```

    Replace `{N_baseline}`, `{P_baseline}`, dll dengan actual stats. Replace `{SURF-317-{n}}` dengan sequential numbering mulai dari SURF-317-A2 (atau SURF-317-{NextN} continuation dari baseline anchors yang sudah ada).

    **STEP 4 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```

    Must exit 0.

    **STEP 5 — Commit:**
    ```bash
    git add tests/e2e/exam-taking.spec.ts docs/test-reports/2026-05-12-surf-317-a1-patch.md
    git commit -m "fix(318-01): SURF-317-A1 — user-check-item selector form-check compat (line 40 single patch)"
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types.spec.ts --reporter=list && cd tests && npx tsc --noEmit && test -f docs/test-reports/2026-05-12-surf-317-a1-patch.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/test-reports/2026-05-12-surf-317-a1-patch.md` exists
    - Report contains text "SURF-317-A1", "Patch Detail", "Phase 317 Regression Gate", "Cascade Delta" (grep verifiable)
    - Report contains BEFORE/AFTER code block dengan `input.user-checkbox`
    - `cd tests && npx playwright test exam-types.spec.ts` exit 0 dengan 28/28 sub-tests HIJAU (regression gate)
    - `cd tests && npx tsc --noEmit` exit 0
    - Report contains per-test breakdown table FLOW A1-A{N} dengan kolom Pre-patch vs Post-patch status
    - Report contains "Out of Scope (Phase 320 territory)" section dengan list obsolete selectors yang BUKAN target Phase 318
    - `git diff` post-Task-2 shows only 2 files changed: exam-taking.spec.ts (line 40) + new file docs/test-reports/2026-05-12-surf-317-a1-patch.md
  </acceptance_criteria>
  <done>
    Phase 317 regression gate HIJAU (28/28). exam-taking.spec.ts A1 PASS visibility unblocked. Cascade delta + new SURF-317-{n} anchors ter-catalog di delta report. Scope guardrail preserved — patch terbatas line 40, sisa obsolete selectors ter-anchor untuk Phase 320.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → dev DB | FLOW A1 wizard create masuk dev DB (cleanup via global.teardown matrix snapshot restore) |
| Test code → DOM markup | Selector `.user-check-item[data-email]` assumed stable per Phase 304+ markup baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-01 | Tampering | Selector patch breaks A1 silently via DOM markup change post-Phase 304 | mitigate | Wave-0-style isolated run `npx playwright test --grep "A1"` SEBELUM regression gate. Fallback pattern documented (text-based `.first().check()`). |
| T-318-02 | Denial of Service | Phase 317 regression rerun overflow runtime | accept | Phase 317 suite proven ~1.9m runtime di 317-02-SUMMARY.md — well within 2-min default timeout per-test |
| T-318-03 | Information Disclosure | Delta report leaks test failure detail | accept | Report committed ke `docs/test-reports/` — internal scope only, no PII (test pakai dev creds `rino.prasetyo@pertamina.com`) |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# Task 1: A1 isolated verification
cd tests && npx playwright test exam-taking.spec.ts --grep "A1 - HC creates" --reporter=list

# Task 2: Phase 317 regression gate
cd tests && npx playwright test exam-types.spec.ts --reporter=list

# Task 2: Full exam-taking re-run untuk cascade delta
cd tests && npx playwright test exam-taking.spec.ts --reporter=list

# TypeScript compile gate
cd tests && npx tsc --noEmit

# Scope guard verify — only 1 spec file + 1 report changed
git diff --name-only HEAD~1 | sort
# Expected output:
# docs/test-reports/2026-05-12-surf-317-a1-patch.md
# tests/e2e/exam-taking.spec.ts
```

## Manual UAT (post-automation)

- Manual run `npx playwright test exam-taking.spec.ts --headed --grep "A1"` di lokal — visually confirm checkbox tick event fires + `#selectedCountBadge` text update "1 selected"
- Browse `docs/test-reports/2026-05-12-surf-317-a1-patch.md` di GitHub/local — verifikasi delta table readable + SURF-317-{n} anchor terstruktur
- Setelah commit final Plan 01: trigger Plan 02 sequential (Wave 1 parallel selesai → Wave 2 ready)
</verification>

<success_criteria>
## Plan 01 Complete When

1. `tests/e2e/exam-taking.spec.ts` line 40 patched ke `.user-check-item[data-email="..."] input.user-checkbox` + `.check()` semantic
2. FLOW A1 sub-test PASS via `npx playwright test exam-taking.spec.ts --grep "A1"`
3. Phase 317 regression gate HIJAU: `npx playwright test exam-types.spec.ts` 28/28 PASS
4. `docs/test-reports/2026-05-12-surf-317-a1-patch.md` created dengan delta table A1..A{N} + SURF-317-{n} backlog catalog
5. TypeScript clean (`npx tsc --noEmit` exit 0)
6. Scope guard preserved — `git diff` shows hanya 2 file changed (exam-taking.spec.ts + new report)
7. Commit: `fix(318-01): SURF-317-A1 — user-check-item selector form-check compat (line 40 single patch)`
</success_criteria>

<output>
After completion, create `.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-01-SUMMARY.md` mencakup:
- Files modified: `tests/e2e/exam-taking.spec.ts` (line 40), `docs/test-reports/2026-05-12-surf-317-a1-patch.md` (new)
- Patch detail before/after dengan LOC delta exact
- FLOW A1 status pre/post-patch
- Phase 317 regression gate result (28/28 + runtime measurement)
- Cascade delta vs baseline: total tests, pass/fail breakdown, new SURF-317-{n} anchors list
- Out-of-scope obsolete selectors ter-anchor untuk Phase 320
- Any deviation (e.g., fallback text-based selector kalau attribute-based gagal)
- Next: Plan 02 (Wave 1 parallel — SURF-317-A production fix)
</output>
</content>
</invoke>