---
phase: 318
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - tests/e2e/helpers/wizardSelectors.ts
  - tests/e2e/helpers/examTypes.ts
  - tests/helpers/utils.ts
  - tests/e2e/exam-types.spec.ts
autonomous: true
requirements: [QA-08]
tags: [e2e, playwright, exam-types, pretest-posttest, ewcd, examwindowclosedate, signalr]

must_haves:
  truths:
    - "Helper `createPrePostAssessmentViaWizard(page, opts)` ada di examTypes.ts — wrap wizard dengan `assessmentTypeInput='PrePostTest'` + Pre/Post discrete schedule/duration/EWCD fields, return `{ preIds: number[]; postIds: number[] }`"
    - "Selector const `prePostWizardSelectors` append di wizardSelectors.ts — 7 fields (preSchedule, preDurationMinutes, preExamWindowCloseDate, postSchedule, postDurationMinutes, postExamWindowCloseDate, samePackageCheck) + jadwalSection collapse parent"
    - "Helper `yesterday()` append di tests/helpers/utils.ts — return YYYY-MM-DD format string untuk FLOW Q EWCD past date"
    - "FLOW P (PreTest/PostTest paired full cycle, 6 sub-tests P1-P6) HIJAU: wizard PrePostTest create 2 sessions ATOMIC → 2 packages + 2 questions → worker complete both → MonitoringDetail statusSummary `PreTest:Completed,PostTest:Completed` format"
    - "FLOW Q (ExamWindowCloseDate reject, 4 sub-tests Q1-Q4) HIJAU: wizard yesterday EWCD → worker direct nav StartExam → server-side reject via TempData `.alert-danger` 'Ujian sudah ditutup' + redirect /CMP/Assessment + DB Status tetap NotStarted/Upcoming/Open"
    - "Wave 0 verify YELLOW assumptions A2/A3/A7 dari RESEARCH (createdAssessmentData JSON parse, MonitoringDetail statusSummary rendering, PreTest schedule today list visibility) ter-resolve di P1 atau gunakan DB-fallback path"
  artifacts:
    - path: "tests/e2e/helpers/wizardSelectors.ts"
      provides: "Extended with prePostWizardSelectors const map"
      contains: "export const prePostWizardSelectors"
    - path: "tests/e2e/helpers/examTypes.ts"
      provides: "Extended with createPrePostAssessmentViaWizard + CreatePrePostOpts interface"
      exports: ["createPrePostAssessmentViaWizard", "CreatePrePostOpts"]
    - path: "tests/helpers/utils.ts"
      provides: "Extended with yesterday() helper"
      exports: ["yesterday"]
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "Extended with FLOW P (6 sub-tests) + FLOW Q (4 sub-tests)"
      contains: "FLOW P"
  key_links:
    - from: "tests/e2e/exam-types.spec.ts"
      to: "tests/e2e/helpers/examTypes.ts"
      via: "import createPrePostAssessmentViaWizard from examTypes"
      pattern: "createPrePostAssessmentViaWizard"
    - from: "tests/e2e/helpers/examTypes.ts"
      to: "Controllers/AssessmentAdminController.cs"
      via: "POST /Admin/CreateAssessment with isPrePostMode → ATOMIC create 2 sessions per peserta (LinkedSessionId cross-link + LinkedGroupId shared)"
      pattern: "PrePostTest"
    - from: "tests/e2e/helpers/examTypes.ts"
      to: "Views/Admin/CreateAssessment.cshtml"
      via: "#assessmentTypeInput select PrePostTest → #ppt-jadwal-section collapse show → fill preSchedule/preDurationMinutes/preExamWindowCloseDate + postSchedule/postDurationMinutes/postExamWindowCloseDate"
      pattern: "ppt-jadwal-section"
    - from: "tests/e2e/exam-types.spec.ts"
      to: "Controllers/CMPController.cs"
      via: "GET /CMP/StartExam/{id} dengan EWCD past → guard line 863 (DateTime.UtcNow.AddHours(7) > ExamWindowCloseDate.Value) → TempData Error + redirect /CMP/Assessment"
      pattern: "Ujian sudah ditutup"
---

<objective>
Implementasi FLOW P (PreTest/PostTest Paired Full Cycle, 6 sub-tests P1-P6) + FLOW Q (ExamWindowCloseDate Reject, 4 sub-tests Q1-Q4) sebagai append ke `tests/e2e/exam-types.spec.ts`. Tambah helper baru `createPrePostAssessmentViaWizard` + `prePostWizardSelectors` const + `yesterday()` utils helper. Wave 2 sequential setelah Wave 1 (Plan 01 SURF-317-A1 patch + Plan 02 SURF-317-A production fix) selesai — depends_on [01, 02] memastikan SURF fixes landed sebelum FLOW P/Q karena: (a) FLOW P + Q gunakan UI assertion default (no DB workaround needed); (b) FLOW P PostTest submit verification reuse pattern Phase 317 sekarang fully UI-driven.

Purpose: Tutup 2/4 FLOW QA-08 coverage gap (D-318-02 PreTest/PostTest pairing + scoring; D-318-03 EWCD server-side reject). Both FLOW MC-only per CONTEXT.md (D-318-02 skip Razor dual-render, skip PostTest start-gating; D-318-03 skip PDF text parse, skip EWCD Tier-1/Tier-2 extension).

Output:
- `tests/e2e/helpers/wizardSelectors.ts` extended dengan `prePostWizardSelectors` const (8 fields)
- `tests/e2e/helpers/examTypes.ts` extended dengan `createPrePostAssessmentViaWizard` helper + `CreatePrePostOpts` interface (returns `{ preIds, postIds }`)
- `tests/helpers/utils.ts` extended dengan `yesterday()` helper
- `tests/e2e/exam-types.spec.ts` extended dengan FLOW P (6 sub-tests) + FLOW Q (4 sub-tests) — total spec sekarang 27 + 10 = 37 sub-tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-CONTEXT.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-01-SUMMARY.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-02-SUMMARY.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-01-SUMMARY.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md
@tests/e2e/exam-types.spec.ts
@tests/e2e/helpers/examTypes.ts
@tests/e2e/helpers/wizardSelectors.ts
@tests/helpers/utils.ts
@tests/helpers/dbSnapshot.ts
@Views/Admin/CreateAssessment.cshtml
@Views/Admin/AssessmentMonitoringDetail.cshtml
@Controllers/AssessmentAdminController.cs
@Controllers/CMPController.cs

<interfaces>
<!-- Existing Phase 317 deliverables yang Plan 03 consume directly. -->

From `tests/e2e/helpers/examTypes.ts` (Phase 317 complete state):
```typescript
export type QuestionInput =
  | { type: 'MultipleChoice'; text: string; options: [string, string, string, string]; correctIndex: 0|1|2|3; score: number }
  | { type: 'MultipleAnswer'; text: string; options: [string, string, string, string]; correctIndices: (0|1|2|3)[]; score: number }
  | { type: 'Essay'; text: string; rubrik: string; maxCharacters?: number; score: number };

export interface CreateAssessmentOpts {
  title: string; category: string;
  scheduleDate: string; scheduleTime?: string;
  durationMinutes: number; passPercentage: number;
  allowAnswerReview: boolean; generateCertificate?: boolean;
  participantEmails: string[];
  ewcdDate?: string; ewcdTime?: string;
}

export async function createAssessmentViaWizard(page: Page, opts: CreateAssessmentOpts): Promise<void>;
export async function createDefaultPackage(page: Page, packageName?: string): Promise<number>;  // returns packageId
export async function addQuestionViaForm(page: Page, packageId: number, q: QuestionInput): Promise<void>;
export async function submitExamTwoStep(page: Page): Promise<void>;
export async function checkMAOptionsForQuestion(page: Page, qMarker: string, optionTexts: string[]): Promise<void>;
export async function fillEssayAnswer(page: Page, qIndex: number, answer: string): Promise<void>;
export async function gradeSingleEssaySession(pageHc: Page, opts: { title; category; scheduleDate; sessionId; score }): Promise<void>;
export async function addExtraTimeViaModal(pageHc: Page, pageWorker: Page, opts: { title; category; scheduleDate; extraMinutes }): Promise<void>;
```

From `tests/e2e/helpers/wizardSelectors.ts` (Phase 317 complete state):
```typescript
export const selectors = { /* Phase 307/308 — preserve */ } as const;
export const wizardSelectors = { /* 4-step wizard standard fields */ } as const;
export const extraTimeSelectors = { /* AddExtraTime modal */ } as const;
export const questionFormSelectors = { /* ManagePackageQuestions form */ } as const;
// Plan 03 APPEND: prePostWizardSelectors
```

From `tests/helpers/utils.ts`:
```typescript
export function uniqueTitle(prefix: string): string;
export function today(): string;  // YYYY-MM-DD
export function autoConfirm(page: Page): void;
// Plan 03 APPEND: yesterday()
```

From `tests/helpers/dbSnapshot.ts` (verified 2026-05-12):
```typescript
export const db = {
  queryScalar: async (sql: string): Promise<string | number | null>;
  // Used in Phase 317 K5 + M5 + Plan 02 (if K5/M5 NOT upgraded)
};
```

From `Views/Admin/CreateAssessment.cshtml` (verified 2026-05-12):
- `#assessmentTypeInput` dropdown (line 198-201): values `Standard` (default) / `PrePostTest`
- `#ppt-jadwal-section` collapse panel (line 411): toggle via `change` event listener line 950-960
- Pre fields (line 420-440): `#preSchedule`, `#preDurationMinutes`, `#preExamWindowCloseDate`
- Post fields (line 445-465): `#postSchedule`, `#postDurationMinutes`, `#postExamWindowCloseDate`
- SamePackage checkbox (line ~432): `#samePackageCheck`
- Validation skip for PrePost (line 1443): `if (isPrePost) return;` di EWCD≥Schedule check
- `#createdAssessmentData` script tag (line 1660-1700): injected JSON dengan PrePostTest mode `{ IsPrePostTest: true, Sessions: [{ PreId, PostId, UserId, UserName, UserEmail }, ...] }`

From `Controllers/AssessmentAdminController.cs` (verified 2026-05-12):
- Lines 1155-1279: POST CreateAssessment branch `isPrePostMode` → ATOMIC transaction create 2 sessions per peserta
- LinkedSessionId cross-link: Pre→Post + Post→Pre
- LinkedGroupId: shared (= preSessions[0].Id)
- Sessions array di response via ViewBag.CreatedAssessmentData JSON
- Lines 2342, 3614, 5584: statusSummary format `"PreTest:{status},PostTest:{status}"` (e.g., `"PreTest:Completed,PostTest:Completed"`)
- Lines 3600-3640: GetSessionStatus action returns JSON dengan field `statusSummary` untuk type=prepost

From `Controllers/CMPController.cs` (verified 2026-05-12):
- Line 863: EWCD guard `if (DateTime.UtcNow.AddHours(7) > assessment.ExamWindowCloseDate.Value) { TempData["Error"] = "Ujian sudah ditutup..."; return RedirectToAction("Assessment"); }`
- Effect: session.Status tetap "NotStarted" / "Upcoming" / "Open" (tidak flip ke "InProgress")

From `Views/Admin/AssessmentMonitoringDetail.cshtml` (referenced by Phase 317 FLOW O for ExtraTime — Plan 03 FLOW P6 reuse navigation pattern):
- URL pattern: `/Admin/AssessmentMonitoringDetail?title={title}&category={category}&scheduleDate={date}` (URLSearchParams)
- statusSummary text rendering: **YELLOW per RESEARCH A3** — likely rendered via JS post-load (not initial Razor render). Plan 03 P6 Wave 0 verify: scrape DOM atau intercept JSON endpoint
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append `prePostWizardSelectors` ke wizardSelectors.ts + `yesterday()` ke utils.ts + scaffold `createPrePostAssessmentViaWizard` helper di examTypes.ts</name>
  <files>
    tests/e2e/helpers/wizardSelectors.ts,
    tests/e2e/helpers/examTypes.ts,
    tests/helpers/utils.ts
  </files>
  <read_first>
    - tests/e2e/helpers/wizardSelectors.ts (full file — Phase 317 final state dengan 4 const)
    - tests/e2e/helpers/examTypes.ts (full file — 8 exports + QuestionInput + CreateAssessmentOpts)
    - tests/helpers/utils.ts (full file — today/uniqueTitle/autoConfirm)
    - Views/Admin/CreateAssessment.cshtml (lines 195-210 — #assessmentTypeInput; lines 411-465 — PrePost section markup; lines 950-960 — change event toggle; lines 1660-1700 — createdAssessmentData script)
    - Controllers/AssessmentAdminController.cs (lines 1155-1279 — isPrePostMode transaction)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 100-200 — Helper Extensions Required + Selectors append)
  </read_first>
  <behavior>
    - Test 1 (TypeScript compile gate): `cd tests && npx tsc --noEmit` exit 0
    - Test 2 (selector const presence): `wizardSelectors.ts` has `export const prePostWizardSelectors` with 8 fields (jadwalSection + 7 pre/post fields + samePackageCheck)
    - Test 3 (helper export): `examTypes.ts` exports `createPrePostAssessmentViaWizard` + `CreatePrePostOpts` interface
    - Test 4 (utils export): `tests/helpers/utils.ts` exports `yesterday()` (function returning YYYY-MM-DD)
    - Test 5 (Playwright dry-discovery): `cd tests && npx playwright test exam-types --list` exit 0 (semua test ter-discover, no syntax error)
  </behavior>
  <action>
    **STEP 1 — Append `prePostWizardSelectors` ke `tests/e2e/helpers/wizardSelectors.ts`:**

    Append at END of file (preserve Phase 307/308 `selectors`, Phase 317 `wizardSelectors` + `extraTimeSelectors` + `questionFormSelectors`):
    ```typescript
    // ============================================================
    // Phase 318 Plan 03 — PrePostTest wizard fields
    // Source: Views/Admin/CreateAssessment.cshtml lines 411-465 (verified 2026-05-12)
    // Source: Views/Admin/CreateAssessment.cshtml lines 195-210 (#assessmentTypeInput dropdown)
    // Behavior: select #assessmentTypeInput='PrePostTest' → change event → #ppt-jadwal-section collapse show
    // ============================================================
    export const prePostWizardSelectors = {
      jadwalSection: '#ppt-jadwal-section',          // collapse panel parent
      preSchedule: '#preSchedule',                    // datetime-local: 'YYYY-MM-DDTHH:mm'
      preDurationMinutes: '#preDurationMinutes',
      preExamWindowCloseDate: '#preExamWindowCloseDate',
      postSchedule: '#postSchedule',
      postDurationMinutes: '#postDurationMinutes',
      postExamWindowCloseDate: '#postExamWindowCloseDate',
      samePackageCheck: '#samePackageCheck',          // optional — both packages auto-sync kalau checked
    } as const;
    ```

    **STEP 2 — Append `yesterday()` ke `tests/helpers/utils.ts`:**

    Add export after `today()`:
    ```typescript
    /** Returns YYYY-MM-DD format date string for yesterday (UTC-naive). */
    export const yesterday = (): string => {
      const d = new Date(Date.now() - 24 * 60 * 60 * 1000);
      return d.toISOString().split('T')[0];
    };
    ```

    **STEP 3 — Scaffold `createPrePostAssessmentViaWizard` di `tests/e2e/helpers/examTypes.ts`:**

    Update imports at top:
    ```typescript
    import { wizardSelectors, questionFormSelectors, extraTimeSelectors, prePostWizardSelectors } from './wizardSelectors';
    ```

    Add interface after `CreateAssessmentOpts`:
    ```typescript
    /** Phase 318 Plan 03 — opts untuk PrePostTest wizard create.
     * Schedules pakai 'YYYY-MM-DDTHH:mm' datetime-local format.
     * Source: Views/Admin/CreateAssessment.cshtml lines 411-465.
     */
    export interface CreatePrePostOpts {
      title: string;
      category: string;
      preSchedule: string;          // 'YYYY-MM-DDTHH:mm'
      preDurationMinutes: number;
      preExamWindowCloseDate: string;
      postSchedule: string;
      postDurationMinutes: number;
      postExamWindowCloseDate: string;
      passPercentage: number;
      allowAnswerReview: boolean;
      generateCertificate?: boolean;
      participantEmails: string[];
      samePackage?: boolean;
    }
    ```

    Add helper function at end of file:
    ```typescript
    /**
     * HC create PrePostTest assessment via 4-step Bootstrap wizard.
     *
     * Pattern source: Views/Admin/CreateAssessment.cshtml lines 195-465 (verified 2026-05-12).
     * Controller: AssessmentAdminController.cs lines 1155-1279 — ATOMIC 2-session create per peserta.
     * Sessions array exposed via #createdAssessmentData script tag JSON (line 1660-1700).
     *
     * Pitfalls mitigated:
     *  - AssessmentType change event MUST fire to show #ppt-jadwal-section.
     *  - Validation skip for PrePost: standard #schedDateInput + #ewcdDateInput tidak fill
     *    (per CreateAssessment.cshtml:1443).
     *  - Schedule format 'YYYY-MM-DDTHH:mm' datetime-local — caller wajib build ISO string.
     *  - Success modal #successModal data-bs-backdrop="static" — function returns AFTER modal
     *    show; caller harus dismiss via #modal-manage-btn click kalau butuh nav.
     *
     * @param page HC user page (sudah login)
     * @param opts wizard fields
     * @returns { preIds, postIds } sessionId arrays per peserta (extracted dari #createdAssessmentData JSON)
     */
    export async function createPrePostAssessmentViaWizard(
      page: Page,
      opts: CreatePrePostOpts
    ): Promise<{ preIds: number[]; postIds: number[] }> {
      await page.goto('/Admin/CreateAssessment');

      // STEP 1 — Title + Category + Type=PrePostTest (CRITICAL FIRST — toggle #ppt-jadwal-section)
      await page.locator(wizardSelectors.step1).waitFor({ state: 'visible' });
      await page.selectOption(wizardSelectors.category, opts.category);
      await page.fill(wizardSelectors.title, opts.title);
      // CRITICAL: selectOption FIRES change event automatically — toggle ppt-jadwal-section visible
      await page.selectOption(wizardSelectors.assessmentType, 'PrePostTest');
      // Wait jadwal section collapse animate done
      await page.locator(prePostWizardSelectors.jadwalSection).waitFor({ state: 'visible', timeout: 3_000 });
      await page.locator(wizardSelectors.btnNext1).click();

      // STEP 2 — Peserta selection (reuse Phase 317 pattern from createAssessmentViaWizard)
      await page.locator(wizardSelectors.step2).waitFor({ state: 'visible', timeout: 5_000 });
      for (const email of opts.participantEmails) {
        const attrSelector = `${wizardSelectors.userCheckItem}[data-email="${email}"] ${wizardSelectors.userCheckbox}`;
        try {
          await page.locator(attrSelector).check({ timeout: 3_000 });
        } catch {
          const localPart = email.split('@')[0];
          await page.locator(`${wizardSelectors.userCheckItem}:has-text("${localPart}") ${wizardSelectors.userCheckbox}`).first().check();
        }
      }
      await page.locator(wizardSelectors.btnNext2).click();

      // STEP 3 — Pre + Post discrete schedule/duration/EWCD
      await page.locator(wizardSelectors.step3).waitFor({ state: 'visible', timeout: 5_000 });
      await page.fill(prePostWizardSelectors.preSchedule, opts.preSchedule);
      await page.fill(prePostWizardSelectors.preDurationMinutes, String(opts.preDurationMinutes));
      await page.fill(prePostWizardSelectors.preExamWindowCloseDate, opts.preExamWindowCloseDate);
      await page.fill(prePostWizardSelectors.postSchedule, opts.postSchedule);
      await page.fill(prePostWizardSelectors.postDurationMinutes, String(opts.postDurationMinutes));
      await page.fill(prePostWizardSelectors.postExamWindowCloseDate, opts.postExamWindowCloseDate);

      await page.fill(wizardSelectors.passPercentage, String(opts.passPercentage));
      if (opts.allowAnswerReview) await page.locator(wizardSelectors.allowAnswerReview).check();
      if (opts.generateCertificate) await page.locator(wizardSelectors.generateCertificate).check();
      if (opts.samePackage) {
        await page.locator(prePostWizardSelectors.samePackageCheck).check().catch(() => { /* checkbox optional */ });
      }

      await page.locator(wizardSelectors.btnNext3).click();

      // STEP 4 — Summary + Submit
      await page.locator(wizardSelectors.step4).waitFor({ state: 'visible', timeout: 5_000 });
      await page.locator(wizardSelectors.btnSubmit).click();

      // Success modal — static backdrop, wait visible
      await page.locator(wizardSelectors.successModal).waitFor({ state: 'visible', timeout: 15_000 });

      // Extract preIds + postIds dari #createdAssessmentData JSON
      // Source: Views/Admin/CreateAssessment.cshtml:1660-1700 script injection
      const dataRaw = await page.locator(wizardSelectors.createdAssessmentData).textContent();
      if (!dataRaw) {
        throw new Error('createPrePostAssessmentViaWizard: #createdAssessmentData empty — wizard mungkin gagal submit');
      }
      const parsed = JSON.parse(dataRaw) as {
        IsPrePostTest?: boolean;
        Sessions?: Array<{ PreId: number; PostId: number; UserId: string; UserName: string; UserEmail: string }>;
      };
      if (!parsed.IsPrePostTest || !parsed.Sessions) {
        // Fallback: DB query post-wizard (extract dari title + category)
        // Caller bisa pakai db.queryScalar di test step kalau JSON parse gagal.
        throw new Error('createPrePostAssessmentViaWizard: createdAssessmentData JSON missing PrePostTest fields');
      }
      const preIds = parsed.Sessions.map(s => s.PreId);
      const postIds = parsed.Sessions.map(s => s.PostId);
      return { preIds, postIds };
    }
    ```

    **STEP 4 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```

    Expected: exit 0. Fix any TypeScript error sebelum proceed.

    **STEP 5 — Playwright dry-discovery:**

    ```bash
    cd tests && npx playwright test exam-types --list
    ```

    Expected: list semua test discoverable (existing 27 + FLOW P + FLOW Q nanti).
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Grep `tests/e2e/helpers/wizardSelectors.ts` for `export const prePostWizardSelectors` returns 1 line
    - Grep `tests/e2e/helpers/wizardSelectors.ts` for `preSchedule`, `postSchedule`, `samePackageCheck` returns ≥3 lines total
    - Grep `tests/helpers/utils.ts` for `export const yesterday` returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `export async function createPrePostAssessmentViaWizard` returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `export interface CreatePrePostOpts` returns 1 line
    - `cd tests && npx tsc --noEmit` exit 0
    - `cd tests && npx playwright test exam-types --list` exit 0
  </acceptance_criteria>
  <done>
    Helper foundation Plan 03 selesai. `prePostWizardSelectors` + `yesterday()` + `createPrePostAssessmentViaWizard` exports + interface tersedia untuk Task 2/3 FLOW P + FLOW Q consume.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FLOW P — PreTest/PostTest Paired Full Cycle (6 sub-tests P1-P6) + Wave 0 verify YELLOW A2/A3/A7</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (full file — Phase 317 final state, study FLOW K/M structure untuk pattern reference)
    - tests/e2e/helpers/examTypes.ts (post-Task-1 state — confirm createPrePostAssessmentViaWizard signature + return shape)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 240-290 — FLOW P Skeleton, Critical Pitfalls FLOW P, DB verify queries; lines 1065-1080 — A2/A3/A7 Wave 0 verify recommendations; lines 670-825 — FLOW P Skeleton verbatim code)
    - Views/Admin/AssessmentMonitoringDetail.cshtml (statusSummary rendering — RESEARCH says YELLOW unclear apakah DOM atau JS post-load; Wave 0 verify)
    - Controllers/AssessmentAdminController.cs (lines 3600-3640 — statusSummary JSON format `PreTest:{status},PostTest:{status}`)
  </read_first>
  <behavior>
    - **Wave 0 — VERIFY A2 (createdAssessmentData JSON parse):** P1 includes try/catch wrap untuk parse JSON; kalau parse gagal → fall back ke DB query `SELECT Id, AssessmentType FROM AssessmentSessions WHERE Title=@t AND Category=@c ORDER BY Id` → split rows by AssessmentType IN ('PreTest', 'PostTest')
    - **Wave 0 — VERIFY A3 (MonitoringDetail statusSummary rendering):** P6 includes 2 verify strategies — first try DOM text scrape, fallback ke `page.route` intercept JSON endpoint kalau text TIDAK rendered initial HTML
    - **Wave 0 — VERIFY A7 (PreTest schedule today list visibility):** Set `preSchedule = '${today()}T00:01'` (TIDAK depends on past schedule). Both Pre + Post visible di /CMP/Assessment list immediately post-wizard
    - P1: HC wizard PrePostTest create → 2 session pairs created (deterministic ATOMIC); extract preId + postId
    - P2: HC ManagePackages?assessmentId={preId} → createDefaultPackage + 1 MC question (correct=A)
    - P3: HC ManagePackages?assessmentId={postId} → createDefaultPackage + 1 MC question (correct=B); DB verify LinkedSessionId pair
    - P4: Worker /CMP/Assessment → click PreTest card → submit correct → DB verify Status=Completed
    - P5: Worker /CMP/Assessment → click PostTest card → submit correct → DB verify Status=Completed
    - P6: HC navigate AssessmentMonitoringDetail → verify statusSummary `PreTest:Completed,PostTest:Completed` (DOM scrape OR JSON intercept)
    - Sub-test count: 6 (P1-P6) sequential mode shared state per describe — match Phase 317 FLOW M pattern
  </behavior>
  <action>
    **STEP 1 — Update imports di `tests/e2e/exam-types.spec.ts`:**

    Update existing import line untuk `examTypes.ts`:
    ```typescript
    import {
      createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm, submitExamTwoStep,
      fillEssayAnswer, gradeSingleEssaySession, addExtraTimeViaModal,
      createPrePostAssessmentViaWizard,
      type QuestionInput, type CreatePrePostOpts,
    } from './helpers/examTypes';
    ```

    Update `tests/helpers/utils.ts` import:
    ```typescript
    import { uniqueTitle, today, yesterday, autoConfirm } from '../helpers/utils';
    ```

    (Atau path-relatif sesuai struktur existing.)

    **STEP 2 — Append FLOW P describe block (di akhir file, setelah FLOW O):**

    ```typescript
    test.describe('FLOW P — PreTest/PostTest Paired Full Cycle', () => {
      let title: string;
      let category: string;
      let scheduleDate: string;
      let preId: number;
      let postId: number;
      let prePackageId: number;
      let postPackageId: number;
      const Q_PRE_MARKER = '[318-P-PRE] Q1 — Apa singkatan OJT?';
      const Q_POST_MARKER = '[318-P-POST] Q1 — Apa hasil utama OJT?';
      const PRE_CORRECT = 'On the Job Training';
      const POST_CORRECT = 'Competency improvement';

      test('P1 — HC creates PrePost assessment via wizard (Wave 0 verify A2)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        title = uniqueTitle('[318-P] PrePost Exam');
        category = 'OJT';
        scheduleDate = today();
        await login(page, 'hc');
        try {
          const result = await createPrePostAssessmentViaWizard(page, {
            title, category,
            preSchedule: `${scheduleDate}T00:01`,
            preDurationMinutes: 60,
            preExamWindowCloseDate: `${scheduleDate}T23:59`,
            postSchedule: `${scheduleDate}T00:02`,
            postDurationMinutes: 60,
            postExamWindowCloseDate: `${scheduleDate}T23:59`,
            passPercentage: 70,
            allowAnswerReview: true,
            generateCertificate: false,
            participantEmails: ['rino.prasetyo@pertamina.com'],
            samePackage: false,
          });
          preId = result.preIds[0];
          postId = result.postIds[0];
        } catch (err) {
          // A2 fallback: createdAssessmentData JSON parse gagal → DB query
          // eslint-disable-next-line no-console
          console.warn(`[FLOW P] createdAssessmentData parse fail (${err}) — fallback DB query`);
          const rows = await db.queryScalar(
            `SELECT TOP 1 Id FROM AssessmentSessions
             WHERE Title = '${title}' AND Category = '${category}'
               AND AssessmentType = 'PreTest' ORDER BY Id`
          );
          preId = rows as number;
          const postRow = await db.queryScalar(
            `SELECT TOP 1 Id FROM AssessmentSessions
             WHERE Title = '${title}' AND Category = '${category}'
               AND AssessmentType = 'PostTest' ORDER BY Id`
          );
          postId = postRow as number;
        }
        expect(preId).toBeGreaterThan(0);
        expect(postId).toBeGreaterThan(0);
        expect(preId).not.toBe(postId);

        // DB verify pair integrity: LinkedSessionId cross-pointing + LinkedGroupId shared
        const linkedCount = await db.queryScalar(
          `SELECT COUNT(*) FROM AssessmentSessions
           WHERE Title = '${title}' AND Category = '${category}'
             AND AssessmentType IN ('PreTest', 'PostTest')
             AND LinkedSessionId IS NOT NULL`
        );
        expect(linkedCount).toBe(2);
      });

      test('P2 — HC creates package + MC question for PreTest', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto(`/Admin/ManagePackages?assessmentId=${preId}`);
        await page.waitForLoadState('networkidle');
        prePackageId = await createDefaultPackage(page);
        await addQuestionViaForm(page, prePackageId, {
          type: 'MultipleChoice',
          text: Q_PRE_MARKER,
          options: [PRE_CORRECT, 'Online Job Test', 'Off Job Theory', 'Operational Training'],
          correctIndex: 0,
          score: 100,
        });
      });

      test('P3 — HC creates package + MC question for PostTest', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto(`/Admin/ManagePackages?assessmentId=${postId}`);
        await page.waitForLoadState('networkidle');
        postPackageId = await createDefaultPackage(page);
        await addQuestionViaForm(page, postPackageId, {
          type: 'MultipleChoice',
          text: Q_POST_MARKER,
          options: ['Theory only', POST_CORRECT, 'Salary increase', 'Promotion'],
          correctIndex: 1,
          score: 100,
        });
      });

      test('P4 — Worker submits PreTest', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        // Filter card by title + PreTest marker (badge atau AssessmentType text)
        const preCard = page.locator('.assessment-card, .card', { hasText: title })
          .filter({ hasText: /Pre-?Test/i }).first();
        await expect(preCard).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await preCard.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });
        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );

        // DOM-text matching (anti-cheat shuffle) — Phase 317 Plan 01 pattern
        const qCard = page.locator('[id^="qcard_"]').filter({ hasText: Q_PRE_MARKER });
        await qCard.locator('label.list-group-item', { hasText: PRE_CORRECT })
          .locator('input.exam-radio').check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i })
          .first().waitFor({ timeout: 7_500 });
        await submitExamTwoStep(page);

        // DB verify PreTest completed
        const preStatus = await db.queryScalar(
          `SELECT Status FROM AssessmentSessions WHERE Id = ${preId}`
        );
        expect(preStatus).toBe('Completed');
      });

      test('P5 — Worker submits PostTest', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        const postCard = page.locator('.assessment-card, .card', { hasText: title })
          .filter({ hasText: /Post-?Test/i }).first();
        await expect(postCard).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await postCard.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });
        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );

        const qCard = page.locator('[id^="qcard_"]').filter({ hasText: Q_POST_MARKER });
        await qCard.locator('label.list-group-item', { hasText: POST_CORRECT })
          .locator('input.exam-radio').check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i })
          .first().waitFor({ timeout: 7_500 });
        await submitExamTwoStep(page);

        const postStatus = await db.queryScalar(
          `SELECT Status FROM AssessmentSessions WHERE Id = ${postId}`
        );
        expect(postStatus).toBe('Completed');
      });

      test('P6 — HC MonitoringDetail statusSummary verify (Wave 0 verify A3)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        const params = new URLSearchParams({ title, category, scheduleDate });

        // A3 verify Strategy 1: DOM text scrape
        let domHit = false;
        try {
          await page.goto(`/Admin/AssessmentMonitoringDetail?${params.toString()}`);
          await page.waitForLoadState('networkidle');
          await expect(
            page.locator('body', { hasText: /PreTest:\s*Completed.*PostTest:\s*Completed/is })
          ).toBeVisible({ timeout: 8_000 });
          domHit = true;
        } catch {
          // A3 fallback: intercept JSON endpoint
          // eslint-disable-next-line no-console
          console.warn('[FLOW P6] DOM text scrape fail — fallback page.route intercept JSON');
        }

        if (!domHit) {
          // Intercept GetSessionStatus AJAX (atau similar — verify endpoint actual)
          // Source: AssessmentAdminController.cs:3614 JSON field statusSummary
          const respPromise = page.waitForResponse(
            r => r.url().includes('SessionStatus') || r.url().includes('MonitoringDetail'),
            { timeout: 10_000 }
          );
          await page.reload();
          const resp = await respPromise;
          const json = await resp.json();
          // Expected JSON contain field statusSummary OR sessions with status arrays
          const summaryText = JSON.stringify(json);
          expect(summaryText).toMatch(/PreTest.*Completed.*PostTest.*Completed/is);
        }
      });
    });
    ```

    **STEP 3 — Run FLOW P isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW P"
    ```

    Expected: 6/6 PASS.

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | P1 createdAssessmentData parse fail | A2 wrong-assumption — JSON not injected for PrePost mode | Fallback DB query already coded — verify branch executed |
    | P1 LinkedSessionId count != 2 | Controller atomic transaction issue — investigate `AssessmentAdminController.cs:1180` | Manual reproduce wizard di lokal + inspect DB |
    | P4/P5 card not visible | A7 wrong — scheduleDate filter hide today's session | Adjust to `${yesterday()}T23:00` for preSchedule (yesterday past), `${today()}T00:02` for postSchedule |
    | P6 DOM scrape fail | A3 wrong — statusSummary di JS post-load only | Fallback intercept JSON already coded |
    | P6 JSON intercept fail (no endpoint match) | Endpoint URL different dari assumed | Use `page.on('response', ...)` debug + inspect URL pattern, adjust grep |

    Document any deviation di SUMMARY.md.

    **STEP 4 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "FLOW P"</automated>
  </verify>
  <acceptance_criteria>
    - 6 sub-tests P1-P6 PASSED dalam describe `FLOW P — PreTest/PostTest Paired Full Cycle`
    - Grep `tests/e2e/exam-types.spec.ts` for `'FLOW P — PreTest/PostTest` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `P1 —`, `P2 —`, `P3 —`, `P4 —`, `P5 —`, `P6 —` returns 6 lines
    - P1: DB linkedCount=2 verified (LinkedSessionId IS NOT NULL untuk 2 sessions)
    - P4: preStatus='Completed' DB verified
    - P5: postStatus='Completed' DB verified
    - P6: statusSummary text/JSON matches `PreTest:Completed.*PostTest:Completed` (Strategy 1 atau Strategy 2)
    - `[318-P]` title marker + `[318-P-PRE]` + `[318-P-POST]` question markers ter-present
    - Per-sub-test runtime ≤ 120s
  </acceptance_criteria>
  <done>
    FLOW P (6 sub-tests) HIJAU end-to-end. PreTest + PostTest ATOMIC pair create verified via DB LinkedSessionId. Both workers complete-able. statusSummary `PreTest:Completed,PostTest:Completed` rendering verified (DOM atau JSON intercept). Wave 0 YELLOW assumptions A2/A3/A7 resolved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FLOW Q — ExamWindowCloseDate Reject (4 sub-tests Q1-Q4)</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Task-2 state — current FLOW P final structure)
    - tests/e2e/helpers/examTypes.ts (createAssessmentViaWizard signature — opts.ewcdDate + ewcdTime field support)
    - tests/helpers/utils.ts (yesterday() helper from Task 1)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 292-330 — FLOW Q Skeleton + Critical Pitfalls FLOW Q + DB verify queries; lines 828-908 — FLOW Q Skeleton verbatim code)
    - Controllers/CMPController.cs (line 863 — EWCD guard formula `DateTime.UtcNow.AddHours(7) > ExamWindowCloseDate.Value`, TempData["Error"], RedirectToAction("Assessment"))
    - Views/Admin/CreateAssessment.cshtml (line 1446 — wizard validation EWCD>=Schedule; pitfall — schedule juga harus yesterday agar pass)
  </read_first>
  <behavior>
    - Q1: HC wizard standard mode dengan `scheduleDate=yesterday()` + `scheduleTime='00:01'` + `ewcdDate=yesterday()` + `ewcdTime='23:59'` — ensures wizard validation pass (EWCD ≥ Schedule equality OK)
    - Q2: HC createDefaultPackage + 1 MC question (placeholder — never executed) + extract sessionId via DB query (Worker `/CMP/Assessment` card mungkin disabled, hindari card click hazard)
    - Q3: Worker direct nav `page.goto('/CMP/StartExam/{sessionId}')` — bypass UI guard kalau ada → expect server-side guard trigger → redirect to `/CMP/Assessment` + TempData Error `.alert-danger` "Ujian sudah ditutup"
    - Q4: DB verify session.Status NOT IN ('InProgress', 'Completed') + StartedAt IS NULL
  </behavior>
  <action>
    **STEP 1 — Append FLOW Q describe (setelah FLOW P di exam-types.spec.ts):**

    ```typescript
    test.describe('FLOW Q — ExamWindowCloseDate Reject', () => {
      let title: string;
      let assessmentId: number;
      let packageId: number;
      let sessionId: number;

      test('Q1 — HC creates assessment with yesterday EWCD (past window)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        title = uniqueTitle('[318-Q] EWCD Past Exam');
        await login(page, 'hc');
        // CRITICAL: scheduleDate=yesterday() supaya wizard validation EWCD>=Schedule pass
        // (per CreateAssessment.cshtml:1446 — equality is OK)
        await createAssessmentViaWizard(page, {
          title,
          category: 'OJT',
          scheduleDate: yesterday(),
          scheduleTime: '00:01',
          durationMinutes: 60,
          passPercentage: 70,
          allowAnswerReview: true,
          generateCertificate: false,
          participantEmails: ['rino.prasetyo@pertamina.com'],
          ewcdDate: yesterday(),
          ewcdTime: '23:59',  // yesterday 23:59 WIB = already passed di today
        });
        const href = await page.locator('#modal-manage-btn').getAttribute('href');
        assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
        expect(assessmentId).toBeGreaterThan(0);
      });

      test('Q2 — HC adds package + MC question + extract sessionId via DB', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto(`/Admin/ManagePackages?assessmentId=${assessmentId}`);
        await page.waitForLoadState('networkidle');
        packageId = await createDefaultPackage(page);
        await addQuestionViaForm(page, packageId, {
          type: 'MultipleChoice',
          text: '[318-Q] Q1 — placeholder, never executed',
          options: ['A', 'B', 'C', 'D'],
          correctIndex: 0,
          score: 100,
        });

        // Extract sessionId via DB query (avoid card click hazard kalau card di-disable post-EWCD)
        const row = await db.queryScalar(
          `SELECT TOP 1 Id FROM AssessmentSessions
           WHERE Title = '${title}'
             AND UserId = (SELECT TOP 1 Id FROM AspNetUsers WHERE Email = 'rino.prasetyo@pertamina.com')`
        );
        sessionId = row as number;
        expect(sessionId).toBeGreaterThan(0);
      });

      test('Q3 — Worker attempt StartExam → server-side reject + TempData redirect', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        // Direct nav StartExam bypass UI guard — pure server-side guard test
        await page.goto(`/CMP/StartExam/${sessionId}`);

        // Server-side guard (CMPController.cs:863) → RedirectToAction("Assessment") + TempData["Error"]
        await page.waitForURL(/\/CMP\/Assessment\b/, { timeout: 10_000 });

        // Verify TempData Error message rendered (likely via _TempDataAlert.cshtml partial)
        await expect(
          page.locator('.alert-danger, .alert.alert-danger', { hasText: /Ujian sudah ditutup/i }).first()
        ).toBeVisible({ timeout: 5_000 });
      });

      test('Q4 — DB verify session stays NotStarted (Status not InProgress)', async () => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        const status = await db.queryScalar(
          `SELECT Status FROM AssessmentSessions WHERE Id = ${sessionId}`
        );
        expect(status).not.toBe('InProgress');
        expect(status).not.toBe('Completed');
        // Allow values: 'NotStarted' / 'Upcoming' / 'Open' / 'Closed' / 'Expired' — semua bukan InProgress

        const startedAt = await db.queryScalar(
          `SELECT ISNULL(CAST(StartedAt AS varchar(50)), '<null>')
           FROM AssessmentSessions WHERE Id = ${sessionId}`
        );
        expect(startedAt).toBe('<null>');
      });
    });
    ```

    **STEP 2 — Run FLOW Q isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW Q"
    ```

    Expected: 4/4 PASS.

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | Q1 wizard reject | Validation EWCD<Schedule (kalau time mismatched) | Ensure both scheduleDate + ewcdDate = yesterday() + ewcdTime='23:59' |
    | Q3 not redirect | EWCD guard timezone calc wrong (WIB+7 offset edge) | Set ewcdTime even earlier (e.g., '12:00' yesterday) untuk safety margin |
    | Q3 alert-danger missing | TempData rendered di different selector | Investigate via `page.content()` debug, broaden selector (`text=/Ujian sudah/i`) |
    | Q4 Status='InProgress' | Guard tidak trigger — investigate controller line 863 actual code | Likely Plan 02 SURF-317-A refactor didn't break — verify guard preserved |

    **STEP 3 — Run full Phase 318 Plan 03 suite (FLOW P + Q):**

    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW P|FLOW Q"
    ```

    Expected: 10/10 PASS (6 P + 4 Q).

    **STEP 4 — Full Phase 317 + Plan 03 regression check:**

    ```bash
    cd tests && npx playwright test exam-types.spec.ts --reporter=list
    ```

    Expected: 38/38 (1 setup + 27 Phase 317 + 6 P + 4 Q). JIKA RED: identify which old FLOW regressed (unlikely since Plan 03 only appends), rollback Task 2/3 changes selektif.

    **STEP 5 — TypeScript compile gate + commit:**

    ```bash
    cd tests && npx tsc --noEmit
    git add tests/e2e/exam-types.spec.ts tests/e2e/helpers/examTypes.ts tests/e2e/helpers/wizardSelectors.ts tests/helpers/utils.ts
    git commit -m "feat(318-03): FLOW P PrePostTest paired + FLOW Q EWCD reject hijau (10/10 — QA-08)"
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "FLOW P|FLOW Q"</automated>
  </verify>
  <acceptance_criteria>
    - 4 sub-tests Q1-Q4 PASSED dalam describe `FLOW Q — ExamWindowCloseDate Reject`
    - Grep `tests/e2e/exam-types.spec.ts` for `'FLOW Q — ExamWindowCloseDate` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `Q1 —`, `Q2 —`, `Q3 —`, `Q4 —` returns 4 lines
    - Q3: `.alert-danger` containing "Ujian sudah ditutup" visible PASS
    - Q4: DB Status not 'InProgress' + StartedAt IS NULL PASS
    - Full Plan 03 suite (FLOW P + FLOW Q) = 10 sub-tests PASS
    - Full Phase 318 cumulative regression = 38/38 (Phase 317 27 + Plan 03 10 + setup 1) HIJAU
    - `cd tests && npx tsc --noEmit` exit 0
    - `[318-Q]` title marker ter-present
  </acceptance_criteria>
  <done>
    FLOW Q (4 sub-tests) HIJAU end-to-end. Server-side EWCD guard verified via direct StartExam nav → redirect + TempData. DB session.Status preserved (no InProgress flip). Full Plan 03 (FLOW P + Q = 10 sub-tests) HIJAU. Phase 317 regression preserved 27/27. TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → dev DB | FLOW P creates 2 sessions per peserta + 2 packages + 2 questions; FLOW Q creates 1 session NotStarted |
| Worker context → SignalR hub | Phase 317 inherit — PreTest + PostTest worker submit menggunakan StartExam SignalR readiness pattern |
| HC wizard → controller | PrePostTest mode atomic transaction (1 POST creates 2 sessions) |
| Worker direct nav `/CMP/StartExam/{id}` → server-side EWCD guard | Bypass UI guard untuk pure server-side test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-08 | Tampering | A2/A3/A7 YELLOW assumptions fail → FLOW P fragile | mitigate | Wave 0 verify embedded di P1 (A2 try/catch + DB fallback) + P6 (A3 DOM-or-JSON dual strategy). A7 mitigated via scheduleDate=today(T00:01) |
| T-318-09 | Information Disclosure | DB query exposes UserId/email via SELECT subquery | accept | Test pakai dev creds `rino.prasetyo@pertamina.com` — known dev identifier, no production PII |
| T-318-10 | Elevation of Privilege | EWCD bypass via direct StartExam nav | mitigate | This is the test target — FLOW Q verifies guard works. Test pakai expired EWCD, no admin escalation |
| T-318-11 | Denial of Service | FLOW P 6 sub-tests + FLOW Q 4 sub-tests cumulative + Phase 317 27 = ~5-7 min full suite runtime | accept | Acceptable for CI test suite. Per-test timeout 120s sufficient |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# Task 1: Foundation
cd tests && npx tsc --noEmit
cd tests && npx playwright test exam-types --list

# Task 2: FLOW P
cd tests && npx playwright test exam-types --grep "FLOW P"

# Task 3: FLOW Q
cd tests && npx playwright test exam-types --grep "FLOW Q"

# Combined Plan 03 suite
cd tests && npx playwright test exam-types --grep "FLOW P|FLOW Q"

# Cumulative regression (Phase 317 + Plan 03)
cd tests && npx playwright test exam-types.spec.ts --reporter=list
```

## Manual UAT (post-automation)

- HC manual: create PrePostTest via wizard di UI → verify success modal + DB 2 sessions
- Worker manual: visit `/CMP/Assessment` → confirm 2 cards visible (Pre + Post) untuk title `[318-P]`
- Worker manual: visit `/CMP/StartExam/{Q-sessionId}` direct → confirm redirect + TempData warning natural Indonesian copy
- Setelah commit: cleanup dev DB hapus rows prefix `[318-P]` + `[318-Q]` per CLAUDE.md SEED_WORKFLOW
</verification>

<success_criteria>
## Plan 03 Complete When

1. `tests/e2e/helpers/wizardSelectors.ts` extended dengan `prePostWizardSelectors` (8 fields)
2. `tests/e2e/helpers/examTypes.ts` extended dengan `createPrePostAssessmentViaWizard` + `CreatePrePostOpts` interface
3. `tests/helpers/utils.ts` extended dengan `yesterday()` helper
4. FLOW P HIJAU: 6 sub-tests P1-P6 — wizard PrePostTest → 2 sessions ATOMIC → 2 packages + questions → worker complete both → statusSummary verified
5. FLOW Q HIJAU: 4 sub-tests Q1-Q4 — wizard yesterday EWCD → worker direct nav StartExam → server-side reject + DB Status preserved
6. Cumulative regression `npx playwright test exam-types.spec.ts` 38/38 HIJAU
7. `npx tsc --noEmit` exit 0
8. Wave 0 YELLOW assumptions A2/A3/A7 ter-resolve (dengan fallback path kalau primary path gagal)
9. Commit: `feat(318-03): FLOW P PrePostTest paired + FLOW Q EWCD reject hijau (10/10 — QA-08)`
</success_criteria>

<output>
After completion, create `.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-03-SUMMARY.md` mencakup:
- Files modified: `wizardSelectors.ts` (prePostWizardSelectors), `examTypes.ts` (createPrePostAssessmentViaWizard + CreatePrePostOpts), `utils.ts` (yesterday), `exam-types.spec.ts` (FLOW P + Q)
- FLOW P runtime per sub-test + total
- FLOW Q runtime per sub-test + total
- Wave 0 verification results (A2: createdAssessmentData JSON parse success/fallback, A3: MonitoringDetail DOM-or-JSON path, A7: PreTest schedule today visibility)
- Any deviation dari RESEARCH (e.g., samePackage default behavior, statusSummary endpoint URL pattern, MonitoringDetail rendering shift)
- DB integrity verifications: LinkedSessionId pair count, AssessmentType filter, Status not InProgress untuk Q
- Cumulative regression stats: 38/38 vs baseline 28/28
- Next: Plan 04 (Wave 2 sequential — FLOW R Cert PDF + FLOW S AllowAnswerReview comparison)
</output>
</content>
</invoke>