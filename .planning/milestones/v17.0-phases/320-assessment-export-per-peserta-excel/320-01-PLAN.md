---
phase: 320-assessment-export-per-peserta-excel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.csproj
  - Helpers/SpiderChartRenderer.cs
  - Helpers/SheetNameSanitizer.cs
autonomous: true
requirements:
  - EXP-03
  - EXP-06
requirements_addressed:
  - EXP-03
  - EXP-06

must_haves:
  truths:
    - "Project HcPortal.csproj memiliki PackageReference SkiaSharp 3.116.1 + SkiaSharp.NativeAssets.Win32 3.116.1"
    - "Helper SpiderChartRenderer dapat menghasilkan PNG radar 500×500 dari list (label, percentage)"
    - "Helper SpiderChartRenderer return byte[0] saat data < 3 elemen (skip rule untuk REQ EXP-03)"
    - "Helper SheetNameSanitizer menghasilkan sheet name `{NIP}_{FullName}` maksimum 31 char tanpa karakter Excel-invalid"
    - "Helper SheetNameSanitizer punya collision guard via ISet<string> usedNames"
    - "dotnet build pass dengan 0 error dan 0 warning baru"
  artifacts:
    - path: "HcPortal.csproj"
      provides: "PackageReference SkiaSharp + native asset Win32"
      contains: 'SkiaSharp" Version="3.116.1'
    - path: "Helpers/SpiderChartRenderer.cs"
      provides: "Static class PNG radar chart renderer"
      contains: "public static byte[] RenderRadarPng"
    - path: "Helpers/SheetNameSanitizer.cs"
      provides: "Static class Excel-safe sheet name sanitizer"
      contains: "public static string Sanitize"
  key_links:
    - from: "Helpers/SpiderChartRenderer.cs"
      to: "SkiaSharp NuGet package"
      via: "using SkiaSharp; namespace declaration block-scope"
      pattern: "using SkiaSharp"
    - from: "Helpers/SheetNameSanitizer.cs"
      to: "Controllers/AssessmentAdminController.cs (downstream consumer Plan 02)"
      via: "Static method Sanitize(nip, fullName, usedNames)"
      pattern: "SheetNameSanitizer.Sanitize"
---

<objective>
Tambah dependency SkiaSharp + 2 helper static class baru di folder `Helpers/` sebagai foundation untuk per-peserta sheet generation di Plan 02. Helper bersifat pure-function (no DI, no I/O selain encode PNG) sehingga dapat diuji isolated dan dipanggil dari controller maupun parallel pre-compute (Plan 03).

Purpose: Foundation layer Phase 320 — tanpa helper ini, refactor controller di Plan 02 dan benchmark Parallel.ForEachAsync di Plan 03 tidak bisa berjalan. Helpers locked dulu supaya downstream plan tidak revisit signature.

Output:
- 2 baris baru di `<ItemGroup>` `HcPortal.csproj` (SkiaSharp + SkiaSharp.NativeAssets.Win32)
- File baru `Helpers/SpiderChartRenderer.cs` (1 method public `RenderRadarPng`)
- File baru `Helpers/SheetNameSanitizer.cs` (1 method public `Sanitize`, 1 private `ScrubChars`)
- 3 commit atomic (Task 1 csproj, Task 2 SpiderChartRenderer, Task 3 SheetNameSanitizer)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/320-assessment-export-per-peserta-excel/320-CONTEXT.md
@.planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md
@.planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md
@HcPortal.csproj
@Helpers/CertNumberHelper.cs
@Helpers/PaginationHelper.cs
@Helpers/ExcelExportHelper.cs

<interfaces>
<!-- Contract yang akan dikonsumsi Plan 02 — executor MUST match signature persis ini. -->

From Helpers/SpiderChartRenderer.cs (created in Task 2):
```csharp
namespace HcPortal.Helpers
{
    public static class SpiderChartRenderer
    {
        // Return byte[0] kalau data == null || data.Count < 3
        public static byte[] RenderRadarPng(
            IList<(string label, double percentage)> data,
            int size = 500);
    }
}
```

From Helpers/SheetNameSanitizer.cs (created in Task 3):
```csharp
namespace HcPortal.Helpers
{
    public static class SheetNameSanitizer
    {
        // Mutates usedNames (adds candidate). Returns Excel-safe name max 31 chars.
        public static string Sanitize(string nip, string fullName, ISet<string> usedNames);
    }
}
```

From HcPortal.csproj (existing ItemGroup baris 11-26):
```xml
<ItemGroup>
  <PackageReference Include="ClosedXML" Version="0.105.0" />
  <!-- ... -->
  <PackageReference Include="QuestPDF" Version="2026.2.2" />
  <!-- NEW (Task 1): insert SkiaSharp 2 baris di sini -->
</ItemGroup>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah PackageReference SkiaSharp ke HcPortal.csproj</name>
  <files>HcPortal.csproj</files>
  <read_first>
    - HcPortal.csproj (verify ItemGroup PackageReference baris 11-26, QuestPDF di baris 25 sebagai anchor insertion)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 1 Step 1-4)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md (section `HcPortal.csproj (package config)`)
  </read_first>
  <action>
    Edit `HcPortal.csproj`. Di dalam `<ItemGroup>` existing yang berisi PackageReference (baris 11-26), tepat **setelah** baris `<PackageReference Include="QuestPDF" Version="2026.2.2" />` (line 25), sisipkan 2 baris baru — JANGAN buat ItemGroup baru:

    ```xml
    <PackageReference Include="SkiaSharp" Version="3.116.1" />
    <PackageReference Include="SkiaSharp.NativeAssets.Win32" Version="3.116.1" />
    ```

    Rationale (per CONTEXT.md D-04 + PATTERNS.md): Konsisten dengan pola sibling existing (ClosedXML, QuestPDF). Win32 native asset selaras dengan deployment IIS Windows (NoWarn CA1416 sudah aktif di csproj baris 7).

    Setelah edit, jalankan:
    ```bash
    dotnet restore
    dotnet build
    ```

    Verify build sukses tanpa warning baru. Verify DLL muncul di `bin/Debug/net8.0/runtimes/win-x64/native/libSkiaSharp.dll`.

    Commit (per CONTEXT.md D-06 — commit per task, format `feat(v17.0-p320): ...`):
    ```bash
    git add HcPortal.csproj
    git commit -m "feat(v17.0-p320): add SkiaSharp 3.116.1 for spider chart PNG render"
    ```

    Lakukan DEV_WORKFLOW §5 pre-commit checklist sebelum commit.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error|warning)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.csproj` contains literal string `SkiaSharp" Version="3.116.1`
    - `HcPortal.csproj` contains literal string `SkiaSharp.NativeAssets.Win32" Version="3.116.1`
    - `dotnet build` exits 0 with 0 errors and 0 new warnings
    - File `bin/Debug/net8.0/runtimes/win-x64/native/libSkiaSharp.dll` exists after restore + build
    - Commit hash baru di `git log --oneline -1` dengan subject `feat(v17.0-p320): add SkiaSharp 3.116.1 for spider chart PNG render`
  </acceptance_criteria>
  <done>
    PackageReference SkiaSharp + Win32 native asset ter-commit, build pass, native DLL ter-extract di bin runtime path.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Helpers/SpiderChartRenderer.cs (PNG radar via SkiaSharp)</name>
  <files>Helpers/SpiderChartRenderer.cs</files>
  <read_first>
    - Helpers/CertNumberHelper.cs (analog struktur file: namespace block-scope, using di luar namespace, XML doc-comment)
    - Helpers/ExcelExportHelper.cs (analog binary-producing helper)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 2 Step 1 — code lengkap verbatim, codebase-verified)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md (section `Helpers/SpiderChartRenderer.cs (static utility, transform)`)
  </read_first>
  <action>
    Create file baru `Helpers/SpiderChartRenderer.cs` dengan konten **verbatim** dari RESEARCH.md Task 2 Step 1 (sudah codebase-verified — JANGAN paraphrase atau ringkas). Convention wajib (per PATTERNS.md):
    - `using SkiaSharp;` di luar `namespace` block
    - `namespace HcPortal.Helpers { ... }` block-scope (BUKAN file-scoped)
    - `public static class SpiderChartRenderer`
    - XML doc-comment `/// <summary>` pada method public

    Konten file (copy persis):

    ```csharp
    using SkiaSharp;

    namespace HcPortal.Helpers
    {
        public static class SpiderChartRenderer
        {
            /// <summary>
            /// Render radar/spider chart sebagai PNG byte array.
            /// </summary>
            /// <param name="data">List (label, percentage 0..100). Minimum 3 untuk render valid; kalau &lt; 3 return empty array.</param>
            /// <param name="size">Lebar+tinggi canvas (default 500px).</param>
            /// <returns>PNG bytes, atau byte[0] kalau data &lt; 3 elemen.</returns>
            public static byte[] RenderRadarPng(IList<(string label, double percentage)> data, int size = 500)
            {
                if (data == null || data.Count < 3) return Array.Empty<byte>();

                using var bitmap = new SKBitmap(size, size);
                using var canvas = new SKCanvas(bitmap);
                canvas.Clear(SKColors.White);

                float cx = size / 2f;
                float cy = size / 2f;
                float radius = size * 0.35f;
                int n = data.Count;

                // Grid radial (0/25/50/75/100)
                using var gridPaint = new SKPaint { Color = SKColors.LightGray, Style = SKPaintStyle.Stroke, StrokeWidth = 1, IsAntialias = true };
                for (int level = 1; level <= 4; level++)
                {
                    float r = radius * level / 4f;
                    using var path = new SKPath();
                    for (int i = 0; i < n; i++)
                    {
                        double angle = -Math.PI / 2 + 2 * Math.PI * i / n;
                        float x = cx + (float)(r * Math.Cos(angle));
                        float y = cy + (float)(r * Math.Sin(angle));
                        if (i == 0) path.MoveTo(x, y); else path.LineTo(x, y);
                    }
                    path.Close();
                    canvas.DrawPath(path, gridPaint);
                }

                // Axis lines
                using var axisPaint = new SKPaint { Color = SKColors.LightGray, Style = SKPaintStyle.Stroke, StrokeWidth = 1, IsAntialias = true };
                for (int i = 0; i < n; i++)
                {
                    double angle = -Math.PI / 2 + 2 * Math.PI * i / n;
                    float x = cx + (float)(radius * Math.Cos(angle));
                    float y = cy + (float)(radius * Math.Sin(angle));
                    canvas.DrawLine(cx, cy, x, y, axisPaint);
                }

                // Data polygon (D-02: stroke RGB(54,162,235), fill RGBA(54,162,235,96))
                using var fillPaint = new SKPaint { Color = new SKColor(54, 162, 235, 96), Style = SKPaintStyle.Fill, IsAntialias = true };
                using var strokePaint = new SKPaint { Color = new SKColor(54, 162, 235), Style = SKPaintStyle.Stroke, StrokeWidth = 2, IsAntialias = true };
                using var dataPath = new SKPath();
                for (int i = 0; i < n; i++)
                {
                    double angle = -Math.PI / 2 + 2 * Math.PI * i / n;
                    double pct = Math.Clamp(data[i].percentage, 0, 100);
                    float r = (float)(radius * pct / 100);
                    float x = cx + (float)(r * Math.Cos(angle));
                    float y = cy + (float)(r * Math.Sin(angle));
                    if (i == 0) dataPath.MoveTo(x, y); else dataPath.LineTo(x, y);
                }
                dataPath.Close();
                canvas.DrawPath(dataPath, fillPaint);
                canvas.DrawPath(dataPath, strokePaint);

                // Labels (truncate > 20 char dengan ellipsis, konsisten Results.cshtml:274)
                using var textFont = new SKFont(SKTypeface.Default, 12);
                using var textPaint = new SKPaint { Color = SKColors.Black, IsAntialias = true };
                for (int i = 0; i < n; i++)
                {
                    double angle = -Math.PI / 2 + 2 * Math.PI * i / n;
                    float labelR = radius + 25;
                    float x = cx + (float)(labelR * Math.Cos(angle));
                    float y = cy + (float)(labelR * Math.Sin(angle));
                    string label = data[i].label;
                    if (label.Length > 20) label = label.Substring(0, 17) + "...";
                    var textWidth = textFont.MeasureText(label);
                    canvas.DrawText(label, x - textWidth / 2, y, SKTextAlign.Left, textFont, textPaint);
                }

                using var image = SKImage.FromBitmap(bitmap);
                using var pngData = image.Encode(SKEncodedImageFormat.Png, 100);
                return pngData.ToArray();
            }
        }
    }
    ```

    PENTING (per security threat model — PNG buffer leak): SEMUA `SKBitmap`, `SKCanvas`, `SKPaint`, `SKPath`, `SKImage`, `SKData` di-wrap `using var` agar GC release native handle. JANGAN hilangkan `using`.

    Warna chart (per CONTEXT.md D-02): stroke `SKColor(54, 162, 235)`, fill `SKColor(54, 162, 235, 96)` — JANGAN ubah ke warna brand Pertamina.

    Setelah create file:
    ```bash
    dotnet build
    ```

    Smoke test optional via temporary endpoint `Controllers/DebugController.cs` (lihat RESEARCH.md Task 2 Step 3) — boleh skip kalau confident, HAPUS endpoint setelah verify.

    Commit:
    ```bash
    git add Helpers/SpiderChartRenderer.cs
    git commit -m "feat(v17.0-p320): add SpiderChartRenderer (SkiaSharp PNG radar)"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `Helpers/SpiderChartRenderer.cs` exists
    - File contains literal string `public static byte[] RenderRadarPng`
    - File contains literal string `using SkiaSharp;` di luar namespace block
    - File contains literal string `namespace HcPortal.Helpers` block-scope (followed by `{` newline, bukan `;`)
    - File contains literal string `new SKColor(54, 162, 235)` (D-02 stroke)
    - File contains literal string `new SKColor(54, 162, 235, 96)` (D-02 fill, alpha 96)
    - File contains `if (data == null || data.Count < 3) return Array.Empty<byte>();` (skip rule REQ EXP-03)
    - File contains `using var bitmap = new SKBitmap` (disposal pattern)
    - `dotnet build` exits 0 with 0 errors
    - Commit hash baru subject `feat(v17.0-p320): add SpiderChartRenderer (SkiaSharp PNG radar)`
  </acceptance_criteria>
  <done>
    SpiderChartRenderer.cs ter-commit, method `RenderRadarPng` callable dari assembly, build pass, semua `SK*` resource di-dispose via `using var`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create Helpers/SheetNameSanitizer.cs ({NIP}_{FullName} 31-char format)</name>
  <files>Helpers/SheetNameSanitizer.cs</files>
  <read_first>
    - Helpers/PaginationHelper.cs (analog terbaik: pure function, no using directives, ringan)
    - Helpers/CertNumberHelper.cs (analog XML doc-comment + namespace pattern)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 3 Step 1 — code lengkap verbatim)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md (section `Helpers/SheetNameSanitizer.cs (static utility, transform)`)
  </read_first>
  <action>
    Create file baru `Helpers/SheetNameSanitizer.cs` dengan konten **verbatim** dari RESEARCH.md Task 3 Step 1. Convention (per PATTERNS.md analog PaginationHelper):
    - TIDAK ada `using` directives (tidak butuh)
    - `namespace HcPortal.Helpers { ... }` block-scope
    - `private static readonly char[]` untuk `InvalidChars`
    - `private const int` untuk `ExcelSheetNameLimit = 31`
    - XML doc-comment `/// <summary>` pada method public `Sanitize`

    Konten file (copy persis):

    ```csharp
    namespace HcPortal.Helpers
    {
        public static class SheetNameSanitizer
        {
            private static readonly char[] InvalidChars = { '\\', '/', '?', '*', '[', ']', ':' };
            private const int ExcelSheetNameLimit = 31;

            /// <summary>
            /// Format sheet name {NIP}_{FullName}, truncate ke 31 char dengan collision guard.
            /// NIP-first ensures uniqueness karena NIP guaranteed unique per worker.
            /// </summary>
            /// <param name="nip">NIP peserta (akan di-scrub Excel-invalid chars).</param>
            /// <param name="fullName">Nama lengkap peserta (akan di-scrub).</param>
            /// <param name="usedNames">Set sheet name yang sudah dipakai (case-insensitive). Method ini akan menambahkan hasil ke set.</param>
            /// <returns>Sheet name Excel-safe (max 31 char, no invalid char, unique).</returns>
            public static string Sanitize(string nip, string fullName, ISet<string> usedNames)
            {
                string cleanNip = ScrubChars(nip ?? "");
                string cleanName = ScrubChars(fullName ?? "");
                string raw = $"{cleanNip}_{cleanName}";
                if (raw.Length > ExcelSheetNameLimit)
                    raw = raw.Substring(0, ExcelSheetNameLimit);

                // Collision guard (rare — only if truncation creates collision)
                string candidate = raw;
                int counter = 2;
                while (usedNames.Contains(candidate))
                {
                    string suffix = $"({counter})";
                    int allowed = ExcelSheetNameLimit - suffix.Length;
                    candidate = (raw.Length > allowed ? raw.Substring(0, allowed) : raw) + suffix;
                    counter++;
                }
                usedNames.Add(candidate);
                return candidate;
            }

            private static string ScrubChars(string s)
            {
                foreach (var c in InvalidChars) s = s.Replace(c, '_');
                return s;
            }
        }
    }
    ```

    Rationale Excel-invalid chars `\ / ? * [ ] :` (per REQ EXP-06 + Microsoft docs): ini 7 char yang Excel reject saat AddWorksheet. Scrub jadi `_` untuk safety. Threat model "sheet name injection" mitigated di sini (input dari `User.FullName` user-provided text → wajib scrub).

    Setelah create file:
    ```bash
    dotnet build
    ```

    Commit:
    ```bash
    git add Helpers/SheetNameSanitizer.cs
    git commit -m "feat(v17.0-p320): add SheetNameSanitizer ({NIP}_{FullName} format)"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `Helpers/SheetNameSanitizer.cs` exists
    - File contains literal string `public static string Sanitize(string nip, string fullName, ISet<string> usedNames)`
    - File contains literal string `private const int ExcelSheetNameLimit = 31;`
    - File contains literal `'\\', '/', '?', '*', '[', ']', ':'` (Excel-invalid chars array)
    - File contains `namespace HcPortal.Helpers` block-scope (followed by `{` newline)
    - File contains private helper `private static string ScrubChars(string s)`
    - File contains collision guard loop `while (usedNames.Contains(candidate))`
    - `dotnet build` exits 0 with 0 errors
    - Commit hash baru subject `feat(v17.0-p320): add SheetNameSanitizer ({NIP}_{FullName} format)`
  </acceptance_criteria>
  <done>
    SheetNameSanitizer.cs ter-commit, method `Sanitize` siap dipanggil dari Plan 02 dengan signature `(string, string, ISet<string>) -> string`, build pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → sheet name | `User.FullName` & `User.NIP` (user-provided text saat onboarding) menjadi nama worksheet Excel — Excel-invalid chars MUST di-scrub |
| SkiaSharp managed → native handle | `SKBitmap`/`SKCanvas`/`SKPaint`/`SKImage` allocate native memory yang harus di-dispose explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-320-01-01 | Tampering | Sheet name injection (Excel-invalid char `\ / ? * [ ] :` di FullName user) | mitigate | `SheetNameSanitizer.ScrubChars` replace 7 invalid char → `_` sebelum return. Acceptance: file contains `InvalidChars` array dengan tepat 7 char. |
| T-320-01-02 | Denial of Service | PNG buffer leak (SKBitmap native handle tidak release → OOM saat 50 peserta parallel render) | mitigate | Semua `SK*` resource di-wrap `using var` di `RenderRadarPng`. Acceptance: grep file shows ≥6 occurrences of `using var` pattern. |
| T-320-01-03 | Denial of Service | Render dengan data terlalu kecil (< 3 elemen) menghasilkan polygon corrupt → exception ClosedXML AddPicture | mitigate | Guard `if (data == null \|\| data.Count < 3) return Array.Empty<byte>();` di paling awal method. REQ EXP-03 explicit skip rule. |
| T-320-01-04 | Information Disclosure | Helper sendiri tidak baca PII/secret — pure transform | accept | No PII, no DB, no I/O. Helper hanya transform input → byte[]/string. |
| T-320-01-05 | Elevation of Privilege | Helper sebagai `public static` callable dari mana saja | accept | No side-effect (selain mutate `usedNames` set caller-owned). Caller (Plan 02 controller) sudah punya `[Authorize(Roles = "Admin, HC")]`. |
</threat_model>

<verification>
**Per-task verification** (lihat `<verify>` setiap task).

**Plan-level integration:**

1. `dotnet build` overall pass tanpa warning baru
2. `git log --oneline -3` menampilkan 3 commit dengan subject sesuai (csproj → SpiderChartRenderer → SheetNameSanitizer)
3. `grep -c "using var" Helpers/SpiderChartRenderer.cs` ≥ 6 (memastikan semua SK* di-dispose)
4. `grep "namespace HcPortal.Helpers" Helpers/SpiderChartRenderer.cs Helpers/SheetNameSanitizer.cs` keduanya match (konsisten dengan Helpers/*.cs existing)
5. `ls bin/Debug/net8.0/runtimes/win-x64/native/libSkiaSharp.dll` exists (native DLL ter-extract)

**No manual UAT untuk Plan 01** — pure foundation, helper tidak punya behavior visible end-user. UAT visual + cross-Excel verify happens di Plan 03 setelah controller (Plan 02) merangkai keseluruhan.
</verification>

<success_criteria>
- 3 commit atomic merged ke local branch (1 untuk csproj, 2 untuk helper files)
- `dotnet build` pass 0 error 0 warning baru
- 2 helper class siap dipanggil dari `Controllers/AssessmentAdminController.cs` di Plan 02
- SkiaSharp native asset Win32 ter-extract dan dapat load runtime
- 0 helper file melanggar convention (`namespace HcPortal.Helpers` block-scope, `using` di luar namespace, XML doc-comment, no DI)
- REQ EXP-03 skip rule (<3 elemen) ter-implementasi di `RenderRadarPng`
- REQ EXP-06 sheet name format `{NIP}_{FullName}` + 31-char + invalid char scrub ter-implementasi di `Sanitize`

**DEV_WORKFLOW §5 pre-commit checklist per commit:**
- [ ] dotnet build pass (tanpa warning baru)
- [ ] No DB migration (Plan 01 tidak touch DB)
- [ ] No team IT notification needed di akhir Plan 01 (notif gabungan setelah Phase 320 selesai full per Plan 03 Task akhir)
</success_criteria>

<output>
After completion, create `.planning/phases/320-assessment-export-per-peserta-excel/320-01-SUMMARY.md` dengan:
- Daftar 3 commit hash dengan subject
- Path 3 file (1 csproj modified + 2 helper created)
- Konfirmasi `dotnet build` pass
- Catatan apakah smoke test debug endpoint dijalankan (opsional)
- Sinyal Plan 02 ready: "Helpers locked, Plan 02 dapat mulai consume SpiderChartRenderer.RenderRadarPng + SheetNameSanitizer.Sanitize"
</output>
</content>
</invoke>