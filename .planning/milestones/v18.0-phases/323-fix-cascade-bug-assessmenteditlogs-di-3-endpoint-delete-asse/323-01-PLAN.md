---
phase: 323
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements:
  - CASCADE-01

must_haves:
  truths:
    - "Admin/HC dapat hapus AssessmentSession yang sudah pernah di-edit (single, group, atau Pre-Post group) tanpa FK Restrict exception"
    - "Session tanpa edit log tetap bisa dihapus (no regression)"
    - "Audit log Description berisi token EditLogsCount={N} di 3 endpoint"
    - "Transaction scope existing tetap membungkus cascade + audit (rollback bersih saat exception)"
    - "Tidak ada perubahan schema/model/migration"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "3 cascade endpoint patched dengan RemoveRange(AssessmentEditLogs) block + snapshot preDeleteEditLogsCount + audit description EditLogsCount token"
      contains: "AssessmentEditLogs.RemoveRange"
      min_occurrences: 3
  key_links:
    - from: "Controllers/AssessmentAdminController.cs DeleteAssessment (~L2071)"
      to: "_context.AssessmentEditLogs.RemoveRange(editLogs)"
      via: "in-transaction block sebelum PackageUserResponses (D-01)"
      pattern: "AssessmentEditLogs[\\s\\S]*?RemoveRange"
    - from: "Controllers/AssessmentAdminController.cs DeleteAssessmentGroup (~L2215)"
      to: "_context.AssessmentEditLogs.RemoveRange(allEditLogs)"
      via: "siblingIds.Contains predicate"
      pattern: "siblingIds\\.Contains[\\s\\S]*?AssessmentEditLogs"
    - from: "Controllers/AssessmentAdminController.cs DeletePrePostGroup (~L2348)"
      to: "_context.AssessmentEditLogs.RemoveRange(allEditLogs)"
      via: "groupIds.Contains predicate"
      pattern: "groupIds\\.Contains[\\s\\S]*?AssessmentEditLogs"
    - from: "audit description di 3 endpoint"
      to: "EditLogsCount={preDeleteEditLogsCount} token"
      via: "string interpolation append (D-02)"
      pattern: "EditLogsCount=\\{preDeleteEditLogsCount"
---

<objective>
Patch 3 endpoint cascade delete di `Controllers/AssessmentAdminController.cs` (`DeleteAssessment` ~L2071, `DeleteAssessmentGroup` ~L2215, `DeletePrePostGroup` ~L2348) dengan menambah block `RemoveRange(AssessmentEditLogs)` paling awal di cascade chain (per D-01) + snapshot `preDeleteEditLogsCount` sebelum cascade + extend audit log Description dengan token `EditLogsCount={N}` (per D-02). Block sisip DI DALAM transaction scope existing (L2040, L2184, L2313) — tidak buat scope baru.

Purpose: Menutup oversight Phase 321 — model `AssessmentEditLog` baru di-deklarasi `DeleteBehavior.Restrict` di `Data/ApplicationDbContext.cs:241`, sehingga FK menolak delete saat session pernah di-edit (repro Dev: Session Id 2, Id 5 → "Gagal menghapus assessment"). Pattern 100% copy Phase 312 `PackageUserResponses` block — bukan refactor, bukan helper extraction (deferred per CONTEXT.md).

Output: 3 endpoint patched, `dotnet build` clean, code siap untuk Wave 2 E2E smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-CONTEXT.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-RESEARCH.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-PATTERNS.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-VALIDATION.md
@Controllers/AssessmentAdminController.cs
@CLAUDE.md

<interfaces>
<!-- Key contracts from existing codebase (Phase 312 cascade pattern + Phase 321 AssessmentEditLog model). -->
<!-- Executor uses these directly — DO NOT re-explore codebase. -->

From `Models/AssessmentEditLog.cs` (Phase 321):
```csharp
public class AssessmentEditLog
{
    public int Id { get; set; }
    public int AssessmentSessionId { get; set; }   // FK → AssessmentSession (Restrict)
    public int PackageQuestionId { get; set; }     // FK → PackageQuestion
    public string QuestionTextSnapshot { get; set; } = "";
    public string? OldAnswerJson { get; set; }
    public string? OldAnswerTextSnapshot { get; set; }
    public string? NewAnswerJson { get; set; }
    public string? NewAnswerTextSnapshot { get; set; }
    public decimal? OldScore { get; set; }
    public decimal? NewScore { get; set; }
    public bool? OldIsPassed { get; set; }
    public bool? NewIsPassed { get; set; }
    public string ActorUserId { get; set; } = "";
    public string ActorName { get; set; } = "";
    public string ActorRole { get; set; } = "";
    public DateTime EditedAt { get; set; }
    public string ReasonCode { get; set; } = "";
    public string? ReasonText { get; set; }
    public virtual AssessmentSession AssessmentSession { get; set; } = null!;
    public virtual PackageQuestion PackageQuestion { get; set; } = null!;
}
```

From `Data/ApplicationDbContext.cs:235-246` (Phase 321 — FK Restrict confirmed):
```csharp
modelBuilder.Entity<AssessmentEditLog>(entity =>
{
    entity.HasOne(e => e.AssessmentSession)
        .WithMany()
        .HasForeignKey(e => e.AssessmentSessionId)
        .OnDelete(DeleteBehavior.Restrict);  // ← THIS is why explicit RemoveRange required
});
public DbSet<AssessmentEditLog> AssessmentEditLogs { get; set; }
```

From `Services/AuditLogService.cs:21-42` (existing, no change):
```csharp
public Task LogAsync(
    string actorUserId,
    string actorName,
    string actionType,
    string description,        // ← Phase 323 append " EditLogsCount={N}"
    int? targetId = null,
    string? targetType = null
);
```

From `Controllers/AssessmentAdminController.cs` (EXISTING Phase 312 cascade pattern — EXACT template to copy):

DeleteAssessment L2072-2080 (THE TEMPLATE):
```csharp
// Delete PackageUserResponses (Restrict FK — must be removed before session)
var pkgResponses = await _context.PackageUserResponses
    .Where(r => r.AssessmentSessionId == id)
    .ToListAsync();
if (pkgResponses.Any())
{
    logger.LogInformation($"Deleting {pkgResponses.Count} package user responses");
    _context.PackageUserResponses.RemoveRange(pkgResponses);
}
```

DeleteAssessment L2056-2057 (snapshot pattern):
```csharp
int preDeleteResponseCount = await _context.PackageUserResponses
    .CountAsync(r => r.AssessmentSessionId == id);
```

DeleteAssessment L2123-2129 (audit description pattern):
```csharp
await _auditLog.LogAsync(
    deleteUser?.Id ?? "",
    deleteActorName,
    "DeleteAssessment",
    $"Deleted assessment '{assessmentTitle}' [ID={id}] Status={preDeleteStatus} ResponseCount={preDeleteResponseCount}",
    id,
    "AssessmentSession");
```

DeleteAssessment L2040 (transaction scope — preserve):
```csharp
using var tx = await _context.Database.BeginTransactionAsync();
```

Logger acquisition (per-method, line 2017/2154/2292):
```csharp
var logger = HttpContext.RequestServices.GetRequiredService<ILogger<AssessmentAdminController>>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch DeleteAssessment endpoint (~L2056-2127)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (lines 2010-2150 — full DeleteAssessment method dengan transaction scope, snapshot, cascade chain, audit log)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-CONTEXT.md (D-01 cascade order, D-02 audit format, D-04 .Any() guard)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-RESEARCH.md §"Endpoint 1: DeleteAssessment patch" (full snippet to apply)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-PATTERNS.md §1.3-1.7 (insertion order summary)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-VALIDATION.md (per-task verification map row 323-01-01..03)
  </read_first>
  <action>
Patch `Controllers/AssessmentAdminController.cs` method `DeleteAssessment(int id)` (sekitar L2007-2146) dengan 3 perubahan atomic:

**Perubahan A — Snapshot capture (sisip TEPAT SETELAH existing `preDeleteResponseCount` capture di ~L2057, SEBELUM HC re-check guard ~L2062):**

```csharp
// PHASE 323: snapshot EditLog count SEBELUM cascade (sama pola preDeleteResponseCount)
int preDeleteEditLogsCount = await _context.AssessmentEditLogs
    .CountAsync(e => e.AssessmentSessionId == id);
```

**Perubahan B — Cascade block baru (sisip ANTARA HC re-check guard return ~L2070 dan existing `pkgResponses` block ~L2072 — block ini WAJIB pertama di cascade chain per D-01):**

```csharp
// PHASE 323: Delete AssessmentEditLogs (Restrict FK — must be removed before session)
var editLogs = await _context.AssessmentEditLogs
    .Where(e => e.AssessmentSessionId == id)
    .ToListAsync();
if (editLogs.Any())
{
    logger.LogInformation($"Deleting {editLogs.Count} assessment edit logs");
    _context.AssessmentEditLogs.RemoveRange(editLogs);
}
```

**Perubahan C — Extend audit description (modify existing `_auditLog.LogAsync(...)` call ~L2123-2129 — APPEND ` EditLogsCount={preDeleteEditLogsCount}` di tail Description string, semua field lain unchanged):**

BEFORE:
```csharp
$"Deleted assessment '{assessmentTitle}' [ID={id}] Status={preDeleteStatus} ResponseCount={preDeleteResponseCount}",
```

AFTER:
```csharp
$"Deleted assessment '{assessmentTitle}' [ID={id}] Status={preDeleteStatus} ResponseCount={preDeleteResponseCount} EditLogsCount={preDeleteEditLogsCount}",
```

**Constraint:**
- JANGAN ubah `[HttpPost]` / `[Authorize(Roles = "Admin, HC")]` / `[ValidateAntiForgeryToken]` attribute (preserve auth).
- JANGAN buat `using var tx` baru — sisip block DI DALAM scope existing (L2040).
- JANGAN ubah `catch (Exception ex)` block (preserve auto-rollback via `using` disposal).
- Variable name: `editLogs` (lokal, single-session) + `preDeleteEditLogsCount` (snapshot, ikut pola `preDeleteResponseCount`).
- Comment header WAJIB pakai prefix `// PHASE 323:` untuk traceability.

**Verify intermediate (manual sanity check sebelum proceed):**
```bash
grep -n "PHASE 323" Controllers/AssessmentAdminController.cs | wc -l  # expect: 3 (2 dari Task 1 di DeleteAssessment, akan jadi 9 setelah Task 2+3)
grep -n "preDeleteEditLogsCount" Controllers/AssessmentAdminController.cs | wc -l  # expect: 3 (1 capture + 1 audit di DeleteAssessment + akan jadi 6 setelah Task 2+3)
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tee /tmp/p323-task1-build.log; grep -E "error|Build succeeded" /tmp/p323-task1-build.log; grep -c "Build succeeded" /tmp/p323-task1-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit 0, output mengandung "Build succeeded" (0 error)
    - `grep -c "PHASE 323" Controllers/AssessmentAdminController.cs` ≥ 3 (1 snapshot comment + 1 cascade block comment di DeleteAssessment + per Task 2/3 nanti tambah lagi)
    - `grep -c "preDeleteEditLogsCount" Controllers/AssessmentAdminController.cs` ≥ 3 (1 declaration + 1 audit reference di DeleteAssessment; intermediate state)
    - `grep "EditLogsCount=" Controllers/AssessmentAdminController.cs | head -3` shows: at least 1 hit di string interpolation `EditLogsCount={preDeleteEditLogsCount}`
    - `git diff --stat Models/ Migrations/ Data/ApplicationDbContext.cs` empty (no schema/model change)
    - `git diff Controllers/AssessmentAdminController.cs` menunjukkan tidak ada perubahan `[HttpPost]` / `[Authorize]` / `[ValidateAntiForgeryToken]` / `using var tx` / `catch (Exception ex)` line
  </acceptance_criteria>
  <done>
    DeleteAssessment endpoint memiliki: (1) `preDeleteEditLogsCount` capture sebelum HC re-check guard, (2) `editLogs` RemoveRange block sebagai langkah PERTAMA di cascade chain (sebelum pkgResponses), (3) audit description dengan token `EditLogsCount={preDeleteEditLogsCount}` di tail. dotnet build hijau, 0 warning baru. Schema files untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch DeleteAssessmentGroup endpoint (~L2202-2265) — multi-session siblingIds variant</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (lines 2147-2284 — full DeleteAssessmentGroup method, identifying siblingIds collection + transaction L2184 + existing pkgResponses block L2215-2220 + audit L2265)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-RESEARCH.md §"Endpoint 2: DeleteAssessmentGroup patch" (multi-session adaptation snippet)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-PATTERNS.md §1.3 (per endpoint predicate adjustment — siblingIds.Contains)
    - Hasil Task 1 (DeleteAssessment) — pattern referensi langsung di file yang sama
  </read_first>
  <action>
Patch `Controllers/AssessmentAdminController.cs` method `DeleteAssessmentGroup(int id)` (sekitar L2147-2284) dengan 3 perubahan analog Task 1, tapi pakai predicate multi-session `siblingIds.Contains(...)`.

**Perubahan A — Snapshot capture (sisip TEPAT SETELAH existing `preDeleteResponseCount` capture di ~L2202-an):**

```csharp
// PHASE 323: snapshot EditLog count agregat semua siblings
int preDeleteEditLogsCount = await _context.AssessmentEditLogs
    .CountAsync(e => siblingIds.Contains(e.AssessmentSessionId));
```

**Perubahan B — Cascade block baru (sisip SEBELUM existing `pkgResponses` block di ~L2215-an, paling awal di cascade chain per D-01):**

```csharp
// PHASE 323: Delete AssessmentEditLogs untuk semua siblings (Restrict FK)
var allEditLogs = await _context.AssessmentEditLogs
    .Where(e => siblingIds.Contains(e.AssessmentSessionId))
    .ToListAsync();
if (allEditLogs.Any())
{
    logger.LogInformation($"DeleteAssessmentGroup: deleting {allEditLogs.Count} edit logs across {siblingIds.Count} sessions");
    _context.AssessmentEditLogs.RemoveRange(allEditLogs);
}
```

**Perubahan C — Extend audit description (modify existing `_auditLog.LogAsync(...)` call di ~L2265 — APPEND ` EditLogsCount={preDeleteEditLogsCount}` di tail string):**

BEFORE (representative — exact existing format dapat berbeda; preserve semua field existing + tambah token baru di akhir):
```csharp
$"Deleted assessment group '{rep.Title}' ({rep.Category}) [RepId={id}] SessionCount={preDeleteSessionCount} Status={preDeleteStatus} ResponseCount={preDeleteResponseCount}",
```

AFTER:
```csharp
$"Deleted assessment group '{rep.Title}' ({rep.Category}) [RepId={id}] SessionCount={preDeleteSessionCount} Status={preDeleteStatus} ResponseCount={preDeleteResponseCount} EditLogsCount={preDeleteEditLogsCount}",
```

**Constraint:**
- Variable name: `allEditLogs` (collection, multi-session) + `preDeleteEditLogsCount` (snapshot, konsisten naming dengan Task 1).
- Predicate WAJIB `siblingIds.Contains(...)` — bukan `== id` (id = representative session, bukan keseluruhan group).
- Comment header pakai `// PHASE 323:` prefix.
- Pelajari Pitfall 3 RESEARCH.md — JANGAN lupa endpoint ini. JANGAN refactor jadi helper (deferred per CONTEXT.md).
- Tidak ubah `using var tx`, `[ValidateAntiForgeryToken]`, atau `catch` block existing.

**Sanity check post-edit:**
```bash
grep -c "siblingIds.Contains(e.AssessmentSessionId)" Controllers/AssessmentAdminController.cs  # expect: 2 (1 capture CountAsync + 1 Where ToListAsync)
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tee /tmp/p323-task2-build.log; grep -E "error|Build succeeded" /tmp/p323-task2-build.log; grep -c "Build succeeded" /tmp/p323-task2-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit 0, "Build succeeded" present, 0 error
    - `grep -c "siblingIds.Contains(e.AssessmentSessionId)" Controllers/AssessmentAdminController.cs` ≥ 2 (1 CountAsync + 1 ToListAsync di DeleteAssessmentGroup)
    - `grep -c "allEditLogs" Controllers/AssessmentAdminController.cs` ≥ 4 (1 declaration + 1 `.Any()` + 1 log + 1 RemoveRange di DeleteAssessmentGroup; akan bertambah di Task 3)
    - `grep -c "PHASE 323" Controllers/AssessmentAdminController.cs` ≥ 5 (Task 1 contributed 3, Task 2 adds 2 more — comment baris atas snapshot + cascade block)
    - `git diff --stat Models/ Migrations/ Data/ApplicationDbContext.cs` empty
  </acceptance_criteria>
  <done>
    DeleteAssessmentGroup endpoint memiliki: (1) `preDeleteEditLogsCount` snapshot pakai `siblingIds.Contains`, (2) `allEditLogs` RemoveRange block sebagai langkah PERTAMA di cascade chain, (3) audit description extend dengan token `EditLogsCount={preDeleteEditLogsCount}`. dotnet build hijau.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch DeletePrePostGroup endpoint (~L2333-2392) — multi-session groupIds variant + final verification</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (lines 2286-2410+ — full DeletePrePostGroup method, identifying groupIds collection + linkedGroupId parameter + transaction L2313 + existing pkgResponses block ~L2348 + audit ~L2392)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-RESEARCH.md §"Endpoint 3: DeletePrePostGroup patch" (LinkedGroupId variant)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-PATTERNS.md §1.3-1.7 (insertion order final summary)
    - Hasil Task 1 + Task 2 (DeleteAssessment + DeleteAssessmentGroup) sebagai pattern di file yang sama
  </read_first>
  <action>
Patch `Controllers/AssessmentAdminController.cs` method `DeletePrePostGroup(int linkedGroupId)` (sekitar L2286-2410+) dengan 3 perubahan analog Task 2, tapi pakai predicate `groupIds.Contains(...)` (LinkedGroupId variant).

**Perubahan A — Snapshot capture (sisip TEPAT SETELAH existing `preDeleteResponseCount` capture di ~L2333-an):**

```csharp
// PHASE 323: snapshot EditLog count agregat PrePost group
int preDeleteEditLogsCount = await _context.AssessmentEditLogs
    .CountAsync(e => groupIds.Contains(e.AssessmentSessionId));
```

**Perubahan B — Cascade block baru (sisip SEBELUM existing `pkgResponses` block di ~L2348-an, paling awal di cascade chain per D-01):**

```csharp
// PHASE 323: Delete AssessmentEditLogs untuk semua sessions di Pre-Post group
var allEditLogs = await _context.AssessmentEditLogs
    .Where(e => groupIds.Contains(e.AssessmentSessionId))
    .ToListAsync();
if (allEditLogs.Any())
{
    logger.LogInformation($"DeletePrePostGroup: deleting {allEditLogs.Count} edit logs across {groupIds.Count} sessions (LinkedGroupId={linkedGroupId})");
    _context.AssessmentEditLogs.RemoveRange(allEditLogs);
}
```

**Perubahan C — Extend audit description (modify existing `_auditLog.LogAsync(...)` call di ~L2392 — APPEND ` EditLogsCount={preDeleteEditLogsCount}` di tail string):**

BEFORE (representative — preserve semua field existing):
```csharp
$"Deleted Pre-Post group '{groupTitle}' [LinkedGroupId={linkedGroupId}] Status={preDeleteStatus} ResponseCount={preDeleteResponseCount}",
```

AFTER:
```csharp
$"Deleted Pre-Post group '{groupTitle}' [LinkedGroupId={linkedGroupId}] Status={preDeleteStatus} ResponseCount={preDeleteResponseCount} EditLogsCount={preDeleteEditLogsCount}",
```

**Constraint:**
- Variable name: `allEditLogs` + `preDeleteEditLogsCount` (konsisten Task 2).
- Predicate WAJIB `groupIds.Contains(...)` — bukan `siblingIds` (variabel berbeda di endpoint ini).
- Log message wajib include `(LinkedGroupId={linkedGroupId})` token untuk diagnostic.
- Comment header pakai `// PHASE 323:` prefix.
- Tidak ubah `using var tx`, attributes, atau `catch` block existing.

**FINAL verification batch (semua 3 endpoint sudah patched):**

```bash
# 1. Grep verification — wajib semua green sebelum commit
echo "=== Per-endpoint cascade block count (expect 3) ==="
grep -c "AssessmentEditLogs.RemoveRange\|_context\.AssessmentEditLogs\.RemoveRange" Controllers/AssessmentAdminController.cs
# expect: ≥3

echo "=== Per-endpoint audit token count (expect 3) ==="
grep -c "EditLogsCount={preDeleteEditLogsCount}" Controllers/AssessmentAdminController.cs
# expect: ≥3 (1 per endpoint di string audit description)

echo "=== Per-endpoint snapshot count (expect 3) ==="
grep -c "preDeleteEditLogsCount = await _context.AssessmentEditLogs" Controllers/AssessmentAdminController.cs
# expect: 3 (1 declaration per endpoint)

echo "=== PHASE 323 comment count (expect ≥ 9: 3 snapshot + 3 cascade + 3 audit comments) ==="
grep -c "PHASE 323" Controllers/AssessmentAdminController.cs
# expect: ≥6 (snapshot + cascade comments — audit description may not have inline comment but still has token; minimum 6 if only 2 comment per endpoint)

echo "=== Schema/model/migration untouched ==="
git diff --stat Models/ Migrations/ Data/ApplicationDbContext.cs
# expect: empty output (0 changed files)

echo "=== Final build ==="
dotnet build 2>&1 | tail -5
# expect: Build succeeded, 0 Error(s)
```

Jika semua grep PASS + git diff empty + dotnet build green → Wave 1 selesai, ready for Wave 2 (Plan 02).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tee /tmp/p323-task3-build.log; grep -c "Build succeeded" /tmp/p323-task3-build.log; grep -c "AssessmentEditLogs.RemoveRange\|_context\.AssessmentEditLogs\.RemoveRange" Controllers/AssessmentAdminController.cs; grep -c "EditLogsCount={preDeleteEditLogsCount}" Controllers/AssessmentAdminController.cs; git diff --stat Models/ Migrations/ Data/ApplicationDbContext.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit 0, "Build succeeded" present, 0 error, 0 warning baru (compare ke baseline pre-phase)
    - `grep -c "AssessmentEditLogs.RemoveRange\|_context\.AssessmentEditLogs\.RemoveRange" Controllers/AssessmentAdminController.cs` ≥ 3 (1 per endpoint)
    - `grep -c "EditLogsCount={preDeleteEditLogsCount}" Controllers/AssessmentAdminController.cs` ≥ 3 (audit description di 3 endpoint)
    - `grep -c "preDeleteEditLogsCount = await _context.AssessmentEditLogs" Controllers/AssessmentAdminController.cs` = 3 (1 snapshot declaration per endpoint)
    - `grep -c "PHASE 323" Controllers/AssessmentAdminController.cs` ≥ 6 (minimum 2 comments per endpoint × 3 endpoint)
    - `grep -c "groupIds.Contains(e.AssessmentSessionId)" Controllers/AssessmentAdminController.cs` ≥ 2 (1 CountAsync + 1 ToListAsync di DeletePrePostGroup)
    - `git diff --stat Models/ Migrations/ Data/ApplicationDbContext.cs` empty (0 file modified) — CASCADE-01 #7
    - `git diff Controllers/AssessmentAdminController.cs` menunjukkan TIDAK ada modifikasi line: `[HttpPost]`, `[Authorize(Roles`, `[ValidateAntiForgeryToken]`, `using var tx = await`, `catch (Exception ex)`, `RedirectToAction("ManageAssessment")` (preserve auth/error/redirect existing)
  </acceptance_criteria>
  <done>
    DeletePrePostGroup endpoint memiliki: (1) `preDeleteEditLogsCount` snapshot pakai `groupIds.Contains`, (2) `allEditLogs` RemoveRange block sebagai langkah PERTAMA di cascade chain dengan log mention `LinkedGroupId`, (3) audit description extend dengan token `EditLogsCount={preDeleteEditLogsCount}`. Final grep verification 4-batch green. Schema untouched. dotnet build clean. Plan 01 selesai — siap commit + lanjut Wave 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → AssessmentAdminController endpoints | Admin/HC role-authenticated user POST request — auth gate sudah ada (Phase 312) |
| Controller → EF Core LINQ → SQL Server | Parameterized via EF — no raw SQL added di Phase 323 |
| Controller → AuditLogService → AuditLog table | Description field `nvarchar(max)` — no MaxLength constraint (verified `Models/AuditLog.cs:36-37`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-323-01 | Tampering | EF Core cascade query `Where(e => e.AssessmentSessionId == id)` / `siblingIds.Contains(...)` / `groupIds.Contains(...)` | accept | EF Core parameterized LINQ auto-bind `id` / collection as SQL parameter — no SQL injection vector. Verified Phase 312 pattern (production-tested). Inherited from existing standard. |
| T-323-02 | Tampering | Audit log Description interpolation `$"... EditLogsCount={preDeleteEditLogsCount}"` | accept | `preDeleteEditLogsCount` is `int` (cannot inject special chars); `assessmentTitle` already interpolated existing tanpa sanitize karena stored as plain text `nvarchar(max)` (bukan HTML/SQL context). No new attack surface. |
| T-323-03 | Tampering | TOCTOU race — concurrent edit jawaban (Phase 321 EditPesertaAnswers) sambil delete | mitigate | Existing `using var tx = await _context.Database.BeginTransactionAsync()` di L2040/L2184/L2313 (Phase 312 WR-01) wraps EditLogs RemoveRange + cascade + audit — atomic. EditLog write di Phase 321 EDIT path juga tx-wrapped → konflik concurrent diserialize SQL Server isolation level default (ReadCommitted). Inherited mitigation. |
| T-323-04 | Tampering | CSRF on POST DeleteAssessment / DeleteAssessmentGroup / DeletePrePostGroup | accept | Existing `[ValidateAntiForgeryToken]` di L2010, L2151, L2289 — PRESERVED (Task constraint: tidak ubah attribute). Inherited from Phase 312. |
| T-323-05 | Elevation of Privilege | Worker (non-Admin, non-HC) bypass delete | accept | Existing `[Authorize(Roles = "Admin, HC")]` attribute + `EnsureCanDeleteAsync` role-tier guard (Phase 312) — PRESERVED. Phase 323 hanya tambah cascade block DI DALAM guard scope. Inherited mitigation. |
| T-323-06 | Information Disclosure | Logger `LogInformation` mungkin print sensitive count info ke log file production | accept | Log message hanya count integer (`{N} assessment edit logs`) + LinkedGroupId — tidak ada PII / data jawaban. Mirror existing pattern `PackageUserResponses` (production-tested no incident). |

**Risk summary:** Phase 323 = pure cascade extension dengan zero new attack surface. Semua threat existing sudah ada mitigasi di Phase 312 attribute guard + transaction scope + EF parameterization. No new threat introduced — Risk LOW per ASVS L1 baseline.
</threat_model>

<verification>
## Plan-level verification (semua 3 task complete)

```bash
# 1. Build clean
dotnet build 2>&1 | grep -E "Build succeeded|Error" | head -5
# Expect: "Build succeeded.", "0 Error(s)"

# 2. Cascade block presence — 3 endpoint
grep -n "AssessmentEditLogs.RemoveRange\|_context\.AssessmentEditLogs\.RemoveRange" Controllers/AssessmentAdminController.cs
# Expect: 3+ lines, dengan line number berbeda menunjukkan 3 endpoint terpisah (~L2071, ~L2215, ~L2348)

# 3. Audit description token — 3 endpoint
grep -n "EditLogsCount={preDeleteEditLogsCount}" Controllers/AssessmentAdminController.cs
# Expect: 3 lines (DeleteAssessment audit, DeleteAssessmentGroup audit, DeletePrePostGroup audit)

# 4. Snapshot capture — 3 endpoint
grep -n "preDeleteEditLogsCount = await _context.AssessmentEditLogs" Controllers/AssessmentAdminController.cs
# Expect: 3 lines

# 5. Order verification — EditLogs block sebelum pkgResponses di tiap endpoint
awk '/PHASE 323.*Delete AssessmentEditLogs/ {p323=NR} /Delete PackageUserResponses.*Restrict/ {p312=NR; if (p323 && p323 < p312) print "OK order ", p323, "<", p312; else print "BAD ORDER"; p323=0; p312=0}' Controllers/AssessmentAdminController.cs
# Expect: 3 "OK order" lines (per D-01 lock)

# 6. Schema/model/migration untouched (CASCADE-01 #7)
git diff --stat Models/ Migrations/ Data/ApplicationDbContext.cs
# Expect: empty output

# 7. Auth attributes preserved (defensive)
grep -c "\\[Authorize(Roles = \"Admin, HC\")\\]" Controllers/AssessmentAdminController.cs
# Expect: ≥3 (pre-existing — should not decrease)
grep -c "\\[ValidateAntiForgeryToken\\]" Controllers/AssessmentAdminController.cs
# Expect: ≥3 (pre-existing — should not decrease)
```
</verification>

<success_criteria>

1. ✅ `dotnet build` clean (0 error, 0 new warning) — CLAUDE.md Develop Workflow Step 3 partial
2. ✅ 3 endpoint memiliki `_context.AssessmentEditLogs.RemoveRange(...)` block — CASCADE-01 acceptance #1
3. ✅ Block EditLogs paling awal di cascade chain (sebelum pkgResponses) di 3 endpoint — D-01 LOCKED
4. ✅ Snapshot `preDeleteEditLogsCount` di-capture SEBELUM cascade di 3 endpoint — Pitfall 1 avoided
5. ✅ Audit description di 3 endpoint mengandung token `EditLogsCount={preDeleteEditLogsCount}` — D-02 LOCKED + CASCADE-01 acceptance #4
6. ✅ Schema/Model/Migration files (Models/ Migrations/ Data/ApplicationDbContext.cs) TIDAK berubah — CASCADE-01 acceptance #7
7. ✅ `[Authorize(Roles = "Admin, HC")]` + `[ValidateAntiForgeryToken]` + `using var tx` + `catch (Exception ex)` block existing PRESERVED — auth/atomicity inherited mitigation
8. ✅ `if (collection.Any())` guard di 3 endpoint cascade block (D-04 LOCKED — skip log saat no edits)
9. ✅ Comment header `// PHASE 323:` di-inject di setiap block baru untuk traceability future audit

Ready for Wave 2 (Plan 02 — Playwright E2E smoke test + manual UAT).
</success_criteria>

<output>
After completion, create `.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-01-SUMMARY.md` dengan:
- 3 endpoint patched (line range pre/post per endpoint)
- Grep verification output snapshot
- `dotnet build` final tail
- Pointer ke Plan 02 untuk Wave 2 E2E test
</output>
