---
phase: 324
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/GradingService.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [DUPL-01]
tags: [bug-fix, regression-removal, grading-service, assessment-admin]
must_haves:
  truths:
    - "Block insert TR auto di GradingService.GradeAndCompleteAsync hilang"
    - "Block insert TR auto di AssessmentAdminController.FinalizeEssayGrading hilang"
    - "Cascade TR Pass-Fail update + Fail-Pass insert di RegradeAfterEditAsync hilang"
    - "AssessmentSession.NomorSertifikat revoke (Pass-Fail) tetap jalan"
    - "AssessmentSession cert generate retry loop (Fail-Pass) tetap jalan"
    - "dotnet build 0 Error setelah 3 edit"
    - "Cross-grep TrainingRecords.Add di Services + AssessmentAdminController + CMPController returns 0 hit"
  artifacts:
    - path: "Services/GradingService.cs"
      provides: "GradeAndCompleteAsync + RegradeAfterEditAsync (TR auto-create removed)"
      contains: "Phase 324 D-01"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "FinalizeEssayGrading (TR auto-create removed)"
      contains: "Phase 324 D-02"
  key_links:
    - from: "CMPController.SubmitExam line 1727"
      to: "GradingService.GradeAndCompleteAsync"
      via: "method invocation"
    - from: "AssessmentAdminController.AkhiriUjian + AkhiriSemuaUjian"
      to: "GradingService.GradeAndCompleteAsync"
      via: "method invocation"
    - from: "AssessmentAdminController.SubmitEditAnswers line 2948"
      to: "GradingService.RegradeAfterEditAsync"
      via: "method invocation"
---

<objective>
Hapus mekanisme auto-create `TrainingRecord` di 3 lokasi production code (D-01, D-02, D-03 dari CONTEXT.md) supaya `AssessmentSession` (Status=Completed) jadi sole source-of-truth untuk row "Assessment Online" di `/CMP/Records`. Phase 324 = subtract phase, tidak ada feature baru, tidak ada migration, tidak ada model change.

Purpose: Resolve regression dari commit `766011b6` (2026-04-10) yang re-introduce auto-create TR setelah sebelumnya dihapus `79284609` (2026-03-18). Visual duplicate 2 row per event di `/CMP/Records` (1 dari AssessmentSession + 1 dari TR copy) hilang.

Output: 2 file edit (3 lokasi block hapus) + `dotnet build` 0 Error + cross-grep audit final 0 hit di scope production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-CONTEXT.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md
@CLAUDE.md

<interfaces>
Public API yang TIDAK berubah:
- `GradingService.GradeAndCompleteAsync(AssessmentSession) -> Task<bool>`
- `GradingService.RegradeAfterEditAsync(AssessmentSession) -> Task<(int,bool,int?,bool?)>`
- `GradingService.PreviewScoreAsync(...)` - tetap
- `AssessmentAdminController.FinalizeEssayGrading(int sessionId)` - signature sama
- `AssessmentAdminController.AkhiriUjian / AkhiriSemuaUjian` - signature sama

Call sites (regression surface):
- `Controllers/CMPController.cs:1727` SubmitExam -> GradeAndCompleteAsync
- `Controllers/AssessmentAdminController.cs:3767, 3847` AkhiriUjian/AkhiriSemuaUjian -> GradeAndCompleteAsync
- `Controllers/AssessmentAdminController.cs:2948` SubmitEditAnswers -> RegradeAfterEditAsync
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hapus block TR auto-create di GradingService.GradeAndCompleteAsync (D-01)</name>
  <read_first>
    - Services/GradingService.cs (lines 200-330 untuk full GradeAndCompleteAsync context, block target di lines 254-285)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (section "Diff Spec - D-01", "Pitfall 1", "Pitfall 2")
  </read_first>
  <files>Services/GradingService.cs</files>
  <behavior>
    - Sebelum edit: `grep -c "TrainingRecords.Add" Services/GradingService.cs` returns 2 (1 di line 268 GradeAndCompleteAsync, 1 di line 545 RegradeAfterEditAsync)
    - Setelah edit Task 1: returns 1 (hanya RegradeAfterEditAsync yang masih ada, akan dihapus di Task 3)
    - Lines 251-253 UserPackageAssignments ExecuteUpdateAsync TETAP (commit IsCompleted=true)
    - Lines 287+ cert generate retry loop TETAP UTUH
    - Line 324 NotifyIfGroupCompleted call TETAP
    - dotnet build 0 Error, no new unused-variable warning (judul + trainingRecordExists ikut hilang bersama block)
  </behavior>
  <action>
    Edit `Services/GradingService.cs` — DELETE lines 254-285 inclusive dan REPLACE dengan 4-line comment marker untuk audit trail.

    EXACT range to delete (verbatim from Read verification): lines 254 sampai 285 (32 line total). Mulai dari `// ---- 5. Buat TrainingRecord (dengan duplicate guard) ----` (line 254) sampai closing brace `}` (line 285) yang menutup blok `if (!trainingRecordExists && session.AssessmentType != "PreTest") { try { _context.TrainingRecords.Add(...); SaveChangesAsync(); } catch (DbUpdateException) { LogWarning(...); } }`.

    EXACT replacement (4 line, indent 12 spaces):
    Line 1: `// Phase 324 D-01: TrainingRecord auto-create removed.`
    Line 2: `// AssessmentSession (Status=Completed) is sole source for "Assessment Online" row`
    Line 3: `// di /CMP/Records via WorkerDataService.GetUnifiedRecords. Regression dari commit 766011b6`
    Line 4: `// (re-add 2026-04-10) - original removal di 79284609 (2026-03-18).`

    JANGAN sentuh:
    - Lines 250-253 (UserPackageAssignments ExecuteUpdateAsync) - KEEP
    - Lines 287+ (cert generate retry loop `if (session.GenerateCertificate && isPassed)`) - KEEP UTUH
    - Lines 323-324 (NotifyIfGroupCompleted) - KEEP
    - Any `using` statement - KEEP semua

    Verify post-edit: Read lines 248-295 untuk confirm replacement clean dan downstream lines tidak corrupted.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 Error (warning baseline preserved)
    - `grep -c "TrainingRecords.Add" Services/GradingService.cs` returns 1 (Task 3 akan turunkan ke 0)
    - `grep -n "Phase 324 D-01" Services/GradingService.cs` returns 1 hit
    - `grep -n "trainingRecordExists" Services/GradingService.cs` returns 0 hit
    - `grep -n "GenerateCertificate && isPassed" Services/GradingService.cs` returns 1 hit (cert logic intact)
    - Lines 251-253 (UserPackageAssignments ExecuteUpdateAsync) masih ada via Read verification
  </acceptance_criteria>
  <done>Block TR auto-create di GradeAndCompleteAsync DELETED, marker comment Phase 324 D-01 ditambahkan, build green, cert/notify logic intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hapus block TR auto-create di AssessmentAdminController.FinalizeEssayGrading (D-02)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (lines 3380-3470 untuk full FinalizeEssayGrading context, block target lines 3404-3421)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (section "Diff Spec - D-02")
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <behavior>
    - Sebelum edit: `grep -c "TrainingRecords.Add" Controllers/AssessmentAdminController.cs` returns 1 (line 3410)
    - Setelah edit: returns 0
    - Lines 3423+ cert generate `if (session.GenerateCertificate && isPassed)` TETAP UTUH
    - Lines 3439+ audit log (5b. Audit log Phase 310 D-07) TETAP
    - dotnet build 0 Error
  </behavior>
  <action>
    Edit `Controllers/AssessmentAdminController.cs` — DELETE lines 3404-3421 inclusive (18 line) dan REPLACE dengan 3-line comment marker.

    EXACT range to delete (verbatim from Read verification): lines 3404 sampai 3421. Mulai dari `// 4. Generate TrainingRecord (duplicate guard - same as GradingService)` (line 3404) sampai closing brace `}` (line 3421) yang menutup blok `if (!trExists) { _context.TrainingRecords.Add(new TrainingRecord {...}); await _context.SaveChangesAsync(); }`.

    EXACT replacement (3 line, indent 12 spaces):
    Line 1: `// Phase 324 D-02: TrainingRecord auto-create removed dari FinalizeEssayGrading path.`
    Line 2: `// Konsisten dengan GradingService.GradeAndCompleteAsync (D-01).`
    Line 3: `// AssessmentSession sole source-of-truth untuk Records page display.`

    JANGAN sentuh:
    - Lines 3380-3402 (current check + race detection + alreadyFinalized Json return) - KEEP
    - Lines 3423+ (cert generate `if (session.GenerateCertificate && isPassed)`) - KEEP UTUH
    - Lines 3439+ (audit log "5b" + downstream logic) - KEEP

    Verify post-edit: Read lines 3398-3445 untuk confirm replacement clean, cert generate dan audit log masih intact.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 Error
    - `grep -c "TrainingRecords.Add" Controllers/AssessmentAdminController.cs` returns 0
    - `grep -n "Phase 324 D-02" Controllers/AssessmentAdminController.cs` returns 1 hit
    - `grep -n "trExists" Controllers/AssessmentAdminController.cs` returns 0 hit
    - `grep -n "GenerateCertificate && isPassed" Controllers/AssessmentAdminController.cs` returns >=1 hit (cert generate logic intact)
  </acceptance_criteria>
  <done>Block TR auto-create di FinalizeEssayGrading DELETED, marker comment Phase 324 D-02 ditambahkan, cert generate logic intact, build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Hapus cascade TR Pass-Fail di GradingService.RegradeAfterEditAsync (D-03) + audit cross-grep final</name>
  <read_first>
    - Services/GradingService.cs (lines 440-575 untuk full RegradeAfterEditAsync context)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (section "Diff Spec - D-03", "Pitfall 3", "Pitfall 4", "Pitfall 7")
  </read_first>
  <files>Services/GradingService.cs</files>
  <behavior>
    - Pass-Fail branch: revoke NomorSertifikat + ValidUntil ExecuteUpdateAsync TETAP (lines 488-492). DELETE TR Status="Failed" block (lines 494-497 including `var judul`).
    - Fail-Pass branch: cert generate retry loop TETAP UTUH (lines 506-538). DELETE TR insert/update Status="Passed" block (lines 540-561 including `var judul`).
    - Log messages di-update: hilangkan reference "TR=Failed" dan "TR=Passed".
    - Cross-grep final via Grep tool: `TrainingRecords\.(Add|AddAsync|AddRange)` di paths `Services/`, `Controllers/AssessmentAdminController.cs`, `Controllers/CMPController.cs` returns 0 hit.
    - dotnet build 0 Error.
  </behavior>
  <action>
    Edit `Services/GradingService.cs` — 2 sub-edit di RegradeAfterEditAsync.

    SUB-EDIT 1 (Pass-Fail branch, lines 485-502):

    DELETE lines 494-497 (4 line - block TR Status="Failed") yang isinya:
      `var judul = $"Assessment: {session.Title}";` (line 494)
      `await _context.TrainingRecords` (line 495)
      `    .Where(t => t.UserId == session.UserId && t.Judul == judul && t.Tanggal == session.Schedule)` (line 496)
      `    .ExecuteUpdateAsync(t => t.SetProperty(r => r.Status, "Failed"));` (line 497)

    Lines 488-492 (revoke NomorSertifikat ExecuteUpdateAsync) KEEP UTUH.

    UPDATE log message line 500 (di dalam `_logger.LogInformation(...)`): ganti string `"RegradeAfterEditAsync: session {SessionId} flip Pass->Fail - cert dicabut, TR=Failed."` menjadi `"RegradeAfterEditAsync: session {SessionId} flip Pass->Fail - sertifikat dicabut (Phase 324 D-03)."`.

    UPDATE comment line 487 (di dalam branch body, sebelum ExecuteUpdateAsync revoke): dari `// Pass -> Fail` menjadi `// Pass -> Fail: revoke sertifikat (Phase 324 D-03: TR cascade removed)`.

    SUB-EDIT 2 (Fail-Pass branch, lines 503-567):

    DELETE lines 540-561 (22 line - block TR insert/update Status="Passed") yang dimulai dari `var judul = $"Assessment: {session.Title}";` (line 540) sampai closing brace `}` (line 561) yang menutup `else { existingTr.Status = "Passed"; await _context.SaveChangesAsync(); }`.

    Lines 506-538 (cert generate retry loop) KEEP UTUH - jangan sentuh sama sekali.

    UPDATE log message lines 564-566 (di dalam `_logger.LogInformation(...)` setelah cert retry loop): ganti string `"RegradeAfterEditAsync: session {SessionId} flip Fail->Pass - cert generated (if applicable), TR=Passed."` menjadi `"RegradeAfterEditAsync: session {SessionId} flip Fail->Pass - sertifikat dibuat (jika applicable, Phase 324 D-03)."`.

    UPDATE comment line 505 (di dalam branch body, sebelum `if (session.GenerateCertificate ...)`): dari `// Fail -> Pass` menjadi `// Fail -> Pass: generate sertifikat (Phase 324 D-03: TR cascade removed)`.

    JANGAN sentuh:
    - Lines 440-484 (XML doc + method signature + steps 1-4 DELETE ET / recompute / insert ET / update session) - KEEP
    - Lines 568-571 (comment "Pass-Pass, Fail-Fail" + return tuple + closing brace method) - KEEP
    - Lines 506-538 (cert retry loop body) - KEEP UTUH

    FINAL CROSS-GREP AUDIT (mandatory per Pitfall 7) - jalankan via Grep tool:
    - Pattern: `TrainingRecords\.(Add|AddAsync|AddRange)`
    - Paths: `Services/`, `Controllers/AssessmentAdminController.cs`, `Controllers/CMPController.cs`
    - Expected: 0 hit. Kalau ada hit, STOP dan investigate sebelum commit.

    Note: `Controllers/TrainingAdminController.cs` HARUS ada hits (admin manual add - OUT OF SCOPE). Verify by running Grep di `Controllers/TrainingAdminController.cs` separately returns >=4 hit (Phase 324 tidak touch ini).

    Verify post-edit: Read lines 480-575 untuk confirm replacement clean.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 Error
    - `grep -c "TrainingRecords.Add" Services/GradingService.cs` returns 0
    - `grep -n "Phase 324 D-03" Services/GradingService.cs` returns 2 hit (Pass-Fail branch comment + Fail-Pass branch comment + log messages variasi - minimal 2)
    - Grep tool: pattern `TrainingRecords\.(Add|AddAsync|AddRange)` di `Services/` returns 0 hit
    - Grep tool: pattern `TrainingRecords\.(Add|AddAsync|AddRange)` di `Controllers/AssessmentAdminController.cs` returns 0 hit
    - Grep tool: pattern `TrainingRecords\.(Add|AddAsync|AddRange)` di `Controllers/CMPController.cs` returns 0 hit
    - Grep tool: pattern `TrainingRecords\.(Add|AddAsync|AddRange)` di `Controllers/TrainingAdminController.cs` returns >=4 hit (out of scope, intact)
    - `grep -n "ExecuteUpdateAsync.*NomorSertifikat.*null" Services/GradingService.cs` returns 1 hit (Pass-Fail revoke logic intact)
    - `grep -n "CertNumberHelper.GetNextSeqAsync" Services/GradingService.cs` returns >=2 hit (cert generate logic intact - di GradeAndCompleteAsync + RegradeAfterEditAsync Fail-Pass)
  </acceptance_criteria>
  <done>Cascade TR di RegradeAfterEditAsync DELETED kedua branch, cert revoke (Pass-Fail) + cert generate (Fail-Pass) logic intact, log messages updated dengan reference Phase 324 D-03, cross-grep audit confirms 0 hit di scope production, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Controller/Test) -> GradingService | trusted in-process call, no untrusted input crosses |
| caller (Controller/Test) -> AssessmentAdminController endpoint | already protected by `[Authorize(Roles="Admin, HC")]` attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-324-01 | Tampering | GradingService.GradeAndCompleteAsync edit | mitigate | dotnet build 0 Error gate + cross-grep audit verify 3 call sites (CMP/Admin/Admin) tetap jalan via Plan 02 UAT |
| T-324-02 | Tampering | RegradeAfterEditAsync cert revoke logic accidentally over-deleted | mitigate | EXACT line range di action (lines 488-492 KEEP, lines 494-497 DELETE) + acceptance_criteria grep verify `ExecuteUpdateAsync.*NomorSertifikat.*null` masih ada |
| T-324-03 | Tampering | RegradeAfterEditAsync cert generate loop accidentally over-deleted | mitigate | EXACT line range di action (lines 506-538 KEEP UTUH) + acceptance_criteria grep `CertNumberHelper.GetNextSeqAsync` returns >=2 hit |
| T-324-04 | Repudiation | Edit hilang audit trail karena tidak ada commit marker | mitigate | Marker comment `Phase 324 D-01/D-02/D-03` di 3 lokasi + atomic commit per task dengan message `feat(324-01): hapus TR auto-create ...` |
| T-324-05 | DoS-self | Build break dari unused variable / orphan brace | mitigate | DELETE block seluruhnya (variable judul + trainingRecordExists + trExists ikut hilang) + dotnet build gate per task |
| T-324-06 | Info Disclosure | Log message bocor PII | accept | Log existing pakai `{SessionId}` (numeric), tidak ada PII string. New log message sama struktur. |
</threat_model>

<verification>
End-of-plan verification (executor harus jalankan sebelum mark plan complete):

1. `dotnet build` - 0 Error
2. Grep audit (Grep tool, output_mode count):
   - `TrainingRecords\.(Add|AddAsync|AddRange)` di `Services/` -> 0
   - `TrainingRecords\.(Add|AddAsync|AddRange)` di `Controllers/AssessmentAdminController.cs` -> 0
   - `TrainingRecords\.(Add|AddAsync|AddRange)` di `Controllers/CMPController.cs` -> 0
   - `TrainingRecords\.(Add|AddAsync|AddRange)` di `Controllers/TrainingAdminController.cs` -> >=4 (OUT OF SCOPE, sanity intact)
3. Marker presence:
   - `grep -n "Phase 324 D-01" Services/GradingService.cs` -> 1 hit
   - `grep -n "Phase 324 D-02" Controllers/AssessmentAdminController.cs` -> 1 hit
   - `grep -n "Phase 324 D-03" Services/GradingService.cs` -> >=2 hit (comment + log)
4. Cert logic intact:
   - `grep -n "CertNumberHelper.GetNextSeqAsync" Services/GradingService.cs` -> >=2 hit
   - `grep -n "ExecuteUpdateAsync.*NomorSertifikat" Services/GradingService.cs` -> >=2 hit (cert assign di GradeAndComplete + cert revoke di RegradeAfterEdit Pass-Fail)
5. Manual smoke (post-build): `dotnet run` di lokal, login worker, submit assessment biasa, buka `/CMP/Records` - assert hanya 1 row "Assessment Online" muncul (visual proof, screenshot disimpan di Plan 03 task D-09).
</verification>

<success_criteria>
- 3 atomic commit terbentuk dengan message `feat(324-01-NN): <description>` per task
- Final commit hash di-record untuk dipakai di Plan 04 IT handoff document
- DUPL-01 acceptance criteria 1-6 all green (lihat REQUIREMENTS.md)
- No regression: 3 call sites GradeAndCompleteAsync (CMP SubmitExam, Admin AkhiriUjian, Admin AkhiriSemuaUjian) + 1 call site RegradeAfterEditAsync (Admin SubmitEditAnswers) tetap functional (verified via Plan 02 Playwright UAT)
- Marker comments di 3 lokasi untuk audit trail Phase 324
</success_criteria>

<output>
After completion, create `.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-01-SUMMARY.md` containing:
- 3 commit hash (per task)
- Final commit hash (untuk dipakai Plan 04 IT handoff)
- Pre/post grep counts (TrainingRecords.Add di 4 file)
- Build status (0 Error confirmed)
- Files modified: `Services/GradingService.cs` + `Controllers/AssessmentAdminController.cs`
- Notes untuk Plan 02 executor: code edit selesai, UAT siap di-run di Wave 2
</output>
