# Requirements Archive: v19.0 Portal HC Bug Fixes (Cascade Hardening)

**Archived:** 2026-05-29
**Status:** SHIPPED LOCAL (push pending IT)
**Scope:** v18.0 + v19.0 multi-milestone snapshot at archive time. v18.0 section also archived separately at `milestones/v18.0-REQUIREMENTS.md`.

For current requirements (v20.0+), see `.planning/REQUIREMENTS.md`.

---

# Project Requirements (multi-milestone, v18.0 + v19.0)

> File scope: v18.0 + v19.0. Setiap section punya header milestone sendiri. Older milestones (v1.0-v17.0) di `MILESTONES.md` log.

---

## v18.0 Cascade Delete Hardening + Duplicate TrainingRecord Fix

**Milestone:** v18.0
**Started:** 2026-05-26
**Status:** SHIPPED (323 PUSHED tag v18.0-p323-complete; 324 SHIPPED LOCAL, push pending IT bundle dengan v19.0 batch)

## Goal

1. Tutup oversight Phase 321 (model `AssessmentEditLog` baru, FK Restrict) di Phase 312 cascade (`DeleteAssessment` / `DeleteAssessmentGroup` / `DeletePrePostGroup`).
2. Hapus regression dari commit `766011b6` (2026-04-10) yang re-introduce auto-create `TrainingRecord` di `GradingService.GradeAndCompleteAsync` setelah sebelumnya dihapus `79284609` (2026-03-18) karena visual duplicate di `/CMP/Records`.

## v18.0 Requirements

### Cascade Delete (Phase 323)

- [x] **CASCADE-01** (Phase 323, SHIPPED PUSHED): Admin/HC dapat menghapus `AssessmentSession` (single, group, atau Pre-Post group) yang sudah pernah di-edit soalnya — `AssessmentEditLogs` ikut ter-cascade tanpa FK Restrict exception. Tag `v18.0-p323-complete` pushed origin/main.

  **Acceptance criteria:**
  1. `DeleteAssessment(id)`, `DeleteAssessmentGroup(id)`, `DeletePrePostGroup(linkedGroupId)` di `Controllers/AssessmentAdminController.cs` masing-masing tambah `RemoveRange(AssessmentEditLogs)` sebelum cascade existing
  2. Session belum pernah di-edit → tetap sukses (no regression)
  3. Session sudah di-edit ≥1 soal → sukses, `AssessmentEditLogs` ikut terhapus
  4. Audit log `DeleteAssessment` / `DeleteAssessmentGroup` / `DeletePrePostGroup` tetap tercatat normal
  5. Transaction scope existing (line 2040, 2184, 2313) tetap membungkus delete + cascade — rollback bersih saat exception
  6. Smoke test 3 skenario: (a) session no-edits → delete OK, (b) session 1+ edits → delete OK, (c) group dengan campuran sibling no-edits + edits → delete OK
  7. Tidak ubah schema DB, model class, FK definition, atau migration

### Duplicate TrainingRecord Fix (Phase 324)

- [x] **DUPL-01** (Phase 324, SHIPPED LOCAL): Hapus block auto-create `TrainingRecord` di 3 lokasi production code supaya `AssessmentSession` jadi sole source-of-truth untuk row "Assessment Online" di `/CMP/Records`.

  **Acceptance criteria:**
  1. `Services/GradingService.cs:255-285` block `_context.TrainingRecords.Add(...)` di `GradeAndCompleteAsync` DIHAPUS (lines 255-285 including PreTest gate dan try-catch DbUpdateException dead code)
  2. `Controllers/AssessmentAdminController.cs:3404-3421` block `_context.TrainingRecords.Add(...)` di `FinalizeEssayGrading` DIHAPUS
  3. `Services/GradingService.cs:483-567` cascade `TrainingRecord` Pass↔Fail flip DIHAPUS — TR update Status="Failed" (lines 494-497) + TR insert/update Status="Passed" (lines 540-561) hilang; `AssessmentSession.NomorSertifikat` revoke (lines 488-492) + cert generate retry loop (lines 506-538) TETAP
  4. `dotnet build` 0 Error setelah edit (no compile regression)
  5. Cross-grep `TrainingRecords\.(Add|AddAsync|AddRange)` di `Services/` + `Controllers/AssessmentAdminController.cs` + `Controllers/CMPController.cs` returns 0 hit (TrainingAdminController boleh, out-of-scope)
  6. Tidak ubah schema DB, model class, atau migration

- [x] **DUPL-02a (Phase 324, SHIPPED LOCAL — 1 override)**: Playwright E2E UAT cover S1 + S2 sebagai primary regression guard, dengan 7-scenario spec skeleton file untuk Phase 325 continuation. Override: live Playwright run deferred (equivalent UAT via browser MCP proof).

  **Acceptance criteria:**
  1. `tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` exists dengan 7 test block (S1-S7) — file structure complete
  2. S1: Worker submit assessment biasa (non-essay) → `/CMP/Records` tampil EXACTLY 1 row "Assessment Online" untuk event (bukan 2) — **IMPLEMENTED + GREEN**
  3. S2: PreTest tetap skip TR (regression guard existing behavior) — **IMPLEMENTED + GREEN**
  4. S3-S7: file block exists dengan `test.skip(true, "Implementasi di Phase 325 — butuh fixture seed assessment + sertifikat existing state")` + TODO comment referencing CONTEXT.md D-07b
  5. Full spec run ≤ 3 menit di lokal (`cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts`)
  6. Helper module `tests/e2e/helpers/phase324.ts` exists dengan fully implemented helpers (no placeholder bodies, no `break; // placeholder` patterns)

- [ ] **DUPL-02b (Phase 325 — DEFERRED-SUPERSEDED)**: Implementasi S3-S7 sebagai dedicated phase berikutnya untuk close UAT coverage. **Status final**: Phase 325 pivoted ke v19.0 security hardening (SEC-01/02/03 + xUnit foundation), tidak pick up S3-S7. Skeleton `test.skip(true, "...Phase 325...")` jadi historical placeholder. Re-promote ke v20.0 jika butuh full coverage.

  **Acceptance criteria:**
  1. S3: Essay flow finalize → tidak insert TR (butuh seed essay assessment + HC FinalizeEssayGrading action)
  2. S4: HC `AkhiriUjian` (force-end single) → grading tetap jalan, tidak insert TR (butuh seed running session + 1 worker yang belum submit)
  3. S5: HC `AkhiriSemuaUjian` (bulk) → grading tetap jalan untuk semua, tidak insert TR (butuh seed running session + multi-worker pending)
  4. S6: HC `RegradeAfterEdit` Pass→Fail → `AssessmentSession.IsPassed=false` + `NomorSertifikat=null`, tidak ada TR cascade (butuh seed Pass session + sertifikat existing)
  5. S7: HC `RegradeAfterEdit` Fail→Pass → `AssessmentSession.NomorSertifikat != null` (kalau `GenerateCertificate && !PreTest`), tidak ada TR cascade (butuh seed Fail session)
  6. `test.skip(true, ...)` annotations di S3-S7 dihapus + actual implementation
  7. Full spec run ≤ 5 menit (toleransi tinggi karena tambah 5 scenario + fixture setup)

  **Phase 325 spawn:** User akan jalankan `/gsd-add-phase` setelah Phase 324 ship. Slug draft: `complete-uat-phase324-s3-to-s7`. Rationale split documented di CONTEXT.md D-07b.

- [x] **DUPL-03** (Phase 324, SHIPPED LOCAL): Cleanup data legacy `TrainingRecord` auto-generated di DB lokal via SEED_WORKFLOW BACKUP/RESTORE lifecycle. SQL script `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` + SEED_JOURNAL append.

  **Acceptance criteria:**
  1. Schema verify pre-cleanup: identifikasi filter column yang tepat (RESEARCH A3 — `CreatedAt` TIDAK ada di model; alternative `TanggalSelesai` atau Id-based)
  2. Pre-check: identifikasi orphan risk via `RenewsTrainingId` (RESEARCH Open Question #3) — query count child TR yang me-renew parent target delete; null-clear bila > 0
  3. BACKUP DB lokal via `sqlcmd ... BACKUP DATABASE HcPortalDB_Dev` sebelum eksekusi cleanup
  4. SQL script `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` exists dengan structure: SET XACT_ABORT ON + transaction + pre-count + safety cap (5000) + DELETE + post-count + COMMIT (atau ROLLBACK on cap exceeded / post-count > 0)
  5. Pre-count > 0 dan post-count = 0 (cleanup berhasil)
  6. Re-run script: post-count tetap 0 (idempotent)
  7. `docs/SEED_JOURNAL.md` append 1 entry dengan classification `temporary + local-only`, status awal `active`, status akhir `cleaned` setelah verifikasi
  8. Tidak hapus `AssessmentSessions` atau dependent (sole source-of-truth tetap utuh)

- [x] **DUPL-04** (Phase 324, SHIPPED LOCAL): IT handoff HTML doc siap untuk eksekusi cleanup di server Dev/Prod. `docs/DB_HANDOFF_IT_2026-05-26.html`.

  **Acceptance criteria:**
  1. `docs/DB_HANDOFF_IT_2026-05-26.html` exists, fork dari `docs/DB_HANDOFF_IT_2026-05-13.html` (verbatim CSS variables — `--brand: #e30613`, `--navy: #1e3a8a`)
  2. Section: Context (kenapa cleanup) + Commit hash (final Wave 1 hash) + Pre-check sample query (Pitfall 6 — visual sanity 20 row) + Pre-check orphan query (RESEARCH OQ #3) + Backup command (BACKUP DATABASE) + SQL cleanup script embedded (verbatim copy dari `docs/sql/`) + Verification query (post-count) + Rollback plan (RESTORE FROM DISK)
  3. EXPLICIT ordering callout: **Step 1 deploy code dulu (commit hash sudah merge), Step 2 cleanup data** (RESEARCH Pitfall 5 — race condition prevention)
  4. Section "Yang TIDAK perlu IT lakukan" (no schema change, no migration) — sejajar template 2026-05-13
  5. NO schema change flag eksplisit di TL;DR

- [x] **DUPL-05** (Phase 324, SHIPPED LOCAL): Pre-fix repro + post-fix verify lokal terdokumentasi. Screenshots `docs/screenshots/phase324/before-fix.png` + `after-fix.png` + cross-grep audit 0 hit.

  **Acceptance criteria:**
  1. Pre-fix screenshot `docs/screenshots/phase324/before-fix.png` — `/CMP/Records` 2 row state (1 "Assessment Online" + 1 "Training Manual" untuk event sama)
  2. Post-fix screenshot `docs/screenshots/phase324/after-fix.png` — `/CMP/Records` 1 row state (hanya "Assessment Online")
  3. SQL count verify lokal (D-10): sebelum cleanup `SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND {date_filter}` returns > 0; setelah cleanup returns 0
  4. Cross-grep audit final via `Grep` tool returns 0 hit untuk `TrainingRecords.(Add|AddAsync|AddRange)` di `Services/GradingService.cs`, `Controllers/AssessmentAdminController.cs`, `Controllers/CMPController.cs`

## Future Requirements (deferred)

Tidak ada — milestone hotfix-scope. Bisa expand via `/gsd-add-phase` bila ada bug serupa ditemukan saat audit.

## Out of Scope (explicit)

### Phase 323
- **Audit endpoint delete lain** (DeleteCategory, DeletePackage, DeleteQuestion, DeleteWorker, DeleteTraining, DeleteManualAssessment, DeleteOrganizationUnit, DeleteCoachingSession, BudgetTrainingDelete, dll.) — fokus milestone hanya 3 endpoint `DeleteAssessment*` yang directly affected oleh Phase 321 `AssessmentEditLog`. Audit luas masuk milestone berikutnya bila perlu.
- **Refactor cascade helper** (extract reusable `CascadeAssessmentSessionDependents(sessionIds)` helper) — tidak perlu untuk 3 endpoint; tunggu ada signal pattern reuse.
- **Migration ubah FK Restrict → Cascade DB-level** — endpoint cascade lebih eksplisit + audit-friendly daripada DB cascade silent.
- **UI surface old assessment (filter `>= 7 hari`)** di `ManageAssessmentTab_Assessment` line 115 — separate UX issue, masuk backlog tersendiri.

### Phase 324
- **Tambah unique index `(UserId, Judul, Tanggal)` di TrainingRecord** — tidak diperlukan setelah auto-create dihapus. Phase masa depan defensive measure jika Excel import generate duplicate.
- **Refactor `GetUnifiedRecords` query** — kalau di masa depan ada perubahan source-of-truth (misal pindah ke materialized view), boleh refactor. Tidak Phase 324.
- **Audit TR legacy dengan Judul similar pattern tapi admin manual** — kalau ternyata ada admin yang ketik manual "Assessment: ..." pre-bug, audit terpisah.
- **Touch `TrainingAdminController`** — admin manual add TR tetap utuh, tidak diubah.
- **Schema migration** — Phase 324 = subtract phase, NO migration.

## Traceability

| REQ-ID | Phase | Status |
|--------|-------|--------|
| CASCADE-01 | 323 | shipped (pushed v18.0-p323-complete) |
| DUPL-01 | 324 | shipped (local) |
| DUPL-02a | 324 | shipped (local, 1 override — live Playwright deferred, browser MCP proof equivalent) |
| DUPL-02b | 325 | **deferred-superseded** (Phase 325 pivoted ke v19.0 SEC; re-promote ke v20.0 jika butuh) |
| DUPL-03 | 324 | shipped (local) |
| DUPL-04 | 324 | shipped (local) |
| DUPL-05 | 324 | shipped (local) |

**Shipped: 6/7 — Deferred-Superseded: 1 (DUPL-02b)** (DUPL-02 split → DUPL-02a Phase 324 + DUPL-02b Phase 325 per checker iter 3 BLOCKER 1). v18.0 closed substantively 2026-05-29 audit.

---

*Requirements created: 2026-05-26 · Phase 324 DUPL-01..05 appended 2026-05-26 saat `/gsd-plan-phase 324`.*

---

# Requirements — v19.0 Portal HC Bug Fixes (Cascade Hardening)

**Milestone:** v19.0
**Started:** 2026-05-26
**Status:** SHIPPED LOCAL 11/11 (2026-05-28) — push origin/main pending IT availability
**Spec:** `docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md` (P01-P06 spec covers Phase 325-327. Phase 328-335 = scope expansion audit-driven, no spec backref.)

## Goal

Tindak lanjut audit "Sertifikat Ecosystem" (12 finding, 1 HIGH) + cascade hardening sweep (Phase 328 audit → 7 next-phase proposal). 6 finding actionable di spec (P01-P06) + 7 cascade hardening (HIGH + MED) di-resolve sebelum production ship.

## v19.0 Requirements

### Wave 1 — Spec-Driven (Phase 325-327)

- [x] **SEC-01 (P01 Path Traversal Fix)** — Phase 325 Plan 02 (helper `FileUploadHelper.SaveFileAsync` strip via `Path.GetFileName` D-01). **Acceptance**: 1 file `Path.GetFileName` + reject `..` traversal, dotnet test pass.
- [x] **SEC-02 (P02 Magic Byte Validation)** — Phase 325 Plan 02 (helper magic byte gate `MatchesMagicByte` D-09 + 4 key Dictionary) + Plan 03 (enforce di endpoint Add/Edit Training — eliminate inline duplicate bypass). **Acceptance**: PDF/JPG/PNG upload accept valid magic, reject mismatched magic.
- [x] **SEC-03 (P05 Hard Delete FK)** — Phase 325 Plan 04 (DeleteTraining + DeleteManualAssessment + DeleteAssessment renewal pre-check pre-tx). **Acceptance**: session yg jadi RenewsSessionId source di-delete → TempData["Error"], no FK 500.
- [x] **FOUNDATION (xUnit bootstrap)** — Phase 325 Plan 01 (HcPortal.Tests/ project + 6 stub test + sln entry). **Acceptance**: `dotnet test` 5 pass + 2 skip (flipped GREEN by Plan 02).
- [x] **CLOSURE (SC end-to-end UAT)** — Phase 325 Plan 05 (5 threat manual UAT browser + 3 SC autoplay + final test 10/10 PASS). **Acceptance**: T-325-01..05 all MITIGATED, dotnet test final 10/0/0.
- [x] **VAL-01 (P03 DAG Enforcement Cycle Prevention)** — Phase 326 Plan 01. Validasi RenewsSessionId tidak introduce cycle (DAG topology). **Acceptance**: insert/update yg buat cycle ditolak via validator + UX message.
- [x] **VAL-02 (P06 Permanent + ValidUntil Reject)** — Phase 326 Plan 02. Validator block save kalau certificate marked Permanent tapi ValidUntil field ada nilai (kontradiktif). **Acceptance**: form save reject + inline error.
- [x] **TZ-01 (P04 DateOnly Refactor)** — Phase 327 Plan 01-08. Migrasi `ValidUntil DateTime` → `DateOnly` (8 plan: model, migration `ChangeValidUntilToDateOnly`, Razor view binding, ViewModel, status derivation, EF query, tests, smoke pass). **Acceptance**: tz drift permanently eliminated, 1 migration applied lokal pending IT promo.

### Wave 2 — Audit-Driven Cascade (Phase 328-335)

- [x] **CSCD-AUDIT (Phase 328 Cascade Audit Sweep)** — Audit-only phase, zero source code change. 14 mutator endpoint + 5 preview endpoint diaudit. Classification: 8 HIGH + 5 MED + 0 LOW + 1 NONE (DeleteAssessment gold standard). 7 next-phase fix proposal documented. **Acceptance**: 19 endpoint scope assessed + AUDIT.md detail per endpoint + 7 fix proposal disetujui user.
- [x] **CSCD-01 (DeleteAssessmentGroup + DeletePrePostGroup Renewal Pre-Check)** — Phase 329. Apply Phase 325 P05 pattern ke 2 group endpoint sebelum BeginTransactionAsync. **Acceptance**: 5/5 must-haves VERIFIED, dotnet test 18/18 pass, UAT Playwright 2/2.
- [x] **CSCD-02 (Cascade MED Bundle)** — Phase 330. 5 endpoint: DeleteCategory + DeletePackage + DeleteQuestion (AssessmentAdminController) + DeleteOrganizationUnit (OrganizationController) + NotificationService.DeleteAsync. **Acceptance**: 9/9 AC PASS, try/catch DbUpdateException + audit log (DeleteQuestion+OrgUnit baru).
- [x] **CSCD-03 (DeleteTraining + DeleteManualAssessment Atomicity)** — Phase 331. File capture-before-tx + tx wrap Remove+SaveChanges+AuditLog + tx.CommitAsync SEBELUM File.Delete + inner try/catch warn-only post-commit. **Acceptance**: 8/8 AC PASS, 7/7 grep, dotnet test 18/18.
- [x] **CSCD-04 (DeleteBagian File Atomicity)** — Phase 332. Extract kkjPaths+cpdpPaths SEBELUM tx + RemoveRange(KKJ+CPDP+Bagian) tx + 2 File.Delete loops POST commit. **Acceptance**: 8/8 AC PASS, 8/8 grep, dotnet test 18/18.
- [x] **CSCD-05 (DeleteCoachingSession File Atomicity)** — Phase 333. Reorganize existing tx (BUKAN tambah baru) — List<string>? pathsToDelete outer + populate INSIDE tx + File.Delete POST CommitAsync + catch DbUpdateException specific + catch Exception fallback. **Acceptance**: 10/10 AC PASS, 5/5 grep.
- [x] **CSCD-06 (DeleteKompetensi Orphan Evidence + D6 Info Leak)** — Phase 334. List<string>? evidencePaths outer tx + populate INSIDE tx Step 2 + JSON parse EvidencePathHistory inner try/catch + File.Delete POST audit log + catch friendly (NO + ex.Message D6 fix CRITICAL). **Acceptance**: 11/11 AC PASS, 6/6 grep.
- [x] **CSCD-07 (DeleteWorker Triple-Fix D2+D5+D7)** — Phase 335 (FINAL HIGH). D5 cross-user renewal pre-check BEFORE tx (2 count queries WHERE UserId != id). D2 file collection BEFORE tx + INSIDE tx EvidencePath JSON. D7 tx wrap 9-step RemoveRange + UserManager.DeleteAsync + audit log + CommitAsync (D-11 verified Program.cs L47 same DbContext). Identity result.Succeeded early return INSIDE try. File.Delete POST commit. **Acceptance**: 12/12 AC PASS, 10/10 grep, dotnet test 18/18 — v19.0 MILESTONE CLOSE.

## Traceability Matrix v19.0

| REQ-ID | Phase | Plans | Verification | Status |
|---|---|---|---|---|
| SEC-01 | 325 | 325-02-PLAN.md | 325-VERIFICATION.md | passed |
| SEC-02 | 325 | 325-02 + 325-03-PLAN.md | 325-VERIFICATION.md | passed |
| SEC-03 | 325 | 325-04-PLAN.md | 325-VERIFICATION.md | passed |
| FOUNDATION | 325 | 325-01-PLAN.md | 325-VERIFICATION.md | passed |
| CLOSURE | 325 | 325-05-PLAN.md | 325-VERIFICATION.md | passed |
| VAL-01 | 326 | 326-01-PLAN.md | 326-VERIFICATION.md | passed |
| VAL-02 | 326 | 326-02-PLAN.md | 326-VERIFICATION.md | passed |
| TZ-01 | 327 | 327-01..08-PLAN.md | 327-VERIFICATION.md | passed |
| CSCD-AUDIT | 328 | 328-01-PLAN.md | 328-VERIFICATION.md | passed (audit-only) |
| CSCD-01 | 329 | 329-01-PLAN.md | 329-VERIFICATION.md | passed |
| CSCD-02 | 330 | 330-01-PLAN.md | 330-VERIFICATION.md | passed |
| CSCD-03 | 331 | 331-01-PLAN.md | 331-VERIFICATION.md | passed |
| CSCD-04 | 332 | 332-01-PLAN.md | 332-VERIFICATION.md | passed |
| CSCD-05 | 333 | 333-01-PLAN.md | 333-VERIFICATION.md | passed |
| CSCD-06 | 334 | 334-01-PLAN.md | 334-VERIFICATION.md | passed |
| CSCD-07 | 335 | 335-01-PLAN.md | 335-VERIFICATION.md | passed |

**Active mapped: 16/16 ✓ — Orphans: 0 — Duplicates: 0** (14 originally + 2 added 2026-05-29 audit fix — FOUNDATION + CLOSURE map Phase 325 Plans 01 + 05 yang sebelumnya unmapped)

## Out of Scope (explicit)

- 6 finding non-actionable dari "Sertifikat Ecosystem" audit (recommendations + future hardening) — defer ke v20.0+
- Phase 281, 285 (v11.2 paused) — tetap deferred
- Carry-over v13-v15 (7 item) + v15.0 EPRV-01 = 8 item — v20.0 backlog
- Bulk PDF export + Excel breakdown elemen teknis (`.planning/todos/pending/001-gap-ux-assessment-monitoring.md` — promoted note 2026-05-29) — v20.0 candidate

---

*Requirements v19.0 created retroactively: 2026-05-29 (post-execution audit migration). Spec v19.0 hanya cover Phase 325-327 (SEC/VAL/TZ); Phase 328-335 (CSCD-*) ditambahkan audit-driven dari Phase 328 sweep proposal.*
