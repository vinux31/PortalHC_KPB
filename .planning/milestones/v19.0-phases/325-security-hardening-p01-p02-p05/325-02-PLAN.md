---
phase: 325
plan: 02
type: execute
wave: 1
depends_on: [325-01]
files_modified:
  - Models/AssessmentConstants.cs
  - Helpers/FileUploadHelper.cs
  - HcPortal.Tests/FileUploadHelperTests.cs
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-6]
security_threats: [T-325-01, T-325-02]

must_haves:
  truths:
    - "AssessmentConstants.FileValidation.MagicBytes Dictionary ada dengan 4 key (.pdf .jpg .jpeg .png) per D-09"
    - "AssessmentConstants.FileValidation.MatchesMagicByte(ext, header) helper ada returns bool"
    - "FileUploadHelper.ValidateCertificateFile membaca 8 byte stream + reset Position=0 + reject magic byte mismatch dengan pesan VERBATIM D-06"
    - "FileUploadHelper.SaveFileAsync strip path traversal via Path.GetFileName() sebelum compose safeFileName per D-01"
    - "SaveFileAsync log warning kalau filename mengandung directory component per D-10"
    - "SaveFileAsync menerima ILogger? opsional param (default null) per RESEARCH §Pattern 1"
    - "Path.GetExtension(file.FileName).ToLowerInvariant() applied (spec line 100 micro-cleanup)"
    - "6 xUnit test PASS (0 Skip 0 Fail) — Skip attribute dihapus dari ExeRenamedPdf + JpegAlias"
  artifacts:
    - path: "Models/AssessmentConstants.cs"
      provides: "MagicBytes Dictionary + MatchesMagicByte helper"
      contains: "MagicBytes"
    - path: "Helpers/FileUploadHelper.cs"
      provides: "Magic byte validation + path traversal strip + optional ILogger param"
      contains: "Path.GetFileName"
    - path: "HcPortal.Tests/FileUploadHelperTests.cs"
      provides: "6 test GREEN"
      contains: "[Fact]"
  key_links:
    - from: "Helpers/FileUploadHelper.cs"
      to: "Models/AssessmentConstants.cs"
      via: "AssessmentConstants.FileValidation.MatchesMagicByte"
      pattern: "MatchesMagicByte"
    - from: "Helpers/FileUploadHelper.cs SaveFileAsync"
      to: "ILogger? parameter"
      via: "logger.LogWarning"
      pattern: "Path traversal attempt"
---

<objective>
Implementasi P01 (path traversal fix) + P02 (magic byte validation) di `Helpers/FileUploadHelper.cs` + extend `Models/AssessmentConstants.FileValidation` dengan `MagicBytes` Dictionary + `MatchesMagicByte` helper per D-09. Tambah `ILogger?` opsional parameter ke `SaveFileAsync` per D-10 untuk audit trail path traversal attempts. Flip 6 test xUnit ke GREEN (hapus 2 Skip attribute dari Plan 01).

Purpose: Address T-325-01 (path traversal HIGH) + T-325-02 (MIME spoof MED). Centralize magic byte signatures di constants per pattern existing. Helper static + optional ILogger param ringan (RESEARCH §Pattern 1), zero breaking change ke caller existing.

Output: Magic byte dict + helper di constants (D-09), helper validate + save updated (D-01/D-02/D-03/D-10), 6 unit test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-01-SUMMARY.md
@docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md
@Helpers/FileUploadHelper.cs
@Models/AssessmentConstants.cs
@HcPortal.Tests/FileUploadHelperTests.cs

<interfaces>
**Current `Helpers/FileUploadHelper.cs`** (61 lines, verified RESEARCH.md):
```csharp
public static class FileUploadHelper
{
    public static (bool IsValid, string? Error) ValidateCertificateFile(IFormFile? file) {
        if (file == null || file.Length == 0) return (true, null);
        var ext = Path.GetExtension(file.FileName);                                  // line 16 — MISSING .ToLowerInvariant()
        if (!AssessmentConstants.FileValidation.AllowedCertificateExtensions.Contains(ext))
            return (false, "Hanya file PDF, JPG, dan PNG yang diperbolehkan.");
        if (file.Length > AssessmentConstants.FileValidation.MaxCertificateFileSizeBytes)
            return (false, "Ukuran file maksimal 10MB.");
        return (true, null);                                                          // P02 INSERT POINT: magic byte check sebelum return
    }

    public static async Task<string?> SaveFileAsync(IFormFile? file, string webRootPath, string subFolder) {
        if (file == null || file.Length == 0) return null;
        var uploadDir = Path.Combine(webRootPath, subFolder);
        Directory.CreateDirectory(uploadDir);
        var uniqueId = Guid.NewGuid().ToString("N")[..8];
        var safeFileName = $"{DateTime.UtcNow:yyyyMMddHHmmssfff}_{uniqueId}_{file.FileName}";  // line 37 — P01 TARGET: ganti file.FileName → originalName
        // ... FileStream write
    }
}
```

**Current `Models/AssessmentConstants.cs` `FileValidation`** (lines 30-41):
```csharp
public static class FileValidation
{
    public const long MaxCertificateFileSizeBytes = 10 * 1024 * 1024;
    public static readonly HashSet<string> AllowedCertificateExtensions = new(StringComparer.OrdinalIgnoreCase)
    { ".pdf", ".jpg", ".jpeg", ".png" };
}
```
**EXTEND: tambah `MagicBytes` Dictionary + `MatchesMagicByte` helper per D-09 (verbatim RESEARCH §Pattern 2 line 219-254).**

**Error message strings LOCKED VERBATIM (D-06):**
- Magic byte mismatch: `"Isi file tidak cocok dengan ekstensi (magic byte mismatch)."`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AssessmentConstants.FileValidation dengan MagicBytes + MatchesMagicByte (D-09)</name>
  <files>Models/AssessmentConstants.cs</files>

  <behavior>
    - `MagicBytes` Dictionary punya 4 key lowercase (.pdf, .jpg, .jpeg, .png) — `.jpeg` alias share value sama dengan `.jpg`
    - `MatchesMagicByte(".pdf", new byte[]{0x25,0x50,0x44,0x46,...})` returns true
    - `MatchesMagicByte(".jpg", new byte[]{0xFF,0xD8,0xFF,...})` returns true
    - `MatchesMagicByte(".jpeg", new byte[]{0xFF,0xD8,0xFF,...})` returns true (alias)
    - `MatchesMagicByte(".png", new byte[]{0x89,0x50,0x4E,0x47,...})` returns true
    - `MatchesMagicByte(".pdf", new byte[]{0x4D,0x5A,...})` returns false (exe MZ header)
    - `MatchesMagicByte(".unknown", anyHeader)` returns false (unknown extension)
    - `MatchesMagicByte(".pdf", new byte[]{0x25,0x50})` returns false (header too short, length < prefix.length)
  </behavior>

  <read_first>
    - `Models/AssessmentConstants.cs` — current state (48 lines)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md` §D-09 — extract magic byte ke constants + helper method
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Pattern 2 line 219-254 — VERBATIM snippet source
    - `docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md` §5.2 line 113-119 — magic byte hex sequences (PDF `25 50 44 46`, JPG `FF D8 FF`, PNG `89 50 4E 47`)
  </read_first>

  <action>
    Edit `Models/AssessmentConstants.cs` — di dalam `public static class FileValidation` (line 30-41), tambah setelah `AllowedCertificateExtensions` HashSet declaration (verbatim per RESEARCH §Pattern 2 line 229-254):

    ```csharp
    // Phase 325 D-09: Magic byte signatures per extension (lowercase keys).
    // Value = array of valid byte prefixes (multiple = OR match).
    // Sumber: docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md §5.2 line 113-119.
    public static readonly Dictionary<string, byte[][]> MagicBytes = new(StringComparer.OrdinalIgnoreCase)
    {
        [".pdf"]  = new[] { new byte[] { 0x25, 0x50, 0x44, 0x46 } },  // %PDF
        [".jpg"]  = new[] { new byte[] { 0xFF, 0xD8, 0xFF } },         // JFIF/EXIF/SPIFF universal prefix
        [".jpeg"] = new[] { new byte[] { 0xFF, 0xD8, 0xFF } },         // alias .jpg (share value)
        [".png"]  = new[] { new byte[] { 0x89, 0x50, 0x4E, 0x47 } }    // PNG signature
    };

    /// <summary>
    /// Phase 325 D-09: Cek apakah header byte sequence cocok dengan magic byte signature untuk extension.
    /// </summary>
    /// <param name="ext">File extension (lowercase, eg ".pdf")</param>
    /// <param name="header">Buffer byte hasil baca stream 8-byte awal</param>
    /// <returns>true kalau header match salah satu prefix terdaftar untuk ext; false kalau ext tidak terdaftar atau no prefix match</returns>
    public static bool MatchesMagicByte(string ext, byte[] header)
    {
        if (!MagicBytes.TryGetValue(ext, out var prefixes)) return false;
        foreach (var prefix in prefixes)
        {
            if (header.Length < prefix.Length) continue;
            bool match = true;
            for (int i = 0; i < prefix.Length; i++)
            {
                if (header[i] != prefix[i]) { match = false; break; }
            }
            if (match) return true;
        }
        return false;
    }
    ```

    Pastikan `using System.Collections.Generic;` ada di file (untuk `Dictionary<>`) — kalau belum, tambah di top.

    Pastikan komentar dalam Bahasa Indonesia (per CLAUDE.md line 3).
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      grep -nE "MagicBytes|MatchesMagicByte" Models/AssessmentConstants.cs &amp;&amp; \
      grep -c "MagicBytes" Models/AssessmentConstants.cs &amp;&amp; \
      grep -q "0x25, 0x50, 0x44, 0x46" Models/AssessmentConstants.cs &amp;&amp; \
      grep -q "0xFF, 0xD8, 0xFF" Models/AssessmentConstants.cs &amp;&amp; \
      grep -q "0x89, 0x50, 0x4E, 0x47" Models/AssessmentConstants.cs &amp;&amp; \
      dotnet build HcPortal.sln --nologo -v quiet
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "MagicBytes|MatchesMagicByte" Models/AssessmentConstants.cs` returns ≥4 matches (1 dict decl + 1 helper signature + 1 usage di helper body + 1 doc-comment)
    - Magic byte hex literal exact: `0x25, 0x50, 0x44, 0x46` (PDF) + `0xFF, 0xD8, 0xFF` (JPG) + `0x89, 0x50, 0x4E, 0x47` (PNG)
    - `MagicBytes` Dictionary punya 4 key entry (`.pdf`, `.jpg`, `.jpeg`, `.png`)
    - `MatchesMagicByte` method signature: `public static bool MatchesMagicByte(string ext, byte[] header)`
    - `dotnet build HcPortal.sln` exit code 0
    - Komentar dalam Bahasa Indonesia
  </acceptance_criteria>

  <done>
    Constants extended. `MatchesMagicByte` callable dari helper + dari test. Plan 01 test `MatchesMagicByte_JpegAliasMatchesJpg` siap di-unskip di Task 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementasi P01 path traversal strip + P02 magic byte + ILogger opsional di FileUploadHelper</name>
  <files>Helpers/FileUploadHelper.cs</files>

  <behavior>
    - `ValidateCertificateFile(null)` returns `(true, null)` (no change)
    - `ValidateCertificateFile(file.pdf dengan magic byte 0x25 0x50 0x44 0x46)` returns `(true, null)`
    - `ValidateCertificateFile(malware.pdf dengan content MZ exe header 0x4D 0x5A)` returns `(false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).")`
    - `ValidateCertificateFile(test.jpg dengan 0xFF 0xD8 0xFF prefix)` returns `(true, null)`
    - `ValidateCertificateFile(test.png dengan 0x89 0x50 0x4E 0x47 prefix)` returns `(true, null)`
    - `ValidateCertificateFile(test.docx dengan random bytes)` returns `(false, "Hanya file PDF, JPG, dan PNG yang diperbolehkan.")` (extension reject sebelum magic check)
    - `ValidateCertificateFile(file 1-byte saja)` returns `(false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).")` (header read < 3 bytes minimum)
    - `SaveFileAsync(file dengan filename "../../test.pdf", ...)` menulis file dengan name `{ts}_{guid}_test.pdf` (path component "../../" stripped via `Path.GetFileName`)
    - `SaveFileAsync(file dengan filename "../../test.pdf", ..., logger)` memanggil `logger.LogWarning(...)` dengan template `"Path traversal attempt: filename={Original} stripped to {Safe}"` (D-10)
    - `SaveFileAsync(file dengan filename "normal.pdf", ..., logger)` TIDAK memanggil LogWarning (originalName == file.FileName)
    - `SaveFileAsync(file, webRoot, sub)` (tanpa logger param) tetap compile + jalan — backward compat
  </behavior>

  <read_first>
    - `Helpers/FileUploadHelper.cs` — current state (61 lines)
    - `Models/AssessmentConstants.cs` — post-Task1 state (verify MagicBytes + MatchesMagicByte ada)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md` §D-01 to D-03 + D-10 — locked behavior
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Pattern 1 line 174-217 — VERBATIM SaveFileAsync source
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Pattern 2 line 258-284 — VERBATIM ValidateCertificateFile source
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Pitfall 5 line 374-379 — stream.Read return value capture, reject kalau read < 3
    - `docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md` §5.1 + §5.2 — verbatim snippet
  </read_first>

  <action>
    **EDIT FILE:** `Helpers/FileUploadHelper.cs`.

    **Step 1: Tambah using untuk `Microsoft.Extensions.Logging` di top.**
    Tambah after existing `using` statements:
    ```csharp
    using Microsoft.Extensions.Logging;
    ```

    **Step 2: Replace `ValidateCertificateFile` (line 8-24) dengan implementasi magic byte (verbatim RESEARCH §Pattern 2 line 258-284):**

    ```csharp
    /// <summary>
    /// Phase 325 D-02/D-03/D-09: Validasi ekstensi + size + magic byte signature.
    /// Returns (true, null) kalau valid, (false, errorMessage) kalau invalid.
    /// </summary>
    public static (bool IsValid, string? Error) ValidateCertificateFile(IFormFile? file)
    {
        if (file == null || file.Length == 0) return (true, null);

        // Spec line 100: tambah .ToLowerInvariant() (micro-cleanup walau HashSet sudah OrdinalIgnoreCase).
        var ext = Path.GetExtension(file.FileName).ToLowerInvariant();
        if (!AssessmentConstants.FileValidation.AllowedCertificateExtensions.Contains(ext))
            return (false, "Hanya file PDF, JPG, dan PNG yang diperbolehkan.");

        if (file.Length > AssessmentConstants.FileValidation.MaxCertificateFileSizeBytes)
            return (false, "Ukuran file maksimal 10MB.");

        // Phase 325 D-02/D-03: Magic byte check — baca 8 byte awal stream, reset Position=0 untuk SaveFileAsync reuse.
        using var stream = file.OpenReadStream();
        var header = new byte[8];
        var read = stream.Read(header, 0, 8);
        stream.Position = 0; // reset agar SaveFileAsync tetap dapat full stream

        // Pitfall 5 RESEARCH: reject file lebih kecil dari smallest magic prefix (JPG 3-byte).
        if (read < 3)
            return (false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).");

        if (!AssessmentConstants.FileValidation.MatchesMagicByte(ext, header))
            return (false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).");

        return (true, null);
    }
    ```

    **PESAN ERROR LOCKED VERBATIM (D-06):** Jangan ubah ke "isi file" lowercase atau tambah period atau hapus "(magic byte mismatch)". Test `ExeRenamedPdf` assertion `Assert.Contains("magic byte", err!)` mengandalkan string ini.

    **Step 3: Replace `SaveFileAsync` (line 30-45) dengan implementasi D-01 + D-10 (verbatim RESEARCH §Pattern 1 line 177-207):**

    ```csharp
    /// <summary>
    /// Phase 325 D-01/D-10: Save file ke wwwroot/{subFolder}, strip directory component dari filename (path traversal),
    /// log warning kalau original filename mengandung path component (audit trail).
    /// Returns relative URL, atau null kalau file null/empty.
    /// </summary>
    public static async Task<string?> SaveFileAsync(
        IFormFile? file,
        string webRootPath,
        string subFolder,
        ILogger? logger = null)
    {
        if (file == null || file.Length == 0) return null;

        var uploadDir = Path.Combine(webRootPath, subFolder);
        Directory.CreateDirectory(uploadDir);

        // HIGH-01 fix: timestamp ms + short GUID cegah collision saat concurrent upload
        var uniqueId = Guid.NewGuid().ToString("N")[..8];

        // Phase 325 D-01: Strip directory component dari filename (defense-in-depth path traversal).
        var originalName = Path.GetFileName(file.FileName);

        // Phase 325 D-10: Audit trail kalau filename asli mengandung path component (forensik attack pattern).
        if (logger != null && !string.Equals(originalName, file.FileName, StringComparison.Ordinal))
        {
            logger.LogWarning(
                "Path traversal attempt: filename={Original} stripped to {Safe}",
                file.FileName, originalName);
        }

        var safeFileName = $"{DateTime.UtcNow:yyyyMMddHHmmssfff}_{uniqueId}_{originalName}";
        var filePath = Path.Combine(uploadDir, safeFileName);
        await using (var stream = new FileStream(filePath, FileMode.Create))
        {
            await file.CopyToAsync(stream);
        }
        // Normalisasi path separator untuk URL
        return $"/{subFolder.Trim().Trim('\\', '/')}/{safeFileName}";
    }
    ```

    **CRITICAL:** Parameter `ILogger? logger = null` HARUS default null (RESEARCH §Pitfall 3 line 363-367) supaya caller existing `FileUploadHelper.SaveFileAsync(file, _env.WebRootPath, "uploads/certificates")` tetap compile tanpa breaking change.

    **Step 4:** `DeleteFile` (line 51-59) — TIDAK disentuh. Phase 325 scope tidak ubah delete.
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      grep -n "Path.GetFileName" Helpers/FileUploadHelper.cs &amp;&amp; \
      grep -n "MatchesMagicByte" Helpers/FileUploadHelper.cs &amp;&amp; \
      grep -n "ILogger? logger = null" Helpers/FileUploadHelper.cs &amp;&amp; \
      grep -n "Path traversal attempt" Helpers/FileUploadHelper.cs &amp;&amp; \
      grep -n "Isi file tidak cocok dengan ekstensi (magic byte mismatch)" Helpers/FileUploadHelper.cs &amp;&amp; \
      grep -n "stream.Position = 0" Helpers/FileUploadHelper.cs &amp;&amp; \
      grep -n "ToLowerInvariant" Helpers/FileUploadHelper.cs &amp;&amp; \
      dotnet build HcPortal.sln --nologo -v quiet
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "Path.GetFileName" Helpers/FileUploadHelper.cs` returns ≥1 match (di SaveFileAsync)
    - `grep -n "MatchesMagicByte" Helpers/FileUploadHelper.cs` returns ≥1 match (di ValidateCertificateFile)
    - `grep -n "ILogger? logger = null" Helpers/FileUploadHelper.cs` returns 1 match (parameter signature)
    - `grep -n "Path traversal attempt: filename={Original} stripped to {Safe}" Helpers/FileUploadHelper.cs` returns 1 match (D-10 verbatim)
    - `grep -n "Isi file tidak cocok dengan ekstensi (magic byte mismatch)." Helpers/FileUploadHelper.cs` returns 2 match (1× read<3 + 1× MatchesMagicByte fail) — D-06 verbatim
    - `grep -n "stream.Position = 0" Helpers/FileUploadHelper.cs` returns 1 match (D-03)
    - `grep -n "ToLowerInvariant" Helpers/FileUploadHelper.cs` returns 1 match (spec line 100 micro-cleanup)
    - `using Microsoft.Extensions.Logging;` present di top of file
    - `dotnet build HcPortal.sln` exit code 0 (no breaking change ke caller existing — verify AddManualAssessment line 581 + EditManualAssessment line 681 tetap compile tanpa update)
    - Komentar dalam Bahasa Indonesia
  </acceptance_criteria>

  <done>
    P01 + P02 implementasi landed di helper. T-325-01 (path traversal HIGH) ditutup dengan defense-in-depth (`Path.GetFileName` + audit log). T-325-02 (MIME spoof MED) ditutup dengan magic byte signature. Caller existing tidak break (D-10 optional param). Task 3 yang flip test ke GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Hapus Skip attribute dari 2 test stub, flip 6 test ke GREEN</name>
  <files>HcPortal.Tests/FileUploadHelperTests.cs</files>

  <read_first>
    - `HcPortal.Tests/FileUploadHelperTests.cs` — post-Plan 01 state (4 pass + 2 skip)
    - `Helpers/FileUploadHelper.cs` — post-Task2 state (verify magic byte check ada)
    - `Models/AssessmentConstants.cs` — post-Task1 state (verify MatchesMagicByte ada)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-VALIDATION.md` — SC2/SC3/SC6 coverage map
  </read_first>

  <action>
    Edit `HcPortal.Tests/FileUploadHelperTests.cs`:

    **Step 1:** Cari `[Fact(Skip = "TODO Plan 02 D-02/D-03: magic byte check belum diimplementasikan — Plan 02 akan aktifkan + remove Skip.")]` di atas method `ValidateCertificateFile_ExeRenamedPdf_ReturnsInvalidMagicByte`. Ganti jadi `[Fact]`.

    **Step 2:** Cari `[Fact(Skip = "TODO Plan 02 D-09: MatchesMagicByte belum diimplementasikan.")]` di atas method `MatchesMagicByte_JpegAliasMatchesJpg`. Ganti jadi `[Fact]`. Un-comment body method (hapus komentar `//` di depan 3 baris `var jpg = ...` + 2 `Assert.True(...)`).

    **Hasil akhir kedua method:**
    ```csharp
    [Fact]
    public void ValidateCertificateFile_ExeRenamedPdf_ReturnsInvalidMagicByte()
    {
        var exe = new byte[] { 0x4D, 0x5A, 0x90, 0x00, 0x03, 0x00, 0x00, 0x00 }; // MZ exe
        var (ok, err) = FileUploadHelper.ValidateCertificateFile(MakeFile("malware.pdf", exe));
        Assert.False(ok);
        Assert.Contains("magic byte", err!);
    }

    [Fact]
    public void MatchesMagicByte_JpegAliasMatchesJpg()
    {
        var jpg = new byte[] { 0xFF, 0xD8, 0xFF, 0xE1, 0x00, 0x00, 0x00, 0x00 }; // EXIF variant
        Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpeg", jpg));
        Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpg", jpg));
    }
    ```
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      ! grep -q "Skip = \"TODO Plan 02" HcPortal.Tests/FileUploadHelperTests.cs &amp;&amp; \
      grep -c "\[Fact\]" HcPortal.Tests/FileUploadHelperTests.cs &amp;&amp; \
      dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo --filter "FullyQualifiedName~FileUploadHelperTests"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "Skip = \"TODO Plan 02" HcPortal.Tests/FileUploadHelperTests.cs` returns 0 matches (Skip attributes removed)
    - `grep -c "\[Fact\]" HcPortal.Tests/FileUploadHelperTests.cs` returns 7 (6 [Fact] + 1 maybe class-level — adjust expectation to ≥6)
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~FileUploadHelperTests"` exit code 0 dengan output: "Passed: 6, Failed: 0, Skipped: 0"
    - Test individu pass verifiable via filter:
      - `--filter "DisplayName~NullFile"` → 1 pass
      - `--filter "DisplayName~ValidPdf"` → 1 pass
      - `--filter "DisplayName~ValidJpg"` → 1 pass
      - `--filter "DisplayName~ValidPng"` → 1 pass
      - `--filter "DisplayName~ExeRenamed"` → 1 pass
      - `--filter "DisplayName~JpegAlias"` → 1 pass
  </acceptance_criteria>

  <done>
    Plan 02 GREEN. SC-2 (`.exe→.pdf` reject) + SC-3 (PDF/JPG/PNG valid lolos) + SC-6 (6 test pass) verified via xUnit. SC-1 (path traversal) butuh manual Postman test di Plan 05 (file system check tidak fit unit test).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP multipart upload → server filesystem | File upload via `IFormFile` melintas trust boundary. Filename = untrusted user input |
| Untrusted byte stream → file system write | Server harus validate isi + sanitize filename SEBELUM `FileStream.Create` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-01 | Tampering | `SaveFileAsync` line 37 — `file.FileName` raw composed ke path | mitigate | D-01: `Path.GetFileName(file.FileName)` strip directory component sebelum compose `safeFileName`. Even if attacker mengirim `../../etc/passwd`, server tulis `{ts}_{guid}_passwd` di `uploads/certificates/` (flat). |
| T-325-02 | Spoofing | `.exe` rename `.pdf` → existing extension check lolos | mitigate | D-02 + D-09: `MatchesMagicByte(ext, header[0..8])` validate isi byte cocok extension. PDF wajib `%PDF` (0x25 0x50 0x44 0x46), JPG `0xFF 0xD8 0xFF`, PNG `0x89 0x50 0x4E 0x47`. |
| T-325-01-aux | Information Disclosure | Path traversal attempts silent — no audit trail | mitigate | D-10: `logger.LogWarning("Path traversal attempt: filename={Original} stripped to {Safe}", ...)` kalau filename mengandung path component. Forensic capability untuk security incident review. |
| — | Denial of Service | Magic byte read pada file gigantic | accept | RESEARCH §Pitfall 5 + spec line 105: size check (`MaxCertificateFileSizeBytes` 10MB) executed BEFORE stream read 8-byte. File > 10MB rejected sebelum buka stream. |
| — | False positive | PDF variant lama tidak start `%PDF` | accept | RESEARCH §Risk 2: ISO 32000-1 mandate `%PDF` prefix. Manual test 3 PDF variant di Plan 05 SC3. Fallback `MimeDetective` NuGet defer kalau miss reproduced. |

Block on: T-325-01 (HIGH severity).
</threat_model>

<verification>
- `dotnet build HcPortal.sln` exit code 0
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit code 0 dengan Passed=6, Failed=0, Skipped=0
- `grep -nE "Path.GetFileName|MatchesMagicByte|ILogger\?" Helpers/FileUploadHelper.cs` returns ≥3 matches
- `grep -nE "MagicBytes|MatchesMagicByte" Models/AssessmentConstants.cs` returns ≥4 matches
- Error message verbatim D-06 ada di helper (Indonesian, mengandung "magic byte mismatch")
- Backward compat: existing caller `SaveFileAsync(file, env.WebRootPath, "uploads/certificates")` (line 581, 681 di TrainingAdminController) tetap compile tanpa update
</verification>

<success_criteria>
- SC-2 verified via unit test `ExeRenamedPdf_ReturnsInvalidMagicByte`
- SC-3 verified via 3 unit test `ValidPdf` + `ValidJpg` + `ValidPng`
- SC-6 verified via `dotnet test` 6/6 pass
- SC-1 partial coverage (helper jalan, manual Postman test masih di Plan 05)
- T-325-01 HIGH mitigated (path traversal strip + audit log)
- T-325-02 MED mitigated (magic byte validation centralized di constants)
</success_criteria>

<output>
After completion, create `.planning/phases/325-security-hardening-p01-p02-p05/325-02-SUMMARY.md` covering:
- Constants extended (MagicBytes dict + helper) per D-09
- Helper Validate + Save updated per D-01/D-02/D-03/D-10
- 6/6 test pass (no skip)
- Caller existing tidak break
- Handoff: Plan 03 yang refactor 3 inline duplicate sites di TrainingAdminController (else P02 bypass-able)
</output>
