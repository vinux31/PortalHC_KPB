---
phase: 326
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/TrainingAdminController.cs
  - Models/EditTrainingRecordViewModel.cs
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4
  - SC-5
  - SC-6
must_haves:
  truths:
    - "Add TR renewal tanggal < source ditolak dengan ModelState summary error"
    - "Add TR renewal tanggal valid (> source) lolos no regression"
    - "Add TR Permanent + ValidUntil isi ditolak dengan ModelState field error ValidUntil"
    - "Edit TR self-renewal (RenewsTrainingId == model.Id) ditolak ModelState summary"
    - "Edit handler persist RenewsTrainingId + RenewsSessionId ke entity (clear bekerja)"
    - "EditTrainingRecordViewModel punya 3 field baru: RenewsTrainingId, RenewsSessionId, RenewalSourceTitle"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "P03 DAG + self-renewal + P06 validators di AddTraining POST + EditTraining GET + EditTraining POST"
      contains: "Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew"
    - path: "Models/EditTrainingRecordViewModel.cs"
      provides: "Renewal FK passthrough + display title untuk Edit form"
      contains: "RenewalSourceTitle"
  key_links:
    - from: "AddTraining POST handler"
      to: "_context.TrainingRecords.FindAsync + _context.AssessmentSessions.FindAsync"
      via: "DAG check setelah L254 srcAlreadyRenewed"
      pattern: "src.Tanggal >= model.Tanggal"
    - from: "EditTraining POST handler"
      to: "entity assignment block L479-490"
      via: "tambah record.RenewsTrainingId = model.RenewsTrainingId"
      pattern: "record\\.RenewsTrainingId = model\\.RenewsTrainingId"
    - from: "EditTraining GET handler"
      to: "model.RenewalSourceTitle"
      via: "lookup TR or AS source sebelum SetTrainingCategoryViewBag"
      pattern: "RenewalSourceTitle = src\\?\\.Judul"
---

<objective>
Implement P03 DAG enforcement + self-renewal guard + P06 Permanent/ValidUntil rejection di Controllers/TrainingAdminController.cs (AddTraining POST + EditTraining GET + EditTraining POST) dan extend Models/EditTrainingRecordViewModel.cs dengan 3 field renewal passthrough.

Purpose: Tutup bug P03 (cycle detection via monotonic DAG constraint) + P06 (Permanent + ValidUntil mutual exclusion) di server-side ModelState. Persist RenewsTrainingId/RenewsSessionId di Edit handler agar "Hapus link renewal" button (Plan 02) actually clears FK di DB.

Output: 2 file modified. AddTraining POST punya 3 validator baru (P03 TR branch + P03 AS branch + P06). EditTraining POST punya 4 validator baru (P03 TR branch + P03 AS branch + self-renewal + P06) plus 2-line entity assignment. EditTraining GET populate RenewalSourceTitle via lookup TR/AS source. VM extended dengan RenewsTrainingId/RenewsSessionId/RenewalSourceTitle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md
@.planning/phases/326-validator-hardening-p03-p06/326-PATTERNS.md
@docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md
@CLAUDE.md
@Controllers/TrainingAdminController.cs
@Models/EditTrainingRecordViewModel.cs
@Models/CreateTrainingRecordViewModel.cs
@Models/TrainingRecord.cs
@Models/AssessmentSession.cs

<interfaces>
<!-- Key types untuk executor. Extract dari codebase. -->

EditTrainingRecordViewModel current shape (Models/EditTrainingRecordViewModel.cs:6-66):
```csharp
public int Id { get; set; }
public string WorkerId { get; set; } = "";
public string WorkerName { get; set; } = "";
public string Judul { get; set; } = "";
public string Penyelenggara { get; set; } = "";
public string? Kota { get; set; }
public string Kategori { get; set; } = "";
public string? SubKategori { get; set; }
public DateTime Tanggal { get; set; } = DateTime.Today;
public DateTime? TanggalMulai { get; set; }
public DateTime? TanggalSelesai { get; set; }
public string Status { get; set; } = "Passed";
public string? NomorSertifikat { get; set; }
public DateTime? ValidUntil { get; set; }
public string? CertificateType { get; set; }
public IFormFile? CertificateFile { get; set; }
public string? ExistingSertifikatUrl { get; set; }
```

CreateTrainingRecordViewModel renewal FK shape (Models/CreateTrainingRecordViewModel.cs:59-60):
```csharp
public int? RenewsTrainingId { get; set; }
public int? RenewsSessionId { get; set; }
```

AssessmentSession date field (Models/AssessmentSession.cs:18) — D-08 resolved per PATTERNS.md:
```csharp
public DateTime Schedule { get; set; }  // non-null, semantik = jadwal/issued date
```

TrainingRecord.Tanggal field (Models/TrainingRecord.cs):
```csharp
public DateTime Tanggal { get; set; }  // non-null, semantik = tanggal sertifikat issued
```

AddTraining POST current renewal validator pattern (Controllers/TrainingAdminController.cs:241-254):
```csharp
if (model.RenewsTrainingId.HasValue)
{
    var srcAlreadyRenewed = await _context.TrainingRecords.AnyAsync(t => t.RenewsTrainingId == model.RenewsTrainingId)
        || await _context.AssessmentSessions.AnyAsync(a => a.RenewsTrainingId == model.RenewsTrainingId && a.IsPassed == true);
    if (srcAlreadyRenewed)
        ModelState.AddModelError("", "Sertifikat ini sudah di-renew sebelumnya.");
}
if (model.RenewsSessionId.HasValue) { /* symmetric branch */ }
```

EditTraining POST current ModelState compress pattern (Controllers/TrainingAdminController.cs:453-461):
```csharp
if (!ModelState.IsValid)
{
    var firstError = ModelState.Values.SelectMany(v => v.Errors).Select(e => e.ErrorMessage).FirstOrDefault() ?? "Data tidak valid.";
    TempData["Error"] = firstError;
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EditTrainingRecordViewModel dengan 3 field renewal passthrough (D-06)</name>
  <files>Models/EditTrainingRecordViewModel.cs</files>
  <read_first>
    - Models/EditTrainingRecordViewModel.cs (full file ~66 baris)
    - Models/CreateTrainingRecordViewModel.cs:55-65 (reference field RenewsTrainingId/RenewsSessionId pattern)
    - .planning/phases/326-validator-hardening-p03-p06/326-PATTERNS.md section "2. Models/EditTrainingRecordViewModel.cs"
    - .planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md D-06
  </read_first>
  <action>
Tambah 3 field nullable di Models/EditTrainingRecordViewModel.cs SEBELUM closing brace `}` namespace (saat ini L65), SETELAH field `ExistingSertifikatUrl` (L64).

Insertion verbatim (per D-06 + PATTERNS Pattern 2):
```csharp
        // Phase 326 D-06: Renewal FK passthrough + display title
        public int? RenewsTrainingId { get; set; }
        public int? RenewsSessionId { get; set; }
        public string? RenewalSourceTitle { get; set; }
```

Rules:
- JANGAN tambah `[Required]` ke field manapun — semua nullable.
- JANGAN tambah `[Display(Name=...)]` — hidden input passthrough only, label sudah di Razor view (Plan 02).
- JANGAN modify field existing.
- Backward compatible: form POST existing yang tidak include 3 field baru tetap bind null OK.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | grep -E "(error|Error)" | grep -v "warning"</automated>
  </verify>
  <acceptance_criteria>
    - File Models/EditTrainingRecordViewModel.cs grep `public int\? RenewsTrainingId` returns 1 hit
    - File grep `public int\? RenewsSessionId` returns 1 hit
    - File grep `public string\? RenewalSourceTitle` returns 1 hit
    - `dotnet build` returns "Build succeeded" dengan 0 Error (warning OK)
    - Field existing (Id, WorkerId, ..., ExistingSertifikatUrl) unchanged — grep `public string\? ExistingSertifikatUrl` masih 1 hit
  </acceptance_criteria>
  <done>VM punya 3 field baru nullable. Build sukses 0 Error.</done>
</task>

<task type="auto">
  <name>Task 2: Tambah P03 DAG + P06 validator di AddTraining POST handler (D-01, D-02, D-03, D-05, D-08, D-10)</name>
  <files>Controllers/TrainingAdminController.cs</files>
  <read_first>
    - Controllers/TrainingAdminController.cs:230-290 (existing renewal validator pattern + ModelState.IsValid gate)
    - .planning/phases/326-validator-hardening-p03-p06/326-PATTERNS.md section "1. Pattern A + Pattern B"
    - .planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md D-01, D-02, D-03, D-05, D-08, D-10
    - docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md §6.1 + §6.2
  </read_first>
  <action>
Buka Controllers/TrainingAdminController.cs. Cari blok `if (model.RenewsSessionId.HasValue)` yang berakhir di L254 dengan closing `}`. SETELAH closing `}` L254 dan SEBELUM komentar `// Mixed-type bulk validation (per D-11, EDGE-01)` di L255, INSERT block validator P03 + P06 verbatim:

```csharp
            // P03 (Phase 326 D-01/D-08/D-10): DAG enforcement — tanggal renewal harus > tanggal source (monotonic constraint, strict reject same-day)
            if (model.RenewsTrainingId.HasValue)
            {
                var src = await _context.TrainingRecords.FindAsync(model.RenewsTrainingId.Value);
                if (src != null && src.Tanggal >= model.Tanggal)
                    ModelState.AddModelError("", "Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew.");
            }
            if (model.RenewsSessionId.HasValue)
            {
                var srcAs = await _context.AssessmentSessions.FindAsync(model.RenewsSessionId.Value);
                if (srcAs != null && srcAs.Schedule >= model.Tanggal)
                    ModelState.AddModelError("", "Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew.");
            }
            // P06 (Phase 326 D-02/D-03/D-05): Permanent + ValidUntil mutual exclusion (field-level error key=ValidUntil)
            if (model.CertificateType == "Permanent" && model.ValidUntil != null)
                ModelState.AddModelError("ValidUntil", "Sertifikat Permanent tidak boleh punya tanggal expired.");
```

Rules verbatim (jangan ubah byte):
- String error P03: `"Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew."` (key `""` summary-level per D-05)
- String error P06: `"Sertifikat Permanent tidak boleh punya tanggal expired."` (key `"ValidUntil"` field-level per D-05)
- Operator `>=` (strict reject same-day per D-10)
- AS branch pakai `srcAs.Schedule` (D-08 confirmed via PATTERNS.md — Schedule non-null DateTime L18 AssessmentSession.cs)
- `FindAsync(id.Value)` (bukan `FirstOrDefaultAsync`) untuk PK lookup (cached + faster)
- Null-guard `if (src != null)` + `if (srcAs != null)` mandatory (defense kalau FK invalid form tampering)

JANGAN modify:
- L235-254 existing validators (FK mutual exclusion + srcAlreadyRenewed)
- L255+ Mixed-type bulk validation
- L264 `if (!ModelState.IsValid)` gate

Insertion HARUS sebelum `if (!ModelState.IsValid)` di L264.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | grep -E "(error|Error)" | grep -v "warning"</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/TrainingAdminController.cs grep `"Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew."` returns ≥ 1 hit (1 hit di AddTraining; 2 hit setelah Plan 01 Task 3 Edit handler ditambah)
    - File grep `"Sertifikat Permanent tidak boleh punya tanggal expired."` returns ≥ 1 hit (1 di Add, akan jadi 2 setelah Task 3)
    - File grep `src.Tanggal >= model.Tanggal` returns ≥ 1 hit
    - File grep `srcAs.Schedule >= model.Tanggal` returns ≥ 1 hit
    - File grep `ModelState.AddModelError\("ValidUntil"` returns ≥ 1 hit
    - `dotnet build` returns 0 Error
    - L254 closing `}` srcAlreadyRenewed block + L264 `if (!ModelState.IsValid)` masih ada — execution flow tidak rusak
  </acceptance_criteria>
  <done>AddTraining POST punya 3 validator baru (P03 TR + P03 AS + P06). Build sukses. ModelState error string verbatim per D-03.</done>
</task>

<task type="auto">
  <name>Task 3: Tambah validator + entity mapping + RenewalSourceTitle populate di EditTraining GET + POST (D-06, D-07, D-08)</name>
  <files>Controllers/TrainingAdminController.cs</files>
  <read_first>
    - Controllers/TrainingAdminController.cs:405-501 (EditTraining GET L405-436 + EditTraining POST L438-501)
    - .planning/phases/326-validator-hardening-p03-p06/326-PATTERNS.md section "1. Pattern C + Pattern D" (CRITICAL: includes Edit POST mapping gap warning L217-221)
    - .planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md D-06, D-07, D-08
    - docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md §6.1 Note Edit case
  </read_first>
  <action>
Tiga modifikasi di Controllers/TrainingAdminController.cs (EditTraining GET + POST):

**Modifikasi 1 — EditTraining GET (L415-435): tambah VM mapping + populate RenewalSourceTitle.**

Cari blok VM construction `var model = new EditTrainingRecordViewModel { ... };` (L415-433). DI DALAM object initializer, SEBELUM closing `}` di L433 (setelah baris `ExistingSertifikatUrl = record.SertifikatUrl,`), tambah 2 line mapping FK:

```csharp
                ExistingSertifikatUrl = record.SertifikatUrl,
                RenewsTrainingId = record.RenewsTrainingId,
                RenewsSessionId = record.RenewsSessionId,
            };
```

SETELAH closing `};` (saat ini L433) dan SEBELUM `await SetTrainingCategoryViewBag();` (saat ini L434), tambah block lookup RenewalSourceTitle:

```csharp
            // Phase 326 D-07: Populate RenewalSourceTitle untuk display read-only di EditTraining view
            if (model.RenewsTrainingId != null)
            {
                var src = await _context.TrainingRecords.FirstOrDefaultAsync(t => t.Id == model.RenewsTrainingId);
                model.RenewalSourceTitle = src?.Judul ?? "(sertifikat sumber tidak ditemukan)";
            }
            else if (model.RenewsSessionId != null)
            {
                var srcAs = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == model.RenewsSessionId);
                model.RenewalSourceTitle = srcAs?.Title ?? "(sertifikat sumber tidak ditemukan)";
            }
```

Rules:
- TR pakai `.Judul` (TrainingRecord field name); AS pakai `.Title` (AssessmentSession field name L13) — JANGAN tertukar (verified PATTERNS L168).
- Fallback string `"(sertifikat sumber tidak ditemukan)"` verbatim per UI-SPEC L180 + PATTERNS L169.
- TIDAK pakai `.Include(.User)` (tidak perlu display user — title saja sufficient per UI-SPEC).

**Modifikasi 2 — EditTraining POST (sebelum L453 `if (!ModelState.IsValid)`): tambah 4 validator P03 + self-renewal + P06.**

Setelah blok file validation L446-451 dan SEBELUM `if (!ModelState.IsValid)` L453, INSERT block:

```csharp
            // P03 (Phase 326 D-01/D-08/D-10): DAG enforcement — mirror AddTraining
            if (model.RenewsTrainingId.HasValue)
            {
                var src = await _context.TrainingRecords.FindAsync(model.RenewsTrainingId.Value);
                if (src != null && src.Tanggal >= model.Tanggal)
                    ModelState.AddModelError("", "Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew.");
            }
            if (model.RenewsSessionId.HasValue)
            {
                var srcAs = await _context.AssessmentSessions.FindAsync(model.RenewsSessionId.Value);
                if (srcAs != null && srcAs.Schedule >= model.Tanggal)
                    ModelState.AddModelError("", "Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew.");
            }
            // P03 Self-renewal guard (Phase 326 D-07 defense kalau form tampering set RenewsTrainingId=model.Id)
            if (model.RenewsTrainingId.HasValue && model.RenewsTrainingId.Value == model.Id)
                ModelState.AddModelError("", "Sertifikat tidak boleh renewal dirinya sendiri.");
            // P06 (Phase 326 D-02/D-03): Permanent + ValidUntil mutual exclusion
            if (model.CertificateType == "Permanent" && model.ValidUntil != null)
                ModelState.AddModelError("ValidUntil", "Sertifikat Permanent tidak boleh punya tanggal expired.");
```

Rules:
- Self-renewal pakai key `""` summary-level (per D-05 + spec line 217 — note: spec line 217 sebut `"RenewsTrainingId"` key tapi PATTERNS D-07 + CONTEXT D-05 confirm key `""` untuk konsistensi summary display; pakai `""`).
- String error verbatim per D-03 (3 string locked).
- AS branch tidak punya self-renewal (TR vs AS beda tabel, FK cross-table cannot point ke diri sendiri).

**Modifikasi 3 — EditTraining POST (L479-490 entity assignment block): tambah 2 line mapping FK renewal.**

PATTERNS.md "Critical findings #3 + Pattern D L217-221" — existing L479-490 BELUM include `RenewsTrainingId`/`RenewsSessionId`. Tanpa ini, "Hapus link renewal" button (Plan 02) silent ignore.

Cari blok mapping VM → entity yang berakhir dengan `record.CertificateType = model.CertificateType;` (L490). SETELAH baris ini, SEBELUM `await _context.SaveChangesAsync();` (L492), tambah:

```csharp
            record.CertificateType = model.CertificateType;
            // Phase 326 D-07: Persist renewal FK clear/passthrough (else "Hapus link renewal" button silent ignore)
            record.RenewsTrainingId = model.RenewsTrainingId;
            record.RenewsSessionId = model.RenewsSessionId;
```

Rules:
- Tepat 2 baris assignment.
- Backward compatible: kalau model.RenewsTrainingId == null (button clicked) → record.RenewsTrainingId = null (FK cleared di DB).
- Kalau model.RenewsTrainingId == existing value (button NOT clicked) → record.RenewsTrainingId = existing (no-op).

JANGAN modify:
- File upload handling L466-477
- ManageAssessment redirect L500
- AuditLog L494-497
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | grep -E "(error|Error)" | grep -v "warning"</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/TrainingAdminController.cs grep `"Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew."` returns exactly 4 hits (2 di AddTraining dari Task 2 [TR+AS branch] + 2 di EditTraining [TR+AS branch])
    - File grep `"Sertifikat tidak boleh renewal dirinya sendiri."` returns 1 hit (Edit-only)
    - File grep `"Sertifikat Permanent tidak boleh punya tanggal expired."` returns exactly 2 hits (1 Add + 1 Edit)
    - File grep `"(sertifikat sumber tidak ditemukan)"` returns exactly 2 hits (TR branch + AS branch GET)
    - File grep `record.RenewsTrainingId = model.RenewsTrainingId` returns 1 hit (Edit POST entity mapping)
    - File grep `record.RenewsSessionId = model.RenewsSessionId` returns 1 hit
    - File grep `model.RenewsTrainingId.Value == model.Id` returns 1 hit (self-renewal check Edit-only)
    - File grep `RenewsTrainingId = record.RenewsTrainingId` returns 1 hit (GET VM mapping)
    - `dotnet build` 0 Error
  </acceptance_criteria>
  <done>EditTraining GET populate RenewalSourceTitle + GET VM mapping include RenewsTrainingId/RenewsSessionId. EditTraining POST punya 4 validator + entity mapping 2 line baru. Build sukses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form POST → AddTraining/EditTraining handler | Untrusted input crosses here — admin user but defense-in-depth tetap perlu (form tampering via Postman/curl) |
| Controller → EF Core _context (DB) | Trusted internal boundary; protected by ValidateAntiForgeryToken + Authorize roles Admin/HC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-01 | Tampering | AddTraining POST RenewsTrainingId | mitigate | DAG check `src.Tanggal >= model.Tanggal` reject monotonic violation. Cycle (A↔B) otomatis ditolak karena minimum 1 arah pasti melanggar constraint. |
| T-326-02 | Tampering | AddTraining/EditTraining POST RenewsSessionId | mitigate | Symmetric DAG check `srcAs.Schedule >= model.Tanggal` cover cycle AS→TR→AS yang lolos kalau hanya TR branch divalidasi (D-08). |
| T-326-03 | Tampering | EditTraining POST self-renewal | mitigate | Guard `model.RenewsTrainingId.Value == model.Id` reject — defense kalau form tampering set sertifikat as parent of self. |
| T-326-04 | Tampering | AddTraining/EditTraining POST Permanent+ValidUntil | mitigate | ModelState reject `CertificateType == "Permanent" && ValidUntil != null` — app-level mitigasi P09 DB CHECK constraint (deferred v20.0). |
| T-326-05 | Information Disclosure | RenewalSourceTitle lookup di EditTraining GET | accept | Lookup expose hanya `Judul` (TR) atau `Title` (AS) — low sensitivity, admin user only (Authorize Admin/HC). No PII leak. |
| T-326-06 | Race Condition (TOCTOU) | DAG check FindAsync vs SaveChangesAsync window | accept | Pre-existing pattern Phase 325 P05 — concurrent renewal modification window very narrow (~ms), low business risk; rollback possible via existing DbUpdateException catch. |
| T-326-07 | Repudiation | Edit clear renewal link no audit | accept | AuditLog "Update" existing L494-497 catch update event; specific field-level diff (RenewsTrainingId null→value) tidak di-record — acceptable per existing audit granularity convention (cf. Phase 321 EditLog limited scope). |
| T-326-08 | Denial of Service | AddTraining POST extra 2 FindAsync per request | accept | PK lookup `FindAsync` cached + low overhead; admin endpoint rate-limited via Authorize role gate. |
</threat_model>

<verification>
Manual verification deferred ke Plan 03 UAT. Plan 01 sufficient kalau:
1. `dotnet build` 0 Error setelah ketiga task selesai
2. Grep acceptance criteria pass per task
3. File modify hanya 2 file declared di frontmatter `files_modified`
4. String error 3 verbatim per D-03 match byte-for-byte
</verification>

<success_criteria>
- Models/EditTrainingRecordViewModel.cs extended dengan 3 field nullable (RenewsTrainingId, RenewsSessionId, RenewalSourceTitle)
- Controllers/TrainingAdminController.cs AddTraining POST punya P03 TR + P03 AS + P06 validator (3 new validators)
- Controllers/TrainingAdminController.cs EditTraining GET populate RenewalSourceTitle + VM mapping include 2 FK field
- Controllers/TrainingAdminController.cs EditTraining POST punya P03 TR + P03 AS + self-renewal + P06 validator (4 new validators) + entity mapping 2 line baru
- `dotnet build HcPortal.csproj` returns 0 Error
- No file outside `files_modified` frontmatter list disentuh
</success_criteria>

<output>
After completion, create `.planning/phases/326-validator-hardening-p03-p06/326-01-SUMMARY.md` dengan:
- Files modified (2)
- Validator added (Add: 3, Edit: 4)
- VM extension (3 field)
- Build status (0 Error)
- Next: Plan 02 view tweaks (Razor section + 2 span)
</output>
