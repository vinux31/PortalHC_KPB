---
phase: 327-timezone-dateonly-refactor-p04
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - Controllers/TrainingAdminController.cs
autonomous: false
requirements: [SC-5]
threats: [T-327-02]
must_haves:
  truths:
    - "ImportTraining handler L1037 (Assessment branch) + L1138 (Training branch) cast DateOnly.FromDateTime(vu)"
    - "Razor TagHelper smoke verify 1 Edit form — input value attribute render yyyy-MM-dd format"
    - "Kalau bug #47628 confirmed aktif (render d-M-yyyy), add [DisplayFormat] ke 4 VM via Shared Pattern F (Plan 03 SKIP, plan ini fallback fix)"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "ImportTraining cast Shared Pattern D di 2 site"
      contains: "DateOnly.FromDateTime(vu)"
  key_links:
    - from: "TrainingAdminController.cs ImportTraining L1037+L1138"
      to: "Excel cell DateTime.TryParse → DateOnly cast"
      via: "Shared Pattern D"
      pattern: "DateOnly\\.FromDateTime\\(vu\\)"
---

<objective>
Apply Shared Pattern D di ImportTraining handler (2 site D-13). Browser smoke verify TagHelper bug #47628 — kalau confirmed, retrofit Shared Pattern F (`[DisplayFormat]`) ke 4 input VM.

Purpose: Step 7 dari 11-step Implementation Order + smoke proactive risk T-327-02 sebelum migration apply Plan 07.
Output: 1-2 file touched, smoke result documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@Controllers/TrainingAdminController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Shared Pattern D ImportTraining cast (L1037 + L1138)</name>
  <files>Controllers/TrainingAdminController.cs</files>
  <read_first>
    - Controllers/TrainingAdminController.cs L991-1138 (ImportTraining handler, 2 branch)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-13)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 1 + "Code Examples"
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md Shared Pattern D + §10
  </read_first>
  <action>
    Di `Controllers/TrainingAdminController.cs` apply cast pattern di 2 site:

    **L1037 (Assessment branch):**
    ```csharp
    // SEBELUM:
    ValidUntil = DateTime.TryParse(validUntilStr, out var vu) ? vu : (DateTime?)null,

    // SESUDAH:
    ValidUntil = DateTime.TryParse(validUntilStr, out var vu) ? DateOnly.FromDateTime(vu) : (DateOnly?)null,
    ```

    **L1138 (Training branch):**
    ```csharp
    // SEBELUM:
    ValidUntil = DateTime.TryParse(validUntilStr, out var vu) ? vu : (DateTime?)null,

    // SESUDAH:
    ValidUntil = DateTime.TryParse(validUntilStr, out var vu) ? DateOnly.FromDateTime(vu) : (DateOnly?)null,
    ```

    Existing null/empty guard `string.IsNullOrWhiteSpace(...)` PRESERVE (TryParse return false untuk empty → null).

    Conventional commit: `refactor(327-06): ImportTraining cast DateOnly Shared Pattern D (L1037 + L1138)`.

    Post-edit: `dotnet build HcPortal.sln --nologo` confirm 0 Error.
  </action>
  <verify>
    <automated>grep -c "DateOnly.FromDateTime(vu)" Controllers/TrainingAdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DateOnly.FromDateTime(vu)" Controllers/TrainingAdminController.cs` returns 2
    - `grep -c "? vu : (DateTime?)null" Controllers/TrainingAdminController.cs` returns 0 (old pattern gone)
    - `grep -c "(DateOnly?)null" Controllers/TrainingAdminController.cs` returns 2
    - `dotnet build HcPortal.sln --nologo` exit 0, "0 Error"
    - `dotnet test HcPortal.sln --nologo` exit 0, ≥17 passed
  </acceptance_criteria>
  <done>
    ImportTraining Shared Pattern D applied 2 site. Build + test tetap GREEN.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (CHECKPOINT): Browser smoke Razor TagHelper #47628 bug verify + decide Shared Pattern F retrofit</name>
  <what-built>
    Build LOCAL development server (`dotnet run` http://localhost:5277). User akan login admin + navigate ke `/Admin/EditTraining/{id}` (any existing TrainingRecord ID dengan ValidUntil non-null) + inspect `<input asp-for="ValidUntil">` value attribute di browser DevTools.

    Risk T-327-02: bug dotnet/aspnetcore #47628 — TagHelper render DateOnly sebagai `d-M-yyyy` (locale) instead `yyyy-MM-dd` (HTML5 spec) → form Edit reject + data loss on POST.
  </what-built>
  <how-to-verify>
    1. Pre-step: pastikan local DB punya minimal 1 TrainingRecord dengan ValidUntil non-null (kalau belum, jalan `/Admin/AddTraining` dulu input dummy ValidUntil=2027-03-15 + commit ke local DB — TIDAK perlu seed temporary kalau sudah ada existing data).
    2. Jalan `dotnet run --project HcPortal.csproj` (background atau separate terminal).
    3. Login admin di `http://localhost:5277` (credential per memory `reference_dev_credentials.md` — admin@pertamina.com).
    4. Navigate ke `/Admin/ManageAssessment` tab Training → klik Edit row sertifikat dengan ValidUntil.
    5. Browser DevTools (F12) → Elements tab → find `<input ... id="ValidUntil" type="date">` → check `value` attribute:
       - **Expected (NO BUG):** `value="2027-03-15"` (yyyy-MM-dd HTML5 spec). User input field tampil populated dengan tanggal benar.
       - **Bug confirmed:** `value="15-3-2027"` atau `value="15-03-2027"` (d-M-yyyy locale). HTML5 date input mungkin tampak kosong / placeholder.
    6. Ulangi step 5 untuk `/Admin/AddTraining` form (input ValidUntil dummy → cek behavior).
    7. Ulangi untuk `/Admin/AddManualAssessment` + `/Admin/EditManualAssessment` (kalau ada existing ManualAssessment data).
    8. Capture screenshot/snippet inspect value attribute untuk dokumentasi.

    **Decision rule:**
    - **NO BUG** → resume signal: "no bug, skip pattern F". Plan ini SELESAI.
    - **BUG confirmed di ANY form** → resume signal: "bug confirmed, apply pattern F". Plan executor lanjut Task 3 (retrofit `[DisplayFormat]` ke 4 VM).
  </how-to-verify>
  <resume-signal>Type "no bug, skip pattern F" OR "bug confirmed, apply pattern F" + URL evidence/screenshot</resume-signal>
  <action>
    HUMAN CHECKPOINT — lihat `<what-built>` + `<how-to-verify>` di atas untuk konteks lengkap. Executor (Claude) HANYA pause + tampilkan checkpoint info ke user, tunggu resume signal sesuai `<resume-signal>`. Tidak ada action otomatis di task ini — semua manual user action.
  </action>
  <verify>
    <automated>echo "checkpoint:human-gated - awaiting user resume signal"</automated>
  </verify>
  <done>
    User reply via resume-signal. Executor parse signal + lanjut task berikutnya sesuai rule (kondisional kalau ada).
  </done>
</task>

<task type="auto">
  <name>Task 3 (CONDITIONAL — execute only if Task 2 resume = "bug confirmed"): Retrofit Shared Pattern F [DisplayFormat] ke 4 input VM</name>
  <files>Models/CreateTrainingRecordViewModel.cs, Models/EditTrainingRecordViewModel.cs, Models/CreateManualAssessmentViewModel.cs</files>
  <read_first>
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md Shared Pattern F
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 9 + Pitfall 5
    - Task 2 checkpoint outcome (bug confirmed)
  </read_first>
  <action>
    **HANYA EXECUTE kalau Task 2 resume = "bug confirmed, apply pattern F".** Skip kalau resume = "no bug".

    Add `[DisplayFormat]` annotation ke 4 ValidUntil property di 3 file (4 occurrence total):

    **Pattern (apply ke setiap):**
    ```csharp
    // SEBELUM:
    [DataType(DataType.Date)]
    [Display(Name = "Valid Until")]
    public DateOnly? ValidUntil { get; set; }

    // SESUDAH:
    [DisplayFormat(DataFormatString = "{0:yyyy-MM-dd}", ApplyFormatInEditMode = true)]
    [DataType(DataType.Date)]
    [Display(Name = "Valid Until")]
    public DateOnly? ValidUntil { get; set; }
    ```

    Apply ke:
    - `Models/CreateTrainingRecordViewModel.cs:51`
    - `Models/EditTrainingRecordViewModel.cs:53`
    - `Models/CreateManualAssessmentViewModel.cs:34` + `:97` (2 kelas dalam file)

    Conventional commit: `fix(327-06): retrofit [DisplayFormat] 4 VM (TagHelper #47628 workaround)`.

    Post-edit: re-test step 5 di Task 2 — value attribute harus `value="2027-03-15"` sekarang. Update Task 2 dokumentasi.
  </action>
  <verify>
    <automated>grep -rc "DisplayFormat(DataFormatString" Models/CreateTrainingRecordViewModel.cs Models/EditTrainingRecordViewModel.cs Models/CreateManualAssessmentViewModel.cs</automated>
  </verify>
  <acceptance_criteria>
    - **Skip kalau Task 2 = "no bug"** (acceptance: file `Models/*.cs` unchanged, grep `DisplayFormat` returns 0 di 3 file).
    - **Execute kalau Task 2 = "bug confirmed":**
      - `grep -c "DisplayFormat(DataFormatString = \"{0:yyyy-MM-dd}\"" Models/CreateTrainingRecordViewModel.cs` returns 1
      - `grep -c "DisplayFormat(DataFormatString = \"{0:yyyy-MM-dd}\"" Models/EditTrainingRecordViewModel.cs` returns 1
      - `grep -c "DisplayFormat(DataFormatString = \"{0:yyyy-MM-dd}\"" Models/CreateManualAssessmentViewModel.cs` returns 2
      - Re-test browser Edit form value attribute = `yyyy-MM-dd` post-retrofit
      - `dotnet build HcPortal.sln --nologo` exit 0
      - `dotnet test HcPortal.sln --nologo` ≥17 passed
  </acceptance_criteria>
  <done>
    Conditional task: kalau bug aktif, 4 VM punya [DisplayFormat] workaround → input value yyyy-MM-dd confirmed. Kalau no bug, skip clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImportTraining Excel cell parse ↔ Entity ValidUntil DateOnly? | String DateTime.TryParse → DateOnly cast — empty/null guard preserved |
| Razor TagHelper asp-for ↔ HTML5 input type=date | DateOnly? format format `yyyy-MM-dd` ISO required HTML5 spec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-02 | Tampering (TagHelper format bug) | 4 input VM Razor render | mitigate (smoke + conditional retrofit) | Task 2 checkpoint browser inspect. Task 3 retrofit [DisplayFormat] kalau confirmed. Skip kalau no bug. |
</threat_model>

<verification>
- TrainingAdminController.cs L1037 + L1138 cast `DateOnly.FromDateTime(vu)` confirmed via grep (2 occurrence).
- `dotnet build HcPortal.sln` 0 Error.
- `dotnet test HcPortal.sln` ≥17 passed.
- Checkpoint Task 2 resume signal documented dengan evidence (screenshot atau snippet inspect).
- Task 3 conditional execution: zero retrofit kalau no bug, 4 retrofit kalau bug aktif.
</verification>

<success_criteria>
SC-5 prep complete: ImportTraining cast applied + Razor TagHelper bug verified/mitigated. Plan 07 EF migration siap apply.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-06-SUMMARY.md`: ImportTraining 2 site cast + Task 2 checkpoint outcome (bug status) + Task 3 conditional execution status.
</output>
