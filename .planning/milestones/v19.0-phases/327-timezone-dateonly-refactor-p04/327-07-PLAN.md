---
phase: 327-timezone-dateonly-refactor-p04
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - Migrations/{ts}_ChangeValidUntilToDateOnly.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [SC-1, SC-2, SC-7]
threats: [T-327-01]
must_haves:
  truths:
    - "Pre-migration sqlcmd zero row ValidUntil punya jam non-zero (D-11)"
    - "DB lokal snapshot via sqlcmd BACKUP DATABASE SEBELUM apply (SEED_WORKFLOW)"
    - "Migration file ChangeValidUntilToDateOnly.cs generated dengan 2× AlterColumn<DateOnly> (NOT DropColumn+AddColumn — Pitfall 4)"
    - "dotnet ef database update applied sukses lokal"
    - "INFORMATION_SCHEMA TrainingRecords.ValidUntil + AssessmentSessions.ValidUntil DATA_TYPE = 'date' (verified sqlcmd)"
    - "SEED_JOURNAL.md entry classification 'permanent + prod-required' + status active"
    - "Rollback procedure siap (dotnet ef database update {prev-migration} test optional lokal)"
    - "JSON API grep audit endpoint return ValidUntil — flag risk Pitfall 3 di summary"
  artifacts:
    - path: "Migrations/{ts}_ChangeValidUntilToDateOnly.cs"
      provides: "EF Up/Down AlterColumn 2 tabel"
      contains: "AlterColumn<DateOnly>"
    - path: "Migrations/ApplicationDbContextModelSnapshot.cs"
      provides: "Auto-update DateOnly + date column type"
      contains: "Property<DateOnly?>"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Journal entry pre-migration snapshot + post-apply"
      contains: "Phase 327"
  key_links:
    - from: "dotnet ef migrations add ChangeValidUntilToDateOnly"
      to: "Migrations/ApplicationDbContextModelSnapshot.cs auto-update L510 + L1829"
      via: "EF generator sync"
      pattern: "Property<DateOnly\\?>"
---

<objective>
Generate EF migration `ChangeValidUntilToDateOnly` + apply lokal dengan pre-check guardrail (sqlcmd D-11) + DB snapshot SEED_WORKFLOW + post-apply verification. Risk T-327-01 HIGH (data loss kalau jam non-zero CAST truncate).

Purpose: Step 8 dari 11-step Implementation Order. WAJIB ada checkpoint user sebelum apply migration (irreversible-ish — rollback EF Down() lossy datetime2 ↔ date round-trip).
Output: 1 migration file generated + DB schema migrated lokal + journal entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/SEED_WORKFLOW.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@Migrations/ApplicationDbContextModelSnapshot.cs
@Migrations/20260402024553_FixInterviewResultsJsonColumnType.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-migration sqlcmd check (D-11) + DB snapshot BACKUP DATABASE</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - CLAUDE.md Seed Data Workflow section
    - docs/SEED_WORKFLOW.md (full BACKUP/RESTORE protokol)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-11 sqlcmd queries L90-93)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md "Code Examples" Pre-migration sqlcmd
    - docs/SEED_JOURNAL.md (existing entry format reference Phase 324)
  </read_first>
  <action>
    **Step 1 — Run pre-migration check** (D-11):
    ```bash
    sqlcmd -S {server} -d HcPortalDB_Dev -Q "SELECT COUNT(*) AS TR_NonMidnight FROM TrainingRecords WHERE ValidUntil IS NOT NULL AND CAST(ValidUntil AS TIME) <> '00:00:00';"
    sqlcmd -S {server} -d HcPortalDB_Dev -Q "SELECT COUNT(*) AS AS_NonMidnight FROM AssessmentSessions WHERE ValidUntil IS NOT NULL AND CAST(ValidUntil AS TIME) <> '00:00:00';"
    ```

    **Connection string detail:** ambil dari `appsettings.Development.json` atau `appsettings.json` (`ConnectionStrings:DefaultConnection`). DB lokal nama: confirm via grep `Database=` di config.

    **Expected:** Both counts = 0 (per RESEARCH Audit 8 — probability efektif nol karena POST handler pakai `<input type="date">` HTML5 midnight default).

    Kalau COUNT > 0:
    - STOP. Tampilkan list 10 row pertama yang punya jam non-zero: `SELECT TOP 10 Id, ValidUntil, CAST(ValidUntil AS TIME) FROM {table} WHERE ValidUntil IS NOT NULL AND CAST(ValidUntil AS TIME) <> '00:00:00';`
    - Eskalasi user — keputusan: (a) accept lossy (jam dibuang), atau (b) manual UPDATE round midnight dulu.

    **Step 2 — BACKUP DATABASE lokal** (SEED_WORKFLOW):
    ```bash
    sqlcmd -S {server} -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK = 'C:/temp/HcPortalDB_Dev_pre-327-migration_$(date +%Y%m%d_%H%M%S).bak' WITH FORMAT, INIT, NAME = 'HcPortalDB_Dev Pre-Phase327 Migration';"
    ```

    **Step 3 — Append entry ke docs/SEED_JOURNAL.md:**
    ```markdown
    ## 2026-05-28 — Phase 327 Pre-Migration Snapshot

    **Classification:** permanent + prod-required (schema change Phase 327)
    **Status:** active

    ### Purpose
    Snapshot DB lokal pre `ChangeValidUntilToDateOnly` migration apply. Rollback safety untuk T-327-01 HIGH risk (CAST datetime2 → date lossy kalau jam non-zero).

    ### Entitas Tersentuh
    - `TrainingRecords.ValidUntil` (datetime2 → date)
    - `AssessmentSessions.ValidUntil` (datetime2 → date)

    ### Pre-check Output (D-11)
    - TR_NonMidnight: {N}
    - AS_NonMidnight: {N}

    ### Backup File
    `C:/temp/HcPortalDB_Dev_pre-327-migration_{ts}.bak`

    ### Rollback Plan
    `dotnet ef database update {prev-migration-name}` then `sqlcmd RESTORE DATABASE` if needed.

    Migration applied: pending Task 2-3.
    ```

    Conventional commit: `docs(327-07): SEED_JOURNAL pre-migration snapshot entry + sqlcmd check`.
  </action>
  <verify>
    <automated>grep -c "Phase 327" docs/SEED_JOURNAL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 327" docs/SEED_JOURNAL.md` returns ≥1
    - `grep -c "permanent + prod-required" docs/SEED_JOURNAL.md` returns ≥1
    - Backup file exists: `ls C:/temp/HcPortalDB_Dev_pre-327-migration_*.bak` returns ≥1 file
    - Pre-check output captured di journal entry (TR_NonMidnight + AS_NonMidnight counts)
  </acceptance_criteria>
  <done>
    Pre-check sukses + DB snapshot ada + journal entry committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate EF migration ChangeValidUntilToDateOnly + REVIEW file content (Pitfall 4 guardrail)</name>
  <files>Migrations/{ts}_ChangeValidUntilToDateOnly.cs, Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <read_first>
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-02 + D-03)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Pattern 2 (expected AlterColumn output) + Pitfall 4
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §19 (analog EXACT FixInterviewResultsJsonColumnType.cs)
    - Migrations/20260402024553_FixInterviewResultsJsonColumnType.cs (analog file pattern)
  </read_first>
  <action>
    **Step 1 — Generate migration:**
    ```bash
    dotnet ef migrations add ChangeValidUntilToDateOnly --project HcPortal.csproj
    ```

    EF generator akan auto-update `ApplicationDbContextModelSnapshot.cs:510, 1829` (Property<DateTime?> → Property<DateOnly?> + HasColumnType "date").

    **Step 2 — WAJIB REVIEW file generated** `Migrations/{ts}_ChangeValidUntilToDateOnly.cs`:

    **EXPECTED Up() (Pattern 2 ready-to-paste):**
    ```csharp
    protected override void Up(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.AlterColumn<DateOnly>(
            name: "ValidUntil",
            table: "TrainingRecords",
            type: "date",
            nullable: true,
            oldClrType: typeof(DateTime),
            oldType: "datetime2",
            oldNullable: true);

        migrationBuilder.AlterColumn<DateOnly>(
            name: "ValidUntil",
            table: "AssessmentSessions",
            type: "date",
            nullable: true,
            oldClrType: typeof(DateTime),
            oldType: "datetime2",
            oldNullable: true);
    }
    ```

    **EXPECTED Down() (D-03 reverse):**
    ```csharp
    protected override void Down(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.AlterColumn<DateTime>(
            name: "ValidUntil",
            table: "TrainingRecords",
            type: "datetime2",
            nullable: true,
            oldClrType: typeof(DateOnly),
            oldType: "date",
            oldNullable: true);

        migrationBuilder.AlterColumn<DateTime>(
            name: "ValidUntil",
            table: "AssessmentSessions",
            type: "datetime2",
            nullable: true,
            oldClrType: typeof(DateOnly),
            oldType: "date",
            oldNullable: true);
    }
    ```

    **DEFECT DETECTION (Pitfall 4):** Kalau file generated ada `migrationBuilder.DropColumn(...)` + `migrationBuilder.AddColumn<DateOnly>(...)` instead of AlterColumn:
    - STOP commit.
    - Replace dengan manual SQL fallback:
      ```csharp
      protected override void Up(MigrationBuilder migrationBuilder)
      {
          migrationBuilder.Sql("ALTER TABLE TrainingRecords ALTER COLUMN ValidUntil date NULL;");
          migrationBuilder.Sql("ALTER TABLE AssessmentSessions ALTER COLUMN ValidUntil date NULL;");
      }
      protected override void Down(MigrationBuilder migrationBuilder)
      {
          migrationBuilder.Sql("ALTER TABLE TrainingRecords ALTER COLUMN ValidUntil datetime2 NULL;");
          migrationBuilder.Sql("ALTER TABLE AssessmentSessions ALTER COLUMN ValidUntil datetime2 NULL;");
      }
      ```

    Conventional commit: `feat(327-07): EF migration ChangeValidUntilToDateOnly (2× AlterColumn)`.
  </action>
  <verify>
    <automated>ls Migrations/*ChangeValidUntilToDateOnly.cs && grep -c "AlterColumn<DateOnly>" Migrations/*ChangeValidUntilToDateOnly.cs</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists: `ls Migrations/*ChangeValidUntilToDateOnly.cs` returns 1 file
    - `grep -c "AlterColumn<DateOnly>" Migrations/*ChangeValidUntilToDateOnly.cs` returns 2 (TR + AS) — ATAU fallback `grep -c "ALTER COLUMN ValidUntil date" Migrations/*ChangeValidUntilToDateOnly.cs` returns 2
    - `grep -c "DropColumn" Migrations/*ChangeValidUntilToDateOnly.cs` returns 0 (defect detection — Pitfall 4)
    - `grep -c "AlterColumn<DateTime>" Migrations/*ChangeValidUntilToDateOnly.cs` returns 2 (Down() reverse) — atau fallback `grep -c "ALTER COLUMN ValidUntil datetime2" Migrations/*ChangeValidUntilToDateOnly.cs` returns 2
    - `grep -c "Property<DateOnly?>" Migrations/ApplicationDbContextModelSnapshot.cs` returns 2 (auto-update L510 + L1829)
    - `dotnet build HcPortal.sln --nologo` exit 0
  </acceptance_criteria>
  <done>
    Migration file generated + reviewed + clean AlterColumn (no DropColumn). Snapshot auto-updated. Build sukses.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): User approve dotnet ef database update apply lokal</name>
  <what-built>
    Migration file `Migrations/{ts}_ChangeValidUntilToDateOnly.cs` siap apply ke DB lokal HcPortalDB_Dev. Pre-check sqlcmd zero (Task 1), snapshot BACKUP ada (Task 1), file content reviewed clean (Task 2).

    Action: `dotnet ef database update --project HcPortal.csproj`. Akan ALTER COLUMN ValidUntil di 2 tabel SQL Server lokal. Reversible via Down() (lossy round-trip jam = 00:00:00 acceptable per D-03).
  </what-built>
  <how-to-verify>
    1. Review terakhir migration file content (cat `Migrations/{ts}_ChangeValidUntilToDateOnly.cs`) — confirm AlterColumn 2× tanpa DropColumn.
    2. Confirm BACKUP file exists: `ls C:/temp/HcPortalDB_Dev_pre-327-migration_*.bak`.
    3. Approve apply OR cancel:
       - **Approve:** type "apply migration". Executor lanjut Task 4.
       - **Cancel:** type "cancel, revert". Executor revert via `dotnet ef migrations remove` + restore from backup kalau perlu.
  </how-to-verify>
  <resume-signal>Type "apply migration" OR "cancel, revert"</resume-signal>
  <action>
    HUMAN CHECKPOINT — lihat `<what-built>` + `<how-to-verify>` di atas untuk konteks lengkap. Executor (Claude) HANYA pause + tampilkan checkpoint info ke user, tunggu resume signal sesuai `<resume-signal>`. Tidak ada action otomatis di task ini — semua manual user action.
  </action>
  <verify>
    <automated>echo "checkpoint:human-gated - awaiting user resume signal"</automated>
  </verify>
  <done>
    User reply via resume-signal. Executor parse signal + lanjut task berikutnya sesuai rule (kondisional kalau ada).
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply EF migration + post-apply schema verify + JSON API audit + commit</name>
  <files>Migrations/{ts}_ChangeValidUntilToDateOnly.cs, docs/SEED_JOURNAL.md</files>
  <read_first>
    - Task 3 checkpoint resume (must be "apply migration")
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 3 (JSON API enumeration) + Pitfall 3
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-15)
  </read_first>
  <action>
    **HANYA EXECUTE kalau Task 3 resume = "apply migration".**

    **Step 1 — Apply migration lokal:**
    ```bash
    dotnet ef database update --project HcPortal.csproj
    ```

    Expected output: "Done." atau "Applying migration '{ts}_ChangeValidUntilToDateOnly'."

    **Step 2 — Post-apply schema verify (SC-1):**
    ```bash
    sqlcmd -S {server} -d HcPortalDB_Dev -Q "SELECT TABLE_NAME, COLUMN_NAME, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME IN ('TrainingRecords','AssessmentSessions') AND COLUMN_NAME='ValidUntil';"
    ```

    Expected output: 2 row, DATA_TYPE = `date` keduanya.

    **Step 3 — Run dotnet build + test (regression check):**
    ```bash
    dotnet build HcPortal.sln --nologo
    dotnet test HcPortal.sln --nologo
    ```

    Build 0 Error, test ≥17 passed.

    **Step 4 — JSON API audit (D-15):**
    ```bash
    grep -rn "return Json(" Controllers/ Services/ | grep -i "validuntil\|tanggalexpired"
    ```

    Capture sites yang return ValidUntil/TanggalExpired sebagai JSON. Per RESEARCH Audit 3, 2 endpoint risk: `GET /CMP/GetExpiringSoonData` + `GET /CMP/GetAnalyticsData`. Catat di journal entry untuk smoke Plan 08.

    **Step 5 — Append SEED_JOURNAL entry update:**
    ```markdown
    ### Migration Applied
    - Date: {ISO timestamp}
    - Status: applied
    - Post-check: TrainingRecords.ValidUntil DATA_TYPE = date ✓; AssessmentSessions.ValidUntil DATA_TYPE = date ✓
    - Build: 0 Error
    - Test: {N} passed
    - JSON API audit endpoints at-risk: /CMP/GetExpiringSoonData, /CMP/GetAnalyticsData (per RESEARCH Audit 3) — smoke Plan 08
    ```

    Conventional commit: `feat(327-07): apply ChangeValidUntilToDateOnly migration lokal + SC-1 verified`.

    **Rollback test (SC-7) OPTIONAL:** kalau user mau, jalan `dotnet ef migrations list` find prev migration name, lalu `dotnet ef database update {prev}` test reverse, lalu re-forward. Demonstrate Down() works. Document outcome di journal. SKIP kalau user tidak request explicit.
  </action>
  <verify>
    <automated>sqlcmd -Q "SELECT DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='TrainingRecords' AND COLUMN_NAME='ValidUntil';" -h-1</automated>
  </verify>
  <acceptance_criteria>
    - `sqlcmd -Q "SELECT DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='TrainingRecords' AND COLUMN_NAME='ValidUntil';"` output contains `date` (NOT `datetime2`)
    - `sqlcmd -Q "SELECT DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='AssessmentSessions' AND COLUMN_NAME='ValidUntil';"` output contains `date`
    - `dotnet build HcPortal.sln --nologo` exit 0
    - `dotnet test HcPortal.sln --nologo` exit 0, ≥17 passed
    - docs/SEED_JOURNAL.md updated: `grep -c "Migration Applied" docs/SEED_JOURNAL.md` returns ≥1 (most recent Phase 327 block)
    - JSON API audit output captured di journal (2 endpoint risk noted)
  </acceptance_criteria>
  <done>
    SC-1 + SC-2 PASS lokal. Migration applied successfully. Schema verified `date` 2 tabel. JSON API risk endpoint catat untuk Plan 08 smoke. Plan 08 final UAT siap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF migration ↔ SQL Server schema (irreversible-ish) | AlterColumn type change `datetime2 → date` SQL Server CAST truncate jam komponen lossy |
| sqlcmd ↔ DB lokal HcPortalDB_Dev | Read pre-check + BACKUP write to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-01 | Tampering + DoS (data loss) | EF migration apply (CAST datetime2 → date truncate jam) | mitigate (multi-layer) | (1) Task 1 sqlcmd pre-check zero non-midnight row (expected zero per RESEARCH Audit 8). (2) Task 1 BACKUP DATABASE snapshot. (3) Task 2 review migration file (AlterColumn not DropColumn — Pitfall 4). (4) Task 3 human checkpoint before apply. (5) Down() migration reverse siap (D-03 lossy acceptable). |
</threat_model>

<verification>
- Pre-check sqlcmd output captured (TR_NonMidnight + AS_NonMidnight).
- Backup file exists di C:/temp/.
- Migration file content reviewed: 2× AlterColumn DateOnly Up() + 2× AlterColumn DateTime Down(), zero DropColumn.
- Post-apply: `sqlcmd INFORMATION_SCHEMA` confirm `date` 2 tabel (SC-1).
- Build + test green.
- SEED_JOURNAL entry: snapshot + migration applied status.
</verification>

<success_criteria>
SC-1 PASS (migration apply sukses 2 tabel datetime2 → date). SC-2 PASS (pre-check zero row jam non-zero). SC-7 procedure documented (Down() siap kalau drama). JSON API audit done — risk noted Plan 08 smoke.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-07-SUMMARY.md`: migration file name + timestamp + Up/Down content excerpt + sqlcmd pre+post check output + backup path + JSON API audit result + journal entry update.
</output>
