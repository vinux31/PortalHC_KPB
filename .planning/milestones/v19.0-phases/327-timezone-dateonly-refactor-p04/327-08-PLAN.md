---
phase: 327-timezone-dateonly-refactor-p04
plan: 08
type: execute
wave: 8
depends_on: [07]
files_modified:
  - .planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md
  - docs/IT_NOTIFY.md
autonomous: false
requirements: [SC-3, SC-4, SC-5, SC-6, SC-7]
threats: [T-327-01, T-327-02, T-327-03, T-327-05]
must_haves:
  truths:
    - "7 SC manual UAT lokal: SC-1 (schema verified Plan 07) + SC-2 (pre-check zero Plan 07) + SC-3 (xUnit 8/8 GREEN Plan 04) + SC-4 (Add training + display AkanExpired manual smoke Plan 08) + SC-5 (5 halaman wajib display tanpa jam Plan 08) + SC-6 (PDF CertificatePdf render benar Plan 08) + SC-7 (rollback procedure documented Plan 07)"
    - "JSON consumer JS timezone smoke (Pitfall 3): /CMP/AnalyticsDashboard badge sisa hari off-by-one absence"
    - "Phase 326 P03/P06 validator regression smoke (T-327-05): 6 SC Phase 326 manual repro tetap PASS"
    - "327-UAT.md captured per SC pass/fail + screenshot/snippet"
    - "IT_NOTIFY.md draft siap: 3 phase batch (325+326+327) commit hash + migration flag ChangeValidUntilToDateOnly + sqlcmd pre-check + Up/Down command"
    - "Push approval gate explicit user — DO NOT push origin/main tanpa user say 'push approved'"
  artifacts:
    - path: ".planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md"
      provides: "Manual UAT 7 SC + JSON timezone smoke + Phase 326 regression smoke result"
      contains: "SC-1"
    - path: "docs/IT_NOTIFY.md"
      provides: "IT runbook batch v19.0 (325+326+327) + migration apply command"
      contains: "ChangeValidUntilToDateOnly"
  key_links:
    - from: "Manual UAT 5 halaman wajib"
      to: "/Admin/ManageAssessment + /Admin/RenewalCertificate + /CMP/Records + /CDP/CertificationManagement + Worker dashboard + /CMP/CertificatePdf"
      via: "Browser navigate + visual verify tanggal tanpa jam"
      pattern: "tanggal render dd MMM yyyy tanpa 00:00:00"
---

<objective>
Final phase gate: manual UAT 7 SC + Pitfall 3 JSON timezone smoke + T-327-05 Phase 326 regression smoke + draft IT_NOTIFY.md siap pelaksanaan IT promo Dev/Prod batch akhir v19.0 (325+326+327 sekaligus per spec §11).

Purpose: Step 9-11 dari 11-step Implementation Order. Phase 327 SHIPPED LOCAL gate. Push origin/main BUTUH user explicit approval (per CLAUDE.md + memory pattern Phase 325/326).
Output: 2 dokumen (UAT result + IT runbook draft). NO push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@.planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md
@docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (CHECKPOINT): Manual UAT 7 SC — 5 halaman wajib + PDF + JSON timezone smoke + Phase 326 regression smoke</name>
  <what-built>
    Phase 327 code complete LOKAL: entity flip + computed props rewrite + VM flip + DeriveCertificateStatus refactor + Controller var today sweep + RenewalController DateOnly.MaxValue + GradingService cast + ImportTraining cast + EF migration `ChangeValidUntilToDateOnly` applied + xUnit 8/8 GREEN.

    Pending verification 4 SC manual + 2 smoke + 1 regression smoke.
  </what-built>
  <how-to-verify>
    **Pre-step:** Jalan `dotnet run --project HcPortal.csproj` (background). Login `http://localhost:5277` admin (per `reference_dev_credentials.md`).

    Capture hasil per checkpoint di file baru `.planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md` dengan format:
    ```markdown
    ## SC-X: {behavior}
    - Steps: {what done}
    - Result: PASS | FAIL
    - Evidence: {screenshot path atau snippet inspect}
    - Notes: {anomaly kalau ada}
    ```

    **SC-1 (already PASS Plan 07):** Schema verify `sqlcmd INFORMATION_SCHEMA` returns `date` 2 tabel.

    **SC-2 (already PASS Plan 07):** Pre-check COUNT = 0 both tables.

    **SC-3 (already PASS Plan 04):** `dotnet test --filter "FullyQualifiedName~CertificateStatusTests"` 8/8 GREEN. Re-run final regression.

    **SC-4 — Add training + display "AkanExpired":**
    1. Navigate `/Admin/AddTraining`.
    2. Input: NamaPekerja existing, NamaSertifikat="Test 327 SC-4", CertificateType="Annual", Tanggal=today, ValidUntil=today+1.
    3. POST submit → expect 200 redirect ManageAssessment.
    4. Navigate `/Admin/ManageAssessment` tab Training → cari row "Test 327 SC-4".
    5. Cek badge status: HARUS "AkanExpired" (days = 1, ≤ 30 inclusive).
    6. Record PASS/FAIL.

    **SC-5 — 5 halaman wajib display tanpa jam:**
    Navigate setiap route, visual verify tanggal render `dd MMM yyyy` atau `yyyy-MM-dd` TANPA suffix "00:00:00":
    1. `/Admin/ManageAssessment` tab Training column "Valid Until"
    2. `/Admin/RenewalCertificate` column "Tanggal Expired"
    3. `/CMP/Records` column "Valid Until" (login coachee atau impersonate)
    4. `/CDP/CertificationManagement` column "Valid Until"
    5. Worker dashboard (`/Home/Index` atau Worker route equivalent) badge sertifikat

    Capture screenshot setiap route.

    **SC-6 — PDF CertificatePdf:**
    1. Find AssessmentSession dengan NomorSertifikat non-null + ValidUntil non-null (query DB lokal `SELECT TOP 1 Id FROM AssessmentSessions WHERE NomorSertifikat IS NOT NULL AND ValidUntil IS NOT NULL;`).
    2. Navigate `/CMP/CertificatePdf/{id}` → browser download/open PDF.
    3. Visual cek text "Berlaku Hingga: {tanggal Indonesia}" e.g., "15 Maret 2027" — tidak ada "00:00" jam.
    4. Record PASS/FAIL.

    **SC-7 — Rollback procedure documented (already Plan 07):**
    Confirm `Migrations/{ts}_ChangeValidUntilToDateOnly.cs` Down() exists. Optional: jalan rollback test lokal kalau user mau prove (skip default).

    **PITFALL 3 SMOKE — JSON consumer JS timezone shift:**
    1. Navigate `/CMP/AnalyticsDashboard` (kalau ada route, atau equivalent yang consume `GET /CMP/GetExpiringSoonData`).
    2. Open DevTools Network tab → find request `GetExpiringSoonData`.
    3. Response payload format: `tanggalExpired: "2027-03-15"` (NOT `"2027-03-15T00:00:00"`).
    4. UI render badge "sisa hari" — kalau ada row ValidUntil=today+1, badge harus "1 hari" (NOT "0 hari" atau "2 hari").
    5. Kalau shift confirmed → flag risk di summary, fix di `wwwroot/js/analyticsDashboard.js:852-853` pakai split parse (per RESEARCH Pitfall 3) — sebagai follow-up sub-task atau defer kalau non-critical.

    **PHASE 326 REGRESSION SMOKE (T-327-05) — 6 SC repro:**
    1. Add Training P03 cycle test: pick existing TrainingRecord A → AddTraining `RenewsTrainingId=A` + Tanggal=A.Tanggal-1 → submit. Expect ModelState error "Tanggal renewal harus lebih besar...".
    2. Add Training P06: CertificateType="Permanent" + ValidUntil=2027-03-15 → submit. Expect ModelState error "Permanent tidak boleh punya ValidUntil...".
    3. Edit Training P03 cycle: similar repro Edit handler.
    4. Edit Training P06: similar repro Edit handler.
    5. Edit Training self-renewal (sourceId=editing record itself sendiri): expect ModelState error self-renewal blocked.
    6. Edit form renewal source display read-only + clear button (per Phase 326 plan 02): visual verify.

    Catat hasil tiap SC Phase 326 di 327-UAT.md section "Phase 326 Regression Smoke".

    **Resume signal options:**
    - **"all pass"**: Plan executor commit 327-UAT.md + lanjut Task 2 (IT_NOTIFY draft).
    - **"fail SC-X"**: Stop, fix-forward atau revert per case. Tidak boleh lanjut Task 2 sampai diselesaikan.
  </how-to-verify>
  <resume-signal>Type "all pass" OR "fail SC-X" + evidence path</resume-signal>
  <action>
    HUMAN CHECKPOINT — lihat `<what-built>` + `<how-to-verify>` di atas untuk konteks lengkap. Executor (Claude) HANYA pause + tampilkan checkpoint info ke user, tunggu resume signal sesuai `<resume-signal>`. Tidak ada action otomatis di task ini — semua manual user action.
  </action>
  <verify>
    <automated>echo "checkpoint:human-gated - awaiting user resume signal"</automated>
  </verify>
  <done>
    User reply via resume-signal. Executor parse signal + lanjut task berikutnya sesuai rule (kondisional kalau ada).
  </done>
</task>

<task type="auto">
  <name>Task 2: Draft 327-UAT.md commit + draft docs/IT_NOTIFY.md (3 phase batch promo runbook)</name>
  <files>.planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md, docs/IT_NOTIFY.md</files>
  <read_first>
    - Task 1 checkpoint outcome (must be "all pass" untuk proceed)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-11 sqlcmd inline IT_NOTIFY)
    - docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md §11 (IT promo batch strategy)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md "Code Examples" Pre-migration sqlcmd
    - Memory project_323_shipped.md + project_325_code_complete_uat_partial.md + project_326_shipped.md (analog IT_NOTIFY patterns existing)
  </read_first>
  <action>
    **HANYA EXECUTE kalau Task 1 = "all pass".**

    **Step 1 — Finalize `.planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md`** dengan hasil UAT (capture dari Task 1):
    ```markdown
    # Phase 327 — Manual UAT Result

    **UAT date:** {ISO date}
    **Lokal env:** http://localhost:5277, DB HcPortalDB_Dev (post-migration ChangeValidUntilToDateOnly)

    ## SC Pass Summary

    | SC | Status | Plan Source | Evidence |
    |----|--------|-------------|----------|
    | SC-1 | PASS | 07 | sqlcmd INFORMATION_SCHEMA returns 'date' 2 tabel |
    | SC-2 | PASS | 07 | Pre-check COUNT = 0 (TR + AS) |
    | SC-3 | PASS | 04 | xUnit 8/8 GREEN |
    | SC-4 | PASS | 08 | {screenshot} |
    | SC-5 | PASS | 08 | 5 halaman {screenshots} |
    | SC-6 | PASS | 08 | PDF {screenshot} |
    | SC-7 | DOCUMENTED | 07 | Down() exists + rollback procedure |

    ## Pitfall 3 — JSON Timezone Smoke
    Result: {PASS | SHIFT_CONFIRMED}

    ## Phase 326 Regression Smoke (T-327-05)
    | Phase 326 SC | Status |
    |--------------|--------|
    | P03 Add cycle | {PASS|FAIL} |
    | P06 Add Permanent+ValidUntil | {PASS|FAIL} |
    | P03 Edit cycle | {PASS|FAIL} |
    | P06 Edit Permanent+ValidUntil | {PASS|FAIL} |
    | Edit self-renewal block | {PASS|FAIL} |
    | Edit renewal display + clear | {PASS|FAIL} |
    ```

    Conventional commit: `docs(327-08): UAT result 7 SC + Phase 326 regression smoke PASS`.

    **Step 2 — Draft `docs/IT_NOTIFY.md`:**

    File baru pattern dari Phase 323/324/325/326 IT_NOTIFY (per memory). Sections wajib:

    ```markdown
    # IT Notify — v19.0 Batch Promo Dev/Prod

    **Tanggal:** {ISO date}
    **Milestone:** v19.0 (4 phase batch sequential)
    **Status batch:** 325 + 326 + 327 SHIPPED LOCAL — siap promo Dev (Prod menyusul post-Dev verify)

    ## TL;DR untuk IT
    - **Jumlah commit batch:** {N} commit total dari `{hash-325-start}` hingga `{hash-327-end}` di branch main.
    - **Migration flag:** ✅ ADA — `ChangeValidUntilToDateOnly` (Phase 327). 2 tabel ALTER COLUMN datetime2 → date.
    - **Schema change:** ✅ YA (Phase 327 saja). 325 + 326 zero schema change.
    - **Yang IT lakukan:** deploy code + run `dotnet ef database update` di Dev, kemudian Prod.
    - **Yang TIDAK perlu IT lakukan:** apa-apa selain di atas (no manual SQL, no config edit).

    ## Commit Hash Range
    - Phase 325 (Security Hardening P01+P02+P05): `7069ead2..77a9c375`
    - Phase 326 (Validator Hardening P03+P06): `718c67b8..f659ff91`
    - Phase 327 (Timezone DateOnly Refactor P04): `{hash-327-start}..{hash-327-end}` (akan diisi post-push)

    ## Pre-Migration Check (Phase 327)

    **WAJIB run sqlcmd berikut SEBELUM `dotnet ef database update`** untuk confirm zero data loss:

    ```sql
    -- Konfirmasi zero row ValidUntil punya komponen jam non-zero
    SELECT COUNT(*) AS TR_NonMidnight FROM TrainingRecords
    WHERE ValidUntil IS NOT NULL AND CAST(ValidUntil AS TIME) <> '00:00:00';

    SELECT COUNT(*) AS AS_NonMidnight FROM AssessmentSessions
    WHERE ValidUntil IS NOT NULL AND CAST(ValidUntil AS TIME) <> '00:00:00';
    ```

    **Expected:** Both counts = 0. Kalau > 0, STOP — eskalasi developer (kontak: rino).

    ## Migration Apply Command

    ```bash
    cd {server-deploy-path}
    dotnet ef database update --project HcPortal.csproj
    ```

    Expected output: "Applying migration '{ts}_ChangeValidUntilToDateOnly'." → "Done."

    ## Post-Apply Verification

    ```sql
    SELECT TABLE_NAME, COLUMN_NAME, DATA_TYPE
    FROM INFORMATION_SCHEMA.COLUMNS
    WHERE TABLE_NAME IN ('TrainingRecords','AssessmentSessions') AND COLUMN_NAME='ValidUntil';
    ```

    Expected: 2 row, DATA_TYPE = `date` keduanya.

    ## Rollback Plan (kalau drama)

    ```bash
    dotnet ef database update {prev-migration-name} --project HcPortal.csproj
    ```

    Down() reverse: `date → datetime2` (lossy: jam komponen = 00:00:00, acceptable per design D-03).

    Kalau Down() gagal: restore from backup `BACKUP DATABASE` snapshot (lokasi tergantung server policy IT).

    ## Order of Operations (CRITICAL)

    1. Deploy code dulu (3 phase batch sudah merged di main).
    2. Pre-check sqlcmd (Step "Pre-Migration Check").
    3. BACKUP DATABASE (per IT policy).
    4. Run `dotnet ef database update`.
    5. Post-apply verification (Step "Post-Apply Verification").
    6. Smoke 5 halaman wajib (per developer notes — sama checklist Phase 327 UAT).

    ## Contact
    Developer: rino (asistensme@gmail.com)
    ```

    Conventional commit: `docs(327-08): IT_NOTIFY v19.0 batch promo runbook (3 phase + migration)`.

    **Step 3 — Update STATE.md last_activity Phase 327 SHIPPED LOCAL:**
    Edit `.planning/STATE.md` last_activity + current position fields per pattern Phase 325/326 (memory references). Conventional commit: `docs(state): record Phase 327 SHIPPED LOCAL`.

    **Step 4 — NO PUSH.** Final user gate.
  </action>
  <verify>
    <automated>ls .planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md docs/IT_NOTIFY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md` exists
    - `grep -c "SC-1" .planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md` returns ≥1
    - `grep -c "PASS" .planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md` returns ≥7 (SC-1..7 + Pitfall + regression)
    - `grep -c "Phase 326 Regression" .planning/phases/327-timezone-dateonly-refactor-p04/327-UAT.md` returns ≥1
    - `docs/IT_NOTIFY.md` exists
    - `grep -c "ChangeValidUntilToDateOnly" docs/IT_NOTIFY.md` returns ≥1
    - `grep -c "TR_NonMidnight" docs/IT_NOTIFY.md` returns ≥1
    - `grep -c "INFORMATION_SCHEMA" docs/IT_NOTIFY.md` returns ≥1
    - `grep -c "dotnet ef database update" docs/IT_NOTIFY.md` returns ≥1
    - `grep -c "Yang TIDAK perlu IT lakukan" docs/IT_NOTIFY.md` returns ≥1 (parity Phase 324 pattern)
    - .planning/STATE.md updated dengan "Phase 327 SHIPPED LOCAL"
  </acceptance_criteria>
  <done>
    UAT result documented, IT runbook draft, STATE update committed LOKAL. NO push origin/main.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3 (CHECKPOINT): User decide push origin/main batch v19.0 (325+326+327)?</name>
  <decision>
    Phase 327 SHIPPED LOCAL: code + migration + xUnit + UAT 7 SC PASS + IT_NOTIFY draft. Push origin/main batch v19.0 sekarang?
  </decision>
  <context>
    Per memory pattern (Phase 325 + 326 NOT PUSHED defer batch akhir per spec §11):
    - Phase 325 SHIPPED LOCAL 7069ead2..77a9c375 — NOT pushed
    - Phase 326 SHIPPED LOCAL 718c67b8..f659ff91 — NOT pushed
    - Phase 327 SHIPPED LOCAL (just done) — NOT pushed

    Spec §11 strategi batch: push 3 phase sekaligus → IT 1× notify + deploy + migration. Ini moment-nya.

    Risk push:
    - Migration apply di Dev butuh IT coordination — IT_NOTIFY draft siap deliver via email/Teams.
    - Dev server butuh dotnet ef CLI installed (per memory umumnya sudah).
    - Pre-check sqlcmd di Dev sebelum apply (CRITICAL per T-327-01).
  </context>
  <options>
    <option id="option-a">
      <name>Push batch v19.0 sekarang (325+326+327)</name>
      <pros>
        - IT 1× notify + deploy (efficiency)
        - Dev cycle continuous (no stale local-only batch)
        - Migration deploy gating selesai
      </pros>
      <cons>
        - Butuh IT immediate availability handle migration apply
        - Pre-check sqlcmd di Dev wajib (extra step IT)
      </cons>
    </option>
    <option id="option-b">
      <name>Hold push, wait IT availability confirm</name>
      <pros>
        - IT bisa schedule apply ke window low-traffic
        - Coordinated rollout
      </pros>
      <cons>
        - Local-only batch makin gemuk
        - Risk forget push detail
      </cons>
    </option>
    <option id="option-c">
      <name>Cancel push, defer Phase 328 audit done dulu</name>
      <pros>
        - 328 audit-only baca-saja (no migration) — kalau cepat selesai (1-2 jam) bisa batch v19.0 = 4 phase
      </pros>
      <cons>
        - 328 belum di-plan, butuh `/gsd-plan-phase 328` dulu
        - Spec §11 design batch 3 phase, 328 audit deliverable terpisah
      </cons>
    </option>
  </options>
  <resume-signal>Select: option-a (push now), option-b (hold for IT), option-c (defer for 328)</resume-signal>
  <action>
    HUMAN CHECKPOINT — lihat `<what-built>` + `<how-to-verify>` di atas untuk konteks lengkap. Executor (Claude) HANYA pause + tampilkan checkpoint info ke user, tunggu resume signal sesuai `<resume-signal>`. Tidak ada action otomatis di task ini — semua manual user action.
  </action>
  <verify>
    <automated>echo "checkpoint:human-gated - awaiting user resume signal"</automated>
  </verify>
  <done>
    User reply via resume-signal. Executor parse signal + lanjut task berikutnya sesuai rule (kondisional kalau ada).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOCAL ↔ origin/main | Push gate user-explicit (per CLAUDE.md + Phase 325/326 memory pattern) |
| Code + migration ↔ Dev server | IT promo via IT_NOTIFY runbook (sqlcmd pre-check + ef update + post-verify) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-01 | Tampering (Dev/Prod migration apply data loss) | EF migration apply di server Dev/Prod | mitigate (IT runbook) | IT_NOTIFY.md draft step-by-step: pre-check sqlcmd → BACKUP → ef update → post-verify. Down() reverse documented kalau drama. |
| T-327-02 | Tampering (Razor TagHelper bug) | 4 input VM Razor | accept (Plan 06 mitigated) | Already smoke + retrofit kalau perlu di Plan 06. Plan 08 UAT konfirm post-apply. |
| T-327-03 | Tampering (JSON consumer JS tz shift) | /CMP/AnalyticsDashboard badge | mitigate (smoke + flag) | Task 1 Pitfall 3 smoke wajib. Kalau shift confirmed, fix `wwwroot/js/analyticsDashboard.js` split parse — atau defer follow-up sub-task. |
| T-327-05 | Tampering (Phase 326 regression) | TrainingAdminController Add/Edit POST handler | mitigate (regression smoke) | Task 1 Phase 326 6 SC repro smoke wajib. Kalau ada FAIL, fix-forward sebelum batch push. |
</threat_model>

<verification>
- 7 SC manual UAT documented di 327-UAT.md (PASS evidence per SC).
- Pitfall 3 + Phase 326 regression smoke documented.
- IT_NOTIFY.md draft siap (sqlcmd pre-check + ef update + post-verify + rollback).
- STATE.md last_activity updated Phase 327 SHIPPED LOCAL.
- Push gate user-explicit (Task 3 checkpoint).
</verification>

<success_criteria>
**Phase 327 SHIPPED LOCAL gate complete.** SC-1..SC-7 all PASS or DOCUMENTED. Phase 326 regression PASS. IT runbook ready. Push decision user-driven.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-08-SUMMARY.md`: 7 SC outcome summary, smoke results (Pitfall 3 + Phase 326 regression), IT_NOTIFY draft commit hash, push decision outcome (option-a/b/c), final status.

Update STATE.md current_position dengan "Phase 327 (Timezone DateOnly Refactor P04) SHIPPED LOCAL — pending push approval / Phase 328 audit next".
</output>
