---
phase: 330-fix-cascade-med-bundle-delete-category-package-question-orgu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Controllers/OrganizationController.cs
  - Services/NotificationService.cs
  - docs/IT_NOTIFY.md
autonomous: true
requirements:
  - D-02-DeleteCategory
  - D-03-DeletePackage
  - D-04-DeleteQuestion
  - D-05-DeleteOrganizationUnit
  - D-06-NotificationService
must_haves:
  truths:
    - "DeleteCategory: FK violation menghasilkan TempData[Error] friendly, bukan raw HTTP 500"
    - "DeletePackage: SaveChangesAsync di-wrap try/catch DbUpdateException → TempData[Error] friendly pada FK violation"
    - "DeleteQuestion: SaveChangesAsync di-wrap try/catch DbUpdateException + _auditLog.LogAsync ditambah post-save"
    - "DeleteOrganizationUnit: try/catch DbUpdateException dual-path (JSON+TempData) + _auditLog.LogAsync ditambah post-save"
    - "NotificationService.DeleteAsync: catch (Exception) → catch (DbUpdateException) refactored"
    - "dotnet build clean, dotnet test 18/18 pass — tidak ada regresi dari Phase 329 baseline"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "DeleteCategory + DeletePackage + DeleteQuestion fixed"
      contains: "catch (DbUpdateException)"
    - path: "Controllers/OrganizationController.cs"
      provides: "DeleteOrganizationUnit fixed"
      contains: "catch (DbUpdateException)"
    - path: "Services/NotificationService.cs"
      provides: "DeleteAsync catch-type refactored"
      contains: "catch (DbUpdateException ex)"
    - path: "docs/IT_NOTIFY.md"
      provides: "Phase 330 IT notification entry"
      contains: "Phase 330"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs"
      to: "DeleteCategory L473-474"
      via: "try/catch DbUpdateException wrap"
      pattern: "catch \\(DbUpdateException\\)"
    - from: "Controllers/AssessmentAdminController.cs"
      to: "DeletePackage L5122"
      via: "try/catch DbUpdateException wrap around SaveChangesAsync"
      pattern: "Tidak bisa hapus paket"
    - from: "Controllers/AssessmentAdminController.cs"
      to: "DeleteQuestion L6017"
      via: "try/catch DbUpdateException + _auditLog.LogAsync DeleteQuestion"
      pattern: "DeleteQuestion.*q\\.Id"
    - from: "Controllers/OrganizationController.cs"
      to: "DeleteOrganizationUnit L410-411"
      via: "try/catch DbUpdateException dual-path + _auditLog"
      pattern: "DeleteOrganizationUnit.*unit\\.Id"
---

<objective>
Bundle-fix 5 MED cascade-safety endpoint dari Phase 328 audit: tambah `try/catch DbUpdateException` dan/atau `_auditLog.LogAsync` di endpoint yang hilang. Perubahan ini bersifat mechanical controller-only — tidak ada migration, tidak ada schema change, tidak ada view change.

Purpose: Menutup 5 MED finding (§5 Phase 328 RESEARCH.md + §9 proposal #7) untuk melindungi pengguna dari raw HTTP 500 saat FK violation dan mengisi celah audit trail pada operasi delete yang sensitif.

Output: 3 file controller/service dimodifikasi (+~75 LoC delta total), docs/IT_NOTIFY.md diupdate, 1 commit bertag dengan pesan standar D-09 item 8.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/330-fix-cascade-med-bundle-delete-category-package-question-orgu/330-CONTEXT.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
@.planning/phases/329-fix-cascade-deleteassessmentgroup-deleteprepostgroup-renewal/329-01-SUMMARY.md

<interfaces>
<!-- Kontrak yang digunakan executor — tidak perlu explorasi codebase lebih jauh. -->

Dari Controllers/AdminBaseController.cs (base class semua target controller):
```csharp
protected readonly ApplicationDbContext _context;
protected readonly UserManager<ApplicationUser> _userManager;
protected readonly AuditLogService _auditLog;
protected readonly IWebHostEnvironment _env;
// TIDAK ada _logger di AdminBaseController — OrganizationController tidak punya _logger field
```

Dari Controllers/AssessmentAdminController.cs — audit log pattern (L481 DeleteCategory):
```csharp
var currentUser = await _userManager.GetUserAsync(User);
var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
    ? (currentUser?.FullName ?? "Unknown")
    : $"{currentUser.NIP} - {currentUser.FullName}";
await _auditLog.LogAsync(currentUser?.Id ?? "", actorName, "DeleteCategory",
    $"Deleted assessment category '{category.Name}'",
    category.Id, "AssessmentCategory");
```

Dari Controllers/AssessmentAdminController.cs — audit log guard try/catch pattern (L5124-5137 DeletePackage):
```csharp
try
{
    var delUser = await _userManager.GetUserAsync(User);
    var delActorName = string.IsNullOrWhiteSpace(delUser?.NIP)
        ? (delUser?.FullName ?? "Unknown")
        : $"{delUser.NIP} - {delUser.FullName}";
    await _auditLog.LogAsync(delUser?.Id ?? "", delActorName, "DeletePackage",
        $"Deleted package '...' from assessment [ID={assessmentId}]",
        assessmentId, "AssessmentPackage");
}
catch (Exception ex) { _logger.LogWarning(ex, "Audit log failed for DeletePackage (packageId={Id})", packageId); }
```

State kode aktual sebelum fix:
- DeleteCategory L473-474: `_context.AssessmentCategories.Remove(category); await _context.SaveChangesAsync();` TIDAK wrapped
- DeletePackage L5120-5122: `_context.AssessmentPackages.Remove(pkg); await _context.SaveChangesAsync();` TIDAK wrapped
- DeleteQuestion L6015-6017: `_context.PackageQuestions.Remove(q); await _context.SaveChangesAsync();` TIDAK wrapped + tidak ada audit log
- DeleteOrganizationUnit L410-411: `_context.OrganizationUnits.Remove(unit); await _context.SaveChangesAsync();` TIDAK wrapped + tidak ada audit log
- NotificationService.DeleteAsync L286: `catch (Exception ex)` terlalu broad
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix DeleteCategory + DeletePackage + DeleteQuestion di AssessmentAdminController.cs</name>
  <files>Controllers/AssessmentAdminController.cs</files>

  <read_first>
    1. Controllers/AssessmentAdminController.cs L462-487 (DeleteCategory — baca state saat ini sebelum modifikasi)
    2. Controllers/AssessmentAdminController.cs L5090-5153 (DeletePackage — baca state saat ini sebelum modifikasi)
    3. Controllers/AssessmentAdminController.cs L5999-6037 (DeleteQuestion — baca state saat ini sebelum modifikasi)
    4. .planning/phases/330-fix-cascade-med-bundle-delete-category-package-question-orgu/330-CONTEXT.md (D-02, D-03, D-04, D-08 locked decisions)
  </read_first>

  <action>
**[A] DeleteCategory (L462-487) — per D-02**

Lokasi: L473-475 saat ini:
```csharp
_context.AssessmentCategories.Remove(category);
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);  // Phase 311 Plan 03 (D-04 invalidation)
```

Ganti dengan:
```csharp
try
{
    _context.AssessmentCategories.Remove(category);
    await _context.SaveChangesAsync();
    _cache.Remove(CategoriesCacheKey);  // Phase 311 Plan 03 (D-04 invalidation)
}
catch (DbUpdateException)
{
    TempData["Error"] = "Tidak bisa hapus kategori: masih ada data yang berelasi.";
    return RedirectToAction("ManageCategories");
}
```

Baris setelah try/catch (audit log + TempData["Success"] + return) TIDAK disentuh — tetap di tempatnya.

---

**[B] DeletePackage (L5090-5153) — per D-03**

Lokasi: L5122 saat ini:
```csharp
await _context.SaveChangesAsync();
```

Ganti baris tunggal itu dengan try/catch di sekelilingnya (HANYA baris itu, tidak restructure blok audit log L5124-5137):
```csharp
try
{
    await _context.SaveChangesAsync();
}
catch (DbUpdateException)
{
    TempData["Error"] = "Tidak bisa hapus paket: masih ada data yang berelasi.";
    return RedirectToAction("ManagePackages", new { assessmentId });
}
```

Blok audit log `try { ... } catch (Exception ex) { _logger.LogWarning(...); }` yang sudah ada di L5124 TIDAK disentuh — biarkan tepat di tempatnya.

---

**[C] DeleteQuestion (L5999-6037) — per D-04**

Lokasi: L6015-6017 saat ini:
```csharp
_context.PackageOptions.RemoveRange(q.Options);
_context.PackageQuestions.Remove(q);
await _context.SaveChangesAsync();
```

Ganti dengan (wrap SaveChangesAsync + tambah audit log block SETELAH try):
```csharp
_context.PackageOptions.RemoveRange(q.Options);
_context.PackageQuestions.Remove(q);
try
{
    await _context.SaveChangesAsync();
}
catch (DbUpdateException)
{
    TempData["Error"] = "Tidak bisa hapus soal: masih ada data yang berelasi.";
    return RedirectToAction("ManagePackageQuestions", new { packageId });
}

// Phase 330 D-04: audit log post-save (pattern verbatim DeletePackage L5124-5137)
try
{
    var currentUser = await _userManager.GetUserAsync(User);
    var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
        ? (currentUser?.FullName ?? "Unknown")
        : $"{currentUser.NIP} - {currentUser.FullName}";
    await _auditLog.LogAsync(currentUser?.Id ?? "", actorName, "DeleteQuestion",
        $"Deleted question [ID={q.Id}] from package [ID={packageId}]",
        packageId, "PackageQuestion");
}
catch (Exception ex) { _logger.LogWarning(ex, "Audit log failed for DeleteQuestion (questionId={Id})", q.Id); }
```

Baris setelah blok audit (TempData["Success"] + Auto-sync + return) tetap tidak disentuh.
  </action>

  <verify>
    <automated>grep -n "Tidak bisa hapus kategori" "Controllers/AssessmentAdminController.cs" && grep -n "Tidak bisa hapus paket" "Controllers/AssessmentAdminController.cs" && grep -n "Tidak bisa hapus soal" "Controllers/AssessmentAdminController.cs" && grep -n "DeleteQuestion.*q\.Id" "Controllers/AssessmentAdminController.cs"</automated>
  </verify>

  <acceptance_criteria>
    - grep "Tidak bisa hapus kategori: masih ada data yang berelasi." Controllers/AssessmentAdminController.cs → 1 match (D-08 exact string)
    - grep "Tidak bisa hapus paket: masih ada data yang berelasi." Controllers/AssessmentAdminController.cs → 1 match (D-08 exact string)
    - grep "Tidak bisa hapus soal: masih ada data yang berelasi." Controllers/AssessmentAdminController.cs → 1 match (D-08 exact string)
    - grep -n "DeleteQuestion.*q\.Id" Controllers/AssessmentAdminController.cs → 1 match (audit log LogAsync baris baru)
    - grep -c "catch (DbUpdateException)" Controllers/AssessmentAdminController.cs → nilai ≥ 5 (dari 2 existing di Phase 329 + 3 baru Task 1)
    - Blok audit log DeleteCategory L481 masih UTUH di posisi semula (bukan dipindah)
    - Blok audit log DeletePackage L5124-5137 masih UTUH di posisi semula (tidak di-restructure)
  </acceptance_criteria>

  <done>DeleteCategory + DeletePackage + DeleteQuestion di AssessmentAdminController.cs: masing-masing SaveChangesAsync sudah wrapped try/catch DbUpdateException dengan TempData["Error"] per D-08. DeleteQuestion memiliki _auditLog.LogAsync baru post-save.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix DeleteOrganizationUnit + NotificationService.DeleteAsync catch refactor</name>
  <files>Controllers/OrganizationController.cs, Services/NotificationService.cs</files>

  <read_first>
    1. Controllers/OrganizationController.cs L357-417 (DeleteOrganizationUnit — baca state saat ini sebelum modifikasi)
    2. Controllers/AdminBaseController.cs L14-30 (konfirmasi field _auditLog + _userManager tersedia, tidak ada _logger)
    3. Services/NotificationService.cs L270-291 (DeleteAsync — baca state saat ini sebelum modifikasi)
    4. .planning/phases/330-fix-cascade-med-bundle-delete-category-package-question-orgu/330-CONTEXT.md (D-05, D-06, D-08 locked decisions)
  </read_first>

  <action>
**[A] DeleteOrganizationUnit (L357-417 OrganizationController.cs) — per D-05**

Lokasi: L410-416 saat ini:
```csharp
_context.OrganizationUnits.Remove(unit);
await _context.SaveChangesAsync();

if (IsAjaxRequest())
    return Json(new { success = true, message = "Unit berhasil dihapus." });
TempData["Success"] = "Unit berhasil dihapus.";
return RedirectToAction("ManageOrganization");
```

Ganti dengan (wrap Remove+SaveChanges dalam try/catch DbUpdateException dual-path, lalu tambah audit log SETELAH try block):
```csharp
try
{
    _context.OrganizationUnits.Remove(unit);
    await _context.SaveChangesAsync();
}
catch (DbUpdateException)
{
    if (IsAjaxRequest())
        return Json(new { success = false, message = "Tidak bisa hapus unit: masih ada data yang berelasi." });
    TempData["Error"] = "Tidak bisa hapus unit: masih ada data yang berelasi.";
    return RedirectToAction("ManageOrganization");
}

// Phase 330 D-05: audit log post-save
// CATATAN: OrganizationController tidak punya _logger — audit log wrap pakai silent catch (no LogWarning)
try
{
    var currentUser = await _userManager.GetUserAsync(User);
    var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
        ? (currentUser?.FullName ?? "Unknown")
        : $"{currentUser.NIP} - {currentUser.FullName}";
    await _auditLog.LogAsync(currentUser?.Id ?? "", actorName, "DeleteOrganizationUnit",
        $"Deleted organization unit '{unit.Name}' [ID={unit.Id}] (Level={unit.Level})",
        unit.Id, "OrganizationUnit");
}
catch { /* audit log failure tidak block response */ }

if (IsAjaxRequest())
    return Json(new { success = true, message = "Unit berhasil dihapus." });
TempData["Success"] = "Unit berhasil dihapus.";
return RedirectToAction("ManageOrganization");
```

---

**[B] NotificationService.DeleteAsync (L270-291 Services/NotificationService.cs) — per D-06**

Lokasi: L286 saat ini:
```csharp
catch (Exception ex)
```

Ganti type exception saja (satu-satunya perubahan):
```csharp
catch (DbUpdateException ex)
```

`_logger.LogWarning` dan `return false;` di dalam catch TIDAK disentuh. TIDAK ada audit log (D-06 decision: skip — scope creep). TIDAK ada injection baru.

Pastikan `using Microsoft.EntityFrameworkCore;` sudah ada di paling atas file NotificationService.cs (untuk resolve `DbUpdateException`). Jika belum ada, tambahkan.
  </action>

  <verify>
    <automated>grep -n "Tidak bisa hapus unit" "Controllers/OrganizationController.cs" && grep -n "catch (DbUpdateException" "Services/NotificationService.cs" && grep -n "DeleteOrganizationUnit.*unit\.Id" "Controllers/OrganizationController.cs"</automated>
  </verify>

  <acceptance_criteria>
    - grep "Tidak bisa hapus unit: masih ada data yang berelasi." Controllers/OrganizationController.cs → 2 match (1 JSON + 1 TempData per dual-path D-05)
    - grep "DeleteOrganizationUnit.*unit\.Id" Controllers/OrganizationController.cs → 1 match (audit log _auditLog.LogAsync baru)
    - grep "catch (DbUpdateException" Services/NotificationService.cs → 1 match (bukan "catch (Exception" lagi)
    - grep "catch (Exception" Services/NotificationService.cs → 0 match (sudah diganti)
    - Audit log wrap di OrganizationController memakai `catch { }` silent (bukan catch (Exception ex) dengan _logger karena _logger tidak tersedia di base)
    - Tidak ada injection baru di NotificationService constructor
  </acceptance_criteria>

  <done>DeleteOrganizationUnit di OrganizationController.cs: try/catch DbUpdateException dengan dual-path JSON+TempData dan _auditLog.LogAsync post-save ditambahkan. NotificationService.DeleteAsync: catch type direfactor dari Exception → DbUpdateException.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify build + test + smoke check + IT_NOTIFY + commit + SUMMARY</name>
  <files>docs/IT_NOTIFY.md</files>

  <read_first>
    1. docs/IT_NOTIFY.md (baca entry terakhir untuk menentukan format append Phase 330)
    2. .planning/phases/330-fix-cascade-med-bundle-delete-category-package-question-orgu/330-CONTEXT.md (D-07, D-09 acceptance criteria lengkap)
  </read_first>

  <action>
**[A] Build verification**

Jalankan:
```bash
dotnet build
```
Expected: 0 error, 0 warning kritis. Jika ada error compile → fix di Task 1 atau Task 2 sesuai file yang error sebelum lanjut.

**[B] Test suite**

Jalankan:
```bash
dotnet test
```
Expected: 18/18 pass (baseline dari Phase 329). Tidak boleh ada regresi.

**[C] Manual smoke — grep-based AC verification (D-09 items 1-5)**

Jalankan grep pattern berikut sebagai verifikasi tanpa spin server:
```bash
# AC-1: DeleteCategory wrapped
grep -n "Tidak bisa hapus kategori" Controllers/AssessmentAdminController.cs

# AC-2: DeletePackage wrapped
grep -n "Tidak bisa hapus paket" Controllers/AssessmentAdminController.cs

# AC-3: DeleteQuestion wrapped + audit log
grep -n "Tidak bisa hapus soal" Controllers/AssessmentAdminController.cs
grep -n "DeleteQuestion.*q\.Id" Controllers/AssessmentAdminController.cs

# AC-4: DeleteOrganizationUnit wrapped + audit log
grep -n "Tidak bisa hapus unit" Controllers/OrganizationController.cs
grep -n "DeleteOrganizationUnit.*unit\.Id" Controllers/OrganizationController.cs

# AC-5: NotificationService catch refactor
grep -n "catch (DbUpdateException" Services/NotificationService.cs
grep -n "catch (Exception" Services/NotificationService.cs   # harus 0 match
```

Semua harus menghasilkan match sesuai acceptance criteria Task 1 + Task 2.

**[D] Append docs/IT_NOTIFY.md — per D-07**

Baca file IT_NOTIFY.md terlebih dahulu, lalu append section baru di bawah entry Phase 329 (atau di bawah entry terakhir):

```markdown
## Phase 330 — Fix Cascade MED Bundle (DeleteCategory/Package/Question/OrgUnit/Notification)

**Commit:** [commit hash setelah git commit di langkah E]
**Date shipped local:** 2026-05-28
**Flag migration:** NO — zero schema change, zero migration

### Files Modified
- `Controllers/AssessmentAdminController.cs` — DeleteCategory + DeletePackage + DeleteQuestion: try/catch DbUpdateException + _auditLog.LogAsync (DeleteQuestion baru)
- `Controllers/OrganizationController.cs` — DeleteOrganizationUnit: try/catch DbUpdateException dual-path (JSON+TempData) + _auditLog.LogAsync
- `Services/NotificationService.cs` — DeleteAsync: catch (Exception) → catch (DbUpdateException)

### Action Required by IT
Bundle v19.0 batch push (Phase 325+326+327+329+330) — tunggu user release push lock per Phase 327 option-b. Tidak ada migration DB. Tidak ada perubahan config. Tidak ada seed data.
```

**[E] Git commit**

```bash
git add Controllers/AssessmentAdminController.cs Controllers/OrganizationController.cs Services/NotificationService.cs docs/IT_NOTIFY.md
git commit -m "feat(330): cascade med-bundle try-catch + audit log (DeleteCategory/Package/Question/OrgUnit)"
```

Setelah commit, catat commit hash → update entry IT_NOTIFY.md Phase 330 (ganti `[commit hash setelah git commit di langkah E]` dengan hash aktual).

**[F] Generate SUMMARY.md**

Buat file `.planning/phases/330-fix-cascade-med-bundle-delete-category-package-question-orgu/330-01-SUMMARY.md` mengikuti template dari `.claude/get-shit-done/templates/summary.md`. Isi:
- Status: SHIPPED LOCAL
- Files modified table (3 file + IT_NOTIFY.md)
- D-09 AC table 9/9 checklist
- Grep marker counts (DbUpdateException total, error message strings, audit log patterns)
- Threat model disposition table
- Next steps (bundle push Phase 325+326+327+329+330 menunggu push lock release)
  </action>

  <verify>
    <automated>dotnet build 2>&1 | tail -5 && dotnet test 2>&1 | grep -E "passed|failed|Total"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build: 0 error (Build succeeded)
    - dotnet test: 18 passed, 0 failed (atau lebih jika ada test baru dari phase lain)
    - grep "Tidak bisa hapus kategori" Controllers/AssessmentAdminController.cs → 1 match
    - grep "Tidak bisa hapus paket" Controllers/AssessmentAdminController.cs → 1 match
    - grep "Tidak bisa hapus soal" Controllers/AssessmentAdminController.cs → 1 match
    - grep "Tidak bisa hapus unit" Controllers/OrganizationController.cs → 2 match (JSON + TempData)
    - grep "catch (DbUpdateException" Services/NotificationService.cs → 1 match
    - grep "catch (Exception" Services/NotificationService.cs → 0 match
    - git log --oneline -1 → commit message "feat(330): cascade med-bundle try-catch + audit log (DeleteCategory/Package/Question/OrgUnit)"
    - docs/IT_NOTIFY.md mengandung "Phase 330" section
    - 330-01-SUMMARY.md file ada di direktori phase
  </acceptance_criteria>

  <done>Build bersih, 18/18 test pass, semua 9 AC dari D-09 terverifikasi via grep, IT_NOTIFY.md diupdate dengan Phase 330 entry, commit dibuat dengan message per D-09 item 8, SUMMARY.md digenerate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller | POST request dengan anti-forgery token; `[ValidateAntiForgeryToken]` aktif di semua endpoint target |
| Controller → Database | EF Core DbUpdateException boundary — sebelumnya unhandled, sekarang di-catch friendly |
| Controller → AuditLog | Service call yang bisa gagal — di-wrap dalam try/catch independent agar tidak block response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-01 | D (Denial of Service) | DeleteCategory/Package/Question/OrgUnit | mitigate | try/catch DbUpdateException + TempData["Error"] friendly menggantikan raw HTTP 500 — user mendapat feedback informatif, bukan crash |
| T-330-02 | I (Information Disclosure) | catch block error message | accept | "Tidak bisa hapus {entity}: masih ada data yang berelasi." — pesan generik, tidak mengekspos struktur DB atau nama tabel ke user |
| T-330-03 | T (Tampering) | DeleteOrganizationUnit dual-path response | accept | IsAjaxRequest() check untuk path branching; `[Authorize(Roles="Admin, HC")]` masih aktif via attribute trio — tidak ada bypass path baru |
| T-330-04 | R (Repudiation) | DeleteQuestion + DeleteOrganizationUnit (no audit trail sebelumnya) | mitigate | _auditLog.LogAsync ditambahkan di kedua endpoint — operasi delete sekarang terlacak di AuditLog table |
| T-330-05 | I (Information Disclosure) | catch (DbUpdateException ex) di NotificationService | accept | ex variable hanya dipakai untuk _logger.LogWarning server-side; tidak ada ex.Message yang dikembalikan ke client |
</threat_model>

<verification>
Verifikasi akhir keseluruhan Phase 330:

1. **Build clean:** `dotnet build` → 0 error
2. **Test suite:** `dotnet test` → 18/18 pass (baseline Phase 329)
3. **Grep AC (5 endpoint):**
   - `grep -c "catch (DbUpdateException" Controllers/AssessmentAdminController.cs` → ≥ 5
   - `grep -c "Tidak bisa hapus" Controllers/AssessmentAdminController.cs` → ≥ 3
   - `grep -c "Tidak bisa hapus unit" Controllers/OrganizationController.cs` → 2
   - `grep -c "catch (DbUpdateException" Services/NotificationService.cs` → 1
   - `grep -c "catch (Exception" Services/NotificationService.cs` → 0
4. **Audit log baru:** `grep -n "DeleteQuestion.*q\.Id\|DeleteOrganizationUnit.*unit\.Id" Controllers/*.cs` → 2 match di 2 file berbeda
5. **IT_NOTIFY:** `grep "Phase 330" docs/IT_NOTIFY.md` → 1 match
6. **Commit message:** `git log --oneline -1` → mengandung "feat(330): cascade med-bundle"
</verification>

<success_criteria>
Phase 330 selesai ketika:
- [ ] 5 endpoint target sudah dimodifikasi sesuai D-02..D-06
- [ ] `dotnet build` clean (0 error)
- [ ] `dotnet test` 18/18 pass
- [ ] Semua 9 AC dari D-09 terverifikasi
- [ ] docs/IT_NOTIFY.md diupdate dengan Phase 330 entry + commit hash
- [ ] 1 commit dengan message `feat(330): cascade med-bundle try-catch + audit log (DeleteCategory/Package/Question/OrgUnit)`
- [ ] 330-01-SUMMARY.md digenerate
- [ ] Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-07)
</success_criteria>

<output>
Setelah Task 3 selesai, buat `.planning/phases/330-fix-cascade-med-bundle-delete-category-package-question-orgu/330-01-SUMMARY.md` dengan isi:
- Status: SHIPPED LOCAL
- Files modified (3 controller/service + IT_NOTIFY.md)
- AC D-09 table 9/9 checklist
- Grep marker count verification
- Threat model disposition 5/5
- Next steps: bundle push Phase 325+326+327+329+330 menunggu user push lock release (Phase 327 option-b)
</output>
