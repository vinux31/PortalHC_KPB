---
phase: 333-fix-cascade-deletecoachingsession-file-atomicity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - docs/IT_NOTIFY.md
autonomous: true
requirements:
  - D-01-DeleteCoachingSession-file-post-commit
  - D-02-DeleteCoachingSession-catch-friendly
must_haves:
  truths:
    - "DeleteCoachingSession: pathsToDelete dideklarasi sebagai List<string>? = null di SCOPE OUTER tx (sebelum await using var tx) sehingga visible POST CommitAsync"
    - "DeleteCoachingSession: build pathsToDelete + JSON parse EvidencePathHistory + cleanupNote tetap INSIDE tx scope (collection-only, no actual File.Delete)"
    - "DeleteCoachingSession: progress revert state (~13 fields) + RecordStatusHistory + SaveChangesAsync + AuditLog.LogAsync preserved verbatim INSIDE tx scope, mendahului CommitAsync"
    - "DeleteCoachingSession: System.IO.File.Delete loop POST CommitAsync, OUTSIDE tx scope, dengan inner try/catch warn-only per file (log + iterate, tidak break + tidak rollback)"
    - "DeleteCoachingSession: catch generic Exception + throw direfactor → catch DbUpdateException (friendly TempData) + catch Exception fallback (friendly TempData), keduanya return RedirectToAction Deliverable"
    - "DeleteCoachingSession: TIDAK ada eksplisit await tx.RollbackAsync() di catch blocks (await using disposal auto-rollback)"
    - "DeleteCoachingSession: TIDAK ada throw; di catch blocks (replaced dengan friendly TempData + RedirectToAction)"
    - "DeleteCoachingSession: existing BeginTransactionAsync L2455 dipreserve (TIDAK ditambah baru — beda dari Phase 331/332)"
    - "DeleteCoachingSession: Role check [Authorize(Roles = RolesCoachAndAbove)] L2432 + user challenge L2436 + active mapping guard L2441-2453 + null check session L2440 preserved verbatim OUTSIDE tx scope"
    - "DeleteCoachingSession: Status='Approved' branch + sibling sessions branch preserved verbatim INSIDE tx (no File.Delete dalam branch ini — intentional)"
    - "dotnet build clean, dotnet test 18/18 pass — tidak ada regresi dari Phase 332 baseline"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "DeleteCoachingSession refactored: pathsToDelete outer scope + File.Delete POST commit + catch DbUpdateException friendly + catch Exception fallback friendly"
      contains: "List<string>? pathsToDelete = null"
    - path: "docs/IT_NOTIFY.md"
      provides: "Phase 333 IT notification entry (bundle v19.0 append)"
      contains: "Phase 333"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "DeleteCoachingSession L2433-2543"
      via: "List<string>? pathsToDelete = null (outer); await using var tx; ...; await tx.CommitAsync(); ...; if (pathsToDelete != null) foreach File.Delete"
      pattern: "File\\.Delete post-commit failed \\(CoachingSession evidence\\)"
    - from: "Controllers/CDPController.cs"
      to: "Catch DbUpdateException + Catch Exception fallback baru"
      via: "catch (DbUpdateException dbEx) → friendly TempData + RedirectToAction; catch (Exception ex) → friendly TempData + RedirectToAction; no throw, no tx.RollbackAsync"
      pattern: "Gagal hapus sesi coaching"
    - from: "Controllers/CDPController.cs"
      to: "Role check + active mapping guard"
      via: "[Authorize(Roles = UserRoles.RolesCoachAndAbove)] L2432 + L2441-2453 OUTSIDE tx scope, early return TempData friendly tanpa tx creation"
      pattern: "Mapping Anda dengan coachee ini sudah tidak aktif"
---

<objective>
Tutup 1 HIGH finding Phase 328 Cascade Audit Sweep (§4.6 + §9 proposal #4): file-DB atomicity di `DeleteCoachingSession` via reorder `System.IO.File.Delete` POST `tx.CommitAsync` + refactor catch generic Exception throw → catch DbUpdateException friendly + catch Exception fallback friendly. Perubahan ini bersifat mechanical controller-only — tidak ada migration, tidak ada schema change, tidak ada service injection, tidak ada view change.

**KEY DIFFERENCE dari Phase 331/332:** existing `BeginTransactionAsync` di L2455 SUDAH ADA — Phase 333 TIDAK perlu menambahkan tx baru. Phase 333 = REORGANIZE existing tx scope (move pathsToDelete declaration ke outer scope) + REORDER File.Delete loop dari pre-CommitAsync (INSIDE tx L2490-2503) ke post-CommitAsync (OUTSIDE tx) + REFACTOR catch generic Exception throw menjadi 2-catch (DbUpdateException + Exception) friendly TempData tanpa throw + tanpa eksplisit RollbackAsync.

Purpose: Menghilangkan window orphan-delete (file evidence hilang dari disk tapi DB tidak ter-commit — progress state masih reference path null-able dan tidak ter-revert) dan menjamin atomicity Remove(ActionItems) + Remove(CoachingSession) + progress revert state (~13 fields) + RecordStatusHistory + SaveChanges + AuditLog dalam satu tx commit. Status="Approved" branch + sibling sessions branch + JSON parse EvidencePathHistory inner try/catch + role check + active mapping guard tetap dipertahankan verbatim. Catch refactor menambahkan friendly UX (sebelumnya raw 500 + throw escape).

Output: 1 file controller dimodifikasi (~40 LoC delta, 1 endpoint), `docs/IT_NOTIFY.md` diupdate dengan Phase 333 entry + scenario #11, 1 commit bertag pesan standar D-10 item 9, SUMMARY.md digenerate.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-CONTEXT.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
@.planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-01-PLAN.md
@.planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-01-PLAN.md

<interfaces>
<!-- Kontrak yang digunakan executor — tidak perlu eksplorasi codebase lebih jauh. -->

Dari Controllers/CDPController.cs (base ApplicationDbContext + DI):
```csharp
private readonly ApplicationDbContext _context;
private readonly UserManager<ApplicationUser> _userManager;
private readonly AuditLogService _auditLog;
private readonly IWebHostEnvironment _env;
private readonly ILogger<CDPController> _logger;
// RecordStatusHistory(int progressId, string action, string actorId, string actorName, string actorRole) — instance method INSIDE CDPController
```

Dari Microsoft.EntityFrameworkCore (sudah imported di CDPController.cs):
```csharp
DbContext.Database.BeginTransactionAsync();   // returns IDbContextTransaction
IDbContextTransaction.CommitAsync();
// await using var tx = ...; → disposal otomatis rollback jika exception escape sebelum CommitAsync
```

AuditLog signature verbatim L2536-2537 (preserve INSIDE tx):
```csharp
await _auditLog.LogAsync(
    user.Id, actorName, "DeleteCoachingSession",
    $"Session ID={id} dihapus.{cleanupNote}", id, "CoachingSession");
```

State kode aktual sebelum fix (verbatim dari Controllers/CDPController.cs L2433-2543):

**DeleteCoachingSession L2433-2543** (target Task 1):
- L2433: signature `public async Task<IActionResult> DeleteCoachingSession(int id)`
- L2435-2436: `var user = await _userManager.GetUserAsync(User);` + `if (user == null) return Challenge();` (preserve)
- L2437-2439: load session Include ActionItems FirstOrDefault (preserve)
- L2440: `if (session == null) return NotFound();` (preserve)
- L2441-2442: role check isHcOrAdmin + Forbid (preserve)
- L2443-2453: active mapping guard Coach-only TempData friendly (preserve verbatim)
- L2454: `var progressId = session.ProtonDeliverableProgressId;` (preserve)
- L2455: `await using var tx = await _context.Database.BeginTransactionAsync();` (PRESERVE — sudah ada, beda dari Phase 331/332)
- L2456: `try {` (preserve)
- L2458-2459: RemoveRange ActionItems + Remove CoachingSession (preserve INSIDE tx)
- L2461: `string cleanupNote = "";` (preserve INSIDE tx)
- L2463-2530: nested-if conditional cleanup branch (most preserved):
  - L2467: `if (!hasOtherSessions)` branch (preserve outer)
  - L2471: `if (progress != null && progress.Status != "Approved")` branch (preserve outer)
  - L2473: `var pathsToDelete = new List<string>();` ← UBAH jadi reassignment outer var (no `var`)
  - L2474-2487: JSON parse EvidencePathHistory inner try/catch warn-only (preserve VERBATIM)
  - L2490-2503: foreach pathsToDelete → File.Delete inner try/catch IOException ← **HAPUS SELURUHNYA** dari INSIDE tx
  - L2505-2517: progress revert state ~13 field (preserve verbatim INSIDE tx)
  - L2518: `RecordStatusHistory(progress.Id, "Reverted to Pending", user.Id, user.FullName ?? "", "Coach");` (preserve INSIDE tx)
  - L2519: `cleanupNote = $"... {pathsToDelete.Count} file(s) cleaned.";` (preserve verbatim — pathsToDelete.Count still accurate at build time)
  - L2521-2524: Status=="Approved" branch (preserve verbatim)
  - L2526-2529: sibling sessions branch (preserve verbatim)
- L2532: `await _context.SaveChangesAsync();` (preserve INSIDE tx)
- L2533-2537: audit log block actorName + LogAsync (preserve verbatim INSIDE tx)
- L2538: `await tx.CommitAsync();` (preserve INSIDE tx, akhir try block)
- L2540: `catch (Exception ex) { _logger.LogError(...); await tx.RollbackAsync(); throw; }` ← **REFACTOR** jadi 2 catch (DbUpdateException + Exception fallback) friendly, NO throw, NO RollbackAsync
- L2541: `TempData["Success"] = "Sesi coaching berhasil dihapus.";` ← pindah POSISI setelah File.Delete loop POST commit
- L2542: `return RedirectToAction("Deliverable", new { id = progressId });` (preserve final)

Pattern yang sudah benar (preserve verbatim):
- `await using var tx` disposal — auto-rollback jika exception escape sebelum CommitAsync (NO explicit RollbackAsync needed)
- JSON parse EvidencePathHistory inner try/catch L2484-2487 — warn-only pattern sudah benar
- cross-platform path normalization L2495 `Replace('/', Path.DirectorySeparatorChar)` — preserve

Method response patterns:
- Existing returns: Challenge (no user) + NotFound (no session) + Forbid (role) + RedirectToAction Deliverable (success + mapping fail)
- **NEW** DbUpdateException catch: `TempData["Error"] = "Gagal hapus sesi coaching: ada constraint database yang dilanggar."; return RedirectToAction("Deliverable", new { id = progressId });`
- **NEW** Exception fallback catch: `TempData["Error"] = "Gagal hapus sesi coaching: terjadi kesalahan internal. Hubungi admin."; return RedirectToAction("Deliverable", new { id = progressId });`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor DeleteCoachingSession — move pathsToDelete outer tx scope, defer File.Delete POST CommitAsync, refactor catch generic+throw → DbUpdateException + Exception fallback friendly</name>
  <files>Controllers/CDPController.cs</files>

  <read_first>
    1. Controllers/CDPController.cs L2430-2545 (DeleteCoachingSession — baca state saat ini lengkap, termasuk attribute + signature)
    2. .planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-CONTEXT.md (D-02 pathsToDelete outer scope, D-03 catch refactor, D-04 cleanupNote timing, D-05 file warn-only, D-06 active mapping preserve, D-07 Approved branch preserve, D-08 sibling branch preserve, D-10 acceptance criteria lengkap)
    3. .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md §4.6 (DeleteCoachingSession root cause D2 + D6 remediation)
    4. .planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-01-PLAN.md (catch DbUpdateException friendly pattern reference)
  </read_first>

  <action>
**Refactor `DeleteCoachingSession` (L2433-2543) — per D-02 + D-03 + D-04 + D-05**

Ganti body method DeleteCoachingSession dari setelah L2453 (closing brace active mapping guard `}` di L2453) sampai L2542 (sebelum closing brace method `}` di L2543) dengan struktur berikut. Reuse semua kode existing verbatim — hanya extract `pathsToDelete` declaration ke OUTER tx scope + HAPUS File.Delete loop INSIDE tx + TAMBAH File.Delete loop POST CommitAsync + REFACTOR catch generic+throw menjadi catch DbUpdateException + catch Exception fallback friendly.

**Bagian PRESERVE verbatim L2433-2453:**
```csharp
public async Task<IActionResult> DeleteCoachingSession(int id)
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();
    var session = await _context.CoachingSessions
        .Include(s => s.ActionItems)
        .FirstOrDefaultAsync(s => s.Id == id);
    if (session == null) return NotFound();
    bool isHcOrAdmin = User.IsInRole("HC") || User.IsInRole("Admin");
    if (!isHcOrAdmin && session.CoachId != user.Id) return Forbid();
    // MED-08 consistency: same active mapping guard as EditCoachingSession
    if (!isHcOrAdmin)
    {
        bool stillMapped = await _context.CoachCoacheeMappings
            .AnyAsync(m => m.CoachId == user.Id && m.CoacheeId == session.CoacheeId && m.IsActive);
        if (!stillMapped)
        {
            TempData["Error"] = "Mapping Anda dengan coachee ini sudah tidak aktif — hapus session tidak diizinkan. Hubungi HC.";
            return RedirectToAction("Deliverable", new { id = session.ProtonDeliverableProgressId });
        }
    }
```

**Bagian REFACTOR L2454-2542 — ganti dengan struktur baru ini:**
```csharp
    var progressId = session.ProtonDeliverableProgressId;

    // Phase 333 D-02: declare pathsToDelete di SCOPE OUTER tx — visible POST CommitAsync untuk File.Delete loop
    List<string>? pathsToDelete = null;

    await using var tx = await _context.Database.BeginTransactionAsync();
    try
    {
        _context.ActionItems.RemoveRange(session.ActionItems);
        _context.CoachingSessions.Remove(session);

        // HIGH-06 fix: jika ini session terakhir utk progress, cleanup file fisik + revert status.
        string cleanupNote = "";
        if (progressId.HasValue)
        {
            bool hasOtherSessions = await _context.CoachingSessions
                .AnyAsync(s => s.ProtonDeliverableProgressId == progressId.Value && s.Id != id);
            if (!hasOtherSessions)
            {
                var progress = await _context.ProtonDeliverableProgresses
                    .FirstOrDefaultAsync(p => p.Id == progressId.Value);
                if (progress != null && progress.Status != "Approved")
                {
                    // Phase 333 D-02: build pathsToDelete INSIDE tx (collection only, no actual File.Delete)
                    pathsToDelete = new List<string>();
                    if (!string.IsNullOrEmpty(progress.EvidencePath))
                        pathsToDelete.Add(progress.EvidencePath);
                    if (!string.IsNullOrEmpty(progress.EvidencePathHistory))
                    {
                        try
                        {
                            var history = System.Text.Json.JsonSerializer
                                .Deserialize<List<string>>(progress.EvidencePathHistory) ?? new List<string>();
                            pathsToDelete.AddRange(history);
                        }
                        catch (Exception jex)
                        {
                            _logger.LogWarning(jex, "Failed to parse EvidencePathHistory for progress {Pid}", progress.Id);
                        }
                    }

                    // Phase 333 D-02: File.Delete loop DIPINDAH POST CommitAsync — DIHAPUS dari sini
                    // (was L2490-2503 — file delete inside tx pre-commit)

                    progress.EvidencePath = null;
                    progress.EvidenceFileName = null;
                    progress.EvidencePathHistory = null;
                    progress.SubmittedAt = null;
                    progress.Status = "Pending";
                    progress.SrSpvApprovalStatus = "Pending";
                    progress.SrSpvApprovedById = null;
                    progress.SrSpvApprovedAt = null;
                    progress.ShApprovalStatus = "Pending";
                    progress.ShApprovedById = null;
                    progress.ShApprovedAt = null;
                    progress.RejectedAt = null;
                    progress.RejectionReason = null;
                    RecordStatusHistory(progress.Id, "Reverted to Pending", user.Id, user.FullName ?? "", "Coach");
                    cleanupNote = $" Progress {progress.Id} reverted to Pending, {pathsToDelete.Count} file(s) cleaned.";
                }
                else if (progress != null && progress.Status == "Approved")
                {
                    cleanupNote = $" Progress {progress.Id} sudah Approved — state & file dipertahankan.";
                }
            }
            else
            {
                cleanupNote = " Sibling sessions masih ada — progress state dipertahankan.";
            }
        }

        await _context.SaveChangesAsync();
        var actorName = string.IsNullOrWhiteSpace(user.NIP)
            ? (user.FullName ?? "Unknown")
            : $"{user.NIP} - {user.FullName}";
        await _auditLog.LogAsync(user.Id, actorName, "DeleteCoachingSession",
            $"Session ID={id} dihapus.{cleanupNote}", id, "CoachingSession");
        await tx.CommitAsync();
    }
    catch (DbUpdateException dbEx)
    {
        // Phase 333 D-03: await using disposal auto-rollback — no explicit RollbackAsync
        _logger.LogWarning(dbEx, "DbUpdate failed for DeleteCoachingSession ID={Id}", id);
        TempData["Error"] = "Gagal hapus sesi coaching: ada constraint database yang dilanggar.";
        return RedirectToAction("Deliverable", new { id = progressId });
    }
    catch (Exception ex)
    {
        // Phase 333 D-03: fallback generic — friendly TempData, NO throw, NO explicit RollbackAsync
        _logger.LogError(ex, "Failed to delete coaching session ID={Id}", id);
        TempData["Error"] = "Gagal hapus sesi coaching: terjadi kesalahan internal. Hubungi admin.";
        return RedirectToAction("Deliverable", new { id = progressId });
    }

    // Phase 333 D-05: File.Delete loop POST CommitAsync — inner try/catch warn-only per file
    if (pathsToDelete != null && pathsToDelete.Count > 0)
    {
        foreach (var relUrl in pathsToDelete)
        {
            try
            {
                var physical = Path.Combine(_env.WebRootPath,
                    relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
                if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
            }
            catch (Exception fex)
            {
                _logger.LogWarning(fex, "File.Delete post-commit failed (CoachingSession evidence): {Path}", relUrl);
            }
        }
    }

    TempData["Success"] = "Sesi coaching berhasil dihapus.";
    return RedirectToAction("Deliverable", new { id = progressId });
}
```

**Catatan presisi (per CONTEXT D-02..D-08):**
- L2432 `[Authorize(Roles = UserRoles.RolesCoachAndAbove)]` attribute + L2433 signature `public async Task<IActionResult> DeleteCoachingSession(int id)` — **TIDAK disentuh**.
- L2435-2453 user load + null check + session load + null check + role check + active mapping guard — **preserve verbatim**, **TIDAK disentuh**. Posisi tetap OUTSIDE tx scope.
- L2454 `var progressId = session.ProtonDeliverableProgressId;` — **preserve verbatim**.
- **TAMBAH** baris baru SEBELUM `await using var tx`: `List<string>? pathsToDelete = null;` (nullable initial null, allocate inside conditional branch).
- L2455 `await using var tx = await _context.Database.BeginTransactionAsync();` — **preserve verbatim** (SUDAH ADA, beda dari Phase 331/332).
- L2456 `try {` — preserve.
- L2458-2459 RemoveRange ActionItems + Remove CoachingSession — preserve verbatim INSIDE tx.
- L2461 `string cleanupNote = "";` — preserve verbatim INSIDE tx.
- L2463-2467 nested-if `progressId.HasValue` + `!hasOtherSessions` — preserve verbatim INSIDE tx.
- L2469-2471 progress load + `progress != null && progress.Status != "Approved"` branch — preserve verbatim INSIDE tx.
- L2473 `var pathsToDelete = new List<string>();` → **UBAH** menjadi `pathsToDelete = new List<string>();` (reassign outer var, hapus `var` keyword agar tidak shadow outer declaration).
- L2474-2487 JSON parse EvidencePathHistory inner try/catch — **preserve verbatim**.
- L2490-2503 foreach File.Delete inner try/catch IOException — **HAPUS SELURUHNYA** dari INSIDE tx. Ganti dengan komentar `// Phase 333 D-02: File.Delete loop DIPINDAH POST CommitAsync — DIHAPUS dari sini` (opsional komentar boleh skip).
- L2505-2517 progress revert state (~13 fields: EvidencePath, EvidenceFileName, EvidencePathHistory, SubmittedAt, Status, SrSpv*, Sh*, RejectedAt, RejectionReason) — **preserve verbatim** INSIDE tx.
- L2518 RecordStatusHistory call — **preserve verbatim** INSIDE tx.
- L2519 `cleanupNote = $"... {pathsToDelete.Count} file(s) cleaned.";` — **preserve verbatim** (pathsToDelete.Count tetap akurat dari INSIDE tx build, count == intended file count meskipun delete deferred).
- L2521-2524 `else if (progress != null && progress.Status == "Approved")` branch + cleanupNote — preserve verbatim INSIDE tx, **TIDAK ada File.Delete** dalam branch ini per D-07.
- L2526-2529 `else { cleanupNote = " Sibling sessions masih ada..."; }` — preserve verbatim INSIDE tx, **TIDAK ada File.Delete** dalam branch ini per D-08.
- L2532 `await _context.SaveChangesAsync();` — preserve verbatim INSIDE tx.
- L2533-2537 actorName + audit log `_auditLog.LogAsync` — preserve verbatim INSIDE tx.
- L2538 `await tx.CommitAsync();` — preserve verbatim INSIDE tx, akhir try block.
- L2540 `catch (Exception ex) { _logger.LogError(...); await tx.RollbackAsync(); throw; }` → **REFACTOR SELURUHNYA** menjadi 2 catch blocks:
  - `catch (DbUpdateException dbEx)` FIRST (specific): `_logger.LogWarning(dbEx, ...)`, `TempData["Error"] = "Gagal hapus sesi coaching: ada constraint database yang dilanggar.";`, `return RedirectToAction("Deliverable", new { id = progressId });`
  - `catch (Exception ex)` SECOND (general fallback): `_logger.LogError(ex, ...)`, `TempData["Error"] = "Gagal hapus sesi coaching: terjadi kesalahan internal. Hubungi admin.";`, `return RedirectToAction("Deliverable", new { id = progressId });`
  - **TIDAK ada** `await tx.RollbackAsync();` di kedua catch (await using disposal auto-rollback).
  - **TIDAK ada** `throw;` di kedua catch (replaced dengan return RedirectToAction).
- **TAMBAH** File.Delete loop POST CommitAsync (OUTSIDE try/catch, SEBELUM `TempData["Success"]`):
  ```csharp
  if (pathsToDelete != null && pathsToDelete.Count > 0)
  {
      foreach (var relUrl in pathsToDelete)
      {
          try
          {
              var physical = Path.Combine(_env.WebRootPath,
                  relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
              if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
          }
          catch (Exception fex)
          {
              _logger.LogWarning(fex, "File.Delete post-commit failed (CoachingSession evidence): {Path}", relUrl);
          }
      }
  }
  ```
  Inner catch `Exception` (bukan `IOException` specific) — konsisten Phase 331/332 D-07.
  Cross-platform path normalization `Replace('/', Path.DirectorySeparatorChar)` preserved.
- L2541 `TempData["Success"] = "Sesi coaching berhasil dihapus.";` — preserve verbatim, **POSISI BARU**: SETELAH File.Delete loop POST commit.
- L2542 `return RedirectToAction("Deliverable", new { id = progressId });` — preserve verbatim, akhir method.
- **TIDAK ada import baru** — `Microsoft.EntityFrameworkCore` (DbUpdateException + BeginTransactionAsync), `System.IO.File`, `Path`, `System.Text.Json` semua sudah tersedia di file.
- **TIDAK ada perubahan ke method lain** dalam CDPController.cs.
  </action>

  <verify>
    <automated>grep -c "List<string>? pathsToDelete = null" Controllers/CDPController.cs && grep -c "File.Delete post-commit failed (CoachingSession evidence)" Controllers/CDPController.cs && grep -c "Gagal hapus sesi coaching" Controllers/CDPController.cs && grep -c "catch (DbUpdateException dbEx)" Controllers/CDPController.cs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "List<string>? pathsToDelete = null" Controllers/CDPController.cs` → 1 match (outer var declaration baru)
    - `grep -c "File.Delete post-commit failed (CoachingSession evidence)" Controllers/CDPController.cs` → 1 match (new post-commit log)
    - `grep -c "catch (DbUpdateException dbEx)" Controllers/CDPController.cs` → ≥1 match (catch baru spesifik di DeleteCoachingSession)
    - `grep -c "Gagal hapus sesi coaching" Controllers/CDPController.cs` → 2 match (2 catch blocks friendly TempData)
    - `grep -c "tx.RollbackAsync" Controllers/CDPController.cs` → 0 match (removed dari DeleteCoachingSession) — NOTE: kalau method lain memang ada explicit RollbackAsync, scope check via grep -n line range L2433-2543 saja
    - `grep -n "throw;" Controllers/CDPController.cs` → confirm tidak ada `throw;` dalam range L2433-2543 (DeleteCoachingSession method body)
    - `grep -c "Failed to delete coaching session ID=" Controllers/CDPController.cs` → 1 match (LogError Exception fallback preserved)
    - `grep -c "DbUpdate failed for DeleteCoachingSession" Controllers/CDPController.cs` → 1 match (LogWarning DbUpdateException baru)
    - `grep -c "Reverted to Pending" Controllers/CDPController.cs` → ≥1 match (RecordStatusHistory preserved)
    - `grep -c "Mapping Anda dengan coachee ini sudah tidak aktif" Controllers/CDPController.cs` → 1 match (active mapping guard preserved)
    - `grep -c "sudah Approved — state & file dipertahankan" Controllers/CDPController.cs` → 1 match (Approved branch preserved per D-07)
    - `grep -c "Sibling sessions masih ada" Controllers/CDPController.cs` → 1 match (sibling branch preserved per D-08)
    - `grep -c "Sesi coaching berhasil dihapus" Controllers/CDPController.cs` → 1 match (success TempData preserved)
    - `grep -c "Failed to parse EvidencePathHistory" Controllers/CDPController.cs` → 1 match (JSON parse inner catch preserved verbatim)
    - File DeleteCoachingSession menjaga semua jalur response existing (Challenge + NotFound + Forbid + RedirectToAction mapping fail + RedirectToAction success) PLUS 2 jalur baru (DbUpdateException catch redirect + Exception fallback catch redirect)
  </acceptance_criteria>

  <done>DeleteCoachingSession L2433-2543 di CDPController.cs: `List<string>? pathsToDelete = null` dideklarasi SEBELUM `await using var tx`, semua DB mutation (RemoveRange ActionItems + Remove CoachingSession + progress revert state ~13 fields + RecordStatusHistory + SaveChanges + AuditLog) tetap INSIDE tx scope mendahului `tx.CommitAsync()`, File.Delete loop dipindah POST CommitAsync dengan inner try/catch warn-only per file (log distinct "(CoachingSession evidence)"), catch generic Exception+throw direfactor menjadi catch DbUpdateException + catch Exception fallback friendly TempData + RedirectToAction tanpa `throw;` tanpa eksplisit `tx.RollbackAsync()`, role check + active mapping guard + Status="Approved" branch + sibling sessions branch + JSON parse EvidencePathHistory preserved verbatim, success TempData + return RedirectToAction final pindah POSISI setelah File.Delete loop POST commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify build + test + grep AC + IT_NOTIFY append + commit + SUMMARY</name>
  <files>docs/IT_NOTIFY.md, .planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-SUMMARY.md</files>

  <read_first>
    1. docs/IT_NOTIFY.md (baca entry terakhir — Phase 332 — untuk format append Phase 333)
    2. .planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-CONTEXT.md (D-09 IT_NOTIFY bundle v19.0, D-10 acceptance criteria lengkap 11 item)
    3. C:/Users/Administrator/.claude/get-shit-done/templates/summary.md (template SUMMARY.md)
  </read_first>

  <action>
**[A] Build verification (D-10 item 7)**

Jalankan:
```bash
dotnet build
```
Expected: 0 error CS*. Jika ada compile error → kembali ke Task 1 sesuai baris yang error sebelum lanjut.

**[B] Test suite (D-10 item 8, no regression Phase 332 baseline)**

Jalankan:
```bash
dotnet test --no-build
```
Expected: 18/18 pass (baseline dari Phase 332). Tidak boleh ada regresi.

**[C] Grep-based AC verification (D-10 items 1-6)**

Jalankan grep pattern berikut sebagai verifikasi mekanis tanpa spin server. Untuk pattern dengan kemungkinan match di method lain (mis. `throw;`, `tx.RollbackAsync`), batasi scope ke range L2433-2543 menggunakan `awk '/^        public async Task<IActionResult> DeleteCoachingSession/,/^        \/\/ HIGH-07/'` atau `sed -n '2433,2543p'` sebelum grep:

```bash
# AC-1 (D-10 item 1): pathsToDelete declared di OUTER tx scope
grep -c "List<string>? pathsToDelete = null" Controllers/CDPController.cs      # expected 1

# AC-2 (D-10 item 1): File.Delete loop POST commit dengan log distinct
grep -c "File.Delete post-commit failed (CoachingSession evidence)" Controllers/CDPController.cs   # expected 1

# AC-3 (D-10 item 5): catch DbUpdateException baru + LogWarning
grep -c "catch (DbUpdateException dbEx)" Controllers/CDPController.cs           # expected ≥1
grep -c "DbUpdate failed for DeleteCoachingSession" Controllers/CDPController.cs    # expected 1

# AC-4 (D-10 item 5): friendly TempData 2 catch blocks
grep -c "Gagal hapus sesi coaching" Controllers/CDPController.cs                # expected 2

# AC-5 (D-10 item 5): NO throw + NO RollbackAsync di range DeleteCoachingSession L2433-2543
sed -n '2433,2543p' Controllers/CDPController.cs | grep -c "tx.RollbackAsync"     # expected 0
sed -n '2433,2543p' Controllers/CDPController.cs | grep -c "throw;"                # expected 0

# AC-6 (D-10 item 4): preserve INSIDE tx (revert state + RecordStatusHistory + audit log + SaveChanges)
grep -c "Reverted to Pending" Controllers/CDPController.cs                       # expected ≥1 (RecordStatusHistory preserved)
grep -c "Failed to parse EvidencePathHistory" Controllers/CDPController.cs       # expected 1 (JSON inner catch preserved)

# AC-7 (D-10 item 6): preserve OUTSIDE tx (role check + active mapping)
grep -c "Mapping Anda dengan coachee ini sudah tidak aktif" Controllers/CDPController.cs   # expected 1

# AC-8: preserve branches (Approved + sibling)
grep -c "sudah Approved — state & file dipertahankan" Controllers/CDPController.cs   # expected 1
grep -c "Sibling sessions masih ada" Controllers/CDPController.cs                    # expected 1

# AC-9: success TempData preserved
grep -c "Sesi coaching berhasil dihapus" Controllers/CDPController.cs            # expected 1
```

Semua harus menghasilkan match sesuai expected. Kalau ada mismatch → revisit Task 1 di line precision.

**[D] Manual smoke (D-10 item 9) — DEFERRED ke Dev promo (scenario #11)**

Per D-10 item 9: Manual smoke (delete CoachingSession solo dengan progress != Approved + EvidencePath + EvidencePathHistory parse + DB FK violation simulasi catch DbUpdateException hit) deferred ke Dev promo by IT. Tidak dilaksanakan di lokal Phase 333 — sufficient via grep AC mekanis + build + test pass. Catat di SUMMARY.md sebagai "Manual smoke: DEFERRED to Dev promo (D-10 item 9 explicit) — IT_NOTIFY scenario #11".

**[E] Append docs/IT_NOTIFY.md — per D-09 (bundle v19.0)**

Baca docs/IT_NOTIFY.md, append section berikut di bawah entry Phase 332:

```markdown
## Phase 333 — Fix Cascade DeleteCoachingSession File Atomicity (file post-commit + catch friendly)

**Commit:** [commit hash setelah git commit di langkah F]
**Date shipped local:** 2026-05-28
**Flag migration:** NO — zero schema change, zero migration, zero service injection

### Files Modified
- `Controllers/CDPController.cs` — DeleteCoachingSession (L2433-2543): `List<string>? pathsToDelete = null` dideklarasi di SCOPE OUTER tx (sebelum existing `await using var tx`), File.Delete loop dipindah dari INSIDE tx (L2490-2503 pre-CommitAsync) ke OUTSIDE tx (POST `CommitAsync`, sebelum `TempData["Success"]`) dengan inner try/catch warn-only per file (log distinct "CoachingSession evidence"), catch generic Exception+throw L2540 direfactor menjadi `catch (DbUpdateException dbEx)` (friendly TempData + RedirectToAction) + `catch (Exception ex)` fallback (friendly TempData + RedirectToAction), TIDAK pakai `throw;` TIDAK pakai eksplisit `tx.RollbackAsync()` (await using disposal auto-rollback). Role check + active mapping guard L2441-2453 + Status="Approved" branch L2521-2524 + sibling sessions branch L2526-2529 + JSON parse EvidencePathHistory L2478-2487 + progress revert state ~13 fields L2505-2517 + RecordStatusHistory L2518 + SaveChanges L2532 + audit log L2536 preserved verbatim.

### Closes Phase 328 Audit Findings
- §4.6 DeleteCoachingSession HIGH (D2 file-DB atomicity + D6 generic catch + throw raw 500) → CLOSED
- §9 proposal #4 (`fix-cascade-deletecoachingsession-file-atomicity`) → CLOSED

### Action Required by IT
Bundle v19.0 batch push (Phase 325+326+327+329+330+331+332+333) — tunggu user release push lock per Phase 327 option-b. Tidak ada migration DB. Tidak ada perubahan config. Tidak ada seed data.

### Scenario #11 — Manual Smoke @ Dev (deferred dari lokal per D-10 item 9)
1. **Skenario A — Solo session delete sukses dengan cleanup file:** Login sebagai Coach yang punya mapping aktif. Buat CoachingSession solo (no sibling) untuk ProtonDeliverableProgress yang statusnya Submitted/Pending dengan EvidencePath terisi + EvidencePathHistory JSON 2-3 entries. Klik delete. Expected: Session + ActionItems hapus, progress status revert ke "Pending" (SrSpv & Sh ApprovalStatus reset, RejectedAt null, EvidencePath/EvidenceFileName/EvidencePathHistory null), file evidence (current + history) terhapus dari disk wwwroot, audit log entry "DeleteCoachingSession" + cleanupNote "X file(s) cleaned", TempData success "Sesi coaching berhasil dihapus.", redirect ke Deliverable page progressId.
2. **Skenario B — Session dengan sibling (no cleanup):** Buat 2 CoachingSession untuk progress yang sama. Delete salah satu. Expected: hanya session yang dipilih + ActionItems hapus, progress state TIDAK ter-revert (cleanupNote " Sibling sessions masih ada — progress state dipertahankan."), file evidence di disk TIDAK terhapus, audit log entry tanpa "file(s) cleaned" text.
3. **Skenario C — Progress status Approved (no cleanup):** Buat session untuk progress yang status="Approved". Delete session. Expected: session hapus, progress state + file TIDAK disentuh (cleanupNote " Progress X sudah Approved — state & file dipertahankan."), file evidence tetap di disk.
4. **Skenario D — DB FK violation simulasi catch DbUpdateException hit:** Trigger constraint violation (mis. manual lock RowVersion conflict atau drop FK reference). Expected: catch DbUpdateException hit, TempData Error "Gagal hapus sesi coaching: ada constraint database yang dilanggar.", redirect ke Deliverable, log warning server-side dengan ex.Message + stack trace, tx auto-rollback (tidak ada record terhapus), file di disk TIDAK terhapus (loop POST commit tidak dieksekusi karena early return dari catch).
5. **Skenario E — Active mapping guard fail:** Login sebagai Coach yang mappingnya sudah IsActive=false. Coba delete session. Expected: TempData Error "Mapping Anda dengan coachee ini sudah tidak aktif...", redirect ke Deliverable, tidak ada tx creation (early return sebelum BeginTransactionAsync), tidak ada audit log.
```

**[F] Git commit (D-10 item 9)**

```bash
git add Controllers/CDPController.cs docs/IT_NOTIFY.md
git commit -m "feat(333): cascade atomicity DeleteCoachingSession (File.Delete post-commit + catch friendly)"
```

Setelah commit, catat commit hash → update entry IT_NOTIFY.md Phase 333 (ganti `[commit hash setelah git commit di langkah F]` dengan hash aktual, lalu `git add docs/IT_NOTIFY.md && git commit --amend --no-edit` atau commit terpisah `docs(333): record commit hash in IT_NOTIFY`).

**[G] Generate SUMMARY.md (D-10 item 11)**

Buat file `.planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-SUMMARY.md` mengikuti template `C:/Users/Administrator/.claude/get-shit-done/templates/summary.md`. Isi minimal:
- Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-09)
- Files modified table (Controllers/CDPController.cs ~40 LoC delta + docs/IT_NOTIFY.md)
- D-10 AC table 11/11 checklist (pathsToDelete outer + File.Delete post-commit + revert state preserve + catch refactor + no throw / no RollbackAsync + role check preserve + build + test + manual smoke deferred + commit + SUMMARY)
- Grep marker counts table:
  - `List<string>? pathsToDelete = null`: 1
  - `File.Delete post-commit failed (CoachingSession evidence)`: 1
  - `catch (DbUpdateException dbEx)`: ≥1
  - `DbUpdate failed for DeleteCoachingSession`: 1
  - `Gagal hapus sesi coaching`: 2
  - `tx.RollbackAsync` (range L2433-2543): 0
  - `throw;` (range L2433-2543): 0
  - `Failed to delete coaching session ID=`: 1 (Exception fallback LogError preserved)
  - `Reverted to Pending`: ≥1
  - `Failed to parse EvidencePathHistory`: 1
  - `Mapping Anda dengan coachee ini sudah tidak aktif`: 1
  - `sudah Approved — state & file dipertahankan`: 1
  - `Sibling sessions masih ada`: 1
  - `Sesi coaching berhasil dihapus`: 1
- Threat model disposition table 4/4 (T-333-01..04)
- Closes Phase 328 §4.6 + §9 proposal #4
- Manual smoke status: DEFERRED to Dev promo (per D-10 item 9 explicit, scenario #11 di IT_NOTIFY)
- Key difference catatan: existing BeginTransactionAsync L2455 dipreserve (TIDAK ditambah baru — beda dari Phase 331/332 yang menambahkan tx baru). Phase 333 = REORGANIZE existing tx + REORDER File.Delete + REFACTOR catch.
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331+332+333 ≈ 71 commit) menunggu user release push lock (Phase 327 option-b)
  </action>

  <verify>
    <automated>dotnet build 2>&1 | tail -5 && dotnet test --no-build 2>&1 | grep -E "passed|failed|Total"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build: 0 error (Build succeeded)
    - dotnet test --no-build: 18 passed, 0 failed (atau lebih jika ada test baru dari phase lain — tidak boleh ada regresi)
    - `grep -c "List<string>? pathsToDelete = null" Controllers/CDPController.cs` → 1
    - `grep -c "File.Delete post-commit failed (CoachingSession evidence)" Controllers/CDPController.cs` → 1
    - `grep -c "catch (DbUpdateException dbEx)" Controllers/CDPController.cs` → ≥1
    - `grep -c "DbUpdate failed for DeleteCoachingSession" Controllers/CDPController.cs` → 1
    - `grep -c "Gagal hapus sesi coaching" Controllers/CDPController.cs` → 2
    - `sed -n '2433,2543p' Controllers/CDPController.cs | grep -c "tx.RollbackAsync"` → 0
    - `sed -n '2433,2543p' Controllers/CDPController.cs | grep -c "throw;"` → 0
    - `grep -c "Reverted to Pending" Controllers/CDPController.cs` → ≥1 (RecordStatusHistory preserved)
    - `grep -c "Failed to parse EvidencePathHistory" Controllers/CDPController.cs` → 1 (JSON inner catch preserved)
    - `grep -c "Mapping Anda dengan coachee ini sudah tidak aktif" Controllers/CDPController.cs` → 1 (active mapping preserved)
    - `grep -c "sudah Approved — state & file dipertahankan" Controllers/CDPController.cs` → 1 (Approved branch preserved)
    - `grep -c "Sibling sessions masih ada" Controllers/CDPController.cs` → 1 (sibling branch preserved)
    - `grep -c "Sesi coaching berhasil dihapus" Controllers/CDPController.cs` → 1 (success TempData preserved)
    - Manual smoke status DEFERRED dicatat di SUMMARY.md (per D-10 item 9) + scenario #11 di IT_NOTIFY.md
    - git log --oneline -1 → commit message mengandung "feat(333): cascade atomicity DeleteCoachingSession"
    - docs/IT_NOTIFY.md mengandung section "Phase 333" dengan commit hash terisi + scenario #11 5 sub-skenario
    - 333-01-SUMMARY.md file ada di direktori phase dengan D-10 11/11 AC checklist
  </acceptance_criteria>

  <done>Build bersih, 18/18 test pass, semua 11 AC dari D-10 terverifikasi (grep mekanis 14 counter + range-scoped throw/RollbackAsync check + manual smoke deferred per explicit D-10 item 9), IT_NOTIFY.md diupdate Phase 333 entry + scenario #11 + commit hash terisi, commit dibuat dengan message per D-10 item 9, SUMMARY.md digenerate dengan status SHIPPED LOCAL + catatan key difference (existing tx reuse, beda dari Phase 331/332).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller | POST/redirect request dengan `[Authorize(Roles = UserRoles.RolesCoachAndAbove)]` aktif di DeleteCoachingSession endpoint + user challenge (Challenge) + null session check (NotFound) + role check Forbid |
| Controller → Database | EF Core DbUpdateException boundary — Phase 333 TAMBAH catch DbUpdateException spesifik (sebelumnya catch generic Exception + `throw;` → raw 500 dengan EF detail leak) + preserve existing BeginTransactionAsync L2455 untuk atomicity Remove(ActionItems) + Remove(CoachingSession) + progress revert state + RecordStatusHistory + SaveChanges + AuditLog |
| Controller → Filesystem | `_env.WebRootPath` + `relUrl` (EvidencePath + EvidencePathHistory entries) — sekarang dieksekusi POST commit dengan inner try/catch warn-only per file (log distinct "CoachingSession evidence"), cross-platform path normalization `Replace('/', Path.DirectorySeparatorChar)` preserved |
| Controller → AuditLog | `_auditLog.LogAsync` call yang preserve verbatim INSIDE tx scope SEBELUM CommitAsync — atomic dengan DB mutation (audit fail = tx escape = rollback DB; konsisten dengan existing pattern Phase 327 P05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-333-01 | D (Denial of Service) | DeleteCoachingSession file deletion (EvidencePath + EvidencePathHistory N entries) INSIDE tx pre-CommitAsync → orphan-delete bila SaveChanges/Commit fail | mitigate | Reorder File.Delete loop dari INSIDE tx (L2490-2503 pre-CommitAsync) ke OUTSIDE tx (POST `tx.CommitAsync()`, sebelum `TempData["Success"]`). Kalau SaveChanges/Commit fail, `pathsToDelete` masih null atau berisi paths tapi loop tidak dieksekusi karena early return dari catch block — semua file evidence masih ada di disk (no orphan-delete). Kalau File.Delete fail post-commit per file, inner try/catch log warning `File.Delete post-commit failed (CoachingSession evidence)` + iterate file berikutnya (DB sudah consistent, orphan-on-disk acceptable manual cleanup). |
| T-333-02 | T (Atomicity violation, mapped to T - Tampering) | DeleteCoachingSession partial state — file evidence gone dari disk tetapi progress.EvidencePath/EvidencePathHistory tidak ter-null + progress.Status tidak ter-revert ke "Pending" (mismatch DB state vs filesystem) | mitigate | `pathsToDelete` declared di SCOPE OUTER tx sebagai `List<string>?` (null initial) — visible POST CommitAsync. File deletion DEFERRED sampai setelah `tx.CommitAsync()` sukses. Kalau tx fail (DbUpdateException atau Exception lain), `await using` disposal auto-rollback DB + early return dari catch block (loop POST commit tidak dieksekusi). Tidak ada window partial-state: DB consistent OR rollback, dan file di disk hanya terhapus kalau DB sudah commit sukses (progress revert state + RecordStatusHistory + AuditLog semua sudah persisted). |
| T-333-03 | R (Repudiation) | Catch generic Exception + `throw;` L2540 → raw 500 page hides audit trail context (LogError minimal + stack unwind ke middleware default error handler — no structured warn vs error distinction, no contextual TempData) | mitigate | Catch refactor menjadi 2-tier: `catch (DbUpdateException dbEx)` SPECIFIC dengan `_logger.LogWarning(dbEx, "DbUpdate failed for DeleteCoachingSession ID={Id}", id)` — warn level untuk constraint violation (expected operational case, bukan critical). `catch (Exception ex)` FALLBACK dengan `_logger.LogError(ex, "Failed to delete coaching session ID={Id}", id)` — error level untuk unexpected case. Distinct log levels = distinct disposition di alerting. TempData friendly per kategori catch = user dapat context ("constraint database dilanggar" vs "kesalahan internal"). Audit trail server-side preserved with `id` field + exception detail. |
| T-333-04 | I (Information Disclosure) | Raw exception via `throw;` di L2540 → middleware default error handler kirim 500 page dengan ex.Message + StackTrace ke browser (mis. SQL constraint name `FK_CoachingSessions_*` + table name leak ke developer console / page source) | mitigate | Replace `throw;` dengan `return RedirectToAction("Deliverable", new { id = progressId });` + `TempData["Error"] = "..."` generic friendly. Exception detail (`ex.Message`, `ex.StackTrace`, `ex.InnerException`) hanya di server log via `_logger.LogWarning`/`_logger.LogError`, TIDAK pernah dikirim ke client. User dapat TempData generic message ("Gagal hapus sesi coaching: ada constraint database yang dilanggar." atau "Gagal hapus sesi coaching: terjadi kesalahan internal. Hubungi admin.") tanpa technical leak. |
</threat_model>

<verification>
Verifikasi akhir keseluruhan Phase 333:

1. **Build clean:** `dotnet build` → 0 error
2. **Test suite:** `dotnet test --no-build` → 18/18 pass (baseline Phase 332, no regression)
3. **Grep AC mekanis (14 counter):**
   - `grep -c "List<string>? pathsToDelete = null" Controllers/CDPController.cs` → 1
   - `grep -c "File.Delete post-commit failed (CoachingSession evidence)" Controllers/CDPController.cs` → 1
   - `grep -c "catch (DbUpdateException dbEx)" Controllers/CDPController.cs` → ≥1
   - `grep -c "DbUpdate failed for DeleteCoachingSession" Controllers/CDPController.cs` → 1
   - `grep -c "Gagal hapus sesi coaching" Controllers/CDPController.cs` → 2
   - `sed -n '2433,2543p' Controllers/CDPController.cs | grep -c "tx.RollbackAsync"` → 0
   - `sed -n '2433,2543p' Controllers/CDPController.cs | grep -c "throw;"` → 0
   - `grep -c "Failed to delete coaching session ID=" Controllers/CDPController.cs` → 1
   - `grep -c "Reverted to Pending" Controllers/CDPController.cs` → ≥1
   - `grep -c "Failed to parse EvidencePathHistory" Controllers/CDPController.cs` → 1
   - `grep -c "Mapping Anda dengan coachee ini sudah tidak aktif" Controllers/CDPController.cs` → 1
   - `grep -c "sudah Approved — state & file dipertahankan" Controllers/CDPController.cs` → 1
   - `grep -c "Sibling sessions masih ada" Controllers/CDPController.cs` → 1
   - `grep -c "Sesi coaching berhasil dihapus" Controllers/CDPController.cs` → 1
4. **Manual smoke:** DEFERRED to Dev promo (per D-10 item 9 explicit, scenario #11 5 sub-skenario di IT_NOTIFY)
5. **IT_NOTIFY:** `grep "Phase 333" docs/IT_NOTIFY.md` → 1 section dengan commit hash terisi + scenario #11
6. **Commit:** `git log --oneline -1` → mengandung "feat(333): cascade atomicity DeleteCoachingSession"
7. **SUMMARY:** `ls .planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-SUMMARY.md` → file ada
</verification>

<success_criteria>
Phase 333 selesai ketika:
- [ ] DeleteCoachingSession L2433-2543 di-refactor sesuai D-02 + D-03 + D-04 + D-05 (pathsToDelete outer scope + File.Delete POST commit + catch DbUpdateException + Exception fallback friendly)
- [ ] Existing BeginTransactionAsync L2455 dipreserve (TIDAK ditambah baru — key difference dari Phase 331/332)
- [ ] Role check L2432 + user/session null check L2435-2440 + role check + active mapping guard L2441-2453 preserved verbatim OUTSIDE tx
- [ ] Progress revert state ~13 fields L2505-2517 + RecordStatusHistory L2518 + SaveChanges L2532 + audit log L2536 + JSON parse EvidencePathHistory L2478-2487 preserved verbatim INSIDE tx
- [ ] Status="Approved" branch L2521-2524 + sibling sessions branch L2526-2529 preserved verbatim (no File.Delete per D-07/D-08)
- [ ] Catch generic Exception+throw L2540 direfactor jadi catch DbUpdateException friendly + catch Exception fallback friendly, NO `throw;`, NO eksplisit `tx.RollbackAsync()` (await using disposal handles it)
- [ ] `dotnet build` clean (0 error CS*)
- [ ] `dotnet test --no-build` 18/18 pass (no regression Phase 332 baseline)
- [ ] Manual smoke: DEFERRED to Dev promo (per D-10 item 9 — tidak blocking ship lokal)
- [ ] Grep AC mekanis 14 counter terpenuhi (termasuk range-scoped check L2433-2543 untuk `tx.RollbackAsync` + `throw;` = 0)
- [ ] docs/IT_NOTIFY.md diupdate dengan Phase 333 entry + commit hash terisi + scenario #11 5 sub-skenario
- [ ] 1 commit dengan message `feat(333): cascade atomicity DeleteCoachingSession (File.Delete post-commit + catch friendly)`
- [ ] 333-01-SUMMARY.md digenerate
- [ ] Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-09)
</success_criteria>

<output>
Setelah Task 2 selesai, buat `.planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-SUMMARY.md` dengan isi:
- Status: SHIPPED LOCAL
- Files modified (Controllers/CDPController.cs ~40 LoC delta + docs/IT_NOTIFY.md)
- AC D-10 table 11/11 checklist
- Grep marker count verification (14 counter — pathsToDelete outer, File.Delete post-commit log, catch DbUpdateException, LogWarning DbUpdate, Gagal hapus sesi coaching x2, range-scoped tx.RollbackAsync=0 + throw;=0, LogError Exception fallback, Reverted to Pending, JSON parse inner catch, active mapping, Approved branch, sibling branch, success TempData)
- Manual smoke status: DEFERRED to Dev promo (D-10 item 9 explicit, scenario #11 di IT_NOTIFY)
- Threat model disposition 4/4 (T-333-01..04)
- Closes Phase 328 §4.6 + §9 proposal #4
- Key difference catatan: existing BeginTransactionAsync L2455 dipreserve (TIDAK ditambah baru — beda dari Phase 331/332)
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331+332+333 ≈ 71 commit) menunggu user release push lock (Phase 327 option-b)
</output>
