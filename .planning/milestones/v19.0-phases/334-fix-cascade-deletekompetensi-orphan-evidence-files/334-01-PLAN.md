---
phase: 334-fix-cascade-deletekompetensi-orphan-evidence-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/ProtonDataController.cs
  - docs/IT_NOTIFY.md
autonomous: true
requirements:
  - D-01-DeleteKompetensi-orphan-files
  - D-02-DeleteKompetensi-catch-no-info-leak
must_haves:
  truths:
    - "DeleteKompetensi: evidencePaths dideklarasi sebagai List<string>? = null di SCOPE OUTER tx (sebelum using var transaction L1529) sehingga visible POST CommitAsync L1576 untuk File.Delete loop"
    - "DeleteKompetensi: evidencePaths dipopulasi INSIDE tx (loop progresses + JSON parse EvidencePathHistory inner try/catch warn-only) SEBELUM RemoveRange(progresses) L1559 — progress object detach post-Remove"
    - "DeleteKompetensi: 5-step cascade order L1542-1574 preserved verbatim (CoachingSessions → ProtonDeliverableProgresses → ProtonDeliverableList → ProtonSubKompetensiList → ProtonKompetensiList)"
    - "DeleteKompetensi: await transaction.CommitAsync() L1576 + audit log L1578-1580 POST commit preserved verbatim (D7 positioning out-of-scope per Phase 328 §4.8 D2+D6 only)"
    - "DeleteKompetensi: System.IO.File.Delete loop POST audit log (urutan: CommitAsync → audit log → File.Delete → return Json success), OUTSIDE tx scope, inner try/catch warn-only per file dengan log distinct '(Kompetensi evidence)'"
    - "DeleteKompetensi: catch generic Exception + 'Gagal menghapus: ' + ex.Message direfactor → catch DbUpdateException (LogWarning + generic Json) + catch Exception fallback (LogError + generic Json) — D6 info leak fix HAPUS ex.Message dari client response"
    - "DeleteKompetensi: TIDAK ada eksplisit await transaction.RollbackAsync() di catch blocks (using var transaction synchronous disposal auto-rollback on exception escape)"
    - "DeleteKompetensi: TIDAK ada throw; di catch blocks (replaced dengan return Json friendly)"
    - "DeleteKompetensi: existing using var transaction L1529 dipreserve (TIDAK diganti ke await using — synchronous disposal pattern intentional preserved)"
    - "DeleteKompetensi: Include(SubKompetensiList).ThenInclude(Deliverables) L1521-1524 + null check komp L1526-1527 + deliverableIds collection L1532-1535 + progressIds collection L1537-1540 + CoachingSessions RemoveRange L1542-1551 preserved verbatim"
    - "dotnet build clean, dotnet test --no-build 18/18 pass — tidak ada regresi dari Phase 333 baseline"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "DeleteKompetensi L1516-1590 refactored: evidencePaths outer scope nullable + populate inside tx (between ToListAsync L1558 dan RemoveRange L1559) + File.Delete loop POST audit log + catch DbUpdateException friendly + catch Exception fallback friendly (NO ex.Message leak)"
      contains: "List<string>? evidencePaths = null"
    - path: "docs/IT_NOTIFY.md"
      provides: "Phase 334 IT notification entry (bundle v19.0 append) + smoke scenario #12"
      contains: "Phase 334"
  key_links:
    - from: "Controllers/ProtonDataController.cs"
      to: "DeleteKompetensi L1516-1590"
      via: "List<string>? evidencePaths = null (outer); using var transaction; ...; collect evidencePaths sebelum RemoveRange(progresses) L1559; ...; await transaction.CommitAsync(); audit log; foreach File.Delete; return Json success"
      pattern: "File\\.Delete post-commit failed \\(Kompetensi evidence\\)"
    - from: "Controllers/ProtonDataController.cs"
      to: "Catch DbUpdateException + Catch Exception fallback baru (NO ex.Message)"
      via: "catch (DbUpdateException dbEx) → Json generic 'constraint database dilanggar'; catch (Exception ex) → Json generic 'kesalahan internal'; no throw, no transaction.RollbackAsync, no + ex.Message"
      pattern: "Gagal hapus kompetensi"
    - from: "Controllers/ProtonDataController.cs"
      to: "Audit log + File.Delete order POST commit"
      via: "transaction.CommitAsync L1576 → _auditLog.LogAsync L1578-1580 (preserved verbatim) → foreach evidencePaths File.Delete (new) → return Json success"
      pattern: "Hard-deleted kompetensi"
---

<objective>
Tutup 1 HIGH finding Phase 328 Cascade Audit Sweep §4.8 + §9 proposal #5: orphan EvidencePath files (D2) + info leak `ex.Message` ke client (D6) di `DeleteKompetensi`. Strategi mengikuti Phase 333 D-02..D-05 verbatim reuse (outer-scope nullable list + INSIDE tx collection + File.Delete POST commit + catch friendly no info leak) dengan adaptasi: existing `using var transaction` (synchronous disposal) di L1529 dipreserve (TIDAK diganti ke `await using`), audit log L1578-1580 SAAT INI sudah POST CommitAsync dan TIDAK dipindah (D3 positioning out-of-scope per Phase 328 §4.8 yang scope D2+D6 only), response Json (AJAX-only) bukan TempData+Redirect. Perubahan ini bersifat mechanical controller-only — tidak ada migration, tidak ada schema change, tidak ada service injection, tidak ada view change.

**KEY DIFFERENCE dari Phase 333:**
1. Existing `using var transaction` (synchronous disposal) di L1529 — Phase 333 pakai `await using var tx` (asynchronous disposal). Sama-sama auto-rollback on exception escape. Phase 334 preserve existing pattern.
2. Audit log L1578-1580 SAAT INI POST CommitAsync (Phase 333 audit log INSIDE tx pre-commit). Phase 334 TIDAK pindah audit log ke INSIDE tx — out of scope D3 per Phase 328 §4.8.
3. Response Json `{ success = false, message = "..." }` bukan TempData+Redirect — friendly message wrapping Json error.
4. **CRITICAL D6**: `ex.Message` leak via `"Gagal menghapus: " + ex.Message` → DB constraint detail / table names / exception type BOCOR ke client. Must remove from client response.
5. Multi-level nested cascade (SubKompetensi → Deliverables → Progresses → CoachingSessions) — collection evidencePaths happens INSIDE tx loop progresses ANTARA `ToListAsync()` L1558 dan `RemoveRange(progresses)` L1559.

Purpose: Menghilangkan orphan disk files (file evidence tetap di disk meskipun parent Kompetensi + descendant progresses sudah ter-delete dari DB) + menghilangkan information disclosure dari `ex.Message` ke browser (DB constraint detail / table names leak via Json response). Atomicity sudah dijamin existing tx; Phase 334 = COLLECT evidence paths INSIDE tx + DEFER File.Delete POST commit + REFACTOR catch friendly no info leak.

Output: 1 file controller dimodifikasi (~40 LoC delta, 1 endpoint), `docs/IT_NOTIFY.md` diupdate dengan Phase 334 entry + scenario #12, 1 commit bertag pesan standar D-09 item 10, SUMMARY.md digenerate.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-CONTEXT.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
@.planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-PLAN.md
@.planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-SUMMARY.md

<interfaces>
<!-- Kontrak yang digunakan executor — tidak perlu eksplorasi codebase lebih jauh. -->

Dari Controllers/ProtonDataController.cs (base ApplicationDbContext + DI yang sudah tersedia):
```csharp
private readonly ApplicationDbContext _context;
private readonly UserManager<ApplicationUser> _userManager;
private readonly AuditLogService _auditLog;
private readonly IWebHostEnvironment _env;
private readonly ILogger<ProtonDataController> _logger;
```

Dari Microsoft.EntityFrameworkCore (sudah imported di ProtonDataController.cs):
```csharp
DbContext.Database.BeginTransactionAsync();   // returns IDbContextTransaction
IDbContextTransaction.CommitAsync();
// using var transaction = ...; → synchronous disposal otomatis rollback jika exception escape sebelum CommitAsync (NO eksplisit RollbackAsync needed)
DbUpdateException;   // catch FIRST untuk constraint violation friendly
```

AuditLog signature verbatim L1578-1580 (PRESERVE POST CommitAsync — D7 out of scope):
```csharp
await _auditLog.LogAsync(user.Id, user.FullName ?? user.UserName ?? user.Id, "Delete",
    $"Hard-deleted kompetensi '{komp.NamaKompetensi}' (ID {komp.Id}) with all descendants",
    targetId: komp.Id, targetType: "ProtonKompetensi");
```

Entity properties yang dipakai (preserved):
```csharp
ProtonDeliverableProgress.Id (int)
ProtonDeliverableProgress.ProtonDeliverableId (int)
ProtonDeliverableProgress.EvidencePath (string?)
ProtonDeliverableProgress.EvidencePathHistory (string? JSON List<string>)
ProtonKompetensi.SubKompetensiList (ICollection<ProtonSubKompetensi>)
ProtonSubKompetensi.Deliverables (ICollection<ProtonDeliverable>)
```

State kode aktual sebelum fix (verbatim dari Controllers/ProtonDataController.cs L1516-1590):

**DeleteKompetensi L1516-1590** (target Task 1):
- L1514-1515: `[HttpPost]` + `[ValidateAntiForgeryToken]` attribute — preserve.
- L1516: signature `public async Task<IActionResult> DeleteKompetensi([FromBody] KompetensiDeleteRequest req)` — preserve.
- L1518-1519: `var user = await _userManager.GetUserAsync(User);` + `if (user == null) return Challenge();` — preserve.
- L1521-1524: `var komp = await _context.ProtonKompetensiList.Include(k => k.SubKompetensiList).ThenInclude(s => s.Deliverables).FirstOrDefaultAsync(k => k.Id == req.KompetensiId);` — preserve verbatim.
- L1526-1527: `if (komp == null) return Json(new { success = false, message = "Kompetensi tidak ditemukan." });` — preserve.
- L1529: `using var transaction = await _context.Database.BeginTransactionAsync();` (PRESERVE — synchronous disposal, TIDAK diganti ke await using).
- L1530: `try {` (preserve).
- L1532-1535: deliverableIds collection (`SelectMany s.Deliverables.Select d.Id.ToList()`) — preserve verbatim INSIDE tx.
- L1537-1540: progressIds collection (`Where deliverableIds.Contains(p.ProtonDeliverableId).Select(p.Id).ToListAsync()`) — preserve verbatim INSIDE tx.
- L1542-1551: Step 1 CoachingSessions RemoveRange (Include ActionItems, Where ProtonDeliverableProgressId.HasValue && progressIds.Contains) — preserve verbatim INSIDE tx.
- L1553-1561: Step 2 ProtonDeliverableProgresses load + RemoveRange — **REFACTOR**: INSERT evidencePaths allocation + populate block ANTARA `.ToListAsync()` L1558 dan `RemoveRange(progresses)` L1559.
- L1563-1566: Step 3 Deliverables RemoveRange — preserve verbatim INSIDE tx.
- L1568-1570: Step 4 SubKompetensi RemoveRange — preserve verbatim INSIDE tx.
- L1572-1574: Step 5 Kompetensi Remove — preserve verbatim INSIDE tx.
- L1576: `await transaction.CommitAsync();` — preserve verbatim.
- L1578-1580: audit log POST CommitAsync — preserve verbatim (D7 positioning out of scope).
- L1582: `return Json(new { success = true });` — preserve POSISI: setelah File.Delete loop POST audit log.
- L1584-1589 catch block:
  ```csharp
  catch (Exception ex)
  {
      await transaction.RollbackAsync();
      _logger.LogError(ex, "Failed to delete Kompetensi {Id}", req.KompetensiId);
      return Json(new { success = false, message = "Gagal menghapus: " + ex.Message });
  }
  ```
  ← **REFACTOR SELURUHNYA** menjadi 2 catch blocks (DbUpdateException + Exception fallback) friendly Json NO ex.Message NO RollbackAsync NO throw.
- L1590: closing brace method — preserve.

Pattern yang sudah benar (preserve verbatim):
- `using var transaction` disposal — auto-rollback synchronous jika exception escape sebelum CommitAsync (NO explicit RollbackAsync needed)
- 5-step cascade order FK Restrict-safe (CoachingSessions → ProtonDeliverableProgresses → ProtonDeliverableList → ProtonSubKompetensiList → ProtonKompetensiList)
- Audit log POST CommitAsync L1578-1580 — D7 positioning out of scope per Phase 328 §4.8 (D2+D6 only)
- Include preload graph L1521-1524 (Kompetensi → SubKompetensi → Deliverables)

Method response patterns:
- Existing returns: Challenge (no user) + Json(success=false, "Kompetensi tidak ditemukan") (no komp) + Json(success=true) (sukses)
- **NEW** DbUpdateException catch: `return Json(new { success = false, message = "Gagal hapus kompetensi: ada constraint database yang dilanggar." });`
- **NEW** Exception fallback catch: `return Json(new { success = false, message = "Gagal hapus kompetensi: terjadi kesalahan internal. Hubungi admin." });`
- **REMOVE**: `"Gagal menghapus: " + ex.Message` — D6 info leak fix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor DeleteKompetensi — evidencePaths outer tx scope, populate INSIDE tx sebelum RemoveRange progresses, File.Delete loop POST audit log, catch refactor friendly NO ex.Message leak</name>
  <files>Controllers/ProtonDataController.cs</files>

  <read_first>
    1. Controllers/ProtonDataController.cs L1513-1592 (DeleteKompetensi — baca state saat ini lengkap, termasuk attribute + signature + null check + try/catch block)
    2. .planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-CONTEXT.md (D-02 evidencePaths outer scope, D-03 JSON parse Phase 333 reuse, D-04 reorganize cascade collect SEBELUM RemoveRange, D-05 File.Delete POST audit log, D-06 catch refactor NO ex.Message, D-07 audit log position preserve, D-09 acceptance criteria 12 item, D-10 plan structure 2 task)
    3. .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md §4.8 (DeleteKompetensi HIGH D2 orphan EvidencePath + D6 info leak ex.Message)
    4. .planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-PLAN.md (pattern Phase 333 D-02..D-05 verbatim reuse — JSON parse, File.Delete POST commit, catch friendly, outer scope nullable list)
  </read_first>

  <action>
**Refactor `DeleteKompetensi` (L1516-1590) — per D-02 + D-03 + D-04 + D-05 + D-06 + D-07**

Ganti body method DeleteKompetensi dari L1516 (setelah signature brace) sampai L1590 (closing brace method) dengan struktur target di bawah. Reuse semua kode existing verbatim — hanya:
1. TAMBAH `List<string>? evidencePaths = null;` SEBELUM `using var transaction` L1529
2. INSERT evidencePaths allocation + populate block ANTARA `.ToListAsync()` L1558 dan `RemoveRange(progresses)` L1559
3. INSERT File.Delete loop SETELAH audit log L1580, SEBELUM `return Json(new { success = true });` L1582
4. REFACTOR catch L1584-1589: split jadi catch DbUpdateException + catch Exception fallback. HAPUS `await transaction.RollbackAsync()`. HAPUS `+ ex.Message`. NO throw.

**Bagian PRESERVE verbatim L1514-1527 (attribute + signature + user + komp load + null check):**
```csharp
        // POST: /ProtonData/DeleteKompetensi — hard delete with full cascade
        [HttpPost]
        [ValidateAntiForgeryToken]
        public async Task<IActionResult> DeleteKompetensi([FromBody] KompetensiDeleteRequest req)
        {
            var user = await _userManager.GetUserAsync(User);
            if (user == null) return Challenge();

            var komp = await _context.ProtonKompetensiList
                .Include(k => k.SubKompetensiList)
                    .ThenInclude(s => s.Deliverables)
                .FirstOrDefaultAsync(k => k.Id == req.KompetensiId);

            if (komp == null)
                return Json(new { success = false, message = "Kompetensi tidak ditemukan." });
```

**Bagian REFACTOR L1528-1590 — ganti dengan struktur baru ini:**
```csharp
            // Phase 334 D-02: declare evidencePaths di SCOPE OUTER tx — visible POST CommitAsync untuk File.Delete loop
            List<string>? evidencePaths = null;

            using var transaction = await _context.Database.BeginTransactionAsync();
            try
            {
                var deliverableIds = komp.SubKompetensiList
                    .SelectMany(s => s.Deliverables)
                    .Select(d => d.Id)
                    .ToList();

                var progressIds = await _context.ProtonDeliverableProgresses
                    .Where(p => deliverableIds.Contains(p.ProtonDeliverableId))
                    .Select(p => p.Id)
                    .ToListAsync();

                // 1. Delete CoachingSessions (no FK constraint, must be explicit; ActionItems cascade automatically)
                if (progressIds.Any())
                {
                    var sessions = await _context.CoachingSessions
                        .Include(cs => cs.ActionItems)
                        .Where(cs => cs.ProtonDeliverableProgressId != null && progressIds.Contains(cs.ProtonDeliverableProgressId!.Value))
                        .ToListAsync();
                    _context.CoachingSessions.RemoveRange(sessions);
                    await _context.SaveChangesAsync();
                }

                // 2. Delete ProtonDeliverableProgresses
                if (progressIds.Any())
                {
                    var progresses = await _context.ProtonDeliverableProgresses
                        .Where(p => progressIds.Contains(p.Id))
                        .ToListAsync();

                    // Phase 334 D-02 + D-03: collect evidence paths SEBELUM RemoveRange (progress object detach post-Remove)
                    evidencePaths = new List<string>();
                    foreach (var p in progresses)
                    {
                        if (!string.IsNullOrEmpty(p.EvidencePath))
                            evidencePaths.Add(p.EvidencePath);
                        if (!string.IsNullOrEmpty(p.EvidencePathHistory))
                        {
                            try
                            {
                                var history = System.Text.Json.JsonSerializer
                                    .Deserialize<List<string>>(p.EvidencePathHistory) ?? new List<string>();
                                evidencePaths.AddRange(history);
                            }
                            catch (Exception jex)
                            {
                                _logger.LogWarning(jex, "Failed to parse EvidencePathHistory for progress {Pid}", p.Id);
                            }
                        }
                    }

                    _context.ProtonDeliverableProgresses.RemoveRange(progresses);
                    await _context.SaveChangesAsync();
                }

                // 3. Delete Deliverables
                var deliverables = komp.SubKompetensiList.SelectMany(s => s.Deliverables).ToList();
                _context.ProtonDeliverableList.RemoveRange(deliverables);
                await _context.SaveChangesAsync();

                // 4. Delete SubKompetensi
                _context.ProtonSubKompetensiList.RemoveRange(komp.SubKompetensiList);
                await _context.SaveChangesAsync();

                // 5. Delete Kompetensi
                _context.ProtonKompetensiList.Remove(komp);
                await _context.SaveChangesAsync();

                await transaction.CommitAsync();

                await _auditLog.LogAsync(user.Id, user.FullName ?? user.UserName ?? user.Id, "Delete",
                    $"Hard-deleted kompetensi '{komp.NamaKompetensi}' (ID {komp.Id}) with all descendants",
                    targetId: komp.Id, targetType: "ProtonKompetensi");

                // Phase 334 D-05: File.Delete loop POST audit log — inner try/catch warn-only per file
                if (evidencePaths != null && evidencePaths.Count > 0)
                {
                    foreach (var relUrl in evidencePaths)
                    {
                        try
                        {
                            var physical = Path.Combine(_env.WebRootPath,
                                relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
                            if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
                        }
                        catch (Exception fex)
                        {
                            _logger.LogWarning(fex, "File.Delete post-commit failed (Kompetensi evidence): {Path}", relUrl);
                        }
                    }
                }

                return Json(new { success = true });
            }
            catch (DbUpdateException dbEx)
            {
                // Phase 334 D-06: using var transaction disposal auto-rollback — no explicit RollbackAsync
                _logger.LogWarning(dbEx, "DbUpdate failed for DeleteKompetensi {Id}", req.KompetensiId);
                return Json(new { success = false, message = "Gagal hapus kompetensi: ada constraint database yang dilanggar." });
            }
            catch (Exception ex)
            {
                // Phase 334 D-06: fallback generic — NO + ex.Message di response (D6 info leak fix), NO explicit RollbackAsync
                _logger.LogError(ex, "Failed to delete Kompetensi {Id}", req.KompetensiId);
                return Json(new { success = false, message = "Gagal hapus kompetensi: terjadi kesalahan internal. Hubungi admin." });
            }
        }
```

**Catatan presisi (per CONTEXT D-02..D-07):**
- L1513 `// POST: /ProtonData/DeleteKompetensi — hard delete with full cascade` komentar — preserve.
- L1514-1515 `[HttpPost]` + `[ValidateAntiForgeryToken]` attribute — preserve verbatim, **TIDAK disentuh**.
- L1516 signature `public async Task<IActionResult> DeleteKompetensi([FromBody] KompetensiDeleteRequest req)` — preserve verbatim.
- L1518-1519 user load + Challenge — preserve verbatim.
- L1521-1524 komp Include ThenInclude FirstOrDefault — preserve verbatim.
- L1526-1527 null check komp + Json error message "Kompetensi tidak ditemukan." — preserve verbatim.
- **TAMBAH** baris baru SEBELUM `using var transaction`: `List<string>? evidencePaths = null;` (nullable initial null, allocate inside Step 2 conditional branch).
- L1529 `using var transaction = await _context.Database.BeginTransactionAsync();` — **preserve verbatim** (SUDAH ADA, synchronous disposal TIDAK diganti ke `await using`).
- L1530 `try {` — preserve.
- L1532-1540 deliverableIds + progressIds collection — preserve verbatim INSIDE tx.
- L1542-1551 Step 1 CoachingSessions RemoveRange — preserve verbatim INSIDE tx.
- L1553-1561 Step 2 ProtonDeliverableProgresses — **REFACTOR**:
  - L1553-1557 conditional `if (progressIds.Any())` + load `var progresses = ... .ToListAsync();` — preserve verbatim.
  - **INSERT** ANTARA L1558 `.ToListAsync()` dan L1559 `RemoveRange(progresses)`:
    ```csharp
    // Phase 334 D-02 + D-03: collect evidence paths SEBELUM RemoveRange (progress object detach post-Remove)
    evidencePaths = new List<string>();
    foreach (var p in progresses)
    {
        if (!string.IsNullOrEmpty(p.EvidencePath))
            evidencePaths.Add(p.EvidencePath);
        if (!string.IsNullOrEmpty(p.EvidencePathHistory))
        {
            try
            {
                var history = System.Text.Json.JsonSerializer
                    .Deserialize<List<string>>(p.EvidencePathHistory) ?? new List<string>();
                evidencePaths.AddRange(history);
            }
            catch (Exception jex)
            {
                _logger.LogWarning(jex, "Failed to parse EvidencePathHistory for progress {Pid}", p.Id);
            }
        }
    }
    ```
    Catatan: `evidencePaths = new List<string>();` (reassign outer var, hapus `var` keyword agar tidak shadow outer declaration).
  - L1559-1560 `RemoveRange(progresses)` + `SaveChangesAsync` — preserve verbatim.
- L1563-1566 Step 3 Deliverables RemoveRange — preserve verbatim INSIDE tx.
- L1568-1570 Step 4 SubKompetensi RemoveRange — preserve verbatim INSIDE tx.
- L1572-1574 Step 5 Kompetensi Remove — preserve verbatim INSIDE tx.
- L1576 `await transaction.CommitAsync();` — preserve verbatim.
- L1578-1580 audit log `await _auditLog.LogAsync(...)` — preserve verbatim POST commit (D7 positioning out of scope per Phase 328 §4.8 D2+D6 only).
- **INSERT** File.Delete loop SETELAH audit log L1580, SEBELUM `return Json(new { success = true });` L1582:
  ```csharp
  // Phase 334 D-05: File.Delete loop POST audit log — inner try/catch warn-only per file
  if (evidencePaths != null && evidencePaths.Count > 0)
  {
      foreach (var relUrl in evidencePaths)
      {
          try
          {
              var physical = Path.Combine(_env.WebRootPath,
                  relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
              if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
          }
          catch (Exception fex)
          {
              _logger.LogWarning(fex, "File.Delete post-commit failed (Kompetensi evidence): {Path}", relUrl);
          }
      }
  }
  ```
  Inner catch `Exception` (bukan `IOException` specific) — konsisten Phase 331/332/333 D-05.
  Cross-platform path normalization `Replace('/', Path.DirectorySeparatorChar)` Phase 333 pattern.
- L1582 `return Json(new { success = true });` — preserve verbatim, **POSISI BARU**: SETELAH File.Delete loop POST audit log.
- L1584-1589 catch generic Exception block → **REFACTOR SELURUHNYA** menjadi 2 catch blocks:
  - `catch (DbUpdateException dbEx)` FIRST (specific): `_logger.LogWarning(dbEx, "DbUpdate failed for DeleteKompetensi {Id}", req.KompetensiId);`, `return Json(new { success = false, message = "Gagal hapus kompetensi: ada constraint database yang dilanggar." });`
  - `catch (Exception ex)` SECOND (general fallback): `_logger.LogError(ex, "Failed to delete Kompetensi {Id}", req.KompetensiId);`, `return Json(new { success = false, message = "Gagal hapus kompetensi: terjadi kesalahan internal. Hubungi admin." });`
  - **TIDAK ada** `await transaction.RollbackAsync();` di kedua catch (using var synchronous disposal auto-rollback).
  - **TIDAK ada** `throw;` di kedua catch (replaced dengan return Json).
  - **TIDAK ada** `+ ex.Message` di Json response message (D6 info leak fix CRITICAL).
- L1590 closing brace method `}` — preserve verbatim.
- **TIDAK ada import baru** — `Microsoft.EntityFrameworkCore` (DbUpdateException + BeginTransactionAsync), `System.IO.File`, `Path`, `System.Text.Json` semua sudah tersedia di file (DeleteKompetensi pre-existing pakai EF Core + Path methods di method lain pakai System.IO.File).
- **TIDAK ada perubahan ke method lain** dalam ProtonDataController.cs.
  </action>

  <verify>
    <automated>grep -c "List<string>? evidencePaths = null" Controllers/ProtonDataController.cs && grep -c "File.Delete post-commit failed (Kompetensi evidence)" Controllers/ProtonDataController.cs && grep -c "Gagal hapus kompetensi" Controllers/ProtonDataController.cs && grep -c "catch (DbUpdateException dbEx)" Controllers/ProtonDataController.cs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "List<string>? evidencePaths = null" Controllers/ProtonDataController.cs` → 1 match (outer var declaration baru)
    - `grep -c "File.Delete post-commit failed (Kompetensi evidence)" Controllers/ProtonDataController.cs` → 1 match (new post-commit log distinct)
    - `grep -c "catch (DbUpdateException dbEx)" Controllers/ProtonDataController.cs` → 1 match (catch baru spesifik di DeleteKompetensi)
    - `grep -c "Gagal hapus kompetensi" Controllers/ProtonDataController.cs` → 2 match (2 catch blocks friendly Json)
    - `grep -c "+ ex.Message" Controllers/ProtonDataController.cs` → 0 match (REMOVED — D6 info leak fix)
    - `grep -c "transaction.RollbackAsync" Controllers/ProtonDataController.cs` → 0 match (REMOVED — using var synchronous disposal handles it)
    - `grep -c "Failed to delete Kompetensi" Controllers/ProtonDataController.cs` → 1 match (LogError Exception fallback preserved)
    - `grep -c "DbUpdate failed for DeleteKompetensi" Controllers/ProtonDataController.cs` → 1 match (LogWarning DbUpdateException baru)
    - `grep -c "Failed to parse EvidencePathHistory" Controllers/ProtonDataController.cs` → 1 match (JSON parse inner catch baru — kemungkinan 1 ATAU 2 jika method lain juga punya pattern ini; minimal 1)
    - `grep -c "Hard-deleted kompetensi" Controllers/ProtonDataController.cs` → 1 match (audit log message preserved verbatim)
    - `grep -c "Kompetensi tidak ditemukan" Controllers/ProtonDataController.cs` → 1 match (null check Json preserved)
    - `sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c "throw;"` → 0 (no throw in DeleteKompetensi range)
    - File DeleteKompetensi menjaga semua jalur response existing (Challenge + Json "Kompetensi tidak ditemukan" + Json success=true) PLUS 2 jalur baru (DbUpdateException catch Json friendly + Exception fallback catch Json friendly)
  </acceptance_criteria>

  <done>DeleteKompetensi L1516-1590 di ProtonDataController.cs: `List<string>? evidencePaths = null` dideklarasi SEBELUM `using var transaction` L1529, semua 5-step cascade (CoachingSessions → ProtonDeliverableProgresses → ProtonDeliverableList → ProtonSubKompetensiList → ProtonKompetensiList) tetap INSIDE tx scope dengan order verbatim L1542-1574, evidencePaths populate INSIDE Step 2 antara `.ToListAsync()` L1558 dan `RemoveRange(progresses)` L1559 (loop progresses, EvidencePath add + JSON parse EvidencePathHistory inner try/catch warn-only), `await transaction.CommitAsync()` L1576 + audit log L1578-1580 preserved verbatim POST commit, File.Delete loop ADDED POST audit log SEBELUM `return Json(success=true)` dengan inner try/catch warn-only per file (log distinct "(Kompetensi evidence)"), catch generic Exception+RollbackAsync+ex.Message leak direfactor menjadi catch DbUpdateException (LogWarning + Json generic constraint) + catch Exception fallback (LogError + Json generic internal) tanpa `throw;` tanpa eksplisit `transaction.RollbackAsync()` tanpa `+ ex.Message` (D6 info leak fix CRITICAL).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify build + test + grep AC + IT_NOTIFY append + commit + SUMMARY</name>
  <files>docs/IT_NOTIFY.md, .planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-SUMMARY.md</files>

  <read_first>
    1. docs/IT_NOTIFY.md (baca entry terakhir — Phase 333 — untuk format append Phase 334)
    2. .planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-CONTEXT.md (D-08 IT_NOTIFY bundle v19.0, D-09 acceptance criteria 12 item)
    3. C:/Users/Administrator/.claude/get-shit-done/templates/summary.md (template SUMMARY.md)
    4. .planning/phases/333-fix-cascade-deletecoachingsession-file-atomicity/333-01-SUMMARY.md (referensi format SUMMARY untuk Phase 334 mirror struktur)
  </read_first>

  <action>
**[A] Build verification (D-09 item 8)**

Jalankan:
```bash
dotnet build
```
Expected: 0 error CS*. Jika ada compile error → kembali ke Task 1 sesuai baris yang error sebelum lanjut.

**[B] Test suite (D-09 item 9, no regression Phase 333 baseline)**

Jalankan:
```bash
dotnet test --no-build
```
Expected: 18/18 pass (baseline dari Phase 333). Tidak boleh ada regresi.

**[C] Grep-based AC verification (D-09 items 1-7)**

Jalankan grep pattern berikut sebagai verifikasi mekanis tanpa spin server. Untuk pattern dengan kemungkinan match di method lain (mis. `+ ex.Message` di endpoint lain controller, `throw;`), batasi scope ke range L1516-1590 (DeleteKompetensi) menggunakan `sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c ...`:

```bash
# AC-1 (D-09 item 1): evidencePaths declared di OUTER tx scope
grep -c "List<string>? evidencePaths = null" Controllers/ProtonDataController.cs      # expected 1

# AC-2 (D-09 items 2-3): evidencePaths populate inside tx + 5-step cascade preserved
grep -c "Phase 334 D-02" Controllers/ProtonDataController.cs                          # expected ≥2 (komentar declaration + komentar populate inside Step 2)
grep -c "Hard-deleted kompetensi" Controllers/ProtonDataController.cs                 # expected 1 (audit log message preserved)

# AC-3 (D-09 item 6): File.Delete loop POST audit log dengan log distinct
grep -c "File.Delete post-commit failed (Kompetensi evidence)" Controllers/ProtonDataController.cs   # expected 1

# AC-4 (D-09 item 7): catch DbUpdateException baru + LogWarning + 2 catch friendly
grep -c "catch (DbUpdateException dbEx)" Controllers/ProtonDataController.cs           # expected 1
grep -c "DbUpdate failed for DeleteKompetensi" Controllers/ProtonDataController.cs     # expected 1
grep -c "Gagal hapus kompetensi" Controllers/ProtonDataController.cs                   # expected 2 (2 catch blocks)

# AC-5 (D-09 item 7 + D-06 CRITICAL): NO ex.Message leak + NO RollbackAsync + NO throw di range DeleteKompetensi L1516-1600
grep -c "+ ex.Message" Controllers/ProtonDataController.cs                              # expected 0 (REMOVED — D6 info leak fix)
sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c "transaction.RollbackAsync"    # expected 0 (REMOVED)
sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c "throw;"             # expected 0 (REMOVED)

# AC-6 (D-09 item 4): CommitAsync + audit log POST commit preserved
grep -c "transaction.CommitAsync" Controllers/ProtonDataController.cs                  # expected ≥1 (preserve verbatim L1576)
grep -c "Failed to delete Kompetensi" Controllers/ProtonDataController.cs              # expected 1 (Exception fallback LogError preserved)

# AC-7: JSON parse + null check + Include preserved
grep -c "Failed to parse EvidencePathHistory" Controllers/ProtonDataController.cs      # expected ≥1 (JSON parse inner catch baru)
grep -c "Kompetensi tidak ditemukan" Controllers/ProtonDataController.cs               # expected 1 (null check Json preserved)
```

Semua harus menghasilkan match sesuai expected. Kalau ada mismatch → revisit Task 1 di line precision.

**[D] Manual smoke (D-09 item 10) — DEFERRED ke Dev promo (scenario #12)**

Per D-09 item 10: Manual smoke deferred ke Dev promo by IT. Tidak dilaksanakan di lokal Phase 334 — sufficient via grep AC mekanis + build + test pass. Catat di SUMMARY.md sebagai "Manual smoke: DEFERRED to Dev promo (D-09 item 10 explicit) — IT_NOTIFY scenario #12".

**[E] Append docs/IT_NOTIFY.md — per D-08 (bundle v19.0)**

Baca docs/IT_NOTIFY.md, append section berikut di bawah entry Phase 333:

```markdown
## Phase 334 — Fix Cascade DeleteKompetensi Orphan Evidence Files (File.Delete post-commit + catch friendly no info leak)

**Commit:** [commit hash setelah git commit di langkah F]
**Date shipped local:** 2026-05-28
**Flag migration:** NO — zero schema change, zero migration, zero service injection

### Files Modified
- `Controllers/ProtonDataController.cs` — DeleteKompetensi (L1516-1590): `List<string>? evidencePaths = null` dideklarasi di SCOPE OUTER tx (sebelum existing `using var transaction` L1529), evidencePaths populate INSIDE Step 2 ProtonDeliverableProgresses ANTARA `.ToListAsync()` L1558 dan `RemoveRange(progresses)` L1559 (loop progresses, EvidencePath add + JSON parse EvidencePathHistory inner try/catch warn-only per progress), File.Delete loop ADDED POST audit log L1580 SEBELUM `return Json(success=true)` L1582 dengan inner try/catch warn-only per file (log distinct "Kompetensi evidence"), catch generic Exception + `await transaction.RollbackAsync()` + `+ ex.Message` L1584-1589 direfactor menjadi `catch (DbUpdateException dbEx)` (friendly Json + LogWarning) + `catch (Exception ex)` fallback (friendly Json + LogError), TIDAK pakai `throw;` TIDAK pakai eksplisit `transaction.RollbackAsync()` (using var synchronous disposal auto-rollback) TIDAK pakai `+ ex.Message` (D6 info leak fix CRITICAL). 5-step cascade order L1542-1574 (CoachingSessions → Progresses → Deliverables → SubKompetensi → Kompetensi) + Include preload L1521-1524 + null check L1526-1527 + CommitAsync L1576 + audit log L1578-1580 POST commit preserved verbatim.

### Closes Phase 328 Audit Findings
- §4.8 DeleteKompetensi HIGH (D2 orphan EvidencePath + EvidencePathHistory) → CLOSED
- §4.8 DeleteKompetensi D6 (info leak `+ ex.Message` to client) → CLOSED
- §9 proposal #5 (`fix-cascade-deletekompetensi-orphan-evidence-files`) → CLOSED

### Action Required by IT
Bundle v19.0 batch push (Phase 325+326+327+329+330+331+332+333+334) — tunggu user release push lock per Phase 327 option-b hold. Tidak ada migration DB. Tidak ada perubahan config. Tidak ada seed data.

### Scenario #12 — Manual Smoke @ Dev (deferred dari lokal per D-09 item 10)
1. **Skenario A — Delete Kompetensi sukses dengan cleanup file (multi-level nested):** Login sebagai admin. Buat Kompetensi dengan 2 SubKompetensi, masing-masing 2 Deliverable, masing-masing Progress terisi EvidencePath + EvidencePathHistory JSON 2-3 entries + CoachingSession 1-2 dengan ActionItems. POST `/ProtonData/DeleteKompetensi` dengan KompetensiId. Expected: Json `{ success = true }`, Kompetensi + SubKompetensi + Deliverable + Progress + CoachingSession + ActionItems semua terhapus dari DB (5-step cascade berhasil), semua file evidence (current + history) dari semua progress terhapus dari disk wwwroot, audit log entry "Delete" + target "ProtonKompetensi" + message "Hard-deleted kompetensi ... with all descendants".
2. **Skenario B — Delete Kompetensi tanpa Progress / EvidencePath (cleanup no-op):** Buat Kompetensi → SubKompetensi → Deliverable tapi belum ada Progress yang submitted (EvidencePath null). Delete Kompetensi. Expected: Json success=true, cascade berhasil tanpa file deletion (evidencePaths null atau empty, File.Delete loop skipped), audit log normal.
3. **Skenario C — Kompetensi tidak ditemukan:** POST dengan KompetensiId yang tidak ada di DB. Expected: Json `{ success = false, message = "Kompetensi tidak ditemukan." }` tanpa tx creation.
4. **Skenario D — DB FK violation simulasi catch DbUpdateException hit (CRITICAL D6 verification):** Trigger constraint violation (mis. drop FK reference / inject lock conflict). Expected: catch DbUpdateException hit, Json `{ success = false, message = "Gagal hapus kompetensi: ada constraint database yang dilanggar." }` — **VERIFY**: response BODY TIDAK mengandung table name / constraint name / ex.Message / stack trace (D6 info leak fix verification). Server log warning dengan ex detail (`DbUpdate failed for DeleteKompetensi {Id}`). Tx auto-rollback via using disposal (tidak ada record terhapus). File di disk TIDAK terhapus (loop POST commit tidak dieksekusi karena early return dari catch).
5. **Skenario E — File.Delete fail post-commit (orphan disk acceptable):** Buat Kompetensi dengan EvidencePath yang sudah dihapus manual dari disk SEBELUM delete (atau permission denied simulasi). Expected: cascade DB sukses + audit log + Json success=true (file warn-only log "File.Delete post-commit failed (Kompetensi evidence): {Path}" di server log tapi tidak fail request). Loop iterate file berikutnya, tidak break.
```

**[F] Git commit (D-09 item 11)**

```bash
git add Controllers/ProtonDataController.cs docs/IT_NOTIFY.md
git commit -m "feat(334): cascade orphan evidence DeleteKompetensi (File.Delete post-commit + catch friendly no info leak)"
```

Setelah commit, catat commit hash → update entry IT_NOTIFY.md Phase 334 (ganti `[commit hash setelah git commit di langkah F]` dengan hash aktual, lalu commit terpisah `docs(334): record commit hash in IT_NOTIFY`).

**[G] Generate SUMMARY.md (D-09 item 12)**

Buat file `.planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-SUMMARY.md` mengikuti template `C:/Users/Administrator/.claude/get-shit-done/templates/summary.md`. Isi minimal:
- Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-08)
- Files modified table (Controllers/ProtonDataController.cs ~40 LoC delta + docs/IT_NOTIFY.md)
- D-09 AC table 12/12 checklist
- Grep marker counts table:
  - `List<string>? evidencePaths = null`: 1
  - `File.Delete post-commit failed (Kompetensi evidence)`: 1
  - `catch (DbUpdateException dbEx)`: 1
  - `DbUpdate failed for DeleteKompetensi`: 1
  - `Gagal hapus kompetensi`: 2
  - `+ ex.Message`: 0 (REMOVED — D6 info leak fix)
  - `transaction.RollbackAsync` (range L1516-1600): 0
  - `throw;` (range L1516-1600): 0
  - `Failed to delete Kompetensi`: 1 (Exception fallback LogError preserved)
  - `Failed to parse EvidencePathHistory`: ≥1 (JSON parse inner catch baru)
  - `Hard-deleted kompetensi`: 1 (audit log message preserved verbatim)
  - `Kompetensi tidak ditemukan`: 1 (null check Json preserved)
  - `transaction.CommitAsync`: ≥1 (preserved L1576)
- Threat model disposition table 4/4 (T-334-01..04)
- Closes Phase 328 §4.8 (D2 + D6) + §9 proposal #5
- Manual smoke status: DEFERRED to Dev promo (per D-09 item 10 explicit, scenario #12 di IT_NOTIFY)
- Key difference catatan: existing `using var transaction` (synchronous disposal) L1529 dipreserve (TIDAK diganti ke `await using` — beda dari Phase 333); audit log POST CommitAsync L1578-1580 dipreserve di posisi existing (TIDAK pindah ke INSIDE tx — D3 out of scope per Phase 328 §4.8 D2+D6 only); response Json (AJAX-only) bukan TempData+Redirect; D6 info leak fix CRITICAL (HAPUS `+ ex.Message` dari Json response message)
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331+332+333+334) menunggu user release push lock (Phase 327 option-b)
  </action>

  <verify>
    <automated>dotnet build 2>&1 | tail -5 && dotnet test --no-build 2>&1 | grep -E "passed|failed|Total"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build: 0 error (Build succeeded)
    - dotnet test --no-build: 18 passed, 0 failed (atau lebih jika ada test baru — tidak boleh ada regresi)
    - `grep -c "List<string>? evidencePaths = null" Controllers/ProtonDataController.cs` → 1
    - `grep -c "File.Delete post-commit failed (Kompetensi evidence)" Controllers/ProtonDataController.cs` → 1
    - `grep -c "catch (DbUpdateException dbEx)" Controllers/ProtonDataController.cs` → 1
    - `grep -c "DbUpdate failed for DeleteKompetensi" Controllers/ProtonDataController.cs` → 1
    - `grep -c "Gagal hapus kompetensi" Controllers/ProtonDataController.cs` → 2
    - `grep -c "+ ex.Message" Controllers/ProtonDataController.cs` → 0 (REMOVED — D6 CRITICAL)
    - `sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c "transaction.RollbackAsync"` → 0
    - `sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c "throw;"` → 0
    - `grep -c "Failed to delete Kompetensi" Controllers/ProtonDataController.cs` → 1 (Exception fallback LogError preserved)
    - `grep -c "Failed to parse EvidencePathHistory" Controllers/ProtonDataController.cs` → ≥1 (JSON parse inner catch baru)
    - `grep -c "Hard-deleted kompetensi" Controllers/ProtonDataController.cs` → 1 (audit log message preserved)
    - `grep -c "Kompetensi tidak ditemukan" Controllers/ProtonDataController.cs` → 1 (null check Json preserved)
    - Manual smoke status DEFERRED dicatat di SUMMARY.md (per D-09 item 10) + scenario #12 di IT_NOTIFY.md
    - git log --oneline -1 → commit message mengandung "feat(334): cascade orphan evidence DeleteKompetensi"
    - docs/IT_NOTIFY.md mengandung section "Phase 334" dengan commit hash terisi + scenario #12 5 sub-skenario
    - 334-01-SUMMARY.md file ada di direktori phase dengan D-09 12/12 AC checklist
  </acceptance_criteria>

  <done>Build bersih, 18/18 test pass, semua 12 AC dari D-09 terverifikasi (grep mekanis 13 counter + range-scoped throw/RollbackAsync check + D6 ex.Message leak verification = 0 + manual smoke deferred per explicit D-09 item 10), IT_NOTIFY.md diupdate Phase 334 entry + scenario #12 5 sub-skenario (termasuk Skenario D D6 verification) + commit hash terisi, commit dibuat dengan message per D-09 item 11, SUMMARY.md digenerate dengan status SHIPPED LOCAL + catatan key difference (using var synchronous preserved, audit log position preserved, response Json bukan TempData, D6 info leak fix CRITICAL).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller | POST AJAX request dengan `[HttpPost]` + `[ValidateAntiForgeryToken]` aktif di DeleteKompetensi endpoint + user challenge (Challenge) + null komp check (Json error). Catatan: controller ProtonData TIDAK punya `[Authorize]` attribute eksplisit — existing convention out of scope Phase 334. |
| Controller → Database | EF Core DbUpdateException boundary — Phase 334 TAMBAH catch DbUpdateException spesifik (sebelumnya catch generic Exception + `+ ex.Message` → info leak ke client via Json response) + preserve existing `using var transaction` L1529 untuk atomicity 5-step cascade (CoachingSessions → Progresses → Deliverables → SubKompetensi → Kompetensi) + SaveChanges + audit log POST commit. |
| Controller → Filesystem | `_env.WebRootPath` + `relUrl` (EvidencePath + EvidencePathHistory entries dari setiap progress dalam tree Kompetensi) — sekarang dieksekusi POST CommitAsync + POST audit log dengan inner try/catch warn-only per file (log distinct "Kompetensi evidence"), cross-platform path normalization `Replace('/', Path.DirectorySeparatorChar)` Phase 333 pattern. |
| Controller → AuditLog | `_auditLog.LogAsync` call yang preserve verbatim POST CommitAsync L1578-1580 — D7 positioning out of scope per Phase 328 §4.8 (D2+D6 only). Kalau audit log fail post-commit, Json success=true tetap return (existing behavior — acceptable trade-off untuk minimal scope phase). |
| Controller → Browser (Response) | Json response body — Phase 334 CRITICAL D6 fix: HAPUS `+ ex.Message` dari client-facing message. Exception detail (`ex.Message`, `ex.StackTrace`, `ex.InnerException`, table/constraint names) hanya di server log via `_logger.LogWarning`/`_logger.LogError`, TIDAK pernah dikirim ke client Json response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-334-01 | D (Denial of Service / file orphan) | DeleteKompetensi 5-step cascade RemoveRange Progresses L1559 TIDAK cleanup `EvidencePath` + `EvidencePathHistory` (JSON list) physical files → orphan disk (file evidence tetap di disk wwwroot meskipun parent Kompetensi + descendant progresses sudah ter-delete dari DB) | mitigate | Declare `List<string>? evidencePaths = null;` di SCOPE OUTER tx (sebelum `using var transaction` L1529). Allocate + populate `evidencePaths = new List<string>();` INSIDE Step 2 ProtonDeliverableProgresses ANTARA `.ToListAsync()` L1558 dan `RemoveRange(progresses)` L1559 — loop semua progresses, add EvidencePath string + JSON parse EvidencePathHistory inner try/catch warn-only per progress entry. File.Delete loop dieksekusi POST `CommitAsync` L1576 + POST audit log L1578-1580 SEBELUM `return Json(success=true)` L1582 dengan inner try/catch warn-only per file (log distinct `File.Delete post-commit failed (Kompetensi evidence): {Path}` + iterate file berikutnya, tidak break loop). Orphan disk acceptable scenario (file di disk gone tapi DB consistent) — manual cleanup via cron janitor backlog Phase 999.x. |
| T-334-02 | T (Atomicity violation, mapped to T - Tampering) | DeleteKompetensi partial state — file evidence gone dari disk tetapi DB rollback fails (RemoveRange Progresses fail di SaveChanges atau CommitAsync) → DB inconsistent dengan filesystem (Kompetensi/Progresses masih ada di DB tapi file evidence sudah dihapus dari disk) | mitigate | `evidencePaths` declared di SCOPE OUTER tx sebagai `List<string>?` (null initial) — visible POST CommitAsync. File deletion DEFERRED sampai SETELAH `transaction.CommitAsync()` L1576 sukses + audit log L1578-1580. Kalau tx fail (DbUpdateException atau Exception lain), `using var transaction` synchronous disposal auto-rollback DB + early return dari catch block (loop POST commit tidak dieksekusi karena execution flow tidak mencapai L1582 setelah CommitAsync). Tidak ada window partial-state: DB consistent OR rollback, dan file di disk hanya terhapus kalau DB sudah commit sukses (5-step cascade + audit log semua sudah persisted). |
| T-334-03 | R (Repudiation) | Audit log POST commit (L1578-1580) — kalau audit log fail setelah CommitAsync sukses, Json success=true tetap return (existing behavior); File.Delete loop juga setelah audit log, kalau audit log fail tapi file sudah dibersih → audit trail tidak record cleanup intent. Phase 333 audit log INSIDE tx pre-commit (atomic dengan DB). Phase 334 audit log POST commit (TIDAK atomic). | accept | Out of scope D3 positioning per Phase 328 §4.8 yang scope D2+D6 only. Trade-off acceptable untuk minimal scope phase: audit log POST commit = potential audit-fail-but-data-committed window, tetapi konsisten existing pattern Phase 332 P05 dan tidak introduce regression. File.Delete order setelah audit log (per D-05) ensures audit captures intent first → physical cleanup follows. Accept trade-off; D3 reposition deferred ke phase backlog terpisah jika dibutuhkan. |
| T-334-04 | I (Information Disclosure) | Existing `return Json(new { success = false, message = "Gagal menghapus: " + ex.Message });` L1588 → DB constraint detail (FK_ProtonKompetensiList_*, table names), exception type, EF Core internal error message BOCOR ke client browser via Json response body (developer console / network tab inspect). CRITICAL info leak per Phase 328 §4.8 D6 finding. | mitigate | Replace `+ ex.Message` dengan generic friendly message di kedua catch blocks. `catch (DbUpdateException dbEx)` → `return Json(new { success = false, message = "Gagal hapus kompetensi: ada constraint database yang dilanggar." });` (generic, no constraint/table name leak). `catch (Exception ex)` fallback → `return Json(new { success = false, message = "Gagal hapus kompetensi: terjadi kesalahan internal. Hubungi admin." });` (generic, no exception type/message leak). Exception detail (`ex.Message`, `ex.StackTrace`, `ex.InnerException`) hanya di server log via `_logger.LogWarning(dbEx, "DbUpdate failed for DeleteKompetensi {Id}", req.KompetensiId)` dan `_logger.LogError(ex, "Failed to delete Kompetensi {Id}", req.KompetensiId)`, TIDAK pernah dikirim ke client Json response. Verification: `grep -c "+ ex.Message" Controllers/ProtonDataController.cs` → 0 (mandatory). |
</threat_model>

<verification>
Verifikasi akhir keseluruhan Phase 334:

1. **Build clean:** `dotnet build` → 0 error
2. **Test suite:** `dotnet test --no-build` → 18/18 pass (baseline Phase 333, no regression)
3. **Grep AC mekanis (13 counter):**
   - `grep -c "List<string>? evidencePaths = null" Controllers/ProtonDataController.cs` → 1
   - `grep -c "File.Delete post-commit failed (Kompetensi evidence)" Controllers/ProtonDataController.cs` → 1
   - `grep -c "catch (DbUpdateException dbEx)" Controllers/ProtonDataController.cs` → 1
   - `grep -c "DbUpdate failed for DeleteKompetensi" Controllers/ProtonDataController.cs` → 1
   - `grep -c "Gagal hapus kompetensi" Controllers/ProtonDataController.cs` → 2
   - `grep -c "+ ex.Message" Controllers/ProtonDataController.cs` → 0 (CRITICAL D6 REMOVED)
   - `sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c "transaction.RollbackAsync"` → 0
   - `sed -n '1516,1600p' Controllers/ProtonDataController.cs | grep -c "throw;"` → 0
   - `grep -c "Failed to delete Kompetensi" Controllers/ProtonDataController.cs` → 1
   - `grep -c "Failed to parse EvidencePathHistory" Controllers/ProtonDataController.cs` → ≥1
   - `grep -c "Hard-deleted kompetensi" Controllers/ProtonDataController.cs` → 1
   - `grep -c "Kompetensi tidak ditemukan" Controllers/ProtonDataController.cs` → 1
   - `grep -c "transaction.CommitAsync" Controllers/ProtonDataController.cs` → ≥1
4. **Manual smoke:** DEFERRED to Dev promo (per D-09 item 10 explicit, scenario #12 5 sub-skenario termasuk D6 verification skenario D di IT_NOTIFY)
5. **IT_NOTIFY:** `grep "Phase 334" docs/IT_NOTIFY.md` → 1 section dengan commit hash terisi + scenario #12
6. **Commit:** `git log --oneline -1` → mengandung "feat(334): cascade orphan evidence DeleteKompetensi"
7. **SUMMARY:** `ls .planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-SUMMARY.md` → file ada
</verification>

<success_criteria>
Phase 334 selesai ketika:
- [ ] DeleteKompetensi L1516-1590 di-refactor sesuai D-02 + D-03 + D-04 + D-05 + D-06 (evidencePaths outer scope nullable + populate INSIDE tx Step 2 SEBELUM RemoveRange progresses + File.Delete POST audit log + catch DbUpdateException friendly + catch Exception fallback friendly NO `+ ex.Message`)
- [ ] Existing `using var transaction` L1529 dipreserve (synchronous disposal, TIDAK diganti ke `await using` — key difference dari Phase 333)
- [ ] Audit log POST CommitAsync L1578-1580 dipreserve verbatim (D7 positioning out of scope per Phase 328 §4.8 D2+D6 only — key difference dari Phase 333 yang audit INSIDE tx)
- [ ] Include preload graph L1521-1524 + null check komp L1526-1527 + deliverableIds collection L1532-1535 + progressIds collection L1537-1540 preserved verbatim OUTSIDE tx body cascade
- [ ] 5-step cascade order L1542-1574 preserved verbatim (CoachingSessions → ProtonDeliverableProgresses → ProtonDeliverableList → ProtonSubKompetensiList → ProtonKompetensiList) — FK Restrict-safe
- [ ] CommitAsync L1576 + audit log L1578-1580 preserved verbatim POST commit, File.Delete loop INSERTED POST audit log SEBELUM `return Json(success=true)` L1582
- [ ] Catch generic Exception+RollbackAsync+ex.Message L1584-1589 direfactor jadi catch DbUpdateException (LogWarning + Json generic constraint) + catch Exception fallback (LogError + Json generic internal), NO `throw;`, NO eksplisit `transaction.RollbackAsync()` (using var disposal handles it), NO `+ ex.Message` (D6 info leak fix CRITICAL)
- [ ] `dotnet build` clean (0 error CS*)
- [ ] `dotnet test --no-build` 18/18 pass (no regression Phase 333 baseline)
- [ ] Manual smoke: DEFERRED to Dev promo (per D-09 item 10 — tidak blocking ship lokal)
- [ ] Grep AC mekanis 13 counter terpenuhi (termasuk range-scoped check L1516-1600 untuk `transaction.RollbackAsync` + `throw;` = 0 + full-file `+ ex.Message` = 0 untuk D6 CRITICAL verification)
- [ ] docs/IT_NOTIFY.md diupdate dengan Phase 334 entry + commit hash terisi + scenario #12 5 sub-skenario (termasuk Skenario D D6 verification: response body NO table/constraint/ex.Message leak)
- [ ] 1 commit dengan message `feat(334): cascade orphan evidence DeleteKompetensi (File.Delete post-commit + catch friendly no info leak)`
- [ ] 334-01-SUMMARY.md digenerate
- [ ] Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-08)
</success_criteria>

<output>
Setelah Task 2 selesai, buat `.planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-SUMMARY.md` dengan isi:
- Status: SHIPPED LOCAL
- Files modified (Controllers/ProtonDataController.cs ~40 LoC delta + docs/IT_NOTIFY.md)
- AC D-09 table 12/12 checklist
- Grep marker count verification (13 counter — evidencePaths outer, File.Delete post-commit log, catch DbUpdateException, LogWarning DbUpdate, Gagal hapus kompetensi x2, full-file ex.Message=0 D6 CRITICAL, range-scoped transaction.RollbackAsync=0 + throw;=0 L1516-1600, LogError Exception fallback, JSON parse inner catch, audit log message Hard-deleted, null check Json, CommitAsync preserved)
- Manual smoke status: DEFERRED to Dev promo (D-09 item 10 explicit, scenario #12 di IT_NOTIFY 5 sub-skenario termasuk Skenario D D6 verification)
- Threat model disposition 4/4 (T-334-01..04 — T03 R repudiation accept out of scope D3 positioning)
- Closes Phase 328 §4.8 (D2 orphan + D6 info leak) + §9 proposal #5
- Key difference catatan: existing `using var transaction` L1529 dipreserve synchronous disposal (beda Phase 333 await using); audit log POST CommitAsync L1578-1580 dipreserve di posisi existing (D3 out of scope Phase 328 §4.8 D2+D6 only); response Json AJAX-only bukan TempData+Redirect (beda Phase 333); D6 info leak fix CRITICAL (HAPUS `+ ex.Message` dari Json response message — verification grep full-file = 0)
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331+332+333+334) menunggu user release push lock (Phase 327 option-b)
</output>
