---
phase: 337-cmp-records-full-overhaul-filter-data-arch-a11y
plan: 03
type: execute
wave: 3
depends_on: [337-01, 337-02]
files_modified:
  - Services/WorkerDataService.cs
  - Services/IWorkerDataService.cs
  - Controllers/CMPController.cs
  - Views/CMP/RecordsTeam.cshtml
  - Views/CMP/_RecordsTeamBody.cshtml
autonomous: false
requirements: [CMP-24, CMP-25, CMP-26]
must_haves:
  truths:
    - "GetAllWorkersHistory eksekusi SQL push-down via IQueryable projection — TIDAK lagi load semua TrainingRecords/AssessmentSessions ke memory lalu filter via HashSet (CMP-24)"
    - "Date filter di GetWorkersInSection diterjemahkan ke SQL WHERE clause — TIDAK lagi in-memory .Where loop per worker (CMP-25)"
    - "Team View menampilkan pagination numeric pager 20/page default (50/100 option) di bawah tabel — server-side via PaginationHelper.Calculate (CMP-26, D-02)"
    - "RecordsTeamPartial accept page+pageSize param, slice workerList dengan Skip/Take dari PaginationResult (CMP-26)"
    - "Pagination state (page+pageSize) persist via sessionStorage 'cmp-records-team-pagination' (OQ-337-3 resolve: sessionStorage, NOT cookie — same scope dengan filter state Plan 02)"
  artifacts:
    - path: "Services/WorkerDataService.cs"
      provides: "GetAllWorkersHistory dengan IQueryable composition + GetWorkersInSection date filter SQL where + pagination support"
      contains: "IQueryable<AllWorkersHistoryRow>"
    - path: "Services/IWorkerDataService.cs"
      provides: "Interface updated dengan page/pageSize parameter di GetWorkersInSection"
    - path: "Controllers/CMPController.cs"
      provides: "RecordsTeamPartial accept page/pageSize + PaginationHelper.Calculate + ViewBag.Pagination"
      contains: "PaginationHelper.Calculate"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "Pagination UI numeric pager + page size selector + sessionStorage page state"
    - path: "Views/CMP/_RecordsTeamBody.cshtml"
      provides: "Tidak berubah strukturnya, slicing dilakukan controller via Skip/Take"
  key_links:
    - from: "Controllers/CMPController.cs RecordsTeamPartial"
      to: "Helpers/PaginationHelper.cs Calculate"
      via: "PaginationHelper.Calculate(workerList.Count, page, pageSize)"
      pattern: "PaginationHelper\\.Calculate"
    - from: "Services/WorkerDataService.cs GetAllWorkersHistory"
      to: "SQL push-down query"
      via: "Select projection + Where clauses BEFORE materialization (ToListAsync)"
      pattern: "Select\\(.*new AllWorkersHistoryRow"
    - from: "Views/CMP/RecordsTeam.cshtml pagination UI"
      to: "filterTeamTable + doFetch dengan page param"
      via: "params.set('page', currentPage) + params.set('pageSize', pageSize)"
      pattern: "params\\.set\\('page'"
---

<objective>
Wave 3 Phase 337 — Final wave arch refactor. 3 REQ: (CMP-24) SQL push-down GetAllWorkersHistory eliminate in-memory HashSet filter, (CMP-25) Date filter SQL where eliminate in-memory loop, (CMP-26) Pagination Team View via PaginationHelper.Calculate (D-02 numeric pager 20/page default, 50/100 option).

Purpose: Eliminate N+1 query risk + memory bloat untuk org > 100 worker. Foundation untuk scalability — saat Pertamina KPB ekspansi ke organisasi unit besar, /CMP/Records page tetap responsive. Pagination juga reduce DOM render time (saat ini full 100-200 worker per page = heavy paint).

OQ-337-2 RESOLVED: **IQueryable composition + Select projection (DTO)** dipilih. Rationale:
- Pure IQueryable mempertahankan EF query optimization, hindari raw SQL maintainability overhead
- Projection ke AllWorkersHistoryRow DTO eliminate over-fetching (cuma kolom yang dibutuhkan)
- Raw SQL ditolak: brittle terhadap schema change, tidak portable ke SQLite test
- Trade-off: EF Core 8 untuk SQL Server sudah cukup smart compile IQueryable composition ke single query dengan JOIN (lihat Phase 311 PATTERNS Cross-cutting Pattern 2)

OQ-337-3 RESOLVED: **sessionStorage** (bukan cookie). Rationale:
- Konsisten dengan Plan 02 filter state persist mechanism (`cmp-records-team-filter`)
- Per-tab scope — user buka 2 tab dengan filter beda tidak konflik
- Cookie overhead (~50 bytes per request) tidak perlu untuk UI-only state
- Trade-off: tidak persist antar browser restart — acceptable, filter sendiri juga reset

Output:
- `Services/WorkerDataService.cs` — `GetAllWorkersHistory` refactor IQueryable composition + `GetWorkersInSection` date filter SQL push-down + pagination support (page/pageSize param)
- `Services/IWorkerDataService.cs` — interface updated bila ada
- `Controllers/CMPController.cs` — `RecordsTeamPartial` accept page/pageSize + PaginationHelper.Calculate + pass pagination ViewBag
- `Views/CMP/RecordsTeam.cshtml` — pagination UI (numeric pager + page size selector 20/50/100) + sessionStorage page state + page reset on filter change
- `Views/CMP/_RecordsTeamBody.cshtml` — TIDAK berubah (slicing di controller)

EXPLAIN PLAN sanity check WAJIB sebelum ship — SQL profile via SQL Server Profiler atau EF Core logging untuk pastikan single query JOIN, BUKAN multiple round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-CONTEXT.md
@.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-01-PLAN.md
@.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md

# File sumber
@Services/WorkerDataService.cs
@Controllers/CMPController.cs
@Views/CMP/RecordsTeam.cshtml
@Helpers/PaginationHelper.cs

<interfaces>
<!-- PaginationHelper pattern from Helpers/PaginationHelper.cs -->

```csharp
namespace HcPortal.Helpers
{
    public record PaginationResult(int CurrentPage, int TotalPages, int TotalCount, int Skip, int Take);

    public static class PaginationHelper
    {
        public static PaginationResult Calculate(int totalCount, int page, int pageSize)
        {
            var totalPages = (int)Math.Ceiling(totalCount / (double)pageSize);
            var currentPage = Math.Max(1, Math.Min(page, Math.Max(1, totalPages)));
            var skip = (currentPage - 1) * pageSize;
            return new PaginationResult(currentPage, totalPages, totalCount, skip, pageSize);
        }
    }
}
```

Usage pattern reference (AssessmentAdminController.cs L62 ManageAssessment):
```csharp
public async Task<IActionResult> ManageAssessment(string? search, int page = 1, int pageSize = 20, ...)
{
    // ... query build ...
    ViewBag.CurrentPage = page;
    ViewBag.PageSize = pageSize;
    return View();
}
```

Pattern AssessmentAdmin L4508 (export tab partial):
```csharp
var totalCount = await query.CountAsync();
var paging = PaginationHelper.Calculate(totalCount, page, pageSize);
var items = await query.Skip(paging.Skip).Take(paging.Take).ToListAsync();
ViewBag.Pagination = paging;
```

From Services/WorkerDataService.cs L85-172 GetAllWorkersHistory CURRENT (memory-heavy):
```csharp
public async Task<(List<AllWorkersHistoryRow> assessment, List<AllWorkersHistoryRow> training)> GetAllWorkersHistory()
{
    var archivedCounts = await _context.AssessmentAttemptHistory
        .AsNoTracking()
        .GroupBy(h => new { h.UserId, h.Title })
        .Select(g => new { g.Key.UserId, g.Key.Title, Count = g.Count() })
        .ToListAsync();
    // ... build lookup ...

    var archivedAttempts = await _context.AssessmentAttemptHistory
        .AsNoTracking()
        .Include(h => h.User)
        .ToListAsync();   // ← LOAD ALL HISTORY ke memory

    // ... build assessmentRows in-memory ...

    var currentCompleted = await _context.AssessmentSessions
        .AsNoTracking()
        .Include(a => a.User)
        .Where(a => a.Status == "Completed")
        .ToListAsync();   // ← LOAD ALL COMPLETED ke memory

    // ... build assessmentRows lagi ...

    var trainings = await _context.TrainingRecords
        .AsNoTracking()
        .Include(t => t.User)
        .ToListAsync();   // ← LOAD ALL TRAININGS ke memory

    return (assessmentRows, trainingRows);
}
```

TARGET: IQueryable composition dengan projection. Catatan PENTING: method ini dipanggil dari Export endpoints yang akhirnya FILTER by `filteredIds.Contains` di-memory. Refactor:
- Option 1: Tambah overload `GetAllWorkersHistory(IEnumerable<string>? workerIds = null, DateTime? from = null, DateTime? to = null, string? category = null, string? subCategory = null)` — semua filter di SQL.
- Option 2: Keep method tanpa filter tapi return IQueryable (lazy), caller compose filter.

PILIH **Option 1** (less risky, callers existing tidak butuh refactor signature, hanya tambah optional params).

```csharp
public async Task<(List<AllWorkersHistoryRow> assessment, List<AllWorkersHistoryRow> training)>
    GetAllWorkersHistory(
        IEnumerable<string>? workerIds = null,
        DateTime? from = null,
        DateTime? to = null,
        string? category = null,
        string? subCategory = null)
{
    var workerIdList = workerIds?.ToList();   // materialize once

    // Assessment query — composed IQueryable
    var assessmentBaseQuery = _context.AssessmentAttemptHistory
        .AsNoTracking()
        .Where(h => workerIdList == null || workerIdList.Contains(h.UserId));

    if (from.HasValue) assessmentBaseQuery = assessmentBaseQuery
        .Where(h => (h.CompletedAt ?? h.StartedAt ?? h.ArchivedAt) >= from.Value);
    if (to.HasValue) assessmentBaseQuery = assessmentBaseQuery
        .Where(h => (h.CompletedAt ?? h.StartedAt ?? h.ArchivedAt) <= to.Value);

    // Project to DTO before materialization
    var archivedRows = await assessmentBaseQuery
        .Select(h => new AllWorkersHistoryRow
        {
            WorkerId      = h.UserId,
            WorkerName    = h.User != null ? h.User.FullName : h.UserId,
            WorkerNIP     = h.User != null ? h.User.NIP : null,
            RecordType    = "Assessment Online",
            Title         = h.Title,
            Date          = (h.CompletedAt ?? h.StartedAt ?? h.ArchivedAt) ?? DateTime.MinValue,
            Score         = h.Score,
            IsPassed      = h.IsPassed,
            AttemptNumber = h.AttemptNumber
        })
        .ToListAsync();

    // ... similar for currentCompleted + trainings ...
}
```

Note: AttemptNumber dari `currentCompleted` butuh archivedCountLookup — TIDAK bisa pure SQL push-down karena cross-table count. Solusi: keep `archivedCounts` GroupBy ke list (already efficient), lalu di-memory join. Atau gunakan subquery dengan `_context.AssessmentAttemptHistory.Count(h => h.UserId == a.UserId && h.Title == a.Title) + 1`. EF Core 8 translate subquery ke correlated subquery — acceptable.

From Services/WorkerDataService.cs L175-311 GetWorkersInSection CURRENT (date filter in-memory L213-246):
```csharp
public async Task<List<WorkerTrainingStatus>> GetWorkersInSection(string? section, ..., DateTime? dateFrom = null, DateTime? dateTo = null, string? subCategory = null)
{
    // Load users + assessmentSessions in-memory
    // ...
    bool hasDateFilter = dateFrom.HasValue || dateTo.HasValue;
    foreach (var user in users)
    {
        // ...
        if (hasDateFilter)
        {
            trainingRecords = allTrainingRecords.Where(tr => {
                var effectiveDate = (tr.TanggalMulai ?? tr.Tanggal).Date;
                return ...;
            }).ToList();
            // ↑ in-memory filter per worker
        }
    }
}
```

TARGET: date filter di SQL via composed query:
```csharp
var trainingsByUser = await _context.TrainingRecords
    .AsNoTracking()
    .Where(tr => userIds.Contains(tr.UserId))
    .Where(tr => !dateFrom.HasValue ||
                 (tr.TanggalMulai ?? tr.Tanggal).Date >= dateFrom.Value.Date)
    .Where(tr => !dateTo.HasValue ||
                 (tr.TanggalMulai ?? tr.Tanggal).Date <= dateTo.Value.Date)
    .GroupBy(tr => tr.UserId)
    .ToDictionaryAsync(g => g.Key, g => g.ToList());
```

Note: existing code load `user.TrainingRecords` via `Include` di L179. Untuk SQL push-down date filter, harus PISAH dari Include — Include load ALL training tanpa date filter, lalu memory filter. Refactor: HAPUS `.Include(u => u.TrainingRecords)` di L179, ganti dengan separate query trainingsByUser di-load dengan SQL date filter, lalu join di-memory.

From Controllers/CMPController.cs L740-758 RecordsTeamPartial CURRENT (no pagination):
```csharp
public async Task<IActionResult> RecordsTeamPartial(
    string? section, string? unit, string? category, string? subCategory,
    string? statusFilter, string? dateFrom, string? dateTo)
{
    // ...
    var workerList = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, null, statusFilter, from, to, subCategory);
    return PartialView("_RecordsTeamBody", workerList);
}
```

TARGET signature:
```csharp
public async Task<IActionResult> RecordsTeamPartial(
    string? section, string? unit, string? category, string? subCategory,
    string? statusFilter, string? dateFrom, string? dateTo,
    int page = 1, int pageSize = 20)
{
    // ... existing logic ...
    var workerList = await _workerDataService.GetWorkersInSection(...);

    // Pagination
    var pageSizeValidated = (pageSize == 20 || pageSize == 50 || pageSize == 100) ? pageSize : 20;
    var paging = HcPortal.Helpers.PaginationHelper.Calculate(workerList.Count, page, pageSizeValidated);
    var pagedWorkerList = workerList.Skip(paging.Skip).Take(paging.Take).ToList();

    ViewBag.Pagination = paging;
    return PartialView("_RecordsTeamBody", pagedWorkerList);
}
```

Pagination UI (RecordsTeam.cshtml — render OUTSIDE partial, di shell):
- Karena `_RecordsTeamBody` HANYA render `<tr>` rows (untuk tbody innerHTML replace via AJAX), pagination UI harus di `RecordsTeam.cshtml` shell, di-update via separate AJAX call atau JS-side.

DESIGN choice: pagination UI render via SECOND AJAX response — extend RecordsTeamPartial untuk return JSON `{ rowsHtml, paginationHtml, totalCount }`, BUKAN PartialView murni. Atau lebih simple: return view model dengan rows + pagination, render bareng-bareng.

LEBIH SIMPLE: ubah `_RecordsTeamBody` return = wrap `<tbody><tr>...</tr></tbody>` + tambah `<tr>` footer pagination. Jadi JS replace seluruh `#workerTable` body+footer.

Atau yang paling clean: return pagination data via response header `X-Pagination` (JSON), JS parse + render ke separate `<nav>` element.

PILIH approach: **return data via response header X-Pagination** — clean separation rows HTML vs pagination data, minim invasive ke _RecordsTeamBody.

Controller:
```csharp
Response.Headers.Append("X-Pagination", System.Text.Json.JsonSerializer.Serialize(new {
    paging.CurrentPage,
    paging.TotalPages,
    paging.TotalCount,
    paging.Take
}));
return PartialView("_RecordsTeamBody", pagedWorkerList);
```

JS read header + render pagination ke `<nav id="paginationNav">` element baru di RecordsTeam.cshtml shell.

From Helpers/PaginationHelper.cs already supports this — no helper change needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor GetAllWorkersHistory dengan IQueryable composition + filter params (CMP-24)</name>
  <files>Services/WorkerDataService.cs, Services/IWorkerDataService.cs</files>
  <behavior>
    Setelah refactor:
    - `GetAllWorkersHistory` signature tambah optional params: `IEnumerable<string>? workerIds`, `DateTime? from`, `DateTime? to`, `string? category`, `string? subCategory`
    - Saat semua params null, behavior identik dengan sebelumnya (backward compat)
    - Saat params populated, filter applied di SQL (composed IQueryable WHERE) sebelum materialization
    - Projection ke `AllWorkersHistoryRow` dilakukan di SQL via `.Select(h => new AllWorkersHistoryRow { ... })` — tidak load full entity ke memory
    - AttemptNumber untuk `currentCompleted` tetap gunakan `archivedCountLookup` (cross-table count tidak push-down sempurna, acceptable)
    - Verify EF Core query plan via logging — single SELECT JOIN per category (archived + completed + trainings = 3 SELECT total), BUKAN N+1
  </behavior>
  <action>
    1. **Cek interface `Services/IWorkerDataService.cs`:**
    ```bash
    cat Services/IWorkerDataService.cs 2>/dev/null
    ```
    Bila ada method `GetAllWorkersHistory` di interface, update signature dengan optional params.

    **Pre-step (drift verify ±5 baris):** Sebelum edit, jalankan `grep -n "public.*GetAllWorkersHistory" Services/WorkerDataService.cs` confirm line actual ≈ L85 (sejak audit 2026-05-27 ada 1 commit `c7adcb73` DateOnly sweep + Wave 1/2 changes Phase 337 sebelumnya). Adjust line reference seluruh action steps kalau drift > ±5.

    2. **Edit `Services/WorkerDataService.cs` GetAllWorkersHistory** — replace seluruh method L85-172:
    ```csharp
    public async Task<(List<AllWorkersHistoryRow> assessment, List<AllWorkersHistoryRow> training)>
        GetAllWorkersHistory(
            IEnumerable<string>? workerIds = null,
            DateTime? from = null,
            DateTime? to = null,
            string? category = null,
            string? subCategory = null)
    {
        var workerIdList = workerIds?.ToList();
        bool hasWorkerFilter = workerIdList != null;

        // --- Assessment history (archived attempts) ---
        var archivedQuery = _context.AssessmentAttemptHistory
            .AsNoTracking()
            .AsQueryable();

        if (hasWorkerFilter)
            archivedQuery = archivedQuery.Where(h => workerIdList!.Contains(h.UserId));
        if (from.HasValue)
            archivedQuery = archivedQuery.Where(h =>
                (h.CompletedAt ?? h.StartedAt ?? h.ArchivedAt) >= from.Value);
        if (to.HasValue)
            archivedQuery = archivedQuery.Where(h =>
                (h.CompletedAt ?? h.StartedAt ?? h.ArchivedAt) <= to.Value);
        // Note: AssessmentAttemptHistory tidak punya Category/SubCategory column —
        // skip category/subCategory filter untuk assessment rows (sesuai existing behavior)

        var archivedRows = await archivedQuery
            .Select(h => new AllWorkersHistoryRow
            {
                WorkerId      = h.UserId,
                WorkerName    = h.User != null ? h.User.FullName : h.UserId,
                WorkerNIP     = h.User != null ? h.User.NIP : null,
                RecordType    = "Assessment Online",
                Title         = h.Title,
                Date          = (h.CompletedAt ?? h.StartedAt ?? h.ArchivedAt) ?? DateTime.MinValue,
                Score         = h.Score,
                IsPassed      = h.IsPassed,
                AttemptNumber = h.AttemptNumber
            })
            .ToListAsync();

        // --- Current completed sessions ---
        var currentQuery = _context.AssessmentSessions
            .AsNoTracking()
            .Where(a => a.Status == "Completed");

        if (hasWorkerFilter)
            currentQuery = currentQuery.Where(a => workerIdList!.Contains(a.UserId));
        if (from.HasValue)
            currentQuery = currentQuery.Where(a => (a.CompletedAt ?? a.Schedule) >= from.Value);
        if (to.HasValue)
            currentQuery = currentQuery.Where(a => (a.CompletedAt ?? a.Schedule) <= to.Value);

        // Materialize current completed dengan projection (excl AttemptNumber)
        var currentRowsRaw = await currentQuery
            .Select(a => new {
                a.UserId,
                UserFullName = a.User != null ? a.User.FullName : a.UserId,
                UserNIP = a.User != null ? a.User.NIP : null,
                a.Title,
                Date = a.CompletedAt ?? a.Schedule,
                a.Score,
                a.IsPassed
            })
            .ToListAsync();

        // Compute archivedCountLookup (cross-table count)
        var archivedCountLookup = await _context.AssessmentAttemptHistory
            .AsNoTracking()
            .Where(h => !hasWorkerFilter || workerIdList!.Contains(h.UserId))
            .Where(h => h.Title != null)
            .GroupBy(h => new { h.UserId, h.Title })
            .Select(g => new { g.Key.UserId, g.Key.Title, Count = g.Count() })
            .ToDictionaryAsync(x => (x.UserId, x.Title!), x => x.Count);

        var currentRows = currentRowsRaw.Select(a =>
        {
            int attemptNumber;
            if (string.IsNullOrEmpty(a.Title))
            {
                attemptNumber = 1;   // CMP-11 from Plan 01 — preserve
            }
            else
            {
                var key = (a.UserId, a.Title);
                int archived = archivedCountLookup.TryGetValue(key, out var c) ? c : 0;
                attemptNumber = archived + 1;
            }
            return new AllWorkersHistoryRow
            {
                WorkerId      = a.UserId,
                WorkerName    = a.UserFullName,
                WorkerNIP     = a.UserNIP,
                RecordType    = "Assessment Online",
                Title         = a.Title ?? "",
                Date          = a.Date,
                Score         = a.Score,
                IsPassed      = a.IsPassed,
                AttemptNumber = attemptNumber
            };
        }).ToList();

        var assessmentRows = archivedRows.Concat(currentRows)
            .OrderBy(r => r.Title)
            .ThenByDescending(r => r.Date)
            .ToList();

        // --- Training history ---
        var trainingsQuery = _context.TrainingRecords
            .AsNoTracking()
            .AsQueryable();

        if (hasWorkerFilter)
            trainingsQuery = trainingsQuery.Where(t => workerIdList!.Contains(t.UserId));
        if (from.HasValue)
            trainingsQuery = trainingsQuery.Where(t =>
                (t.TanggalMulai ?? t.Tanggal) >= from.Value);
        if (to.HasValue)
            trainingsQuery = trainingsQuery.Where(t =>
                (t.TanggalMulai ?? t.Tanggal) <= to.Value);
        if (!string.IsNullOrEmpty(category))
            trainingsQuery = trainingsQuery.Where(t => t.Kategori == category);
        if (!string.IsNullOrEmpty(subCategory))
            trainingsQuery = trainingsQuery.Where(t => t.SubKategori == subCategory);

        var trainingRows = await trainingsQuery
            .Select(t => new AllWorkersHistoryRow
            {
                WorkerId      = t.UserId,
                WorkerName    = t.User != null ? t.User.FullName : t.UserId,
                WorkerNIP     = t.User != null ? t.User.NIP : null,
                RecordType    = "Manual",
                Title         = t.Judul ?? "",
                Date          = t.TanggalMulai ?? t.Tanggal,
                Penyelenggara = t.Penyelenggara,
                Kategori      = t.Kategori,        // Plan 01 Task 2 — preserve
                SubKategori   = t.SubKategori      // Plan 01 Task 2 — preserve
            })
            .OrderByDescending(r => r.Date)
            .ToListAsync();

        return (assessmentRows, trainingRows);
    }
    ```

    3. **Update Interface bila ada** — `Services/IWorkerDataService.cs`:
    ```csharp
    Task<(List<AllWorkersHistoryRow> assessment, List<AllWorkersHistoryRow> training)>
        GetAllWorkersHistory(
            IEnumerable<string>? workerIds = null,
            DateTime? from = null,
            DateTime? to = null,
            string? category = null,
            string? subCategory = null);
    ```

    4. **Verify callers tidak break** — `Controllers/CMPController.cs` L654, L707 panggil `GetAllWorkersHistory()` tanpa argumen. Karena semua optional params default null, backward compat. Tetapi optimization: pass filteredIds + from/to + category + subCategory supaya SQL push-down terpakai. Edit di Task 2.

    5. **Build:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - `GetAllWorkersHistory` accept 5 optional filter params
    - Backward compat: panggil tanpa argumen menghasilkan output identik dengan sebelumnya
    - IQueryable composition + `.Select` projection di SQL (no full entity load)
    - Interface updated bila ada
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Date filter SQL push-down di GetWorkersInSection + caller export gunakan filter params (CMP-25)</name>
  <files>Services/WorkerDataService.cs, Controllers/CMPController.cs</files>
  <behavior>
    Setelah refactor:
    - `GetWorkersInSection` hapus `.Include(u => u.TrainingRecords)` di L179
    - Load `TrainingRecords` separate query dengan SQL date filter (composed IQueryable Where)
    - Load `AssessmentSessions` juga dengan date filter (extend existing L201)
    - `foreach (var user in users)` loop tetap, TAPI tidak lagi `.Where(tr => effectiveDate >= ...)` in-memory — gunakan pre-filtered lookup
    - Export endpoints (`ExportRecordsTeamAssessment`, `ExportRecordsTeamTraining`) pass `filteredIds + from + to + category + subCategory` ke `GetAllWorkersHistory` supaya SQL push-down terpakai end-to-end
  </behavior>
  <action>
    1. **Edit `Services/WorkerDataService.cs` GetWorkersInSection L175-311** — restructure date filter SQL:

    Replace L177-251 (sebelum category narrow yang sudah di Plan 01):
    ```csharp
    public async Task<List<WorkerTrainingStatus>> GetWorkersInSection(
        string? section,
        string? unitFilter = null,
        string? category = null,
        string? search = null,
        string? statusFilter = null,
        DateTime? dateFrom = null,
        DateTime? dateTo = null,
        string? subCategory = null)
    {
        var usersQuery = _context.Users
            .Where(u => u.IsActive)
            .AsQueryable();
        // ← HAPUS .Include(u => u.TrainingRecords) — load separate dengan SQL date filter

        if (!string.IsNullOrEmpty(section))
            usersQuery = usersQuery.Where(u => u.Section == section);

        if (!string.IsNullOrEmpty(unitFilter))
            usersQuery = usersQuery.Where(u => u.Unit == unitFilter);

        if (!string.IsNullOrEmpty(search))
        {
            search = search.ToLower();
            usersQuery = usersQuery.Where(u =>
                u.FullName.ToLower().Contains(search) ||
                (u.NIP != null && u.NIP.Contains(search))
            );
        }

        var users = await usersQuery.AsNoTracking().ToListAsync();
        var userIds = users.Select(u => u.Id).ToList();

        // Load TrainingRecords dengan SQL date filter (CMP-25)
        var trainingsQuery = _context.TrainingRecords
            .AsNoTracking()
            .Where(tr => userIds.Contains(tr.UserId));

        if (dateFrom.HasValue)
            trainingsQuery = trainingsQuery.Where(tr =>
                (tr.TanggalMulai ?? tr.Tanggal) >= dateFrom.Value);
        if (dateTo.HasValue)
            trainingsQuery = trainingsQuery.Where(tr =>
                (tr.TanggalMulai ?? tr.Tanggal) <= dateTo.Value);

        var trainingsByUser = (await trainingsQuery.ToListAsync())
            .GroupBy(tr => tr.UserId)
            .ToDictionary(g => g.Key, g => g.ToList());

        // Load AssessmentSessions dengan SQL date filter
        var sessionsQuery = _context.AssessmentSessions
            .AsNoTracking()
            .Where(a => userIds.Contains(a.UserId));

        if (dateFrom.HasValue)
            sessionsQuery = sessionsQuery.Where(a =>
                (a.CompletedAt ?? a.Schedule) >= dateFrom.Value);
        if (dateTo.HasValue)
            sessionsQuery = sessionsQuery.Where(a =>
                (a.CompletedAt ?? a.Schedule) <= dateTo.Value);

        var sessionsByUser = (await sessionsQuery.ToListAsync())
            .GroupBy(a => a.UserId)
            .ToDictionary(g => g.Key, g => g.ToList());

        // Untuk passed assessment lookup (preserve existing optimization — non date-filtered version)
        // saat dateFilter tidak aktif. Bila date filter aktif, gunakan sessionsByUser.
        bool hasDateFilter = dateFrom.HasValue || dateTo.HasValue;
        var passedAssessmentLookup = hasDateFilter
            ? sessionsByUser.ToDictionary(kv => kv.Key, kv => kv.Value.Count(a => a.IsPassed == true))
            : await _context.AssessmentSessions
                .AsNoTracking()
                .Where(a => userIds.Contains(a.UserId) && a.IsPassed == true)
                .GroupBy(a => a.UserId)
                .Select(g => new { UserId = g.Key, Count = g.Count() })
                .ToDictionaryAsync(x => x.UserId, x => x.Count);

        var workerList = new List<WorkerTrainingStatus>();

        foreach (var user in users)
        {
            var trainingRecords = trainingsByUser.TryGetValue(user.Id, out var trs) ? trs : new List<TrainingRecord>();
            var assessmentSessions = sessionsByUser.TryGetValue(user.Id, out var sess) ? sess : new List<AssessmentSession>();

            // Skip worker if no records fall within date range (CMP-25 — preserve existing semantic)
            if (hasDateFilter && trainingRecords.Count == 0 && assessmentSessions.Count == 0)
                continue;

            int completedAssessments = passedAssessmentLookup.TryGetValue(user.Id, out var aCount) ? aCount : 0;

            var totalTrainings = trainingRecords.Count;
            var completedTrainings = trainingRecords.Count(tr =>
                tr.Status == "Passed" || tr.Status == "Valid" || tr.Status == "Permanent"
            );
            var pendingTrainings = trainingRecords.Count(tr => tr.Status == "Pending");
            var expiringTrainings = trainingRecords.Count(tr => tr.IsExpiringSoon);

            var worker = new WorkerTrainingStatus
            {
                WorkerId = user.Id,
                WorkerName = user.FullName,
                NIP = user.NIP,
                Position = user.Position ?? "Staff",
                Section = user.Section ?? "",
                Unit = user.Unit ?? "",
                TotalTrainings = totalTrainings,
                CompletedTrainings = completedTrainings,
                PendingTrainings = pendingTrainings,
                ExpiringSoonTrainings = expiringTrainings,
                TrainingRecords = trainingRecords,
                CompletedAssessments = completedAssessments,
                AssessmentSessions = assessmentSessions
            };

            // Category CompletionPercentage (preserved dari Plan 01 Task 1)
            if (!string.IsNullOrEmpty(category))
            {
                bool isCompleted = trainingRecords.Any(r =>
                    !string.IsNullOrEmpty(r.Kategori) &&
                    string.Equals(r.Kategori, category, StringComparison.OrdinalIgnoreCase) &&
                    (r.Status == "Passed" || r.Status == "Valid" || r.Status == "Permanent")
                );
                worker.CompletionPercentage = isCompleted ? 100 : 0;
            }
            else
            {
                worker.CompletionPercentage = totalTrainings > 0
                    ? (int)((double)completedTrainings / totalTrainings * 100)
                    : 0;
            }

            workerList.Add(worker);
        }

        // Category narrow (Plan 01 Task 1)
        if (!string.IsNullOrEmpty(category))
        {
            workerList = workerList.Where(w =>
                w.TrainingRecords.Any(t => !string.IsNullOrEmpty(t.Kategori) &&
                                           string.Equals(t.Kategori, category, StringComparison.OrdinalIgnoreCase))
                || w.AssessmentSessions.Any(a => !string.IsNullOrEmpty(a.Category) &&
                                                  string.Equals(a.Category, category, StringComparison.OrdinalIgnoreCase))
            ).ToList();
        }

        // SubCategory narrow (Plan 01 Task 1)
        if (!string.IsNullOrEmpty(subCategory))
        {
            workerList = workerList.Where(w =>
                w.TrainingRecords.Any(t => !string.IsNullOrEmpty(t.SubKategori) &&
                                           string.Equals(t.SubKategori, subCategory, StringComparison.OrdinalIgnoreCase))
            ).ToList();
        }

        // Status filter (Plan 01 Task 1)
        if (!string.IsNullOrEmpty(statusFilter) && statusFilter != "ALL")
        {
            if (statusFilter == "Sudah")
                workerList = workerList.Where(w => w.CompletionPercentage == 100).ToList();
            else if (statusFilter == "Belum")
                workerList = workerList.Where(w => w.CompletionPercentage != 100).ToList();
        }

        return workerList;
    }
    ```

    Note PENTING: refactor ini PRESERVE semua Plan 01 fix (Category narrow + SubCategory narrow + Status guard removal). Jangan regress.

    2. **Edit `Controllers/CMPController.cs` ExportRecordsTeamAssessment L637-686** — pass filter params ke GetAllWorkersHistory:
    ```csharp
    // Get filtered worker IDs
    var filteredWorkers = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, null, search, statusFilter, from, to, subCategory);
    var filteredIds = filteredWorkers.Select(w => w.WorkerId).ToList();

    // SQL push-down: pass filteredIds + date range + category to service
    var (assessmentRows, _) = await _workerDataService.GetAllWorkersHistory(
        workerIds: filteredIds,
        from: from,
        to: to,
        category: null,         // Assessment tidak filter by category column
        subCategory: null);

    // No need lagi memory filter — assessmentRows sudah pre-filtered
    var filtered = assessmentRows;
    ```

    3. **Edit `Controllers/CMPController.cs` ExportRecordsTeamTraining L690-737** — sama:
    ```csharp
    var filteredWorkers = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, search, statusFilter, from, to, subCategory);
    var filteredIds = filteredWorkers.Select(w => w.WorkerId).ToList();

    var (_, trainingRows) = await _workerDataService.GetAllWorkersHistory(
        workerIds: filteredIds,
        from: from,
        to: to,
        category: category,
        subCategory: subCategory);

    var filtered = trainingRows;
    ```

    4. **Build:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - `GetWorkersInSection` tidak lagi `.Include(TrainingRecords)`, load separate dengan SQL date filter
    - `sessionsByUser` + `trainingsByUser` lookups di-build dari SQL-filtered queries
    - Plan 01 fix preserved (Category/SubCategory narrow + Status guard)
    - Export endpoints pass filter params end-to-end
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Pagination Team View — Controller + UI + sessionStorage page state (CMP-26, D-02)</name>
  <files>Controllers/CMPController.cs, Views/CMP/RecordsTeam.cshtml</files>
  <behavior>
    Setelah implement:
    - `RecordsTeamPartial` signature tambah `int page = 1, int pageSize = 20`
    - `pageSize` validated ke whitelist (20/50/100), default 20 bila invalid (D-02)
    - `PaginationHelper.Calculate(workerList.Count, page, pageSize)` dipanggil setelah filter applied
    - `Skip(paging.Skip).Take(paging.Take)` slice worker list
    - Response header `X-Pagination` berisi JSON `{ CurrentPage, TotalPages, TotalCount, Take }`
    - `RecordsTeam.cshtml` shell render pagination UI: page size selector + numeric pager (Bootstrap pagination component)
    - JS `doFetch` parse `X-Pagination` header, render pagination nav
    - Klik page button → set page state + re-fetch
    - Change page size → reset page to 1 + re-fetch
    - sessionStorage `cmp-records-team-pagination` persist `{page, pageSize}` (OQ-337-3)
    - Filter change RESET page to 1 (jangan stuck di page 5 saat filter narrow ke 1 page)
  </behavior>
  <action>
    1. **Edit `Controllers/CMPController.cs` RecordsTeamPartial L740-758:**
    ```csharp
    [HttpGet]
    public async Task<IActionResult> RecordsTeamPartial(
        string? section, string? unit, string? category, string? subCategory,
        string? statusFilter, string? dateFrom, string? dateTo,
        int page = 1, int pageSize = 20)
    {
        var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
        if (user == null) return RedirectToAction("Login", "Account");
        if (roleLevel >= 5) return Forbid();

        string? sectionFilter = (roleLevel == 4 && !string.IsNullOrEmpty(user.Section))
            ? user.Section : section;

        DateTime? from = DateTime.TryParse(dateFrom, out var parsedFrom) ? parsedFrom : null;
        DateTime? to = DateTime.TryParse(dateTo, out var parsedTo) ? parsedTo : null;

        var workerList = await _workerDataService.GetWorkersInSection(
            sectionFilter, unit, category, null, statusFilter, from, to, subCategory);

        // Pagination (CMP-26, D-02: numeric pager + 20/page default, 50/100 option)
        var pageSizeValidated = (pageSize == 20 || pageSize == 50 || pageSize == 100) ? pageSize : 20;
        var paging = HcPortal.Helpers.PaginationHelper.Calculate(workerList.Count, page, pageSizeValidated);
        var pagedWorkerList = workerList.Skip(paging.Skip).Take(paging.Take).ToList();

        // Response header untuk pagination data (JS read di doFetch)
        Response.Headers.Append("X-Pagination", System.Text.Json.JsonSerializer.Serialize(new
        {
            paging.CurrentPage,
            paging.TotalPages,
            paging.TotalCount,
            PageSize = paging.Take
        }));

        return PartialView("_RecordsTeamBody", pagedWorkerList);
    }
    ```

    Tambah `using` `HcPortal.Helpers` di top file bila belum ada (atau FQN `HcPortal.Helpers.PaginationHelper.Calculate`).

    2. **Edit `Views/CMP/RecordsTeam.cshtml`** — tambah pagination UI di bawah `#workerTable` (sebelum closing `</div>` card body). Tambah block setelah L151:
    ```cshtml
    </div>  @* close table-responsive *@
    </div>  @* close card-body *@
    @* Pagination UI *@
    <div class="card-footer bg-white border-top-0">
        <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
            <div class="d-flex align-items-center">
                <label class="form-label small text-muted mb-0 me-2">Tampilkan:</label>
                <select id="pageSizeSelector" class="form-select form-select-sm" style="width:auto">
                    <option value="20" selected>20</option>
                    <option value="50">50</option>
                    <option value="100">100</option>
                </select>
                <span class="text-muted small ms-2" id="paginationInfo">Page 1 of 1</span>
            </div>
            <nav aria-label="Worker pagination">
                <ul class="pagination pagination-sm mb-0" id="paginationNav">
                    @* Diisi JS via renderPagination() *@
                </ul>
            </nav>
        </div>
    </div>
    </div>  @* close card *@
    ```

    Note: PERHATIKAN closing tag yang sudah ada di file — adjust placement supaya HTML structure valid (`<div class="card">` → `<div class="card-body p-0">` → `<div class="table-responsive">` → table → close ke card → tambah card-footer → close card).

    Edit struktur L129-152:
    ```cshtml
    <div class="card border-0 shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0" id="workerTable">
                    <thead class="sticky-header">
                        ...
                    </thead>
                    <tbody id="workerTableBody">
                        <partial name="_RecordsTeamBody" model="Model" />
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer bg-white">
            <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
                <div class="d-flex align-items-center">
                    <label class="form-label small text-muted mb-0 me-2">Tampilkan:</label>
                    <select id="pageSizeSelector" class="form-select form-select-sm" style="width:auto">
                        <option value="20" selected>20</option>
                        <option value="50">50</option>
                        <option value="100">100</option>
                    </select>
                    <span class="text-muted small ms-2" id="paginationInfo">Page 1 of 1</span>
                </div>
                <nav aria-label="Worker pagination">
                    <ul class="pagination pagination-sm mb-0" id="paginationNav"></ul>
                </nav>
            </div>
        </div>
    </div>
    ```

    3. **Edit `<script>` block — tambah pagination state + renderPagination + page reset on filter change:**

    Tambah di top:
    ```javascript
    let currentPage = 1;
    let currentPageSize = 20;

    function getPaginationState() {
        return { page: currentPage, pageSize: currentPageSize };
    }

    function savePaginationState() {
        sessionStorage.setItem('cmp-records-team-pagination',
            JSON.stringify(getPaginationState()));
    }

    function restorePaginationState() {
        var stored = sessionStorage.getItem('cmp-records-team-pagination');
        if (!stored) return;
        try {
            var s = JSON.parse(stored);
            if (s.page) currentPage = s.page;
            if (s.pageSize) {
                currentPageSize = s.pageSize;
                document.getElementById('pageSizeSelector').value = s.pageSize;
            }
        } catch (e) { /* ignore */ }
    }
    ```

    Modify `getFilterState()` (Plan 02) — TIDAK include page/pageSize (separate concern).

    Modify `filterTeamTable()`:
    ```javascript
    function filterTeamTable() {
        // Filter change → RESET page ke 1 (CMP-26 UX guard)
        currentPage = 1;
        savePaginationState();
        updateExportLinks();
        saveFilterState();
        clearTimeout(debounceTimer);
        debounceTimer = setTimeout(doFetch, 300);
    }
    ```

    Modify `doFetch` — tambah page/pageSize ke params + parse X-Pagination header:
    ```javascript
    async function doFetch() {
        if (currentAbortController) currentAbortController.abort();
        currentAbortController = new AbortController();

        const params = new URLSearchParams();
        const s = getFilterState();
        if (s.section) params.set('section', s.section);
        if (s.unit) params.set('unit', s.unit);
        if (s.category) params.set('category', s.category);
        if (s.subCategory) params.set('subCategory', s.subCategory);
        if (s.status && s.status !== 'ALL') params.set('statusFilter', s.status);
        if (s.dateFrom) params.set('dateFrom', s.dateFrom);
        if (s.dateTo) params.set('dateTo', s.dateTo);
        params.set('page', currentPage);
        params.set('pageSize', currentPageSize);

        document.getElementById('tableLoadingIndicator').style.display = '';

        try {
            const resp = await fetch('@Url.Action("RecordsTeamPartial", "CMP")?' + params.toString(), {
                headers: { 'X-Requested-With': 'XMLHttpRequest' },
                signal: currentAbortController.signal
            });
            if (!resp.ok) throw new Error('Server error');
            const html = await resp.text();
            document.getElementById('workerTableBody').innerHTML = html;
            const count = document.querySelectorAll('#workerTableBody .worker-row').length;
            document.getElementById('workerCount').textContent = count;

            // Parse pagination header (CMP-26)
            var pHeader = resp.headers.get('X-Pagination');
            if (pHeader) {
                try {
                    var paging = JSON.parse(pHeader);
                    renderPagination(paging);
                } catch (e) { console.warn('pagination parse fail', e); }
            }

            updateDateHint(count, s);
        } catch (e) {
            if (e.name === 'AbortError') return;
            document.getElementById('workerTableBody').innerHTML =
                '<tr><td colspan="8" class="text-center text-danger p-4">Gagal memuat data. Coba lagi.</td></tr>';
        } finally {
            document.getElementById('tableLoadingIndicator').style.display = 'none';
        }
    }
    ```

    Tambah `renderPagination`:
    ```javascript
    function renderPagination(paging) {
        var nav = document.getElementById('paginationNav');
        var info = document.getElementById('paginationInfo');
        info.textContent = `Page ${paging.CurrentPage} dari ${paging.TotalPages} (${paging.TotalCount} workers)`;

        nav.innerHTML = '';
        if (paging.TotalPages <= 1) return;

        // Prev
        var prevDisabled = paging.CurrentPage <= 1 ? 'disabled' : '';
        nav.insertAdjacentHTML('beforeend',
            `<li class="page-item ${prevDisabled}"><a class="page-link page-nav" data-page="${paging.CurrentPage - 1}" href="#">&laquo;</a></li>`);

        // Numeric pages (sliding window max 7 buttons)
        var start = Math.max(1, paging.CurrentPage - 3);
        var end = Math.min(paging.TotalPages, start + 6);
        if (end - start < 6) start = Math.max(1, end - 6);
        for (var i = start; i <= end; i++) {
            var active = i === paging.CurrentPage ? 'active' : '';
            nav.insertAdjacentHTML('beforeend',
                `<li class="page-item ${active}"><a class="page-link page-nav" data-page="${i}" href="#">${i}</a></li>`);
        }

        // Next
        var nextDisabled = paging.CurrentPage >= paging.TotalPages ? 'disabled' : '';
        nav.insertAdjacentHTML('beforeend',
            `<li class="page-item ${nextDisabled}"><a class="page-link page-nav" data-page="${paging.CurrentPage + 1}" href="#">&raquo;</a></li>`);

        // Wire click handlers
        nav.querySelectorAll('.page-nav').forEach(a => {
            a.addEventListener('click', (e) => {
                e.preventDefault();
                var newPage = parseInt(a.dataset.page);
                if (newPage < 1 || newPage > paging.TotalPages) return;
                if (a.parentElement.classList.contains('disabled')) return;
                currentPage = newPage;
                savePaginationState();
                doFetch();
            });
        });
    }
    ```

    Wire page size selector:
    ```javascript
    document.addEventListener('DOMContentLoaded', function() {
        // ... existing wire-up (categoryFilter change, etc) ...

        // Page size selector (D-02)
        document.getElementById('pageSizeSelector').addEventListener('change', function() {
            currentPageSize = parseInt(this.value) || 20;
            currentPage = 1;   // reset
            savePaginationState();
            doFetch();
        });

        // Restore pagination state on load
        restorePaginationState();

        // Trigger initial fetch dengan pagination params (kalau ada restored state non-default)
        // Existing initial render is server-side rendered tanpa pagination — tetapi pagination UI
        // harus accurate. Solusi: trigger doFetch() initial sekali untuk render pagination.
        doFetch();
    });
    ```

    4. **Build:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```

    5. **EF Core query logging sanity check** — temporary aktifkan logging untuk verify SQL output:

    Edit `appsettings.json` tambah:
    ```json
    "Logging": {
      "LogLevel": {
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
    ```

    Run `dotnet run`, akses `/CMP/Records` Team View tab, filter Category + Date range. Inspect console log — verify:
    - GetAllWorkersHistory: 3 SELECT (archived + completed + trainings + 1 groupby count) — total ~4 queries, BUKAN N+1
    - GetWorkersInSection: ~3-4 SELECT (users + trainings + sessions + passedAssessment), BUKAN per-worker N+1
    - Date filter: WHERE clause harus muncul di SQL output (e.g., `WHERE [t].[TanggalMulai] >= @p_dateFrom`)

    Bila ada N+1 atau missing WHERE → fix sebelum claim done.

    Setelah verify, REVERT appsettings.json (jangan commit logging level Info ke prod).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - `RecordsTeamPartial` signature accept page/pageSize, validated (20/50/100)
    - `PaginationHelper.Calculate` + Skip/Take applied
    - Response header `X-Pagination` JSON ada
    - `RecordsTeam.cshtml` punya pagination UI (page size selector + numeric pager)
    - JS `renderPagination` + `restorePaginationState` + page reset on filter change
    - sessionStorage `cmp-records-team-pagination` set/restore
    - EF Core query log verify: single SELECT per category, WHERE date di SQL (manual check)
    - `dotnet build` PASS
    - **`appsettings.json` REVERTED** (logging level Info untuk EFCore.Database.Command DIHAPUS sebelum commit) — verify: `git diff appsettings.json` → no changes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — Final UAT Wave 3 + Regression Wave 1/2 + SQL profile</name>
  <files>N/A — checkpoint task, verification gating only (no file modification by Claude di task ini)</files>
  <action>Wait for user manual verification per how-to-verify steps. Tidak ada code change di task ini — semua code change selesai di task sebelumnya. Tugas Claude: PAUSE eksekusi sampai user respond resume-signal.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User reply 'approved' di resume-signal — verified semua acceptance criteria per how-to-verify section PASS browser-side.</done>
  <what-built>
    Wave 3 selesai code-side:
    - 3 REQ (CMP-24, CMP-25, CMP-26) implemented
    - GetAllWorkersHistory IQueryable composition, GetWorkersInSection date filter SQL push-down, pagination Team View
    - 5 file modified
    - `dotnet build` PASS

    Phase 337 = 26/26 REQ ready ship. Final UAT WAJIB sebelum claim phase complete (bundle v20.0).
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Backup DB lokal SEBELUM test (per `docs/SEED_WORKFLOW.md`):
       ```bash
       sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "BACKUP DATABASE [HCPortalDb] TO DISK='C:\Temp\HCPortalDb_pre_337-03_uat.bak'"
       ```
    2. Restart `dotnet run`
    3. Login `admin@pertamina.com` / `123456`
    4. DevTools: Console + Network tab + Application > Session Storage

    **A. Pagination Team View (CMP-26):**

    A1. Navigate `/CMP/Records` Team View tab
    - Expected: pagination footer muncul dengan:
      - Page size selector default "20" (atau restored dari session)
      - "Page X dari Y (Z workers)" info text
      - Numeric pager `« 1 2 3 4 5 »` (sliding window 7 buttons max)
    - Klik page 2 → table refresh, button "2" active
    - Klik »  → page+1, refresh
    - Klik « di page 1 → disabled (no action)

    A2. Change page size:
    - Pilih "50" → table refresh dengan 50 row, page back to 1, info update
    - Pilih "100" → similar

    A3. Filter + page reset:
    - Di page 3, ubah Category → expected: page reset ke 1, fetch dengan filter

    A4. Pagination state persist:
    - Di page 3 + pageSize 50
    - Switch tab → kembali
    - Expected: pageSize 50 restored, page kembali (might be reset to 1 jika filter changed — acceptable)
    - DevTools Session Storage: `cmp-records-team-pagination` JSON exists

    **B. SQL push-down verify (CMP-24, CMP-25):**

    B1. EF Core logging sanity:
    - Aktifkan logging level Info untuk EFCore.Database.Command (sementara — REVERT setelah verify)
    - Trigger fetch dengan Date range filter
    - Console log harus muncul: `WHERE [t].[TanggalMulai] IS NOT NULL... >= @p_...`
    - Tidak ada multiple round-trip per worker (N+1)
    - GetAllWorkersHistory dipanggil dari Export — log harus tunjukkan SELECT dengan WHERE workerIds IN (...)

    B2. SQL Server Profiler (optional, lebih akurat):
    - Open SSMS → Tools → SQL Server Profiler
    - Filter event SP:StmtCompleted untuk database HCPortalDb
    - Click "Export Training" dari /CMP/Records dengan Category + Date filter
    - Expected: query trainings punya WHERE Kategori = + WHERE TanggalMulai >= clause di SQL trace
    - BUKAN: SELECT * FROM TrainingRecords lalu app filter

    B3. Performance smoke:
    - Hapus DB filter (no date, no category) di Team View
    - Network tab timing untuk RecordsTeamPartial response
    - Compare dengan baseline (pre-Wave-3) — should be similar atau lebih cepat untuk org < 100 worker (no regression)
    - Untuk org > 500 worker, expected response < 2s (vs pre-fix bisa > 5s)

    **C. Regression smoke Wave 1 + Wave 2:**

    C1. 5 CRITICAL filter Wave 1 masih bekerja:
    - Status without Category → narrow
    - SubCategory narrow
    - Category narrow + equality
    - Export Training honor subCategory

    C2. 6 data integrity Wave 1:
    - SertifikatUrl link tampil di My Records
    - Permanent badge tampil
    - Counter update real-time
    - HtmlEncode search "&" match
    - Status Completed null → "Completed" (bukan "Failed")

    C3. 12 UX/Quality Wave 2:
    - AbortController cancel late fetch
    - sessionStorage filter persist
    - Year quick filter button group My Records
    - Keyboard nav row Enter trigger navigate
    - ARIA tab role + aria-selected toggle
    - ViewModel — view tidak panggil UserManager.GetUserAsync

    **D. Export end-to-end:**

    D1. Set filter Section + Category + SubCategory + Date range
    - Click "Export Assessment" → xlsx download
    - Open: verify rows sesuai filter (Worker IDs sesuai Section filtered, Date in range)
    - Click "Export Training" → xlsx download
    - Open: verify Kategori column sesuai filter, SubKategori sesuai filter, Date in range

    **E. EXPLAIN PLAN sanity (optional final check):**
    - SSMS → connect DB
    - Query: `SET STATISTICS PROFILE ON; EXEC <RecordsTeamPartial generated query>`
    - Verify: estimated rows reasonable, no full table scan untuk filtered query

    **F. Cleanup:**
    - REVERT appsettings.json logging level (kalau diaktifkan B1) — verify: `git diff appsettings.json` → no changes
    - Clear sessionStorage
    - Restore DB bila ada temp seed (per `docs/SEED_WORKFLOW.md`):
       ```bash
       sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "RESTORE DATABASE [HCPortalDb] FROM DISK='C:\Temp\HCPortalDb_pre_337-03_uat.bak' WITH REPLACE"
       ```
    - Update `docs/SEED_JOURNAL.md`

    **G. Document UAT result:**
    Format per REQ-ID di SUMMARY:
    ```
    - CMP-24: PASS — IQueryable composition, EF log verify single SELECT per query
    - CMP-25: PASS — Date filter WHERE clause di SQL trace
    - CMP-26: PASS — Pagination UI works, page reset on filter, sessionStorage persist
    ```
  </how-to-verify>
  <resume-signal>
    Ketik "approved" bila ALL 3 REQ Wave 3 + regression Wave 1/2 + SQL profile PASS. Phase 337 ready ship (bundle v20.0). Bila FAIL, sebutkan REQ-ID + detail.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RecordsTeamPartial page/pageSize param | Untrusted, integer parsing required |
| EF Core IQueryable → SQL Server | Parameterized via EF, low SQL injection risk |
| Response header X-Pagination → JS parse | Trusted server-set, JSON parse safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-337-03-01 | Tampering | pageSize injection `?pageSize=99999` → DoS via large page | mitigate | Whitelist validation di controller: `(pageSize == 20 || 50 || 100) ? pageSize : 20`. Invalid value default ke 20. |
| T-337-03-02 | Information Disclosure | Pagination expose total count data cross-section | mitigate | TotalCount di X-Pagination header HANYA workerList yang sudah-difilter via L4 section lock (existing). Tidak ada cross-section leakage. |
| T-337-03-03 | Denial of Service | SQL push-down regression generate cartesian product | mitigate | EF Core log verify single SELECT per category (Task 3 step 5). EXPLAIN PLAN sanity check optional (Task 4 E). |
| T-337-03-04 | Repudiation | Pagination state tidak ter-audit | accept | Read-only UI state, no audit required. |
| T-337-03-05 | Elevation of Privilege | page param bypass L4 scope | mitigate | Pagination apply SETELAH `GetWorkersInSection` (which honors L4 section lock di L749). Pagination cuma slice already-scoped list. |
| T-337-03-06 | Information Disclosure | Response header X-Pagination bocor ke proxy log | accept | TotalCount + page count tidak sensitive — sama level dengan UI display. Production: jika sensitif, pakai response body wrapping (refactor Phase 999.x). |
</threat_model>

<verification>
- `dotnet build` PASS
- 3 REQ (CMP-24, CMP-25, CMP-26) browser-verified
- Wave 1 + Wave 2 regression PASS
- EF Core log atau SQL Profiler verify SQL push-down (no N+1, WHERE clause di SQL)
- Pagination UI numeric pager work (prev/next/numeric/disabled state)
- pageSize whitelist enforce
- sessionStorage pagination state persist
- L4 section lock regression (Wave 1 carryover)
</verification>

<success_criteria>
- [ ] 3 REQ (CMP-24, CMP-25, CMP-26) implemented + 5 file modified
- [ ] Phase 337 = 26/26 REQ delivered total (Wave 1+2+3)
- [ ] OQ-337-2 + OQ-337-3 resolved + documented
- [ ] SQL push-down verified via EF logging atau SQL Profiler (no N+1)
- [ ] Pagination UI + sessionStorage + page reset works
- [ ] Threats T-337-03-01/02/03/05 mitigated, T-337-03-04/06 accepted
- [ ] User "approved" sebelum claim Phase 337 complete
- [ ] Phase 337 ready bundle v20.0 push (NOT pushed di plan ini — defer ke milestone close)
</success_criteria>

<output>
Create `.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-03-SUMMARY.md`. Catat:
- 3 REQ closed (CMP-24, CMP-25, CMP-26) + OQ-337-2 + OQ-337-3 resolved
- Files modified (5 file)
- SQL push-down verification: EF log/Profiler result
- Pagination smoke result + page reset behavior
- Threats mitigated/accepted
- **PHASE 337 SUMMARY**: 26/26 REQ delivered (Wave 1 11 + Wave 2 12 + Wave 3 3)
- Bundle status: NOT pushed yet (defer v20.0 milestone close, follow Phase 325-335 pattern)
- Next: Phase 338 Cilacap UX + Restore (per ROADMAP v20.0)
</output>
