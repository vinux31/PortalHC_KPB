---
phase: 338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ManageAssessment.cshtml
  - Views/Admin/AssessmentMonitoring.cshtml
autonomous: false
requirements: [CIL-01, CIL-02]
must_haves:
  truths:
    - "Tab list /Admin/ManageAssessment + /Admin/AssessmentMonitoring tampilkan badge counter Closed di samping option filter Closed (CIL-01)"
    - "Default filter TETAP 'Open' (Closed disembunyikan by default) — no breaking change user lama (CIL-01)"
    - "Saat user search 'Cilacap' tanpa pilih status (search non-empty), parent group Closed MUNCUL di hasil aggregation (CIL-02)"
    - "Saat statusFilter explicit di-set 'Open', Closed group TETAP disembunyikan walau search ada (preserve existing behavior)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ManageAssessmentTab_Assessment search guard fix + count Closed aggregate query"
      contains: "string.IsNullOrEmpty(search)"
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Badge counter Closed di filter dropdown/tab"
      contains: "badge bg-secondary"
    - path: "Views/Admin/AssessmentMonitoring.cshtml"
      provides: "Badge counter Closed di filter dropdown/tab"
      contains: "badge bg-secondary"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs L198-201"
      to: "grouped.Where(GroupStatus != 'Closed')"
      via: "guard '&& string.IsNullOrEmpty(search)' tambahkan"
      pattern: "GroupStatus != \"Closed\".*string\\.IsNullOrEmpty\\(search\\)"
    - from: "Views/Admin/ManageAssessment.cshtml status filter dropdown"
      to: "ViewBag.ClosedCount"
      via: "render badge bg-secondary"
      pattern: "ViewBag\\.ClosedCount"
---

<objective>
Wave 1 Phase 338 — Fix 2 gap UX dasar di Admin Assessment Monitoring: (1) tambah badge counter Closed di tab/filter dropdown supaya user discover ada session Closed (D-01 — preserve default Open filter), (2) fix search aggregation supaya search "Cilacap" / kata kunci lokasi tetap return parent group Closed (Gap #2 — bug current: Closed di-filter di L198-201 walau search ada).

Purpose: Hilangkan silent-fail UX dimana user search lokasi tertentu (Cilacap, Dumai, Balongan, dst) dapat 0 result padahal data ada — root cause: aggregation query hard-filter `GroupStatus != "Closed"` ketika statusFilter kosong, sehingga search Cilacap (semua group Cilacap status Closed) return 0. Phase 338 Wave 1 ini blocking untuk Wave 2 (CIL-03 history drill-down) karena Wave 2 tergantung user dapat reach history tab dari search result.

Output:
- `Controllers/AssessmentAdminController.cs` `ManageAssessmentTab_Assessment` L198-201 patch — guard search non-empty supaya Closed include di hasil + tambah count aggregate Closed di-set ke ViewBag
- `Views/Admin/ManageAssessment.cshtml` — badge counter Closed di status filter dropdown
- `Views/Admin/AssessmentMonitoring.cshtml` — badge counter Closed sama pattern

Setelah Wave 1 selesai: search Cilacap di tab Assessment Groups return parent group Closed (verified via Playwright) DAN user lihat "(N) Closed" badge sebagai cue visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-CONTEXT.md
@.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-RESEARCH.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md

# File sumber yang akan dimodifikasi
@Controllers/AssessmentAdminController.cs
@Views/Admin/ManageAssessment.cshtml
@Views/Admin/AssessmentMonitoring.cshtml

<interfaces>
<!-- Key signatures dari current codebase L198-201 -->

From Controllers/AssessmentAdminController.cs L106 (current ManageAssessmentTab_Assessment signature):
```csharp
public async Task<IActionResult> ManageAssessmentTab_Assessment(string? search, int page = 1, int pageSize = 20,
    string? statusFilter = null, string? category = null, /* ... */)
```

From Controllers/AssessmentAdminController.cs L196-203 (current aggregation BUG — CIL-02 target):
```csharp
var grouped = prePostGrouped.Concat(standardGrouped)
    .OrderByDescending(g => g.Schedule)
    .ToList();

if (string.IsNullOrEmpty(statusFilter))
    grouped = grouped.Where(g => g.GroupStatus != "Closed").ToList();
                                  // ↑ CIL-02 BUG: filter Closed bahkan saat user search "Cilacap"
else if (statusFilter == "Open" || statusFilter == "Upcoming" || statusFilter == "Closed")
    grouped = grouped.Where(g => g.GroupStatus == statusFilter).ToList();
```

TARGET fix (CIL-02): tambah guard `&& string.IsNullOrEmpty(search)` supaya saat search ada (user mencari spesifik), Closed include di hasil:
```csharp
if (string.IsNullOrEmpty(statusFilter) && string.IsNullOrEmpty(search))
    grouped = grouped.Where(g => g.GroupStatus != "Closed").ToList();
else if (statusFilter == "Open" || statusFilter == "Upcoming" || statusFilter == "Closed")
    grouped = grouped.Where(g => g.GroupStatus == statusFilter).ToList();
```

Untuk CIL-01 badge counter, tambah aggregate count SEBELUM filter apply:
```csharp
// CIL-01: counter status untuk badge UI (sebelum apply statusFilter)
ViewBag.OpenCount     = grouped.Count(g => g.GroupStatus == "Open");
ViewBag.UpcomingCount = grouped.Count(g => g.GroupStatus == "Upcoming");
ViewBag.ClosedCount   = grouped.Count(g => g.GroupStatus == "Closed");
```

Same pattern di `ManageAssessmentTab_History` L277 dan `AssessmentMonitoring` action (cek file, kemungkinan similar grouped status pattern).

From Views/Admin/ManageAssessment.cshtml (existing status dropdown TARGET location):
```html
<!-- Sekitar status filter dropdown. Cek file actual untuk lokasi tepat. -->
<select name="statusFilter" id="statusFilter" class="form-select">
    <option value="">Semua Status</option>
    <option value="Open" @(ViewBag.SelectedStatus == "Open" ? "selected" : "")>Open</option>
    <option value="Upcoming" @(ViewBag.SelectedStatus == "Upcoming" ? "selected" : "")>Upcoming</option>
    <option value="Closed" @(ViewBag.SelectedStatus == "Closed" ? "selected" : "")>Closed</option>
</select>
```

TARGET: render badge counter dekat option Closed (atau di tab nav bila tab structure). Approach disesuaikan struktur view actual — kalau dropdown, tampilkan label tambahan; kalau tab nav `<a class="nav-link">`, tambah `<span class="badge bg-secondary ms-1">@ViewBag.ClosedCount</span>` inline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix search aggregation di ManageAssessmentTab_Assessment + ManageAssessmentTab_History (CIL-02)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <behavior>
    Setelah fix:
    - `ManageAssessmentTab_Assessment` L198-201: guard `if (string.IsNullOrEmpty(statusFilter))` di-extend dengan `&& string.IsNullOrEmpty(search)` — Closed include saat search non-empty
    - `ManageAssessmentTab_History` L277: cek pattern serupa; bila ada filter Closed berdasarkan statusFilter empty, apply guard sama
    - Saat user search "Cilacap" tanpa pilih status: Closed group Cilacap MUNCUL di hasil
    - Saat user pilih statusFilter "Open" + search "Cilacap": Closed Cilacap TETAP disembunyikan (preserve explicit filter intent)
    - Saat user kosongkan search + tidak pilih status: behavior LAMA preserve (Closed disembunyikan by default)
  </behavior>
  <action>
    **Pre-step (drift verify):** Sebelum edit, jalankan `grep -n "GroupStatus != \"Closed\"" Controllers/AssessmentAdminController.cs` confirm lokasi L198-201. Bila drift > ±5 baris, adjust line reference.

    1. Buka `Controllers/AssessmentAdminController.cs` baca L106-260 (seluruh `ManageAssessmentTab_Assessment` method).

    2. **Fix CIL-02 guard di L198** — tambah `&& string.IsNullOrEmpty(search)`:
    ```csharp
    // SEBELUM (L198):
    if (string.IsNullOrEmpty(statusFilter))
        grouped = grouped.Where(g => g.GroupStatus != "Closed").ToList();

    // SESUDAH:
    // CIL-02: Saat user search spesifik, jangan hard-filter Closed.
    // Hanya hide Closed default ketika BOTH statusFilter kosong DAN search kosong.
    if (string.IsNullOrEmpty(statusFilter) && string.IsNullOrEmpty(search))
        grouped = grouped.Where(g => g.GroupStatus != "Closed").ToList();
    ```

    3. **Set count ViewBag untuk badge** — TEPAT SEBELUM `if (string.IsNullOrEmpty(statusFilter)...)` block di L198, tambah:
    ```csharp
    // CIL-01: aggregate counter (sebelum apply status filter)
    ViewBag.OpenCount     = grouped.Count(g => g.GroupStatus == "Open");
    ViewBag.UpcomingCount = grouped.Count(g => g.GroupStatus == "Upcoming");
    ViewBag.ClosedCount   = grouped.Count(g => g.GroupStatus == "Closed");
    ```

    4. **Apply same pattern di `ManageAssessmentTab_History` L277-450**:
    - Cek struktur method via `sed -n '277,400p' Controllers/AssessmentAdminController.cs`
    - Bila ada filter Closed serupa berdasarkan statusFilter kosong, apply guard `&& string.IsNullOrEmpty(search)`
    - Set ViewBag.OpenCount / UpcomingCount / ClosedCount untuk badge (kalau view History mau pakai badge)
    - Bila method tidak punya filter Closed, SKIP (history tab semua status display, badge counter tetap tambahkan untuk visibility)

    5. **Apply same pattern di AssessmentMonitoring action** — cek lokasi via:
    ```bash
    grep -n "AssessmentMonitoring\|public.*Monitoring" Controllers/AssessmentAdminController.cs | head
    ```
    Apply guard + counter sama struktur.

    6. **Build sanity:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error CS" | head -5</automated>
  </verify>
  <done>
    - L198 guard di-extend `&& string.IsNullOrEmpty(search)` — CIL-02 fix
    - ViewBag.OpenCount/UpcomingCount/ClosedCount di-set SEBELUM filter apply — CIL-01 data
    - Same pattern applied di `ManageAssessmentTab_History` + `AssessmentMonitoring` action
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render badge counter Closed di Views/Admin/ManageAssessment.cshtml (CIL-01)</name>
  <files>Views/Admin/ManageAssessment.cshtml</files>
  <behavior>
    Setelah edit:
    - Tab nav / filter dropdown di view tampilkan badge counter Closed: `<span class="badge bg-secondary ms-1">@ViewBag.ClosedCount Closed</span>`
    - Format: badge gray, font size kecil, tooltip "X assessment dengan status Closed"
    - Default filter TIDAK berubah (Open masih default — D-01 preserve user lama)
    - Bonus: tambah badge Open + Upcoming juga untuk parity (counter konsisten 3 status)
    - Animate badge first load: subtle fade-in untuk catch user attention (mitigation T-338-05)
  </behavior>
  <action>
    1. **Baca view structure** untuk identifikasi lokasi tepat:
    ```bash
    grep -n "statusFilter\|Closed\|Open.*Upcoming\|nav-link.*tab\|form-select" Views/Admin/ManageAssessment.cshtml | head -30
    ```

    2. **Lokasi rendering badge** — 2 kemungkinan struktur:
    - **Opsi A — Dropdown filter:** Bila ada `<select name="statusFilter">`, tambah badge di label TERPISAH di atas/samping dropdown:
      ```cshtml
      <div class="d-flex align-items-center gap-2 mb-2">
          <label for="statusFilter" class="form-label mb-0">Status:</label>
          <span class="badge bg-success">@(ViewBag.OpenCount ?? 0) Open</span>
          <span class="badge bg-warning text-dark">@(ViewBag.UpcomingCount ?? 0) Upcoming</span>
          <span class="badge bg-secondary" title="Closed assessment (hidden by default — pilih filter Closed untuk lihat)">@(ViewBag.ClosedCount ?? 0) Closed</span>
      </div>
      ```
    - **Opsi B — Tab nav `<a class="nav-link">`:** Bila status di-render sebagai tab nav, tambah badge inline tiap tab:
      ```cshtml
      <a class="nav-link @(SelectedStatus == "Closed" ? "active" : "")" href="?statusFilter=Closed">
          Closed <span class="badge bg-secondary ms-1">@(ViewBag.ClosedCount ?? 0)</span>
      </a>
      ```

    Pilih sesuai struktur view actual. Bila view pakai dropdown saja, Opsi A. Bila ada tab nav per-status, Opsi B.

    3. **Subtle animate first-load** (T-338-05 mitigation) — tambah inline `<style>` di akhir view atau di section _Layout:
    ```html
    <style>
        .badge.bg-secondary[data-cil01-animate] {
            animation: cil01-pulse 1.5s ease-in-out 1;
        }
        @@keyframes cil01-pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; transform: scale(1.05); }
        }
    </style>
    ```
    Tambah `data-cil01-animate` attribute ke badge Closed.

    4. **Build + view file sanity:**
    ```bash
    dotnet build 2>&1 | tail -5
    ```
    Razor view error akan tampil di build output.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <done>
    - Badge counter Closed render di view dengan `@ViewBag.ClosedCount`
    - Badge Open + Upcoming juga render (parity)
    - Tooltip / data attribute hint "Closed hidden by default — pilih filter Closed untuk lihat"
    - Animate first-load CSS ditambah (T-338-05 mitigation)
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Render badge counter Closed di Views/Admin/AssessmentMonitoring.cshtml (CIL-01)</name>
  <files>Views/Admin/AssessmentMonitoring.cshtml</files>
  <behavior>
    Sama pattern dengan Task 2 tapi untuk view `AssessmentMonitoring.cshtml`. Badge counter Closed visible, default Open preserve, animate first load.
  </behavior>
  <action>
    1. Baca struktur `Views/Admin/AssessmentMonitoring.cshtml`:
    ```bash
    grep -n "statusFilter\|Closed\|nav-link\|form-select" Views/Admin/AssessmentMonitoring.cshtml | head -20
    ```

    2. Apply same badge rendering pattern dengan Task 2 — Opsi A (dropdown) atau Opsi B (tab nav) sesuai struktur view actual.

    3. **Animate first-load CSS** — kalau view sudah include layout dari Task 2 (shared _Layout.cshtml), CSS sudah available. Bila view ini standalone, copy `<style>` block dari Task 2.

    4. **Build sanity:**
    ```bash
    dotnet build 2>&1 | tail -5
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <done>
    - Badge counter Closed render di `AssessmentMonitoring.cshtml`
    - Pattern konsisten dengan ManageAssessment.cshtml
    - `dotnet build` PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — Playwright UAT CIL-01 badge + CIL-02 search aggregation</name>
  <files>N/A — checkpoint task, no file modification</files>
  <action>Wait for user manual verification per how-to-verify steps. Tidak ada code change di task ini. PAUSE eksekusi sampai user respond resume-signal.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User reply 'approved' di resume-signal.</done>
  <what-built>
    Wave 1 selesai code-side:
    - CIL-01 badge counter Closed render di `ManageAssessment` + `AssessmentMonitoring` view
    - CIL-02 search guard fix di `AssessmentAdminController` L198 — search "Cilacap" tanpa statusFilter return Closed group
    - 3 file modified: AssessmentAdminController.cs, ManageAssessment.cshtml, AssessmentMonitoring.cshtml
    - `dotnet build` PASS

    Belum ada Playwright verification bahwa filter behaviour aktual di browser. Wajib UAT sebelum Wave 2 (CIL-03 drill-down).
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Backup DB lokal (per `docs/SEED_WORKFLOW.md`):
       ```bash
       sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "BACKUP DATABASE [HCPortalDb] TO DISK='C:\Temp\HCPortalDb_pre_338-01_uat.bak'"
       ```
    2. Run app lokal: `dotnet run` → buka `http://localhost:5277`
    3. Login `admin@pertamina.com` / `123456`

    **A. UAT CIL-01 Badge Counter — gunakan Playwright MCP atau manual browser:**

    Navigate `http://localhost:5277/Admin/ManageAssessment`:

    **A1. Badge counter render:**
    - Expected: 3 badge visible di filter area — "(N) Open" hijau, "(N) Upcoming" kuning, "(N) Closed" abu
    - Verify nilai N akurat — bandingkan dengan SQL:
      ```sql
      SELECT GroupStatus = CASE
          WHEN CompletedAt IS NOT NULL THEN 'Closed'
          WHEN Schedule < GETUTCDATE() THEN 'Open'
          ELSE 'Upcoming'
      END, COUNT(*) FROM AssessmentSessions WHERE Status != 'Cancelled' GROUP BY ...
      ```
      (catatan: simplifikasi — exact query lihat ManageAssessmentTab_Assessment)
    - PASS criteria: jumlah badge match SQL count per status

    **A2. Animate first load:**
    - Hard refresh (Ctrl+F5) → badge Closed pulse 1.5s
    - PASS criteria: animation visible (mitigation T-338-05)

    **A3. Default filter behavior preserve:**
    - Tanpa pilih statusFilter, tab list TIDAK menampilkan Closed group
    - PASS criteria: D-01 preserve user lama, badge cuma counter visual

    Navigate `http://localhost:5277/Admin/AssessmentMonitoring`:
    - Repeat A1-A3 untuk view ini

    **B. UAT CIL-02 Search Aggregation Fix:**

    **B1. Setup seed data (kalau Cilacap data belum ada di DB lokal):**
    - Check via SQL:
      ```sql
      SELECT TOP 5 Id, Title, Status, CompletedAt FROM AssessmentSessions WHERE Title LIKE '%Cilacap%'
      ```
    - Bila tidak ada, create temporary seed Cilacap Closed session via SQL INSERT atau via admin create form set CompletedAt past date. Document di `docs/SEED_JOURNAL.md`.

    **B2. Test search Cilacap tanpa statusFilter:**
    - Navigate `http://localhost:5277/Admin/ManageAssessment?search=Cilacap` (atau via input box)
    - Expected: parent group Cilacap (status Closed) MUNCUL di hasil
    - Pre-fix behavior (bug): 0 result
    - PASS criteria: row count > 0, judul group berisi "Cilacap"

    **B3. Test search Cilacap + statusFilter=Open:**
    - URL: `http://localhost:5277/Admin/ManageAssessment?search=Cilacap&statusFilter=Open`
    - Expected: Closed Cilacap TETAP disembunyikan (preserve explicit filter intent)
    - PASS criteria: row count = jumlah Cilacap dengan status Open only

    **B4. Test search kosong + statusFilter kosong:**
    - URL: `http://localhost:5277/Admin/ManageAssessment` (clean)
    - Expected: behavior LAMA preserve — Closed disembunyikan, hanya Open + Upcoming display
    - PASS criteria: no Closed row in result

    **C. Regression smoke:**
    - Pagination masih bekerja (page=2, pageSize=20)
    - Category filter masih bekerja
    - Sort order (OrderByDescending Schedule) masih correct

    **D. Cleanup:**
    - Restore DB bila ada temporary Cilacap seed:
      ```bash
      sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "RESTORE DATABASE [HCPortalDb] FROM DISK='C:\Temp\HCPortalDb_pre_338-01_uat.bak' WITH REPLACE"
      ```
    - Update `docs/SEED_JOURNAL.md` mark cleaned
  </how-to-verify>
  <resume-signal>Ketik "approved" bila CIL-01 + CIL-02 PASS. Bila ada FAIL, sebut REQ-ID (e.g., "CIL-02 fail — search Cilacap masih 0 result").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → AssessmentAdminController | Untrusted query params (search, statusFilter, page, pageSize) crossing dari client request |
| Controller → AspNetCore Razor view | Internal trusted; ViewBag values di-render ke HTML (XSS risk bila ada user input) |
| Controller → DbContext | LINQ-to-EF query — parameterized via EF, low SQL injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-338-01-01 | Tampering | `search` param injection mengubah filter logic | mitigate | `search` di-pass ke `.Where(Title.Contains(search))` via EF parameterized — safe SQL-side. Guard `string.IsNullOrEmpty` cek explicit, tidak bypass-able via query string. |
| T-338-01-02 | Information Disclosure | User non-Admin bypass `Authorize(Roles="Admin,HC")` melihat Closed Cilacap data | mitigate | Existing `[Authorize(Roles = "Admin, HC")]` di controller class L100 PRESERVE. Task tidak menyentuh auth attribute. |
| T-338-01-03 | Information Disclosure | Badge counter expose total Closed jumlah ke user yang tidak punya akses semua section | accept | Counter `ViewBag.ClosedCount` aggregate GLOBAL (semua section) — risk: SectionHead L4 lihat total Closed Bagian lain. Mitigation: scope counter ke `User.Section` bila roleLevel >= 4. **Atau accept** dengan rationale: ManageAssessment view sudah Admin/HC only (L4 worker tidak akses). T-338-01-03 accept dengan justifikasi authentication boundary. |
| T-338-01-04 | DoS | search wildcard `%` trigger full table scan | accept | Existing `Title.Contains(search)` sudah indexable bila Title indexed; performance acceptable untuk N < 10K sessions. Phase 338 tidak introduce new query. |
| T-338-01-05 | Tampering (UX) | User notice badge tapi tidak paham gunanya | mitigate | Tooltip "Closed assessment (hidden by default — pilih filter Closed untuk lihat)" + animate first-load + warna abu konsisten dengan Bootstrap semantics. T-338-05 from CONTEXT.md. |
</threat_model>

<verification>
- `dotnet build` PASS untuk seluruh solution
- Playwright/manual browser UAT CIL-01 badge counter visible + value akurat
- Playwright/manual browser UAT CIL-02 search Cilacap return Closed group
- Regression smoke pagination + category filter + sort PASS
- DB restored bila ada temporary seed Cilacap
- `docs/SEED_JOURNAL.md` updated bila ada seed
</verification>

<success_criteria>
- [ ] 2 REQ closed: CIL-01 + CIL-02
- [ ] 3 file modified: AssessmentAdminController.cs (L198 guard + ViewBag counter), ManageAssessment.cshtml, AssessmentMonitoring.cshtml
- [ ] Default filter TETAP Open (no breaking change user lama — D-01)
- [ ] Search non-empty tanpa statusFilter include Closed (CIL-02 fix verified)
- [ ] Badge counter Closed accurate + animated first-load (T-338-05 mitigation)
- [ ] L4 section auth boundary preserve
- [ ] User explicit "approved" sebelum claim Wave 1 complete
</success_criteria>

<output>
Setelah completion, create `.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-01-SUMMARY.md` dengan format `$HOME/.claude/get-shit-done/templates/summary.md`. Wajib catat:
- 2 REQ closed (CIL-01 + CIL-02)
- Files modified + line ranges actual
- UAT result per REQ-ID (PASS/FAIL/SKIP dengan reason)
- Threats mitigated/accepted
- Next: Wave 2 Plan 02 (CIL-03 history drill-down + CIL-04 banner alert)
</output>
