---
phase: 338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute
plan: 02
type: execute
wave: 2
depends_on: [338-01]
files_modified:
  - Models/AllWorkersHistoryRow.cs
  - Services/WorkerDataService.cs
  - Views/CMP/RecordsTeam.cshtml
  - Views/CMP/_RecordsTeamBody.cshtml
  - Views/CMP/Assessment.cshtml
  - Controllers/CMPController.cs
autonomous: false
requirements: [CIL-03, CIL-04]
must_haves:
  truths:
    - "Row `<tr>` di history tab assessment branch clickable (cursor pointer + data-href) drill-down ke /CMP/Results/{sessionId} (CIL-03)"
    - "Row history keyboard-navigable: Tab focus + Enter/Space trigger drill-down (a11y CIL-03 + reuse Plan 337-02 CMP-19 pattern)"
    - "Kolom Actions dengan ikon link explicit `<i class='bi bi-eye'></i> Lihat` tampil per row assessment (CIL-03)"
    - "Banner alert info muncul di /CMP/Assessment HANYA untuk user role Admin atau HC: 'Cari completed assessment lain? Lihat history' link /Admin/ManageAssessment?tab=history (CIL-04)"
    - "Worker non-Admin/HC TIDAK lihat banner (preserve UX worker tidak distract dengan link admin)"
  artifacts:
    - path: "Models/AllWorkersHistoryRow.cs"
      provides: "Schema delta — tambah SessionId int? untuk drill-down link"
      contains: "public int? SessionId"
    - path: "Services/WorkerDataService.cs"
      provides: "GetAllWorkersHistory assessment branch populate SessionId = a.Id"
      contains: "SessionId = a.Id"
    - path: "Views/CMP/_RecordsTeamBody.cshtml"
      provides: "<tr> data-href + tabindex + role=link + kolom Actions per assessment row"
      contains: "data-href=\"/CMP/Results/"
    - path: "Views/CMP/Assessment.cshtml"
      provides: "Banner alert info Admin/HC role-gated"
      contains: "alert alert-info"
  key_links:
    - from: "Views/CMP/_RecordsTeamBody.cshtml assessment row"
      to: "/CMP/Results/{sessionId} endpoint"
      via: "data-href attribute + JS click handler"
      pattern: "data-href=\"/CMP/Results/@item\\.SessionId\""
    - from: "Models/AllWorkersHistoryRow"
      to: "WorkerDataService.GetAllWorkersHistory assessment branch L120-145"
      via: "SessionId = a.Id projection"
      pattern: "SessionId\\s*=\\s*a\\.Id"
    - from: "Views/CMP/Assessment.cshtml banner"
      to: "/Admin/ManageAssessment?tab=history"
      via: "<a href> + User.IsInRole guard"
      pattern: "User\\.IsInRole\\(\"Admin\"\\) \\|\\| User\\.IsInRole\\(\"HC\"\\)"
---

<objective>
Wave 2 Phase 338 — Fix 2 gap UX discoverability: (1) CIL-03 row clickable + Actions column di History tab assessment branch supaya admin/HC dapat drill-down dari list view ke detail result session, (2) CIL-04 banner alert di `/CMP/Assessment` untuk user role Admin/HC redirect ke admin assessment history view.

Purpose: Saat ini history tab list assessment-only display row tanpa link drill-down — admin/HC harus navigate manual ke `/CMP/Results/{id}` via URL guess atau via /Admin tab. CIL-03 fix discoverability lewat reuse Plan 337-02 CMP-19 row-clickable pattern. CIL-04 banner attach context shortcut di /CMP/Assessment page untuk admin/HC user (tidak distract worker biasa).

Output:
- `Models/AllWorkersHistoryRow.cs` — schema delta tambah `SessionId int?` (D-09)
- `Services/WorkerDataService.cs` — assessment branch GetAllWorkersHistory populate SessionId (training branch null)
- `Views/CMP/_RecordsTeamBody.cshtml` (atau partial history actual) — `<tr>` row clickable + Actions column ikon Lihat
- `Views/CMP/Assessment.cshtml` — banner alert info Admin/HC role-gated
- `Controllers/CMPController.cs` — verify L195 Assessment action expose User.IsInRole context ke view (atau pass via ViewBag bila view tidak punya akses User directly)

Setelah Wave 2 selesai: admin/HC user dapat klik row history → drill-down result session, DAN dapat lihat banner shortcut di /CMP/Assessment page. Wave 3 (CIL-05 Excel) selanjutnya.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-CONTEXT.md
@.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-RESEARCH.md
@.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-01-SUMMARY.md
@.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-02-PLAN.md
@CLAUDE.md

# File sumber yang akan dimodifikasi
@Models/AllWorkersHistoryRow.cs
@Services/WorkerDataService.cs
@Views/CMP/RecordsTeam.cshtml
@Views/CMP/_RecordsTeamBody.cshtml
@Views/CMP/Assessment.cshtml
@Controllers/CMPController.cs

<interfaces>
<!-- Schema target — D-09 CONTEXT.md amendment -->

From Models/AllWorkersHistoryRow.cs (current state post Phase 337):
```csharp
public class AllWorkersHistoryRow
{
    public string WorkerId { get; set; } = "";
    public string WorkerName { get; set; } = "";
    public string? WorkerNIP { get; set; }
    public string RecordType { get; set; } = "";  // "Manual" or "Assessment Online"
    public string Title { get; set; } = "";
    public DateTime Date { get; set; }
    public string? Penyelenggara { get; set; }
    public int? Score { get; set; }
    public bool? IsPassed { get; set; }
    public int? AttemptNumber { get; set; }
    public string? Kategori { get; set; }      // Phase 337
    public string? SubKategori { get; set; }   // Phase 337
    // CIL-03 D-09: tambah SessionId
}
```

TARGET delta (D-09 CIL-03):
```csharp
public int? SessionId { get; set; }  // Assessment-only — null untuk training branch
```

From Services/WorkerDataService.cs L120-145 (current assessment branch — projection target):
```csharp
return new AllWorkersHistoryRow
{
    WorkerId      = a.UserId,
    WorkerName    = a.User?.FullName ?? a.UserId,
    WorkerNIP     = a.User?.NIP,
    RecordType    = "Assessment Online",
    Title         = a.Title ?? "",
    Date          = a.CompletedAt ?? a.Schedule ?? DateTime.MinValue,
    Score         = a.Score,
    IsPassed      = a.IsPassed,
    AttemptNumber = attemptNumber
    // CIL-03 D-09 TARGET: tambah SessionId = a.Id
};
```

Plus training branch (L155-167) — SessionId TETAP default null:
```csharp
return new AllWorkersHistoryRow
{
    /* ... */
    Kategori    = t.Kategori,
    SubKategori = t.SubKategori
    // CIL-03: training branch — SessionId TIDAK di-set (default null)
};
```

From Phase 337 Plan 02 (CMP-19 pattern reuse — table row keyboard-navigable):
```cshtml
<tr class="table-row-clickable"
    data-href="/CMP/Results/@item.SessionId"
    tabindex="0"
    role="link"
    aria-label="Lihat detail assessment @item.Title oleh @item.WorkerName">
    ...
</tr>
```

JS handler (di Phase 337 Plan 02 pattern):
```javascript
document.querySelectorAll('.table-row-clickable[data-href]').forEach(row => {
    row.style.cursor = 'pointer';
    row.addEventListener('click', (e) => {
        if (e.target.closest('a, button, input, select')) return;
        window.location.href = row.dataset.href;
    });
    row.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            window.location.href = row.dataset.href;
        }
    });
});
```

From Controllers/CMPController.cs L195 Assessment action (current):
```csharp
public async Task<IActionResult> Assessment(string? search, int page = 1, int pageSize = 20)
{
    // ... personal view worker's own assessments
    return View(viewModel);
}
```

User context untuk view sudah available via `User.IsInRole("Admin")` directly di Razor — TIDAK perlu controller change untuk CIL-04 banner. Hanya Razor markup edit di Views/CMP/Assessment.cshtml.

From Views/CMP/Assessment.cshtml (banner location TARGET — di atas content table):
```cshtml
@* CIL-04: Banner alert role-gated Admin/HC *@
@if (User.IsInRole("Admin") || User.IsInRole("HC"))
{
    <div class="alert alert-info d-flex align-items-center mb-3" role="alert">
        <i class="bi bi-info-circle me-2"></i>
        <div class="flex-grow-1">
            Cari completed assessment lain di seluruh user?
            <a href="/Admin/ManageAssessment?tab=history" class="alert-link">View admin assessment history</a>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schema delta — tambah SessionId di AllWorkersHistoryRow + populate di GetAllWorkersHistory (CIL-03 D-09)</name>
  <files>Models/AllWorkersHistoryRow.cs, Services/WorkerDataService.cs</files>
  <behavior>
    Setelah fix:
    - `AllWorkersHistoryRow` punya property `public int? SessionId { get; set; }` (nullable, default null)
    - `GetAllWorkersHistory` assessment branch (sekitar L120-145) populate `SessionId = a.Id`
    - Training branch (sekitar L155-167) TIDAK populate SessionId (default null — training tidak punya session concept)
    - Non-breaking change: existing caller AllWorkersHistoryRow tetap valid; new property additive
  </behavior>
  <action>
    1. **Edit `Models/AllWorkersHistoryRow.cs`** — tambah property setelah `SubKategori`:
    ```csharp
    // Phase 337 CMP-05: Category + SubCategory for filter parity di ExportRecordsTeamTraining
    public string? Kategori { get; set; }
    public string? SubKategori { get; set; }

    // Phase 338 CIL-03 (D-09): SessionId untuk drill-down /CMP/Results/{id}
    // Assessment branch only — training branch default null
    public int? SessionId { get; set; }
    ```

    2. **Edit `Services/WorkerDataService.cs` assessment branch** — cari projection assessment di GetAllWorkersHistory:
    ```bash
    grep -n "RecordType.*=.*\"Assessment Online\"" Services/WorkerDataService.cs
    ```
    Lokasi expected L120-145. Tambah baris `SessionId = a.Id`:
    ```csharp
    return new AllWorkersHistoryRow
    {
        WorkerId      = a.UserId,
        WorkerName    = a.User?.FullName ?? a.UserId,
        WorkerNIP     = a.User?.NIP,
        RecordType    = "Assessment Online",
        Title         = a.Title ?? "",
        Date          = a.CompletedAt ?? a.Schedule ?? DateTime.MinValue,
        Score         = a.Score,
        IsPassed      = a.IsPassed,
        AttemptNumber = attemptNumber,
        SessionId     = a.Id   // CIL-03 D-09
    };
    ```

    3. **Training branch tidak diubah** — verify projection training (L155-167) tidak set SessionId (default null acceptable per D-09).

    4. **Build sanity:**
    ```bash
    dotnet build 2>&1 | tail -5
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error CS" | head -5</automated>
  </verify>
  <done>
    - `AllWorkersHistoryRow.SessionId` property added (nullable int)
    - `GetAllWorkersHistory` assessment branch populate SessionId = a.Id
    - Training branch tidak diubah
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Row clickable + Actions column di history view (CIL-03 — reuse Plan 337-02 CMP-19 pattern)</name>
  <files>Views/CMP/_RecordsTeamBody.cshtml, Views/CMP/RecordsTeam.cshtml</files>
  <behavior>
    Setelah fix:
    - Row `<tr>` di history tab assessment branch render dengan `class="table-row-clickable"` + `data-href="/CMP/Results/{SessionId}"` + `tabindex="0"` + `role="link"`
    - Cursor pointer di hover, focus visible outline, Enter/Space trigger navigation
    - Kolom Actions baru dengan ikon `<i class="bi bi-eye"></i> Lihat` link explicit
    - Training row TETAP tidak clickable (SessionId null) — atau tampil "—" di Actions column
    - JS handler include guard `e.target.closest('a, button')` supaya klik tombol Actions tidak double-navigate
  </behavior>
  <action>
    1. **Identifikasi view yang render history tab assessment branch:**
    ```bash
    grep -rn "Assessment Online\|RecordType.*Assessment\|AllWorkersHistoryRow" Views/CMP/ | head -20
    ```
    Kemungkinan partial: `Views/CMP/_RecordsTeamBody.cshtml` atau `Views/CMP/_HistoryTab.cshtml` atau bagian di `Views/CMP/RecordsTeam.cshtml`.

    2. **Edit `<tr>` markup assessment row** — bila row history pakai if/else per RecordType, modify hanya branch Assessment:
    ```cshtml
    @foreach (var item in Model.HistoryRows)
    {
        if (item.RecordType == "Assessment Online" && item.SessionId.HasValue)
        {
            <tr class="table-row-clickable"
                data-href="/CMP/Results/@item.SessionId"
                tabindex="0"
                role="link"
                aria-label="Lihat detail assessment @item.Title oleh @item.WorkerName">
                <td>@item.Date.ToString("dd MMM yyyy", new System.Globalization.CultureInfo("id-ID"))</td>
                <td>@item.WorkerName</td>
                <td>@item.WorkerNIP</td>
                <td>@item.Title</td>
                <td>@item.Score</td>
                <td>
                    @if (item.IsPassed == true) { <span class="badge bg-success">Passed</span> }
                    else if (item.IsPassed == false) { <span class="badge bg-danger">Failed</span> }
                    else { <span class="badge bg-secondary">Completed</span> }
                </td>
                <td class="text-center">
                    @* CIL-03: Actions column explicit link *@
                    <a href="/CMP/Results/@item.SessionId" class="btn btn-sm btn-outline-primary"
                       title="Lihat detail" aria-label="Lihat detail @item.Title">
                        <i class="bi bi-eye"></i> Lihat
                    </a>
                </td>
            </tr>
        }
        else  // Training Manual atau Assessment tanpa SessionId
        {
            <tr>
                <td>@item.Date.ToString("dd MMM yyyy", new System.Globalization.CultureInfo("id-ID"))</td>
                <td>@item.WorkerName</td>
                <td>@item.WorkerNIP</td>
                <td>@item.Title</td>
                <td>—</td>
                <td><span class="badge bg-light text-dark">@item.RecordType</span></td>
                <td class="text-center"><span class="text-muted">—</span></td>
            </tr>
        }
    }
    ```

    NOTE: kolom layout di-adjust sesuai struktur actual view. Cek `<thead>` untuk count kolom expected.

    3. **Header `<thead>` tambah kolom Actions** — kalau belum ada:
    ```cshtml
    <thead>
        <tr>
            <th>Tanggal</th>
            <th>Nama</th>
            <th>NIP</th>
            <th>Judul</th>
            <th>Skor</th>
            <th>Status</th>
            <th class="text-center">Actions</th>   @* CIL-03 *@
        </tr>
    </thead>
    ```

    4. **JS handler di view atau shared script** — cek apakah Phase 337 Plan 02 sudah include `table-row-clickable` handler. Kalau YA, sudah cukup. Kalau BELUM ada handler global, tambah di akhir view atau di `Views/Shared/_Layout.cshtml`:
    ```html
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('tr.table-row-clickable[data-href]').forEach(function(row) {
                row.style.cursor = 'pointer';
                row.addEventListener('click', function(e) {
                    if (e.target.closest('a, button, input, select')) return;
                    window.location.href = row.dataset.href;
                });
                row.addEventListener('keydown', function(e) {
                    if (e.key === 'Enter' || e.key === ' ') {
                        e.preventDefault();
                        window.location.href = row.dataset.href;
                    }
                });
            });
        });
    </script>
    ```

    Verify pre-existing:
    ```bash
    grep -rn "table-row-clickable" Views/Shared/ Views/CMP/
    ```
    Bila Phase 337 sudah register handler global (kemungkinan di Plan 337-02), SKIP step ini.

    5. **CSS focus outline** (a11y) — tambah inline `<style>` atau di site.css:
    ```css
    tr.table-row-clickable:focus {
        outline: 2px solid #0d6efd;
        outline-offset: -2px;
    }
    tr.table-row-clickable:hover {
        background-color: rgba(13, 110, 253, 0.05);
    }
    ```

    6. **Build:**
    ```bash
    dotnet build 2>&1 | tail -5
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <done>
    - Row assessment branch render dengan data-href + tabindex + role=link + Actions column
    - JS handler global (reused atau new) handle click + keyboard
    - CSS focus + hover style a11y-friendly
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Banner alert di /CMP/Assessment role-gated Admin/HC (CIL-04)</name>
  <files>Views/CMP/Assessment.cshtml</files>
  <behavior>
    Setelah fix:
    - Banner alert info muncul di atas content table di `/CMP/Assessment` HANYA untuk user role Admin atau HC
    - Worker non-Admin/HC TIDAK lihat banner
    - Banner content: "Cari completed assessment lain di seluruh user? View admin assessment history" — link ke `/Admin/ManageAssessment?tab=history`
    - Banner dismissable dengan close button (Bootstrap alert dismissible — session-only dismiss, no cookie persist per D-04 deferred)
  </behavior>
  <action>
    1. **Baca struktur view:**
    ```bash
    grep -n "@model\|<div class=\"container\|<table\|<h1\|<h2" Views/CMP/Assessment.cshtml | head -20
    ```

    2. **Lokasi banner** — tepat setelah header/breadcrumb, SEBELUM content table. Kemungkinan lokasi di awal `<div class="container">` body.

    3. **Insert markup**:
    ```cshtml
    @* CIL-04: Banner alert role-gated Admin/HC — context shortcut ke admin history *@
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
        <div class="alert alert-info alert-dismissible fade show d-flex align-items-center mb-3" role="alert">
            <i class="bi bi-info-circle me-2 fs-5"></i>
            <div class="flex-grow-1">
                <strong>Tip Admin/HC:</strong> Cari completed assessment lain di seluruh user?
                <a href="/Admin/ManageAssessment?tab=history" class="alert-link">View admin assessment history</a>
            </div>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    ```

    4. **Verify Bootstrap Icons** sudah included via _Layout. Kalau belum, fallback emoji atau text-only:
    ```cshtml
    @* Fallback tanpa Bootstrap Icons *@
    <span class="me-2">ℹ️</span>
    ```

    5. **Build + view sanity:**
    ```bash
    dotnet build 2>&1 | tail -5
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <done>
    - Banner alert info render conditional `User.IsInRole("Admin") || User.IsInRole("HC")`
    - Link href `/Admin/ManageAssessment?tab=history` correct
    - Banner dismissable (Bootstrap alert-dismissible)
    - `dotnet build` PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — Playwright UAT CIL-03 drill-down + CIL-04 banner</name>
  <files>N/A — checkpoint task</files>
  <action>Wait for user verification. PAUSE eksekusi sampai user respond resume-signal.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User reply 'approved' di resume-signal.</done>
  <what-built>
    Wave 2 selesai code-side:
    - CIL-03 schema delta (SessionId) + row clickable + Actions column + a11y keyboard nav
    - CIL-04 banner alert role-gated Admin/HC di /CMP/Assessment
    - 6 file modified: AllWorkersHistoryRow.cs, WorkerDataService.cs, _RecordsTeamBody.cshtml, RecordsTeam.cshtml, Assessment.cshtml, (optional CMPController.cs)
    - `dotnet build` PASS

    Belum ada Playwright verification interactive di browser. Wajib UAT sebelum Wave 3 (CIL-05 Excel breakdown HIGH PRIORITY).
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Backup DB lokal (per `docs/SEED_WORKFLOW.md`):
       ```bash
       sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "BACKUP DATABASE [HCPortalDb] TO DISK='C:\Temp\HCPortalDb_pre_338-02_uat.bak'"
       ```
    2. Run app lokal: `dotnet run` → buka `http://localhost:5277`
    3. Login `admin@pertamina.com` / `123456`

    **A. UAT CIL-03 Row Clickable + Actions Column:**

    Navigate `http://localhost:5277/CMP/Records` → klik tab "Team View" → switch ke sub-tab "History".

    **A1. Visual — Actions column ada:**
    - Expected: `<thead>` punya kolom "Actions" paling kanan
    - Per assessment row: button `<i class='bi bi-eye'></i> Lihat`
    - Per training row: "—" (training tidak punya session drill-down)
    - PASS criteria: kolom Actions render, button visible per assessment row

    **A2. Functional — Click row drill-down:**
    - Klik row assessment (bukan klik tombol Lihat) — expected: navigate ke `/CMP/Results/{sessionId}`
    - Klik tombol "Lihat" — expected: navigate ke `/CMP/Results/{sessionId}` (same destination)
    - PASS criteria: drill-down works dari both click target

    **A3. Keyboard a11y:**
    - Tab key fokus ke row → expected: outline biru visible
    - Tekan Enter atau Space → expected: navigate ke `/CMP/Results/{sessionId}`
    - PASS criteria: keyboard accessible per WCAG 2.1 AA

    **A4. Training row TIDAK clickable:**
    - Click training row (RecordType="Manual") → expected: NOT navigate (no data-href)
    - PASS criteria: training row safe, tidak ada broken link

    **A5. Screen reader (optional bila tester ada NVDA/JAWS):**
    - Focus ke row → expected: SR baca aria-label "Lihat detail {Title} oleh {WorkerName}"
    - PASS criteria: a11y label informative

    **B. UAT CIL-04 Banner Alert:**

    **B1. Admin user lihat banner:**
    - Login sebagai `admin@pertamina.com` (role Admin)
    - Navigate `http://localhost:5277/CMP/Assessment`
    - Expected: banner info biru muncul di atas content table
    - Klik link "View admin assessment history" → navigate ke `/Admin/ManageAssessment?tab=history`
    - PASS criteria: banner visible + link redirect correct

    **B2. HC user lihat banner:**
    - Logout, login sebagai user role HC (cari di DB: `SELECT TOP 3 Email FROM AspNetUsers u JOIN AspNetUserRoles r ON u.Id = r.UserId JOIN AspNetRoles rl ON r.RoleId = rl.Id WHERE rl.Name = 'HC'`)
    - Navigate `/CMP/Assessment`
    - Expected: banner visible
    - PASS criteria: same as B1

    **B3. Worker non-Admin/HC TIDAK lihat banner:**
    - Logout, login sebagai user role Pekerja (atau Coach)
    - Navigate `/CMP/Assessment`
    - Expected: banner TIDAK muncul
    - PASS criteria: role-gating correct

    **B4. Banner dismissable:**
    - Login admin, navigate `/CMP/Assessment`
    - Klik close button (X) di banner
    - Expected: banner hilang (Bootstrap fade out)
    - Refresh page → banner muncul lagi (no cookie persist per D-04 deferred)
    - PASS criteria: dismiss works session-only

    **C. Regression smoke:**
    - Phase 337 Plan 02 row-clickable pattern di CMP/Records tetap bekerja (no CSS conflict)
    - Wave 1 badge counter Closed di /Admin/ManageAssessment tetap render
    - Tab navigation di CMP/Records tidak broken
    - Sertifikat link Training (Phase 337 CMP-07) tetap render

    **D. Cleanup:**
    - Restore DB:
      ```bash
      sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "RESTORE DATABASE [HCPortalDb] FROM DISK='C:\Temp\HCPortalDb_pre_338-02_uat.bak' WITH REPLACE"
      ```
  </how-to-verify>
  <resume-signal>Ketik "approved" bila CIL-03 + CIL-04 PASS. Bila ada FAIL, sebut REQ-ID + scenario.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /CMP/Results/{sessionId} | Untrusted sessionId query param crossing dari client navigation (drill-down) |
| Razor view → User.IsInRole | Internal trusted Identity check (banner gating) |
| Service → DbContext projection | EF parameterized, low SQL injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-338-02-01 | Information Disclosure | User drill-down ke /CMP/Results/{N} session yang bukan miliknya | mitigate | Existing `/CMP/Results` endpoint sudah validate ownership / role (CMPController). Tidak ada bypass — Plan 02 hanya add link cue, server-side auth tetap enforce. |
| T-338-02-02 | Tampering | sessionId injection via DOM manipulation (devtools) | mitigate | Same as T-338-02-01 — server-side validation di /CMP/Results/{id} action sudah ada. Client-side data-href hanya convenience cue. |
| T-338-02-03 | Information Disclosure | Banner expose admin link ke worker yang somehow bypass role check | mitigate | `User.IsInRole("Admin") || User.IsInRole("HC")` di Razor — ASP.NET Identity standard pattern. Roles cached di User principal post-login, sulit tampered tanpa session hijack. |
| T-338-02-04 | Tampering (UX) | Click row + click button double-navigate / browser back | mitigate | JS handler guard `e.target.closest('a, button')` — kalau click target tombol Lihat, JS handler abort, hanya `<a href>` yang trigger. No double navigation. |
| T-338-02-05 | Repudiation | User drill-down ke session tidak ter-audit | accept | Read-only navigation — audit log di /CMP/Results action sudah handle (kalau di-required). Tidak modify data. |
| T-338-02-06 | DoS (low) | Banner render tiap page load tambah ~200 bytes HTML | accept | Negligible payload increase, no perf impact. |
</threat_model>

<verification>
- `dotnet build` PASS untuk seluruh solution
- Playwright UAT CIL-03 row clickable + Actions column + a11y keyboard nav PASS
- Playwright UAT CIL-04 banner Admin/HC visible + Worker hidden PASS
- Regression Phase 337 CMP-19 pattern tetap bekerja
- DB restored bila ada temporary seed
</verification>

<success_criteria>
- [ ] 2 REQ closed: CIL-03 + CIL-04
- [ ] 6 file modified (or 5 bila CMPController tidak perlu touch)
- [ ] Schema delta SessionId additive (non-breaking)
- [ ] Row clickable + Actions column + a11y keyboard nav verified
- [ ] Banner role-gated Admin/HC verified (worker non-admin tidak lihat)
- [ ] Threat T-338-02-01..04 mitigated, T-338-02-05/06 accepted
- [ ] User explicit "approved" sebelum claim Wave 2 complete
</success_criteria>

<output>
Setelah completion, create `.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-02-SUMMARY.md` dengan format `$HOME/.claude/get-shit-done/templates/summary.md`. Wajib catat:
- 2 REQ closed (CIL-03 + CIL-04)
- Files modified + line ranges actual
- UAT result per REQ-ID
- Threats mitigated/accepted
- Next: Wave 3 Plan 03 (CIL-05 Excel +2 sheet HIGH PRIORITY)
</output>
