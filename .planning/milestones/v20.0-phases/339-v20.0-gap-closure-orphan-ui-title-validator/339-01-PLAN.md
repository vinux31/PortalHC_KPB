---
phase: 339-v20.0-gap-closure-orphan-ui-title-validator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
  - Views/Admin/Index.cshtml
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/CreateAssessment.cshtml
autonomous: true
requirements:
  - CIL-06
  - REST-04
  - REST-06
gap_closure: true
response_language: id-ID

must_haves:
  truths:
    - "Admin melihat dropdown-item 'Bulk Export PDF (ZIP)' di per-group action dropdown pada /Admin/ManageAssessment, klik → ZIP download mulai (CIL-06 closed)"
    - "Admin melihat link 'Bulk Backfill (Restore Lost Data)' di dropdown grup + entry top-level di /Admin (Section D System), klik → /Admin/BulkBackfill form muncul (REST-04 closed)"
    - "Admin submit CreateAssessment dengan title invalid (mis. 'Quiz Random' dengan AssessmentTypeInput='Standard') → ModelState error 'Title harus pola...' tampil di view, save di-block (REST-06 closed)"
    - "Admin submit CreateAssessment dengan title valid ('Pre Test OJT GAST Cilacap') → save sukses + TempData Info auto-pair (jika counterpart ada)"
    - "Existing auto-pair Phase 338-05 (TryAutoDetectCounterpartGroup) tetap jalan tanpa regresi"
    - "dotnet build → 0 error; dotnet test → 18/18 PASS (no regression)"
  artifacts:
    - path: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml"
      provides: "Two new dropdown-items wired to BulkExportPdf + BulkBackfill"
      contains: "BulkExportPdf"
    - path: "Views/Admin/Index.cshtml"
      provides: "Admin-only card link 'Bulk Backfill (Restore Lost Data)' di Section D System"
      contains: "BulkBackfill"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Conditional regex validator on AssessmentSession.Title (REST-06 hardening)"
      contains: "Regex.IsMatch(model.Title"
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "asp-validation-for Title span (server-side error rendering)"
      contains: "asp-validation-for=\"Title\""
  key_links:
    - from: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml dropdown-item Bulk Export PDF"
      to: "AssessmentAdminController.BulkExportPdf endpoint (Phase 338-04 L4489)"
      via: "Url.Action with title + category + scheduleDate route values mirroring Export Excel sibling L278"
      pattern: "BulkExportPdf.*AssessmentAdmin.*title.*category.*scheduleDate"
    - from: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml dropdown-item Bulk Backfill"
      to: "TrainingAdminController.BulkBackfill GET endpoint (Phase 338-04 L720)"
      via: "Url.Action TrainingAdmin/BulkBackfill"
      pattern: "BulkBackfill.*TrainingAdmin"
    - from: "Views/Admin/Index.cshtml Section D card"
      to: "TrainingAdminController.BulkBackfill"
      via: "Url.Action with @if (User.IsInRole(\"Admin\")) gate"
      pattern: "BulkBackfill.*TrainingAdmin"
    - from: "AssessmentAdminController.CreateAssessment POST L835-845"
      to: "ModelState.AddModelError + Views/Admin/CreateAssessment.cshtml asp-validation-for"
      via: "Conditional regex check inserted AFTER auto-pair block (L845) BEFORE token validation (L847)"
      pattern: "Regex\\.IsMatch.*model\\.Title.*Pre.*Post.*Test"
---

<objective>
Tutup 3 partial REQ dari audit milestone v20.0 (CIL-06 orphan endpoint, REST-04 orphan route, REST-06 Title validation missing) lewat surgical UI link + defensive regex validator. Tidak ada endpoint baru atau business logic baru — semua endpoint sudah ada dari Phase 338, fase ini hanya membuat mereka discoverable + memvalidasi input.

**Purpose:** Audit `/gsd-audit-milestone v20.0` menemukan 3 REQ partial: endpoint Bulk Export PDF (Phase 338-04 W4) + endpoint Bulk Backfill Restore (Phase 338-04 W4) hidup tetapi tidak punya UI entry; auto-pair LinkedGroupId (Phase 338-05) sukses tetapi spec naming convention `336-NAMING-CONVENTION-SPEC.md` belum di-enforce server-side (admin bisa input title sembarang). Phase 339 menambahkan UI link + regex defensive check untuk menutup gap tanpa breaking change.

**Output:**
- 4 file dimodifikasi (2 view + 1 controller + 1 view validation span)
- 0 file baru
- 0 migration / 0 schema change / 0 seed
- dotnet build 0 error, dotnet test 18/18 PASS (no regression)
- Manual UAT Playwright skenario per acceptance_criteria
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/339-v20.0-gap-closure-orphan-ui-title-validator/339-CONTEXT.md
@.planning/v20.0-MILESTONE-AUDIT.md
@.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-04-SUMMARY.md
@.planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-05-SUMMARY.md
@.planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md

<interfaces>
<!-- Key code surface verified 2026-06-02. Executor MUST reference these line:anchor pairs. -->
<!-- DO NOT modify entity definitions — Title field is plain string by D-03 design. -->

From Controllers/AssessmentAdminController.cs:
```csharp
// L820-828 method signature
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> CreateAssessment(
    AssessmentSession model, List<string> UserIds,
    string? RenewalFkMap = null, string? RenewalFkMapType = null,
    string? AssessmentTypeInput = null,
    DateTime? PreSchedule = null, int? PreDurationMinutes = null, DateTime? PreExamWindowCloseDate = null,
    DateTime? PostSchedule = null, int? PostDurationMinutes = null, DateTime? PostExamWindowCloseDate = null,
    bool SamePackage = false)
{
    // L830-831
    ModelState.Remove("UserId");

    // L833-845 EXISTING auto-pair block (Phase 338-05) — DO NOT MODIFY
    if (AssessmentTypeInput != "PrePostTest"
        && model.LinkedGroupId == null
        && !string.IsNullOrEmpty(model.Title))
    {
        var counterpartId = await TryAutoDetectCounterpartGroup(model.Title, model.Category);
        if (counterpartId.HasValue)
        {
            model.LinkedGroupId = counterpartId.Value;
            TempData["Info"] = $"Auto-paired LinkedGroupId={counterpartId.Value} ...";
        }
    }

    // L847 token validation (REST-06 validator inserted BEFORE this line)
    if (model.IsTokenRequired) { ... }

// L4489 target endpoint (CIL-06)
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> BulkExportPdf(string title, string category, DateTime scheduleDate)
```

From Controllers/TrainingAdminController.cs:
```csharp
// L720 target endpoint (REST-04 — GET form)
[Authorize(Roles = "Admin")]
[HttpGet]
public IActionResult BulkBackfill() { ... return View(); }

// L733 POST endpoint (NOT linked from UI in this phase — form posts to itself)
[Authorize(Roles = "Admin")]
[HttpPost]
public async Task<IActionResult> BulkBackfillAssessment(...) { ... }
```

From Views/Admin/Shared/_AssessmentGroupsTab.cshtml (L277-281 anchor — existing Export Excel sibling):
```cshtml
<li>
    <a class="dropdown-item" href="@Url.Action("ExportAssessmentResults", "AssessmentAdmin", new { title = (string)group.Title, category = (string)group.Category, scheduleDate = ((DateTime)group.Schedule).Date.ToString("yyyy-MM-dd") })">
        <i class="bi bi-file-earmark-excel me-2"></i>Export Excel
    </a>
</li>
```

From Views/Admin/CreateAssessment.cshtml (L183-193 anchor — Title input block):
```cshtml
<!-- Title -->
<div class="col-md-8">
    <label asp-for="Title" class="form-label fw-bold">
        Judul Assessment <span class="text-danger">*</span>
    </label>
    <input asp-for="Title" class="form-control" id="Title" placeholder="Masukkan judul assessment" maxlength="255" oninput="document.getElementById('titleCounter').textContent=this.value.length+'/255'" />
    <div class="d-flex justify-content-between">
        <div class="invalid-feedback">Judul assessment wajib diisi.</div>
        <small class="text-muted" id="titleCounter">0/255</small>
    </div>
</div>
```

From Views/Admin/Index.cshtml (L250-273 anchor — Section D System):
```cshtml
<hr class="my-4">

@* Section D: System *@
<div class="d-flex align-items-center mb-3">
    <span class="badge bg-secondary me-2">D</span>
    <h5 class="fw-bold text-secondary mb-0">System</h5>
</div>
<div class="row g-3 mb-2">
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("Maintenance", "Admin")" class="text-decoration-none">
            ...Maintenance Mode card...
        </a>
    </div>
    }
</div>
```

From Models/AssessmentSession.cs:
```csharp
// L13 — DO NOT MODIFY (entity shared across CMP/IHT/Licencor/OTS — annotation would break legacy)
public string Title { get; set; } = "";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CIL-06 — Tambah dropdown-item Bulk Export PDF (ZIP) di per-group action dropdown</name>

  <read_first>
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml (full file untuk anchor L278-281)
    - .planning/phases/339-v20.0-gap-closure-orphan-ui-title-validator/339-CONTEXT.md (D-01 verbatim block)
    - .planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-04-SUMMARY.md (CIL-06 endpoint signature verifikasi)
    - Controllers/AssessmentAdminController.cs L4480-4500 (BulkExportPdf signature: string title, string category, DateTime scheduleDate)
  </read_first>

  <files>Views/Admin/Shared/_AssessmentGroupsTab.cshtml</files>

  <action>
    Insert NEW `<li>...</li>` block IMMEDIATELY AFTER line 281 (closing `</li>` of existing "Export Excel" dropdown-item L277-281) dan SEBELUM L282 (`@if ((bool)group.IsTokenRequired)`).

    Konten verbatim (Bahasa Indonesia per CLAUDE.md, icon `bi-file-zip` per D-01):

    ```cshtml
    <li>
        <a class="dropdown-item" href="@Url.Action("BulkExportPdf", "AssessmentAdmin", new { title = (string)group.Title, category = (string)group.Category, scheduleDate = ((DateTime)group.Schedule).Date.ToString("yyyy-MM-dd") })">
            <i class="bi bi-file-zip me-2"></i>Bulk Export PDF (ZIP)
        </a>
    </li>
    ```

    Catatan struktural: gunakan WRAPPER `<li>` (sibling pattern existing Export Excel L277-281, BUKAN bare `<a>` D-01 verbatim — D-01 menunjukkan inti elemen, struktur `<li>` wajib ikut existing Bootstrap dropdown convention).

    ALSO insert pada baris SETELAH `</li>` baru ini (sebelum L282) dropdown-divider + dropdown-item Bulk Backfill (REST-04 dropdown variant per D-02):

    ```cshtml
    <li><hr class="dropdown-divider"></li>
    <li>
        <a class="dropdown-item" href="@Url.Action("BulkBackfill", "TrainingAdmin")">
            <i class="bi bi-arrow-counterclockwise me-2"></i>Bulk Backfill (Restore Lost Data)
        </a>
    </li>
    ```

    Jangan ubah baris 260-281 existing (Monitoring/Edit/ManagePackages/Export Excel). Jangan ubah L282+ (token regenerate + delete logic). Hanya INSERT 4 elemen baru antara L281 dan L282.
  </action>

  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && grep -c "BulkExportPdf" Views/Admin/Shared/_AssessmentGroupsTab.cshtml && grep -c "BulkBackfill" Views/Admin/Shared/_AssessmentGroupsTab.cshtml && grep -c "bi-file-zip" Views/Admin/Shared/_AssessmentGroupsTab.cshtml && grep -c "bi-arrow-counterclockwise" Views/Admin/Shared/_AssessmentGroupsTab.cshtml && dotnet build --nologo --verbosity quiet</automated>
  </verify>

  <acceptance_criteria>
    - grep "BulkExportPdf" Views/Admin/Shared/_AssessmentGroupsTab.cshtml → 1 match (di baris baru, sebelumnya 0)
    - grep "BulkBackfill" Views/Admin/Shared/_AssessmentGroupsTab.cshtml → 1 match (di baris baru, sebelumnya 0)
    - grep "Bulk Export PDF (ZIP)" Views/Admin/Shared/_AssessmentGroupsTab.cshtml → 1 match (label string Bahasa Indonesia)
    - grep "Bulk Backfill (Restore Lost Data)" Views/Admin/Shared/_AssessmentGroupsTab.cshtml → 1 match
    - grep "dropdown-divider" Views/Admin/Shared/_AssessmentGroupsTab.cshtml → ≥1 match (boleh ada existing divider lain, minimum 1 baru)
    - Sequential ordering: di file output, baris "ExportAssessmentResults" (L278 existing) muncul SEBELUM baris "BulkExportPdf", yang muncul SEBELUM baris "dropdown-divider", yang muncul SEBELUM baris "BulkBackfill"
    - File tetap valid Razor syntax: dotnet build → 0 error 0 warning baru
    - URL helper untuk BulkExportPdf membawa 3 parameter: title, category, scheduleDate (sama dengan sibling ExportAssessmentResults L278) — grep regex `BulkExportPdf.*title.*category.*scheduleDate` matches
  </acceptance_criteria>

  <done>
    File `Views/Admin/Shared/_AssessmentGroupsTab.cshtml` ter-update: dropdown per-group sekarang punya 4 elemen baru (li-BulkExportPdf, li-divider, li-BulkBackfill) tepat antara existing Export Excel item (L281) dan token regenerate block (L282); dotnet build PASS 0 error; semua grep assertion di acceptance_criteria PASS; tidak ada existing line 260-281 yang dimodifikasi.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire REST-04 — Tambah Admin-only card "Bulk Backfill Restore" di Views/Admin/Index.cshtml Section D System</name>

  <read_first>
    - Views/Admin/Index.cshtml (full file — anchor L250-273 Section D System)
    - .planning/phases/339-v20.0-gap-closure-orphan-ui-title-validator/339-CONTEXT.md (D-02 top-nav surface decision)
    - Controllers/TrainingAdminController.cs L715-740 (BulkBackfill GET signature + [Authorize(Roles = "Admin")] attribute confirmation)
    - Views/Admin/BulkBackfill.cshtml (existing form view — read first 30 lines untuk confirm route active)
  </read_first>

  <files>Views/Admin/Index.cshtml</files>

  <action>
    Insert NEW Admin-only card block IMMEDIATELY AFTER Maintenance Mode card (closing `</div>` `}` block at end of Section D — sebelum closing `</div>` Section D row container).

    Lokasi tepat: SETELAH `}` penutup `@if (User.IsInRole("Admin") || User.IsInRole("HC"))` block Maintenance Mode (yang berakhir di sekitar L273), BEFORE `</div>` penutup `<div class="row g-3 mb-2">` Section D (sekitar L274).

    Konten verbatim (gate Admin-only `@if (User.IsInRole("Admin"))` — BUKAN OR HC karena endpoint `[Authorize(Roles = "Admin")]` di TrainingAdminController.cs L720):

    ```cshtml
    @if (User.IsInRole("Admin"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("BulkBackfill", "TrainingAdmin")" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-arrow-counterclockwise fs-5 text-warning"></i>
                        <span class="fw-bold">Bulk Backfill (Restore Lost Data)</span>
                    </div>
                    <small class="text-muted">Pulihkan AssessmentSession yang hilang/orphan dari TrainingRecord cadangan (Admin only — emergency restore tool)</small>
                </div>
            </div>
        </a>
    </div>
    }
    ```

    Pattern card menyamai existing card di Index.cshtml (col-md-4 + card shadow-sm + flex layout + bi-icon + fw-bold title + small text-muted description). Icon `bi-arrow-counterclockwise text-warning` konsisten dengan dropdown variant Task 1.

    JANGAN modifikasi card Maintenance Mode (L260-272 existing). JANGAN ubah Section A/B/C/D heading. Hanya INSERT 1 card baru di akhir Section D row.
  </action>

  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && grep -c "BulkBackfill" Views/Admin/Index.cshtml && grep -c "TrainingAdmin" Views/Admin/Index.cshtml && grep -c "User.IsInRole(\"Admin\")" Views/Admin/Index.cshtml && dotnet build --nologo --verbosity quiet</automated>
  </verify>

  <acceptance_criteria>
    - grep "BulkBackfill" Views/Admin/Index.cshtml → 1 match (baru, sebelumnya 0)
    - grep `Url.Action.*BulkBackfill.*TrainingAdmin` Views/Admin/Index.cshtml → 1 match
    - grep `@if \(User.IsInRole\("Admin"\)\)` Views/Admin/Index.cshtml → ≥1 match (Admin-only gate, BUKAN `|| HC`)
    - grep "Bulk Backfill (Restore Lost Data)" Views/Admin/Index.cshtml → 1 match (label Bahasa Indonesia konsisten dengan dropdown Task 1)
    - Card baru muncul SETELAH card Maintenance Mode (urutan: grep -n line number "Maintenance" < line number "BulkBackfill")
    - File tetap valid Razor: dotnet build → 0 error 0 warning baru
    - Surface count: greenpath "BulkBackfill" muncul di EXACTLY 2 file edited (`_AssessmentGroupsTab.cshtml` + `Index.cshtml`) PLUS `BulkBackfill.cshtml` view existing — totalnya 3 grep -l result via `grep -rl BulkBackfill Views/` (1 dropdown surface + 1 Admin Index surface + 1 form view existing)
  </acceptance_criteria>

  <done>
    File `Views/Admin/Index.cshtml` ter-update: Section D System sekarang punya 2 card (Maintenance Mode existing + Bulk Backfill baru); card baru Admin-only gated; link mengarah ke `/TrainingAdmin/BulkBackfill`; dotnet build PASS 0 error; semua grep di acceptance_criteria PASS; tidak ada existing card di Section A/B/C/D yang dimodifikasi.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire REST-06 — Conditional regex validator Title di AssessmentAdminController.CreateAssessment + asp-validation-for span di CreateAssessment.cshtml</name>

  <read_first>
    - Controllers/AssessmentAdminController.cs L815-860 (anchor signature L822 + existing auto-pair block L833-845 + token validation L847)
    - Views/Admin/CreateAssessment.cshtml L180-200 (anchor Title input block L183-193)
    - .planning/phases/339-v20.0-gap-closure-orphan-ui-title-validator/339-CONTEXT.md (D-03 verbatim code + D-04 regex rationale)
    - .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md (full — authoritative regex spec cited di error message)
    - .planning/phases/338-cilacap-ux-gap-bundle-excel-bulkpdf-restore-execute/338-05-SUMMARY.md (auto-pair Phase 338-05 — DO NOT regress)
    - Models/AssessmentSession.cs L1-30 (verify Title plain string, JANGAN modifikasi entity)
  </read_first>

  <files>Controllers/AssessmentAdminController.cs, Views/Admin/CreateAssessment.cshtml</files>

  <action>
    **Part A — Controllers/AssessmentAdminController.cs**

    Insert validator block INSIDE `CreateAssessment(...)` method body, IMMEDIATELY AFTER closing `}` of existing auto-pair block (di L845 saat ini — penutup `if (AssessmentTypeInput != "PrePostTest" && model.LinkedGroupId == null && !string.IsNullOrEmpty(model.Title)) { ... }`) dan SEBELUM `// Handle Token Validation` comment (L847).

    Konten verbatim per D-03 (gunakan FQN `System.Text.RegularExpressions.Regex` untuk hindari risiko using directive missing — penulis review file untuk konfirmasi `using System.Text.RegularExpressions;` ada di top; jika ada, boleh `Regex.IsMatch(...)` bare, kalau tidak ada gunakan FQN):

    ```csharp
    // Phase 339 REST-06 (336-NAMING-CONVENTION-SPEC): Validate Title pattern for standard Pre/Post tests
    if (AssessmentTypeInput != "PrePostTest"
        && !string.IsNullOrEmpty(model.Title)
        && !System.Text.RegularExpressions.Regex.IsMatch(model.Title, @"^(Pre|Post)\s*Test\s+.+$"))
    {
        ModelState.AddModelError("Title",
            "Title harus pola '{Stage} Test {Track} {Lokasi}' (Pre Test atau Post Test diikuti track + lokasi). " +
            "Contoh valid: 'Pre Test OJT GAST Cilacap'. Reference: 336-NAMING-CONVENTION-SPEC.");
    }
    ```

    **CRITICAL guard behavior:**
    - Guard SAMA dengan auto-pair block existing: HANYA jalan untuk `AssessmentTypeInput != "PrePostTest"` (standard CMP/IHT/Licencor/OTS — BUKAN PrePostTest dual-create mode, karena PrePostTest punya UI separate yang generate title sendiri)
    - SKIP jika `model.Title` empty/null (existing `[Required]` data annotation handles itu, regex check tidak perlu duplicate "required" error)
    - Regex pattern: `^(Pre|Post)\s*Test\s+.+$` (D-04 — toleran whitespace inkonsisten: matches `PreTest OJT`, `Pre Test OJT`, `Post Test IHT Refinery`)
    - JANGAN gunakan `[RegularExpression]` data annotation di Models/AssessmentSession.cs:13 (D-03 rationale: entity shared CMP/IHT/Licencor/OTS, annotation break legacy data + non-test categories)
    - JANGAN ubah auto-pair block L833-845 existing
    - JANGAN ubah signature L822 atau early `ModelState.Remove("UserId")` L831
    - JANGAN ubah token validation L847+ (insertion strictly BEFORE comment marker `// Handle Token Validation`)

    **Part B — Views/Admin/CreateAssessment.cshtml**

    Verify atau ADD `<span asp-validation-for="Title" class="text-danger small"></span>` di Title input block.

    Anchor: AFTER `</div>` penutup `<div class="d-flex justify-content-between">` (sekitar L192 — penutup row counter `0/255`), BEFORE `</div>` penutup `<div class="col-md-8">` (L193).

    Konten verbatim:
    ```cshtml
    <span asp-validation-for="Title" class="text-danger small"></span>
    ```

    Pattern konsisten dengan existing `<span asp-validation-for="Category" class="text-danger small"></span>` di Category block (L177). Jika sudah ada span asp-validation-for="Title" di file (defensive grep dulu), JANGAN duplicate — log skip.
  </action>

  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && grep -c 'Regex.IsMatch(model.Title' Controllers/AssessmentAdminController.cs && grep -c 'ModelState.AddModelError("Title"' Controllers/AssessmentAdminController.cs && grep -c '336-NAMING-CONVENTION-SPEC' Controllers/AssessmentAdminController.cs && grep -c 'asp-validation-for="Title"' Views/Admin/CreateAssessment.cshtml && dotnet build --nologo --verbosity quiet && dotnet test --nologo --verbosity quiet</automated>
  </verify>

  <acceptance_criteria>
    - grep `Regex\.IsMatch\(model\.Title.*Pre.*Post.*Test` Controllers/AssessmentAdminController.cs → 1 match (regex literal `^(Pre|Post)\s*Test\s+.+$`)
    - grep `ModelState.AddModelError\("Title"` Controllers/AssessmentAdminController.cs → ≥1 match (boleh ada existing AddModelError lain, minimum 1 baru terkait Title regex)
    - grep `336-NAMING-CONVENTION-SPEC` Controllers/AssessmentAdminController.cs → ≥1 match (di error message OR comment header)
    - grep `AssessmentTypeInput != "PrePostTest"` Controllers/AssessmentAdminController.cs → ≥2 match (1 existing di auto-pair block L835 + 1 baru di validator block — guard parity confirmed)
    - grep `asp-validation-for="Title"` Views/Admin/CreateAssessment.cshtml → ≥1 match (baru atau existing — di-add jika belum)
    - Inserted block muncul AFTER existing auto-pair block dan BEFORE `// Handle Token Validation` comment: line number `Regex.IsMatch(model.Title` > line number `Auto-paired LinkedGroupId` AND line number `Regex.IsMatch(model.Title` < line number `// Handle Token Validation`
    - dotnet build → 0 error 0 warning baru
    - dotnet test → 18/18 PASS (no regression — Phase 338 baseline preserved)
    - JANGAN ada modifikasi di Models/AssessmentSession.cs (grep -L `RegularExpression` Models/AssessmentSession.cs → file harus ada di output / atau git diff Models/AssessmentSession.cs harus empty)
    - JANGAN ada modifikasi di TryAutoDetectCounterpartGroup helper L6589 (git diff range L6580-6620 di AssessmentAdminController.cs harus empty)
  </acceptance_criteria>

  <done>
    Validator regex aktif di `CreateAssessment` POST handler — admin yang submit title tidak match `^(Pre|Post)\s*Test\s+.+$` dengan AssessmentTypeInput != PrePostTest akan dapat ModelState error "Title harus pola..." dirender di view via `<span asp-validation-for="Title">`. Auto-pair Phase 338-05 tetap jalan saat title valid. Models/AssessmentSession.cs UNTOUCHED. dotnet build + dotnet test PASS. Semua grep di acceptance_criteria PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /Admin/* | Sudah dikoveri oleh existing `[Authorize(Roles="Admin"/"Admin,HC")]` di endpoint target (BulkExportPdf, BulkBackfill, CreateAssessment) sejak Phase 338 — Phase 339 hanya menambah UI link discoverable, tidak buka trust boundary baru |

## STRIDE Threat Register

**Per Phase 339 D-06 LOCKED: No new threats — UI link addition (D-01 + D-02) hanya membuat endpoint existing discoverable, role check `[Authorize]` Phase 338 preserved verbatim. D-03 server-side regex validator = defensive HARDENING (rejects invalid input BEFORE DB insert) yang mengurangi attack surface, bukan menambah. Tidak ada new file upload, new query param, new auth surface, atau new endpoint.**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | 0 mitigations required — gap closure only wires UI ke endpoint Phase 338 yang already STRIDE-cleared, plus adds defensive validation. Existing role gates: `[Authorize(Roles="Admin,HC")]` (CreateAssessment + BulkExportPdf), `[Authorize(Roles="Admin")]` (BulkBackfill) preserved verbatim. Admin-only card di Views/Admin/Index.cshtml gated dengan `@if (User.IsInRole("Admin"))` matching endpoint authorize attribute. |
</threat_model>

<verification>
## Verification Commands

```bash
# CWD: PortalHC_KPB root
cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"

# 1. Build + test regression smoke (Nyquist gate)
dotnet build --nologo --verbosity quiet           # Expect: 0 error 0 warning baru
dotnet test  --nologo --verbosity quiet           # Expect: 18/18 PASS

# 2. Grep CIL-06 surface (Task 1)
grep -n "BulkExportPdf" Views/Admin/Shared/_AssessmentGroupsTab.cshtml          # Expect: 1 match new
grep -n "Bulk Export PDF (ZIP)" Views/Admin/Shared/_AssessmentGroupsTab.cshtml  # Expect: 1 match
grep -n "bi-file-zip" Views/Admin/Shared/_AssessmentGroupsTab.cshtml            # Expect: 1 match

# 3. Grep REST-04 surface — dual entry (Task 1 dropdown + Task 2 Admin Index)
grep -rln "BulkBackfill" Views/                                                 # Expect: ≥3 files (_AssessmentGroupsTab + Index + BulkBackfill.cshtml existing)
grep -n  "Url.Action.*BulkBackfill" Views/Admin/Shared/_AssessmentGroupsTab.cshtml  # Expect: 1 match
grep -n  "Url.Action.*BulkBackfill" Views/Admin/Index.cshtml                    # Expect: 1 match
grep -n  '@if (User.IsInRole("Admin"))' Views/Admin/Index.cshtml                # Expect: ≥1 match (Admin-only gate baru)

# 4. Grep REST-06 surface (Task 3)
grep -n "Regex.IsMatch(model.Title" Controllers/AssessmentAdminController.cs    # Expect: 1 match
grep -n 'ModelState.AddModelError("Title"' Controllers/AssessmentAdminController.cs  # Expect: ≥1 match new
grep -n "336-NAMING-CONVENTION-SPEC" Controllers/AssessmentAdminController.cs   # Expect: ≥1 match (comment/error)
grep -nc 'AssessmentTypeInput != "PrePostTest"' Controllers/AssessmentAdminController.cs  # Expect: 2 (existing auto-pair + new validator)
grep -n 'asp-validation-for="Title"' Views/Admin/CreateAssessment.cshtml        # Expect: ≥1 match

# 5. Negative assertions — no unintended modifications
git diff Models/AssessmentSession.cs       # Expect: empty diff (entity untouched per D-03)
git diff -- Views/Admin/BulkBackfill.cshtml  # Expect: empty diff (form view untouched, only linked-to)
```

## Manual UAT (Playwright, lokal localhost:5277)

Login admin via reference_dev_credentials (admin@pertamina.com), lalu execute skenario:

1. **CIL-06 — Bulk Export PDF discoverable + functional**
   - Visit `/Admin/ManageAssessment`
   - Klik per-group action dropdown (3-dot menu)
   - Expect: lihat item "Bulk Export PDF (ZIP)" (icon zip) DI BAWAH "Export Excel"
   - Klik "Bulk Export PDF (ZIP)"
   - Expect: ZIP download mulai (atau download dialog browser muncul); response 200 dengan Content-Type `application/zip`

2. **REST-04 — Bulk Backfill dual entry**
   - Visit `/Admin/ManageAssessment` per-group dropdown
   - Expect: lihat divider + item "Bulk Backfill (Restore Lost Data)" (icon arrow-counterclockwise)
   - Klik → expect redirect ke `/TrainingAdmin/BulkBackfill` form view
   - Visit `/Admin` (Kelola Data home)
   - Expect: Section D System punya card "Bulk Backfill (Restore Lost Data)" di samping "Maintenance Mode"
   - Klik card → expect redirect ke `/TrainingAdmin/BulkBackfill` form view
   - Logout, login sebagai HC role (non-Admin) → visit `/Admin` → expect: card "Bulk Backfill" TIDAK muncul (Admin-only gate)

3. **REST-06 — Title regex validation invalid path**
   - Login admin, visit `/Admin/CreateAssessment`
   - Pilih AssessmentTypeInput = "Standard"
   - Pilih Category = (any CMP/IHT/Licencor/OTS — non-PrePost)
   - Isi Title = `"Quiz Random"` (TIDAK match regex)
   - Submit
   - Expect: form re-render dengan error `<span asp-validation-for="Title">` menampilkan "Title harus pola '{Stage} Test {Track} {Lokasi}'..." — submit BLOCKED, tidak ada AssessmentSession baru di DB

4. **REST-06 — Title regex validation valid path + auto-pair preserved**
   - Same form, ganti Title = `"Pre Test OJT GAST Cilacap"` (match regex)
   - Submit
   - Expect: save sukses; jika counterpart `"Post Test OJT GAST Cilacap"` ada di DB → TempData Info "Auto-paired LinkedGroupId=..." muncul (Phase 338-05 preserved)

5. **REST-06 — PrePostTest mode SKIP regex check (parity guard)**
   - Same form, pilih AssessmentTypeInput = "PrePostTest"
   - Isi Title = `"Quiz Random"` (TIDAK match regex, tapi PrePostTest mode)
   - Submit (dengan PreSchedule + PostSchedule valid)
   - Expect: regex validator SKIP (guard `AssessmentTypeInput != "PrePostTest"` false), submit lanjut ke logic existing PrePostTest dual-create

6. **Regression smoke — Phase 338 auto-pair tidak rusak**
   - Visit `/Admin/CreateAssessment` standard mode dengan title pattern `Pre Test ...` yang punya counterpart Post existing → TempData Info auto-pair muncul (Phase 338-05 SUMMARY skenario)
</verification>

<success_criteria>

## Plan Closes When

- [ ] Task 1: `Views/Admin/Shared/_AssessmentGroupsTab.cshtml` punya 4 elemen baru (li-BulkExportPdf + li-divider + li-BulkBackfill) tepat di antara L281 dan L282; semua grep Task 1 PASS
- [ ] Task 2: `Views/Admin/Index.cshtml` Section D punya card baru "Bulk Backfill" Admin-only gated `@if (User.IsInRole("Admin"))`; muncul setelah card Maintenance Mode; semua grep Task 2 PASS
- [ ] Task 3: `Controllers/AssessmentAdminController.cs` punya regex validator block AFTER auto-pair (L845) BEFORE token validation (L847); `Views/Admin/CreateAssessment.cshtml` punya `<span asp-validation-for="Title">`; Models/AssessmentSession.cs UNTOUCHED; semua grep Task 3 PASS
- [ ] `dotnet build` → 0 error 0 warning baru
- [ ] `dotnet test` → 18/18 PASS (Phase 338 regression baseline preserved)
- [ ] Manual UAT Playwright 6 skenario PASS (lihat `<verification>` block)
- [ ] CIL-06 + REST-04 + REST-06 di `.planning/v20.0-MILESTONE-AUDIT.md` status partial → closed (audit re-run optional, di luar scope Phase 339 task — handled via `/gsd-complete-milestone v20.0` housekeeping)
- [ ] Git commit per CLAUDE.md convention: 3 commit (1 per task) atau 1 commit gabungan (executor decision) dengan message Bahasa Indonesia + Co-Authored-By footer
- [ ] **PUSH STATUS:** TIDAK push origin/main — Phase 339 bundle ke v20.0 batch (146 commit pending per STATE.md L23 next-action #1)

## Phase Goal Closed When

- 3 partial REQ (CIL-06, REST-04, REST-06) wired/validated
- 0 new endpoints (per Phase 339 D-domain — semua endpoint existing Phase 338)
- 0 new threat surface (per D-06)
- 0 entity modification (per D-03)
- 0 migration (no schema change)

</success_criteria>

<output>
After plan completion, executor creates `.planning/phases/339-v20.0-gap-closure-orphan-ui-title-validator/339-01-SUMMARY.md` per template `$HOME/.claude/get-shit-done/templates/summary.md` with:

- Frontmatter: phase, plan, requirements (CIL-06+REST-04+REST-06), commit_hashes, files_modified
- Sections: What Was Built (4 surface), Decisions Honored (D-01..D-07 reference), Verification Evidence (grep output + build/test pass + UAT Playwright 6 skenario), Deferred (per CONTEXT.md `<deferred>` block — VERIFICATION backfill, REQUIREMENTS sync, MILESTONES log handled via separate `/gsd-complete-milestone v20.0`), Push Status (NOT PUSHED — bundle ke v20.0 batch 146 commit)
- Cross-reference: Phase 338-04-SUMMARY.md (CIL-06+REST-04 endpoint origin), Phase 338-05-SUMMARY.md (REST-06 auto-pair origin), Phase 336-NAMING-CONVENTION-SPEC.md (regex authoritative)
</output>
