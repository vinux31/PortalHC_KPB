---
phase: 342
plan: 03
type: execute
wave: 3
depends_on:
  - "342-01"
  - "342-02"
files_modified:
  - HcPortal.Tests/OrganizationControllerTests.cs
autonomous: false
requirements:
  - ORG-TREE-02
  - ORG-TREE-07
tags:
  - xunit
  - test
  - cascade-accuracy
  - uat
user_setup: []

must_haves:
  truths:
    - "File baru HcPortal.Tests/OrganizationControllerTests.cs ada dengan fixture InMemory (Guid.NewGuid DB) + OrganizationController(ctx, null!, auditLog, null!)"
    - "Test ORG-TREE-02: AddOrganizationUnit_SameNameDifferentParent_Accepted PASS (Operations boleh di 2 Bagian beda)"
    - "Test ORG-TREE-02: AddOrganizationUnit_SameNameSameParent_Rejected PASS (Operations ditolak di parent sama)"
    - "Test ORG-TREE-02: EditOrganizationUnit_SameNameSameParent_Rejected PASS (rename reject dup parent sama exclude self)"
    - "Test ORG-TREE-07: PreviewEditCascade_RenameLevel0_CountMatchesActual PASS (preview count == actual mutated count, 4 field-pair)"
    - "Test ORG-TREE-07: PreviewEditCascade_RenameLevel1_CountMatchesActual PASS (Unit/AssignmentUnit/Unit/Unit count == actual)"
    - "Test ORG-TREE-07: PreviewEditCascade_NoChange_ReturnsEarlyFalseFlags PASS (D-04 early-return)"
    - "Casing test pakai casing IDENTIK (Pitfall 5 — InMemory case-sensitive, tidak andalkan case-insensitivity)"
    - "Full suite `dotnet test HcPortal.Tests` PASS — existing 20 test (Phase 340/341) tidak regress + test baru green"
    - "Manual UAT formal terverifikasi user (10-skenario browser) per CLAUDE.md dev workflow step 3"
  artifacts:
    - path: "HcPortal.Tests/OrganizationControllerTests.cs"
      provides: "xUnit fixture + dup-name per-parent + PreviewEditCascade count-accuracy + early-return tests"
      contains: "public class OrganizationControllerTests"
      min_lines: 120
  key_links:
    - from: "HcPortal.Tests/OrganizationControllerTests.cs"
      to: "Controllers/OrganizationController.cs PreviewEditCascade (Plan 01)"
      via: "ctrl.PreviewEditCascade(id, name, parentId) → JsonResult reflection GetInt"
      pattern: "PreviewEditCascade"
    - from: "HcPortal.Tests/OrganizationControllerTests.cs"
      to: "Controllers/OrganizationController.cs Add/EditOrganizationUnit dup-check (Plan 01)"
      via: "ctrl.AddOrganizationUnit / EditOrganizationUnit → GetSuccess reflection"
      pattern: "AddOrganizationUnit"
---

<objective>
Kunci correctness backend Phase 342 dengan xUnit + verifikasi UAT formal. Tulis `HcPortal.Tests/OrganizationControllerTests.cs` (file baru) yang mengunci ORG-TREE-02 (dup-name per-parent accept/reject) dan ORG-TREE-07 (preview count == actual mutated count — kunci Pitfall 1). Lalu checkpoint manual UAT 10-skenario sebelum phase ditutup.

Purpose: Test preview==actual adalah satu-satunya cara mengunci predikat PreviewEditCascade tidak drift dari mutasi EditOrganizationUnit (Pitfall 1, prioritas tertinggi RESEARCH). Dup-name per-parent test mengunci "Operations" boleh di 2 Bagian. Formal xUnit + Playwright lengkap secara nominal scope Phase 344, tetapi nyquist_validation aktif → test backend murah ditulis di sini (langsung testable, < 5s InMemory). UAT formal menutup loop visual/UX yang tidak ter-cover xUnit.

Output: `HcPortal.Tests/OrganizationControllerTests.cs` (~150 LoC, 6 [Fact]). Full suite green (20 existing + 6 baru). Manual UAT approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v21.0-REQUIREMENTS.md
@.planning/phases/342-manageorganization-page-fixes/342-CONTEXT.md
@.planning/phases/342-manageorganization-page-fixes/342-RESEARCH.md
@.planning/phases/342-manageorganization-page-fixes/342-PATTERNS.md
@.planning/phases/342-manageorganization-page-fixes/342-VALIDATION.md
@CLAUDE.md

# Artifact yang dikonsumsi (Plan 01 — sudah merged sebelum plan ini)
@Controllers/OrganizationController.cs

# Analog fixture pattern (read-only — copy)
@HcPortal.Tests/OrgLabelControllerTests.cs

<interfaces>
<!-- Constructor + fixture facts diverifikasi sesi 2026-06-03. -->
<!-- Executor: pakai langsung — JANGAN eksplorasi lebih dalam. -->

OrganizationController constructor (verbatim, OrganizationController.cs L14-21):
  public OrganizationController(
      ApplicationDbContext context,
      UserManager<ApplicationUser> userManager,
      AuditLogService auditLog,
      IWebHostEnvironment env)

AdminBaseController constructor (L20-30): HANYA assign field — TIDAK dereference.
  → new OrganizationController(ctx, null!, auditLog, null!) AMAN untuk Add/Edit/PreviewEditCascade
    (action ini tidak panggil _userManager/_env). auditLog = new AuditLogService(ctx).

Action signature (Plan 01):
  Task<IActionResult> AddOrganizationUnit(string name, int? parentId)
  Task<IActionResult> EditOrganizationUnit(int id, string name, int? parentId)
  Task<IActionResult> PreviewEditCascade(int id, string name, int? parentId)

Add/Edit return Json(new { success, message }) bila IsAjaxRequest(), ATAU RedirectToAction non-AJAX.
  >>> Test fixture default HttpContext TIDAK set X-Requested-With → IsAjaxRequest()==false → RedirectToAction.
  >>> Untuk assert success/fail dup-check via Json: set header X-Requested-With di ControllerContext
      (lihat OrgLabelControllerTests L62 DefaultHttpContext) — TAMBAH Request.Headers["X-Requested-With"]="XMLHttpRequest".

PreviewEditCascade SELALU return Json (AJAX-only, tidak ada cabang IsAjaxRequest) → tidak butuh header.

PreviewEditCascade JSON properties: nameChanged, parentChanged, affectedUsersCount,
  affectedMappingsCount, affectedKompetensiCount, affectedGuidanceCount
  ATAU early-return { nameChanged=false, parentChanged=false } (hanya 2 prop).

Model field (DIVERIFIKASI) untuk seed denormalized:
  ApplicationUser.Section / .Unit
  CoachCoacheeMapping.AssignmentSection / .AssignmentUnit
  ProtonKompetensi.Bagian / .Unit         (DbSet ProtonKompetensiList)
  CoachingGuidanceFile.Bagian / .Unit     (DbSet CoachingGuidanceFiles)
  OrganizationUnit { Id, Name, Level, ParentId, IsActive, DisplayOrder }

JsonResult reflection helper (dari OrgLabelControllerTests pattern):
  GetSuccess(IActionResult) → (bool)json.Value.GetType().GetProperty("success").GetValue(...)
  GetInt(IActionResult, prop) → (int)json.Value.GetType().GetProperty(prop).GetValue(...)
  GetBool(IActionResult, prop) → (bool)json.Value.GetType().GetProperty(prop).GetValue(...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create OrganizationControllerTests.cs — fixture + dup-name per-parent (ORG-TREE-02)</name>
  <files>HcPortal.Tests/OrganizationControllerTests.cs</files>
  <read_first>
    - 342-RESEARCH.md §Validation Architecture (fixture factory + Pattern A dup-name + Sampling Rate + Wave 0 Gaps) + Pitfall 5 (casing)
    - 342-PATTERNS.md §"No Analog Found" (test file analog = OrgLabelControllerTests) + §Validation note (UserManager null safe)
    - HcPortal.Tests/OrgLabelControllerTests.cs L27-90 (fixture factory + GetSuccess/reflection helpers — copy pattern)
    - Controllers/OrganizationController.cs L74-122 (AddOrganizationUnit) + L128-156 (EditOrganizationUnit dup-check) + L1-21 (constructor + usings)
    - <interfaces> block (constructor null! safety + IsAjaxRequest header requirement)
  </read_first>
  <action>
    Create file BARU `HcPortal.Tests/OrganizationControllerTests.cs`, namespace `HcPortal.Tests`.

    **Header comment:**
    ```csharp
    // Phase 342 Plan 03 — OrganizationController dup-name per-parent + PreviewEditCascade accuracy.
    // REQ coverage: ORG-TREE-02 (per-parent uniqueness), ORG-TREE-07 (preview count == actual cascade).
    // Strategy: InMemory DB (Guid per test) + UserManager/env null-substitute (Add/Edit/Preview tidak deref).
    // Pitfall 5: casing IDENTIK (InMemory case-sensitive vs SQL Server CI) — jangan andalkan case-insensitivity.
    ```

    **Usings:** `Microsoft.AspNetCore.Http; Microsoft.AspNetCore.Mvc; Microsoft.EntityFrameworkCore; HcPortal.Controllers; HcPortal.Data; HcPortal.Models; HcPortal.Services; Xunit;`

    **Fixture factory** (adaptasi OrgLabelControllerTests L27-64 — beda: controller ctor 4-arg + set X-Requested-With agar Add/Edit return Json):
    ```csharp
    public class OrganizationControllerTests
    {
        private static (OrganizationController ctrl, ApplicationDbContext ctx) MakeController()
        {
            var options = new DbContextOptionsBuilder<ApplicationDbContext>()
                .UseInMemoryDatabase(Guid.NewGuid().ToString())
                .Options;
            var ctx = new ApplicationDbContext(options);
            var auditLog = new AuditLogService(ctx);
            #pragma warning disable CS8625
            var ctrl = new OrganizationController(ctx, null!, auditLog, null!);
            #pragma warning restore CS8625
            var httpContext = new DefaultHttpContext();
            httpContext.Request.Headers["X-Requested-With"] = "XMLHttpRequest";  // → IsAjaxRequest()==true → Json
            ctrl.ControllerContext = new ControllerContext { HttpContext = httpContext };
            return (ctrl, ctx);
        }

        private static bool GetSuccess(IActionResult r)
        {
            var j = Assert.IsType<JsonResult>(r);
            return (bool)j.Value!.GetType().GetProperty("success")!.GetValue(j.Value)!;
        }
        private static int GetInt(IActionResult r, string prop)
        {
            var j = Assert.IsType<JsonResult>(r);
            return (int)j.Value!.GetType().GetProperty(prop)!.GetValue(j.Value)!;
        }
        private static bool GetBool(IActionResult r, string prop)
        {
            var j = Assert.IsType<JsonResult>(r);
            return (bool)j.Value!.GetType().GetProperty(prop)!.GetValue(j.Value)!;
        }
    ```

    **3 [Fact] dup-name (ORG-TREE-02)** — RESEARCH Pattern A:
    ```csharp
        [Fact]
        public async Task AddOrganizationUnit_SameNameDifferentParent_Accepted()
        {
            var (ctrl, ctx) = MakeController();
            ctx.OrganizationUnits.AddRange(
                new OrganizationUnit { Id = 1, Name = "RFCC", Level = 0, ParentId = null, IsActive = true },
                new OrganizationUnit { Id = 2, Name = "HSC",  Level = 0, ParentId = null, IsActive = true },
                new OrganizationUnit { Id = 3, Name = "Operations", Level = 1, ParentId = 1, IsActive = true });
            ctx.SaveChanges();
            var result = await ctrl.AddOrganizationUnit("Operations", 2);   // parent beda (HSC) → boleh
            Assert.True(GetSuccess(result));
        }

        [Fact]
        public async Task AddOrganizationUnit_SameNameSameParent_Rejected()
        {
            var (ctrl, ctx) = MakeController();
            ctx.OrganizationUnits.AddRange(
                new OrganizationUnit { Id = 1, Name = "RFCC", Level = 0, ParentId = null, IsActive = true },
                new OrganizationUnit { Id = 3, Name = "Operations", Level = 1, ParentId = 1, IsActive = true });
            ctx.SaveChanges();
            var result = await ctrl.AddOrganizationUnit("Operations", 1);   // parent sama → ditolak
            Assert.False(GetSuccess(result));
        }

        [Fact]
        public async Task EditOrganizationUnit_SameNameSameParent_Rejected()
        {
            var (ctrl, ctx) = MakeController();
            ctx.OrganizationUnits.AddRange(
                new OrganizationUnit { Id = 1, Name = "RFCC", Level = 0, ParentId = null, IsActive = true },
                new OrganizationUnit { Id = 3, Name = "Operations", Level = 1, ParentId = 1, IsActive = true },
                new OrganizationUnit { Id = 4, Name = "Maintenance", Level = 1, ParentId = 1, IsActive = true });
            ctx.SaveChanges();
            // Rename "Maintenance" (id 4) jadi "Operations" di parent sama (1) → ditolak (dup, exclude self)
            var result = await ctrl.EditOrganizationUnit(4, "Operations", 1);
            Assert.False(GetSuccess(result));
        }
    ```
    Pitfall 5: SEMUA nama pakai casing identik ("Operations" vs "Operations"). JANGAN tulis test yang bergantung case-insensitivity.

    Tutup class `}` (test PreviewEditCascade ditambah Task 2).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --nologo --filter "FullyQualifiedName~OrganizationControllerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.Tests` PASS (compile clean)
    - File `HcPortal.Tests/OrganizationControllerTests.cs` ada
    - `grep -c "public class OrganizationControllerTests" HcPortal.Tests/OrganizationControllerTests.cs` = 1
    - `grep -c "new OrganizationController(ctx, null!, auditLog, null!)" HcPortal.Tests/OrganizationControllerTests.cs` = 1
    - `grep -c "X-Requested-With" HcPortal.Tests/OrganizationControllerTests.cs` = 1 (Json response enable)
    - `grep -c "\[Fact\]" HcPortal.Tests/OrganizationControllerTests.cs` = 3 (dup-name trio)
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~OrganizationControllerTests"` → 3 passed, 0 failed
  </acceptance_criteria>
  <done>Fixture + 3 dup-name test green (accept-parent-beda, reject-parent-sama Add, reject-parent-sama Edit); ORG-TREE-02 terkunci; siap tambah PreviewEditCascade test Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PreviewEditCascade accuracy + early-return tests (ORG-TREE-07, Pitfall 1)</name>
  <files>HcPortal.Tests/OrganizationControllerTests.cs</files>
  <read_first>
    - 342-RESEARCH.md §Validation Architecture Pattern B (preview==actual) + Pattern C (early-return) + §Common Pitfalls 1 (predicate drift)
    - 342-PATTERNS.md §"Controllers/OrganizationController.cs" A2 (count predikat mirror — field-pair reference)
    - 342-CONTEXT.md §D-04 (early-return) + 342-01-PLAN.md A1 decision (reparent full-accuracy — note dari Plan 01 SUMMARY)
    - Controllers/OrganizationController.cs L191-263 (cascade mutasi) + PreviewEditCascade (Plan 01, sudah merged)
    - HcPortal.Tests/OrganizationControllerTests.cs (Task 1 — extend class yang sama)
  </read_first>
  <action>
    Extend `HcPortal.Tests/OrganizationControllerTests.cs` (tambah 3 [Fact] di dalam class yang sama, SEBELUM `}` penutup).

    **Helper seed denormalized** (tambah private static, atau inline):
    Seed N user dengan Section, M mapping AssignmentSection, K kompetensi Bagian, G guidance Bagian — semua = nama lama unit Level 0.

    **[Fact] PreviewEditCascade_RenameLevel0_CountMatchesActual** (Pattern B — kunci Pitfall 1):
    ```csharp
        [Fact]
        public async Task PreviewEditCascade_RenameLevel0_CountMatchesActual()
        {
            var (ctrl, ctx) = MakeController();
            ctx.OrganizationUnits.Add(new OrganizationUnit { Id = 1, Name = "RFCC", Level = 0, ParentId = null, IsActive = true });
            // seed denormalized Section="RFCC": 3 user, 2 mapping, 4 kompetensi, 1 guidance
            ctx.Users.AddRange(
                new ApplicationUser { Id = "u1", UserName = "u1", Section = "RFCC" },
                new ApplicationUser { Id = "u2", UserName = "u2", Section = "RFCC" },
                new ApplicationUser { Id = "u3", UserName = "u3", Section = "RFCC" });
            ctx.CoachCoacheeMappings.AddRange(
                new CoachCoacheeMapping { AssignmentSection = "RFCC" },
                new CoachCoacheeMapping { AssignmentSection = "RFCC" });
            ctx.ProtonKompetensiList.AddRange(
                new ProtonKompetensi { Bagian = "RFCC" }, new ProtonKompetensi { Bagian = "RFCC" },
                new ProtonKompetensi { Bagian = "RFCC" }, new ProtonKompetensi { Bagian = "RFCC" });
            ctx.CoachingGuidanceFiles.Add(new CoachingGuidanceFile { Bagian = "RFCC" });
            ctx.SaveChanges();

            // ACT 1: preview SEBELUM mutasi
            var preview = await ctrl.PreviewEditCascade(1, "Refinery Complex", null);
            int pUsers = GetInt(preview, "affectedUsersCount");
            int pMap   = GetInt(preview, "affectedMappingsCount");
            int pKomp  = GetInt(preview, "affectedKompetensiCount");
            int pGuid  = GetInt(preview, "affectedGuidanceCount");

            // ACT 2: edit aktual (mutasi cascade) — share ctx
            await ctrl.EditOrganizationUnit(1, "Refinery Complex", null);
            int aUsers = ctx.Users.Count(u => u.Section == "Refinery Complex");
            int aMap   = ctx.CoachCoacheeMappings.Count(m => m.AssignmentSection == "Refinery Complex");
            int aKomp  = ctx.ProtonKompetensiList.Count(k => k.Bagian == "Refinery Complex");
            int aGuid  = ctx.CoachingGuidanceFiles.Count(g => g.Bagian == "Refinery Complex");

            // ASSERT: preview == actual (predikat tidak drift — Pitfall 1)
            Assert.Equal(aUsers, pUsers);
            Assert.Equal(aMap, pMap);
            Assert.Equal(aKomp, pKomp);
            Assert.Equal(aGuid, pGuid);
            Assert.Equal(3, pUsers);   // sanity
        }
    ```
    Sesuaikan property `ApplicationUser`/`CoachCoacheeMapping`/`ProtonKompetensi`/`CoachingGuidanceFile` dengan required member aktual (read model bila perlu — set minimal field non-null untuk InMemory insert; jangan ikut TrainingRecord FK kalau tidak perlu).

    **[Fact] PreviewEditCascade_RenameLevel1_CountMatchesActual** — sama pola, unit Level 1, seed Users.Unit/AssignmentUnit/Unit/Unit, assert preview==actual untuk field-pair Level>=1.

    **[Fact] PreviewEditCascade_NoChange_ReturnsEarlyFalseFlags** (Pattern C, D-04):
    ```csharp
        [Fact]
        public async Task PreviewEditCascade_NoChange_ReturnsEarlyFalseFlags()
        {
            var (ctrl, ctx) = MakeController();
            ctx.OrganizationUnits.Add(new OrganizationUnit { Id = 1, Name = "RFCC", Level = 0, ParentId = null, IsActive = true });
            ctx.SaveChanges();
            var result = await ctrl.PreviewEditCascade(1, "RFCC", null);   // nama+parent sama
            Assert.False(GetBool(result, "nameChanged"));
            Assert.False(GetBool(result, "parentChanged"));
        }
    ```

    **Catatan implementasi:**
    - Preview + Edit share `ctx` yang sama → urutkan preview DULU (read-only), lalu Edit (mutasi), lalu hitung baris bernama baru.
    - Equality `pX == aX` mengunci predikat tidak drift (Pitfall 1).
    - Jika model punya required non-nullable member tambahan yang menggagalkan InMemory insert, set nilai minimal valid (mis. string kosong / default) — JANGAN tambah FK dependency tidak perlu.
    - JANGAN ubah test dup-name Task 1.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --nologo --filter "FullyQualifiedName~OrganizationControllerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.Tests` PASS
    - `grep -c "\[Fact\]" HcPortal.Tests/OrganizationControllerTests.cs` = 6 (3 dup + 3 preview)
    - `grep -c "PreviewEditCascade_RenameLevel0_CountMatchesActual" HcPortal.Tests/OrganizationControllerTests.cs` = 1
    - `grep -c "PreviewEditCascade_RenameLevel1_CountMatchesActual" HcPortal.Tests/OrganizationControllerTests.cs` = 1
    - `grep -c "PreviewEditCascade_NoChange_ReturnsEarlyFalseFlags" HcPortal.Tests/OrganizationControllerTests.cs` = 1
    - `grep -c "Assert.Equal(aUsers, pUsers)" HcPortal.Tests/OrganizationControllerTests.cs` = 1 (preview==actual)
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~OrganizationControllerTests"` → 6 passed, 0 failed
    - `dotnet test HcPortal.Tests` → full suite PASS (20 existing + 6 baru = 26, 0 failed; regression-safe)
  </acceptance_criteria>
  <done>6 test green; ORG-TREE-07 preview-count==actual terkunci (Pitfall 1 mitigated) + D-04 early-return verified; full suite regression-safe; siap UAT formal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT formal — 10 skenario ManageOrganization end-to-end</name>
  <files>(none — verification-only checkpoint, no file change)</files>
  <action>Manual UAT formal — lihat <how-to-verify> di bawah. TIDAK ada perubahan kode di task ini; checkpoint mengkonfirmasi akurasi end-to-end (cascade count modal == DB aktual lokal) + visual/UX yang tidak ter-cover xUnit. Prasyarat: `dotnet test HcPortal.Tests` 26/26 PASS + `dotnet run`, lalu eksekusi 10 skenario per CLAUDE.md dev workflow step 3.</action>
  <verify>
    <automated>MANUAL — UAT 10-skenario di http://localhost:5277/Admin/ManageOrganization + verifikasi cascade count vs DB lokal (lihat how-to-verify). Automated gate `dotnet test HcPortal.Tests` 26/26 sudah PASS di Task 1-2.</automated>
  </verify>
  <done>User ketik "approved" setelah 10 skenario lulus dan cascade count modal terverifikasi vs DB aktual; deviasi/gap dicatat di SUMMARY bila ada.</done>
  <what-built>
    Phase 342 lengkap: backend dup-name per-parent + PreviewEditCascade (Plan 01, terkunci 6 xUnit), frontend pre-order DFS + parent nonaktif + escape + palette + path + cascade modal + legend + title + badge (Plan 02). Full suite 26 test green.
  </what-built>
  <how-to-verify>
    Prasyarat: `dotnet build` PASS + `dotnet test HcPortal.Tests` 26/26 PASS. `dotnet run` → login Admin (admin@pertamina.com) → `http://localhost:5277/Admin/ManageOrganization`. DevTools Console terbuka (target ZERO error). Cek DB lokal bila perlu konfirmasi cascade (per CLAUDE.md step 3).

    1. **Pre-order DFS dropdown (ORG-TREE-01):** Modal Tambah/Edit → dropdown Induk urut parent→keturunan→sibling (hierarki indent), bukan flat per-level.
    2. **Dup-name per-parent (ORG-TREE-02):** Tambah "Operations" di Bagian A → sukses; ulang di Bagian A → ditolak ("Nama unit sudah digunakan"); tambah "Operations" di Bagian B → sukses.
    3. **Parent nonaktif (ORG-TREE-03):** Toggle 1 parent nonaktif → buka modal → parent itu MASIH di dropdown, suffix " (nonaktif)" + grey.
    4. **Escape (ORG-TREE-04):** Rename/tambah unit nama ada kutip (`O'Brien`) → Hapus → modal tampil nama benar, console zero error.
    5. **Level palette (ORG-TREE-05):** Node level 3+ (jika ada) → warna icon berbeda dari level 2.
    6. **Path breadcrumb (ORG-TREE-06):** Pilih induk di modal → "Path: ... → (unit baru di sini)" real-time + update saat ganti.
    7. **Cascade-confirm (ORG-TREE-07):** Edit unit dengan >0 user (rename) → Simpan → modal "Konfirmasi Perubahan" 4-line count akurat (bandingkan dengan jumlah user di DB lokal). [Batal] abort. [Lanjut Simpan] tersimpan + cascade DB ter-update (cek `SELECT COUNT(*) FROM AspNetUsers WHERE Section='nama-baru'` = count yang ditampilkan modal). Edit tanpa impact → langsung simpan tanpa modal (D-04).
    8. **Legend (ORG-TREE-08):** Card-header tampil legend swatch warna + label tier dari GetLevelLabels.
    9. **Modal title dynamic (ORG-TREE-09):** Tambah di root → "Tambah Bagian"; di bawah Bagian → "Tambah Unit"; di bawah Unit → "Tambah Sub-unit".
    10. **Tier badge + regression (ORG-TREE-10 + Pitfall 2):** Tiap row badge tier label warna sesuai. Drag-reorder + toggle + hapus tetap jalan. Console zero error.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila 10 skenario lulus, atau jelaskan isu spesifik (nomor + gejala + screenshot) untuk gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → Controller | xUnit instantiate controller dengan InMemory ctx — verifikasi server-authoritative dup + count |
| UAT browser → live server | Manual verifikasi end-to-end cascade akurasi vs DB aktual lokal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-342-12 | Tampering | PreviewEditCascade count drift dari mutasi (regression masa depan) | mitigate | Test preview==actual (Task 2) mengunci predikat field-by-field; gagal bila predikat menyimpang — regression tripwire |
| T-342-13 | Tampering | Dup-name per-parent regресi (kembali ke global) | mitigate | Test accept-parent-beda + reject-parent-sama (Task 1) mengunci scope per-parent |
| T-342-14 | Information Disclosure | UAT verifikasi count modal vs DB (sensitive count) | accept | Verifikasi oleh user Admin lokal; count org structure bukan PII |
</threat_model>

<verification>
- `dotnet build HcPortal.Tests` PASS
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~OrganizationControllerTests"` → 6 passed
- `dotnet test HcPortal.Tests` → full suite PASS (26 total, 0 failed — 20 existing Phase 340/341 + 6 baru)
- Manual UAT Task 3: 10 skenario approved (cascade count modal == DB aktual lokal)
</verification>

<success_criteria>
1. **Test file ada:** `HcPortal.Tests/OrganizationControllerTests.cs` dengan 6 [Fact].
2. **ORG-TREE-02 locked:** 3 dup-name test green (Pitfall 5 casing identik).
3. **ORG-TREE-07 locked:** preview==actual (Level 0 + Level 1) + early-return D-04 green (Pitfall 1 mitigated).
4. **Full suite regression-safe:** `dotnet test HcPortal.Tests` 26/26 PASS (existing tidak regress).
5. **UAT approved:** 10 skenario browser lulus, cascade count modal terverifikasi vs DB aktual.
6. **Threat T-342-12, 13 mitigated:** Test tripwire mencegah drift + regресi dup-scope.
</success_criteria>

<output>
After completion, create `.planning/phases/342-manageorganization-page-fixes/342-03-SUMMARY.md` capturing:
- Final commit hash range untuk 3 task
- Test count: 6 baru (3 dup + 3 preview), full suite total (20 → 26)
- preview==actual evidence (count match Level 0 + Level 1)
- Any model required-member adjustment di seed (deviation dari Pattern B template)
- Manual UAT result: 10-skenario pass/fail + cascade count vs DB verification evidence
- Phase 342 closure note: 10/10 ORG-TREE requirement covered (01-10); ready /gsd-verify-work
</output>
