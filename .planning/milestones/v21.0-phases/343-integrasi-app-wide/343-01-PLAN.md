---
phase: 343-integrasi-app-wide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/_ViewImports.cshtml
  - .planning/phases/343-integrasi-app-wide/343-AUDIT.md
autonomous: true
requirements: [ORG-INTEG-01, ORG-INTEG-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Semua view + partial di bawah Views/ mendapat OrgLabels via 1 baris @inject global (build resolve)"
    - "Audit deliverable SC1 mencatat keputusan ganti/skip per occurrence untuk SEMUA area target (7 area + Account + controller)"
    - "ORG-INTEG-02 terdokumentasi: semua controller display-bearing string = export/audit/validation = legit SKIP (no actionable REPLACE)"
  artifacts:
    - path: "Views/_ViewImports.cshtml"
      provides: "Global @inject IOrgLabelService OrgLabels (D-01)"
      contains: "@inject HcPortal.Services.IOrgLabelService OrgLabels"
    - path: ".planning/phases/343-integrasi-app-wide/343-AUDIT.md"
      provides: "SC1 occurrence audit table (REPLACE/SKIP/AMBIGUOUS) + empty-folder finding + controller ORG-INTEG-02 verdict"
      min_lines: 60
  key_links:
    - from: "Views/_ViewImports.cshtml"
      to: "HcPortal.Services.IOrgLabelService"
      via: "@inject (resolve via Program.cs L65 AddScoped)"
      pattern: "@inject HcPortal\\.Services\\.IOrgLabelService OrgLabels"
---

<objective>
Pasang fondasi Phase 343: tambah 1 baris `@inject HcPortal.Services.IOrgLabelService OrgLabels` global di `Views/_ViewImports.cshtml` (D-01), DAN produksi audit deliverable SC1 (tabel klasifikasi occurrence REPLACE/SKIP/AMBIGUOUS per file + temuan empty-folder + verdict controller ORG-INTEG-02).

Purpose: `@inject` global = prasyarat WAJIB sebelum swap apa pun (swap `@OrgLabels.GetLabel(N)` tanpa @inject = build break). Audit deliverable = pemenuhan SC1 + SC3 traceability + ORG-INTEG-02 (dokumentasi "no actionable controller REPLACE" sebagai pemenuhan REQ).

Output: `_ViewImports.cshtml` (+1 baris) + `343-AUDIT.md` (deliverable baru).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/343-integrasi-app-wide/343-CONTEXT.md
@.planning/phases/343-integrasi-app-wide/343-RESEARCH.md
@.planning/phases/343-integrasi-app-wide/343-PATTERNS.md
@.planning/phases/343-integrasi-app-wide/343-VALIDATION.md

<interfaces>
<!-- Kontrak yang dibutuhkan executor — sudah diverifikasi dari codebase. Tidak perlu eksplorasi tambahan. -->

Services/IOrgLabelService.cs (namespace = HcPortal.Services):
```csharp
namespace HcPortal.Services;
public interface IOrgLabelService {
    string GetLabel(int level);   // 0=Bagian, 1=Unit, 2=Sub-unit; fallback "Level {N}" bila tabel kosong
    // ... GetAll() dll
}
```

Program.cs L65 (sudah register — @inject akan resolve):
```csharp
builder.Services.AddScoped<HcPortal.Services.IOrgLabelService, HcPortal.Services.OrgLabelService>();
```

Views/_ViewImports.cshtml (BEFORE — VERIFIED, belum ada @inject custom):
```razor
@using HcPortal
@using HcPortal.Models
@using HcPortal.Models.Guide
@using System.Text.Json
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
```

Analog @inject fully-qualified existing — Views/Shared/_Layout.cshtml:8 (pola yang HARUS diikuti):
```razor
@inject HcPortal.Data.ApplicationDbContext _maintenanceDb
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah @inject global ke Views/_ViewImports.cshtml</name>
  <files>Views/_ViewImports.cshtml</files>
  <read_first>
    - Views/_ViewImports.cshtml (file yang dimodifikasi — 5 baris existing)
    - 343-RESEARCH.md §"Mekanisme @inject (Verified)" (L74-97) + §"Code Examples" (L381-390)
    - 343-PATTERNS.md §`Views/_ViewImports.cshtml` FOUNDATION (L50-96) + analog _Layout.cshtml:5-8
    - 343-CONTEXT.md D-01 (@inject placement)
  </read_first>
  <action>
    Tambah PERSIS 1 baris di AKHIR file `Views/_ViewImports.cshtml` (setelah baris 5 `@addTagHelper ...`):
    ```razor
    @inject HcPortal.Services.IOrgLabelService OrgLabels
    ```
    Pakai fully-qualified namespace `HcPortal.Services.IOrgLabelService` (BUKAN `IOrgLabelService` saja — `_ViewImports` belum punya `@using HcPortal.Services`). Persis pola analog `_Layout.cshtml:8` (`@inject HcPortal.Data.ApplicationDbContext _maintenanceDb`).
    JANGAN ubah 5 baris existing (@using / @addTagHelper). JANGAN tambah `@using HcPortal.Services` (minimal diff per spec §5 L490). JANGAN tambah @inject per-view di file lain (D-01 anti-pattern — global cukup, inherit hierarkis ke semua subfolder + partial).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/_ViewImports.cshtml` contains `@inject HcPortal.Services.IOrgLabelService OrgLabels`
    - `Views/_ViewImports.cshtml` STILL contains `@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers` (baris existing untouched)
    - `Views/_ViewImports.cshtml` STILL contains `@using HcPortal.Models` (baris existing untouched)
    - `Views/_ViewImports.cshtml` does NOT contain `@using HcPortal.Services` (fully-qualified, minimal diff)
    - File berisi TEPAT 6 baris (5 existing + 1 @inject)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Build hijau (membuktikan @inject resolve via DI). `OrgLabels` available di semua view + partial di bawah Views/ tanpa @inject ulang.</done>
</task>

<task type="auto">
  <name>Task 2: Tulis audit deliverable SC1 + ORG-INTEG-02 controller verdict (343-AUDIT.md)</name>
  <files>.planning/phases/343-integrasi-app-wide/343-AUDIT.md</files>
  <read_first>
    - 343-RESEARCH.md §"SC1 DELIVERABLE — Signal-vs-Noise Occurrence Audit" (L101-262) — sumber otoritatif tabel REPLACE/SKIP/AMBIGUOUS + controller audit
    - 343-RESEARCH.md §"Folder Structure Finding" (L108-116) — empty-folder finding (Worker/CoachMapping/Renewal/DocumentAdmin Views = 0 file)
    - 343-CONTEXT.md D-02/D-03/D-04 (filosofi ganti/skip, scope realistis, occurrence ambigu)
    - v21.0-ROADMAP.md Phase 343 Success Criteria 1-5 (L80-85)
  </read_first>
  <action>
    Buat `.planning/phases/343-integrasi-app-wide/343-AUDIT.md` (Bahasa Indonesia) sebagai pemenuhan SC1 (audit grep per file + keputusan eksplisit per occurrence) + ORG-INTEG-02. Struktur:

    1. **Header**: judul, tanggal, tujuan (SC1 audit deliverable + ORG-INTEG-02 pemenuhan). Catat: ini deliverable dokumentasi, bukan kode.

    2. **§1 Folder-Structure Finding (validasi D-03)**: tabel 4 folder KOSONG (Views/Worker, Views/CoachMapping, Views/Renewal, Views/DocumentAdmin = 0 file) + di mana form-nya sebenarnya (Views/Admin/*). Kesimpulan: 4 dari 7 "area" spec tidak punya Views folder sendiri → audit-only (form tercakup di Views/Admin/). Lift dari RESEARCH.md L108-116.

    3. **§2 Tabel REPLACE (display tier-label → swap)**: salin tabel `file:line | snippet | tier | verdict=REPLACE | reason` dari RESEARCH.md L118-209 (~95 occurrence di ~25 file). Tandai kolom "Diterapkan di Plan" = Plan 02 (CMP/CDP), Plan 03 (ProtonData + Admin worker-domain), atau Plan 04 (Admin assessment/upload + Account) supaya traceable.

    4. **§3 Tabel AMBIGUOUS (keputusan eksplisit + alasan)**: salin dari RESEARCH.md L213-226 + PATTERNS.md L364-372. Catat keputusan FINAL planner:
       - CpdpFiles.cshtml:64,87 + KkjMatrix.cshtml:64,87 (`Tambah/Hapus Bagian` button) → **REPLACE button visible text, SKIP JS func/toast** (diterapkan Plan 04).
       - EditAssessment.cshtml:606 + CreateAssessment.cshtml:272 (`Lainnya (Tanpa Bagian)`) → **REPLACE** (default konsistensi, diterapkan Plan 04).
       - Index.cshtml:45/61, ManageOrgLevelLabels.cshtml:17, ManageOrganization.cshtml:115/159 (subtitle combined / Phase 342 page) → **SKIP** (circular/out-of-scope).

    5. **§4 Tabel SKIP (noise — JANGAN ganti)**: salin kategori dari RESEARCH.md L228-242 (element id, JS var/func, ViewBag/property, unit NAME data, import-doc field-table, Razor comment, render-nilai `_PSign.cshtml:42`). Ini = whitelist grep-residual reviewer. Tambah temuan baru ini juga: CoachCoacheeMapping.cshtml:723/724/805/806 `alert('Pilih Bagian Penugasan.')` = JS literal → SKIP.

    6. **§5 Controller Audit (ORG-INTEG-02)**: salin tabel controller dari RESEARCH.md L244-262 + PATTERNS.md L393-408. Verdict FINAL: **ORG-INTEG-02 dipenuhi via "audited, near-zero actionable"** — SEMUA controller display-bearing string = Excel/CSV export header / audit log body / ModelState validation msg / property interpolation = legit SKIP per spec §4.8. Catat satu-satunya kandidat stretch (DocumentAdminController L106/287/299/315/380 TempData/Json "Bagian tidak ditemukan") + keputusan: **DEFAULT SKIP** (D-02 minimize over-replace; audit IS the coverage). NB: jika user/checker minta bukti konkret → opsi inject _orgLabels ke DocumentAdminController dicatat sebagai stretch (BUKAN dikerjakan default).

    7. **§6 SC Mapping**: tabel 5 Success Criteria Phase 343 → di mana dipenuhi:
       - SC1 (audit per file + keputusan eksplisit) → dokumen ini.
       - SC2 (rename demo ≥3 page) → Plan 02/03/04 swaps + manual spot-render (VALIDATION.md Manual-Only; formal = Phase 344).
       - SC3 (Razor @inject consistent, no hardcode di area target) → Plan 01 @inject + Plan 02/03/04 swaps + grep residual.
       - SC4 (audit log body tetap literal) → SKIP guards di §4 + semua swap task Plan 02/03/04.
       - SC5 (test strings pakai label final) → N/A Phase 343 (tests = Phase 344 TEST-01..06); dicatat sebagai deferred-noted.

    JANGAN sertakan kode swap aktual di dokumen ini (itu Plan 02/03/04). Dokumen ini = audit/dokumentasi murni. Boleh referensi `343-RESEARCH.md L###` ketimbang salin verbatim tabel panjang bila ringkas lebih jelas — tapi minimal §1-§6 lengkap + verdict eksplisit per ambiguous/controller.
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path '.planning/phases/343-integrasi-app-wide/343-AUDIT.md') { (Select-String -Path '.planning/phases/343-integrasi-app-wide/343-AUDIT.md' -Pattern 'ORG-INTEG-02','REPLACE','SKIP','AMBIGUOUS' | Measure-Object).Count } else { 'MISSING' }"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/343-integrasi-app-wide/343-AUDIT.md` EXISTS, ≥60 baris
    - Contains heading/section untuk: Folder-Structure Finding, REPLACE, AMBIGUOUS, SKIP, Controller Audit (ORG-INTEG-02), SC Mapping
    - Contains string `ORG-INTEG-02` dengan verdict "audited" / "near-zero actionable" / "SKIP" (dokumentasi pemenuhan REQ)
    - Contains keputusan eksplisit untuk CpdpFiles/KkjMatrix button (REPLACE text, SKIP JS/toast) + `Lainnya (Tanpa Bagian)` (REPLACE)
    - Contains SKIP entry untuk `alert('Pilih Bagian Penugasan.')` (JS literal CoachCoacheeMapping)
    - Contains SC Mapping 5 baris (SC1-SC5) → lokasi pemenuhan
    - Does NOT contain `@OrgLabels.GetLabel` swap aktual sebagai instruksi kode (dokumen audit, bukan plan kode)
  </acceptance_criteria>
  <done>343-AUDIT.md ada dengan §1-§6 lengkap; SC1 terpenuhi (audit per file + keputusan eksplisit); ORG-INTEG-02 terdokumentasi sebagai legit SKIP (audit = coverage).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase ini = display-string refactor + dokumentasi. Tidak ada input user baru, endpoint baru, auth path baru, atau data path baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-01 | Information Disclosure (XSS) | `@OrgLabels.GetLabel(N)` rendered di view | mitigate | Output di-render via plain `@` Razor expression yang HTML-encoded otomatis. JANGAN gunakan `@Html.Raw(OrgLabels.GetLabel(...))`. Label value bersumber dari admin-only CRUD (Phase 341, sudah access-controlled) + seed server-side. Plan 01 hanya menambah @inject + dokumentasi — tidak ada render label di file ini. |
| T-343-02 | Tampering | Label data source `OrganizationLevelLabels` | accept | Mutasi label hanya via `/Admin/ManageOrgLevelLabels` (Phase 341, role Admin/HC). Phase 343 read-only consume; tidak menambah jalur mutasi. Low risk, server-controlled. |
</threat_model>

<verification>
- `dotnet build` exits 0 (membuktikan @inject resolve di seluruh view/partial — A1 partial inheritance terverifikasi compile-time).
- `grep "@inject HcPortal.Services.IOrgLabelService" Views/_ViewImports.cshtml` → 1 match.
- `343-AUDIT.md` ada dengan §1-§6 + verdict eksplisit + SC mapping.
- Per VALIDATION.md Sampling Rate: build per task commit (latency <60s).
</verification>

<success_criteria>
- @inject global terpasang (D-01) — fondasi untuk Plan 02/03/04 swap.
- SC1 terpenuhi: audit deliverable per file + keputusan eksplisit ganti/skip per occurrence (343-AUDIT.md).
- ORG-INTEG-02 terdokumentasi: controller display-bearing string = legit SKIP (audit = coverage), opsi DocumentAdmin inject dicatat sebagai stretch (default SKIP per D-02).
- Build hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/343-integrasi-app-wide/343-01-SUMMARY.md`
</output>
