---
phase: 343-integrasi-app-wide
plan: 02
type: execute
wave: 2
depends_on: ["343-01"]
files_modified:
  - Views/CMP/AnalyticsDashboard.cshtml
  - Views/CMP/RecordsTeam.cshtml
  - Views/CDP/CertificationManagement.cshtml
  - Views/CDP/CoachingProton.cshtml
  - Views/CDP/HistoriProton.cshtml
  - Views/CDP/HistoriProtonDetail.cshtml
  - Views/CDP/PlanIdp.cshtml
  - Views/CDP/Shared/_CoachingProtonPartial.cshtml
  - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml
autonomous: true
requirements: [ORG-INTEG-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Semua filter label + dropdown + table header + form placeholder + definition-list label tier di CMP/* dan CDP/* menampilkan @OrgLabels.GetLabel(N), bukan hardcoded Bagian/Unit"
    - "Element id, JS var, property access, nama data di file CMP/CDP TIDAK berubah (visible-text-only swap)"
    - "dotnet build hijau (semua @OrgLabels resolve, termasuk di 2 partial CDP/Shared yang inherit @inject)"
  artifacts:
    - path: "Views/CMP/AnalyticsDashboard.cshtml"
      provides: "Filter label + dropdown + heading + th tier dinamis"
      contains: "@OrgLabels.GetLabel(0)"
    - path: "Views/CDP/PlanIdp.cshtml"
      provides: "Form field label + placeholder tier dinamis"
      contains: "@OrgLabels.GetLabel(0)"
    - path: "Views/CDP/Shared/_CertificationManagementTablePartial.cshtml"
      provides: "Partial table header tier dinamis (inherit @inject)"
      contains: "@OrgLabels.GetLabel(0)"
  key_links:
    - from: "Views/CMP/AnalyticsDashboard.cshtml"
      to: "OrgLabels.GetLabel"
      via: "@inject inherited dari Views/_ViewImports.cshtml (Plan 01)"
      pattern: "@OrgLabels\\.GetLabel\\([012]\\)"
---

<objective>
Terapkan REPLACE swap (hardcoded "Bagian"/"Unit" → `@OrgLabels.GetLabel(N)`) untuk area **CMP** + **CDP** mengikuti tabel REPLACE otoritatif di `343-RESEARCH.md` L118-209. Murni visible-text swap (`>...<` di antara tag); id/JS var/property access/nama data tetap literal.

Purpose: Pemenuhan ORG-INTEG-01 untuk dua area integrasi utama (CMP filter/analytics + CDP coaching/sertifikasi). Bersama Plan 03 (ProtonData/Admin/Account) memenuhi SC2 (rename muncul ≥3 page) + SC3 (no hardcode tersisa di area target).

Output: 9 file view CMP/CDP (7 view + 2 partial CDP/Shared) di-swap; build hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/343-integrasi-app-wide/343-RESEARCH.md
@.planning/phases/343-integrasi-app-wide/343-PATTERNS.md
@.planning/phases/343-integrasi-app-wide/343-VALIDATION.md

<interfaces>
<!-- Kontrak swap — sudah disediakan, tidak perlu eksplorasi codebase. -->

@inject SUDAH terpasang global oleh Plan 01 (Views/_ViewImports.cshtml). `@OrgLabels.GetLabel(N)` available di SEMUA view + partial (termasuk Views/CDP/Shared/_*.cshtml — inherit hierarkis, JANGAN @inject ulang di partial).

Tier mapping (Data/SeedData.cs:121-123):
- GetLabel(0) = "Bagian"  (tier 0)
- GetLabel(1) = "Unit"    (tier 1)
- GetLabel(2) = "Sub-unit"(tier 2) — tidak ada REPLACE row tier 2 di area ini

3 pola swap kanonik (343-PATTERNS.md §Shared Patterns):
- Pattern A (visible-text-only): `>Bagian<` → `>@OrgLabels.GetLabel(0)<`. JANGAN sentuh `id=`/`for=`/`name=`/JS var.
- Pattern B (combined-text): `Semua Bagian` → `Semua @OrgLabels.GetLabel(0)`; `-- Pilih Unit --` → `-- Pilih @OrgLabels.GetLabel(1) --`.
- Pattern C (tier mapping): kolom tier di tabel REPLACE sudah pre-isi.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap CMP area (AnalyticsDashboard + RecordsTeam)</name>
  <files>Views/CMP/AnalyticsDashboard.cshtml, Views/CMP/RecordsTeam.cshtml</files>
  <read_first>
    - Views/CMP/AnalyticsDashboard.cshtml + Views/CMP/RecordsTeam.cshtml (file yang dimodifikasi)
    - 343-RESEARCH.md REPLACE table L122-134 (rows CMP) + §Pattern 1 (L297-309) + Pitfall 1 (L352-356)
    - 343-PATTERNS.md §AnalyticsDashboard CANONICAL FILTER PATTERN (L100-146) — BEFORE/AFTER konkret
    - 343-AUDIT.md (Plan 01) §4 SKIP whitelist
  </read_first>
  <action>
    Swap visible-text-only per tabel REPLACE RESEARCH.md L122-134:

    **AnalyticsDashboard.cshtml** (verified line numbers):
    - L84 `<label for="filterBagian" ...>Bagian</label>` → teks `>Bagian<` jadi `>@OrgLabels.GetLabel(0)<` (id `filterBagian` & `for` TETAP literal)
    - L86 `<option value="">Semua Bagian</option>` → `Semua @OrgLabels.GetLabel(0)`
    - L94 `<label for="filterUnit" ...>Unit</label>` → `>@OrgLabels.GetLabel(1)<`
    - L96 `<option value="">Semua Unit</option>` → `Semua @OrgLabels.GetLabel(1)`
    - L543 `<h6>Perbandingan Antar Kelompok (Bagian)</h6>` → `...(@OrgLabels.GetLabel(0))</h6>` (hanya kata "Bagian" dalam kurung jadi dinamis; "Perbandingan Antar Kelompok" tetap)
    - L548 `<th>Bagian</th>` → `<th>@OrgLabels.GetLabel(0)</th>`

    **RecordsTeam.cshtml** (RESEARCH L128-134):
    - L20 `<label>Bagian</label>` → tier 0
    - L24 `<option>Semua Bagian</option>` → `Semua @OrgLabels.GetLabel(0)`
    - L41 `<option>Semua Bagian</option>` → `Semua @OrgLabels.GetLabel(0)` (selector ke-2)
    - L50 `<label>Unit</label>` → tier 1
    - L52 `<option>Semua Unit</option>` → `Semua @OrgLabels.GetLabel(1)`
    - L135 `<th class="p-3">Bagian</th>` → `<th class="p-3">@OrgLabels.GetLabel(0)</th>` (class TETAP)
    - L136 `<th class="p-3">Unit</th>` → `<th class="p-3">@OrgLabels.GetLabel(1)</th>`

    JANGAN sentuh: `id="filterBagian"`/`for="filterBagian"`/`id="filterUnit"`, `@foreach`/`@s` loop var, `Model.Sections`, `@row.Bagian`/property access, class attribute. Line numbers panduan — VERIFIKASI konteks aktual (`<label>`/`<th>`/`<option>` body) sebelum edit; jika geser, cari snippet yang cocok.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/CMP/AnalyticsDashboard.cshtml` contains `@OrgLabels.GetLabel(0)` AND `@OrgLabels.GetLabel(1)`
    - `Views/CMP/AnalyticsDashboard.cshtml` does NOT contain `<th>Bagian</th>` AND does NOT contain `>Bagian</label>` AND does NOT contain `Semua Bagian` AND does NOT contain `Semua Unit`
    - `Views/CMP/AnalyticsDashboard.cshtml` STILL contains `id="filterBagian"` AND `id="filterUnit"` (SKIP guard — id untouched)
    - `Views/CMP/RecordsTeam.cshtml` contains `@OrgLabels.GetLabel(0)` AND `@OrgLabels.GetLabel(1)`
    - `Views/CMP/RecordsTeam.cshtml` does NOT contain `<th class="p-3">Bagian</th>` AND does NOT contain `Semua Bagian` AND does NOT contain `Semua Unit`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>CMP filter/heading/th label dinamis; id/JS/property untouched; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Swap CDP views (CertificationManagement + CoachingProton + HistoriProton + HistoriProtonDetail + PlanIdp)</name>
  <files>Views/CDP/CertificationManagement.cshtml, Views/CDP/CoachingProton.cshtml, Views/CDP/HistoriProton.cshtml, Views/CDP/HistoriProtonDetail.cshtml, Views/CDP/PlanIdp.cshtml</files>
  <read_first>
    - 5 file CDP yang dimodifikasi
    - 343-RESEARCH.md REPLACE table L135-153 (rows CDP) + §Pattern 1/2 (L297-318)
    - 343-PATTERNS.md §PlanIdp CANONICAL PLACEHOLDER PATTERN (L150-206) + §detail/definition-list (L240-262) + sibling table L273-275
    - 343-AUDIT.md (Plan 01) §4 SKIP whitelist
  </read_first>
  <action>
    Swap visible-text-only per tabel REPLACE RESEARCH.md L135-153:

    **CertificationManagement.cshtml** (L135-139): L89 `<label>Bagian</label>` (0); L97 `<option>Semua Bagian</option>` → `Semua @OrgLabels.GetLabel(0)`; L105 `<option>Semua Bagian</option>` (0, branch lain); L110 `<label>Unit</label>` (1); L112 `<option>Semua Unit</option>` → `Semua @OrgLabels.GetLabel(1)`.

    **CoachingProton.cshtml** (L140-141): L100 `<option>Semua Bagian</option>` (0); L120 `<option>Semua Unit</option>` (1). **L95 Razor comment `@* Bagian dropdown ... *@` → SKIP** (tidak dirender).

    **HistoriProton.cshtml** (L142-147): L30 `<label>Bagian</label>` (0); L34 `<option>Semua Bagian</option>` (0, disabled branch); L51 `<option>Semua Bagian</option>` (0, enabled branch); L60 `<label>Unit</label>` (1); L62 `<option>Semua Unit</option>` (1); L109 `<th>Unit</th>` (1).

    **HistoriProtonDetail.cshtml** (L148-149): L36 `<dt>Unit</dt>` → `<dt>@OrgLabels.GetLabel(1)</dt>` (1); L84 `<small>Unit</small>` → `<small>@OrgLabels.GetLabel(1)</small>` (1). KRITIS: ini LABEL field, BUKAN nilai data — JANGAN sentuh `<dd>`/sel nilai di sebelahnya (mis. `@Model.Unit`).

    **PlanIdp.cshtml** (L150-153): L72 `<label>Bagian</label>` (0); L86 `<option>-- Pilih Bagian --</option>` → `-- Pilih @OrgLabels.GetLabel(0) --`; L91 `<label>Unit</label>` (1); L94 `<option>-- Pilih Unit --</option>` → `-- Pilih @OrgLabels.GetLabel(1) --`.

    JANGAN sentuh: `id="silabusBagian"`/`id="silabusUnit"`, `name="bagian"`/`name="unit"`, `@coacheeBagian`/`selectedBagian` (C# var di expression `disabled=`), `value="@coacheeBagian"`, `@Model.Unit`/property render, Razor comment. Line numbers panduan — verifikasi konteks aktual; combined "Semua X"/"-- Pilih X --" pakai Pattern B (label dinamis + literal prefix/suffix).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/CDP/PlanIdp.cshtml` contains `-- Pilih @OrgLabels.GetLabel(0) --` AND `-- Pilih @OrgLabels.GetLabel(1) --`
    - `Views/CDP/PlanIdp.cshtml` does NOT contain `-- Pilih Bagian --` AND does NOT contain `-- Pilih Unit --`
    - `Views/CDP/PlanIdp.cshtml` STILL contains `id="silabusBagian"` (SKIP guard)
    - `Views/CDP/CertificationManagement.cshtml` contains `Semua @OrgLabels.GetLabel(0)` AND does NOT contain `Semua Bagian`
    - `Views/CDP/HistoriProton.cshtml` does NOT contain `<th>Unit</th>` (swapped) AND contains `@OrgLabels.GetLabel(1)`
    - `Views/CDP/HistoriProtonDetail.cshtml` contains `<dt>@OrgLabels.GetLabel(1)</dt>` AND does NOT contain `<dt>Unit</dt>`
    - `Views/CDP/CoachingProton.cshtml` does NOT contain `Semua Bagian` AND does NOT contain `Semua Unit`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>5 view CDP filter/placeholder/th/dt label dinamis; id/name/property/comment untouched; build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Swap CDP partials (_CoachingProtonPartial + _CertificationManagementTablePartial)</name>
  <files>Views/CDP/Shared/_CoachingProtonPartial.cshtml, Views/CDP/Shared/_CertificationManagementTablePartial.cshtml</files>
  <read_first>
    - 2 file partial yang dimodifikasi
    - 343-RESEARCH.md REPLACE table L154-157 + §Catatan partial views (L211) + Pitfall 5 (L370-373)
    - 343-PATTERNS.md §Pattern D Partial @inject inheritance (L327-330)
  </read_first>
  <action>
    Partial mewarisi `@OrgLabels` dari `Views/_ViewImports.cshtml` (Plan 01) — JANGAN tambah @inject ulang di partial (Pitfall 5; verify via build). Swap per RESEARCH.md L154-157:

    **_CoachingProtonPartial.cshtml**: L29 `<label>Unit</label>` → `<label>@OrgLabels.GetLabel(1)</label>` (1); L35 `<option>Semua Unit</option>` → `Semua @OrgLabels.GetLabel(1)` (1).

    **_CertificationManagementTablePartial.cshtml**: L19 `<th>Bagian</th>` → `<th>@OrgLabels.GetLabel(0)</th>` (0); L20 `<th>Unit</th>` → `<th>@OrgLabels.GetLabel(1)</th>` (1).

    JANGAN sentuh: id/name/property access/data render. Line numbers panduan — verifikasi konteks. Build hijau = bukti @inject inheritance ke partial bekerja (A1 verified).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/CDP/Shared/_CertificationManagementTablePartial.cshtml` contains `<th>@OrgLabels.GetLabel(0)</th>` AND `<th>@OrgLabels.GetLabel(1)</th>`
    - `Views/CDP/Shared/_CertificationManagementTablePartial.cshtml` does NOT contain `<th>Bagian</th>` AND does NOT contain `<th>Unit</th>`
    - `Views/CDP/Shared/_CoachingProtonPartial.cshtml` contains `@OrgLabels.GetLabel(1)` AND does NOT contain `Semua Unit`
    - Neither partial contains a duplicate `@inject ... IOrgLabelService` line (inherit dari _ViewImports, no re-inject)
    - `dotnet build` exits 0 (membuktikan @inject inheritance ke CDP/Shared/* partial)
  </acceptance_criteria>
  <done>2 partial CDP/Shared label dinamis tanpa @inject ulang; build hijau membuktikan inheritance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Display-string swap di view CMP/CDP. Tidak ada input user baru, endpoint, auth, atau data path baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-03 | Information Disclosure (XSS) | `@OrgLabels.GetLabel(N)` di view CMP/CDP | mitigate | Semua swap pakai plain `@OrgLabels.GetLabel(N)` (Razor auto HTML-encode). ZERO `@Html.Raw` dengan output label. Acceptance criteria memverifikasi pola `@OrgLabels.GetLabel(N)` polos. Label value = admin-only CRUD (Phase 341) + seed server-side. |
| T-343-04 | Tampering | Element id / JS hook (filterBagian, silabusBagian) | mitigate | SKIP guard di setiap task: acceptance criteria asserts `id="..."` STILL present (over-replace = #1 risk per Pitfall 1; merusak id = JS cascade dropdown putus). |
</threat_model>

<verification>
- `dotnet build` exits 0 setiap task (membuktikan @OrgLabels resolve, termasuk 2 partial CDP/Shared inherit @inject).
- Grep residual (per VALIDATION.md Sampling): di 9 file REPLACE, pola `>Bagian<`/`>Unit<`/`Semua Bagian`/`Semua Unit`/`-- Pilih Bagian --`/`<th>Bagian</th>` = 0 (whitelist: id `filterBagian`/`silabusBagian`, JS var, `.Bagian` property, Razor comment CoachingProton:95 — cocokkan ke 343-AUDIT.md §4 SKIP).
- Spot-render manual (Phase 344 formal): 1 page CMP (AnalyticsDashboard filter) + 1 page CDP (CertificationManagement filter) tampil label benar; fallback "Level N" TIDAK muncul (seed ada).
</verification>

<success_criteria>
- 9 file CMP/CDP: filter label + dropdown + th + form placeholder + dt/small field label tier dinamis via `@OrgLabels.GetLabel(N)`.
- SKIP guards utuh: id/`for`/JS var/property/data/Razor comment literal.
- Build hijau (partial inheritance terbukti).
- ORG-INTEG-01 (CMP/CDP portion) terpenuhi.
</success_criteria>

<output>
After completion, create `.planning/phases/343-integrasi-app-wide/343-02-SUMMARY.md`
</output>
