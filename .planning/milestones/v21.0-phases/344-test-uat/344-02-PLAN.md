---
phase: 344-test-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/HcPortal.Tests.csproj
  - HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "The real EF migration 20260603012335_AddOrganizationLevelLabel applies cleanly against a real SQL Server instance (validated via MigrateAsync, NOT EnsureCreated)"
    - "After migration + production seed, OrgLabelService.GetLabel(0/1/2) returns configured labels (Bagian/Unit/Sub-unit), and GetLabel(99) returns fallback 'Level 99'"
    - "The integration test uses a disposable HcPortalDB_Test_<guid> that is dropped per run (even on migration failure); HcPortalDB_Dev is never touched"
    - "The integration test is filterable via [Trait(Category, Integration)] so a SQL-less environment can skip it"
  artifacts:
    - path: "HcPortal.Tests/HcPortal.Tests.csproj"
      provides: "Microsoft.EntityFrameworkCore.SqlServer 8.0.0 provider for the integration test"
      contains: "Microsoft.EntityFrameworkCore.SqlServer"
    - path: "HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs"
      provides: "TEST-05 disposable real-SQL-Server migration + seed + first-read integration test"
      contains: "IAsyncLifetime"
  key_links:
    - from: "HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs"
      to: "Data/SeedData.cs SeedOrganizationLevelLabelsAsync"
      via: "production seed call after MigrateAsync"
      pattern: "SeedData\\.SeedOrganizationLevelLabelsAsync"
    - from: "HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs"
      to: "real SQL Server localhost\\SQLEXPRESS"
      via: "UseSqlServer connection string + MigrateAsync"
      pattern: "MigrateAsync"
---

<objective>
Add the one real-database integration test (TEST-05) that the existing EF InMemory suite cannot provide, and (as Wave 1, Task 1) add the SqlServer provider package that all downstream `dotnet test` runs depend on.

Purpose: EF InMemory never executes migration DDL — it builds schema from the model and bypasses the migrations pipeline. TEST-05 must prove the actual migration `20260603012335_AddOrganizationLevelLabel` applies against real SQL Server, the production seed populates the 3 default rows, and `OrgLabelService` reads configured labels (not fallback). Per D-02 this runs on a disposable `HcPortalDB_Test_<guid>` on `localhost\SQLEXPRESS`, dropped per run, never touching `HcPortalDB_Dev` (so no SEED_WORKFLOW snapshot/restore is needed).

**Wave ordering (H1):** Task 1 (csproj package add + restore) is the ONLY Wave-1 work and MUST complete before any `dotnet test` runs — it mutates `HcPortal.Tests.csproj` and triggers a restore that races with concurrent `dotnet test` on the same project. Task 2 (the integration test) is Wave-2 work (it runs `dotnet test`), sequenced alongside Plan 01's unit tests AFTER the package is restored.

Output: csproj package add (Wave 1) + new fixture-backed integration test marked `[Trait("Category","Integration")]` (Wave 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/344-test-uat/344-CONTEXT.md
@.planning/phases/344-test-uat/344-RESEARCH.md
@.planning/phases/344-test-uat/344-PATTERNS.md

<interfaces>
<!-- Seed source-of-truth (TEST-05). Verified Data/SeedData.cs:114-127 2026-06-04. -->
public static async Task SeedOrganizationLevelLabelsAsync(ApplicationDbContext context)
  // idempotent: AnyAsync() guard; inserts Level 0=Bagian, 1=Unit, 2=Sub-unit
  // NOTE: seed is NOT in the migration (no HasData/InsertData) — migration only CreateTable + CreateIndex.

<!-- Service ctor (same triple-arg across InMemory + SqlServer). OrgLabelServiceTests.cs:42-44 -->
new OrgLabelService(ctx, new MemoryCache(new MemoryCacheOptions()), new AuditLogService(ctx))
  // GetLabel(0)=="Bagian", GetLabel(99)=="Level 99" (fallback)

<!-- Migration under test: Migrations/20260603012335_AddOrganizationLevelLabel.cs (CreateTable + unique index IX_OrganizationLevelLabels_Label only) -->
<!-- Analog package ref to add (match version 8.0.0 from HcPortal.csproj:18): -->
<PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="8.0.0" />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 1): Add Microsoft.EntityFrameworkCore.SqlServer 8.0.0 to the Tests project</name>
  <read_first>
    - HcPortal.Tests/HcPortal.Tests.csproj (target file — existing PackageReference ItemGroup lines 11-23)
    - HcPortal.csproj line 18 (analog: SqlServer 8.0.0 already referenced in main project — match this version)
  </read_first>
  <action>
    **This is Wave-1, single-threaded work (H1).** It MUST complete and the restore MUST settle before ANY `dotnet test HcPortal.Tests` runs (Plan 01 unit tests or Task 2 below). A package add + restore mutates `obj/`/`bin/` and `HcPortal.Tests.csproj`; running it concurrently with `dotnet test` on the same project causes a restore+test race. Do NOT start Wave 2 (`dotnet test`) until this task's restore exits 0.

    Run `dotnet add HcPortal.Tests/HcPortal.Tests.csproj package Microsoft.EntityFrameworkCore.SqlServer --version 8.0.0` then `dotnet restore HcPortal.Tests/HcPortal.Tests.csproj`. This adds one PackageReference line alongside the existing `Microsoft.EntityFrameworkCore.InMemory` 8.0.0 in the PackageReference ItemGroup. Match version 8.0.0 exactly to stay consistent with the app (HcPortal.csproj:18) and avoid an EF Core version mismatch. The InMemory provider stays (unit tests still use it).
  </action>
  <verify>
    <automated>dotnet build HcPortal.Tests exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Microsoft.EntityFrameworkCore.SqlServer" HcPortal.Tests/HcPortal.Tests.csproj` returns 1.
    - `grep -c "Version=\"8.0.0\"" HcPortal.Tests/HcPortal.Tests.csproj` shows the SqlServer line at 8.0.0 (consistent with InMemory line).
    - `dotnet restore HcPortal.Tests/HcPortal.Tests.csproj` exits 0.
    - `dotnet build HcPortal.Tests` exits 0 (provider resolves).
  </acceptance_criteria>
  <done>HcPortal.Tests.csproj references Microsoft.EntityFrameworkCore.SqlServer 8.0.0; restore + build succeed. This gates all subsequent `dotnet test` runs (Wave 2).</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 2): Disposable real-SQL-Server migration + seed + first-read integration test (TEST-05)</name>
  <read_first>
    - HcPortal.Tests/OrgLabelServiceTests.cs lines 25-45 (analog: DbContext + cache + AuditLogService + OrgLabelService construction shape)
    - Data/SeedData.cs lines 114-127 (source-of-truth: SeedOrganizationLevelLabelsAsync — seed is NOT in the migration)
    - Migrations/20260603012335_AddOrganizationLevelLabel.cs (the migration under test — CreateTable + CreateIndex, no InsertData)
    - 344-RESEARCH.md Pattern 3 (authoritative fixture template — IClassFixture + IAsyncLifetime) + Pitfalls 2,3,6
  </read_first>
  <action>
    **Wave-2 task — must run AFTER Plan 02 Task 1 restore is complete (H1). To avoid two concurrent `dotnet test` processes racing on the same project's obj/bin, this integration test and Plan 01's unit tests should NOT run as two parallel `dotnet test` invocations. Prefer a single `dotnet test HcPortal.Tests` pass that includes both, or sequence them (one after the other). Also: the MigrateAsync here hits the same `localhost\SQLEXPRESS` instance Plan 03's BACKUP/RESTORE uses — Plan 03 (Wave 3) must NOT overlap with this task.**

    Create NEW file `HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` with a fixture + one [Fact], copied from 344-RESEARCH.md Pattern 3 / 344-PATTERNS.md (verbatim, adjust naming). Imports: `Microsoft.EntityFrameworkCore`, `Microsoft.Extensions.Caching.Memory`, `HcPortal.Data`, `HcPortal.Services`, `Xunit`.

    Fixture (`IAsyncLifetime`):
    - DbName = `$"HcPortalDB_Test_{Guid.NewGuid():N}"` (unique per run — Pitfall 3 collision/residue guard).
    - Connection string: `$"Server=localhost\\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30"` (localhost-only, Integrated Security — mirrors dbSnapshot.ts guard; no appsettings change).
    - `InitializeAsync` (M1 — guard against DB leak on a mid-migration throw): build `DbContextOptionsBuilder<ApplicationDbContext>().UseSqlServer(cs).Options`; wrap the migrate+seed in try/catch so a partial failure does NOT leak `HcPortalDB_Test_<guid>`:
      ```csharp
      public async Task InitializeAsync()
      {
          _options = new DbContextOptionsBuilder<ApplicationDbContext>().UseSqlServer(_cs).Options;
          try
          {
              await using var ctx = new ApplicationDbContext(_options);
              await ctx.Database.MigrateAsync();   // MUST be MigrateAsync, NOT EnsureCreated —
                                                   // validates the real migration DDL. The full
                                                   // ~91-migration chain runs here (acceptable).
              await SeedData.SeedOrganizationLevelLabelsAsync(ctx); // production seed; migration does NOT seed (Pitfall 2)
          }
          catch (Exception ex)
          {
              // M1: a mid-migration throw must NOT leave HcPortalDB_Test_<guid> behind.
              try { await using var cleanup = new ApplicationDbContext(_options); await cleanup.Database.EnsureDeletedAsync(); } catch { /* best-effort */ }
              throw new Xunit.Sdk.XunitException(
                  $"TEST-05 setup failed during MigrateAsync/seed of disposable DB {DbName}. " +
                  $"This indicates a MIGRATION-CHAIN break (full ~91-migration chain runs), NOT necessarily an OrgLabel bug. Inner: {ex}");
          }
      }
      ```
    - `DisposeAsync`: `await using var ctx = new ApplicationDbContext(_options); await ctx.Database.EnsureDeletedAsync();` (drop the disposable DB per run on the success path — D-02).

    Test class `[Trait("Category", "Integration")]` (Pitfall 6 — lets `dotnet test --filter "Category!=Integration"` skip on SQL-less CI) implementing `IClassFixture<OrgLabelMigrationFixture>`. One [Fact] `Migrate_Seed_FirstRead_ReturnsConfiguredLabel_NotFallback`:
    - construct `OrgLabelService(ctx, new MemoryCache(new MemoryCacheOptions()), new AuditLogService(ctx))` (same triple-arg ctor as the InMemory analog).
    - `Assert.Equal("Bagian", svc.GetLabel(0))`, `Assert.Equal("Unit", svc.GetLabel(1))`, `Assert.Equal("Sub-unit", svc.GetLabel(2))` (configured, NOT "Level N").
    - `Assert.Equal("Level 99", svc.GetLabel(99))` (fallback still works on real DB).
    - `Assert.Equal(3, ctx.OrganizationLevelLabels.Count())` (exactly 3 seeded rows).

    Critical guards: assert rows AFTER seed (never expect Migrate alone to populate — Pitfall 2); unique Guid DB + EnsureDeletedAsync on BOTH success (DisposeAsync) and failure (InitializeAsync catch — M1), never touch HcPortalDB_Dev (D-02, no SEED_WORKFLOW snapshot needed — Pitfall 3); do NOT use EnsureCreated.

    Run prerequisite: SQLEXPRESS must be running locally (guaranteed by DEV_WORKFLOW step 3). Verify with the full suite which includes the [Trait] test.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "Category=Integration" exits 0 (requires local SQLEXPRESS per DEV_WORKFLOW)</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` exists.
    - `grep -c "MigrateAsync" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns >= 1 AND `grep -c "EnsureCreated" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns 0 (Pitfall: must validate migration DDL, not bypass it).
    - `grep -c "SeedData.SeedOrganizationLevelLabelsAsync" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns 1 (seed via production path, not inline AddRange).
    - `grep -c "EnsureDeletedAsync" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns >= 2 (DisposeAsync success-path drop AND InitializeAsync failure-path cleanup — M1).
    - `grep -c "Guid.NewGuid" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns >= 1 (disposable per-run guid DB).
    - `grep -c "catch" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns >= 1 AND the catch path references the migration-chain failure message (M1 — a chain break is not misread as an OrgLabel bug).
    - `grep -c "HcPortalDB_Dev" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns 0 (dev DB never referenced — D-02).
    - `grep -c "Trait(\"Category\", \"Integration\")" HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs` returns 1.
    - `dotnet test HcPortal.Tests --filter "Category=Integration"` exits 0 with the single integration test passing (SQLEXPRESS present locally).
    - `dotnet test HcPortal.Tests --filter "Category!=Integration"` exits 0 AND does not run the integration test (skip path proven).
    - No leftover test DB: after the run (success OR mid-migration failure), no `HcPortalDB_Test_*` database remains (EnsureDeletedAsync dropped it).
  </acceptance_criteria>
  <done>The integration test migrates a fresh disposable real-SQL-Server DB, seeds via the production method, asserts configured labels + fallback + 3 rows, drops the DB on success AND on mid-migration failure (M1), and is excluded by the Category!=Integration filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → SQL Server instance | Connection string must target a local instance only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-02 | Tampering | integration-test connection string | mitigate | Connection string hardcoded to `localhost\SQLEXPRESS` with Integrated Security; unique disposable `HcPortalDB_Test_<guid>` dropped per run (success AND failure paths — M1); `HcPortalDB_Dev` never referenced (D-02). No production data at risk. |
| T-344-INFO | (test-only note) | new integration test | accept | Test-only phase, no new attack surface. Uses Integrated Security (no secrets/env vars). The test validates the real migration DDL; it adds no application endpoint or input path. No new threats. |
</threat_model>

<verification>
- `dotnet build HcPortal.Tests` exits 0 (SqlServer provider resolves).
- `dotnet test HcPortal.Tests` (full, includes integration) exits 0 on the local SQLEXPRESS box.
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` exits 0 and skips the integration test (SQL-less safety).
- No residual `HcPortalDB_Test_*` databases after a run (success or mid-migration failure — M1).
</verification>

<success_criteria>
- SqlServer provider package added in Wave 1 before any `dotnet test` runs (H1).
- Real migration `20260603012335_AddOrganizationLevelLabel` applies via MigrateAsync (DDL validated).
- Production seed populates exactly 3 default rows; service returns configured labels + fallback.
- Disposable guid DB dropped per run (success + failure — M1); HcPortalDB_Dev untouched.
- Integration test cleanly excludable via Category!=Integration filter.
</success_criteria>

<output>
After completion, create `.planning/phases/344-test-uat/344-02-SUMMARY.md`
</output>
</content>
</invoke>
