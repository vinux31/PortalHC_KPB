---
phase: 345-assessment-pending-grade-display-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/RecordsWorkerDetail.cshtml
  - Services/WorkerDataService.cs
  - Views/CMP/Records.cshtml
  - Controllers/CMPController.cs
autonomous: true
requirements: [CMP06R-01, CMP06R-04]
tags: [cmp, records, badge, display-correctness, status]

must_haves:
  truths:
    - "Sesi Completed+IsPassed-null di /CMP/RecordsWorkerDetail tampil badge amber 'Menunggu Penilaian' (bukan 'Failed' merah)"
    - "Sesi Completed+IsPassed-null di /CMP/Records tampil badge amber 'Menunggu Penilaian' (bukan 'Completed' bg-info)"
    - "Sesi graded (IsPassed true/false) di kedua halaman tetap tampil Passed hijau / Failed merah (no regression)"
    - "Excel ExportRecordsTeamAssessment sel status sesi pending = 'Menunggu Penilaian' (bukan kosong)"
  artifacts:
    - path: "Views/CMP/RecordsWorkerDetail.cshtml"
      provides: "3-way status badge (true/false/null) untuk RecordType Assessment Online"
      contains: "bg-warning text-dark"
    - path: "Services/WorkerDataService.cs"
      provides: "GetUnifiedRecords switch null => PendingGrading konstanta"
      contains: "AssessmentConstants.AssessmentStatus.PendingGrading"
    - path: "Views/CMP/Records.cshtml"
      provides: "switch sc tambah case PendingGrading => bg-warning text-dark"
      contains: "AssessmentConstants.AssessmentStatus.PendingGrading"
    - path: "Controllers/CMPController.cs"
      provides: "Excel cell null-branch konstanta"
      contains: "AssessmentConstants.AssessmentStatus.PendingGrading"
  key_links:
    - from: "Services/WorkerDataService.cs (GetUnifiedRecords:52-57)"
      to: "Views/CMP/Records.cshtml (switch sc:188)"
      via: "Status string 'Menunggu Penilaian' feed ke badge class switch"
      pattern: "AssessmentConstants\\.AssessmentStatus\\.PendingGrading"
---

<objective>
Tutup surface CMP06R-01 (Worker Detail) + CMP06R-04 (label-unify My Records) + minor Excel (D-09): sesi assessment Status="Completed" + IsPassed==null harus tampil badge amber "Menunggu Penilaian" alih-alih "Failed" merah (RecordsWorkerDetail) atau "Completed" abu (Records), dan sel Excel kosong jadi "Menunggu Penilaian".

Purpose: Phase 337 (CMP-06) hanya menyentuh GetUnifiedRecords + Records.cshtml. RecordsWorkerDetail.cshtml masih binary (else menelan null jadi "Failed" palsu). Sumber teks status di service masih null => "Completed". Phase ini meluruskan tampilan tanpa menambah kapabilitas atau migration.

Output: 4 file ter-edit (1 service + 2 view + 1 controller), dotnet build 0 error, badge amber konsisten di Worker Detail + My Records + Excel.

Honor Critical Corrections (RESEARCH):
- C-2: Records.cshtml:188 switch SUDAH 3-way - pending saat ini tampil "Completed" bg-info, BUKAN "Failed". Tugas di sini = label-unify (tambah case), JANGAN cari/perbaiki "Failed" di Records.cshtml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/345-assessment-pending-grade-display-fix/345-CONTEXT.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-RESEARCH.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-UI-SPEC.md

<interfaces>
Konstanta label (Models/AssessmentConstants.cs:18) - WAJIB pakai (D-02), JANGAN literal string:

namespace HcPortal.Models {
  public static class AssessmentConstants {
    public static class AssessmentStatus {
      public const string Completed = "Completed";
      public const string PendingGrading = "Menunggu Penilaian"; // ini
    }
  }
}

- Razor: @AssessmentConstants.AssessmentStatus.PendingGrading (Views/_ViewImports.cshtml:2 sudah @using HcPortal.Models -> tanpa @using tambahan).
- C#: AssessmentConstants.AssessmentStatus.PendingGrading. Karena const, valid sebagai case label di switch.
- Services/WorkerDataService.cs:2 sudah using HcPortal.Models. Controllers/CMPController.cs:4 sudah using HcPortal.Models (VERIFIED - tidak perlu tambah using).

Model UnifiedTrainingRecord (dirender Records.cshtml + RecordsWorkerDetail.cshtml):
- bool? IsPassed (preserve null dari AssessmentSession.IsPassed)
- string Status (di-set GetUnifiedRecords switch)
- string RecordType ("Assessment Online" | "Training Manual" | dst)

Model AllWorkersHistoryRow (dirender Excel ExportRecordsTeamAssessment):
- bool? IsPassed (Models/AllWorkersHistoryRow.cs:26 - upstream GetAllWorkersHistory preserve null, bersih)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service label-unify + Excel cell - GetUnifiedRecords + ExportRecordsTeamAssessment null branch (CMP06R-04 source + D-09)</name>
  <files>Services/WorkerDataService.cs, Controllers/CMPController.cs</files>
  <read_first>
    - Services/WorkerDataService.cs:28-60 (method GetUnifiedRecords - switch di L52-57; verifikasi using HcPortal.Models di L2)
    - Controllers/CMPController.cs:685-696 (loop Excel ExportRecordsTeamAssessment - cell status di L694; verifikasi using HcPortal.Models di L4)
    - RESEARCH Code Examples #5 (service excerpt) + #6 (Excel excerpt) - copy-ready
    - RESEARCH Q7 (boundary Phase 346 - JANGAN ubah filter Status == "Completed" di WorkerDataService.cs:33)
  </read_first>
  <action>
1a. Services/WorkerDataService.cs - method GetUnifiedRecords, ubah switch arm null (saat ini L56) dari literal "Completed" ke konstanta. Switch saat ini (L52-57):

    // Phase 337 CMP-06: three-way switch (null = Completed, bukan Failed palsu)
    Status = a.IsPassed switch
    {
        true => "Passed",
        false => "Failed",
        null => "Completed"          // GANTI baris ini
    },

Menjadi:

    // Phase 345 CMP06R-04: null = Menunggu Penilaian (label-unify dari Phase 337 "Completed")
    Status = a.IsPassed switch
    {
        true => "Passed",
        false => "Failed",
        null => AssessmentConstants.AssessmentStatus.PendingGrading
    },

JANGAN sentuh arm true/false (rename "Passed"/"Failed"->Indonesia = POL-01/Phase 347). JANGAN ubah filter .Where(a => ... a.Status == "Completed") di L33 (inklusi status-murni = Phase 346 REC-07, OUT OF SCOPE).

1b. Controllers/CMPController.cs:694 (D-09 minor) - ubah ternary cell value, null branch dari "" ke konstanta. Saat ini:

    ws.Cell(i + 2, 7).Value = r.IsPassed == true ? "Passed" : (r.IsPassed == false ? "Failed" : "");

Menjadi:

    ws.Cell(i + 2, 7).Value = r.IsPassed == true ? "Passed" : (r.IsPassed == false ? "Failed" : AssessmentConstants.AssessmentStatus.PendingGrading);

using HcPortal.Models sudah ada di CMPController.cs:4 (VERIFIED) - tidak perlu tambah using. JANGAN sentuh arm "Passed"/"Failed".
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; if ((Select-String -Path Services/WorkerDataService.cs -Pattern 'null => AssessmentConstants\.AssessmentStatus\.PendingGrading') -and (Select-String -Path Controllers/CMPController.cs -Pattern 'IsPassed == false \? "Failed" : AssessmentConstants\.AssessmentStatus\.PendingGrading')) { dotnet build HcPortal.csproj -clp:ErrorsOnly } else { Write-Error "grep miss" }</automated>
  </verify>
  <acceptance_criteria>
    - Services/WorkerDataService.cs contains: null => AssessmentConstants.AssessmentStatus.PendingGrading
    - Services/WorkerDataService.cs NO longer contains: null => "Completed"
    - Services/WorkerDataService.cs STILL contains: a.Status == "Completed" (filter L33 unchanged)
    - Controllers/CMPController.cs line 694 null-branch contains AssessmentConstants.AssessmentStatus.PendingGrading (no longer : "")
    - dotnet build HcPortal.csproj exits 0 (no errors)
  </acceptance_criteria>
  <done>Service produces Status="Menunggu Penilaian" untuk sesi Completed+IsPassed-null; Excel cell pending = "Menunggu Penilaian"; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: View 3-way - RecordsWorkerDetail badge + Records switch case (CMP06R-01 + CMP06R-04 view)</name>
  <files>Views/CMP/RecordsWorkerDetail.cshtml, Views/CMP/Records.cshtml</files>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml:225-232 (binary block if(IsPassed==true){Passed}else{Failed} di L226-231)
    - Views/CMP/Records.cshtml:182-192 (template 3-way SUDAH ADA - pola referensi; switch sc inline di L188)
    - RESEARCH Code Examples #1 (RecordsWorkerDetail 3-way) + #5 (Records switch case)
    - RESEARCH Pitfall 5 (JANGAN sentuh teks "Passed"/"Failed" true/false - itu POL-01/347; JANGAN tambah kolom Aksi - itu REC-01/346)
    - UI-SPEC Color section (amber bg-warning text-dark, WAJIB text-dark untuk kontras WCAG)
  </read_first>
  <action>
2a. Views/CMP/RecordsWorkerDetail.cshtml:226-231 (CMP06R-01) - ubah binary jadi 3-way. Block saat ini:

    @if (item.RecordType == "Assessment Online") {
        @if (item.IsPassed == true) {
            <span class="badge bg-success">Passed</span>
        } else {
            <span class="badge bg-danger">Failed</span>
        }
    } else {
        ... (block training/status existing - JANGAN diubah)
    }

Menjadi (tambah else if (== false) lalu else amber):

    @if (item.RecordType == "Assessment Online") {
        @if (item.IsPassed == true) {
            <span class="badge bg-success">Passed</span>
        } else if (item.IsPassed == false) {
            <span class="badge bg-danger">Failed</span>
        } else {
            <span class="badge bg-warning text-dark">@AssessmentConstants.AssessmentStatus.PendingGrading</span>
        }
    } else {
        ... (block training/status existing - JANGAN diubah)
    }

item = UnifiedTrainingRecord, IsPassed = bool? (sudah benar). Hanya tambah dua cabang. JANGAN sentuh block else (training/status di L232+).

2b. Views/CMP/Records.cshtml:188 (CMP06R-04 view) - C-2: switch SUDAH 3-way, hanya tambah case di switch sc. Saat ini (L188, inline):

    var sc = item.Status switch { "Passed" => "bg-success", "Valid" => "bg-success", "Completed" => "bg-info", "Permanent" => "bg-success", "Failed" => "bg-danger", "Expired" => "bg-warning text-dark", _ => "bg-secondary" };

Tambah satu case (konstanta valid sebagai case label karena const):

    var sc = item.Status switch { "Passed" => "bg-success", "Valid" => "bg-success", "Completed" => "bg-info", "Permanent" => "bg-success", "Failed" => "bg-danger", "Expired" => "bg-warning text-dark", AssessmentConstants.AssessmentStatus.PendingGrading => "bg-warning text-dark", _ => "bg-secondary" };

JANGAN hapus case "Completed" => "bg-info" (masih dipakai surface lain non-assessment). JANGAN cari "Failed" untuk diperbaiki - Records.cshtml tidak punya bug Failed (C-2). Karena Task 1 ubah service jadi null => PendingGrading, sesi pending sekarang punya item.Status == "Menunggu Penilaian" -> case baru ini match -> badge amber.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; if ((Select-String -Path Views/CMP/RecordsWorkerDetail.cshtml -Pattern 'bg-warning text-dark.*AssessmentConstants\.AssessmentStatus\.PendingGrading') -and (Select-String -Path Views/CMP/Records.cshtml -Pattern 'AssessmentConstants\.AssessmentStatus\.PendingGrading => "bg-warning text-dark"')) { dotnet build HcPortal.csproj -clp:ErrorsOnly } else { Write-Error "grep miss" }</automated>
  </verify>
  <acceptance_criteria>
    - Views/CMP/RecordsWorkerDetail.cshtml contains both: else if (item.IsPassed == false) AND bg-warning text-dark with @AssessmentConstants.AssessmentStatus.PendingGrading
    - Views/CMP/RecordsWorkerDetail.cshtml STILL contains: badge bg-success (Passed) AND badge bg-danger (Failed)
    - Views/CMP/Records.cshtml contains: AssessmentConstants.AssessmentStatus.PendingGrading => "bg-warning text-dark"
    - Views/CMP/Records.cshtml STILL contains: "Completed" => "bg-info"
    - dotnet build HcPortal.csproj exits 0 (Razor compile clean)
  </acceptance_criteria>
  <done>RecordsWorkerDetail render badge amber untuk pending; Records switch map "Menunggu Penilaian" -> bg-warning text-dark; graded sessions unchanged; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase ini display-correctness murni: tidak ada endpoint baru, tidak ada input user baru, tidak ada perubahan authz, tidak ada migration. Data yang dirender sudah ada di DB dan sudah lolos authz existing (CMP/Records + RecordsWorkerDetail + Excel export sudah ber-[Authorize]). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-345-01-01 | Information Disclosure | Badge/Excel label "Menunggu Penilaian" | accept | Label hanya mengganti teks status yang SUDAH ditampilkan (Failed/Completed/kosong) untuk data yang SUDAH lolos authz. Tidak mengekspos field baru, tidak menurunkan filter. Sesi yang sebelumnya tidak tampil tetap tidak tampil (filter Status=="Completed" tidak diubah - C-2/Q7). Zero new exposure. |
| T-345-01-02 | Tampering | switch case label di Records.cshtml | accept | Konstanta const compile-time (AssessmentConstants.AssessmentStatus.PendingGrading), bukan input runtime. Tidak ada jalur tamper. |
| T-345-01-03 | (S/R/D/E) | - | N/A | Tidak ada autentikasi/non-repudiation/DoS/elevation surface yang disentuh. Tidak ada endpoint, query, atau role baru. |
</threat_model>

<verification>
- dotnet build HcPortal.csproj exits 0 (Razor + C# compile clean setelah edit service/controller/view).
- grep konfirmasi keempat file ter-edit mengandung AssessmentConstants.AssessmentStatus.PendingGrading di lokasi yang benar.
- grep konfirmasi NO regression: arm true/false ("Passed"/"Failed") + filter Status=="Completed" + case "Completed"=>"bg-info" tetap ada.
- Visual UAT badge amber 3 surface (RecordsWorkerDetail + Records + Excel) dilakukan di Plan 345-04 via Playwright/MCP (SEED_WORKFLOW).
</verification>

<success_criteria>
- ROADMAP SC1 (sebagian): /CMP/RecordsWorkerDetail sesi Completed+IsPassed-null tampil "Menunggu Penilaian" (bukan Failed).
- ROADMAP SC2: /CMP/Records konsisten label "Menunggu Penilaian" (ganti "Completed"); sesi graded Pass/Fail tetap normal (no regression).
- ROADMAP SC4 (sebagian): dotnet build 0 error.
- D-09: Excel ExportRecordsTeamAssessment cell pending = "Menunggu Penilaian".
- D-02: semua surface pakai konstanta PendingGrading (zero literal string baru).
</success_criteria>

<output>
After completion, create `.planning/phases/345-assessment-pending-grade-display-fix/345-01-SUMMARY.md`
</output>
