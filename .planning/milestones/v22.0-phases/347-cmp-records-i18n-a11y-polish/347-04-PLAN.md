---
phase: 347-cmp-records-i18n-a11y-polish
plan: 04
type: execute
wave: 4
depends_on: [347-01, 347-02, 347-03]
files_modified: []
autonomous: false
requirements: [POL-01, POL-02, POL-03, POL-04, POL-05, POL-06, POL-07, POL-08, POL-09, POL-10]
user_setup: []

must_haves:
  truths:
    - "dotnet build selesai 0 error (semua perubahan Razor + records.css valid)"
    - "Semua teks i18n + atribut a11y ADA (grep sweep lolos); semua teks Inggris lama TIDAK ADA"
    - "Tidak ada regresi visual: stat-card hover, sticky-header, fadeIn render identik via records.css"
    - "Badge IsPassed null tetap 'Menunggu Penilaian' amber (Phase 345 + REC-07 Phase 346 tidak regresi)"
    - "REC-01..09 Phase 346 fungsional (kolom Aksi/Lihat Hasil/modal/Team search/pagination/export) tidak rusak"
  artifacts: []
  key_links:
    - from: "records.css (POL-08)"
      to: "Records + RecordsWorkerDetail render"
      via: "@section Styles -> _Layout RenderSection"
      pattern: "no visual regression"
---

<objective>
Verifikasi akhir fase 347: build gate + grep sweep menyeluruh (semua POL terpenuhi, semua teks Inggris hilang) + Playwright spot-check ringan untuk memastikan TIDAK ADA regresi visual (POL-08 risiko utama) dan badge/label benar. Tidak ada perubahan kode — plan ini murni gate + UAT checkpoint.

Purpose: D-03 verification (build + grep + Playwright spot-check 1-2 surface). Memastikan trilogi 345->346->347 utuh: Phase 346 REC-01..09 tidak regresi, Phase 345 null-case tetap. TIDAK perlu xUnit baru (no logic change).
Output: Hasil build, grep sweep, dan checkpoint UAT visual; siap commit fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md
@.planning/phases/346-cmp-records-detail-search-logic/346-VERIFICATION.md
@docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md

<interfaces>
<!-- Dev environment (CLAUDE.md DEV_WORKFLOW + 347-CONTEXT specifics): -->
<!-- App run WAJIB ASPNETCORE_ENVIRONMENT=Development (tanpa ini connection string default ke placeholder Production — lihat 346-06 env note). -->
<!-- Dev creds: admin + coach pwd 123456; DB HcPortalDB_Dev (SQLEXPRESS). Local URL http://localhost:5277. -->
<!-- POL-01 null-case verify: bila perlu sesi pending, reuse tests/sql/cmp346-seed.sql (jangan buat seed baru — restore DB setelah test per SEED_WORKFLOW). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dotnet build 0-error + grep sweep menyeluruh (10 POL)</name>
  <read_first>
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-01-SUMMARY.md
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-02-SUMMARY.md
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-03-SUMMARY.md
    - docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md (§"Phase 347" baris 135-157: tabel POL lengkap untuk cross-check)
  </read_first>
  <action>
    A. BUILD GATE (CLAUDE.md DEV_WORKFLOW):
    Jalankan `dotnet build` dari root proyek. HARUS exit 0 (0 error). Bila ada error Razor (mis. @section salah tempat, tag <style> yatim, escape @keyframes), perbaiki atau laporkan ke plan terkait sebelum lanjut.

    B. GREP SWEEP (konfirmasi seluruh 10 POL + non-regresi). Jalankan grep dan pastikan hasil sesuai:

    POL-01/02/05 i18n PRESENT:
    - Records.cshtml: `>Lulus<`, `>Tidak Lulus<`, `>Nilai<` ADA.
    - RecordsWorkerDetail.cshtml: `>Lulus<`, `>Tidak Lulus<`, `>Jabatan<`, "rekam jejak penilaian dan pelatihan anggota tim" ADA.

    i18n ABSENT (teks Inggris lama hilang):
    - Records.cshtml + RecordsWorkerDetail.cshtml: `>Passed</span>`, `>Failed</span>` TIDAK ADA.
    - RecordsWorkerDetail.cshtml: `>Position<`, `>Section<` (label `<small>`), "for team member" TIDAK ADA.
    - RecordsWorkerDetail.cshtml + RecordsTeam.cshtml: `All Categories`, `All Sub Categories`, `All Types` TIDAK ADA (markup + JS).

    POL-03/04 PRESENT:
    - RecordsTeam.cshtml: `>Jabatan<` ADA; `Semua Kategori`, `Semua Sub Kategori` ADA.
    - RecordsWorkerDetail.cshtml: `Semua Kategori`, `Semua Sub Kategori`, `Semua Tipe` ADA.

    POL-06 a11y modal:
    - Records.cshtml + RecordsWorkerDetail.cshtml: `aria-labelledby="modalTrainingTitle"` 2x; `aria-label="Tutup"` 2x; `role="dialog"` ada.

    POL-07 label for:
    - RecordsWorkerDetail.cshtml: `for="categoryFilter"`, `for="subCategoryFilter"`, `for="yearFilter"`, `for="typeFilter"`, `for="searchInput"` ADA.
    - RecordsTeam.cshtml: `for="categoryFilter"`, `for="subCategoryFilter"`, `for="statusFilter"`, `for="dateFrom"`, `for="dateTo"`, `for="teamSearch"`, `for="searchScope"` ADA.
    - Records.cshtml: `for="searchInput"` ADA.

    POL-08 DRY:
    - wwwroot/css/records.css ADA + `.stat-card`/`.sticky-header`/`@keyframes fadeIn`/`.worker-row`/`.training-row` ADA; TIDAK ada `@@keyframes`.
    - _Layout.cshtml: `RenderSectionAsync("Styles"` 1x.
    - Records.cshtml + RecordsWorkerDetail.cshtml: `~/css/records.css` 2x + `@section Styles` 2x; `.stat-card {` (inline) TIDAK ADA.
    - RecordsTeam.cshtml: `<style>` TIDAK ADA; `~/css/records.css` TIDAK ADA (partial).

    POL-09 grid:
    - RecordsWorkerDetail.cshtml: `col-12 col-sm-6 col-md-2` >=5x.

    POL-10:
    - Records.cshtml + RecordsWorkerDetail.cshtml + RecordsTeam.cshtml: tombol reset `type="button"`.
    - RecordsTeam.cshtml: `aria-current="page"` ADA.

    NON-REGRESI Phase 345/346 (CRITICAL):
    - Records.cshtml + RecordsWorkerDetail.cshtml: "Menunggu Penilaian" path utuh (`AssessmentConstants.AssessmentStatus.PendingGrading` masih ada di badge null-case).
    - Records.cshtml: `Url.Action("Results"`, `#trainingDetailModal`, `colspan="7"` masih ada (REC-01/02).
    - RecordsWorkerDetail.cshtml: `asp-action="Results"`, `id="mdKategori"`, `id="mdSubKategori"` masih ada (REC-03/05).
    - RecordsTeam.cshtml: `id="teamSearch"`, `id="searchScope"`, `id="paginationNav"`, export link wiring masih ada (REC-06/08).
    - CMPController.cs: `IsResultsAuthorized` tidak tersentuh (REC-04 — fase 347 tidak edit controller).

    Catat hasil build + setiap baris grep (PASS/FAIL) di SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; dotnet build 2>&1 | Select-String -Pattern 'Build succeeded','0 Error','error' | Select-Object -First 20</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit code 0 (Build succeeded, 0 Error).
    - Semua grep "PRESENT" di atas: hasil sesuai (teks/atribut ADA).
    - Semua grep "ABSENT" di atas: hasil 0 match (teks Inggris lama hilang).
    - Semua grep "NON-REGRESI": Phase 345/346 anchor masih ADA.
    - records.css tidak mengandung `@@keyframes`.
  </acceptance_criteria>
  <done>Build hijau; seluruh 10 POL terkonfirmasi via grep; Phase 345/346 tidak regresi.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Playwright spot-check visual no-regression (Records + RecordsWorkerDetail + Tab Team)</name>
  <what-built>
    Fase 347 menukar teks Inggris→Indonesia, menambah atribut a11y (ARIA modal, label for=, grid responsif, type=button, aria-current), dan mengekstrak `<style>` duplikat ke `wwwroot/css/records.css` (di-link via @section Styles + _Layout RenderSection). Risiko utama: regresi visual dari POL-08 CSS extraction.
  </what-built>
  <how-to-verify>
    Jalankan app lokal dengan environment Development (WAJIB — tanpa ini connection string default Production placeholder):
    1. Set env: `$env:ASPNETCORE_ENVIRONMENT = "Development"` lalu `dotnet run` (atau gunakan profil Development). App di `http://localhost:5277`.
    2. Login admin (`admin@pertamina.com` / pwd `123456`) — atau coach untuk Team/Worker Detail.

    Spot-check via Playwright MCP (ATAU manual browser bila MCP tak tersedia):

    A. Halaman My Records (`/CMP/Records`):
       - Stat cards: hover → efek translateY(-5px) + box-shadow + garis gradient atas muncul (records.css bekerja). Stat-icon scale+rotate saat hover.
       - Tabel: header sticky saat scroll (background gradient abu).
       - Baris training: animasi fadeIn saat render.
       - Header kolom: "Nilai" (bukan Score). Badge assessment lulus = "Lulus" hijau; gagal = "Tidak Lulus" merah.
       - Badge assessment PENDING (esai belum dinilai) = "Menunggu Penilaian" amber (REC-07/Phase 345 — JANGAN hilang). Bila DB lokal tak punya sesi pending, reuse `tests/sql/cmp346-seed.sql` (restore DB setelah test per SEED_WORKFLOW).
       - Modal Detail training: buka via tombol Detail → judul "Detail Training", tombol close (×) ada aria-label "Tutup"; field lengkap.
       - Search My Records: ada label "Cari" terlihat.

    B. Halaman Worker Detail (`/CMP/RecordsWorkerDetail?...` via tombol di Team View):
       - Subtitle Indonesia ("Lihat detail rekam jejak penilaian dan pelatihan anggota tim.").
       - Info worker: label "Jabatan" + label organisasi (Bagian/sesuai OrgLabels). 
       - Stat cards + sticky-header + fadeIn render identik (records.css).
       - Filter bar: di layar sempit (resize/mobile viewport) kolom filter TIDAK menumpuk 6 baris — responsif (col-sm-6). Setiap filter punya label.
       - Badge Lulus/Tidak Lulus/Menunggu Penilaian benar.
       - Modal training: aria + close "Tutup".

    C. Tab Team (di halaman Records, klik tab Team — partial RecordsTeam):
       - sticky-header + animasi worker-row tetap render (styling via records.css parent page — INI verifikasi nuance partial D-01).
       - Header kolom "Jabatan" (bukan Position).
       - Pagination: halaman aktif ditandai (aria-current; visual highlight).
       - Search Nama/Training/Keduanya + export link masih berfungsi (REC-06 tidak regresi).

    Konfirmasi: TIDAK ada elemen kehilangan styling (stat-card flat tanpa hover, sticky-header hilang, fadeIn mati = REGRESI → laporkan). Badge & label sesuai. Phase 346 fungsi (Aksi/Lihat Hasil/modal/Team search) tetap jalan.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila semua surface render benar tanpa regresi visual + badge/label sesuai; atau deskripsikan masalah yang ditemukan (mis. "stat-card hover hilang di Worker Detail").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Tidak ada. Plan verifikasi — tidak mengubah kode. (Threat model substantif ada di plan 347-01/02/03; semuanya accept, tidak ada surface baru.)
</threat_model>

<verification>
- `dotnet build` exit 0.
- Grep sweep 10 POL: semua PRESENT ADA, semua ABSENT 0-match, semua NON-REGRESI utuh.
- Playwright/manual spot-check: no visual regression (stat-card/sticky-header/fadeIn) + badge Lulus/Tidak Lulus/Menunggu Penilaian benar + Phase 346 fungsi utuh.
- Tidak ada xUnit baru (no logic change — per D-03).
</verification>

<success_criteria>
- SC1 (ROADMAP §347): Badge Passed/Failed→Lulus/Tidak Lulus (null tetap Menunggu Penilaian). VERIFIED via grep + Playwright.
- SC2: Header Score→Nilai; Position→Jabatan; Section→OrgLabels; All...→Semua...; subtitle ID. VERIFIED via grep.
- SC3: a11y modal aria + label for= semua filter + pagination aria-current. VERIFIED via grep.
- SC4: DRY <style>→records.css; mobile grid responsif; type=button reset. VERIFIED via grep + Playwright (no regresi).
- SC5: dotnet build 0 error + no visual regression (Playwright spot-check). VERIFIED.
- Non-regresi: Phase 346 REC-01..09 + Phase 345 null-case utuh.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/347-cmp-records-i18n-a11y-polish/347-04-SUMMARY.md`.
</output>
