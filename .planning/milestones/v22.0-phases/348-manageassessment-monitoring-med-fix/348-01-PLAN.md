---
phase: 348-manageassessment-monitoring-med-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
autonomous: true
requirements: [MAM-01, MAM-02, MAM-03]
must_haves:
  truths:
    - "RegenerateToken untuk grup Pre-Post me-regenerate token PreTest DAN PostTest (token sama), walau PostTest beda tanggal"
    - "Link Monitoring di row Pre-Post Tab1 mengarah ke kedua half (Pre + Post terpisah), bukan PreTest saja"
    - "Export Excel + Bulk PDF untuk grup Pre-Post menyertakan peserta PreTest DAN PostTest (LinkedGroupId-aware), tidak silently miss PostTest"
    - "Badge 'X belum dinilai' muncul untuk grup Pre-Post di Assessment Monitoring saat ada essay PostTest pending"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "RegenerateToken LinkedGroupId match (MAM-01) + ExportAssessmentResults/BulkExportPdf LinkedGroupId-aware (MAM-02) + prePostGroups MenungguPenilaianCount (MAM-03)"
      contains: "a.LinkedGroupId == assessment.LinkedGroupId"
    - path: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml"
      provides: "Link Monitoring/Export Pre-Post per-half atau LinkedGroupId-aware (MAM-02)"
      contains: "linkedGroupId"
  key_links:
    - from: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml dropdown Aksi"
      to: "AssessmentMonitoringDetail / ExportAssessmentResults / BulkExportPdf"
      via: "Url.Action param linkedGroupId (Pre-Post) bukan single scheduleDate"
      pattern: "linkedGroupId"
    - from: "AssessmentAdminController.RegenerateToken"
      to: "AssessmentSessions siblings"
      via: "Where LinkedGroupId match bila Pre-Post"
      pattern: "LinkedGroupId == assessment.LinkedGroupId"
---

<objective>
Tutup 3 bug MED Tema A (konsistensi Pre-Post group) di ManageAssessment + Assessment Monitoring: token regenerate, link Monitoring/Export, dan badge pending-grading. Akar masalah ketiganya sama — kode mengasumsikan Pre-Post = same-date dan memfilter `Schedule.Date` (rep = PreTest), sehingga PostTest (yang boleh beda tanggal) silently ke-miss.

Purpose: Admin/HC yang mengelola assessment Pre-Post mendapat perilaku konsisten antara PreTest dan PostTest (token sama-regenerate, monitoring/export mencakup keduanya, badge pending muncul).
Output: `AssessmentAdminController.cs` (3 method) + `_AssessmentGroupsTab.cshtml` (dropdown Aksi row Pre-Post) ter-fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-CONTEXT.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md
@docs/superpowers/specs/2026-06-04-manageassessment-monitoring-audit-design.md

Project rules (./CLAUDE.md): respond Bahasa Indonesia; verifikasi lokal `dotnet build` + `dotnet run` (http://localhost:5277, WAJIB `ASPNETCORE_ENVIRONMENT=Development`) sebelum commit; JANGAN edit kode/DB di server Dev/Prod. No migration, no schema change (D-A).

Dev creds UAT: admin `admin@pertamina.com` pwd `123456`, DB `HcPortalDB_Dev`.

<interfaces>
<!-- Diekstrak dari codebase 2026-06-04. Executor pakai langsung — tak perlu eksplorasi ulang. -->

Tab1 dynamic shape (AssessmentAdminController.cs:160-191) — properti tersedia di `group` (IEnumerable<dynamic> ViewBag.ManagementData):
  Title, Category, Schedule (DateTime), ExamWindowCloseDate, DurationMinutes, Status, IsTokenRequired,
  AccessToken, PassPercentage, AllowAnswerReview, RepresentativeId (int), Users, AllIds (List<int>),
  UserCount (int), GroupStatus (string: Open/Upcoming/Closed), IsPrePostGroup (bool), LinkedGroupId (int?)
  CATATAN: Tab1 dynamic TIDAK punya PreSubRow/PostSubRow dan TIDAK punya Post schedule. Untuk MAM-02 route Pre-Post → pakai LinkedGroupId (tersedia), bukan dua scheduleDate.

ExportAssessmentResults signature (AssessmentAdminController.cs:4115):
  public async Task<IActionResult> ExportAssessmentResults(string title, string category, DateTime scheduleDate)
  Filter saat ini: .Where(a => a.Title == title && a.Category == category && a.Schedule.Date == scheduleDate.Date)

BulkExportPdf signature (AssessmentAdminController.cs:4499):
  public async Task<IActionResult> BulkExportPdf(string title, string category, DateTime scheduleDate)
  Filter saat ini: .Where(s => s.Title == title && s.Category == category && s.Schedule.Date == scheduleDate.Date)

AssessmentMonitoringDetail endpoint (AssessmentAdminController.cs:3163) — SUDAH terima assessmentType:
  AssessmentMonitoringDetail(string title, string category, DateTime scheduleDate, string? assessmentType = null)
  Sudah filter: if (!string.IsNullOrEmpty(assessmentType)) query = query.Where(a => a.AssessmentType == assessmentType);
  → Monitoring-link MAM-02 = MURNI view-side (tambah link per-half PreTest/PostTest). Endpoint NO CHANGE.

prePostGroups MonitoringGroupViewModel (AssessmentAdminController.cs:2749-2795) — TIDAK assign MenungguPenilaianCount (default 0).
standardGroups parity (AssessmentAdminController.cs:2825): MenungguPenilaianCount = g.Count(a => a.IsMenungguPenilaian)
postSubs di scope prePostGroups = sessions PostTest grup (sudah dipakai L2757-2761 untuk TotalCount/CompletedCount/PassedCount).
IsMenungguPenilaian = computed property AssessmentSession (= Status == PendingGrading); terbukti dipakai L2825.

RegenerateToken (AssessmentAdminController.cs:2600-2640) — siblings match by Title+Category+Schedule.Date (L2617-2621).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MAM-01 RegenerateToken match by LinkedGroupId + MAM-03 prePostGroups MenungguPenilaianCount</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:2600-2640 (RegenerateToken — siblings query L2617-2621 + audit msg L2636) — EDIT TARGET MAM-01
    - Controllers/AssessmentAdminController.cs:2749-2795 (prePostGroups MonitoringGroupViewModel) — EDIT TARGET MAM-03
    - Controllers/AssessmentAdminController.cs:2825 (standardGroups parity: MenungguPenilaianCount = g.Count(a => a.IsMenungguPenilaian)) — ANALOG
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-01" + §"MAM-03" (catatan adaptasi + pitfall PostSchedule > PreSchedule)
    - 348-CONTEXT.md §decisions (MAM-01, MAM-03 terkunci verbatim)
  </read_first>
  <behavior>
    - Test 1 (MAM-01 helper, jika diekstrak): grup Pre-Post (LinkedGroupId=5, PreTest tgl 2026-06-10 + PostTest tgl 2026-06-17) → sibling selection mengembalikan KEDUA session (Pre + Post), bukan hanya yang same-date.
    - Test 2 (MAM-01 fallback): grup Standard (LinkedGroupId=null) → sibling selection fallback ke Title+Category+Schedule.Date (perilaku lama, tak berubah).
  </behavior>
  <action>
    **MAM-01 — RegenerateToken (AssessmentAdminController.cs:2616-2621):**
    Ganti query siblings agar route-by-LinkedGroupId bila Pre-Post. Saat ini (BUG):
    ```csharp
    var siblings = await _context.AssessmentSessions
        .Where(a => a.Title == assessment.Title
                 && a.Category == assessment.Category
                 && a.Schedule.Date == assessment.Schedule.Date)
        .ToListAsync();
    ```
    Ubah jadi:
    ```csharp
    // MAM-01: Pre-Post route by LinkedGroupId (PostTest bisa beda tanggal — validasi cuma enforce PostSchedule > PreSchedule).
    // Fallback ke Title+Category+Schedule.Date untuk grup standard.
    var siblings = assessment.LinkedGroupId != null
        ? await _context.AssessmentSessions
            .Where(a => a.LinkedGroupId == assessment.LinkedGroupId)
            .ToListAsync()
        : await _context.AssessmentSessions
            .Where(a => a.Title == assessment.Title
                     && a.Category == assessment.Category
                     && a.Schedule.Date == assessment.Schedule.Date)
            .ToListAsync();
    ```
    Audit msg L2636 (`{siblings.Count} sibling(s) updated`) ikut benar otomatis (count siblings sudah benar). JANGAN ubah signature method atau audit format.

    **MAM-03 — prePostGroups MenungguPenilaianCount (AssessmentAdminController.cs:2749-2795):**
    Tambah satu baris assignment ke object initializer `new MonitoringGroupViewModel { ... }` prePostGroups (object yang dimulai L2749). Sisipkan SETELAH baris `CancelledCount = g.Count(a => a.Status == "Cancelled"),` (L2762) atau di posisi konsisten dengan stat lain:
    ```csharp
    MenungguPenilaianCount = postSubs.Count(a => a.IsMenungguPenilaian),
    ```
    PENTING: pakai `postSubs` (grading terjadi di Post half), BUKAN `g` atau `preSubs`. Parity dengan standardGroups L2825 yang pakai `g.Count(a => a.IsMenungguPenilaian)`. Tanpa fix ini, badge "X belum dinilai" (`AssessmentMonitoring.cshtml:276`) tak pernah muncul untuk SELURUH grup Pre-Post.

    **TDD (jika feasibel tanpa over-engineering):** MAM-01 sibling-selection bisa diekstrak ke static helper testable hanya bila trivial; bila tidak, biarkan inline dan andalkan UAT Plan 05. JANGAN paksa refactor besar untuk test — pure-logic xUnit untuk sibling selection opsional. MAM-03 = 1 baris, tidak butuh test unit (verify via UAT badge).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - grep `Controllers/AssessmentAdminController.cs` menemukan `a.LinkedGroupId == assessment.LinkedGroupId` (MAM-01 LinkedGroupId branch).
    - grep menemukan blok fallback masih ada: `a.Schedule.Date == assessment.Schedule.Date` (grup standard tak break).
    - grep menemukan `MenungguPenilaianCount = postSubs.Count(a => a.IsMenungguPenilaian)` (MAM-03, persis pakai `postSubs`).
    - `dotnet build` → "Build succeeded", 0 error.
  </acceptance_criteria>
  <done>RegenerateToken route-by-LinkedGroupId untuk Pre-Post (fallback standard preserved); prePostGroups assign MenungguPenilaianCount dari postSubs; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: MAM-02 Export/PDF LinkedGroupId-aware (both-half)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:4113-4123 (ExportAssessmentResults signature + filter Schedule.Date L4120-4122) — EDIT TARGET
    - Controllers/AssessmentAdminController.cs:4497-4504 (BulkExportPdf signature + filter Schedule.Date L4503) — EDIT TARGET
    - Controllers/AssessmentAdminController.cs:158-171 (prePostGrouped GroupBy LinkedGroupId — referensi shape Pre-Post)
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"No Analog Found" (both-half export = satu-satunya yang perlu sedikit invent; prinsip D-01 no-single-date-filter)
    - 348-CONTEXT.md §decisions D-01 (Export Excel + Bulk PDF → LinkedGroupId-aware, ekspor KEDUA half; JANGAN filter by single scheduleDate)
  </read_first>
  <action>
    Prinsip D-01: untuk grup Pre-Post, Export Excel + Bulk PDF harus query by `LinkedGroupId` (mencakup Pre + Post), BUKAN single `Schedule.Date`. Pendekatan backward-compatible: tambah parameter opsional `int? linkedGroupId = null` ke kedua endpoint; bila non-null → filter by LinkedGroupId, else → filter by Schedule.Date (perilaku lama untuk grup standard).

    **ExportAssessmentResults (AssessmentAdminController.cs:4115):**
    Ubah signature:
    ```csharp
    public async Task<IActionResult> ExportAssessmentResults(string title, string category, DateTime scheduleDate, int? linkedGroupId = null)
    ```
    Ubah query sessions (L4118-4123):
    ```csharp
    var sessions = await _context.AssessmentSessions
        .Include(a => a.User)
        .Where(a => linkedGroupId != null
            ? a.LinkedGroupId == linkedGroupId
            : (a.Title == title && a.Category == category && a.Schedule.Date == scheduleDate.Date))
        .ToListAsync();
    ```
    Sisanya (header workbook, rows, OrderBy) TETAP. CATATAN: bila linkedGroupId-mode, `firstSession.Schedule` (L4211 "Jadwal") akan menampilkan schedule rep — acceptable (header info, bukan filter). JANGAN ubah logic rows/Result/Score.

    **BulkExportPdf (AssessmentAdminController.cs:4499):**
    Ubah signature:
    ```csharp
    public async Task<IActionResult> BulkExportPdf(string title, string category, DateTime scheduleDate, int? linkedGroupId = null)
    ```
    Ubah query sessions (L4501-4504):
    ```csharp
    var sessions = await _context.AssessmentSessions
        .Include(s => s.User)
        .Where(s => linkedGroupId != null
            ? s.LinkedGroupId == linkedGroupId
            : (s.Title == title && s.Category == category && s.Schedule.Date == scheduleDate.Date))
        .ToListAsync();
    ```
    `eligibleSessions` filter (L4506-4509: `Status != "Cancelled" && (CompletedAt != null || Score != null)`) + DoS guard 50 (L4515) + fileName (L4534) TETAP. CATATAN: untuk both-half, jumlah peserta = Pre + Post bisa lebih besar — DoS guard 50 tetap berlaku (acceptable; admin filter lebih spesifik bila >50). JANGAN ubah GeneratePerPesertaPdf.

    JANGAN ubah cara call-site lama (grup standard view tetap kirim scheduleDate tanpa linkedGroupId — default null → perilaku identik). Wiring call-site Pre-Post ada di Task 3.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - grep `ExportAssessmentResults(string title, string category, DateTime scheduleDate, int? linkedGroupId` ditemukan (signature extended).
    - grep `BulkExportPdf(string title, string category, DateTime scheduleDate, int? linkedGroupId` ditemukan.
    - grep menemukan `a.LinkedGroupId == linkedGroupId` (Export) DAN `s.LinkedGroupId == linkedGroupId` (PDF) — both-half branch.
    - Filter fallback Schedule.Date masih ada di kedua endpoint (grup standard tak break).
    - `dotnet build` → "Build succeeded", 0 error.
  </acceptance_criteria>
  <done>ExportAssessmentResults + BulkExportPdf terima `int? linkedGroupId` opsional; bila non-null query by LinkedGroupId (both-half), else fallback Schedule.Date; grup standard backward-compatible; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: MAM-02 view-side — link Monitoring per-half + Export/PDF LinkedGroupId untuk row Pre-Post</name>
  <files>Views/Admin/Shared/_AssessmentGroupsTab.cshtml</files>
  <read_first>
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml:253-286 (dropdown Aksi: Monitoring L261-263, Export Excel L278-280, Bulk PDF L283-285 — semua kirim scheduleDate=group.Schedule.Date PreTest rep) — EDIT TARGET
    - Views/Admin/AssessmentMonitoring.cshtml:337-388 (pola preDetailUrl/postDetailUrl split per-half assessmentType=PreTest/PostTest) — ANALOG PROVEN
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-02" (excerpt analog verbatim + catatan dynamic shape Tab1 TIDAK punya Post schedule → route by LinkedGroupId)
    - 348-CONTEXT.md §decisions D-01 + §"Claude's Discretion" (MAM-02 button layout = planner discretion; prinsip no-single-date-filter WAJIB; hindari clutter)
  </read_first>
  <action>
    Untuk row grup Pre-Post (`(bool)group.IsPrePostGroup == true`), perbaiki dropdown Aksi agar tidak silently miss PostTest. Tab1 dynamic shape punya `LinkedGroupId` (int?) tapi TIDAK punya Post schedule — jadi:

    **Monitoring link (L261-263):** untuk row Pre-Post, ganti 1 link "Monitoring" jadi 2 link per-half ("Monitoring Pre" + "Monitoring Post") meniru `AssessmentMonitoring.cshtml:337-388`. Karena dynamic shape Tab1 tak punya Post schedule, dan endpoint `AssessmentMonitoringDetail` filter by `assessmentType`, kirim `scheduleDate=group.Schedule.Date` + `assessmentType="PreTest"` untuk Pre, dan `assessmentType="PostTest"` untuk Post. (Endpoint sudah filter `a.AssessmentType == assessmentType` DALAM grup yang sama title+category — verified L3163-3172; scheduleDate untuk Post = schedule rep, tapi filter AssessmentType yang membatasi ke Post session. CATATAN: bila PostTest beda tanggal dari rep, `scheduleDate` filter di endpoint bisa miss — gunakan `linkedGroupId` bila endpoint mendukung. Endpoint AssessmentMonitoringDetail saat ini filter BY scheduleDate.Date AND assessmentType; bila Post beda tanggal, ini tetap miss. AMAN: route Monitoring Pre/Post pakai `assessmentType` + scheduleDate rep HANYA bila Pre & Post same-date; untuk robust beda-tanggal, lihat catatan di bawah.)

    **Keputusan robust (hindari ambiguitas beda-tanggal):** Karena endpoint `AssessmentMonitoringDetail` masih filter `Schedule.Date == scheduleDate.Date` (verified — assessmentType ADALAH filter tambahan, bukan pengganti date filter), link per-half dengan scheduleDate rep AKAN miss PostTest yang beda tanggal. Maka untuk Monitoring link Pre-Post, render 2 link tapi:
    - Pre: `scheduleDate = group.Schedule.Date` + `assessmentType = "PreTest"` (PreTest = rep, same date — benar).
    - Post: render link "Monitoring Post" yang mengarah ke grup Monitoring via title+category TANPA single-date — tapi endpoint butuh scheduleDate. SOLUSI: tampilkan "Monitoring Post" hanya bila bisa diandalkan; bila tidak, fallback satu link "Monitoring" yang mengarah ke list `AssessmentMonitoring` (controller AssessmentAdmin) yang aman menampilkan kedua half. **Pilihan minimal-risk yang konsisten dengan prinsip D-01:** untuk row Pre-Post, ganti link "Monitoring" tunggal jadi link ke halaman list `AssessmentMonitoring` (`Url.Action("AssessmentMonitoring","AssessmentAdmin", new { search = (string)group.Title })`) yang merender grup Pre-Post lengkap dengan preDetailUrl/postDetailUrl (sudah benar L337-388). Ini menghindari single-date filter sepenuhnya dan reuse view yang SUDAH handle both-half.

    Implementasi (row Pre-Post, ganti `<li>` Monitoring L260-264):
    ```csharp
    @if ((bool)group.IsPrePostGroup)
    {
        <li>
            <a class="dropdown-item" href="@Url.Action("AssessmentMonitoring", "AssessmentAdmin", new { search = (string)group.Title })">
                <i class="bi bi-binoculars me-2"></i>Monitoring (Pre &amp; Post)
            </a>
        </li>
    }
    else
    {
        <li>
            <a class="dropdown-item" href="@Url.Action("AssessmentMonitoringDetail", "AssessmentAdmin", new { title = (string)group.Title, category = (string)group.Category, scheduleDate = ((DateTime)group.Schedule).Date.ToString("yyyy-MM-dd") })">
                <i class="bi bi-binoculars me-2"></i>Monitoring
            </a>
        </li>
    }
    ```

    **Export Excel (L278-280) + Bulk PDF (L283-285):** untuk row Pre-Post, tambah param `linkedGroupId = (int?)group.LinkedGroupId` ke `Url.Action`, sehingga endpoint (Task 2) query both-half. Untuk row standard, tetap kirim scheduleDate tanpa linkedGroupId. Pola:
    ```csharp
    <li>
        <a class="dropdown-item" href="@Url.Action("ExportAssessmentResults", "AssessmentAdmin", new { title = (string)group.Title, category = (string)group.Category, scheduleDate = ((DateTime)group.Schedule).Date.ToString("yyyy-MM-dd"), linkedGroupId = (bool)group.IsPrePostGroup ? (int?)group.LinkedGroupId : null })">
            <i class="bi bi-file-earmark-excel me-2"></i>Export Excel@((bool)group.IsPrePostGroup ? " (Pre & Post)" : "")
        </a>
    </li>
    <li>
        <a class="dropdown-item" href="@Url.Action("BulkExportPdf", "AssessmentAdmin", new { title = (string)group.Title, category = (string)group.Category, scheduleDate = ((DateTime)group.Schedule).Date.ToString("yyyy-MM-dd"), linkedGroupId = (bool)group.IsPrePostGroup ? (int?)group.LinkedGroupId : null })">
            <i class="bi bi-file-zip me-2"></i>Bulk Export PDF (ZIP)@((bool)group.IsPrePostGroup ? " (Pre & Post)" : "")
        </a>
    </li>
    ```
    CATATAN clutter (D-01 discretion): hindari tambah 6 tombol — cukup label sufiks "(Pre & Post)" pada row Pre-Post agar admin paham scope. Koord MAP-17/349 (Monitoring list token-required Pre-Post) — JANGAN over-reach ke MAP di sini.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - grep `Views/Admin/Shared/_AssessmentGroupsTab.cshtml` menemukan `linkedGroupId = (bool)group.IsPrePostGroup ? (int?)group.LinkedGroupId : null` (Export + PDF both-half wiring).
    - grep menemukan branch `@if ((bool)group.IsPrePostGroup)` di sekitar link Monitoring (per-half / list routing).
    - grep menemukan link `AssessmentMonitoring` (controller AssessmentAdmin) untuk row Pre-Post (menghindari single-date filter).
    - TIDAK ada link Pre-Post yang masih HANYA kirim `scheduleDate` tunggal ke ExportAssessmentResults/BulkExportPdf tanpa linkedGroupId.
    - `dotnet build` → "Build succeeded", 0 error (Razor compile).
  </acceptance_criteria>
  <done>Row Pre-Post: link Monitoring route ke list (both-half view) atau per-half; Export Excel + Bulk PDF kirim linkedGroupId (query both-half); row standard backward-compatible; sufiks "(Pre & Post)" sebagai cue; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → AssessmentAdmin endpoints | Input ter-authorize `[Authorize(Roles="Admin, HC")]`; param title/category/scheduleDate/linkedGroupId dari Url.Action server-generated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-01 | I (Info Disclosure) | ExportAssessmentResults/BulkExportPdf both-half via `linkedGroupId` | accept | Endpoint tetap `[Authorize(Roles="Admin, HC")]` (verified L4114/L4498). linkedGroupId hanya memperluas scope dari 1 half ke grup yang SAMA (Pre+Post LinkedGroupId), bukan ke grup lain — Admin/HC sudah berhak lihat seluruh grup. Tidak ada eskalasi data: peserta PostTest dalam grup yang sama sudah accessible via Monitoring. No new PII surface. |
| T-348-02 | T (Tampering) | linkedGroupId param dari query string | accept | linkedGroupId = int FK; bila attacker ubah ke grup lain, tetap dibatasi role Admin/HC (sudah berhak semua grup). Tidak ada IDOR cross-tenant (single-tenant internal app). EF parameterized query (no SQL injection). |
| T-348-03 | D (DoS) | BulkExportPdf both-half (Pre+Post bisa >50 peserta) | mitigate | DoS guard existing L4515 (`eligibleSessions.Count > 50 → BadRequest`) TETAP berlaku pada gabungan both-half. Admin diarahkan filter lebih spesifik. Tidak ada perubahan guard. |
| T-348-04 | E (Elevation) | RegenerateToken LinkedGroupId match | accept | RegenerateToken sudah `[Authorize]` (verified method scope). LinkedGroupId match hanya memilih siblings dalam grup yang sama (Pre+Post linked) — token baru di-apply ke grup yang memang satu kesatuan. Tidak ada cross-group token leak. |

Block-worthy: NONE. Semua threat = accept/mitigate dengan kontrol existing (authz role + DoS guard + EF parameterization). Phase ini correctness-fix surface admin existing, tanpa endpoint baru / input baru / authz baru.
</threat_model>

<verification>
- `dotnet build HcPortal.csproj -c Debug` → 0 error.
- Manual smoke (Plan 05 UAT): RegenerateToken pada grup Pre-Post → cek 2 session (Pre+Post) dapat token sama di DB (`SELECT Id, AssessmentType, AccessToken FROM AssessmentSessions WHERE LinkedGroupId = X`).
- Manual smoke: Export Excel + Bulk PDF row Pre-Post → file mencakup peserta PreTest DAN PostTest.
- Manual smoke: badge "X belum dinilai" muncul di Monitoring list untuk grup Pre-Post dengan essay PostTest pending.
</verification>

<success_criteria>
- MAM-01: RegenerateToken Pre-Post → PreTest + PostTest dapat token baru sama (walau beda tanggal); grup standard tak berubah.
- MAM-02: Link Monitoring + Export Excel + Bulk PDF row Pre-Post sadar LinkedGroupId / both-half; tidak silently miss PostTest.
- MAM-03: prePostGroups assign MenungguPenilaianCount dari postSubs → badge pending muncul untuk Pre-Post.
- Build 0 error; grup standard backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/348-manageassessment-monitoring-med-fix/348-01-SUMMARY.md`
</output>
