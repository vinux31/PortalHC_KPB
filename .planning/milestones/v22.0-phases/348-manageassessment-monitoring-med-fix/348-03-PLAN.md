---
phase: 348-manageassessment-monitoring-med-fix
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Controllers/TrainingAdminController.cs
  - Views/Admin/Shared/_TrainingRecordsTab.cshtml
autonomous: true
requirements: [MAM-06, MAM-07, MAM-08, MAM-09]
must_haves:
  truths:
    - "Tab Input Records first paint menampilkan empty-state 'Pilih filter' (tidak load seluruh roster) sampai admin benar-benar memfilter"
    - "Daftar pekerja Tab2 ter-paginate (Skip/Take + kontrol halaman) saat hasil banyak"
    - "Hapus Training/ManualAsm di Tab2 mempertahankan filter aktif (re-swap, tidak terlempar ke full-page reload)"
    - "Pasca-hapus, Tab2 tetap di state ter-filter (tidak balik ke empty-state)"
    - "Label filter status Tab2 berbunyi 'Status Training' (jujur: dipetakan ke CompletionPercentage training)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "isInitialState derive dari absennya filter (MAM-06) + pagination caller (MAM-07)"
      contains: "isInitialState ="
    - path: "Controllers/TrainingAdminController.cs"
      provides: "DeleteTraining/DeleteManualAssessment return partial Tab2 untuk HTMX (MAM-08)"
      contains: "ManageAssessmentTab_Training"
    - path: "Views/Admin/Shared/_TrainingRecordsTab.cshtml"
      provides: "Delete hx-post re-swap (MAM-08) + pagination markup (MAM-07) + label Status Training (MAM-09)"
      contains: "hx-post"
  key_links:
    - from: "ManageAssessmentTab_Training"
      to: "empty-state vs workers list"
      via: "isInitialState dari isFiltered + filter params"
      pattern: "isInitialState = string.IsNullOrEmpty"
    - from: "_TrainingRecordsTab delete button"
      to: "TrainingAdminController.DeleteTraining (HTMX)"
      via: "hx-post + hx-include #filterFormTraining + re-swap wrapper"
      pattern: "hx-post"
---

<objective>
Tutup 4 bug MED Tema C (Tab2 Input Records struktural): empty-state mati + full-roster-on-every-paint (MAM-06), pagination tak ada walau param diterima (MAM-07), delete full-page reload buang filter (MAM-08), label filter status menyesatkan (MAM-09). Koordinasi kritis: MAM-06 (isInitialState dari filter) + MAM-08 (post-delete re-swap harus jaga isInitialState=false).

Purpose: Admin/HC mengelola Input Records dengan performa & UX benar — load data hanya pasca-filter, paginate roster besar, hapus tanpa kehilangan konteks filter, label jujur.
Output: `ManageAssessmentTab_Training` (controller) + `DeleteTraining`/`DeleteManualAssessment` (controller) + `_TrainingRecordsTab.cshtml` (view).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-CONTEXT.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md
@docs/superpowers/specs/2026-06-04-manageassessment-monitoring-audit-design.md

PITFALL MAM-06 (WAJIB cek sebelum execute): @.planning/phases/322-filter-scope-per-tab-manage-assessment/322-UAT.md — UAT lama PASS dengan full-roster-on-load (FP-rejected "Reset full-roster by design"). Fix MAM-06 mengembalikan empty-state. Verifikasi parity Phase 287 commit fc161a18 (`git show fc161a18 --stat` bila perlu konfirmasi behavior asli). Pastikan fix TIDAK break expectation 322.

Project rules (./CLAUDE.md): Bahasa Indonesia; verifikasi lokal dotnet build+run. No migration (D-A).

<interfaces>
<!-- Diekstrak dari codebase 2026-06-04. -->

ManageAssessmentTab_Training (Controllers/AssessmentAdminController.cs:245-279):
  Signature: (string? search, int page = 1, int pageSize = 20, string? section, string? unit, string? category, string? statusFilter, string? isFiltered)
  L251: bool isInitialState = false; (HARDCODE — BUG MAM-06)
  L259-265: List<WorkerTrainingStatus> workers; if (isInitialState) workers = new(); else workers = await GetWorkersInSection(section, unit, category, search, statusFilter); ViewBag.Workers = workers;
  L274-276: log page= (BUG MAM-07: page tak dipakai paginate)
  ViewBag: IsInitialState, SelectedSection, SelectedUnit, SelectedCategory, SelectedStatus, SearchTerm, Workers, TrainingSections, TrainingUnits
  return PartialView("Shared/_TrainingRecordsTab", null);

GetWorkersInSection (Services/WorkerDataService.cs:242) — TANPA Skip/Take, banyak caller (CMP, history, export):
  REKOMENDASI PATTERNS: paginate di CALLER (controller), JANGAN ubah signature ini (ripple ke caller lain).
  Analog CMPController.cs:776: var pagedWorkerList = workerList.Skip(paging.Skip).Take(paging.Take).ToList();

PaginationHelper.Calculate (Helpers/PaginationHelper.cs:7):
  Calculate(int totalCount, int page, int pageSize) → PaginationResult(CurrentPage, TotalPages, TotalCount, Skip, Take)

Analog pagination markup Tab1 (Views/Admin/Shared/_AssessmentGroupsTab.cshtml:348-427):
  hx-get paginateAction + hx-vals page + hx-include="#filterFormAssessment" + hx-target="closest .htmx-tab-wrapper" + hx-swap="innerHTML"; struktur First/Prev/PageNumbers/Next/Last
  ViewBag header (_AssessmentGroupsTab.cshtml:1-9): currentPage/totalPages/totalCount dari ViewBag.

_TrainingRecordsTab.cshtml structure:
  filterFormTraining (L30) + hidden tab=training (L31) + hidden isFiltered=true (L32)
  isInitialState (L8): bool isInitialState = (bool)(ViewBag.IsInitialState ?? true);
  Branch: if (isInitialState) empty-state "Pilih filter" (L163-171); else if workers.Count==0 (L173-181); else workers table (L183-367)
  workers table closes L366 (</div> card); else block closes L367. Pagination footer ditambah SETELAH L366 (DALAM else, sebelum L367 }).
  Filter status label: L108 <label class="form-label fw-bold small">Status</label> (BUG MAM-09 — relabel "Status Training")
  Header tabel kolom SUDAH "Status Training" (L195) — JANGAN ubah (beda dari filter label).
  Delete forms: DeleteTraining full-page <form method=post> L327-334; DeleteManualAssessment <form method=post> L342-349. Keduanya: @Html.AntiForgeryToken() + hidden name="id" value="@row.Id" + button type=submit + onclick confirm.

DeleteTraining (Controllers/TrainingAdminController.cs:556-620): [HttpPost][ValidateAntiForgeryToken][Authorize(Roles="Admin, HC")]; class TrainingAdminController : AdminBaseController; redirect L586 (pre-check block) + L619 (success/end) RedirectToAction("ManageAssessment","AssessmentAdmin", new { tab="training" })
DeleteManualAssessment (Controllers/TrainingAdminController.cs:958-1017): [HttpPost][ValidateAntiForgeryToken][Authorize]; redirect L985 + L1016
AdminBaseController.IsAjaxRequest (Controllers/AdminBaseController.cs:32): => Request.Headers["X-Requested-With"] == "XMLHttpRequest"  (HTMX kirim "HX-Request", BUKAN XMLHttpRequest — perlu deteksi terpisah)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MAM-06 isInitialState dari filter + MAM-07 pagination caller & markup</name>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/Shared/_TrainingRecordsTab.cshtml</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:245-279 (ManageAssessmentTab_Training full method) — EDIT TARGET
    - .planning/phases/322-filter-scope-per-tab-manage-assessment/322-UAT.md (PITFALL: full-roster-on-load PASS lama — fix kembalikan empty-state, jangan break)
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-07" (analog CMP L776 paginate caller + markup Tab1 L348-427 + catatan paginate caller bukan service) + §"MAM-06" (adaptasi isInitialState dari isFiltered)
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml:1-9,348-427 (pola ViewBag header + markup pagination HTMX) — ANALOG VERBATIM
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml:1-9,163-367 (header block + branch empty-state/workers + tempat pagination footer) — EDIT TARGET
    - 348-CONTEXT.md §decisions MAM-06 + D-02 (MAM-07 = pagination ASLI, paginate di caller per PATTERNS)
  </read_first>
  <behavior>
    - Test 1 (MAM-06 helper, jika diekstrak): semua filter null/empty (isFiltered=null, section=null, unit=null, category=null, statusFilter=null, search=null) → isInitialState=true.
    - Test 2: isFiltered="true" (admin sudah interaksi) → isInitialState=false.
    - Test 3: salah satu filter terisi (mis. section="Refinery") tapi isFiltered=null → isInitialState=false.
  </behavior>
  <action>
    **MAM-06 — isInitialState derive (AssessmentAdminController.cs:251):**
    Ganti `bool isInitialState = false;` jadi turunan dari absennya filter:
    ```csharp
    // MAM-06: isInitialState true bila TIDAK ada filter aktif (skip full-roster query sampai admin filter).
    // isFiltered = hidden field "true" yang di-post saat user interaksi filter (_TrainingRecordsTab.cshtml:32).
    bool isInitialState = string.IsNullOrEmpty(isFiltered)
        && string.IsNullOrEmpty(section)
        && string.IsNullOrEmpty(unit)
        && string.IsNullOrEmpty(category)
        && string.IsNullOrEmpty(statusFilter)
        && string.IsNullOrEmpty(search);
    ```
    REKOMENDASI TDD: ekstrak ke static helper `IsTrainingInitialState(string? isFiltered, string? section, string? unit, string? category, string? statusFilter, string? search)` (static method di AssessmentAdminController atau helper class), panggil dari sini, tulis xUnit 3 test (Test 1-3) di HcPortal.Tests pola CertificateStatusTests Theory/Fact. Bila ekstraksi tak feasibel tanpa kompleksitas, inline + andalkan UAT (dokumentasikan).

    **MAM-07 — pagination di caller (AssessmentAdminController.cs:259-265):**
    Ganti blok `List<WorkerTrainingStatus> workers; if (isInitialState) ... ViewBag.Workers = workers;` (L259-265) dengan paginate di caller (pola CMPController.cs:776), JANGAN ubah GetWorkersInSection signature:
    ```csharp
    List<WorkerTrainingStatus> workers;
    if (isInitialState)
    {
        workers = new List<WorkerTrainingStatus>();
        ViewBag.CurrentPage = 1;
        ViewBag.TotalPages = 1;
        ViewBag.TotalCount = 0;
        ViewBag.PageSize = pageSize;
    }
    else
    {
        var fullList = await _workerDataService.GetWorkersInSection(section, unit, category, search, statusFilter);
        var pageSizeValidated = (pageSize == 20 || pageSize == 50 || pageSize == 100) ? pageSize : 20;
        var paging = HcPortal.Helpers.PaginationHelper.Calculate(fullList.Count, page, pageSizeValidated);
        workers = fullList.Skip(paging.Skip).Take(paging.Take).ToList();
        ViewBag.CurrentPage = paging.CurrentPage;
        ViewBag.TotalPages = paging.TotalPages;
        ViewBag.TotalCount = paging.TotalCount;
        ViewBag.PageSize = paging.Take;
    }
    ViewBag.Workers = workers;
    ```
    Param `page`/`pageSize` sekarang BENAR-BENAR dipakai. Biarkan log L274-276 apa adanya. JANGAN ubah ViewBag.IsInitialState assignment L252 (tetap set dari `isInitialState`).

    **MAM-07 — markup pagination Tab2 (_TrainingRecordsTab.cshtml):**
    1. Di header `@{ }` block (L1-9), tambah baca ViewBag pagination (pola _AssessmentGroupsTab.cshtml:1-9):
    ```csharp
    var currentPage = (int)(ViewBag.CurrentPage ?? 1);
    var totalPages = (int)(ViewBag.TotalPages ?? 1);
    var totalCount = (int)(ViewBag.TotalCount ?? 0);
    var pageSize = (int)(ViewBag.PageSize ?? 20);
    ```
    2. SETELAH closing table card `</div>` (L366, DALAM branch `else` workers-table, SEBELUM `}` penutup else L367), tambah pagination footer meniru _AssessmentGroupsTab.cshtml:348-427 dengan adaptasi:
       - `paginateAction = Url.Action("ManageAssessmentTab_Training", "AssessmentAdmin")` (BUKAN _Assessment)
       - `hx-include="#filterFormTraining"` (BUKAN #filterFormAssessment)
       - `hx-target="closest .htmx-tab-wrapper"`, `hx-swap="innerHTML"`
       - teks "Menampilkan @((currentPage - 1) * pageSize + 1) - @(Math.Min(currentPage * pageSize, totalCount)) dari @totalCount pekerja"
       - struktur First/Prev/PageNumbers(startPage=Max(1,currentPage-2)..endPage=Min(totalPages,currentPage+2))/Next/Last identik pola Tab1 (copy verbatim, ganti action + form id + teks "grup"→"pekerja").
       Render HANYA dalam `@if (totalPages > 1)`. Karena footer ada di branch `else` (workers.Count > 0, non-initial), otomatis tak muncul di empty-state/initial (koord MAM-06).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Controllers/AssessmentAdminController.cs` menemukan `isInitialState = string.IsNullOrEmpty(isFiltered)` (atau helper `IsTrainingInitialState`) — TIDAK lagi hardcode `bool isInitialState = false;`.
    - grep menemukan `PaginationHelper.Calculate` + `.Skip(paging.Skip).Take(paging.Take)` di ManageAssessmentTab_Training.
    - grep menemukan `ViewBag.CurrentPage = paging.CurrentPage` (pagination ViewBag set).
    - grep `Views/Admin/Shared/_TrainingRecordsTab.cshtml` menemukan `hx-include="#filterFormTraining"` di pagination footer + `Url.Action("ManageAssessmentTab_Training"` di paginateAction + teks "pekerja".
    - Bila helper diekstrak: xUnit baru 3 test (initial true/false) PASS.
    - `dotnet build` 0 error; `dotnet test` "Passed!".
  </acceptance_criteria>
  <done>isInitialState turun dari filter (empty-state hidup, skip full-roster); pagination di caller pakai PaginationHelper + markup HTMX Tab2 hx-include filterFormTraining; pagination tak muncul di initial; build+test PASS.</done>
</task>

<task type="auto">
  <name>Task 2: MAM-08 delete Training/ManualAsm hx-post re-swap preserve filter</name>
  <files>Controllers/TrainingAdminController.cs, Views/Admin/Shared/_TrainingRecordsTab.cshtml</files>
  <read_first>
    - Controllers/TrainingAdminController.cs:556-620 (DeleteTraining — redirect L586 pre-check + L619 success) — EDIT TARGET
    - Controllers/TrainingAdminController.cs:958-1017 (DeleteManualAssessment — redirect L985 + L1016) — EDIT TARGET
    - Controllers/AssessmentAdminController.cs:242-279 (ManageAssessmentTab_Training — action yang di-re-swap; TrainingAdmin perlu cara render partial ini) — REFERENSI
    - Controllers/AdminBaseController.cs:32 (IsAjaxRequest — HTMX butuh deteksi HX-Request) — REFERENSI
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml:327-349 (delete forms — EDIT TARGET) + L147-153 (Reset Filter hx-get pola) + L362-368 _AssessmentGroupsTab (pagination hx pola)
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-08" (catatan adaptasi kritis: konversi form→hx-post, antiforgery, handler return partial, KOORD MAM-06 jaga isInitialState=false)
    - 348-CONTEXT.md §decisions D-03 (hx-post re-swap, hx-include filter; KOORD MAM-06: pasca-delete isInitialState tetap false)
  </read_first>
  <action>
    **Strategi (D-03):** Konversi delete dari full-page `<form method=post>` jadi `hx-post` yang re-swap wrapper Tab2 + preserve filter via `hx-include="#filterFormTraining"`. Handler delete (TrainingAdmin) harus DETEKSI HTMX request dan return partial `_TrainingRecordsTab` (bukan redirect full-page).

    **Controller — DeleteTraining (TrainingAdminController.cs):**
    Karena delete handler ada di TrainingAdminController tapi partial Tab2 di-render oleh logic ManageAssessmentTab_Training (AssessmentAdminController), pendekatan minimal-risk: setelah delete sukses/gagal, bila request HTMX → re-render Tab2 partial dengan memanggil ulang data + return PartialView. Karena memanggil ManageAssessmentTab_Training cross-controller rumit, gunakan pendekatan: handler delete deteksi HTMX, lalu untuk HTMX path lakukan HTTP-302-free re-swap via header `HX-Location` ATAU re-render inline. PILIH pendekatan paling sederhana yang preserve filter:

    Pendekatan TERPILIH (HTMX redirect-after-post via HX-Redirect header tidak preserve filter form values). Maka: tambah deteksi HTMX di handler delete; bila HTMX, set TempData (Success/Error) lalu return `Content` kosong dengan response header `HX-Refresh: false` TIDAK cukup. SOLUSI ROBUST: handler delete bila HTMX → kembalikan trigger client-side re-fetch Tab2 dengan filter. Implementasi: tambahkan helper deteksi HTMX:
    ```csharp
    private bool IsHtmxRequest() => Request.Headers.ContainsKey("HX-Request");
    ```
    (Letakkan di TrainingAdminController, atau pakai AdminBaseController bila ingin reusable — tapi minimal tambah di TrainingAdminController.)

    Di DeleteTraining, ganti KEDUA `return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });` (L586 pre-check block + L619 akhir) dengan branching:
    ```csharp
    if (IsHtmxRequest())
    {
        // MAM-08: HTMX delete — re-swap Tab2 wrapper preserve filter.
        // Filter values di-include via hx-include="#filterFormTraining"; redirect ke partial action.
        Response.Headers["HX-Location"] = System.Text.Json.JsonSerializer.Serialize(new {
            path = Url.Action("ManageAssessmentTab_Training", "AssessmentAdmin"),
            target = "closest .htmx-tab-wrapper",
            select = (string?)null
        });
        return new EmptyResult();
    }
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    ```
    CATATAN: `HX-Location` GET tidak otomatis menyertakan filter form values (form ada di halaman yang ter-swap, bukan di request delete). Karena itu pendekatan HX-Location KEHILANGAN filter. **PENDEKATAN BENAR (preserve filter):** biarkan VIEW yang melakukan re-swap via `hx-post` + `hx-include`, dan handler delete cukup return PartialView Tab2. Tapi cross-controller render Tab2 rumit. **KEPUTUSAN FINAL (paling sederhana + benar):** handler delete TETAP redirect untuk non-HTMX; untuk HTMX, return `Ok()` kosong + set response header `HX-Trigger` dengan event custom `recordDeleted`. View mendengar event ini dan trigger re-fetch Tab2 dengan filter via `hx-get` + `hx-include`. Lihat bagian View.

    Revisi handler (KEDUA return di DeleteTraining + KEDUA return di DeleteManualAssessment):
    ```csharp
    if (IsHtmxRequest())
    {
        Response.Headers["HX-Trigger"] = "recordDeleted";
        return new EmptyResult();
    }
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    ```
    Terapkan identik di DeleteManualAssessment (L985 + L1016). TempData Success/Error tetap di-set sebelum return (untuk non-HTMX path; HTMX path akan refresh via event sehingga TempData tak ditampilkan — acceptable, atau tampilkan via toast — di luar scope minimal).

    **View — _TrainingRecordsTab.cshtml (L327-349):**
    Konversi kedua `<form method=post>` jadi tombol `hx-post`:
    ```html
    <button type="button" class="btn btn-sm btn-outline-danger" title="Hapus"
            hx-post="@Url.Action("DeleteTraining", "TrainingAdmin")"
            hx-vals='{"id": @row.Id}'
            hx-confirm="Hapus training record ini?"
            hx-swap="none">
        <i class="bi bi-trash"></i>
    </button>
    ```
    (Antiforgery untuk HTMX: pastikan token terkirim. Halaman ManageAssessment sudah punya `@Html.AntiForgeryToken()` global + pola JS fetch existing. Tambahkan `hx-headers='{"RequestVerificationToken": "..."}'` BILA token global tak otomatis ke-attach. CEK: apakah ada konfigurasi HTMX global antiforgery di ManageAssessment.cshtml / _Layout — bila `htmx.config` atau meta token sudah ada, cukup. Bila tidak, tambah `hx-headers` dengan token dari hidden field `__RequestVerificationToken`. Verifikasi saat execute via grep `RequestVerificationToken` di ManageAssessment.cshtml + _Layout.)

    Identik untuk DeleteManualAssessment button (L342-349): `hx-post="@Url.Action("DeleteManualAssessment", "TrainingAdmin")"` + `hx-confirm="Hapus assessment record ini?"`.

    **Re-fetch trigger pada event recordDeleted:** Tambah satu trigger element di view (mis. di dalam wrapper, setelah filter form atau di area workers) yang mendengar event dan re-swap Tab2 dengan filter:
    ```html
    <div hx-get="@Url.Action("ManageAssessmentTab_Training", "AssessmentAdmin")"
         hx-trigger="recordDeleted from:body"
         hx-include="#filterFormTraining"
         hx-target="closest .htmx-tab-wrapper"
         hx-swap="innerHTML"
         style="display:none;"></div>
    ```
    Letakkan element ini DALAM partial `_TrainingRecordsTab` (mis. tepat setelah `<form id="filterFormTraining">` close L157, atau di awal partial) agar ter-load setiap render. `hx-include="#filterFormTraining"` memastikan filter values + hidden `isFiltered=true` ikut → **KOORD MAM-06: re-fetch membawa isFiltered=true → isInitialState=false → tetap di state ter-filter, tidak balik ke empty-state.** Inilah yang menjamin truth "pasca-delete tetap ter-filter".
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Controllers/TrainingAdminController.cs` menemukan `IsHtmxRequest` (atau `HX-Request`) + `Response.Headers["HX-Trigger"] = "recordDeleted"` di DeleteTraining DAN DeleteManualAssessment (kedua return path tiap method).
    - grep menemukan fallback `RedirectToAction("ManageAssessment"` masih ada (non-HTMX path preserved).
    - grep `Views/Admin/Shared/_TrainingRecordsTab.cshtml` menemukan `hx-post="@Url.Action("DeleteTraining"` + `hx-post="@Url.Action("DeleteManualAssessment"` (form→button konversi).
    - grep menemukan `hx-trigger="recordDeleted from:body"` + `hx-include="#filterFormTraining"` (re-fetch element preserve filter).
    - grep menemukan `hx-confirm="Hapus training record ini?"` (konfirmasi dipertahankan).
    - TIDAK ada lagi `<form method="post" action="@Url.Action("DeleteTraining"` (full-page form dihapus).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Delete Training/ManualAsm via hx-post (HTMX) → handler set HX-Trigger recordDeleted; view re-fetch Tab2 dengan hx-include filterFormTraining (filter + isFiltered=true preserved → isInitialState tetap false); non-HTMX fallback redirect; konfirmasi dipertahankan; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: MAM-09 relabel filter status Tab2 → "Status Training"</name>
  <files>Views/Admin/Shared/_TrainingRecordsTab.cshtml</files>
  <read_first>
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml:106-125 (dropdown filter Status — label L108) — EDIT TARGET
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml:195 (header tabel kolom SUDAH "Status Training" — JANGAN ubah, referensi konsistensi)
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-09" (D-04 = relabel ONLY; JANGAN ubah WorkerDataService logic)
    - 348-CONTEXT.md §decisions D-04 (relabel "Status Training" saja; JANGAN fold passed manual-assessment — deferred)
  </read_first>
  <action>
    D-04 = opsi A relabel ONLY. Ubah label dropdown filter Status (_TrainingRecordsTab.cshtml:108):
    ```html
    <label class="form-label fw-bold small">Status</label>
    ```
    jadi:
    ```html
    <label class="form-label fw-bold small">Status Training</label>
    ```
    Ini menyamakan label filter dengan header kolom tabel (L195 sudah "Status Training") dan jujur bahwa filter dipetakan ke `CompletionPercentage` training-only (WorkerDataService.cs:393-398). JANGAN ubah opsi dropdown "Sudah"/"Belum" (L115-124) dan JANGAN sentuh `WorkerDataService.cs:360-398` logic (fold passed manual-assessment = deferred per CONTEXT). Koord MAP-19/349 (badge "Status Training") — di luar scope 348.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Views/Admin/Shared/_TrainingRecordsTab.cshtml` di sekitar L108 (dropdown filter, BUKAN header tabel) menemukan `<label class="form-label fw-bold small">Status Training</label>`.
    - WorkerDataService.cs TIDAK berubah (git diff kosong untuk file itu di task ini).
    - Opsi "Sudah"/"Belum" masih ada (L115-124 utuh).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Label filter dropdown Status → "Status Training" (jujur, konsisten dengan header kolom); opsi dropdown + WorkerDataService logic tak berubah; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → ManageAssessmentTab_Training / DeleteTraining / DeleteManualAssessment | Input ter-authorize `[Authorize(Roles="Admin, HC")]`; delete butuh antiforgery token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-08 | T (Tampering) / CSRF | DeleteTraining/DeleteManualAssessment via hx-post (HTMX) | mitigate | Handler tetap `[ValidateAntiForgeryToken]` (verified L557/959). Konversi form→hx-post WAJIB tetap mengirim antiforgery token via `hx-headers` RequestVerificationToken atau global HTMX antiforgery config. Acceptance criteria mengharuskan verifikasi token attach saat execute (grep RequestVerificationToken). Bila token tak terkirim, request ditolak 400 (fail-safe, bukan fail-open). |
| T-348-09 | E (Elevation) | ManageAssessmentTab_Training pagination params | accept | page/pageSize/filter params hanya mempengaruhi Skip/Take/filter pada query yang SUDAH ter-authorize Admin/HC. Tidak ada eskalasi: roster yang ditampilkan = roster yang memang berhak diakses HC (IsActive users). PaginationHelper.Calculate clamp page ke [1, totalPages] (verified L11) — no negative-skip / overflow. |
| T-348-10 | D (DoS) | GetWorkersInSection full-list sebelum Skip/Take (in-memory paginate) | accept | Paginate in-memory (caller, pola CMP existing) pada list yang sudah di-filter section/unit/category. MAM-06 gate query (load hanya pasca-filter) MENGURANGI beban (sebelumnya full-roster setiap paint). Untuk section refinery ratusan worker, in-memory paginate acceptable (sama dengan precedent CMPController.cs:776 yang sudah production). Optimasi SQL-side Skip/Take = backlog, bukan MED. |

Block-worthy: NONE. T-348-08 (CSRF) di-mitigate dengan `[ValidateAntiForgeryToken]` existing + kewajiban token attach pada hx-post (fail-safe). Tidak ada endpoint/authz baru; pagination + empty-state gate justru mengurangi beban.
</threat_model>

<verification>
- `dotnet build` 0 error + `dotnet test` PASS (xUnit MAM-06 helper bila diekstrak).
- Manual UAT (Plan 05): buka Tab Input Records → tampil empty-state "Pilih filter" (BUKAN full roster). Pilih section → roster muncul ter-paginate. Klik halaman 2 → filter tetap. Hapus 1 record → tetap di view ter-filter (tidak full-page reload, tidak balik empty-state).
- Regresi 322: verifikasi expectation UAT 322 tidak break (cek 322-UAT.md — bila ada step yang expect full-roster-on-load, koordinasikan; fix MAM-06 sengaja mengubah ke empty-state per spec MED).
- Label filter dropdown menampilkan "Status Training".
</verification>

<success_criteria>
- MAM-06: first paint empty-state, full-roster query skip sampai filter; parity 322 tak break.
- MAM-07: roster ter-paginate (Skip/Take + kontrol HTMX hx-include filter); pagination tak muncul di initial/empty.
- MAM-08: delete re-swap preserve filter (hx-post + HX-Trigger recordDeleted + re-fetch hx-include); pasca-delete isInitialState tetap false; antiforgery preserved.
- MAM-09: label filter "Status Training"; WorkerDataService logic tak berubah.
- Build + test PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/348-manageassessment-monitoring-med-fix/348-03-SUMMARY.md`
</output>
