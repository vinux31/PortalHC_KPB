---
phase: 348-manageassessment-monitoring-med-fix
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - HcPortal.Tests/ManageAssessmentMedFixTests.cs
  - .planning/phases/348-manageassessment-monitoring-med-fix/348-UAT.md
autonomous: false
requirements: [MAM-01, MAM-02, MAM-03, MAM-04, MAM-05, MAM-06, MAM-07, MAM-08, MAM-09, MAM-10, MAM-11, MAM-12, MAM-13]
must_haves:
  truths:
    - "dotnet build seluruh solution 0 error"
    - "xUnit HcPortal.Tests PASS (termasuk test logic-bearing MAM-04 status derivation, MAM-06 isInitialState, MAM-07 pagination bila helper diekstrak)"
    - "5 Success Criteria ROADMAP Phase 348 terverifikasi via Playwright UAT per surface + cross-check DB"
  artifacts:
    - path: "HcPortal.Tests/ManageAssessmentMedFixTests.cs"
      provides: "xUnit logic-bearing untuk MAM-04/06/07 (status derivation, isInitialState, pagination clamp)"
      contains: "[Fact]"
    - path: ".planning/phases/348-manageassessment-monitoring-med-fix/348-UAT.md"
      provides: "UAT checklist 5 SC Bahasa Indonesia (per surface) + hasil"
      contains: "MAM-"
  key_links:
    - from: "Plan 05 verify gate"
      to: "5 Success Criteria ROADMAP Phase 348"
      via: "Playwright UAT + xUnit + dotnet build"
      pattern: "Success Criteria"
---

<objective>
Verify gate Phase 348: pastikan 13 MAM fix (Plan 01-04) memenuhi 5 Success Criteria ROADMAP — `dotnet build` 0 error, xUnit PASS (logic-bearing), Playwright UAT per surface + cross-check DB. Checkpoint human-verify untuk UAT visual yang tak ter-automasi penuh (live SignalR, reshuffle spinner, empty-state, badge match).

NO migration (D-A). Bila perlu seed data untuk UAT (mis. grup Pre-Post + essay-pending), ikuti SEED_WORKFLOW: klasifikasi → snapshot DB lokal → catat SEED_JOURNAL → restore setelah selesai.

Purpose: Gate kualitas sebelum phase ditandai complete; bukti tiap SC terpenuhi.
Output: `ManageAssessmentMedFixTests.cs` (xUnit) + `348-UAT.md` (checklist + hasil).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-CONTEXT.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md

Project rules (./CLAUDE.md): Bahasa Indonesia; verifikasi lokal `dotnet build` + `dotnet run` (http://localhost:5277, WAJIB `ASPNETCORE_ENVIRONMENT=Development`); SEED_WORKFLOW untuk seed temporary. Dev creds: admin `admin@pertamina.com` pwd `123456`, DB `HcPortalDB_Dev`.

5 Success Criteria Phase 348 (ROADMAP L810-815):
1. Pre-Post: RegenerateToken + link Monitoring/Export by LinkedGroupId; badge "X belum dinilai" muncul (MAM-01/02/03).
2. Essay PendingGrading tak salah "Completed" di Monitoring Detail (server + live SignalR); CompletedCount/passRate benar (MAM-04/05).
3. Tab2: empty-state hidup (skip full-roster), pagination/param benar, delete preserve filter, Status filter jujur (MAM-06/07/08/09).
4. Badge status Tab1 = GroupStatus (match filter); dropdown Kategori Monitoring data-driven (buang "Proton"); tooltip Closed jujur; Reshuffle selector scoped (MAM-10/11/12/13).
5. `dotnet build` 0 error + xUnit + Playwright UAT per surface.

<interfaces>
<!-- Test infra existing (2026-06-04). -->
HcPortal.Tests project: xUnit. Pola pure-logic: CertificateStatusTests.cs (Theory/Fact), AssessmentHistoryStatsTests.cs, WorkerDataServiceSearchTests.cs.
Run: dotnet test HcPortal.Tests/HcPortal.Tests.csproj
Playwright: tests/e2e/ (helpers wizardSelectors.ts dll). Run via npx playwright test. UAT MCP per user (Claude jalankan via Playwright MCP — pola Phase 344/346).

Helper logic-bearing yang MUNGKIN diekstrak di Plan 02/03 (verifikasi keberadaannya saat execute):
- DeriveUserStatus(status, completedAt, startedAt) [MAM-04] — AssessmentAdminController atau helper
- IsTrainingInitialState(isFiltered, section, unit, category, statusFilter, search) [MAM-06]
Bila helper TIDAK diekstrak (inline), test SC tetap via Playwright UAT + grep acceptance (Plan 01-04 sudah grep-verify); xUnit fokus ke helper yang ADA + PaginationHelper.Calculate clamp.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: xUnit logic-bearing MAM-04/06/07 + build/test gate</name>
  <files>HcPortal.Tests/ManageAssessmentMedFixTests.cs</files>
  <read_first>
    - HcPortal.Tests/CertificateStatusTests.cs (pola Theory/Fact pure-logic) — POLA
    - HcPortal.Tests/AssessmentHistoryStatsTests.cs (pola stats test) — POLA
    - Helpers/PaginationHelper.cs:7-12 (Calculate clamp logic — testable langsung) — TARGET TEST
    - 348-02-PLAN.md Task 1 (apakah DeriveUserStatus diekstrak) + 348-03-PLAN.md Task 1 (apakah IsTrainingInitialState diekstrak) — CEK keberadaan helper
    - 348-PATTERNS.md §"MAM-04" + §"MAM-07" (logic yang di-test)
  </read_first>
  <action>
    Buat `HcPortal.Tests/ManageAssessmentMedFixTests.cs` dengan test pure-logic untuk MAM yang logic-bearing. Cakupan WAJIB (sesuaikan dengan helper yang benar-benar ada — Plan 02/03 mungkin ekstrak atau inline):

    1. **PaginationHelper.Calculate (MAM-07, SELALU bisa di-test — helper sudah ada):**
       - Theory: totalCount=0/page=1/pageSize=20 → CurrentPage=1, TotalPages=0, Skip=0.
       - Theory: totalCount=45/page=2/pageSize=20 → CurrentPage=2, Skip=20, Take=20, TotalPages=3.
       - Theory: totalCount=45/page=99 (overflow) → CurrentPage clamp ke TotalPages (3), Skip valid (tidak negatif/overflow).
       - Theory: totalCount=45/page=0 atau negatif → CurrentPage clamp ke 1, Skip=0.

    2. **DeriveUserStatus (MAM-04) — HANYA bila Plan 02 mengekstrak helper:**
       - Fact: status=PendingGrading, completedAt=now, startedAt=now → "Menunggu Penilaian" (BUKAN "Completed").
       - Fact: status="Completed", completedAt=now → "Completed".
       - Fact: status="Cancelled" → "Dibatalkan"; startedAt!=null & completedAt=null → "InProgress"; semua null → "Not started".
       Bila helper TIDAK diekstrak (inline di Select), SKIP test ini + catat di SUMMARY bahwa MAM-04 di-cover via Playwright UAT (Task 2 SC2).

    3. **IsTrainingInitialState (MAM-06) — HANYA bila Plan 03 mengekstrak helper:**
       - Theory: semua arg null/empty → true.
       - Theory: isFiltered="true" → false. Theory: section="X" (lainnya null) → false.
       Bila helper TIDAK diekstrak, SKIP + catat di SUMMARY (cover via UAT SC3).

    Pakai konstanta `AssessmentConstants.AssessmentStatus.PendingGrading` di test (bukan literal). Bila helper inline-only, minimal test PaginationHelper (poin 1) WAJIB ada + dokumentasikan di SUMMARY mana yang via xUnit vs Playwright.

    Jalankan gate: `dotnet build` (seluruh solution) 0 error + `dotnet test HcPortal.Tests` semua PASS. Bila gagal, perbaiki/koordinasi sebelum lanjut.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/ManageAssessmentMedFixTests.cs` ada dengan minimal test PaginationHelper.Calculate (4 case termasuk overflow/underflow clamp).
    - Bila helper DeriveUserStatus / IsTrainingInitialState diekstrak: test untuk masing-masing ADA dan PASS (termasuk Fact essay-pending → "Menunggu Penilaian").
    - grep test file menemukan `AssessmentConstants.AssessmentStatus.PendingGrading` (bila MAM-04 di-test) — konstanta, bukan literal.
    - `dotnet build` → "Build succeeded" 0 error.
    - `dotnet test` → "Passed!" tanpa "Failed!".
  </acceptance_criteria>
  <done>xUnit ManageAssessmentMedFixTests dengan PaginationHelper clamp (+ DeriveUserStatus/IsTrainingInitialState bila helper ada); build+test PASS; cakupan via xUnit vs Playwright didokumentasikan.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright UAT per surface (SC1-4) + tulis 348-UAT.md</name>
  <files>.planning/phases/348-manageassessment-monitoring-med-fix/348-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md L810-815 (5 Success Criteria Phase 348) — SUMBER UAT
    - .planning/phases/346-cmp-records-detail-search-logic/346-UAT.md (pola UAT MCP Playwright Phase v22.0) — POLA bila ada
    - docs/SEED_WORKFLOW.md (bila butuh seed grup Pre-Post + essay-pending) — WAJIB ikuti bila seed
    - 348-CONTEXT.md §specifics (Dev creds + ASPNETCORE_ENVIRONMENT=Development + localhost:5277)
  </read_first>
  <action>
    Tulis `.planning/phases/348-manageassessment-monitoring-med-fix/348-UAT.md` — checklist Bahasa Indonesia memetakan 5 SC ke langkah UAT konkret per surface, lalu eksekusi via Playwright MCP (login admin `admin@pertamina.com` / `123456`, app `ASPNETCORE_ENVIRONMENT=Development` di http://localhost:5277). Bila data UAT tak tersedia (mis. tak ada grup Pre-Post / essay-pending), buat seed temporary per SEED_WORKFLOW (snapshot DB → insert → catat journal → restore setelah).

    **Struktur UAT (per SC, masing-masing langkah + ekspektasi + hasil PASS/FAIL):**

    **SC1 — Pre-Post (MAM-01/02/03):**
    - Buka /Admin/ManageAssessment Tab Assessment Groups → cari grup Pre-Post. Klik dropdown Aksi → verifikasi link Monitoring mengarah ke list (both-half) + Export/Bulk PDF berlabel "(Pre & Post)".
    - RegenerateToken pada grup Pre-Post (via Monitoring atau endpoint) → cross-check DB: `SELECT Id, AssessmentType, AccessToken FROM AssessmentSessions WHERE LinkedGroupId = X` → PreTest & PostTest punya AccessToken SAMA (walau Schedule beda tanggal).
    - Export Excel grup Pre-Post → file berisi peserta PreTest DAN PostTest (bukan PreTest saja).
    - Buka /Admin/AssessmentMonitoring → grup Pre-Post dengan essay PostTest pending → badge "X belum dinilai" muncul.

    **SC2 — Essay PendingGrading (MAM-04/05):**
    - Assessment ber-essay, worker submit (belum dinilai HC). Buka Monitoring Detail per-peserta → kolom Status = "Menunggu Penilaian" (bukan "Completed" hijau). CompletedCount TIDAK menghitung essay-pending.
    - Live SignalR: buka Monitoring Detail di 1 tab (HC), worker submit essay di tab lain → row update real-time ke badge "Menunggu Penilaian" + result "—" (bukan Pass/Fail). [checkpoint visual]
    - Regresi: worker submit non-essay (MC/MA) → tetap "Completed" + Pass/Fail benar.

    **SC3 — Tab2 (MAM-06/07/08/09):**
    - Buka Tab Input Records (first paint) → empty-state "Pilih filter" muncul (BUKAN full roster). [verifikasi network: GetWorkersInSection tak ke-query saat initial — opsional via log].
    - Pilih section → roster muncul ter-paginate (kontrol halaman bila >pageSize). Klik halaman 2 → filter section tetap.
    - Hapus 1 training record → tetap di view ter-filter (tidak full-page reload, tidak balik empty-state). Cross-check DB record terhapus.
    - Label filter dropdown = "Status Training".

    **SC4 — Display/Monitoring/detail (MAM-10/11/12/13):**
    - Tab1: filter "Open" → semua baris berlabel "Open" (bukan "Completed/Abandoned"). Filter "Semua Status" → grup Closed berlabel "Closed".
    - Monitoring list dropdown Kategori → berisi kategori real dari DB, TANPA "Proton" phantom. (Cross-check: bandingkan dengan `SELECT Name FROM AssessmentCategories WHERE IsActive=1 ORDER BY SortOrder`.)
    - Hover badge Closed (kedua surface Tab1 + Monitoring) → tooltip tak menyebut "lokasi".
    - Monitoring Detail: klik Reshuffle pada worker Not started/Abandoned → spinner HANYA di tombol, baris utuh; reshuffle sukses. [checkpoint visual]

    Catat hasil tiap langkah (PASS/FAIL + screenshot bila relevan). Bila ada seed temporary, pastikan RESTORE DB + tandai SEED_JOURNAL `cleaned` setelah UAT.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; Test-Path ".planning/phases/348-manageassessment-monitoring-med-fix/348-UAT.md"</automated>
  </verify>
  <acceptance_criteria>
    - `348-UAT.md` ada, memetakan 5 SC ke langkah konkret per surface (MAM-01..13 ter-referensi).
    - Setiap SC punya hasil PASS/FAIL terisi (bukan template kosong).
    - SC1 menyertakan cross-check DB AccessToken Pre+Post sama + Export both-half.
    - SC2 menyertakan verifikasi "Menunggu Penilaian" (server + live SignalR) + regresi non-essay.
    - SC3 menyertakan empty-state + pagination + delete-preserve-filter + label.
    - SC4 menyertakan badge GroupStatus + dropdown data-driven (no Proton) + tooltip + reshuffle.
    - Bila seed temporary dipakai: SEED_JOURNAL entry ada + DB di-restore (journal `cleaned`).
  </acceptance_criteria>
  <done>348-UAT.md lengkap 5 SC per surface dengan hasil; UAT dieksekusi via Playwright MCP; DB cross-check; seed (bila ada) di-restore per SEED_WORKFLOW.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — 5 SC visual (SignalR, spinner, empty-state, badge)</name>
  <what-built>
    13 MAM correctness fix (Plan 01-04) + xUnit (Plan 05 Task 1) + Playwright UAT 5 SC (Plan 05 Task 2). Build 0 error + test PASS. UAT per surface dieksekusi Claude via Playwright MCP dengan cross-check DB.
  </what-built>
  <how-to-verify>
    Konfirmasi visual untuk item yang tak sepenuhnya ter-automasi (live SignalR, spinner, empty-state, badge match). Jalankan app: `dotnet run` dengan `ASPNETCORE_ENVIRONMENT=Development`, buka http://localhost:5277, login admin `admin@pertamina.com` / `123456`.

    1. **SC2 live SignalR (visual):** buka /Admin/AssessmentMonitoring → grup ber-essay → Detail. Di tab lain, worker submit assessment essay. Verifikasi row HC update real-time ke badge kuning "Menunggu Penilaian" + result "—" (BUKAN hijau "Completed" + Pass/Fail).
    2. **SC3 empty-state (visual):** buka Tab Input Records → harus muncul "Pilih filter untuk menampilkan data pekerja" (BUKAN langsung roster). Pilih section → roster muncul ter-paginate. Hapus 1 record → tetap di view ter-filter.
    3. **SC4 badge + reshuffle (visual):** Tab Assessment Groups filter "Open" → semua badge "Open". Monitoring Detail → klik Reshuffle worker Not started → spinner HANYA di tombol (baris utuh).
    4. **SC1 Pre-Post (visual + opsional DB):** grup Pre-Post → RegenerateToken → konfirmasi token PreTest & PostTest sama (via UI atau DB query di 348-UAT.md).

    Review `.planning/phases/348-manageassessment-monitoring-med-fix/348-UAT.md` untuk hasil lengkap 5 SC.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila 5 SC terverifikasi, atau jelaskan surface yang gagal (mis. "SC2 SignalR masih Completed") untuk gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test/UAT harness → app lokal | xUnit pure-logic (no I/O sensitif); Playwright UAT pakai Dev creds lokal (bukan staging/prod); seed temporary lokal-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-14 | I (Info Disclosure) | Dev creds di 348-UAT.md / seed data | accept | Creds = Dev lokal (`admin@pertamina.com`/`123456`, DB HcPortalDB_Dev) bukan staging/prod (reference_dev_credentials memory). 348-UAT.md di .planning/ (internal, tidak ter-deploy). Seed temporary lokal-only + di-restore per SEED_WORKFLOW (tak nempel). |
| T-348-15 | T (Tampering) | Seed data temporary di DB lokal | mitigate | SEED_WORKFLOW WAJIB: snapshot DB sebelum insert → SEED_JOURNAL entry → RESTORE setelah UAT (sukses atau gagal) → tandai `cleaned`. Tidak ada promosi seed temporary ke permanent tanpa review. |

Block-worthy: NONE. Plan verify-only — tidak mengubah production code/authz. Seed temporary dikelola via SEED_WORKFLOW (snapshot+restore). Tidak ada surface baru.
</threat_model>

<verification>
- `dotnet build` 0 error + `dotnet test HcPortal.Tests` PASS.
- `348-UAT.md` ada + 5 SC terisi hasil.
- Checkpoint human-verify di-approve (5 SC visual terverifikasi).
</verification>

<success_criteria>
- SC5: build 0 error + xUnit PASS + Playwright UAT per surface dieksekusi.
- SC1-4: terverifikasi via UAT (cross-check DB untuk token/export/category) + checkpoint visual (SignalR/spinner/empty-state/badge).
- Seed temporary (bila ada) di-restore; SEED_JOURNAL `cleaned`.
- Phase 348 siap ditandai complete (atau gap-closure bila ada SC gagal).
</success_criteria>

<output>
After completion, create `.planning/phases/348-manageassessment-monitoring-med-fix/348-05-SUMMARY.md`
</output>
