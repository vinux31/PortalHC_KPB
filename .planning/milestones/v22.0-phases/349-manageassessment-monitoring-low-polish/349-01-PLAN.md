---
phase: 349-manageassessment-monitoring-low-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/Shared/_HistoryTab.cshtml
  - Views/Admin/ManageAssessment.cshtml
autonomous: true
requirements: [MAP-02, MAP-04, MAP-07, MAP-08, MAP-09, MAP-20]
must_haves:
  truths:
    - "Tab3 History sub-tab Training memakai label 'NIP' (bukan 'Nopeg') di header + placeholder"
    - "Baris Riwayat Assessment Tab3 tidak lagi nested-interactive (tr role=link membungkus a Lihat) — hanya tombol Lihat affordance"
    - "Filter client Tab3 yang menghasilkan 0 baris menampilkan pesan 'Tidak ada hasil untuk filter ini.' (aria-live polite)"
    - "Tab3 menampilkan counter 'Menampilkan X dari Y' yang ikut filter aktif"
    - "Skeleton loader Tab2 Training + Tab3 History punya jumlah filter & kolom identik dengan tabel asli (zero layout-shift)"
    - "Tab3 History cell Pass/Fail tetap menampilkan badge 'Menunggu Penilaian' (Phase 346 REC-07) tanpa duplikat"
  artifacts:
    - path: "Views/Admin/Shared/_HistoryTab.cshtml"
      provides: "i18n NIP + drop ARIA nested-interactive + counter Menampilkan X dari Y + 0-match container"
      contains: "NIP"
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "MAP-07 0-match injection JS + MAP-08 counter update JS + MAP-09 skeleton kolom-match"
      contains: "Tidak ada hasil untuk filter ini"
  key_links:
    - from: "ManageAssessment.cshtml filterAssessmentRows()/filterTrainingRows()"
      to: "_HistoryTab.cshtml #assessmentNoResult / #assessmentVisibleCount"
      via: "getElementById toggle display + textContent"
      pattern: "getElementById\\('assessment(NoResult|VisibleCount)'\\)"
---

<objective>
Polish Tab3 History (sub-tab Riwayat Assessment + Riwayat Training) di halaman ManageAssessment: i18n label identitas "NIP", buang ARIA nested-interactive, empty-state/feedback filter client (pesan 0-match + counter "Menampilkan X dari Y"), dan samakan skeleton loader dengan kolom tabel asli. Plus verifikasi no-op MAP-20 (badge pending sudah ada dari Phase 346).

Purpose: Tutup 6 item LOW polish (MAP-02/04/07/08/09/20) yang semuanya berada di file partisi Tab3 (`_HistoryTab.cshtml`) + host page (`ManageAssessment.cshtml` untuk client-filter JS & skeleton). Plan ini controller-free → Wave 1, tidak konflik dengan plan controller berikutnya.
Output: `_HistoryTab.cshtml` + `ManageAssessment.cshtml` ter-polish; build 0 error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/349-manageassessment-monitoring-low-polish/349-CONTEXT.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-RESEARCH.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Konstanta status (D-C) — sudah ter-render di _HistoryTab.cshtml L102-106 (Phase 346 REC-07). -->
Dari Models/AssessmentConstants.cs:
```csharp
public const string PendingGrading = "Menunggu Penilaian"; // AssessmentConstants.AssessmentStatus.PendingGrading
```
<!-- MAP-07 client-filter functions berada di HOST page (ManageAssessment.cshtml), BUKAN partial.
     Markup pesan 0-match + counter HARUS di-render server-side di _HistoryTab.cshtml; JS hanya toggle display. -->
Dari ManageAssessment.cshtml (host page, @section Scripts):
- function filterAssessmentRows()  // L316-327 — loop row Riwayat Assessment, set display none/''
- function filterTrainingRows()    // L330-337 — loop row Riwayat Training
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n NIP (MAP-02) + drop ARIA nested-interactive Tab3 (MAP-04) + verify badge pending no-op (MAP-20)</name>
  <read_first>
    - Views/Admin/Shared/_HistoryTab.cshtml (FILE penuh — basis pasca-348)
    - 349-RESEARCH.md §phase_requirements (baris MAP-02/04/20) + §Pattern 3 (drop ARIA) + Pitfall 3 (MAP-20 sudah done)
    - 349-UI-SPEC.md §Copywriting (i18n label identitas MAP-02) + §A11y Contract (drop ARIA nested-interactive MAP-04)
  </read_first>
  <action>
    Re-Grep dulu sebelum edit (line drift — Pitfall 1). Target pasca-348:

    **MAP-02 (NIP):** Di `_HistoryTab.cshtml` sub-tab Training:
    - Header kolom Training (~L178): ganti teks `Nopeg` → `NIP`.
    - Placeholder search Training (~L159): ganti `Nopeg` → `NIP`. String LOCK placeholder: `Cari nama atau NIP...`.
    - Sub-tab Assessment (header ~L62 + placeholder ~L33) SUDAH "NIP" → verify saja, no edit.

    **MAP-04 (drop ARIA nested-interactive):** Di sub-tab Riwayat Assessment (~L78-81 `<tr>` drill-down):
    - Hapus atribut `role="link"`, `tabindex="0"`, `aria-label="..."`, dan blok `Html.Raw(...)` (yang inject data-href/role/tabindex) dari `<tr class="assessment-history-row ...">`. Sisakan `<tr class="assessment-history-row">` polos (tetap boleh ada class untuk styling non-interaktif, tapi drop `cil03-row-link` interactive class).
    - PERTAHANKAN tombol/link `<a href="/CMP/Results/...">Lihat</a>` (~L112) sebagai satu-satunya affordance interaktif.
    - Hapus handler JS click/keydown row-navigation (~L132-147) di dalam `<script>` partial + CSS dead `tr.cil03-row-link` (~L127-131). Kalau tidak dibuang, row tetap clickable tanpa role (UX inkonsisten + dead code).
    - Bonus security: drop `Html.Raw` mengurangi surface XSS (sejalan konvensi Phase 347).

    **MAP-20 (no-op verify):** Konfirmasi cell Pass/Fail (~L102-106) SUDAH render badge `@AssessmentConstants.AssessmentStatus.PendingGrading` saat `IsPassed==null && Status==PendingGrading` (Phase 346 REC-07). JANGAN tambah badge kedua (D-D). Bila ditemukan badge ganda → hapus duplikat. Bila hanya 1 → no edit (cukup verifikasi).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `_HistoryTab.cshtml` mengandung `NIP` di header & placeholder Training; TIDAK mengandung `Nopeg`
    - `_HistoryTab.cshtml` `<tr ...assessment-history-row...>` TIDAK mengandung `role="link"`, `tabindex="0"`, maupun `Html.Raw`
    - `_HistoryTab.cshtml` masih mengandung `>Lihat<` (tombol affordance dipertahankan)
    - `_HistoryTab.cshtml` TIDAK mengandung `cil03-row-link` lagi (CSS + class dead dibuang)
    - `_HistoryTab.cshtml` mengandung tepat SATU badge `@AssessmentConstants.AssessmentStatus.PendingGrading` di cell Pass/Fail (no duplikat — MAP-20)
  </acceptance_criteria>
  <done>Training pakai "NIP"; Tab3 Assessment drill-down bukan nested-interactive (hanya tombol Lihat); badge pending tetap single (REC-07); build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Empty-state/feedback filter client Tab3 (MAP-07 + MAP-08) — markup partial + JS host</name>
  <read_first>
    - Views/Admin/Shared/_HistoryTab.cshtml (badge count statis L13,L20 + thead Assessment L60-70 + thead Training L176-182)
    - Views/Admin/ManageAssessment.cshtml (filterAssessmentRows L316-327 + filterTrainingRows L330-337 di @section Scripts)
    - 349-RESEARCH.md §Pattern 4 (client-filter 0-match injection) + Pitfall 9 (HTMX re-swap host vs partial JS)
    - 349-UI-SPEC.md §Empty-state & feedback copy (string LOCK MAP-07/08)
  </read_first>
  <action>
    **Markup server-side di `_HistoryTab.cshtml`** (di-render sekali, JS hanya toggle display):

    Untuk SUB-TAB Riwayat Assessment:
    - Tambah baris/container counter visible (LOCK awal pakai total): `<div id="assessmentVisibleCount" class="text-muted small mb-2">Menampilkan @assessmentRows.Count dari @assessmentRows.Count</div>` (sesuaikan nama koleksi yang ada di view; jika tidak ada variabel koleksi, pakai `.Count` dari `ViewBag.AssessmentHistory`).
    - Tambah baris 0-match (di dalam/sesudah `<tbody>` atau sebagai elemen sibling tabel), hidden default:
      `<div id="assessmentNoResult" class="text-center py-4 text-muted" style="display:none;" aria-live="polite">Tidak ada hasil untuk filter ini.</div>`

    Untuk SUB-TAB Riwayat Training: ulangi dengan id `trainingVisibleCount` + `trainingNoResult` (string LOCK identik).

    **JS di HOST `ManageAssessment.cshtml`** (extend `filterAssessmentRows()` + `filterTrainingRows()` — JANGAN pindahkan ke partial, Pitfall 9):
    Di akhir tiap fungsi filter, setelah loop set display row, tambah (verbatim pola RESEARCH §Pattern 4):
    ```javascript
    var visible = 0;
    rows.forEach(function(row){ if (row.style.display !== 'none') visible++; });
    var counter = document.getElementById('assessmentVisibleCount');
    if (counter) counter.textContent = 'Menampilkan ' + visible + ' dari ' + rows.length;
    var noResult = document.getElementById('assessmentNoResult');
    if (noResult) noResult.style.display = (visible === 0) ? '' : 'none';
    ```
    Sesuaikan id (`assessment*` vs `training*`) per fungsi + variabel `rows` sesuai nama koleksi row yang sudah dipakai di tiap fungsi. String "Menampilkan X dari Y" + "Tidak ada hasil untuk filter ini." WAJIB persis (case-sensitive, UI-SPEC LOCK).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `_HistoryTab.cshtml` mengandung `id="assessmentNoResult"` + `id="trainingNoResult"` keduanya dengan `aria-live="polite"` dan teks `Tidak ada hasil untuk filter ini.`
    - `_HistoryTab.cshtml` mengandung `id="assessmentVisibleCount"` + `id="trainingVisibleCount"` dengan teks awal `Menampilkan`
    - `ManageAssessment.cshtml` mengandung `getElementById('assessmentNoResult')` dan `getElementById('trainingNoResult')`
    - `ManageAssessment.cshtml` mengandung string `'Menampilkan ' + visible + ' dari ' + rows.length` (atau ekuivalen untuk training)
  </acceptance_criteria>
  <done>Filter Tab3 0-match menampilkan pesan + counter "Menampilkan X dari Y" yang ikut filter; container 0-match aria-live polite; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: Skeleton loader match kolom asli (MAP-09)</name>
  <read_first>
    - Views/Admin/ManageAssessment.cshtml (skeleton Tab2 L146-165 + skeleton History L186-198)
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml (thead L206-214 = 7 kolom; filter L42-163 = 5 filter)
    - Views/Admin/Shared/_HistoryTab.cshtml (thead Assessment L60-70 = 8 kolom; thead Training L176-182 = 5 kolom)
    - 349-RESEARCH.md Pitfall 6 (skeleton column-count mismatch) + 349-UI-SPEC.md §Skeleton Loader Contract
  </read_first>
  <action>
    Re-Grep thead aktual dulu (hitung kolom REAL, bukan dari spec line-drift — Pitfall 6). Target jumlah dari UI-SPEC §Skeleton Loader Contract:

    **Skeleton Tab2 Training** (`ManageAssessment.cshtml` ~L149,156-165):
    - Filter placeholder: ASLI 5 (section / category / unit / status / search). Sesuaikan skeleton dari 4 → 5 placeholder filter.
    - Kolom placeholder tabel: ASLI 7 (No / Nama / NIP / Jabatan / Unit / Status Training / Aksi). Sesuaikan dari 5 → 7 kolom placeholder per baris skeleton.

    **Skeleton Tab3 History** (`ManageAssessment.cshtml` ~L186-198):
    - Filter: 2 (sudah OK).
    - Kolom placeholder: Assessment ASLI 8 kolom; Training ASLI 5 kolom. Sesuaikan baris skeleton agar tidak lebih pendek dari tabel asli (gunakan 8 kolom untuk skeleton Assessment; jika satu skeleton dipakai bersama, pakai 8 sebagai max agar tak under-shoot).

    Pakai Bootstrap `placeholder-glow` + `placeholder col-*` existing (zero CSS baru). Tujuan: zero layout-shift saat HTMX swap (skeleton ≥ lebar/kolom konten asli).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - Skeleton Tab2 di `ManageAssessment.cshtml` punya 5 placeholder filter + 7 placeholder kolom per baris (match thead `_TrainingRecordsTab.cshtml`)
    - Skeleton History di `ManageAssessment.cshtml` punya ≥8 placeholder kolom (match thead Assessment `_HistoryTab.cshtml`)
    - Tidak ada CSS custom baru — hanya utility Bootstrap `placeholder*`
  </acceptance_criteria>
  <done>Skeleton Tab2 (5 filter/7 kolom) + Tab3 (8/5 kolom) match tabel asli; tidak ada flash layout-shift; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server (HTMX partial fetch) | Tab partial di-render server; filter client murni DOM toggle (tak ada input baru ke server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-01 | Tampering/Info (XSS) | `_HistoryTab.cshtml` MAP-04 drop `Html.Raw` | mitigate | MAP-04 MENGHAPUS `Html.Raw` di `<tr>` drill-down → mengurangi surface XSS; sisanya `@var` Razor auto-encode |
| T-349-02 | Info | counter/0-match teks (MAP-07/08) | accept | Teks statis + angka `visible/rows.length` dari DOM count (tak ada data sensitif, tak ada user-input echo) |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj -c Debug` exits 0 (per task — CLAUDE.md mandat)
- Grep acceptance per task terpenuhi (string LOCK case-sensitive)
- Manual/Playwright spot di phase gate (Plan 05): filter Tab3 0-match menampilkan pesan; tab switch tidak rusakkan filter (Pitfall 9)
</verification>

<success_criteria>
- Training Tab3 pakai "NIP" (bukan "Nopeg"); Assessment sudah "NIP" terkonfirmasi
- Tab3 Riwayat Assessment bukan nested-interactive — hanya tombol "Lihat"
- Filter client Tab3 0-match → "Tidak ada hasil untuk filter ini." (aria-live polite) + counter "Menampilkan X dari Y"
- Skeleton Tab2/Tab3 kolom-match → zero layout-shift
- Badge "Menunggu Penilaian" tetap single (REC-07, no duplikat)
- `dotnet build` 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/349-manageassessment-monitoring-low-polish/349-01-SUMMARY.md`
</output>
