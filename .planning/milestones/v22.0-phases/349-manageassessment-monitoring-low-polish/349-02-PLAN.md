---
phase: 349-manageassessment-monitoring-low-polish
plan: 02
type: execute
wave: 2
depends_on: [349-01]
files_modified:
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
  - Views/Admin/Shared/_TrainingRecordsTab.cshtml
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [MAP-03, MAP-05, MAP-06, MAP-18, MAP-19, MAP-21]
must_haves:
  truths:
    - "Tombol toggle collapse Tab1 'N peserta' punya aria-label deskriptif + chevron yang rotate saat dibuka"
    - "Tombol expand-records Tab2 punya aria-label deskriptif + chevron yang rotate (CSS [aria-expanded=true])"
    - "Tab1 empty-state filter-aware: kategori/status aktif + search kosong → 'Tidak ada assessment untuk filter ini' + tombol 'Reset Semua Filter'"
    - "Tab2 manual-assessment IsPassed==null menampilkan 'Menunggu Penilaian' (konstanta), bukan 'Tidak Lulus'"
    - "Tab2 badge 'Status Training' selalu render CompletionDisplayText (bukan gated 'Belum ada')"
    - "Pagination Tab1 tidak lagi pakai magic-number 20 — rowNum/text/hidden input baca ViewBag.PageSize (paging.Take)"
  artifacts:
    - path: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml"
      provides: "MAP-03 chevron+aria-label + MAP-05 empty-state filter-aware + MAP-06 Reset Semua Filter + MAP-21 paging.Take"
      contains: "Reset Semua Filter"
    - path: "Views/Admin/Shared/_TrainingRecordsTab.cshtml"
      provides: "MAP-03 chevron rotate CSS + MAP-18 tri-state pending + MAP-19 selalu CompletionDisplayText"
      contains: "AssessmentConstants.AssessmentStatus.PendingGrading"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "MAP-21 ViewBag.PageSize = paging.Take di ManageAssessmentTab_Assessment"
      contains: "ViewBag.PageSize = paging.Take"
  key_links:
    - from: "AssessmentAdminController.ManageAssessmentTab_Assessment"
      to: "_AssessmentGroupsTab.cshtml pageSize/rowNum"
      via: "ViewBag.PageSize"
      pattern: "ViewBag.PageSize\\s*=\\s*paging.Take"
    - from: "_TrainingRecordsTab.cshtml assessmentRows projection"
      to: "AssessmentConstants.AssessmentStatus.PendingGrading"
      via: "null-branch tri-state"
      pattern: "PendingGrading"
---

<objective>
Polish Tab1 Assessment Groups + Tab2 Training Records: a11y chevron+aria-label toggle collapse (MAP-03), empty-state filter-aware + relabel "Reset Semua Filter" (MAP-05/06 / D-01/D-02), tri-state IsPassed==null + selalu render CompletionDisplayText (MAP-18/19), dan buang magic-number 20 pagination via ViewBag.PageSize (MAP-21).

Purpose: Tutup 6 item LOW polish di file partisi Tab1/Tab2 (`_AssessmentGroupsTab.cshtml` + `_TrainingRecordsTab.cshtml`, keduanya pakai chevron MAP-03 yang sama). MAP-21 menyentuh controller `ManageAssessmentTab_Assessment` (ViewBag-only, tanpa logic-bearing) → Wave 2 (setelah Plan 01 controller-free; serialize dengan plan controller berikutnya).
Output: kedua partial + controller (1 baris ViewBag) ter-polish; build 0 error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/349-manageassessment-monitoring-low-polish/349-CONTEXT.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-RESEARCH.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Konstanta status (D-C — WAJIB konstanta, BUKAN literal). -->
Dari Models/AssessmentConstants.cs:
```csharp
public const string PendingGrading = "Menunggu Penilaian"; // AssessmentConstants.AssessmentStatus.PendingGrading
```
<!-- Pola referensi MAP-21: Tab2 (_TrainingRecordsTab.cshtml) SUDAH expose ViewBag.PageSize.
     Controller ManageAssessmentTab_Training L268/279 set ViewBag.PageSize = paging.Take. -->
Dari Controllers/AssessmentAdminController.cs:
- ManageAssessmentTab_Assessment(string? search, int page = 1, int pageSize = 20, ...) // L106
  // PaginationHelper.Calculate(grouped.Count, page, pageSize) → paging.Take; saat ini set CurrentPage/TotalPages/TotalCount tapi TIDAK ViewBag.PageSize
Dari Helpers/PaginationHelper.cs:
- PaginationResult Calculate(int totalCount, int page, int pageSize) // returns { CurrentPage, TotalPages, TotalCount, Skip, Take }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: a11y chevron + aria-label toggle collapse Tab1 & Tab2 (MAP-03)</name>
  <read_first>
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml (button "N peserta" L232-237)
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml (button expand-records L249-254 — chevron bi-chevron-down STATIS)
    - 349-RESEARCH.md §Pattern 2 (chevron rotate via CSS) + Assumption A1 (Bootstrap auto-set aria-expanded)
    - 349-UI-SPEC.md §A11y Contract → Chevron rotate + aria-label toggle collapse (CSS LOCK)
  </read_first>
  <action>
    Re-Grep target sebelum edit (Pitfall 1).

    **Tab1 (`_AssessmentGroupsTab.cshtml` ~L232-237)** — button "N peserta" toggle collapse (BELUM punya aria-label & chevron):
    - Tambah class `.toggle-chevron`, atribut `aria-label="Tampilkan/sembunyikan @userCount peserta"` (sesuaikan nama variabel count peserta yang ada), dan ikon chevron `<i class="bi bi-chevron-down chevron-icon ms-1"></i>`.
    - Pastikan button `type="button"` (cegah implicit submit) + `data-bs-toggle="collapse"` + `aria-expanded="false"` + `aria-controls`.

    **Tab2 (`_TrainingRecordsTab.cshtml` ~L249-254)** — button expand-records (SUDAH ada `aria-expanded` + chevron `bi-chevron-down` statis):
    - Tambah class `.toggle-chevron` ke button + class `chevron-icon` ke `<i class="bi bi-chevron-down">`.
    - Tambah `aria-label="Tampilkan/sembunyikan rekam jejak @worker.FullName"` (sesuaikan nama variabel nama worker).

    **CSS rotate** (tambah `<style>` di salah satu partial atau keduanya — pakai blok identik; aman karena CSS-only). LOCK dari UI-SPEC:
    ```css
    .toggle-chevron .chevron-icon { transition: transform 0.2s; }
    .toggle-chevron[aria-expanded="true"] .chevron-icon { transform: rotate(180deg); }
    ```
    CATATAN A1: Bootstrap 5 auto-set `aria-expanded` saat collapse toggle. Bila saat verifikasi visual (Plan 05) chevron TIDAK rotate, fallback ke JS toggle ala Monitoring-list (`AssessmentMonitoring.cshtml:401-411`) — tapi default = CSS-only (lebih simpel, bebas-bug, Pitfall 9 re-bind aman).
    Anti-pattern: JANGAN ciptakan nested-interactive baru (button collapse OK, `<tr>` interaktif TIDAK — koord MAP-04 Plan 01).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `_AssessmentGroupsTab.cshtml` mengandung `aria-label="Tampilkan/sembunyikan` + `chevron-icon` + class `toggle-chevron`
    - `_TrainingRecordsTab.cshtml` mengandung `aria-label="Tampilkan/sembunyikan rekam jejak` + `chevron-icon` + class `toggle-chevron`
    - CSS rule `.toggle-chevron[aria-expanded="true"] .chevron-icon { transform: rotate(180deg); }` hadir di salah satu/keduanya
    - Button toggle adalah `type="button"`
  </acceptance_criteria>
  <done>Toggle collapse Tab1 & Tab2 punya aria-label deskriptif + chevron rotate via CSS [aria-expanded]; type=button; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Empty-state filter-aware Tab1 + Reset Semua Filter (MAP-05 / MAP-06, D-01/D-02)</name>
  <read_first>
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml (empty-state L133-156 + link "Hapus Pencarian" L143-146 + tombol reset existing L100-108)
    - 349-RESEARCH.md §phase_requirements (MAP-05/06) + ViewBag tersedia: selectedAssessmentCategory, selectedAssessmentStatus, searchTerm
    - 349-UI-SPEC.md §Empty-state & feedback copy (string LOCK MAP-05/06) + §Decisions D-01 (Reset Semua Filter) / D-02 (filter-aware)
  </read_first>
  <action>
    Re-Grep empty-state block dulu (Pitfall 1).

    **MAP-05 (empty-state filter-aware)** di `_AssessmentGroupsTab.cshtml` (~L133-156):
    - Empty-state saat ini hanya bedakan `searchTerm` vs tidak. Tambah cabang: bila `selectedAssessmentCategory` ATAU `selectedAssessmentStatus` aktif (non-empty/non-default) DAN search kosong → tampilkan pesan filter-aware (LOCK):
      - heading: `Tidak ada assessment untuk filter ini`
      - body: `Coba ubah atau reset filter untuk melihat assessment lain.`
      - BUKAN "Buat assessment pertama" (jangan tampil saat filter aktif).
    - Pola visual MATCH existing empty-state: `text-center py-5`, ikon `bi-inbox` ~4rem `text-muted`, heading `h5 text-muted`, body `text-muted small`.
    - Variabel ViewBag tersedia: `selectedAssessmentCategory`, `selectedAssessmentStatus`, `searchTerm` (RESEARCH-verified).

    **MAP-06 (Reset Semua Filter — D-01)** di empty-state (~L143-146):
    - Relabel link/tombol "Hapus Pencarian" → `Reset Semua Filter` (LOCK). Aksi: hapus SEMUA filter sekaligus (search + kategori + status), bukan clear-search-only. Reuse aksi reset existing (L100-108 sudah hapus SEMUA filter) — arahkan tombol empty-state ke aksi/handler reset yang sama (atau navigate ke tab tanpa query param). String "Reset Semua Filter" WAJIB persis (case-sensitive).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `_AssessmentGroupsTab.cshtml` mengandung `Tidak ada assessment untuk filter ini`
    - `_AssessmentGroupsTab.cshtml` mengandung `Coba ubah atau reset filter untuk melihat assessment lain.`
    - `_AssessmentGroupsTab.cshtml` mengandung `Reset Semua Filter`; TIDAK lagi mengandung `Hapus Pencarian` di empty-state
    - Cabang empty-state membaca `selectedAssessmentCategory`/`selectedAssessmentStatus` untuk membedakan filter-aware vs "buat pertama"
  </acceptance_criteria>
  <done>Empty-state Tab1 filter-aware (pesan + Reset Semua Filter saat kategori/status aktif); tombol reset hapus semua filter; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: Tab2 tri-state pending + CompletionDisplayText (MAP-18/19) + MAP-21 paging.Take (controller + view)</name>
  <read_first>
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml (assessmentRows projection L264-266 + statusClass switch L322-329 + "Belum ada" gate L239-246 + pageSize hidden L16 + rowNum L180 + text L366)
    - Controllers/AssessmentAdminController.cs (ManageAssessmentTab_Assessment L106-218 — PaginationHelper.Calculate)
    - Models/AssessmentConstants.cs (PendingGrading konstanta — D-C)
    - 349-RESEARCH.md §Code Examples (MAP-18 tri-state + MAP-21 paging.Take) + §phase_requirements (MAP-18/19/21) + Anti-Patterns (literal forbidden)
    - 349-UI-SPEC.md §Display Binding Contract (MAP-18/19/21)
  </read_first>
  <action>
    Re-Grep ketiga target dulu (Pitfall 1).

    **MAP-18 (tri-state IsPassed==null — konstanta D-C)** di `_TrainingRecordsTab.cshtml` assessmentRows projection (~L264-266):
    Ubah Detail + Status agar null→"Menunggu Penilaian" (verbatim RESEARCH Code Example):
    ```csharp
    Detail = a.Score.HasValue
        ? $"{a.Score} - {(a.IsPassed == true ? "Lulus" : a.IsPassed == false ? "Tidak Lulus" : AssessmentConstants.AssessmentStatus.PendingGrading)}"
        : "—",
    Status = a.IsPassed == true ? "Passed"
           : a.IsPassed == false ? "Failed"
           : AssessmentConstants.AssessmentStatus.PendingGrading,  // null = pending
    ```
    Plus: statusClass switch (~L322-329) tambah cabang untuk "Menunggu Penilaian" → `bg-warning text-dark`. WAJIB konstanta `AssessmentConstants.AssessmentStatus.PendingGrading`, BUKAN literal (D-C, Anti-Pattern).

    **MAP-19 (selalu CompletionDisplayText)** di `_TrainingRecordsTab.cshtml` (~L239-246):
    Badge "Status Training" saat ini gated `worker.TotalTrainings == 0 → "Belum ada"`. Ubah jadi SELALU render `@worker.CompletionDisplayText` (konsisten, bukan gated). Hapus cabang "Belum ada".

    **MAP-21 (drop magic-number 20 → paging.Take via ViewBag)**:
    - Controller `AssessmentAdminController.cs` `ManageAssessmentTab_Assessment` (setelah `var paging = PaginationHelper.Calculate(...)`, sekitar L215): tambah `ViewBag.PageSize = paging.Take;` (verbatim RESEARCH Code Example). Jangan ubah perilaku paging — hanya expose metadata.
    - View `_TrainingRecordsTab.cshtml`... TUNGGU: MAP-21 berada di `_AssessmentGroupsTab.cshtml` (Tab1), bukan Training. Edit `_AssessmentGroupsTab.cshtml`:
      - Header view: tambah `var pageSize = (int)(ViewBag.PageSize ?? 20);` (baca ViewBag).
      - L16 hidden input: `<input type="hidden" name="pageSize" value="@pageSize" />` (ganti literal 20).
      - L180 rowNum: `var rowNum = (currentPage - 1) * pageSize + 1;` (ganti literal 20).
      - L366 text "Menampilkan": `@((currentPage - 1) * pageSize + 1) - @(Math.Min(currentPage * pageSize, totalCount))` (ganti literal 20).
    Pola identik SUDAH di `_TrainingRecordsTab.cshtml:16,391` + controller Tab2 L268/279 (referensi — JANGAN edit Tab2 untuk MAP-21, hanya referensi pola).

    CATATAN: `_AssessmentGroupsTab.cshtml` juga disentuh di Task 1 & Task 2 (plan ini) — semua edit di plan yang sama, tidak konflik.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `_TrainingRecordsTab.cshtml` mengandung `AssessmentConstants.AssessmentStatus.PendingGrading` di projection Detail + Status (MAP-18); TIDAK mengandung literal `"Menunggu Penilaian"` di kode baru
    - `_TrainingRecordsTab.cshtml` statusClass switch punya cabang "Menunggu Penilaian" → `bg-warning text-dark`
    - `_TrainingRecordsTab.cshtml` badge Status Training selalu render `CompletionDisplayText` (MAP-19); TIDAK ada lagi cabang gated `"Belum ada"`
    - `AssessmentAdminController.cs` mengandung `ViewBag.PageSize = paging.Take` di `ManageAssessmentTab_Assessment`
    - `_AssessmentGroupsTab.cshtml` membaca `ViewBag.PageSize` (var pageSize) dan TIDAK lagi memakai literal `20` untuk hidden input/rowNum/text "Menampilkan"
  </acceptance_criteria>
  <done>Tab2 IsPassed==null → "Menunggu Penilaian" (konstanta) + badge selalu CompletionDisplayText; Tab1 pagination baca paging.Take (no magic-number 20); build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server (HTMX) | Pagination param `pageSize` di-clamp `PaginationHelper.Calculate` (sudah ter-test 98/98 Phase 348) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-03 | Tampering | `pageSize` param ManageAssessmentTab_Assessment | accept | `PaginationHelper.Calculate` clamp page/pageSize (overflow/underflow ter-test); ViewBag.PageSize hanya expose nilai ter-clamp, tak menambah surface |
| T-349-04 | Info (XSS) | aria-label/Detail `@worker.*`/`@a.Score` | accept | Razor `@var` auto-encode; tak ada `Html.Raw` baru; konstanta status statis |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj -c Debug` exits 0 (per task)
- Grep acceptance per task (konstanta PendingGrading, bukan literal; ViewBag.PageSize; Reset Semua Filter; aria-label/chevron)
- Phase gate (Plan 05): Playwright spot — chevron rotate saat expand; empty-state filter-aware muncul saat kategori aktif; Tab1 paging text konsisten
</verification>

<success_criteria>
- Tab1 & Tab2 toggle collapse: aria-label deskriptif + chevron rotate (CSS)
- Tab1 empty-state filter-aware → "Tidak ada assessment untuk filter ini" + "Reset Semua Filter"
- Tab2 IsPassed==null → "Menunggu Penilaian" (konstanta D-C); badge selalu CompletionDisplayText
- Tab1 pagination baca ViewBag.PageSize (paging.Take); no magic-number 20
- `dotnet build` 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/349-manageassessment-monitoring-low-polish/349-02-SUMMARY.md`
</output>
