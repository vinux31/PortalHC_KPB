---
phase: 349-manageassessment-monitoring-low-polish
plan: 03
type: execute
wave: 3
depends_on: [349-02]
files_modified:
  - Views/Admin/AssessmentMonitoring.cshtml
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [MAP-13, MAP-14, MAP-15, MAP-16, MAP-17, MAP-23]
must_haves:
  truths:
    - "Subtitle Monitoring list tidak lagi menyebut 'real-time' (list tak ada SignalR)"
    - "Kategori tidak tampil dobel di kartu grup (muted subtitle dihapus, badge dipertahankan)"
    - "Grup Pre-Post token-required punya dropdown Aksi: 'View Detail' + 'Regenerate Token' (target LinkedGroupId via RepresentativeId)"
    - "Search Monitoring list cocok dengan Category (selain Title); placeholder diperbarui"
    - "Progress bar Monitoring list bisa mencapai 100% — TotalCount exclude Cancelled (Pre-Post + standard); standardGroups punya CancelledCount parity"
    - "Dropdown Status tidak misrepresent saat search broaden scope (set status='All' saat search-only)"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoring.cshtml"
      provides: "MAP-14 buang real-time + MAP-16 buang kategori dobel + MAP-17 Pre-Post dropdown Regenerate + MAP-23 placeholder"
      contains: "Regenerate Token"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "MAP-13 TotalCount exclude Cancelled + CancelledCount parity + MAP-15 status=All saat search + MAP-23 search Category"
      contains: "a.Category.ToLower().Contains(lower)"
  key_links:
    - from: "AssessmentMonitoring controller search predicate"
      to: "a.Category.ToLower().Contains(lower)"
      via: "EF parameterized Where"
      pattern: "Category.ToLower\\(\\).Contains\\(lower\\)"
    - from: "AssessmentMonitoring.cshtml Pre-Post Aksi dropdown"
      to: "RegenerateToken(int id) LinkedGroupId-aware"
      via: ".btn-regenerate-token data-id RepresentativeId"
      pattern: "btn-regenerate-token"
---

<objective>
Polish Monitoring **list** (halaman list, full reload — NO HTMX/SignalR): buang "real-time" (MAP-14), buang kategori dobel (MAP-16), tambah dropdown Aksi Pre-Post (View Detail + Regenerate Token, MAP-17/D-04), extend search ke Category (MAP-23), fix progress bar bisa 100% via TotalCount exclude Cancelled (MAP-13), dan dropdown Status jujur saat search broaden scope (MAP-15).

Purpose: Tutup 6 item LOW di partisi Monitoring list (`AssessmentMonitoring.cshtml`) + controller query list (MAP-13/15/23). Wave 3 — controller-touching, serialize setelah Plan 02 (MAP-21 ViewBag) dan sebelum Plan 04 (Detail action MAP-10).
Output: `AssessmentMonitoring.cshtml` + controller list query ter-polish; build 0 error; helper logic-bearing MAP-13/MAP-23 ditest di Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/349-manageassessment-monitoring-low-polish/349-CONTEXT.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-RESEARCH.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-UI-SPEC.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Konstanta status (D-C). MAP-13 WAJIB konstanta Cancelled. -->
Dari Models/AssessmentConstants.cs:
```csharp
public const string Cancelled = "Cancelled"; // AssessmentConstants.AssessmentStatus.Cancelled
```
<!-- Controller AssessmentMonitoring (list) — line PASCA-348 ter-verifikasi commit d998b256. -->
Dari Controllers/AssessmentAdminController.cs (method AssessmentMonitoring list):
- Search predicate L2742:  query = query.Where(a => a.Title.ToLower().Contains(lower));   // MAP-23 tambah || Category
- Pre-Post group MonitoringGroupViewModel L2803-2851:
    TotalCount = postSubs.Count > 0 ? postSubs.Count : preSubs.Count,   // L2811 — exclude Cancelled apply ke postSubs/preSubs, BUKAN g
    CancelledCount = g.Count(a => a.Status == "Cancelled"),             // L2816 (sudah ada)
- standardGroups MonitoringGroupViewModel L2868-2882:
    TotalCount = g.Count(),   // L2875 — ubah exclude Cancelled
    // CancelledCount MISSING di standardGroups — tambah parity
- Status filter logic L2898-2911:
    if (string.IsNullOrEmpty(status) && string.IsNullOrEmpty(search)) { grouped = ...!=Closed; status="active"; }
    // search non-empty + status kosong → no filter (Closed ikut) tapi ViewBag.SelectedStatus null → view default "active" SALAH
    ViewBag.SelectedStatus = status;  // L2911
- RegenerateToken(int id) // L2616 — SUDAH LinkedGroupId-aware (Phase 348 MAM-01); kirim RepresentativeId (PreTest rep) → regen semua siblings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n + display nits Monitoring list (MAP-14 real-time + MAP-16 kategori dobel + MAP-23 placeholder)</name>
  <read_first>
    - Views/Admin/AssessmentMonitoring.cshtml (subtitle L27 + kategori muted subtitle L256-259 + badge L271 + placeholder search L75)
    - 349-RESEARCH.md §phase_requirements (MAP-14/16/23) + §Code Examples (MAP-23 placeholder)
    - 349-UI-SPEC.md §Copywriting → i18n Monitoring list (MAP-14/16/23 LOCK)
  </read_first>
  <action>
    Re-Grep target dulu (Pitfall 1).

    **MAP-14 (buang "real-time")** di `AssessmentMonitoring.cshtml` (~L27): hapus frasa "real-time" dari subtitle `<p class="text-muted">` (list tak ada SignalR — Monitoring list full reload). Sisakan subtitle deskriptif tanpa "real-time".

    **MAP-16 (buang kategori dobel)** (~L256-259 vs L271): kartu grup menampilkan kategori DUA kali — muted subtitle (`@group.Category`, ~L256-259) DAN badge (`@group.Category`, ~L271). Hapus muted subtitle duplikat; PERTAHANKAN badge L271.

    **MAP-23 placeholder** (~L75): update placeholder search jadi LOCK: `Cari nama atau kategori assessment...` (mencerminkan scope Category yang ditambah di Task 2).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `AssessmentMonitoring.cshtml` subtitle TIDAK lagi mengandung `real-time`
    - `AssessmentMonitoring.cshtml` hanya menampilkan `@group.Category` SATU kali per kartu (badge dipertahankan, muted subtitle dihapus)
    - `AssessmentMonitoring.cshtml` placeholder search = `Cari nama atau kategori assessment...`
  </acceptance_criteria>
  <done>Subtitle tanpa "real-time"; kategori tidak dobel; placeholder search mencerminkan Category; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-Post dropdown Aksi Regenerate Token (MAP-17 / D-04)</name>
  <read_first>
    - Views/Admin/AssessmentMonitoring.cshtml (Aksi <td> L297-327 — KOSONG untuk IsPrePostGroup; dropdown hanya !IsPrePostGroup; JS .btn-regenerate-token L413-436)
    - Controllers/AssessmentAdminController.cs (RegenerateToken L2616 — LinkedGroupId-aware MAM-01)
    - 349-RESEARCH.md §Pattern 5 (MAP-17 dropdown reuse) + §Don't Hand-Roll (token regen reuse) + §phase_requirements MAP-17
    - 349-UI-SPEC.md §Display Binding Contract (MAP-17) + §Primary CTA (Monitoring list Pre-Post)
    - 348-CONTEXT.md (MAM-01 LinkedGroupId koordinasi)
  </read_first>
  <action>
    Re-Grep Aksi `<td>` block dulu (Pitfall 1).

    Di `AssessmentMonitoring.cshtml` (~L297-327), Aksi `<td>` untuk grup `IsPrePostGroup` saat ini KOSONG (dropdown hanya render untuk `!IsPrePostGroup`). Tambah dropdown untuk Pre-Post (verbatim pola RESEARCH §Pattern 5):
    ```cshtml
    <div class="dropdown">
        <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">Aksi</button>
        <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="@detailUrl"><i class="bi bi-eye me-1"></i>View Detail</a></li>
            @if (group.IsTokenRequired)
            {
                <li><hr class="dropdown-divider"></li>
                <li><button type="button" class="dropdown-item text-warning btn-regenerate-token"
                            data-id="@group.RepresentativeId">
                    <i class="bi bi-arrow-repeat me-1"></i>Regenerate Token</button></li>
            }
        </ul>
    </div>
    ```
    - `@detailUrl` = URL detail yang sudah dipakai grup standard (gunakan variabel/pemanggilan Url.Action yang konsisten dengan baris existing).
    - `data-id="@group.RepresentativeId"` — RepresentativeId Pre-Post = PreTest rep yang punya LinkedGroupId → handler `RegenerateToken(int id)` (L2616, MAM-01) regen semua siblings via LinkedGroupId. JANGAN buat endpoint baru (Don't Hand-Roll).
    - JS `.btn-regenerate-token` (~L413-436) sudah ada — REUSE (delegated/selector match), tidak ubah. Handler sudah `[ValidateAntiForgeryToken]` + kirim `RequestVerificationToken` header.
    String LOCK: `View Detail`, `Regenerate Token`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `AssessmentMonitoring.cshtml` Aksi `<td>` untuk `IsPrePostGroup` mengandung dropdown dengan item `View Detail` + (gated `IsTokenRequired`) `Regenerate Token`
    - Tombol Regenerate pakai class `btn-regenerate-token` + `data-id="@group.RepresentativeId"` (reuse JS existing, no endpoint baru)
    - Tidak ada method controller baru ditambah untuk regen Pre-Post (reuse `RegenerateToken`)
  </acceptance_criteria>
  <done>Grup Pre-Post token-required punya dropdown Aksi View Detail + Regenerate Token (target LinkedGroupId via RepresentativeId); reuse handler+JS existing; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: TotalCount exclude Cancelled (MAP-13) + Status dropdown jujur (MAP-15) + search Category (MAP-23) — controller</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (search L2739-2743; Pre-Post TotalCount L2811 + CancelledCount L2816; standardGroups L2868-2882; status logic L2898-2911)
    - Models/AssessmentConstants.cs (Cancelled konstanta — D-C)
    - 349-RESEARCH.md §Pattern 6 (aggregate exclude — KONSTANTA) + §Code Examples (MAP-23) + Pitfall 7 (MAP-15 search-broaden) + Pitfall 8 (jangan tertukar dengan Training param)
    - 349-UI-SPEC.md §Display Binding Contract (MAP-13/15/23)
  </read_first>
  <action>
    Re-Grep semua target controller dulu (Pitfall 1) + cek Pitfall 2 (jangan sentuh reshuffleWorker, itu MAM-13/348).

    **MAP-13 (TotalCount exclude Cancelled — konstanta)**:
    - standardGroups `TotalCount` (~L2875): `g.Count()` → `g.Count(a => a.Status != AssessmentConstants.AssessmentStatus.Cancelled)`.
    - standardGroups parity: tambah `CancelledCount = g.Count(a => a.Status == AssessmentConstants.AssessmentStatus.Cancelled),` (saat ini MISSING di L2868-2882).
    - Pre-Post `TotalCount` (~L2811): saat ini `postSubs.Count > 0 ? postSubs.Count : preSubs.Count` (BUKAN `g.Count()` — Pitfall 6/Pattern6). Apply exclude-Cancelled ke count `postSubs`/`preSubs`: ubah jadi
      `TotalCount = (postSubs.Count > 0 ? postSubs : preSubs).Count(a => a.Status != AssessmentConstants.AssessmentStatus.Cancelled),`
      (sesuaikan tipe; postSubs/preSubs adalah list session sub). Existing `CancelledCount` Pre-Post L2816 sudah ada — biarkan (atau ubah literal "Cancelled" → konstanta untuk konsistensi).
    - Verifikasi konsistensi `progressPct` view (CompletedCount/TotalCount) tetap valid — progress bisa 100% saat semua non-Cancelled selesai.
    - Anti-pattern: WAJIB konstanta `AssessmentConstants.AssessmentStatus.Cancelled`, BUKAN literal (meski controller existing kadang literal — polish = konsisten).

    **MAP-15 (Status dropdown jujur — Pitfall 7)** (~L2898-2911):
    - Saat search non-empty + status kosong → cabang ini saat ini biarkan no filter (Closed ikut) tapi `ViewBag.SelectedStatus` tetap null/kosong → view `selStatus ?? "active"` SALAH tampil "Open+Upcoming". Tambah: di cabang `else if` (atau setelah blok BOTH-empty), bila `!string.IsNullOrEmpty(search) && string.IsNullOrEmpty(status)` → set `status = "All";` (broaden scope jujur). Sehingga `ViewBag.SelectedStatus = status;` (L2911) jadi "All" → dropdown tampil "Semua Status".
    - Jangan break CIL-02 (Phase 338 search "Cilacap" tetap include Closed). Hanya ubah representasi dropdown, bukan logika filter hasil.

    **MAP-23 (search Category)** (~L2739-2743):
    ```csharp
    if (!string.IsNullOrEmpty(search))
    {
        var lower = search.ToLower();
        query = query.Where(a => a.Title.ToLower().Contains(lower)
                              || a.Category.ToLower().Contains(lower));  // MAP-23
    }
    ```
    Nama/NIP TIDAK (list aggregate, OUT OF SCOPE per CONTEXT D). EF parameterized `.Contains` — no injection.

    JANGAN sentuh `reshuffleWorker` (AssessmentMonitoringDetail.cshtml:739 — MAM-13/348 done, Pitfall 2). JANGAN drop param Training (Pitfall 8 — itu MAP-22 Plan 05, dan Training param fungsional).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `AssessmentAdminController.cs` standardGroups `TotalCount` = `g.Count(a => a.Status != AssessmentConstants.AssessmentStatus.Cancelled)` + ada `CancelledCount` parity
    - `AssessmentAdminController.cs` Pre-Post `TotalCount` count exclude `AssessmentConstants.AssessmentStatus.Cancelled` (apply ke postSubs/preSubs, bukan g)
    - `AssessmentAdminController.cs` search predicate mengandung `a.Category.ToLower().Contains(lower)`
    - `AssessmentAdminController.cs` set `status = "All"` saat `!IsNullOrEmpty(search) && IsNullOrEmpty(status)` (sebelum ViewBag.SelectedStatus)
    - TIDAK ada literal `"Cancelled"` baru di kode MAP-13 (pakai konstanta); reshuffleWorker TIDAK disentuh
  </acceptance_criteria>
  <done>Progress bar list bisa 100% (TotalCount exclude Cancelled, parity CancelledCount standard); dropdown Status="All" saat search broaden; search cocok Category; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server (GET search/status/category) | Query param di-filter EF parameterized; token regen reuse endpoint guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-05 | Tampering (injection) | search predicate MAP-23 `Category.ToLower().Contains` | mitigate | EF Core `.Where(...Contains)` = parameterized SQL, no raw concat → no injection (ASVS V5) |
| T-349-06 | Spoofing (CSRF) | MAP-17 Regenerate Token Pre-Post | mitigate | Reuse `RegenerateToken` existing — sudah `[ValidateAntiForgeryToken]` (L2616) + JS kirim RequestVerificationToken header; ZERO endpoint baru |
| T-349-07 | Info | dropdown Status / count display (MAP-13/15) | accept | Hanya representasi UI count/dropdown; tak ubah otorisasi atau data exposure |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj -c Debug` exits 0 (per task)
- Grep acceptance (konstanta Cancelled bukan literal; Category.Contains; status="All"; btn-regenerate-token; no real-time)
- Logic-bearing MAP-13/MAP-23 → unit test di Plan 05 (`ManageAssessmentLowPolishTests.cs`)
- Phase gate (Plan 05): browser-verify MAP-13 progress bisa 100%; MAP-17 Regenerate Pre-Post berfungsi; MAP-23 search "kategori" muncul; MAP-15 dropdown "Semua Status" saat search Closed
</verification>

<success_criteria>
- Subtitle tanpa "real-time"; kategori tidak dobel
- Pre-Post dropdown Aksi: View Detail + Regenerate Token (LinkedGroupId, reuse handler)
- Search list cocok Category (Title || Category)
- Progress bar bisa 100% (TotalCount exclude Cancelled, Pre-Post + standard parity)
- Dropdown Status="All" saat search broaden scope (jujur)
- `dotnet build` 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/349-manageassessment-monitoring-low-polish/349-03-SUMMARY.md`
</output>
