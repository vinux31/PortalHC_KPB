---
phase: 349-manageassessment-monitoring-low-polish
plan: 04
type: execute
wave: 4
depends_on: [349-03]
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Models/AssessmentMonitoringViewModel.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [MAP-01, MAP-10, MAP-11, MAP-12]
must_haves:
  truths:
    - "Monitoring Detail header tabel + kartu + tombol back + export memakai Bahasa Indonesia penuh"
    - "Summary cards Detail = 7 kartu (Total/Selesai/Sedang Mengerjakan/Belum Mulai/Dibatalkan/Abandoned/Menunggu Penilaian) dengan invariant Total = jumlah 6 kartu lain"
    - "Kartu 'Abandoned' bind AbandonedCount (field baru) + kartu 'Menunggu Penilaian' bind MenungguPenilaianCount (di-assign di Detail action)"
    - "JS updateSummaryFromDOM punya cabang Abandoned + Menunggu Penilaian (tidak salah masuk notStarted saat SignalR push)"
    - "Kartu 'Sedang Mengerjakan' bind @Model.InProgressCount (bukan inline LINQ); dead var completedPct/passRatePct dibuang"
    - "Tombol 'Akhiri Semua Ujian' render kondisional (InProgressCount > 0 || GroupStatus == Open); modal wording predikat identik aksi cancel"
  artifacts:
    - path: "Models/AssessmentMonitoringViewModel.cs"
      provides: "field AbandonedCount baru"
      contains: "AbandonedCount"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "assign AbandonedCount + MenungguPenilaianCount di Detail action (L3320-3336)"
      contains: "AbandonedCount = sessionViewModels.Count"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "i18n ID + 7 kartu summary + JS sync + InProgressCount bind + conditional Akhiri Semua"
      contains: "count-abandoned"
  key_links:
    - from: "AssessmentMonitoringDetail Detail action model"
      to: "AbandonedCount / MenungguPenilaianCount fields"
      via: "sessionViewModels.Count(s => s.UserStatus == ...)"
      pattern: "AbandonedCount\\s*=\\s*sessionViewModels.Count"
    - from: "updateSummaryFromDOM JS"
      to: "count-abandoned / count-pending elements"
      via: "else if branch + textContent"
      pattern: "count-(abandoned|pending)"
---

<objective>
Polish Monitoring **Detail** (per-grup, page dengan SignalR live push): i18n chrome Inggris→Indonesia (MAP-01), summary cards set LENGKAP 7 kartu dengan invariant Total=sum (MAP-10 — item paling kompleks, tambah field ViewModel + assign controller + sync JS), bind InProgressCount + drop dead var (MAP-11), dan conditional render "Akhiri Semua Ujian" (MAP-12).

Purpose: Tutup 4 item LOW di partisi Monitoring Detail. MAP-10 = single most complex item: 6 status bucket dari `DeriveUserStatus` butuh 7 kartu (existing 5 + Abandoned + Menunggu Penilaian) agar Total = jumlah semua kartu (D-03). Wave 4 — controller Detail action (beda method dari Plan 03 list, tapi file sama → serialize).
Output: Detail view + ViewModel field + controller assign + JS sync; build 0 error; invariant card-sum siap browser-verify di Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/349-manageassessment-monitoring-low-polish/349-CONTEXT.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-RESEARCH.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Konstanta status (D-C). -->
Dari Models/AssessmentConstants.cs:
```csharp
public const string PendingGrading = "Menunggu Penilaian"; // AssessmentConstants.AssessmentStatus.PendingGrading
```
<!-- DeriveUserStatus menghasilkan 6 string distinct (PASCA-348, verified): -->
Dari Controllers/AssessmentAdminController.cs L2677-2690:
```csharp
public static string DeriveUserStatus(string? status, DateTime? completedAt, DateTime? startedAt)
// returns: "Menunggu Penilaian" | "Completed" | "Dibatalkan" | "Abandoned" | "InProgress" | "Not started"
```
<!-- Detail action assign (L3320-3336) — saat ini set 5 count; AbandonedCount + MenungguPenilaianCount BELUM di-assign. -->
Dari Controllers/AssessmentAdminController.cs Detail action (~L3320-3336):
```csharp
var model = new MonitoringGroupViewModel {
    ...
    TotalCount     = sessionViewModels.Count,
    CompletedCount = sessionViewModels.Count(s => s.UserStatus == "Completed"),
    PassedCount    = sessionViewModels.Count(s => s.IsPassed == true),
    PendingCount   = sessionViewModels.Count(s => s.UserStatus == "Not started"),
    CancelledCount = sessionViewModels.Count(s => s.UserStatus == "Dibatalkan"),
    InProgressCount = sessionViewModels.Count(s => s.UserStatus == "InProgress")
};
```
<!-- ViewModel: MenungguPenilaianCount SUDAH ada (L22); AbandonedCount BELUM. InProgressCount L42, CancelledCount L15/43 ada. -->
Dari Models/AssessmentMonitoringViewModel.cs:
```csharp
public int CancelledCount { get; set; }     // L15
public int InProgressCount { get; set; }    // L42
public int MenungguPenilaianCount { get; set; } // L22 — ADA, perlu di-assign di Detail
// AbandonedCount — BELUM ADA, tambah field
```
<!-- JS updateSummaryFromDOM (L1281-1300): else→notStarted++ (Abandoned + Pending salah masuk notStarted). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n Monitoring Detail (MAP-01)</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (header tabel L214-220 + kartu L150/156/162/168 + "Back to Monitoring" L72 + "Per-User Status" L183 + "Export Results" L192 + "No sessions found" L227)
    - 349-RESEARCH.md §Pattern 1 (i18n inline Razor) + §phase_requirements MAP-01
    - 349-UI-SPEC.md §Copywriting → i18n string lock Monitoring Detail (LOCK case-sensitive)
  </read_first>
  <action>
    Re-Grep string Inggris dulu (Pitfall 1). Ganti string LOCK (verbatim UI-SPEC, case-sensitive) di `AssessmentMonitoringDetail.cshtml`:

    Header tabel: `Name`→`Nama`, `Progress`→`Progres`, `Status`→`Status` (tetap), `Score`→`Nilai`, `Result`→`Hasil`, `Completed At`→`Selesai Pada`, `Actions`→`Aksi`.
    Tombol back: `Back to Monitoring`→`Kembali ke Monitoring`.
    Heading section: `Per-User Status`→`Status Per-Peserta`.
    Tombol export: `Export Results`→`Ekspor Hasil`.
    Empty tabel: `No sessions found`→`Belum ada sesi`.

    Murni teks Razor (parity dengan Monitoring list yang sudah Indonesia). JANGAN ubah binding/logic.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `AssessmentMonitoringDetail.cshtml` mengandung `<th>Nama</th>`, `Progres`, `Nilai`, `Hasil`, `Selesai Pada`, `Aksi` di header tabel
    - `AssessmentMonitoringDetail.cshtml` mengandung `Kembali ke Monitoring`, `Status Per-Peserta`, `Ekspor Hasil`, `Belum ada sesi`
    - TIDAK mengandung lagi `Back to Monitoring`, `Per-User Status`, `Export Results`, `No sessions found`, `<th>Name</th>`, `<th>Score</th>`, `<th>Result</th>`, `Completed At`
  </acceptance_criteria>
  <done>Chrome Monitoring Detail Indonesia penuh (header/kartu/back/export/empty); build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Summary cards 7-kartu + AbandonedCount field + controller assign + JS sync (MAP-10) + InProgressCount bind + drop dead var (MAP-11)</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (summary cards L146-177 + inline LINQ L161 + dead var completedPct L33-35 / passRatePct L37-39 + updateSummaryFromDOM L1281-1300)
    - Models/AssessmentMonitoringViewModel.cs (FILE penuh — field count existing)
    - Controllers/AssessmentAdminController.cs (Detail action model assign L3320-3336 + DeriveUserStatus L2677-2690)
    - 349-RESEARCH.md Pitfall 4 (Total≠sum) + Pitfall 5 (JS bucket-all-else) + §Don't Hand-Roll (status derivation JS) + §phase_requirements MAP-10/11
    - 349-UI-SPEC.md §Summary Card Layout (KONTRAK KRITIS — 7 kartu, id, struktur verbatim, JS sync block) + §Display Binding (MAP-11)
  </read_first>
  <behavior>
    - Invariant: `Total Ditugaskan == Selesai + Sedang Mengerjakan + Belum Mulai + Dibatalkan + Abandoned + Menunggu Penilaian` (browser-verify Plan 05).
    - Saat SignalR `workerStarted`/`workerSubmitted` memanggil updateSummaryFromDOM → kartu Abandoned & Menunggu Penilaian update benar (tidak naik notStarted salah).
  </behavior>
  <action>
    Re-Grep semua target dulu (Pitfall 1, 4, 5).

    **(a) ViewModel field baru** — `Models/AssessmentMonitoringViewModel.cs`: tambah
    `public int AbandonedCount { get; set; }` (sejajar field count lain; `MenungguPenilaianCount` L22 SUDAH ada).

    **(b) Controller assign** — `AssessmentAdminController.cs` Detail action (~L3320-3336), tambah 2 assign ke `model` (sumber = `DeriveUserStatus` string, verified):
    ```csharp
    AbandonedCount = sessionViewModels.Count(s => s.UserStatus == "Abandoned"),
    MenungguPenilaianCount = sessionViewModels.Count(s => s.UserStatus == AssessmentConstants.AssessmentStatus.PendingGrading),
    ```
    (`"Menunggu Penilaian"` = nilai konstanta PendingGrading; pakai konstanta untuk Menunggu Penilaian. "Abandoned" boleh literal — belum ada konstanta, sumber derivasi DeriveUserStatus.)

    **(c) View 7 kartu** — `AssessmentMonitoringDetail.cshtml` summary cards (~L146-177). Struktur tiap kartu MATCH existing (UI-SPEC verbatim):
    `<div class="col"><div class="card border-0 shadow-sm text-center py-3"><div class="fs-3 fw-bold text-{color}" id="count-{key}">@Model.{Count}</div><div class="text-muted small">{Label}</div></div></div>`.
    Set lengkap 7 kartu (id + sumber + warna LOCK):
    | Kartu | id | Sumber | Warna |
    | Total | count-total | @Model.TotalCount | text-primary |
    | Selesai | count-completed | @Model.CompletedCount | text-success |
    | Sedang Mengerjakan | count-inprogress | @Model.InProgressCount | text-warning |
    | Belum Mulai | count-notstarted | @Model.PendingCount | text-secondary |
    | Dibatalkan | count-cancelled | @Model.CancelledCount | text-secondary |
    | Abandoned | count-abandoned | @Model.AbandonedCount | text-dark |
    | Menunggu Penilaian | count-pending | @Model.MenungguPenilaianCount | text-warning |
    Label LOCK: `Total Ditugaskan`/`Selesai`/`Sedang Mengerjakan`/`Belum Mulai`/`Dibatalkan`/`Abandoned`/`Menunggu Penilaian`.
    Layout: pertahankan `.col` auto-distribute dalam `<div class="row g-3 mb-4">`; jika terlalu sempit boleh tambah `row-cols-2 row-cols-md-4 row-cols-xl-7` (utility Bootstrap, JANGAN custom CSS).

    **MAP-11 (di kartu Sedang Mengerjakan)**: kartu "Sedang Mengerjakan" bind `@Model.InProgressCount` (BUKAN inline LINQ L161). Plus drop dead var `completedPct` (L33-35) + `passRatePct` (L37-39) — JANGAN surface jadi progress bar baru (D minimal-risk).

    **(d) JS sync** — `updateSummaryFromDOM` (~L1281-1300). Saat ini `else → notStarted++`. Tambah cabang (UI-SPEC LOCK) SEBELUM `else`:
    ```js
    else if (text === 'Abandoned') abandoned++;
    else if (text === '@AssessmentConstants.AssessmentStatus.PendingGrading') pending++;
    else notStarted++;
    ```
    + deklarasi var `abandoned`/`pending` (init 0) + update elemen `count-abandoned` & `count-pending` (`document.getElementById('count-abandoned').textContent = abandoned;` dst). Tanpa ini, kartu live salah saat SignalR push (Pitfall 5). Baca status dari `.status-cell` textContent (pola existing — Don't Hand-Roll, jangan re-derive dari raw field).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `AssessmentMonitoringViewModel.cs` mengandung `public int AbandonedCount { get; set; }`
    - `AssessmentAdminController.cs` Detail action mengandung `AbandonedCount = sessionViewModels.Count(s => s.UserStatus == "Abandoned")` + `MenungguPenilaianCount = sessionViewModels.Count(s => s.UserStatus == AssessmentConstants.AssessmentStatus.PendingGrading)`
    - `AssessmentMonitoringDetail.cshtml` mengandung 7 kartu dengan id `count-total`/`count-completed`/`count-inprogress`/`count-notstarted`/`count-cancelled`/`count-abandoned`/`count-pending`
    - Kartu Sedang Mengerjakan bind `@Model.InProgressCount` (bukan inline LINQ); var `completedPct`/`passRatePct` dibuang (grep TIDAK menemukan)
    - `updateSummaryFromDOM` JS mengandung `else if (text === 'Abandoned')` + `else if (text === '@AssessmentConstants.AssessmentStatus.PendingGrading')` + update `count-abandoned`/`count-pending`
  </acceptance_criteria>
  <done>7 kartu summary dengan invariant Total=sum; AbandonedCount+MenungguPenilaianCount di-assign; JS sync benar; InProgressCount bind + dead var dibuang; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: Conditional render "Akhiri Semua Ujian" + modal wording predikat-identik (MAP-12)</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (tombol Akhiri Semua L196-199 + modal wording L541-542 + GetAkhiriSemuaCounts populate)
    - 349-RESEARCH.md §Pattern 5 (conditional button render — MAP-12) + §phase_requirements MAP-12
    - 349-UI-SPEC.md §Destructive confirmation (MAP-12 LOCK gate + modal wording)
  </read_first>
  <action>
    Re-Grep tombol + modal dulu (Pitfall 1).

    **MAP-12 (conditional render)** di `AssessmentMonitoringDetail.cshtml` (~L196-199):
    Bungkus tombol "Akhiri Semua Ujian" dengan gate (LOCK):
    ```cshtml
    @if (Model.InProgressCount > 0 || Model.GroupStatus == "Open")
    {
        <button type="button" class="btn btn-danger btn-sm" id="akhiriSemuaBtn" ...>
            <i class="bi bi-x-circle me-1"></i>Akhiri Semua Ujian
        </button>
    }
    ```
    (pertahankan atribut/data existing pada button — hanya tambah `@if` wrapper).

    **Modal wording predikat-identik** (~L541-542): modal wording "{X} belum mulai" harus memakai predikat IDENTIK dengan aksi cancel (jangan divergen). Count di-populate JS via `GetAkhiriSemuaCounts`. Verifikasi predikat hitung "belum mulai" di modal = predikat yang dipakai aksi cancel server-side (keduanya konsisten). Bila ada divergensi (mis. modal hitung Not-started sementara cancel target InProgress) → samakan ke predikat aksi cancel.
    Tombol `btn-danger` (destructive — UI-SPEC color). String LOCK: `Akhiri Semua Ujian`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0
    - `AssessmentMonitoringDetail.cshtml` tombol "Akhiri Semua Ujian" dibungkus `@if (Model.InProgressCount > 0 || Model.GroupStatus == "Open")`
    - Tombol pakai class `btn-danger` (destructive)
    - Modal wording count "belum mulai" memakai predikat identik dengan aksi cancel (terkonfirmasi via read — no divergensi)
  </acceptance_criteria>
  <done>Tombol Akhiri Semua Ujian render hanya saat InProgressCount>0 || GroupStatus==Open; modal wording predikat-identik aksi cancel; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SignalR push→client JS (updateSummaryFromDOM) | Live count update dari DOM textContent (server-pushed status); tak ada eval/innerHTML |
| client→server (Akhiri Semua / cancel) | Aksi destruktif via endpoint existing (tak diubah) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-08 | Info (XSS) | summary card `@Model.*Count` + i18n teks | accept | Razor `@var` auto-encode; count = integer; teks statis; JS `textContent` (bukan innerHTML) → no XSS |
| T-349-09 | Elevation | MAP-12 Akhiri Semua Ujian conditional | accept | Hanya gate RENDER tombol (UI); otorisasi + endpoint cancel existing tak diubah; gate tidak melemahkan kontrol server |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj -c Debug` exits 0 (per task)
- Grep acceptance (AbandonedCount field+assign; 7 count-* id; cabang JS Abandoned/pending; @if gate)
- Phase gate (Plan 05): browser-verify invariant Total = jumlah 6 kartu lain (MAP-10 card-sum); SignalR push update kartu Abandoned/Pending benar; tombol Akhiri Semua hilang saat tak ada InProgress & status Closed
</verification>

<success_criteria>
- Chrome Detail Indonesia penuh (MAP-01)
- 7 kartu summary, invariant Total = jumlah 6 kartu lain (MAP-10)
- AbandonedCount field + assign; MenungguPenilaianCount assign; JS sync benar
- Kartu Sedang Mengerjakan bind InProgressCount; dead var dibuang (MAP-11)
- Akhiri Semua Ujian conditional + modal wording predikat-identik (MAP-12)
- `dotnet build` 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/349-manageassessment-monitoring-low-polish/349-04-SUMMARY.md`
</output>
