---
phase: 349
slug: manageassessment-monitoring-low-polish
status: passed
environment: lokal (ASPNETCORE_ENVIRONMENT=Development, http://localhost:5277)
db: HcPortalDB_Dev (SQLEXPRESS) — data existing, TANPA seed temporary
tester: Claude (Playwright MCP) — pending human-verify approval
date: 2026-06-05
---

# Phase 349 — UAT (5 Success Criteria)

Verifikasi lokal via Playwright MCP. App `http://localhost:5277` (Development), login `admin@pertamina.com`. **Tidak ada seed temporary** — pakai data existing DB lokal (cukup untuk cover semua SC: ada grup Open/Upcoming/Closed, grup completed 2/2 untuk progress-100%).

## Gate otomasi (sebelum browser)

| Gate | Hasil |
|------|-------|
| `dotnet build HcPortal.csproj -c Debug` | **0 Error** (21 warning pre-existing) |
| `dotnet test HcPortal.Tests` (full suite) | **105/105 PASS** (98 baseline Phase 348 + 7 baru ManageAssessmentLowPolish) |
| `dotnet test --filter ~ManageAssessmentLowPolish` | **7/7 PASS** (MAP-13 ×4 + MAP-23 ×3) |

## Success Criteria

### SC1 — i18n Monitoring Detail Indonesia penuh + "NIP" konsisten History — **PASS**
- Monitoring Detail header tabel: **Nama / Progres / Status / Nilai / Hasil / Selesai Pada / Aksi** (semua ID).
- "Kembali ke Monitoring" (was Back to Monitoring), "Status Per-Peserta" (was Per-User Status), "Ekspor Hasil" (was Export Results).
- History Riwayat Assessment: placeholder "Cari nama pekerja / **NIP**...", header kolom "**NIP**"; Riwayat Training header "**NIP**" (was Nopeg).
- Grep EN residual di Detail = 0.

### SC2 — a11y chevron + aria-label; Tab3 drill-down tanpa ARIA nested — **PASS**
- Tab1 toggle "N peserta": `aria-label="Tampilkan/sembunyikan 1 peserta"` + chevron rotate saat expand → computed `transform: matrix(-1, 0, 0, -1, 0, 0)` (= rotate 180°), CSS `[aria-expanded="true"]` rule aktif.
- Tab3 Riwayat Assessment: baris drill-down BUKAN nested-interactive — hanya tombol/link "Lihat" (tr tanpa role=link/tabindex/Html.Raw; grep role=link/cil03-row-link = 0).

### SC3 — empty-state/feedback filter-aware + Tab3 no-results + skeleton — **PASS**
- Tab1: pilih kategori "MANDATORY" (0 grup) → empty-state filter-aware "**Tidak ada assessment untuk filter ini**" + "Coba ubah atau reset filter untuk melihat assessment lain." + tombol "**Reset Semua Filter**" (→ `/Admin/ManageAssessment?tab=assessment`). BUKAN "Buat assessment pertama".
- Tab3: search "zzz-tidak-ada-cocok" (0 match) → container `#assessmentNoResult` tampil (display=""), `aria-live="polite"`, teks "**Tidak ada hasil untuk filter ini.**"; counter "**Menampilkan 0 dari 34**" (sebelum filter "Menampilkan 34 dari 34").
- Skeleton kolom-match: build pass + markup placeholder Bootstrap (Tab2 5 filter/7 kolom, Tab3 8 kolom) — verified grep Plan 01; visual swap cepat tanpa layout-shift mencolok.

### SC4 — display nits: Abandoned card + invariant Total=sum + progress 100% + subtitle + kategori — **PASS**
- **Kartu Abandoned + Menunggu Penilaian** render (7 kartu set lengkap), warna `text-dark` / `text-warning`.
- **Invariant Total = jumlah 6 kartu lain (BROWSER-VERIFY MAP-10):**
  - Grup "OJT Assessment": Total Ditugaskan **2** = Selesai(2) + Sedang Mengerjakan(0) + Belum Mulai(0) + Dibatalkan(0) + Abandoned(0) + Menunggu Penilaian(0) = **2** ✓
  - Grup "Pre Test OJT GAST Cilacap": Total **1** = Belum Mulai(1) + sisanya 0 = **1** ✓
- **Progress bar bisa 100% (BROWSER-VERIFY MAP-13):** grup "OJT Assessment" (2 peserta, 2 Completed, 0 Cancelled) → kolom Progress = **100** (progressbar "2/2 selesai", bar hijau). Denominator exclude Cancelled.
- Subtitle Monitoring list: "Pantau progress assessment — status grup, peserta, dan token." → **TANPA "real-time"**.
- Kategori **tidak dobel**: hanya badge di kolom Kategori (muted subtitle dihapus).

### SC5 — code-hygiene: build 0 error + no visual regression — **PASS**
- `dotnet build` 0 error; magic-number 20 → ViewBag.PageSize (Tab1 paging baca `paging.Take`); param mati History dibuang (signature `(string? search)`).
- No visual regression: Tab1/Tab2/Tab3 + Monitoring list + Detail render normal (navigasi, filter, dropdown, modal trigger berfungsi).

## Verifikasi MAP tambahan (browser)

| MAP | Hasil browser |
|-----|---------------|
| MAP-12 conditional Akhiri Semua | Open grup "Pre Test OJT GAST Cilacap" → tombol "Akhiri Semua Ujian" **render**; Closed grup "OJT Assessment" (InProgress=0) → **tidak render**. Gate dua arah PASS. |
| MAP-15 dropdown jujur | Direct URL `?search=ojt` (status kosong) → dropdown Status = "**Semua Status**" (Closed "OJT Assessment" muncul, jujur). |
| MAP-17 Pre-Post Regenerate | Grup Pre-Post "Quiz Random" → dropdown Aksi "**View Detail**" (Regenerate Token gated IsTokenRequired; grup ini token=Tidak → hanya View Detail, perilaku benar). |
| MAP-23 search Category | search "ojt" → "Quiz Random" (Title tanpa "ojt", Category=OJT) muncul = match via Category. |

## Catatan / Observasi (non-blocking)

1. **MAP-05 live SignalR card update** — tidak bisa ditrigger via Playwright (butuh peserta submit ujian live; tak ada sesi aktif di data lokal). JS branch `updateSummaryFromDOM` (cabang Abandoned + Menunggu Penilaian) terverifikasi di kode + initial server-render kartu benar. Defer live spot-check (sama pola Phase 348 MAM-05; gap data lokal, BUKAN defect).
2. **MAP-15 form-path** — saat filter via FORM (select Status default "Open + Upcoming" → submit `status=active`), Closed tetap muncul tapi dropdown "Open + Upcoming". Ini path BERBEDA dari scope MAP-15 (RESEARCH Pitfall 7 = `status kosong`). MAP-15 fix sengaja menyasar path status-kosong (direct link/HTMX) — terverifikasi PASS. Path form `status=active` = pre-existing behavior di luar scope MAP-22/15. Kandidat backlog kecil bila perlu (set form default value="" atau handle "active"+search).

## Hasil akhir

**5/5 Success Criteria PASS** + invariant card-sum (MAP-10) + progress-100% (MAP-13) browser-verified. Full suite 105/105. Tidak ada visual regression. Tidak ada seed temporary (DB lokal utuh).

Screenshot: `349-uat-detail-7cards.png` (7 kartu summary + Akhiri Semua Ujian grup Open).

**Status: passed** — pending human-verify approval (checkpoint Plan 05 Task 3).
