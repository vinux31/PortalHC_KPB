---
phase: 351-worker-detail-cross-surface-filter-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/sql/cmp351-seed.sql
  - tests/e2e/cmp-records-351.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [SF-03, SF-04, SF-05, SF-07]
must_haves:
  truths:
    - "Seed cmp351-seed.sql menyisipkan 1 TrainingRecord [PENDING351] dengan Kategori free-text off-master (Legacy-FreeText-351) untuk worker rino.prasetyo"
    - "cmp-records-351.spec.ts ada, ter-load oleh Playwright, dan punya beforeAll backup+seed + afterAll restore+Layer-4-clean"
    - "Spec memuat 4 test group (0-match, legacy-Kategori, parity, back-nav) yang menargetkan selector DOM final SF-03/04/05/07"
  artifacts:
    - path: "tests/sql/cmp351-seed.sql"
      provides: "Off-master Kategori seed untuk SF-04 + 0-match fixture untuk SF-03"
      contains: "Legacy-FreeText-351"
    - path: "tests/e2e/cmp-records-351.spec.ts"
      provides: "Playwright UAT SF-03/04/05/07 + SEED_WORKFLOW lifecycle"
      contains: "cmp351-seed.sql"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Jurnal seed temporary [PENDING351]"
      contains: "PENDING351"
  key_links:
    - from: "tests/e2e/cmp-records-351.spec.ts"
      to: "tests/sql/cmp351-seed.sql"
      via: "db.execScript(path.resolve(__dirname, '../sql/cmp351-seed.sql'))"
      pattern: "cmp351-seed\\.sql"
    - from: "tests/e2e/cmp-records-351.spec.ts"
      to: "tests/helpers/accounts + dbSnapshot"
      via: "import dari '../helpers/accounts' dan '../helpers/dbSnapshot'"
      pattern: "from '\\.\\./helpers/"
---

<objective>
Wave 0 test infrastructure untuk Phase 351 (Nyquist). Buat SQL seed temporary + Playwright spec yang menjadi automated gate untuk SF-03/04/05/07. Spec ditulis lebih dulu dalam keadaan RED (DOM selector belum ada sampai Plan 02/03/04 selesai) — ini adalah kontrak verifikasi, bukan implementasi fitur.

Purpose: Tanpa Wave 0, plan SF-03/05/07 tidak punya `<automated>` verify (mereka view/JS-only → Playwright satu-satunya jalur otomatis). Seed off-master Kategori membuktikan SF-04 secara end-to-end.
Output: `tests/sql/cmp351-seed.sql` (temporary+local-only) + `tests/e2e/cmp-records-351.spec.ts` (4 test group) + entry `docs/SEED_JOURNAL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-RESEARCH.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-PATTERNS.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Seed analog — tests/sql/cmp350-seed.sql (copy header+lifecycle, swap table+value): -->
- SET NOCOUNT ON; DELETE WHERE prefix LIKE '[[]PENDING350]%'; DECLARE @uid via Email='rino.prasetyo@pertamina.com'; THROW jika null; INSERT; SELECT COUNT(*).
- 351 menukar target tabel ke TrainingRecords dan Kategori ke off-master 'Legacy-FreeText-351'.

<!-- TrainingRecord schema (Models/TrainingRecord.cs) — kolom yang relevan untuk INSERT: -->
- UserId NVARCHAR(450) NOT NULL (FK Users.Id)
- Judul NVARCHAR(200) nullable  → gunakan prefix '[PENDING351]'
- Kategori NVARCHAR(50) nullable → 'Legacy-FreeText-351' (OFF-MASTER, bukan di AssessmentCategories)
- Tanggal DATETIME NOT NULL → GETDATE()
- Status NVARCHAR(20) nullable → 'Valid' (status valid per docstring TrainingRecord)
- Sisanya nullable (Penyelenggara, SertifikatUrl, ValidUntil, CertificateType, TanggalMulai, TanggalSelesai, NomorSertifikat, Kota, SubKategori, RenewsTrainingId, RenewsSessionId) → boleh dihilangkan dari INSERT list.

<!-- Playwright helper contract (tests/helpers) — VERIFIED present (git ls-files): -->
From tests/helpers/accounts.ts:
  export const accounts = { manager: { email:'manager@pertamina.com', password:'123456', role:'Manager' }, ... }
  export type AccountKey = keyof typeof accounts;
From tests/helpers/dbSnapshot.ts (dipakai cmp350 spec):
  db.queryString(sql), db.backup(path), db.execScript(path), db.queryScalar(sql), db.restore(path)
From tests/e2e/cmp-records-350.spec.ts (analog scaffold — copy verbatim :19-63):
  import { test, expect, Page } from '@playwright/test';
  import { accounts, AccountKey } from '../helpers/accounts';
  import * as db from '../helpers/dbSnapshot';
  import * as path from 'node:path';
  async function loginAny(page, accountKey) { ... } // copy verbatim :26-35
  test.describe.configure({ mode: 'serial' });

<!-- DOM selectors the spec must target (final state delivered by Plan 02/03/04): -->
SF-03 (Worker Detail): #wdRecordCounter (text "Menampilkan X dari Y"), #workerDetailEmptyState (visible saat 0-match)
SF-04 (Worker Detail): #categoryFilter option dengan teks/value 'Legacy-FreeText-351'
SF-05 (My Records): #categoryFilter + #typeFilter ada; #typeFilter option value="assessment"/"training"
SF-07 (Records.cshtml): setelah klik "Back to Team View", #pane-team adalah tab AKTIF + #dateFrom value ter-restore
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat SQL seed cmp351-seed.sql (off-master Kategori + 0-match fixture)</name>
  <files>tests/sql/cmp351-seed.sql, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/sql/cmp350-seed.sql (analog penuh — header block, SET NOCOUNT ON, wipe-and-insert, THROW guard, SELECT COUNT trailing)
    - Models/TrainingRecord.cs (schema kolom TrainingRecords — confirm nullable set; hanya UserId/Judul/Kategori/Tanggal/Status yang diisi)
    - 351-PATTERNS.md "tests/sql/cmp351-seed.sql (CREATE, Wave 0)" (key delta + SEED_WORKFLOW)
    - CLAUDE.md "Seed Data Workflow" (temporary+local-only, snapshot/journal/restore)
  </read_first>
  <action>
Buat `tests/sql/cmp351-seed.sql` model verbatim pada `cmp350-seed.sql` header+lifecycle, dengan delta:

1. Header comment block (Bahasa Indonesia) jelaskan: tujuan = seed 1 TrainingRecord [PENDING351] dengan Kategori off-master 'Legacy-FreeText-351' (TIDAK ada di master AssessmentCategories) untuk worker rino.prasetyo. SEBELUM SF-04: kategori itu tak muncul di dropdown Worker Detail/My Records (opsi dari master). SETELAH SF-04: opsi actual-distinct memunculkannya + baris ter-filter. Record ini juga jadi data untuk SF-03 (search non-matching → empty-state). Klasifikasi: temporary + local-only. Cara run sqlcmd ke localhost\SQLEXPRESS HcPortalDB_Dev (JANGAN Dev/Prod).
2. `SET NOCOUNT ON;`
3. Idempotent wipe: `DELETE FROM TrainingRecords WHERE Judul LIKE '[[]PENDING351]%';`
4. Resolve uid:
   ```sql
   DECLARE @uid NVARCHAR(450) = (SELECT TOP 1 Id FROM Users WHERE Email = 'rino.prasetyo@pertamina.com');
   IF @uid IS NULL
       THROW 51351, 'Seed pre-condition gagal: user rino.prasetyo@pertamina.com tidak ditemukan di Users.', 1;
   ```
5. INSERT (hanya kolom non-null yang diperlukan — semua lain nullable per Models/TrainingRecord.cs):
   ```sql
   INSERT INTO TrainingRecords (UserId, Judul, Kategori, Tanggal, Status)
   VALUES (@uid, '[PENDING351] Legacy Training Migas', 'Legacy-FreeText-351', GETDATE(), 'Valid');
   ```
6. Trailing verify: `SELECT COUNT(*) AS Pending351Seeded FROM TrainingRecords WHERE Judul LIKE '[[]PENDING351]%';`

JANGAN seed kategori yang ada di master. JANGAN promosikan ke Data/SeedData.cs. Bracket-escape `[[]PENDING351]` di LIKE adalah WAJIB (mirror cmp350).
  </action>
  <verify>
    <automated>powershell -Command "if (Select-String -Path 'tests/sql/cmp351-seed.sql' -Pattern 'Legacy-FreeText-351' -Quiet) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/sql/cmp351-seed.sql` ada.
    - Memuat literal `Legacy-FreeText-351`.
    - Memuat `INSERT INTO TrainingRecords`.
    - Memuat `[[]PENDING351]` (bracket-escaped LIKE) dan `THROW 51351`.
    - INSERT TIDAK menyentuh tabel AssessmentCategories (off-master).
  </acceptance_criteria>
  <done>Seed file ada, off-master Kategori 'Legacy-FreeText-351', idempotent wipe-and-insert, THROW guard, trailing COUNT.</done>
</task>

<task type="auto">
  <name>Task 2: Buat Playwright spec cmp-records-351.spec.ts (4 test group RED) + SEED_JOURNAL entry</name>
  <files>tests/e2e/cmp-records-351.spec.ts, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/cmp-records-350.spec.ts (scaffold penuh :1-75 — copy beforeAll/afterAll + loginAny verbatim, swap 350→351)
    - tests/helpers/accounts.ts (key 'manager' = L3, dipakai untuk Team View + Worker Detail cross-section)
    - 351-PATTERNS.md "tests/e2e/cmp-records-351.spec.ts (CREATE, Wave 0)" (struktur reuse + auth + Pitfall 4 SF-07 active-tab assert)
    - 351-RESEARCH.md "Common Pitfalls" Pitfall 2/3/4 (Tipe value-map, data-category, SF-07 false-pass)
    - docs/SEED_JOURNAL.md (format entry existing)
  </read_first>
  <action>
Buat `tests/e2e/cmp-records-351.spec.ts`. Copy scaffold dari `cmp-records-350.spec.ts`:
- Import block identik: `@playwright/test`, `../helpers/accounts`, `../helpers/dbSnapshot`, `node:path`.
- `loginAny()` verbatim (:26-35).
- `test.describe.configure({ mode: 'serial' });`
- `test.describe('Phase 351 — Worker Detail + cross-surface filter consistency', () => { ... })`.
- `beforeAll`: backup ke `HcPortalDB_Dev-pre351-${ts}.bak` → `db.execScript(path.resolve(__dirname, '../sql/cmp351-seed.sql'))` → Layer-1 assert `SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE '[[]PENDING351]%'` > 0.
- `afterAll`: restore (success ATAU fail) + unlink .bak + Layer-4 assert remaining = 0.

Tambah 4 test (boleh RED sampai Plan 02/03/04; assertion menargetkan selector FINAL):

1. `test('SF-03 0-match — empty-state + counter')`: login `manager` → goto `/CMP/RecordsWorkerDetail?workerId=<rino id>` (resolve via `db.queryString` Email='rino.prasetyo@pertamina.com', atau navigasi via Team View → klik Detail rino). Ketik di `#searchInput` term yang tidak match (mis. 'zzz-nomatch-zzz'). Assert `#workerDetailEmptyState` visible DAN `#wdRecordCounter` bertekstkan `Menampilkan 0 dari`.
2. `test('SF-04 legacy-Kategori filters')`: di Worker Detail rino → assert `#categoryFilter` punya option dengan value/teks `Legacy-FreeText-351`. Pilih option itu → assert baris `[PENDING351] Legacy Training Migas` visible, baris lain hidden.
3. `test('SF-05 parity — My Records Kategori+Tipe value-map')`: login `coachee` (rino) ATAU `manager`, goto `/CMP/Records` (My Records aktif). Assert `#categoryFilter` dan `#typeFilter` ada. Pilih `#typeFilter` value `assessment` → assert baris `data-type="training"` jadi hidden, baris `data-type="assessment"` tetap visible (T2 guard).
4. `test('SF-07 back-nav — Team tab active + restored filter')`: login `manager`, goto `/CMP/Records`, aktifkan Team View, set `#dateFrom` (atau searchScope), klik Detail worker → di Worker Detail klik "Back to Team View". Assert tanpa klik manual: `#pane-team` ber-class `active`/visible DAN `#dateFrom` value ter-restore (Pitfall 4: jangan klik tab manual).

Auth: gunakan `manager` (L3) untuk test 1/2/4. Untuk My Records test 3 gunakan account yang punya record assessment+training (coachee 'rino.prasetyo' atau manager — pilih yang menampilkan kedua tipe baris; dokumentasikan pilihan dalam komentar). JANGAN buat helper path baru — `../helpers/accounts` + `../helpers/dbSnapshot` resolve benar dari `tests/e2e/`.

Tambah header comment SEED_WORKFLOW (mirror cmp350 :5-17). Lalu tambah entry baru di `docs/SEED_JOURNAL.md`: tanggal 2026-06-05, slug cmp351, tujuan (off-master Kategori SF-04 + 0-match SF-03), klasifikasi temporary+local-only, entitas tersentuh = TrainingRecords prefix [PENDING351], status `pending` (akan ditandai `cleaned` setelah afterAll restore).
  </action>
  <verify>
    <automated>cd tests; npx playwright test e2e/cmp-records-351.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/cmp-records-351.spec.ts` ada.
    - `npx playwright test e2e/cmp-records-351.spec.ts --list` menemukan ≥4 test (tidak error parse/import).
    - Spec memuat `cmp351-seed.sql` (path.resolve), `from '../helpers/accounts'`, `from '../helpers/dbSnapshot'`.
    - Spec memuat selector `#workerDetailEmptyState`, `#wdRecordCounter`, `#categoryFilter`, `#typeFilter`, `#pane-team`, `Legacy-FreeText-351`.
    - Spec memuat `Menampilkan 0 dari` (SF-03 counter assert) dan assert `data-type` (T2 guard).
    - `docs/SEED_JOURNAL.md` memuat entry `[PENDING351]` / `cmp351`.
  </acceptance_criteria>
  <done>Spec ter-load Playwright (--list sukses), 4 test group menargetkan selector final, SEED_JOURNAL entry ada. RED state diterima (kode fitur belum ada).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → local SQL Server | Seed script menulis ke HcPortalDB_Dev lokal (localhost\SQLEXPRESS) — temporary+local-only, di-restore di afterAll |
| Playwright → app localhost:5277 | Browser-driven UAT terhadap app lokal; auth via real login form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-SEED | Tampering | cmp351-seed.sql data residue di DB lokal | mitigate | SEED_WORKFLOW: backup sebelum seed, restore di afterAll (success ATAU fail), Layer-4 assert remaining=0; prefix [PENDING351] untuk wipe idempotent; JANGAN run di Dev/Prod |
| T-351-CRED | Information Disclosure | accounts.ts password lokal '123456' | accept | Kredensial dev-only lokal (per reference_dev_credentials memory), bukan staging/prod; sudah ter-commit di harness existing |
</threat_model>

<verification>
- `powershell Select-String tests/sql/cmp351-seed.sql -Pattern Legacy-FreeText-351` → match.
- `cd tests; npx playwright test e2e/cmp-records-351.spec.ts --list` → ≥4 test terdaftar tanpa error import.
- `docs/SEED_JOURNAL.md` memuat entry cmp351.
- CATATAN: test BELUM hijau (RED) — itu benar; Plan 02/03/04 yang membuat selector hijau.
</verification>

<success_criteria>
- `tests/sql/cmp351-seed.sql` ada, off-master 'Legacy-FreeText-351', idempotent, THROW guard.
- `tests/e2e/cmp-records-351.spec.ts` ada, ter-load, 4 test group menargetkan selector SF-03/04/05/07 final + SEED_WORKFLOW lifecycle (backup/seed/restore/Layer-4).
- SEED_JOURNAL entry [PENDING351] tercatat.
</success_criteria>

<output>
After completion, create `.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-01-SUMMARY.md`
</output>
