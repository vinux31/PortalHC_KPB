---
phase: 351-worker-detail-cross-surface-filter-consistency
plan: 03
type: execute
wave: 2
depends_on: [351-02]
files_modified:
  - Views/CMP/RecordsWorkerDetail.cshtml
autonomous: true
requirements: [SF-03, SF-04]
must_haves:
  truths:
    - "Worker Detail menampilkan counter 'Menampilkan X dari Y' (aria-live=polite) yang ikut filter aktif"
    - "Saat semua baris ter-filter habis (0 match), baris #workerDetailEmptyState injected dengan teks 'Tidak ada hasil untuk filter ini.'"
    - "Dropdown Kategori Worker Detail dibangun dari ViewBag.ActualCategoriesJson (record aktual), bukan master"
    - "Compare exact-equals category === categoryFilter tetap, server empty-state 'Belum ada data' tetap utuh"
  artifacts:
    - path: "Views/CMP/RecordsWorkerDetail.cshtml"
      provides: "Counter + filtered-empty-state + Kategori actual-source"
      contains: "workerDetailEmptyState"
  key_links:
    - from: "RecordsWorkerDetail.cshtml filterTable()"
      to: "#workerDetailEmptyState + #wdRecordCounter"
      via: "visibleCount loop → inject/hide empty row + set counter textContent"
      pattern: "workerDetailEmptyState"
    - from: "RecordsWorkerDetail.cshtml Kategori <select>"
      to: "ViewBag.ActualCategoriesJson"
      via: "JsonSerializer.Deserialize ganti dari MasterCategoriesJson"
      pattern: "ActualCategoriesJson"
---

<objective>
SF-03 + SF-04 view-side pada `Views/CMP/RecordsWorkerDetail.cshtml`:
- SF-03: mirror pola My Records — track `visibleCount`, inject baris empty-state `#workerDetailEmptyState` saat 0-match, tambah counter "Menampilkan X dari Y" (`aria-live="polite"`).
- SF-04: ganti sumber opsi `<select id="categoryFilter">` dari `ViewBag.MasterCategoriesJson` → `ViewBag.ActualCategoriesJson` (disediakan Plan 02). Markup loop tetap, hanya sumber deserialize berubah.

Purpose: feedback 0-match + a11y parity dengan My Records/Team View; Kategori cocok record aktual.
Output: `RecordsWorkerDetail.cshtml` (counter element + filterTable extend + ViewBag key swap).

CRITICAL (locked / 3 traps): (T1) string baru WAJIB verbatim "Tidak ada hasil untuk filter ini." — JANGAN unify dengan server-side "Belum ada data" (`:205`). `aria-live="polite"` NON-NEGOTIABLE. Compare exact-equals `:352` tetap. SubKategori cascade master-based tidak disentuh. Tidak ada CSS baru (reuse records.css + Bootstrap utilities). colspan="7".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-UI-SPEC.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-PATTERNS.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- My Records empty-state TEMPLATE (Records.cshtml:365-379) — copy, rename id, ubah string: -->
var emptyRow = document.getElementById('myRecordsEmptyState');   // → workerDetailEmptyState
if (visibleCount === 0 && document.querySelectorAll('#recordsTable .training-row').length > 0) {
    if (!emptyRow) {
        var tbody = document.querySelector('#recordsTable tbody');
        var tr = document.createElement('tr');
        tr.id = 'myRecordsEmptyState';                            // → workerDetailEmptyState
        tr.innerHTML = '<td colspan="7" class="text-center p-5 text-muted"><i class="bi bi-inbox fs-1 d-block mb-2"></i>Data belum ada</td>';
        //                                                         T1: ganti 'Data belum ada' → 'Tidak ada hasil untuk filter ini.'
        tbody.appendChild(tr);
    } else { emptyRow.style.display = ''; }
} else if (emptyRow) { emptyRow.style.display = 'none'; }

<!-- Worker Detail filterTable() SAAT INI (RecordsWorkerDetail.cshtml:336-358) — hanya toggle display: -->
function filterTable() {
    const searchTerm = ...; const categoryFilter = ...; const subCategoryFilter = ...;
    const yearFilter = ...; const typeFilter = ...;
    document.querySelectorAll('#recordsTable .training-row').forEach(row => {
        // 5 compare (search/category/subcategory/year/type) — KEEP AS-IS
        row.style.display = (matchSearch && matchYear && matchCategory && matchSubCategory && matchType) ? '' : 'none';
    });
}

<!-- Kategori <select> SAAT INI (RecordsWorkerDetail.cshtml:140-147): -->
var masterCategories = System.Text.Json.JsonSerializer.Deserialize<List<string>>(
    (string)(ViewBag.MasterCategoriesJson ?? "[]"));   // → ViewBag.ActualCategoriesJson (Plan 02)
@foreach (var cat in masterCategories) { <option value="@cat">@cat</option> }

<!-- Tabel: 7 kolom (Tanggal, Nama Kegiatan, Tipe, Kategori, Sub Kategori, Status, Action) → colspan="7" -->
<!-- @unifiedRecords.Count tersedia di scope view (var di :11, count assessment/training :14-15) -->
<!-- Counter element placement: di atas card tabel (~:184) — UI-SPEC C1 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SF-04 — swap sumber opsi Kategori ke ActualCategoriesJson</name>
  <files>Views/CMP/RecordsWorkerDetail.cshtml</files>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml :136-149 (Kategori select + deserialize MasterCategoriesJson)
    - 351-PATTERNS.md "SF-04 — Kategori options from actual records (view side)"
    - 351-CONTEXT.md D-02 (compare exact-equals dipertahankan)
  </read_first>
  <action>
Di `RecordsWorkerDetail.cshtml` blok `@{ var masterCategories = ... ViewBag.MasterCategoriesJson ... }` (`:140-143`), ganti sumber deserialize dari `ViewBag.MasterCategoriesJson` → `ViewBag.ActualCategoriesJson`:
```cshtml
@{
    var actualCategories = System.Text.Json.JsonSerializer.Deserialize<List<string>>(
        (string)(ViewBag.ActualCategoriesJson ?? "[]"));
}
@foreach (var cat in actualCategories)
{
    <option value="@cat">@cat</option>
}
```
- Markup `<select id="categoryFilter">` + `<option value="">Semua Kategori</option>` TIDAK berubah; hanya sumber loop.
- JANGAN sentuh `subCategoryMap` / `ViewBag.SubCategoryMapJson` (`:334`, `:361-375`) — SubKategori cascade master-based out of scope.
- JANGAN ubah compare exact-equals `category === categoryFilter` (`:352`).
  </action>
  <verify>
    <automated>powershell -Command "$a = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'ViewBag.ActualCategoriesJson'; $sub = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'ViewBag.SubCategoryMapJson'; $cmp = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'category === categoryFilter'; if ($a -and $sub -and $cmp) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `RecordsWorkerDetail.cshtml` memuat `ViewBag.ActualCategoriesJson`.
    - `RecordsWorkerDetail.cshtml` MASIH memuat `ViewBag.SubCategoryMapJson` (SubKategori utuh).
    - `RecordsWorkerDetail.cshtml` MASIH memuat `category === categoryFilter` (compare exact-equals utuh).
    - TIDAK ada lagi `ViewBag.MasterCategoriesJson` sebagai sumber option Kategori Worker Detail.
  </acceptance_criteria>
  <done>Opsi Kategori dibaca dari ActualCategoriesJson; SubCategoryMap + compare exact-equals tetap.</done>
</task>

<task type="auto">
  <name>Task 2: SF-03 — counter element + filterTable visibleCount + filtered-empty-state</name>
  <files>Views/CMP/RecordsWorkerDetail.cshtml</files>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml :183-208 (card tabel + server empty-state) dan :336-388 (filterTable + listeners + clearFilters)
    - Views/CMP/Records.cshtml :334-380 (TEMPLATE counter + empty-state — copy near-verbatim)
    - 351-UI-SPEC.md C1 (counter markup contract + empty-state verbatim shape) + Copywriting Contract
    - 351-RESEARCH.md Pitfall 1 (copy divergence) + Pitfall 5 (jangan restructure agresif)
  </read_first>
  <action>
**1. Counter element (NEW)** — letakkan tepat di atas `<!-- Unified Records Table -->` card (`~:183`), reuse class label existing:
```html
<div class="small text-muted mb-2" id="wdRecordCounter" aria-live="polite">
    Menampilkan @unifiedRecords.Count dari @unifiedRecords.Count
</div>
```
`aria-live="polite"` NON-NEGOTIABLE (D-01).

**2. Extend `filterTable()`** (`:336-358`) — tambah `visibleCount`, restructure ke var `show`, tambah counter update + empty-state block (KEEP 5 compare existing):
```javascript
function filterTable() {
    const searchTerm = document.getElementById('searchInput').value.toLowerCase();
    const categoryFilter = document.getElementById('categoryFilter').value.toLowerCase();
    const subCategoryFilter = document.getElementById('subCategoryFilter').value.toLowerCase();
    const yearFilter = document.getElementById('yearFilter').value;
    const typeFilter = document.getElementById('typeFilter').value.toLowerCase();
    let visibleCount = 0;
    const allRows = document.querySelectorAll('#recordsTable .training-row');
    allRows.forEach(row => {
        const title = row.getAttribute('data-title');
        const year = row.getAttribute('data-year');
        const category = row.getAttribute('data-category');
        const subcategory = row.getAttribute('data-subcategory');
        const type = row.getAttribute('data-type');
        const matchSearch = !searchTerm || title.includes(searchTerm);
        const matchYear = !yearFilter || year === yearFilter;
        const matchCategory = !categoryFilter || category === categoryFilter;
        const matchSubCategory = !subCategoryFilter || subcategory === subCategoryFilter;
        const matchType = !typeFilter || type === typeFilter;
        const show = matchSearch && matchYear && matchCategory && matchSubCategory && matchType;
        row.style.display = show ? '' : 'none';
        if (show) visibleCount++;
    });

    // Counter update
    var counter = document.getElementById('wdRecordCounter');
    if (counter) counter.textContent = 'Menampilkan ' + visibleCount + ' dari ' + allRows.length;

    // Filtered-empty-state (mirror My Records Records.cshtml:365-379) — string T1
    var emptyRow = document.getElementById('workerDetailEmptyState');
    if (visibleCount === 0 && allRows.length > 0) {
        if (!emptyRow) {
            var tbody = document.querySelector('#recordsTable tbody');
            var tr = document.createElement('tr');
            tr.id = 'workerDetailEmptyState';
            tr.innerHTML = '<td colspan="7" class="text-center p-5 text-muted"><i class="bi bi-inbox fs-1 d-block mb-2"></i>Tidak ada hasil untuk filter ini.</td>';
            tbody.appendChild(tr);
        } else {
            emptyRow.style.display = '';
        }
    } else if (emptyRow) {
        emptyRow.style.display = 'none';
    }
}
```
- Listeners (`:360-378`) + `clearFilters()` (`:380-388`) sudah memanggil `filterTable()` → counter/empty-state otomatis ter-update; tidak perlu diubah selain memastikan `filterTable()` dipanggil sekali on-load jika belum (jika perlu, panggil `filterTable()` sekali setelah definisi untuk inisialisasi counter — opsional, tapi rekomendasi agar counter awal akurat).
- JANGAN sentuh server-side empty-state `:200-208` ("Belum ada data") — itu no-data state, beda dari filtered-to-zero.
- T1: string injected WAJIB `Tidak ada hasil untuk filter ini.` verbatim. `colspan="7"`.
- Tidak ada CSS/file baru.
  </action>
  <verify>
    <automated>dotnet build; powershell -Command "$c = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'wdRecordCounter'; $aria = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'aria-live=\"polite\"'; $empty = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'workerDetailEmptyState'; $copy = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'Tidak ada hasil untuk filter ini\.'; $server = Select-String -Path 'Views/CMP/RecordsWorkerDetail.cshtml' -Pattern 'Belum ada data'; if ($c -and $aria -and $empty -and $copy -and $server) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `RecordsWorkerDetail.cshtml` memuat element `id="wdRecordCounter"` dengan `aria-live="polite"`.
    - `RecordsWorkerDetail.cshtml` memuat `workerDetailEmptyState` (inject id).
    - `RecordsWorkerDetail.cshtml` memuat string verbatim `Tidak ada hasil untuk filter ini.` (T1).
    - `RecordsWorkerDetail.cshtml` MASIH memuat `Belum ada data` (server-side no-data state utuh — T1 divergence).
    - `RecordsWorkerDetail.cshtml` memuat `colspan="7"` pada baris empty-state injected.
    - filterTable men-set `wdRecordCounter.textContent` ber-pola `Menampilkan ... dari ...`.
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Counter aria-live ada; filterTable track visibleCount + inject/hide #workerDetailEmptyState dengan string verbatim; server "Belum ada data" tidak tersentuh; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewBag.ActualCategoriesJson → view option render | nilai kategori (sebagian free-text) di-render sebagai `<option>` |
| client JS filterTable | murni DOM mutation client-side; tidak ada data baru ke server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-XSS | Tampering/XSS | render `<option value="@cat">@cat</option>` dari kategori free-text | mitigate | Razor `@cat` auto-HTML-encode; nilai berasal dari `JsonSerializer.Deserialize` server-side (bukan `@Html.Raw`). JANGAN inject raw category ke inline JS. Severity low |
| T-351-VIEW | Information Disclosure | counter/empty-state view-only | accept | Tidak ada perubahan data/akses; murni feedback DOM atas record yang sudah terotorisasi (authz di Plan 02, tidak disentuh) |
</threat_model>

<verification>
- `dotnet build` 0 error.
- Grep RecordsWorkerDetail.cshtml: `wdRecordCounter`, `aria-live="polite"`, `workerDetailEmptyState`, `Tidak ada hasil untuk filter ini.`, `Belum ada data` (preserve), `ViewBag.ActualCategoriesJson`, `ViewBag.SubCategoryMapJson` (preserve), `category === categoryFilter` (preserve).
- Wave gate (setelah Plan 04): `dotnet run` :5277 + `npx playwright test e2e/cmp-records-351.spec.ts -g "0-match"` + `-g "Kategori"` hijau.
</verification>

<success_criteria>
- Counter "Menampilkan X dari Y" + aria-live ter-render dan update per filter.
- #workerDetailEmptyState injected saat 0-match dengan string verbatim; server no-data state utuh.
- Opsi Kategori dari ActualCategoriesJson; compare + SubCategoryMap utuh.
- build hijau; SF-03 0-match + SF-04 Kategori Playwright group hijau di wave gate.
</success_criteria>

<output>
After completion, create `.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-03-SUMMARY.md`
</output>
