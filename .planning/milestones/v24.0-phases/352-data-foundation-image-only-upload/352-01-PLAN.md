---
phase: 352-data-foundation-image-only-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentPackage.cs
  - Models/AssessmentConstants.cs
  - Helpers/FileUploadHelper.cs
  - HcPortal.Tests/FileUploadHelperTests.cs
  - Migrations/  # EF-generated AddImageToPackageQuestionAndOption + ApplicationDbContextModelSnapshot.cs
autonomous: true
requirements: [IMG-04]
user_setup: []

must_haves:
  truths:
    - "Entity PackageQuestion punya ImagePath (string?) + ImageAlt (string?)"
    - "Entity PackageOption punya ImagePath (string?) + ImageAlt (string?)"
    - "AssessmentConstants.FileValidation punya AllowedImageExtensions {.jpg,.jpeg,.png} + MaxImageFileSizeBytes (5MB)"
    - "FileUploadHelper punya ValidateImageFile(IFormFile?) yang menolak PDF/non-image, terima JPG/PNG, batas 5MB"
    - "Migration AddImageToPackageQuestionAndOption menambah 4 kolom nullable dan applied ke DB lokal"
    - "dotnet build dan dotnet test lulus (helper image-only ter-cover)"
  artifacts:
    - path: "Models/AssessmentPackage.cs"
      provides: "4 properti gambar nullable di PackageQuestion + PackageOption"
      contains: "public string? ImagePath"
    - path: "Models/AssessmentConstants.cs"
      provides: "Konstanta image-only (extensions allowlist + 5MB cap)"
      contains: "MaxImageFileSizeBytes"
    - path: "Helpers/FileUploadHelper.cs"
      provides: "Method validasi image-only ValidateImageFile"
      contains: "ValidateImageFile"
    - path: "HcPortal.Tests/FileUploadHelperTests.cs"
      provides: "xUnit coverage ValidateImageFile (valid/PDF-reject/magic-byte/oversize/null)"
      contains: "ValidateImageFile"
    - path: "Migrations/"
      provides: "EF migration 4 kolom nullable + snapshot terupdate"
      contains: "AddImageToPackageQuestionAndOption"
  key_links:
    - from: "Helpers/FileUploadHelper.cs ValidateImageFile"
      to: "AssessmentConstants.FileValidation.AllowedImageExtensions + MaxImageFileSizeBytes + MatchesMagicByte"
      via: "static reference"
      pattern: "AssessmentConstants\\.FileValidation\\.(AllowedImageExtensions|MaxImageFileSizeBytes|MatchesMagicByte)"
    - from: "Migrations/*_AddImageToPackageQuestionAndOption"
      to: "PackageQuestions + PackageOptions tables"
      via: "AddColumn ImagePath/ImageAlt (nullable)"
      pattern: "AddColumn.*Image(Path|Alt)"
---

<objective>
Membangun pondasi data + validasi upload untuk fitur "gambar di soal assessment".

Tiga deliverable (scope ketat — TIDAK ada controller/form/render/sync):
1. **Entity** — 4 properti gambar nullable di `PackageQuestion` + `PackageOption`.
2. **Validasi image-only** — konstanta baru di `AssessmentConstants.FileValidation` + method `ValidateImageFile` di `FileUploadHelper` (mirror `ValidateCertificateFile`, image-only, cap 5MB) + xUnit coverage.
3. **Migration** — EF migration 4 kolom nullable + apply ke DB lokal + verifikasi build/test (per CLAUDE.md Develop Workflow).

Purpose: REQ IMG-04 — phase 353 (admin CRUD) akan memanggil `ValidateImageFile` + menulis kolom; phase 352 hanya menyediakan method + konstanta + entity + migration. Belum ada call-site.
Output: 4 file C# diubah + 1 EF migration di-commit (flag IT-notify saat shipped).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/352-data-foundation-image-only-upload/352-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Kontrak existing yang di-mirror/di-reuse. Executor pakai langsung — tidak perlu eksplorasi codebase. -->

Pola validasi yang DI-MIRROR (Helpers/FileUploadHelper.cs, ValidateCertificateFile L13-39):
```csharp
public static (bool IsValid, string? Error) ValidateCertificateFile(IFormFile? file)
{
    if (file == null || file.Length == 0) return (true, null);
    var ext = Path.GetExtension(file.FileName).ToLowerInvariant();
    if (!AssessmentConstants.FileValidation.AllowedCertificateExtensions.Contains(ext))
        return (false, "Hanya file PDF, JPG, dan PNG yang diperbolehkan.");
    if (file.Length > AssessmentConstants.FileValidation.MaxCertificateFileSizeBytes)
        return (false, "Ukuran file maksimal 10MB.");
    using var stream = file.OpenReadStream();
    var header = new byte[8];
    var read = stream.Read(header, 0, 8);
    stream.Position = 0;                      // reset agar SaveFileAsync dapat full stream
    if (read < 3)
        return (false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).");
    if (!AssessmentConstants.FileValidation.MatchesMagicByte(ext, header))
        return (false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).");
    return (true, null);
}
```

Konstanta existing yang DI-REUSE (Models/AssessmentConstants.cs, FileValidation L30-74):
```csharp
public const long MaxCertificateFileSizeBytes = 10 * 1024 * 1024; // jangan dipakai untuk gambar
public static readonly HashSet<string> AllowedCertificateExtensions = new(StringComparer.OrdinalIgnoreCase) { ".pdf", ".jpg", ".jpeg", ".png" };
public static readonly Dictionary<string, byte[][]> MagicBytes = ...   // sudah ada .jpg/.jpeg/.png — REUSE, jangan tambah entri
public static bool MatchesMagicByte(string ext, byte[] header)        // REUSE apa adanya
```

Pola test existing yang DI-MIRROR (HcPortal.Tests/FileUploadHelperTests.cs):
```csharp
private static IFormFile MakeFile(string fileName, byte[] content) { ... } // in-memory FormFile, sudah ada
// JPG header: { 0xFF, 0xD8, 0xFF, 0xE0, ... }
// PNG header: { 0x89, 0x50, 0x4E, 0x47, ... }
// PDF header: { 0x25, 0x50, 0x44, 0x46, ... }  (untuk uji TOLAK di image-only)
// exe/MZ:    { 0x4D, 0x5A, 0x90, ... }          (untuk uji magic-byte mismatch)
```

Entity target (Models/AssessmentPackage.cs): `PackageQuestion` mulai L27, `PackageOption` mulai L63.
DbSet existing (Data/ApplicationDbContext.cs L53-54): `PackageQuestions`, `PackageOptions` — tabel target migration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tambah konstanta image-only + entity gambar + method ValidateImageFile (TDD)</name>
  <files>Models/AssessmentConstants.cs, Helpers/FileUploadHelper.cs, Models/AssessmentPackage.cs, HcPortal.Tests/FileUploadHelperTests.cs</files>
  <read_first>
    - Models/AssessmentConstants.cs (FileValidation L30-74 — lokasi tambah konstanta, MagicBytes dict untuk REUSE)
    - Helpers/FileUploadHelper.cs (ValidateCertificateFile L13-39 — pola yang di-mirror)
    - Models/AssessmentPackage.cs (PackageQuestion L27, PackageOption L63 — target properti)
    - HcPortal.Tests/FileUploadHelperTests.cs (MakeFile helper + pola [Fact] + assertion error string)
  </read_first>
  <behavior>
    ValidateImageFile(IFormFile?):
    - Test 1: file null → (true, null) [kontrak sama cert helper]
    - Test 2: file.Length == 0 → (true, null)
    - Test 3: JPG valid (header FF D8 FF, ext .jpg) → (true, _)
    - Test 4: PNG valid (header 89 50 4E 47, ext .png) → (true, _)
    - Test 5: JPEG valid (ext .jpeg, header FF D8 FF) → (true, _)
    - Test 6: PDF (ext .pdf, header %PDF) → (false, err mengandung "JPG") — PDF DITOLAK karena ext tak ada di AllowedImageExtensions
    - Test 7: exe rename jadi .png (header MZ 4D 5A) → (false, err mengandung "magic byte")
    - Test 8: oversize > 5MB (byte array 5MB+1, ext .png header valid) → (false, err mengandung "5MB")
  </behavior>
  <action>
    1. **Models/AssessmentConstants.cs** — di dalam `public static class FileValidation`, TAMBAH (jangan ubah konstanta cert existing):
       ```csharp
       // Phase 352 IMG-04 / D-03: cap khusus gambar 5MB (BUKAN 10MB cert).
       public const long MaxImageFileSizeBytes = 5 * 1024 * 1024; // 5MB

       // Phase 352 IMG-04 / D-01/D-02: image-only allowlist (JPG/PNG, termasuk .jpeg) — TANPA .pdf.
       public static readonly HashSet<string> AllowedImageExtensions = new(StringComparer.OrdinalIgnoreCase)
       {
           ".jpg", ".jpeg", ".png"
       };
       ```
       JANGAN tambah entri baru ke `MagicBytes` — entri .jpg/.jpeg/.png sudah ada (D-02), di-reuse via `MatchesMagicByte`.

    2. **Helpers/FileUploadHelper.cs** — TAMBAH method baru (D-05: terpisah, jangan ubah `ValidateCertificateFile`), mirror identik tapi pakai konstanta image-only:
       ```csharp
       /// <summary>
       /// Phase 352 IMG-04 / D-01..D-05: Validasi upload gambar soal/opsi — JPG/PNG saja (tolak PDF/non-image),
       /// cap 5MB, magic-byte reuse. Mirror ValidateCertificateFile; read<3 guard + stream.Position=0 dipertahankan identik.
       /// </summary>
       public static (bool IsValid, string? Error) ValidateImageFile(IFormFile? file)
       {
           if (file == null || file.Length == 0) return (true, null);

           var ext = Path.GetExtension(file.FileName).ToLowerInvariant();
           if (!AssessmentConstants.FileValidation.AllowedImageExtensions.Contains(ext))
               return (false, "Hanya file gambar JPG dan PNG yang diperbolehkan.");

           if (file.Length > AssessmentConstants.FileValidation.MaxImageFileSizeBytes)
               return (false, "Ukuran gambar maksimal 5MB.");

           using var stream = file.OpenReadStream();
           var header = new byte[8];
           var read = stream.Read(header, 0, 8);
           stream.Position = 0; // reset agar SaveFileAsync tetap dapat full stream

           if (read < 3)
               return (false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).");

           if (!AssessmentConstants.FileValidation.MatchesMagicByte(ext, header))
               return (false, "Isi file tidak cocok dengan ekstensi (magic byte mismatch).");

           return (true, null);
       }
       ```

    3. **Models/AssessmentPackage.cs** — di kelas `PackageQuestion` (setelah `MaxCharacters` L57, sebelum `// Navigation`) TAMBAH:
       ```csharp
       /// <summary>Phase 352 IMG-04: path relatif gambar soal (nullable). Diisi via FileUploadHelper.SaveFileAsync phase 353.</summary>
       public string? ImagePath { get; set; }

       /// <summary>Phase 352 IMG-04: teks alternatif gambar soal (aksesibilitas), maks 255 char. Nullable.</summary>
       [System.ComponentModel.DataAnnotations.MaxLength(255)]
       public string? ImageAlt { get; set; }
       ```
       Di kelas `PackageOption` (setelah `IsCorrect` L79) TAMBAH 2 properti identik (komentar "gambar opsi" alih-alih "gambar soal"):
       ```csharp
       /// <summary>Phase 352 IMG-04: path relatif gambar opsi (nullable).</summary>
       public string? ImagePath { get; set; }

       /// <summary>Phase 352 IMG-04: teks alternatif gambar opsi, maks 255 char. Nullable.</summary>
       [System.ComponentModel.DataAnnotations.MaxLength(255)]
       public string? ImageAlt { get; set; }
       ```
       Catatan: `[MaxLength(255)]` membuat EF generate `nvarchar(255)` untuk ImageAlt; `ImagePath` tanpa attribute → `nvarchar(max)` (D-06).

    4. **HcPortal.Tests/FileUploadHelperTests.cs** — TAMBAH 8 [Fact] sesuai blok `<behavior>` di atas, reuse `MakeFile`. Untuk oversize: `var big = new byte[5 * 1024 * 1024 + 1]; big[0]=0x89; big[1]=0x50; big[2]=0x4E; big[3]=0x47;` lalu `MakeFile("big.png", big)`. Assertion error string: PDF-reject → `Assert.Contains("JPG", err!)`; magic mismatch → `Assert.Contains("magic byte", err!)`; oversize → `Assert.Contains("5MB", err!)`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ValidateImageFile" 2>&1 | Select-String -Pattern "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - Models/AssessmentConstants.cs mengandung `MaxImageFileSizeBytes = 5 * 1024 * 1024`
    - Models/AssessmentConstants.cs mengandung `AllowedImageExtensions` dengan `.jpg`, `.jpeg`, `.png` dan TANPA `.pdf`
    - MagicBytes dict TIDAK bertambah entri (masih 4 key: .pdf/.jpg/.jpeg/.png)
    - Helpers/FileUploadHelper.cs mengandung `public static (bool IsValid, string? Error) ValidateImageFile(IFormFile? file)`
    - ValidateImageFile mengandung `stream.Position = 0` dan guard `read < 3` (identik pola cert)
    - ValidateImageFile mereferensikan `AllowedImageExtensions` dan `MaxImageFileSizeBytes` (BUKAN konstanta cert)
    - Models/AssessmentPackage.cs: PackageQuestion DAN PackageOption masing-masing punya `public string? ImagePath` + `public string? ImageAlt` (total 4 properti baru)
    - ImageAlt punya `[MaxLength(255)]` (atau `[System.ComponentModel.DataAnnotations.MaxLength(255)]`)
    - ValidateCertificateFile TIDAK diubah (signature + body identik)
    - HcPortal.Tests/FileUploadHelperTests.cs mengandung minimal 8 [Fact] baru bernama *ValidateImageFile*
    - `dotnet build` exit 0 (Build succeeded)
    - `dotnet test --filter ValidateImageFile` semua Passed
  </acceptance_criteria>
  <done>Konstanta + helper + entity + test ada; build + test ValidateImageFile lulus.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate + apply EF migration 4 kolom nullable + verifikasi build/test full</name>
  <files>Migrations/ (EF-generated *_AddImageToPackageQuestionAndOption.cs + ApplicationDbContextModelSnapshot.cs)</files>
  <read_first>
    - CLAUDE.md (Develop Workflow — verifikasi lokal wajib, snapshot DB sebelum apply, flag IT)
    - Data/ApplicationDbContext.cs (L53-54 DbSet PackageQuestions/PackageOptions)
    - Models/AssessmentPackage.cs (hasil Task 1 — 4 properti baru jadi sumber migration)
  </read_first>
  <action>
    PRASYARAT: Task 1 selesai (entity sudah punya 4 properti) — migration di-generate DARI perubahan entity Task 1, jadi WAJIB urutan setelah Task 1.

    1. **Snapshot DB lokal** (kebiasaan baik per CLAUDE.md; kolom nullable aman tapi tetap backup). Jalankan SQL Server BACKUP DB lokal (`HcPortalDB` / nama DB di appsettings ConnectionStrings DefaultConnection) ke file .bak di lokasi temp. Bila tool sqlcmd tak tersedia di environment, catat sebagai langkah manual yang sudah/akan dilakukan dan lanjut (kolom nullable = non-destruktif).

    2. **Generate migration:**
       ```
       dotnet ef migrations add AddImageToPackageQuestionAndOption
       ```
       Verifikasi file `Migrations/<timestamp>_AddImageToPackageQuestionAndOption.cs` ter-generate dengan 4 `AddColumn` (PackageQuestions.ImagePath nvarchar(max) nullable, PackageQuestions.ImageAlt nvarchar(255) nullable, PackageOptions.ImagePath nvarchar(max) nullable, PackageOptions.ImageAlt nvarchar(255) nullable) dan `Up()`/`Down()` simetris. Pastikan `ApplicationDbContextModelSnapshot.cs` ikut terupdate.

    3. **Apply ke DB lokal:**
       ```
       dotnet ef database update
       ```
       Harus "Done." tanpa error. Bila gagal karena DB lokal tak tersedia/terkoneksi di environment ini, JANGAN edit Dev/Prod — tandai sebagai BLOCKING-pending dan dokumentasikan di SUMMARY agar di-apply saat DB lokal tersedia. Build + test tetap harus lulus.

    4. **Verifikasi full build + test:**
       ```
       dotnet build
       dotnet test HcPortal.Tests/HcPortal.Tests.csproj
       ```
       Build succeeded + semua test Passed (termasuk 8 test ValidateImageFile Task 1 + regression FileUploadHelper existing).

    JANGAN edit kode/DB di server Dev/Prod. File migration WAJIB di-commit (orchestrator handle commit) + flag ke IT saat shipped (catat di SUMMARY: commit hash + flag migration=true).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "Passed!|Failed!"</automated>
    <automated>Get-ChildItem Migrations -Filter "*AddImageToPackageQuestionAndOption*"</automated>
  </verify>
  <acceptance_criteria>
    - File `Migrations/*_AddImageToPackageQuestionAndOption.cs` ada
    - Migration Up() mengandung 4 AddColumn untuk ImagePath/ImageAlt di PackageQuestions + PackageOptions
    - Kolom ImageAlt = nvarchar(255) nullable; ImagePath = nvarchar(max) nullable
    - Migration Down() menghapus 4 kolom (simetris)
    - ApplicationDbContextModelSnapshot.cs terupdate (mengandung ImagePath/ImageAlt)
    - `dotnet ef database update` "Done." (atau didokumentasikan BLOCKING-pending bila DB lokal tak tersedia — TIDAK boleh di-apply ke Dev/Prod)
    - `dotnet build` exit 0
    - `dotnet test` (full suite) semua Passed — tidak ada regresi FileUploadHelper existing
  </acceptance_criteria>
  <done>Migration ter-generate dengan 4 kolom nullable benar, applied lokal (atau BLOCKING-pending terdokumentasi), build + full test lulus, siap commit + flag IT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → upload endpoint (phase 353) | File upload tak-terpercaya; ValidateImageFile (phase 352) adalah gatekeeper pertama |
| filename → filesystem | Nama file dari user; SaveFileAsync (existing, phase 325) strip directory component |
| DB schema migration → SQL Server lokal | DDL menambah kolom; nullable = non-destruktif terhadap data existing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-352-01 | Spoofing | File non-gambar (exe/script) di-rename .png/.jpg | mitigate | ValidateImageFile cek magic-byte via MatchesMagicByte (reuse FF D8 FF / 89 50 4E 47) + AllowedImageExtensions allowlist; test exe→.png reject |
| T-352-02 | Tampering | PDF/dokumen disusupkan via field gambar | mitigate | AllowedImageExtensions TANPA .pdf → PDF ditolak di gate ekstensi (Gap 4); test PDF reject |
| T-352-03 | Denial of Service | Upload file raksasa habiskan storage | mitigate | MaxImageFileSizeBytes = 5MB cap (D-03); test oversize >5MB reject |
| T-352-04 | Elevation/Injection | SVG/script injection (`<script>` di SVG) | mitigate | SVG TIDAK di allowlist (hanya .jpg/.jpeg/.png) → ditolak; render phase 354 pakai `src` encoded (out of scope 352) |
| T-352-05 | Tampering | Path traversal `../../` di filename | mitigate | SaveFileAsync existing (phase 325) strip directory + log warning; ValidateImageFile reset stream.Position=0 agar SaveFileAsync dapat full stream — tidak ubah perilaku traversal-strip |
| T-352-06 | Tampering | Migration merusak data PackageQuestions/PackageOptions existing | accept | 4 kolom NULLABLE → data lama tetap valid (NULL); Down() simetris; snapshot DB lokal sebelum apply sebagai safety net |

Semua threat HIGH (T-352-01/02/03/04) di-mitigate dalam scope phase 352 — tidak ada threat dengan disposition accept yang HIGH (ASVS L1: block on high terpenuhi).
</threat_model>

<verification>
- `dotnet build` exit 0 (no error)
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` semua Passed (8 test ValidateImageFile baru + regression existing FileUploadHelper)
- `dotnet ef database update` applied lokal (atau BLOCKING-pending terdokumentasi bila DB lokal tak tersedia di environment)
- grep: `AllowedImageExtensions`, `MaxImageFileSizeBytes`, `ValidateImageFile`, 4× `ImagePath`/`ImageAlt`, migration file ada
- ValidateCertificateFile + MagicBytes dict tidak berubah (no regresi cert call-site)
</verification>

<success_criteria>
1. Entity PackageQuestion + PackageOption masing-masing punya ImagePath (nvarchar(max) null) + ImageAlt (nvarchar(255) null) — IMG-04 data foundation.
2. AssessmentConstants.FileValidation punya AllowedImageExtensions {.jpg,.jpeg,.png} + MaxImageFileSizeBytes (5MB), tanpa menambah entri MagicBytes.
3. FileUploadHelper.ValidateImageFile menolak PDF (Gap 4), menolak non-image (magic-byte), menolak >5MB, menerima JPG/PNG, null/empty → valid — terbukti via 8 xUnit Passed.
4. Migration AddImageToPackageQuestionAndOption 4 kolom nullable di-generate + applied lokal (snapshot DB dulu) + committed + flag IT-notify.
5. dotnet build + full dotnet test lulus tanpa regresi cert helper.
6. Scope tidak melebihi migration + entity + helper + tests (tanpa controller/form/render/sync).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/352-data-foundation-image-only-upload/352-01-SUMMARY.md`. Catat: commit hash + flag migration=true untuk IT-notify; status `dotnet ef database update` (applied / BLOCKING-pending); jumlah test Passed.
</output>
