---
phase: 353-admin-backend-gambar-crud-sync-atomic-delete
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/PackageImageDeleteTests.cs
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03, IMG-05, IMG-06, IMG-07, SYN-02]
must_haves:
  truths:
    - "CreateQuestion menerima IFormFile soal + opsi A-D + alt, memvalidasi via ValidateImageFile, menyimpan via SaveFileAsync, set ImagePath/ImageAlt"
    - "EditQuestion GET JSON membawa imagePath+imageAlt soal & tiap opsi (D-06, IMG-07 prefill)"
    - "EditQuestion POST: file-baru-menang (D-05), checkbox hapus (IMG-06), replace hapus file lama atomic + ref-count (IMG-05, SYN-02)"
    - "EditQuestion TIDAK menghapus gambar opsi yang tidak diubah (OQ1 — option image preserve)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "CreateQuestion + EditQuestion GET/POST dengan image binding + atomic replace"
      contains: "IFormFile? questionImage"
    - path: "HcPortal.Tests/PackageImageDeleteTests.cs"
      provides: "Test D-05 replace conflict (file-baru-menang) + OQ1 option preserve"
      contains: "ReplaceConflict"
  key_links:
    - from: "CreateQuestion/EditQuestion POST"
      to: "FileUploadHelper.ValidateImageFile + SaveFileAsync"
      via: "panggil sebelum set ImagePath"
      pattern: "ValidateImageFile\\(.*Image\\)"
    - from: "EditQuestion POST replace/remove"
      to: "File.Delete fisik old path"
      via: "ref-count (AnyAsync) POST save → File.Delete warn-only"
      pattern: "AnyAsync\\(.*ImagePath == "
---

<objective>
Wiring CRUD gambar di `AssessmentAdminController.cs`: `CreateQuestion` (upload soal+opsi), `EditQuestion` GET JSON (prefill D-06), dan `EditQuestion` POST (replace/remove dengan D-05 file-baru-menang + atomic delete + ref-count + **preservasi gambar opsi OQ1**). Ini bagian PALING BERISIKO fase (OQ1 = A3 HIGH regression: RemoveRange opsi existing menghapus ImagePath opsi).

Purpose: IMG-01/02/03 (upload soal+opsi+alt), IMG-05 (ganti, file lama terhapus), IMG-06 (hapus via checkbox), IMG-07 (prefill thumbnail), SYN-02 (atomic delete saat replace). Semua di satu file controller → sequential setelah Wave 0.

Output: 3 method controller diperluas + 2 test (D-05 replace conflict + OQ1 option preserve) GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-CONTEXT.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md

<interfaces>
<!-- Helper Phase 352 — pakai langsung -->
FileUploadHelper.ValidateImageFile(IFormFile?) → (bool IsValid, string? Error)   // Helpers/FileUploadHelper.cs:45; null/empty → (true,null)
FileUploadHelper.SaveFileAsync(IFormFile?, string webRootPath, string subFolder, ILogger?) → Task<string?>  // :75; null untuk file null; return relative URL "/{subFolder}/{safeName}"; auto-mkdir + strip path traversal + timestamp+GUID prefix
_env.WebRootPath  // protected readonly di AdminBaseController:18 (inherited)

<!-- Entity (Models/AssessmentPackage.cs) -->
PackageQuestion: Id, QuestionText, Order, ScoreValue, QuestionType?, ElemenTeknis?, Rubrik?, MaxCharacters, ImagePath?, ImageAlt? ([MaxLength(255)]), Options
PackageOption:   Id, OptionText, IsCorrect, ImagePath?, ImageAlt? ([MaxLength(255)])

<!-- CreateQuestion POST signature SEKARANG (L6067) — flat params, BUKAN DTO -->
public async Task<IActionResult> CreateQuestion(
    int packageId, string questionText, string questionType, int scoreValue,
    string? elemenTeknis, string? rubrik, int maxCharacters,
    string? optionA, string? optionB, string? optionC, string? optionD,
    bool correctA, bool correctB, bool correctC, bool correctD)
// atribut: [HttpPost][Authorize(Roles="Admin, HC")][ValidateAntiForgeryToken]

<!-- EditQuestion GET JSON SEKARANG (L6196 method, Json di ~L6214) -->
return Json(new { id, order, questionText, questionType, scoreValue, affectedSessions,
                  elemenTeknis, rubrik, maxCharacters, options = [...{ optionText, isCorrect }] });
// entity Include(q => q.Options) sudah ada (~L6199)

<!-- EditQuestion POST SEKARANG (L6241) — RemoveRange opsi + recreate (L6302), single SaveChanges (~L6323), auto-sync (~L6357-6369) -->
// L6302: _context.PackageOptions.RemoveRange(q.Options); q.Options.Clear();
//        lalu re-add PackageOption baru dari optionA..D → INI MENGHAPUS ImagePath opsi (OQ1!)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreateQuestion — upload gambar soal + opsi A-D (IMG-01/02/03)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baca CreateQuestion LENGKAP L6067 s/d akhir method + blok auto-sync ~L6177-6189 + cara Options dibangun L6132-6145)
    - Controllers/TrainingAdminController.cs (L205-207 validate cert + L307-311 SaveFileAsync — pola validate-before-save SHARED-3)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md (SHARED-3 + "CreateQuestion POST" assignment)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-UI-SPEC.md (Copywriting Contract — string error)
  </read_first>
  <behavior>
    - Upload valid (JPG/PNG ≤5MB) ke soal → newQ.ImagePath ter-set ke URL hasil SaveFileAsync, ImageAlt = alt (truncate 255).
    - Upload valid ke opsi A → PackageOption A.ImagePath ter-set.
    - Upload invalid (PDF / >5MB) → TempData["Error"] di-set (pesan dari ValidateImageFile) + RedirectToAction ManagePackageQuestions, TIDAK ada file tersimpan, TIDAK ada soal dibuat.
    - File null (admin tidak pilih) → ImagePath tetap null (no crash, SaveFileAsync no-op).
    - Essay: hanya soal punya gambar (opsi null → diabaikan).
  </behavior>
  <action>
1) Perluas signature `CreateQuestion` (L6067), TAMBAHKAN param setelah `correctD` (gaya flat, BUKAN DTO — D-67 discretion, konsisten file ini):
```csharp
IFormFile? questionImage, string? questionImageAlt,
IFormFile? optionAImage, IFormFile? optionBImage, IFormFile? optionCImage, IFormFile? optionDImage,
string? optionAImageAlt, string? optionBImageAlt, string? optionCImageAlt, string? optionDImageAlt
```
Pertahankan `[HttpPost][Authorize(Roles="Admin, HC")][ValidateAntiForgeryToken]`.

2) SETELAH validasi tipe/score existing (jangan ubah validasi existing), VALIDATE-ALL (fail-fast, SHARED-3 / D-08). Loop semua 5 IFormFile:
```csharp
foreach (var f in new[] { questionImage, optionAImage, optionBImage, optionCImage, optionDImage })
{
    var (ok, err) = FileUploadHelper.ValidateImageFile(f);
    if (!ok)
    {
        TempData["Error"] = err ?? "File harus berupa gambar JPG atau PNG.";
        return RedirectToAction("ManagePackageQuestions", new { packageId });
    }
}
```
(ValidateImageFile(null) = (true,null) → file null lolos otomatis.)

3) Helper truncate alt (tambah method private kecil bila belum ada):
```csharp
static string? Truncate(string? s, int max) => string.IsNullOrEmpty(s) ? s : (s.Length <= max ? s : s.Substring(0, max));
```

4) Simpan gambar soal SEBELUM/SAAT bangun newQ:
```csharp
var qImgUrl = await FileUploadHelper.SaveFileAsync(questionImage, _env.WebRootPath, $"uploads/questions/{packageId}", _logger);
// set saat construct newQ:
newQ.ImagePath = qImgUrl;                       // null bila tak ada file
newQ.ImageAlt  = Truncate(questionImageAlt, 255);
```

5) Untuk opsi: zip array paralel `(IFormFile? img, string? alt)` indeks A-D ke loop yang membangun PackageOption (L6132-6145). Setiap PackageOption yang dibuat → set ImagePath = SaveFileAsync(img...) + ImageAlt = Truncate(alt,255). Contoh:
```csharp
var optImgs = new[] { optionAImage, optionBImage, optionCImage, optionDImage };
var optAlts = new[] { optionAImageAlt, optionBImageAlt, optionCImageAlt, optionDImageAlt };
// di dalam loop bangun opsi (indeks i 0..3 untuk A..D):
var oImgUrl = await FileUploadHelper.SaveFileAsync(optImgs[i], _env.WebRootPath, $"uploads/questions/{packageId}", _logger);
newOption.ImagePath = oImgUrl;
newOption.ImageAlt  = Truncate(optAlts[i], 255);
```
(Sesuaikan dengan struktur loop existing — bila opsi dibangun dari tuple list, indeks-kan ke array di atas.)

6) JANGAN ubah `SaveChangesAsync` existing (~L6149) maupun blok auto-sync existing (~L6177-6189). TIDAK ada ref-count di Create (tidak ada delete). TIDAK ada tx baru.
  </action>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ValidateImageFile"</automated>
  </verify>
  <acceptance_criteria>
    - grep `IFormFile? questionImage` di AssessmentAdminController.cs CreateQuestion = 1+ match.
    - grep `ValidateImageFile(` di method CreateQuestion ≥1; grep `SaveFileAsync(` di CreateQuestion ≥1.
    - grep `$"uploads/questions/{packageId}"` ≥1 (subfolder C-02).
    - `dotnet build` exit 0; suite ValidateImageFile tetap hijau.
  </acceptance_criteria>
  <done>CreateQuestion menerima + memvalidasi + menyimpan gambar soal & opsi; invalid → TempData error redirect; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: EditQuestion GET JSON — prefill imagePath+imageAlt (D-06 / IMG-07)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baca EditQuestion GET method L6196-6235 — blok Json(new {...}) lengkap + Include Options L6199)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md ("EditQuestion GET JSON" assignment) + 353-RESEARCH.md (Pattern 4)
  </read_first>
  <action>
Di method `EditQuestion(int questionId)` GET (L6196), pada blok `return Json(new { ... })` (~L6214), TAMBAHKAN 2 field di objek soal dan 2 field di tiap objek opsi. Pertahankan SEMUA field existing verbatim:
```csharp
return Json(new {
    id = q.Id, order = q.Order, questionText = q.QuestionText,
    questionType = q.QuestionType ?? "MultipleChoice", scoreValue = q.ScoreValue,
    affectedSessions, elemenTeknis = q.ElemenTeknis, rubrik = q.Rubrik, maxCharacters = q.MaxCharacters,
    imagePath = q.ImagePath, imageAlt = q.ImageAlt,                       // ← TAMBAH (D-06)
    options = q.Options.OrderBy(o => o.Id).Select(o => new {
        optionText = o.OptionText, isCorrect = o.IsCorrect,
        imagePath = o.ImagePath, imageAlt = o.ImageAlt                    // ← TAMBAH (D-06)
    }).ToList()
});
```
PENTING: jaga urutan opsi konsisten dengan urutan yang dipakai POST (OrderBy(o => o.Id)) supaya prefill thumbnail A-D cocok posisi. Bila blok existing pakai urutan berbeda, samakan urutan GET & POST (catat di SUMMARY). Include Options sudah ada (~L6199) — jangan hapus.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `imagePath = q.ImagePath` DAN `imageAlt = q.ImageAlt` di EditQuestion GET = 1 match masing-masing.
    - grep `imagePath = o.ImagePath` (dalam Select opsi) = 1 match.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>JSON EditQuestion GET membawa imagePath+imageAlt soal & tiap opsi; build hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EditQuestion POST — replace/remove + OQ1 option preserve + ref-count delete (D-05/IMG-05/IMG-06/SYN-02)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baca EditQuestion POST L6241-6375 LENGKAP — perhatikan RemoveRange(q.Options) L6302 + recreate opsi L6314-6320 + SaveChanges L6323 + auto-sync L6357-6369)
    - Controllers/CDPController.cs (L2456-2563 DeleteCoachingSession — pola atomic delete Phase 333: outer-scope List<string>?, build, File.Delete POST commit warn-only)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md (SHARED-1, SHARED-2, "EditQuestion POST" assignment — 🚩 NO-ANALOG option preserve)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md (Pattern 2, OQ1, OQ2, Pitfall 2/3/4)
  </read_first>
  <behavior>
    - D-05 file-baru-menang: pada item dengan checkbox hapus DICENTANG DAN file baru DIPILIH → file baru tersimpan, path lama jadi delete-candidate, checkbox diabaikan.
    - IMG-05 replace: pilih file baru tanpa centang hapus → path baru tersimpan, path lama dihapus disk (setelah ref-count).
    - IMG-06 remove: centang hapus tanpa file baru → ImagePath=null, path lama dihapus disk (setelah ref-count).
    - OQ1 PRESERVE: edit soal TANPA menyentuh gambar opsi → ImagePath opsi TETAP (tidak hilang) setelah save.
    - SYN-02/D-10 ordering: SaveChanges → auto-sync → ref-count old path → File.Delete (warn-only).
  </behavior>
  <action>
1) Perluas signature `EditQuestion` POST (L6241) dengan param SAMA seperti CreateQuestion (questionImage/alt + optionAImage..D + optionAImageAlt..D) PLUS checkbox edit-only:
```csharp
bool removeQuestionImage,
bool removeOptionAImage, bool removeOptionBImage, bool removeOptionCImage, bool removeOptionDImage
```
(checkbox unchecked → false otomatis). Pertahankan atribut [HttpPost][Authorize][ValidateAntiForgeryToken].

2) Validate-all SHARED-3 (fail-fast → TempData["Error"] + redirect) — sama pola Task 1.

3) **OQ1 — UBAH strategi opsi dari RemoveRange+recreate ke UPDATE-IN-PLACE** (rekomendasi RESEARCH/PATTERNS, hindari wipe ImagePath opsi). Ganti blok L6302-6320:
   - Muat opsi existing terurut: `var existing = q.Options.OrderBy(o => o.Id).ToList();` (urutan SAMA dgn GET JSON Task 2).
   - Untuk tiap posisi A-D (i=0..3): jika ada opsi existing di posisi i → UPDATE OptionText + IsCorrect dari optionA..D/correctA..D; PRESERVE o.Id + o.ImagePath/o.ImageAlt KECUALI image-intent berlaku (lihat langkah 4). Jika TIDAK ada opsi existing di posisi i tapi optionX terisi → ADD PackageOption baru. Jika ada opsi existing di posisi i tapi optionX kosong (opsi dihapus, mis. 4→3) → Remove opsi itu DAN tambahkan o.ImagePath (jika ada) ke `imagePathsToDelete`.
   - JANGAN lagi `RemoveRange(q.Options)` membabi-buta. (Bila terlalu berisiko mengubah total, FALLBACK PATTERNS: snapshot oldOpt[i].ImagePath sebelum RemoveRange + reassign by posisi A→A saat recreate + opsi yang hilang → delete-candidate. Dokumentasikan pilihan di SUMMARY. Update-in-place LEBIH DISUKAI.)

4) **Resolusi niat per item (D-05, helper lokal)** — kumpulkan delete-candidate ke `var imagePathsToDelete = new List<string>();`:
```
// untuk soal + tiap opsi:
if (newFile != null) {                         // file baru menang (D-05)
    var saved = await SaveFileAsync(newFile, _env.WebRootPath, $"uploads/questions/{packageId}", _logger);
    if (!string.IsNullOrEmpty(oldPath)) imagePathsToDelete.Add(oldPath);
    target.ImagePath = saved; target.ImageAlt = Truncate(alt, 255);   // IGNORE checkbox
} else if (removeChecked) {                     // hapus (IMG-06)
    if (!string.IsNullOrEmpty(oldPath)) imagePathsToDelete.Add(oldPath);
    target.ImagePath = null; target.ImageAlt = null;
} else {                                        // keep — alt boleh update
    target.ImageAlt = Truncate(alt, 255);
}
```
Terapkan untuk soal (q + questionImage/removeQuestionImage/questionImageAlt) dan tiap opsi yang DIPERTAHANKAN (gunakan ImagePath opsi existing sebagai oldPath). Untuk opsi yang baru di-ADD → tidak ada oldPath, hanya SaveFileAsync bila ada file.

5) `await _context.SaveChangesAsync();` (existing ~L6323 — pertahankan, sekarang setelah update-in-place).

6) Pertahankan blok auto-sync existing (~L6357-6369) — jalan SETELAH SaveChanges.

7) **Ref-count + File.Delete SETELAH auto-sync** (OQ2 ordering! SHARED-2 + SHARED-1). Tambahkan di akhir SEBELUM return sukses:
```csharp
foreach (var relUrl in imagePathsToDelete)
{
    bool stillUsedQ = await _context.PackageQuestions.AnyAsync(x => x.ImagePath == relUrl);
    bool stillUsedO = await _context.PackageOptions.AnyAsync(x => x.ImagePath == relUrl);
    if (stillUsedQ || stillUsedO) continue;   // masih dipakai Post/lain → SKIP (D-10)
    try
    {
        var physical = Path.Combine(_env.WebRootPath, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
        if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
    }
    catch (Exception fex)
    {
        _logger.LogWarning(fex, "File.Delete post-commit failed (question image): {Path}", relUrl);
    }
}
```
KRITIS (Pitfall 2 + OQ2): ref-count HARUS setelah SaveChanges DAN setelah auto-sync — kalau tidak, Post mungkin masih merujuk old path → file ter-skip selamanya, atau shared file ter-hapus salah. Tx: EditQuestion saat ini single-SaveChanges; boleh tetap single-SaveChanges + post-save delete (delete tetap post-persist + warn-only + ref-counted = memenuhi C-04). Bila membungkus BeginTransactionAsync, build pathsToDelete outer-scope + delete setelah CommitAsync.
  </action>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ReplaceConflict|FullyQualifiedName~OptionPreserve|FullyQualifiedName~RefCount"</automated>
  </verify>
  <acceptance_criteria>
    - grep `removeQuestionImage` di EditQuestion POST = 1+ match.
    - grep `AnyAsync(x => x.ImagePath == relUrl)` (atau setara) di EditQuestion POST ≥1 (ref-count D-10).
    - grep `RemoveRange(q.Options)` di EditQuestion POST = 0 match (update-in-place) ATAU bila fallback dipakai, snapshot ImagePath sebelum RemoveRange terdokumentasi + reassign by posisi ada.
    - Test `ReplaceConflict` (file-baru-menang) + test OQ1 option-preserve GREEN di PackageImageDeleteTests.cs.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>EditQuestion POST: replace/remove dengan D-05, opsi tidak kehilangan gambar (OQ1), file lama dihapus atomic + ref-count setelah auto-sync; test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test D-05 replace conflict + OQ1 option preserve (PackageImageDeleteTests.cs)</name>
  <read_first>
    - HcPortal.Tests/PackageImageDeleteTests.cs (file dari Plan 01 — tambahkan test, jangan tulis ulang)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md (Code Examples ApplyImage + Pitfall 3 + OQ1)
  </read_first>
  <behavior>
    - ReplaceConflict (D-05): item dengan removeChecked=true DAN newFile!=null → resolusi = file baru tersimpan (path baru non-null), oldPath masuk delete-candidate (file-baru-menang).
    - OptionPreserve (OQ1): update soal tanpa file/checkbox pada opsi → ImagePath opsi yang dipertahankan TETAP (logika update-in-place mempertahankan path).
  </behavior>
  <action>
Tambahkan ke `HcPortal.Tests/PackageImageDeleteTests.cs`:
- `ReplaceConflict_NewFileWins_OverRemoveCheckbox`: replika logika resolusi niat (langkah 4 Task 3) sebagai unit — input (oldPath, newFile!=null, removeChecked=true, alt) → assert: target.ImagePath == savedNewPath, delete-candidate berisi oldPath, ImagePath != null.
- `OptionPreserve_KeepsImagePath_WhenOptionUntouched`: bangun list opsi existing dengan ImagePath set; jalankan logika update-in-place (newFile=null, removeChecked=false) untuk update text saja → assert opsi.ImagePath TIDAK berubah.

Jika logika resolusi diekstrak ke method testable di controller/helper, panggil langsung; jika tidak, replika ekspresi IDENTIK dengan komentar "mirror of EditQuestion POST D-05 resolution — keep in sync". Reuse MakeTempDir bila perlu file nyata untuk SaveFileAsync simulasi (boleh pakai path string dummy untuk unit logika murni).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ReplaceConflict|FullyQualifiedName~OptionPreserve"</automated>
  </verify>
  <acceptance_criteria>
    - 2 test baru GREEN; nama mengandung "ReplaceConflict" + "OptionPreserve".
    - `dotnet test --filter "FullyQualifiedName~ReplaceConflict"` exit 0.
  </acceptance_criteria>
  <done>Test D-05 + OQ1 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → CreateQuestion/EditQuestion POST | File upload (IFormFile) + alt text melewati batas; input tak terpercaya. |
| Controller → filesystem (wwwroot/uploads/questions) | Penulisan & penghapusan file fisik. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-353-02-01 | Tampering | Upload non-image disamarkan .png | mitigate | `FileUploadHelper.ValidateImageFile` (magic-byte, Phase 352) dipanggil SEBELUM SaveFileAsync di tiap titik upload. |
| T-353-02-02 | Tampering | Path traversal nama file | mitigate | `SaveFileAsync` strip path + timestamp+GUID prefix (Phase 325/352). |
| T-353-02-03 | DoS | File besar | mitigate | Cap 5MB di ValidateImageFile (C-01). |
| T-353-02-04 | Spoofing | CSRF | mitigate | `[ValidateAntiForgeryToken]` dipertahankan di POST. |
| T-353-02-05 | Elevation | Akses non-admin | mitigate | `[Authorize(Roles="Admin, HC")]` dipertahankan. |
| T-353-02-06 | Tampering | Shared-file salah-hapus saat replace Pre yang masih dipakai Post | mitigate | Ref-count (AnyAsync) SETELAH auto-sync, sebelum File.Delete (D-10/OQ2). |
| T-353-02-07 | Info Disclosure | alt text >255 → DbUpdateException bocor stack | mitigate | Truncate alt ke 255 sebelum assign (Pitfall 6). |
</threat_model>

<verification>
- `dotnet build` exit 0.
- `dotnet test --filter "FullyQualifiedName~Image"` hijau (sync + refcount + replace + preserve).
- Full suite `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` tidak regresi (≥126 hijau).
- Manual/Playwright (smoke, dikonsolidasi Phase 355): upload soal+opsi → simpan → edit → thumbnail prefill → replace → file lama hilang dari disk.
</verification>

<success_criteria>
- SC #1: upload gambar soal+opsi tersimpan ke /uploads/questions/{packageId}/ + alt opsional.
- SC #2: edit prefill thumbnail (JSON imagePath/imageAlt) + ganti/hapus.
- SC #5 (sebagian): replace hapus file lama atomic + ref-count.
- OQ1 tidak regresi: gambar opsi tidak hilang saat edit.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-02-SUMMARY.md` (catat pilihan strategi opsi: update-in-place vs carry-forward).
</output>
