---
phase: 353-admin-backend-gambar-crud-sync-atomic-delete
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - Views/Admin/ManagePackageQuestions.cshtml
  - Views/Admin/_PreviewQuestion.cshtml
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/PackageImageDeleteTests.cs
autonomous: true
requirements: [RND-04, SYN-02]
must_haves:
  truths:
    - "Form #questionForm punya enctype=multipart/form-data + field gambar inline (soal di bawah textarea, opsi A-D inline tiap baris) dengan nama field konsisten controller (questionImage, optionAImage..D, *Alt, remove*Image) (SC #1/#2, D-01/D-02)"
    - "FileReader menampilkan thumbnail instan saat admin pilih file; edit prefill thumbnail lama dari JSON imagePath (D-07a/D-03/IMG-07)"
    - "_PreviewQuestion.cshtml render <img> gambar soal (cap 240px) + tiap opsi (cap 120px) (RND-04 / SC #3)"
    - "DeleteQuestion menghapus file gambar soal+opsi atomic: path-collect-before + File.Delete post-commit + ref-count D-10 + inner try/catch warn-only (SYN-02 / SC #5/#6)"
    - "DeletePackage menghapus file gambar semua soal+opsi paket atomic + ref-count (D-11 / SC #5/#6)"
  artifacts:
    - path: "Views/Admin/ManagePackageQuestions.cshtml"
      provides: "enctype multipart + field upload gambar inline + JS FileReader/prefill"
      contains: "enctype=\"multipart/form-data\""
    - path: "Views/Admin/_PreviewQuestion.cshtml"
      provides: "render <img> gambar soal + opsi (RND-04)"
      contains: "<img"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "DeleteQuestion + DeletePackage atomic file delete + ref-count"
      contains: "AnyAsync"
  key_links:
    - from: "ManagePackageQuestions.cshtml <input type=file name=questionImage>"
      to: "CreateQuestion/EditQuestion POST param questionImage (Plan 02)"
      via: "model binding multipart (enctype wajib)"
      pattern: "name=\"questionImage\""
    - from: "DeleteQuestion / DeletePackage path-collect"
      to: "File.Delete fisik post-commit"
      via: "ref-count AnyAsync(ImagePath==relUrl) → skip-or-delete warn-only"
      pattern: "AnyAsync\\(.*ImagePath == relUrl"
---

<objective>
Menutup sisi VIEW (form upload gambar inline + FileReader/prefill + preview render `<img>`) dan ATOMIC DELETE (`DeleteQuestion` + `DeletePackage`) — semua yang belum tertutup Plan 01/02. Plan ini menyelesaikan RND-04 (preview admin render gambar) dan SYN-02 sisa (atomic delete + ref-count saat soal/opsi/paket dihapus, pola Phase 333 + D-10/D-11).

Purpose: tanpa plan ini, form tak bisa mengirim file (no enctype → binding Plan 02 mati), admin tak lihat thumbnail, preview tak render gambar (RND-04 = 0 plan), dan hapus soal/paket meninggalkan file orphan (D-11 belum diimplement). Sisi controller (`DeleteQuestion`/`DeletePackage`) menyentuh `AssessmentAdminController.cs` yang sama dengan Plan 02 → wave 2 sequential-strict.

Output: 1 view form diperluas (enctype + field + JS), 1 partial preview render `<img>`, 2 method delete diperkuat atomic+ref-count, + test DeletePackage path-collect & ref-count delete GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-CONTEXT.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-UI-SPEC.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md
@docs/mockup-presentasi/353-layout-form-gambar-mockup.html

<interfaces>
<!-- Param controller yang HARUS dicocokkan name= di form (dari Plan 02 — Create + Edit POST) -->
CreateQuestion/EditQuestion POST menerima (case-insensitive model binding):
  questionImage (IFormFile?), questionImageAlt (string?),
  optionAImage..optionDImage (IFormFile?), optionAImageAlt..optionDImageAlt (string?),
  removeQuestionImage (bool, EDIT only), removeOptionAImage..removeOptionDImage (bool, EDIT only)
→ name= di Razor WAJIB sama persis nama-nama ini, mis. name="questionImage", name="optionAImage", name="removeQuestionImage".
→ Binding HANYA jalan jika form enctype="multipart/form-data" (D-02 / Pitfall 1).

<!-- JSON EditQuestion GET (Plan 02 Task 2) yang dikonsumsi populateEditForm -->
{ id, order, questionText, questionType, scoreValue, affectedSessions, elemenTeknis, rubrik, maxCharacters,
  imagePath, imageAlt,
  options: [ { optionText, isCorrect, imagePath, imageAlt } ] }   // urutan opsi = OrderBy(o => o.Id)

<!-- Entity / Model partial preview -->
_PreviewQuestion.cshtml Model = PackageQuestion (Models/AssessmentPackage.cs):
  Model.ImagePath (string? nullable), Model.ImageAlt (string?), Model.Options (ICollection<PackageOption>)
  PackageOption: opt.ImagePath (string?), opt.ImageAlt (string?), opt.OptionText, opt.IsCorrect

<!-- Helper Phase 333 / SHARED-1+2 untuk delete (pakai langsung) -->
_env.WebRootPath  // protected readonly AdminBaseController:18
_logger           // ILogger inherited
physical path: Path.Combine(_env.WebRootPath, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar))
ref-count: await _context.PackageQuestions.AnyAsync(x => x.ImagePath == relUrl)
         || await _context.PackageOptions.AnyAsync(x => x.ImagePath == relUrl)

<!-- Lokasi kode aktual (read 2026-06-08, match by symbol bukan line) -->
DeletePackage POST  : AssessmentAdminController.cs L5457 (cascade RemoveRange L5468-5487, SaveChanges ~L5489, audit L5499-5512, auto-sync L5517-5525)
DeleteQuestion POST : AssessmentAdminController.cs L6377 (responses L6385-6388, RemoveRange Options+Remove q L6390-6391, SaveChanges L6392, catch DbUpdateException L6396, auto-sync L6417-6425)
View form           : ManagePackageQuestions.cshtml L122 (form, NO enctype), textarea questionText L140, opsi loop L150-163, @section Scripts L278, submit handler L299, loadPreview L370, populateEditForm L399-441, resetForm L444
Preview partial     : _PreviewQuestion.cshtml — question text L17, option loop L45-60 (list-group, <span>@opt.OptionText</span> L60)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Form gambar inline + enctype multipart (D-01/D-02, View)</name>
  <read_first>
    - Views/Admin/ManagePackageQuestions.cshtml (LENGKAP — terutama form L122, textarea questionText L140, opsi loop L150-163, AntiForgeryToken L123)
    - Views/Admin/ImportPackageQuestions.cshtml (L76 `enctype="multipart/form-data"` + L81 `<input type="file" name=...>` — analog enctype + file input)
    - docs/mockup-presentasi/353-layout-form-gambar-mockup.html (komponen `.img-drop` Opsi A Inline — layout disetujui user, ACUAN VISUAL UTAMA)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-UI-SPEC.md (Component Inventory + Copywriting Contract + Spacing 46px thumb + Accessibility)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md ("ManagePackageQuestions.cshtml" assignment)
  </read_first>
  <files>Views/Admin/ManagePackageQuestions.cshtml</files>
  <action>
1) **enctype (D-02, WAJIB)** — pada `<form id="questionForm" ...>` (L122) tambahkan atribut `enctype="multipart/form-data"`. Pertahankan semua atribut existing (`asp-action`, `asp-controller`, `method="post"`) + `@Html.AntiForgeryToken()` (L123). Tanpa ini binding Plan 02 mati total.

2) **Field gambar SOAL (inline di bawah textarea, D-01)** — tepat SETELAH `<textarea ... name="questionText">` (L140) sisipkan blok `.img-drop` per UI-SPEC + mockup. Komponen:
   - Label `Gambar Soal (opsional)` (`.form-label small fw-semibold`).
   - Thumbnail box 46px (`<img>` kosong/hidden + placeholder `bi bi-image` saat empty).
   - Meta text (`.72rem`) — empty: `Belum ada gambar`.
   - Tombol pilih: `<label class="btn btn-outline-primary btn-sm">Pilih<input type="file" name="questionImage" accept="image/png,image/jpeg" hidden></label>` (label-wrap = keyboard-focusable, Accessibility UI-SPEC). Saat edit: teks tombol `Ganti`.
   - Tombol clear icon-only `<button type="button" class="btn btn-outline-danger btn-sm" aria-label="Hapus gambar ini"><i class="bi bi-x-lg"></i></button>` (hidden sampai ada gambar).
   - Checkbox hapus (EDIT-only, hidden saat create): `<input type="checkbox" name="removeQuestionImage" value="true" id="removeQuestionImage"><label for="removeQuestionImage">Hapus gambar</label>`.
   - Alt input: `<input type="text" name="questionImageAlt" class="form-control form-control-sm" placeholder="Teks alternatif (opsional)">` + helper `Deskripsi singkat gambar untuk pembaca layar.`.
   - Format hint: `JPG atau PNG, maksimal 5 MB.`.
   Beri `id` stabil untuk dipakai JS Task 2 (mis. `questionImgThumb`, `questionImgMeta`, `questionImgField`, `questionImgClear`, `questionRemoveWrap`).

3) **Field gambar OPSI A-D (inline tiap baris, D-01)** — di dalam loop opsi (L150-163), tepat di bawah `input-group` masing-masing opsi, sisipkan `.img-drop` versi opsi. Nama field WAJIB: `name="optionAImage"`/`optionBImage`/`optionCImage`/`optionDImage` + `optionAImageAlt`..D + `removeOptionAImage`..D (checkbox EDIT-only). Gunakan suffix huruf A-D pada `id` (mis. `optAImgThumb`, `optAImgField`) supaya JS handler generik per huruf. Layout = flex row `[thumb 46px][meta flex-grow][tombol]` `gap-2` per UI-SPEC.

4) Jangan ubah field correctA..D / optionText existing — field gambar adalah TAMBAHAN inline, bukan pengganti. Jangan ubah submit handler/validasi JS existing di langkah ini (itu Task 2).

5) Copy SEMUA verbatim dari Copywriting Contract UI-SPEC (Bahasa Indonesia). Empty/has-img state warna per UI-SPEC (`.img-drop` dashed gray empty / solid green has-img + `bi-check-circle-fill`).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `enctype="multipart/form-data"` di ManagePackageQuestions.cshtml = 1+ match (pada #questionForm).
    - grep `name="questionImage"` = 1; `name="optionAImage"` = 1; `name="optionDImage"` = 1; `name="removeQuestionImage"` = 1.
    - grep `name="questionImageAlt"` = 1; `name="optionAImageAlt"` = 1.
    - grep `Gambar Soal (opsional)` = 1; `Belum ada gambar` ≥1; `Teks alternatif (opsional)` ≥1.
    - `dotnet build` exit 0 (Razor compile bersih).
  </acceptance_criteria>
  <done>Form punya enctype + field gambar inline soal & opsi A-D dengan nama field cocok param Plan 02; copy sesuai contract; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: JS FileReader thumbnail instan + prefill edit (D-07a/D-03/IMG-07, View)</name>
  <read_first>
    - Views/Admin/ManagePackageQuestions.cshtml (@section Scripts L278, submit handler L299, loadPreview L370, populateEditForm L399-441 LENGKAP, resetForm L444 — pahami pola vanilla JS existing, IIFE/event listener)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md (Pattern 5 FileReader)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-UI-SPEC.md (Interaction Contract — file pick / clear / Ganti / Hapus / conflict)
  </read_first>
  <files>Views/Admin/ManagePackageQuestions.cshtml</files>
  <action>
Tambahkan vanilla JS di `@section Scripts` existing (L278) — KONSISTEN gaya file (plain JS, no framework). JANGAN tulis ulang script existing; SISIPKAN handler baru.

1) **FileReader thumbnail instan (D-07a)** — untuk setiap `<input type=file>` (soal + 4 opsi), pasang listener `change`:
   - Baca file via `FileReader.readAsDataURL`, set `thumb.src = e.target.result`, tampilkan thumb (hapus placeholder), flip field ke state has-img (kelas hijau).
   - Tampilkan meta `{namafile} · {ukuran}` (format KB/MB; mis. `(file.size/1024).toFixed(0)+' KB'` atau MB bila ≥1024KB).
   - Tampilkan tombol clear (`×`).
   - **Konflik D-05 (nicety UI-SPEC):** saat file baru dipilih DAN checkbox `removeXImage` ada → uncheck + disable checkbox (mirror resolusi backend file-baru-menang; backend tetap otoritatif).
   Buat fungsi generik `wireImageField(fileInputId, thumbId, metaId, clearId, removeId?)` lalu panggil untuk 5 field (soal + A-D) agar DRY.

2) **Tombol clear (× pre-submit)** — reset `fileInput.value=''`, kosongkan thumb, kembalikan ke empty state, sembunyikan tombol clear, re-enable checkbox hapus.

3) **Prefill edit (D-06/D-03/IMG-07)** — di `populateEditForm(data)` (L399), TAMBAHKAN:
   - Soal: jika `data.imagePath` ada → set `questionImgThumb.src = data.imagePath`, flip ke has-img state, set tombol jadi `Ganti`, tampilkan checkbox `Hapus gambar` + tombol `×`; set `questionImageAlt.value = data.imageAlt || ''`. `<input type=file>` TETAP kosong (browser security, D-03).
   - Opsi: loop `data.options` (urutan = OrderBy Id, cocok posisi A-D dengan render Task 1) → untuk tiap opsi set thumbnail/alt/checkbox analog. Pertahankan pengisian optionText/correct existing (jangan hapus).
   Gunakan `id` field dari Task 1.

4) **resetForm / mode create (L444)** — pastikan reset mengosongkan semua thumb + alt + checkbox + sembunyikan checkbox/×/Ganti (kembali ke create state, tombol `Pilih`). Sembunyikan elemen EDIT-only (checkbox Hapus) saat create.

5) Jangan ubah logika submit/preview existing selain yang relevan thumbnail. Tidak ada client-side blocking validation tambahan (D-08 = server-side TempData).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `FileReader` di ManagePackageQuestions.cshtml ≥1.
    - grep `readAsDataURL` ≥1.
    - grep `data.imagePath` di populateEditForm ≥1 (prefill thumbnail).
    - grep `addEventListener('change'` (atau setara wiring file input) ≥1.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>Pilih file → thumbnail instan; edit → thumbnail lama prefill dari JSON + alt terisi + tombol Ganti/checkbox muncul; reset bersih; build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: _PreviewQuestion render <img> soal + opsi (RND-04 / SC #3, partial)</name>
  <read_first>
    - Views/Admin/_PreviewQuestion.cshtml (LENGKAP — Model PackageQuestion, question text L17, option loop list-group L45-60)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-UI-SPEC.md (Preview render sizing: soal 240px / opsi 120px, rounded border, loading=lazy, alt, render nothing bila null)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md ("_PreviewQuestion.cshtml" assignment — snippet razor)
  </read_first>
  <files>Views/Admin/_PreviewQuestion.cshtml</files>
  <action>
1) **Gambar SOAL** — tepat SETELAH `<p ...>@Model.QuestionText</p>` (L17), sisipkan:
```razor
@if (!string.IsNullOrWhiteSpace(Model.ImagePath))
{
    <img src="@Model.ImagePath" alt="@Model.ImageAlt" class="img-fluid rounded border mb-3"
         style="max-height:240px" loading="lazy" />
}
```

2) **Gambar OPSI** — di dalam loop opsi (L45-60), di dalam `<label class="list-group-item ...">` setiap opsi (dekat `<span>@opt.OptionText</span>` L60), sisipkan:
```razor
@if (!string.IsNullOrWhiteSpace(opt.ImagePath))
{
    <img src="@opt.ImagePath" alt="@opt.ImageAlt" class="img-fluid rounded border"
         style="max-height:120px" loading="lazy" />
}
```
(Sesuaikan nama variabel iterator loop existing — `opt` per kode terbaca; pakai variabel loop aktual.)

3) Render NOTHING bila ImagePath null (no placeholder text — Copywriting Contract). `alt="@Model.ImageAlt"` valid kosong (decorative) bila ImageAlt null. JANGAN ubah teks/struktur opsi existing — `<img>` adalah tambahan.

4) Caps WAJIB sesuai UI-SPEC: soal `max-height:240px`, opsi `max-height:120px`, keduanya `img-fluid rounded border` + `loading="lazy"`.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `<img` di _PreviewQuestion.cshtml ≥2 (soal + opsi).
    - grep `max-height:240px` = 1 (soal); grep `max-height:120px` = 1 (opsi).
    - grep `Model.ImagePath` ≥1 DAN `opt.ImagePath` (atau nama iterator aktual `.ImagePath`) ≥1.
    - grep `loading="lazy"` ≥2.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>Preview admin render gambar soal (cap 240px) + tiap opsi (cap 120px) dengan alt+lazy; null → tidak render; build hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: DeleteQuestion atomic delete + ref-count (SYN-02 / SC #5/#6, controller)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (DeleteQuestion POST L6377-6425 LENGKAP — Include Options L6380, responses L6385-6388, RemoveRange Options+Remove q L6390-6391, SaveChanges L6392, catch DbUpdateException L6396, auto-sync L6417-6425)
    - Controllers/CDPController.cs (L2456-2563 DeleteCoachingSession — pola Phase 333: outer-scope List<string>?, build inside, File.Delete POST commit warn-only)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md (SHARED-1 + SHARED-2 + "DeleteQuestion POST" assignment)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md (Pattern 2 ref-count, Pitfall 2 ordering)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <behavior>
    - Hapus soal bergambar (soal + ≥1 opsi punya ImagePath) tanpa shared → semua file fisik terhapus dari disk setelah commit.
    - Shared-file (path soal/opsi juga dipakai baris Post lain) → ref-count > 0 → File.Delete di-SKIP (file tetap ada; SC #6 no double-delete).
    - File.Delete gagal (file sudah hilang/locked) → warn-only, TIDAK throw, hapus DB tetap sukses.
    - Soal tanpa gambar → tidak ada file op (no crash).
    - Urutan: SaveChanges → auto-sync existing → ref-count → File.Delete (Pitfall 2 / OQ2).
  </behavior>
  <action>
1) **Path-collect SEBELUM delete** — setelah entity dimuat (Include Options L6380), SEBELUM `RemoveRange`/`Remove` (L6390), kumpulkan:
```csharp
var imagePathsToDelete = new List<string>();
if (!string.IsNullOrEmpty(q.ImagePath)) imagePathsToDelete.Add(q.ImagePath);
foreach (var o in q.Options)
    if (!string.IsNullOrEmpty(o.ImagePath)) imagePathsToDelete.Add(o.ImagePath);
```
(Pastikan Options ter-Include; jika belum, tambahkan `.Include(x => x.Options)` pada query muat soal.)

2) **Pertahankan** RemoveRange(q.Options) + Remove(q) + SaveChanges existing (L6390-6392) + catch(DbUpdateException) existing (L6396) + blok auto-sync existing (L6417-6425). JANGAN ubah cascade/audit existing.

3) **Ref-count + File.Delete SETELAH auto-sync** (SHARED-2 + SHARED-1, OQ2 ordering — taruh di akhir, sebelum return sukses, setelah blok auto-sync L6425):
```csharp
foreach (var relUrl in imagePathsToDelete)
{
    bool stillUsedQ = await _context.PackageQuestions.AnyAsync(x => x.ImagePath == relUrl);
    bool stillUsedO = await _context.PackageOptions.AnyAsync(x => x.ImagePath == relUrl);
    if (stillUsedQ || stillUsedO) continue;   // D-10: masih dipakai Post/lain → SKIP (SC #6)
    try
    {
        var physical = Path.Combine(_env.WebRootPath, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
        if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
    }
    catch (Exception fex)
    {
        _logger.LogWarning(fex, "File.Delete post-commit failed (DeleteQuestion image): {Path}", relUrl);
    }
}
```

4) **Tx note (C-04):** DeleteQuestion saat ini single-SaveChanges (tanpa BeginTransactionAsync). Pertahankan single-SaveChanges + post-save ref-count delete (lebih ringan; delete tetap post-persist + warn-only + ref-counted = memenuhi C-04). JANGAN bungkus tx baru kecuali perlu — konsisten dengan pola existing method ini. (Bila membungkus tx, build pathsToDelete outer-scope + delete setelah CommitAsync per SHARED-1.)

5) KRITIS: ref-count HARUS setelah SaveChanges DAN auto-sync — kalau tidak, baris yang baru di-resync mungkin masih merujuk path → file ter-skip selamanya (orphan) ATAU shared file salah-hapus.
  </action>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~RefCount|FullyQualifiedName~DeletePackageImage"</automated>
  </verify>
  <acceptance_criteria>
    - grep `imagePathsToDelete` di DeleteQuestion ≥1 (path-collect).
    - grep `AnyAsync(x => x.ImagePath == relUrl)` di DeleteQuestion ≥1 (ref-count D-10).
    - grep `File.Delete` DAN `LogWarning` di blok DeleteQuestion ≥1 (warn-only).
    - Path-collect berada SEBELUM RemoveRange; ref-count loop SETELAH blok auto-sync (verifikasi urutan saat read).
    - `dotnet build` exit 0; suite RefCount tetap hijau.
  </acceptance_criteria>
  <done>DeleteQuestion menghapus file soal+opsi atomic setelah commit dengan ref-count guard + warn-only; build + RefCount hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: DeletePackage atomic delete + ref-count (D-11 / SC #5/#6) + test path-collect (controller + test)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (DeletePackage POST L5457-5525 LENGKAP — Include Questions+Options L5459-5461, cascade responses L5471-5475, assignments L5478-5482, RemoveRange options+questions+package L5484-5487, SaveChanges L5489, catch L5491, audit L5499-5512, auto-sync L5517-5525)
    - Controllers/CDPController.cs (L2456-2563 — pola Phase 333)
    - HcPortal.Tests/PackageImageDeleteTests.cs (file dari Plan 01/02 — tambahkan test, jangan tulis ulang; reuse MakeTempDir)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md ("DeletePackage POST" assignment — D-11, OQ2 ref-count after auto-sync)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-CONTEXT.md (D-11 + OQ2 catatan auto-sync L5518-5524)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs, HcPortal.Tests/PackageImageDeleteTests.cs</files>
  <behavior>
    - Hapus paket dengan soal+opsi bergambar → semua file fisik (non-shared) terhapus setelah commit.
    - Shared dengan Post yang masih ada → ref-count > 0 → SKIP (file aman). Bila Pre+Post sama-sama terhapus (count==0) → file terhapus.
    - File.Delete gagal → warn-only, tidak throw.
    - Path-collect = union semua ImagePath non-null soal+opsi paket, dikumpulkan SEBELUM RemoveRange.
    - OQ2: ref-count SETELAH auto-sync (L5517-5525), karena auto-sync me-rebuild Post dengan shared path.
  </behavior>
  <action>
**A) Controller (DeletePackage L5457):**
1) **Path-collect SEBELUM cascade RemoveRange** (entity Questions+Options sudah Include L5459-5461; jika belum, tambah `.Include(p => p.Questions).ThenInclude(q => q.Options)`):
```csharp
var imagePathsToDelete = new List<string>();
foreach (var q in pkg.Questions)
{
    if (!string.IsNullOrEmpty(q.ImagePath)) imagePathsToDelete.Add(q.ImagePath);
    foreach (var o in q.Options)
        if (!string.IsNullOrEmpty(o.ImagePath)) imagePathsToDelete.Add(o.ImagePath);
}
```
(Gunakan nama variabel entity paket aktual — baca kode; di sini `pkg`.)

2) **Pertahankan** cascade existing (responses/assignments/options/questions/package RemoveRange L5484-5487) + SaveChanges (L5489) + catch (L5491) + audit (L5499-5512) + auto-sync (L5517-5525) verbatim.

3) **Ref-count + File.Delete SETELAH auto-sync (OQ2)** — sisipkan setelah blok auto-sync (L5525), sebelum return sukses. Loop IDENTIK pola Task 4:
```csharp
foreach (var relUrl in imagePathsToDelete)
{
    bool stillUsedQ = await _context.PackageQuestions.AnyAsync(x => x.ImagePath == relUrl);
    bool stillUsedO = await _context.PackageOptions.AnyAsync(x => x.ImagePath == relUrl);
    if (stillUsedQ || stillUsedO) continue;   // D-10/D-11: dipakai Post/lain → SKIP
    try
    {
        var physical = Path.Combine(_env.WebRootPath, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
        if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
    }
    catch (Exception fex)
    {
        _logger.LogWarning(fex, "File.Delete post-commit failed (DeletePackage image): {Path}", relUrl);
    }
}
```
**Catatan OQ2 (kritis):** ref-count SETELAH auto-sync — auto-sync DeletePackage me-rebuild paket Post (L5518-5524) yang mungkin masih share path; ref-count post-sync memastikan file yang masih dishare TIDAK terhapus, sebaliknya yang benar-benar yatim (Pre+Post hilang) terhapus.

4) Tx: pertahankan pola single-SaveChanges existing method ini + post-save delete (C-04). Jangan bungkus tx baru kecuali method sudah punya.

**B) Test (PackageImageDeleteTests.cs):** tambahkan (jangan tulis ulang file):
- `DeletePackageImage_CollectsAllNonNullPaths`: bangun paket InMemory dengan soal+opsi (campur ImagePath null & non-null) → jalankan logika path-collect (mirror langkah A.1) → assert list = union semua path non-null, tanpa null.
- `RefCount_DeletePackage_SkipsShared_DeletesOrphan` (atau perluas test RefCount existing): seed 2 baris share path → AnyAsync==true → SKIP (File.Exists tetap true via MakeTempDir dummy file); hapus referensi → AnyAsync==false → File.Delete → File.Exists==false. Pakai try/finally cleanup temp dir.
Komentar header: "mirror of DeletePackage path-collect + SHARED-2 ref-count (D-11) — keep in sync".
  </action>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~DeletePackageImage|FullyQualifiedName~RefCount"</automated>
  </verify>
  <acceptance_criteria>
    - grep `imagePathsToDelete` di DeletePackage ≥1 (path-collect).
    - grep `AnyAsync(x => x.ImagePath == relUrl)` di DeletePackage ≥1 (ref-count).
    - grep `File.Delete` DAN `LogWarning` di blok DeletePackage ≥1.
    - Path-collect SEBELUM cascade RemoveRange; ref-count loop SETELAH auto-sync (verifikasi urutan saat read).
    - Test `DeletePackageImage_CollectsAllNonNullPaths` GREEN.
    - `dotnet build` exit 0; full suite tidak regresi.
  </acceptance_criteria>
  <done>DeletePackage menghapus file soal+opsi atomic post-commit + ref-count setelah auto-sync (OQ2) + warn-only; path-collect test GREEN; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → ManagePackageQuestions form (multipart) | File + alt text + checkbox melewati batas (binding ke Plan 02 handler); input tak terpercaya. |
| Controller → filesystem (wwwroot/uploads/questions) | Penghapusan file fisik saat DeleteQuestion/DeletePackage. |
| _PreviewQuestion render → browser | ImagePath/ImageAlt dari DB ditampilkan di `<img src>`/`alt`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-353-03-01 | Tampering | Shared-file salah-hapus (Pre dihapus tapi Post masih pakai) | mitigate | Ref-count (AnyAsync ImagePath==relUrl) SETELAH auto-sync sebelum File.Delete (D-10/D-11/OQ2) di DeleteQuestion + DeletePackage. |
| T-353-03-02 | Denial of Service | File.Delete throw (file locked/hilang) membatalkan request | mitigate | Inner try/catch warn-only per file (pola 333); hapus DB tetap commit, tidak throw. |
| T-353-03-03 | Cross-Site Scripting | ImageAlt berisi markup → render di preview | mitigate | Razor `alt="@Model.ImageAlt"` auto-HTML-encode (default Razor); src adalah path internal `/uploads/...`. |
| T-353-03-04 | Tampering | Path traversal pada relUrl saat membentuk physical path | mitigate | relUrl berasal dari ImagePath yang dibuat SaveFileAsync (strip traversal Phase 352); `TrimStart('/')` + `Path.Combine(_env.WebRootPath,...)` tetap dalam wwwroot. |
| T-353-03-05 | Spoofing | CSRF pada DeleteQuestion/DeletePackage | mitigate | `[ValidateAntiForgeryToken]` existing dipertahankan; form view `@Html.AntiForgeryToken()` dipertahankan. |
| T-353-03-06 | Elevation of Privilege | Non-admin akses delete | mitigate | `[Authorize(Roles="Admin, HC")]` existing dipertahankan. |
| T-353-03-07 | Information Disclosure | Pesan exception bocor ke user saat delete gagal | accept/mitigate | catch(DbUpdateException) existing memakai TempData friendly tanpa ex.Message (pola 333/334); File.Delete warn-only ke log internal. |
</threat_model>

<verification>
- `dotnet build` exit 0 (Razor + controller compile).
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` tidak regresi; suite RefCount + DeletePackageImage hijau.
- `dotnet run` (localhost:5277) sehat — manual/Playwright smoke (dikonsolidasi Phase 355): upload soal+opsi → simpan → preview render gambar → edit thumbnail prefill → hapus soal → file hilang dari disk (kecuali shared dengan Post).
- Integrasi Plan 02↔03: name= field form cocok param controller (binding hidup) — verifikasi 1 upload end-to-end lokal.
</verification>

<success_criteria>
- SC #1 (lengkap): form multipart + field gambar inline soal+opsi kirim file → tersimpan (binding Plan 02 + enctype Plan 03).
- SC #2 (lengkap): edit thumbnail prefill + Ganti/Hapus (JS Task 2 + JSON Plan 02).
- SC #3: `_PreviewQuestion` render `<img>` soal + opsi (RND-04).
- SC #5/#6: DeleteQuestion + DeletePackage hapus file atomic post-commit + ref-count (no double-delete shared, no orphan).
- SC #7 (sebagian): build + xUnit hijau; sisa Playwright UAT = Phase 355.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-03-SUMMARY.md` (catat: keputusan tx single-SaveChanges vs wrap di delete; konfirmasi urutan ref-count after auto-sync OQ2; integrasi name= field ↔ param Plan 02 ter-verifikasi build).
</output>
