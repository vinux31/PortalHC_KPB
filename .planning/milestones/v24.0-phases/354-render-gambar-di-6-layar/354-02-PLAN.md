---
phase: 354-render-gambar-di-6-layar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/PackageExamViewModel.cs
  - Models/AssessmentResultsViewModel.cs
  - Models/AssessmentMonitoringViewModel.cs
  - Models/EditPesertaAnswersViewModel.cs
autonomous: true
requirements: [RND-01, RND-02, RND-03, RND-05, RND-06]
must_haves:
  truths:
    - "Tiap ViewModel item soal & opsi membawa ImagePath + ImageAlt (kecuali essay grading = soal saja)"
    - "ExamSummaryItem bisa membawa gambar soal DAN list opsi-dengan-gambar untuk render block-bawah (RND-02)"
  artifacts:
    - path: "Models/PackageExamViewModel.cs"
      provides: "ImagePath/ImageAlt di ExamQuestionItem, ExamOptionItem, ExamSummaryItem (+carrier opsi)"
      contains: "public string? ImagePath"
    - path: "Models/AssessmentResultsViewModel.cs"
      provides: "ImagePath/ImageAlt di QuestionReviewItem, OptionReviewItem"
      contains: "public string? ImagePath"
    - path: "Models/AssessmentMonitoringViewModel.cs"
      provides: "ImagePath/ImageAlt di EssayGradingItemViewModel (soal saja)"
      contains: "public string? ImagePath"
    - path: "Models/EditPesertaAnswersViewModel.cs"
      provides: "ImagePath/ImageAlt di EditQuestionRow, EditOptionRow"
      contains: "public string? ImagePath"
  key_links:
    - from: "ViewModel item"
      to: "entity PackageQuestion/PackageOption ImagePath/ImageAlt"
      via: "populate assignment di controller (Plan 03/04)"
      pattern: "public string\\? ImagePath"
---

<objective>
Plumbing data (Gap 2, L-01): tambah field `ImagePath` + `ImageAlt` ke item-per-soal/opsi di 4 ViewModel agar gambar mengalir DB→controller→view. Termasuk menyiapkan `ExamSummaryItem` (saat ini text-only) untuk membawa gambar soal + opsi (RND-02 mensyaratkan gambar opsi block-bawah di ExamSummary).

Purpose: Tanpa field VM, view tidak punya data gambar untuk di-render. Ini fondasi yang meng-unblock populate (Plan 03/04) dan render (Plan 05/06).
Output: 4 file ViewModel ditambahi field gambar. File berbeda dari Plan 01 → parallel-safe (Wave 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/354-render-gambar-di-6-layar/354-CONTEXT.md
@.planning/phases/354-render-gambar-di-6-layar/354-PATTERNS.md

<interfaces>
<!-- Entity sumber (Models/AssessmentPackage.cs): PackageQuestion.ImagePath/ImageAlt + PackageOption.ImagePath/ImageAlt — sudah ada sejak Phase 352, terisi via Phase 353. Konvensi nama field = ikuti sumber: ImagePath, ImageAlt. -->

<!-- ExamSummaryItem SAAT INI (Models/PackageExamViewModel.cs L50-76) text-only:
     menyimpan DisplayNumber, QuestionText, SelectedOptionId, SelectedOptionText,
     QuestionType, SelectedOptionTexts (huruf "A","C"), TextAnswer. TIDAK menyimpan list objek opsi.
     Untuk render gambar opsi di ExamSummary → perlu carrier opsi-dengan-gambar. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tambah field gambar ke PackageExamViewModel.cs (ExamQuestionItem, ExamOptionItem, ExamSummaryItem + carrier opsi)</name>
  <read_first>
    - Models/PackageExamViewModel.cs (struktur penuh — ExamQuestionItem L25-41, ExamOptionItem L43-48, ExamSummaryItem L50-76)
    - .planning/phases/354-render-gambar-di-6-layar/354-PATTERNS.md (catatan ExamSummaryItem text-only → planner putuskan carrier opsi)
    - .planning/phases/354-render-gambar-di-6-layar/354-UI-SPEC.md Konsistensi (ExamSummary WAJIB opsi 120px block-bawah)
  </read_first>
  <action>
Di `Models/PackageExamViewModel.cs`:

1. `ExamQuestionItem` (L25-41) — tambah dua property:
```csharp
public string? ImagePath { get; set; }
public string? ImageAlt { get; set; }
```

2. `ExamOptionItem` (L43-48) — tambah dua property:
```csharp
public string? ImagePath { get; set; }
public string? ImageAlt { get; set; }
```

3. `ExamSummaryItem` (L50-76) — tambah gambar SOAL + carrier OPSI. Item ini saat ini text-only (tidak punya list objek opsi). RND-02 + UI-SPEC mensyaratkan gambar opsi block-bawah di ExamSummary. Tambahkan:
```csharp
// RND-02: gambar soal di ExamSummary
public string? ImagePath { get; set; }
public string? ImageAlt { get; set; }

// RND-02: carrier opsi-dengan-gambar untuk render block-bawah di ExamSummary.
// Diisi controller (Plan 03) dari q.Options. Hanya berisi opsi yang relevan ditampilkan.
public List<ExamSummaryOptionItem> OptionImages { get; set; } = new();
```

4. Tambah class baru `ExamSummaryOptionItem` (di namespace yang sama, setelah ExamSummaryItem):
```csharp
public class ExamSummaryOptionItem
{
    public int OptionId { get; set; }
    public string OptionText { get; set; } = "";
    public string? ImagePath { get; set; }
    public string? ImageAlt { get; set; }
}
```

Catatan: properti existing ExamSummaryItem (SelectedOptionTexts, IsAnswered, dsb) JANGAN diubah — hanya tambah. `OptionImages` adalah list terpisah agar view ExamSummary (Plan 05) bisa render gambar opsi block-bawah tanpa mengganggu logika tabel jawaban existing.
  </action>
  <verify>
    <automated>Select-String -Path "Models\PackageExamViewModel.cs" -Pattern "public string\? ImagePath","public string\? ImageAlt","class ExamSummaryOptionItem","List<ExamSummaryOptionItem> OptionImages"</automated>
  </verify>
  <acceptance_criteria>
    - `Models/PackageExamViewModel.cs` contains `public string? ImagePath` minimal 4x (ExamQuestionItem, ExamOptionItem, ExamSummaryItem, ExamSummaryOptionItem)
    - file contains `class ExamSummaryOptionItem`
    - file contains `List<ExamSummaryOptionItem> OptionImages`
    - property existing ExamSummaryItem (SelectedOptionTexts, TextAnswer, IsAnswered) masih ada (tidak terhapus)
  </acceptance_criteria>
  <done>ExamQuestionItem/ExamOptionItem/ExamSummaryItem punya ImagePath+ImageAlt; ExamSummaryOptionItem carrier opsi gambar dibuat.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tambah field gambar ke AssessmentResultsViewModel.cs + AssessmentMonitoringViewModel.cs</name>
  <read_first>
    - Models/AssessmentResultsViewModel.cs (QuestionReviewItem L24-33, OptionReviewItem L35-40)
    - Models/AssessmentMonitoringViewModel.cs (EssayGradingItemViewModel L73-82)
    - .planning/phases/354-render-gambar-di-6-layar/354-CONTEXT.md L-01 (EssayGradingItemViewModel = gambar SOAL SAJA, essay tak punya opsi RND-05)
  </read_first>
  <action>
1. Di `Models/AssessmentResultsViewModel.cs`:
   - `QuestionReviewItem` (L24-33) — tambah `public string? ImagePath { get; set; }` + `public string? ImageAlt { get; set; }` (soal, RND-03).
   - `OptionReviewItem` (L35-40) — tambah `public string? ImagePath { get; set; }` + `public string? ImageAlt { get; set; }` (opsi, RND-03).

2. Di `Models/AssessmentMonitoringViewModel.cs`:
   - `EssayGradingItemViewModel` (L73-82) — tambah HANYA gambar SOAL: `public string? ImagePath { get; set; }` + `public string? ImageAlt { get; set; }`. JANGAN tambah field opsi (essay tak punya opsi — RND-05 soal saja, L-01).

Property existing JANGAN diubah — hanya tambah.
  </action>
  <verify>
    <automated>Select-String -Path "Models\AssessmentResultsViewModel.cs" -Pattern "public string\? ImagePath"; Select-String -Path "Models\AssessmentMonitoringViewModel.cs" -Pattern "public string\? ImagePath"</automated>
  </verify>
  <acceptance_criteria>
    - `Models/AssessmentResultsViewModel.cs` contains `public string? ImagePath` minimal 2x (QuestionReviewItem + OptionReviewItem)
    - `Models/AssessmentResultsViewModel.cs` contains `public string? ImageAlt` minimal 2x
    - `Models/AssessmentMonitoringViewModel.cs` contains `public string? ImagePath` minimal 1x (EssayGradingItemViewModel)
    - `Models/AssessmentMonitoringViewModel.cs` EssayGradingItemViewModel TIDAK punya field opsi (no OptionImages/Options gambar)
  </acceptance_criteria>
  <done>QuestionReviewItem+OptionReviewItem punya ImagePath/ImageAlt; EssayGradingItemViewModel punya ImagePath/ImageAlt soal saja.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tambah field gambar ke EditPesertaAnswersViewModel.cs + verifikasi build</name>
  <read_first>
    - Models/EditPesertaAnswersViewModel.cs (EditQuestionRow L12-21, EditOptionRow L23-28)
    - CLAUDE.md (Develop Workflow — dotnet build wajib)
  </read_first>
  <action>
1. Di `Models/EditPesertaAnswersViewModel.cs`:
   - `EditQuestionRow` (L12-21) — tambah `public string? ImagePath { get; set; }` + `public string? ImageAlt { get; set; }` (soal, RND-06).
   - `EditOptionRow` (L23-28) — tambah `public string? ImagePath { get; set; }` + `public string? ImageAlt { get; set; }` (opsi, RND-06).

2. Jalankan `dotnet build` — semua field baru harus kompilasi 0 error. Tidak ada populate/render di plan ini (hanya field), jadi tidak ada perubahan perilaku — build memvalidasi sintaks property.
  </action>
  <verify>
    <automated>Select-String -Path "Models\EditPesertaAnswersViewModel.cs" -Pattern "public string\? ImagePath"; dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Models/EditPesertaAnswersViewModel.cs` contains `public string? ImagePath` minimal 2x (EditQuestionRow + EditOptionRow)
    - `Models/EditPesertaAnswersViewModel.cs` contains `public string? ImageAlt` minimal 2x
    - `dotnet build` output contains "Build succeeded", 0 baris ": error"
  </acceptance_criteria>
  <done>EditQuestionRow+EditOptionRow punya ImagePath/ImageAlt; build hijau dengan semua field baru di 4 VM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none baru) | Plan ini hanya menambah property string nullable ke POCO ViewModel — tidak ada I/O, render, atau input baru |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-03 | Information disclosure | ViewModel field ImagePath/ImageAlt | accept | Field hanya membawa path relatif yang sama dengan yang sudah disimpan admin (Phase 353). Tidak mengekspos data baru. Render aman ditangani Plan 01 partial (encode + render-if-not-null). |
</threat_model>

<verification>
- `dotnet build` succeeded 0 error.
- 4 VM punya ImagePath/ImageAlt di item soal & opsi (acceptance criteria per task); EssayGradingItemViewModel soal saja.
- ExamSummaryItem punya carrier OptionImages untuk RND-02.
</verification>

<success_criteria>
- L-01 (Gap 2) terpenuhi: 4 ViewModel bawa gambar soal + opsi (kecuali essay = soal saja).
- ExamSummary siap render gambar opsi block-bawah (carrier OptionImages).
- Build lokal hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/354-render-gambar-di-6-layar/354-02-SUMMARY.md`
</output>
