---
phase: 354-render-gambar-di-6-layar
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [RND-05, RND-06]
must_haves:
  truths:
    - "Essay grading populate mengisi ImagePath/ImageAlt soal (EssayGradingItemViewModel) — soal saja"
    - "EditPesertaAnswers populate mengisi ImagePath/ImageAlt soal (EditQuestionRow) + opsi (EditOptionRow)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Populate gambar di 2 loop (essay grading L3397, EditPesertaAnswers L2986)"
      contains: "ImagePath = q.ImagePath"
  key_links:
    - from: "AssessmentAdminController populate loop"
      to: "EssayGradingItemViewModel/EditQuestionRow/EditOptionRow"
      via: "assignment ImagePath = q.ImagePath / o.ImagePath"
      pattern: "ImagePath = (q|o)\\.ImagePath"
---

<objective>
Isi data gambar (RND-05/06) di 2 populate loop `AssessmentAdminController.cs`: essay grading (~L3397-3406, soal SAJA) dan EditPesertaAnswers (~L2986-2998, soal + opsi). Map entity→VM field (Plan 02). Setelah ini, view admin (Plan 06) punya data render.

Purpose: Wiring DB→VM untuk 2 layar admin (nilai essay + edit jawaban peserta).
Output: AssessmentAdminController.cs (1 file). Tidak menyentuh CMPController (Plan 03) → parallel-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/354-render-gambar-di-6-layar/354-PATTERNS.md

<interfaces>
<!-- Essay grading L3397-3406 (PATTERNS.md): map dari q (PackageQuestion).
     essayQs di-query L3388-3390 _context.PackageQuestions.Where(...Essay) — kolom ImagePath/ImageAlt auto-load.
     new EssayGradingItemViewModel { QuestionId=q.Id, QuestionText=q.QuestionText, ... }. SOAL SAJA (RND-05). -->

<!-- EditPesertaAnswers L2986-2998: map dari q + o.
     new EditQuestionRow { PackageQuestionId=q.Id, QuestionText=q.QuestionText, QuestionType=...,
       Options = q.Options.Select(o => new EditOptionRow { Id=o.Id, OptionText=o.OptionText, IsCorrect=o.IsCorrect }) }.
     questions di-query dengan .Include(q => q.Options) (verifikasi ~L2965-2978). -->

<!-- Field VM baru (Plan 02): EssayGradingItemViewModel.ImagePath/ImageAlt (soal saja);
     EditQuestionRow.ImagePath/ImageAlt + EditOptionRow.ImagePath/ImageAlt. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Populate gambar soal di essay grading (~L3397-3406, RND-05 soal saja)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs L3385-3410 (essay grading populate + query essayQs L3388-3390)
    - Models/AssessmentMonitoringViewModel.cs (EssayGradingItemViewModel field baru Plan 02)
    - .planning/phases/354-render-gambar-di-6-layar/354-CONTEXT.md L-01 (essay = SOAL SAJA, no opsi)
  </read_first>
  <action>
Di `AssessmentAdminController.cs` essay grading populate (~L3397-3406), pada `new EssayGradingItemViewModel { QuestionId = q.Id, QuestionText = q.QuestionText, ... }` → tambah HANYA gambar soal:
```csharp
ImagePath = q.ImagePath,
ImageAlt = q.ImageAlt,
```
JANGAN tambah field opsi (essay tak punya opsi, RND-05). Query `essayQs` (L3388-3390 `_context.PackageQuestions.Where(...Essay)`) sudah load entity penuh → ImagePath/ImageAlt scalar auto-load, tidak perlu ubah query (PATTERNS.md). Verifikasi tidak ada projection eksplisit yang membatasi kolom saat read_first.
  </action>
  <verify>
    <automated>Select-String -Path "Controllers\AssessmentAdminController.cs" -Pattern "ImagePath = q\.ImagePath","ImageAlt = q\.ImageAlt"</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/AssessmentAdminController.cs` contains `ImagePath = q.ImagePath` dan `ImageAlt = q.ImageAlt` (EssayGradingItemViewModel)
    - tidak ada penambahan field opsi gambar di EssayGradingItemViewModel populate
  </acceptance_criteria>
  <done>EssayGradingItemViewModel terisi gambar soal saja.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate gambar soal + opsi di EditPesertaAnswers (~L2986-2998, RND-06) + build</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs L2960-3000 (EditPesertaAnswers populate + query questions/responses L2965-2978 — konfirmasi .Include(q => q.Options))
    - Models/EditPesertaAnswersViewModel.cs (EditQuestionRow/EditOptionRow field baru Plan 02)
    - CLAUDE.md (dotnet build wajib)
  </read_first>
  <action>
Di `AssessmentAdminController.cs` EditPesertaAnswers populate (~L2986-2998):

1. Pada `new HcPortal.Models.EditQuestionRow { PackageQuestionId = q.Id, QuestionText = q.QuestionText, ... }` → tambah:
```csharp
ImagePath = q.ImagePath,
ImageAlt = q.ImageAlt,
```

2. Pada `new HcPortal.Models.EditOptionRow { Id = o.Id, OptionText = o.OptionText, IsCorrect = o.IsCorrect }` → tambah:
```csharp
ImagePath = o.ImagePath,
ImageAlt = o.ImageAlt,
```

3. Verifikasi query `questions` di-load dengan `.Include(q => q.Options)` (~L2965-2978). Bila Options belum di-Include, tambahkan agar o.ImagePath ter-load.

4. Jalankan `dotnet build` — 0 error.
  </action>
  <verify>
    <automated>Select-String -Path "Controllers\AssessmentAdminController.cs" -Pattern "ImagePath = o\.ImagePath","ImageAlt = o\.ImageAlt"; dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - file contains `ImagePath = o.ImagePath` dan `ImageAlt = o.ImageAlt` (EditOptionRow)
    - file contains `ImagePath = q.ImagePath` minimal 2x (essay Task 1 + EditQuestionRow)
    - `.Include(q => q.Options)` ada di query EditPesertaAnswers
    - `dotnet build` output contains "Build succeeded", 0 baris ": error"
  </acceptance_criteria>
  <done>EditQuestionRow + EditOptionRow terisi gambar; Include(Options) terverifikasi; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB entity → ViewModel (admin surface) | Path gambar admin-input dipetakan ke VM admin (render Plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-06 | XSS (deferred to render) | populate ImagePath ke VM admin | mitigate | Populate copy string DB→VM; encoding di render (Plan 01 partial Razor-encoded). No XSS surface baru. Surface admin-only (authenticated). |
</threat_model>

<verification>
- `dotnet build` succeeded 0 error.
- 2 loop AssessmentAdminController terisi gambar (essay soal-saja, EditPeserta soal+opsi).
- Render verifikasi visual di Plan 06.
</verification>

<success_criteria>
- RND-05/06 data-side: admin essay grading menerima gambar soal; EditPesertaAnswers menerima gambar soal + opsi.
- Build lokal hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/354-render-gambar-di-6-layar/354-04-SUMMARY.md`
</output>
