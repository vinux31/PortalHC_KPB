---
phase: 354-render-gambar-di-6-layar
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - Views/CMP/StartExam.cshtml
  - Views/CMP/ExamSummary.cshtml
  - Views/CMP/Results.cshtml
autonomous: false
requirements: [RND-01, RND-02, RND-03, RND-07]
must_haves:
  truths:
    - "Peserta melihat gambar soal (240px) + opsi (120px block-bawah) saat ujian (StartExam)"
    - "Peserta melihat gambar soal + opsi di review sebelum submit (ExamSummary)"
    - "Peserta melihat gambar soal + opsi di halaman hasil/pembahasan (Results)"
    - "Klik gambar membuka lightbox full-res di ketiga layar"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Render gambar soal+opsi + lightbox (RND-01)"
      contains: "_QuestionImage"
    - path: "Views/CMP/ExamSummary.cshtml"
      provides: "Render gambar soal+opsi block-bawah + lightbox (RND-02)"
      contains: "_QuestionImage"
    - path: "Views/CMP/Results.cshtml"
      provides: "Render gambar soal+opsi + lightbox (RND-03)"
      contains: "_QuestionImage"
  key_links:
    - from: "view peserta"
      to: "_QuestionImage.cshtml + _ImageLightboxModal.cshtml"
      via: "@await Html.PartialAsync"
      pattern: "PartialAsync\\(\"_QuestionImage\""
---

<objective>
Render gambar (RND-01/02/03/07) di 3 view peserta: StartExam, ExamSummary, Results. Setiap view memanggil partial `_QuestionImage` (Plan 01) di titik soal + opsi (opsi block-bawah D-03) dan menyertakan partial lightbox `_ImageLightboxModal` sekali per halaman. Verifikasi visual via Playwright (checkpoint human-verify, SC#5).

Purpose: Layar yang membuat gambar benar-benar TAMPIL ke peserta. Menutup RND-01/02/03 + RND-07 (responsif inheren via partial).
Output: 3 view CMP. File berbeda dari Plan 06 → parallel-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/354-render-gambar-di-6-layar/354-UI-SPEC.md
@.planning/phases/354-render-gambar-di-6-layar/354-PATTERNS.md
@Views/Shared/_QuestionImage.cshtml
@Views/Shared/_ImageLightboxModal.cshtml

<interfaces>
<!-- Pemanggilan partial gambar SOAL:
     @await Html.PartialAsync("_QuestionImage", new { ImagePath = ITEM.ImagePath, ImageAlt = ITEM.ImageAlt, Cap = 240 })
   Pemanggilan partial gambar OPSI (block-bawah, cap 120):
     @await Html.PartialAsync("_QuestionImage", new { ImagePath = OPT.ImagePath, ImageAlt = OPT.ImageAlt, Cap = 120, AriaContext = "opsi " + letter })
   Lightbox (sekali per halaman, sebelum @section Scripts atau di akhir body):
     @await Html.PartialAsync("_ImageLightboxModal") -->

<!-- StartExam titik soal L100-104 (<p class="fw-bold mb-3">...@q.QuestionText</p>); opsi MA L134-145 + MC L164-174 (<span>@opt.OptionText</span> dalam <label ... d-flex align-items-center gap-3>, input class exam-checkbox/exam-radio, id lbl_@q_@opt — JS selectors). -->
<!-- Results titik soal L329 (<h6>...@question.QuestionText</h6>); opsi L371-385 (<div class="list-group-item @itemClass d-flex align-items-center"> ... <span>@option.OptionText</span>). -->
<!-- ExamSummary: SAAT INI tabel text-only (L34-97). ExamSummaryItem.ImagePath + OptionImages (Plan 02/03). @section Scripts L176. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Render gambar + lightbox di StartExam.cshtml (RND-01)</name>
  <read_first>
    - Views/CMP/StartExam.cshtml L90-200 (loop soal + opsi MA/MC) + cari @section Scripts
    - Views/Shared/_QuestionImage.cshtml + _ImageLightboxModal.cshtml (Plan 01 — kontrak param)
    - .planning/phases/354-render-gambar-di-6-layar/354-UI-SPEC.md §3 (opsi block-bawah, mekanisme A diutamakan)
  </read_first>
  <action>
Di `Views/CMP/StartExam.cshtml`:

1. Gambar SOAL — setelah `<p class="fw-bold mb-3">...@q.QuestionText</p>` (L100-104), sebelum blok essay/MA/MC, sisipkan:
```cshtml
@await Html.PartialAsync("_QuestionImage", new { ImagePath = q.ImagePath, ImageAlt = q.ImageAlt, Cap = 240 })
```

2. Gambar OPSI (MA L134-145 + MC L164-174) — restruktur `<label>` jadi block-bawah (Mekanisme A diutamakan, UI-SPEC §3): ubah label ke `d-flex flex-column`, bungkus baris `[input] [huruf] [teks]` dalam `<div class="d-flex align-items-center gap-3">`, lalu panggil partial opsi sebagai block setelah div baris teks. Contoh MC:
```cshtml
<label class="list-group-item list-group-item-action d-flex flex-column" style="cursor: pointer;" id="lbl_@(q.QuestionId)_@opt.OptionId">
    <div class="d-flex align-items-center gap-3">
        <input class="form-check-input flex-shrink-0 exam-radio" type="radio" name="radio_@q.QuestionId" value="@opt.OptionId" data-question-id="@q.QuestionId" style="transform: scale(1.2);">
        <span class="fw-bold me-1 text-secondary">@letter.</span>
        <span>@opt.OptionText</span>
    </div>
    @await Html.PartialAsync("_QuestionImage", new { ImagePath = opt.ImagePath, ImageAlt = opt.ImageAlt, Cap = 120, AriaContext = "opsi " + letter })
</label>
```
Terapkan pola sama di cabang MA (input type=checkbox name=check_... class exam-checkbox). WAJIB pertahankan SEMUA atribut input existing (name, value, data-question-id, class exam-checkbox/exam-radio, id lbl_...) — JS auto-save bergantung pada selectors `exam-radio`/`exam-checkbox`/`#lbl_...`.

3. Lightbox — di akhir konten view (sebelum `@section Scripts`), sisipkan sekali:
```cshtml
@await Html.PartialAsync("_ImageLightboxModal")
```

Render-if-not-null ditangani partial (gambar null → render nothing, no slot kosong). Kriteria placement: gambar opsi di baris terpisah DI BAWAH teks, lebar penuh, tidak berdampingan (UI-SPEC §3).
  </action>
  <verify>
    <automated>Select-String -Path "Views\CMP\StartExam.cshtml" -Pattern "_QuestionImage","_ImageLightboxModal","Cap = 240","Cap = 120","d-flex flex-column","exam-radio","exam-checkbox"</automated>
  </verify>
  <acceptance_criteria>
    - StartExam.cshtml contains `_QuestionImage` minimal 3x (soal + MA opsi + MC opsi)
    - contains `Cap = 240` dan `Cap = 120`
    - contains `_ImageLightboxModal` (1x)
    - contains `d-flex flex-column` (label restruktur mekanisme A)
    - input opsi masih punya class `exam-radio` dan `exam-checkbox` (JS selectors utuh)
  </acceptance_criteria>
  <done>StartExam render gambar soal 240 + opsi 120 block-bawah + lightbox; JS auto-save selectors utuh.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render gambar + lightbox di ExamSummary.cshtml (RND-02) + Results.cshtml (RND-03) + build</name>
  <read_first>
    - Views/CMP/ExamSummary.cshtml (tabel L34-97 + @section Scripts L176; ExamSummaryItem.ImagePath + OptionImages dari Plan 02/03)
    - Views/CMP/Results.cshtml L320-389 (loop QuestionReviews soal L329 + opsi L371-385) + cari @section Scripts
    - Views/Shared/_QuestionImage.cshtml + _ImageLightboxModal.cshtml
    - CLAUDE.md (dotnet build wajib)
  </read_first>
  <action>
**ExamSummary.cshtml (RND-02):** tabel text-only. Di kolom Pertanyaan `<td>` (L50-56), setelah `<span ...>@item.QuestionText</span>`, sisipkan gambar soal + loop opsi bergambar (block-bawah):
```cshtml
@await Html.PartialAsync("_QuestionImage", new { ImagePath = item.ImagePath, ImageAlt = item.ImageAlt, Cap = 240 })
@foreach (var optImg in item.OptionImages)
{
    @await Html.PartialAsync("_QuestionImage", new { ImagePath = optImg.ImagePath, ImageAlt = optImg.ImageAlt, Cap = 120, AriaContext = "opsi" })
}
```
(Partial render nothing bila ImagePath null → hanya opsi bergambar tampil, block-bawah otomatis via d-block w-100 cap<=120.)
Sisipkan lightbox sekali sebelum `@section Scripts` (L176):
```cshtml
@await Html.PartialAsync("_ImageLightboxModal")
```

**Results.cshtml (RND-03):**
1. Gambar SOAL — setelah `<h6 class="mb-2"><strong>Soal @question.QuestionNumber:</strong> @question.QuestionText</h6>` (L329):
```cshtml
@await Html.PartialAsync("_QuestionImage", new { ImagePath = question.ImagePath, ImageAlt = question.ImageAlt, Cap = 240 })
```
2. Gambar OPSI — restruktur `<div class="list-group-item @itemClass d-flex align-items-center">` (L371) ke block-bawah:
```cshtml
<div class="list-group-item @itemClass d-flex flex-column">
    <div class="d-flex align-items-center">
        @if (!string.IsNullOrEmpty(icon)) { <i class="bi @icon me-2"></i> }
        <span>@option.OptionText</span>
        @if (option.IsSelected && !option.IsCorrect) { <small class="ms-auto text-muted">(Jawaban Anda)</small> }
        @if (option.IsCorrect) { <small class="ms-auto text-muted">(Jawaban Benar)</small> }
    </div>
    @await Html.PartialAsync("_QuestionImage", new { ImagePath = option.ImagePath, ImageAlt = option.ImageAlt, Cap = 120, AriaContext = "opsi" })
</div>
```
3. Lightbox sekali di akhir konten Results (sebelum @section Scripts bila ada, atau akhir body):
```cshtml
@await Html.PartialAsync("_ImageLightboxModal")
```
Pertahankan itemClass success/danger + label "(Jawaban Anda)"/"(Jawaban Benar)" + icon existing.

Jalankan `dotnet build` — 0 error.
  </action>
  <verify>
    <automated>Select-String -Path "Views\CMP\ExamSummary.cshtml" -Pattern "_QuestionImage","OptionImages","_ImageLightboxModal"; Select-String -Path "Views\CMP\Results.cshtml" -Pattern "_QuestionImage","_ImageLightboxModal","d-flex flex-column"; dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - ExamSummary.cshtml contains `_QuestionImage` (soal + opsi loop) dan `OptionImages` dan `_ImageLightboxModal`
    - Results.cshtml contains `_QuestionImage` minimal 2x (soal + opsi) dan `_ImageLightboxModal` dan `d-flex flex-column`
    - `dotnet build` output contains "Build succeeded", 0 baris ": error"
  </acceptance_criteria>
  <done>ExamSummary + Results render gambar soal + opsi block-bawah + lightbox; build hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi visual Playwright 3 layar peserta (SC#5)</name>
  <action>
Checkpoint human-verify — verifikasi visual end-to-end via browser/Playwright sesuai langkah di <how-to-verify>. Bukan tugas implementasi kode; executor menyiapkan `dotnet run` + data uji lalu pause untuk konfirmasi user.
  </action>
  <what-built>
    Gambar soal (240px) + opsi (120px block-bawah) + lightbox klik-perbesar di StartExam, ExamSummary, Results. Render via partial _QuestionImage + _ImageLightboxModal.
  </what-built>
  <how-to-verify>
    1. `dotnet run` → buka http://localhost:5277. Login peserta (kredensial Dev, lihat reference_dev_credentials).
    2. Pastikan ada assessment paket dengan soal+opsi bergambar (upload via admin Phase 353 bila perlu; gunakan akun admin admin@pertamina.com).
    3. **StartExam (RND-01):** mulai ujian. Verifikasi: gambar soal tampil ≤240px tinggi; gambar opsi tampil ≤120px DI BAWAH teks opsi (bukan di samping), lebar penuh. Klik gambar → lightbox modal "Pratinjau Gambar" buka resolusi penuh. Esc/X/backdrop menutup. Soal tanpa gambar = tidak ada slot kosong.
    4. **ExamSummary (RND-02):** klik "Tinjau Jawaban" / submit-review. Verifikasi gambar soal + opsi tampil di ringkasan, lightbox berfungsi.
    5. **Results (RND-03):** selesaikan ujian (atau buka hasil existing dengan AllowAnswerReview). Verifikasi gambar soal + opsi di pembahasan + lightbox.
    6. **Responsif (RND-07):** kecilkan viewport (DevTools mobile) — gambar img-fluid scale turun, opsi tetap block-bawah tidak gepeng.
    7. Jalankan via Playwright MCP bila tersedia (auto-screenshot tiap layar).
  </how-to-verify>
  <verify>Manual/Playwright human-verify per <how-to-verify> — tidak ada automated gate (visual).</verify>
  <done>3 layar peserta render gambar soal+opsi block-bawah + lightbox berfungsi + responsif; user approve.</done>
  <resume-signal>Ketik "approved" bila 3 layar render gambar + lightbox benar, atau jelaskan masalah (mis. opsi masih di samping, lightbox tak buka).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VM ImagePath/ImageAlt → HTML peserta | Path/alt admin-input di-render ke `<img src>`/`alt` di halaman peserta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-07 | Tampering/XSS | render `<img src>` via partial di 3 view peserta | mitigate | Render melalui partial _QuestionImage (Plan 01) yang pakai Razor `@`-encode (no Html.Raw) + render-if-not-null (L-02). View tidak menulis `<img>` mentah sendiri — semua via partial. No XSS surface baru. |
| T-354-08 | XSS via lightbox data-img-src | data attribute di partial | accept | Mitigasi sama T-354-02 (Plan 01): getAttribute→img.src (DOM property). Path image-only tervalidasi upload (Phase 352). |
</threat_model>

<verification>
- `dotnet build` succeeded 0 error.
- 3 view memanggil _QuestionImage (soal+opsi) + _ImageLightboxModal (acceptance criteria).
- Checkpoint human-verify Playwright SC#5 approved (gambar tampil + block-bawah opsi + lightbox + responsif).
</verification>

<success_criteria>
- RND-01/02/03 terpenuhi: peserta lihat gambar soal+opsi di StartExam, ExamSummary, Results.
- RND-07: img-fluid+lazy+alt responsif (inheren via partial).
- Lightbox berfungsi di 3 layar; opsi block-bawah; build hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/354-render-gambar-di-6-layar/354-05-SUMMARY.md`
</output>
