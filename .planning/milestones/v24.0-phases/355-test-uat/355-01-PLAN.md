---
phase: 355-test-uat
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - HcPortal.Tests/PackageImageDeleteTests.cs
autonomous: true
requirements: [TST-01]
tags: [testing, image-upload, xunit, file-delete]

must_haves:
  truths:
    - "Suite xUnit gambar (FileUploadHelper + PackageImageSync + PackageImageDelete) hijau di dotnet test"
    - "Ada satu [Fact] yang menulis file LAMA nyata ke disk, menjalankan delete loop, lalu assert File.Exists(old)==false + File.Exists(new)==true (gap replace-delete-on-disk)"
    - "Tidak ada penulisan ulang / konsolidasi 3 file tes existing (D-02 no churn)"
  artifacts:
    - path: "HcPortal.Tests/PackageImageDeleteTests.cs"
      provides: "[Fact] Replace_NewFileWins_DeletesOldFileOnDisk reusing MakeTempDir/ApplyIntent/DeleteIfUnreferenced"
      contains: "Replace_NewFileWins_DeletesOldFileOnDisk"
  key_links:
    - from: "Replace_NewFileWins_DeletesOldFileOnDisk"
      to: "DeleteIfUnreferenced + ApplyIntent (helper existing di file yang sama)"
      via: "reuse helper, tidak buat helper baru"
      pattern: "DeleteIfUnreferenced\\("
---

<objective>
Tutup satu-satunya gap nyata di suite xUnit gambar v24.0 (TST-01, D-02): membuktikan bahwa saat gambar di-REPLACE, file LAMA benar-benar di-`File.Delete` dari disk (bukan sekadar masuk delete-candidate list). Sekaligus audit + pastikan 3 file tes gambar existing tetap hijau.

Purpose: SC#1 Phase 355 mensyaratkan suite xUnit mencakup "replace menghapus file lama". RESEARCH §xUnit Gap Audit mengonfirmasi `ReplaceConflict_NewFileWins_OverRemoveCheckbox` HANYA membuktikan path lama jadi `deleteList` candidate — TIDAK menulis file lama nyata lalu assert `File.Exists==false` setelah delete loop. Itu gap konkret tunggal.
Output: 1 `[Fact]` baru di `PackageImageDeleteTests.cs` + konfirmasi suite hijau.

Constraint kritis (test-only phase): plan ini HANYA menyentuh `HcPortal.Tests/PackageImageDeleteTests.cs`. ZERO production code/view/controller/migration. TIDAK menulis ulang / konsolidasi 3 file tes existing (D-02). TIDAK menambah integration test controller-level (Deferred).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/355-test-uat/355-CONTEXT.md
@.planning/phases/355-test-uat/355-RESEARCH.md
@.planning/phases/355-test-uat/355-PATTERNS.md

<interfaces>
<!-- Helper signatures SUDAH ADA di HcPortal.Tests/PackageImageDeleteTests.cs — reuse, JANGAN buat baru. -->
<!-- Source: PackageImageDeleteTests.cs:20-39, 132-151 [VERIFIED]. Executor pakai langsung. -->

From HcPortal.Tests/PackageImageDeleteTests.cs:
```csharp
// L20 — temp dir helper
private static string MakeTempDir();

// L30 — predikat ref-count (mirror controller AnyAsync(ImagePath==path))
private static bool PathStillReferenced(IEnumerable<PackageQuestion> questions, IEnumerable<PackageOption> options, string path);

// L34 — atomic delete loop SHARED-2: File.Delete HANYA bila tak direferensi, inner try/catch warn-only
private static void DeleteIfUnreferenced(string path, IEnumerable<PackageQuestion> remainingQ, IEnumerable<PackageOption> remainingO);

// L132 — apply intent gambar opsi (newFilePresent → target.ImagePath=savedNewPath, kumpul old ke deleteList)
private static void ApplyIntent(PackageOption target, bool newFilePresent, string? savedNewPath,
                                string? alt, bool removeChecked, List<string> deleteList);
```

Model types (HcPortal.Models): `PackageOption { OptionText, IsCorrect, ImagePath, ImageAlt }`, `PackageQuestion { ImagePath, ... }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit suite xUnit gambar hijau (baseline gate)</name>
  <files>(none — run-only gate, no file edit)</files>
  <read_first>
    - HcPortal.Tests/PackageImageDeleteTests.cs (file yang akan diedit — pelajari gaya [Fact] existing, helper, pesan assert Bahasa Indonesia)
    - .planning/phases/355-test-uat/355-RESEARCH.md §xUnit Gap Audit (tabel SC#1 → tes existing + gap konkret tunggal)
    - .planning/phases/355-test-uat/355-PATTERNS.md §PackageImageDeleteTests.cs (representative [Fact] body L67-88 + helper signatures)
  </read_first>
  <action>
    Jalankan suite xUnit gambar untuk konfirmasi baseline hijau SEBELUM menambah tes (RESEARCH A3: asumsi suite hijau pre-355, wajib verifikasi):
    ```
    dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~PackageImage"
    dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~FileUploadHelperTests.ValidateImageFile"
    ```
    Konfirmasi bahwa 3 file (FileUploadHelperTests, PackageImageSyncTests, PackageImageDeleteTests) PASS (bukan Skip). Catat jumlah tes pass di SUMMARY.
    Jika ada tes RED/Skip → STOP dan laporkan (jangan tambah tes baru di atas suite merah). Ini test phase: tidak boleh menyentuh production code untuk "memperbaiki" — bila merah karena bug produksi, itu temuan untuk fase lain.
    TIDAK ada edit file pada task ini — murni gate baseline.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~PackageImage"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~PackageImage"` exits 0
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~FileUploadHelperTests.ValidateImageFile"` exits 0
    - Tidak ada file yang dimodifikasi pada task ini (git status PackageImageDeleteTests.cs unchanged)
  </acceptance_criteria>
  <done>3 file tes gambar existing hijau (Passed, bukan Skip); jumlah tes pass tercatat; baseline aman untuk menambah 1 [Fact].</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tambah [Fact] Replace_NewFileWins_DeletesOldFileOnDisk (gap D-02)</name>
  <files>HcPortal.Tests/PackageImageDeleteTests.cs</files>
  <read_first>
    - HcPortal.Tests/PackageImageDeleteTests.cs (sisip [Fact] baru SETELAH ReplaceConflict_NewFileWins_OverRemoveCheckbox ~L187-201; ikuti gaya MakeTempDir → write file → run logic → assert File.Exists → finally Directory.Delete)
    - .planning/phases/355-test-uat/355-RESEARCH.md:294-325 (template lengkap [Fact] verbatim + penjelasan produksi nyata AssessmentAdminController.cs:6386-6525)
    - .planning/phases/355-test-uat/355-PATTERNS.md:143-181 (helper yang DIPAKAI ULANG + representative body)
  </read_first>
  <behavior>
    - Tulis file LAMA nyata (old.jpg) + file BARU nyata (new.jpg) ke MakeTempDir()
    - target PackageOption.ImagePath = oldPath; panggil ApplyIntent(newFilePresent:true, savedNewPath:newPath, removeChecked:false) → target.ImagePath jadi newPath + deleteList memuat oldPath
    - remainingOptions = { target } (kini menunjuk newPath); remainingQuestions = kosong → oldPath TIDAK direferensi
    - loop DeleteIfUnreferenced(p) untuk tiap p di deleteList.Distinct()
    - Assert.False(File.Exists(oldPath)) — file LAMA terhapus on disk (SYN-02 replace)
    - Assert.True(File.Exists(newPath)) — file BARU tetap ada
    - Assert.Equal(newPath, target.ImagePath) — pointer pindah ke file baru
  </behavior>
  <action>
    Tambah TEPAT SATU `[Fact]` baru bernama `Replace_NewFileWins_DeletesOldFileOnDisk` ke `PackageImageDeleteTests.cs`, REUSE helper existing `MakeTempDir`/`ApplyIntent`/`DeleteIfUnreferenced` (JANGAN buat helper baru). Gunakan template verbatim dari RESEARCH.md:296-323:
    ```csharp
    [Fact]
    public void Replace_NewFileWins_DeletesOldFileOnDisk()
    {
        var dir = MakeTempDir();
        try
        {
            var oldPath = Path.Combine(dir, "old.jpg");
            var newPath = Path.Combine(dir, "new.jpg");
            File.WriteAllBytes(oldPath, new byte[] { 1 });
            File.WriteAllBytes(newPath, new byte[] { 2 });

            var target = new PackageOption { OptionText = "A", IsCorrect = true, ImagePath = oldPath };
            var deleteList = new List<string>();
            ApplyIntent(target, newFilePresent: true, savedNewPath: newPath,
                alt: "baru", removeChecked: false, deleteList);

            // Tidak ada baris lain memuat oldPath → harus dihapus on disk.
            var remainingQ = new List<PackageQuestion>();
            var remainingO = new List<PackageOption> { target }; // target kini menunjuk newPath
            foreach (var p in deleteList.Distinct())
                DeleteIfUnreferenced(p, remainingQ, remainingO);

            Assert.False(File.Exists(oldPath), "file LAMA harus terhapus on disk (SYN-02 replace).");
            Assert.True(File.Exists(newPath), "file BARU harus tetap ada.");
            Assert.Equal(newPath, target.ImagePath);
        }
        finally { Directory.Delete(dir, recursive: true); }
    }
    ```
    Pesan assert WAJIB Bahasa Indonesia + kode butir ("SYN-02 replace") sesuai konvensi file. Ini tetap logic-mirror (konsisten 3 file existing), BUKAN integration test (Deferred). Verifikasi signature `ApplyIntent` sesuai L132 (nama parameter `newFilePresent`/`savedNewPath`/`alt`/`removeChecked`/`deleteList`) — bila beda, sesuaikan ke signature aktual yang dibaca di file.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~PackageImageDeleteTests.Replace_NewFileWins_DeletesOldFileOnDisk"</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/PackageImageDeleteTests.cs` contains `Replace_NewFileWins_DeletesOldFileOnDisk`
    - `HcPortal.Tests/PackageImageDeleteTests.cs` contains `Assert.False(File.Exists(oldPath)` (assert file lama hilang on disk)
    - `HcPortal.Tests/PackageImageDeleteTests.cs` contains `Assert.True(File.Exists(newPath)` (assert file baru tetap)
    - `dotnet test ... --filter "FullyQualifiedName~PackageImageDeleteTests.Replace_NewFileWins_DeletesOldFileOnDisk"` exits 0
    - Hanya 1 [Fact] baru ditambah; 3 file tes existing tidak ditulis ulang (git diff hanya menampilkan tambahan di PackageImageDeleteTests.cs)
  </acceptance_criteria>
  <done>1 [Fact] baru hijau membuktikan replace menghapus file lama on disk + mempertahankan file baru; reuse helper existing; no churn ke tes lain.</done>
</task>

<task type="auto">
  <name>Task 3: Konfirmasi suite penuh PackageImage hijau (post-add gate)</name>
  <files>(none — run-only gate, no file edit)</files>
  <read_first>
    - HcPortal.Tests/PackageImageDeleteTests.cs (file yang baru diedit)
    - .planning/phases/355-test-uat/355-VALIDATION.md §Sampling Rate (after every task commit: filter PackageImage)
  </read_first>
  <action>
    Jalankan ulang filter `PackageImage` penuh untuk memastikan [Fact] baru tidak meregresi tes lain di file yang sama (ref-count/replace/option-preserve saling berbagi helper):
    ```
    dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~PackageImage"
    ```
    Catat total tes pass (existing + 1 baru) di SUMMARY. No file edit.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~PackageImage"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~PackageImage"` exits 0 (semua hijau, termasuk [Fact] baru)
    - SUMMARY mencatat total tes pass (baseline + 1)
  </acceptance_criteria>
  <done>Suite PackageImage penuh hijau dengan tambahan replace-delete-on-disk; siap untuk gate phase di Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only phase) | Tidak ada surface produksi baru. Plan 01 hanya menambah kode tes xUnit in-memory/temp-dir; tidak ada input eksternal, endpoint, atau mutasi DB lokal/prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-02 | Tampering | Atomic file delete saat replace (SYN-02) — fitur sudah ada (AssessmentAdminController EditQuestion ref-count + post-commit File.Delete) | mitigate (already-mitigated-by-feature) | Plan ini = EVIDENCE: `[Fact] Replace_NewFileWins_DeletesOldFileOnDisk` membuktikan file lama benar terhapus + file baru dipertahankan (no orphan, no over-delete). Tidak menambah surface; hanya menguji. |

Catatan: Plan 01 adalah kode tes murni in-memory + temp-dir lokal. Tidak ada NEW high-severity threat yang diperkenalkan. Tidak ada auth/crypto/session/endpoint baru.
</threat_model>

<verification>
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~PackageImage"` hijau (existing + 1 baru).
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~FileUploadHelperTests.ValidateImageFile"` hijau.
- git diff menunjukkan HANYA tambahan 1 [Fact] di PackageImageDeleteTests.cs (no churn, no production code).
</verification>

<success_criteria>
- 3 file tes gambar existing tetap hijau (audit baseline + post-add).
- 1 [Fact] `Replace_NewFileWins_DeletesOldFileOnDisk` hijau: file lama `File.Exists==false`, file baru `File.Exists==true`, pointer pindah ke file baru.
- Zero perubahan production code/view/controller/migration; zero penulisan ulang tes existing.
</success_criteria>

<output>
After completion, create `.planning/phases/355-test-uat/355-01-SUMMARY.md`
</output>
