---
phase: 355-test-uat
plan: 03
type: execute
wave: 2
depends_on: [355-01, 355-02]
files_modified:
  - .planning/phases/355-test-uat/355-VALIDATION.md
autonomous: false
requirements: [TST-01, TST-02]
tags: [testing, gate, uat, regression, seed-workflow]

must_haves:
  truths:
    - "dotnet build HcPortal.sln 0 error"
    - "dotnet test (full suite) hijau termasuk [Fact] replace-delete-on-disk baru"
    - "image-in-assessment.spec.ts run penuh end-to-end di localhost:5277 exits 0 (StartExam+Results render + guard toggle + guard null)"
    - "1 spec exam existing (exam-taking.spec.ts) rerun sebagai baseline regresi tetap hijau (MC/MA/Essay tanpa gambar normal)"
    - "Setelah run: DB ter-RESTORE bersih (Layer-4) + folder wwwroot/uploads/questions/{pkgId} terhapus (rmSync) + SEED_JOURNAL status cleaned"
    - "Human menyetujui UAT live di localhost:5277 (CLAUDE.md Develop Workflow)"
  artifacts:
    - path: ".planning/phases/355-test-uat/355-VALIDATION.md"
      provides: "frontmatter nyquist_compliant + wave_0_complete di-update ke true setelah gate hijau"
      contains: "nyquist_compliant"
  key_links:
    - from: "phase gate"
      to: "dotnet build + dotnet test + npx playwright test image-in-assessment + exam-taking baseline"
      via: "L-03 SC#3 gate (CLAUDE.md Develop Workflow)"
      pattern: "playwright test image-in-assessment"
    - from: "post-run cleanup verify"
      to: "wwwroot/uploads/questions + DB Layer-4 + SEED_JOURNAL"
      via: "Seed Workflow: tidak ada seed/file nyangkut"
      pattern: "uploads/questions"

user_setup: []
---

<objective>
Gate akhir Phase 355 (SC#3 / L-03): pastikan seluruh stack hijau end-to-end di lokal sesuai CLAUDE.md Develop Workflow, tanpa regresi, dan tanpa seed/file nyangkut. Jalankan `dotnet build` (0 error) + `dotnet test` penuh (termasuk [Fact] replace-delete-on-disk dari Plan 01) + `image-in-assessment.spec.ts` penuh end-to-end di localhost:5277 (Plan 02) + rerun 1 spec exam existing sebagai baseline regresi. Verifikasi cleanup (DB RESTORE bersih + folder upload terhapus + SEED_JOURNAL cleaned). Tutup dengan checkpoint human-verify UAT live.

Purpose: SC#3 mensyaratkan build 0 error + suite hijau + UAT di localhost:5277 + tanpa regresi flow ujian existing. Plan 01 & 02 menghasilkan artefak; Plan 03 membuktikan semuanya hijau bersama + Seed Workflow tertutup.
Output: bukti gate hijau + VALIDATION.md di-update (nyquist_compliant/wave_0_complete) + approval human UAT.

Constraint kritis (test-only phase): plan ini TIDAK menulis production code/test code baru. Hanya MENJALANKAN suite + verifikasi cleanup + update 1 dokumen validasi + checkpoint. Bila ada test RED karena bug produksi -> itu temuan, laporkan (jangan patch production di phase test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/355-test-uat/355-CONTEXT.md
@.planning/phases/355-test-uat/355-RESEARCH.md
@.planning/phases/355-test-uat/355-VALIDATION.md
@CLAUDE.md
@docs/SEED_JOURNAL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate build + full xUnit (SC#3 bagian 1)</name>
  <files>(none — run-only gate, no file edit)</files>
  <read_first>
    - .planning/phases/355-test-uat/355-RESEARCH.md (sec Code Examples build+test + sec Validation Architecture)
    - .planning/phases/355-test-uat/355-VALIDATION.md (Sampling Rate: phase gate)
    - HcPortal.Tests/PackageImageDeleteTests.cs (konfirmasi [Fact] Replace_NewFileWins_DeletesOldFileOnDisk dari Plan 01 ada)
    - CLAUDE.md (Develop Workflow — verifikasi lokal)
  </read_first>
  <action>
    Jalankan dari worktree root (PowerShell):
    ```
    dotnet build HcPortal.sln
    dotnet test HcPortal.Tests/HcPortal.Tests.csproj
    ```
    - `dotnet build HcPortal.sln` WAJIB 0 error.
    - `dotnet test` (full suite) WAJIB hijau, termasuk `Replace_NewFileWins_DeletesOldFileOnDisk` (Plan 01) + 3 file tes gambar existing.
    - Catat jumlah total tes pass + konfirmasi 0 failed/0 skipped-yang-relevan di SUMMARY.
    Jika ada RED: laporkan tes mana + apakah berasal dari kode produksi (di luar scope patch phase test) atau dari kode tes baru (perbaiki kode tes). JANGAN ubah production code.
  </action>
  <verify>
    <automated>dotnet build HcPortal.sln && dotnet test HcPortal.Tests/HcPortal.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.sln` exits 0 (0 error)
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exits 0 (full suite hijau)
    - Output test memuat `Replace_NewFileWins_DeletesOldFileOnDisk` Passed
    - SUMMARY mencatat total tes pass + 0 failed
  </acceptance_criteria>
  <done>Build 0 error + full xUnit suite hijau termasuk [Fact] replace-delete-on-disk; bukti SC#3 bagian 1.</done>
</task>

<task type="auto">
  <name>Task 2: Run image spec end-to-end live + baseline regresi + verifikasi cleanup (SC#3 bagian 2 + D-06)</name>
  <files>(none — run-only gate, no file edit)</files>
  <read_first>
    - tests/e2e/image-in-assessment.spec.ts (spec dari Plan 02 — pahami test 1-4 + afterAll cleanup)
    - .planning/phases/355-test-uat/355-RESEARCH.md (sec Code Examples Playwright UAT + Open Question 1 global.setup matrix interaction + Pitfall 2 cleanup file)
    - .planning/phases/355-test-uat/355-PATTERNS.md (Anti-Patterns — jangan andalkan RESTORE untuk file)
    - docs/SEED_JOURNAL.md (entry 355 dari Plan 02 — verifikasi status cleaned setelah run)
    - CLAUDE.md (Seed Workflow — tidak boleh seed/file nyangkut)
  </read_first>
  <action>
    PRECONDITION: app HARUS jalan di localhost:5277. Jalankan `dotnet run` di terminal terpisah (atau run_in_background), tunggu app siap (cek `/Account/Login` reachable). DB lokal HcPortalDB_Dev (SQLEXPRESS).

    Step A — run spec image penuh (TST-02 + D-06):
    ```
    cd tests
    npx playwright test image-in-assessment.spec.ts
    ```
    - WAJIB exits 0: test StartExam render gambar (img-fluid+lazy+src /uploads/questions) + guard toggle (lightbox open, radio TIDAK ke-toggle) + Results render + guard null (soal tanpa gambar tak render img).
    - Bila gagal "browser not found": `cd tests; npx playwright install chromium` lalu ulangi.
    - Catatan global.setup (Open Q1): project chromium punya dependency `setup` (matrix Phase 315). Run akan memicu global.setup/teardown matrix DI LUAR spec; spec image punya snapshot beforeAll/afterAll-nya sendiri (nested) — net bersih. Terima overhead (opsi a). JANGAN edit global.setup/teardown.

    Step B — baseline regresi (D-06 / L-03 tanpa regresi flow ujian existing):
    ```
    npx playwright test exam-taking.spec.ts
    ```
    - WAJIB exits 0 (MC/MA/Essay tanpa gambar tetap normal). Bila spec exam lain lebih representatif (mis. exam-types.spec.ts), boleh tambahkan; minimal 1 baseline hijau.

    Step C — verifikasi cleanup (Seed Workflow — tidak ada seed/file nyangkut):
    - DB: spec afterAll sudah RESTORE; konfirmasi tidak ada assessment sisa dari run (Layer-4). Bila spec sudah meng-assert Layer-4, cukup pastikan afterAll lewat tanpa error.
    - File upload: konfirmasi folder `wwwroot/uploads/questions/{pkgId}` yang dibuat run sudah terhapus (rmSync di afterAll). Cek `wwwroot/uploads/questions/` tidak menyisakan folder package run ini.
    - Journal: konfirmasi `docs/SEED_JOURNAL.md` entry Phase 355 status `cleaned`.
  </action>
  <verify>
    <automated>cd tests; npx playwright test image-in-assessment.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd tests; npx playwright test image-in-assessment.spec.ts` exits 0 (StartExam + Results + guard toggle + guard null semua hijau)
    - `cd tests; npx playwright test exam-taking.spec.ts` exits 0 (baseline regresi hijau)
    - Setelah run: tidak ada folder package run-ini tersisa di `wwwroot/uploads/questions/` (cleanup file ter-verifikasi)
    - `docs/SEED_JOURNAL.md` entry Phase 355 status `cleaned`
    - DB lokal kembali ke state pre-run (afterAll RESTORE sukses, no error)
  </acceptance_criteria>
  <done>Spec image hijau end-to-end live + baseline regresi hijau + cleanup terverifikasi (DB restore, folder upload terhapus, journal cleaned); bukti SC#2/SC#3/D-06.</done>
</task>

<task type="auto">
  <name>Task 3: Update VALIDATION.md frontmatter (nyquist_compliant + wave_0_complete)</name>
  <files>.planning/phases/355-test-uat/355-VALIDATION.md</files>
  <read_first>
    - .planning/phases/355-test-uat/355-VALIDATION.md (frontmatter + Validation Sign-Off checklist)
  </read_first>
  <action>
    Setelah Task 1 & 2 hijau, update frontmatter `.planning/phases/355-test-uat/355-VALIDATION.md`:
    - `nyquist_compliant: false` -> `true` (semua task punya automated verify; Wave 0 gaps tertutup).
    - `wave_0_complete: false` -> `true` (fixtures + helper extend + [Fact] replace-delete + spec semua ada & hijau).
    - `status: draft` -> `complete`.
    - Centang item di sec "Validation Sign-Off" yang sudah terpenuhi (all tasks automated verify, no 3-consecutive-gap, Wave 0 covered, no watch-mode, latency, nyquist_compliant true).
    JANGAN ubah konten Per-Task Verification Map kecuali menandai Status -> green untuk baris yang sudah lulus.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/355-test-uat/355-VALIDATION.md','utf8');if(!/nyquist_compliant:\s*true/.test(s))throw new Error('nyquist_compliant belum true');if(!/wave_0_complete:\s*true/.test(s))throw new Error('wave_0_complete belum true');console.log('VALIDATION updated OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/355-test-uat/355-VALIDATION.md frontmatter `nyquist_compliant: true`
    - frontmatter `wave_0_complete: true`
    - frontmatter `status: complete`
  </acceptance_criteria>
  <done>VALIDATION.md mencerminkan Wave 0 selesai + nyquist compliant setelah gate hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint UAT live localhost:5277 (human-verify, SC#3)</name>
  <files>(none — human verification, no file edit)</files>
  <action>Setelah semua gate otomatis hijau (Task 1-3), minta human menjalankan UAT live di localhost:5277 sesuai blok how-to-verify, lalu PAUSE menunggu resume-signal sebelum menutup fase. Jangan tutup fase tanpa approval.</action>
  <what-built>
    Spec Playwright committed `tests/e2e/image-in-assessment.spec.ts` (TST-02) + 1 [Fact] xUnit replace-delete-on-disk (TST-01). Semua otomatis sudah hijau: dotnet build 0 error, full dotnet test hijau, image spec end-to-end hijau di localhost:5277 (StartExam+Results render gambar soal+opsi responsif, klik gambar opsi -> lightbox tanpa toggle radio, soal tanpa gambar tak render img), baseline regresi exam-taking hijau, DB ter-RESTORE + folder upload terhapus + SEED_JOURNAL cleaned.
  </what-built>
  <how-to-verify>
    UAT live di localhost:5277 (CLAUDE.md Develop Workflow — human sign-off sebelum tutup fase):
    1. Pastikan app jalan: `dotnet run` -> buka http://localhost:5277 .
    2. (Opsional, paling cepat) Jalankan headed untuk lihat render: `cd tests; npx playwright test image-in-assessment.spec.ts --headed` — amati gambar soal (240px) + gambar opsi (120px block-bawah) muncul di StartExam; klik gambar opsi -> lightbox terbuka, radio TIDAK ter-pilih; di Results gambar soal+opsi muncul di "Tinjauan Jawaban".
    3. Konfirmasi tidak ada seed/file nyangkut: `wwwroot/uploads/questions/` tidak menyisakan folder dari run; `docs/SEED_JOURNAL.md` entry 355 = `cleaned`; DB lokal bersih.
    4. (Opsional nice-to-have, di luar deliverable inti) eyeball surface admin essay (AssessmentMonitoringDetail) / EditPesertaAnswers bila ingin — bukan syarat lulus SC#2.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila render gambar + lightbox + cleanup benar, atau jelaskan masalah yang ditemukan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only gate phase) | Plan 03 hanya MENJALANKAN suite + verifikasi cleanup + update 1 dokumen validasi + checkpoint. Tidak ada surface produksi baru. Boundary yang relevan (upload magic-byte, encoded src, DB snapshot localhost-guard) diuji oleh Plan 01/02; Plan 03 mengonfirmasi semua hijau bersama. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-01 | Tampering | Upload file-type magic-byte (diuji Plan 01 xUnit + Plan 02 fixture valid) | mitigate (already-mitigated-by-feature) | Gate full `dotnet test` + image spec hijau = evidence kolektif. Tidak menambah surface. |
| T-355-02 | Tampering | Atomic file delete (diuji Plan 01) | mitigate (already-mitigated-by-feature) | Full suite hijau termasuk replace-delete-on-disk. |
| T-355-05 | Tampering / Info Disclosure | Seed/file nyangkut setelah UAT (DB rows + wwwroot/uploads/questions) | mitigate | Task 2 Step C verifikasi cleanup: DB RESTORE (Layer-4) + rmSync folder upload + SEED_JOURNAL cleaned. CLAUDE.md Seed Workflow ditegakkan. |

Catatan (test-only phase): Plan 03 tidak memperkenalkan NEW high-severity threat. Semua dipatuhi via menjalankan + verifikasi cleanup. Test creds dev lokal `123456` JANGAN dipakai staging/prod.
</threat_model>

<verification>
- `dotnet build HcPortal.sln` 0 error.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full hijau (incl Replace_NewFileWins_DeletesOldFileOnDisk).
- `cd tests; npx playwright test image-in-assessment.spec.ts` exits 0 (live localhost:5277).
- `cd tests; npx playwright test exam-taking.spec.ts` exits 0 (baseline regresi).
- Cleanup: folder `wwwroot/uploads/questions/{pkgId}` run terhapus + DB Layer-4 bersih + SEED_JOURNAL 355 `cleaned`.
- VALIDATION.md nyquist_compliant=true + wave_0_complete=true + status=complete.
- Human approval UAT live.
</verification>

<success_criteria>
- SC#1 (TST-01): full xUnit hijau termasuk replace-menghapus-file-lama.
- SC#2 (TST-02): image spec hijau end-to-end live (admin upload -> StartExam render -> Results render).
- SC#3 (L-03): build 0 error + suite hijau + UAT di localhost:5277 + baseline regresi hijau (tanpa regresi flow ujian existing) + tidak ada seed/file nyangkut (Seed Workflow cleaned).
- Human menyetujui UAT live.
</success_criteria>

<output>
After completion, create `.planning/phases/355-test-uat/355-03-SUMMARY.md`
</output>
