---
phase: 356-audit-fix-assign-coach-coachee
plan: 02
type: execute
wave: 2
depends_on: [356-01]
files_modified:
  - Controllers/CoachMappingController.cs
autonomous: true
requirements: [AF-3, AF-6, AF-4]
tags: [coaching, graduation, transaction, audit-fix]

must_haves:
  truths:
    - "MarkMappingCompleted set IsCompleted=true DAN IsActive=false + CompletedAt + EndDate (membebaskan unique-index)"
    - "Graduate cascade deactivate ProtonTrackAssignment aktif coachee (stamp DeactivatedAt), histori progress unit lama UTUH (tidak dihapus)"
    - "MarkMappingCompleted dibungkus transaksi (BeginTransactionAsync), rollback saat gagal"
    - "Coachee graduated bisa di-assign lagi untuk unit lain (unique-index bebas)"
    - "Assign race duplicate (pelanggaran unique-index) menghasilkan pesan ramah spesifik tanpa bocorkan ex.Message"
  artifacts:
    - path: "Controllers/CoachMappingController.cs"
      provides: "MarkMappingCompleted transaksi+cascade+IsActive=false; Assign catch DbUpdateException spesifik; Reactivate komentar AF-4"
      contains: "mapping.IsActive = false"
  key_links:
    - from: "MarkMappingCompleted"
      to: "ProtonTrackAssignments aktif coachee"
      via: "cascade IsActive=false + DeactivatedAt (mirror Deactivate L930-940), bukan RemoveRange"
      pattern: "a\\.DeactivatedAt = deactivationTime"
    - from: "CoachCoacheeMappingAssign catch DbUpdateException"
      to: "IX_CoachCoacheeMappings_CoacheeId_ActiveUnique"
      via: "match index name + 2601/2627 (pola CertNumberHelper) SEBELUM catch generic"
      pattern: "IX_CoachCoacheeMappings_CoacheeId_ActiveUnique"
---

<objective>
AF-3 (MED): `MarkMappingCompleted` saat graduate set `IsCompleted=true` DAN `IsActive=false` (+ `CompletedAt` + `EndDate`), bungkus transaksi, cascade deactivate `ProtonTrackAssignment` aktif coachee (stamp `DeactivatedAt`, JANGAN hapus histori progress) — membebaskan unique-index `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique` agar coachee graduated bisa di-assign lagi untuk unit lain.

AF-6 (LOW): `CoachCoacheeMappingAssign` tangkap `DbUpdateException` yang melanggar unique-index SEBELUM catch generic → pesan ramah spesifik (tanpa bocorkan internal). Ini backstop dari AF-3 yang sengaja melepas index.

AF-4 (DEFER, comment-only): tambah komentar Bahasa Indonesia mendokumentasikan asumsi window ±5s di `CoachCoacheeMappingReactivate`. TIDAK ada fix, TIDAK ada migration.

Purpose: Semantik graduated per-unit konsisten dengan AF-1 (assignment, progress, eligibility, graduation semua scoped per unit). Coachee yang sudah graduate satu unit bisa lanjut unit lain.

Output: Modifikasi `Controllers/CoachMappingController.cs` (3 action). Migration = false (semua kolom sudah ada di Models/CoachCoacheeMapping.cs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-CONTEXT.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-RESEARCH.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-PATTERNS.md

<interfaces>
<!-- Analog VERBATIM yang di-mirror — executor TIDAK perlu eksplorasi codebase. -->

ANALOG transaksi+cascade — Controllers/CoachMappingController.cs CoachCoacheeMappingDeactivate L924-944 (MIRROR pola, kontrak return BERBEDA — lihat Pitfall 4):
```csharp
using var transaction = await _context.Database.BeginTransactionAsync();
try
{
    mapping.IsActive = false;
    mapping.EndDate = DateTime.UtcNow;
    var deactivationTime = mapping.EndDate.Value;
    var activeAssignments = await _context.ProtonTrackAssignments
        .Where(a => a.CoacheeId == mapping.CoacheeId && a.IsActive)
        .ToListAsync();
    foreach (var a in activeAssignments)
    {
        a.IsActive = false;
        a.DeactivatedAt = deactivationTime;   // JANGAN RemoveRange progress
    }
    int cascadeCount = activeAssignments.Count;
    await _context.SaveChangesAsync();
    await transaction.CommitAsync();
    // audit log SETELAH commit
}
catch (Exception ex)
{
    await transaction.RollbackAsync();
    _logger.LogError(ex, "...transaction failed for mapping {Id}", id);
    // return ...
}
```

KONTRAK RETURN MarkMappingCompleted (BERBEDA dari Deactivate — Pitfall 4): return `RedirectToAction("CoachCoacheeMapping")` + `TempData["Success"/"Error"]` (form POST), BUKAN Json.

ANALOG duplicate-key — Helpers/CertNumberHelper.cs L37-42:
```csharp
public static bool IsDuplicateKeyException(DbUpdateException ex)
{
    return ex.InnerException?.Message.Contains("IX_AssessmentSessions_NomorSertifikat") == true
        || ex.InnerException?.Message.Contains("2601") == true
        || ex.InnerException?.Message.Contains("2627") == true;
}
```

Kolom verified ADA (Models/CoachCoacheeMapping.cs): IsActive (L24), EndDate (L34), IsCompleted (L47), CompletedAt (L50). ProtonTrackAssignment.DeactivatedAt dipakai di Deactivate L939. → migration = FALSE.

Unique-index AF-6 = `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique` (Data/ApplicationDbContext.cs L328, HasFilter `[IsActive]=1`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MarkMappingCompleted — transaksi + IsActive=false + EndDate + cascade deactivate (AF-3)</name>
  <read_first>
    - Controllers/CoachMappingController.cs L1075-1109 (MarkMappingCompleted — yang dibungkus transaksi & ditambah field)
    - Controllers/CoachMappingController.cs L924-944 (CoachCoacheeMappingDeactivate — pola transaksi+cascade VERBATIM)
    - Controllers/CoachMappingController.cs L924-975 (lihat juga return Json di Deactivate untuk KONTRAS — MarkCompleted TETAP Redirect+TempData)
    - .planning/phases/356-.../356-PATTERNS.md §"AF-3 MarkMappingCompleted" + RESEARCH Pitfall 4
  </read_first>
  <action>
Di `Controllers/CoachMappingController.cs` action `MarkMappingCompleted` (L1075-1109):

PERTAHANKAN VERBATIM SEBELUM transaksi:
- L1077-1080 guard user/mapping null
- L1081-1098 validasi Tahun-3 (`tahun3Assignment` + `tahun3Complete` + early-return TempData["Error"]) — TETAP DI AWAL, di luar transaksi.

GANTI blok mutasi L1099-1108 (saat ini single `SaveChangesAsync`):
```csharp
mapping.IsCompleted = true;
mapping.CompletedAt = DateTime.UtcNow;
await _context.SaveChangesAsync();
// ... audit log + TempData["Success"] + RedirectToAction ...
```
menjadi transaksi + IsActive=false + EndDate + cascade (mirror Deactivate L924-944), TETAP return Redirect+TempData:

```csharp
using var transaction = await _context.Database.BeginTransactionAsync();
try
{
    mapping.IsCompleted = true;
    mapping.CompletedAt = DateTime.UtcNow;
    mapping.IsActive = false;            // AF-3 D-03: bebaskan unique-index IX_CoachCoacheeMappings_CoacheeId_ActiveUnique
    mapping.EndDate = DateTime.UtcNow;   // AF-3 D-03: tandai akhir mapping graduated

    // AF-3 D-04: cascade deactivate ProtonTrackAssignment aktif coachee (mirror Deactivate L930-940).
    // Stamp DeactivatedAt (pola FIX-01); JANGAN hapus histori progress (BUKAN RemoveRange).
    var deactivationTime = mapping.EndDate.Value;
    var activeAssignments = await _context.ProtonTrackAssignments
        .Where(a => a.CoacheeId == mapping.CoacheeId && a.IsActive)
        .ToListAsync();
    foreach (var a in activeAssignments)
    {
        a.IsActive = false;
        a.DeactivatedAt = deactivationTime;
    }
    int cascadeCount = activeAssignments.Count;

    await _context.SaveChangesAsync();
    await transaction.CommitAsync();

    var actorName = string.IsNullOrWhiteSpace(user.NIP)
        ? (user.FullName ?? "Unknown")
        : $"{user.NIP} - {user.FullName}";
    await _auditLog.LogAsync(user.Id, actorName, "MarkMappingCompleted",
        $"Mapping ID={mappingId} ditandai graduated (IsActive=false). CoacheeId={mapping.CoacheeId} — {cascadeCount} ProtonTrackAssignment juga dinonaktifkan", mappingId, "CoachCoacheeMapping");
    TempData["Success"] = "Coachee berhasil ditandai sebagai graduated.";
    return RedirectToAction("CoachCoacheeMapping");
}
catch (Exception ex)
{
    await transaction.RollbackAsync();
    _logger.LogError(ex, "MarkMappingCompleted transaction failed for mapping {Id}", mappingId);
    TempData["Error"] = "Operasi gagal. Semua perubahan dibatalkan.";
    return RedirectToAction("CoachCoacheeMapping");
}
```

KRITIS Pitfall 4: KONTRAK RETURN tetap `RedirectToAction("CoachCoacheeMapping")` + `TempData` (BUKAN Json seperti Deactivate). Audit log POST-commit (sudah dalam try, setelah CommitAsync). JANGAN `RemoveRange` progress (D-04 — histori unit lama lestari). JANGAN longgarkan `[Authorize(Roles="Admin, HC")]`.

PLANNER FLAG (RESEARCH OQ1/A2 — keputusan kecil, non-blocker): jalankan saat eksekusi (DB lokal `HcPortalDB_Dev` via SSMS/sqlcmd — Bash sandbox tidak bisa Named Pipes):
`SELECT Id, CoacheeId, IsActive, IsCompleted FROM CoachCoacheeMappings WHERE IsCompleted = 1;`
Bila ADA record `IsActive=1 & IsCompleted=1` (graduated lama dari logic lama): tambah one-off SQL UPDATE local-only (`UPDATE CoachCoacheeMappings SET IsActive=0, EndDate=GETUTCDATE() WHERE IsCompleted=1 AND IsActive=1;`) DAN catat di SEED_JOURNAL, ATAU dokumentasikan di SUMMARY "AF-3 berlaku untuk graduate baru saja" bila tak ada record. INI BUKAN migration (D-12 tetap false) — hanya nilai data lokal. Bukan blocker.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0, 0 error
    - `MarkMappingCompleted` body berisi `mapping.IsActive = false`
    - `MarkMappingCompleted` body berisi `mapping.EndDate = DateTime.UtcNow`
    - `MarkMappingCompleted` body berisi `BeginTransactionAsync` dan `transaction.CommitAsync()` dan `transaction.RollbackAsync()`
    - `MarkMappingCompleted` body berisi `a.DeactivatedAt = deactivationTime`
    - `MarkMappingCompleted` TIDAK berisi `RemoveRange` (histori utuh)
    - `MarkMappingCompleted` masih `return RedirectToAction("CoachCoacheeMapping")` (bukan Json)
  </acceptance_criteria>
  <done>Graduate set IsActive=false+IsCompleted=true+EndDate dalam transaksi, cascade deactivate assignment, histori utuh, kontrak Redirect+TempData dipertahankan.</done>
</task>

<task type="auto">
  <name>Task 2: AF-6 catch DbUpdateException spesifik + AF-4 komentar defer (Reactivate)</name>
  <read_first>
    - Controllers/CoachMappingController.cs L620-628 (CoachCoacheeMappingAssign catch generic — tambah catch spesifik SEBELUMNYA)
    - Helpers/CertNumberHelper.cs L37-42 (pola IsDuplicateKeyException — index name + 2601/2627)
    - Data/ApplicationDbContext.cs L321-330 (unique-index IX_CoachCoacheeMappings_CoacheeId_ActiveUnique HasFilter [IsActive]=1)
    - Controllers/CoachMappingController.cs L1008-1020 (CoachCoacheeMappingReactivate — window ±5s, AF-4 comment-only)
    - .planning/phases/356-.../356-UI-SPEC.md §AF-6 (pesan dikunci) + 356-PATTERNS.md §AF-6 / §AF-4
  </read_first>
  <action>
**AF-6** — Di `Controllers/CoachMappingController.cs` action `CoachCoacheeMappingAssign`, TAMBAH `catch (DbUpdateException ...)` SPESIFIK tepat SEBELUM catch generic L623-628 (urutan WAJIB: catch spesifik dulu, generic terakhir):

```csharp
catch (DbUpdateException dbEx) when (
    dbEx.InnerException?.Message.Contains("IX_CoachCoacheeMappings_CoacheeId_ActiveUnique") == true
    || dbEx.InnerException?.Message.Contains("2601") == true
    || dbEx.InnerException?.Message.Contains("2627") == true)
{
    _logger.LogWarning(dbEx, "Assign race: coachee already has active coach (unique-index violation)");
    await tx.RollbackAsync();
    return Json(new { success = false,
        message = "Coachee sudah memiliki coach aktif untuk unit ini. Nonaktifkan mapping lama terlebih dahulu." });
}
catch (Exception ex)   // generic existing L623-628 TETAP, di BAWAH catch spesifik
{
    _logger.LogError(ex, "CoachCoacheeMappingAssign failed (CoachId={CoachId}, Coachees={Count})",
        req.CoachId, req.CoacheeIds?.Count ?? 0);
    await tx.RollbackAsync();
    return Json(new { success = false, message = "Gagal menyimpan assignment. Operasi dibatalkan." });
}
```

Pesan DIKUNCI UI-SPEC §129: `Coachee sudah memiliki coach aktif untuk unit ini. Nonaktifkan mapping lama terlebih dahulu.` KEAMANAN (RESEARCH §507): JANGAN expose `dbEx.Message`/`ex.Message` mentah ke user — pesan ramah saja, detail hanya di `_logger.LogWarning`. Pastikan `using Microsoft.EntityFrameworkCore;` ada (untuk `DbUpdateException`) — cek di top file.

(Discretion: boleh extend CertNumberHelper jadi generic param index — TAPI inline `when`-filter di atas adalah default yang dipilih PATTERNS.md, lebih jelas di call-site.)

**AF-4 (DEFER, comment-only)** — Di `CoachCoacheeMappingReactivate` (sekitar L1008-1020, di atas blok `inactiveAssignments` window ±5s), TAMBAH komentar Bahasa Indonesia mendokumentasikan asumsi window (TIDAK ubah logic, TIDAK tambah kolom/migration):

```csharp
// AF-4 (Phase 356, DEFER ke backlog): korelasi assignment via DeactivatedAt dalam window ±5 detik
// adalah asumsi rapuh — assignment yang dinonaktifkan secara independen dalam window yang sama bisa
// ikut ter-restore. FIX-01 sudah memitigasi salah-restore untuk skenario umum. Fix proper memerlukan
// kolom korelasi eksplisit (mis. DeactivatedByMappingEventId) yang butuh migration; di-defer agar
// Phase 356 tetap 0-migration (leg v24 belum di-push). Promote via /gsd-add-backlog bila volume
// reactivate / track multi-unit meningkat. JANGAN ubah logic window di phase ini.
```

JANGAN ubah query/logic Reactivate apa pun selain menambah komentar ini. JANGAN longgarkan `[Authorize]`.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0, 0 error
    - `CoachCoacheeMappingAssign` berisi `catch (DbUpdateException dbEx) when` dan `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique`
    - Pesan AF-6 berisi `Coachee sudah memiliki coach aktif untuk unit ini`
    - catch spesifik berada SEBELUM `catch (Exception ex)` generic
    - TIDAK ada `dbEx.Message`/`ex.Message` yang dikembalikan ke `Json(...)` user (no info-leak)
    - `CoachCoacheeMappingReactivate` berisi komentar berisi `AF-4` + `DEFER`
  </acceptance_criteria>
  <done>Race duplicate → pesan ramah spesifik tanpa info-leak; AF-4 didokumentasikan sebagai defer; logic Reactivate tak berubah.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → MarkMappingCompleted | form POST `mappingId`; `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` |
| Admin/HC browser → CoachCoacheeMappingAssign | JSON payload `CoachAssignRequest`; DB unique-index = backstop integritas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-04 | Tampering/Repudiation | MarkMappingCompleted state mutation lintas-entitas | mitigate | `BeginTransactionAsync` membungkus mapping + cascade ProtonTrackAssignment → atomik; rollback saat gagal; audit log POST-commit |
| T-356-05 | Elevation | AF-3 melepas unique-index (IsActive=false) bisa membuka re-assign tak sah | mitigate | unique-index invariant 1-coach-aktif/coachee TETAP utuh (hanya record graduated yang IsActive=false keluar filter); re-assign baru tetap lewat validasi unit + role-gate |
| T-356-06 | Information Disclosure | AF-6 pesan error duplikat | mitigate | pesan ramah dikunci UI-SPEC (no SQL/stack); `ex.Message` HANYA ke `_logger`, tidak ke user (pola D6 Phase 334) |
| T-356-07 | Tampering | Assign race TOCTOU duplicate mapping | accept | DB unique-index `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique` = backstop atomik; AF-6 hanya memperbaiki PESAN, bukan invariant |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error
- `dotnet test` full suite hijau (jalankan saat wave-merge; tidak ada test baru di plan ini — AF-3 cascade opsional di-cover Plan 05 UAT)
- AF-3 graduate → coachee kembali ke pool EligibleCoachees: diverifikasi Plan 05 (UAT)
- AF-6 race duplicate: diverifikasi Plan 05 (smoke / manual) — sulit otomatis tanpa race
</verification>

<success_criteria>
1. `MarkMappingCompleted` set IsActive=false+IsCompleted=true+CompletedAt+EndDate dalam transaksi + cascade deactivate (histori utuh).
2. Kontrak return MarkMappingCompleted tetap Redirect+TempData.
3. `CoachCoacheeMappingAssign` punya catch DbUpdateException spesifik (pesan ramah, no info-leak) sebelum catch generic.
4. AF-4 didokumentasikan sebagai defer (comment-only), logic Reactivate tak berubah.
5. `dotnet build` 0 error.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-02-SUMMARY.md`
</output>
