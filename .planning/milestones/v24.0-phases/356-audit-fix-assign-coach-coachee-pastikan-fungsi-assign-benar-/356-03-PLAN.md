---
phase: 356-audit-fix-assign-coach-coachee
plan: 03
type: execute
wave: 3
depends_on: [356-02]
files_modified:
  - Controllers/CoachMappingController.cs
autonomous: true
requirements: [AF-5, AF-7]
tags: [coaching, notification, performance, audit-fix]

must_haves:
  truths:
    - "ApproveReassignSuggestion mengirim 3 notifikasi: coach lama (dilepas), coach baru (ditunjuk), coachee (dipindah)"
    - "Notif reassign warn-only (gagal kirim tidak menggagalkan reassign)"
    - "Progression-warning loop tidak lagi N+1 (pre-load batch dict), dengan output incompleteCoachees IDENTIK sebelum/sesudah (zero behavior change)"
  artifacts:
    - path: "Controllers/CoachMappingController.cs"
      provides: "ApproveReassignSuggestion 3 SendAsync COACH_REASSIGNED; Assign progression-warning batch query"
      contains: "COACH_REASSIGNED"
  key_links:
    - from: "ApproveReassignSuggestion"
      to: "_notificationService.SendAsync (3 recipient)"
      via: "warn-only try/catch (mirror Deactivate L949-966), EVENT_TYPE COACH_REASSIGNED"
      pattern: "SendAsync\\([^)]*COACH_REASSIGNED"
    - from: "CoachCoacheeMappingAssign progression-warning"
      to: "incompleteCoachees (3 cabang identik)"
      via: "pre-load dict batch lalu evaluasi cabang in-memory (zero behavior change)"
      pattern: "incompleteCoachees"
---

<objective>
AF-5 (LOW): `ApproveReassignSuggestion` saat ini tidak mengirim notifikasi (inkonsisten dengan Assign/Edit/Deactivate). Tambah 3 `_notificationService.SendAsync` warn-only: coach lama (dilepas), coach baru (ditunjuk), coachee (dipindah) — EVENT_TYPE `COACH_REASSIGNED`, microcopy Bahasa Indonesia dikunci UI-SPEC.

AF-7 (INFO, user opt-in): Loop progression-warning di `CoachCoacheeMappingAssign` (L508-535) melakukan ~4 query per coachee (N+1). Refactor jadi batch query (pre-load dict sebelum loop) — TAPI dengan ZERO behavior change pada output `incompleteCoachees`.

Purpose: Konsistensi notifikasi reassign + reduksi query count tanpa mengubah perilaku warning.

Output: Modifikasi `Controllers/CoachMappingController.cs` (2 action). Migration = false.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-CONTEXT.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-RESEARCH.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-PATTERNS.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-UI-SPEC.md

<interfaces>
<!-- Analog VERBATIM — executor TIDAK perlu eksplorasi codebase. -->

SendAsync signature (Services/INotificationService.cs L20):
```csharp
Task<bool> SendAsync(string userId, string type, string title, string message, string? actionUrl = null);
```

ANALOG notif warn-only — CoachCoacheeMappingDeactivate L949-966 (COACH-03):
```csharp
try
{
    var coachUser = await _context.Users.FindAsync(mapping.CoachId);
    var coacheeUser = await _context.Users.FindAsync(mapping.CoacheeId);
    var coachName = coachUser?.FullName ?? coachUser?.UserName ?? mapping.CoachId;
    var coacheeName = coacheeUser?.FullName ?? coacheeUser?.UserName ?? mapping.CoacheeId;
    await _notificationService.SendAsync(mapping.CoachId, "COACH_MAPPING_DEACTIVATED", "Judul", "Pesan", "/CDP/CoachingProton");
    // ...
}
catch (Exception ex) { _logger.LogWarning(ex, "Notification send failed"); }
```

ApproveReassignSuggestion existing (L1614-1638): `oldCoachId = mapping.CoachId` (L1621) ditangkap SEBELUM `mapping.CoachId = newCoachId` (L1622). Audit log L1625-1634. `await _context.SaveChangesAsync()` L1636. `return Json(new { success = true })` L1637.

AF-5 microcopy DIKUNCI (UI-SPEC §116-120), EVENT_TYPE `COACH_REASSIGNED`:
| Recipient | userId | Judul | Pesan |
| Coach lama | oldCoachId | `Penugasan Coaching Dialihkan` | `Penugasan coaching Anda dengan {coacheeName} telah dialihkan ke coach lain.` |
| Coach baru | newCoachId | `Coach Ditunjuk` | `Anda ditunjuk sebagai coach untuk {coacheeName}.` |
| Coachee | mapping.CoacheeId | `Coach Anda Berubah` | `Coach Anda telah diganti menjadi {newCoachName}.` |

AF-7 loop existing — CoachCoacheeMappingAssign L508-535 (3 cabang yang HARUS direproduksi PERSIS):
```csharp
foreach (var coacheeId in req.CoacheeIds)
{
    var hasExistingAssignment = await _context.ProtonTrackAssignments
        .AnyAsync(a => a.CoacheeId == coacheeId && a.ProtonTrackId == req.ProtonTrackId.Value);
    if (hasExistingAssignment) continue;                                  // cabang 1: skip
    var prevAssignment = await _context.ProtonTrackAssignments
        .FirstOrDefaultAsync(a => a.CoacheeId == coacheeId && a.ProtonTrackId == prevTrack.Id);
    if (prevAssignment == null) { incompleteCoachees.Add(coacheeId); continue; } // cabang 2: null → incomplete
    var prevProgressCount = await _context.ProtonDeliverableProgresses
        .CountAsync(p => p.ProtonTrackAssignmentId == prevAssignment.Id);
    var allApproved = prevProgressCount > 0 && !await _context.ProtonDeliverableProgresses
        .AnyAsync(p => p.ProtonTrackAssignmentId == prevAssignment.Id && p.Status != "Approved");
    if (!allApproved) incompleteCoachees.Add(coacheeId);                  // cabang 3: not-all-approved → incomplete
}
```
Response warning L538-543 (`$"{incompleteCoachees.Count} coachee belum menyelesaikan {prevTrack.DisplayName}. Tetap lanjutkan?"`) dipertahankan VERBATIM.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AF-5 — 3 notifikasi reassign warn-only (COACH_REASSIGNED)</name>
  <read_first>
    - Controllers/CoachMappingController.cs L1614-1638 (ApproveReassignSuggestion — yang ditambahi notif)
    - Controllers/CoachMappingController.cs L949-966 (Deactivate notif COACH-03 — pola warn-only VERBATIM)
    - .planning/phases/356-.../356-UI-SPEC.md §"AF-5 Microcopy" (3 recipient, dikunci)
  </read_first>
  <action>
Di `Controllers/CoachMappingController.cs` action `ApproveReassignSuggestion` (L1614-1638): SISIPKAN blok notif try/catch warn-only SETELAH `await _context.SaveChangesAsync();` (L1636) dan SEBELUM `return Json(new { success = true });` (L1637). PERTAHANKAN: `oldCoachId` capture L1621, `mapping.CoachId = newCoachId` L1622, audit log L1625-1634 — semua tetap.

```csharp
await _context.SaveChangesAsync();

// AF-5 (Phase 356): notifikasi reassign 3 recipient (warn-only, mirror Deactivate COACH-03 L949-966)
try
{
    var oldCoach   = await _context.Users.FindAsync(oldCoachId);
    var newCoach   = await _context.Users.FindAsync(newCoachId);
    var coacheeUsr = await _context.Users.FindAsync(mapping.CoacheeId);
    var coacheeName  = coacheeUsr?.FullName ?? coacheeUsr?.UserName ?? mapping.CoacheeId;
    var newCoachName = newCoach?.FullName ?? newCoach?.UserName ?? newCoachId;

    await _notificationService.SendAsync(oldCoachId, "COACH_REASSIGNED",
        "Penugasan Coaching Dialihkan",
        $"Penugasan coaching Anda dengan {coacheeName} telah dialihkan ke coach lain.",
        "/CDP/CoachingProton");
    await _notificationService.SendAsync(newCoachId, "COACH_REASSIGNED",
        "Coach Ditunjuk",
        $"Anda ditunjuk sebagai coach untuk {coacheeName}.",
        "/CDP/CoachingProton");
    await _notificationService.SendAsync(mapping.CoacheeId, "COACH_REASSIGNED",
        "Coach Anda Berubah",
        $"Coach Anda telah diganti menjadi {newCoachName}.",
        "/CDP/CoachingProton");
}
catch (Exception ex) { _logger.LogWarning(ex, "Notification send failed"); }

return Json(new { success = true });
```

Microcopy + EVENT_TYPE `COACH_REASSIGNED` DIKUNCI UI-SPEC §116-120 — gunakan PERSIS. Resolve nama pakai pola `?.FullName ?? ?.UserName ?? id` (Deactivate L954-955). Warn-only: gagal kirim notif TIDAK menggagalkan reassign. JANGAN longgarkan `[Authorize(Roles="Admin")]`.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0, 0 error
    - `ApproveReassignSuggestion` berisi `COACH_REASSIGNED` 3 kali (3 SendAsync)
    - berisi judul `Penugasan Coaching Dialihkan`, `Coach Ditunjuk`, `Coach Anda Berubah`
    - blok notif dibungkus `try { ... } catch (Exception ex) { _logger.LogWarning(ex, "Notification send failed"); }`
    - `oldCoachId` capture (L1621) + audit log (L1625-1634) MASIH ada
  </acceptance_criteria>
  <done>Reassign mengirim 3 notif warn-only ke coach lama/baru/coachee dengan microcopy dikunci.</done>
</task>

<task type="auto">
  <name>Task 2: AF-7 — batch query progression-warning (zero behavior change)</name>
  <read_first>
    - Controllers/CoachMappingController.cs L497-546 (blok progression-warning + loop L508-535)
    - .planning/phases/356-.../356-PATTERNS.md §"AF-7" + RESEARCH Pitfall 3 (§376, 3 cabang harus identik)
  </read_first>
  <action>
Di `Controllers/CoachMappingController.cs` action `CoachCoacheeMappingAssign`, refactor loop progression-warning L508-535 dari N+1 (≈4 query/coachee) jadi pre-load batch dict SEBELUM loop, lalu evaluasi 3 cabang SAMA in-memory. Output `incompleteCoachees` HARUS IDENTIK.

Pre-load (sebelum `foreach`):
```csharp
var coacheeIds = req.CoacheeIds;

// (cabang 1) set coachee yang SUDAH punya assignment untuk track yang diminta → skip
var hasForRequestedTrack = (await _context.ProtonTrackAssignments
    .Where(a => coacheeIds.Contains(a.CoacheeId) && a.ProtonTrackId == req.ProtonTrackId.Value)
    .Select(a => a.CoacheeId).Distinct().ToListAsync())
    .ToHashSet();

// (cabang 2) prev-assignment per coachee untuk prevTrack
var prevAssignments = await _context.ProtonTrackAssignments
    .Where(a => coacheeIds.Contains(a.CoacheeId) && a.ProtonTrackId == prevTrack.Id)
    .Select(a => new { a.Id, a.CoacheeId })
    .ToListAsync();
var prevByCoachee = prevAssignments
    .GroupBy(a => a.CoacheeId)
    .ToDictionary(g => g.Key, g => g.First().Id);   // FirstOrDefault-equivalent: ambil satu prev-assignment
var prevAssignmentIds = prevAssignments.Select(a => a.Id).ToList();

// (cabang 3) status progress per prev-assignment-id
var progressByAssignment = (await _context.ProtonDeliverableProgresses
    .Where(p => prevAssignmentIds.Contains(p.ProtonTrackAssignmentId))
    .Select(p => new { p.ProtonTrackAssignmentId, p.Status })
    .ToListAsync())
    .GroupBy(p => p.ProtonTrackAssignmentId)
    .ToDictionary(g => g.Key, g => g.Select(x => x.Status).ToList());
```

Loop in-memory (reproduksi 3 cabang PERSIS):
```csharp
var incompleteCoachees = new List<string>();
foreach (var coacheeId in coacheeIds)
{
    if (hasForRequestedTrack.Contains(coacheeId)) continue;          // cabang 1: skip

    if (!prevByCoachee.TryGetValue(coacheeId, out var prevId))
    {
        incompleteCoachees.Add(coacheeId);                           // cabang 2: prev null → incomplete
        continue;
    }

    var statuses = progressByAssignment.TryGetValue(prevId, out var s) ? s : new List<string>();
    var prevProgressCount = statuses.Count;
    var allApproved = prevProgressCount > 0 && statuses.All(st => st == "Approved");  // count>0 && tak ada non-Approved
    if (!allApproved) incompleteCoachees.Add(coacheeId);             // cabang 3: not-all-approved → incomplete
}
```

KRITIS Pitfall 3 (zero behavior change):
- cabang 1 (`hasExistingAssignment` → continue) HARUS dipertahankan.
- cabang 2 (`prevAssignment == null` → add+continue) HARUS dipertahankan.
- cabang 3: `prevProgressCount > 0 && all == "Approved"` — count==0 → allApproved=false → incomplete (JANGAN hilangkan syarat `> 0`).
- `FirstOrDefaultAsync` original mengambil SATU prev-assignment → `prevByCoachee` ambil `g.First()` (sepadan). Bila ada >1 prev-assignment per coachee+prevTrack (jarang), perilaku ambil-satu dipertahankan.
- Response warning L538-543 (`$"{incompleteCoachees.Count} coachee belum menyelesaikan {prevTrack.DisplayName}. Tetap lanjutkan?"` + `incompleteCount`) dipertahankan VERBATIM.

D-10: hanya kurangi query count, JANGAN ubah perilaku warning. Status literal `"Approved"` case-sensitive (IDENTIK existing).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0, 0 error
    - blok progression-warning TIDAK lagi memanggil `AnyAsync`/`CountAsync`/`FirstOrDefaultAsync` DI DALAM `foreach (var coacheeId in ...)` (query dipindah ke pre-load sebelum loop)
    - 3 cabang dipertahankan: `Contains` skip + prev-null add + `prevProgressCount > 0 && all "Approved"`
    - response warning `belum menyelesaikan {prevTrack.DisplayName}` + `incompleteCount` VERBATIM
  </acceptance_criteria>
  <done>Progression-warning pakai batch dict (3 query batch, bukan N+1), output incompleteCoachees identik.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → ApproveReassignSuggestion | form POST `mappingId`,`newCoachId`; `[Authorize(Roles="Admin")]` + `[ValidateAntiForgeryToken]` |
| Admin/HC browser → CoachCoacheeMappingAssign progression-warning | JSON payload `CoachAssignRequest` (read-only query untuk warning) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-08 | Denial of Service | AF-5 notif gagal menggagalkan reassign | mitigate | notif warn-only `try/catch` — kegagalan kirim hanya di-log, reassign tetap commit (pola Deactivate) |
| T-356-09 | Information Disclosure | AF-5 isi notif | accept | microcopy tidak memuat data sensitif (hanya nama + status reassign); URL `/CDP/CoachingProton` internal |
| T-356-10 | Tampering | AF-7 batch refactor mengubah perilaku warning | mitigate | 3 cabang direproduksi persis; warning hanya informatif (user confirm), bukan kontrol akses; parity dijaga (RESEARCH Pitfall 3) |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error
- `dotnet test` full suite hijau (wave-merge)
- AF-5 reassign → 3 notif terkirim: diverifikasi Plan 05 (Playwright/manual UAT)
- AF-7 parity: opsional regresi test (RESEARCH Wave 0 opsional) ATAU verifikasi manual warning muncul sama untuk track Tahun 2/3 — Plan 05
</verification>

<success_criteria>
1. `ApproveReassignSuggestion` mengirim 3 notif warn-only (coach lama/baru/coachee) EVENT_TYPE COACH_REASSIGNED.
2. Progression-warning loop pakai batch dict (tidak ada query di dalam foreach), output incompleteCoachees identik (zero behavior change).
3. `dotnet build` 0 error.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-03-SUMMARY.md`
</output>
