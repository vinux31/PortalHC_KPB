---
phase: 356-audit-fix-assign-coach-coachee
plan: 05
type: execute
wave: 4
depends_on: [356-01, 356-02, 356-03, 356-04]
files_modified:
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [AF-1, AF-2, AF-3, AF-5, AF-6, AF-7]
tags: [coaching, verification, uat, playwright, seed-workflow]

must_haves:
  truths:
    - "dotnet build 0 error + dotnet test full suite hijau (baseline ~131 + 4 [Fact] AF-1)"
    - "UAT track id=4: coachee Alkylation 3/3 Approved muncul di GetEligibleCoachees / dropdown CreateAssessment Assessment Proton (AF-1 e2e)"
    - "UAT AF-2: centang coachee unit X → checkbox unit lain disabled + hint; clear → re-enable"
    - "UAT AF-5: reassign → 3 notif terkirim (coach lama/baru/coachee)"
    - "UAT AF-3: graduate coachee → mapping IsActive=false, badge Graduated (showAll), coachee bisa di-assign unit lain"
    - "DB lokal di-snapshot sebelum seed + di-restore setelah test (SEED_WORKFLOW), SEED_JOURNAL ditandai cleaned"
    - "Tidak ada regresi assign/deactivate/reactivate"
  artifacts:
    - path: "docs/SEED_JOURNAL.md"
      provides: "Entri seed fixture track id=4 (temporary+local-only) dengan status active→cleaned"
      contains: "track id=4"
  key_links:
    - from: "Playwright UAT / browser localhost:5277"
      to: "GetEligibleCoachees track id=4"
      via: "assign Alkylation coachee → approve 3 deliverable → CreateAssessment Assessment Proton → coachee muncul"
      pattern: "track id=4"
---

<objective>
Gate verifikasi & UAT Phase 356 (CONTEXT D-13/D-14/D-15). Buktikan keenam fix (AF-1/2/3/5/6/7) bekerja end-to-end di localhost:5277 sesuai CLAUDE.md Develop Workflow, dengan disiplin SEED_WORKFLOW (snapshot→journal→restore) untuk fixture track id=4. Pastikan `dotnet build` 0 error + `dotnet test` hijau + tidak ada regresi.

Purpose: Bukti otomatis (xUnit) + manual (Playwright/browser) bahwa fitur Assign Coach×Coachee berfungsi benar. AF-1 adalah headline (track multi-unit eligibility).

Output: Gate hijau + entri SEED_JOURNAL + checkpoint human-verify. Tidak ada perubahan kode produksi di plan ini (hanya verifikasi + dokumentasi seed). Migration = false.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md
@docs/SEED_WORKFLOW.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-CONTEXT.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-VALIDATION.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-RESEARCH.md

<interfaces>
<!-- Konteks operasional UAT — executor TIDAK perlu eksplorasi. -->

Data track id=4 (DB lokal HcPortalDB_Dev, audit data-verified 2026-06-06): 2 unit — Alkylation Unit 065 = 3 deliverable, RFCC NHT 053 = 1 deliverable. Coachee Alkylation 3/3 Approved HARUS muncul setelah AF-1 fix.

AD auth lokal (MEMORY Phase 355, RESEARCH A5): jalankan dengan `Authentication__UseActiveDirectory=false dotnet run` (appsettings handoff AD=true). Login admin@pertamina.com / 123456 (DB HcPortalDB_Dev SQLEXPRESS).

SEED_WORKFLOW (docs/SEED_WORKFLOW.md §5): snapshot `sqlcmd BACKUP DATABASE` → catat SEED_JOURNAL status `active` → restore `RESTORE ... WITH REPLACE` setelah test (sukses ATAU gagal) → tandai `cleaned`. Klasifikasi fixture = `temporary + local-only`. CATATAN: track id=4 mungkin SUDAH ada di DB lokal — verifikasi dulu; bila cukup pakai data existing + reset state coachee, seed tambahan minimal.

Playwright e2e infra existing: tests/e2e/helpers/ (wizardSelectors.ts dll). Auth helper e2e ada (MEMORY: AD=false WAJIB saat e2e auth).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate otomatis — dotnet build + dotnet test full suite + baseline regresi</name>
  <files>(verifikasi-only — tidak ada file produksi diubah)</files>
  <read_first>
    - .planning/phases/356-.../356-VALIDATION.md §"Sampling Rate" + §"Per-Task Verification Map"
    - CLAUDE.md §"Develop Workflow" (verifikasi lokal wajib)
  </read_first>
  <action>
Jalankan gate otomatis sesuai VALIDATION.md:
1. `dotnet build HcPortal.csproj` — WAJIB 0 error (baseline ~22 warning OK).
2. `dotnet test` — full suite WAJIB hijau. Baseline ~131 test (Phase 355) + 4 [Fact] AF-1 baru = ~135. Konfirmasi 4 [Fact] `IsEligiblePerUnit` lolos.
3. Bila ada test gagal: identifikasi apakah regresi dari Phase 356 (assign/deactivate/reactivate/graduate) atau pre-existing. Regresi Phase 356 = blocker; perbaiki sebelum lanjut.

Catat jumlah test pass/fail di SUMMARY. Tidak ada perubahan kode produksi di task ini.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -3 && dotnet test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0, 0 error
    - `dotnet test` exit 0, semua test passed (baseline + 4 AF-1)
    - 4 [Fact] `IsEligiblePerUnit` termasuk yang lolos
  </acceptance_criteria>
  <done>Build 0 error + full suite hijau termasuk AF-1 helper; tidak ada regresi.</done>
</task>

<task type="auto">
  <name>Task 2: Seed fixture track id=4 (SEED_WORKFLOW) + jalankan UAT otomatis Playwright/browser AF-1/AF-2/AF-3/AF-5</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_WORKFLOW.md (§5 command sqlcmd BACKUP/RESTORE, klasifikasi, format journal)
    - docs/DEV_WORKFLOW.md (verifikasi lokal, localhost:5277)
    - tests/e2e/helpers/ (auth + selector helper existing untuk pola Playwright)
    - .planning/phases/356-.../356-VALIDATION.md §"Manual-Only Verifications" (instruksi UAT 3 behavior)
  </read_first>
  <action>
**(a) SEED_WORKFLOW snapshot** — SEBELUM seed/reset state apa pun di DB lokal:
- Snapshot: `sqlcmd -S localhost\SQLEXPRESS -E -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev_pre356uat_<timestamp>.bak' WITH INIT"` (sesuaikan command persis docs/SEED_WORKFLOW.md §5.1).
- Catat di `docs/SEED_JOURNAL.md`: tujuan = "UAT Phase 356 AF-1 track id=4 eligibility", klasifikasi = `temporary + local-only`, entitas tersentuh = CoachCoacheeMappings/ProtonTrackAssignments/ProtonDeliverableProgress, status = `active`, path .bak.

**(b) Verifikasi data track id=4** — query DB lokal: track id=4 ada 2 unit (Alkylation 065=3 deliverable, RFCC 053=1)? Bila data existing cukup, reset state coachee Alkylation ke 3/3 Approved (minimal seed). Bila track id=4 berubah/hilang, buat fixture minimal sesuai struktur audit.

**(c) Jalankan app lokal** dengan AD off: `Authentication__UseActiveDirectory=false dotnet run` → localhost:5277. Login admin@pertamina.com / 123456.

**(d) UAT (Playwright MCP atau browser manual, dokumentasikan langkah+hasil di SUMMARY):**
- **AF-1 e2e (headline):** assign coachee Alkylation ke track id=4 → approve 3 deliverable unit Alkylation → buka CreateAssessment kategori Assessment Proton track 4 → coachee Alkylation MUNCUL di dropdown `GetEligibleCoachees`. (Sebelum fix: tidak muncul.) Bonus: coachee RFCC 0/1 TIDAK muncul.
- **AF-2:** buka modal Assign → centang coachee unit X → coachee unit lain ter-disable + label text-muted + hint "Satu batch assign hanya untuk satu unit..." muncul → uncheck semua → semua re-enabled, hint hilang.
- **AF-3:** graduate coachee (tombol Graduated, butuh Tahun-3 complete) → mapping jadi IsActive=false → di list `showAll=true` tampil badge "Graduated" (bukan tombol Aktifkan) → coachee bisa di-assign lagi untuk unit lain (unique-index bebas).
- **AF-5:** ApproveReassignSuggestion → cek 3 notif terkirim (coach lama "Penugasan Coaching Dialihkan", coach baru "Coach Ditunjuk", coachee "Coach Anda Berubah") via UI notifikasi atau query UserNotifications.
- **AF-6 (smoke/manual):** opsional — assign duplikat (race sulit otomatis) → pesan "Coachee sudah memiliki coach aktif untuk unit ini..." (verifikasi kode-path bila race tak bisa direproduksi).
- **AF-7 (parity):** assign track Tahun 2/3 dengan coachee belum-complete prev-track → warning "X coachee belum menyelesaikan {track}. Tetap lanjutkan?" muncul SAMA seperti sebelum refactor (zero behavior change).

**(e) SEED_WORKFLOW restore** — SETELAH test (sukses ATAU gagal): `sqlcmd ... RESTORE DATABASE HcPortalDB_Dev FROM DISK='...bak' WITH REPLACE` (docs/SEED_WORKFLOW.md §5.2). Tandai SEED_JOURNAL entri jadi `cleaned`. JANGAN biarkan seed temporary nempel di DB lokal.

JANGAN edit kode/DB di server Dev/Prod (CLAUDE.md). Semua di lokal.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SEED_JOURNAL.md` berisi entri `track id=4` Phase 356 dengan status awal `active`
    - setelah test, SEED_JOURNAL entri ditandai `cleaned` (DB lokal di-restore)
    - SUMMARY mendokumentasikan hasil UAT AF-1/AF-2/AF-3/AF-5 (langkah + observasi PASS/FAIL)
    - coachee Alkylation 3/3 terbukti muncul di GetEligibleCoachees track id=4 (AF-1)
  </acceptance_criteria>
  <done>Fixture track id=4 di-snapshot+restore (DB lokal bersih); UAT AF-1/2/3/5 dijalankan dan didokumentasikan; AF-1 headline terbukti.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — UAT Assign Coach×Coachee + sign-off</name>
  <files>(checkpoint — tidak ada file diubah)</files>
  <action>
CHECKPOINT human-verify (blocking). Setelah Task 1+2 hijau, sajikan hasil ke user untuk verifikasi visual/fungsional di browser dan tunggu sign-off. Tidak ada otomasi tambahan di task ini — Claude sudah mengotomasi build/test/seed/UAT di Task 1+2; checkpoint ini hanya konfirmasi manusia.
  </action>
  <what-built>
    Phase 356 audit-fix lengkap: AF-1 eligibility per-unit (track multi-unit), AF-2 UI guard 1-unit/batch, AF-3 graduate IsActive=false+cascade, AF-5 notif reassign, AF-6 pesan duplikat spesifik, AF-7 batch query. Gate otomatis (build+test) hijau; UAT track id=4 dijalankan; DB lokal di-restore bersih.
  </what-built>
  <how-to-verify>
    1. Jalankan lokal: `Authentication__UseActiveDirectory=false dotnet run` → buka http://localhost:5277, login admin@pertamina.com / 123456.
    2. AF-1 (headline): buka CreateAssessment kategori Assessment Proton track 4 → konfirmasi coachee Alkylation (deliverable unit 3/3 Approved) MUNCUL di dropdown coachee. Sebelumnya tidak pernah muncul.
    3. AF-2: /Admin/CoachCoacheeMapping → tombol Assign → centang satu coachee → konfirmasi coachee unit lain ter-disable (redup) + hint "Satu batch assign hanya untuk satu unit..." → uncheck → semua aktif lagi.
    4. AF-3: graduate satu coachee (Tahun-3 complete) → list (showAll) tampil badge "Graduated"; coachee yang sebelumnya graduated bisa di-assign unit lain.
    5. AF-5: lakukan reassign (ApproveReassignSuggestion) → cek lonceng notifikasi: coach lama/baru/coachee menerima notif reassign.
    6. Konfirmasi tidak ada error di flow assign/deactivate/reactivate existing (regresi).
  </how-to-verify>
  <verify>Human sign-off "approved" via browser localhost:5277 (manual verification — tidak ada automated command).</verify>
  <done>User mengetik "approved" setelah keenam fix berperilaku benar di browser.</done>
  <resume-signal>Ketik "approved" bila keenam fix berperilaku benar, atau jelaskan isu yang ditemukan (mis. "AF-1 coachee masih tak muncul").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB lokal HcPortalDB_Dev ↔ seed fixture | snapshot/restore disiplin agar seed temporary tidak bocor (SEED_WORKFLOW) |
| localhost:5277 admin session | UAT dengan role Admin; AD off lokal (handoff AD=true) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-13 | Tampering | seed temporary mengotori DB lokal lintas-session | mitigate | SEED_WORKFLOW snapshot BEFORE + restore WITH REPLACE AFTER (sukses/gagal) + SEED_JOURNAL active→cleaned (RESEARCH Pitfall 5) |
| T-356-14 | Spoofing | UAT auth lokal | accept | AD off lokal hanya untuk UAT (`Authentication__UseActiveDirectory=false`); appsettings handoff tetap AD=true untuk IT; role-gate Admin/HC tetap berlaku |
</threat_model>

<verification>
- `dotnet build` 0 error + `dotnet test` hijau (full suite + 4 AF-1)
- UAT AF-1/AF-2/AF-3/AF-5 di localhost:5277 (D-14) — didokumentasikan + human-verify
- SEED_WORKFLOW: snapshot+restore, SEED_JOURNAL active→cleaned (D-15)
- Tidak ada regresi assign/deactivate/reactivate
- Human sign-off "approved"
</verification>

<success_criteria>
1. Build 0 error + full test suite hijau (termasuk 4 [Fact] AF-1).
2. AF-1 e2e track id=4: coachee Alkylation 3/3 muncul di GetEligibleCoachees (headline terbukti).
3. AF-2/AF-3/AF-5 UAT PASS di browser; AF-6/AF-7 diverifikasi (smoke/parity).
4. DB lokal di-restore bersih (SEED_JOURNAL cleaned).
5. Human verify "approved".
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-05-SUMMARY.md`. Catat: commit hash + flag migration=false untuk notifikasi IT handoff (CLAUDE.md Develop Workflow). Catat AF-4 sebagai backlog (defer, comment-only) untuk `/gsd-add-backlog`.
</output>
