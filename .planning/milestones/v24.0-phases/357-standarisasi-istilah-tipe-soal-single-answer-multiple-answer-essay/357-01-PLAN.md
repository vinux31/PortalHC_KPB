---
phase: 357-standarisasi-istilah-tipe-soal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/QuestionTypeLabelsTests.cs
  - Models/QuestionTypeLabels.cs
autonomous: true
requirements: [LBL-02]

must_haves:
  truths:
    - "QuestionTypeLabels.Long(\"MultipleChoice\") mengembalikan 'Single Answer (1 jawaban benar)'"
    - "QuestionTypeLabels.Long(\"MultipleAnswer\") mengembalikan 'Multiple Answer (≥2 jawaban benar)'"
    - "QuestionTypeLabels.Short(\"MultipleChoice\") mengembalikan 'Single Answer'"
    - "QuestionTypeLabels.Short(\"MultipleAnswer\") mengembalikan 'Multiple Answer'"
    - "Short/Long untuk Essay tetap 'Essay'; BadgeClass tidak berubah"
    - "Surface yang sudah pakai helper (badge ManagePackageQuestions L77, flash TempData) otomatis ikut wording baru"
  artifacts:
    - path: "HcPortal.Tests/QuestionTypeLabelsTests.cs"
      provides: "Lock wording baru Long/Short via [Theory]/[Fact]"
      contains: "Single Answer (1 jawaban benar)"
    - path: "Models/QuestionTypeLabels.cs"
      provides: "Helper single-source wording baru"
      contains: "Single Answer (1 jawaban benar)"
  key_links:
    - from: "Models/QuestionTypeLabels.cs"
      to: "HcPortal.Tests/QuestionTypeLabelsTests.cs"
      via: "Assert.Equal terhadap return Long/Short"
      pattern: "QuestionTypeLabels\\.(Long|Short)"
---

<objective>
Wave 0 + Grup A: kunci wording baru via test xUnit dulu (RED), lalu ubah string di helper `Models/QuestionTypeLabels.cs` (`Long()` + `Short()` + fallback `_`) menjadi "Single Answer / Multiple Answer / Essay" (GREEN). `BadgeClass()` TIDAK disentuh.

Purpose: Helper adalah single-source-of-truth. Mengubah string di sini otomatis mem-propagate ke semua surface yang sudah memakai helper (badge tabel ManagePackageQuestions L77, flash TempData AssessmentAdminController L6155/6160/6362/6367). Test mengunci wording baru agar regresi label tertangkap.

Output: 1 file test baru + 1 file helper diubah. DB enum value TIDAK berubah (no migration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/357-standarisasi-istilah-tipe-soal-single-answer-multiple-answer-essay/357-CONTEXT.md
@docs/superpowers/specs/2026-06-09-question-type-naming-single-answer-design.md

<interfaces>
<!-- Kontrak helper saat ini (CURRENT). Executor ubah string return, JANGAN ubah signature/switch keys. -->
From Models/QuestionTypeLabels.cs (CURRENT — akan diubah):
```csharp
public static string Long(string? type) => type switch {
    "MultipleChoice" => "Single Choice (1 jawaban benar)",
    "MultipleAnswer" => "Multiple Answers (≥2 jawaban benar)",
    "Essay"          => "Essay",
    _                => "Single Choice (1 jawaban benar)"
};
public static string Short(string? type) => type switch {
    "MultipleChoice" => "Single Choice",
    "MultipleAnswer" => "Multiple Answers",
    "Essay"          => "Essay",
    _                => "Single Choice"
};
public static string BadgeClass(string? type) => ... // JANGAN DISENTUH
```
Switch keys ("MultipleChoice"/"MultipleAnswer"/"Essay") = DB enum value, TETAP. Hanya string return yang diubah.

Test style (lihat HcPortal.Tests/CertificateStatusTests.cs sebagai contoh):
```csharp
using HcPortal.Models;
using Xunit;
namespace HcPortal.Tests;
public class XxxTests { [Theory]/[Fact] ... Assert.Equal(...) }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Buat QuestionTypeLabelsTests yang mengunci wording baru</name>
  <read_first>
    - HcPortal.Tests/CertificateStatusTests.cs (gaya test, namespace `HcPortal.Tests`, `using HcPortal.Models;` + `using Xunit;`)
    - Models/QuestionTypeLabels.cs (signature Long/Short)
    - spec §"Mapping Wording" (tabel) + §"Verifikasi" SC#1
    - 357-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - Long("MultipleChoice") == "Single Answer (1 jawaban benar)"
    - Long("MultipleAnswer") == "Multiple Answer (≥2 jawaban benar)"
    - Long("Essay") == "Essay"
    - Long(null) == "Single Answer (1 jawaban benar)" (fallback)
    - Short("MultipleChoice") == "Single Answer"
    - Short("MultipleAnswer") == "Multiple Answer"
    - Short("Essay") == "Essay"
    - Short(null) == "Single Answer" (fallback)
  </behavior>
  <action>
    Buat file `HcPortal.Tests/QuestionTypeLabelsTests.cs`. Header komentar: "// Phase 357 LBL-02 — lock wording baru Single Answer/Multiple Answer/Essay (override Phase 305)."
    Namespace `HcPortal.Tests`, `using HcPortal.Models;` + `using Xunit;`.
    Kelas `public class QuestionTypeLabelsTests`. Pakai `[Theory]` + `[InlineData(...)]` untuk Long dan Short:
    - `[Fact]` atau `[Theory]` `Long_NewWording`: assert 4 case di atas (termasuk null fallback) dengan `Assert.Equal(expected, QuestionTypeLabels.Long(input))`.
    - `[Fact]` atau `[Theory]` `Short_NewWording`: assert 4 case di atas dengan `Assert.Equal(expected, QuestionTypeLabels.Short(input))`.
    Gunakan literal PERSIS dari `<behavior>` (perhatikan karakter "≥" dan "(1 jawaban benar)").
    Test ini WAJIB GAGAL dulu (helper masih wording lama) — itu state RED yang benar.
    Tidak ada test lain di repo yang assert label lama (sudah dicek: grep "Single Choice"/"Multiple Answers" di HcPortal.Tests = 0 match), jadi tidak perlu update test existing.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTypeLabels"</automated>
  </verify>
  <acceptance_criteria>
    - QuestionTypeLabelsTests.cs ADA dan berisi literal "Single Answer (1 jawaban benar)" dan "Multiple Answer (≥2 jawaban benar)"
    - QuestionTypeLabelsTests.cs berisi `Assert.Equal` terhadap `QuestionTypeLabels.Long` dan `QuestionTypeLabels.Short`
    - Test RUN dan GAGAL (RED) sebelum Task 2 (bukti test mengunci sesuatu yang belum ada)
  </acceptance_criteria>
  <done>File test ada, run, dan gagal merah karena helper masih "Single Choice"/"Multiple Answers". Commit: `test(357-01): lock new question-type wording (RED)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Ubah string Long/Short di QuestionTypeLabels.cs ke wording baru</name>
  <read_first>
    - Models/QuestionTypeLabels.cs (FULL — 29 baris)
    - spec §"Scope Kerja → Grup A" + §"Mapping Wording"
    - CONTEXT.md D-02, D-05
  </read_first>
  <action>
    Edit `Models/QuestionTypeLabels.cs` HANYA pada string return method `Long()` dan `Short()` (termasuk fallback `_`). Mapping PERSIS old→new:

    Di `Long()`:
    - `"MultipleChoice" => "Single Choice (1 jawaban benar)"` → `"MultipleChoice" => "Single Answer (1 jawaban benar)"`
    - `"MultipleAnswer" => "Multiple Answers (≥2 jawaban benar)"` → `"MultipleAnswer" => "Multiple Answer (≥2 jawaban benar)"`
    - `"Essay" => "Essay"` → TETAP
    - `_ => "Single Choice (1 jawaban benar)"` → `_ => "Single Answer (1 jawaban benar)"`

    Di `Short()`:
    - `"MultipleChoice" => "Single Choice"` → `"MultipleChoice" => "Single Answer"`
    - `"MultipleAnswer" => "Multiple Answers"` → `"MultipleAnswer" => "Multiple Answer"`
    - `"Essay" => "Essay"` → TETAP
    - `_ => "Single Choice"` → `_ => "Single Answer"`

    JANGAN sentuh `BadgeClass()` (semua return bg-* tetap). JANGAN ubah switch key ("MultipleChoice" dst = DB enum value). JANGAN ubah signature.
    Setelah edit, jalankan test (harus GREEN).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTypeLabels"</automated>
  </verify>
  <acceptance_criteria>
    - QuestionTypeLabels.cs contains "Single Answer (1 jawaban benar)"
    - QuestionTypeLabels.cs contains "Multiple Answer (≥2 jawaban benar)"
    - QuestionTypeLabels.cs contains `=> "Single Answer"` (Short) dan `=> "Multiple Answer"` (Short)
    - QuestionTypeLabels.cs NOT contains "Single Choice"
    - QuestionTypeLabels.cs NOT contains "Multiple Answers" (perhatikan 's' — kata baru "Multiple Answer" tanpa 's')
    - QuestionTypeLabels.cs masih contains "bg-secondary" / "bg-primary" / "bg-info text-dark" (BadgeClass utuh)
    - Test QuestionTypeLabels GREEN (semua hijau)
  </acceptance_criteria>
  <done>Helper return wording baru; test hijau; BadgeClass utuh; switch key DB enum utuh. Commit: `feat(357-01): rebrand question-type labels to Single/Multiple Answer (GREEN)`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (tidak ada baru) | Phase pure label/string-return; tidak ada input baru, tidak ada authz baru, tidak ada query baru |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-01 | Tampering | QuestionTypeLabels.Long/Short return string | accept | Static literal di-render via Razor encoding di surface; tidak ada interpolasi input user. Test mengunci nilai persis. Risiko sangat rendah (ASVS L1 N/A — tidak ada input/authz). |
| T-357-02 | Information Disclosure | switch key DB enum value | accept | Key tidak diubah; label tidak membocorkan data. Tidak ada PII. |
</threat_model>

<verification>
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTypeLabels"` hijau
- `dotnet build HcPortal.csproj -clp:ErrorsOnly` 0 error
- grep `Single Choice` / `Multiple Answers` di Models/QuestionTypeLabels.cs = 0
</verification>

<success_criteria>
- Helper Long/Short mengembalikan wording baru; fallback ikut baru; BadgeClass tak berubah; DB enum key utuh.
- Test mengunci wording baru (RED→GREEN tercatat di 2 commit).
- Surface yang sudah pakai helper (badge L77, flash TempData) otomatis ikut tanpa edit manual.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/357-standarisasi-istilah-tipe-soal-single-answer-multiple-answer-essay/357-01-SUMMARY.md`
</output>
