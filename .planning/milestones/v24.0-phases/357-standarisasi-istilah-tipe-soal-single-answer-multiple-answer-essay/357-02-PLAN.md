---
phase: 357-standarisasi-istilah-tipe-soal
plan: 02
type: execute
wave: 2
depends_on: [357-01]
files_modified:
  - Views/Admin/ManagePackageQuestions.cshtml
  - Views/Admin/EditPesertaAnswers.cshtml
  - Views/Admin/ImportPackageQuestions.cshtml
  - Controllers/AssessmentAdminController.cs
  - Controllers/CMPController.cs
autonomous: true
requirements: [LBL-02]

must_haves:
  truths:
    - "Dropdown tipe soal di ManagePackageQuestions menampilkan teks dari helper (Single Answer/Multiple Answer/Essay)"
    - "Badge tipe soal di EditPesertaAnswers menampilkan label penuh via Short() (bukan singkatan MC/MA)"
    - "Tombol Template di ImportPackageQuestions berbunyi 'Template Single Answer' / 'Template Multiple Answer'"
    - "Sel tipe pada export Excel per-peserta berisi 'SA' (bukan 'MC'); 'MA' tetap"
    - "Cabang dead code 'TrueFalse' di CMPController dihapus; analitik 3 tipe valid tidak berubah"
  artifacts:
    - path: "Views/Admin/ManagePackageQuestions.cshtml"
      provides: "Dropdown option text via @QuestionTypeLabels.Long binding (value attribute tetap)"
      contains: "@QuestionTypeLabels.Long"
    - path: "Views/Admin/EditPesertaAnswers.cshtml"
      provides: "Badge via @QuestionTypeLabels.Short"
      contains: "@QuestionTypeLabels.Short(q.QuestionType)"
    - path: "Views/Admin/ImportPackageQuestions.cshtml"
      provides: "Tombol template wording baru"
      contains: "Template Single Answer"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Excel cell SA/MA (S1)"
      contains: "\"SA\""
    - path: "Controllers/CMPController.cs"
      provides: "TrueFalse dead branch dihapus"
  key_links:
    - from: "Views/Admin/ManagePackageQuestions.cshtml"
      to: "Models/QuestionTypeLabels.cs"
      via: "@QuestionTypeLabels.Long(...) di option text"
      pattern: "@QuestionTypeLabels\\.Long"
    - from: "Views/Admin/EditPesertaAnswers.cshtml"
      to: "Models/QuestionTypeLabels.cs"
      via: "@QuestionTypeLabels.Short(q.QuestionType)"
      pattern: "QuestionTypeLabels\\.Short"
---

<objective>
Grup B + Grup C: konsolidasi surface UI hardcode ke helper + ubah abbrev export Excel + hapus dead code `"TrueFalse"`.

Grup B (label user-facing):
- `ManagePackageQuestions.cshtml:131-132` — dropdown `<option>` text → binding `@QuestionTypeLabels.Long(...)` (D-06 sesi ini: binding, bukan static — wujudkan single-source penuh). `value` attribute TETAP.
- `EditPesertaAnswers.cshtml:49` — badge ternary "MC/MA/Essay" → `@QuestionTypeLabels.Short(q.QuestionType)` (D-03 opsi-i: label penuh).
- `ImportPackageQuestions.cshtml:39,42` — tombol "Template Single Choice"/"Template Multiple Answers" → "Template Single Answer"/"Template Multiple Answer". Bullet L32 (`<code>MultipleChoice</code>`) + href L38,41 (`type=MC/MA`) TETAP.
- `AssessmentAdminController.cs:4550` — sel Excel "MC"→"SA" (S1); "MA" tetap.

Grup C (dead code):
- `CMPController.cs:3389,3624` — hapus cabang `|| questionType == "TrueFalse"` / `&& questionType != "TrueFalse"` (tipe hantu, unreachable — `NormalizeQuestionType` coerce unknown→MultipleChoice). Hasil analitik 3 tipe valid TIDAK berubah.

Purpose: Helper jadi single-source penuh + buang istilah lama yang tersisa di surface + bersihkan cabang mati.
Output: 3 view + 2 controller diubah. Tidak ada perubahan DB/route/JS handler/grading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-standarisasi-istilah-tipe-soal-single-answer-multiple-answer-essay/357-CONTEXT.md
@docs/superpowers/specs/2026-06-09-question-type-naming-single-answer-design.md

<interfaces>
<!-- Helper sudah wording baru setelah Plan 01. @using HcPortal.Models tersedia GLOBAL via Views/_ViewImports.cshtml L2 → @QuestionTypeLabels dapat dipakai di SEMUA view tanpa import lokal. -->
From Models/QuestionTypeLabels.cs (POST Plan 01):
```csharp
QuestionTypeLabels.Long("MultipleChoice")  => "Single Answer (1 jawaban benar)"
QuestionTypeLabels.Long("MultipleAnswer")  => "Multiple Answer (≥2 jawaban benar)"
QuestionTypeLabels.Long("Essay")           => "Essay"
QuestionTypeLabels.Short("MultipleChoice") => "Single Answer"
QuestionTypeLabels.Short("MultipleAnswer") => "Multiple Answer"
QuestionTypeLabels.Short("Essay")          => "Essay"
```

CURRENT ManagePackageQuestions.cshtml L130-133:
```html
<select class="form-select form-select-sm" id="QuestionType" name="questionType">
    <option value="MultipleChoice">Single Choice (1 jawaban benar)</option>
    <option value="MultipleAnswer">Multiple Answers (≥2 jawaban benar)</option>
    <option value="Essay">Essay</option>
</select>
```
JS handler L407-414 membaca `value` ("MultipleChoice" dst) — value attribute WAJIB tetap.

CURRENT EditPesertaAnswers.cshtml L49 (model item `q` punya prop `QuestionType` string):
```html
<span class="badge bg-secondary">@(q.QuestionType == "MultipleChoice" ? "MC" : q.QuestionType == "MultipleAnswer" ? "MA" : "Essay")</span>
```

CURRENT AssessmentAdminController.cs L4550 (var `tipe` = QuestionType string):
```csharp
ws.Cell(currentRow, 3).Value = tipe == "MultipleChoice" ? "MC" : "MA";
```

CURRENT CMPController.cs dead-code spots:
```csharp
// L3389:
if (questionType == "MultipleChoice" || questionType == "TrueFalse")
// L3624:
if (questionType != "MultipleChoice" && questionType != "TrueFalse") continue;
```
`questionType` selalu salah satu 3 enum (NormalizeQuestionType coerce unknown→MultipleChoice). "TrueFalse" tak pernah match → cabang TrueFalse mati. Buang term TrueFalse, perilaku untuk MultipleChoice identik.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grup B — dropdown binding + badge Short + tombol Import</name>
  <read_first>
    - Views/Admin/ManagePackageQuestions.cshtml L1 (@using) + L127-135 (dropdown) + L407-414 (JS handler baca value — JANGAN diubah, hanya verifikasi tak tersentuh)
    - Views/Admin/EditPesertaAnswers.cshtml L1 (@model) + L47-52 (badge)
    - Views/Admin/ImportPackageQuestions.cshtml L28-46 (bullet + tombol + href)
    - Views/_ViewImports.cshtml (konfirmasi `@using HcPortal.Models` global → helper accessible)
    - spec §"Grup B" + CONTEXT.md D-06, D-03
  </read_first>
  <action>
    **A. ManagePackageQuestions.cshtml L131-132** — ganti TEXT dua option pertama jadi binding helper (D-06: binding, bukan static). `value` attribute TETAP:
    - L131: `<option value="MultipleChoice">Single Choice (1 jawaban benar)</option>` → `<option value="MultipleChoice">@QuestionTypeLabels.Long("MultipleChoice")</option>`
    - L132: `<option value="MultipleAnswer">Multiple Answers (≥2 jawaban benar)</option>` → `<option value="MultipleAnswer">@QuestionTypeLabels.Long("MultipleAnswer")</option>`
    - L133 Essay: boleh `@QuestionTypeLabels.Long("Essay")` untuk konsistensi atau biarkan `Essay` static (sama hasil). Pilih binding untuk konsistensi penuh: `<option value="Essay">@QuestionTypeLabels.Long("Essay")</option>`.
    - JANGAN sentuh `value="..."`. JANGAN sentuh JS handler L407-414.
    - Null-guard tidak perlu (literal string konstan dilewatkan, bukan variabel) — Discretion CONTEXT.md.

    **B. EditPesertaAnswers.cshtml L49** — ganti badge ternary jadi helper:
    - `<span class="badge bg-secondary">@(q.QuestionType == "MultipleChoice" ? "MC" : q.QuestionType == "MultipleAnswer" ? "MA" : "Essay")</span>` → `<span class="badge bg-secondary">@QuestionTypeLabels.Short(q.QuestionType)</span>`
    - (Catatan: class `bg-secondary` dipertahankan apa adanya per spek — D-03 hanya ubah teks, bukan warna; jangan ganti ke BadgeClass.)

    **C. ImportPackageQuestions.cshtml L39,42** — ganti TEKS tombol saja:
    - L39: `<i class="bi bi-download me-1"></i>Template Single Choice` → `...Template Single Answer`
    - L42: `<i class="bi bi-download me-1"></i>Template Multiple Answers` → `...Template Multiple Answer`
    - JANGAN sentuh L38 `new { type = "MC" }` dan L41 `new { type = "MA" }` (URL contract / route key).
    - JANGAN sentuh L32 bullet `<code>MultipleChoice</code>` dst (enum value dev-facing).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - ManagePackageQuestions.cshtml contains `@QuestionTypeLabels.Long("MultipleChoice")` dan `@QuestionTypeLabels.Long("MultipleAnswer")`
    - ManagePackageQuestions.cshtml masih contains `value="MultipleChoice"` dan `value="MultipleAnswer"` dan `value="Essay"` (value attribute utuh)
    - ManagePackageQuestions.cshtml dropdown TIDAK lagi contains literal "Single Choice (1 jawaban benar)" / "Multiple Answers (≥2 jawaban benar)" sebagai teks option
    - EditPesertaAnswers.cshtml contains `@QuestionTypeLabels.Short(q.QuestionType)`
    - EditPesertaAnswers.cshtml NOT contains `? "MC" :` (ternary lama hilang)
    - ImportPackageQuestions.cshtml contains "Template Single Answer" dan "Template Multiple Answer"
    - ImportPackageQuestions.cshtml NOT contains "Template Single Choice" / "Template Multiple Answers"
    - ImportPackageQuestions.cshtml masih contains `type = "MC"` dan `type = "MA"` (href route key utuh)
    - dotnet build 0 error
  </acceptance_criteria>
  <done>3 view memakai/menampilkan wording baru; value attribute + route key + JS handler utuh; build hijau. Commit: `feat(357-02): konsolidasi label dropdown/badge/import ke helper`</done>
</task>

<task type="auto">
  <name>Task 2: Grup B Excel (SA/MA) + Grup C hapus dead TrueFalse</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs L4544-4555 (export Excel per-peserta) — pastikan ini export per-peserta (var `tipe`)
    - Controllers/CMPController.cs L3380-3400 (spot L3389 distractor MC) + L3615-3635 (spot L3624 export distractor)
    - spec §"Grup B (Excel)" §"Grup C" + CONTEXT.md D-04, D-07
    - spec §"TIDAK Disentuh" (konfirmasi L5589-5633 route key & flash TempData tidak masuk scope task ini)
  </read_first>
  <action>
    **A. AssessmentAdminController.cs L4550** — ubah abbrev sel Excel (S1: MC→SA, MA tetap):
    - `ws.Cell(currentRow, 3).Value = tipe == "MultipleChoice" ? "MC" : "MA";` → `ws.Cell(currentRow, 3).Value = tipe == "MultipleChoice" ? "SA" : "MA";`
    - Hanya literal `"MC"` → `"SA"`. JANGAN ubah kondisi `tipe == "MultipleChoice"` (itu baca DB enum value). JANGAN sentuh `"MA"`.

    **B. CMPController.cs L3389** — hapus cabang TrueFalse pada filter distractor:
    - `if (questionType == "MultipleChoice" || questionType == "TrueFalse")` → `if (questionType == "MultipleChoice")`
    - Perilaku untuk MultipleChoice identik; "TrueFalse" tak pernah match (dead) → tidak ada perubahan hasil analitik untuk 3 tipe valid.

    **C. CMPController.cs L3624** — hapus cabang TrueFalse pada guard export distractor:
    - `if (questionType != "MultipleChoice" && questionType != "TrueFalse") continue;` → `if (questionType != "MultipleChoice") continue;`
    - Logika: sebelumnya skip kecuali (MC atau TrueFalse). Karena TrueFalse mustahil, sekarang skip kecuali MC — identik untuk data nyata.

    JANGAN sentuh spot lain `== "MultipleChoice"` / `== "Essay"` / `== "MultipleAnswer"` (logic-check baca enum, BUKAN label). JANGAN sentuh DownloadQuestionTemplate L5589-5633 (route key). JANGAN edit flash TempData L6155/6160/6362/6367 (auto-update via helper Plan 01).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs L~4550 contains `? "SA" : "MA"`
    - AssessmentAdminController.cs L~4550 NOT contains `? "MC" : "MA"`
    - CMPController.cs NOT contains "TrueFalse" (kedua spot hilang)
    - CMPController.cs L~3389 masih contains `questionType == "MultipleChoice"` (kondisi MC dipertahankan)
    - CMPController.cs L~3624 masih contains `questionType != "MultipleChoice"` (guard MC dipertahankan)
    - dotnet build 0 error
  </acceptance_criteria>
  <done>Sel Excel SA/MA; dead TrueFalse hilang dari CMPController; analitik 3 tipe valid tak berubah (cabang MC utuh); build hijau. Commit: `feat(357-02): Excel SA/MA + hapus dead branch TrueFalse`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→export Excel | output string sel (statik literal "SA"/"MA"), tidak ada input user baru |
| (analitik) | hapus cabang unreachable, tidak mengubah query/agregasi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-03 | Tampering | CMPController distractor branch removal | mitigate | Cabang TrueFalse unreachable (NormalizeQuestionType coerce); kondisi MultipleChoice dipertahankan verbatim → hasil 3 tipe valid identik. Diverifikasi via build + Playwright analitik tidak regresi (Plan 04). |
| T-357-04 | Information Disclosure | Excel cell value | accept | Literal "SA"/"MA" statik, tidak ada PII; render via ClosedXML. Risiko sangat rendah. |
| T-357-05 | Tampering | Razor option binding | accept | `@QuestionTypeLabels.Long(...)` di-encode Razor; argumen literal konstan, bukan input user. value attribute (kontrol JS) tidak diubah. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj -clp:ErrorsOnly` 0 error
- grep `Single Choice` / `Multiple Answers` (sebagai teks) di 3 view = 0
- grep `TrueFalse` di Controllers/CMPController.cs = 0
- grep `? "MC" : "MA"` di Controllers/AssessmentAdminController.cs = 0
- value attribute & route key & JS handler tidak berubah (manual diff check)
</verification>

<success_criteria>
- Dropdown ManagePackageQuestions menampilkan label dari helper; badge EditPeserta pakai Short(); tombol Import wording baru; sel Excel SA/MA.
- Dead code TrueFalse hilang tanpa mengubah hasil analitik 3 tipe valid.
- DB enum value, route key (type=MC/MA), JS handler value-read tidak tersentuh.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/357-standarisasi-istilah-tipe-soal-single-answer-multiple-answer-essay/357-02-SUMMARY.md`
</output>
