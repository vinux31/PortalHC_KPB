---
phase: 357-standarisasi-istilah-tipe-soal
plan: 04
type: execute
wave: 3
depends_on: [357-01, 357-02, 357-03]
files_modified: []
autonomous: false
requirements: [LBL-02]

must_haves:
  truths:
    - "dotnet build 0 error + dotnet test full hijau"
    - "grep residual 'Single Choice'/'Multiple Answers'/'Multiple Choice' (tipe soal) = 0 di file non-arsip"
    - "SELECT DISTINCT QuestionType masih 3 enum (MultipleChoice/MultipleAnswer/Essay) — bukti no-migration"
    - "5 surface menampilkan wording baru: dropdown form Manage, badge tabel Manage, StartExam, ExamSummary, EditPesertaAnswers"
    - "Export Excel per-peserta sel tipe = SA/MA (bukan MC/MA)"
    - "Flow ujian existing (MC/MA/Essay tanpa gambar) tanpa regresi"
  artifacts: []
  key_links:
    - from: "(5 surface UI)"
      to: "Models/QuestionTypeLabels.cs"
      via: "render label dari helper / wording baru"
      pattern: "Single Answer|Multiple Answer"
---

<objective>
Gate akhir + UAT lintas-surface untuk Phase 357. Tidak mengubah kode (verifikasi only). Jalankan gate otomatis (build + test + grep residual + cek enum DB) lalu UAT 5 surface via Playwright MCP di localhost:5277 (AD=false), kemudian checkpoint human-verify.

Purpose: Membuktikan SC#1-6 spec terpenuhi: helper+surface konsisten, no-migration (enum utuh), dead code hilang tanpa regresi analitik, docs konsisten, Excel SA/MA, flow ujian tanpa regresi.
Output: VERIFICATION-ready evidence + human sign-off. Catat di SUMMARY untuk IT handoff (commit hash, flag migration=NONE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/357-standarisasi-istilah-tipe-soal-single-answer-multiple-answer-essay/357-CONTEXT.md
@.planning/phases/357-standarisasi-istilah-tipe-soal-single-answer-multiple-answer-essay/357-VALIDATION.md
@docs/superpowers/specs/2026-06-09-question-type-naming-single-answer-design.md

<interfaces>
<!-- Dev credentials UAT: admin@pertamina.com / 123456 (lokal). Jalankan dengan AD disabled. -->
Run command (CLAUDE.md Develop Workflow + MEMORY catatan AD lokal):
  Authentication__UseActiveDirectory=false dotnet run   (lalu buka http://localhost:5277)
DB lokal: SQLEXPRESS, HcPortalDB_Dev.

Surface UAT (5):
1. Dropdown form Manage: /Admin/ManagePackageQuestions?packageId={id} -> tombol Tambah Soal -> dropdown Tipe Soal menampilkan "Single Answer (1 jawaban benar)" / "Multiple Answer (≥2 jawaban benar)" / "Essay".
2. Badge tabel Manage: di halaman sama, badge tipe per soal (via QuestionTypeLabels.Short L77) -> "Single Answer"/"Multiple Answer"/"Essay".
3. StartExam: /CMP/StartExam (peserta) -> badge/label tipe soal wording baru.
4. ExamSummary: ringkasan ujian -> wording baru.
5. EditPesertaAnswers: /Admin/EditPesertaAnswers/... -> badge "Single Answer"/"Multiple Answer"/"Essay" (bukan "MC"/"MA").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate otomatis — build + test + grep residual + cek enum DB</name>
  <read_first>
    - 357-VALIDATION.md (Sampling Rate + Phase gate)
    - spec bagian "Verifikasi (Success Criteria)" SC#1,2,4,5
    - CONTEXT.md D-09
  </read_first>
  <action>
    Jalankan berurutan, semua harus lulus:
    1. `dotnet build HcPortal.csproj -clp:ErrorsOnly` -> 0 error.
    2. `dotnet test` (suite penuh, ~135 baseline + QuestionTypeLabelsTests) -> seluruhnya hijau. Termasuk regresi analitik (tidak ada test gagal akibat hapus TrueFalse).
    3. grep residual non-arsip (harus 0 untuk konteks tipe soal). Cakupan: Models, Views, Controllers, Services, wwwroot/documents/guides, wwwroot/documents/TKI/{*.py,*.html,*.md}. Pola: "Single Choice", "Multiple Answers", "Multiple Choice".
       - Gunakan Grep tool (ripgrep) dengan glob, EXCLUDE .planning/ dan docs/superpowers/ (arsip) dan .docx/.pdf (binary).
       - Catatan: enum value `MultipleChoice`/`MultipleAnswer` (satu kata, tanpa spasi) BUKAN target -- yang dicari adalah frasa berspasi "Multiple Choice"/"Multiple Answers"/"Single Choice".
    4. Cek enum DB no-migration (SC#2): jalankan query via sqlcmd ke HcPortalDB_Dev:
       `sqlcmd -S .\SQLEXPRESS -d HcPortalDB_Dev -E -Q "SELECT DISTINCT QuestionType FROM PackageQuestions"` -> hasil hanya MultipleChoice/MultipleAnswer/Essay (atau subset; tidak ada nilai baru). Bila sqlcmd tidak tersedia/credential beda, catat sebagai manual-pending dan lanjut (akan dikonfirmasi di checkpoint).
    5. grep `TrueFalse` di Controllers/CMPController.cs -> 0 (konfirmasi Grup C).
    6. grep `? "MC" : "MA"` di Controllers/AssessmentAdminController.cs -> 0 (konfirmasi Excel SA).
    Catat semua hasil (PASS/angka) untuk SUMMARY.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly; dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build 0 error
    - dotnet test full hijau (0 failed) — termasuk QuestionTypeLabelsTests
    - grep "Single Choice"/"Multiple Answers"/"Multiple Choice" (frasa berspasi) di file non-arsip = 0
    - grep "TrueFalse" di CMPController.cs = 0
    - grep `? "MC" : "MA"` di AssessmentAdminController.cs = 0
    - SELECT DISTINCT QuestionType = subset {MultipleChoice, MultipleAnswer, Essay} (tidak ada nilai baru) — atau ditandai manual-pending bila sqlcmd tak tersedia
  </acceptance_criteria>
  <done>Gate otomatis hijau; residual istilah lama 0; enum DB utuh; dead code & Excel terkonfirmasi. Tidak ada commit (verifikasi only).</done>
</task>

<task type="auto">
  <name>Task 2: UAT Playwright 5 surface + Excel + flow ujian (via MCP)</name>
  <read_first>
    - <interfaces> (5 surface + run command + credentials)
    - CONTEXT.md D-10 + spec SC#3,5,6
    - reference_dev_credentials (admin@pertamina.com lokal)
  </read_first>
  <action>
    Pastikan app berjalan: `Authentication__UseActiveDirectory=false dotnet run` (port 5277). Login admin@pertamina.com / 123456.
    Via Playwright MCP, verifikasi 5 surface menampilkan wording baru (screenshot/teks-assert tiap surface):
    1. Dropdown form Manage (ManagePackageQuestions, Tambah Soal): opsi "Single Answer (1 jawaban benar)" / "Multiple Answer (≥2 jawaban benar)" / "Essay".
    2. Badge tabel Manage: badge tipe soal "Single Answer"/"Multiple Answer"/"Essay".
    3. StartExam (peserta): label/badge tipe soal wording baru. (gunakan paket/sesi existing tanpa gambar -- tidak perlu seed; bila perlu data, ikuti SEED_WORKFLOW snapshot+restore).
    4. ExamSummary: wording baru.
    5. EditPesertaAnswers: badge "Single Answer"/"Multiple Answer"/"Essay" (bukan "MC"/"MA").
    Tambahan:
    - Export Excel per-peserta -> buka file -> sel tipe = "SA"/"MA" (SC#5). Bila download/buka xlsx tak praktis di MCP, verifikasi via kode L4550 + catat manual-confirm.
    - Flow ujian existing (jawab 1 soal MC + 1 MA + 1 Essay tanpa gambar) berjalan normal -> submit -> hasil tampil; tidak ada error/regresi (SC#6).
    Catat hasil tiap surface (PASS + bukti) untuk checkpoint.
    Bila butuh seed data: snapshot DB lokal dulu (SEED_WORKFLOW), catat di SEED_JOURNAL, restore setelah selesai.
  </action>
  <verify>
    <automated>MISSING — UAT visual via Playwright MCP (manual-driven); evidence dicatat di SUMMARY untuk checkpoint Task 3</automated>
  </verify>
  <acceptance_criteria>
    - 5 surface menampilkan "Single Answer"/"Multiple Answer"/"Essay" (tidak ada "Single Choice"/"Multiple Answers"/"MC"/"MA" sebagai label tipe pada surface ini)
    - Export Excel sel tipe = "SA"/"MA" (atau code-verified + manual-confirm dicatat)
    - Flow ujian MC/MA/Essay tanpa gambar berjalan tanpa regresi
  </acceptance_criteria>
  <done>5 surface + Excel + flow ujian terverifikasi via Playwright MCP; bukti dicatat untuk human sign-off. Tidak ada commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify final wording 5 surface</name>
  <what-built>
    Phase 357 selesai: helper QuestionTypeLabels rebrand ke "Single Answer / Multiple Answer / Essay"; dropdown/badge/import konsolidasi ke helper; Excel SA/MA; dead code TrueFalse dihapus; docs (6 guide + GuideContentProvider + TKI BAB-X) konsisten. Gate otomatis hijau (build+test+grep residual 0+enum DB utuh). Claude sudah UAT 5 surface via Playwright MCP.
  </what-built>
  <action>
    PAUSE untuk verifikasi manusia (blocking). Sajikan ringkasan hasil gate + UAT Task 1/2, lalu minta user mengikuti langkah <how-to-verify> dan memberi sinyal di <resume-signal>. Jangan lanjut menutup phase sebelum user mengetik "approved".
  </action>
  <how-to-verify>
    1. Pastikan app jalan: `Authentication__UseActiveDirectory=false dotnet run` -> http://localhost:5277 (login admin@pertamina.com / 123456).
    2. /Admin/ManagePackageQuestions (buka satu paket) -> klik "Tambah Soal" -> cek dropdown Tipe Soal berbunyi "Single Answer (1 jawaban benar)" / "Multiple Answer (≥2 jawaban benar)" / "Essay".
    3. Di tabel daftar soal paket yang sama -> badge tipe per soal = "Single Answer"/"Multiple Answer"/"Essay".
    4. Sebagai peserta, /CMP/StartExam (paket aktif) -> label/badge tipe soal = wording baru.
    5. ExamSummary (ringkasan ujian) -> wording baru.
    6. /Admin/EditPesertaAnswers (sesi peserta yang sudah submit) -> badge = "Single Answer"/"Multiple Answer"/"Essay" (BUKAN "MC"/"MA").
    7. (Opsional) Export Excel per-peserta -> kolom tipe = "SA"/"MA".
    8. (Opsional) Buka guide /Home/Guide (CMP, "Tipe Soal") -> "Single Answer (SA) / Multiple Answer (MA) / Essay".
    9. Konfirmasi flow ujian existing (jawab MC/MA/Essay tanpa gambar) tetap normal.
  </how-to-verify>
  <verify>Human-driven (blocking). Resume hanya setelah user mengetik "approved".</verify>
  <resume-signal>Ketik "approved" bila 5 surface + (opsional) Excel/guide sesuai; atau jelaskan ketidaksesuaian untuk diperbaiki.</resume-signal>
  <done>User memberi sinyal "approved" untuk 5 surface (+ opsional Excel/guide); phase siap ditutup. Bila ada ketidaksesuaian, buat catatan gap untuk gap-closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (tidak ada perubahan) | Plan verifikasi only; tidak menulis kode/DB/route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-08 | (regresi fungsional) | flow ujian + analitik | mitigate | UAT flow ujian MC/MA/Essay + dotnet test full + cek analitik (hapus TrueFalse tidak ubah hasil 3 tipe). Human sign-off blocking. |
| T-357-09 | (akurasi label lintas-surface) | 5 surface | mitigate | Playwright MCP assert teks tiap surface + human verify. |
</threat_model>

<verification>
- Gate: dotnet build 0 error + dotnet test full hijau + grep residual 0 + enum DB 3 nilai + TrueFalse 0 + Excel SA.
- UAT: 5 surface wording baru + Excel SA/MA + flow ujian tanpa regresi.
- Human sign-off "approved".
</verification>

<success_criteria>
- Semua SC#1-6 spec terbukti (build/test, no-migration enum, dead code tanpa regresi, docs konsisten, Excel SA/MA, 5 surface UI).
- Human verify approved.
- SUMMARY mencatat: commit range, flag migration = NONE (untuk IT handoff), residual = 0, hasil UAT 5 surface.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/357-standarisasi-istilah-tipe-soal-single-answer-multiple-answer-essay/357-04-SUMMARY.md`
</output>
