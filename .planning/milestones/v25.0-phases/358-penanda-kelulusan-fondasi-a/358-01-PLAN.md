---
phase: 358-penanda-kelulusan-fondasi-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/ProtonModels.cs
  - Migrations/<ts>_AddOriginToProtonFinalAssessment.cs
  - Migrations/<ts>_AddOriginToProtonFinalAssessment.Designer.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - HcPortal.Tests/ProtonCompletionServiceTests.cs
autonomous: false
requirements: [PCOMP-04]
must_haves:
  truths:
    - "Kolom Origin (nullable, maxlength 20) ada di ProtonFinalAssessment"
    - "Semua baris ProtonFinalAssessments lama ber-Origin='Interview' setelah migration"
    - "Test fixture disposable real-SQL (ProtonCompletionFixture) tersedia untuk plan berikutnya"
  artifacts:
    - path: "Models/ProtonModels.cs"
      provides: "Field Origin di ProtonFinalAssessment"
      contains: "public string? Origin"
    - path: "Migrations/<ts>_AddOriginToProtonFinalAssessment.cs"
      provides: "AddColumn Origin + data-seed UPDATE ke 'Interview'"
      contains: "UPDATE ProtonFinalAssessments SET Origin = 'Interview'"
    - path: "HcPortal.Tests/ProtonCompletionServiceTests.cs"
      provides: "Fixture TEST-05 disposable + 1 smoke fact migration apply"
      contains: "class ProtonCompletionFixture"
  key_links:
    - from: "Migrations/<ts>_AddOriginToProtonFinalAssessment.cs"
      to: "ProtonFinalAssessments table"
      via: "migrationBuilder.Sql data-seed"
      pattern: "UPDATE ProtonFinalAssessments SET Origin"
---

<objective>
Tambah kolom `Origin` (nullable, `[MaxLength(20)]`) ke `ProtonFinalAssessment` + migration data-seed yang men-set semua baris lama → `"Interview"` (semua penanda lama = interview Tahun 3). Buat scaffold test integration real-SQL (fixture TEST-05 disposable) yang akan dipakai plan service & backfill.

Purpose: Origin = pembeda 3 jalur penanda (Exam/Interview/Bypass), inti dari re-grade selektif (PCOMP-02) + reuse Bypass (Phase 360). Migration = satu-satunya schema change Diskusi A.
Output: model + migration pair (+snapshot) + test fixture + 1 smoke fact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-CONTEXT.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-RESEARCH.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md
@docs/DEV_WORKFLOW.md

<interfaces>
<!-- Shape ACTUAL ProtonFinalAssessment (VERIFIED Models/ProtonModels.cs:207-226). Field Origin yang ditambah. -->
public class ProtonFinalAssessment {
    public int Id { get; set; }
    public string CoacheeId { get; set; } = "";          // no FK
    public string CreatedById { get; set; } = "";        // HC user id, no FK
    public int ProtonTrackAssignmentId { get; set; }
    public ProtonTrackAssignment? ProtonTrackAssignment { get; set; }
    public string Status { get; set; } = "Completed";
    [Range(0,5)] public int CompetencyLevelGranted { get; set; }  // dormant (A-3), JANGAN drop
    public int? KkjMatrixItemId { get; set; }            // ADA, biarkan null
    public string? Notes { get; set; }
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime? CompletedAt { get; set; }
    // + public string? Origin { get; set; }  <- TAMBAH
}
<!-- DbSet name PLURAL: ProtonFinalAssessments (VERIFIED Data/ApplicationDbContext.cs:43). -->
<!-- using System.ComponentModel.DataAnnotations; sudah ada (file pakai [Range]). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah field Origin + generate migration data-seed</name>
  <files>Models/ProtonModels.cs, Migrations/&lt;ts&gt;_AddOriginToProtonFinalAssessment.cs, Migrations/&lt;ts&gt;_AddOriginToProtonFinalAssessment.Designer.cs, Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <read_first>
    - Models/ProtonModels.cs:207-226 (kelas ProtonFinalAssessment + verifikasi `using System.ComponentModel.DataAnnotations;` di top file)
    - Migrations/20260303073729_SetExistingRecordsActive.cs (pola data-seed migrationBuilder.Sql — analog exact)
    - 358-PATTERNS.md §1 + §2 (excerpt verified)
    - docs/SEED_WORKFLOW.md (snapshot/restore SQL Server BACKUP/RESTORE)
  </read_first>
  <action>
    1. Di `Models/ProtonModels.cs`, kelas `ProtonFinalAssessment`, tambah field SETELAH `Notes` (L223):
       `[MaxLength(20)] public string? Origin { get; set; }   // "Exam" | "Interview" | "Bypass"`
       (using `System.ComponentModel.DataAnnotations` sudah ada karena `[Range]` dipakai — JANGAN duplikat).
    2. Verifikasi `dotnet ef --version` (bila tidak ada: `dotnet tool install --global dotnet-ef`).
    3. **Snapshot DB lokal SEBELUM apply** (SEED_WORKFLOW): `sqlcmd -S localhost\SQLEXPRESS -E -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev_pre358origin.bak' WITH INIT"`. Catat di `docs/SEED_JOURNAL.md` (tujuan: apply migration AddOrigin; klasifikasi: schema migration permanent).
    4. `dotnet ef migrations add AddOriginToProtonFinalAssessment` → hasilkan pair `.cs` + `.Designer.cs` + update `ApplicationDbContextModelSnapshot.cs`. COMMIT ketiganya.
    5. Edit `Up()` migration: SETELAH `AddColumn` (auto-gen EF), TAMBAH manual baris data-seed (tabel PLURAL):
       `migrationBuilder.Sql("UPDATE ProtonFinalAssessments SET Origin = 'Interview' WHERE Origin IS NULL;");`
       JANGAN tulis singular `ProtonFinalAssessment` — akan gagal runtime.
    6. `dotnet ef database update` (LOKAL HcPortalDB_Dev saja — ❌ JANGAN Dev/Prod, CLAUDE.md).
    7. `dotnet build` (0 error).
    8. Flag IT: catat commit hash + flag migration di SUMMARY (promosi Dev/Prod = Team IT).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Models/ProtonModels.cs` contains `public string? Origin` (dalam kelas ProtonFinalAssessment, dengan `[MaxLength(20)]`)
    - File `Migrations/*_AddOriginToProtonFinalAssessment.cs` ADA dan contains `UPDATE ProtonFinalAssessments SET Origin = 'Interview'` (PLURAL)
    - File `Migrations/*_AddOriginToProtonFinalAssessment.Designer.cs` ADA
    - `dotnet build` exit 0
    - `dotnet ef database update` lokal sukses (kolom Origin nullable di tabel ProtonFinalAssessments)
    - `docs/SEED_JOURNAL.md` ada entry snapshot pre-migration
  </acceptance_criteria>
  <done>Kolom Origin ter-apply lokal; baris lama='Interview'; migration pair + snapshot committed; build 0 error; IT-flag dicatat.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold test fixture integration real-SQL + smoke fact migration</name>
  <files>HcPortal.Tests/ProtonCompletionServiceTests.cs</files>
  <read_first>
    - HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs:24-68 (TEST-05 fixture analog exact)
    - 358-PATTERNS.md §8 (fixture excerpt verified — mirror persis, ganti nama)
    - 358-RESEARCH.md §Validation Architecture (apa yang di-assert)
  </read_first>
  <action>
    Buat `HcPortal.Tests/ProtonCompletionServiceTests.cs`. Mirror persis fixture `OrgLabelMigrationIntegrationTests.cs:24-66`, ganti nama:
    - `public class ProtonCompletionFixture : IAsyncLifetime` dengan `DbName = $"HcPortalDB_Test_{Guid.NewGuid():N}"`, connection string `Server=localhost\\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30`, `InitializeAsync` panggil `ctx.Database.MigrateAsync()` (pipeline penuh — buktikan migration Origin apply), `DisposeAsync` panggil `EnsureDeletedAsync`. Catch-block sama (cleanup + throw XunitException).
    - `[Trait("Category", "Integration")] public class ProtonCompletionServiceTests : IClassFixture<ProtonCompletionFixture>` dengan ctor terima fixture.
    - 1 smoke [Fact] `Migration_AddsOriginColumn`: buat `ApplicationDbContext(_fixture.Options)`, seed 1 `ProtonTrackAssignment` + 1 `ProtonFinalAssessment` (TANPA set Origin → biarkan null pada insert baru), `SaveChanges`; lalu Assert kolom Origin ada/queryable: `Assert.NotNull(await ctx.ProtonFinalAssessments.Select(fa => fa.Origin).FirstOrDefaultAsync())` TIDAK valid (bisa null) → instead Assert query `Where(fa => fa.Origin == null)` tidak throw + `Assert.Single(ProtonFinalAssessments)`. Tujuan smoke = membuktikan kolom Origin ter-map (compile + query EF tidak error). [Fact] lain (Ensure/Remove/GetPassedYears) ditambah Plan 02.
    > Disposable `HcPortalDB_Test_<guid>` — DB lokal `HcPortalDB_Dev` TAK tersentuh (no SEED snapshot untuk test ini).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests" --no-build</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/ProtonCompletionServiceTests.cs` ADA dan contains `class ProtonCompletionFixture` + `IClassFixture<ProtonCompletionFixture>`
    - File contains `[Trait("Category", "Integration")]` dan `MigrateAsync`
    - File contains `[Fact]` smoke (referensi `.Origin`)
    - `dotnet build` exit 0 (test project compile dengan field Origin)
    - `dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests"` exit 0 (smoke green, butuh SQLEXPRESS)
  </acceptance_criteria>
  <done>Fixture disposable real-SQL + 1 smoke fact green; field Origin queryable via EF; siap diisi [Fact] service di Plan 02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migration `Origin` ter-apply ke DB lokal HcPortalDB_Dev (baris lama='Interview') + snapshot DB diambil sebelum apply.</what-built>
  <how-to-verify>
    1. Cek DB lokal: `sqlcmd -S localhost\SQLEXPRESS -E -d HcPortalDB_Dev -Q "SELECT Origin, COUNT(*) FROM ProtonFinalAssessments GROUP BY Origin"` → semua baris lama harus Origin='Interview' (atau 0 baris bila DB lokal kosong penanda — itu OK).
    2. Konfirmasi snapshot `C:\Temp\HcPortalDB_Dev_pre358origin.bak` ada (`docs/SEED_JOURNAL.md` ter-update).
    3. Konfirmasi migration pair + Designer + snapshot ter-commit untuk flag IT.
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan masalah (mis. baris lama tidak ber-Interview).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| developer → DB lokal | migration DDL + data-seed dijalankan lokal saja; promosi Dev/Prod = IT |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-01 | Tampering | migration data-seed `UPDATE ProtonFinalAssessments` | accept | Internal dev-only; snapshot DB sebelum apply (SEED_WORKFLOW) memungkinkan rollback. Tidak ada input eksternal. |
| T-358-02 | Denial | apply migration ke Dev/Prod tanpa snapshot | mitigate | CLAUDE.md: promosi = IT; developer flag migration + commit hash, tidak apply ke Dev/Prod sendiri. |
</threat_model>

<verification>
- `dotnet build` 0 error
- `dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests"` green (SQLEXPRESS)
- SQL cek: baris ProtonFinalAssessments lama → Origin='Interview'
- Migration pair + Designer + snapshot committed; IT-flag dicatat
</verification>

<success_criteria>
- Field `Origin` nullable [MaxLength(20)] ada di ProtonFinalAssessment (PCOMP-04)
- Migration apply lokal + baris lama='Interview'
- Fixture TEST-05 disposable siap pakai untuk Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/358-penanda-kelulusan-fondasi-a/358-01-SUMMARY.md`
</output>
