---
phase: 358-penanda-kelulusan-fondasi-a
plan: 02
type: execute
wave: 2
depends_on: ["358-01"]
files_modified:
  - Services/ProtonCompletionService.cs
  - Program.cs
  - HcPortal.Tests/ProtonCompletionServiceTests.cs
autonomous: true
requirements: [PCOMP-02, PCOMP-03]
must_haves:
  truths:
    - "Pembuatan/penghapusan penanda terpusat di satu service ProtonCompletionService"
    - "EnsureAsync idempotent: panggilan kedua untuk assignment yang sama tidak buat duplikat"
    - "RemoveExamOriginAsync hapus HANYA penanda Origin='Exam'; Bypass/Interview kebal"
  artifacts:
    - path: "Services/ProtonCompletionService.cs"
      provides: "Single-source EnsureAsync / RemoveExamOriginAsync / GetPassedYearsAsync"
      contains: "public async Task<bool> EnsureAsync"
    - path: "Program.cs"
      provides: "Scoped DI registration"
      contains: "AddScoped<HcPortal.Services.ProtonCompletionService>"
    - path: "HcPortal.Tests/ProtonCompletionServiceTests.cs"
      provides: "[Fact] idempotency + selektif Remove + GetPassedYears"
      contains: "RemoveExamOriginAsync"
  key_links:
    - from: "Services/ProtonCompletionService.cs"
      to: "ProtonTrackAssignments + ProtonFinalAssessments"
      via: "resolve assignment aktif + dedup AnyAsync"
      pattern: "AnyAsync\\(fa => fa.ProtonTrackAssignmentId"
    - from: "Program.cs"
      to: "ProtonCompletionService"
      via: "AddScoped"
      pattern: "AddScoped<HcPortal.Services.ProtonCompletionService>"
---

<objective>
Buat `ProtonCompletionService` (scoped DI) — sumber TUNGGAL pembuatan/penghapusan penanda `ProtonFinalAssessment`. Tiga method: `EnsureAsync` (idempotent, resolve assignment aktif, set `Origin`), `RemoveExamOriginAsync` (hapus HANYA `Origin=="Exam"`), `GetPassedYearsAsync` (helper murni query, TANPA gate enforcement — dipakai backfill/future). Register di `Program.cs`. Lengkapi [Fact] di test fixture (Plan 01).

Purpose: PCOMP-03 single-source + PCOMP-02 re-grade selektif. Bypass (Phase 360) akan reuse helper yang sama (`Origin="Bypass"`) → itu sebab Origin selektif penting.
Output: service + DI + 3-4 [Fact] integration real-SQL green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-CONTEXT.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-RESEARCH.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md

<interfaces>
<!-- DI shape analog (VERIFIED Services/GradingService.cs:16-30) -->
public class GradingService {
    private readonly ApplicationDbContext _context;
    private readonly IWorkerDataService _workerDataService;
    private readonly ILogger<GradingService> _logger;
    public GradingService(ApplicationDbContext context, IWorkerDataService workerDataService, ILogger<GradingService> logger) { ... }
}
<!-- ProtonTrackAssignment shape (VERIFIED Models/ProtonModels.cs:71-84) — NO CompletedAt -->
public class ProtonTrackAssignment {
    public int Id; public string CoacheeId; public string AssignedById;
    public int ProtonTrackId; public ProtonTrack? ProtonTrack;
    public bool IsActive = true; public DateTime AssignedAt; public DateTime? DeactivatedAt;
}
<!-- ProtonTrack (VERIFIED Models/ProtonModels.cs:8-22): TrackType ("Operator"/"Panelman"), TahunKe ("Tahun 1".."Tahun 3") -->
<!-- ProtonFinalAssessment shape: lihat Plan 01 interfaces (Origin sudah ada setelah 358-01). -->
<!-- Program.cs registration analog (VERIFIED Program.cs:54): builder.Services.AddScoped<HcPortal.Services.GradingService>(); -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat ProtonCompletionService + register DI</name>
  <files>Services/ProtonCompletionService.cs, Program.cs</files>
  <read_first>
    - Services/GradingService.cs:16-30 (DI/constructor pattern — mirror, tanpa IWorkerDataService)
    - Controllers/AssessmentAdminController.cs:3742-3765 (inline ensure+dedup yang di-ekstrak)
    - 358-PATTERNS.md §3 (excerpt EnsureAsync + RemoveExamOriginAsync + GetPassedYears)
    - Program.cs:50-58 (titik AddScoped)
    - docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md §4.1 (helper spec)
  </read_first>
  <behavior>
    - EnsureAsync(coacheeId, protonTrackId, createdById, origin, notes): resolve assignment aktif (CoacheeId+ProtonTrackId+IsActive); jika tidak ada → return false (0 penanda). Jika ada + belum ada penanda untuk assignment.Id → Add penanda (Origin=origin, CompetencyLevelGranted=0, Status="Completed") + SaveChanges → return true. Jika sudah ada → return false (idempotent), tidak duplikat.
    - RemoveExamOriginAsync(coacheeId, protonTrackId): hapus penanda untuk assignment cocok HANYA `Origin=="Exam"`; Bypass/Interview kebal. SaveChanges.
    - GetPassedYearsAsync(coacheeId, trackType): kembalikan daftar TahunKe yang sudah ber-penanda untuk trackType cocok. TANPA gate/blok apa pun.
  </behavior>
  <action>
    Buat `Services/ProtonCompletionService.cs` (namespace `HcPortal.Services`). Inject `ApplicationDbContext _context` + `ILogger<ProtonCompletionService> _logger` (mirror GradingService DI, TANPA IWorkerDataService).

    **`public async Task<bool> EnsureAsync(string coacheeId, int protonTrackId, string createdById, string origin, string? notes)`**:
    ```
    var assignment = await _context.ProtonTrackAssignments
        .FirstOrDefaultAsync(a => a.CoacheeId == coacheeId && a.ProtonTrackId == protonTrackId && a.IsActive);
    if (assignment == null) { _logger.LogWarning(...); return false; }
    var exists = await _context.ProtonFinalAssessments.AnyAsync(fa => fa.ProtonTrackAssignmentId == assignment.Id);
    if (exists) return false;
    _context.ProtonFinalAssessments.Add(new ProtonFinalAssessment {
        CoacheeId = coacheeId, CreatedById = createdById, ProtonTrackAssignmentId = assignment.Id,
        Status = "Completed", CompetencyLevelGranted = 0, Origin = origin, Notes = notes,
        CreatedAt = DateTime.UtcNow, CompletedAt = DateTime.UtcNow });
    await _context.SaveChangesAsync();
    return true;
    ```

    **`public async Task<bool> RemoveExamOriginAsync(string coacheeId, int protonTrackId)`**:
    resolve assignment(s) cocok `(CoacheeId, ProtonTrackId)` (IsActive aktif cukup — re-grade pada assignment aktif); ambil penanda `Where(fa => fa.ProtonTrackAssignmentId == assignment.Id && fa.Origin == "Exam")`, `RemoveRange`, `SaveChanges`. Return true bila ada yang dihapus. **JANGAN hapus Origin != "Exam"** (A-M9 — Bypass/Interview kebal).

    **`public async Task<List<string>> GetPassedYearsAsync(string coacheeId, string trackType)`**:
    join `ProtonFinalAssessments` → `ProtonTrackAssignment` → `ProtonTrack`, `Where(track.TrackType == trackType && fa.CoacheeId == coacheeId)`, `Select(track.TahunKe).Distinct().ToList()`. TANPA enforce/blok (D-02 — gate = Phase 359).

    Register di `Program.cs` dekat L54 (setelah GradingService):
    `builder.Services.AddScoped<HcPortal.Services.ProtonCompletionService>();`
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Services/ProtonCompletionService.cs` contains `public async Task<bool> EnsureAsync`
    - File contains `public async Task<bool> RemoveExamOriginAsync` dan `fa.Origin == "Exam"`
    - File contains `public async Task<List<string>> GetPassedYearsAsync`
    - File contains `AnyAsync(fa => fa.ProtonTrackAssignmentId == assignment.Id)` (dedup)
    - `Program.cs` contains `AddScoped<HcPortal.Services.ProtonCompletionService>`
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>Service 3-method + DI ter-register; build 0 error; tidak ada gate enforcement di GetPassedYears.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [Fact] idempotency + Remove selektif + GetPassedYears</name>
  <files>HcPortal.Tests/ProtonCompletionServiceTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonCompletionServiceTests.cs (fixture dari Plan 01)
    - 358-PATTERNS.md §8 (assert detail)
    - 358-RESEARCH.md §Validation Architecture L319-324 (apa yang di-assert, Nyquist)
  </read_first>
  <behavior>
    - EnsureAsync idempotent: seed assignment aktif → call ke-1 true, call ke-2 false, hanya 1 penanda.
    - EnsureAsync no-assignment: tanpa assignment aktif → false + 0 penanda.
    - RemoveExamOriginAsync selektif: 2 penanda (Origin="Exam" & "Bypass") di 2 assignment beda → Remove pada assignment Exam → penanda Exam hilang, Bypass tetap.
    - GetPassedYearsAsync: penanda Tahun 1 (TrackType "Operator") → Contains "Tahun 1" untuk trackType cocok; Empty untuk TrackType beda.
  </behavior>
  <action>
    Tambah [Fact] di `ProtonCompletionServiceTests` (gunakan fixture `_fixture.Options`). Setiap fact: buat `ApplicationDbContext`, seed `ProtonTrack` + `ProtonTrackAssignment` (set IsActive sesuai kasus), instansiasi `new ProtonCompletionService(ctx, NullLogger<ProtonCompletionService>.Instance)`, lalu assert:
    1. `EnsureAsync_Idempotent`: seed assignment aktif. `Assert.True(await svc.EnsureAsync(coacheeId, trackId, "hc", "Exam", "t"))`; `Assert.False(await svc.EnsureAsync(...))`; `Assert.Single(ctx.ProtonFinalAssessments.Where(fa => fa.ProtonTrackAssignmentId == assignment.Id))`.
    2. `EnsureAsync_NoAssignment_ReturnsFalse`: tanpa assignment → `Assert.False(...)` + `Assert.Empty(ctx.ProtonFinalAssessments)`.
    3. `RemoveExamOrigin_SelektifExamOnly`: seed 2 assignment (A1, A2) + 2 penanda (A1 Origin="Exam", A2 Origin="Bypass") via direct Add. Call `svc.RemoveExamOriginAsync(coacheeId, trackId_A1)`. `Assert.Empty(ctx.ProtonFinalAssessments.Where(fa => fa.Origin == "Exam"))`; `Assert.NotEmpty(ctx.ProtonFinalAssessments.Where(fa => fa.Origin == "Bypass"))`.
    4. `GetPassedYears_MatchTrackType`: seed penanda Tahun 1 TrackType "Operator". `Assert.Contains("Tahun 1", await svc.GetPassedYearsAsync(coacheeId, "Operator"))`; `Assert.Empty(await svc.GetPassedYearsAsync(coacheeId, "Panelman"))`.
    > Gunakan `Microsoft.Extensions.Logging.Abstractions.NullLogger<>`. Pastikan tiap fact pakai coacheeId/trackId unik atau context fresh untuk isolasi (DB shared antar-fact dalam fixture).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `EnsureAsync_Idempotent`, `RemoveExamOrigin` (selektif fact), `GetPassedYears` fact
    - File contains `Assert.NotEmpty` pada query `Origin == "Bypass"` (kebal)
    - `dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests"` exit 0 (semua [Fact] green, SQLEXPRESS)
  </acceptance_criteria>
  <done>4 [Fact] green membuktikan PCOMP-03 (idempotent) + PCOMP-02 (Remove selektif) + GetPassedYears match-trackType.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| caller (GradingService/Controller) → service | input coacheeId/trackId/origin dari kode internal terpercaya; tidak ada input user langsung |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-03 | Tampering | RemoveExamOriginAsync menghapus penanda salah-origin | mitigate | Filter `Origin == "Exam"` verbatim; [Fact] selektif membuktikan Bypass/Interview kebal (A-M9). |
| T-358-04 | Elevation | EnsureAsync membuat penanda tanpa assignment valid | mitigate | Resolve assignment aktif dulu; null → return false, 0 penanda ([Fact] no-assignment). |
</threat_model>

<verification>
- `dotnet build` 0 error
- `dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests"` green (4 [Fact] + smoke)
- Grep: `AddScoped<HcPortal.Services.ProtonCompletionService>` di Program.cs
</verification>

<success_criteria>
- Service single-source 3-method ada + ter-register (PCOMP-03)
- EnsureAsync idempotent; RemoveExamOriginAsync selektif Exam-only (PCOMP-02)
- GetPassedYears tanpa gate enforcement (D-02 honored)
</success_criteria>

<output>
After completion, create `.planning/phases/358-penanda-kelulusan-fondasi-a/358-02-SUMMARY.md`
</output>
