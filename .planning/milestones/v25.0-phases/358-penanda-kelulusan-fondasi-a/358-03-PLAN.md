---
phase: 358-penanda-kelulusan-fondasi-a
plan: 03
type: execute
wave: 3
depends_on: ["358-02"]
files_modified:
  - Services/GradingService.cs
requirements: [PCOMP-01, PCOMP-02]
autonomous: false
must_haves:
  truths:
    - "Exam Proton Tahun 1/2 yang lulus otomatis menerbitkan penanda Origin='Exam' via GradingService"
    - "Re-grade Fail→Pass menerbitkan penanda; Pass→Fail menghapus penanda Origin='Exam'"
    - "Hook hanya jalan untuk Category='Assessment Proton' && IsPassed && ProtonTrackId.HasValue"
  artifacts:
    - path: "Services/GradingService.cs"
      provides: "Inject ProtonCompletionService + 2 hook (completion + 2 re-grade flip)"
      contains: "_protonCompletionService"
  key_links:
    - from: "Services/GradingService.cs GradeAndCompleteAsync"
      to: "ProtonCompletionService.EnsureAsync"
      via: "hook setelah notif completion, guard D-05"
      pattern: "EnsureAsync\\(.*\"Exam\""
    - from: "Services/GradingService.cs RegradeAfterEditAsync"
      to: "ProtonCompletionService.RemoveExamOriginAsync / EnsureAsync"
      via: "cabang flip Pass→Fail / Fail→Pass, guard D-05"
      pattern: "RemoveExamOriginAsync"
---

<objective>
Wire `ProtonCompletionService` ke `GradingService`: inject service + 3 hook. (1) `GradeAndCompleteAsync` non-essay completion → `EnsureAsync(Origin="Exam")`. (2) `RegradeAfterEditAsync` Fail→Pass (L471) → `EnsureAsync(Origin="Exam")`. (3) Pass→Fail (L458) → `RemoveExamOriginAsync`. Semua di-guard D-05.

Purpose: PCOMP-01 (exam lulus → penanda) + PCOMP-02 (re-grade flip). Inilah fix bug "Tahun 1/2 gak pernah Lulus" di jalur exam.
Output: GradingService ber-hook, build green, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-CONTEXT.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-RESEARCH.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md

<interfaces>
<!-- ProtonCompletionService API (dari Plan 02) -->
Task<bool> EnsureAsync(string coacheeId, int protonTrackId, string createdById, string origin, string? notes);
Task<bool> RemoveExamOriginAsync(string coacheeId, int protonTrackId);
Task<List<string>> GetPassedYearsAsync(string coacheeId, string trackType);

<!-- GradingService ctor saat ini (VERIFIED Services/GradingService.cs:16-30) -->
private readonly ApplicationDbContext _context;
private readonly IWorkerDataService _workerDataService;
private readonly ILogger<GradingService> _logger;
public GradingService(ApplicationDbContext context, IWorkerDataService workerDataService, ILogger<GradingService> logger) { ... }

<!-- AssessmentSession field yang dibaca hook: session.UserId (CoacheeId), session.Category, session.IsPassed, session.ProtonTrackId (int?) -->
<!-- VERIFIED titik hook completion: GradeAndCompleteAsync non-essay tail L296-299 (setelah NotifyIfGroupCompleted, sebelum return true). -->
<!-- VERIFIED re-grade flips: RegradeAfterEditAsync L457-513 (wasPassed && !isPassed = L458; !wasPassed && isPassed = L471). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject ProtonCompletionService + hook completion (GradeAndCompleteAsync)</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs:16-30 (ctor — tambah param/field)
    - Services/GradingService.cs:189-227 (cabang hasEssay early-return — JANGAN sentuh; ini sebab defensive hook ada di Plan 04 FinalizeEssayGrading)
    - Services/GradingService.cs:296-299 (titik hook non-essay: setelah NotifyIfGroupCompleted, sebelum return true)
    - 358-PATTERNS.md §5 (Hook A) + Shared Patterns "Guard D-05"
    - docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md §4.1 + §4.9
  </read_first>
  <action>
    1. Tambah field + ctor param `ProtonCompletionService protonCompletionService` (mirror `_workerDataService` L19/L24):
       field `private readonly ProtonCompletionService _protonCompletionService;`, assign di ctor `_protonCompletionService = protonCompletionService;`.
    2. Hook A — di `GradeAndCompleteAsync`, SETELAH `await _workerDataService.NotifyIfGroupCompleted(session);` (L297), SEBELUM `return true;` (L299):
       ```
       // PCOMP-01 (D-06): exam Proton lulus → penanda Origin="Exam" (guard D-05)
       if (session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue)
       {
           await _protonCompletionService.EnsureAsync(
               session.UserId, session.ProtonTrackId.Value, session.CreatedBy ?? "",
               "Exam", $"Exam Proton lulus (skor {finalPercentage}%).");
       }
       ```
       > `isPassed` + `finalPercentage` tersedia di scope (L237/L234). Verifikasi nama field actor untuk createdById — gunakan `session.CreatedBy` (VERIFIED AssessmentSession L89) atau "" bila tidak sesuai; JANGAN panggil UserManager di service. Guard D-05 wajib: cabang hasEssay (L190-226) early-return TIDAK lewat hook ini — itu di-cover defensive hook Plan 04.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Services/GradingService.cs` contains `private readonly ProtonCompletionService _protonCompletionService`
    - File contains `session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue`
    - File contains `EnsureAsync(` dengan argumen `"Exam"`
    - Hook berada SETELAH `NotifyIfGroupCompleted` dan SEBELUM `return true` di GradeAndCompleteAsync
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>GradingService inject service; exam Proton non-essay lulus → EnsureAsync Exam; guard D-05 verbatim; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Hook re-grade flip (RegradeAfterEditAsync) Pass→Fail / Fail→Pass</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs:457-513 (dua cabang flip — titik hook)
    - 358-PATTERNS.md §5 (Hook B+C) + Shared Patterns "Origin selektif"
    - docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md §4.1 (A-M1/A-M9)
  </read_first>
  <action>
    Di `RegradeAfterEditAsync`, tambah hook di kedua cabang flip (guard D-05 — `session.Category`/`ProtonTrackId` tersedia di entity session):
    - Cabang `if (wasPassed && !isPassed)` (L458, Pass→Fail) — SETELAH revoke sertifikat (sebelum tutup blok L470):
      ```
      // PCOMP-02 (D-06/A-M9): flip Pass→Fail hapus penanda HANYA Origin="Exam"
      if (session.Category == "Assessment Proton" && session.ProtonTrackId.HasValue)
          await _protonCompletionService.RemoveExamOriginAsync(session.UserId, session.ProtonTrackId.Value);
      ```
      > Catatan guard: di cabang ini `isPassed==false`, jadi guard TIDAK menyertakan `isPassed` (hapus = saat gagal). Hanya Category + ProtonTrackId.
    - Cabang `else if (!wasPassed && isPassed)` (L471, Fail→Pass) — SETELAH cert generate (sebelum tutup blok L512):
      ```
      // PCOMP-01/02 (D-06): flip Fail→Pass terbit penanda Origin="Exam" (guard D-05)
      if (session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue)
          await _protonCompletionService.EnsureAsync(
              session.UserId, session.ProtonTrackId.Value, session.CreatedBy ?? "",
              "Exam", $"Re-grade Fail→Pass (skor {newPct}%).");
      ```
      > `newPct` + `isPassed` tersedia (L432/L431). RemoveExamOriginAsync TIDAK menyentuh Origin Bypass/Interview (A-M9 — terbukti [Fact] Plan 02).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `RemoveExamOriginAsync(session.UserId, session.ProtonTrackId.Value)` di cabang Pass→Fail
    - File contains kedua hook (Remove di L458-branch, EnsureAsync "Exam" di L471-branch)
    - `dotnet build` exit 0
    - `dotnet test` full suite exit 0
  </acceptance_criteria>
  <done>Re-grade flip terhubung: Fail→Pass terbit, Pass→Fail hapus Origin=Exam; build + full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>GradingService menerbitkan/menghapus penanda exam Proton via ProtonCompletionService (completion + 2 re-grade flip).</what-built>
  <how-to-verify>
    1. Jalankan lokal: `Authentication__UseActiveDirectory=false dotnet run` → http://localhost:5277.
    2. Grade exam Proton Tahun 1/2 sampai LULUS (admin@... pwd 123456) → buka dashboard CDP coachee → status harus "Lulus/Completed" (penanda terbit).
    3. (opsional) Re-grade Pass→Fail via edit jawaban → dashboard kembali "In Progress" (penanda Exam dihapus). Re-grade balik Fail→Pass → "Completed" lagi.
    4. SQL cek: `SELECT Origin FROM ProtonFinalAssessments WHERE ...` → Origin='Exam' untuk exam tsb.
    5. Pastikan exam non-Proton/Pre-Test TIDAK terbit penanda (guard D-05).
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan masalah (mis. dashboard tetap In Progress untuk Tahun 1/2 lulus).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| grading flow → penanda service | session sudah ter-grade (Completed) sebelum hook; tidak ada input user baru |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-05 | Tampering | hook terbit penanda untuk exam non-Proton | mitigate | Guard D-05 verbatim (`Category=="Assessment Proton" && isPassed && ProtonTrackId.HasValue`); UAT cek non-Proton tidak terbit. |
| T-358-06 | Repudiation | re-grade hapus penanda tanpa jejak | accept | Re-grade sudah ber-audit di controller pemanggil (Phase 296/324); penanda Origin selektif mencegah hapus salah jalur. |
</threat_model>

<verification>
- `dotnet build` 0 error
- `dotnet test` full suite green
- UAT @5277: exam Proton Tahun 1/2 lulus → dashboard "Lulus"; re-grade flip konsisten; non-Proton tidak terbit
</verification>

<success_criteria>
- Exam Proton Tahun 1/2 lulus → penanda Origin='Exam' (PCOMP-01)
- Re-grade Fail→Pass terbit; Pass→Fail hapus Exam-only (PCOMP-02)
- Guard D-05 dihormati (tidak kepicu non-Proton)
</success_criteria>

<output>
After completion, create `.planning/phases/358-penanda-kelulusan-fondasi-a/358-03-SUMMARY.md`
</output>
