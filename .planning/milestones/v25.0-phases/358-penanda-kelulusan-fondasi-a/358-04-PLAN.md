---
phase: 358-penanda-kelulusan-fondasi-a
plan: 04
type: execute
wave: 3
depends_on: ["358-02"]
files_modified:
  - Controllers/AssessmentAdminController.cs
requirements: [PCOMP-03, PCOMP-05]
autonomous: false
must_haves:
  truths:
    - "SubmitInterviewResults menerbitkan penanda Origin='Interview' via helper (perilaku lama tak berubah)"
    - "FinalizeEssayGrading punya defensive hook penanda Origin='Exam' (Proton + IsPassed) — D-05a"
    - "Endpoint backfill admin-only 1x idempotent menerbitkan penanda Exam untuk exam Tahun 1/2 lama yang lulus + deliverable 100%"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Inject service + interview refactor + essay defensive hook + BackfillProtonPenanda"
      contains: "BackfillProtonPenanda"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs SubmitInterviewResults"
      to: "ProtonCompletionService.EnsureAsync"
      via: "refactor inline-create → helper Origin='Interview'"
      pattern: "EnsureAsync\\(.*\"Interview\""
    - from: "Controllers/AssessmentAdminController.cs BackfillProtonPenanda"
      to: "ProtonFinalAssessments"
      via: "resolve assignment (inactive/>1) + enforce 100% + idempotent"
      pattern: "BackfillProtonPenanda"
---

<objective>
Tiga perubahan di `AssessmentAdminController`: (1) inject `ProtonCompletionService`; (2) refactor `SubmitInterviewResults` (L3737-3766) inline-create → `EnsureAsync(Origin="Interview")` (jaga urutan SaveChanges); (3) defensive hook D-05a di `FinalizeEssayGrading` (~L3604) Proton+IsPassed → `EnsureAsync(Origin="Exam")`; (4) endpoint backfill admin-only POST 1x idempotent — enforce deliverable 100% + resolve assignment A-M10 (inactive/>1, AssignedAt terdekat sebelum exam.CompletedAt).

Purpose: PCOMP-03 (single-source untuk interview + essay) + PCOMP-05 (backfill exam Tahun 1/2 lama).
Output: controller ber-hook + endpoint backfill, build green, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-CONTEXT.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-RESEARCH.md
@.planning/phases/358-penanda-kelulusan-fondasi-a/358-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md

<interfaces>
<!-- ProtonCompletionService API (Plan 02) -->
Task<bool> EnsureAsync(string coacheeId, int protonTrackId, string createdById, string origin, string? notes);

<!-- AssessmentAdminController ctor (VERIFIED L31-50): inject ApplicationDbContext context, UserManager, AuditLogService auditLog (base AdminBaseController simpan _context/_userManager/_auditLog), IWebHostEnvironment env, IMemoryCache cache, ILogger logger, INotificationService, IHubContext<AssessmentHub>, IWorkerDataService workerDataService, GradingService gradingService. TAMBAH: ProtonCompletionService protonCompletionService. -->

<!-- SubmitInterviewResults inline-create (VERIFIED L3737-3766): block diganti EnsureAsync. session.SaveChangesAsync di L3768 jaga. actorForFix = await _userManager.GetUserAsync(User) (L3739). -->
<!-- FinalizeEssayGrading (VERIFIED L3561-3617): isPassed=L3562, session var di scope, cert generate L3604. Tambah hook setelah cert/IsPassed di-set. -->
<!-- ResetAssessment admin POST skeleton (VERIFIED L3793+): [HttpPost][Authorize(Roles="Admin, HC")][ValidateAntiForgeryToken]. -->

<!-- ProtonTrackAssignment: NO CompletedAt. Backfill pakai exam.CompletedAt (AssessmentSession.CompletedAt L39). -->
<!-- ProtonDeliverableProgress (VERIFIED Models/ProtonModels.cs:89-99): CoacheeId, ProtonTrackAssignmentId, Status ("Pending"/"Submitted"/"Approved"/"Rejected"). -->
<!-- AssessmentSession Proton: Category=="Assessment Proton", ProtonTrackId (int?), TahunKe ("Tahun 1".."Tahun 3"), IsPassed, CompletedAt, CreatedBy. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject service + refactor SubmitInterviewResults + defensive essay hook</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:28-50 (ctor — tambah DI)
    - Controllers/AssessmentAdminController.cs:3723-3768 (SubmitInterviewResults inline-create + SaveChanges L3768)
    - Controllers/AssessmentAdminController.cs:3561-3617 (FinalizeEssayGrading: isPassed L3562, cert L3604)
    - 358-PATTERNS.md §6 + Shared Patterns "SaveChanges order" + "Guard D-05"
    - 358-RESEARCH.md Pitfall 1 (essay bypass) + Pitfall 2 (SaveChanges order)
  </read_first>
  <action>
    1. **Inject:** tambah ctor param `ProtonCompletionService protonCompletionService` + field `private readonly ProtonCompletionService _protonCompletionService;` + assign (mirror `_gradingService` L29/L41/L49).

    2. **Refactor SubmitInterviewResults (L3737-3766):** GANTI seluruh blok inline-create (`if (isPassed && session.ProtonTrackId.HasValue) { ... ProtonFinalAssessments.Add(...) ... }`) dengan:
       ```
       // PCOMP-03 (D-07): refactor inline-create → single-source helper
       if (isPassed && session.ProtonTrackId.HasValue)
       {
           await _protonCompletionService.EnsureAsync(
               session.UserId, session.ProtonTrackId.Value, actorForFix?.Id ?? "",
               "Interview", $"Interview Tahun 3 lulus. Assessor: {dto.Judges}");
       }
       ```
       **JAGA urutan SaveChanges (Pitfall 2):** `session.InterviewResultsJson`/`IsPassed`/`Status` di-set di memory L3732-3735. `EnsureAsync` share `_context` scoped yang sama (punya SaveChanges internal) → tracked session changes ikut ter-flush. Biarkan `await _context.SaveChangesAsync();` L3768 tetap ada (menyimpan session bila EnsureAsync skip/return false). Perilaku lama TIDAK berubah: interview lulus → JSON tersimpan + penanda Origin="Interview" ada (tidak dobel).

    3. **Defensive hook D-05a di FinalizeEssayGrading (~L3604):** SETELAH cert generate (sekitar L3617, setelah blok `if (session.GenerateCertificate && isPassed)`), TAMBAH:
       ```
       // PCOMP-01 D-05a (defensive): Proton essay lulus → penanda Origin="Exam".
       // Praktis idle (Proton tak ada essay di data sekarang) tapi nutup celah hasEssay early-return
       // di GradingService.GradeAndCompleteAsync L226 yang lewati hook penanda (RESEARCH Pitfall 1).
       if (session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue)
       {
           await _protonCompletionService.EnsureAsync(
               session.UserId, session.ProtonTrackId.Value, currentUser?.Id ?? "",
               "Exam", $"Essay Proton finalisasi lulus (skor {finalPercentage}%).");
       }
       ```
       > `session`, `isPassed` (L3562), `finalPercentage` (L3561), `currentUser` (L3620) tersedia. Letakkan SETELAH `currentUser` di-load (L3620) bila perlu, atau load actor lokal. Guard D-05 verbatim.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - File contains `private readonly ProtonCompletionService _protonCompletionService` + assign di ctor
    - SubmitInterviewResults contains `EnsureAsync(` dengan `"Interview"` dan TIDAK lagi `ProtonFinalAssessments.Add(` di blok interview
    - FinalizeEssayGrading contains `session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue` + `EnsureAsync(` `"Exam"`
    - `await _context.SaveChangesAsync()` L3768 (session) masih ada
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>Service ter-inject; interview pakai helper Origin="Interview"; essay defensive hook D-05a ada; SaveChanges order terjaga; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint backfill admin-only POST idempotent (enforce 100% + A-M10)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:3793+ (ResetAssessment skeleton admin POST)
    - Controllers/AssessmentAdminController.cs:3770-3784 (audit + TempData pattern)
    - 358-PATTERNS.md §7 (backfill logic BEDA dari EnsureAsync) + §Audit
    - 358-RESEARCH.md Pitfall 3 (A-M10 resolve ≠ EnsureAsync) + Pitfall 4 (no CompletedAt di assignment)
    - docs/superpowers/specs/2026-06-09-proton-completion-logic-design.md §4.7 (D-08 ENFORCE 100%)
    - docs/SEED_WORKFLOW.md (snapshot sebelum jalan backfill)
  </read_first>
  <action>
    Tambah action di `AssessmentAdminController`:
    ```
    [HttpPost]
    [Authorize(Roles = "Admin")]
    [ValidateAntiForgeryToken]
    public async Task<IActionResult> BackfillProtonPenanda()
    ```
    Logic (idempotent, ENFORCE 100% — D-08, BUKAN opsional):
    1. Query semua `AssessmentSession` `Category == "Assessment Proton"` && `IsPassed == true` && `ProtonTrackId.HasValue` && (`TahunKe == "Tahun 1" || TahunKe == "Tahun 2"`) && `CompletedAt != null`.
    2. Untuk tiap exam, **resolve assignment SENDIRI (BUKAN EnsureAsync — Pitfall 3)** tanpa filter IsActive (bisa inactive & >1):
       ```
       var assignment = await _context.ProtonTrackAssignments
           .Where(a => a.CoacheeId == exam.UserId && a.ProtonTrackId == exam.ProtonTrackId.Value
                    && a.AssignedAt <= exam.CompletedAt.Value)
           .OrderByDescending(a => a.AssignedAt)
           .FirstOrDefaultAsync();
       if (assignment == null) { skipped++; _logger.LogInformation("Backfill skip exam {Id}: no assignment match", exam.Id); continue; }
       ```
       ⚠️ Pakai `exam.CompletedAt` (AssessmentSession), BUKAN `assignment.CompletedAt` (tidak ada — Pitfall 4).
    3. **Idempotent:** `if (await _context.ProtonFinalAssessments.AnyAsync(fa => fa.ProtonTrackAssignmentId == assignment.Id)) { alreadyExists++; continue; }`.
    4. **ENFORCE deliverable 100% (D-08):** ambil semua `ProtonDeliverableProgress` untuk `assignment.Id`:
       ```
       var statuses = await _context.ProtonDeliverableProgresses
           .Where(p => p.ProtonTrackAssignmentId == assignment.Id)
           .Select(p => p.Status).ToListAsync();
       if (statuses.Count == 0 || !statuses.All(s => s == "Approved")) { notEligible++; continue; }
       ```
       (Setara IsEligiblePerUnit assignment-scoped: count>0 + semua Approved.)
    5. Create penanda manual: `_context.ProtonFinalAssessments.Add(new ProtonFinalAssessment { CoacheeId=exam.UserId, CreatedById=<actorId>, ProtonTrackAssignmentId=assignment.Id, Status="Completed", CompetencyLevelGranted=0, Origin="Exam", Notes=$"Backfill exam Tahun {exam.TahunKe} lulus.", CreatedAt=DateTime.UtcNow, CompletedAt=exam.CompletedAt });` → `created++`.
    6. `await _context.SaveChangesAsync()` sekali di akhir (atau per-batch).
    7. **Audit** (pattern L3770-3784): `var user = await _userManager.GetUserAsync(User);` actorName=`NIP - FullName`; `await _auditLog.LogAsync(user.Id, actorName, "BackfillProtonPenanda", $"Backfill: {created} dibuat, {alreadyExists} sudah ada, {notEligible} belum 100%, {skipped} tanpa assignment", 0, "ProtonFinalAssessment");` (try/catch warn-only — audit gagal jangan break).
    8. `TempData["Success"] = $"Backfill selesai: {created} penanda dibuat, {alreadyExists} dilewati, {notEligible} belum 100%, {skipped} tanpa assignment.";` lalu `RedirectToAction(...)` ke halaman admin yang sesuai (mis. ManageAssessment).
    > Error handling: bungkus try/catch; pesan user-facing JANGAN sertakan `ex.Message` (info-leak — Phase 334 D6). Log detail ke `_logger`, tampilkan pesan generik ke TempData.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonCompletionServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `[Authorize(Roles = "Admin")]` + `[ValidateAntiForgeryToken]` di atas `BackfillProtonPenanda`
    - File contains `a.AssignedAt <= exam.CompletedAt.Value` (resolve A-M10) dan `OrderByDescending(a => a.AssignedAt)`
    - File contains `statuses.All(s => s == "Approved")` (enforce 100%) dan `AnyAsync(fa => fa.ProtonTrackAssignmentId == assignment.Id)` (idempotent)
    - File contains `Origin = "Exam"` di penanda backfill
    - TIDAK ada `ex.Message` di TempData/Json user-facing (no info-leak)
    - `dotnet build` exit 0 dan `dotnet test` full suite exit 0
  </acceptance_criteria>
  <done>Endpoint backfill admin-only idempotent: enforce 100%, resolve assignment inactive/>1 by AssignedAt≤exam.CompletedAt, log skipped; build + full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interview refactor + essay defensive hook + endpoint backfill `POST /Admin/BackfillProtonPenanda` (admin-only, idempotent, enforce 100%).</what-built>
  <how-to-verify>
    1. **Snapshot DB lokal SEBELUM jalan backfill** (SEED_WORKFLOW): `sqlcmd ... BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev_pre358backfill.bak'`. Catat `docs/SEED_JOURNAL.md`.
    2. Jalankan `Authentication__UseActiveDirectory=false dotnet run` → http://localhost:5277, login admin (pwd 123456).
    3. Interview: SubmitInterviewResults Tahun 3 lulus → SQL cek penanda Origin="Interview" + InterviewResultsJson tersimpan (tidak dobel).
    4. Backfill: trigger `POST /Admin/BackfillProtonPenanda` (via form/tombol admin atau curl dengan antiforgery) → TempData report jumlah; SQL cek exam Tahun 1/2 lama lulus+100% → penanda Origin="Exam" terbit; dashboard worker lama → "Lulus".
    5. **Idempotency:** trigger backfill 2x → run kedua `created=0` (semua alreadyExists). Tidak ada duplikat.
    6. Cek exam belum-100% deliverable TIDAK dapat penanda (notEligible++).
    7. **Restore DB** bila perlu setelah test; tandai journal `cleaned`.
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan masalah (mis. backfill duplikat di run kedua, atau penanda terbit walau deliverable belum 100%).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → POST /Admin/BackfillProtonPenanda | endpoint maintenance admin-only; trigger batch mutasi data |
| controller → DB | mutasi penanda massal (idempotent) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-07 | Elevation/IDOR | BackfillProtonPenanda diakses non-admin | mitigate | `[Authorize(Roles = "Admin")]` (lebih ketat dari "Admin, HC" untuk operasi maintenance 1x). |
| T-358-08 | Tampering/CSRF | POST backfill dipicu cross-site | mitigate | `[ValidateAntiForgeryToken]` wajib pada action POST. |
| T-358-09 | Information Disclosure | error backfill bocorkan detail DB ke user | mitigate | TempData/Json pesan generik; `ex.Message` HANYA ke `_logger` (Phase 334 D6 lesson). |
| T-358-10 | Tampering | backfill terbit penanda tanpa deliverable 100% | mitigate | Enforce `statuses.All(s=="Approved") && count>0` (D-08); UAT cek belum-100% tidak terbit. |
</threat_model>

<verification>
- `dotnet build` 0 error; `dotnet test` full suite green
- Grep: `BackfillProtonPenanda` + `[Authorize(Roles = "Admin")]` + `[ValidateAntiForgeryToken]`; no `ex.Message` user-facing
- UAT @5277: interview Origin="Interview"; backfill idempotent + enforce 100%; dashboard worker lama "Lulus"
- Snapshot DB sebelum backfill (SEED_WORKFLOW)
</verification>

<success_criteria>
- SubmitInterviewResults pakai helper Origin="Interview", perilaku lama utuh (PCOMP-03)
- FinalizeEssayGrading defensive hook D-05a ada (PCOMP-01 nutup celah essay)
- Backfill admin-only idempotent, enforce 100%, resolve A-M10 (PCOMP-05)
</success_criteria>

<output>
After completion, create `.planning/phases/358-penanda-kelulusan-fondasi-a/358-04-SUMMARY.md`
</output>
