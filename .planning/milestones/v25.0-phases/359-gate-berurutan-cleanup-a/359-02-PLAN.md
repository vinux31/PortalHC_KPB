---
phase: 359-gate-berurutan-cleanup-a
plan: 02
type: execute
wave: 2
depends_on: ["359-01"]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PCOMP-06, PCOMP-07, PCOMP-08]
must_haves:
  truths:
    - "POST CreateAssessment untuk Category=='Assessment Proton' menolak worker yang deliverable-nya belum 100% (server-side), bukan hanya filter JS."
    - "POST CreateAssessment menolak worker yang Tahun N-1 (TrackType sama) belum lulus (penanda)."
    - "Worker tak-eligible di-SKIP; worker eligible TETAP dapat session (BUKAN all-or-nothing); ringkasan skip ditampilkan via TempData."
    - "Tahun 3 tanpa deliverable (0 deliverable) tetap eligible (fallback transisi); begitu silabus diisi gate 100% otomatis berlaku."
    - "Jalur renewal exam Proton (RenewsSessionId) tetap melewati gate eligibility server-side yang sama — tidak ada pintu samping."
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Server-side eligibility + cross-year gate di POST CreateAssessment loop (Assessment Proton)"
      contains: "IsEligiblePerUnit"
  key_links:
    - from: "AssessmentAdminController.CreateAssessment (loop :1335)"
      to: "CoacheeEligibilityCalculator.IsEligiblePerUnit"
      via: "per-unit 100% check sebelum bikin session"
      pattern: "IsEligiblePerUnit"
    - from: "AssessmentAdminController.CreateAssessment"
      to: "ProtonYearGate / IsPrevYearPassedAsync"
      via: "cross-year gate per userId"
      pattern: "IsPrevYearPassedAsync|GetPassedYearsAsync"
---

<objective>
Paksa eligibility Proton di SERVER pada POST CreateAssessment (PCOMP-06): tiap UserId di-validate sebelum bikin session — deliverable 100% per-unit (D-02) DAN Tahun N-1 lulus (D-03). Worker tak-eligible di-SKIP & dihitung; worker eligible tetap dapat session (D-01, bukan all-or-nothing). Ringkasan skip via TempData (S1). Tahun 3 data-driven (PCOMP-08/D-08). Renewal tetap lewat gate (D-07).

Purpose: Worker tidak bisa nyelip via request manual yang skip filter JS (A-M2). Menutup pintu CreateAssessment yang setara dengan gate assign di Plan 03.

Output: Gate server-side di branch `Category=="Assessment Proton"` di loop session standard. TANPA migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-CONTEXT.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang dipakai — pakai langsung, jangan eksplorasi. -->

Dari Plan 359-01 (Services/ProtonCompletionService.cs):
```csharp
public static class ProtonYearGate { public static bool IsAllowed(string? prevTahunKe, IEnumerable<string>? passedYears); }
public async Task<bool> IsPrevYearPassedAsync(string coacheeId, string trackType, string? prevTahunKe);
public async Task<List<string>> GetPassedYearsAsync(string coacheeId, string trackType);
```

Dari Helpers/CoacheeEligibilityCalculator.cs:14 (reuse as-is, no edit):
```csharp
// expectedCount<=0 → false; jumlah mismatch → false; else semua status == "Approved".
public static bool IsEligiblePerUnit(IEnumerable<string> myProgressStatuses, int expectedCount);
```

DI existing di AssessmentAdminController.cs (sudah ada, JANGAN tambah lagi):
```csharp
// _protonCompletionService dipakai di :3649 & :3758; _context; _auditLog; _logger; _userManager.
private readonly ProtonCompletionService _protonCompletionService; // :30, :43
```

Existing di file (POINT HOOK):
- `protonTrack`/`protonTahunKe` resolved di :1187 (protonTahunKe = protonTrack.TahunKe).
- Loop session standard :1335-1373; branch Proton :1363-1370 (set ProtonTrackId, TahunKe, Tahun 3 → Duration=0).
- Transaction :1379 (BeginTransactionAsync + retry 3x UNIQUE).
- Validasi early-return convention :1167-1186 (TempData["Error"] + rebuild ViewBag + return View).
</interfaces>

<analog>
<!-- Per-unit 100% gate — COPY dari CoachMappingController.GetEligibleCoachees:1382-1422 (sudah diverifikasi planner): -->
```csharp
// resolve unit per-coachee (MIRROR AutoCreateProgressForAssignment, .Trim() dua sisi)
var assignmentUnit = await _context.CoachCoacheeMappings
    .Where(m => m.CoacheeId == coacheeId && m.IsActive).Select(m => m.AssignmentUnit).FirstOrDefaultAsync();
var resolvedUnit = assignmentUnit;
if (string.IsNullOrWhiteSpace(resolvedUnit))
    resolvedUnit = await _context.Users.Where(u => u.Id == coacheeId).Select(u => u.Unit).FirstOrDefaultAsync();
if (string.IsNullOrWhiteSpace(resolvedUnit)) { /* skip: unit tak terselesaikan → tidak eligible */ }
var unitDeliverableIds = await _context.ProtonDeliverableList
    .Where(d => d.ProtonSubKompetensi!.ProtonKompetensi!.ProtonTrackId == protonTrackId
             && d.ProtonSubKompetensi!.ProtonKompetensi!.Unit!.Trim() == resolvedUnit.Trim())
    .Select(d => d.Id).ToListAsync();
var expectedCount = unitDeliverableIds.Count;
var myStatuses = (await _context.ProtonDeliverableProgresses
    .Where(p => p.CoacheeId == coacheeId && unitDeliverableIds.Contains(p.ProtonDeliverableId))
    .Select(p => p.Status).ToListAsync());
bool eligible = CoacheeEligibilityCalculator.IsEligiblePerUnit(myStatuses, expectedCount);
```
<!-- D-08 fallback (Tahun 3 / track 0 deliverable → eligible) — MIRROR GetEligibleCoachees:1364-1373: -->
```csharp
// trackDeliverableIds = semua deliverable track. Jika kosong → semua assigned eligible (interview-only).
if (!trackDeliverableIds.Any()) { /* eligible = true; lewati cek 100% */ }
```
<!-- Skip-with-summary counters — COPY pola dari BackfillProtonPenanda:3799 + :3879 (same file): -->
```csharp
int created = 0, skipped = 0, notHundred = 0, prevYearNotPassed = 0;
// ... continue + increment counter per skip-reason ...
```
</analog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-pass eligibility gate (filter UserIds eligible) di branch Assessment Proton</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baca rentang :1160-1400 — resolve protonTrack:1187, loop:1335, branch Proton:1363, transaction:1379; DAN :3791-3889 BackfillProtonPenanda untuk pola counter+summary; DAN :3640-3660/:3750-3780 contoh pemakaian _protonCompletionService)
    - Helpers/CoacheeEligibilityCalculator.cs (IsEligiblePerUnit:14 — reuse)
    - Controllers/CoachMappingController.cs:1382-1422 (analog per-unit resolve)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (bagian AssessmentAdminController gate + Shared Patterns)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md (Copywriting Contract S1/S2)
  </read_first>
  <action>
SCOPE: HANYA saat `model.Category == "Assessment Proton"` DAN bukan PrePost mode (gate jalan di flow standard, sebelum loop :1335 membangun sessions). Tahun 1 (protonTahunKe == "Tahun 1") tetap kena gate deliverable 100% TAPI tidak kena cross-year (prevTahunKe = null → ProtonYearGate.IsAllowed = true).

Tambahkan PRE-PASS sebelum `var sessions = new List<AssessmentSession>();` (:1333), HANYA di branch Proton. Bangun `eligibleUserIds` + counters:

```csharp
// Phase 359 (PCOMP-06/07/08) — gate eligibility server-side untuk Assessment Proton.
// BUKAN all-or-nothing (D-01): worker tak-eligible di-SKIP, eligible tetap dapat session.
var eligibleUserIds = new List<string>(UserIds);
int gateSkippedNotHundred = 0, gateSkippedPrevYear = 0;
if (model.Category == "Assessment Proton" && model.ProtonTrackId.HasValue && model.ProtonTrackId.Value > 0)
{
    int protonTrackId = model.ProtonTrackId.Value;
    // Resolve trackType + prevTahunKe (untuk cross-year). protonTrack sudah di-resolve di :1187.
    string trackType = protonTrack.TrackType;
    int urutan = protonTrack.Urutan;
    // prevTahunKe = TahunKe track dgn TrackType sama & Urutan-1; null jika Urutan<=1 (Tahun 1).
    string? prevTahunKe = urutan > 1
        ? await _context.ProtonTracks
            .Where(t => t.TrackType == trackType && t.Urutan == urutan - 1)
            .Select(t => t.TahunKe).FirstOrDefaultAsync()
        : null;

    // Deliverable track (D-08 fallback): jika kosong → skip cek 100% (interview-only/transisi).
    var trackDeliverableIds = await _context.ProtonKompetensiList
        .Where(k => k.ProtonTrackId == protonTrackId)
        .SelectMany(k => k.SubKompetensiList).SelectMany(s => s.Deliverables)
        .Select(d => d.Id).ToListAsync();
    bool trackHasDeliverables = trackDeliverableIds.Any();

    var filtered = new List<string>();
    foreach (var uid in UserIds)
    {
        // (a) Cross-year gate (D-03/D-07): renewal exempt? renewal Tahun N tetap WAJIB lewat gate eligibility,
        //     TAPI cross-year prereq tidak diblok untuk renewal (renewal = perpanjangan tahun yang SUDAH dilewati).
        bool isRenewal = model.RenewsSessionId.HasValue || model.RenewsTrainingId.HasValue;
        if (!isRenewal && !await _protonCompletionService.IsPrevYearPassedAsync(uid, trackType, prevTahunKe))
        {
            gateSkippedPrevYear++; continue;
        }
        // (b) Deliverable 100% per-unit (D-02). Fallback: track 0 deliverable → eligible (D-08).
        if (trackHasDeliverables)
        {
            var resolvedUnit = await _context.CoachCoacheeMappings
                .Where(m => m.CoacheeId == uid && m.IsActive).Select(m => m.AssignmentUnit).FirstOrDefaultAsync();
            if (string.IsNullOrWhiteSpace(resolvedUnit))
                resolvedUnit = await _context.Users.Where(u => u.Id == uid).Select(u => u.Unit).FirstOrDefaultAsync();
            if (string.IsNullOrWhiteSpace(resolvedUnit)) { gateSkippedNotHundred++; continue; }
            var unitDeliverableIds = await _context.ProtonDeliverableList
                .Where(d => d.ProtonSubKompetensi!.ProtonKompetensi!.ProtonTrackId == protonTrackId
                         && d.ProtonSubKompetensi!.ProtonKompetensi!.Unit!.Trim() == resolvedUnit.Trim())
                .Select(d => d.Id).ToListAsync();
            var myStatuses = await _context.ProtonDeliverableProgresses
                .Where(p => p.CoacheeId == uid && unitDeliverableIds.Contains(p.ProtonDeliverableId))
                .Select(p => p.Status).ToListAsync();
            if (!CoacheeEligibilityCalculator.IsEligiblePerUnit(myStatuses, unitDeliverableIds.Count))
            { gateSkippedNotHundred++; continue; }
        }
        filtered.Add(uid);
    }
    eligibleUserIds = filtered;
}
```

PENTING: ubah loop pembangun sessions (:1335) agar iterasi `eligibleUserIds` (BUKAN `UserIds`). Pastikan referensi `UserIds[i]` / `userDictionary[...]` di dalam loop standard tetap konsisten — gunakan `eligibleUserIds` sebagai sumber iterasi. Pre-pass HANYA mengubah flow standard Proton; PrePost mode (isPrePostMode) TIDAK disentuh (Proton bukan PrePost).

Pastikan `using HcPortal.Helpers;` ada (untuk CoacheeEligibilityCalculator) — tambah bila build error.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `IsEligiblePerUnit` di Controllers/AssessmentAdminController.cs → minimal 1 match baru di area CreateAssessment.
    - grep `IsPrevYearPassedAsync` di Controllers/AssessmentAdminController.cs → minimal 1 match.
    - grep `eligibleUserIds` → muncul; loop session standard iterasi eligibleUserIds (verifikasi via baca file).
    - grep `trackHasDeliverables` → ada (D-08 fallback hadir).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Pre-pass membangun eligibleUserIds (deliverable 100% + cross-year + D-08 fallback + renewal exempt cross-year); loop session iterasi eligibleUserIds; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Skip-summary TempData (S1) + audit warn-only + empty-result guard</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (rentang :1375-1420 area setelah loop + commit; DAN BackfillProtonPenanda :3865-3889 untuk summary string + audit warn-only pattern; DAN validasi early-return :1167-1186)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md (Copywriting Contract — S1 strings verbatim)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (Shared Patterns: TempData feedback + Audit log warn-only + No info-leak)
  </read_first>
  <action>
1) GUARD semua-di-skip: jika `model.Category == "Assessment Proton"` dan `eligibleUserIds.Count == 0`, JANGAN buka transaksi/buat session. Set `TempData["Warning"]` dan return ke view (ikuti convention :1167-1186: rebuild ViewBag via `SetCategoriesViewBag()` + `SetTrainingCategoryViewBag()` lalu `return View("CreateAssessment", model);`). Pesan:
   `$"0 session dibuat. Semua {UserIds.Count} pekerja di-skip. Alasan: {gateSkippedNotHundred} belum 100% deliverable, {gateSkippedPrevYear} Tahun sebelumnya belum lulus."`

2) SUMMARY setelah commit sukses (di area `TempData["SuccessMessage"]` flow standard, SETELAH sessions dibuat). Untuk Proton:
   - Jika `gateSkippedNotHundred + gateSkippedPrevYear == 0` → `TempData["Success"] = $"{eligibleUserIds.Count} session berhasil dibuat.";`
   - Jika ada yang di-skip (partial) → pakai `TempData["Warning"]` (S1 partial, harus terlihat walau sukses sebagian):
     `$"{eligibleUserIds.Count} session dibuat, {gateSkippedNotHundred + gateSkippedPrevYear} di-skip. Alasan: {gateSkippedNotHundred} belum 100% deliverable, {gateSkippedPrevYear} Tahun sebelumnya belum lulus."`
   JANGAN gandakan `TempData["SuccessMessage"]` existing untuk non-Proton; tambahkan cabang khusus Proton tanpa menghapus pesan non-Proton.

3) AUDIT warn-only (opsional tapi disarankan, COPY pola :3865-3877): jika ada skip, panggil `_auditLog.LogAsync(...)` dalam inner try/catch (audit gagal TIDAK boleh memutus operasi). actorName: `string.IsNullOrWhiteSpace(user.NIP) ? (user.FullName ?? "Unknown") : $"{user.NIP} - {user.FullName}"`. Catat jumlah created vs skipped + alasan.

4) NO INFO-LEAK: catch apa pun di gate → `_logger.LogError(ex, ...)` penuh, pesan user generik Bahasa Indonesia, JANGAN surface `ex.Message`.

Copy string S1 VERBATIM dari 359-UI-SPEC Copywriting Contract (sesuaikan placeholder {X}/{Y}/{N1}/{N2}). TempData key harus salah satu Success/Warning/Error/Info (dirender _Layout.cshtml:190-226).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `di-skip` di Controllers/AssessmentAdminController.cs → minimal 1 (string ringkasan S1).
    - grep `TempData["Warning"]` di area CreateAssessment → ada (partial skip path).
    - Empty-result guard hadir: ketika eligibleUserIds.Count==0 → return View tanpa BeginTransactionAsync (verifikasi via baca file).
    - grep `_auditLog.LogAsync` di area gate → ada (jika diimplementasikan), dibungkus try/catch.
    - Tidak ada `ex.Message` yang di-assign ke TempData di kode gate baru (grep cek tidak ada `TempData[...] = ... ex.Message`).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Skip-summary S1 dirender (Success all-eligible / Warning partial), empty-result guard menghentikan create, audit warn-only + no info-leak; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /Admin/CreateAssessment | Admin/HC mengirim UserIds + ProtonTrackId. Filter JS bisa di-bypass via request manual → server WAJIB re-validate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-03 | Elevation of Privilege / Broken Access Control | CreateAssessment POST (Proton) | mitigate | Re-validate TIAP UserId server-side (deliverable 100% per-unit + cross-year penanda) sebelum bikin session; worker tak-eligible di-SKIP. Filter JS bukan satu-satunya gate (PCOMP-06). |
| T-359-04 | Tampering (IDOR via UserId) | UserIds list di body | mitigate | Setiap UserId dilewatkan gate yang sama; tidak ada UserId yang bisa nyelip tanpa eligibility. Gate tidak mempercayai input klien. |
| T-359-05 | Elevation via renewal side-door | RenewsSessionId path | mitigate | Renewal tetap melewati gate eligibility 100% (D-07); hanya prereq cross-year yang exempt (renewal = tahun yang sudah dilewati). Tidak ada jalur yang skip cek 100%. |
| T-359-06 | Information Disclosure | catch block gate | mitigate | Log `ex` penuh; pesan user generik Bahasa Indonesia; tidak surface ex.Message (pola Phase 334 D6). |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests` hijau (tidak ada regresi).
- UAT lokal (CLAUDE.md Develop Workflow, AD lokal `Authentication__UseActiveDirectory=false dotnet run`, localhost:5277):
  - Buat Assessment Proton untuk campuran worker eligible + tak-100% → hanya yang eligible dapat session; banner Warning menyebut jumlah di-skip + alasan.
  - Coba Tahun 2 untuk worker yang Tahun 1 belum lulus → di-skip dengan alasan cross-year.
  - Semua tak-eligible → 0 session, banner Warning, tidak ada session terbuat.
- Snapshot/restore DB lokal per SEED_WORKFLOW bila menyuntik fixture eligible/tak-eligible.
</verification>

<success_criteria>
- Gate server-side menolak worker tak-eligible (100% + cross-year) di POST CreateAssessment, bukan hanya JS.
- Partial-create operasional: eligible dapat session, tak-eligible di-skip + ringkasan terlihat.
- Renewal & Tahun 3 (data-driven + fallback) terbukti tetap benar.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/359-gate-berurutan-cleanup-a/359-02-SUMMARY.md`.
</output>
