---
phase: 359-gate-berurutan-cleanup-a
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Models/CDPDashboardViewModel.cs
  - Models/ProtonModels.cs
  - Views/CDP/Shared/_CoacheeDashboardPartial.cshtml
  - Views/CDP/Shared/_CoachingProtonContentPartial.cshtml
  - Views/CDP/HistoriProtonDetail.cshtml
autonomous: true
requirements: [PCOMP-10]
must_haves:
  truths:
    - "Tidak ada angka level kompetensi yang ditampilkan di mana pun (CDP dashboard, tabel coachee, histori) — hanya badge 'Lulus'/'Belum Lulus'/'Selesai'."
    - "Grafik tren competency level dihapus seluruhnya (card + Chart.js init), tanpa meninggalkan placeholder 'no data'."
    - "ViewModel tidak lagi punya field TrendLabels/TrendValues/CompetencyLevelGranted (prune); kolom DB CompetencyLevelGranted tetap dormant (TIDAK di-drop, no migration)."
    - "Status 'Lulus/Selesai' tetap dirender dari CurrentStatus/HasFinalAssessment/node.Status (penanda-presence)."
    - "Semua halaman CDP/HistoriProton render tanpa error."
  artifacts:
    - path: "Models/CDPDashboardViewModel.cs"
      provides: "ViewModel tanpa field level/trend"
      contains: "CurrentStatus"
    - path: "Views/CDP/Shared/_CoachingProtonContentPartial.cshtml"
      provides: "Tabel badge Lulus tanpa angka + tanpa trend chart"
      contains: "Lulus"
  key_links:
    - from: "Views/CDP/Shared/_CoacheeDashboardPartial.cshtml"
      to: "Model.CurrentStatus"
      via: "badge Lulus/Belum Lulus (no number)"
      pattern: "CurrentStatus"
    - from: "Views/CDP/Shared/_CoachingProtonContentPartial.cshtml"
      to: "row.HasFinalAssessment"
      via: "badge Lulus (replaces Level N)"
      pattern: "HasFinalAssessment"
---

<objective>
Matikan SELURUH tampilan `CompetencyLevelGranted` + grafik tren di CDP (PCOMP-10 / A-M12). Pembersihan menyeluruh (D-12): prune binding di controller + field ViewModel + grafik tren + render view. Badge "Lulus/Selesai" tanpa angka menggantikan "Level N". Kolom DB `CompetencyLevelGranted` dibiarkan DORMANT (TIDAK di-drop, no migration).

Purpose: Level kompetensi dibuang (A-3); `ProtonFinalAssessment` = penanda "Lulus" murni. UI tidak boleh menampilkan angka level apa pun.

Output: 1 controller + 2 ViewModel + 3 view dibersihkan; badge status reuse style sibling existing. TANPA migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-CONTEXT.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Titik prune yang sudah diverifikasi planner — pakai langsung. -->

Models/CDPDashboardViewModel.cs (HAPUS field):
```csharp
// CoacheeDashboardSubModel
public int? CompetencyLevelGranted { get; set; }   // :28 — HAPUS. Keep CurrentStatus (:29).
// ProtonProgressSubModel
public List<string> TrendLabels { get; set; } = new();   // :49 — HAPUS
public List<double> TrendValues { get; set; } = new();   // :50 — HAPUS
// CoacheeProgressRow
public int? CompetencyLevelGranted { get; set; }   // :90 — HAPUS. Keep HasFinalAssessment (:89).
```

Controllers/CDPController.cs (HAPUS binding):
- :376  `subModel.CompetencyLevelGranted = finalAssessment?.CompetencyLevelGranted;` — HAPUS. Keep :377 CurrentStatus.
- :542  `CompetencyLevelGranted = finalAssessment?.CompetencyLevelGranted` (init CoacheeProgressRow) — HAPUS. Keep :541 HasFinalAssessment.
- :574-594  blok trend chart (scopedCompletedAssessments, trendLabels, trendValues, .Average(...CompetencyLevelGranted)) — HAPUS seluruh blok.
- :627-628  `TrendLabels = trendLabels, TrendValues = trendValues,` (init ProtonProgressSubModel) — HAPUS 2 baris.
- :3517  `CompetencyLevel = hasAssessment ? fa!.CompetencyLevelGranted : null` (ProtonTimelineNode) — HAPUS.

Models/ProtonModels.cs:
```csharp
// ProtonTimelineNode.CompetencyLevel — HAPUS (dibind :3517, dirender HistoriProtonDetail.cshtml:91-97). Keep Status (:3516).
```

Badge style yang DI-REUSE (sudah ada di _CoachingProtonContentPartial.cshtml:191-204):
```cshtml
@if (row.HasFinalAssessment) { <span class="badge bg-success"><i class="bi bi-award-fill me-1"></i>Lulus</span> }
else if (row.TotalDeliverables == 0) { <span class="badge bg-light text-muted">No track</span> }
else { <span class="badge bg-secondary bg-opacity-25 text-secondary">In Progress</span> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune model + controller binding (data layer)</name>
  <files>Models/CDPDashboardViewModel.cs, Models/ProtonModels.cs, Controllers/CDPController.cs</files>
  <read_first>
    - Models/CDPDashboardViewModel.cs (:20-94 — field CompetencyLevelGranted :28/:90, TrendLabels/TrendValues :49-50; JANGAN hapus CurrentStatus :29, HasFinalAssessment :89)
    - Models/ProtonModels.cs (cari class ProtonTimelineNode — field CompetencyLevel; JANGAN hapus Status)
    - Controllers/CDPController.cs (:370-380, :535-545, :574-635, :3508-3521 — semua titik di interfaces)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (Level cleanup — Controllers + Models)
  </read_first>
  <action>
1) Models/CDPDashboardViewModel.cs — hapus 3 field: `CompetencyLevelGranted` di `CoacheeDashboardSubModel` (:28), `TrendLabels` + `TrendValues` di `ProtonProgressSubModel` (:49-50), `CompetencyLevelGranted` di `CoacheeProgressRow` (:90). PERTAHANKAN `CurrentStatus` (:29) dan `HasFinalAssessment` (:89) dan `ProgressPercent`.

2) Models/ProtonModels.cs — hapus `CompetencyLevel` di `ProtonTimelineNode`. PERTAHANKAN `Status`.

3) Controllers/CDPController.cs — hapus 5 titik binding (lihat interfaces):
   - :376 baris `subModel.CompetencyLevelGranted = ...;` (keep :377).
   - :542 baris `CompetencyLevelGranted = finalAssessment?.CompetencyLevelGranted` di init CoacheeProgressRow (keep :541 HasFinalAssessment; hati-hati koma trailing — pastikan object initializer tetap valid).
   - :574-594 SELURUH blok trend chart (komentar "Trend chart: competency level granted..." + `scopedCompletedAssessments` + `trendLabels`/`trendValues` declarations + `if (scopedCompletedAssessments.Any()) {...}`). Hapus utuh.
   - :627-628 baris `TrendLabels = trendLabels,` dan `TrendValues = trendValues,` di init `ProtonProgressSubModel` (pastikan initializer tetap valid, koma).
   - :3517 baris `CompetencyLevel = hasAssessment ? fa!.CompetencyLevelGranted : null` di init `ProtonTimelineNode` (keep `Status` :3516; rapikan koma).

PENTING: jangan menghapus variabel yang masih dipakai blok lain (mis. `finalAssessments`, `finalAssessmentDict`, `scopedCompletedAssessments` HANYA dipakai trend → boleh ikut hapus; verifikasi via grep tidak ada pemakaian lain). `CompetencyLevelGranted` di `ProtonFinalAssessment` entity (EF model) JANGAN disentuh — kolom DB dormant.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `CompetencyLevelGranted` di Models/CDPDashboardViewModel.cs → 0 match.
    - grep `TrendLabels|TrendValues` di Models/CDPDashboardViewModel.cs → 0 match.
    - grep `CompetencyLevel` di Models/ProtonModels.cs (ProtonTimelineNode) → 0 match untuk node field (entity ProtonFinalAssessment.CompetencyLevelGranted boleh tetap ada).
    - grep `CompetencyLevelGranted` di Controllers/CDPController.cs → 0 match (semua binding view dihapus; akses entity untuk badge sudah tidak ada).
    - grep `trendValues|trendLabels|scopedCompletedAssessments` di Controllers/CDPController.cs → 0 match.
    - grep `CurrentStatus` di Models/CDPDashboardViewModel.cs → masih ada (tidak terhapus).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Field level/trend di ViewModel + ProtonTimelineNode dihapus; 5 binding controller dihapus; CurrentStatus/HasFinalAssessment/Status tetap; entity DB tak disentuh; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Prune views — badge Lulus tanpa angka + hapus trend chart</name>
  <files>Views/CDP/Shared/_CoacheeDashboardPartial.cshtml, Views/CDP/Shared/_CoachingProtonContentPartial.cshtml, Views/CDP/HistoriProtonDetail.cshtml</files>
  <read_first>
    - Views/CDP/Shared/_CoacheeDashboardPartial.cshtml (:36-47 card "Competency Level", :94-104 success alert)
    - Views/CDP/Shared/_CoachingProtonContentPartial.cshtml (:57-83 trend card, :190-204 badge cell, :221-264 Chart.js protonTrendChart init)
    - Views/CDP/HistoriProtonDetail.cshtml (:91-97 blok node.CompetencyLevel)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md (S3/S4 + Copywriting Contract: badge Lulus/Belum Lulus/No track verbatim)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (Level cleanup — Views, badge reuse snippet)
  </read_first>
  <action>
**_CoacheeDashboardPartial.cshtml:**
- :36-47 — card "Competency Level" (`bi-award-fill text-warning` + `h3 "Level N"`). REPLACE jadi stat "Status Proton": badge `"Lulus"` (`<span class="badge bg-success"><i class="bi bi-award-fill me-1"></i>Lulus</span>`, no number) bila `Model.CurrentStatus == "Completed"`, else `"Belum Lulus"` (`<span class="badge bg-secondary bg-opacity-25 text-secondary">Belum Lulus</span>`). Label card "Status Proton". JANGAN tampilkan angka.
- :94-104 — PERTAHANKAN success alert `progressPercent == 100` "Congratulations" (:96-98). HAPUS inner block `@if (Model.CompetencyLevelGranted.HasValue) { ... "Competency Level N granted" ... }` (:99-103).

**_CoachingProtonContentPartial.cshtml:**
- :57-78 — trend-chart card (`col-lg-8`, "Competency Level Granted Over Time", `canvas#protonTrendChart`). HAPUS seluruh blok `col-lg-8`. Doughnut "Deliverable Status" (:79-98, `col-lg-4`) dipertahankan; bodohnut boleh dilebarkan ke `col-12`/`col-lg-6` atau biarkan `col-lg-4` (pilihan estetis). JANGAN tinggalkan placeholder "no data" (UI-SPEC: chart card dihapus penuh).
- :190-204 — cell tabel badge: ubah `Level @row.CompetencyLevelGranted` → `Lulus` (lihat snippet badge reuse). Cabang `TotalDeliverables == 0` → "No track" (UNCHANGED) + else "In Progress" (UNCHANGED). HANYA baris `Level @row.CompetencyLevelGranted` yang diubah jadi `Lulus`.
- :221-259 — blok Chart.js init `protonTrendChart` (`@if (Model.TrendLabels.Any()) { ... type:'line' ... }`). HAPUS seluruh blok. PERTAHANKAN init doughnut/bottleneck (:261+).

**HistoriProtonDetail.cshtml:**
- :91-97 — blok `@if (node.CompetencyLevel.HasValue) { ... "Level Kompetensi" ... @node.CompetencyLevel }`. HAPUS seluruhnya. Status "Lulus/Dalam Proses" sudah dirender dari `node.Status` di tempat lain — tidak perlu ganti.

Semua copy Bahasa Indonesia. Reuse class Bootstrap existing (no CSS baru). Tidak ada angka level di mana pun.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `CompetencyLevelGranted|CompetencyLevel` di ketiga view → 0 match.
    - grep `protonTrendChart` di _CoachingProtonContentPartial.cshtml → 0 match (canvas + JS init dihapus).
    - grep `TrendLabels|TrendValues` di _CoachingProtonContentPartial.cshtml → 0 match.
    - grep `Level @` (angka level) di ketiga view → 0 match.
    - grep `Lulus` di _CoacheeDashboardPartial.cshtml dan _CoachingProtonContentPartial.cshtml → ada (badge).
    - grep `Status Proton` di _CoacheeDashboardPartial.cshtml → ada (label card baru).
    - Tidak ada placeholder "no data"/"tidak ada data" baru ditambahkan untuk chart yang dihapus.
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Angka "Level N" diganti badge "Lulus"/"Belum Lulus" (no number); trend chart card + Chart.js init dihapus tanpa placeholder; blok Level Kompetensi histori dihapus; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 04 murni prune tampilan read-only; tidak ada input user baru, tidak ada endpoint baru, tidak ada perubahan otorisasi. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-11 | Information Disclosure | CDP views (level numeric removal) | accept | Menghapus tampilan angka mengurangi (bukan menambah) permukaan data; tidak ada data sensitif baru diekspos. |
| T-359-12 | Denial of Service (render error) | View binding setelah prune | mitigate | Field ViewModel + binding controller dihapus berpasangan; `dotnet build` + UAT render memastikan tidak ada NullReference/binding-missing yang meng-crash halaman. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests` hijau (tidak ada regresi).
- UAT lokal (CLAUDE.md, AD lokal `Authentication__UseActiveDirectory=false dotnet run`, localhost:5277):
  - Buka dashboard CDP coachee → "Status Proton" badge "Lulus"/"Belum Lulus", TANPA angka level.
  - Buka view supervisor/HC Proton → tabel coachee badge "Lulus"/"In Progress"/"No track" tanpa angka; TIDAK ada card trend chart; tidak ada placeholder kosong.
  - Buka HistoriProtonDetail untuk worker → tidak ada blok "Level Kompetensi"; status "Lulus/Dalam Proses" tetap muncul.
  - Semua halaman render tanpa error (cek console + tidak ada exception 500).
</verification>

<success_criteria>
- Tidak ada angka competency level di seluruh CDP/HistoriProton (PCOMP-10).
- Grafik tren dihapus penuh tanpa placeholder.
- Status "Lulus/Selesai" tetap tampil dari penanda; halaman render bersih.
- Kolom DB CompetencyLevelGranted tetap dormant (no migration).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/359-gate-berurutan-cleanup-a/359-04-SUMMARY.md`.
</output>
