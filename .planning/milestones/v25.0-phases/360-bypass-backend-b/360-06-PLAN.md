---
phase: 360-bypass-backend-b
plan: 06
type: execute
wave: 6
depends_on: [04, 05]
files_modified:
  - Services/GradingService.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PBYP-03]
tags: [proton, bypass, grading-hook, notification, essay]
user_setup: []

must_haves:
  truths:
    - "Exam Proton lulus (online) → MarkPendingReadyIfAnyAsync dipanggil setelah EnsureAsync (GradeAndCompleteAsync)"
    - "Re-grade Fail→Pass → MarkPendingReadyIfAnyAsync (flip Siap); Re-grade Pass→Fail → RevertPendingToMenungguAsync (D-15)"
    - "Essay Proton finalisasi lulus → MarkPendingReadyIfAnyAsync (Pitfall 2: hasEssay early-return tak lewat hook utama)"
    - "GradingService inject ProtonBypassService satu arah (tak circular)"
  artifacts:
    - path: "Services/GradingService.cs"
      provides: "inject ProtonBypassService + 3 hook"
      contains: "MarkPendingReadyIfAnyAsync"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "essay hook MarkPendingReady (titik 4)"
      contains: "MarkPendingReadyIfAnyAsync"
  key_links:
    - from: "GradingService.GradeAndCompleteAsync"
      to: "ProtonBypassService.MarkPendingReadyIfAnyAsync"
      via: "hook setelah EnsureAsync (exam lulus)"
      pattern: "MarkPendingReadyIfAnyAsync\\(session.Id\\)"
    - from: "AssessmentAdminController.FinalizeEssayGrading"
      to: "ProtonBypassService.MarkPendingReadyIfAnyAsync"
      via: "hook essay (titik 4, Pitfall 2)"
      pattern: "MarkPendingReadyIfAnyAsync"
---

<objective>
Pasang hook notif §7 di 4 titik (RESEARCH Pitfall 2 — bukan 1) supaya exam CL-B(b) yang lulus selalu mem-flip pending→Siap + notif HC, termasuk cabang essay early-return. Hook = flip flag + notif SAJA (tanpa transaksi, tanpa eksekusi pindah — pindah tetap di HC plan 05).

Purpose: GradeAndCompleteAsync early-return saat ada soal essay (:229) → sesi bare CL-B(b) ber-essay TIDAK lewat hook utama, hanya lewat FinalizeEssayGrading → tanpa titik ke-4, pending nyangkut "Menunggu" selamanya walau worker lulus. Re-grade Pass→Fail harus revert pending (D-15).
Output: GradingService inject + 3 hook + essay hook ke-4 di AssessmentAdminController.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@.planning/phases/360-bypass-backend-b/360-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md
@Services/ProtonBypassService.cs

<interfaces>
<!-- Method dari plan 04 yang dipanggil hook -->
```csharp
ProtonBypassService.MarkPendingReadyIfAnyAsync(int sessionId);   // flip Menunggu→Siap + notif (NO tx)
ProtonBypassService.RevertPendingToMenungguAsync(int sessionId); // Siap→Menunggu (NO tx, D-15)
```

4 titik hook (sisipkan PERSIS setelah EnsureAsync/RemoveExamOriginAsync existing — guard sama: Category=="Assessment Proton" && ProtonTrackId.HasValue):
1. GradingService.GradeAndCompleteAsync :304-309 (exam langsung lulus) — setelah EnsureAsync. [BRACED block — aman]
2. GradingService.RegradeAfterEditAsync cabang Fail→Pass :531-534 — setelah EnsureAsync → MarkPendingReady (flip Siap). [BRACE-LESS if — W-09: braced dulu]
3. GradingService.RegradeAfterEditAsync cabang Pass→Fail :485-486 — setelah RemoveExamOriginAsync → RevertPendingToMenunggu (D-15). [BRACE-LESS if — W-09: braced dulu]
4. AssessmentAdminController.FinalizeEssayGrading :3754-3759 (essay finalisasi lulus) — setelah EnsureAsync.
</interfaces>

<!-- Open Q3: GradingService → ProtonBypassService satu arah; ProtonBypassService TIDAK inject GradingService (no circular) -->
<!-- W-09 (review): titik 2 (:531-534) & titik 3 (:485-486) adalah brace-less single-statement if. -->
<!--   Append await mentah SETELAH statement tunggal akan menjalankan hook UNCONDITIONALLY (di luar guard). -->
<!--   WAJIB konversi ke braced block { } DULU sebelum append hook call. Titik 1 (:304-309) sudah braced — aman. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: GradingService inject ProtonBypassService + 3 hook (braced fix W-09)</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs (ctor :23-33 inject ProtonCompletionService — pola; hook titik 1 :304-309 GradeAndCompleteAsync [BRACED]; titik 2 Fail→Pass :531-534 [BRACE-LESS]; titik 3 Pass→Fail :485-486 setelah RemoveExamOriginAsync [BRACE-LESS])
    - Services/ProtonBypassService.cs (MarkPendingReadyIfAnyAsync + RevertPendingToMenungguAsync — method dari plan 04, NO tx)
  </read_first>
  <action>
    1. `GradingService` ctor: tambah parameter `ProtonBypassService protonBypassService` (setelah `ProtonCompletionService protonCompletionService` :27) + field `private readonly ProtonBypassService _protonBypassService;` + assign. (DI sudah AddScoped<ProtonBypassService> dari plan 03.)
    2. Titik 1 — `GradeAndCompleteAsync` di dalam blok BRACED `if (session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue)` (:304-309), SETELAH `EnsureAsync(...)`:
       ```csharp
       await _protonBypassService.MarkPendingReadyIfAnyAsync(session.Id);  // §7: flip Menunggu→Siap + notif HC
       ```
    3. **W-09 — Titik 2** — `RegradeAfterEditAsync` cabang Fail→Pass (:531-534). Ini BRACE-LESS single-statement if. KONVERSI KE BRACED BLOCK dulu:
       - SEBELUM: `if (... isPassed && session.ProtonTrackId.HasValue)\n    await ...EnsureAsync(...);`
       - SESUDAH (tambah `{ }` lalu append hook DI DALAM block):
       ```csharp
       if (session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue)
       {
           await _protonCompletionService.EnsureAsync(...);          // existing, dipindah ke dalam block
           await _protonBypassService.MarkPendingReadyIfAnyAsync(session.Id);  // re-grade lulus → pending Siap
       }
       ```
    4. **W-09 — Titik 3** — `RegradeAfterEditAsync` cabang Pass→Fail (:485-486). Ini BRACE-LESS single-statement if. KONVERSI KE BRACED BLOCK dulu:
       ```csharp
       if (session.Category == "Assessment Proton" && session.ProtonTrackId.HasValue)
       {
           await _protonCompletionService.RemoveExamOriginAsync(...);   // existing, dipindah ke dalam block
           await _protonBypassService.RevertPendingToMenungguAsync(session.Id);  // D-15: pending balik Menunggu
       }
       ```
    Hook = flip + notif/revert SAJA (TIDAK buka transaksi — method plan 04 sudah no-tx). JANGAN ubah logic grading existing selain menambah `{ }` + hook call. Verifikasi indentasi + tak ada statement existing yang terlepas dari guard.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ProtonBypassService protonBypassService" Services/GradingService.cs` → 1 match (ctor param)
    - `grep -cF "MarkPendingReadyIfAnyAsync(session.Id)" Services/GradingService.cs` → 2 (titik 1 + titik 2)
    - `grep -cF "RevertPendingToMenungguAsync(session.Id)" Services/GradingService.cs` → 1 (titik 3)
    - W-09 braced: manual review titik 2 (:531-534) & titik 3 (:485-486) sekarang punya `{ }` mengelilingi EnsureAsync/RemoveExamOriginAsync + hook (hook DI DALAM guard, bukan di luar)
    - Hook titik 1/2 muncul SETELAH `EnsureAsync` (urutan); titik 3 SETELAH `RemoveExamOriginAsync` — verifikasi review
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau (grading existing tak regresi)
  </acceptance_criteria>
  <done>GradingService inject ProtonBypassService + 3 hook (2 MarkPendingReady + 1 Revert); titik 2/3 brace-less di-konversi braced (W-09) — hook di dalam guard; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Essay hook titik 4 (AssessmentAdminController.FinalizeEssayGrading)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (:3750-3759 FinalizeEssayGrading — blok `if (session.Category=="Assessment Proton" && isPassed && session.ProtonTrackId.HasValue)` :3754 dengan EnsureAsync :3756-3758; cek apakah braced atau brace-less; cek apakah controller punya akses `_protonBypassService` atau perlu inject)
    - Services/ProtonBypassService.cs (MarkPendingReadyIfAnyAsync)
    - Controllers/AssessmentAdminController.cs (ctor — cek field service yang sudah di-inject; tambah ProtonBypassService bila belum ada)
  </read_first>
  <action>
    1. Pastikan `AssessmentAdminController` punya akses `ProtonBypassService`. Cek ctor: kalau belum inject, tambah parameter `ProtonBypassService protonBypassService` + field `private readonly ProtonBypassService _protonBypassService;` + assign. (DI sudah ada.)
    2. Di `FinalizeEssayGrading` (:3754-3759), di dalam blok `if (session.Category == "Assessment Proton" && isPassed && session.ProtonTrackId.HasValue)`, SETELAH `_protonCompletionService.EnsureAsync(...)` (:3756-3758):
       ```csharp
       await _protonBypassService.MarkPendingReadyIfAnyAsync(session.Id);  // Pitfall 2: essay early-return tutup celah hook utama
       ```
    Kalau blok :3754 ternyata brace-less single-statement (cek saat read_first), konversi ke braced block dulu (sama W-09) sebelum append. Idempotent — aman walau Hook titik-1 juga jalan (MarkPendingReady guard rowsAffected). JANGAN ubah logic finalize existing.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MarkPendingReadyIfAnyAsync" Controllers/AssessmentAdminController.cs` → ≥1 (titik 4)
    - Hook muncul SETELAH `EnsureAsync` di FinalizeEssayGrading (urutan, di dalam guard braced) — review
    - `grep -n "ProtonBypassService" Controllers/AssessmentAdminController.cs` → ada (field/ctor)
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>Essay hook titik 4 terpasang (Pitfall 2 tertutup, di dalam guard braced); build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grading hot-path → bypass hook | Hook jalan saat worker submit/grade; tak boleh ganggu grading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-19 | Denial of Service | hook crash grading | mitigate | Hook = MarkPendingReady/Revert (no tx, guard); idempotent; tak melempar yang menghentikan grading. Method plan 04 sudah aman |
| T-360-20 | Tampering | essay path miss (Pitfall 2) | mitigate | Hook ke-4 di FinalizeEssayGrading menutup cabang hasEssay early-return → pending tak nyangkut "Menunggu" |
| T-360-21 | Tampering | circular DI | accept | GradingService → ProtonBypassService satu arah; ProtonBypassService tidak inject GradingService (Open Q3) — build memverifikasi tidak ada cycle |
| T-360-35 | Tampering | hook jalan di luar guard (brace-less) | mitigate | W-09: titik 2/3 brace-less if di-konversi braced { } dulu → hook dijamin di dalam guard Proton, tak jalan untuk sesi non-Proton |
</threat_model>

<verification>
- `dotnet build` 0 error (membuktikan tidak ada circular DI).
- `dotnet test --filter "Category!=Integration"` hijau (grading existing tak regresi).
- 4 titik hook terpasang (2 MarkPendingReady + 1 Revert di GradingService + 1 MarkPendingReady di FinalizeEssayGrading), semua di dalam guard braced (W-09).
- Flip behavior end-to-end diverifikasi integration test plan 04 (MarkPendingReady) + UAT plan 08.
</verification>

<success_criteria>
- GradingService inject ProtonBypassService + 3 hook; essay hook ke-4 di AssessmentAdminController.
- Titik 2/3 brace-less di-konversi braced (W-09); hook flip/revert tanpa transaksi; build + test hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-06-SUMMARY.md`. Catat 4 titik hook + konversi braced W-09 (titik 2/3) + konfirmasi tidak ada circular DI.
</output>
</content>
