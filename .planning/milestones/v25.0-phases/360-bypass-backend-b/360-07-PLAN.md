---
phase: 360-bypass-backend-b
plan: 07
type: execute
wave: 7
depends_on: [05, 06]
files_modified:
  - Controllers/ProtonDataController.cs
  - HcPortal.Tests/ProtonBypassEndpointTests.cs
autonomous: true
requirements: [PBYP-07]
tags: [proton, bypass, endpoint, controller, authorize, antiforgery]
user_setup: []

must_haves:
  truths:
    - "6 endpoint bypass ada: BypassList, BypassPendingList, BypassDetail (GET) + BypassSave, BypassConfirm, BypassCancelPending (POST)"
    - "Semua endpoint di bawah [Authorize(Roles=Admin,HC)]; 3 POST punya [ValidateAntiForgeryToken]"
    - "POST mutator validasi server-side (alasan wajib, mode valid) + delegasi ke ProtonBypassService + audit"
    - "CL-B(b) BypassSave surface reminder lampirkan paket (D-02) via TempData['Warning'] + flag Json"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "6 endpoint bypass + DTO"
      contains: "public async Task<IActionResult> BypassSave"
    - path: "HcPortal.Tests/ProtonBypassEndpointTests.cs"
      provides: "reflection test atribut Authorize/AntiForgery"
      contains: "ValidateAntiForgeryToken"
  key_links:
    - from: "ProtonDataController.BypassSave"
      to: "ProtonBypassService.BypassSaveAsync"
      via: "delegasi + audit"
      pattern: "BypassSaveAsync"
    - from: "POST endpoint"
      to: "ValidateAntiForgeryToken"
      via: "CSRF guard"
      pattern: "ValidateAntiForgeryToken"
---

<objective>
Expose 6 endpoint bypass di `ProtonDataController` (spec §10, pola `OverrideSave`): 3 GET read (BypassList/BypassPendingList/BypassDetail) untuk feed UI Phase 361, 3 POST mutator (BypassSave/BypassConfirm/BypassCancelPending) yang delegasi ke `ProtonBypassService`. Semua `[Authorize(Admin,HC)]` (dari kelas) + POST `[ValidateAntiForgeryToken]` + validasi server-side + audit.

Purpose: Trust boundary Browser → POST. Endpoint = satu-satunya pintu fitur bypass; UI Phase 361 cuma konsumen. Validasi server-side (V5) + CSRF (V2) + Authorize (V4) + audit + no ex.Message (D6) wajib di sini.
Output: 6 endpoint + DTO request + reflection auth test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@.planning/phases/360-bypass-backend-b/360-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md
@Services/ProtonBypassService.cs

<interfaces>
<!-- Service methods (plan 03/04/05) yang dipanggil endpoint -->
```csharp
ProtonBypassService.BypassSaveAsync(BypassRequest req) → BypassResult(Success, Message, PendingId?, ShowAttachPackageReminder)
ProtonBypassService.ConfirmBypassAsync(int pendingId, string hcId, string hcName) → BypassResult
ProtonBypassService.CancelPendingAsync(int pendingId, string hcId, string hcName) → BypassResult
```

DTO request (taruh top file dekat OverrideSaveRequest :70-77):
```csharp
public class BypassSaveRequest
{
    public string CoacheeId { get; set; } = "";
    public int SourceProtonTrackId { get; set; }
    public int TargetProtonTrackId { get; set; }
    public string TargetUnit { get; set; } = "";
    public string? TargetCoachId { get; set; }
    public string Reason { get; set; } = "";
    public string Mode { get; set; } = "";       // CL-A | CL-B(a) | CL-B(b) | CL-C
    public int? DurationMinutes { get; set; }
}
public class BypassPendingActionRequest { public int PendingId { get; set; } }
```

Pola endpoint POST: OverrideSave (ProtonDataController.cs:1397-1474) — [HttpPost][ValidateAntiForgeryToken][FromBody] + GetUserAsync/Challenge + validasi awal + Json(new { success, message }) + _auditLog.LogAsync.
Class header [Authorize(Roles="Admin,HC")] :79 (semua endpoint otomatis tergate).

BypassDetail eligibleModes (B-03 — selaras spec §4/§5):
- CL-A eligible ⟺ `sourceComplete && sourceHasFinal` (allApproved DAN penanda final ada). JANGAN cuma `sourceComplete`.
- CL-B(a)/CL-B(b) eligible ⟺ `!sourceHasFinal` (final belum ada — D-D).
- CL-C eligible selalu (turun/koreksi).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject service + DTO + 3 GET endpoint (BypassList/BypassPendingList/BypassDetail)</name>
  <files>Controllers/ProtonDataController.cs</files>
  <read_first>
    - Controllers/ProtonDataController.cs (class header [Authorize] :79; ctor :88-100 — tambah ProtonBypassService; DTO OverrideSaveRequest :70-77 pola; StatusData :102-150 + Index :152 pola query Proton track/unit untuk BypassList)
    - Services/ProtonBypassService.cs (untuk paham field PendingProtonBypass + status untuk BypassPendingList)
    - Models/ProtonModels.cs (ProtonTrackAssignment + ProtonFinalAssessment + PendingProtonBypass untuk shape data wizard)
  </read_first>
  <action>
    1. Inject `ProtonBypassService` ke `ProtonDataController` ctor (tambah param + field `_protonBypassService` + assign, pola existing :88-100).
    2. Tambah DTO `BypassSaveRequest` + `BypassPendingActionRequest` di top file (dekat :77).
    3. `GET BypassList(string? bagian, string? unit, int? trackId)` — tabel worker untuk Tab2: query worker dengan ProtonTrackAssignment aktif (filter bagian/unit/track), proyeksikan { coacheeId, nama, trackAktif (DisplayName), progress X/Y (count Approved / total deliverable unit), finalAda (ProtonFinalAssessment ada?) } → `Json(...)`. (Read-only, dari kelas sudah [Authorize].)
    4. `GET BypassPendingList()` — daftar PendingProtonBypass Status ∈ {Menunggu, Siap} untuk panel atas: proyeksikan { id, coacheeId, nama, source→target (DisplayName track), status, hasilExam (IsPassed linked), createdAt } → `Json(...)`. (TANPA field mode — kolom Mode di-drop W-05; pending selalu CL-B(b).)
    5. `GET BypassDetail(string coacheeId)` — state worker untuk wizard (mode mana yang boleh): resolve assignment aktif + tahun + SourceComplete (allApproved) + SourceHasFinal → { sourceTrackId, sourceTahun, sourceComplete, sourceHasFinal, eligibleModes[] } → `Json(...)`.
       **eligibleModes (B-03):** CL-A ⟺ `sourceComplete && sourceHasFinal`; CL-B(a)/CL-B(b) ⟺ `!sourceHasFinal`; CL-C selalu. JANGAN CL-A = sourceComplete saja (worker 100% deliverable tapi penanda belum terbit harus diarahkan CL-B(a), bukan CL-A).
    JANGAN ubah endpoint Override/OverrideSave existing (Tab1).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public async Task<IActionResult> BypassList" Controllers/ProtonDataController.cs` → 1 match
    - `grep -n "public async Task<IActionResult> BypassPendingList" Controllers/ProtonDataController.cs` → 1 match
    - `grep -n "public async Task<IActionResult> BypassDetail" Controllers/ProtonDataController.cs` → 1 match
    - `grep -n "class BypassSaveRequest" Controllers/ProtonDataController.cs` → 1 match
    - `grep -n "_protonBypassService" Controllers/ProtonDataController.cs` → ≥1 (injected)
    - BypassPendingList query Status Menunggu/Siap: `grep -n "\"Menunggu\"\|\"Siap\"" Controllers/ProtonDataController.cs` → ≥1
    - B-03 eligibleModes: `grep -n "sourceHasFinal\|SourceHasFinal" Controllers/ProtonDataController.cs` → ≥1 (CL-A pakai sourceComplete && sourceHasFinal)
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>3 GET endpoint + DTO + service injected build hijau; BypassDetail eligibleModes CL-A=complete&&final (B-03); Tab1 existing utuh.</done>
</task>

<task type="auto">
  <name>Task 2: 3 POST endpoint (BypassSave/BypassConfirm/BypassCancelPending) + AntiForgery + validasi + audit + D-02 TempData</name>
  <files>Controllers/ProtonDataController.cs</files>
  <read_first>
    - Controllers/ProtonDataController.cs (OverrideSave :1397-1474 — pola [HttpPost][ValidateAntiForgeryToken][FromBody] + GetUserAsync/Challenge + validasi awal + Json + _auditLog.LogAsync :1469-1471; cek pola TempData existing di controller bila ada)
    - Services/ProtonBypassService.cs (BypassSaveAsync/ConfirmBypassAsync/CancelPendingAsync signature + BypassResult)
  </read_first>
  <action>
    Ketiga POST: `[HttpPost]` + `[ValidateAntiForgeryToken]` + `[FromBody]` + `var user = await _userManager.GetUserAsync(User); if (user==null) return Challenge();`.
    1. `BypassSave([FromBody] BypassSaveRequest req)`:
       - Validasi server-side awal (V5, jangan percaya form): `string.IsNullOrWhiteSpace(req.Reason)` → Json(false, "Alasan wajib diisi."); mode ∈ {CL-A,CL-B(a),CL-B(b),CL-C} else Json(false,"Mode tidak valid."); req.CoacheeId tidak kosong.
       - Build `BypassRequest` dari DTO + `InitiatedById=user.Id`. `var result = await _protonBypassService.BypassSaveAsync(bypassReq);`
       - **D-02 (W-12, decision #4 opsi a) — TempData WAJIB:** bila `result.ShowAttachPackageReminder` true (CL-B(b)), SET `TempData["Warning"] = result.Message;` (selain flag Json). Ini memenuhi D-02 locked "WAJIB pengingat TempData setelah BypassSave CL-B(b)". Flag Json tetap diteruskan (dipakai UI Phase 361). Contoh:
       ```csharp
       if (result.ShowAttachPackageReminder)
           TempData["Warning"] = result.Message;  // D-02: reminder lampirkan paket
       ```
       - `_auditLog.LogAsync(user.Id, user.FullName ?? user.UserName ?? user.Id, "ProtonBypassSave", $"BypassSave {req.Mode} coachee {req.CoacheeId}: track {req.SourceProtonTrackId}→{req.TargetProtonTrackId}. Alasan: {req.Reason}", targetType:"PendingProtonBypass");` (audit di controller = jejak request).
       - Return `Json(new { success = result.Success, message = result.Message, pendingId = result.PendingId, showAttachPackageReminder = result.ShowAttachPackageReminder });` (D-02 reminder diteruskan ke UI Phase 361).
    2. `BypassConfirm([FromBody] BypassPendingActionRequest req)`: delegasi `await _protonBypassService.ConfirmBypassAsync(req.PendingId, user.Id, user.FullName ?? user.Id)` + audit "ProtonBypassConfirm" + Json(result).
    3. `BypassCancelPending([FromBody] BypassPendingActionRequest req)`: delegasi `await _protonBypassService.CancelPendingAsync(req.PendingId, user.Id, user.FullName ?? user.Id)` + audit "ProtonBypassCancel" + Json(result).
    JANGAN expose exception/stacktrace (service sudah return pesan ramah; controller cuma teruskan result.Message — D6).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public async Task<IActionResult> BypassSave\b" Controllers/ProtonDataController.cs` → 1 match
    - `grep -n "public async Task<IActionResult> BypassConfirm" Controllers/ProtonDataController.cs` → 1 match
    - `grep -n "public async Task<IActionResult> BypassCancelPending" Controllers/ProtonDataController.cs` → 1 match
    - `grep -cF "[ValidateAntiForgeryToken]" Controllers/ProtonDataController.cs` → ≥4 (existing OverrideSave + 3 baru; W-14 fixed-string grep; verifikasi count naik 3 dari baseline)
    - `grep -n "BypassSaveAsync\|ConfirmBypassAsync\|CancelPendingAsync" Controllers/ProtonDataController.cs` → 3 (delegasi)
    - `grep -cF "_auditLog.LogAsync" Controllers/ProtonDataController.cs` → naik ≥3 dari baseline (audit per mutator)
    - D-02 TempData (W-12): `grep -n "TempData\[\"Warning\"\]" Controllers/ProtonDataController.cs` → ≥1 (set saat ShowAttachPackageReminder)
    - `grep -n "showAttachPackageReminder" Controllers/ProtonDataController.cs` → ≥1 (D-02 flag Json diteruskan)
    - No info-leak: manual review endpoint bypass — Json return pakai `result.Message`, TIDAK ada `ex.Message`/stacktrace
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>3 POST endpoint + AntiForgery + validasi server-side + delegasi service + audit + D-02 TempData["Warning"] + flag Json (W-12) build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Reflection test atribut Authorize + ValidateAntiForgeryToken (pola Phase 344)</name>
  <files>HcPortal.Tests/ProtonBypassEndpointTests.cs</files>
  <read_first>
    - HcPortal.Tests/ (cari test reflection permission existing pola Phase 344 TEST-05 — `[Fact]` yang baca atribut method via reflection)
    - Controllers/ProtonDataController.cs (6 endpoint baru)
  </read_first>
  <action>
    Buat `HcPortal.Tests/ProtonBypassEndpointTests.cs` (unit, no DB) — reflection assert:
    1. `ProtonDataController` punya `[Authorize(Roles="Admin,HC")]` di class level (atribut ada + Roles mengandung "Admin" dan "HC").
    2. Method `BypassSave`, `BypassConfirm`, `BypassCancelPending` masing-masing punya `[HttpPost]` DAN `[ValidateAntiForgeryToken]`.
    3. (opsional) GET endpoint BypassList/BypassPendingList/BypassDetail TIDAK punya [ValidateAntiForgeryToken] (read-only).
    Pakai `typeof(ProtonDataController).GetMethod("BypassSave").GetCustomAttributes(typeof(ValidateAntiForgeryTokenAttribute), false)`.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ProtonBypassEndpoint" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "[Fact]" HcPortal.Tests/ProtonBypassEndpointTests.cs` → ≥3 (W-14: fixed-string grep)
    - `grep -n "ValidateAntiForgeryTokenAttribute" HcPortal.Tests/ProtonBypassEndpointTests.cs` → ≥1
    - `grep -n "AuthorizeAttribute" HcPortal.Tests/ProtonBypassEndpointTests.cs` → ≥1
    - `dotnet test --filter "FullyQualifiedName~ProtonBypassEndpoint"` → semua Passed (membuktikan Authorize + AntiForgery di 3 POST)
  </acceptance_criteria>
  <done>Reflection test membuktikan Authorize(Admin,HC) + AntiForgery di endpoint mutator; hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST endpoint | Input tak terpercaya (form bypass) menyeberang ke server; aksi hak tinggi pindah tahun |
| Non-HC user → endpoint | Pencegahan akses oleh role non-Admin/HC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-22 | Elevation of Privilege | endpoint diakses non-HC | mitigate | Class `[Authorize(Roles="Admin,HC")]` (:79) men-gate 6 endpoint; reflection test verifikasi |
| T-360-23 | Tampering (CSRF) | POST mutator | mitigate | `[ValidateAntiForgeryToken]` di BypassSave/Confirm/CancelPending; reflection test verifikasi |
| T-360-24 | Tampering | input form tak valid | mitigate | Validasi server-side (alasan wajib, mode valid, coacheeId) di controller + ValidateBypass di service; tidak percaya UI Phase 361 |
| T-360-25 | Information Disclosure | error leak | mitigate | Service return pesan ramah; controller teruskan result.Message; tak ada ex.Message/stacktrace (D6 Phase 334) |
| T-360-26 | Repudiation | aksi tanpa jejak | mitigate | `_auditLog.LogAsync` per mutator (siapa, kapan, mode, alasan) |
| T-360-27 | Tampering (SQLi) | query param | accept | EF Core parameterized (LINQ) — tidak ada raw SQL string-concat di endpoint |
</threat_model>

<verification>
- `dotnet build` 0 error; `dotnet test --filter "FullyQualifiedName~ProtonBypassEndpoint"` hijau.
- 6 endpoint ada; 3 POST punya AntiForgery; validasi server-side + audit + no ex.Message.
- D-02 TempData["Warning"] di-set saat ShowAttachPackageReminder (W-12); eligibleModes CL-A=complete&&final (B-03).
- Tab1 (Override/OverrideSave) tak berubah.
</verification>

<success_criteria>
- 6 endpoint bypass [Authorize(Admin,HC)] + 3 POST AntiForgery + validasi + delegasi service + audit.
- D-02 reminder via TempData["Warning"] + flag Json; no info-leak; reflection test hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-07-SUMMARY.md`. Catat 6 endpoint + DTO + D-02 TempData (W-12) + eligibleModes B-03 untuk konsumsi UI Phase 361.
</output>
</content>
