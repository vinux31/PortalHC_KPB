---
phase: 361-bypass-ui-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/seeds/361-bypass-fixtures.sql
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [PBYP-10]
must_haves:
  truths:
    - "Ada SQL fixture idempotent yang membuat worker multi-state untuk skenario bypass (komplit CL-A, partial CL-B, punya final → tolak CL-B, exam in-progress E5)"
    - "Fixture WIPE-AND-INSERT aman di-rerun (cleanup by greppable marker sebelum insert)"
    - "Fixture punya THROW guard kalau user referensi tidak ada (anti FK-NULL Phase 309)"
    - "Klasifikasi seed tercatat di docs/SEED_JOURNAL.md sebagai temporary + local-only"
  artifacts:
    - path: ".planning/seeds/361-bypass-fixtures.sql"
      provides: "Worker multi-state fixture untuk e2e bypass"
      contains: "SET XACT_ABORT ON"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Jurnal seed Phase 361 (active state)"
      contains: "Phase 361"
  key_links:
    - from: "361-bypass-fixtures.sql"
      to: "Users (Email lookup)"
      via: "THROW guard subquery"
      pattern: "THROW 5000"
    - from: "361-bypass-fixtures.sql"
      to: "ProtonTrackAssignments / ProtonDeliverableProgresses / ProtonFinalAssessments / PendingProtonBypasses"
      via: "INSERT multi-state + cleanup by marker"
      pattern: "Phase 361"
---

<objective>
Buat SQL fixture worker multi-state untuk e2e UAT bypass (D-23), mengikuti pola `.planning/seeds/313-timer-fixtures.sql` (WIPE-AND-INSERT idempotent + THROW guard + BEGIN TRAN). Fixture menyiapkan worker dalam 4 state yang menutup 4 closure mode + skenario pending:
- **Worker A — komplit:** assignment aktif + SEMUA ProtonDeliverableProgress Status="Approved" + punya ProtonFinalAssessment → eligible **CL-A**.
- **Worker B — partial:** assignment aktif + sebagian Approved, sebagian Pending + TIDAK punya final → eligible **CL-B(a)** & **CL-B(b)**.
- **Worker C — punya final:** assignment aktif + punya ProtonFinalAssessment → **CL-B ditolak** (D-D), hanya CL-A/CL-C path.
- **Worker D — pending CL-B(b) (E5):** ada baris PendingProtonBypass Status="Menunggu" + AssessmentSession terkait belum lulus → panel pending badge "Menunggu Exam".

Purpose: e2e spec (Plan 04) butuh data deterministik untuk assert eligibleModes + panel pending tanpa bergantung data produksi. SEED_WORKFLOW (CLAUDE.md): klasifikasi temporary + local-only, snapshot sebelum (di spec beforeAll), restore sesudah.
Output: `.planning/seeds/361-bypass-fixtures.sql` + entry `docs/SEED_JOURNAL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/361-bypass-ui-b/361-CONTEXT.md
@.planning/phases/361-bypass-ui-b/361-RESEARCH.md
@.planning/phases/361-bypass-ui-b/361-PATTERNS.md
@docs/SEED_WORKFLOW.md

<interfaces>
<!-- Schema kolom REAL (diekstrak dari Models/ProtonModels.cs + AssessmentSession.cs) -->

ProtonTrackAssignment (Models/ProtonModels.cs:71-87) — tabel ProtonTrackAssignments:
  Id (int PK), CoacheeId (nvarchar, no-FK), AssignedById (nvarchar), ProtonTrackId (int),
  IsActive (bit), AssignedAt (datetime2), DeactivatedAt (datetime2?), Origin (nvarchar(20)?)
  → BypassDetail butuh TEPAT 1 assignment IsActive=1 per worker.

ProtonDeliverableProgress (Models/ProtonModels.cs:92-146) — tabel ProtonDeliverableProgresses:
  Id (int PK), CoacheeId (nvarchar), ProtonDeliverableId (int), ProtonTrackAssignmentId (int),
  Status (nvarchar: "Pending"|"Submitted"|"Approved"|"Rejected"), CreatedAt, + banyak kolom approval.
  → sourceComplete = semua Status=="Approved" (BypassDetail:1583). Untuk Worker A: semua "Approved".

ProtonFinalAssessment (Models/ProtonModels.cs:210+) — tabel ProtonFinalAssessments:
  Id (int PK), ProtonTrackAssignmentId (int), ... → sourceHasFinal = Any(fa.ProtonTrackAssignmentId == assignment.Id).

PendingProtonBypass (Models/ProtonModels.cs:235-249) — tabel PendingProtonBypasses:
  Id, CoacheeId, SourceProtonTrackId, TargetProtonTrackId, TargetUnit, TargetCoachId?,
  Reason, LinkedAssessmentSessionId (int, non-null), Status ("Menunggu"|"Siap"|"Selesai"|"Dibatalkan"),
  InitiatedById, CreatedAt, ResolvedAt?.

AssessmentSession (Models/AssessmentSession.cs) — tabel AssessmentSessions:
  Id, UserId, Score (int?), IsPassed (bool?), CompletedAt (datetime2?), StartedAt, dll.
  → Worker D pending butuh LinkedAssessmentSessionId valid → AssessmentSession IsPassed=NULL/0 (belum lulus).

CATATAN no-FK: CoacheeId/AssignedById tidak punya FK constraint TAPI BypassDetail mensyaratkan
assignment aktif tepat 1 dan worker harus user nyata supaya UI bisa render nama. Pakai user
existing dari Users (THROW guard).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tulis 361-bypass-fixtures.sql (WIPE-AND-INSERT idempotent + THROW guard + BEGIN TRAN)</name>
  <read_first>
    - .planning/seeds/313-timer-fixtures.sql (pola lengkap: header, SET XACT_ABORT ON, THROW guard, cleanup chain FK-respecting, BEGIN TRAN, verification SELECT)
    - Models/ProtonModels.cs:71-249 (ProtonTrackAssignment, ProtonDeliverableProgress, ProtonFinalAssessment, PendingProtonBypass — kolom REAL)
    - Models/AssessmentSession.cs:26,38,39 (Score, IsPassed, CompletedAt)
    - Controllers/ProtonDataController.cs:1561-1605 (BypassDetail — logic eligibleModes yang fixture harus picu: sourceComplete=semua Approved; sourceHasFinal=Any final; CL-A butuh complete&&final)
    - docs/SEED_WORKFLOW.md (klasifikasi + format jurnal + command BACKUP/RESTORE)
  </read_first>
  <action>
Buat file `.planning/seeds/361-bypass-fixtures.sql`. Tiru struktur `313-timer-fixtures.sql` PERSIS:

1. **Header komentar** — tujuan, REQ (PBYP-10), cara run (`sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -b -i .planning\seeds\361-bypass-fixtures.sql`), klaim idempotent, pre-condition (user fixture ada di Users), anti-pattern Phase 309. WAJIB cantumkan: DB lokal SAJA, jangan jalankan di Dev/Prod.

2. **`SET NOCOUNT ON; SET XACT_ABORT ON;`**

3. **Resolve referensi user + THROW guard.** Pilih 4 user existing dari Users untuk Worker A/B/C/D. Gunakan akun yang sudah ada (mis. coachee fixtures yang dipakai test lain — verifikasi dengan `SELECT Email FROM Users` sebelum hardcode). Untuk SETIAP user:
```sql
DECLARE @WorkerA NVARCHAR(450) = (SELECT TOP 1 Id FROM Users WHERE Email = '<emailA>@pertamina.com');
IF @WorkerA IS NULL THROW 50001, 'User <emailA> tidak ditemukan — abort. Seed user dulu.', 1;
```
Resolve juga 2 ProtonTrackId nyata (source & target) dari ProtonTracks (`SELECT TOP 1 Id FROM ProtonTracks ORDER BY Urutan` untuk source; target = Urutan source+1 supaya |Δtahun|≤1). THROW kalau track tak ada.

4. **Idempotent cleanup chain SEBELUM BEGIN TRAN** (FK-respecting order, pakai greppable marker `'Phase 361%'` di kolom Reason untuk PendingProtonBypasses; untuk ProtonTrackAssignments/Progresses/Final pakai marker via AssignedById = sentinel atau filter CoacheeId IN (@WorkerA..@WorkerD) + AssignedAt window). Hapus dalam urutan: PendingProtonBypasses → AssessmentSessions fixture → ProtonFinalAssessments → ProtonDeliverableProgresses → ProtonTrackAssignments. JANGAN sentuh data lain — scope ketat ke 4 worker fixture + marker.

5. **`BEGIN TRAN;`** lalu INSERT 4 state:
   - **Worker A (komplit→CL-A):** 1 ProtonTrackAssignment IsActive=1 + ≥2 ProtonDeliverableProgress semua Status='Approved' + 1 ProtonFinalAssessment (ProtonTrackAssignmentId = assignment A).
   - **Worker B (partial→CL-B(a)/(b)):** 1 assignment aktif + 2 progress: satu 'Approved' satu 'Pending' + TANPA ProtonFinalAssessment.
   - **Worker C (punya final→tolak CL-B):** 1 assignment aktif + progress campur + 1 ProtonFinalAssessment.
   - **Worker D (pending E5):** 1 assignment aktif + 1 AssessmentSession (IsPassed=NULL atau 0, CompletedAt=NULL) + 1 PendingProtonBypass Status='Menunggu', Reason='Phase 361 Bypass Fixture — pending CL-B(b)', LinkedAssessmentSessionId = session yang baru di-insert (pakai SCOPE_IDENTITY()), TargetUnit diisi unit valid, SourceProtonTrackId/TargetProtonTrackId dari track resolved.
   Set `AssignedAt`/`CreatedAt` = SYSUTCDATETIME() supaya konsisten.

6. **`COMMIT;`**

7. **Verification SELECT (post-COMMIT, read-only)** — `PRINT` jumlah baris per worker + assert via SELECT count yang menampilkan: A punya final, B tanpa final, C punya final, D punya 1 pending Menunggu. Tujuan: executor bisa lihat hasil saat run manual.

JANGAN gunakan FK yang tak ada (CoacheeId/AssignedById no-FK — tapi tetap pakai @WorkerX valid). Pakai `SCOPE_IDENTITY()` untuk menyambung Id assignment→progress→final dan session→pending.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -b -i .planning\seeds\361-bypass-fixtures.sql 2>&1 | Select-String -Pattern "Phase 361|Msg|error" </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/seeds/361-bypass-fixtures.sql` ada
    - `grep -c "SET XACT_ABORT ON" .planning/seeds/361-bypass-fixtures.sql` = 1
    - `grep -c "THROW 5000" .planning/seeds/361-bypass-fixtures.sql` ≥ 1 (THROW guard user/track)
    - `grep -c "BEGIN TRAN" .planning/seeds/361-bypass-fixtures.sql` ≥ 1 dan `grep -c "COMMIT" ...` ≥ 1
    - `grep "Phase 361" .planning/seeds/361-bypass-fixtures.sql` menemukan marker cleanup/Reason
    - `grep -i "PendingProtonBypasses\|ProtonTrackAssignments\|ProtonDeliverableProgresses\|ProtonFinalAssessments" ...` menemukan keempat tabel
    - Menjalankan sqlcmd 2x berturut (idempotent) TIDAK menghasilkan "Msg" error pada run kedua (WIPE-AND-INSERT aman)
    - sqlcmd run mencetak PRINT verification tanpa error (exit code 0; `-b` flag aktif)
  </acceptance_criteria>
  <done>Fixture membuat 4 worker multi-state, idempotent (rerun aman), THROW guard aktif, sqlcmd run sukses tanpa error.</done>
</task>

<task type="auto">
  <name>Task 2: Catat entry di docs/SEED_JOURNAL.md (active)</name>
  <read_first>
    - docs/SEED_JOURNAL.md (format entry existing — tiru kolom/struktur; jika belum ada, ikuti format yang dirujuk SEED_WORKFLOW.md)
    - docs/SEED_WORKFLOW.md (field jurnal wajib: tujuan, klasifikasi, dampak entitas)
  </read_first>
  <action>
Tambah satu entry baru di `docs/SEED_JOURNAL.md` untuk fixture Phase 361. Isi (verbatim field):
- **Tanggal:** (tanggal eksekusi)
- **Slug/Phase:** Phase 361 — bypass-ui-b fixtures
- **File:** `.planning/seeds/361-bypass-fixtures.sql`
- **Tujuan:** Seed 4 worker multi-state (komplit CL-A, partial CL-B, punya final→tolak, pending E5) untuk e2e UAT bypass (PBYP-10).
- **Klasifikasi:** `temporary + local-only` (BUKAN permanent — tidak masuk Data/SeedData.cs).
- **Entitas tersentuh:** ProtonTrackAssignments, ProtonDeliverableProgresses, ProtonFinalAssessments, AssessmentSessions, PendingProtonBypasses (scope: 4 worker fixture + marker 'Phase 361').
- **Snapshot:** ditangani oleh spec e2e `beforeAll` (db.backup) — restore di `afterAll`.
- **Status:** `active` (akan diubah ke `cleaned` setelah e2e selesai restore — Plan 04).

JANGAN promosikan jadi permanent. Pertahankan entry jurnal existing tak berubah.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && Select-String -Path docs/SEED_JOURNAL.md -Pattern "Phase 361"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 361" docs/SEED_JOURNAL.md` ≥ 1
    - `grep "temporary + local-only" docs/SEED_JOURNAL.md` menemukan entry Phase 361
    - `grep "361-bypass-fixtures.sql" docs/SEED_JOURNAL.md` menemukan path file
    - Entry mengandung kata "active"
    - Entry jurnal existing (phase lain) masih ada — tidak terhapus
  </acceptance_criteria>
  <done>SEED_JOURNAL.md punya entry Phase 361 status active, klasifikasi temporary+local-only, daftar entitas tersentuh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer (lokal) → SQL Server lokal (HcPortalDB_Dev) | Eksekusi fixture SQL via sqlcmd; DB lokal SAJA (CLAUDE.md ❌ Dev/Prod) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-05 | Tampering (data pollution) | fixture INSERT ke DB | mitigate | WIPE-AND-INSERT by greppable marker 'Phase 361'; scope ketat 4 worker fixture; restore via snapshot di e2e afterAll (SEED_WORKFLOW) |
| T-361-06 | Denial of Service (FK violation crash) | INSERT dengan referensi user/track tak ada | mitigate | THROW guard subquery (anti Phase 309) + SET XACT_ABORT ON auto-rollback + BEGIN TRAN boundary |
| T-361-07 | Tampering (run di Dev/Prod) | sqlcmd target salah | accept | Header file + SEED_JOURNAL explicit "DB lokal SAJA"; CLAUDE.md DEV_WORKFLOW ❌ edit DB Dev/Prod; dbSnapshot.ts punya localhost-only guard |
</threat_model>

<verification>
- sqlcmd run fixture sukses (exit 0) + idempotent (rerun aman).
- 4 worker state terverifikasi via PRINT/SELECT count.
- SEED_JOURNAL.md entry tercatat (active).
- TIDAK ada perubahan Data/SeedData.cs (fixture temporary, bukan permanent).
</verification>

<success_criteria>
- `.planning/seeds/361-bypass-fixtures.sql` idempotent + THROW guard + BEGIN TRAN, membuat 4 worker multi-state.
- docs/SEED_JOURNAL.md entry Phase 361 status active, klasifikasi temporary+local-only.
- Fixture siap dipanggil oleh e2e spec (Plan 04).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/361-bypass-ui-b/361-02-SUMMARY.md`.
</output>
