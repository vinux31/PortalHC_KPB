---
phase: 361-bypass-ui-b
plan: 03
type: execute
wave: 2
depends_on: [361-01]
files_modified:
  - Views/ProtonData/Override.cshtml
autonomous: true
requirements: [PBYP-08, PBYP-09]
must_haves:
  truths:
    - "Page /ProtonData/Override punya 2 tab; Tab1 'Override Deliverable' markup & perilaku TIDAK berubah"
    - "Tab2 'Bypass Tahun' menampilkan panel pending (selalu tampil), filter cascade, tabel worker, dan wizard 3-langkah"
    - "Wizard linear: Lanjut disabled sampai step valid, bisa Kembali, step indicator 1-2-3, ringkasan+warning di step 3"
    - "4 closure mode card enable/disable per BypassDetail.eligibleModes"
    - "Deep-link ?tab=bypass&pending={id} membuka Tab2 + auto-open modal Lihat & Konfirmasi; pending stale → toast info"
    - "Semua POST (Save/Confirm/Cancel) disable tombol + spinner; sukses → toast + auto-refresh; gagal → toast merah pesan backend"
    - "Semua data server dirender via escHtml() saat innerHTML (XSS-safe)"
  artifacts:
    - path: "Views/ProtonData/Override.cshtml"
      provides: "2-tab shell + Tab2 Bypass UI lengkap (markup + JS IIFE + showToast helper)"
      contains: "pane-bypass"
      min_lines: 700
  key_links:
    - from: "Tab2 JS"
      to: "/ProtonData/BypassList, /BypassPendingList, /BypassDetail"
      via: "fetch GET appUrl()"
      pattern: "appUrl\\('/ProtonData/Bypass"
    - from: "Tab2 wizard submit + confirm/cancel"
      to: "/ProtonData/BypassSave, /BypassConfirm, /BypassCancelPending"
      via: "fetch POST + RequestVerificationToken header"
      pattern: "RequestVerificationToken"
    - from: "deep-link param"
      to: "Tab2 activation + confirm modal auto-open"
      via: "URLSearchParams + bootstrap.Tab().show()"
      pattern: "URLSearchParams|tab=bypass"
---

<objective>
Redesign `/ProtonData/Override` menjadi 2 tab dan bangun seluruh UI Tab2 "Bypass Tahun" dengan mengkonsumsi 6 endpoint JSON 360 (+ field D-18 dari Plan 01). Tab1 "Override Deliverable" markup & perilaku WAJIB tidak berubah (PBYP-08, D-01) — hanya dibungkus dalam `tab-pane`.

Tab2 berisi (D-01..D-21): panel "Menunggu Konfirmasi" (selalu tampil, D-16), filter cascade Bagian→Unit→Track + Muat Data (D-14), tabel worker dengan search nama client-side (D-15), wizard modal 3-langkah Tujuan→Closure→Detail (D-01/D-02/D-03), 4 closure-mode card enable/disable per eligibleModes (D-10), modal "Lihat & Konfirmasi" (D-18), deep-link routing (D-05/D-06/D-07/D-08), toast helper baru (Pitfall 1), anti-double-click spinner (D-21), escHtml XSS-safe (Security V5).

Purpose: Nilai utama fase = alur notif→aksi 1-klik (lonceng → Tab2 → modal konfirmasi langsung terbuka) + "jejak jujur" warning konsekuensi sebelum eksekusi.
Output: `Views/ProtonData/Override.cshtml` ter-redesign, build hijau, page runnable @5277.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/361-bypass-ui-b/361-CONTEXT.md
@.planning/phases/361-bypass-ui-b/361-RESEARCH.md
@.planning/phases/361-bypass-ui-b/361-PATTERNS.md
@.planning/phases/361-bypass-ui-b/361-UI-SPEC.md

<interfaces>
<!-- Kontrak endpoint (diekstrak ProtonDataController.cs:1499-1684) — executor JANGAN eksplorasi ulang -->

GET /ProtonData/BypassList?bagian=&unit=&trackId=  → BARE ARRAY (tanpa {success}):
  [{ coacheeId, nama, trackId, trackAktif, progressApproved, progressTotal, finalAda(bool) }]

GET /ProtonData/BypassPendingList  → BARE ARRAY (status Menunggu|Siap), EXTENDED Plan 01:
  [{ id, coacheeId, nama, sourceTrack, targetTrack, targetUnit, status, hasilExam(bool?),
     createdAt, skorExam(int?), tanggalExam(DateTime?), reason, targetCoachId, targetCoachNama }]

GET /ProtonData/BypassDetail?coacheeId=X  → OBJECT {success,...}:
  sukses: { success:true, sourceTrackId, sourceTahun(int Urutan global utk Δ≤1),
            sourceTahunKe(string label "Tahun 1"), sourceComplete(bool), sourceHasFinal(bool),
            eligibleModes:[ "CL-A"|"CL-B(a)"|"CL-B(b)"|"CL-C" ] }
  gagal:  { success:false, message }   (mis. assignment aktif != 1)

POST /ProtonData/BypassSave  [FromBody] BypassSaveRequest, header RequestVerificationToken:
  payload: { CoacheeId, SourceProtonTrackId(int), TargetProtonTrackId(int), TargetUnit,
             TargetCoachId(null=keep), Reason, Mode, DurationMinutes(null — D-09 default murni) }
  → { success, message, pendingId(int?), showAttachPackageReminder(bool) }

POST /ProtonData/BypassConfirm  [FromBody] {PendingId:int}  → { success, message }
POST /ProtonData/BypassCancelPending  [FromBody] {PendingId:int}  → { success, message }

Helper GLOBAL (sudah ada di _Layout.cshtml — JANGAN redefine):
  basePath = '@Url.Content("~/")'.replace(/\/$/,'')
  appUrl(path) → basePath + path

Helper sudah ada di Override.cshtml (REUSE — jangan duplikat):
  escHtml(str)  (:437-444)   — WAJIB untuk SEMUA data server di innerHTML
  statusToBadgeClass(status) (:381) — idiom badge

ViewBag (dari Plan 01): ViewBag.AllCoaches (List<ApplicationUser> — Id, FullName, UserName).
ViewBag existing: ViewBag.AllTracks (List<ProtonTrack> — Id, DisplayName, Urutan, TahunKe), ViewBag.SectionUnitsJson.
orgStructure const sudah di-inject di Override.cshtml:170 dari SectionUnitsJson — REUSE, jangan re-inject.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restruktur jadi 2 tab + markup statik Tab2 (panel pending, filter, worker table, wizard modal, confirm modal)</name>
  <read_first>
    - Views/ProtonData/Override.cshtml (SELURUH file — markup Tab1 :1-165 yang harus dibungkus utuh + overrideModal shell :75-165 sebagai template wizard)
    - .planning/phases/361-bypass-ui-b/361-PATTERNS.md (bagian "Nav-tabs shell pattern", "Filter cascade markup", "Wizard modal shell", "Empty-state placeholder")
    - .planning/phases/361-bypass-ui-b/361-UI-SPEC.md (Copywriting Contract — copy verbatim Bahasa Indonesia; Color — kelas badge/button; Component Inventory)
    - Views/Admin/ManageAssessment.cshtml:68-92 (nav-tabs markup — copy HANYA struktur, BUKAN atribut hx-* HTMX)
  </read_first>
  <action>
Di `Views/ProtonData/Override.cshtml`:

**A. Bungkus markup existing dalam tab shell** (D-01/PBYP-08). Tepat setelah `@Html.AntiForgeryToken()` (`:22`), bungkus SELURUH konten existing (filter card `:24-65` + `#overrideTableContainer` `:67-72` + `overrideModal` `:75-165`) ke dalam struktur tab:
```html
<ul class="nav nav-tabs mb-0" id="overrideTabs" role="tablist">
  <li class="nav-item" role="presentation">
    <button class="nav-link active" id="tab-deliverable" data-bs-toggle="tab" data-bs-target="#pane-deliverable"
            type="button" role="tab" aria-controls="pane-deliverable" aria-selected="true">
      <i class="bi bi-pencil-square me-1"></i> Override Deliverable
    </button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link" id="tab-bypass" data-bs-toggle="tab" data-bs-target="#pane-bypass"
            type="button" role="tab" aria-controls="pane-bypass" aria-selected="false">
      <i class="bi bi-arrow-left-right me-1"></i> Bypass Tahun
    </button>
  </li>
</ul>
<div class="tab-content pt-2" id="overrideTabsContent">
  <div class="tab-pane fade show active" id="pane-deliverable" role="tabpanel">
    <!-- MARKUP TAB1 EXISTING DIPINDAH KE SINI APA ADANYA (filter card + #overrideTableContainer + overrideModal) -->
  </div>
  <div class="tab-pane fade" id="pane-bypass" role="tabpanel">
    <!-- TAB2 markup baru (poin B-F) -->
  </div>
</div>
```
KRITIS: JANGAN ubah id/class/atribut markup Tab1. `overrideModal` boleh tetap di dalam `#pane-deliverable` atau dipindah ke root — yang penting tidak berubah perilaku. JANGAN tiru atribut `hx-get`/`hx-trigger` dari ManageAssessment (RESEARCH Pitfall 5 — Override = vanilla).

**B. Deklarasi `var allCoaches` di blok `@{}` kepala file** (`:6` setelah `allTracks`):
```cshtml
var allCoaches = ViewBag.AllCoaches as List<ApplicationUser> ?? new List<ApplicationUser>();
```

**C. Panel "Menunggu Konfirmasi" (D-16 selalu tampil)** di awal `#pane-bypass`:
- `card border-0 shadow-sm mb-4`, heading `Menunggu Konfirmasi (<span id="pendingCount">0</span>)`.
- Container `#pendingPanelContainer` untuk rows (di-render JS). Empty-state default: `Tidak ada bypass menunggu konfirmasi.`

**D. Filter cascade + tabel worker (D-14/D-15)** — copy struktur filter `Override.cshtml:24-65` dengan id distinct: `bypassBagian`, `bypassUnit`, `bypassTrack`, `btnLoadBypass` (TANPA Status Filter Tab1 yang tidak relevan). Track dropdown render dari `allTracks` (`@foreach`). Setelah filter, input search `#bypassSearch` placeholder `Cari nama pekerja...` + container `#bypassTableContainer` dengan empty-state pre-filter: `Pilih Bagian, Unit, dan Track lalu klik Muat Data untuk menampilkan pekerja.` (icon `bi bi-arrow-left-right fs-1 opacity-50`).

**E. Wizard modal 3-langkah (`#bypassWizardModal`, D-01/D-02/D-03)** — copy shell `overrideModal` (`:75-165`): `modal fade` + `modal-lg`. Isi:
- Step indicator 1-2-3 di header/atas body: `1 Tujuan` → `2 Closure Mode` → `3 Detail & Konfirmasi`.
- 3 `<div>` step (`#wizStep1`, `#wizStep2`, `#wizStep3`) — hanya 1 visible via JS:
  - Step1: `<select id="wizTargetTrack">` (render dari allTracks) + info Δtahun.
  - Step2: container `#wizModeCards` (4 card di-render JS dari eligibleModes).
  - Step3: TargetUnit cascade (`#wizTargetBagian` + `#wizTargetUnit`, WAJIB), Coach `<select id="wizCoach">` dengan option pertama `<option value="">Pertahankan coach saat ini</option>` lalu `@foreach (var c in allCoaches)` render `<option value="@c.Id">@(c.FullName ?? c.UserName)</option>`, `<textarea id="wizReason">` (WAJIB), container ringkasan `#wizRecap` + warning `#wizWarning`.
- Footer: `[Kembali]` (`#wizBack`), `[Lanjut]` (`#wizNext`, btn-primary, disabled), `[Jalankan Bypass]` (`#wizSubmit`, btn-primary, hidden sampai step 3).

**F. Modal "Lihat & Konfirmasi" (`#bypassConfirmModal`, D-18)** — read-only recap (worker, S→T, unit, coach, alasan, hasil+skor exam, tanggal) di container `#confirmModalBody` (di-render JS) + footer `[Konfirmasi Pindah]` (`#confirmBtnGo`, btn-success) + `[Batal]` (btn-outline-danger).

Pakai copy verbatim dari UI-SPEC Copywriting Contract. Kelas warna per UI-SPEC Color table. Markup Tab2 = kerangka data-driven (Pitfall 3: data di-fetch JS, bukan Razor model dynamic — hindari RuntimeBinder). JANGAN pecah ke partial `@model dynamic`.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"tab-deliverable\"\|id=\"tab-bypass\"" Views/ProtonData/Override.cshtml` = 2
    - `grep -c "id=\"pane-deliverable\"\|id=\"pane-bypass\"" Views/ProtonData/Override.cshtml` = 2
    - `grep "MARKUP TAB1\|overrideBagian\|btnLoadOverride" Views/ProtonData/Override.cshtml` — markup Tab1 (id `overrideBagian`, `btnLoadOverride`, `overrideModal`) MASIH ADA tak berubah
    - `grep -c "bypassBagian\|bypassUnit\|bypassTrack\|btnLoadBypass" Views/ProtonData/Override.cshtml` ≥ 4 (filter Tab2 distinct)
    - `grep "pendingPanelContainer\|pendingCount" Views/ProtonData/Override.cshtml` ditemukan (panel pending)
    - `grep "bypassWizardModal\|wizStep1\|wizStep2\|wizStep3" Views/ProtonData/Override.cshtml` ditemukan (wizard 3 step)
    - `grep "bypassConfirmModal\|confirmModalBody\|confirmBtnGo" Views/ProtonData/Override.cshtml` ditemukan (confirm modal)
    - `grep "Pertahankan coach saat ini" Views/ProtonData/Override.cshtml` ditemukan (D-12 default)
    - `grep "1 Tujuan\|2 Closure Mode\|3 Detail" Views/ProtonData/Override.cshtml` ditemukan (step indicator)
    - `grep -c "hx-get\|hx-trigger\|hx-target" Views/ProtonData/Override.cshtml` = 0 (TIDAK ada HTMX di Tab2)
    - `grep "ViewBag.AllCoaches as List" Views/ProtonData/Override.cshtml` ditemukan
    - `dotnet build` mengandung "Build succeeded" tanpa "error"
  </acceptance_criteria>
  <done>Page jadi 2 tab; Tab1 markup utuh; Tab2 markup lengkap (panel pending + filter + worker table + wizard 3-step modal + confirm modal); build hijau.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: JS IIFE Tab2 — showToast helper, deep-link routing, cascade, fetch render, wizard state, POST mutasi</name>
  <read_first>
    - Views/ProtonData/Override.cshtml (@section Scripts :167-445 — IIFE Tab1 existing yang harus dibiarkan utuh; pola fetch :226-252, POST :402-433, escHtml :437-444, cascade :191-219, statusToBadgeClass :381)
    - .planning/phases/361-bypass-ui-b/361-PATTERNS.md (semua bagian "Tab2 JS IIFE" + "Deep-link + programmatic tab activation" + "NEW HELPER REQUIRED showToast" + "CONTRACT DIFFERENCE bare array vs {success}")
    - .planning/phases/361-bypass-ui-b/361-RESEARCH.md (Pitfall 1 showToast tidak ada, Pitfall 3 runtime verify, Open Question 3 stale detection client-side)
    - .planning/phases/361-bypass-ui-b/361-UI-SPEC.md (Interaction Contract — semua state/async behavior; warning per-mode verbatim; copy)
  </read_first>
  <action>
Di `@section Scripts` `Views/ProtonData/Override.cshtml`, tambah IIFE Tab2 TERPISAH (setelah IIFE Tab1 existing — JANGAN ubah IIFE Tab1). Implementasi:

**A. `showToast(message, variant)` helper baru** (Pitfall 1 — tidak ada global toast). Buat fungsi yang inject markup `toast align-items-center text-bg-{variant} border-0 show position-fixed top-0 end-0 m-3` (variant: `success` hijau, `danger` merah, `warning`/`info` kuning), auto-dismiss ~4 detik. Letakkan di scope yang bisa dipanggil Tab2.

**B. Cascade filter Tab2** (copy `:191-219`) — `bypassBagian` change → repopulate `bypassUnit` dari `orgStructure[bagian]` (REUSE const `orgStructure` `:170`, JANGAN re-inject), enable/disable berurutan, `btnLoadBypass` disabled sampai track dipilih. Cascade SAMA dipakai untuk `wizTargetBagian`→`wizTargetUnit` (D-11).

**C. Load pending panel** `loadPendingPanel()` — `fetch GET appUrl('/ProtonData/BypassPendingList')` → BARE ARRAY. Render rows: nama (escHtml), S→T, badge status (D-17: DB `Menunggu`→`bg-warning text-dark` label "Menunggu Exam"; DB `Siap`→`bg-success` label "Siap Dikonfirmasi"), tombol `[Lihat & Konfirmasi]` (`btn-success btn-sm`) HANYA jika status==="Siap" (D-13), `[Batal]` (`btn-outline-danger btn-sm`) selalu. Update `#pendingCount`. 0 pending → empty-state. Simpan array hasil untuk deep-link stale check.

**D. Load worker table** `loadBypassWorkers()` (copy `:237-251`) — `fetch GET appUrl('/ProtonData/BypassList?...')` → BARE ARRAY. Render 5 kolom: nama (escHtml), track aktif, progress `progressApproved/progressTotal`, final ✓(`bg-success`)/✗(`bg-secondary` per `finalAda`), `[Bypass]` (`btn-outline-primary btn-sm`). Search nama client-side via `#bypassSearch` input (filter array, no pagination, D-15). Empty results → `Tidak ada pekerja untuk filter ini.` (`alert alert-info`). catch → `Terjadi kesalahan jaringan. Silakan coba lagi.`

**E. Wizard open + state machine** (D-01/D-02/D-03):
- `[Bypass]` row click → `fetch GET appUrl('/ProtonData/BypassDetail?coacheeId='+...)`. Jika `success:false` → showToast danger `data.message`. Jika sukses → simpan state (sourceTrackId, sourceTahun, eligibleModes, coacheeId, nama), buka `#bypassWizardModal` di step 1.
- Linear nav: `wizNext` disabled sampai step valid (step1: target track dipilih & `|targetTrack.Urutan - sourceTahun| <= 1` client-side; step2: 1 mode eligible dipilih; step3: TargetUnit terisi & Reason terisi). `wizBack` aktif. Toggle visibility step + highlight step indicator.
- Step2: render 4 card `CL-A`/`CL-B(a)`/`CL-B(b)`/`CL-C` di `#wizModeCards`; mode TIDAK di `eligibleModes` → card `opacity-50` + teks alasan muted (D-10, mis. "CL-A butuh semua deliverable approved + final ada"). Hanya mode eligible bisa dipilih (radio).
- Step3: cascade TargetUnit (reuse), coach select (default keep), reason textarea. Saat masuk step3 build `#wizRecap` (label:value — Pekerja, Perpindahan "Tahun X → Tahun Y", Closure Mode, Unit Tujuan, Coach, Alasan; SEMUA via escHtml) + `#wizWarning` `alert alert-warning` sesuai mode terpilih (VERBATIM dari UI-SPEC):
  - CL-A: `Worker dipindahkan instan dengan penanda kelulusan yang sudah ada.`
  - CL-B(a): `Deliverable akan di-force-approve dan penanda kelulusan diterbitkan tanpa nilai exam.`
  - CL-B(b): `Sesi exam dibuat tanpa paket soal. Worker baru pindah setelah exam dinilai lulus.`
  - CL-C: `Worker dipindahkan tanpa nilai (demosi/koreksi). Tidak ada penanda kelulusan.`

**F. Submit wizard** `wizSubmit` click (D-21 + D-04) — disable + spinner `Menyimpan...`. POST `appUrl('/ProtonData/BypassSave')` header `RequestVerificationToken` (token dari `input[name="__RequestVerificationToken"]`), body BypassSaveRequest (`DurationMinutes: null` — D-09). On `data.success`: tutup modal, `showToast(green)`, jika `data.showAttachPackageReminder` → showToast warning EXTRA `Sesi exam dibuat — lampirkan paket soal di Kelola Assessment sebelum pekerja bisa ujian.` (D-04), refresh worker table + pending panel. On `!success`: `showToast(danger, data.message)` verbatim (D-20). Restore tombol di finally.

**G. Confirm modal + actions** (D-18/D-19/D-20):
- `[Lihat & Konfirmasi]` → cari row pending {id} (dari array panel), render `#confirmModalBody` (worker, S→T, unit, coach `targetCoachNama`, alasan `reason`, hasil exam (Lulus/Gagal per hasilExam, skor `skorExam`), tanggal `tanggalExam` — SEMUA escHtml), buka `#bypassConfirmModal`.
- `[Konfirmasi Pindah]` → disable+spinner, POST `appUrl('/ProtonData/BypassConfirm')` body `{PendingId:id}`. Sukses → tutup, toast green, refresh pending+worker. Gagal → toast danger `data.message` + refresh pending (D-20), modal tutup.
- `[Batal]` pending → `confirm('Batalkan rencana bypass? Exam yang belum dikerjakan akan dihapus.')` (D-19); jika ok → POST `appUrl('/ProtonData/BypassCancelPending')` body `{PendingId:id}`, sama handling sukses/gagal.

**H. Tab activation + deep-link** (D-05/D-06/D-07/D-08):
- Lazy-load Tab2 sekali saat `shown.bs.tab` pertama (load pending panel; worker table tetap butuh Muat Data) (D-07).
- `history.replaceState(null,'',appUrl('/ProtonData/Override?tab=bypass'))` saat switch ke Tab2; `?tab=deliverable` saat balik (D-08).
- On page load: baca `URLSearchParams`. `tab=bypass` → `new bootstrap.Tab(document.getElementById('tab-bypass')).show()`. Jika ada `pending={id}`: setelah pending panel load, cari {id} di array → ketemu → auto-buka confirm modal untuk {id} (D-05); TIDAK ketemu (stale/Selesai/Dibatalkan) → `showToast(info, 'Pending bypass sudah diproses atau tidak ditemukan.')` + Tab2 normal (D-06).

REUSE `escHtml`/`appUrl`/`basePath`/`statusToBadgeClass` — JANGAN duplikat. Semua data server via escHtml di innerHTML (Security). JANGAN ubah IIFE Tab1.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "function showToast" Views/ProtonData/Override.cshtml` ditemukan (helper baru)
    - `grep -c "appUrl('/ProtonData/BypassList\|appUrl('/ProtonData/BypassPendingList\|appUrl('/ProtonData/BypassDetail" Views/ProtonData/Override.cshtml` ≥ 3 (fetch GET)
    - `grep -c "appUrl('/ProtonData/BypassSave\|appUrl('/ProtonData/BypassConfirm\|appUrl('/ProtonData/BypassCancelPending" Views/ProtonData/Override.cshtml` ≥ 3 (fetch POST)
    - `grep -c "RequestVerificationToken" Views/ProtonData/Override.cshtml` ≥ 1 (header pada POST Tab2; Tab1 existing juga punya — total ≥ 2)
    - `grep "URLSearchParams\|tab=bypass" Views/ProtonData/Override.cshtml` ditemukan (deep-link)
    - `grep "bootstrap.Tab(" Views/ProtonData/Override.cshtml` ditemukan (programmatic activation)
    - `grep "history.replaceState" Views/ProtonData/Override.cshtml` ditemukan (D-08)
    - `grep "Menunggu Exam\|Siap Dikonfirmasi" Views/ProtonData/Override.cshtml` ditemukan (D-17 label)
    - `grep "Pending bypass sudah diproses atau tidak ditemukan" Views/ProtonData/Override.cshtml` ditemukan (D-06 stale)
    - `grep "Sesi exam dibuat tanpa paket soal\|force-approve" Views/ProtonData/Override.cshtml` ditemukan (warning per-mode)
    - `grep "Batalkan rencana bypass" Views/ProtonData/Override.cshtml` ditemukan (D-19 confirm)
    - `grep "DurationMinutes" Views/ProtonData/Override.cshtml` — dikirim null atau dihilangkan (D-09; jika ada, nilainya null)
    - `grep -c "spinner-border" Views/ProtonData/Override.cshtml` ≥ 2 (anti-double-click D-21 + existing)
    - IIFE Tab1 existing utuh: `grep "OverrideList\|btnLoadOverride\|OverrideSave" Views/ProtonData/Override.cshtml` masih ada
    - `dotnet build` mengandung "Build succeeded" tanpa "error"
  </acceptance_criteria>
  <done>Tab2 JS lengkap: showToast helper, cascade, pending panel + worker table render (escHtml), wizard 3-step linear dengan 4 mode card + warning, POST Save/Confirm/Cancel anti-double-click, deep-link routing dengan stale handling; IIFE Tab1 utuh; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Tab2 JS) → ProtonDataController endpoints | fetch GET/POST dari HC/Admin; data user (nama, reason, track, coach) dirender ke DOM |
| server response → DOM innerHTML | data dari DB di-render via innerHTML — titik XSS |
| client form (wizard dropdown) → BypassSave | UI tidak bisa dipercaya; backend re-validasi (V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-08 | Tampering/Info disclosure (XSS) | render nama/reason/track/coach via innerHTML | mitigate | WAJIB `escHtml()` untuk SEMUA data server di innerHTML (pola Override.cshtml:437-444) — verified di acceptance |
| T-361-09 | Tampering (CSRF) | POST BypassSave/Confirm/CancelPending | mitigate | Header `RequestVerificationToken` dari `@Html.AntiForgeryToken()` field; backend `[ValidateAntiForgeryToken]` (:1609/1652/1671) |
| T-361-10 | Tampering (double-submit/race) | tombol POST diklik ganda | mitigate | Client disable+spinner (D-21) berlapis backend guard atomik D-12 (message "klik ganda" :516) |
| T-361-11 | Tampering (stale-state confirm) | konfirmasi pending yang state-nya basi | mitigate | Backend re-cek (D-11, message :505) → UI surface message + auto-refresh panel (D-20) |
| T-361-12 | Elevation (dropdown coach manipulasi) | pilih coach invalid via DOM | accept | Backend constraint E15 + re-validasi service menolak coach invalid — UI dropdown tidak bisa bypass server (dispatch authority backend) |
</threat_model>

<verification>
- `dotnet build` 0 error (gate compile setelah kedua task).
- Runtime verify WAJIB (Pitfall 3 — Lesson Phase 354): page Override harus dibuka via Playwright/browser @5277 (AD=false), bukan hanya build. Verifikasi runtime detail dilakukan di Plan 04 (e2e) + UAT MCP.
- Tab1 perilaku regresi: buka Tab1, jalankan filter + override existing → harus identik perilaku lama (Manual-Only Verification VALIDATION.md).
</verification>

<success_criteria>
- Page Override 2 tab; Tab1 markup & perilaku tak berubah (PBYP-08 SC1).
- Tab2 wizard Tujuan→Closure→Detail linear dengan ringkasan+warning (SC1).
- Panel pending tampil + [Konfirmasi]/[Batal]; deep-link buka Tab2 + auto-open modal pending (PBYP-09 SC2).
- Semua data server escHtml; semua POST anti-double-click + AntiForgery header.
- Page runnable @5277 tanpa 500 saat Tab2 dibuka.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/361-bypass-ui-b/361-03-SUMMARY.md`.
</output>
