---
phase: 361-bypass-ui-b
plan: 04
type: execute
wave: 3
depends_on: [361-02, 361-03]
files_modified:
  - tests/e2e/proton-bypass.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [PBYP-08, PBYP-09, PBYP-10]
must_haves:
  truths:
    - "Spec e2e committed proton-bypass.spec.ts ada dan menjalankan flow Tab2 (snapshot→seed→assert→restore)"
    - "Spec mengassert: 2 tab + Tab1 utuh, wizard nav 3-step, save per closure mode, panel pending + deep-link auto-open modal, batal pending, re-grade Pass→Fail badge balik Menunggu Exam"
    - "DB di-snapshot beforeAll + restore afterAll (sukses atau gagal) per SEED_WORKFLOW"
    - "SEED_JOURNAL.md entry Phase 361 ditandai cleaned setelah restore"
  artifacts:
    - path: "tests/e2e/proton-bypass.spec.ts"
      provides: "e2e regression coverage PBYP-08/09/10"
      contains: "Phase 361"
  key_links:
    - from: "proton-bypass.spec.ts beforeAll"
      to: "db.backup + db.execScript(361-bypass-fixtures.sql)"
      via: "dbSnapshot helper"
      pattern: "db.backup|execScript"
    - from: "proton-bypass.spec.ts"
      to: "/ProtonData/Override?tab=bypass deep-link + wizard flow"
      via: "Playwright login(hc) + page.goto"
      pattern: "tab=bypass"
---

<objective>
Tulis spec e2e committed `tests/e2e/proton-bypass.spec.ts` (D-22 lapis regresi) yang membuktikan lintas-stack UI Tab2 Bypass: snapshot DB → seed fixture (Plan 02) → jalankan flow di browser nyata @5277 → assert → restore. Lalu jalankan spec + UAT live MCP (4 closure mode + pending konfirmasi + batal + re-grade fail) sebagai checkpoint human-verify (PBYP-10).

Purpose: Lesson Phase 354 (MEMORY) — perubahan Razor/JS WAJIB diverifikasi runtime, build+grep tidak cukup. Spec committed = regresi; UAT MCP = bukti UAT. Restore DB jaga kebersihan lokal (SEED_WORKFLOW).
Output: `tests/e2e/proton-bypass.spec.ts` committed + SEED_JOURNAL ditandai cleaned + UAT approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/361-bypass-ui-b/361-CONTEXT.md
@.planning/phases/361-bypass-ui-b/361-RESEARCH.md
@.planning/phases/361-bypass-ui-b/361-PATTERNS.md
@.planning/phases/361-bypass-ui-b/361-VALIDATION.md

<interfaces>
<!-- Infra test (diekstrak codebase) -->

tests/helpers/auth.ts:4-11  → login(page, account) : goto /Account/Login, fill email/password, waitForURL **/Home/**
tests/helpers/accounts.ts   → keys: admin(admin@pertamina.com/123456), hc(meylisa.tjiang@pertamina.com/123456),
                              coachee(rino.prasetyo@pertamina.com/123456), coach. Pakai 'hc' (Authorize Roles="Admin,HC").
tests/helpers/dbSnapshot.ts:
  backup(path), restore(path), execScript(sqlPath), queryString(sql), queryScalar(sql)
  → resolve backup dir: queryString("SELECT CAST(SERVERPROPERTY('InstanceDefaultBackupPath') AS NVARCHAR(260))")
tests/playwright.config.ts  → testDir ./e2e, baseURL http://localhost:5277, globalTeardown RESTORE
Pola lengkap serial+snapshot lifecycle: tests/e2e/image-in-assessment.spec.ts:21-65

Endpoint/UI selector (dari Plan 03 markup):
  #tab-bypass / #pane-bypass, #pendingPanelContainer, #bypassWizardModal (#wizStep1/2/3, #wizNext/#wizBack/#wizSubmit),
  #bypassConfirmModal (#confirmBtnGo), tombol [Bypass] di worker table, [Lihat & Konfirmasi]/[Batal] di pending row.
Deep-link: /ProtonData/Override?tab=bypass  dan  ?tab=bypass&pending={id}

Re-grade trigger (D-24): /AssessmentAdmin/EditPesertaAnswers → RegradeAfterEditAsync (GradingService.cs:469).
  Pass→Fail → pending balik status "Menunggu" → badge "Menunggu Exam", tombol Konfirmasi hilang.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tulis tests/e2e/proton-bypass.spec.ts (serial + snapshot/seed/restore + assert flow)</name>
  <read_first>
    - tests/e2e/image-in-assessment.spec.ts:1-90 (pola header, serial config, beforeAll backup + resolve dir, afterAll restore sukses-atau-gagal)
    - tests/helpers/auth.ts (login signature) + tests/helpers/accounts.ts (akun + pwd)
    - tests/helpers/dbSnapshot.ts (backup/restore/execScript/queryString signature)
    - .planning/seeds/361-bypass-fixtures.sql (fixture dari Plan 02 — 4 worker state yang di-assert)
    - .planning/phases/361-bypass-ui-b/361-PATTERNS.md (bagian "tests/e2e/proton-bypass.spec.ts" — imports + lifecycle)
    - .planning/phases/361-bypass-ui-b/361-VALIDATION.md (Per-Task Verification Map — apa yang harus di-cover)
    - Views/ProtonData/Override.cshtml (selector id final dari Plan 03 — verifikasi nama id sebelum tulis assert)
  </read_first>
  <action>
Buat `tests/e2e/proton-bypass.spec.ts`. Struktur (tiru `image-in-assessment.spec.ts`):

1. **Header komentar:** tujuan (UAT Tab2 bypass PBYP-08/09/10), lesson 354 (runtime verify), SEED_WORKFLOW (snapshot/restore), PRECONDITION app @5277 `Authentication__UseActiveDirectory=false dotnet run`, auth (hc).

2. **Imports:** `{ test, expect }`, `* as db from '../helpers/dbSnapshot'`, `{ login } from '../helpers/auth'`, `path`.
   Const `SEED_SQL = path.resolve(__dirname, '../../.planning/seeds/361-bypass-fixtures.sql')`.

3. **`test.describe.configure({ mode: 'serial' })`** + `let snapshotPath`.

4. **`beforeAll`:** resolve `InstanceDefaultBackupPath` (pola :45-49), `snapshotPath = ${dir}/HcPortalDB_Dev-pre361-${ts}.bak`, `await db.backup(snapshotPath)`, lalu `await db.execScript(SEED_SQL)` (seed 4 worker fixture).

5. **`afterAll`:** `await db.restore(snapshotPath)` dalam try (sukses ATAU gagal), best-effort unlink snapshot. (Restore mengembalikan DB bersih — menutup SEED_JOURNAL cleaned di Task 3.)

6. **Test cases** (serial — assert minimal berikut; pembagian sub-test = discretion D-22):
   - **T1 — 2 tab + Tab1 utuh (PBYP-08 SC1):** login('hc'); `page.goto('/ProtonData/Override')`; expect `#tab-deliverable` active + `#tab-bypass` visible; klik Tab2 → `#pane-bypass` visible; verifikasi Tab1 filter masih ada (`#overrideBagian` exists). Smoke: page tidak 500 (status ok + no error banner).
   - **T2 — wizard nav 3-step (PBYP-08):** Tab2, klik `[Bypass]` Worker A → wizard buka step1; `[Lanjut]` disabled sampai target track dipilih; pilih → Lanjut; step2 card mode muncul (CL-A eligible untuk Worker A); pilih → Lanjut; step3 recap + warning tampil. Assert step indicator berpindah.
   - **T3 — save closure mode (PBYP-08):** untuk minimal Worker A (CL-A) dan Worker B (CL-B(b)): jalankan wizard penuh → submit → expect toast sukses; khusus CL-B(b) expect alert/toast kuning "lampirkan paket soal". (4 mode penuh = boleh di sub-test terpisah atau di-cover UAT MCP — minimal CL-A + CL-B(b) di spec committed.)
   - **T4 — panel pending + deep-link auto-open (PBYP-09 SC2):** Worker D fixture punya pending → load Tab2, expect `#pendingPanelContainer` row dengan badge "Menunggu Exam"; ambil pending id (dari fixture/DOM); `page.goto('/ProtonData/Override?tab=bypass&pending={id}')` → expect `#bypassConfirmModal` auto-visible (D-05). Stale: `pending=999999` → expect toast info "sudah diproses atau tidak ditemukan" (D-06).
   - **T5 — batal pending (PBYP-10):** pada pending Worker D klik `[Batal]` → confirm dialog (handle `page.on('dialog')` accept) → expect toast sukses + row hilang dari panel.
   - **T6 — re-grade Pass→Fail (PBYP-10, D-24):** siapkan pending yang Status="Siap" (exam lulus) lalu trigger re-grade via admin Edit Nilai → assert badge pending balik "Menunggu Exam" + tombol "Lihat & Konfirmasi" hilang. (Jika setup terlalu berat untuk spec, dokumentasikan sebagai UAT-MCP-only dan assert bagian yang feasible; logic sudah xUnit-covered 360.)

   Gunakan `login(page, 'hc')` untuk Tab2; `login(page, 'admin')` untuk trigger re-grade. App harus jalan AD=false @5277.

CATATAN: jika fixture id pending tidak deterministik, query via `db.queryScalar`/`queryString` untuk ambil PendingProtonBypass.Id milik Worker D (Reason LIKE 'Phase 361%'). Pembagian 1 file vs sub-test = discretion (D-22) — 1 file `proton-bypass.spec.ts` direkomendasikan.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff\tests" && npx playwright test proton-bypass.spec.ts --list 2>&1 | Select-String -Pattern "proton-bypass|Error|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/proton-bypass.spec.ts` ada
    - `grep "Phase 361" tests/e2e/proton-bypass.spec.ts` ditemukan (header)
    - `grep -c "db.backup\|db.restore\|db.execScript" tests/e2e/proton-bypass.spec.ts` ≥ 3 (snapshot lifecycle + seed)
    - `grep "361-bypass-fixtures.sql" tests/e2e/proton-bypass.spec.ts` ditemukan (path fixture)
    - `grep "mode: 'serial'" tests/e2e/proton-bypass.spec.ts` ditemukan
    - `grep -c "tab=bypass" tests/e2e/proton-bypass.spec.ts` ≥ 2 (deep-link + stale)
    - `grep "login(page, 'hc')" tests/e2e/proton-bypass.spec.ts` ditemukan
    - `grep "bypassConfirmModal\|pendingPanelContainer\|bypassWizardModal" tests/e2e/proton-bypass.spec.ts` ditemukan (selector Tab2)
    - `grep "Menunggu Exam" tests/e2e/proton-bypass.spec.ts` ditemukan (assert badge re-grade/pending)
    - `npx playwright test proton-bypass.spec.ts --list` listing test TANPA error parse/import
  </acceptance_criteria>
  <done>Spec committed dengan snapshot→seed→assert→restore; cover 2 tab/Tab1 utuh, wizard nav, save mode, pending deep-link, batal, re-grade; `--list` parse bersih.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT live MCP — 4 closure mode + pending konfirmasi + batal + deep-link + re-grade fail</name>
  <what-built>
UI Tab2 Bypass Tahun (`/ProtonData/Override`) + spec e2e committed `proton-bypass.spec.ts`. Claude sudah:
- Menjalankan `dotnet build` (0 error) dan menjalankan spec e2e @5277 (AD=false) dengan fixture seed + restore otomatis.
- Memverifikasi 2 tab, wizard, panel pending, deep-link, batal, re-grade via assert spec.
  </what-built>
  <action>Checkpoint verifikasi manusia — Claude pause setelah otomatisasi (build + spec e2e) selesai. Jalankan UAT live MCP sesuai langkah how-to-verify di bawah; tidak ada perubahan file pada task ini.</action>
  <how-to-verify>
PRA-SYARAT: jalankan app lokal dengan AD dimatikan:
1. Buka terminal di root project, jalankan: `Authentication__UseActiveDirectory=false dotnet run` (tunggu listening di http://localhost:5277).
2. (Opsional, jika ingin data fixture) seed: `sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -b -i .planning\seeds\361-bypass-fixtures.sql` — INGAT restore DB setelah selesai (SEED_WORKFLOW).

UAT MCP (login hc: meylisa.tjiang@pertamina.com / 123456):
3. Buka `http://localhost:5277/ProtonData/Override`. Konfirmasi 2 tab muncul; Tab1 "Override Deliverable" — jalankan filter + buka override existing → perilaku HARUS sama seperti sebelumnya (tidak rusak, PBYP-08 SC1).
4. Klik Tab2 "Bypass Tahun". Konfirmasi URL berubah `?tab=bypass` (D-08). Panel "Menunggu Konfirmasi (N)" tampil (walau 0 → empty state, D-16).
5. Filter Bagian→Unit→Track → Muat Data → tabel worker tampil. Search nama berfungsi.
6. Klik [Bypass] satu worker → wizard 3-langkah. Uji: Lanjut disabled sampai valid, bisa Kembali, step indicator 1-2-3, card mode disabled untuk mode tak-eligible (D-10), step 3 ada ringkasan + warning konsekuensi. Jalankan 4 closure mode (CL-A, CL-B(a), CL-B(b), CL-C) bila data memungkinkan → toast sukses; CL-B(b) → alert kuning "lampirkan paket soal".
7. Pending: klik [Lihat & Konfirmasi] (muncul hanya saat "Siap Dikonfirmasi") → modal detail (skor exam, tanggal, coach, alasan) → Konfirmasi Pindah. Klik [Batal] pending → konfirmasi dialog → exam dihapus.
8. Deep-link: buka `?tab=bypass&pending={id}` valid → modal konfirmasi auto-buka (D-05). `pending=999999` → toast info "sudah diproses atau tidak ditemukan" (D-06).
9. Re-grade fail: pada pending "Siap", ubah nilai exam jadi gagal via admin Edit Nilai → panel pending badge balik "Menunggu Exam", tombol Konfirmasi hilang (D-24).

Setelah selesai: RESTORE DB lokal (spec afterAll otomatis; jika seed manual, restore snapshot) + tandai SEED_JOURNAL cleaned (Task 3 / Plan summary).
  </how-to-verify>
  <resume-signal>Ketik "approved" jika 4 closure mode + pending konfirmasi + batal + deep-link + re-grade fail bekerja dan Tab1 tidak berubah. Jika ada masalah, deskripsikan untuk perbaikan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Tandai SEED_JOURNAL.md entry Phase 361 → cleaned</name>
  <read_first>
    - docs/SEED_JOURNAL.md (entry Phase 361 dari Plan 02 — ubah status active → cleaned)
  </read_first>
  <action>
Setelah e2e/UAT selesai dan DB di-restore, ubah status entry Phase 361 di `docs/SEED_JOURNAL.md` dari `active` menjadi `cleaned` (tambah timestamp restore). Konfirmasi DB lokal sudah bersih (fixture sudah ter-restore via spec afterAll atau restore manual). JANGAN ubah entry phase lain.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && Select-String -Path docs/SEED_JOURNAL.md -Pattern "Phase 361" -Context 0,3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Phase 361" docs/SEED_JOURNAL.md` masih ada
    - Entry Phase 361 mengandung "cleaned" (status diperbarui)
    - Entry phase lain tidak berubah
  </acceptance_criteria>
  <done>SEED_JOURNAL entry Phase 361 ditandai cleaned; DB lokal bersih.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright (test runner) → app @5277 + DB lokal | login HC/admin, snapshot/restore DB lokal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-13 | Tampering (DB pollution dari test) | seed fixture + flow create pending | mitigate | beforeAll backup + afterAll restore (sukses ATAU gagal) — DB kembali bersih; SEED_JOURNAL cleaned |
| T-361-14 | Info disclosure (kredensial test) | akun hc/admin pwd 123456 | accept | Akun DEV LOKAL saja (CLAUDE.md ❌ staging/prod); pola identik spec existing |
| T-361-15 | DoS (test jalan tanpa app) | spec gagal jika app tidak @5277 | accept | PRECONDITION terdokumentasi; checkpoint human-verify memastikan app jalan AD=false |
</threat_model>

<verification>
- `npx playwright test proton-bypass.spec.ts --list` parse bersih (gate tanpa run app).
- Full run (checkpoint): spec hijau @5277 (AD=false) + UAT MCP 4 mode + pending konfirmasi + batal + deep-link + re-grade fail.
- DB di-restore (afterAll) — lokal bersih; SEED_JOURNAL cleaned.
</verification>

<success_criteria>
- proton-bypass.spec.ts committed, cover PBYP-08/09/10, snapshot/restore + seed.
- UAT live (checkpoint) approved: 4 closure mode + pending konfirmasi + batal + re-grade fail (PBYP-10 SC3).
- Tab1 perilaku tidak berubah (PBYP-08 SC1).
- DB lokal bersih, SEED_JOURNAL cleaned.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/361-bypass-ui-b/361-04-SUMMARY.md`.
</output>
