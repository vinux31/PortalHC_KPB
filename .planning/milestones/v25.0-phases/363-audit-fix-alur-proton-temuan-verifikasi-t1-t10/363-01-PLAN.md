---
phase: 363-audit-fix-alur-proton-temuan-verifikasi-t1-t10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - HcPortal.Tests/ProtonApproveRejectParityTests.cs
autonomous: true
requirements: [T1, T2, T7]
must_haves:
  truths:
    - "ApproveDeliverable & RejectDeliverable existing behavior fully preserved after extraction (parity tests green on gold-standard path)"
    - "Approve/reject domain logic exists as a single shared, directly-testable seam (no duplicated state-mutation code)"
    - "Race-guard reload-fresh (D-10/T7) lives inside the approve core, not only at the call-site"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Static cores ApproveDeliverableCoreAsync / RejectDeliverableCoreAsync + shared notif dispatch; ApproveDeliverable/RejectDeliverable rewired to call them"
      contains: "ApproveDeliverableCoreAsync"
    - path: "HcPortal.Tests/ProtonApproveRejectParityTests.cs"
      provides: "Pin tests for gold-standard approve/reject end-state contract (real-SQL fixture)"
      contains: "ProtonApproveRejectParityTests"
  key_links:
    - from: "Controllers/CDPController.cs ApproveDeliverable (:818)"
      to: "ApproveDeliverableCoreAsync"
      via: "direct call replacing inline state-mutation"
      pattern: "ApproveDeliverableCoreAsync\\("
    - from: "Controllers/CDPController.cs RejectDeliverable (:978)"
      to: "RejectDeliverableCoreAsync"
      via: "direct call replacing inline reset block"
      pattern: "RejectDeliverableCoreAsync\\("
---

<objective>
Kill the root cause of drift T1/T2/T7 (duplicate approve/reject endpoint logic) by extracting the gold-standard domain logic of `ApproveDeliverable`/`RejectDeliverable` into a single, directly-testable seam — WITHOUT changing the behavior of the gold-standard endpoints. This is the safe half of D-01: extract + prove no-regression via pin tests, BEFORE the FromProgress endpoints are rewired in Plan 02.

This plan does NOT touch `ApproveFromProgress`/`RejectFromProgress` (still buggy) — that is Plan 02. By isolating the behavior-preserving extraction here, the risky behavioral change in Plan 02 lands on top of a test-pinned core.

Purpose: Make drift structurally impossible (one code path per action) and create the executable contract (pin tests) that Plan 02 must satisfy.
Output: Static cores `ApproveDeliverableCoreAsync`/`RejectDeliverableCoreAsync` + shared private notif dispatch on CDPController; gold-standard endpoints rewired; `ProtonApproveRejectParityTests.cs` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-FINDINGS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-CONTEXT.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-RESEARCH.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-PATTERNS.md

<interfaces>
<!-- Seam DECIDED by planner (CONTEXT discretion "helper form: planner pilih, perhatikan testability").
     Static methods on ApplicationDbContext = directly testable WITHOUT constructing CDPController/UserManager
     (CDPControllerAuthTests uses reflection precisely because controller construction is impractical). -->

Target seam to create in CDPController.cs:

```csharp
// State-mutation cores — STATIC, only need ApplicationDbContext → directly testable via ProtonCompletionFixture.
// allApproved is COMPUTED and RETURNED; notif dispatch stays in the (instance) endpoints via shared helper below.
internal static async Task<(bool ok, string? error, bool allApproved)> ApproveDeliverableCoreAsync(
    ApplicationDbContext context, int progressId,
    string actorId, string actorName, string actorRole, bool isSrSpv, bool isSH);

internal static async Task<(bool ok, string? error)> RejectDeliverableCoreAsync(
    ApplicationDbContext context, int progressId,
    string actorId, string actorName, string actorRole, string rejectionReason);

// Shared notif dispatch — INSTANCE (needs _userManager/_notificationService). Both approve endpoints call it.
private async Task DispatchApproveNotificationsAsync(ProtonDeliverableProgress progress, bool allApproved);

// De-dup status-history insert so the static core and existing RecordStatusHistory share one insert.
internal static void AddDeliverableStatusHistory(ApplicationDbContext context,
    int progressId, string statusType, string actorId, string actorName, string actorRole, string? reason = null);
```

Existing gold-standard logic the cores must absorb VERBATIM (PATTERNS.md Pattern A/B/C/D):
- Per-role approval set: CDPController.cs:868-880
- Race-guard reload-fresh (D-10): CDPController.cs:882-901
- allApproved check (load all progress, order, .All(Status=="Approved")): CDPController.cs:908-931
- Full chain reset (reject): CDPController.cs:1021-1037 (incl. HCApprovalStatus + HCReviewedById/At → Pending/null)
- RecordStatusHistory current private impl: CDPController.cs:3559-3571
- CreateHCNotificationAsync (HC broadcast, reused by dispatch): CDPController.cs:1111-1140

Ctor deps already available (no DI change) — CDPController.cs:33-50:
  _userManager, _context, _notificationService, _logger, _auditLog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract static cores + shared notif dispatch; rewire gold-standard endpoints (behavior-preserving)</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs:818-973 (ApproveDeliverable — gold standard, read FULLY: authz/section-check :858-862, role-set :868-880, race-guard :882-901, allApproved :908-931, notif coach/coachee :935-964, HC notif :966-969)
    - Controllers/CDPController.cs:978-1080 (RejectDeliverable — full chain reset :1021-1037)
    - Controllers/CDPController.cs:1111-1140 (CreateHCNotificationAsync — reuse as-is)
    - Controllers/CDPController.cs:3559-3571 (RecordStatusHistory current impl)
    - 363-PATTERNS.md "Helper approve-core / reject-core" section (Pattern A/B/C/D + "WAJIB perhatikan saat extract")
    - 363-RESEARCH.md "Pola 1: Helper Extraction yang Preserve Dua Return Style"
  </read_first>
  <action>
Create the seam declared in &lt;interfaces&gt; and rewire ONLY the two gold-standard endpoints. Behavior MUST be byte-for-byte equivalent — this is an extraction, not a behavior change.

1. Add `internal static void AddDeliverableStatusHistory(ApplicationDbContext context, int progressId, string statusType, string actorId, string actorName, string actorRole, string? reason = null)` — move the body of RecordStatusHistory (:3559-3571) here. Rewrite existing instance `RecordStatusHistory(...)` to a thin wrapper: `AddDeliverableStatusHistory(_context, progressId, statusType, actorId, actorName, actorRole, reason);` so all existing callers stay valid.

2. Add `internal static async Task<(bool ok, string? error, bool allApproved)> ApproveDeliverableCoreAsync(ApplicationDbContext context, int progressId, string actorId, string actorName, string actorRole, bool isSrSpv, bool isSH)` containing, in this exact order (copied from :868-931):
   - Load tracked `progress` by id (+ the include chain needed for trackId); if null return `(false, "Deliverable tidak ditemukan.", false)`.
   - Per-role approval set (Pattern A, :868-880) using actorRole/isSrSpv/isSH + actorId.
   - Race-guard reload-fresh AsNoTracking + `stillCanApprove` (Pattern B, :882-901); if not still-approvable return `(false, "Deliverable sudah diproses oleh approver lain.", false)`.
   - Set overall `Status="Approved"` + ApprovedById/At (:903-906).
   - `AddDeliverableStatusHistory(context, ...)` for the approve event.
   - allApproved computation (Pattern C, :908-931).
   - `await context.SaveChangesAsync();`
   - return `(true, null, allApproved)`.

3. Add `internal static async Task<(bool ok, string? error)> RejectDeliverableCoreAsync(ApplicationDbContext context, int progressId, string actorId, string actorName, string actorRole, string rejectionReason)` containing the FULL chain reset (Pattern D, :1021-1037) — Status="Rejected" + RejectedAt + RejectionReason + ApprovedById/At null + SrSpv/SH/HC ALL → "Pending" with null ID/timestamp (INCLUDING HCApprovalStatus + HCReviewedById/At). Add status history, SaveChanges, return `(true, null)`. NOTE: do NOT set any approver-id on reject (the RejectFromProgress anomaly :2057-2059 must NOT be replicated here).

4. Add `private async Task DispatchApproveNotificationsAsync(ProtonDeliverableProgress progress, bool allApproved)` — move the coach/coachee `COACH_EVIDENCE_APPROVED` notif block (:935-964) here, then `if (allApproved) await CreateHCNotificationAsync(progress.CoacheeId);` (:966-969).

5. Rewire `ApproveDeliverable` (:818): keep authz/section-check/`_userManager.GetUserAsync`/role-resolve/response shaping in the endpoint. Replace the inline state-mutation block with `var (ok, error, allApproved) = await ApproveDeliverableCoreAsync(_context, progressId, user.Id, user.FullName, userRole, isSrSpv, isSH);` then on `ok` call `await DispatchApproveNotificationsAsync(progress, allApproved);` (reload `progress` if needed for notif recipients) and preserve the existing TempData/RedirectToAction. On `!ok` preserve existing error redirect using `error`.

6. Rewire `RejectDeliverable` (:978): keep authz/section-check/`rejectionReason` empty-check/response shaping; replace inline reset with `var (ok, error) = await RejectDeliverableCoreAsync(_context, progressId, user.Id, user.FullName, userRole, rejectionReason);` preserving existing redirect/TempData.

DO NOT touch ApproveFromProgress/RejectFromProgress (Plan 02). DO NOT change section-check (Pitfall 1 — stays per-endpoint, divergence intentional). DO NOT change A-2 semantics (1 approver L4 enough, HC not approver).
  </action>
  <acceptance_criteria>
    - `grep -n "ApproveDeliverableCoreAsync" Controllers/CDPController.cs` returns the definition AND a call inside ApproveDeliverable.
    - `grep -n "RejectDeliverableCoreAsync" Controllers/CDPController.cs` returns the definition AND a call inside RejectDeliverable.
    - `grep -n "AddDeliverableStatusHistory" Controllers/CDPController.cs` shows static helper + RecordStatusHistory wrapper still present.
    - `grep -n "HCApprovalStatus = \"Pending\"" Controllers/CDPController.cs` — the reject reset including HC appears inside RejectDeliverableCoreAsync.
    - `grep -nc "ApproveFromProgress\|RejectFromProgress" Controllers/CDPController.cs` — these method signatures still exist UNCHANGED (no edits this plan).
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>Cores + dispatch + static status-history helper exist; ApproveDeliverable/RejectDeliverable call the cores; FromProgress endpoints untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Pin tests — ProtonApproveRejectParityTests.cs (gold-standard contract, real-SQL fixture)</name>
  <files>HcPortal.Tests/ProtonApproveRejectParityTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonCompletionServiceTests.cs:25-115 (ProtonCompletionFixture pattern + seeding ProtonTrack/ProtonTrackAssignment; reuse ProtonTracks seeded by migration, unique coacheeId per fact)
    - HcPortal.Tests/ProtonYearGateIntegrationTests.cs:1-78 (IClassFixture usage + seeding ProtonDeliverable chain + ProtonDeliverableProgress)
    - 363-RESEARCH.md "Validation Architecture → Per Test Map" (T1/T2/T7 rows)
    - Controllers/CDPController.cs ApproveDeliverableCoreAsync / RejectDeliverableCoreAsync (just created — these are the System-Under-Test)
  </read_first>
  <action>
Create `HcPortal.Tests/ProtonApproveRejectParityTests.cs` as `[Trait("Category","Integration")]` using `IClassFixture<ProtonCompletionFixture>`. Call the static cores DIRECTLY (no controller construction). Seed a track + ProtonKompetensi/SubKompetensi/Deliverable chain + ProtonDeliverableProgress rows (model on ProtonYearGateIntegrationTests :104-127). Unique coacheeId per fact (`$"par-{Guid.NewGuid():N}"`).

Facts (these PIN the gold-standard end-state contract — they MUST pass on the code from Task 1):
1. `RejectCore_ResetsFullChain_IncludingHC`: seed a progress with SrSpvApprovalStatus="Approved", ShApprovalStatus="Approved", HCApprovalStatus="Reviewed" + non-null HCReviewedById; call `RejectDeliverableCoreAsync(...)`; assert reloaded progress: Status=="Rejected", SrSpvApprovalStatus=="Pending" && SrSpvApprovedById==null, ShApprovalStatus=="Pending" && ShApprovedById==null, HCApprovalStatus=="Pending" && HCReviewedById==null && HCReviewedAt==null, RejectionReason set.
2. `ApproveCore_LastDeliverable_ReturnsAllApprovedTrue`: seed N deliverables for one track, all "Approved" except one "Submitted"; call `ApproveDeliverableCoreAsync(...)` on the last as SrSpv; assert returns ok==true && allApproved==true, and progress.Status=="Approved".
3. `ApproveCore_NotLast_ReturnsAllApprovedFalse`: same but another deliverable still "Submitted"; assert allApproved==false.
4. `ApproveCore_RaceGuard_RejectsStaleSecondApprove`: set the row to a state where stillCanApprove==false (e.g., Status already "Approved" and role already approved); call core; assert ok==false && error mentions "diproses oleh approver lain".

Do NOT assert notif here (notif dispatch needs UserManager — covered by Plan 07 UAT). These facts pin STATE mutation only — that is the drift surface for T2/T7 and the allApproved flag for T1.
  </action>
  <acceptance_criteria>
    - File `HcPortal.Tests/ProtonApproveRejectParityTests.cs` exists with `[Trait("Category", "Integration")]` and 4 `[Fact]`.
    - `grep -n "RejectCore_ResetsFullChain_IncludingHC\|ApproveCore_LastDeliverable_ReturnsAllApprovedTrue\|ApproveCore_RaceGuard" HcPortal.Tests/ProtonApproveRejectParityTests.cs` returns 3+ matches.
    - `dotnet test --filter "FullyQualifiedName~ProtonApproveRejectParity"` → all green (requires localhost\SQLEXPRESS).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonApproveRejectParity"</automated>
  </verify>
  <done>Pin tests exist and pass against the extracted gold-standard cores, proving the extraction preserved behavior and locking the contract Plan 02 must satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach/L4 reviewer → CDPController approve/reject endpoints | Authenticated reviewer submits approve/reject; server mutates approval chain state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-363-07 | Tampering | ApproveDeliverableCoreAsync (concurrent approvers) | mitigate | Race-guard reload-fresh AsNoTracking + stillCanApprove re-check moved INSIDE the core (D-04/T7) — both endpoints inherit it in Plan 02 |
| T-363-02a | Tampering (state integrity) | RejectDeliverableCoreAsync | mitigate | Full chain reset including HCApprovalStatus + null HCReviewedById/At (D-03) — no stale "Reviewed" survives a reject |
| T-363-AUTHZ | Elevation of Privilege | Approve/reject authz | accept (preserved) | `[Authorize(RolesReviewerAndAbove)]` + HasSectionAccess(level==4) + section-check stay in endpoints unchanged (A-2 locked); extraction does not touch authz |
</threat_model>

<verification>
- `dotnet build` 0 errors.
- `dotnet test --filter "FullyQualifiedName~ProtonApproveRejectParity"` green.
- Code review: ApproveDeliverable/RejectDeliverable produce identical state to pre-extraction (extraction is behavior-preserving); FromProgress endpoints unchanged.
</verification>

<success_criteria>
- Single shared seam for approve/reject state mutation exists and is directly unit-testable.
- Gold-standard endpoints rewired with zero behavior change (pin tests green).
- Race-guard and full HC-inclusive reset live in the core (foundation for T1/T2/T7 completion in Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-01-SUMMARY.md`
</output>
