---
phase: 363-audit-fix-alur-proton-temuan-verifikasi-t1-t10
plan: 05
type: execute
wave: 2
depends_on: [03]
files_modified:
  - Controllers/CoachMappingController.cs
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/ProtonYearGateIntegrationTests.cs
autonomous: true
requirements: [T3, T9, T10]
must_haves:
  truths:
    - "Reactivating an inactive cross-year assignment now runs the year-gate — a Tahun N assignment cannot be reactivated unless Tahun N-1 is passed (T3)"
    - "An inactive assignment with Origin='Bypass' stays exempt from the reactivation gate (D-06, permanent stamp)"
    - "An already-ACTIVE assignment still skips the gate (no behavior change for valid actives)"
    - "When prev-track resolution returns null while Urutan > 1, both call-sites log a warning (T9) — gate still runs, no throw"
    - "BackfillProtonPenanda documents that having no year-gate is by-design (T10) — zero logic change"
  artifacts:
    - path: "Controllers/CoachMappingController.cs"
      provides: "Refined cabang-1 (active-only skip) + reactivation gate + reactExempt (Origin=Bypass inactive) + T9 log-warn"
      contains: "reactExempt"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "T9 prev-track log-warn + T10 by-design comment in BackfillProtonPenanda"
      contains: "by-design"
    - path: "HcPortal.Tests/ProtonYearGateIntegrationTests.cs"
      provides: "Reactivation gate predicate facts (blocked / bypass-exempt / active-skip)"
      contains: "Reactivation"
  key_links:
    - from: "Controllers/CoachMappingController.cs reactivation gate (pre-tx :516-552)"
      to: "ProtonCompletionService.IsPrevYearPassedAsync"
      via: "gate now reached by reactivation candidates (inactive, non-bypass)"
      pattern: "IsPrevYearPassedAsync"
---

<objective>
Close the T3 HIGH loophole: today, `CoachCoacheeMappingAssign` cabang-1 (`:517-528`) skips the year-gate for ANY coachee who has an assignment (active OR inactive) for the requested track — so a Tahun 2 assignment deactivated earlier can be reactivated (`:597-606`) without ever checking Tahun 1, escaping the Phase 359 D-05 hard-block.

Per D-05/D-06 (LOCKED): refine cabang-1 so it skips only ALREADY-ACTIVE assignments; route reactivation candidates (inactive-only assignment for the track) through the same year-gate, EXEMPTING those whose inactive assignment has `Origin="Bypass"` (permanent bypass stamp, consistent with Phase 360 D-04). Open Q1 resolution: this does NOT contradict the Phase 360 "JANGAN ubah cabang 1" comment (360 forbade changing the bypass EXEMPT; 363 ADDS a gate on the reactivation path) — update the comment to describe the new design.

Also lands the two LOW items: T9 (defensive log-warn when prev-track resolution is null while Urutan>1, at both call-sites) and T10 (by-design comment in BackfillProtonPenanda, zero logic change).

Purpose: No reactivation escape from the cross-year prerequisite; defensive logging; documented triage closure.
Output: CoachMapping reactivation gate + T9 logs (2 points) + T10 comment + reactivation predicate tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-FINDINGS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-CONTEXT.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-RESEARCH.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-PATTERNS.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md

<interfaces>
Reuse (no change): `_protonCompletionService.IsPrevYearPassedAsync(coacheeId, trackType, prevTahunKe)` — Services/ProtonCompletionService.cs:107.

Current loophole — CoachMappingController.cs (verified lines):
- :517-520 `hasForRequestedTrack` — query WITHOUT `IsActive` filter.
- :528 `if (hasForRequestedTrack.Contains(coacheeId)) continue;` — cabang-1 blanket skip (the loophole).
- :531-534 the "JANGAN ubah cabang 1" comment (Phase 360 W-07/I-08).
- :535-538 existing exempt: `IsActive && Origin == "Bypass"`.
- :542-544 gate predicate; :548-552 hard-block message.
- :597-606 reactivation reuse block (sets inactive assignment IsActive=true). Inside the tx.

Error message pattern (hard-block 359 D-05, :550-551):
`$"Tidak bisa assign {requestedTrack.TahunKe}: {prevTrack.TahunKe} ({requestedTrack.TrackType}) belum lulus untuk {N} coachee."`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine cabang-1 + add reactivation gate + reactExempt (T3/D-05/D-06)</name>
  <files>Controllers/CoachMappingController.cs</files>
  <read_first>
    - Controllers/CoachMappingController.cs:500-614 (full gate block + reactivation reuse — read to confirm pre-tx vs in-tx structure)
    - 363-PATTERNS.md "CoachMappingController — T3 Gate Reaktivasi" (loophole + reactExempt query + recommended pre-tx location + comment reconciliation)
    - 363-RESEARCH.md "T3 (Wave 2 — CoachMapping)" deep notes + Open Question 1
    - 363-CONTEXT.md D-05, D-06; 360-CONTEXT.md D-04 (permanent bypass stamp)
  </read_first>
  <action>
Edit the PRE-tx gate block (:516-552) so reactivation candidates are gated. Keep all changes in the pre-tx block so a block returns `Json(success=false)` without rollback (RESEARCH A5).

1. Change `hasForRequestedTrack` (:517-520) to filter `&& a.IsActive` — rename to `activeForRequestedTrack`. This set now means "already active for this track" (true no-op).
2. In the loop, replace `if (hasForRequestedTrack.Contains(coacheeId)) continue;` (:528) with `if (activeForRequestedTrack.Contains(coacheeId)) continue;` (already-active → skip gate, status quo).
3. Keep the existing `isExemptFromCrossYear` (active Bypass, :535-538) check + `continue`.
4. AFTER that exempt check and BEFORE the `IsPrevYearPassedAsync` predicate (:542), add the reactivation exempt:
   ```csharp
   // T3/D-06: reaktivasi-candidate (punya assignment INACTIVE utk track diminta, akan direaktivasi :597-606)
   //          exempt bila inactive assignment ber-Origin="Bypass" (stempel permanen, konsisten 360 D-04).
   bool reactExempt = await _context.ProtonTrackAssignments
       .AnyAsync(a => a.CoacheeId == coacheeId && a.ProtonTrackId == requestedTrack.Id && !a.IsActive && a.Origin == "Bypass");
   if (reactExempt) continue;
   ```
   The existing `IsPrevYearPassedAsync` predicate now also gates reactivation candidates (inactive, non-bypass) → added to `incompleteCoachees` → hard-block `:548-552` (unchanged message).
5. Update the comment block :531-534: replace "JANGAN ubah cabang 1 ... tanpa filter IsActive" with the new design — cabang-1 now skips ONLY active assignments; reactivation candidates run the gate; bypass (active or inactive) exempt via the two exempt checks. Cite T3/D-05/D-06 + 360 D-04. (Reconciles Open Q1.)

Do NOT touch the reactivation reuse block at :597-606 (it executes after the gate has passed). Do NOT alter the hard-block message or A-2 semantics.
  </action>
  <acceptance_criteria>
    - `grep -n "activeForRequestedTrack" Controllers/CoachMappingController.cs` returns the renamed set (with `&& a.IsActive` in its query).
    - `grep -n "hasForRequestedTrack" Controllers/CoachMappingController.cs` → NO match (renamed).
    - `grep -n "reactExempt" Controllers/CoachMappingController.cs` returns the new inactive-Bypass exempt query (`!a.IsActive && a.Origin == \"Bypass\"`).
    - `grep -n "JANGAN ubah cabang 1" Controllers/CoachMappingController.cs` → NO match (comment updated to new design).
    - `grep -n "IsPrevYearPassedAsync" Controllers/CoachMappingController.cs` still present (gate intact).
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>Reactivation candidates are gated; active assignments still skip; inactive Bypass exempt; comment reflects the new design.</done>
</task>

<task type="auto">
  <name>Task 2: T9 log-warn at both prev-track resolution points</name>
  <files>Controllers/CoachMappingController.cs, Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/CoachMappingController.cs:506-510 (prevTrack resolve)
    - Controllers/AssessmentAdminController.cs:1348-1352 (prevTahunKe resolve — confirm exact lines)
    - 363-PATTERNS.md "T9 Log-Warn" (both titik + existing _logger usage)
    - 363-CONTEXT.md D-12 (log only, no throw/block; Urutan=1 → null is valid Tahun 1)
  </read_first>
  <action>
1. CoachMappingController :506-510: after resolving `prevTrack`, add:
   ```csharp
   if (requestedTrack.Urutan > 1 && prevTrack == null)
       _logger.LogWarning("CoachCoacheeMappingAssign: prev-track null padahal Urutan={Urutan} > 1 (TrackType={TrackType}) — Urutan tidak kontigu di master ProtonTracks. Gate untuk track ini dilewati.", requestedTrack.Urutan, requestedTrack.TrackType);
   ```
   (Place so the gate logic continues unchanged — for Urutan=1 prevTrack==null is normal, no warning.)
2. AssessmentAdminController :1348-1352: after resolving `prevTahunKe`, add:
   ```csharp
   if (protonUrutan > 1 && prevTahunKe == null)
       _logger.LogWarning("CreateAssessment gate: prevTahunKe null padahal protonUrutan={Urutan} > 1 (TrackType={TrackType}) — Urutan tidak kontigu. Cross-year gate dilewati untuk track ini.", protonUrutan, trackType);
   ```
   No throw, no block. Confirm both controllers already have an `_logger` field (CoachMapping `_logger`, AssessmentAdmin `_logger`).
  </action>
  <acceptance_criteria>
    - `grep -n "Urutan tidak kontigu" Controllers/CoachMappingController.cs Controllers/AssessmentAdminController.cs` returns 2 matches (one per file).
    - No `throw` or early `return` added near these log lines (gate behavior unchanged): review confirms log-only.
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>Both prev-track resolution sites emit a warning on non-contiguous Urutan; gate logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: T10 by-design comment in BackfillProtonPenanda + reactivation predicate tests (T3)</name>
  <files>Controllers/AssessmentAdminController.cs, HcPortal.Tests/ProtonYearGateIntegrationTests.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:3909-3960 (BackfillProtonPenanda header + 100% enforce block — confirm lines)
    - HcPortal.Tests/ProtonYearGateIntegrationTests.cs:64-163 (existing predikat-replikasi pattern: SkippedByCrossYearGateAsync + facts — extend in the SAME style, NewSvc now has 4-arg ctor after Plan 03)
    - 363-CONTEXT.md D-13 (T10 by-design); 363-FINDINGS.md T10
  </read_first>
  <action>
1. T10: In `BackfillProtonPenanda` (AssessmentAdminController :3909+), add an explicit comment near the 100%-enforce block stating no year-gate is by-design: e.g. `// T10/D-13 (by-design): Backfill SENGAJA tanpa year-gate — menambal data historis pre-Phase 358 yang lulus exam sungguhan; year-gate baru bermakna setelah penanda lengkap. Enforce 100% deliverable Approved tetap berlaku.` Zero logic change.
   Also append a note to 363-FINDINGS.md T10 section recording the by-design decision (D-13).
2. T3 tests: extend `ProtonYearGateIntegrationTests.cs` mirroring the existing predikat-replikasi style. Add a private helper that REPLICATES the refined CoachMapping reactivation gate EXACTLY (active-skip → active-bypass-exempt → reactExempt inactive-bypass → IsPrevYearPassedAsync):
   ```csharp
   private static async Task<bool> ReactivationBlockedAsync(ApplicationDbContext ctx, ProtonCompletionService svc, string uid, int requestedTrackId, string trackType, string? prevTahunKe)
   ```
   Add facts (unique coacheeId per fact `$"react-{Guid.NewGuid():N}"`):
   - `Reactivation_InactiveNonBypass_NoPrevYear_Blocked`: inactive Tahun2 (Origin=null), no Tahun1 penanda → ReactivationBlockedAsync == true.
   - `Reactivation_InactiveBypass_Exempt`: inactive Tahun2 Origin="Bypass", no Tahun1 → ReactivationBlockedAsync == false.
   - `Reactivation_ActiveAssignment_SkipsGate`: active Tahun2 (no penanda) → ReactivationBlockedAsync == false (regression: actives never blocked).
   Keep the replicated predicate byte-identical to the controller logic from Task 1 (note this in a comment, per the established file convention's known weakness).
  </action>
  <acceptance_criteria>
    - `grep -n "T10/D-13" Controllers/AssessmentAdminController.cs` returns the by-design comment.
    - 363-FINDINGS.md T10 section contains a "by-design (D-13)" note.
    - `grep -n "ReactivationBlockedAsync\|Reactivation_InactiveNonBypass_NoPrevYear_Blocked\|Reactivation_InactiveBypass_Exempt\|Reactivation_ActiveAssignment_SkipsGate" HcPortal.Tests/ProtonYearGateIntegrationTests.cs` returns 4+ matches.
    - `dotnet test --filter "FullyQualifiedName~ProtonYearGateIntegration"` → green (NewSvc 4-arg ctor from Plan 03 in effect).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonYearGateIntegration"</automated>
  </verify>
  <done>T10 documented by-design (no logic change); reactivation gate predicate proven (blocked / bypass-exempt / active-skip).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin/HC → CoachCoacheeMappingAssign (POST) | Assigning/reactivating Proton track assignments; cross-year progression is an access-control gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-363-03 | Elevation of Privilege | reactivation skipping year-gate | mitigate | Reactivation candidates (inactive, non-bypass) now run `IsPrevYearPassedAsync`; Tahun N cannot reactivate without Tahun N-1 passed; server-side hard-block (V1/V4) |
| T-363-03e | (Authorized exemption) | inactive Origin="Bypass" | accept (explicit) | Exempted by design (D-06 permanent bypass stamp) — documented + test-covered, consistent with Phase 360 D-04 |
| T-363-03i | Information Disclosure | gate error message | mitigate | Generic friendly message (count of coachees), technical detail to _logger only (Phase 334/356 precedent) |
| T-363-09 | V7 Logging | non-contiguous Urutan silent | mitigate | Log-warn at both prev-track resolution points (T9/D-12); no behavior change |
</threat_model>

<verification>
- `dotnet build` 0 errors.
- `dotnet test --filter "FullyQualifiedName~ProtonYearGateIntegration"` green + full suite at wave merge.
- UAT (Plan 07): assign reactivation of a cross-year inactive assignment without prev-year passed → blocked with the hard-block message; bypass-origin inactive → allowed.
</verification>

<success_criteria>
- T3 SC: reactivation cross-year gated (or explicit Bypass exempt, documented) — loophole closed.
- T9: defensive logs at both points (gate unchanged).
- T10: by-design documented (zero logic change). SC5 triage advanced.
</success_criteria>

<output>
After completion, create `.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-05-SUMMARY.md`
</output>
