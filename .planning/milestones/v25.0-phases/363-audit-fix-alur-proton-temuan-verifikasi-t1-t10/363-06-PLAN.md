---
phase: 363-audit-fix-alur-proton-temuan-verifikasi-t1-t10
plan: 06
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Controllers/CDPController.cs
  - HcPortal.Tests/ProtonHistoriAndEvidenceTests.cs
autonomous: true
requirements: [T5, T8]
must_haves:
  truths:
    - "A coachee with an active CoachCoacheeMapping but NO ProtonTrackAssignment now appears in HistoriProton with status 'Belum Mulai' (T5)"
    - "The 'Belum Mulai' rows appear identically in HistoriProton AND ExportHistoriProton (no drift, Pitfall 5)"
    - "SubmitEvidenceWithCoaching appends the previous EvidencePath to EvidencePathHistory before overwriting (T8) — at parity with UploadEvidence; no File.Delete (E10 keep)"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "'Belum Mulai' rows in both HistoriProton + ExportHistoriProton; shared AppendEvidencePathHistory used by both evidence paths"
      contains: "Belum Mulai"
    - path: "HcPortal.Tests/ProtonHistoriAndEvidenceTests.cs"
      provides: "Append-history test + belum-mulai set computation test"
      contains: "ProtonHistoriAndEvidenceTests"
  key_links:
    - from: "Controllers/CDPController.cs SubmitEvidenceWithCoaching (:2284-2290)"
      to: "AppendEvidencePathHistory(progress)"
      via: "shared static helper called before EvidencePath overwrite"
      pattern: "AppendEvidencePathHistory\\("
    - from: "Controllers/CDPController.cs HistoriProton + ExportHistoriProton"
      to: "Belum Mulai worker rows"
      via: "active-mapping-without-assignment union added in both"
      pattern: "Belum Mulai"
---

<objective>
Two CDPController polish/drift fixes, layered on top of the helper work from Plans 01/02:

- T5 (LOW): the "Belum Mulai" badge + filter option already exist in the view (`HistoriProton.cshtml:79,152-155`) but are unreachable because the query only includes coachees with ≥1 assignment and the status ternary has only 2 branches. Per D-09: include coachees with an active `CoachCoacheeMapping` but NO `ProtonTrackAssignment`, status "Belum Mulai" — and MIRROR this into `ExportHistoriProton` (Pitfall 5, the same drift root as T1/T2).
- T8 (MED): `SubmitEvidenceWithCoaching` overwrites `EvidencePath` without appending the old path to `EvidencePathHistory` (UploadEvidence does append). Per D-11: append before overwrite, at parity with UploadEvidence. Keep the physical file (E10 keep-evidence — NO File.Delete). Kill the drift structurally by extracting a shared static helper used by both paths.

Purpose: Make the designed "Belum Mulai" state functional; preserve evidence history trail on the coaching submit path.
Output: query union (both methods) + shared AppendEvidencePathHistory helper + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-FINDINGS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-CONTEXT.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-RESEARCH.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-PATTERNS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-02-SUMMARY.md

<interfaces>
HistoriProton role-scoping (CDPController.cs:3145-3171) and ExportHistoriProton role-scoping (:3304-3320) — IDENTICAL shape:
- Level <=3 (Admin/HC): all assignments' coachees
- Level 4 (SH/SrSpv): coachees whose Section == user.Section
- Level 5 (Coach): mapped coachees (CoachCoacheeMappings.CoachId==user.Id && IsActive) with assignments

Status ternary (CDPController.cs:3250, mirrored in ExportHistoriProton ~:3399):
```csharp
string status = (latestHasAssessment && latestAllApproved) ? "Lulus" : "Dalam Proses";
```
Worker row model: HistoriProtonWorkerRow { UserId, Nama, NIP, Section, Unit, Jalur, Tahun{1,2,3}Done, Tahun{1,2,3}InProgress, Status }.

"Belum Mulai" definition (D-09 / specifics): coachee with `CoachCoacheeMappings.IsActive == true` (role-scoped same as above) AND NOT present in `coacheeIdsWithAssignments`. Row: all Tahun flags false, Status="Belum Mulai", Jalur="" (or mapping-derived if available).

View is READY — HistoriProton.cshtml badge :152-155 (else-branch) + filter option :79 — NO view change needed.

T8 evidence append pattern (UploadEvidence.cs:1280-1286, to share):
```csharp
if (!string.IsNullOrEmpty(progress.EvidencePath)) {
    var pathHistory = string.IsNullOrEmpty(progress.EvidencePathHistory)
        ? new List<string>()
        : System.Text.Json.JsonSerializer.Deserialize<List<string>>(progress.EvidencePathHistory) ?? new List<string>();
    pathHistory.Add(progress.EvidencePath);
    progress.EvidencePathHistory = System.Text.Json.JsonSerializer.Serialize(pathHistory);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: T5 — add "Belum Mulai" rows in HistoriProton AND ExportHistoriProton (D-09 + Pitfall 5)</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs:3138-3288 (HistoriProton — scoping, worker-row build loop, status ternary :3250, viewModel)
    - Controllers/CDPController.cs:3290-3417 (ExportHistoriProton — duplicated scoping + status + status filter)
    - Views/CDP/HistoriProton.cshtml:79,143-155 (filter option + 3-branch badge — confirm "Belum Mulai" already rendered, NO change)
    - 363-PATTERNS.md "T5 Query Belum Mulai" + 363-RESEARCH.md "T5 (Wave 3 — query)" + Pitfall 5
  </read_first>
  <action>
In BOTH `HistoriProton` and `ExportHistoriProton`, after the assignment-based worker rows are built (after the coacheeGroups loop), add the "Belum Mulai" rows:

1. Compute role-scoped active-mapping coachees using the SAME role-branching already present for assignments:
   - Level <=3: `_context.CoachCoacheeMappings.Where(m => m.IsActive)`
   - Level 4: active mappings joined to Users where Section == user.Section
   - Level 5: active mappings where CoachId == user.Id
   Select distinct CoacheeId → `activeMappingCoacheeIds`.
2. `belumMulaiIds = activeMappingCoacheeIds.Except(coacheeIdsWithAssignments)` — coachees with active mapping but no assignment.
3. Load those coachee Users (IsActive) and add a worker row each: all Tahun{1,2,3}Done/InProgress = false, Status = "Belum Mulai", Jalur = "" (or the mapping's track/jalur if readily available). Append to `workers` BEFORE the `OrderBy(w => w.Nama)` sort.
4. In ExportHistoriProton, ensure the status filter (`status` param) treats "Belum Mulai" as a valid filterable value (so filtering "Belum Mulai" exports those rows).

Apply IDENTICAL logic in both methods. RECOMMENDED (anti-drift, sefilosofi D-01): extract a shared private helper that returns the belum-mulai rows given (userLevel, user, coacheeIdsWithAssignments) and call it from both. Minimal acceptable: mirror the block verbatim in both + a comment noting the duplication must stay in sync. Record which approach you took in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -nc "Belum Mulai" Controllers/CDPController.cs` → 2+ (present in HistoriProton AND ExportHistoriProton, or 1 shared helper called by both — in which case the helper string appears once and both call it: verify both call-sites in that case).
    - `grep -n "Except(coacheeIdsWithAssignments)\|belumMulaiIds\|BelumMulai" Controllers/CDPController.cs` shows the set computation in both methods (or the shared helper).
    - No edit to Views/CDP/HistoriProton.cshtml (badge/filter already present): `git diff --stat` shows the view unchanged.
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>Coachees with active mapping but no assignment surface as "Belum Mulai" in both list and export; view untouched.</done>
</task>

<task type="auto">
  <name>Task 2: T8 — extract AppendEvidencePathHistory + wire SubmitEvidenceWithCoaching (D-11)</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs:1280-1287 (UploadEvidence append block — pattern source)
    - Controllers/CDPController.cs:2281-2292 (SubmitEvidenceWithCoaching evidence block — overwrite at :2290 WITHOUT append; note Plan 02 already edited the resubmit block :2270-2279 — different block)
    - 363-PATTERNS.md "T8 Append EvidencePathHistory" + 363-CONTEXT.md D-11 (keep file, no File.Delete)
  </read_first>
  <action>
1. Add `internal static void AppendEvidencePathHistory(ProtonDeliverableProgress progress)` containing the pattern from &lt;interfaces&gt; (null-safe JSON deserialize → Add(progress.EvidencePath) → serialize). Idempotent-safe (only appends when EvidencePath non-empty).
2. In `SubmitEvidenceWithCoaching`, inside `if (evidenceBytes != null && evidenceSafeFileName != null)` (:2284), add `AppendEvidencePathHistory(progress);` IMMEDIATELY BEFORE `progress.EvidencePath = $"/uploads/evidence/...";` (:2290). Do NOT add File.Delete (E10 keep-evidence).
3. Refactor UploadEvidence's inline append block (:1280-1286) to call `AppendEvidencePathHistory(progress);` (replacing the inline copy) — kills the drift structurally so the two paths share one implementation. Verify UploadEvidence behavior unchanged.
  </action>
  <acceptance_criteria>
    - `grep -n "AppendEvidencePathHistory" Controllers/CDPController.cs` shows the static definition + a call in UploadEvidence + a call in SubmitEvidenceWithCoaching (3 matches).
    - The SubmitEvidenceWithCoaching call appears BEFORE the `progress.EvidencePath = ` overwrite (verify by line order).
    - `grep -n "File.Delete" Controllers/CDPController.cs` shows NO new File.Delete added in the evidence blocks (E10).
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>Both evidence paths append history via one shared helper; old path preserved; physical file kept.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — append-history + belum-mulai set (T5/T8)</name>
  <files>HcPortal.Tests/ProtonHistoriAndEvidenceTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonCompletionServiceTests.cs:25-115 (fixture + seeding helpers)
    - Controllers/CDPController.cs AppendEvidencePathHistory (created Task 2 — directly testable static)
    - 363-RESEARCH.md "Validation Architecture" T5/T8 rows
  </read_first>
  <action>
Create `HcPortal.Tests/ProtonHistoriAndEvidenceTests.cs` as `[Trait("Category","Integration")]` (reuse ProtonCompletionFixture where DB is needed; the append test is pure and needs no DB):
1. `AppendEvidencePathHistory_AppendsOldPath`: a ProtonDeliverableProgress with EvidencePath="/uploads/evidence/1/a.pdf", EvidencePathHistory=null; call `CDPController.AppendEvidencePathHistory(progress)`; deserialize EvidencePathHistory; assert it contains "/uploads/evidence/1/a.pdf". Call again after setting a new EvidencePath; assert history now has both (cumulative).
2. `AppendEvidencePathHistory_NoOp_WhenEmptyPath`: EvidencePath null/empty → history stays null/empty (no spurious entry).
3. `BelumMulai_SetComputation`: (DB fixture) seed a coachee with an active CoachCoacheeMapping but no ProtonTrackAssignment + another coachee WITH an assignment; replicate the `activeMappingCoacheeIds.Except(coacheeIdsWithAssignments)` set computation (predikat-replikasi, keep identical to controller); assert only the no-assignment coachee is in the belum-mulai set. (LOW item — predikat-replikasi acceptable, per file convention.)
Unique coacheeId per fact.
  </action>
  <acceptance_criteria>
    - File `HcPortal.Tests/ProtonHistoriAndEvidenceTests.cs` exists with the 3 `[Fact]` named above.
    - `grep -n "AppendEvidencePathHistory_AppendsOldPath\|BelumMulai_SetComputation" HcPortal.Tests/ProtonHistoriAndEvidenceTests.cs` → 2+ matches.
    - `dotnet test --filter "FullyQualifiedName~ProtonHistoriAndEvidence"` → green.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonHistoriAndEvidence"</automated>
  </verify>
  <done>Append helper proven cumulative + no-op-safe; belum-mulai set computation proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach (submit evidence) → SubmitEvidenceWithCoaching | Overwriting evidence must preserve the prior file's path in history (auditability) |
| reviewer/coach (read) → HistoriProton/ExportHistoriProton | Role-scoped read; "Belum Mulai" rows must respect the same scoping as assignment rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-363-08 | Repudiation (lost trail) | SubmitEvidenceWithCoaching overwrite | mitigate | Append prior path to EvidencePathHistory via shared helper before overwrite (D-11); physical file retained (E10) |
| T-363-05 | Information Disclosure (over-scope) | HistoriProton/Export "Belum Mulai" rows | mitigate | Belum-Mulai coachees resolved through the SAME role-scoping (Level<=3/4/5) as assignment rows — no widening of visibility |
| T-363-05d | (Drift) | Export vs view divergence | mitigate | Identical logic mirrored (or shared helper) in both methods (Pitfall 5) |
</threat_model>

<verification>
- `dotnet build` 0 errors + targeted tests green.
- Full `dotnet test` at wave merge.
- UAT (Plan 07): HistoriProton shows a "Belum Mulai" coachee; filter "Belum Mulai" works; Export includes the row. Coaching submit overwrite preserves prior path in history (DB check).
</verification>

<success_criteria>
- T5 SC (SC5 triage): "Belum Mulai" reachable in list + export, role-scoped.
- T8 SC (SC5 triage): evidence history appended on the coaching-submit path; file kept.
</success_criteria>

<output>
After completion, create `.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-06-SUMMARY.md`
</output>
