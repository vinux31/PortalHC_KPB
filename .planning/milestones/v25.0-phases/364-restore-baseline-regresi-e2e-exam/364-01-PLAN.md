---
phase: 364-restore-baseline-regresi-e2e-exam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []   # test-only phase — no product REQ-IDs; success = ROADMAP Phase 364 SC#1-4 (D-10 supports SC#2)
user_setup: []

must_haves:
  truths:
    - "Both target specs run as-is @localhost:5277 before any edit (D-10 honored)"
    - "Every failing flow is classified as title-failure vs non-title-failure with evidence"
    - "The 21 standard-create flows are confirmed to fail at the create step (validator reject)"
    - "FLOW P (PrePostTest exempt) and FLOW T (Manual path) behavior is recorded for SC#2"
  artifacts:
    - path: ".planning/phases/364-restore-baseline-regresi-e2e-exam/364-01-SUMMARY.md"
      provides: "Per-flow baseline failure classification table (title vs non-title)"
      contains: "Baseline Diagnosa"
  key_links:
    - from: "app @localhost:5277 (AD off)"
      to: "npx playwright test exam-taking.spec.ts exam-types.spec.ts"
      via: "Playwright run as-is, no edits"
      pattern: "npx playwright test"
---

<objective>
Run the two target e2e specs (`exam-taking.spec.ts` + `exam-types.spec.ts`) EXACTLY AS-IS against a live local app @localhost:5277, capture the per-flow failures, and classify each as a title-failure (validator REST-06 reject at create step) or a non-title-failure (selector/feature drift v18-v25). This is the D-10 baseline diagnostic — it MUST happen before any spec is edited, so later title/drift fixes are targeted "per-flow, not blind" (SC#2).

Purpose: Produces the ground-truth classification that Plan 02 (title edits) and Plan 03 (drift fixes + gate) depend on. Without this run, drift fixes would be guesswork and SC#2 ("fix per-flow, bukan ganti buta") could not be satisfied.
Output: A classification table embedded in `364-01-SUMMARY.md` — one row per flow with: PASS/FAIL, failure step, and category (TITLE / NON-TITLE / EXEMPT-OK).

NOTE: This plan modifies ZERO files (`files_modified: []`). It only RUNS the existing specs and records results. No edits to specs, helpers, or production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-CONTEXT.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-RESEARCH.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-PATTERNS.md
@docs/SEED_WORKFLOW.md

<flow_inventory>
<!-- Source: 364-RESEARCH.md §Flow Inventory + grep verified 2026-06-11. These are the flows whose create step the validator rejects. The baseline run MUST classify each. -->

exam-taking.spec.ts standard-create flows (10, all expected TITLE-failure at create):
  :35 Legacy Exam | :316 Token Exam | :402 ForceClose Exam | :548 Package Exam
  :702 Proton T3 Interview (HIGH drift risk — Proton v25.0 overhaul) | :833 Multi Worker
  :963 Timer Expired | :1057 RealTime Mon | :1291 EditTest | :1404 Abandon Test
  Phase 313 block (:1608+): fixture SQL via clickResumeForFixture — NOT created via UI → NOT validator-gated. Record actual run result.

exam-types.spec.ts flows:
  Standard-create (11, expected TITLE-failure): :37 [317-SMOKE-W0] | :191 [317-K] MA | :310 [317-L] Essay
    | :435 [317-M] Mixed | :588 [317-N] NoReview | :690 [317-O] ExtraTime | :1065 [318-Q] EWCD Past
    | :1151 [318-R] Cert | :1278 [318-S-TRUE] Review | :1364 [318-S-FALSE] NoReview
  EXEMPT/N-A (record but do not expect title-failure):
    :860 [318-P] PrePost Exam — PrePostTest mode → validator EXEMPT (SC#2). Should PASS unless non-title drift.
    :1457 [319-T] Manual CRUD — AddManualAssessment controller, no REST-06. Should PASS unless drift.
    [319-U/V/W/X/Y] — no standard-create-via-title; record run result.
</flow_inventory>

<run_mechanics>
<!-- Source: 364-RESEARCH.md §Playwright Run Config & Lifecycle [VERIFIED] -->
- App MUST run with AD off (Pitfall 3): `Authentication__UseActiveDirectory=false dotnet run` (env override, DO NOT edit appsettings.json — HEAD has AD on, handoff-ready).
- Playwright lifecycle is automatic: `setup` project (global.setup.ts) runs FIRST → checks app /Account/Login 200 → collision pre-check Id 9001-9018 == 0 → BACKUP DB → seed matrix SQL → validation. `globalTeardown` runs LAST → RESTORE DB → Layer 4 (expect 0 matrix rows). So snapshot/restore is enforced by the run itself (D-13/D-14). No manual snapshot needed for a diagnostic run that creates nothing extra.
- Run command (from `tests/` dir, node_modules + Playwright 1.58.2 present):
    cd tests && npx playwright test exam-taking.spec.ts exam-types.spec.ts
  Or per-file for clearer per-flow attribution (recommended for diagnosis):
    cd tests && npx playwright test exam-taking.spec.ts
    cd tests && npx playwright test exam-types.spec.ts
- Both specs use `test.describe.configure({mode:'serial'})` → on first failure in a describe, remaining sub-tests in that describe SKIP. So the create-step failure cascades: a single TITLE reject at K1 shows K2-K5 as skipped. Read the report carefully — attribute the ROOT failure (create step), not the cascaded skips.
- retries: 0, per-test timeout 60s. Validator reject manifests as: (a) `expect(success||alert).toBeTruthy()` FALSE in exam-taking; (b) `#successModal.show` waitFor TIMEOUT 15s in exam-types wizard helper (examTypes.ts:103).
- Pitfall 4: if global.setup throws "ID range 9001-9018 sudah dipakai", a prior run did not teardown cleanly. Check `docs/SEED_JOURNAL.md` tail — last matrix entry must be `cleaned`. As of 2026-06-11 research: clean. If `active`, restore the snapshot manually before running.
</run_mechanics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start app (AD off) and run both target specs as-is, capture report</name>
  <files>(none — read-only diagnostic run; no source files modified)</files>
  <read_first>
    - tests/playwright.config.ts (confirm testDir ./e2e, baseURL localhost:5277, retries 0, setup→chromium dependency, globalTeardown)
    - tests/e2e/global.setup.ts (confirm BACKUP + collision pre-check 9001-9018 + matrix seed)
    - tests/e2e/global.teardown.ts (confirm RESTORE + Layer 4)
    - docs/SEED_JOURNAL.md (tail — confirm last matrix entry is `cleaned` before running, per Pitfall 4)
    - 364-RESEARCH.md §Playwright Run Config & Lifecycle + §Selector Drift (the as-is run is the source of truth for drift)
  </read_first>
  <action>
    Do NOT edit any file in this task. Steps:

    1. Confirm `docs/SEED_JOURNAL.md` tail shows the last matrix-seed entry as `cleaned`. If it shows `active`, the prior run left residue — restore the latest local snapshot first (per docs/SEED_WORKFLOW.md), then re-confirm clean. Record the journal state in your run notes.

    2. Start the app with AD off in the background (so Playwright can reach it @localhost:5277). Use the env-override form — DO NOT edit appsettings.json:
         Authentication__UseActiveDirectory=false dotnet run
       (PowerShell equivalent: `$env:Authentication__UseActiveDirectory="false"; dotnet run`). Wait until the app responds 200 at http://localhost:5277/Account/Login before running Playwright.

    3. From the `tests/` directory, run BOTH target specs as-is (no edits). Prefer per-file runs for clearer per-flow attribution:
         cd tests
         npx playwright test exam-taking.spec.ts --reporter=list
         npx playwright test exam-types.spec.ts --reporter=list
       (Combined `npx playwright test exam-taking.spec.ts exam-types.spec.ts` is also acceptable.) The `setup` project (BACKUP + matrix seed) and `globalTeardown` (RESTORE) run automatically around each invocation.

    4. Capture the full list-reporter output for both runs (pass/fail/skip per test id). Note for each FAILED test: which step failed and the error signature (e.g. `expect(success||alert)` false, or `#successModal.show` 15s timeout, or a selector/assertion error unrelated to create).

    5. After the run, confirm `globalTeardown` restored the DB: `docs/SEED_JOURNAL.md` tail should return to `cleaned`, and there should be NO leftover `Pre Test%`/test-title rows from this diagnostic (this run creates none with the new prefix, but old titles like `Legacy Exam%` should also be reverted by RESTORE). Record the residue-check result.
  </action>
  <verify>
    <automated>cd tests; npx playwright test exam-taking.spec.ts exam-types.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - The command `npx playwright test exam-taking.spec.ts exam-types.spec.ts` was executed against a live app @localhost:5277 with AD off (app reachable, login page returns 200 — confirmed by setup project not aborting at the /Account/Login check).
    - Raw list-reporter output for BOTH specs is captured (every test id with PASS/FAIL/SKIP visible).
    - For each failing flow, the failing STEP and error signature is recorded (create-step validator reject vs a later-step selector/assertion error).
    - `docs/SEED_JOURNAL.md` tail returns to `cleaned` after the run (teardown restored DB). Recorded explicitly.
    - ZERO files were modified: `git status --porcelain tests/ Controllers/ Services/` shows no changes (the run is read-only; .matrix-state.json and report artifacts are gitignored/transient and do not count).
  </acceptance_criteria>
  <done>Both specs ran as-is @5277 with AD off; full per-test results captured; DB restored to clean per journal; no source files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Classify every flow as TITLE vs NON-TITLE failure and write the baseline table to SUMMARY</name>
  <files>.planning/phases/364-restore-baseline-regresi-e2e-exam/364-01-SUMMARY.md</files>
  <read_first>
    - 364-RESEARCH.md §Flow Inventory (the 21 standard-create flows expected to fail at create) + §Selector Drift (non-title drift candidates: FLOW E Proton HIGH, H4/H7 monitoring MED, O SignalR MED, K5/M Results MA = old SURF-317-A bug)
    - 364-PATTERNS.md §Shared Patterns → Selector Drift (per-flow drift action table)
    - The raw run output captured in Task 1
  </read_first>
  <action>
    Using the Task 1 run output, build a per-flow classification table and record it in the plan SUMMARY. For EACH flow (use the line-number markers from the Flow Inventory), assign exactly one category:

    - TITLE — failed at the create step with the validator signature (`expect(success||alert)` false in exam-taking, or `#successModal.show` 15s timeout in exam-types wizard). These are fixed by Plan 02 (prefix `Pre Test`).
    - NON-TITLE — failed at a step AFTER create, OR a flow that does not hit the validator but still failed (selector/feature drift, or the known SURF-317-A Results MA bug). These are triaged by Plan 03 (fix-in-test D-05, or `test.fixme` + backlog D-06).
    - EXEMPT-OK — FLOW P (:860 PrePost, validator-exempt) and FLOW T (:1457 Manual, different controller) — record actual result; if PASS, they are evidence for SC#2.
    - PASS — any flow already green as-is (record it; do not touch it).

    For each NON-TITLE row, note a one-line drift hypothesis (e.g. "FLOW E Proton T3 — Proton v25.0 overhaul likely changed #protonTrackSelect/interview form; candidate test.fixme"). Flag FLOW E (exam-taking :702-805) explicitly as the HIGHEST drift risk per research (Proton 358-363 overhaul) — it is the strongest `test.fixme` candidate.

    Write the table into `.planning/phases/364-restore-baseline-regresi-e2e-exam/364-01-SUMMARY.md` under a heading `## Baseline Diagnosa (D-10)`. Columns: Flow | Spec | Line | Result | Failing step | Category | Drift hypothesis (if NON-TITLE). Also write a 1-paragraph rollup: count of TITLE failures, count of NON-TITLE failures, and the explicit list of likely `test.fixme` candidates for Plan 03 to verify.

    This SUMMARY is the hand-off artifact for Plan 02 and Plan 03 — they will read it to know which flows need title edits vs drift fixes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/364-restore-baseline-regresi-e2e-exam/364-01-SUMMARY.md','utf8');if(!/Baseline Diagnosa/.test(s))throw new Error('missing Baseline Diagnosa heading');if(!/(TITLE|NON-TITLE)/.test(s))throw new Error('missing classification categories');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `364-01-SUMMARY.md` exists and contains a heading matching `## Baseline Diagnosa` (grep: `Baseline Diagnosa`).
    - The classification table has a row for EVERY flow in the Flow Inventory (21 standard-create flows + FLOW P + FLOW T at minimum), each tagged with exactly one category: TITLE / NON-TITLE / EXEMPT-OK / PASS (grep finds both `TITLE` and `NON-TITLE` tokens present).
    - FLOW E (Proton T3, exam-taking :702) appears with a NON-TITLE drift hypothesis OR is explicitly noted as `test.fixme` candidate.
    - A rollup paragraph states the count of TITLE vs NON-TITLE failures and lists the candidate `test.fixme` flows for Plan 03.
    - The SUMMARY records that the run modified zero source files and the DB was restored clean.
  </acceptance_criteria>
  <done>SUMMARY contains a complete per-flow classification table (every Flow Inventory row categorized) + rollup + fixme-candidate list, ready for Plans 02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → local app @localhost:5277 | Test-controlled HTTP requests; no external/untrusted input. Local-only. |
| global.setup/teardown → local SQL Server (`localhost\SQLEXPRESS`) | sqlcmd BACKUP/RESTORE against local DB only; guarded to reject non-localhost `-S`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-364-01 | Tampering | DB snapshot/restore against wrong server | mitigate | `dbSnapshot.runSqlcmd` already rejects non-localhost `-S` (guard T-315-01); connection hardcoded `localhost\SQLEXPRESS`/`HcPortalDB_Dev`. This plan runs no DB SQL of its own — only the existing guarded lifecycle. No new surface. |
| T-364-02 | Information disclosure | Test credentials in repo (accounts.ts, password `123456`) | accept | Local-only test accounts against local DB; AD off is local-dev only. No production secret. Diagnostic run introduces nothing new. |
| T-364-03 | Denial of service | Stale matrix rows (Id 9001-9018) abort the run | accept | Setup collision pre-check fails loudly with clear message; D-10 task confirms journal `cleaned` before run. Non-security operational guard. |
</threat_model>

<verification>
- Both target specs executed as-is @localhost:5277 with AD off; full per-test results captured.
- `364-01-SUMMARY.md` contains the `## Baseline Diagnosa (D-10)` classification table covering every Flow Inventory row.
- DB restored to clean state (journal `cleaned`, no residual test-title rows) by the automatic teardown.
- Zero source files modified (`git status` clean for tests/Controllers/Services).
</verification>

<success_criteria>
- D-10 satisfied: baseline diagnostic ran BEFORE any edit, classifying failures per-flow (supports SC#2 "fix per-flow, bukan ganti buta").
- Every flow is tagged TITLE / NON-TITLE / EXEMPT-OK / PASS with evidence.
- FLOW E (Proton T3) flagged as highest-risk drift / `test.fixme` candidate.
- Plan 02 and Plan 03 have an authoritative classification to work from.
</success_criteria>

<output>
After completion, create `.planning/phases/364-restore-baseline-regresi-e2e-exam/364-01-SUMMARY.md` containing the `## Baseline Diagnosa (D-10)` per-flow classification table + rollup + fixme-candidate list.
</output>
