---
phase: 364-restore-baseline-regresi-e2e-exam
plan: 03
type: execute
wave: 3
depends_on: [364-02]
files_modified:
  - tests/e2e/exam-taking.spec.ts
  - tests/e2e/exam-types.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: []   # test-only — success = ROADMAP Phase 364 SC#2 + SC#4 (both specs green or documented non-title failure) + D-15 gate
user_setup: []

must_haves:
  truths:
    - "Every remaining NON-TITLE failure is either fixed in test-code (D-05) or marked test.fixme with reason + backlog ref (D-06)"
    - "No production code, no migration, no helper, and utils.ts are changed (zero production edits)"
    - "Both target specs run @localhost:5277 with a single full run and report green (or fixme-documented per D-09)"
    - "dotnet test suite is green (D-15 second gate)"
    - "FLOW P passes as evidence of exempt correctness (SC#2); any real production bug is recorded as a backlog finding, not fixed (D-06)"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "Drift fixes in test-code and/or test.fixme markers for non-title failures"
      contains: "test"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Phase 364 run journal entry (snapshot/restore lifecycle record per D-13)"
      contains: "364"
    - path: ".planning/phases/364-restore-baseline-regresi-e2e-exam/364-03-SUMMARY.md"
      provides: "Final gate result + deviations + backlog 999.x entries for any production bugs found"
      min_lines: 20
  key_links:
    - from: "remaining non-title failures"
      to: "test-code fix (D-05) OR test.fixme + backlog (D-06)"
      via: "per-flow triage from baseline classification"
      pattern: "test\\.fixme|fix"
    - from: "both specs + dotnet test"
      to: "phase gate (D-15)"
      via: "single full run @5277 + xUnit suite"
      pattern: "npx playwright test|dotnet test"
---

<objective>
Close out Phase 364: triage every remaining NON-TITLE failure (after Plan 02 made all titles validator-compliant) into either a test-code fix (D-05, zero production change) or a `test.fixme('reason + backlog ref')` marker for a real production bug / behavior drift that must NOT be fixed here (D-06). Then run the final gate (D-15): both target specs in a single full run @localhost:5277 + `dotnet test` suite green. Record the honest outcome — a run containing `fixme` is NOT "full pass"; it falls to the SC#4 alternative "documented non-title failure" path (D-09). Any production bug discovered is recorded as a backlog 999.x finding, never fixed in this phase.

Purpose: Restores the two specs as a usable regression baseline (the phase goal). Whatever remains red after honest triage is documented, not hidden. The strongest `test.fixme` candidate is FLOW E (Proton T3 Interview) given the Proton v25.0 overhaul (Phase 358-363).
Output: Final spec state (drift-fixed + fixme-marked as needed), SEED_JOURNAL entry, and a SUMMARY with the gate result, deviations, and any backlog entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-CONTEXT.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-RESEARCH.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-PATTERNS.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-01-SUMMARY.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-02-SUMMARY.md
@docs/SEED_WORKFLOW.md
@docs/SEED_JOURNAL.md

<drift_triage_map>
<!-- Source: 364-RESEARCH.md §Selector Drift + 364-PATTERNS.md §Shared Patterns → Selector Drift. Use the Plan 01/02 SUMMARY actuals; this is the static prediction to confirm against. -->
| Flow | Location | Predicted drift | Action policy |
|------|----------|-----------------|---------------|
| exam-taking E (Proton T3) | :702-805 | HIGH — Proton v25.0 overhaul (358-363) likely changed #protonTrackSelect / interview form / Tahun-3 track | If failure = production behavior change (not a selector typo) -> `test.fixme('drift Proton v25.0 Phase 358-363 + ref backlog 999.x')` (D-06/D-09). If it is a simple selector rename, fix in test-code (D-05). |
| exam-taking H4/H7 (Monitoring) | :1136-1262 | MED — monitoring markup / GetMonitoringProgress JSON shape | Fix selector in test-code (D-05) if drift is cosmetic. |
| exam-taking A8 | :205-207 | LOW — hardcoded EN badge "Your Score"/"PASSED" may be ID now | If UI text changed EN->ID, update assertion text or use partial-match (D-17). |
| exam-types O (SignalR ExtraTime) | :747-754 | MED — hub state gate `assessmentHub.state === 'Connected'` | Fix selector/timing in test-code (D-05). |
| exam-types K5 / M (Results MA) | :278-296 / :552-575 | KNOWN production bug SURF-317-A (Results 500); already DB-workaround | DO NOT fix (Pitfall 5, D-06). If it surfaces, record as existing finding only. |
| exam-types Q (EWCD window-close) | :1110-1135 | LOW — `Ujian sudah ditutup` guard text | Fix selector/text (D-05) or partial-match (D-17). |
| exam-types R (Cert PDF) | :1236-1258 | LOW — NomorSertifikat / cert generation | Fix selector (D-05). |
| Long-title assertions (card hasText / history row / cert toContainText) | many | UI truncates long `Pre Test ...` titles | Use PARTIAL-MATCH (D-17): match on unique substring (marker + timestamp), not the full title. |

> D-09 KRITIS: a run containing ANY `test.fixme` is NOT counted as "PASS penuh". Be honest to the verifier — report it as the SC#4 alternative path (documented non-title failure). Never count a skip as green.
> D-06/D-07: real production bugs -> record as a deviation in SUMMARY + a backlog 999.x entry (actionable). No separate FINDINGS file needed.
</drift_triage_map>

<gate_definition>
<!-- D-08 / D-15 -->
- "PASS penuh @5277" = ONE full run, green, default Playwright retry policy (retries: 0 in config). D-08.
- Phase gate (D-15) = BOTH target specs PASS @localhost:5277 (single full run) + `dotnet test HcPortal.Tests` green. NO full e2e suite (other specs untouched; utils.ts unchanged -> zero impact).
- If any flow is `test.fixme`, the e2e half is "documented non-title failure" (SC#4 alternative), NOT full pass — but the phase can still ship with that documented per D-09. The `dotnet test` half must be green regardless.
- Run commands:
    cd tests && npx playwright test exam-taking.spec.ts exam-types.spec.ts --reporter=list
    dotnet test HcPortal.Tests
- App MUST run AD off: `Authentication__UseActiveDirectory=false dotnet run` (env override, never edit appsettings.json).
</gate_definition>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage and resolve every remaining NON-TITLE failure (fix-in-test D-05 or test.fixme D-06)</name>
  <files>tests/e2e/exam-taking.spec.ts, tests/e2e/exam-types.spec.ts</files>
  <read_first>
    - 364-01-SUMMARY.md + 364-02-SUMMARY.md (the actual remaining-failure list — this is the work list; do NOT re-derive from scratch)
    - tests/e2e/exam-taking.spec.ts + tests/e2e/exam-types.spec.ts (current post-Plan-02 state)
    - 364-RESEARCH.md §Selector Drift + §Common Pitfalls (Pitfall 5 SURF-317-A is a KNOWN bug, do not fix)
    - 364-PATTERNS.md §Shared Patterns → Selector Drift (per-flow action policy)
  </read_first>
  <action>
    For EACH flow still failing after Plan 02 (use the Plan 02 SUMMARY drift list), apply the policy from `<drift_triage_map>`:

    1. SELECTOR/TEXT DRIFT (cosmetic — renamed id, changed button text, EN->ID label, truncated title): fix in TEST-CODE only (D-05). For long-title assertions that fail because the UI truncates the now-longer `Pre Test ... {ts}` title, switch to PARTIAL-MATCH on a unique substring (marker + timestamp) per D-17 — e.g. `hasText: title` -> `hasText: title.slice(0, 40)` or match on the `[317-K]`+timestamp fragment. Re-run the affected spec to confirm green.

    2. REAL PRODUCTION BEHAVIOR CHANGE / PRODUCTION BUG (the flow can't pass because production behaves differently now, e.g. Proton interview flow restructured by v25.0, or a genuine bug): DO NOT fix production. Mark the failing test with `test.fixme('<concrete reason> — ref backlog 999.x')` (D-06). Record the reason and the backlog id you will create in Task 3. The strongest candidate is FLOW E (Proton T3, exam-taking :702-805) — if `#protonTrackSelect`/interview form/Tahun-3 track no longer match production, fixme it with reason "drift Proton v25.0 Phase 358-363".

    3. KNOWN bug SURF-317-A (exam-types K5/M Results MA 500): leave as-is — already DB-workaround. Do NOT fix (Pitfall 5). If it manifests differently, record as existing finding only.

    Constraints (NON-NEGOTIABLE): zero production code, zero migration, zero new helper, `tests/helpers/utils.ts` untouched, FLOW P (:860) and FLOW T (:1457) titles untouched. Every edit lands ONLY in the two target spec files. Each `test.fixme` MUST carry a non-empty reason string ending with a backlog ref (e.g. `999.x`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for(const f of ['tests/e2e/exam-taking.spec.ts','tests/e2e/exam-types.spec.ts']){const s=fs.readFileSync(f,'utf8');const fixmes=s.match(/test\.fixme\([^)]*\)/g)||[];for(const fm of fixmes){if(!/['\`].+['\`]/.test(fm))throw new Error('test.fixme without reason in '+f+': '+fm);}}console.log('OK fixme reasons present (count varies)')"</automated>
  </verify>
  <acceptance_criteria>
    - Every flow in the Plan 02 remaining-failure list is accounted for: either it now passes (fixed in test-code) OR it carries a `test.fixme(...)` with a non-empty reason string that references a backlog id (grep: each `test.fixme(` has a quoted reason; `999.` appears in at least the fixme reasons if any fixme exists).
    - No production file changed: `git status --porcelain Controllers/ Services/ Views/ Migrations/ Data/` is EMPTY.
    - `git diff tests/helpers/utils.ts` is EMPTY (helper untouched).
    - FLOW P and FLOW T titles still unprefixed: grep finds `uniqueTitle('[318-P] PrePost Exam')` and `uniqueTitle('[319-T] Manual CRUD')` intact.
    - Long-title assertion fixes use partial-match where applied (D-17) — no full-title `hasText` left that fails on truncation (verified by the affected spec passing in Task 2).
    - Only tests/e2e/exam-taking.spec.ts and tests/e2e/exam-types.spec.ts are modified by this task.
  </acceptance_criteria>
  <done>Every non-title failure is resolved (test-code fix) or honestly fixme-marked with reason+backlog ref; zero production/helper/migration changes; FLOW P/T intact.</done>
</task>

<task type="auto">
  <name>Task 2: Run the final gate — both specs @5277 (single full run) + dotnet test suite</name>
  <files>tests/e2e/exam-taking.spec.ts, tests/e2e/exam-types.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts + tests/e2e/exam-types.spec.ts (post-Task-1 final state)
    - 364-RESEARCH.md §Validation Architecture (gate commands) + §Playwright Run Config (AD off, lifecycle)
    - docs/SEED_JOURNAL.md (confirm clean before run)
  </read_first>
  <action>
    Start app AD off (`Authentication__UseActiveDirectory=false dotnet run`; PowerShell `$env:Authentication__UseActiveDirectory="false"; dotnet run`). Wait for /Account/Login 200 @localhost:5277.

    Gate part 1 (e2e, D-15): from `tests/`, run BOTH target specs in a single combined full run (lifecycle auto-BACKUP/RESTORE wraps it):
      cd tests
      npx playwright test exam-taking.spec.ts exam-types.spec.ts --reporter=list

    Gate part 2 (unit, D-15): run the xUnit suite:
      dotnet test HcPortal.Tests

    Record the EXACT outcome honestly:
      - If ALL e2e tests pass with ZERO `fixme` skips -> SC#4 "PASS penuh @5277" (D-08).
      - If any `test.fixme` is present -> the run is NOT "PASS penuh"; report it as SC#4 alternative "failure terdokumentasi bukan-karena-judul" (D-09). List exactly which flows are fixme'd and why.
      - `dotnet test` MUST be green regardless (record pass count, e.g. `XYZ/XYZ`).
    Confirm FLOW P passed (SC#2 exempt evidence). Confirm the FLOW K D-11 `LinkedGroupId IS NULL` assertion passed. After the run, confirm DB restored clean (SEED_JOURNAL tail `cleaned`, no residual `Pre Test%` rows).

    Do NOT loosen, skip, or fixme additional tests just to make the gate green — that violates D-09. Honest red-with-reason beats fake green.
  </action>
  <verify>
    <automated>cd tests; npx playwright test exam-taking.spec.ts exam-types.spec.ts --reporter=list; cd ..; dotnet test HcPortal.Tests</automated>
  </verify>
  <acceptance_criteria>
    - Both target specs executed in a single combined run @5277 with AD off; full list output captured.
    - `dotnet test HcPortal.Tests` is GREEN (record the pass count, e.g. `229/229`).
    - The e2e outcome is classified honestly: either "PASS penuh (0 fixme)" OR "documented: N flows fixme'd — <list reasons>" (D-09). No silent skip counted as pass.
    - FLOW P (PrePostTest) is among the passing tests (SC#2 evidence) — explicitly noted.
    - FLOW K D-11 assertion passed (`linkedNull === 1`) — explicitly noted.
    - SEED_JOURNAL tail returns to `cleaned`; no residual `Pre Test%` rows in DB after teardown.
    - git status shows only the 2 spec files + docs/SEED_JOURNAL.md (Task 3) as changes; zero production/migration/helper changes.
  </acceptance_criteria>
  <done>Combined e2e full run + dotnet test executed @5277; outcome classified honestly (full pass or documented fixme path); FLOW P + D-11 green; DB clean; no production changes.</done>
</task>

<task type="auto">
  <name>Task 3: Record SEED_JOURNAL entry, write SUMMARY with deviations + backlog 999.x findings, mark baseline available</name>
  <files>docs/SEED_JOURNAL.md, .planning/phases/364-restore-baseline-regresi-e2e-exam/364-03-SUMMARY.md</files>
  <read_first>
    - docs/SEED_JOURNAL.md (append a Phase 364 entry per the existing format)
    - docs/SEED_WORKFLOW.md (journal entry format: purpose, classification, entities touched, cleaned status)
    - 364-CONTEXT.md (D-07 documentation policy, D-16 baseline-available note)
    - The Task 1/2 results (fixme list + production bug findings + gate outcome)
  </read_first>
  <action>
    1. Append a Phase 364 entry to `docs/SEED_JOURNAL.md` following the existing format: purpose = "e2e baseline regresi exam restore (Phase 364) — run exam-taking + exam-types @5277", classification = "temporary + local-only" (data created by the run is reverted by the automatic global teardown RESTORE), entities touched = AssessmentSessions / AssessmentPackages / matrix seed ids 9001-9018, status = `cleaned` (teardown restored DB). Note that snapshot/restore was enforced by the Playwright lifecycle (D-13/D-14), not a manual snapshot.

    2. Write `.planning/phases/364-restore-baseline-regresi-e2e-exam/364-03-SUMMARY.md` covering:
       - Gate outcome: e2e classification (PASS penuh vs documented-fixme per D-09) + `dotnet test` count.
       - Per-flow final status table (PASS / FIXME / fixed-drift), with the fixme reasons.
       - Deviations (pattern of 355 Deviasi): each `test.fixme` flow and each real production bug found, with a concrete backlog 999.x entry id and a one-line actionable description (D-06/D-07). If a production bug surfaced (e.g. FLOW E Proton drift requiring real fix, or SURF-317-A reconfirmed), record it as a finding — do NOT fix in 364.
       - D-16 note: "Baseline e2e exam (exam-taking + exam-types) tersedia — on-demand, dipakai saat fase relevan sentuh area exam. BUKAN gate wajib tiap fase."
       - Confirmation: zero production code, zero migration, utils.ts untouched, FLOW P/T untouched.

    3. If any actionable production bug was found, add a one-line backlog item under the v25.0 backlog section reference (the SUMMARY records the 999.x id and description; the orchestrator/state-update handles ROADMAP backlog insertion). Do NOT create a separate FINDINGS file (D-07).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const j=fs.readFileSync('docs/SEED_JOURNAL.md','utf8');if(!/364/.test(j))throw new Error('SEED_JOURNAL missing 364 entry');const s=fs.readFileSync('.planning/phases/364-restore-baseline-regresi-e2e-exam/364-03-SUMMARY.md','utf8');if(!/[Bb]aseline.*(tersedia|available)/.test(s))throw new Error('SUMMARY missing D-16 baseline-available note');if(!/dotnet test|[0-9]+\/[0-9]+/.test(s))throw new Error('SUMMARY missing gate result');console.log('OK journal+summary')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SEED_JOURNAL.md` has a Phase 364 entry (grep `364`) with status `cleaned`.
    - `364-03-SUMMARY.md` exists (>=20 lines) and contains: gate outcome (e2e classification + `dotnet test` count), per-flow final status, deviations with backlog 999.x ids (if any fixme/bug), and the D-16 baseline-available note (grep: `baseline` + `tersedia`/`available`).
    - SUMMARY explicitly confirms zero production code / zero migration / utils.ts untouched / FLOW P+T untouched.
    - If any `test.fixme` exists in the specs, the SUMMARY lists each with its reason and a backlog 999.x id (honest D-09 reporting — not claimed as full pass).
    - No separate FINDINGS file was created (D-07): only SUMMARY + SEED_JOURNAL.
  </acceptance_criteria>
  <done>SEED_JOURNAL Phase 364 entry recorded (cleaned); SUMMARY documents the honest gate outcome, per-flow status, deviations/backlog 999.x findings, and baseline-available note; zero production changes confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test spec → local app @5277 | Test-controlled requests; production code read-only (not modified). |
| Playwright lifecycle → local SQL Server | Automatic BACKUP/RESTORE, localhost-guarded. |
| SEED_JOURNAL write | Local documentation file; no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-364-07 | Tampering | DB left in dirty state if teardown skipped (residual matrix/test rows) | mitigate | globalTeardown always runs (Playwright config) and RESTOREs from snapshot + Layer 4 validation; Task 2/3 verify journal returns to `cleaned` and no residual `Pre Test%` rows. |
| T-364-08 | Repudiation | A real production bug hidden by a fixme without a record | mitigate | D-06/D-07 require each fixme to carry a reason + backlog 999.x id recorded in SUMMARY; D-09 forbids counting fixme as pass. Verifier sees the honest list. |
| T-364-09 | Information disclosure | Local test credentials / AD-off in repo | accept | Local-dev only; no production secret; unchanged by this phase. |
</threat_model>

<verification>
- Every remaining non-title failure is fixed-in-test or fixme-marked with reason+backlog ref (no silent skips).
- Gate run: both specs @5277 single full run + `dotnet test` green; outcome classified honestly (D-08/D-09/D-15).
- FLOW P passes (SC#2); FLOW K D-11 assertion green (SC#3 carried from Plan 02).
- SEED_JOURNAL Phase 364 entry `cleaned`; SUMMARY records deviations + 999.x findings + D-16 baseline note.
- Zero production code, zero migration, zero helper, utils.ts untouched, FLOW P/T untouched.
</verification>

<success_criteria>
- SC#2 satisfied: fixes are per-flow (driven by baseline classification); FLOW P exempt and green.
- SC#4 satisfied: both specs PASS penuh @5277, OR remaining failures are documented as non-title (D-09) with backlog entries.
- D-15 gate met: e2e target specs green/documented + `dotnet test` suite green.
- D-16: baseline recorded as available (on-demand).
- D-06/D-07: any production bug recorded to backlog 999.x, never fixed in 364.
</success_criteria>

<output>
After completion, create `.planning/phases/364-restore-baseline-regresi-e2e-exam/364-03-SUMMARY.md` (gate outcome, per-flow final status, deviations + backlog 999.x findings, D-16 baseline-available note) and append a Phase 364 entry to `docs/SEED_JOURNAL.md` (status `cleaned`).
</output>
