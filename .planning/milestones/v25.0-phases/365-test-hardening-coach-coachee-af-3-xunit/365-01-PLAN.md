---
phase: 365-test-hardening-coach-coachee-af-3-xunit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CoachMappingController.cs
  - .planning/ROADMAP.md
autonomous: true
requirements: []
nyquist_compliant: true
# Test-only phase — no product REQ-ID. Scope-tag: D-01/D-02/D-03/D-05 (core extraction) + SC#2 amendment.

must_haves:
  truths:
    - "MarkMappingCompletedCore ada sebagai public static method (context-only, no UserManager) — callable dari test tanpa instance controller"
    - "IsYearCompletedAsync jadi static (ctx param) — predikat lulus testable tanpa instance controller"
    - "Wrapper MarkMappingCompleted tetap berperilaku byte-identik: resolve-user/Challenge, BeginTransaction, panggil core, Commit/Rollback, audit post-commit, TempData, redirect — zero behavior change"
    - "dotnet build 0 error + dotnet test full suite hijau (0 regresi) — bukti parity"
    - "ROADMAP SC#2 Phase 365 di-amend dari 'zero file produksi berubah' jadi 'behavior-preserving refactor disentuh + dibuktikan parity'"
  artifacts:
    - path: "Controllers/CoachMappingController.cs"
      provides: "Static core MarkMappingCompletedCore + static IsYearCompletedAsync + thin wrapper"
      contains: "public static async Task<(bool ok, string? error, int cascadeCount)> MarkMappingCompletedCore"
    - path: ".planning/ROADMAP.md"
      provides: "SC#2 Phase 365 amended (longgar untuk core extraction)"
      contains: "behavior-preserving"
  key_links:
    - from: "MarkMappingCompleted (wrapper)"
      to: "MarkMappingCompletedCore (static core)"
      via: "var (ok, error, cascadeCount) = await MarkMappingCompletedCore(_context, mappingId)"
      pattern: "MarkMappingCompletedCore\\(_context"
    - from: "MarkMappingCompletedCore (core)"
      to: "IsYearCompletedAsync (static)"
      via: "await IsYearCompletedAsync(context, tahun3.Id)"
      pattern: "IsYearCompletedAsync\\(context"
---

<objective>
Ekstrak logika graduate `MarkMappingCompleted` (`Controllers/CoachMappingController.cs:1112-1179`) jadi `public static` core `MarkMappingCompletedCore(ApplicationDbContext, int)` + jadikan helper `IsYearCompletedAsync` static — mengikuti pola anti-drift Phase 363 (`CDPController.ApproveDeliverableCoreAsync`/`RejectDeliverableCoreAsync`). Endpoint controller jadi wrapper tipis (resolve-user + transaksi + audit + redirect). Ini **refactor behavior-preserving** (parity-lock seam), BUKAN perubahan fungsi.

Plan ini juga **mengamendemen ROADMAP SC#2** karena keputusan D-01 (extract core) melonggarkan klausa lama "zero file produksi berubah" — tanpa edit ini verifier akan menggagalkan gate "git diff Controllers/ kosong".

Purpose: Membuat logika graduate testable tanpa UserManager-double (endpoint deref `_userManager.GetUserAsync(User)` di baris pertama `:1118` → trik null-substitute GAGAL, repo tak punya preseden UserManager-double). Core static + context-only = langsung dipanggil dari fixture real-SQL di Plan 02.

Output:
- `Controllers/CoachMappingController.cs` — `MarkMappingCompletedCore` (static) + `IsYearCompletedAsync` (static) + wrapper tipis.
- `.planning/ROADMAP.md` — SC#2 Phase 365 di-amend.
- Bukti parity: `dotnet build` 0 error + `dotnet test` full suite hijau (zero regresi vs baseline ~214+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-CONTEXT.md
@.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-RESEARCH.md
@.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-PATTERNS.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diekstrak dari kode aktual sesi planning ini. -->
<!-- Pakai langsung — TIDAK perlu eksplorasi codebase tambahan. -->

PRODUKSI TARGET SAAT INI — `Controllers/CoachMappingController.cs:1100-1179` (VERIFIED sesi ini):
```csharp
// :1100 — helper private instance (akan jadi static, D-05)
private async Task<bool> IsYearCompletedAsync(int assignmentId)
{
    var progresses = await _context.ProtonDeliverableProgresses
        .Where(p => p.ProtonTrackAssignmentId == assignmentId).ToListAsync();
    if (!progresses.Any()) return false;
    bool allApproved = progresses.All(p => p.Status == "Approved");
    bool hasFinalAssessment = await _context.ProtonFinalAssessments
        .AnyAsync(fa => fa.ProtonTrackAssignmentId == assignmentId);
    return allApproved && hasFinalAssessment;
}

// :1112-1179 — endpoint monolitik (akan dipecah jadi wrapper + core)
[HttpPost]
[ValidateAntiForgeryToken]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> MarkMappingCompleted(int mappingId)
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();
    var mapping = await _context.CoachCoacheeMappings.FindAsync(mappingId);
    if (mapping == null) return NotFound();
    var assignments = await _context.ProtonTrackAssignments
        .Include(a => a.ProtonTrack)
        .Where(a => a.CoacheeId == mapping.CoacheeId && a.IsActive)
        .ToListAsync();
    var tahun3Assignment = assignments
        .FirstOrDefault(a => a.ProtonTrack != null && a.ProtonTrack.TahunKe == "Tahun 3");
    if (tahun3Assignment == null) { TempData["Error"] = "Coachee belum memiliki assignment Tahun 3."; return RedirectToAction("CoachCoacheeMapping"); }
    bool tahun3Complete = await IsYearCompletedAsync(tahun3Assignment.Id);
    if (!tahun3Complete) { TempData["Error"] = "Tidak bisa menandai lulus (graduated): Tahun 3 belum lulus untuk pekerja ini."; return RedirectToAction("CoachCoacheeMapping"); }
    using var transaction = await _context.Database.BeginTransactionAsync();
    try
    {
        mapping.IsCompleted = true;
        mapping.CompletedAt = DateTime.UtcNow;
        mapping.IsActive = false;            // AF-3 D-03
        mapping.EndDate = DateTime.UtcNow;   // AF-3 D-03
        var deactivationTime = mapping.EndDate.Value;
        var activeAssignments = await _context.ProtonTrackAssignments
            .Where(a => a.CoacheeId == mapping.CoacheeId && a.IsActive)
            .ToListAsync();
        foreach (var a in activeAssignments) { a.IsActive = false; a.DeactivatedAt = deactivationTime; }
        int cascadeCount = activeAssignments.Count;
        await _context.SaveChangesAsync();
        await transaction.CommitAsync();
        var actorName = string.IsNullOrWhiteSpace(user.NIP) ? (user.FullName ?? "Unknown") : $"{user.NIP} - {user.FullName}";
        await _auditLog.LogAsync(user.Id, actorName, "MarkMappingCompleted",
            $"Mapping ID={mappingId} ditandai graduated (IsActive=false). CoacheeId={mapping.CoacheeId} — {cascadeCount} ProtonTrackAssignment juga dinonaktifkan", mappingId, "CoachCoacheeMapping");
        TempData["Success"] = "Coachee berhasil ditandai sebagai graduated.";
        return RedirectToAction("CoachCoacheeMapping");
    }
    catch (Exception ex)
    {
        await transaction.RollbackAsync();
        _logger.LogError(ex, "MarkMappingCompleted transaction failed for mapping {Id}", mappingId);
        TempData["Error"] = "Operasi gagal. Semua perubahan dibatalkan.";
        return RedirectToAction("CoachCoacheeMapping");
    }
}
```

ANALOG POLA 363 — `Controllers/CDPController.cs` (core + wrapper call, VERIFIED):
```csharp
// core (signature + not-found di core + SaveChanges di core, TANPA transaksi di core)
public static async Task<(bool ok, string? error, bool allApproved)> ApproveDeliverableCoreAsync(
    ApplicationDbContext context, int progressId, string actorId, string actorName, string actorRole, bool isSrSpv, bool isSH)
{
    var progress = await context.ProtonDeliverableProgresses...FirstOrDefaultAsync(...);
    if (progress == null) return (false, "Deliverable tidak ditemukan.", false);  // not-found DI CORE
    // ... mutasi ...
    await context.SaveChangesAsync();   // SaveChanges DI CORE
    return (true, null, allApproved);
}

// wrapper memanggil core (CDPController.cs:865-873)
var (ok, error, allApproved) = await ApproveDeliverableCoreAsync(_context, progressId, user.Id, ...);
if (!ok) {
    if (error == "Deliverable tidak ditemukan.") return NotFound();   // core error → NotFound
    TempData["Error"] = error;
    return RedirectToAction("CoachingProton");
}
```

CONFIRMED FACTS (sesi planning ini):
- `IsYearCompletedAsync(` punya TEPAT 1 pemanggil produksi (`:1134`) — grep Controllers/ + Services/ → ganti LANGSUNG ke static, TIDAK perlu instance-delegasi (OQ-3 RESOLVED).
- Attrs wrapper `[HttpPost]` `[ValidateAntiForgeryToken]` `[Authorize(Roles = "Admin, HC")]` (`:1113-1115`) WAJIB PRESERVE verbatim (zero new attack surface).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ekstrak MarkMappingCompletedCore (static) + static IsYearCompletedAsync + wrapper tipis</name>
  <files>Controllers/CoachMappingController.cs</files>
  <read_first>
    - Controllers/CoachMappingController.cs (baca :1095-1180 — method target IsYearCompletedAsync + MarkMappingCompleted yang dipecah)
    - .planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-PATTERNS.md (§"Controllers/CoachMappingController.cs" — signature core 365 baris 104-127 + wrapper call baris 148-159 + transform IsYearCompletedAsync baris 130-146)
    - .planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-RESEARCH.md (§Code Examples baris 211-269 + OQ-1 baris 357-360 + OQ-2 baris 362-364)
    - Controllers/CDPController.cs (baca area :855-944 + :975-1069 — analog ApproveDeliverableCoreAsync/RejectDeliverableCoreAsync sebagai referensi struktur core+wrapper)
  </read_first>
  <action>
**Stop dulu dev server lokal bila ada** (caveat Phase 356): `dotnet run` yang sedang jalan memegang `HcPortal.dll` dan memblokir `dotnet build` (MSB3027/MSB3021). Pastikan tidak ada `dotnet run`/`dotnet watch` aktif sebelum build.

Lakukan 3 perubahan di `Controllers/CoachMappingController.cs`:

**(A) Jadikan `IsYearCompletedAsync` static (D-05).** Ganti `:1100-1110` dari instance jadi static dengan param `ApplicationDbContext ctx`:
```csharp
// Phase 236 COMP-04 + Phase 365 D-05: static agar core callable tanpa instance controller. Logika NOL berubah (_context → ctx).
private static async Task<bool> IsYearCompletedAsync(ApplicationDbContext ctx, int assignmentId)
{
    var progresses = await ctx.ProtonDeliverableProgresses
        .Where(p => p.ProtonTrackAssignmentId == assignmentId)
        .ToListAsync();
    if (!progresses.Any()) return false;
    bool allApproved = progresses.All(p => p.Status == "Approved");
    bool hasFinalAssessment = await ctx.ProtonFinalAssessments
        .AnyAsync(fa => fa.ProtonTrackAssignmentId == assignmentId);
    return allApproved && hasFinalAssessment;
}
```
(Hanya `_context` → `ctx`. Tidak ada caller lain — sudah grep-verified 1 pemanggil; ganti langsung ke static, no delegasi.)

**(B) Tambahkan core static baru** `MarkMappingCompletedCore` (pola 363, signature persis RESEARCH:265-269). Letakkan tepat SEBELUM endpoint `MarkMappingCompleted`. Tuple `(bool ok, string? error, int cascadeCount)`:
```csharp
// Phase 365 D-01/D-02: static core — guard + mutasi + cascade + SaveChanges. NO transaksi (D-02, di wrapper).
// Mengikuti pola CDPController.ApproveDeliverableCoreAsync (Phase 363): not-found & guard return (false, error, 0).
// OQ-1: SaveChangesAsync DI CORE (konsisten 363); wrapper membungkus dengan BeginTransaction/Commit.
public static async Task<(bool ok, string? error, int cascadeCount)> MarkMappingCompletedCore(
    ApplicationDbContext context, int mappingId)
{
    var mapping = await context.CoachCoacheeMappings.FindAsync(mappingId);
    if (mapping == null) return (false, "Mapping tidak ditemukan.", 0);   // OQ-2: not-found DI CORE

    var assignments = await context.ProtonTrackAssignments
        .Include(a => a.ProtonTrack)
        .Where(a => a.CoacheeId == mapping.CoacheeId && a.IsActive)
        .ToListAsync();
    var tahun3Assignment = assignments
        .FirstOrDefault(a => a.ProtonTrack != null && a.ProtonTrack.TahunKe == "Tahun 3");
    if (tahun3Assignment == null)
        return (false, "Coachee belum memiliki assignment Tahun 3.", 0);   // token "Tahun 3"

    bool tahun3Complete = await IsYearCompletedAsync(context, tahun3Assignment.Id);
    if (!tahun3Complete)
        return (false, "Tidak bisa menandai lulus (graduated): Tahun 3 belum lulus untuk pekerja ini.", 0);  // token "belum lulus"

    // MUTASI (verbatim dari produksi :1143-1159)
    mapping.IsCompleted = true;
    mapping.CompletedAt = DateTime.UtcNow;
    mapping.IsActive = false;            // AF-3 D-03: bebaskan IX_CoachCoacheeMappings_CoacheeId_ActiveUnique
    mapping.EndDate = DateTime.UtcNow;   // AF-3 D-03
    var deactivationTime = mapping.EndDate.Value;
    // CASCADE deactivate assignment aktif (BUKAN RemoveRange → histori utuh). Reuse list 'assignments' di atas (sudah IsActive filter).
    foreach (var a in assignments) { a.IsActive = false; a.DeactivatedAt = deactivationTime; }
    int cascadeCount = assignments.Count;

    await context.SaveChangesAsync();    // OQ-1: SaveChanges di core (dalam scope transaksi wrapper)
    return (true, null, cascadeCount);
}
```
PENTING: produksi lama melakukan query `activeAssignments` KEDUA (`:1151-1153`) yang identik dengan `assignments` di `:1123-1126` (keduanya `CoacheeId == mapping.CoacheeId && a.IsActive`). Di core, **reuse list `assignments` yang sama** untuk cascade — ini zero behavior change (kedua list isi sama; cascade hanya butuh entity tracked, dan `assignments` sudah tracked). Hasil `cascadeCount` identik.

**(C) Ubah endpoint `MarkMappingCompleted` jadi wrapper tipis.** Pertahankan attrs `:1113-1115` VERBATIM. Resolve-user + transaksi + panggil core + audit/TempData/redirect:
```csharp
[HttpPost]
[ValidateAntiForgeryToken]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> MarkMappingCompleted(int mappingId)
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();

    using var transaction = await _context.Database.BeginTransactionAsync();
    try
    {
        var (ok, error, cascadeCount) = await MarkMappingCompletedCore(_context, mappingId);   // D-01 seam
        if (!ok)
        {
            await transaction.RollbackAsync();
            if (error == "Mapping tidak ditemukan.") return NotFound();   // OQ-2: core error → NotFound (pola 363)
            TempData["Error"] = error;
            return RedirectToAction("CoachCoacheeMapping");
        }
        await transaction.CommitAsync();

        var mapping = await _context.CoachCoacheeMappings.FindAsync(mappingId);   // re-read untuk CoacheeId di pesan audit
        var actorName = string.IsNullOrWhiteSpace(user.NIP)
            ? (user.FullName ?? "Unknown")
            : $"{user.NIP} - {user.FullName}";
        await _auditLog.LogAsync(user.Id, actorName, "MarkMappingCompleted",
            $"Mapping ID={mappingId} ditandai graduated (IsActive=false). CoacheeId={mapping?.CoacheeId} — {cascadeCount} ProtonTrackAssignment juga dinonaktifkan", mappingId, "CoachCoacheeMapping");
        TempData["Success"] = "Coachee berhasil ditandai sebagai graduated.";
        return RedirectToAction("CoachCoacheeMapping");
    }
    catch (Exception ex)
    {
        await transaction.RollbackAsync();
        _logger.LogError(ex, "MarkMappingCompleted transaction failed for mapping {Id}", mappingId);
        TempData["Error"] = "Operasi gagal. Semua perubahan dibatalkan.";
        return RedirectToAction("CoachCoacheeMapping");
    }
}
```

**Zero-behavior-change reasoning (catat di SUMMARY):**
- Urutan eksekusi identik: resolve-user → guard → mutasi → SaveChanges → Commit → audit → TempData.
- Guard yang dulu di LUAR transaksi (`:1129-1139`) kini di DALAM core (di dalam transaksi wrapper). Beda kosmetik saja: guard hanya melakukan READ (FindAsync, query, predikat) → tidak ada write sebelum guard lolos, jadi membungkusnya dengan transaksi yang langsung di-rollback saat fail = byte-identik secara efek (rollback transaksi read-only = no-op). TempData/redirect yang sama tetap terbit per error.
- `_auditLog.LogAsync` tetap POST-commit dengan pesan & format string identik (CoacheeId via re-read; nilai sama karena mapping tak berubah CoacheeId-nya).
- `cascadeCount` identik (list assignment aktif yang sama).

JANGAN ubah `Services/` apa pun — `git diff Services/` HARUS tetap kosong.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/CoachMappingController.cs` mengandung string EXACT `public static async Task<(bool ok, string? error, int cascadeCount)> MarkMappingCompletedCore`
    - `Controllers/CoachMappingController.cs` mengandung string EXACT `private static async Task<bool> IsYearCompletedAsync(ApplicationDbContext ctx, int assignmentId)`
    - `Controllers/CoachMappingController.cs` wrapper mengandung `await MarkMappingCompletedCore(_context, mappingId)`
    - Core mengandung `await IsYearCompletedAsync(context, tahun3Assignment.Id)` (memanggil versi static, bukan instance)
    - Attrs `[HttpPost]`, `[ValidateAntiForgeryToken]`, `[Authorize(Roles = "Admin, HC")]` masih ada di wrapper (grep menemukan ketiganya tepat di atas `MarkMappingCompleted(int mappingId)`)
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error
    - `git diff --name-only Services/` → KOSONG (zero output)
  </acceptance_criteria>
  <done>Core static + static helper terbentuk, wrapper tipis memanggil core, build 0 error, Services/ tak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 2: Amendemen ROADMAP SC#2 + bukti parity full suite hijau</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (baca :256-265 — entry "Phase 365" + SC#2 yang berbunyi "zero file produksi berubah (git diff Controllers/ Services/ kosong)")
    - .planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-RESEARCH.md (§"🚨 Flag untuk Planner — Amendemen ROADMAP SC#2" baris 448-456 — teks pengganti persis)
    - .planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-CONTEXT.md (§specifics baris 101-106 — alasan amendemen)
  </read_first>
  <action>
**(A) Edit `.planning/ROADMAP.md`** entry Phase 365. SC#2 saat ini (di sekitar baris 263):
```
  2. Suite penuh `dotnet test` hijau; zero file produksi berubah (git diff Controllers/ Services/ kosong).
```
Ganti jadi:
```
  2. Refactor behavior-preserving + parity-locked; `Controllers/CoachMappingController.cs` disentuh (extract static core `MarkMappingCompletedCore` + static `IsYearCompletedAsync` + thin wrapper), zero behavior change dibuktikan via core test hijau + `dotnet test` full suite hijau + `dotnet build` 0 error; `git diff Services/` tetap kosong; migration=false.
```
Juga update baris SC#1 bila masih menyebut "InMemory DbContext pola `OrganizationControllerTests`" — ganti referensi pola jadi real-SQL fixture, agar tidak kontradiksi dengan D-04. SC#1 baru:
```
  1. `MarkMappingCompletedTests` hijau — real-SQL `ProtonCompletionFixture` (D-04, enforce filtered unique index): graduate set IsCompleted=true + IsActive=false + CompletedAt/EndDate; cascade assignment aktif → IsActive=false + DeactivatedAt; histori progress utuh; coachee re-assignable pasca-graduate.
```
(SC#1 lama menyebut "InMemory DbContext pola OrganizationControllerTests" — itu pendekatan yang sengaja DITOLAK D-01/D-04. Selaraskan agar verifier tidak menggagalkan kontradiksi sumber.)

**(B) Jalankan bukti parity — full suite + build.** Pastikan tidak ada `dotnet run` aktif (build-lock caveat). Jalankan:
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test` full suite → semua hijau, 0 regresi vs baseline (~214+ test sebelum phase ini; angka pasti = baseline run pertama).

Karena Plan ini hanya me-refactor seam (belum ada test baru — itu Plan 02), full suite yang lolos = bukti zero behavior change pada refactor core. Bila ada test merah yang menyentuh `CoachMappingController` / graduate, investigasi & perbaiki refactor (jangan ubah test existing kecuali memang ada test yang assert struktur internal — tak ada yang seperti itu diketahui).

**Catatan SEED_WORKFLOW:** tidak diperlukan — phase ini tak menyentuh DB lokal `HcPortalDB_Dev` (fixture test pakai DB disposable; Plan 01 ini bahkan tak menjalankan test real-SQL baru). Migration=false.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test 2>&1 | grep -E "Passed!|Failed!|error|Passed:|Failed:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 365 SC#2 mengandung string `behavior-preserving` DAN `git diff Services/` (bukan lagi "git diff Controllers/ Services/ kosong")
    - `.planning/ROADMAP.md` Phase 365 SC#1 tidak lagi mengandung `InMemory` (referensi pola usang dihapus/diganti real-SQL fixture)
    - `dotnet build HcPortal.csproj` → 0 error
    - `dotnet test` → "Passed!" (0 Failed) untuk full suite; tidak ada regresi
    - `git diff --name-only Services/` → KOSONG
  </acceptance_criteria>
  <done>ROADMAP SC#1+SC#2 selaras dengan core-extraction (real-SQL, behavior-preserving); build 0 error; full suite hijau = bukti zero behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /Admin/MarkMappingCompleted | Input `mappingId` (int route/form). Boundary TIDAK berubah — wrapper tetap memegang authz + anti-forgery. |
| Wrapper → static core | Pemanggilan internal in-process (bukan endpoint). Core static tidak ter-route sebagai HTTP action. |

## STRIDE Threat Register

> Phase ini TEST-ONLY refactor (extract behavior-preserving core). TIDAK ada endpoint baru, input baru, data flow baru, atau permukaan eksternal baru. Satu-satunya perubahan produksi = memindahkan logika DB ke method static yang dipanggil wrapper yang sama. Tidak ada high-severity threat — reasoning didokumentasikan, bukan threat dikarang.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-365-01 | Elevation of Privilege | `MarkMappingCompletedCore` (static, public) | mitigate | Core static = unit-testable saja, TIDAK ter-expose sebagai HTTP endpoint (tidak ada atribut routing). Authz `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` + `[HttpPost]` tetap di wrapper VERBATIM — satu-satunya jalur HTTP. Acceptance criteria Task 1 mem-verify ketiga atribut masih ada. |
| T-365-02 | Tampering | Mutasi DB tanpa atomicity | accept | Transaksi tetap di wrapper (D-03): `BeginTransactionAsync` sebelum core, `CommitAsync`/`RollbackAsync` sesudah. Core `SaveChangesAsync` berjalan dalam scope transaksi wrapper → atomicity dipertahankan. Zero behavior change dari produksi lama yang juga ber-transaksi. |
| T-365-03 | Information Disclosure | Pesan error guard | accept | Error message domain-safe (token "Tahun 3"/"belum lulus"/"tidak ditemukan"), TIDAK membocorkan `ex.Message` ke user — preserved verbatim dari produksi. |

**Kesimpulan keamanan:** Zero behavior change = zero new attack surface. Refactor mem-preserve `[Authorize(Roles="Admin, HC")]`, `[ValidateAntiForgeryToken]`, `[HttpPost]` di wrapper. Tidak ada threat high-severity. Bukti: full `dotnet test` hijau + build 0 error membuktikan perilaku (termasuk authz path via wrapper) tak berubah.
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error (stop dev server dulu — build-lock caveat).
- `dotnet test` full suite → semua hijau, 0 regresi (bukti parity zero behavior change).
- `git diff --name-only Services/` → kosong (hanya `Controllers/CoachMappingController.cs` + `.planning/ROADMAP.md` berubah).
- grep konfirmasi: `MarkMappingCompletedCore` (public static), `IsYearCompletedAsync` (static, ctx param), wrapper attrs preserved, ROADMAP SC#2 amended.
</verification>

<success_criteria>
- `MarkMappingCompletedCore(ApplicationDbContext, int)` ada sebagai `public static`, return `(bool ok, string? error, int cascadeCount)`, berisi guard + mutasi + cascade + SaveChanges (NO transaksi di core).
- `IsYearCompletedAsync(ApplicationDbContext ctx, int)` static; satu-satunya caller (core) memakai versi static.
- Wrapper `MarkMappingCompleted` mempertahankan attrs + resolve-user/Challenge + BeginTransaction + Commit/Rollback + audit post-commit + TempData + redirect — byte-identik secara perilaku.
- ROADMAP SC#1 (real-SQL, bukan InMemory) + SC#2 (behavior-preserving) di-amend.
- `dotnet build` 0 error + `dotnet test` full suite hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-01-SUMMARY.md`. Sertakan: zero-behavior-change reasoning (urutan eksekusi identik + guard-dalam-transaksi = no-op rollback + audit/cascadeCount identik), hasil build + full suite (angka pass), konfirmasi `git diff Services/` kosong, teks SC#1/SC#2 ROADMAP sebelum→sesudah.
</output>
