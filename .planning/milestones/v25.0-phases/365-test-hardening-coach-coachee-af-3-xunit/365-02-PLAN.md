---
phase: 365-test-hardening-coach-coachee-af-3-xunit
plan: 02
type: tdd
wave: 2
depends_on: ["365-01"]
files_modified:
  - HcPortal.Tests/MarkMappingCompletedTests.cs
autonomous: true
requirements: []
nyquist_compliant: true
# Test-only phase — no product REQ-ID. Scope-tag: D-04/D-06/D-07/D-08 (6 [Fact] real-SQL lock).

must_haves:
  truths:
    - "6 skenario AF-3 (D-06) terkunci xUnit real-SQL: happy graduate, re-assignability, guard no-Tahun3, guard Tahun3-incomplete, mapping-null, history-intact"
    - "Happy path assert full end-state (D-07): IsCompleted=true + IsActive=false + CompletedAt/EndDate non-null + cascade IsActive=false+DeactivatedAt + cascadeCount==N + progress count tak berubah"
    - "Re-assignability membuktikan filtered unique index bebas pasca-graduate (real-SQL only)"
    - "Guard assert (D-08) cek token kunci ('Tahun 3'/'belum lulus'/'tidak ditemukan') + mapping/assignment tak termutasi — BUKAN microcopy verbatim"
    - "dotnet test full suite hijau (test baru + 0 regresi)"
  artifacts:
    - path: "HcPortal.Tests/MarkMappingCompletedTests.cs"
      provides: "6 [Fact] integration real-SQL + helper SeedGraduateReadyAsync"
      contains: "[Trait(\"Category\", \"Integration\")]"
      min_lines: 120
  key_links:
    - from: "MarkMappingCompletedTests"
      to: "CoachMappingController.MarkMappingCompletedCore (static core dari Plan 01)"
      via: "await CoachMappingController.MarkMappingCompletedCore(ctx, mappingId)"
      pattern: "MarkMappingCompletedCore\\(ctx"
    - from: "MarkMappingCompletedTests"
      to: "ProtonCompletionFixture (real-SQL disposable, REUSE)"
      via: "IClassFixture<ProtonCompletionFixture>"
      pattern: "IClassFixture<ProtonCompletionFixture>"
---

<objective>
Buat `HcPortal.Tests/MarkMappingCompletedTests.cs` — 6 [Fact] integration real-SQL yang mengunci (parity-lock) perilaku graduate AF-3 D-03/D-04, memanggil `CoachMappingController.MarkMappingCompletedCore` (static core dari Plan 01) langsung via `ProtonCompletionFixture` (real SQL Server disposable, REUSE — bukan bikin baru). Termasuk helper seed `SeedGraduateReadyAsync` (state Tahun-3-lulus-penuh) yang diadaptasi dari `SeedProgressChainAsync` analog.

Purpose: Regresi pada AF-3 (graduate set IsActive=false + cascade deactivate + histori utuh + re-assignability) langsung ketahuan di `dotnet test`. Real-SQL WAJIB (D-04) — hanya SQL nyata yang meng-enforce filtered unique index `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique (WHERE [IsActive]=1)`; InMemory akan beri re-assignability [Fact] palsu-hijau.

Output: `HcPortal.Tests/MarkMappingCompletedTests.cs` dengan 6 [Fact] + helper seed, `dotnet test` full suite hijau.

Dependency: Plan 01 (Wave 1) HARUS selesai dulu — file ini memanggil `MarkMappingCompletedCore` yang baru diekstrak Plan 01. File-overlap NOL (Plan 01 = Controllers/+ROADMAP; Plan 02 = test baru), tapi dependensi simbol → Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-RESEARCH.md
@.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-PATTERNS.md
@.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-VALIDATION.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diekstrak dari kode aktual sesi planning ini. -->

CORE YANG DI-TEST (dibuat Plan 01) — `Controllers/CoachMappingController.cs`:
```csharp
public static async Task<(bool ok, string? error, int cascadeCount)> MarkMappingCompletedCore(
    ApplicationDbContext context, int mappingId);
// not-found  → (false, "Mapping tidak ditemukan.", 0)
// no-Tahun3  → (false, "Coachee belum memiliki assignment Tahun 3.", 0)
// belum-lulus→ (false, "Tidak bisa menandai lulus (graduated): Tahun 3 belum lulus untuk pekerja ini.", 0)
// happy      → (true, null, cascadeCount)   // mutasi mapping + cascade assignment, SaveChanges di core
```

FIXTURE REUSE (jangan redefinisi) — `HcPortal.Tests/ProtonCompletionServiceTests.cs:25`:
```csharp
public class ProtonCompletionFixture : IAsyncLifetime
{
    public DbContextOptions<ApplicationDbContext> Options => _options;   // dipakai: new ApplicationDbContext(_fixture.Options)
    // InitializeAsync → MigrateAsync() penuh → IX_..._ActiveUnique + 6 ProtonTrack seed migration ADA
    // DisposeAsync → EnsureDeletedAsync (DB HcPortalDB_Test_<guid> disposable; HcPortalDB_Dev TAK tersentuh)
}
```

POLA ANALOG (COPY) — `HcPortal.Tests/ProtonApproveRejectParityTests.cs`:
```csharp
// Class skeleton + fixture (baris 17-25)
[Trait("Category", "Integration")]
public class ProtonApproveRejectParityTests : IClassFixture<ProtonCompletionFixture>
{
    private readonly ProtonCompletionFixture _fixture;
    public ProtonApproveRejectParityTests(ProtonCompletionFixture fixture) { _fixture = fixture; }

    // Seed chain (baris 31-65) — REUSE track migration, save bertahap untuk FK
    private static async Task<List<int>> SeedProgressChainAsync(ApplicationDbContext ctx, string coacheeId, string[] statuses)
    {
        var track = await ctx.ProtonTracks.FirstAsync(t => t.TrackType == "Operator" && t.TahunKe == "Tahun 1");
        var asg = new ProtonTrackAssignment { CoacheeId = coacheeId, AssignedById = "hc", ProtonTrackId = track.Id, IsActive = true };
        ctx.ProtonTrackAssignments.Add(asg);
        var komp = new ProtonKompetensi { Bagian = "Bagian-PAR", Unit = $"U-{coacheeId[..10]}", NamaKompetensi = $"K-{coacheeId}", Urutan = 1, ProtonTrackId = track.Id };
        ctx.ProtonKompetensiList.Add(komp); await ctx.SaveChangesAsync();
        var sub = new ProtonSubKompetensi { ProtonKompetensiId = komp.Id, NamaSubKompetensi = "Sub", Urutan = 1 };
        ctx.ProtonSubKompetensiList.Add(sub); await ctx.SaveChangesAsync();
        // loop deliverable + progress per status, save bertahap ...
    }

    // Panggil core langsung + verifikasi 2nd context AsNoTracking (baris 82-99)
    var (ok, error) = await CDPController.RejectDeliverableCoreAsync(ctx, ids[0], ...);
    Assert.True(ok); Assert.Null(error);
    await using var verify = new ApplicationDbContext(_fixture.Options);
    var p = await verify.ProtonDeliverableProgresses.AsNoTracking().FirstAsync(x => x.Id == ids[0]);
    Assert.Equal("Rejected", p.Status);

    // Guard token (baris 162-164) — token kunci, BUKAN verbatim
    Assert.False(ok); Assert.Contains("diproses oleh approver lain", error!);

    // Isolasi per-fact (baris 71)
    var coachee = $"par-{Guid.NewGuid():N}";
}
```

FIELD WAJIB (VERIFIED dari model, RESEARCH:332-335):
- `CoachCoacheeMapping`: `CoachId`, `CoacheeId`, `IsActive`, `StartDate` (DateTime non-null — WAJIB di-set, jangan lupa), `IsCompleted` (default false). `EndDate`/`CompletedAt` nullable.
- `ProtonFinalAssessment`: `CoacheeId`, `CreatedById`, `ProtonTrackAssignmentId`, `Status` (default "Completed"), `CreatedAt`. Unik 1:1 per assignment (`HasIndex(...).IsUnique()`) → JANGAN seed 2 baris per assignment (Pitfall 4).
- `ProtonTrackAssignment`: `CoacheeId`, `AssignedById`, `ProtonTrackId`, `IsActive`. `DeactivatedAt`/`Origin` nullable.

PITFALL:
- JANGAN insert ProtonTrack baru — UNIQUE `AK_ProtonTracks_TrackType_TahunKe`. Selalu `FirstAsync(t => t.TrackType=="Operator" && t.TahunKe=="Tahun 3")` (Id=6, seed migration MERGE).
- Track Tahun 3 PASTI ada di fixture (di-seed migration `20260223060707_CreateProtonTrackTable` Step 5, BUKAN SeedData.cs runtime).
</interfaces>
</context>

<feature>
  <name>MarkMappingCompletedTests — 6 [Fact] AF-3 parity-lock (real-SQL)</name>
  <files>HcPortal.Tests/MarkMappingCompletedTests.cs</files>
  <behavior>
Semua 6 [Fact] memanggil `CoachMappingController.MarkMappingCompletedCore(ctx, mappingId)` langsung dan verifikasi end-state via 2nd context `AsNoTracking`. DB di-share antar-fact → `coacheeId` unik per fact (`$"grad-{Guid.NewGuid():N}"`).

**Fact 1 — `MarkMappingCompleted_Happy_FullEndState` (D-07):**
- Seed Tahun-3-lulus-penuh via `SeedGraduateReadyAsync` → (mappingId, t3AssignmentId, progressCount).
- Hitung `activeBefore` = jumlah ProtonTrackAssignment aktif coachee SEBELUM core.
- `(ok, error, cascadeCount) = await MarkMappingCompletedCore(ctx, mappingId)`.
- Assert: `ok==true`, `error==null`, `cascadeCount == activeBefore`.
- 2nd context: mapping `IsCompleted==true` + `IsActive==false` + `CompletedAt!=null` + `EndDate!=null`; SETIAP assignment yang tadinya aktif → `IsActive==false` + `DeactivatedAt!=null`; `count(ProtonDeliverableProgresses where ProtonTrackAssignmentId==t3) == progressCount` (TAK berubah).

**Fact 2 — `MarkMappingCompleted_ReassignableAfterGraduate` (D-06 #2, bukti D-03):**
- Seed graduate-ready, panggil core → ok=true.
- (Opsional perkuat, Pitfall 3) SEBELUM graduate, coba insert mapping aktif KEDUA untuk coachee yang sama → HARUS throw `DbUpdateException` (buktikan index bergigi). Kalau dilakukan, lakukan di sub-context/try-catch terpisah & pastikan dibersihkan agar tak mengganggu graduate utama.
- SETELAH graduate: `ctx.CoachCoacheeMappings.Add(new CoachCoacheeMapping { CoachId="coach-baru", CoacheeId=<same>, IsActive=true, StartDate=DateTime.UtcNow, IsCompleted=false })` + `SaveChangesAsync` → TIDAK throw (index bebas karena mapping lama IsActive=false).
- Assert: insert sukses (mapping baru punya Id > 0).

**Fact 3 — `MarkMappingCompleted_Guard_NoTahun3` (D-06 #3, D-08):**
- Seed coachee + CoachCoacheeMapping aktif, TAPI assignment-nya Tahun 1 (atau tanpa Tahun 3) → `tahun3==null`.
- `(ok, error, cascadeCount) = core(...)`.
- Assert: `ok==false`, `Assert.Contains("Tahun 3", error!)`, `cascadeCount==0`; 2nd context: mapping `IsActive==true` + `IsCompleted==false` (tak termutasi).

**Fact 4 — `MarkMappingCompleted_Guard_Tahun3Incomplete` (D-06 #4, D-08):**
- Dua sub-kasus (boleh 1 [Fact] dengan 2 setup atau 2 [Theory] case — pilih [Fact] terpisah `..._ProgressNotApproved` & `..._NoFinalAssessment` bila lebih jelas):
  - (a) Tahun 3 ada, semua progress di-set `Status="Submitted"` (bukan Approved) → `allApproved==false`.
  - (b) Tahun 3 ada, semua progress `Approved` TAPI SKIP `ProtonFinalAssessment` → `hasFinalAssessment==false`.
- Assert keduanya: `ok==false`, `Assert.Contains("belum lulus", error!)`, `cascadeCount==0`, mapping/assignment tak termutasi.

**Fact 5 — `MarkMappingCompleted_MappingNotFound` (D-06 #5, OQ-2):**
- TANPA seed. `core(ctx, mappingId: -1)`.
- Assert: `ok==false`, `Assert.Contains("tidak ditemukan", error!)`, `cascadeCount==0`.

**Fact 6 — `MarkMappingCompleted_ProgressHistoryIntact` (D-06 #6):**
- Seed graduate-ready (progressCount baseline). Panggil core → ok=true.
- 2nd context: `count(ProtonDeliverableProgresses where ProtonTrackAssignmentId==t3AssignmentId) == progressCount` (graduate TIDAK RemoveRange). Boleh fold ke Fact 1 sebagai assert tambahan, TAPI buat [Fact] terpisah agar lock eksplisit & nama jelas saat regresi.
  </behavior>
  <implementation>
**RED→GREEN flow:** Plan 01 sudah membuat `MarkMappingCompletedCore` (GREEN state core sudah ada). Untuk plan test ini, tulis [Fact] yang mendeskripsikan kontrak end-state → jalankan → harus langsung hijau (karena core sudah benar). Bila ada [Fact] merah, itu menandakan (a) seed salah, atau (b) core tak sesuai kontrak → perbaiki seed/investigasi core. JANGAN melonggarkan assert hanya agar hijau.

**Helper seed `SeedGraduateReadyAsync`** — adaptasi `SeedProgressChainAsync` + tambah Tahun 3 + FinalAssessment + Mapping. Resep lengkap siap-pakai ada di RESEARCH.md:286-331 (field wajib/nullable sudah [VERIFIED]). Inti:
```csharp
private static async Task<(int mappingId, int t3AssignmentId, int progressCount)>
    SeedGraduateReadyAsync(ApplicationDbContext ctx, string coacheeId)
{
    var coachId = $"coach-{Guid.NewGuid():N}";
    var t3 = await ctx.ProtonTracks.FirstAsync(t => t.TrackType == "Operator" && t.TahunKe == "Tahun 3");  // REUSE, jangan insert
    var asg = new ProtonTrackAssignment { CoacheeId = coacheeId, AssignedById = "hc", ProtonTrackId = t3.Id, IsActive = true };
    ctx.ProtonTrackAssignments.Add(asg); await ctx.SaveChangesAsync();
    var komp = new ProtonKompetensi { Bagian = "B-PAR", Unit = $"U-{coacheeId[..8]}", NamaKompetensi = $"K-{coacheeId}", Urutan = 1, ProtonTrackId = t3.Id };
    ctx.ProtonKompetensiList.Add(komp); await ctx.SaveChangesAsync();
    var sub = new ProtonSubKompetensi { ProtonKompetensiId = komp.Id, NamaSubKompetensi = "Sub", Urutan = 1 };
    ctx.ProtonSubKompetensiList.Add(sub); await ctx.SaveChangesAsync();
    int progressCount = 0;
    foreach (var i in new[] { 1, 2 })
    {
        var d = new ProtonDeliverable { ProtonSubKompetensiId = sub.Id, NamaDeliverable = $"D{i}", Urutan = i };
        ctx.ProtonDeliverableList.Add(d); await ctx.SaveChangesAsync();
        ctx.ProtonDeliverableProgresses.Add(new ProtonDeliverableProgress {
            CoacheeId = coacheeId, ProtonDeliverableId = d.Id, ProtonTrackAssignmentId = asg.Id,
            Status = "Approved", CreatedAt = DateTime.UtcNow });   // Approved = syarat lulus
        await ctx.SaveChangesAsync(); progressCount++;
    }
    ctx.ProtonFinalAssessments.Add(new ProtonFinalAssessment {
        CoacheeId = coacheeId, CreatedById = "hc", ProtonTrackAssignmentId = asg.Id,
        Status = "Completed", CompletedAt = DateTime.UtcNow });   // 1:1 unik — JANGAN 2 baris
    await ctx.SaveChangesAsync();
    var mapping = new CoachCoacheeMapping {
        CoachId = coachId, CoacheeId = coacheeId, IsActive = true,
        StartDate = DateTime.UtcNow, IsCompleted = false };       // StartDate WAJIB set
    ctx.CoachCoacheeMappings.Add(mapping); await ctx.SaveChangesAsync();
    return (mapping.Id, asg.Id, progressCount);
}
```
**Varian seed untuk guard** (turunkan dari helper di atas, jangan duplikasi penuh):
- #3 no-Tahun3: ganti `t3` jadi `FirstAsync(... TahunKe=="Tahun 1")` (atau seed mapping tanpa assignment Tahun 3).
- #4a: helper normal, lalu set 1 progress `Status="Submitted"` sebelum core.
- #4b: helper normal, lalu SKIP/hapus `ProtonFinalAssessment` (seed varian tanpa FinalAssessment).

**Struktur file:** `using` (System, EF Core, HcPortal.Controllers, HcPortal.Data, HcPortal.Models, Xunit). `namespace HcPortal.Tests;`. `[Trait("Category", "Integration")]` di class. `IClassFixture<ProtonCompletionFixture>` (JANGAN definisi ulang fixture — sudah ada di `ProtonCompletionServiceTests.cs`). Setiap [Fact]: `await using var ctx = new ApplicationDbContext(_fixture.Options);` + `coacheeId` unik `$"grad-{Guid.NewGuid():N}"`. Verifikasi via `await using var verify = new ApplicationDbContext(_fixture.Options)` + `AsNoTracking`.

**Stop dev server dulu** (build-lock caveat Phase 356) sebelum `dotnet test`.

**Catatan:** TIDAK butuh `FakeNotificationService`/`AuditLogService` (notif/audit di wrapper, D-09 tak di-assert). TIDAK butuh SEED_WORKFLOW (DB disposable). Test pakai SQL Server lokal `localhost\SQLEXPRESS` (sama dengan ProtonCompletionServiceTests yang sudah hijau).
  </implementation>
</feature>

<verify>
  <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~MarkMappingCompletedTests" 2>&1 | grep -E "Passed!|Failed!|Passed:|Failed:|error" | head -20</automated>
</verify>

<acceptance_criteria>
- `HcPortal.Tests/MarkMappingCompletedTests.cs` ada, mengandung `[Trait("Category", "Integration")]` DAN `IClassFixture<ProtonCompletionFixture>`
- File mengandung 6 `[Fact]` (grep `\[Fact\]` count >= 6; bila #4 dipecah jadi 2, total >= 6)
- File mengandung `MarkMappingCompletedCore(ctx,` (memanggil core Plan 01)
- File mengandung helper `SeedGraduateReadyAsync` + reuse `ProtonTracks.FirstAsync(t => t.TrackType == "Operator" && t.TahunKe == "Tahun 3")` (TIDAK ada `ctx.ProtonTracks.Add(` — tak insert track baru)
- File TIDAK mengandung definisi `class ProtonCompletionFixture` (reuse, tak redefinisi)
- File mengandung assert token kunci: `Assert.Contains("Tahun 3"`, `Assert.Contains("belum lulus"`, `Assert.Contains("tidak ditemukan"`
- Happy [Fact] mengandung assert: `IsCompleted`, `IsActive`, `CompletedAt`, `EndDate`, `DeactivatedAt`, `cascadeCount`
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~MarkMappingCompletedTests"` → "Passed!" (0 Failed), minimal 6 test jalan
- `dotnet test` full suite → "Passed!" (0 regresi)
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test-only file) | Plan ini hanya menambah file test xUnit. Tidak ada endpoint, input HTTP, data flow produksi, atau permukaan eksternal baru. |

## STRIDE Threat Register

> Plan ini TEST-ONLY (menambah `HcPortal.Tests/MarkMappingCompletedTests.cs`). NOL perubahan produksi. Tidak ada attack surface. Threat register di-dokumentasikan kosong dengan reasoning — bukan threat dikarang.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-365-04 | Information Disclosure | Connection string fixture | accept | Hard-coded `localhost\SQLEXPRESS` + Integrated Security (NO secrets/env vars) di fixture EXISTING yang di-reuse — bukan ditambah plan ini. DB disposable `HcPortalDB_Test_<guid>`; `HcPortalDB_Dev` TAK tersentuh. |
| T-365-05 | Tampering | DB lokal test | accept | Fixture `DisposeAsync` → `EnsureDeletedAsync` drop DB disposable per run (sukses & gagal-mid-migration). Tidak menyentuh data produksi/dev. |

**Kesimpulan keamanan:** File test murni — zero new attack surface. Test memanggil core static in-process via fixture real-SQL disposable. Tidak ada threat high-severity.
</threat_model>

<verification>
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~MarkMappingCompletedTests"` → 6 [Fact] hijau.
- `dotnet test` full suite → semua hijau (test baru + 0 regresi).
- grep konfirmasi: `[Trait("Category","Integration")]`, `IClassFixture<ProtonCompletionFixture>`, 6 [Fact], token kunci D-08, reuse track (no insert).
</verification>

<success_criteria>
- 6 [Fact] AF-3 (D-06) hijau di real-SQL fixture: happy full-end-state (D-07), re-assignability (bukti D-03), guard no-Tahun3, guard Tahun3-incomplete (2 sub-kasus, D-08), mapping-null (OQ-2), history-intact.
- Guard assert pakai token kunci (BUKAN microcopy verbatim) + cek tak-termutasi.
- Helper `SeedGraduateReadyAsync` reuse track migration (no insert), seed FinalAssessment 1:1, mapping aktif dengan StartDate.
- `dotnet test` full suite hijau (bukti parity zero behavior change tergabung dengan Plan 01).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/365-test-hardening-coach-coachee-af-3-xunit/365-02-SUMMARY.md`. Sertakan: daftar 6 [Fact] + nama persis, hasil run filter MarkMappingCompletedTests (jumlah pass), hasil full suite (angka pass + konfirmasi 0 regresi vs baseline Plan 01), catatan bila #4 dipecah jadi 2 [Fact]. WAJIB `git add` file SUMMARY eksplisit (lesson: untracked SUMMARY tidak ter-commit otomatis).
</output>
