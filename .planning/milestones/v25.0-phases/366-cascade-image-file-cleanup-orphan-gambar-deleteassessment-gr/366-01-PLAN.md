---
phase: 366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/ImageFileCleanup.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [SC1-helper-extract]
must_haves:
  truths:
    - "Helper static ImageFileCleanup.DeleteUnreferencedAsync ada di Helpers/ dan meng-encode ref-count AnyAsync + File.Delete warn-only"
    - "3 call-site inline lama (DeletePackage, EditQuestion POST, DeleteQuestion) memanggil helper, bukan loop inline"
    - "Perilaku 3 call-site tak berubah: build 0 error + PackageImageSyncTests + PackageImageDeleteTests existing tetap hijau"
  artifacts:
    - path: "Helpers/ImageFileCleanup.cs"
      provides: "Helper ref-count + File.Delete warn-only (static, di Helpers/)"
      contains: "public static async Task DeleteUnreferencedAsync"
      min_lines: 25
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "3 call-site swap ke helper (perilaku identik)"
      contains: "ImageFileCleanup.DeleteUnreferencedAsync"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs DeletePackage/EditQuestion/DeleteQuestion"
      to: "Helpers/ImageFileCleanup.DeleteUnreferencedAsync"
      via: "await call menggantikan foreach inline"
      pattern: "ImageFileCleanup\\.DeleteUnreferencedAsync"
---

<objective>
Ekstrak satu helper static `Helpers/ImageFileCleanup.cs` dari 3 blok inline ref-count yang byte-identik di `AssessmentAdminController.cs`, lalu alihkan 3 call-site lama (`DeletePackage`, `EditQuestion` POST, `DeleteQuestion`) untuk memanggil helper itu. **Perilaku WAJIB identik** (SC#1) — ini murni lift-and-replace kode sendiri, bukan perubahan logika.

Purpose: 1 sumber kebenaran ref-count (gantikan duplikasi 3x + mirror palsu di test), fondasi untuk pemasangan di 3 method `Delete*` (Plan 02).
Output: `Helpers/ImageFileCleanup.cs` (baru) + 3 loop inline di controller dikompres jadi 1 await call masing-masing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-CONTEXT.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md

<interfaces>
<!-- Kontrak yang dipakai/dibuat plan ini — executor TIDAK perlu explore codebase. -->

DbContext type (ctor AssessmentAdminController.cs:34):
```csharp
HcPortal.Data.ApplicationDbContext   // namespace: HcPortal.Data
```

Field/inherited di AssessmentAdminController (verified):
```csharp
// _context  (ApplicationDbContext) — inherited dari AdminBaseController (base ctor :46)
// _env      (IWebHostEnvironment)  — inherited dari AdminBaseController → _env.WebRootPath
// _logger   (ILogger<AssessmentAdminController>) — field LOKAL controller (:25). Dipakai 3 call-site.
```

Model props yang dibaca helper (sudah ada):
```csharp
// PackageQuestion.ImagePath : string?
// PackageOption.ImagePath   : string?
// DbSet: ctx.PackageQuestions, ctx.PackageOptions
```

Helper analog gaya static (FileUploadHelper.cs:75-79 — konvensi param webRootPath + ILogger):
```csharp
public static async Task<string?> SaveFileAsync(IFormFile? file, string webRootPath, string subFolder, ILogger? logger = null)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat helper static Helpers/ImageFileCleanup.cs (lift blok inline verbatim)</name>
  <files>Helpers/ImageFileCleanup.cs</files>
  <read_first>
    - .planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md (bagian "Helpers/ImageFileCleanup.cs (NEW)" — excerpt (a) blok yang di-lift + "Proposed helper shape" verbatim)
    - Helpers/FileUploadHelper.cs (analog gaya static: namespace HcPortal.Helpers, public static, param webRootPath + ILogger, pola Path.Combine + File.Exists/File.Delete di DeleteFile :114-122)
    - Controllers/AssessmentAdminController.cs:5764-5778 (blok inline sumber DeletePackage — byte-identik dgn 2 call-site lain kecuali label log)
  </read_first>
  <action>
Buat file BARU `Helpers/ImageFileCleanup.cs` PERSIS seperti "Proposed helper shape" di PATTERNS.md. Salin verbatim:

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using HcPortal.Data;

namespace HcPortal.Helpers
{
    public static class ImageFileCleanup
    {
        /// <summary>
        /// Phase 366: Ref-count + physical delete. Untuk tiap path, jika TIDAK ada baris
        /// PackageQuestion/PackageOption yang masih mereferensikannya (post-commit AnyAsync = false
        /// → path khusus-batch), File.Delete; selain itu SKIP (shared Pre/Post path selamat, SC#3).
        /// Warn-only per file (pola Phase 333). WAJIB dipanggil SETELAH tx.CommitAsync.
        /// Path hanya dari kolom DB ImagePath (upload flow tervalidasi) — helper TIDAK menerima
        /// path eksternal user. File.Delete confined di bawah webRootPath via Path.Combine.
        /// </summary>
        public static async Task DeleteUnreferencedAsync(
            ApplicationDbContext ctx,
            string webRootPath,
            ILogger logger,
            IEnumerable<string> paths,
            string source = "")
        {
            foreach (var relUrl in paths.Distinct())
            {
                if (string.IsNullOrEmpty(relUrl)) continue;
                bool stillUsedQ = await ctx.PackageQuestions.AnyAsync(x => x.ImagePath == relUrl);
                bool stillUsedO = await ctx.PackageOptions.AnyAsync(x => x.ImagePath == relUrl);
                if (stillUsedQ || stillUsedO) continue;
                try
                {
                    var physical = Path.Combine(webRootPath, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
                    if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
                }
                catch (Exception fex)
                {
                    logger.LogWarning(fex, "File.Delete post-commit failed ({Source}): {Path}", source, relUrl);
                }
            }
        }
    }
}
```

CATATAN PENTING:
- DbContext type = `ApplicationDbContext` (BUKAN `AppDbContext` — CONTEXT.md keliru tulis AppDbContext; nama nyata di repo = `ApplicationDbContext`, verified ctor :34).
- Ref-count predikat (`AnyAsync` Q + O) WAJIB verbatim — itu yang bikin shared Pre/Post selamat (SC#3). Jangan ganti ke `Any()`/in-memory.
- Path resolution WAJIB pakai `.Replace('/', Path.DirectorySeparatorChar)` (Windows) — identik dgn inline `:5771`.
- Tambah guard `if (string.IsNullOrEmpty(relUrl)) continue;` (defense — collect di Plan 02 sudah filter, ini lapis kedua).
- `using System.Linq;` mungkin perlu untuk `.Distinct()`; cek apakah implicit usings net8 sudah cover (proyek `<ImplicitUsings>enable</ImplicitUsings>` → `System.Linq`/`System.Collections.Generic`/`System.IO` biasanya implicit). Kalau build error CS0103/CS1061 untuk Distinct/IEnumerable/Path, tambah `using System.Linq;` + `using System.IO;` eksplisit.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/ImageFileCleanup.cs` ada; `Select-String "public static async Task DeleteUnreferencedAsync" Helpers/ImageFileCleanup.cs` cocok 1x.
    - `Select-String "AnyAsync" Helpers/ImageFileCleanup.cs` cocok 2x (Q + O guard preserved).
    - `Select-String "namespace HcPortal.Helpers" Helpers/ImageFileCleanup.cs` cocok 1x.
    - `dotnet build` exit 0 (Build succeeded).
  </acceptance_criteria>
  <done>Helper static terkompilasi, ref-count guard utuh, namespace HcPortal.Helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Swap 3 call-site inline ke helper (perilaku identik, SC#1)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - .planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md (tabel "3 call-site swaps" — Site/Method/Replace lines/source label + excerpt replacement)
    - Controllers/AssessmentAdminController.cs:5763-5781 (DeletePackage inline loop + return)
    - Controllers/AssessmentAdminController.cs:6726-6746 (EditQuestion POST inline loop + return)
    - Controllers/AssessmentAdminController.cs:6833-6851 (DeleteQuestion inline loop + return)
  </read_first>
  <action>
Tambah `using HcPortal.Helpers;` di blok using atas `AssessmentAdminController.cs` bila belum ada (cek dulu — kemungkinan sudah ada karena controller pakai FileUploadHelper).

Ganti TIAP dari 3 loop `foreach (var relUrl in imagePathsToDelete.Distinct()) { ... }` jadi SATU baris await. **Variabel `imagePathsToDelete` SUDAH ada & terisi** di tiap site (jangan bikin ulang). **Posisi panggilan TIDAK boleh pindah** (D-02): tetap SETELAH `SyncPackagesToPost`/`SaveChangesAsync`, di tempat persis loop lama berada — kalau dipindah lebih awal, ref-count shared-path rusak.

Ketiga site pakai field kelas `_context`, `_env`, `_logger` (BUKAN logger lokal — itu hanya di 3 Delete* method, Plan 02).

**Site 1 — DeletePackage**, ganti `:5764-5778` (loop) jadi:
```csharp
            await ImageFileCleanup.DeleteUnreferencedAsync(_context, _env.WebRootPath, _logger, imagePathsToDelete, "DeletePackage image");
```

**Site 2 — EditQuestion POST**, ganti `:6729-6743` (loop) jadi:
```csharp
            await ImageFileCleanup.DeleteUnreferencedAsync(_context, _env.WebRootPath, _logger, imagePathsToDelete, "question image");
```

**Site 3 — DeleteQuestion**, ganti `:6834-6848` (loop) jadi:
```csharp
            await ImageFileCleanup.DeleteUnreferencedAsync(_context, _env.WebRootPath, _logger, imagePathsToDelete, "DeleteQuestion image");
```

CATATAN:
- Label `source` per site dipertahankan persis (PATTERNS.md kolom "source label") supaya konteks log warn-only tak hilang.
- Jangan sentuh komentar ordering di atas loop (mis. "ref-count + File.Delete SETELAH auto-sync") — biarkan sebagai dokumentasi ordering.
- Setelah swap, jumlah kemunculan inline predikat `_context.PackageQuestions.AnyAsync(x => x.ImagePath` di controller WAJIB turun dari 3 → 0.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 5; dotnet test --filter "FullyQualifiedName~PackageImageDeleteTests|FullyQualifiedName~PackageImageSyncTests" 2>&1 | Select-String -Pattern "Passed!|Failed!|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String "_context.PackageQuestions.AnyAsync\(x => x.ImagePath" Controllers/AssessmentAdminController.cs` cocok 0x (3 inline predikat tergantikan).
    - `Select-String "ImageFileCleanup.DeleteUnreferencedAsync" Controllers/AssessmentAdminController.cs` cocok 3x (3 call-site).
    - Label `"DeletePackage image"`, `"question image"`, `"DeleteQuestion image"` masing-masing masih ada 1x di controller.
    - `dotnet build` exit 0.
    - `dotnet test --filter "FullyQualifiedName~PackageImageSyncTests"` exit 0 (regresi sync Pre→Post tak berubah).
  </acceptance_criteria>
  <done>3 call-site memanggil helper, 0 loop inline tersisa, build hijau, test sync existing hijau (perilaku identik SC#1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin/HC → DeletePackage/EditQuestion/DeleteQuestion endpoint | Input request (packageId/questionId) — tervalidasi & ber-AntiForgery; TIDAK membawa path file |
| controller → File.Delete (filesystem) | `ImagePath` mengalir ke `Path.Combine(webRoot, ...)` lalu `File.Delete` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-366-01 | Tampering | `ImageFileCleanup.DeleteUnreferencedAsync` path traversal | mitigate | Helper menerima `IEnumerable<string> paths` HANYA dari kolom DB `ImagePath` (ditulis upload flow tervalidasi FileUploadHelper Phase 325 — strip dir component + magic-byte). Helper TIDAK menerima path eksternal user. `Path.Combine(webRootPath, relUrl.TrimStart('/')...)` confine di bawah webroot; tak ada `..` karena nilai DB selalu `/uploads/questions/{id}/{file}`. Posisi keamanan IDENTIK dgn inline lama (no new vector). |
| T-366-02 | Denial of Service | File.Delete gagal (lock/permission) abort request | accept→mitigate | `try/catch` warn-only per file (pola 333) — kegagalan FS hanya di-log `LogWarning`, tak melempar, tak rollback DB. Tak ada DoS surface baru. |
| T-366-03 | Information Disclosure | log path di warn | accept | Path relatif `/uploads/...` di log warn = non-sensitive (sudah pola existing); low value. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- Grep: 0 loop `AnyAsync(x => x.ImagePath` inline di controller; 3 `ImageFileCleanup.DeleteUnreferencedAsync`.
- `dotnet test --filter "FullyQualifiedName~PackageImageSyncTests"` hijau (SC#1 perilaku identik — sync Pre→Post tak regresi).
</verification>

<success_criteria>
- Helper static `Helpers/ImageFileCleanup.cs` ada + terkompilasi, ref-count guard utuh.
- 3 call-site lama memanggil helper; 0 logika inline tersisa; perilaku tak berubah (build + test sync hijau).
- Fondasi siap untuk Plan 02 (install di 3 Delete*).
</success_criteria>

<notes>
**File-overlap Phase 367:** 3 method `Delete*` (target Plan 02) = endpoint yang dirombak Phase 367 (cascade overhaul). Helper ini WAJIB di-preserve oleh 367 (anotasi ROADMAP :282). Plan 01 hanya bikin helper + swap call-site Manage Package (BUKAN Delete*), jadi konflik minimal — tapi simpan helper sebagai 1 sumber kebenaran.
**Logger gotcha (untuk Plan 02):** 3 call-site di plan ini pakai field `_logger`. Tapi 3 method `Delete*` (Plan 02) pakai logger LOKAL `logger` (`HttpContext.RequestServices.GetRequiredService<...>()`), bukan `_logger`. Jangan tertukar.
</notes>

<output>
After completion, create `.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-01-SUMMARY.md`
</output>
