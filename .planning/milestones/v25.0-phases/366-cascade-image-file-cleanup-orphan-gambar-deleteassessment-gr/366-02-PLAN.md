---
phase: 366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [SC2-cascade-install, SC3-shared-survive]
must_haves:
  truths:
    - "DeleteAssessment kumpul ImagePath Distinct dari packages SEBELUM RemoveRange, panggil helper SETELAH tx.CommitAsync"
    - "DeleteAssessmentGroup kumpul ImagePath dari allPackages (multi-sibling) SEBELUM RemoveRange, panggil helper SETELAH commit"
    - "DeletePrePostGroup kumpul ImagePath dari allPackages (Pre+Post 1 batch) SEBELUM RemoveRange, panggil helper SETELAH commit"
    - "Shared-path Pre↔Post selamat saat 1 sisi dihapus (post-commit AnyAsync masih true) — dijamin tanpa exclusion-set (D-05)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Image cleanup terpasang di 3 cascade method (post-commit)"
      contains: "DeleteUnreferencedAsync(_context, _env.WebRootPath, logger, imagePaths"
  key_links:
    - from: "DeleteAssessment/DeleteAssessmentGroup/DeletePrePostGroup (post-commit)"
      to: "Helpers/ImageFileCleanup.DeleteUnreferencedAsync"
      via: "await call SETELAH tx.CommitAsync, paths dikumpul SEBELUM RemoveRange"
      pattern: "ImageFileCleanup\\.DeleteUnreferencedAsync"
    - from: "collect imagePaths"
      to: "packages/allPackages.SelectMany(Questions+Options).ImagePath"
      via: "Distinct() sebelum RemoveRange"
      pattern: "SelectMany.*ImagePath"
---

<objective>
Pasang helper `ImageFileCleanup.DeleteUnreferencedAsync` (dari Plan 01) di 3 method cascade-delete `AssessmentAdminController.cs`: `DeleteAssessment`, `DeleteAssessmentGroup`, `DeletePrePostGroup`. Tiap method saat ini `RemoveRange` Questions/Options dari DB tapi membiarkan file gambar fisik jadi sampah disk (orphan). Pasang dgn pola atomic Phase 333: **kumpul `ImagePath` Distinct SEBELUM `RemoveRange`, panggil helper SETELAH `tx.CommitAsync`**.

Purpose: tutup orphan disk file saat cascade delete besar (SC#2) sambil menjamin shared-path Pre↔Post selamat (SC#3) — keduanya gratis via post-commit `AnyAsync` (D-05, tanpa exclusion-set).
Output: 3 method cascade dgn collect-before + helper-after-commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-CONTEXT.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-01-SUMMARY.md

<interfaces>
<!-- Kontrak helper dari Plan 01 — executor pakai langsung. -->

Helper (Helpers/ImageFileCleanup.cs, dibuat Plan 01):
```csharp
public static async Task DeleteUnreferencedAsync(
    ApplicationDbContext ctx, string webRootPath, ILogger logger,
    IEnumerable<string> paths, string source = "")
```

Logger per method (KRITIS — beda dari 3 call-site Plan 01):
```csharp
// DeleteAssessment      : logger lokal (:2191)  var logger = HttpContext.RequestServices.GetRequiredService<ILogger<AssessmentAdminController>>();
// DeleteAssessmentGroup : logger lokal (awal method, pola sama)  → pakai `logger`
// DeletePrePostGroup    : logger lokal (:2565)  → pakai `logger`
// _env tetap field kelas → _env.WebRootPath
```

Model (sudah ter-Include nested di ketiga method):
```csharp
// AssessmentPackage.Questions : List<PackageQuestion>
// PackageQuestion.Options     : List<PackageOption>
// PackageQuestion.ImagePath / PackageOption.ImagePath : string?
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pasang collect+helper di DeleteAssessment</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - .planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md (bagian "DeleteAssessment ... anchors" — tabel line + excerpt collect SelectMany + posisi call)
    - Controllers/AssessmentAdminController.cs:2189-2348 (DeleteAssessment full: logger :2191, packages Include :2314-2317, RemoveRange :2318-2328, SaveChanges+Commit :2333-2334, audit try :2337)
    - Helpers/ImageFileCleanup.cs (signature helper dari Plan 01)
  </read_first>
  <action>
Di `DeleteAssessment` (`:2189`):

**(1) Collect** — sisipkan PERSIS setelah `:2317` (akhir `.ToListAsync()` untuk `packages`), SEBELUM `if (packages.Any())` di `:2318`:
```csharp
                var imagePaths = packages
                    .SelectMany(p => p.Questions)
                    .SelectMany(q => new[] { q.ImagePath }.Concat(q.Options.Select(o => o.ImagePath)))
                    .Where(p => !string.IsNullOrEmpty(p))
                    .Select(p => p!)
                    .Distinct()
                    .ToList();
```

**(2) Panggil helper** — sisipkan PERSIS setelah `await tx.CommitAsync();` (`:2334`), SEBELUM blok audit-log `try` (`:2337`):
```csharp
                // Phase 366: hapus file gambar orphan SETELAH commit (atomic Phase 333). Post-commit
                // AnyAsync auto-sadar batch + shared Pre/Post selamat (D-05). logger lokal (BUKAN _logger).
                await ImageFileCleanup.DeleteUnreferencedAsync(_context, _env.WebRootPath, logger, imagePaths, "DeleteAssessment image");
```

CATATAN PENTING:
- `logger` di sini = variabel LOKAL (`:2191`), BUKAN `_logger`. Pass `logger`.
- `_env` tetap field kelas → `_env.WebRootPath`.
- Collect WAJIB sebelum `RemoveRange` (`:2320-2326`) — kalau setelah, Questions/Options sudah dilepas dari graph & ImagePath hilang.
- `packages` sudah `.Include(p => p.Questions).ThenInclude(q => q.Options)` (`:2314-2317`) → ImagePath in-memory, TIDAK perlu query/Include tambahan.
- `DeleteAssessment` blok-block individual session (bukan Pre/Post group) — pre-check di `:2209-2213` sudah tolak PreTest/PostTest. Jadi file di sini umumnya tak shared; tapi helper tetap ref-count (defense, gratis).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String "DeleteAssessment image" Controllers/AssessmentAdminController.cs` cocok 1x.
    - Dalam method DeleteAssessment, `var imagePaths =` muncul SEBELUM `_context.AssessmentPackages.RemoveRange(packages)` (urutan baris collect < RemoveRange).
    - Panggilan `ImageFileCleanup.DeleteUnreferencedAsync(...imagePaths, "DeleteAssessment image")` muncul SETELAH `await tx.CommitAsync();` (urutan baris commit < helper).
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>DeleteAssessment kumpul paths sebelum RemoveRange + panggil helper post-commit, build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Pasang collect+helper di DeleteAssessmentGroup + DeletePrePostGroup</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - .planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md (bagian "DeleteAssessmentGroup anchors" + "DeletePrePostGroup anchors" + catatan D-05/SC#3 Pre+Post 1 batch)
    - Controllers/AssessmentAdminController.cs:2429-2520 (DeleteAssessmentGroup: allPackages Include :2497-2500, RemoveRange :2503-2511, SaveChanges+Commit :2518-2519, logger lokal di awal method)
    - Controllers/AssessmentAdminController.cs:2563-2697 (DeletePrePostGroup: logger :2565, allPackages Include :2677-2680, RemoveRange :2683-2690, SaveChanges+Commit :2695-2696)
  </read_first>
  <action>
**Method A — DeleteAssessmentGroup** (collect var `allPackages`, multi-sibling batch):

(1) Collect — sisipkan setelah `:2500` (`.ToListAsync()` untuk `allPackages`), SEBELUM `if (allPackages.Any())` (`:2501`):
```csharp
                var imagePaths = allPackages
                    .SelectMany(p => p.Questions)
                    .SelectMany(q => new[] { q.ImagePath }.Concat(q.Options.Select(o => o.ImagePath)))
                    .Where(p => !string.IsNullOrEmpty(p))
                    .Select(p => p!)
                    .Distinct()
                    .ToList();
```

(2) Panggil helper — sisipkan setelah `await tx.CommitAsync();` (`:2519`), sebelum blok audit `try` (`:2522`):
```csharp
                // Phase 366: hapus file gambar orphan SETELAH commit. logger lokal method ini.
                await ImageFileCleanup.DeleteUnreferencedAsync(_context, _env.WebRootPath, logger, imagePaths, "DeleteAssessmentGroup image");
```
Konfirmasi nama variabel logger lokal di method ini saat baca file (`var logger = HttpContext.RequestServices...` di awal method DeleteAssessmentGroup — pola sama spt DeleteAssessment/DeletePrePostGroup). Pass nama itu.

**Method B — DeletePrePostGroup** (collect var `allPackages`, Pre+Post = 2 paket 1 batch):

(1) Collect — sisipkan setelah `:2680` (`.ToListAsync()` untuk `allPackages`), SEBELUM `if (allPackages.Any())` (`:2681`):
```csharp
                var imagePaths = allPackages
                    .SelectMany(p => p.Questions)
                    .SelectMany(q => new[] { q.ImagePath }.Concat(q.Options.Select(o => o.ImagePath)))
                    .Where(p => !string.IsNullOrEmpty(p))
                    .Select(p => p!)
                    .Distinct()
                    .ToList();
```

(2) Panggil helper — sisipkan setelah `await tx.CommitAsync();` (`:2696`), sebelum blok audit `try` (`:2699`):
```csharp
                // Phase 366 / SC#3: Pre+Post dihapus 1 batch → shared path tak lagi direferensikan
                // baris mana pun → AnyAsync false → file dihapus benar (D-05). logger lokal (:2565).
                await ImageFileCleanup.DeleteUnreferencedAsync(_context, _env.WebRootPath, logger, imagePaths, "DeletePrePostGroup image");
```

CATATAN PENTING:
- Kedua method: `logger` = variabel LOKAL (BUKAN `_logger`). `_env` = field kelas.
- Kedua `allPackages` sudah `.Include(p => p.Questions).ThenInclude(q => q.Options)` → tak perlu Include tambahan.
- Collect WAJIB sebelum RemoveRange masing-masing.
- DeletePrePostGroup: ini kasus paling rawan (Pre+Post share path) — TAPI karena keduanya dihapus dalam 1 batch & helper dipanggil post-commit, `AnyAsync` jadi false → file SHARED dihapus dgn benar (memang harus, tak ada sisi lain yg pakai). Bedakan dgn DeletePackage/DeleteQuestion (single side) di Plan 01 dimana sisi Post survive → file selamat. Jangan tambah exclusion-set apa pun.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 5; (Select-String "ImageFileCleanup.DeleteUnreferencedAsync" Controllers/AssessmentAdminController.cs | Measure-Object).Count</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String "DeleteAssessmentGroup image" Controllers/AssessmentAdminController.cs` cocok 1x.
    - `Select-String "DeletePrePostGroup image" Controllers/AssessmentAdminController.cs` cocok 1x.
    - Total `Select-String "ImageFileCleanup.DeleteUnreferencedAsync" Controllers/AssessmentAdminController.cs` cocok 6x (3 dari Plan 01 + 3 install Plan 02).
    - Tidak ada `exclusion` / `excludeSet` / variabel set tambahan untuk ref-count (D-05 — murni post-commit AnyAsync).
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>DeleteAssessmentGroup + DeletePrePostGroup kumpul paths sebelum RemoveRange + panggil helper post-commit; total 6 call helper di controller; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin/HC → DeleteAssessment/Group/PrePostGroup endpoint | Input id/linkedGroupId — ber-AntiForgery (`[ValidateAntiForgeryToken]`), `[Authorize(Admin,HC)]`; TIDAK membawa path |
| controller (post-commit) → File.Delete | `ImagePath` dari kolom DB packages ter-Include → `Path.Combine(webRoot,...)` → File.Delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-366-04 | Tampering | path traversal via ImagePath di 3 cascade | mitigate | `imagePaths` dikumpul HANYA dari `packages/allPackages` (kolom DB ImagePath, di-Include dari DB) — sumber sama dgn inline lama. Helper confine `Path.Combine(webRootPath,...)` di bawah webroot; nilai DB selalu `/uploads/questions/...`, tak ada user-supplied path. Tak ada vektor baru: sentralisasi logika = posisi keamanan identik. |
| T-366-05 | Tampering | over-delete file shared (hapus file yg masih dipakai) | mitigate | Post-commit `AnyAsync` ref-count (D-05): file dihapus HANYA jika TIADA baris Q/O lain mereferensikan. Shared Pre↔Post single-side → Post survive → `AnyAsync` true → SKIP (SC#3). Diuji eksplisit Plan 03 integration test. |
| T-366-06 | Denial of Service | File.Delete gagal abort cascade | mitigate | Helper dipanggil SETELAH `CommitAsync` (atomic Phase 333) + internal try/catch warn-only → kegagalan FS tak rollback DB, tak melempar. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- Grep: 6x `ImageFileCleanup.DeleteUnreferencedAsync` total (3 swap Plan 01 + 3 install Plan 02); label `DeleteAssessment image`/`DeleteAssessmentGroup image`/`DeletePrePostGroup image` masing-masing 1x.
- Urutan per method: collect (`var imagePaths`) < `RemoveRange` < `CommitAsync` < helper call.
- Tak ada exclusion-set (D-05 murni post-commit AnyAsync).
- Verifikasi fungsional (file fisik benar terhapus / shared selamat) = Plan 03 (integration test + UAT).
</verification>

<success_criteria>
- 3 method cascade memanggil helper post-commit dgn paths dikumpul pre-RemoveRange.
- SC#2 (orphan file dibersihkan) + SC#3 (shared selamat) ter-encode via pola D-05/D-06 (post-commit AnyAsync, tanpa exclusion-set).
- Build hijau; siap diverifikasi end-to-end di Plan 03.
</success_criteria>

<notes>
**File-overlap Phase 367 (KRITIS):** 3 method ini (`DeleteAssessment`/`DeleteAssessmentGroup`/`DeletePrePostGroup`) = endpoint yang dirombak Phase 367 (cascade overhaul + file-cert + pre-check renewal). Pemasangan helper image di sini WAJIB di-preserve oleh 367 (anotasi ROADMAP :282 + CONTEXT `<code_context>`). Jaga perubahan surgical + komentar `// Phase 366` jelas supaya 367 bisa build di atasnya tanpa menghapus cleanup gambar.
**Migration=false. NOT pushed.** Verifikasi lokal saja (CLAUDE.md Develop Workflow).
</notes>

<output>
After completion, create `.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-02-SUMMARY.md`
</output>
