---
phase: 367-delete-records-cascade-overhaul
plan: 05
type: execute
wave: 3
depends_on: ["367-02", "367-04"]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/CertFileTab1Tests.cs
autonomous: true
requirements: ["#19", "L-03", "L-08"]

must_haves:
  truths:
    - "3 endpoint tab 1 (DeleteAssessment/DeleteAssessmentGroup/DeletePrePostGroup) mengumpulkan + menghapus file sertifikat manual (ManualSertifikatUrl) post-commit warn-only (#19)"
    - "Pre-check renewal BLOKIR (fase 325/329) di 3 endpoint tab 1 DIBALIK jadi cascade penuh (L-03) — induk dihapus = turunan ikut, BUKAN ditolak"
    - "Image-cleanup 366 di 3 endpoint dipertahankan VERBATIM (collect ImagePath sebelum RemoveRange + helper post-commit, logger LOKAL) — tidak hilang, tidak dobel"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "3 endpoint tab 1: collect sertifikat manual + file-delete post-commit (#19), pre-check renewal → cascade (L-03), preserve image 366"
    - path: "HcPortal.Tests/CertFileTab1Tests.cs"
      provides: "[Fact] file sertifikat manual terhapus post-commit di endpoint tab 1 (#19)"
  key_links:
    - from: "DeleteAssessment/Group/PrePost"
      to: "file sertifikat manual on disk"
      via: "collect ManualSertifikatUrl pre-Remove → File.Delete post-commit"
      pattern: "ManualSertifikatUrl"
    - from: "DeleteAssessment/Group/PrePost"
      to: "ImageFileCleanup.DeleteUnreferencedAsync (366)"
      via: "preserve verbatim post-commit"
      pattern: "ImageFileCleanup.DeleteUnreferencedAsync"
---

<objective>
Tiga endpoint hapus tab 1 (`DeleteAssessment` :2189, `DeleteAssessmentGroup` :2389, `DeletePrePostGroup` :2586) saat ini: (a) TIDAK menghapus file fisik sertifikat manual (#19 — hanya image soal via 366), dan (b) memBLOKIR penghapusan jika ada turunan renewal (pre-check fase 325/329), bertentangan dengan kebijakan no-blocker (L-03).

Plan ini:
1. **#19 file-cert:** di 3 endpoint, kumpulkan `ManualSertifikatUrl` (sertifikat manual) dari session yang terhapus SEBELUM RemoveRange → File.Delete POST-commit, inner try/catch warn-only (L-08). Jalur file TERPISAH dari image-cleanup 366 (Opsi B: image soal = 366, sertifikat = 367).
2. **L-03 no-blocker:** ganti pre-check renewal BLOKIR (DeleteAssessment :2218-2230, DeleteAssessmentGroup pre-check, DeletePrePostGroup :2612-2624) → panggil cascade engine `RecordCascadeDeleteService` agar turunan renewal IKUT terhapus (parity perilaku tab 2). Untuk DeleteAssessment node tunggal: route ke `ExecuteAsync("session", id, ...)` ATAU tambahkan traversal renewal in-line yang konsisten dengan engine.
3. **PRESERVE image 366 (D-04, Pitfall 4):** image-cleanup 366 di 3 endpoint dipertahankan verbatim (collect ImagePath sebelum RemoveRange + `ImageFileCleanup.DeleteUnreferencedAsync` post-commit, `logger` LOKAL). Jangan hilangkan, jangan duplikasi.

Purpose: Konsistensi no-blocker lintas tab 1 dan tab 2 (kebijakan user L-01/L-03) + bersihkan file sertifikat orphan (#19). Tanpa ini, hapus dari tab 1 masih blokir saat ada renewal dan tinggalkan file sertifikat di disk.
Output: 3 endpoint ter-update + [Fact] file sertifikat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-PATTERNS.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-02-SUMMARY.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-04-SUMMARY.md

<image-366-preserve>
<!-- KONTRAK D-04. Sumber: RESEARCH §"Overlap 366" (VERIFIED). JANGAN ubah/hilangkan/duplikasi. -->
| Endpoint | Collect imagePaths (sebelum RemoveRange) | Helper call (POST tx.CommitAsync) | Logger |
|----------|------------------------------------------|-----------------------------------|--------|
| DeleteAssessment | :2318-2325 (packages.SelectMany Q+O ImagePath, Distinct) | :2346 "DeleteAssessment image" | `logger` LOKAL (:2191 GetRequiredService) |
| DeleteAssessmentGroup | :2513-2520 | :2542 "DeleteAssessmentGroup image" | `logger` LOKAL :2391 |
| DeletePrePostGroup | :2704-2711 | :2732 "DeletePrePostGroup image" | `logger` LOKAL :2588 |
GOTCHA: `logger` LOKAL di method ini (BUKAN field `_logger`). File sertifikat (#19) = jalur cleanup TERPISAH, jangan campur dengan ImageFileCleanup.
</image-366-preserve>

<file-atomicity>
<!-- Pola L-08. Sumber: TrainingAdminController.cs:579-623 (VERIFIED). Confined webroot (V12). -->
```csharp
// Collect SEBELUM Remove (record detached post-Remove):
var certPaths = sessions.Where(s => !string.IsNullOrEmpty(s.ManualSertifikatUrl))
    .Select(s => Path.Combine(_env.WebRootPath, s.ManualSertifikatUrl!.TrimStart('/'))).ToList();
// ... RemoveRange + SaveChanges + tx.CommitAsync ...
// File.Delete POST commit warn-only:
foreach (var p in certPaths)
    if (System.IO.File.Exists(p)) { try { System.IO.File.Delete(p); } catch (Exception ex) { logger.LogWarning(ex, "Cert File.Delete post-commit failed: {Path}", p); } }
```
VERIFIKASI nama field saat baca Models/AssessmentSession.cs: `ManualSertifikatUrl` (sertifikat untuk assessment manual). Jika field bernama beda, pakai nama aktual.
</file-atomicity>

<pre-check-blokir>
<!-- L-03: pre-check BLOKIR yang DIBALIK. Sumber VERIFIED. -->
- DeleteAssessment :2218-2230: `refTr + refAs > 0` → TempData Error + return (BLOKIR). Hapus blok return; ganti dengan cascade turunan.
- DeleteAssessmentGroup pre-check renewal (~:2420+, substitusi siblingIds.Contains) → BLOKIR. Balik jadi cascade.
- DeletePrePostGroup pre-check renewal :2612-2624 → BLOKIR. Balik jadi cascade.
Cascade konsisten: gunakan RecordCascadeDeleteService.ExecuteAsync (untuk node session) atau traversal renewal in-line yang memanggil engine. Untuk grup (multi-session), iterasi ExecuteAsync per representative ATAU perluas engine menerima multi-root (putuskan saat plan; minimal: pastikan turunan renewal tiap session grup ikut terhapus, tidak diblokir).
</pre-check-blokir>

<interfaces>
RecordCascadeDeleteService (dari plan 01/02) — sudah ter-DI:
```csharp
public async Task<CascadeResult> ExecuteAsync(string rootType, int rootId, IEnumerable<int> mirrorTrainingIds, string actorId, string actorName);
```
Inject ke AssessmentAdminController via ctor (pola DI existing) atau `HttpContext.RequestServices.GetRequiredService<RecordCascadeDeleteService>()`.
CATATAN: engine ExecuteAsync sudah handle artefak per node (termasuk image? TIDAK — Opsi B: image soal tetap di endpoint via ImageFileCleanup). Jika endpoint route ke engine, pastikan image-cleanup 366 TETAP dipanggil endpoint (engine tidak sentuh image soal). Pertimbangkan: endpoint kumpulkan imagePaths SEBELUM panggil engine (entitas masih ada), panggil engine (hapus DB + commit), lalu endpoint panggil ImageFileCleanup + File.Delete cert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: #19 collect + File.Delete sertifikat manual post-commit di 3 endpoint tab 1</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:2189-2383 (DeleteAssessment penuh — tx, image 366 :2318-2346, audit)
    - Controllers/AssessmentAdminController.cs:2389-2560 (DeleteAssessmentGroup — sibling filter dari plan 04, image 366 :2513-2542)
    - Controllers/AssessmentAdminController.cs:2586-2769 (DeletePrePostGroup — image 366 :2704-2732, pre-check :2612-2624)
    - Models/AssessmentSession.cs (verifikasi field ManualSertifikatUrl — nama eksak)
    - Controllers/TrainingAdminController.cs:579-623 (pola capture-path-pre-Remove + File.Delete post-commit warn-only)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Overlap 366" + #19
  </read_first>
  <action>
Untuk MASING-MASING dari 3 endpoint (DeleteAssessment, DeleteAssessmentGroup, DeletePrePostGroup):
1. SEBELUM RemoveRange session (di samping collect imagePaths 366 existing), kumpulkan `certPaths` dari `ManualSertifikatUrl` session(s) yang akan dihapus → `Path.Combine(_env.WebRootPath, url.TrimStart('/'))` (confined webroot, V12). Untuk grup: kumpulkan dari semua sibling/group session.
2. SETELAH `tx.CommitAsync` (DAN setelah `ImageFileCleanup.DeleteUnreferencedAsync` 366 yang dipreserve), tambahkan loop File.Delete certPaths, inner try/catch warn-only per file (pakai `logger` LOKAL yang sama di method ini).
3. PRESERVE image-cleanup 366 verbatim (collect + helper post-commit) — JANGAN ubah baris itu. Jalur cert TERPISAH dan TAMBAHAN, bukan pengganti.

Buat `HcPortal.Tests/CertFileTab1Tests.cs` — [Fact] (≥3, satu per endpoint atau parametrik): seed session dengan ManualSertifikatUrl + file fisik (WriteFakeImage pola ImageCleanupIntegrationTests) di temp webroot, panggil endpoint/logic delete, assert `File.Exists(certPath)==false` post-commit. `[Trait("Category","Integration")]` bila pakai real-SQL.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~CertFileTab1Tests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ManualSertifikatUrl"` muncul di ketiga endpoint (collect cert)
    - File.Delete cert dipanggil POST `CommitAsync` (baca urutan: collect < RemoveRange < SaveChanges < CommitAsync < ImageFileCleanup(366) < File.Delete cert)
    - Image-cleanup 366 TETAP utuh: `grep "DeleteAssessment image"`, `"DeleteAssessmentGroup image"`, `"DeletePrePostGroup image"` ketiganya masih match
    - `CertFileTab1Tests.cs` assert `File.Exists(...)==false`; filter test exit 0
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>3 endpoint tab 1 hapus file sertifikat manual post-commit warn-only; image 366 utuh; teruji file-on-disk.</done>
</task>

<task type="auto">
  <name>Task 2: L-03 balik pre-check renewal BLOKIR → cascade penuh di 3 endpoint tab 1</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:2218-2230 (DeleteAssessment pre-check BLOKIR refTr+refAs>0)
    - Controllers/AssessmentAdminController.cs:2420-2440 (DeleteAssessmentGroup pre-check renewal siblingIds.Contains)
    - Controllers/AssessmentAdminController.cs:2612-2624 (DeletePrePostGroup pre-check renewal)
    - Services/RecordCascadeDeleteService.cs (ExecuteAsync final dari plan 02 — signature + behavior)
    - .planning/phases/367-...-cascad/367-02-SUMMARY.md (signature ExecuteAsync final + apakah actor param ditambah)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"State of the Art" (pre-check = BLOKIR → no-blocker)
  </read_first>
  <action>
Untuk 3 endpoint: HAPUS blok pre-check renewal yang melakukan `TempData["Error"] + return` saat ada turunan renewal (DeleteAssessment :2223-2230, dan blok serupa di Group/PrePost). GANTI agar turunan renewal IKUT terhapus (no-blocker L-03):
- **DeleteAssessment (node tunggal):** route ke `RecordCascadeDeleteService.ExecuteAsync("session", id, Enumerable.Empty<int>(), actor.Id, actorName)`. Endpoint TETAP kumpulkan imagePaths 366 + certPaths SEBELUM panggil engine (entitas masih ada), lalu setelah engine sukses panggil `ImageFileCleanup.DeleteUnreferencedAsync` (366 preserve) + File.Delete cert. ATAU: jika lebih aman pertahankan struktur tx existing, tambahkan traversal renewal in-line yang menghapus turunan dengan urutan engine — putuskan saat plan, prioritas: turunan IKUT terhapus + image 366 tetap jalan + cert terhapus.
- **DeleteAssessmentGroup / DeletePrePostGroup (multi-session):** untuk tiap session dalam grup, pastikan turunan renewal-nya ikut terhapus (panggil engine per session, atau perluas traversal). Hindari duplikasi penghapusan node yang sama (engine sudah idempoten via cycle guard, tapi cross-call bisa dobel — pakai HashSet Id yang sudah terhapus, atau panggil engine sekali per root unik).

KONSISTENSI: tidak boleh ada lagi jalur yang menolak hapus karena turunan renewal (L-03). Preserve guard NON-renewal lain (mis. D-19 Pre/Post block di DeleteAssessment :2208-2213 = guard berbeda, BUKAN renewal — boleh tetap, atau sesuaikan jika spec; jangan hapus tanpa alasan). AuditLog tetap 1 entri/operasi.

Pastikan tidak ada DOBEL image-cleanup: jika engine TIDAK sentuh image (Opsi B), endpoint memanggilnya sekali. Jika route ke engine mengubah lifecycle tx, sesuaikan agar image 366 + cert tetap post-commit sekali.

Tambah/extend test di `CertFileTab1Tests.cs` (atau test integration baru): seed session induk tab 1 + turunan renewal (TR/AS anak) → DeleteAssessment → assert turunan IKUT terhapus (tidak diblokir), DB bersih.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~CertFileTab1Tests"; dotnet test HcPortal.Tests</automated>
  </verify>
  <acceptance_criteria>
    - DeleteAssessment TIDAK lagi memiliki blok `refTr + refAs > 0 → TempData Error + return` (pre-check BLOKIR dihapus); `grep "menggunakan record ini sebagai sumber renewal"` di DeleteAssessment TIDAK match (pesan blokir hilang)
    - Turunan renewal IKUT terhapus: test seed induk+anak → DeleteAssessment → anak CountAsync==0
    - Image-cleanup 366 ketiga endpoint TETAP match (`grep "image"` label ketiganya) dan dipanggil TEPAT sekali per operasi (tidak dobel)
    - File sertifikat cert (#19, Task 1) tetap terhapus
    - `dotnet test` full suite hijau (termasuk regresi image 366 ImageCleanupIntegrationTests)
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>3 endpoint tab 1 no-blocker: turunan renewal ikut terhapus, image 366 preserved (sekali), cert terhapus; konsisten tab 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC → DeleteAssessment/Group/PrePost | POST terotentikasi; cascade rekursif high-impact dari tab 1 |
| Endpoint → filesystem (image 366 + cert 367) post-commit | 2 jalur File.Delete; path traversal risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-16 | Tampering (over-deletion) | Cascade tab 1 nyasar saat balik no-blocker | mitigate | Pakai engine ExecuteAsync (preview==execute traversal); guard cycle; 1-tx rollback. Sibling filter #18 (plan 04) batasi grup |
| T-367-17 | Tampering | Path traversal File.Delete cert | mitigate | Confined webroot Path.Combine TrimStart('/') (V12) |
| T-367-18 | Repudiation | Hapus cascade tab 1 tanpa jejak | mitigate | AuditLog 1 entri/operasi (engine + endpoint) |
| T-367-19 | Tampering | Image 366 hilang/dobel saat refactor | mitigate | Preserve verbatim (Opsi B separasi); acceptance grep label image ketiga endpoint + test ImageCleanupIntegrationTests hijau |
| T-367-20 | Spoofing/CSRF | POST tanpa token | mitigate | `[ValidateAntiForgeryToken]` + `[Authorize(Roles="Admin, HC")]` existing — preserve |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` full suite hijau (termasuk regresi 366 + cert baru).
- Pre-check BLOKIR hilang; turunan renewal ikut terhapus dari tab 1; image 366 utuh sekali; cert terhapus.
</verification>

<success_criteria>
- 3 endpoint tab 1: no-blocker (L-03) + file cert post-commit (#19) + image 366 preserved (D-04); Migration = FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-05-SUMMARY.md`
</output>
