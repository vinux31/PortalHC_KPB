---
phase: 367-delete-records-cascade-overhaul
plan: 07
type: execute
wave: 4
depends_on: ["367-06"]
files_modified:
  - Controllers/TrainingAdminController.cs
  - Models/ImportTrainingResult.cs
  - HcPortal.Tests/DuplicateGuardTests.cs
autonomous: true
requirements: ["#12", "#14", "D-02"]

must_haves:
  truths:
    - "AddManualAssessment menolak (reject + pesan) kombinasi user+judul+tanggal yang PERSIS sama dengan session existing (D-02 EXACT, #12)"
    - "ImportTraining melewati (skip-with-report) baris duplikat exact user+judul+tanggal — status 'duplikat — dilewati' (#12)"
    - "BulkBackfillAssessment melewati (skip-with-report) baris duplikat exact — tidak increment success (#14)"
    - "Toleransi ±1 hari TIDAK dipakai di guard create (re-entry tanggal beda lolos) — itu hanya untuk mirror preview"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "Guard duplikat EXACT user+judul+tanggal di 3 pintu (reject single / skip import+backfill)"
    - path: "Models/ImportTrainingResult.cs"
      provides: "Dukungan status 'Skip' untuk laporan duplikat"
    - path: "HcPortal.Tests/DuplicateGuardTests.cs"
      provides: "Unit: 3 pintu guard (reject/skip) + EXACT match + re-entry tanggal beda lolos"
  key_links:
    - from: "AddManualAssessment / ImportTraining / BulkBackfillAssessment"
      to: "AssessmentSessions existing (UserId+Title+CompletedAt)"
      via: "AnyAsync EXACT guard sebelum Add"
      pattern: "CompletedAt =="
---

<objective>
Pasang guard duplikat di 3 pintu input (#12/#14) — cegah session manual ganda dengan kombinasi `UserId + Title + CompletedAt` (tanggal) yang PERSIS sama. D-02: EXACT match (BEDA dari heuristik mirror ±1 hari yang hanya untuk preview). Perilaku per pintu (locked §3.3):
- **AddManualAssessment** (single): **REJECT** dengan `ModelState.AddModelError` + `return View(model)`.
- **ImportTraining** (batch Excel): **SKIP-with-report** — `ImportTrainingResult.Status="Skip"`, message "duplikat — dilewati", `continue`.
- **BulkBackfillAssessment** (batch Excel, in-tx): **SKIP-with-report** — kolom status "duplikat — dilewati", jangan increment `success`.

Purpose: Cegah akumulasi duplikat di sumber (akar masalah data kotor #12/#14) tanpa false-positive (re-entry tanggal beda = sah, lolos). EXACT match minim false-positive (Pitfall 7). Plan ini di Wave 4 karena `TrainingAdminController.cs` overlap dengan plan 06 (sekuensial).
Output: 3 guard + dukungan status Skip + unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-PATTERNS.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-06-SUMMARY.md

<interfaces>
<!-- Line aktual VERIFIED 2026-06-12. Predikat EXACT (D-02). -->

| Pintu | Insert point | Perilaku | Predikat cek existing |
|-------|-------------|----------|------------------------|
| AddManualAssessment | loop :684-715, `Add(session)` :714 (per worker `wc`) | REJECT (ModelState.AddModelError + return View(model)) | `AnyAsync(s => s.UserId==wc.UserId && s.Title==model.Title && s.CompletedAt==model.CompletedAt && s.IsManualEntry)` |
| ImportTraining (assessment branch) | `Add` :1255 (per row Excel) | SKIP (result.Status="Skip", message "duplikat — dilewati", continue) | `AnyAsync(s => s.UserId==targetUser.Id && s.Title==judul && s.CompletedAt==parsedDate && s.IsManualEntry)` |
| BulkBackfillAssessment | loop :821-848, `Add` :845 (in-tx, BeginTransactionAsync :817) | SKIP-with-report (kolom status, jangan increment success) | `AnyAsync(s => s.UserId==user.Id && s.Title==title && s.CompletedAt==completedAt && s.IsManualEntry)` |

AddManualAssessment insert (VERIFIED :690-714): session baru pakai `UserId=wc.UserId, Title=model.Title, CompletedAt=model.CompletedAt, IsManualEntry=true`.
ImportTrainingResult (Models/ImportTrainingResult.cs): { NIP, Judul, Status ("Success"|"Error"), Message } — tambah dukungan "Skip" (komentar + konvensi; struktur sudah bisa muat string "Skip").

CATATAN PERFORMA: untuk import/backfill batch, pre-load existing keys (HashSet UserId+Title+CompletedAt) sekali daripada AnyAsync per-row (N query). Untuk BulkBackfill DALAM tx, juga track in-memory set dup intra-batch (2 row identik dalam file yang sama → row kedua skip).

D-02 EXACT vs #15 ±1 hari: guard create pakai `CompletedAt == ` (EXACT), BUKAN AddDays(±1). ±1 hari HANYA mirror preview (plan 01). JANGAN tertukar (Pitfall 7).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard EXACT AddManualAssessment (reject) + dukungan Skip ImportTrainingResult</name>
  <read_first>
    - Controllers/TrainingAdminController.cs:653-725 (AddManualAssessment POST: loop :684-715, Add :714)
    - Models/ImportTrainingResult.cs (struktur Status/Message)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Guard Duplikat 3 Pintu" (predikat + perilaku per pintu)
  </read_first>
  <behavior>
    - Test: AddManualAssessment dengan (user X, judul "Y", tanggal D) sementara session existing (X,"Y",D,IsManualEntry) ada → REJECT (ModelState invalid, tidak Add).
    - Test: tanggal berbeda (D+1) → LOLOS (tidak reject, Add berjalan) — re-entry sah (EXACT, bukan ±1 hari).
    - Test: judul berbeda → LOLOS.
    - Test: user berbeda → LOLOS.
  </behavior>
  <action>
Di `AddManualAssessment` loop per worker (`wc`), SEBELUM `_context.AssessmentSessions.Add(session)` (:714), tambah cek EXACT:
```csharp
bool dup = await _context.AssessmentSessions.AnyAsync(s =>
    s.UserId == wc.UserId && s.Title == model.Title &&
    s.CompletedAt == model.CompletedAt && s.IsManualEntry);
if (dup)
{
    ModelState.AddModelError("", $"Duplikat: assessment '{model.Title}' untuk pekerja ini pada tanggal {model.CompletedAt:dd MMM yyyy} sudah ada.");
    await PopulateWorkersViewBag();   // sesuai pola return View existing
    return View(model);
}
```
(Jika model multi-worker, putuskan: reject seluruh submit pada dup pertama ATAU reject hanya worker dup — pilih reject submit dengan pesan jelas, konsisten "single = reject". Dokumentasikan di SUMMARY.)

Update `Models/ImportTrainingResult.cs`: tambah komentar Status mendukung "Skip" (string sudah muat; tambahkan konstanta/komentar `// "Success" | "Error" | "Skip"`). Bila perlu kejelasan, tambah konstanta string.

Buat `HcPortal.Tests/DuplicateGuardTests.cs` — 4 [Fact] sesuai <behavior> AddManual. Pakai integration kecil real-SQL atau in-memory yang seed existing session + panggil logic guard.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~DuplicateGuardTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "s.CompletedAt == model.CompletedAt"` di AddManualAssessment match (EXACT, bukan AddDays)
    - `grep "AddDays"` TIDAK muncul di blok guard AddManualAssessment (bukan ±1 hari)
    - `grep "ModelState.AddModelError"` di guard dup match + `return View(model)`
    - `ImportTrainingResult.cs` mendukung "Skip" (komentar/konstanta)
    - `DuplicateGuardTests.cs` ≥4 [Fact]; filter test exit 0
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>AddManualAssessment reject dup EXACT; re-entry tanggal beda lolos; ImportTrainingResult dukung Skip; teruji 4 kasus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard EXACT skip-with-report ImportTraining + BulkBackfillAssessment</name>
  <read_first>
    - Controllers/TrainingAdminController.cs:1152-1267 (ImportTraining: assessment-branch insert :1255, result list)
    - Controllers/TrainingAdminController.cs:748-878 (BulkBackfillAssessment: tx :817, loop :821-848, Add :845, success counter)
    - Models/ImportTrainingResult.cs (Status "Skip" dari Task 1)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Guard Duplikat 3 Pintu" (skip-with-report + performa pre-load HashSet)
  </read_first>
  <behavior>
    - Test: ImportTraining baris dup exact (user+judul+tanggal existing) → result.Status=="Skip", message berisi "duplikat", row TIDAK di-Add.
    - Test: ImportTraining baris non-dup → Status=="Success", di-Add.
    - Test: BulkBackfill baris dup exact → skip, success counter TIDAK bertambah untuk baris itu.
    - Test: BulkBackfill 2 baris identik dalam 1 file → baris kedua skip (intra-batch dedup in-memory).
    - Test: tanggal beda → lolos (EXACT).
  </behavior>
  <action>
**ImportTraining (assessment branch, sebelum Add :1255):** tambah cek EXACT `AnyAsync(s => s.UserId==targetUser.Id && s.Title==judul && s.CompletedAt==parsedDate && s.IsManualEntry)`. Jika dup → `result.Status="Skip"; result.Message="duplikat — dilewati"; results.Add(result); continue;` (jangan Add). (Training branch :1268+ analog: cek `TrainingRecords` by UserId+Judul+Tanggal jika spec menuntut — fokus #12 assessment branch; training-branch sesuaikan bila relevan, dokumentasikan.)

**BulkBackfillAssessment (in-tx, sebelum Add :845):** tambah cek EXACT (sama predikat). Jika dup (DB existing ATAU intra-batch set) → catat kolom status "duplikat — dilewati", JANGAN increment `success`, `continue`. Track `HashSet<(string,string,DateTime)> seenInBatch` untuk dup intra-file. Karena DALAM tx, query DB AnyAsync tetap valid (uncommitted rows belum tampak di AnyAsync untuk row sebelumnya yang sudah Add tapi belum Save — gunakan seenInBatch untuk itu).

Performa: pre-load existing keys `var existingKeys = (await _context.AssessmentSessions.Where(s => s.IsManualEntry && relevantUserIds.Contains(s.UserId)).Select(s => new { s.UserId, s.Title, s.CompletedAt }).ToListAsync()).ToHashSet(...)` sekali, cek terhadap HashSet daripada AnyAsync per-row.

Extend `DuplicateGuardTests.cs` — 5 [Fact] sesuai <behavior> (ImportTraining + BulkBackfill). `[Trait("Category","Integration")]` bila real-SQL.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~DuplicateGuardTests"; dotnet test HcPortal.Tests</automated>
  </verify>
  <acceptance_criteria>
    - ImportTraining: `grep "Status = \"Skip\""` (atau ekuivalen) + message "duplikat" match; dup → continue tanpa Add
    - BulkBackfillAssessment: guard dup tidak increment success; `grep "seenInBatch"` (atau set intra-batch) match untuk dedup intra-file
    - Predikat EXACT `CompletedAt ==` di kedua pintu (no AddDays di guard)
    - `DuplicateGuardTests.cs` total ≥9 [Fact] (4 AddManual + 5 import/backfill); filter test exit 0
    - `dotnet test` full suite hijau
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>ImportTraining + BulkBackfill skip-with-report dup EXACT (DB + intra-batch); success tak ke-inflate; teruji.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC → AddManualAssessment/ImportTraining/BulkBackfill (POST) | Input form + Excel batch; data integrity risk (duplikat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-26 | Tampering (data integrity) | Insert duplikat akumulasi data kotor | mitigate | Guard EXACT user+judul+tanggal sebelum Add (reject/skip); test 9 kasus |
| T-367-27 | DoS (false-positive) | Guard terlalu longgar blok re-entry sah | mitigate | EXACT match (CompletedAt ==), bukan ±1 hari; test re-entry tanggal beda lolos (Pitfall 7) |
| T-367-28 | Spoofing/CSRF | POST tanpa token | accept | Endpoint existing sudah ber-antiforgery/authz (tidak diubah plan ini); guard tidak melonggarkan |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` full suite hijau.
- Guard EXACT 3 pintu teruji (reject/skip + re-entry tanggal beda lolos + intra-batch dedup).
</verification>

<success_criteria>
- 3 pintu guard duplikat EXACT (#12/#14, D-02) terimplementasi & teruji; no false-positive; Migration = FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-07-SUMMARY.md`
</output>
