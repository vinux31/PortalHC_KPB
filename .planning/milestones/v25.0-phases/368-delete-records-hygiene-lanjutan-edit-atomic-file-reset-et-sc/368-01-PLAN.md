---
phase: 368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/TrainingAdminController.cs
  - HcPortal.Tests/EditAtomicFileTests.cs
  - HcPortal.Tests/RenewalValidationTests.cs
autonomous: true
requirements: ["#21", "#26"]
must_haves:
  truths:
    - "Edit-replace sertifikat: upload baru sukses → file lama terhapus on-disk; upload gagal → file lama utuh; edit metadata-only (tanpa upload) → file lama TIDAK terhapus"
    - "EditTraining (SertifikatUrl) + EditManualAssessment (ManualSertifikatUrl) keduanya atomik: save baru → SaveChanges → File.Delete lama POST-commit warn-only"
    - "EditTraining renewal: Renews*Id invalid (tak exist / beda user) DAN field berubah → tolak + flash error; field tidak berubah → tidak divalidasi (record legacy ber-Renews invalid tetap bisa edit field lain)"
    - "Zero schema change (migration=false)"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "EditTraining + EditManualAssessment atomic file replace + EditTraining renewal validation"
      contains: "FileUploadHelper.DeleteFile"
    - path: "HcPortal.Tests/EditAtomicFileTests.cs"
      provides: "[Fact] replace-file atomic (pola Phase 355)"
      contains: "DeletesOldFileOnDisk"
    - path: "HcPortal.Tests/RenewalValidationTests.cs"
      provides: "integration real-SQL renewal validation (#26)"
      contains: "RecordCascadeFixture"
  key_links:
    - from: "Controllers/TrainingAdminController.cs EditTraining"
      to: "FileUploadHelper.DeleteFile (post-commit)"
      via: "File.Delete setelah SaveChangesAsync"
      pattern: "FileUploadHelper\\.DeleteFile"
    - from: "Controllers/TrainingAdminController.cs EditTraining"
      to: "ModelState.AddModelError → TempData firstError + redirect"
      via: "same-user check src.UserId != record.UserId"
      pattern: "src\\.UserId != record\\.UserId"
---

<objective>
Pasang **edit atomic file replace** (#21) di `EditTraining` (`SertifikatUrl`) + `EditManualAssessment` (`ManualSertifikatUrl`), dan **validasi renewal** (#26) di `EditTraining` — keduanya di method POST yang sama di `TrainingAdminController.cs`.

Purpose: (#21) Cegah kehilangan data — bug saat ini menghapus file lama SEBELUM `SaveFileAsync`, jadi upload gagal = sertifikat hilang permanen. (#26) Cegah link renewal lintas-user palsu (IDOR root-cause) tanpa merusak edit data legacy.
Output: 2 method `TrainingAdminController.cs` di-hardened + 2 test file baru.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-CONTEXT.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-RESEARCH.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md

<interfaces>
<!-- Kontrak yang dibutuhkan executor — diekstrak dari source. JANGAN eksplorasi codebase. -->

FileUploadHelper [VERIFIED: Helpers/FileUploadHelper.cs]:
```csharp
static Task<string?> SaveFileAsync(IFormFile?, string webRootPath, string subFolder, ILogger? = null); // → URL relatif "/uploads/certificates/..." atau null
static void DeleteFile(string webRootPath, string? relativeUrl);   // null-safe, cek File.Exists; confined webroot
static (bool IsValid, string? Error) ValidateCertificateFile(IFormFile?); // ext+size+magic-byte
```

EditTraining POST struktur AKTUAL [VERIFIED: TrainingAdminController.cs:471-553]:
- L475-480 ValidateCertificateFile → TempData["Error"] + RedirectToAction("ManageAssessment","AssessmentAdmin",{tab="training"}) bila invalid
- L483-500 DAG validation existing (pakai model.* saja, SEBELUM FindAsync) → ModelState.AddModelError
- L502-510 `if (!ModelState.IsValid)` → TempData firstError + RedirectToAction (BLOK YANG DI-MIRROR untuk #26)
- L512 `var record = await _context.TrainingRecords.FindAsync(model.Id);` L513 `if (record == null) return NotFound();`
- L516-526 file upload block (NON-atomik — yang DIPERBAIKI #21)
- L528-542 assign metadata BARU ke record (termasuk L541-542 `record.RenewsTrainingId/RenewsSessionId = model.*`)
- L544 `await _context.SaveChangesAsync();`
- L546-552 audit + TempData success + redirect

EditManualAssessment POST struktur AKTUAL [VERIFIED: TrainingAdminController.cs:960-1013]:
- L962-980 ValidateCertificateFile
- L982-987 file upload block: `FileUploadHelper.DeleteFile(_env.WebRootPath, session.ManualSertifikatUrl)` PRE-save (WRONG) → SaveFileAsync → set ManualSertifikatUrl
- SaveChangesAsync ~L1004 (setelah assign metadata)

ApplicationUser FK: AssessmentSession.UserId + TrainingRecord.UserId = string (Identity Id).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [TEST-FIRST] EditAtomicFileTests.cs — [Fact] replace-file atomik (#21)</name>
  <files>HcPortal.Tests/EditAtomicFileTests.cs</files>
  <read_first>
    - HcPortal.Tests/PackageImageDeleteTests.cs:209-238 (analog `Replace_NewFileWins_DeletesOldFileOnDisk`, Phase 355 — temp-dir + File.WriteAllBytes + try/finally Directory.Delete)
    - Helpers/FileUploadHelper.cs (SaveFileAsync/DeleteFile signature)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (section "Test files — analogs A")
  </read_first>
  <action>
    Buat `HcPortal.Tests/EditAtomicFileTests.cs`. Test logika replace atomik via temp-dir + FileUploadHelper langsung (BUKAN controller — pola Phase 355 murni, no DB). Namespace `HcPortal.Tests`, class `EditAtomicFileTests`. Helper privat `MakeTempDir()` → `Path.Combine(Path.GetTempPath(), "hcp_edit_" + Guid.NewGuid().ToString("N"))` + Directory.CreateDirectory.

    Tiga [Fact] (semua nama mengandung substring "EditAtomicFile" agar `--filter "EditAtomicFile"` jalan; gunakan nama method `EditAtomicFile_NewUpload_DeletesOldFileOnDisk`, `EditAtomicFile_MetadataOnly_KeepsOldFile`, `EditAtomicFile_UploadFails_KeepsOldFile`):
    1. **New upload menang**: tulis old.jpg ke `wwwroot/uploads/certificates/` di temp-webroot, simulasikan url lama `/uploads/certificates/old.jpg`. Panggil `FileUploadHelper.SaveFileAsync(formFile_baru, tempRoot, "uploads/certificates")` (gunakan `FormFile` in-memory dengan magic-byte JPEG valid `0xFF 0xD8 0xFF`), assert return non-null → lalu panggil `FileUploadHelper.DeleteFile(tempRoot, "/uploads/certificates/old.jpg")` → `Assert.False(File.Exists(oldPath))` + `Assert.True(File.Exists(newPath))`.
    2. **Metadata-only (tanpa upload)**: TIDAK panggil SaveFileAsync/DeleteFile (simulasikan guard `CertificateFile == null`) → `Assert.True(File.Exists(oldPath))` (file lama utuh).
    3. **Upload gagal (uploadedUrl == null)**: simulasikan `SaveFileAsync` return null (FormFile kosong / invalid) → guard "DeleteFile hanya jika uploadedUrl != null" → JANGAN panggil DeleteFile → `Assert.True(File.Exists(oldPath))`.
    Semua dalam `try { ... } finally { Directory.Delete(dir, recursive: true); }`.

    Buat FormFile in-memory helper: `static IFormFile MakeJpeg(string name)` → `MemoryStream` berisi `new byte[]{0xFF,0xD8,0xFF, ...}` (≥ beberapa byte agar lolos magic-byte ValidateCertificateFile), return `new FormFile(stream, 0, stream.Length, "file", name){ Headers = new HeaderDictionary(), ContentType="image/jpeg" }`.

    Test ini WAVE-0: menetapkan kontrak SEBELUM Task 2 mengubah controller. Commit terpisah dengan `git add HcPortal.Tests/EditAtomicFileTests.cs` eksplisit (untracked-file lesson).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "EditAtomicFile"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/EditAtomicFileTests.cs` ada; `grep "DeletesOldFileOnDisk"` ≥1; `grep "Directory.Delete(dir, recursive: true)"` ≥1 (cleanup).
    - 3 [Fact] mengandung substring "EditAtomicFile" di nama; `dotnet test HcPortal.Tests --filter "EditAtomicFile"` = 3 passed.
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>3 [Fact] hijau membuktikan: new-upload hapus lama, metadata-only pertahankan lama, upload-gagal pertahankan lama.</done>
</task>

<task type="auto">
  <name>Task 2: EditTraining + EditManualAssessment atomic file replace (#21) + EditTraining renewal validation (#26)</name>
  <files>Controllers/TrainingAdminController.cs</files>
  <read_first>
    - Controllers/TrainingAdminController.cs:471-553 (EditTraining POST — file block L516-526, FindAsync L512, ModelState block L502-510, SaveChanges L544)
    - Controllers/TrainingAdminController.cs:960-1013 (EditManualAssessment POST — file block L982-987)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (sections "EditTraining #21", "EditTraining #26", "EditManualAssessment #21")
    - HcPortal.Tests/EditAtomicFileTests.cs (kontrak dari Task 1)
  </read_first>
  <action>
    **A. #21 EditTraining atomic (ganti L516-526 NON-atomik):**
    Ubah block file upload jadi pola Phase 331 (capture-before → save → commit → delete post-commit). Hapus PRE-save delete (L518-522 `File.Delete oldPath`). Pola baru:
    ```csharp
    string? oldSertifikatUrl = null;
    if (model.CertificateFile != null && model.CertificateFile.Length > 0)
    {
        oldSertifikatUrl = record.SertifikatUrl;  // capture LAMA sebelum overwrite
        var uploadedUrl = await FileUploadHelper.SaveFileAsync(model.CertificateFile, _env.WebRootPath, "uploads/certificates");
        if (uploadedUrl != null) record.SertifikatUrl = uploadedUrl;
        else oldSertifikatUrl = null; // upload gagal → JANGAN hapus lama (file lama utuh)
    }
    ```
    Lalu SETELAH `await _context.SaveChangesAsync();` (L544), tambah delete post-commit warn-only:
    ```csharp
    // #21 D-06: hapus file LAMA POST-commit, hanya jika upload baru sukses (oldSertifikatUrl != null)
    if (!string.IsNullOrEmpty(oldSertifikatUrl) && oldSertifikatUrl != record.SertifikatUrl)
    {
        try { FileUploadHelper.DeleteFile(_env.WebRootPath, oldSertifikatUrl); }
        catch (Exception ex) { _logger.LogWarning(ex, "Gagal hapus file sertifikat lama post-commit (EditTraining): {Url}", oldSertifikatUrl); }
    }
    ```
    (Cek nama logger field aktual di file — bila `_logger` tidak ada di scope, pakai logger yang dipakai method lain di file ini; lihat `read_first`.)

    **B. #26 EditTraining renewal validation** — sisipkan SETELAH L513 `if (record == null) return NotFound();`, SEBELUM block file (L516). Validasi HANYA saat field berubah (D-04), block dengan ModelState→TempData→redirect (D-05, mirror L502-510):
    ```csharp
    // #26 D-04/D-05: validasi Renews*Id (exist + same-user) HANYA saat field renewal BERUBAH (toleran legacy)
    if (model.RenewsTrainingId != record.RenewsTrainingId && model.RenewsTrainingId.HasValue)
    {
        var srcTr = await _context.TrainingRecords.FindAsync(model.RenewsTrainingId.Value);
        if (srcTr == null || srcTr.UserId != record.UserId)
            ModelState.AddModelError("", "Sertifikat renewal tidak ditemukan atau bukan milik peserta ini.");
    }
    if (model.RenewsSessionId != record.RenewsSessionId && model.RenewsSessionId.HasValue)
    {
        var srcAs = await _context.AssessmentSessions.FindAsync(model.RenewsSessionId.Value);
        if (srcAs == null || srcAs.UserId != record.UserId)
            ModelState.AddModelError("", "Sesi renewal tidak ditemukan atau bukan milik peserta ini.");
    }
    if (!ModelState.IsValid)
    {
        var firstError = ModelState.Values.SelectMany(v => v.Errors).Select(e => e.ErrorMessage).FirstOrDefault() ?? "Data tidak valid.";
        TempData["Error"] = firstError;
        return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    }
    ```
    PENTING: penempatan SETELAH FindAsync (butuh `record.UserId` + `record.Renews*Id` LAMA) tapi SEBELUM assign L541-542 yang menimpa nilai lama. JANGAN ubah/pindah DAG validation existing L483-494.

    **C. #21 EditManualAssessment atomic (ganti L982-987):**
    Ubah `FileUploadHelper.DeleteFile(...)` PRE-save jadi capture-before → save → set url → (SaveChanges existing ~L1004) → DeleteFile POST-commit warn-only, hanya jika `uploadedUrl != null`. Field = `session.ManualSertifikatUrl` (BUKAN SertifikatUrl):
    ```csharp
    string? oldManualUrl = null;
    if (model.CertificateFile != null && model.CertificateFile.Length > 0)
    {
        oldManualUrl = session.ManualSertifikatUrl;
        var uploadedUrl = await FileUploadHelper.SaveFileAsync(model.CertificateFile, _env.WebRootPath, "uploads/certificates");
        if (uploadedUrl != null) session.ManualSertifikatUrl = uploadedUrl;
        else oldManualUrl = null;
    }
    ```
    Setelah SaveChangesAsync method ini: `if (!string.IsNullOrEmpty(oldManualUrl) && oldManualUrl != session.ManualSertifikatUrl) { try { FileUploadHelper.DeleteFile(_env.WebRootPath, oldManualUrl); } catch (Exception ex) { _logger.LogWarning(...); } }`.

    Migration=false — JANGAN buat migration. JANGAN sentuh method lain.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FileUploadHelper.DeleteFile" Controllers/TrainingAdminController.cs` muncul di KEDUA EditTraining + EditManualAssessment di POSISI setelah SaveChangesAsync (post-commit).
    - `grep "System.IO.File.Delete(oldPath)" Controllers/TrainingAdminController.cs` = 0 match di EditTraining (PRE-save delete dihapus).
    - `grep "src.UserId != record.UserId" Controllers/TrainingAdminController.cs` ≥1 (same-user check, dengan nama var `srcTr`/`srcAs` cek varian) — minimal `grep "UserId != record.UserId"` ≥2.
    - `grep "model.RenewsTrainingId != record.RenewsTrainingId" Controllers/TrainingAdminController.cs` ≥1 (validasi only-when-changed).
    - `dotnet build` 0 error; quick test suite hijau (no regression).
    - Tidak ada file `Migrations/*368*` baru.
  </acceptance_criteria>
  <done>EditTraining + EditManualAssessment atomik (hapus lama post-commit conditional); EditTraining tolak Renews*Id invalid saat berubah; legacy edit field-lain lolos.</done>
</task>

<task type="auto">
  <name>Task 3: [TEST] RenewalValidationTests.cs — integration real-SQL #26</name>
  <files>HcPortal.Tests/RenewalValidationTests.cs</files>
  <read_first>
    - HcPortal.Tests/RecordCascadeIntegrationTests.cs:21-100 (RecordCascadeFixture + FakeWebHostEnvironment + SeedUserAsync + NewSession + NewTraining — REUSE pola; semua private static, salin/adaptasi)
    - .planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-02-SUMMARY.md (fixture disposable real-SQL)
    - Controllers/TrainingAdminController.cs:471-553 (EditTraining hasil Task 2)
  </read_first>
  <action>
    Buat `HcPortal.Tests/RenewalValidationTests.cs`. `[Trait("Category","Integration")]`, `IClassFixture<RecordCascadeFixture>` (reuse fixture 367 yang sudah ada — class `RecordCascadeFixture` public di RecordCascadeIntegrationTests.cs). Salin helper `NewCtx()`, `SeedUserAsync(ctx)` (atau panggil ulang pola yang sama — buat versi lokal karena helper 367 private static).

    Karena #26 validasi ada di controller (butuh HttpContext/User/TempData), test paling bersih = **uji logika validasi langsung pada data** via [Fact] yang merefleksikan kontrak (bukan invoke controller penuh). Buat 3 [Fact] (nama mengandung "RenewalValidation"):
    1. **RenewalValidation_DifferentUser_IsInvalid**: seed user A + user B; seed TrainingRecord src milik B. Buat record target milik A. Assert: `src.UserId != record.UserId` == true (kondisi block) → membuktikan same-user check menolak cross-user.
    2. **RenewalValidation_NonExistent_IsInvalid**: `FindAsync(999999)` → null → kondisi block (`src == null`) true.
    3. **RenewalValidation_SameUserExisting_IsValid**: src + record sama-user → `src != null && src.UserId == record.UserId` → tidak block.
    Bila feasible & murah, tambahkan [Fact] integration controller-level memanggil `EditTraining(model)` dengan `Renews*Id` cross-user dan assert TempData["Error"] berisi pesan + record TIDAK berubah di DB; bila controller butuh terlalu banyak mock (HubContext/UserManager/SignalR) → SKIP controller-level dan andalkan 3 [Fact] data-level di atas + acceptance grep Task 2 (catat di summary alasan, pola A4 RESEARCH fallback). JANGAN paksa mock berat.

    Commit terpisah `git add HcPortal.Tests/RenewalValidationTests.cs` eksplisit.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "RenewalValidation"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/RenewalValidationTests.cs` ada; `grep "RecordCascadeFixture"` ≥1 (reuse fixture); `grep "[Trait(\"Category\", \"Integration\")]"` ≥1.
    - ≥3 [Fact] mengandung "RenewalValidation"; `dotnet test HcPortal.Tests --filter "RenewalValidation"` semua passed (butuh localhost\SQLEXPRESS aktif).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Test real-SQL membuktikan same-user + exist check #26: cross-user invalid, non-existent invalid, same-user valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → EditTraining/EditManualAssessment POST | Admin/HC mengirim metadata + Renews*Id + file upload (untrusted) |
| Controller → wwwroot file system | File.Delete/SaveFile pada disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-368-01 | Tampering/Info Disclosure | #26 EditTraining `Renews*Id` (IDOR — link sertifikat user lain untuk palsukan "IsRenewed"/sembunyikan expired) | mitigate | Same-user check `srcTr.UserId == record.UserId` / `srcAs.UserId == record.UserId` → ModelState block + TempData error (D-05). Ini ROOT-CAUSE #26. |
| T-368-02 | Tampering | #21 File.Delete path traversal dari filename user | mitigate | `FileUploadHelper.SaveFileAsync` strip directory + magic-byte (Phase 325 existing); `DeleteFile` null-safe + cek File.Exists; confined webroot (V12). Path lama = `record.SertifikatUrl` server-stored, bukan input langsung. |
| T-368-03 | Denial of Service | #21 upload gagal → File.Delete lama → data loss | mitigate | Delete-old STRICTLY conditional `uploadedUrl != null` (oldUrl di-null-kan saat gagal) + POST-commit only. Upload gagal → file lama utuh. |
| T-368-04 | Info Disclosure | Exception message bocor ke user (#26 / #21 catch) | mitigate | Pesan ModelState GENERIK (no ex.Message, V7 pola 367); File.Delete warn-only di `_logger.LogWarning` (server-side, bukan ke user). |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "EditAtomicFile"` = 3 passed; `--filter "RenewalValidation"` passed (real-SQL).
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` no regression (baseline post-367 ≥209).
- grep acceptance per task.
- Migration=false: tidak ada `Migrations/*` baru.
</verification>

<success_criteria>
- #21: EditTraining + EditManualAssessment atomik (save→commit→delete-old post-commit warn-only, conditional on upload sukses).
- #26: EditTraining tolak Renews*Id invalid (tak exist/beda user) saat field berubah; legacy edit field-lain lolos.
- 2 test file baru hijau; no regression; migration=false.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-01-SUMMARY.md`.
</output>
