---
phase: 368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc
plan: 04
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - Models/CertificationManagementViewModel.cs
  - Controllers/CMPController.cs
  - Controllers/CDPController.cs
  - HcPortal.Tests/CertDedupTests.cs
autonomous: false
requirements: ["#25", "#23", "#27"]
must_haves:
  truths:
    - "Helper static shared BuildParentNameLookup di SertifikatRow (Models/CertificationManagementViewModel.cs) — single-source GroupBy dedup, dikonsumsi CMP + CDP (anti-drift)"
    - "CMPController + CDPController CertificationManagement: callsite ToDictionary(c=>c.Name) (throw pada duplicate child Name) DIGANTI panggilan helper shared → tidak 500 saat ada sub-kategori ber-Name sama lintas parent"
    - "Lookup benar: child Name → parent Name (sama hasil dengan AdminBase GroupBy L140-143)"
    - "UAT human-verify: #23 preview→execute→idempotent di browser (Seed Workflow); #27 label 'Bulk Import Nilai (Excel)' tampil; #25 CertificationManagement tidak 500"
    - "Zero schema change (migration=false)"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "SertifikatRow.BuildParentNameLookup static helper (#25)"
      contains: "BuildParentNameLookup"
    - path: "Controllers/CMPController.cs"
      provides: "callsite konsumsi helper shared (#25)"
      contains: "BuildParentNameLookup"
    - path: "Controllers/CDPController.cs"
      provides: "callsite konsumsi helper shared (#25)"
      contains: "BuildParentNameLookup"
    - path: "HcPortal.Tests/CertDedupTests.cs"
      provides: "[Fact] static helper duplicate-Name tidak throw (#25)"
      contains: "BuildParentNameLookup"
  key_links:
    - from: "Controllers/CMPController.cs CertificationManagement"
      to: "SertifikatRow.BuildParentNameLookup"
      via: "panggilan helper shared (ganti ToDictionary(c=>c.Name))"
      pattern: "BuildParentNameLookup"
    - from: "Controllers/CDPController.cs CertificationManagement"
      to: "SertifikatRow.BuildParentNameLookup"
      via: "panggilan helper shared (ganti ToDictionary(c=>c.Name))"
      pattern: "BuildParentNameLookup"
---

<objective>
Ekstrak GroupBy dedup ke helper static shared `SertifikatRow.BuildParentNameLookup` (#25, D-03) dan ganti callsite broken `ToDictionary(c=>c.Name)` di CMPController + CDPController. Lalu UAT human-verify keseluruhan fase 368 (#23 cleanup browser, #27 label, #25 no-500).

Purpose: (#25) `ToDictionary(c=>c.Name)` melempar `ArgumentException` (500) bila ada 2 sub-kategori ber-Name sama lintas parent berbeda. GroupBy dedup memperbaiki + helper single-source mencegah drift CMP/CDP (spirit anti-drift 367). Home NETRAL = `SertifikatRow` (Models, sudah shared CMP/CDP via `DeriveCertificateStatus`) — BUKAN AdminBaseController (CMP/CDP plain Controller, tak inherit AdminBase).
Output: 1 helper baru + 2 callsite di-rewire + 1 test file + UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-CONTEXT.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-RESEARCH.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md

<interfaces>
<!-- Kontrak diekstrak dari source. JANGAN eksplorasi codebase. -->

SertifikatRow [VERIFIED: Models/CertificationManagementViewModel.cs:25-66] — home NETRAL, sudah punya `public static CertificateStatus DeriveCertificateStatus(...)` (dikonsumsi CMP+CDP). Tambah helper static baru di sini.

CMP callsite broken [VERIFIED: CMPController.cs:4152-4159]:
```csharp
var allCategories = await _context.AssessmentCategories.Where(c => c.IsActive)
    .Select(c => new { c.Id, c.Name, c.ParentId }).ToListAsync();
var categoryById = allCategories.ToDictionary(c => c.Id);
var categoryNameLookup = allCategories
    .Where(c => c.ParentId != null && categoryById.ContainsKey(c.ParentId.Value))
    .ToDictionary(c => c.Name, c => categoryById[c.ParentId!.Value].Name);   // ← throws on dup key
```
CDP callsite broken [VERIFIED: CDPController.cs:4001-4009] — IDENTIK.

AdminBase correct GroupBy [VERIFIED: AdminBaseController.cs:140-143]:
```csharp
var categoryNameLookup = allCategories
    .Where(c => c.ParentId != null && categoryById.ContainsKey(c.ParentId.Value))
    .GroupBy(c => c.Name)
    .ToDictionary(g => g.Key, g => categoryById[g.First().ParentId!.Value].Name);
```
CATATAN tipe: `allCategories` = anon-type `{ Id, Name, ParentId }` (tak bisa lintas-method). Helper harus terima tipe netral — gunakan tuple `IEnumerable<(int Id, string Name, int? ParentId)>`; callsite project anon → tuple sebelum memanggil.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [TEST-FIRST] CertDedupTests.cs + SertifikatRow.BuildParentNameLookup helper (#25)</name>
  <files>Models/CertificationManagementViewModel.cs, HcPortal.Tests/CertDedupTests.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs:25-66 (SertifikatRow + DeriveCertificateStatus shape static helper)
    - Controllers/AdminBaseController.cs:140-143 (GroupBy dedup yang BENAR — logika yang diekstrak)
    - HcPortal.Tests/PackageImageDeleteTests.cs (pola [Fact] unit no-DB)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (section "NEW static BuildParentNameLookup #25")
  </read_first>
  <action>
    **A. Tambah helper static** di `SertifikatRow` (`Models/CertificationManagementViewModel.cs`, dekat `DeriveCertificateStatus`):
    ```csharp
    /// <summary>
    /// #25: child-category Name → parent Name lookup, GroupBy-dedup (cegah ArgumentException pada
    /// duplicate child Name lintas parent). Single-source dikonsumsi CMP + CDP (anti-drift, D-03).
    /// </summary>
    public static Dictionary<string, string> BuildParentNameLookup(IEnumerable<(int Id, string Name, int? ParentId)> categories)
    {
        var list = categories as IList<(int Id, string Name, int? ParentId)> ?? categories.ToList();
        var byId = list.ToDictionary(c => c.Id);
        return list
            .Where(c => c.ParentId != null && byId.ContainsKey(c.ParentId.Value))
            .GroupBy(c => c.Name)
            .ToDictionary(g => g.Key, g => byId[g.First().ParentId!.Value].Name);
    }
    ```
    Pastikan `using System.Collections.Generic;` + `using System.Linq;` ada di file.

    **B. Test `HcPortal.Tests/CertDedupTests.cs`** (unit, no DB). 2-3 [Fact] (nama mengandung "ParentNameLookup"):
    1. **ParentNameLookup_DuplicateChildName_DoesNotThrow**: input dengan 2 child ber-Name sama ("Sub-X") lintas parent berbeda (parentA Id=1 Name="A", parentB Id=2 Name="B", childX1 Id=3 Name="Sub-X" ParentId=1, childX2 Id=4 Name="Sub-X" ParentId=2) → panggil helper → TIDAK throw; assert `result["Sub-X"]` ∈ {"A","B"} (GroupBy ambil First — deterministik untuk dedup, nilai parent salah satunya OK karena duplicate Name memang ambigu — yang penting tidak 500).
    2. **ParentNameLookup_NormalCase_MapsChildToParent**: child unik → assert `result["Sub-Y"] == "A"`.
    3. (opsional) **ParentNameLookup_RootOnly_Empty**: hanya kategori root (ParentId=null) → assert kosong.

    Test ini WAVE-0: tetapkan kontrak helper SEBELUM Task 2 me-rewire callsite. Commit terpisah `git add HcPortal.Tests/CertDedupTests.cs` eksplisit.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "ParentNameLookup"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public static Dictionary<string, string> BuildParentNameLookup" Models/CertificationManagementViewModel.cs` ≥1.
    - `grep "GroupBy(c => c.Name)" Models/CertificationManagementViewModel.cs` ≥1 (dedup logic).
    - File `HcPortal.Tests/CertDedupTests.cs` ada; ≥2 [Fact] mengandung "ParentNameLookup"; `grep "DoesNotThrow"` ≥1.
    - `dotnet test HcPortal.Tests --filter "ParentNameLookup"` semua passed; `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Helper static GroupBy-dedup ada di SertifikatRow; test membuktikan duplicate child Name tidak throw + lookup benar.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire CMP + CDP CertificationManagement callsites ke helper shared (#25)</name>
  <files>Controllers/CMPController.cs, Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs:4152-4159 (callsite broken — ganti ToDictionary(c=>c.Name))
    - Controllers/CDPController.cs:4001-4009 (callsite broken — IDENTIK)
    - Models/CertificationManagementViewModel.cs (helper BuildParentNameLookup dari Task 1)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (section "#25 callsites")
  </read_first>
  <action>
    Di KEDUA controller, ganti baris `categoryNameLookup = allCategories.Where(...).ToDictionary(c => c.Name, ...)` dengan panggilan helper shared. Project anon-type ke tuple sebelum memanggil:
    ```csharp
    var categoryById = allCategories.ToDictionary(c => c.Id);
    var categoryNameLookup = SertifikatRow.BuildParentNameLookup(
        allCategories.Select(c => (c.Id, c.Name, c.ParentId)));
    ```
    (`categoryById` mungkin masih dipakai di tempat lain dalam method — JANGAN hapus bila ada konsumen lain; cukup ganti baris `categoryNameLookup`.)
    - CMPController.cs: ganti L4157-4159 (block ToDictionary(c=>c.Name)).
    - CDPController.cs: ganti L4007-4009 (block IDENTIK).
    Pastikan namespace `HcPortal.Models` ter-import (SertifikatRow) — cek using di kepala file; tambah `using HcPortal.Models;` bila belum.
    Anti-drift (D-03): KEDUA callsite WAJIB panggil helper yang SAMA — JANGAN GroupBy-inline ulang di salah satu. Migration=false.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "ParentNameLookup"; dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SertifikatRow.BuildParentNameLookup" Controllers/CMPController.cs` ≥1 + `grep -c "SertifikatRow.BuildParentNameLookup" Controllers/CDPController.cs` ≥1 (KEDUA konsumsi shared).
    - `grep "ToDictionary(c => c.Name" Controllers/CMPController.cs` = 0 match + `grep "ToDictionary(c => c.Name" Controllers/CDPController.cs` = 0 match (callsite broken hilang).
    - `grep "GroupBy(c => c.Name)" Controllers/CMPController.cs Controllers/CDPController.cs` = 0 match (TIDAK inline ulang — hanya helper).
    - `dotnet build` 0 error; quick suite no regression.
    - Tidak ada `Migrations/*` baru.
  </acceptance_criteria>
  <done>CMP + CDP konsumsi helper shared yang sama; callsite broken hilang; tidak ada inline GroupBy duplikat; no regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT human-verify fase 368 (#23 cleanup browser + #27 label + #25 no-500)</name>
  <files>— (checkpoint: tidak ada perubahan kode)</files>
  <action>Checkpoint human-verify — TIDAK ada perubahan kode di task ini. Jalankan UAT browser sesuai blok how-to-verify di bawah (#23 preview→execute→idempotent dengan Seed Workflow restore, #27 label, #25 no-500). Pause untuk persetujuan user sebelum membuat SUMMARY.</action>
  <what-built>
    Seluruh fase 368 ter-otomasi: #21 edit atomik, #22 reset ET, #23 endpoint cleanup orphan, #24 import audit, #25 dedup helper, #26 renewal validation, #27 label. `dotnet build` 0 error + full test suite hijau (quick + integration real-SQL). Checkpoint ini memverifikasi 3 perilaku Manual-Only dari 368-VALIDATION.md yang butuh browser + DB cross-check.
  </what-built>
  <how-to-verify>
    Prasyarat lokal: `Authentication__UseActiveDirectory=false dotnet run` (AD lokal), buka `http://localhost:5277`, login admin (admin@pertamina.com). SEBELUM test #23: **Seed Workflow** — snapshot DB lokal (`sqlcmd ... BACKUP DATABASE`), catat `docs/SEED_JOURNAL.md`.

    1. **#23 orphan cleanup (preview→execute→idempotent):**
       a. Seed orphan: insert beberapa `AssessmentAttemptHistory` dengan `SessionId` dangling (mis. 999990-999992, UserId admin valid) via sqlcmd ke DB lokal (catat journal).
       b. Buka endpoint `GET /Admin/CleanupAttemptHistory` → verifikasi preview-count menampilkan jumlah orphan (mis. "Ditemukan 3 baris ...").
       c. Klik "Hapus Orphan" → konfirmasi → verifikasi flash "Selesai: 3 orphan ... dihapus."
       d. `SELECT COUNT(*)` orphan via sqlcmd → 0 bersih.
       e. Re-run GET preview → count = 0, tombol disable/"DB bersih" (idempotent).
       f. **Restore DB** (`RESTORE DATABASE`) + tandai journal `cleaned`.
    2. **#27 label**: buka `/Admin/BulkBackfill` → judul/breadcrumb/h2 = "Bulk Import Nilai (Excel)"; buka Admin Index + tab Assessment Groups → tombol/teks = "Bulk Import Nilai (Excel)" (bukan "Bulk Backfill (Restore Lost Data)").
    3. **#25 CertificationManagement no-500**: buka halaman CMP CertificationManagement + CDP CertificationManagement → render OK (tidak 500). Bila ada data sub-kategori duplicate-Name, halaman tetap tampil (sebelumnya akan 500).
    4. (opsional bila waktu) **#21 edit metadata-only**: edit 1 training record ubah nama TANPA upload file → simpan → file sertifikat lama masih ada (tidak hilang).
  </how-to-verify>
  <resume-signal>Ketik "approved" bila 3 perilaku (+opsional) sesuai, atau deskripsikan masalah.</resume-signal>
  <verify>Manual UAT — 3 perilaku (#23/#27/#25) ter-konfirmasi di browser; Seed Workflow journal `cleaned`. Otomatis pendukung: `dotnet test HcPortal.Tests` full suite hijau.</verify>
  <done>User ketik "approved"; #23 idempotent (re-run preview=0) terverifikasi; #27 label tampil; #25 CertificationManagement tidak 500; DB lokal di-restore (journal cleaned).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CMP/CDP CertificationManagement | User lihat data sertifikat (existing auth/scope) |
| Data kategori → GroupBy lookup | transformasi data internal (no user input langsung ke helper) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-368-12 | Denial of Service | #25 `ToDictionary(c=>c.Name)` throw → 500 (availability) | mitigate | GroupBy dedup (helper shared) → tidak throw pada duplicate child Name; halaman tetap render. |
| T-368-13 | — (no new surface) | #25 helper = transformasi data internal, tidak terima input user langsung | accept | Tidak ada permukaan serangan baru: refactor murni (GroupBy menggantikan ToDictionary), authz/scope CMP/CDP existing tak berubah. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "ParentNameLookup"` passed.
- `dotnet test HcPortal.Tests` FULL suite hijau (semua plan 01-04 merged — quick + integration real-SQL).
- grep: helper di SertifikatRow; KEDUA callsite konsumsi shared; 0 ToDictionary(c=>c.Name); 0 GroupBy inline di CMP/CDP.
- Migration=false.
- UAT Task 3 approved (Seed Workflow journal `cleaned`).
</verification>

<success_criteria>
- #25: helper static shared di SertifikatRow; CMP+CDP konsumsi yang sama; tidak 500 pada duplicate child Name; anti-drift (no inline ganda).
- UAT: #23 preview→execute→idempotent browser-verified (Seed Workflow restore); #27 label tampil; #25 no-500.
- 1 test file baru hijau; full suite hijau; no regression; migration=false.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-04-SUMMARY.md`.
</output>
