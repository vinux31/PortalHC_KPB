---
phase: 369-sync-h1-search-drop-fix-main-ithandoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/WorkerDataService.cs
  - HcPortal.Tests/WorkerDataServiceSearchTests.cs
autonomous: true
requirements: [URG-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Guard pre-narrow di WorkerDataService.GetWorkersInSection identik main: treat searchScope null/kosong sebagai 'Nama'"
    - "Test regresi Scope_Null_WithSearch_FiltersByName_H1 ada dan hijau"
    - "Full suite dotnet test hijau (zero regresi pada scope-gating REC-06)"
    - "Commit cherry-pick punya jejak '(cherry picked from commit 14e7adc5' (audit trail -x)"
    - "Tab Input Records search nama/NIP memfilter list (bukan balikin semua row)"
  artifacts:
    - path: "Services/WorkerDataService.cs"
      provides: "Guard SQL name pre-narrow untuk searchScope null/kosong (fix H1)"
      contains: "string.IsNullOrEmpty(searchScope) || searchScope == \"Nama\""
    - path: "HcPortal.Tests/WorkerDataServiceSearchTests.cs"
      provides: "Test regresi H1 (searchScope=null + ada search → filter by name)"
      contains: "Scope_Null_WithSearch_FiltersByName_H1"
  key_links:
    - from: "Services/WorkerDataService.cs (guard line ~259)"
      to: "Controllers/AssessmentAdminController.cs (ManageAssessmentTab_Training, line 280 — searchScope=null)"
      via: "GetWorkersInSection pre-narrow guard aktif saat searchScope null"
      pattern: "GetWorkersInSection\\(section, unit, category, search, statusFilter\\)"
---

<objective>
Bawa fix H1 dari branch `main` (commit `14e7adc5`) ke branch `ITHandoff` via `git cherry-pick -x` — SAJA commit ini, BUKAN full merge main→ITHandoff (D-01). Fix mengubah satu kondisi guard di `WorkerDataService.GetWorkersInSection`: `searchScope == "Nama"` menjadi `(string.IsNullOrEmpty(searchScope) || searchScope == "Nama")`, sehingga caller lama `ManageAssessmentTab_Training` (Tab Input Records, panggil tanpa searchScope → null) tidak lagi men-drop search nama/NIP diam-diam. Commit membawa serta 1 test regresi `Scope_Null_WithSearch_FiltersByName_H1`.

Purpose: Search nama/NIP di Tab Input Records `/Admin/ManageAssessment` saat ini diabaikan diam-diam di ITHandoff (mengembalikan SEMUA row di section). Setelah fix, list terfilter. Menyelaraskan ITHandoff dengan main yang sudah benar (URG-01, v26.0 URGENT).

Output: 1 commit cherry-pick (`-x`, pesan asli + jejak audit), guard identik main, test H1 hijau, full suite hijau, UAT live ter-verifikasi terfilter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/369-sync-h1-search-drop-fix-main-ithandoff/369-CONTEXT.md
@.planning/phases/369-sync-h1-search-drop-fix-main-ithandoff/369-RESEARCH.md
@.planning/phases/369-sync-h1-search-drop-fix-main-ithandoff/369-VALIDATION.md

<interfaces>
<!-- Kontrak yang dibutuhkan executor — TIDAK perlu eksplorasi codebase. -->

Signature target (TIDAK berubah di fase ini — Services/IWorkerDataService.cs:14):
```csharp
Task<List<WorkerTrainingStatus>> GetWorkersInSection(
    string? section, string? unitFilter = null, string? category = null,
    string? search = null, string? statusFilter = null,
    DateTime? dateFrom = null, DateTime? dateTo = null,
    string? subCategory = null, string? searchScope = null)
```

Guard SEBELUM (ITHandoff sekarang, Services/WorkerDataService.cs:257-266):
```csharp
// REC-06 D-07: SQL name pre-narrow HANYA untuk scope "Nama".
// "Training"/"Keduanya" di-handle post-load (union) supaya tidak buang training-only match.
if (searchScope == "Nama" && !string.IsNullOrEmpty(search))
{
    search = search.ToLower();
    usersQuery = usersQuery.Where(u =>
        u.FullName.ToLower().Contains(search) ||
        (u.NIP != null && u.NIP.Contains(search))
    );
}
```

Guard SESUDAH (hasil cherry-pick = identik main `14e7adc5`):
```csharp
// REC-06 D-07: SQL name pre-narrow untuk scope "Nama".
// H1: searchScope null/kosong di-treat "Nama" supaya caller lama (ManageAssessmentTab_Training) tidak drop search.
// "Training"/"Keduanya" di-handle post-load (union) supaya tidak buang training-only match.
if ((string.IsNullOrEmpty(searchScope) || searchScope == "Nama") && !string.IsNullOrEmpty(search))
{
    ...
}
```

Caller terdampak (TIDAK diubah — Controllers/AssessmentAdminController.cs:280):
```csharp
// route: [HttpGet][Authorize(Roles="Admin, HC")] ManageAssessmentTab_Training(search, page, pageSize, section, unit, category, statusFilter, isFiltered)
var fullList = await _workerDataService.GetWorkersInSection(section, unit, category, search, statusFilter);
// searchScope = param ke-9 default null → fix mengaktifkan guard.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight + cherry-pick -x 14e7adc5 + verifikasi guard/build/test</name>
  <files>Services/WorkerDataService.cs, HcPortal.Tests/WorkerDataServiceSearchTests.cs</files>
  <read_first>
    - 369-RESEARCH.md §Common Pitfalls (Pitfall 1 working tree dirty, Pitfall 2 sesi paralel 363, Pitfall 3 CRLF, Pitfall 5 verifikasi identik main) + §Code Examples
    - 369-CONTEXT.md §Implementation Decisions (D-01 cherry-pick SAJA, D-02 `-x`, D-03 verifikasi)
    - `<interfaces>` di atas (guard SEBELUM/SESUDAH)
  </read_first>
  <action>
    Operasi murni lokal di branch ITHandoff. JANGAN edit guard manual — gunakan cherry-pick (D-01/D-02 locked). JANGAN buat commit kode tambahan; cherry-pick SENDIRI adalah commit-nya.

    **Langkah A — Pre-flight (lindungi dari sesi paralel 363 + tree dirty):**
    1. `git rev-parse --abbrev-ref HEAD` → HARUS `ITHandoff`. Kalau bukan, STOP dan lapor.
    2. Cek tidak ada git op berjalan: pastikan file `.git/index.lock` TIDAK ada. (PowerShell: `Test-Path .git/index.lock` → harus False.) Kalau ADA → sesi paralel 363 sedang menulis index; TUNGGU (jeda), cek ulang sampai hilang. JANGAN paksa lanjut.
    3. `git status --short` → HARUS TIDAK memunculkan `Services/WorkerDataService.cs` atau `HcPortal.Tests/WorkerDataServiceSearchTests.cs` sebagai modified. (File non-target dirty seperti `.claude/settings.local.json`, `.planning/config.json`, `docs/SEED_JOURNAL.md`, `*-PATTERNS.md` AMAN — cherry-pick tidak menyentuhnya.) Kalau file TARGET muncul modified → STOP dan lapor (kemungkinan sesi 363 menyentuh file target; re-verify diperlukan).
    4. `git merge-base --is-ancestor 14e7adc5 HEAD; echo $LASTEXITCODE` (PowerShell: `git merge-base --is-ancestor 14e7adc5 HEAD; echo $?`) → exit 1 = belum ada (lanjut). Exit 0 = sudah di ITHandoff → SKIP cherry-pick, langsung verifikasi guard (Langkah C).

    **Langkah B — Cherry-pick (D-02):**
    5. Jalankan: `git cherry-pick -x 14e7adc5`
    6. CRLF warnings (`warning: LF will be replaced by CRLF`) NORMAL di repo Windows ini — abaikan, BUKAN kegagalan (Pitfall 3). Sukses = exit code 0 + commit baru terbentuk.
    7. **Kalau konflik tak terduga** (tidak diharapkan — file ITHandoff terverifikasi identik parent `14e7adc5^`): `git cherry-pick --abort` lalu STOP dan lapor ke orchestrator. JANGAN resolve manual sendiri tanpa konfirmasi (Claude's Discretion: hasil akhir harus guard identik main).

    **Langkah C — Verifikasi guard identik main (SC #1):**
    8. `git grep -n "string.IsNullOrEmpty(searchScope) || searchScope == ""Nama""" -- Services/WorkerDataService.cs` → HARUS menemukan baris guard.
    9. `git diff main -- Services/WorkerDataService.cs` → expect KOSONG. Kalau ada perbedaan, periksa apakah di LUAR hunk H1 (line ~257-266); SC #1 hanya menuntut guard identik main → grep (langkah 8) adalah kriteria utama, diff sebagai konfirmasi.
    10. `git diff main -- HcPortal.Tests/WorkerDataServiceSearchTests.cs` → expect KOSONG.
    11. Verifikasi commit punya jejak audit: `git log -1 --format=%B` → body HARUS berisi `(cherry picked from commit 14e7adc5`.

    **Langkah D — Build + test (SC #2):**
    12. `dotnet build` → 0 error.
    13. Test cepat H1: `dotnet test --filter "FullyQualifiedName~Scope_Null_WithSearch_FiltersByName_H1"` → hijau (Passed! 1).
    14. Test grup search: `dotnet test --filter "FullyQualifiedName~WorkerDataServiceSearchTests"` → hijau.
    15. Full suite: `dotnet test` → semua hijau (Failed: 0).
  </action>
  <verify>
    <automated>git grep -n "string.IsNullOrEmpty(searchScope) || searchScope == \"Nama\"" -- Services/WorkerDataService.cs ; dotnet test --filter "FullyQualifiedName~WorkerDataServiceSearchTests" ; dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - `git grep` menemukan guard `string.IsNullOrEmpty(searchScope) || searchScope == "Nama"` di Services/WorkerDataService.cs (SC #1)
    - `git diff main -- Services/WorkerDataService.cs` KOSONG (atau beda hanya di luar hunk H1 line 257-266)
    - `git log -1 --format=%B` body berisi `(cherry picked from commit 14e7adc5` (jejak `-x`, D-02)
    - `dotnet build` 0 error
    - `dotnet test --filter "FullyQualifiedName~Scope_Null_WithSearch_FiltersByName_H1"` → Passed 1
    - `dotnet test` full suite → Failed: 0 (zero regresi REC-06)
  </acceptance_criteria>
  <done>Commit cherry-pick `-x` terbentuk di ITHandoff; guard `WorkerDataService.cs` identik main; test H1 + full suite hijau; jejak audit "(cherry picked from commit 14e7adc5" ada. TIDAK ada commit kode tambahan dibuat.</done>
</task>

<task type="auto">
  <name>Task 2: UAT live autonomous — Tab Input Records search nama memfilter (curl HTML)</name>
  <files>(tidak ada file diubah — verifikasi runtime saja)</files>
  <read_first>
    - 369-RESEARCH.md §Code Examples (UAT live) + Pitfall 4 (AD env var)
    - 369-VALIDATION.md §Manual-Only Verifications + §Per-Task Verification Map
    - 369-CONTEXT.md D-03 (UAT 1 skenario @5277)
    - `<interfaces>` caller `ManageAssessmentTab_Training` querystring
  </read_first>
  <action>
    Verifikasi runtime bahwa caller lama (searchScope=null) sekarang memfilter. Executor TANPA browser MCP → gunakan pendekatan curl (autonomous); orchestrator akan spot-check browser terpisah.

    **Langkah A — Jalankan app dengan AD off (WAJIB — Pitfall 4):**
    1. Set env var + run di background (PowerShell): `$env:Authentication__UseActiveDirectory="false"; dotnet run` → tunggu sampai listening @ `http://localhost:5277` (cek log "Now listening on: http://localhost:5277" atau "Application started").
    2. Kalau port 5277 sudah dipakai (mungkin sesi lain), STOP dan lapor — jangan adu port.

    **Langkah B — Verifikasi via curl querystring (autonomous, route `[Authorize(Admin,HC)]` → butuh session):**
    Route `ManageAssessmentTab_Training` ber-`[Authorize(Roles="Admin, HC")]`. Untuk akses, login dulu dapatkan cookie auth:
    3. Login admin lokal (admin@pertamina.com — lihat MEMORY reference_dev_credentials) via POST ke endpoint login dengan cookie jar (curl `-c cookies.txt` lalu `-b cookies.txt`). Ambil anti-forgery token dari halaman login bila form butuh (parse `__RequestVerificationToken` hidden input). Kalau alur login terlalu rumit untuk curl murni dalam batas wajar, JATUH ke checkpoint:human-verify (lihat fallback di bawah).
    4. Dengan cookie auth, panggil DUA request dan bandingkan jumlah row hasil:
       - Baseline (tanpa search, terfilter section): `GET http://localhost:5277/Admin/ManageAssessmentTab_Training?section=<section_ada_data>&isFiltered=true`
       - Dengan search nama spesifik: `GET http://localhost:5277/Admin/ManageAssessmentTab_Training?section=<section_sama>&search=<nama_atau_NIP_worker_existing>&isFiltered=true`
    5. Verifikasi: HTML respon request ber-search berisi HANYA row yang match nama/NIP (count row < baseline, dan row yang muncul mengandung string search). Hitung row via pola tabel worker (mis. occurrence `WorkerTrainingStatus` row / `<tr` di tabel hasil / `data-worker` bila ada). Kriteria PASS: hasil search TERFILTER (subset), BUKAN sama dengan baseline penuh.
    6. Catat di SUMMARY: section + search term yang dipakai, jumlah row baseline vs terfilter.

    **Fallback (kalau login curl tak praktis):** Tandai langkah ini sebagai catatan checkpoint:human-verify di SUMMARY dengan instruksi persis: `Authentication__UseActiveDirectory=false dotnet run` → http://localhost:5277 → login admin@pertamina.com → /Admin/ManageAssessment → Tab Input Records → pilih section → search nama/NIP → konfirmasi list TERFILTER (bukan full roster). Orchestrator melakukan spot-check browser apa pun hasilnya.

    **Langkah C — Bersihkan:**
    7. Hentikan proses `dotnet run` background setelah verifikasi. Hapus `cookies.txt` bila dibuat.
    8. TIDAK ada perubahan DB (search read-only) → tidak perlu snapshot/restore (RESEARCH §Seed Data Workflow).
  </action>
  <verify>
    <automated>curl -s -b cookies.txt "http://localhost:5277/Admin/ManageAssessmentTab_Training?section=$env:UAT_SECTION&search=$env:UAT_SEARCH&isFiltered=true" | Select-String -Pattern "$env:UAT_SEARCH"</automated>
  </verify>
  <acceptance_criteria>
    - App berjalan @5277 dengan `Authentication__UseActiveDirectory=false`
    - Request ber-search ke `ManageAssessmentTab_Training` mengembalikan list TERFILTER (subset baseline; row yang muncul match nama/NIP search) — BUKAN semua row di section (SC #3)
    - Jumlah row terfilter < jumlah row baseline (tanpa search) untuk section yang sama
    - SUMMARY mencatat section + search term + row count baseline vs terfilter (atau, bila fallback: instruksi checkpoint human-verify persis)
    - Proses `dotnet run` dihentikan; tidak ada perubahan DB
  </acceptance_criteria>
  <done>Search nama/NIP di Tab Input Records terbukti memfilter list (subset, bukan full roster) via curl HTML — atau instruksi checkpoint human-verify terdokumentasi di SUMMARY untuk spot-check orchestrator. App background dihentikan, working tree bersih dari artefak UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller (`ManageAssessmentTab_Training`) | Input `search`/`section`/`unit` dari user; route ber-`[Authorize(Roles="Admin, HC")]` |
| Controller → Service (`GetWorkersInSection`) → EF Core | `search` dipakai di LINQ `.Where(... Contains(search))` — query read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-369-01 | Tampering (SQL injection via `search`) | `WorkerDataService.GetWorkersInSection` LINQ `.Contains(search)` | accept | EF Core LINQ `.Contains()` → parameterized query (bukan string concat / raw SQL). Sudah aman; fix H1 tidak mengubah pola query, hanya kondisi guard boolean. ASVS V5 terpenuhi by-design. [VERIFIED RESEARCH §Security Domain] |
| T-369-02 | Information Disclosure (search expose worker lain) | hasil `GetWorkersInSection` | accept | Fix MENYEMPITKAN hasil (filter by nama/NIP); scope section/unit + `[Authorize(Admin, HC)]` TIDAK dilonggarkan. Risiko menurun bukan naik — sebelum fix malah balikin SEMUA row. |
| T-369-03 | Elevation of Privilege (akses tanpa role) | route `ManageAssessmentTab_Training` | mitigate | Route tetap `[Authorize(Roles="Admin, HC")]` (line 251) — fix service-layer tidak menyentuh otorisasi. UAT login pakai admin lokal; tidak ada perubahan access control. [VERIFIED AssessmentAdminController.cs:251] |
</threat_model>

<verification>
- SC #1: `git grep -n "string.IsNullOrEmpty(searchScope) || searchScope == \"Nama\"" -- Services/WorkerDataService.cs` menemukan guard + `git diff main -- Services/WorkerDataService.cs` kosong
- SC #1 audit trail: `git log -1 --format=%B` berisi `(cherry picked from commit 14e7adc5`
- SC #2: `dotnet build` 0 error + `dotnet test` full suite Failed:0 + test `Scope_Null_WithSearch_FiltersByName_H1` Passed
- SC #3: curl `ManageAssessmentTab_Training?section=...&search=...` mengembalikan subset terfilter (atau checkpoint human-verify terdokumentasi)
</verification>

<success_criteria>
1. `WorkerDataService.cs` guard pre-narrow = `(string.IsNullOrEmpty(searchScope) || searchScope == "Nama") && !string.IsNullOrEmpty(search)` — identik main `14e7adc5`, dibawa via cherry-pick `-x` (jejak audit ada).
2. Test regresi `Scope_Null_WithSearch_FiltersByName_H1` ada + hijau; full suite `dotnet test` hijau (zero regresi REC-06).
3. UAT @5277: Tab Input Records search nama/NIP memfilter list (subset, bukan semua row).
4. Zero file lain disentuh (hanya 2 file dalam commit cherry-pick); migration=false; TIDAK ada commit kode tambahan; AssessmentAdminController/CDPController TIDAK disentuh (sesi 363).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/369-sync-h1-search-drop-fix-main-ithandoff/369-01-SUMMARY.md`. Catat: hash commit cherry-pick, hasil `git diff main` (kosong/tidak), jumlah test hijau, dan detail UAT (section + search term + row count baseline vs terfilter, atau status checkpoint human-verify). Tandai migration=FALSE + ITHandoff NOT PUSHED (push = event terpisah pre-handoff IT per D-01/STATE Push pending IT).
</output>
