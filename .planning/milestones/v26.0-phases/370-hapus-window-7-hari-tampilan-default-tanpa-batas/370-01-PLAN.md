---
phase: 370-hapus-window-7-hari-tampilan-default-tanpa-batas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/AssessmentSearchWindowTests.cs
autonomous: false
requirements: [URG-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Tab Assessment (ManageAssessmentTab_Assessment) default view (tanpa search) menampilkan sesi >7 hari"
    - "AssessmentMonitoring default view (tanpa search) menampilkan sesi >7 hari"
    - "Search judul sesi lama tetap memunculkan sesi tsb (behavior 260611-m9r tidak regresi)"
    - "Filter status default Aktif + hide-Closed CIL-02 tetap berlaku"
    - "Pagination Tab Assessment tetap jalan dengan dataset membesar"
    - "dotnet build 0 error + full suite hijau (226 setelah hapus 3 [Fact])"
    - "Zero sisa referensi ApplySevenDayWindow/sevenDaysAgo di kode (Controllers/, HcPortal.Tests/, Views/, wwwroot/, tests/)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ManageAssessmentTab_Assessment + AssessmentMonitoring tanpa window 7-hari; helper ApplySevenDayWindow dihapus; AsNoTracking ditambah di Monitoring"
      contains: "Phase 370 (URG-02)"
    - path: "HcPortal.Tests/AssessmentSearchWindowTests.cs"
      provides: "DELETED — 3 [Fact] uji helper yang sudah tak ada (compile-coupling)"
  key_links:
    - from: "ManageAssessmentTab_Assessment"
      to: "_context.AssessmentSessions"
      via: "AsNoTracking().AsQueryable() langsung ke .Where(search) tanpa ApplySevenDayWindow"
      pattern: "AsNoTracking"
    - from: "AssessmentMonitoring"
      to: "_context.AssessmentSessions"
      via: "AsNoTracking().AsQueryable() langsung ke .Where(search) tanpa ApplySevenDayWindow"
      pattern: "AsNoTracking"
---

<objective>
Hapus window 7-hari (`sevenDaysAgo` + helper `ApplySevenDayWindow`) dari tampilan default `ManageAssessmentTab_Assessment` + `AssessmentMonitoring` sehingga SEMUA sesi tampil tanpa batas umur. Helper `ApplySevenDayWindow` (quick task 260611-m9r) di-retire utuh, file test `AssessmentSearchWindowTests.cs` (3 [Fact]) dihapus dalam commit yang sama (compile-coupling), tambah `.AsNoTracking()` di query Monitoring (D-05), bersihkan komentar stale (260611-m9r + 90-review).

Purpose: Keputusan user 2026-06-11 verbatim "7 hari jadi tanpa batas" — sesi assessment lama (mis. Post Test OJT >7 hari, 55 sesi legacy) harus tampil di default view, bukan hanya saat search. Filter status default "Aktif" + hide-Closed (CIL-02) TETAP sebagai penyaring utama. URG-02.

Output: `AssessmentAdminController.cs` bersih dari window 7-hari (2 method + helper); `AssessmentSearchWindowTests.cs` terhapus; suite 229→226 hijau; grep-guard zero hit di kode. Migration=false, view tak berubah, zero dependency baru.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-CONTEXT.md
@.planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-PATTERNS.md
@.planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-RESEARCH.md

<interfaces>
<!-- State kode SAAT INI yang harus diedit. Diverifikasi langsung 2026-06-11 (line number cocok PATTERNS.md, 363 tidak menggeser region ini). Executor bekerja dari teks ini — tidak perlu eksplorasi codebase. -->

ManageAssessmentTab_Assessment — signature & query start (AssessmentAdminController.cs:112-125):
```csharp
        public async Task<IActionResult> ManageAssessmentTab_Assessment(string? search, int page = 1, int pageSize = 20,
            string? category = null, string? statusFilter = null)
        {
            var sw = System.Diagnostics.Stopwatch.StartNew();
            var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);

            // Phase 311 Plan 03: AsNoTracking di chain start read-only partial action (PATTERNS Cross-cutting Pattern 2)
            // Quick fix (260611-m9r): window 7-hari via helper — di-skip saat search non-empty (cari sesi lama >7 hari).
            var managementQuery = _context.AssessmentSessions
                .AsNoTracking()
                .AsQueryable();
            managementQuery = ApplySevenDayWindow(managementQuery, search, sevenDaysAgo);

            if (!string.IsNullOrEmpty(search))
```

Helper ApplySevenDayWindow — definisi (AssessmentAdminController.cs:2816-2826):
```csharp
        // Quick fix (260611-m9r): window 7-hari hanya berlaku saat search KOSONG.
        // Search eksplisit dari user mengalahkan penyempitan default (preseden CIL-02 Phase 338),
        // supaya assessment lama (>7 hari, mis. Post Test OJT) bisa ditemukan via search.
        // Static + pure → testable (xUnit) via LINQ-to-Objects.
        public static IQueryable<AssessmentSession> ApplySevenDayWindow(
            IQueryable<AssessmentSession> query, string? search, DateTime cutoff)
        {
            if (string.IsNullOrEmpty(search))
                return query.Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= cutoff);
            return query;
        }
```
Tetangga blok helper (JANGAN sentuh): sebelum = `:2814` (`return "Not started";` + `}`); sesudah = `:2828` (`// MAM-06: Tab2 Input Records...` → `IsTrainingInitialState` helper static lain yang TETAP).

AssessmentMonitoring — signature & query start (AssessmentAdminController.cs:2858-2876):
```csharp
        [Authorize(Roles = "Admin, HC")]
        public async Task<IActionResult> AssessmentMonitoring(
            string? search,
            string? status,
            string? category)
        {
            // 7-day window — same as ManageAssessment.
            // Quick fix (260611-m9r): window HANYA saat search kosong. Saat search non-empty,
            // window di-skip supaya assessment lama (>7 hari) bisa ditemukan via search.
            // Abandoned sessions tanpa ExamWindowCloseDate fallback ke Schedule untuk cek window
            // (relevan hanya saat search kosong).
            var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);

            var query = _context.AssessmentSessions.AsQueryable();
            query = ApplySevenDayWindow(query, search, sevenDaysAgo);

            // Text search by title
            if (!string.IsNullOrEmpty(search))
```

File test yang DIHAPUS (HcPortal.Tests/AssessmentSearchWindowTests.cs) — 3 [Fact] me-reference `AssessmentAdminController.ApplySevenDayWindow` di :33/:44/:55. Setelah helper hilang → `CS0117` compile error di test project bila tidak dihapus.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hapus window 7-hari (2 method + helper) + AsNoTracking Monitoring + git rm test file (atomic)</name>
  <files>Controllers/AssessmentAdminController.cs, HcPortal.Tests/AssessmentSearchWindowTests.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (regions :112-136, :2808-2832, :2858-2890 — baca SEBELUM edit untuk konfirmasi line stability)
    - .planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-PATTERNS.md (state SAAT INI vs TARGET verbatim untuk Edit A/B/C/D)
    - .planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-CONTEXT.md (D-01..D-05 locked)
    - HcPortal.Tests/AssessmentSearchWindowTests.cs (konfirmasi 3 [Fact] sebelum hapus)
  </read_first>
  <behavior>
    Tidak ada test unit baru (D-04 locked — controller butuh DbContext, "absence of filter" tak punya assertion bermakna). Guard = grep-guard + full suite + UAT (Task 2). Setelah edit:
    - dotnet build → 0 error (test project tidak lagi compile-break karena file test + helper hilang bersamaan)
    - dotnet test → Failed: 0, Total: 226 (turun dari 229 karena 3 [Fact] hilang)
    - grep ApplySevenDayWindow|sevenDaysAgo di kode → zero hit
  </behavior>
  <action>
ATURAN ATOMIC (Pitfall 1 + preseden 359-04): keempat edit + git rm HARUS dalam SATU commit logis. Hapus helper (Edit B) tanpa hapus file test (Edit D) → `CS0117: 'AssessmentAdminController' does not contain a definition for 'ApplySevenDayWindow'` di test project. Lakukan semua perubahan, lalu build+test, baru commit sekali.

**Edit A — ManageAssessmentTab_Assessment (AssessmentAdminController.cs ~:114-125)**
GANTI blok ini (state SAAT INI):
```csharp
        {
            var sw = System.Diagnostics.Stopwatch.StartNew();
            var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);

            // Phase 311 Plan 03: AsNoTracking di chain start read-only partial action (PATTERNS Cross-cutting Pattern 2)
            // Quick fix (260611-m9r): window 7-hari via helper — di-skip saat search non-empty (cari sesi lama >7 hari).
            var managementQuery = _context.AssessmentSessions
                .AsNoTracking()
                .AsQueryable();
            managementQuery = ApplySevenDayWindow(managementQuery, search, sevenDaysAgo);

            if (!string.IsNullOrEmpty(search))
```
MENJADI (state TARGET):
```csharp
        {
            var sw = System.Diagnostics.Stopwatch.StartNew();

            // Phase 370 (URG-02): window 7-hari dihapus — tampilan default tanpa batas umur.
            // Phase 311 Plan 03: AsNoTracking di chain start read-only partial action.
            var managementQuery = _context.AssessmentSessions
                .AsNoTracking()
                .AsQueryable();

            if (!string.IsNullOrEmpty(search))
```
PERTAHANKAN VERBATIM (jangan sentuh): `var sw = Stopwatch.StartNew();` (telemetry Phase 311, independen dari sevenDaysAgo) + `.AsNoTracking().AsQueryable()` (sudah ada sejak 311) + seluruh blok `if (!string.IsNullOrEmpty(search))` dst (search behavior tidak regresi).

**Edit B — Hapus definisi helper ApplySevenDayWindow (AssessmentAdminController.cs ~:2816-2826)**
HAPUS UTUH blok ini (4 baris komentar + method body):
```csharp
        // Quick fix (260611-m9r): window 7-hari hanya berlaku saat search KOSONG.
        // Search eksplisit dari user mengalahkan penyempitan default (preseden CIL-02 Phase 338),
        // supaya assessment lama (>7 hari, mis. Post Test OJT) bisa ditemukan via search.
        // Static + pure → testable (xUnit) via LINQ-to-Objects.
        public static IQueryable<AssessmentSession> ApplySevenDayWindow(
            IQueryable<AssessmentSession> query, string? search, DateTime cutoff)
        {
            if (string.IsNullOrEmpty(search))
                return query.Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= cutoff);
            return query;
        }
```
JANGAN sentuh tetangga: blok sebelum berakhir `return "Not started"; }` (:2814), blok sesudah `// MAM-06: Tab2 Input Records...` → `IsTrainingInitialState` (:2828+, helper static lain yang TETAP). Hapus juga baris kosong yang membuat double-blank-line bila ada.

**Edit C — AssessmentMonitoring (AssessmentAdminController.cs ~:2864-2873)**
GANTI blok ini (state SAAT INI):
```csharp
            // 7-day window — same as ManageAssessment.
            // Quick fix (260611-m9r): window HANYA saat search kosong. Saat search non-empty,
            // window di-skip supaya assessment lama (>7 hari) bisa ditemukan via search.
            // Abandoned sessions tanpa ExamWindowCloseDate fallback ke Schedule untuk cek window
            // (relevan hanya saat search kosong).
            var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);

            var query = _context.AssessmentSessions.AsQueryable();
            query = ApplySevenDayWindow(query, search, sevenDaysAgo);

            // Text search by title
            if (!string.IsNullOrEmpty(search))
```
MENJADI (state TARGET):
```csharp
            // Phase 370 (URG-02): window 7-hari dihapus — tampilan default tanpa batas umur.
            // D-05: .AsNoTracking() — read-only method (no SaveChanges), selaras Tab Assessment Phase 311.
            var query = _context.AssessmentSessions
                .AsNoTracking()
                .AsQueryable();

            // Text search by title
            if (!string.IsNullOrEmpty(search))
```
PERTAHANKAN VERBATIM: blok `if (!string.IsNullOrEmpty(search))` + MAP-23 (:2876-2881), category filter (:2884-2885), seluruh grouping + CIL-01 badge + CIL-02 hide-Closed + MAP-15 status="All" (downstream, tidak terdampak).

**Edit D — Hapus file test (git rm, BUKAN Edit/Write)**
Jalankan: `git rm HcPortal.Tests/AssessmentSearchWindowTests.cs`
File berisi 3 [Fact] yang me-reference helper yang sudah dihapus (D-02 — tidak di-repurpose).

**Build + suite + grep-guard SEBELUM commit:**
1. `dotnet build` → 0 error (jika `CS0117` muncul → file test belum ke-`git rm`, ulangi Edit D)
2. `dotnet test` → Failed: 0, Total: 226
3. grep-guard (PowerShell): `Get-ChildItem -Recurse -Include *.cs,*.cshtml,*.ts,*.js -Path Controllers,Views,wwwroot,tests,HcPortal.Tests | Select-String -Pattern "ApplySevenDayWindow|sevenDaysAgo"` → zero hit (abaikan .planning/ docs — Pitfall 3, bukan compiled code)

Lalu commit sekali (atomic): `git commit` dengan pesan referensi URG-02 + D-01/D-02/D-05.

CATATAN working tree: file dirty non-target (.claude/settings.local.json, .planning/config.json, *-PATTERNS.md) AMAN — JANGAN ikut di-stage di commit ini. Stage hanya 2 file target.
  </action>
  <verify>
    <automated>dotnet build</automated>
    <automated>dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` output mengandung "Build succeeded" / 0 error (tidak ada CS0117).
    - `dotnet test` output mengandung "Failed: 0" dan "Total: 226".
    - PowerShell `(Get-ChildItem -Recurse -Include *.cs,*.cshtml,*.ts,*.js -Path Controllers,Views,wwwroot,tests,HcPortal.Tests | Select-String -Pattern "ApplySevenDayWindow|sevenDaysAgo" | Measure-Object).Count` returns 0.
    - `Select-String -Path Controllers/AssessmentAdminController.cs -Pattern "Phase 370 \(URG-02\)"` returns 2 hit (Edit A + Edit C).
    - `Test-Path HcPortal.Tests/AssessmentSearchWindowTests.cs` returns False.
    - `Select-String -Path Controllers/AssessmentAdminController.cs -Pattern "var sw = System.Diagnostics.Stopwatch.StartNew"` masih ada (telemetry 311 dipertahankan).
    - `Select-String -Path Controllers/AssessmentAdminController.cs -Pattern "7-day|260611-m9r|90-review"` returns 0 hit (komentar stale dibersihkan).
  </acceptance_criteria>
  <done>
    Window 7-hari hilang dari kedua method; helper ApplySevenDayWindow + file test terhapus dalam 1 commit; AsNoTracking ditambah di Monitoring; komentar stale bersih; build 0 error + suite 226 hijau + grep-guard zero hit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT @5277 — default view + search + pagination (data legacy existing, zero seed)</name>
  <files>(read-only verification — no file modified)</files>
  <read_first>
    - .planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-VALIDATION.md (Manual-Only Verifications)
    - .planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-CONTEXT.md (D-06 — data legacy existing, zero seed/snapshot/restore)
  </read_first>
  <what-built>
    Window 7-hari sudah dihapus dari `ManageAssessmentTab_Assessment` + `AssessmentMonitoring` (Task 1). Default view kini menampilkan semua sesi tanpa batas umur. UAT memverifikasi perilaku runtime (controller+DbContext) yang TIDAK bisa diuji unit (D-04 locked).

    Data UAT: legacy existing DB lokal (55 sesi >7 hari: 12 InProgress + 8 Open legacy + 22 OJT). ZERO seed, ZERO snapshot/restore (D-06 — read-only verification; SEED_WORKFLOW tidak berlaku).

    Fallback bila browser/Playwright tidak tersedia (pola Phase 369): orchestrator jalankan UAT inline via Playwright MCP. Bila tetap absen, executor lakukan code-verify (konfirmasi via grep bahwa tidak ada `.Where(>= sevenDaysAgo)` tersisa di chain + DB query manual menghitung row >7 hari yang kini lolos) dan catat di SUMMARY sebagai "code-verified, human UAT pending".
  </what-built>
  <action>Checkpoint human-verify — eksekusi 5 langkah UAT @5277 sesuai &lt;how-to-verify&gt;. Jalankan server `Authentication__UseActiveDirectory=false dotnet run`, login admin@pertamina.com / 123456, verifikasi: default view Tab+Monitoring tampilkan sesi >7 hari, filter status CIL-02 tetap, search no-regresi, pagination jalan. Zero seed (D-06). Fallback code-verify bila browser absen (pola Phase 369).</action>
  <how-to-verify>
    Prasyarat (Pitfall 2 — AD lokal): jalankan server dengan flag AD off, JANGAN `dotnet run` polos:
    ```
    Authentication__UseActiveDirectory=false dotnet run
    ```
    (PowerShell: `$env:Authentication__UseActiveDirectory="false"; dotnet run`)
    Login admin@pertamina.com / 123456 di http://localhost:5277 (kredensial UAT lokal).

    1. **SC1 — default view tanpa batas (Tab Assessment):** Buka `/Admin/ManageAssessment` → Tab Assessment (tanpa ketik search). VERIFIKASI: sesi lama >7 hari TAMPIL (mis. Post Test OJT, atau sesi InProgress/Open legacy). Sebelum fase ini sesi tsb tersembunyi.
    2. **SC1 — default view (Monitoring):** Buka `/Admin/AssessmentMonitoring` (tanpa search). VERIFIKASI: sesi lama >7 hari tampil di group list.
    3. **SC1 — filter status TETAP:** Konfirmasi default masih "Aktif (Open/Upcoming)" + sesi Closed TERSEMBUNYI saat status & search kosong (CIL-02 utuh, bukan ikut terhapus).
    4. **SC2 — search tidak regresi:** Ketik judul sesi lama (mis. "OJT") di search → sesi tsb tetap MUNCUL (behavior 260611-m9r tidak rusak).
    5. **SC4 — pagination Tab Assessment:** Di Tab Assessment default → navigasi page 2+ → row berlanjut konsisten, tidak crash dengan dataset membesar.

    Expected: semua 5 langkah PASS. Sesi historis kini tampil sebagai default; trade-off (sesi terbengkalai lama ikut tampil) sudah diterima user 2026-06-11 (SC3).
  </how-to-verify>
  <verify>Manual UAT @5277 — 5 langkah PASS (atau code-verify fallback bila browser absen, dicatat di SUMMARY).</verify>
  <acceptance_criteria>
    - Default view Tab Assessment tampilkan minimal 1 sesi >7 hari tanpa search.
    - Default view AssessmentMonitoring tampilkan minimal 1 sesi >7 hari tanpa search.
    - Sesi Closed tetap tersembunyi saat status & search kosong (CIL-02 utuh).
    - Search judul sesi lama tetap memunculkan sesi tsb (no regresi 260611-m9r).
    - Pagination Tab Assessment page 2+ konsisten, tidak crash.
  </acceptance_criteria>
  <done>5 langkah UAT PASS @5277 (atau code-verified dengan human UAT pending dicatat di SUMMARY); user ketik "approved".</done>
  <resume-signal>Ketik "approved" jika 5 langkah UAT PASS, atau jelaskan masalah yang ditemukan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (admin browser) → controller endpoint | Request GET ke `/Admin/ManageAssessmentTab_Assessment` + `/Admin/AssessmentMonitoring`; input `search`/`category`/`statusFilter` user-controlled |
| controller → EF Core / SQL Server | Query `_context.AssessmentSessions` — parameterized via EF Core; window filter dilepas (perubahan fase ini) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-370-01 | Information Disclosure | ManageAssessmentTab_Assessment + AssessmentMonitoring (window dilepas → sesi historis >7 hari kini tampil) | accept | Kedua endpoint `[Authorize(Roles = "Admin, HC")]` (VERIFIED :110, :2859) — TIDAK diubah. Admin/HC memang berhak melihat SEMUA sesi; window murni perf/UX, BUKAN kontrol akses (selaras T-m9r-01 quick task). Tidak ada PII baru yang ter-expose ke pihak tak berhak. |
| T-370-02 | Denial of Service | AssessmentMonitoring (tanpa window → full-table load + in-memory grouping, tanpa pagination) | accept | Skala saat ini 58 row lokal; endpoint admin-only low-volume; `.AsNoTracking()` (D-05) kurangi overhead tracking; Tab Assessment tetap punya pagination in-memory. Pagination Monitoring = deferred (catat untuk fase perf bila row membengkak — CONTEXT.md Deferred Ideas). |
| T-370-03 | Tampering | Jalur search `.Where(Title/Category Contains)` | mitigate (existing) | EF Core memparameterisasi otomatis (parameterized SQL) — jalur search TIDAK diubah fase ini; tetap aman dari SQL injection. |
</threat_model>

<verification>
- `dotnet build` → 0 error (Build succeeded).
- `dotnet test` → Failed: 0, Total: 226 (turun 3 dari 229 karena hapus 3 [Fact]).
- Grep-guard: zero hit `ApplySevenDayWindow|sevenDaysAgo` di `Controllers/ Views/ wwwroot/ tests/ HcPortal.Tests/` (abaikan `.planning/` docs historis — Pitfall 3).
- Grep-guard komentar stale: zero hit `7-day|260611-m9r|90-review` di `Controllers/AssessmentAdminController.cs`.
- `Test-Path HcPortal.Tests/AssessmentSearchWindowTests.cs` → False.
- UAT @5277 (Task 2): 5 langkah PASS (default view Tab+Monitoring, filter status CIL-02 tetap, search no-regresi, pagination jalan).
</verification>

<success_criteria>
1. **SC1:** Tab Assessment + Monitoring tanpa search menampilkan sesi lama >7 hari (window hilang); filter status default "Aktif" + hide-Closed CIL-02 TETAP berlaku.
2. **SC2:** Behavior search 260611-m9r tidak regresi (search tetap menjangkau semua sesi).
3. **SC3:** Trade-off tercatat & sudah diterima user 2026-06-11 (sesi terbengkalai lama ikut tampil; perf aman skala 58 row).
4. **SC4:** `dotnet build` 0 error + full suite hijau (226) + UAT @5277 (default view + search + pagination).
</success_criteria>

<output>
After completion, create `.planning/phases/370-hapus-window-7-hari-tampilan-default-tanpa-batas/370-01-SUMMARY.md`.
</output>
