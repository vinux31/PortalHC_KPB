---
phase: 372-data-foundation-propagasi-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentSession.cs
  - Data/ApplicationDbContext.cs
  - Migrations/*_AddShuffleTogglesToAssessmentSession.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - HcPortal.Tests/ShuffleMigrationTests.cs
  - HcPortal.Tests/ShuffleCreatePersistenceTests.cs
  - HcPortal.Tests/ShufflePropagationTests.cs
autonomous: false
requirements: [SHUF-01]
user_setup: []

must_haves:
  truths:
    - "Setelah migration, baris AssessmentSessions LAMA punya ShuffleQuestions=true dan ShuffleOptions=true (data lama tak berubah)."
    - "Entity AssessmentSession punya 2 properti bool baru dengan default = true (anti EF bool-trap di level object-init)."
    - "Migration AddShuffleTogglesToAssessmentSession ter-apply ke DB lokal; kolom queryable via EF."
  artifacts:
    - path: "Models/AssessmentSession.cs"
      provides: "Entity props ShuffleQuestions/ShuffleOptions (bool, = true)"
      contains: "public bool ShuffleQuestions { get; set; } = true;"
    - path: "Data/ApplicationDbContext.cs"
      provides: "Fluent HasDefaultValue(true) untuk kedua kolom"
      contains: "ShuffleQuestions).HasDefaultValue(true)"
    - path: "Migrations/*_AddShuffleTogglesToAssessmentSession.cs"
      provides: "AddColumn bit defaultValue:true x2 + DropColumn x2"
      contains: "defaultValue: true"
    - path: "HcPortal.Tests/ShuffleMigrationTests.cs"
      provides: "Real-SQL test backfill baris lama → true (SHUF-01)"
      contains: "ShuffleQuestions"
    - path: "HcPortal.Tests/ShuffleCreatePersistenceTests.cs"
      provides: "Wave-0 stub SHUF-02 (diisi Plan 02)"
    - path: "HcPortal.Tests/ShufflePropagationTests.cs"
      provides: "Wave-0 stub SHUF-03 (diisi Plan 02)"
  key_links:
    - from: "Models/AssessmentSession.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "entity.Property(a => a.ShuffleQuestions).HasDefaultValue(true)"
      pattern: "ShuffleQuestions\\).HasDefaultValue\\(true\\)"
    - from: "Migrations/*_AddShuffleTogglesToAssessmentSession.cs"
      to: "AssessmentSessions table"
      via: "AddColumn bit defaultValue:true"
      pattern: "AddColumn<bool>"
---

<objective>
Pondasi data Shuffle Toggle: tambah 2 kolom bool `ShuffleQuestions` + `ShuffleOptions` ke entity `AssessmentSession`, Fluent `HasDefaultValue(true)`, generate + apply migration `AddShuffleTogglesToAssessmentSession` (`bit NOT NULL DEFAULT 1` → baris LAMA otomatis ON), dan scaffold 3 file test Wave 0 (real-SQL fixture). Plan ini juga membuktikan SHUF-01 (backfill baris lama → true) lewat test integration real-SQL.

Purpose: Tanpa kolom + migration ter-apply, tidak ada Plan 02/03 yang bisa jalan. Migration adalah gerbang [BLOCKING] untuk seluruh phase.
Output: Entity props + Fluent + migration file + snapshot ter-update + DB lokal ter-migrate + 1 test SHUF-01 hijau + 2 stub test (SHUF-02/03 diisi Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-CONTEXT.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-RESEARCH.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-PATTERNS.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-VALIDATION.md

<interfaces>
<!-- Triplet analog AllowAnswerReview — copy verbatim, ganti nama simbol. Verified live 2026-06-13. -->

From Models/AssessmentSession.cs (lines 28-36, verbatim live):
```csharp
[Range(0, 100)]
[Display(Name = "Pass Percentage (%)")]
public int PassPercentage { get; set; } = 70;

[Display(Name = "Allow Answer Review")]
public bool AllowAnswerReview { get; set; } = true;

[Display(Name = "Terbitkan Sertifikat")]
public bool GenerateCertificate { get; set; } = false;
```

From Data/ApplicationDbContext.cs (lines 213-216, verbatim live, di dalam builder.Entity<AssessmentSession>):
```csharp
// Default values
entity.Property(a => a.PassPercentage).HasDefaultValue(70);
entity.Property(a => a.AllowAnswerReview).HasDefaultValue(true);
entity.Property(a => a.CreatedAt).HasDefaultValueSql("GETUTCDATE()");
```

From Migrations/20260214011828_AddAssessmentResultFields.cs (lines 14-19, AddColumn bool defaultValue:true shape):
```csharp
migrationBuilder.AddColumn<bool>(
    name: "AllowAnswerReview",
    table: "AssessmentSessions",
    type: "bit",
    nullable: false,
    defaultValue: true);
```

Real-SQL fixture pattern (HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs:24-66, verified):
- `IAsyncLifetime` fixture, `DbName = $"HcPortalDB_Test_{Guid.NewGuid():N}"`
- connstr: `Server=localhost\\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30`
- `InitializeAsync`: `await ctx.Database.MigrateAsync();` (jalankan PIPELINE, bukan schema-from-model) + try/catch EnsureDeletedAsync on mid-migration fail
- `DisposeAsync`: `await ctx.Database.EnsureDeletedAsync();`
- class test pakai `[Trait("Category","Integration")]` + `IClassFixture<...>`
- REUSE ALT: `ProtonCompletionFixture` (HcPortal.Tests/ProtonCompletionServiceTests.cs:25-89) juga disposable real-SQL `HcPortalDB_Test_<guid>` + MigrateAsync — boleh dipakai ulang jika butuh seed AssessmentSession.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tambah 2 entity props + Fluent HasDefaultValue (triplet analog AllowAnswerReview)</name>
  <read_first>
    - Models/AssessmentSession.cs (baca baris 28-36 — anchor AllowAnswerReview/GenerateCertificate)
    - Data/ApplicationDbContext.cs (baca baris 213-216 — blok "Default values" di dalam builder.Entity&lt;AssessmentSession&gt;)
    - 372-PATTERNS.md (bagian "Models/AssessmentSession.cs" + "Data/ApplicationDbContext.cs")
  </read_first>
  <files>Models/AssessmentSession.cs, Data/ApplicationDbContext.cs</files>
  <action>
  Di `Models/AssessmentSession.cs`, SETELAH baris `public bool GenerateCertificate { get; set; } = false;` (line 36), tambahkan VERBATIM (default `= true` load-bearing — anti EF bool-trap; `[Display]` Bahasa Indonesia per spec §4):
  ```csharp

  [Display(Name = "Acak Soal")]
  public bool ShuffleQuestions { get; set; } = true;

  [Display(Name = "Acak Pilihan Jawaban")]
  public bool ShuffleOptions { get; set; } = true;
  ```

  Di `Data/ApplicationDbContext.cs`, SETELAH baris `entity.Property(a => a.AllowAnswerReview).HasDefaultValue(true);` (line 215, di dalam blok "Default values" milik `builder.Entity<AssessmentSession>`), tambahkan VERBATIM:
  ```csharp
  entity.Property(a => a.ShuffleQuestions).HasDefaultValue(true);
  entity.Property(a => a.ShuffleOptions).HasDefaultValue(true);
  ```

  WAJIB tambah Fluent SEBELUM generate migration (Task 2) — supaya snapshot emit `.ValueGeneratedOnAdd().HasDefaultValue(true)` konsisten (hindari drift pola GenerateCertificate; Pitfall 3 RESEARCH).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Models/AssessmentSession.cs` contains `public bool ShuffleQuestions { get; set; } = true;` AND `public bool ShuffleOptions { get; set; } = true;`
    - `Data/ApplicationDbContext.cs` contains `ShuffleQuestions).HasDefaultValue(true)` AND `ShuffleOptions).HasDefaultValue(true)`
    - `dotnet build` exits 0 (Build succeeded)
  </acceptance_criteria>
  <done>2 props bool `= true` ada di entity + 2 Fluent `HasDefaultValue(true)` ada di DbContext; build hijau.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: Generate + apply migration AddShuffleTogglesToAssessmentSession ke DB lokal</name>
  <read_first>
    - 372-PATTERNS.md (bagian Migrations — DO NOT hand-write, CLI generate)
    - 372-RESEARCH.md (§Code Examples — EF migration workflow + verifikasi backfill SQL)
    - docs/DEV_WORKFLOW.md (SOP migration + snapshot DB lokal)
  </read_first>
  <files>Migrations/*_AddShuffleTogglesToAssessmentSession.cs, Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <action>
  (BLOCKING checkpoint — butuh DB lokal + SQLEXPRESS running. Snapshot DB lokal SEBELUM apply per SEED_WORKFLOW.) Generate migration via EF CLI (JANGAN hand-write — auto-emit DDL + update snapshot, hindari drift), apply ke DB lokal, lalu verifikasi backfill baris lama = 1. Langkah lengkap + bentuk DDL yang diharapkan + cara verifikasi ada di blok &lt;what-built&gt; dan &lt;how-to-verify&gt; di bawah. Urutan ringkas:
  ```
  dotnet ef migrations add AddShuffleTogglesToAssessmentSession --context ApplicationDbContext
  dotnet ef database update --context ApplicationDbContext
  dotnet build
  ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String "Build succeeded|error"</automated>
  </verify>
  <what-built>
  Task 1 sudah menambah entity props + Fluent. Sekarang generate migration via EF CLI (auto-emit DDL + update snapshot — JANGAN hand-write) lalu apply ke DB lokal. Per CLAUDE.md SEED/Develop Workflow: snapshot DB lokal SEBELUM apply (destructive DDL pada DB shared `HcPortalDB_Dev`).

  Jalankan (urut):
  ```
  # (opsional tapi disarankan) snapshot DB lokal dulu — lihat docs/SEED_WORKFLOW.md command BACKUP DATABASE
  dotnet ef migrations add AddShuffleTogglesToAssessmentSession --context ApplicationDbContext
  dotnet ef database update --context ApplicationDbContext
  dotnet build
  ```

  Migration `Up` HARUS berisi (auto-generated; verifikasi bentuknya sesuai analog):
  ```csharp
  migrationBuilder.AddColumn<bool>(
      name: "ShuffleQuestions", table: "AssessmentSessions", type: "bit", nullable: false, defaultValue: true);
  migrationBuilder.AddColumn<bool>(
      name: "ShuffleOptions", table: "AssessmentSessions", type: "bit", nullable: false, defaultValue: true);
  ```
  `Down` HARUS berisi `DropColumn` untuk kedua kolom.
  </what-built>
  <how-to-verify>
  1. File `Migrations/*_AddShuffleTogglesToAssessmentSession.cs` ada; `Up` mengandung `defaultValue: true` DUA kali; `Down` mengandung 2 `DropColumn`.
  2. `ApplicationDbContextModelSnapshot.cs` ter-update: mengandung `ShuffleQuestions` + `ShuffleOptions` dengan `.ValueGeneratedOnAdd()` + `.HasDefaultValue(true)` (pola AllowAnswerReview, BUKAN drift GenerateCertificate).
  3. DB lokal ter-apply: query SQL
     ```sql
     SELECT TOP 5 Id, ShuffleQuestions, ShuffleOptions FROM AssessmentSessions ORDER BY Id;
     ```
     → semua baris LAMA = `1` (true) kedua kolom.
  4. `dotnet build` exits 0.
  </how-to-verify>
  <resume-signal>Ketik "applied" setelah migration ter-generate + ter-apply ke DB lokal + baris lama terverifikasi = 1; atau laporkan error (mis. SQLEXPRESS tidak running, snapshot drift).</resume-signal>
  <acceptance_criteria>
    - `Migrations/` contains a file matching `*AddShuffleTogglesToAssessmentSession.cs`
    - migration file contains `defaultValue: true` exactly twice (in Up)
    - migration file contains `DropColumn` for both ShuffleQuestions and ShuffleOptions (in Down)
    - `Migrations/ApplicationDbContextModelSnapshot.cs` contains `ShuffleQuestions` and `ShuffleOptions`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Migration ter-generate via CLI (snapshot ter-update), ter-apply ke DB lokal, baris lama = 1 dua-duanya, build hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold 3 file test Wave 0 + isi SHUF-01 (migration backfill real-SQL hijau)</name>
  <read_first>
    - HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs (fixture real-SQL lengkap — pola disposable HcPortalDB_Test_&lt;guid&gt; + MigrateAsync)
    - HcPortal.Tests/ProtonCompletionServiceTests.cs:25-89 (alt fixture reusable)
    - 372-RESEARCH.md (§Validation Architecture — Test→Map + Wave 0 Gaps + contoh ShuffleMigrationTests)
    - 372-VALIDATION.md (Per-Task Verification Map — isi baris SHUF-01)
  </read_first>
  <files>HcPortal.Tests/ShuffleMigrationTests.cs, HcPortal.Tests/ShuffleCreatePersistenceTests.cs, HcPortal.Tests/ShufflePropagationTests.cs</files>
  <behavior>
    - Test SHUF-01a (real-SQL): baris AssessmentSession yang di-insert via raw SQL TANPA menyebut kolom shuffle (simulasi baris lama) → setelah migration, `ShuffleQuestions == true` DAN `ShuffleOptions == true`.
    - Test SHUF-01b (real-SQL smoke): kolom shuffle ter-map + queryable via EF setelah `MigrateAsync` (insert baris baru via EF dengan flag eksplisit → round-trip baca balik benar).
    - Stub SHUF-02 (ShuffleCreatePersistenceTests.cs): kelas + fixture wiring + `[Fact(Skip = "Wave 0 stub — diisi Plan 02")]` placeholder (BELUM implementasi; Plan 02 mengisi).
    - Stub SHUF-03 (ShufflePropagationTests.cs): idem stub skipped.
  </behavior>
  <action>
  Buat 3 file test di `HcPortal.Tests/`:

  1. **ShuffleMigrationTests.cs** — fixture real-SQL (boleh REUSE `ProtonCompletionFixture` via `IClassFixture<ProtonCompletionFixture>` JIKA fixture itu sudah MigrateAsync disposable DB; kalau lebih jelas, buat `ShuffleMigrationFixture` baru meniru `OrgLabelMigrationFixture` 1:1 — disposable `HcPortalDB_Test_<guid>`, connstr localhost\SQLEXPRESS, `MigrateAsync()` di InitializeAsync, `EnsureDeletedAsync()` di DisposeAsync, try/catch mid-migration). Kelas `[Trait("Category","Integration")]`.
     - `[Fact] Migration_BackfillsOldRows_ToTrue`: insert baris AssessmentSession via `ExecuteSqlRawAsync` TANPA kolom shuffle (hanya kolom NOT NULL minimal: UserId, Title, Category, Schedule, DurationMinutes, Status, dst — sesuaikan dengan kolom NOT NULL aktual; periksa skema bila perlu), lalu baca baris via EF `AssessmentSessions.OrderByDescending(s=>s.Id).FirstAsync()` → `Assert.True(row.ShuffleQuestions)` + `Assert.True(row.ShuffleOptions)`.
     - `[Fact] Migration_ColumnsQueryable_RoundTrip`: tambah AssessmentSession via EF dengan `ShuffleQuestions=false, ShuffleOptions=true` → SaveChanges → baca balik → assert nilai cocok (buktikan kolom benar-benar persisted, bukan default).
  2. **ShuffleCreatePersistenceTests.cs** — kelas + `[Trait("Category","Integration")]` + `[Fact(Skip = "Wave 0 stub — implementasi di Plan 02 (SHUF-02)")] public void Stub_ShuffleCreate() {}`.
  3. **ShufflePropagationTests.cs** — idem stub `[Fact(Skip = "Wave 0 stub — implementasi di Plan 02 (SHUF-03)")]`.

  Catatan: kalau raw INSERT butuh banyak kolom NOT NULL, lebih simpel pakai EF untuk insert baris "lama" TAPI itu TIDAK menguji backfill (EF kirim nilai C#). Untuk membuktikan backfill DEFAULT yang sesungguhnya, insert HARUS lewat raw SQL yang OMIT kolom shuffle. Jika kolom NOT NULL lain menyulitkan, gunakan `INSERT ... DEFAULT VALUES` tidak berlaku — sebut kolom wajib lain secara eksplisit dan biarkan KEDUA kolom shuffle tak disebut sehingga DB DEFAULT 1 mengisinya. Itu inti bukti SHUF-01.

  Setelah selesai, isi baris SHUF-01 di `372-VALIDATION.md` Per-Task Verification Map (Task ID = 372-01-T3, Plan 01, Wave 1, File Exists = ✅, Status = ✅ green).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ShuffleMigration" 2>&1 | Select-String "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/ShuffleMigrationTests.cs` exists and contains `ShuffleQuestions` and `Assert.True`
    - `HcPortal.Tests/ShuffleCreatePersistenceTests.cs` exists (stub)
    - `HcPortal.Tests/ShufflePropagationTests.cs` exists (stub)
    - `dotnet test --filter "FullyQualifiedName~ShuffleMigration"` exits 0 (Passed!)
    - `372-VALIDATION.md` SHUF-01 row marked ✅ green
  </acceptance_criteria>
  <done>3 file test ada; SHUF-01 migration-backfill test hijau real-SQL; 2 stub SHUF-02/03 skipped (placeholder Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin browser → POST CreateAssessment/EditAssessment | Input form melintasi sini; sudah `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`. Plan 01 hanya menambah kolom + migration, TIDAK menambah endpoint. |
| App → DB lokal (migration DDL) | Migration DDL statis (tidak ada input user); apply hanya ke DB lokal (Dev/Prod = tugas IT). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-372-01 | Tampering | Migration DDL `AddColumn defaultValue:true` | accept | DDL statis, tanpa input user; `nullable:false` + DEFAULT 1 menjamin tak ada NULL/garbage. Tidak ada attack surface. |
| T-372-02 | Information Disclosure | 2 kolom bool baru di AssessmentSessions | accept | Nilai bool (true/false) bukan data sensitif/PII; tidak mengubah data exposure existing. |
| T-372-03 | Denial of Service | Migration apply pada tabel besar | accept | `ADD COLUMN ... DEFAULT` di SQL Server = metadata-only untuk NOT NULL DEFAULT constant (cepat); skala lokal 58 baris. Dev/Prod = tugas IT (DEV_WORKFLOW). |

block_on: high. Semua threat low/accept — tidak ada yang memblokir. Grading TIDAK terpengaruh (spec §13: `PackageOption.Id`-based; Plan 01 cuma menambah kolom, tak sentuh grading).
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "FullyQualifiedName~ShuffleMigration"` hijau (SHUF-01 real-SQL backfill).
- DB lokal: `SELECT TOP 5 Id, ShuffleQuestions, ShuffleOptions FROM AssessmentSessions ORDER BY Id;` → semua baris lama = 1.
- Snapshot `ApplicationDbContextModelSnapshot.cs` mengandung kedua kolom + `HasDefaultValue(true)`.
</verification>

<success_criteria>
- SHUF-01 terpenuhi: migration jalan, baris lama kedua flag `true`, terbukti via test real-SQL + query DB lokal.
- Entity props + Fluent + migration + snapshot lengkap (triplet analog AllowAnswerReview, no drift).
- 3 file test Wave 0 ada (1 hijau SHUF-01, 2 stub untuk Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/372-data-foundation-propagasi-toggle/372-01-SUMMARY.md`.
Catat: commit hash + flag migration `AddShuffleTogglesToAssessmentSession` untuk notifikasi IT (DEV_WORKFLOW step 5; bundle dengan carry-over IT existing).
</output>
