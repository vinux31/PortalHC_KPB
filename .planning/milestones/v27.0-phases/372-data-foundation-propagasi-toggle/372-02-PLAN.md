---
phase: 372-data-foundation-propagasi-toggle
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/ShuffleCreatePersistenceTests.cs
  - HcPortal.Tests/ShufflePropagationTests.cs
autonomous: true
requirements: [SHUF-02, SHUF-03]
user_setup: []

must_haves:
  truths:
    - "Assessment baru via form persist flag sesuai centang (default ON), tidak kena EF bool-false trap — di SEMUA jalur create (standard/Pre/Post)."
    - "Toggle OFF dari form → flag tersimpan false (eksplisit), bukan dipaksa true."
    - "Ubah toggle di EditAssessment → SEMUA sibling grup ikut (standard branch + Pre-Post branch)."
    - "Peserta yang ditambah saat Edit (add-participant) dapat nilai flag grup, bukan OFF (cegah EF bool-trap kedua)."
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Set ShuffleQuestions/ShuffleOptions di 6 new AssessmentSession + 2 foreach"
      contains: "ShuffleQuestions = model.ShuffleQuestions"
    - path: "HcPortal.Tests/ShuffleCreatePersistenceTests.cs"
      provides: "Test SHUF-02 (ON default + OFF eksplisit persist, real-SQL)"
      contains: "ShuffleQuestions"
    - path: "HcPortal.Tests/ShufflePropagationTests.cs"
      provides: "Test SHUF-03 (sibling propagation standard + Pre-Post, real-SQL)"
      contains: "ShuffleOptions"
  key_links:
    - from: "POST CreateAssessment create loops"
      to: "AssessmentSessions rows"
      via: "ShuffleQuestions = model.ShuffleQuestions di setiap new AssessmentSession"
      pattern: "ShuffleQuestions = (model|savedAssessment)\\.ShuffleQuestions"
    - from: "POST EditAssessment foreach siblings"
      to: "all sibling AssessmentSessions"
      via: "sibling/s.ShuffleQuestions = model.ShuffleQuestions"
      pattern: "(sibling|s)\\.ShuffleQuestions = model\\.ShuffleQuestions"
---

<objective>
Set flag shuffle eksplisit dari form di SEMUA write-site `new AssessmentSession` (6 yang membangun baris dari path Create/Edit form) + propagate ke sibling di 2 `foreach` EditAssessment. Cegah EF bool-false trap (SHUF-02) dan propagate perubahan ke seluruh grup sibling (SHUF-03), termasuk jalur tambah-peserta-saat-Edit yang spec hilangkan (CONFIRMED IN-SCOPE — cegah EF bool-trap kedua).

Purpose: Tanpa explicit-set, assessment baru tersimpan OFF meski toggle ON (migration `defaultValue` hanya backfill baris lama). Tanpa propagasi, ubah toggle tak menyentuh sibling.
Output: 6 write-site + 2 foreach ter-set + 2 test real-SQL (SHUF-02 ON/OFF persist, SHUF-03 propagation) hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-CONTEXT.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-RESEARCH.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-PATTERNS.md

<interfaces>
<!-- 6 write-site + 2 foreach. Line numbers verified live 2026-06-13 (367/368 sudah ship; AllowAnswerReview anchor lines: 1227,1261,1438,1802,1904,1923,2027,2122). EXECUTOR WAJIB RE-GREP saat execute-time — JANGAN percaya angka baris buta. -->

POST signatures (auth inherited, NO new endpoint):
- `CreateAssessment(AssessmentSession model, ...)` — :846, `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` :844-845
- `EditAssessment(int id, AssessmentSession model, ...)` — :1766, auth :1764-1765

7 `new AssessmentSession` sites (re-grep `new AssessmentSession`):
| # | ~Line | Site | Source value | Tindakan |
|---|------|------|--------------|----------|
| 1 | 684  | GET model init | entity default = true | SKIP — entity default cukup (render checked). |
| 2 | 1218 | Pre create loop `preSession` | model.* (D-04) | TAMBAH ShuffleQuestions/Options = model.* |
| 3 | 1252 | Post create loop `postSession` | model.* (D-04) | TAMBAH idem |
| 4 | 1427 | Standard create loop `session` | model.* | TAMBAH idem |
| 5 | 1895 | Add-participant Edit `newPre` | model.* (ikuti AllowAnswerReview = model.* :1904) | TAMBAH idem |
| 6 | 1914 | Add-participant Edit `newPost` | model.* (ikuti :1923) | TAMBAH idem |
| 7 | 2111 | Add-participant Edit `newSessions` | **savedAssessment.*** (ikuti :2122) | TAMBAH ShuffleQuestions = savedAssessment.ShuffleQuestions, dst. |

2 propagation foreach (re-grep `AllowAnswerReview = model.AllowAnswerReview`):
| F | ~Line | Branch | Tindakan |
|---|------|--------|----------|
| F1 | 1797-1806 | Pre-Post `foreach (var s in allGroupSessions)` (anchor :1802) | TAMBAH s.ShuffleQuestions = model.ShuffleQuestions; s.ShuffleOptions = model.ShuffleOptions; |
| F2 | 2016-2031 | Standard `foreach (var sibling in siblings)` (anchor :2027) | TAMBAH sibling.ShuffleQuestions = model.ShuffleQuestions; sibling.ShuffleOptions = model.ShuffleOptions; |

Create-loop assignment shape (verbatim live standard loop 1435-1439):
```csharp
var session = new AssessmentSession {
    // ...
    AllowAnswerReview = model.AllowAnswerReview,   // ← tambah Shuffle* tepat setelah ini
    GenerateCertificate = model.GenerateCertificate,
};
```
Site 2111 source-from-savedAssessment shape (verbatim 2121-2123):
```csharp
AllowAnswerReview = savedAssessment.AllowAnswerReview,   // ← tambah ShuffleQuestions = savedAssessment.ShuffleQuestions, dst
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Set ShuffleQuestions/ShuffleOptions di 6 write-site + 2 foreach propagation</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs — RE-GREP DULU `new AssessmentSession` (dapatkan 7 baris aktual) DAN `AllowAnswerReview = model.AllowAnswerReview` + `AllowAnswerReview = savedAssessment.AllowAnswerReview` (dapatkan 8 anchor baris aktual). Baca tiap site sebelum edit. Line 1200-2130 BISA bergeser — jangan percaya angka literal.
    - 372-PATTERNS.md (bagian Controllers — tabel 7 site + 2 foreach + shape verbatim)
    - 372-RESEARCH.md (§Common Pitfalls — Pitfall 1 EF bool-trap, Pitfall 2 lupa write-site)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
  Strategi: di SETIAP tempat yang sudah ada `AllowAnswerReview = model.AllowAnswerReview` (object-init create + foreach) → tambahkan tepat setelahnya `ShuffleQuestions = model.ShuffleQuestions` + `ShuffleOptions = model.ShuffleOptions` (atau `;`-statement untuk foreach). Di tempat dengan `AllowAnswerReview = savedAssessment.AllowAnswerReview` (site 2111) → pakai `savedAssessment.*`. Gunakan AllowAnswerReview sebagai jangkar 1:1 — jangan tambah/kurang site.

  **6 object-init sites** (Pre :1218, Post :1252, standard :1427, newPre :1895, newPost :1914 → `model.*`; newSessions :2111 → `savedAssessment.*`):
  ```csharp
  // setelah AllowAnswerReview = model.AllowAnswerReview, (5 site model)
  ShuffleQuestions = model.ShuffleQuestions,
  ShuffleOptions = model.ShuffleOptions,
  ```
  ```csharp
  // setelah AllowAnswerReview = savedAssessment.AllowAnswerReview, (site 2111)
  ShuffleQuestions = savedAssessment.ShuffleQuestions,
  ShuffleOptions = savedAssessment.ShuffleOptions,
  ```

  **2 foreach** (Pre-Post `s` :1802, standard `sibling` :2027) — setelah baris `.AllowAnswerReview = model.AllowAnswerReview;`:
  ```csharp
  s.ShuffleQuestions = model.ShuffleQuestions;
  s.ShuffleOptions = model.ShuffleOptions;
  ```
  ```csharp
  sibling.ShuffleQuestions = model.ShuffleQuestions;
  sibling.ShuffleOptions = model.ShuffleOptions;
  ```

  **JANGAN** ubah GET init :684 (entity default `= true` cukup; Open Q#2 RESEARCH = skip). Total = 6 object-init + 2 foreach = 8 lokasi diubah.

  Self-check: jumlah `ShuffleQuestions =` (semua varian) di file HARUS >= 8 (6 object-init `,` + 2 foreach `;`), plus 0 di GET init.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String "Build succeeded|error"; (Select-String -Path "Controllers/AssessmentAdminController.cs" -Pattern "ShuffleQuestions = (model|savedAssessment)\.ShuffleQuestions").Count</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ShuffleQuestions = " Controllers/AssessmentAdminController.cs` >= 8 (6 object-init + 2 foreach)
    - `Controllers/AssessmentAdminController.cs` contains `ShuffleQuestions = savedAssessment.ShuffleQuestions` (site 2111)
    - `Controllers/AssessmentAdminController.cs` contains `sibling.ShuffleQuestions = model.ShuffleQuestions` (F2 standard)
    - `Controllers/AssessmentAdminController.cs` contains `s.ShuffleQuestions = model.ShuffleQuestions` (F1 Pre-Post)
    - count `ShuffleOptions` mirror count `ShuffleQuestions` (setiap site set DUA kolom)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>6 object-init + 2 foreach set kedua flag (5 model + 1 savedAssessment); GET init tidak diubah; build hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isi test SHUF-02 (create persist ON/OFF) + SHUF-03 (sibling propagation) real-SQL</name>
  <read_first>
    - HcPortal.Tests/ShuffleCreatePersistenceTests.cs + ShufflePropagationTests.cs (stub dari Plan 01 — ganti isinya)
    - HcPortal.Tests/ProtonCompletionServiceTests.cs:25-89 (fixture real-SQL reusable + pola seed AssessmentSession)
    - 372-RESEARCH.md (§Validation Architecture — Test→Map SHUF-02/SHUF-03)
    - 372-VALIDATION.md (Per-Task Verification Map — isi baris SHUF-02 + SHUF-03)
  </read_first>
  <files>HcPortal.Tests/ShuffleCreatePersistenceTests.cs, HcPortal.Tests/ShufflePropagationTests.cs</files>
  <behavior>
    - SHUF-02a (ON default persist): bangun AssessmentSession lewat jalur create (atau replikasi assignment object-init create-loop: `ShuffleQuestions = model.ShuffleQuestions` dengan model ON) → SaveChanges → baca balik → `Assert.True` kedua flag.
    - SHUF-02b (OFF eksplisit persist): model dengan `ShuffleQuestions=false, ShuffleOptions=false` → setelah save → `Assert.False` kedua (buktikan TIDAK dipaksa true; bukan EF bool-trap, bukan default override).
    - SHUF-02c (independen D-10): `ShuffleQuestions=true, ShuffleOptions=false` round-trip → assert beda nilai tetap (true/false).
    - SHUF-03a (standard propagation): grup sibling >=2 baris (key Title/Category/Schedule.Date sama) dengan flag awal true; jalankan logika propagate (replikasi foreach: `sibling.ShuffleQuestions = model.ShuffleQuestions` dengan model.ShuffleQuestions=false) → assert SEMUA sibling jadi false.
    - SHUF-03b (Pre-Post propagation): grup Pre+Post; propagate flag → assert seluruh `allGroupSessions` ikut nilai model.
  </behavior>
  <action>
  Ganti 2 stub Plan 01 dengan implementasi real-SQL (`[Trait("Category","Integration")]`, fixture disposable `ProtonCompletionFixture` atau `ShuffleMigrationFixture`).

  Pendekatan testing: karena POST action besar + butuh banyak dependency, test pada LEVEL DATA/persistence — seed AssessmentSession langsung ke DB via EF, jalankan transformasi yang IDENTIK dengan controller (set flag dari "model" → save → baca balik), assert hasil. Ini menguji invariant data (persist ON/OFF + propagate ke semua sibling) tanpa men-instansiasi controller penuh. JIKA proyek punya pola test controller-level yang sudah dipakai (cek `ManageAssessmentMedFixTests`/`BadgeRecomputeTests`), boleh ikut pola itu untuk memanggil action langsung. Pilih yang paling sesuai pola existing.

  - **ShuffleCreatePersistenceTests.cs**: 3 `[Fact]` (SHUF-02a/b/c) — seed + persist + round-trip assert ON-default, OFF-eksplisit, independen.
  - **ShufflePropagationTests.cs**: 2 `[Fact]` (SHUF-03a/b) — seed grup sibling (standard) + grup Pre-Post, terapkan propagate-foreach, assert SEMUA baris ikut nilai model.

  Setelah hijau, isi baris SHUF-02 + SHUF-03 di `372-VALIDATION.md` (Task ID 372-02-T2, Plan 02, Wave 2, File Exists ✅, Status ✅ green) dan set frontmatter `nyquist_compliant: true` + `wave_0_complete: true` bila ketiga REQ sudah punya test hijau.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ShuffleCreate|FullyQualifiedName~ShufflePropagation" 2>&1 | Select-String "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/ShuffleCreatePersistenceTests.cs` contains 3 `[Fact]` (no `Skip`) asserting ON default, OFF explicit, independen
    - `HcPortal.Tests/ShufflePropagationTests.cs` contains 2 `[Fact]` (no `Skip`) asserting standard + Pre-Post propagation
    - `dotnet test --filter "FullyQualifiedName~ShuffleCreate|FullyQualifiedName~ShufflePropagation"` exits 0 (Passed!)
    - `372-VALIDATION.md` SHUF-02 + SHUF-03 rows marked ✅ green
  </acceptance_criteria>
  <done>SHUF-02 (ON/OFF/independen) + SHUF-03 (standard+Pre-Post propagation) test real-SQL hijau; VALIDATION map terisi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin browser → POST CreateAssessment/EditAssessment | Form input (termasuk 2 bool baru) melintasi sini; sudah `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`. Plan 02 menumpang form existing, TIDAK menambah endpoint. |
| Edit propagation → sibling rows | `foreach` set flag di seluruh sibling grup; scope ditentukan oleh key grup `(Title, Category, Schedule.Date)` + authorization existing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-372-04 | Tampering (over-posting) | Bind `AssessmentSession model` langsung di POST (mass-assignment) | mitigate | Plan 02 hanya meng-EXPOSE 2 bool baru ke form; field non-form di-set server-side. Tidak memperluas surface over-posting (cuma 2 bool yang memang editable). Authorization + AntiForgery existing tetap memagari. |
| T-372-05 | Elevation of Privilege | User non-Admin/HC flip flag assessment | accept (mitigated by existing) | `[Authorize(Roles="Admin, HC")]` pada kedua POST sudah memblokir non-Admin/HC. Plan 02 tidak melonggarkan. |
| T-372-06 | Tampering (cross-group) | Propagasi `foreach` menyentuh sibling assessment lain | accept | Sibling di-fetch berdasarkan key grup yang sama (Title/Category/Schedule.Date) milik assessment yang sedang diedit oleh Admin/HC terotorisasi — tidak ada jalur menyentuh grup di luar scope edit. Pola identik dengan PassPercentage/AllowAnswerReview existing. |

block_on: high. Semua threat low/accept atau ter-mitigasi oleh kontrol existing — tidak ada yang memblokir. Grading TIDAK terpengaruh (spec §13: `PackageOption.Id`-based; Plan 02 tak sentuh grading).
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "FullyQualifiedName~Shuffle"` hijau (SHUF-01 dari Plan 01 + SHUF-02 + SHUF-03).
- `grep -c "ShuffleQuestions = " Controllers/AssessmentAdminController.cs` >= 8; GET init :684 tidak ter-set.
- Full suite `dotnet test` hijau (no regresi) sebelum wave merge.
</verification>

<success_criteria>
- SHUF-02 terpenuhi: assessment baru via SEMUA jalur create persist sesuai centang (ON default / OFF eksplisit), tidak kena EF bool-trap; jalur tambah-peserta-saat-Edit juga aman.
- SHUF-03 terpenuhi: ubah toggle EditAssessment → semua sibling grup ikut (standard + Pre-Post branch).
- 6 write-site + 2 foreach ter-set (5 model + 1 savedAssessment); GET init skip.
</success_criteria>

<output>
After completion, create `.planning/phases/372-data-foundation-propagasi-toggle/372-02-SUMMARY.md`.
</output>
