---
phase: 372-data-foundation-propagasi-toggle
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Views/Admin/CreateAssessment.cshtml
autonomous: false
requirements: [SHUF-02]
user_setup: []

must_haves:
  truths:
    - "Wizard Langkah 3 Grup B menampilkan 2 toggle form-switch 'Acak Soal' + 'Acak Pilihan Jawaban' (default checked) dengan help-text edukatif Bahasa Indonesia."
    - "Toggle default ON datang dari model (entity = true) → asp-for render checked, bukan atribut hardcoded."
    - "Langkah 4 summary menampilkan status ON/OFF kedua toggle di KEDUA blok (standard + Pre-Post), terisi oleh populateSummary()."
  artifacts:
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "2 form-switch toggle (Grup B) + 2 baris summary x2 blok + populateSummary mirror x2 cabang"
      contains: "asp-for=\"ShuffleQuestions\""
  key_links:
    - from: "CreateAssessment.cshtml toggle inputs"
      to: "AssessmentSession entity props (model bind)"
      via: "asp-for=\"ShuffleQuestions\" / asp-for=\"ShuffleOptions\""
      pattern: "asp-for=\"Shuffle(Questions|Options)\""
    - from: "populateSummary() JS"
      to: "summary span elements"
      via: ".checked → 'Aktif (ON)'/'Nonaktif (OFF)'"
      pattern: "summary-shuffle-questions"
---

<objective>
Tambah UI toggle Shuffle ke wizard `CreateAssessment.cshtml`: (1) 2 toggle `form-switch` "Acak Soal" + "Acak Pilihan Jawaban" di Langkah 3 Grup B "Pengaturan Ujian" (default checked dari model, help-text edukatif Bahasa Indonesia VERBATIM dari UI-SPEC), (2) 2 baris status di summary Langkah 4 di KEDUA blok (standard + Pre-Post), (3) `populateSummary()` mirror status ON/OFF di KEDUA cabang JS.

Purpose: HC bisa set + review shuffle saat membuat assessment. Copy mendidik HC non-teknis (D-02). Bind ke entity props yang ditambah Plan 01 (no ViewModel).
Output: Toggle + summary + JS lengkap; build hijau; UAT smoke lokal (default ON + summary tampil).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/372-data-foundation-propagasi-toggle/372-CONTEXT.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-UI-SPEC.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-PATTERNS.md

<interfaces>
<!-- Anchor view verified live 2026-06-13. Bind target = entity AssessmentSession (NO ViewModel). asp-for="ShuffleQuestions" resolve ke prop entity (Plan 01). -->

Toggle analog (CreateAssessment.cshtml:500-519, blok Token "col-md-6"):
```html
<div class="col-md-6">
    <label class="form-label fw-bold">
        <i class="bi bi-shield-lock text-primary me-1"></i>Security Token
    </label>
    <div class="form-check form-switch mb-2">
        <input class="form-check-input" type="checkbox" asp-for="IsTokenRequired" id="IsTokenRequired" />
        <label class="form-check-label" for="IsTokenRequired">Wajib token untuk memulai ujian</label>
    </div>
    ...
</div>
```
Tempat sisip toggle baru: `div.col-md-6` BARU SETELAH kolom Token (setelah line 519, di dalam `div.row.g-3` Grup B yang dibuka :478, ditutup :520). Grup B = `card mb-4` :473-522 (header "Pengaturan Ujian" :475). TANPA card/grup baru (D-01).

Summary standard block (`summary-standard-settings` :652-661, `dl.row` :653) — sisip 2 baris setelah "Pass Percentage" (:659):
```html
<dt class="col-sm-4">Pass Percentage</dt><dd class="col-sm-8" id="summary-pass"></dd>
```
Summary Pre-Post block (`summary-ppt-settings` :664-698, `dl.row` :691) — sisip 2 baris setelah "Pass Percentage" (:694):
```html
<dt class="col-sm-4">Pass Percentage</dt><dd class="col-sm-8" id="summary-ppt-pass"></dd>
```

populateSummary() (:1053) — DUA cabang:
- Pre-Post cabang `isPrePost` (~:1119+, anchor token :1119-1122)
- standard cabang else (~:1160+, anchor token :1160-1163):
```javascript
var tokenEl = document.getElementById('IsTokenRequired');
var summToken = document.getElementById('summary-token');
if (summToken) summToken.textContent = (tokenEl && tokenEl.checked) ? 'Ya — ...' : 'Tidak';
```
populateSummary dipanggil ulang tiap masuk Langkah 4 (:862).
</interfaces>

<ui_copy>
<!-- LOCKED VERBATIM dari 372-UI-SPEC.md §Copywriting Contract — JANGAN dipangkas. Bahasa Indonesia (mandat CLAUDE.md). -->

Sub-heading kolom (label.form-label.fw-bold + ikon bi-shuffle text-primary):
  Pengacakan Soal & Jawaban

Toggle 1 label: Acak Soal
Toggle 1 help-text (form-text text-muted), VERBATIM:
  Saat aktif, urutan dan pemilihan soal diacak berbeda untuk setiap peserta — sehingga peserta yang duduk berdekatan kecil kemungkinan mendapat soal yang sama urutannya. Saat nonaktif, semua peserta mendapat soal yang sama dengan urutan yang sama persis (urutan asli sesuai paket soal).

Toggle 2 label: Acak Pilihan Jawaban
Toggle 2 help-text (form-text text-muted), VERBATIM (frasa "jawaban benar tetap dinilai dengan benar" WAJIB ada — D-11):
  Saat aktif, urutan pilihan jawaban (A, B, C, D) diacak berbeda di setiap soal untuk tiap peserta — jawaban benar tetap dinilai dengan benar, hanya posisinya yang berpindah. Saat nonaktif, urutan pilihan jawaban tampil sama untuk semua peserta sesuai urutan yang tersimpan di sistem.

Summary baris (dt/dd): "Acak Soal" + "Acak Pilihan Jawaban"; nilai ON = "Aktif (ON)", OFF = "Nonaktif (OFF)" (teks polos, BUKAN badge hijau/merah — ON & OFF dua-duanya valid).
</ui_copy>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tambah 2 toggle form-switch di Langkah 3 Grup B + 2 baris summary x2 blok + populateSummary x2 cabang</name>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml — baca :472-522 (Grup B + Token), :650-698 (kedua blok summary), :1053-1172 (populateSummary 2 cabang). RE-GREP DULU `summary-pass` + `IsTokenRequired` + `populateSummary` bila baris bergeser.
    - 372-UI-SPEC.md (§Copywriting Contract — copy VERBATIM + §Placement contract + §Color teks polos)
    - 372-PATTERNS.md (bagian Views/Admin/CreateAssessment.cshtml — (A) toggle (B) summary (C) JS)
  </read_first>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <action>
  **(A) Toggle** — sisip `div.col-md-6` BARU setelah kolom Token (penutup `</div>` kolom Token, sebelum penutup `div.row.g-3` Grup B). Satu kolom berisi sub-heading + 2 toggle ber-stack + help-text. Default ON datang dari model (`asp-for` → checked karena prop entity `= true`) — JANGAN tulis atribut `checked` hardcoded (D-08):
  ```html
  <!-- Pengacakan Soal & Jawaban -->
  <div class="col-md-6">
      <label class="form-label fw-bold">
          <i class="bi bi-shuffle text-primary me-1"></i>Pengacakan Soal & Jawaban
      </label>
      <div class="form-check form-switch mb-2">
          <input class="form-check-input" type="checkbox" asp-for="ShuffleQuestions" id="ShuffleQuestions" />
          <label class="form-check-label" for="ShuffleQuestions">Acak Soal</label>
      </div>
      <div class="form-text text-muted mb-2">Saat aktif, urutan dan pemilihan soal diacak berbeda untuk setiap peserta — sehingga peserta yang duduk berdekatan kecil kemungkinan mendapat soal yang sama urutannya. Saat nonaktif, semua peserta mendapat soal yang sama dengan urutan yang sama persis (urutan asli sesuai paket soal).</div>
      <div class="form-check form-switch mb-2">
          <input class="form-check-input" type="checkbox" asp-for="ShuffleOptions" id="ShuffleOptions" />
          <label class="form-check-label" for="ShuffleOptions">Acak Pilihan Jawaban</label>
      </div>
      <div class="form-text text-muted">Saat aktif, urutan pilihan jawaban (A, B, C, D) diacak berbeda di setiap soal untuk tiap peserta — jawaban benar tetap dinilai dengan benar, hanya posisinya yang berpindah. Saat nonaktif, urutan pilihan jawaban tampil sama untuk semua peserta sesuai urutan yang tersimpan di sistem.</div>
  </div>
  ```

  **(B) Summary rows** — sisip 2 baris di KEDUA blok, setelah baris "Pass Percentage" (D-04: 1 pasang toggle berlaku Pre & Post; D-05: tampil di summary):
  - di `summary-standard-settings` (setelah `summary-pass` :659):
    ```html
    <dt class="col-sm-4">Acak Soal</dt><dd class="col-sm-8" id="summary-shuffle-questions"></dd>
    <dt class="col-sm-4">Acak Pilihan Jawaban</dt><dd class="col-sm-8" id="summary-shuffle-options"></dd>
    ```
  - di `summary-ppt-settings` (setelah `summary-ppt-pass` :694):
    ```html
    <dt class="col-sm-4">Acak Soal</dt><dd class="col-sm-8" id="summary-ppt-shuffle-questions"></dd>
    <dt class="col-sm-4">Acak Pilihan Jawaban</dt><dd class="col-sm-8" id="summary-ppt-shuffle-options"></dd>
    ```

  **(C) populateSummary() JS** — tambah di KEDUA cabang (teks polos, BUKAN badge):
  - di cabang standard (dekat anchor token :1160-1163):
    ```javascript
    var sqEl = document.getElementById('ShuffleQuestions');
    var summSQ = document.getElementById('summary-shuffle-questions');
    if (summSQ) summSQ.textContent = (sqEl && sqEl.checked) ? 'Aktif (ON)' : 'Nonaktif (OFF)';
    var soEl = document.getElementById('ShuffleOptions');
    var summSO = document.getElementById('summary-shuffle-options');
    if (summSO) summSO.textContent = (soEl && soEl.checked) ? 'Aktif (ON)' : 'Nonaktif (OFF)';
    ```
  - di cabang Pre-Post `isPrePost` (dekat anchor token :1119-1122) — target span `summary-ppt-shuffle-questions` / `summary-ppt-shuffle-options`, baca dari input toggle yang SAMA (`ShuffleQuestions`/`ShuffleOptions`, karena 1 pasang toggle berlaku Pre & Post — D-04):
    ```javascript
    var sqElP = document.getElementById('ShuffleQuestions');
    var summSQP = document.getElementById('summary-ppt-shuffle-questions');
    if (summSQP) summSQP.textContent = (sqElP && sqElP.checked) ? 'Aktif (ON)' : 'Nonaktif (OFF)';
    var soElP = document.getElementById('ShuffleOptions');
    var summSOP = document.getElementById('summary-ppt-shuffle-options');
    if (summSOP) summSOP.textContent = (soElP && soElP.checked) ? 'Aktif (ON)' : 'Nonaktif (OFF)';
    ```

  Copy help-text WAJIB VERBATIM (jangan pangkas, frasa "jawaban benar tetap dinilai dengan benar" wajib ada). TANPA card baru, TANPA badge berwarna, TANPA ViewModel.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/CreateAssessment.cshtml` contains `asp-for="ShuffleQuestions"` AND `asp-for="ShuffleOptions"`
    - file contains the verbatim phrase `jawaban benar tetap dinilai dengan benar`
    - file contains `bi-shuffle text-primary` AND `Pengacakan Soal &amp; Jawaban` (sub-heading)
    - file contains `id="summary-shuffle-questions"` AND `id="summary-ppt-shuffle-questions"` (2 blok summary)
    - file contains `summSQ.textContent` AND `summSQP.textContent` (JS mirror 2 cabang)
    - NO hardcoded `checked` attribute on the shuffle inputs (default ON via model)
    - file does NOT introduce a separate ViewModel (still binds AssessmentSession entity)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>2 toggle form-switch (Grup B, default checked, help-text verbatim) + 2 baris summary di 2 blok + populateSummary mirror 2 cabang; build hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT smoke lokal — toggle default ON + summary tampil (Razor runtime)</name>
  <read_first>
    - 372-VALIDATION.md (§Manual-Only Verifications — Razor UI runtime, lesson Phase 354 grep/build tak cukup)
    - CLAUDE.md Develop Workflow (verifikasi lokal @localhost:5277; AD lokal off)
  </read_first>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <action>
  (BLOCKING checkpoint — smoke UAT manual; butuh app jalan lokal.) Razor dynamic WAJIB diverifikasi runtime (grep+build tak cukup — lesson Phase 354 RuntimeBinderException/label-toggle). Jalankan `Authentication__UseActiveDirectory=false dotnet run`, buka wizard Create Assessment @http://localhost:5277, dan verifikasi toggle default ON + help-text + summary reaktif (standard + Pre-Post) sesuai langkah di blok &lt;how-to-verify&gt;. UAT Playwright LENGKAP = scope Phase 375; ini smoke manual saja.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String "Build succeeded|error"</automated>
  </verify>
  <what-built>
  Task 1 menambah 2 toggle + summary + JS. Razor dynamic WAJIB diverifikasi runtime (grep+build tak cukup — lesson Phase 354 RuntimeBinderException/label-toggle). Jalankan app lokal:
  ```
  Authentication__UseActiveDirectory=false dotnet run
  ```
  Buka http://localhost:5277 → halaman Create Assessment (admin@pertamina.com bila perlu login).
  Catatan: UAT Playwright LENGKAP = scope Phase 375. Ini smoke manual saja.
  </what-built>
  <how-to-verify>
  1. Buka wizard Create Assessment → maju ke Langkah 3 "Pengaturan Ujian" (Grup B).
  2. Verifikasi: kolom "Pengacakan Soal & Jawaban" (ikon bi-shuffle) muncul di sebelah/bawah Token; 2 switch "Acak Soal" + "Acak Pilihan Jawaban" DEFAULT CHECKED (ON); help-text edukatif tampil penuh di bawah masing-masing (termasuk frasa "jawaban benar tetap dinilai dengan benar").
  3. Maju ke Langkah 4 Konfirmasi → verifikasi baris "Acak Soal" + "Acak Pilihan Jawaban" tampil dengan "Aktif (ON)".
  4. Kembali ke Langkah 3, matikan satu toggle (OFF), maju lagi ke Langkah 4 → status berubah ke "Nonaktif (OFF)" untuk yang dimatikan (populateSummary re-run).
  5. (Opsional) Ulang untuk mode Pre-Post Test → summary Pre-Post juga menampilkan status shuffle.
  6. Tidak ada error console / RuntimeBinderException; halaman render tanpa exception.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila toggle default ON + help-text + summary ON/OFF tampil benar di Langkah 3 & 4 (standard + Pre-Post); atau jelaskan masalah render.</resume-signal>
  <acceptance_criteria>
    - Manual: 2 toggle default ON di Langkah 3, help-text penuh tampil
    - Manual: summary Langkah 4 menampilkan "Aktif (ON)"/"Nonaktif (OFF)" reaktif terhadap perubahan toggle
    - Manual: tidak ada error runtime Razor/console
  </acceptance_criteria>
  <done>Smoke UAT lokal PASS: toggle default ON, help-text tampil, summary reaktif, no runtime error (standard + Pre-Post).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin browser ↔ Razor view (form render) | View hanya render input `asp-for` ke entity props + JS client-side mirror (read .checked → span). Tidak ada endpoint baru; nilai dikirim lewat POST form existing (auth + AntiForgery di Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-372-07 | Tampering | Client JS `populateSummary` (read-only mirror) | accept | JS hanya MEMBACA `.checked` dan menulis ke span summary (display); tidak roundtrip server, tidak menyimpan state otoritatif. Nilai otoritatif hanya dari posted form → entity (Plan 02). |
| T-372-08 | Information Disclosure | Help-text + summary copy | accept | Copy edukatif statis Bahasa Indonesia; tidak membocorkan data/PII. |
| T-372-09 | Spoofing/CSRF | Submit form dengan toggle | accept (mitigated) | Form di-submit lewat POST CreateAssessment/EditAssessment yang sudah `[ValidateAntiForgeryToken]` + `[Authorize(Roles="Admin, HC")]` (Plan 02). View tidak melonggarkan. |

block_on: high. Semua threat low/accept — view murni server-render + client mirror tanpa endpoint baru. Grading TIDAK terpengaruh (spec §13).
</threat_model>

<verification>
- `dotnet build` 0 error.
- grep view: `asp-for="ShuffleQuestions"`, `asp-for="ShuffleOptions"`, frasa "jawaban benar tetap dinilai dengan benar", `summary-shuffle-questions`, `summary-ppt-shuffle-questions`, `summSQ.textContent`, `summSQP.textContent` semua ada.
- UAT smoke lokal @5277: toggle default ON + summary reaktif (standard + Pre-Post), no runtime error.
</verification>

<success_criteria>
- SHUF-02 (sisi UI) terpenuhi: wizard menampilkan 2 toggle default ON + help-text edukatif verbatim; summary Langkah 4 tampilkan status ON/OFF di kedua blok.
- Bind ke entity props (no ViewModel); default ON dari model (bukan hardcoded checked).
- Copy VERBATIM dari UI-SPEC (Bahasa Indonesia, tak dipangkas).
</success_criteria>

<output>
After completion, create `.planning/phases/372-data-foundation-propagasi-toggle/372-03-SUMMARY.md`.
</output>
