---
phase: 374-ui-managepackages-lock-pre-post
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/ShuffleToggleRules.cs
  - HcPortal.Tests/ShuffleToggleRulesTests.cs
  - HcPortal.Tests/ShuffleLockGuardTests.cs
  - HcPortal.Tests/ShuffleUpdateEndpointTests.cs
autonomous: true
requirements: [SHUF-10, SHUF-11, SHUF-12, SHUF-14]
must_haves:
  truths:
    - "Lock-condition (started OR assignment) dihitung oleh SATU helper pure, dipakai GET dan POST (cegah divergensi Pitfall 2)."
    - "Hide-condition (Proton Tahun 3 OR Manual entry) dihitung oleh helper pure."
    - "Warning §9 predikat (>=2 paket-ber-soal AND Acak Soal OFF AND mismatch) dihitung oleh helper pure."
    - "Wave 0: test guard-reject lock + test propagate sibling ADA dan menunjukkan kontrak endpoint (RED/skeleton sebelum impl Plan 02)."
  artifacts:
    - path: "Helpers/ShuffleToggleRules.cs"
      provides: "Pure decision helpers IsShuffleLocked / ShouldHideShuffleToggle / ShouldShowSizeMismatchWarning"
      contains: "static class ShuffleToggleRules"
    - path: "HcPortal.Tests/ShuffleToggleRulesTests.cs"
      provides: "Unit test decision-logic tanpa DB (SHUF-11/14 + warning predikat)"
      contains: "ShuffleToggleRulesTests"
    - path: "HcPortal.Tests/ShuffleLockGuardTests.cs"
      provides: "Real-SQL POST-replica reject saat locked / write saat clean (SHUF-11)"
      contains: "[Trait(\"Category\", \"Integration\")]"
    - path: "HcPortal.Tests/ShuffleUpdateEndpointTests.cs"
      provides: "Real-SQL propagate foreach ke SEMUA sibling (SHUF-10)"
      contains: "[Trait(\"Category\", \"Integration\")]"
  key_links:
    - from: "Helpers/ShuffleToggleRules.cs"
      to: "AssessmentAdminController.ManagePackages GET + UpdateShuffleSettings POST (Plan 02)"
      via: "static helper call dipakai di dua tempat"
      pattern: "ShuffleToggleRules\\.(IsShuffleLocked|ShouldHideShuffleToggle)"
---

<objective>
Buat fondasi testable Phase 374: SATU helper pure `ShuffleToggleRules` untuk tiga keputusan (lock, hide, warning) + Wave 0 test files yang mengunci kontrak endpoint `UpdateShuffleSettings` SEBELUM endpoint ditulis (Plan 02).

Purpose: Mencegah Pitfall 2 (lock-condition divergen GET vs POST) dengan satu sumber kebenaran, dan menyediakan test guard-reject + propagate (real-SQL) yang menjadi acceptance SHUF-10/SHUF-11.

Output:
- `Helpers/ShuffleToggleRules.cs` — 3 fungsi static pure (tanpa DB, tanpa EF).
- `HcPortal.Tests/ShuffleToggleRulesTests.cs` — unit test decision-logic.
- `HcPortal.Tests/ShuffleLockGuardTests.cs` — real-SQL POST-replica reject/accept.
- `HcPortal.Tests/ShuffleUpdateEndpointTests.cs` — real-SQL propagate foreach.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-CONTEXT.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-RESEARCH.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-VALIDATION.md

<interfaces>
<!-- Kontrak yang dipakai/diproduksi. Executor pakai langsung — TANPA eksplorasi codebase. -->

Models/AssessmentSession.cs (VERIFIED 2026-06-13):
```csharp
public bool ShuffleQuestions { get; set; } = true;   // L39
public bool ShuffleOptions  { get; set; } = true;   // L42
public DateTime? StartedAt  { get; set; }            // L46
public DateTime? UpdatedAt  { get; set; }            // L94
public string? TahunKe      { get; set; }            // L108  -> "Tahun 1" | "Tahun 2" | "Tahun 3"
public bool IsManualEntry   { get; set; } = false;   // L137
public int? LinkedSessionId { get; set; }            // L178
public bool SamePackage     { get; set; } = false;   // L191
public string Category      { get; set; }            // -> "Assessment Proton" untuk Proton
```

Lock-condition (spec §7, VERIFIED dari ReshufflePackage:5078-5104):
- anyStarted = ada sibling dengan StartedAt != null
- anyAssignment = ada UserPackageAssignment dengan AssessmentSessionId ∈ siblingSessionIds
- isLocked = anyStarted || anyAssignment

Hide-condition (spec §7):
- hide = (Category == "Assessment Proton" && TahunKe == "Tahun 3") || IsManualEntry

Warning §9 predikat (spec §9):
- show = packagesWithQuestions >= 2 && shuffleQuestionsOff && hasMismatch

Test fixture existing (pakai verbatim):
HcPortal.Tests/ShufflePropagationTests.cs — pola IClassFixture<ProtonCompletionFixture>, SeedUserAsync, helper Sib(userId, title, schedule), [Trait("Category","Integration")], ApplicationDbContext(_fixture.Options).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure helper ShuffleToggleRules (3 keputusan)</name>
  <read_first>
    - Helpers/ShuffleToggleRules.cs (file yang dibuat — pastikan belum ada)
    - .planning/phases/374-ui-managepackages-lock-pre-post/374-RESEARCH.md (Pitfall 2 + Code Examples hide-flag + warning predikat)
    - docs/superpowers/specs/2026-06-13-shuffle-toggle-design.md (§7 lock/hide condition, §9 warning) — re-baca anchor "Tahun 3", "IsManualEntry", lock-condition
    - Helpers/ShuffleEngine.cs (pola namespace/struktur file Helpers/ — TIDAK diubah, hanya pola)
  </read_first>
  <behavior>
    - IsShuffleLocked(bool anyStarted, bool anyAssignment) => anyStarted || anyAssignment. Test: (true,false)=>true; (false,true)=>true; (false,false)=>false; (true,true)=>true.
    - ShouldHideShuffleToggle(string? category, string? tahunKe, bool isManualEntry):
      - ("Assessment Proton","Tahun 3",false) => true
      - ("Assessment Proton","Tahun 2",false) => false
      - ("Assessment OJT",null,false) => false
      - (anything,anything,true) => true (manual entry selalu hide)
      - ("Assessment Proton","Tahun 1",true) => true
    - ShouldShowSizeMismatchWarning(int packagesWithQuestions, bool shuffleQuestions, bool hasMismatch):
      - (2,false,true) => true
      - (2,true,true) => false (Acak Soal ON menutup warning)
      - (1,false,true) => false (cuma 1 paket)
      - (2,false,false) => false (tidak ada mismatch)
      - (3,false,true) => true
  </behavior>
  <action>
    Buat file `Helpers/ShuffleToggleRules.cs`. Namespace `HcPortal.Helpers` (samakan dengan `Helpers/ShuffleEngine.cs` — re-grep `namespace` di ShuffleEngine.cs untuk konfirmasi exact).

    Tulis `public static class ShuffleToggleRules` berisi 3 method static pure (TIDAK boleh menyentuh DbContext / EF / async — murni input bool/string):

    ```csharp
    namespace HcPortal.Helpers;

    /// <summary>
    /// Phase 374 — keputusan pure UI toggle shuffle di ManagePackages.
    /// Dipakai DI DUA TEMPAT (GET ViewBag + POST guard) untuk cegah divergensi (Pitfall 2).
    /// Tidak menyentuh DbContext: caller menyediakan fakta (anyStarted/anyAssignment dst).
    /// </summary>
    public static class ShuffleToggleRules
    {
        // SHUF-11: terkunci bila ada peserta mulai ATAU ada assignment di grup sibling.
        public static bool IsShuffleLocked(bool anyStarted, bool anyAssignment)
            => anyStarted || anyAssignment;

        // SHUF-14: sembunyikan total untuk Proton Tahun 3 ATAU Manual entry.
        public static bool ShouldHideShuffleToggle(string? category, string? tahunKe, bool isManualEntry)
            => (category == "Assessment Proton" && tahunKe == "Tahun 3") || isManualEntry;

        // SHUF-12: warning ukuran-paket-beta hanya bila >=2 paket-ber-soal, Acak Soal OFF, dan ukuran beda.
        public static bool ShouldShowSizeMismatchWarning(int packagesWithQuestions, bool shuffleQuestions, bool hasMismatch)
            => packagesWithQuestions >= 2 && !shuffleQuestions && hasMismatch;
    }
    ```

    Catatan: literal string `"Assessment Proton"` dan `"Tahun 3"` WAJIB persis (spec §7). JANGAN tambah dependency.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/ShuffleToggleRules.cs` contains `public static class ShuffleToggleRules`
    - `Helpers/ShuffleToggleRules.cs` contains `IsShuffleLocked`
    - `Helpers/ShuffleToggleRules.cs` contains `ShouldHideShuffleToggle`
    - `Helpers/ShuffleToggleRules.cs` contains `ShouldShowSizeMismatchWarning`
    - `Helpers/ShuffleToggleRules.cs` contains `"Assessment Proton"` and `"Tahun 3"`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Helper pure ada, build hijau, tidak menyentuh DbContext/EF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test ShuffleToggleRulesTests (decision-logic tanpa DB)</name>
  <read_first>
    - Helpers/ShuffleToggleRules.cs (dibuat Task 1)
    - HcPortal.Tests/ShuffleEngineTests.cs (pola unit test pure tanpa fixture — namespace + [Fact]/[Theory])
  </read_first>
  <behavior>
    - Cover 3 method dengan [Theory]/[InlineData] sesuai matriks di Task 1 behavior block (lock 4 kasus, hide 5 kasus, warning 5 kasus).
  </behavior>
  <action>
    Buat `HcPortal.Tests/ShuffleToggleRulesTests.cs`. Namespace `HcPortal.Tests` (samakan ShuffleEngineTests.cs). Tanpa fixture (pure, tidak butuh DB). Pakai `[Theory]` + `[InlineData]`:

    - `Lock_OrLogic` — InlineData(true,false,true), (false,true,true), (false,false,false), (true,true,true) → assert `ShuffleToggleRules.IsShuffleLocked(a,b) == expected`.
    - `Hide_ProtonTahun3OrManual` — InlineData("Assessment Proton","Tahun 3",false,true), ("Assessment Proton","Tahun 2",false,false), ("Assessment OJT",null,false,false), ("Assessment OJT",null,true,true), ("Assessment Proton","Tahun 1",true,true) → assert `ShouldHideShuffleToggle(cat,th,manual) == expected`.
    - `Warning_Predicate` — InlineData(2,false,true,true), (2,true,true,false), (1,false,true,false), (2,false,false,false), (3,false,true,true) → assert `ShouldShowSizeMismatchWarning(n,sq,mismatch) == expected`.

    Catatan: `[InlineData]` dengan arg `string?` null → tulis `(string?)null` bila compiler rewel, atau pakai `null` langsung (xUnit menerima null untuk param nullable).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ShuffleToggleRules" 2>&1 | Select-String -Pattern "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/ShuffleToggleRulesTests.cs` contains `ShuffleToggleRulesTests`
    - `HcPortal.Tests/ShuffleToggleRulesTests.cs` contains `IsShuffleLocked`
    - `dotnet test --filter "FullyQualifiedName~ShuffleToggleRules"` exits 0
  </acceptance_criteria>
  <done>Semua [Theory] hijau; decision-logic terkunci tanpa DB.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 real-SQL tests — lock-guard reject/accept + propagate sibling</name>
  <read_first>
    - HcPortal.Tests/ShufflePropagationTests.cs (TEMPLATE verbatim — IClassFixture<ProtonCompletionFixture>, SeedUserAsync, Sib helper, [Trait("Category","Integration")], ApplicationDbContext(_fixture.Options))
    - HcPortal.Tests/ProtonCompletionServiceTests.cs (ProtonCompletionFixture — disposable SQL DB per-fixture, baris ~25-61)
    - Helpers/ShuffleToggleRules.cs (Task 1 — dipakai untuk decision lock di POST-replica)
    - docs/superpowers/specs/2026-06-13-shuffle-toggle-design.md (§7 endpoint body — guard re-check lalu foreach propagate)
    - Models/UserPackageAssignment.cs (re-grep field: AssessmentSessionId, AssessmentPackageId, UserId — untuk seed assignment yang memicu lock)
  </read_first>
  <action>
    Buat DUA file test real-SQL. Pola PERSIS `ShufflePropagationTests.cs` (replika logika endpoint atas grup REAL di SQL Server, BUKAN memanggil controller HTTP). Keduanya `[Trait("Category","Integration")]` + `IClassFixture<ProtonCompletionFixture>`. Pinjam `SeedUserAsync` + `Sib(userId, title, schedule)` (copy private helper ke kedua file, atau buat helper lokal identik).

    **File A — `HcPortal.Tests/ShuffleUpdateEndpointTests.cs` (SHUF-10 propagate):**
    Test `UpdateShuffleSettings_PropagatesToAllSiblings`:
    1. Seed grup 3 sibling (key Title=marker, Category sama, Schedule.Date sama) dengan ShuffleQuestions=true, ShuffleOptions=true.
    2. Replika body endpoint: query siblings by (Title==marker && Category==cat && Schedule.Date==date); foreach set ShuffleQuestions=false, ShuffleOptions=false, UpdatedAt=now; SaveChanges.
    3. Assert AsNoTracking re-read: 3 rows, semua ShuffleQuestions==false && ShuffleOptions==false.
    (Ini mirror `Propagation_Standard_AllSiblingsFollowModel` tapi pakai filter key lengkap (Title+Category+Schedule.Date), bukan hanya Title — untuk membuktikan key sibling spec §5.)

    **File B — `HcPortal.Tests/ShuffleLockGuardTests.cs` (SHUF-11 guard):**
    Test 1 `Guard_RejectsWrite_WhenSiblingStarted`:
    1. Seed grup 2 sibling, ShuffleQuestions=true. Set salah satu sibling.StartedAt = DateTime.UtcNow (peserta mulai).
    2. Replika guard endpoint: hitung anyStarted = siblings.Any(s => s.StartedAt != null); anyAssignment = false; isLocked = ShuffleToggleRules.IsShuffleLocked(anyStarted, anyAssignment).
    3. Assert isLocked == true. Karena locked → JANGAN tulis. Re-read AsNoTracking → semua ShuffleQuestions MASIH true (tidak berubah).

    Test 2 `Guard_AllowsWrite_WhenClean`:
    1. Seed grup 2 sibling, ShuffleQuestions=true, StartedAt=null, tanpa UserPackageAssignment.
    2. anyStarted=false; anyAssignment=false; isLocked=false → tulis (foreach set ShuffleQuestions=false; SaveChanges).
    3. Re-read → semua ShuffleQuestions==false.

    Test 3 `Guard_RejectsWrite_WhenAssignmentExists`:
    1. Seed grup 2 sibling clean + 1 UserPackageAssignment dengan AssessmentSessionId == salah satu sibling.Id (re-grep field UserPackageAssignment untuk konstruksi minimal valid; AssessmentPackageId boleh pakai paket dummy yang di-seed atau 0 bila FK mengizinkan — bila FK strict, seed AssessmentPackage minimal).
    2. anyAssignment = ctx.UserPackageAssignments.Any(a => siblingIds.Contains(a.AssessmentSessionId)); isLocked = true.
    3. Assert isLocked==true; JANGAN tulis; re-read → ShuffleQuestions MASIH true.

    Gunakan `ShuffleToggleRules.IsShuffleLocked` di ketiga guard test (mengikat helper Task 1 ke kontrak). Marker GUID per-test untuk isolasi (pola `ShufflePropagationTests`).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ShuffleLockGuard|FullyQualifiedName~ShuffleUpdateEndpoint" 2>&1 | Select-String -Pattern "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/ShuffleUpdateEndpointTests.cs` contains `[Trait("Category", "Integration")]`
    - `HcPortal.Tests/ShuffleUpdateEndpointTests.cs` contains `UpdateShuffleSettings_PropagatesToAllSiblings`
    - `HcPortal.Tests/ShuffleLockGuardTests.cs` contains `Guard_RejectsWrite_WhenSiblingStarted`
    - `HcPortal.Tests/ShuffleLockGuardTests.cs` contains `Guard_AllowsWrite_WhenClean`
    - `HcPortal.Tests/ShuffleLockGuardTests.cs` contains `ShuffleToggleRules.IsShuffleLocked`
    - `dotnet test --filter "FullyQualifiedName~ShuffleLockGuard|FullyQualifiedName~ShuffleUpdateEndpoint"` exits 0
  </acceptance_criteria>
  <done>Test real-SQL hijau: propagate ke semua sibling + guard reject saat started/assignment + accept saat clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin browser → AssessmentAdminController | Input toggle bool + assessmentId menyeberang; endpoint POST diuji di Plan 02. Plan ini menyiapkan kontrak guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-374-lock | Tampering/Elevation | Lock-guard decision (`IsShuffleLocked`) | mitigate | Helper pure single-source dipakai GET+POST (Plan 02); Wave 0 test `ShuffleLockGuardTests` membuktikan POST-replica TIDAK menulis saat locked (started OR assignment). Cegah Pitfall 2 divergensi. |
| T-374-csrf | Tampering | Propagate write path | mitigate | Kontrak propagate diuji (`ShuffleUpdateEndpointTests`); enforcement CSRF token = Plan 02 (`[ValidateAntiForgeryToken]`). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exits 0.
- `dotnet test --filter "FullyQualifiedName~Shuffle"` hijau (termasuk file baru + 329/329 baseline tidak regresi pada subset Shuffle).
- Helper `ShuffleToggleRules` TIDAK menyentuh DbContext/EF (grep tidak ada `_context`/`DbContext` di file helper).
</verification>

<success_criteria>
- 3 method pure ada + unit test hijau (SHUF-11/12/14 decision-logic).
- 2 file real-SQL Wave 0 hijau (SHUF-10 propagate + SHUF-11 guard reject/accept).
- Helper menjadi single-source lock-condition yang Plan 02 WAJIB pakai di GET dan POST.
</success_criteria>

<output>
After completion, create `.planning/phases/374-ui-managepackages-lock-pre-post/374-01-SUMMARY.md`.
Catat: namespace exact helper, signature 3 method, dan nama test agar Plan 02 referensi tepat.
</output>
