---
phase: 374-ui-managepackages-lock-pre-post
plan: 02
type: execute
wave: 2
depends_on: [374-01]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [SHUF-10, SHUF-11, SHUF-13, SHUF-14]
must_haves:
  truths:
    - "Endpoint POST UpdateShuffleSettings ada: [Authorize(Roles=\"Admin, HC\")] + [ValidateAntiForgeryToken] + audit + propagate sibling + PRG redirect."
    - "Endpoint re-cek lock server-side (defense-in-depth): saat locked → TempData error + redirect TANPA menulis."
    - "ManagePackages GET meng-enrich ViewBag: ShuffleQuestions/ShuffleOptions, IsShuffleLocked, HideShuffleToggle, ShowSizeMismatchWarning, PreShuffleQuestions (Post via LinkedSessionId)."
    - "Lock-condition GET dan POST memakai helper ShuffleToggleRules.IsShuffleLocked yang SAMA (no divergensi)."
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "UpdateShuffleSettings POST + ManagePackages GET ViewBag enrich"
      contains: "UpdateShuffleSettings"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs UpdateShuffleSettings"
      to: "AssessmentSessions sibling group (Title, Category, Schedule.Date)"
      via: "foreach propagate + SaveChangesAsync"
      pattern: "foreach.*sibling.*ShuffleQuestions"
    - from: "Controllers/AssessmentAdminController.cs ManagePackages GET"
      to: "Views/Admin/ManagePackages.cshtml (Plan 03)"
      via: "ViewBag.IsShuffleLocked / ViewBag.HideShuffleToggle / ViewBag.PreShuffleQuestions"
      pattern: "ViewBag\\.(IsShuffleLocked|HideShuffleToggle|ShowSizeMismatchWarning)"
---

<objective>
Tambah endpoint `UpdateShuffleSettings` (PRG + guard + propagate + audit) dan enrich `ManagePackages` GET dengan ViewBag yang dibutuhkan view (Plan 03). Semua di `Controllers/AssessmentAdminController.cs`.

Purpose: Backend SHUF-10 (endpoint+propagate), SHUF-11 (server guard defense-in-depth), data untuk SHUF-13 (reminder Pre/Post) + SHUF-14 (hide). Mengikat helper Plan 01 sebagai single-source lock-condition.

Output: 1 method POST baru + ~6 baris ViewBag baru di GET. TANPA migration. TANPA file baru.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-CONTEXT.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-RESEARCH.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-01-SUMMARY.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-UI-SPEC.md

<interfaces>
<!-- Kontrak yang dipakai. Re-grep anchor (BUKAN line number) — file ini area sibuk v25.0 367/368, offset PASTI drift. -->

Helper Plan 01 (Helpers/ShuffleToggleRules.cs):
```csharp
bool ShuffleToggleRules.IsShuffleLocked(bool anyStarted, bool anyAssignment)
bool ShuffleToggleRules.ShouldHideShuffleToggle(string? category, string? tahunKe, bool isManualEntry)
bool ShuffleToggleRules.ShouldShowSizeMismatchWarning(int packagesWithQuestions, bool shuffleQuestions, bool hasMismatch)
```
(Namespace HcPortal.Helpers — tambah `using HcPortal.Helpers;` bila belum ada di controller.)

Audit (VERIFIED Services/AuditLogService.cs:9-43):
```csharp
Task LogAsync(string actorUserId, string actorName, string actionType, string description, int? targetId = null, string? targetType = null)
```

Pola lock-detection (VERIFIED ReshufflePackage, re-grep method `ReshufflePackage`):
```csharp
var siblingSessionIds = await _context.AssessmentSessions
    .Where(s => s.Title == assessment.Title &&
                s.Category == assessment.Category &&
                s.Schedule.Date == assessment.Schedule.Date)
    .Select(s => s.Id).ToListAsync();
```

Pola audit try/catch warn-only (VERIFIED ReshufflePackage, re-grep `Audit log failed for ReshufflePackage`):
```csharp
var hcUser = await _userManager.GetUserAsync(User);
var actorNameStr = string.IsNullOrWhiteSpace(hcUser?.NIP) ? (hcUser?.FullName ?? "Unknown") : $"{hcUser.NIP} - {hcUser.FullName}";
```

ManagePackages GET existing (VERIFIED, re-grep method `ManagePackages`): sudah set ViewBag.Packages, AssignmentCounts, IsPostSession, PreSessionId (Post+LinkedSessionId), IsSamePackageLocked. Akhir method `return View();`.

AssessmentSession fields: ShuffleQuestions(bool), ShuffleOptions(bool), StartedAt(DateTime?), UpdatedAt(DateTime?), TahunKe(string?), IsManualEntry(bool), LinkedSessionId(int?), Category(string), AssessmentType(string -> "PostTest").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint POST UpdateShuffleSettings (guard + propagate + audit + PRG)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs — RE-GREP method `ReshufflePackage` (pola attribute trio + siblingSessionIds query + lock-detect StartedAt/UserPackageAssignment + audit try/catch). RE-GREP method `EditAssessment` (POST overload `EditAssessment(int id, AssessmentSession model, ...)`) untuk pola propagate foreach. JANGAN pakai line number sebagai authority — area drift v25.0 367/368.
    - Helpers/ShuffleToggleRules.cs (Plan 01 — IsShuffleLocked)
    - Services/AuditLogService.cs (signature LogAsync)
    - .planning/phases/374-ui-managepackages-lock-pre-post/374-UI-SPEC.md (Copywriting Contract — TempData success/error verbatim)
    - docs/superpowers/specs/2026-06-13-shuffle-toggle-design.md (§7 endpoint, §5 propagate sibling)
    - Models/UserPackageAssignment.cs (field AssessmentSessionId untuk anyAssignment query)
  </read_first>
  <action>
    Sisipkan method baru `UpdateShuffleSettings` di `AssessmentAdminController.cs` SETELAH `ReshuffleAll` (re-grep method `ReshuffleAll` sebagai anchor; sisip setelah closing brace-nya, sebelum `ManagePackages` GET atau di dekat blok reshuffle agar berkerumun secara semantik). Tambah `using HcPortal.Helpers;` di top file bila belum ada.

    Signature + attribute (VERBATIM, role-string persis `"Admin, HC"`):
    ```csharp
    [HttpPost]
    [Authorize(Roles = "Admin, HC")]
    [ValidateAntiForgeryToken]
    public async Task<IActionResult> UpdateShuffleSettings(int assessmentId, bool shuffleQuestions, bool shuffleOptions)
    {
        var assessment = await _context.AssessmentSessions
            .FirstOrDefaultAsync(a => a.Id == assessmentId);
        if (assessment == null) return NotFound();

        // Sibling group — key identik StartExam/Reshuffle/EditAssessment (spec §5).
        var siblingSessionIds = await _context.AssessmentSessions
            .Where(s => s.Title == assessment.Title &&
                        s.Category == assessment.Category &&
                        s.Schedule.Date == assessment.Schedule.Date)
            .Select(s => s.Id)
            .ToListAsync();

        // Defense-in-depth (D-04a / SHUF-11): re-cek lock server-side.
        bool anyStarted = await _context.AssessmentSessions
            .AnyAsync(s => siblingSessionIds.Contains(s.Id) && s.StartedAt != null);
        bool anyAssignment = await _context.UserPackageAssignments
            .AnyAsync(a => siblingSessionIds.Contains(a.AssessmentSessionId));

        if (ShuffleToggleRules.IsShuffleLocked(anyStarted, anyAssignment))
        {
            TempData["Error"] = "Pengaturan pengacakan tidak dapat diubah karena sudah ada peserta yang memulai ujian.";
            return RedirectToAction("ManagePackages", new { assessmentId });
        }

        // Propagate ke SEMUA sibling grup (pola EditAssessment foreach).
        var siblings = await _context.AssessmentSessions
            .Where(s => siblingSessionIds.Contains(s.Id))
            .ToListAsync();
        var now = DateTime.UtcNow;
        foreach (var sibling in siblings)
        {
            sibling.ShuffleQuestions = shuffleQuestions;
            sibling.ShuffleOptions   = shuffleOptions;
            sibling.UpdatedAt = now;
        }
        await _context.SaveChangesAsync();

        // Audit try/catch warn-only (pola ReshufflePackage).
        try
        {
            var hcUser = await _userManager.GetUserAsync(User);
            var actorNameStr = string.IsNullOrWhiteSpace(hcUser?.NIP) ? (hcUser?.FullName ?? "Unknown") : $"{hcUser.NIP} - {hcUser.FullName}";
            await _auditLog.LogAsync(
                hcUser?.Id ?? "",
                actorNameStr,
                "UpdateShuffleSettings",
                $"Set Acak Soal={shuffleQuestions}, Acak Pilihan={shuffleOptions} for assessment '{assessment.Title}' [grup {siblingSessionIds.Count} sesi]",
                assessmentId,
                "AssessmentSession");
        }
        catch (Exception ex) { _logger.LogWarning(ex, "Audit log failed for UpdateShuffleSettings (assessmentId={Id})", assessmentId); }

        TempData["Success"] = "Pengaturan pengacakan berhasil disimpan.";
        return RedirectToAction("ManagePackages", new { assessmentId });
    }
    ```

    PENTING:
    - Re-grep field injeksi `_context`, `_userManager`, `_auditLog`, `_logger` (dipakai ReshufflePackage di file yang sama — nama harus persis). Bila salah satu beda nama, samakan dengan yang dipakai ReshufflePackage.
    - TempData string copy VERBATIM dari UI-SPEC (success: "Pengaturan pengacakan berhasil disimpan."; error: "Pengaturan pengacakan tidak dapat diubah karena sudah ada peserta yang memulai ujian.").
    - JANGAN return Json (D-01a = PRG). JANGAN tambah `[HttpGet]`.
    - Checkbox→bool binding: param `bool shuffleQuestions, bool shuffleOptions` — unchecked tidak terkirim → binder default false (Pitfall 3 / Q2). Tidak perlu hidden-fallback bila form mengirim `value="true"` saat checked (Plan 03 menangani).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` contains `UpdateShuffleSettings(int assessmentId, bool shuffleQuestions, bool shuffleOptions)`
    - `AssessmentAdminController.cs` contains `[Authorize(Roles = "Admin, HC")]` immediately preceding UpdateShuffleSettings (re-grep proximity)
    - `AssessmentAdminController.cs` contains `[ValidateAntiForgeryToken]` for UpdateShuffleSettings
    - `AssessmentAdminController.cs` contains `ShuffleToggleRules.IsShuffleLocked`
    - `AssessmentAdminController.cs` contains `"Pengaturan pengacakan berhasil disimpan."`
    - `AssessmentAdminController.cs` contains `"Pengaturan pengacakan tidak dapat diubah karena sudah ada peserta yang memulai ujian."`
    - `AssessmentAdminController.cs` contains `"UpdateShuffleSettings"` passed to LogAsync
    - `AssessmentAdminController.cs` contains `return RedirectToAction("ManagePackages"` (no Json return in this method)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Endpoint ada dengan attribute trio, guard helper-based, propagate foreach, audit warn-only, PRG. Build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich ManagePackages GET ViewBag (lock/hide/saved-state/Pre-state)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs — RE-GREP method `ManagePackages` (GET, `ManagePackages(int assessmentId)`). Anchor akhir: blok `ViewBag.IsSamePackageLocked = ...` lalu `return View();`. Sisip ViewBag baru SEBELUM `return View();`.
    - Helpers/ShuffleToggleRules.cs (IsShuffleLocked, ShouldHideShuffleToggle, ShouldShowSizeMismatchWarning)
    - Views/Admin/ManagePackages.cshtml (L70-81 — hasMismatch dihitung DI VIEW; controller perlu emit packagesWithQuestions + hasMismatch agar bisa dipakai warning predikat + JS Plan 03)
  </read_first>
  <action>
    Di method GET `ManagePackages`, SEBELUM `return View();` (re-grep `ViewBag.IsSamePackageLocked` sebagai anchor — sisip setelah baris itu), tambah enrichment:

    ```csharp
    // === Phase 374: Shuffle toggle state ===
    var siblingSessionIds = await _context.AssessmentSessions
        .Where(s => s.Title == assessment.Title &&
                    s.Category == assessment.Category &&
                    s.Schedule.Date == assessment.Schedule.Date)
        .Select(s => s.Id)
        .ToListAsync();

    bool anyStarted = await _context.AssessmentSessions
        .AnyAsync(s => siblingSessionIds.Contains(s.Id) && s.StartedAt != null);
    bool anyAssignment = await _context.UserPackageAssignments
        .AnyAsync(a => siblingSessionIds.Contains(a.AssessmentSessionId));

    ViewBag.ShuffleQuestions = assessment.ShuffleQuestions;   // saved state -> render checked
    ViewBag.ShuffleOptions   = assessment.ShuffleOptions;
    ViewBag.IsShuffleLocked  = ShuffleToggleRules.IsShuffleLocked(anyStarted, anyAssignment);
    ViewBag.HideShuffleToggle = ShuffleToggleRules.ShouldHideShuffleToggle(
        assessment.Category, assessment.TahunKe, assessment.IsManualEntry);

    // Warning §9 precondition: jumlah paket-ber-soal + mismatch ukuran (mirror view L70-81).
    var pkgWithQuestions = packages.Where(p => p.Questions.Any()).ToList();
    int packagesWithQuestions = pkgWithQuestions.Count;
    bool hasMismatch = false;
    if (packagesWithQuestions > 0)
    {
        int refCount = pkgWithQuestions[0].Questions.Count;
        hasMismatch = pkgWithQuestions.Any(p => p.Questions.Count != refCount);
    }
    ViewBag.PackagesWithQuestions = packagesWithQuestions;
    ViewBag.HasSizeMismatch = hasMismatch;
    ViewBag.ShowSizeMismatchWarning = ShuffleToggleRules.ShouldShowSizeMismatchWarning(
        packagesWithQuestions, assessment.ShuffleQuestions, hasMismatch);

    // Reminder Pre/Post (opsi Z, SHUF-13): Post page only, saved-state Pre via LinkedSessionId.
    if (isPostSession && assessment.LinkedSessionId.HasValue)
    {
        var preShuffle = await _context.AssessmentSessions
            .Where(s => s.Id == assessment.LinkedSessionId.Value)
            .Select(s => (bool?)s.ShuffleQuestions)
            .FirstOrDefaultAsync();
        ViewBag.PreShuffleQuestions = preShuffle;   // null bila Pre tak ada
    }
    ```

    PENTING:
    - `isPostSession` sudah dideklarasikan di method GET existing (re-grep `bool isPostSession`). Pakai variabel itu, JANGAN deklarasi ulang.
    - `packages` sudah ada di scope GET (re-grep `var packages = await`). Pakai langsung.
    - `assessment` sudah di scope. Pakai langsung.
    - Lock-condition GET WAJIB pakai `ShuffleToggleRules.IsShuffleLocked` yang SAMA dengan POST (Task 1) — Pitfall 2.
    - `ViewBag.ShowSizeMismatchWarning` adalah nilai INITIAL server-render (state saved ShuffleQuestions); JS Plan 03 recompute live saat flip. Emit juga `HasSizeMismatch` + `PackagesWithQuestions` agar JS bisa recompute tanpa hitung ulang dari nol (Pitfall 4 opsi b).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` contains `ViewBag.IsShuffleLocked = ShuffleToggleRules.IsShuffleLocked`
    - `AssessmentAdminController.cs` contains `ViewBag.HideShuffleToggle = ShuffleToggleRules.ShouldHideShuffleToggle`
    - `AssessmentAdminController.cs` contains `ViewBag.ShowSizeMismatchWarning`
    - `AssessmentAdminController.cs` contains `ViewBag.PreShuffleQuestions`
    - `AssessmentAdminController.cs` contains `ViewBag.ShuffleQuestions = assessment.ShuffleQuestions`
    - `AssessmentAdminController.cs` contains `ViewBag.HasSizeMismatch`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>GET meng-enrich 7 ViewBag baru; lock-condition GET pakai helper yang sama dengan POST; Pre-state hanya untuk Post.</done>
</task>

<task type="auto">
  <name>Task 3: Sampling gate — Shuffle subset + lock-guard tetap hijau</name>
  <read_first>
    - .planning/phases/374-ui-managepackages-lock-pre-post/374-VALIDATION.md (Sampling Rate)
  </read_first>
  <action>
    Jalankan subset Shuffle penuh untuk memastikan endpoint + GET enrich tidak meregresi Wave 0 tests Plan 01 maupun test Phase 372/373. Tidak ada edit kode di task ini — hanya gate. Bila ada kegagalan, perbaiki di task terkait (bukan task baru) lalu re-run.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~Shuffle" 2>&1 | Select-String -Pattern "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~Shuffle"` exits 0
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Subset Shuffle hijau pasca endpoint+GET; tidak ada regresi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin browser → UpdateShuffleSettings POST | assessmentId(int) + 2 bool menyeberang dari form; state-changing write ke semua sibling. |
| ManagePackages GET → DB | query sibling lock-state + Pre saved-state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-374-csrf | Tampering | UpdateShuffleSettings POST | mitigate | `[ValidateAntiForgeryToken]` pada method; token form di Plan 03. |
| T-374-lock | Tampering/Elevation | Server lock guard | mitigate | Re-cek `ShuffleToggleRules.IsShuffleLocked(anyStarted, anyAssignment)` server-side; locked → TempData error + redirect TANPA write (bypass UI-disabled via curl/devtools tertutup). |
| T-374-priv | Elevation | Endpoint authorization | mitigate | `[Authorize(Roles = "Admin, HC")]` — non-Admin/HC ditolak. |
| T-374-audit | Repudiation | Audit trail | mitigate | `AuditLogService.LogAsync` actor "NIP - FullName" + actionType "UpdateShuffleSettings" + targetId assessmentId; try/catch warn-only (tak gagalkan operasi). |
| IDOR | Tampering | assessmentId milik orang lain | accept | Admin/HC = role global, konsisten pola ReshufflePackage/EditAssessment (tidak ada per-owner check di assessment admin). Documented. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exits 0.
- `dotnet test --filter "FullyQualifiedName~Shuffle"` hijau.
- Lock-condition GET dan POST memanggil `ShuffleToggleRules.IsShuffleLocked` yang sama (grep dua call-site).
- Endpoint TIDAK return Json (grep tidak ada `return Json` di body UpdateShuffleSettings).
</verification>

<success_criteria>
- SHUF-10: endpoint propagate ke semua sibling + attribute trio + audit (test Wave 0 hijau).
- SHUF-11: server guard tolak saat locked (test Wave 0 hijau).
- SHUF-13 data: ViewBag.PreShuffleQuestions tersedia di Post.
- SHUF-14 data: ViewBag.HideShuffleToggle tersedia.
</success_criteria>

<output>
After completion, create `.planning/phases/374-ui-managepackages-lock-pre-post/374-02-SUMMARY.md`.
Catat: nama exact field injeksi (_context/_userManager/_auditLog/_logger), lokasi sisip endpoint, daftar ViewBag baru + tipe (untuk Plan 03), apakah UpdatedAt di-set.
</output>
