---
phase: 374-ui-managepackages-lock-pre-post
plan: 03
type: execute
wave: 3
depends_on: [374-02]
files_modified:
  - Views/Admin/ManagePackages.cshtml
autonomous: false
requirements: [SHUF-10, SHUF-11, SHUF-12, SHUF-13, SHUF-14]
must_haves:
  truths:
    - "Card 'Pengacakan Soal & Jawaban' tampil di ManagePackages dengan 2 form-switch + tombol Simpan Pengaturan (form POST + AntiForgeryToken)."
    - "Saat locked: kedua switch + tombol disabled + lock alert banner alert-info tampil."
    - "Saat Proton Tahun 3 / Manual entry: seluruh card TIDAK dirender (hide != disabled)."
    - "Warning §9 muncul/hilang live via JS saat flip Acak Soal (>=2 paket-ber-soal + OFF + mismatch)."
    - "Reminder Pre/Post (opsi Z) tampil HANYA di Post saat Pre OFF & Post ON (saved-state, no live JS)."
    - "Copy toggle + help-text 372-UI-SPEC verbatim; frasa 'jawaban benar tetap dinilai dengan benar' ADA."
  artifacts:
    - path: "Views/Admin/ManagePackages.cshtml"
      provides: "Card Pengacakan + 2 toggle + lock/warning/reminder alert + live JS"
      contains: "Pengacakan Soal & Jawaban"
      min_lines: 40
  key_links:
    - from: "Views/Admin/ManagePackages.cshtml form"
      to: "AssessmentAdminController.UpdateShuffleSettings"
      via: "asp-action UpdateShuffleSettings + @Html.AntiForgeryToken()"
      pattern: "asp-action=\"UpdateShuffleSettings\""
    - from: "ManagePackages.cshtml @section Scripts"
      to: "warning §9 alert (#shuffleSizeWarning)"
      via: "vanilla JS toggle d-none on flip"
      pattern: "shuffleSizeWarning"
---

<objective>
Render card "Pengacakan Soal & Jawaban" di `Views/Admin/ManagePackages.cshtml`: 2 form-switch toggle + tombol Simpan + lock alert + warning §9 (live JS) + reminder Pre/Post. Konsumsi ViewBag dari Plan 02.

Purpose: UI layer SHUF-10/11/12/13/14. Copy verbatim 372-UI-SPEC + 374-UI-SPEC. Pola reuse Bootstrap existing (card, form-switch, alert-info lock, alert-warning).

Output: ~40-60 baris Razor disisip + ~15 baris JS di `@section Scripts`. Diakhiri UAT browser checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-CONTEXT.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-UI-SPEC.md
@.planning/phases/372-data-foundation-propagasi-toggle/372-UI-SPEC.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-02-SUMMARY.md

<interfaces>
<!-- ViewBag dari Plan 02 (ManagePackages GET). Render dari ini, JANGAN hitung ulang. -->

ViewBag.AssessmentId          (int)
ViewBag.ShuffleQuestions      (bool)   saved Acak Soal -> checked
ViewBag.ShuffleOptions        (bool)   saved Acak Pilihan -> checked
ViewBag.IsShuffleLocked       (bool)   true -> disabled switch+tombol + lock banner
ViewBag.HideShuffleToggle     (bool)   true -> seluruh card TIDAK dirender
ViewBag.ShowSizeMismatchWarning (bool) initial server-render warning §9
ViewBag.HasSizeMismatch       (bool)   untuk JS recompute
ViewBag.PackagesWithQuestions (int)    untuk JS recompute (>=2 precondition)
ViewBag.IsPostSession         (bool)   existing
ViewBag.PreShuffleQuestions   (bool?)  null/false/true — reminder Pre/Post (Post only)

Existing view (VERIFIED ManagePackages.cshtml):
- L19-27 = TempData Success/Error alert blocks (sudah ada — JANGAN duplikasi).
- L29-44 = SamePackage lock alert (POLA CETAKAN lock banner: `alert alert-info d-flex align-items-center mb-3` role=alert + `bi-lock-fill me-2 fs-5`).
- L46-68 = CopyFromPre form (POLA form POST + @Html.AntiForgeryToken() + hidden input).
- L70-81 = blok @{} hitung hasMismatch/referenceCount IN-VIEW (variabel `hasMismatch`, `packages` dalam scope Razor).
- L83-114 = panel ringkasan paket (`<div class="card mb-4">`).
- L261-272 area = @section Scripts (pola vanilla JS, no framework).

Pola form-switch (VERIFIED CreateAssessment.cshtml form-switch IsTokenRequired): `div.form-check.form-switch.mb-2` > `input.form-check-input[type=checkbox]` + `label.form-check-label[for]`, lalu `div.form-text.text-muted`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sisip card Pengacakan (2 toggle + tombol Simpan + lock alert + reminder + warning slot)</name>
  <files>Views/Admin/ManagePackages.cshtml</files>
  <read_first>
    - Views/Admin/ManagePackages.cshtml — RE-GREP anchor `@if (ViewBag.IsSamePackageLocked == true)` dan blok `@{` yang menghitung `hasMismatch` (sekitar `bool hasMismatch = false;`) dan panel `<div class="card mb-4">` ringkasan. Card baru disisip SETELAH CopyFromPre form, SEBELUM panel ringkasan paket.
    - .planning/phases/374-ui-managepackages-lock-pre-post/374-UI-SPEC.md (Copywriting Contract + Color + Interaction Contract — VERBATIM)
    - .planning/phases/372-data-foundation-propagasi-toggle/372-UI-SPEC.md (help-text toggle VERBATIM)
  </read_first>
  <action>
    Sisip blok Razor berikut SETELAH CopyFromPre form (re-grep anchor `id="copyPreBtn"` closing, atau blok `@if (ViewBag.IsSamePackageLocked == true)`), SEBELUM panel ringkasan paket (`<div class="card mb-4">`). Render murni dari ViewBag (Plan 02 sudah emit semua precondition — JANGAN bergantung urutan var Razor view, JANGAN hitung ulang).

    Markup card (copy VERBATIM string Bahasa Indonesia):

    ```html
    @if (ViewBag.HideShuffleToggle != true)
    {
        bool isShuffleLocked = ViewBag.IsShuffleLocked == true;
        bool sqChecked = ViewBag.ShuffleQuestions == true;
        bool soChecked = ViewBag.ShuffleOptions == true;
        <div class="card mb-4">
            <div class="card-header bg-light">
                <h5 class="mb-0"><i class="bi bi-shuffle text-primary me-1"></i>Pengacakan Soal &amp; Jawaban</h5>
            </div>
            <div class="card-body">
                @if (isShuffleLocked)
                {
                    <div class="alert alert-info d-flex align-items-center mb-3" role="alert">
                        <i class="bi bi-lock-fill me-2 fs-5"></i>
                        <div>
                            <strong>Pengaturan pengacakan terkunci.</strong>
                            Sudah ada peserta yang memulai ujian, sehingga pengaturan Acak Soal dan Acak Pilihan Jawaban tidak dapat diubah lagi untuk menjaga keadilan ujian.
                        </div>
                    </div>
                }
                <form method="post" asp-action="UpdateShuffleSettings" asp-controller="AssessmentAdmin">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="assessmentId" value="@ViewBag.AssessmentId" />
                    <div class="form-check form-switch mb-2">
                        <input class="form-check-input" type="checkbox" name="shuffleQuestions" id="shuffleQuestions" value="true" @(sqChecked ? "checked" : "") @(isShuffleLocked ? "disabled" : "") />
                        <label class="form-check-label" for="shuffleQuestions">Acak Soal</label>
                    </div>
                    <div class="form-text text-muted mb-3">Saat aktif, urutan dan pemilihan soal diacak berbeda untuk setiap peserta — sehingga peserta yang duduk berdekatan kecil kemungkinan mendapat soal yang sama urutannya. Saat nonaktif, semua peserta mendapat soal yang sama dengan urutan yang sama persis (urutan asli sesuai paket soal).</div>
                    <div class="form-check form-switch mb-2">
                        <input class="form-check-input" type="checkbox" name="shuffleOptions" id="shuffleOptions" value="true" @(soChecked ? "checked" : "") @(isShuffleLocked ? "disabled" : "") />
                        <label class="form-check-label" for="shuffleOptions">Acak Pilihan Jawaban</label>
                    </div>
                    <div class="form-text text-muted mb-3">Saat aktif, urutan pilihan jawaban (A, B, C, D) diacak berbeda di setiap soal untuk tiap peserta — jawaban benar tetap dinilai dengan benar, hanya posisinya yang berpindah. Saat nonaktif, urutan pilihan jawaban tampil sama untuk semua peserta sesuai urutan yang tersimpan di sistem.</div>
                    @if (ViewBag.IsPostSession == true && ViewBag.PreShuffleQuestions == false && sqChecked)
                    {
                        <div class="alert alert-warning d-flex align-items-center mb-3" role="alert">
                            <i class="bi bi-question-circle me-2"></i>
                            <div>Pre diatur OFF, Post masih ON — sengaja?</div>
                        </div>
                    }
                    <div id="shuffleSizeWarning" class="alert alert-warning d-flex align-items-center mb-3 @(ViewBag.ShowSizeMismatchWarning == true ? "" : "d-none")" role="alert">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <div>Jumlah soal antar paket berbeda — nilai maksimal &amp; pass% bisa tak setara antar peserta.</div>
                    </div>
                    <button type="submit" class="btn btn-primary" @(isShuffleLocked ? "disabled" : "")><i class="bi bi-save me-1"></i>Simpan Pengaturan</button>
                </form>
            </div>
        </div>
    }
    ```

    PENTING (VERBATIM copy — JANGAN dipangkas/ubah):
    - Help-text Toggle 1 & Toggle 2 PERSIS dari 372-UI-SPEC. Frasa "jawaban benar tetap dinilai dengan benar" WAJIB ada di Toggle 2.
    - Reminder Pre/Post: kondisi `ViewBag.IsPostSession == true && ViewBag.PreShuffleQuestions == false && sqChecked` (saved Post ON = sqChecked dari saved state; SHUF-13 opsi Z). TIDAK live JS — server-render. `PreShuffleQuestions == false` (BUKAN null) agar Pre tak-ada tidak memicu reminder.
    - Warning §9 = `#shuffleSizeWarning` selalu DI-RENDER tapi `d-none` bila tidak perlu; JS Task 2 toggle visibility. Class `alert alert-warning d-flex align-items-center mb-3`.
    - Lock banner copy VERBATIM (heading "Pengaturan pengacakan terkunci." + body). Class `alert alert-info d-flex align-items-center mb-3` (pola SamePackage lock).
    - `&amp;` untuk "&" dalam judul card + warning text (Razor encoding).
    - JANGAN pakai alert-danger/btn-danger di card ini.
    - SamePackage independensi: card TIDAK boleh disabled oleh `ViewBag.IsSamePackageLocked`. Lock HANYA dari `ViewBag.IsShuffleLocked`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `ManagePackages.cshtml` contains `Pengacakan Soal`
    - `ManagePackages.cshtml` contains `asp-action="UpdateShuffleSettings"`
    - `ManagePackages.cshtml` contains `@Html.AntiForgeryToken()` inside the shuffle form
    - `ManagePackages.cshtml` contains `bi-shuffle text-primary`
    - `ManagePackages.cshtml` contains `name="shuffleQuestions"` and `name="shuffleOptions"`
    - `ManagePackages.cshtml` contains `jawaban benar tetap dinilai dengan benar`
    - `ManagePackages.cshtml` contains `Pengaturan pengacakan terkunci.`
    - `ManagePackages.cshtml` contains `Pre diatur OFF, Post masih ON — sengaja?`
    - `ManagePackages.cshtml` contains `Jumlah soal antar paket berbeda`
    - `ManagePackages.cshtml` contains `id="shuffleSizeWarning"`
    - `ManagePackages.cshtml` contains `ViewBag.HideShuffleToggle != true`
    - `ManagePackages.cshtml` contains `Simpan Pengaturan`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Card render dengan 2 toggle verbatim, lock banner, reminder Post-only, warning slot, tombol Simpan; hide-wrap @if; build hijau (Razor compile).</done>
</task>

<task type="auto">
  <name>Task 2: Live JS warning §9 recompute (@section Scripts)</name>
  <files>Views/Admin/ManagePackages.cshtml</files>
  <read_first>
    - Views/Admin/ManagePackages.cshtml — RE-GREP `@section Scripts` (bila ada — tambahkan blok di dalamnya; bila tidak ada, buat `@section Scripts { ... }` di akhir file). Pola vanilla JS existing (re-grep `confirmDeletePackage` atau script lain di view ini).
    - .planning/phases/374-ui-managepackages-lock-pre-post/374-UI-SPEC.md (Interaction Contract — warning live recompute)
  </read_first>
  <action>
    Tambah blok JS di `@section Scripts`. Emit precondition dari ViewBag (Pitfall 4 opsi b — JANGAN hitung ulang dari nol):

    ```html
    @section Scripts {
        <script>
        (function () {
            var sq = document.getElementById('shuffleQuestions');
            var warn = document.getElementById('shuffleSizeWarning');
            if (!sq || !warn) return;
            var hasMismatch = @((ViewBag.HasSizeMismatch == true) ? "true" : "false");
            var multiPkg = @(((int)(ViewBag.PackagesWithQuestions ?? 0)) >= 2 ? "true" : "false");
            function recompute() {
                // Warning muncul bila: >=2 paket-ber-soal AND Acak Soal OFF (state saat ini) AND ukuran beda.
                var show = multiPkg && hasMismatch && !sq.checked;
                warn.classList.toggle('d-none', !show);
            }
            sq.addEventListener('change', recompute);
            recompute(); // page load
        })();
        </script>
    }
    ```

    PENTING:
    - Bila view SUDAH punya `@section Scripts` existing, GABUNG blok script ini ke dalamnya (JANGAN deklarasi `@section Scripts` dua kali — Razor error). Re-grep `@section Scripts` dulu.
    - `ViewBag.PackagesWithQuestions` bisa null bila tak di-set — guard `?? 0`. Cast `(int)(ViewBag.PackagesWithQuestions ?? 0)` aman.
    - Predikat JS HARUS sinkron dengan `ShuffleToggleRules.ShouldShowSizeMismatchWarning` (multiPkg && hasMismatch && !checked) — sama logika, beda layer (server initial vs live).
    - JS hanya menyentuh Acak Soal (`shuffleQuestions`); Acak Pilihan tidak memengaruhi warning.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `ManagePackages.cshtml` contains `getElementById('shuffleSizeWarning')`
    - `ManagePackages.cshtml` contains `addEventListener('change'`
    - `ManagePackages.cshtml` contains `ViewBag.HasSizeMismatch`
    - `ManagePackages.cshtml` contains `classList.toggle('d-none'`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>JS recompute warning saat flip Acak Soal + page load, pakai precondition dari ViewBag (tidak hitung ulang).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT browser ManagePackages (CLAUDE.md verifikasi lokal wajib)</name>
  <action>
    Checkpoint UAT manual — lihat how-to-verify untuk 7 skenario. Jalankan `Authentication__UseActiveDirectory=false dotnet run`, buka `http://localhost:5277`, verifikasi card ManagePackages + endpoint + full suite. Tidak ada edit kode di task ini (checkpoint verifikasi pasca-automasi).
  </action>
  <what-built>
    Card "Pengacakan Soal & Jawaban" di ManagePackages: 2 toggle + Simpan Pengaturan (PRG), lock state, warning §9 live, reminder Pre/Post, hide Proton Th3/Manual. Endpoint UpdateShuffleSettings (guard+propagate+audit).
  </what-built>
  <how-to-verify>
    Jalankan lokal (AD lokal mati): `Authentication__UseActiveDirectory=false dotnet run` → buka `http://localhost:5277`.
    1. Buka ManagePackages assessment OJT online biasa (belum ada peserta mulai): card tampil, 2 toggle reflect saved state, frasa help-text "jawaban benar tetap dinilai dengan benar" tampil di Toggle 2.
    2. Flip Acak Soal OFF lalu klik "Simpan Pengaturan" → redirect (PRG) + alert hijau "Pengaturan pengacakan berhasil disimpan." Reload → toggle tetap OFF (tersimpan + propagate sibling).
    3. Multi-paket dengan ukuran soal berbeda + Acak Soal OFF → warning kuning "Jumlah soal antar paket berbeda..." muncul; flip ON → hilang (live JS, tanpa reload).
    4. Assessment dengan peserta sudah StartedAt → switch + tombol disabled + lock banner biru "Pengaturan pengacakan terkunci." (Opsional curl/devtools POST saat locked → TempData error merah, tidak tersimpan — server guard.)
    5. Post-Test yang Pre-nya OFF tapi Post ON → reminder kuning "Pre diatur OFF, Post masih ON — sengaja?" di Post; di Pre TIDAK ada reminder.
    6. Assessment Proton Tahun 3 ATAU Manual entry → seluruh card TIDAK ada (hide).
    7. Toggle tetap aktif walau SamePackage lock (Post SamePackage=true) → banner SamePackage tampil TAPI toggle shuffle tetap bisa diubah.
    Full suite: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` baseline hijau (Shuffle subset + 329/329 tidak regresi).
  </how-to-verify>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "Passed!|Failed!"</automated>
  </verify>
  <done>7 skenario UAT browser PASS + full suite hijau; user ketik "approved".</done>
  <resume-signal>Ketik "approved" atau jelaskan isu (sebutkan langkah # yang gagal).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin browser → form POST | Form mengirim assessmentId + 2 checkbox bool; token anti-forgery menyertai. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-374-csrf | Tampering | Form shuffle | mitigate | `@Html.AntiForgeryToken()` di form (endpoint `[ValidateAntiForgeryToken]` Plan 02). |
| T-374-lock | Tampering/Elevation | UI disabled state | mitigate (UX layer) | Switch+tombol `disabled` saat IsShuffleLocked; enforcement nyata = server guard Plan 02 (UI bukan satu-satunya). |
| XSS | Tampering | Copy statis Razor | accept | Semua copy literal statis (tidak ada interpolasi user-input ke markup); Razor auto-encode. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exits 0 (Razor compile).
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` baseline hijau.
- `dotnet run` → `http://localhost:5277` smoke ManagePackages render + Simpan PRG sukses (CLAUDE.md verifikasi lokal wajib).
- Grep: copy verbatim (frasa grading, lock heading, reminder, warning) ADA persis.
</verification>

<success_criteria>
- SHUF-10 UI: card + 2 toggle + tombol Simpan POST UpdateShuffleSettings, aktif walau SamePackage lock.
- SHUF-11 UI: disabled + lock banner saat started/assignment.
- SHUF-12: warning §9 live JS muncul/hilang saat flip.
- SHUF-13: reminder Post-only Pre OFF/Post ON, no auto-cascade.
- SHUF-14: hide total Proton Th3/Manual.
- Copy 372/374-UI-SPEC verbatim (frasa grading WAJIB ada).
</success_criteria>

<output>
After completion, create `.planning/phases/374-ui-managepackages-lock-pre-post/374-03-SUMMARY.md`.
Catat: lokasi sisip card (anchor), apakah @section Scripts baru/gabung, hasil UAT 7 langkah, deviasi copy bila ada (seharusnya nol).
</output>
