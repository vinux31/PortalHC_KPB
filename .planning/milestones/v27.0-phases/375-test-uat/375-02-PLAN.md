---
phase: 375-test-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/shuffle.spec.ts
autonomous: true
requirements: [SHUF-16]
must_haves:
  truths:
    - "shuffle.spec.ts merender card Pengacakan + toggle saved-state, Simpan → success PRG (.alert-success)"
    - "Lock state: switch + tombol disabled + banner muncul saat ada peserta started"
    - "Reminder Pre/Post muncul di Post saat Pre OFF + Post ON"
    - "Warning §9 live-JS flip: multi-paket ukuran beda → flip Acak Soal OFF muncul, ON hilang"
    - "Hide: card Pengacakan tidak dirender untuk Proton Tahun 3 / Manual"
  artifacts:
    - path: "tests/e2e/shuffle.spec.ts"
      provides: "Playwright UAT 5 skenario ManagePackages (render + save-PRG) — regresi-proof"
      contains: ".alert-success"
      min_lines: 120
  key_links:
    - from: "tests/e2e/shuffle.spec.ts"
      to: "/Admin/ManagePackages?assessmentId=X"
      via: "createAssessmentViaWizard + card selectors (#shuffleQuestions/#shuffleOptions/#shuffleSizeWarning)"
      pattern: "#shuffleQuestions|#shuffleOptions"
    - from: "tests/e2e/shuffle.spec.ts"
      to: "/Admin/UpdateShuffleSettings"
      via: "button Simpan Pengaturan submit → PRG → .alert-success"
      pattern: "Simpan Pengaturan"
---

<objective>
Automate sisi ManagePackages dari UAT shuffle toggle (Phase 374 sudah manual-verified 7/7) jadi `tests/e2e/shuffle.spec.ts` regresi-proof — 5 skenario render + save-PRG (D-02a). Setup pakai `createAssessmentViaWizard` (D-06, JANGAN flat-form Phase 364). Exam-taking effect TIDAK di-cover di sini (itu manual D-03 di Plan 03); propagate detail TIDAK di-assert ulang (sudah unit `ShufflePropagationTests`/`ShuffleUpdateEndpointTests`).

Purpose: Render-conditional yang di-defer dari Phase 374 (hide/reminder/warning-visibility/lock-disabled — 374-VALIDATION Manual-Only rows) di-cover otomatis di sini. Bagian dari bukti SC#2 (sisi ManagePackages); exam-effect = manual.
Output: `tests/e2e/shuffle.spec.ts` (baru), 5 skenario hijau via `npx playwright test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/375-test-uat/375-CONTEXT.md
@.planning/phases/375-test-uat/375-PATTERNS.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-UI-SPEC.md

<interfaces>
<!-- Helper signatures dari tests/e2e/helpers/examTypes.ts + selectors — executor pakai langsung. -->

```typescript
// tests/e2e/helpers/examTypes.ts
export async function createAssessmentViaWizard(page: Page, opts: CreateAssessmentOpts): Promise<void>;
//   opts: { title, category, scheduleDate (YYYY-MM-DD), scheduleTime?, durationMinutes,
//           passPercentage, allowAnswerReview, generateCertificate?, participantEmails: string[] }
//   Title WAJIB match ^(Pre|Post)\s*Test\s+.+$ untuk standard (AssessmentAdminController:866-874).
//   Setelah submit → success modal STATIC backdrop muncul; caller WAJIB dismiss.
export async function createDefaultPackage(page: Page, packageName?='Paket A'): Promise<number>;  // returns packageId
export async function addQuestionViaForm(page, pkgId, { type:'MultipleChoice', text, options:[..], correctIndex, score }): Promise<void>;

// tests/e2e/helpers/wizardSelectors.ts
wizardSelectors.modalManageBtn === '#modal-manage-btn';   // dismiss success modal → arrive ManagePackages

// tests/helpers/auth.ts + accounts.ts
export async function login(page, 'admin'): Promise<void>;   // admin@pertamina.com / 123456

// tests/helpers/dbSnapshot.ts
await db.backup(snapshotPath); await db.restore(snapshotPath); await db.queryString(sql);
```

ManagePackages route + card selectors (Views/Admin/ManagePackages.cshtml — VERBATIM, NYATA):
```typescript
// Route: /Admin/ManagePackages?assessmentId={id}   (controller [Route("Admin/[action]")])
const card        = page.locator('.card', { hasText: 'Pengacakan Soal & Jawaban' });
const swQuestions = page.locator('#shuffleQuestions');   // "Acak Soal"
const swOptions   = page.locator('#shuffleOptions');     // "Acak Pilihan Jawaban"
const sizeWarning = page.locator('#shuffleSizeWarning'); // alert §9 (d-none saat tak relevan)
const lockBanner  = card.locator('.alert-info', { hasText: 'Pengaturan pengacakan terkunci' });
const postReminder= card.locator('.alert-warning', { hasText: 'Pre diatur OFF, Post masih ON' });
const saveBtn     = card.locator('button[type="submit"]:has-text("Simpan Pengaturan")');
// Save success (PRG): page.locator('.alert-success', { hasText: 'berhasil disimpan' })
//   ("Pengaturan pengacakan berhasil disimpan." — endpoint /Admin/UpdateShuffleSettings RedirectToAction)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold shuffle.spec.ts (serial + DB snapshot lifecycle + wizard skeleton)</name>
  <read_first>
    - tests/e2e/image-in-assessment.spec.ts (analog template proven: imports lines 21-25, `mode:'serial'` lines 40-42, beforeAll/afterAll DB backup/restore lines 44-69, wizard-create lines 76-115, text-based option selectors lines 144-148)
    - tests/e2e/helpers/examTypes.ts (createAssessmentViaWizard signature lines 23-104, createDefaultPackage lines 121-138, addQuestionViaForm lines 168+)
    - tests/e2e/helpers/wizardSelectors.ts (modalManageBtn = '#modal-manage-btn' line 73)
    - tests/playwright.config.ts (testDir ./e2e, baseURL localhost:5277, fullyParallel:false)
    - .planning/phases/375-test-uat/375-PATTERNS.md (§ shuffle.spec.ts — imports/serial/beforeAll/wizard skeleton copy verbatim)
  </read_first>
  <action>
    Buat file BARU `tests/e2e/shuffle.spec.ts`. Copy struktur dari `image-in-assessment.spec.ts`:

    Imports (drop image-only imports):
    ```typescript
    import { test, expect } from '@playwright/test';
    import * as db from '../helpers/dbSnapshot';
    import { login } from '../helpers/auth';
    import { createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm } from './helpers/examTypes';
    import * as wizardSelectors from './helpers/wizardSelectors';   // sesuaikan dgn cara import existing di image-in-assessment.spec.ts
    ```

    Serial + describe:
    ```typescript
    test.describe.configure({ mode: 'serial' });   // WAJIB — DB isolation (playwright.config fullyParallel:false)
    test.describe('Phase 375 — Shuffle Toggle ManagePackages (UAT e2e)', () => { ... });
    ```

    DB snapshot lifecycle beforeAll/afterAll (copy struktur image-in-assessment.spec.ts lines 44-69, DROP fs upload-cleanup karena shuffle tak upload file): `InstanceDefaultBackupPath` query → `snapshotPath = .../HcPortalDB_Dev-pre375-{ts}.bak` → `db.backup` di beforeAll; `db.restore` + `fs.unlinkSync` best-effort di afterAll dengan re-throw error.

    Helper lokal `today()`:
    ```typescript
    const today = () => new Date().toISOString().slice(0, 10);
    ```

    Tambah helper internal `arriveAtManagePackages(page, title)` (dipakai banyak skenario): `await login(page,'admin')` → `createAssessmentViaWizard(page, { title, category:'OJT', scheduleDate: today(), scheduleTime:'00:01', durationMinutes:60, passPercentage:50, allowAnswerReview:true, generateCertificate:false, participantEmails:['rino.prasetyo@pertamina.com'] })` → dismiss modal `await page.locator(wizardSelectors.modalManageBtn).click()` → `await page.waitForLoadState('networkidle')` (page kini di `/Admin/ManagePackages?assessmentId={id}`). Title WAJIB pola `Pre Test OJT SHUF375 ${Date.now()}`.

    JANGAN: pakai flat-form create, JANGAN sentuh exam-taking.spec.ts / exam-types.spec.ts. `shuffle.spec.ts` berdiri sendiri.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff\tests"; npx tsc --noEmit -p tsconfig.json 2>&1 | Select-String "shuffle.spec" ; echo "TS-CHECK-DONE"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/shuffle.spec.ts` ada
    - Berisi `test.describe.configure({ mode: 'serial' })` dan `createAssessmentViaWizard` (grep match)
    - Berisi `db.backup` dan `db.restore` (DB snapshot lifecycle)
    - TIDAK berisi referensi flat-form / `exam-taking.spec` / `exam-types.spec`
    - `npx tsc --noEmit` tidak melaporkan error untuk `shuffle.spec.ts` (TS-CHECK-DONE muncul tanpa error shuffle.spec di atasnya)
  </acceptance_criteria>
  <done>shuffle.spec.ts scaffold lengkap (serial, DB lifecycle, wizard helper) — type-clean, siap diisi 5 skenario.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementasi 5 skenario render + save-PRG</name>
  <read_first>
    - .planning/phases/375-test-uat/375-PATTERNS.md (§ Scenario 1-5 — kode skenario verbatim + selectors NYATA)
    - .planning/phases/374-ui-managepackages-lock-pre-post/374-UI-SPEC.md (Copywriting Contract — copy alert verbatim; Interaction Contract — lock/reminder/warning visibility rules)
    - Views/Admin/ManagePackages.cshtml (verifikasi: card wrap `@if (ViewBag.HideShuffleToggle != true)`, `#shuffleQuestions`/`#shuffleOptions` checked default, JS `addEventListener('change')` #shuffleSizeWarning d-none toggle, reminder `@if ViewBag.IsPostSession && PreShuffleQuestions==false`, lock `@(isShuffleLocked?"disabled":"")`)
  </read_first>
  <behavior>
    - Scenario 1: card visible; #shuffleQuestions & #shuffleOptions default checked (migration ON); uncheck Acak Soal → klik Simpan → `.alert-success` "berhasil disimpan" muncul (PRG)
    - Scenario 2: assessment dgn peserta started → lockBanner visible, kedua switch disabled, saveBtn disabled
    - Scenario 3: halaman Post (Pre OFF + Post ON) → postReminder visible; di Pre TIDAK ada
    - Scenario 4: multi-paket ukuran beda + Acak Soal di-uncheck → sizeWarning visible; check kembali → sizeWarning hidden (live JS, no reload)
    - Scenario 5: assessment Proton Tahun 3 / Manual → card Pengacakan toHaveCount(0) (tidak dirender)
  </behavior>
  <action>
    Tambah 5 `test(...)` di dalam describe. Pakai selectors NYATA dari interfaces block. Copy detail kode dari PATTERNS § Scenario 1-5:

    **Scenario 1 — card render + saved-state + Simpan PRG** (assert `.alert-success` saja, D-02a TempData success only):
    `arriveAtManagePackages` → `createDefaultPackage` → `addQuestionViaForm(MultipleChoice, 4 opsi)` → kembali ke ManagePackages (goto `/Admin/ManagePackages?assessmentId={id}` bila perlu) → `await expect(card).toBeVisible()` → `await expect(swQuestions).toBeChecked()` + `swOptions` checked → `await swQuestions.uncheck()` → `await saveBtn.click()` → `waitForLoadState('networkidle')` → `await expect(page.locator('.alert-success', { hasText: 'berhasil disimpan' })).toBeVisible()`.

    **Scenario 2 — lock disabled + banner**: setup assessment dgn ≥1 peserta yang sudah `StartedAt != null` (cara paling stabil tanpa exam-taking flat-form: insert `UserPackageAssignment` grup atau set `StartedAt` via `db` exec SQL pada session yang baru dibuat — gunakan `db.queryString`/exec helper untuk UPDATE `AssessmentSessions SET StartedAt=GETDATE()` ATAU insert assignment; pilih path yang konsisten dgn `IsShuffleLocked` controller). Lalu reload ManagePackages → `await expect(lockBanner).toBeVisible()` + `swQuestions`/`swOptions`/`saveBtn` `toBeDisabled()`.

    **Scenario 3 — reminder Pre/Post**: buat Pre+Post linked (atau set ViewBag prasyarat via saved-state: Pre `ShuffleQuestions=false`, Post `ShuffleQuestions=true`). Buka halaman Post → `await expect(postReminder).toBeVisible()`. Buka halaman Pre → `await expect(card.locator('.alert-warning', { hasText: 'Pre diatur OFF' })).toHaveCount(0)` (no cascade).

    **Scenario 4 — warning §9 live-JS flip**: assessment multi-paket (`createDefaultPackage` 2× dgn jumlah soal beda → hasMismatch) → di ManagePackages: `await swQuestions.uncheck()` → `await expect(sizeWarning).toBeVisible()` → `await swQuestions.check()` → `await expect(sizeWarning).toBeHidden()` (live JS d-none, no reload).

    **Scenario 5 — hide Proton-Th3 / Manual**: buat assessment Proton Tahun 3 (`category` Proton + TahunKe Tahun 3) ATAU IsManualEntry → buka ManagePackages → `await expect(page.locator('.card', { hasText: 'Pengacakan Soal & Jawaban' })).toHaveCount(0)`.

    Untuk Scenario 2/3/5 yang butuh state khusus (started / Pre-Post linked / Proton-Th3): bila wizard tak bisa langsung set, pakai `db` exec SQL UPDATE pada record yang baru dibuat wizard (snapshot beforeAll/restore afterAll melindungi). DOKUMENTASIKAN setiap SQL setup di komentar. JANGAN re-assert propagate detail (sudah unit-tested). Gunakan text-based selectors (`hasText`) BUKAN `.nth()` posisi.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff\tests"; $env:Authentication__UseActiveDirectory="false"; npx playwright test e2e/shuffle.spec.ts --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/shuffle.spec.ts` berisi 5 `test(` block + string `.alert-success`, `lockBanner`/`Pengaturan pengacakan terkunci`, `Pre diatur OFF`, `#shuffleSizeWarning`, `toHaveCount(0)` (grep semua match)
    - `npx playwright test e2e/shuffle.spec.ts --workers=1` → 5 passed, 0 failed
    - DB ter-restore setelah run (afterAll restore — DB lokal bersih, no seed nempel)
  </acceptance_criteria>
  <done>5 skenario ManagePackages hijau via Playwright; DB ter-restore; render-conditional Phase 374 (hide/reminder/warning/lock) ter-automate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only — Playwright e2e read/exercise UI existing. Tak ada production code/endpoint baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-375-02 | I (Info Disclosure) | e2e seed data di DB lokal | mitigate | DB `backup` di beforeAll → `restore` di afterAll (image-in-assessment.spec.ts pola) → seed test tidak nempel di DB lokal. `dbSnapshot.ts` localhost-guard menolak non-localhost target. No new production attack surface — test-only phase. |
</threat_model>

<verification>
- `npx playwright test e2e/shuffle.spec.ts --workers=1` (env `Authentication__UseActiveDirectory=false`) → 5 passed
- `npx tsc --noEmit` → no error shuffle.spec.ts
- DB lokal ter-restore (afterAll)
</verification>

<success_criteria>
5 skenario ManagePackages (card render+save-PRG, lock, reminder Pre/Post, warning §9 live-JS, hide) hijau otomatis — render-conditional Phase 374 ter-cover. Bagian sisi-ManagePackages dari SC#2; exam-effect = manual (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/375-test-uat/375-02-SUMMARY.md`
</output>
