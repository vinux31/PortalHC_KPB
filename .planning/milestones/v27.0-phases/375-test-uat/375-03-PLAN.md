---
phase: 375-test-uat
plan: 03
type: execute
wave: 2
depends_on: ["375-01", "375-02"]
files_modified:
  - .planning/phases/375-test-uat/375-HUMAN-UAT.md
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [SHUF-16]
must_haves:
  truths:
    - "ShuffleQuestions ON → urutan soal beda antar 2 peserta (visual + screenshot)"
    - "ShuffleOptions ON → urutan opsi beda antar 2 peserta"
    - "ShuffleQuestions OFF + ≥2 paket → tiap worker 1 paket UTUH urutan asli (round-robin index)"
    - "DB lokal ter-restore setelah exam-diff (seed temporary tidak nempel)"
    - "375-HUMAN-UAT.md mencatat hasil manual exam-diff + 5 skenario e2e ManagePackages, status partial"
  artifacts:
    - path: ".planning/phases/375-test-uat/375-HUMAN-UAT.md"
      provides: "Dokumen UAT — bukti SC#2 (exam-effect manual + ManagePackages e2e)"
      contains: "status: partial"
      min_lines: 30
    - path: "docs/SEED_JOURNAL.md"
      provides: "Entry seed temporary multi-paket+2 peserta dengan status cleaned"
      contains: "cleaned"
  key_links:
    - from: "manual exam-diff (2 peserta StartExam)"
      to: "/CMP/StartExam/{sessionId}"
      via: "banding urutan soal/opsi ON vs OFF visual"
      pattern: "StartExam"
    - from: "375-HUMAN-UAT.md"
      to: "SC#2 pass-bar D-03a"
      via: "human-verify checkpoint approve"
      pattern: "status: partial"
---

<objective>
Manual browser exam-diff 2 peserta untuk membuktikan toggle shuffle BEREFEK di pengalaman ujian (SC#2, D-03/D-03a) — efek wiring engine→view yang TIDAK layak unit/automated (Razor/JS runtime; full automated order-diff ditolak by design). Claude jalankan app + seed multi-paket + 2 peserta StartExam + banding urutan soal/opsi ON vs OFF + screenshot, lalu SATU checkpoint `human-verify` (pola Phase 374) — user approve. Catat semua di `375-HUMAN-UAT.md` (D-08, status partial) + kelola seed via snapshot/RESTORE + journal cleaned (D-04). Ini stop point TERAKHIR phase.

Purpose: SC#2 = "UAT @5277: toggle ON/OFF berefek di exam (urutan soal & opsi), lock & reminder & warning tampil benar". Sisi ManagePackages sudah otomatis (Plan 02). Sisi exam-effect = manual di sini. Manual-approve memenuhi SC#2; verifier terima manual-UAT sbg bukti (render-conditional + exam-effect = runtime, dijustifikasi 374-VALIDATION Manual-Only + D-08).
Output: `375-HUMAN-UAT.md` (status partial → updated saat approve), `docs/SEED_JOURNAL.md` entry cleaned, DB lokal restored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/375-test-uat/375-CONTEXT.md
@.planning/phases/375-test-uat/375-PATTERNS.md
@docs/SEED_WORKFLOW.md
@.planning/phases/373-shuffle-engine-read-logic-reshuffle/373-HUMAN-UAT.md
@.planning/phases/374-ui-managepackages-lock-pre-post/374-03-SUMMARY.md

<interfaces>
<!-- Routes + flow exam-taking + env lokal. -->

```
Env lokal (CLAUDE.md + memory reference_local_e2e_sql_env_fix):
  Authentication__UseActiveDirectory=false dotnet run     # AD off lokal, app @ http://localhost:5277
  # bila login 500 (NTLM loopback): tambah
  #   ConnectionStrings__DefaultConnection='Server=lpc:localhost\SQLEXPRESS;Database=HcPortalDB_Dev;...'

Akun (tests/helpers/accounts.ts):
  admin  → admin@pertamina.com / 123456   (setup ManagePackages)
  peserta diff → 'coachee' (rino.prasetyo@) + 'coachee2' (iwan3@)  — keduanya pwd 123456

Exam flow peserta:
  /CMP/StartExam/{sessionId}  → jawab (text-match label) → submit "Kumpulkan Ujian"

DB snapshot (SEED_WORKFLOW, D-04) — sqlcmd lokal:
  BACKUP DATABASE HcPortalDB_Dev TO DISK='...' WITH INIT, FORMAT
  RESTORE: SINGLE_USER ROLLBACK IMMEDIATE → RESTORE WITH REPLACE → MULTI_USER
  # sqlcmd UPDATE/DDL lokal WAJIB flag -I (memory: ManagePackages sqlcmd UPDATE wajib -I)
```

SC#2 pass-bar EXACT (D-03a):
- ShuffleQuestions ON  → urutan soal BEDA antar 2 peserta
- ShuffleOptions   ON  → urutan opsi BEDA antar 2 peserta
- ShuffleQuestions OFF + ≥2 paket → tiap worker 1 paket UTUH urutan asli (round-robin index, paket beda per worker)
- Bukti = visual browser + screenshot
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot DB + seed assessment multi-paket + 2 peserta + jalankan exam-diff</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_WORKFLOW.md (klasifikasi temporary+local-only, command BACKUP/RESTORE, format journal)
    - docs/SEED_JOURNAL.md (format entry existing)
    - .planning/phases/375-test-uat/375-PATTERNS.md (§ HUMAN-UAT exam-effect D-03 manual + § Shared Patterns DB snapshot/restore)
    - tests/helpers/dbSnapshot.ts (BACKUP/RESTORE shape, localhost-guard)
    - tests/e2e/helpers/examTypes.ts (StartExam flow route + submit "Kumpulkan Ujian")
  </read_first>
  <action>
    Per SEED_WORKFLOW (D-04, klasifikasi `temporary + local-only`):

    1. **Snapshot DB lokal SEBELUM seed**: `BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev_pre375uat_{ts}.bak' WITH INIT, FORMAT` (sqlcmd, flag `-I`). Catat path snapshot.
    2. **Catat di `docs/SEED_JOURNAL.md`**: entry baru — tujuan "Phase 375 manual exam-diff SC#2", klasifikasi `temporary + local-only`, entitas tersentuh (AssessmentSession + AssessmentPackage + PackageQuestion/Option + UserPackageAssignment + 2 peserta assignment), status awal `active`, path snapshot.
    3. **Start app**: `Authentication__UseActiveDirectory=false dotnet run` → `http://localhost:5277` (bila login 500 → tambah `lpc:` conn override). JANGAN edit DB Dev/Prod.
    4. **Seed via UI (login admin)**: buat 1 assessment online berbasis paket dgn ≥2 paket (jumlah soal cukup untuk lihat urutan beda, mis. 2 paket × 4-5 soal MultipleChoice, tiap soal ≥3 opsi). Assign 2 peserta (coachee + coachee2). Setup pakai pola wizard/ManagePackages (BUKAN flat-form). Atur toggle untuk tiap ronde uji (lihat langkah 5).
    5. **Exam-diff 2 peserta — 3 ronde** (login peserta via 2 browser context/sesi terpisah, route `/CMP/StartExam/{sessionId}`):
       - **Ronde A (ShuffleQuestions ON, ShuffleOptions ON)**: peserta1 vs peserta2 → catat & screenshot urutan soal (harus BEDA) + urutan opsi salah satu soal (harus BEDA).
       - **Ronde B (ShuffleOptions ON, fokus opsi)**: konfirmasi urutan opsi BEDA antar 2 peserta (boleh digabung Ronde A bila sudah jelas).
       - **Ronde C (ShuffleQuestions OFF + ≥2 paket)**: tiap peserta dapat 1 paket UTUH urutan asli (`q.Order`), paket BERBEDA per peserta (round-robin index). Screenshot bukti paket-utuh per worker.
       (Untuk ganti toggle antar ronde: ubah di ManagePackages SEBELUM peserta StartExam ronde itu; bila sudah locked karena started, buat assessment seed baru per ronde atau reset session — dokumentasikan.)
    6. Simpan screenshot (mis. `docs/uat-evidence/375-*.png` atau lampirkan path di HUMAN-UAT). Catat hasil tiap ronde (PASS/FAIL terhadap pass-bar D-03a) untuk Task 2.

    JANGAN: sentuh `exam-taking.spec.ts` `.fixme` / flat-form (D-06). JANGAN sentuh bug SURF-317-A MultipleAnswer 500 (out of scope — pakai MultipleChoice/single-answer). JANGAN biarkan seed nempel — RESTORE di Task 4.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff"; Test-Path docs/SEED_JOURNAL.md; Select-String -Path docs/SEED_JOURNAL.md -Pattern "375" -SimpleMatch | Select-Object -First 1</automated>
  </verify>
  <acceptance_criteria>
    - Snapshot `.bak` pre-UAT dibuat (path tercatat di SEED_JOURNAL)
    - `docs/SEED_JOURNAL.md` punya entry Phase 375 dgn klasifikasi `temporary + local-only`
    - 3 ronde exam-diff dijalankan; hasil + screenshot tercatat untuk tiap pass-bar D-03a (ON soal beda / ON opsi beda / OFF ≥2 paket round-robin paket utuh)
    - App jalan @localhost:5277 (env `Authentication__UseActiveDirectory=false`)
  </acceptance_criteria>
  <done>Seed multi-paket+2 peserta dibuat (snapshot dulu), 3 ronde exam-diff dijalankan dgn screenshot, hasil siap dicatat. Seed belum di-restore (Task 4).</done>
</task>

<task type="auto">
  <name>Task 2: Tulis 375-HUMAN-UAT.md (hasil exam-diff + 5 skenario e2e)</name>
  <files>.planning/phases/375-test-uat/375-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/373-shuffle-engine-read-logic-reshuffle/373-HUMAN-UAT.md (analog frontmatter `status: partial` + struktur Tests/Summary/Gaps)
    - .planning/phases/374-ui-managepackages-lock-pre-post/374-03-SUMMARY.md (tabel "UAT Browser — 7/7 PASS" pola untuk 5 skenario e2e + exam-diff)
    - .planning/phases/375-test-uat/375-02-SUMMARY.md (hasil 5 skenario Playwright dari Plan 02 — referensi untuk tabel ManagePackages)
  </read_first>
  <action>
    Buat `.planning/phases/375-test-uat/375-HUMAN-UAT.md` (pola 373-HUMAN-UAT.md, D-08, status `partial`):

    Frontmatter:
    ```markdown
    ---
    status: partial
    phase: 375-test-uat
    source: [375-VERIFICATION.md]
    started: 2026-06-13
    updated: 2026-06-13
    ---
    ```

    Isi:
    - `## Current Test` — instruksi run lokal (`Authentication__UseActiveDirectory=false dotnet run` @localhost:5277).
    - `## Tests` — 2 grup:
      - **Grup A — ManagePackages e2e (5 skenario, dari Plan 02)**: tabel pola 374-03-SUMMARY `| # | Skenario | Hasil |` — card render+save-PRG, lock disabled+banner, reminder Pre/Post, warning §9 live-JS, hide Proton-Th3/Manual. Status PASS (referensi hasil Playwright 375-02).
      - **Grup B — Exam-taking effect manual (SC#2, D-03a) — pending human-verify**: 3 baris dengan `expected` + `result: [pending]`:
        1. ShuffleQuestions ON → urutan soal beda antar 2 peserta (lampirkan screenshot path).
        2. ShuffleOptions ON → urutan opsi beda antar 2 peserta.
        3. ShuffleQuestions OFF + ≥2 paket → tiap worker 1 paket UTUH urutan asli (round-robin index).
    - `## Summary` — total/passed/issues/pending (Grup B pending sampai user approve di checkpoint Task 3).
    - `## Gaps` — none (atau catat bila ronde gagal).

    Isi `result` Grup B dengan observasi Task 1 (PASS/screenshot) tapi tandai `status: partial` sampai checkpoint approve.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff"; Select-String -Path .planning/phases/375-test-uat/375-HUMAN-UAT.md -Pattern "status: partial" -SimpleMatch</automated>
  </verify>
  <acceptance_criteria>
    - `375-HUMAN-UAT.md` ada, frontmatter berisi `status: partial`
    - Berisi tabel Grup A (5 skenario ManagePackages) + Grup B (3 baris exam-effect SC#2 pass-bar D-03a)
    - Grup B mereferensi screenshot bukti exam-diff (path tercatat)
  </acceptance_criteria>
  <done>375-HUMAN-UAT.md lengkap — 5 skenario e2e PASS + 3 baris exam-effect terisi observasi, status partial menunggu approve.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint UAT akhir — approve exam-diff</name>
  <action>
    Checkpoint human-verify (pola Phase 374) — Claude PAUSE, sajikan bukti exam-diff (375-HUMAN-UAT.md + screenshot) ke user, tunggu approve. JANGAN lanjut Task 4 sebelum user respon. Lihat what-built / how-to-verify / resume-signal.
  </action>
  <what-built>
    Claude sudah: (1) jalankan full xUnit suite hijau ≥348 incl consolidation sweep (Plan 01), (2) 5 skenario Playwright ManagePackages hijau (Plan 02), (3) seed multi-paket + 2 peserta + 3 ronde exam-diff manual @localhost:5277 dgn screenshot (Task 1), (4) catat semua di 375-HUMAN-UAT.md status partial (Task 2). SHUF-15 confirmed clean.
  </what-built>
  <how-to-verify>
    Tinjau bukti exam-diff SC#2 (D-03a pass-bar) di 375-HUMAN-UAT.md + screenshot:
    1. **Acak Soal ON** → urutan soal yang dilihat peserta 1 BEDA dari peserta 2? (lihat screenshot Ronde A)
    2. **Acak Pilihan ON** → urutan opsi (A/B/C/D) salah satu soal BEDA antar 2 peserta?
    3. **Acak Soal OFF + ≥2 paket** → tiap peserta dapat 1 paket UTUH urutan asli, dan paket BERBEDA antar peserta (round-robin index)? (lihat screenshot Ronde C)
    Bila perlu, jalankan ulang manual: `Authentication__UseActiveDirectory=false dotnet run` → login peserta → `/CMP/StartExam/{sessionId}`.
  </how-to-verify>
  <verify>Checkpoint — user approve manual; tidak ada automated verify (Task 4 melakukan finalize).</verify>
  <done>User mengetik "approved" (atau menjelaskan ronde gagal) → checkpoint selesai, lanjut Task 4.</done>
  <resume-signal>Ketik "approved" bila ketiga pass-bar SC#2 terpenuhi (atau jelaskan ronde yang gagal). Setelah approve (atau apa pun hasilnya), Claude WAJIB lanjut Task 4: RESTORE DB lokal dari snapshot pre-UAT + tandai docs/SEED_JOURNAL.md `cleaned` + finalize 375-HUMAN-UAT.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: RESTORE DB + tandai journal cleaned + finalize UAT doc (post-checkpoint)</name>
  <files>docs/SEED_JOURNAL.md, .planning/phases/375-test-uat/375-HUMAN-UAT.md</files>
  <read_first>
    - docs/SEED_WORKFLOW.md (command RESTORE + format journal cleaned)
    - docs/SEED_JOURNAL.md (entry Phase 375 dari Task 1)
  </read_first>
  <action>
    Jalankan SETELAH checkpoint Task 3 — WAJIB dijalankan apa pun hasilnya (D-04 "restore after test success OR fail"):

    1. **RESTORE DB lokal** dari snapshot pre-UAT (Task 1): `ALTER DATABASE HcPortalDB_Dev SET SINGLE_USER WITH ROLLBACK IMMEDIATE` → `RESTORE DATABASE HcPortalDB_Dev FROM DISK='...pre375uat...bak' WITH REPLACE` → `ALTER DATABASE HcPortalDB_Dev SET MULTI_USER` (sqlcmd flag `-I`). Verifikasi seed assessment SHUF375 sudah hilang.
    2. **Tandai `docs/SEED_JOURNAL.md`** entry Phase 375 → status `cleaned` (+ timestamp restore).
    3. **Update `375-HUMAN-UAT.md`**: Grup B `result: PASS` (bila approved), Summary `passed` naik, `pending: 0`. Status frontmatter tetap `partial` per D-08 (manual-approve memenuhi SC#2; verifier terima manual-UAT sbg bukti — render-conditional + exam-effect = Razor/JS runtime, dijustifikasi 374-VALIDATION Manual-Only). Bila ronde gagal → catat di `## Gaps`.

    JANGAN biarkan seed temporary nempel — RESTORE wajib walau hasil approve.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff"; Select-String -Path docs/SEED_JOURNAL.md -Pattern "cleaned" -SimpleMatch | Select-Object -Last 1</automated>
  </verify>
  <acceptance_criteria>
    - DB lokal ter-restore (seed assessment "SHUF375" tidak ada lagi di DB)
    - `docs/SEED_JOURNAL.md` entry Phase 375 status `cleaned`
    - `375-HUMAN-UAT.md` Grup B result terisi (PASS bila approved / Gaps bila fail), Summary updated
  </acceptance_criteria>
  <done>DB lokal bersih (seed di-restore), journal cleaned, UAT doc final — SC#2 evidence lengkap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test/UAT-only — manual exam-diff exercise UI existing. Tak ada production code/endpoint baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-375-03 | I (Info Disclosure) | seed temporary (assessment + 2 peserta) di DB lokal | mitigate | Snapshot DB SEBELUM seed → RESTORE SETELAH exam-diff (success OR fail, D-04) → seed temporary tidak nempel; tak ada credential/PII baru ditinggalkan. Journal `docs/SEED_JOURNAL.md` lacak lifecycle (active → cleaned). sqlcmd localhost-only, JANGAN edit DB Dev/Prod (CLAUDE.md). No new production attack surface — test-only phase. |
</threat_model>

<verification>
- 3 ronde exam-diff manual memenuhi pass-bar D-03a (ON soal beda / ON opsi beda / OFF ≥2 paket round-robin paket utuh) — visual + screenshot
- Checkpoint human-verify approved (SC#2)
- DB lokal ter-restore; `docs/SEED_JOURNAL.md` cleaned
- `375-HUMAN-UAT.md` status partial, Grup A 5 PASS + Grup B exam-effect terisi
</verification>

<success_criteria>
SC#2 phase 375 terpenuhi: UAT @5277 membuktikan toggle ON/OFF berefek di exam (urutan soal & opsi beda; OFF ≥2 paket round-robin paket utuh) + ManagePackages render benar (e2e Plan 02). Manual-approve checkpoint = bukti SC#2. Seed bersih (restored), journal cleaned.
</success_criteria>

<output>
After completion, create `.planning/phases/375-test-uat/375-03-SUMMARY.md`
</output>
