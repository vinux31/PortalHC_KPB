---
phase: 376-fix-essay-only-score-aggregation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md
  - HcPortal.Tests/AssessmentScoreAggregatorTests.cs
  - tests/e2e/exam-types.spec.ts
autonomous: true
requirements: [GRADE-01, GRADE-02]
must_haves:
  truths:
    - "Repro lokal essay-only finalize → Score=0 dikonfirmasi (SC1)"
    - "Root cause persis teridentifikasi (H1/H2/H3/H4) + didokumentasikan sebelum fix ditulis"
    - "Unit test helper agregasi ada (RED — helper belum dibuat)"
    - "e2e FLOW L6 un-fixme (RED sampai fix Plan 02/03 landing)"
  artifacts:
    - path: ".planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md"
      provides: "Temuan repro SC1 (ShuffledQuestionIds/QuestionType/EssayScore/rowsAffected) + root cause locked"
      min_lines: 30
    - path: "HcPortal.Tests/AssessmentScoreAggregatorTests.cs"
      provides: "Unit test murni helper agregasi (essay-only, mixed no-drift, maxScore=0)"
      contains: "AssessmentScoreAggregator"
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "FLOW L6 un-fixme — assert Score=80"
      contains: "L6 — Worker scores 80"
  key_links:
    - from: "HcPortal.Tests/AssessmentScoreAggregatorTests.cs"
      to: "HcPortal.Helpers.AssessmentScoreAggregator"
      via: "static call Compute()"
      pattern: "AssessmentScoreAggregator\\.Compute"
    - from: "tests/e2e/exam-types.spec.ts L6"
      to: "AssessmentSessions.Score"
      via: "db.queryScalar SELECT Score"
      pattern: "SELECT ISNULL\\(Score"
---

<objective>
Diagnose-first (SC1) + Wave 0 test scaffold. REPRODUKSI bug essay-only finalize → konfirmasi `AssessmentSessions.Score=0` di DB lokal, INSPEKSI data shape (ShuffledQuestionIds, QuestionType, EssayScore, rowsAffected) untuk MENGUNCI root cause persis (H1..H4 dari RESEARCH), lalu tulis test scaffold yang RED sampai fix landing di Plan 02/03.

**Gate:** Plan 02 (fix forward) TIDAK BOLEH ditulis sebagai asumsi H1. Plan ini WAJIB menghasilkan `376-DIAGNOSE.md` dengan temuan runtime sebelum fix. Per D-06 + RESEARCH Pitfall 1 (diagnose-first discipline).

Purpose: Mencegah fix berbasis asumsi salah; menyediakan harness RED→GREEN untuk Plan 02/03.
Output: `376-DIAGNOSE.md` (temuan SC1 + root cause locked), `AssessmentScoreAggregatorTests.cs` (unit murni, RED — helper belum ada), `exam-types.spec.ts` L6 un-fixme (RED sampai fix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-CONTEXT.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-RESEARCH.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang harus dibuat Plan 02 — test di-tulis di sini sebagai SPEC RED. -->
<!-- Helper signature usulan (Plan 02 finalkan; test ini mengunci behavior): -->

Helpers/AssessmentScoreAggregator.cs (DIBUAT di Plan 02 — belum ada saat plan ini jalan):
```csharp
namespace HcPortal.Helpers
{
    public readonly record struct ScoreAggregateResult(int TotalScore, int MaxScore, int Percentage, bool IsPassed);
    public static class AssessmentScoreAggregator
    {
        public static ScoreAggregateResult Compute(
            IEnumerable<PackageQuestion> questions,
            IEnumerable<PackageUserResponse> responses,
            int passPercentage);
    }
}
```

Model fields [VERIFIED]: PackageQuestion.QuestionType (string?), PackageQuestion.ScoreValue (int), PackageQuestion.Options (List<PackageOption>); PackageOption.Id, PackageOption.IsCorrect (bool); PackageUserResponse.PackageQuestionId (int), PackageUserResponse.PackageOptionId (int?), PackageUserResponse.EssayScore (int?).
Formula D-04 LOCKED: `percentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0; isPassed = percentage >= passPercentage`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repro SC1 + dokumentasikan root cause (376-DIAGNOSE.md)</name>
  <files>.planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md</files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (FLOW L4/L5/L6, L355-426 — essay-only flow yang repro bug)
    - Controllers/AssessmentAdminController.cs (FinalizeEssayGrading L3469-3669 — jalur bug)
    - Models/UserPackageAssignment.cs (GetShuffledQuestionIds L60-71 — return [] saat kosong)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-RESEARCH.md (§Root Cause Analysis — tabel H1..H5 + cara konfirmasi SC1)
    - docs/DEV_WORKFLOW.md (verifikasi lokal), reference MEMORY: AD lokal `Authentication__UseActiveDirectory=false dotnet run`, e2e `--workers=1` + SQLBrowser/`lpc:`
  </read_first>
  <action>
    REPRODUKSI bug di lokal (diagnose-first, JANGAN asumsi H1):
    1. Jalankan FLOW L essay-only sampai finalize. Opsi A (preferred): `npx playwright test exam-types --grep "FLOW L" --workers=1` (L4 worker isi essay → PendingGrading; L5 HC grade 80 + finalize). Opsi B (fallback bila e2e env tak siap): repro manual via `Authentication__UseActiveDirectory=false dotnet run` (localhost:5277) + buat assessment essay-only 1 soal, kerjakan, HC nilai 80, finalize.
    2. Setelah finalize, INSPEKSI DB lokal untuk session essay-only tersebut (`sessionId`). Query (sqlcmd `-I` flag wajib):
       - `SELECT Id, Score, Status, IsPassed, PassPercentage, HasManualGrading FROM AssessmentSessions WHERE Id={sessionId}` → konfirmasi Score=0 (atau NULL) walau Status=Completed.
       - `SELECT ShuffledQuestionIds FROM UserPackageAssignments WHERE AssessmentSessionId={sessionId}` → cek apakah `[]`/`{}`/null (uji H1) atau berisi id valid.
       - `SELECT DISTINCT QuestionType FROM PackageQuestions q JOIN ... WHERE q.Id IN (...)` untuk soal essay → cek casing/whitespace (uji H2; harapan "Essay").
       - `SELECT PackageQuestionId, EssayScore FROM PackageUserResponses WHERE AssessmentSessionId={sessionId}` → konfirmasi EssayScore terisi (80) per komentar e2e L413.
       - Cek log aplikasi untuk `rowsAffected` di FinalizeEssayGrading (uji H4 race) — apakah finalize benar-benar menulis (rowsAffected>0) atau no-op.
    3. TULIS `.planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md` (Bahasa Indonesia, min 30 baris):
       - Ringkasan repro (command/langkah, sessionId, hasil Score).
       - Tabel temuan per query di atas (nilai aktual ShuffledQuestionIds, QuestionType, EssayScore, rowsAffected).
       - **Root cause LOCKED**: pilih H1/H2/H3/H4 (atau kombinasi) berdasarkan DATA, bukan asumsi. Bila ShuffledQuestionIds=`[]` → H1 terkonfirmasi. Bila berisi id tapi Score tetap 0 → telusuri H2/H3.
       - **Pilihan derivasi fallback (RESEARCH open-Q2)**: putuskan question-set fallback = dari `PackageUserResponses` session (Distinct PackageQuestionId) ATAU dari `AssessmentPackage.Questions` sentinel — justifikasi berdasarkan data shape aktual. Untuk essay-only (1 soal pasti ada response) `PackageUserResponses` aman.
       - **Predicate kandidat recompute (RESEARCH open-Q3/Q5)**: konfirmasi nilai aktual baris rusak = `Score==0` atau `Score IS NULL` → tetapkan predicate untuk Plan 03.
       - Catatan ke Plan 02/03: instruksi konkret berdasar temuan.
    4. JANGAN tulis fix di plan ini. JANGAN edit FinalizeEssayGrading. JANGAN sentuh DB Dev/Prod (CLAUDE.md — lokal only).
    Bila repro butuh seed temporary lokal → ikuti SEED_WORKFLOW (snapshot DB sebelum + restore sesudah + catat docs/SEED_JOURNAL.md).
  </action>
  <verify>
    <automated>test -f ".planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md" && grep -qi "root cause\|H1\|H2\|H3\|H4" ".planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `376-DIAGNOSE.md` ada, ≥30 baris, berisi kata "root cause" + minimal satu hipotesis (H1..H4) yang dipilih dengan data.
    - Dokumen menyebut nilai aktual: ShuffledQuestionIds, QuestionType, EssayScore, rowsAffected, dan Score=0 dikonfirmasi.
    - Dokumen menetapkan pilihan derivasi fallback + predicate kandidat recompute untuk Plan 02/03.
    - TIDAK ada perubahan pada `Controllers/AssessmentAdminController.cs` (`git diff --stat Controllers/AssessmentAdminController.cs` kosong).
  </acceptance_criteria>
  <done>SC1 terpenuhi: Score=0 di-repro + root cause persis terdokumentasi sebelum fix ditulis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test scaffold helper agregasi (RED) + un-fixme e2e FLOW L6 (RED)</name>
  <files>HcPortal.Tests/AssessmentScoreAggregatorTests.cs, tests/e2e/exam-types.spec.ts</files>
  <read_first>
    - HcPortal.Tests/ShuffleToggleRulesTests.cs (pola pure test, no fixture, header XML doc)
    - HcPortal.Tests/ShuffleEngineTests.cs (in-memory model builder `PackageQuestion`+`PackageOption` tanpa DB)
    - Controllers/AssessmentAdminController.cs (L3535-3565 — formula inline sumber no-drift, L3564 D-04)
    - Services/GradingService.cs (L200 interimPercentage — formula identik untuk no-drift mixed)
    - tests/e2e/exam-types.spec.ts (L412-426 FLOW L6 — fixme yang di-hapus)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md (§AssessmentScoreAggregatorTests + §FLOW L6 un-fixme)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md (output Task 1 — finalkan ekspektasi)
  </read_first>
  <behavior>
    - essay-only: 1 soal Essay ScoreValue=100, EssayScore=80, passPercentage=70 → Percentage=80, IsPassed=true.
    - essay-only gagal: 1 soal Essay ScoreValue=100, EssayScore=50, passPercentage=70 → Percentage=50, IsPassed=false.
    - maxScore=0 (D-05): 1 soal Essay ScoreValue=0 → Percentage=0, TIDAK throw.
    - mixed no-drift (GRADE-02): MC(ScoreValue=50, jawab benar) + MA(ScoreValue=50, set benar) + Essay(ScoreValue=100, EssayScore=80) → Percentage SAMA dengan formula inline L3564 untuk dataset identik (total=50+50+80=180, max=200 → 90).
    - empty questions: Percentage=0, no throw (guard kosong).
  </behavior>
  <action>
    BUAT `HcPortal.Tests/AssessmentScoreAggregatorTests.cs` (pure unit, no DB, no `[Trait("Category","Integration")]`):
    - Header XML doc: "Phase 376 GRADE-01/02 — pure unit tests for AssessmentScoreAggregator. No DB, no fixture." (pola ShuffleToggleRulesTests.cs:1-13).
    - `using HcPortal.Helpers; using HcPortal.Models; using System.Linq; using System.Collections.Generic; using Xunit; namespace HcPortal.Tests;`
    - In-memory builder helper (pola ShuffleEngineTests.cs):
      ```csharp
      private static PackageQuestion Q(int id, string type, int scoreValue, params (int optId, bool correct)[] opts) =>
          new PackageQuestion { Id = id, QuestionType = type, ScoreValue = scoreValue,
              Options = opts.Select(o => new PackageOption { Id = o.optId, IsCorrect = o.correct }).ToList() };
      private static PackageUserResponse Resp(int qId, int? optId = null, int? essay = null) =>
          new PackageUserResponse { PackageQuestionId = qId, PackageOptionId = optId, EssayScore = essay };
      ```
    - Tulis [Fact]/[Theory] untuk SEMUA case di <behavior> di atas. Untuk no-drift mixed: assert `AssessmentScoreAggregator.Compute(...).Percentage == 90` (hitung manual sesuai formula L3564 D-04).
    - Test memanggil `AssessmentScoreAggregator.Compute(questions, responses, passPercentage)` (helper belum ada → COMPILE-FAIL/RED diharapkan; Plan 02 buat helper → GREEN).

    UN-FIXME `tests/e2e/exam-types.spec.ts` FLOW L6 (~L415): HAPUS baris `test.fixme(true, '364: essay finalize ...');`. BIARKAN assertion existing (`expect(score).toBe(80)` + status Completed). Update komentar L413-414 jadi referensi Phase 376 GRADE-01 (bukan "do not fix here"). Test ini akan RED sampai fix Plan 02 landing — itulah bukti diagnose-first.
  </action>
  <verify>
    <automated>test -f HcPortal.Tests/AssessmentScoreAggregatorTests.cs && grep -q "AssessmentScoreAggregator.Compute" HcPortal.Tests/AssessmentScoreAggregatorTests.cs && ! grep -q "test.fixme" tests/e2e/exam-types.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/AssessmentScoreAggregatorTests.cs` ada + berisi `AssessmentScoreAggregator.Compute` + minimal 4 [Fact]/[Theory] case (essay-only pass, essay-only fail, maxScore=0, mixed no-drift).
    - Tidak ada `[Trait("Category", "Integration")]` di file unit ini (pure, cepat).
    - `grep -c "test.fixme" tests/e2e/exam-types.spec.ts` = 0 (semua fixme dihapus — atau minimal FLOW L6 fixme dihapus; verifikasi tak ada fixme tersisa di file).
    - `grep -c "L6 — Worker scores 80" tests/e2e/exam-types.spec.ts` = 1 (test L6 tetap ada).
    - Build test project: `dotnet build HcPortal.Tests` GAGAL COMPILE karena `AssessmentScoreAggregator` belum ada (RED diharapkan) — ATAU bila executor stub kosong helper supaya compile, unit test FAIL (RED). Dokumentasikan RED state di SUMMARY.
  </acceptance_criteria>
  <done>Unit test + e2e L6 dalam keadaan RED (gate untuk Plan 02 GREEN). Test mengunci formula D-04 + behavior dua jalur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none baru) | Plan ini hanya repro + test scaffold. Tidak ada endpoint/input baru. DB lokal only (CLAUDE.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-376-01 | I (Info Disclosure) | 376-DIAGNOSE.md berisi data DB lokal | accept | Data test lokal, bukan PII prod; dokumen di .planning (tak ter-deploy). DB Dev/Prod tak disentuh (CLAUDE.md). |
| T-376-02 | T (Tampering) | Seed temporary lokal untuk repro | mitigate | SEED_WORKFLOW: snapshot DB sebelum + restore sesudah + journal. Tak promosikan seed ke permanent. |
</threat_model>

<verification>
- `dotnet build HcPortal.Tests` → RED expected (helper absent) ATAU unit test FAIL (RED) — dokumentasikan.
- `376-DIAGNOSE.md` ada + root cause locked.
- `tests/e2e/exam-types.spec.ts` FLOW L6 un-fixme.
- `git diff --stat Controllers/` kosong (no fix di plan ini).
</verification>

<success_criteria>
- SC1: Repro lokal essay-only finalize → Score=0 dikonfirmasi + root cause persis teridentifikasi & terdokumentasi.
- Test harness (unit + e2e L6) RED dan siap GREEN oleh Plan 02/03.
</success_criteria>

<output>
After completion, create `.planning/phases/376-fix-essay-only-score-aggregation/376-01-SUMMARY.md`. WAJIB sertakan: root cause locked (H#), pilihan derivasi fallback, predicate kandidat recompute, RED state evidence.
</output>
