---
phase: 376-fix-essay-only-score-aggregation
plan: 03
type: execute
wave: 3
depends_on: [376-02]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/EssayFinalizeRecomputeTests.cs
  - docs/IT_NOTIFY.md
autonomous: true
requirements: [GRADE-01, GRADE-02]
must_haves:
  truths:
    - "Endpoint admin POST RecomputeEssayScores me-reuse helper agregasi (D-02 kill-drift)"
    - "Recompute set Score + IsPassed ONLY — NO cert/Proton/notif/TrainingRecord (D-03)"
    - "Recompute idempotent: 2x run → kandidat sama tak berubah; hanya sentuh Score=0/null (D-01)"
    - "Endpoint gated Admin-only + antiforgery + audit + no-info-leak (security)"
    - "Integration real-SQL hijau: forward Score≠0 + recompute idempotent + D-03 no-side-effect"
    - "IT handoff note: no-migration, commit hash, ada endpoint recompute (eksekusi = IT)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Action RecomputeEssayScores (Admin+antiforgery+audit, idempotent batch, Score+IsPassed only)"
      contains: "RecomputeEssayScores"
    - path: "HcPortal.Tests/EssayFinalizeRecomputeTests.cs"
      provides: "Integration real-SQL: forward finalize Score≠0 + recompute idempotent + no-side-effect"
      contains: "EssayFinalizeRecomputeFixture"
    - path: "docs/IT_NOTIFY.md"
      provides: "Handoff note IT — no migration, endpoint recompute, commit hash"
      contains: "RecomputeEssayScores"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs RecomputeEssayScores"
      to: "HcPortal.Helpers.AssessmentScoreAggregator"
      via: "reuse helper Compute() (single source of truth)"
      pattern: "AssessmentScoreAggregator\\.Compute"
    - from: "RecomputeEssayScores"
      to: "AssessmentSessions.Score + IsPassed"
      via: "ExecuteUpdateAsync 2 SetProperty WHERE Score==0/null (idempotent)"
      pattern: "ExecuteUpdateAsync"
---

<objective>
Tambah endpoint admin idempotent `RecomputeEssayScores` (POST) untuk repair baris essay-only historis `Score=0` yang sudah dinilai+finalized (D-01). Me-reuse helper `AssessmentScoreAggregator` (D-02 kill-drift) — set **Score + IsPassed ONLY** (D-03: NO cert/Proton/notif/TrainingRecord). Pola `BackfillProtonPenanda` (gated Admin + antiforgery + counters + warn-only audit + no-info-leak). Plus integration test real-SQL (forward Score≠0 + recompute idempotent + D-03 no-side-effect) + IT handoff note.

**Eksekusi endpoint di DB Dev/Prod = tanggung jawab IT** (CLAUDE.md). Developer verifikasi lokal only + flag IT.

Purpose: Repair data lama aman/auditable tanpa SQL manual; tutup SC bug end-to-end.
Output: `RecomputeEssayScores` action (baru), `EssayFinalizeRecomputeTests.cs` (baru), `docs/IT_NOTIFY.md` (handoff).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-CONTEXT.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-RESEARCH.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md
@CLAUDE.md

<interfaces>
<!-- Helper (dibuat Plan 02) — REUSE, jangan re-implement math (D-02). -->
HcPortal.Helpers.AssessmentScoreAggregator.Compute(IEnumerable<PackageQuestion>, IEnumerable<PackageUserResponse>, int passPercentage) → ScoreAggregateResult(int TotalScore, int MaxScore, int Percentage, bool IsPassed)

<!-- Endpoint analog: BackfillProtonPenanda [VERIFIED L3797-3895] — Admin-only + counters + warn-audit + no-info-leak. -->
[HttpPost] [Authorize(Roles = "Admin")] [ValidateAntiForgeryToken]  // mass-repair → Admin-only (BulkBackfill L3798 precedent; RESEARCH OQ4)

<!-- Candidate predicate [VERIFIED fields]: AssessmentSession.Score int? L26, HasManualGrading bool L184, IsPassed bool? L44, PassPercentage int L30; AssessmentStatus.Completed; PackageUserResponse.EssayScore int? L32 -->
var candidateIds = await _context.AssessmentSessions
    .Where(s => s.Status == AssessmentConstants.AssessmentStatus.Completed
             && s.HasManualGrading
             && (s.Score == null || s.Score == 0))
    .Select(s => s.Id).ToListAsync();

<!-- Idempotent update Score+IsPassed ONLY (D-03) — pola ExecuteUpdateAsync WHERE-guard L3569: -->
var rows = await _context.AssessmentSessions
    .Where(s => s.Id == candId && (s.Score == null || s.Score == 0))
    .ExecuteUpdateAsync(s => s.SetProperty(r => r.Score, agg.Percentage).SetProperty(r => r.IsPassed, agg.IsPassed));

<!-- Audit signature [VERIFIED AuditLogService.cs L21-27]: -->
_auditLog.LogAsync(string actorUserId, string actorName, string actionType, string description, int? targetId = null, string? targetType = null)

<!-- No-info-leak [VERIFIED L3887-3892]: _logger.LogError(ex, "..."); TempData["Error"] = "...generik (BI)..."; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah action RecomputeEssayScores (idempotent, Score+IsPassed only, D-03)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (BackfillProtonPenanda L3795-3895 PENUH — analog batch; FinalizeEssayGrading L3527-3575 — derivasi+helper Plan 02; constructor deps L19-55)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-RESEARCH.md (§Recompute candidate predicate + §Idempotent update + §Common Pitfalls 3/4/5 + §Security Domain)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md (§RecomputeEssayScores — attributes/predicate/counters/audit/idempotent)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md (predicate kandidat final: Score==0 vs IS NULL)
    - Services/AuditLogService.cs (L21-27 — signature)
  </read_first>
  <action>
    Tambah action `RecomputeEssayScores` di `AssessmentAdminController.cs` (dekat BackfillProtonPenanda, pola identik):
    1. **Attributes (V2/V4 ASVS — mass-repair Admin-only):**
       ```csharp
       [HttpPost]
       [Authorize(Roles = "Admin")]              // lebih ketat dari "Admin, HC" — operasi mass-repair (RESEARCH OQ4)
       [ValidateAntiForgeryToken]
       public async Task<IActionResult> RecomputeEssayScores()
       ```
    2. **Counters + outer try/catch no-info-leak** (pola BackfillProtonPenanda L3802/L3886-3893):
       ```csharp
       int repaired = 0, skipped = 0, alreadyOk = 0;
       try { /* ...body... */ }
       catch (Exception ex)
       {
           _logger.LogError(ex, "RecomputeEssayScores gagal.");
           TempData["Error"] = "Recompute skor essay gagal. Cek log untuk detail.";
           return RedirectToAction("ManageAssessment");
       }
       ```
    3. **Body — query kandidat (predicate final dari 376-DIAGNOSE.md):**
       ```csharp
       var candidateIds = await _context.AssessmentSessions
           .Where(s => s.Status == AssessmentConstants.AssessmentStatus.Completed
                    && s.HasManualGrading
                    && (s.Score == null || s.Score == 0))
           .Select(s => s.Id).ToListAsync();
       ```
    4. **foreach kandidat:** load session + packageAssignment; derive question-set ROBUST (SAMA persis Plan 02: shuffledIds bila ada, else dari PackageUserResponses session); load responses;
       - **Skip bila ada essay response dengan EssayScore==null** (belum lengkap dinilai → `skipped++; continue;`).
       - `var agg = AssessmentScoreAggregator.Compute(allQuestions, allResponses, session.PassPercentage);`
       - Bila `agg.MaxScore == 0` → `_logger.LogWarning(...); skipped++; continue;` (D-05 — jangan tulis Score salah).
       - **Idempotent update Score+IsPassed ONLY (D-03):**
         ```csharp
         var rows = await _context.AssessmentSessions
             .Where(s => s.Id == candId && (s.Score == null || s.Score == 0))
             .ExecuteUpdateAsync(s => s
                 .SetProperty(r => r.Score, agg.Percentage)
                 .SetProperty(r => r.IsPassed, agg.IsPassed));
         if (rows > 0) repaired++; else alreadyOk++;
         ```
       - **NO** `.SetProperty(Status,...)` (Pitfall 5), **NO** `CertNumberHelper`, **NO** `_protonCompletionService`, **NO** `_protonBypassService`, **NO** `NotifyIfGroupCompleted`, **NO** `_context.TrainingRecords.Add` (Pitfall 3/4). D-03 LOCKED.
    5. **Audit warn-only** (pola L3871-3883; signature AuditLogService L21-27):
       ```csharp
       try {
           var actor = await _userManager.GetUserAsync(User);
           var actorName = string.IsNullOrWhiteSpace(actor?.NIP) ? (actor?.FullName ?? "Unknown") : $"{actor.NIP} - {actor.FullName}";
           await _auditLog.LogAsync(actor?.Id ?? "", actorName, "RecomputeEssayScores",
               $"Recompute essay-only: {repaired} diperbaiki, {skipped} dilewati, {alreadyOk} sudah benar", 0, "AssessmentSession");
       } catch (Exception auditEx) { _logger.LogWarning(auditEx, "RecomputeEssayScores: audit log gagal."); }
       TempData["Success"] = $"Recompute selesai: {repaired} skor diperbaiki, {skipped} dilewati.";
       return RedirectToAction("ManageAssessment");
       ```
    Pesan user-facing (TempData) WAJIB Bahasa Indonesia. `using HcPortal.Helpers;` sudah ada (Plan 02). UI trigger = DISCRETION; headless POST cukup (RESEARCH OQ5) — TIDAK perlu view baru.
  </action>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RecomputeEssayScores" Controllers/AssessmentAdminController.cs` ≥ 1.
    - `grep -B3 "public async Task<IActionResult> RecomputeEssayScores" Controllers/AssessmentAdminController.cs` menunjukkan `[Authorize(Roles = "Admin")]` + `[ValidateAntiForgeryToken]` + `[HttpPost]`.
    - `grep -c "AssessmentScoreAggregator.Compute" Controllers/AssessmentAdminController.cs` ≥ 2 (forward Plan 02 + recompute — reuse helper, no math duplikasi D-02).
    - Dalam action RecomputeEssayScores TIDAK ada: `CertNumberHelper`, `_protonCompletionService`, `_protonBypassService`, `NotifyIfGroupCompleted`, `TrainingRecords.Add`, `SetProperty(r => r.Status` (D-03/Pitfall 3/4/5). Verifikasi manual lingkup action.
    - ExecuteUpdateAsync di recompute punya WHERE-guard `(s.Score == null || s.Score == 0)` (idempotent).
    - `dotnet build` exit 0. `dotnet test HcPortal.Tests --filter "Category!=Integration"` GREEN.
  </acceptance_criteria>
  <done>Endpoint recompute idempotent, Admin-gated, reuse helper, Score+IsPassed only (D-03), no-info-leak.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test real-SQL (forward Score≠0 + recompute idempotent + D-03 no-side-effect)</name>
  <files>HcPortal.Tests/EssayFinalizeRecomputeTests.cs</files>
  <read_first>
    - HcPortal.Tests/RecordCascadeIntegrationTests.cs (L21-54 fixture disposable DB + SeedUserAsync L83-89 + init-failure wrap L41-46)
    - HcPortal.Tests/ProtonCompletionServiceTests.cs (L25-61 IClassFixture real-SQL)
    - Controllers/AssessmentAdminController.cs (RecomputeEssayScores Task 1 + FinalizeEssayGrading Plan 02)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md (§EssayFinalizeRecomputeTests — fixture verbatim + 3 [Fact])
    - .planning/phases/376-fix-essay-only-score-aggregation/376-RESEARCH.md (§Validation Architecture — test map GRADE-01/02 + D-02/D-03)
  </read_first>
  <behavior>
    - Forward_EssayOnly_ScoreNotZero (GRADE-02): seed essay-only session PendingGrading + EssayScore terisi → finalize (atau path agregasi) → AssessmentSessions.Score = agregasi (bukan 0).
    - Recompute_Idempotent_OnlyTouchesScoreZero (D-02/D-07): seed baris essay-only historis Score=0 (Completed, semua EssayScore terisi) → recompute → Score diperbaiki; jalankan recompute KEDUA kali → Score TIDAK berubah (idempotent); baris Score≠0 lain TIDAK tersentuh.
    - Recompute_NoSideEffects (D-03): setelah recompute → NomorSertifikat tetap null, TIDAK ada TrainingRecord baru, Status TIDAK berubah (tetap Completed), IsPassed di-set sesuai agg.
  </behavior>
  <action>
    BUAT `HcPortal.Tests/EssayFinalizeRecomputeTests.cs` (pola RecordCascadeFixture — disposable real-SQL DB, `[Trait("Category","Integration")]`):
    1. **Fixture** `EssayFinalizeRecomputeFixture : IAsyncLifetime` (VERBATIM struktur RecordCascadeIntegrationTests L21-54): `DbName = $"HcPortalDB_Test_{Guid.NewGuid():N}"`, connection string `Server=localhost\\SQLEXPRESS;...`, `InitializeAsync` → `MigrateAsync()`, `DisposeAsync` → `EnsureDeletedAsync()`. Wrap init-failure (catch → EnsureDeleted best-effort → `throw new Xunit.Sdk.XunitException("...MIGRATION-CHAIN break...")`).
    2. **Test class** `[Trait("Category","Integration")] EssayFinalizeRecomputeTests : IClassFixture<EssayFinalizeRecomputeFixture>` + `NewCtx()` helper. DB lokal `HcPortalDB_Dev` TAK tersentuh (disposable DB per fixture — no SEED_WORKFLOW snapshot needed).
    3. Per-[Fact] seed user/session unik (Guid) + assert by id spesifik:
       - `[Fact] Forward_EssayOnly_ScoreNotZero` → GRADE-02.
       - `[Fact] Recompute_Idempotent_OnlyTouchesScoreZero` → D-02/D-07 (2x run, kandidat sama tak berubah + baris Score≠0 untouched).
       - `[Fact] Recompute_NoSideEffects_NoCertNoProtonNoTR` → D-03 (NomorSertifikat null, no TrainingRecord, Status Completed).
    4. Test memanggil logika agregasi/recompute. Bila memanggil action controller butuh DI berat → boleh test pada level data: seed → jalankan derivasi+`AssessmentScoreAggregator.Compute`+ExecuteUpdateAsync persis seperti action, assert hasil DB. (DISCRETION — yang penting cover behavior di atas dengan real-SQL.)
    Jalankan dengan `dotnet test` (Integration). Bila SQLEXPRESS lokal tak siap → fallback unit-only sudah cover formula; dokumentasikan di SUMMARY.
  </action>
  <verify>
    <automated>dotnet test --filter "Category=Integration&FullyQualifiedName~EssayFinalizeRecompute"</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/EssayFinalizeRecomputeTests.cs` ada + berisi `EssayFinalizeRecomputeFixture` + `[Trait("Category", "Integration")]` + 3 `[Fact]` (Forward_EssayOnly_ScoreNotZero, Recompute_Idempotent..., Recompute_NoSideEffects...).
    - `dotnet build` exit 0.
    - `dotnet test --filter "Category=Integration&FullyQualifiedName~EssayFinalizeRecompute"` GREEN (bila SQLEXPRESS lokal tersedia; bila tidak → catat skip-reason di SUMMARY + unit-only fallback green).
    - Full suite `dotnet test` GREEN (no regression test lain).
  </acceptance_criteria>
  <done>Integration real-SQL membuktikan forward Score≠0 + recompute idempotent + D-03 no-side-effect (SC4 + D-02/D-03/D-07).</done>
</task>

<task type="auto">
  <name>Task 3: IT handoff note (no-migration + endpoint recompute, eksekusi = IT)</name>
  <files>docs/IT_NOTIFY.md</files>
  <read_first>
    - CLAUDE.md (Develop Workflow — flag IT commit hash + migration; eksekusi DB Dev/Prod = IT)
    - docs/IT_NOTIFY.md (bila sudah ada — append; pola handoff fase sebelumnya)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md (root cause untuk konteks IT)
  </read_first>
  <action>
    BUAT/APPEND `docs/IT_NOTIFY.md` (Bahasa Indonesia) — entri Phase 376:
    - **Ringkasan:** Fix bug grading essay-only (Score=0 walau dinilai+finalize) — forward path diperbaiki + endpoint recompute untuk repair baris historis.
    - **Migration:** **FALSE** (no schema change). Recompute = DML repair via endpoint, BUKAN EF migration.
    - **Commit hash:** {isi saat commit} — flag ke IT.
    - **Endpoint recompute:** `POST /Admin/RecomputeEssayScores` (atau route aktual) — gated **Admin-only** + antiforgery. **Eksekusi di DB Dev/Prod = tanggung jawab IT** (developer verifikasi lokal only). Idempotent: hanya sentuh baris kandidat (Completed + HasManualGrading + Score=0/null + semua essay dinilai); set **Score + IsPassed ONLY** (NO cert/Proton/notif retroaktif — D-03). Aman dijalankan berulang.
    - **Catatan D-03:** Sertifikat untuk baris repaired yang lulus TIDAK auto-terbit; HC re-trigger per-orang manual bila perlu.
    - **Verifikasi pra-eksekusi (saran IT):** query count kandidat dulu (`SELECT COUNT(*) FROM AssessmentSessions WHERE Status='Completed' AND HasManualGrading=1 AND (Score IS NULL OR Score=0)`), backup, lalu POST endpoint, cek audit log + counters di TempData/response.
    - JANGAN sertakan instruksi edit DB langsung — eksekusi via endpoint saja.
  </action>
  <verify>
    <automated>test -f docs/IT_NOTIFY.md && grep -qi "RecomputeEssayScores\|recompute" docs/IT_NOTIFY.md && grep -qi "migration.*false\|no migration\|tanpa migrasi" docs/IT_NOTIFY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/IT_NOTIFY.md` ada + berisi "RecomputeEssayScores" + pernyataan migration FALSE + "eksekusi ... IT" (Bahasa Indonesia).
    - Note menyebut D-03 (no auto-cert) + idempotent + Admin-only.
  </acceptance_criteria>
  <done>IT handoff jelas: no-migration, ada endpoint recompute, eksekusi DB = IT (sesuai CLAUDE.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin client → POST RecomputeEssayScores | NEW admin endpoint. Mass-mutation (Score+IsPassed banyak baris). Input: none dari user (server hitung dari DB). Untrusted CSRF surface. |
| Endpoint → DB (ExecuteUpdateAsync) | Atomic WHERE-guard. Blast radius dibatasi D-03 (Score+IsPassed only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-376-08 | S/T (CSRF) | POST RecomputeEssayScores | mitigate | `[ValidateAntiForgeryToken]` (pola L3468/L3799). |
| T-376-09 | E (Elevation) | Non-admin trigger mass-recompute | mitigate | `[Authorize(Roles = "Admin")]` — lebih ketat dari "Admin, HC" (V4 ASVS; BulkBackfill precedent). |
| T-376-10 | T (Mass-state corruption) | Recompute salah → Score massal salah | mitigate | Reuse helper teruji (no-drift unit Plan 01 + integration Plan 03) + idempotent WHERE-guard `Score==0/null` + skip maxScore=0 + counters; eksekusi terkontrol IT (query count + backup dulu). |
| T-376-11 | T (Blast radius) | Recompute trigger cert/Proton/notif massal di prod | mitigate | D-03: Score+IsPassed ONLY via ExecuteUpdateAsync; NO cert/Proton/notif/TR (acceptance grep + integration Recompute_NoSideEffects). |
| T-376-12 | R (Repudiation) | Tak ada jejak siapa jalankan recompute | mitigate | `_auditLog.LogAsync` batch (warn-only, action "RecomputeEssayScores" + counters). |
| T-376-13 | I (Info Disclosure) | Error bocorkan detail internal | mitigate | `_logger.LogError(ex,...)` detail → log; `TempData["Error"]` generik BI (Phase 334 D6). |
| T-376-14 | T (Idempotency) | Replay recompute → double-write/corrupt | mitigate | WHERE-guard `(Score==null||Score==0)` → run kedua no-op (integration idempotency test). |
</threat_model>

<verification>
- `dotnet build` exit 0.
- `dotnet test` full (unit + integration) GREEN.
- Recompute action: grep TIDAK ada cert/Proton/notif/TR/Status-set (D-03).
- `grep -c "AssessmentScoreAggregator.Compute"` ≥ 2 (forward + recompute reuse, no drift).
- `docs/IT_NOTIFY.md` lengkap (no-migration, endpoint, eksekusi IT, D-03).
- Phase gate: `npx playwright test exam-types --grep "L6" --workers=1` GREEN.
</verification>

<success_criteria>
- Recompute endpoint repair baris lama idempotent, Score+IsPassed only (D-01/D-02/D-03), gated+audited.
- Integration real-SQL hijau (forward Score≠0 + recompute idempotent + no-side-effect) → SC4 + D-07.
- IT handoff jelas (no-migration, endpoint, eksekusi = IT).
- Helper kill-drift: forward + recompute pakai math identik (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/376-fix-essay-only-score-aggregation/376-03-SUMMARY.md`. Sertakan: route endpoint final, hasil integration test, konfirmasi D-03 no-side-effect (grep evidence), entri IT_NOTIFY, status full suite + e2e L6.
</output>
