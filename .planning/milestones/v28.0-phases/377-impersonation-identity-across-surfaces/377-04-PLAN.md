---
phase: 377-impersonation-identity-across-surfaces
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Controllers/CDPController.cs
autonomous: true
requirements: [IMP-01, IMP-02]
tags: [impersonation, identity, authorization, cdp, di-injection]

must_haves:
  truths:
    - "CDPController inject ImpersonationService (sebelumnya tidak — Pitfall 2 build break)"
    - "CDP GetCurrentUserRoleLevelAsync impersonation-aware + signature diseragamkan ke nullable (mode-role butuh null path, D-03)"
    - "BuildSertifikatRowsAsync (CDP:3859 l5OwnDataOnly) resolve effective user X saat impersonasi (SC3)"
    - "Caller non-null lama (CDP:3740 .User.Section) diberi null-guard agar tak NRE setelah signature jadi nullable"
    - "Mode normal (non-impersonate) tak berubah (SC4)"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "DI ImpersonationService + resolver nullable impersonation-aware + null-guard caller"
      contains: "_impersonationService"
  key_links:
    - from: "CDPController.GetCurrentUserRoleLevelAsync"
      to: "ImpersonationService.GetEffectiveUserAsync"
      via: "resolver hulu single-source (D-05), seragam dengan CMP"
      pattern: "GetEffectiveUserAsync"
    - from: "CDPController constructor"
      to: "ImpersonationService (Program.cs:63 AddScoped)"
      via: "constructor injection (template CMP:40/56/71)"
      pattern: "ImpersonationService impersonationService"
---

<objective>
Buat `CDPController` impersonation-aware: inject `ImpersonationService` (belum ada — Pitfall 2), rewrite `GetCurrentUserRoleLevelAsync` konsumsi resolver Plan 02 + seragamkan signature divergen ke NULLABLE (CMP sudah nullable; CDP saat ini non-null `user!`). Resolusi otomatis memperbaiki `BuildSertifikatRowsAsync` (CDP:3859, l5OwnDataOnly self-data). Tambah null-guard di caller lama yang asumsi non-null (CDP:3740 `.User.Section`).

Purpose: CDP CertificationManagement (l5OwnDataOnly) adalah self-worker-data read L5 — saat impersonate L5, harus tampil data X. Kill split-brain CMP/CDP (D-05). Pitfall 2 (missing DI) WAJIB ditangani dulu agar build tak break.
Output: CDPController impersonation-aware, seragam dengan CMP, build 0 error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-CONTEXT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-AUDIT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-02-SUMMARY.md

<interfaces>
<!-- API resolver dari Plan 02. CDP BELUM inject ImpersonationService (Pitfall 2) — WAJIB tambah. -->
public enum EffectiveUserDecision { UseRealUser, RoleModeEmpty, TargetUser }
public async Task<(ApplicationUser? User, EffectiveUserDecision Decision)> GetEffectiveUserAsync(UserManager<ApplicationUser> userManager);
public bool IsImpersonating();
public string? GetMode();
public int? GetEffectiveRoleLevel();

<!-- DI template (CMPController.cs:40/56/71 — copy persis): -->
private readonly ImpersonationService _impersonationService;            // field
public CMPController(..., ImpersonationService impersonationService)    // param
{ ... _impersonationService = impersonationService; }                   // assignment
<!-- DI sudah terdaftar Program.cs:63 AddScoped<ImpersonationService>() — TAK perlu registrasi baru. -->

<!-- Resolver CDP SEKARANG (L3696-3702, signature DIVERGEN non-null — yang di-rewrite ke nullable): -->
private async Task<(ApplicationUser User, int RoleLevel)> GetCurrentUserRoleLevelAsync() {
    var user = await _userManager.GetUserAsync(User);
    var userRoles = await _userManager.GetRolesAsync(user!);
    var roleLevel = UserRoles.GetRoleLevel(userRoles.FirstOrDefault() ?? "");
    return (user!, roleLevel);
}

<!-- Caller yang asumsi non-null (perlu null-guard setelah jadi nullable): -->
<!-- L3740: ViewBag.UserBagian = (await GetCurrentUserRoleLevelAsync()).User.Section;  (CertificationManagement) -->
<!-- L3859: var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();  (BuildSertifikatRowsAsync l5OwnDataOnly — IN-SCOPE) -->

<!-- Constructor CDP SEKARANG (L33-50) TANPA ImpersonationService — lihat PATTERNS §CDPController. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject ImpersonationService ke CDPController (Pitfall 2)</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs (L33-50 field + constructor + assignment; using directives di atas file)
    - Controllers/CMPController.cs (L40/56/71 DI template — copy persis pola)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§CDPController — constructor SEKARANG verbatim + aksi fix)
  </read_first>
  <action>
    Edit `Controllers/CDPController.cs`.

    1. Pastikan `using HcPortal.Services;` ada di atas file (untuk `ImpersonationService` + `EffectiveUserDecision`). Bila belum, tambah.

    2. Tambah field (setelah field existing, L33-39):
    ```csharp
    private readonly ImpersonationService _impersonationService;
    ```

    3. Tambah parameter ke constructor (L41) — sisipkan `ImpersonationService impersonationService` di list param (urutan bebas, mis. setelah `AuditLogService auditLog`):
    ```csharp
    public CDPController(..., AuditLogService auditLog, ImpersonationService impersonationService)
    ```

    4. Tambah assignment di body constructor (setelah `_auditLog = auditLog;`):
    ```csharp
    _impersonationService = impersonationService;
    ```

    JANGAN ubah field/param/assignment lain. DI sudah terdaftar di `Program.cs:63` — tak perlu sentuh Program.cs. Build harus 0 error setelah field di-wire (sebelum dipakai resolver di Task 2).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0 (DI terpasang, tak ada CS0103 `_impersonationService`).
    - `Controllers/CDPController.cs` memuat field `private readonly ImpersonationService _impersonationService;`, param `ImpersonationService impersonationService` di constructor, dan assignment `_impersonationService = impersonationService;`.
    - git diff Program.cs KOSONG (tak perlu registrasi baru).
  </acceptance_criteria>
  <done>ImpersonationService ter-inject di CDPController; build 0 error (Pitfall 2 ditangani).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CDP resolver nullable impersonation-aware + null-guard caller</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs (L3696-3702 resolver; L3704-3742 CertificationManagement caller L3740; L3859 BuildSertifikatRowsAsync caller)
    - Services/ImpersonationService.cs (API baru Plan 02)
    - Controllers/CMPController.cs (L2388 resolver hasil rewrite Plan 03 — pola yang DISAMAKAN; baca SUMMARY 377-03 bila tersedia)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (signature divergence VERIFIED + Pitfall 5 SC4)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-AUDIT.md (klasifikasi CDP:3740 OUT default vs CDP:3859 IN-SCOPE)
  </read_first>
  <action>
    Edit `Controllers/CDPController.cs`.

    A. REWRITE `GetCurrentUserRoleLevelAsync()` (L3696-3702) — seragamkan ke NULLABLE + impersonation-aware (pola IDENTIK dengan CMP Plan 03):
    ```csharp
    private async Task<(ApplicationUser? User, int RoleLevel)> GetCurrentUserRoleLevelAsync()
    {
        var (effUser, decision) = await _impersonationService.GetEffectiveUserAsync(_userManager);

        if (decision == EffectiveUserDecision.UseRealUser)
        {
            var real = await _userManager.GetUserAsync(User);
            if (real == null) return (null, 0);
            var realRoles = await _userManager.GetRolesAsync(real);
            return (real, UserRoles.GetRoleLevel(realRoles.FirstOrDefault() ?? ""));
        }
        if (decision == EffectiveUserDecision.RoleModeEmpty)
            return (null, _impersonationService.GetEffectiveRoleLevel() ?? 0);
        if (effUser == null) return (null, 0);
        var effLevel = _impersonationService.GetEffectiveRoleLevel()
                       ?? UserRoles.GetRoleLevel((await _userManager.GetRolesAsync(effUser)).FirstOrDefault() ?? "");
        return (effUser, effLevel);
    }
    ```
    Signature berubah dari `(ApplicationUser User, ...)` → `(ApplicationUser? User, ...)`. Ini akan memunculkan warning/error di caller yang akses `.User.X` tanpa guard — perbaiki di B/C.

    B. Caller `CertificationManagement:3740` (`ViewBag.UserBagian = (await GetCurrentUserRoleLevelAsync()).User.Section;`) — tambah null-guard (klasifikasi AUDIT: admin-scope cert mgmt, default OUT A4 — tapi WAJIB null-safe agar tak NRE saat nullable). Ubah jadi:
    ```csharp
    var (cmUser, _) = await GetCurrentUserRoleLevelAsync();
    ViewBag.UserBagian = cmUser?.Section;
    ```
    (Perilaku: saat impersonate-role, `cmUser` null → `ViewBag.UserBagian = null` → view tampil tanpa filter bagian admin; konsisten D-03 tidak bocor section admin. Catat di SUMMARY.)

    C. Caller `BuildSertifikatRowsAsync:3859` (`var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();`) — IN-SCOPE (l5OwnDataOnly self-data). Setelah resolver nullable, tambah null-handling: bila `user == null` (mode-role/null) → scoped user-ids kosong → 0 row (D-03 konsisten). Cek downstream pemakaian `user.Id`/`user.Section` di method ini (baca L3859-3895) dan ganti ke `user?.Id`/`user?.Section` dengan default aman (mis. L5 own-data filter `user?.Id ?? ""` → 0 match). Pastikan tak ada NRE.

    D. Cek SELURUH caller `GetCurrentUserRoleLevelAsync()` di CDP (grep: hanya 3740 + 3859 + def 3696) sudah null-safe. Build 0 error + 0 nullable-warning-as-error.

    JANGAN sentuh `[Authorize]` (D-02). JANGAN ubah `BuildSertifikatRowsAsync` scoping logic selain null-safety (logic role-scope sudah benar; effective level/user mengalir dari resolver).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo 2>&1 | powershell -Command "$input | Select-String -Pattern 'Passed!|Failed!|error'"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0 (tak ada NRE-warning-as-error dari `.User.Section`).
    - `GetCurrentUserRoleLevelAsync` di CDP: signature `(ApplicationUser? User, int RoleLevel)` (nullable) + memuat `GetEffectiveUserAsync`.
    - CDP:3740 caller memuat `cmUser?.Section` (null-safe), TIDAK lagi `.User.Section` tanpa guard.
    - CDP:3859 caller null-safe (`user?.Id`/`user?.Section` atau guard).
    - Full xUnit suite hijau (`dotnet test` exits 0) — SC4 no regression (termasuk `CDPControllerAuthTests`).
  </acceptance_criteria>
  <done>CDP resolver nullable + impersonation-aware seragam CMP; caller null-safe; BuildSertifikatRows self-data resolve X; suite hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET CDP CertificationManagement (impersonate L5) → self-cert data | l5OwnDataOnly self-worker-data; harus pakai effective X. |
| nullable signature migration | Caller lama asumsi non-null; null-guard mencegah NRE/500 (availability). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-377-13 | Information Disclosure | BuildSertifikatRowsAsync(l5OwnDataOnly) pakai admin.Id → impersonate L5 lihat cert admin | mitigate | Resolver hulu → user.Id=X.Id; scoped user-ids ikut X (SC3). |
| T-377-14 | Build break (DoS) | CDP resolver pakai `_impersonationService` belum inject → kompilasi gagal | mitigate (block: high) | Task 1 inject dulu (Pitfall 2) sebelum dipakai Task 2. |
| T-377-15 | NRE/500 (Availability) | Signature jadi nullable, caller `.User.Section` NRE saat mode-role | mitigate | Null-guard CDP:3740/3859 (Task 2B/C). |
| T-377-16 | Information Disclosure | CertificationManagement `ViewBag.UserBagian` bocor section admin saat impersonate-role | mitigate | `cmUser?.Section` → null saat mode-role (tak bocor admin section). |
| T-377-17 | Regression (SC4) | Resolver non-impersonate berubah | mitigate | Branch `UseRealUser` identik; CDPControllerAuthTests + full suite hijau. |
</threat_model>

<verification>
- Build 0 error (DI + nullable null-safe).
- Full xUnit suite hijau (SC4, termasuk CDPControllerAuthTests).
- CDP resolver nullable + GetEffectiveUserAsync; caller 3740/3859 null-safe.
- Program.cs tak berubah.
</verification>

<success_criteria>
- CDP impersonation-aware, seragam CMP (D-05 kill split-brain).
- SC3 path siap (CDP self-cert L5 = data X) — divalidasi Plan 06.
- SC4 no regression.
</success_criteria>

<output>
After completion, create `.planning/phases/377-impersonation-identity-across-surfaces/377-04-SUMMARY.md`.
Catat: perilaku CertificationManagement ViewBag.UserBagian saat mode-role, null-handling BuildSertifikatRows, hasil suite.
</output>
</output>
