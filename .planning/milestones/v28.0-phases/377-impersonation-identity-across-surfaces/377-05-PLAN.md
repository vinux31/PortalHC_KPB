---
phase: 377-impersonation-identity-across-surfaces
plan: 05
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Controllers/HomeController.cs
autonomous: true
requirements: [IMP-01, IMP-02]
tags: [impersonation, identity, home, dashboard, split-brain]

must_haves:
  truths:
    - "Home/Index GetProgress + GetUpcomingEvents resolve effective user X saat impersonasi (bukan admin) — fold split-brain L38 vs L53 (SC3)"
    - "Mode role (tanpa target user) → Home progress/events tampil kosong (D-03), bukan data admin"
    - "Guide/GuideDetail (L329/346) TIDAK disentuh — konten per-role, OUT (A4)"
    - "Mode normal (non-impersonate) tak berubah (SC4)"
  artifacts:
    - path: "Controllers/HomeController.cs"
      provides: "Index effective user resolution + fold L53 split-brain"
      contains: "_impersonationService.GetEffectiveUserAsync"
  key_links:
    - from: "HomeController.Index"
      to: "ImpersonationService.GetEffectiveUserAsync"
      via: "effective user untuk GetProgress/GetUpcomingEvents (D-05)"
      pattern: "GetEffectiveUserAsync"
---

<objective>
Fold split-brain HomeController.Index: saat ini L38 pakai user ASLI (admin) untuk `GetProgress`/`GetUpcomingEvents` tapi L53 sudah pakai role-level EFEKTIF. Buat keduanya konsisten — resolve effective user X (Plan 02 resolver) sehingga dashboard progress + upcoming events = data X saat impersonasi (SC3). Mode-role → kosong (D-03). Guide/GuideDetail tetap OUT.

Purpose: Home dashboard adalah surface progress yang disebut eksplisit di IMP-01/SC3. Split-brain (role-level efektif + identitas asli) = bukti bug parsial. Folding = konsistensi identitas efektif.
Output: HomeController.Index impersonation-aware, split-brain hilang.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-CONTEXT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-AUDIT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-02-SUMMARY.md

<interfaces>
<!-- API resolver dari Plan 02. _impersonationService SUDAH ter-inject di Home (L22/26/33). -->
public enum EffectiveUserDecision { UseRealUser, RoleModeEmpty, TargetUser }
public async Task<(ApplicationUser? User, EffectiveUserDecision Decision)> GetEffectiveUserAsync(UserManager<ApplicationUser> userManager);
public bool IsImpersonating();
public string? GetMode();
public int? GetEffectiveRoleLevel();   // existing — sudah dipakai L53

<!-- Index SEKARANG (L36-56, split-brain L38 asli vs L53 efektif): -->
public async Task<IActionResult> Index() {
    var user = await _userManager.GetUserAsync(User);   // L38 = admin asli (BUG)
    if (user == null) return Challenge();
    var upcomingEvents = await GetUpcomingEvents(user.Id);   // L41 = events admin (BUG)
    var progress = await GetProgress(user.Id);               // L42 = progress admin (BUG)
    var viewModel = new DashboardHomeViewModel { CurrentUser = user, ... };
    var effectiveLevel = _impersonationService.GetEffectiveRoleLevel();   // L53 = EFEKTIF (sudah benar)
    var isHcOrAdmin = effectiveLevel.HasValue ? effectiveLevel.Value <= 2 : (User.IsInRole("HC") || User.IsInRole("Admin"));
    ...
}

<!-- Konsumen agnostik (terima param, otomatis benar bila user.Id di-resolve di hulu): -->
private async Task<...> GetProgress(string userId);        // L213
private async Task<...> GetUpcomingEvents(string userId);  // L275

<!-- OUT (A4 — JANGAN sentuh): Guide:329, GuideDetail:346 (konten guide per-role, bukan data-diri). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Effective user di Home/Index + fold split-brain L53</name>
  <files>Controllers/HomeController.cs</files>
  <read_first>
    - Controllers/HomeController.cs (L34-72 Index FULL; L213 GetProgress; L275 GetUpcomingEvents; L22/26/33 DI _impersonationService; L320-352 Guide/GuideDetail untuk konfirmasi OUT)
    - Services/ImpersonationService.cs (API baru Plan 02)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§HomeController — bug split-brain verbatim + OUT Guide)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (Pitfall 5 SC4; D-03 mode-role kosong)
  </read_first>
  <action>
    Edit `Controllers/HomeController.cs` — HANYA action `Index` (L36-...). JANGAN sentuh Guide/GuideDetail (L329/346, OUT A4) atau action lain.

    Ubah resolusi user di awal Index agar `GetProgress`/`GetUpcomingEvents` pakai effective user X:
    ```csharp
    public async Task<IActionResult> Index()
    {
        var (effUser, decision) = await _impersonationService.GetEffectiveUserAsync(_userManager);

        // SC4 non-impersonate → real user identik
        ApplicationUser? user;
        if (decision == EffectiveUserDecision.UseRealUser)
        {
            user = await _userManager.GetUserAsync(User);
            if (user == null) return Challenge();   // genuinely not logged in
        }
        else
        {
            user = effUser;   // TargetUser=X; RoleModeEmpty=null (D-03)
        }

        // D-03 mode-role: effective user null → progress/events kosong (userId="" → 0 record), JANGAN admin
        var userId = user?.Id ?? "";
        var upcomingEvents = await GetUpcomingEvents(userId);
        var progress = await GetProgress(userId);

        var viewModel = new DashboardHomeViewModel
        {
            CurrentUser = user,   // null saat mode-role → view harus null-safe (lihat catatan)
            Greeting = GetTimeBasedGreeting(),
            UpcomingEvents = upcomingEvents,
            Progress = progress
        };

        // L53 fold: tetap pakai GetEffectiveRoleLevel (sudah efektif) — sekarang konsisten dgn identitas efektif
        var effectiveLevel = _impersonationService.GetEffectiveRoleLevel();
        var isHcOrAdmin = effectiveLevel.HasValue
            ? effectiveLevel.Value <= 2
            : (User.IsInRole("HC") || User.IsInRole("Admin"));
        // ... sisa logika Index TIDAK berubah
    }
    ```

    Cek `DashboardHomeViewModel.CurrentUser` nullable atau tidak + apakah view `Home/Index.cshtml` mengakses `Model.CurrentUser.<...>` tanpa guard. Bila view bisa NRE saat mode-role (CurrentUser null), tambahkan null-guard MINIMAL di view (`Model.CurrentUser?.FullName ?? ""`) ATAU — bila lebih aman & tak ganggu kontrak — render hint untuk mode-role. Pilih perubahan minimal; jangan ubah layout dashboard. Bila `CurrentUser` non-nullable kontrak yang sulit diubah, alternatif aman: untuk mode-role tetap set `viewModel.CurrentUser = await _userManager.GetUserAsync(User)` (admin, HANYA untuk display nama header) TAPI `userId=""` untuk progress/events (data tetap kosong, tak bocor) — dokumentasikan keputusan di SUMMARY (D-03 inti = DATA kosong, bukan nama header).

    Pastikan `using HcPortal.Services;` ada (untuk `EffectiveUserDecision`). Build 0 error.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo 2>&1 | powershell -Command "$input | Select-String -Pattern 'Passed!|Failed!|error'"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0.
    - `HomeController.Index` memuat `_impersonationService.GetEffectiveUserAsync` dan branch `UseRealUser`.
    - `GetProgress`/`GetUpcomingEvents` di Index dipanggil dengan `userId` yang berasal dari effective user (bukan `(await _userManager.GetUserAsync(User)).Id` langsung).
    - git diff HomeController.cs TIDAK menyentuh `Guide`/`GuideDetail` (L329/346) atau method lain selain `Index` (+ null-guard view bila perlu).
    - Full xUnit suite hijau (`dotnet test` exits 0) — SC4 no regression.
  </acceptance_criteria>
  <done>Home/Index resolve effective user; split-brain L38/L53 hilang; mode-role data kosong (D-03); Guide OUT; suite hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET Home/Index (impersonate) → dashboard progress/events | Self-worker-data; harus effective X (IDOR bila admin). |
| split-brain (role-level efektif + identitas asli) | Inkonsistensi = bug parsial; folding menutup celah. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-377-18 | Information Disclosure | GetProgress/GetUpcomingEvents pakai admin.Id → dashboard tampil progress admin saat impersonate X | mitigate (block: high) | Resolver hulu → userId=X.Id; data X (SC3). e2e Plan 06 assert. |
| T-377-19 | Information Disclosure | Mode-role → fallback admin progress | mitigate | userId="" saat RoleModeEmpty → 0 record, bukan admin (D-03). |
| T-377-20 | Scope creep / regression | Menyentuh Guide/GuideDetail (OUT) atau over-fix | mitigate | Hanya Index diedit; Guide A4 OUT; git diff acceptance. |
| T-377-21 | Regression (SC4) | Index non-impersonate berubah | mitigate | Branch `UseRealUser` → `GetUserAsync(User)` + `Challenge()` identik; full suite hijau. |
</threat_model>

<verification>
- Build 0 error; full xUnit suite hijau (SC4).
- Index memuat GetEffectiveUserAsync; GetProgress/Events pakai effective userId.
- git diff hanya Index (+ view null-guard); Guide/GuideDetail utuh.
</verification>

<success_criteria>
- SC3 path siap (Home progress/events = data X) — divalidasi e2e Plan 06.
- Split-brain folded (D-05 konsistensi identitas).
- SC4 no regression.
</success_criteria>

<output>
After completion, create `.planning/phases/377-impersonation-identity-across-surfaces/377-05-SUMMARY.md`.
Catat: keputusan CurrentUser display saat mode-role (null vs admin-header-only), null-guard view, hasil suite.
</output>
</output>
