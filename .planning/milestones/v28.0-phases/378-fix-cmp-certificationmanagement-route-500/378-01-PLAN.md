---
phase: 378-fix-cmp-certificationmanagement-route-500
plan: 01
type: execute
wave: 1
depends_on: []
migration: false
autonomous: true
requirements: [CMPRT-01]
files_modified:
  - Controllers/CMPController.cs
  - tests/e2e/exam-types.spec.ts
  - Views/CDP/Shared/_SertifikatGroupTablePartial.cshtml

must_haves:
  truths:
    - "GET /CMP/CertificationManagement tidak lagi mengembalikan HTTP 500 (view-not-found)"
    - "GET /CMP/CertificationManagement me-redirect (302) ke /CDP/CertificationManagement yang resolve 200"
    - "Entry produktif Kelola Sertifikat (Views/CMP/Index.cshtml:98) tetap menunjuk route CDP — bukti SC1 tak ada link/test produktif butuh view CMP"
    - "CMPController.cs tetap kompilasi (dotnet build exit 0) setelah cluster dead cert-mgmt dihapus — tidak ada orphan-reference compile error"
    - "Path CDP CertificationManagement (action + view + JS) tidak disentuh — FLOW X, W0.X0, Y1/Y2 tetap hijau (SC3 no-regression)"
    - "Test e2e Y0 meng-assert redirect→CDP+status200 (bukan lagi documenting-only no-assert)"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Action CertificationManagement sebagai thin redirect ke CDP; cluster dead cert-mgmt + 2 orphan builder dihapus; BuildSertifikatRowsAsync dipertahankan (caller ExportSertifikatDetailExcel hidup)"
      contains: "RedirectToAction(\"CertificationManagement\", \"CDP\")"
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "Test Y0 di-tighten: assert page.url() berisi /CDP/CertificationManagement + status 200, tanpa cabang 500-tolerance"
      contains: "CDP/CertificationManagement"
  key_links:
    - from: "Controllers/CMPController.cs CertificationManagement()"
      to: "CDPController.CertificationManagement"
      via: "RedirectToAction string controller/action fixed"
      pattern: "RedirectToAction\\(\"CertificationManagement\", \"CDP\"\\)"
    - from: "tests/e2e/exam-types.spec.ts Y0"
      to: "/CMP/CertificationManagement → /CDP/CertificationManagement"
      via: "page.goto follow-redirect + expect(page.url()) + expect(status).toBe(200)"
      pattern: "expect\\(page\\.url\\(\\)\\)\\.toContain\\('/CDP/CertificationManagement'\\)"
---

<objective>
Fix `GET /CMP/CertificationManagement` yang HTTP 500 (view-not-found). Action `CMPController.CertificationManagement` adalah duplikat orphan — `return View(vm)` mencari `Views/CMP/CertificationManagement.cshtml` yang tak pernah dibuat. Entry produktif sudah route ke CDP canonical (`Views/CMP/Index.cshtml:98`).

Pendekatan (locked di 378-CONTEXT.md, D-01..D-06): ubah action jadi thin redirect 302 ke CDP, hapus cluster method dead cert-mgmt + 2 private builder yang jadi orphan, dan tighten 1 test e2e (Y0) jadi assert. **TIDAK** menyentuh path CDP (canonical) maupun model/view shared.

Purpose: Hilangkan route 500 yang membingungkan + bersihkan dead code (REQ CMPRT-01).
Output: CMPController.cs dengan redirect bersih + cluster dead dihapus; test Y0 yang meng-assert; (opsional) partial view dead dihapus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/378-fix-cmp-certificationmanagement-route-500/378-CONTEXT.md

# Project workflow rules — WAJIB
@CLAUDE.md

<audit_facts>
<!-- Hasil verifikasi kode AKTUAL saat planning (2026-06-14). Line number approximate — executor WAJIB locate by signature, bukan trust line. -->

ENTRY PRODUKTIF (bukti SC1):
- Views/CMP/Index.cshtml:98 → `@Url.Action("CertificationManagement", "CDP")` (route ke CDP, BUKAN CMP)
- 0 occurrence `asp-controller="CMP"` + `asp-action="CertificationManagement"` repo-wide
- Semua fetch JS produktif (Views/CDP/CertificationManagement.cshtml, Dashboard.cshtml, Admin/RenewalCertificate.cshtml) panggil `/CDP/GetCascadeOptions`, `/CDP/GetSubCategories`, `/CDP/FilterCertificationManagement`, `/CDP/ExportSertifikatExcel` — TIDAK ADA yang panggil endpoint /CMP/* cert-mgmt.

DELETE-SET CMP (semua 0 caller produktif — public methods, locate by signature):
- `GetCascadeOptions(string? section)` (~3591)
- `GetSubCategories(string? category)` (~3598)
- `FilterCertificationManagement(...)` (~3639) — return PartialView("Shared/_SertifikatGroupTablePartial")
- `CertificationManagementDetail(string judul, int page)` (~3666) — return View(vm) [view CMP detail juga tak ada]
- `FilterCertificationManagementDetail(...)` (~3699) — return PartialView("Shared/_CertificationManagementTablePartial")
- `ExportSertifikatExcel(...)` (~3738) — HANYA versi CMP; CDP punya sendiri, JANGAN sentuh

UBAH (bukan hapus):
- `CertificationManagement(int page = 1)` (~3616) → thin redirect ke CDP

KOMENTAR yang dihapus saat refactor:
- `// Cascade helpers for CertificationManagement filters` (~3587)
- `// CertificationManagement — dipindah dari CDPController` (~3613) [menyesatkan: nyatanya DUPLIKAT botched, bukan move]

PRIVATE BUILDERS — cek caller setelah cluster dihapus:
- `BuildSertifikatGroups(...)` (~3830) — caller saat ini: CertificationManagement(3620), FilterCertificationManagement(3647), ExportSertifikatExcel(3745). SEMUA dihapus → orphan → HAPUS.
- `BuildGroupViewModel(...)` (~3845) — caller: CertificationManagement(3622), FilterCertificationManagement(3656). SEMUA dihapus → orphan → HAPUS.
- ⚠️ `BuildSertifikatRowsAsync(...)` (~3868) — caller termasuk `ExportSertifikatDetailExcel` (~3777) yang TIDAK di delete-set → MASIH HIDUP → **KEEP** (jangan hapus). Re-grep defensif D-04 wajib konfirmasi ini.

JANGAN SENTUH (no-regression anchor SC3):
- `ExportSertifikatDetailExcel(...)` (~3777) — di luar delete-set CONTEXT D-03; biarkan utuh (memakai BuildSertifikatRowsAsync)
- `BuildActualCategories(...)` (~3823), `MapKategori(...)` (~3859) — di luar scope, biarkan
- Controllers/CDPController.cs — `CertificationManagement` (~3704) + helper CDP (canonical target redirect)
- Views/CDP/CertificationManagement.cshtml — view canonical (panggil partial `_CertificationManagementTablePartial`)
- Models/CertificationManagementViewModel.cs + class shared (D-05 KEEP)

PARTIAL VIEW (D-05 / discretion):
- `Views/CDP/Shared/_SertifikatGroupTablePartial.cshtml` — HANYA dipakai CMP.FilterCertificationManagement (3663, dihapus). Setelah delete → 0 ref produktif → kandidat HAPUS opsional.
- `Views/CDP/Shared/_CertificationManagementTablePartial.cshtml` — MASIH dipakai Views/CDP/CertificationManagement.cshtml:156 → **JANGAN HAPUS**.
</audit_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit-restate SC1 + ubah CertificationManagement jadi thin redirect (D-01/D-02)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (action CertificationManagement ~3616, locate by signature `public async Task<IActionResult> CertificationManagement(int page = 1)`)
    - Controllers/CDPController.cs (CertificationManagement ~3704 — bukti target redirect ada & canonical)
    - Views/CMP/Index.cshtml (baris ~98 — bukti entry link route ke CDP, restate SC1)
  </read_first>
  <action>
    LANGKAH 1 (audit SC1 — restate, bukan ubah kode):
    - Konfirmasi via grep bahwa entry produktif route ke CDP: `Views/CMP/Index.cshtml` baris 98 = `@Url.Action("CertificationManagement", "CDP")`.
    - Konfirmasi 0 occurrence kombinasi `asp-controller="CMP"` + `asp-action="CertificationManagement"` repo-wide (grep).
    - Konfirmasi 0 fetch JS ke `/CMP/CertificationManagement` (grep Views/ + wwwroot/). Catat hasil di SUMMARY sebagai bukti SC1.

    LANGKAH 2 (D-01/D-02 — ubah action jadi redirect):
    - Locate `public async Task<IActionResult> CertificationManagement(int page = 1)` (~3616) DAN comment block `// CertificationManagement — dipindah dari CDPController` (~3613) tepat di atasnya.
    - GANTI seluruh body action + comment menyesatkan dengan versi bersih (param `int page` jadi unused → drop; tak perlu async lagi):
      ```csharp
      public IActionResult CertificationManagement()
          => RedirectToAction("CertificationManagement", "CDP");
      ```
      Gunakan `RedirectToAction` default (302 temporary) — BUKAN `RedirectToActionPermanent`/301 (D-02: hindari browser-cache lock-in). String controller/action FIXED ("CertificationManagement","CDP") — bukan dari input user (no open-redirect, lihat threat_model T-378-01).
    - Hapus comment block menyesatkan `// CertificationManagement — dipindah dari CDPController` (3 baris dengan garis `====`).
    - JANGAN buat file Views/CMP/CertificationManagement.cshtml — kita redirect, bukan tambah view.

    LANGKAH 3: `dotnet build` — pastikan exit 0 (action redirect baru valid; sisa cluster dead masih ada, dihapus Task 2/3).
  </action>
  <acceptance_criteria>
    - grep `Controllers/CMPController.cs` mengandung literal `RedirectToAction("CertificationManagement", "CDP")` (1 occurrence).
    - grep `Controllers/CMPController.cs` TIDAK lagi mengandung `// CertificationManagement — dipindah dari CDPController`.
    - grep TIDAK ada file baru `Views/CMP/CertificationManagement.cshtml` dibuat (0 hit `Get-ChildItem Views/CMP/CertificationManagement.cshtml`).
    - `Views/CMP/Index.cshtml:98` tetap `@Url.Action("CertificationManagement", "CDP")` (tidak diubah — bukti SC1 dicatat di SUMMARY).
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>Action CertificationManagement = thin redirect 302 ke CDP; comment menyesatkan dihapus; tak ada view CMP dibuat; build hijau; bukti SC1 (entry→CDP) dicatat.</done>
</task>

<task type="auto">
  <name>Task 2: Hapus cluster dead public cert-mgmt CMP (D-03) + verify build</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (region ~3586–3773: comment header + 6 method dead; locate setiap method by signature)
  </read_first>
  <action>
    Hapus method PUBLIK dead berikut dari Controllers/CMPController.cs — locate masing-masing by signature (line approximate), hapus seluruh body method INCL. attribute (`[HttpGet]`, `[Authorize...]`) di atasnya:
    1. `GetCascadeOptions(string? section)` (~3591) + `[HttpGet]`
    2. `GetSubCategories(string? category)` (~3598) + `[HttpGet]`
    3. `FilterCertificationManagement(...)` (~3639) + `[HttpGet]`  ← me-return PartialView("Shared/_SertifikatGroupTablePartial")
    4. `CertificationManagementDetail(string judul, int page)` (~3666)
    5. `FilterCertificationManagementDetail(...)` (~3699) + `[HttpGet]`  ← me-return PartialView("Shared/_CertificationManagementTablePartial")
    6. `ExportSertifikatExcel(string? category, string? subCategory, string? search)` (~3738) + `[HttpGet][Authorize(Roles="Admin, HC")]`

    JUGA hapus comment header yang jadi kosong/menyesatkan:
    - `// Cascade helpers for CertificationManagement filters` (~3587) + garis `====` pembungkusnya.

    JANGAN HAPUS (no-regression):
    - `ExportSertifikatDetailExcel(...)` (~3777) — di luar delete-set CONTEXT, biarkan utuh.
    - Action `CertificationManagement` (sudah jadi redirect di Task 1).
    - `BuildActualCategories`, `MapKategori`, `BuildSertifikatGroups/BuildGroupViewModel/BuildSertifikatRowsAsync` (builder ditangani Task 3).
    - Apapun di CDPController.cs.

    Setelah hapus: `dotnet build`. Build menangkap orphan-reference (kalau ada method tak terduga masih panggil yang dihapus). Jika build error karena BuildSertifikatGroups/BuildGroupViewModel kini tak terpakai → itu warning bukan error (private unused = CS0169/IDE0051 warning, bukan compile error); builder akan dihapus Task 3.
  </action>
  <acceptance_criteria>
    - `grep -c "FilterCertificationManagementDetail" Controllers/CMPController.cs` = 0.
    - `grep -c "CertificationManagementDetail" Controllers/CMPController.cs` = 0 (catatan: `FilterCertificationManagementDetail` substring sudah ikut 0; pastikan tak ada sisa).
    - grep `Controllers/CMPController.cs` untuk `FilterCertificationManagement` (tanpa Detail) = 0.
    - grep `Controllers/CMPController.cs` untuk `public async Task<IActionResult> GetCascadeOptions` = 0 DAN `public async Task<IActionResult> GetSubCategories` = 0 (versi CMP hilang; CDP file terpisah, tak terpengaruh).
    - grep `Controllers/CMPController.cs` untuk `ExportSertifikatExcel\b` = 0 occurrence dari method `ExportSertifikatExcel` (catatan: `ExportSertifikatDetailExcel` MASIH ada = 1 occurrence yang BERBEDA dan SAH).
    - grep `Controllers/CMPController.cs` mengandung `ExportSertifikatDetailExcel` = tetap ≥1 (TIDAK terhapus).
    - grep `Controllers/CMPController.cs` TIDAK mengandung `// Cascade helpers for CertificationManagement filters`.
    - `dotnet build` exit 0 (0 error; warning private-unused diizinkan, dibereskan Task 3).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>6 method public cert-mgmt dead + comment header dihapus; ExportSertifikatDetailExcel & CDP utuh; build exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Hapus orphan private builder dgn re-grep defensif (D-04) + verify build</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (private builders ~3830 BuildSertifikatGroups, ~3845 BuildGroupViewModel, ~3868 BuildSertifikatRowsAsync — locate by signature; cek SEMUA caller pasca Task 2)
  </read_first>
  <action>
    DEFENSIVE RE-GREP (WAJIB — file 3900+ baris, line number tak boleh dipercaya):
    Untuk SETIAP dari 3 builder, grep ENTIRE Controllers/CMPController.cs hitung occurrence nama method MINUS baris deklarasinya sendiri = jumlah caller tersisa.

    1. `BuildSertifikatGroups` — setelah Task 2, caller (CertificationManagement/FilterCertificationManagement/ExportSertifikatExcel) sudah hilang. Re-grep harus = 1 occurrence (deklarasi saja, 0 caller) → HAPUS seluruh method (`private static List<SertifikatGroupRow> BuildSertifikatGroups(...)`).
    2. `BuildGroupViewModel` — caller (CertificationManagement/FilterCertificationManagement) sudah hilang. Re-grep harus = 1 (deklarasi saja) → HAPUS (`private static SertifikatGroupViewModel BuildGroupViewModel(...)`).
    3. ⚠️ `BuildSertifikatRowsAsync` — re-grep akan menemukan caller MASIH HIDUP: `ExportSertifikatDetailExcel` (~3777, di luar delete-set). Occurrence ≥ 2 (deklarasi + ≥1 caller) → **KEEP, JANGAN HAPUS**. Ini hasil yang BENAR — D-04 defensive rule: "If any builder still has a caller, KEEP it."

    ATURAN UMUM: HAPUS hanya jika caller tersisa = 0. Jika masih ada caller, KEEP dan catat alasannya di SUMMARY.

    Setelah hapus 2 builder orphan: `dotnet build` exit 0 (sekaligus menghilangkan warning private-unused dari Task 2).
  </action>
  <acceptance_criteria>
    - grep `Controllers/CMPController.cs` untuk `BuildSertifikatGroups` = 0 occurrence (dihapus penuh, 0 caller + 0 deklarasi).
    - grep `Controllers/CMPController.cs` untuk `BuildGroupViewModel` = 0 occurrence (dihapus penuh).
    - grep `Controllers/CMPController.cs` untuk `BuildSertifikatRowsAsync` = TETAP ≥2 occurrence (deklarasi + caller ExportSertifikatDetailExcel) — TIDAK dihapus; SUMMARY mencatat alasan KEEP (caller hidup).
    - grep `Controllers/CMPController.cs` mengandung `ExportSertifikatDetailExcel` (caller yang menjaga BuildSertifikatRowsAsync hidup) = tetap ada.
    - `dotnet build` exit 0, 0 warning private-method-unused untuk 2 builder yang dihapus.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>BuildSertifikatGroups + BuildGroupViewModel dihapus (terbukti 0 caller via re-grep); BuildSertifikatRowsAsync di-KEEP (caller ExportSertifikatDetailExcel hidup); build hijau.</done>
</task>

<task type="auto">
  <name>Task 4: Tighten e2e Y0 jadi assert redirect→CDP (D-06) + cleanup partial dead opsional (D-05)</name>
  <files>tests/e2e/exam-types.spec.ts, Views/CDP/Shared/_SertifikatGroupTablePartial.cshtml</files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (test Y0 ~2045 'Y0 — /CMP/CertificationManagement status documenting', describe "FLOW Y — Gap Closure Smoke" ~2044)
    - Controllers/CMPController.cs (konfirmasi redirect Task 1 sudah ada — sumber kebenaran yang di-assert test)
  </read_first>
  <action>
    LANGKAH 1 (D-06 — tighten Y0):
    - Locate test `'Y0 — /CMP/CertificationManagement status documenting (Gap 2)'` (~2045).
    - GANTI body test (hapus seluruh logika documenting/console.log-only + cabang `if (status === 500)`). Versi baru (Playwright follow redirect by default pada page.goto):
      ```ts
      test('Y0 — /CMP/CertificationManagement redirects to CDP canonical (Gap 2)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        const response = await page.goto('/CMP/CertificationManagement');
        // page.goto mengikuti redirect 302 → response = halaman CDP final
        expect(response?.status()).toBe(200);
        expect(response?.status()).not.toBe(500);
        expect(page.url()).toContain('/CDP/CertificationManagement');
      });
      ```
    - Update juga judul test (drop kata "documenting") + perbarui komentar header FLOW Y blok `Y0:` (~2039) agar tak lagi bilang "document".
    - Jaga `--workers=1` compatible (tak ada perubahan struktur describe; cuma body 1 test). JANGAN ubah Y1/Y2 (~2060-2086) atau FLOW X (~1951) atau W0.X0 (~1938) — itu target CDP canonical, no-regression SC3.

    LANGKAH 2 (D-05 — cleanup partial dead opsional, discretion):
    - Grep repo-wide untuk `_SertifikatGroupTablePartial` di file produktif (Views/**/*.cshtml + Controllers/*.cs + wwwroot/**/*.js). Setelah Task 2 menghapus FilterCertificationManagement, occurrence produktif harus = 0 (sisa hanya di .planning/ arsip = bukan kode).
    - JIKA 0 referensi produktif → HAPUS file `Views/CDP/Shared/_SertifikatGroupTablePartial.cshtml`.
    - JIKA masih ada referensi produktif tak terduga → BIARKAN file (inert) dan catat di SUMMARY. (Bukan blocker.)
    - JANGAN HAPUS `Views/CDP/Shared/_CertificationManagementTablePartial.cshtml` — masih dipakai Views/CDP/CertificationManagement.cshtml:156 (live).

    LANGKAH 3 (verifikasi):
    - `dotnet build` exit 0 (memastikan tak ada PartialView reference compile-time pecah — Razor partial dipanggil by string, build tetap acuan utama).
    - Best-effort e2e (butuh env lokal per CLAUDE.md: `Authentication__UseActiveDirectory=false`, SQLBrowser + lpc: conn override, `--workers=1`): `npx playwright test exam-types.spec.ts -g "Y0" --workers=1`. JIKA env e2e tak tersedia → edit test-file tetap valid (autonomous); tandai run e2e sebagai best-effort di SUMMARY, jangan blok phase.
  </action>
  <acceptance_criteria>
    - `tests/e2e/exam-types.spec.ts` test Y0 mengandung `expect(page.url()).toContain('/CDP/CertificationManagement')` DAN `expect(response?.status()).toBe(200)`.
    - `tests/e2e/exam-types.spec.ts` test Y0 TIDAK lagi mengandung cabang `if (status === 500)` maupun `console.log('[Y0]`...) documenting-only.
    - grep `_SertifikatGroupTablePartial` di Views/**/*.cshtml + Controllers/*.cs + wwwroot/**/*.js (file produktif) = 0 referensi (arsip .planning/ diabaikan).
    - JIKA dihapus: `Views/CDP/Shared/_SertifikatGroupTablePartial.cshtml` tak lagi ada (Get-ChildItem 0 hit). JIKA dibiarkan: alasan dicatat di SUMMARY.
    - `Views/CDP/Shared/_CertificationManagementTablePartial.cshtml` TETAP ada (dipakai CDP view).
    - FLOW X / W0.X0 / Y1 / Y2 block di exam-types.spec.ts TIDAK diubah (grep diff hanya menyentuh Y0 + komentar header).
    - `dotnet build` exit 0.
    - (best-effort) `npx playwright test exam-types.spec.ts -g "Y0" --workers=1` hijau JIKA env e2e lokal tersedia.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>Y0 meng-assert redirect→/CDP/CertificationManagement+200 (no 500-tolerance); partial dead dihapus jika 0 ref produktif; CDP partial & test CDP lain utuh; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /CMP/CertificationManagement | request GET dari user terautentikasi; setelah fix = redirect server-side ke action internal fixed |
| (none new) | Tidak ada input user baru diproses; redirect target hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-378-01 | Tampering / Open-Redirect | CMPController.CertificationManagement redirect | mitigate | `RedirectToAction("CertificationManagement", "CDP")` memakai string controller/action FIXED literal — TIDAK ada input user (`int page` dihapus). Tak ada permukaan open-redirect. Acceptance Task 1 grep memverifikasi literal fixed, bukan variabel. |
| T-378-02 | Elevation of Privilege | penghapusan method dead menghilangkan permukaan [Authorize] | accept | Method dihapus (GetCascadeOptions/GetSubCategories/Filter*/Detail*/ExportSertifikatExcel CMP) adalah 0-caller produktif; analog ber-`[Authorize]` (CDP) tetap utuh & canonical. Menghapus endpoint dead MENGURANGI attack surface, bukan menambah. dotnet build + grep 0-caller = bukti tak ada konsumen kehilangan akses. |
| T-378-03 | Information Disclosure | CDP CertificationManagement otorisasi pasca-redirect | accept | Action CDP target redirect TIDAK diubah — mempertahankan kontrol otorisasi existing-nya (role-scoped via BuildSertifikatRowsAsync L4/L5 scoping di CDP). Redirect tidak melonggarkan auth; user yang sebelumnya 500 kini melihat data CDP yang sudah ter-scope identik dgn entry produktif. |
| T-378-04 | Denial of Service | redirect loop | accept | Redirect satu-arah CMP→CDP; CDP TIDAK redirect balik ke CMP (action CDP render View langsung). Tak ada siklus. dotnet build + test Y0 (status 200 final) membuktikan resolve tunggal. |

Semua threat low-severity (route consolidation + dead-code deletion + 1 test). Tidak ada threat high-severity; ASVS L1 terpenuhi (no new input boundary, redirect fixed-target, auth canonical CDP tak tersentuh).
</threat_model>

<verification>
Phase-level checks (semua di lokal per CLAUDE.md — JANGAN edit/run di Dev/Prod):
1. `dotnet build` exit 0 setelah SEMUA task (no orphan-reference compile error) — gate utama untuk delete-heavy work.
2. grep CMPController.cs: redirect ada (`RedirectToAction("CertificationManagement", "CDP")`); 6 method dead + 2 builder orphan = 0 occurrence; `BuildSertifikatRowsAsync` + `ExportSertifikatDetailExcel` = tetap ada (KEEP).
3. grep no-regression: CDPController.cs + Views/CDP/* tak disentuh (git diff kosong untuk path itu); Views/CMP/Index.cshtml:98 tak berubah.
4. test Y0 di exam-types.spec.ts meng-assert /CDP/CertificationManagement + 200 (bukan documenting).
5. (best-effort, butuh env e2e lokal) `npx playwright test exam-types.spec.ts --workers=1` — Y0 hijau, FLOW X/W0.X0/Y1/Y2 tetap hijau (SC3).
6. `dotnet test` (xUnit) — pastikan suite unit tetap hijau (tak ada unit menyentuh cert-mgmt CMP yang dihapus; jalankan sebagai sanity net).
</verification>

<success_criteria>
- SC1 (audit): grep membuktikan entry produktif = `Url.Action("CertificationManagement","CDP")` (Views/CMP/Index.cshtml:98) + 0 link/test produktif butuh view CMP. Dicatat di SUMMARY.
- SC2 (no-500): `GET /CMP/CertificationManagement` redirect 302 → `/CDP/CertificationManagement` resolve 200 (tidak lagi 500). Dibuktikan test Y0 + dotnet build.
- SC3 (no-regression CDP): CDPController.cs + Views/CDP/* + test CDP (FLOW X, W0.X0, Y1, Y2) tidak disentuh & tetap hijau.
- Build hijau setelah penghapusan dead code; migration=false.
</success_criteria>

<output>
After completion, create `.planning/phases/378-fix-cmp-certificationmanagement-route-500/378-01-SUMMARY.md`.
WAJIB git add eksplisit SUMMARY (lesson: untracked SUMMARY pernah hilang). Catat di SUMMARY:
- Bukti SC1 (grep entry→CDP).
- Konfirmasi BuildSertifikatRowsAsync di-KEEP (alasan: caller ExportSertifikatDetailExcel hidup).
- Keputusan D-05 (partial dihapus / dibiarkan) + alasan.
- Status run e2e (hijau / di-skip karena env e2e lokal tak tersedia).
- Reminder: migration=false; NOT PUSHED (handoff IT terpisah).
</output>
