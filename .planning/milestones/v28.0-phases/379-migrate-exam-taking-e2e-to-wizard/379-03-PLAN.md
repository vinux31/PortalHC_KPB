---
phase: 379-migrate-exam-taking-e2e-to-wizard
plan: 03
type: execute
wave: 3
depends_on: [379-02]
files_modified:
  - tests/e2e/exam-taking.spec.ts
autonomous: true
requirements: [E2E-01]
tags: [e2e, playwright, test-infra, wizard, migration, proton, paste-import]

must_haves:
  truths:
    - "Flow D (package-based + paste-import + reshuffle) berjalan via wizard — hijau, fixme dihapus"
    - "Flow E (Proton Tahun 3 interview) berjalan PENUH via wizard + proton extension — hijau, fixme dihapus, NO test.skip"
    - "Flow E interview form (judges/aspect_*/notes/isPassed) ter-verifikasi cocok controller v25.0 — submit hijau"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "Flow D paste-import + Flow E proton create+interview pakai helper; 2 fixme dihapus; test.skip Proton dihapus"
      contains: "protonTrackTahun"
  key_links:
    - from: "exam-taking.spec.ts Flow E1"
      to: "createAssessmentViaWizard({category:'Assessment Proton', protonTrackTahun:'Tahun 3'})"
      via: "proton extension Plan 01"
      pattern: "protonTrackTahun"
    - from: "exam-taking.spec.ts Flow E3"
      to: "form[action*='SubmitInterviewResults']"
      via: "interview submit (judges/aspect_*/notes/isPassed)"
      pattern: "SubmitInterviewResults"
---

<objective>
Migrasi Flow D (package-based + paste-import + reshuffle) dan Flow E (Proton Tahun 3 interview — D-02 double-drift) di `tests/e2e/exam-taking.spec.ts`. Flow E = migrasi PENUH: create via wizard + proton extension (Step 1) + re-check interview form terhadap Proton v25.0 (Phase 358-363). HAPUS `test.skip` Proton (D-02 LARANG skip — ProtonTrack T3 sudah disiapkan Plan 01). Hapus 2 fixme.

Purpose: 2 flow paling berisiko-drift dikelompokkan (D paste-import, E proton). Single-file constraint: depends_on Plan 02 (file sama exam-taking.spec.ts), sequential — wave 3, tidak paralel dengan plan lain pada file ini. Flow E adalah satu-satunya yang butuh data ProtonTrack T3 (Plan 01 Task 2) + interview form re-check.
Output: Flow D + E hijau `--workers=1`, 2 fixme + test.skip Proton dihapus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-CONTEXT.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md

<interfaces>
<!-- Helper kanonik + extension Plan 01. -->
From tests/e2e/helpers/examTypes.ts:
```typescript
createAssessmentViaWizard(page, { ...standardOpts,
  protonTrackTahun?: 'Tahun 1'|'Tahun 2'|'Tahun 3',  // extension Plan 01 (pilih by data-tahun)
  protonTrackId?: number }): Promise<void>
createDefaultPackage(page, packageName?): Promise<number>
addQuestionViaForm(page, packageId, QuestionInput): Promise<void>
importQuestionsViaPaste(page, packageId, tsvRows): Promise<void>  // ADA jika W0-2 VALID (Plan 01)
```

Flow E interview submit (E3 — verified controller AssessmentAdminController.cs:3669-3705 + view AssessmentMonitoringDetail.cshtml:657-733):
```typescript
const form = page.locator('form[action*="SubmitInterviewResults"]').first();
await form.locator('input[name="judges"]').fill('Dr. Andi, Ir. Budi');
const aspects = form.locator('select[name^="aspect_"]');           // 5 aspek
for (let i = 0; i < await aspects.count(); i++) await aspects.nth(i).selectOption('4');
await form.locator('textarea[name="notes"]').fill('E2E — kompetensi baik.');
await form.locator('input[name="isPassed"]').check();
await form.locator('button[type="submit"]').click();              // @Html.AntiForgeryToken auto-included
await page.waitForLoadState('networkidle');
await expect(page.locator('body')).toContainText(/Lulus|berhasil disimpan/i);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrasi Flow D (create + paste-import), hapus fixme D</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:549-705 (Flow D full: D1 create flat-form :555-574, D2 ManagePackages create package :576-598, D3 paste-import :599-621, D4 reshuffle, D5 worker start, D6 answer+submit, D7 cleanup — D4-D7 SURVIVE)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"Flow D (paste-import)"
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md (W0-2: paste route VALID → helper importQuestionsViaPaste, DRIFT → fallback addQuestionViaForm×3)
  </read_first>
  <action>
    (1a) Hapus `test.fixme` Flow D (`:551`).

    (1b) Migrasi D1 (`:555-574`): ganti flat-form create dengan helper:
    ```typescript
    title = uniqueTitle('Pre Test Package Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 30, passPercentage: 60, allowAnswerReview: true,
      participantEmails: ['rino.prasetyo@pertamina.com'],
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    ```

    (1c) Migrasi D2 (`:576-598`): D2 sudah package-aware (`input[name="packageName"]` + Create Package). Ganti dengan `packageId = await createDefaultPackage(page)` (navigate ke `/Admin/ManagePackages?assessmentId=${assessmentId}` dulu bila perlu). Deklarasi `let packageId` di scope describe.

    (1d) Migrasi D3 (`:599-621`) — paste-import (nilai UNIK Flow D, PRESERVE coverage):
    - Bila W0-2 = VALID: ganti dengan `await importQuestionsViaPaste(page, packageId, tsvRows)` dengan `tsvRows` = 6-kolom TSV (Q\toptA\toptB\toptC\toptD\tcorrect) seperti data D3 asli (minimal 3 baris).
    - Bila W0-2 = DRIFT: fallback `addQuestionViaForm(page, packageId, {...})` ×3 (3 MC), catat di SUMMARY bahwa paste-import route drift → di-surface ke backlog.

    (1e) D4 reshuffle, D5 worker start, D6 answer+submit, D7 cleanup = SURVIVE. D6 answer: bila positional `.nth()` fragile (shuffle aktif), ganti ke option-text match (PATTERNS Pattern 3). Minimal-change bila hijau.

    (1f) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow D"` → passed.
  </action>
  <acceptance_criteria>
    - Flow D describe TIDAK lagi `test.fixme` (grep di range = 0).
    - Blok Flow D pakai `createAssessmentViaWizard` + `createDefaultPackage`.
    - Paste-import coverage dipertahankan: `importQuestionsViaPaste` dipakai (W0-2 VALID) ATAU fallback addQuestionViaForm×3 + catatan backlog di SUMMARY (W0-2 DRIFT).
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow D"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow D"</automated>
  </verify>
  <done>Flow D pakai wizard+package, paste-import coverage dipertahankan, fixme dihapus, hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Migrasi Flow E (Proton T3 create via extension), hapus test.skip + fixme E</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:706-837 (Flow E full: E1 create Proton flat-form :711-755 dengan test.skip :741-745, E2 badge "Interview Dijadwalkan" no-Start, E3 SubmitInterviewResults form :790-813, E4 cleanup)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"Flow E (Proton T3 interview)" + Shared Patterns "Proton Extension"
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md (W0-1 ProtonTrack T3 ada)
    - tests/e2e/helpers/examTypes.ts (blok proton STEP 1 yang ditambah Plan 01)
    - Views/Admin/AssessmentMonitoringDetail.cshtml:657-733 (interview form field) + Controllers/AssessmentAdminController.cs:3669-3705 (SubmitInterviewResults)
  </read_first>
  <action>
    (2a) Hapus `test.fixme` Flow E (`:708`) DAN hapus blok `test.skip(true, 'No Tahun 3 ProtonTrack available')` (`:741-745`) — D-02 LARANG skip; ProtonTrack T3 sudah disiapkan Plan 01.

    (2b) Migrasi E1 (`:711-755`): ganti flat-form create Proton (selectOption '#Category' 'Assessment Proton' + manual loop `#protonTrackSelect` options + fill schedule/duration/passPercentage + submitBtn) dengan helper + proton extension:
    ```typescript
    title = uniqueTitle('Pre Test Proton T3 Interview');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'Assessment Proton', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 0,             // Tahun 3 offline interview (duration boleh 0)
      passPercentage: 60, allowAnswerReview: false,
      participantEmails: ['rino.prasetyo@pertamina.com'],
      protonTrackTahun: 'Tahun 3',    // extension Plan 01: pilih track by data-tahun
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    ```
    Catatan: controller set `TahunKe='Tahun 3'`. Bila wizard Step 3 auto-hide duration untuk Tahun 3, helper tetap aman (durationMinutes di-pass; bila field hidden, sesuaikan helper agar skip fill—tapi itu sudah di Plan 01 scope helper, jangan ubah di sini; bila gagal, catat untuk Plan 01 fix).

    (2c) E2 (badge "Interview Dijadwalkan" / no Start button untuk Tahun 3) = SURVIVE. Verifikasi cepat selector badge masih cocok (CMP/Assessment.cshtml:417-446). Bila drift, sesuaikan selector test (test-only, bukan view).

    (2d) E3 interview submit = re-check vs controller v25.0 (lihat <interfaces>). Pakai field `judges`/`aspect_*`/`notes`/`isPassed` + `button[type="submit"]` (antiforgery auto). Form ada di AssessmentMonitoringDetail. Navigate ke monitoring detail via `goToMonitoringDetail(page, title)` (helper existing di file). Assert hasil: `body` contains `/Lulus|berhasil disimpan/i`. OPSIONAL DB-assert persist via `db.queryScalar` (interview result tersimpan).

    (2e) E4 cleanup = SURVIVE.

    (2f) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow E"` → passed (PENUH, tanpa skip).

    Bila interview form ter-drift Proton 358-363 (field hilang/berubah) → SURFACE ke backlog (catat di SUMMARY) + sesuaikan test agar hijau bila perubahan murni test-side; JANGAN fix kode produksi (CONTEXT D).
  </action>
  <acceptance_criteria>
    - Flow E describe TIDAK lagi `test.fixme` (grep di range = 0).
    - Tidak ada `test.skip` di blok Flow E (`grep "test.skip" range Flow E` = 0) — D-02.
    - Blok Flow E pakai `protonTrackTahun: 'Tahun 3'` via createAssessmentViaWizard.
    - E3 pakai `form[action*="SubmitInterviewResults"]` + field judges/aspect_*/notes/isPassed.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow E"` → "passed" (semua E1-E4 RUN, bukan skip).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow E"</automated>
  </verify>
  <done>Flow E migrasi PENUH (create proton extension + interview re-check), test.skip + fixme dihapus, hijau penuh tanpa skip (D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app localhost | wizard create proton + interview submit via real form (antiforgery auto) |
| test → SQL localhost | (Flow E optional) db.queryScalar verify interview persist — localhost only |
| ProtonTrack T3 seed → DB lokal | data prereq Flow E (Plan 01) — temporary, restore Plan 06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-379-07 | Tampering | seed ProtonTrack T3 + assessment proton nempel pasca-run | mitigate | global.teardown db.restore() + journal restore Plan 06 + uniqueTitle isolasi |
| T-379-08 | Spoofing/CSRF | interview form SubmitInterviewResults | accept | form pakai @Html.AntiForgeryToken (test submit via real form → token auto-included); bukan attack surface baru |
| T-379-09 | Scope | migrasi ungkap drift Proton produksi → tergoda fix inline | accept | CONTEXT D: drift prod → backlog, test menyesuaikan test-side only; tidak ubah controller/view |

**Honest note:** Flow E menguji interview form existing v25.0. NOL perubahan kode produksi; drift apa pun → backlog.
</threat_model>

<verification>
- Flow D + E hijau `--workers=1` (run terpisah).
- 2 fixme dihapus; test.skip Proton dihapus (D-02).
- Paste-import coverage dipertahankan (atau fallback + catatan backlog).
- Interview form re-check hijau.
</verification>

<success_criteria>
- Flow D, E migrasi ke wizard (SC1 sebagian); E PENUH tanpa skip (D-02).
- Proton extension (Plan 01) terbukti dipakai Flow E.
- Paste-import nilai unik Flow D dipertahankan.
</success_criteria>

<output>
After completion, create `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-03-SUMMARY.md`
</output>
