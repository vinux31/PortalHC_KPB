---
phase: 379-migrate-exam-taking-e2e-to-wizard
plan: 04
type: execute
wave: 4
depends_on: [379-03]
files_modified:
  - tests/e2e/exam-taking.spec.ts
autonomous: true
requirements: [E2E-01]
tags: [e2e, playwright, test-infra, wizard, migration, deterministic, timer]

must_haves:
  truths:
    - "Flow F (multiple workers same assessment) berjalan via wizard — hijau, fixme dihapus"
    - "Flow G (timer expired) berjalan via wizard + deterministik (no waitForTimeout 70s) — hijau, fixme dihapus"
    - "Flow H (real-time monitoring) berjalan via wizard + sleep-buta terbesar diganti event-driven assert — hijau, fixme dihapus"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "Flow F/G/H create+question pakai wizard; G timer deterministik via waitForFunction; H6 sleep diganti auto-retry assert; 3 fixme dihapus"
      contains: "waitForFunction"
  key_links:
    - from: "exam-taking.spec.ts Flow G2"
      to: "waitForFunction (timerStartRemaining<=0 / #examExpiredModal / Results URL)"
      via: "ganti waitForTimeout(70_000) deterministik (D-03)"
      pattern: "waitForFunction"
    - from: "exam-taking.spec.ts Flow H"
      to: "GetMonitoringProgress JSON + count cards auto-retry"
      via: "ganti waitForTimeout(12_000) → expect toBeHidden/toHaveText"
      pattern: "GetMonitoringProgress"
---

<objective>
Migrasi Flow F (multiple workers same assessment), G (exam timer expired), H (real-time monitoring) di `tests/e2e/exam-taking.spec.ts`. Flow G + H = deterministik (D-03): ganti `waitForTimeout(70_000)` Flow G dengan `waitForFunction` event-driven, ganti sleep-buta terbesar Flow H (`waitForTimeout(12_000)`) dengan assertion auto-retry. Hapus 3 fixme.

Purpose: Batch flow time-dependent (G timer, H real-time) + multi-worker (F). D-03 mandat deterministik untuk hindari flaky/lambat. Single-file constraint: depends_on Plan 03 (file sama), sequential wave 4. Flow G/H paling rentan flaky → fokus deterministik.
Output: Flow F + G + H hijau `--workers=1`, 3 fixme dihapus, sleep-buta terbesar dihilangkan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-CONTEXT.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md

<interfaces>
From tests/e2e/helpers/examTypes.ts: createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm (lihat Plan 02 interfaces).
From tests/helpers/dbSnapshot.ts: db.queryScalar(sql): Promise<number> (Flow G fallback Status assert).

Flow G deterministik (ganti waitForTimeout(70_000) :1032) — event-driven (W0-3 dependent):
```typescript
// Bila W0-3 = #examExpiredModal ADA:
await page.waitForFunction(() => {
  const remaining = (window as unknown as { timerStartRemaining?: number }).timerStartRemaining ?? 1;
  const expired = document.querySelector('#examExpiredModal');
  return remaining <= 0 || (expired && getComputedStyle(expired).display !== 'none')
    || /\/CMP\/Results\//.test(location.href);
}, undefined, { timeout: 75_000 });
// Bila W0-3 = ABSEN: fallback poll DB-state:
//   await expect.poll(async () => db.queryScalar(`SELECT COUNT(*) FROM AssessmentSessions WHERE Id=${sessionId} AND Status IN ('Completed','Abandoned')`), { timeout: 75_000 }).toBe(1);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrasi Flow F (multiple workers), hapus fixme F</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:838-969 (Flow F full: F1 create 2-worker, F2 QADD 1 MC, F3 coachee, F4 coachee2, F5 monitoring, F6 cleanup — F3-F6 SURVIVE)
    - tests/e2e/exam-types.spec.ts:314-355 (analog create+QADD)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §Shared Patterns "Auth"
  </read_first>
  <action>
    (1a) Hapus `test.fixme` Flow F (`:840`).

    (1b) Migrasi F1: ganti flat-form create dengan helper, 2 peserta:
    ```typescript
    title = uniqueTitle('Pre Test MultiWorker Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 30, passPercentage: 60, allowAnswerReview: false,
      participantEmails: ['rino.prasetyo@pertamina.com', 'iwan3@pertamina.com'],
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    ```

    (1c) Migrasi F2: createDefaultPackage + addQuestionViaForm ×1 (1 MC, sesuai Flow F asli). Deklarasi `let packageId` bila perlu.

    (1d) F3 (coachee start/answer), F4 (`login(page,'coachee2')` start/answer), F5 (monitoring count), F6 cleanup = SURVIVE.

    (1e) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow F"` → passed.
  </action>
  <acceptance_criteria>
    - Flow F describe TIDAK lagi `test.fixme` (grep di range = 0).
    - Blok Flow F pakai `participantEmails: [...,'iwan3@pertamina.com']`.
    - Tidak ada residu flat-form selector di blok Flow F.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow F"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow F"</automated>
  </verify>
  <done>Flow F pakai wizard+package (2 worker), fixme dihapus, hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Migrasi Flow G (timer expired) + deterministik (no waitForTimeout 70s), hapus fixme G</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:970-1059 (Flow G full: G1 create durationMinutes:1 + QADD, G2 worker start :1018-1041 dengan waitForTimeout(70_000) :1032, G3 cleanup :1043-1058)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"Flow G (timer 1-min) deterministik"
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md (W0-3: #examExpiredModal ADA → waitForFunction modal; ABSEN → fallback DB-state poll)
    - tests/e2e/helpers/examTypes.ts:501-508 (pola waitForFunction event-driven)
  </read_first>
  <action>
    (2a) Hapus `test.fixme` Flow G (`:972`).

    (2b) Migrasi G1: create `durationMinutes: 1` + createDefaultPackage + addQuestionViaForm ×1:
    ```typescript
    title = uniqueTitle('Pre Test Timer Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 1, passPercentage: 60, allowAnswerReview: false,
      participantEmails: ['rino.prasetyo@pertamina.com'],
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    // packageId = await createDefaultPackage(page); addQuestionViaForm(...1 MC...)
    ```

    (2c) Migrasi G2 worker start (SURVIVE selector start) + GANTI `waitForTimeout(70_000)` (`:1032`) DENGAN deterministik (D-03). Capture `sessionId` dari URL `/CMP/StartExam/{id}` setelah start. Lalu sesuai W0-3:
    - W0-3 ADA `#examExpiredModal`: pakai `waitForFunction` (lihat <interfaces>) bounded 75s — resolve segera saat expired.
    - W0-3 ABSEN: pakai `await expect.poll(async () => await db.queryScalar(\`SELECT COUNT(*) FROM AssessmentSessions WHERE Id=${sessionId} AND Status IN ('Completed','Abandoned')\`), { timeout: 75_000, intervals: [2000] }).toBe(1);`
    Pertahankan assertion outcome existing (`onResults || onAssessment || modalVisible` ATAU DB Status) — tapi event-driven, BUKAN fixed sleep. HAPUS `waitForTimeout(70_000)`.

    (2d) G3 cleanup = SURVIVE.

    (2e) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow G"` → passed. Catat durasi run (harus < ~75s, resolve saat expired bukan tunggu penuh bila auto-submit lebih cepat).
  </action>
  <acceptance_criteria>
    - Flow G describe TIDAK lagi `test.fixme` (grep di range = 0).
    - Tidak ada `waitForTimeout(70_000)` ATAU `waitForTimeout(70000)` di blok Flow G (`grep` = 0) — D-03.
    - Blok Flow G pakai `waitForFunction` ATAU `expect.poll` (event-driven) untuk timer-expiry.
    - `durationMinutes: 1` dipakai di create Flow G.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow G"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow G"</automated>
  </verify>
  <done>Flow G pakai wizard+package, timer-expiry deterministik (waitForTimeout 70s dihapus), fixme dihapus, hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Migrasi Flow H (real-time monitoring) + ganti sleep-buta terbesar, hapus fixme H</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:1066-1301 (Flow H full: H1 create+QADD, H2-H4 count cards/badge/polling, H5 submit, H6 completed + waitForTimeout(12_000) poll-hide, H7 GetMonitoringProgress JSON, H8 cleanup)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"Flow G ... Flow H" deterministik bagian H
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md §"Deterministic Time-Dependent Flows" Flow H
  </read_first>
  <action>
    (3a) Hapus `test.fixme` Flow H (`:1068`).

    (3b) Migrasi H1: create + createDefaultPackage + addQuestionViaForm ×1 (1 MC). Pola sama Task 1/2.

    (3c) H2-H5, H7 (GetMonitoringProgress JSON), H8 = SURVIVE (sudah deterministik via count cards + JSON endpoint). H7 JSON fetch deterministik — biarkan.

    (3d) H6: ganti `waitForTimeout(12_000)` (poll-hide "Submit Assessment" / count update) dengan assertion auto-retry:
    - Untuk "Submit Assessment hidden": `await expect(closeBtn).toBeHidden({ timeout: 15_000 });` (auto-retry sampai poll-cycle update DOM).
    - Untuk count update: `await expect(page.locator('#count-completed')).toHaveText('1', { timeout: 15_000 });` (auto-retry).
    HAPUS `waitForTimeout(12_000)`. Boleh pertahankan `waitForTimeout(2_000)` kecil bila perlu (bukan sleep-buta terbesar) — prioritas hilangkan 12s.

    (3e) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow H"` → passed.
  </action>
  <acceptance_criteria>
    - Flow H describe TIDAK lagi `test.fixme` (grep di range = 0).
    - Tidak ada `waitForTimeout(12_000)` ATAU `waitForTimeout(12000)` di blok Flow H (`grep` = 0).
    - Blok Flow H pakai `expect(...).toBeHidden` ATAU `toHaveText` auto-retry untuk poll-state.
    - Blok Flow H tetap pakai `GetMonitoringProgress` JSON assert (SURVIVE).
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow H"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow H"</automated>
  </verify>
  <done>Flow H pakai wizard+package, sleep-buta 12s diganti auto-retry assert, JSON polling dipertahankan, fixme dihapus, hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app localhost | wizard create + worker exam + monitoring polling via UI |
| test → SQL localhost | (Flow G fallback) db.queryScalar Status poll — localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-379-10 | Tampering | test data F/G/H nempel pasca-run | mitigate | uniqueTitle isolasi + cleanup SURVIVE + global.teardown db.restore() |
| T-379-11 | DoS (flaky/timeout) | wall-clock sleep 70s/12s bikin suite lambat/flaky | mitigate | D-03: ganti event-driven waitForFunction/expect.poll/auto-retry assert (bounded) |
| T-379-12 | Scope | migrasi ungkap bug timer/monitoring produksi | accept | bug prod → backlog, test menyesuaikan test-side; tidak ubah kode produksi |

**Honest note:** NOL perubahan kode produksi. Deterministik = robustness test, bukan attack surface.
</threat_model>

<verification>
- Flow F + G + H hijau `--workers=1` (run terpisah).
- 3 fixme dihapus.
- Flow G: `waitForTimeout(70_000)` dihapus → event-driven.
- Flow H: `waitForTimeout(12_000)` dihapus → auto-retry assert; JSON polling dipertahankan.
</verification>

<success_criteria>
- Flow F, G, H migrasi ke wizard (SC1 sebagian).
- D-03 deterministik diterapkan (G timer + H sleep terbesar).
</success_criteria>

<output>
After completion, create `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-04-SUMMARY.md`
</output>
