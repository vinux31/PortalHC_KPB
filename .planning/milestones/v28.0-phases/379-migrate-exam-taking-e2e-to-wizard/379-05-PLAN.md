---
phase: 379-migrate-exam-taking-e2e-to-wizard
plan: 05
type: execute
wave: 5
depends_on: [379-04]
files_modified:
  - tests/e2e/exam-taking.spec.ts
autonomous: true
requirements: [E2E-01]
tags: [e2e, playwright, test-infra, wizard, migration, essay, grade-01, db-assert]

must_haves:
  truths:
    - "Flow I (edit assessment) berjalan via wizard create + edit-form (SURVIVE) — hijau, fixme dihapus"
    - "Flow J (abandon & reset recovery) berjalan via wizard — hijau, fixme dihapus"
    - "Flow K (BARU) essay full cycle + DB-assert Score teragregasi (===80, BUKAN 0) — hijau (sinergi GRADE-01/376)"
    - "10 fixme A-J SEMUA dihapus setelah plan ini (count test.fixme = 0)"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "Flow I/J wizard create; Flow K BARU port FLOW L (essay→grade→finalize→DB Score assert); 2 fixme dihapus; ZERO test.fixme tersisa"
      contains: "queryScalar"
  key_links:
    - from: "exam-taking.spec.ts Flow K K6"
      to: "AssessmentSessions.Score via db.queryScalar"
      via: "DB-assert Score===80 (bukan UI badge) — bukti fix GRADE-01"
      pattern: "queryScalar.*Score"
    - from: "exam-taking.spec.ts Flow K K5"
      to: "gradeSingleEssaySession(score:80) + FinalizeEssayGrading"
      via: "HC grade + finalize essay-only"
      pattern: "gradeSingleEssaySession"
---

<objective>
Migrasi Flow I (edit assessment) + J (abandon & reset recovery), DAN tambah Flow K BARU (D-01): essay full cycle + DB-assert Score teragregasi — port verbatim dari `exam-types.spec.ts` FLOW L (`:305-428`). Flow K = bukti hidup fix GRADE-01 Phase 376 di suite exam-taking: assert `AssessmentSessions.Score === 80` (BUKAN 0) via `db.queryScalar`, bukan UI badge. Setelah plan ini, SEMUA 10 fixme A-J terhapus.

Purpose: Batch terakhir migrasi (I/J) + flow esai baru (K) yang adalah deliverable sinergi terkuat dengan 376. Single-file constraint: depends_on Plan 04 (file sama), sequential wave 5 — plan terakhir yang edit exam-taking.spec.ts sebelum gate. Flow K = SC3 + E2E-01 essay coverage.
Output: Flow I + J + K hijau `--workers=1`; ZERO `test.fixme` tersisa di file; Flow K assert Score===80 via DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-CONTEXT.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md

<interfaces>
From tests/e2e/helpers/examTypes.ts:
```typescript
createAssessmentViaWizard(page, opts): Promise<void>
createDefaultPackage(page, packageName?): Promise<number>
addQuestionViaForm(page, packageId, QuestionInput): Promise<void>  // type 'Essay' → {type:'Essay', text, rubrik, maxCharacters?, score}
fillEssayAnswer(page, qCard: Locator, answer: string): Promise<void>  // direct SignalR invoke + await
submitExamTwoStep(page): Promise<void>  // StartExam → ExamSummary → Results
gradeSingleEssaySession(pageHc, { title, category, scheduleDate, sessionId, score }): Promise<void>  // grade + finalize
```
From tests/helpers/dbSnapshot.ts:
```typescript
db.queryScalar(sql: string): Promise<number>   // localhost-only guard
```

Flow K blueprint (port verbatim dari exam-types.spec.ts:305-428 FLOW L — un-fixme'd by Phase 376):
```typescript
test.describe('Flow K: Essay Full Cycle + Score Aggregation (GRADE-01)', () => {
  let title: string; const category = 'IHT'; let scheduleDate: string;
  let assessmentId: number; let packageId: number; let sessionId: number;
  const Q_MARKER = '[379-K] Essay GRADE-01 regression';

  test('K1 — HC creates Essay assessment via wizard', async ({ page }) => {
    title = uniqueTitle('Pre Test [379-K] Essay'); scheduleDate = today();
    await login(page, 'hc');
    await createAssessmentViaWizard(page, { title, category, scheduleDate, scheduleTime: '00:01',
      durationMinutes: 60, passPercentage: 70, allowAnswerReview: true,
      participantEmails: ['rino.prasetyo@pertamina.com'] });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
  });
  test('K2 — create package', async ({ page }) => {
    await login(page, 'hc');
    await page.goto(`/Admin/ManagePackages?assessmentId=${assessmentId}`);
    await page.waitForLoadState('networkidle');
    packageId = await createDefaultPackage(page);
  });
  test('K3 — add Essay question', async ({ page }) => {
    await login(page, 'hc');
    await addQuestionViaForm(page, packageId, { type: 'Essay',
      text: `${Q_MARKER} — jelaskan kompetensi OJT`, rubrik: 'Skor by kelengkapan',
      maxCharacters: 2000, score: 100 });
  });
  test('K4 — worker fills essay + submits', async ({ page }) => {
    await login(page, 'coachee'); await page.goto('/CMP/Assessment');
    const card = page.locator('.assessment-card', { hasText: title }).first();
    page.once('dialog', (d) => d.accept());
    await card.locator('a:has-text("Mulai"), .btn-start-standard, a:has-text("Resume")').first().click();
    await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });
    await page.waitForFunction(() => (window as any).assessmentHub?.state === 'Connected', undefined, { timeout: 15_000 });
    sessionId = parseInt(page.url().match(/StartExam\/(\d+)/)![1], 10);
    const qCard = page.locator('[id^="qcard_"]').filter({ hasText: Q_MARKER });
    await fillEssayAnswer(page, qCard, 'Jawaban esai E2E untuk regression GRADE-01.');
    await submitExamTwoStep(page);
  });
  test('K5 — HC grades + finalizes', async ({ page }) => {
    await login(page, 'hc');
    await gradeSingleEssaySession(page, { title, category, scheduleDate, sessionId, score: 80 });
  });
  test('K6 — DB assert Score aggregated (GRADE-01: NOT 0)', async () => {
    const score = await db.queryScalar(`SELECT ISNULL(Score, -1) FROM AssessmentSessions WHERE Id = ${sessionId}`);
    expect(score).toBe(80);   // bukan 0 → fix 376 terbukti e2e
    const completed = await db.queryScalar(`SELECT COUNT(*) FROM AssessmentSessions WHERE Id = ${sessionId} AND Status = 'Completed'`);
    expect(completed).toBe(1);
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrasi Flow I (edit assessment), hapus fixme I</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:1302-1416 (Flow I full: I1 create flat-form, I2 Edit form #Title/#DurationMinutes/#PassPercentage, I3 edit, I4 verify persist, I5 cleanup — I2-I5 SURVIVE karena EditAssessment.cshtml masih flat)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md Per-Flow Map row I
  </read_first>
  <action>
    (1a) Hapus `test.fixme` Flow I (`:1304`).

    (1b) Migrasi I1: ganti flat-form create dengan helper:
    ```typescript
    title = uniqueTitle('Pre Test Edit Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 30, passPercentage: 60, allowAnswerReview: false,
      participantEmails: ['rino.prasetyo@pertamina.com'],
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    ```
    Flow I tidak butuh question (cek I asli; bila perlu, tambah createDefaultPackage+addQuestionViaForm).

    (1c) I2 (Edit form `#Title`/`#DurationMinutes`/`#PassPercentage` — EditAssessment.cshtml masih flat), I3 edit, I4 verify persist, I5 cleanup = SURVIVE. Verifikasi cepat selector edit page masih cocok; bila drift, sesuaikan test-side.

    (1d) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow I"` → passed.
  </action>
  <acceptance_criteria>
    - Flow I describe TIDAK lagi `test.fixme` (grep di range = 0).
    - I1 pakai `createAssessmentViaWizard`.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow I"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow I"</automated>
  </verify>
  <done>Flow I create via wizard, edit-form SURVIVE, fixme dihapus, hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Migrasi Flow J (abandon & reset recovery), hapus fixme J</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:1417-end (Flow J full: J1 create+QADD, J2 start, J3 abandon #abandonForm, J4 cannot-restart, J5 monitoring Abandoned, J6 reset, J7 retake, J8 cleanup — J2-J8 SURVIVE)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md Per-Flow Map row J
  </read_first>
  <action>
    (2a) Hapus `test.fixme` Flow J (`:1419`).

    (2b) Migrasi J1: ganti flat-form create dengan helper + createDefaultPackage + addQuestionViaForm ×1 (1 MC):
    ```typescript
    title = uniqueTitle('Pre Test Abandon Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 30, passPercentage: 60, allowAnswerReview: false,
      participantEmails: ['rino.prasetyo@pertamina.com'],
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    // packageId = await createDefaultPackage(page); addQuestionViaForm(...1 MC...)
    ```

    (2c) J2 start, J3 abandon (#abandonForm/Keluar btn), J4 cannot-restart, J5 monitoring Abandoned, J6 reset, J7 retake, J8 cleanup = SURVIVE.

    (2d) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow J"` → passed.
  </action>
  <acceptance_criteria>
    - Flow J describe TIDAK lagi `test.fixme` (grep di range = 0).
    - J1 pakai `createAssessmentViaWizard` + `createDefaultPackage`.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow J"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow J"</automated>
  </verify>
  <done>Flow J create via wizard+package, abandon/reset lifecycle SURVIVE, fixme dihapus, hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Tambah Flow K BARU (essay full cycle + DB-assert Score teragregasi, D-01/GRADE-01)</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-types.spec.ts:305-428 (FLOW L — blueprint port VERBATIM; un-fixme'd by Phase 376)
    - tests/e2e/helpers/examTypes.ts:320-362 (fillEssayAnswer — direct SignalR invoke) + :386-... (gradeSingleEssaySession) + :255-... (submitExamTwoStep)
    - tests/helpers/dbSnapshot.ts:116-128 (queryScalar localhost-only guard)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"Flow K (BARU, D-01)" + Shared Patterns "DB Assert"
    - .planning/phases/376-fix-essay-only-score-aggregation/376-01-SUMMARY.md + 376-03-SUMMARY.md (fix AssessmentScoreAggregator + RecomputeEssayScores — apa yang Flow K validasi e2e)
  </read_first>
  <action>
    Append blok `test.describe('Flow K: Essay Full Cycle + Score Aggregation (GRADE-01)', ...)` ke akhir `exam-taking.spec.ts` (default D-01 diskresi: satu suite, append). Port pola FLOW L verbatim (lihat <interfaces> Flow K blueprint) dengan penyesuaian marker `[379-K]` + title `uniqueTitle('Pre Test [379-K] Essay')`.

    Struktur 6 sub-test (K1-K6):
    - K1: createAssessmentViaWizard essay-context (category 'IHT', durationMinutes 60, passPercentage 70, allowAnswerReview true) → extract assessmentId.
    - K2: goto ManagePackages → createDefaultPackage → packageId.
    - K3: addQuestionViaForm({ type:'Essay', text:`${Q_MARKER} — ...`, rubrik:'...', maxCharacters:2000, score:100 }).
    - K4: worker (coachee) start → waitForFunction assessmentHub Connected → capture sessionId dari URL → `fillEssayAnswer(page, qCard, '...')` (qCard filter hasText Q_MARKER) → `submitExamTwoStep(page)`.
    - K5: HC `gradeSingleEssaySession(page, { title, category, scheduleDate, sessionId, score: 80 })` (grade + finalize essay-only).
    - K6: **DB-assert (WAJIB, D-01)** — `expect(await db.queryScalar('SELECT ISNULL(Score,-1) FROM AssessmentSessions WHERE Id='+sessionId)).toBe(80)` (BUKAN 0 → bukti fix 376) + `expect(await db.queryScalar("...AND Status='Completed'")).toBe(1)`.

    JANGAN assert via UI badge "Sudah Dinilai" saja (tidak buktikan agregasi numerik — D-01). DB-scalar WAJIB.

    Tambah cleanup K7 (opsional, pola flow lain): hapus assessment via "Hapus Grup" (atau andalkan global.teardown restore). Pastikan `db` sudah di-import (Plan 02 Task 1 menambahkan).

    Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow K"` → passed (K1-K6).

    Bila K6 GAGAL (Score=0) → ini berarti fix 376 tidak ter-trigger e2e: SURFACE temuan ke SUMMARY + STATE sebagai regression GRADE-01 (JANGAN fix kode produksi di fase test). Tapi karena 376 SHIPPED + FLOW L hijau, K6 diharapkan PASS.
  </action>
  <acceptance_criteria>
    - `grep -c "Flow K: Essay" tests/e2e/exam-taking.spec.ts` >= 1.
    - `grep -c "queryScalar" tests/e2e/exam-taking.spec.ts` >= 1 dengan referensi `Score` (K6).
    - `grep -c "gradeSingleEssaySession" tests/e2e/exam-taking.spec.ts` >= 1.
    - K6 assert `.toBe(80)` (BUKAN UI badge) untuk Score.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow K"` → "passed" (K1-K6, termasuk K6 Score===80).
    - Setelah plan ini: `grep -c "test.fixme" tests/e2e/exam-taking.spec.ts` = 0 (semua 10 A-J dihapus + K tidak ada fixme).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow K"</automated>
  </verify>
  <done>Flow K BARU hijau: essay full cycle + DB-assert Score===80 (sinergi GRADE-01/376); ZERO test.fixme tersisa di file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app localhost | wizard create essay + worker SignalR essay answer + HC grade/finalize via UI |
| test → SQL localhost | db.queryScalar AssessmentSessions.Score (K6 assert) — localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-379-13 | Tampering | test data I/J/K nempel pasca-run | mitigate | uniqueTitle isolasi + cleanup + global.teardown db.restore() |
| T-379-14 | Tampering | db.queryScalar (K6) menyentuh non-localhost | mitigate | dbSnapshot localhost-only guard ("Refusing to target non-localhost SQL Server") |
| T-379-15 | Scope | Flow K ungkap regression GRADE-01 → tergoda fix prod | accept | bila K6 gagal → surface backlog/STATE, JANGAN fix kode produksi (CONTEXT D); 376 SHIPPED → diharapkan pass |

**Honest note:** Flow K = validasi e2e fix 376 existing. NOL perubahan kode produksi.
</threat_model>

<verification>
- Flow I + J + K hijau `--workers=1` (run terpisah).
- 2 fixme (I/J) dihapus → total 10 A-J terhapus.
- ZERO `test.fixme` tersisa di file (count = 0).
- Flow K K6 assert Score===80 via DB (bukan UI badge).
</verification>

<success_criteria>
- Flow I, J migrasi ke wizard (SC1 lengkap — 10 flow A-J selesai).
- Flow K BARU hijau, DB-assert Score teragregasi (SC3 + E2E-01 essay coverage).
- Tidak ada fixme tersisa.
</success_criteria>

<output>
After completion, create `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-05-SUMMARY.md`
</output>
