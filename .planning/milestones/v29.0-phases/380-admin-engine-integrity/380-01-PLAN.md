---
phase: 380-admin-engine-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/ShuffleEngine.cs
  - Controllers/CMPController.cs
  - HcPortal.Tests/ShuffleEngineTests.cs
  - tests/e2e/exam-taking.spec.ts
autonomous: true
requirements: [WSE-01]
tags: [assessment, shuffle, engine, exam-entry]

must_haves:
  truths:
    - "Worker dengan ≥2 paket sibling di mana satu paket kosong + shuffle ON tetap menerima set soal non-kosong (dari paket berisi)"
    - "Engine fix otomatis berlaku untuk 3 caller (StartExam, ReshufflePackage, ReshuffleAll) — tidak ada perubahan di call-site"
    - "Worker yang membuka ujian dengan SEMUA paket sibling kosong DIBLOKIR dengan pesan ramah dan TIDAK ada write StartedAt/Status/UserPackageAssignment/SignalR/auto-grade"
    - "Tidak ada migration / perubahan model — dotnet ef migrations add tidak menghasilkan snapshot diff"
  artifacts:
    - path: "Helpers/ShuffleEngine.cs"
      provides: "ON-path BuildCrossPackageAssignment empty-package filter (mirror OFF-path :53-57)"
      contains: ".Where(p => p.Questions != null && p.Questions.Count > 0)"
    - path: "Controllers/CMPController.cs"
      provides: "StartExam D-05 all-empty guard (block before any write)"
    - path: "HcPortal.Tests/ShuffleEngineTests.cs"
      provides: "2 ON-path unit facts (2-pkg-one-empty → non-empty; all-empty → empty)"
      contains: "On_MultiPackage_OneEmpty_ReturnsFilledPackageQuestions"
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "E2E #6 empty-package + shuffle ON scenario"
  key_links:
    - from: "Controllers/CMPController.cs StartExam"
      to: "Helpers/ShuffleEngine.BuildQuestionAssignment"
      via: "engine call at ~:1019"
      pattern: "BuildQuestionAssignment"
    - from: "Helpers/ShuffleEngine.cs BuildCrossPackageAssignment (filtered list)"
      to: "single-package early-return + K=Min compute"
      via: "filter at TOP of method, before Count==1 check"
      pattern: "packagesWithQuestions"
---

<objective>
Fix WSE-01 (SHF-01): worker harus tetap menerima soal saat satu paket sibling kosong + shuffle ON (default), dan diblokir dengan pesan ramah saat SEMUA paket kosong.

Purpose: Tanpa fix ini, `ShuffleEngine` ON-path menghitung `K = packages.Min(p => p.Questions.Count)` = 0 saat ada paket kosong → semua worker dapat 0 soal → submit → maxScore=0 → 0% Fail seluruh batch (silent data corruption). OFF-path sudah punya guard ini (drift v27.0); fix = samakan ON ke OFF.

Output:
- `Helpers/ShuffleEngine.cs` — empty-package filter di ON-path `BuildCrossPackageAssignment` (D-04).
- `Controllers/CMPController.cs` — guard all-empty di `StartExam` SEBELUM write apa pun (D-05).
- 2 unit fact baru di `ShuffleEngineTests.cs` + e2e scenario #6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/380-admin-engine-integrity/380-CONTEXT.md
@.planning/phases/380-admin-engine-integrity/380-RESEARCH.md
@.planning/phases/380-admin-engine-integrity/380-PATTERNS.md
@.planning/phases/380-admin-engine-integrity/380-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from current source this session (anchors VERIFIED, zero drift). -->

ENGINE ENTRY (Helpers/ShuffleEngine.cs):
  public static List<int> BuildQuestionAssignment(
      List<AssessmentPackage> packages, bool shuffleQuestions, int workerIndex, Random rng)
  // ON-path delegates to: private static List<int> BuildCrossPackageAssignment(List<AssessmentPackage> packages, Random rng)

ON-path BUG site (BuildCrossPackageAssignment, VERIFIED current lines):
  :91  private static List<int> BuildCrossPackageAssignment(List<AssessmentPackage> packages, Random rng)
  :93      if (packages.Count == 0)              // ← insert filter RIGHT AFTER this guard
  :97      if (packages.Count == 1)             // ← Pitfall 2: this must run on the FILTERED list
  :108     int K = packages.Min(p => p.Questions.Count);   // ← BUG: empty pkg ⇒ K=0

OFF-path canonical template to MIRROR (ShuffleEngine.cs:52-57, VERIFIED current source — copy verbatim):
  // OFF + ≥2 paket (SHUF-06)
  var packagesWithQuestions = packages
      .Where(p => p.Questions != null && p.Questions.Count > 0)   // guard SEBELUM modulo
      .OrderBy(p => p.PackageNumber)
      .ToList();
  if (packagesWithQuestions.Count == 0) return new List<int>();

Pkg(...) test builder (ShuffleEngineTests.cs:18-30, reuse as-is — empty pkg = Pkg(n) with no question args):
  private static AssessmentPackage Pkg(int packageNumber, params (int id, int order, string? et)[] qs)

CMPController.StartExam consume site (VERIFIED current lines):
  :960-967  justStarted block — WRITES Status="InProgress" + StartedAt + SaveChanges  ◄── D-05 GOTCHA: write happens BEFORE engine call
  :970-978  SignalR workerStarted broadcast + LogActivityAsync (also suppress on all-empty)
  :995-1000 sibling package load (.Include(p => p.Questions))
  :1019-1020 var shuffledIds = ShuffleEngine.BuildQuestionAssignment(packages, assessment.ShuffleQuestions, workerIndex, rng);
  :1198-1203 existing no-package else guard (zero-packages case) — TEMPLATE for the friendly redirect contract:
      TempData["Error"] = "Sesi ujian ini tidak memiliki paket soal. Hubungi Admin atau HC.";
      return RedirectToAction("Assessment");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Add ON-path empty-package unit facts to ShuffleEngineTests.cs (RED)</name>
  <files>HcPortal.Tests/ShuffleEngineTests.cs</files>
  <read_first>
    - HcPortal.Tests/ShuffleEngineTests.cs (existing facts + Pkg(...) builder :18-30; mirror Off_AllPackagesEmpty_* + On_MultiPackage_SeedStable_* style)
    - 380-PATTERNS.md §7 (exact fact bodies to copy verbatim)
  </read_first>
  <behavior>
    - Test 1 (On_MultiPackage_OneEmpty_ReturnsFilledPackageQuestions): 2 packages, p1 has 3 questions (ids 10,11,12), p2 EMPTY, shuffle ON → result is non-empty AND result.ToHashSet() == {10,11,12} (worker gets the full filled package's questions). Currently RED: returns [] because K=Min=0.
    - Test 2 (On_AllPackagesEmpty_ReturnsEmpty): 2 empty packages, shuffle ON → result is empty (engine half of D-05; controller decides how to react). Currently RED.
  </behavior>
  <action>
Add exactly these two [Fact] methods to `HcPortal.Tests/ShuffleEngineTests.cs` (inside the existing `ShuffleEngineTests` class, reusing the existing `Pkg(...)` builder — do NOT add a new builder):

```csharp
[Fact] // WSE-01: ON-path, 2 packages one empty → worker gets the filled package's questions (NOT empty)
public void On_MultiPackage_OneEmpty_ReturnsFilledPackageQuestions()
{
    var p1 = Pkg(1, (10, 1, null), (11, 2, null), (12, 3, null)); // 3 questions
    var p2 = Pkg(2);                                              // EMPTY
    var packages = new List<AssessmentPackage> { p1, p2 };
    var result = ShuffleEngine.BuildQuestionAssignment(packages, shuffleQuestions: true, workerIndex: 0, rng: new Random(42));
    Assert.NotEmpty(result);                                       // BUG: currently returns [] (K=Min=0)
    Assert.Equal(new HashSet<int> { 10, 11, 12 }, result.ToHashSet());
}

[Fact] // D-05 engine half: ON-path, all packages empty → engine returns empty (controller blocks)
public void On_AllPackagesEmpty_ReturnsEmpty()
{
    var packages = new List<AssessmentPackage> { Pkg(1), Pkg(2) };
    var result = ShuffleEngine.BuildQuestionAssignment(packages, shuffleQuestions: true, workerIndex: 0, rng: new Random(42));
    Assert.Empty(result);
}
```

Verify the file already imports `System`, `System.Collections.Generic`, `System.Linq`, `HcPortal.Helpers`, `HcPortal.Models`, `Xunit` (it does — :1-6). Do NOT modify any existing fact. Run the filtered test — `On_MultiPackage_OneEmpty_*` MUST FAIL (RED) before Task 2, proving the bug; `On_AllPackagesEmpty_*` should already pass (all-empty already returns [] via K=0).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~On_MultiPackage_OneEmpty" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `ShuffleEngineTests.cs` contains `On_MultiPackage_OneEmpty_ReturnsFilledPackageQuestions`
    - `ShuffleEngineTests.cs` contains `On_AllPackagesEmpty_ReturnsEmpty`
    - Before Task 2: `dotnet test HcPortal.Tests --filter "On_MultiPackage_OneEmpty"` FAILS (RED — proves bug exists)
  </acceptance_criteria>
  <done>Both facts compiled into ShuffleEngineTests.cs; On_MultiPackage_OneEmpty is RED (bug confirmed); On_AllPackagesEmpty is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SHF-01 engine fix — mirror OFF-path empty filter into ON-path BuildCrossPackageAssignment (GREEN)</name>
  <files>Helpers/ShuffleEngine.cs, HcPortal.Tests/ShuffleEngineTests.cs</files>
  <read_first>
    - Helpers/ShuffleEngine.cs (read full BuildCrossPackageAssignment :91-end + OFF-path :52-59 template)
    - 380-PATTERNS.md §1 (fix placement — CRITICAL Pitfall 2)
    - 380-RESEARCH.md "Pattern 1" + "Pitfall 2"
  </read_first>
  <action>
In `Helpers/ShuffleEngine.cs`, inside `private static List<int> BuildCrossPackageAssignment(List<AssessmentPackage> packages, Random rng)`, insert the OFF-path filter at the **TOP of the method — RIGHT AFTER the `packages.Count == 0` guard at :93-94, and BEFORE the `packages.Count == 1` early-return at :97** (Pitfall 2 — placement is load-bearing).

Insert exactly (mirror OFF-path :52-57 verbatim):

```csharp
// WSE-01 (SHF-01 / D-04): filter empty packages BEFORE Count==1 / K=Min — mirror OFF-path :53-57.
// So "2 paket, satu kosong" collapses into the single-package shuffle (worker dapat paket berisi).
packages = packages
    .Where(p => p.Questions != null && p.Questions.Count > 0)
    .OrderBy(p => p.PackageNumber)
    .ToList();
if (packages.Count == 0) return new List<int>();
```

CRITICAL placement detail (Pitfall 2): after this filter, the EXISTING `if (packages.Count == 1)` early-return at :97 now operates on the FILTERED list — so "2 paket [3 soal, 0 soal]" → filtered to 1 package → hits single-package shuffle → returns all 3 questions shuffled. The existing `int K = packages.Min(...)` at :108 now only sees non-empty packages (K ≥ 1, no DivideByZero). Do NOT change the method signature; reassign the local `packages` variable (it is a parameter; reassignment is fine and keeps all downstream references — `packages.Count==1`, `packages.Min`, `SelectMany` — pointing at the filtered list). Do NOT touch the OFF-path (:53-57) or `BuildQuestionAssignment` entry. Do NOT remove the existing `if (K == 0) return new List<int>();` at :109-110 (harmless redundant guard, keep it).

After the fix, both Task 1 facts MUST pass.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~ShuffleEngine" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `ShuffleEngine.cs` `BuildCrossPackageAssignment` contains `.Where(p => p.Questions != null && p.Questions.Count > 0)` placed BEFORE the `packages.Count == 1` check
    - `dotnet test HcPortal.Tests --filter ShuffleEngine` exits 0 (all ShuffleEngine facts green, including the 2 new ON-path facts)
    - `dotnet build` exits 0 (0 errors)
    - No model/snapshot change: `git diff --name-only` shows NO `Migrations/` file and NO `*ModelSnapshot.cs`
  </acceptance_criteria>
  <done>ON-path filters empty packages before K-min; 2-pkg-one-empty returns the filled package's 3 questions; all-empty returns []; full ShuffleEngine filter suite green; build 0 errors; no migration scaffolded.</done>
</task>

<task type="auto">
  <name>Task 3: D-05 all-empty guard in CMPController.StartExam (block BEFORE any write) + e2e #6</name>
  <files>Controllers/CMPController.cs, tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - Controllers/CMPController.cs :888-1213 (StartExam — focus :955-1045 justStarted/SignalR/package-load/engine-call + the no-package else :1198-1203)
    - 380-PATTERNS.md §3 (D-05 placement — CRITICAL Pitfall 1: write at :960-967 happens BEFORE engine call)
    - 380-RESEARCH.md "Pitfall 1" (approach A recommended) + "Pattern 3 friendly-message contract"
    - tests/e2e/exam-taking.spec.ts (existing Flow B structure) + tests/e2e/helpers/examTypes.ts (createDefaultPackage, addQuestionViaForm, submitExamTwoStep) + tests/helpers/dbSnapshot.ts
  </read_first>
  <action>
**Part A — controller guard (D-05, approach A: hoist emptiness check BEFORE the justStarted write).**

In `Controllers/CMPController.cs` `StartExam`, the `justStarted` block at :960-967 writes `Status="InProgress"` + `StartedAt` + `SaveChangesAsync()` BEFORE the package load (:995) and engine call (:1019). To honor D-05 (NO StartedAt/Status/assignment/SignalR/auto-grade write when all packages empty), hoist a cheap "any non-empty sibling package?" check to run BEFORE the `:960` justStarted write.

Implementation:
1. Locate where sibling packages are available. The package load at :995-1000 (`.Include(p => p.Questions)`) currently runs AFTER the justStarted write. Move/duplicate just enough of that load (or a `.AnyAsync(...)` count query over the same sibling set) to run BEFORE the justStarted write at :960.
2. Compute: does at least one sibling package have ≥1 question? Use the SAME sibling-resolution predicate the existing :995 load uses (same Title/Category/Schedule/LinkedGroup filter — read it first and reuse it exactly; do NOT invent a new query shape).
3. Guard: if the session HAS packages but ALL of them are empty (0 questions across all), block BEFORE any write — mirror the friendly-redirect contract from the existing :1198-1203 else guard:

```csharp
// WSE-01 (D-05): paket ada tapi semua kosong → blokir SEBELUM tulis StartedAt/Status.
// Cegah 0% Fail palsu. (Kasus "zero paket" sudah ditangani else :1198.)
TempData["Error"] = "Ujian belum siap — belum ada soal pada paket. Silakan hubungi Admin atau HC.";
return RedirectToAction("Assessment");
```

Place this guard so it returns BEFORE: the justStarted write (:960-967), the SignalR `workerStarted` broadcast (:970-978), `LogActivityAsync` (:977), the engine call (:1019), and any `UserPackageAssignment` creation (:1012-1042). Do NOT touch the existing zero-packages `else` at :1198-1203 (distinct case, keep as-is). The common case (1 empty among several) must still proceed normally — only ALL-empty blocks.

**Part B — e2e #6 (empty package + shuffle ON).**

Add a scenario to `tests/e2e/exam-taking.spec.ts` (mirror existing Flow B + reuse helpers from `tests/e2e/helpers/examTypes.ts`):
- HC creates an assessment (shuffle ON is default — do NOT disable). Call `createDefaultPackage` TWICE to get 2 packages; call `addQuestionViaForm` to add ≥3 questions to ONLY ONE package, leave the other EMPTY.
- Login as worker (`login(page, 'coachee')`) → StartExam → assert the rendered question count > 0 (NOT an empty exam).
- Complete + submit via `submitExamTwoStep` → assert Score > 0 (NOT 0% Fail).
- DB-assert via `tests/helpers/dbSnapshot.ts` if convenient (Score > 0 / Status Completed).
Tag/title the test so it is selectable with `-g "empty"` (e.g. test title contains "empty package"). Follow Pitfall 6 run constraints: this spec runs with `--workers=1`; local `dotnet run` needs `Authentication__UseActiveDirectory=false`; local SQL needs SQLBrowser + `lpc:` override. Snapshot+restore DB per CLAUDE.md Seed Workflow if the test seeds data.
  </action>
  <verify>
    <automated>dotnet build --nologo; cd tests && npx playwright test exam-taking --workers=1 -g "empty"</automated>
  </verify>
  <acceptance_criteria>
    - `CMPController.cs` StartExam contains the all-empty guard with `return RedirectToAction("Assessment")` placed BEFORE the `assessment.StartedAt = DateTime.UtcNow` write
    - `dotnet build` exits 0 (0 errors)
    - `tests/e2e/exam-taking.spec.ts` contains a scenario selectable via `-g "empty"` that creates 2 packages (one empty) + shuffle ON and asserts question count > 0 and Score > 0
    - Manual/trace check: after blocking an all-empty exam, DB shows StartedAt=NULL and Status != "InProgress" for that session (no write occurred)
    - No model/snapshot change (`git diff --name-only` shows no `Migrations/` file)
  </acceptance_criteria>
  <done>All-empty StartExam blocks with friendly BI message and zero writes (StartedAt stays null); 1-empty-among-several proceeds and worker gets questions; e2e #6 green; build 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker browser → CMPController.StartExam (GET) | Authenticated worker triggers exam-start; controller mutates session state (StartedAt/Status) and builds question assignment |
| engine (pure) → controller | ShuffleEngine returns the question set the controller persists as the worker's exam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-380-01 | Tampering / Integrity (silent data corruption) | `ShuffleEngine.BuildCrossPackageAssignment` ON-path — empty package ⇒ K=0 ⇒ everyone gets 0 questions ⇒ batch-wide 0% Fail | mitigate | D-04: filter empty packages before K-min (mirror OFF-path :53-57); unit facts assert non-empty result for 2-pkg-one-empty and empty for all-empty |
| T-380-02 | Tampering / Integrity (false fail-state persistence) | `CMPController.StartExam` writes Status=InProgress/StartedAt then renders empty exam → worker auto-graded 0% with no recourse | mitigate | D-05 (approach A): hoist all-empty check BEFORE the justStarted write; block + friendly redirect, no StartedAt/Status/assignment/SignalR/auto-grade write |
| T-380-03 | Denial of Service (no input — empty set) | engine DivideByZero / empty K on all-empty | accept | engine returns `[]` safely (no exception); controller guard (T-380-02) converts to friendly UX. No HIGH severity. |

No HIGH-severity unmitigated threats remain in this plan. (RST-01 privilege-escalation and TOK-01 access-gate threats are modeled in Plan 02.)
</threat_model>

<verification>
- `dotnet build` exits 0 (0 errors).
- `dotnet test HcPortal.Tests --filter ShuffleEngine` exits 0 (all ShuffleEngine facts green incl. 2 new ON-path facts).
- e2e #6 (`-g "empty"`, `--workers=1`) green: 2 packages one empty + shuffle ON → worker gets questions > 0 → Score > 0.
- NO migration: `git diff --name-only` shows no `Migrations/*` and no `*ModelSnapshot.cs` (verify NO stray `dotnet ef migrations add`).
- After all-empty block, DB session row: StartedAt=NULL, Status != "InProgress".
</verification>

<success_criteria>
- WSE-01 satisfied: worker with ≥2 sibling packages where one is empty + shuffle ON receives a non-empty question set (from filled package).
- Engine fix is single-point: ReshufflePackage (:5210) and ReshuffleAll (:5308) heal automatically (no call-site change).
- All-empty exam blocked with friendly BI message, zero state mutation.
- `migration=false` confirmed (no model/snapshot diff).
</success_criteria>

<output>
After completion, create `.planning/phases/380-admin-engine-integrity/380-01-SUMMARY.md`.
</output>
