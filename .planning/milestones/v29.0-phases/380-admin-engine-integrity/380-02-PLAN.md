---
phase: 380-admin-engine-integrity
plan: 02
type: execute
wave: 2
depends_on: [380-01]
files_modified:
  - Controllers/CMPController.cs
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/AddExtraTimeAuthTests.cs
  - HcPortal.Tests/AddExtraTimeCapTests.cs
  - HcPortal.Tests/VerifyTokenTests.cs
  - tests/e2e/exam-taking.spec.ts
autonomous: true
requirements: [WSE-02, WSE-03]
tags: [assessment, token, authorization, extra-time, security]

must_haves:
  truths:
    - "Worker dengan ujian Pre/Post token-required bisa masuk pakai token meski token tersimpan lowercase (admin sempat edit lowercase) — auto-heal tanpa migration"
    - "Token Pre/Post yang baru di-edit admin tersimpan UPPERCASE di 3 titik tulis (kebersihan data)"
    - "Hanya Admin/HC yang boleh memberi extra time (AddExtraTime tergerbang [Authorize(Roles=\"Admin, HC\")]); worker/peserta diblokir"
    - "Total extra time per-sesi dibatasi ≤ DurationMinutes asli; grant yang melampaui ditolak dengan pesan jelas (JSON contract), reject-whole-batch"
    - "Tidak ada migration / perubahan model — dotnet ef migrations add tidak menghasilkan snapshot diff"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "VerifyToken defensive both-sides uppercase compare (D-01a, auto-heal lowercase)"
      contains: "(assessment.AccessToken ?? \"\").Trim().ToUpper()"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "EditAssessment Pre/Post token uppercase-on-write (3 sites, D-01b) + AddExtraTime [Authorize] + cap (D-02/D-03)"
      contains: "[Authorize(Roles = \"Admin, HC\")]"
    - path: "HcPortal.Tests/AddExtraTimeAuthTests.cs"
      provides: "reflection-authz fact asserting AddExtraTime has Roles == \"Admin, HC\""
      contains: "Assert.Equal(\"Admin, HC\""
    - path: "HcPortal.Tests/AddExtraTimeCapTests.cs"
      provides: "cap-reject fact (existing+minutes > DurationMinutes → rejected)"
    - path: "HcPortal.Tests/VerifyTokenTests.cs"
      provides: "defensive-compare fact (stored lowercase matches uppercased input)"
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "E2E #5 token lowercase edit → worker enters scenario"
  key_links:
    - from: "Controllers/CMPController.cs VerifyToken"
      to: "AssessmentSession.AccessToken (stored)"
      via: "both-sides .Trim().ToUpper() compare at :876"
      pattern: "AccessToken \\?\\? \"\"\\)\\.Trim\\(\\)\\.ToUpper"
    - from: "Controllers/AssessmentAdminController.cs AddExtraTime"
      to: "AssessmentSession.DurationMinutes / ExtraTimeMinutes"
      via: "per-session cap pre-check before accumulation"
      pattern: "currentExtra \\+ minutes > session.DurationMinutes"
---

<objective>
Fix WSE-02 (TOK-01) + WSE-03 (RST-01 + RST-04):
- WSE-02: worker terkunci dari Pre/Post token-required karena admin meng-edit token lowercase (tersimpan apa adanya) sementara compare hanya meng-uppercase sisi input. Fix defensif dua-sisi (auto-heal) + uppercase-on-write.
- WSE-03: `AddExtraTime` tidak punya role gate (worker mana pun bisa menambah waktu ujiannya sendiri — privilege escalation HIGH) dan tidak ada batas total (extra time tak terbatas). Fix: `[Authorize(Roles="Admin, HC")]` + cap per-sesi ≤ DurationMinutes.

Purpose: Membuka gerbang masuk worker (token) + menutup lubang otorisasi keamanan (RST-01) tanpa migration / aksi IT.

Output:
- `Controllers/CMPController.cs` — VerifyToken both-sides compare (D-01a).
- `Controllers/AssessmentAdminController.cs` — 3 token write `.ToUpper()` (D-01b) + AddExtraTime `[Authorize]` (D-02) + cap (D-03).
- 3 test baru (reflection-authz, cap, token-compare) + e2e #5.

NOTE: Wave 2 — depends_on 380-01 karena KEDUA plan menyentuh `CMPController.cs` (Plan 01 = StartExam guard, Plan 02 = VerifyToken). WAJIB sekuensial, JANGAN paralel pada controller yang sama.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/380-admin-engine-integrity/380-CONTEXT.md
@.planning/phases/380-admin-engine-integrity/380-RESEARCH.md
@.planning/phases/380-admin-engine-integrity/380-PATTERNS.md
@.planning/phases/380-admin-engine-integrity/380-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from current source this session (anchors VERIFIED, zero drift). -->

VERIFYTOKEN BUG site (CMPController.cs:876, single-side compare — VERIFIED current):
  if (string.IsNullOrEmpty(token) || assessment.AccessToken != token.ToUpper())
  {
      return Json(new { success = false, message = "Token tidak valid. Silakan periksa dan coba lagi." });
  }
  // :876 is the ONLY token comparison in the entire codebase (VERIFIED grep). Uppercasing the stored
  // side never breaks an already-uppercase token (CreateAssessment + GenerateSecureToken emit uppercase;
  // client force-uppercases at Views/CMP/Assessment.cshtml:757 — do NOT remove that).

EDITASSESSMENT Pre/Post token write BUG sites (AssessmentAdminController.cs — VERIFIED current lines, all lack .ToUpper()):
  :1812  s.AccessToken = model.IsTokenRequired ? (model.AccessToken ?? s.AccessToken ?? "") : "";   // shared-field loop — has fallback-to-existing (PRESERVE)
  :1916  AccessToken = model.IsTokenRequired ? (model.AccessToken ?? "") : "",   // new Pre session
  :1937  AccessToken = model.IsTokenRequired ? (model.AccessToken ?? "") : "",   // new Post session
  // Pre/Post branch RETURNS at :1957 (RedirectToAction("ManageAssessment")) BEFORE the single-mode
  // uppercase at :2010-2017 — so these 3 sites are the ONLY normalization the Pre/Post path gets.

CREATEASSESSMENT uppercase template to MIRROR (AssessmentAdminController.cs:1104-1112, VERIFIED):
  // Ensure Token is uppercase
  if (model.IsTokenRequired && !string.IsNullOrEmpty(model.AccessToken))
      model.AccessToken = model.AccessToken.ToUpper();
  else
      model.AccessToken = "";

ADDEXTRATIME BUG site (AssessmentAdminController.cs — VERIFIED current lines):
  :6866  [HttpPost]
  :6867  [ValidateAntiForgeryToken]
  :6868  public async Task<IActionResult> AddExtraTime(int assessmentId, int minutes)   // ← NO [Authorize(Roles)] — RST-01
  :6870  if (minutes < 5 || minutes > 120 || minutes % 5 != 0)
  :6871      return Json(new { success = false, message = "Waktu harus antara 5-120 menit, kelipatan 5." });   // JSON reject contract template
  :6887-6892  batch resolve: .Where(s => s.Title==repTitle && s.Category==repCategory && s.Schedule.Date==repDate && s.Status=="InProgress")
  :6899  session.ExtraTimeMinutes = (session.ExtraTimeMinutes ?? 0) + minutes;   // ← RST-04: unbounded accumulation

SIBLING authz template to MIRROR (AssessmentAdminController.cs:3996-4000, ResetAssessment — VERIFIED):
  [HttpPost]
  [Authorize(Roles = "Admin, HC")]      // ← "Admin, HC" WITH A SPACE — copy verbatim (Pitfall 4)
  [ValidateAntiForgeryToken]
  public async Task<IActionResult> ResetAssessment(int id)

MODEL fields (Models/AssessmentSession.cs — both already exist, NO migration):
  :19   public int DurationMinutes { get; set; }   // original duration
  :199  public int? ExtraTimeMinutes { get; set; } // accumulator

REFLECTION-AUTHZ test template (HcPortal.Tests/CDPControllerAuthTests.cs:11-27 — VERIFIED):
  var method = typeof(CDPController).GetMethod(nameof(CDPController.ExportHistoriProton));
  var authz = method!.GetCustomAttributes(typeof(AuthorizeAttribute), false).Cast<AuthorizeAttribute>().FirstOrDefault();
  Assert.NotNull(authz);
  Assert.Equal(UserRoles.RolesCoachAndAbove, authz!.Roles);

INTEGRATION test scaffold template (HcPortal.Tests/ProtonYearGateIntegrationTests.cs:16-28 — VERIFIED):
  [Trait("Category", "Integration")]
  public class ProtonYearGateIntegrationTests : IClassFixture<ProtonCompletionFixture>
  // await using var ctx = new ApplicationDbContext(_fixture.Options);  ← disposable real-SQL test DB (NOT HcPortalDB_Dev)

E2E token wizard support (tests/e2e/helpers/examTypes.ts:129-139 — createAssessmentViaWizard isTokenRequired/accessToken):
  if (opts.isTokenRequired) { ...check tokenRequired... if (opts.accessToken) await page.fill(wizardSelectors.accessToken, opts.accessToken); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Reflection-authz + cap + token-compare tests (RED)</name>
  <files>HcPortal.Tests/AddExtraTimeAuthTests.cs, HcPortal.Tests/AddExtraTimeCapTests.cs, HcPortal.Tests/VerifyTokenTests.cs</files>
  <read_first>
    - HcPortal.Tests/CDPControllerAuthTests.cs (whole file — exact reflection pattern)
    - HcPortal.Tests/ProtonYearGateIntegrationTests.cs :1-31 (IClassFixture + [Trait("Category","Integration")] disposable-DB scaffold)
    - 380-PATTERNS.md §8, §9 (test templates) + 380-RESEARCH.md "Code Examples"
    - Models/AssessmentSession.cs (DurationMinutes :19, ExtraTimeMinutes :199 — for cap test seeding)
  </read_first>
  <behavior>
    - AddExtraTime_RequiresAdminOrHc: reflection on AssessmentAdminController.AddExtraTime → AuthorizeAttribute present AND authz.Roles == "Admin, HC" (exact string WITH space — Pitfall 4). Currently RED (no attribute).
    - AddExtraTime_Cap_RejectsWhenTotalExceedsDuration: integration — seed an InProgress session with DurationMinutes=60 and ExtraTimeMinutes=40, request +30 (40+30=70 > 60) → action returns JSON success=false AND ExtraTimeMinutes UNCHANGED (still 40). Currently RED (no cap → would accumulate to 70).
    - VerifyToken_StoredLowercase_MatchesUppercasedInput: stored AccessToken = "abc23x" (lowercase), input token = "abc23x" (or "ABC23X") → compare succeeds (success=true / not "Token tidak valid"). Currently RED (single-side compare: stored lowercase != uppercased input).
  </behavior>
  <action>
Create the three new test files. Use `"Admin, HC"` (WITH space) as the exact expected authz string everywhere (Pitfall 4 — must match the attribute written in Task 3).

**File 1 — `HcPortal.Tests/AddExtraTimeAuthTests.cs` (pure reflection, mirror CDPControllerAuthTests.cs):**
```csharp
using System.Linq;
using Microsoft.AspNetCore.Authorization;
using HcPortal.Controllers;
using Xunit;

namespace HcPortal.Tests;

public class AddExtraTimeAuthTests
{
    [Fact] // WSE-03 / RST-01: only Admin/HC may grant extra time
    public void AddExtraTime_RequiresAdminOrHc()
    {
        var method = typeof(AssessmentAdminController)
            .GetMethod(nameof(AssessmentAdminController.AddExtraTime));
        Assert.NotNull(method);
        var authz = method!.GetCustomAttributes(typeof(AuthorizeAttribute), false)
            .Cast<AuthorizeAttribute>().FirstOrDefault();
        Assert.NotNull(authz);
        Assert.Equal("Admin, HC", authz!.Roles);   // exact string WITH space (Pitfall 4)
    }
}
```

**File 2 — `HcPortal.Tests/VerifyTokenTests.cs` (defensive-compare).**
Prefer a pure unit test of the compare expression if the compare logic can be exercised without a full HTTP pipeline; otherwise use the integration scaffold (`[Trait("Category","Integration")]` + disposable test DB per ProtonYearGateIntegrationTests.cs) to seed an AssessmentSession with a LOWERCASE `AccessToken` then invoke the compare path. Minimum assertion: stored lowercase token + matching (uppercased) input → compare SUCCEEDS (not "Token tidak valid"). Keep the test deterministic; reuse the existing fixture pattern, do NOT touch `HcPortalDB_Dev`.

**File 3 — `HcPortal.Tests/AddExtraTimeCapTests.cs` (integration, mirror ProtonYearGateIntegrationTests.cs scaffold).**
`[Trait("Category","Integration")]` + IClassFixture disposable test DB. Seed an `AssessmentSession` Status="InProgress", DurationMinutes=60, ExtraTimeMinutes=40. Invoke AddExtraTime (or its cap logic) with minutes=30 → assert returned JSON `success == false` AND the persisted `ExtraTimeMinutes` is STILL 40 (cap rejected the whole grant). Also add a positive case: ExtraTimeMinutes=10, +30 → 40 ≤ 60 → success, persisted 40.

Imports for File 1: `System.Linq`, `Microsoft.AspNetCore.Authorization`, `HcPortal.Controllers`, `Xunit`. Run all three filtered — File 1 + the cap reject + the token-compare MUST be RED before Tasks 2-4 (proving each bug). Build must still compile (RED = failing assertions, not compile errors).
  </action>
  <verify>
    <automated>dotnet build --nologo; dotnet test HcPortal.Tests --filter "FullyQualifiedName~AddExtraTime|FullyQualifiedName~VerifyToken" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/AddExtraTimeAuthTests.cs` exists and contains `Assert.Equal("Admin, HC"`
    - `HcPortal.Tests/AddExtraTimeCapTests.cs` exists with a reject case asserting `success == false` and ExtraTimeMinutes unchanged
    - `HcPortal.Tests/VerifyTokenTests.cs` exists asserting stored-lowercase matches uppercased input
    - `dotnet build` exits 0 (tests compile)
    - Before Tasks 2-4: AddExtraTime_RequiresAdminOrHc, the cap-reject case, and the token-compare case are all RED
  </acceptance_criteria>
  <done>3 test files compiled; reflection-authz, cap-reject, and token-compare facts are RED (bugs confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WSE-02 — VerifyToken defensive compare (D-01a) + EditAssessment uppercase-on-write (D-01b) (GREEN for token-compare)</name>
  <files>Controllers/CMPController.cs, Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/CMPController.cs :850-884 (VerifyToken, compare :876)
    - Controllers/AssessmentAdminController.cs :1780-1958 (Pre/Post branch — token writes :1812/1916/1937, branch return :1957) + :1104-1112 (CreateAssessment uppercase template)
    - 380-PATTERNS.md §2, §6 + 380-RESEARCH.md "Pattern 2", "Pattern 3" (gotcha on :1812 fallback)
  </read_first>
  <action>
**Part A — D-01a defensive compare (CMPController.cs:876).** Replace the single-side compare verbatim:

```csharp
if (string.IsNullOrEmpty(token)
    || (assessment.AccessToken ?? "").Trim().ToUpper() != (token ?? "").Trim().ToUpper())
{
    return Json(new { success = false, message = "Token tidak valid. Silakan periksa dan coba lagi." });
}
```

This is the ONLY token comparison in the codebase (VERIFIED) — auto-heals all stored-lowercase tokens at read-time, zero DB touch. Do NOT remove the client force-uppercase at `Views/CMP/Assessment.cshtml:757` (UX invariant; defensive compare is additive).

**Part B — D-01b uppercase-on-write (AssessmentAdminController.cs Pre/Post branch).** Compute one normalized token near the TOP of the Pre/Post branch (after ~:1786, mirroring CreateAssessment :1104-1108), then assign uppercase at all 3 sites:

```csharp
// D-01b: normalize token uppercase for Pre/Post (mirror CreateAssessment :1104-1108)
string normalizedToken = (model.IsTokenRequired && !string.IsNullOrWhiteSpace(model.AccessToken))
    ? model.AccessToken.ToUpper()
    : "";
```

- Site :1812 (shared-field loop — PRESERVE fallback-to-existing): change to
  `s.AccessToken = model.IsTokenRequired ? (normalizedToken != "" ? normalizedToken : (s.AccessToken ?? "")) : "";`
  GOTCHA: keep "if model didn't supply a token, keep the existing one" semantics — do NOT silently wipe an existing token when `model.AccessToken` is null; but uppercase whatever ends up stored.
- Site :1916 (new Pre): `AccessToken = model.IsTokenRequired ? normalizedToken : "",`
- Site :1937 (new Post): `AccessToken = model.IsTokenRequired ? normalizedToken : "",`

Do NOT touch the single-mode uppercase at :2010-2017 (the Pre/Post branch returns at :1957 before reaching it — unaffected). After this task the token-compare test (VerifyTokenTests) MUST pass.
  </action>
  <verify>
    <automated>dotnet build --nologo; dotnet test HcPortal.Tests --filter "FullyQualifiedName~VerifyToken" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `CMPController.cs:876` contains `(assessment.AccessToken ?? "").Trim().ToUpper()` AND `(token ?? "").Trim().ToUpper()`
    - `AssessmentAdminController.cs` Pre/Post branch defines `normalizedToken` and the 3 write sites (:1812/:1916/:1937) assign the uppercased value; :1812 preserves the fallback-to-existing token
    - `dotnet test HcPortal.Tests --filter VerifyToken` exits 0 (token-compare green)
    - `dotnet build` exits 0
    - Client force-uppercase at Assessment.cshtml:757 still present (NOT removed)
  </acceptance_criteria>
  <done>VerifyToken compares both sides upper+trim (auto-heals lowercase); EditAssessment writes uppercase token at all 3 Pre/Post sites with :1812 fallback preserved; token-compare test green; build 0 errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WSE-03 — AddExtraTime authz attribute (D-02) (GREEN for reflection-authz)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs :3996-4000 (ResetAssessment sibling authz template) + :6866-6868 (AddExtraTime signature)
    - 380-PATTERNS.md §4 + 380-RESEARCH.md "Pitfall 4" (exact "Admin, HC" string)
  </read_first>
  <action>
In `Controllers/AssessmentAdminController.cs`, insert `[Authorize(Roles = "Admin, HC")]` between the two existing attributes on `AddExtraTime` (:6866-6868), mirroring sibling ResetAssessment (:3998) EXACTLY:

```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]      // RST-01 fix — exact sibling string, "Admin, HC" WITH space
[ValidateAntiForgeryToken]
public async Task<IActionResult> AddExtraTime(int assessmentId, int minutes)
```

Use the EXACT string `"Admin, HC"` (with the space) so the reflection-authz test string-equality passes (Pitfall 4 — CONTEXT.md D-02 wrote `"Admin,HC"` without space; defer to the actual sibling convention `"Admin, HC"`). Verify `using Microsoft.AspNetCore.Authorization;` is already imported at the top of the file (it is — other actions use `[Authorize]`). Do NOT change the method body in this task. After this task the reflection-authz test MUST pass.
  </action>
  <verify>
    <automated>dotnet build --nologo; dotnet test HcPortal.Tests --filter "FullyQualifiedName~AddExtraTime_RequiresAdminOrHc" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` AddExtraTime carries `[Authorize(Roles = "Admin, HC")]` (exact string with space)
    - `dotnet test HcPortal.Tests --filter AddExtraTime_RequiresAdminOrHc` exits 0
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>AddExtraTime gated by [Authorize(Roles = "Admin, HC")]; reflection-authz test green; build 0 errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: WSE-03 — AddExtraTime cap (D-03) + e2e #5 (token lowercase) (GREEN)</name>
  <files>Controllers/AssessmentAdminController.cs, tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs :6866-6910 (AddExtraTime — per-call bound :6870-6871, batch resolve :6887-6892, accumulation :6899)
    - 380-PATTERNS.md §5 + 380-RESEARCH.md "Pattern 4", "Pitfall 3" (per-session reject-whole-batch), "Pitfall 5" (JSON contract, not TempData)
    - tests/e2e/exam-taking.spec.ts (Flow B) + tests/e2e/helpers/examTypes.ts (createAssessmentViaWizard token support :129-139) + tests/helpers/dbSnapshot.ts (queryString)
  </read_first>
  <action>
**Part A — D-03 cap (AssessmentAdminController.cs AddExtraTime).** Insert a per-session cap pre-check loop AFTER the batch resolve (`sessions` at :6887-6892, after the `if (!sessions.Any())` guard) and BEFORE the accumulation foreach (:6899). Reject the WHOLE batch (atomic) if ANY session would exceed its own DurationMinutes, using the SAME JSON contract as :6871 (NOT TempData — Pitfall 5):

```csharp
// RST-04 cap (D-03): total extra time per-sesi ≤ durasi asli. Reject-whole-batch (atomic).
foreach (var session in sessions)
{
    var currentExtra = session.ExtraTimeMinutes ?? 0;
    if (currentExtra + minutes > session.DurationMinutes)
        return Json(new { success = false,
            message = $"Total tambahan waktu tidak boleh melebihi durasi ujian ({session.DurationMinutes} menit). Saat ini sudah +{currentExtra} menit." });
}
```

Leave the existing accumulation foreach (:6899) unchanged — it runs only when no session exceeds the cap. Do NOT add a `[Range]` DataAnnotation (the cap is a runtime business rule against DurationMinutes, not a static range). After this task the cap-reject test MUST pass (and the positive case still succeeds).

**Part B — e2e #5 (token lowercase edit → worker enters).** Add a scenario to `tests/e2e/exam-taking.spec.ts` (reuse `createAssessmentViaWizard` token support + `dbSnapshot`):
- HC creates a token-required Pre/Post assessment, then EDITs it (EditAssessment) typing a LOWERCASE token e.g. `'abc23x'`.
- Login worker (`login(page, 'coachee')`) → /CMP/Assessment → token modal → type `'abc23x'` (input auto-uppercases to `'ABC23X'` per Assessment.cshtml:757) → POST /CMP/VerifyToken.
- Assert SUCCESS redirect to StartExam (NOT "Token tidak valid").
- DB-assert via `tests/helpers/dbSnapshot.ts` `queryString`: stored `AccessToken` is UPPERCASE after the edit-fix.
Title the test so it is selectable with `-g "token"`. Run constraints (Pitfall 6): `--workers=1`; local `dotnet run` needs `Authentication__UseActiveDirectory=false`; local SQL needs SQLBrowser + `lpc:` override. Snapshot+restore DB per CLAUDE.md Seed Workflow if the test seeds data.
  </action>
  <verify>
    <automated>dotnet build --nologo; dotnet test HcPortal.Tests --filter "FullyQualifiedName~AddExtraTime" --nologo; cd tests && npx playwright test exam-taking --workers=1 -g "token"</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` AddExtraTime contains `currentExtra + minutes > session.DurationMinutes` cap check returning `Json(new { success = false, ...})` placed BEFORE the accumulation foreach
    - `dotnet test HcPortal.Tests --filter AddExtraTime` exits 0 (authz + cap reject + cap positive all green)
    - `tests/e2e/exam-taking.spec.ts` contains a scenario selectable via `-g "token"`: admin edits lowercase token → worker enters successfully + DB token stored UPPERCASE
    - `dotnet build` exits 0
    - No model/snapshot change (`git diff --name-only` shows no `Migrations/` file)
  </acceptance_criteria>
  <done>AddExtraTime rejects grants exceeding DurationMinutes (per-session, reject-whole-batch, JSON contract); under-cap grants still succeed; e2e #5 green (lowercase token edit → worker enters, token stored uppercase); build 0 errors; no migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker browser → CMPController.VerifyToken (POST) | Untrusted token input crosses the access gate; correct comparison decides exam entry |
| any-authenticated client → AssessmentAdminController.AddExtraTime (POST) | Admin-write endpoint reachable by any authenticated user (incl. worker) absent a role gate |
| admin browser → AssessmentAdminController.EditAssessment (POST) | Admin writes token data that the worker gate later compares against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-380-04 | Elevation of Privilege (HIGH) | `AddExtraTime` lacks `[Authorize(Roles)]` — base `[Authorize]` on AdminBaseController permits ANY authenticated user; no global FallbackPolicy → a worker can POST to grant themselves extra exam time | mitigate | D-02: add `[Authorize(Roles = "Admin, HC")]` (mirror ResetAssessment :3998); reflection-authz test asserts the attribute + exact roles. `[ValidateAntiForgeryToken]` alone does NOT stop a legit-authenticated worker. |
| T-380-05 | Tampering / business-logic abuse | `AddExtraTime` unbounded accumulation (:6899) — repeated grants extend exam time without limit, breaking timed-exam integrity (V11) | mitigate | D-03: server-side per-session ceiling `currentExtra + minutes ≤ DurationMinutes`, reject-whole-batch with clear BI message (JSON contract); cap integration test asserts reject + unchanged persisted value |
| T-380-06 | Integrity / availability of access gate | `VerifyToken` single-side compare — stored lowercase token (from admin edit) permanently locks out workers despite correct input (TOK-01) | mitigate | D-01a: both-sides `.Trim().ToUpper()` defensive compare at the single comparison site (:876) — auto-heals legacy lowercase, zero DB touch; D-01b uppercase-on-write prevents new lowercase; token-compare test + e2e #5 assert entry |
| T-380-07 | Spoofing (token guessing) | Worker brute-forces the access token at VerifyToken | accept | Out of scope this phase — no rate-limit change requested; tokens are uppercase access codes, not crypto secrets (V6 n/a). Pre-existing posture unchanged; note for audit backlog if needed. |

T-380-04 (HIGH) is fully mitigated by D-02 + reflection test. No HIGH-severity unmitigated threat remains.
</threat_model>

<verification>
- `dotnet build` exits 0 (0 errors).
- `dotnet test HcPortal.Tests --filter "AddExtraTime|VerifyToken"` exits 0 (reflection-authz + cap reject/positive + token-compare green).
- Full xUnit suite (`dotnet test --nologo`) green (baseline 350s/360s passing — no regression).
- e2e #5 (`-g "token"`, `--workers=1`) green: admin edits lowercase token → worker enters; DB token UPPERCASE.
- NO migration: `git diff --name-only` shows no `Migrations/*` and no `*ModelSnapshot.cs`.
</verification>

<success_criteria>
- WSE-02 satisfied: worker with token-required Pre/Post enters using the token even after admin edited it lowercase (auto-heal); new edits store uppercase.
- WSE-03 satisfied: only Admin/HC may grant extra time (attribute enforced + reflection-tested); total extra time capped at original DurationMinutes per session (reject-whole-batch).
- HIGH-severity RST-01 authorization hole closed.
- `migration=false` confirmed (no model/snapshot diff).
</success_criteria>

<output>
After completion, create `.planning/phases/380-admin-engine-integrity/380-02-SUMMARY.md`.
</output>
