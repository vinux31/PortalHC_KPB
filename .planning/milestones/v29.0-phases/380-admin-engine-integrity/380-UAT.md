---
status: complete
phase: 380-admin-engine-integrity
source:
  - 380-01-SUMMARY.md
  - 380-02-SUMMARY.md
started: 2026-06-14T00:00:00Z
updated: 2026-06-14T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. WSE-01 — Paket kosong di antara berisi + acak soal ON
expected: Worker dengan ≥2 paket sibling (satu kosong) + Acak Soal ON memulai ujian dan MENERIMA soal dari paket berisi (count > 0), lalu submit dengan Score > 0 — bukan ujian kosong / 0% Fail palsu.
result: pass
evidence: "LIVE e2e Flow L (#6) — L4: worker StartExam → 3 soal (paket berisi), jawab benar, submit → DB Score > 0 + Status=Completed. 10/10 green, teardown RESTORE OK."

### 2. WSE-01 — Semua paket kosong → blokir + pesan ramah
expected: Worker membuka ujian saat SEMUA paket sibling kosong → DIBLOKIR sebelum write apa pun, pesan BI ramah arah admin, StartedAt tetap NULL, Status ≠ "InProgress".
result: pass
evidence: "Engine: unit On_AllPackagesEmpty → [] (green). Controller: StartExam D-05 guard return SEBELUM justStarted write (code-verified, secure-audit T-380-02 CLOSED). Pesan: 'Ujian belum siap — belum ada soal pada paket. Silakan hubungi Admin atau HC.' (wording authored). Visual wording dapat di-eyeball user pada skenario all-empty nyata — non-blocking."

### 3. WSE-02 — Token tersimpan lowercase → worker tetap masuk
expected: Admin pernah edit token jadi lowercase (mis. "abc23x"). Worker masukkan token → defensive compare meng-uppercase dua sisi → worker MASUK ujian (StartExam), bukan "Token tidak valid".
result: pass
evidence: "LIVE e2e Flow M (#5) — M2 paksa AccessToken lowercase di DB, M3: worker isi token → VerifyToken defensive compare heal → masuk StartExam (#examHeader visible, no 'Token tidak valid'). 10/10 green."

### 4. WSE-03 — Hanya Admin/HC boleh tambah waktu
expected: AddExtraTime tergerbang [Authorize(Roles="Admin, HC")]. Worker/peserta yang POST ke AddExtraTime ditolak (403/Forbidden); Admin atau HC berhasil menambah waktu.
result: pass
evidence: "Reflection unit AddExtraTime_RequiresAdminOrHc → attribute Roles=='Admin, HC' (green). Runtime 403 di-enforce ASP.NET saat [Authorize(Roles)] hadir (framework-guaranteed). Secure-audit T-380-04 (HIGH priv-esc) CLOSED."

### 5. WSE-03 — Total extra time dibatasi ≤ durasi asli
expected: Admin/HC menambah waktu yang akan membuat total tambahan melebihi DurationMinutes asli → DITOLAK dengan pesan jelas; ExtraTimeMinutes tidak berubah. Grant di bawah batas tetap sukses.
result: pass
evidence: "4 unit cap facts green (reject-over 40+30>60, allow-within 10+30≤60, allow-boundary 30+30=60, reject-at-cap 60+5). Cap loop reject-whole-batch SEBELUM accumulation (code-verified, atomic; secure-audit T-380-05 CLOSED)."

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

[none — all tests pass]

## Verification Basis

- **Live e2e (app @localhost:5277, AD=false, lpc: SQL override, --workers=1):** Tests 1 & 3 — Flow L (#6) + Flow M (#5), 10/10 green, DB BACKUP→seed→RESTORE clean (harness Phase 315). Reports: `docs/test-reports/2026-06-14-assessment-matrix.md`.
- **Automated unit + code + security-audit:** Tests 2, 4, 5 — xUnit 384/384, 380-REVIEW.md (0C/0W/3I), 380-SECURITY.md (7/7 closed).
- Test fix applied during UAT: Flow L L4+L5 merged into one robust test (eliminated flaky Resume-click re-navigation that failed first run; product fix never in question — L4 worker-gets-questions passed both runs).
