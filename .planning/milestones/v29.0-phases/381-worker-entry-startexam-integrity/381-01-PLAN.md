---
phase: 381-worker-entry-startexam-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/SiblingSessionQuery.cs
  - Controllers/CMPController.cs
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/SiblingPrePostFilterTests.cs
  - HcPortal.Tests/SiblingDeterminismTests.cs
autonomous: true
requirements: [WSE-04]
migration: false

must_haves:
  truths:
    - "Worker StartExam sesi Pre menerima HANYA soal paket Pre (Post tak tercampur)"
    - "Worker StartExam sesi Post menerima HANYA soal paket Post (Pre tak tercampur)"
    - "Normal exam (AssessmentType='Standard'/''/null) menerima soal yang sama seperti sebelum fix (zero behavior-change)"
    - "Reshuffle (ReshufflePackage/ReshuffleAll) menghasilkan workerIndex identik dengan StartExam untuk sesi type yang sama (determinisme Phase 373 terjaga)"
  artifacts:
    - path: "Helpers/SiblingSessionQuery.cs"
      provides: "Shared type-aware sibling predicate (D-09) dipakai identik di StartExam + reshuffle"
      contains: "SiblingPrePostAwarePredicate"
    - path: "HcPortal.Tests/SiblingPrePostFilterTests.cs"
      provides: "Unit test predikat sibling: Pre≠Post isolation, Standard/''/null satu grup"
      contains: "SiblingPrePostAwarePredicate"
    - path: "HcPortal.Tests/SiblingDeterminismTests.cs"
      provides: "Unit test determinisme workerIndex = OrderBy(x=>x).IndexOf(id) identik dua sisi"
      contains: "IndexOf"
  key_links:
    - from: "Controllers/CMPController.cs StartExam GET (~1012)"
      to: "Helpers/SiblingSessionQuery.SiblingPrePostAwarePredicate"
      via: "EF .Where(predicate) sibling-query menentukan POOL + workerIndex"
      pattern: "SiblingPrePostAwarePredicate"
    - from: "Controllers/AssessmentAdminController.cs ReshufflePackage (~5189) + ReshuffleAll (~5258)"
      to: "Helpers/SiblingSessionQuery.SiblingPrePostAwarePredicate"
      via: "EF .Where(predicate) sibling-query menentukan POOL + workerIndex per session"
      pattern: "SiblingPrePostAwarePredicate"
---

<objective>
WSE-04 — Worker yang mengerjakan Pre/Post yang dijadwalkan same-day menerima HANYA paket ujian itu (Pre & Post tak tercampur), tanpa memecah invariant determinisme round-robin Phase 373.

Akar masalah: query sibling di `StartExam` GET (dan di reshuffle) memfilter `Title + Category + Schedule.Date` TANPA `AssessmentType`. Pre & Post same-day share Title + LinkedGroupId yang sama (beda HANYA `AssessmentType`), sehingga saling memungut paket. Fix: extract satu shared sibling-predicate **type-aware** (D-09 — bukan equality ketat, aman legacy) dan pakai IDENTIK di tiga titik round-robin: StartExam sibling-query, ReshufflePackage, ReshuffleAll (D-02/D-03/D-08). Determinisme `workerIndex = sortedSiblingIds.IndexOf(id)` harus tetap konsisten lintas StartExam ↔ reshuffle.

Purpose: cegah Pre/Post same-day saling tercampur soal; jaga invariant Phase 373 (worker tak pindah paket saat reshuffle vs entry).
Output: helper `SiblingSessionQuery` baru + 2 unit test (RED-first) + 3 call-site di-rewire ke helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md
@.planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md
@.planning/phases/381-worker-entry-startexam-integrity/381-RESEARCH.md

<dependency_note>
**Phase 381 depends on Phase 380.** StartExam meng-CONSUME `ShuffleEngine.BuildQuestionAssignment` (CMPController.cs:1049). SHF-01 (Phase 380, ON-path empty-package filter) harus landing dulu. Plan ini hanya menyentuh sibling-DISKRIMINASI (pool + workerIndex), BUKAN empty-package — tapi tetap eksekusi SETELAH 380 landing (soft same-file overlap CMPController.cs). JANGAN sentuh blok empty-package pre-check (CMPController.cs:970-990, WSE-01/D-05 dari Phase 380).
</dependency_note>

<interfaces>
<!-- Kontrak yang sudah ada di codebase — executor pakai langsung, no exploration. VERIFIED live 2026-06-14. -->

Model `Models/AssessmentSession.cs`:
```csharp
public int Id { get; set; }
public string Title { get; set; }
public string Category { get; set; }
public DateTime Schedule { get; set; }
public string? AssessmentType { get; set; }   // "PreTest"/"PostTest"/"Standard"/""/null
public int? LinkedGroupId { get; set; }        // Pre & Post share SAMA → JANGAN dipakai pemisah (D-01)
```

Precedent POLA helper (JANGAN reuse — predicate ini EXCLUDE Pre/Post, intent beda) — `Controllers/AssessmentAdminController.cs:2335`:
```csharp
public static System.Linq.Expressions.Expression<Func<AssessmentSession, bool>> StandardGroupSiblingPredicate(
    string title, string category, DateTime scheduleDate)
    => a => a.Title == title && a.Category == category && a.Schedule.Date == scheduleDate
            && a.LinkedGroupId == null && a.AssessmentType != "PreTest"
            && a.AssessmentType != "PostTest" && !a.IsManualEntry;
```

Analog test POLA `HcPortal.Tests/SiblingFilterTests.cs` — factory `S(...)` + `Predicate(...).Compile()` + `[Fact]` assert in-memory (no DB, no Moq).

StartExam sibling-query VERIFIED `CMPController.cs:1012-1023`:
```csharp
var siblingSessionIds = await _context.AssessmentSessions
    .Where(s => s.Title == assessment.Title && s.Category == assessment.Category &&
                s.Schedule.Date == assessment.Schedule.Date)   // ← TAMBAH diskriminator D-09
    .Select(s => s.Id).ToListAsync();
var sortedSiblingIds = siblingSessionIds.OrderBy(x => x).ToList();   // 1022
int workerIndex = sortedSiblingIds.IndexOf(id);                      // 1023 → feed ShuffleEngine OFF≥2
```

ReshufflePackage sibling-query VERIFIED `AssessmentAdminController.cs:5189-5217` (by `assessment.*`; sortedSibling+IndexOf 5216-5217).
ReshuffleAll VERIFIED `AssessmentAdminController.cs:5256-5316` (by-param `title,category,scheduleDate`; loads `sessions` ALL types, loop computes `workerIndex = sortedSiblingIds.IndexOf(session.Id)` per session at 5315).

ShuffleEngine OFF≥2 (`Helpers/ShuffleEngine.cs:58`): `packagesWithQuestions[workerIndex % count]` — INILAH alasan order+set harus identik dua sisi.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat shared type-aware sibling helper + unit test (RED-first)</name>
  <read_first>
    - .planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md (§"Helper sibling baru" Opsi 2 Expression form; §"SiblingPrePostFilterTests")
    - .planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md (D-09 exact filter expression; D-01 LinkedGroupId NOT used)
    - HcPortal.Tests/SiblingFilterTests.cs (analog EXACT: factory S + Compile + [Fact])
    - Controllers/AssessmentAdminController.cs:2335-2343 (StandardGroupSiblingPredicate — POLA, beda intent)
    - Models/AssessmentSession.cs (AssessmentType, LinkedGroupId fields)
  </read_first>
  <behavior>
    - Test 1 (Pre isolation): predikat dgn assessmentType="PreTest" → match session PreTest, TIDAK match PostTest, TIDAK match Standard/''/null.
    - Test 2 (Post isolation): predikat dgn assessmentType="PostTest" → match PostTest, TIDAK match PreTest, TIDAK match Standard.
    - Test 3 (non-PrePost satu grup / D-09 legacy-safe): predikat dgn assessmentType="Standard" → match "Standard" DAN "" DAN null (semua non-PrePost satu grup), TIDAK match PreTest/PostTest.
    - Test 4 (null caller / D-09): predikat dgn assessmentType=null → match Standard/''/null (non-PrePost grup), TIDAK match Pre/Post.
    - Test 5 (key mismatch): beda Title atau Category atau Schedule.Date → bukan sibling (false) terlepas type.
  </behavior>
  <action>
Buat file baru `Helpers/SiblingSessionQuery.cs` berisi static class `SiblingSessionQuery` dengan satu pure Expression predicate (mengikuti POLA `StandardGroupSiblingPredicate` Phase 367, tapi INCLUDE AssessmentType-match type-aware D-09 — BUKAN reuse). Signature:

```csharp
using System;
using System.Linq.Expressions;
using HcPortal.Models;

namespace HcPortal.Helpers;

public static class SiblingSessionQuery
{
    // WSE-04 (D-01/D-09): type-aware sibling isolation. Hanya PreTest/PostTest yang diisolasi;
    // Standard/""/null dikelompokkan bersama sebagai non-PrePost (zero behavior-change + aman legacy).
    // LinkedGroupId TIDAK dipakai (Pre & Post share nilai sama). Dipakai IDENTIK di StartExam +
    // ReshufflePackage + ReshuffleAll untuk jaga determinisme workerIndex (Phase 373 invariant).
    public static Expression<Func<AssessmentSession, bool>> SiblingPrePostAwarePredicate(
        string title, string category, DateTime scheduleDate, string? assessmentType)
    {
        bool isPrePost = assessmentType == "PreTest" || assessmentType == "PostTest";
        return s => s.Title == title
                    && s.Category == category
                    && s.Schedule.Date == scheduleDate.Date
                    && ( isPrePost
                         ? s.AssessmentType == assessmentType
                         : (s.AssessmentType != "PreTest" && s.AssessmentType != "PostTest") );
    }
}
```

CATATAN type-aware D-09 (jangan diubah jadi equality ketat): ketika `isPrePost==false`, klausa `(s.AssessmentType != "PreTest" && s.AssessmentType != "PostTest")` memastikan Standard/''/null tetap satu grup. Di EF/LINQ-to-SQL, `!=` terhadap nilai non-null akan match `null` AssessmentType bila EF emit null-aware comparison; untuk in-memory `.Compile()` (unit test), C# `null != "PreTest"` = true → null masuk grup non-PrePost (sesuai D-09). Verifikasi via Test 4.

Lalu buat `HcPortal.Tests/SiblingPrePostFilterTests.cs` mengikuti analog `SiblingFilterTests.cs`:
- Static readonly `Sched` (mis. `new DateTime(2026, 3, 10, 9, 0, 0)`).
- Factory `S(int id, string? type, string title="Welding", string category="OJ")` → `new AssessmentSession { Id=id, UserId="u1", Title=title, Category=category, Schedule=Sched, Status="Open", AccessToken="", AssessmentType=type }`.
- Helper `Pred(string? type)` → `SiblingSessionQuery.SiblingPrePostAwarePredicate("Welding","OJ",Sched.Date,type).Compile()`.
- 5 `[Fact]` sesuai `<behavior>` di atas.

RED-first: tulis test → jalankan → konfirmasi compile error / fail (predikat belum ada). Lalu tambah helper → test GREEN. (Helper + test commit bersama; ini Wave 0-equivalent untuk WSE-04 unit coverage — RESEARCH §Wave 0 Gaps item 1.)
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~SiblingPrePostFilter"</automated>
  </verify>
  <acceptance_criteria>
    - File `Helpers/SiblingSessionQuery.cs` ada dan berisi string `SiblingPrePostAwarePredicate` + `s.AssessmentType != "PreTest"` + `s.AssessmentType != "PostTest"`.
    - File `HcPortal.Tests/SiblingPrePostFilterTests.cs` ada dengan ≥5 `[Fact]` dan memanggil `SiblingSessionQuery.SiblingPrePostAwarePredicate`.
    - `dotnet build` exit 0.
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~SiblingPrePostFilter"` exit 0 (semua [Fact] pass).
    - `Helpers/SiblingSessionQuery.cs` TIDAK mengandung `LinkedGroupId` (D-01 — bukan pemisah).
  </acceptance_criteria>
  <done>Helper type-aware ada; 5 unit test hijau membuktikan Pre↔Pre/Post↔Post isolation + Standard/''/null satu grup (D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Determinism unit test + rewire StartExam sibling-query ke helper</name>
  <read_first>
    - .planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md (§"SiblingDeterminismTests"; §Pattern C sibling-query StartExam 1012-1017; Pitfall 2 OrderBy)
    - Controllers/CMPController.cs:1010-1030 (sibling-query 1012-1017, sortedSiblingIds 1022, workerIndex 1023)
    - HcPortal.Tests/ShuffleReshuffleTests.cs (analog determinisme pure, no DB)
    - Helpers/ShuffleEngine.cs:39-60 (OFF≥2 path workerIndex % count :58)
    - Helpers/SiblingSessionQuery.cs (helper dari Task 1)
  </read_first>
  <behavior>
    - Test 1 (order determinism): untuk sibling-set Id list yang sama (acak input), `OrderBy(x=>x).IndexOf(targetId)` menghasilkan workerIndex IDENTIK terlepas urutan input list (mensimulasikan "SQL tak jamin order" Pitfall 2). Matriks sampling: {1 id, ≥2 id} × posisi target {pertama, tengah, terakhir}.
    - Test 2 (cross-call parity simulasi): dua list dengan ISI sama tapi URUTAN beda → `OrderBy(x=>x)` keduanya identik → `IndexOf(id)` identik (membuktikan StartExam-side == reshuffle-side bila sibling-SET identik).
  </behavior>
  <action>
Buat `HcPortal.Tests/SiblingDeterminismTests.cs` (analog `ShuffleReshuffleTests.cs` — pure, no DB). Test mengunci invariant order/index yang menjadi alasan helper harus dipakai identik dua sisi:
- Test 1: ambil List<int> acak (mis. `{7,3,9,1}`), assert `list.OrderBy(x=>x).ToList().IndexOf(3)` == expected stabil; ulang dengan urutan input shuffle berbeda dari isi yang sama → IndexOf identik.
- Test 2: dua List<int> isi sama urutan beda → `a.OrderBy(x=>x).SequenceEqual(b.OrderBy(x=>x))` true DAN IndexOf(target) sama.

Lalu rewire StartExam sibling-query (`CMPController.cs:1012-1017`) untuk pakai helper:
```csharp
var siblingSessionIds = await _context.AssessmentSessions
    .Where(SiblingSessionQuery.SiblingPrePostAwarePredicate(
        assessment.Title, assessment.Category, assessment.Schedule.Date, assessment.AssessmentType))
    .Select(s => s.Id)
    .ToListAsync();
```
JANGAN ubah baris 1022 (`sortedSiblingIds = siblingSessionIds.OrderBy(x => x).ToList()`) dan 1023 (`workerIndex = sortedSiblingIds.IndexOf(id)`) — biarkan apa adanya (Pitfall 2 sudah benar). Tambah `using HcPortal.Helpers;` jika belum ada.

KRITIS — JANGAN sentuh blok empty-package pre-check `CMPController.cs:970-990` (punya sibling-query KEDUA tanpa AssessmentType, milik WSE-01/D-05 Phase 380, out-of-scope 381). Hanya rewire sibling-query di 1012-1017.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~SiblingDeterminism"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/SiblingDeterminismTests.cs` ada dengan ≥2 `[Fact]` mengandung `OrderBy` dan `IndexOf`.
    - `CMPController.cs` mengandung `SiblingSessionQuery.SiblingPrePostAwarePredicate` di sekitar StartExam sibling-query (~1012).
    - `CMPController.cs:970-990` (empty-package pre-check) TIDAK berubah — masih mengandung `preCheckSiblingIds` dengan filter `Title/Category/Schedule.Date` tanpa AssessmentType.
    - `dotnet build` exit 0; `dotnet test HcPortal.Tests --filter "FullyQualifiedName~SiblingDeterminism"` exit 0.
  </acceptance_criteria>
  <done>StartExam sibling-query pakai helper type-aware; unit determinisme hijau mengunci invariant order/IndexOf; empty-package pre-check tak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire ReshufflePackage + ReshuffleAll ke helper (determinisme dua sisi)</name>
  <read_first>
    - .planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md (§reshuffle table 5189/5258 IN; 5367/5489 OUT; ReshuffleAll loop nuance)
    - .planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md (D-08 — HANYA StartExam + ReshufflePackage + ReshuffleAll; UpdateShuffleSettings/ManagePackages OUT)
    - Controllers/AssessmentAdminController.cs:5185-5250 (ReshufflePackage sibling 5189-5194, sortedSibling+IndexOf 5216-5217)
    - Controllers/AssessmentAdminController.cs:5256-5337 (ReshuffleAll: load sessions 5258-5263, sortedSibling 5270, loop workerIndex 5315)
    - Helpers/SiblingSessionQuery.cs (helper dari Task 1)
  </read_first>
  <action>
Rewire DUA endpoint reshuffle agar sibling-SET identik dengan StartExam (D-02/D-03/D-08). HANYA dua titik ini — JANGAN sentuh `UpdateShuffleSettings` (~5367) atau `ManagePackages` (~5489): keduanya lock-detection, lock tetap group-wide (D-08, out-of-scope WSE-04, hindari ubah semantik lock v27).

**ReshufflePackage (`AssessmentAdminController.cs:5189-5194`)** — ganti sibling-query inline dgn helper (by `assessment.*`):
```csharp
var siblingSessionIds = await _context.AssessmentSessions
    .Where(SiblingSessionQuery.SiblingPrePostAwarePredicate(
        assessment.Title, assessment.Category, assessment.Schedule.Date, assessment.AssessmentType))
    .Select(s => s.Id)
    .ToListAsync();
```
Biarkan `sortedSiblingIds`+`IndexOf(sessionId)` (5216-5217) apa adanya — sekarang sibling-set = type-aware → workerIndex match StartExam.

**ReshuffleAll (`AssessmentAdminController.cs:5258-5316`)** — NUANCE: endpoint ini memuat `sessions` SEMUA type dalam grup (by-param title/category/scheduleDate) lalu loop tiap session menghitung `workerIndex = sortedSiblingIds.IndexOf(session.Id)` (5315) terhadap SATU set. Untuk Pre/Post same-day, set ini mencampur Pre+Post → workerIndex divergen dari StartExam (yang kini type-filtered). Fix: di dalam loop (sebelum baris 5315), hitung `workerIndex` terhadap sibling-set TYPE-AWARE untuk type session itu sendiri. Pendekatan minimal:
1. Bangun `sortedSiblingIds` PER-TYPE sekali (di luar loop) atau per-session di dalam loop. Rekomendasi: bangun dictionary type→sortedIds sekali sebelum loop dari `sessions` yang sudah dimuat (in-memory, no extra query):
```csharp
// Determinisme type-aware (WSE-04): workerIndex harus match StartExam yang kini type-filtered.
// Group sibling per non-PrePost-vs-(Pre|Post) sama seperti SiblingPrePostAwarePredicate.
string SiblingKey(string? t) => (t == "PreTest" || t == "PostTest") ? t : "__NONPREPOST__";
var sortedByKey = sessions
    .GroupBy(s => SiblingKey(s.AssessmentType))
    .ToDictionary(g => g.Key, g => g.Select(s => s.Id).OrderBy(x => x).ToList());
```
2. Di dalam loop, ganti `int workerIndex = sortedSiblingIds.IndexOf(session.Id);` (5315) dengan:
```csharp
int workerIndex = sortedByKey[SiblingKey(session.AssessmentType)].IndexOf(session.Id);
```
Ini memastikan workerIndex tiap session di-hitung terhadap siblings type-nya saja — IDENTIK dengan StartExam (yang type-filter). Untuk normal exam (semua Standard/''/null → satu key `__NONPREPOST__`) hasil identik dgn perilaku lama (no behavior-change). Tambah `using HcPortal.Helpers;` jika perlu (untuk konsistensi, walau ReshuffleAll memakai in-memory grouping — helper Expression tetap basis semantik yang sama; dokumentasikan keterkaitan di komentar).

CATATAN: `sortedSiblingIds` lama (5270) bisa tetap ada untuk `packages` query/lainnya bila dipakai di tempat lain — JANGAN hapus jika ada referensi lain; cek pemakaiannya sebelum mengubah. Yang WAJIB berubah hanya perhitungan `workerIndex` per-session di loop (5315) agar type-aware.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` ReshufflePackage (~5189) memakai `SiblingSessionQuery.SiblingPrePostAwarePredicate`.
    - `AssessmentAdminController.cs` ReshuffleAll loop menghitung `workerIndex` per-session secara type-aware (mengandung `SiblingKey` atau equivalent yang memisah Pre/Post/non-PrePost).
    - `AssessmentAdminController.cs` `UpdateShuffleSettings` (~5367) dan `ManagePackages` (~5489) sibling-query TIDAK berubah (masih filter `Title/Category/Schedule.Date` tanpa AssessmentType) — D-08.
    - `dotnet build` exit 0.
    - `dotnet test HcPortal.Tests` exit 0 (full xUnit hijau — no regression).
  </acceptance_criteria>
  <done>ReshufflePackage + ReshuffleAll memakai sibling-set type-aware identik dgn StartExam; lock-detection (UpdateShuffleSettings/ManagePackages) tak tersentuh; full suite hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker browser → CMPController.StartExam GET | input `id` route param; owner/role gated (CMPController.cs:905) — UNCHANGED |
| HC browser → AssessmentAdminController reshuffle | `[Authorize(Roles="Admin, HC")]` + antiforgery — UNCHANGED |
| EF query → AssessmentSessions/AssessmentPackages | server-authoritative; klien tak menentukan set soal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-381-01 | Tampering / Integrity | StartExam sibling-query (cross-AssessmentType pool contamination — Pre pool memungut soal Post) | mitigate | Diskriminator server-side `AssessmentType` via `SiblingPrePostAwarePredicate` (D-01/D-09); set soal ditentukan server, bukan klien |
| T-381-02 | Tampering | Determinisme divergen StartExam vs reshuffle → worker "pindah paket" (memecah invariant Phase 373) | mitigate | Shared helper dipakai IDENTIK di 3 titik round-robin + `OrderBy(x=>x)` sebelum IndexOf (Pitfall 2); ReshuffleAll per-session type-aware workerIndex |
| T-381-03 | Tampering | Legacy data (`AssessmentType==null`/`''`) ter-eksklusi dari pool-nya sendiri akibat filter | accept | D-09 type-aware: non-PrePost (Standard/''/null) tetap satu grup, tak pernah terpecah; unit Test 3/4 membuktikan null/'' masuk grup non-PrePost. Asumsi grup homogen (A1) — risiko hanya grup campur null+Standard (sangat tidak mungkin pasca-ISS-04) |
| T-381-04 | Elevation (IDOR) | Akses ujian orang lain via `id` | accept | Owner-check + role-gate existing (CMPController.cs:905) UNCHANGED — di luar perubahan plan ini |
</threat_model>

<verification>
- `dotnet build` exit 0.
- `dotnet test HcPortal.Tests` full hijau (termasuk SiblingPrePostFilterTests + SiblingDeterminismTests baru + no regression SiblingFilterTests/ShuffleReshuffleTests existing).
- Grep: `SiblingPrePostAwarePredicate` muncul di `Helpers/SiblingSessionQuery.cs` + `CMPController.cs` + `AssessmentAdminController.cs` (ReshufflePackage).
- Grep: empty-package pre-check `CMPController.cs:970-990` + lock-detection `UpdateShuffleSettings`/`ManagePackages` TIDAK berubah.
- `migration: false` — tidak ada edit model/DbContext (verifikasi final di Plan 03).
</verification>

<success_criteria>
- WSE-04: sibling helper type-aware (D-09) dipakai identik di StartExam + ReshufflePackage + ReshuffleAll → Pre/Post same-day terpisah, normal exam no behavior-change.
- Determinisme Phase 373 terjaga: workerIndex StartExam == reshuffle untuk session type yang sama.
- Lock-detection (UpdateShuffleSettings/ManagePackages) group-wide tak berubah (D-08).
- Unit test RED→GREEN membuktikan isolation Pre/Post + grup non-PrePost utuh.
</success_criteria>

<output>
After completion, create `.planning/phases/381-worker-entry-startexam-integrity/381-01-SUMMARY.md`
</output>
