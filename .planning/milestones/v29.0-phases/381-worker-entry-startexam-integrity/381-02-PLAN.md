---
phase: 381-worker-entry-startexam-integrity
plan: 02
type: execute
wave: 2
depends_on: [381-01]
files_modified:
  - Controllers/CMPController.cs
autonomous: true
requirements: [WSE-05]
migration: false

must_haves:
  truths:
    - "Admin impersonate buka StartExam worker (Open, StartedAt==null, non-token) → tidak ada mutasi DB: StartedAt tetap null, Status tetap Open"
    - "Admin impersonate → tidak ada UserPackageAssignment dibuat, tidak ada SignalR workerStarted, tidak ada LogActivity 'started'"
    - "Admin impersonate tetap melihat preview soal read-only (assignment dibangun in-memory tanpa persist)"
    - "Setelah stop impersonate, worker asli StartExam → BARU saat itu StartedAt ter-set + 1 UserPackageAssignment ter-persist (timer mulai dari nol)"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "3 write-site StartExam GET dibungkus guard impersonate + in-memory assignment preview (D-04/D-06)"
      contains: "_impersonationService.IsImpersonating()"
  key_links:
    - from: "CMPController.cs StartExam write-site 1 (justStarted ~992)"
      to: "_impersonationService.IsImpersonating()"
      via: "if (justStarted && !_impersonationService.IsImpersonating())"
      pattern: "justStarted && !_impersonationService.IsImpersonating"
    - from: "CMPController.cs StartExam assignment create (~1042)"
      to: "_impersonationService.IsImpersonating()"
      via: "guard di sekitar _context.Add + SaveChangesAsync (persist HANYA worker asli)"
      pattern: "!_impersonationService.IsImpersonating"
---

<objective>
WSE-05 (OPS-01 + TOK-03) — Admin yang impersonate/membuka ujian worker TIDAK memulai ujian atau membakar waktu/mengunci shuffle worker.

Akar masalah: guard write-on-GET dari Phase 377 (CMPController.cs:911-917) hanya membungkus transisi Upcoming→Open. Tiga mutasi lain di `StartExam` GET tidak dijaga: (1) set `StartedAt`+`Status=InProgress`, (2) SignalR `workerStarted` broadcast + `LogActivity("started")`, (3) create `UserPackageAssignment`. Akibatnya admin yang impersonate worker X dan membuka ujian X → membakar timer X + mengunci shuffle X + broadcast palsu. Fix: bungkus 3 write-site dengan `if(!_impersonationService.IsImpersonating())` (mirror precedent 911-917), dan saat impersonate + assignment==null build assignment IN-MEMORY (D-06) tanpa persist agar admin tetap bisa preview soal read-only.

OPS-01 & TOK-03 = SATU fix koheren (jangan dipecah). `VerifyToken` TIDAK disentuh (D-05 — hanya TempData, bukan mutasi DB).

Purpose: tegakkan read-only invariant impersonate penuh di StartExam (cegah tampering state worker oleh admin).
Output: 3 guard + 1 cabang in-memory preview di `CMPController.cs` StartExam GET.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md
@.planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md
@.planning/phases/381-worker-entry-startexam-integrity/381-RESEARCH.md

<sequencing_note>
**depends_on 381-01** (Wave 2). Plan 01 sudah me-rewire sibling-query StartExam di `CMPController.cs` (~1012). Plan ini menyentuh `CMPController.cs` StartExam yang SAMA (write-site 992/1000 + assignment create 1042) → sequential, hindari merge-conflict. Eksekusi setelah 381-01 landing. Juga setelah Phase 380 (dependency milestone). JANGAN sentuh sibling-query (sudah milik Plan 01) maupun empty-package pre-check (970-990, milik Phase 380).
</sequencing_note>

<interfaces>
<!-- Kontrak existing — VERIFIED live 2026-06-14. Executor pakai langsung. -->

Field injeksi SUDAH ADA (JANGAN tambah) — `CMPController.cs:40,71`:
```csharp
private readonly ImpersonationService _impersonationService;  // line 40 (concrete type, Phase 377)
// _impersonationService.IsImpersonating() sudah dipakai 4× (88, 486, 637, 912)
```

Guard PRECEDENT (template persis untuk D-04) — `CMPController.cs:911-917`:
```csharp
// Phase 377 (Pitfall 3 / T-377-09): write-on-GET guard — JANGAN tulis DB saat impersonasi.
if (!_impersonationService.IsImpersonating())
{
    assessment.Status = "Open";
    assessment.UpdatedAt = DateTime.UtcNow;
    await _context.SaveChangesAsync();
}
```

Effective user (X bila impersonate) — `CMPController.cs:903`: `var (user, _) = await GetCurrentUserRoleLevelAsync();` (Phase 377 resolver). `user.Id` = X saat impersonate.

`Models/UserPackageAssignment.cs`: field `ShuffledQuestionIds` (string JSON), `ShuffledOptionIdsPerQuestion` (string JSON), `SavedQuestionCount` (int), `GetShuffledQuestionIds()` (deserialize). View-build downstream HANYA baca object ini (no DB re-read pasca-create — VERIFIED).

`Helpers/ShuffleEngine.cs`: `BuildQuestionAssignment(packages, shuffleQuestions, workerIndex, rng)` + `BuildOptionShuffle(assignedQuestions, shuffleOptions, rng)` (pure, no DB).
</interfaces>

<write_sites_verified>
<!-- VERIFIED live CMPController.cs 2026-06-14 (line numbers post-Phase-380 empty-package block) -->

Write-site 1 — justStarted DB write (992-997):
```csharp
if (justStarted)                       // ← bungkus: if (justStarted && !_impersonationService.IsImpersonating())
{
    assessment.Status = "InProgress";
    assessment.StartedAt = DateTime.UtcNow;
    await _context.SaveChangesAsync();
}
```

Write-site 2 — SignalR workerStarted + LogActivity (1000-1008):
```csharp
if (justStarted)                       // ← bungkus: if (justStarted && !_impersonationService.IsImpersonating())
{
    var startBatchKey = $"{assessment.Title}|{assessment.Category}|{assessment.Schedule.Date:yyyy-MM-dd}";
    await _hubContext.Clients.Group($"monitor-{startBatchKey}").SendAsync("workerStarted",
        new { sessionId = assessment.Id, workerName = user.FullName, status = "InProgress" });
    LogActivityAsync(assessment.Id, "started");
}
```

Write-site 3 — create UserPackageAssignment + SaveChanges (di dalam `if (assignment == null)` ~1042):
```csharp
assignment = new UserPackageAssignment { ... };   // 1062-1069
assignment.SavedQuestionCount = shuffledIds.Count; // 1071
_context.UserPackageAssignments.Add(assignment);   // 1072 ← guard persist di sini
try { await _context.SaveChangesAsync(); }          // 1075
catch (DbUpdateException) { /* race-recovery 1077-1086 */ }
```

`justStarted = assessment.StartedAt == null;` (968) DIHITUNG SEBELUM guard — Pitfall 1: JANGAN pindah/ubah. `isResume = !justStarted` (~1156) bergantung padanya.
</write_sites_verified>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard write-site 1 + 2 (justStarted DB write + SignalR/LogActivity)</name>
  <read_first>
    - .planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md (§Pattern A — write-site 1/2, Pitfall 1 justStarted/isResume)
    - .planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md (D-04 mirror 905; D-05 VerifyToken NOT touched; D-07 stale-check)
    - Controllers/CMPController.cs:892-1010 (guard precedent 911-917, justStarted 968, write-site 1 992-997, write-site 2 1000-1008)
  </read_first>
  <action>
Bungkus DUA write-site pertama di StartExam GET dengan guard impersonate, mirror precedent 911-917 (D-04).

**Write-site 1 (`CMPController.cs:992-997`)** — ubah kondisi `if (justStarted)` menjadi:
```csharp
if (justStarted && !_impersonationService.IsImpersonating())
{
    assessment.Status = "InProgress";
    assessment.StartedAt = DateTime.UtcNow;
    await _context.SaveChangesAsync();
}
```

**Write-site 2 (`CMPController.cs:1000-1008`)** — ubah kondisi `if (justStarted)` menjadi:
```csharp
if (justStarted && !_impersonationService.IsImpersonating())
{
    var startBatchKey = $"{assessment.Title}|{assessment.Category}|{assessment.Schedule.Date:yyyy-MM-dd}";
    await _hubContext.Clients.Group($"monitor-{startBatchKey}").SendAsync("workerStarted",
        new { sessionId = assessment.Id, workerName = user.FullName, status = "InProgress" });
    LogActivityAsync(assessment.Id, "started");
}
```

KRITIS:
- JANGAN ubah baris 968 `bool justStarted = assessment.StartedAt == null;` (dihitung pra-guard — Pitfall 1). Saat impersonate, `StartedAt` tetap null → `justStarted=true` tetapi guard mencegah write. `isResume = !justStarted` (~1156) → false saat impersonate → blok SavedAnswers (~1180) tak fire (benar: preview = soal kosong, D-07).
- JANGAN sentuh empty-package pre-check 970-990 (Phase 380).
- JANGAN tambah guard di `VerifyToken` (D-05).
- Tambahkan komentar singkat `// WSE-05/D-04 (OPS-01/TOK-03): write-on-GET guard, mirror 911-917` di kedua site.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `CMPController.cs` mengandung `if (justStarted && !_impersonationService.IsImpersonating())` MINIMAL 2× (grep count ≥ 2).
    - Baris `bool justStarted = assessment.StartedAt == null;` masih ada dan TIDAK dipindah (masih sebelum empty-package pre-check).
    - `VerifyToken` method TIDAK mengandung `IsImpersonating` (D-05 — tak ada guard ditambah di sana).
    - empty-package pre-check (`preCheckSiblingIds`) tak berubah.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>Write-site 1 (StartedAt/Status) + write-site 2 (SignalR/LogActivity) ter-guard impersonate; justStarted/isResume tak berubah; VerifyToken tak disentuh.</done>
</task>

<task type="auto">
  <name>Task 2: Guard write-site 3 + cabang in-memory assignment preview (D-06)</name>
  <read_first>
    - .planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md (§Pattern B in-memory preview; "Kenapa aman" view-build no DB re-read; vm.AssignmentId=0)
    - .planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md (D-06 in-memory build; Claude's Discretion RNG seed)
    - .planning/phases/381-worker-entry-startexam-integrity/381-RESEARCH.md (§Pattern 4; Pitfall 5 Razor runtime; A3 vm.AssignmentId=0 no NRE)
    - Controllers/CMPController.cs:1034-1090 (package path, assignment==null block 1042-1087, build+Add+Save)
    - Models/UserPackageAssignment.cs (GetShuffledQuestionIds, fields)
  </read_first>
  <action>
Di dalam blok `if (assignment == null)` (`CMPController.cs:1042-1087`), pisahkan PERSIST dari BUILD. Build assignment selalu (worker asli + impersonate); HANYA persist saat worker asli (D-04/D-06). Saat impersonate → object in-memory feed view (preview read-only), zero mutasi DB.

Ubah ekor blok (bagian 1072-1086, dari `_context.UserPackageAssignments.Add(assignment);` sampai akhir try/catch) menjadi:
```csharp
assignment.SavedQuestionCount = shuffledIds.Count;   // existing 1071 (biarkan di atas)

// WSE-05/D-06: saat impersonate, JANGAN persist — object in-memory cukup feed view (preview read-only).
// Worker asli StartExam nanti → assignment baru ter-create & persist normal (SC#3 deferred-start).
if (!_impersonationService.IsImpersonating())
{
    _context.UserPackageAssignments.Add(assignment);
    try
    {
        await _context.SaveChangesAsync();
    }
    catch (DbUpdateException)
    {
        // Race condition: another request already created the assignment (double-click/duplicate tab)
        _context.ChangeTracker.Clear();
        assignment = await _context.UserPackageAssignments
            .FirstOrDefaultAsync(a => a.AssessmentSessionId == id);
        if (assignment == null)
            throw; // genuinely unexpected — rethrow
    }
}
```

JANGAN ubah bagian build di atasnya (1044-1071: `rng`, `BuildQuestionAssignment`, `assignedQuestions`, `BuildOptionShuffle`, `sentinelPackage`, `new UserPackageAssignment{...}`) — assignment dibangun identik. `UserId = user.Id` sudah = effective user X saat impersonate (resolver 903).

DISKRESI (boleh): untuk preview stabil per-reload saat impersonate, ganti `var rng = Random.Shared;` (1044) dengan `var rng = _impersonationService.IsImpersonating() ? new Random(id) : Random.Shared;` (seed dari `id` → preview konsisten). Opsional — preview-only, tak memengaruhi worker asli. Bila tak yakin, biarkan `Random.Shared` (acak per refresh, tetap valid preview).

KONSEKUENSI (terverifikasi aman): saat impersonate `assignment.Id == 0` (belum persist) → `vm.AssignmentId = 0`. View-build downstream (1089-1225) hanya baca `assignment.GetShuffledQuestionIds()` + field string → in-memory object cukup, no DB re-read. Submit oleh admin di luar scope (read-only). Render preview = manual UAT Playwright runtime (Plan 03 checkpoint, Pitfall 5).

Stale-question check (~1095, `if (assessment.StartedAt != null && ...)`): saat impersonate `StartedAt==null` → block tak fire (D-07, no change).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `CMPController.cs` blok `if (assignment == null)` membungkus `_context.UserPackageAssignments.Add(assignment)` + `SaveChangesAsync` di dalam `if (!_impersonationService.IsImpersonating())`.
    - Bagian build assignment (`ShuffleEngine.BuildQuestionAssignment` + `new UserPackageAssignment`) TETAP dieksekusi tanpa guard (jalan untuk worker asli DAN impersonate).
    - `UserId = user.Id` masih ada di pembuatan assignment.
    - Grep `!_impersonationService.IsImpersonating` di `CMPController.cs` total ≥ 3 (write-site 1 + 2 dari Task 1 + persist guard ini), plus precedent 912 = ≥ 4 occurrence keseluruhan.
    - `dotnet build` exit 0; `dotnet test HcPortal.Tests` exit 0 (no regression).
  </acceptance_criteria>
  <done>Persist assignment ter-guard impersonate; build in-memory selalu jalan → admin preview read-only tanpa mutasi; worker asli persist normal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (impersonate X) browser → CMPController.StartExam GET | admin membuka ujian X; harus read-only (no mutasi state worker X) |
| worker asli browser → CMPController.StartExam GET | worker memulai ujian — write diizinkan (timer/assignment) |
| StartExam GET → AssessmentSession/UserPackageAssignment (DB write) | mutasi hanya boleh dari aksi worker asli |
| StartExam GET → SignalR monitor group | broadcast `workerStarted` hanya saat worker asli mulai |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-381-05 | Tampering | Write-on-GET side-effect saat impersonate membakar timer worker (set StartedAt+Status=InProgress) | mitigate | Guard `if (justStarted && !_impersonationService.IsImpersonating())` di write-site 1 (D-04, mirror 911-917) |
| T-381-06 | Tampering | Impersonate mengunci shuffle worker (create UserPackageAssignment → shuffle X terkunci lebih awal) | mitigate | Persist assignment ter-guard `if(!IsImpersonating())`; impersonate build in-memory only (D-06) → worker asli tetap dapat shuffle baru saat login (SC#3) |
| T-381-07 | Spoofing | SignalR `workerStarted` broadcast palsu saat admin membuka ujian (HC monitor mengira worker mulai) | mitigate | Guard broadcast di write-site 2 (`justStarted && !IsImpersonating()`) |
| T-381-08 | Repudiation | LogActivity "started" palsu ter-catat saat impersonate | mitigate | LogActivity di dalam guard write-site 2 → tak fire saat impersonate |
| T-381-09 | Information Disclosure | Preview impersonate render NRE/500 (vm.AssignmentId=0) membocorkan stack/state | accept | View-build no DB re-read; AssignmentId=0 path read-only; diverifikasi via UAT Playwright runtime (Plan 03, Pitfall 5/lesson 354) |
| T-381-10 | Elevation | VerifyToken bypass jika guard salah ditambahkan/dihapus | accept | D-05 — VerifyToken hanya TempData (bukan mutasi DB worker), sengaja TIDAK di-guard; gate token sungguh (StartedAt) = scope Phase 382 TOK-02 |
</threat_model>

<verification>
- `dotnet build` exit 0; `dotnet test HcPortal.Tests` full hijau (no regression).
- Grep `!_impersonationService.IsImpersonating` di `CMPController.cs` ≥ 3 site baru + precedent (total ≥ 4).
- Grep: `bool justStarted = assessment.StartedAt == null;` tetap di posisi pra-guard.
- Grep: `VerifyToken` tak mengandung `IsImpersonating` (D-05).
- Grep: empty-package pre-check (`preCheckSiblingIds`) tak berubah.
- `migration: false` — tidak ada edit model/DbContext.
</verification>

<success_criteria>
- WSE-05 SC#2: impersonate buka StartExam (Open/StartedAt-null/non-token) → no mutation (StartedAt null, Status Open, 0 assignment, no SignalR, no LogActivity).
- WSE-05 SC#3: stop impersonate + worker asli StartExam → StartedAt ter-set + 1 assignment persist.
- D-06: admin tetap preview soal read-only (assignment in-memory).
- D-05: VerifyToken tak disentuh. OPS-01 & TOK-03 = satu fix koheren (3 write-site di-guard).
</success_criteria>

<output>
After completion, create `.planning/phases/381-worker-entry-startexam-integrity/381-02-SUMMARY.md`
</output>
