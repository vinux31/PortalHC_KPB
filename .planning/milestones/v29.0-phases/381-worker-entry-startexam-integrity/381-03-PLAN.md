---
phase: 381-worker-entry-startexam-integrity
plan: 03
type: execute
wave: 3
depends_on: [381-01, 381-02]
files_modified:
  - tests/e2e/exam-taking.spec.ts
  - tests/e2e/impersonation.spec.ts
autonomous: false
requirements: [WSE-04, WSE-05]
migration: false

must_haves:
  truths:
    - "E2E: StartExam sesi Pre same-day → jumlah & teks soal == paket Pre saja (Post tak tercampur)"
    - "E2E: impersonate buka StartExam (Open/StartedAt-null/non-token) → DB assert no mutation (StartedAt null, Status Open, 0 UserPackageAssignment)"
    - "E2E: stop impersonate + worker asli StartExam → StartedAt ter-set + 1 UserPackageAssignment (SC#3)"
    - "Impersonate render StartExam tidak NRE/500 (preview soal read-only tampil — manual UAT runtime)"
    - "migration: false terbukti — `dotnet ef migrations` tidak menghasilkan diff baru"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "E2E #4 PrePost same-day pool-only (entry assertion)"
      contains: "StartExam"
    - path: "tests/e2e/impersonation.spec.ts"
      provides: "E2E #7 impersonate read-only + deferred-start (DB assert)"
      contains: "queryScalar"
  key_links:
    - from: "tests/e2e/exam-taking.spec.ts (#4)"
      to: "CMPController.StartExam sibling-query (type-aware Plan 01)"
      via: "createPrePostAssessmentViaWizard → GET StartExam → assert pool"
      pattern: "createPrePostAssessmentViaWizard"
    - from: "tests/e2e/impersonation.spec.ts (#7)"
      to: "CMPController.StartExam write-site guards (Plan 02)"
      via: "startUser → goto StartExam → dbSnapshot.queryScalar no-mutation"
      pattern: "UserPackageAssignments"
---

<objective>
Acceptance & verifikasi Phase 381 — buktikan WSE-04 (Pre/Post pool isolation) + WSE-05 (impersonate read-only) end-to-end via Playwright, konfirmasi preview impersonate render tanpa NRE (manual UAT runtime — lesson Phase 354/355), dan kunci `migration: false`.

Plan ini TIDAK menulis kode produksi — hanya extend e2e harness existing + checkpoint UAT + verifikasi migration. Eksekusi setelah 381-01 + 381-02 landing (Wave 3).

Purpose: gate phase — assertion entry-pool (WSE-04) + no-mutation impersonate (WSE-05) terbukti; preview render aman; zero migration.
Output: 2 e2e spec di-extend + UAT checkpoint approved + migration-clean confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md
@.planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md
@.planning/phases/381-worker-entry-startexam-integrity/381-RESEARCH.md
@.planning/phases/381-worker-entry-startexam-integrity/381-VALIDATION.md

<env_note>
Local e2e quirks (memory: "Local e2e SQL env fix"):
- Start SQLBrowser service manual sebelum run.
- Run lokal AD off: `Authentication__UseActiveDirectory=false dotnet run` (login non-AD).
- Combined Playwright run WAJIB `--workers=1` (DB isolation; globalTeardown RESTORE).
- baseURL http://localhost:5277. `dbSnapshot.ts` sqlcmd localhost-guarded (`-S localhost\SQLEXPRESS`).
- Bila MCP browser gagal launch (system-Chrome conflict), pakai bundled chromium `--headed` (memory Phase 379).
</env_note>

<interfaces>
<!-- Harness existing — VERIFIED 2026-06-14. Pakai langsung, no exploration. -->

`tests/e2e/helpers/examTypes.ts`:
- `createPrePostAssessmentViaWizard(page, opts) → { preIds, postIds }` (~:610) — create Pre/Post same-day group.
- `createDefaultPackage(...)` (~:166), `addQuestionViaForm(...)` (~:240) — isi paket Pre vs Post dengan SOAL BERBEDA.

`tests/e2e/impersonation.spec.ts` (helper di file):
- `startUser(page, query, target)` (:38), `resolveUser(page, query, fullName)` (:55), `stopImpersonation(page)` (:50) via `/Admin/Impersonate` + `/Admin/SearchUsersApi`.

`tests/helpers/dbSnapshot.ts`:
- `queryScalar(sql)` (:116) — sqlcmd scalar; `backup`/`restore` (:67/:80) localhost-guarded.

`tests/helpers/auth.ts` + `accounts.ts`:
- `login(page, key)`; worker = `coachee` (rino.prasetyo@pertamina.com) / `coachee2` (iwan3@pertamina.com); admin.

StartExam URL: `GET /CMP/StartExam/{id}`. Question card DOM selector: `[id^="qcard_"]` (jumlah soal). Untuk teks soal assert via DOM text content.
</interfaces>

<db_assert_sql>
E2E #7 no-mutation (impersonate buka StartExam) — `dbSnapshot.queryScalar`:
```sql
SELECT CASE WHEN StartedAt IS NULL THEN 1 ELSE 0 END FROM AssessmentSessions WHERE Id={id}   -- expect 1
SELECT COUNT(*) FROM AssessmentSessions WHERE Id={id} AND Status='Open'                       -- expect 1
SELECT COUNT(*) FROM UserPackageAssignments WHERE AssessmentSessionId={id}                    -- expect 0
```
SC#3 deferred-start (stop impersonate → worker asli StartExam):
```sql
SELECT CASE WHEN StartedAt IS NOT NULL THEN 1 ELSE 0 END FROM AssessmentSessions WHERE Id={id} -- expect 1
SELECT COUNT(*) FROM UserPackageAssignments WHERE AssessmentSessionId={id}                      -- expect 1
```
SignalR `workerStarted` absence = konsekuensi guard, diverifikasi via 3 DB-assert di atas (tak ada hook spy di harness — RESEARCH A4).
</db_assert_sql>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E #4 — PrePost same-day pool-only (entry assertion)</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - .planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md (§"E2E #4"; harness examTypes.ts)
    - .planning/phases/381-worker-entry-startexam-integrity/381-CONTEXT.md (D-01 entry-pool only; full pass/grade = deferred pasca-382)
    - tests/e2e/exam-taking.spec.ts (struktur describe/test existing; helper import)
    - tests/e2e/helpers/examTypes.ts (createPrePostAssessmentViaWizard return preIds/postIds; createDefaultPackage; addQuestionViaForm)
    - tests/helpers/auth.ts + tests/helpers/accounts.ts (login, worker keys)
  </read_first>
  <action>
Extend `tests/e2e/exam-taking.spec.ts` dengan describe block baru "Phase 381 WSE-04 — PrePost same-day pool isolation" (atau buat spec baru `tests/e2e/prepost-pool-381.spec.ts` bila lebih bersih — Claude's discretion, tapi reuse `examTypes.ts` helper):

Skenario (entry-pool assertion ONLY — full pass/grade = acceptance pasca-Phase 382, DEFERRED):
1. Login admin → `createPrePostAssessmentViaWizard` → dapat `{ preIds, postIds }` same-day group.
2. Isi paket Pre vs Post dengan SOAL BERBEDA (teks distinct, jumlah bisa berbeda) via `createDefaultPackage` + `addQuestionViaForm` — mis. Pre 3 soal ("PRE-Q1".."PRE-Q3"), Post 2 soal ("POST-Q1".."POST-Q2").
3. Login worker (`coachee`) → `GET /CMP/StartExam/{preId}` → assert:
   - jumlah question card (`[id^="qcard_"]`) == jumlah soal paket Pre (3), BUKAN gabungan Pre+Post (5).
   - teks soal hanya mengandung "PRE-Q*", TIDAK mengandung "POST-Q*".
4. (Mirror, opsional bila waktu/budget cukup) `GET /CMP/StartExam/{postId}` → assert jumlah == paket Post (2), teks hanya "POST-Q*".

Pakai `test.describe.configure({ mode: 'serial' })` bila perlu state berurutan. Pastikan teardown/restore via globalTeardown existing (jangan tinggalkan seed). Tandai full pass/grade sebagai `test.skip` atau komentar "DEFERRED pasca-382" agar tak salah dianggap gate.

CATATAN dependency: WSE-04 fix (Plan 01) + Phase 380 (ShuffleEngine) harus landing. Jika paket Pre berisi tapi soal 0 → indikasi Phase 380 belum landing (Pitfall 4), bukan bug 381.
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-taking --workers=1 -g "WSE-04" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/exam-taking.spec.ts` (atau spec baru) mengandung describe/test ber-label "WSE-04" dan memanggil `createPrePostAssessmentViaWizard`.
    - Test mengandung assertion jumlah `[id^="qcard_"]` == jumlah soal paket Pre DAN assertion teks tak mengandung marker Post.
    - `npx playwright test ... -g "WSE-04" --workers=1` exit 0 (test PASS) — dijalankan dgn app lokal running (AD off) + SQLBrowser.
    - Full pass/grade ditandai DEFERRED (skip/komentar) — bukan gate.
  </acceptance_criteria>
  <done>E2E #4 hijau: StartExam Pre pool == paket Pre saja; Post tak tercampur (WSE-04 entry assertion).</done>
</task>

<task type="auto">
  <name>Task 2: E2E #7 — impersonate read-only no-mutation + deferred-start (DB assert)</name>
  <files>tests/e2e/impersonation.spec.ts</files>
  <read_first>
    - .planning/phases/381-worker-entry-startexam-integrity/381-PATTERNS.md (§"E2E #7" + DB-assert SQL)
    - .planning/phases/381-worker-entry-startexam-integrity/381-RESEARCH.md (§Code Examples E2E #7; A4 SignalR absence via DB)
    - tests/e2e/impersonation.spec.ts (startUser/resolveUser/stopImpersonation helpers + describe struktur)
    - tests/helpers/dbSnapshot.ts (queryScalar :116)
    - tests/helpers/accounts.ts (worker target id/fullName)
  </read_first>
  <action>
Extend `tests/e2e/impersonation.spec.ts` dengan test baru "IMP-381 WSE-05 — StartExam read-only impersonate":

Setup: butuh 1 AssessmentSession milik worker target, Status='Open', StartedAt=null, non-token (AccessToken kosong / IsTokenRequired=false), punya paket berisi soal. Buat via wizard helper (`createAssessmentViaWizard` untuk worker target) ATAU reuse pola seed existing — Claude's discretion; pastikan restore via globalTeardown.

Skenario:
1. Login admin → `resolveUser` worker target → `startUser(page, query, target)` (impersonate worker X).
2. `page.goto('/CMP/StartExam/{id}')` → tunggu render (networkidle).
3. ASSERT no-mutation via `dbSnapshot.queryScalar`:
   - `StartedAt IS NULL` → 1
   - `Status='Open'` count → 1
   - `UserPackageAssignments WHERE AssessmentSessionId={id}` count → 0
4. `stopImpersonation(page)` → `login(page, 'coachee')` (worker asli, sesuaikan key dgn owner session) → `page.goto('/CMP/StartExam/{id}')`.
5. ASSERT deferred-start (SC#3):
   - `StartedAt IS NOT NULL` → 1
   - `UserPackageAssignments WHERE AssessmentSessionId={id}` count → 1

CATATAN: SignalR `workerStarted` absence diverifikasi via 3 DB-assert step 3 (broadcast di-gate `justStarted && !IsImpersonating()` → tak fire; tak ada hook spy, RESEARCH A4). Run combined `--workers=1` (DB isolation).
  </action>
  <verify>
    <automated>cd tests && npx playwright test impersonation --workers=1 -g "WSE-05" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/impersonation.spec.ts` mengandung test ber-label "WSE-05" yang memanggil `startUser` + `goto('/CMP/StartExam/` + `dbSnapshot` / `queryScalar`.
    - Test mengandung assertion `StartedAt IS NULL` (saat impersonate) DAN `StartedAt IS NOT NULL` (setelah worker asli) DAN `UserPackageAssignments` count 0→1.
    - `npx playwright test impersonation -g "WSE-05" --workers=1` exit 0 (PASS) — app lokal running (AD off) + SQLBrowser.
  </acceptance_criteria>
  <done>E2E #7 hijau: impersonate buka StartExam → no mutation; stop + worker asli → StartedAt ter-set + 1 assignment (SC#2+SC#3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT manual — impersonate preview render + migration-clean</name>
  <action>Checkpoint manual (bukan auto). Setelah E2E #4 (Task 1) + #7 (Task 2) hijau, verifikasi DUA hal yang tak tertangkap automated, lalu tunggu approval user. (a) Preview StartExam saat impersonate render Razor tanpa NRE/500 (lesson Phase 354/355, `vm.AssignmentId=0` path). (b) `migration: false` terbukti — `dotnet ef migrations add` menghasilkan body kosong lalu di-remove. Ikuti langkah persis di &lt;how-to-verify&gt;. Jangan lanjut/ship sampai user mengetik "approved".</action>
  <files>—</files>
  <what-built>
    WSE-04 sibling type-aware (Pre/Post pool isolation) + WSE-05 write-on-GET guard + in-memory preview (D-06). E2E #4 (pool) + #7 (no-mutation) sudah hijau otomatis. Checkpoint ini memverifikasi DUA hal yang tak tertangkap automated: (a) preview impersonate render Razor tanpa NRE/500 (lesson Phase 354/355 — `vm.AssignmentId=0` path), (b) `migration: false` benar (zero model edit → tak ada scaffold migration baru).
  </what-built>
  <how-to-verify>
    **Bagian A — Preview impersonate render (Razor runtime, Pitfall 5):**
    1. Jalankan app lokal: `Authentication__UseActiveDirectory=false dotnet run` (start SQLBrowser dulu). Buka http://localhost:5277.
    2. Login admin → `/Admin/Impersonate` → impersonate worker yang punya ujian Open/StartedAt-null/non-token (yang dibuat di Task 2, atau seed manual).
    3. Buka `/CMP/StartExam/{id}` → EXPECTED: halaman render soal (preview read-only) TANPA error 500 / RuntimeBinderException / NRE. Soal tampil.
    4. (Opsional sanity) Cek DB lokal saat masih impersonate via sqlcmd:
       `SELECT StartedAt, Status FROM AssessmentSessions WHERE Id={id}` → StartedAt NULL, Status 'Open';
       `SELECT COUNT(*) FROM UserPackageAssignments WHERE AssessmentSessionId={id}` → 0.

    **Bagian B — Migration clean (migration: false):**
    5. Jalankan: `dotnet ef migrations add _Phase381MigrationCheck_DELETE_ME --no-build` (atau `dotnet ef migrations add` lalu inspeksi). EXPECTED: migration yang ter-generate KOSONG (body `Up`/`Down` tak ada perubahan skema) ATAU command melaporkan "No changes". JIKA ada perubahan skema → ADA model edit tak sengaja (BUG — perbaiki sebelum approve).
    6. HAPUS migration cek tsb: `dotnet ef migrations remove --force` (jangan tinggalkan file cek di tree). Pastikan `git status` bersih dari file migration cek.
  </how-to-verify>
  <verify>Manual (checkpoint:human-verify). Tidak ada automated command — verifikasi via langkah &lt;how-to-verify&gt; (browser render + `dotnet ef migrations add`/`remove`).</verify>
  <done>User mengetik "approved" setelah konfirmasi: preview impersonate render tanpa NRE/500 (Bagian A) DAN `dotnet ef migrations add` menghasilkan body kosong + di-remove + git status bersih (Bagian B).</done>
  <resume-signal>Ketik "approved" jika preview render OK (no NRE) DAN migration kosong/removed, atau jelaskan masalah (mis. 500 di preview, atau migration menghasilkan diff skema).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (impersonate) browser → StartExam render | preview read-only; render tak boleh leak error/state |
| test harness → DB (sqlcmd localhost) | assert-only; restore via globalTeardown |
| dev → EF migrations scaffold | verifikasi zero schema change (migration: false invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-381-11 | Information Disclosure | Preview impersonate render NRE/500 (`vm.AssignmentId=0`) bocorkan stack/internal | mitigate | UAT Playwright/manual runtime render check (Bagian A) — lesson Phase 354/355 (grep+build tak cukup untuk Razor dynamic) |
| T-381-12 | Tampering | Migration tak sengaja (model edit terselip) → schema drift + IT action tak terduga | mitigate | `dotnet ef migrations add` cek menghasilkan body kosong (Bagian B); remove file cek; git status bersih |
| T-381-13 | Tampering | E2E seed tertinggal di DB lokal mengotori sesi berikut | mitigate | globalTeardown RESTORE existing + `--workers=1` isolation; verifikasi DB bersih pasca-run |
</threat_model>

<verification>
- E2E #4 (`exam-taking` -g "WSE-04") PASS — pool Pre-only.
- E2E #7 (`impersonation` -g "WSE-05") PASS — no-mutation + deferred-start.
- Full xUnit `dotnet test HcPortal.Tests` hijau (regression gate dari Plan 01/02).
- Manual UAT Bagian A: preview impersonate render OK (no NRE/500).
- Manual UAT Bagian B: `dotnet ef migrations add` → body kosong / no changes → removed; git status bersih (migration: false terbukti).
</verification>

<success_criteria>
- WSE-04 acceptance entry-pool: StartExam Pre == paket Pre saja (E2E #4). Full pass/grade DEFERRED pasca-382.
- WSE-05 acceptance: impersonate no-mutation (SC#2) + deferred-start (SC#3) (E2E #7) + preview render aman (UAT A).
- migration: false terbukti (UAT B).
- Phase gate: full xUnit + e2e #4/#7 green sebelum `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/381-worker-entry-startexam-integrity/381-03-SUMMARY.md`
</output>
