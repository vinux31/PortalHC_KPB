---
phase: 382-grading-lifecycle-cert
plan: 02
type: execute
wave: 2
depends_on: [382-01]
files_modified:
  - Controllers/CMPController.cs
  - HcPortal.Tests/AbandonGuardTests.cs
  - HcPortal.Tests/EnsureCanSubmitStandardTests.cs
  - HcPortal.Tests/AutoSubmitTokenRetryTests.cs
  - HcPortal.Tests/TokenGateTests.cs
autonomous: true
requirements: [WSE-06, WSE-07, WSE-08, WSE-09, WSE-10]
tags: [submit-exam, abandon, timer, token-gate, race-safety, coherent-single-stream]

must_haves:
  truths:
    - "SubmitExam SAVE-01: GroupBy final answer pakai OrderByDescending(SubmittedAt).First() → Score push (SignalR) == Score GradingService (tidak divergen)"
    - "SubmitExam STAT-01: POST ke sesi Completed/Abandoned/Cancelled/PendingGrading ditolak (TempData + redirect + audit SubmitExamBlocked), tidak jadi Completed-lulus"
    - "SubmitExam + SaveAnswer TOK-02: sesi IsTokenRequired && StartedAt==null → reject (belum lewat lobby token)"
    - "AbandonExam STAT-02: ExecuteUpdate atomic Where(Id && UserId && (InProgress||Open)); rowsAffected==0 → reject; ownership tetap di WHERE"
    - "EnsureCanSubmitExamAsync TMR-01: 'Standard' kini DI-ENFORCE (skip hanya Manual/null); submit manual jauh-telat ditolak + audit, on-time diterima"
    - "TMR-03: AutoSubmitToken TIDAK dikonsumsi sebelum grading commit (retry pasca-DB-hiccup tidak permanent-reject)"
    - "TMR-02: gate incomplete-submit pakai serverTimerExpired sebagai otoritas, client isAutoSubmit bukan satu-satunya bypass"
    - "Tidak ada migration / perubahan model"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "SubmitExam coherent (SAVE-01 OrderBy + STAT-01 guard + TOK-02 gate + TMR-02), SaveAnswer TOK-02, AbandonExam STAT-02 ExecuteUpdate, EnsureCanSubmit TMR-01 invert + TMR-03 token-defer"
      contains: "ExecuteUpdateAsync"
    - path: "HcPortal.Tests/AbandonGuardTests.cs"
      provides: "real-SQL: AbandonExam pada Completed → rowsAffected==0, status tetap Completed; ownership negatif"
      contains: "AbandonGuard"
    - path: "HcPortal.Tests/EnsureCanSubmitStandardTests.cs"
      provides: "unit: Standard elapsed>allowed tanpa token → reject + audit; on-time → null (pass)"
      contains: "EnsureCanSubmitStandard"
    - path: "HcPortal.Tests/AutoSubmitTokenRetryTests.cs"
      provides: "unit: token tidak dikonsumsi sebelum grading sukses (retry aman)"
      contains: "AutoSubmitTokenRetry"
    - path: "HcPortal.Tests/TokenGateTests.cs"
      provides: "unit/integration: SaveAnswer/SubmitExam token-required + StartedAt==null → reject"
      contains: "TokenGate"
  key_links:
    - from: "Controllers/CMPController.cs SubmitExam GroupBy L1646"
      to: "OrderByDescending(SubmittedAt).First()"
      via: "final-write-wins dedupe agar push==stored Score"
      pattern: "OrderByDescending\\(r => r.SubmittedAt\\)"
    - from: "Controllers/CMPController.cs AbandonExam"
      to: "AssessmentSessions.Where(UserId && status guard).ExecuteUpdateAsync"
      via: "atomic guarded transition (was TOCTOU L1257-1267)"
      pattern: "rowsAffected"
    - from: "Controllers/CMPController.cs EnsureCanSubmitExamAsync"
      to: "skip only Manual/null (enforce Standard)"
      via: "allowlist inversion L4413-4418"
      pattern: "AssessmentType.Manual"
---

<objective>
Garap SEMUA mutasi sisi `Controllers/CMPController.cs` untuk WSE-06/07/08/09/10 dalam SATU plan koheren single-stream. Audit (§Risiko same-method density) MEWAJIBKAN ini tidak di-split ke sub-agent paralel: `SubmitExam` dimutasi oleh SAVE-01 + STAT-01 + TOK-02 + TMR-02 sekaligus; `SaveAnswer` oleh TOK-02; `AbandonExam` oleh STAT-02; `EnsureCanSubmitExamAsync` oleh TMR-01/03. Urutan commit dalam plan: SAVE-01 (SubmitExam GroupBy) → STAT-01 (SubmitExam early guard) → STAT-02 (AbandonExam) → TMR-01/03 (EnsureCanSubmit) → TOK-02 (SaveAnswer+SubmitExam gate) → TMR-02 (incomplete-gate harden).

Purpose:
- SAVE-01 (SubmitExam): `GroupBy(...).First()` tanpa OrderBy memilih baris arbitrer → push Score bisa beda dari GradingService. Tambah OrderBy di SATU site (Pitfall 1).
- STAT-01 (SubmitExam): early guard L1568 hanya `== "Completed"` → resurrection terbuka. Perluas + audit.
- STAT-02 (AbandonExam): L1257-1267 TOCTOU read-check-then-SaveChanges → ganti atomic ExecuteUpdate ber-guard + ownership (Pitfall 2).
- TMR-01 (EnsureCanSubmit): allowlist L4413-4418 tak mencakup "Standard" → dead code (skip). Balik jadi blocklist (skip hanya Manual/null).
- TMR-03: token di-`TempData.Remove` L4445 SEBELUM grading → retry kehilangan token (Pitfall 3). Tunda konsumsi ke success path.
- TOK-02: SaveAnswer/SubmitExam tak cek StartedAt → bypass lobby token. Tambah gate.
- TMR-02 (D-06): incomplete-gate L1584 percaya client `isAutoSubmit` mentah → utamakan serverTimerExpired.

Output: `Controllers/CMPController.cs` (5 region) + 4 file test xUnit baru (Wave 0, termasuk real-SQL `AbandonGuardTests` pola ProtonCompletionFixture). Migration=false.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/382-grading-lifecycle-cert/382-CONTEXT.md
@.planning/phases/382-grading-lifecycle-cert/382-RESEARCH.md
@.planning/phases/382-grading-lifecycle-cert/382-PATTERNS.md
@.planning/phases/382-grading-lifecycle-cert/382-VALIDATION.md
@.planning/phases/382-grading-lifecycle-cert/382-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Anchors VERIFIED dari source code session ini. Line numbers bisa drift ±5 bila Phase 381 mendarat dulu — cari oleh konten, bukan nomor baris saja. -->

const Abandoned: ditambahkan Plan 01 → `AssessmentConstants.AssessmentStatus.Abandoned == "Abandoned"`. `using S = HcPortal.Models.AssessmentConstants.AssessmentStatus;` boleh.

SaveAnswer (Controllers/CMPController.cs, VERIFIED):
  :348  public async Task<IActionResult> SaveAnswer(int sessionId, int questionId, int optionId)
  :367  if (session.Status == "Completed" || session.Status == "Abandoned" || session.Status == "Cancelled")  // ◄ lifecycle guard SUDAH ada; TOK-02 gate sisip SETELAH owner check, returns Json
        // handler ini return Json(new { success=..., error=... }) — TOK-02 reject pakai Json, BUKAN redirect.

AbandonExam (Controllers/CMPController.cs, VERIFIED):
  :1243 public async Task<IActionResult> AbandonExam(int id)
  :1245 var assessment = FirstOrDefaultAsync(a => a.Id == id);
  :1253 if (assessment.UserId != user.Id) return Forbid();         // ◄ ownership (keep / move into WHERE)
  :1257 if (assessment.Status != "InProgress" && assessment.Status != "Open") { TempData reject; return; }  // ◄ TOCTOU read-check
  :1264 assessment.Status = "Abandoned"; assessment.UpdatedAt=now;  // ◄ change-tracker mutate
  :1267 await SaveChangesAsync();                                    // ◄ TOCTOU save → REPLACE seluruh blok :1256-1270 dgn ExecuteUpdate atomic

SubmitExam (Controllers/CMPController.cs, VERIFIED):
  :1546 public async Task<IActionResult> SubmitExam(int id, [FromForm(Name="answers")] Dictionary<int,int>? answers, bool isAutoSubmit=false, string? autoSubmitToken=null)
  :1568 if (assessment.Status == "Completed") { ... }               // ◄ STAT-01: EXPAND ke terminal set + audit
        // (TOK-02 gate sisip setelah ownership check, sebelum mutasi — returns RedirectToAction("Assessment"))
  :1575-1582 serverTimerExpired computed server-side
  :1584 if (!isAutoSubmit && !serverTimerExpired) { ... }           // ◄ TMR-02: utamakan serverTimerExpired
  :1618 var timerBlockResult = await EnsureCanSubmitExamAsync(assessment, isAutoSubmit, autoSubmitToken, id);
  :1646 .GroupBy(r => r.PackageQuestionId)  ... ToDictionary(g => g.Key, g => g.First())  // ◄ SAVE-01: tambah OrderByDescending(SubmittedAt)

EnsureCanSubmitExamAsync (Controllers/CMPController.cs, VERIFIED L4405-4467):
  :4413-4418 if (AssessmentType != Online && != PreTest && != PostTest) return null;  // ◄ TMR-01: invert → skip hanya Manual/null (enforce Standard)
  :4437 var expectedToken = TempData[tempKey] as string;
  :4445 TempData.Remove(tempKey);  // ◄ TMR-03: JANGAN remove di sini bila pre-grading. Tunda ke success path SubmitExam.
  :4450 if (elapsedSec >= allowedSec && !serverApprovedAutoSubmit) { WriteSubmitBlockedAuditAsync(...); reject; }  // ◄ Tier-1; reuse audit
  :4460 if (elapsedSec >= graceLimitSec) { reject; }  // ◄ Tier-2
  WriteSubmitBlockedAuditAsync (:4473) — reuse verbatim untuk STAT-01 audit juga.

AssessmentType consts (Models/AssessmentConstants.cs): Manual="Manual", Online="Online", PreTest="PreTest", PostTest="PostTest". "Standard" = AssessmentType Normal (string literal "Standard" di data; bukan const di allowlist — itulah dead-code-nya).

ProtonCompletionFixture (HcPortal.Tests/ProtonCompletionServiceTests.cs:25-61) — copy shape untuk AbandonGuardTests real-SQL: IAsyncLifetime, DbName=$"HcPortalDB_Test_{Guid.NewGuid():N}", conn `Server=localhost\\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true`, InitializeAsync MigrateAsync, DisposeAsync EnsureDeletedAsync. `[Trait("Category","Integration")]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 4 test files: AbandonGuard (real-SQL) + EnsureCanSubmitStandard + AutoSubmitTokenRetry + TokenGate (RED)</name>
  <files>HcPortal.Tests/AbandonGuardTests.cs, HcPortal.Tests/EnsureCanSubmitStandardTests.cs, HcPortal.Tests/AutoSubmitTokenRetryTests.cs, HcPortal.Tests/TokenGateTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonCompletionServiceTests.cs L25-61 (fixture shape real-SQL — COPY verbatim untuk AbandonGuardTests)
    - HcPortal.Tests/ProtonCompletionMissTests.cs L43 (GUID-unique entity ids per fact pattern)
    - Controllers/CMPController.cs L1243-1271 (AbandonExam) + L4405-4467 (EnsureCanSubmitExamAsync) + L348-417 (SaveAnswer) + L1546-1660 (SubmitExam)
    - 382-PATTERNS.md §NEW xUnit integration fixtures + §AbandonExam + §EnsureCanSubmitExamAsync
    - 382-RESEARCH.md Pattern 4/5/6 + Pitfall 2/3 + §Validation test map
  </read_first>
  <behavior>
    - AbandonGuard (real-SQL, [Trait Integration]) Test A: sesi Status=Completed → panggil logika abandon (ExecuteUpdate ber-guard) → rowsAffected==0, status DB tetap Completed. RED (saat ini TOCTOU bisa menimpa). Test B (ownership): sesi InProgress milik user X, abandon dengan user Y → rowsAffected==0 (WHERE UserId menolak). Test C (happy): InProgress milik X, abandon oleh X → rowsAffected==1, Status=Abandoned.
    - EnsureCanSubmitStandard (unit) Test A: AssessmentType="Standard", StartedAt mundur (elapsed>allowed), tanpa token sah → return non-null (reject) + audit ter-tulis. RED (saat ini Standard di-skip → return null). Test B: "Standard" on-time (elapsed<allowed) → return null (pass). Test C: "Manual" elapsed>allowed → return null (tetap skip).
    - AutoSubmitTokenRetry (unit): token valid + grading di-simulasi gagal sekali → token MASIH tersedia untuk retry (tidak dikonsumsi sebelum grading commit). RED jika TempData.Remove masih di EnsureCanSubmit pre-grading. (Bila sulit memodelkan grading-fail tanpa heavy infra, assert minimal: EnsureCanSubmit yang lolos TIDAK menghapus token; token dihapus hanya pada success path — sesuaikan assertion ke struktur fix Task 4.)
    - TokenGate (unit/integration) Test A: SaveAnswer sesi IsTokenRequired=true && StartedAt==null → response reject (success=false). Test B: SubmitExam idem → redirect/reject (tidak grading). Test C: IsTokenRequired=false → tidak ter-gate (lolos ke logika berikut).
  </behavior>
  <action>
**AbandonGuardTests.cs** — copy `ProtonCompletionFixture` shape (real SQL Server, disposable GUID DB). `[Trait("Category","Integration")]`. Seed via fixture context: AssessmentSession dengan UserId GUID-unique. Test A/B/C sesuai behavior. Catatan: test boleh memanggil ExecuteUpdate-pattern langsung terhadap context (mirror fix Task 3) ATAU memanggil controller action bila feasible; pilih level yang stabil. Real-SQL WAJIB karena race/atomic guard tak ditegakkan InMemory.

**EnsureCanSubmitStandardTests.cs** — unit. `EnsureCanSubmitExamAsync` private → uji via reflection ATAU (lebih baik) via SubmitExam path / extract-testable bila perlu. Bila reflection: panggil method, seed AssessmentSession AssessmentType="Standard"/StartedAt mundur/DurationMinutes kecil. Assert reject untuk Standard-telat, pass untuk Standard-on-time & Manual-telat. (Jika private tak praktis di-test langsung, dokumentasikan dan uji lewat SubmitExam dengan TestServer/controller-instantiation pola test existing — grep `EnsureCanSubmit` atau `SubmitExam` di HcPortal.Tests untuk precedent.)

**AutoSubmitTokenRetryTests.cs** — unit. Assert token tidak dikonsumsi pre-grading (lihat behavior). Sesuaikan assertion ke bentuk fix Task 4 (consume on success path).

**TokenGateTests.cs** — unit/integration. SaveAnswer + SubmitExam gate StartedAt==null pada IsTokenRequired. Assert reject. IsTokenRequired=false tidak ter-gate.

Semua RED-relevan harus FAIL sebelum task fix berikutnya. Untuk test yang butuh real-SQL ([Trait Integration]) catat di SUMMARY bahwa run lokal butuh SQLEXPRESS + (untuk e2e nanti) `--workers=1`. Jika konstruksi controller/service butuh banyak dependency, mirror cara test existing di repo (grep precedent dulu).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~EnsureCanSubmitStandard|FullyQualifiedName~TokenGate|FullyQualifiedName~AutoSubmitTokenRetry" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist: `AbandonGuardTests.cs`, `EnsureCanSubmitStandardTests.cs`, `AutoSubmitTokenRetryTests.cs`, `TokenGateTests.cs`
    - `AbandonGuardTests.cs` contains `[Trait("Category", "Integration")]` and a GUID-suffixed `DbName`
    - `EnsureCanSubmitStandardTests.cs` contains a Standard-late-reject test and a Standard-on-time-pass test
    - `TokenGateTests.cs` contains SaveAnswer and SubmitExam StartedAt==null gate tests
    - Before fixes: `dotnet test --filter "EnsureCanSubmitStandard|TokenGate"` shows the relevant RED facts FAILING
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>4 Wave-0 test files compile; Standard-enforce, token-gate, token-retry, abandon-guard RED facts fail (bugs confirmed).</done>
</task>

<task type="auto">
  <name>Task 2: SAVE-01 (SubmitExam OrderBy) + STAT-01 (SubmitExam early guard + audit) + TMR-02 (incomplete-gate harden)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs L1546-1660 (SubmitExam: early guard 1568, serverTimerExpired 1575-1584, GroupBy 1646)
    - Controllers/CMPController.cs L4473-4516 (WriteSubmitBlockedAuditAsync — reuse pola audit)
    - 382-PATTERNS.md §SubmitExam
    - 382-RESEARCH.md Pattern 1/3 + Pitfall 1
  </read_first>
  <action>
Tambah `using S = AssessmentConstants.AssessmentStatus;` (atau alias lokal) jika belum.

**SAVE-01 (L1646):** GANTI
```csharp
.GroupBy(r => r.PackageQuestionId).ToDictionary(g => g.Key, g => g.First());
```
menjadi:
```csharp
.GroupBy(r => r.PackageQuestionId).ToDictionary(g => g.Key, g => g.OrderByDescending(r => r.SubmittedAt).First());
```
(SATU site — agar push Score == GradingService Score, Pitfall 1.)

**STAT-01 (L1568):** GANTI `if (assessment.Status == "Completed") { ... }` menjadi guard terminal set + audit + BI message:
```csharp
if (assessment.Status == S.Completed || assessment.Status == S.Abandoned
    || assessment.Status == S.Cancelled || assessment.Status == S.PendingGrading)
{
    try { await WriteSubmitBlockedAuditAsync(assessment, TimeSpan.Zero, 0); }
    catch (Exception ex) { _logger.LogWarning(ex, "audit_drop SubmitExamBlocked resurrection sessionId={Id}", assessment.Id); }
    TempData["Error"] = "Sesi ujian ini sudah berakhir dan tidak dapat dikirim ulang.";
    return RedirectToAction("Assessment");
}
```
(Sesuaikan signature `WriteSubmitBlockedAuditAsync` ke yang ada — read first; jika argumen berbeda, panggil dengan nilai wajar/0. Pertahankan perilaku redirect existing untuk pesan.)

**TMR-02 (L1584):** gate incomplete `if (!isAutoSubmit && !serverTimerExpired)` — utamakan `serverTimerExpired` (server-computed L1575-1582) sebagai otoritas. Jangan biarkan client `isAutoSubmit` jadi satu-satunya jalan lolos: ubah logika agar bypass incomplete-gate hanya ketika `serverTimerExpired` true (auto-submit sah karena waktu habis), client `isAutoSubmit` saja tidak cukup. Pertahankan jalur auto-submit on-time yang sah (D-05) — yang penting bukan reject on-time, melainkan client-flag mentah tak boleh jadi satu-satunya bypass. Komentari keputusan (BI/EN) singkat di kode.

JANGAN sentuh AbandonExam/EnsureCanSubmit/SaveAnswer di task ini (task lain).
  </action>
  <verify>
    <automated>dotnet build --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/CMPController.cs` SubmitExam GroupBy contains `OrderByDescending(r => r.SubmittedAt)`
    - SubmitExam early guard contains `S.Abandoned`, `S.Cancelled`, `S.PendingGrading` and calls the audit helper + `RedirectToAction("Assessment")`
    - SubmitExam incomplete-gate references `serverTimerExpired` as the authoritative bypass (client `isAutoSubmit` not the sole gate)
    - `dotnet build` exits 0; `git diff --name-only` shows no `Migrations/`
  </acceptance_criteria>
  <done>SubmitExam reads final answer (push==stored), rejects resurrection with audit, and incomplete-gate trusts server timer; build green; no migration.</done>
</task>

<task type="auto">
  <name>Task 3: STAT-02 — AbandonExam atomic guarded ExecuteUpdate (ownership in WHERE) + AbandonGuard GREEN</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs L1243-1271 (AbandonExam full)
    - Services/GradingService.cs L237-255 (ExecuteUpdate + rowsAffected precedent)
    - 382-PATTERNS.md §AbandonExam + Pitfall 2 (ownership)
    - 382-RESEARCH.md Pattern 4
  </read_first>
  <action>
Di `AbandonExam` (L1243-1271), GANTI blok TOCTOU L1256-1270 (read-check L1257 + change-tracker mutate L1264 + `SaveChangesAsync` L1267) dengan SATU atomic guarded UPDATE. Pertahankan ownership di WHERE (Pitfall 2 — jangan hilang). Boleh tetap menyimpan early-load + `Forbid()` (L1253) untuk 403 semantics, lalu ExecuteUpdate dengan UserId di WHERE untuk atomicity:

```csharp
using S = AssessmentConstants.AssessmentStatus;
var rowsAffected = await _context.AssessmentSessions
    .Where(a => a.Id == id && a.UserId == user.Id
        && (a.Status == S.InProgress || a.Status == S.Open))
    .ExecuteUpdateAsync(a => a
        .SetProperty(x => x.Status, S.Abandoned)
        .SetProperty(x => x.UpdatedAt, DateTime.UtcNow));

if (rowsAffected == 0)
{
    TempData["Error"] = "Sesi ujian ini tidak dapat dibatalkan karena sudah selesai atau dinilai.";
    return RedirectToAction("Assessment");
}

TempData["Info"] = "Ujian telah dibatalkan. Hubungi HC jika Anda ingin mengulang.";
return RedirectToAction("Assessment");
```
Pakai const `S.Abandoned` (sudah ada dari Plan 01). Hapus assignment change-tracker + `SaveChangesAsync` lama. Pertahankan `if (assessment == null) return NotFound();` (L1248) dan owner `Forbid()` early (L1253) bila ingin 403; WHERE UserId menjaga atomicity. Setelah fix, AbandonGuard Test A/B/C pass.
  </action>
  <verify>
    <automated>dotnet build --nologo; dotnet test HcPortal.Tests --filter "FullyQualifiedName~AbandonGuard" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AbandonExam` contains `.ExecuteUpdateAsync(` with a WHERE including `a.UserId == user.Id` and `(a.Status == S.InProgress || a.Status == S.Open)`
    - `AbandonExam` no longer mutates via `assessment.Status = "Abandoned"` + `SaveChangesAsync()` (TOCTOU removed)
    - `AbandonExam` has `if (rowsAffected == 0)` reject branch
    - `dotnet test --filter AbandonGuard` exits 0 (real-SQL: Completed→rowsAffected==0 status preserved; ownership negative; happy path)
    - `dotnet build` exits 0; no `Migrations/` diff
  </acceptance_criteria>
  <done>AbandonExam is single atomic guarded UPDATE with ownership in WHERE; graded verdict preserved on late abandon; AbandonGuard green.</done>
</task>

<task type="auto">
  <name>Task 4: TMR-01 allowlist inversion + TMR-03 token-not-consumed-pre-commit + EnsureCanSubmitStandard/AutoSubmitTokenRetry GREEN</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs L4405-4467 (EnsureCanSubmitExamAsync) + L1618-1660 (SubmitExam call site + grading commit) + L4473-4516 (audit)
    - 382-PATTERNS.md §EnsureCanSubmitExamAsync + Pitfall 3
    - 382-RESEARCH.md Pattern 5 + Pitfall 3 + D-05 (on-time auto-submit must reach grading)
  </read_first>
  <action>
**TMR-01 inversion (L4413-4418):** allowlist saat ini `if (AssessmentType != Online && != PreTest && != PostTest) return null;` membuat "Standard" lolos ke `return null` (skip). Balik ke blocklist — skip HANYA Manual/null:
```csharp
if (assessment.AssessmentType == AssessmentConstants.AssessmentType.Manual
    || string.IsNullOrEmpty(assessment.AssessmentType))
{
    return null; // skip guard — hanya Manual/null
}
// Standard / Online / PreTest / PostTest → enforce tier-1/tier-2 di bawah (logika existing tak berubah)
```
Legacy-session skip `if (!assessment.StartedAt.HasValue) return null;` (L4421) BIARKAN.

**TMR-03 token-not-consumed-pre-commit (Pitfall 3):** saat ini `TempData.Remove(tempKey)` L4445 menghapus token SEBELUM grading commit di SubmitExam. Ubah agar token DIVALIDASI tanpa langsung di-remove di sini; konsumsi (remove) HANYA pada success path SETELAH `GradeAndCompleteAsync` mengembalikan sukses di `SubmitExam`. Opsi implementasi:
1. Di `EnsureCanSubmitExamAsync`: validasi token (set `serverApprovedAutoSubmit`) TANPA `TempData.Remove`. Hapus baris L4445 `TempData.Remove(tempKey)`.
2. Di `SubmitExam`: setelah grading commit sukses (setelah call yang melakukan GradeAndCompleteAsync / status jadi Completed), baru `TempData.Remove($"AutoSubmitToken_{id}")`.
ATAU andalkan idempotency grading (rowsAffected==0 = already done) + biarkan TempData expire natural — namun pendekatan eksplisit (remove on success) lebih jelas; pilih dan dokumentasikan. KRITIS (D-05): jalur on-time auto-submit (`serverApprovedAutoSubmit==true`) HARUS tetap sampai grading — JANGAN reject. Yang berubah hanya MOMENT konsumsi token.

Reuse `WriteSubmitBlockedAuditAsync` (L4452) apa adanya untuk Tier-1 reject. Setelah fix, EnsureCanSubmitStandard + AutoSubmitTokenRetry pass.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~EnsureCanSubmitStandard|FullyQualifiedName~AutoSubmitTokenRetry" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `EnsureCanSubmitExamAsync` skip-guard uses `AssessmentConstants.AssessmentType.Manual` / `string.IsNullOrEmpty(assessment.AssessmentType)` (blocklist) — Online/PreTest/PostTest allowlist removed
    - `TempData.Remove(tempKey)` no longer fires inside `EnsureCanSubmitExamAsync` before grading; token consume moved to SubmitExam success path (or documented idempotency approach)
    - `dotnet test --filter "EnsureCanSubmitStandard|AutoSubmitTokenRetry"` exits 0 (Standard-late reject + audit; Standard-on-time pass; Manual-late skip; token-retry safe)
    - `dotnet build` exits 0; no `Migrations/` diff
  </acceptance_criteria>
  <done>Standard timer enforced (skip only Manual/null); on-time auto-submit still grades; token consumed only on grading success; facts green.</done>
</task>

<task type="auto">
  <name>Task 5: TOK-02 StartedAt-gate in SaveAnswer (Json) + SubmitExam (redirect) + TokenGate GREEN</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs L348-417 (SaveAnswer — owner check + lifecycle guard L367; handler returns Json)
    - Controllers/CMPController.cs L1546-1575 (SubmitExam — ownership + early guard region)
    - 382-PATTERNS.md §SaveAnswer + §SubmitExam (TOK-02 gate)
    - 382-RESEARCH.md Pattern 6 + §Security V4
  </read_first>
  <action>
Tambah gate TOK-02 di KEDUA handler, SETELAH ownership/lifecycle check yang ada, SEBELUM mutasi apa pun.

**SaveAnswer** (setelah owner check, dekat L367 — handler ini return Json):
```csharp
if (session.IsTokenRequired && session.StartedAt == null)
    return Json(new { success = false, error = "Ujian belum dimulai. Masukkan token melalui halaman ujian." });
```

**SubmitExam** (setelah ownership check, sebelum STAT-01 guard / mutasi — handler return redirect):
```csharp
if (assessment.IsTokenRequired && assessment.StartedAt == null)
{
    TempData["Error"] = "Ujian belum dimulai. Masukkan token melalui halaman ujian.";
    return RedirectToAction("Assessment");
}
```
Gate ini = proxy "sudah lewat lobby token" (StartExam set StartedAt hanya setelah VerifyToken). IsTokenRequired=false → tidak ter-gate. JANGAN ubah StartExam lobby. Setelah fix, TokenGate Test A/B/C pass.

Konfirmasi koherensi urutan dalam SubmitExam (audit): TOK-02 gate diletakkan bersama STAT-01 guard di awal handler, sebelum mutasi — satu edit koheren, bukan sub-agent terpisah.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~TokenGate" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `SaveAnswer` contains `session.IsTokenRequired && session.StartedAt == null` returning Json `success = false`
    - `SubmitExam` contains `assessment.IsTokenRequired && assessment.StartedAt == null` returning `RedirectToAction("Assessment")`
    - `dotnet test --filter TokenGate` exits 0 (SaveAnswer + SubmitExam gate; IsTokenRequired=false not gated)
    - `dotnet build` exits 0; no `Migrations/` diff
    - Full xUnit: `dotnet test HcPortal.Tests --nologo` exits 0 (no regression)
  </acceptance_criteria>
  <done>Token-required sessions reject SaveAnswer/SubmitExam when StartedAt==null; non-token sessions unaffected; TokenGate green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker/HC browser → CMPController POST (SaveAnswer/SubmitExam/AbandonExam) | Authenticated requests mutate session lifecycle, answers, verdict |
| client-supplied flags (isAutoSubmit, autoSubmitToken) → SubmitExam | Untrusted client hints; server must be authoritative |
| concurrent requests → AssessmentSession row | Race between Abandon and grading; must be atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-382-04 | Tampering / Elevation (resurrection via SubmitExam) | SubmitExam early guard only `== "Completed"` → Abandoned/Cancelled POST → Completed-lulus + cert | mitigate | STAT-01 expand guard to terminal set + audit `SubmitExamBlocked` (Task 2) |
| T-382-05 | Tampering (overwrite graded via late abandon, TOCTOU) | AbandonExam read-check-then-SaveChanges (L1257-1267) → another request completes between read & save | mitigate | STAT-02 atomic `ExecuteUpdateAsync` WHERE (InProgress\|\|Open) + rowsAffected==0 reject (Task 3) |
| T-382-06 | Spoofing (abandon other worker's session) | ExecuteUpdate without UserId in WHERE | mitigate | Pitfall 2: WHERE includes `a.UserId == user.Id` (Task 3) |
| T-382-07 | Tampering (timer bypass, Standard) | EnsureCanSubmit allowlist excludes Standard → no enforcement | mitigate | TMR-01 invert to blocklist (skip only Manual/null) + Tier-1/2 + audit (Task 4) |
| T-382-08 | Tampering (spoofed isAutoSubmit) | incomplete-gate trusts raw client `isAutoSubmit` | mitigate | TMR-02 (D-06): serverTimerExpired authoritative (Task 2) |
| T-382-09 | Elevation (proctoring/token bypass) | SaveAnswer/SubmitExam never check StartedAt → bypass lobby token | mitigate | TOK-02 StartedAt-gate both handlers (Task 5) |
| T-382-10 | DoS (token one-shot loss on retry) | token consumed before grading commit → retry permanent reject | mitigate | TMR-03: consume token only on grading success (Task 4) |

No HIGH-severity unmitigated threats remain. V7 audit (SubmitExamBlocked) reused try/catch-swallow (does not block primary action).
</threat_model>

<verification>
- `dotnet build` exits 0.
- `dotnet test HcPortal.Tests --nologo` exits 0 (full xUnit incl. AbandonGuard[real-SQL], EnsureCanSubmitStandard, AutoSubmitTokenRetry, TokenGate, plus Plan 01 GradingDedupe/SubmitResurrection).
- SubmitExam: OrderBy added (push==stored), terminal-set guard + audit, server-timer-authoritative incomplete-gate.
- AbandonExam: atomic ExecuteUpdate WHERE (UserId && (InProgress||Open)) + rowsAffected reject.
- EnsureCanSubmit: skip only Manual/null; token consumed on success only.
- SaveAnswer/SubmitExam: StartedAt token-gate.
- NO migration: `git diff --name-only` shows no `Migrations/*` and no `*ModelSnapshot.cs`.
</verification>

<success_criteria>
- WSE-06 (SubmitExam half): push Score == GradingService Score (final-write-wins OrderBy).
- WSE-07 (SubmitExam half): resurrection rejected + audited.
- WSE-08: graded verdict not overwritten by late abandon (atomic guard, ownership preserved).
- WSE-09: Standard timer enforced; on-time auto-submit still grades; token retry-safe.
- WSE-10: token gate at save/submit.
- migration=false confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/382-grading-lifecycle-cert/382-02-SUMMARY.md`.
</output>
