---
phase: 82-cleanup-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/ProtonProgress.cshtml
  - Views/CDP/CoachingProton.cshtml
  - Views/CDP/Shared/_ProtonProgressPartial.cshtml
  - Views/CDP/Shared/_CoachingProtonPartial.cshtml
  - Views/CDP/Dashboard.cshtml
  - Views/CDP/Index.cshtml
autonomous: true
requirements:
  - CLN-01

must_haves:
  truths:
    - "Every display-facing text that said 'Proton Progress' now reads 'Coaching Proton'"
    - "The URL for the progress page is /CDP/CoachingProton (old /CDP/ProtonProgress returns 404)"
    - "The CDP hub card and button both say 'Coaching Proton'"
    - "The CDP Dashboard tab label reads 'Coaching Proton'"
    - "The Excel worksheet and PDF header both say 'Coaching Proton'"
  artifacts:
    - path: "Views/CDP/CoachingProton.cshtml"
      provides: "Renamed view file"
    - path: "Views/CDP/Shared/_CoachingProtonPartial.cshtml"
      provides: "Renamed partial file"
    - path: "Controllers/CDPController.cs"
      provides: "Renamed action and updated references"
      contains: "public async Task<IActionResult> CoachingProton("
  key_links:
    - from: "Views/CDP/Index.cshtml"
      to: "CDPController.CoachingProton"
      via: "Url.Action('CoachingProton', 'CDP')"
    - from: "Views/CDP/Dashboard.cshtml"
      to: "Views/CDP/Shared/_CoachingProtonPartial.cshtml"
      via: "partial name='Shared/_CoachingProtonPartial'"
---

<objective>
Rename "Proton Progress" to "Coaching Proton" throughout the CDP feature — controller action, view file, partial file, all display text, form targets, pagination links, redirect calls, Excel worksheet name, and PDF header.

Purpose: Establishes the correct terminology before QA testing in later phases.
Output: /CDP/CoachingProton URL is live; old /CDP/ProtonProgress returns 404; all user-visible text says "Coaching Proton".
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-cleanup-rename/82-CONTEXT.md

<interfaces>
<!-- Key references extracted from codebase. Use these directly. -->

From Controllers/CDPController.cs (lines to rename/update):
- Line ~1011: `public async Task<IActionResult> ProtonProgress(` → rename to `CoachingProton`
- Lines ~916, ~926: `return RedirectToAction("ProtonProgress")` → `RedirectToAction("CoachingProton")`
- Line ~1744: `workbook.Worksheets.Add("Proton Progress")` → `"Coaching Proton"`
- Line ~1832: `col.Item().Text($"Proton Progress — {coacheeName}")` → `"Coaching Proton — {coacheeName}"`
- Internal method `BuildProtonProgressSubModelAsync` and class `ProtonProgressSubModel` — per decisions, leave internal code names unchanged (display-facing only)
- Property `model.ProtonProgressData` (line ~148) — leave unchanged (internal/programmatic)

From Views/CDP/ProtonProgress.cshtml (all Url.Action calls and display text):
- Line 3: `ViewData["Title"] = "Proton Progress"` → `"Coaching Proton"`
- Line 70: `<h2>...<i...>Proton Progress</h2>` → `Coaching Proton`
- Line 79: `asp-action="ProtonProgress"` → `asp-action="CoachingProton"`
- Lines 632, 655, 675, 690, 702: `Url.Action("ProtonProgress", "CDP", ...)` → `"CoachingProton"`
- Line 982: `Url.Action("ProtonProgress", "CDP")` → `"CoachingProton"`

From Views/CDP/Dashboard.cshtml:
- Line 19: `Proton Progress` tab label → `Coaching Proton`
- Line 42: `partial name="Shared/_ProtonProgressPartial"` → `"Shared/_CoachingProtonPartial"`

From Views/CDP/Index.cshtml:
- Line 46: `<h5 class="mb-0">Proton Progress</h5>` → `Coaching Proton`
- Line 51: `Url.Action("ProtonProgress", "CDP")` → `"CoachingProton"`
- Line 52: `<i...>Proton Progress` button text → `Coaching Proton`

From Views/CDP/Shared/_ProtonProgressPartial.cshtml:
- Line 1: `@model HcPortal.Models.ProtonProgressSubModel` — keep (model class name unchanged per decisions)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename CDPController action and update all CDPController references</name>
  <files>Controllers/CDPController.cs</files>
  <action>
In Controllers/CDPController.cs, make the following targeted changes:

1. Rename the action method signature (display-facing route rename):
   - Find: `public async Task<IActionResult> ProtonProgress(`
   - Replace with: `public async Task<IActionResult> CoachingProton(`

2. Update the two RedirectToAction calls that target the old action name:
   - Find: `return RedirectToAction("ProtonProgress");` (appears twice, around lines 916 and 926)
   - Replace with: `return RedirectToAction("CoachingProton");`

3. Update the Excel worksheet name (user-visible string):
   - Find: `workbook.Worksheets.Add("Proton Progress")`
   - Replace with: `workbook.Worksheets.Add("Coaching Proton")`

4. Update the PDF header text (user-visible string):
   - Find: `col.Item().Text($"Proton Progress — {coacheeName}")`
   - Replace with: `col.Item().Text($"Coaching Proton — {coacheeName}")`

Do NOT rename: `BuildProtonProgressSubModelAsync`, `ProtonProgressSubModel`, or `ProtonProgressData` — these are internal/programmatic identifiers (per user decision: rename display-facing only).
  </action>
  <verify>
    <automated>grep -n "\"ProtonProgress\"\|Action.*ProtonProgress\|Add.*\"Proton Progress\"\|Text.*\"Proton Progress" Controllers/CDPController.cs | grep -v "BuildProtonProgressSubModelAsync\|ProtonProgressSubModel\|ProtonProgressData\|// " | head -20</automated>
  </verify>
  <done>No remaining "ProtonProgress" string literals or "Proton Progress" display strings in CDPController.cs (only internal method/class names remain)</done>
</task>

<task type="auto">
  <name>Task 2: Rename view files and update all display text in views</name>
  <files>
    Views/CDP/CoachingProton.cshtml
    Views/CDP/Shared/_CoachingProtonPartial.cshtml
    Views/CDP/Dashboard.cshtml
    Views/CDP/Index.cshtml
  </files>
  <action>
Step A — Rename view files by creating new files then removing old ones:

1. Copy `Views/CDP/ProtonProgress.cshtml` → `Views/CDP/CoachingProton.cshtml` (new file with all content), then update inside the new file:
   - Line 3: `ViewData["Title"] = "Proton Progress"` → `ViewData["Title"] = "Coaching Proton"`
   - Line 70: h2 inner text `Proton Progress` → `Coaching Proton`
   - Line 79: `asp-action="ProtonProgress"` → `asp-action="CoachingProton"`
   - All `Url.Action("ProtonProgress", "CDP", ...)` calls (lines 632, 655, 675, 690, 702, 982) → `Url.Action("CoachingProton", "CDP", ...)`
   Then delete `Views/CDP/ProtonProgress.cshtml`.

2. Copy `Views/CDP/Shared/_ProtonProgressPartial.cshtml` → `Views/CDP/Shared/_CoachingProtonPartial.cshtml`. The content is the same (the @model line references ProtonProgressSubModel which stays unchanged per decisions). Then delete `Views/CDP/Shared/_ProtonProgressPartial.cshtml`.

Step B — Update hub and dashboard views:

3. In `Views/CDP/Dashboard.cshtml`:
   - Tab label text `Proton Progress` → `Coaching Proton`
   - `partial name="Shared/_ProtonProgressPartial"` → `partial name="Shared/_CoachingProtonPartial"`

4. In `Views/CDP/Index.cshtml`:
   - Card heading `<h5 class="mb-0">Proton Progress</h5>` → `Coaching Proton`
   - `Url.Action("ProtonProgress", "CDP")` → `Url.Action("CoachingProton", "CDP")`
   - Button text `<i...>Proton Progress` → `Coaching Proton`
  </action>
  <verify>
    <automated>grep -rn "ProtonProgress\|Proton Progress" Views/CDP/ | grep -v "_CoachingProtonPartial\|CoachingProton\|ProtonProgressSubModel\|ProtonProgressData\|model HcPortal" | head -20</automated>
  </verify>
  <done>
    - Views/CDP/CoachingProton.cshtml exists; Views/CDP/ProtonProgress.cshtml does not exist
    - Views/CDP/Shared/_CoachingProtonPartial.cshtml exists; Views/CDP/Shared/_ProtonProgressPartial.cshtml does not exist
    - No remaining "Proton Progress" display text in Views/CDP/ (only internal model references permitted)
    - Dashboard tab and Index card both display "Coaching Proton"
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full rename is consistent:

```bash
# Should return zero results (no display-facing ProtonProgress strings remain)
grep -rn "\"ProtonProgress\"\|>Proton Progress\|asp-action=\"ProtonProgress\"\|Url\.Action.*\"ProtonProgress\"\|Add.*\"Proton Progress\"\|Text.*\"Proton Progress" Controllers/CDPController.cs Views/CDP/ | grep -v "ProtonProgressSubModel\|ProtonProgressData\|BuildProtonProgressSubModelAsync\|model HcPortal.Models.ProtonProgressSubModel"

# Should confirm old files are gone
ls Views/CDP/ProtonProgress.cshtml 2>/dev/null && echo "FAIL: old view still exists" || echo "OK: old view removed"
ls Views/CDP/Shared/_ProtonProgressPartial.cshtml 2>/dev/null && echo "FAIL: old partial still exists" || echo "OK: old partial removed"

# Should confirm new action exists
grep -n "public async Task<IActionResult> CoachingProton(" Controllers/CDPController.cs
```
</verification>

<success_criteria>
1. GET /CDP/CoachingProton resolves (action exists and view file exists)
2. GET /CDP/ProtonProgress returns 404 (action renamed, no view)
3. All display-facing text in views and controller exports says "Coaching Proton"
4. Internal code names (ProtonProgressSubModel, BuildProtonProgressSubModelAsync, ProtonProgressData) are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/82-cleanup-rename/82-01-SUMMARY.md`
</output>
