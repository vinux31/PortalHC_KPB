---
phase: 83-master-data-qa
plan: 08
type: execute
wave: 3
depends_on: [83-06]
files_modified:
  - Views/Admin/ManageWorkers.cshtml
  - Controllers/AdminController.cs
  - Views/Admin/ImportWorkers.cshtml
autonomous: true
requirements: [DATA-05, DATA-06]
gap_closure: true

must_haves:
  truths:
    - "ManageWorkers view has Tampilkan Inactive checkbox that reloads list with showInactive=true"
    - "Active workers show Nonaktifkan button; inactive workers show Aktifkan Kembali button with greyed styling"
    - "ExportWorkers includes showInactive parameter — when showInactive toggle is active, export includes inactive workers with Status column"
    - "ImportWorkers detects existing inactive users by email match and shows them as 'Perlu Review' with Reaktivasi button"
  artifacts:
    - path: "Views/Admin/ManageWorkers.cshtml"
      provides: "Tampilkan Inactive toggle, Nonaktifkan/Aktifkan Kembali conditional buttons, inactive row styling"
    - path: "Controllers/AdminController.cs"
      provides: "ExportWorkers with showInactive param + Status column; ImportWorkers detects inactive email matches"
  key_links:
    - from: "Views/Admin/ManageWorkers.cshtml Tampilkan Inactive checkbox"
      to: "/Admin/ManageWorkers?showInactive=true"
      via: "form GET or JS link with showInactive query param"
      pattern: "showInactive"
    - from: "Views/Admin/ManageWorkers.cshtml Nonaktifkan button"
      to: "/Admin/DeactivateWorker POST"
      via: "form with AntiForgeryToken"
      pattern: "DeactivateWorker"
    - from: "Views/Admin/ManageWorkers.cshtml Aktifkan Kembali button"
      to: "/Admin/ReactivateWorker POST"
      via: "form with AntiForgeryToken"
      pattern: "ReactivateWorker"
    - from: "Views/Admin/ManageWorkers.cshtml Export button"
      to: "/Admin/ExportWorkers?showInactive=..."
      via: "GET anchor link including showInactive param"
      pattern: "ExportWorkers.*showInactive"
---

<objective>
Build the Worker soft delete UI in ManageWorkers.cshtml (toggle, buttons, inactive styling) and update ExportWorkers to include inactive workers and Status column when toggle is active. Update ImportWorkers to detect inactive email matches and present a review list with Reaktivasi button.

Purpose: The backend (Plan 83-06) is complete — this plan wires the UI so Admin/HC can actually use the soft delete workflow from the browser.
Output: ManageWorkers view with full soft delete controls; Export and Import updated for inactive handling.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/83-master-data-qa/83-CONTEXT.md
@.planning/phases/83-master-data-qa/83-06-SUMMARY.md

<interfaces>
<!-- ManageWorkers.cshtml context -->
Model: List&lt;ApplicationUser&gt;
ViewBag.ShowInactive: bool (set by ManageWorkers GET after Plan 83-06)
ViewBag.Search, ViewBag.SectionFilter, ViewBag.RoleFilter: filter values
ViewBag.UserRoles: Dictionary&lt;string, string&gt; mapping userId to role name

Current action buttons area (per-row, ~line 230-245):
```html
<a href="@Url.Action("WorkerDetail", "Admin", new { id = user.Id })" class="btn btn-sm btn-outline-info">...</a>
<a href="@Url.Action("EditWorker", "Admin", new { id = user.Id })" class="btn btn-sm btn-outline-warning">...</a>
<button class="btn btn-sm btn-outline-danger" onclick="confirmDelete('@user.Id', '@user.FullName')" title="Hapus">...</button>
```

Current Export button (header area ~line 36):
```html
<a href="@Url.Action("ExportWorkers", "Admin", new { search = ViewBag.Search, sectionFilter = ViewBag.SectionFilter, roleFilter = ViewBag.RoleFilter })" ...>Export Excel</a>
```

ExportWorkers (AdminController, current signature):
```csharp
public async Task<IActionResult> ExportWorkers(string? search, string? sectionFilter, string? roleFilter)
```

ImportWorkerResult class (existing):
```csharp
public class ImportWorkerResult { public string Nama; public string Email; public string Role; public string Status; public string Message; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ManageWorkers.cshtml with Tampilkan Inactive toggle and soft delete buttons</name>
  <files>Views/Admin/ManageWorkers.cshtml</files>
  <action>
**1. Add Tampilkan Inactive toggle** — add a form/link near the search bar or filter area. Use a simple anchor link approach:

```html
@{
    var showInactive = (bool)(ViewBag.ShowInactive ?? false);
}
```

Add toggle button next to the filter bar (after existing filters):
```html
@if (showInactive)
{
    <a href="@Url.Action("ManageWorkers", "Admin", new { search = ViewBag.Search, sectionFilter = ViewBag.SectionFilter, roleFilter = ViewBag.RoleFilter, showInactive = false })"
       class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-eye-slash me-1"></i>Sembunyikan Inactive
    </a>
}
else
{
    <a href="@Url.Action("ManageWorkers", "Admin", new { search = ViewBag.Search, sectionFilter = ViewBag.SectionFilter, roleFilter = ViewBag.RoleFilter, showInactive = true })"
       class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-eye me-1"></i>Tampilkan Inactive
    </a>
}
```

**2. Update Export button** to include showInactive param:
```html
<a href="@Url.Action("ExportWorkers", "Admin", new { search = ViewBag.Search, sectionFilter = ViewBag.SectionFilter, roleFilter = ViewBag.RoleFilter, showInactive = showInactive })" ...>
```

**3. Replace the per-row Hapus button** with conditional Nonaktifkan/Aktifkan Kembali buttons:

Remove the existing `confirmDelete` button and replace with:
```html
@if (user.IsActive)
{
    <form method="post" action="@Url.Action("DeactivateWorker", "Admin")" style="display:inline;" onsubmit="return confirmDeactivate('@user.FullName.Replace("'", "\\'")');">
        @Html.AntiForgeryToken()
        <input type="hidden" name="id" value="@user.Id" />
        <button type="submit" class="btn btn-sm btn-outline-warning" title="Nonaktifkan">
            <i class="bi bi-person-dash"></i>
        </button>
    </form>
}
else
{
    <form method="post" action="@Url.Action("ReactivateWorker", "Admin")" style="display:inline;">
        @Html.AntiForgeryToken()
        <input type="hidden" name="id" value="@user.Id" />
        <button type="submit" class="btn btn-sm btn-outline-success" title="Aktifkan Kembali">
            <i class="bi bi-person-check"></i>
        </button>
    </form>
}
```

**4. Add inactive row styling** — wrap each table row `<tr>` to add a CSS class for inactive users:
```html
<tr class="@(!user.IsActive ? "table-secondary text-muted" : "")">
```

**5. Add JS confirmation for deactivate:**
```javascript
function confirmDeactivate(userName) {
    return confirm(`Nonaktifkan user "${userName}"?\n\nCoaching aktif akan ditutup dan assessment aktif akan dibatalkan. Tindakan ini dapat dibatalkan dengan Aktifkan Kembali.`);
}
```

**6. Remove the old Delete Modal** (the modal div with id="deleteModal" and the confirmDelete JS function) since it's replaced by the inline form with onsubmit confirm.

Note: The DeleteWorker hard delete action still exists in AdminController — do NOT remove it. We're only changing the UI to use soft delete. Hard delete may still be called programmatically.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "showInactive\|DeactivateWorker\|ReactivateWorker\|Nonaktifkan\|Aktifkan Kembali\|confirmDeactivate" Views/Admin/ManageWorkers.cshtml | head -20</automated>
  </verify>
  <done>ManageWorkers.cshtml has Tampilkan/Sembunyikan Inactive toggle, Nonaktifkan/Aktifkan Kembali conditional buttons, inactive row greying, confirmDeactivate JS</done>
</task>

<task type="auto">
  <name>Task 2: Update ExportWorkers (showInactive + Status column) and ImportWorkers (inactive email match)</name>
  <files>Controllers/AdminController.cs</files>
  <action>
**ExportWorkers updates:**

1. Change signature to accept `showInactive = false`:
```csharp
public async Task<IActionResult> ExportWorkers(string? search, string? sectionFilter, string? roleFilter, bool showInactive = false)
```

2. After existing filters and BEFORE `.ToListAsync()`, add:
```csharp
if (!showInactive)
    query = query.Where(u => u.IsActive);
```

3. When showInactive is true, add a "Status" column to the Excel export. Update the headers array and row writing:
```csharp
// Conditional headers
var headers = showInactive
    ? new[] { "No", "Nama", "Email", "NIP", "Jabatan", "Bagian", "Unit", "Directorate", "Role", "Tgl Bergabung", "Status" }
    : new[] { "No", "Nama", "Email", "NIP", "Jabatan", "Bagian", "Unit", "Directorate", "Role", "Tgl Bergabung" };
```

After writing row 10 (Tgl Bergabung), conditionally write Status:
```csharp
if (showInactive)
    ws.Cell(row, 11).Value = u.IsActive ? "Aktif" : "Tidak Aktif";
```

**ImportWorkers updates:**

The ImportWorkers currently skips existing users (email already registered) with `Status = "Skip"`. Update to detect inactive matches:

Find the block:
```csharp
var existing = await _userManager.FindByEmailAsync(email);
if (existing != null)
{
    result.Status = "Skip";
    result.Message = "Email sudah terdaftar, dilewati";
    results.Add(result);
    continue;
}
```

Replace with:
```csharp
var existing = await _userManager.FindByEmailAsync(email);
if (existing != null)
{
    if (!existing.IsActive)
    {
        result.Status = "PerluReview";
        result.Message = "Email terdaftar tapi tidak aktif — dapat diaktifkan kembali";
        result.ExistingUserId = existing.Id;
    }
    else
    {
        result.Status = "Skip";
        result.Message = "Email sudah terdaftar, dilewati";
    }
    results.Add(result);
    continue;
}
```

Add `ExistingUserId` property to the `ImportWorkerResult` class (find this class in AdminController.cs, it's a nested/local class):
```csharp
public string? ExistingUserId { get; set; }
```

Also update the audit log message to include PerluReview count:
```csharp
var reviewCount = results.Count(r => r.Status == "PerluReview");
// Add reviewCount to the audit log description string
```

In the ImportWorkers.cshtml view, add a "Perlu Review" section showing inactive matches with a Reaktivasi button. Since this plan only touches AdminController.cs, add ViewBag.HasReviewItems = results.Any(r => r.Status == "PerluReview") for the view to detect. The view update for the Reaktivasi UI is in this same plan — update ImportWorkers.cshtml too:

**Add ImportWorkers.cshtml to files for this task:** Update the results table to show a third category "Perlu Review" with a Reaktivasi form button (POST to ReactivateWorker) for PerluReview rows.

In the ImportWorkers.cshtml results table, after Success/Error rows, add:
```html
@foreach (var r in ViewBag.ImportResults as List<HcPortal.Models.ImportWorkerResult>)
{
    if (r.Status == "PerluReview")
    {
        <tr class="table-warning">
            <td>@r.Nama</td>
            <td>@r.Email</td>
            <td><span class="badge bg-warning text-dark">Perlu Review</span></td>
            <td>@r.Message
                <form method="post" action="@Url.Action("ReactivateWorker", "Admin")" style="display:inline;">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" value="@r.ExistingUserId" />
                    <button type="submit" class="btn btn-sm btn-outline-success ms-2">Aktifkan Kembali</button>
                </form>
            </td>
        </tr>
    }
}
```

Check the existing ImportWorkers.cshtml structure first before editing — read the file to see the results table format. Match the existing table column structure.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "showInactive\|PerluReview\|ExistingUserId\|Aktifkan Kembali" Controllers/AdminController.cs | head -15</automated>
  </verify>
  <done>ExportWorkers has showInactive param and conditional Status column; ImportWorkerResult has ExistingUserId; ImportWorkers detects inactive email matches and marks as PerluReview; ImportWorkers.cshtml shows Aktifkan Kembali button for review rows; dotnet build passes</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -n "showInactive\|DeactivateWorker\|ReactivateWorker" Views/Admin/ManageWorkers.cshtml` returns matches
- `grep -n "showInactive\|PerluReview\|ExistingUserId" Controllers/AdminController.cs` returns matches
- `dotnet build` passes with no CS compiler errors
</verification>

<success_criteria>
- ManageWorkers: Tampilkan/Sembunyikan Inactive toggle works; active rows show Nonaktifkan button; inactive rows show Aktifkan Kembali button with greyed styling
- ExportWorkers: when showInactive=true, export includes all users with Status column; when false, only active users exported
- ImportWorkers: inactive email match produces PerluReview status with ExistingUserId; ImportWorkers.cshtml shows Aktifkan Kembali button per review row
</success_criteria>

<output>
After completion, create `.planning/phases/83-master-data-qa/83-08-SUMMARY.md`
</output>
