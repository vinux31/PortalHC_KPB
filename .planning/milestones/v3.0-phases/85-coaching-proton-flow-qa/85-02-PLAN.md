---
phase: 85-coaching-proton-flow-qa
plan: 02
type: execute
wave: 2
depends_on: [85-01]
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/CoachingProton.cshtml
  - Views/CDP/Deliverable.cshtml
autonomous: true
requirements: [COACH-03, COACH-04]

must_haves:
  truths:
    - "Coachee navigating to /CDP/CoachingProton sees only their own deliverables with correct status badges"
    - "Coachee can view a deliverable detail page (/CDP/Deliverable/{id}) showing evidence upload section, status badge, and coaching report (if a CoachingSession is linked)"
    - "Coach navigating to /CDP/CoachingProton sees only their mapped coachees (from CoachCoacheeMapping) and no others"
    - "Coach can select a coachee from the dropdown, load their deliverables, and see the SubmitEvidenceWithCoaching form for Pending/Rejected deliverables"
    - "SubmitEvidenceWithCoaching POST saves a CoachingSession record linked to the selected progress IDs and optionally saves an evidence file"
    - "Evidence file for an Approved record is downloadable via EvidencePath in the Deliverable view"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "CoachingProton, Deliverable, SubmitEvidenceWithCoaching actions reviewed and bug-free"
    - path: "Views/CDP/CoachingProton.cshtml"
      provides: "Role-scoped coachee tracking table, approval buttons, filter controls"
    - path: "Views/CDP/Deliverable.cshtml"
      provides: "Evidence upload form (Coach only), status display, coaching session history"
  key_links:
    - from: "Views/CDP/CoachingProton.cshtml"
      to: "CDPController.SubmitEvidenceWithCoaching"
      via: "AJAX POST with multipart/form-data from coaching modal — Coach role only"
    - from: "Views/CDP/Deliverable.cshtml"
      to: "CDPController.UploadEvidence"
      via: "POST form with evidenceFile — Coach role only (RoleLevel==5)"
    - from: "CoachingProton coachee dropdown"
      to: "CDPController.GetCoacheeDeliverables"
      via: "AJAX GET returning deliverable list for selected coacheeId"
---

<objective>
Code review of CDPController CoachingProton, Deliverable, and SubmitEvidenceWithCoaching actions, plus targeted review of CoachingProton.cshtml (76KB) focused on approval buttons, filter logic, and pagination.

Purpose: COACH-03 (coachee progress view) and COACH-04 (coach evidence upload) require the main tracking page and deliverable detail to work correctly for both Coachee and Coach roles. The 76KB view is highest-risk for rendering bugs. This plan fixes any code issues before the browser verification checkpoint in Plan 03.
Output: CDPController coaching actions reviewed and bug-free; CoachingProton.cshtml approval button and filter logic verified; Deliverable.cshtml evidence upload and coaching session display verified.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/85-coaching-proton-flow-qa/85-01-SUMMARY.md

@Controllers/CDPController.cs
@Views/CDP/CoachingProton.cshtml
@Views/CDP/Deliverable.cshtml
</context>

<interfaces>
<!-- Key contracts extracted from codebase for executor reference. -->

CDPController.CoachingProton role scoping (line 1089-1128):
- Level <=2 (HC/Admin): all coachees with RoleLevel==6
- Level 4 (SrSpv/SH): same section, RoleLevel==6
- Level 5 (Coach): CoachCoacheeMappings WHERE CoachId==user.Id AND IsActive
- Level 6 (Coachee): only user.Id itself

CDPController.Deliverable access check (line 719-738):
- isCoachee = progress.CoacheeId == user.Id
- isCoach = user.RoleLevel <= 5 (any role level 1-5)
- isHC = userRole == UserRoles.HC
- HC has full access (no section check)
- Coach (non-HC, non-coachee): must be in same section as coachee OR return Forbid()

CDPController.SubmitEvidenceWithCoaching (line 1652):
- Coach-only (RoleLevel==5 check — not role string check)
- Validates all progress IDs belong to coach's mapped coachees
- Status must be "Pending" or "Rejected"
- Creates CoachingSession with ProtonDeliverableProgressId = first progressId
- Updates each progress to Status="Submitted"
- Optional file upload to /uploads/evidence/{firstProgressId}/

CDPController.GetCoacheeDeliverables (line 1963):
- Returns deliverables for selected coacheeId
- Used by coaching modal in CoachingProton.cshtml for Coach role dropdown interaction

DeliverableViewModel (Models/ProtonViewModels.cs):
```csharp
public bool CanUpload { get; set; }   // true when Status==Pending||Rejected AND userRole==Coach
public bool CanApprove { get; set; }  // true when isAtasanAccess && Status=="Submitted"
public bool CanHCReview { get; set; } // true when isHC && HCApprovalStatus=="Pending"
public string CurrentUserRole { get; set; }
```

CoachingSession model (Models/CoachingSession.cs):
- Linked via ProtonDeliverableProgressId (nullable int)
- CoachId, CoacheeId are string user IDs (no FK)
- Status: "Draft" or "Submitted"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Code review CDPController coaching actions and fix bugs</name>
  <files>Controllers/CDPController.cs</files>
  <action>
Review the following CDPController action groups for correctness:

**CoachingProton (line 1089):**
- Confirm role scoping logic is correct for all 4 levels (HC/Admin, SrSpv, Coach, Coachee)
- Confirm IsActive filter is applied to CoachCoacheeMappings query for Coach scope (line ~1121)
- Confirm Phase 83 IsActive filter is applied to user queries (should filter .Where(u => u.IsActive) — check lines 1109-1128)
- Confirm coacheeId filter (line 1090 param): when a specific coacheeId is passed, it narrows to that coachee correctly
- Check pagination: page param used correctly, totalPages computed correctly from coachee list count

**Deliverable (line 700):**
- Confirm access check: Coachee can access their own, Coach can access if same section, HC unrestricted
- Confirm CanUpload: ONLY when (Status=="Pending" OR Status=="Rejected") AND userRole==UserRoles.Coach
- Confirm CanApprove: isAtasanAccess (SrSpv or SH) AND Status=="Submitted"
- Confirm CanHCReview: isHC AND HCApprovalStatus=="Pending"
- Check CoachingSession load: WHERE ProtonDeliverableProgressId==id (correct — links by progress ID, not deliverable ID)

**SubmitEvidenceWithCoaching (line 1652):**
- Confirm RoleLevel==5 check (not role string — correct per Phase 74 decision)
- Confirm validCoacheeIds loads from CoachCoacheeMappings WHERE CoachId==user.Id AND IsActive
- Confirm Status validation: "Pending" or "Rejected" only
- After file upload, confirm evidencePath assigned to ALL selected progress records (not just first)
- After save: confirm each progress.Status = "Submitted" and progress.SubmittedAt = DateTime.UtcNow

**GetCoacheeDeliverables (line 1963):**
- Check it returns proper structure for the coaching modal JS

Fix any bugs found. Do not restructure — patch only. If a bug requires >50 lines of change, add a comment `// TODO(85-02): describe bug` and note in summary.

Also check: ApproveFromProgress (line 1481), RejectFromProgress (line 1546), HCReviewFromProgress (line 1614) — these are AJAX variants used from CoachingProton.cshtml. Confirm they:
- Have [ValidateAntiForgeryToken] or explicit CSRF handling if called via fetch with X-RequestVerificationToken header
- Have correct role guards (SrSpv/SH for approve/reject, HC for HCReview)
- Return Json({success, message}) pattern
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" &amp;&amp; dotnet build --no-restore -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>dotnet build passes 0 errors; coaching action code review complete; any bugs patched; AJAX variant guards confirmed correct</done>
</task>

<task type="auto">
  <name>Task 2: Targeted review of CoachingProton.cshtml and Deliverable.cshtml</name>
  <files>Views/CDP/CoachingProton.cshtml, Views/CDP/Deliverable.cshtml</files>
  <action>
**CoachingProton.cshtml (76KB) — targeted review on these sections only:**

1. **Approval buttons visibility**: Find the section rendering Approve/Reject buttons for inline AJAX approval. Confirm buttons are wrapped in a Razor role check:
   - SrSpv/SH buttons: `@if (User.IsInRole("SrSupervisor") || User.IsInRole("SectionHead"))`
   - HC Review button: `@if (User.IsInRole("HC"))`
   - Coach buttons (SubmitEvidenceWithCoaching modal trigger): `@if (User.IsInRole("Coach"))`
   If any of these use wrong role names or missing guards, fix them.

2. **Filter form**: Confirm the filter form (bagian, unit, trackType, tahun dropdowns) uses GET method and correct action URL `/CDP/CoachingProton`. Confirm all filter params are preserved in hidden inputs when paginating.

3. **Pagination**: Confirm page links pass all current filter params as query string params (not just page number in isolation).

4. **Coachee dropdown + GetCoacheeDeliverables AJAX**: Confirm the coachee selection in the coaching modal triggers a fetch to `/CDP/GetCoacheeDeliverables?coacheeId=` with the correct URL, and the response populates the deliverable checkboxes in the modal.

5. **CSRF token for AJAX posts**: Confirm ApproveFromProgress, RejectFromProgress, HCReviewFromProgress fetch calls include the antiforgery token header. Pattern should be:
   ```javascript
   headers: { 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value }
   ```
   If missing, add a hidden antiforgery input (`@Html.AntiForgeryToken()`) to the page and add the header to each fetch call.

**Deliverable.cshtml:**
1. Confirm evidence upload form has `enctype="multipart/form-data"` and posts to `/CDP/UploadEvidence`
2. Confirm coaching session history section renders: date, coach name, result badge, and CatatanCoach text
3. Confirm status badge shows correct colors: Approved=green, Submitted=blue, Rejected=red, Pending=gray
4. Confirm Approve/Reject buttons (page-level, non-AJAX) only show when `Model.CanApprove == true`
5. Confirm HC Review button only shows when `Model.CanHCReview == true`
6. Confirm evidence download link uses `Model.Progress.EvidencePath` as href (raw path, no controller action)

Fix any rendering bugs found. Note any issues that require user verification in the Plan 03 summary.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" &amp;&amp; dotnet build --no-restore -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>dotnet build passes 0 errors; CoachingProton.cshtml approval button guards confirmed correct; CSRF tokens confirmed on AJAX calls; Deliverable.cshtml evidence upload and session display verified correct</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `dotnet build` passes with 0 errors
2. CDPController coaching actions code-reviewed — IsActive filters confirmed, role guards correct, SubmitEvidenceWithCoaching sets Status="Submitted" on all selected progress records
3. CoachingProton.cshtml approval buttons wrapped in correct Razor role guards
4. CSRF tokens present on all AJAX approval calls in CoachingProton.cshtml
5. Deliverable.cshtml evidence upload form and coaching session display correct
</verification>

<success_criteria>
- CDPController CoachingProton, Deliverable, SubmitEvidenceWithCoaching actions reviewed and patched
- CoachingProton.cshtml: approval button role guards correct, filter/pagination preserves params, AJAX calls have CSRF tokens
- Deliverable.cshtml: evidence upload form correct, coaching session history renders, status badges correct
- dotnet build passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/85-coaching-proton-flow-qa/85-02-SUMMARY.md` with:
- Tasks completed and files modified
- Bugs found and fixed
- Issues flagged for user browser verification (with specific URLs to check)
- Confirmation that build passes
</output>
