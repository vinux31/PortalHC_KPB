---
phase: 85-coaching-proton-flow-qa
plan: 04
type: execute
wave: 4
depends_on: [85-03]
files_modified:
  - Controllers/ProtonDataController.cs
  - Controllers/CDPController.cs
  - Views/ProtonData/Index.cshtml
autonomous: false
requirements: [COACH-07, COACH-08]

must_haves:
  truths:
    - "Admin/HC can navigate to ProtonData Override tab and see the deliverable status grid for a selected Bagian/Unit/Track"
    - "Admin/HC can click a badge in the Override grid to load OverrideDetail and change a deliverable status"
    - "OverrideSave POST saves the new status and optional rejection reason to ProtonDeliverableProgress"
    - "ExportProgressExcel returns a valid .xlsx file with coachee's deliverable statuses and coaching info"
    - "ExportProgressPdf returns a PDF file (or appropriate file type) with coachee's progress data"
    - "CDP Dashboard (HC/Supervisor role) shows correct pending approval counts and coachee summary rows"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "Override, OverrideList, OverrideDetail, OverrideSave actions reviewed and working"
    - path: "Controllers/CDPController.cs"
      provides: "ExportProgressExcel, ExportProgressPdf, Dashboard actions reviewed"
  key_links:
    - from: "ProtonData/Override page"
      to: "ProtonDataController.OverrideList AJAX"
      via: "fetch GET /ProtonData/OverrideList?bagian=&unit=&trackId= — loads coachee grid"
    - from: "ProtonData/Override badge click"
      to: "ProtonDataController.OverrideDetail AJAX"
      via: "fetch GET /ProtonData/OverrideDetail?id={progressId} — loads detail panel"
    - from: "ProtonData/Override save button"
      to: "ProtonDataController.OverrideSave POST"
      via: "fetch POST /ProtonData/OverrideSave with JSON body {progressId, newStatus, ...}"
---

<objective>
Code review of ProtonDataController Override tab and CDPController export/dashboard actions, plus browser verification of Override and export flows.

Purpose: COACH-07 (Override tab) and COACH-08 (Excel/PDF exports) are the final two coaching QA requirements. This plan reviews the Override AJAX flow and exports for correctness, then asks the user to verify in browser. On approval, Phase 85 is complete.
Output: Override and export actions reviewed and patched; user confirms Override tab and both exports work in browser; Phase 85 closed.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/85-coaching-proton-flow-qa/85-03-SUMMARY.md

@Controllers/ProtonDataController.cs
@Controllers/CDPController.cs
@Views/ProtonData/Index.cshtml
</context>

<interfaces>
<!-- Key contracts extracted from codebase for executor reference. -->

ProtonDataController.Override (line 121):
- Returns the ProtonData/Index.cshtml view with Override tab active
- Auth: [Authorize(Roles = "Admin, HC")] at class level on ProtonDataController

ProtonDataController.OverrideList (line 595):
- GET endpoint returning JSON {success, coachees[], deliverableHeaders[]}
- Params: bagian, unit, trackId (int), statusFilter (optional: "rejected", "pendingHC", or empty for all)
- Loads deliverables filtered to Bagian+Unit+Track scope
- Returns per-coachee badge data (progressId, deliverableId, status)

ProtonDataController.OverrideDetail (line 660):
- GET endpoint returning JSON with progress detail
- Param: id (progressId)
- Returns: status, evidence path, rejection reason, approver info

ProtonDataController.OverrideSave (line 717):
- POST endpoint accepting JSON body (OverrideSaveRequest)
- OverrideSaveRequest fields:
  ```csharp
  public int ProgressId { get; set; }
  public string NewStatus { get; set; } = "";  // "Approved", "Rejected", "Pending"
  public string? NewRejectionReason { get; set; }
  public string OverrideReason { get; set; } = "";
  ```
- Updates progress.Status, conditionally sets RejectionReason
- Should require [ValidateAntiForgeryToken] or CSRF header

CDPController.ExportProgressExcel (line 1792):
- GET endpoint, param: coacheeId
- Returns .xlsx with coachee deliverable data
- Auth: implicit from class-level [Authorize]

CDPController.ExportProgressPdf (line 1872):
- GET endpoint, param: coacheeId
- Returns file response (PDF or similar)

CDPController.Dashboard (line 200):
- HC/Supervisor view: ProtonProgressSubModel with TotalCoachees, PendingSpvApprovals, PendingHCReviews, CoacheeRows
- Built by BuildProtonProgressSubModelAsync (line 283)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Code review Override tab and export actions, fix any bugs</name>
  <files>Controllers/ProtonDataController.cs, Controllers/CDPController.cs, Views/ProtonData/Index.cshtml</files>
  <action>
**Review ProtonDataController Override actions:**

1. OverrideList (line 595): Confirm the Bagian/Unit/Track filter is applied to deliverables query (WHERE Bagian==bagian AND Unit==unit AND ProtonTrackId==trackId). Confirm status filter logic is correct ("rejected" = any progress.Status=="Rejected", "pendingHC" = HCApprovalStatus=="Pending" AND Status=="Approved"). Confirm JSON response structure matches what View JS expects.

2. OverrideDetail (line 660): Confirm it returns the full detail including rejection reason and approver info. Check if it returns 404 or error JSON when progress not found.

3. OverrideSave (line 717): Check CSRF handling — is this called with [FromBody] JSON? If so, [ValidateAntiForgeryToken] alone won't work for JSON POST. Confirm the JS in Index.cshtml sends the X-RequestVerificationToken header. If missing, add the header to the fetch call in the view. Also confirm OverrideSave sets Status to the new value and conditionally sets/clears RejectionReason.

4. Check Views/ProtonData/Index.cshtml Override tab section: confirm the bagian/unit/track selector form calls OverrideList, the status filter dropdown calls OverrideList with statusFilter param, and badge clicks call OverrideDetail then render the detail panel.

**Review CDPController exports:**

5. ExportProgressExcel (line 1792): Confirm the query loads all ProtonDeliverableProgresses for coacheeId, joins with deliverable hierarchy (Include chain), and exports with correct column headers. Check if it handles the case where coacheeId has no progress records (empty sheet vs error).

6. ExportProgressPdf (line 1872): Read the action to understand what library is used. Confirm it does not crash with an empty coachee record set.

7. CDPController.Dashboard (line 200): Review BuildProtonProgressSubModelAsync (line 283) — confirm PendingSpvApprovals counts Status=="Submitted" correctly, PendingHCReviews counts HCApprovalStatus=="Pending" AND Status=="Approved" correctly, CompletedCoachees counts distinct coachees with a ProtonFinalAssessment record.

Fix any bugs found (patch only). For the Override CSRF fix, find the fetch call in Index.cshtml that posts to OverrideSave and add:
```javascript
headers: {
  'Content-Type': 'application/json',
  'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value ?? ''
}
```
And ensure `@Html.AntiForgeryToken()` is present in the Override tab section of the view.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" &amp;&amp; dotnet build --no-restore -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>dotnet build passes 0 errors; Override actions reviewed; OverrideSave CSRF confirmed or fixed; ExportProgressExcel/Pdf reviewed; Dashboard BuildProtonProgressSubModel counts verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1 reviewed and patched:
- ProtonDataController Override tab: OverrideList, OverrideDetail, OverrideSave — any CSRF issues fixed
- CDPController ExportProgressExcel, ExportProgressPdf — confirmed functional
- CDPController Dashboard pending counts logic verified correct

Test data from Plan 01 (SeedCoachingTestData) is still in the database.
  </what-built>
  <how-to-verify>
Start the app: `dotnet run` from the project directory. Login as Admin or HC for all Override flows.

**Flow 5 — Override Tab (COACH-07)**
1. Login as Admin (or HC)
2. Navigate to Kelola Data → ProtonData (or directly /ProtonData) → Override tab
3. Select a Bagian, Unit, and Track from the selectors → click "Muat Data" (or equivalent load button)
4. Confirm the coachee grid appears with colored badges (Approved=green, Submitted=blue, Rejected=red, Pending=gray, "—"=not started)
5. Click a Rejected badge for testCoachee1 → confirm the detail panel opens showing current status, rejection reason, and Override form
6. Change status to "Approved" via the Override form → enter an override reason → click Save → confirm the badge changes color in the grid
7. Try status filter "Pending HC Review" → confirm only records with HCApprovalStatus==Pending AND Status==Approved appear

**Flow 6 — Excel Export (COACH-08)**
1. Login as HC or Admin
2. Navigate to /CDP/CoachingProton and select testCoachee1
3. Click "Export Excel" button (if present on CoachingProton page) OR navigate to /CDP/ExportProgressExcel?coacheeId={testCoachee1Id}
4. Confirm .xlsx file downloads; open it and spot-check: coachee name correct, deliverable names correct, statuses correct

**Flow 7 — PDF Export (COACH-08)**
1. Navigate to /CDP/ExportProgressPdf?coacheeId={testCoachee1Id}
2. Confirm a file downloads (PDF or other format — just confirm no error page)
3. Open the file and confirm it contains the coachee's name and deliverable data

**Flow 8 — CDP Dashboard (COACH-07)**
1. Login as HC
2. Navigate to /CDP/Dashboard → confirm the Coaching Proton tab is visible
3. Confirm stat cards show non-zero values: Total Coachees, Pending Approvals, Pending HC Reviews (based on seeded data)
4. Confirm the coachee list table shows testCoachee1 and testCoachee2 with their progress percentages
5. Switch to the Assessment Analytics tab → confirm it loads without error

Type "approved" to confirm all flows pass, or describe any specific issues found.
  </how-to-verify>
  <resume-signal>Type "approved" if all flows pass, or describe issues (include which flow and step failed)</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
1. COACH-07: Override tab — Bagian/Unit/Track selector loads coachee grid, badge click opens detail, OverrideSave changes status in DB
2. COACH-08: ExportProgressExcel returns valid .xlsx, ExportProgressPdf returns file without error
3. CDP Dashboard: pending approval counts correct, coachee list renders with correct progress %
4. All 8 COACH requirements (COACH-01 through COACH-08) formally closed:
   - COACH-01: Mapping assign/edit/deactivate/reactivate — verified
   - COACH-02: Mapping Excel export — verified
   - COACH-03: Coachee progress view — verified
   - COACH-04: Coach evidence upload + coaching log — verified
   - COACH-05: Approval chain (SrSpv/SH approve, HC review) — verified
   - COACH-06: Deliverable detail (evidence, rejection reason, coaching history, status) — verified
   - COACH-07: Override tab + dashboard — verified
   - COACH-08: Excel and PDF exports — verified
</verification>

<success_criteria>
- Override tab works end-to-end: load grid → click badge → override status → confirm change
- ExportProgressExcel and ExportProgressPdf both download without errors
- CDP Dashboard shows correct data for HC/Supervisor role
- All COACH-01 through COACH-08 requirements closed
- Phase 85 complete
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/85-coaching-proton-flow-qa/85-04-SUMMARY.md` with:
- All 8 flows verification results (PASS/FAIL)
- Bugs found and fixed
- Formal closure of COACH-01 through COACH-08
- Phase 85 complete status

Also update .planning/ROADMAP.md Phase 85 status to "✅ Complete" and add completion date "2026-03-04" (or actual date).
</output>
