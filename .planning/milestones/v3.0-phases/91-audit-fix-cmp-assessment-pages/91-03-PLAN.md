---
phase: 91-audit-fix-cmp-assessment-pages
plan: 03
type: execute
wave: 3
depends_on: [91-02]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - All 9 browser verification flows are confirmed PASS or bugs are filed for gap closure
  artifacts: []
  key_links:
    - Test data from Phase 90 (SeedAssessmentTestData) provides 5 groups for testing
---

<objective>
Browser verification of all Phase 91 fixes across the CMP Assessment worker flow.

Purpose: Confirm all backend security fixes, Records redesign, navigation fixes, exam resilience improvements, and shuffle changes work correctly in a real browser session before closing Phase 91.

Output: Human confirmation that all 9 verification flows PASS or list of failing flows for gap closure.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-audit-fix-cmp-assessment-pages/91-CONTEXT.md
@.planning/phases/91-audit-fix-cmp-assessment-pages/91-01-SUMMARY.md
@.planning/phases/91-audit-fix-cmp-assessment-pages/91-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start application and prepare verification</name>
  <files></files>
  <action>
Run `dotnet build` to confirm zero errors, then start the application. Confirm the app loads at http://localhost:5000 (or configured port).

Also verify the test data from Phase 90 is still present: there should be at least 5 assessment groups (Open, Upcoming/token, Completed/pass, Completed/fail, Abandoned) visible in Admin > ManageAssessment.

If test data is missing, re-run the seed action: GET /Admin/SeedAssessmentTestData (from Phase 90).
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -3</automated>
  </verify>
  <done>Build succeeds with 0 errors. App starts and test assessment data is present.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human verify all 9 CMP Assessment flows</name>
  <files></files>
  <action>
Verify these 9 flows in the browser. For each flow, mark PASS or FAIL:

**FLOW 1 — Records page redesign:**
1. Login as a Worker who has completed at least 1 assessment
2. Navigate to CMP > Assessment > Records (or /CMP/Records)
3. VERIFY: Breadcrumb shows CMP > Assessment > Records
4. VERIFY: Stat cards show "Assessment Online" / "Training Manual" / "Total Records" (not Total/Selesai/Pending)
5. VERIFY: Two tabs — "Assessment Online" and "Training Manual" — are present
6. VERIFY: Assessment Online tab shows completed assessment rows; clicking a row opens CMP/Results for that assessment
7. VERIFY: Training Manual tab shows training record rows

**FLOW 2 — Results back button (worker path):**
1. Login as Worker, complete or navigate to a completed assessment's results page (/CMP/Results?id=X)
2. VERIFY: "Kembali" button navigates to CMP/Assessment (no returnUrl in URL)
3. From Admin/UserAssessmentHistory, click a Results link that includes returnUrl param
4. VERIFY: "Kembali" button on Results page navigates back to Admin page (not to CMP/Assessment)

**FLOW 3 — Certificate back button:**
1. Navigate to /CMP/Certificate?id=X (passed assessment)
2. VERIFY: Back/Kembali button is present and navigates to CMP/Assessment by default
3. If accessed with ?returnUrl=... param, VERIFY: it navigates to that URL instead

**FLOW 4 — Token verification CSRF (VerifyToken):**
1. Login as Worker with an "Open + Token Required" assessment in CMP/Assessment
2. Click the assessment card, enter the correct token, click Verify
3. VERIFY: Token verification succeeds and exam loads (no 400 Bad Request)
4. (Optional) Open browser DevTools Network tab — confirm POST to /CMP/VerifyToken includes RequestVerificationToken header

**FLOW 5 — SubmitExam HC authorization:**
1. Login as HC user
2. Navigate directly to /CMP/StartExam?id=X for an open assessment assigned to a Worker
3. VERIFY: HC user can access the exam page (was previously blocked by missing HC in SubmitExam auth)
4. Attempt to submit — VERIFY: no 403 error for HC

**FLOW 6 — Auto-save retry (StartExam):**
1. Login as Worker, start an exam (/CMP/StartExam?id=X)
2. Select an answer for a question
3. VERIFY: Save indicator briefly shows "saving" then "saved" (bottom right)
4. (If able to simulate a network hiccup) Confirm 3 retries happen before showing error warning

**FLOW 7 — Force-close modal (StartExam polling):**
1. Login as Worker and start an exam
2. In a separate Admin/HC session, force-close the assessment (Admin > ManageAssessment > Force Close action)
3. Within 10 seconds, VERIFY: Worker's exam page shows a modal dialog: "Ujian Ditutup — Ujian ini telah ditutup oleh HC/Administrator" with an OK button
4. VERIFY: Clicking OK redirects worker away from the exam (not a silent banner redirect)

**FLOW 8 — Single-package question shuffle:**
1. Ensure at least one assessment with a single package exists and has multiple questions
2. Login as two different Worker accounts and each starts the exam
3. VERIFY: The question order is different between the two workers (was previously identical for all workers)

**FLOW 9 — Option shuffle:**
1. Login as Worker, start a package-based exam
2. VERIFY: The A/B/C/D option order for the same question appears different compared to another worker's session
3. VERIFY: Scoring still works correctly — submitting correct answer gives expected score

Report: PASS all 9 flows, or list failing flows with description of the failure.
  </action>
  <verify>
    <automated>MISSING — visual/functional browser verification required</automated>
  </verify>
  <done>User confirms all 9 flows PASS, OR lists failing flows that need gap closure plans.</done>
</task>

</tasks>

<verification>
Phase 91 is complete when all 9 browser verification flows are confirmed PASS by the user.

If any flows FAIL, create gap closure plans (91-04, 91-05, etc.) targeting the specific bugs found.
</verification>

<success_criteria>
1. Records page has 2-tab layout with clickable Assessment Online rows
2. Results and Certificate back buttons work correctly with and without returnUrl
3. VerifyToken CSRF protection confirmed working (no 400 errors)
4. HC can submit/access exams without 403
5. Auto-save indicator works; force-close shows modal (not silent banner)
6. Question order varies per worker for single-package exams
7. Option A/B/C/D order varies per worker per question
</success_criteria>

<output>
After completion, create `.planning/phases/91-audit-fix-cmp-assessment-pages/91-03-SUMMARY.md` with:
- All 9 flow results (PASS/FAIL)
- Any bugs found and whether gap closure plans were created
- Phase 91 completion status
</output>
