---
phase: 152-account-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AccountController.cs
  - Models/SettingsViewModel.cs
  - Models/ProfileViewModel.cs
  - Views/Account/Profile.cshtml
  - Views/Account/Settings.cshtml
autonomous: true
requirements: [SEC-01, VAL-01, VAL-02, CODE-01, UI-01, UI-02]

must_haves:
  truths:
    - "Unauthenticated users visiting /Account/Profile or /Account/Settings get redirected to login"
    - "Login and AccessDenied remain accessible without authentication"
    - "Settings form shows client-side validation errors without round-trip"
    - "Phone field accepts +62 812-3456-7890 format"
    - "Profile page displays role from ViewModel (no ViewBag)"
    - "Profile button says Pengaturan, not Edit Profile"
    - "Profile and Settings pages have consistent mb-3 row spacing"
  artifacts:
    - path: "Controllers/AccountController.cs"
      provides: "Class-level [Authorize] with [AllowAnonymous] on Login/AccessDenied"
      contains: "[Authorize]"
    - path: "Models/ProfileViewModel.cs"
      provides: "ProfileViewModel with Role property"
      exports: ["ProfileViewModel"]
    - path: "Models/SettingsViewModel.cs"
      provides: "Updated phone regex"
      contains: "RegularExpression"
    - path: "Views/Account/Profile.cshtml"
      provides: "Updated model, button label, spacing"
      contains: "Pengaturan"
    - path: "Views/Account/Settings.cshtml"
      provides: "ValidationScriptsPartial included"
      contains: "_ValidationScriptsPartial"
  key_links:
    - from: "Controllers/AccountController.cs"
      to: "Models/ProfileViewModel.cs"
      via: "Profile action returns ProfileViewModel"
      pattern: "new ProfileViewModel"
    - from: "Views/Account/Profile.cshtml"
      to: "Models/ProfileViewModel.cs"
      via: "@model directive"
      pattern: "@model.*ProfileViewModel"
---

<objective>
Fix all 6 issues on Account Profile and Settings pages: authorization pattern, client-side validation, phone regex, ViewModel refactor, button label, and UI spacing consistency.

Purpose: Account pages become secure, validated, and visually consistent with the rest of the app.
Output: Updated AccountController, new ProfileViewModel, fixed views.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-account-cleanup/152-CONTEXT.md

@Controllers/AccountController.cs
@Models/SettingsViewModel.cs
@Views/Account/Profile.cshtml
@Views/Account/Settings.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth pattern, phone regex, ProfileViewModel, and controller updates</name>
  <files>Controllers/AccountController.cs, Models/SettingsViewModel.cs, Models/ProfileViewModel.cs</files>
  <action>
1. **AccountController.cs — Authorization (SEC-01):**
   - Add `using Microsoft.AspNetCore.Authorization;` if not present
   - Add `[Authorize]` attribute at class level (line 9, before `public class AccountController`)
   - Add `[AllowAnonymous]` to Login GET (line ~29), Login POST (line ~42), and AccessDenied (line ~275)
   - Remove the `if (User.Identity?.IsAuthenticated != true)` guard blocks from Profile() and Settings() actions — they are now redundant since class-level [Authorize] handles it
   - Keep the `if (user == null) return RedirectToAction("Login")` null checks after GetUserAsync (still needed)

2. **SettingsViewModel.cs — Phone regex (VAL-02):**
   - Change line 40 regex from `@"^[0-9]+$"` to `@"^[\d\s\-\+\(\)]+$"`
   - Update ErrorMessage to `"Format nomor telepon tidak valid"`
   - Also increase StringLength from 20 to 30 (international formats with spaces/dashes can be longer)

3. **ProfileViewModel.cs — New ViewModel (CODE-01):**
   - Create new file `Models/ProfileViewModel.cs` in namespace `HcPortal.Models`
   - Properties: FullName, NIP, Email, PhoneNumber, Position, Directorate, Section, Unit, Role (all string?, matching ApplicationUser fields + Role)

4. **AccountController.cs — Profile action update (CODE-01):**
   - Update Profile() to create and return `ProfileViewModel` instead of `ApplicationUser`
   - Map all fields from user object + set Role from roles.FirstOrDefault() ?? "No Role"
   - Remove `ViewBag.UserRole = ...` line
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>AccountController has [Authorize] at class level with [AllowAnonymous] on Login/AccessDenied; phone regex accepts international formats; ProfileViewModel exists and Profile action uses it</done>
</task>

<task type="auto">
  <name>Task 2: View updates — validation scripts, button label, row spacing</name>
  <files>Views/Account/Profile.cshtml, Views/Account/Settings.cshtml</files>
  <action>
1. **Profile.cshtml — Model change (CODE-01):**
   - Change `@model HcPortal.Models.ApplicationUser` to `@model HcPortal.Models.ProfileViewModel`
   - Change `ViewBag.UserRole as string` to `Model.Role` (line 4: `var userRole = Model.Role ?? "—";`)

2. **Profile.cshtml — Button label (UI-01):**
   - Change "Edit Profile" text to "Pengaturan" on line 95 (keep the bi-gear icon)

3. **Profile.cshtml — Row spacing (UI-02):**
   - Change all `mb-2 py-1` to `mb-3` on the row divs (lines 32, 38, 44, 50, 62, 68, 74, 80, 86)
   - That is: replace `class="row mb-2 py-1"` with `class="row mb-3"`

4. **Settings.cshtml — Validation scripts (VAL-01):**
   - Inside the existing `@section Scripts { }` block (line 225), add `@await Html.PartialAsync("_ValidationScriptsPartial")` BEFORE the existing script tag
   - Result should be:
     ```
     @section Scripts {
         @await Html.PartialAsync("_ValidationScriptsPartial")
         <script>
             $(document).ready(function() { ... });
         </script>
     }
     ```
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Profile uses ProfileViewModel with Role from model; button says "Pengaturan"; all rows use mb-3; Settings includes _ValidationScriptsPartial for client-side validation</done>
</task>

</tasks>

<verification>
Build succeeds with `dotnet build`. Manual spot-check:
- Visit /Account/Profile while logged out — should redirect to /Account/Login
- Visit /Account/Login while logged out — should render normally
- Visit /Account/Settings while logged in — submit empty required field — error shows without page reload
- Enter "+62 812-3456-7890" in phone field — no validation error
- Profile page button says "Pengaturan"
- Profile page rows have consistent spacing (mb-3)
- Profile page shows role without ViewBag
</verification>

<success_criteria>
- dotnet build succeeds
- AccountController has [Authorize] class-level + [AllowAnonymous] on Login/AccessDenied
- ProfileViewModel.cs exists with Role property
- Profile.cshtml uses ProfileViewModel, button says "Pengaturan", rows use mb-3
- Settings.cshtml includes _ValidationScriptsPartial
- Phone regex accepts international formats
</success_criteria>

<output>
After completion, create `.planning/phases/152-account-cleanup/152-01-SUMMARY.md`
</output>
