---
phase: 107-backend-worker-list-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/HistoriProtonViewModel.cs
  - Controllers/CDPController.cs
  - Views/CDP/Index.cshtml
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04

must_haves:
  truths:
    - "CDP Hub shows Histori Proton card after Coaching Proton card"
    - "HC/Admin accessing HistoriProton sees all workers data passed to view"
    - "Coach accessing HistoriProton sees only mapped coachees"
    - "SrSpv/SH accessing HistoriProton sees only section workers"
    - "Coachee accessing HistoriProton is redirected to HistoriProtonDetail"
  artifacts:
    - path: "Models/HistoriProtonViewModel.cs"
      provides: "ViewModel classes for list page"
      contains: "HistoriProtonWorkerRow"
    - path: "Controllers/CDPController.cs"
      provides: "HistoriProton and HistoriProtonDetail actions"
      contains: "HistoriProton"
    - path: "Views/CDP/Index.cshtml"
      provides: "Histori Proton card in CDP hub"
      contains: "bi-clock-history"
  key_links:
    - from: "Views/CDP/Index.cshtml"
      to: "CDPController.HistoriProton"
      via: "Url.Action link on card"
      pattern: "HistoriProton.*CDP"
    - from: "CDPController.HistoriProton"
      to: "Models/HistoriProtonViewModel.cs"
      via: "View(model)"
      pattern: "HistoriProtonViewModel"
---

<objective>
Build the backend infrastructure for Histori Proton: ViewModel, CDPController actions with role-scoped access, and CDP Hub card navigation.

Purpose: Establishes the controller actions and data pipeline so Plan 02 can build the list view against a working backend.
Output: ViewModel file, two controller actions (HistoriProton + HistoriProtonDetail stub), CDP Hub card.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-backend-worker-list-page/107-CONTEXT.md
@.planning/phases/107-backend-worker-list-page/107-RESEARCH.md

@Controllers/CDPController.cs
@Models/ProtonModels.cs
@Views/CDP/Index.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ViewModel and CDPController actions with role-scoped access</name>
  <files>Models/HistoriProtonViewModel.cs, Controllers/CDPController.cs</files>
  <action>
1. Create Models/HistoriProtonViewModel.cs with two classes:
   - HistoriProtonViewModel: Workers list, SearchQuery, FilterSection, FilterUnit, FilterJalur, FilterStatus, AvailableSections list, AvailableUnits list (exact properties in RESEARCH.md code example)
   - HistoriProtonWorkerRow: UserId, Nama, NIP, Unit, Jalur (string), Tahun1Done/Tahun2Done/Tahun3Done (bool), Tahun1InProgress/Tahun2InProgress/Tahun3InProgress (bool), Status (string)

2. Add HistoriProton() action to CDPController.cs:
   - Clone role-scoping logic from CoachingProton action (lines ~1270-1295): RoleLevel <= 3 = all active coachees, RoleLevel 4 = same Section, RoleLevel 5 = CoachCoacheeMappings, RoleLevel >= 6 = redirect to HistoriProtonDetail(user.Id)
   - Dual role: widest scope wins naturally via RoleLevel integer comparison (lower = wider)
   - Query ProtonTrackAssignments for scoped coachee IDs, Include ProtonTrack
   - Query ProtonFinalAssessments for those assignments
   - Group by CoacheeId, build HistoriProtonWorkerRow for each:
     - Join with Users for Nama, NIP, Unit
     - Jalur = latest assignment's ProtonTrack.TrackType
     - For each TahunKe (1,2,3): check if assignment exists AND has ProtonFinalAssessment = Done, assignment exists without assessment = InProgress, no assignment = neither
     - Status = latest assignment status: has completed ProtonFinalAssessment = "Lulus", has assignment but no assessment = "Dalam Proses"
   - Populate AvailableSections and AvailableUnits from the scoped workers for filter dropdowns
   - Return View(viewModel)
   - IMPORTANT: Always filter u.IsActive on Users and m.IsActive on CoachCoacheeMappings

3. Add HistoriProtonDetail(string userId) stub action:
   - For now just return View() (Phase 108 builds the detail page)
   - Add basic authorization check: Coachee can only view own (userId == currentUser.Id), Coach can view mapped coachees, SrSpv/SH can view section, HC/Admin can view all. Return Forbid() if unauthorized.

Sort workers by Nama A-Z (default sort per CONTEXT.md).
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>HistoriProtonViewModel.cs exists with both classes. CDPController has HistoriProton action with role-scoped access returning populated ViewModel. HistoriProtonDetail stub exists with auth check. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add Histori Proton card to CDP Hub</name>
  <files>Views/CDP/Index.cshtml</files>
  <action>
Add a new card to Views/CDP/Index.cshtml positioned AFTER the Coaching Proton card and BEFORE the Deliverable card. Follow the exact same card structure as existing cards.

Card details (from CONTEXT.md locked decisions):
- Icon: bi-clock-history
- Color: info (bg-info bg-opacity-10 text-info)
- Title: "Histori Proton"
- Subtitle: "Riwayat Perjalanan"
- Description: "Lihat riwayat perjalanan Proton per pekerja"
- Button: "Lihat Riwayat" linking to Url.Action("HistoriProton", "CDP"), btn-info
- Button icon: bi-arrow-right-circle

Hub order must be: Plan IDP, Coaching Proton, Histori Proton, Deliverable, Dashboard.

Use the exact HTML pattern from RESEARCH.md Pattern 6 section, adapting to match the existing card structure in Index.cshtml.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -3 && grep -c "bi-clock-history" Views/CDP/Index.cshtml</automated>
  </verify>
  <done>CDP Hub shows Histori Proton card in correct position (after Coaching Proton, before Deliverable). Card has correct icon, color, text, and link. Build succeeds.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds with no errors
- CDPController.cs contains HistoriProton and HistoriProtonDetail actions
- Models/HistoriProtonViewModel.cs exists with HistoriProtonWorkerRow class
- Views/CDP/Index.cshtml contains bi-clock-history card in correct position
</verification>

<success_criteria>
- Build compiles successfully
- ViewModel has all required properties for list page rendering
- Role scoping matches CoachingProton pattern (RoleLevel branching)
- CDP Hub card appears after Coaching Proton card
</success_criteria>

<output>
After completion, create `.planning/phases/107-backend-worker-list-page/107-01-SUMMARY.md`
</output>
