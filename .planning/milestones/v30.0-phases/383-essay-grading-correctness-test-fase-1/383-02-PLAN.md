---
phase: 383-essay-grading-correctness-test-fase-1
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/Results.cshtml
  - HcPortal.Tests/ResultsEssayCorrectnessTests.cs
autonomous: false
requirements: [ECG-02, ECG-03, ECG-04]
must_haves:
  truths:
    - "Sesi N MC (benar) + 2 essay graded >0 di CMP/Results menampilkan count (N+2)/(N+2) — di kedua jalur review-on dan review-off"
    - "Breakdown Elemen Teknis menghitung soal essay sesuai nilai HC (essay benar dihitung benar)"
    - "Badge Tinjauan per-soal essay: Benar (hijau) untuk >0, Salah untuk =0, Menunggu Penilaian untuk null — terlepas status sesi"
    - "Teks jawaban essay (TextAnswer) worker tampil di baris review Tinjauan"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "3 site Results + IsEssayPending + D-07 ViewModel via IsQuestionCorrect helper"
      contains: "AssessmentScoreAggregator.IsQuestionCorrect"
    - path: "Views/CMP/Results.cshtml"
      provides: "Blok Razor render UserAnswer essay (Jawaban Anda) di baris review"
      contains: "Jawaban Anda"
    - path: "HcPortal.Tests/ResultsEssayCorrectnessTests.cs"
      provides: "Regression pure unit: N MC + 2 graded essay → CorrectAnswers == N+2; ET counts essays"
      contains: "IsQuestionCorrect"
  key_links:
    - from: "Controllers/CMPController.cs Results()"
      to: "AssessmentScoreAggregator.IsQuestionCorrect"
      via: "panggil helper di 3 site + IsEssayPending"
      pattern: "AssessmentScoreAggregator\\.IsQuestionCorrect"
    - from: "Views/CMP/Results.cshtml"
      to: "question.UserAnswer (essay TextAnswer)"
      via: "blok Razor baru render essay answer"
      pattern: "question\\.UserAnswer"
---

<objective>
Rewire 3 site recompute correctness inline di `CMPController.Results()` agar memanggil helper `IsQuestionCorrect` (Plan 01) — sehingga count "(X/Y benar)" (ECG-02), breakdown Elemen Teknis (ECG-03), dan badge Tinjauan (ECG-04) menghitung essay yang dinilai HC dengan benar. Broaden `IsEssayPending` (D-06) jadi correctness-based. Set `UserAnswer = TextAnswer` untuk essay (D-07) DAN tambah blok Razor di `Results.cshtml` yang me-render teks jawaban essay (Pitfall 1: view belum pernah render UserAnswer). Plus regression test pure-unit yang mengunci reproduksi 6/6.

Purpose: Menutup ECG-02 (RES-02), ECG-03, ECG-04 — gejala "Nilai Anda 100% tapi 4/6 benar" hilang di SELURUH surface web Results.

Output: `CMPController.cs` (3 site + IsEssayPending + D-07) + `Views/CMP/Results.cshtml` (blok essay answer) + `HcPortal.Tests/ResultsEssayCorrectnessTests.cs` (regression).

CRITICAL: ECG-04 view butuh verifikasi RUNTIME (`dotnet run` → `CMP/Results/{id}`) — grep+build TIDAK cukup (pelajaran Phase 354). Karena itu Task 3 adalah checkpoint human-verify (autonomous: false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-CONTEXT.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-RESEARCH.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md

# Reference DB lokal admin login (memory): admin@pertamina.com untuk /CMP/Results/* (roleLevel ≤3, lewat authz REC-04).
# CLAUDE.md: dev workflow dotnet build + test + run localhost:5277. Branch ITHandoff. JANGAN edit DB/kode Dev/Prod.

<interfaces>
<!-- Kontrak yang dipakai — diekstrak dari codebase, executor TIDAK perlu eksplorasi. -->

Helper dari Plan 01 (Helpers/AssessmentScoreAggregator.cs):
```csharp
public static bool? IsQuestionCorrect(PackageQuestion q, IEnumerable<PackageUserResponse> responsesForQ);
// true=Benar, false=Salah, null=essay pending. Per-soal: panggil dengan response list utk soal itu saja.
```

CMPController.Results data sudah tersedia per-soal (verified):
- review-on (L2250): `var userResponses = responseLookup[qId].ToList();` (list per-soal)
- review-off (L2310): `responseLookup[qId]` (ILookup grouping per-soal)
- Elemen Teknis (L2346): `responseLookup[q.Id]`
- `responseLookup = packageResponses.ToLookup(r => r.PackageQuestionId)` (L2230) — multi-row MA-safe (SURF-317-A).

QuestionReviewItem (Models/AssessmentResultsViewModel.cs:24-37) — field SUDAH ADA, tidak perlu tambah:
- `UserAnswer` (string?, L28), `CorrectAnswer` (string?, L29), `IsCorrect` (bool, L30), `IsEssayPending` (bool, L32)

AssessmentConstants.AssessmentStatus.PendingGrading = "Menunggu Penilaian" (Models/AssessmentConstants.cs:18).

CMPController.cs SUDAH punya `using HcPortal.Helpers;` (L17) — tidak perlu tambah.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire 3 site Results + IsEssayPending (D-06) + D-07 UserAnswer (ECG-02/03/04 controller-side)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs:2240-2390 (Results action — 3 site + IsEssayPending L2299-2300 + QuestionReviewItem build L2280-2301 + ViewModel L2371-2390)
    - .planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md (section CMPController — instruksi ganti per-site, baris 98-157)
    - Helpers/AssessmentScoreAggregator.cs (helper IsQuestionCorrect dari Plan 01)
  </read_first>
  <action>
    Edit `Controllers/CMPController.cs` Results() di 4 tempat. Semua memanggil `AssessmentScoreAggregator.IsQuestionCorrect`. JANGAN biarkan logic correctness inline lagi (kill-drift).

    **Site 1 — review-on (L2258-2271)**: ganti blok `bool isCorrect; if (... MultipleAnswer) {...} else {...} if (isCorrect) correctCount++;` dengan:
    ```csharp
    var verdict = AssessmentScoreAggregator.IsQuestionCorrect(question, userResponses);
    bool isCorrect = verdict == true;
    if (isCorrect) correctCount++;
    ```
    (`userResponses` sudah ada L2250. `verdict==null` artinya essay pending → isCorrect=false, badge ditangani IsEssayPending.)

    **Site 1b — IsEssayPending (L2299-2300, D-06)**: ganti
    ```csharp
    IsEssayPending = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading
                     && (question.QuestionType ?? "MultipleChoice") == "Essay")
    ```
    menjadi:
    ```csharp
    IsEssayPending = (question.QuestionType ?? "MultipleChoice") == "Essay"
                     && AssessmentScoreAggregator.IsQuestionCorrect(question, userResponses) == null
    ```
    (independen status sesi: graded essay di sesi Completed render Benar/Salah; ungraded selalu pending.)

    **Site 1c — D-07 essay UserAnswer (di QuestionReviewItem build, L2273-2285)**: setelah baris yang menghitung `userAnswerText` (L2273-2275) dan `correctAnswerText` (L2276-2278), tambah override untuk essay. Essay punya Options kosong → `userAnswerText==null`. Tambahkan SEBELUM `questionReviews.Add(...)`:
    ```csharp
    // D-07: essay tak punya Options → tampilkan TextAnswer worker + label "Dinilai manual"
    bool isEssay = (question.QuestionType ?? "MultipleChoice") == "Essay";
    if (isEssay)
    {
        userAnswerText = userResponses.FirstOrDefault(r => r.PackageQuestionId == qId)?.TextAnswer;
        correctAnswerText = "Dinilai manual";
    }
    ```
    (catatan: `userAnswerText`/`correctAnswerText` saat ini di-declare `var ... = ...`. Keduanya local var biasa, override aman. Lalu `QuestionReviewItem { ... UserAnswer = userAnswerText, CorrectAnswer = correctAnswerText, ... }` sudah meng-konsumsi keduanya L2284-2285.)

    **Site 2 — review-off count (L2304-2327)**: ganti seluruh isi `foreach (var qId in orderedQuestionIds) { ... }` body dengan:
    ```csharp
    foreach (var qId in orderedQuestionIds)
    {
        if (!questionLookup.TryGetValue(qId, out var question)) continue;
        if (AssessmentScoreAggregator.IsQuestionCorrect(question, responseLookup[qId].ToList()) == true)
            correctCount++;
    }
    ```
    (BUANG guard `if (selectedIds.Count == 0) continue;` — itu yang men-skip essay. Helper sudah handle non-empty internal.)

    **Site 3 — Elemen Teknis predicate (L2336-2369)**: ganti delegate `var correct = g.Count(q => { ... });` dengan:
    ```csharp
    var correct = g.Count(q => AssessmentScoreAggregator.IsQuestionCorrect(q, responseLookup[q.Id].ToList()) == true);
    ```
    (`total = g.Count()` L2342 dan formula `Percentage` L2364 TIDAK berubah.)

    ViewModel build L2371-2390 (`CorrectAnswers = correctCount` L2384) TIDAK berubah.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 error (grep "Build succeeded").
    - grep di CMPController.cs Results region: `AssessmentScoreAggregator.IsQuestionCorrect` muncul ≥4 kali (Site1, Site1b, Site2, Site3).
    - grep NEGATIF: tidak ada lagi `if (selectedIds.Count == 0) continue;` di review-off body (L2304-2327 region) maupun `if (selectedIds.Count == 0) return false;` di Elemen Teknis predicate.
    - grep `correctAnswerText = "Dinilai manual"` match (D-07).
    - grep `IsEssayPending = (question.QuestionType ?? "MultipleChoice") == "Essay"` match (D-06 broaden — TIDAK lagi `assessment.Status == ... PendingGrading`).
  </acceptance_criteria>
  <done>3 site + IsEssayPending memanggil helper; guard Count==0 dihapus di review-off & ET; D-07 essay UserAnswer di-set ke TextAnswer + CorrectAnswer "Dinilai manual"; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Render blok jawaban essay di Results.cshtml (ECG-04 view-side) + regression test (ECG-02/03)</name>
  <files>Views/CMP/Results.cshtml, HcPortal.Tests/ResultsEssayCorrectnessTests.cs</files>
  <read_first>
    - Views/CMP/Results.cshtml:320-396 (review row — badge L333-350 sudah benar; options loop L354-391; UserAnswer TIDAK pernah dirujuk = GAP D-07/Pitfall 1)
    - .planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md (section Results.cshtml — blok Razor baru baris 201-211)
    - HcPortal.Tests/IsQuestionCorrectTests.cs (Plan 01 — pola test pure, builder Q/Resp)
  </read_first>
  <action>
    **Bagian A — View (ECG-04 view-side, D-07):** Edit `Views/CMP/Results.cshtml`. Tambah blok Razor di dalam review row, SETELAH options loop `</div>` (akhir blok options ~L391) dan SEBELUM penutup `</div>` baris review item (~L392), yang me-render teks jawaban essay (untuk soal tanpa Options, mis. essay):
    ```cshtml
    @if (!question.Options.Any() && !string.IsNullOrEmpty(question.UserAnswer))
    {
        <div class="list-group">
            <div class="list-group-item">
                <small class="text-muted d-block">Jawaban Anda:</small>
                <span>@question.UserAnswer</span>
                <small class="text-muted d-block mt-2">@question.CorrectAnswer</small>
            </div>
        </div>
    }
    ```
    Gunakan gaya `list-group-item` agar konsisten dengan options loop existing. JANGAN ubah blok badge L333-350 (sudah benar — IsEssayPending/IsCorrect/Salah).

    **Bagian B — Regression test (ECG-02/03, pure unit):** Buat `HcPortal.Tests/ResultsEssayCorrectnessTests.cs`. COPY builder `Q`/`Resp` verbatim dari `AssessmentScoreAggregatorTests.cs:20-30` (sama Plan 01). Tulis test yang me-mirror logika count CMPController (loop soal, sum `IsQuestionCorrect==true`) dan Elemen Teknis (group by ElemenTeknis, count `IsQuestionCorrect==true`). Builder `Q` perlu set `ElemenTeknis` untuk test ET — gunakan inisialisasi object langsung bila builder Q tak punya param ElemenTeknis (set `.ElemenTeknis` setelah Q(...) atau buat helper Qet lokal). Header file copy gaya AssessmentScoreAggregatorTests.cs:1-7 (`using System.Collections.Generic; using System.Linq; using HcPortal.Helpers; using HcPortal.Models; using Xunit; namespace HcPortal.Tests;`). Test wajib:
    ```csharp
    [Fact]
    public void Count_4Mc_2GradedEssay_Equals6()  // ECG-02
    {
        var questions = new[] {
            Q(1,"MultipleChoice",10,(10,true),(11,false)),
            Q(2,"MultipleChoice",10,(20,true),(21,false)),
            Q(3,"MultipleChoice",10,(30,true),(31,false)),
            Q(4,"MultipleChoice",10,(40,true),(41,false)),
            Q(5,"Essay",100), Q(6,"Essay",100)
        };
        var responses = new[] {
            Resp(1,optId:10),Resp(2,optId:20),Resp(3,optId:30),Resp(4,optId:40),
            Resp(5,essay:80),Resp(6,essay:50)
        };
        int correctCount = questions.Count(q =>
            AssessmentScoreAggregator.IsQuestionCorrect(q, responses.Where(r => r.PackageQuestionId == q.Id)) == true);
        Assert.Equal(6, correctCount); // 6/6 — bukan 4/6
    }

    [Fact]
    public void ElemenTeknis_GroupCountsEssay()  // ECG-03
    {
        var q1 = Q(1,"MultipleChoice",10,(10,true),(11,false)); q1.ElemenTeknis = "Teknik A";
        var q2 = Q(2,"Essay",100); q2.ElemenTeknis = "Teknik A";
        var questions = new[] { q1, q2 };
        var responses = new[] { Resp(1,optId:10), Resp(2,essay:75) };
        var groups = questions.GroupBy(q => q.ElemenTeknis);
        var teknikA = groups.First(g => g.Key == "Teknik A");
        int correct = teknikA.Count(q =>
            AssessmentScoreAggregator.IsQuestionCorrect(q, responses.Where(r => r.PackageQuestionId == q.Id)) == true);
        Assert.Equal(2, correct); // MC benar + essay benar
    }

    [Fact]
    public void Essay_ScoreZero_NotCounted()  // ECG-02 boundary
    {
        var q = Q(1,"Essay",100);
        var responses = new[] { Resp(1,essay:0) };
        Assert.False(AssessmentScoreAggregator.IsQuestionCorrect(q, responses) == true);
    }
    ```
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ResultsEssayCorrectness" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 error.
    - grep `Jawaban Anda:` di Views/CMP/Results.cshtml match (blok essay answer ada).
    - grep `question.UserAnswer` di Views/CMP/Results.cshtml match (sebelumnya TIDAK ada — Pitfall 1 closed).
    - File `HcPortal.Tests/ResultsEssayCorrectnessTests.cs` ada; grep `Count_4Mc_2GradedEssay_Equals6` + `ElemenTeknis_GroupCountsEssay` match.
    - `dotnet test --filter "FullyQualifiedName~ResultsEssayCorrectness"` SEMUA hijau (≥3 passed, 0 failed), termasuk count==6.
  </acceptance_criteria>
  <done>Blok essay answer ter-render di view; regression test count==6 + ET-counts-essay hijau; build 0 error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT runtime CMP/Results — 6/6 + badge essay + teks jawaban (ECG-04 view, Phase 354 lesson)</name>
  <files>Views/CMP/Results.cshtml (runtime verification only)</files>
  <action>Checkpoint verifikasi runtime — lihat what-built + how-to-verify. Tidak ada perubahan kode di task ini; semua kode sudah ditulis Task 1+2.</action>
  <what-built>
    Wiring `CMP/Results` lewat helper `IsQuestionCorrect`: count "(X/Y benar)" + Elemen Teknis + badge Tinjauan kini essay-aware, plus blok view yang me-render teks jawaban essay worker. Verifikasi RUNTIME wajib (Razor dynamic — grep+build tak cukup, pelajaran Phase 354).
  </what-built>
  <how-to-verify>
    1. Jalankan lokal sesuai CLAUDE.md dev workflow: `dotnet run` (set `Authentication__UseActiveDirectory=false` bila perlu login lokal, pelajaran Phase 355).
    2. Login sebagai admin lokal: `admin@pertamina.com` (roleLevel ≤3, lolos authz REC-04 — RESEARCH Pitfall 3).
    3. Buka `http://localhost:5277/CMP/Results/166` (sesi N MC + 2 essay graded fully). Bila id 166 TIDAK ada di DB lokal (RESEARCH A2/Open Question 1): cari sesi essay lain yang sudah finalize via DB lokal, ATAU seed sesi temporary sesuai SEED_WORKFLOW (snapshot DB dulu, restore setelah, catat di SEED_JOURNAL.md). JANGAN tinggalkan seed nempel.
    4. Verifikasi:
       a. Count di header menampilkan "(6/6 benar)" (atau N+2/N+2 sesuai data) — BUKAN "(4/6 benar)".
       b. Badge Tinjauan untuk Soal essay yang dinilai >0 = hijau "Benar"; essay =0 = merah "Salah"; essay belum dinilai = abu "Menunggu Penilaian".
       c. Di baris Tinjauan soal essay tampil "Jawaban Anda: {teks essay worker}" + "Dinilai manual" (BUKAN baris kosong).
       d. Breakdown "Elemen Teknis" menghitung essay benar di elemennya (tidak 0 untuk grup yang punya essay benar).
    5. (Opsional, bila SEED dipakai) restore DB lokal + tandai SEED_JOURNAL.md cleaned.
  </how-to-verify>
  <verify>
    <automated>MISSING — verifikasi runtime manual (Razor dynamic, Phase 354 lesson). Unit-level sudah dicakup ResultsEssayCorrectnessTests (Task 2).</automated>
  </verify>
  <done>Runtime UAT approved: 6/6 + badge essay hijau/merah/pending sesuai nilai + teks jawaban essay tampil + ET hitung essay benar.</done>
  <resume-signal>Ketik "approved" bila 6/6 + badge essay hijau + teks jawaban tampil + ET benar; atau jelaskan masalahnya (mis. baris kosong → blok view perlu cek, atau count masih salah → site mana).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CMPController.Results(id) | User minta hasil sesi by id. Authz REC-04 (IsResultsAuthorized) di L2192-2193 — owner / roleLevel≤3 / L4 section-scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-383-03 | Information Disclosure (IDOR baca hasil orang lain) | `CMPController.Results` | accept (no change) | `IsResultsAuthorized` (L2192-2193, REC-04) sudah ada + dikunci `ResultsAuthorizationTests`. Plan ini hanya ubah body di BAWAH guard authz — tidak menyentuh authz. |
| T-383-04 | Information Disclosure (essay TextAnswer terekspos) | Results.cshtml blok essay answer | accept | TextAnswer = jawaban worker sendiri, hanya tampil di Results yang sudah lewat authz REC-04. Tidak ada data lintas-user baru. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "FullyQualifiedName~ResultsEssayCorrectness"` hijau (count==6, ET counts essay).
- grep konfirmasi 3 site + IsEssayPending lewat helper; guard Count==0 dihapus; view render UserAnswer.
- Runtime UAT (checkpoint): 6/6 + badge essay benar + teks essay tampil + ET benar di `CMP/Results/{id}`.
</verification>

<success_criteria>
- ECG-02: count "(X/Y benar)" sertakan essay benar di kedua jalur (review-on + review-off).
- ECG-03: Elemen Teknis hitung essay sesuai nilai HC.
- ECG-04: badge essay Benar/Salah/Pending sesuai nilai (terlepas status); TextAnswer tampil di baris review.
- Kill-drift: tidak ada recompute correctness inline tersisa di Results — semua lewat helper.
- Regression test pure-unit hijau + UAT runtime approved.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/383-essay-grading-correctness-test-fase-1/383-02-SUMMARY.md` (ringkas: 4 site yang di-rewire, blok view, hasil test, hasil UAT runtime + id sesi yang dipakai/seed).
</output>
