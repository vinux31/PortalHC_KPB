---
phase: 383-essay-grading-correctness-test-fase-1
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [ECG-05]
must_haves:
  truths:
    - "PDF export per-peserta menandai soal essay benar/salah dengan aturan yang sama seperti web Results (EssayScore > 0)"
    - "Aturan lama PDF (EssayScore >= ScoreValue/2) digantikan oleh helper IsQuestionCorrect (D-03 unify)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "PDF essay correctness via IsQuestionCorrect (GeneratePerPesertaPdf)"
      contains: "AssessmentScoreAggregator.IsQuestionCorrect"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs GeneratePerPesertaPdf"
      to: "AssessmentScoreAggregator.IsQuestionCorrect"
      via: "ganti threshold essay >= ScoreValue/2 dengan helper"
      pattern: "AssessmentScoreAggregator\\.IsQuestionCorrect"
---

<objective>
Unify aturan correctness essay di PDF export (`AssessmentAdminController.GeneratePerPesertaPdf`, ~L5017) ke helper `IsQuestionCorrect` (Plan 01) — mengganti threshold lama `EssayScore >= ScoreValue/2` dengan aturan `> 0`, agar PDF dan web Results memakai satu aturan (D-03). Perubahan behavior PDF disengaja.

Purpose: Menutup ECG-05. File berbeda dari Plan 02 (AssessmentAdminController.cs vs CMPController.cs) → wave 2 paralel-aman dengan Plan 02. Tidak menyentuh SubmitEssayScore/FinalizeEssayGrading (D-05 lock-with-tests, Plan 04).

Output: 1 perubahan kecil di `Controllers/AssessmentAdminController.cs` (PDF essay branch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-CONTEXT.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-RESEARCH.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md

# CLAUDE.md: dotnet build wajib. Branch ITHandoff.

<interfaces>
<!-- Kontrak diekstrak dari codebase — executor tidak perlu eksplorasi. -->

Helper dari Plan 01:
```csharp
public static bool? IsQuestionCorrect(PackageQuestion q, IEnumerable<PackageUserResponse> responsesForQ);
// essay: EssayScore > 0 → true, == 0 → false, null → null (pending)
```

GeneratePerPesertaPdf context (verified, AssessmentAdminController.cs):
- `sessionQuestions` di-load `.Include(q => q.Options)` (L4858-4861) → helper MC/MA aman dipanggil.
- `sessionResponses` = semua respons sesi; per-soal: `sessionResponses.Where(r => r.PackageQuestionId == q.Id)`.
- Loop per soal (L5001-5037): `var resp = sessionResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id);` lalu set `jawaban` + `bool? correct`.
- `statusColor`/`statusText` mapping (L5021-5024): Green/Red/Grey + "✓ Benar"/"✗ Salah"/"— Pending" — cocok langsung dengan bool? (TIDAK diubah).

AssessmentAdminController.cs SUDAH punya `using HcPortal.Helpers;` (L13) — tidak perlu tambah.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ganti essay correctness PDF ke IsQuestionCorrect (ECG-05, D-03)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:4985-5044 (GeneratePerPesertaPdf loop per-soal — branch essay L5014-5017, statusColor/Text L5021-5024)
    - .planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md (section AssessmentAdminController PDF — baris 161-175)
    - Helpers/AssessmentScoreAggregator.cs (helper IsQuestionCorrect dari Plan 01)
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs` di `GeneratePerPesertaPdf`, branch essay (L5014-5017). Saat ini:
    ```csharp
    else if (!string.IsNullOrEmpty(resp.TextAnswer))
    {
        jawaban = resp.TextAnswer.Length > 300 ? resp.TextAnswer.Substring(0, 300) + "..." : resp.TextAnswer;
        correct = resp.EssayScore.HasValue ? (bool?)(resp.EssayScore.Value >= (q.ScoreValue / 2)) : null;
    }
    ```
    Ganti HANYA baris perhitungan `correct` (baris terakhir blok) menjadi:
    ```csharp
        correct = AssessmentScoreAggregator.IsQuestionCorrect(q, sessionResponses.Where(r => r.PackageQuestionId == q.Id));
    ```
    Pertahankan baris `jawaban = ...` (truncate 300 char) apa adanya. JANGAN ubah `statusColor`/`statusText` (L5021-5024) — bool? cocok langsung (true→Green "✓ Benar", false→Red "✗ Salah", null→Grey "— Pending").
    JANGAN sentuh `SubmitEssayScore` (L3458-3487) atau `FinalizeEssayGrading` (L3499+) — itu D-05 lock-only (Plan 04).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 error (grep "Build succeeded").
    - grep `correct = AssessmentScoreAggregator.IsQuestionCorrect(q, sessionResponses.Where(r => r.PackageQuestionId == q.Id))` match di AssessmentAdminController.cs.
    - grep NEGATIF: `resp.EssayScore.Value >= (q.ScoreValue / 2)` TIDAK lagi ada di AssessmentAdminController.cs (threshold lama dihapus).
    - grep `jawaban = resp.TextAnswer.Length > 300` masih ada (truncate dipertahankan).
    - grep NEGATIF: tidak ada perubahan pada `SubmitEssayScore` / `FinalizeEssayGrading` (grep `response.EssayScore = score;` masih ada utuh, `ExecuteUpdateAsync` di Finalize masih ada).
  </acceptance_criteria>
  <done>PDF essay correctness pakai IsQuestionCorrect (essay >0); threshold lama >= ScoreValue/2 hilang; statusColor/Text + truncate utuh; Submit/Finalize tak disentuh; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin → GeneratePerPesertaPdf | Export PDF detail jawaban per peserta. Action di controller [Authorize(Roles="Admin, HC")] (existing). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-383-05 | Information Disclosure | `GeneratePerPesertaPdf` | accept (no change) | Authz attribute existing tidak diubah. Plan hanya ganti perhitungan correctness essay di dalam loop render — tidak menyentuh authz / akses data. |
| T-383-06 | Tampering (drift PDF vs web) | aturan correctness essay | mitigate | Unify ke satu helper `IsQuestionCorrect` (D-03) — PDF & web tak bisa lagi divergen. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- grep konfirmasi helper dipakai + threshold lama hilang + Submit/Finalize utuh.
- (Opsional manual, VALIDATION manual-only) export PDF sesi essay graded → correctness essay sama dengan web Results. QuestPDF render bisa env-blocked lokal (Phase 327) — bila terblok, helper-rule sudah dikunci unit test Plan 01 (essay >0).
</verification>

<success_criteria>
- ECG-05: PDF export essay correctness = `> 0` lewat helper IsQuestionCorrect, konsisten dengan web Results (D-03 unify).
- Threshold lama `>= ScoreValue/2` digantikan.
- SubmitEssayScore / FinalizeEssayGrading tak disentuh.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/383-essay-grading-correctness-test-fase-1/383-03-SUMMARY.md` (ringkas: baris yang diganti, konfirmasi threshold lama hilang + Submit/Finalize utuh).
</output>
