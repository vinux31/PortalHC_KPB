---
phase: 384-monitoring-essay-grading-ui-refactor-fase-2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/sql/essay-grading-384-seed.sql
  - tests/e2e/essay-grading-384.spec.ts
autonomous: true
requirements: [UIG-04]
must_haves:
  truths:
    - "Seed SQL menanam 1 AssessmentSession PendingGrading + HasManualGrading dengan ≥1 essay question + UserPackageAssignment + PackageUserResponse (TextAnswer terisi, EssayScore NULL)"
    - "Spec Playwright FLOW 384 ada dengan struktur describe + snapshot/seed/restore harness, status RED (test bodies stub/skip menunggu UI Wave 1)"
    - "Snapshot DB diambil sebelum seed dan restore di afterAll (SEED_WORKFLOW)"
  artifacts:
    - path: "tests/sql/essay-grading-384-seed.sql"
      provides: "Fixture session essay-pending untuk UIG-04 (title prefix [ESSAY384])"
      contains: "[ESSAY384]"
    - path: "tests/e2e/essay-grading-384.spec.ts"
      provides: "FLOW 384 describe + harness (RED stub)"
      contains: "essay-grading-384-seed.sql"
  key_links:
    - from: "tests/e2e/essay-grading-384.spec.ts"
      to: "tests/sql/essay-grading-384-seed.sql"
      via: "db.execScript path resolve"
      pattern: "execScript.*essay-grading-384-seed"
    - from: "tests/e2e/essay-grading-384.spec.ts"
      to: "helpers/dbSnapshot"
      via: "backup/restore around describe"
      pattern: "db\\.(backup|restore)"
---

<objective>
Bangun infrastruktur test UIG-04 LEBIH DULU (konvensi repo: test infra di Wave 0 — Phase 307/313): SQL seed fixture untuk 1 session essay-pending + spec Playwright FLOW 384 dengan harness snapshot→seed→restore. Test bodies sengaja STUB/RED — UI yang membuatnya hijau dibangun di Wave 1 (Plan 02/03) dan diisi penuh + dijalankan hijau di Wave 3 (Plan 04).

Purpose: Mengunci kontrak e2e (selector, route, flow) sebelum implementasi, sehingga executor Wave 1 punya target perilaku yang jelas (Razor dynamic → runtime wajib, pelajaran Phase 354).
Output: `tests/sql/essay-grading-384-seed.sql` + `tests/e2e/essay-grading-384.spec.ts` (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-RESEARCH.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-PATTERNS.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-CONTEXT.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md

<interfaces>
<!-- Endpoint kontrak yang akan diuji round-trip (BACKEND TAK DIUBAH — reuse) -->
GET  /Admin/EssayGrading?sessionId={id}&title=&category=&scheduleDate=yyyy-MM-dd&assessmentType=
POST /Admin/SubmitEssayScore   (sessionId, questionId, score)   -> { success, pendingCount, allGraded }
POST /Admin/FinalizeEssayGrading (sessionId)                    -> { success } | { success, alreadyFinalized, message, nomorSertifikat }

Selector yang akan di-assert (HARUS match markup Wave 1):
  tabel worker-list: a:has-text("Tinjau Essay"), badge bg-warning text-dark "{N} belum dinilai"
  page per-worker:   h2 (UserFullName), .essay-grading-card, .essay-score-input,
                     #badge_{sessionId}_{questionId}, .btn-save-essay-score,
                     #finalizeSection_{sessionId}, .btn-finalize-grading

Status enum yang dipakai seed (cek AssessmentConstants.AssessmentStatus):
  PendingGrading = "Menunggu Penilaian"   (agar FinalizeEssayGrading lolos gate)
  Completed      = "Completed"             (untuk fixture read-only D-10 opsional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL seed fixture session essay-pending</name>
  <read_first>
    - tests/sql/pending345-seed.sql (analog seed shape — INSERT chain + Title prefix + cleanup query)
    - tests/sql/cmp350-seed.sql (analog seed dengan AssessmentSession + response)
    - Models/AssessmentMonitoringViewModel.cs (field EssayGradingItemViewModel + MonitoringSessionViewModel)
    - Controllers/AssessmentAdminController.cs method EssayGradingMap builder (cari by method name AssessmentMonitoringDetail; baca blok "Essay grading items per session" — confirm: butuh UserPackageAssignment.ShuffledQuestionIds berisi essay question id + PackageQuestion QuestionType='Essay' + PackageUserResponse)
    - Constants/AssessmentConstants.cs (nilai literal AssessmentStatus.PendingGrading + Completed — confirm string value)
    - docs/SEED_WORKFLOW.md (klasifikasi + format + command)
  </read_first>
  <action>
    Buat `tests/sql/essay-grading-384-seed.sql` mengikuti pola `pending345-seed.sql` (hierarchical INSERT, FK-respecting, Title prefix untuk cleanup). Klasifikasi seed = `temporary + local-only`.

    Fixture WAJIB menanam (minimal 1 session "PendingGrading" untuk flow utama):
    1. Cleanup chain di AWAL (idempotent re-run): DELETE child→parent untuk Title LIKE '[ESSAY384]%' (PackageUserResponses → UserPackageAssignments → PackageOptions → PackageQuestions → AssessmentPackages → AssessmentSessions), urutan FK-safe seperti pending345-seed.
    2. 1 `AssessmentSession`: `Title='[ESSAY384] Penilaian Essay UIG-04'`, `Category` (pilih kategori valid, mis. 'Operasional' atau samakan pola seed lain), `Schedule = CAST(GETDATE() AS DATE)` (atau pola tz-safe seperti seed existing), `Status = 'Menunggu Penilaian'` (= AssessmentConstants.AssessmentStatus.PendingGrading — CONFIRM literal value dari read_first; gate FinalizeEssayGrading `:3524`), `HasManualGrading = 1`, `NomorSertifikat = NULL`, `UserId` = user worker existing (pola pending345 pakai 'rino.prasetyo@pertamina.com'; reuse user yang sama). Pakai OUTPUT INTO untuk capture session Id (pola pending345/313.1).
    3. 1 `AssessmentPackage` untuk session itu + 1 `PackageQuestion` `QuestionType='Essay'`, `QuestionText='[ESSAY384] Jelaskan proses alkylation.'`, `ScoreValue=10`, `Rubrik` terisi opsional, `ImagePath=NULL`.
    4. 1 `UserPackageAssignment` (AssessmentSessionId=session, AssessmentPackageId=package) dengan `ShuffledQuestionIds` berisi essay question id (CONFIRM format kolom ShuffledQuestionIds dari seed existing / model — biasanya CSV/JSON string; ikuti format yang `GetShuffledQuestionIds()` parse).
    5. 1 `PackageUserResponse`: `AssessmentSessionId=session`, `PackageQuestionId=essayQ`, `TextAnswer='[ESSAY384] Alkylation menggabungkan isobutane dengan olefin.'`, `EssayScore = NULL` (ungraded → muncul sebagai "belum dinilai").
    6. (OPSIONAL fixture kedua read-only D-10) — JIKA mudah: 1 session kedua `Title='[ESSAY384-RO] ...'`, `Status='Completed'`, `NomorSertifikat='CERT-ESSAY384-RO'`, EssayScore terisi. Boleh dilewati jika menambah kompleksitas; flow utama hanya butuh fixture PendingGrading.

    JANGAN masukkan ke `Data/SeedData.cs` (itu permanent prod). Ini local-only test fixture.
    Tambah komentar header SQL: tujuan + klasifikasi temporary + Title prefix cleanup.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'tests/sql/essay-grading-384-seed.sql' -Pattern '\[ESSAY384\]','HasManualGrading','Menunggu Penilaian','QuestionType' -SimpleMatch -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/sql/essay-grading-384-seed.sql` ada
    - Berisi string `[ESSAY384]` (Title prefix untuk cleanup)
    - Berisi `HasManualGrading = 1`
    - Berisi `Status` = literal PendingGrading (`Menunggu Penilaian`)
    - Berisi INSERT ke `PackageUserResponses` dengan `EssayScore` NULL untuk essay (kolom ungraded)
    - Berisi INSERT `UserPackageAssignments` dengan `ShuffledQuestionIds`
    - Cleanup DELETE chain di awal untuk `Title LIKE '[ESSAY384]%'`
    - TIDAK mengubah `Data/SeedData.cs`
  </acceptance_criteria>
  <done>Seed SQL fixture self-contained untuk session essay-pending, idempotent re-run, klasifikasi temporary+local-only terdokumentasi.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright FLOW 384 spec (RED stub + harness)</name>
  <read_first>
    - tests/e2e/assessment-pending-grade.spec.ts (CLONE harness: describe.configure serial + beforeAll backup→execScript→Layer1 assert + afterAll restore→Layer4 assert + loginAny inline)
    - tests/e2e/assessment.spec.ts blok FLOW 9 (selector essay finalize: .btn-finalize-grading, waitForResponse FinalizeEssayGrading, dialog.accept)
    - tests/helpers/dbSnapshot.ts (signature backup/restore/execScript/queryString/queryScalar)
    - tests/helpers/accounts.ts (key 'admin' creds)
    - tests/sql/essay-grading-384-seed.sql (Task 1 — title prefix + entity untuk resolve scheduleDate/sessionId)
    - tests/playwright.config.ts (baseURL localhost:5277, fullyParallel false)
  </read_first>
  <action>
    Buat `tests/e2e/essay-grading-384.spec.ts`. CLONE harness dari `assessment-pending-grade.spec.ts`:
    - `test.describe.configure({ mode: 'serial' })`
    - inline `loginAny(page, 'admin')` (pola `:25-34`)
    - `beforeAll`: resolve InstanceDefaultBackupPath → `db.backup(snapshotPath)` → `db.execScript('../sql/essay-grading-384-seed.sql')` → Layer 1 assert `COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[[]ESSAY384]%' > 0`. Resolve `scheduleDate = CONVERT(varchar(10), Schedule, 23)` + `title`/`category` + `sessionId` dari DB (hindari tz drift — pola `:65-67`).
    - `afterAll`: `db.restore(snapshotPath)` dalam try + Layer 4 assert `COUNT == 0` (DB bersih). Pola `:70-83`.

    Test bodies = STUB/RED (UI belum ada di Wave 0). Buat describe `FLOW 384 — Monitoring Essay UI Refactor (UIG-04)` dengan test cases ber-`test.fixme()` ATAU body yang sengaja gagal dengan komentar `// RED: diisi penuh di Plan 04 setelah Wave 1 UI ada`:
      1. `UIG-01 tabel worker-list render + badge pending` — goto `/Admin/AssessmentMonitoringDetail?title&category&scheduleDate` → (stub) assert `a:has-text("Tinjau Essay")` visible + badge `.badge.bg-warning` hasText `belum dinilai`.
      2. `UIG-02 Tinjau Essay navigasi ke page per-worker` — click `a:has-text("Tinjau Essay")` → `waitForURL(/EssayGrading/)` → assert `h2` (nama worker) + `.essay-grading-card`.
      3. `UIG-03 Simpan Skor + Selesaikan round-trip` — fill `.essay-score-input` → click `.btn-save-essay-score` → `waitForResponse(/SubmitEssayScore/ 200)` → assert `#badge_{sid}_{qid}` text "Sudah Dinilai" (`bg-success`); lalu `page.on('dialog', d=>d.accept())` + click `.btn-finalize-grading` → `waitForResponse(/FinalizeEssayGrading/ 200)`.
      4. `UIG-04 D-09 in-place "Selesai", URL tetap /EssayGrading` — setelah finalize, assert URL masih mengandung `/EssayGrading` (BUKAN redirect/reload ke monitoring) + input `.essay-score-input` disabled.

    Gunakan `test.fixme(...)` untuk 4 test agar suite TIDAK gagal di CI Wave 0 (RED-tertunda terkontrol) TAPI struktur + assertion sudah final. Plan 04 menghapus `.fixme` dan menjalankan hijau. Helper assertion (selector constant) boleh di-extract ke top-of-file const agar Plan 04 tinggal pakai.

    PENTING (Pitfall 5 / memory Phase 355): tulis komentar header bahwa app harus running via `dotnet run` dengan `Authentication__UseActiveDirectory=false` + run `--workers=1` (NTLM loopback/shared-memory). `playwright.config.ts` tak punya webServer → app tak auto-start.
  </action>
  <verify>
    <automated>powershell -Command "Push-Location tests; npx tsc --noEmit -p tsconfig.json 2>&1 | Select-Object -First 30; Pop-Location"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/essay-grading-384.spec.ts` ada
    - Berisi `essay-grading-384-seed.sql` (execScript reference)
    - Berisi `db.backup` dan `db.restore` (snapshot harness)
    - Berisi 4 test mengcover UIG-01..04 dengan selector: `Tinjau Essay`, `.essay-grading-card`, `.btn-save-essay-score`, `.btn-finalize-grading`, `/EssayGrading`
    - 4 test memakai `test.fixme` (RED-tertunda) ATAU jelas-stub dengan komentar Plan 04
    - TypeScript compile bersih (`tsc --noEmit` exit 0)
    - Komentar menyebut `Authentication__UseActiveDirectory=false` + `--workers=1`
  </acceptance_criteria>
  <done>Spec FLOW 384 ada dengan harness lengkap + 4 assertion final (fixme/RED), TS compile bersih, siap diaktifkan Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test seed → DB lokal | Seed menulis ke HcPortalDB_Dev lokal; di-snapshot sebelum + restore sesudah (SEED_WORKFLOW) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-384-01-T | Tampering | seed pollutes DB lokal permanen | mitigate | snapshot (`db.backup`) sebelum seed + `db.restore` di `afterAll` finally + Layer 4 assert COUNT==0; klasifikasi temporary+local-only (CLAUDE.md Seed Workflow) |
| T-384-01-I | Information Disclosure | fixture berisi data sensitif | accept | data dummy `[ESSAY384]` non-PII, local-only test DB, tidak di-commit ke prod seed |
</threat_model>

<verification>
- `tests/sql/essay-grading-384-seed.sql` ada + berisi marker `[ESSAY384]` + HasManualGrading + PendingGrading + essay response EssayScore NULL.
- `tests/e2e/essay-grading-384.spec.ts` ada + harness snapshot/restore + 4 assertion (fixme/RED).
- `dotnet build` 0 error (tidak ada perubahan C# di plan ini — sanity).
- TypeScript: `npx tsc --noEmit` di `tests/` bersih.
- Seed BELUM dijalankan ke DB di plan ini (hanya artefak SQL + spec). Eksekusi e2e penuh + snapshot/restore di Plan 04.
</verification>

<success_criteria>
- Seed fixture self-contained untuk 1 session essay-pending (UIG-04) tersedia.
- Spec FLOW 384 dengan 4 assertion final (RED/fixme) + harness snapshot/restore tersedia.
- TypeScript compile bersih.
- 0 perubahan backend / view (test infra murni).
</success_criteria>

<output>
After completion, create `.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-01-SUMMARY.md`
</output>
