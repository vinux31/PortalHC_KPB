---
phase: 384-monitoring-essay-grading-ui-refactor-fase-2
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
autonomous: true
requirements: [UIG-01]
must_haves:
  truths:
    - "Blok essay inline panjang (:381-481, stacked cards per-worker) DIGANTI tabel worker-list ringkas"
    - "Tabel hanya menampilkan worker HasManualGrading==true, urut by UserNIP, dengan kolom Worker/NIP, Essay Belum Dinilai, Status badge 3-state, tombol Tinjau Essay"
    - "Tombol Tinjau Essay navigasi GET ke /Admin/EssayGrading dengan 4 nav param (sessionId/title/category/scheduleDate/assessmentType)"
    - "Guard essayGradingMap.Any() dipertahankan (section hidden bila tak ada worker beressay)"
    - "Handler AJAX inline (.btn-save-essay-score/.btn-finalize-grading + showAlert essay) yang kini dead DIHAPUS dari view ini"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Tabel worker-list (ganti inline block) + tombol Tinjau Essay"
      contains: "Tinjau Essay"
  key_links:
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      to: "/Admin/EssayGrading"
      via: "@Url.Action EssayGrading 4 param"
      pattern: "Url\\.Action\\(\"EssayGrading\""
    - from: "tabel worker-list badge"
      to: "Phase 310 D-02 finalize gate"
      via: "Status==Completed && NomorSertifikat!=null"
      pattern: "AssessmentStatus\\.Completed"
---

<objective>
Ganti blok essay inline panjang (`AssessmentMonitoringDetail.cshtml:381-481`, stacked cards per-worker) dengan **tabel worker-list ringkas** (UIG-01): kolom Worker+NIP, Essay Belum Dinilai, badge status 3-state (D-04), tombol "Tinjau Essay" (navigasi ke page per-worker dari Plan 02). Hapus handler AJAX essay inline yang menjadi dead setelah refactor.

Purpose: HC lihat daftar worker beressay ringkas (status + jumlah pending) lalu klik "Tinjau Essay" untuk masuk page per-worker (Plan 02), bukan scroll kartu numpuk.
Output: 1 view dimodifikasi (blok diganti + handler dead dihapus).

Depends on Plan 02: tombol "Tinjau Essay" me-link ke GET action `EssayGrading` yang dibuat di Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-PATTERNS.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-UI-SPEC.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Tabel sesi existing (MIRROR pola), data source, gate, route target Plan 02 -->

Pola tabel sesi existing (mirror struktur) — AssessmentMonitoringDetail.cshtml:218-231:
  <div class="table-responsive">
    <table class="table table-hover align-middle mb-0">
      <thead class="table-light"><tr><th>...</th></tr></thead>
      <tbody>...</tbody>

Guard yang DIPERTAHANKAN (:382-385):
  var essayGradingMap = ViewBag.EssayGradingMap as Dictionary<int, List<HcPortal.Models.EssayGradingItemViewModel>>;
  @if (essayGradingMap != null && essayGradingMap.Any()) { ... }

Data source + urut (D-02/D-03):
  Model.Sessions.Where(x => x.HasManualGrading).OrderBy(x => x.UserNIP)

Field MonitoringSessionViewModel: Id, UserFullName, UserNIP, EssayPendingCount, Status, NomorSertifikat

Badge 3-state (D-04) — REUSE Phase 310 D-02 gate verbatim:
  isFinalized = s.Status == AssessmentConstants.AssessmentStatus.Completed && !string.IsNullOrEmpty(s.NomorSertifikat)
  🟡 EssayPendingCount > 0                -> badge bg-warning text-dark  "{N} belum dinilai"
  🔵 EssayPendingCount == 0 && !isFinalized -> badge bg-info             "Siap difinalisasi"
  🟢 isFinalized                          -> badge bg-success            "Selesai"

Route target (Plan 02) — carry 4 nav param (Pitfall 3/4):
  @Url.Action("EssayGrading", new {
      sessionId = s.Id, title = Model.Title, category = Model.Category,
      scheduleDate = Model.Schedule.ToString("yyyy-MM-dd"), assessmentType = ViewBag.AssessmentType })

ViewBag.AssessmentType di-set action (existing); Model.Schedule format yyyy-MM-dd (pola hidden field :489).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ganti blok essay inline :381-481 dengan tabel worker-list</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml baris 381-489 (BLOK YANG DIGANTI: essayGradingMap guard + stacked cards + finalizeSection + antiforgeryForm + hidden fields). Confirm batas blok yang diganti = HANYA card stacked per-worker (:387-480 dalam guard); guard `@if` + `<div class="mt-4"><h5>Penilaian Essay</h5>` dipertahankan; antiforgeryForm `:484` + hidden fields `:486-489` di LUAR blok DIPERTAHANKAN.
    - Views/Admin/AssessmentMonitoringDetail.cshtml baris 218-260 (pola tabel sesi existing: table-responsive + table table-hover align-middle + thead table-light + dropdown aksi min-height:40px)
    - .planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-UI-SPEC.md Section A (kolom + copy + badge)
  </read_first>
  <action>
    Di dalam guard `@if (essayGradingMap != null && essayGradingMap.Any())` (`:385`), GANTI seluruh isi (`<div class="mt-4">...stacked cards...</div>` `:387-480`) dengan tabel worker-list. PERTAHANKAN guard `@if` + struktur `essayGradingMap` declaration (`:382-385`). Hasil:

    ```cshtml
    @if (essayGradingMap != null && essayGradingMap.Any())
    {
        <div class="mt-4">
            <h5 class="fw-semibold mb-3"><i class="bi bi-pencil-square me-2 text-warning"></i>Penilaian Essay</h5>
            <div class="card border-0 shadow-sm">
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Worker</th>
                                    <th class="text-center">Essay Belum Dinilai</th>
                                    <th class="text-center">Status</th>
                                    <th class="text-end">Aksi</th>
                                </tr>
                            </thead>
                            <tbody>
                            @foreach (var s in Model.Sessions.Where(x => x.HasManualGrading).OrderBy(x => x.UserNIP))
                            {
                                var isFinalized = s.Status == AssessmentConstants.AssessmentStatus.Completed
                                                  && !string.IsNullOrEmpty(s.NomorSertifikat);
                                <tr>
                                    <td class="fw-semibold">
                                        @s.UserFullName<br>
                                        <small class="text-muted">@s.UserNIP</small>
                                    </td>
                                    <td class="text-center">@(s.EssayPendingCount > 0 ? s.EssayPendingCount.ToString() : "—")</td>
                                    <td class="text-center">
                                        @if (s.EssayPendingCount > 0)
                                        {
                                            <span class="badge bg-warning text-dark">@s.EssayPendingCount belum dinilai</span>
                                        }
                                        else if (!isFinalized)
                                        {
                                            <span class="badge bg-info">Siap difinalisasi</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-success">Selesai</span>
                                        }
                                    </td>
                                    <td class="text-end">
                                        <a class="btn btn-primary btn-sm" style="min-height:40px;"
                                           href="@Url.Action("EssayGrading", new {
                                               sessionId = s.Id,
                                               title = Model.Title,
                                               category = Model.Category,
                                               scheduleDate = Model.Schedule.ToString("yyyy-MM-dd"),
                                               assessmentType = ViewBag.AssessmentType })">
                                            <i class="bi bi-pencil-square me-1"></i>Tinjau Essay
                                        </a>
                                    </td>
                                </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    }
    ```

    PENTING:
    - Badge 3-state EXACT copy (D-04): "{N} belum dinilai" / "Siap difinalisasi" / "Selesai". `bg-warning` WAJIB `text-dark` (a11y).
    - Tombol "Tinjau Essay" TETAP MUNCUL untuk worker finalized (D-10 — buka read-only).
    - JANGAN sentuh tabel sesi utama di atas (`~:218-378`), antiforgeryForm `:484`, hidden fields `:486-489` (dipakai polling surface lain).
    - Tabel sesi atas TIDAK lagi punya tombol/markup essay grading individu.
  </action>
  <verify>
    <automated>dotnet build --nologo 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentMonitoringDetail.cshtml` berisi `>Tinjau Essay<` dan `Url.Action("EssayGrading"` dengan param `sessionId`, `title`, `category`, `scheduleDate`, `assessmentType`
    - Berisi badge `bg-warning text-dark` "belum dinilai", `bg-info` "Siap difinalisasi", `bg-success` "Selesai"
    - Berisi `Model.Sessions.Where(x => x.HasManualGrading).OrderBy(x => x.UserNIP)`
    - Guard `@if (essayGradingMap != null && essayGradingMap.Any())` masih ada
    - `<form id="antiforgeryForm"` masih ada (DIPERTAHANKAN)
    - TIDAK lagi berisi stacked `essay-grading-card` di view ini (dipindah ke EssayGrading.cshtml)
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>Blok inline diganti tabel worker-list 4-kolom dengan badge 3-state + tombol Tinjau Essay; guard + antiforgery dipertahankan; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Hapus handler AJAX essay inline yang dead</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml baris 1450-1558 (showAlert helper + handler .btn-save-essay-score + .btn-finalize-grading — kini dead setelah Task 1). Confirm showAlert juga dipakai surface LAIN di view ini sebelum dihapus.
    - Views/Admin/AssessmentMonitoringDetail.cshtml (grep usage showAlert di seluruh file — jika dipakai regenToken/extra-time/lain, JANGAN hapus showAlert; hanya hapus 2 handler essay)
  </read_first>
  <action>
    Setelah Task 1, blok markup essay-grading hilang dari view ini → handler `.btn-save-essay-score` (`:1472-1517`) + `.btn-finalize-grading` (`:1520-1558`) menjadi DEAD (querySelectorAll match 0 elemen). HAPUS kedua handler tsb dari `@section Scripts` view ini (mereka sudah dipindah ke `wwwroot/js/essay-grading.js` di Plan 02).

    **Penanganan `showAlert` (`:1452-1468`):** GREP dulu seluruh file untuk pemakaian `showAlert(` LAIN (mis. regenToken, AddExtraTime, atau handler lain). 
    - JIKA `showAlert` masih dipakai surface lain di view ini → PERTAHANKAN `showAlert` helper, HANYA hapus 2 handler essay.
    - JIKA `showAlert` HANYA dipakai oleh 2 handler essay → boleh dihapus sekalian (dead). Konfirmasi via grep sebelum hapus.

    Verifikasi pasca-hapus (RESEARCH Pitfall 2 / OQ#1): grep `btn-save-essay-score` + `btn-finalize-grading` + `essay-grading-card` di `AssessmentMonitoringDetail.cshtml` → harus 0 match (semua pindah ke page baru). Ini mengonfirmasi handler finalize hidup TOTAL di `essay-grading.js` page baru dengan behavior in-place (D-09), tak ada dual-mode reload yang perlu di-handle.

    JANGAN hapus handler non-essay (regenToken, AddExtraTime, hub/polling, copyToken) — hanya 2 handler essay grading.
  </action>
  <verify>
    <automated>dotnet build --nologo 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 5; powershell -Command "if ((Select-String -Path 'Views/Admin/AssessmentMonitoringDetail.cshtml' -Pattern 'btn-save-essay-score','btn-finalize-grading','essay-grading-card' -SimpleMatch).Count -eq 0) { 'CLEAN: 0 essay handler match' } else { 'FAIL: essay handler masih ada' }"</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentMonitoringDetail.cshtml` grep `btn-save-essay-score` → 0 match
    - grep `btn-finalize-grading` → 0 match
    - grep `essay-grading-card` → 0 match
    - Handler non-essay (regenToken/AddExtraTime/copyToken/hub) TIDAK terhapus (tetap ada)
    - `showAlert` dihapus HANYA jika tak dipakai surface lain (grep-confirmed)
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>Handler essay AJAX dead dihapus dari monitoring view; 0 match selector essay; handler lain utuh; build hijau. Mengonfirmasi handler finalize hidup total di page baru (D-09 in-place, no dual-mode).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view → GET /Admin/EssayGrading | anchor link membawa sessionId + 4 nav param (navigasi GET, bukan state-change) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-384-03-IDOR | Information Disclosure | link Tinjau Essay → EssayGrading(sessionId) | mitigate | Target action `EssayGrading` ber-`[Authorize(Roles="Admin, HC")]` (Plan 02). Link hanya navigasi; otorisasi di-enforce di server (action), bukan di view. |
| T-384-03-A11Y | (a11y, bukan STRIDE) | badge kontras | mitigate | `bg-warning` dipasangkan `text-dark` (WCAG); pola existing in-repo. |
| T-384-03-DEAD | (regression) | handler dead tertinggal | mitigate | Task 2 hapus + grep 0-match gate; cegah double-attach/zombie handler bila markup essay muncul lagi di surface lain. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` (non-Integration) tetap hijau (view-only, tak ubah C# logic).
- grep gate: `btn-save-essay-score` / `btn-finalize-grading` / `essay-grading-card` = 0 match di `AssessmentMonitoringDetail.cshtml`.
- grep konfirmasi: `Tinjau Essay` + `Url.Action("EssayGrading"` ada; badge 3-state ada.
- Tabel sesi utama (`~:218-378`) + antiforgeryForm + hidden fields TIDAK berubah (diff inspeksi).
- Runtime/e2e (tabel render + navigasi) ditunda ke Plan 04 (Razor dynamic → Playwright wajib, Phase 354).
</verification>

<success_criteria>
- Tabel worker-list menggantikan blok essay inline `:381-481` (UIG-01).
- Tabel: hanya HasManualGrading, urut UserNIP, kolom Worker/NIP + Essay Belum Dinilai + badge 3-state + tombol Tinjau Essay (kanan).
- Tombol Tinjau Essay → GET EssayGrading dengan 4 nav param.
- Guard essayGradingMap.Any() + antiforgery dipertahankan; handler essay dead dihapus.
- Build + unit suite hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-03-SUMMARY.md`
</output>
