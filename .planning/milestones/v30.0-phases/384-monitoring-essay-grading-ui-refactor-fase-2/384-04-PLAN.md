---
phase: 384-monitoring-essay-grading-ui-refactor-fase-2
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - tests/e2e/essay-grading-384.spec.ts
autonomous: false
requirements: [UIG-04]
must_haves:
  truths:
    - "Spec FLOW 384 dijalankan HIJAU end-to-end: list worker-list render → Tinjau Essay navigasi → Simpan Skor AJAX → Selesaikan Penilaian → state Selesai in-place (URL tetap /EssayGrading)"
    - "test.fixme dihapus; 4 test aktif + pass dengan app running (Authentication__UseActiveDirectory=false, --workers=1)"
    - "DB di-snapshot sebelum seed + restore sesudah (SEED_WORKFLOW), journal docs/SEED_JOURNAL.md di-update"
  artifacts:
    - path: "tests/e2e/essay-grading-384.spec.ts"
      provides: "FLOW 384 e2e GREEN (4 test aktif)"
      contains: "EssayGrading"
  key_links:
    - from: "e2e UIG-04 round-trip"
      to: "/Admin/EssayGrading + SubmitEssayScore + FinalizeEssayGrading"
      via: "waitForResponse 200 + URL assertion"
      pattern: "waitForResponse"
---

<objective>
Aktifkan + jalankan e2e FLOW 384 (UIG-04) HIJAU: round-trip penuh list worker-list → "Tinjau Essay" → "Simpan Skor" (AJAX) → "Selesaikan Penilaian" → state "Selesai" in-place (D-09, URL tetap `/EssayGrading`). Hapus `test.fixme` dari spec Plan 01, jalankan dengan app lokal running, perbaiki selector/timing yang miss runtime (Razor dynamic → Playwright wajib, Phase 354), lalu UAT manual browser (checkpoint).

Purpose: Bukti runtime bahwa refactor UI bekerja end-to-end (grep+build tak cukup, Phase 354).
Output: spec FLOW 384 aktif + hijau; UAT manual confirmed.

Depends on Plan 01 (spec+seed), Plan 02 (page+action+js), Plan 03 (tabel monitoring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-RESEARCH.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-PATTERNS.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-CONTEXT.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md
@docs/SEED_JOURNAL.md

<interfaces>
<!-- Flow e2e UIG-04 (D-11) — selector + endpoint round-trip -->
1. loginAny(page, 'admin')
2. goto /Admin/AssessmentMonitoringDetail?title&category&scheduleDate (dari seed)
   -> assert a:has-text("Tinjau Essay") visible + badge .badge.bg-warning hasText "belum dinilai"
3. click a:has-text("Tinjau Essay") -> waitForURL(/EssayGrading/)
   -> assert h2 (UserFullName) + .essay-grading-card visible
4. fill .essay-score-input -> click .btn-save-essay-score
   -> waitForResponse(/SubmitEssayScore/ 200) -> assert #badge_{sid}_{qid} "Sudah Dinilai" (bg-success)
5. page.on('dialog', d=>d.accept()); click .btn-finalize-grading
   -> waitForResponse(/FinalizeEssayGrading/ 200)
   -> assert URL tetap /EssayGrading (BUKAN reload ke monitoring) + .essay-score-input disabled (D-09 in-place)
6. (opsional) click "Kembali ke Monitoring" -> assert balik benar

Run lokal (Pitfall 5 / Phase 355):
  dotnet run  dengan env Authentication__UseActiveDirectory=false  (app TIDAK auto-start; playwright.config tak punya webServer)
  cd tests; npx playwright test essay-grading-384 --workers=1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aktifkan spec FLOW 384 + jalankan hijau (snapshot→seed→e2e→restore)</name>
  <files>tests/e2e/essay-grading-384.spec.ts, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/essay-grading-384.spec.ts (spec Plan 01 — hapus test.fixme, finalisasi assertion bodies)
    - tests/sql/essay-grading-384-seed.sql (fixture Plan 01)
    - Views/Admin/EssayGrading.cshtml (Plan 02 — confirm selector aktual: .essay-grading-card, .essay-score-input, #badge_{sid}_{qid}, .btn-save-essay-score, .btn-finalize-grading)
    - Views/Admin/AssessmentMonitoringDetail.cshtml (Plan 03 — confirm tabel worker-list: a "Tinjau Essay" + badge bg-warning)
    - wwwroot/js/essay-grading.js (Plan 02 — confirm D-09 in-place: no location.reload, input disabled on finalize)
    - tests/e2e/assessment-pending-grade.spec.ts (harness reference)
    - tests/helpers/dbSnapshot.ts (backup/restore/execScript)
    - docs/SEED_JOURNAL.md (format entry — tujuan + klasifikasi + entitas + status cleaned)
    - .planning/reference (memory): local e2e SQL env fix (SQLBrowser + --workers=1 shared-memory)
  </read_first>
  <action>
    1. Di `tests/e2e/essay-grading-384.spec.ts`: hapus `test.fixme` dari 4 test (UIG-01..04). Finalisasi assertion bodies sesuai selector AKTUAL (cross-check dengan EssayGrading.cshtml + AssessmentMonitoringDetail.cshtml). Pastikan:
       - resolve `sessionId`/`title`/`category`/`scheduleDate` dari DB di `beforeAll` (sudah dari Plan 01) untuk membangun URL monitoring + matching baris worker.
       - step 5 (D-09 kunci): setelah `waitForResponse(/FinalizeEssayGrading/ 200)`, assert `expect(page.url()).toContain('/EssayGrading')` (BUKAN redirect ke AssessmentMonitoringDetail) + `await expect(page.locator('.essay-score-input').first()).toBeDisabled()`.
       - `page.on('dialog', d => d.accept())` dipasang SEBELUM klik finalize (confirm() auto-accept).

    2. Catat entry di `docs/SEED_JOURNAL.md` (SEED_WORKFLOW): tujuan = e2e UIG-04 Phase 384, klasifikasi `temporary + local-only`, entitas tersentuh (AssessmentSessions/AssessmentPackages/PackageQuestions/UserPackageAssignments/PackageUserResponses dengan prefix `[ESSAY384]`), status awal `seeded` → akhir `cleaned` setelah restore.

    3. Jalankan e2e (CLAUDE.md Develop Workflow):
       - `dotnet build` (0 error gate).
       - start app: `dotnet run` dengan `Authentication__UseActiveDirectory=false` (background; localhost:5277).
       - `cd tests; npx playwright test essay-grading-384 --workers=1`.
       - Spec sendiri snapshot DB di beforeAll → seed → run → restore di afterAll (Layer 4 assert bersih). Jika SQLBrowser/shared-memory issue muncul (memory: NTLM loopback), ikuti reference local e2e SQL env fix.

    4. Jika ada selector/timing miss (Razor dynamic): perbaiki spec ATAU flag mismatch markup ke executor (jika markup Plan 02/03 salah selector → catat di SUMMARY untuk gap-closure; JANGAN ubah markup di plan e2e ini kecuali trivial — markup adalah tanggung jawab Plan 02/03). Tujuan plan ini: spec hijau terhadap UI yang sudah dibangun.

    5. Stop app `dotnet run` setelah e2e selesai (bebaskan port; CLAUDE.md note: dev server lokal di-stop saat build test).
  </action>
  <verify>
    <automated>powershell -Command "Push-Location tests; npx playwright test essay-grading-384 --workers=1 2>&1 | Select-String -Pattern 'passed|failed|Error' | Select-Object -First 20; Pop-Location"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/essay-grading-384.spec.ts` TIDAK lagi berisi `test.fixme`
    - 4 test (UIG-01..04) aktif + PASS (`npx playwright test essay-grading-384 --workers=1` exit 0, "4 passed")
    - Step finalize meng-assert `page.url()` mengandung `/EssayGrading` (D-09 in-place, BUKAN reload monitoring)
    - Step finalize meng-assert `.essay-score-input` disabled
    - `docs/SEED_JOURNAL.md` punya entry `[ESSAY384]` dengan status akhir `cleaned`
    - DB lokal bersih pasca-run (Layer 4 assert COUNT==0 di afterAll lulus)
  </acceptance_criteria>
  <done>e2e FLOW 384 hijau end-to-end (4 test), D-09 in-place terbukti runtime, DB di-restore + journal cleaned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT manual browser (round-trip + read-only finalized)</name>
  <files>tests/e2e/essay-grading-384.spec.ts</files>
  <action>
    Checkpoint human-verify — TIDAK ada perubahan kode di task ini. Executor menyiapkan app lokal running lalu menyerahkan ke user untuk verifikasi manual sesuai langkah di `<how-to-verify>`. Self-approve DILARANG (pelajaran 383-02 Task 3). Tunggu sinyal "approved" dari user sebelum menutup phase.
  </action>
  <what-built>
    Refactor UI Monitoring penilaian essay: (a) tabel worker-list ringkas menggantikan blok inline stacked cards (UIG-01), (b) page penilaian essay per-worker terpisah via "Tinjau Essay" (UIG-02/03), (c) Simpan Skor + Selesaikan Penilaian reuse endpoint existing, (d) D-09 state "Selesai" in-place (tetap di page), (e) D-10 read-only setelah finalized. Backend endpoint TIDAK diubah, 0 migration.
  </what-built>
  <how-to-verify>
    Prasyarat: app running lokal. Jalankan `dotnet run` dengan env `Authentication__UseActiveDirectory=false`, buka `http://localhost:5277`. Login admin lokal (`admin@pertamina.com`). Pastikan ada sesi assessment ber-essay PendingGrading (boleh pakai seed `[ESSAY384]` — jalankan `tests/sql/essay-grading-384-seed.sql` manual lalu restore setelah UAT, ATAU pakai sesi essay-pending existing di DB lokal).

    1. Buka Assessment Monitoring → pilih grup assessment ber-essay → halaman `AssessmentMonitoringDetail`.
    2. Verifikasi section "Penilaian Essay" kini TABEL worker-list (bukan kartu numpuk): kolom Worker+NIP, Essay Belum Dinilai (angka), badge status (🟡 "{N} belum dinilai" / 🔵 "Siap difinalisasi" / 🟢 "Selesai"), tombol "Tinjau Essay" di kanan. Worker murni MC TIDAK muncul. Urut by NIP.
    3. Klik "Tinjau Essay" pada worker pending → masuk page per-worker terpisah: header nama worker (`h2`) + NIP, tombol "Kembali ke Monitoring", kartu essay (jawaban pekerja, gambar soal bila ada, rubrik, input skor, "Simpan Skor").
    4. Isi skor (>0) → klik "Simpan Skor" → badge soal jadi hijau "Sudah Dinilai" TANPA reload page. Nilai semua essay → section "Selesaikan Penilaian" muncul.
    5. Klik "Selesaikan Penilaian" → konfirmasi → **TETAP di page** (URL masih `/EssayGrading`, BUKAN balik ke monitoring), state berubah "Selesai" in-place: input skor disabled, tombol disabled, alert sukses hijau (D-09).
    6. Klik "Kembali ke Monitoring" → balik ke grup yang benar (bukan 404 "Assessment group not found"); badge worker tsb → 🟢 "Selesai".
    7. (D-10 read-only) Klik "Tinjau Essay" lagi pada worker yang sudah finalized → page terbuka READ-ONLY: skor tampil, input disabled, tombol Simpan/Selesaikan disabled. Hover tombol disabled → tooltip muncul.
    8. (a11y) Badge 🟡 kuning teks gelap terbaca (text-dark).

    Bila pakai seed: setelah UAT, RESTORE DB lokal (snapshot) + tandai journal `cleaned` (SEED_WORKFLOW). JANGAN edit DB/kode Dev/Prod (CLAUDE.md).
  </how-to-verify>
  <verify>
    <automated>MANUAL — checkpoint human-verify; tidak ada automated gate. Verifikasi via browser localhost:5277 sesuai how-to-verify; user mengetik "approved".</automated>
  </verify>
  <acceptance_criteria>
    - User mengetik "approved" setelah 8 langkah how-to-verify lulus
    - Tidak ada self-approve (executor menunggu sinyal user)
  </acceptance_criteria>
  <done>User approve round-trip UIG-04 + D-09 in-place + D-10 read-only via browser; phase 384 siap verify-work.</done>
  <resume-signal>Ketik "approved" bila semua langkah benar, atau jelaskan masalah yang ditemukan (selector tak match / navigasi salah / state tak in-place / read-only bocor).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e seed → DB lokal | snapshot sebelum + restore sesudah (SEED_WORKFLOW); journal cleaned |
| browser UAT → app lokal | localhost:5277 only; tidak ada edit Dev/Prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-384-04-T | Tampering | seed pollutes DB lokal | mitigate | snapshot/restore di spec afterAll + journal cleaned; klasifikasi temporary+local-only |
| T-384-04-IDOR | Information Disclosure | (regression check) GET EssayGrading non-Admin/HC | accept (covered Plan 02) | authz di-enforce Plan 02 `[Authorize(Roles="Admin, HC")]`; e2e login admin (authorized) — verifikasi 403 non-auth = opsional manual |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `npx playwright test essay-grading-384 --workers=1` → 4 passed.
- DB lokal bersih pasca-run (Layer 4 afterAll) + `docs/SEED_JOURNAL.md` entry `[ESSAY384]` cleaned.
- UAT manual checkpoint approved (round-trip + D-09 in-place + D-10 read-only).
- App lokal di-stop pasca-test (port bebas).
- TIDAK ada edit Dev/Prod (CLAUDE.md).
</verification>

<success_criteria>
- e2e UIG-04 round-trip HIJAU (list → Tinjau → Simpan Skor → Selesaikan → Selesai in-place).
- D-09 in-place (URL tetap /EssayGrading) + D-10 read-only terbukti runtime + UAT.
- DB di-restore + journal cleaned; 0 residu seed.
- Checkpoint UAT manual approved.
</success_criteria>

<output>
After completion, create `.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-04-SUMMARY.md`
</output>
