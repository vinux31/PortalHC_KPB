---
phase: 385-exam-taking-image-render-hotfix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/StartExam.cshtml
  - tests/e2e/essay-flush-385.spec.ts
autonomous: true
requirements: [PXF-03]
user_setup: []

must_haves:
  truths:
    - "Saat peserta menekan Kumpulkan/Review, teks essay terbaru di-flush ke server (SaveTextAnswer) SEBELUM examForm.submit() — keystroke ~2 detik terakhir tidak hilang"
    - "Saat peserta keluar dari textarea essay (blur), teks langsung disimpan (tidak menunggu debounce 2s)"
    - "Saat pindah halaman (changePage), pending essay flush ikut ditunggu sebelum navigasi"
    - "Saat timer habis (auto-submit), save essay ditembak best-effort lalu submit — TIDAK memblokir/menunda deadline"
    - "Peserta yang sudah mengetik essay tidak ditolak submit 'Masih ada N soal belum dijawab' (baris response essay sudah ter-flush sebelum submit)"
    - "Debounce 2s autosave normal tetap berfungsi (tidak dihapus)"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Essay flush-on-submit + save-on-blur + pre-submit/changePage wait termasuk essay + timeout best-effort flush"
      contains: "flushEssay"
    - path: "tests/e2e/essay-flush-385.spec.ts"
      provides: "Playwright e2e: ketik essay → submit langsung → assert tersimpan; peserta sudah-ketik tak ditolak submit"
      contains: "essay"
  key_links:
    - from: "reviewSubmitBtn click handler"
      to: "Hub.SaveTextAnswer (flush pending essay)"
      via: "await flushEssay() / essayInFlight tracking sebelum examForm.submit()"
      pattern: "flushEssay|essayInFlight"
    - from: "textarea.exam-essay blur"
      to: "Hub.SaveTextAnswer"
      via: "addEventListener('blur', ...) save langsung"
      pattern: "addEventListener\\('blur'"
---

<objective>
Perbaiki PXF-03 (F-21, HIGH): jawaban essay HANYA tersimpan via SignalR debounce 2 detik tanpa flush saat submit/blur/timeout. Akibat: keystroke ~2 detik terakhir HILANG saat submit, dan peserta yang sudah mengetik essay bisa ditolak submit "Masih ada N soal belum dijawab" (gate incomplete baca baris DB yang belum ter-save). Fix JS-side di `StartExam.cshtml`: flush essay + await SEBELUM submit/pindah-halaman, save-on-blur, dan timeout best-effort.

Purpose: Peserta ujian lisensor (~2026-06-17, ada essay) tidak kehilangan jawaban dan tidak ditolak submit di menit akhir.
Output: `StartExam.cshtml` dengan essay tracking ter-integrasi ke guard pre-submit/changePage + blur handler (D-02/D-03/D-04) + Playwright e2e (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/385-exam-taking-image-render-hotfix/385-CONTEXT.md
@.planning/notes/2026-06-15-readiness-ujian-lisensor.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang executor butuhkan — extracted dari codebase, jangan eksplorasi ulang. -->

State save engine — Views/CMP/StartExam.cshtml:
- L519-520: `let pendingSaves = {};` (qId→debounce timeout) + `let inFlightSaves = new Set();` (MC in-flight). Guard pre-submit/changePage menunggu KEDUA ini kosong.
- L513: `const SESSION_ID = @Model.AssessmentSessionId;`
- `window.assessmentHub` = koneksi SignalR; `window.assessmentHub.state === 'Connected'` cek siap.

Essay autosave SEKARANG (debounce 2s, TIDAK ditunggu guard) — L868-913:
```js
var essayTimers = {};
document.querySelectorAll('.exam-essay').forEach(function(ta) {
    ta.addEventListener('input', function() {
        const qId = this.dataset.questionId;
        // ... char counter, answeredQuestions tracking, hidden input update ...
        var self = this;
        clearTimeout(essayTimers[qId]);
        essayTimers[qId] = setTimeout(function() {        // <-- debounce 2s
            if (window.assessmentHub && window.assessmentHub.state === 'Connected') {
                window.assessmentHub.invoke('SaveTextAnswer', SESSION_ID, parseInt(qId), self.value)
                    .catch(function(err) { console.warn('SaveTextAnswer failed:', err); });
            }
            showSaveIndicator(qId, 'saved'); updateNetworkBadge('saved');
        }, 2000);
    });
});
```

changePage guard (MC-only) — L915-948 menunggu `Object.keys(pendingSaves).length > 0 || inFlightSaves.size > 0`.
reviewSubmitBtn guard (MC-only) — L977-1002 menunggu hal yang sama lalu `document.getElementById('examForm').submit()`.
Timer auto-submit — L472-483: saat `remaining <= 0` → `clearInterval(...); window.onbeforeunload = null;` → modal show → `document.getElementById('examForm').submit();`

Endpoint simpan essay — Hubs/AssessmentHub.cs:134-182 `SaveTextAnswer(int sessionId, int questionId, string textAnswer)`:
- Cek ownership + session `Status == "InProgress"` (auth/ownership AMAN — jangan bypass).
- Upsert PackageUserResponse: TextAnswer = teks, PackageOptionId = null. `await db.SaveChangesAsync()`.

Gate incomplete server (REFERENSI, sudah benar — JANGAN diubah kecuali perlu) — CMPController.cs:1627-1653:
- Saat `!serverTimerExpired`: hitung answered = form MC answers (Value>0) UNION DB responses (essay/MA). Jadi BILA baris essay sudah ter-flush ke DB sebelum submit → essay dihitung "terjawab" → tak ditolak palsu. Inilah alasan D-03: pastikan flush SELESAI sebelum manual submit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StartExam.cshtml — essay flush-on-submit + save-on-blur + integrasi guard pre-submit/changePage + timeout best-effort (D-02/D-03/D-04)</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - Views/CMP/StartExam.cshtml L455-513 (updateTimer + timer auto-submit L472-483 + answer-save URL/token/SESSION_ID)
    - Views/CMP/StartExam.cshtml L514-680 (state pendingSaves/inFlightSaves L519-520, saveAnswerAsync pola in-flight tracking L620-671, saveAnswerWithDebounce L673-679)
    - Views/CMP/StartExam.cshtml L868-1002 (essay input listener L868-913 + changePage guard L915-948 + reviewSubmitBtn guard L977-1002)
    - Hubs/AssessmentHub.cs:134-182 (SaveTextAnswer kontrak — auth/ownership/InProgress, JANGAN bypass)
    - CMPController.cs:1627-1653 (gate incomplete — UNION form+DB; baris essay yang sudah ter-flush dihitung terjawab)
  </read_first>
  <action>
    Edit `Views/CMP/StartExam.cshtml`. Tujuan: jadikan essay ikut "ditunggu" oleh guard pre-submit/changePage + tambah blur + timeout best-effort. TETAP pertahankan debounce 2s untuk autosave normal. JANGAN ubah `Hubs/AssessmentHub.cs` dan JANGAN ubah `CMPController.cs` (gate sudah benar — flush JS-side cukup, D-03).

    **(a) State tracking essay in-flight** — di dekat deklarasi `essayTimers` (L869), tambah:
    ```js
    var essayInFlight = new Set();   // qId essay yang request SaveTextAnswer-nya sedang berjalan
    ```

    **(b) Helper flush essay** — tambah fungsi yang: (1) clear semua `essayTimers` pending, (2) untuk tiap textarea essay yang punya nilai, invoke `SaveTextAnswer` segera + lacak via `essayInFlight`, (3) kembalikan Promise yang resolve saat SEMUA save selesai (atau timeout). Letakkan setelah blok essay input listener (sekitar L913):
    ```js
    // F-21 (PXF-03): flush semua essay pending segera (bukan tunggu debounce), return Promise.all.
    function flushEssay() {
        var promises = [];
        document.querySelectorAll('.exam-essay').forEach(function(ta) {
            var qId = ta.dataset.questionId;
            if (essayTimers[qId]) { clearTimeout(essayTimers[qId]); delete essayTimers[qId]; }
            if (window.assessmentHub && window.assessmentHub.state === 'Connected') {
                essayInFlight.add(String(qId));
                var p = window.assessmentHub.invoke('SaveTextAnswer', SESSION_ID, parseInt(qId), ta.value)
                    .then(function() { showSaveIndicator(qId, 'saved'); updateNetworkBadge('saved'); })
                    .catch(function(err) { console.warn('flushEssay SaveTextAnswer failed:', err); })
                    .finally(function() { essayInFlight.delete(String(qId)); });
                promises.push(p);
            }
        });
        return Promise.all(promises);
    }
    ```
    Catatan: panggil `SaveTextAnswer` untuk SEMUA textarea essay (termasuk kosong) aman — Hub upsert; kosong = TextAnswer "" tetap membuat baris sehingga gate tak tolak palsu. Bila ingin hemat, boleh skip `ta.value.trim()===''` TAPI untuk D-03 (pastikan baris ada) lebih aman flush semua yang sudah pernah disentuh; default: flush semua textarea essay.

    **(c) Save-on-blur (D-02)** — di dalam `forEach` essay (L870), tambah listener blur di samping `input`:
    ```js
    ta.addEventListener('blur', function() {
        var qId = this.dataset.questionId;
        if (essayTimers[qId]) { clearTimeout(essayTimers[qId]); delete essayTimers[qId]; }
        if (window.assessmentHub && window.assessmentHub.state === 'Connected') {
            essayInFlight.add(String(qId)); var self = this;
            window.assessmentHub.invoke('SaveTextAnswer', SESSION_ID, parseInt(qId), self.value)
                .then(function() { showSaveIndicator(qId, 'saved'); updateNetworkBadge('saved'); })
                .catch(function(err) { console.warn('blur SaveTextAnswer failed:', err); })
                .finally(function() { essayInFlight.delete(String(qId)); });
        }
    });
    ```

    **(d) Integrasi guard pre-submit (reviewSubmitBtn, L977-1002) — D-04.** Saat ini kondisi tunggu = `Object.keys(pendingSaves).length > 0 || inFlightSaves.size > 0`. Ubah handler agar:
    1. Di awal handler, panggil `flushEssay()` (tembak save essay segera).
    2. Perluas kondisi "ada yang pending" + poll-clear untuk MENYERTAKAN essay: tambahkan `|| essayInFlight.size > 0` ke kedua cek (kondisi masuk-branch DAN kondisi poll-clear). Sehingga submit menunggu MC (pendingSaves/inFlightSaves) DAN essay (essayInFlight) ter-flush.
    Contoh kondisi gabungan (pakai helper kecil agar DRY):
    ```js
    function hasPendingSaves() {
        return Object.keys(pendingSaves).length > 0 || inFlightSaves.size > 0 || essayInFlight.size > 0;
    }
    ```
    Lalu di reviewSubmitBtn handler: panggil `flushEssay();` lebih dulu, ganti `if (Object.keys(pendingSaves).length > 0 || inFlightSaves.size > 0)` → `if (hasPendingSaves())`, dan ganti kondisi poll `if (Object.keys(pendingSaves).length === 0 && inFlightSaves.size === 0)` → `if (!hasPendingSaves())`. Pertahankan `rTimeout` 5s fallback existing (best-effort, jangan hang). Jika `flushEssay()` dipanggil dan `hasPendingSaves()` jadi true (karena essayInFlight terisi), branch tunggu jalan; jika semua sync-cepat dan tak ada pending, default submit lanjut.

    **(e) Integrasi guard changePage (L915-948) — D-04.** Ganti kondisi `Object.keys(pendingSaves).length > 0 || inFlightSaves.size > 0` (L920) → `hasPendingSaves()` dan kondisi poll-clear `=== 0 && === 0` (L936) → `!hasPendingSaves()`. (Pindah-halaman ikut menunggu essay; blur handler sudah men-trigger save saat fokus pindah, jadi essayInFlight terisi.) CATATAN urutan deklarasi: pastikan `hasPendingSaves` dideklarasikan (function declaration → hoisted) atau didefinisikan SEBELUM changePage dipakai; gunakan `function hasPendingSaves() {...}` (hoisted) agar aman dipanggil di mana pun.

    **(f) Timeout best-effort (D-03, L472-483).** Di blok `if (remaining <= 0)` SETELAH `submitted = true;` dan SEBELUM `document.getElementById('examForm').submit();`, tembak `flushEssay()` TANPA await yang memblokir deadline (fire-and-forget, best-effort):
    ```js
    if (!submitted) {
        submitted = true;
        var timeupModal = new bootstrap.Modal(document.getElementById('timeUpWarningModal'));
        timeupModal.show();
        try { flushEssay(); } catch (e) { /* best-effort, jangan blokir deadline */ }
        document.getElementById('examForm').submit();
    }
    ```
    JANGAN `await` di sini (D-03: cutoff 0-detik = best-effort, jangan tunda submit). Submit tetap fire segera; flush jalan paralel.

    JANGAN hapus debounce 2s pada listener `input` (autosave normal tetap). JANGAN ubah perilaku MC save. JANGAN sentuh F-22/F-20 (deferred).
  </action>
  <acceptance_criteria>
    - grep `function flushEssay` di StartExam.cshtml → 1 match.
    - grep `addEventListener\('blur'` di StartExam.cshtml → minimal 1 match (save-on-blur essay).
    - grep `essayInFlight` di StartExam.cshtml → minimal 4 match (deklarasi + flush + blur + hasPendingSaves/guard).
    - grep `function hasPendingSaves` di StartExam.cshtml → 1 match; dan reviewSubmitBtn + changePage memakai `hasPendingSaves()` (grep `hasPendingSaves()` ≥ 4 match: 2 di reviewSubmitBtn cek+poll, 2 di changePage cek+poll).
    - grep `}, 2000);` di StartExam.cshtml → tetap ada (debounce 2s autosave normal DIPERTAHANKAN).
    - Timer block (L472-483): `flushEssay()` dipanggil SEBELUM `examForm').submit()` tanpa `await` (grep urutan: `flushEssay()` lalu `submit()` di blok `remaining <= 0`).
    - `dotnet build` → 0 error.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build PortalHC_KPB.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <done>Essay di-flush + di-await sebelum submit/changePage; save-on-blur; timeout best-effort (non-blocking); debounce 2s dipertahankan; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright e2e — ketik essay → submit langsung → tersimpan utuh + peserta sudah-ketik tak ditolak (D-05)</name>
  <files>tests/e2e/essay-flush-385.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts (analog flow exam-taking — login peserta, start exam, jawab, submit)
    - tests/e2e/assessment-pending-grade.spec.ts (analog assessment ber-essay → grading)
    - tests/e2e/helpers/examTypes.ts (createAssessmentViaWizard, addQuestionViaForm — buat soal Essay)
    - tests/helpers/auth.ts + tests/helpers/dbSnapshot.ts (login + backup/restore + query DB)
    - reference: MEMORY reference_local_e2e_sql_env_fix (SQLBrowser + lpc: + --workers=1) & reference_dev_credentials (admin@pertamina.com)
  </read_first>
  <action>
    Buat `tests/e2e/essay-flush-385.spec.ts` (`mode: 'serial'`, beforeAll BACKUP DB, afterAll RESTORE). Dua skenario membuktikan F-21 fix:

    **Skenario A — flush keystroke terakhir (D-02).** Admin buat assessment + paket 1 soal Essay (+ boleh 1 MC) via UI, assign peserta. Peserta (`rino.prasetyo@pertamina.com`/`123456`) buka StartExam, ketik teks essay panjang ke `textarea.exam-essay`, lalu LANGSUNG (tanpa menunggu 2 detik) klik tombol Review/Kumpulkan → submit. Assert teks essay tersimpan UTUH:
    - via DB query (dbSnapshot helper): `SELECT TextAnswer FROM PackageUserResponses WHERE AssessmentSessionId=@sid AND PackageQuestionId=@qid` == teks yang diketik; ATAU
    - via UI: ExamSummary/Results menampilkan teks essay lengkap.
    Kunci: JANGAN `waitForTimeout(2000)` sebelum submit — buktikan flush, bukan debounce. Pakai `await page.fill(...)` lalu segera klik submit.

    **Skenario B — peserta sudah-ketik tak ditolak submit (gate incomplete).** Peserta ketik SEMUA soal (essay terisi), submit langsung tanpa jeda. Assert TIDAK muncul TempData error "Masih ada N soal yang belum dijawab" (tidak redirect balik ke ExamSummary dengan pesan tsb) — yakni `await expect(page.locator('text=belum dijawab')).toHaveCount(0)` setelah submit (atau assert landed di ExamSummary/Results sukses, bukan error banner). Karena fix flush memastikan baris essay ada di DB sebelum gate cek (CMPController:1627-1653 UNION form+DB).

    Header komentar: spec memverifikasi PXF-03/F-21 (flush essay sebelum submit, no reject palsu). Lokal only. `--workers=1`. Bila browser belum ada: `npx playwright install chromium`.
  </action>
  <acceptance_criteria>
    - File `tests/e2e/essay-flush-385.spec.ts` ada.
    - grep `exam-essay` ATAU `textarea` di file → ada (mengisi essay).
    - grep `belum dijawab` di file → ada (assert tidak ditolak — skenario B).
    - grep `TextAnswer` ATAU `ExamSummary|Results` di file → ada (assert tersimpan — skenario A).
    - grep `mode: 'serial'` ATAU `describe.configure` di file → ada.
    - Tidak ada `waitForTimeout(2000)` sebelum submit (grep konteks: tidak menunggu debounce).
    - Jalankan: `cd tests; npx playwright test essay-flush-385 --workers=1` → PASS (app + DB lokal jalan).
  </acceptance_criteria>
  <verify>
    <automated>cd tests; npx playwright test essay-flush-385 --workers=1 2>&1 | Select-String -Pattern "passed|failed"</automated>
  </verify>
  <done>e2e: ketik essay → submit langsung → tersimpan utuh (skenario A) + peserta sudah-ketik tak ditolak "belum dijawab" (skenario B); PASS lokal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JS essay flush → Hub.SaveTextAnswer (server) | input essay peserta menyeberang ke server saat flush/blur/submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-385-03 | Elevation / Tampering | `Hub.SaveTextAnswer` dipanggil ekstra (flush/blur) | accept | Hub sudah cek ownership (`s.UserId == userId`) + `Status == "InProgress"` (AssessmentHub.cs:143-149) + truncate MaxChars (L157-159). Flush hanya menambah pemanggilan endpoint yang SAMA dan sudah ter-authz — tidak membuka jalur baru. JANGAN bypass guard ini. |
| T-385-04 | Tampering (post-deadline write) | flush saat timeout menulis essay | accept | F-22 (guard timer di SaveTextAnswer) di-DEFER (LOW, pasca-acara). Timeout flush best-effort happy-path; window di luar deadline sangat sempit (submit fire bersamaan). Diterima untuk hotfix. |
| T-385-05 | DoS (submit hang) | guard menunggu essayInFlight | mitigate | Guard pre-submit/changePage tetap punya `rTimeout`/`navTimeout` 5s fallback (SAVE_TIMEOUT_MS) — submit/navigasi tak pernah hang permanen. Timeout-path flush fire-and-forget (tak memblokir deadline, D-03). |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet run` (localhost:5277, AD off: `Authentication__UseActiveDirectory=false`) — ketik essay → submit langsung → cek DB `PackageUserResponses.TextAnswer` terisi utuh.
- `cd tests; npx playwright test essay-flush-385 --workers=1` PASS (skenario A flush + B no-reject).
- UAT final Dev (`http://10.55.3.3/KPB-PortalHC`) diserahkan IT setelah re-deploy — tak dijadwalkan di plan.
</verification>

<success_criteria>
1. Teks essay terbaru di-flush sebelum submit/changePage (keystroke ~2s terakhir tak hilang). (PXF-03)
2. Save-on-blur aktif; debounce 2s autosave normal dipertahankan.
3. Timeout auto-submit = best-effort flush, TIDAK memblokir deadline.
4. Peserta sudah-ketik essay tak ditolak "belum dijawab". (PXF-03)
5. `dotnet build` 0 error + Playwright e2e PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/385-exam-taking-image-render-hotfix/385-02-SUMMARY.md`
</output>
