---
phase: 386-assessmentadmincontroller-hardening
plan: 06
type: execute
wave: 5
depends_on: [03, 04, 05]
files_modified:
  - tests/e2e/option-validation-386.spec.ts
  - tests/e2e/essay-empty-finalize-386.spec.ts
autonomous: false
requirements: [PXF-02, PXF-04, PXF-05]

must_haves:
  truths:
    - "PXF-02 e2e (admin rejected saving Single 0-option question) passes against the running app"
    - "PXF-04 e2e (HC finalizes a session with an empty essay, round-trip) passes"
    - "Full dotnet test suite green; dotnet build 0 error"
    - "Human confirms PDF per-peserta MA label + Jawaban column correct in a real exported PDF"
  artifacts:
    - path: "tests/e2e/option-validation-386.spec.ts"
      provides: "PXF-02 reject path, un-gated (fixme removed)"
      contains: "alert-danger"
    - path: "tests/e2e/essay-empty-finalize-386.spec.ts"
      provides: "PXF-04 finalize round-trip, un-gated"
      contains: "Selesaikan"
  key_links:
    - from: "tests/e2e/option-validation-386.spec.ts"
      to: "AssessmentAdminController.CreateQuestion (PXF-02 wired Plan 03)"
      via: "Playwright submit malformed question → .alert-danger"
      pattern: "alert-danger"
    - from: "tests/e2e/essay-empty-finalize-386.spec.ts"
      to: "AssessmentAdminController.SubmitEssayScore/FinalizeEssayGrading (PXF-04 wired Plan 04)"
      via: "Playwright grade + Selesaikan round-trip"
      pattern: "Selesaikan"
---

<objective>
Wave 5 — turn the two Wave-0 e2e specs GREEN against the running app, run the full test suite, and
gate the phase behind a human verification checkpoint for the official PDF/Excel evidence (which is
QuestPDF/ClosedXML byte output — unit covers the helper, a human confirms the rendered document).

This is the final wave; it depends on all three controller-edit waves (PXF-02, PXF-04, PXF-05). No
production code changes — only un-gating the e2e specs and verifying end-to-end + manual sign-off.

Purpose: Prove PXF-02/04/05 work end-to-end on localhost:5277 before commit, and obtain human
confirmation of the official-evidence PDF/Excel rendering per CLAUDE.md Develop Workflow (verify
locally before commit; Dev UAT is IT/user's responsibility post-redeploy).
Output: 2 e2e specs un-gated + green; full suite green; human UAT approval recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/386-assessmentadmincontroller-hardening/386-CONTEXT.md
@.planning/phases/386-assessmentadmincontroller-hardening/386-RESEARCH.md
@.planning/phases/386-assessmentadmincontroller-hardening/386-VALIDATION.md
@.planning/phases/386-assessmentadmincontroller-hardening/386-03-SUMMARY.md
@.planning/phases/386-assessmentadmincontroller-hardening/386-04-SUMMARY.md
@.planning/phases/386-assessmentadmincontroller-hardening/386-05-SUMMARY.md

<run_instructions>
<!-- CLAUDE.md Develop Workflow + reference_local_e2e_sql_env_fix -->
Start the app for e2e: `Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5277`
Admin login: `admin@pertamina.com`. Playwright: ALWAYS `--workers=1`. Ensure SQLBrowser running + shared-memory
conn fix per reference_local_e2e_sql_env_fix (login 500 otherwise).
</run_instructions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-gate + green the PXF-02 and PXF-04 e2e specs</name>
  <files>tests/e2e/option-validation-386.spec.ts, tests/e2e/essay-empty-finalize-386.spec.ts</files>
  <read_first>
    - tests/e2e/option-validation-386.spec.ts + tests/e2e/essay-empty-finalize-386.spec.ts (Wave-0 scaffolds, fixme-gated)
    - 386-03-SUMMARY.md (PXF-02 message strings to assert) + 386-04-SUMMARY.md (PXF-04 finalize behavior)
    - tests/e2e/helpers/wizardSelectors.ts + tests/e2e/helpers/examTypes.ts (selectors + flow helpers)
    - reference_local_e2e_sql_env_fix (SQL env), CLAUDE.md (AD off, localhost:5277)
  </read_first>
  <action>
    Remove the `test.fixme`/`test.skip` gate from BOTH specs so they execute. Reconcile the spec bodies with the
    actual wired behavior:
      - option-validation-386: assert the EXACT rejection message produced by ValidateQuestionOptions (from
        386-03-SUMMARY — e.g. text matching /minimal 2 opsi.*berisi teks/i) appears in `.alert-danger`, and the
        malformed soal does NOT appear in the question list.
      - essay-empty-finalize-386: drive a session to PendingGrading with ≥1 empty essay, open the per-worker
        EssayGrading page, assert "Selesaikan Penilaian" is visible, grade the answered essay(s), click Selesaikan,
        assert success + that the empty essay scored 0 (no "Jawaban tidak ditemukan").
    Fix any selector drift discovered at runtime against wizardSelectors.ts. Keep `--workers=1`. No `--watch`.
    Start the app per <run_instructions> before running.
  </action>
  <acceptance_criteria>
    - Neither spec contains `test.fixme` or `test.skip` (gates removed).
    - `cd tests; npx playwright test option-validation-386 --workers=1` exits 0 (PXF-02 reject path passes).
    - `cd tests; npx playwright test essay-empty-finalize-386 --workers=1` exits 0 (PXF-04 finalize round-trip passes).
  </acceptance_criteria>
  <verify>
    <automated>cd tests; npx playwright test option-validation-386 essay-empty-finalize-386 --workers=1</automated>
  </verify>
  <done>Both e2e specs un-gated and passing against the running app.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite + build gate</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - 386-VALIDATION.md §Sampling Rate (phase gate: full dotnet test + run + Playwright)
    - all three controller-edit SUMMARYs (03/04/05)
  </read_first>
  <action>
    Run the complete verification gate (no code changes; if a regression surfaces, STOP and report — do not patch
    blindly):
      1. `dotnet build` — 0 error.
      2. `dotnet test` — full suite green (unit + integration real-SQL incl. EssayEmptyPendingParity, OptionValidation,
         PdfAnswerCell, IsQuestionCorrect, Authz). If SQLEXPRESS absent, run `dotnet test --filter "Category!=Integration"`
         green and note the integration skip (RESEARCH A1).
    Capture the pass counts for the SUMMARY (e.g. "NNN/NNN passed").
  </action>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - `dotnet test` exits 0 (or `dotnet test --filter "Category!=Integration"` exits 0 with documented integration skip).
    - No new test failures vs the v30.0 baseline (440/440 family) — the SUMMARY records the new total.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build; dotnet test</automated>
  </verify>
  <done>Build clean; full suite green (or pure-suite green with documented integration skip).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — official PDF/Excel MA evidence + PXF-04 finalize</name>
  <files>(verification only — no files modified)</files>
  <action>
    PAUSE for human verification. All automated gates (unit + e2e) are green; this checkpoint confirms the
    byte-rendered official documents (QuestPDF PDF / ClosedXML Excel) and the PXF-04 finalize flow look correct
    to a human, since QuestPDF/ClosedXML byte output is not unit-assertable. Present the steps in <how-to-verify>
    to the user and wait for the resume-signal before committing. Do NOT proceed to commit until "approved".
  </action>
  <what-built>
    PXF-02 server-side option validation (Create/Edit reject malformed questions), PXF-04 single pending
    predicate + upsert + status-guard (HC can finalize with empty essays), PXF-05 PDF + Excel MA correctness
    via shared IsQuestionCorrect + BuildAnswerCell helpers. Automated unit + e2e all green; this checkpoint
    confirms the byte-rendered official documents (QuestPDF / ClosedXML) look correct to a human.
  </what-built>
  <how-to-verify>
    With the app running (`Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5277`,
    login admin@pertamina.com):
    1. PXF-05 PDF: open an assessment that has a Multiple Answer question with a known correct set (e.g. correct = {Avtur, Solar, Bensin}).
       Export the per-peserta PDF (BulkExportPdf). For a participant who selected EXACTLY {Avtur, Solar, Bensin} → the soal shows "✓ Benar"
       and the "Jawaban" column lists ALL three ("Avtur, Solar, Bensin"). For a participant who selected only {Avtur} → "✗ Salah" and Jawaban "Avtur".
    2. PXF-05 Excel (F-DEV-02): export the Excel with the "Detail Per Soal" sheet for the same session → the MA "Benar?" column matches the PDF
       (all-or-nothing), and the "Jawaban" column lists all selected options. Confirm essay rows now mark Benar/Salah by >0 (consistent with PDF/web).
    3. PXF-04: take/seed a session where one essay is left empty → as HC, open the per-worker EssayGrading page → confirm "Selesaikan Penilaian"
       is visible (not blocked), grade the answered essay, click Selesaikan → success; the empty essay contributes 0, final % computed correctly.
    4. PXF-02: in ManagePackageQuestions, try to save a Single Answer question with all options empty (one checked correct) → rejected with a clear
       Indonesian message; the malformed soal is not saved.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human verifies QuestPDF/ClosedXML byte output + finalize flow; no automated command (covered by unit helper tests + e2e in Tasks 1-2).</automated>
  </verify>
  <resume-signal>Type "approved" if all four render/behaviors are correct, or describe the discrepancy (which surface, expected vs actual).</resume-signal>
  <done>Human typed "approved"; PDF MA label + Jawaban list, Excel Detail Per Soal parity, PXF-04 finalize, and PXF-02 reject all confirmed correct.</done>
</task>

</tasks>

<verification>
- Both e2e specs un-gated and green (`--workers=1`).
- `dotnet build` 0 error; `dotnet test` full green (or pure-suite green + documented integration skip).
- Human approves PDF MA label + Jawaban list, Excel Detail Per Soal parity, PXF-04 finalize, PXF-02 reject.
</verification>

<success_criteria>
- All of PXF-02/04/05 verified end-to-end on localhost:5277 (unit + e2e + manual).
- Official PDF + Excel MA evidence confirmed correct by a human (PXF-05 SC#3 + F-DEV-02).
- Ready for commit (CLAUDE.md: verify locally → commit → notify IT, migration flag = FALSE). 0 migration.
</success_criteria>

<output>
After completion, create `.planning/phases/386-assessmentadmincontroller-hardening/386-06-SUMMARY.md`
recording the e2e results, full test pass count, the human UAT verdict, and the reminder that promotion
to Dev (10.55.3.3) is IT's responsibility with commit hash + migration flag = FALSE.
</output>
