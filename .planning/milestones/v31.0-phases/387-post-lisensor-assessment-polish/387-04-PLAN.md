---
phase: 387-post-lisensor-assessment-polish
plan: 04
type: execute
wave: 2
depends_on: [387-01, 387-02, 387-03]   # verifies the behavior changes from all three implementation plans
files_modified:
  - HcPortal.Tests/PostLisensorPolishTests.cs
  - tests/e2e/aria-opsi-387.spec.ts
autonomous: false
requirements: [PXF-06, PXF-09, PXF-11, PXF-12]
must_haves:
  truths:
    - "A unit test proves SubmitEssayScore's guard blocks Completed and allows PendingGrading (PXF-06)"
    - "A unit test proves the BulkExport essay cell logic yields TextAnswer + Score, not '—' (PXF-09)"
    - "A unit test proves the MC upsert preserves a saved answer when the question is absent from the form (PXF-12)"
    - "A Playwright spec proves option-image aria-label contains the letter on Results AND ExamSummary at runtime (PXF-11)"
  artifacts:
    - path: "HcPortal.Tests/PostLisensorPolishTests.cs"
      provides: "xUnit data-level tests for PXF-06/09/12 using disposable real-SQL fixture"
      contains: "[Trait(\"Category\", \"Integration\")]"
    - path: "tests/e2e/aria-opsi-387.spec.ts"
      provides: "Playwright a11y assertion for PXF-11 (2 surfaces)"
      contains: "aria-label"
  key_links:
    - from: "PostLisensorPolishTests"
      to: "AssessmentSession.Status guard behavior"
      via: "seed Completed vs PendingGrading + assert"
      pattern: "AssessmentStatus\\.(Completed|PendingGrading)"
    - from: "aria-opsi-387.spec.ts"
      to: "_QuestionImage aria-label"
      via: "Playwright getByRole/locator aria assertion"
      pattern: "opsi (A|B)"
---

<objective>
Add the proportional verification (D-09) for Phase 387: xUnit data-level tests for the three logic-bearing fixes (PXF-06, PXF-09, PXF-12) and one Playwright a11y spec for PXF-11 across both review surfaces. Ends with a human-verify checkpoint covering the manual LOW items (PXF-08/10/13).

Purpose: Lock the behavior changes against regression at the appropriate depth — unit for logic, Playwright for runtime a11y render (Phase 354 lesson), manual for the SignalR/collision/timer LOW items that lack a controller/hub harness.
Output: HcPortal.Tests/PostLisensorPolishTests.cs (new) + tests/e2e/aria-opsi-387.spec.ts (new).

This is Wave 2 — depends on Plans 01/02/03 being implemented. Tests written here assert the POST-fix behavior (write them GREEN against the already-applied fixes; if a test is RED, the corresponding fix is incomplete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/387-post-lisensor-assessment-polish/387-CONTEXT.md
@.planning/phases/387-post-lisensor-assessment-polish/387-RESEARCH.md
@.planning/phases/387-post-lisensor-assessment-polish/387-VALIDATION.md

<interfaces>
<!-- Test harness pattern — VERIFIED from existing tests this session. -->

Existing disposable real-SQL fixture pattern (HcPortal.Tests/SubmitResurrectionTests.cs, EssayFinalizeRecomputeTests.cs):
```csharp
public class <Name>Fixture : IAsyncLifetime
{
    public string DbName { get; } = $"HcPortalDB_Test_{Guid.NewGuid():N}";
    // cs = Server=localhost\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30
    public DbContextOptions<ApplicationDbContext> Options => _options;
    public async Task InitializeAsync() { /* MigrateAsync on disposable DB */ }
    public async Task DisposeAsync()    { /* EnsureDeletedAsync */ }
}

[Trait("Category", "Integration")]      // excluded by --filter "Category!=Integration"; DB lokal HcPortalDB_Dev NOT touched
public class <Name>Tests : IClassFixture<<Name>Fixture> { ... ApplicationDbContext NewCtx() => new(_fixture.Options); }
```
Seed helper pattern (SubmitResurrectionTests.SeedTerminalSessionAsync) seeds AssessmentSession + AssessmentPackage + PackageQuestion + PackageOption + PackageUserResponse + UserPackageAssignment.

Status constants alias (used in existing tests): `using S = HcPortal.Models.AssessmentConstants.AssessmentStatus;`

Models for seeding (verified fields):
- AssessmentSession: Status, Title, Category, Schedule(DateTime), StartedAt, DurationMinutes, ExtraTimeMinutes, GenerateCertificate, IsPassed, Score, PassPercentage, AccessToken, UserId
- PackageUserResponse: AssessmentSessionId, PackageQuestionId, PackageOptionId(int?), TextAnswer(string?), EssayScore(int?), SubmittedAt
- PackageQuestion: AssessmentPackageId, QuestionType ("MultipleChoice"/"Essay"), ScoreValue, Order, QuestionText

Playwright env (reference_local_e2e_sql_env_fix): start SQLBrowser + lpc: shared-memory override; combined runs WAJIB `--workers=1`. Admin/exam login lokal: see reference_dev_credentials. Local run: `Authentication__UseActiveDirectory=false`.
Existing e2e specs live in tests/e2e/*.spec.ts (23 specs) — copy harness/login pattern from a neighboring exam/results spec (e.g. tests/e2e/image-pathbase-385.spec.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests PXF-06 / PXF-09 / PXF-12 (data-level, disposable real-SQL fixture)</name>
  <files>HcPortal.Tests/PostLisensorPolishTests.cs</files>
  <read_first>
    - HcPortal.Tests/SubmitResurrectionTests.cs (FULL — fixture + SeedTerminalSessionAsync + [Trait Integration] pattern to copy)
    - HcPortal.Tests/EssayFinalizeRecomputeTests.cs:1-60 (fixture variant, same disposable-DB approach)
    - Controllers/AssessmentAdminController.cs SubmitEssayScore guard (PXF-06, from Plan 01) + BulkExport essay branch (PXF-09, from Plan 01)
    - Controllers/CMPController.cs SubmitExam MC upsert guard (PXF-12, from Plan 02)
    - Models/AssessmentConstants.cs:15-20 (status constants)
  </read_first>
  <behavior>
    - PXF-06 (guard): seed a session Status=Completed with an essay response → applying the guard logic ("if Status==Completed reject") yields rejection and EssayScore unchanged; seed Status=PendingGrading → guard allows, EssayScore updates. Assert both branches.
    - PXF-09 (essay cell): given a PackageUserResponse with TextAnswer="jawaban X", EssayScore=8, ScoreValue=10 → the cell-derivation logic produces col4="jawaban X" and col6="Skor: 8/10"; with TextAnswer blank → col4="Tidak dijawab"; with EssayScore null → col6="Belum dinilai".
    - PXF-12 (no null-overwrite): seed an existingResponse with PackageOptionId set; run the upsert logic with an `answers` dict that does NOT contain that question's id → PackageOptionId is UNCHANGED (not null). With the id present → it updates.
  </behavior>
  <action>
Create `HcPortal.Tests/PostLisensorPolishTests.cs` following the SubmitResurrectionTests fixture pattern verbatim:
- A `PostLisensorPolishFixture : IAsyncLifetime` (disposable `HcPortalDB_Test_{guid}` @ `localhost\SQLEXPRESS`, MigrateAsync on init, EnsureDeletedAsync on dispose). Wrap MigrateAsync failure with the same "MIGRATION-CHAIN break, BUKAN bug" XunitException message style.
- `[Trait("Category", "Integration")]` on the test class + `IClassFixture<PostLisensorPolishFixture>`. `using S = HcPortal.Models.AssessmentConstants.AssessmentStatus;`.
- Seed helpers analogous to `SeedTerminalSessionAsync` but parameterized for each scenario.

Because the guards/cell-logic live inside controller methods that are heavy to instantiate, replicate the DECISION logic at data level exactly as the controller does (the existing tests do this — "logika direplikasi di-level data PERSIS seperti AssessmentAdminController"):
- PXF-06: load session, evaluate `session.Status == S.Completed` → assert reject path leaves `response.EssayScore` at its seeded value; for `S.PendingGrading` assert the set-path writes the new score.
- PXF-09: call/replicate the exact expression `string.IsNullOrWhiteSpace(essayResp?.TextAnswer) ? "Tidak dijawab" : essayResp.TextAnswer` and `essayResp?.EssayScore.HasValue == true ? $"Skor: {EssayScore}/{ScoreValue}" : "Belum dinilai"` against seeded responses; assert the three behavior cases.
- PXF-12: build `existingResponses` dict + `answers` dict; run the guarded upsert `if (answers.ContainsKey(q.Id)) { existingResponse.PackageOptionId = selectedOptId; }`; assert the absent-key case leaves PackageOptionId at its seeded non-null value.

Each scenario = one `[Fact]`. Aim for ~6-8 facts total (2 per REQ minimum: positive + negative). Name them descriptively, e.g. `Guard_CompletedSession_RejectsEssayEdit`, `Guard_PendingGrading_AllowsEssayEdit`, `EssayCell_Graded_ShowsScore`, `EssayCell_Blank_ShowsTidakDijawab`, `McUpsert_AbsentQuestion_PreservesSavedAnswer`, `McUpsert_PresentQuestion_Updates`.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~PostLisensorPolish" 2>&1 | Select-String -Pattern "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/PostLisensorPolishTests.cs` exists and contains `[Trait("Category", "Integration")]`
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~PostLisensorPolish"` reports all facts Passed (>= 6 facts)
    - At least one fact each references `S.Completed`, `S.PendingGrading`, `"Skor:"`, `"Belum dinilai"`, and `answers.ContainsKey`
    - Tests use disposable `HcPortalDB_Test_{guid}` — `HcPortalDB_Dev` is NOT referenced in any connection string
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Six-plus integration facts cover PXF-06/09/12 positive+negative branches and pass; HcPortalDB_Dev untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright spec PXF-11 — option-image aria letter on Results + ExamSummary</name>
  <files>tests/e2e/aria-opsi-387.spec.ts</files>
  <read_first>
    - tests/e2e/image-pathbase-385.spec.ts (neighboring spec — copy login/setup/PathBase harness)
    - tests/playwright.config.ts (base URL, workers config)
    - Views/CMP/Results.cshtml (PXF-11 surface 1, from Plan 03) + Views/CMP/ExamSummary.cshtml (surface 2)
    - Views/Shared/_QuestionImage.cshtml (how AriaContext becomes aria-label — confirm rendered attribute)
    - reference: reference_local_e2e_sql_env_fix, reference_dev_credentials (login + SQL env)
  </read_first>
  <action>
Create `tests/e2e/aria-opsi-387.spec.ts`. Reuse the login + navigation harness from `image-pathbase-385.spec.ts` (same CMP exam/results area, same PathBase handling). The spec must, against a seeded exam that has option IMAGES (image options are required for `_QuestionImage` to render — confirm via the existing image e2e seed or reuse its fixture):

1. Navigate to a Results page (CMP exam result with image options) and assert that at least one option image's `aria-label` contains "opsi A" (and ideally "opsi B"). Use a locator like `page.locator('img[aria-label*="opsi A"]')` or `getByRole('img')` filtered by accessible name — match whatever attribute `_QuestionImage.cshtml` actually renders AriaContext into (verify in read_first; it may be `aria-label` or `alt`-composed — assert the rendered accessible text contains the letter).
2. Navigate to the ExamSummary page for the same exam and assert the same: an option image's accessible label contains "opsi A".

Both assertions must run at RUNTIME (Razor-rendered DOM) — this is why grep+build is insufficient (D-09, Phase 354 lesson). Combined runs WAJIB `--workers=1`. If a dedicated image-option seed fixture does not already exist, reuse the one backing `image-pathbase-385.spec.ts`; document any seed in docs/SEED_JOURNAL.md as temporary+local-only and restore after (per CLAUDE.md Seed Workflow).
  </action>
  <verify>
    <automated>cd tests; npx playwright test e2e/aria-opsi-387.spec.ts --workers=1 2>&1 | Select-String -Pattern "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/aria-opsi-387.spec.ts` exists and contains an assertion matching `opsi A` (and asserts on BOTH Results and ExamSummary surfaces)
    - `npx playwright test e2e/aria-opsi-387.spec.ts --workers=1` reports the spec passed
    - The spec asserts the rendered accessible label (aria-label / accessible name) at runtime, not source text
  </acceptance_criteria>
  <done>Playwright confirms option-image aria letters render at runtime on both Results and ExamSummary; spec passes with --workers=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Manual verify PXF-08/10/13 (cert retry, monitor broadcast, timer guard)</name>
  <action>Pause execution. Present the manual verification steps below to the user and wait for approval. These three LOW items (PXF-08/10/13) have no controller/hub test harness, so per D-09 they are verified manually after the automated tests pass.</action>
  <what-built>
    Phase 387 implementation (Plans 01-03) + automated tests (Tasks 1-2 above). Automated coverage:
    unit (PXF-06/09/12), Playwright a11y (PXF-11). The three LOW items below have no controller/hub
    harness and are verified manually per D-09: PXF-08 (cert retry/collision), PXF-10 (monitor broadcast),
    PXF-13 (SaveTextAnswer timer guard).
  </what-built>
  <how-to-verify>
    Run locally with `Authentication__UseActiveDirectory=false dotnet run` (localhost:5277), login as admin@pertamina.com:
    1. PXF-10 (monitor broadcast): open the monitoring page in two browser tabs for an essay session; finalize essay grading in one tab → the other tab updates to "Completed" WITHOUT refresh.
    2. PXF-08 (cert surface): finalize a passing essay session that generates a certificate → confirm a NomorSertifikat is assigned; inspect logs — on a forced collision, an "Failed to generate certificate number" LogError appears and the JSON response carries a certError message (HC-visible).
    3. PXF-13 (timer guard): start an exam, let the timer (incl. ExtraTimeMinutes) expire, then attempt to autosave an essay answer via the hub → write is rejected and a "SaveTextAnswer: timer expired" warning is logged; the saved answer is unchanged.
    4. Smoke: `dotnet test --filter "Category!=Integration"` green; full `dotnet test` green; build 0 error.
  </how-to-verify>
  <resume-signal>Type "approved" if all three manual checks pass, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → disposable SQL DB | Tests create/drop `HcPortalDB_Test_{guid}`; never touch `HcPortalDB_Dev` — verified by connection-string isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-387-09 | (test-integrity) | Integration tests accidentally mutating the local Dev DB | mitigate | Disposable `HcPortalDB_Test_{guid}` fixture + `[Trait Category=Integration]`; acceptance criterion asserts HcPortalDB_Dev is not referenced |
| T-387-10 | (verification gap) | LOW items (PXF-08/10/13) shipping unverified | mitigate | Human-verify checkpoint with explicit per-item manual steps (D-09 proportional) |
</threat_model>

<verification>
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~PostLisensorPolish"` → all facts pass.
- `npx playwright test e2e/aria-opsi-387.spec.ts --workers=1` → pass.
- `dotnet test --filter "Category!=Integration"` → green (fast suite, no regression).
- Human-verify checkpoint approves PXF-08/10/13 manual checks.
</verification>

<success_criteria>
- PXF-06/09/12 covered by passing xUnit integration facts (positive + negative).
- PXF-11 covered by a passing Playwright a11y spec on both surfaces.
- PXF-08/10/13 manually verified and approved at the checkpoint.
- HcPortalDB_Dev untouched; 0 migration; build + full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/387-post-lisensor-assessment-polish/387-04-SUMMARY.md`
</output>
