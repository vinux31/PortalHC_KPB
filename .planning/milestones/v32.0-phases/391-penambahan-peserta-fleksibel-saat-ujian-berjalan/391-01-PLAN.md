---
phase: 391-penambahan-peserta-fleksibel-saat-ujian-berjalan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PART-01, PART-02, PART-03]
user_setup: []

must_haves:
  truths:
    - "HC dapat menambah peserta baru ke assessment yang sedang berjalan (ada peserta lain InProgress) tanpa diblokir guard Completed selama window terbuka"
    - "Sesi peserta baru lahir ber-status siap-mulai (Open jika jadwal sudah tiba, Upcoming jika belum) — BUKAN mewarisi status induk (tidak InProgress / Completed)"
    - "Sesi peserta yang sedang berjalan (StartedAt!=null && CompletedAt==null) tidak ter-overwrite Status/Schedule/DurationMinutes oleh edit-loop shared-field"
    - "Saat ada peserta InProgress, HC melihat notice Info (alert biru, informatif) bukan Warning (alert kuning kesan-error)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Helper DeriveReadyStatus + 5 titik bedah EditAssessment POST (D-01..D-05)"
      contains: "DeriveReadyStatus"
  key_links:
    - from: "EditAssessment POST BULK ASSIGN newSessions"
      to: "DeriveReadyStatus(savedAssessment.Schedule, savedAssessment.ExamWindowCloseDate)"
      via: "Status = DeriveReadyStatus(...) menggantikan Status = savedAssessment.Status"
      pattern: "Status = DeriveReadyStatus"
    - from: "EditAssessment POST edit-loop siblings"
      to: "skip sesi berjalan"
      via: "if (sibling.StartedAt != null && sibling.CompletedAt == null) continue;"
      pattern: "StartedAt != null && sibling\\.CompletedAt == null"
    - from: "EditAssessment POST guard Completed"
      to: "izinkan penambahan saat window terbuka"
      via: "guard hanya blokir EDIT murni (!hasAddition)"
      pattern: "&& !hasAddition"
    - from: "EditAssessment POST hasInProgress notice"
      to: "_Layout Info alert"
      via: "TempData[\"Info\"] menggantikan TempData[\"Warning\"]"
      pattern: "TempData\\[\"Info\"\\]"
---

<objective>
Wire ulang 5 titik bedah di `EditAssessment` POST (`Controllers/AssessmentAdminController.cs`, ~L1790-2229) agar HC dapat menambah peserta secara fleksibel ke assessment yang sedang berjalan, tanpa merusak sesi peserta yang sedang ujian, dengan notice yang menenangkan. Implementasi keputusan terkunci D-01..D-05 dari CONTEXT.md.

Purpose: Saat ini (1) peserta baru mewarisi `savedAssessment.Status` sehingga bisa lahir "InProgress" padahal Progress=0; (2) guard `Completed` pada sesi representatif salah-memblokir penambahan saat sebagian peserta sudah selesai; (3) edit-loop menimpa Status/Schedule/Duration SEMUA sibling termasuk sesi yang sedang ujian (merusak timer); (4) notice pakai `TempData["Warning"]` kuning yang berkesan error. Phase ini = wire-ulang + filter (semua pola sudah ada di repo), bukan bangun baru.

Output: `AssessmentAdminController.cs` termodifikasi — 1 helper privat `DeriveReadyStatus` + 4 perubahan inline. Migration = FALSE. Tidak ada perubahan view (D-04 reuse `_Layout` Info alert yang sudah ada).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-CONTEXT.md
@.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-RESEARCH.md
@.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-PATTERNS.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diekstrak langsung dari kode terbaca. Pakai langsung — TIDAK perlu eksplorasi codebase. -->

Dari Models/AssessmentConstants.cs L13-22 (JANGAN hardcode string status):
```csharp
public static class AssessmentStatus
{
    public const string Open = "Open";
    public const string Upcoming = "Upcoming";
    public const string Completed = "Completed";
    public const string PendingGrading = "Menunggu Penilaian";
    public const string InProgress = "InProgress";
    public const string Cancelled = "Cancelled";
    public const string Abandoned = "Abandoned";
}
```

Dari Models/AssessmentSession.cs (field relevan):
```csharp
public string UserId { get; set; } = "";
public string Title { get; set; } = "";
public string Category { get; set; } = "";
public DateTime Schedule { get; set; }              // WIB-local naive
public int DurationMinutes { get; set; }
public string Status { get; set; } = "";
public int Progress { get; set; } = 0;
public DateTime? StartedAt { get; set; }            // null = belum mulai
public DateTime? CompletedAt { get; set; }          // null = belum selesai
public DateTime? ExamWindowCloseDate { get; set; }  // WIB-local naive; null = tidak diset
```

Sumber kebenaran WIB/window — Controllers/CMPController.cs StartExam (VERIFIED L915 + L953):
```csharp
// Auto Upcoming → Open saat jadwal tiba (WIB = UTC+7):
if (assessment.Status == "Upcoming" && assessment.Schedule <= DateTime.UtcNow.AddHours(7)) { /* → Open */ }
// Window tutup:
if (assessment.ExamWindowCloseDate.HasValue && DateTime.UtcNow.AddHours(7) > assessment.ExamWindowCloseDate.Value) { /* ditutup */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper DeriveReadyStatus + ganti Status inherit di BULK ASSIGN (D-01)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs L2114-2226 (blok BULK ASSIGN; target L2161) — file yang dimodifikasi
    - Controllers/CMPController.cs L905-1010 (StartExam window/status — analog/pattern WIB)
    - Models/AssessmentConstants.cs L13-22 (konstanta status)
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-PATTERNS.md Titik A + Titik B
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
    Dua perubahan untuk D-01.

    (1) Tambah helper privat static di kelas `AssessmentAdminController` (letakkan dekat method `EditAssessment`, sebelum atau sesudahnya — sembarang lokasi di dalam kelas yang rapi). Salin VERBATIM (mirror `StartExam` L915, WIB = `DateTime.UtcNow.AddHours(7)` — JANGAN `DateTime.Now`, JANGAN `DateTime.UtcNow` polos; regresi historis d844c552):
    ```csharp
    // Phase 391 D-01: derive status siap-mulai untuk sesi peserta baru.
    // Mirror CMPController.StartExam L915 (WIB = UTC+7). Schedule = WIB-local naive.
    private static string DeriveReadyStatus(DateTime schedule, DateTime? examWindowCloseDate)
    {
        var nowWib = DateTime.UtcNow.AddHours(7);
        // Jadwal sudah tiba → Open; belum → Upcoming.
        if (schedule <= nowWib)
            return AssessmentConstants.AssessmentStatus.Open;
        return AssessmentConstants.AssessmentStatus.Upcoming;
    }
    ```
    (2) Di blok BULK ASSIGN, ganti baris L2161:
    `Status = savedAssessment.Status,`
    menjadi:
    `Status = DeriveReadyStatus(savedAssessment.Schedule, savedAssessment.ExamWindowCloseDate),`
    SEMUA field lain di inisialisasi `newSessions` (L2155-2174) TETAP tidak berubah (Title, Category, Schedule, DurationMinutes, BannerColor, IsTokenRequired, AccessToken, PassPercentage, AllowAnswerReview, ShuffleQuestions, ShuffleOptions, GenerateCertificate, ExamWindowCloseDate, Progress=0, UserId, CreatedBy).

    JANGAN hardcode "Open"/"Upcoming" — gunakan `AssessmentConstants.AssessmentStatus.*`. JANGAN ubah filter idempotency L2125-2137.
  </action>
  <acceptance_criteria>
    - `grep -n "private static string DeriveReadyStatus" Controllers/AssessmentAdminController.cs` → tepat 1 match
    - `grep -n "DateTime.UtcNow.AddHours(7)" Controllers/AssessmentAdminController.cs` → match di dalam DeriveReadyStatus (verifikasi WIB, BUKAN DateTime.Now / UtcNow polos)
    - `grep -n "Status = DeriveReadyStatus(savedAssessment.Schedule, savedAssessment.ExamWindowCloseDate)" Controllers/AssessmentAdminController.cs` → tepat 1 match
    - `grep -n "Status = savedAssessment.Status," Controllers/AssessmentAdminController.cs` → 0 match (baris lama tergantikan)
    - `grep -c "AssessmentConstants.AssessmentStatus.Open" Controllers/AssessmentAdminController.cs` ≥ 1 (helper pakai konstanta, bukan literal "Open")
    - `dotnet build HcPortal.csproj` → 0 error
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Helper DeriveReadyStatus ada (WIB, konstanta) + BULK ASSIGN set Status via helper menggantikan inherit; build 0 error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard Completed berbasis penambahan (D-02) + skip sesi berjalan di edit-loop (D-03)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs L1985-2085 (guard Completed L1992, edit-loop L2056-2075, hasInProgress L2077-2081) — file yang dimodifikasi
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-PATTERNS.md Titik C + Titik D
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-RESEARCH.md Pitfall 2 + Pitfall 3 + Open Question A1
  </read_first>
  <behavior>
    - Guard: assessment.Status == Completed DAN tanpa penambahan (NewUserIds kosong/null) → tetap tolak "Cannot edit completed assessments." (jalur EDIT murni tak berubah)
    - Guard: assessment.Status == Completed TAPI ada penambahan (NewUserIds.Count > 0) → TIDAK terblokir (lolos ke jalur penambahan) — selama window terbuka
    - Edit-loop: sibling dengan StartedAt!=null && CompletedAt==null (sedang berjalan) → di-skip total (Status/Schedule/DurationMinutes + semua field volatil UNCHANGED)
    - Edit-loop: sibling belum-mulai (StartedAt==null) atau sudah-selesai → tetap menerima update field bersama seperti semula
  </behavior>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
    Dua perubahan.

    (1) D-02 — Guard Completed (L1991-1996). Ganti blok:
    ```csharp
    // Prevent editing completed assessments (optional - you can remove this if needed)
    if (assessment.Status == "Completed")
    {
        TempData["Error"] = "Cannot edit completed assessments.";
        return RedirectToAction("ManageAssessment");
    }
    ```
    menjadi (deklarasi `hasAddition` SEBELUM guard; blokir HANYA untuk EDIT murni — saat ada penambahan, lolos):
    ```csharp
    // Phase 391 D-02: izinkan penambahan peserta meski sesi representatif Completed (selama grup masih aktif/window terbuka).
    // Guard Completed hanya blokir EDIT murni (tanpa penambahan peserta).
    bool hasAddition = NewUserIds != null && NewUserIds.Count > 0;
    if (assessment.Status == AssessmentConstants.AssessmentStatus.Completed && !hasAddition)
    {
        TempData["Error"] = "Cannot edit completed assessments.";
        return RedirectToAction("ManageAssessment");
    }
    ```
    Fallback `ExamWindowCloseDate == null` (Open Question A1): pilih LONGGAR — null = boleh tambah (sejajar StartExam yang hanya cek window bila `.HasValue`). TIDAK menambah cek window penolakan tambahan di guard ini; cukup lolos saat hasAddition. (Keputusan ini dikunci di test fact (d), Plan 02.) JANGAN hapus guard total — jalur EDIT murni harus tetap ditolak saat Completed.

    (2) D-03 — Edit-loop skip sesi berjalan (L2058-2075). Di awal body `foreach (var sibling in siblings)` (sebelum `sibling.Title = model.Title;`), sisipkan:
    ```csharp
    // Phase 391 D-03: sesi sedang berjalan (StartedAt set, belum selesai) → jangan sentuh field volatil apa pun (lindungi timer & integritas ujian).
    if (sibling.StartedAt != null && sibling.CompletedAt == null) continue;
    ```
    Sisa body loop (assign Title..ExamWindowCloseDate + UpdatedAt) TETAP tidak berubah. Predikat `StartedAt != null && CompletedAt == null` = definisi "berjalan", konsisten dengan `hasInProgress` query L2078-2081.
  </action>
  <acceptance_criteria>
    - `grep -n "bool hasAddition = NewUserIds != null && NewUserIds.Count > 0;" Controllers/AssessmentAdminController.cs` → ≥ 1 match
    - `grep -n "AssessmentStatus.Completed && !hasAddition" Controllers/AssessmentAdminController.cs` → tepat 1 match
    - `grep -n "if (sibling.StartedAt != null && sibling.CompletedAt == null) continue;" Controllers/AssessmentAdminController.cs` → tepat 1 match (di dalam edit-loop siblings)
    - `grep -c "if (assessment.Status == \"Completed\")" Controllers/AssessmentAdminController.cs` → 1 (hanya guard L4228 yang tersisa; guard EditAssessment sudah pakai konstanta + !hasAddition)
    - `dotnet build HcPortal.csproj` → 0 error
    - `dotnet test --filter "Category!=Integration"` → green (no regression fast suite)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly; dotnet test --filter "Category!=Integration"</automated>
  </verify>
  <done>Guard Completed lolos saat penambahan (window terbuka), tetap blokir EDIT murni; edit-loop skip sesi berjalan; build + fast suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Notice informatif Info ganti Warning (D-04) + konsistensi Pre-Post (D-05)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs L2077-2085 (hasInProgress + TempData Warning) — file yang dimodifikasi
    - Controllers/AssessmentAdminController.cs L1806-1988 (cabang Pre-Post — verifikasi D-05)
    - Views/Shared/_Layout.cshtml L189-228 (render TempData Info/Warning/Success — KONFIRMASI sebelum klaim "no view change")
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-PATTERNS.md Titik E + Titik F
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-RESEARCH.md Pitfall 5 + Pitfall 6
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
    Dua bagian.

    (1) D-04 — Notice (L2082-2085). Ganti baris:
    ```csharp
    TempData["Warning"] = "Perhatian: Ada peserta yang sedang mengerjakan ujian. Perubahan Title/Category/Schedule tidak akan berlaku untuk sesi yang sedang berjalan.";
    ```
    menjadi (gunakan key `Info`, wording menenangkan; `_Layout` L210-219 sudah render Info sebagai alert biru → NOL perubahan view):
    ```csharp
    TempData["Info"] = "Ada peserta yang sedang mengerjakan ujian. " +
        "Peserta baru tetap dapat ditambahkan dan langsung mulai selama waktu ujian masih terbuka. " +
        "Sesi peserta yang sedang berjalan tidak terpengaruh perubahan.";
    ```
    Blok `var hasInProgress = await _context.AssessmentSessions.AnyAsync(...)` L2078-2081 TETAP tidak berubah. JANGAN pakai `TempData["Success"]` (double-render di ManageAssessment, Pitfall 5). JANGAN pakai `TempData["Warning"]` (alert kuning kesan-error).

    (2) D-05 — Konsistensi Pre-Post (VERIFIKASI saja, low-risk). Baca cabang Pre-Post L1806-1988. Konfirmasi (jangan over-engineer, Pitfall 6):
    - Sesi baru Pre/Post sudah di-set `Status = "Upcoming"` (L1940/L1961) — sudah sesuai D-01. JANGAN ubah.
    - `return RedirectToAction("ManageAssessment")` (L1987) terjadi SEBELUM guard Completed L1992 — D-02 sudah aman di Pre-Post. JANGAN tambah guard.
    - Pre-Post shared-field loop (sekitar L1832-1866) menimpa sesi grup. JIKA loop tsb mengandung overwrite Status/Schedule/DurationMinutes pada sesi yang bisa sedang berjalan, terapkan filter D-03 yang sama (`if (s.StartedAt != null && s.CompletedAt == null) continue;`) untuk konsistensi. JIKA Pre/Post tidak punya jalur penambahan-saat-berjalan yang realistis (sesi umumnya belum mulai), CUKUP catat di SUMMARY bahwa Pre-Post diverifikasi konsisten tanpa perubahan. Pilih sesuai temuan baca — JANGAN menambah perubahan spekulatif.

    Dokumentasikan keputusan Pre-Post (diubah/diverifikasi-tanpa-ubah) di SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -n "TempData\[\"Info\"\] = \"Ada peserta yang sedang mengerjakan ujian." Controllers/AssessmentAdminController.cs` → ≥ 1 match
    - `grep -n "Perubahan Title/Category/Schedule tidak akan berlaku" Controllers/AssessmentAdminController.cs` → 0 match (wording lama Warning hilang)
    - `grep -c "TempData\[\"Warning\"\]" Controllers/AssessmentAdminController.cs` → tidak bertambah dari baseline (warning EditAssessment L2084 sudah jadi Info; warning lain di file untuk fitur berbeda tetap)
    - Konfirmasi via Read: `_Layout.cshtml` punya blok render `TempData["Info"]` (alert biru) → tidak ada perubahan view yang dibutuhkan
    - `dotnet build HcPortal.csproj` → 0 error
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Notice memakai TempData["Info"] dengan wording menenangkan (Warning lama hilang); Pre-Post diverifikasi konsisten (diubah bila ada overwrite sesi berjalan, atau dicatat tanpa-ubah); build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin browser → EditAssessment POST | Form input (model + NewUserIds[]) menyeberang dari klien terautentikasi ke server. Endpoint `[Authorize(Roles="Admin, HC")]` — bukan permukaan publik. |
| EditAssessment POST → AssessmentSessions (DB) | Penulisan sesi baru + update sibling. Server-authoritative untuk status & window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-391-01 | Tampering | Status sesi baru ditentukan klien/form | mitigate | `DeriveReadyStatus` hitung status server-side dari `Schedule` vs now-WIB — klien tidak bisa menentukan status sesi baru (D-01). Tidak ada path form→Status untuk sesi baru. |
| T-391-02 | Tampering | D-03 disalahgunakan untuk merusak sesi berjalan | mitigate | Filter `StartedAt!=null && CompletedAt==null → continue` justru MELINDUNGI sesi berjalan dari overwrite Status/Schedule/Duration; tidak membuka jalur korupsi baru. Dikunci test fact (c) Plan 02. |
| T-391-03 | Elevation of Privilege | Authorization endpoint berubah | accept | Tidak ada perubahan atribut `[Authorize]`; endpoint tetap Admin/HC-only. Tidak ada permukaan publik baru ditambahkan. |
| T-391-04 | Information Disclosure / IDOR | Penambahan ke assessment milik grup lain | accept | Query sibling sudah ter-scope ke group-key (Title+Category+Schedule.Date) L2125-2131 — tidak diubah. Tidak ada parameter id baru yang membuka IDOR. Idempotency filter L2134-2137 dipertahankan. |
| T-391-05 | Denial of Service | Penambahan saat Completed melonggarkan guard | mitigate | Guard hanya dilonggarkan untuk jalur penambahan (`!hasAddition`); EDIT murni saat Completed tetap ditolak (D-02). Rate-limit existing (≤50 user, L1999) tidak diubah. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test --filter "Category!=Integration"` → green (fast suite, no regression).
- Grep acceptance semua task PASS (helper + 4 titik inline).
- Migration = FALSE — git diff tidak menyentuh `Migrations/` atau skema; hanya `Controllers/AssessmentAdminController.cs`.
- Tidak ada perubahan file view (D-04 reuse `_Layout` Info alert — dikonfirmasi via Read di Task 3).
</verification>

<success_criteria>
- Helper `DeriveReadyStatus` ada (WIB `DateTime.UtcNow.AddHours(7)`, konstanta `AssessmentConstants.AssessmentStatus.*`).
- BULK ASSIGN sesi baru ber-status siap-mulai (Open/Upcoming) bukan inherit (PART-01).
- Guard Completed lolos saat penambahan + window terbuka, tetap blokir EDIT murni (PART-02).
- Edit-loop skip sesi berjalan (lindungi timer; mendukung PART-04 fact c).
- Notice `TempData["Info"]` informatif menggantikan `TempData["Warning"]` (PART-03).
- Pre-Post diverifikasi konsisten (D-05).
- `dotnet build` 0 error + fast suite green; 0 migration; 0 perubahan view.
</success_criteria>

<output>
After completion, create `.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-01-SUMMARY.md`.
Catat: lokasi helper DeriveReadyStatus, keputusan fallback ExamWindowCloseDate==null (longgar/null=boleh-tambah), keputusan Pre-Post (diubah vs diverifikasi-tanpa-ubah), dan konfirmasi 0 migration + 0 view change. SUMMARY ini dibaca Plan 02 untuk mereplikasi helper byte-identik.
</output>
