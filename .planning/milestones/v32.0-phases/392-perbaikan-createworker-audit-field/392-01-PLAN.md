---
phase: 392-perbaikan-createworker-audit-field
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/CreateWorker.cshtml
autonomous: true
requirements: [WRKR-01, WRKR-02]
must_haves:
  truths:
    - "Input FullName & Email render editable (no readonly, no bg-light) in all modes"
    - "Email input has type=email"
    - "AD info-text reads the reworded LOCKED Bahasa Indonesia string (not the old contradictory one)"
    - "Position/Directorate/Section/Unit each have an inline asp-validation-for span"
    - "No validation span is duplicated (existing 6 untouched)"
    - "Client-side validation is enabled via _ValidationScriptsPartial inside @section Scripts"
    - "dotnet build succeeds (0 error)"
  artifacts:
    - path: "Views/Admin/CreateWorker.cshtml"
      provides: "Editable Nama/Email + type=email + 4 org spans + reworded AD info-text + @section Scripts w/ validation partial"
      contains: "_ValidationScriptsPartial"
  key_links:
    - from: "Views/Admin/CreateWorker.cshtml @section Scripts"
      to: "_ValidationScriptsPartial.cshtml"
      via: "@await Html.PartialAsync"
      pattern: "_ValidationScriptsPartial"
    - from: "Views/Admin/CreateWorker.cshtml input Email"
      to: "HTML5 email constraint"
      via: "explicit type attribute"
      pattern: "type=\"email\""
---

<objective>
Edit the single VIEW-ONLY file `Views/Admin/CreateWorker.cshtml` to make `/Admin/CreateWorker` usable in all environments: unlock FullName & Email (remove the AD-mode `readonly` + `bg-light`), reword the AD info-text, add explicit `type="email"`, add inline validation spans to the 4 organization fields (+ optional Role), and enable live client-side validation by moving the bottom script block into `@section Scripts` with `_ValidationScriptsPartial`.

Purpose: Closes the HIGH bug F-VIEW-07 (form unusable in AD mode because Nama/Email were read-only) plus F-NEW-01/F-NEW-03 (missing org spans + no client-side validator), implementing D-01..D-05.
Output: One modified file `Views/Admin/CreateWorker.cshtml`. Controller (`WorkerController.cs`) and model (`ManageUserViewModel.cs`) remain 0-diff. 0 migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-CONTEXT.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-RESEARCH.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-PATTERNS.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Verified live anchors (file is 206 lines; RE-READ FRESH before editing — anchors confirmed accurate 2026-06-17 but confirm again). -->

CreateWorker.cshtml CURRENT (verified live):
- L6: `var isAdMode = Config.GetValue<bool>("Authentication:UseActiveDirectory", false);`
- L62-64 FullName input: `class="form-control @(isAdMode ? "bg-light" : "")"` + `readonly="@(isAdMode ? "readonly" : null)"`
- L65-68: `@if (isAdMode) { <div class="form-text text-info"><i class="bi bi-info-circle me-1"></i>Dikelola oleh AD — akan disinkronkan saat login</div> }`
- L69: `<span asp-validation-for="FullName" class="text-danger small"></span>` (EXISTS — do not duplicate)
- L73-75 Email input: `class="form-control @(isAdMode ? "bg-light" : "")"` + `readonly="@(isAdMode ? "readonly" : null)"`, NO `type` attr (renders type=text)
- L76-79: same AD info-text "Dikelola oleh AD..."
- L80: `<span asp-validation-for="Email" class="text-danger small"></span>` (EXISTS — do not duplicate)
- L85, L90, L158, L168: existing spans NIP/JoinDate/Password/ConfirmPassword (do not duplicate)
- L107: `<input asp-for="Position" class="form-control" placeholder="..." />` (NO span — add after)
- L111: `<input asp-for="Directorate" class="form-control" placeholder="..." />` (NO span — add after)
- L114-117: `<select asp-for="Section" class="form-select" id="sectionSelect">...</select>` (NO span — add after `</select>` L117)
- L120-123: `<select asp-for="Unit" class="form-select" id="unitSelect">...</select>` (NO span — add after `</select>` L123)
- L139-146: `<select asp-for="Role">` + `<div class="form-text">...</div>` L146 (Role span optional — add after L146)
- L194-205: bottom inline script block (shared-cascade.js + shared-loading.js + initSectionUnitCascade + initFormLoading)

Model annotations (FROZEN — do NOT edit ManageUserViewModel.cs):
- `[Required]` FullName, Email, Role; `[EmailAddress]` Email (NOT `[DataType]` → asp-for does NOT auto-render type=email)
- `[Compare("Password")]` ConfirmPassword; `[StringLength(100, MinimumLength=6)]` Password (NOT Required)
- Position/Directorate/Section/Unit/NIP/JoinDate = OPTIONAL (do NOT add `required`)

Reuse partial (FROZEN — read only):
- `Views/Shared/_ValidationScriptsPartial.cshtml` = 2 lines (jquery.validate.min.js + jquery.validate.unobtrusive.min.js)
- `_Layout.cshtml`: jQuery footer L241 → `@RenderSectionAsync("Scripts")` L267 (so @section Scripts loads AFTER jQuery)
- Precedent: `Views/Account/Settings.cshtml:137-146` (exact `@section Scripts { @await Html.PartialAsync("_ValidationScriptsPartial") ... }`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unlock FullName/Email + reword AD info-text + type=email + 4 org validation spans (D-01/D-02/D-03/D-04)</name>
  <read_first>
    - Views/Admin/CreateWorker.cshtml (RE-READ FRESH — confirm anchors before editing; treat line numbers as hypotheses, verify against live file)
    - .planning/phases/392-perbaikan-createworker-audit-field/392-PATTERNS.md (Analog 1 in-file span verbatim + Analog 2 FullName/Email transform)
    - .planning/phases/392-perbaikan-createworker-audit-field/392-UI-SPEC.md (IC-1/IC-2/IC-3 + Copywriting LOCKED string)
  </read_first>
  <files>Views/Admin/CreateWorker.cshtml</files>
  <action>
PREFLIGHT (mandatory, prevents F-NEW-01 duplicate spans): run `grep -n "asp-validation-for" Views/Admin/CreateWorker.cshtml`. Confirm EXACTLY 6 existing spans: FullName (~L69), Email (~L80), NIP (~L85), JoinDate (~L90), Password (~L158), ConfirmPassword (~L168). DO NOT touch or duplicate any of these 6.

EDIT 1 — FullName input (D-01 + D-02). Replace the current FullName input + AD info-text block:
CURRENT:
```html
<input asp-for="FullName" class="form-control @(isAdMode ? "bg-light" : "")"
       placeholder="Masukkan nama lengkap"
       readonly="@(isAdMode ? "readonly" : null)" />
@if (isAdMode)
{
    <div class="form-text text-info"><i class="bi bi-info-circle me-1"></i>Dikelola oleh AD — akan disinkronkan saat login</div>
}
```
REPLACE WITH (remove bg-light ternary + remove readonly entirely + reword info-text):
```html
<input asp-for="FullName" class="form-control"
       placeholder="Masukkan nama lengkap" />
@if (isAdMode)
{
    <div class="form-text text-info"><i class="bi bi-info-circle me-1"></i>Isi sesuai akun AD Pertamina pekerja. Nama &amp; Email akan diselaraskan otomatis dari Active Directory saat pekerja login pertama kali.</div>
}
```
Leave the existing `<span asp-validation-for="FullName" class="text-danger small"></span>` (next line) UNCHANGED.

EDIT 2 — Email input (D-01 + D-02 + D-03). Replace the current Email input + AD info-text block:
CURRENT:
```html
<input asp-for="Email" class="form-control @(isAdMode ? "bg-light" : "")"
       placeholder="contoh@pertamina.com"
       readonly="@(isAdMode ? "readonly" : null)" />
@if (isAdMode)
{
    <div class="form-text text-info"><i class="bi bi-info-circle me-1"></i>Dikelola oleh AD — akan disinkronkan saat login</div>
}
```
REPLACE WITH (remove bg-light + readonly, ADD explicit type="email", reword info-text):
```html
<input asp-for="Email" type="email" class="form-control"
       placeholder="contoh@pertamina.com" />
@if (isAdMode)
{
    <div class="form-text text-info"><i class="bi bi-info-circle me-1"></i>Isi sesuai akun AD Pertamina pekerja. Nama &amp; Email akan diselaraskan otomatis dari Active Directory saat pekerja login pertama kali.</div>
}
```
The reworded info-text string is IDENTICAL for both fields (LOCKED per UI-SPEC Copywriting / D-02). Use `&amp;` for the ampersand in markup. Leave the existing `<span asp-validation-for="Email">` (next line) UNCHANGED.

EDIT 3 — Position span (D-04). Insert IMMEDIATELY AFTER `<input asp-for="Position" ... />` (~L107), inside its `col-md-6`:
```html
<span asp-validation-for="Position" class="text-danger small"></span>
```

EDIT 4 — Directorate span (D-04). Insert IMMEDIATELY AFTER `<input asp-for="Directorate" ... />` (~L111):
```html
<span asp-validation-for="Directorate" class="text-danger small"></span>
```

EDIT 5 — Section span (D-04). Insert IMMEDIATELY AFTER the Section `</select>` (~L117, the `<select asp-for="Section" id="sectionSelect">` block):
```html
<span asp-validation-for="Section" class="text-danger small"></span>
```

EDIT 6 — Unit span (D-04). Insert IMMEDIATELY AFTER the Unit `</select>` (~L123, the `<select asp-for="Unit" id="unitSelect">` block):
```html
<span asp-validation-for="Unit" class="text-danger small"></span>
```

EDIT 7 — Role span (D-04, OPTIONAL — Claude's discretion; RECOMMENDED for visual consistency, zero risk). Insert AFTER the Role `<div class="form-text">Role menentukan...</div>` (~L146):
```html
<span asp-validation-for="Role" class="text-danger small"></span>
```

CONSTRAINTS:
- DO NOT add `required` or asterisk to any org field — Position/Directorate/Section/Unit stay OPTIONAL (model semantics preserved). Spans only surface server messages ("Bagian tidak ditemukan"/"Unit tidak valid").
- DO NOT edit `Controllers/WorkerController.cs` or `Models/ManageUserViewModel.cs` (D-08, 0-diff mandatory).
- DO NOT touch `Views/Admin/EditWorker.cshtml` (out of scope, conscious divergence).
- All 4 org spans use class EXACTLY `text-danger small` (identical to existing 6) — no margin/spacing class.
  </action>
  <verify>
    <automated>grep -c "asp-validation-for" Views/Admin/CreateWorker.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'readonly=|bg-light' Views/Admin/CreateWorker.cshtml` returns NO matches on the FullName/Email inputs (both removed unconditionally).
    - `grep -n 'type="email"' Views/Admin/CreateWorker.cshtml` returns exactly 1 match (on the Email input) and there is NO duplicate `type` attribute on that line.
    - `grep -c 'asp-validation-for' Views/Admin/CreateWorker.cshtml` == 10 (6 existing FullName/Email/NIP/JoinDate/Password/ConfirmPassword + 4 new Position/Directorate/Section/Unit) — OR 11 if the optional Role span was added. (Confirm by inspecting which field names appear; no field appears twice.)
    - `grep -n 'Isi sesuai akun AD Pertamina' Views/Admin/CreateWorker.cshtml` returns exactly 2 matches (FullName + Email info-text); `grep -n 'Dikelola oleh AD' Views/Admin/CreateWorker.cshtml` returns 0 matches.
    - `git diff --name-only` does NOT list `Controllers/WorkerController.cs` or `Models/ManageUserViewModel.cs` or `Views/Admin/EditWorker.cshtml`.
    - `dotnet build HcPortal.csproj` exits 0 (no Razor parse error).
  </acceptance_criteria>
  <done>FullName/Email editable unconditionally (no readonly/bg-light), Email has type=email, AD info-text reworded to the LOCKED string in both blocks, 4 org spans (+ optional Role span) added with class `text-danger small`, no existing span duplicated, build green, controller/model 0-diff.</done>
</task>

<task type="auto">
  <name>Task 2: Enable live client-side validation — move bottom scripts into @section Scripts with _ValidationScriptsPartial (D-05)</name>
  <read_first>
    - Views/Admin/CreateWorker.cshtml (the bottom inline script block ~L194-205, after Task 1 edits)
    - Views/Account/Settings.cshtml (lines ~137-146 — exact @section Scripts { @await Html.PartialAsync("_ValidationScriptsPartial") ... } precedent)
    - Views/Shared/_ValidationScriptsPartial.cshtml (the 2-line partial being included)
    - .planning/phases/392-perbaikan-createworker-audit-field/392-PATTERNS.md (Analog 3 — verbatim @section Scripts wrapper + CAUTION F-NEW-10)
  </read_first>
  <files>Views/Admin/CreateWorker.cshtml</files>
  <action>
REMOVE the current bottom inline script block (~L194-205), which sits in the body AFTER the closing `</div>` of the container:
```html
<script src="~/js/shared-cascade.js"></script>
<script src="~/js/shared-loading.js"></script>
<script>
    initSectionUnitCascade({
        sectionUnits: @Html.Raw(ViewBag.SectionUnitsJson ?? "{}"),
        sectionId: 'sectionSelect',
        unitId: 'unitSelect',
        currentSection: "@(Model.Section ?? "")",
        currentUnit: "@(Model.Unit ?? "")"
    });
    initFormLoading('createWorkerForm', 'Menyimpan...');
</script>
```

REPLACE it with a `@section Scripts` block (place at the END of the file, after the container `</div>`). This wraps `_ValidationScriptsPartial` FIRST, then the moved cascade/loading scripts:
```cshtml
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script src="~/js/shared-cascade.js"></script>
    <script src="~/js/shared-loading.js"></script>
    <script>
        initSectionUnitCascade({
            sectionUnits: @Html.Raw(ViewBag.SectionUnitsJson ?? "{}"),
            sectionId: 'sectionSelect',
            unitId: 'unitSelect',
            currentSection: "@(Model.Section ?? "")",
            currentUnit: "@(Model.Unit ?? "")"
        });
        initFormLoading('createWorkerForm', 'Menyimpan...');
    </script>
}
```

CRITICAL constraints:
- The partial MUST be INSIDE `@section Scripts`, NOT inline in the body (F-NEW-10). Inline-in-body loads before jQuery (`_Layout` jQuery footer L241) → `$ is not defined`. Section content renders at `@RenderSectionAsync("Scripts")` L267, AFTER jQuery — correct order: jQuery → jquery.validate + unobtrusive → shared-cascade/loading → init.
- PRESERVE the Razor interpolation `currentSection: "@(Model.Section ?? "")"` and `currentUnit: "@(Model.Unit ?? "")"` exactly — needed to restore selections after a POST re-render.
- Do NOT change the body of `initSectionUnitCascade` / `initFormLoading` — only relocate the block.
- This view currently has NO `@section Scripts` (verified) — adding a new one will not collide; `dotnet build` would error on a duplicate section (it will not).
  </action>
  <verify>
    <automated>grep -c "_ValidationScriptsPartial" Views/Admin/CreateWorker.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '@section Scripts' Views/Admin/CreateWorker.cshtml` returns exactly 1 match.
    - `grep -n '_ValidationScriptsPartial' Views/Admin/CreateWorker.cshtml` returns exactly 1 match, AND that line appears BEFORE the `shared-cascade.js` line inside the section.
    - `grep -n 'initSectionUnitCascade' Views/Admin/CreateWorker.cshtml` returns exactly 1 match, now inside `@section Scripts` (no leftover inline body copy — the old bottom `<script src="~/js/shared-cascade.js">` outside the section is gone).
    - `grep -n 'currentSection' Views/Admin/CreateWorker.cshtml` still shows the `@(Model.Section ?? "")` interpolation intact.
    - `dotnet build HcPortal.csproj` exits 0 (no duplicate-section / Razor error).
    - `git diff --name-only` does NOT list `Controllers/WorkerController.cs` or `Models/ManageUserViewModel.cs`.
  </acceptance_criteria>
  <done>Bottom script block relocated into a single `@section Scripts` with `_ValidationScriptsPartial` loaded first; cascade/loading init preserved with Razor interpolation intact; build green; controller/model 0-diff. Client-side unobtrusive validation now active (runtime-confirmed in Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WorkerController POST `/Admin/CreateWorker` | Untrusted form input crosses here; FROZEN server validates (ModelState + Section/Unit validity) and authorizes (`[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-392-01 | Tampering | FullName/Email input (removed `readonly`) | accept | `readonly` is a client-side display attribute, NOT a security control. A client could already POST any FullName/Email regardless of `readonly` (it never bound server trust). Server-side `[Required]`/`[EmailAddress]` in the FROZEN model + ModelState remain authoritative. No new trust surface. |
| T-392-02 | Tampering | Client-side validation (jquery-validation-unobtrusive now enabled) | accept | Client-side validation is UX-only. Server ModelState (FROZEN `WorkerController` L216-254) remains the authoritative gate and rejects invalid input even if client-side is bypassed (defense in depth unchanged). |
| T-392-03 | Spoofing/Elevation | CreateWorker POST authorization | mitigate | `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` + `@Html.AntiForgeryToken()` (form L49) — all in FROZEN controller/view-form, unchanged by this plan. |
| T-392-04 | Info Disclosure | `@Html.Raw(ViewBag.SectionUnitsJson)` in moved script | accept | Data is `JsonSerializer.Serialize(dict)` of server-controlled OrganizationUnits (not arbitrary user input). Relocating the same `@Html.Raw` into `@section Scripts` does not expand the surface. Unchanged. |

**Conclusion:** No threat ≥ HIGH introduced. This is a VIEW-ONLY markup change; all server-side authz, anti-forgery, and validation live in the FROZEN controller/model and are untouched. `readonly` was never (and is not) a security boundary.
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `grep -nE 'readonly=|bg-light' Views/Admin/CreateWorker.cshtml` → no matches on FullName/Email inputs.
- `grep -c 'asp-validation-for' Views/Admin/CreateWorker.cshtml` → 10 (or 11 with Role span); no field duplicated.
- `grep` confirms `type="email"`, the reworded info-text ×2, and `_ValidationScriptsPartial` inside `@section Scripts`.
- `git diff --name-only` shows ONLY `Views/Admin/CreateWorker.cshtml` changed (no controller/model/EditWorker).
- Runtime behavior (editable + type=email + cascade + live validation + create success) is proven by Plan 02 Playwright (Razor + cascade JS dynamics require runtime, lesson Phase 354).
</verification>

<success_criteria>
- FullName & Email editable in all modes (readonly + bg-light removed unconditionally) — WRKR-01.
- Email has explicit `type="email"`; 4 org fields have inline validation spans; client-side validation enabled via `_ValidationScriptsPartial` in `@section Scripts` — WRKR-02.
- Build green; controller (`WorkerController.cs`) + model (`ManageUserViewModel.cs`) 0-diff; EditWorker untouched; 0 migration.
</success_criteria>

<output>
After completion, create `.planning/phases/392-perbaikan-createworker-audit-field/392-01-SUMMARY.md`
</output>
