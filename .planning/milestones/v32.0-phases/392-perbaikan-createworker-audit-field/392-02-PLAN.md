---
phase: 392-perbaikan-createworker-audit-field
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tests/e2e/createworker-392.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [WRKR-03]
must_haves:
  truths:
    - "Playwright spec (AD-off, --workers=1) proves FullName/Email typeable, Email type=email, cascade Bagian→Unit builds Unit options at runtime, and create submission succeeds (redirect to ManageWorkers + Success flash + DB row)"
    - "Static source-grep guard asserts CreateWorker.cshtml has NO readonly= and NO bg-light ternary on FullName/Email (proves AD-mode editability by construction)"
    - "git-diff guard asserts WorkerController.cs + ManageUserViewModel.cs are 0-diff (view-only)"
    - "Test worker is created with a unique-per-run email and torn down in afterAll even on failure (Identity cascade, no orphan roles)"
    - "1 SEED_JOURNAL.md entry recorded (active → cleaned)"
  artifacts:
    - path: "tests/e2e/createworker-392.spec.ts"
      provides: "e2e runtime verification of WRKR-01/02/03 + self-cleaning teardown + source-grep + git-diff guards"
      min_lines: 60
    - path: "docs/SEED_JOURNAL.md"
      provides: "1 journal entry for the transient test worker (temporary + local-only)"
  key_links:
    - from: "tests/e2e/createworker-392.spec.ts"
      to: "/Admin/CreateWorker form submit"
      via: "page.fill + page.click + waitForURL('**/ManageWorkers')"
      pattern: "ManageWorkers"
    - from: "tests/e2e/createworker-392.spec.ts afterAll"
      to: "/Admin/DeleteWorker (Identity cascade)"
      via: "page.request.post with __RequestVerificationToken"
      pattern: "DeleteWorker"
---

<objective>
Create the Playwright e2e spec `tests/e2e/createworker-392.spec.ts` that verifies — at runtime, in local AD-OFF mode (`Authentication__UseActiveDirectory=false`, `--workers=1`) — that `/Admin/CreateWorker` works end-to-end: Nama/Email typeable, Email `type="email"`, cascade Bagian→Unit builds Unit options, live validation span surfaces, and a create submission SUCCEEDS (record saved + redirect to ManageWorkers + Success flash). Add a STATIC source-grep guard (readonly/bg-light removed) and a git-diff 0-diff guard (controller/model). Self-clean the test worker via DeleteWorker POST in `afterAll`, and record 1 SEED_JOURNAL entry.

Purpose: Closes WRKR-03 (audit ALL fields + create success) and proves WRKR-01/WRKR-02 at runtime — Razor + cascade JS dynamics are NOT provable by grep+build (lesson Phase 354). The source-grep guard covers the AD-mode readonly bug by construction (AD-off runtime cannot exercise it — Pitfall F-NEW-04).
Output: New spec file + 1 SEED_JOURNAL entry. No production code changed. 0 migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-CONTEXT.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-RESEARCH.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-PATTERNS.md
@.planning/phases/392-perbaikan-createworker-audit-field/392-VALIDATION.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md

<interfaces>
<!-- Verified test-harness contracts (read live this session). Use these directly — no exploration needed. -->

Login + navigate analog: tests/e2e/assessment-title-flexible.spec.ts (lines 6-18 inline loginAny + Promise.all([waitForURL-off-login, click])).
Account fixture: tests/helpers/accounts.ts → `accounts.admin = { email: 'admin@pertamina.com', password: '123456', role: 'Admin' }`. Import: `import { accounts } from '../helpers/accounts';`
DB helper: tests/helpers/dbSnapshot.ts → `db.queryString(sql): Promise<string>` (first non-empty scalar line), `db.queryScalar(sql): Promise<number>`. Import: `import * as db from '../helpers/dbSnapshot';` Table alias `Users` works (used live in delete-records-cascade.spec.ts:42). Connection hard-coded to localhost\SQLEXPRESS / HcPortalDB_Dev.
Serial mode: `test.describe.configure({ mode: 'serial' });`

Verified field selectors (asp-for → id = property name; password ids explicit):
- `#FullName`, `#Email`, `#NIP`, `#Position`, `#Directorate`, `#sectionSelect`, `#unitSelect`, `#Role`, `#passwordField`, `#confirmPasswordField`
- Submit: `button[type="submit"]` (the "Simpan Pekerja" button)
- Success flash on ManageWorkers: `.alert` containing the success text (TempData["Success"]).

Teardown — authenticated DeleteWorker POST (VERIFIED analog exam-types.spec.ts:1609-1618):
```ts
const tokenInput = await page.locator('input[name="__RequestVerificationToken"]').first().getAttribute('value');
const resp = await page.request.post('/Admin/DeleteWorker', {
  form: { id: workerId, __RequestVerificationToken: tokenInput! },
});
```
IMPORTANT teardown nuance (VERIFIED ManageWorkers.cshtml): a freshly-created worker is ACTIVE → its row renders `#deactivate-@user.Id` (L274), NOT `#delete-@user.Id` (which only renders in the inactive `else` branch L293). So clicking `#delete-{id}` will NOT find the button. Use the authenticated `page.request.post('/Admin/DeleteWorker', ...)` above (Option B — robust, bypasses active/inactive UI gating). Identity `_userManager.DeleteAsync` cascades AspNetUserRoles (WorkerController.cs:656). Get the anti-forgery token from any authenticated admin page that has the hidden field (e.g. ManageWorkers, which renders the delete/deactivate forms with `@Html.AntiForgeryToken()`). DO NOT raw-SQL `DELETE FROM AspNetUsers` (skips cascade → orphan roles, F-NEW-07).

Spec skeleton (recommended, from RESEARCH §Playwright spec skeleton) — finalize details:
```ts
const TS = Date.now();
const EMAIL = `e2e-cw-${TS}@local.test`;
const FULLNAME = `E2E CreateWorker ${TS}`;
let workerId = '';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/e2e/createworker-392.spec.ts (runtime e2e + source-grep guard + self-cleaning teardown) + SEED_JOURNAL entry (D-06/D-07)</name>
  <read_first>
    - tests/e2e/assessment-title-flexible.spec.ts (login helper + navigate + admin-form-fill shape, lines ~6-34)
    - tests/e2e/delete-records-cascade.spec.ts (serial mode + afterAll always-runs + db.queryString worker-id resolution, lines ~30,42,72-75)
    - tests/e2e/exam-types.spec.ts (lines ~1609-1618 — authenticated page.request.post DeleteWorker-style with __RequestVerificationToken)
    - tests/helpers/accounts.ts + tests/helpers/dbSnapshot.ts (fixture + db API)
    - Views/Admin/CreateWorker.cshtml (the fixed file — confirm selectors #FullName/#Email/#sectionSelect/#unitSelect/#passwordField/#confirmPasswordField)
    - .planning/phases/392-perbaikan-createworker-audit-field/392-RESEARCH.md (§Validation Architecture + §Playwright spec skeleton + Pitfalls 1-5)
    - docs/SEED_WORKFLOW.md + docs/SEED_JOURNAL.md (journal format)
  </read_first>
  <files>tests/e2e/createworker-392.spec.ts, docs/SEED_JOURNAL.md</files>
  <action>
PRECONDITION header comment in the spec: app must run `Authentication__UseActiveDirectory=false dotnet run` (localhost:5277), start SQLBrowser + shared-memory conn ([[reference_local_e2e_sql_env_fix]]), run with `--workers=1`. Login: admin@pertamina.com / 123456.

Create `tests/e2e/createworker-392.spec.ts` with `test.describe.configure({ mode: 'serial' })`. Imports: `@playwright/test`, `{ accounts }` from `../helpers/accounts`, `* as db` from `../helpers/dbSnapshot`, and `fs`/`path` for the source-grep guard. Define module-level `const TS = Date.now(); const EMAIL = \`e2e-cw-${TS}@local.test\`; const FULLNAME = \`E2E CreateWorker ${TS}\`; let workerId = '';`

Inline `loginAdmin(page)` helper (verbatim shape from assessment-title-flexible.spec.ts):
```ts
async function loginAdmin(page) {
  const { email, password } = accounts.admin;
  await page.goto('/Account/Login');
  await page.fill('input[name="email"]', email);
  await page.fill('input[name="password"]', password);
  await Promise.all([
    page.waitForURL(u => !u.toString().includes('/Account/Login'), { timeout: 15_000 }),
    page.click('button[type="submit"]'),
  ]);
}
```

TEST A — "static guard: readonly/bg-light removed from FullName/Email (WRKR-01 by construction)" (no page needed; D-06 source-grep, covers the AD-mode readonly bug that AD-off runtime can't exercise — Pitfall F-NEW-04):
- Read `Views/Admin/CreateWorker.cshtml` from disk (resolve path relative to spec, e.g. `path.resolve(__dirname, '../../Views/Admin/CreateWorker.cshtml')`).
- Assert the file content does NOT match `/readonly=/` AND does NOT match `/bg-light/`. (After Plan 01, both are removed unconditionally → editable in AD mode is guaranteed by construction.)
- Assert it DOES contain `type="email"` and `_ValidationScriptsPartial` (sanity that Plan 01 landed).

TEST B — "field editable + type=email + cascade + live validation + create succeeds (WRKR-01/02/03)":
- `await loginAdmin(page); await page.goto('/Admin/CreateWorker');`
- WRKR-02 type: `expect(await page.locator('#Email').getAttribute('type')).toBe('email');`
- WRKR-02 live validation (inline span before submit): focus `#FullName`, fill `#Email` with an invalid value (e.g. `'not-an-email'`), blur, then click submit once with an empty FullName → assert a `.text-danger` / `.field-validation-error` message becomes visible WITHOUT a full navigation (URL still `/Admin/CreateWorker`). (If unobtrusive timing is flaky, assert the `#Email`-associated validation span text becomes non-empty after blur+submit attempt.) Then reset/fill valid values to continue.
- WRKR-01 editable: `await page.fill('#FullName', FULLNAME); await page.fill('#Email', EMAIL);` (succeeds = not readonly).
- WRKR-03 cascade: pick the first non-empty Section option value, `selectOption('#sectionSelect', value)`, then `await expect.poll(() => page.locator('#unitSelect option:not([value=""])').count()).toBeGreaterThan(0);` (cascade built Unit options at runtime).
- AD-off → password block visible: `await page.fill('#passwordField', 'Test123!'); await page.fill('#confirmPasswordField', 'Test123!');`
- WRKR-03 submit success: `await Promise.all([ page.waitForURL('**/ManageWorkers', { timeout: 15_000 }), page.click('button[type="submit"]') ]);` then assert success flash visible (`await expect(page.locator('.alert')).toContainText(/berhasil/i)`).
- DB assert + resolve workerId for teardown: `workerId = await db.queryString(\`SELECT TOP 1 Id FROM Users WHERE Email='${EMAIL}'\`); expect(workerId).toBeTruthy();` Also assert the Role persisted if desired (`db.queryString` Role column).

`test.afterAll` (D-07 — MUST run even on failure; self-cleaning):
- If `workerId` empty, try resolving it: `workerId = await db.queryString(\`SELECT TOP 1 Id FROM Users WHERE Email='${EMAIL}'\`).catch(() => '');`
- If `workerId` present: open a fresh authenticated context (`await loginAdmin(page)` on a new page, or reuse browser), `await page.goto('/Admin/ManageWorkers')`, read the anti-forgery token `const token = await page.locator('input[name="__RequestVerificationToken"]').first().getAttribute('value');`, then `await page.request.post('/Admin/DeleteWorker', { form: { id: workerId, __RequestVerificationToken: token! } });` (Identity cascade roles — verified WorkerController.cs:656). Wrap in try/catch (warn-only).
- Verify cleanup: optionally `db.queryScalar(\`SELECT COUNT(*) FROM Users WHERE Email='${EMAIL}'\`)` should be 0.
- DO NOT raw-SQL DELETE (F-NEW-07). DO NOT click `#delete-{id}` (fresh worker is active → only #deactivate-{id} renders; Option B POST bypasses this).

SEED_JOURNAL: append 1 entry to `docs/SEED_JOURNAL.md` per CLAUDE.md Seed Workflow — classification `temporary + local-only`, purpose "Phase 392 e2e create-worker verification", touched entities `AspNetUsers` + `AspNetUserRoles` (1 transient row, email `e2e-cw-{ts}@local.test`), status `active` while running → mark `cleaned` (teardown via DeleteWorker cascade self-cleans each run). Follow the existing journal entry format in the file.

Selector caveats (document in spec comments): live-validation timing for Password "empty" is a SERVER message (Password is NOT [Required] in model — only StringLength + Compare), so do NOT assert a client-side "Password wajib" message (Pitfall 4). The reliable client-side live assertions are: FullName required + Email format.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/createworker-392.spec.ts --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/createworker-392.spec.ts` exists and `grep -c 'test(' tests/e2e/createworker-392.spec.ts` >= 2 (static guard test + e2e test).
    - `grep -n 'mode:.*serial' tests/e2e/createworker-392.spec.ts` present; `grep -n 'afterAll' tests/e2e/createworker-392.spec.ts` present.
    - `grep -n 'DeleteWorker' tests/e2e/createworker-392.spec.ts` present; `grep -n 'DELETE FROM' tests/e2e/createworker-392.spec.ts` returns NO matches (no raw-SQL delete).
    - `grep -nE 'readonly=|bg-light' tests/e2e/createworker-392.spec.ts` present (the source-grep guard references these strings to assert their ABSENCE in the cshtml).
    - With app running AD-off on localhost:5277 + SQLBrowser + `--workers=1`: `npx playwright test e2e/createworker-392.spec.ts --workers=1` passes (both tests green), and after the run `SELECT COUNT(*) FROM Users WHERE Email LIKE 'e2e-cw-%@local.test'` == 0 (teardown self-cleaned).
    - `docs/SEED_JOURNAL.md` has 1 new entry for this test (status reaches `cleaned`).
    - `git diff --name-only` does NOT list `Controllers/WorkerController.cs`, `Models/ManageUserViewModel.cs`, or `Views/Admin/EditWorker.cshtml`.
  </acceptance_criteria>
  <done>Spec runs AD-off `--workers=1` and is green: static source-grep proves readonly/bg-light removed (AD-mode editable by construction), runtime proves Email type=email + cascade Bagian→Unit + live validation + create→redirect ManageWorkers + Success flash + DB row; teardown via DeleteWorker POST (Identity cascade) runs in afterAll even on failure, no orphan rows; 1 SEED_JOURNAL entry recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Final scope verification — git-diff 0-diff guard + build + no-regression (D-08)</name>
  <read_first>
    - .planning/phases/392-perbaikan-createworker-audit-field/392-CONTEXT.md (D-08 scope lock)
    - CLAUDE.md (Develop Workflow — dotnet build + verify before commit; respond Bahasa Indonesia user-facing)
  </read_first>
  <files>(verification only — no production file edits)</files>
  <action>
Run the scope + regression guards that prove the phase is VIEW-ONLY and didn't break anything:

1. git-diff 0-diff guard (D-08): run `git diff --name-only` and confirm the changed set is EXACTLY: `Views/Admin/CreateWorker.cshtml` (Plan 01), `tests/e2e/createworker-392.spec.ts` (new), `docs/SEED_JOURNAL.md` (new entry), plus `.planning/**` plan/doc files. It MUST NOT list `Controllers/WorkerController.cs`, `Models/ManageUserViewModel.cs`, or `Views/Admin/EditWorker.cshtml`. Additionally run `git diff --quiet -- Controllers/WorkerController.cs Models/ManageUserViewModel.cs Views/Admin/EditWorker.cshtml` and confirm exit code 0 (no diff). If any of these three show a diff → STOP, revert that file, the phase is view-only.

2. Build gate: `dotnet build HcPortal.csproj` → 0 error.

3. No-regression on existing test suite (phase adds no xUnit; confirm nothing broke): `dotnet test --filter Category!=Integration` → green (same baseline as before — phase is view + new e2e only). This is a sanity gate; if it was already failing on an unrelated pre-existing issue, note it in the summary rather than block.

4. Confirm the Playwright spec from Task 1 is green (re-state result; do not re-run if already verified in Task 1 within the same session).

If all guards pass, the phase is ready for `/gsd-verify-work`. Per CLAUDE.md Develop Workflow: after local verification (build + run localhost:5277 + Playwright green), commit on branch main and notify IT with commit hash + flag migration=FALSE. Do NOT edit Dev/Prod.
  </action>
  <verify>
    <automated>git diff --quiet -- Controllers/WorkerController.cs Models/ManageUserViewModel.cs Views/Admin/EditWorker.cshtml && echo ZERO_DIFF_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet -- Controllers/WorkerController.cs Models/ManageUserViewModel.cs Views/Admin/EditWorker.cshtml` exits 0 (prints `ZERO_DIFF_OK`).
    - `git diff --name-only` lists `Views/Admin/CreateWorker.cshtml`, `tests/e2e/createworker-392.spec.ts`, `docs/SEED_JOURNAL.md` (+ `.planning/**`) and NOTHING from the frozen set.
    - `dotnet build HcPortal.csproj` exits 0.
    - `dotnet test --filter Category!=Integration` green (or any failure is a documented pre-existing/unrelated issue, not introduced by this phase).
    - 0 migration files added (`git status` shows no new `Migrations/*.cs`).
  </acceptance_criteria>
  <done>Controller (`WorkerController.cs`), model (`ManageUserViewModel.cs`), and `EditWorker.cshtml` confirmed 0-diff; build green; existing suite not regressed; 0 migration. Phase scope lock (D-08) verified — ready for `/gsd-verify-work` and IT notify (migration=FALSE).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test → app `/Admin/CreateWorker` + `/Admin/DeleteWorker` | Test acts as an authenticated admin (accounts fixture). Creates + deletes a transient local test worker. |
| Test → local SQL Server (`db.queryString`) | Read-only DB verification; helper hard-guards localhost-only target (rejects non-localhost). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-392-05 | Info Disclosure | Test credentials in spec | mitigate | Use the existing `accounts.admin` fixture (dev-local `admin@pertamina.com`/`123456`), do NOT hard-code real production credentials. AD-off local-only mode; no prod secrets in the spec. |
| T-392-06 | Tampering (hygiene) | Transient test worker residue in DB | mitigate | Unique-per-run email (`e2e-cw-{ts}@local.test`) + teardown via DeleteWorker POST (Identity cascade) in `afterAll` (runs even on failure) + 1 SEED_JOURNAL entry (active→cleaned). No raw-SQL delete (F-NEW-07 — would orphan AspNetUserRoles). |
| T-392-07 | Tampering | DeleteWorker POST without anti-forgery | accept/mitigate | Teardown reads the real `__RequestVerificationToken` from an authenticated admin page and posts it (matches `[ValidateAntiForgeryToken]` on DeleteWorker L486) — same defense the app enforces; not bypassed. |
| T-392-08 | Elevation | Test targets non-local DB | mitigate | `dbSnapshot` helper rejects any `-S` target that is not `localhost*` (T-315-01 guard, verified) — cannot touch Dev/Prod DB. |

**Conclusion:** No new production attack surface (no production code changes — view edits in Plan 01, test-only here). The only new state is a transient local test worker, fully self-cleaning. Server-side authz + anti-forgery on CreateWorker/DeleteWorker remain FROZEN and enforced.
</threat_model>

<verification>
- `cd tests && npx playwright test e2e/createworker-392.spec.ts --workers=1` → green (static guard + e2e), with app running AD-off on localhost:5277.
- Source-grep guard inside the spec proves `CreateWorker.cshtml` has no `readonly=`/`bg-light` (AD-mode editability by construction).
- `git diff --quiet -- Controllers/WorkerController.cs Models/ManageUserViewModel.cs Views/Admin/EditWorker.cshtml` → exit 0 (0-diff).
- `git diff --name-only` → only `Views/Admin/CreateWorker.cshtml` + new spec + SEED_JOURNAL + `.planning/**`.
- `dotnet build HcPortal.csproj` → 0 error; `dotnet test --filter Category!=Integration` → green.
- Post-run DB check: `COUNT(*) FROM Users WHERE Email LIKE 'e2e-cw-%@local.test'` == 0 (teardown self-cleaned).
- 0 migration.
</verification>

<success_criteria>
- All fields verified runtime; create submission succeeds (record saved + redirect ManageWorkers + Success flash) — WRKR-03.
- Static source-grep proves readonly/bg-light removal (AD-mode editable by construction) — supports WRKR-01.
- Email type=email + live validation span confirmed at runtime — supports WRKR-02.
- Controller + model + EditWorker 0-diff (view-only, D-08); build green; suite not regressed; 0 migration.
- Test worker self-cleaned (unique email + DeleteWorker cascade in afterAll); 1 SEED_JOURNAL entry.
</success_criteria>

<output>
After completion, create `.planning/phases/392-perbaikan-createworker-audit-field/392-02-SUMMARY.md`
</output>
