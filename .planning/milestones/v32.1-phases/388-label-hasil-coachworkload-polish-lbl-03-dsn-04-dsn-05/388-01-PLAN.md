---
phase: 388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/Results.cshtml
autonomous: false
requirements:
  - LBL-03
nyquist_compliant: true

must_haves:
  truths:
    - "Di halaman hasil assessment (/CMP/Results/{id}), kartu tengah menampilkan label 'Batas Nilai Kelulusan' (bukan 'Nilai Kelulusan')"
    - "Nilai persen di bawah label (@Model.PassPercentage%) tidak berubah angka/format"
    - "Tidak ada string 'Nilai Kelulusan' (tanpa kata 'Batas') tersisa di Results.cshtml"
  artifacts:
    - path: "Views/CMP/Results.cshtml"
      provides: "Label kartu tengah ringkasan hasil = 'Batas Nilai Kelulusan'"
      contains: "Batas Nilai Kelulusan"
  key_links:
    - from: "Views/CMP/Results.cshtml L60 (<h6>)"
      to: "Views/CMP/Results.cshtml L61 (<h2>@Model.PassPercentage%)"
      via: "kartu tengah col-md-4 — label di atas nilai persen; nilai tidak disentuh"
      pattern: "Batas Nilai Kelulusan"
---

<objective>
Ubah label kartu tengah ringkasan hasil assessment dari "Nilai Kelulusan" menjadi "Batas Nilai Kelulusan" (LBL-03). HANYA menambah kata "Batas" pada teks `<h6>`; nilai persen `@Model.PassPercentage%` di baris bawahnya tidak diubah.

Purpose: Memperjelas bahwa angka tersebut adalah AMBANG/BATAS kelulusan (bukan nilai yang dicapai peserta — itu ada di kartu "Nilai Anda" di sebelahnya). Murni kosmetik teks.
Output: `Views/CMP/Results.cshtml` dengan label "Batas Nilai Kelulusan".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-CONTEXT.md
@.planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-RESEARCH.md

# Project rules (WAJIB):
# - Respon developer & teks UI = Bahasa Indonesia; kode/commit = English.
# - CLAUDE.md Develop Workflow: verifikasi lokal (dotnet build + dotnet run localhost:5277) SEBELUM commit. JANGAN edit Dev/Prod. JANGAN push tanpa verifikasi lokal.

<interfaces>
<!-- Konteks markup target — executor pakai langsung, tak perlu eksplorasi codebase. -->
<!-- Source: Views/CMP/Results.cshtml L51-63 (kartu skor 3 kolom) [VERIFIED 2026-06-17] -->

L51-56  Kartu KIRI "Nilai Anda" (skor peserta) — JANGAN diubah:
  <div class="col-md-4">
    <div class="text-center p-4 border rounded">
      <h6 class="text-muted mb-2">Nilai Anda</h6>
      <h2 class="mb-1">@Model.Score%</h2>
      <small class="text-muted">(@Model.CorrectAnswers/@Model.TotalQuestions benar)</small>
    </div>
  </div>

L58-63  Kartu TENGAH (target LBL-03):
  <div class="col-md-4">
    <div class="text-center p-4 border rounded">
      <h6 class="text-muted mb-2">Nilai Kelulusan</h6>   <!-- L60 — UBAH jadi "Batas Nilai Kelulusan" -->
      <h2 class="mb-0">@Model.PassPercentage%</h2>        <!-- L61 — JANGAN diubah -->
    </div>
  </div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ubah label kartu tengah jadi "Batas Nilai Kelulusan" (LBL-03 / D-01)</name>
  <read_first>
    - Views/CMP/Results.cshtml (baca L40-75 — konfirmasi konteks kartu skor 3 kolom; pastikan match L60)
    - .planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-RESEARCH.md (§"LBL-03 Exact Target" — grep verified 1 match)
  </read_first>
  <files>Views/CMP/Results.cshtml</files>
  <action>
    Di `Views/CMP/Results.cshtml` baris 60, ubah HANYA teks `<h6>` pada kartu TENGAH:

    DARI:
      `<h6 class="text-muted mb-2">Nilai Kelulusan</h6>`
    MENJADI:
      `<h6 class="text-muted mb-2">Batas Nilai Kelulusan</h6>`

    Aturan (D-01, NON-NEGOTIABLE):
    - HANYA tambah kata "Batas " di depan "Nilai Kelulusan". Class `text-muted mb-2` TETAP (jangan diubah).
    - Baris L61 `<h2 class="mb-0">@Model.PassPercentage%</h2>` JANGAN disentuh (nilai persen tak berubah).
    - JANGAN sentuh kartu "Nilai Anda" (L53 `<h6>Nilai Anda</h6>`) — string berbeda, bukan target.
    - "Nilai Kelulusan" muncul TEPAT 1× di seluruh tree Views/ (grep verified) — tak ada risiko co-replacement.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` 0 error (Razor compile gate).
    - grep di Views/CMP/Results.cshtml: string `Batas Nilai Kelulusan` ADA (>=1 match).
    - grep di Views/CMP/Results.cshtml: string `>Nilai Kelulusan<` (tanpa "Batas") = 0 match.
    - grep di Views/CMP/Results.cshtml: baris `<h2 class="mb-0">@Model.PassPercentage%</h2>` masih ADA tak berubah.
    - grep di Views/CMP/Results.cshtml: `>Nilai Anda<` masih ADA (kartu kiri tak tersentuh).
  </acceptance_criteria>
  <done>
    Label kartu tengah di Results.cshtml = "Batas Nilai Kelulusan"; nilai persen + kartu "Nilai Anda" tak berubah; build hijau.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser label Results (LBL-03)</name>
  <action>Checkpoint UAT manusia — bukan aksi otomatis. Setelah Task 1 build hijau, hentikan eksekusi dan minta user verifikasi label di browser sesuai langkah <how-to-verify>. Lanjut hanya setelah resume-signal.</action>
  <what-built>Label kartu tengah ringkasan hasil assessment diubah jadi "Batas Nilai Kelulusan" (hanya menambah kata "Batas"); nilai persen tidak berubah.</what-built>
  <how-to-verify>
    Prasyarat: `dotnet run` dengan env `Authentication__UseActiveDirectory=false` (login admin lokal `admin@pertamina.com`).
    1. Buka `http://localhost:5277`, login.
    2. Buka satu halaman hasil assessment yang sudah ada — `http://localhost:5277/CMP/Results/{id}` (mis. lewat Daftar Assessment → hasil peserta, atau langsung URL id yang valid mis. /CMP/Results/166).
    3. Lihat 3 kartu skor di atas: KIRI "Nilai Anda" (skor peserta), TENGAH harus berlabel **"Batas Nilai Kelulusan"** dengan angka persen di bawahnya (mis. "70%"), KANAN "Status".
    4. Konfirmasi: label tengah = "Batas Nilai Kelulusan" (bukan "Nilai Kelulusan"); angka persen di bawahnya SAMA seperti sebelumnya (tidak berubah format/nilai).
  </how-to-verify>
  <resume-signal>Ketik "approved" bila label benar "Batas Nilai Kelulusan" & persen tak berubah; atau jelaskan masalah.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (tidak ada baru) | Phase view-only; tak ada input/endpoint/data baru yang melewati boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-388-01 | I (Information Disclosure) | Label statis Razor `<h6>` di Results.cshtml | accept | No new attack surface — teks statis hardcoded, Razor `@` auto-encode untuk `@Model.PassPercentage` (tak diubah). Tidak ada endpoint/input/data user baru di-render mentah. Risiko nihil. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- grep: `Batas Nilai Kelulusan` ADA di Results.cshtml; `>Nilai Kelulusan<` (tanpa Batas) = 0 match.
- UAT browser: kartu tengah Results berlabel "Batas Nilai Kelulusan"; persen tak berubah.
</verification>

<success_criteria>
1. Kartu tengah `/CMP/Results/{id}` menampilkan "Batas Nilai Kelulusan" (bukan "Nilai Kelulusan").
2. `@Model.PassPercentage%` di bawah label tidak berubah.
3. `dotnet build` hijau; kartu "Nilai Anda" tak tersentuh.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-01-SUMMARY.md`.
</output>
