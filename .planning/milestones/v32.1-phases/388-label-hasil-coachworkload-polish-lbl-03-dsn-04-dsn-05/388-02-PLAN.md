---
phase: 388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/CoachWorkload.cshtml
  - tests/e2e/coachworkload-388.spec.ts
autonomous: false
requirements:
  - DSN-04
  - DSN-05
nyquist_compliant: true

must_haves:
  truths:
    - "Filter bar (select section + tombol Filter/Reset) di /Admin/CoachWorkload terbungkus dalam card dengan card-header ikon+judul, konsisten dgn card chart/tabel di halaman yang sama"
    - "Section 'Saran Penyeimbangan' (heading + isi: alert seimbang ATAU daftar suggestion) terbungkus dalam 1 card; item saran = baris list-group (BUKAN card-in-card)"
    - "Tidak ada lagi inline style font-size magic-number (11px/12px/0.85rem) di CoachWorkload.cshtml"
    - "Legend dot grafik memakai kelas .legend-dot (warna ditahan inline background = data status); bulat & berwarna sama seperti sebelum"
    - "Behavior parity terjaga: Setujui Saran, Lewati Saran, Set Threshold (Admin), Filter, Export Excel, Chart render TETAP berfungsi byte-for-byte"
  artifacts:
    - path: "Views/Admin/CoachWorkload.cshtml"
      provides: "Filter bar + Saran Penyeimbangan ber-card; legend dot ber-kelas; font-size util Bootstrap; blok <style> scoped .legend-dot"
      contains: "legend-dot"
    - path: "tests/e2e/coachworkload-388.spec.ts"
      provides: "Playwright parity spec: card framing render + approve/skip/filter/chart hooks utuh"
      contains: "suggestion-card"
  key_links:
    - from: "Views/Admin/CoachWorkload.cshtml list-group-item saran"
      to: "@section Scripts approve-btn/skip-btn handler"
      via: "id=sug-{MappingId} + class suggestion-card + data-* + .approve-btn/.skip-btn dipertahankan (D-08)"
      pattern: "id=\"sug-@sug.MappingId\""
    - from: "Views/Admin/CoachWorkload.cshtml .legend-dot span"
      to: "blok <style> scoped .legend-dot"
      via: "kelas CSS scoped lokal, warna inline background"
      pattern: "class=\"legend-dot\""
---

<objective>
Polish tampilan `Views/Admin/CoachWorkload.cshtml` (DSN-04 + DSN-05) TANPA mengubah perilaku apa pun:
- **DSN-04:** Bungkus filter bar (form L114-131) dan section "Saran Penyeimbangan" (L228-272) masing-masing ke dalam card konsisten dgn card chart/tabel di halaman; item saran jadi baris `list-group` di dalam satu card (hilangkan card-in-card nesting).
- **DSN-05:** Ganti 3 inline magic-number font-size (L93/L104/L156) ke util Bootstrap `small`; pindahkan legend dot inline (L157-159) ke kelas scoped `.legend-dot` (warna ditahan inline); selaraskan spacing (`mb-4` konsisten di card).

Purpose: Tampilan CoachWorkload konsisten & bersih dari magic-number, tetapi SEMUA aksi existing (filter/export/threshold/approve/skip/chart) tetap jalan byte-for-byte (parity = risiko #1 fase ini).
Output: `CoachWorkload.cshtml` ter-polish + spec Playwright parity `coachworkload-388.spec.ts`.

PARITY (D-08, NON-NEGOTIABLE): saat refactor item saran → list-group, setiap hook JS WAJIB dipertahankan: `id="sug-@sug.MappingId"`, class `suggestion-card`, `.approve-btn`/`.skip-btn`, semua `data-*`, role-gate `@if (User.IsInRole("Admin"))`, empty-state alert. Selector di card chart/tabel/modal threshold (`#workloadChart`, `.expand-chevron`, `#thresholdModal` dst) JANGAN disentuh — bukan target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-CONTEXT.md
@.planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-RESEARCH.md
@.planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-VALIDATION.md

# Project rules (WAJIB):
# - Respon developer & teks UI = Bahasa Indonesia; kode/commit = English.
# - CLAUDE.md Develop Workflow: verifikasi lokal (dotnet build + dotnet run localhost:5277) SEBELUM commit. JANGAN edit Dev/Prod. JANGAN push tanpa verifikasi lokal.
# - Lesson Phase 354/385/387: Razor dinamis + JS-driven UI WAJIB Playwright/UAT runtime — grep+build TAK cukup.
# - Playwright: app TIDAK auto-start. Jalankan `dotnet run` (AD-off) DULU, lalu `cd tests; npx playwright test ... --workers=1` (--workers=1 WAJIB, NTLM loopback/shared-memory SQL).

<interfaces>
<!-- JS Hook Preservation Map — selector yang dibaca @section Scripts (L310-524). -->
<!-- Markup refactor HARUS mempertahankan semua ini agar parity terjaga. [VERIFIED 2026-06-17] -->

PERTAHANKAN (di list-group-item saran baru):
  .approve-btn          → L380 querySelectorAll('.approve-btn')
  .skip-btn             → L426 querySelectorAll('.skip-btn')
  .suggestion-card      → L393/L431 btn.closest('.suggestion-card')
  id="sug-{MappingId}"  → L407/L444 getElementById('sug-' + mappingId)
  data-mapping-id       → L382/L428 btn.dataset.mappingId
  data-new-coach-id     → L383 btn.dataset.newCoachId    (approve-btn)
  data-coachee-name     → L384 btn.dataset.coacheeName   (approve-btn)
  data-from-coach       → L385 btn.dataset.fromCoach     (approve-btn)
  data-to-coach         → L386 btn.dataset.toCoach       (approve-btn)
  @if (User.IsInRole("Admin")) → role-gate membungkus div tombol (V4 access control, JANGAN hapus)
  fadeOutCard(el)       → L470-475: generik (opacity/transform + el.remove()) — jalan di list-group-item asalkan id+class tetap

JANGAN SENTUH (bukan target — di card chart/tabel/modal):
  #workloadChart        → L318/L321 canvas + canvas.style.height runtime
  .expand-chevron       → L455-467 chevron collapse (di table card)
  [data-bs-toggle="collapse"] / #coachee-{idx} → collapse daftar coachee (table card)
  #thresholdModal #maxCoachees #warningThreshold #saveThreshold #thresholdError → modal threshold
  window.basePath / RequestVerificationToken / @Html.AntiForgeryToken() → AJAX prefix + CSRF
  Summary Cards (L70-111), seluruh @section Scripts (L310-524)

INLINE-STYLE INVENTORY (DSN-05):
  GANTI ke util `small`:  L93 font-size:11px (sublabel coachee/coach) · L104 font-size:12px (badge "!") · L156 font-size:0.85rem (legend container)
  KE KELAS .legend-dot:   L157-159 (display:inline-block;width:12px;height:12px;border-radius:50%;margin-right:6px;vertical-align:middle) — warna `background:#198754/#ffc107/#dc3545` DITAHAN inline (data status)
  BIARKAN (fungsional, D-05): L115 max-width:300px (select) · L153 max-height:300px;overflow-y:auto (chart scroll) · L154 min-height:150px (canvas, JS-driven L321) · L193 transition:transform 0.2s (chevron, JS-driven L461/465)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Scaffold Playwright parity spec coachworkload-388.spec.ts (RED/fixme)</name>
  <read_first>
    - Views/Admin/CoachWorkload.cshtml (baca penuh L1-525 — terutama §Saran L228-272, filter L113-131, legend L156-160, @section Scripts L310-524 untuk selector parity)
    - .planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-RESEARCH.md (§JS Hook Preservation Map + §Validation Architecture)
    - tests/e2e/essay-grading-384.spec.ts (L1-70 — pola login `loginAny` + import accounts + dbSnapshot + `--workers=1`)
    - tests/helpers/accounts.ts (akun login lokal — admin@pertamina.com, ref reference_dev_credentials)
  </read_first>
  <files>tests/e2e/coachworkload-388.spec.ts</files>
  <action>
    Buat spec Playwright baru `tests/e2e/coachworkload-388.spec.ts` yang men-cek PARITY runtime CoachWorkload pasca-polish. Tiru pola login dari `essay-grading-384.spec.ts` (`loginAny(page, 'admin')` + import `accounts`/`AccountKey`).

    Header komentar (wajib, ikut konvensi spec lain):
    - Tujuan: parity DSN-04/05 (card framing + selector approve/skip/filter/chart utuh).
    - Cara run: `dotnet run` AD-off DULU, lalu `cd tests; npx playwright test coachworkload-388 --workers=1`.
    - RED/fixme: test struktural di-tulis lengkap; bungkus dgn `test.fixme()` untuk yang butuh UI baru (card framing) sampai Task 2 selesai — Task 4 menghapus `.fixme`.

    Test cases (gunakan selector dari §JS Hook Preservation Map; semua di route `/Admin/CoachWorkload`):
    1. `test('render: filter bar terbungkus card dgn card-header')` — goto /Admin/CoachWorkload; assert form `select[name="section"]` ada DI DALAM `.card .card-body`; assert ada `.card-header` mengandung teks "Filter". (fixme sampai Task 2)
    2. `test('render: Saran Penyeimbangan dalam 1 card; item = list-group-item bukan card')` — assert ada `.card-header` teks "Saran Penyeimbangan"; bila ada saran, assert `.list-group-item.suggestion-card` ada DAN tidak ada `.card.suggestion-card` (no card-in-card); bila tak ada saran, assert `.alert-success` empty-state ada. (fixme sampai Task 2)
    3. `test('parity: hook approve/skip & data-* utuh')` — bila ada `.suggestion-card`: assert tiap punya atribut `id` diawali `sug-`; tombol `.approve-btn` punya `data-mapping-id`, `data-new-coach-id`, `data-coachee-name`, `data-from-coach`, `data-to-coach`; tombol `.skip-btn` punya `data-mapping-id`. Bila tak ada saran, `test.skip` dgn pesan "no suggestion data". (fixme sampai Task 2)
    4. `test('parity: chart canvas + legend dot render')` — assert `#workloadChart` ada (bila ada data); assert `.legend-dot` ada minimal 3 (Normal/Mendekati/Overloaded) dengan inline `background`. (fixme sampai Task 2)
    5. `test('parity: filter submit')` — pilih satu opsi `select[name="section"]` (jika ada >1 opsi), klik tombol "Filter", assert URL mengandung `?section=`; klik "Reset", assert URL kembali tanpa query section. (fixme sampai Task 2 — selektor select ada di card)

    Catatan data: saran penyeimbangan butuh data overload (coach >threshold). Bila DB lokal tak punya data overload, test 3 boleh `test.skip` runtime (cek count `.suggestion-card`); JANGAN buat seed permanen. Bila perlu seed, ikuti SEED_WORKFLOW (snapshot → seed temporary → restore di `finally`) — atau biarkan manual UAT (Task 5 checkpoint) yang menutup parity approve/skip. Spec MINIMAL harus assert card framing + chart + filter (test 1/2/4/5) yang tidak butuh data overload.

    Jangan hardcode URL absolut — pakai relative path (`page.goto('/Admin/CoachWorkload')`); baseURL dari playwright.config.ts (localhost:5277).
  </action>
  <verify>
    <automated>cd tests; npx playwright test coachworkload-388 --list</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/coachworkload-388.spec.ts` ADA.
    - `npx playwright test coachworkload-388 --list` menampilkan >=4 test (tidak error parse/import).
    - grep di spec: string `suggestion-card`, `legend-dot`, `workloadChart`, `select[name="section"]` semua ADA (selector parity ter-encode).
    - Test card-framing (1,2,5) ber-`test.fixme()` (RED — UI belum ada sampai Task 2).
  </acceptance_criteria>
  <done>
    Spec parity coachworkload-388 ada, ter-parse, selector parity ter-encode, test framing di-fixme (RED). Belum dijalankan hijau (UI dibangun Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polish CoachWorkload — card framing (DSN-04) + cleanup inline (DSN-05)</name>
  <read_first>
    - Views/Admin/CoachWorkload.cshtml (baca PENUH L1-525 — wajib lihat exact markup yang diedit + @section Scripts untuk konfirmasi selector tak putus)
    - .planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-RESEARCH.md (§Card Pattern Excerpt + §list-group Conversion Sketch + §Inline-Style Inventory + §JS Hook Preservation Map — markup verbatim ada di sini)
    - .planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-CONTEXT.md (D-02..D-08 keputusan terkunci)
  </read_first>
  <files>Views/Admin/CoachWorkload.cshtml</files>
  <behavior>
    - Filter bar ada DI DALAM `.card > .card-body`; ada `.card-header` teks "Filter" + bi-icon.
    - Section "Saran Penyeimbangan" ada DI DALAM 1 `.card`; ada `.card-header` teks "Saran Penyeimbangan"; item saran = `.list-group-item.suggestion-card` (BUKAN `.card.suggestion-card`).
    - Tiap `.list-group-item.suggestion-card` punya `id="sug-{MappingId}"`; `.approve-btn` punya semua 5 data-* ; `.skip-btn` punya data-mapping-id; tombol terbungkus `@if (User.IsInRole("Admin"))`.
    - Empty-state alert "Tidak ada saran penyeimbangan..." TETAP ada bila tak ada saran.
    - 0 inline `font-size:11px` / `font-size:12px` / `font-size: 0.85rem` tersisa.
    - `.legend-dot` kelas terdefinisi di blok `<style>` scoped; 3 span legend pakai `class="legend-dot"` dgn `style="background:..."`.
    - `#workloadChart`, `.expand-chevron`, modal threshold, summary cards, @section Scripts byte-identik (tak diedit).
  </behavior>
  <action>
    Edit `Views/Admin/CoachWorkload.cshtml` dalam SATU task (file ini diedit HANYA di sini = file ownership eksklusif). Lakukan 4 sub-edit; JANGAN sentuh apa pun di luar yang disebut.

    --- A. Blok &lt;style&gt; scoped (DSN-05, D-02/D-04) ---
    Tambahkan SATU blok `<style>` di ATAS file, tepat SETELAH blok `@{ ... }` (setelah baris 14, sebelum `@Html.AntiForgeryToken()`):
      <style>
          .legend-dot {
              display: inline-block;
              width: 12px;
              height: 12px;
              border-radius: 50%;
              margin-right: 6px;
              vertical-align: middle;
          }
      </style>
    (Chevron transition L193 BIARKAN inline — D-04 discretion, JS men-set transform runtime.)

    --- B. Magic-number font-size → util `small` (DSN-05, D-03) ---
    - L93: `<div class="text-muted" style="font-size:11px;">coachee/coach</div>`
        → `<div class="text-muted small">coachee/coach</div>`  (hapus inline style, tambah class `small`)
    - L104: `<span class="badge bg-danger ms-1" style="font-size:12px;">!</span>`
        → `<span class="badge bg-danger ms-1 small">!</span>`  (hapus inline, tambah `small`)
    - L156: `<div class="d-flex gap-4 mt-3 flex-wrap" style="font-size: 0.85rem;">`
        → `<div class="d-flex gap-4 mt-3 flex-wrap small">`  (hapus inline, tambah `small`)

    --- C. Legend dot → kelas .legend-dot (DSN-05, D-04) ---
    Ganti L157-159 — pindahkan properti layout ke kelas, TAHAN warna inline (data status):
      <span><span class="legend-dot" style="background:#198754;"></span>Normal</span>
      <span><span class="legend-dot" style="background:#ffc107;"></span>Mendekati Batas</span>
      <span><span class="legend-dot" style="background:#dc3545;"></span>Overloaded</span>

    --- D. Filter bar → card (DSN-04, D-06) ---
    Bungkus form filter (L114-131) ke card konsisten dgn chart/table card. Tiru class `card shadow-sm mb-4` (SAMA seperti chart card L150 & table card L165 yang berdampingan — JANGAN pakai `border-0` agar seragam dgn tetangga langsung; resolusi Open Question A1 RESEARCH). Hapus `mb-4` dari form (pindah ke card luar agar tak dobel-margin):
      <div class="card shadow-sm mb-4">
          <div class="card-header fw-semibold"><i class="bi bi-funnel me-2"></i>Filter</div>
          <div class="card-body">
              <form method="get" class="d-flex gap-2">
                  <select name="section" class="form-select" style="max-width: 300px;">
                      ... (isi @foreach option PERSIS seperti existing L116-127, JANGAN ubah) ...
                  </select>
                  <button type="submit" class="btn btn-primary">Filter</button>
                  <a href="@Url.Action("CoachWorkload")" class="btn btn-outline-secondary">Reset</a>
              </form>
          </div>
      </div>
    (Select `style="max-width: 300px;"` BIARKAN — D-05 fungsional.)

    --- E. Saran Penyeimbangan → 1 card + list-group (DSN-04, D-07/D-08) ---
    Ganti L228-272 (heading `<h5>` + if-empty alert + foreach card-per-item) dengan SATU card berisi list-group. Gunakan sketch verbatim dari RESEARCH §"list-group Conversion Sketch". Struktur:
      <div class="card shadow-sm mb-4">
          <div class="card-header fw-semibold"><i class="bi bi-arrow-left-right me-2"></i>Saran Penyeimbangan</div>
          @if (!reassignSuggestions.Any())
          {
              <div class="card-body">
                  <div class="alert alert-success mb-0">
                      <i class="bi bi-check-circle me-2"></i>Tidak ada saran penyeimbangan saat ini. Semua coach memiliki beban yang seimbang.
                  </div>
              </div>
          }
          else
          {
              <div class="list-group list-group-flush">
                  @foreach (var sug in reassignSuggestions)
                  {
                      <div class="list-group-item suggestion-card" id="sug-@sug.MappingId">
                          <div class="d-flex justify-content-between align-items-center">
                              <div>
                                  <strong>@sug.CoacheeName</strong>
                                  <span class="text-muted small ms-2">(@sug.CoacheeSection)</span>
                                  <div class="text-muted small mt-1">
                                      Dari <span class="fw-bold text-danger">@sug.FromCoachName</span>
                                      &rarr; ke <span class="fw-bold text-success">@sug.ToCoachName</span>
                                  </div>
                              </div>
                              @if (User.IsInRole("Admin"))
                              {
                                  <div class="d-flex gap-2">
                                      <button class="btn btn-primary btn-sm approve-btn"
                                              data-mapping-id="@sug.MappingId"
                                              data-new-coach-id="@sug.ToCoachId"
                                              data-coachee-name="@sug.CoacheeName"
                                              data-from-coach="@sug.FromCoachName"
                                              data-to-coach="@sug.ToCoachName">
                                          <i class="bi bi-check-circle me-1"></i>Setujui Saran
                                      </button>
                                      <button class="btn btn-outline-secondary btn-sm skip-btn"
                                              data-mapping-id="@sug.MappingId">
                                          <i class="bi bi-x-circle me-1"></i>Lewati Saran
                                      </button>
                                  </div>
                              }
                          </div>
                      </div>
                  }
              </div>
          }
      </div>

    PARITY KRITIS (D-08):
    - `id="sug-@sug.MappingId"` + `class="list-group-item suggestion-card"` WAJIB di tiap baris (closest('.suggestion-card') + getElementById('sug-'+id) keduanya harus match).
    - Semua 5 `data-*` di approve-btn + `data-mapping-id` di skip-btn PERSIS (byte-for-byte dari existing L255-263).
    - `@if (User.IsInRole("Admin"))` membungkus div tombol (role-gate — JANGAN keluarkan tombol dari gate).
    - Empty-state alert TETAP (D-08).
    - `<h5 class="mb-2">Saran Penyeimbangan</h5>` (L229) DIHAPUS — judul pindah ke card-header.

    JANGAN sentuh: @section Scripts (L310-524), modal threshold (L274-308), summary cards (L70-111), chart card (L150-162) kecuali edit B/C di dalamnya, table card (L165-225). #workloadChart, .expand-chevron, semua id JS = byte-identik.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` 0 error (Razor compile, terutama @foreach/@if di list-group baru).
    - grep CoachWorkload.cshtml: `font-size:11px` = 0 match; `font-size:12px` = 0 match; `font-size: 0.85rem` = 0 match.
    - grep CoachWorkload.cshtml: `.legend-dot {` ADA (blok style) DAN `class="legend-dot"` muncul 3× (3 legend).
    - grep CoachWorkload.cshtml: `bi-funnel` ADA (card-header Filter) DAN `>Filter<` di card-header.
    - grep CoachWorkload.cshtml: `list-group-item suggestion-card` ADA; `card shadow-sm mb-3 suggestion-card` = 0 match (no card-in-card).
    - grep CoachWorkload.cshtml: `id="sug-@sug.MappingId"` ADA; `class="card shadow-sm mb-4"` muncul untuk card Filter + card Saran (>=2 card baru, total >=4 dgn chart+table).
    - grep CoachWorkload.cshtml: semua 5 `data-new-coach-id`/`data-coachee-name`/`data-from-coach`/`data-to-coach`/`data-mapping-id` masih ADA; `approve-btn` + `skip-btn` masih ADA; `@if (User.IsInRole("Admin"))` masih membungkus tombol saran.
    - grep CoachWorkload.cshtml: `<h5 class="mb-2">Saran Penyeimbangan</h5>` = 0 match (pindah ke card-header).
    - grep CoachWorkload.cshtml: `id="workloadChart"` masih ADA (chart tak tersentuh); `@section Scripts` masih ADA; `#thresholdModal`/`saveThreshold` masih ADA.
    - grep CoachWorkload.cshtml: `style="max-width: 300px;"` masih ADA (select fungsional dibiarkan); `min-height: 150px` masih ADA (canvas JS-driven).
  </acceptance_criteria>
  <done>
    Filter bar + Saran ber-card konsisten; item saran = list-group (no nesting); 0 magic-number font-size; legend dot ber-kelas; SEMUA hook JS (id/class/data-*/role-gate/empty-state) + chart + modal byte-identik; build hijau.
  </done>
</task>

<task type="auto">
  <name>Task 3: Aktifkan spec parity coachworkload-388 (hapus .fixme, jalankan hijau)</name>
  <read_first>
    - tests/e2e/coachworkload-388.spec.ts (spec dari Task 1)
    - Views/Admin/CoachWorkload.cshtml (markup pasca Task 2 — konfirmasi selector match spec)
    - .planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-RESEARCH.md (§Validation Architecture — command + --workers=1 + AD-off)
  </read_first>
  <files>tests/e2e/coachworkload-388.spec.ts</files>
  <action>
    Prasyarat: `dotnet run` dengan `Authentication__UseActiveDirectory=false` (login admin lokal) di terminal terpisah, app live di localhost:5277.

    1. Hapus `test.fixme()` → `test()` pada test framing (1,2,5) di `coachworkload-388.spec.ts` — UI sekarang ada (Task 2 selesai).
    2. Jalankan: `cd tests; npx playwright test coachworkload-388 --workers=1`.
    3. Selesaikan kegagalan:
       - Bila test 3 (approve/skip data-*) gagal karena TAK ADA data saran di DB lokal → ubah jadi `test.skip` runtime (cek `count(.suggestion-card) === 0`) ATAU seed temporary ikut SEED_WORKFLOW (snapshot DB → seed coach overload → restore di `finally`). JANGAN biarkan seed nempel. Parity approve/skip yang butuh data overload boleh ditutup oleh UAT manual (Task 4 checkpoint) bila seed tak praktis.
       - Bila selector mismatch → perbaiki SPEC agar match markup Task 2 (JANGAN ubah markup yang sudah benar untuk parity).
    4. Pastikan test framing (card filter, card saran, list-group bukan card-in-card, chart canvas, legend dot, filter submit) HIJAU.

    Catatan: --workers=1 WAJIB (NTLM loopback/shared-memory SQL). Jangan tambah watch-mode flag.
  </action>
  <verify>
    <automated>cd tests; npx playwright test coachworkload-388 --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test coachworkload-388 --workers=1` exit 0 (semua test pass atau `test.skip` ber-alasan eksplisit untuk yang butuh data overload — TIDAK ada test gagal/red).
    - grep spec: 0 `test.fixme(` tersisa untuk test framing (1,2,5).
    - Test "filter bar dalam card", "Saran dalam card + list-group", "chart+legend dot render", "filter submit" PASS (bukan skip).
  </acceptance_criteria>
  <done>
    Spec parity coachworkload-388 hijau; card framing + chart + legend + filter ter-assert runtime; approve/skip ter-cover (pass atau skip-with-reason ditutup UAT Task 4).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT browser parity CoachWorkload (DSN-04 + DSN-05)</name>
  <action>Checkpoint UAT manusia — bukan aksi otomatis. Setelah Task 3 (Playwright parity hijau), hentikan eksekusi dan minta user verifikasi tampilan + parity di browser sesuai langkah <how-to-verify>. Lanjut hanya setelah resume-signal.</action>
  <what-built>
    CoachWorkload di-polish: filter bar & "Saran Penyeimbangan" dibungkus card konsisten (item saran = list-group, bukan card-in-card); inline magic-number font-size diganti util `small`; legend dot pakai kelas `.legend-dot`. SEMUA aksi existing dipertahankan (approve/skip/threshold/filter/export/chart).
  </what-built>
  <how-to-verify>
    Prasyarat: `dotnet run` env `Authentication__UseActiveDirectory=false`; login `admin@pertamina.com`.
    Buka `http://localhost:5277/Admin/CoachWorkload` (via Kelola Data → Coach Workload).

    Tampilan (DSN-04/05):
    1. Filter bar tampil DALAM card berlabel "Filter" (card-header ikon corong) — seragam dgn card "Grafik Beban Coach" & "Detail Beban Coach".
    2. Section "Saran Penyeimbangan" tampil dalam 1 card (card-header), item saran = baris list-group (BUKAN card kecil bertumpuk); bila tak ada saran → alert hijau "Tidak ada saran penyeimbangan..." di dalam card.
    3. Legend grafik: 3 titik BULAT berwarna (Normal hijau / Mendekati kuning / Overloaded merah), font legend kecil seragam.
    4. Sublabel "coachee/coach" (kartu Rata-rata Rasio) & badge "!" (kartu Overloaded) tampil kecil & rapi.
    5. Spacing antar card seragam (gap konsisten, tak dobel/rapat).

    Parity (DSN-06 prelim — TIDAK boleh rusak):
    6. Pilih section di dropdown → klik **Filter** → URL `?section=...` + data ter-filter; klik **Reset** → kembali.
    7. **Export Excel** → file ter-unduh.
    8. **Set Threshold** (Admin): klik → modal buka → simpan nilai valid → reload, badge status (Normal/Mendekati/Overloaded) sesuai.
    9. Bila ADA saran: **Setujui Saran** → confirm → baris fade-out & hilang; **Lewati Saran** → baris fade-out & hilang. (Bila tak ada saran di DB lokal, catat "no suggestion data — parity approve/skip dikunci Phase 390".)
    10. Bar chart render (batang horizontal + tooltip).

    Negatif (security parity): login sebagai HC non-Admin (bila ada akun) → tombol Setujui/Lewati & "Set Threshold" TIDAK tampil; notice HC read-only tetap muncul.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila tampilan card konsisten + SEMUA aksi (filter/export/threshold/approve/skip/chart) tetap jalan; atau jelaskan defect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS → /Admin AJAX endpoints | approve/skip/threshold POST (existing, TIDAK diubah — markup hanya menyediakan selector hook) |
| Admin role gate | tombol setujui/lewati/threshold hanya untuk `User.IsInRole("Admin")` (existing, dipertahankan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-388-02 | E (Elevation of Privilege) | role-gate `@if (User.IsInRole("Admin"))` pada tombol saran saat refactor list-group | mitigate | Pertahankan `@if (User.IsInRole("Admin"))` membungkus div tombol PERSIS (D-08, §list-group Conversion Sketch); UAT Task 4 verifikasi HC non-Admin tak melihat tombol. ASVS V4. |
| T-388-03 | T (Tampering) / S | CSRF antiforgery + AJAX endpoint approve/skip/threshold | accept | No new attack surface — `@Html.AntiForgeryToken()` + `RequestVerificationToken` header + endpoint TIDAK disentuh (view-only, Razor auto-encoded). AJAX pakai `(window.basePath||'')` tak hardcode. Existing controls untouched. ASVS V13. |
| T-388-04 | I | Render `@sug.*` di list-group-item (CoacheeName/CoachName) | accept | Razor `@` auto-encode mencegah XSS; data sama yang sudah di-render sebelum refactor (hanya tag pembungkus berubah). Tak ada input baru. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- grep guard CoachWorkload.cshtml: 0 match `font-size:11px`/`font-size:12px`/`font-size: 0.85rem`; `legend-dot` 3×; `list-group-item suggestion-card` ADA; `card shadow-sm mb-3 suggestion-card` 0 match; semua data-* + id="sug-" + role-gate utuh; `#workloadChart` + @section Scripts byte-identik.
- `npx playwright test coachworkload-388 --workers=1` hijau (framing + chart + filter; approve/skip pass atau skip-with-reason).
- UAT browser: card konsisten + parity penuh (filter/export/threshold/approve/skip/chart) jalan; HC non-Admin tak lihat tombol Admin.
</verification>

<success_criteria>
1. Filter bar & "Saran Penyeimbangan" terbungkus card konsisten (`card shadow-sm mb-4` + card-header ikon); item saran = list-group (no card-in-card).
2. 0 inline magic-number font-size; legend dot pakai `.legend-dot`; spacing seragam.
3. SEMUA aksi existing (approve/skip/threshold/filter/export/chart) tetap jalan byte-for-byte (D-08 parity).
4. `dotnet build` + Playwright parity + UAT browser hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/388-label-hasil-coachworkload-polish-lbl-03-dsn-04-dsn-05/388-02-SUMMARY.md`.
</output>
