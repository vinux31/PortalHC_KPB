---
phase: 389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Views/Admin/CoachCoacheeMapping.cshtml
autonomous: false
requirements: [DSN-01, DSN-02, DSN-03]
tags: [ui, razor, bootstrap, accordion, collapse, parity, a11y]

must_haves:
  truths:
    - "Tiap coach group dirender 1 card border-0 shadow-sm mb-3; header berisi avatar inisial + nama + section + badge beban (warna ikut threshold 5/8)"
    - "Klik header card buka/tutup mini-table coachee (default tertutup, card independen multiple-open); 9 kolom existing tampil tanpa 'Coachee Aktif'"
    - "Toolbar seragam: 3 tombol Excel dalam btn-group, 'Tambah Mapping' btn-primary solo, dead onclick dihapus, fungsi modal utuh"
    - "Semua aksi/modal/AJAX/threshold existing berfungsi byte-for-byte (parity) — @section Scripts tak disentuh"
    - "dotnet build 0 error + Playwright spec 389 (V-01..V-14) hijau di localhost:5277"
  artifacts:
    - path: "Views/Admin/CoachCoacheeMapping.cshtml"
      provides: "Accordion card per coach (DSN-01/02) + toolbar seragam (DSN-03), parity-locked hooks utuh"
      contains: "card border-0 shadow-sm mb-3"
  key_links:
    - from: "card-header (collapse trigger)"
      to: "collapse body #collapse-@idx"
      via: "data-bs-toggle=collapse + data-bs-target=#collapse-@idx ↔ id=collapse-@idx"
      pattern: "data-bs-target=\"#collapse-@idx\""
    - from: "<tr data-mapping-id> mini-table"
      to: "submitDelete() row.remove (L973-974, FROZEN)"
      via: "querySelector('tr[data-mapping-id]')"
      pattern: "data-mapping-id=\"@coachee.Id\""
    - from: "tombol Tambah Mapping"
      to: "#assignModal (parity)"
      via: "data-bs-toggle=modal data-bs-target=#assignModal (onclick mati DIHAPUS)"
      pattern: "data-bs-target=\"#assignModal\""
---

<objective>
Rewrite SURGIKAL `Views/Admin/CoachCoacheeMapping.cshtml`: ganti tabel grouped telanjang (`else`-block L228-347)
menjadi **accordion card per coach** (DSN-01/02) + normalisasi toolbar (DSN-03), TANPA meregresi satu pun aksi
existing. RISK TERTINGGI milestone v32.1 → disiplin = SALIN, jangan REINVENT.

Yang DITULIS: toolbar L48-61 (DSN-03) + `else`-block L228-347 (DSN-01/02) + 1 blok `@section Styles` chevron.
Yang DIBEKUKAN (JANGAN sentuh): `@section Scripts` L606-1028 (D-12), semua 5 modal L376-604 + L1031 (D-13),
filter form L176-214, pagination L350-372 (re-emit verbatim SETELAH loop card), empty-state L217-227,
import-results card L104-173, semua alert L64-102, `@Html.AntiForgeryToken()` L30.

Migration: FALSE. 0 backend/controller/endpoint/JS-contract. Verifikasi WAJIB runtime Playwright (Phase 354).

Purpose: DSN-01/02/03 selesai dgn behavior parity terjaga.
Output: `CoachCoacheeMapping.cshtml` ter-redesign + spec 389 hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-CONTEXT.md
@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-UI-SPEC.md
@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-RESEARCH.md
@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-PATTERNS.md
@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-VALIDATION.md

@Views/Admin/CoachCoacheeMapping.cshtml
@Views/Admin/ManageWorkers.cshtml

<parity_checklist>
<!-- Hook yg WAJIB bertahan (RESEARCH §Parity Contract Inventory H-1..H-23). Cek tiap item di markup baru. -->
H-1  : <tr data-mapping-id="@coachee.Id"> tetap <tr> dlm <table> nyata (submitDelete L973-974).
H-2  : onclick="openEditModal(@coachee.Id,'@coachee.CoachId',0,'...yyyy-MM-dd...','@CoacheeName','@AssignmentSection','@AssignmentUnit')" — 7 arg, escaping SAMA.
H-3  : onclick="confirmDeactivate(@coachee.Id,'@coachee.CoacheeName')" — hanya saat IsActive && !IsCompleted.
H-4  : <form asp-action="MarkMappingCompleted" asp-controller="CoachMapping"> + @Html.AntiForgeryToken() + hidden mappingId + onclick="return confirm('Tandai...graduated?')".
H-5  : onclick="reactivateMapping(@coachee.Id)" — hanya saat !IsCompleted && !IsActive.
H-6  : onclick="confirmDelete(@coachee.Id,'@coachee.CoacheeName')" — hanya saat !IsCompleted && !IsActive.
H-7  : urutan if(IsCompleted){badge} else if(IsActive){Nonaktif+Graduated} else{Aktifkan+Hapus} — JANGAN diubah (Phase 356 D-06).
H-8  : <tr class="@(coachee.IsActive ? "" : "table-light text-muted")">.
H-9  : Tambah Mapping tetap data-bs-toggle="modal" data-bs-target="#assignModal"; HAPUS hanya onclick mati.
H-10 : Import tetap data-bs-toggle="modal" data-bs-target="#importMappingModal".
H-11 : Download Template asp-action="DownloadMappingImportTemplate" + Export asp-action="CoachCoacheeMappingExport".
H-12 : data-bs-toggle="collapse" data-bs-target="#collapse-@idx" PINDAH ke card-header.
H-13 : id="collapse-@idx" PINDAH ke <div class="collapse"> (BUANG show — default closed).
H-14 : var idx=0; (L245) + idx++; (L345) dipertahankan → pasangan id↔target unik.
H-15 : badge threshold activeCount>=8?"bg-danger":activeCount>=5?"bg-warning text-dark":"bg-info text-dark" PINDAH ke header.
H-16..H-23 : filter form / empty-state / pagination / 5 modal / coachee-checkbox / import-results / alert / token — TAK DISENTUH.
Pitfall 5 : drop "Coachee Aktif" header L241 DAN sel kosong <td></td> L300 BERSAMA → 9 kolom.
Pitfall 6 : header kolom pakai @OrgLabels.GetLabel(0) Penugasan / @OrgLabels.GetLabel(1) Penugasan verbatim.
</parity_checklist>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalisasi toolbar + hapus dead onclick (DSN-03, V-08/V-09)</name>
  <read_first>
    - Views/Admin/CoachCoacheeMapping.cshtml (toolbar L48-61 — yang ditulis; L52 Import data-bs-target, L55 Export asp-action, L58 dead onclick)
    - .planning/phases/389-.../389-PATTERNS.md (Excerpt F toolbar — markup target verbatim)
    - .planning/phases/389-.../389-RESEARCH.md (§Code Examples toolbar L462-481 + Pitfall 7 dead-onclick removal)
    - .planning/phases/389-.../389-UI-SPEC.md (Component Inventory §5 + Color: btn-primary solo CTA)
  </read_first>
  <files>Views/Admin/CoachCoacheeMapping.cshtml</files>
  <behavior>
    - V-08: "Tambah Mapping" kelas mengandung btn-primary; 3 tombol Excel dalam .btn-group; semua tombol Excel btn-sm.
    - V-09: tombol "Tambah Mapping" getAttribute('onclick') == null/'' (dead onclick hilang); klik → #assignModal visible.
  </behavior>
  <action>
    Ganti blok toolbar L48-61 (`<div class="d-flex gap-2"> ... </div>`) dengan markup berikut (Excerpt F target,
    D-10/D-11). Container `d-flex gap-2 flex-wrap` (tambah `flex-wrap` untuk wrap layar sempit). Bungkus 3 aksi
    Excel dalam `<div class="btn-group">` dgn warna outline SERAGAM `btn-outline-secondary` (Claude's discretion —
    keseragaman agar CTA jadi satu-satunya aksen, RESEARCH Open-Q #2). "Tambah Mapping" solo `btn-sm btn-primary`
    di kanan, HAPUS atribut `onclick="document.getElementById('assignModal').querySelector('[data-bs-dismiss]') && null"`
    TAPI PERTAHANKAN `data-bs-toggle="modal" data-bs-target="#assignModal"`:

    ```cshtml
    <div class="d-flex gap-2 flex-wrap">
        <div class="btn-group">
            <a href="@Url.Action("DownloadMappingImportTemplate", "CoachMapping")" class="btn btn-sm btn-outline-secondary">
                <i class="bi bi-download me-1"></i>Download Template
            </a>
            <button class="btn btn-sm btn-outline-secondary" data-bs-toggle="modal" data-bs-target="#importMappingModal">
                <i class="bi bi-file-earmark-arrow-up me-1"></i>Import Excel
            </button>
            <a asp-controller="CoachMapping" asp-action="CoachCoacheeMappingExport" class="btn btn-sm btn-outline-secondary">
                <i class="bi bi-file-earmark-excel me-1"></i>Export Excel
            </a>
        </div>
        <button class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#assignModal">
            <i class="bi bi-plus-circle me-1"></i>Tambah Mapping
        </button>
    </div>
    ```

    Parity wajib (parity_checklist): H-9 (assignModal toggle), H-10 (importMappingModal toggle), H-11
    (asp-action Download/Export). JANGAN ubah label teks (Bahasa Indonesia verbatim). JANGAN sentuh page-header
    H2/subtitle L41-46.
  </action>
  <verify>
    <automated>dotnet build --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit 0 (Razor compile OK).
    - `grep -c "querySelector('\[data-bs-dismiss\]')" Views/Admin/CoachCoacheeMapping.cshtml` == 0 (dead onclick hilang).
    - `grep "btn-group" Views/Admin/CoachCoacheeMapping.cshtml` ketemu (Excel grouped).
    - `grep 'data-bs-target="#assignModal"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-9 utuh).
    - `grep 'data-bs-target="#importMappingModal"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-10 utuh).
    - `grep 'asp-action="CoachCoacheeMappingExport"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-11 utuh).
    - `grep 'asp-action="DownloadMappingImportTemplate"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-11 utuh).
    - `grep -c "Tambah Mapping" Views/Admin/CoachCoacheeMapping.cshtml` >= 1; tombolnya `btn-primary` (grep `btn-sm btn-primary`).
  </acceptance_criteria>
  <done>Toolbar seragam (Excel btn-group + CTA btn-primary solo), dead onclick dihapus, modal toggle parity utuh, build 0 error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite else-block jadi accordion card per coach + mini-table + Aksi + chevron style (DSN-01/02, V-01..V-07, V-10..V-12)</name>
  <read_first>
    - Views/Admin/CoachCoacheeMapping.cshtml (else-block L228-347 — yang ditulis; coach-header L249-267, badge L260-264, coachee rows + Aksi L271-343, pagination L350-372 KEEP, var idx L245/L345)
    - Views/Admin/ManageWorkers.cshtml (avatar idiom L251-254 — reuse verbatim D-03)
    - .planning/phases/389-.../389-RESEARCH.md (§Bootstrap Collapse-in-Card Technique L265-310 markup target + chevron CSS; §Code Examples mini-row L404-459)
    - .planning/phases/389-.../389-PATTERNS.md (Excerpt B card wrapper, Excerpt C a11y toggle, Excerpt D badge MOVE, Excerpt E mini-row PARITY, Excerpt G chevron style)
    - .planning/phases/389-.../389-UI-SPEC.md (Component Inventory §1-4,6 + Accessibility section + States)
    - .planning/phases/389-.../389-CONTEXT.md (D-01..D-09, D-14 + Claude's Discretion)
  </read_first>
  <files>Views/Admin/CoachCoacheeMapping.cshtml</files>
  <behavior>
    - V-01: setiap coach group = 1 .card.shadow-sm; header punya .avatar-initial + nama coach.
    - V-02: badge header warna ikut threshold (n<5 bg-info / 5<=n<8 bg-warning / n>=8 bg-danger).
    - V-03: default ALL CLOSED — .collapse.show count==0 saat load; header aria-expanded="false".
    - V-04: klik header → #collapse-0 visible + aria-expanded="true"; klik lagi → tutup + aria-expanded="false".
    - V-05: card independen — buka card0 & card1 → keduanya visible bersamaan (no data-bs-parent).
    - V-06: mini-table thead th == 9; tak ada th "Coachee Aktif".
    - V-07: header card a11y — role="button" atau <button>, aria-controls==body id, Enter/Space toggle.
    - V-10/V-11/V-12: Edit→#editModal, Hapus→#deleteModal, branch IsCompleted DULU (parity).
  </behavior>
  <action>
    Ganti SELURUH isi `else { <table>...</table> + pagination }` L228-373. STRUKTUR BARU:

    1) BUANG `<table class="table table-bordered align-middle">` + `<thead class="table-dark">` lama (L230-244)
       — header kolom global dipindah ke dalam tiap mini-table.
    2) Pertahankan `@{ var idx = 0; }` SEBELUM loop (D-14/H-14).
    3) `@foreach (var group in groupedCoaches) { ... idx++; }` — tiap group render SATU card (Collapse-in-Card
       Technique RESEARCH L265-299), card-header sbg collapse toggle:

    ```cshtml
    @{ var idx = 0; }
    @foreach (var group in groupedCoaches)
    {
        <div class="card border-0 shadow-sm mb-3">
            <button type="button"
                    class="card-header bg-white w-100 text-start border-0 d-flex align-items-center gap-2 collapsed"
                    style="cursor:pointer"
                    data-bs-toggle="collapse" data-bs-target="#collapse-@idx"
                    aria-expanded="false" aria-controls="collapse-@idx">
                <div class="avatar-initial rounded-circle bg-primary text-white d-flex align-items-center justify-content-center fw-bold"
                     style="width:36px;height:36px;font-size:0.8rem" aria-hidden="true">
                    @( ((string)group.CoachName).Length > 0 ? ((string)group.CoachName).Substring(0,1).ToUpper() : "?" )
                </div>
                <span class="fw-semibold">@group.CoachName</span>
                @if (!string.IsNullOrEmpty((string)group.CoachSection))
                {
                    <span class="text-muted small">— @group.CoachSection</span>
                }
                <span class="ms-auto">
                    @{
                        int activeCount = (int)group.ActiveCount;
                        string badgeClass = activeCount >= 8 ? "bg-danger" : activeCount >= 5 ? "bg-warning text-dark" : "bg-info text-dark";
                    }
                    <span class="badge @badgeClass" title="@activeCount coachee aktif">@activeCount</span>
                </span>
                <i class="bi bi-chevron-down chevron-toggle"></i>
            </button>
            <div class="collapse" id="collapse-@idx">
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table align-middle mb-0">
                            <thead>
                                <tr>
                                    <th>Nama</th>
                                    <th>NIP</th>
                                    <th>@OrgLabels.GetLabel(0) Penugasan</th>
                                    <th>@OrgLabels.GetLabel(1) Penugasan</th>
                                    <th>Jabatan</th>
                                    <th>Proton Track</th>
                                    <th>Status</th>
                                    <th>Mulai</th>
                                    <th>Aksi</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var coachee in group.Coachees)
                                {
                                    @* SALIN baris VERBATIM dari L273-342, HANYA buang sel kosong <td></td> (L300) *@
                                    <tr class="@(coachee.IsActive ? "" : "table-light text-muted")" data-mapping-id="@coachee.Id">
                                        ... (8 td data + 1 td Aksi — lihat parity_checklist H-1..H-8, Excerpt E) ...
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        idx++;
    }
    ```

    Untuk isi `<tr>` mini-table: SALIN VERBATIM dari file lama L273-342 (Excerpt E / RESEARCH §Code Examples
    L404-459) — 8 `<td>` data (Nama, NIP, AssignmentSection em-dash fallback `—`, AssignmentUnit em-dash,
    CoacheePosition, ProtonTrack badge, Status badge, StartDate `dd MMM yyyy` id-ID) LALU `<td>` Aksi.
    **PERUBAHAN TUNGGAL vs lama: BUANG `<td></td>` kosong L300 (kolom "Coachee Aktif").** Semua `onclick`/form/
    branch Aksi (H-2..H-7) SALIN PERSIS — urutan `if IsCompleted → else if IsActive → else` JANGAN diubah.

    4) Pagination L350-372: re-emit VERBATIM SETELAH `@foreach` card ditutup (masih di dalam `else`-block, sekali
       saja — Pitfall 8). JANGAN taruh di dalam loop.

    5) Tambah `@section Styles` (slot `_Layout.cshtml:44`) untuk rotasi chevron (Excerpt G, scoped, no shared CSS):
    ```cshtml
    @section Styles {
        <style>
            [data-bs-toggle="collapse"] .chevron-toggle { transition: transform .2s ease; }
            [data-bs-toggle="collapse"]:not(.collapsed) .chevron-toggle { transform: rotate(180deg); }
        </style>
    }
    ```
    Pastikan trigger awal punya kelas `collapsed` (sudah di markup di atas) supaya chevron tidak ter-rotate saat
    load (Pitfall 2/3). Body `collapse` TANPA `show` TANPA `data-bs-parent` (D-05, independen).

    JANGAN sentuh: empty-state `if`-block (L217-227), modal (L376+), `@section Scripts` (L606-1028), filter form,
    alert, import-results, token. JANGAN ubah label teks/format tanggal/budaya id-ID. JANGAN hardcode path AJAX.
  </action>
  <verify>
    <automated>dotnet build --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit 0 (Razor compile OK).
    - `grep -c "card border-0 shadow-sm mb-3" Views/Admin/CoachCoacheeMapping.cshtml` >= 1 (card per coach).
    - `grep -c "avatar-initial" Views/Admin/CoachCoacheeMapping.cshtml` >= 1 (D-03 reuse).
    - `grep 'data-bs-target="#collapse-@idx"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-12) DAN
      `grep 'id="collapse-@idx"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-13 pair).
    - `grep -c 'class="collapse show"' Views/Admin/CoachCoacheeMapping.cshtml` == 0 di blok coach (default closed; show lama hilang).
    - `grep -c "data-bs-parent" Views/Admin/CoachCoacheeMapping.cshtml` == 0 (independen, D-05).
    - `grep 'activeCount >= 8 ? "bg-danger" : activeCount >= 5 ? "bg-warning text-dark" : "bg-info text-dark"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-15 verbatim threshold 5/8).
    - `grep -c "Coachee Aktif" Views/Admin/CoachCoacheeMapping.cshtml` == 0 (kolom dilepas, D-07 — header lama hilang).
    - `grep -c 'data-mapping-id="@coachee.Id"' Views/Admin/CoachCoacheeMapping.cshtml` >= 1 (H-1 hook delete).
    - `grep "openEditModal(@coachee.Id, '@coachee.CoachId', 0," Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-2 7-arg).
    - `grep 'asp-action="MarkMappingCompleted"' Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-4 graduated form).
    - `grep "reactivateMapping(@coachee.Id)" Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-5) DAN
      `grep "confirmDeactivate(@coachee.Id" Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-3) DAN
      `grep "confirmDelete(@coachee.Id" Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-6).
    - `grep -c "@OrgLabels.GetLabel(0) Penugasan" Views/Admin/CoachCoacheeMapping.cshtml` >= 1 (Pitfall 6).
    - `grep "chevron-toggle" Views/Admin/CoachCoacheeMapping.cshtml` ketemu DAN `grep "@section Styles" Views/Admin/CoachCoacheeMapping.cshtml` ketemu.
    - `grep -c "@section Scripts" Views/Admin/CoachCoacheeMapping.cshtml` == 1 (Scripts utuh, tak terganggu).
    - `grep "var idx = 0" Views/Admin/CoachCoacheeMapping.cshtml` ketemu DAN `grep "idx++" Views/Admin/CoachCoacheeMapping.cshtml` ketemu (H-14).
  </acceptance_criteria>
  <done>else-block jadi accordion card per coach (default closed, independen, 9 kolom, avatar+badge threshold), Aksi block parity verbatim, chevron style scoped, build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: Isi spec V-01..V-14 ke selector aktual + run build + Playwright + smoke localhost:5277</name>
  <read_first>
    - tests/e2e/coachcoacheemapping-389.spec.ts (spec dari Plan 01 — isi assertion stub ke selector markup aktual)
    - Views/Admin/CoachCoacheeMapping.cshtml (markup baru hasil Task 1-2 — sumber selector aktual: .card.shadow-sm, .card-header button, #collapse-N, .avatar-initial, badge, thead th, #assignModal, #editModal, #deleteModal, #editCoacheeName)
    - .planning/phases/389-.../389-VALIDATION.md (Per-Task Map + run command + Manual-Only verifications)
    - .planning/phases/389-.../389-RESEARCH.md (§Concrete Playwright Parity Test Plan 1-14 — assertion presisi)
    - tests/e2e/coachworkload-388.spec.ts (referensi gaya assertion + data-skip yang sudah lulus)
  </read_first>
  <files>tests/e2e/coachcoacheemapping-389.spec.ts</files>
  <action>
    Lengkapi/sesuaikan assertion V-01..V-14 di spec agar cocok dgn selector markup AKTUAL hasil Task 1-2.
    Verifikasi nama tombol submit filter (Cari/Filter) di halaman & sesuaikan V-14. Pastikan locator card-header
    cocok dgn `<button class="card-header ...">` (tagName BUTTON, V-07). Pastikan V-04/V-05 pakai `#collapse-0`/
    `#collapse-1`. Pertahankan SEMUA `test.skip` data-guard (DB lokal bisa kurang data → skip, jangan fail).

    Lalu jalankan gate verifikasi (CLAUDE.md Develop Workflow):
    1. `dotnet build` → 0 error.
    2. Jalankan app lokal: `dotnet run` dgn `Authentication__UseActiveDirectory=false` (app TIDAK auto-start),
       tunggu localhost:5277 siap. (Lihat reference_local_e2e_sql_env_fix: start SQLBrowser bila perlu.)
    3. `cd tests; npx playwright test coachcoacheemapping-389 --workers=1` → semua test PASS atau SKIP (0 FAIL).
       (`--workers=1` WAJIB — NTLM/shared-mem SQL.)
    4. Stop app setelah test.

    Bila ada test FAIL karena selector tak cocok markup → perbaiki ASSERTION spec (bukan markup, kecuali markup
    benar-benar melanggar parity_checklist — bila begitu, perbaiki markup Task 2 & catat di SUMMARY). Test yang
    butuh data mutasi penuh harus ber-`test.skip` (mutasi penuh = Phase 390), bukan FAIL.
  </action>
  <verify>
    <automated>cd tests && npx playwright test coachcoacheemapping-389 --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit 0.
    - `cd tests && npx playwright test coachcoacheemapping-389 --workers=1` → 0 FAIL (semua PASS atau SKIP).
    - Minimal test struktural NON-skip hijau: `card per coach + header` (V-01), `default closed` (V-03),
      `collapse buka tutup` (V-04), `9 kolom` (V-06), `a11y header toggle` (V-07), `toolbar seragam` (V-08),
      `tambah mapping buka modal` (V-09) — PASS (bukan skip) bila ada minimal 1 coach group di DB lokal.
    - Spec output mencatat alasan skip yang jelas (Bahasa Indonesia) untuk test parity tanpa data.
    - Tak ada error 404/500 di console saat collapse/modal/AJAX intercept (V-13).
  </acceptance_criteria>
  <done>Spec 389 dijalankan di localhost:5277: 0 FAIL; test struktural hijau; parity ber-skip wajar; build 0 error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint UAT visual/aksi — verifikasi manusia tampilan & parity (Phase 354 lesson)</name>
  <what-built>
    Redesign CoachCoacheeMapping.cshtml jadi accordion card per coach (DSN-01/02) + toolbar seragam (DSN-03).
    Otomatis sudah lulus: dotnet build 0 error + Playwright spec 389 (V-01..V-14) 0 FAIL di localhost:5277.
    Yang TAK bisa di-assert selector (Phase 354 — visual/animasi subjektif): ritme spacing antar-card, animasi
    chevron buka/tutup, legibilitas avatar, kerapian toolbar di layar sempit.
  </what-built>
  <how-to-verify>
    Aplikasi lokal sudah jalan (`dotnet run`, `Authentication__UseActiveDirectory=false`) di http://localhost:5277.
    1. Buka http://localhost:5277/Admin/CoachCoacheeMapping (login admin@pertamina.com / 123456).
    2. Cek tiap coach = 1 card; header: avatar inisial (bulat biru) + nama + section muted + badge angka beban
       di kanan; warna badge: <5 biru(info) / 5-7 kuning(warning) / >=8 merah(danger). Semua card TERTUTUP saat load.
    3. Klik header sebuah card → mini-table coachee muncul; chevron berputar; kolom: Nama, NIP, (label0)
       Penugasan, (label1) Penugasan, Jabatan, Proton Track, Status, Mulai, Aksi (9 kolom, TANPA "Coachee Aktif").
    4. Buka 2 card sekaligus → keduanya tetap terbuka (independen).
    5. Klik Edit → modal Edit muncul terisi. Klik "Tambah Mapping" → modal Assign muncul. Klik "Import Excel" →
       modal Import muncul. Tombol toolbar Excel rapi dalam satu grup; "Tambah Mapping" tombol biru solo.
    6. (bila ada data) Nonaktifkan / Graduated / Aktifkan / Hapus berfungsi seperti sebelum redesign.
    7. Filter Seksi + Cari + "Tampilkan Semua" + pagination tetap jalan.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila tampilan & semua aksi benar, atau jelaskan masalah (akan diperbaiki sebelum lanjut).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server (Razor render) | Server-rendered view, admin-only page. Tak ada perubahan auth/role. |
| markup → @section Scripts (FROZEN) | Markup baru memanggil fungsi JS existing via onclick/data-* (kontrak tak berubah). |
| client → AJAX endpoints (CoachMapping/Admin) | Tak disentuh — appUrl + RequestVerificationToken existing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-389-02-01 | T (Tampering/XSS) | Output coach/coachee text di markup baru | mitigate | Razor auto-encode semua `@group.CoachName`/`@coachee.*` (HTML-encoded). TIDAK menambah `@Html.Raw` baru — dua `@Html.Raw(JsonSerializer...)` existing (sectionUnits/coachWorkloads) TAK disentuh. Tak ada XSS sink baru. Verify: `grep -c "@Html.Raw" CoachCoacheeMapping.cshtml` tak bertambah dari baseline. |
| T-389-02-02 | E (Elevation) | Auth/role halaman | accept | Halaman admin-only TAK berubah (tak sentuh `[Authorize]`/controller). Murni view markup. Tak ada elevation surface baru. |
| T-389-02-03 | I (Info disclosure) | Inline onclick arg escaping (apostrof, mis. O'Brien) | accept | Risiko PRE-EXISTING di markup lama (RESEARCH Pitfall 4); SALIN escaping apa adanya = parity. Hardening (data-attribute) = JS-contract change = OUT scope (D-12). Bukan regresi phase ini. |
| T-389-02-04 | T (Tampering) | AJAX path hardcode → 404 sub-path | mitigate | Markup baru TAK menambah path AJAX (semua di @section Scripts FROZEN pakai appUrl). Tag-helper asp-action route-generated. V-13 route-intercept assert prefix benar. |

**Residual-risk verdict:** LOW. Murni view-markup; Razor auto-encode mencegah XSS baru; 0 endpoint/auth/JS-contract
change; AJAX PathBase-aware tak disentuh. Tak ada threat high → tak ada block-on security. Block-on perilaku =
checkpoint human-verify (visual/animasi tak assertable selector — Phase 354 lesson).
</threat_model>

<verification>
- `dotnet build` 0 error (Razor compile).
- `cd tests && npx playwright test coachcoacheemapping-389 --workers=1` → 0 FAIL.
- Semua parity_checklist H-1..H-15 ter-grep utuh di markup baru; H-16..H-23 tak tergeser.
- `@section Scripts` (L606-1028) byte-identik (1 occurrence, tak diubah).
- Visual/animasi/aksi mutasi → checkpoint human-verify (+ parity PENUH = Phase 390 / DSN-06).
- Migration=FALSE (0 backend/controller/skema).
</verification>

<success_criteria>
- DSN-01: accordion card per coach (avatar inisial + nama + section + badge beban warna-ikut-threshold 5/8).
- DSN-02: klik header buka/tutup mini-table coachee (default tertutup, independen, 9 kolom existing tanpa "Coachee Aktif").
- DSN-03: toolbar seragam (Excel btn-group + CTA btn-primary solo) + dead onclick dihapus tanpa ubah fungsi modal.
- Behavior parity terjaga (V-01..V-14 0 FAIL + checkpoint human-verify approved).
- dotnet build 0 error + Playwright hijau di localhost:5277; migration=FALSE.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-02-SUMMARY.md`
</output>
