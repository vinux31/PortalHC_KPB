---
phase: 390-test-uat-behavior-parity-dsn-06
plan: 02
type: execute
wave: 2
depends_on: [390-01]
files_modified:
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [DSN-06]
tags: [uat, parity, playwright-mcp, snapshot-restore, regression]

must_haves:
  truths:
    - "Every D-04 roundtrip action on CoachCoacheeMapping succeeds live with no error/500: tambah, edit, nonaktifkan, graduated, hapus, aktifkan-kembali"
    - "Every D-04 roundtrip action on CoachWorkload succeeds live: filter section, set threshold (Admin), setujui & lewati saran (conditional on overload)"
    - "Manual Excel import (fixture from Plan 01) succeeds: import-results card renders + new mapping row appears"
    - "All live DB mutations are wrapped BACKUP → mutate → RESTORE; SEED_JOURNAL.md logs the run and is marked cleaned (mapping COUNT == baseline)"
    - "dotnet test green (backend suite not regressed) + escalated Playwright parity specs green"
    - "No controller/service/Helpers/migration file modified across the v32.1 milestone (grep diff confirms); AJAX uses appUrl()/basePath, not hardcoded paths"
  artifacts:
    - path: "docs/SEED_JOURNAL.md"
      provides: "Audit entry for the Phase 390 UAT live-mutation run (temporary + local-only), marked cleaned after restore"
      contains: "390"
  key_links:
    - from: "live UAT roundtrip"
      to: "HcPortalDB_Dev (snapshot/restore)"
      via: "BACKUP DATABASE → mutate via Playwright MCP → RESTORE DATABASE WITH REPLACE"
      pattern: "RESTORE DATABASE HcPortalDB_Dev"
    - from: "manual import"
      to: "/Admin/ImportCoachCoacheeMapping"
      via: "fixture import-mapping-390.xlsx upload (inside snapshot window)"
      pattern: "ImportCoachCoacheeMapping"
---

<objective>
Prove the v32.1 redesign did NOT regress any live mutation on the two Admin coach surfaces — by running the
full D-04 action roundtrip via Playwright MCP wrapped in DB snapshot/restore, doing the manual Excel import,
and signing off the regression suite. This is the non-autonomous (checkpoint) half of DSN-06: an autonomous
executor cannot drive Playwright MCP or mutate the live DB, so each block is a human/main-agent checkpoint
with an explicit step list, snapshot/restore protocol, and acceptance signals.

Purpose: CONTEXT D-01 splits verification — Playwright (Plan 01) covers non-destructive parity; the REAL
mutation roundtrip (tambah/edit/nonaktif/graduated/hapus/reactivate + filter/threshold/approve/skip) is
executed live via Playwright MCP per D-03/D-04, ALWAYS wrapped in SEED_WORKFLOW BACKUP→RESTORE. Manual Excel
import per D-05. Defect-fix boundary per D-06 (view-only inline OK; backend → STOP + report).

Output:
- Live-mutation UAT roundtrip executed + screenshots, DB cross-checked, then RESTORED.
- Manual Excel import verified inside the snapshot window, then RESTORED.
- SEED_JOURNAL.md entry logged → marked cleaned (COUNT back to baseline).
- `dotnet test` green + escalated Playwright specs green.
- Milestone v32.1 sign-off: 0 backend / 0 migration confirmed; ready for 1 push → notify IT.

CONSTRAINT LOCKED: 0 backend, 0 controller, 0 endpoint, 0 migration. If any defect found needs a tier
outside `Views/Admin/*.cshtml` (markup / `@section Scripts` / `@section Styles`) or `tests/` → STOP + report
(D-06), do NOT fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/390-test-uat-behavior-parity-dsn-06/390-CONTEXT.md
@.planning/phases/390-test-uat-behavior-parity-dsn-06/390-RESEARCH.md
@.planning/phases/390-test-uat-behavior-parity-dsn-06/390-UI-SPEC.md
@.planning/phases/390-test-uat-behavior-parity-dsn-06/390-01-SUMMARY.md
@docs/SEED_WORKFLOW.md
@docs/SEED_JOURNAL.md
@CLAUDE.md

<parity_action_map>
<!-- The exact D-04 roundtrip — one verification per action. Selectors verified against shipped markup. -->

CoachCoacheeMapping (/Admin/CoachCoacheeMapping):
- C1 TAMBAH: "Tambah Mapping" → #assignModal → pick #assignCoachSelect + a .coachee-checkbox + #assignStartDate → "Simpan" (submitAssign) → POST appUrl('/Admin/CoachCoacheeMappingAssign') → reload, new row appears, no 500.
- C2 EDIT: open card → row "Edit" (openEditModal 7-arg → #editModal) → change #editStartDate / #editCoachSelect → "Simpan" (submitEdit) → POST .../CoachCoacheeMappingEdit → value updated.
- C3 NONAKTIFKAN: "Nonaktifkan" (confirmDeactivate) → #deactivateModal (#deactivateSessionInfo preloaded) → confirm (submitDeactivate) → POST .../CoachCoacheeMappingDeactivate → row badge "Non-aktif".
- C4 GRADUATED: form MarkMappingCompleted (POST + AntiForgeryToken + confirm('Tandai coachee ini sebagai graduated?')) → row badge "Graduated".
- C5 HAPUS: "Hapus" (confirmDelete → #deleteModal, preview preload) → submit "Hapus" (submitDelete) → POST .../CoachCoacheeMappingDelete → tr[data-mapping-id].remove() + toast.
- C6 AKTIFKAN-KEMBALI: #showAllCheck=on → "Aktifkan" on inactive row (reactivateMapping confirm) → POST .../CoachCoacheeMappingReactivate → badge "Aktif" (or Swal assign-track prompt if data.showAssignPrompt).
- C7 IMPORT (manual): "Import Excel" → #importMappingModal → upload tests/fixtures/import-mapping-390.xlsx → "Upload & Proses" → redirect + import-results card (Berhasil Dibuat / Diaktifkan / Skip / Error).

CoachWorkload (/Admin/CoachWorkload):
- W1 FILTER: select[name=section] → "Filter" → URL section= ; "Reset" clears.
- W3 SET THRESHOLD (Admin): "Set Threshold" → #thresholdModal → change #maxCoachees / #warningThreshold → #saveThreshold ("Simpan Threshold", validates warning<=max) → POST (basePath)+'/Admin/SetWorkloadThreshold' → reload, status badges re-evaluate.
- W4 SETUJUI (conditional overload): .approve-btn (confirm) → POST .../ApproveReassignSuggestion → fadeOutCard(#sug-ID).
- W5 LEWATI (conditional overload): .skip-btn → POST .../SkipReassignSuggestion → fadeOutCard(#sug-ID).
</parity_action_map>

<snapshot_restore_protocol>
<!-- SEED_WORKFLOW §5. NEVER mutate the local DB without this wrapper. -->

DB: HcPortalDB_Dev @ localhost\SQLEXPRESS, Integrated Security. Backup dir = resolve at runtime
(SERVERPROPERTY('InstanceDefaultBackupPath')) — do NOT hardcode (A1; C:\Temp blocked by service account).
Prefer the helper `tests/helpers/dbSnapshot.ts` (`backup()`/`restore()`/`queryScalar()`) which the matrix
setup already uses; sqlcmd manual is the fallback.

BACKUP (before any mutation):
  sqlcmd -S "localhost\SQLEXPRESS" -E -C -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='<defaultBackupDir>\HcPortalDB_Dev-pre390.bak' WITH INIT, FORMAT"

RESTORE (after — success OR failure; Kestrel must be down or evicted):
  sqlcmd -S "localhost\SQLEXPRESS" -E -C -Q "USE master; ALTER DATABASE HcPortalDB_Dev SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE HcPortalDB_Dev FROM DISK='<defaultBackupDir>\HcPortalDB_Dev-pre390.bak' WITH REPLACE; ALTER DATABASE HcPortalDB_Dev SET MULTI_USER;"

Baseline check (record before, verify after restore): `queryScalar("SELECT COUNT(*) FROM CoachCoacheeMappings")` must match.
Pitfall 3: RESTORE needs exclusive access → stop Kestrel (or rely on ROLLBACK IMMEDIATE evicting it) before restore.
</snapshot_restore_protocol>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-mutation UAT roundtrip (Playwright MCP, snapshot→restore) — CoachCoacheeMapping C1-C6 + CoachWorkload W1/W3/W4/W5</name>
  <read_first>
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-RESEARCH.md (§Live-Mutation UAT Plan L282-334 — full step sequence + commands; §Common Pitfalls 1/2/3)
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-CONTEXT.md (D-03 snapshot/restore mandatory, D-04 action list)
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-UI-SPEC.md (§B C1-C6 + §D W1/W3/W4/W5 success signals; threshold→color contract for badge re-evaluation)
    - docs/SEED_WORKFLOW.md (§5 BACKUP/RESTORE) + docs/SEED_JOURNAL.md (format: table row Tanggal|Phase|Klasifikasi|Tujuan|Dampak|Snapshot file|Status)
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-01-SUMMARY.md (import NIP pair + spec status from Plan 01)
    - tests/helpers/dbSnapshot.ts (backup/restore/queryScalar helpers)
  </read_first>
  <what-built>
    Plan 01 escalated the non-destructive parity specs + built the import fixture. This checkpoint runs the
    REAL mutation roundtrip (the part Playwright specs deliberately do NOT do, per D-01) via Playwright MCP at
    localhost:5277, wrapped in DB snapshot/restore. The MAIN AGENT drives Playwright MCP (an autonomous
    executor cannot — hence checkpoint). NO production code is written here.
  </what-built>
  <action>
    MAIN AGENT (not an autonomous executor) drives Playwright MCP against localhost:5277. Execute the full
    snapshot → mutate → restore sequence in <how-to-verify> exactly: BACKUP the local DB, run the D-04
    roundtrip (C1-C6 on CoachCoacheeMapping + W1/W3/W4/W5 on CoachWorkload) per <parity_action_map> selectors,
    screenshot each action's success signal, then RESTORE WITH REPLACE and verify COUNT==baseline, logging
    SEED_JOURNAL.md active→cleaned. Honor the D-06 defect boundary (view-only inline fix; backend → STOP+report).
    This is a blocking human/main-agent checkpoint — pause for "approved".
  </action>
  <how-to-verify>
    PRE: `dotnet build` green (Plan 01 gate). Start the app, leave it running:
      `$env:Authentication__UseActiveDirectory="false"; dotnet run`  (localhost:5277)

    1. BASELINE + BACKUP. Resolve backup dir at runtime; record baseline:
       `queryScalar("SELECT COUNT(*) FROM CoachCoacheeMappings")` → BASE_COUNT.
       BACKUP DATABASE HcPortalDB_Dev → ...-pre390.bak (see snapshot_restore_protocol).
       Write a SEED_JOURNAL.md row NOW with Status=active (Phase=390-02, Klasifikasi=temporary + local-only,
       Tujuan=UAT live-mutation parity DSN-06, Dampak=CoachCoacheeMappings/ProtonTrackAssignments/
       WorkloadThreshold/AuditLogs, Snapshot file=the .bak path).

    2. Login admin@pertamina.com / 123456 (Playwright MCP). Go to /Admin/CoachCoacheeMapping.

    3. CoachCoacheeMapping roundtrip (do all on a NEW disposable mapping you create in step a, so restore is clean):
       a. TAMBAH (C1): "Tambah Mapping" → #assignModal → select #assignCoachSelect (any eligible coach) +
          tick one .coachee-checkbox + set #assignStartDate → "Simpan". Assert: no 500, page reload, new
          coachee row appears under that coach's card. Screenshot.
       b. EDIT (C2): open that coach's card → "Edit" on the new row → #editModal (assert #editCoacheeName +
          #editCoachSelect + #editStartDate populated) → change #editStartDate → "Simpan". Assert: value updated. Screenshot.
       c. NONAKTIFKAN (C3): "Nonaktifkan" on the row → #deactivateModal (assert #deactivateSessionInfo not
          "Memuat...") → confirm → assert badge "Non-aktif". Screenshot.
       d. AKTIFKAN-KEMBALI (C6): tick #showAllCheck (Tampilkan Semua) → "Aktifkan" on the inactive row →
          confirm → assert badge "Aktif" (or Swal assign-track prompt handled). Screenshot.
       e. GRADUATED (C4): on an active row, click Graduated (form MarkMappingCompleted) → accept
          confirm('Tandai coachee ini sebagai graduated?') → assert badge "Graduated". Screenshot.
       f. HAPUS (C5): "Hapus" on the disposable row → #deleteModal preview (assert #deleteCoacheeName
          rendered) → "Hapus" → assert row removed (tr[data-mapping-id] gone) + toast. Screenshot.

    4. Go to /Admin/CoachWorkload. CoachWorkload roundtrip:
       g. FILTER (W1): select a section → "Filter" → assert URL contains section= and table filters; "Reset"
          → assert URL clears. Screenshot.
       h. SET THRESHOLD (W3, Admin): "Set Threshold" → #thresholdModal → change #maxCoachees / #warningThreshold
          (keep warning<=max) → "Simpan Threshold" → assert modal closes, reload, status badges re-evaluate
          (OK=bg-success Normal / Warning=bg-warning Mendekati / else=bg-danger Overloaded per UI-SPEC). Screenshot.
       i. SETUJUI/LEWATI (W4/W5, CONDITIONAL): if a `.list-group-item.suggestion-card` exists (coach overload),
          click .approve-btn → confirm → assert fadeOutCard(#sug-ID); and .skip-btn → assert fadeOutCard.
          If NO overload in local DB: either assign many coachees to one coach within the snapshot window to
          force overload then approve/skip, OR mark W4/W5 as conditional-skipped + screenshot the
          "seimbang" empty-state. Record which path was taken (A3 limitation note).

    5. RESTORE. Stop Kestrel. RESTORE DATABASE ... WITH REPLACE (see protocol). Restart app.
       Verify `queryScalar("SELECT COUNT(*) FROM CoachCoacheeMappings")` == BASE_COUNT.
       Update the SEED_JOURNAL.md row → Status=cleaned (note COUNT==baseline verified).

    DEFECT HANDLING (D-06): if any action errors/500/visibly broken AND the fix is view-only
    (Views/Admin/*.cshtml markup / @section Scripts / @section Styles) → fix inline, commit atomic, re-verify.
    If the fix needs Controllers/Services/Helpers/Data/Migrations/Models → STOP, document the finding, report
    to user (do NOT fix — would break 0-backend v32.1).
  </how-to-verify>
  <verify>Manual checkpoint — main agent drives Playwright MCP; success signals asserted per <how-to-verify> step (no autonomous command). Post-restore COUNT==baseline confirmed via queryScalar.</verify>
  <done>C1-C6 + W1/W3 all pass (W4/W5 conditional/documented); screenshots saved; DB restored to baseline; SEED_JOURNAL row Status=cleaned; no backend file touched.</done>
  <acceptance_criteria>
    - SEED_JOURNAL.md has a Phase 390 row that ends Status=cleaned.
    - Post-restore: `SELECT COUNT(*) FROM CoachCoacheeMappings` == BASE_COUNT recorded pre-backup.
    - Screenshots exist for C1-C6 + W1/W3 (+ W4/W5 OR documented conditional-skip).
    - Every action succeeded with no error/500 (or any defect found is documented; view-only defects fixed inline atomic, backend defects reported + STOPPED).
    - No file under Controllers/ Services/ Helpers/ Data/ Migrations/ Models/ was modified (git diff confirms).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the roundtrip passed, DB is restored to baseline, and SEED_JOURNAL is marked cleaned — or describe issues / defects found.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual Excel import UAT (fixture upload, inside snapshot window) — C7</name>
  <read_first>
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-RESEARCH.md (§Excel Verification "Import" L347-364 — fixture schema + manual steps + restore)
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-CONTEXT.md (D-05 import = manual UAT)
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-01-SUMMARY.md (the NIP pair baked into the fixture — what the import should create)
    - docs/SEED_WORKFLOW.md (§5 snapshot/restore)
  </read_first>
  <what-built>
    File upload via Playwright is flaky and needs a fixture + restore, so import is a manual UAT step (D-05).
    The fixture `tests/fixtures/import-mapping-390.xlsx` (built in Plan 01, 2 cols NIP Coach / NIP Coachee
    with existing NIPs) is uploaded through the real Import Excel flow inside a DB snapshot window. This is a
    human-action checkpoint: a person uploads the file via the browser and confirms the result.
  </what-built>
  <action>
    Human uploads `tests/fixtures/import-mapping-390.xlsx` via the real Import Excel flow on
    /Admin/CoachCoacheeMapping, inside a DB snapshot window (BACKUP before, RESTORE after). Confirm the
    import-results card renders a valid (non-error) outcome consistent with the fixture and the affected row
    appears, then restore and mark SEED_JOURNAL cleaned. Follow the numbered steps in <how-to-verify>.
    D-06 defect boundary applies (backend import-validation defect → STOP+report). Blocking — pause for "approved".
  </action>
  <how-to-verify>
    1. BACKUP DATABASE HcPortalDB_Dev → ...-pre390-import.bak (if not already inside the Task 1 snapshot
       window; otherwise reuse). Add/extend a SEED_JOURNAL.md row Status=active for the import.
    2. /Admin/CoachCoacheeMapping → "Import Excel" → #importMappingModal → choose
       `tests/fixtures/import-mapping-390.xlsx` → "Upload & Proses".
    3. Assert: redirect back to CoachCoacheeMapping + the "Hasil Import" card renders with counts
       ("Berhasil Dibuat" >= 1, OR "Diaktifkan Kembali" if the pair already mapped, OR "Dilewati"/"Error"
       consistent with the fixture). Then verify the new/affected mapping row appears in the accordion under
       the coach. Screenshot the import-results card.
    4. RESTORE DATABASE ... WITH REPLACE. Verify CoachCoacheeMappings COUNT back to baseline.
       Mark the SEED_JOURNAL.md import row Status=cleaned.

    DEFECT HANDLING (D-06): same boundary — view-only fix inline; backend (Helpers/ExcelExportHelper.cs,
    controller import validation) → STOP + report.
  </how-to-verify>
  <verify>Manual human-action checkpoint — fixture uploaded via browser; import-results card outcome asserted by inspection; post-restore COUNT==baseline.</verify>
  <done>Import-results card rendered (non-error); affected row visible; DB restored to baseline; SEED_JOURNAL import row Status=cleaned; no backend file touched.</done>
  <acceptance_criteria>
    - Import-results card rendered with a non-error outcome consistent with the fixture (screenshot saved).
    - The affected mapping row visible in the accordion before restore.
    - DB restored: CoachCoacheeMappings COUNT == baseline; SEED_JOURNAL import row Status=cleaned.
    - No backend file modified.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the import produced a valid import-results card, the row appeared, and the DB was restored — or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regression sign-off + milestone v32.1 parity gate (dotnet test + Playwright + 0-backend confirm)</name>
  <read_first>
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-RESEARCH.md (§Regression Suite L424-450 — commands + ship order)
    - .planning/phases/390-test-uat-behavior-parity-dsn-06/390-VALIDATION.md (sampling: phase gate = full suite green + UAT roundtrip + Excel)
    - .planning/ROADMAP.md (§Phase 390 Success Criteria #3/#4 — endpoints/JS-contract unchanged; build+test+playwright green; ready 1 push migration=FALSE)
    - CLAUDE.md (Develop Workflow — verify locally before commit; notify IT is out of scope for execution)
  </read_first>
  <what-built>
    Final gate before the milestone is declared parity-clean. Confirms the backend suite did not regress, the
    escalated Playwright parity specs are green, and the LOCKED constraint (0 backend / 0 controller / 0
    migration across v32.1) holds via grep/diff. After this, the milestone is ready for 1 push → notify IT
    (notify-IT itself is OUT of scope here — closing note only).
  </what-built>
  <action>
    Run the regression gate (dotnet build, dotnet test, escalated Playwright specs --workers=1) and confirm
    the 0-backend / 0-migration constraint via git diff + Migrations/ check + AJAX appUrl grep, per the
    numbered steps in <how-to-verify>. Report results; pause for "approved". After approval the milestone is
    parity-clean and ready for 1 push (migration=FALSE); notify-IT is a closing note, not executed here.
  </action>
  <how-to-verify>
    1. `dotnet build` → 0 errors.
    2. `dotnet test` → all green (backend suite not regressed; CONTEXT discretion = full suite).
    3. App running (`$env:Authentication__UseActiveDirectory="false"; dotnet run`), then:
       `cd tests && npx playwright test coachcoacheemapping-389 coachworkload-388 --workers=1` → exits 0
       (all pass or data-guard skip; 0 failures). --workers=1 MANDATORY.
    4. 0-BACKEND CONFIRM (Success Criterion #3). Across the v32.1 milestone branch, confirm NO controller/
       service/helper/migration file was modified:
       `git diff --name-only <v32.1-base>..HEAD` should show ONLY Views/*.cshtml, tests/*, docs/*, .planning/*
       — NOT Controllers/ Services/ Helpers/ Data/ Migrations/ Models/. Also grep the two views to confirm
       AJAX still uses appUrl()/(window.basePath||'') and NOT hardcoded '/Admin/...' literals in fetch:
       `grep -n "fetch(" Views/Admin/CoachCoacheeMapping.cshtml Views/Admin/CoachWorkload.cshtml` → all
       wrapped in appUrl()/basePath.
    5. Confirm migration=FALSE (no new EF migration file in Migrations/ for v32.1).
  </how-to-verify>
  <verify>Run `dotnet build` (exit 0), `dotnet test` (exit 0), `cd tests && npx playwright test coachcoacheemapping-389 coachworkload-388 --workers=1` (exit 0); git diff name-only against v32.1 base shows no backend tier files; no new Migrations/ file.</verify>
  <done>build+test+playwright green; 0-backend/0-migration confirmed; AJAX appUrl/basePath confirmed; milestone v32.1 parity-clean, ready 1 push (migration=FALSE).</done>
  <acceptance_criteria>
    - `dotnet build` exits 0; `dotnet test` exits 0 (green).
    - `npx playwright test coachcoacheemapping-389 coachworkload-388 --workers=1` exits 0 (0 failures).
    - git diff <v32.1-base>..HEAD touches NO file under Controllers/ Services/ Helpers/ Data/ Migrations/ Models/.
    - No new file in Migrations/ (migration=FALSE).
    - Both views' AJAX uses appUrl()/(window.basePath||''), not hardcoded /Admin/ in fetch.
  </acceptance_criteria>
  <resume-signal>Type "approved" once build+test+playwright are green and the 0-backend/0-migration constraint is confirmed — milestone v32.1 is parity-clean and ready for 1 push (notify IT, migration=FALSE). Or describe regressions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (admin) → /Admin/* endpoints | EXISTING boundary, unchanged. UAT exercises it but adds no new input/endpoint. Re-verifies CSRF token + role-gate still hold post-redesign. |
| local file upload → /Admin/ImportCoachCoacheeMapping | EXISTING boundary. Manual import uses a local disposable fixture; controller validation (header / ext / size) is UNCHANGED and re-verified, not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-390-03 | Tampering | local DB mutation during UAT | mitigate | All mutations wrapped BACKUP → mutate → RESTORE WITH REPLACE (SEED_WORKFLOW §5); SEED_JOURNAL logged + marked cleaned; post-restore COUNT == baseline. No permanent seed. Dev/Prod never touched (local-only; dbSnapshot rejects non-localhost). |
| T-390-04 | Elevation | Set Threshold / approve / skip (Admin-only) | accept | Existing control: role-gate UI (`User.IsInRole("Admin")`) + controller `[Authorize(Roles="Admin")]`. UAT runs as admin and RE-VERIFIES the gate (button visible for admin); no control change. |
| T-390-05 | (verification-only) | parity verification | n/a | No new inputs/endpoints/auth introduced. Preserved controls re-verified: CSRF token on AJAX POST, role-gate, PathBase-aware URLs. ASVS L1 / block-on-high: no new high-severity items. |

**Statement:** v32.1 (this phase) adds no new attack surface. Live UAT mutates only the local DB inside a snapshot/restore wrapper. The phase RE-VERIFIES existing security controls remain intact after the redesign; it does not add or alter any.
</threat_model>

<verification>
- Live roundtrip C1-C6 + W1/W3 all succeed (W4/W5 conditional on overload, documented); screenshots saved.
- Manual import produced a valid import-results card; affected row appeared.
- All DB mutations restored: CoachCoacheeMappings COUNT == baseline; SEED_JOURNAL.md entry marked cleaned.
- `dotnet build` + `dotnet test` green; escalated Playwright specs green (--workers=1, 0 failures).
- 0-backend / 0-migration confirmed via git diff + Migrations/ check; AJAX appUrl/basePath confirmed.
</verification>

<success_criteria>
- DSN-06 fully verified: every existing action on both surfaces works post-redesign (Playwright parity from Plan 01 + live mutation roundtrip + manual import here).
- No regression in backend suite; no endpoint/JS-contract change beyond render needs.
- 0 backend / 0 controller / 0 migration confirmed (constraint LOCKED, honored end-to-end).
- Milestone v32.1 parity-clean and ready for 1 push → notify IT (migration=FALSE). [notify-IT = closing note, out of execution scope.]
</success_criteria>

<output>
After completion, create `.planning/phases/390-test-uat-behavior-parity-dsn-06/390-02-SUMMARY.md`.
Record: the roundtrip pass/fail per action (C1-C6, C7 import, W1/W3/W4/W5), W4/W5 conditional path taken,
the snapshot file path + baseline COUNT + restore confirmation, the SEED_JOURNAL entry status, the final
`dotnet test` + Playwright results, the 0-backend/0-migration git-diff confirmation, and any defects found
(view-only fixed inline vs backend deferred per D-06). Note milestone v32.1 is ready for 1 push (migration=FALSE).
</output>
