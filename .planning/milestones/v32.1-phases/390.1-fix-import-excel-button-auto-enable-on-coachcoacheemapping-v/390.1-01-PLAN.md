---
phase: 390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/CoachCoacheeMapping.cshtml
  - tests/e2e/coachcoacheemapping-389.spec.ts
autonomous: false
requirements: [DSN-07]

must_haves:
  truths:
    - "Saat admin memilih file di modal Import Excel, tombol Upload & Proses (#btnImportMapping) menjadi enabled"
    - "Saat admin menghapus pilihan file (deselect), tombol Upload & Proses menjadi disabled lagi (parity D-02)"
    - "Spec coachcoacheemapping-389.spec.ts existing (V-01..V-17) tetap green/skip tanpa failure (no regression)"
    - "dotnet build 0 error (migration=FALSE: 0 backend, 0 controller, 0 endpoint, 0 migration)"
  artifacts:
    - path: "Views/Admin/CoachCoacheeMapping.cshtml"
      provides: "Listener change file-input dibungkus DOMContentLoaded (binding nempel pasca-parse)"
      contains: "DOMContentLoaded"
    - path: "tests/e2e/coachcoacheemapping-389.spec.ts"
      provides: "Test case DSN-07 enable-on-file-select (+ deselect→disable)"
      contains: "btnImportMapping"
  key_links:
    - from: "Views/Admin/CoachCoacheeMapping.cshtml (DOMContentLoaded callback)"
      to: "#importMappingFile change listener"
      via: "binding ditunda sampai DOM siap → getElementById tak lagi null"
      pattern: "DOMContentLoaded[\\s\\S]*getElementById\\('importMappingFile'\\)"
    - from: "tests/e2e/coachcoacheemapping-389.spec.ts (test DSN-07)"
      to: "#btnImportMapping toBeEnabled"
      via: "klik Import Excel → setInputFiles fixture → assert enabled"
      pattern: "setInputFiles[\\s\\S]*btnImportMapping"
---

<objective>
Perbaiki defect view-only: tombol "Upload & Proses" (`#btnImportMapping`) di modal Import Excel pada `/Admin/CoachCoacheeMapping` tidak pernah enable saat file dipilih, sehingga import via UI tak bisa dipakai. Akar masalah (D-03, verified): listener `change` pada `#importMappingFile` didaftarkan di top-level inline `<script>` (baris 1038–1040) saat parse-time, TAPI elemen `#importMappingFile` baru muncul di baris 1059 (di dalam modal yang ditempatkan SETELAH `</script>` baris 1041). Saat script jalan, elemen belum ter-parse → `?.` no-op → listener tak nempel.

Fix LOCKED (D-01/D-02): bungkus HANYA baris 1038–1040 dalam `document.addEventListener('DOMContentLoaded', ...)`, handler body IDENTIK. BUKAN event delegation (D-01: input statis server-rendered, delegation overkill). Lalu tambah satu test case DSN-07 di akhir spec 389 existing (D-04: extend, BUKAN file baru) yang membuktikan tombol enable saat file dipilih + disable saat di-deselect.

Purpose: Mengembalikan fungsi import Excel via UI (defect parity DSN-07) yang sebelumnya mati senyap.
Output: 1 view diedit (3 baris dibungkus DOMContentLoaded) + 1 test case baru di spec 389 + verifikasi build & Playwright hijau.

Migration: **FALSE** — 0 backend, 0 controller, 0 endpoint, 0 JS-contract, 0 migration. Murni timing-binding view + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-CONTEXT.md
@.planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-RESEARCH.md
@Views/Admin/CoachCoacheeMapping.cshtml
@tests/e2e/coachcoacheemapping-389.spec.ts

<interfaces>
<!-- Kontrak yang dipakai executor. Diekstrak dari codebase — TIDAK perlu eksplorasi tambahan. -->

KONDISI AKTUAL `Views/Admin/CoachCoacheeMapping.cshtml` (verified byte-exact):

Baris 56–67 (toolbar header — tombol pembuka modal Import Excel, Bootstrap deklaratif, JANGAN diubah):
```html
<div class="btn-group">
    <a href="@Url.Action("DownloadMappingImportTemplate", "CoachMapping")" class="btn btn-sm btn-outline-secondary"> ... Download Template</a>
    <button class="btn btn-sm btn-outline-secondary" data-bs-toggle="modal" data-bs-target="#importMappingModal">
        <i class="bi bi-file-earmark-arrow-up me-1"></i>Import Excel
    </button>
    <a asp-controller="CoachMapping" asp-action="CoachCoacheeMappingExport" class="btn btn-sm btn-outline-secondary"> ... Export Excel</a>
</div>
```

Baris 1038–1040 (TARGET EDIT — satu-satunya top-level DOM access saat parse-time; akar bug):
```js
    document.getElementById('importMappingFile')?.addEventListener('change', function() {
        document.getElementById('btnImportMapping').disabled = !this.files.length;
    });
```
Baris 1041: `</script>`  (penutup inline script `@section Scripts`)

Baris 1059 (input target — di dalam modal SETELAH `</script>`, JANGAN diubah):
```html
<input type="file" name="excelFile" class="form-control" accept=".xlsx,.xls" required id="importMappingFile" />
```
Baris 1064 (tombol submit target — atribut `disabled` hard-coded di markup, di-toggle oleh listener):
```html
<button type="submit" class="btn btn-primary" id="btnImportMapping" disabled>
```

KONFIRMASI (verified, grep): TIDAK ada `DOMContentLoaded` lain di file → blok baru tak akan duplikat/bentrok. Selain 1038–1040, tak ada top-level `getElementById` saat parse-time lain (sisa isi `<script>` adalah function declarations + `var` data) → cukup bungkus 1038–1040 saja.

KONTRAK `tests/e2e/coachcoacheemapping-389.spec.ts` (verified):
- `import { test, expect, Page } from '@playwright/test';`
- helper lokal `loginAny(page, accountKey)` (login `admin@pertamina.com` via `/Account/Login`).
- `beforeEach`: `loginAny(page,'admin')` → `page.goto('/Admin/CoachCoacheeMapping')` (BARE PATH relatif baseURL `http://localhost:5277`) → assert `h2` "Coach-Coachee Mapping" visible.
- Semua test di dalam SATU `test.describe('Phase 389 — CoachCoacheeMapping accordion parity (DSN-01/02/03)', () => { ... })`. Test terakhir saat ini V-17 (baris 331–346), penutup describe `});` di baris 348.
- Pola buka modal (V-09): `page.locator('button:has-text("Tambah Mapping")').click()` → `expect(page.locator('#assignModal')).toBeVisible()`. Pola sama untuk Import: klik `button:has-text("Import Excel")` → `#importMappingModal`.
- Belum ada `import path`/`__dirname` di spec — perlu ditambah `import path from 'path';` (atau gunakan path relatif cwd `tests/`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bungkus listener change file-input dalam DOMContentLoaded (DSN-07 fix)</name>
  <files>Views/Admin/CoachCoacheeMapping.cshtml</files>
  <read_first>
    - Views/Admin/CoachCoacheeMapping.cshtml (terutama baris 1036–1072 — listener target + struktur penutup script/modal)
    - .planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-RESEARCH.md (bagian "Exact Code Shape" + "Recommended edit")
    - .planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-CONTEXT.md (D-01/D-02/D-03)
  </read_first>
  <action>
    Edit `Views/Admin/CoachCoacheeMapping.cshtml`. Ganti TEPAT 3 baris listener top-level (saat ini baris 1038–1040, di dalam inline `<script>` sebelum `</script>` baris 1041) dengan versi yang dibungkus `DOMContentLoaded`.

    BEFORE (3 baris yang ada sekarang):
    ```js
    document.getElementById('importMappingFile')?.addEventListener('change', function() {
        document.getElementById('btnImportMapping').disabled = !this.files.length;
    });
    ```

    AFTER (final code — gunakan PERSIS ini; handler body IDENTIK per D-02, `?.` dipertahankan defensif per RESEARCH):
    ```js
    document.addEventListener('DOMContentLoaded', function () {
        document.getElementById('importMappingFile')?.addEventListener('change', function() {
            document.getElementById('btnImportMapping').disabled = !this.files.length;
        });
    });
    ```

    Batasan ketat:
    - Edit HANYA blok 1038–1040 (bungkus dengan `DOMContentLoaded`). JANGAN sentuh kode lain (function declarations, var, modal markup, tombol baris 61/1064, input baris 1059, form `asp-action` baris 1051). [D-03: hanya 1 instance anti-pattern, no sibling]
    - JANGAN ubah handler body (`disabled = !this.files.length;`) — parity disable-on-deselect bergantung pada ini (D-02).
    - JANGAN pakai event delegation di `document` (D-01 explicitly REJECTED — input statis server-rendered).
    - Blok `DOMContentLoaded` HARUS berada di DALAM `<script>` yang ada (sebelum `</script>` baris 1041), BUKAN di markup modal. [Pitfall 1 RESEARCH]
    - 0 backend / 0 controller / 0 migration — file tunggal ini saja.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/CoachCoacheeMapping.cshtml` contains "DOMContentLoaded" (grep cocok 1 instance).
    - `Views/Admin/CoachCoacheeMapping.cshtml` masih contains `document.getElementById('btnImportMapping').disabled = !this.files.length;` (handler body utuh — parity D-02).
    - Listener `getElementById('importMappingFile')?.addEventListener('change'` kini berada DI DALAM callback `DOMContentLoaded` (bukan top-level lagi) — verifikasi grep multiline: `DOMContentLoaded[\s\S]*getElementById\('importMappingFile'\)`.
    - `</script>` (penutup `@section Scripts` inline) tetap ada setelah blok DOMContentLoaded; markup modal (`id="importMappingModal"`, `#importMappingFile`, `#btnImportMapping disabled`) TIDAK berubah.
    - `dotnet build` exits 0 (0 error).
  </acceptance_criteria>
  <done>
    Listener `change` file-input dibungkus `DOMContentLoaded`; handler body identik; tak ada perubahan lain di file; `dotnet build` hijau.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tambah test case DSN-07 (enable-on-select + deselect→disable) di spec 389</name>
  <files>tests/e2e/coachcoacheemapping-389.spec.ts</files>
  <read_first>
    - tests/e2e/coachcoacheemapping-389.spec.ts (full — terutama import block baris 21–22, beforeEach baris 37–41, pola buka modal V-09 baris 187–194, penutup describe baris 348)
    - .planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-RESEARCH.md (bagian "Test Verification Detail" + "Assertion inti (DSN-07)")
    - .planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-CONTEXT.md (D-04/D-05)
  </read_first>
  <action>
    Extend (BUKAN buat file baru — D-04) `tests/e2e/coachcoacheemapping-389.spec.ts`:

    1. Di blok import atas file, tambahkan import `path` (Node built-in) bila belum ada:
       ```ts
       import path from 'path';
       ```
       (taruh setelah baris `import { accounts, AccountKey } from '../helpers/accounts';`)

    2. Tambahkan SATU test case baru SEBAGAI test terakhir di dalam `test.describe(...)` existing (sebelum penutup `});` di akhir file). Test ini NON-DESTRUCTIVE — JANGAN klik submit `#btnImportMapping` (no DB write, selaras CLAUDE.md Seed Workflow). Gunakan persis pola berikut:

       ```ts
       // V-18 (DSN-07): Import Excel — tombol "Upload & Proses" enable saat file dipilih,
       // disable lagi saat di-deselect (parity D-02). Bukti listener change nempel pasca
       // fix DOMContentLoaded. Tak butuh data DB (modal & input statis) -> selalu jalan.
       // NON-DESTRUCTIVE: tidak menekan submit (#btnImportMapping) -> tak ada import nyata.
       test('V-18 import button enable on file select', async ({ page }) => {
         // buka modal via tombol Bootstrap data-bs-toggle (pola V-09)
         await page.locator('button:has-text("Import Excel")').click();
         await expect(page.locator('#importMappingModal')).toBeVisible();

         // baseline: tombol submit disabled (atribut markup)
         await expect(page.locator('#btnImportMapping')).toBeDisabled();

         // pilih fixture xlsx -> event change -> listener set disabled=false
         const fixture = path.join(__dirname, '../fixtures/import-mapping-390.xlsx');
         await page.locator('#importMappingFile').setInputFiles(fixture);
         await expect(page.locator('#btnImportMapping')).toBeEnabled();

         // deselect (parity D-02): kosongkan file -> tombol disable lagi
         await page.locator('#importMappingFile').setInputFiles([]);
         await expect(page.locator('#btnImportMapping')).toBeDisabled();
       });
       ```

    Batasan:
    - JANGAN ubah test existing V-01..V-17 (D-05: harus tetap green/skip).
    - JANGAN klik / submit `#btnImportMapping` (Pitfall 3 RESEARCH — destruktif ke DB lokal).
    - Path fixture pakai `path.join(__dirname, '../fixtures/import-mapping-390.xlsx')` (absolut, robust terhadap cwd — Open Question #1 RESEARCH).
    - Test wajib berada DI DALAM `test.describe(...)` existing (gunakan `test`, `expect` yang sudah di-import).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff\tests" && npx playwright test coachcoacheemapping-389 --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/coachcoacheemapping-389.spec.ts` contains "btnImportMapping".
    - `tests/e2e/coachcoacheemapping-389.spec.ts` contains "V-18 import button enable on file select" (test case baru terdaftar).
    - File contains `setInputFiles` dan `import-mapping-390.xlsx` (interaksi upload fixture).
    - File contains `import path from 'path';`.
    - File TIDAK menekan submit: tidak ada `'#btnImportMapping').click(` (NON-DESTRUCTIVE).
    - `npx playwright test coachcoacheemapping-389 --list` parse OK dan menampilkan 18 test (V-01..V-18) tanpa error parse.
  </acceptance_criteria>
  <done>
    Spec 389 ber-test-case DSN-07 baru (V-18) yang assert enable-on-select + disable-on-deselect, non-destruktif, parse-able via `--list`; test existing utuh.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi runtime — dotnet run + Playwright spec 389 full green (DSN-07)</name>
  <files>Views/Admin/CoachCoacheeMapping.cshtml, tests/e2e/coachcoacheemapping-389.spec.ts</files>
  <read_first>
    - .planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-RESEARCH.md (bagian "Test Verification Detail" — run instruction --workers=1, no webServer)
    - tests/playwright.config.ts (baseURL, fullyParallel:false, no webServer)
  </read_first>
  <action>
    Verifikasi runtime (langkah lengkap di how-to-verify di bawah). Ringkas: (1) `dotnet build` 0 error;
    (2) jalankan app lokal di terminal terpisah dengan `Authentication__UseActiveDirectory=false` pada
    `http://localhost:5277` (app TIDAK auto-start — config tak punya webServer); (3) `npx playwright test
    coachcoacheemapping-389 --workers=1` dari folder `tests/`; konfirmasi V-18 (DSN-07) PASS dan V-01..V-17
    green/skip tanpa failure (0 failed). Phase 354 lesson: binding Razor dinamis hanya terbukti di runtime
    browser — build+grep tak cukup. PAUSE untuk human verify hasil; JANGAN klik submit "Upload & Proses"
    (non-destruktif, tak kotori DB lokal).
  </action>
  <what-built>
    - View `CoachCoacheeMapping.cshtml`: listener `change` `#importMappingFile` dibungkus `DOMContentLoaded` (binding kini nempel pasca-parse).
    - Spec `coachcoacheemapping-389.spec.ts`: test V-18 DSN-07 (enable-on-select + deselect->disable, non-destruktif).
    - `dotnet build` sudah hijau (Task 1) dan `--list` parse OK (Task 2).

    Lesson Phase 354 (WAJIB): binding event di Razor dinamis hanya TERBUKTI di runtime browser — grep + build TIDAK cukup. Karena itu verifikasi final = Playwright runtime di localhost.
  </what-built>
  <how-to-verify>
    1. Build gate (otomatis, harus 0 error):
       ```
       cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff"
       dotnet build
       ```
       Harapan: `Build succeeded. 0 Error(s)`.

    2. Jalankan app lokal (terminal terpisah; app TIDAK auto-start — playwright.config.ts tak punya webServer). Login AD WAJIB dimatikan:
       ```
       cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff"
       $env:Authentication__UseActiveDirectory="false"; dotnet run
       ```
       Tunggu sampai listening di `http://localhost:5277`.

    3. Jalankan Playwright spec 389 (WAJIB `--workers=1` — NTLM loopback / shared-memory SQL conn):
       ```
       cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff\tests"
       npx playwright test coachcoacheemapping-389 --workers=1
       ```
       Harapan:
       - **V-18 (DSN-07) PASS** — tak butuh data DB (modal+input statis), jadi TIDAK skip; harus benar-benar pass (tombol enable saat file dipilih, disable saat deselect).
       - **V-01..V-17 tetap green/skip TANPA failure** (no regression — D-05). Sebagian V-skip karena data-guard DB lokal; itu WAJAR (bukan failure).
       - Summary akhir: `X passed (Y skipped)`, `0 failed`.

    4. (Opsional manual cross-check) Buka `http://localhost:5277/Admin/CoachCoacheeMapping` → klik "Import Excel" → pilih file `.xlsx` → konfirmasi tombol "Upload & Proses" berubah enabled; hapus pilihan → kembali disabled. JANGAN klik "Upload & Proses" (hindari import nyata ke DB lokal).
  </how-to-verify>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff\tests" && echo "MANUAL CHECKPOINT: dotnet run dengan Authentication__UseActiveDirectory=false, lalu npx playwright test coachcoacheemapping-389 --workers=1 — V-18 PASS dan 0 failed"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - `npx playwright test coachcoacheemapping-389 --workers=1` exits 0; output menunjukkan **V-18 passed** dan **0 failed** (V-01..V-17 pass atau skip, tak ada failure).
  </acceptance_criteria>
  <done>
    V-18 (DSN-07) PASS di runtime localhost:5277; spec 389 0 failed (no regression); tombol Import enable-on-select + disable-on-deselect terbukti. Human approve.
  </done>
  <resume-signal>Ketik "approved" bila V-18 PASS + 0 failed (spec 389 hijau), atau jelaskan kegagalan/skip yang tak terduga.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server (import POST) | EXISTING, di luar scope. Form `ImportCoachCoacheeMapping` (baris 1051) menerima upload Excel. TIDAK disentuh fase ini. |

## STRIDE Threat Register

Perubahan fase ini = **pure client-side event-binding timing fix** (3 baris JS dibungkus `DOMContentLoaded`). TIDAK menambah endpoint baru, TIDAK menambah data flow baru, TIDAK menambah input handling baru, TIDAK mengubah authz surface. Tombol submit hanya di-enable/disable di UI; validasi & otorisasi tetap 100% di server (tak berubah). Maka **tidak ada attack surface baru** yang diperkenalkan.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-390.1-01 | Tampering (client) | Tombol `#btnImportMapping` di-enable via JS | accept | Enable/disable hanya UI affordance; submit nyata divalidasi server-side. Penyerang sudah bisa enable tombol via devtools sebelum & sesudah fix → fix ini tak mengubah surface. Kontrol asli (anti-forgery token `@Html.AntiForgeryToken()` baris 1052, validasi Excel server-side, otorisasi Admin/HC di `CoachMapping.ImportCoachCoacheeMapping`) tetap berlaku & di luar scope edit view ini. |
| T-390.1-02 | Elevation / Spoofing | Endpoint import POST `ImportCoachCoacheeMapping` | accept | UNCHANGED oleh fase ini. Anti-forgery + authz controller existing tetap menjaga; tak ada baris controller/endpoint ter-modifikasi (migration=FALSE, 0 backend). Out of scope view-only edit. |

**Block-on severity:** high (none expected — no new surface).
**Verdict:** No new attack surface introduced; existing controls (anti-forgery token on import form, server-side Excel validation, Admin/HC authorization on the controller) remain in force and are out of scope for this view-only edit.
</threat_model>

<verification>
1. `dotnet build` 0 error (Task 1 gate + Task 3 re-confirm) — migration=FALSE, tak ada perubahan backend/controller/schema.
2. Grep parity:
   - `Views/Admin/CoachCoacheeMapping.cshtml` mengandung `DOMContentLoaded` + handler body `disabled = !this.files.length;` utuh.
   - `tests/e2e/coachcoacheemapping-389.spec.ts` mengandung V-18 + `setInputFiles` + `import-mapping-390.xlsx`; tak ada `'#btnImportMapping').click(`.
3. `npx playwright test coachcoacheemapping-389 --list` → 18 test terdaftar (parse OK).
4. Runtime (Phase 354 lesson — WAJIB): `dotnet run` (`Authentication__UseActiveDirectory=false`, localhost:5277) + `npx playwright test coachcoacheemapping-389 --workers=1` → **V-18 PASS**, V-01..V-17 green/skip, **0 failed**.
5. 0 controller/service/endpoint/migration ter-modifikasi (hanya 2 file di `files_modified`).
</verification>

<success_criteria>
- [ ] `#btnImportMapping` enable saat file dipilih di modal Import Excel (DSN-07) — terbukti V-18 PASS runtime.
- [ ] `#btnImportMapping` disable lagi saat file di-deselect (parity D-02) — terbukti V-18 PASS runtime.
- [ ] Spec 389 existing (V-01..V-17) tetap green/skip tanpa failure (no regression — D-05).
- [ ] `dotnet build` 0 error; 0 backend / 0 controller / 0 endpoint / 0 migration (migration=FALSE LOCKED).
- [ ] Fix = `DOMContentLoaded` wrapper (D-01), handler body identik (D-02), file tunggal `CoachCoacheeMapping.cshtml` + spec 389 di-extend (D-04).
</success_criteria>

<output>
After completion, create `.planning/phases/390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v/390.1-01-SUMMARY.md`
</output>
