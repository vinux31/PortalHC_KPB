---
phase: 390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v
plan: 01
status: complete
requirements: [DSN-07]
migration: false
---

# Phase 390.1 Plan 01 — Summary

**Goal:** Fix defect view-only — tombol "Upload & Proses" (`#btnImportMapping`) di modal Import Excel `/Admin/CoachCoacheeMapping` tak pernah enable saat file dipilih → import via UI tak bisa dipakai.

## What was built

- **Fix view (`Views/Admin/CoachCoacheeMapping.cshtml`)** — listener `change` pada `#importMappingFile` (baris 1038–1040, top-level inline `<script>`) dibungkus `document.addEventListener('DOMContentLoaded', …)`. Akar bug: listener register saat parse-time sebelum input (baris 1059, di modal setelah `</script>`) ada → `?.` no-op → listener tak nempel. Handler body IDENTIK (`disabled = !this.files.length;`) → parity disable-on-deselect terjaga. Bukan event delegation (D-01 ditolak: input statis server-rendered).
- **Test (`tests/e2e/coachcoacheemapping-389.spec.ts`)** — extend spec 389 existing (D-04, bukan file baru): test **V-18** buka modal Import Excel → assert `#btnImportMapping` disabled → `setInputFiles` fixture `import-mapping-390.xlsx` → `toBeEnabled` → deselect → `toBeDisabled`. NON-DESTRUCTIVE (tak klik submit, tak ada import nyata ke DB). Tambah `import path from 'path'`.

## Verification (runtime, localhost:5277, AD off, `--workers=1`)

- `dotnet build` → **0 Error** (25 warning pre-existing).
- `npx playwright test coachcoacheemapping-389 --workers=1` → **V-18 PASS** (1.9s). Total: **15 passed, 4 skipped (V-05/V-11/V-12/V-13 = data-guard DB lokal, WAJAR), 0 failed**. No regression V-01..V-17.
- Seed matrix auto backup→seed→restore→cleaned oleh global.setup/teardown (DB lokal bersih pasca-test, SEED_JOURNAL status=cleaned).

## Key files

- created: `tests/e2e/coachcoacheemapping-389.spec.ts` (V-18 test case) — *modified, file existing*
- modified: `Views/Admin/CoachCoacheeMapping.cshtml` (DOMContentLoaded wrapper)

## Commits

- `0f21efff` fix(390.1): wrap import-file change listener in DOMContentLoaded (DSN-07)
- `c7ebfe26` test(390.1): add V-18 import-button enable-on-select spec (DSN-07)

## Deviations

None. Locked decisions D-01..D-05 dipatuhi. migration=FALSE (0 backend/controller/endpoint/migration). 2 file di `files_modified` only.

## Self-Check: PASSED
