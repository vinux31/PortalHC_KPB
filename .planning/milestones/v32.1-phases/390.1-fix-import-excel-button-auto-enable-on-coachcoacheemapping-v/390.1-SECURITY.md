---
phase: 390.1
slug: fix-import-excel-button-auto-enable-on-coachcoacheemapping-v
status: verified
threats_open: 0
asvs_level: 1
created: 2026-06-17
---

# Phase 390.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Scope: view-only defect fix (DSN-07) — wrap `#importMappingFile` change-listener in
> `DOMContentLoaded`. migration=FALSE, 0 backend/controller/endpoint changed.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| browser → server (import POST) | EXISTING, out of scope. Form `ImportCoachCoacheeMapping` (`CoachCoacheeMapping.cshtml:1053`) uploads Excel mapping file. NOT touched by this phase. | Excel file (`excelFile`), anti-forgery token |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-390.1-01 | Tampering (client) | `#btnImportMapping` enabled via JS | accept | Enable/disable is UI affordance only; real submit validated server-side. Attacker could already enable via devtools before AND after the fix → no surface change. Existing controls confirmed present: `@Html.AntiForgeryToken()` (`CoachCoacheeMapping.cshtml:1054`), `[ValidateAntiForgeryToken]` (`CoachMappingController.cs:205`), `[Authorize(Roles = "Admin, HC")]` (`CoachMappingController.cs:204`). | closed |
| T-390.1-02 | Elevation / Spoofing | import POST endpoint `ImportCoachCoacheeMapping` | accept | UNCHANGED by this phase. POST action (`CoachMappingController.cs:203-206`) carries `[Authorize(Roles = "Admin, HC")]` + `[ValidateAntiForgeryToken]`. Git diff of commits `0f21efff` + `c7ebfe26` confirms only 2 files modified (`CoachCoacheeMapping.cshtml`, `coachcoacheemapping-389.spec.ts`); controller untouched. | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-390.1-01 | T-390.1-01 | Client-side enable/disable of `#btnImportMapping` is a UI affordance, not a security control. Server enforces anti-forgery + Admin/HC authz on the import endpoint regardless of button state. The DOMContentLoaded fix changes binding timing only — zero new attack surface. | Rino | 2026-06-17 |
| AR-390.1-02 | T-390.1-02 | Import endpoint and its controls (`[ValidateAntiForgeryToken]` + `[Authorize(Roles="Admin, HC")]`) are pre-existing and UNCHANGED (0 backend in this phase). | Rino | 2026-06-17 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-06-17 | 2 | 2 | 0 | gsd-security-auditor (sonnet) — controls verified present in code |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-06-17
