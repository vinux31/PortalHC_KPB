---
phase: 393-backend-core-inject
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/InjectAssessmentServiceTests.cs
  - Models/InjectAssessmentDtos.cs
  - Services/InjectAssessmentService.cs
  - Program.cs
autonomous: true
requirements: [INJ-01, INJ-02]
tags: [assessment, grading, inject, backend, service, dto]

must_haves:
  truths:
    - "DTO kontrak InjectRequest/InjectResult/InjectWorkerSpec/InjectQuestionSpec/InjectOptionSpec/InjectAnswerSpec ada & ter-compile (interface yang dipakai service + test)"
    - "InjectAssessmentService stub ada dengan ctor DI (ApplicationDbContext, GradingService, ILogger) + method InjectBatchAsync(InjectRequest req, string actorUserId, string actorName) yang ter-compile"
    - "Test class InjectAssessmentServiceTests + fixture disposable real-SQL (IAsyncLifetime, [Trait Category=Integration]) ada dengan 5 stub fact (SC1..SC5) + seed helper authored-package + NewInjectService factory"
    - "InjectAssessmentService ter-register di Program.cs (AddScoped) — dotnet build 0 error"
  artifacts:
    - path: "Models/InjectAssessmentDtos.cs"
      provides: "DTO kontrak request/result service inject (POCO, tidak ter-attach DbContext)"
      contains: "class InjectRequest"
    - path: "Services/InjectAssessmentService.cs"
      provides: "Service skeleton + ctor DI + signature InjectBatchAsync"
      contains: "public async Task<InjectResult> InjectBatchAsync"
    - path: "HcPortal.Tests/InjectAssessmentServiceTests.cs"
      provides: "xUnit Integration class + disposable SQL fixture + 5 stub fact + seed helper"
      contains: "[Trait(\"Category\", \"Integration\")]"
    - path: "Program.cs"
      provides: "DI registration service inject"
      contains: "AddScoped<HcPortal.Services.InjectAssessmentService>"
  key_links:
    - from: "HcPortal.Tests/InjectAssessmentServiceTests.cs"
      to: "Services/InjectAssessmentService.cs"
      via: "NewInjectService factory instantiate dengan real GradingService"
      pattern: "new InjectAssessmentService"
    - from: "Services/InjectAssessmentService.cs"
      to: "Models/InjectAssessmentDtos.cs"
      via: "signature InjectBatchAsync menerima InjectRequest mengembalikan InjectResult"
      pattern: "InjectBatchAsync\\(InjectRequest"
---

<objective>
Letakkan fondasi kontrak (interface-first) untuk Phase 393: definisikan DTO request/result service inject, buat skeleton `InjectAssessmentService` dengan ctor DI + signature method, daftarkan DI di `Program.cs`, dan buat kelas test xUnit Integration + fixture disposable real-SQL + 5 stub fact (SC1..SC5) + seed helper authored-package. Plan ini SENGAJA tidak mengisi logic implementasi (Plan 02) maupun assertion test (Plan 03) — ia menetapkan KONTRAK yang dikonsumsi keduanya.

Purpose: Hindari "scavenger hunt" — Plan 02 (implementasi) & Plan 03 (test) menerima kontrak DTO + skeleton + fixture langsung dari plan ini, tanpa eksplorasi codebase. Wave 0 test-infra ini juga jadi syarat Nyquist (VALIDATION.md).
Output: 3 file baru (`Models/InjectAssessmentDtos.cs`, `Services/InjectAssessmentService.cs` [stub], `HcPortal.Tests/InjectAssessmentServiceTests.cs` [stub]) + 1 baris DI di `Program.cs`. `dotnet build` 0 error; `dotnet test --filter Category!=Integration` tetap 347/347 (stub Integration ter-exclude).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/393-backend-core-inject/393-CONTEXT.md
@.planning/phases/393-backend-core-inject/393-RESEARCH.md
@.planning/phases/393-backend-core-inject/393-PATTERNS.md
@.planning/phases/393-backend-core-inject/393-VALIDATION.md

<interfaces>
<!-- Kontrak yang diverifikasi langsung dari codebase 2026-06-17. Executor pakai ini langsung — TIDAK perlu eksplorasi ulang. -->

Method delegasi yang dipanggil service (Plan 02) — Services/GradingService.cs:57
```csharp
public async Task<bool> GradeAndCompleteAsync(AssessmentSession session)
// baca DB (UserPackageAssignments + PackageUserResponses by session.Id), NON-transaksional,
// set Status/Score/IsPassed, insert SessionElemenTeknisScore, generate cert (gate isPassed).
// WAJIB: insert session+assignment+responses + SaveChanges DULU sebelum panggil ini.
```

GradingService ctor (untuk NewGradingService factory di test) — Services/GradingService.cs:25
```csharp
public GradingService(ApplicationDbContext context, IWorkerDataService workerDataService,
    ILogger<GradingService> logger, ProtonCompletionService protonCompletionService,
    ProtonBypassService protonBypassService)
```

Pure aggregator (dipakai Plan 02 finalize-block + Plan 03 expected-value) — Helpers/AssessmentScoreAggregator.cs:26
```csharp
public static ScoreAggregateResult Compute(IEnumerable<PackageQuestion> questions,
    IEnumerable<PackageUserResponse> responses, int passPercentage)
public readonly record struct ScoreAggregateResult(int TotalScore, int MaxScore, int Percentage, bool IsPassed);
// MC: opt.IsCorrect → +ScoreValue. MA: SetEquals(selected, correct) → +ScoreValue (all-or-nothing).
// Essay: + EssayScore.Value (0..ScoreValue). Percentage = maxScore>0 ? (int)((double)total/max*100) : 0.
```

Cert helper (Plan 02 cert + dedup collision detect) — Helpers/CertNumberHelper.cs
```csharp
public static string Build(int seq, DateTime date) => $"KPB/{seq:D3}/{ToRomanMonth(date.Month)}/{date.Year}";
public static async Task<int> GetNextSeqAsync(ApplicationDbContext context, int year);   // MAX+1 dari NomorSertifikat EndsWith "/{year}"
public static bool IsDuplicateKeyException(DbUpdateException ex);   // substring "IX_AssessmentSessions_NomorSertifikat" / 2601 / 2627
```

Dedup normalizer (Plan 02 [B]) — Controllers/AdminBaseController.cs:271 (static, panggil langsung)
```csharp
public static string NormalizeTitleForDup(string? s)
    => System.Text.RegularExpressions.Regex.Replace((s ?? "").Trim(), @"\s+", " ").ToLowerInvariant();
```

Entity fields KUNCI (diverifikasi dari Models/):
```csharp
// AssessmentSession.cs — UserId(string), Title(string), Category(string), Schedule(DateTime),
//   DurationMinutes(int), Status(string), Score(int?), PassPercentage(int)=70, AllowAnswerReview(bool)=true,
//   GenerateCertificate(bool)=false, IsPassed(bool?), CompletedAt(DateTime?), StartedAt(DateTime?),
//   ValidUntil(DateOnly?), NomorSertifikat(string?, UNIQUE), IsTokenRequired(bool), AccessToken(string),
//   CreatedAt(DateTime), CreatedBy(string?), ProtonTrackId(int?), IsManualEntry(bool)=false,
//   CertificateType(string?), AssessmentType(string?), LinkedGroupId(int?), LinkedSessionId(int?),
//   HasManualGrading(bool)=false, Progress(int)=0
// AssessmentPackage.cs — Id, AssessmentSessionId, PackageName(string), PackageNumber(int), CreatedAt, Questions(nav)
// PackageQuestion.cs — Id, AssessmentPackageId, QuestionText(string), Order(int), ScoreValue(int)=10,
//   QuestionType(string? — "MultipleChoice"/"MultipleAnswer"/"Essay"; null=MultipleChoice), ElemenTeknis(string?),
//   Rubrik(string?), MaxCharacters(int)=2000, Options(nav)
// PackageOption.cs — Id, PackageQuestionId, OptionText(string), IsCorrect(bool)
// UserPackageAssignment.cs — Id, AssessmentSessionId, AssessmentPackageId, UserId(string),
//   ShuffledQuestionIds(string JSON "[]"), ShuffledOptionIdsPerQuestion(string JSON "{}"),
//   AssignedAt(DateTime), IsCompleted(bool)=false, SavedQuestionCount(int?); helper GetShuffledQuestionIds()
// PackageUserResponse.cs — Id, AssessmentSessionId, PackageQuestionId, PackageOptionId(int?),
//   SubmittedAt(DateTime), TextAnswer(string?), EssayScore(int?)  [0..ScoreValue]
// SessionElemenTeknisScore.cs — Id, AssessmentSessionId, ElemenTeknis(string), CorrectCount(int), QuestionCount(int)
// AuditLog.cs — Id, ActorUserId(string,Required), ActorName(string,Required), ActionType(string,Required,MaxLength 50),
//   Description(string,Required), TargetId(int?), TargetType(string?,MaxLength 100), CreatedAt(DateTime)
// ApplicationUser — Id(string), UserName, Email, FullName, NIP(string?)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definisikan DTO kontrak service inject + register DI</name>
  <files>Models/InjectAssessmentDtos.cs, Program.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-CONTEXT.md (decisions D-01..D-12 + Claude's Discretion: bentuk DTO)
    - .planning/phases/393-backend-core-inject/393-RESEARCH.md (Open Questions #1 DTO; rekomendasi service terima POCO question/option lalu insert sendiri)
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [D] field values AssessmentSession; block [E]/[F] cert mode)
    - Program.cs:48-72 (lokasi DI registration; sisip sejajar L54 GradingService)
    - Models/AssessmentSession.cs, Models/AssessmentPackage.cs, Models/PackageUserResponse.cs (field entity referensi DTO)
  </read_first>
  <behavior>
    - DTO ter-compile sebagai POCO murni (tidak ter-attach DbContext, tidak ada field navigasi EF) → service standalone-testable (RESEARCH Open Q#1).
    - InjectRequest cukup membawa: room settings (Title, Category, Schedule/CompletedAt/StartedAt backdate, DurationMinutes, PassPercentage, AllowAnswerReview, AssessmentType, GenerateCertificate, ValidUntil, CertMode, optional LinkedGroupId/LinkedSessionId), authored questions+options (POCO), dan list workers (NIP) masing-masing dengan list jawaban per-soal + optional manual cert number.
    - InjectResult membawa: status sukses/tolak, list SessionId sukses, list NIP yang di-skip duplikat, dan PerRowErrors (daftar error per-baris untuk D-03 reject-all).
  </behavior>
  <action>
Buat file BARU `Models/InjectAssessmentDtos.cs` (namespace `HcPortal.Models`) berisi DTO POCO berikut. Bentuk ini = kontrak yang dikonsumsi service (Plan 02) + test (Plan 03). Gunakan struktur ini PERSIS (boleh tambah XML-doc komentar Bahasa Indonesia, JANGAN ubah nama/tipe field):

```csharp
namespace HcPortal.Models
{
    /// <summary>Mode sertifikat per-room inject (D-07/D-08/D-09/D-10).</summary>
    public enum InjectCertMode { None = 0, Auto = 1, Manual = 2 }

    /// <summary>Spesifikasi opsi soal authored (POCO — belum ter-persist).</summary>
    public class InjectOptionSpec
    {
        public string OptionText { get; set; } = "";
        public bool IsCorrect { get; set; }
        public int TempId { get; set; }   // id sementara untuk peta jawaban worker → opsi (pre-persist)
    }

    /// <summary>Spesifikasi soal authored (POCO — belum ter-persist).</summary>
    public class InjectQuestionSpec
    {
        public string QuestionText { get; set; } = "";
        public string QuestionType { get; set; } = "MultipleChoice";   // "MultipleChoice"/"MultipleAnswer"/"Essay"
        public int ScoreValue { get; set; } = 10;
        public int Order { get; set; }
        public string? ElemenTeknis { get; set; }
        public string? Rubrik { get; set; }
        public int TempId { get; set; }   // id sementara untuk peta jawaban worker → soal (pre-persist)
        public List<InjectOptionSpec> Options { get; set; } = new();
    }

    /// <summary>Jawaban satu worker untuk satu soal (model "input asli", D-08/INJ-08).</summary>
    public class InjectAnswerSpec
    {
        public int QuestionTempId { get; set; }
        public List<int> SelectedOptionTempIds { get; set; } = new();   // MC: 1 elemen; MA: ≥1; Essay: kosong
        public string? TextAnswer { get; set; }   // Essay
        public int? EssayScore { get; set; }       // Essay — wajib (D-05), divalidasi 0..ScoreValue (D-07)
    }

    /// <summary>Satu worker penerima inject + jawabannya + optional nomor cert manual.</summary>
    public class InjectWorkerSpec
    {
        public string Nip { get; set; } = "";
        public List<InjectAnswerSpec> Answers { get; set; } = new();
        public string? ManualCertNumber { get; set; }   // dipakai hanya bila CertMode==Manual (D-09)
        public DateOnly? CertValidUntil { get; set; }    // null = permanent (D-10)
    }

    /// <summary>Request inject batch (1 room, 1 paket, banyak worker).</summary>
    public class InjectRequest
    {
        public string Title { get; set; } = "";
        public string Category { get; set; } = "";
        public string AssessmentType { get; set; } = "Standard";   // BUKAN "Manual" — D-deviation
        public DateTime CompletedAt { get; set; }   // backdate (D-06: ≤ today)
        public DateTime? StartedAt { get; set; }     // backdate; null → pakai CompletedAt
        public DateTime? Schedule { get; set; }      // backdate; null → pakai CompletedAt
        public int DurationMinutes { get; set; } = 60;
        public int PassPercentage { get; set; } = 70;
        public bool AllowAnswerReview { get; set; } = true;
        public InjectCertMode CertMode { get; set; } = InjectCertMode.None;
        public int? LinkedGroupId { get; set; }
        public int? LinkedSessionId { get; set; }
        public List<InjectQuestionSpec> Questions { get; set; } = new();
        public List<InjectWorkerSpec> Workers { get; set; } = new();
    }

    /// <summary>Satu error per-baris (NIP) untuk D-03 reject-all.</summary>
    public class InjectRowError
    {
        public string Nip { get; set; } = "";
        public string Message { get; set; } = "";   // Bahasa Indonesia
    }

    /// <summary>Hasil inject batch.</summary>
    public class InjectResult
    {
        public bool Success { get; set; }            // true bila ter-commit (boleh ada skip)
        public bool Rejected { get; set; }            // true bila pre-flight tolak-semua (D-03)
        public List<int> SuccessSessionIds { get; set; } = new();
        public List<string> SkippedNips { get; set; } = new();   // duplikat di-skip (D-01)
        public List<InjectRowError> PerRowErrors { get; set; } = new();
        public string? Message { get; set; }          // ringkasan Bahasa Indonesia (mis. pesan rollback)
    }
}
```

Lalu daftarkan DI di `Program.cs` — sisipkan SETELAH baris 54 (`AddScoped<HcPortal.Services.GradingService>()`):
```csharp
// Inject Assessment service — Phase 393 INJ-01/02 (Scoped, sama seperti GradingService — captive DbContext aman)
builder.Services.AddScoped<HcPortal.Services.InjectAssessmentService>();
```
(Service class dibuat di Task 2 sebagai stub agar DI ter-resolve & build hijau.)
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `Models/InjectAssessmentDtos.cs` ada.
    - `Select-String -Path Models/InjectAssessmentDtos.cs -Pattern "class InjectRequest"` cocok 1×.
    - `Select-String -Path Models/InjectAssessmentDtos.cs -Pattern "enum InjectCertMode"` cocok 1×.
    - `Select-String -Path Models/InjectAssessmentDtos.cs -Pattern "class InjectResult"` cocok 1×.
    - `Select-String -Path Program.cs -Pattern "AddScoped<HcPortal.Services.InjectAssessmentService>"` cocok 1× (setelah Task 2 selesai build hijau).
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
  </acceptance_criteria>
  <done>DTO kontrak ter-compile sebagai POCO; InjectAssessmentService ter-register di Program.cs; build 0 error (bergantung stub Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Buat skeleton InjectAssessmentService (ctor DI + signature InjectBatchAsync)</name>
  <files>Services/InjectAssessmentService.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [A] struktur service + ctor DI; signature delegasi GradeAndCompleteAsync)
    - Services/GradingService.cs:1-38 (analog struktur kelas service + field readonly + ctor injection — TIRU header/namespace/usings)
    - Models/InjectAssessmentDtos.cs (DTO yang baru dibuat Task 1)
  </read_first>
  <action>
Buat file BARU `Services/InjectAssessmentService.cs`. TIRU struktur `GradingService` (block [A] PATTERNS.md): namespace `HcPortal.Services`, usings `HcPortal.Data`/`HcPortal.Helpers`/`HcPortal.Models`/`Microsoft.EntityFrameworkCore`, field readonly + ctor injection.

Deps ctor (PATTERNS [A] "Change"): `(ApplicationDbContext context, GradingService gradingService, ILogger<InjectAssessmentService> logger)`. Simpan ke `_context`, `_gradingService`, `_logger`. JANGAN inject `AuditLogService` (audit pakai `_context.AuditLogs.Add` langsung di Plan 02).

Method publik tunggal (signature FINAL — Plan 02 isi body, Plan 03 panggil):
```csharp
public async Task<InjectResult> InjectBatchAsync(InjectRequest req, string actorUserId, string actorName)
```
Body STUB sementara (Plan 02 ganti): kembalikan `new InjectResult { Success = false, Message = "Belum diimplementasikan (Plan 02)." }`. Tambah `// TODO Plan 02: pre-flight (D-03) → dedup (D-01/D-02) → tx (D-04) → per-worker insert+grade+finalize+backdate+audit.` agar batas implementasi jelas.

Catatan: `actorUserId`/`actorName` = parameter (service tak punya `HttpContext` — RESEARCH A4). Stub ini cukup untuk build hijau + DI resolve + factory test.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `Services/InjectAssessmentService.cs` ada.
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "public async Task<InjectResult> InjectBatchAsync\(InjectRequest req, string actorUserId, string actorName\)"` cocok 1×.
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "private readonly GradingService _gradingService"` cocok 1×.
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "AuditLogService"` cocok 0× (audit via _context.AuditLogs.Add di Plan 02, bukan service).
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
  </acceptance_criteria>
  <done>Skeleton service ter-compile dengan ctor DI benar + signature InjectBatchAsync final; DI Task 1 ter-resolve.</done>
</task>

<task type="auto">
  <name>Task 3: Buat kelas test xUnit Integration + fixture disposable SQL + 5 stub fact (SC1..SC5) + seed helper</name>
  <files>HcPortal.Tests/InjectAssessmentServiceTests.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [T1] fixture, [T2] real GradingService wiring, [T3] seed helper, [T4] assertion style + map 5 SC)
    - .planning/phases/393-backend-core-inject/393-VALIDATION.md (Per-Task Verification Map SC1..SC5 + Wave 0 Requirements)
    - HcPortal.Tests/SubmitResurrectionTests.cs:25-122 (fixture IAsyncLifetime + NewGradingService factory + SeedUserAsync — TIRU verbatim, ganti nama)
    - HcPortal.Tests/EssayFinalizeRecomputeTests.cs (assertion style + read-after-commit + scope query ke sessionId, BUKAN FirstAsync global)
    - Models/InjectAssessmentDtos.cs + Services/InjectAssessmentService.cs (kontrak yang dipanggil)
  </read_first>
  <action>
Buat file BARU `HcPortal.Tests/InjectAssessmentServiceTests.cs`. TIRU `SubmitResurrectionTests.cs` (block [T1]/[T2]/[T3]):

1. **Fixture** `InjectAssessmentFixture : IAsyncLifetime` — SALIN VERBATIM dari `SubmitResurrectionFixture` (SubmitResurrectionTests.cs:25-58): `DbName = $"HcPortalDB_Test_{Guid.NewGuid():N}"`, connstring `Server=localhost\\SQLEXPRESS;...TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30`, `InitializeAsync` → `MigrateAsync` (catch → EnsureDeleted + throw XunitException "MIGRATION-CHAIN break, BUKAN bug"), `DisposeAsync` → `EnsureDeletedAsync`. Ganti nama kelas → `InjectAssessmentFixture`. (`HcPortalDB_Dev` TAK disentuh — VALIDATION Wave 0 req #3.)

2. **Kelas test** `[Trait("Category", "Integration")] public class InjectAssessmentServiceTests : IClassFixture<InjectAssessmentFixture>` + ctor terima fixture + `NewCtx()` = `new ApplicationDbContext(_fixture.Options)`.

3. **Factory** (block [T2]): SALIN `NewGradingService(ApplicationDbContext ctx)` VERBATIM dari SubmitResurrectionTests.cs:68-76 (pakai `FakeNotificationService`, `AuditLogService`, `ProtonCompletionService`, `ProtonBypassService`, `FakeWorkerDataService`, `NullLogger` — SEMUA SUDAH ADA di HcPortal.Tests/, JANGAN buat baru). Tambah:
```csharp
private InjectAssessmentService NewInjectService(ApplicationDbContext ctx)
    => new InjectAssessmentService(ctx, NewGradingService(ctx), NullLogger<InjectAssessmentService>.Instance);
```

4. **Seed helper** (block [T3], "Change"): `SeedUserAsync(ApplicationDbContext ctx, string nip)` — buat `ApplicationUser` dengan `NIP = nip` (WAJIB set NIP — inject resolve by NIP), UserName/Email unik via Guid, return user.Id. Plus helper `BuildSampleRequest(...)` yang menyusun `InjectRequest` minimal (1 paket MC+MA+Essay, list worker dengan jawaban) — dipakai stub & Plan 03. Bentuk helper boleh disesuaikan; cukup untuk dikembangkan Plan 03.

5. **5 stub fact** — nama method WAJIB cocok dengan filter VALIDATION.md (FullyQualifiedName~):
   - `[Fact] public async Task InjectAssessment_ByteIdentikOnline_MC_MA_Essay()` → SC1 (`FullyQualifiedName~InjectAssessment`)
   - `[Fact] public async Task InjectAtomic_RollbackOnError()` → SC2 (`FullyQualifiedName~InjectAtomic`)
   - `[Fact] public async Task InjectEssayCompleted_AfterFinalize()` → SC3 (`FullyQualifiedName~InjectEssayCompleted`)
   - `[Fact] public async Task InjectAudit_ManualInjectCountPerSession()` → SC4 (`FullyQualifiedName~InjectAudit`)
   - `[Fact] public async Task InjectCertPolicy_BackdateSuppressManualRange()` → SC5 (`FullyQualifiedName~InjectCertPolicy`)
   Tiap stub body: `await Task.CompletedTask;` + `Assert.True(true, "STUB — diisi Plan 03");` (HIJAU sebagai placeholder; Plan 03 ganti dengan assertion nyata). JANGAN biarkan stub gagal — ini placeholder build-only, bukan RED.
  </action>
  <verify>
    <automated>dotnet build HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/InjectAssessmentServiceTests.cs` ada.
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern '\[Trait\("Category", "Integration"\)\]'` cocok 1×.
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "HcPortalDB_Test_"` cocok (fixture disposable; DB Dev tak disentuh).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "new InjectAssessmentService"` cocok 1× (factory NewInjectService).
    - 5 stub fact ada: grep cocok untuk `InjectAssessment_ByteIdentikOnline_MC_MA_Essay`, `InjectAtomic_RollbackOnError`, `InjectEssayCompleted_AfterFinalize`, `InjectAudit_ManualInjectCountPerSession`, `InjectCertPolicy_BackdateSuppressManualRange`.
    - `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → "Build succeeded", 0 error.
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration"` → 347 passed (stub Integration ter-exclude, no regression baseline Phase 387).
  </acceptance_criteria>
  <done>Kelas test Integration + fixture disposable real-SQL + factory real GradingService + seed helper + 5 stub fact (nama match filter VALIDATION) ter-compile; fast suite 347/347 hijau (stub ter-exclude).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Phase 394 controller) → InjectAssessmentService | Service tak punya HttpContext; identitas actor (actorUserId/actorName) dilewatkan sbg parameter. RBAC Admin/HC ditegakkan di Phase 394, BUKAN di service (V4 partial). Plan ini hanya menetapkan kontrak — boundary belum aktif (no HTTP). |
| DTO POCO → DbContext | DTO tidak ter-attach; service (Plan 02) yang insert. Tidak ada mass-assignment EF dari DTO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-393-01 | Spoofing | actor identity param | mitigate | DTO/signature membawa `actorUserId`+`actorName` sebagai parameter eksplisit (bukan resolve di service) → Phase 394 controller wajib pass identitas terotentikasi; audit (Plan 02) merekamnya |
| T-393-02 | Tampering | DTO field liar (score/cert/date) | accept (di plan ini) | Plan 01 hanya kontrak; validasi nilai (D-03/D-06/D-07/D-09) = Plan 02 pre-flight. Tidak ada write di Plan 01 → permukaan nihil |
| T-393-03 | Repudiation | jejak inject | mitigate | Kontrak `InjectResult` membawa SuccessSessionIds + SkippedNips + PerRowErrors; AuditLog (Plan 02) ActionType "ManualInject"/"...Skipped"/"...Rejected" |
| T-393-04 | Information Disclosure | DB test bocor ke Dev | mitigate | Fixture pakai `HcPortalDB_Test_{guid}` disposable + EnsureDeletedAsync; `HcPortalDB_Dev` TAK disentuh (VALIDATION Wave 0 req #3) |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error (DTO + service stub + DI).
- `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → 0 error (test stub).
- `dotnet test --filter "Category!=Integration"` → 347/347 hijau (no regression; stub Integration ter-exclude).
- Kontrak DTO + signature InjectBatchAsync stabil untuk dikonsumsi Plan 02 (implementasi) & Plan 03 (test bodies).
</verification>

<success_criteria>
- `Models/InjectAssessmentDtos.cs` ada dengan InjectRequest/InjectResult/InjectWorkerSpec/InjectQuestionSpec/InjectOptionSpec/InjectAnswerSpec/InjectRowError + enum InjectCertMode (POCO, ter-compile).
- `Services/InjectAssessmentService.cs` ada dengan ctor `(ApplicationDbContext, GradingService, ILogger<InjectAssessmentService>)` + `InjectBatchAsync(InjectRequest, string, string)` stub.
- `Program.cs` punya `AddScoped<HcPortal.Services.InjectAssessmentService>()`.
- `HcPortal.Tests/InjectAssessmentServiceTests.cs` ada: `[Trait("Category","Integration")]` + fixture disposable SQL + NewInjectService factory + seed helper NIP + 5 stub fact (nama match filter VALIDATION).
- Build 0 error; fast suite 347/347; 0 migration (no schema change).
</success_criteria>

<output>
After completion, create `.planning/phases/393-backend-core-inject/393-01-SUMMARY.md`
</output>
