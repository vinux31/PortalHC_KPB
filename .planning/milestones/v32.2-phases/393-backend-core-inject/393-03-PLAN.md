---
phase: 393-backend-core-inject
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - HcPortal.Tests/InjectAssessmentServiceTests.cs
autonomous: true
requirements: [INJ-01, INJ-02]
tags: [assessment, grading, inject, test, xunit, integration]

must_haves:
  truths:
    - "SC1: inject MC/MA/Essay → Score/IsPassed/SessionElemenTeknisScore/NomorSertifikat byte-identik vs AssessmentScoreAggregator.Compute (jalur online) — dikunci xUnit Integration"
    - "SC2: 1 NIP invalid → Rejected + 0 sesi tertulis; error mid-batch (cert manual collision pekerja ke-2 di-buat menabrak) → rollback total (0 sesi/assignment/response/audit committed)"
    - "SC3: sesi essay ber-EssayScore → Status=Completed (BUKAN PendingGrading) + Score benar + IsPassed + backdate CompletedAt ter-preserve (bukan ≈ now)"
    - "SC4: N pekerja sukses → AuditLogs.Count(ActionType==\"ManualInject\")==N (tiap TargetId=sessionId); 1 duplikat → ManualInjectSkipped TIDAK menambah count ManualInject"
    - "SC5: cert auto ROMAN/tahun = tanggal backdate (D-12); cert suppress bila !IsPassed (D-08); cert manual collision → reject (D-09); EssayScore>ScoreValue → invalid (D-07); tanggal masa depan → invalid (D-06)"
  artifacts:
    - path: "HcPortal.Tests/InjectAssessmentServiceTests.cs"
      provides: "5 fact xUnit Integration berisi assertion nyata (SC1..SC5) — bukan stub"
      contains: "AssessmentScoreAggregator.Compute"
  key_links:
    - from: "HcPortal.Tests/InjectAssessmentServiceTests.cs"
      to: "Services/InjectAssessmentService.InjectBatchAsync"
      via: "panggil InjectBatchAsync lalu assert hasil DB read-after-commit"
      pattern: "InjectBatchAsync"
    - from: "HcPortal.Tests/InjectAssessmentServiceTests.cs"
      to: "Helpers/AssessmentScoreAggregator.Compute"
      via: "expected-value byte-identik (sumber kebenaran sama dengan online)"
      pattern: "AssessmentScoreAggregator\\.Compute"
---

<objective>
Ganti 5 stub fact (dari Plan 01) di `HcPortal.Tests/InjectAssessmentServiceTests.cs` dengan assertion nyata yang mengunci 5 Success Criteria (SC1..SC5) dari ROADMAP/VALIDATION. Tiap test seed input via `InjectRequest`, panggil `InjectAssessmentService.InjectBatchAsync` (Plan 02), lalu verifikasi hasil DB lewat context BARU (read-after-commit) — byte-identik vs `AssessmentScoreAggregator.Compute` (sumber kebenaran yang sama dipakai online).

Purpose: Nyquist proof — tiap perilaku phase punya verifikasi otomatis. Ini menutup Wave 0 gap VALIDATION.md (5 fact ❌ red → ✅ green).
Output: `HcPortal.Tests/InjectAssessmentServiceTests.cs` dengan 5 fact ber-assertion nyata. `dotnet test --filter "FullyQualifiedName~InjectAssessment&Category=Integration"` hijau (butuh SQLEXPRESS lokal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/393-backend-core-inject/393-RESEARCH.md
@.planning/phases/393-backend-core-inject/393-PATTERNS.md
@.planning/phases/393-backend-core-inject/393-VALIDATION.md

<interfaces>
<!-- Kontrak Plan 01/02 + analog test. Pakai langsung. -->

Service (Plan 02): `await injectSvc.InjectBatchAsync(InjectRequest req, string actorUserId, string actorName)` → InjectResult{Success,Rejected,SuccessSessionIds[],SkippedNips[],PerRowErrors[],Message?}.

Helper test sudah ada (Plan 01): fixture `InjectAssessmentFixture` (disposable HcPortalDB_Test_{guid}), `NewCtx()`, `NewGradingService(ctx)`, `NewInjectService(ctx)`, `SeedUserAsync(ctx, nip)`, `BuildSampleRequest(...)`.

Expected-value (byte-identik): `AssessmentScoreAggregator.Compute(IEnumerable<PackageQuestion> questions, IEnumerable<PackageUserResponse> responses, int passPercentage)` → ScoreAggregateResult(TotalScore,MaxScore,Percentage,IsPassed). Inject memakai mesin yang SAMA → assert `session.Score == agg.Percentage`, `session.IsPassed == agg.IsPassed`.

Cert format: `KPB/{seq:D3}/{ROMAN}/{year}` (CertNumberHelper.Build). D-12: ROMAN/year = bulan/tahun `req.CompletedAt` (backdate), BUKAN saat test dijalankan.

Entity untuk assert: AssessmentSession{Score,IsPassed,Status,CompletedAt,NomorSertifikat,IsManualEntry}; SessionElemenTeknisScore{ElemenTeknis,CorrectCount,QuestionCount}; AuditLog{ActionType,TargetId,Description}. Status Completed = `AssessmentConstants.AssessmentStatus.Completed` ("Completed"); PendingGrading = "Menunggu Penilaian".

Caveat shared-DB (EssayFinalizeRecomputeTests.cs:174-179): JANGAN `FirstAsync` global — scope semua query ke sessionId/userId tertentu (DB di-share antar test dalam 1 fixture).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC1 byte-identik (MC/MA/Essay) + SC3 essay→Completed + backdate preserve</name>
  <files>HcPortal.Tests/InjectAssessmentServiceTests.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [T4] assertion style + read-after-commit + scope query ke sessionId; daftar test WAJIB per SC)
    - .planning/phases/393-backend-core-inject/393-VALIDATION.md (SC1/SC3 rows + fixtures yang membuktikan tiap SC)
    - .planning/phases/393-backend-core-inject/393-RESEARCH.md (Code Examples byte-identik assertion; Pitfall 1 backdate preserve; Pitfall 5 essay PendingGrading)
    - HcPortal.Tests/EssayFinalizeRecomputeTests.cs:251-335 (assertion style + read-after-commit via context baru)
    - HcPortal.Tests/InjectAssessmentServiceTests.cs (stub Plan 01 yang diganti)
  </read_first>
  <behavior>
    - Test 1 (SC1): seed 1 room 3 soal — 1 MC (jawab benar), 1 MA (all-or-nothing: skenario benar = pilih SEMUA opsi benar persis), 1 Essay (EssayScore=80% dari ScoreValue). Tiap soal beda ElemenTeknis. Inject 1 worker. Assert: session.Score == Compute(questions,responses,passPct).Percentage; session.IsPassed == agg.IsPassed; SessionElemenTeknisScores cocok (CorrectCount/QuestionCount per ET); NomorSertifikat format KPB/.../ROMAN/year bila lulus+cert auto.
    - Test 1b (SC1 negatif): tambah skenario MA partial-select → all-or-nothing = 0 poin soal itu; MC salah = 0. Assert Score turun sesuai Compute.
    - Test 3 (SC3): seed essay ber-EssayScore → setelah inject, Status == Completed (BUKAN "Menunggu Penilaian"), Score == agg.Percentage, IsPassed sesuai; CompletedAt == backdate (req.CompletedAt.Date), BUKAN DateTime.Today (Pitfall 1 — backdate ter-preserve pasca finalize/grade).
  </behavior>
  <action>
Isi fact `InjectAssessment_ByteIdentikOnline_MC_MA_Essay()` (SC1) dan `InjectEssayCompleted_AfterFinalize()` (SC3). Pola read-after-commit (EssayFinalizeRecomputeTests.cs:330-335):

SC1:
1. `await using var seed = NewCtx();` seed user (NIP unik) via `SeedUserAsync`.
2. Bangun `InjectRequest` (BuildSampleRequest atau inline): 3 InjectQuestionSpec — MC (2 opsi, 1 benar, ScoreValue=10, ElemenTeknis="ET-A"), MA (3 opsi, 2 benar, ScoreValue=10, ET="ET-B"), Essay (ScoreValue=10, ET="ET-C", Rubrik). 1 InjectWorkerSpec menjawab benar (MC pilih opsi benar; MA pilih KEDUA opsi benar; Essay EssayScore=8, TextAnswer="..."). CertMode=Auto, PassPercentage=70, CompletedAt = backdate (mis. `new DateTime(2026,5,15)` — bulan Mei untuk uji D-12 ROMAN=V).
3. `var injectSvc = NewInjectService(NewCtx());` `var result = await injectSvc.InjectBatchAsync(req, "test-actor-id", "Test Actor");`
4. `Assert.True(result.Success); Assert.Single(result.SuccessSessionIds);`
5. **Read-after-commit** via context baru: `await using var verify = NewCtx();` load session by `result.SuccessSessionIds[0]`, load PackageQuestions (Include Options) + PackageUserResponses scoped ke session.Id.
6. **Expected via Compute** (sumber kebenaran online): `var agg = AssessmentScoreAggregator.Compute(questions, responses, req.PassPercentage);` `Assert.Equal(agg.Percentage, s.Score); Assert.Equal(agg.IsPassed, s.IsPassed); Assert.True(s.IsManualEntry);`
7. **ET assert**: load `SessionElemenTeknisScores` by session.Id; assert ada 3 ET (ET-A/B/C), CorrectCount/QuestionCount sesuai (MC benar → 1/1, MA all-or-nothing benar → 1/1, Essay >0 → 1/1).
8. **Cert assert** (bila lulus): `Assert.Matches(@"^KPB/\d{3}/V/2026$", s.NomorSertifikat)` (D-12: bulan Mei=V, tahun 2026 = backdate, BUKAN bulan test dijalankan).
9. **Tambah Test 1b** (boleh fact terpisah atau Theory): MA partial-select (pilih 1 dari 2 benar) → assert Score == Compute (soal MA = 0 poin, all-or-nothing). MC salah → 0.

SC3 (`InjectEssayCompleted_AfterFinalize`):
1. Seed user; `InjectRequest` 1 soal Essay (ScoreValue=10, EssayScore=8) — lulus (80% ≥ 70%). CompletedAt = backdate `new DateTime(2026,5,15)`. CertMode=None (atau Auto).
2. `result = await InjectBatchAsync(...)`; `Assert.True(result.Success);`
3. Read-after-commit: `var s = await verify.AssessmentSessions.FindAsync(result.SuccessSessionIds[0]);`
4. `Assert.Equal(AssessmentConstants.AssessmentStatus.Completed, s.Status);` (⚠ BUKAN PendingGrading — D-05, Pitfall 5).
5. `Assert.Equal(80, s.Score); Assert.True(s.IsPassed);`
6. **Backdate preserve** (Pitfall 1): `Assert.Equal(new DateTime(2026,5,15).Date, s.CompletedAt!.Value.Date);` (BUKAN DateTime.Today — buktikan re-apply [G] jalan).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~InjectAssessment_ByteIdentik|FullyQualifiedName~InjectEssayCompleted" 2>&1 | Select-String -Pattern "Passed|Failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "AssessmentScoreAggregator.Compute"` cocok ≥1× (expected byte-identik).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "AssessmentConstants.AssessmentStatus.Completed"` cocok ≥1× (SC3 essay→Completed).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern 'KPB/'` cocok (SC1 cert format D-12).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "STUB"` cocok 0× untuk fact SC1 & SC3 (stub diganti assertion nyata).
    - `dotnet test --filter "FullyQualifiedName~InjectAssessment_ByteIdentik"` → 1+ passed.
    - `dotnet test --filter "FullyQualifiedName~InjectEssayCompleted"` → 1 passed.
  </acceptance_criteria>
  <done>SC1 (byte-identik MC/MA/Essay + ET + cert) & SC3 (essay→Completed + backdate preserve) hijau via xUnit Integration real-SQL.</done>
</task>

<task type="auto">
  <name>Task 2: SC2 atomic rollback + SC5 cert/validasi policy</name>
  <files>HcPortal.Tests/InjectAssessmentServiceTests.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-VALIDATION.md (SC2/SC5 rows + fixtures yang membuktikan)
    - .planning/phases/393-backend-core-inject/393-RESEARCH.md (SC2 atomic: NIP invalid → 0 tulisan + exception mid-batch → rollback; Pitfall 4 cert manual collision; D-06/D-07/D-08/D-09/D-12)
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [F] cert backdate D-12; [B] cert manual collision pre-flight)
    - HcPortal.Tests/InjectAssessmentServiceTests.cs (stub SC2/SC5 yang diganti)
  </read_first>
  <behavior>
    - SC2a: batch dengan 1 NIP tak ada di AspNetUsers → result.Rejected == true; AssessmentSessions.Count(inject untuk batch ini)==0 (0 tulisan).
    - SC2b: rollback mid-batch — gunakan skenario deterministik. Cara terbaik: inject batch sukses A (cert manual unik) commit; lalu inject batch B berisi worker valid + cert manual yang menabrak A → pre-flight D-09 menangkap → Rejected, 0 tulisan untuk batch B (buktikan tak ada parsial). Bila executor ingin menguji jalur catch→Rollback murni (bukan pre-flight), dokumentasikan di SUMMARY bahwa pre-flight mencegah collision sampai DB; yang WAJIB lulus: 0 sesi parsial pada kedua skenario gagal (NIP-invalid + cert-collision).
    - SC5: (i) cert auto ROMAN = bulan backdate (perkuat dgn bulan beda dari SC1); (ii) !IsPassed → NomorSertifikat == null walau CertMode=Auto (D-08 suppress); (iii) cert manual collision intra-batch → Rejected (D-09); (iv) EssayScore > ScoreValue → Rejected + PerRowErrors (D-07); (v) CompletedAt masa depan → Rejected (D-06).
  </behavior>
  <action>
Isi fact `InjectAtomic_RollbackOnError()` (SC2) dan `InjectCertPolicy_BackdateSuppressManualRange()` (SC5).

SC2 (`InjectAtomic_RollbackOnError`):
1. **SC2a NIP invalid**: `InjectRequest` 2 worker — 1 NIP valid (seeded), 1 NIP "NIP-TIDAK-ADA". `result = await InjectBatchAsync(...)`. `Assert.True(result.Rejected); Assert.False(result.Success);`
2. Read-after: `await using var verify = NewCtx(); var count = await verify.AssessmentSessions.CountAsync(s => s.Title == req.Title && s.CompletedAt!.Value.Date == req.CompletedAt.Date); Assert.Equal(0, count);` (0 tulisan — D-03 reject-all). Plus `Assert.Equal(0, await verify.AuditLogs.CountAsync(a => a.ActionType=="ManualInject" && a.Description.Contains(req.Title)));` untuk batch ini.
3. **SC2b rollback/no-partial mid-batch** (mekanisme deterministik): inject batch sukses A (pekerja-A, cert manual "KPB/999/X/2099") commit. Lalu inject batch B (judul beda) berisi pekerja-B valid + cert manual "KPB/999/X/2099" (sama dgn A) → pre-flight D-09 menangkap → Rejected, 0 tulisan untuk batch B. Assert: `Assert.True(resultB.Rejected); Assert.Equal(0, await verify.AssessmentSessions.CountAsync(s => s.Title == reqB.Title));` — buktikan tak ada parsial.

SC5 (`InjectCertPolicy_BackdateSuppressManualRange`) — boleh beberapa Assert dalam 1 fact atau pecah jadi Theory:
1. **D-12 ROMAN backdate**: inject lulus, CertMode=Auto, CompletedAt=`new DateTime(2024,3,10)` (Maret 2024). Assert `Assert.Matches(@"^KPB/\d{3}/III/2024$", s.NomorSertifikat);` (ROMAN III, tahun 2024 = backdate).
2. **D-08 suppress !IsPassed**: inject TIDAK lulus (jawaban salah → Score < PassPercentage), CertMode=Auto. Assert `Assert.False(s.IsPassed); Assert.Null(s.NomorSertifikat);` (cert di-suppress walau toggle ON).
3. **D-09 cert manual collision**: 2 worker, CertMode=Manual, kedua ManualCertNumber sama "KPB/777/I/2025". `Assert.True(result.Rejected); Assert.Contains(result.PerRowErrors, e => e.Message.Contains("sertifikat"));` + 0 tulisan.
4. **D-07 EssayScore range**: 1 worker, soal Essay ScoreValue=10, EssayScore=15 (>ScoreValue). `Assert.True(result.Rejected); Assert.Contains(result.PerRowErrors, e => e.Message.Contains("rentang") || e.Message.Contains("essay"));` + 0 tulisan.
5. **D-06 tanggal masa depan**: CompletedAt = `DateTime.Today.AddDays(5)`. `Assert.True(result.Rejected); Assert.Contains(result.PerRowErrors, e => e.Message.Contains("masa depan") || e.Message.Contains("Tanggal"));` + 0 tulisan.

Semua assert "0 tulisan" pakai read-after-commit context baru + scope ke title/tanggal batch (caveat shared-DB).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~InjectAtomic|FullyQualifiedName~InjectCertPolicy" 2>&1 | Select-String -Pattern "Passed|Failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "result.Rejected"` cocok ≥3× (SC2a, SC5 manual/range/date).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "Assert.Null\(s.NomorSertifikat\)"` cocok ≥1× (D-08 suppress).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "III/2024|V/2026|/2024|/2025"` cocok (D-12 ROMAN backdate).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "Equal\(0,"` cocok ≥2× (0 tulisan pada reject/rollback).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "STUB"` cocok 0× untuk fact SC2 & SC5.
    - `dotnet test --filter "FullyQualifiedName~InjectAtomic"` → passed; `dotnet test --filter "FullyQualifiedName~InjectCertPolicy"` → passed.
  </acceptance_criteria>
  <done>SC2 (atomic reject-all + 0 parsial) & SC5 (cert backdate D-12 / suppress D-08 / manual collision D-09 / EssayScore range D-07 / tanggal D-06) hijau.</done>
</task>

<task type="auto">
  <name>Task 3: SC4 audit count + full-suite gate + 0-migration confirm</name>
  <files>HcPortal.Tests/InjectAssessmentServiceTests.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-VALIDATION.md (SC4 row + Manual-Only Verification 0-migration; Validation Sign-Off)
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [H] 3 ActionType — count ManualInject bersih)
    - .planning/phases/393-backend-core-inject/393-RESEARCH.md (SC4 audit count fixture)
    - HcPortal.Tests/InjectAssessmentServiceTests.cs (stub SC4 yang diganti)
  </read_first>
  <behavior>
    - SC4: inject N=3 pekerja sukses → AuditLogs.Count(ActionType=="ManualInject") == 3; tiap entry TargetId == sessionId yang sesuai + Description berisi NIP & skor. Plus: 1 pekerja duplikat (di-skip) → ActionType "ManualInjectSkipped" muncul, dan count "ManualInject" TETAP == jumlah sesi sukses (skip TIDAK menggembungkan).
  </behavior>
  <action>
Isi fact `InjectAudit_ManualInjectCountPerSession()` (SC4):
1. Seed 3 user (NIP unik). `InjectRequest` 1 soal MC + 3 worker (semua jawab benar). `result = await InjectBatchAsync(...)`. `Assert.True(result.Success); Assert.Equal(3, result.SuccessSessionIds.Count);`
2. Read-after-commit: `await using var verify = NewCtx();`
3. **Count ManualInject == N**: scope ke sesi batch ini — `var sessionIds = result.SuccessSessionIds; var manualInjectLogs = await verify.AuditLogs.Where(a => a.ActionType == "ManualInject" && a.TargetId != null && sessionIds.Contains(a.TargetId.Value)).ToListAsync(); Assert.Equal(3, manualInjectLogs.Count);`
4. **Tiap entry valid**: `Assert.All(manualInjectLogs, log => { Assert.NotNull(log.TargetId); Assert.Contains("NIP", log.Description); Assert.Equal("AssessmentSession", log.TargetType); });`
5. **Skip tidak menggembungkan**: jalankan inject ke-2 dengan judul+kategori+tanggal sama untuk salah satu NIP yang SUDAH ada (duplikat) + 1 NIP baru valid → `result2`. Assert `result2.SkippedNips` berisi NIP duplikat; `verify.AuditLogs.CountAsync(a => a.ActionType=="ManualInjectSkipped")` ≥ 1; dan count "ManualInject" untuk NIP duplikat itu TETAP 1 (tidak bertambah dari skip). Verifikasi `ActionType=="ManualInjectSkipped"` BUKAN "ManualInject".

Setelah 5 fact selesai (Task 1-3 plan ini), jalankan **gate fase**:
- Full suite: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` (incl. Integration) — semua hijau (5 inject fact + 347 baseline).
- **0-migration confirm** (VALIDATION Manual-Only): `dotnet ef migrations add _verify` → buka file migration Up()/Down() → konfirmasi kosong/no-op (service hanya INSERT ke tabel existing, tidak ubah model) → HAPUS file migration (`dotnet ef migrations remove` atau git clean) sehingga tidak ada migration baru ter-commit. Catat hasil di SUMMARY.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~InjectAudit" 2>&1 | Select-String -Pattern "Passed|Failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern 'ActionType == "ManualInject"'` cocok ≥1× (count assert).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "ManualInjectSkipped"` cocok ≥1× (skip tak menggembungkan).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "Assert.Equal\(3, "` cocok ≥1× (N=3 audit count).
    - `Select-String -Path HcPortal.Tests/InjectAssessmentServiceTests.cs -Pattern "STUB"` cocok 0× (semua 5 fact berisi assertion nyata).
    - `dotnet test --filter "FullyQualifiedName~InjectAudit"` → passed.
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` (full) → semua hijau (5 inject Integration + 347 baseline; 0 failed).
    - `dotnet ef migrations add _verify` → Up()/Down() kosong (0 model diff) → file dihapus (git status bersih dari migration baru). Konfirmasi 0 migration.
  </acceptance_criteria>
  <done>SC4 (audit count ManualInject==N, skip terpisah) hijau; full suite (5 inject Integration + 347 baseline) hijau; 0-migration ter-konfirmasi (no schema change).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → disposable DB | Fixture pakai HcPortalDB_Test_{guid} (EnsureDeletedAsync); HcPortalDB_Dev TAK disentuh. Tidak ada boundary produksi. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-393-11 | Information Disclosure | test DB bocor / sentuh Dev | mitigate | Disposable HcPortalDB_Test_{guid} + EnsureDeletedAsync di DisposeAsync; semua query scoped ke sessionId/title batch (caveat shared-DB) — DB Dev untouched |
| T-393-12 | (Integrity verification) | SC2/SC5 rollback tak benar-benar 0-write | mitigate | Assert eksplisit `CountAsync(... batch ...) == 0` read-after-commit untuk tiap skenario reject/rollback — bukti tak ada parsial |
| T-393-13 | (Audit verification) | count ManualInject ter-kontaminasi skip/reject | mitigate | SC4 assert count "ManualInject" == N DAN skip pakai ActionType terpisah "ManualInjectSkipped" — kunci D-11 ActionType-bersih |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~InjectAssessment&Category=Integration"` → 5 fact hijau (butuh SQLEXPRESS lokal).
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` (full) → semua hijau (5 inject + 347 baseline, 0 failed).
- `dotnet build` 0 error.
- `dotnet ef migrations add _verify` → 0 model diff → discard. 0 migration ter-konfirmasi.
- Semua 5 SC (ROADMAP Phase 393) terbukti otomatis.
</verification>

<success_criteria>
- 5 fact (SC1..SC5) berisi assertion nyata (bukan stub), nama match filter VALIDATION.
- SC1 byte-identik (MC/MA/Essay + ET + cert D-12); SC2 atomic reject-all + 0 parsial; SC3 essay→Completed + backdate preserve; SC4 audit count ManualInject==N + skip terpisah; SC5 cert backdate/suppress/manual/range/date.
- Full suite hijau (5 inject Integration + 347 baseline); build 0 error; 0 migration ter-konfirmasi.
</success_criteria>

<output>
After completion, create `.planning/phases/393-backend-core-inject/393-03-SUMMARY.md`
</output>
