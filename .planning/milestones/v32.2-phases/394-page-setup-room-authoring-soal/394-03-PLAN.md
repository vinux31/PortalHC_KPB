---
phase: 394-page-setup-room-authoring-soal
plan: 03
type: execute
wave: 3
depends_on: ["394-02"]
files_modified:
  - Views/Admin/InjectAssessment.cshtml
  - Views/Admin/_InjectQuestionForm.cshtml
  - tests/e2e/inject-assessment-394.spec.ts
autonomous: true
requirements: [INJ-05, INJ-07]
must_haves:
  truths:
    - "HC dapat menulis soal MultipleChoice/MultipleAnswer/Essay dengan opsi A-D + kunci IsCorrect + ScoreValue + ElemenTeknis + Rubrik di dalam alur wizard"
    - "Mengubah Tipe Soal men-toggle field: MC=radio, MA=checkbox + notice '≥2 benar', Essay=sembunyikan opsi + tampil rubrik"
    - "'Tambah Soal' menambah soal ke daftar in-flow (state JS) tanpa write DB; daftar soal menampilkan #, teks, tipe, skor"
    - "Tidak ada POST ke CreateQuestion; soal ditahan di form-state sampai commit final (D-07, 0 migration)"
    - "Radio sertifikat 3-mode: Auto→preview format KPB/xxx/ROMAN/year + ValidUntil + Permanent; Manual→NomorSertifikat + ValidUntil + Permanent; Tanpa→tak ada field"
  artifacts:
    - path: "Views/Admin/_InjectQuestionForm.cshtml"
      provides: "Authoring form markup mirror ManagePackageQuestions (MC/MA/Essay, opsi, scoreValue, elemenTeknis, rubrik) sans image/maxCharacters/edit-mode"
      contains: "QuestionType"
    - path: "Views/Admin/InjectAssessment.cshtml"
      provides: "Step-3 authoring (form partial + client-state capture JS + applyQTypeSwitch + daftar soal); Step-4 cert radio 3-mode + conditional fields + JS toggle; hidden serialization of Questions"
      contains: "applyQTypeSwitch"
  key_links:
    - from: "Views/Admin/InjectAssessment.cshtml (Tambah Soal)"
      to: "in-memory questions[] → hidden form fields → VM.Questions"
      via: "client-state capture JS (NOT CreateQuestion POST)"
      pattern: "questions"
    - from: "Views/Admin/InjectAssessment.cshtml (cert radio change)"
      to: "conditional cert blocks (auto preview / manual input / hide)"
      via: "radio change JS toggle"
      pattern: "CertMode"
---

<objective>
Isi Step 3 (Authoring Soal) dan Step 4 (Sertifikat) di wizard. Step 3 = reuse markup form soal dari `ManagePackageQuestions` (extract ke partial `_InjectQuestionForm.cshtml`, field identik) + `applyQTypeSwitch` type-toggle JS, tetapi REWIRE submit: "Tambah Soal" membaca field → push ke array JS `questions[]` → render ke daftar soal → serialize ke hidden fields saat submit (TIDAK pernah POST ke `CreateQuestion`). Step 4 = radio 3-mode sertifikat (Auto/Manual/Tanpa) dengan field kondisional + JS toggle. Image-upload + maxCharacters + edit-mode di-OMIT (DTO tak punya field-nya).

Purpose: Menutup INJ-05 (authoring reuse ManagePackages, nol-duplikasi semantik) dan INJ-07 (toggle sertifikat 3-mode). Ini bagian tersulit (rewire authoring ke client-state per Pitfall 1).
Output: partial baru `_InjectQuestionForm.cshtml`; InjectAssessment.cshtml step 3+4 + authoring/cert JS; 2 Playwright test di-unskip.

NOTE file-overlap: meng-extend `InjectAssessment.cshtml` yang sama dengan Plan 02/04 → WAVE 3 (sequential, setelah Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/394-page-setup-room-authoring-soal/394-CONTEXT.md
@.planning/phases/394-page-setup-room-authoring-soal/394-RESEARCH.md
@.planning/phases/394-page-setup-room-authoring-soal/394-PATTERNS.md
@.planning/phases/394-page-setup-room-authoring-soal/394-UI-SPEC.md
@.planning/phases/394-page-setup-room-authoring-soal/394-02-SUMMARY.md

<interfaces>
<!-- Authoring form markup analog — VERIFIED ManagePackageQuestions.cshtml -->
:116-260 question form. KEEP field names/IDs identical for nol-duplikasi:
  #QuestionType name="questionType" (MultipleChoice/MultipleAnswer/Essay)
  #questionText name="questionText" (required textarea)
  #optionsSection w/ #maLabel "Centang semua opsi yang benar (minimal 2)." (alert-info, display:none)
    A-D rows: input-group-sm w/ .correct-input radio/checkbox name="correctA".."correctD" + input name="optionA".."optionD"
  #rubrikSection (display:none) w/ name="rubrik" #rubrik
  #scoreValue name="scoreValue" number value=10 min=1 max=100 required
  #elemenTeknis name="elemenTeknis" text (opsional)
  #submitBtn "Tambah Soal"
OMIT (DTO has no field): all img-drop blocks (:144-169 question img, :190-210 option imgs + FileReader),
  #maxCharacters (:221-226), editQuestionId/edit-mode/type-change-warning-modal machinery.
:402-418 applyQTypeSwitch(qtype): optionsSection display (Essay→none); rubrikSection (Essay→''); maLabel (MA→'');
   .correct-input type = MA?checkbox:radio, uncheck if not MA.
:6539-6561 (CreateQuestion validation parity): scoreValue 1-100; MC exactly 1 correct; MA ≥2 correct; Essay rubrik required.

<!-- CRITICAL Pitfall 1 — VERIFIED -->
CreateQuestion (AssessmentAdminController.cs:6493-6561) is [HttpPost] + needs persisted packageId + RedirectToAction (full reload).
INCOMPATIBLE with D-07 (no DB write) + D-01 (single-form wizard). DO NOT POST to it.
REWIRE: on "Tambah Soal", build JS obj {questionText, questionType, scoreValue, elemenTeknis, rubrik,
  options:[{text,isCorrect}], tempId} → push to questions[] → render Daftar Soal → clear form.
On wizard submit, serialize questions[] into hidden inputs bound to InjectAssessmentViewModel.Questions
  (e.g. Questions[i].QuestionText, Questions[i].Options[j].OptionText/IsCorrect, .ScoreValue, .Order, .TempId)
  OR a single hidden JSON input #QuestionsJson deserialized in controller (planner discretion — pick one).

<!-- Cert radio target — VM (Plan 01) + DTO -->
InjectCertMode { None=0, Auto=1, Manual=2 }
VM: CertMode, ManualCertNumber?, CertValidUntil?, CertPermanent
Cert card STYLE analog CreateAssessment.cshtml:556-602 (card mb-4 + card-header bg-light bi-award).
UI-SPEC line 124-126: radio labels "Generate Otomatis (Nomor Resmi)"/"Input Nomor Manual"/"Tanpa Sertifikat";
  Auto preview "Format nomor: <code>KPB/xxx/{ROMAN}/{year}</code> (tahun mengikuti tanggal ujian).";
  ValidUntil label "Masa Berlaku Sertifikat" + checkbox "Berlaku selamanya (permanen)".

<!-- UI-SPEC authoring copy -->
line 115 "Tambah Soal" + bi-plus-circle (btn-primary btn-sm)
line 116 MA helper "Centang semua opsi yang benar (minimal 2)."
line 118 empty daftar soal "Belum ada soal. Tambahkan soal menggunakan form di samping." (drop "import dari Excel" clause)
line 121 step3 error "Tambahkan minimal 1 soal sebelum melanjutkan." (alert-danger d-none)
line 127 Hapus soal native confirm('Hapus soal ini?') — affects in-flow state only (no DB)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authoring partial + Step-3 client-state capture (REWIRE, never CreateQuestion)</name>
  <read_first>
    - Views/Admin/ManagePackageQuestions.cshtml:116-260 (form markup), :402-418 (applyQTypeSwitch), :47-50 (empty-state)
    - Controllers/AssessmentAdminController.cs:6493-6561 (CreateQuestion — READ to mirror VALIDATION ONLY, do NOT call it)
    - Views/Admin/InjectAssessment.cshtml (step-3 panel scaffold from Plan 01)
    - .planning/phases/394-page-setup-room-authoring-soal/394-PATTERNS.md (CRITICAL REUSE-vs-REWIRE box) + 394-RESEARCH.md Pattern 3
  </read_first>
  <behavior>
    - Test 'authoring type toggle + add soal': step-3 visible; default QuestionType=MultipleChoice shows options + radio .correct-input; switch to MultipleAnswer → .correct-input become checkbox + #maLabel visible; switch to Essay → #optionsSection hidden, #rubrikSection visible. Fill a MC question (text + 2 options + 1 correct) → click Tambah Soal → daftar soal shows 1 row (#, teks, tipe badge, skor); form clears. Clicking Selanjutnya with 0 soal → step3 error (validateStep(3) false).
    - No CreateQuestion POST occurs (no page reload; URL stays on InjectAssessment).
  </behavior>
  <files>Views/Admin/_InjectQuestionForm.cshtml, Views/Admin/InjectAssessment.cshtml</files>
  <action>
    Create `Views/Admin/_InjectQuestionForm.cshtml` partial: copy the question-form MARKUP from ManagePackageQuestions.cshtml:116-260, keeping field names/IDs IDENTICAL (#QuestionType/name=questionType, #questionText/name=questionText, #optionsSection + #maLabel, A-D rows with .correct-input radio + name="optionA".."optionD", #rubrikSection + name="rubrik", #scoreValue 1-100, #elemenTeknis). The "Tambah Soal" control = `<button type="button" id="injAddQuestionBtn" class="btn btn-primary btn-sm"><i class="bi bi-plus-circle me-1"></i>Tambah Soal</button>` (type="button" — NOT submit; the partial is NOT a nested form). OMIT entirely: all img-drop blocks + FileReader, #maxCharacters, editQuestionId/edit-mode/cancel-edit/type-change-warning-modal. Do NOT wrap the partial in `<form asp-action="CreateQuestion">` — it lives inside the wizard's single form as plain fields read by JS.
    In InjectAssessment.cshtml step-3 panel: 2-column layout — left = `<div id="injQuestionList">` Daftar Soal table (#, Teks, Tipe badge, Skor, Aksi[Hapus]) + empty-state `bi bi-inbox` "Belum ada soal. Tambahkan soal menggunakan form di samping." + a running total-poin display; right = `@await Html.PartialAsync("_InjectQuestionForm")`. Add `<div id="step3Error" class="alert alert-danger d-none">Tambahkan minimal 1 soal sebelum melanjutkan.</div>`. Add a container for serialized hidden question fields `<div id="injQuestionsHidden"></div>` (or a single `<input type="hidden" asp-for="QuestionsJson" id="QuestionsJson">`).
    In `@section Scripts` add the authoring JS:
      - `applyQTypeSwitch(qtype)` ported verbatim from ManagePackageQuestions:402-418; wire to #QuestionType change + call once on load.
      - In-memory `var injQuestions = [];` `var nextTempId = 1;`. On #injAddQuestionBtn click: read fields, CLIENT-VALIDATE (scoreValue 1-100; MC exactly 1 .correct-input:checked; MA ≥2 checked; Essay rubrik required) — on fail add is-invalid + return; build `{tempId, questionText, questionType, scoreValue, elemenTeknis, rubrik, options:[{tempId, text, isCorrect}]}` (options only for MC/MA, skip blank option text), push to injQuestions, increment nextTempId, render row into #injQuestionList (use `.textContent` for question text — XSS), recompute total poin, clear the form fields + reset to MC.
      - Hapus soal: native `confirm('Hapus soal ini?')` → splice from injQuestions + re-render (in-flow only, no DB).
      - On wizard form submit (or in validateStep(6)/populateSummary in Plan 04), serialize injQuestions: either emit indexed hidden inputs into #injQuestionsHidden matching `Questions[i].QuestionText`, `Questions[i].QuestionType`, `Questions[i].ScoreValue`, `Questions[i].Order=i`, `Questions[i].ElemenTeknis`, `Questions[i].Rubrik`, `Questions[i].TempId`, `Questions[i].Options[j].OptionText`, `Questions[i].Options[j].IsCorrect`, `Questions[i].Options[j].TempId` — OR set #QuestionsJson.value = JSON.stringify(injQuestions). (Pick the approach matching how the controller reads it; document the choice in the SUMMARY for Plan 04.) Expose `injQuestions` (e.g. on window) so Plan 04 step-5 answers can reference authored questions.
      - `validateStep(3)` requires injQuestions.length >= 1 (else reveal #step3Error, return false).
    NEVER POST to CreateQuestion. NEVER create a draft package in step 3.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | findstr /C:"Build succeeded" & cd tests && npx playwright test e2e/inject-assessment-394.spec.ts -g "authoring" --workers=1 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <acceptance_criteria>
    - `_InjectQuestionForm.cshtml` exists; grep finds `QuestionType`, `name="questionText"`, `name="rubrik"`, `id="scoreValue"`, `id="injAddQuestionBtn"`; grep does NOT find `questionImage`, `optionAImage`, `maxCharacters`, `editQuestionId`, `asp-action="CreateQuestion"`.
    - InjectAssessment.cshtml grep: contains `applyQTypeSwitch`, `injQuestions`, `injAddQuestionBtn`, `step3Error`; does NOT contain `CreateQuestion`.
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
    - Playwright (AD-off, --workers=1, MAIN tree): 'authoring type toggle + add soal' PASS (un-skipped); no reload on Tambah Soal.
  </acceptance_criteria>
  <done>Authoring reuses ManagePackages field semantics; type-toggle works; Tambah Soal captures to client state + renders list with NO DB write/no CreateQuestion POST; ≥1 soal required to advance; questions serialized for VM binding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Step-4 certificate radio 3-mode + conditional fields + JS toggle</name>
  <read_first>
    - Views/Admin/InjectAssessment.cshtml (step-4 panel scaffold from Plan 01)
    - Views/Admin/CreateAssessment.cshtml:556-602 (cert card styling analog)
    - Helpers/CertNumberHelper.cs (Build pattern → preview string KPB/xxx/ROMAN/year)
    - .planning/phases/394-page-setup-room-authoring-soal/394-UI-SPEC.md:124-126, 140 (cert radio copy + contract)
  </read_first>
  <behavior>
    - Test 'cert radio 3-mode toggle': step-4 has 3 radios (Auto/Manual/Tanpa). Selecting Auto → format-preview block + ValidUntil + Permanent checkbox visible, NomorSertifikat hidden. Selecting Manual → NomorSertifikat input + ValidUntil + Permanent visible, preview hidden. Selecting Tanpa → all conditional blocks hidden. Checking Permanent disables/ignores the ValidUntil input.
  </behavior>
  <files>Views/Admin/InjectAssessment.cshtml</files>
  <action>
    In step-4 panel, build a `card mb-4` + `card-header bg-light` (`bi bi-award me-2` "Sertifikat") mirroring CreateAssessment.cshtml:556-602 STYLING but REWIRE to 3 `form-check` radios bound to `asp-for="CertMode"` (values matching InjectCertMode: None/Auto/Manual; default the "Tanpa Sertifikat" = None checked, OR Auto — match D-06; UI-SPEC labels line 124): "Generate Otomatis (Nomor Resmi)", "Input Nomor Manual", "Tanpa Sertifikat".
    Conditional blocks (toggle via JS on radio change):
      - Auto block `#certAutoBlock`: format preview `<span>Format nomor: </span><code>KPB/xxx/{ROMAN}/{year}</code><span> (tahun mengikuti tanggal ujian).</span>` (display-only string; do NOT call CertNumberHelper here — real allocation = service 393) + ValidUntil `<input type="date" id="CertValidUntil" asp-for="CertValidUntil">` (label "Masa Berlaku Sertifikat") + Permanent `<input type="checkbox" id="CertPermanent" asp-for="CertPermanent">` ("Berlaku selamanya (permanen)") + hint line 126.
      - Manual block `#certManualBlock`: `<input type="text" id="NomorSertifikat" asp-for="ManualCertNumber">` (wajib unik — note in hint; uniqueness enforced server-side by service D-09) + the SAME ValidUntil + Permanent controls (share the #CertValidUntil/#CertPermanent or duplicate with consistent ids — keep single source preferred).
      - Tanpa = None: hide all conditional blocks.
    JS: on radio change, show/hide #certAutoBlock / #certManualBlock per selection (None hides both). On #CertPermanent change, if checked → disable + clear #CertValidUntil (null = permanent, D-10). Set initial state on load matching the default-checked radio.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | findstr /C:"Build succeeded" & cd tests && npx playwright test e2e/inject-assessment-394.spec.ts -g "cert radio" --workers=1 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <acceptance_criteria>
    - Grep InjectAssessment.cshtml: contains `CertMode` (3 radio inputs), `certAutoBlock`, `certManualBlock`, `KPB/xxx/`, `CertValidUntil`, `CertPermanent`, `ManualCertNumber` (or `NomorSertifikat`).
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
    - Playwright (AD-off, --workers=1, MAIN tree): 'cert radio 3-mode toggle' PASS (un-skipped) — Auto/Manual/Tanpa show/hide correct fields.
  </acceptance_criteria>
  <done>Cert radio 3-mode toggles conditional fields correctly; Auto shows preview+ValidUntil+Permanent; Manual shows NomorSertifikat+ValidUntil+Permanent; Tanpa hides all; choice bound to VM.CertMode for service consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form-state → VM (authored questions + cert choice) | Captured client-side; re-validated server-side at commit (393 PreflightValidate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-394-03-scorerange | Tampering | scoreValue / option count via crafted POST | mitigate | Client validation (1-100, MC=1/MA≥2 correct, Essay rubrik) is convenience; service preflight reject-all re-validates at commit (393 D-03). Never trust client. |
| T-394-03-XSS-question | Tampering (Injection) | question/option text rendered into Daftar Soal + confirm summary | mitigate | Render via `.textContent` (not innerHTML); Razor auto-encodes any server round-trip. No `@Html.Raw`. |
| T-394-03-certdup | Tampering / data integrity | manual NomorSertifikat | mitigate | Uniqueness enforced server-side (service preflight + UNIQUE index, 393 D-09); UI hint only. |
| T-394-03-nodbwrite | Tampering | authoring step | mitigate | No CreateQuestion POST / no draft package — all held in form state (D-07). Verified by Playwright (no reload) + 0-migration gate (Plan 04). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- Playwright `e2e/inject-assessment-394.spec.ts -g "authoring|cert radio" --workers=1` (AD-off, MAIN tree) → green.
- `dotnet test --filter Category!=Integration` → no regression.
- Grep: no `CreateQuestion`/`questionImage`/`maxCharacters` in inject authoring; cert 3-mode present.
</verification>

<success_criteria>
INJ-05 + INJ-07 satisfied: HC authors MC/MA/Essay with full field parity to ManagePackages held in client state (no DB write, no CreateQuestion POST); cert radio 3-mode toggles correct conditional fields and binds to VM.CertMode. 0 migration.
</success_criteria>

<output>
After completion, create `.planning/phases/394-page-setup-room-authoring-soal/394-03-SUMMARY.md`
</output>
