---
phase: 394-page-setup-room-authoring-soal
plan: 04
type: execute
wave: 4
depends_on: ["394-03"]
files_modified:
  - Views/Admin/InjectAssessment.cshtml
  - Controllers/InjectAssessmentController.cs
  - ViewModels/InjectAssessmentViewModel.cs
  - HcPortal.Tests/InjectViewModelMapTests.cs
  - tests/e2e/inject-assessment-394.spec.ts
autonomous: true
requirements: [INJ-03, INJ-04, INJ-05, INJ-06, INJ-07]
must_haves:
  truths:
    - "Step 5 (Jawaban) tampil sebagai panel placeholder navigable dengan notice info; dirancang agar Phase 395 menyisipkan input jawaban tanpa refactor scaffold"
    - "Step 6 (Konfirmasi) menampilkan ringkasan Setup/Pekerja/Soal/Sertifikat via .textContent; tombol Inject Assessment hadir (commit di-wire Phase 395)"
    - "POST handler memetakan InjectAssessmentViewModel → InjectRequest dengan benar (scalars/Questions/CertMode/AssessmentType), menerjemahkan UserIds→NIP untuk InjectWorkerSpec, Workers[].Answers kosong di 394"
    - "Tidak ada baris AssessmentSession yang ditulis saat membuka/menavigasi page 394 (D-07, 0 DB write)"
    - "dotnet ef migrations add _verify → 0 model diff (konfirmasi 0 migration); seluruh suite + Playwright hijau"
  artifacts:
    - path: "Controllers/InjectAssessmentController.cs"
      provides: "POST map VM→InjectRequest + UserIds→NIP translation (single _context.Users query); NO InjectBatchAsync call (deferred 395)"
      contains: "InjectRequest"
    - path: "Views/Admin/InjectAssessment.cshtml"
      provides: "Step-5 placeholder panel + Step-6 confirm summary (populateSummary via textContent) + final commit button gated"
      contains: "hourglass-split"
    - path: "HcPortal.Tests/InjectViewModelMapTests.cs"
      provides: "Full xUnit mapping facts (Questions[]/Workers/CertMode/AssessmentType/backdate + UserId→NIP resolution)"
      contains: "InjectRequest"
  key_links:
    - from: "Controllers/InjectAssessmentController.cs (POST)"
      to: "InjectRequest (service contract)"
      via: "VM→InjectRequest mapping + UserIds→NIP translation"
      pattern: "InjectWorkerSpec"
    - from: "Views/Admin/InjectAssessment.cshtml (step-6)"
      to: "confirm summary spans"
      via: "populateSummary() .textContent"
      pattern: "populateSummary"
---

<objective>
Tutup wizard: Step 5 (Jawaban) = panel placeholder navigable (notice `alert-info` + `bi-hourglass-split`) yang dirancang agar Phase 395 menyisipkan input tanpa refactor; Step 6 (Konfirmasi) = ringkasan Setup/Pekerja/Soal/Sertifikat via `.textContent` + tombol commit `btn-success` (commit di-wire 395). Wire POST handler memetakan `InjectAssessmentViewModel`→`InjectRequest` (scalars + Questions + CertMode + AssessmentType), menerjemahkan `UserIds`→`NIP` saat membangun `InjectWorkerSpec` (Pitfall 2), `Workers[].Answers` KOSONG di 394, TANPA memanggil `InjectBatchAsync`. Lengkapi xUnit mapping facts. Jalankan gate fase: full suite + Playwright + `dotnet ef migrations add _verify` → 0 diff.

Purpose: Menyatukan semua langkah (INJ-03..07 end-to-end) + mengamankan seam Phase 395 (step-5 + commit) + membuktikan 0-migration. Ini plan penutup fase.
Output: InjectAssessment.cshtml step 5+6 + populateSummary; controller POST map + UserId→NIP; VM mapping helper; xUnit facts; 0-migration gate.

NOTE file-overlap: meng-extend `InjectAssessment.cshtml` + `InjectAssessmentController.cs` + VM yang sama dengan Plan 02/03 → WAVE 4 (sequential, setelah Plan 03). Plan 395/396/397 meng-extend file-file yang SAMA → seam step-5 + POST commit harus bersih.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/394-page-setup-room-authoring-soal/394-CONTEXT.md
@.planning/phases/394-page-setup-room-authoring-soal/394-RESEARCH.md
@.planning/phases/394-page-setup-room-authoring-soal/394-PATTERNS.md
@.planning/phases/394-page-setup-room-authoring-soal/394-UI-SPEC.md
@.planning/phases/394-page-setup-room-authoring-soal/394-03-SUMMARY.md

<interfaces>
<!-- VM→InjectRequest field map — VERIFIED Models/InjectAssessmentDtos.cs + PATTERNS.md -->
vm.Title→Title; vm.Category→Category; vm.AssessmentType→AssessmentType ("Standard"/"PreTest"/"PostTest", NEVER "Manual");
vm.CompletedAt→CompletedAt (StartedAt/Schedule null→service falls back to CompletedAt);
vm.DurationMinutes→DurationMinutes; vm.PassPercentage→PassPercentage; vm.AllowAnswerReview→AllowAnswerReview;
vm.CertMode→CertMode (InjectCertMode); vm.Questions[]→Questions (InjectQuestionSpec QuestionText/QuestionType/
  ScoreValue/Order/ElemenTeknis/Rubrik/TempId + Options[{OptionText,IsCorrect,TempId}]);
vm.UserIds[] → Workers (InjectWorkerSpec: Nip + ManualCertNumber[only if CertMode==Manual] + CertValidUntil[null=permanent];
  Answers EMPTY in 394).
CertPermanent==true → CertValidUntil = null (D-10). CertValidUntil(DateTime?) → DateOnly? for the spec.

<!-- CRITICAL Pitfall 2 — UserId→NIP translation, VERIFIED -->
picker checkbox value = user.Id (CreateAssessment.cshtml:319). InjectWorkerSpec.Nip keys on ApplicationUser.NIP
  (InjectAssessmentService.cs:347-350 builds usersByNip via .Where(u => u.NIP != null && nips.Contains(u.NIP))).
Controller MUST resolve selected UserIds → each user's NIP via a single _context.Users query before building Workers.
If a selected user has null NIP → that is an authoring error surfaced at commit (395); in 394 mapping, skip/flag null-NIP users.

<!-- Step-5/6 + summary -->
UI-SPEC line 123 step-5 notice: alert-info + bi-hourglass-split "Pengisian jawaban per-pekerja tersedia pada tahap
  berikutnya. Selesaikan Setup, Pekerja, dan Soal terlebih dahulu." (Phase 395 replaces this panel)
UI-SPEC line 117 final CTA "Inject Assessment" + bi-cloud-arrow-up-fill (btn-success btn-lg w-100); present but commit wired 395.
CreateAssessment.cshtml:638-752 confirm summary cards + edit-from-confirm; :1096 populateSummary uses .textContent (XSS-safe).

<!-- POST stub from Plan 01 -->
[HttpPost][Authorize(Roles="Admin, HC")][ValidateAntiForgeryToken] InjectAssessment(InjectAssessmentViewModel vm)
  — Plan 01 made it a stub re-rendering the view. This plan fills the VM→InjectRequest MAPPING + NIP translation,
  but STILL does NOT call _injectService.InjectBatchAsync (commit = Phase 395, D-07). Re-render view with an info notice.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Step-5 placeholder + Step-6 confirm summary + final commit button (gated)</name>
  <read_first>
    - Views/Admin/InjectAssessment.cshtml (step-5/step-6 scaffold from Plan 01; populateSummary stub from Plan 01)
    - Views/Admin/CreateAssessment.cshtml:638-752 (confirm summary + edit-from-confirm), :1096 (populateSummary .textContent)
    - .planning/phases/394-page-setup-room-authoring-soal/394-UI-SPEC.md:117, 123, 141 (step-5 notice, final CTA, confirm contract)
  </read_first>
  <behavior>
    - Test 'step5 placeholder navigable': advance through steps 1-4 (valid data) → step 5 visible with #step5Placeholder notice (bi-hourglass-split text "tahap berikutnya"); Selanjutnya advances to step 6 (validateStep(5) returns true — placeholder).
    - Test 'confirm summary': step 6 shows summary reflecting entered Title, selected worker count, soal count, cert mode (via spans populated by populateSummary); edit-from-confirm button jumps back to a step then returns; #btnInject (btn-success) is present.
  </behavior>
  <files>Views/Admin/InjectAssessment.cshtml</files>
  <action>
    Step-5 panel: body = `<div id="step5Placeholder" class="alert alert-info"><i class="bi bi-hourglass-split me-2"></i>Pengisian jawaban per-pekerja tersedia pada tahap berikutnya. Selesaikan Setup, Pekerja, dan Soal terlebih dahulu.</div>` + the standard prev/next footer (btnPrev5 + btnNext5). Keep `validateStep(5)` returning true (placeholder seam — Phase 395 fills inputs here; design so its answer table slots into #step5Placeholder's parent without touching pills/nav). Optionally gate step-5 "complete" on soal+pekerja done (injQuestions.length>=1 && ≥1 checked) so the seam is logically correct.
    Step-6 panel: confirm summary mirroring CreateAssessment.cshtml:638-752 — summary cards for Setup (Title/Category/Type/CompletedAt/Duration/PassPercentage via `<span id="sum-title">` etc.), Pekerja (count badge), Soal (count + total poin), Sertifikat (mode label). Each summary card has an `edit-from-confirm` `btn-outline-secondary` button with `data-step="N"` to jump back. Final commit `<button type="submit" id="btnInject" class="btn btn-success btn-lg w-100"><i class="bi bi-cloud-arrow-up-fill me-2"></i>Inject Assessment</button>`.
    In `@section Scripts`: fill `populateSummary()` (Plan 01 stub) to set the summary spans via `.textContent` (XSS-safe): title/category/type/date/duration/passPct from the step-1 inputs, worker count from `#userCheckboxContainer .user-checkbox:checked`, soal count + total poin from `injQuestions` (window-exposed by Plan 03), cert mode from the checked CertMode radio label. Wire `.edit-from-confirm` clicks → `returnToConfirm=true; goToStep(data-step)`; provide a `.btnBackToConfirm` path (or rely on Selanjutnya) to return to step 6. Ensure goToStep(6) calls populateSummary() (set in Plan 01). On form submit, ensure injQuestions are serialized (the hidden-fields/JSON emit from Plan 03) BEFORE the POST fires.
    SCOPE: the #btnInject submit triggers the POST handler (Task 2) which maps but does NOT commit in 394 — keep the button present; gate its meaning to "ready" but the actual InjectBatchAsync lands in 395.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | findstr /C:"Build succeeded" & cd tests && npx playwright test e2e/inject-assessment-394.spec.ts -g "step5 placeholder|confirm summary" --workers=1 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <acceptance_criteria>
    - Grep InjectAssessment.cshtml: contains `step5Placeholder`, `bi-hourglass-split`, `id="btnInject"`, `populateSummary`, `edit-from-confirm`, `.textContent` usage in populateSummary.
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
    - Playwright (AD-off, --workers=1, MAIN tree): 'step5 placeholder navigable' + 'confirm summary' PASS (un-skipped).
  </acceptance_criteria>
  <done>Step-5 placeholder navigable (seam for 395); step-6 confirm summary populated via textContent; edit-from-confirm + final commit button present; full 6-step nav works end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST VM→InjectRequest mapping + UserId→NIP translation + xUnit facts (NO commit)</name>
  <read_first>
    - Controllers/InjectAssessmentController.cs (POST stub from Plan 01)
    - Models/InjectAssessmentDtos.cs (InjectRequest/InjectWorkerSpec/InjectQuestionSpec/InjectCertMode — target)
    - Services/InjectAssessmentService.cs:347-350 (NIP keying — confirms Workers must carry NIP)
    - HcPortal.Tests/InjectViewModelMapTests.cs (scaffold from Plan 01)
    - .planning/phases/394-page-setup-room-authoring-soal/394-RESEARCH.md (Pattern 4 field map + Open Q1)
  </read_first>
  <behavior>
    - xUnit Fact 'Maps_scalars': VM with Title/Category/AssessmentType/CompletedAt/Duration/PassPercentage/AllowAnswerReview → InjectRequest carries identical values; AssessmentType is one of Standard/PreTest/PostTest (never "Manual").
    - xUnit Fact 'Maps_questions': VM.Questions (MC w/ 4 options, 1 correct; Essay w/ rubrik) → InjectRequest.Questions with matching QuestionText/QuestionType/ScoreValue/Order/Rubrik + Options[OptionText/IsCorrect/TempId].
    - xUnit Fact 'Maps_cert': CertMode=Manual + ManualCertNumber set + CertPermanent=true → InjectRequest workers carry ManualCertNumber + CertValidUntil null (permanent); CertMode=Auto → ManualCertNumber null; CertMode=None → cert fields cleared.
    - xUnit Fact 'Resolves_UserIds_to_NIP': given a UserId→NIP lookup, selected UserIds produce InjectWorkerSpec with the correct NIP; Answers list empty.
  </behavior>
  <files>Controllers/InjectAssessmentController.cs, ViewModels/InjectAssessmentViewModel.cs, HcPortal.Tests/InjectViewModelMapTests.cs</files>
  <action>
    Add a pure mapping method on the controller (or a static helper class for testability), e.g. `internal static InjectRequest MapToRequest(InjectAssessmentViewModel vm, IReadOnlyDictionary<string,string> userIdToNip)`:
      - Map all scalars per the field map. Build `Questions` from `vm.Questions` (read the serialized shape Plan 03 chose — bound `vm.Questions` list OR deserialize `vm.QuestionsJson`; set `Order = index`, copy TempId/options). Set `CertMode = vm.CertMode`.
      - Build `Workers`: for each `userId` in `vm.UserIds`, look up NIP via `userIdToNip`; create `InjectWorkerSpec { Nip = nip, Answers = new() /* empty in 394 */, ManualCertNumber = vm.CertMode==InjectCertMode.Manual ? vm.ManualCertNumber : null, CertValidUntil = vm.CertPermanent ? null : (vm.CertValidUntil.HasValue ? DateOnly.FromDateTime(vm.CertValidUntil.Value) : null) }`. Skip users with no NIP (or collect to a flagged list).
    In the POST action: build `userIdToNip` via a single query `await _context.Users.Where(u => vm.UserIds.Contains(u.Id)).ToDictionaryAsync(u => u.Id, u => u.NIP ?? "")`. Call `MapToRequest(vm, dict)` to produce the `InjectRequest`. DO NOT call `_injectService.InjectBatchAsync` — re-render the view with an info notice (e.g. TempData) "Setup tersimpan. Lengkapi jawaban pada tahap berikutnya untuk meng-inject." (commit lands Phase 395). Keep `[ValidateAntiForgeryToken]`. Re-populate ViewBag feed before returning the view so the page renders.
    If Plan 03 chose `QuestionsJson`, add `[BindNever]`-free `string? QuestionsJson` already on the VM (Plan 01 declared it) and deserialize in MapToRequest; if Plan 03 used bound `Questions[i]...` indexed fields, use `vm.Questions` directly. Match whichever Plan 03 implemented (read 394-03-SUMMARY).
    Fill `HcPortal.Tests/InjectViewModelMapTests.cs`: implement the 4 facts above as pure unit tests (no DB) by calling `MapToRequest` with a hand-built `userIdToNip` dictionary. `[Trait("Category","Unit")]` so they run under `--filter Category!=Integration`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | findstr /C:"Build succeeded" & dotnet test --filter "FullyQualifiedName~InjectViewModelMap" 2>&1 | findstr /C:"Passed" /C:"Failed"</automated>
  </verify>
  <acceptance_criteria>
    - Controller contains `MapToRequest`, builds `userIdToNip` via `_context.Users`, creates `InjectWorkerSpec` with resolved `Nip`; grep confirms NO `InjectBatchAsync` call.
    - `InjectViewModelMapTests.cs` has the 4 named facts; `dotnet test --filter "FullyQualifiedName~InjectViewModelMap"` → all Passed.
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
  </acceptance_criteria>
  <done>POST maps VM→InjectRequest correctly with UserId→NIP translation, empty Answers, cert fields per mode; mapping unit-tested; commit deliberately deferred to Phase 395.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full Playwright + suite + 0-migration verify + no-DB-write check</name>
  <read_first>
    - .planning/phases/394-page-setup-room-authoring-soal/394-RESEARCH.md (Sampling Rate → Phase gate; Validation Architecture)
    - tests/e2e/inject-assessment-394.spec.ts (all tests should now be un-skipped)
  </read_first>
  <files>tests/e2e/inject-assessment-394.spec.ts</files>
  <action>
    Add/finalize the D-07 no-DB-write Playwright assertion in the 'D-07 step5 placeholder + no DB write' describe: a test that records `SELECT COUNT(*) FROM AssessmentSessions` (via the existing tests/helpers DB utility, or a known-stable count) BEFORE opening the page, navigates the full wizard (steps 1-6 with valid setup/picker/soal/cert) WITHOUT clicking the final commit (since commit is 395), and asserts the AssessmentSessions count is UNCHANGED — proving 394 writes nothing. If a DB helper is unavailable, assert via the absence of any new session at /CMP/Records for a test worker after navigation. Ensure all previously-skipped tests are un-skipped and passing.
    Run the FULL phase gate (this is verification, the spec edit is the only file change):
      1. `dotnet build HcPortal.csproj` → 0 error (MAIN tree).
      2. `dotnet test --filter Category!=Integration` → no regression (baseline 347) + new Inject facts green.
      3. App from MAIN tree on :5277 AD-off → `cd tests && npx playwright test e2e/inject-assessment-394.spec.ts --workers=1` → ALL green (RBAC, nav, cek-judul, backdate, picker, authoring, cert radio, step5, confirm, no-DB-write).
      4. `dotnet ef migrations add _verify_394` → confirm 0 model diff (empty Up/Down) → then `dotnet ef migrations remove` to discard (do NOT commit the verify migration). Record the 0-diff result in the SUMMARY.
    If any check fails, fix within the touched files (view/controller/VM/spec) — do NOT introduce schema changes.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/inject-assessment-394.spec.ts --workers=1 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/inject-assessment-394.spec.ts --workers=1` (AD-off, MAIN tree): ALL tests pass, 0 skipped that should be implemented; the no-DB-write test PASSES (session count unchanged after navigating without commit).
    - `dotnet test --filter Category!=Integration` → no regression + Inject facts green.
    - `dotnet ef migrations add _verify_394` produces an EMPTY migration (0 model diff); migration removed/not committed (note in SUMMARY: "0 migration confirmed").
    - `dotnet build HcPortal.csproj` → 0 error.
  </acceptance_criteria>
  <done>Full phase gate green: all INJ-03..07 Playwright behaviors pass, suite no-regression, 0-migration confirmed, page writes nothing to DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /Admin/InjectAssessment | Full wizard form crosses; CSRF + RBAC + server re-validation apply |
| Controller VM → InjectRequest → (future) service | Mapping boundary; UserId→NIP resolution + cert-field gating happen here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-394-04-RBAC | Elevation of Privilege | POST InjectAssessment | mitigate | `[Authorize(Roles = "Admin, HC")]` on POST (carried from Plan 01); verified by RBAC Playwright. |
| T-394-04-CSRF | Tampering | POST InjectAssessment | mitigate | `[ValidateAntiForgeryToken]` + `@Html.AntiForgeryToken()` in the single form. |
| T-394-04-NIPforge | Tampering / IDOR | UserIds→NIP mapping | mitigate | NIP resolved ONLY from `_context.Users` for the selected ids; unknown/foreign ids resolve to no NIP and are skipped/flagged; service preflight reject-all is the final gate (393). No arbitrary NIP accepted from client. |
| T-394-04-future-backdate | Tampering | CompletedAt | mitigate | Mapping passes CompletedAt straight to InjectRequest; server preflight (`CompletedAt.Date > today` reject) at commit (393) is authoritative; client `max=today` is UX. |
| T-394-04-XSS-summary | Tampering (Injection) | confirm summary | mitigate | populateSummary uses `.textContent`; no `@Html.Raw` on user strings. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test --filter Category!=Integration` → no regression + Inject mapping facts green.
- Playwright `e2e/inject-assessment-394.spec.ts --workers=1` (AD-off, MAIN tree) → ALL green incl. no-DB-write.
- `dotnet ef migrations add _verify_394` → 0 model diff (then removed). 0 migration confirmed.
- Grep: controller maps VM→InjectRequest + UserId→NIP, NO InjectBatchAsync; view has step-5 placeholder + confirm summary.
</verification>

<success_criteria>
Phase 394 complete: INJ-03..07 all satisfied end-to-end (RBAC page, setup room, authoring, picker, cert toggle), wizard 6 steps navigable, POST maps to InjectRequest with UserId→NIP (Answers empty, commit deferred to 395), step-5 + commit seam clean for Phase 395, 0 DB write, 0 migration confirmed, full suite + Playwright green.
</success_criteria>

<output>
After completion, create `.planning/phases/394-page-setup-room-authoring-soal/394-04-SUMMARY.md`
</output>
