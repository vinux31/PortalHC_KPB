---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 02
type: execute
wave: 2
depends_on: ["398.1-01"]
files_modified:
  - Services/InjectAssessmentService.cs
  - Controllers/InjectAssessmentController.cs
  - HcPortal.Tests/UnlinkInjectGroupTests.cs
autonomous: true
requirements: ["tech-debt"]
user_setup: []

must_haves:
  truths:
    - "UnlinkInjectGroupAsync hanya melepas sesi inject milik batch yang di-unlink (tidak menyentuh batch lain yang se-LinkedGroupId di Kasus A) — di-narrow bila reachable, atau drop dengan bukti unreachable"
    - "Pemilihan sibling representatif (GroupBy g.First) deterministik via OrderBy(CreatedAt).ThenBy(Id) di kedua site (commit :132 + preview :685) — preview==commit terjaga"
    - "Stray TempData di UnlinkInjectGroup (JSON action) dihapus — tidak ada flash banner bocor ke view berikutnya"
    - "dotnet build 0 error; fast suite GREEN; integration UnlinkInjectGroupTests GREEN"
    - "Jalur grading online TIDAK dimutasi; audit ActionType <= 50 char; git diff Migrations/ Data/ kosong"
  artifacts:
    - path: "Services/InjectAssessmentService.cs"
      provides: "g.First() deterministik (OrderBy CreatedAt) di :132 + :685; unlink scope di-narrow (jika real)"
      contains: "OrderBy"
    - path: "Controllers/InjectAssessmentController.cs"
      provides: "UnlinkInjectGroup tanpa stray TempData (Json-only)"
      contains: "return Json"
  key_links:
    - from: "Services/InjectAssessmentService.cs PreviewPairingAsync sibling pick"
      to: "Services/InjectAssessmentService.cs InjectBatchAsync sibling pick"
      via: "OrderBy byte-identik (preview==commit)"
      pattern: "OrderBy\\(.*CreatedAt\\)"
---

<objective>
Cluster B — bersihkan 3 temuan code-review Phase 397 di pasangan file Link Pre/Post: `Services/InjectAssessmentService.cs` + `Controllers/InjectAssessmentController.cs`.

Tiga item: (1) 397 WR-01 unlink cross-batch correctness [MED, verify-first -> likely narrow per D-04], (2) 397 WR-02 `g.First()` non-deterministic representative [LOW-MED, fix OrderBy], (3) 397 WR-03 stray TempData di JSON action [LOW, hapus].

Purpose: tutup tech-debt v32.2; jaga invariant cross-grouping LinkedGroupId (spec section 13), preview==commit, dan jalur online tak-tersentuh. Setiap item diverifikasi real DULU (D-01).

Output: service + controller ter-update untuk item real + SUMMARY drop terdokumentasi untuk item unreachable. 0 migration. v32.2 tetap 13/13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-CONTEXT.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-PATTERNS.md
@docs/superpowers/specs/2026-06-17-inject-assessment-manual-design.md
@CLAUDE.md

<interfaces>
<!-- Kontrak aktual yang disentuh. JANGAN eksplorasi ulang. -->

Services/InjectAssessmentService.cs:130-132 (commit sibling map — WR-02 non-determinism):
```csharp
siblingByUserId = siblingQuery
    .GroupBy(s => s.UserId)
    .ToDictionary(g => g.Key, g => g.First());   // SQL tak ber-order
```

Services/InjectAssessmentService.cs:683-685 (PreviewPairingAsync sibling — WR-02, harus SAMA dgn commit):
```csharp
var siblingByUser = targetSessions
    .GroupBy(t => t.UserId)
    .ToDictionary(g => g.Key, g => g.First());
```

Analog deterministik (repo convention = oldest CreatedAt + ThenBy(Id) untuk tie-break batch):
- Controllers/InjectAssessmentController.cs:209,224 -> `var rep = g.OrderBy(x => x.CreatedAt).First();`
- AssessmentAdminController.cs:181,2922 -> `var rep = g.OrderBy(a => a.CreatedAt).First();`

Services/InjectAssessmentService.cs:742-744 (unlink step-1 — WR-01 cross-batch):
```csharp
var injectSessions = await _context.AssessmentSessions
    .Where(s => s.LinkedGroupId == injectGroupId && s.IsManualEntry)   // SEMUA manual di grup -> cross-batch
    .ToListAsync();
```
Services/InjectAssessmentService.cs:780-782 (unlink step-4 remaining):
```csharp
var remaining = await _context.AssessmentSessions
    .Where(s => s.LinkedGroupId == injectGroupId && !injectIds.Contains(s.Id))
    .ToListAsync();
```

Kasus A vs B (ResolveLinkContextAsync :615-650): Kasus A adopt -> `LinkedGroupId = rep.LinkedGroupId` (BISA dibagi banyak batch); Kasus B standalone -> `groupId = rep.Id` (batch-unique).
Commit batch-identity tersedia: `result.SuccessSessionIds` (:343) = id sesi inject batch ini.
UI unlink surface: View :52-60 `data-inject-group-id="@TempData["InjectedGroupId"]"` di-set controller :98 = `injResult.LinkedGroupId.Value` (groupId batch terakhir; di Kasus A = adopted group yang BISA dibagi).

Controllers/InjectAssessmentController.cs:265-273 (WR-03 stray TempData di JSON action):
```csharp
var res = await _injectService.UnlinkInjectGroupAsync(injectGroupId, actorUserId, actorName);
if (res.Success) TempData["Success"] = res.Message ?? "Tautan dilepas.";   // stray (Json action, tak dikonsumsi)
else TempData["Error"] = res.Message ?? "Gagal melepas tautan.";           // stray
return Json(new { ok = res.Success, message = res.Message ?? (...) });
```
Audit shape preserve: `ActionType = "LinkPrePostUndo"` (15 char <= 50); skor/jawaban/status online NEVER touched (T-397-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 397 WR-02 g.First deterministik (OrderBy) + WR-03 stray TempData remove</name>
  <files>Services/InjectAssessmentService.cs, Controllers/InjectAssessmentController.cs</files>
  <read_first>
    - Services/InjectAssessmentService.cs:120-140 (commit sibling map, file dimodifikasi)
    - Services/InjectAssessmentService.cs:675-700 (PreviewPairingAsync sibling, file dimodifikasi)
    - Controllers/InjectAssessmentController.cs:255-273 (UnlinkInjectGroup JSON action, file dimodifikasi)
    - Controllers/InjectAssessmentController.cs:209,224 (analog OrderBy(CreatedAt).First())
    - Controllers/InjectAssessmentController.cs:164,236,254,381 (analog JSON action TANPA TempData)
    - HcPortal.Tests/SiblingDeterminismTests.cs (analog determinism test)
  </read_first>
  <action>
    **D-01 VERIFY (WR-02):** Konfirmasi kedua site `g.First()` (:132 commit, :685 preview) beroperasi pada koleksi tanpa order eksplisit (`GroupBy(...).ToDictionary(g => g.Key, g => g.First())`). REAL — pemilihan sibling tak-stabil, dan karena preview & commit pakai `g.First()` yang sama-tak-berorder, keduanya bisa pilih sibling berbeda -> ancam preview==commit. FIX.

    **FIX WR-02** — tambah OrderBy deterministik di KEDUA site, byte-identik (preview==commit). CreatedAt = `DateTime.UtcNow` bisa tabrakan dalam 1 batch -> ThenBy(Id) pemecah-seri.
    Site commit `Services/InjectAssessmentService.cs:130-132`:
    ```csharp
    siblingByUserId = siblingQuery
        .GroupBy(s => s.UserId)
        .ToDictionary(g => g.Key, g => g.OrderBy(s => s.CreatedAt).ThenBy(s => s.Id).First());
    ```
    Site preview `Services/InjectAssessmentService.cs:683-685`:
    ```csharp
    var siblingByUser = targetSessions
        .GroupBy(t => t.UserId)
        .ToDictionary(g => g.Key, g => g.OrderBy(t => t.CreatedAt).ThenBy(t => t.Id).First());
    ```
    Kedua ekspresi HARUS identik bentuk (jaga preview==commit). JANGAN ubah filter `siblingQuery`/`targetSessions`.

    **D-01 VERIFY (WR-03):** Konfirmasi `UnlinkInjectGroup` action `return Json(...)` dan TIDAK ada redirect/view setelah set TempData -> TempData[:270-271] tak dikonsumsi (bocor ke view berikutnya). REAL. FIX.

    **FIX WR-03** — hapus 2 baris `TempData[...]` di `Controllers/InjectAssessmentController.cs:270-271`. JSON `message` sudah membawa notice ke JS toast. JANGAN ubah `return Json(...)` (:272). Verifikasi tak ada konsumen TempData["Success"]/["Error"] yang bergantung pasca-unlink (tidak ada — JS handle in-place; grep cepat untuk konfirmasi).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj</automated>
  </verify>
  <acceptance_criteria>
    - WR-02 FIX: grep "OrderBy(s => s.CreatedAt).ThenBy(s => s.Id).First()" di Services/InjectAssessmentService.cs muncul (commit site); grep "OrderBy(t => t.CreatedAt).ThenBy(t => t.Id).First()" muncul (preview site); tidak ada lagi bare `g => g.First()` di 2 site sibling tsb.
    - WR-03 FIX: grep "TempData[\"Success\"] = res.Message" = 0 hit; grep "TempData[\"Error\"] = res.Message" = 0 hit (di action UnlinkInjectGroup); "return Json(new { ok = res.Success" tetap ada.
    - dotnet build HcPortal.csproj -> "Build succeeded" / 0 error.
  </acceptance_criteria>
  <done>g.First() di kedua site deterministik (OrderBy CreatedAt ThenBy Id, byte-identik); stray TempData dihapus dari JSON action; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: 397 WR-01 unlink cross-batch (verify->narrow per D-04, atau drop) + integration test</name>
  <files>Services/InjectAssessmentService.cs, HcPortal.Tests/UnlinkInjectGroupTests.cs</files>
  <read_first>
    - Services/InjectAssessmentService.cs:736-818 (UnlinkInjectGroupAsync penuh, file dimodifikasi)
    - Services/InjectAssessmentService.cs:615-650 (ResolveLinkContextAsync Kasus A/B scoping reference)
    - Services/InjectAssessmentService.cs:340-410 (commit: SuccessSessionIds + LinkedGroupId write — batch identity)
    - Controllers/InjectAssessmentController.cs:90-105 (TempData InjectedGroupId set pasca-commit)
    - Views/Admin/InjectAssessment.cshtml:50-60 (unlink surface — groupId yang dilewatkan JS)
    - HcPortal.Tests/UnlinkInjectGroupTests.cs (file dimodifikasi — tambah cross-batch test)
  </read_first>
  <action>
    **D-01/D-04 VERIFY (WR-01 cross-batch) — reachability analysis WAJIB sebelum ubah:**
    Telusuri jalur: di **Kasus A (adopt)**, `ResolveLinkContextAsync:634-637` mengembalikan `rep.LinkedGroupId` -> DUA batch inject berbeda bisa adopt `LinkedGroupId` yang SAMA. UI unlink (View:52-60) memakai `TempData["InjectedGroupId"]` = `injResult.LinkedGroupId.Value` (controller:98) = group yang BISA dibagi di Kasus A. `UnlinkInjectGroupAsync:742-744` memilih SEMUA `IsManualEntry && LinkedGroupId==injectGroupId` -> akan melepas sesi inject batch LAIN juga, dan step-4 `remaining` (:780-784) bisa null-kan stiker online yang masih dipakai batch sibling.
    **Disposisi:** karena UI BISA menyodorkan adopted-group (Kasus A) ke unlink, jalur cross-batch **reachable** -> **NARROW guard** (D-04: scope ke grup inject sendiri). JANGAN rewrite luas.
    Jika telusuran membuktikan UI HANYA pernah meng-unlink standalone Kasus B (di mana `LinkedGroupId == rep.Id` batch-unique) dan Kasus A tak pernah sampai ke tombol unlink -> **DROP** dengan bukti file:line (trace TempData["InjectedGroupId"] -> JS -> action) di SUMMARY, jangan ubah service.

    **NARROW FIX (jika reachable — default):** batasi scope `UnlinkInjectGroupAsync` ke sesi inject batch yang di-unlink, BUKAN semua manual se-grup. Pendekatan minimal-risiko (signature `(int injectGroupId, ...)` tetap):
    - Step-4 `remaining` revert (:780-791): SEBELUM null-kan stiker online, cek `AnyAsync` apakah masih ada sesi inject LAIN (batch sibling) ber-`LinkedGroupId == injectGroupId` setelah step-3. Bila masih ada -> JANGAN revert stiker online (masih dipakai sibling). Hanya revert bila grup benar-benar kosong dari inject.
    - Bila perlu lebih ketat dan discriminator batch tersedia (mis. CreatedAt window / pasangan LinkedSessionId konsisten satu commit), batasi step-1 ke sesi inject batch tsb saja. Pilih variant yang andal di signature saat ini.
    Pilih variant yang: (a) tidak menyentuh skor/jawaban/status online (T-397-04), (b) menjaga audit `"LinkPrePostUndo"` <= 50 char, (c) bidirectional revert (D-12) tetap untuk batch sendiri. Dokumentasikan variant yang dipilih + alasan di SUMMARY.

    **INTEGRATION TEST** — tambah ke `HcPortal.Tests/UnlinkInjectGroupTests.cs`: skenario 2 batch inject adopt group sama (Kasus A) -> unlink batch-1 -> assert sesi inject batch-2 + stiker online-nya UTUH (LinkedGroupId/LinkedSessionId batch-2 tidak ter-null). Jika di-drop, ganti dengan test yang membuktikan unreachable (mis. UI hanya expose Kasus B) + catat di SUMMARY. Pakai fixture disposable existing di file (HcPortalDB_Test_{guid}); jangan sentuh HcPortalDB_Dev.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~UnlinkInjectGroup"</automated>
  </verify>
  <acceptance_criteria>
    - NARROW branch (default, reachable): SUMMARY mendokumentasikan variant narrow yang dipilih + alasan; dotnet test --filter "FullyQualifiedName~UnlinkInjectGroup" -> cross-batch test baru PASS (batch sibling utuh); grep "\"LinkPrePostUndo\"" di Services/InjectAssessmentService.cs masih ada (<= 50 char).
    - DROP branch (jika unreachable terbukti): SUMMARY berisi "397 WR-01 DROPPED: Kasus A adopted-group tak reachable dari UI unlink (trace TempData InjectedGroupId->JS->action), Kasus B batch-unique" + bukti file:line; service TIDAK diubah.
    - Jalur online tak-tersentuh: tidak ada penulisan ke kolom Score/Status/EssayScore di UnlinkInjectGroupAsync (grep konfirmasi hanya LinkedGroupId/LinkedSessionId yang dimutasi).
  </acceptance_criteria>
  <done>WR-01 di-narrow ke batch sendiri (atau di-drop dengan bukti unreachable); cross-batch integration test PASS; online untouched; audit shape terjaga.</done>
</task>

<task type="auto">
  <name>Task 3: Regresi penuh Cluster B + 0-migration gate</name>
  <files>HcPortal.Tests/UnlinkInjectGroupTests.cs</files>
  <read_first>
    - HcPortal.Tests/UnlinkInjectGroupTests.cs (suite link)
    - .planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-CONTEXT.md (D-05 kedalaman verifikasi)
  </read_first>
  <action>
    **D-05 verifikasi:** Cluster B = backend logic murni (service + controller JSON) -> xUnit/integration cukup, TIDAK perlu Playwright (tak ada perubahan view/JS di cluster ini).
    (1) Jalankan fast suite `dotnet test --filter "Category!=Integration"` -> GREEN, no regresi (baseline ~390).
    (2) Jalankan integration link suite yang tersentuh: `--filter "FullyQualifiedName~InjectLink|FullyQualifiedName~PreviewPairing|FullyQualifiedName~CrossGrouping|FullyQualifiedName~UnlinkInjectGroup"` (real SQLEXPRESS; HcPortalDB_Dev jangan disentuh — pakai fixture disposable existing). Cross-grouping section 13 + preview==commit HARUS tetap hijau (membuktikan OrderBy fix tidak memecah pairing).
    (3) **0-MIGRATION GATE:** `git diff --stat -- Migrations/ Data/` -> KOSONG. Catat di SUMMARY.
    Konsolidasikan disposisi 3 item Cluster B (WR-01 narrow/drop, WR-02 fix, WR-03 fix) di SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - dotnet test --filter "Category!=Integration" -> "Passed!" / failed: 0 (baseline ~390, no regresi).
    - Integration link suite (InjectLink/PreviewPairing/CrossGrouping/UnlinkInjectGroup) -> failed: 0; cross-grouping section 13 + preview==commit hijau.
    - git diff --stat -- Migrations/ Data/ -> output KOSONG. SUMMARY catat "0 migration: git diff Migrations/ Data/ kosong".
    - SUMMARY menyimpulkan disposisi 3 item Cluster B (WR-01 narrow/drop+bukti, WR-02 fix, WR-03 fix).
  </acceptance_criteria>
  <done>Fast suite + integration link suite GREEN; cross-grouping section 13 & preview==commit terjaga; 0-migration gate kosong; SUMMARY disposisi 3 item.</done>
</task>

</tasks>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- `dotnet test --filter "Category!=Integration"` GREEN (baseline ~390).
- Integration link suite (InjectLink/PreviewPairing/CrossGrouping/UnlinkInjectGroup) GREEN; cross-grouping section 13 + preview==commit terjaga.
- `git diff --stat -- Migrations/ Data/` KOSONG.
- Tiap item punya disposisi terdokumentasi di SUMMARY (fix dengan grep-evidence ATAU drop dengan file:line evidence).
</verification>

<success_criteria>
- WR-01 unlink cross-batch: di-narrow ke batch sendiri (reachable) ATAU drop dengan bukti unreachable; jalur online tak-tersentuh.
- WR-02 g.First: deterministik OrderBy(CreatedAt).ThenBy(Id) di kedua site, byte-identik (preview==commit).
- WR-03 stray TempData: dihapus dari JSON action.
- 0 migration; audit ActionType <= 50 char; jalur grading online tidak dimutasi.
</success_criteria>

<output>
After completion, create `.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-02-SUMMARY.md` mencatat disposisi tiap item (fix/drop + bukti), hasil suite + integration, dan 0-migration gate.
</output>
