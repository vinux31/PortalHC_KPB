---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 02
wave: 2
status: complete
requirements-completed: ["tech-debt"]
migration: false
commits:
  - "58c72e1c fix(398.1-02): WR-02 deterministic sibling pick + WR-03 stray TempData (Task 1)"
  - "8e72f10f test(398.1-02): WR-01 cross-batch behavior-lock; WR-01 code DROP (Task 2)"
---

# Plan 398.1-02 SUMMARY — Cluster B (Phase 397 fixes)

Bersihkan 3 temuan code-review Phase 397 di `Services/InjectAssessmentService.cs` + `Controllers/InjectAssessmentController.cs`. Verify-first (D-01).

## Disposisi item (3)

| Item | Sev | Disposisi | Bukti |
|------|-----|-----------|-------|
| **397 WR-02** `g.First()` non-deterministic | LOW-MED | **FIX** | Sibling-by-UserId map pakai `GroupBy().First()` tanpa order di 2 site: commit `:132` + preview `:685`. Pick tak-stabil ancam preview==commit (preview `DateWarn` `:697` baca `CompletedAt` sibling terpilih). Tambah `OrderBy(CreatedAt).ThenBy(Id).First()` di KEDUA site (byte-identik; repo convention oldest-CreatedAt). Preview projection diperluas (+`CreatedAt`,`Id`) agar OrderBy compile (deviasi perlu). |
| **397 WR-03** stray TempData | LOW | **FIX** | `UnlinkInjectGroup` action `return Json(...)` (fetch+toast JS) tapi set `TempData[Success/Error]` (`:270-271`) → bocor flash banner ke view berikutnya. Dihapus; notice tetap dibawa lewat JSON `message`. |
| **397 WR-01** unlink cross-batch | MED | **DROP (verified — narrow infeasible in-scope)** | Lihat analisis di bawah. Service code TAK diubah; cross-batch behavior-lock test ditambah. |

## WR-01 — analisis reachability + alasan DROP (D-01/D-04)

- `UnlinkInjectGroupAsync(int injectGroupId, ...)` menerima HANYA group id; step-1 (`:746-748`) load SEMUA `IsManualEntry && LinkedGroupId==injectGroupId` — **tak ada batch discriminator** di entity (`AssessmentSession` tak punya batch id; `CreatedAt` fragile).
- Narrow yang disarankan planner (step-4 `AnyAsync` cek sibling inject tersisa) = **no-op**: step-3 sudah null-kan SEMUA inject di grup → tak ada yang tersisa untuk dicek.
- Per-batch scoping sejati butuh perubahan **signature + UI + commit batch-marker** = **feature scope, di luar tech-debt**.
- UI hanya expose unlink **level-grup** (`Views/Admin/InjectAssessment.cshtml:52-60` `data-inject-group-id`=`TempData["InjectedGroupId"]`=seluruh grup) → intent per-batch **tak bisa diekspresikan** user.
- Step-4 online-sticker revert **sudah ter-guard benar**: `remaining.Select(AssessmentType).Distinct().Count() <= 1` → skip saat grup multi-type (pairing online Pre+Post asli terlindungi); single-type = cleanup Kasus B yang memang diinginkan.
- **T-397-04 utuh**: unlink TIDAK PERNAH menulis `Score`/`Status`/`EssayScore`/jawaban — hanya `LinkedGroupId`/`LinkedSessionId`, atomic dalam transaction.

→ Disposisi: **DROP code change** (group-wide revert = perilaku by-design konsisten satu-satunya afordans UI), + **behavior-lock test** mengunci invariant keselamatan lintas-batch.

## Verifikasi (D-05)

- **dotnet build** → 0 error.
- **Fast suite** (`Category!=Integration`) → **391/391 GREEN** (no regression).
- **Integration link suite** (`InjectLink|PreviewPairing|CrossGrouping|UnlinkInjectGroup`, SQLEXPRESS) → **15/15 GREEN**. Cross-grouping §13 + preview==commit **terjaga** setelah OrderBy fix (membuktikan sibling-pick deterministik tak memecah pairing).
- **UnlinkInjectGroup** suite → **4/4** (3 existing + 1 cross-batch baru: group-wide revert by-design + online Score/Status untouched + atomic).
- **0-migration gate**: `git diff Migrations/ Data/` (58c72e1c~1..HEAD) → **KOSONG**.
- **D-05**: Cluster B = backend logic murni (service + controller JSON), tak ada perubahan view/JS → xUnit/integration cukup, TIDAK perlu Playwright.

## Invariant intact

preview==commit (OrderBy byte-identik 2 site; §13 + PreviewPairing green) · jalur grading online TAK dimutasi · audit `LinkPrePostUndo` (15 char ≤ 50) terjaga · 0 migration.

## Self-Check: PASSED
3 item ber-disposisi terdokumentasi (2 FIX + 1 DROP-with-evidence + behavior-lock test). Suite green. 0 migration.
