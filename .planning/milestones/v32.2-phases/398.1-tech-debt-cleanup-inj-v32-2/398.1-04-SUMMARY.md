---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 04
wave: 4
status: complete
requirements-completed: ["tech-debt"]
migration: false
commits:
  - "22d78106 test(398.1-04): 999.12 de-tautologize 391 regression tests (a)/(b)/(d) (Task 1)"
  - "b940fd3a test(398.1-04): 999.13 fix flaky essay helper — rely on product flush (Task 2)"
---

# Plan 398.1-04 SUMMARY — Cluster D (test-only: 999.12 + 999.13)

Bersihkan 2 tech-debt kualitas-test TANPA menyentuh kode produk. Verify-first (D-01).

## Disposisi (2)

| Item | Disposisi | Bukti |
|------|-----------|-------|
| **999.12** 391 regression test tautological | **FIX** | (a)/(b)/(d) seed sesi dgn `DeriveReadyStatus(...)` output lalu assert == output itu (assert seed). Rewrite jadi before/after: seed status warisan-SALAH (`InProgress`) → jalankan replica decision terpisah → assert hasil BERBEDA (Open/Upcoming) + sesi protected (InProgress/Completed sibling) tetap. Mirror gold-standard (c). Test (c) tak disentuh. |
| **999.13** e2e essay helper flaky | **FIX** | `fillEssayAnswer` re-implement save via direct `hub.invoke('SaveTextAnswer')` pakai qId parse `qcard_{id}` yang bisa ≠ DOM `ta.dataset.questionId` di ujian ter-shuffle → save salah/tak-persist (akar flaky). Rewrite mirror essay-flush-385 (PROVEN): wait Connected → set value via `evaluate` (bukan `page.fill`) + dispatch input → inputValue guard → ANDALKAN product `flushEssay()` (baca DOM benar). Kode produk UTUH. |

## ⚠ Catatan verify-first penting (D-01)

Disposisi 999.13 di-REVISI saat eksekusi via verifikasi runtime:
- Percobaan-1 (rely flush + `page.fill`) → Flow K K4 GAGAL ("Belum dijawab").
- Percobaan-2 (direct invoke + wait-Connected) → MASIH GAGAL → membuktikan direct invoke BUKAN solusi (qId mismatch shuffled).
- Percobaan-3 (mirror essay-flush-385: `evaluate` + product flush) → **PASS**. Root cause sejati: parsed-qId ≠ DOM-dataset-qId; product flush baca DOM live → benar.

## Verifikasi (D-05)

- **dotnet build** → 0 error.
- **Fast suite** (`Category!=Integration`) → **391/391 GREEN**; `FlexibleParticipantAdd` 4/4 (non-tautologis).
- **e2e (live @5277 AD-off, DB snapshot→RESTORE per Seed Workflow):**
  - `essay-flush-385` → **3/3** (product flush path intact).
  - `exam-taking` **Flow K** → **7/7** (K6 DB assert Score aggregated NOT 0 — full essay lifecycle via helper).
  - `exam-types` **FLOW L** → **7/7** (essay full cycle + HC grading).
- **Kode produk UTUH**: `git diff --name-only -- Views/ Services/ Controllers/ Hubs/` = KOSONG.
- **0-migration gate**: `git diff Migrations/ Data/` = KOSONG.
- **grep** `hub.invoke('SaveTextAnswer'` di helper = 0 (tak ada re-implement save).

---

# RINGKASAN STATUS PHASE 398.1 (4 cluster) — untuk re-audit milestone v32.2

| Cluster | Item | Disposisi | Bukti ringkas |
|---------|------|-----------|---------------|
| A (396) | WR-01 dup-NIP | **FIX** | GroupBy deterministik (NIP tak unique-indexed); integration test PASS |
| A | WR-02 essay decimal | **DROP** | pipeline int-only end-to-end (3 file:line); Math.Round = divergensi (violate D-03) |
| A | WR-03 download race | **FIX** | debounce guard+disable+timeout; Playwright PASS |
| A | LBL-02 Legenda | **FIX** | `QuestionTypeLabels.Short`; ClosedXML unit PASS |
| B (397) | WR-01 unlink cross-batch | **DROP** | narrow infeasible in-signature (no batch discriminator); UI group-level only; T-397-04 utuh; behavior-lock test |
| B | WR-02 g.First non-determ | **FIX** | `OrderBy(CreatedAt).ThenBy(Id)` 2 site byte-identik; link suite 15/15 (§13+preview==commit intact) |
| B | WR-03 stray TempData | **FIX** | hapus dari JSON action |
| C (999.11) | PendingGrading edit-guard | **FIX** | hard-block 2 cabang + konstanta + carve-out `!hasAddition`; edit-guard 3/3 + 391 4/4 |
| D | 999.12 tautological test | **FIX** | de-tautologize (a)/(b)/(d) before/after |
| D | 999.13 essay helper flaky | **FIX** | rely product flush; Flow K 7/7 + FLOW L 7/7 |

**Total: 8 FIX + 2 DROP (terdokumentasi bukti).** 0 migration sepanjang phase. v32.2 tetap 13/13 REQ (tak nambah REQ). Jalur grading online & preview==commit tak tersentuh. Semua DROP confirmed via D-01 verify-first (bukan skip).

## Self-Check: PASSED
999.12 + 999.13 FIX; kode produk utuh; suite 391/0 + e2e essay 17/17 (385 3 + K 7 + L 7); 0 migration + test-only.
