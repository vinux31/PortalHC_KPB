---
phase: 399-foundation-junction-userunits-primary-mirror-multi-select-ui-display
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - Models/ManageUserViewModel.cs
  - Controllers/WorkerController.cs
  - HcPortal.Tests/UserUnitsWriteThroughTests.cs
  - HcPortal.Tests/PrimaryMirrorTests.cs
  - HcPortal.Tests/UserUnitsAuditDiffTests.cs
  - HcPortal.Tests/ImportMultiUnitParseTests.cs
  - HcPortal.Tests/UnitInSectionValidationTests.cs
  - HcPortal.Tests/RemoveUnitGuardTests.cs
autonomous: true
requirements: [MU-01, MU-02, MU-04, MU-05, MU-07]
must_haves:
  truths:
    - "CreateWorker/EditWorker/Import menulis baris UserUnits via SyncUserUnitsAsync (tidak set user.Unit langsung)"
    - "ApplicationUser.Unit selalu == baris UserUnits.IsPrimary setelah setiap write (mirror)"
    - "Menghapus primary mem-promote unit lain; mengosongkan semua unit set Unit=null + 0 baris IsPrimary"
    - "Setiap junction-write memvalidasi Unit ∈ unit-Bagian; unit asing ditolak"
    - "Import parse pipe 'UnitA|UnitB' (first=primary, dedup); template 1-unit tetap valid"
    - "Hapus unit dgn PTA aktif → HARD-BLOCK; dgn CoachCoacheeMapping aktif (no PTA) → confirm→auto-deactivate (1 tx)"
    - "Audit-log mencatat set-diff (added/removed/primary-changed), bukan if user.Unit != model.Unit"
    - "Export Excel kolom unit = semua unit primary-first comma-join"
  artifacts:
    - path: "Controllers/WorkerController.cs"
      provides: "SyncUserUnitsAsync helper + Create/Edit/Import/Export wiring + MU-07 guard"
      contains: "SyncUserUnitsAsync"
    - path: "Models/ManageUserViewModel.cs"
      provides: "List<string> Units + PrimaryUnit + ConfirmedDeactivate + ImpactedMappings"
      contains: "public List<string> Units"
  key_links:
    - from: "Controllers/WorkerController.cs (CreateWorker/EditWorker/Import)"
      to: "UserUnits table + ApplicationUser.Unit mirror"
      via: "SyncUserUnitsAsync(user, units, primary)"
      pattern: "SyncUserUnitsAsync"
    - from: "Controllers/WorkerController.cs (EditWorker MU-07 guard)"
      to: "ProtonTrackAssignment + CoachCoacheeMapping aktif"
      via: "AnyAsync IsActive query → block atau auto-deactivate"
      pattern: "ProtonTrackAssignments[\\s\\S]*IsActive"
---

<objective>
Pusatkan kontrak WRITE-THROUGH primary-mirror di satu helper `SyncUserUnitsAsync(user, units, primary)` di `WorkerController`, panggil dari Create/Edit/Import. Tambah validasi `Unit ∈ Bagian` tiap junction-write, parse Import pipe-delimited (D-04/05), guard MU-07 asimetris (PROTON aktif=hard-block D-11 / coach-mapping aktif=confirm→auto-deactivate D-10), audit set-diff (D-12), dan export Excel multi-unit. Isi 6 file test logic Wave 0 (RED→GREEN).

Purpose: Invariant #3 (mirror = primary) + #4 (no orphan AssignmentUnit) dijaga di backend. Satu helper kill-desync. Plan 03 (widget) + Plan 04 (display) konsumsi ViewModel `Units`/`PrimaryUnit` yang dibuat di sini.
Output: `SyncUserUnitsAsync` + wiring Create/Edit/Import/Export di `WorkerController.cs`; `ManageUserViewModel` diperluas; 6 test logic hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-RESEARCH.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-PATTERNS.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-CONTEXT.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-VALIDATION.md

<interfaces>
<!-- Kontrak dari plan 01 (sudah ada). Konsumsi langsung. -->
From Models/UserUnit.cs: { int Id; string UserId; string Unit; bool IsPrimary; bool IsActive; ApplicationUser? User; }
From Data/ApplicationDbContext.cs:
  DbSet<UserUnit> UserUnits;
  Task<List<string>> GetUnitsForSectionAsync(string? section);    // :106-116 — unit anak 1 Bagian
  Task<Dictionary<string,List<string>>> GetSectionUnitsDictAsync(); // :118-133

WorkerController DI tersedia (:20-31): _context (ApplicationDbContext), _userManager, _auditLog, _config, _logger. TANPA injection baru.

Active-record query idiom (MU-07 guard) — VERIFIED:
  active PTA: a.CoacheeId == user.Id && a.IsActive   (CDPController.cs:75,398)
  active mapping: m.CoacheeId == user.Id && m.IsActive (CDPController.cs:944)
  ⚠️ ProtonTrackAssignment TIDAK punya kolom Unit (Pitfall 4) — resolve unit PROTON via active mapping AssignmentUnit ?? User.Unit (di Phase 399 fallback masih ada; dibuang Phase 401).

Auto-deactivate analog (CoachMappingController.cs:959-982): mapping.IsActive=false; mapping.EndDate=DateTime.UtcNow; tx; audit.
Audit: _auditLog.LogAsync(actorId, actorName, actionType, desc, targetId?, targetType?).
</interfaces>

<threat_model_ref>
Lihat blok <threat_model> di bawah — mass-assignment Units/PrimaryUnit, mirror desync, MU-07 data-loss vector. Mitigasi dijahit ke task.
</threat_model_ref>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ManageUserViewModel + SyncUserUnitsAsync helper (write-through + set-diff)</name>
  <files>Models/ManageUserViewModel.cs, Controllers/WorkerController.cs, HcPortal.Tests/UserUnitsWriteThroughTests.cs, HcPortal.Tests/PrimaryMirrorTests.cs, HcPortal.Tests/UserUnitsAuditDiffTests.cs</files>
  <read_first>
    - Models/ManageUserViewModel.cs (:33-37 Section/Unit scalar — perluas, Section TETAP scalar)
    - Controllers/WorkerController.cs (:20-31 DI; :563 pola tx DeleteWorker; :406 anti-pattern audit scalar yang diganti)
    - Controllers/CoachMappingController.cs (:959-979 pola tx BeginTransactionAsync→SaveChanges→Commit)
    - .planning/phases/399-.../399-PATTERNS.md (§SyncUserUnitsAsync helper code-to-write + §ViewModels)
    - HcPortal.Tests/UserUnitsWriteThroughTests.cs, PrimaryMirrorTests.cs, UserUnitsAuditDiffTests.cs (scaffold dari plan 01 — diisi di sini)
  </read_first>
  <behavior>
    - SyncUserUnitsAsync(user, ["A","B"], "B"): persist 2 baris UserUnits, B IsPrimary=true, user.Unit=="B"
    - primaryUnit null/invalid + units=["A","B"] → primary = units[0] ("A"), user.Unit=="A" (D-02)
    - units=[] → 0 baris UserUnits, user.Unit==null
    - hapus primary (old=["A","B" prim B], new=["A"]) → A jadi primary, user.Unit=="A"
    - set-diff: added "Unit +'B'", removed "Unit -'C'", "Primary: 'A' → 'B'" entri benar
    - replace-set: DELETE lama + INSERT baru dalam 1 SaveChanges (no window 2-primary)
  </behavior>
  <action>
Perluas `Models/ManageUserViewModel.cs` (Section TETAP scalar — invariant #1):
```csharp
public List<string> Units { get; set; } = new();
public string? PrimaryUnit { get; set; }
public bool ConfirmedDeactivate { get; set; }              // MU-07 round-trip
public List<string> ImpactedMappings { get; set; } = new(); // MU-07 display dampak
```

Tambah helper privat di `WorkerController.cs` PERSIS dari §PATTERNS code-to-write:
```csharp
// Dipanggil CreateWorker / EditWorker / Import. Replace-set: DELETE lama + INSERT baru
// dalam 1 SaveChanges → EF emit DELETE sebelum INSERT, tidak ada window 2-primary vs filtered-unique.
private async Task<List<string>> SyncUserUnitsAsync(
    ApplicationUser user, List<string> units, string? primaryUnit)
{
    var changes = new List<string>();
    var existing = await _context.UserUnits.Where(uu => uu.UserId == user.Id).ToListAsync();
    var oldSet = existing.Select(e => e.Unit).ToHashSet();
    var newSet = units.Distinct().ToHashSet();

    foreach (var added   in newSet.Except(oldSet))  changes.Add($"Unit +'{added}'");
    foreach (var removed in oldSet.Except(newSet))  changes.Add($"Unit -'{removed}'");

    var primary = (primaryUnit != null && newSet.Contains(primaryUnit))
        ? primaryUnit : units.FirstOrDefault();                 // D-02 deterministik
    var oldPrimary = existing.FirstOrDefault(e => e.IsPrimary)?.Unit;
    if (oldPrimary != primary) changes.Add($"Primary: '{oldPrimary}' → '{primary}'");

    _context.UserUnits.RemoveRange(existing);
    foreach (var u in newSet)
        _context.UserUnits.Add(new UserUnit { UserId = user.Id, Unit = u,
                                              IsPrimary = (u == primary), IsActive = true });

    user.Unit = primary;     // MIRROR (invariant #3); null bila 0 unit
    return changes;
}
```
Helper TIDAK commit sendiri — caller (Create/Edit/Import) yang panggil `_context.SaveChangesAsync()` (+ tx bila gabung MU-07 deactivate). user.Unit di-set di helper; caller persist via `_userManager.UpdateAsync(user)` (lihat Open Q3 atomicity — Task 2).

ISI 3 test (InMemory, pola WorkerDataServiceSearchTests :21-24): UserUnitsWriteThroughTests (set+mirror+1 IsPrimary), PrimaryMirrorTests (promote/null recompute), UserUnitsAuditDiffTests (entri set-diff). Aktifkan dari skip → hijau.
  </action>
  <acceptance_criteria>
    - `Models/ManageUserViewModel.cs` memuat "public List<string> Units" DAN "public string? PrimaryUnit" DAN "ConfirmedDeactivate"
    - `Controllers/WorkerController.cs` memuat "private async Task<List<string>> SyncUserUnitsAsync"
    - `Controllers/WorkerController.cs` memuat "user.Unit = primary"
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~WriteThrough|FullyQualifiedName~PrimaryMirror|FullyQualifiedName~AuditDiff"` exits 0 (3 test hijau)
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~WriteThrough|FullyQualifiedName~PrimaryMirror|FullyQualifiedName~AuditDiff"</automated>
  </verify>
  <done>ViewModel diperluas; SyncUserUnitsAsync menulis junction + mirror + set-diff; 3 test logic hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CreateWorker + EditWorker POST (validasi Unit∈Bagian + MU-07 guard + audit set-diff + atomicity)</name>
  <files>Controllers/WorkerController.cs, HcPortal.Tests/UnitInSectionValidationTests.cs, HcPortal.Tests/RemoveUnitGuardTests.cs</files>
  <read_first>
    - Controllers/WorkerController.cs (:212-309 CreateWorker; :229-236 validasi single-unit yang diperluas; :346-481 EditWorker; :400-413 audit scalar [GANTI]; :437 UpdateAsync; :464-477 audit emit)
    - Controllers/CoachMappingController.cs (:959-982 CoachCoacheeMappingDeactivate — analog auto-deactivate IsActive/EndDate/audit/tx)
    - Controllers/CDPController.cs (:75,:398 active PTA idiom; :944 active mapping idiom)
    - .planning/phases/399-.../399-PATTERNS.md (§CreateWorker POST + §EditWorker POST + §MU-07 auto-deactivate)
    - .planning/phases/399-.../399-RESEARCH.md (§Open Questions 1/2/3 — resolusi DI BAWAH; §Code Examples MU-07 round-trip)
    - .planning/phases/399-.../399-UI-SPEC.md (§Copywriting Contract — copy error MU-07 Bahasa Indonesia)
    - HcPortal.Tests/UnitInSectionValidationTests.cs, RemoveUnitGuardTests.cs (scaffold — diisi di sini)
  </read_first>
  <behavior>
    - Create dgn Units=["A","B"] valid (∈ Bagian) → 2 baris UserUnits, mirror=primary
    - Create dgn unit asing (∉ Bagian) → ModelState error, tidak tersimpan
    - Edit hapus unit dgn PTA aktif (unit = AssignmentUnit resolved) → ModelState block, return View, tidak tersimpan
    - Edit hapus unit dgn mapping aktif tanpa PTA, ConfirmedDeactivate=false → ViewBag.NeedConfirm=true, return View (re-prompt)
    - Edit hapus unit dgn mapping aktif tanpa PTA, ConfirmedDeactivate=true → mapping.IsActive=false + hapus unit dalam 1 tx
    - Edit kosongkan semua unit saat ada PTA aktif → block (Open Q2)
  </behavior>
  <action>
**CreateWorker POST** (§PATTERNS): tambah validasi loop SEBELUM cek ModelState.IsValid (perluas :229-236):
```csharp
var validUnits = await _context.GetUnitsForSectionAsync(model.Section);
var invalid = (model.Units ?? new()).Where(u => !validUnits.Contains(u)).ToList();
if (invalid.Any())
    ModelState.AddModelError("Units", $"@OrgLabels.GetLabel(1) tidak valid untuk {model.Section}: {string.Join(", ", invalid)}");
// re-validasi PrimaryUnit ∈ Units (server-side, jangan trust client)
if ((model.Units?.Any() ?? false) && model.PrimaryUnit != null && !model.Units.Contains(model.PrimaryUnit))
    ModelState.AddModelError("PrimaryUnit", "Pilih salah satu Unit sebagai Unit Utama.");
```
Setelah `CreateAsync` sukses (:280): `await SyncUserUnitsAsync(user, model.Units ?? new(), model.PrimaryUnit);` lalu `await _userManager.UpdateAsync(user);` (persist mirror Unit) `+ await _context.SaveChangesAsync();`. Pertahankan ViewBag.SectionUnitsJson re-populate on error (:243-244).

**EditWorker POST** (§PATTERNS):
1. HAPUS anti-pattern audit scalar `if (user.Unit != model.Unit)...` (:406) + assignment `user.Unit = model.Unit` (:413). PERTAHANKAN baris Section scalar.
2. Validasi `Unit ∈ Bagian` (sama seperti Create) + PrimaryUnit ∈ Units.
3. MU-07 GUARD (sebelum SyncUserUnitsAsync) — resolusi Open Q1/Q2 EKSPLISIT:
```csharp
var oldUnits = await _context.UserUnits.Where(uu => uu.UserId == user.Id).Select(uu => uu.Unit).ToListAsync();
var removed  = oldUnits.Except(model.Units ?? new()).ToList();
var oldPrimary = (await _context.UserUnits.FirstOrDefaultAsync(uu => uu.UserId==user.Id && uu.IsPrimary))?.Unit;
if (removed.Any())
{
    var activeMapping = await _context.CoachCoacheeMappings.FirstOrDefaultAsync(m => m.CoacheeId == user.Id && m.IsActive);
    var hasActivePta  = await _context.ProtonTrackAssignments.AnyAsync(a => a.CoacheeId == user.Id && a.IsActive);
    // Resolusi unit PROTON (Pitfall 4 — PTA tak punya kolom Unit): AssignmentUnit ?? oldPrimary
    var protonUnit = activeMapping?.AssignmentUnit ?? oldPrimary;
    // HARD-BLOCK (D-11) — Open Q1: (a) AssignmentUnit∈removed; ATAU (b) AssignmentUnit==null && oldPrimary∈removed
    if (hasActivePta && protonUnit != null && removed.Contains(protonUnit))
    {
        ModelState.AddModelError("", $"Tidak bisa menghapus Unit '{protonUnit}': masih ada PROTON tahun-berjalan aktif. Tutup atau bypass PROTON terlebih dahulu melalui halaman PROTON.");
        /* re-populate ViewBag.SectionUnitsJson */ return View(model);
    }
    // AUTO-DEACTIVATE-after-confirm (D-10): mapping aktif AssignmentUnit∈removed, no PTA terkait
    if (activeMapping?.AssignmentUnit != null && removed.Contains(activeMapping.AssignmentUnit))
    {
        if (!model.ConfirmedDeactivate)
        {
            model.ImpactedMappings = new() { $"Mapping coach aktif unit '{activeMapping.AssignmentUnit}' akan dinonaktifkan" };
            ViewBag.NeedConfirm = true; /* re-populate ViewBag.SectionUnitsJson */ return View(model);
        }
        // confirmed → reuse pola CoachCoacheeMappingDeactivate DALAM tx yang sama (Task ini)
        activeMapping.IsActive = false; activeMapping.EndDate = DateTime.UtcNow;
        changes.Add($"Mapping coach unit '{activeMapping.AssignmentUnit}' dinonaktifkan (hapus unit)");  // D-12
    }
}
```
4. ATOMICITY (Open Q3): bungkus `_userManager.UpdateAsync(user)` + `SyncUserUnitsAsync` + (deactivate bila confirmed) + `_context.SaveChangesAsync()` dalam SATU `using var tx = await _context.Database.BeginTransactionAsync(); ... await tx.CommitAsync();`. Set `user.Unit` di SyncUserUnitsAsync (sebelum SaveChanges). UpdateAsync + _context share connection via DI scope → mirror + junction commit bersama (no desync).
5. Merge `SyncUserUnitsAsync` returned diff ke `changes`; audit emit (:464-477) sudah `string.Join("; ", changes)` — tak berubah.

ISI 2 test: UnitInSectionValidationTests (unit asing ditolak), RemoveUnitGuardTests (PTA→block, mapping→confirm→auto-deactivate). InMemory.

⚠️ JANGAN longgarkan `[Authorize(Roles="Admin, HC")]` (:344) atau `[ValidateAntiForgeryToken]` (:345). Bind hanya `model.Units`/`model.PrimaryUnit` yang sudah di ViewModel (no over-posting field lain).
  </action>
  <acceptance_criteria>
    - `Controllers/WorkerController.cs` TIDAK lagi memuat `if (user.Unit != model.Unit)` (anti-pattern dihapus)
    - `Controllers/WorkerController.cs` memuat "ConfirmedDeactivate" DAN "NeedConfirm" DAN "PROTON tahun-berjalan aktif"
    - `Controllers/WorkerController.cs` memuat "BeginTransactionAsync" di EditWorker (atomicity Open Q3)
    - `Controllers/WorkerController.cs` memuat "GetUnitsForSectionAsync" di Create DAN Edit
    - `[Authorize(Roles = "Admin, HC")]` + `[ValidateAntiForgeryToken]` masih ada di Create/Edit POST
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~UnitInSectionValidation|FullyQualifiedName~RemoveUnitGuard"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~UnitInSectionValidation|FullyQualifiedName~RemoveUnitGuard"</automated>
  </verify>
  <done>Create/Edit POST validasi Unit∈Bagian + MU-07 guard asimetris (block/confirm) + audit set-diff + tx atomic mirror; 2 test hijau; authz/CSRF utuh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Import (pipe-parse multi-unit) + Export Excel (multi-unit) + template help-text</name>
  <files>Controllers/WorkerController.cs, HcPortal.Tests/ImportMultiUnitParseTests.cs</files>
  <read_first>
    - Controllers/WorkerController.cs (:915-1040 Import; :944 Cell(6) parse; :976-983 validasi Unit∈Bagian; :1018-1037 user-create; :180-187 Export ws.Cell)
    - .planning/phases/399-.../399-PATTERNS.md (§Import pipe-split + §Export primary-first comma-join)
    - .planning/phases/399-.../399-CONTEXT.md (D-04/05/06 — delimiter pipe, first=primary, backward-compat, update template)
    - .planning/phases/399-.../399-UI-SPEC.md (§Copywriting — import help text Bahasa Indonesia)
    - HcPortal.Tests/ImportMultiUnitParseTests.cs (scaffold — diisi di sini)
  </read_first>
  <behavior>
    - Import "UnitA|UnitB|UnitC" → split+trim+dedup → [A,B,C], A=primary; validasi tiap ∈ Bagian
    - Import "UnitA" (no pipe, backward-compat) → [A], A=primary
    - Import unit asing dalam pipe → error per-baris "Unit tidak valid untuk '{bagian}': {invalid}"
    - Export: worker dgn units [A,B prim B] → cell 7 = "B, A" (primary-first comma)
  </behavior>
  <action>
**Import** (§PATTERNS, Cell(6) posisi TETAP — D-04): ganti parse :944 + validasi :976-983:
```csharp
var unitCell = (row.Cell(6).GetString() ?? "").Trim();   // posisi Cell(6) TIDAK bergeser
var unitList = unitCell.Split('|', StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries)
                       .Distinct(StringComparer.OrdinalIgnoreCase).ToList();   // first=primary (D-04)
if (unitList.Any())
{
    if (string.IsNullOrWhiteSpace(bagian))
        errors.Add("Unit tidak boleh diisi tanpa Section");
    else if (sectionUnitsDict.TryGetValue(bagian, out var validUnits))
    {
        var invalid = unitList.Where(u => !validUnits.Contains(u)).ToList();   // validasi PER unit (MU-05)
        if (invalid.Any())
            errors.Add($"Unit tidak valid untuk '{bagian}': {string.Join(", ", invalid)}");
    }
}
```
Setelah `CreateAsync` sukses (:1035): `await SyncUserUnitsAsync(newUser, unitList, unitList.FirstOrDefault());` + persist (UpdateAsync + SaveChanges sesuai pola Create). "UnitA" tanpa pipe → 1 elemen primary (D-05 backward-compat).

**Export** (§PATTERNS, kolom 7 SAMA — D-08): ganti :186-187. Batch-load UserUnits (group by UserId, hindari N+1) lalu:
```csharp
string unitsText = (units == null || !units.Any()) ? "-"
    : string.Join(", ", units.OrderByDescending(x => x == primaryUnit).ThenBy(x => x));
ws.Cell(i + 2, 7).Value = unitsText;
```
Tanpa star/"Utama" di Excel — ordering primary-first satu-satunya sinyal (D-08).

**Template/help-text** (D-06): update teks bantuan import template dengan copy UI-SPEC: "Untuk Unit ganda, pisahkan dengan tanda pipa | (contoh: UnitA|UnitB|UnitC). Unit pertama menjadi Unit Utama. Satu Unit tetap valid tanpa pipa." (lokasi: view Import / generator template Excel bila ada).

**ManageWorkers list VM populate (untuk display Plan 04):** WorkerController.ManageWorkers (:39) menyuplai list pekerja ke tabel. Karena WorkerController dimiliki Plan 02 (file-ownership), POPULATE di sini `Units`/`PrimaryUnit` per worker pada VM/DTO list: batch-load `_context.UserUnits.Where(uu => userIds.Contains(uu.UserId)).ToListAsync()` lalu group by UserId (hindari N+1); set `worker.Units = grp.Select(x=>x.Unit).ToList()` + `worker.PrimaryUnit = grp.FirstOrDefault(x=>x.IsPrimary)?.Unit`. Plan 04 Task 2 hanya BACA field ini di view (no edit WorkerController). Tambah `List<string> Units`/`string? PrimaryUnit` ke worker-DTO/VM list bila belum ada. Filter `unitFilter` existing TETAP scalar di Phase 399 (set-aware = Phase 400 MU-06 — JANGAN ubah di sini).

ISI test ImportMultiUnitParseTests (pure-logic split+dedup+backward-compat). Bila parse di-extract ke helper static, test panggil langsung; bila inline, ekstrak logic split ke helper `ParseUnitCell(string)` agar testable.

⚠️ Pertahankan `[Authorize(Roles="Admin, HC")]` (:896) + `[ValidateAntiForgeryToken]` (:897) di Import. Validasi server-side (jangan trust isi file).
  </action>
  <acceptance_criteria>
    - `Controllers/WorkerController.cs` Import memuat `.Split('|'` DAN "Distinct" DAN "Unit tidak valid untuk"
    - `Controllers/WorkerController.cs` memuat "row.Cell(6)" (posisi Cell(6) tetap)
    - `Controllers/WorkerController.cs` Export memuat "OrderByDescending" + "ws.Cell(i + 2, 7)"
    - Help-text/template memuat string "UnitA|UnitB" (atau "pipa")
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~ImportMultiUnitParse"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~ImportMultiUnitParse"</automated>
  </verify>
  <done>Import parse pipe multi-unit (first=primary, backward-compat) + validasi per-unit; Export primary-first comma-join kolom 7; help-text update; test parse hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form/Excel → WorkerController POST | `model.Units[]`, `model.PrimaryUnit`, sel Excel `Cell(6)` = input tak-tepercaya melintasi sini |
| WorkerController → DB (UserUnits + mirror) | Write-through transaksional; MU-07 deactivate mengubah mapping/PTA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-399-02-01 | Tampering / EoP | Mass-assignment `List<string> Units` (inject unit asing/cross-Bagian) | mitigate | Validasi server `u ∈ GetUnitsForSectionAsync(Section)` SETIAP write (Create/Edit/Import); tolak `PrimaryUnit ∉ Units`. Bind hanya Units/PrimaryUnit (no over-posting). |
| T-399-02-02 | Tampering (integrity) | Mirror desync (`ApplicationUser.Unit` ≠ baris IsPrimary) | mitigate | Write-through terpusat `SyncUserUnitsAsync` + tx atomic (UpdateAsync + _context dalam 1 BeginTransactionAsync — Open Q3); replace-set DELETE-before-INSERT. |
| T-399-02-03 | Tampering (data-loss) | MU-07: hapus unit dirujuk PTA/mapping aktif → orphan AssignmentUnit / PROTON ter-abandon | mitigate | Guard server-side di POST path: PTA aktif → HARD-BLOCK (D-11); mapping aktif → confirm→auto-deactivate atomic + audit (D-10). BUKAN hanya JS. |
| T-399-02-04 | Repudiation | Perubahan membership tanpa jejak | mitigate | Audit set-diff (`Unit +'X'`/`-'Y'`/`Primary: A→B`) + event auto-deactivate, mencatat actor via `_auditLog.LogAsync(actor.Id, actorName, ...)` (D-12). |
| T-399-02-05 | EoP | Non-Admin/HC ubah unit | mitigate | `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` pada Create/Edit/Import POST — TIDAK dilonggarkan. Authz Section scalar tak disentuh. |
| T-399-02-06 | DoS | Excel cell patologis (oversized / pipe berlebih) | mitigate | Split pipe → Distinct dedup; validasi per-unit ∈ Bagian (unit asing ditolak, bukan di-insert). Parse server-side; ClosedXML cell read existing. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau (6 test logic Wave 0 + suite existing).
- `dotnet run` localhost:5277: Create worker 2-unit round-trip tersimpan (cek DB lokal: 2 baris UserUnits, 1 IsPrimary, Users.Unit == primary); Edit hapus unit dgn mapping aktif → re-prompt; Import pipe "A|B" → 2 unit; Export Excel kolom unit primary-first.
- CLAUDE.md gate: build + run + cek DB lokal sebelum commit.
</verification>

<success_criteria>
- `SyncUserUnitsAsync` satu-satunya jalur tulis junction + mirror (Create/Edit/Import).
- ApplicationUser.Unit == baris IsPrimary setelah setiap write; promote/null recompute benar.
- Validasi Unit∈Bagian tiap write; MU-07 asimetris (block/confirm→auto-deactivate atomic); audit set-diff.
- Import pipe multi-unit (backward-compat); Export primary-first comma-join.
- 6 test logic Wave 0 hijau; authz/CSRF utuh.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-02-SUMMARY.md`. Catat: resolusi Open Q1/Q2/Q3 yang dipakai, hasil round-trip 2-unit DB lokal, daftar test hijau, konfirmasi authz/CSRF tidak dilonggarkan.
</output>
