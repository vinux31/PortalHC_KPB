---
phase: 400-membership-listing-set-aware-rollup-dedup
plan: 01
type: execute
wave: 1
depends_on: []
migration: false
files_modified:
  - HcPortal.Tests/WorkerDataServiceSearchTests.cs
  - Services/WorkerDataService.cs
  - Controllers/WorkerController.cs
autonomous: false
requirements: [MU-06]

must_haves:
  truths:
    - "Pekerja anggota 2 unit {X,Y} dalam 1 Bagian muncul saat difilter unit-X DAN saat difilter unit-Y (set-aware, bukan hanya primary)"
    - "Tanpa filter unit, pekerja {X,Y} muncul sebagai TEPAT 1 baris (dedup by-construction → completion%/denominator tidak ganda)"
    - "Unit yang sudah di-deactivate (IsActive=false, jalur MU-07) TIDAK muncul di roster maupun kolom Unit (D-03)"
    - "Kolom Unit tabel CMP records team kontekstual: saat difilter unit-X tampil 'X', tanpa filter tampil semua unit aktif primary-first comma-join 'X, Y' (D-02)"
    - "Pekerja 0 unit aktif (legacy/Unit=null) fallback ke user.Unit, view render '---' bila null (D-05)"
    - "CMP analytics/renewal + Team View list (tanpa unitFilter) TIDAK berubah perilakunya — 0 drift D1=b (SC#3)"
  artifacts:
    - path: "Services/WorkerDataService.cs"
      provides: "Predikat unit set-aware (correlated subquery _context.UserUnits) + kolom Unit kontekstual + batch-load dict unitsByUser"
      contains: "_context.UserUnits.Any(uu => uu.UserId == u.Id"
    - path: "Controllers/WorkerController.cs"
      provides: "Predikat unit set-aware di ManageWorkers + ExportWorkers (D-06 predicate-only, display tak diubah)"
      contains: "_context.UserUnits.Any(uu => uu.UserId == u.Id"
    - path: "HcPortal.Tests/WorkerDataServiceSearchTests.cs"
      provides: "~7 test MU-06 (set-aware both-units, dedup, IsActive D-03, kontekstual filtered/unfiltered D-02, fallback D-05)"
      contains: "MultiUnitWorker_AppearsInBothUnitFilters_SetAware"
  key_links:
    - from: "Services/WorkerDataService.cs GetWorkersInSection predikat :254-255"
      to: "_context.UserUnits junction (Phase 399)"
      via: "correlated subquery .Any(uu => uu.UserId == u.Id && uu.Unit == unitFilter && uu.IsActive)"
      pattern: "_context\\.UserUnits\\.Any\\(uu => uu\\.UserId == u\\.Id"
    - from: "Services/WorkerDataService.cs assign Unit :347"
      to: "unitsByUser dict (batch-load active-only, primary-first)"
      via: "ternary kontekstual unitFilter ?? string.Join(\", \", uList) ?? user.Unit"
      pattern: "string\\.Join\\(\", \", uList\\)"
    - from: "Consumer #4 AssessmentAdminController.cs:278 (ManageAssessmentTab)"
      to: "GetWorkersInSection(section, unit, ...)"
      via: "meneruskan unit → mewarisi set-aware otomatis (no code change, verification scope)"
      pattern: "GetWorkersInSection"
---

<objective>
Ubah filter unit dari scalar `u.Unit == unitFilter` (hanya primary) menjadi keanggotaan **set-aware** terhadap junction `UserUnits` (Phase 399), sehingga pekerja anggota >1 unit dalam 1 Bagian muncul saat difilter per **tiap** unit-nya — di roster tim/section (`WorkerDataService.GetWorkersInSection`) + role-filter `WorkerController` (ManageWorkers + ExportWorkers). Rollup tingkat Bagian tetap dedup (1 baris/pekerja by-construction, tanpa `.Distinct()`). Kolom Unit di tabel CMP records team jadi **kontekstual** (D-02). CMP analytics/renewal TIDAK disentuh (D1=b primary).

Purpose: Tutup MU-06 — satu-satunya REQ Phase 400. Tanpa ini, pekerja multi-unit hanya tampil di unit primary-nya pada listing, melanggar kontrak v32.3.
Output: 3 predikat set-aware + 1 kolom kontekstual + batch-load dict + ~7 unit test. 0 migration. 0 perubahan markup view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Phase artifacts (LOCKED decisions + verified touchpoints + analogs)
@.planning/phases/400-membership-listing-set-aware-rollup-dedup/400-CONTEXT.md
@.planning/phases/400-membership-listing-set-aware-rollup-dedup/400-RESEARCH.md
@.planning/phases/400-membership-listing-set-aware-rollup-dedup/400-PATTERNS.md
@.planning/phases/400-membership-listing-set-aware-rollup-dedup/400-VALIDATION.md
@.planning/phases/400-membership-listing-set-aware-rollup-dedup/400-UI-SPEC.md

<interfaces>
<!-- Kontrak yang executor butuh — TIDAK perlu eksplorasi kodebase. Diekstrak langsung dari kode live. -->

Models/UserUnit.cs (junction Phase 399):
```csharp
public class UserUnit {
    public int Id { get; set; }
    public string UserId { get; set; }     // FK ApplicationUser.Id
    public string Unit { get; set; }       // NAME-string unit (anak Bagian)
    public bool IsPrimary { get; set; }
    public bool IsActive { get; set; }     // default true; MU-07 deactivate set false
}
```

Data/ApplicationDbContext.cs:
- `DbSet<UserUnit> UserUnits` ADA.
- ⚠️ PITFALL #1 (compile-blocker): `UserUnit.HasOne(uu => uu.User).WithMany()` — `.WithMany()` TANPA argumen → **nav prop `ApplicationUser.UserUnits` TIDAK ADA**. JANGAN tulis `u.UserUnits.Any(...)` (CS1061). WAJIB correlated subquery `_context.UserUnits.Any(uu => uu.UserId == u.Id && ...)`.

Models/WorkerTrainingStatus.cs:
- `public string? Unit { get; set; }` — nullable, set in-place type-safe (view byte-stable).

Services/WorkerDataService.cs — signature (TIDAK BERUBAH, reuse `unitFilter` existing):
```csharp
public async Task<List<WorkerTrainingStatus>> GetWorkersInSection(
    string? section, string? unitFilter = null, string? category = null, string? search = null,
    string? statusFilter = null, DateTime? dateFrom = null, DateTime? dateTo = null,
    string? subCategory = null, string? searchScope = null)
```
- Predikat unit scalar saat ini @ `:254-255`. Batch-load dict-pola CMP-25 @ `:283`/`:295`. `var userIds` @ `:271`. Assign `Unit = user.Unit ?? "",` @ `:347` (di dalam `foreach (var user in users)` `:318-355`).

Controllers/WorkerController.cs:
- `ManageWorkers` predikat unit scalar @ `:202-204`. `ExportWorkers` predikat unit scalar @ `:300-301`.
- Pola batch-load dict existing `userUnitsDict` @ `:224-232` (ViewBag, Phase 399) — JANGAN diubah (D-06).
- Validasi `unitFilter`-vs-section @ `:171-176`/`:274-279` — JANGAN diubah (security V5).

ANALOG form correlated-subquery (satu-satunya di repo, terbukti compile + translate EXISTS + ada test InMemory hijau):
- `Controllers/TrainingAdminController.cs:802`:
  `.Where(h => !_context.AssessmentSessions.Any(s => s.Id == h.SessionId))`
- Test InMemory presedennya: `HcPortal.Tests/OrphanCleanupTests.cs:34`.

ANALOG batch-load dict + primary-first ordering:
- `Controllers/WorkerController.cs:224-232` (UserUnitsDict).
- `Controllers/AccountController.cs:155-163` (subquery `_context.UserUnits` + primary-first ordering).
- in-file CMP-25 `WorkerDataService.cs:283` (`(await q.ToListAsync()).GroupBy().ToDictionary()`).

Consumer GetWorkersInSection (4 call-site dgn unitFilter — temuan kritis #2):
1. `CMPController.cs:819` RecordsTeamPartial (paginate `workerList.Count` :824 — aman, 1 row/worker)
2. `CMPController.cs:710` / `:771` ExportRecordsTeam (`filteredWorkers.Select(w => w.WorkerId)` — set-aware ikut otomatis)
3. `CMPController.cs:543` Team View list — **tanpa** unitFilter → predikat skip → 0 drift (SC#3)
4. `AssessmentAdminController.cs:278` ManageAssessmentTab — meneruskan `unit` → mewarisi set-aware OTOMATIS (no code change, WAJIB di verification scope)

PATH TERPISAH yang TIDAK boleh disentuh (D1=b no-drift, SC#3):
- `CMPController.cs:2581` `baseQuery.Where(s => s.User!.Unit == unit)` — scalar mirror analytics (BUKAN lewat GetWorkersInSection)
- `CMPController.cs:2589` `GroupBy(s => s.User!.Section)` — Section-level analytics
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 — RED): Tulis ~7 unit test MU-06 di WorkerDataServiceSearchTests.cs</name>
  <files>HcPortal.Tests/WorkerDataServiceSearchTests.cs</files>
  <read_first>
    - HcPortal.Tests/WorkerDataServiceSearchTests.cs (helper `MakeService(out var ctx)` :19-28, helper `User(id,name,section,nip)` :30-35, structural template fact `Scope_Nama_FiltersByName` :49-58, regresi existing `Scope_Null_NoFilter_BackwardCompat` :86-93)
    - HcPortal.Tests/OrphanCleanupTests.cs:34 (preseden `.Any(...)`-in-Where hijau di InMemory)
    - Models/UserUnit.cs (field UserId/Unit/IsPrimary/IsActive)
    - 400-RESEARCH.md §Code Examples (badan test lengkap) + 400-PATTERNS.md §test (caveat helper)
  </read_first>
  <behavior>
    Test-first (RED). Tambah test ke kelas `WorkerDataServiceSearchTests` (reuse `MakeService`/`User`). Seed UserUnits via `ctx.UserUnits.AddRange(new UserUnit { UserId=..., Unit=..., IsPrimary=..., IsActive=... })`.
    - Test 1 `MultiUnitWorker_AppearsInBothUnitFilters_SetAware`: pekerja {UnitX(primary), UnitY} → `GetWorkersInSection("A", unitFilter:"UnitX")` == Single AND `...unitFilter:"UnitY"` == Single AND `...("A")` (tanpa filter) == Single (dedup, SC#1+SC#2 dalam 1 test).
    - Test 2 `MultiUnitWorker_SingleRow_NoFilter`: pekerja {UnitX,UnitY} → `GetWorkersInSection("A").Count == 1` (dedup eksplisit, SC#2).
    - Test 3 `InactiveUnit_ExcludedFromFilter_D03`: pekerja {UnitX(active,primary), UnitY(IsActive=false)} → `...unitFilter:"UnitY"` == Empty (D-03).
    - Test 4 `UnfilteredColumn_AllActiveUnits_PrimaryFirst_D02`: primary=UnitY, sekunder=UnitX, keduanya active → `GetWorkersInSection("A")[0].Unit == "UnitY, UnitX"` (primary-first comma-join, D-02).
    - Test 5 `FilteredColumn_ShowsUnitFilter_D02`: pekerja {UnitX(primary),UnitY} → `...unitFilter:"UnitY")[0].Unit == "UnitY"` (D-02 filtered = matched unit).
    - Test 6 `ZeroUnit_Fallback_D05`: pekerja 0 baris UserUnits aktif, `u.Unit="Legacy"` → `GetWorkersInSection("A")[0].Unit == "Legacy"` (D-05 fallback). (Boleh tambah varian Unit=null → cek `""` atau null, opsional.)
    - Test 7: TIDAK perlu tulis ulang — `Scope_Null_NoFilter_BackwardCompat` (:86) yang sudah ada = regresi no-drift D1=b (SC#3); pastikan tetap hijau setelah perubahan.
  </behavior>
  <action>
CRITICAL helper caveat: `User()` hardcode `Unit = "U1"` (`:33`). Setiap test multi-unit WAJIB override `u.Unit` agar mirror cocok dgn primary UserUnit, mis:
```csharp
var u = User("u1", "Budi", "A"); u.Unit = "UnitX";  // mirror primary
ctx.Users.Add(u);
ctx.UserUnits.AddRange(
    new UserUnit { UserId = "u1", Unit = "UnitX", IsPrimary = true,  IsActive = true },
    new UserUnit { UserId = "u1", Unit = "UnitY", IsPrimary = false, IsActive = true });
await ctx.SaveChangesAsync();
```
Salin badan test PERSIS dari 400-RESEARCH.md §Code Examples (4 contoh lengkap di sana: `MultiUnitWorker_AppearsInBothUnitFilters_SetAware`, `InactiveUnit_ExcludedFromFilter_D03`, `UnfilteredColumn_AllActiveUnits_PrimaryFirst_D02`, `FilteredColumn_ShowsUnitFilter_D02`) + tambah Test 2 (`Count == 1`) dan Test 6 (`ZeroUnit_Fallback_D05`: seed 0 UserUnit aktif, set `u.Unit="Legacy"`, assert `r[0].Unit == "Legacy"`).
Tempatkan semua di bawah test existing, di section komentar baru `// ── MU-06 set-aware + kontekstual ──`.
JANGAN sentuh `FakeWorkerDataService.cs` (return empty, dipakai GradingService test saja).
Jalankan test → HARUS RED (gagal-assert karena predikat masih scalar). Lebih disukai: test compile + RED assert (predikat scalar membuat unitFilter:"UnitY" Empty, kontekstual masih `user.Unit`).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" ; dotnet build HcPortal.Tests</automated>
  </verify>
  <acceptance_criteria>
    - `WorkerDataServiceSearchTests.cs` berisi string `MultiUnitWorker_AppearsInBothUnitFilters_SetAware`
    - `WorkerDataServiceSearchTests.cs` berisi string `InactiveUnit_ExcludedFromFilter_D03`
    - `WorkerDataServiceSearchTests.cs` berisi string `UnfilteredColumn_AllActiveUnits_PrimaryFirst_D02`
    - `WorkerDataServiceSearchTests.cs` berisi string `FilteredColumn_ShowsUnitFilter_D02`
    - `WorkerDataServiceSearchTests.cs` berisi string `ZeroUnit_Fallback_D05`
    - `WorkerDataServiceSearchTests.cs` berisi string `ctx.UserUnits.AddRange`
    - Test project compile (`dotnet build HcPortal.Tests` exit 0); menjalankan filter MU-06 menunjukkan RED (set-aware/kontekstual belum diimplement) — ditangkap Task 2.
    - `FakeWorkerDataService.cs` TIDAK berubah (git diff kosong untuk file itu)
  </acceptance_criteria>
  <done>~7 test MU-06 ada di WorkerDataServiceSearchTests.cs, project test compile, test set-aware/kontekstual RED (belum implement). Helper User() di-override Unit per test multi-unit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Predikat set-aware (3 lokasi) + kolom Unit kontekstual + batch-load dict</name>
  <files>Services/WorkerDataService.cs, Controllers/WorkerController.cs</files>
  <read_first>
    - Services/WorkerDataService.cs:244-355 (GetWorkersInSection: predikat :254-255, `var userIds` :271, CMP-25 batch-load :283/:295, foreach hydrate :318-355, assign Unit :347)
    - Controllers/WorkerController.cs:165-336 (ManageWorkers predikat :202-204, ExportWorkers predikat :300-301, userUnitsDict pola :224-232 [JANGAN ubah], validasi unit-vs-section :171-176 [JANGAN ubah])
    - Controllers/TrainingAdminController.cs:802 (form correlated subquery analog)
    - Controllers/AccountController.cs:155-163 (primary-first ordering analog)
    - 400-PATTERNS.md §Pattern Assignments (A)(B)(C) + 400-RESEARCH.md §Architecture Patterns 1-3
  </read_first>
  <behavior>
    GREEN — buat 7 test Task 1 hijau + suite existing tetap hijau (366+ baseline).
    - 3 predikat unit jadi correlated subquery set-aware active-only (D-01/D-03).
    - Kolom `WorkerTrainingStatus.Unit` kontekstual (D-02): filtered → `unitFilter`; unfiltered → all-active primary-first comma-join; 0-unit → fallback `user.Unit` (D-05).
    - Batch-load dict `unitsByUser` (1 query, active-only, primary-first) di GetWorkersInSection (D-04) — TIDAK di ManageWorkers (D-06).
  </behavior>
  <action>
**(A) Services/WorkerDataService.cs:254-255** — GANTI predikat scalar:
```csharp
// LAMA:
if (!string.IsNullOrEmpty(unitFilter))
    usersQuery = usersQuery.Where(u => u.Unit == unitFilter);
// BARU (correlated subquery — PITFALL #1: PAKAI _context.UserUnits, BUKAN u.UserUnits):
if (!string.IsNullOrEmpty(unitFilter))
    usersQuery = usersQuery.Where(u =>
        _context.UserUnits.Any(uu => uu.UserId == u.Id && uu.Unit == unitFilter && uu.IsActive));
```

**(B) Services/WorkerDataService.cs** — SISIPKAN batch-load dict TEPAT SETELAH `var userIds = users.Select(u => u.Id).ToList();` (`:271`), gaya CMP-25 in-file:
```csharp
// Phase 400 (MU-06 D-02/D-04): batch-load semua unit AKTIF per pekerja (primary-first), no N+1,
// utk kolom Unit kontekstual. Active-only (D-03).
var unitsByUser = (await _context.UserUnits
        .Where(uu => userIds.Contains(uu.UserId) && uu.IsActive)
        .ToListAsync())
    .GroupBy(uu => uu.UserId)
    .ToDictionary(
        g => g.Key,
        g => g.OrderByDescending(x => x.IsPrimary).ThenBy(x => x.Unit)
              .Select(x => x.Unit).ToList());
```

**(C) Services/WorkerDataService.cs:347** — GANTI assign `Unit = user.Unit ?? "",` jadi kontekstual:
```csharp
Unit = !string.IsNullOrEmpty(unitFilter)
    ? unitFilter                                                       // D-02 filtered → matched unit
    : (unitsByUser.TryGetValue(user.Id, out var uList) && uList.Count > 0
        ? string.Join(", ", uList)                                     // D-02 unfiltered → all-active primary-first
        : (user.Unit ?? "")),                                          // D-05 fallback (legacy/0-unit; view `?? "---"` guard null)
```

**(D) Controllers/WorkerController.cs:202-204** (ManageWorkers) — GANTI body predikat dgn subquery IDENTIK (`query.Where(u => _context.UserUnits.Any(uu => uu.UserId == u.Id && uu.Unit == unitFilter && uu.IsActive))`). JANGAN ubah display/badge (D-06).

**(E) Controllers/WorkerController.cs:300-301** (ExportWorkers) — GANTI body predikat dgn subquery IDENTIK yang sama.

LARANGAN keras:
- JANGAN tambah `.Distinct()` di mana pun (dedup by-construction — `.Any()` subquery boolean = 1 row/user, no fan-out).
- JANGAN tulis `u.UserUnits.Any(...)` (nav prop tak ada — CS1061).
- JANGAN ubah signature GetWorkersInSection (reuse `unitFilter`).
- JANGAN sentuh `userUnitsDict` ManageWorkers (:224-232), validasi unit-vs-section (:171-176/:274-279), export comma-join (:336-351), atau path analytics CMPController (:2581/:2589/:543).
- JANGAN tambah OR-fallback scalar (`u.Unit == unitFilter OR ...`) — backfill 399 + invariant mirror buat redundan. `.Any()` murni.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" ; dotnet build ; dotnet test HcPortal.Tests --filter "FullyQualifiedName~WorkerDataServiceSearchTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exit 0 (tangkap Pitfall #1)
    - `Services/WorkerDataService.cs` berisi `_context.UserUnits.Any(uu => uu.UserId == u.Id`
    - `Services/WorkerDataService.cs` berisi `string.Join(", ", uList)`
    - `Services/WorkerDataService.cs` TIDAK lagi berisi `usersQuery.Where(u => u.Unit == unitFilter)` (scalar dihapus)
    - `Controllers/WorkerController.cs` berisi `_context.UserUnits.Any(uu => uu.UserId == u.Id` (muncul ≥2× — ManageWorkers + ExportWorkers)
    - `Controllers/WorkerController.cs` TIDAK lagi berisi `query.Where(u => u.Unit == unitFilter)` (scalar dihapus dari 2 lokasi)
    - `Services/WorkerDataService.cs` + `Controllers/WorkerController.cs` TIDAK berisi `.Distinct(`
    - `dotnet test --filter "FullyQualifiedName~WorkerDataServiceSearchTests"` exit 0 (semua test MU-06 + existing hijau, termasuk `Scope_Null_NoFilter_BackwardCompat`)
    - `git diff` menunjukkan TIDAK ada perubahan di `CMPController.cs` (analytics :2581/:2589/Team View :543 utuh), `_RecordsTeamBody.cshtml`, `Models/WorkerTrainingStatus.cs`, `Services/IWorkerDataService.cs`
  </acceptance_criteria>
  <done>3 predikat set-aware (subquery `_context.UserUnits`), kolom Unit kontekstual + batch-load dict di GetWorkersInSection, build 0 error, semua test MU-06 + existing hijau. Tidak ada `.Distinct()`, tidak ada nav-prop, tidak ada perubahan analytics/view/model/interface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (UAT lokal): Runtime render kolom kontekstual + fixture {X,Y} + no-drift D1=b</name>
  <files>(verifikasi runtime — tidak menulis file produksi; hanya docs/SEED_JOURNAL.md untuk jurnal seed temporary)</files>
  <what-built>
  Filter unit set-aware di 3 predikat (GetWorkersInSection + ManageWorkers + ExportWorkers) + kolom Unit kontekstual D-02 di tabel CMP records team (value-driven, markup `_RecordsTeamBody.cshtml:27` tak berubah). Consumer ke-4 `AssessmentAdminController:278` (ManageAssessmentTab / Kelola Data Section C) mewarisi set-aware otomatis. Full suite + `dotnet build` sudah hijau di Task 2 (automated). Checkpoint ini = verifikasi runtime yang TIDAK bisa di-assert grep/build (lesson Phase 354: Razor render + SQL EXISTS translation + dedup DB riil).
  </what-built>
  <how-to-verify>
  Ikuti CLAUDE.md Develop Workflow (verifikasi lokal WAJIB sebelum commit) + Seed Data Workflow (fixture temporary). migration=FALSE.

  1. **Build + full suite** (jaring pengaman):
     - `dotnet build` → 0 error.
     - `dotnet test HcPortal.Tests` → ≥366 + ~7 test MU-06 baru hijau (0 regresi).
  2. **Anomali backfill check** (putuskan OR-fallback — RESEARCH Open Q2). Jalankan di DB lokal `HcPortalDB_Dev`:
     `SELECT u.Id FROM Users u WHERE u.Unit IS NOT NULL AND NOT EXISTS (SELECT 1 FROM UserUnits uu WHERE uu.UserId=u.Id AND uu.IsActive=1)`
     - Harap **0 baris** → `.Any()` murni final (sesuai lean). Bila >0 → STOP, eskalasi (kemungkinan gap backfill 399, JANGAN diam-diam tambah fallback).
  3. **Seed fixture {X,Y}** (Seed Workflow): snapshot DB lokal (`BACKUP DATABASE`) → catat `docs/SEED_JOURNAL.md` (tujuan="UAT MU-06 set-aware", klasifikasi=temporary+local-only, entitas=Users+UserUnits) → insert 1 pekerja 1 Bagian anggota 2 unit aktif (UnitX primary + UnitY sekunder, mirror `Users.Unit=UnitX`).
  4. **`dotnet run`** → http://localhost:5277 → buka **/CMP/Records → Team View** (Bagian fixture):
     a. **Tanpa filter unit:** baris pekerja {X,Y} muncul **1 baris** (bukan 2); kolom Unit tampil **"UnitX, UnitY"** (primary-first comma-join, teks polos, no badge).
     b. **Filter unit = UnitX:** pekerja muncul; kolom Unit tampil **"UnitX"**.
     c. **Filter unit = UnitY (non-primary):** pekerja **tetap muncul** (set-aware ✓); kolom Unit tampil **"UnitY"**.
     d. **Worker count / pagination** akurat (1/pekerja — tak menggelembung).
  5. **No-drift D1=b (SC#3):** buka halaman CMP analytics/dashboard yang pakai GroupBy Section (mis. tampilan ringkasan completion per Bagian) → angka **IDENTIK** sebelum/sesudah (analytics pakai `s.User.Unit` scalar, tak lewat GetWorkersInSection). Team View **tanpa** filter unit tak berubah jumlah pekerja.
  6. **Consumer #4 (opsional, bila mudah):** /Admin Kelola Data Section C (ManageAssessmentTab) filter unit → pekerja multi-unit muncul di tiap unit (konsisten MU-06, benign).
  7. **Restore DB** lokal (`RESTORE DATABASE`) setelah selesai (sukses ATAU gagal) → tandai `docs/SEED_JOURNAL.md` = `cleaned`.
  8. SQL-real (EXISTS translation + count akurat di SQLEXPRESS) + UAT browser penuh = **DEFER Phase 404** — di sini cukup `dotnet run` manual + cek DB lokal.
  </how-to-verify>
  <action>Jalankan UAT lokal sesuai how-to-verify: build + full suite, anomali-backfill check (putuskan OR-fallback), seed fixture {X,Y} (Seed Workflow snapshot→journal), `dotnet run` localhost:5277 verifikasi set-aware + kolom kontekstual + dedup + no-drift D1=b, lalu restore DB + tandai journal cleaned. JANGAN commit sebelum verifikasi lolos (CLAUDE.md Develop Workflow). migration=FALSE.</action>
  <verify>Manual runtime verification (checkpoint blocking) — automated safety net `dotnet build` + `dotnet test HcPortal.Tests` sudah hijau di Task 2.</verify>
  <done>Set-aware (filter X & Y), kolom kontekstual ("X" filtered / "X, Y" unfiltered), dedup (1 baris/pekerja), dan no-drift D1=b (analytics IDENTIK) terverifikasi runtime di localhost:5277; anomali-backfill = 0; DB lokal di-restore + SEED_JOURNAL cleaned; user ketik "approved".</done>
  <resume-signal>Ketik "approved" bila set-aware + kolom kontekstual + dedup + no-drift terverifikasi runtime dan DB lokal sudah di-restore, atau jelaskan masalah yang ditemukan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CMP/Worker controller | Operator (Admin/HC/Section Head) kirim `unitFilter`/`sectionFilter` sebagai query param ke listing read-path |
| Controller/Service → EF Core → SQL Server | `unitFilter` masuk ke predikat correlated subquery terhadap junction `UserUnits` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-400-01 | Tampering (SQLi) | `unitFilter` di predikat `_context.UserUnits.Any(uu.Unit == unitFilter && uu.IsActive)` (WorkerDataService:254, WorkerController:202/300) | mitigate | EF Core parameterized query — `unitFilter` di-bind sebagai parameter SQL (`@p`), BUKAN string-concat → `WHERE EXISTS(... AND Unit=@p)`. By-default safe. [VERIFIED idiom existing] |
| T-400-02 | Elevation of Privilege (scope-widening / broken access control) | Set-aware predikat memperluas match dari primary→semua-unit | mitigate | Perluasan TETAP dalam Section yang sudah ter-otorisasi: junction `UserUnits` semua anak 1 Bagian (invariant #1 v32.3), tak lintas-Bagian. RBAC existing tak diubah — `RecordsTeamPartial:810` `roleLevel>=5 Forbid`, L4 section-lock `:813`, `ExportWorkers [Authorize(Roles="Admin,HC")]`. Predikat tak menyentuh authz/Section filter. |
| T-400-03 | Information Disclosure (kolom Unit bocor unit pekerja lain) | Kolom kontekstual `WorkerTrainingStatus.Unit` (comma-join all-active units) | mitigate | Kolom hanya menampilkan unit milik **pekerja itu sendiri** (dari `unitsByUser` yang di-key `userId` pekerja baris itu) — tak ada cross-user leak. Filtered case = `unitFilter` yang sudah lolos predikat keanggotaan (Pitfall #4: hanya anggota aktif unitFilter yang lolos → menampilkan unitFilter selalu benar). |
| T-400-04 | Information Disclosure (unit deactivated muncul) | Batch-load dict + predikat | mitigate | Filter `&& uu.IsActive` (D-03) di KEDUA predikat dan batch-load → unit yang sudah di-deactivate (jalur MU-07) tak muncul di roster maupun kolom. |

**Verdict:** Tidak ada threat surface baru (read/filter-path, 0 endpoint baru, 0 input baru, 0 perubahan auth). Semua threat ter-mitigate oleh kontrol existing. **threats_open: 0** (ASVS L1).
</threat_model>

<verification>
**Per task commit (Sampling Rate — VALIDATION.md):**
- `dotnet build` (0 error — tangkap Pitfall #1 nav-prop) + `dotnet test --filter "FullyQualifiedName~WorkerDataServiceSearchTests"`

**Per wave merge:**
- `dotnet test HcPortal.Tests` (full suite ≥366 + ~7 MU-06 baru hijau, 0 regresi)

**Phase gate (sebelum /gsd-verify-work):**
- Full suite hijau + `dotnet run` localhost:5277 cek fixture {X,Y} (filter X & Y muncul, kolom kontekstual benar, rollup dedup) + cek DB lokal (anomali backfill = 0)
- Diff bersih di path analytics (`CMPController:2581/:2589`), Team View (`:543`), `_RecordsTeamBody.cshtml`, `Models/WorkerTrainingStatus.cs`, `Services/IWorkerDataService.cs`, `FakeWorkerDataService.cs`

**Consumer #4 verification (temuan kritis #2):** Konfirmasi `AssessmentAdminController:278` (ManageAssessmentTab) mewarisi set-aware otomatis (no code change) — masuk how-to-verify checkpoint langkah 6.

**Defer ke Phase 404:** SQL-real EXISTS translation + pagination count di SQLEXPRESS + UAT browser penuh.
</verification>

<success_criteria>
1. **SC#1 set-aware:** pekerja {X,Y} muncul saat filter unit-X DAN filter unit-Y (test `MultiUnitWorker_AppearsInBothUnitFilters_SetAware` hijau + runtime fixture).
2. **SC#2 dedup:** tanpa filter, pekerja {X,Y} == 1 baris (test `MultiUnitWorker_SingleRow_NoFilter` `Count==1` + runtime worker count akurat); TANPA `.Distinct()`.
3. **SC#3 no-drift D1=b:** `Scope_Null_NoFilter_BackwardCompat` tetap hijau + diff TIDAK menyentuh analytics (`CMPController:2581/:2589`) & Team View call (`:543`) + angka analytics runtime IDENTIK.
4. **SC#4 build/run/DB:** `dotnet build` 0 error + `dotnet test` full hijau + fixture {X,Y} tampil di filter X & Y + kolom kontekstual ("X" filtered / "X, Y" unfiltered) + rollup Bagian dedup terverifikasi DB lokal.
5. D-02/D-03/D-04/D-05 ter-cover unit test (kontekstual filtered/unfiltered, IsActive exclude, primary-first, fallback).
6. migration=FALSE; 0 perubahan markup view; 0 perubahan signature/model/interface.
</success_criteria>

<output>
After completion, create `.planning/phases/400-membership-listing-set-aware-rollup-dedup/400-01-SUMMARY.md`.

Catat di SUMMARY: hasil anomali-backfill check (0 / >0), keputusan final OR-fallback (lean `.Any()` murni bila 0), consumer #4 verification result, dan reminder **notify IT migration=FALSE** saat milestone push (Phase 400 = 0 migration; satu-satunya migration v32.3 = Phase 399 `AddUserUnitsTable` `fc015f4d`).
</output>
