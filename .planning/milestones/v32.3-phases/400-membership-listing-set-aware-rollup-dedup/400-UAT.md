---
status: complete
phase: 400-membership-listing-set-aware-rollup-dedup
source: [400-01-SUMMARY.md, 400-REVIEW-FIX.md]
started: 2026-06-18T00:00:00Z
updated: 2026-06-18T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Set-aware filter unit NON-primary + exclude unit non-aktif (regresi inti pasca-fix)
expected: Filter unit NON-primary milik pekerja multi-unit → pekerja tetap muncul (set-aware EXISTS); filter unit non-aktif → pekerja TIDAK muncul (D-03); filter primary → tetap muncul.
result: pass
evidence: |
  /Admin/ManageWorkers?sectionFilter=GAST (set-aware predikat sama dgn GetWorkersInSection), fixture Iwan (iwan3@pertamina.com) 3 baris UserUnits: Alkylation (065) primary/aktif, RFCC NHT (053) sekunder/aktif, Sulfur Recovery (169) sekunder/NON-aktif.
  - unitFilter=RFCC NHT (053) [NON-primary, aktif] → totalRows=1, Iwan PRESENT (set-aware EXISTS; pre-fix scalar tak match). ✓
  - unitFilter=Sulfur Recovery Unit (169) [NON-aktif] → totalRows=0, Iwan ABSENT (D-03 IsActive exclude). ✓
  - unitFilter=Alkylation Unit (065) [primary] → totalRows=6, Iwan PRESENT. ✓

### 2. WR-02 — Export Excel sembunyikan unit non-aktif
expected: Export Excel kolom "Unit" pekerja yang punya UserUnits IsActive=0 → TIDAK menampilkan unit non-aktif (hanya unit aktif primary-first). Sebelum fix a0b9468d, unit non-aktif ikut terbawa.
result: pass
evidence: |
  GET /Admin/ExportWorkers?sectionFilter=GAST (browser terotentikasi, fetch xlsx 7091 bytes, MIME spreadsheetml.sheet valid 10 entri). Inflate xl/sharedStrings.xml (DecompressionStream deflate-raw) → string-content:
  - "RFCC NHT (053)" PRESENT (unit aktif sekunder Iwan tampil). ✓
  - "Alkylation Unit (065)" PRESENT (primary). ✓
  - "Sulfur Recovery Unit (169)" ABSENT (unit non-aktif TIDAK terbawa di Excel). ✓
  Log SQL runtime mengonfirmasi batch-load exportUnitsByUser dieksekusi `WHERE ... AND [u].[IsActive] = CAST(1 AS bit)`.

### 3. IN-01 — Badge "semua unit" ManageWorkers sembunyikan unit non-aktif
expected: /Admin/ManageWorkers tanpa filter unit → badge "semua unit" pekerja dgn UserUnits IsActive=0 → unit non-aktif TIDAK tampil (hanya unit aktif). Sebelum fix 764397c6, unit non-aktif tampil di badge.
result: pass
evidence: |
  /Admin/ManageWorkers?sectionFilter=GAST (tanpa unitFilter), badge unit Iwan (isolasi via pola kode unit 3-digit):
  - Badge = ["Alkylation Unit (065) ...Utama", "RFCC NHT (053)"] — hanya 2 unit AKTIF, primary ditandai "Utama". ✓
  - "Sulfur Recovery Unit (169)" (non-aktif) TIDAK ada di badge. ✓

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0

## Method

Orchestrator-driven (Playwright MCP + SQL), patuh CLAUDE.md Develop Workflow + Seed Workflow. migration=FALSE.
- Snapshot → seed fixture (2 baris UserUnits Iwan: 1 aktif + 1 NON-aktif) → RESTORE WITH REPLACE.
- Snapshot: HcPortalDB_Dev_pre400uat2.bak (dihapus). Journal docs/SEED_JOURNAL.md → cleaned.
- Restore verified: UserUnits=6 baseline, Iwan=1 row (Alkylation primary). Fixture fully reverted.
- App localhost:5277 (Authentication__UseActiveDirectory=false), admin@pertamina.com terotentikasi.
- Verifikasi via real SQL Server (EXISTS translation + active-only batch-load), bukan in-memory.

## Gaps

[none]
