---
phase: 403-organizationcontroller-cascade-guard-userunits-aware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/OrganizationController.cs
  - HcPortal.Tests/OrganizationControllerTests.cs
autonomous: true
requirements: [ORG-01, ORG-02]
tags: [organization, cascade, userunits, multi-unit, guard]

must_haves:
  truths:
    - "Rename unit Level>=1 me-rename SEMUA baris UserUnits.Unit==oldName (termasuk membership sekunder & IsActive=false), dan mirror ApplicationUser.Unit baris primary tetap konsisten"
    - "DeleteOrganizationUnit menolak unit yang masih jadi membership sekunder aktif (UserUnits.IsActive) dengan pesan spesifik 'sekunder'"
    - "ToggleOrganizationUnitActive deactivate-branch menolak unit yang masih jadi membership sekunder aktif"
    - "Reparent unit cross-Bagian di-hard-BLOCK bila ada pekerja yang UserUnits-nya terpecah >1 Bagian; pesan blok menyebut NIP/nama pekerja"
    - "Reparent single-unit worker (tanpa split) tetap DIIZINKAN + Section ter-update (regresi perilaku existing L235-265 dipertahankan)"
    - "PreviewEditCascade memancarkan affectedUserUnitsCount yang nilainya == jumlah baris UserUnits yang benar-benar di-rename EditOrganizationUnit (preview==actual)"
    - "Seluruh cascade EditOrganizationUnit terbungkus BeginTransactionAsync (atomic; rollback bila split-block)"
  artifacts:
    - path: "Controllers/OrganizationController.cs"
      provides: "rename UserUnits cascade + split-detect block + 2 guard scan + affectedUserUnitsCount + tx wrap"
      contains: "_context.UserUnits"
    - path: "HcPortal.Tests/OrganizationControllerTests.cs"
      provides: "~6 test ORG-01/02 + MakeController() suppress TransactionIgnoredWarning"
      contains: "TransactionIgnoredWarning"
  key_links:
    - from: "OrganizationController.EditOrganizationUnit (rename Level>=1)"
      to: "_context.UserUnits.Where(uu => uu.Unit == oldName)"
      via: "rename string loop (no IsActive filter)"
      pattern: "_context\\.UserUnits\\.Where\\(uu => uu\\.Unit == oldName\\)"
    - from: "OrganizationController.PreviewEditCascade"
      to: "affectedUserUnitsCount JSON field"
      via: "CountAsync(uu => uu.Unit == oldName) Level>=1"
      pattern: "affectedUserUnitsCount"
    - from: "OrganizationController.DeleteOrganizationUnit / ToggleOrganizationUnitActive"
      to: "_context.UserUnits.AnyAsync(uu => uu.Unit == name && uu.IsActive)"
      via: "OR-clause ke guard Users existing"
      pattern: "_context\\.UserUnits\\.AnyAsync\\(uu => uu\\.Unit == .*&& uu\\.IsActive\\)"
---

<objective>
Buat `OrganizationController` **sadar junction `UserUnits`** (fondasi Phase 399) di 4 operasi yang saat ini HANYA menyentuh scalar `Users.Unit`/`Users.Section`:

1. **Rename cascade (ORG-01, D-04):** rename unit Level>=1 meng-update SEMUA baris `UserUnits.Unit==oldName` (incl sekunder), jaga mirror.
2. **Reparent split-detect hard-BLOCK (ORG-02, D-01):** blok HANYA bila pekerja akan terpecah >1 Bagian; single-unit reparent tetap diizinkan.
3. **Delete + Deactivate guard (ORG-01, D-02):** scan `UserUnits` aktif (membership sekunder) di `DeleteOrganizationUnit` (L447) + `ToggleOrganizationUnitActive` (L391).
4. **PreviewEditCascade (ORG-02, D-03):** tambah field JSON `affectedUserUnitsCount` (preview==actual).
5. **Atomicity (D-04):** bungkus seluruh cascade `EditOrganizationUnit` dalam `BeginTransactionAsync`.

Plus extend `OrganizationControllerTests.cs` dengan ~6 test (Wave-0 RED → GREEN).

Purpose: Tutup risiko split-brain Section saat reparent (spec §10 risiko-b) + cegah orphan nama unit di membership sekunder + jaga Invariant #1 (1 Bagian/akun) & #3 (primary mirror).
Output: `Controllers/OrganizationController.cs` (backend) + `HcPortal.Tests/OrganizationControllerTests.cs` (~6 test + suppress warning). 0 migration.

**File isolation (Wave-1 paralel — CRITICAL):** Plan ini HANYA menyentuh 2 file di atas. JANGAN sentuh `WorkerController`/`WorkerDataService` (Phase 400), `CoachMapping`/`CDP`/`ProtonData` (Phase 401), atau helper shared. **D-04a: recompute mirror INLINE — JANGAN import/panggil helper `WorkerController`** (hindari compile-dependency lintas-controller / merge-conflict). View + JS (`ManageOrganization.cshtml`, `orgTree.js`) = Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-CONTEXT.md
@.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-RESEARCH.md
@.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-PATTERNS.md
@.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang executor butuh — JANGAN eksplorasi codebase, sudah diekstrak di sini. -->

From Models/UserUnit.cs (junction sejak 399 — TIDAK ada nav-prop balik di ApplicationUser):
```csharp
public class UserUnit {
    public int Id { get; set; }
    public string UserId { get; set; } = "";   // FK -> AspNetUsers.Id
    [MaxLength(200)] public string Unit { get; set; } = "";  // NAME-string, anak Section
    public bool IsPrimary { get; set; } = false;  // tepat 1 per user (filtered-unique index)
    public bool IsActive { get; set; } = true;
    public ApplicationUser? User { get; set; }   // nav SATU ARAH
}
```
AKSES via `_context.UserUnits` correlated (CS1061 bila pakai nav-prop `u.UserUnits` — lesson 400-01).

From Data/ApplicationDbContext.cs:121-136 (untuk split-detect map unit->Bagian):
```csharp
public async Task<Dictionary<string, List<string>>> GetSectionUnitsDictAsync();
// returns Bagian(name) -> List<unit-anak name>; HANYA unit IsActive=true (catatan: unit org non-aktif tak masuk map)
```

From Controllers/OrganizationController.cs — touchpoint AKTUAL (line verified 2026-06-18):
- `EditOrganizationUnit(int id, string name, int? parentId)` POST L129; rename scalar Level0 L200-215 / Level>=1 L216-231; reparent walk+cascade L235-265; `unit.Name = name.Trim(); await _context.SaveChangesAsync();` L267-268; msg+return L270-276.
- `PreviewEditCascade(int id, string name, int? parentId)` POST L283; count block L298-324; return Json L326-333.
- `ToggleOrganizationUnitActive(int id)` POST L364; deactivate-branch `if (unit.IsActive)` L385; guard `hasActiveUsers` L387-399.
- `DeleteOrganizationUnit(int id)` POST L416; guard `hasUsers` L447-454.
- `IsAjaxRequest()` helper → dua jalur respons: `Json(new { success, message })` (AJAX) ATAU `TempData["Error/Success"] + RedirectToAction(...)` (non-AJAX).

From Controllers/WorkerController.cs:665 — idiom transaksi 399 (TIRU idiom, JANGAN panggil helper-nya):
```csharp
using var tx = await _context.Database.BeginTransactionAsync();
// ... mutasi ...
await _context.SaveChangesAsync();
await tx.CommitAsync();
```

From HcPortal.Tests/OrganizationControllerTests.cs:19-49 — seam test siap pakai:
```csharp
private static (OrganizationController ctrl, ApplicationDbContext ctx) MakeController() {
    var options = new DbContextOptionsBuilder<ApplicationDbContext>()
        .UseInMemoryDatabase(Guid.NewGuid().ToString())
        .Options;
    var ctx = new ApplicationDbContext(options);
    var auditLog = new AuditLogService(ctx);
    var ctrl = new OrganizationController(ctx, null!, auditLog, null!);  // _userManager/_env null-substitute
    var httpContext = new DefaultHttpContext();
    httpContext.Request.Headers["X-Requested-With"] = "XMLHttpRequest";  // -> IsAjaxRequest()==true -> Json
    ctrl.ControllerContext = new ControllerContext { HttpContext = httpContext };
    return (ctrl, ctx);
}
// extractor: GetSuccess(r) -> bool "success"; GetInt(r,prop) -> int; GetBool(r,prop) -> bool
```
Pola preview-parity existing: `PreviewEditCascade_RenameLevel1_CountMatchesActual` (L137-174) — seed → ACT1 Preview → ACT2 Edit pada ctx SAMA → assert preview==actual.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED — extend OrganizationControllerTests.cs (~6 test) + suppress TransactionIgnoredWarning di MakeController()</name>
  <files>HcPortal.Tests/OrganizationControllerTests.cs</files>
  <read_first>
    - HcPortal.Tests/OrganizationControllerTests.cs (seluruh file — pola MakeController L19-49, extractor, pola preview-parity L94-174, komentar casing Pitfall 5 L4)
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-PATTERNS.md (§"HcPortal.Tests" + §"Shared Patterns" Transaksi+InMemory + Filter IsActive)
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-RESEARCH.md (§"Common Pitfalls" Pitfall 1/2/5 + §"Validation Architecture" Wave 0 Gaps)
    - Models/UserUnit.cs (schema: UserId/Unit/IsPrimary/IsActive)
  </read_first>
  <behavior>
    Test 1 — `EditOrganizationUnit_RenameLevel1_RenamesAllUserUnitsRows`: seed Bagian "RFCC"(L0,Id1) + unit "Alkylation"(L1,Id2,parent1); seed UserUnits: u1 primary aktif "Alkylation", u2 sekunder aktif "Alkylation" (primary u2="Lain"), u3 "Alkylation" IsActive=FALSE (diskriminatif); seed Users u1.Unit="Alkylation"(mirror primary). ACT: rename unit Id2 → "Alkylation New". ASSERT: SEMUA 3 baris UserUnits kini "Alkylation New" (incl IsActive=false), 0 baris masih "Alkylation"; baris primary u1 IsPrimary tetap true; Users.Single(u=>u.Id=="u1").Unit=="Alkylation New" (mirror konsisten).
    Test 2 — `DeleteOrganizationUnit_SecondaryMembershipActive_Rejected`: seed unit "Alkylation"(L1); seed UserUnits sekunder aktif "Alkylation" TANPA scalar Users.Unit/Section=="Alkylation" (kasus murni sekunder). ACT: DeleteOrganizationUnit. ASSERT: `GetSuccess==false`; pesan mengandung kata "sekunder"; unit MASIH ada di ctx.
    Test 3 — `ToggleOrganizationUnitActive_SecondaryMembershipActive_Rejected`: seed unit aktif "Alkylation"(L1) + UserUnits sekunder aktif "Alkylation" tanpa scalar. ACT: Toggle. ASSERT: `GetSuccess==false`; pesan mengandung "sekunder"; unit.IsActive MASIH true.
    Test 4 — `EditOrganizationUnit_ReparentSplitsWorker_Blocked`: seed 2 Bagian "RFCC"(Id1,L0) + "HSC"(Id5,L0); unit "Alkylation"(Id2,L1,parent1) + unit-lain "X"(Id6,L1,parent1,anak RFCC), semua IsActive=true di org tree; pekerja P1 (NIP "12345", FullName "Budi") UserUnits aktif: "Alkylation"(primary) + "X"(sekunder, Bagian RFCC). ACT: reparent "Alkylation" (Id2) → parentId HSC(Id5) → newSectionName "HSC", sedang unit-lain "X" Bagian-nya "RFCC" (≠"HSC") → split. ASSERT: `GetSuccess==false`; pesan mengandung NIP "12345" ATAU "Budi".
    Test 5 — `EditOrganizationUnit_ReparentSingleUnitWorker_Allowed`: seed 2 Bagian "RFCC"(Id1) + "HSC"(Id5) + unit "Alkylation"(L1,parent RFCC Id1); pekerja P1 single-unit hanya "Alkylation" (UserUnits 1 baris primary) + Users.Unit="Alkylation",Section="RFCC". ACT: reparent "Alkylation"→parentId HSC(Id5). ASSERT: `GetSuccess==true`; Users P1.Section kini "HSC" (cascade Section existing dipertahankan).
    Test 6 — `PreviewEditCascade_RenameLevel1_UserUnitsCountMatchesActual`: seed unit "Alkylation"(L1) + UserUnits: 2 aktif + 1 IsActive=FALSE (semua Unit="Alkylation", diskriminatif Pitfall 2). ACT1: Preview rename → baca affectedUserUnitsCount. ACT2: Edit rename pada ctx sama → hitung baris UserUnits yg kini "Alkylation New". ASSERT: affectedUserUnitsCount==jumlah aktual==3 (SEMUA baris, tanpa filter IsActive).
  </behavior>
  <action>
1. **Suppress TransactionIgnoredWarning (opsi B — WAJIB, Pitfall 1):** Test existing `PreviewEditCascade_RenameLevel1_CountMatchesActual` (L161) memanggil `EditOrganizationUnit` langsung; setelah Task 2 membungkusnya dalam `BeginTransactionAsync`, test itu + test baru yang panggil Edit akan GAGAL InMemory. Edit `MakeController()` (L21-23) tambah `.ConfigureWarnings(...)`:
   ```csharp
   var options = new DbContextOptionsBuilder<ApplicationDbContext>()
       .UseInMemoryDatabase(Guid.NewGuid().ToString())
       .ConfigureWarnings(w => w.Ignore(Microsoft.EntityFrameworkCore.Diagnostics.InMemoryEventId.TransactionIgnoredWarning))
       .Options;
   ```
   (InMemory abaikan tx = no-op; operasi tetap jalan → parity teruji. Atomicity = domain SQL Phase 404, JANGAN duplikat di sini.)

2. **Tambah ~6 [Fact] test** di akhir class `OrganizationControllerTests` (sebelum `}` penutup), meniru struktur `PreviewEditCascade_RenameLevel1_CountMatchesActual`. Seed via `ctx.OrganizationUnits.AddRange(...)`, `ctx.Users.AddRange(...)`, `ctx.UserUnits.AddRange(...)`, `ctx.SaveChanges()`.

   Contoh seed UserUnits (casing IDENTIK — Pitfall 5):
   ```csharp
   ctx.UserUnits.AddRange(
       new UserUnit { UserId = "u1", Unit = "Alkylation", IsPrimary = true,  IsActive = true },
       new UserUnit { UserId = "u2", Unit = "Alkylation", IsPrimary = false, IsActive = true },
       new UserUnit { UserId = "u3", Unit = "Alkylation", IsPrimary = false, IsActive = false }); // diskriminatif
   ```

   Untuk Test 1 mirror-assert: seed `new ApplicationUser { Id = "u1", UserName = "u1", Unit = "Alkylation" }` lalu setelah Edit assert `Assert.Equal("Alkylation New", ctx.Users.Single(u => u.Id == "u1").Unit);`.

   Untuk Test 2/3 pesan "sekunder": ekstrak message dari JsonResult — tambah helper inline atau pakai refleksi mirip GetSuccess:
   ```csharp
   var j = Assert.IsType<JsonResult>(result);
   var msg = (string)j.Value!.GetType().GetProperty("message")!.GetValue(j.Value)!;
   Assert.Contains("sekunder", msg);
   ```
   (Bila helper `GetMessage` belum ada, tambahkan satu private static helper meniru `GetSuccess`.)

   Untuk Test 4 (split): atur fixture agar `GetSectionUnitsDictAsync()` memetakan unit-lain pekerja ke Bagian ≠ newSectionName. Seed SEMUA OrganizationUnit yang dipakai dengan IsActive=true (GetSectionUnitsDictAsync HANYA ambil unit IsActive=true). Reparent unit Id2 ke parentId Bagian "HSC"; pekerja punya UserUnits aktif unit-lain "X" yang anak Bagian "RFCC". Assert `Assert.Contains("12345", msg)` ATAU `Assert.Contains("Budi", msg)`.

3. **State RED:** test baru kompilasi tetapi GAGAL (logika belum ada di controller). Jalankan untuk konfirmasi RED. `using HcPortal.Models;` sudah ada di file.

4. Commit: `test(403): add failing UserUnits cascade/guard tests + suppress InMemory tx warning`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.Tests 2>&1 | grep -q "Build succeeded" && echo BUILD_OK; dotnet test --filter "FullyQualifiedName~OrganizationController" --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `OrganizationControllerTests.cs` mengandung string `TransactionIgnoredWarning` (grep verifiable)
    - `OrganizationControllerTests.cs` mengandung 6 nama test baru: `RenamesAllUserUnitsRows`, `DeleteOrganizationUnit_SecondaryMembershipActive_Rejected`, `ToggleOrganizationUnitActive_SecondaryMembershipActive_Rejected`, `ReparentSplitsWorker_Blocked`, `ReparentSingleUnitWorker_Allowed`, `UserUnitsCountMatchesActual` (grep verifiable)
    - `OrganizationControllerTests.cs` mengandung `ctx.UserUnits.AddRange` (fixture pakai junction)
    - Minimal satu fixture menyeed `IsActive = false` (diskriminatif Pitfall 2) — grep `IsActive = false`
    - `dotnet build HcPortal.Tests` exit 0 (test kompilasi)
    - 6 test baru RED (gagal) pada run ini — logika controller belum ada. Test existing `PreviewEditCascade_*` masih HIJAU (suppress warning sudah aktif sebelum Task 2 → masih no-tx, tetap hijau)
  </acceptance_criteria>
  <done>6 test baru ada & kompilasi & RED; `MakeController()` suppress `TransactionIgnoredWarning`; test existing tidak regresi. Commit RED dibuat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement 4 perubahan UserUnits-aware di OrganizationController.cs (rename cascade + split-block + 2 guard + affectedUserUnitsCount) + wrap tx</name>
  <files>Controllers/OrganizationController.cs</files>
  <read_first>
    - Controllers/OrganizationController.cs (L120-340 EditOrganizationUnit + PreviewEditCascade; L360-410 Toggle; L412-468 Delete — line aktual verified)
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-PATTERNS.md (§"OrganizationController.cs" Sub-1..Sub-6 — code excerpt copy-paste-ready + verified line numbers)
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-RESEARCH.md (§"Code Examples" #1/#2/#3/#4 + §"Common Pitfalls" Pitfall 1/2/3 + Pattern 1/2/3/4)
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-CONTEXT.md (D-01/D-01a/D-01b, D-02/D-02a/D-02b, D-03/D-03a, D-04/D-04a)
    - Data/ApplicationDbContext.cs:109-136 (GetSectionUnitsDictAsync / GetUnitsForSectionAsync)
  </read_first>
  <action>
**ATURAN FILTER TEGAS (anti-drift, Pitfall 2 — PARITY RULE):**
- Rename (Sub-1) + Preview count (Sub-4) = `Where/CountAsync(uu => uu.Unit == oldName)` **TANPA `IsActive`** (parity WAJIB identik).
- Split-detect (Sub-2) + Delete guard (Sub-5) + Deactivate guard (Sub-6) = **`&& uu.IsActive`**.
- SELALU `_context.UserUnits` correlated, JANGAN nav-prop `u.UserUnits` (CS1061, lesson 400-01).

**Sub-1 — Rename cascade UserUnits (ORG-01, di EditOrganizationUnit cabang `else` Level>=1):**
Tepat SETELAH blok scalar `Users.Unit` (setelah L220 `foreach (var u in affectedUsers) u.Unit = name.Trim();`), tambah:
```csharp
// NEW 403 (ORG-01, D-04): rename SEMUA baris UserUnits (incl sekunder, incl IsActive=false).
// IsPrimary TIDAK disentuh -> baris primary ikut rename = mirror Users.Unit (L219) tetap konsisten (Invariant #3).
var affectedUnitRows = await _context.UserUnits.Where(uu => uu.Unit == oldName).ToListAsync();
foreach (var uu in affectedUnitRows) uu.Unit = name.Trim();
cascadedUserUnits += affectedUnitRows.Count;
```
Deklarasikan `int cascadedUserUnits = 0;` di scope method (dekat `cascadedUsers`/`cascadedMappings` L194-195) agar bisa dipakai di pesan. Cabang Level0 → biarkan 0 (UserUnits hanya berisi nama unit Level>=1).

**Sub-2 — Reparent split-detection + hard-BLOCK (ORG-02, D-01/D-01a):** Di cabang reparent `if (oldParentId != parentId && unit.Level >= 1)` (L235), SETELAH walk menghitung `newSectionName` (L246) tetapi SEBELUM mutasi Section existing (sebelum L249 `if (!string.IsNullOrEmpty(newSectionName))`), sisipkan:
```csharp
// NEW 403 (ORG-02, D-01a): blok HANYA bila ada pekerja yang akan terpecah >1 Bagian. Berbasis UserUnits AKTIF.
if (!string.IsNullOrEmpty(newSectionName))
{
    var memberIds = await _context.UserUnits
        .Where(uu => uu.Unit == oldName && uu.IsActive).Select(uu => uu.UserId).Distinct().ToListAsync();
    var dict = await _context.GetSectionUnitsDictAsync();             // Bagian -> List<unitName> (IsActive only)
    var unitToSection = dict.SelectMany(kv => kv.Value.Select(u => (Unit: u, Section: kv.Key)))
                            .ToDictionary(x => x.Unit, x => x.Section);
    var otherRows = await _context.UserUnits
        .Where(uu => memberIds.Contains(uu.UserId) && uu.IsActive && uu.Unit != oldName).ToListAsync();
    var splitUserIds = otherRows
        .Where(uu => unitToSection.TryGetValue(uu.Unit, out var sec) && sec != newSectionName)
        .Select(uu => uu.UserId).Distinct().ToList();
    if (splitUserIds.Any())
    {
        var names = await _context.Users.Where(u => splitUserIds.Contains(u.Id))
            .Select(u => (u.NIP ?? "") + " - " + (u.FullName ?? u.UserName)).ToListAsync();
        var shown = names.Take(5).ToList();
        var suffix = names.Count > 5 ? ("; dan " + (names.Count - 5) + " lainnya") : "";
        var blokMsg = "Tidak dapat memindahkan unit ke Bagian lain: " + names.Count +
            " pekerja akan terpecah ke >1 Bagian (" + string.Join("; ", shown) + suffix +
            "). Selesaikan keanggotaan lintas-Bagian pekerja tersebut terlebih dahulu.";
        if (IsAjaxRequest()) return Json(new { success = false, message = blokMsg });
        TempData["Error"] = blokMsg; return RedirectToAction("ManageOrganization", new { editId = id });
    }
}
```
**PENTING:** return-early ini di DALAM tx (Sub-3) → dispose tanpa Commit = rollback otomatis. Cascade Section existing (L249-264 `reparentUsers`...) DIPERTAHANKAN tepat di bawahnya — JANGAN hapus. Reparent TIDAK mengubah baris `UserUnits` (D-01b).

**Sub-3 — Transaksi wrap (ORG-01, D-04):** Bungkus seluruh blok mutasi `EditOrganizationUnit`. Sisipkan `using var tx = await _context.Database.BeginTransactionAsync();` SEBELUM blok cascade (sekitar awal L193 / sebelum `int cascadedUsers = 0;`), dan SETELAH `await _context.SaveChangesAsync();` (L268) tambah `await tx.CommitAsync();`. Idiom WorkerController.cs:665 — TIRU, jangan panggil helper. Pre-mutasi guard yang return-early SEBELUM `BeginTransactionAsync` (dup-check L150, circular L160-178) biarkan di atas tx (tak ada mutasi). Yang dibungkus tx = mulai dari Level recompute L180 / cascade L193 sampai SaveChanges+Commit. Tidak perlu try/catch eksplisit (dispose tanpa commit = rollback); bila ingin lebih jelas boleh `try { ... await tx.CommitAsync(); } catch { await tx.RollbackAsync(); throw; }`.

**Sub-4 — PreviewEditCascade affectedUserUnitsCount (ORG-02, D-03/D-03a):** Di `PreviewEditCascade` (L283), SEBELUM `return Json(...)` (L326), tambah:
```csharp
int affectedUserUnits = 0;
if (nameChanged && unit.Level >= 1)                  // Level 0 -> UserUnits tak tersentuh (D-03a)
    affectedUserUnits = await _context.UserUnits.CountAsync(uu => uu.Unit == oldName);  // ALL rows = match Sub-1
// reparent tanpa rename -> UserUnits TIDAK berubah -> 0 (D-01b)
```
Lalu tambah field ke objek Json return (L326-333): `affectedUserUnitsCount = affectedUserUnits`.

**Sub-5 — Delete guard scan UserUnits (ORG-01, D-02/D-02a):** Di `DeleteOrganizationUnit` (L447), ganti blok `hasUsers`:
```csharp
bool hasUsers = await _context.Users.AnyAsync(u => u.Section == unit.Name || u.Unit == unit.Name);
bool hasUnitMembership = await _context.UserUnits.AnyAsync(uu => uu.Unit == unit.Name && uu.IsActive);  // NEW (D-02b)
if (hasUsers || hasUnitMembership)
{
    var why = (!hasUsers && hasUnitMembership)
        ? "Unit ini masih menjadi keanggotaan (sekunder) sejumlah pekerja yang tidak terlihat di unit utama mereka. Lepaskan keanggotaan unit ini dari pekerja terlebih dahulu."
        : "Unit ini masih memiliki pekerja yang ter-assign. Pindahkan pekerja terlebih dahulu.";
    if (IsAjaxRequest()) return Json(new { success = false, message = why });
    TempData["Error"] = why; return RedirectToAction("ManageOrganization");
}
```
Edge Level 0: nama Bagian tak pernah match `UserUnits.Unit` → scan no-op aman (A2). Tak perlu cabang Level.

**Sub-6 — Deactivate guard scan UserUnits (ORG-01, D-02):** Di `ToggleOrganizationUnitActive` deactivate-branch `if (unit.IsActive)` (L385), setelah `hasActiveUsers` dihitung (L387-391), GANTI blok `if (hasActiveUsers) { ... }` existing (L393-399) dengan versi diperluas:
```csharp
bool hasUnitMembership = await _context.UserUnits.AnyAsync(uu => uu.Unit == unit.Name && uu.IsActive);  // NEW (D-02b)
if (hasActiveUsers || hasUnitMembership)
{
    var why = (!hasActiveUsers && hasUnitMembership)
        ? "Tidak dapat menonaktifkan unit. Unit ini masih menjadi keanggotaan (sekunder) sejumlah pekerja. Lepaskan keanggotaan unit ini dari pekerja terlebih dahulu."
        : "Tidak dapat menonaktifkan unit. Masih ada user aktif yang terdaftar di unit ini. Pindahkan semua user terlebih dahulu.";
    if (IsAjaxRequest()) return Json(new { success = false, message = why });
    TempData["Error"] = why; return RedirectToAction("ManageOrganization");
}
```

**Pesan rename (opsional, konsisten):** boleh perluas msg L270-271 sertakan `cascadedUserUnits` bila >0 (discretion; tak wajib). JANGAN ubah authz/CSRF.

**Authz/CSRF (WAJIB pertahankan — security gate):** JANGAN hapus/ubah `[Authorize(Roles = "Admin, HC")]`, `[HttpPost]`, `[ValidateAntiForgeryToken]` di Edit (L127-128), Preview (L281-282), Toggle (L362-363), Delete (L414-415).

**Anti-pattern (JANGAN):** recompute/promote primary saat rename (rename tak ubah set/IsPrimary — D-04); filter `IsActive` di rename/preview (rusak parity); import helper WorkerController (D-04a); nav-prop `u.UserUnits`.

Jalankan test → 6 test baru HIJAU + existing tidak regresi. Commit: `feat(403): UserUnits-aware cascade rename/guard/split-block + preview count + tx (ORG-01/02)`.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -qE "Build succeeded|0 Error" && echo BUILD_OK; dotnet test --filter "FullyQualifiedName~OrganizationController" --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `OrganizationController.cs` mengandung `_context.UserUnits.Where(uu => uu.Unit == oldName)` (rename cascade, no IsActive — grep verifiable)
    - `OrganizationController.cs` mengandung `_context.UserUnits.AnyAsync(uu => uu.Unit == unit.Name && uu.IsActive)` (2 guard, dengan IsActive — grep, minimal 2 occurrence)
    - `OrganizationController.cs` mengandung `affectedUserUnitsCount` (preview field — grep verifiable)
    - `OrganizationController.cs` mengandung `GetSectionUnitsDictAsync` (split-detect — grep verifiable)
    - `OrganizationController.cs` mengandung `BeginTransactionAsync` + `CommitAsync` (tx wrap — grep verifiable)
    - `OrganizationController.cs` mengandung kata "sekunder" (pesan guard D-02a) DAN "terpecah" (pesan split-block D-01a) — grep verifiable
    - Tidak ada string `u.UserUnits` (nav-prop salah) di file — grep `\.UserUnits` HANYA muncul sebagai `_context.UserUnits`
    - Authz utuh: grep `[Authorize(Roles = "Admin, HC")]` masih muncul minimal 4x; `[ValidateAntiForgeryToken]` masih muncul minimal 4x (tak ada yang hilang dari Edit/Preview/Toggle/Delete)
    - `dotnet build` exit 0 (0 error)
    - `dotnet test --filter "FullyQualifiedName~OrganizationController"` exit 0 (SEMUA test OrganizationController hijau, incl 6 baru + existing PreviewEditCascade)
  </acceptance_criteria>
  <done>4 perubahan UserUnits-aware ter-implement + tx wrap; 6 test baru hijau; existing tidak regresi; authz/CSRF utuh; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Admin/HC) → OrganizationController POST | Input `id`/`name`/`parentId` melintasi; sudah di belakang RBAC + antiforgery |
| OrganizationController → DB (UserUnits/Users/OrganizationUnits) | Mutasi cascade/guard; parameterized EF LINQ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-403-01 | Tampering (mass-assignment) | reparent `parentId` → unit pindah ke Bagian tak sah / split-brain Section | mitigate | parentId divalidasi `FindAsync` + circular guard `IsDescendantAsync` (existing, dipertahankan) + NEW split-detect hard-block jaga Invariant #1 (Sub-2) |
| T-403-02 | Elevation (privilege bypass) | keempat action mutasi org | mitigate | `[Authorize(Roles = "Admin, HC")]` dipertahankan utuh di Edit/Preview/Toggle/Delete (acceptance criteria grep ≥4×); tak ada endpoint baru |
| T-403-03 | Tampering (CSRF) | semua POST | mitigate | `[ValidateAntiForgeryToken]` dipertahankan di 4 action (acceptance criteria grep ≥4×) |
| T-403-04 | Tampering (SQL injection) | query `UserUnits`/split-detect | mitigate | Parameterized via EF LINQ (`Where(uu => uu.Unit == oldName)`); tak ada raw SQL |
| T-403-05 | Info Disclosure | pesan blok reparent menampilkan NIP/nama pekerja | accept | By-design (D-01a): pesan hanya ditampilkan ke role Admin/HC (sudah authorized, bukan publik). Diperlukan operator untuk menyelesaikan split manual |
| T-403-06 | DoS (over-block) | hard-block semua reparent ber-anggota | accept (avoided) | Ditolak eksplisit (D-01): blok HANYA split nyata >1 Bagian; single-unit reparent tetap diizinkan (Test 5 regresi) |
</threat_model>

<verification>
- `dotnet build` 0 error (gate CLAUDE.md)
- `dotnet test --filter "FullyQualifiedName~OrganizationController" --nologo` exit 0 — 6 test baru + existing hijau
- `dotnet test --nologo` (full suite, per-wave merge) — baseline ≥507 tidak regresi; 3 skip SQLEXPRESS-gated (milik Phase 404) dipertahankan
- grep authz: `[Authorize(Roles = "Admin, HC")]` ≥4 + `[ValidateAntiForgeryToken]` ≥4 di OrganizationController.cs (tak ada yang hilang)
- grep parity: rename + preview pakai `uu.Unit == oldName` TANPA IsActive; guard + split-detect pakai `&& uu.IsActive`
</verification>

<success_criteria>
- ORG-01: rename Level>=1 cascade ke SEMUA baris `UserUnits.Unit` (incl sekunder) + mirror konsisten; delete-guard + deactivate-guard scan `UserUnits` aktif (membership sekunder ditolak)
- ORG-02: reparent cross-Bagian split → hard-BLOCK + pesan sebut NIP/nama; single-unit reparent diizinkan; `PreviewEditCascade.affectedUserUnitsCount` == aktual (preview==actual)
- D-04: seluruh cascade `EditOrganizationUnit` terbungkus transaksi
- Test ~6 hijau (RED→GREEN); full suite tidak regresi; authz/CSRF utuh; 0 migration
</success_criteria>

<output>
After completion, create `.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-01-SUMMARY.md`
</output>
