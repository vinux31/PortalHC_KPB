---
phase: 403-organizationcontroller-cascade-guard-userunits-aware
plan: 02
type: execute
wave: 2
depends_on: ["403-01"]
files_modified:
  - Views/Admin/ManageOrganization.cshtml
  - wwwroot/js/orgTree.js
autonomous: false
requirements: [ORG-02]
tags: [organization, ui, cascade-preview, modal, userunits]

must_haves:
  truths:
    - "Modal #cascadeConfirmModal menampilkan baris ke-5 'X baris keanggotaan unit' (id=cascadeUserUnits) dengan gaya/tipografi/spacing/warna IDENTIK 4 baris sebelahnya"
    - "Baris ke-5 terisi nilai affectedUserUnitsCount dari PreviewEditCascade (server-authoritative; preview==actual)"
    - "Modal #cascadeConfirmModal tetap MUNCUL bila satu-satunya dampak adalah baris UserUnits (mis. rename unit yang hanya punya membership sekunder) — affectedUserUnitsCount ikut penjumlahan total"
  artifacts:
    - path: "Views/Admin/ManageOrganization.cshtml"
      provides: "li baris ke-5 cascadeUserUnits di #cascadeConfirmModal"
      contains: "cascadeUserUnits"
    - path: "wwwroot/js/orgTree.js"
      provides: "populate textContent cascadeUserUnits + tambah affectedUserUnitsCount ke total"
      contains: "affectedUserUnitsCount"
  key_links:
    - from: "orgTree.js showCascadeConfirm(pv)"
      to: "#cascadeUserUnits textContent"
      via: "pv.affectedUserUnitsCount || 0"
      pattern: "cascadeUserUnits.*affectedUserUnitsCount"
    - from: "orgTree.js submitUnitModal() total"
      to: "modal muncul saat dampak UserUnits-only"
      via: "+ (pv.affectedUserUnitsCount || 0)"
      pattern: "affectedUserUnitsCount \\|\\| 0"
---

<objective>
Tambah **SATU baris hitung read-only** ke modal Bootstrap existing `#cascadeConfirmModal` (fitur ORG-TREE-07) untuk menampilkan jumlah baris `UserUnits` terdampak saat rename unit (D-03 — "X baris keanggotaan unit"), plus pastikan modal tetap muncul bila satu-satunya dampak adalah `UserUnits`.

Purpose: Transparansi preview==actual ke operator (Admin/HC) — operator melihat dampak ke membership multi-unit sebelum konfirmasi rename. Melengkapi field JSON `affectedUserUnitsCount` yang dipancarkan Plan 01.
Output: `Views/Admin/ManageOrganization.cshtml` (1 `<li>` baru) + `wwwroot/js/orgTree.js` (populate + 1 term di `total`). 0 backend, 0 migration.

**Scope = MINIMAL DELTA (per 403-UI-SPEC.md):** Tidak ada halaman/komponen/warna/tipografi/modal/ikon baru. Baris baru meniru byte-for-byte gaya saudaranya (`cascadeUsers`/`cascadeMappings`/`cascadeKompetensi`/`cascadeGuidance`). Label literal "baris keanggotaan unit" (BUKAN `@OrgLabels.GetLabel()`).

**File isolation (Wave-1 paralel):** Plan ini HANYA menyentuh 2 file di atas. `orgTree.js` & `ManageOrganization.cshtml` TIDAK disentuh Phase 400/401 (cluster disjoint — aman). JANGAN sentuh `OrganizationController.cs` (Plan 01) atau file lain.

**Dependency:** depends_on 403-01 — butuh field JSON `affectedUserUnitsCount` dari `PreviewEditCascade` (Plan 01 Sub-4). Wave 2 (setelah Plan 01 merge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-UI-SPEC.md
@.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-PATTERNS.md
@.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Markup & JS existing yang executor extend — sudah diekstrak, JANGAN eksplorasi. -->

From Views/Admin/ManageOrganization.cshtml:221-226 (#cascadeConfirmModal <ul> existing):
```html
<ul class="mb-2">
    <li><strong id="cascadeUsers">0</strong> user</li>
    <li><strong id="cascadeMappings">0</strong> mapping coach-coachee</li>
    <li><strong id="cascadeKompetensi">0</strong> kompetensi PROTON</li>
    <li><strong id="cascadeGuidance">0</strong> file panduan</li>
</ul>
```

From wwwroot/js/orgTree.js:361-364 (showCascadeConfirm populate):
```javascript
document.getElementById('cascadeUsers').textContent = pv.affectedUsersCount || 0;
document.getElementById('cascadeMappings').textContent = pv.affectedMappingsCount || 0;
document.getElementById('cascadeKompetensi').textContent = pv.affectedKompetensiCount || 0;
document.getElementById('cascadeGuidance').textContent = pv.affectedGuidanceCount || 0;
```

From wwwroot/js/orgTree.js:426-427 (submitUnitModal total — menentukan apakah modal muncul):
```javascript
const total = (pv.affectedUsersCount || 0) + (pv.affectedMappingsCount || 0)
            + (pv.affectedKompetensiCount || 0) + (pv.affectedGuidanceCount || 0);
if (total > 0) { const proceed = await showCascadeConfirm(pv); if (!proceed) return; }
```

Sumber data (dari Plan 01 — JSON `PreviewEditCascade`): field `affectedUserUnitsCount` (int). Server-authoritative, preview==actual (D-03a).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah baris cascadeUserUnits di view + populate & total di orgTree.js</name>
  <files>Views/Admin/ManageOrganization.cshtml, wwwroot/js/orgTree.js</files>
  <read_first>
    - Views/Admin/ManageOrganization.cshtml (L210-237 — modal #cascadeConfirmModal lengkap)
    - wwwroot/js/orgTree.js (L355-432 — showCascadeConfirm + submitUnitModal)
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-UI-SPEC.md (§"The One New Element — Contract" — markup + JS populate + total, copywriting contract)
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-PATTERNS.md (§"Views/Admin/ManageOrganization.cshtml" + §"wwwroot/js/orgTree.js")
  </read_first>
  <action>
**(a) `Views/Admin/ManageOrganization.cshtml`** — sisipkan SATU `<li>` sebagai baris TERAKHIR di dalam `<ul class="mb-2">` (`#cascadeConfirmModal`), tepat SETELAH baris `cascadeGuidance` (L225):
```html
<li><strong id="cascadeUserUnits">0</strong> baris keanggotaan unit</li>
```
Aturan kontrak (UI-SPEC): id camelCase `cascadeUserUnits` (prefix `cascade`), default literal `0` via `<strong>`, label literal lowercase " baris keanggotaan unit" (1 spasi setelah `</strong>`). **JANGAN** pakai `@OrgLabels.GetLabel()`, **JANGAN** tambah ikon, **JANGAN** tambah class/style/aria baru. Modal header/footer/struktur TIDAK disentuh.

**(b) `wwwroot/js/orgTree.js` — populate (di `showCascadeConfirm(pv)`, setelah L364):**
```javascript
document.getElementById('cascadeUserUnits').textContent = pv.affectedUserUnitsCount || 0;
```

**(c) `wwwroot/js/orgTree.js` — total sum (di `submitUnitModal()`, L426-427):** tambah term ke-5 agar modal muncul saat dampak UserUnits-only (mis. rename unit yang hanya punya membership sekunder):
```javascript
const total = (pv.affectedUsersCount || 0) + (pv.affectedMappingsCount || 0)
            + (pv.affectedKompetensiCount || 0) + (pv.affectedGuidanceCount || 0)
            + (pv.affectedUserUnitsCount || 0);   // NEW 403 (D-03)
```

Tidak ada logika klien lain. Tidak ada perubahan endpoint/AJAX. Verifikasi `dotnet build` (Razor compile) tetap 0 error.

Commit: `feat(403): cascade-confirm modal baris keanggotaan unit (D-03)`.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -qE "Build succeeded|0 Error" && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/ManageOrganization.cshtml` mengandung `id="cascadeUserUnits"` (grep verifiable)
    - `Views/Admin/ManageOrganization.cshtml` mengandung literal `baris keanggotaan unit` (grep verifiable)
    - `Views/Admin/ManageOrganization.cshtml` TIDAK mengandung `@OrgLabels.GetLabel` di baris baru (label literal, bukan helper)
    - `wwwroot/js/orgTree.js` mengandung `document.getElementById('cascadeUserUnits').textContent = pv.affectedUserUnitsCount || 0` (grep verifiable)
    - `wwwroot/js/orgTree.js` mengandung `+ (pv.affectedUserUnitsCount || 0)` di penjumlahan total (grep verifiable)
    - `dotnet build` exit 0 (Razor compile OK)
  </acceptance_criteria>
  <done>Baris ke-5 `cascadeUserUnits` ada di view; orgTree.js populate textContent + tambah term ke total; build 0 error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser — modal preview baris keanggotaan unit + parity (SC#5)</name>
  <files>Views/Admin/ManageOrganization.cshtml, wwwroot/js/orgTree.js</files>
  <read_first>
    - .planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-VALIDATION.md (§"Manual-Only Verifications")
    - CLAUDE.md (Develop Workflow gate + Seed Data Workflow)
    - docs/SEED_WORKFLOW.md (snapshot/restore SQL Server command)
  </read_first>
  <what-built>
    Baris ke-5 "X baris keanggotaan unit" di modal `#cascadeConfirmModal`, terisi `affectedUserUnitsCount` dari PreviewEditCascade (Plan 01), dengan gaya identik 4 baris existing; modal tetap muncul bila dampak hanya UserUnits.
  </what-built>
  <action>
CHECKPOINT — verifikasi manusia (bukan implementasi). Executor MENJALANKAN langkah verifikasi LOKAL berikut lalu PAUSE untuk sign-off user. JANGAN sentuh Dev/Prod.

Lihat `<how-to-verify>` untuk langkah lengkap. Ringkas: snapshot DB lokal → seed fixture multi-unit → `dotnet run` localhost:5277 → rename unit ber-membership → cek modal baris ke-5 + nilai == aktual DB (preview==actual) → cek edge UserUnits-only modal muncul → cek guard delete/reparent tolak → RESTORE DB. Tampilkan hasil ke user, tunggu "approved".
  </action>
  <how-to-verify>
    Jalankan gate CLAUDE.md Develop Workflow (verifikasi LOKAL — JANGAN sentuh Dev/Prod):

    1. **Snapshot DB lokal** `HcPortalDB_Dev` (SEED_WORKFLOW: `sqlcmd ... BACKUP DATABASE`) + catat di `docs/SEED_JOURNAL.md` (tujuan: UAT 403 modal preview; klasifikasi temporary local-only).
    2. **Seed fixture multi-unit** lokal: pilih 1 pekerja, tambahkan baris `UserUnits` sekunder aktif untuk sebuah unit Level-1 (selain primary-nya), casing identik nama unit di org tree. Catat di journal.
    3. `dotnet run` → buka `http://localhost:5277` → login Admin (`admin@pertamina.com`, lihat reference_dev_credentials) → `/Admin/ManageOrganization`.
    4. **Rename unit yang punya membership** (unit yang di-seed di langkah 2) → klik "Simpan Perubahan" → modal `#cascadeConfirmModal` muncul. PERIKSA:
       - Ada baris ke-5 "**{N} baris keanggotaan unit**" di bawah "file panduan".
       - Gaya/tipografi/spacing/warna baris ke-5 **identik** dengan 4 baris di atasnya (plain body text, `<strong>` bold, tanpa ikon/warna khusus).
       - Nilai `{N}` = jumlah baris `UserUnits.Unit==oldName` aktual (cek `HcPortalDB_Dev.UserUnits` sebelum konfirmasi). **preview==actual (D-03a).**
    5. Klik "Lanjut Simpan" → cek `HcPortalDB_Dev`: baris `UserUnits.Unit` (incl sekunder) ter-rename ke nama baru + `ApplicationUser.Unit` baris primary mirror konsisten.
    6. **Edge — UserUnits-only:** rename unit yang HANYA punya membership sekunder (tak ada scalar `Users.Unit` match) → konfirmasi modal TETAP MUNCUL (karena `affectedUserUnitsCount` ikut `total`).
    7. **Guard (Plan 01 backend) — verifikasi cepat:** coba Delete unit ber-membership-sekunder → ditolak dengan pesan "sekunder"; coba reparent unit yang akan split pekerja >1 Bagian → ditolak dengan pesan sebut NIP/nama.
    8. (Bila ada harness Playwright org-tree) jalankan spec terkait modal — lesson Phase 354: Razor/JS dinamis WAJIB runtime assert.
    9. **RESTORE DB lokal** dari snapshot (sukses ATAU gagal) → tandai journal `cleaned`.

    Catatan: full suite `dotnet test --nologo` harus hijau (baseline ≥507, 3 skip SQLEXPRESS Phase 404 dipertahankan) sebelum sign-off.
  </how-to-verify>
  <verify>
    <automated>dotnet test --nologo 2>&1 | tail -3</automated>
  </verify>
  <resume-signal>Ketik "approved" bila modal + parity + guard sesuai, atau deskripsikan masalah (mis. nilai {N} tak cocok, modal tak muncul, gaya beda).</resume-signal>
  <done>UAT browser: modal baris ke-5 tampil + gaya identik saudaranya; nilai {N} == jumlah baris UserUnits ter-rename aktual (preview==actual); modal muncul saat dampak UserUnits-only; guard delete/reparent tolak; full suite hijau; user sign-off "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (PreviewEditCascade JSON) → browser (orgTree.js) | Field `affectedUserUnitsCount` ditampilkan read-only; nilai server-authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-403-07 | Tampering (client count manipulation) | nilai baris modal dihitung di klien | accept (avoided) | Klien TIDAK menghitung apa pun — hanya menampilkan `affectedUserUnitsCount` server-authoritative via `.textContent` (no innerHTML). Mutasi aktual tetap divalidasi server (Plan 01) |
| T-403-08 | XSS | render `affectedUserUnitsCount` ke DOM | mitigate | `.textContent` (bukan `innerHTML`) — nilai integer dari server, no markup injection. Pola identik 4 baris existing |
| T-403-09 | Info Disclosure | modal menampilkan jumlah keanggotaan ke operator | accept | Modal hanya muncul di `/Admin/ManageOrganization` (RBAC Admin/HC, inherit dari controller existing); count agregat non-PII |
</threat_model>

<verification>
- `dotnet build` 0 error (Razor compile)
- grep: `cascadeUserUnits` di view + `affectedUserUnitsCount` di orgTree.js (populate + total)
- UAT browser (checkpoint): modal baris ke-5 tampil + nilai == aktual DB + modal muncul saat UserUnits-only + guard tolak
- full suite `dotnet test --nologo` hijau (tidak regresi)
</verification>

<success_criteria>
- ORG-02 (D-03): modal `#cascadeConfirmModal` menampilkan baris "X baris keanggotaan unit" dengan gaya identik saudaranya
- preview==actual: nilai baris == jumlah baris `UserUnits` ter-rename aktual (terverifikasi UAT DB lokal)
- modal tetap muncul bila satu-satunya dampak = UserUnits
- 0 backend, 0 migration; build 0 error; full suite tidak regresi
</success_criteria>

<output>
After completion, create `.planning/phases/403-organizationcontroller-cascade-guard-userunits-aware/403-02-SUMMARY.md`
</output>
