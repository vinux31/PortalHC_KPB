---
phase: 404-test-sql-riil-uat-docs-invariants
plan: 02
type: execute
wave: 2
depends_on: ["404-01"]
files_modified:
  - HcPortal.Tests/SingleActiveInvariantSqlTests.cs
autonomous: true
migration: false
requirements: [QA-03]
tags: [test, sql-real, single-active, proton, bypass, reactivate]

must_haves:
  truths:
    - "A 2nd active CoachCoacheeMapping for the same coachee is rejected by the DB (DbUpdateException) on real SQL"
    - "After driving bypass T1@X→T2@Y via ProtonBypassService, exactly 1 ProtonTrackAssignment is active for the coachee (counted, not exception-asserted)"
    - "A reactivation that forgets to deactivate the prior active mapping is rejected by the DB filtered-unique index"
  artifacts:
    - path: "HcPortal.Tests/SingleActiveInvariantSqlTests.cs"
      provides: "QA-03 single-active Facts across mapping (DbUpdateException) + PTA (count) + reactivate"
      contains: "CountAsync"
      min_lines: 90
  key_links:
    - from: "HcPortal.Tests/SingleActiveInvariantSqlTests.cs"
      to: "IX_CoachCoacheeMappings_CoacheeId_ActiveUnique (DB filtered-unique)"
      via: "Assert.ThrowsAsync<DbUpdateException> on 2nd active mapping"
      pattern: "ThrowsAsync<DbUpdateException>"
    - from: "HcPortal.Tests/SingleActiveInvariantSqlTests.cs"
      to: "ProtonBypassService.ExecuteInstantBypassAsync (app-level single-active PTA)"
      via: "drive bypass T1→T2 then count active PTA == 1"
      pattern: "CountAsync.*IsActive|ExecuteInstantBypassAsync"
---

<objective>
Prove the **single-active** invariants on real SQL Server (QA-03), covering all write-paths (Assign/Edit/Import/bypass-TargetUnit/Reactivate/Import-reactivate) with the CORRECT assert strategy per entity:

- **`CoachCoacheeMapping` single-active = DB-enforced** (filtered-unique `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique`, ApplicationDbContext.cs:333-336) → assert via `Assert.ThrowsAsync<DbUpdateException>`. One Fact covers Assign/Edit/Import/Reactivate at once because all hit the SAME index (R-2).
- **`ProtonTrackAssignment` single-active = APP-level only** (only NON-unique index `(CoacheeId,IsActive)` at :393 — NO filtered-unique; Pitfall #1) → drive the real write-path (bypass T1@X→T2@Y via `ProtonBypassService`) then assert via `Assert.Equal(1, CountAsync(a => a.CoacheeId==id && a.IsActive))`. **NEVER** expect `DbUpdateException` for PTA.

Purpose: EF-InMemory can't enforce the mapping filtered-unique index → InMemory tests are "false green". This closes the 402 carry depth at SQL-real level. (QA-03)

Output: `HcPortal.Tests/SingleActiveInvariantSqlTests.cs` (new).

**BOUNDARY (R-1):** NOL kode produksi. Drive write-paths via (a) direct DB constraint assert, (b) DbContext write-pattern replication (à la AbandonGuardTests), or (c) existing `ProtonBypassService`. Do NOT extract a seam from `CoachCoacheeMappingAssign`/`Edit`/`Reactivate`/`Import` — they need UserManager/HttpContext and extracting = production change. If any single invariant TRULY cannot be covered without a new seam, STOP and flag it as an explicit decision (trade-off: re-trigger code-review+secure 404) — do NOT silently extract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-CONTEXT.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-RESEARCH.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-PATTERNS.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-01-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration. -->

Assert-strategy split (the load-bearing decision — Data/ApplicationDbContext.cs):
```
:333-336  CoachCoacheeMapping  filtered-unique [IsActive]=1  → 2nd active row → DbUpdateException (DB-enforced)
:393      ProtonTrackAssignment  (CoacheeId,IsActive) NON-unique → NO DB enforcement → assert via COUNT after driving write-path
```

ProtonBypassService entry-point for T1→T2 (Services/ProtonBypassService.cs:96):
```csharp
public async Task<BypassResult> ExecuteInstantBypassAsync(BypassRequest req)
```
Construct service + drive it exactly like the precedent (HcPortal.Tests/ProtonBypassServiceTests.cs):
```csharp
:29  static ProtonBypassService NewBypassSvc(ctx, FakeNotificationService? notif=null) =>
       new(ctx, new ProtonCompletionService(ctx, NullLogger<ProtonCompletionService>.Instance, fake, new AuditLogService(ctx)),
           fake, new AuditLogService(ctx), NullLogger<ProtonBypassService>.Instance);
:39  static Task<int> TrackIdAsync(ctx, trackType, tahunKe)  // master tracks from migration InsertData
:42  static Task<ProtonTrackAssignment> SeedAssignmentAsync(ctx, coacheeId, trackId, active, origin)
:52  static Task<List<int>> SeedDeliverablesAsync(ctx, trackId, unit, count)
:68  static Task SeedProgressAsync(ctx, coacheeId, assignmentId, deliverableIds, status)
:84  static BypassRequest Req(coachee, sourceTrack, targetTrack, mode, targetUnit, targetCoachId) =>
       new(coachee, sourceTrack, targetTrack, targetUnit, targetCoachId, "Alasan bypass test", mode, null, "hc-init");
```
Precedent CL-B(a) full lifecycle is at ProtonBypassServiceTests.cs:88-100 — copy that shape for T1@X→T2@Y.

DbContext write-pattern replication for reactivate (NOT production change — test code, pola AbandonGuardTests.cs:88-95): replicate the deactivate-before-reactivate UPDATE at DbContext level; the mapping DB index already rejects a 2nd active row, so a reactivation that omits deactivation → DbUpdateException (same Pattern 2).

Fixture from Plan 01 — HcPortal.Tests/MultiUnitSqlFixture.cs: `_fixture.Options`, consts `MultiUnitSqlFixture.UnitX`, `.UnitY`, `.Bagian`, `.CoacheeId`. Use a FRESH `$"...-{Guid.NewGuid():N}"` coachee per Fact (single-active indexes are per-coachee; collisions across Facts in the shared DB would false-fail — pattern ProtonBypassServiceTests.cs:92).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Single-active CoachCoacheeMapping (DB-enforced) + reactivate path — DbUpdateException Facts</name>
  <files>HcPortal.Tests/SingleActiveInvariantSqlTests.cs</files>
  <read_first>
    - HcPortal.Tests/MultiUnitSqlFixture.cs (Plan 01 — fixture + consts; reuse Options)
    - HcPortal.Tests/ProtonBypassServiceTests.cs:17-25 (class wiring [Trait]+IClassFixture) + :92 (per-Fact unique coachee)
    - HcPortal.Tests/AbandonGuardTests.cs:88-95 (DbContext write-pattern replication — for reactivate)
    - Data/ApplicationDbContext.cs:333-336 (mapping filtered-unique → DbUpdateException)
    - Models/CoachCoacheeMapping.cs (required non-null props to satisfy insert)
    - .planning/phases/404-test-sql-riil-uat-docs-invariants/404-PATTERNS.md (§MultiUnitInvariantSqlTests — Pattern 2 + R-2)
  </read_first>
  <action>
Create `HcPortal.Tests/SingleActiveInvariantSqlTests.cs`, namespace `HcPortal.Tests`, class:
```csharp
[Trait("Category", "Integration")]
public class SingleActiveInvariantSqlTests : IClassFixture<MultiUnitSqlFixture>
{
    private readonly MultiUnitSqlFixture _fixture;
    public SingleActiveInvariantSqlTests(MultiUnitSqlFixture fixture) => _fixture = fixture;
    private ApplicationDbContext NewCtx() => new ApplicationDbContext(_fixture.Options);
    // ... Facts ...
}
```

**Fact A — mapping single-active is DB-enforced (covers Assign/Edit/Import/Reactivate via the shared index, R-2):**
- Fresh `var coachee = $"mapA-{Guid.NewGuid():N}";`
- Insert 1 active `CoachCoacheeMapping { CoachId="co1", CoacheeId=coachee, IsActive=true, StartDate=DateTime.UtcNow, AssignmentUnit=MultiUnitSqlFixture.UnitX }` → SaveChangesAsync (succeeds).
- Insert a 2nd active mapping for the SAME coachee (different CoachId, `AssignmentUnit=UnitY`) → `await Assert.ThrowsAsync<DbUpdateException>(() => ctx.SaveChangesAsync());`
- Comment: this single Fact represents Assign/Edit/Import/Reactivate because they all write through the SAME `IX_CoachCoacheeMappings_CoacheeId_ActiveUnique` (R-2). InMemory would NOT throw → SQL-real reason.

**Fact B — reactivate-without-deactivate replication (R-1 (b)):**
- Fresh coachee. Insert 1 active mapping (succeeds). Add a 2nd INACTIVE mapping for same coachee (IsActive=false → allowed, filter only covers IsActive=1) → SaveChangesAsync succeeds.
- Now simulate a buggy reactivate that flips the inactive row to active WITHOUT deactivating the existing active one: set the inactive row `.IsActive = true` → `await Assert.ThrowsAsync<DbUpdateException>(() => ctx.SaveChangesAsync());`
- Comment: proves the DB index protects the reactivate / import-reactivate path too (both share the index). This is test-code replication of the write-pattern, NOT a production seam.

usings: System, System.Linq, System.Threading.Tasks, HcPortal.Data, HcPortal.Models, Microsoft.EntityFrameworkCore, Xunit.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~SingleActiveInvariantSqlTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `SingleActiveInvariantSqlTests.cs` exists with `[Trait("Category", "Integration")]` and `IClassFixture<MultiUnitSqlFixture>`.
    - Contains at least 2 `Assert.ThrowsAsync<DbUpdateException>` calls (grep count >= 2).
    - Each Fact uses a fresh `Guid.NewGuid()` coachee (grep `Guid.NewGuid` >= 2).
    - `dotnet test --filter "FullyQualifiedName~SingleActiveInvariantSqlTests"` exits 0 with Facts A+B passing (after Task 2 the full class is green).
  </acceptance_criteria>
  <done>Mapping single-active proven DB-enforced via DbUpdateException for both fresh-insert (Assign/Edit/Import) and reactivate-without-deactivate paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Single-active ProtonTrackAssignment via bypass T1@X→T2@Y — COUNT assert (not exception)</name>
  <files>HcPortal.Tests/SingleActiveInvariantSqlTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonBypassServiceTests.cs:29-100 (NewBypassSvc, TrackIdAsync, SeedAssignmentAsync, SeedDeliverablesAsync, SeedProgressAsync, Req helper, CL-B(a) lifecycle :88-100)
    - Services/ProtonBypassService.cs:96 (ExecuteInstantBypassAsync entry-point) + :400-470 (MoveAssignmentAsync deactivate-before-create)
    - Data/ApplicationDbContext.cs:393 (PTA NON-unique index → COUNT strategy, Pitfall #1)
    - Models/ProtonFinalAssessment.cs + Data/ApplicationDbContext.cs:443 (ProtonFinalAssessment unique-per-assignment → cert histori utuh)
    - .planning/phases/404-test-sql-riil-uat-docs-invariants/404-RESEARCH.md (§Pattern 3 + Pattern 4 + Open Q3 cert histori)
  </read_first>
  <action>
Add to `SingleActiveInvariantSqlTests` the PTA single-active Fact driven through the REAL bypass write-path (Pitfall #1: PTA has NO filtered-unique index → assert by COUNT, never DbUpdateException).

**Fact C — PTA single-active after sequential bypass T1@X→T2@Y:**
1. Copy the helper methods `NewBypassSvc`, `TrackIdAsync`, `SeedAssignmentAsync`, `SeedDeliverablesAsync`, `SeedProgressAsync`, `Req` from `ProtonBypassServiceTests.cs:29-86` into this class (private static). They have no production-coupling — pure test helpers. (Reuse `FakeNotificationService` already in the test project.)
2. Fresh `var coachee = $"pta-{Guid.NewGuid():N}";`
3. Seed the source assignment at Tahun 1 / UnitX: `var t1 = await TrackIdAsync(ctx, "Operator", "Tahun 1"); var t2 = await TrackIdAsync(ctx, "Operator", "Tahun 2");` then `var source = await SeedAssignmentAsync(ctx, coachee, t1);` + `SeedDeliverablesAsync(ctx, t1, unitX, 2)` + `SeedProgressAsync(... "Submitted")` (mirror CL-B(a) lifecycle :88-100; use `targetUnit` = a per-coachee unit string consistent with the seeded deliverables, exactly as the precedent does at :96-100).
4. Drive the bypass to Tahun 2: `await NewBypassSvc(ctx).ExecuteInstantBypassAsync(Req(coachee, t1, t2, "CL-B(a)", targetUnit: <unitY-or-precedent-style>));`
5. **Assert single-active by COUNT (Pattern 3):**
   ```csharp
   var activeCount = await ctx.ProtonTrackAssignments.CountAsync(a => a.CoacheeId == coachee && a.IsActive);
   Assert.Equal(1, activeCount);  // T1 deactivated, T2 active → exactly 1. NOT DbUpdateException.
   ```
6. **Assert cert histori utuh (D-02 "sertifikat tiap unit tersimpan utuh sebagai histori", research Open Q3):** assert that BOTH assignments still exist as rows (count total assignments for coachee == 2, one inactive + one active) AND any `ProtonFinalAssessment` produced by the source remains (query `ctx.ProtonFinalAssessments.CountAsync(f => f.CoacheeId == coachee)` is preserved, not deleted). Keep this assertion lenient to the precedent's actual behavior — the load-bearing claim is: the prior track's record is NOT destroyed by the bypass (histori co-exists).

If `ExecuteInstantBypassAsync` requires additional seed (e.g. an active CoachCoacheeMapping or specific deliverable status) to reach the deactivate-then-create path, mirror the exact prerequisites the precedent Fact sets up (ProtonBypassServiceTests.cs:88-100) — do NOT invent new production behavior.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~SingleActiveInvariantSqlTests" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `SingleActiveInvariantSqlTests.cs` contains `ExecuteInstantBypassAsync` and `CountAsync` with `IsActive` (grep all present).
    - Fact C asserts `Assert.Equal(1, activeCount)` (count strategy, NOT `ThrowsAsync` for PTA).
    - File does NOT assert `DbUpdateException` for any ProtonTrackAssignment insert (no PTA exception-assert — grep confirms PTA asserts use Count).
    - `dotnet test --filter "FullyQualifiedName~SingleActiveInvariantSqlTests"` exits 0, all Facts (A/B/C) passing.
  </acceptance_criteria>
  <done>PTA single-active proven by driving the real bypass T1@X→T2@Y and counting exactly 1 active assignment; prior track record preserved as histori (cert co-exists). Correct assert strategy used per Pitfall #1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 02 adds ZERO production surface — one new SQL-real test file. No endpoints/inputs/controllers/services added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-404-02-01 | Tampering (data integrity) | Single-active invariants (mapping + PTA) | mitigate (by test) | This plan PROVES the existing single-active controls hold on real SQL (the control already exists; the test is regression-proof evidence). No new mutable surface. |
| T-404-02-02 | — (test hygiene) | Shared fixture DB across Facts | accept | Per-Fact unique GUID coachee prevents cross-Fact collision on per-coachee single-active indexes; disposable DB dropped by fixture. |

**Justification:** No new production attack surface — one SQL-real test file in a test/UAT/docs phase. Bypass is driven through the EXISTING `ProtonBypassService` (no new code). Threats N/A beyond proving existing data-integrity controls.
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~SingleActiveInvariantSqlTests"` → all Facts green (SQLEXPRESS live).
- Pitfall #1 guard: PTA asserts use `CountAsync(...== 1)`, NOT `ThrowsAsync<DbUpdateException>` (grep review).
- R-2 coverage: one mapping Fact represents Assign/Edit/Import/Reactivate (shared index); reactivate replication Fact present.
- `dotnet test` full suite → no regression vs baseline ~547/0/6 (run at wave merge).
</verification>

<success_criteria>
- CoachCoacheeMapping single-active proven DB-enforced (DbUpdateException) for fresh + reactivate paths (QA-03).
- ProtonTrackAssignment single-active proven app-level by COUNT after real bypass T1@X→T2@Y; cert histori preserved (QA-03, D-06).
- Correct assert strategy per entity (Pitfall #1 honored); zero production code changed (R-1).
</success_criteria>

<output>
After completion, create `.planning/phases/404-test-sql-riil-uat-docs-invariants/404-02-SUMMARY.md`
</output>
