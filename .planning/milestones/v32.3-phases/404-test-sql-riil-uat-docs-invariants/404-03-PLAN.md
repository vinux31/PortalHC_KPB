---
phase: 404-test-sql-riil-uat-docs-invariants
plan: 03
type: execute
wave: 2
depends_on: ["404-01"]
files_modified:
  - HcPortal.Tests/UnitMembershipInvariantSqlTests.cs
  - HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs
autonomous: true
migration: false
requirements: [QA-01, QA-04]
tags: [test, sql-real, assignment-unit, userunits, b-06, backfill, deliverable]

must_haves:
  truths:
    - "ValidateAssignmentUnitInUserUnits returns true for a unit ∈ coachee.UserUnits and false for a unit ∉ — proven against real SQL fixture"
    - "Bootstrapping deliverables for unit X then unit Y for the SAME coachee creates unit-Y deliverables (cross-unit not skipped by B-06 guard)"
    - "A duplicate (ProtonTrackAssignmentId, ProtonDeliverableId) is rejected by the DB unique index"
    - "A 2nd IsPrimary=true UserUnits row for the same user is rejected by the DB filtered-unique index"
    - "The 3 backfill stubs prove migration-399 backfill: 1 primary-row per Unit-non-null user, 0 rows for Unit-null, idempotent re-run adds 0 rows"
  artifacts:
    - path: "HcPortal.Tests/UnitMembershipInvariantSqlTests.cs"
      provides: "QA-04 Facts: AssignmentUnit∈UserUnits + B-06 anti-dobel + ProtonKompetensi 1:1 + one-primary UserUnits"
      contains: "ValidateAssignmentUnitInUserUnits"
      min_lines: 90
    - path: "HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs"
      provides: "3 backfill Facts implemented (no longer [Skip])"
      contains: "ExecuteSqlRawAsync"
  key_links:
    - from: "HcPortal.Tests/UnitMembershipInvariantSqlTests.cs"
      to: "CoachMappingController.ValidateAssignmentUnitInUserUnits + ProtonDeliverableBootstrap.CreateProgressAsync"
      via: "drive existing static helpers against SQL-real fixture"
      pattern: "ValidateAssignmentUnitInUserUnits|CreateProgressAsync"
    - from: "HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs"
      to: "migration 399 backfill SQL (INSERT...SELECT WHERE NOT EXISTS)"
      via: "ExecuteSqlRawAsync re-running the migration's backfill statement"
      pattern: "ExecuteSqlRawAsync"
---

<objective>
Prove the **unit-membership** invariants on real SQL (QA-04) and close the residual migration-399 backfill stubs (QA-01, Open Q1):

1. **`AssignmentUnit ∈ coachee.UserUnits`** — drive the existing static helper `CoachMappingController.ValidateAssignmentUnitInUserUnits` against the SQL-real fixture (∈ → true, ∉ → false). Represents Assign/Edit/Import/bypass-TargetUnit/reactivate write-paths (they all consume this helper).
2. **B-06 anti-dobel** — drive `ProtonDeliverableBootstrap.CreateProgressAsync` for unit X then unit Y for the SAME coachee; assert unit-Y deliverables are still created (cross-unit progress does NOT skip each other).
3. **`ProtonKompetensi.Unit` / deliverable 1:1** — insert duplicate `(ProtonTrackAssignmentId, ProtonDeliverableId)` → `DbUpdateException` (filtered-unique :429).
4. **one-primary UserUnits** — insert a 2nd `IsPrimary=true` row for the same user → `DbUpdateException` (filtered-unique :350-353).
5. **3 backfill stubs** (`UserUnitsBackfillIntegrationTests.cs:72-91`) — seed Users (Unit non-null / null), re-run the EXACT migration-399 backfill SQL via `ExecuteSqlRawAsync`, assert 1-primary / 0-rows / idempotent.

Purpose: these are the data-tenancy + anti-double controls that only real SQL can prove (filtered-unique indexes); the backfill stubs strengthen the migration-399 deploy evidence. (QA-04, QA-01)

Output: `HcPortal.Tests/UnitMembershipInvariantSqlTests.cs` (new) + 3 implemented Facts in `HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs`.

**BOUNDARY (R-1):** NOL kode produksi. Drive via EXISTING static helpers (`ValidateAssignmentUnitInUserUnits`, `ProtonDeliverableBootstrap.CreateProgressAsync`) and direct DB constraint asserts. Do NOT extract any new seam. For the backfill, re-run the migration's OWN SQL statement (do NOT hand-roll new backfill SQL — that would not truthfully prove idempotency).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-CONTEXT.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-RESEARCH.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-PATTERNS.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-01-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no exploration. -->

Static helper — AssignmentUnit ∈ UserUnits (Controllers/CoachMappingController.cs:52-62), already public+static:
```csharp
public static async Task<bool> ValidateAssignmentUnitInUserUnits(ApplicationDbContext context, string coacheeId, string? assignmentUnit)
// returns true if assignmentUnit ∈ active UserUnits of coacheeId (Trim + OrdinalIgnoreCase); empty/whitespace → false
```

B-06 bootstrap helper (Helpers/ProtonDeliverableBootstrap.cs:24-95), public+static, SaveChanges internal:
```csharp
public static async Task<List<string>> CreateProgressAsync(ApplicationDbContext ctx, int assignmentId, int protonTrackId, string coacheeId, string resolvedUnit)
// Filters deliverables by ProtonKompetensi.Unit == resolvedUnit (Trim). B-06 guard (:52-65): SKIPS deliverables that
// ALREADY have progress for THIS coachee. KEY: unit-X and unit-Y have DIFFERENT deliverable ids → they do NOT skip
// each other (cross-unit safe). Returns warnings list.
```

DB filtered-unique indexes to assert (Data/ApplicationDbContext.cs):
```
:350-353  IX_UserUnits_UserId_PrimaryUnique  [IsPrimary]=1 IsUnique  → 2nd primary row → DbUpdateException
:429      (ProtonTrackAssignmentId, ProtonDeliverableId) IsUnique   → duplicate progress row → DbUpdateException
```

Exact migration-399 backfill SQL to re-run via ExecuteSqlRawAsync (Migrations/20260618045427_AddUserUnitsTable.cs:51-57) — copy verbatim, do NOT rewrite:
```sql
INSERT INTO UserUnits (UserId, Unit, IsPrimary, IsActive)
SELECT u.Id, u.Unit, 1, 1
FROM Users u
WHERE u.Unit IS NOT NULL AND u.Unit <> ''
  AND NOT EXISTS (SELECT 1 FROM UserUnits uu WHERE uu.UserId = u.Id)
```

Existing backfill fixture (HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs:25-70) — already migrated on EMPTY DB (0 Users → 0 backfill rows); `_fixture.Options`, `[Trait("Category","Integration")]`+`IClassFixture<UserUnitsBackfillFixture>` already wired. The 3 stubs at :72-91 each currently `Assert.True(false, ...)`.

Seed-user idiom (HcPortal.Tests/AbandonGuardTests.cs:68-74 / ProtonBypassServiceTests.cs:402-406):
```csharp
ctx.Users.Add(new ApplicationUser { Id = userId, UserName = userId, FullName = $"Worker {userId[..8]}", Unit = "..." });
await ctx.SaveChangesAsync();
```

Fixture from Plan 01 — HcPortal.Tests/MultiUnitSqlFixture.cs: `_fixture.Options`, consts `.UnitX`, `.UnitY`, `.CoacheeId`, `.Bagian`. Use FRESH `$"...-{Guid.NewGuid():N}"` ids per Fact (shared DB).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unit-membership QA-04 Facts (AssignmentUnit∈UserUnits + B-06 + 1:1 + one-primary)</name>
  <files>HcPortal.Tests/UnitMembershipInvariantSqlTests.cs</files>
  <read_first>
    - HcPortal.Tests/MultiUnitSqlFixture.cs (Plan 01 — fixture + consts)
    - Controllers/CoachMappingController.cs:45-90 (ValidateAssignmentUnitInUserUnits signature + behavior)
    - Helpers/ProtonDeliverableBootstrap.cs (full — B-06 guard at :52-65; resolvedUnit filter at :36-40)
    - HcPortal.Tests/AssignmentUnitInUserUnitsTests.cs (existing InMemory assert style for the helper — mirror against SQL)
    - HcPortal.Tests/ProtonBypassServiceTests.cs:42-66 (SeedAssignmentAsync + SeedDeliverablesAsync to build the PROTON chain per unit)
    - Data/ApplicationDbContext.cs:350-353, :429 (filtered-unique indexes → DbUpdateException asserts)
    - .planning/phases/404-test-sql-riil-uat-docs-invariants/404-PATTERNS.md (§MultiUnitInvariantSqlTests QA-04 + Pattern 5)
  </read_first>
  <action>
Create `HcPortal.Tests/UnitMembershipInvariantSqlTests.cs`, namespace `HcPortal.Tests`:
```csharp
[Trait("Category", "Integration")]
public class UnitMembershipInvariantSqlTests : IClassFixture<MultiUnitSqlFixture>
{
    private readonly MultiUnitSqlFixture _fixture;
    public UnitMembershipInvariantSqlTests(MultiUnitSqlFixture fixture) => _fixture = fixture;
    private ApplicationDbContext NewCtx() => new ApplicationDbContext(_fixture.Options);
    // copy SeedAssignmentAsync + SeedDeliverablesAsync helpers from ProtonBypassServiceTests:42-66 (private static)
}
```

**Fact A — AssignmentUnit ∈ coachee.UserUnits (QA-04, represents all junction-write paths):**
- Fresh coachee; insert `UserUnits {UnitX primary active}` + `{UnitY secondary active}`.
- `Assert.True (await CoachMappingController.ValidateAssignmentUnitInUserUnits(ctx, coachee, MultiUnitSqlFixture.UnitX));`
- `Assert.True (await CoachMappingController.ValidateAssignmentUnitInUserUnits(ctx, coachee, MultiUnitSqlFixture.UnitY));`
- `Assert.False(await CoachMappingController.ValidateAssignmentUnitInUserUnits(ctx, coachee, "UnitZ-not-member"));`
- `Assert.False(await CoachMappingController.ValidateAssignmentUnitInUserUnits(ctx, coachee, "  "));`  // empty → reject (no silent primary resolve)
- Comment: Assign/Edit/Import/bypass-TargetUnit/reactivate all gate through this helper → one Fact represents the write-paths (D-07).

**Fact B — B-06 anti-dobel cross-unit (QA-04, D-08):**
- Fresh coachee. Seed two PROTON chains via the copied helpers: track t1 with a `ProtonKompetensi{Unit=unitX}`→Sub→Deliverable, track t2 with `{Unit=unitY}`→Sub→Deliverable. Seed two `ProtonTrackAssignment` (asgX on t1, asgY on t2) for the coachee.
- `await ProtonDeliverableBootstrap.CreateProgressAsync(ctx, asgX.Id, t1, coachee, unitX);`
- `await ProtonDeliverableBootstrap.CreateProgressAsync(ctx, asgY.Id, t2, coachee, unitY);`
- Assert that progress rows exist for BOTH unit-X and unit-Y deliverables: e.g. `var n = await ctx.ProtonDeliverableProgresses.CountAsync(p => p.CoacheeId == coachee); Assert.True(n >= 2);` AND assert at least one progress maps to a unit-Y deliverable (query deliverable ids of unitY and confirm intersection). The load-bearing claim: unit-Y deliverables were NOT skipped by the B-06 guard just because unit-X progress already exists (different deliverable ids → no skip).

**Fact C — ProtonKompetensi/deliverable 1:1 DB-enforced (QA-04):**
- Insert one `ProtonDeliverableProgress {ProtonTrackAssignmentId=asg.Id, ProtonDeliverableId=d.Id, CoacheeId=...}` → SaveChangesAsync (ok).
- Insert a SECOND progress with the SAME `(ProtonTrackAssignmentId, ProtonDeliverableId)` → `await Assert.ThrowsAsync<DbUpdateException>(() => ctx.SaveChangesAsync());` (filtered-unique :429). A DIFFERENT deliverableId would be OK — optionally assert that too.

**Fact D — one-primary UserUnits DB-enforced (QA-01/QA-03 supporting):**
- Fresh user. Insert `UserUnits {UnitX IsPrimary=true}` → ok. Insert 2nd `{UnitY IsPrimary=true}` for same user → `await Assert.ThrowsAsync<DbUpdateException>(() => ctx.SaveChangesAsync());` (filtered-unique :350-353).

Use a fresh `Guid.NewGuid()` coachee/user per Fact. usings: System, System.Linq, System.Threading.Tasks, HcPortal.Controllers, HcPortal.Data, HcPortal.Helpers, HcPortal.Models, Microsoft.EntityFrameworkCore, Xunit.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~UnitMembershipInvariantSqlTests" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `UnitMembershipInvariantSqlTests.cs` exists with `[Trait("Category", "Integration")]` + `IClassFixture<MultiUnitSqlFixture>`.
    - Contains `ValidateAssignmentUnitInUserUnits` (>=3 calls), `CreateProgressAsync` (>=2 calls), and >=2 `Assert.ThrowsAsync<DbUpdateException>` (1:1 + one-primary).
    - `dotnet test --filter "FullyQualifiedName~UnitMembershipInvariantSqlTests"` exits 0, all Facts (A/B/C/D) passing (SQLEXPRESS live).
  </acceptance_criteria>
  <done>AssignmentUnit∈UserUnits proven via the production helper; B-06 cross-unit no-skip proven via the production bootstrap; 1:1 and one-primary proven DB-enforced via DbUpdateException. Zero production code changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the 3 migration-399 backfill stubs (QA-01, Open Q1)</name>
  <files>HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs</files>
  <read_first>
    - HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs (full — fixture :25-60 already migrates EMPTY DB; 3 stubs :72-91)
    - Migrations/20260618045427_AddUserUnitsTable.cs:51-57 (the EXACT backfill SQL to re-run — copy verbatim)
    - HcPortal.Tests/AbandonGuardTests.cs:68-74 (seed-user idiom)
    - .planning/phases/404-test-sql-riil-uat-docs-invariants/404-PATTERNS.md (§UserUnitsBackfillIntegrationTests — seed→backfill→assert)
  </read_first>
  <action>
Replace the 3 `Assert.True(false, ...)` stub bodies at `UserUnitsBackfillIntegrationTests.cs:72-91` with real seed→backfill→assert. Keep the existing fixture + `[Trait]`+`IClassFixture` wiring untouched. Remove the `Skip = ...` argument from each `[Fact(...)]` so they run.

Define a private const with the EXACT migration backfill SQL (copy verbatim from `AddUserUnitsTable.cs:51-57`):
```csharp
private const string BackfillSql = @"
    INSERT INTO UserUnits (UserId, Unit, IsPrimary, IsActive)
    SELECT u.Id, u.Unit, 1, 1
    FROM Users u
    WHERE u.Unit IS NOT NULL AND u.Unit <> ''
      AND NOT EXISTS (SELECT 1 FROM UserUnits uu WHERE uu.UserId = u.Id)";
```
NOTE: the migration already ran this once on the empty DB (0 Users → 0 rows). Each Fact seeds Users THEN re-runs `BackfillSql` via `ctx.Database.ExecuteSqlRawAsync(BackfillSql)`. Use a fresh `Guid` userId per Fact (shared DB across the 3 Facts).

1. **`Backfill_UserWithNonNullUnit_HasExactlyOnePrimaryRow`** → make it `async Task`:
   - Seed 1 `ApplicationUser { Id=userId, UserName=userId, FullName=..., Unit="UnitX-bf" }` → SaveChangesAsync.
   - `await ctx.Database.ExecuteSqlRawAsync(BackfillSql);`
   - `var rows = await ctx.UserUnits.CountAsync(uu => uu.UserId == userId && uu.IsPrimary); Assert.Equal(1, rows);`
   - Also `Assert.Equal(1, await ctx.UserUnits.CountAsync(uu => uu.UserId == userId));` (exactly one total).

2. **`Backfill_UserWithNullUnit_HasZeroRows`** → `async Task`:
   - Seed 1 user with `Unit = null` (and optionally one with `Unit=""`).
   - Run BackfillSql. `Assert.Equal(0, await ctx.UserUnits.CountAsync(uu => uu.UserId == userId));`

3. **`Backfill_RerunIsIdempotent_NoDuplicateRows`** → `async Task`:
   - Seed 1 user with non-null Unit. Run BackfillSql once → 1 row. Run BackfillSql AGAIN → assert STILL exactly 1 row for that user (WHERE NOT EXISTS makes run-2 INSERT 0 rows).

Convert each `[Fact(Skip=...)] public void` to `[Fact] public async Task`. Add `using Microsoft.EntityFrameworkCore;` (for `ExecuteSqlRawAsync`/`CountAsync`) and `using HcPortal.Models;` if needed.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~UserUnitsBackfillIntegrationTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `UserUnitsBackfillIntegrationTests.cs` contains NO `Skip =` argument (grep `Skip =` returns 0).
    - File contains `ExecuteSqlRawAsync` and the verbatim `INSERT INTO UserUnits` backfill SQL (grep both present).
    - All 3 Facts are `async Task` (no `Assert.True(false` remaining — grep returns 0).
    - `dotnet test --filter "FullyQualifiedName~UserUnitsBackfillIntegrationTests"` exits 0 with 3 passed, 0 skipped (SQLEXPRESS live).
  </acceptance_criteria>
  <done>The 3 residual backfill stubs are implemented and passing: 1 primary-row per Unit-non-null user, 0 rows for Unit-null, idempotent re-run adds 0 — proving migration-399 backfill behavior on real SQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 03 adds ZERO production surface — two test files (one new, one stub-fill). No endpoints/inputs/controllers/services added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-404-03-01 | Tampering / Elevation (data tenancy) | AssignmentUnit ∈ UserUnits control | mitigate (by test) | This plan PROVES the existing membership/tenancy control (V4/V5 indirect) holds on real SQL; it drives the existing helper, adds no surface. |
| T-404-03-02 | Information Disclosure | Backfill SQL via ExecuteSqlRawAsync | accept | SQL is a verbatim copy of the migration's OWN static literal (no user input, no interpolation) — identical to the migration that already shipped. No injection surface. |

**Justification:** No new production attack surface — test/docs phase. The backfill SQL is the migration's own static statement re-run for evidence; no parameter interpolation. Threats N/A beyond proving existing controls.
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~UnitMembershipInvariantSqlTests"` → all Facts green.
- `dotnet test --filter "FullyQualifiedName~UserUnitsBackfillIntegrationTests"` → 3 passed, 0 skipped (previously 3 [Skip]).
- `dotnet test` full suite → no regression vs baseline ~547/0/6 AND skip count drops by 3 (backfill stubs now live). Run at wave merge.
- R-1 guard: no new seam extracted; backfill SQL is the migration's verbatim literal.
</verification>

<success_criteria>
- AssignmentUnit∈UserUnits, B-06 anti-dobel cross-unit, ProtonKompetensi/deliverable 1:1, one-primary UserUnits all proven on real SQL (QA-04).
- 3 migration-399 backfill stubs implemented and passing (QA-01, Open Q1 closed).
- Zero production code changed (R-1).
</success_criteria>

<output>
After completion, create `.planning/phases/404-test-sql-riil-uat-docs-invariants/404-03-SUMMARY.md`
</output>
