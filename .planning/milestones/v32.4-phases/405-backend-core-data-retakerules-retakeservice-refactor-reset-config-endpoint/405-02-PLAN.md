---
phase: 405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint
plan: 02
type: tdd
wave: 2
depends_on: ["405-01"]
files_modified:
  - Helpers/RetakeRules.cs
  - Helpers/RetakeArchiveBuilder.cs
  - HcPortal.Tests/RetakeRulesTests.cs
  - HcPortal.Tests/RetakeArchiveBuilderTests.cs
autonomous: true
requirements: [RTK-03, RTK-02, RTK-13]
tags: [retake, pure-helper, tdd, eligibility]

must_haves:
  truths:
    - "RetakeRules.CanRetake mengembalikan benar HANYA bila semua syarat terpenuhi (AllowRetake && !PreTest && !ManualEntry && Completed && IsPassed==false && attemptsUsed<MaxAttempts && cooldown lewat)"
    - "RetakeRules.ShouldHideRetakeToggle mengembalikan true untuk PreTest ATAU ManualEntry"
    - "RetakeArchiveBuilder.Build membekukan verdict via AssessmentScoreAggregator.IsQuestionCorrect dan menyimpan jawaban essay FULL-TEXT (tidak truncate 300 char)"
  artifacts:
    - path: "Helpers/RetakeRules.cs"
      provides: "Pure eligibility CanRetake + ShouldHideRetakeToggle (RTK-03/13)"
      contains: "public static bool CanRetake"
    - path: "Helpers/RetakeArchiveBuilder.cs"
      provides: "Pure snapshot builder (RTK-02)"
      contains: "public static List<AssessmentAttemptResponseArchive> Build"
    - path: "HcPortal.Tests/RetakeRulesTests.cs"
      provides: "Unit test semua cabang CanRetake + ShouldHideRetakeToggle"
      contains: "RetakeRules.CanRetake"
    - path: "HcPortal.Tests/RetakeArchiveBuilderTests.cs"
      provides: "Unit test verdict freeze + essay full-text"
      contains: "RetakeArchiveBuilder.Build"
  key_links:
    - from: "Helpers/RetakeArchiveBuilder.cs"
      to: "Helpers/AssessmentScoreAggregator.cs"
      via: "IsQuestionCorrect untuk verdict (kill-drift, no re-grade inline)"
      pattern: "AssessmentScoreAggregator.IsQuestionCorrect"
    - from: "Helpers/RetakeArchiveBuilder.cs"
      to: "Models/AssessmentAttemptResponseArchive.cs"
      via: "return List<AssessmentAttemptResponseArchive>"
      pattern: "AssessmentAttemptResponseArchive"
---

<objective>
Buat dua helper PURE (EF-free, sinkron, unit-testable di SEMUA cabang) dengan TDD: `RetakeRules` (kelayakan ujian ulang `CanRetake` + visibilitas `ShouldHideRetakeToggle`, RTK-03/13) dan `RetakeArchiveBuilder` (snapshot per-soal, verdict beku via aggregator terpusat, RTK-02). Keduanya dipakai DI DUA TEMPAT (service plan 405-03 + Phase 407 ViewModel) → kill-drift wajib helper murni (pola `ShuffleToggleRules`).

Purpose: Memisahkan keputusan logika murni dari I/O DB. `RetakeRules.CanRetake` tetap pure (terima `attemptsUsed` int sebagai param — counting DB-aware D-01 hidup di service plan 405-03). `RetakeArchiveBuilder` memakai `AssessmentScoreAggregator.IsQuestionCorrect`/`BuildAnswerCell` (single source of truth) — TAPI dengan koreksi Pitfall 2: essay disimpan FULL-TEXT (bukan truncate 300 char dari `BuildAnswerCell`).
Output: 2 helper baru + 2 test file baru (semua cabang hijau).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-CONTEXT.md
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-RESEARCH.md

<project_notes>
- Mirror: `Helpers/ShuffleToggleRules.cs` (helper pure no-DI) + `HcPortal.Tests/ShuffleToggleRulesTests.cs` (test pure no-fixture).
- Quick test: `dotnet test --filter "FullyQualifiedName~RetakeRules"` dan `dotnet test --filter "FullyQualifiedName~RetakeArchiveBuilder"`.
- TDD: tulis test (RED) dulu → run (gagal compile/assert) → implement → run (GREEN). Commit RED lalu GREEN terpisah.
</project_notes>

<interfaces>
<!-- Signature aggregator + entity — diverifikasi dari repo (Helpers/AssessmentScoreAggregator.cs:73,110). -->

From Helpers/AssessmentScoreAggregator.cs (pakai langsung — JANGAN re-grade inline):
```csharp
// bool?: true=Benar, false=Salah, null=essay belum dinilai (pending)
public static bool? IsQuestionCorrect(PackageQuestion q, IEnumerable<PackageUserResponse> responsesForQ);
// MC -> single selected OptionText; MA -> join OptionText order-by-Id; Essay -> TextAnswer TRUNCATE 300 + "..."; none -> "—"
public static string BuildAnswerCell(PackageQuestion q, IEnumerable<PackageUserResponse> responsesForQ);
```
⚠️ Pitfall 2: `BuildAnswerCell` me-TRUNCATE essay ke 300 char (AssessmentScoreAggregator.cs:120 — itu untuk DISPLAY PDF/Excel). Archive WAJIB full-text (D-04 / D10 audit) → untuk Essay simpan `responseForQ.TextAnswer` penuh, BUKAN `BuildAnswerCell`. MC/MA tetap pakai `BuildAnswerCell`.

From Models/PackageUserResponse.cs (field relevan):
```csharp
public int PackageQuestionId { get; set; }
public int? PackageOptionId { get; set; }
public string? TextAnswer { get; set; }   // essay full-text source
public int? EssayScore { get; set; }      // null = pending
```

From Models/PackageQuestion.cs (relevan): `Id` (int), `QuestionType` (string?, "MultipleChoice"/"MultipleAnswer"/"Essay"), `QuestionText` (string?), `ScoreValue` (int), `Options` (List<PackageOption> dgn `Id`/`IsCorrect`/`OptionText`).

From Models/AssessmentAttemptResponseArchive.cs (output type — dibuat plan 405-01): `AttemptHistoryId`, `PackageQuestionId`, `QuestionText`, `AnswerText`, `IsCorrect`, `AwardedScore`.

Mirror signature `ShouldHideRetakeToggle` ← `ShuffleToggleRules.ShouldHideShuffleToggle` (tapi retake: hanya PreTest||Manual; Proton TETAP retakeable, beda dari shuffle).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RetakeRules — CanRetake + ShouldHideRetakeToggle (RTK-03/13) [TDD]</name>
  <read_first>
    - Helpers/ShuffleToggleRules.cs (pola helper pure)
    - HcPortal.Tests/ShuffleToggleRulesTests.cs (pola test pure Theory/InlineData, no fixture)
    - 405-RESEARCH.md "Implementation Map → RTK-03" + spec §5 cabang
  </read_first>
  <files>Helpers/RetakeRules.cs, HcPortal.Tests/RetakeRulesTests.cs</files>
  <behavior>
    Signature (PURE, terima fakta — tak query DB):
    `CanRetake(bool allowRetake, string? assessmentType, bool isManualEntry, string status, bool? isPassed, int attemptsUsed, int maxAttempts, int retakeCooldownHours, DateTime? completedAt, DateTime nowUtc) → bool`

    Cabang (urutan guard, return false bila gagal):
    - allowRetake==false → false
    - assessmentType=="PreTest" → false (D6 diagnostik)
    - isManualEntry==true → false (RTK-13 hasil inject tak retakeable)
    - status!="Completed" → false (exclude InProgress/Abandoned/Cancelled/Open)
    - isPassed != false → false (null=PendingGrading & true=Lulus → tak eligible; RTK-13 exclude pending)
    - attemptsUsed >= maxAttempts → false (D7 cap)
    - cooldown: jika retakeCooldownHours <= 0 → true (no jeda); jika completedAt==null → false; else `nowUtc >= completedAt.Value.AddHours(retakeCooldownHours)`

    `ShouldHideRetakeToggle(string? assessmentType, bool isManualEntry) → bool` = `assessmentType=="PreTest" || isManualEntry`.

    Test cases (RED dulu) — mirror ShuffleToggleRulesTests style dengan helper `Can(...)` default-eligible + override per cabang:
    - Eligible_WhenAllConditionsMet → true
    - Blocked_WhenAllowRetakeOff / WhenPreTest / WhenManualEntry / WhenNotCompleted ("InProgress") / WhenCancelled / WhenPassed (isPassed:true) / WhenPendingGrading (isPassed:null) / WhenAttemptsExhausted (attemptsUsed:2,max:2) / WhenCooldownNotElapsed (completedAt: now-1h) → semua false
    - Eligible_WhenCooldownZero_NoWait (cooldownHours:0, completedAt:now) → true
    - Eligible_NullAssessmentType_StandaloneGraded (assessmentType:null) → true
    - ShouldHideRetakeToggle_Cases [Theory]: ("PreTest",false,true), (null,true,true), ("PostTest",false,false), (null,false,false)
    Fixed clock `Now = new DateTime(2026,06,19,12,00,00, DateTimeKind.Utc)`, default completedAt = Now.AddHours(-25).
  </behavior>
  <action>
RED: Buat `HcPortal.Tests/RetakeRulesTests.cs` dengan semua cabang di `<behavior>` (gunakan helper static `Can(...)` ber-default-param eligible + `[Theory]` untuk ShouldHideRetakeToggle). Run `dotnet test --filter "FullyQualifiedName~RetakeRulesTests"` → FAIL compile "RetakeRules does not exist". Commit RED: `test(405-02): RED RetakeRules eligibility cabang (RTK-03/13)`.

GREEN: Buat `Helpers/RetakeRules.cs` namespace `HcPortal.Helpers`, `public static class RetakeRules` dengan `CanRetake(...)` + `ShouldHideRetakeToggle(...)` persis `<behavior>`. XML-doc sebut v32.4 RTK-03 + "pure, dipakai service + Phase 407 ViewModel (kill-drift)" + catatan `completedAt` UTC + `nowUtc` di-inject untuk determinisme test. Run filter → GREEN semua hijau. Commit GREEN: `feat(405-02): RetakeRules pure eligibility (RTK-03/13)`.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeRulesTests" 2>&1 | grep -E "Passed!|Failed!|error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/RetakeRules.cs` berisi `public static bool CanRetake` dan `public static bool ShouldHideRetakeToggle`
    - `HcPortal.Tests/RetakeRulesTests.cs` berisi ≥13 fact/theory cases sesuai `<behavior>`
    - `dotnet test --filter "FullyQualifiedName~RetakeRulesTests"` → Passed (0 failed)
    - Dua commit terpisah RED lalu GREEN
  </acceptance_criteria>
  <done>RetakeRules pure + semua cabang hijau; signature siap dipakai service plan 405-03.</done>
</task>

<task type="tdd">
  <name>Task 2: RetakeArchiveBuilder — snapshot beku + essay full-text (RTK-02) [TDD]</name>
  <read_first>
    - Helpers/AssessmentScoreAggregator.cs:73 (IsQuestionCorrect) + :110-138 (BuildAnswerCell — perhatikan truncate essay :120)
    - Models/AssessmentAttemptResponseArchive.cs (output type, plan 405-01)
    - 405-RESEARCH.md "Implementation Map → RTK-02" + **Pitfall 2 (essay truncate)**
  </read_first>
  <files>Helpers/RetakeArchiveBuilder.cs, HcPortal.Tests/RetakeArchiveBuilderTests.cs</files>
  <behavior>
    Signature: `Build(int attemptHistoryId, IEnumerable<PackageQuestion> questions, IEnumerable<PackageUserResponse> responses) → List<AssessmentAttemptResponseArchive>`.

    Per question q: `forQ` = responses where PackageQuestionId==q.Id; `verdict` = `AssessmentScoreAggregator.IsQuestionCorrect(q, forQ)`.
    - AwardedScore: essay → `forQ.FirstOrDefault(r => r.PackageQuestionId==q.Id)?.EssayScore ?? 0`; MC/MA → `verdict==true ? q.ScoreValue : 0`.
    - AnswerText: **essay → `forQ.FirstOrDefault(...)?.TextAnswer` FULL (Pitfall 2, no truncate); MC/MA → `AssessmentScoreAggregator.BuildAnswerCell(q, forQ)`** (untuk kosong essay AnswerText boleh null atau "—" — pilih: essay kosong → null/"—" konsisten; MC/MA kosong → "—" dari BuildAnswerCell).
    - QuestionText = `q.QuestionText ?? ""`. IsCorrect = verdict. AttemptHistoryId = param.

    Test cases (RED dulu):
    - Build_MarksCorrectAndAwardsScore: MC q(id10,score5, opt100=correct,opt101=wrong), response pilih 100 → Single row: AttemptHistoryId==7, PackageQuestionId==10, QuestionText=="Q10", IsCorrect==true, AwardedScore==5, AnswerText=="opt100".
    - Build_WrongAnswer_ZeroScore_NotCorrect: response pilih 101 → IsCorrect==false, AwardedScore==0.
    - Build_EssayPending_NullCorrect_ZeroScore: Essay q(id20), response TextAnswer="jawaban panjang", EssayScore=null → IsCorrect==null, AwardedScore==0, AnswerText=="jawaban panjang".
    - **Build_EssayLongText_NotTruncated (Pitfall 2 — WAJIB)**: Essay response TextAnswer = string 500 char (mis. new string('x',500)), EssayScore=8 → `row.AnswerText.Length == 500` (BUKAN 303 = truncate+"..."), AwardedScore==8.
    Helper `Mc(int id, int scoreValue, params (int optId, bool correct)[] opts)` membuat PackageQuestion MC dgn Options.
  </behavior>
  <action>
RED: Buat `HcPortal.Tests/RetakeArchiveBuilderTests.cs` dengan 4 cases di `<behavior>` (termasuk Build_EssayLongText_NotTruncated assert `Length==500`). Run `dotnet test --filter "FullyQualifiedName~RetakeArchiveBuilderTests"` → FAIL "RetakeArchiveBuilder does not exist". Commit RED: `test(405-02): RED RetakeArchiveBuilder snapshot+essay-fulltext (RTK-02)`.

GREEN: Buat `Helpers/RetakeArchiveBuilder.cs` namespace `HcPortal.Helpers`, `public static class RetakeArchiveBuilder` dgn `Build(...)`. Verdict via `AssessmentScoreAggregator.IsQuestionCorrect` (kill-drift). **AnswerText cabang per-type:** essay → `forQ.FirstOrDefault(r => r.PackageQuestionId == q.Id)?.TextAnswer` (full, no truncate); MC/MA → `AssessmentScoreAggregator.BuildAnswerCell(q, forQ)`. AwardedScore sesuai `<behavior>`. XML-doc sebut RTK-02 + "verdict via aggregator (kill-drift)" + "essay full-text (Pitfall 2: BuildAnswerCell truncate 300 — archive permanen butuh full per D-04)". Run filter → GREEN. Commit GREEN: `feat(405-02): RetakeArchiveBuilder snapshot beku + essay full-text (RTK-02)`.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeArchiveBuilderTests" 2>&1 | grep -E "Passed!|Failed!|error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/RetakeArchiveBuilder.cs` berisi `public static List<AssessmentAttemptResponseArchive> Build` + memanggil `AssessmentScoreAggregator.IsQuestionCorrect`
    - Essay AnswerText TIDAK pakai `BuildAnswerCell` (pakai `TextAnswer` langsung) — terbukti test `Build_EssayLongText_NotTruncated` assert `Length==500`
    - `dotnet test --filter "FullyQualifiedName~RetakeArchiveBuilderTests"` → Passed (0 failed)
    - Dua commit terpisah RED lalu GREEN
  </acceptance_criteria>
  <done>Builder pure beku verdict + essay full-text; siap dipanggil RetakeService plan 405-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller fakta → RetakeRules | RetakeRules pure tak query DB; caller (service/ViewModel) suplai `attemptsUsed`/`completedAt`. Counting yang salah di caller = cap bypass (di-mitigasi plan 405-03). |
| Worker responses → archive snapshot | Verdict beku saat archive; tak boleh re-grade inline (drift dari Results/PDF). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-405-05 | Tampering | Re-grade inline di builder → verdict divergen dari Results/PDF/Excel | mitigate | Builder WAJIB pakai `AssessmentScoreAggregator.IsQuestionCorrect` (single source) — Task 2 acceptance enforce |
| T-405-06 | Information Disclosure | Essay full-text di archive — apakah bocorkan kunci? | accept | AnswerText = jawaban WORKER sendiri (bukan answer-key). Gating display (kunci ditahan) = Phase 407 (showWrongFlagsOnly). Di 405 helper hanya membentuk baris, tak expose. |
| T-405-07 | Elevation of Privilege | CanRetake longgar (mis. lupa cek IsPassed) → pekerja lulus bisa retake / pending bisa retake | mitigate | Cabang `isPassed != false → false` (null & true di-block) + `status!="Completed"` di-uji eksplisit (Blocked_WhenPassed/WhenPendingGrading/WhenNotCompleted) |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~RetakeRules"` → semua cabang hijau.
- `dotnet test --filter "FullyQualifiedName~RetakeArchiveBuilder"` → hijau, termasuk assert essay full-text (Length==500).
- `dotnet build` 0 error.
- Tidak ada re-grade inline (grep: builder hanya panggil `IsQuestionCorrect`/`BuildAnswerCell`, essay pakai TextAnswer).
</verification>

<success_criteria>
1. `RetakeRules.CanRetake` + `ShouldHideRetakeToggle` pure, semua cabang unit-tested hijau (RTK-03/13).
2. `RetakeArchiveBuilder.Build` beku verdict via aggregator + essay full-text (Pitfall 2 ditutup), unit-tested hijau (RTK-02).
3. `dotnet build` 0 error; tipe/signature siap dipakai RetakeService plan 405-03.
</success_criteria>

<output>
After completion, create `.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-02-SUMMARY.md`. Catat: konfirmasi essay full-text (bukan truncate), jumlah test cases, signature final `CanRetake`.
</output>
