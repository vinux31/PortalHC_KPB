---
phase: 405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint
plan: 04
type: execute
wave: 4
depends_on: ["405-03"]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [RTK-06, RTK-04, RTK-01]
tags: [retake, controller, refactor, endpoint, sibling-propagation]

must_haves:
  truths:
    - "ResetAssessment HC mendelegasikan ke RetakeService.ExecuteAsync (actionType ResetAssessment, reason hc_reset) dan tetap clear TempData[TokenVerified_{id}]; guard HC (IsResettable/Pre-Post/status) tetap di controller"
    - "Endpoint UpdateRetakeSettings menyimpan AllowRetake/MaxAttempts/RetakeCooldownHours + propagasi ke sibling (Title/Category/Schedule.Date) + clamp range + audit + PRG, [Authorize(Admin,HC)]+[ValidateAntiForgeryToken]"
    - "Pekerja baru yang ditambah ke assessment existing (standard add-users) mewarisi 3 kolom retake dari savedAssessment (bukan jatuh ke EF default diam-diam)"
    - "ResetGuardTests regresi tetap hijau (IsResettable tetap public static)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ResetAssessment delegasi + UpdateRetakeSettings + bulk-add carry + ManagePackages ViewBag"
      contains: "UpdateRetakeSettings"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (ResetAssessment)"
      to: "Services/RetakeService.cs (ExecuteAsync)"
      via: "_retakeService.ExecuteAsync(id, ..., actionType: ResetAssessment, reason: hc_reset)"
      pattern: "_retakeService\\.ExecuteAsync"
    - from: "Controllers/AssessmentAdminController.cs (UpdateRetakeSettings)"
      to: "AssessmentSessions sibling group (DB)"
      via: "sibling key Title==&&Category==&&Schedule.Date== → foreach set → SaveChanges"
      pattern: "s.Schedule.Date == assessment.Schedule.Date"
---

<objective>
Wire backend retake ke controller HC: (1) refactor `ResetAssessment` agar delegasi ke `RetakeService.ExecuteAsync(bypassGuards via HC override, reason:"hc_reset")` + clear `TempData[TokenVerified_{id}]` (guard HC tetap di controller); (2) endpoint baru `UpdateRetakeSettings` (mirror `UpdateShuffleSettings` — RBAC + AntiForgery + sibling propagation + clamp + audit + PRG); (3) carry 3 kolom retake di standard add-users bulk-add (mewarisi dari `savedAssessment`); (4) ManagePackages ViewBag state (untuk card UI Phase 406). Semua di `Controllers/AssessmentAdminController.cs`.

Purpose: Menutup jalur HC reset (sekarang via service bersama, dengan snapshot per-soal + claim-atomik) dan menyediakan endpoint config + inheritance bulk-add tanpa silent-default. UI card = Phase 406 (plan ini hanya backend + ViewBag).
Output: 1 file controller dimodifikasi (4 perubahan terpisah).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-CONTEXT.md
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-RESEARCH.md

<project_notes>
- Mirror endpoint: `UpdateShuffleSettings` (AssessmentAdminController.cs:5556-5611). Sibling key Title/Category/Schedule.Date.
- `AssessmentSession.Schedule` = `DateTime` (AssessmentSession.cs:18) → `.Date` legal.
- ResetGuardTests (HcPortal.Tests/ResetGuardTests.cs) memanggil `AssessmentAdminController.IsResettable(...)` STATIC — JANGAN ubah ke instance/DB (Pitfall 8).
- TempData token clear (must-fix #1): `TempData.Remove($"TokenVerified_{id}")` di caller setelah ExecuteAsync sukses (HTTP-scoped, bukan di service).
- Retake config TIDAK punya lock-saat-mulai (beda dari shuffle) — JANGAN copy `IsShuffleLocked` guard ke UpdateRetakeSettings. Hanya guard `ShouldHideRetakeToggle` (PreTest/Manual).
</project_notes>

<interfaces>
<!-- Semua anchor diverifikasi dari repo (file:line). -->

Constructor AssessmentAdminController (:33-56) — 12 param saat ini; `_context`/`_userManager`/`_auditLog` dari AdminBaseController (base). Inject RetakeService sebagai param ke-13:
```csharp
public AssessmentAdminController(
    ApplicationDbContext context, UserManager<ApplicationUser> userManager, AuditLogService auditLog,
    IWebHostEnvironment env, IMemoryCache cache, ILogger<AssessmentAdminController> logger,
    INotificationService notificationService, IHubContext<AssessmentHub> hubContext,
    IWorkerDataService workerDataService, GradingService gradingService,
    ProtonCompletionService protonCompletionService, ProtonBypassService protonBypassService)
    : base(context, userManager, auditLog, env) { ... }
```
Tambah `, HcPortal.Services.RetakeService retakeService` di parameter + field `private readonly HcPortal.Services.RetakeService _retakeService;` + `_retakeService = retakeService;` di body.

From Services/RetakeService.cs (plan 405-03):
```csharp
public readonly record struct RetakeResult(bool Success, string? Error);
public async Task<RetakeResult> ExecuteAsync(int sessionId, string actorUserId, string actorName, string actionType, string reason);
```

EXISTING ResetAssessment (AssessmentAdminController.cs:4192-4331):
- KEEP :4194-4197 (load+null-check), :4200-4208 (IsResettable guard), :4211-4225 (Pre-Post block), :4228-4236 (status guard), :4325-4330 (success TempData+redirect).
- REPLACE :4238-4323 (inline archive→delete→claim→audit→SignalR) dengan service call + error-redirect + TempData.Remove.

EXISTING UpdateShuffleSettings sibling-key (mirror VERBATIM, AssessmentAdminController.cs:5563-5567 analog):
```csharp
var siblingSessionIds = await _context.AssessmentSessions
    .Where(s => s.Title == assessment.Title && s.Category == assessment.Category && s.Schedule.Date == assessment.Schedule.Date)
    .Select(s => s.Id).ToListAsync();
```

Bulk-add standard add-users (AssessmentAdminController.cs:2166-2185) — block `newSessions = filteredNewUserIds.Select(uid => new AssessmentSession { ... })`. Sisip 3 field SETELAH `:2179` (`ShuffleOptions = savedAssessment.ShuffleOptions,`).
⚠️ JANGAN sentuh pre/post block :1944/:1965 (copy dari `model` ViewModel — belum punya field retake di 405 → compile error). Itu jatuh ke EF default (benar untuk Pre/Post per A2).

ManagePackages ViewBag insertion: SETELAH :5701 (`ViewBag.HideShuffleToggle = ShuffleToggleRules.ShouldHideShuffleToggle(...)`).

RetakeRules.ShouldHideRetakeToggle (plan 405-02): `HcPortal.Helpers.RetakeRules.ShouldHideRetakeToggle(string? assessmentType, bool isManualEntry)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject RetakeService + refactor ResetAssessment → delegasi service (RTK-06)</name>
  <read_first>
    - AssessmentAdminController.cs:33-56 (constructor — pola inject _gradingService)
    - AssessmentAdminController.cs:4186 (IsResettable static — JANGAN ubah) + :4192-4331 (ResetAssessment full)
    - Services/RetakeService.cs (ExecuteAsync signature, plan 405-03)
    - HcPortal.Tests/ResetGuardTests.cs (regresi yang harus tetap hijau) + 405-RESEARCH Pitfall 8 + "Implementation Map → RTK-06"
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
**A. Inject RetakeService:** Tambah `HcPortal.Services.RetakeService retakeService` ke parameter constructor (:45, setelah `protonBypassService`); field `private readonly HcPortal.Services.RetakeService _retakeService;` (dekat field service lain ~:29-31); `_retakeService = retakeService;` di body (~:55). (`_context`/`_userManager`/`_auditLog` dari base — tak perlu re-inject.)

**B. Refactor ResetAssessment:** Di `ResetAssessment` (:4192), GANTI blok inline `:4238-4323` (dari komentar `// Phase 46: Archive attempt data if session was Completed` sampai baris SignalR `:4323`) dengan:
```csharp
            var rsUser = await _userManager.GetUserAsync(User);
            var rsActorName = string.IsNullOrWhiteSpace(rsUser?.NIP) ? (rsUser?.FullName ?? "Unknown") : $"{rsUser.NIP} - {rsUser.FullName}";

            var rsResult = await _retakeService.ExecuteAsync(
                sessionId: id,
                actorUserId: rsUser?.Id ?? "",
                actorName: rsActorName,
                actionType: "ResetAssessment",
                reason: "hc_reset");

            if (!rsResult.Success)
            {
                TempData["Error"] = rsResult.Error ?? "Sesi tidak dapat direset.";
                return RedirectToAction("AssessmentMonitoringDetail", new {
                    title = assessment.Title,
                    category = assessment.Category,
                    scheduleDate = assessment.Schedule.Date.ToString("yyyy-MM-dd")
                });
            }

            // HC reset re-arms token verification on next entry (parity dgn worker retake) — must-fix #1.
            TempData.Remove($"TokenVerified_{id}");
```
PERTAHANKAN: guard HC `:4200-4236` (IsResettable + Pre-Post block + status guard) DAN trailing success `:4325-4330` (`TempData["Success"] = ...` + redirect AssessmentMonitoringDetail). `IsResettable` (:4186) TETAP `public static`.

`dotnet build` → 0 error. `dotnet test --filter "FullyQualifiedName~ResetGuardTests"` → hijau (guard tak dipindah).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -3 && dotnet test --filter "FullyQualifiedName~ResetGuardTests" 2>&1 | grep -E "Passed!|Failed!" | head -2</automated>
  </verify>
  <acceptance_criteria>
    - Constructor punya param `RetakeService retakeService` + field `_retakeService` + assignment
    - `ResetAssessment` memanggil `_retakeService.ExecuteAsync(... actionType: "ResetAssessment", reason: "hc_reset")` dan TIDAK lagi punya inline `AssessmentAttemptHistory` Add / `ExecuteUpdateAsync` / `SendAsync("sessionReset")` (dipindah ke service)
    - `ResetAssessment` mengandung `TempData.Remove($"TokenVerified_{id}")` setelah success
    - Guard `IsResettable`/Pre-Post block/status guard tetap di controller; `IsResettable` tetap `public static`
    - `dotnet build` 0 error; `ResetGuardTests` Passed
  </acceptance_criteria>
  <done>ResetAssessment delegasi ke service (snapshot+claim-atomik sekarang via RetakeService); regresi guard hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint UpdateRetakeSettings + ManagePackages ViewBag (RTK-04)</name>
  <read_first>
    - AssessmentAdminController.cs:5556-5611 (UpdateShuffleSettings — mirror VERBATIM: atribut, sibling key, foreach-set, audit try/catch, PRG)
    - AssessmentAdminController.cs:5697-5701 (ManagePackages ViewBag shuffle — insertion point setelah :5701)
    - Helpers/RetakeRules.cs (ShouldHideRetakeToggle, plan 405-02) + 405-RESEARCH "Implementation Map → RTK-04" + Pattern 2
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
**A. UpdateRetakeSettings:** Sisip endpoint baru PERSIS setelah `UpdateShuffleSettings` (setelah :5611):
```csharp
        // v32.4 RTK-04: explicit-save retake config (PRG + sibling propagation + audit), mirror UpdateShuffleSettings.
        [HttpPost]
        [Authorize(Roles = "Admin, HC")]
        [ValidateAntiForgeryToken]
        public async Task<IActionResult> UpdateRetakeSettings(int assessmentId, bool allowRetake, int maxAttempts, int retakeCooldownHours)
        {
            var assessment = await _context.AssessmentSessions.FirstOrDefaultAsync(a => a.Id == assessmentId);
            if (assessment == null) return NotFound();

            // Guard: retake hanya untuk graded (bukan Pre-Test) & bukan manual entry.
            if (HcPortal.Helpers.RetakeRules.ShouldHideRetakeToggle(assessment.AssessmentType, assessment.IsManualEntry))
            {
                TempData["Error"] = "Ujian ulang tidak berlaku untuk Pre-Test atau assessment manual.";
                return RedirectToAction("ManagePackages", new { assessmentId });
            }

            // Clamp ke range model (defense-in-depth; [Range] hanya client/model-validation).
            maxAttempts = Math.Clamp(maxAttempts, 1, 5);
            retakeCooldownHours = Math.Clamp(retakeCooldownHours, 0, 168);

            // Sibling group key identik shuffle (Title/Category/Schedule.Date).
            var siblingSessionIds = await _context.AssessmentSessions
                .Where(s => s.Title == assessment.Title && s.Category == assessment.Category && s.Schedule.Date == assessment.Schedule.Date)
                .Select(s => s.Id).ToListAsync();
            var siblings = await _context.AssessmentSessions.Where(s => siblingSessionIds.Contains(s.Id)).ToListAsync();
            var now = DateTime.UtcNow;
            foreach (var sibling in siblings)
            {
                // MaxAttempts < attempt terpakai = warning non-blocking (tetap simpan — tak hard-lock).
                sibling.AllowRetake = allowRetake;
                sibling.MaxAttempts = maxAttempts;
                sibling.RetakeCooldownHours = retakeCooldownHours;
                sibling.UpdatedAt = now;
            }
            await _context.SaveChangesAsync();

            try
            {
                var hcUser = await _userManager.GetUserAsync(User);
                var actorNameStr = string.IsNullOrWhiteSpace(hcUser?.NIP) ? (hcUser?.FullName ?? "Unknown") : $"{hcUser.NIP} - {hcUser.FullName}";
                await _auditLog.LogAsync(hcUser?.Id ?? "", actorNameStr, "UpdateRetakeSettings",
                    $"Set AllowRetake={allowRetake}, MaxAttempts={maxAttempts}, Cooldown={retakeCooldownHours}h for '{assessment.Title}' [grup {siblingSessionIds.Count} sesi]",
                    assessmentId, "AssessmentSession");
            }
            catch (Exception ex) { _logger.LogWarning(ex, "Audit log failed for UpdateRetakeSettings (assessmentId={Id})", assessmentId); }

            TempData["Success"] = "Pengaturan ujian ulang berhasil disimpan.";
            return RedirectToAction("ManagePackages", new { assessmentId });
        }
```

**B. ManagePackages ViewBag:** Sisip SETELAH :5701 (blok `ViewBag.HideShuffleToggle = ...`):
```csharp
            // === v32.4: Retake config state (RTK-05 — card di-render fase 406) ===
            ViewBag.AllowRetake = assessment.AllowRetake;
            ViewBag.MaxAttempts = assessment.MaxAttempts;
            ViewBag.RetakeCooldownHours = assessment.RetakeCooldownHours;
            ViewBag.HideRetakeToggle = HcPortal.Helpers.RetakeRules.ShouldHideRetakeToggle(assessment.AssessmentType, assessment.IsManualEntry);
            // Warning non-blocking: MaxAttempts < attempt terpakai peserta mana pun di grup.
            int retakeMaxArchivedForGroup = await _context.AssessmentAttemptHistory
                .Where(h => h.Title == assessment.Title && h.Category == assessment.Category)
                .GroupBy(h => h.UserId).Select(g => g.Count()).OrderByDescending(c => c).FirstOrDefaultAsync();
            ViewBag.RetakeMaxAttemptsUsedInGroup = retakeMaxArchivedForGroup + 1;
```
(Verifikasi `assessment.AssessmentType` + `assessment.IsManualEntry` + `assessment.UpdatedAt` ada di model — semua existing. `_logger`/`_auditLog`/`_userManager` tersedia.)

`dotnet build` → 0 error.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/AssessmentAdminController.cs` punya method `UpdateRetakeSettings(int assessmentId, bool allowRetake, int maxAttempts, int retakeCooldownHours)` dengan `[HttpPost]` + `[Authorize(Roles = "Admin, HC")]` + `[ValidateAntiForgeryToken]`
    - Berisi sibling key `s.Schedule.Date == assessment.Schedule.Date` + `Math.Clamp(maxAttempts, 1, 5)` + `Math.Clamp(retakeCooldownHours, 0, 168)` + audit `"UpdateRetakeSettings"` + PRG redirect ManagePackages
    - ManagePackages GET set `ViewBag.AllowRetake`/`MaxAttempts`/`RetakeCooldownHours`/`HideRetakeToggle`/`RetakeMaxAttemptsUsedInGroup`
    - TIDAK ada copy `IsShuffleLocked`-style lock-guard di UpdateRetakeSettings
    - `dotnet build` → "Build succeeded" 0 error
  </acceptance_criteria>
  <done>Endpoint config + sibling propagation + clamp + audit + ViewBag siap (card UI Phase 406 konsumsi ViewBag).</done>
</task>

<task type="auto">
  <name>Task 3: Carry 3 kolom retake di standard add-users bulk-add (RTK-01)</name>
  <read_first>
    - AssessmentAdminController.cs:2166-2185 (newSessions Select dari savedAssessment — insertion setelah :2179)
    - 405-RESEARCH "Implementation Map → RTK-01" + Pitfall 5 (JANGAN sentuh pre/post :1944/:1965 — copy dari model)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Di block `newSessions = filteredNewUserIds.Select(uid => new AssessmentSession { ... })` (:2166), sisip 3 field SETELAH `:2179` (`ShuffleOptions = savedAssessment.ShuffleOptions,`):
```csharp
                                AllowRetake = savedAssessment.AllowRetake,
                                MaxAttempts = savedAssessment.MaxAttempts,
                                RetakeCooldownHours = savedAssessment.RetakeCooldownHours,
```
(Sumber = `savedAssessment` = sesi sibling existing. Pekerja baru mewarisi policy retake yang sama, bukan jatuh diam-diam ke EF default.)

⚠️ JANGAN tambah field retake di pre/post add-users block (:1944 `newPre` / :1965 `newPost`) — keduanya copy dari `model` (ViewModel tanpa field retake di Phase 405) → akan compile-error. Jalur itu jatuh ke EF default (false/2/24) yang BENAR untuk Pre (AllowRetake=false per D6) dan Post (default off sampai admin ON). Wire `model` → pre/post = Phase 406 bila diinginkan.

`dotnet build` → 0 error.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Block `newSessions` (:2166) mengandung `AllowRetake = savedAssessment.AllowRetake`, `MaxAttempts = savedAssessment.MaxAttempts`, `RetakeCooldownHours = savedAssessment.RetakeCooldownHours`
    - Block pre/post (:1944 `newPre` / :1965 `newPost`) TIDAK diubah (tetap jatuh ke EF default)
    - `dotnet build` → "Build succeeded" 0 error
  </acceptance_criteria>
  <done>Pekerja baru di assessment existing mewarisi config retake; tak ada silent-default di standard add-users.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/form → UpdateRetakeSettings | Untrusted POST (assessmentId + values). RBAC + AntiForgery + clamp + guard wajib. |
| Browser/form → ResetAssessment | Untrusted POST (sessionId). RBAC + AntiForgery + IsResettable/Pre-Post/status guard. |
| HC actor → cross-assessment config | Sibling propagation menulis ke grup — actor harus Admin/HC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-405-13 | Tampering | CSRF pada UpdateRetakeSettings/ResetAssessment | mitigate | `[ValidateAntiForgeryToken]` di kedua endpoint (Task 1 keep, Task 2 add) |
| T-405-14 | Elevation of Privilege | Non-Admin/HC ubah config retake atau reset sesi orang lain | mitigate | `[Authorize(Roles = "Admin, HC")]` di UpdateRetakeSettings (Task 2) + ResetAssessment existing (Task 1 keep) |
| T-405-15 | Tampering | MaxAttempts/cooldown di luar range (mis. 9999) via form bypass `[Range]` | mitigate | `Math.Clamp(maxAttempts,1,5)` + `Math.Clamp(retakeCooldownHours,0,168)` server-side (Task 2) |
| T-405-16 | Spoofing | Stale token re-entry pasca-reset (worker masuk tanpa verifikasi token) | mitigate | `TempData.Remove($"TokenVerified_{id}")` setelah ExecuteAsync sukses (Task 1, must-fix #1) |
| T-405-17 | Tampering | Retake config di-set untuk Pre-Test/Manual (tak boleh retakeable) | mitigate | Guard `RetakeRules.ShouldHideRetakeToggle` reject di UpdateRetakeSettings (Task 2) |

**Out-of-scope (deferred):** Worker `CMP/RetakeExam` ownership check + server-side CanRetake re-validation = Phase 407 (UI worker). Card UI render + form binding = Phase 406. Plan ini hanya backend HC + ViewBag.
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "FullyQualifiedName~ResetGuardTests"` → hijau (regresi RTK-06).
- `dotnet test` full suite → hijau (no regresi; integration butuh SQLEXPRESS).
- Grep: `UpdateRetakeSettings` punya RBAC+AntiForgery+clamp+sibling-key; `ResetAssessment` delegasi `_retakeService.ExecuteAsync` + `TempData.Remove`.
</verification>

<success_criteria>
1. ResetAssessment delegasi ke RetakeService (actionType ResetAssessment/reason hc_reset) + clear token; guard HC tetap; ResetGuardTests hijau. (RTK-06)
2. UpdateRetakeSettings: RBAC + AntiForgery + sibling propagation + clamp + audit + PRG. (RTK-04)
3. Standard add-users bulk-add mewarisi 3 kolom retake dari savedAssessment; ManagePackages ViewBag siap. (RTK-01)
4. `dotnet build` 0 error; suite hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-04-SUMMARY.md`. Catat: konfirmasi ResetGuardTests hijau, hasil full suite, daftar 4 perubahan controller. **Catat migration=TRUE flag (dari plan 405-01) untuk notify IT saat promosi (bundle dgn v32.1+v32.3).**
</output>
