---
phase: 406-admin-config-ui-riwayat-hc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/ManagePackages.cshtml
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/EditAssessment.cshtml
  - tests/e2e/retake-config-406.spec.ts
autonomous: true
requirements: [RTK-05]
must_haves:
  truths:
    - "A 'Ujian Ulang' config card renders in ManagePackages AFTER the shuffle card, hidden entirely for Pre-Test / Manual Entry"
    - "Toggling AllowRetake reveals/hides two number inputs (MaxAttempts 1-5, RetakeCooldownHours 0-168) via progressive-disclosure JS"
    - "A non-blocking warning shows when MaxAttempts < RetakeMaxAttemptsUsedInGroup but never disables Save (no lock)"
    - "Saving POSTs to existing UpdateRetakeSettings and the persisted values are reflected on reload"
    - "CreateAssessment (Step 3) and EditAssessment bind AllowRetake/MaxAttempts/RetakeCooldownHours via asp-for on the AssessmentSession model"
  artifacts:
    - path: "Views/Admin/ManagePackages.cshtml"
      provides: "Retake config card (toggle + disclosure + 2 number inputs + warning + hide) + disclosure JS"
      contains: "name=\"allowRetake\""
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "Retake binding column in Step 3 (after shuffle col, before Sertifikat)"
      contains: "asp-for=\"AllowRetake\""
    - path: "Views/Admin/EditAssessment.cshtml"
      provides: "Retake binding fields near PassPercentage"
      contains: "asp-for=\"AllowRetake\""
    - path: "tests/e2e/retake-config-406.spec.ts"
      provides: "Playwright @5270: card render/disclosure/hide/warning/save-persist + Create/Edit binding"
      contains: "allowRetake"
  key_links:
    - from: "Views/Admin/ManagePackages.cshtml retake card form"
      to: "AssessmentAdminController.UpdateRetakeSettings"
      via: "asp-action=UpdateRetakeSettings POST with AntiForgery + assessmentId hidden"
      pattern: "asp-action=\"UpdateRetakeSettings\""
    - from: "Views/Admin/ManagePackages.cshtml #allowRetake"
      to: "#retakeFields"
      via: "change listener toggles .d-none (progressive disclosure)"
      pattern: "getElementById\\('allowRetake'\\)"
    - from: "Views/Admin/CreateAssessment.cshtml + EditAssessment.cshtml"
      to: "AssessmentSession model fields"
      via: "asp-for binding (native, no ViewModel)"
      pattern: "asp-for=\"MaxAttempts\""
---

<objective>
Build SURFACE 1 — the Admin/HC retake-policy config UI (RTK-05): the "Ujian Ulang" card in
ManagePackages (mirror the shuffle card, MINUS lock, PLUS progressive disclosure + non-blocking
warning) and the asp-for binding fields in CreateAssessment (Step 3) and EditAssessment. The
card POSTs to the EXISTING `UpdateRetakeSettings` endpoint (no controller change). All ViewBag
data is already exposed by the ManagePackages action (405-04, :5703-5711).

Purpose: Admin/HC must be able to turn retake on/off and set MaxAttempts + cooldown per
assessment, without a hard lock (D-03 — config is retroactive so it must stay editable), and the
card must vanish for Pre-Test/Manual where retake is nonsensical.

Output: edits to `Views/Admin/ManagePackages.cshtml`, `Views/Admin/CreateAssessment.cshtml`,
`Views/Admin/EditAssessment.cshtml`, and a new Playwright spec
`tests/e2e/retake-config-406.spec.ts` (runtime-verifies the Razor — Lesson Phase 354).

This plan is PARALLEL with Plan 01 (Wave 1): disjoint files (01 = riwayat backend; 02 = config
views). It does NOT touch AssessmentMonitoringDetail.cshtml or the controller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/406-admin-config-ui-riwayat-hc/406-UI-SPEC.md
@.planning/phases/406-admin-config-ui-riwayat-hc/406-RESEARCH.md
@.planning/phases/406-admin-config-ui-riwayat-hc/406-CONTEXT.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md

<interfaces>
<!-- Verified against the codebase. Use directly. -->

ManagePackages action ViewBag (already set — AssessmentAdminController.cs:5670,:5703-5711):
  ViewBag.AssessmentId                 (int)
  ViewBag.AllowRetake                  (bool)              -> assessment.AllowRetake
  ViewBag.MaxAttempts                  (int, default 2)    -> assessment.MaxAttempts
  ViewBag.RetakeCooldownHours          (int, default 24)
  ViewBag.HideRetakeToggle             (bool)              -> RetakeRules.ShouldHideRetakeToggle(type, isManualEntry)
  ViewBag.RetakeMaxAttemptsUsedInGroup (int, >=1 non-null) -> retakeMaxArchivedForGroup + 1

POST target (EXISTING — do NOT change it; AssessmentAdminController.cs:5567):
  UpdateRetakeSettings(int assessmentId, bool allowRetake, int maxAttempts, int retakeCooldownHours)
  [Authorize(Admin,HC)] + [ValidateAntiForgeryToken] + Math.Clamp + sibling propagation + audit + PRG redirect to ManagePackages

Shuffle card to MIRROR (ManagePackages.cshtml:89-131): card mb-4 > card-header bg-light > h5.mb-0 + form asp-action="UpdateShuffleSettings" + @Html.AntiForgeryToken() + form-check form-switch + btn btn-primary. DROP its lock logic (`isShuffleLocked`/`disabled`).

Models/AssessmentSession.cs:44-54 (Create/Edit @model — native asp-for, [Range] already present):
  public bool AllowRetake { get; set; } = false;
  [Range(1, 5)]   public int MaxAttempts { get; set; } = 2;
  [Range(0, 168)] public int RetakeCooldownHours { get; set; } = 24;

CreateAssessment.cshtml: @model HcPortal.Models.AssessmentSession; insert in Step 3 / Group B grid AFTER the shuffle column (~:551), BEFORE Group C "Sertifikat" (~:556).
EditAssessment.cshtml:  @model HcPortal.Models.AssessmentSession; insert in "Pengaturan Assessment" card (:384-413) near PassPercentage (:391) + AllowAnswerReview toggle (:401).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retake config card in ManagePackages.cshtml (mirror shuffle, no lock) + disclosure JS</name>
  <files>Views/Admin/ManagePackages.cshtml</files>
  <read_first>
    - Views/Admin/ManagePackages.cshtml:83-132 (shuffle card to MIRROR; note lock vars to OMIT) and :324-340 (existing inline toggle-warning JS pattern to mirror for the disclosure script) and :24 (TempData error alert convention)
    - 406-UI-SPEC.md "Component Contract 1" (VERBATIM card markup) + "Pattern 1"/"Pattern 2" in 406-RESEARCH.md (the approved card + the `?? 2`/`?? 0` null-coalesce on the warning cast — Pitfall 1)
    - 406-CONTEXT.md D-03 (no lock) + D-04 (2 number inputs + helper + disclosure + hide)
  </read_first>
  <action>
    Insert the retake card in `Views/Admin/ManagePackages.cshtml` immediately AFTER the shuffle card's closing `</div>` (the shuffle `card mb-4` ends ~:132), BEFORE the next card. Use the UI-SPEC Component Contract 1 markup VERBATIM, including:
    - Outer guard `@if (ViewBag.HideRetakeToggle != true) { ... }` so the card is NOT RENDERED for Pre-Test/Manual.
    - `bool arChecked = ViewBag.AllowRetake == true;` then card mb-4 > card-header bg-light > `<h5 class="mb-0"><i class="bi bi-arrow-repeat text-primary me-1"></i>Ujian Ulang</h5>`.
    - `<form method="post" asp-action="UpdateRetakeSettings" asp-controller="AssessmentAdmin">` + `@Html.AntiForgeryToken()` + hidden `assessmentId` = `@ViewBag.AssessmentId`.
    - Toggle `form-check form-switch mb-2` checkbox `name="allowRetake" id="allowRetake" value="true"` checked when arChecked + label `Izinkan Ujian Ulang` + helper `form-text text-muted mb-3` with the toggle-helper copy.
    - `<div id="retakeFields" class="@(arChecked ? "" : "d-none")">` containing the two number inputs (max-width:320px each): `name="maxAttempts" id="maxAttempts" min="1" max="5" value="@(ViewBag.MaxAttempts ?? 2)"` + helper, then the NON-BLOCKING warning `@if ((int)(ViewBag.MaxAttempts ?? 2) < (int)(ViewBag.RetakeMaxAttemptsUsedInGroup ?? 0)) { <div class="alert alert-warning d-flex align-items-start mt-2 mb-0" role="alert">...warning copy...</div> }` (USE the `?? 2`/`?? 0` defensive cast — Pitfall 1), then `name="retakeCooldownHours" id="retakeCooldownHours" min="0" max="168" value="@(ViewBag.RetakeCooldownHours ?? 24)"` + helper.
    - Submit `<button type="submit" class="btn btn-primary"><i class="bi bi-save me-1"></i>Simpan Pengaturan</button>`.
    - DO NOT add any `disabled` / lock alert (D-03 — unlike shuffle).
    All copy strings VERBATIM from 406-UI-SPEC Copywriting Contract (retake config card table).
    Add the disclosure JS at the end of the view (inside/append the existing `<script>` block, mirror :324 pattern):
    ```javascript
    document.getElementById('allowRetake')?.addEventListener('change', function () {
        document.getElementById('retakeFields')?.classList.toggle('d-none', !this.checked);
    });
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -c "name=\"allowRetake\"\|asp-action=\"UpdateRetakeSettings\"\|id=\"retakeFields\"\|Simpan Pengaturan\|alert-warning" Views/Admin/ManagePackages.cshtml && grep -c "disabled" Views/Admin/ManagePackages.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - ManagePackages.cshtml contains `name="allowRetake"`, `asp-action="UpdateRetakeSettings"`, `id="retakeFields"`, `id="maxAttempts"`, `id="retakeCooldownHours"`, `Simpan Pengaturan`, and the `@if (ViewBag.HideRetakeToggle != true)` guard.
    - The warning cast uses `(int)(ViewBag.MaxAttempts ?? 2)` and `(int)(ViewBag.RetakeMaxAttemptsUsedInGroup ?? 0)` (defensive — no bare cast).
    - The retake card region adds NO new `disabled`/lock attribute (the only `disabled` occurrences remain in the shuffle card — confirm count unchanged from baseline).
    - Disclosure JS `getElementById('allowRetake')` listener present.
    - Runtime verification deferred to Task 3 (Playwright) — build+grep is necessary but NOT sufficient for this Razor surface (Lesson 354).
  </acceptance_criteria>
  <done>Retake card mirrors shuffle (no lock), hides for Pre-Test/Manual, reveals 2 number inputs on toggle, shows non-blocking warning, POSTs to existing UpdateRetakeSettings.</done>
</task>

<task type="auto">
  <name>Task 2: asp-for retake binding in CreateAssessment (Step 3) + EditAssessment</name>
  <files>Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml:1 (@model AssessmentSession), :536-551 (shuffle column in Step 3 / Group B grid to MIRROR + insertion point), :521 (existing token-section disclosure JS pattern to mirror)
    - Views/Admin/EditAssessment.cshtml:1 (@model), :384-413 ("Pengaturan Assessment" card), :391 (PassPercentage), :396 (asp-validation-for span pattern), :401 (AllowAnswerReview toggle to mirror)
    - 406-UI-SPEC.md "Component Contract 1b" (binding placement + asp-for + validation span) + Copywriting Contract (same labels/helpers)
  </read_first>
  <action>
    CreateAssessment.cshtml — add a retake column in Step 3 / Group B settings grid, AFTER the shuffle column (~:551) and BEFORE Group C "Sertifikat" (~:556). Mirror the shuffle column's `<div class="col-md-6">` + `form-check form-switch` markup:
    - Toggle: `<input asp-for="AllowRetake" class="form-check-input" type="checkbox" />` + `<label asp-for="AllowRetake" class="form-check-label">Izinkan Ujian Ulang</label>` + helper `form-text text-muted` (toggle-helper copy).
    - Disclosure wrapper `<div id="retakeFieldsCreate" class="@(Model.AllowRetake ? "" : "d-none")">` (or render always + JS toggle keyed on `#AllowRetake`) holding two number inputs: `<input asp-for="MaxAttempts" type="number" min="1" max="5" class="form-control" />` + label + helper + `<span asp-validation-for="MaxAttempts" class="text-danger"></span>`; same for `RetakeCooldownHours` (min 0 max 168). Labels/helpers VERBATIM from Copywriting Contract.
    - Add disclosure JS keyed on the rendered `id="AllowRetake"` (asp-for emits id=property name): on change toggle the wrapper `.d-none`. Mirror the existing token-section toggle JS (~:521).
    EditAssessment.cshtml — add inside the "Pengaturan Assessment" card (:384-413) as a `col-md-6 mb-3` near PassPercentage (:391) and the AllowAnswerReview toggle (:401), mirroring the `form-check form-switch` + `form-text text-muted` + `asp-validation-for` pattern there. Same toggle + 2 number inputs + same disclosure JS keyed on `#AllowRetake`.
    Both forms bind the live `@model AssessmentSession`; native model binding handles persistence on the existing wizard/edit POST (EF default covers create path — RTK-01). No ViewModel, no new POST handler.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -c "asp-for=\"AllowRetake\"\|asp-for=\"MaxAttempts\"\|asp-for=\"RetakeCooldownHours\"" Views/Admin/CreateAssessment.cshtml && grep -c "asp-for=\"AllowRetake\"\|asp-for=\"MaxAttempts\"\|asp-for=\"RetakeCooldownHours\"" Views/Admin/EditAssessment.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - CreateAssessment.cshtml contains `asp-for="AllowRetake"`, `asp-for="MaxAttempts"`, `asp-for="RetakeCooldownHours"` (grep count >= 3).
    - EditAssessment.cshtml contains the same three `asp-for` bindings (grep count >= 3).
    - Both views include a disclosure script keyed on the rendered `AllowRetake` id.
    - Number inputs carry `min="1" max="5"` (MaxAttempts) and `min="0" max="168"` (RetakeCooldownHours); MaxAttempts has an `asp-validation-for` span.
    - Runtime binding proven in Task 3 (Playwright).
  </acceptance_criteria>
  <done>Create + Edit forms bind the three retake fields natively (asp-for) with progressive disclosure and validation span; persistence rides the existing model-bound POST.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright retake-config-406.spec.ts (runtime-verify card render/disclosure/hide/warning/save + binding) @5270</name>
  <files>tests/e2e/retake-config-406.spec.ts</files>
  <read_first>
    - tests/e2e/shuffle.spec.ts (MIRROR template: DB snapshot/restore, ManagePackages navigation, toggle+save POST + reload assert)
    - CLAUDE.md (DEV_WORKFLOW + branch ITHandoff port 5270) + docs/SEED_WORKFLOW.md (snapshot -> seed -> restore; journal cleaned)
    - reference: app + Playwright run @ http://localhost:5270 on branch ITHandoff; Authentication__UseActiveDirectory=false; admin@pertamina.com / 123456; --workers=1
    - 406-VALIDATION.md "Per-Task Verification Map" (RTK-05 e2e behaviors) + "Wave 0 Requirements"
  </read_first>
  <action>
    Create `tests/e2e/retake-config-406.spec.ts` mirroring `shuffle.spec.ts`. baseURL http://localhost:5270; login admin@pertamina.com/123456; `--workers=1`. Per CLAUDE.md SEED_WORKFLOW: snapshot the local DB before seeding the temp graded assessment, restore after (record in docs/SEED_JOURNAL.md, mark cleaned). Cover:
    - "card render": open ManagePackages for a GRADED (non-PreTest, non-Manual) assessment; assert the "Ujian Ulang" card is visible (`#allowRetake` present). With AllowRetake off, `#retakeFields` has class `d-none`.
    - "disclosure": click `#allowRetake`; assert `#retakeFields` no longer has `d-none` and both number inputs are visible.
    - "save": set maxAttempts + retakeCooldownHours, submit, wait for PRG redirect, reload ManagePackages, assert the toggle reflects ON and the number inputs show the saved values (persistence).
    - "hide": open ManagePackages for a Pre-Test (or Manual-entry) assessment; assert the "Ujian Ulang" card / `#allowRetake` is NOT present (hidden entirely).
    - "warning": seed/arrange so RetakeMaxAttemptsUsedInGroup exceeds a low MaxAttempts (or set MaxAttempts below the used count), assert the `.alert-warning` near MaxAttempts is visible AND the Save button is still enabled (non-blocking) — saving succeeds.
    - "binding": open CreateAssessment, toggle AllowRetake, assert the MaxAttempts/RetakeCooldownHours inputs reveal and bind (id AllowRetake/MaxAttempts/RetakeCooldownHours present); open EditAssessment for the seeded assessment and assert the same three fields render and reflect persisted values.
    Use grep-able test titles matching 406-RESEARCH Test Map (`-g "card render"`, `-g "disclosure"`, `-g "save"`, `-g "hide"`, `-g "warning"`, `-g "binding"`).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff/tests" && npx playwright test e2e/retake-config-406.spec.ts --workers=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/retake-config-406.spec.ts --workers=1` passes (all 6 scenarios green) @5270.
    - The spec asserts: card visible for graded; `d-none` toggles on disclosure; saved values persist after reload; card absent for Pre-Test/Manual; warning visible + Save still enabled; Create/Edit bind the three fields.
    - DB restored after run (SEED_JOURNAL.md entry marked cleaned).
  </acceptance_criteria>
  <done>The retake config Razor surface is runtime-verified end-to-end (render, disclosure, hide, non-blocking warning, save-persist, Create/Edit binding) at :5270 — satisfying Lesson 354 (grep+build insufficient).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Admin/HC) -> UpdateRetakeSettings POST | Untrusted form values (maxAttempts/retakeCooldownHours) cross into config persistence |
| Client number inputs (min/max) | Client-side range is advisory only; can be bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-406-06 | Tampering | UpdateRetakeSettings config save | mitigate | `@Html.AntiForgeryToken()` on the card form + `[ValidateAntiForgeryToken]` on the existing endpoint. CSRF closed. (No endpoint change in this plan.) |
| T-406-07 | Tampering | Client bypass of input min/max -> out-of-range MaxAttempts/cooldown | mitigate | Server `Math.Clamp(1,5)` / `Math.Clamp(0,168)` already in UpdateRetakeSettings (:5580-5581). Input min/max are UX hints, NOT the guard. |
| T-406-08 | Elevation | Non-Admin/HC reaching the config endpoint | accept (already mitigated upstream) | `[Authorize(Roles = "Admin, HC")]` on UpdateRetakeSettings + ManagePackages — view only renders inside the authorized page; no new endpoint introduced here. |
| T-406-09 | Information disclosure | Retake card shown where retake is invalid (Pre-Test/Manual) leaking a misleading control | mitigate | `@if (ViewBag.HideRetakeToggle != true)` guard (RetakeRules.ShouldHideRetakeToggle) — card not rendered for Pre-Test/Manual. |
</threat_model>

<verification>
- `dotnet build` exits 0 (all three Razor views compile).
- `npx playwright test e2e/retake-config-406.spec.ts --workers=1` passes @5270 (render/disclosure/hide/warning/save/binding).
- Static: ManagePackages has the `ViewBag.HideRetakeToggle != true` guard, defensive `?? 2`/`?? 0` warning cast, no new lock/disabled.
- Static: Create + Edit each contain the three `asp-for` retake bindings.
- DB snapshot/restore honored (SEED_WORKFLOW); journal marked cleaned.
</verification>

<success_criteria>
- "Ujian Ulang" card renders after the shuffle card for graded assessments, hidden for Pre-Test/Manual, with toggle-driven disclosure of two clamped number inputs and a non-blocking warning that never disables Save.
- Saving persists via the existing UpdateRetakeSettings endpoint and reflects on reload (with sibling propagation handled server-side).
- CreateAssessment + EditAssessment bind the three retake fields via asp-for with validation.
- All proven by green Playwright @5270 (runtime Razor verification).
</success_criteria>

<output>
After completion, create `.planning/phases/406-admin-config-ui-riwayat-hc/406-02-SUMMARY.md`
</output>
