---
phase: 407-worker-self-service-gating-tier-feedback-riwayat-pekerja
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/RetakeRules.cs
  - Models/AssessmentResultsViewModel.cs
  - Models/AllWorkersHistoryRow.cs
  - HcPortal.Tests/RetakeRulesTests.cs
autonomous: true
requirements: [RTK-11, RTK-12]
must_haves:
  truths:
    - "RetakeRules.ResolveReviewMode mengembalikan ShowScoreOnly bila AllowAnswerReview==false (state apa pun)"
    - "ResolveReviewMode mengembalikan ShowWrongFlagsOnly bila AllowAnswerReview==true & belum-lulus (failed ATAU pending null) & attempt-sisa — leak-safe: sembunyikan kunci selama retake masih mungkin"
    - "ResolveReviewMode mengembalikan ShowFullReview HANYA bila passed, ATAU belum-lulus & exhausted (no attempt) — saat retake tak mungkin lagi"
    - "AssessmentResultsViewModel membawa RetakeMode/CanRetake/CurrentAttempt/MaxAttempts/CooldownUntilUtc/IsCapReached/RiwayatAttempts"
    - "AllWorkersHistoryRow punya flag IsCurrentAttempt"
  artifacts:
    - path: "Helpers/RetakeRules.cs"
      provides: "enum RetakeReviewMode + static ResolveReviewMode (pure tier resolver)"
      contains: "public static RetakeReviewMode ResolveReviewMode("
    - path: "Models/AssessmentResultsViewModel.cs"
      provides: "field retake/tier (RetakeMode, CanRetake, CurrentAttempt, MaxAttempts, CooldownUntilUtc, IsCapReached, RiwayatAttempts)"
      contains: "public HcPortal.Helpers.RetakeReviewMode RetakeMode"
    - path: "Models/AllWorkersHistoryRow.cs"
      provides: "flag IsCurrentAttempt untuk badge Percobaan saat ini"
      contains: "public bool IsCurrentAttempt"
    - path: "HcPortal.Tests/RetakeRulesTests.cs"
      provides: "5 test cabang ResolveReviewMode (incl pending null)"
      contains: "ResolveReviewMode"
  key_links:
    - from: "Models/AssessmentResultsViewModel.cs"
      to: "Helpers/RetakeRules.cs"
      via: "tipe RetakeReviewMode dipakai sebagai tipe field RetakeMode"
      pattern: "RetakeReviewMode RetakeMode"
---

<objective>
Sediakan FONDASI murni (helper + VM) untuk sisi-pekerja ujian ulang: helper pure `RetakeRules.ResolveReviewMode` (enum 3-state tier feedback) yang leak-safe, plus perluasan ViewModel (`AssessmentResultsViewModel` + `AllWorkersHistoryRow`) yang akan diisi controller (407-02) dan dirender view (407-03).

Purpose: Tier feedback HARUS deterministik + unit-testable supaya keputusan leak-safety (kunci jawaban ditahan selama masih bisa ulang) diuji di unit, bukan di view. VM diperluas lebih dulu agar controller (Wave 2) dan view (Wave 3) punya kontrak yang sudah ada — interface-first, anti scavenger-hunt.

Output: `RetakeReviewMode` enum + `ResolveReviewMode` (pure) di `Helpers/RetakeRules.cs`; 7 field baru di `AssessmentResultsViewModel`; flag `IsCurrentAttempt` di `AllWorkersHistoryRow`; 5 unit test cabang truth-table di `RetakeRulesTests.cs`. **0 migration.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-CONTEXT.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-RESEARCH.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-PATTERNS.md

<interfaces>
<!-- Kontrak existing yang dipakai/diperluas plan ini. Diekstrak dari kode — executor tak perlu eksplorasi. -->

From Helpers/RetakeRules.cs (existing pure helper — TAMBAH adjacent to CanRetake, JANGAN ubah CanRetake/ShouldHideRetakeToggle):
```csharp
public static class RetakeRules {
    public static bool CanRetake(bool allowRetake, string? assessmentType, bool isManualEntry,
        string status, bool? isPassed, int attemptsUsed, int maxAttempts, int retakeCooldownHours,
        DateTime? completedAt, DateTime nowUtc) { /* fail-fast chain */ }
    public static bool ShouldHideRetakeToggle(string? assessmentType, bool isManualEntry) => ...;
}
```

From Models/AssessmentResultsViewModel.cs (existing — JANGAN hapus field apa pun, terutama AllowAnswerReview & IsPassed bool & IsPendingGrading):
```csharp
public class AssessmentResultsViewModel {
    public int AssessmentId { get; set; }
    public int Score { get; set; }
    public int PassPercentage { get; set; }
    public bool IsPassed { get; set; }          // KEEP (cert/banner) — bool non-nullable
    public bool AllowAnswerReview { get; set; }  // KEEP — feeds tier (CONTEXT: do not delete)
    public DateTime? CompletedAt { get; set; }
    public List<QuestionReviewItem>? QuestionReviews { get; set; }
    public bool IsPendingGrading { get; set; } = false;
    // ... field lain dipertahankan ...
}
```

From Models/RiwayatAttemptViewModel.cs (sudah ada — dipakai sebagai tipe field RiwayatAttempts):
```csharp
public class RiwayatAttemptViewModel {
    public int AttemptNumber { get; set; }
    public int? ScorePercent { get; set; }
    public bool? IsPassed { get; set; }
    public DateTime? CompletedAt { get; set; }
    public bool IsCurrent { get; set; }
    public List<AssessmentAttemptResponseArchive> Rows { get; set; } = new();
}
```

From Models/AllWorkersHistoryRow.cs (existing — TAMBAH satu field di akhir, JANGAN ubah field lain):
```csharp
public class AllWorkersHistoryRow {
    // ... WorkerId/WorkerName/RecordType/Title/Date/Score/IsPassed/AttemptNumber/SessionId ...
}
```

From HcPortal.Tests/RetakeRulesTests.cs (existing — class pola, TAMBAH Fact baru; idiom `Can(...)` default-eligible helper untuk CanRetake; tier test panggil ResolveReviewMode langsung tanpa helper):
```csharp
public class RetakeRulesTests {
    [Fact] public void Eligible_WhenAllConditionsMet() => Assert.True(Can());
    [Theory][InlineData(...)] public void ShouldHideRetakeToggle_Cases(...) => ...;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tambah enum RetakeReviewMode + ResolveReviewMode (pure) ke RetakeRules + 5 unit test</name>
  <files>Helpers/RetakeRules.cs, HcPortal.Tests/RetakeRulesTests.cs</files>
  <read_first>
    - Helpers/RetakeRules.cs (file yang dimodifikasi — pola pure static, namespace HcPortal.Helpers, CanRetake :29-52 sebagai analog idiom fail-fast)
    - HcPortal.Tests/RetakeRulesTests.cs (file test yang diperluas — class RetakeRulesTests, [Fact] per cabang, no DB/no fixture)
    - 407-PATTERNS.md §"Helpers/RetakeRules.cs — ResolveReviewMode" (excerpt siap-salin enum + method + 5 Fact)
    - 407-RESEARCH.md §"Architecture Patterns Pattern 3" (truth table D-03) + §"Pitfall 5" (gunakan IsPassed bool? langsung). NOTE: orchestrator A1 OVERRIDE saran researcher — pending null & attempt-sisa = ShowWrongFlagsOnly (leak-safe), BUKAN ShowFullReview.
  </read_first>
  <behavior>
    Truth table ResolveReviewMode(allowAnswerReview, isPassed (bool?), attemptsRemaining) — LOCKED orchestrator (A1 leak-safe):
    Prinsip D-03: kunci jawaban TIDAK PERNAH tampil selama retake masih MUNGKIN. "Retake mungkin" = belum-lulus (isPassed != true: failed ATAU pending null) DAN attemptsRemaining. Pending diperlakukan SAMA dengan failed (bisa transisi ke failed+retake → kunci yang tampil saat pending bocor untuk retake soal sama).
    - allowAnswerReview==false → ShowScoreOnly (state apa pun)
    - allowAnswerReview==true & isPassed==true → ShowFullReview (lulus, retake tak mungkin)
    - allowAnswerReview==true & isPassed != true (false ATAU null) & attemptsRemaining==true → ShowWrongFlagsOnly (retake mungkin → sembunyikan kunci)
    - allowAnswerReview==true & isPassed != true & attemptsRemaining==false (exhausted) → ShowFullReview (retake tak mungkin lagi)
    Test (6 Fact, mirror idiom RetakeRulesTests :37-92):
    - Tier_ScoreOnly_WhenReviewDisabled → Equal(ShowScoreOnly, ResolveReviewMode(false, false, true))
    - Tier_WrongFlagsOnly_WhenFailedWithAttemptsLeft → Equal(ShowWrongFlagsOnly, ResolveReviewMode(true, false, true))
    - Tier_WrongFlagsOnly_WhenPendingNullWithAttemptsLeft → Equal(ShowWrongFlagsOnly, ResolveReviewMode(true, null, true))  // A1 leak-safe: pending+sisa = sembunyikan kunci
    - Tier_FullReview_WhenFailedExhausted → Equal(ShowFullReview, ResolveReviewMode(true, false, false))
    - Tier_FullReview_WhenPendingNullExhausted → Equal(ShowFullReview, ResolveReviewMode(true, null, false))  // pending tapi tak ada sisa → aman full
    - Tier_FullReview_WhenPassed → Equal(ShowFullReview, ResolveReviewMode(true, true, true))
  </behavior>
  <action>
    Di `Helpers/RetakeRules.cs`, TAMBAHKAN (jangan ubah CanRetake/ShouldHideRetakeToggle), di dalam namespace `HcPortal.Helpers`, adjacent ke `RetakeRules`:

    ```csharp
    public enum RetakeReviewMode { ShowFullReview, ShowWrongFlagsOnly, ShowScoreOnly }
    ```

    Dan di dalam class `RetakeRules`, tambah method pure (XML-doc singkat menjelaskan leak-safe pending → ShowFullReview):
    ```csharp
    /// <summary>
    /// v32.4 RTK-11 (Phase 407) — tier feedback PURE 3-state, leak-safe.
    /// LOCKED orchestrator (A1): sembunyikan kunci selama retake MASIH MUNGKIN — yaitu belum-lulus
    /// (isPassed != true: failed ATAU pending null) DAN attemptsRemaining → ShowWrongFlagsOnly.
    /// Pending (null) diperlakukan SAMA dengan failed: bisa transisi ke failed+retake, jadi kunci
    /// yang tampil saat pending akan bocor untuk retake soal yang sama (D-03). Hanya saat lulus, atau
    /// belum-lulus tapi attempt habis, kunci boleh tampil (ShowFullReview).
    /// Caller pakai assessment.IsPassed (bool?) BUKAN VM.IsPassed (bool non-nullable) — Pitfall 5.
    /// </summary>
    public static RetakeReviewMode ResolveReviewMode(bool allowAnswerReview, bool? isPassed, bool attemptsRemaining)
    {
        if (!allowAnswerReview) return RetakeReviewMode.ShowScoreOnly;
        // belum-lulus (failed ATAU pending) & retake masih mungkin → tahan kunci
        if (isPassed != true && attemptsRemaining) return RetakeReviewMode.ShowWrongFlagsOnly;
        return RetakeReviewMode.ShowFullReview;   // passed | (belum-lulus & exhausted) — retake tak mungkin lagi
    }
    ```

    Di `HcPortal.Tests/RetakeRulesTests.cs`, tambah 5 `[Fact]` (lihat <behavior>) di dalam class — panggil `RetakeRules.ResolveReviewMode(...)` langsung + `RetakeReviewMode` (using HcPortal.Helpers sudah ada di file). JANGAN ubah test CanRetake/ShouldHideRetakeToggle existing.
  </action>
  <acceptance_criteria>
    - Grep: `Helpers/RetakeRules.cs` mengandung `public enum RetakeReviewMode { ShowFullReview, ShowWrongFlagsOnly, ShowScoreOnly }`
    - Grep: `Helpers/RetakeRules.cs` mengandung `public static RetakeReviewMode ResolveReviewMode(bool allowAnswerReview, bool? isPassed, bool attemptsRemaining)`
    - Grep: `Helpers/RetakeRules.cs` masih mengandung `public static bool CanRetake(` dan `public static bool ShouldHideRetakeToggle(` (tak terhapus)
    - Grep: `HcPortal.Tests/RetakeRulesTests.cs` mengandung ke-6 nama Fact: `Tier_ScoreOnly_WhenReviewDisabled`, `Tier_WrongFlagsOnly_WhenFailedWithAttemptsLeft`, `Tier_WrongFlagsOnly_WhenPendingNullWithAttemptsLeft`, `Tier_FullReview_WhenFailedExhausted`, `Tier_FullReview_WhenPendingNullExhausted`, `Tier_FullReview_WhenPassed`
    - Grep: `Helpers/RetakeRules.cs` ResolveReviewMode mengandung `isPassed != true && attemptsRemaining` (leak-safe pending==failed, BUKAN `isPassed == false`)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeRulesTests"</automated>
  </verify>
  <done>build 0 error; semua RetakeRulesTests hijau termasuk 6 cabang ResolveReviewMode (pending null & attempt-sisa → ShowWrongFlagsOnly leak-safe; pending & exhausted → ShowFullReview).</done>
</task>

<task type="auto">
  <name>Task 2: Perluas AssessmentResultsViewModel + AllWorkersHistoryRow (field retake/tier)</name>
  <files>Models/AssessmentResultsViewModel.cs, Models/AllWorkersHistoryRow.cs</files>
  <read_first>
    - Models/AssessmentResultsViewModel.cs (file dimodifikasi — class `AssessmentResultsViewModel`, JANGAN hapus field apa pun)
    - Models/AllWorkersHistoryRow.cs (file dimodifikasi — class, tambah satu field di akhir; lihat comment style :35-39)
    - Models/RiwayatAttemptViewModel.cs (tipe `List<RiwayatAttemptViewModel>` untuk field RiwayatAttempts — sudah ada, jangan ubah)
    - 407-PATTERNS.md §"Models/AssessmentResultsViewModel.cs" + §"Models/AllWorkersHistoryRow.cs" (excerpt field siap-salin)
  </read_first>
  <action>
    Di `Models/AssessmentResultsViewModel.cs`, di dalam class `AssessmentResultsViewModel`, TAMBAHKAN field (jangan hapus/ubah field existing — `AllowAnswerReview`, `IsPassed` bool, `IsPendingGrading` WAJIB tetap):
    ```csharp
    // v32.4 RTK-09/10/11/12 (Phase 407) — sisi-pekerja ujian ulang. Diisi CMPController.Results (407-02), dirender Results.cshtml (407-03).
    public HcPortal.Helpers.RetakeReviewMode RetakeMode { get; set; } = HcPortal.Helpers.RetakeReviewMode.ShowFullReview;
    public bool CanRetake { get; set; }
    public int CurrentAttempt { get; set; }
    public int MaxAttempts { get; set; }
    public DateTime? CooldownUntilUtc { get; set; }
    public bool IsCapReached { get; set; }
    public List<RiwayatAttemptViewModel>? RiwayatAttempts { get; set; }
    ```

    Di `Models/AllWorkersHistoryRow.cs`, di akhir class (mirror comment style nullable-prop existing), TAMBAHKAN:
    ```csharp
    // RTK-12 (Phase 407): tandai baris percobaan aktif saat ini untuk badge "Percobaan saat ini".
    // Mirror RiwayatAttemptViewModel.IsCurrent — set true hanya untuk current Completed branch.
    public bool IsCurrentAttempt { get; set; }
    ```
  </action>
  <acceptance_criteria>
    - Grep: `Models/AssessmentResultsViewModel.cs` mengandung `public HcPortal.Helpers.RetakeReviewMode RetakeMode`, `public bool CanRetake`, `public int CurrentAttempt`, `public int MaxAttempts`, `public DateTime? CooldownUntilUtc`, `public bool IsCapReached`, `public List<RiwayatAttemptViewModel>? RiwayatAttempts`
    - Grep: `Models/AssessmentResultsViewModel.cs` masih mengandung `public bool AllowAnswerReview` DAN `public bool IsPassed` DAN `public bool IsPendingGrading` (field existing tak terhapus)
    - Grep: `Models/AllWorkersHistoryRow.cs` mengandung `public bool IsCurrentAttempt`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>build 0 error; VM membawa 7 field baru + AllWorkersHistoryRow membawa IsCurrentAttempt; tak ada field existing terhapus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view ← VM | View Results.cshtml (407-03) mempercaya nilai tier/eligibility dari VM ini; nilai dihitung server (407-02). Plan ini hanya mendefinisikan kontrak — tak ada input untrusted yang masuk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-407-leak | Information Disclosure | `RetakeRules.ResolveReviewMode` (penentu apakah kunci jawaban dirender) | mitigate | Truth-table leak-safe: failed+attempt-sisa → ShowWrongFlagsOnly (kunci ditahan); pending(null) → ShowFullReview (bukan retake-eligible, leak-safe). Diuji unit semua 5 cabang termasuk pending null (Task 1). View (407-03) yang men-suppress leak-site bergantung pada enum ini. |
| T-407-drift | Tampering (logic drift) | tier dihitung di view vs helper | accept→mitigate | Tier diekstrak ke helper pure unit-tested (bukan inline if/else di view/controller) → tak ada drift dengan `CanRetake`. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeRulesTests"` hijau (CanRetake/ShouldHideRetakeToggle regresi + 5 cabang ResolveReviewMode).
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau (no regresi suite unit, baseline ~600).
</verification>

<success_criteria>
- `RetakeReviewMode` enum + `ResolveReviewMode` pure ada di `Helpers/RetakeRules.cs`, leak-safe (pending → ShowFullReview).
- 5 unit test cabang truth-table hijau.
- `AssessmentResultsViewModel` membawa RetakeMode/CanRetake/CurrentAttempt/MaxAttempts/CooldownUntilUtc/IsCapReached/RiwayatAttempts.
- `AllWorkersHistoryRow` membawa IsCurrentAttempt.
- 0 field existing terhapus; 0 migration.
</success_criteria>

<output>
After completion, create `.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-01-SUMMARY.md`
</output>
