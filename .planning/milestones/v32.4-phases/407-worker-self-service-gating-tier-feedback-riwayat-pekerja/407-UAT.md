---
phase: 407
slug: worker-self-service-gating-tier-feedback-riwayat-pekerja
type: playwright-uat
status: passed
date: 2026-06-22
runner: milestone-autopilot
base_url: http://localhost:5270
db: HcPortalDB_Dev (SQLEXPRESS)
result: 7/7 passed
---

# Phase 407 — Playwright UAT (live browser @5270)

Live-browser UAT independen (Lesson 354/413: real-browser WAJIB untuk Razor/JS — leak-safety + countdown tak bisa dijamin grep+build). Dijalankan langsung terhadap state ter-commit final.

## Setup
- App: `ASPNETCORE_ENVIRONMENT=Development Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5270` (branch ITHandoff). HTTP 200.
- DB: SQLEXPRESS `HcPortalDB_Dev`. SEED_WORKFLOW: spec self-manage — BACKUP → seed `retake-worker-407-seed.sql` (3 sesi `[RETAKE407]`: A=leak-safe+eligible, B=cap-reached, C=cooldown-active) → SEED_JOURNAL `active` → afterAll RESTORE → Layer-4 verifikasi 0 matrix rows post-RESTORE → journal `cleaned`.
- Auth: coachee (worker) + admin@pertamina.com.
- Run: `cd tests && E2E_BASE_URL=http://localhost:5270 npx playwright test e2e/retake-worker-407.spec.ts --workers=1`.

## Hasil — 7 passed (21.8s)

| # | Skenario | RTK | Hasil |
|---|----------|-----|-------|
| 1 | **leak-safety**: gagal+sisa-percobaan → `ShowWrongFlagsOnly` (notice "Kunci jawaban disembunyikan" + verdict ✓/✗ + "Jawaban Anda"); kunci A2 (soal dijawab salah, `KUNCIBENAR`) TIDAK bocor; jawaban worker sendiri A1 (`JAWABANKU`) tampil; no `(Jawaban Benar)`, 0 `list-group-item-success`; no pageerror | RTK-11 | ✅ |
| 2 | control eligible: tombol "Ujian Ulang" enabled + counter "Percobaan ke-X dari N" | RTK-10 | ✅ |
| 3 | modal: klik "Ujian Ulang" → `#retakeConfirmModal` muncul (form antiforgery) | RTK-09/D-02 | ✅ |
| 4 | riwayat: card "Riwayat Percobaan Saya" + accordion + badge "Percobaan saat ini" | RTK-12 | ✅ |
| 5 | cap reached: alert "Batas percobaan tercapai" TANPA tombol Ujian Ulang | RTK-10 | ✅ |
| 6 | **cooldown**: tombol disabled + `#retakeCountdown` ticking HH:MM:SS | RTK-10 | ✅ |

(+1 global.setup verifikasi app+seed = 7 total.)

## Catatan
- **Skenario 6 mengonfirmasi fix WR-01 LIVE** — sebelum fix, tombol cooldown-disabled + countdown tak pernah render (digate `Model.CanRetake` yang false selama cooldown). Setelah fix (`IsInCooldown` VM flag + cabang view terpisah), countdown ticking tampil benar.
- **Skenario 1 mengonfirmasi leak-safety LIVE** — kunci jawaban soal yang dijawab salah disembunyikan di DOM real-browser; jawaban worker sendiri tetap tampil (D-03). Temuan awal (assert keliru pada jawaban-benar worker) sudah diperbaiki (`985db030`).
- DB lokal restored bersih (Layer-4 0 rows).

## Verdict
**PASS 7/7.** Worker self-service retake (endpoint guard, tombol/counter/cooldown/lock, tier feedback leak-safe, riwayat pekerja) terbukti live @5270. Gate UAT 407 TUNTAS.
