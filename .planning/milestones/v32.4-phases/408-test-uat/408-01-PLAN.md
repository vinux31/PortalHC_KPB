---
phase: 408-test-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/RetakeThenPassCertTests.cs
autonomous: true
requirements: [RTK-14]
tags: [test, integration, retake, certificate, assessment]

must_haves:
  truths:
    - "Sebuah sesi gagal (IsPassed=false) yang di-retake lalu di-grade-lulus menerbitkan TEPAT 1 NomorSertifikat (anti-double-cert guard ter-bukti via SQL riil)."
    - "NomorSertifikat hasil retake-then-pass cocok format kanonik KPB/{seq}/{roman}/{year}."
    - "Counting attempt (UserId,Title,Category) tidak konflasi Pre/Post ber-Title sama TETAP benar pasca-grade (referensi test existing, tidak diduplikasi)."
    - "Test ber-Trait Integration sehingga ter-skip otomatis pada CI SQL-less (--filter Category!=Integration), berjalan @SQLEXPRESS lokal."
  artifacts:
    - path: "HcPortal.Tests/RetakeThenPassCertTests.cs"
      provides: "xUnit integration GAP-1: retake→grade-lulus→tepat 1 cert"
      contains: "RetakeThenPass_IssuesExactlyOneCertificate"
      min_lines: 60
  key_links:
    - from: "HcPortal.Tests/RetakeThenPassCertTests.cs"
      to: "Services/RetakeService.cs ExecuteAsync"
      via: "NewRetake(ctx).ExecuteAsync(...)"
      pattern: "ExecuteAsync\\("
    - from: "HcPortal.Tests/RetakeThenPassCertTests.cs"
      to: "Services/GradingService.cs GradeAndCompleteAsync"
      via: "NewGrading(ctx).GradeAndCompleteAsync(session)"
      pattern: "GradeAndCompleteAsync\\("
    - from: "HcPortal.Tests/RetakeThenPassCertTests.cs"
      to: "RetakeServiceFixture (existing, same assembly)"
      via: "IClassFixture<RetakeServiceFixture>"
      pattern: "IClassFixture<RetakeServiceFixture>"
---

<objective>
GAP-1 (CONTEXT D-02): Tambahkan SATU xUnit integration test real-SQL baru `RetakeThenPassCertTests.cs` yang membuktikan invariant capstone milestone v32.4: **sesi gagal → retake (reset) → ambil-ulang lulus (grade) → TEPAT 1 sertifikat terbit** (guard anti-double-cert existing tidak menerbitkan dobel).

Ini GENUINE GAP: `RetakeServiceTests` membuktikan archive/reset/counting, TAPI tidak ada satu pun test yang men-drive jalur lulus pasca-retake hingga sertifikat terbit. Penyebab: `RetakeService.ExecuteAsync` HANYA mereset sesi (Status→Open, hapus responses); sertifikat di-issue jauh kemudian oleh `GradingService.GradeAndCompleteAsync` step 6 (retry 3× WHERE NomorSertifikat==null = guard anti-dobel). Test ini menjahit kedua jalur.

Purpose: Menutup RTK-14 lapisan integration retake-then-pass-1-cert. Tanpa test ini, regresi pada guard anti-double-cert (GradingService.cs:287-312) atau pada unique index `IX_AssessmentSessions_NomorSertifikat` tak akan terdeteksi.

Output: `HcPortal.Tests/RetakeThenPassCertTests.cs` (1 file baru, ≥1 [Fact], 0 production code, 0 migration). REUSE total infrastruktur existing (fixture, NoOpHubContext, seed helpers, GradingService ctor recipe) — tidak ada penemuan-ulang.

**CONSTRAINT KRITIS (CAPSTONE — JANGAN tulis ulang test hijau):** JANGAN sentuh/edit/hapus `RetakeServiceTests.cs`, `RetakeRulesTests.cs`, `RetakeArchiveBuilderTests.cs`, `RiwayatUnifierTests.cs`, `RetakeExamEndpointTests.cs`, `SubmitResurrectionTests.cs`, atau test hijau lain. File ini PURELY ADDITIVE. Counting no-conflate SUDAH ADA (`RetakeServiceTests.Counting_PrePostSameTitle_NoConflate`) — REFERENSI, JANGAN duplikasi (Pitfall 4 / D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/408-test-uat/408-CONTEXT.md
@.planning/phases/408-test-uat/408-RESEARCH.md
@.planning/phases/408-test-uat/408-PATTERNS.md

<project_constraints>
- Branch ITHandoff. App port 5270 (bukan 5277). **migration=FALSE** seluruh fase 408 (test murni).
- Bahasa user-facing = Bahasa Indonesia; kode/komentar boleh Inggris/Indonesia konsisten file.
- `dotnet test` dijalankan dari root repo. Integration test = `[Trait("Category","Integration")]` real-SQL @ `localhost\SQLEXPRESS`; unit-quick filter `--filter "Category!=Integration"`.
- Project test TIDAK punya Moq/NSubstitute — pakai hand-stub existing (`NoOpHubContext`, `FakeNotificationService`, `FakeWorkerDataService`) + `NullLogger<T>.Instance`.
</project_constraints>

<interfaces>
<!-- Kontrak yang executor butuhkan — diekstrak verbatim dari kode/test repo (VERIFIED). Tidak perlu eksplorasi codebase. -->

REUSE existing di assembly test `HcPortal.Tests` (namespace sama — JANGAN deklarasi ulang):
- `RetakeServiceFixture : IAsyncLifetime` — disposable DB `HcPortalDB_Test_{guid}` @SQLEXPRESS, `MigrateAsync` full chain (incl AddRetakeColumnsAndArchive), drop-on-dispose, `XunitException` saat chain-break. Property: `.Options` (DbContextOptions<ApplicationDbContext>). Source: HcPortal.Tests/RetakeServiceTests.cs:34-67.
- `NoOpHubContext : IHubContext<AssessmentHub>` — Source: HcPortal.Tests/RetakeServiceTests.cs:70-100.
- `FakeNotificationService` — HcPortal.Tests/FakeNotificationService.cs:10.
- `FakeWorkerDataService` — HcPortal.Tests/FakeWorkerDataService.cs:11.

RetakeService ctor (Source: RetakeServiceTests.cs:110-111):
```csharp
new RetakeService(ctx, new AuditLogService(ctx), new NoOpHubContext(), NullLogger<RetakeService>.Instance);
```
ExecuteAsync signature (dipakai di test existing): `ExecuteAsync(int sessionId, string? initiatedById, string actorName, string actionType, string reason)`.

GradingService ctor — CORE GAP-1 UNBLOCK (Source verbatim SubmitResurrectionTests.cs:68-76):
```csharp
private static GradingService NewGrading(ApplicationDbContext ctx)
{
    var fakeNotif = new FakeNotificationService();
    var audit = new AuditLogService(ctx);
    var completion = new ProtonCompletionService(ctx, NullLogger<ProtonCompletionService>.Instance, fakeNotif, audit);
    var bypass = new ProtonBypassService(ctx, completion, fakeNotif, audit, NullLogger<ProtonBypassService>.Instance);
    var worker = new FakeWorkerDataService();
    return new GradingService(ctx, worker, NullLogger<GradingService>.Instance, completion, bypass);
}
```
GradeAndCompleteAsync signature: `Task<bool> GradeAndCompleteAsync(AssessmentSession session)` — grade DARI DB (bukan form POST); step 6 issue cert bila `session.GenerateCertificate && isPassed`. Source: Services/GradingService.cs:285-312.

Anti-double-cert guard yang DIBUKTIKAN (Source GradingService.cs:287-312 — JANGAN modify):
```csharp
if (session.GenerateCertificate && isPassed) {
    await _context.AssessmentSessions
        .Where(s => s.Id == session.Id && s.NomorSertifikat == null)   // filtered WHERE null = idempotent
        .ExecuteUpdateAsync(s => s.SetProperty(r => r.NomorSertifikat, CertNumberHelper.Build(nextSeq, certNow)));
}
// Format: KPB/{seq:D3}/{RomanMonth}/{year}  e.g. "KPB/005/VI/2026"  — Helpers/CertNumberHelper.cs:20-21
```

Seed helper existing yang HARUS DISALIN + diperluas (model `AssessmentSession.GenerateCertificate` default false → WAJIB di-set true untuk jalur cert; Pitfall 2). Base: `SeedSessionAsync` (RetakeServiceTests.cs:122-140), `SeedUserAsync` (RetakeServiceTests.cs:114-120), `SeedPackageWithResponsesAsync` (RetakeServiceTests.cs:143-174 — tiap MC soal punya 1 opsi IsCorrect=true yang dipilih response → re-grade 100% → isPassed; juga seed UserPackageAssignment.ShuffledQuestionIds yang dibaca GradeAndCompleteAsync). CONFIRMED via SubmitResurrectionTests.cs:91-122: `PassPercentage=70, GenerateCertificate=true`, `GradeAndCompleteAsync` set `NomorSertifikat`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat RetakeThenPassCertTests.cs — jahit ExecuteAsync + GradeAndCompleteAsync, assert tepat 1 cert</name>

  <read_first>
    - `HcPortal.Tests/RetakeServiceTests.cs` (ANALOG PRIMER) — baca utuh: fixture `RetakeServiceFixture` (:34-67), `NoOpHubContext` (:70-100), `NewService` (:110-111), seed helpers `SeedUserAsync` (:114-120) / `SeedSessionAsync` (:122-140) / `SeedPackageWithResponsesAsync` (:143-174), dan `Counting_PrePostSameTitle_NoConflate` (:336-358) untuk referensi (JANGAN duplikasi).
    - `HcPortal.Tests/SubmitResurrectionTests.cs:68-122` (ANALOG SEKUNDER — RESOLVES OQ-1) — recipe ctor `GradingService` (`NewGradingService` :68-76) + bukti `GenerateCertificate=true`+`PassPercentage=70`+`GradeAndCompleteAsync` set `NomorSertifikat` (:96-97, :141).
    - `Services/GradingService.cs:49-52, 240-312` — grade-loop (baca field yang dibaca: ScoreValue, IsCorrect option, status guard != "Completed" → harus set Status="Completed" sebelum grade ATAU pahami bahwa GradeAndCompleteAsync mengubah Status sendiri) + cert step 6 (:285-312) + format `CertNumberHelper.Build`.
    - `Helpers/CertNumberHelper.cs:20-21` — format `KPB/{seq:D3}/{RomanMonth}/{year}`.
    - `Services/RetakeService.cs` — `ExecuteAsync` (konfirmasi: reset-only, hapus PackageUserResponses + UserPackageAssignment + arsip snapshot; TIDAK issue cert) + `CanRetakeAsync` (untuk optional assert counting).
  </read_first>

  <files>HcPortal.Tests/RetakeThenPassCertTests.cs</files>

  <behavior>
    - Test `RetakeThenPass_IssuesExactlyOneCertificate`:
      - SETUP: seed user → seed sesi GAGAL (`status:"Completed", isPassed:false, allowRetake:true, maxAttempts:2, cooldownHours:0, completedAt: 2 hari lalu, assessmentType:"PostTest", generateCertificate:true, passPercentage default model`) → seed paket + N(3) soal MC dengan respons benar.
      - LANGKAH 1 (retake = reset-only): `ExecuteAsync(sid, userId, "Tester", "RetakeAssessment", "worker_retake")` — JANGAN assert cert di sini (Pitfall 1: ExecuteAsync tak pernah issue cert; assert cert==null tepat setelah ini akan SELALU lulus secara menyesatkan, bukan invariant yang dimaksud).
      - LANGKAH 2 (simulasi ambil-ulang lulus): re-seed paket+assignment+respons-benar pasca-reset (ExecuteAsync menghapus responses/assignment) → muat ulang `session` dari DB → `GradeAndCompleteAsync(session)` (grade dari DB; step 6 issue cert).
      - LANGKAH 3 (CORE ASSERT): pada DbContext BARU (`verify`), `CountAsync(a => a.Id == sid && a.NomorSertifikat != null) == 1` (anti-double-cert guard ter-bukti) + `Assert.Matches(@"^KPB/\d{3}/[IVX]+/\d{4}$", cert)`.
      - OPSIONAL (D-02 nilai-tambah, BUKAN duplikasi): satu assert bahwa `CanRetakeAsync` / counting `(UserId,Title,Category)` tetap konsisten pasca-grade (mis. sesi yang kini lulus tidak lagi eligible retake). Tambahkan HANYA jika menambah nilai; jika ragu, cukup komentar yang merujuk `RetakeServiceTests.Counting_PrePostSameTitle_NoConflate` sebagai cakupan counting.
    - `[Trait("Category","Integration")]` pada class → SQL-less CI skip.
  </behavior>

  <action>
    Buat file `HcPortal.Tests/RetakeThenPassCertTests.cs` (PURELY ADDITIVE — jangan sentuh file lain).

    1. **Imports** (gabung RetakeServiceTests.cs:17-31 + `System.Text.Json` per SubmitResurrectionTests.cs:13):
       ```csharp
       using System;
       using System.Collections.Generic;
       using System.Linq;
       using System.Text.Json;
       using System.Threading.Tasks;
       using HcPortal.Data;
       using HcPortal.Models;
       using HcPortal.Services;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.Extensions.Logging.Abstractions;
       using Xunit;
       namespace HcPortal.Tests;
       ```

    2. **Class + REUSE fixture** (JANGAN deklarasi fixture/NoOpHubContext baru — sudah ada di assembly):
       ```csharp
       [Trait("Category", "Integration")]
       public class RetakeThenPassCertTests : IClassFixture<RetakeServiceFixture>
       {
           private readonly RetakeServiceFixture _fixture;
           public RetakeThenPassCertTests(RetakeServiceFixture f) => _fixture = f;
           private ApplicationDbContext NewCtx() => new ApplicationDbContext(_fixture.Options);

           private static RetakeService NewRetake(ApplicationDbContext ctx) =>
               new RetakeService(ctx, new AuditLogService(ctx), new NoOpHubContext(), NullLogger<RetakeService>.Instance);
       ```

    3. **NewGrading helper** — SALIN VERBATIM dari SubmitResurrectionTests.cs:68-76 (ctor recipe GradingService; semua stub sudah ada di HcPortal.Tests/).

    4. **Seed helpers** — SALIN `SeedUserAsync` + `SeedSessionAsync` + `SeedPackageWithResponsesAsync` dari RetakeServiceTests.cs (114-174) ke file ini sebagai private static (file test self-contained; helper RetakeServiceTests private = tak terjangkau lintas-class). **PERLUAS `SeedSessionAsync`** tambah dua param: `bool generateCertificate = false` (→ set `s.GenerateCertificate = generateCertificate`) dan **set `s.PassPercentage`** ke nilai rendah agar all-correct = lulus (ikuti SubmitResurrectionTests: `PassPercentage = 70`; respons all-correct → Score 100 ≥ 70 → isPassed=true). Pastikan `SeedPackageWithResponsesAsync` mengeset `ShuffledQuestionIds` (sudah, :165-171) karena `GradeAndCompleteAsync` membacanya via `GetShuffledQuestionIds()`.

    5. **Test method** sesuai `<behavior>`. Pola stitch (dari PATTERNS.md §Core stitch):
       ```csharp
       [Fact]
       public async Task RetakeThenPass_IssuesExactlyOneCertificate()
       {
           await using var ctx = NewCtx();
           var userId = await SeedUserAsync(ctx);
           var sid = await SeedSessionAsync(ctx, userId, "CertTitle", "Post",
               status:"Completed", isPassed:false, allowRetake:true, maxAttempts:2,
               cooldownHours:0, completedAt:DateTime.UtcNow.AddDays(-2),
               assessmentType:"PostTest", generateCertificate:true);
           await SeedPackageWithResponsesAsync(ctx, sid, 3);

           await NewRetake(ctx).ExecuteAsync(sid, userId, "Tester", "RetakeAssessment", "worker_retake");

           // simulasi ambil-ulang lulus: re-seed paket+assignment+respons benar pasca-reset, lalu grade.
           await SeedPackageWithResponsesAsync(ctx, sid, 3);
           var session = await ctx.AssessmentSessions.FirstAsync(a => a.Id == sid);
           await NewGrading(ctx).GradeAndCompleteAsync(session);

           await using var verify = NewCtx();
           int certCount = await verify.AssessmentSessions.CountAsync(a => a.Id == sid && a.NomorSertifikat != null);
           Assert.Equal(1, certCount);
           var cert = await verify.AssessmentSessions.Where(a => a.Id == sid).Select(a => a.NomorSertifikat).SingleAsync();
           Assert.Matches(@"^KPB/\d{3}/[IVX]+/\d{4}$", cert);
       }
       ```

    **VERIFIKASI ASUMSI saat eksekusi (RESEARCH A1/A2 — JANGAN abaikan):**
    - A1: `GradeAndCompleteAsync` bisa dipanggil dengan stub ringan — DIKONFIRMASI oleh SubmitResurrectionTests (sudah jalan hijau). Jika ctor butuh deps lain (compile error), baca ctor `GradingService` aktual + ikuti pola SubmitResurrectionTests; JANGAN tambah Moq.
    - A2: Re-seed responses+assignment pasca-ExecuteAsync cukup untuk isPassed=true. Jika `GradeAndCompleteAsync` mengembalikan false / cert null padahal lulus: cek (a) `session.Status` — GradeAndCompleteAsync menolak status terminal non-Completed (lihat SubmitResurrectionTests guard); ExecuteAsync mengubah Status→Open, jadi mungkin perlu set kembali kondisi yang membuat grade memproses (baca grade-loop GradingService.cs untuk status yang diterima — set `session.Status` sesuai jika perlu, tanpa mengubah produksi). (b) `GenerateCertificate` benar-benar true di DB. (c) respons benar-benar memilih opsi IsCorrect=true. Sesuaikan re-seed agar invariant CORE (1 cert) tercapai — INI yang load-bearing, bukan langkah perantara.

    **Aturan deviasi:** Jika perlu menyimpang dari skeleton (mis. set `session.Status` sebelum grade, atau menyesuaikan PassPercentage), lakukan DALAM file test ini saja + catat di SUMMARY dengan alasan. JANGAN edit produksi. JANGAN edit test hijau lain. Batas deviasi: 3 (lebih dari itu → STOP + lapor).
  </action>

  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build HcPortal.Tests 2>&1 | tail -5</automated>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeThenPassCert" 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - File `HcPortal.Tests/RetakeThenPassCertTests.cs` ada; mengandung class `RetakeThenPassCertTests` + `[Fact] RetakeThenPass_IssuesExactlyOneCertificate` + `[Trait("Category","Integration")]`.
    - `grep -c "ExecuteAsync" HcPortal.Tests/RetakeThenPassCertTests.cs` ≥ 1 DAN `grep -c "GradeAndCompleteAsync" HcPortal.Tests/RetakeThenPassCertTests.cs` ≥ 1 (kedua jalur dijahit).
    - `grep -c "Assert.Equal(1, certCount)" ...` ≥ 1 ATAU assert ekuivalen `CountAsync(... NomorSertifikat != null) == 1`.
    - `dotnet build HcPortal.Tests` exit 0.
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeThenPassCert"` → Passed (1 test, 0 failed) @SQLEXPRESS. (Jika SQLEXPRESS tak tersedia di runner: test ter-skip oleh Trait saat `--filter "Category!=Integration"`; namun gate fase WAJIB dijalankan dengan SQLEXPRESS hidup — lihat plan 408-02 Task regresi.)
    - ZERO edit pada `RetakeServiceTests.cs`/`RetakeRulesTests.cs`/`RetakeArchiveBuilderTests.cs`/`RiwayatUnifierTests.cs`/`RetakeExamEndpointTests.cs`/`SubmitResurrectionTests.cs` (git diff hanya menampilkan file baru `RetakeThenPassCertTests.cs`).
  </acceptance_criteria>

  <done>
    `RetakeThenPassCertTests.RetakeThenPass_IssuesExactlyOneCertificate` GREEN @SQLEXPRESS: retake (ExecuteAsync reset) → re-seed + grade (GradeAndCompleteAsync) → TEPAT 1 NomorSertifikat ber-format `KPB/\d{3}/[IVX]+/\d{4}`. Build 0 error. 0 file existing tersentuh. 0 migration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (konsolidasi milestone v32.4 retake — 406 admin/HC + 407 worker + invariant cert)

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| browser (worker, untrusted) → `CMP/RetakeExam` (POST) | Worker memicu mutasi destruktif (arsip + reset sesi) atas `id` route int | `id` (int), antiforgery token |
| view (Razor) ← VM (server) | `Results.cshtml` mempercaya `RetakeMode`/`CanRetake` dari server (`CMPController.Results`); keputusan leak-safety = server | `RetakeReviewMode` enum + eligibility flags |
| browser (Admin/HC) → `RiwayatPercobaan` GET / `UpdateRetakeSettings` POST | Admin/HC baca riwayat worker (sessionId int) / set config (maxAttempts, cooldown) | jawaban worker; nilai config numerik |
| client countdown JS → server | Enable/disable tombol = UX saja; server re-cek `CanRetakeAsync` saat POST | tidak ada data melintas |
| grade → DB (cert) | `GradingService.GradeAndCompleteAsync` menerbitkan `NomorSertifikat` di bawah unique index | sequence cert (bukan kripto) |

## STRIDE Threat Register (konsolidasi 406 [13/13 closed] + 407 [9/9 closed] + invariant cert-uniqueness)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-407-idor | Elevation / Access Control | `CMPController.RetakeExam` ownership | mitigate | `if (assessment.UserId != user.Id) return Forbid();` sebelum CanRetakeAsync+ExecuteAsync. Diuji `RetakeExam_NonOwner_ReturnsForbid`. (closed 407) |
| T-407-csrf | Tampering / CSRF | RetakeExam POST + modal form | mitigate | `[ValidateAntiForgeryToken]` + `@Html.AntiForgeryToken()`; Playwright assert token count 1. (closed 407) |
| T-407-bypass | Tampering / Logic Bypass | cooldown/cap bypass via DevTools | mitigate | Server re-cek `CanRetakeAsync` SEBELUM ExecuteAsync; countdown JS non-authoritative. Diuji `RetakeExam_NotEligible_RedirectsToResultsWithError`. (closed 407) |
| T-407-leak | Information Disclosure | `Results.cshtml` ShowWrongFlagsOnly + `_RiwayatPekerja.cshtml` | mitigate | ShowWrongFlagsOnly TIDAK render `list-group-item-success`/`(Jawaban Benar)`/`CorrectAnswer`; arsip verdict-only. e2e leak-safety 407 + lifecycle 408 (assert `not.toContain` kunci). (closed 407) |
| T-407-token | Tampering / Auth Bypass | stale TempData token pasca-retake | mitigate | `TempData.Remove($"TokenVerified_{id}")` setelah ExecuteAsync; StartExam pakai Peek. Diuji `RetakeExam_Success_ClearsTokenAndRedirectsToStartExam`. (closed 407) |
| T-407-xss | Tampering / XSS | user-content riwayat/review | mitigate | Razor `@` auto-encode; ZERO Html.Raw di partial retake; countdown via `.textContent`. (closed 407) |
| T-407-jsabort | Availability | countdown ReferenceError abort handler (lesson 413) | mitigate | guard `if(!btn)return`/`if(!iso)return`/`isNaN`; Playwright `pageerror` assert empty (lifecycle 408 menegaskan ulang di tiap langkah). (closed 407) |
| T-406-01 | Information Disclosure | RiwayatPercobaan GET (riwayat worker mana pun) | mitigate | `[Authorize(Roles="Admin, HC")]` pada action; RBAC re-cek tiap fetch. (closed 406) |
| T-406-02 | Information Disclosure | per-soal cell membocorkan KUNCI | mitigate | Partial render hanya AnswerText + verdict glyph + AwardedScore; tak ada field kunci di arsip. (closed 406) |
| T-406-06 | Tampering / CSRF | UpdateRetakeSettings POST | mitigate | `@Html.AntiForgeryToken()` + `[ValidateAntiForgeryToken]`. (closed 406) |
| T-406-07 | Tampering | bypass min/max → MaxAttempts/cooldown out-of-range | mitigate | Server `Math.Clamp(maxAttempts,1,5)` + `Math.Clamp(cooldown,0,168)`. (closed 406) |
| T-406-09 | Information Disclosure | card retake muncul di Pre-Test/Manual (invalid) | mitigate | `@if(ViewBag.HideRetakeToggle != true)` (RetakeRules.ShouldHideRetakeToggle). (closed 406) |
| T-408-cert | Tampering / Logic (double-cert) | retake-then-pass menerbitkan sertifikat dobel | mitigate | unique index `IX_AssessmentSessions_NomorSertifikat` + retry 3× `WHERE NomorSertifikat==null` (GradingService.cs:287-312). **DIBUKTIKAN GAP-1 (test ini): `Assert.Equal(1, certCount)`.** |

## Accepted Risks (dibawa dari 407 — JANGAN refactor di 408, test-only phase)
| Risk ID | Threat Ref | Rationale |
|---------|------------|-----------|
| AR-407-01 | T-407-doublearchive | double-submit double-archive dikelola `RetakeService.ExecuteAsync` claim-atomik (Open→no-op; ExecuteUpdate WHERE Status NOT IN(Cancelled,Open)→rows==0 rollback). Ter-cover `RetakeServiceTests`. |
| AR-407-02 | T-407-drift | `eraRetakeArchives` counting diduplikasi 3 tempat (Results + CanRetakeAsync + ExecuteAsync); identik + ter-cover test (RetakeServiceTests + RetakeExamEndpointTests + e2e). Single-source = backlog IN-03. JANGAN refactor di 408. |

**Disposisi 408:** Fase 408 tidak menambah surface kode produksi (test murni). Gerbang formal = `gsd-secure-phase 408` (D-03, dijalankan TERPISAH pasca-eksekusi) memverifikasi seluruh threat 406+407 tetap closed/accepted tanpa regresi + invariant T-408-cert kini di-cover GAP-1.
</threat_model>

<verification>
- `dotnet build HcPortal.Tests` exit 0.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeThenPassCert"` → 1 Passed @SQLEXPRESS.
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` → suite unit tetap hijau (no regresi; baseline 448/0/2 — GAP-1 ter-skip karena Integration).
- `git status` menampilkan TEPAT 1 file baru (`HcPortal.Tests/RetakeThenPassCertTests.cs`); 0 file existing dimodifikasi.
</verification>

<success_criteria>
- GAP-1 ter-tutup: invariant retake-then-pass → tepat 1 cert ter-bukti via SQL riil (RTK-14).
- Anti-double-cert guard (GradingService.cs:287-312) + unique index ter-exercise (bukan jalur palsu).
- Cert format `KPB/\d{3}/[IVX]+/\d{4}` ter-assert.
- 0 test hijau existing tersentuh; 0 production code; 0 migration.
- `<threat_model>` konsolidasi tersedia untuk auditor `gsd-secure-phase 408`.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/408-test-uat/408-01-SUMMARY.md` (template summary). Catat: nama test + hasil run @SQLEXPRESS, deviasi (jika ada) dari skeleton + alasan, konfirmasi 0 file existing tersentuh, migration=FALSE.
</output>
