---
phase: 409-data-foundation-re-entry-guards-exclude-removed-query
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentSession.cs
  - Data/ApplicationDbContext.cs
  - Migrations/{TIMESTAMP}_AddParticipantRemovalColumns.cs
  - Migrations/{TIMESTAMP}_AddParticipantRemovalColumns.Designer.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
autonomous: false
requirements: [PRMV-03]
user_setup: []

must_haves:
  truths:
    - "Kolom RemovedAt/RemovedBy/RemovalReason hadir di tabel AssessmentSessions (verified sqlcmd)"
    - "Migration AddParticipantRemovalColumns apply ke HcPortalDB_Dev lokal tanpa error"
    - "Semua baris existing AssessmentSessions punya 3 kolom baru = NULL (additif, non-destruktif)"
    - "Invarian tunggal terdefinisi: soft-removed ⇔ RemovedAt != null; aktif ⇔ RemovedAt == null"
  artifacts:
    - path: "Models/AssessmentSession.cs"
      provides: "3 properti soft-remove (RemovedAt DateTime?, RemovedBy string?, RemovalReason string?)"
      contains: "public DateTime? RemovedAt"
    - path: "Data/ApplicationDbContext.cs"
      provides: "Fluent HasMaxLength(500) untuk RemovalReason"
      contains: "RemovalReason"
    - path: "Migrations/{TIMESTAMP}_AddParticipantRemovalColumns.cs"
      provides: "3 AddColumn additif nullable + symmetric DropColumn"
      contains: "RemovedAt"
  key_links:
    - from: "Migrations/{TIMESTAMP}_AddParticipantRemovalColumns.cs"
      to: "HcPortalDB_Dev.AssessmentSessions"
      via: "dotnet ef database update"
      pattern: "AddColumn.*RemovedAt"
---

<objective>
Letakkan fondasi skema soft-remove untuk milestone v32.5: tambah 3 kolom nullable additif (`RemovedAt`/`RemovedBy`/`RemovalReason`) ke `AssessmentSession`, scaffold migration `AddParticipantRemovalColumns` via `dotnet ef` (BUKAN hand-roll), apply ke DB lokal, dan verifikasi kolom hadir via sqlcmd. Tetapkan invarian tunggal: **soft-removed ⇔ `RemovedAt != null`**.

Purpose: Skema ini dikonsumsi guard re-entry + exclude-query (Plan 02), serta Phase 410 (idempotency cek sesi aktif `RemovedAt==null`), Phase 411 (tulis/clear kolom), Phase 412 (panel "Peserta Dikeluarkan" baca removed). Migration WAJIB landing di DB live SEBELUM integration test Plan 02 dijalankan agar tak false-positive.
Output: `AssessmentSession` model + Fluent config + migration `.cs`/`.Designer.cs` + updated snapshot, kolom ter-apply di `HcPortalDB_Dev`.

**migration=TRUE** → notify IT (commit hash + flag migration) saat ter-merge (ditangani di ship Phase 409; catat di SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-CONTEXT.md
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-RESEARCH.md
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Template eksak yang dicermin (code-verified 2026-06-21). Executor pakai langsung, no exploration. -->

From Models/AssessmentSession.cs:45-46,95 (nullable audit-field analogs):
```csharp
public DateTime? CompletedAt { get; set; }       // :45 — template RemovedAt (UTC, DateTime.UtcNow)
public DateTime? StartedAt { get; set; }          // :46
public string? CreatedBy { get; set; }            // :95 — template EKSAK RemovedBy (string? userId, nvarchar(max))
```

From Data/ApplicationDbContext.cs:222 (Fluent HasMaxLength template, di dalam block Entity<AssessmentSession> :188-247):
```csharp
entity.Property(a => a.TahunKe).HasMaxLength(20).IsRequired(false);   // template RemovalReason
```

From Migrations/20260613095102_AddShuffleTogglesToAssessmentSession.cs (additive AddColumn/DropColumn shape — KONTRAS: 372 pakai nullable:false+defaultValue; 409 WAJIB nullable:true tanpa defaultValue):
```csharp
migrationBuilder.AddColumn<bool>(name: "ShuffleOptions", table: "AssessmentSessions", type: "bit", nullable: false, defaultValue: true);
migrationBuilder.DropColumn(name: "ShuffleOptions", table: "AssessmentSessions");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah 3 properti soft-remove ke model + Fluent config</name>
  <read_first>
    - Models/AssessmentSession.cs (region audit-field :44-46 + :92-95 — lihat `CompletedAt`/`StartedAt`/`CreatedBy` yang dicermin)
    - Data/ApplicationDbContext.cs :188-247 (block `Entity<AssessmentSession>`; baris :222 `TahunKe.HasMaxLength(20)` = template; JANGAN sentuh filtered index :226-229)
    - 409-PATTERNS.md §"Models/AssessmentSession.cs" + §"Data/ApplicationDbContext.cs" (excerpt copy-paste-ready)
  </read_first>
  <files>Models/AssessmentSession.cs, Data/ApplicationDbContext.cs</files>
  <action>
    **A. Models/AssessmentSession.cs** — tambah 3 properti di region audit-field (dekat `CreatedBy` :95), VERBATIM:
    ```csharp
    // ===== v32.5 Phase 409: Soft-remove participant fields =====
    /// <summary>UTC. null = aktif; non-null = soft-removed (SUMBER KEBENARAN "removed"). Ditulis di Phase 411.</summary>
    public DateTime? RemovedAt { get; set; }
    /// <summary>userId Admin/HC pelaku (cermin CreatedBy :95).</summary>
    public string? RemovedBy { get; set; }
    /// <summary>Alasan opsional dari modal (max 500, Fluent HasMaxLength).</summary>
    public string? RemovalReason { get; set; }
    ```
    - `RemovedAt` = `DateTime?` UTC (cermin `CompletedAt`/`StartedAt`). JANGAN pakai `AddHours(7)` (Pitfall 7 — penulisan nilai = Phase 411; di sini hanya deklarasi properti).
    - `RemovedBy` = plain `string?` (cermin `CreatedBy`), TANPA `[MaxLength]` data-annotation (konvensi repo: length di Fluent).
    - JANGAN tambah `[MaxLength]` annotation di model (D-03 / PATTERNS Notes).

    **B. Data/ApplicationDbContext.cs** — tambah 1 baris Fluent di dalam block `builder.Entity<AssessmentSession>(entity => { ... })` (dekat :222-223), VERBATIM:
    ```csharp
    // v32.5 Phase 409: RemovalReason nvarchar(500) (D-03). RemovedBy/RemovedAt = no extra config (mirror CreatedBy/CompletedAt).
    entity.Property(a => a.RemovalReason).HasMaxLength(500).IsRequired(false);
    ```
    - Fluent dipilih (bukan Data Annotation) — konvensi dominan block ini (Discretion resolved).
    - `RemovedBy`/`RemovedAt` TIDAK perlu config tambahan → scaffolds `nvarchar(max)` / `datetime2` nullable (acceptable, D-03).
    - JANGAN sentuh filtered unique index `IX_AssessmentSessions_NomorSertifikat_Unique` :226-229 (Pitfall 4 — AddColumn tak mempengaruhinya).
  </action>
  <verify>
    <automated>grep -q "public DateTime? RemovedAt" Models/AssessmentSession.cs && grep -q "public string? RemovedBy" Models/AssessmentSession.cs && grep -q "public string? RemovalReason" Models/AssessmentSession.cs && grep -q "RemovalReason).HasMaxLength(500)" Data/ApplicationDbContext.cs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `Models/AssessmentSession.cs` contains `public DateTime? RemovedAt`, `public string? RemovedBy`, `public string? RemovalReason`
    - `Data/ApplicationDbContext.cs` contains `entity.Property(a => a.RemovalReason).HasMaxLength(500).IsRequired(false)`
    - Model has NO `[MaxLength]` data-annotation on the 3 new props (grep `\[MaxLength` near new props returns nothing)
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <done>3 properti soft-remove ada di model (nullable, no annotation); Fluent HasMaxLength(500) untuk RemovalReason ada di DbContext; build hijau.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2 [BLOCKING]: Scaffold migration AddParticipantRemovalColumns + apply DB lokal + verify sqlcmd</name>
  <read_first>
    - 409-RESEARCH.md §"Code Examples" (migration add command) + §"Common Pitfalls" (Pitfall 5 tool-version, Pitfall 6 provider/connstring)
    - 409-PATTERNS.md §"Migrations/{TIMESTAMP}_AddParticipantRemovalColumns.cs" (expected Up/Down shape — nullable:true)
    - Migrations/20260613095102_AddShuffleTogglesToAssessmentSession.cs (additive AddColumn analog)
    - CLAUDE.md §"Develop Workflow" (migration=TRUE → dotnet ef + apply DB lokal + verify + notify IT)
  </read_first>
  <files>Migrations/{TIMESTAMP}_AddParticipantRemovalColumns.cs, Migrations/{TIMESTAMP}_AddParticipantRemovalColumns.Designer.cs, Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <action>
    **Scaffold via `dotnet ef` — JANGAN hand-roll** (manual `ALTER TABLE` memutus migration-chain → break `MigrateAsync` di fixture test Plan 02). Nama migration **LOCKED**: `AddParticipantRemovalColumns`.

    Jalankan (set `ASPNETCORE_ENVIRONMENT=Development` WAJIB — tanpa itu connstring SQLite bocor ke `UseSqlServer`, Pitfall 6):
    ```bash
    export ASPNETCORE_ENVIRONMENT=Development
    dotnet ef migrations add AddParticipantRemovalColumns
    dotnet ef database update
    ```
    Bila `dotnet ef` (tool global 10.0.3, mismatch vs project 8.0.0, Pitfall 5) memunculkan warning/snapshot `ProductVersion 10.x`: pin tool `dotnet tool update dotnet-ef --version 8.0.0 --global` lalu ulang, atau jalankan via `--project HcPortal.csproj`.

    **Review scaffolder output** (Pitfall 5): buka `Migrations/{TS}_AddParticipantRemovalColumns.cs` — pastikan `Up()` berisi 3 `AddColumn` dengan `nullable: true` dan TANPA `defaultValue` (kontras 372 yang pakai `nullable:false+defaultValue`); `RemovalReason` ber-`type: "nvarchar(500)", maxLength: 500`; `Down()` berisi 3 `DropColumn`. Pastikan `ApplicationDbContextModelSnapshot.cs` + `.Designer.cs` ber-`ProductVersion` `8.0.x` (BUKAN 10.x).

    **Verify kolom hadir di DB lokal** (CLAUDE.md sqlcmd `-C -I`):
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -C -I -E -Q "SELECT COLUMN_NAME, DATA_TYPE, IS_NULLABLE, CHARACTER_MAXIMUM_LENGTH FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='AssessmentSessions' AND COLUMN_NAME IN ('RemovedAt','RemovedBy','RemovalReason');"
    ```
    Expected: `RemovedAt datetime2 YES NULL` | `RemovedBy nvarchar YES -1(max)` | `RemovalReason nvarchar YES 500`.

    **Verify data lama tak berubah** (additif non-destruktif — D-03a): pastikan baris existing dapat NULL (kolom baru nullable → otomatis NULL, tak ada backfill).
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -C -I -E -Q "SELECT COUNT(*) AS NonNullRemoved FROM AssessmentSessions WHERE RemovedAt IS NOT NULL;"
    ```
    Expected: `0` (semua baris lama `RemovedAt` NULL).

    Commit migration `.cs` + `.Designer.cs` + snapshot bersama (jangan pisah — chain harus konsisten).

    Catat di SUMMARY: **migration=TRUE → notify IT** dengan commit hash + flag `AddParticipantRemovalColumns` saat ter-merge.
  </action>
  <verify>
    <automated>ls Migrations/*AddParticipantRemovalColumns.cs >/dev/null 2>&1 && grep -q 'AddColumn<DateTime>(' Migrations/*AddParticipantRemovalColumns.cs && grep -q 'name: "RemovedAt"' Migrations/*AddParticipantRemovalColumns.cs && grep -q 'name: "RemovalReason"' Migrations/*AddParticipantRemovalColumns.cs && grep -q 'nullable: true' Migrations/*AddParticipantRemovalColumns.cs && ! grep -q 'defaultValue' Migrations/*AddParticipantRemovalColumns.cs && grep -q 'ProductVersion", "8.0' Migrations/ApplicationDbContextModelSnapshot.cs && dotnet build HcPortal.csproj 2>&1 | tail -3</automated>
    <manual>sqlcmd verify: 3 kolom hadir di AssessmentSessions (RemovedAt datetime2 nullable, RemovedBy nvarchar(max) nullable, RemovalReason nvarchar(500) nullable); COUNT(RemovedAt IS NOT NULL) = 0</manual>
  </verify>
  <acceptance_criteria>
    - File `Migrations/{TS}_AddParticipantRemovalColumns.cs` exists with 3 `AddColumn` (`RemovedAt` datetime2, `RemovedBy` nvarchar(max), `RemovalReason` nvarchar(500) maxLength:500), ALL `nullable: true`, NO `defaultValue`
    - `Migrations/*AddParticipantRemovalColumns.cs` `Down()` has 3 `DropColumn`
    - `Migrations/ApplicationDbContextModelSnapshot.cs` `ProductVersion` is `8.0.x` (NOT `10.x`)
    - `dotnet ef database update` (ASPNETCORE_ENVIRONMENT=Development) succeeds
    - sqlcmd shows 3 columns present in `AssessmentSessions`, all `IS_NULLABLE=YES`, `RemovalReason CHARACTER_MAXIMUM_LENGTH=500`
    - sqlcmd `SELECT COUNT(*) WHERE RemovedAt IS NOT NULL` returns 0 (existing rows unchanged)
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <done>Migration scaffolded (not hand-rolled), 3 kolom nullable additif applied ke HcPortalDB_Dev, sqlcmd confirm kolom hadir + data lama NULL, snapshot ProductVersion 8.0.x, build hijau. BLOCKING gate untuk Plan 02 integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→local DB | Migration DDL apply ke `HcPortalDB_Dev`; salah env (Pitfall 6) bisa connect server salah |
| EF tool→snapshot | Tool global 10.0.3 vs project 8.0.0 (Pitfall 5) → annotation mismatch bisa break runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-409-04 | Tampering | Migration chain (hand-rolled ALTER) | mitigate | Scaffold via `dotnet ef migrations add` only; verify `MigrateAsync` chain via Plan 02 integration fixture; manual ALTER FORBIDDEN |
| T-409-05 | Denial of Service | Migration snapshot version mismatch (tool 10.x stamps EF10 annotations) | mitigate | Review snapshot `ProductVersion` = 8.0.x post-scaffold; pin tool 8.0.0 if drift; `dotnet build` gate |
| T-409-06 | Tampering | Provider/connstring leak (SQLite→SqlServer, Pitfall 6) | mitigate | `ASPNETCORE_ENVIRONMENT=Development` mandatory for all `dotnet ef`/`dotnet run`; sqlcmd verify post-apply |
| T-409-07 | Information disclosure | Destructive default backfills existing rows | accept | 3 cols nullable additive, no defaultValue → existing rows NULL automatically; sqlcmd COUNT(RemovedAt!=null)=0 confirms |

**No high-severity threats in this plan** (schema-only foundation; no new endpoint/input). Re-entry guards (the security-critical mitigations for removed-worker resubmit) are in Plan 02.
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- `dotnet ef database update` (ASPNETCORE_ENVIRONMENT=Development) sukses tanpa error.
- sqlcmd: 3 kolom hadir di `AssessmentSessions`, semua nullable, `RemovalReason` len=500, baris existing `RemovedAt` NULL.
- Snapshot `ProductVersion` = 8.0.x.
- `dotnet test HcPortal.Tests --filter "Category=Integration"` hijau (fixture `MigrateAsync` validasi chain otomatis — bila ada fixture existing yang jalan, chain ter-validasi).
</verification>

<success_criteria>
- ROADMAP SC1: Migration `AddParticipantRemovalColumns` apply tanpa error; 3 kolom hadir; data existing tak berubah (NULL). ✓
- Invarian `RemovedAt != null ⇔ soft-removed` ter-deklarasi di model (XML doc) + Fluent config landing.
- Skema siap dikonsumsi guard + exclude (Plan 02), Phase 410/411/412.
</success_criteria>

<output>
After completion, create `.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-01-SUMMARY.md`.
Note in SUMMARY: **migration=TRUE** → notify IT dengan commit hash + flag `AddParticipantRemovalColumns` saat ter-merge ke main.
</output>
