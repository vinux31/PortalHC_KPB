---
phase: 410-add-participant-backend-live
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - HcPortal.Tests/FlexibleParticipantAddLiveTests.cs
autonomous: true
requirements: [PART-06, PART-07]
migration: false
tags: [assessment, participant, test, integration, de-tautology]
user_setup: []

must_haves:
  truths:
    - "Read-path test (eligible/idempotency/exclude-by-batch) MENJALANKAN action AssessmentAdminController ASLI via InMemory real-controller (pola ParticipantRemovalExcludeTests) — bukan replica predikat"
    - "Write-path test (ready-status/eager-UPA/Pre-Post/window-reject/Proton-reject/atomic) MENJALANKAN AddParticipantsLive ASLI atas SQLEXPRESS disposable HcPortalDB_Test_{guid} — kolom + schema nyata"
    - "GetEligibleParticipantsToAdd ASLI mengecualikan user dengan sesi APAPUN (aktif maupun RemovedAt!=null) di batch (D-01)"
    - "AddParticipantsLive ASLI: sesi baru ready-status (Open/Upcoming, NEVER InProgress) + UserPackageAssignment tercipta EAGER (A1)"
    - "AddParticipantsLive ASLI: window tutup → 400 + 0-write; Proton → 400 + 0-write; Pre/Post → 2 sesi + LinkedSessionId cross-set"
    - "Tidak ada replica predikat (NO WindowAllowsAddition fiktif / NO DeriveReadyStatus tiruan untuk read-path logic) — anti-pattern 999.12"
    - "Full suite tetap hijau (tak regresi 569 + 6 ParticipantRemoval)"
  artifacts:
    - path: "HcPortal.Tests/FlexibleParticipantAddLiveTests.cs"
      provides: "Integration + InMemory real-controller test untuk AddParticipantsLive + GetEligibleParticipantsToAdd"
      contains: "class FlexibleParticipantAddLiveTests"
  key_links:
    - from: "FlexibleParticipantAddLiveTests"
      to: "AssessmentAdminController.GetEligibleParticipantsToAdd"
      via: "InMemory real-controller invocation"
      pattern: "GetEligibleParticipantsToAdd\\("
    - from: "FlexibleParticipantAddLiveTests"
      to: "AssessmentAdminController.AddParticipantsLive"
      via: "SQLEXPRESS write-path invocation"
      pattern: "AddParticipantsLive\\("
---

<objective>
Tambah test de-tautologis untuk endpoint Phase 410 (`AddParticipantsLive` + `GetEligibleParticipantsToAdd`) di file BARU `HcPortal.Tests/FlexibleParticipantAddLiveTests.cs`. Dua pola sesuai dua jenis assert (lesson anti-tautology 999.12 — test JALANKAN logika produksi ASLI, JANGAN replica predikat):

1. **Read-path (Pola 1 — InMemory real-controller):** eligible exclude-by-batch (D-01), eligible no unit/section (D-02), idempotency skip → panggil action `AssessmentAdminController` ASLI (pola `ParticipantRemovalExcludeTests`).
2. **Write-path (Pola 2 — SQLEXPRESS disposable):** ready-status, eager-UPA (A1), window-reject, Proton-reject, Pre/Post pair, atomic → panggil `AddParticipantsLive` ASLI atas DB nyata `HcPortalDB_Test_{guid}` (pola `FlexibleParticipantAddFixture`).

Purpose: kunci PART-06 (ready-status + UPA + idempotent + window) + PART-07 (Pre/Post pair) secara de-tautologis. Mengonsumsi endpoint Plan 410-01.
Output: 1 file test baru. migration=FALSE.

> ⚠️ Suite e2e endpoint penuh (antiforgery + HttpContext lengkap) = Phase 413. Plan 02 = integration minimal yang membuktikan logika inti via action ASLI (tanpa WebApplicationFactory — repo tak punya, catatan 999.12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/410-add-participant-backend-live/410-RESEARCH.md
@.planning/phases/410-add-participant-backend-live/410-PATTERNS.md

<interfaces>
<!-- Pola test existing yang dicermin. VERIFIED. -->

Pola 1 — InMemory real-controller (HcPortal.Tests/ParticipantRemovalGuardTests.cs:46-117):
```csharp
// MakeController(actionName) → instantiate AssessmentAdminController ASLI, service null untuk yang tak dipakai.
// Constructor (12 arg): (context, userManager:null!, auditLog:new AuditLogService(ctx), env:null!,
//   cache:new MemoryCache(...), logger:NullLogger<...>.Instance, notificationService:null!,
//   hubContext:null!, workerDataService:null!, gradingService:null!, protonCompletionService:null!, protonBypassService:null!)
// ControllerContext.ActionDescriptor.ActionName WAJIB di-set (controller override View() ref ActionName → NRE bila null).
// GetEligibleParticipantsToAdd return Json(...) (bukan View) → ActionName="GetEligibleParticipantsToAdd"; tak perlu StubUrlHelper penuh.
// EF InMemory drop baris FK-absen → seed ApplicationUser cocok tiap UserId (MakeUser :102-107).
var json = await ctrl.GetEligibleParticipantsToAdd(repId);   // → JsonResult; baca .Value (anonymous list)
```
> ⚠️ GetEligibleParticipantsToAdd TIDAK pakai _userManager/_notificationService → InMemory real-controller dengan service null AMAN. AddParticipantsLive PAKAI _userManager.GetUserAsync(User) + _notificationService → null akan NRE; karena itu write-path AddParticipantsLive pakai Pola 2 (lihat catatan write-path di Task 2).

Pola 2 — SQLEXPRESS disposable fixture (HcPortal.Tests/FlexibleParticipantAddTests.cs:20-84, REUSE):
```csharp
public class FlexibleParticipantAddFixture : IAsyncLifetime {
    public string DbName { get; } = $"HcPortalDB_Test_{Guid.NewGuid():N}";
    // _cs = Server=localhost\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;...
    public DbContextOptions<ApplicationDbContext> Options => _options;
    public async Task InitializeAsync() { ...; await ctx.Database.MigrateAsync(); }   // HcPortalDB_Dev TIDAK disentuh
    public async Task DisposeAsync()    { await ctx.Database.EnsureDeletedAsync(); }
}
[Trait("Category", "Integration")] class ... : IClassFixture<FlexibleParticipantAddFixture> { ... }
// Seed helpers existing :63-84: SeedUserAsync, SeedSiblingSessionAsync(ctx, userId, title, category, schedule, status, startedAt?, completedAt?, examWindowCloseDate?)
```

DeriveReadyStatus output (assert target): Status ∈ {AssessmentConstants.AssessmentStatus.Open ("Open"), .Upcoming ("Upcoming")}.
UserPackageAssignment (assert eager): `_context.UserPackageAssignments.Where(a => a.AssessmentSessionId == newSessionId)` → ada bila batch punya AssessmentPackages.
</interfaces>
</context>

<feature>
  <name>FlexibleParticipantAddLiveTests — de-tautology integration untuk Phase 410</name>
  <files>HcPortal.Tests/FlexibleParticipantAddLiveTests.cs</files>
  <behavior>
**Read-path (Pola 1, InMemory real-controller — GetEligibleParticipantsToAdd ASLI):**
- T1 (D-01 exclude removed): batch dengan user A (sesi aktif) + user B (sesi RemovedAt!=null) + user C (tanpa sesi, IsActive) → eligible ASLI HANYA berisi C; A DAN B excluded.
- T2 (D-02 no unit/section): user C dengan Section/Unit berbeda dari batch + IsActive + tanpa sesi → muncul di eligible (tak ada filter unit/section).
- T3 (rep absen): sessionId tak ada → NotFoundResult / objek 404.
- T4 (idempotency read-side via eligible): user yang sudah di batch tak pernah muncul eligible (konsisten D-01).

**Write-path (Pola 2, SQLEXPRESS — AddParticipantsLive ASLI atau seed+assert kolom nyata):**
- T5 (ready-status): add user baru ke batch standard (schedule lampau) → sesi baru Status=="Open", StartedAt/CompletedAt/RemovedAt null.
- T6 (eager UPA, A1): batch dengan ≥1 AssessmentPackage+Questions → setelah add, UserPackageAssignment tercipta (AssessmentSessionId==sesi baru, ShuffledQuestionIds != "[]").
- T7 (window reject): rep.ExamWindowCloseDate = kemarin → AddParticipantsLive → 400 "Window ujian sudah tutup, tidak bisa tambah peserta." + 0 sesi baru (count batch tak bertambah).
- T8 (Proton reject): rep.Category=="Assessment Proton" → 400 + 0 write.
- T9 (Pre/Post pair, PART-07): rep AssessmentType "PreTest" + LinkedGroupId set → add → 2 sesi baru (PreTest+PostTest) untuk user, LinkedSessionId cross-set, keduanya ready-status.
- T10 (idempotent write): user dengan sesi APAPUN di batch dalam userIds → masuk skipped, tak dobel-create.
  </behavior>
  <read_first>
    - HcPortal.Tests/ParticipantRemovalGuardTests.cs:46-117 (MakeController helper + StubUrlHelper + MakeUser + MakeSession — Pola 1 verbatim)
    - HcPortal.Tests/FlexibleParticipantAddTests.cs:20-84 (FlexibleParticipantAddFixture + SeedUserAsync + SeedSiblingSessionAsync — Pola 2 verbatim; JANGAN tiru replica :86-99 = anti-pattern)
    - .planning/phases/410-add-participant-backend-live/410-PATTERNS.md §"FlexibleParticipantAddLiveTests" (2 pola + test map + anti-tautology gap)
    - .planning/phases/410-add-participant-backend-live/410-RESEARCH.md §"Validation Architecture" (test framework + de-tautology rules + Wave 0 gaps)
    - Controllers/AssessmentAdminController.cs (endpoint Plan 01 — signature AddParticipantsLive / GetEligibleParticipantsToAdd, untuk panggil ASLI)
  </read_first>
  <implementation>
Buat file `HcPortal.Tests/FlexibleParticipantAddLiveTests.cs`. Struktur: dua kelas terpisah sesuai dua pola.

**Bagian A — `FlexibleParticipantAddEligibleTests` (Pola 1, InMemory, NO Trait Integration → fast suite):**
- Salin `MakeController(actionName)` + `StubUrlHelper` + `MakeUser(id, name)` + `MakeSession(...)` dari `ParticipantRemovalGuardTests.cs:51-117` (boleh copy private static — pola project; atau panggil bila visible). `MakeSession` perlu param `removedAt` + `assessmentType` + `Section`/`Unit` bila perlu T2.
- Untuk T2 (D-02): set `MakeUser` dengan `Section`/`Unit` berbeda — verifikasi field ada di ApplicationUser (Read Models/ApplicationUser.cs: Section :28, Unit :33). Bila ApplicationUser punya Section/Unit string, set nilai berbeda dari batch user.
- T1: seed 3 user (A aktif, B removed, C bebas) + sesi A (RemovedAt null) + sesi B (RemovedAt = DateTime.UtcNow) di batch "BATCH X"/"OJT"/sched; user C IsActive tanpa sesi. `var (ctrl, ctx) = MakeController("GetEligibleParticipantsToAdd");` → seed → `var result = await ctrl.GetEligibleParticipantsToAdd(repId);` → cast `JsonResult` → baca `.Value` (list anonymous; gunakan reflection atau serialize→deserialize ke `List<dynamic>`/JSON untuk assert id). Assert hanya C.Id present; A.Id + B.Id absent.
- ⚠️ `ApplicationUser.IsActive` default? Verifikasi (Read ApplicationUser.cs:66) — set `IsActive=true` eksplisit di MakeUser bila default false, agar eligible tak kosong.
- T3: `MakeController(...)` tanpa seed sesi → `GetEligibleParticipantsToAdd(99999)` → assert `NotFoundObjectResult` atau objek error.

**Bagian B — `FlexibleParticipantAddLiveWriteTests` (Pola 2, SQLEXPRESS, `[Trait("Category","Integration")]`, `IClassFixture<FlexibleParticipantAddFixture>`):**
- REUSE `FlexibleParticipantAddFixture` (sudah ada di `FlexibleParticipantAddTests.cs` — kelas public, share via IClassFixture; JANGAN duplikat). Seed helper: REUSE pola `SeedUserAsync`/`SeedSiblingSessionAsync` (copy private static bila tak visible cross-class).
- **Catatan drive AddParticipantsLive ASLI:** AddParticipantsLive memakai `_userManager.GetUserAsync(User)` + `_notificationService.SendAsync` → tak bisa pakai `null!` service. Dua opsi (executor discretion):
  - **Opsi 2a (PREFERRED bila feasible):** instantiate controller dengan SQLEXPRESS context + stub `UserManager` minimal (return ApplicationUser actor) + stub `INotificationService` (no-op return true) + set `ControllerContext.HttpContext.User` (ClaimsPrincipal Admin). Panggil `await ctrl.AddParticipantsLive(repId, userIds)` ASLI → assert DB nyata (sesi + UPA). Ini de-tautology penuh. Bila stub UserManager terlalu berat (UserManager butuh banyak dependency), gunakan Opsi 2b untuk write-create, TAPI minimal T7/T8 (window/Proton reject) WAJIB drive action ASLI (guard murni — tak butuh userManager sampai setelah guard? verifikasi: guard window/Proton ada SEBELUM `_userManager.GetUserAsync` di Langkah 6 → reject path tak sentuh userManager → bisa drive ASLI dengan service null untuk T7/T8).
  - **Opsi 2b (fallback untuk create-path bila stub UserManager infeasible):** untuk T5/T6/T9/T10 (yang butuh full create melewati userManager), seed via SQLEXPRESS lalu assert kolom NYATA pasca pemanggilan `CreateEagerAssignmentsAsync` / `BuildReadyParticipantSession` bila dibuat `internal` accessible — ATAU panggil AddParticipantsLive ASLI dengan stub minimal. **JANGAN** buat replica predikat `WindowAllowsAddition`/`DeriveReadyStatus` tiruan untuk meng-assert logika (anti-pattern 999.12). Bila harus seed-then-assert, assert kolom DB nyata (Status, RemovedAt, UserPackageAssignment row, LinkedSessionId) yang DIHASILKAN produksi — bukan tiruan.
- **Prioritas de-tautology:** T7 (window) + T8 (Proton) drive action ASLI (guard sebelum userManager → service null OK). T5/T6/T9 drive ASLI bila stub userManager feasible; bila tidak, minimal pastikan eager-UPA + ready-status + Pre/Post pair di-assert atas hasil produksi nyata (bukan replica).
- Seed AssessmentPackage untuk T6: butuh `AssessmentPackage` (PackageNumber, AssessmentSessionId) + `PackageQuestion` + `PackageOption` agar `packages.Any()` true → UPA ter-eager. Verifikasi field model via Read Models/AssessmentPackage.cs + PackageQuestion.cs.
  </implementation>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~FlexibleParticipantAddLive" 2>&1 | grep -E "Passed!|Failed!|error|Passed:|Failed:" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/FlexibleParticipantAddLiveTests.cs` exists
    - Contains `class FlexibleParticipantAddEligibleTests` (Pola 1, InMemory) AND a write-path class with `[Trait("Category", "Integration")]`
    - Read-path test invokes REAL action: grep `await ctrl.GetEligibleParticipantsToAdd(` present (NOT a replica predicate)
    - Write-path test references `AddParticipantsLive(` OR drives reject-path of real action (T7/T8) — grep `AddParticipantsLive(` present
    - Contains assertion for removed-exclusion: grep `RemovedAt` present in a seed for the eligible test (D-01 coverage)
    - Contains assertion for eager UPA: grep `UserPackageAssignment` present in write-path (A1 coverage)
    - Contains verbatim window message check: grep `Window ujian sudah tutup, tidak bisa tambah peserta.` present
    - Contains Pre/Post assertion: grep `LinkedSessionId` present (PART-07 cross-link)
    - NO replica predicate named `WindowAllowsAddition` (anti-pattern 999.12 — must NOT appear)
    - `dotnet test --filter "FullyQualifiedName~FlexibleParticipantAddLive"` → all new tests Passed (read-path always runs; write-path runs when SQLEXPRESS available)
  </acceptance_criteria>
  <done>FlexibleParticipantAddLiveTests menjalankan endpoint Phase 410 ASLI (read-path via InMemory real-controller; write-path/reject via SQLEXPRESS) tanpa replica predikat; T7/T8 reject-path + eligible exclude-removed + eager-UPA + Pre/Post pair ter-assert; tests hijau.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → AssessmentAdminController (in-process) | Test memanggil action langsung; tak ada boundary jaringan |
| Test → SQLEXPRESS disposable HcPortalDB_Test_{guid} | DB nyata terisolasi; HcPortalDB_Dev TIDAK disentuh (CLAUDE.md Seed Workflow) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-410T-01 | Tampering | Test menyentuh HcPortalDB_Dev (korup data dev) | mitigate | Fixture pakai `HcPortalDB_Test_{Guid}` + `EnsureDeletedAsync` di DisposeAsync (REUSE FlexibleParticipantAddFixture); JANGAN connect HcPortalDB_Dev |
| T-410T-02 | Repudiation | Test tautologis (replica predikat) memberi false-confidence (lesson 999.12) | mitigate | Read-path WAJIB drive action ASLI (Pola 1); JANGAN replica `WindowAllowsAddition`/`DeriveReadyStatus`; acceptance criteria melarang replica predicate |
| T-410T-03 | — | Test bocorkan kredensial SQLEXPRESS | accept | Connection string Integrated Security (Windows auth lokal), tak ada secret literal — sama dengan fixture existing |

> Plan 02 = test-only; tak menambah surface produksi. Threat utama = isolasi DB + anti-tautology (keduanya mitigated).
</threat_model>

<verification>
1. `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → 0 error.
2. `dotnet test HcPortal.Tests --filter "FullyQualifiedName~FlexibleParticipantAddLive"` → tests baru hijau (read-path InMemory selalu jalan; write-path Integration jalan bila SQLEXPRESS up).
3. `dotnet test HcPortal.Tests` (full, incl. Integration) → tak regresi 569 + 6 ParticipantRemoval; total naik dengan test baru.
4. Gate fase (CLAUDE.md Develop Workflow): build 0 error + full suite hijau + `dotnet run` @5277 OK.
</verification>

<success_criteria>
- File `FlexibleParticipantAddLiveTests.cs` ada; read-path drive `GetEligibleParticipantsToAdd` ASLI; write-path drive `AddParticipantsLive` ASLI (minimal reject-path T7/T8 + eager-UPA + Pre/Post assert nyata).
- D-01 (removed excluded) + D-02 (no unit/section) + ready-status + eager-UPA (A1) + window-reject + Proton-reject + Pre/Post pair ter-cover.
- NO replica predikat (anti 999.12). Full suite hijau tak regresi. migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/410-add-participant-backend-live/410-02-SUMMARY.md`. Catat: opsi drive write-path yang dipakai (2a stub userManager vs 2b assert-kolom-nyata) + alasan; test mana yang drive action ASLI vs seed-then-assert-real-column; jumlah test + status full suite; konfirmasi NO replica predikat.
</output>
