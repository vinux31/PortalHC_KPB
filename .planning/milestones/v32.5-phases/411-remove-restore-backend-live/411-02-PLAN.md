---
phase: 411-remove-restore-backend-live
plan: 02
type: execute
wave: 2
depends_on: [411-01]
files_modified:
  - HcPortal.Tests/FlexibleParticipantRemoveTests.cs
autonomous: true
requirements: [PRMV-01, PRMV-04, PRMV-05, PLIV-03]
user_setup: []

must_haves:
  truths:
    - "Test menjalankan action AssessmentAdminController ASLI (RemoveParticipantLive/RestoreParticipantLive) — NO replica predikat SessionHasDataAsync (de-tautology 999.12)"
    - "Hard-delete not-started: setelah RemoveParticipantLive, baris AssessmentSession + UserPackageAssignment benar-benar HILANG dari DB (AnyAsync==false)"
    - "Soft-remove in-progress/completed-cert: RemovedAt set NYATA; Score/IsPassed/NomorSertifikat/Status UNCHANGED (assert kolom DB nyata)"
    - "Idempotent: panggil remove 2× → panggilan ke-2 mode=noop sukses"
    - "Restore: soft-removed → RemovedAt/RemovedBy/RemovalReason di-clear; non-removed → 400"
    - "Pre/Post pair via LinkedSessionId: salah satu berdata → soft keduanya; keduanya bersih → hard keduanya (BUKAN seluruh batch)"
    - "Reason-wajib soft (D-02): soft tanpa reason → 400; hard tanpa reason → sukses"
    - "Proton reject → 400 + 0-write"
    - "Audit row tertulis untuk Remove + Restore"
  artifacts:
    - path: "HcPortal.Tests/FlexibleParticipantRemoveTests.cs"
      provides: "Read-path InMemory (idempotency/Proton/restore-guard/reason-validation) + Write-path SQLEXPRESS (hard-delete row+UPA gone, soft preserve, Pre/Post pair, audit) dengan mini-DI service-provider stub untuk RecordCascadeDeleteService"
      contains: "FlexibleParticipantRemove"
      min_lines: 300
  key_links:
    - from: "FlexibleParticipantRemoveTests (write-path hard)"
      to: "RemoveParticipantCoreAsync → RecordCascadeDeleteService"
      via: "ControllerContext.HttpContext.RequestServices = mini-DI ServiceProvider"
      pattern: "RequestServices"
    - from: "test assert hard-delete"
      to: "UserPackageAssignments + AssessmentSessions"
      via: "AnyAsync == false (baris NYATA hilang)"
      pattern: "AnyAsync"
---

<objective>
Kunci kontrak Plan 411-01 (RemoveParticipantLive + RestoreParticipantLive + RemoveParticipantCoreAsync) secara **de-tautologis** (lesson 999.12, WAJIB): setiap test MENJALANKAN action `AssessmentAdminController` ASLI dan meng-assert kolom DB NYATA — TIDAK ada replica predikat `SessionHasDataAsync`. File baru `HcPortal.Tests/FlexibleParticipantRemoveTests.cs` dengan dua bagian (read-path InMemory + write-path SQLEXPRESS), REUSE infra 410 (`FlexibleParticipantAddFixture`, StubUserManager, NoopNotificationService) plus satu mini-DI baru (gap terbesar 411).

Purpose: PRMV-01/04/05 + PLIV-03 terkunci regression; membuktikan hard-delete bersih (baris+UPA hilang), soft-remove preserve cert/score, Pre/Post pair-as-unit, idempotency, reason-wajib-soft, restore, Proton-reject, audit.
Output: 1 file test baru. migration=FALSE. JANGAN sentuh kode produksi (`AssessmentAdminController.cs`) — itu Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/411-remove-restore-backend-live/411-RESEARCH.md
@.planning/phases/411-remove-restore-backend-live/411-PATTERNS.md
@.planning/phases/411-remove-restore-backend-live/411-01-PLAN.md
@HcPortal.Tests/FlexibleParticipantAddLiveTests.cs

<interfaces>
<!-- REUSE infra 410 — Read-verified. JANGAN duplikat fixture/stub yang sudah ada. -->

Fixture (REUSE via [IClassFixture]) — HcPortal.Tests/FlexibleParticipantAddTests.cs:20-53:
  FlexibleParticipantAddFixture : IAsyncLifetime
    .Options (DbContextOptions<ApplicationDbContext> ke SQLEXPRESS HcPortalDB_Test_{guid})
    InitializeAsync → MigrateAsync (gagal = MIGRATION-CHAIN break, BUKAN bug); DisposeAsync → EnsureDeletedAsync
  CONN: Server=localhost\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30

Pola read-path InMemory — FlexibleParticipantAddLiveTests.cs:52-85 (MakeController):
  UseInMemoryDatabase(Guid) + AuditLogService(ctx) + MemoryCache + ctor 12-args (userManager null! aman bila action tak panggil GetUserAsync) + ControllerContext{ HttpContext=DefaultHttpContext, ActionDescriptor=ControllerActionDescriptor{ActionName=...} } + ViewData + TempData.
  CATATAN: RemoveParticipantLive/RestoreParticipantLive PANGGIL _userManager.GetUserAsync → untuk path yang sampai resolve-actor butuh StubUserManager (Bagian B). Idempotency/Proton-reject/restore-guard return SEBELUM resolve-actor → InMemory + userManager null! aman.

Pola write-path SQLEXPRESS — FlexibleParticipantAddLiveTests.cs:213-285:
  [Trait("Category","Integration")] + IClassFixture<FlexibleParticipantAddFixture>
  StubUserManager(actor) override GetUserAsync → actor (UserManager non-sealed; StubUserStore no-op IUserStore)
  NoopNotificationService : INotificationService (7 method no-op)
  MakeLiveController(ctx, actor): ctor 12-args dgn StubUserManager + NoopNotificationService + ControllerContext{ HttpContext=DefaultHttpContext, ActionDescriptor.ActionName="RemoveParticipantLive" }
  SeedUserAsync(ctx,...) → userId; SeedRepSessionAsync(ctx, userId, title, category, schedule, status, examWindowCloseDate?, assessmentType?, linkedGroupId?, generateCertificate?, durationMinutes?) → sessionId

Controller ctor 12-args — AssessmentAdminController.cs:33 (lihat 411-01 interfaces).

Cascade service deps (untuk mini-DI) — VERIFIED:
  RecordCascadeDeleteService(ApplicationDbContext, ILogger<RecordCascadeDeleteService>, ProtonCompletionService, AuditLogService, IWebHostEnvironment) — RecordCascadeDeleteService.cs:28-33
  ProtonCompletionService(ApplicationDbContext, ILogger<ProtonCompletionService>, INotificationService, AuditLogService) — ProtonCompletionService.cs:25-29
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read-path InMemory + Write-path soft/restore/Pre-Post SQLEXPRESS (de-tautology, REUSE 410 infra)</name>
  <files>HcPortal.Tests/FlexibleParticipantRemoveTests.cs</files>
  <read_first>
    - HcPortal.Tests/FlexibleParticipantAddLiveTests.cs (full — copy MakeController, StubUserManager, StubUserStore, NoopNotificationService, MakeLiveController, SeedUserAsync, SeedRepSessionAsync verbatim ke file baru atau referensi)
    - HcPortal.Tests/FlexibleParticipantAddTests.cs:20-53 (FlexibleParticipantAddFixture — REUSE via IClassFixture, JANGAN duplikat)
    - 411-RESEARCH.md §"Phase Requirements → Test Map" (12 test mapping) + §"Test Infrastructure (REUSE 410)"
    - 411-PATTERNS.md §"FlexibleParticipantRemoveTests.cs (NEW)" + de-tautology rules
  </read_first>
  <action>
    Buat file BARU `HcPortal.Tests/FlexibleParticipantRemoveTests.cs`. Header comment jelaskan de-tautology (lesson 999.12): setiap test JALANKAN action ASLI / assert kolom DB NYATA; NO replica predikat. namespace `HcPortal.Tests`. migration=FALSE; DB disposable HcPortalDB_Test_{guid} (HcPortalDB_Dev TIDAK disentuh).

    **BAGIAN A — Read-path InMemory `class FlexibleParticipantRemoveReadTests`** (pola `MakeController` :52-85, copy verbatim helper MakeController + MakeUser + MakeSession). Test yang return SEBELUM resolve-actor (userManager null! aman):
    - **A1 `RemoveParticipantLive_Proton_Rejected400`:** seed sesi `Category="Assessment Proton"` → invoke `RemoveParticipantLive(id, "alasan")` ASLI → assert `BadRequestObjectResult` + 0-write (sesi masih ada, RemovedAt null). (scope Proton)
    - **A2 `RemoveParticipantLive_AlreadyRemoved_NoOp`:** seed sesi `RemovedAt != null` → invoke → assert `JsonResult` dgn `mode="noop"` (idempotency, Pitfall 3). (PRMV-01)
    - **A3 `RemoveParticipantLive_NotFound_404`:** invoke dgn sessionId tak ada → `NotFoundObjectResult`. (PRMV-01)
    - **A4 `RestoreParticipantLive_NotRemoved_Rejected400`:** seed sesi aktif (RemovedAt null) → invoke `RestoreParticipantLive(id)` → `BadRequestObjectResult` "Sesi ini tidak dalam keadaan dihapus." (PRMV-04)
    - **A5 `RestoreParticipantLive_NotFound_404`:** sessionId tak ada → `NotFoundObjectResult`. (PRMV-04)
    > CATATAN: parse JsonResult value via reflection/anonymous (pola FlexibleParticipantAddLiveTests Bagian A — `JsonResult.Value` + `GetType().GetProperty("mode")`). NO replica logika.

    **BAGIAN B — Write-path SQLEXPRESS `class FlexibleParticipantRemoveWriteTests : IClassFixture<FlexibleParticipantAddFixture>`** (`[Trait("Category","Integration")]`). Copy verbatim ke file ini: `StubUserManager`, `StubUserStore`, `NoopNotificationService`, `MakeLiveController` (ganti ActionName="RemoveParticipantLive"), `NewCtx()`, `SeedUserAsync`, `SeedRepSessionAsync` (extend per bawah). **Tambah seed helper:**
    - `SeedResponseAsync(ctx, sessionId)`: insert 1 `PackageUserResponse { AssessmentSessionId = sessionId, ... }` (cek field wajib model PackageUserResponse — minimal valid row). Tandai sesi "berdata".
    - `SeedUpaAsync(ctx, sessionId, userId)`: insert 1 `UserPackageAssignment { AssessmentSessionId = sessionId, ... }` (eager-UPA simulasi 410 — untuk D-01 hard-delete-with-UPA test). Cek field wajib model.

    Test write-path (drive action ASLI, assert kolom NYATA):
    - **B1 `RemoveInProgress_SoftRemoves_PreservesData` (PRMV-01):** seed sesi `StartedAt = UtcNow.AddMinutes(-5)` + Score=80, Status="InProgress" + 1 response. Snapshot Score/Status. invoke `RemoveParticipantLive(id, "tidak hadir")` via MakeLiveController. Assert: response `JsonResult` mode="soft"; reload ctx → `RemovedAt != null`, `RemovedBy == actorId`, `RemovalReason == "tidak hadir"`, **`Score == 80` UNCHANGED, `Status == "InProgress"` UNCHANGED**, response row masih ada (`PackageUserResponses.AnyAsync == true`).
    - **B2 `RemoveCertified_SoftRemoves_PreservesCert` (PRMV-01):** seed sesi Status="Completed" + `NomorSertifikat="KPB/X"` + `ManualSertifikatUrl` set + IsPassed=true + (StartedAt set). invoke `RemoveParticipantLive(id, "salah input")`. Assert: mode="soft"; `RemovedAt != null`; **`NomorSertifikat`, `ManualSertifikatUrl`, `IsPassed`, `Status` UNCHANGED**.
    - **B3 `RemoveSoft_NoReason_Rejected400` (D-02):** seed sesi berdata (StartedAt set). invoke `RemoveParticipantLive(id, reason: null)` → `BadRequestObjectResult` "Alasan penghapusan wajib diisi."; reload → `RemovedAt == null` (0-write). (PLIV-03)
    - **B4 `RemoveInProgress_Idempotent_NoOp` (PRMV-01):** seed berdata → invoke remove (soft) → invoke remove KEDUA kalinya → panggilan ke-2 `JsonResult` mode="noop"; RemovedAt tetap dari panggilan pertama (tak ter-overwrite).
    - **B5 `Restore_SoftRemoved_ClearsColumns` (PRMV-04):** seed sesi berdata → remove (soft) → invoke `RestoreParticipantLive(id)` (controller ActionName-set; pakai MakeLiveController variant atau set ActionName). Assert: `JsonResult` restored=true; reload → `RemovedAt == null && RemovedBy == null && RemovalReason == null`.
    - **B6 `RemovePrePost_OneHasData_SoftBoth` (PRMV-05):** seed pasangan Pre+Post 1 peserta — `AssessmentType="PreTest"`/`"PostTest"`, `LinkedSessionId` cross-set (Pre.LinkedSessionId=Post.Id & sebaliknya). Pre berdata (StartedAt set), Post bersih. invoke `RemoveParticipantLive(preId, "alasan")`. Assert: mode="soft"; **kedua sesi** `RemovedAt != null`; `linkedSessionId` di JSON == postId. **Plus seed sesi peserta LAIN di batch sama → assert peserta lain TIDAK ikut removed** (Pitfall 1 — bukan seluruh batch).
    - **B7 `RemovePrePost_BothClean_HardBoth` (PRMV-05):** seed pasangan Pre+Post bersih (StartedAt null, 0 response), LinkedSessionId cross-set. invoke `RemoveParticipantLive(preId, null)` (reason opsional, hard) — **butuh mini-DI (Task 2)**. → tandai test ini `Skip`/`Trait` group "HardDelete" yang diaktifkan Task 2; ATAU implement di Task 2. **Pindahkan B7 + hard-delete tests ke Task 2** (butuh service-provider stub).
    - **B8 `Remove_WritesAuditRow` (PLIV-03):** seed berdata → remove (soft) → assert `_context.AuditLogs.AnyAsync(a => a.ActionType == "RemoveParticipantLive")` == true.

    **De-tautology assert (WAJIB):** Hard: `AnyAsync == false`. Soft: kolom set/unchanged NYATA via reload ctx. JANGAN tulis ulang `SessionHasDataAsync` di test.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~FlexibleParticipantRemove" 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/FlexibleParticipantRemoveTests.cs` ada
    - `grep -c "\[Fact\]" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` ≥ 10
    - `grep -n "class FlexibleParticipantRemoveReadTests" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → 1 (Bagian A InMemory)
    - `grep -n "IClassFixture<FlexibleParticipantAddFixture>" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → 1 (Bagian B REUSE fixture)
    - `grep -n "RemoveParticipantLive\|RestoreParticipantLive" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥6 (drive action ASLI)
    - `grep -n "AnyAsync" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥1 (de-tautology assert row nyata)
    - `grep -c "SessionHasDataAsync" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` == 0 (NO replica predikat; hanya boleh di komentar)
    - `grep -n "mode=\"soft\"\|mode == \"soft\"\|\"soft\"" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥1 (assert soft outcome)
    - `dotnet test --filter "FullyQualifiedName~FlexibleParticipantRemove"` → semua test bagian A + B (soft/restore/Pre-Post-soft/audit) Passed (hard-delete tests → Task 2)
  </acceptance_criteria>
  <done>File test ada; Bagian A (5 InMemory) + Bagian B soft/restore/Pre-Post-soft/audit/reason/idempotent hijau; drive action ASLI; NO replica predikat; SQLEXPRESS write-path NYATA.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hard-delete write-path via mini-DI service-provider stub (gap terbesar 411)</name>
  <files>HcPortal.Tests/FlexibleParticipantRemoveTests.cs</files>
  <read_first>
    - 411-PATTERNS.md §"🔴 TEST-INFRA GAP TERBESAR 411 (service-provider stub untuk hard-delete)" (mini-DI ServiceCollection blueprint verbatim + dependency chain VERIFIED)
    - 411-RESEARCH.md §"Wave 0 Gaps" butir 2 (service-provider stub) + §"Environment Availability"
    - Services/RecordCascadeDeleteService.cs:28-33 (ctor 5 deps) + :175-260 (ExecuteAsync + cascade UPA :221-222)
    - Services/ProtonCompletionService.cs:25-29 (ctor 4 deps)
    - Task 1 di plan ini (MakeLiveController, fixture, SeedUserAsync/SeedRepSessionAsync/SeedUpaAsync sudah ada)
  </read_first>
  <action>
    Tambah ke `HcPortal.Tests/FlexibleParticipantRemoveTests.cs` (di `FlexibleParticipantRemoveWriteTests`):

    **(a) Stub `IWebHostEnvironment`** (cascade hapus file cert post-commit → WebRootPath harus non-null, walau not-started tak punya cert):
    ```csharp
    private sealed class StubWebHostEnvironment : IWebHostEnvironment
    {
        public string WebRootPath { get; set; } = System.IO.Path.GetTempPath();
        public Microsoft.Extensions.FileProviders.IFileProvider WebRootFileProvider { get; set; } = new Microsoft.Extensions.FileProviders.NullFileProvider();
        public string ApplicationName { get; set; } = "HcPortal.Tests";
        public Microsoft.Extensions.FileProviders.IFileProvider ContentRootFileProvider { get; set; } = new Microsoft.Extensions.FileProviders.NullFileProvider();
        public string ContentRootPath { get; set; } = System.IO.Path.GetTempPath();
        public string EnvironmentName { get; set; } = "Test";
    }
    ```

    **(b) Helper `BuildCascadeServiceProvider(ApplicationDbContext ctx)` → `IServiceProvider`** (mini-DI, blueprint PATTERNS — dependency chain VERIFIED). **PENTING:** semua service share `ctx` SQLEXPRESS yang SAMA dengan controller (agar cascade hapus baris yang sama dilihat assert).
    ```csharp
    var services = new ServiceCollection();
    services.AddSingleton(ctx);
    services.AddSingleton(new AuditLogService(ctx));
    services.AddSingleton<INotificationService>(new NoopNotificationService());
    services.AddSingleton(NullLogger<ProtonCompletionService>.Instance);
    services.AddSingleton(NullLogger<RecordCascadeDeleteService>.Instance);
    services.AddSingleton<IWebHostEnvironment>(new StubWebHostEnvironment());
    services.AddScoped<ProtonCompletionService>();
    services.AddScoped<RecordCascadeDeleteService>();
    return services.BuildServiceProvider();
    ```
    Verifikasi ctor signature ProtonCompletionService + RecordCascadeDeleteService saat implement (AddScoped resolve via reflection ctor — pastikan tiap dep ter-registrasi; bila ProtonCompletionService butuh `ILogger<ProtonCompletionService>` non-generic interface, daftarkan `NullLogger<T>.Instance` sesuai signature Read-verified).

    **(c) `MakeLiveControllerWithCascade(ctx, actor)`** = `MakeLiveController(ctx, actor)` + set `ctrl.ControllerContext.HttpContext.RequestServices = BuildCascadeServiceProvider(ctx);`. Gunakan untuk SEMUA test hard-delete (jalur hard panggil `HttpContext.RequestServices.GetRequiredService<RecordCascadeDeleteService>()`).

    Test hard-delete (drive action ASLI end-to-end, assert baris NYATA hilang):
    - **C1 `RemoveNotStarted_HardDeletes_RowGone` (PRMV-01):** seed sesi bersih (StartedAt null, 0 response), Status="Open". invoke `RemoveParticipantLive(id, null)` via `MakeLiveControllerWithCascade`. Assert: `JsonResult` mode="hard"; reload ctx → `AssessmentSessions.AnyAsync(s => s.Id == id) == false` (baris HILANG).
    - **C2 `RemoveWithEagerUPA_StillHardDeletes_UpaGone` (D-01, PRMV-01):** seed sesi bersih + `SeedUpaAsync` (1 UPA, StartedAt tetap null, 0 response). invoke remove. Assert: mode="hard"; `AssessmentSessions.AnyAsync(...) == false` **DAN `UserPackageAssignments.AnyAsync(a => a.AssessmentSessionId == id) == false`** (D-01: UPA bukan "data", hard-delete tetap jalan + cascade bersihkan UPA).
    - **C3 `RemovePrePost_BothClean_HardBoth` (PRMV-05, = B7 dari Task 1):** seed pasangan Pre+Post bersih + LinkedSessionId cross-set. invoke `RemoveParticipantLive(preId, null)` via WithCascade. Assert: mode="hard"; **kedua** `AssessmentSessions.AnyAsync(preId)==false && AnyAsync(postId)==false`. Plus seed peserta LAIN bersih di batch → assert peserta lain MASIH ada (bukan seluruh batch — Pitfall 1).
    - **C4 (opsional, jika murah) `RemoveNotStarted_NoReason_Succeeds` (D-02):** hard tanpa reason → mode="hard" sukses (reason opsional di hard) — bisa digabung ke C1 dengan reason:null.

    **De-tautology:** hard assert WAJIB `AnyAsync == false` atas DB SQLEXPRESS NYATA (bukan in-memory mock cascade). JANGAN panggil `RecordCascadeDeleteService.ExecuteAsync` langsung — drive lewat `RemoveParticipantLive` ASLI agar core teruji integral (PATTERNS rekomendasi mini-DI, bukan alternatif "panggil cascade langsung").
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~FlexibleParticipantRemove" 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BuildCascadeServiceProvider\|MakeLiveControllerWithCascade" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥2 (mini-DI ada)
    - `grep -n "GetRequiredService<RecordCascadeDeleteService>\|RecordCascadeDeleteService" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥1 (registrasi mini-DI)
    - `grep -n "RequestServices" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥1 (set di ControllerContext.HttpContext)
    - `grep -n "UserPackageAssignments.AnyAsync\|AnyAsync(a => a.AssessmentSessionId" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥1 (D-01 UPA-gone assert)
    - `grep -n "mode=\"hard\"\|\"hard\"" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` → ≥1
    - `grep -c "ExecuteAsync" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` == 0 (JANGAN panggil cascade langsung — drive via RemoveParticipantLive ASLI; hanya boleh di komentar)
    - `dotnet test --filter "FullyQualifiedName~FlexibleParticipantRemove"` → SEMUA test (A + B + C hard-delete) Passed
    - `dotnet test` full fast-suite hijau — tak regresi 409 guard + 410 add
  </acceptance_criteria>
  <done>Mini-DI service-provider stub + StubWebHostEnvironment ada; hard-delete tests (row gone + UPA gone D-01 + Pre/Post both-clean hard) hijau via RemoveParticipantLive ASLI; full suite no-regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → SQLEXPRESS disposable | DB `HcPortalDB_Test_{guid}` terisolasi; HcPortalDB_Dev TIDAK disentuh (CLAUDE.md Seed Workflow) |
| test → action controller ASLI | de-tautology: validasi keamanan produksi (RBAC attr, Proton-reject, idempotency) lewat eksekusi nyata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-411T-01 | Tampering (test tautologis lolos regresi) | replica predikat SessionHasDataAsync | mitigate | De-tautology WAJIB (999.12): drive action ASLI + assert kolom DB nyata; acceptance `grep SessionHasDataAsync == 0` + `grep ExecuteAsync == 0` (tak bypass core) |
| T-411T-02 | Tampering (test hapus DB Dev) | connection string | mitigate | REUSE FlexibleParticipantAddFixture → HcPortalDB_Test_{guid} disposable (EnsureDeletedAsync di DisposeAsync); JANGAN connect HcPortalDB_Dev |
| T-411T-03 | Information Disclosure (false-green tanpa SQLEXPRESS) | write-path skip | accept | SQLEXPRESS tersedia (dipakai 409/410 tests, VERIFIED env); bila absent → Integration trait gagal nyata (bukan silent-skip). Read-path InMemory tetap jalan |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~FlexibleParticipantRemove"` → semua Passed (Task 1 + Task 2).
- `dotnet test` full fast-suite hijau — tak regresi 409 guard (ParticipantRemovalGuardTests) + 410 add (FlexibleParticipantAddLiveTests).
- `grep -c "SessionHasDataAsync\|ExecuteAsync" HcPortal.Tests/FlexibleParticipantRemoveTests.cs` == 0 di luar komentar (de-tautology — drive action ASLI, tak replica/bypass).
- `git status Migrations/ Data/` kosong → migration=FALSE.
- SQLEXPRESS write-path BENAR berjalan (bukan skip): cek output test `[Trait Category=Integration]` count > 0 executed.
- DB test auto-disposed (fixture DisposeAsync EnsureDeletedAsync) — tak ada `HcPortalDB_Test_*` tersisa.
</verification>

<success_criteria>
1. `FlexibleParticipantRemoveTests.cs` ada: Bagian A (InMemory: Proton/idempotency/restore-guard/404) + Bagian B (SQLEXPRESS: soft preserve cert/score, reason-wajib, idempotent, restore, Pre/Post soft, audit) + hard-delete (mini-DI: row gone, UPA gone D-01, Pre/Post both-clean hard). *(PRMV-01/04/05 + PLIV-03)*
2. Drive action `AssessmentAdminController` ASLI; NO replica `SessionHasDataAsync`; NO panggil cascade langsung — de-tautology 999.12. *(test integrity)*
3. Hard-delete assert baris + UPA NYATA hilang (`AnyAsync == false` SQLEXPRESS). *(D-01, PRMV-01)*
4. `dotnet test --filter ~FlexibleParticipantRemove` semua hijau + full fast-suite no-regression; migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/411-remove-restore-backend-live/411-02-SUMMARY.md`
</output>
