---
phase: 412-live-monitoring-ui-signalr
plan: 03
type: execute
wave: 2
depends_on: [412-01]
files_modified:
  - Views/CMP/StartExam.cshtml
autonomous: true
requirements: [PRMV-02]
user_setup: []

must_haves:
  truths:
    - "Worker yang sedang ujian menerima event examRemoved → UI ujian terkunci (timer/save berhenti, onbeforeunload clear)"
    - "Modal #examRemovedModal muncul dengan pesan 'Anda telah dikeluarkan dari ujian ini.' (non-dismissable)"
    - "Setelah countdown 5 detik (atau klik tombol), worker di-redirect ke daftar Assessment (/CMP/Assessment)"
    - "Alasan dari admin (payload.reason) ditampilkan via textContent bila tidak kosong"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Handler examRemoved (mirror examClosed) + modal #examRemovedModal"
      contains: "examRemoved"
  key_links:
    - from: "assessmentHub.on examRemoved"
      to: "#examRemovedModal + redirect /CMP/Assessment"
      via: "lock UI + countdown redirect"
      pattern: "examRemovedModal"
---

<objective>
Tambah handler client `examRemoved` di `StartExam.cshtml` (mirror VERBATIM `examClosed`) + modal non-dismissable `#examRemovedModal`. Saat Admin/HC menghapus peserta yang sedang InProgress, Plan 01 mengirim `examRemoved` ke worker tunggal → handler ini mengunci UI ujian + redirect ke daftar Assessment dengan banner "Anda telah dikeluarkan dari ujian ini." (D-02).

Purpose: PRMV-02 sisi-worker (force-kick). File terpisah dari Plan 02 (view monitoring) → wave sama, paralel; keduanya hanya depends Plan 01 (event examRemoved).

Output: `StartExam.cshtml` (+handler examRemoved +modal). migration=FALSE. Patuh 412-UI-SPEC.md §9 verbatim (markup modal + handler + copywriting). Tidak menyentuh controller atau view monitoring.

CATATAN RUNTIME: SignalR force-kick = runtime (lesson Phase 354). Smoke minimal di task verifikasi; e2e multi-context kick LENGKAP = Phase 413.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/412-live-monitoring-ui-signalr/412-UI-SPEC.md
@.planning/phases/412-live-monitoring-ui-signalr/412-RESEARCH.md
@.planning/phases/412-live-monitoring-ui-signalr/412-PATTERNS.md
@.planning/phases/412-live-monitoring-ui-signalr/412-CONTEXT.md

<interfaces>
<!-- Handler + modal existing untuk di-mirror. VERIFIED file:line. -->

Handler `examClosed` existing (StartExam.cshtml :1254-1284) — TEMPLATE MIRROR VERBATIM:
- `var examClosed = false;` :1249 — dual-trigger guard, REUSE (jangan deklarasi ulang).
- `clearInterval(timerInterval)` — var timer existing.
- `clearInterval(saveInterval)` — var auto-save existing.
- `window.onbeforeunload = null;` — lepas warning navigasi.
- `new bootstrap.Modal(document.getElementById('...Modal')).show();`
- countdown 5 detik → `window.location.href = redirectTarget;` (pola :1274-1283).
- examClosed redirect ke `@Url.Action("Results","CMP")/SESSION_ID`; examRemoved → BEDA: `@Url.Action("Assessment","CMP")` (daftar ujian, BUKAN Results).

Modal `#examClosedModal` existing (:337-357) — POLA MIRROR:
- `data-bs-backdrop="static" data-bs-keyboard="false"` (non-dismissable).
- `style="z-index: 9999;"`, header berwarna, body + footer dengan tombol + countdown span.

Titik insert:
- Handler examRemoved → SETELAH handler examClosed (~:1284), SEBELUM handler sessionReset (:1287).
- Modal #examRemovedModal → SETELAH #examClosedModal/sessionResetModal (sekitar :359), dekat modal lain.

Payload examRemoved dari Plan 01: `{ reason: "..." }` (string alasan dari admin, bisa kosong/null).

Banner D-02: server set TempData["Error"]="Anda telah dikeluarkan dari ujian ini." pada redirect — verifikasi apakah /CMP/Assessment list sudah render TempData["Error"] (bila belum, redirect+modal sudah cukup; banner = best-effort, jangan tambah scope view Assessment di fase ini kecuali render TempData sudah ada).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modal #examRemovedModal + handler examRemoved (mirror examClosed)</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - `412-UI-SPEC.md` §9 (Force-Kick Worker examRemoved — markup modal verbatim + handler JS verbatim + copywriting)
    - `Views/CMP/StartExam.cshtml` :1249 (var examClosed) + :1254-1284 (handler examClosed mirror) + :1287 (sessionReset, batas insert handler)
    - `Views/CMP/StartExam.cshtml` :337-357 (modal examClosedModal pola) + ~:359 (sessionResetModal, lokasi insert modal)
    - `412-PATTERNS.md` §"StartExam.cshtml — examRemoved handler + modal" (mirror in-file)
  </read_first>
  <action>
    **(a) Modal `#examRemovedModal`** — sisip setelah `#sessionResetModal` (~:359), markup verbatim UI-SPEC §9:
    - `class="modal fade" id="examRemovedModal"` + `data-bs-backdrop="static" data-bs-keyboard="false"` + `style="z-index: 9999;"`.
    - `modal-dialog modal-dialog-centered` + `modal-content border-danger`.
    - header `bg-danger text-white` + ikon `bi-person-x` + judul "Dikeluarkan dari Ujian".
    - body: `<p class="mb-2 fw-semibold">Anda telah dikeluarkan dari ujian ini.</p>` (verbatim), `<p id="examRemovedReasonText" style="display:none;">Alasan: <em id="examRemovedReasonValue"></em></p>`, countdown "<strong id="removedCountdown">5</strong> detik...".
    - footer: tombol `#btnExamRemovedKembali` "Kembali ke Daftar Ujian" (`btn btn-danger fw-semibold`, ikon `bi-arrow-left`).

    **(b) Handler `examRemoved`** — sisip setelah handler examClosed (~:1284), SEBELUM sessionReset (:1287). Mirror struktur examClosed verbatim UI-SPEC §9:
    ```javascript
    window.assessmentHub.on('examRemoved', function(payload) {
        if (examClosed) return;            // reuse var existing :1249 (dual-trigger guard)
        examClosed = true;
        clearInterval(timerInterval);
        clearInterval(saveInterval);
        window.onbeforeunload = null;

        if (payload && payload.reason) {
            document.getElementById('examRemovedReasonValue').textContent = payload.reason;  // XSS-safe textContent
            document.getElementById('examRemovedReasonText').style.display = '';
        }

        var redirectTarget = '@Url.Action("Assessment", "CMP")';
        var modal = new bootstrap.Modal(document.getElementById('examRemovedModal'));
        modal.show();

        document.getElementById('btnExamRemovedKembali').onclick = function() {
            window.location.href = redirectTarget;
        };

        var countdown = 5;
        var countdownEl = document.getElementById('removedCountdown');
        var countdownInterval = setInterval(function() {
            countdown--;
            if (countdownEl) countdownEl.textContent = countdown;
            if (countdown <= 0) {
                clearInterval(countdownInterval);
                window.location.href = redirectTarget;
            }
        }, 1000);
    });
    ```
    KRITIS: reuse var `examClosed` `:1249` (JANGAN deklarasi ulang — compile/runtime konflik). reason via `.textContent` (XSS-safe, BUKAN innerHTML). redirect ke `@Url.Action("Assessment", "CMP")` (daftar, BUKAN Results — beda dari examClosed). `Url.Action` sub-path-aware (Razor helper, aman tanpa appUrl).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "examRemovedModal" Views/CMP/StartExam.cshtml` ≥ 2 (modal + handler)
    - `grep -c "on('examRemoved'" Views/CMP/StartExam.cshtml` == 1
    - `grep -c "Anda telah dikeluarkan dari ujian ini" Views/CMP/StartExam.cshtml` ≥ 1 (verbatim banner)
    - `grep -c 'Url.Action("Assessment", "CMP")' Views/CMP/StartExam.cshtml` ≥ 1 (redirect daftar, bukan Results)
    - `grep -c "examRemovedReasonValue" Views/CMP/StartExam.cshtml` ≥ 2 (modal id + textContent set)
    - `grep "examRemovedReasonValue" Views/CMP/StartExam.cshtml` pakai `.textContent` (XSS-safe, BUKAN innerHTML)
    - `grep -c "var examClosed = false" Views/CMP/StartExam.cshtml` == 1 (var tidak diduplikasi)
    - `grep -c 'data-bs-backdrop="static"' Views/CMP/StartExam.cshtml` ≥ 2 (examClosed + examRemoved modal non-dismissable)
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>Build 0 error; handler examRemoved mirror examClosed (reuse var guard, lock timer/save, clear onbeforeunload); modal non-dismissable dengan banner verbatim; redirect ke daftar Assessment; reason via textContent.</done>
</task>

<task type="auto">
  <name>Task 2: Build + runtime smoke @5277 (handler register, modal markup load)</name>
  <files>(verifikasi — tidak memodifikasi file produksi)</files>
  <read_first>
    - `412-RESEARCH.md` §"Validation Architecture" + §"Environment Availability" (port 5277, AD-off)
    - `reference_local_e2e_sql_env_fix` (env SQL bila login 500) + `reference_dev_credentials` (admin@pertamina.com)
  </read_first>
  <action>
    1. `dotnet build HcPortal.csproj` → 0 error.
    2. `dotnet run --urls http://localhost:5277` (AD-off `Authentication__UseActiveDirectory=false`); buka satu sesi StartExam aktif (atau halaman StartExam apa pun yang me-load script) sebagai worker/admin; verifikasi via DevTools/console: (i) NOL JS error saat load (handler `examRemoved` ter-register tanpa error — var examClosed tak konflik); (ii) `document.getElementById('examRemovedModal')` ada di DOM (markup ter-render). Hentikan app.
    3. CATATAN: simulasi force-kick end-to-end (admin hapus → worker kena modal) = Phase 413 Playwright multi-context. Smoke ini cukup buktikan handler+modal ter-load tanpa error (build+grep tak cukup untuk Razor dinamis, lesson Phase 354).
    4. Konfirmasi `git status Migrations/ Data/` kosong (migration=FALSE).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 error
    - Runtime smoke @5277: StartExam load NOL JS console error; `#examRemovedModal` hadir di DOM (dikonfirmasi manual dalam SUMMARY)
    - `git status Migrations/ Data/` kosong (migration=FALSE)
  </acceptance_criteria>
  <done>Build 0 error; smoke @5277 buktikan handler examRemoved + modal ter-load tanpa error; migration=FALSE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| examRemoved payload (server) → DOM (worker browser) | payload.reason dari admin di-tampilkan ke worker |
| SignalR push (server→worker) | event force-kick ke worker tunggal (Clients.User) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-412-14 | Tampering (XSS) | payload.reason ditampilkan di modal worker | mitigate | `.textContent` (Task 1b) — BUKAN innerHTML; reason di-encode di klien |
| T-412-15 | Spoofing | worker terima examRemoved palsu | accept | Server→client only; tak ada hub method client-invokable untuk examRemoved; guard re-entry server (409) tetap penjamin korektnes bila event hilang/dipalsukan |
| T-412-16 | DoS (force-kick salah target) | worker lain ter-kick | mitigate | Plan 01 kirim `Clients.User(targetUserId)` dgn userId dari DB (server-authoritative) + hanya bila wasInProgress; handler ber-guard `if (examClosed) return` (idempotent) |
| T-412-17 | Repudiation | worker klaim tak tahu dikeluarkan | mitigate | Audit RemovedBy/RemovalReason di server (411 PLIV-03); modal + banner + countdown beri notifikasi jelas |
</threat_model>

<verification>
- `dotnet build` 0 error (Razor compile)
- grep: examRemovedModal ≥2, on('examRemoved') ==1, "Anda telah dikeluarkan dari ujian ini" ≥1, Url.Action("Assessment","CMP") ≥1
- var examClosed tidak diduplikasi (==1)
- reason via textContent (XSS-safe)
- Runtime smoke @5277: NOL JS error, modal di DOM
- migration=FALSE
</verification>

<success_criteria>
- Handler examRemoved mengunci UI ujian (timer/save stop, onbeforeunload clear) + modal non-dismissable + redirect ke daftar Assessment (PRMV-02 sisi-worker; D-02)
- Banner verbatim "Anda telah dikeluarkan dari ujian ini."
- reason XSS-safe (textContent); var examClosed reuse (tak konflik)
- Build 0 error; runtime smoke @5277 OK; migration=FALSE
</success_criteria>

<output>
After completion, create `.planning/phases/412-live-monitoring-ui-signalr/412-03-SUMMARY.md`
</output>
