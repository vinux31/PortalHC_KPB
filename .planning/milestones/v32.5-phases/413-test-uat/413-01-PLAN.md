---
phase: 413-test-uat
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/FlexibleParticipantLifecycleTests.cs
autonomous: true
requirements: [PART-06, PART-07, PRMV-01, PRMV-03, PRMV-04, PRMV-05]
migration: false
tags: [assessment, participant, test, integration, lifecycle, de-tautology]
user_setup: []

must_haves:
  truths:
    - "Test menjalankan SELURUH siklus lintas-fase dalam 1 alur DB SQLEXPRESS nyata: AddParticipantsLive ASLI → flip InProgress → RemoveParticipantLive ASLI (soft) → IsParticipantRemoved==true → RestoreParticipantLive ASLI → IsParticipantRemoved==false (de-tautology 999.12)"
    - "Guard re-entry lintas-fase dibuktikan: CMPController.IsParticipantRemoved(reload) helper PRODUKSI ASLI dipanggil di tengah lifecycle (true saat soft-removed, false setelah restore) — bukan replica predikat"
    - "Lifecycle Pre/Post: add buat pasangan (LinkedSessionId cross-set) → remove soft → kedua sesi RemovedAt!=null → restore → kedua sesi RemovedAt==null"
    - "Hard-delete lifecycle: add not-started bersih → remove (hard via mini-DI) → baris + UPA HILANG (AnyAsync==false), restore tak mungkin (404/400)"
    - "Tidak ada replica predikat (NO SessionHasDataAsync/DeriveReadyStatus/IsParticipantRemoved tiruan; NO ExecuteAsync cascade langsung) — drive action ASLI, assert kolom DB nyata"
    - "Full suite tetap hijau tak regresi (baseline 602)"
  artifacts:
    - path: "HcPortal.Tests/FlexibleParticipantLifecycleTests.cs"
      provides: "xUnit Integration lifecycle lintas-fase (add→start→soft-remove→guard→restore + Pre/Post pair + hard-delete) drive action ASLI atas SQLEXPRESS disposable"
      contains: "class FlexibleParticipantLifecycleTests"
      min_lines: 200
  key_links:
    - from: "FlexibleParticipantLifecycleTests"
      to: "AssessmentAdminController.AddParticipantsLive/RemoveParticipantLive/RestoreParticipantLive"
      via: "SQLEXPRESS write-path invocation (MakeLiveController + mini-DI cascade)"
      pattern: "AddParticipantsLive\\(|RemoveParticipantLive\\(|RestoreParticipantLive\\("
    - from: "FlexibleParticipantLifecycleTests (guard assert)"
      to: "CMPController.IsParticipantRemoved"
      via: "panggil helper produksi ASLI atas sesi reload (bukan replica)"
      pattern: "IsParticipantRemoved\\("
---

<objective>
Buat 1 file xUnit Integration BARU `HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` yang membuktikan **integrasi lintas-fase** seluruh fitur add/remove/restore peserta dalam **satu alur DB nyata** — celah yang BELUM ter-cover test per-fase (410 menguji add, 411 menguji remove/restore terpisah, 409 menguji guard terpisah; tidak ada satu test pun yang menjalankan `add → start → soft-remove → guard-block → restore → aktif lagi` sebagai satu siklus utuh).

Tiga lifecycle:
1. **Standard lifecycle (de-tautology penuh):** `AddParticipantsLive` ASLI (ready-status + UPA) → flip InProgress via ctx → `RemoveParticipantLive` ASLI (soft, reason) → `CMPController.IsParticipantRemoved(reload)` PRODUKSI ASLI == true → `RestoreParticipantLive` ASLI → `IsParticipantRemoved` == false. Membuktikan guard re-entry (PRMV-03) terpasang benar pada sesi yang ditambah+dihapus lewat endpoint nyata.
2. **Pre/Post lifecycle (PART-07 + PRMV-05):** add Pre/Post → pasangan (LinkedSessionId cross-set) → remove soft → kedua sesi removed → restore → kedua sesi aktif lagi.
3. **Hard-delete lifecycle (PRMV-01):** add not-started bersih → remove hard via mini-DI → baris + UPA HILANG → restore tak mungkin (404/400, hard tak reversibel).

Purpose: kunci integrasi lintas-fase (regression value tinggi — membuktikan guard `IsParticipantRemoved` benar di tengah lifecycle nyata, bukan hanya pada sesi yang di-seed langsung). REUSE infra existing 410/411 (fixture + stub + mini-DI) — nol pola baru.
Output: 1 file test baru. migration=FALSE. JANGAN sentuh kode produksi.

> ⚠️ De-tautology 999.12 WAJIB: setiap test MENJALANKAN action `AssessmentAdminController` ASLI + memanggil helper produksi `CMPController.IsParticipantRemoved` ASLI + meng-assert kolom DB NYATA. JANGAN tulis ulang `IsParticipantRemoved`/`SessionHasDataAsync`/`DeriveReadyStatus`; JANGAN panggil `RecordCascadeDeleteService.ExecuteAsync` langsung (drive lewat `RemoveParticipantLive`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/413-test-uat/413-CONTEXT.md
@.planning/phases/413-test-uat/413-RESEARCH.md

<interfaces>
<!-- REUSE infra 410/411 — Read-verified. JANGAN duplikat fixture/stub; copy verbatim ke file baru. -->

Fixture (REUSE via [IClassFixture]) — HcPortal.Tests/FlexibleParticipantAddTests.cs:20-53:
  FlexibleParticipantAddFixture : IAsyncLifetime
    .Options (DbContextOptions<ApplicationDbContext> → SQLEXPRESS HcPortalDB_Test_{guid})
    InitializeAsync → MigrateAsync (HcPortalDB_Dev TIDAK disentuh); DisposeAsync → EnsureDeletedAsync

Stub + helper write-path (COPY verbatim dari HcPortal.Tests/FlexibleParticipantAddLiveTests.cs):
  :222 StubUserManager : UserManager<ApplicationUser> (override GetUserAsync → actor)
  :232 StubUserStore : IUserStore<ApplicationUser> (no-op)
  :248 NoopNotificationService : INotificationService (7 method no-op)
  :260 MakeLiveController(ctx, actor) → AssessmentAdminController ASLI (ctor 12-args; ControllerContext.HttpContext=DefaultHttpContext + ActionDescriptor.ActionName) — set ActionName per action yang dipanggil
  :288 SeedUserAsync(ctx, fullName?, nip?) → userId
  :300 SeedRepSessionAsync(ctx, userId, title, category, schedule, status, examWindowCloseDate?, assessmentType?, linkedGroupId?, generateCertificate?, durationMinutes?) → sessionId

Mini-DI hard-delete (COPY verbatim dari HcPortal.Tests/FlexibleParticipantRemoveTests.cs):
  StubWebHostEnvironment : IWebHostEnvironment (WebRootPath = Path.GetTempPath())
  BuildCascadeServiceProvider(ctx) → IServiceProvider (ServiceCollection: ctx + AuditLogService + INotificationService(Noop) + NullLogger<ProtonCompletionService> + NullLogger<RecordCascadeDeleteService> + IWebHostEnvironment(stub) + AddScoped ProtonCompletionService + AddScoped RecordCascadeDeleteService)
  MakeLiveControllerWithCascade(ctx, actor) = MakeLiveController + set HttpContext.RequestServices = BuildCascadeServiceProvider(ctx)

Helper produksi yang DIPANGGIL (guard lintas-fase, JANGAN replica):
  CMPController.IsParticipantRemoved(AssessmentSession session) => session.RemovedAt != null
    [VERIFIED ParticipantRemovalGuardTests.cs:284-315 — helper produksi ASLI di-test de-tautologis di 409;
     dipanggil inline guard StartExam/SubmitExam]
  Cara akses: instantiate CMPController minimal (atau panggil static/instance sesuai signature di Controllers/CMPController.cs)
  ATAU assert langsung `session.RemovedAt != null` HANYA bila helper tak dapat diakses — TAPI prioritas panggil helper produksi ASLI.
  > Read Controllers/CMPController.cs untuk signature IsParticipantRemoved (public/internal? static?) — tentukan cara panggil ASLI.

Assert target:
  ready-status: Status ∈ {AssessmentConstants.AssessmentStatus.Open ("Open"), .Upcoming ("Upcoming")}; StartedAt/CompletedAt/RemovedAt null.
  soft-remove: RemovedAt != null, RemovedBy == actorId, RemovalReason == "<reason>"; Score/Status UNCHANGED.
  restore: RemovedAt == null && RemovedBy == null && RemovalReason == null.
  hard-delete: AssessmentSessions.AnyAsync(s => s.Id == id) == false (+ UserPackageAssignments.AnyAsync == false).
  Pre/Post: LinkedSessionId cross-set; remove soft → kedua RemovedAt!=null; restore → kedua RemovedAt==null.
</interfaces>
</context>

<feature>
  <name>FlexibleParticipantLifecycleTests — lifecycle lintas-fase de-tautologis untuk Phase 413</name>
  <files>HcPortal.Tests/FlexibleParticipantLifecycleTests.cs</files>
  <behavior>
**Standard lifecycle (drive action ASLI, assert kolom DB nyata):**
- L1 `Lifecycle_Add_Start_SoftRemove_GuardBlocks_Restore_Active` (PART-06 + PRMV-01 + PRMV-03 + PRMV-04):
  1. Seed rep sesi batch standard (schedule lampau, window +1d) + AssessmentPackage agar UPA eager. Seed user baru + actor.
  2. `AddParticipantsLive(repId, [newUserId])` ASLI → assert sesi baru Status=="Open"/"Upcoming", RemovedAt null, UserPackageAssignment tercipta (AssessmentSessionId==sesi baru).
  3. Flip sesi baru ke InProgress via ctx (`StartedAt = UtcNow`, `Status="InProgress"`, `CompletedAt=null`) + SaveChanges — simulasi peserta mulai mengerjakan.
  4. `RemoveParticipantLive(newSessionId, "tidak hadir")` ASLI → mode=="soft"; reload → RemovedAt!=null, RemovalReason=="tidak hadir".
  5. **GUARD lintas-fase:** reload sesi → panggil `CMPController.IsParticipantRemoved(session)` PRODUKSI ASLI → assert == true (peserta dihapus tak bisa lanjut/submit — PRMV-03).
  6. `RestoreParticipantLive(newSessionId)` ASLI → reload → RemovedAt==null, RemovedBy==null, RemovalReason==null; `IsParticipantRemoved(session)` == false (aktif lagi).

**Pre/Post lifecycle (PART-07 + PRMV-05):**
- L2 `Lifecycle_PrePost_Add_SoftRemoveBoth_RestoreBoth`:
  1. Seed rep Pre/Post (AssessmentType "PreTest"/"PostTest", LinkedGroupId set) + user baru + actor.
  2. `AddParticipantsLive(preRepId, [newUserId])` ASLI → 2 sesi baru (Pre+Post) untuk user, LinkedSessionId cross-set, keduanya ready-status.
  3. Flip Pre ke InProgress (berdata) via ctx.
  4. `RemoveParticipantLive(newPreSessionId, "alasan")` → mode=="soft"; **kedua** sesi (Pre+Post user) RemovedAt!=null; JSON linkedSessionId == newPostSessionId.
  5. Seed peserta LAIN di batch sama → assert peserta lain TIDAK ikut removed (Pitfall 1 — bukan seluruh batch).
  6. `RestoreParticipantLive(newPreSessionId)` → kedua sesi RemovedAt==null lagi.

**Hard-delete lifecycle (PRMV-01, mini-DI):**
- L3 `Lifecycle_Add_NotStarted_HardRemove_RowAndUpaGone` (gunakan MakeLiveControllerWithCascade):
  1. Seed rep + user baru. `AddParticipantsLive` ASLI → sesi baru bersih (StartedAt null, 0 response) + UPA eager.
  2. `RemoveParticipantLive(newSessionId, null)` via WithCascade → mode=="hard".
  3. Reload → `AssessmentSessions.AnyAsync(s=>s.Id==newSessionId) == false` DAN `UserPackageAssignments.AnyAsync(a=>a.AssessmentSessionId==newSessionId) == false` (D-01: UPA bukan "data", hard tetap cascade bersih).
  4. `RestoreParticipantLive(newSessionId)` → `NotFoundObjectResult`/`BadRequestObjectResult` (hard tak reversibel — baris sudah hilang).
  </behavior>
  <read_first>
    - HcPortal.Tests/FlexibleParticipantAddLiveTests.cs (full — copy StubUserManager :222, StubUserStore :232, NoopNotificationService :248, MakeLiveController :260, SeedUserAsync :288, SeedRepSessionAsync :300; pola write-path Integration class :213+)
    - HcPortal.Tests/FlexibleParticipantRemoveTests.cs (full — copy StubWebHostEnvironment, BuildCascadeServiceProvider, MakeLiveControllerWithCascade; pola hard-delete C1/C2)
    - HcPortal.Tests/FlexibleParticipantAddTests.cs:20-53 (FlexibleParticipantAddFixture — REUSE via IClassFixture, JANGAN duplikat)
    - HcPortal.Tests/ParticipantRemovalGuardTests.cs:284-315 (cara panggil CMPController.IsParticipantRemoved PRODUKSI ASLI de-tautologis — TIRU akses helper ini, JANGAN replica)
    - Controllers/CMPController.cs (signature IsParticipantRemoved — public/internal/static? untuk panggil ASLI) + Controllers/AssessmentAdminController.cs (signature AddParticipantsLive/RemoveParticipantLive/RestoreParticipantLive)
    - .planning/phases/413-test-uat/413-RESEARCH.md §"Pattern 4: xUnit Lifecycle Lintas-Fase" (blueprint + de-tautology)
  </read_first>
  <implementation>
Buat file BARU `HcPortal.Tests/FlexibleParticipantLifecycleTests.cs`. Header comment jelaskan de-tautology lintas-fase (lesson 999.12). namespace `HcPortal.Tests`. migration=FALSE; DB disposable `HcPortalDB_Test_{guid}` (HcPortalDB_Dev TIDAK disentuh).

**Kelas:** `[Trait("Category","Integration")] public class FlexibleParticipantLifecycleTests : IClassFixture<FlexibleParticipantAddFixture>`.

**Reuse helper:** Copy verbatim ke file ini (atau jadikan `internal` di file sumber bila lebih bersih — diskresi executor, TAPI default = copy untuk isolasi): `StubUserManager`, `StubUserStore`, `NoopNotificationService`, `MakeLiveController`, `SeedUserAsync`, `SeedRepSessionAsync` (dari AddLive) + `StubWebHostEnvironment`, `BuildCascadeServiceProvider`, `MakeLiveControllerWithCascade` (dari Remove). `NewCtx()` helper bikin `new ApplicationDbContext(_fixture.Options)`.

**Panggil IsParticipantRemoved ASLI:** Baca `Controllers/CMPController.cs` lebih dulu untuk signature. Bila `public`/`internal static bool IsParticipantRemoved(AssessmentSession)` → panggil langsung `CMPController.IsParticipantRemoved(session)`. Bila instance-method non-static → instantiate `CMPController` minimal (pola `ParticipantRemovalGuardTests.cs` — service null! untuk yang tak dipakai). **TUJUAN: assert guard via helper PRODUKSI, bukan menulis `session.RemovedAt != null` di test** (itu replica). Bila helper benar-benar tak dapat diakses (private instance + butuh full DI), dokumentasikan di SUMMARY + fallback assert `session.RemovedAt != null` DENGAN komentar referensi ke guard inline produksi yang memakainya.

**Flip InProgress via ctx (langkah lifecycle, BUKAN replica):** ambil sesi via `ctx.AssessmentSessions.FindAsync(id)`, set `StartedAt=DateTime.UtcNow`, `Status="InProgress"`, `CompletedAt=null`, `await ctx.SaveChangesAsync()`. Ini state-transition test-driven, sah (bukan meniru logika produksi — produksi flip via StartExam yang butuh HTTP, di luar scope unit).

**Seed AssessmentPackage untuk UPA eager (L1/L3):** REUSE pola seed package dari FlexibleParticipantAddLiveTests T6 (AssessmentPackage + PackageQuestion + PackageOption agar `packages.Any()` true → CreateEagerAssignmentsAsync tulis UPA). Bila pola seed package ada sebagai helper, REUSE; bila inline di T6, copy.

**De-tautology assert (WAJIB):** Hard: `AnyAsync == false`. Soft/restore/ready: kolom NYATA via reload ctx baru. Guard: helper produksi `IsParticipantRemoved`. JANGAN replica `SessionHasDataAsync`/`DeriveReadyStatus`/`WindowAllowsAddition`. JANGAN panggil `RecordCascadeDeleteService.ExecuteAsync` langsung.
  </implementation>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~FlexibleParticipantLifecycle" 2>&1 | grep -E "Passed!|Failed!|error|Passed:|Failed:" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` exists
    - `grep -c "\[Fact\]" HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` ≥ 3 (L1, L2, L3)
    - `grep -n "IClassFixture<FlexibleParticipantAddFixture>" HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` → 1 (REUSE fixture SQLEXPRESS)
    - `grep -cE "AddParticipantsLive\(|RemoveParticipantLive\(|RestoreParticipantLive\(" HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` ≥ 5 (drive ketiga action ASLI dalam lifecycle)
    - `grep -n "IsParticipantRemoved" HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` → ≥1 (guard lintas-fase via helper produksi; bila fallback dipakai, komentar referensi guard wajib hadir)
    - `grep -n "AnyAsync" HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` → ≥1 (hard-delete row+UPA gone — L3)
    - `grep -n "LinkedSessionId" HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` → ≥1 (Pre/Post pair — L2)
    - `grep -c "SessionHasDataAsync\|WindowAllowsAddition\|ExecuteAsync" HcPortal.Tests/FlexibleParticipantLifecycleTests.cs` == 0 (NO replica/bypass; hanya boleh di komentar)
    - `dotnet test --filter "FullyQualifiedName~FlexibleParticipantLifecycle"` → semua [Fact] Passed (SQLEXPRESS write-path BENAR jalan, bukan skip)
  </acceptance_criteria>
  <done>FlexibleParticipantLifecycleTests menjalankan add→start→soft-remove→guard(IsParticipantRemoved ASLI)→restore + Pre/Post pair soft/restore + hard-delete row+UPA gone, semua via action `AssessmentAdminController` ASLI atas SQLEXPRESS disposable, tanpa replica predikat; tests hijau; full suite tak regresi.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → AssessmentAdminController/CMPController (in-process) | Test memanggil action + helper produksi langsung; tak ada boundary jaringan |
| Test → SQLEXPRESS disposable HcPortalDB_Test_{guid} | DB nyata terisolasi; HcPortalDB_Dev TIDAK disentuh (CLAUDE.md Seed Workflow) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-413-01 | Tampering | Test menyentuh HcPortalDB_Dev (korup data dev) | mitigate | REUSE FlexibleParticipantAddFixture → `HcPortalDB_Test_{Guid}` disposable + `EnsureDeletedAsync` di DisposeAsync; JANGAN connect HcPortalDB_Dev |
| T-413-02 | Repudiation | Test tautologis (replica predikat) → false-confidence regression (lesson 999.12) | mitigate | Drive action ASLI + panggil `IsParticipantRemoved` PRODUKSI; acceptance melarang `SessionHasDataAsync`/`WindowAllowsAddition`/`ExecuteAsync` |
| T-413-03 | Tampering (Elevation) | Guard re-entry tak terpasang pada sesi yang ditambah lewat endpoint → peserta dihapus bisa resubmit | mitigate | L1 langkah 5 panggil `IsParticipantRemoved(reload)` PRODUKSI atas sesi hasil AddParticipantsLive+RemoveParticipantLive nyata — membuktikan PRMV-03 terpasang lintas-fase |
| T-413-04 | Information Disclosure | Test bocorkan kredensial SQLEXPRESS | accept | Connection string Integrated Security (Windows auth lokal); nol secret literal — sama fixture existing |

> Plan 01 = test-only; nol surface produksi baru. Threat utama = isolasi DB + anti-tautology + guard lintas-fase (semua mitigated).
</threat_model>

<verification>
1. `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → 0 error.
2. `dotnet test HcPortal.Tests --filter "FullyQualifiedName~FlexibleParticipantLifecycle"` → tests baru hijau (SQLEXPRESS write-path BENAR jalan — count Integration executed > 0, bukan skip).
3. `grep -c "SessionHasDataAsync\|WindowAllowsAddition\|ExecuteAsync"` di luar komentar == 0 (de-tautology).
4. `git status Migrations/ Data/` kosong → migration=FALSE.
5. Full suite no-regression dikonfirmasi di Plan 413-03 (regresi gate). Plan ini cukup verifikasi subset + build.
</verification>

<success_criteria>
- File `FlexibleParticipantLifecycleTests.cs` ada; L1 (standard add→start→soft→guard→restore) + L2 (Pre/Post pair) + L3 (hard-delete row+UPA gone) hijau.
- Drive `AddParticipantsLive`/`RemoveParticipantLive`/`RestoreParticipantLive` ASLI + panggil `IsParticipantRemoved` PRODUKSI; NO replica predikat (anti 999.12).
- SQLEXPRESS write-path NYATA berjalan; migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/413-test-uat/413-01-SUMMARY.md`. Catat: cara panggil `IsParticipantRemoved` (helper produksi langsung vs instantiate CMPController vs fallback + alasan); jumlah [Fact] + status filter run; konfirmasi NO replica predikat; konfirmasi SQLEXPRESS executed (bukan skip).
</output>
