---
phase: 413-test-uat
plan: 03
type: execute
wave: 2
depends_on: [413-01, 413-02]
files_modified:
  - .planning/phases/413-test-uat/413-REGRESSION.md
autonomous: true
requirements: [PART-05, PART-06, PART-07, PRMV-01, PRMV-02, PRMV-03, PRMV-04, PRMV-05, PLIV-01, PLIV-02, PLIV-03]
migration: false
tags: [assessment, participant, regression, gate, milestone-close]
user_setup: []

must_haves:
  truths:
    - "Full `dotnet test` suite hijau (baseline 602 + test baru 413-01 lifecycle) — 0 failed"
    - "Tak ada regresi guard penambahan-peserta Phase 391 (DeriveReadyStatus) + tech-debt Phase 398.1"
    - "Tak ada regresi guard re-entry Phase 409 (ParticipantRemovalGuard StartExam/SubmitExam/JoinBatch block removed)"
    - "Tak ada regresi 410 (FlexibleParticipantAddLive) + 411 (FlexibleParticipantRemove) + 412 (MonitoringRemovedPanel)"
    - "`dotnet build` 0 error + `dotnet run` @5277 boot OK"
    - "413-REGRESSION.md mendokumentasikan: angka suite aktual, mapping 11 REQ → bukti (xUnit + e2e), status ships-last/push-readiness (notify IT migration=TRUE Phase 409 hash 01cd7dd0), git status migration=FALSE"
  artifacts:
    - path: ".planning/phases/413-test-uat/413-REGRESSION.md"
      provides: "Bukti regression full-suite + mapping 11 REQ end-to-end + push-readiness gate (ships terakhir milestone v32.5)"
      contains: "602"
  key_links:
    - from: "413-REGRESSION.md"
      to: "dotnet test full suite + e2e 7 sinyal"
      via: "rangkum hasil 413-01 (xUnit lifecycle) + 413-02 (e2e) + suite penuh"
      pattern: "dotnet test"
---

<objective>
Gate regression + penutup milestone v32.5: jalankan **full `dotnet test`** (xUnit suite penuh, baseline 602 + test baru 413-01) hijau tanpa regresi pada guard yang sudah ada di main (Phase 391 `DeriveReadyStatus`, tech-debt 398.1, guard re-entry 409, plus 410/411/412), verifikasi `dotnet build` 0 error + `dotnet run` @5277 boot, dan dokumentasikan bukti end-to-end (mapping 11 REQ → xUnit + e2e) serta status **ships-last / push-readiness** di `413-REGRESSION.md`.

Purpose: kunci D-04 CONTEXT (bukti regresi: full suite hijau + jalur guard re-entry 409 + guard 391/398.1 tetap lulus) + tutup 11 REQ end-to-end (Success Criteria 4 ROADMAP Phase 413). Konsumsi hasil 413-01 (lifecycle) + 413-02 (e2e 7 sinyal).
Output: 1 file dokumentasi regression. migration=FALSE. NOL kode produksi baru.

> ⚠️ This phase ships last. Setelah semua hijau → verifikasi lokal lengkap → **1 push (deploy bundle v32.5)** + notify IT migration=TRUE Phase 409 (`AddParticipantRemovalColumns` hash `01cd7dd0`). **JANGAN push di plan ini** — push = aksi koordinasi IT terpisah (CLAUDE.md Develop Workflow). Plan ini hanya MENCATAT kesiapan push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/413-test-uat/413-CONTEXT.md
@.planning/phases/413-test-uat/413-RESEARCH.md
@.planning/phases/412-live-monitoring-ui-signalr/412-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Subset filter regression — VERIFIED dari fase 409-412. -->

Suite regression yang HARUS tetap hijau (filter cek per-grup):
  Phase 391 guard add-peserta:    `dotnet test --filter "FullyQualifiedName~FlexibleParticipantAdd"` (DeriveReadyStatus + window/idempotent)
  Phase 398.1 tech-debt:          (terserap di full suite; tak ada filter khusus — angka total tak turun)
  Phase 409 guard re-entry:       `dotnet test --filter "FullyQualifiedName~ParticipantRemovalGuard"` (StartExam/SubmitExam/JoinBatch block removed) + `~ParticipantRemovalExclude`
  Phase 410 add backend:          `dotnet test --filter "FullyQualifiedName~FlexibleParticipantAddLive"`
  Phase 411 remove/restore:       `dotnet test --filter "FullyQualifiedName~FlexibleParticipantRemove"`
  Phase 412 panel:                `dotnet test --filter "FullyQualifiedName~MonitoringRemovedPanel"` + `~MonitoringUserStatus`
  Phase 413 lifecycle (BARU):     `dotnet test --filter "FullyQualifiedName~FlexibleParticipantLifecycle"`
  Full:                           `dotnet test`  (baseline 602/602 per 412-VALIDATION; 413-01 menambah ≥3 → harap ≥605)

11 REQ → bukti (untuk mapping di 413-REGRESSION):
  PART-05 → e2e "add live" (413-02)
  PART-06 → xUnit FlexibleParticipantAddLive (410) + lifecycle L1 (413-01)
  PART-07 → xUnit FlexibleParticipantAddLive Pre/Post (410) + lifecycle L2 (413-01)
  PRMV-01 → xUnit FlexibleParticipantRemove soft/hard/idempotent (411) + lifecycle L1/L3 (413-01)
  PRMV-02 → e2e "hapus keras" + "force kick worker" (413-02)
  PRMV-03 → xUnit ParticipantRemovalGuard (409) + lifecycle L1 guard IsParticipantRemoved (413-01) + e2e force-kick (413-02)
  PRMV-04 → xUnit Restore (411) + lifecycle L1/L2 restore (413-01) + e2e "restore" (413-02)
  PRMV-05 → xUnit Pre/Post pair (411) + lifecycle L2 (413-01)
  PLIV-01 → xUnit MonitoringRemovedPanel (412) + ParticipantRemovalExclude (409) + e2e "panel removed"+"count exclude" (413-02)
  PLIV-02 → e2e "multi observer" (413-02) + broadcast regression (412)
  PLIV-03 → xUnit FlexibleParticipantRemove audit+RBAC (411)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jalankan full regression suite + per-grup guard + build + boot; catat angka aktual</name>
  <files>.planning/phases/413-test-uat/413-REGRESSION.md</files>
  <read_first>
    - .planning/phases/413-test-uat/413-RESEARCH.md §"Validation Architecture" (full suite command + baseline 602 + sampling rate) + §"Assumptions Log" A1 (jalankan suite untuk angka aktual)
    - .planning/phases/412-live-monitoring-ui-signalr/412-VALIDATION.md §"Suite Summary" (baseline 602/602)
    - .planning/phases/413-test-uat/413-01-SUMMARY.md (jumlah [Fact] lifecycle baru — untuk hitung total ekspektasi) + 413-02-SUMMARY.md (hasil 7 sinyal e2e)
  </read_first>
  <action>
    Jalankan dan catat angka AKTUAL (bukan asumsi):
    1. `dotnet build` (HcPortal.csproj + HcPortal.Tests) → harus 0 error. Catat.
    2. **Full suite:** `dotnet test` → catat total Passed/Failed/Skipped (target 0 Failed; total >= 602 baseline + test 413-01). Bila SQLEXPRESS hidup, Integration trait IKUT jalan (write-path BENAR run, bukan skip — catat jumlah Integration executed).
    3. **Per-grup guard (no-regression):** jalankan filter berikut, catat hasil masing-masing:
       - `~FlexibleParticipantAdd` (Phase 391 + 410 add) -> hijau
       - `~ParticipantRemovalGuard` + `~ParticipantRemovalExclude` (Phase 409 guard re-entry + exclude) -> hijau
       - `~FlexibleParticipantRemove` (Phase 411) -> hijau
       - `~MonitoringRemovedPanel` + `~MonitoringUserStatus` (Phase 412) -> hijau
       - `~FlexibleParticipantLifecycle` (Phase 413-01 baru) -> hijau
    4. `Authentication__UseActiveDirectory=false dotnet run --project HcPortal.csproj --urls http://localhost:5277` → boot "Now listening on: http://localhost:5277" (GET / atau /Home 200) → stop. Catat boot OK.
    5. `git status Migrations/ Data/` → kosong (migration=FALSE). Catat.

    Bila ADA regresi (test yang sebelumnya hijau jadi merah): JANGAN tutup-tutupi. Diagnosa: apakah test baru 413-01 menyebabkan (fixture share/urutan) atau regresi nyata kode? Catat di REGRESSION.md sebagai BLOCKER + (bila perlu) eskalasi sebagai gap. Test/UAT phase tak menambah kode produksi — bila regresi nyata muncul, itu temuan yang harus ditangani sebelum push.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && dotnet test 2>&1 | grep -E "Passed!|Failed!|Passed:|Failed:|error" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` full suite → `Failed: 0` (0 regresi)
    - `dotnet test --filter "FullyQualifiedName~ParticipantRemovalGuard"` → Passed (guard re-entry 409 utuh)
    - `dotnet test --filter "FullyQualifiedName~FlexibleParticipantAdd"` → Passed (guard 391 + add 410 utuh)
    - `dotnet test --filter "FullyQualifiedName~FlexibleParticipantLifecycle"` → Passed (413-01 baru, terhitung di total)
    - Total suite >= 602 (baseline) + jumlah [Fact] 413-01 (tak ada test yang HILANG)
    - `dotnet build` → 0 error
    - `git status Migrations/ Data/` → kosong (migration=FALSE)
  </acceptance_criteria>
  <done>Full suite + per-grup guard hijau 0-failed; build 0 error; boot @5277 OK; migration=FALSE; angka aktual tercatat (akan dirangkum Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Tulis 413-REGRESSION.md — mapping 11 REQ -> bukti + suite summary + push-readiness gate</name>
  <files>.planning/phases/413-test-uat/413-REGRESSION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (11 REQ + traceability — sumber mapping)
    - <interfaces> di plan ini (11 REQ -> bukti map; subset filter)
    - .planning/phases/413-test-uat/413-01-SUMMARY.md + 413-02-SUMMARY.md (hasil aktual lifecycle + 7 sinyal e2e)
    - .planning/STATE.md §"Accumulated Context (carry)" (notify IT migration=TRUE Phase 409 hash 01cd7dd0; v32.2+v32.5 push bundle pending)
  </read_first>
  <action>
    Buat `.planning/phases/413-test-uat/413-REGRESSION.md` (prose Bahasa Indonesia). Bagian:

    **§1 Suite Summary** — tabel hasil AKTUAL Task 1: full suite (Passed/Failed/Skipped), per-grup guard (391/409/410/411/412/413-01) + status, Integration executed count, build 0 error, boot @5277 OK. Tegaskan `Failed: 0` + tak ada test hilang vs baseline 602.

    **§2 Mapping 11 REQ -> Bukti End-to-End** — tabel `REQ | xUnit (fase) | e2e (413-02 sinyal) | Status`. Pakai map di <interfaces>. SEMUA 11 REQ harus `COVERED` (tak ada gap). PART-05/PRMV-02/PLIV-02 = e2e-driven; sisanya xUnit + (sebagian) e2e.

    **§3 Regression Guards** — konfirmasi eksplisit D-04 CONTEXT: (a) guard re-entry 409 (StartExam/SubmitExam/JoinBatch block removed) tetap hijau; (b) guard penambahan-peserta Phase 391 (DeriveReadyStatus) + tech-debt 398.1 tetap hijau; (c) 410/411/412 tak regresi.

    **§4 e2e 7 Sinyal (413-02)** — ringkas hasil checkpoint 413-02: add live / modal keras / force-kick 2-ctx / panel removed / restore / count exclude / multi-observer -> PASS; DB ter-restore + SEED_JOURNAL cleaned.

    **§5 Push-Readiness Gate (ships last)** — checklist kesiapan 1 push deploy bundle v32.5 (JANGAN eksekusi push di sini):
       - [ ] Full suite hijau (§1) + e2e 7 sinyal PASS (§4) + build 0 error + boot @5277 OK.
       - [ ] migration=FALSE Phase 413 (git status Migrations/Data kosong).
       - [ ] Carry notify IT: Phase 409 = migration=TRUE `AddParticipantRemovalColumns` hash `01cd7dd0` (3 kolom nullable additif); Phase 410-413 = migration=FALSE. Bundle v32.2 (0 migration baru) + v32.5 -> 1 push origin/main saat koordinasi IT.
       - [ ] DB lokal bersih (SEED_JOURNAL 413 cleaned; tak ada HcPortalDB_Test_*).
       - [ ] JANGAN push di plan — push + notify IT = aksi terpisah pasca-approval (CLAUDE.md Develop Workflow step 4-5).

    **§6 Deferred / Carry** — IN-02 (411 EditAssessment exclude soft-removed) = backlog kecuali UAT 413-02 menemukan inkonsistensi (catat hasil); 3 Info 412 non-blocking; temuan bug produk e2e (bila ada dari 413-02) -> kandidat fase terpisah.

    Footer: phase 413-test-uat, tanggal, migration=FALSE, NOT pushed.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -cE "PART-05|PART-06|PART-07|PRMV-01|PRMV-02|PRMV-03|PRMV-04|PRMV-05|PLIV-01|PLIV-02|PLIV-03" .planning/phases/413-test-uat/413-REGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/413-test-uat/413-REGRESSION.md` exists
    - `grep -cE "PART-05|PART-06|PART-07|PRMV-01|PRMV-02|PRMV-03|PRMV-04|PRMV-05|PLIV-01|PLIV-02|PLIV-03" 413-REGRESSION.md` >= 11 (semua REQ ter-map)
    - `grep -n "602" .planning/phases/413-test-uat/413-REGRESSION.md` → ada (baseline disebut + angka aktual)
    - `grep -n "01cd7dd0" .planning/phases/413-test-uat/413-REGRESSION.md` → 1 (carry notify IT migration Phase 409)
    - `grep -niE "JANGAN push|NOT pushed|tidak push" .planning/phases/413-test-uat/413-REGRESSION.md` → ada (ships-last, push = aksi terpisah)
    - `grep -niE "Failed: 0|0 failed|0 regresi" .planning/phases/413-test-uat/413-REGRESSION.md` → ada (no-regression eksplisit)
  </acceptance_criteria>
  <done>413-REGRESSION.md ada dengan §1 suite summary aktual + §2 mapping 11 REQ COVERED + §3 regression guards (391/398.1/409/410/411/412) + §4 e2e 7 sinyal + §5 push-readiness gate (carry IT migration 01cd7dd0, JANGAN push) + §6 deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness (in-process) → suite penuh | `dotnet test` jalankan semua test (InMemory + SQLEXPRESS Integration) lokal; tak ada boundary jaringan |
| Dokumentasi regression → keputusan push | 413-REGRESSION.md adalah gate sebelum push deploy bundle (push = aksi manusia terpisah) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-413-R1 | Repudiation | Regression "hijau" diklaim tanpa run nyata (false-green) | mitigate | Task 1 verify = `dotnet test` aktual (grep Failed/Passed); acceptance `Failed: 0` + per-grup guard filter; angka aktual dicatat, bukan asumsi (RESEARCH A1) |
| T-413-R2 | Tampering | Push prematur sebelum gate lengkap (migration TRUE 409 belum di-flag IT) | mitigate | §5 push-readiness checklist + acceptance `grep JANGAN push` + carry `01cd7dd0` migration=TRUE wajib hadir; push = aksi terpisah pasca-approval |
| T-413-R3 | Information Disclosure | Suite jalan tanpa SQLEXPRESS → Integration silent-skip (false-confidence) | accept | SQLEXPRESS dipakai 409/410/411 (VERIFIED env); bila absent → Integration gagal nyata, bukan skip diam; Task 1 catat Integration executed count |

> Plan 03 = gate/dokumentasi-only; nol surface produksi baru. Threat dominan = false-green regression + push prematur (keduanya mitigated lewat run nyata + checklist push-readiness).
</threat_model>

<verification>
1. `dotnet test` full suite → `Failed: 0`; total >= 602 + test 413-01 (tak ada test hilang).
2. Per-grup guard filter (391/409/410/411/412/413-01) → semua Passed.
3. `dotnet build` 0 error + `dotnet run` @5277 boot OK.
4. `413-REGRESSION.md` ada: mapping 11 REQ COVERED + regression guards + push-readiness gate (carry 01cd7dd0, JANGAN push) + e2e 7 sinyal summary.
5. `git status Migrations/ Data/` kosong → migration=FALSE.
</verification>

<success_criteria>
- Full `dotnet test` hijau (0 failed, >= 602 + lifecycle 413-01) tanpa regresi guard 391/398.1/409 + 410/411/412.
- `413-REGRESSION.md` memetakan 11 REQ → bukti xUnit + e2e (semua COVERED) + push-readiness gate ships-last (notify IT migration=TRUE Phase 409 `01cd7dd0`; JANGAN push di plan).
- build 0 error + boot @5277 OK; migration=FALSE; nol kode produksi.
</success_criteria>

<output>
After completion, create `.planning/phases/413-test-uat/413-03-SUMMARY.md`. Catat: angka suite aktual (full + per-grup); konfirmasi 0-regresi + Integration executed (bukan skip); status e2e 7 sinyal (dari 413-02); push-readiness (siap/blocked + alasan); konfirmasi migration=FALSE + carry IT 01cd7dd0; verdict milestone v32.5 siap-ship-pending-push.
</output>
