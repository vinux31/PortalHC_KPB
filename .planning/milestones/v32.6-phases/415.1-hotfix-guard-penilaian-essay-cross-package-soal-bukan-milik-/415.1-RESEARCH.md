# Phase 415.1: Hotfix Guard Penilaian Essay Cross-Package - Research

**Researched:** 2026-06-23
**Scope discipline:** Bugfix tunggal. JANGAN ubah desain pooling paket, refactor ShuffleEngine, ubah GET loader, atau sentuh Section v32.6. migration=FALSE.

> Catatan proses: researcher subagent putus koneksi mid-run; riset diselesaikan inline (read kode + git read-only). Semua temuan di bawah terverifikasi langsung dari source.

---

## RESEARCH COMPLETE

## 1. Titik Fix — Exact Code (terverifikasi)

`Controllers/AssessmentAdminController.cs`, method `SubmitEssayScore(int sessionId, int questionId, int score)` (~L4184-4251). Urutan guard saat ini:

| Guard | Line | Aksi | Status fix |
|-------|------|------|-----------|
| session null | L4189-4191 | reject "Session tidak ditemukan" | KEEP |
| status != PendingGrading | L4192-4193 | reject "Penilaian hanya bisa dilakukan saat status Menunggu Penilaian." | KEEP (386 D-08) |
| question null | L4196-4198 | reject "Soal tidak ditemukan" | KEEP |
| range 0..ScoreValue | L4199-4200 | reject "Skor harus antara 0 dan {N}" | KEEP |
| WR-01 tipe-Essay | L4208-4209 | reject "Soal ini bukan tipe Essay." | KEEP |
| **WR-02 ownership** | **L4210-4215** | reject "Soal bukan milik sesi ini." | **GANTI** |
| UPSERT EssayScore | L4217-4237 | buat/update baris | KEEP (tetap jalan untuk essay-kosong 386 D-08) |

**Blok yang diganti (L4210-4215, current):**
```csharp
// 2c. WR-02 (386-REVIEW) — questionId WAJIB milik sesi ini (cross-session tampering guard).
var ownsQuestion = await _context.PackageQuestions
    .AnyAsync(q => q.Id == questionId && q.AssessmentPackage.AssessmentSessionId == sessionId);
if (!ownsQuestion)
    return Json(new { success = false, message = "Soal bukan milik sesi ini." });
```

**Predikat baru (arah, mirror GET loader L4131-4143):**
```csharp
// 2c. WR-02 (FIXED 415.1) — questionId WAJIB bagian assignment peserta INI (bukan ownership paket).
//     Paket di-pool lintas sesi-sibling (by design), jadi ownership-by-package salah. Sumber-kebenaran =
//     UserPackageAssignment.GetShuffledQuestionIds() — IDENTIK dengan GET EssayGrading loader.
var assignment = await _context.UserPackageAssignments
    .FirstOrDefaultAsync(a => a.AssessmentSessionId == sessionId);
// D-02: assignment null (degenerate / non-paket) → IZINKAN (andalkan guard tipe-Essay + status + range).
if (assignment != null && !assignment.GetShuffledQuestionIds().Contains(questionId))
    return Json(new { success = false, message = "Soal bukan milik sesi ini." });
```
Catatan: `GetShuffledQuestionIds()` di-evaluasi in-memory (deserialisasi JSON) — load `assignment` dulu lalu cek `.Contains` (jangan paksa ke SQL). Saran ekstrak helper privat `async Task<bool> SessionAssignmentContainsQuestion(int sessionId, int questionId)` (return true bila assignment null) untuk reuse + testability.

## 2. Helper Sumber-Kebenaran (terverifikasi)

`Models/UserPackageAssignment.cs:60-71`:
```csharp
public List<int> GetShuffledQuestionIds()  // SELALU non-null (catch → new List<int>())
```
- `ShuffledQuestionIds` = kolom string JSON (`"[42,17,33,...]"`, default `"[]"`).
- Satu baris UPA per sesi (`AssessmentSessionId` FK). GET loader memuat UPA `FirstOrDefaultAsync(a => a.AssessmentSessionId == session.Id)` lalu `GetShuffledQuestionIds()` (L4131-4136). Fix POST mirror persis ini → invarian GET↔POST terjaga.
- **Upsert essay-kosong (386 D-08) tetap aman:** soal essay yang dikosongkan peserta tetap ADA di `ShuffledQuestionIds` (peserta melihat soal itu saat ujian, hanya tak menjawab) → `.Contains(questionId)` true → lolos → upsert jalan. Tak ada regresi pada jalur "HC nilai essay kosong".

## 3. Deploy Base Branch (D-01) — terverifikasi git read-only

- Branch aktif: `main`, **366 commit ahead** `origin/main`, **0 behind** (semua v32.x belum push — origin/main = kode v31.0 murni).
- `origin/main` HEAD = **`4b4a09b5`** (docs-only di atas v31.0). Cek: guard "Soal bukan milik sesi ini" **ADA** (count=1) + `GetShuffledQuestionIds` di `UserPackageAssignment.cs` **ADA** (count=1) → fix apply BERSIH.
- `origin/ITHandoff` HEAD = `64456bd5` (juga v31.0). Tidak ada tag `v31.0` lokal (tag lompat v30.0→v32.0) — abaikan, irrelevant; kedua remote branch = kode v31.0.
- **Rekomendasi base hotfix = `origin/main` (`4b4a09b5`).** Branch baru mis. `hotfix/essay-grading-ownership` dari `origin/main`. Method `SubmitEssayScore` + guard + `GetShuffledQuestionIds` semua eksis di sana → develop+verifikasi langsung di branch ini (BUKAN cherry-pick dari `main` yang sudah drift dengan Section).
  - ⚠️ Executor WAJIB konfirmasi konvensi deploy IT: apakah Dev ditarik dari `origin/main` atau `origin/ITHandoff`. Keduanya v31.0; bila ragu, pilih yang dipakai IT terakhir (memory: v31.0 merged origin/main 2026-06-16 + ITHandoff handoff). Fix identik di kedua base.
  - Setelah verifikasi lokal lulus: push branch hotfix → notify IT (commit hash + **migration=FALSE**). JANGAN merge ke main/push main (bawa v32.x).

## 4. Pola Test (terverifikasi) — apa yang di-update & ditambah

`HcPortal.Tests/PostLisensorPolishTests.cs` pakai **real SQL Server** (`UseSqlServer(_cs)`, `NewCtx()=new ApplicationDbContext(_fixture.Options)`), pola **logic-replica** `RunSubmitEssayScoreLogicAsync` (L123-167) yang menyalin urutan guard controller (BUKAN panggil controller). WR-02 direplika di L143-147.

**Wajib di-update (kalau tidak, test jadi salah/false-pass):**
1. **Replica `RunSubmitEssayScoreLogicAsync` (L143-147)** → ganti predikat ke logika baru (load UPA by sessionId, `GetShuffledQuestionIds().Contains(questionId)`, null→allow). Harus byte-identik dengan controller agar replica tetap valid.
2. **Test existing `SubmitEssayScore_CrossSessionQuestion_RejectedAndNoScoreWritten` (L189-207)** → SAAT INI tak seed UPA apa pun. Di logika baru + D-02 (null→allow) test ini akan **LOLOS (success)** → assertion reject GAGAL. **Fix:** seed `UserPackageAssignment` untuk `sessionA` dengan `ShuffledQuestionIds` yang TIDAK memuat `essayB` → tetap reject "Soal bukan milik sesi ini." (genuine tamper). Ini menjaga makna test.

**Wajib ditambah (bukti fix + cabang D-02):**
3. **`SubmitEssayScore_CrossPackageAssignedQuestion_Succeeds`** (BUKTI BUG FIX) — seed: sesi induk punya paket P (questions termasuk `essayQ`); `sessionB` (worker lain, PendingGrading) punya `UserPackageAssignment` yang `ShuffledQuestionIds` MEMUAT `essayQ` (paket P dimiliki sesi induk, BUKAN sessionB). Panggil `SubmitEssayScore(sessionB, essayQ, 5)` → **SUCCESS** + baris `EssayScore=5` tertulis. (Sebelum fix: reject; sesudah: lolos.)
4. **`SubmitEssayScore_NullAssignment_Allowed`** (D-02) — `sessionA` PendingGrading + essayQ milik paketnya, TANPA UPA → **SUCCESS** (andalkan guard sisa). Lock perilaku D-02.
5. **(Opsional, nilai tambah)** `SubmitEssayScore_AssignmentMissingQuestion_Rejected` — UPA ada tapi `ShuffledQuestionIds` tak memuat questionId → reject. (Sebagian tercakup oleh #2.)

**Catatan replica-tautology (backlog 999.12):** replica tidak menangkap regresi controller nyata. Untuk D-03 "full", kepercayaan controller-level didapat dari **repro live @5277 + Playwright** (exercise controller asli), bukan WebApplicationFactory berat. Cukup & pragmatis.

## 5. Repro Live + Seed (D-03) + Playwright

**Symptom front-end (terverifikasi `wwwroot/js/essay-grading.js:56-69`):** tombol `.btn-save-essay-score` → POST `/Admin/SubmitEssayScore` → `if (data.success) { badge "Sudah Dinilai" } else { alert(data.message) }`. Jadi bug = `alert("Soal bukan milik sesi ini.")`. Pasca-fix = badge hijau "Sudah Dinilai", NO alert.

**Seed minimal repro (ikuti `docs/SEED_WORKFLOW.md`: snapshot→seed→test→restore→SEED_JOURNAL cleaned):**
- 1 grup sibling (Title+Category+Schedule.Date+Type sama), 2 sesi worker (induk S1 + S2).
- Paket P (Paket A) dibuat di S1; soal termasuk ≥1 Essay.
- UPA untuk S1 dan S2 keduanya `ShuffledQuestionIds` memuat questionId dari paket P (pooled). S2 status=PendingGrading + ada `PackageUserResponse` essay (boleh TextAnswer terisi atau kosong).
- Buka `/Admin/EssayGrading?sessionId={S2}` → isi skor → Simpan. **Sebelum fix:** alert. **Sesudah:** tersimpan.
- Playwright: gunakan analog `tests/e2e/essay-grading-384.spec.ts` / `essay-empty-finalize-386.spec.ts`. WAJIB pasang `page.on('dialog')` untuk menangkap/menolak `alert()`; assert badge `#badge_{sessionId}_{questionId}` jadi "Sudah Dinilai" + TIDAK ada dialog "Soal bukan milik sesi ini". Baca `Views/Admin/EssayGrading.cshtml` untuk selector pasti (`.essay-score-input`, `.btn-save-essay-score`, `data-session-id`, `data-question-id`).

## 6. Pre/Post SamePackage (D-03)

`SyncPackagesToPost` (L6522-6629) deep-clone paket Pre→Post (baris baru, `AssessmentSessionId = postSessionId`). Assignment Post dibangun (CreateEager/StartExam) dari pool sibling-Post → `ShuffledQuestionIds` Post memuat questionId paket clone milik sesi induk-Post. Predikat baru (question ∈ assignment) menangani ini IDENTIK dengan kasus non-Pre/Post. Test/scenario Pre/Post = paritas; tak ada cabang khusus. Cukup 1 skenario UAT konfirmasi.

## 7. Risiko / Pitfall

- **PITFALL-1 (utama):** lupa update test WR-02 existing (#2) → false-pass/false-fail. Replica + existing test HARUS diselaraskan ke predikat baru.
- **PITFALL-2:** memaksa `GetShuffledQuestionIds().Contains()` masuk ekspresi LINQ-to-SQL → exception translasi. Load UPA dulu, evaluasi in-memory.
- **PITFALL-3:** develop fix di `main` lalu cherry-pick → file sudah drift (Section). Develop LANGSUNG di branch dari `origin/main`.
- **PITFALL-4:** Playwright tak pasang handler `dialog` → `alert()` native menggantung test. Pasang `page.on('dialog', d => d.dismiss())` + assert tak terpicu untuk pesan target.
- **Bukan-pitfall (konfirmasi):** sesi finalized (Completed+cert) tetap ditolak status-guard L4192 — di luar scope, by design.

---

## Validation Architecture

(Untuk Nyquist Dimension 8 — drive `VALIDATION.md`. User pilih kedalaman FULL.)

| Success Criterion | Test Type | Observable Signal (minimum) | False-Positive Guard |
|---|---|---|---|
| SC-1 HC simpan nilai essay untuk peserta paket-pinjam tanpa alert | (a) xUnit `SubmitEssayScore_CrossPackageAssignedQuestion_Succeeds` real-SQL → `result.Success==true` + baris `EssayScore` tertulis di verify-ctx. (b) Playwright live @5277 → badge "Sudah Dinilai", NO dialog "Soal bukan milik sesi ini". | Test HARUS seed UPA memuat questionId milik paket sesi LAIN (bukan paket sesi yg dinilai) — kalau paket dimiliki sesi sendiri, fix tak terbukti. Playwright HARUS pakai 2 worker (paket di induk). |
| SC-2a Anti-tamper: reject bila questionId BUKAN bagian assignment | xUnit `SubmitEssayScore_CrossSessionQuestion_Rejected` (di-update: UPA ada, tak memuat questionId) → `Success==false`, msg "Soal bukan milik sesi ini.", 0 baris tertulis. | Wajib seed UPA (bukan null) — kalau null, jatuh ke cabang D-02 (allow) dan test salah arah. |
| SC-2b Reject bila bukan tipe Essay | xUnit `SubmitEssayScore_NonEssayQuestion_Rejected` (existing, regresi) → msg "Soal ini bukan tipe Essay." | — |
| SC-2c Reject bila status != PendingGrading | xUnit `SubmitEssayScore_CompletedSession_Rejected` (existing, regresi) → skor lama tak berubah. | — |
| SC-2d Reject bila skor di luar 0..ScoreValue | xUnit range test (existing/regresi) | — |
| SC (D-02) Null assignment → allow | xUnit `SubmitEssayScore_NullAssignment_Allowed` → `Success==true`. | Pastikan TANPA UPA + essay valid + PendingGrading. |
| SC Pre/Post SamePackage paritas | 1 skenario UAT/xUnit: Post worker dinilai → success. | Paket Post = clone (AssessmentSessionId=Post induk). |
| SC-3 build + suite hijau | `dotnet build` 0 error + `dotnet test` (PostLisensorPolishTests + suite) hijau. | Jalankan suite penuh, bukan 1 test. |

**Wave_0 (test-first opsional):** test #2 (update) + #3 (positif) = RED dulu di base sebelum fix (membuktikan bug), GREEN sesudah fix.

## Files (planner reference)
- MODIFY: `Controllers/AssessmentAdminController.cs` (`SubmitEssayScore` L4210-4215) — ganti predikat (+helper opsional).
- MODIFY: `HcPortal.Tests/PostLisensorPolishTests.cs` — update replica L143-147 + test L189-207; tambah test #3/#4.
- ADD (test e2e): `tests/e2e/*essay*ownership*.spec.ts` (analog `essay-grading-384.spec.ts`).
- READ-ONLY ref: `Models/UserPackageAssignment.cs`, `Views/Admin/EssayGrading.cshtml`, `wwwroot/js/essay-grading.js`.
- NO migration. NO schema. NO Views change (kecuali e2e read).
