---
status: passed
phase: 415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik
verified: 2026-06-23
migration: false
---

# 415.1 VERIFICATION — Guard Penilaian Essay Cross-Package

## Phase Goal
HC/Admin dapat menilai essay untuk SEMUA peserta (termasuk yang dapat soal dari paket milik sesi-sibling lain / cross-package), tanpa false-positive error "Soal bukan milik sesi ini.", tanpa membuka celah tamper, tanpa migration.

## Bukti (rantai verifikasi lengkap)

| Lapisan | Bukti | Hasil |
|---------|-------|-------|
| Unit (replica real-SQL) | `PostLisensorPolishTests` — cross-package SUCCESS + null-allow (D-02) + genuine-tamper reject + regresi guard lama | **13/13 PASS** |
| Build | `dotnet build HcPortal.csproj` | **0 Error** |
| Regresi | `dotnet test` suite penuh | **633/633 PASS** |
| e2e (controller ASLI, real-browser) | Playwright `essay-grading-ownership-415.1` — badge "Sudah Dinilai"+bg-success, 0 dialog "Soal bukan milik sesi ini" (cross-package + Pre/Post) | **2/2 PASS** |
| **Dev UAT (live, production-like)** | IT deploy branch hotfix → HC menilai + submit essay pekerja @ `10.55.3.3/KPB-PortalHC` | **PASS (2026-06-23)** |

## Must-Haves (Plan 01)
- [x] Cross-package: assignment ADA + memuat questionId → LOLOS (lanjut upsert) walau paket dimiliki sesi lain.
- [x] Genuine tamper: assignment ADA tapi TIDAK memuat questionId → reject "Soal bukan milik sesi ini."
- [x] Null assignment (D-02) → IZINKAN (andalkan guard tipe-Essay + status + range).
- [x] Guard lama UTUH: WR-01 tipe-Essay, status PendingGrading, range 0..ScoreValue, upsert essay-kosong.
- [x] RBAC `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` tetap ada.

## Delivery
- Branch `hotfix/essay-grading-ownership` (commit `390daf8c`) dari `origin/main` v31.0 (`4b4a09b5`) — **PUSHED ke GitHub**, di-deploy IT ke Dev. migration=FALSE. origin/main TIDAK tersentuh.

## Verdict
**PASSED** — semua lapisan hijau termasuk Dev UAT live. Phase goal tercapai. migration=FALSE.
