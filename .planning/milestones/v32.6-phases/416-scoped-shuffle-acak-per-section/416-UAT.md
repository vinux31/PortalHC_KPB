---
status: passed
phase: 416-scoped-shuffle-acak-per-section
source: [416-03-PLAN.md, tests/e2e/scoped-shuffle.spec.ts]
method: playwright-automated (milestone-autopilot §5 — real chromium, no human)
started: 2026-06-23
updated: 2026-06-23
---

## Ringkasan

UAT scoped-shuffle dijalankan otomatis via Playwright real-browser (chromium) @ http://localhost:5277
dengan lifecycle DB snapshot/restore (SEED_WORKFLOW: BACKUP `HcPortalDB_Dev` beforeAll → RESTORE afterAll).
**5/5 skenario PASS.** DB lokal restored bersih pasca-run (SEED_JOURNAL 416-03 = cleaned).

Lesson 354 (Razor/JS render WAJIB UAT browser) terpenuhi: Playwright = bukti runtime nyata.
Checkpoint human-verify Plan 03 Task 2 di-auto-satisfy oleh e2e hijau (autopilot §5, no human present).
Konfirmasi mata-manusia akhir = OPSIONAL pra-ship (rekomendasi, non-blocking).

## Tests

### 1. Isolasi Section (SHF-01) — S1
expected: soal assessment ber-Section hanya teracak DALAM Section-nya; blok kontigu (Sec1 lalu Sec2), tak interleave, "Lainnya" terakhir.
result: **PASS** — DB `UserPackageAssignment.ShuffledQuestionIds` == urutan DOM `qcard_{id}`; map qid→SectionId membuktikan blok per-Section kontigu, no cross-section leak.

### 2. Backward-compat all-null (SHF-04) — S2
expected: assessment tanpa Section → urutan global (1 kolam), tanpa error/grouping.
result: **PASS** — semua soal render sbg 1 kolam global, tanpa error; DOM == DB.

### 3. ET-coverage non-blocking (D-416-03) — S3 + S3b
expected: Section sempit TIDAK memblokir kelola/simpan/mulai ujian; tanpa false-positive alert.
result: **PASS** — S3: kelola/simpan/mulai tetap aktif (inti load-bearing D-416-03 terbukti). S3b kontrol negatif: cakupan ET penuh → tak ada alert `.alert-warning` "Elemen Teknis" (no false-positive).
catatan: render POSITIF alert ET-warning TIDAK teruji — predikat `DistinctEt > K` tak terjangkau data single-package (DEF-416-01, dead nicety, non-blocking). Lihat Gaps.

### 4. Parity drift-free (SHF-04) — S4
expected: assignment peserta = blok-per-section; peserta tambah-live (AddParticipantsLive) pola-konsisten.
result: **PASS** — assignment blok-per-section terverifikasi; jalur AddParticipantsLive best-effort (try/catch + log, hard-assert pada StartExam path).

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

### DEF-416-01 — ET-coverage warning positive render (non-blocking, deferred)
status: deferred
severity: low (dead-nicety; non-security, non-correctness)
detail: Predikat `DistinctEt > K` (AssessmentAdminController.cs:7680) menghitung `DistinctEt` dari himpunan soal
single-package yang sama yang mendefinisikan `K` → `DistinctEt ≤ K` SELALU → alert ET-coverage tak pernah render.
Inti load-bearing D-416-03 ("Section sempit tetap boleh mulai", non-blocking) TETAP benar + ter-runtime-verify (S3/S3b).
fix (jika diangkat): re-spec `DistinctEt` sebagai pool ET lintas paket-saudara vs `K = min count Section antar sibling`;
selaraskan grouping warning dari `SectionId` (per-paket) ke `SectionNumber` (lintas-paket, IN-01).
disposition: **FIX DI PHASE 419** (carry-over, keputusan user 2026-06-23). 416 ditutup non-blocking.
ref: 416-REVIEW.md WR-01, deferred-items.md (DISPOSISI), 416-VERIFICATION.md, 416-VALIDATION.md.
