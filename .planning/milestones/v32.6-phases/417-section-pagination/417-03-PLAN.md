---
phase: 417-section-pagination
plan: 03
type: execute
wave: 3
depends_on: [417-02]
files_modified:
  - tests/e2e/section-pagination.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [PAG-01, PAG-02, PAG-03]
tags: [pagination, e2e, playwright, uat, verify]
user_setup: []

must_haves:
  truths:
    - "e2e real-browser membuktikan header Section (nama saja) muncul saat berganti Section + '(lanjutan)' di halaman auto-split"
    - "e2e membuktikan Section ber-StartNewPage mulai di exam-page div baru (page-break)"
    - "e2e membuktikan navigator dikelompokkan per-Section dengan label grup"
    - "e2e membuktikan resume mendarat di halaman terhitung > 0 + toast 'Lanjut dari soal no. X'"
    - "e2e membuktikan assessment tanpa Section render flat (backward-compat, no header/no label)"
    - "Admin quick-button 'Semua section mulai halaman baru' (Phase 415) terverifikasi: set semua StartNewPage=true → StartExam page-break per section"
  artifacts:
    - path: "tests/e2e/section-pagination.spec.ts"
      provides: "Playwright e2e section-aware pagination (header/lanjutan/break/navigator/resume-toast/no-Section flat/mobile) dgn DB backup/restore"
      contains: "test.describe.configure({ mode: 'serial' })"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Entri seed temporary+local-only untuk e2e 417 (tujuan, klasifikasi, restore-cleaned)"
      contains: "417"
  key_links:
    - from: "tests/e2e/section-pagination.spec.ts"
      to: "Views/CMP/StartExam.cshtml (render section-aware)"
      via: "login peserta → /CMP/Assessment → StartExam, assert DOM header/navigator/indikator/toast"
      pattern: "StartExam|exam-page"
    - from: "tests/e2e/section-pagination.spec.ts"
      to: "DB HcPortalDB_Dev (seed Section via SQL UPDATE)"
      via: "db backup beforeAll / restore afterAll (SEED_WORKFLOW)"
      pattern: "dbSnapshot|BACKUP|RESTORE"
---

<objective>
Buktikan PAG-01/02/03 berfungsi runtime di real-browser (lesson 354: Razor/JS WAJIB UAT browser — unit tak menangkap render/JS) lewat Playwright `section-pagination.spec.ts`, verifikasi admin quick-button "Semua section mulai halaman baru" (sudah ada Phase 415) memicu page-break per section, lalu checkpoint UAT live @localhost:5277.

Purpose: Gerbang akhir fase — menutup gap yang unit test pure tak bisa (render header DOM, navigator grouping, resume toast, page-break visual, backward-compat flat). Mengikuti SEED_WORKFLOW (CLAUDE.md): seed temporary+local-only, snapshot/restore DB.
Output: `tests/e2e/section-pagination.spec.ts` hijau, entri `docs/SEED_JOURNAL.md`, UAT live approved. migration=FALSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/417-section-pagination/417-CONTEXT.md
@.planning/phases/417-section-pagination/417-RESEARCH.md
@.planning/phases/417-section-pagination/417-PATTERNS.md
@.planning/phases/417-section-pagination/417-UI-SPEC.md
@.planning/phases/417-section-pagination/417-02-SUMMARY.md

<interfaces>
<!-- Analog e2e (sudah ada di repo). Executor clone polanya. -->

tests/e2e/scoped-shuffle.spec.ts (Phase 416 — analog LANGSUNG, sama domain Section, sama jalur StartExam):
- `test.describe.configure({ mode: 'serial' })`
- `import * as db from '../helpers/dbSnapshot';` (BACKUP beforeAll / RESTORE afterAll)
- `import { login } from '../helpers/auth';`
- `import { createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm } from './helpers/examTypes';`
- Seed Section via SQL UPDATE pada record wizard: `UPDATE AssessmentPackageSections SET StartNewPage=...` + `UPDATE PackageQuestions SET SectionId=...`
- Auth: admin@pertamina.com / 123456 (dev lokal). Peserta: rino.prasetyo@pertamina.com (coachee).
- Ambil ujian peserta: exam-taking.spec.ts:89-101 (login coachee → /CMP/Assessment → .assessment-card .btn-start-standard → StartExam).
- PRECONDITION: app @http://localhost:5277 (Authentication__UseActiveDirectory=false dotnet run) + DB lokal. WAJIB --workers=1.

DOM yang di-assert (dari Plan 02 render):
- Header Section: `<div class="text-primary fw-semibold border-bottom ...">{NamaSection}</div>` + `<span class="text-muted small fw-normal">(lanjutan)</span>`
- exam-page div: `#page_0`, `#page_1`, ... (page-break = soal Section ber-StartNewPage di `#page_>0`)
- Navigator label grup: `#panelNumbers div[style*="grid-column"]` (label) ; badge `#panelNumbers .badge`
- Indikator: `#pageSectionIndicator` (teks "NamaSection — Halaman n/total")
- Toast resume: `#resumeInfoToast .toast-body` (teks "Lanjut dari soal no. X")

Admin quick-button (Phase 415, VERIFY-ONLY — JANGAN bangun ulang):
- `AssessmentAdminController.cs:6428` `SetAllSectionsNewPage(int packageId)` [Authorize(Admin,HC)][ValidateAntiForgeryToken]
- `ManagePackageQuestions.cshtml:84-93` tombol `bi bi-file-earmark-break` "Semua section mulai halaman baru"
</interfaces>

<read_before_planning>
Sudah dibaca saat planning:
- tests/e2e/scoped-shuffle.spec.ts:1-40 (header doc, mode:serial, imports, SEED_WORKFLOW, auth, seed Section via SQL)
- .planning/phases/417-section-pagination/417-RESEARCH.md (Wave 0 Gaps e2e + Validation Architecture Test Map + catatan migrasi-laten resume)
- .planning/phases/417-section-pagination/417-PATTERNS.md (section-pagination.spec.ts analog + cakupan target)
</read_before_planning>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tulis tests/e2e/section-pagination.spec.ts + entri SEED_JOURNAL</name>
  <files>tests/e2e/section-pagination.spec.ts, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/scoped-shuffle.spec.ts (FULL — clone struktur: header doc, mode:serial, beforeAll BACKUP / afterAll RESTORE, createAssessmentViaWizard + createDefaultPackage + addQuestionViaForm, seed Section via SQL UPDATE pada record baru, login coachee → /CMP/Assessment → StartExam)
    - tests/helpers/dbSnapshot.ts (queryScalar/queryString/BACKUP/RESTORE + localhost-guard)
    - tests/helpers/auth.ts (login)
    - tests/e2e/helpers/examTypes.ts (createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm, today()/tomorrow())
    - tests/playwright.config.ts (baseURL localhost:5277, fullyParallel:false, --workers=1)
    - docs/SEED_WORKFLOW.md (format entri journal + klasifikasi temporary+local-only)
    - .planning/phases/417-section-pagination/417-RESEARCH.md (Wave 0 Gaps e2e — daftar skenario + catatan migrasi-laten resume)
  </read_first>
  <action>
Buat file BARU `tests/e2e/section-pagination.spec.ts`. Clone struktur `scoped-shuffle.spec.ts` (header doc Bahasa Indonesia menjelaskan skenario S1..Sn + SEED_WORKFLOW + precondition + auth). `test.describe.configure({ mode: 'serial' })`. `beforeAll` BACKUP HcPortalDB_Dev / `afterAll` RESTORE + unlink (lewat `helpers/dbSnapshot`). Import `login`, `createAssessmentViaWizard`/`createDefaultPackage`/`addQuestionViaForm`.

Setup data (di beforeAll atau test pertama, REUSE pola scoped-shuffle):
- Buat assessment via wizard (admin) dgn paket berisi cukup soal (≥12 untuk uji auto-split + ≥2 Section). Tambah soal via `addQuestionViaForm`.
- Seed Section via SQL UPDATE pada record baru: buat 2 Section (mis. via endpoint CreateSection ATAU SQL INSERT `AssessmentPackageSections`), set salah satu `StartNewPage=1`, assign `PackageQuestions.SectionId` ke Section sesuai skenario (Section A = 12 soal untuk auto-split, Section B `StartNewPage=1`). Dokumentasikan tiap SQL inline.
- Peserta `rino.prasetyo@pertamina.com` di-add via wizard participantEmails.

Tulis test berikut (gunakan baseURL `/KPB-PortalHC` PathBase bila helper existing pakai — ikuti scoped-shuffle):

1. **header on section change (PAG-01)**: login coachee → StartExam → assert ada `div.text-primary.fw-semibold` berisi NAMA Section (tanpa "Section N:"). Verifikasi header muncul saat berganti Section.

2. **"(lanjutan)" auto-split (PAG-02)**: Section A 12 soal → halaman ke-2 Section A menampilkan header + `span:has-text("(lanjutan)")`. Assert soal #11 ada di `#page_>0` dengan header "(lanjutan)".

3. **StartNewPage page-break (PAG-02)**: Section B (`StartNewPage=1`) → soal pertama Section B berada di `exam-page` div BARU (mis. `#page_1`), bukan menyambung halaman Section A meski belum penuh.

4. **navigator grouping (D-417-03)**: `#panelNumbers` punya elemen label grup (style mengandung `grid-column`) berisi nama Section, di atas badge grup. Assert ≥2 label (2 Section).

5. **resume landing page + toast (PAG-03, D-417-06)**: simulasikan resume — set `AssessmentSessions.LastActivePage` ke nilai >0 via SQL (sesuai page Section B), reload StartExam, klik tombol resume "Lanjutkan", assert halaman aktif = page terhitung (bukan 0) DAN toast `#resumeInfoToast` berisi "Lanjut dari soal no.". (Catatan migrasi-laten RESEARCH: nilai LastActivePage lama valid sebagai page-index global; bila out-of-range → clamp 0.)

6. **no-Section flat smoke (backward-compat, PAG-01)**: buat/gunakan assessment TANPA Section (semua SectionId=null) → StartExam → assert TIDAK ada `div.text-primary.fw-semibold` header Section DAN navigator flat (tak ada label grid-column) DAN indikator `#pageSectionIndicator` = "Halaman n/total" (tanpa nama Section). Smoke: render tanpa error/crash.

7. **(opsional, bila waktu) mobile 5/halaman**: set viewport mobile / UA mobile → assert questionsPerPage=5 menghormati Section (header + break tetap). Boleh diturunkan ke assertion DB bila UI terlalu kompleks.

Tambahkan entri ke `docs/SEED_JOURNAL.md`: tujuan "e2e Phase 417 section-pagination", klasifikasi `temporary + local-only`, entitas tersentuh (AssessmentSessions, AssessmentPackages, AssessmentPackageSections, PackageQuestions, UserPackageAssignments), status `cleaned` setelah restore.
  </action>
  <verify>
    <automated>cd tests && npx playwright test section-pagination.spec.ts --workers=1 --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/section-pagination.spec.ts` ada
    - `grep -n "test.describe.configure({ mode: 'serial' })" tests/e2e/section-pagination.spec.ts` → match
    - `grep -nE "dbSnapshot|BACKUP|RESTORE" tests/e2e/section-pagination.spec.ts` → match (SEED_WORKFLOW)
    - `grep -nE "lanjutan|pageSectionIndicator|resumeInfoToast|grid-column" tests/e2e/section-pagination.spec.ts` → ≥3 match (skenario header/lanjutan/navigator/resume)
    - `grep -nE "SectionId|StartNewPage" tests/e2e/section-pagination.spec.ts` → match (seed Section)
    - `grep -n "417" docs/SEED_JOURNAL.md` → match (entri journal)
    - PRECONDITION: app running @localhost:5277 (AD-off). Jika tidak, executor START dulu: `Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5277` (background).
    - `npx playwright test section-pagination.spec.ts --workers=1` → semua test PASS (atau dokumentasikan kegagalan + fix sebelum lanjut)
    - DB di-RESTORE setelah test (verifikasi 0 baris seed temporary tertinggal); SEED_JOURNAL ditandai cleaned
  </acceptance_criteria>
  <done>section-pagination.spec.ts hijau (header/lanjutan/break/navigator/resume-toast/no-Section flat), DB restore bersih, SEED_JOURNAL cleaned.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verifikasi admin quick-button "Semua section mulai halaman baru" (Phase 415) memicu page-break</name>
  <files>tests/e2e/section-pagination.spec.ts, Views/Admin/ManagePackageQuestions.cshtml (cosmetic-only bila perlu selaras IC-9)</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:6428-6452 (SetAllSectionsNewPage — VERIFY-ONLY, jangan ubah)
    - Views/Admin/ManagePackageQuestions.cshtml:84-93 (tombol bi-file-earmark-break — VERIFY-ONLY)
    - .planning/phases/417-section-pagination/417-RESEARCH.md (Don't Hand-Roll: quick-button SUDAH ADA Phase 415, 417 = VERIFIKASI saja)
    - .planning/phases/417-section-pagination/417-UI-SPEC.md (IC-9 — wording/placement; cosmetic discretion bila perlu selaras btn-outline-primary)
  </read_first>
  <action>
JANGAN bangun ulang quick-button (sudah shipped Phase 415). Tugas ini VERIFIKASI fungsional bahwa tombol memicu render page-break di StartExam:

1. Konfirmasi tombol + action ADA (grep, JANGAN edit logika):
   - `SetAllSectionsNewPage` di `AssessmentAdminController.cs` (`[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` + set semua `StartNewPage=true` + audit `LogSectionAuditAsync` + `SyncToPostIfSamePackageAsync`).
   - Tombol di `ManagePackageQuestions.cshtml` (`bi bi-file-earmark-break`, label "Semua section mulai halaman baru").

2. Tambahkan satu skenario ke `section-pagination.spec.ts` (atau test terpisah dalam file yang sama): login admin → ManagePackageQuestions paket ber-≥2 Section → klik tombol "Semua section mulai halaman baru" → konfirmasi semua `AssessmentPackageSections.StartNewPage=1` (via SQL `queryScalar`) → login coachee → StartExam → assert SETIAP Section (kecuali yang pertama) mulai di `exam-page` div baru (page-break per section). Backward-compat: Section pertama tetap di page0.

3. Bila wording tombol perlu selaras IC-9 (`btn-outline-primary` rekomendasi) DAN ini cosmetic non-blocking — boleh sesuaikan kelas tombol di `ManagePackageQuestions.cshtml:84-93` SAJA (cosmetic, JANGAN ubah action/logic). Bila sudah cukup selaras, lewati (catat di SUMMARY).

migration=FALSE; tak menyentuh logika controller.
  </action>
  <verify>
    <automated>cd tests && npx playwright test section-pagination.spec.ts --workers=1 -g "quick-button|page-break|StartNewPage" --reporter=line 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SetAllSectionsNewPage" Controllers/AssessmentAdminController.cs` → match (terkonfirmasi ada, tak diubah logika)
    - `grep -nE "Semua section mulai halaman baru|file-earmark-break" Views/Admin/ManagePackageQuestions.cshtml` → match
    - `git diff --stat Controllers/AssessmentAdminController.cs` → kosong (logika SetAllSectionsNewPage TAK diubah)
    - e2e skenario quick-button → page-break per section PASS
    - `git status --porcelain Migrations/ Data/` → kosong
  </acceptance_criteria>
  <done>Quick-button Phase 415 terverifikasi fungsional (set semua StartNewPage=true → page-break per section di StartExam); logika controller tak diubah; e2e skenario hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT live @localhost:5277 — render section pagination + resume (lesson 354)</name>
  <files>Views/CMP/StartExam.cshtml, Views/Admin/ManagePackageQuestions.cshtml (verifikasi visual; tak diedit di task ini)</files>
  <action>
Checkpoint manusia — TIDAK ada perubahan kode otomatis di task ini. Claude sudah mengotomasi seluruh implementasi (Plan 01 helper+model+unit, Plan 02 controller+view, Plan 03 Task 1/2 e2e). Task ini HANYA konfirmasi visual manusia atas render section-aware + resume + quick-button + backward-compat di real browser (lesson 354: Razor/JS WAJIB UAT browser). Jalankan langkah di &lt;how-to-verify&gt; lalu tunggu &lt;resume-signal&gt;.
  </action>
  <what-built>
Section-aware pagination ujian: header NAMA Section (D-417-01) + "(lanjutan)" auto-split (D-417-02), page-break StartNewPage (PAG-02), navigator dikelompokkan per-Section (D-417-03), indikator "NamaSection — Halaman n/total" (D-417-04), resume mendarat di halaman terhitung + toast "Lanjut dari soal no. X" (D-417-06), backward-compat no-Section flat. Plus quick-button admin "Semua section mulai halaman baru" terverifikasi memicu page-break.
  </what-built>
  <how-to-verify>
1. Pastikan app jalan: `Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5277` (cek http://localhost:5277/KPB-PortalHC).
2. Login admin (admin@pertamina.com / 123456). Buka Kelola Soal paket ber-≥2 Section (atau buat: tambah Section A + Section B, set Section B "Mulai Halaman Baru" ON, assign soal Section A ≥12 soal untuk uji auto-split).
3. Login peserta (rino.prasetyo@pertamina.com) → /CMP/Assessment → mulai ujian assessment tsb.
4. VERIFIKASI VISUAL:
   - Header NAMA Section (tanpa "Section N:") muncul di atas grup soal saat berganti Section.
   - Section A >10 soal: halaman ke-2 menampilkan header + "(lanjutan)".
   - Section B mulai di HALAMAN BARU (page-break) meski halaman sebelumnya belum penuh.
   - Navigator (sidebar desktop + drawer mobile): badge dikelompokkan per-Section dengan label grup.
   - Indikator halaman: "{NamaSection} — Halaman n/total".
   - Klik beberapa halaman, lalu keluar/refresh → klik "Lanjutkan" (resume): mendarat di halaman terakhir (bukan halaman 1) + toast biru "Lanjut dari soal no. X".
5. Klik quick-button admin "Semua section mulai halaman baru" → ambil ujian lagi → SETIAP Section mulai di halaman baru.
6. BACKWARD-COMPAT: ambil ujian assessment TANPA Section → tampilan flat 10/halaman seperti biasa (TANPA header Section, navigator flat, indikator "Halaman n/total").
7. (opsional) Mobile: 5 soal/halaman menghormati Section.
8. Setelah UAT: RESTORE DB lokal bila ada seed temporary; tandai SEED_JOURNAL cleaned. JANGAN push tanpa approval (CLAUDE.md step 4-5).
  </how-to-verify>
  <verify>Manual checkpoint — konfirmasi visual manusia per langkah &lt;how-to-verify&gt; (tak ada perintah otomatis; e2e otomatis sudah di Task 1/2).</verify>
  <done>Manusia mengetik "approved" setelah seluruh verifikasi visual benar (header/lanjutan/break/navigator/indikator/resume+toast/quick-button/backward-compat), DB lokal di-restore, SEED_JOURNAL cleaned.</done>
  <resume-signal>Ketik "approved" bila semua visual benar, atau jelaskan isu (header/break/navigator/resume/toast/backward-compat) untuk diperbaiki.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → StartExam render | Diuji via e2e real-browser (worker melihat ujiannya sendiri). Tak ada permukaan baru. |
| test → DB lokal HcPortalDB_Dev | Seed temporary+local-only via SQL UPDATE (localhost-guard dbSnapshot); BACKUP/RESTORE melindungi DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-417-07 | Tampering/Info | XSS nama Section di render (verifikasi runtime) | mitigate | e2e assert header/navigator/indikator memakai nama Section yang di-encode; UAT live cek nama Section ber-karakter spesial (mis. `&`) ter-render literal, bukan ter-eksekusi. Menutup T-417-03 secara runtime. |
| T-417-08 | (test hygiene) | Seed e2e bocor ke DB lokal | mitigate | SEED_WORKFLOW: BACKUP beforeAll / RESTORE afterAll + SEED_JOURNAL cleaned. Bukan ancaman produk; disiplin data lokal. |
</threat_model>

<verification>
- `cd tests && npx playwright test section-pagination.spec.ts --workers=1` → semua test PASS (app @5277 AD-off).
- `dotnet test HcPortal.Tests` → full suite 665+ hijau (regression gate sebelum verify-work).
- UAT live checkpoint (Task 3) → approved.
- DB restore bersih; SEED_JOURNAL cleaned; `git status Migrations/ Data/` kosong (migration=FALSE).
</verification>

<success_criteria>
- `section-pagination.spec.ts` membuktikan runtime: header (nama saja) + "(lanjutan)" + StartNewPage break + navigator grouping + resume landing+toast + no-Section flat backward-compat.
- Admin quick-button (Phase 415) terverifikasi memicu page-break per section (logika tak diubah).
- UAT live approved (lesson 354 Razor/JS).
- DB lokal pristine pasca-test; migration=FALSE; tak ada perubahan grading/skor/RBAC.
</success_criteria>

<output>
After completion, create `.planning/phases/417-section-pagination/417-03-SUMMARY.md`
</output>
