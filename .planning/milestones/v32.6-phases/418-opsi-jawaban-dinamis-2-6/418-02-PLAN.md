---
phase: 418-opsi-jawaban-dinamis-2-6
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Models/OptionInput.cs
  - Helpers/OptionShrinkGuard.cs
  - Helpers/QuestionOptionValidator.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [OPT-01, OPT-03]
tags: [controller, options, binding, guard, validation]

must_haves:
  truths:
    - "CreateQuestion/EditQuestion POST menerima List<OptionInput> (2–6 opsi) menggantikan param diskret A–D"
    - "Validator menolak >6 opsi terisi (max-6) dari form authoring"
    - "Guard H3 (tolak edit soal >4 opsi) DIHAPUS — soal 5–6 opsi (import 415) bisa diedit"
    - "Edit-shrink: hapus opsi yang sudah dijawab peserta ditolak via TempData['Error'] (BUKAN 500 FK Restrict)"
    - "Radio MC single-select lintas baris memakai satu name grup 'correctIndex'; checkbox MA per-index options[i].IsCorrect"
    - "GET JSON EditQuestion options[] shape TIDAK berubah (variable-length, OrderBy Id)"
  artifacts:
    - path: "Models/OptionInput.cs"
      provides: "Binding model opsi dinamis (Text, IsCorrect, Image, ImageAlt, RemoveImage) — whitelist, no Id"
      contains: "class OptionInput"
    - path: "Helpers/OptionShrinkGuard.cs"
      provides: "FindBlockedOptionIds(removed, answered) pure intersection (kontrak Plan 01)"
      contains: "FindBlockedOptionIds"
    - path: "Helpers/QuestionOptionValidator.cs"
      provides: "max-6 enforcement"
      contains: "filled > 6"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "CreateQuestion/EditQuestion POST refactor list-binding + guard edit-shrink + delete H3 + loop A–F"
      contains: "List<OptionInput>"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs EditQuestion POST"
      to: "Helpers/OptionShrinkGuard.cs"
      via: "FindBlockedOptionIds(removedOptionIds, answeredOptionIds) sebelum SaveChangesAsync"
      pattern: "FindBlockedOptionIds"
    - from: "Controllers/AssessmentAdminController.cs CreateQuestion/EditQuestion"
      to: "Helpers/QuestionOptionValidator.cs"
      via: "ValidateQuestionOptions(type, options.Select(Text), options.Select(IsCorrect))"
      pattern: "ValidateQuestionOptions"
---

<objective>
Refactor kontrak HTTP authoring dari 16 param diskret (`optionA..D` + `correctA..D` + 12 param gambar) menjadi binding **`List<OptionInput>`** (≤6) di `CreateQuestion` & `EditQuestion` POST. Hapus **guard H3** (`:7972`) yang menolak edit soal >4 opsi. Pasang **guard edit-shrink** (D-418-02) yang menolak hapus opsi yang sudah dijawab peserta SEBELUM `SaveChangesAsync` (tutup hazard 999.14 FK Restrict 500). Tambah **max-6** ke validator. Generalisasi loop upsert opsi A–D → A–F.

Purpose: Ini adalah **keystone** Fase 418 — mengunci 3 keputusan keystone (radio `name` strategy, konvensi binding/id, penempatan guard) yang dikonsumsi Plan 03 (view/JS) dan Plan 04 (e2e). Server-authoritative; otoritas validasi & integritas data di sini.

Output: Kode produksi yang membuat test Plan 01 GREEN + kontrak action baru untuk markup Plan 03. migration=FALSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-CONTEXT.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-RESEARCH.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-UI-SPEC.md
@./CLAUDE.md

<interfaces>
<!-- Kontrak existing yang di-refactor — VERIFIED file:line 2026-06-24 -->

CreateQuestion POST signature LAMA (AssessmentAdminController.cs:7695-7708):
```csharp
public async Task<IActionResult> CreateQuestion(
    int packageId, string questionText, string questionType, int scoreValue,
    string? elemenTeknis, string? rubrik, int maxCharacters,
    string? optionA, string? optionB, string? optionC, string? optionD,
    bool correctA, bool correctB, bool correctC, bool correctD,
    IFormFile? questionImage, string? questionImageAlt,
    IFormFile? optionAImage..optionDImage, string? optionAImageAlt..optionDImageAlt,
    int? sectionId = null)
```

EditQuestion POST signature LAMA (:7915-7930): sama + `removeQuestionImage` + `removeOption{A..D}Image`.

EditQuestion GET JSON shape (:7897 — JANGAN UBAH):
```csharp
options = q.Options.OrderBy(o => o.Id).Select(o => new {
    optionText = o.OptionText, isCorrect = o.IsCorrect,
    imagePath = o.ImagePath, imageAlt = o.ImageAlt }).ToList()
```

QuestionOptionValidator.ValidateQuestionOptions(type, string?[] texts, bool[] corrects) — Helpers/QuestionOptionValidator.cs:20. Sudah variable-length.

ApplyOptionImageIntent(PackageOption target, IFormFile? newFile, string? alt, bool removeChecked, int packageId, List<string> deleteList) — :8147. REUSE apa adanya.

Guard H3 yang DIHAPUS (:7972): `if (q.Options.Count > 4) { TempData["Error"]=...; return RedirectToAction(...); }`

Loop upsert existing (:8061-8097): for i<4 dengan 4 cabang (update/remove/add/skip). Generalisasi ke ≤6.

FK Restrict: PackageUserResponse.PackageOptionId (int? nullable, Models/PackageUserResponse.cs:19) → PackageOption = DeleteBehavior.Restrict (ApplicationDbContext.cs:561-564).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OptionInput model + OptionShrinkGuard helper + validator max-6</name>
  <read_first>
    - 418-RESEARCH.md Pattern 1 (OptionInput model) + §"Validator Max-6"
    - Helpers/QuestionOptionValidator.cs (tambah max-6 setelah filled<2)
    - Models/EditPesertaAnswersViewModel.cs:17 (precedent List binding di proyek)
    - HcPortal.Tests/OptionValidationTests.cs + EditShrinkGuardLogicTests.cs (test Plan 01 yang harus GREEN)
  </read_first>
  <behavior>
    - `OptionShrinkGuard.FindBlockedOptionIds(removed, answered)` = irisan distinct (test Plan 01 GREEN).
    - `QuestionOptionValidator` menolak `filled > 6` dgn pesan "Maksimal 6 opsi per soal." (test `MaxSix_Rejected` GREEN); 5/6-opsi accept tetap hijau.
  </behavior>
  <action>
    1. Buat `Models/OptionInput.cs`:
    ```csharp
    namespace HcPortal.Models;
    public class OptionInput {
        public string? Text { get; set; }
        public bool IsCorrect { get; set; }
        public IFormFile? Image { get; set; }     // authoring only
        public string? ImageAlt { get; set; }
        public bool RemoveImage { get; set; }
    }
    ```
    Whitelist properti eksplisit — JANGAN tambah `Id` (cegah mass-assignment T-418-06). `using Microsoft.AspNetCore.Http;` untuk IFormFile.
    2. Buat `Helpers/OptionShrinkGuard.cs` dgn signature kontrak Plan 01:
    ```csharp
    public static IReadOnlyList<int> FindBlockedOptionIds(IEnumerable<int> removedOptionIds, IEnumerable<int> answeredOptionIds)
        => removedOptionIds.Intersect(answeredOptionIds).Distinct().ToList();
    ```
    (Bila Plan 01 sudah membuat stub, ganti body NotImplementedException dgn implementasi nyata.)
    3. `Helpers/QuestionOptionValidator.cs` — tambah SETELAH cek `filled < 2`:
    ```csharp
    if (filled > 6) return (false, "Maksimal 6 opsi per soal.");
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "class OptionInput" Models/OptionInput.cs` cocok; TIDAK ada properti `Id`.
    - `grep -n "FindBlockedOptionIds" Helpers/OptionShrinkGuard.cs` cocok dgn body Intersect.
    - `grep -n "filled > 6" Helpers/QuestionOptionValidator.cs` cocok dgn pesan "Maksimal 6 opsi per soal.".
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~OptionValidation"` GREEN (max-6 + 5/6-opsi).
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~EditShrinkGuard"` GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5 && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~OptionValidation|FullyQualifiedName~EditShrinkGuard" 2>&1 | tail -15</automated>
  </verify>
  <done>OptionInput.cs + OptionShrinkGuard.cs ada; validator punya max-6; build 0-err; test Plan 01 (OptionValidation max-6 + EditShrinkGuard) GREEN. Commit `feat(418-02): OptionInput model + OptionShrinkGuard + validator max-6 (GREEN)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CreateQuestion POST → List&lt;OptionInput&gt; binding (≤6) + validator wiring</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:7695-7910 (CreateQuestion POST penuh — signature, image-validate loop, correctCount gate, validator call, persist loop)
    - 418-RESEARCH.md Pattern 1 + flag #1 (radio name) + Pitfall 4 (empty trailing rows)
    - Helpers/QuestionOptionValidator.cs (call site :7773)
    - 418-UI-SPEC.md C4 (radio↔checkbox)
  </read_first>
  <behavior>
    - CreateQuestion menerima `List<OptionInput> options` + `int? correctIndex` (MC) menggantikan `optionA..D`/`correctA..D` + 12 param gambar opsi.
    - Untuk MC: `correctIndex` menentukan satu opsi benar (set `options[correctIndex].IsCorrect=true`); untuk MA: baca `options[i].IsCorrect`.
    - Validasi gambar fail-fast loop SEMUA `options[i].Image` (+ questionImage) — termasuk E/F.
    - Validator dipanggil dgn `options.Select(o=>o.Text).ToArray()` + `options.Select(o=>o.IsCorrect).ToArray()` → max-6 ditegakkan.
    - Persist: skip opsi `IsNullOrWhiteSpace(Text)` (Pitfall 4 — trailing rows kosong diabaikan, mirror import §171).
  </behavior>
  <action>
    Refactor signature CreateQuestion POST: hapus `optionA..D`, `correctA..D`, `optionAImage..optionDImage`, `optionAImageAlt..optionDImageAlt`. Tambah `List<OptionInput> options` + `int? correctIndex`. Pertahankan `packageId, questionText, questionType, scoreValue, elemenTeknis, rubrik, maxCharacters, questionImage, questionImageAlt, sectionId`.
    - **Radio MC resolusi (flag #1, KEYSTONE — LOCK):** untuk `questionType=="MultipleChoice"`, abaikan `options[i].IsCorrect` dari binding dan set kebenaran via `correctIndex` (radio native single-select kirim 1 nilai). Bila `correctIndex.HasValue && correctIndex >= 0 && < options.Count` → `options[correctIndex].IsCorrect=true`, sisanya false. Untuk `MultipleAnswer` → pakai `options[i].IsCorrect` checkbox apa adanya. **Dokumentasikan keputusan ini sebagai kontrak untuk Plan 03 markup.**
    - Image-validate loop: `foreach (var f in new[]{questionImage}.Concat(options.Select(o=>o.Image)))` → `FileUploadHelper.ValidateImageFile(f)` fail-fast (V12, T-418-04). Pertahankan whitelist QuestionType + Section IDOR guard + scoreValue 1–100 GATE existing (jangan ubah).
    - correctCount gate: hitung dari `options` (MC==1, MA>=2) — sumber kebenaran setelah resolusi correctIndex.
    - Validator: `QuestionOptionValidator.ValidateQuestionOptions(questionType, options.Select(o=>o.Text).ToArray(), options.Select(o=>o.IsCorrect).ToArray())`.
    - Persist loop (questionType != "Essay"): `foreach (var inp in options)` → skip `string.IsNullOrWhiteSpace(inp.Text)`; untuk yang terisi: `SaveFileAsync(inp.Image, ...)` + `new PackageOption{OptionText=inp.Text.Trim(), IsCorrect=inp.IsCorrect, ImagePath=..., ImageAlt=TruncateAlt(inp.ImageAlt,255)}`. Cap pada 6 (validator sudah menolak >6; persist `Take(6)` defensif).
    - Pertahankan `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`.
  </action>
  <acceptance_criteria>
    - `grep -n "List<OptionInput> options" Controllers/AssessmentAdminController.cs` cocok di CreateQuestion.
    - `grep -n "optionA\b\|correctA\b" Controllers/AssessmentAdminController.cs` TIDAK menemukan param diskret di signature CreateQuestion (cek block :7695-7708 lama hilang).
    - `grep -n "correctIndex" Controllers/AssessmentAdminController.cs` cocok (resolusi MC).
    - `grep -n "ValidateImageFile" Controllers/AssessmentAdminController.cs` masih ada di CreateQuestion (loop dinamis).
    - `dotnet build HcPortal.csproj` 0-error.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>CreateQuestion POST bind List&lt;OptionInput&gt; + correctIndex (MC single-select via index); validator + image-loop + Section IDOR + antiforgery utuh; build 0-err. Commit `feat(418-02): CreateQuestion list-binding opsi dinamis + correctIndex MC`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EditQuestion POST refactor + hapus guard H3 + guard edit-shrink + loop upsert A–F</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:7910-8166 (EditQuestion POST penuh: signature, guard H3 :7972, correctCount, validator :8005, image-intent, loop upsert :8061-8097, SaveChanges :8102, ApplyOptionImageIntent :8147)
    - 418-RESEARCH.md §"Edit-Shrink Guard" (pola removedOptionIds + intersection) + Pitfall 1 (hapus H3) + Pitfall 2 (guard wajib) + Pattern 2 (loop upsert dinamis)
    - 418-UI-SPEC.md C5 (edit-shrink ditolak: TempData["Error"] full-page) + Copywriting Contract (pesan persis)
    - Helpers/OptionShrinkGuard.cs (FindBlockedOptionIds dari Task 1)
  </read_first>
  <behavior>
    - EditQuestion menerima `List<OptionInput> options` + `int? correctIndex` + `removeQuestionImage` (param gambar opsi diskret dihapus, kini per-OptionInput).
    - Guard H3 (tolak >4 opsi) DIHAPUS → soal 5–6 opsi editable.
    - SEBELUM Remove/SaveChanges: hitung `removedOptionIds` (opsi existing yang akan dihapus: posisi tak terisi lagi ATAU konversi Essay) → query `PackageUserResponses` untuk Id yang sudah dijawab → `OptionShrinkGuard.FindBlockedOptionIds(...)`. Bila ada blokir → `TempData["Error"]` (copy UI-SPEC C5) + `RedirectToAction("ManagePackageQuestions", new {packageId})` TANPA SaveChanges (full-page, BUKAN JSON).
    - Loop upsert generalisasi ke ≤6 (Math.Max(filledCount, existing.Count)), preserve Id + gambar (cabang update/remove/add/skip sama).
    - GET JSON shape TIDAK berubah.
  </behavior>
  <action>
    1. Refactor signature EditQuestion POST sama seperti CreateQuestion (Task 2): `List<OptionInput> options` + `int? correctIndex`, pertahankan `questionId, packageId, ..., questionImage, questionImageAlt, removeQuestionImage, sectionId`. Hapus `optionA..D`, `correctA..D`, `optionAImage..D`, `optionAImageAlt..D`, `removeOption{A..D}Image`.
    2. **HAPUS guard H3** seluruh block `:7972-7976` (`if (q.Options.Count > 4) {...}`). Update komentar di bawahnya (:8098-8099) yang merujuk H3.
    3. Resolusi MC correctIndex (sama Task 2) + correctCount gate dari `options` + validator dipanggil dgn `options.Select(...)` (sama Task 2). Image-validate loop dinamis termasuk E/F.
    4. **GUARD EDIT-SHRINK (D-418-02), SETELAH validator lulus & SEBELUM mutasi opsi/SaveChanges:**
    ```csharp
    var existingOpts = q.Options.OrderBy(o => o.Id).ToList();
    int filled = options.Count(o => !string.IsNullOrWhiteSpace(o.Text));
    int keep = Math.Min(filled, 6);
    var removedOptionIds = new List<int>();
    if (questionType == "Essay")
        removedOptionIds.AddRange(existingOpts.Select(o => o.Id));
    else
        for (int i = 0; i < existingOpts.Count; i++)
            if (i >= keep) removedOptionIds.Add(existingOpts[i].Id);
            // catatan: posisi i terisi/tidak ditentukan urutan binding; lihat Pitfall 4 — gunakan urutan options stabil
    if (removedOptionIds.Count > 0) {
        var answered = await _context.PackageUserResponses
            .Where(r => r.PackageOptionId.HasValue && removedOptionIds.Contains(r.PackageOptionId.Value))
            .Select(r => r.PackageOptionId!.Value).Distinct().ToListAsync();
        var blocked = OptionShrinkGuard.FindBlockedOptionIds(removedOptionIds, answered);
        if (blocked.Count > 0) {
            // huruf opsi terblok (posisi di existingOpts) untuk pesan jelas — opsional, murah
            TempData["Error"] = "Opsi yang sudah dijawab peserta tidak bisa dihapus. Batalkan perubahan atau pertahankan opsi tersebut.";
            return RedirectToAction("ManagePackageQuestions", new { packageId });
        }
    }
    ```
    PENTING: penentuan "opsi mana yang akan dihapus" harus konsisten dgn loop upsert berikutnya. Karena binding `options` index-aligned dgn `existingOpts` (OrderBy Id), opsi pada posisi i dihapus bila `i >= keep` ATAU `options[i].Text` kosong. Gunakan aturan SAMA persis di guard dan di loop upsert (kill-drift). Pesan WAJIB pakai copy UI-SPEC C5 (boleh sertakan huruf/teks opsi terblok bila murah).
    5. Loop upsert generalisasi (Pattern 2 RESEARCH): `for (int i = 0; i < Math.Max(keep, existingOpts.Count); i++)` dengan 4 cabang existing (update in-place preserve Id+gambar / remove / add / skip), `ApplyOptionImageIntent(slot, inp.Image, inp.ImageAlt, inp.RemoveImage, packageId, imagePathsToDelete)`. Essay branch tetap RemoveRange (sudah lolos guard). `await _context.SaveChangesAsync()` + audit + `SyncToPostIfSamePackageAsync` + `DeleteUnreferencedAsync` UTUH.
    6. Pertahankan `[Authorize]` + `[ValidateAntiForgeryToken]`. GET JSON (:7863-7909) TIDAK disentuh.
  </action>
  <acceptance_criteria>
    - `grep -n "q.Options.Count > 4" Controllers/AssessmentAdminController.cs` → 0 hasil (guard H3 hilang).
    - `grep -n "menyusul\|belum dapat diedit" Controllers/AssessmentAdminController.cs` → 0 hasil.
    - `grep -n "FindBlockedOptionIds" Controllers/AssessmentAdminController.cs` cocok di EditQuestion (sebelum SaveChanges).
    - `grep -n "List<OptionInput> options" Controllers/AssessmentAdminController.cs` cocok di EditQuestion.
    - `grep -n "options = q.Options.OrderBy(o => o.Id)" Controllers/AssessmentAdminController.cs` MASIH ada (GET JSON shape utuh).
    - `dotnet build HcPortal.csproj` 0-error + `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full suite GREEN (no regresi grading/option-validation).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5 && dotnet test HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | tail -12</automated>
  </verify>
  <done>EditQuestion POST: list-binding + correctIndex; guard H3 DIHAPUS; guard edit-shrink terpasang sebelum SaveChanges (TempData full-page, tutup 999.14); loop upsert A–F preserve Id+gambar; GET JSON utuh; build 0-err; full xUnit suite GREEN. Commit `feat(418-02): EditQuestion list-binding + edit-shrink guard + drop H3 + loop A–F`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → CreateQuestion/EditQuestion POST | HC (Admin/HC role) mengirim multipart form: teks opsi, flag benar, file gambar, sectionId. Input tak-tepercaya menyeberang di sini. |
| Controller → DB (EF Core) | Mutasi PackageOption (add/update/remove) + query PackageUserResponse. FK Restrict aktif. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-418-03 | Tampering | CSRF pada POST authoring | mitigate | Pertahankan `[ValidateAntiForgeryToken]` di Create/EditQuestion (:7690/:7913) — JANGAN dihapus saat refactor signature. |
| T-418-04 | Tampering | Upload gambar opsi 5–6 (E/F) tak tervalidasi | mitigate | Perluas loop `FileUploadHelper.ValidateImageFile` ke `options[i].Image` SEMUA index (jangan lupa E/F). Jangan hand-roll (V12 ASVS). |
| T-418-05 | Denial of Service / Tampering | FK-Restrict 500 saat hapus opsi terjawab (hazard 999.14) | mitigate | Guard edit-shrink: `OptionShrinkGuard.FindBlockedOptionIds` query existence SEBELUM SaveChanges → TempData + redirect, bukan 500. |
| T-418-06 | Tampering / Elevation | Mass-assignment opsi via binding (client kirim Id) | mitigate | `OptionInput` whitelist properti eksplisit (Text/IsCorrect/Image/ImageAlt/RemoveImage) — TIDAK ada `Id` dari client; Id opsi ditentukan server (preserve via existing[i]). |
| T-418-07 | Elevation / Info Disclosure | IDOR Section/Package | mitigate | Pertahankan Section IDOR guard (:7735/:7951) + packageId scoping — tak diubah refactor. |
| T-418-08 | Spoofing / Elevation | Akses authoring oleh non-Admin/HC | mitigate | Pertahankan `[Authorize(Roles="Admin, HC")]` (:7689/:7913). |
| T-418-09 | Tampering | Bypass batas max-6 dari form | mitigate | Validator server-authoritative `filled > 6` + persist `Take(6)` defensif. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0-error.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full suite GREEN (OptionValidation max-6, EditShrinkGuard, grading regresi, no regresi 386/415).
- grep: guard H3 hilang; `FindBlockedOptionIds` ada di EditQuestion; `List<OptionInput>` di kedua action; GET JSON shape utuh; antiforgery+authorize utuh.
- Tidak ada migration (verify-migration empty).
</verification>

<success_criteria>
- CreateQuestion/EditQuestion POST menerima `List<OptionInput>` (2–6) + `int? correctIndex` (MC single-select).
- Guard H3 dihapus; soal 5–6 opsi import (415) dapat diedit.
- Guard edit-shrink menolak hapus opsi terjawab via TempData full-page (bukan 500); tutup backlog 999.14 di jalur ini.
- Validator menegakkan max-6; persist mengabaikan baris kosong (≤6).
- GET JSON `options[]` shape tak berubah (backward-compat populateEditForm).
- Kontrak radio `name="correctIndex"` (MC) + `options[i].IsCorrect` (MA) + binding index terdokumentasi untuk Plan 03.
- Full xUnit suite GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/418-opsi-jawaban-dinamis-2-6/418-02-SUMMARY.md` — WAJIB sertakan kontrak markup untuk Plan 03: nama field exact (`options[i].Text`, `options[i].IsCorrect`, `correctIndex` value=index untuk MC radio, `options[i].Image`, `options[i].ImageAlt`, `options[i].RemoveImage`), konvensi id (`option_{letter}`), dan aturan "posisi i dihapus bila i>=keep atau Text kosong".
</output>
