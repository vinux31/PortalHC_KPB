---
phase: 418-opsi-jawaban-dinamis-2-6
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Views/Admin/ManagePackageQuestions.cshtml
  - Views/Admin/_InjectQuestionForm.cshtml
  - Views/Admin/InjectAssessment.cshtml
  - Views/CMP/StartExam.cshtml
  - Views/CMP/Results.cshtml
  - Views/CMP/ExamSummary.cshtml
  - Views/Admin/_PreviewQuestion.cshtml
  - Views/Admin/PreviewPackage.cshtml
  - tests/e2e/helpers/wizardSelectors.ts
autonomous: true
requirements: [OPT-01, OPT-02]
tags: [razor, javascript, ui, render, options]

must_haves:
  truths:
    - "Form authoring mulai 4 baris opsi; '+ Tambah Opsi' menambah s/d 6 (disabled @6); 'Hapus' aktif hanya >2 baris"
    - "Tambah/hapus baris me-re-letter A–F + renumber name index + reasosiasi blok gambar ke baris-nya"
    - "populateEditForm enumerasi dinamis data.options.length (prefill 5–6 baris untuk soal import)"
    - "Form Inject (client-side) mendukung tambah/hapus baris A–F tanpa perubahan kontrak server"
    - "StartExam/Results/ExamSummary/_PreviewQuestion menampilkan huruf A–F dinamis"
    - "PreviewPackage opsi ke-6 tampil 'F' (bukan 'A') — bug modulo diperbaiki"
  artifacts:
    - path: "Views/Admin/ManagePackageQuestions.cshtml"
      provides: "Baris opsi dinamis + addOptionBtn + remove-btn + re-letter JS + populateEditForm dinamis + IMAGE_FIELDS dinamis + #authError role=alert"
      contains: "addOptionBtn"
    - path: "Views/Admin/PreviewPackage.cshtml"
      provides: "Array A–F tanpa modulo wrap"
      contains: "letters[optIdx]"
    - path: "tests/e2e/helpers/wizardSelectors.ts"
      provides: "Selektor optionE/F, correctE/F, addOptionBtn, removeOptionBtn"
      contains: "addOptionBtn"
  key_links:
    - from: "Views/Admin/ManagePackageQuestions.cshtml form"
      to: "Controllers/AssessmentAdminController.cs CreateQuestion/EditQuestion"
      via: "name='options[i].Text' / name='correctIndex' / name='options[i].Image' (kontrak Plan 02)"
      pattern: "options\\[.*\\]\\.Text"
    - from: "Views/CMP/StartExam.cshtml"
      to: "render huruf opsi"
      via: "letters array {A..F} index-derived fallback numerik"
      pattern: "\\{ \"A\", \"B\", \"C\", \"D\", \"E\", \"F\" \\}"
---

<objective>
Wujudkan UI opsi dinamis 2–6 di lapisan presentasi: (1) form authoring (`ManagePackageQuestions.cshtml`) jadi baris dinamis dgn tombol Tambah/Hapus + re-letter A–F + reasosiasi gambar saat hapus baris-tengah + `populateEditForm`/`IMAGE_FIELDS` dinamis; (2) form Inject (`_InjectQuestionForm.cshtml` + `InjectAssessment.cshtml` JS) baris dinamis tanpa gambar; (3) perluas array huruf render `{A..D}`→`{A..F}` di 5 view; (4) perbaiki bug modulo `PreviewPackage.cshtml`; (5) extend `wizardSelectors.ts`.

Purpose: Mengonsumsi kontrak field Plan 02 (`options[i].Text`, `correctIndex`, `options[i].Image`) dan menyelesaikan OPT-01 (UI tambah/hapus 2–6) + OPT-02 (render A–F). Lesson 354 — Razor/JS WAJIB diuji real-browser (Plan 04). File-overlap `StartExam.cshtml` dgn Phase 417 sudah selesai (417 CLOSED).

Output: View + JS yang membuat form authoring & inject mendukung 2–6 opsi dengan huruf A–F dinamis di semua layar. migration=FALSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-CONTEXT.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-RESEARCH.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-UI-SPEC.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Kontrak field dari Plan 02 (baca 418-02-SUMMARY untuk yang final). VERIFIED markup existing 2026-06-24. -->

Field POST yang HARUS dikirim form (kontrak Plan 02):
- `options[i].Text` (input teks per baris, i=0..5)
- `correctIndex` (radio MC: SATU name grup, value=index baris) — single-select native
- `options[i].IsCorrect` (checkbox MA: per-index, value="true")
- `options[i].Image` (file authoring), `options[i].ImageAlt`, `options[i].RemoveImage`
- Konvensi id: `option_{letter}` (A–F) + `correct_{letter}` — backward-compat e2e + populateEditForm.

Markup baris existing (ManagePackageQuestions.cshtml:395-429 @foreach 4-tuple A–D hardcoded + blok gambar inline opt{letter}Img*). Inject (_InjectQuestionForm.cshtml:35-48, sama tanpa gambar).

populateEditForm existing (:668-722): arrays hardcoded optLetters/optFields/correctFields = [A,B,C,D]; loop `if (i < 4)`; IMAGE_FIELDS (:726-732) 5 entri statis. applyQTypeSwitch (:620-636) toggle radio↔checkbox `.correct-input`.

Render sites (VERIFIED file:line):
- StartExam.cshtml :137 (MA) + :170 (MC): `string[] letters = { "A", "B", "C", "D" };` + `oi < letters.Length ? letters[oi] : (oi+1).ToString()`
- Results.cshtml :363, ExamSummary.cshtml :57, _PreviewQuestion.cshtml :50 — sama pola
- PreviewPackage.cshtml :6 `string[] letters = { "A", "B", "C", "D", "E" };` (cap-E) + :62 `@letters[optIdx % letters.Length]` (MODULO BUG)

Inject JS reader (InjectAssessment.cshtml :1144/:1173): loop `['A','B','C','D']` baca `option_L`/`correct_L`; :1139 injResetAuthoringForm; :1069 applyQTypeSwitch (versi inject). Preview inject :1476 sudah ['A'..'F'].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render huruf A–F (5 view) + perbaiki bug modulo PreviewPackage</name>
  <read_first>
    - Views/CMP/StartExam.cshtml :130-180 (dua array letters MA + MC)
    - Views/CMP/Results.cshtml :355-370 ; Views/CMP/ExamSummary.cshtml :50-65 ; Views/Admin/_PreviewQuestion.cshtml :45-60
    - Views/Admin/PreviewPackage.cshtml :1-70 (array :6 + index :62 modulo)
    - 418-RESEARCH.md §"Render A–F — Site Inventory" + 418-UI-SPEC.md C7
  </read_first>
  <behavior>
    - Soal 6-opsi: huruf E & F tampil benar di ujian/hasil/ringkasan/preview-modal.
    - PreviewPackage soal 6-opsi: opsi ke-6 tampil "F" (BUKAN "A" akibat modulo).
    - Soal 4-opsi lama: tetap tampil A–D identik (array superset, fallback numerik).
  </behavior>
  <action>
    1. `StartExam.cshtml` :137 dan :170: ubah `{ "A", "B", "C", "D" }` → `{ "A", "B", "C", "D", "E", "F" }` (DUA tempat). Pola `oi < letters.Length ? letters[oi] : (oi+1).ToString()` dipertahankan.
    2. `Results.cshtml` :363, `ExamSummary.cshtml` :57, `_PreviewQuestion.cshtml` :50: array → `{ "A", "B", "C", "D", "E", "F" }`.
    3. `PreviewPackage.cshtml`: :6 array → `{ "A", "B", "C", "D", "E", "F" }` (6, bukan cap-E); :62 GANTI `@letters[optIdx % letters.Length]` → `@(optIdx < letters.Length ? letters[optIdx] : (optIdx + 1).ToString())` (no wrap). Hilangkan modulo.
    Tidak ada perubahan styling/penanda-benar (success badge tetap). Bahasa display tak berubah.
  </action>
  <acceptance_criteria>
    - `grep -c '"A", "B", "C", "D", "E", "F"' Views/CMP/StartExam.cshtml` ≥ 2.
    - `grep -n '"A", "B", "C", "D", "E", "F"' Views/CMP/Results.cshtml Views/CMP/ExamSummary.cshtml Views/Admin/_PreviewQuestion.cshtml Views/Admin/PreviewPackage.cshtml` cocok di tiap file.
    - `grep -n "% letters.Length" Views/Admin/PreviewPackage.cshtml` → 0 hasil (modulo dihapus).
    - `dotnet build HcPortal.csproj` 0-error.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>5 view render A–F; PreviewPackage modulo dihapus (opsi ke-6="F"); build 0-err. Commit `feat(418-03): render huruf A–F 5 view + fix bug modulo PreviewPackage`.</done>
</task>

<task type="auto">
  <name>Task 2: Form authoring dinamis — markup baris + Tambah/Hapus + re-letter + reasosiasi gambar + populateEditForm/IMAGE_FIELDS dinamis</name>
  <read_first>
    - Views/Admin/ManagePackageQuestions.cshtml :388-431 (markup opsi+gambar), :547-839 (JS: applyQTypeSwitch :620, loadEditForm :654, populateEditForm :668, IMAGE_FIELDS :726, wireImageField/prefillImage/showThumb)
    - 418-UI-SPEC.md C1–C6 + Accessibility + Copywriting Contract (semua copy BI)
    - 418-RESEARCH.md Pattern 1 (flag #1/#2), Pitfall 5 (populateEditForm asumsi 4), Pitfall 6 (reasosiasi gambar baris-tengah), Code Examples (re-letter JS)
    - 418-02-SUMMARY.md (kontrak field final: options[i].Text, correctIndex, options[i].Image...)
  </read_first>
  <behavior>
    - Form fresh: 4 baris (A–D); tombol "+ Tambah Opsi" menambah baris s/d 6 (disabled @6, title="Maksimal 6 opsi"); tombol "Hapus" per baris ekstra, hidden saat baris==2.
    - Tambah/hapus → re-letter A–F semua baris (data-letter span, placeholder, aria-label, id=option_{letter}/correct_{letter}, name=options[i].Text + renumber index) + reasosiasi blok gambar (gambar terikat node baris, ikut pindah/hapus saat baris di atasnya dihapus).
    - Hapus baris-tengah B saat C punya gambar → gambar tetap di soal yang benar (C jadi B, gambarnya ikut).
    - `applyQTypeSwitch` dipanggil ulang saat tambah baris (radio/checkbox baru ter-wire); MC radio share satu `name="correctIndex"` value=index → single-select lintas baris; MA checkbox per-index.
    - `populateEditForm` enumerasi `data.options.length` (prefill 4 ATAU 5/6 baris); IMAGE_FIELDS dibangun dinamis 0..n dari DOM aktual; `#authError` punya `role="alert"`.
  </behavior>
  <action>
    1. **Markup baris (`#optionsSection`):** ganti `@foreach` 4-tuple hardcoded (:395-429) jadi render 4 baris awal via loop index 0..3 dengan struktur per-baris UI-SPEC C1: `data-option-row data-index="@i"`, sel selektor `width:36px` `.correct-input` (radio `name="correctIndex"` value=`@i` untuk MC awal; JS toggle ke checkbox `name="options[@i].IsCorrect"` saat MA), span `data-letter` huruf, input teks `name="options[@i].Text" id="option_@letter"` placeholder/aria, tombol hapus `.remove-option-btn` (`btn btn-outline-danger`, ikon `bi-x-lg`, aria-label "Hapus opsi {letter}", hidden saat baris==minimum). Blok gambar inline per baris (pertahankan struktur opt{letter}Img* tapi name field jadi `options[@i].Image`/`options[@i].ImageAlt`/`options[@i].RemoveImage` + id berbasis letter agar IMAGE_FIELDS dinamis menemukannya; ms-4 indentasi pertahankan).
    2. **Tombol "+ Tambah Opsi" (C2):** `<button type="button" class="btn btn-outline-primary btn-sm mt-1" id="addOptionBtn"><i class="bi bi-plus-circle me-1"></i>Tambah Opsi</button>` setelah daftar baris, sebelum form-text bantuan.
    3. **`#authError` (flag #3/C5):** tambah `<div id="authError" class="alert alert-danger py-1 px-2 small d-none mb-2" role="alert"></div>` di `#optionsSection` untuk pesan client-side min/max/correct-hilang.
    4. **JS:** tambah `var LETTERS=['A','B','C','D','E','F'];` + fungsi `reletterRows()` (re-letter + renumber name index + relabel aria + reasosiasi gambar prefix, lihat RESEARCH Code Examples), `addOptionRow()` (clone template baris s/d 6, panggil applyQTypeSwitch+reletter, fokus ke input baru), `removeOptionRow(btn)` (hapus node baris utuh termasuk blok gambar bila baris>2, reletter, fokus ke baris sebelumnya). Wire `addOptionBtn` click + delegate `.remove-option-btn`. Toggle disabled add @6 + hidden hapus saat ==2. Untuk MC radio: pastikan satu `name="correctIndex"` (re-set value=index saat reletter); MA checkbox: `name="options[i].IsCorrect"`.
    5. **populateEditForm (Pitfall 5):** HAPUS arrays hardcoded optLetters/optFields/correctFields; loop `data.options.forEach((opt,i)=>{...})` TANPA `if (i<4)`; sebelum prefill, bangun baris sampai `data.options.length` (panggil addOptionRow s/d cukup, minimum 2); set `option_{letter}`/checked + prefill gambar via IMAGE_FIELDS[i] dinamis. Untuk MC, set radio `correctIndex` ke index opsi yang isCorrect.
    6. **IMAGE_FIELDS dinamis (C6):** ganti array statis 5-entri jadi builder `function buildImageFields(){ return [question, ...rows] }` dari DOM aktual (0..n baris). prefillImage/resetImageField/wireImageField pakai builder ini.
    7. Semua copy BI sesuai Copywriting Contract (tombol, error min-2/max-6/correct-hilang). Keyboard: tombol type=button; fokus dipindah setelah tambah/hapus (Accessibility UI-SPEC).
  </action>
  <acceptance_criteria>
    - `grep -n "addOptionBtn" Views/Admin/ManagePackageQuestions.cshtml` cocok (markup + JS wire).
    - `grep -n "remove-option-btn" Views/Admin/ManagePackageQuestions.cshtml` cocok.
    - `grep -n 'name="correctIndex"\|correctIndex' Views/Admin/ManagePackageQuestions.cshtml` cocok (radio MC single-name).
    - `grep -n "options\[" Views/Admin/ManagePackageQuestions.cshtml` cocok (name index-based).
    - `grep -n 'role="alert"' Views/Admin/ManagePackageQuestions.cshtml` cocok (#authError).
    - `grep -n "if (i < 4)" Views/Admin/ManagePackageQuestions.cshtml` → 0 hasil (populateEditForm dinamis).
    - `grep -n "LETTERS" Views/Admin/ManagePackageQuestions.cshtml` cocok (re-letter A–F).
    - `dotnet build HcPortal.csproj` 0-error.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>Form authoring: 4 baris awal, Tambah s/d 6 (disabled @6), Hapus >2, re-letter A–F, reasosiasi gambar baris-tengah, populateEditForm + IMAGE_FIELDS dinamis, #authError role=alert, radio MC single-name correctIndex. Build 0-err. Commit `feat(418-03): form authoring opsi dinamis 2-6 + re-letter + reasosiasi gambar`.</done>
</task>

<task type="auto">
  <name>Task 3: Form Inject dinamis (client-side) + extend wizardSelectors.ts</name>
  <read_first>
    - Views/Admin/_InjectQuestionForm.cshtml :30-80 (markup 4-tuple A–D + #injAuthError + #maLabel + applyQTypeSwitch inject)
    - Views/Admin/InjectAssessment.cshtml :1060-1210 (applyQTypeSwitch :1069, injResetAuthoringForm :1139, reader option_L/correct_L :1144/:1173 loop ['A','B','C','D'], injAddQuestionBtn, #QuestionsJson serialize) + :1476 preview (sudah A–F)
    - tests/e2e/helpers/wizardSelectors.ts :109-133 (questionFormSelectors: option_A..D, correct_A..D, opt{A..D}ImgField/ImageAlt)
    - 418-UI-SPEC.md C1 (inject = input-group saja, TANPA gambar) + 418-RESEARCH.md §"Surface tambahan"
  </read_first>
  <behavior>
    - Form Inject: 4 baris awal, "+ Tambah Opsi" s/d 6, "Hapus" >2, re-letter A–F (TANPA blok gambar — scope 394). applyQTypeSwitch inject toggle radio↔checkbox.
    - Reader client (`injQuestions[]`) enumerasi baris dinamis 0..n (bukan hardcoded ['A','B','C','D']) → `option_{letter}`/`correct_{letter}` per baris terisi → serialize ke `#QuestionsJson`. KONTRAK SERVER TIDAK BERUBAH (parsing client-side, deserialize sama).
    - injResetAuthoringForm reset ke 4 baris default.
    - wizardSelectors.ts punya selektor E/F + addOptionBtn + removeOptionBtn.
  </behavior>
  <action>
    1. `_InjectQuestionForm.cshtml`: ganti `@foreach` 4-tuple (:35-48) jadi markup baris dinamis (sama pola Task 2 TAPI tanpa blok gambar) + tombol `addOptionBtn` (id unik inject, mis. `injAddOptionBtn`) + `.remove-option-btn`. Radio MC single `name` grup (inject pakai client-state, jadi `name` bisa lokal — yang penting reader baca per baris). Pertahankan `#injAuthError` + `#maLabel`.
    2. `InjectAssessment.cshtml`: tambah JS re-letter + addOptionRow/removeOptionRow untuk form inject (mirror Task 2 tanpa gambar/IMAGE_FIELDS); ubah reader (:1144/:1173) dari loop `['A','B','C','D']` jadi enumerasi baris DOM aktual (`document.querySelectorAll('#inj... [data-option-row]')`) baca teks+correct per baris terisi → push ke option object. `injResetAuthoringForm` reset ke 4 baris. applyQTypeSwitch inject re-wire input baru. Preview :1476 sudah A–F — tak diubah.
    3. `tests/e2e/helpers/wizardSelectors.ts`: tambah ke `questionFormSelectors`: `optionE`, `optionF` (`#option_E`/`#option_F`), `correctE`, `correctF`, `optEImgField`/`optFImgField` + `optEImageAlt`/`optFImageAlt` (pola E/F dari A–D existing), `addOptionBtn` (`#addOptionBtn`), `removeOptionBtn` (`.remove-option-btn`).
  </action>
  <acceptance_criteria>
    - `grep -n "data-option-row\|injAddOptionBtn\|remove-option-btn" Views/Admin/_InjectQuestionForm.cshtml` cocok.
    - `grep -n "\['A','B','C','D'\]\|\['A', 'B', 'C', 'D'\]" Views/Admin/InjectAssessment.cshtml` → 0 hasil di reader injQuestions (loop hardcoded dihapus; preview A–F boleh tetap).
    - `grep -n "optionE\|addOptionBtn\|removeOptionBtn" tests/e2e/helpers/wizardSelectors.ts` cocok.
    - `dotnet build HcPortal.csproj` 0-error.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>Form Inject baris dinamis 2–6 (tanpa gambar); reader injQuestions[] enumerasi dinamis; injReset 4-baris; wizardSelectors.ts punya E/F + add/remove btn; build 0-err; kontrak server inject tak berubah. Commit `feat(418-03): form inject opsi dinamis 2-6 + extend wizardSelectors`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM/JS → form POST | Manipulasi baris via JS di DALAM form existing; token antiforgery tetap. Reader inject serialize client-state ke #QuestionsJson. |
| Razor render → browser | OptionText ditampilkan di ujian/hasil/preview. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-418-10 | Tampering | XSS via OptionText di render A–F | mitigate | Razor auto-encode (`@option.OptionText`) — JANGAN gunakan `Html.Raw`. Inject preview pakai `.textContent` (sudah ada). |
| T-418-11 | Tampering | Antiforgery hilang saat manipulasi form via JS | mitigate | Manipulasi baris di DALAM `#questionForm` existing (token `@Html.AntiForgeryToken()` tetap). JANGAN bangun form baru via JS (Pitfall 7). |
| T-418-12 | Tampering | Bypass max-6 dari client (tambah >6 baris) | accept | Client membatasi addBtn disabled @6 (UX); otoritas sebenarnya = validator server max-6 (Plan 02). Client-only = nicety, server-authoritative. |
| T-418-13 | Info Disclosure | Reasosiasi gambar salah baris → bocor/hilang gambar opsi | mitigate | Hapus node baris utuh + reletter (Pitfall 6); diuji eksplisit e2e flag#4 (Plan 04). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0-error (semua view + JS valid Razor).
- grep: render A–F di 5 view; modulo dihapus; addOptionBtn/remove-option-btn/correctIndex/role=alert di authoring; reader inject dinamis; wizardSelectors E/F.
- Verifikasi visual & interaksi (tambah/hapus/re-letter/render) di Plan 04 e2e + UAT live (lesson 354 — JS/Razor tak bisa diuji unit).
- Tidak ada migration.
</verification>

<success_criteria>
- Form authoring: 4 baris awal, Tambah s/d 6 (disabled @6), Hapus >2, re-letter A–F, reasosiasi gambar baris-tengah benar, populateEditForm + IMAGE_FIELDS dinamis, #authError role=alert.
- Form Inject: baris dinamis 2–6 tanpa gambar; reader enumerasi dinamis; kontrak server tak berubah.
- StartExam (2 site)/Results/ExamSummary/_PreviewQuestion render A–F; PreviewPackage opsi ke-6="F" (modulo fixed).
- wizardSelectors.ts punya optionE/F, correctE/F, addOptionBtn, removeOptionBtn.
- Soal 4-opsi lama render & prefill identik (backward-compat — diverifikasi Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/418-opsi-jawaban-dinamis-2-6/418-03-SUMMARY.md`
</output>
