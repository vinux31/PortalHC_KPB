---
phase: 418-opsi-jawaban-dinamis-2-6
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - HcPortal.Tests/EditShrinkGuardIntegrationTests.cs
  - tests/e2e/option-dynamic-418.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [OPT-01, OPT-02, OPT-03]
tags: [e2e, integration, uat, playwright, sqlserver]

must_haves:
  truths:
    - "Integration real-SQL: hapus opsi BELUM dijawab → boleh; opsi SUDAH dijawab → tidak 500, redirect + pesan"
    - "e2e: form authoring tambah s/d 6 (disabled@6), hapus s/d 2, re-letter A–F"
    - "e2e: hapus baris-tengah B saat C punya gambar → gambar tetap di soal benar (flag#4 KRITIS)"
    - "e2e: ambil ujian soal 6-opsi → huruf E,F tampil; PreviewPackage opsi ke-6 'F' (bukan 'A')"
    - "e2e: edit soal 5-opsi import → prefill 5 baris (flag#2)"
    - "e2e: form Inject tambah/hapus baris A–F"
    - "e2e: hapus opsi yang sudah dijawab → alert-danger (BUKAN 500)"
    - "UAT live @5277: alur opsi dinamis 2–6 berfungsi runtime (lesson 354)"
  artifacts:
    - path: "HcPortal.Tests/EditShrinkGuardIntegrationTests.cs"
      provides: "Integration real-SQL guard D-418-02 (seed PackageUserResponse, pola SectionFixRegressionTests)"
      contains: "EditShrinkGuard"
    - path: "tests/e2e/option-dynamic-418.spec.ts"
      provides: "e2e add/remove/disabled@6/min-2/re-letter/render A-F/preview-F/prefill-5/image-reassoc/edit-shrink-blocked"
      contains: "option-dynamic"
  key_links:
    - from: "tests/e2e/option-dynamic-418.spec.ts"
      to: "form authoring + StartExam render"
      via: "wizardSelectors addOptionBtn/removeOptionBtn/optionE/F"
      pattern: "addOptionBtn"
    - from: "HcPortal.Tests/EditShrinkGuardIntegrationTests.cs"
      to: "EditQuestion POST guard"
      via: "seed PackageUserResponse → assert no 500 + redirect"
      pattern: "PackageUserResponse"
---

<objective>
Tutup Wave 0 gap pengujian: (1) integration test real-SQL untuk guard edit-shrink (D-418-02) yang membuktikan hapus opsi terjawab TIDAK melempar 500 dan opsi belum-terjawab boleh dihapus; (2) Playwright e2e `option-dynamic-418.spec.ts` lengkap (add/remove/disabled@6/min-2/re-letter/render A–F/preview-F/prefill-5/reasosiasi-gambar-flag#4/edit-shrink-blocked); (3) checkpoint UAT live @5277 (lesson 354 — Razor/JS/SignalR WAJIB real-browser).

Purpose: OPT-01/02/03 acceptance membutuhkan bukti runtime (form interaktif + render Razor). Pure unit (Plan 01) hanya mencakup logika; UI & FK-guard butuh e2e + integration. Ini gate sebelum `/gsd-verify-work`.

Output: Suite test baru hijau + UAT live PASS. migration=FALSE. autonomous:false (UAT checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-CONTEXT.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-RESEARCH.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-VALIDATION.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-02-SUMMARY.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-03-SUMMARY.md
@./CLAUDE.md
@docs/SEED_WORKFLOW.md

<interfaces>
<!-- Pola test existing untuk di-clone -->
- Integration real-SQL: `HcPortal.Tests/SectionFixRegressionTests.cs` / `SubmitResurrectionTests.cs` — pola seed `PackageUserResponse` + real SQLEXPRESS fixture (MigrateAsync). Clone pola fixture+seed.
- e2e DB backup/restore: `tests/e2e/section-pagination.spec.ts` (Phase 417) — pola sqlcmd BACKUP DATABASE sebelum + RESTORE sesudah (SEED_WORKFLOW). Clone struktur.
- Selektor: `tests/e2e/helpers/wizardSelectors.ts` (extended Plan 03: optionE/F, correctE/F, addOptionBtn, removeOptionBtn) + `createAssessmentViaWizard` di `helpers/examTypes.ts`.
- Env lokal: app `dotnet run` @5277 (main branch port 5277). Playwright `--workers=1`. SQLEXPRESS lokal + SQLBrowser (lihat reference_local_e2e_sql_env_fix). sqlcmd `-C -I`.

Kontrak field form (Plan 02/03): `options[i].Text`, `correctIndex` (radio MC), `options[i].IsCorrect` (checkbox MA), `options[i].Image`, id `option_{letter}`.
EditQuestion guard: hapus opsi terjawab → `TempData["Error"]` + redirect ManagePackageQuestions (alert-danger), BUKAN 500.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test real-SQL — edit-shrink guard (D-418-02)</name>
  <files>HcPortal.Tests/EditShrinkGuardIntegrationTests.cs, docs/SEED_JOURNAL.md</files>
  <read_first>
    - HcPortal.Tests/SectionFixRegressionTests.cs ATAU SubmitResurrectionTests.cs (pola fixture real-SQL + seed PackageUserResponse + MigrateAsync)
    - HcPortal.Tests/EditShrinkGuardLogicTests.cs (Plan 01 — pure logic; ini integration komplemen)
    - 418-RESEARCH.md §"Edit-Shrink Guard" + Open Question 3 (pure vs integration — keduanya)
    - Controllers/AssessmentAdminController.cs EditQuestion POST (guard Plan 02)
    - Data/ApplicationDbContext.cs:561-564 (FK Restrict)
  </read_first>
  <behavior>
    - `EditShrinkGuard_AnsweredOption_NotRemoved_NoException`: seed soal 4-opsi + `PackageUserResponse.PackageOptionId` → opsi B (sudah dijawab). Jalankan jalur EditQuestion yang men-shrink (kosongkan teks B / 4→3) → guard memblok → tidak ada `DbUpdateException`/500; opsi B masih ada di DB (tak terhapus); hasil = redirect/TempData error (bukan exception).
    - `EditShrinkGuard_UnansweredOption_Removed_Succeeds`: seed soal 4-opsi tanpa response ke opsi D → shrink 4→3 (hapus D) → sukses, opsi D terhapus, tak ada error.
    - Gunakan real SQLEXPRESS (FK Restrict hanya nyata di SQL Server; InMemory tak menegakkan FK). Bila men-drive via controller terlalu berat, boleh test pada jalur service/logic yang melakukan query `PackageUserResponses` + `OptionShrinkGuard` + Remove + SaveChanges dengan DbContext real-SQL (buktikan no-throw + state DB benar).
  </behavior>
  <action>
    Buat `HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` clone pola fixture real-SQL dari `SectionFixRegressionTests`/`SubmitResurrectionTests`: `[Collection]` SqlServer fixture, `MigrateAsync`, seed AssessmentPackage+PackageQuestion+PackageOption+AssessmentSession+PackageUserResponse. Dua test di atas. Bersihkan data seed di Dispose/teardown (jangan tinggalkan di DB lokal — CLAUDE.md Seed Workflow). Catat di `docs/SEED_JOURNAL.md` (klasifikasi temporary+local-only, entitas tersentuh, tandai cleaned setelah). Bila fixture sudah self-contained (transaction rollback/respawn), journal tetap dicatat untuk jejak.
  </action>
  <acceptance_criteria>
    - `grep -n "PackageUserResponse" HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` cocok (seed response).
    - File punya 2 test (answered→no-throw+preserved, unanswered→removed).
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~EditShrinkGuard"` GREEN (pure Plan 01 + integration ini).
    - `docs/SEED_JOURNAL.md` punya entri 418 edit-shrink (klasifikasi + cleaned).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~EditShrinkGuard" 2>&1 | tail -15</automated>
  </verify>
  <done>EditShrinkGuardIntegrationTests.cs real-SQL: answered→no-500+preserved, unanswered→removed; GREEN; SEED_JOURNAL dicatat+cleaned. Commit `test(418-04): edit-shrink guard integration real-SQL (D-418-02)`.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright e2e option-dynamic-418.spec.ts</name>
  <files>tests/e2e/option-dynamic-418.spec.ts, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/section-pagination.spec.ts (pola DB backup/restore + struktur spec Phase 417)
    - tests/e2e/option-validation-386.spec.ts (pola fill #option_A..D + create soal — regresi backward-compat)
    - tests/e2e/helpers/wizardSelectors.ts (selektor extended Plan 03) + helpers/examTypes.ts (createAssessmentViaWizard)
    - 418-VALIDATION.md Per-Task Verification Map + Wave 0 (daftar skenario e2e WAJIB)
    - 418-UI-SPEC.md C1–C8 (interaksi yang diuji) + reference_local_e2e_sql_env_fix (env)
  </read_first>
  <behavior>
    Spec mencakup (semua skenario VALIDATION.md):
    - `addRow_to6_thenDisabled`: form fresh 4 baris → klik Tambah 2× → 6 baris (E,F muncul) → addOptionBtn disabled.
    - `removeRow_min2`: hapus baris → s/d 2 → tombol Hapus hidden/disabled saat 2.
    - `reletter_afterRemove`: hapus baris B → baris berikutnya jadi B (huruf re-letter A,B,C…).
    - `imageReassoc_flag4` (KRITIS): C punya gambar, hapus B → gambar tetap di soal benar (cek thumbnail/field di baris yang benar).
    - `render_AF_inExam`: buat soal 6-opsi → ambil ujian → huruf E & F tampil di StartExam.
    - `previewPackage_6thIsF`: soal 6-opsi → PreviewPackage opsi ke-6 tampil "F" (bukan "A").
    - `editImported5Opt_prefill5` (flag#2): soal 5-opsi (import/seed) → buka edit → prefill 5 baris.
    - `inject_addRemoveRows`: form Inject tambah/hapus baris A–F.
    - `editShrink_answered_blocked`: seed soal+response ke opsi → edit hapus opsi itu → `alert-danger` muncul (BUKAN 500 / halaman error).
    - `backwardCompat_4opt`: soal 4-opsi lama create/render identik (regresi).
  </behavior>
  <action>
    Buat `tests/e2e/option-dynamic-418.spec.ts` clone struktur backup/restore `section-pagination.spec.ts` (sqlcmd BACKUP DATABASE sebelum suite + RESTORE sesudah, SEED_WORKFLOW). Gunakan `wizardSelectors` extended + `createAssessmentViaWizard`. Untuk skenario render exam & edit-imported-5-opt & edit-shrink: seed data via UI authoring (buat soal 6-opsi via form) atau seed SQL minimal (catat SEED_JOURNAL). Pin `workers:1`. App harus jalan @5277 (`dotnet run`). Assert: jumlah baris, disabled state, huruf data-letter, thumbnail/field gambar di baris benar, teks huruf di StartExam/PreviewPackage, jumlah baris prefill edit, alert-danger pada edit-shrink. Catat seed di `docs/SEED_JOURNAL.md` + restore.
    CATATAN: Task ini menulis + menjalankan spec; bila app belum jalan, executor start `dotnet run --urls http://localhost:5277` (background) lalu jalankan Playwright. Backward-compat soal 4-opsi WAJIB hijau (regresi).
  </action>
  <acceptance_criteria>
    - `tests/e2e/option-dynamic-418.spec.ts` ada; `grep -n "addOptionBtn\|remove-option-btn\|disabled\|data-letter\|alert-danger" tests/e2e/option-dynamic-418.spec.ts` cocok.
    - Spec memuat skenario image-reassoc (flag#4) + render A–F + previewPackage-F + prefill-5 + edit-shrink-blocked + backward-compat-4opt.
    - `cd tests && npx playwright test option-dynamic-418 --workers=1` HIJAU (semua skenario PASS).
    - `docs/SEED_JOURNAL.md` dicatat + restore confirmed.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test option-dynamic-418 --workers=1 2>&1 | tail -25</automated>
  </verify>
  <done>option-dynamic-418.spec.ts HIJAU mencakup semua skenario VALIDATION.md (incl flag#4 image-reassoc + edit-shrink-blocked + backward-compat); DB di-backup/restore; SEED_JOURNAL dicatat. Commit `test(418-04): e2e option-dynamic-418 (OPT-01/02/03 + flag#4 + edit-shrink)`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT live @localhost:5277 — opsi dinamis 2–6 + render A–F + edit-shrink (lesson 354)</name>
  <files>Views/Admin/ManagePackageQuestions.cshtml, Views/CMP/StartExam.cshtml, Views/Admin/PreviewPackage.cshtml, Views/Admin/_InjectQuestionForm.cshtml (verifikasi visual; tak diedit di task ini)</files>
  <action>
Checkpoint manusia — TIDAK ada perubahan kode otomatis di task ini. Claude sudah mengotomasi seluruh implementasi (Plan 01 test, Plan 02 controller+guard, Plan 03 view+JS, Plan 04 Task 1/2 integration+e2e). Task ini HANYA konfirmasi visual & interaksi manusia atas form opsi dinamis 2–6 + render A–F + guard edit-shrink di real browser (lesson 354: Razor/JS WAJIB UAT browser). Jalankan langkah di &lt;how-to-verify&gt; lalu tunggu &lt;resume-signal&gt;.
  </action>
  <what-built>
Form authoring + Inject opsi dinamis 2–6 (Tambah/Hapus baris, re-letter A–F, reasosiasi gambar baris-tengah, single-select MC radio lintas 6 baris), render huruf A–F di ujian/hasil/preview + fix bug modulo PreviewPackage (opsi ke-6 "F"), guard edit-shrink (hapus opsi terjawab ditolak via alert-danger, BUKAN 500 — tutup 999.14), validator max-6, edit soal 5–6 opsi import (H3 dihapus). Semua otomasi (xUnit + Playwright) sudah hijau.
  </what-built>
  <how-to-verify>
Prasyarat: app jalan `dotnet run --urls http://localhost:5277` (branch main; cek http://localhost:5277). Login admin (admin@pertamina.com — reference_dev_credentials). SNAPSHOT DB lokal dulu (SEED_WORKFLOW) sebelum membuat data uji.
1. Buka `/Admin/ManagePackageQuestions?packageId={paket uji}`. Form fresh = 4 baris opsi (A–D). Klik "+ Tambah Opsi" 2× → muncul baris E lalu F; tombol Tambah jadi disabled (title "Maksimal 6 opsi"). Klik "Hapus" pada baris → baris hilang, huruf re-letter berurutan; turun s/d 2 baris → tombol Hapus hilang/nonaktif.
2. Buat soal MultipleChoice 6-opsi: isi A–F, pilih satu radio benar (mis. E) → cek hanya SATU radio bisa terpilih (single-select lintas 6 baris, flag#1). Simpan → sukses.
3. (Reasosiasi gambar, flag#4) Buat soal: opsi C beri gambar, lalu hapus baris B → konfirmasi gambar tetap menempel pada opsi yang benar (C→jadi B, gambarnya ikut), tidak hilang/pindah salah baris.
4. (Edit 5–6 opsi, flag#2) Edit soal 5/6-opsi (hasil langkah 2 atau soal import 415) → form mem-prefill 5/6 baris terisi (bukan 4). Sebelumnya soal >4 opsi DITOLAK ("menyusul") — sekarang bisa diedit.
5. (Edit-shrink, D-418-02) Ambil ujian soal MC sbg worker, jawab salah satu opsi → kembali sbg admin, edit soal itu, kosongkan/hapus opsi yang TADI dijawab → muncul `alert-danger` "Opsi … sudah dijawab … tidak bisa dihapus" (BUKAN halaman error 500). Opsi yang belum dijawab tetap bisa dihapus.
6. (Render A–F, OPT-02) Ambil ujian soal 6-opsi sbg worker → huruf E & F tampil di kartu soal. Buka `/Admin/PreviewPackage` → opsi ke-6 tampil "F" (BUKAN "A").
7. (Inject) Buka `/Admin/InjectAssessment`, di form soal inject: Tambah/Hapus baris opsi berfungsi A–F (tanpa blok gambar).
8. (Backward-compat) Soal 4-opsi lama tetap render & edit identik.
Setelah selesai: RESTORE DB lokal (SEED_WORKFLOW) + tandai SEED_JOURNAL cleaned. JANGAN push tanpa approval (CLAUDE.md step 4-5).
  </how-to-verify>
  <verify>Manual checkpoint — konfirmasi visual manusia per langkah &lt;how-to-verify&gt; (tak ada perintah otomatis; e2e + integration otomatis sudah di Task 1/2).</verify>
  <done>Manusia mengetik "approved" setelah seluruh 8 langkah PASS (tambah/hapus/re-letter, single-select MC, reasosiasi gambar, prefill 5–6, edit-shrink alert bukan 500, render A–F + preview-F, inject, backward-compat), DB lokal di-restore, SEED_JOURNAL cleaned.</done>
  <resume-signal>Ketik "approved" bila semua 8 langkah PASS, atau jelaskan langkah yang gagal (sertakan screenshot/pesan) untuk diperbaiki.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker browser → exam render | Render huruf A–F soal 6-opsi diverifikasi runtime. |
| Admin browser → EditQuestion (shrink) | Guard FK-Restrict diverifikasi tak melempar 500 (real-SQL + live). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-418-14 | Denial of Service | FK-Restrict 500 saat hapus opsi terjawab | mitigate | Integration real-SQL (Task 1) + e2e edit-shrink-blocked (Task 2) + UAT langkah 5 membuktikan redirect+alert, bukan 500. |
| T-418-15 | Tampering | Single-select MC rusak lintas 6 baris (flag#1) | mitigate | UAT langkah 2 + e2e memverifikasi hanya satu radio terpilih dari 6. |
| T-418-16 | Info Disclosure | Gambar opsi salah-baris setelah hapus baris-tengah (flag#4) | mitigate | e2e imageReassoc (Task 2, KRITIS) + UAT langkah 3. |
</threat_model>

<verification>
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full suite GREEN (incl EditShrinkGuard integration).
- `cd tests && npx playwright test option-dynamic-418 --workers=1` HIJAU.
- UAT live 8-langkah @5277 PASS (checkpoint).
- DB lokal di-restore + SEED_JOURNAL cleaned (CLAUDE.md Seed Workflow).
- Tidak ada migration (verify-migration empty); notify IT migration=FALSE saat ship.
</verification>

<success_criteria>
- Integration real-SQL guard: answered→no-500+preserved, unanswered→removed.
- e2e option-dynamic-418 hijau: add/remove/disabled@6/min-2/re-letter/render-AF/preview-F/prefill-5/image-reassoc(flag#4)/edit-shrink-blocked/backward-compat-4opt.
- UAT live 8-langkah PASS — bukti runtime OPT-01/02/03 + flag#1/#4 + D-418-02.
- DB lokal pristine (restore) + SEED_JOURNAL cleaned.
</success_criteria>

<output>
After completion, create `.planning/phases/418-opsi-jawaban-dinamis-2-6/418-04-SUMMARY.md` + `.planning/phases/418-opsi-jawaban-dinamis-2-6/418-UAT.md` (hasil 8-langkah).
</output>
