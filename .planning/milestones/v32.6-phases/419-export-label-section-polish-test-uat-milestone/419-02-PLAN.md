---
phase: 419-export-label-section-polish-test-uat-milestone
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Helpers/ExcelExportHelper.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PAG-04]
must_haves:
  truths:
    - "Export Excel 'Detail Per Soal' mengurutkan kolom soal per (SectionNumber, Order) dengan baris band-header merged 'Section {n}: {Nama}' di atas tiap grup; 'Lainnya' terakhir"
    - "Export PDF per-peserta menyisipkan heading 'Section {n}: {Nama}' sebelum tiap blok soal Section; 'Lainnya' terakhir"
    - "Assessment tanpa Section (semua null) menghasilkan output praktis identik dengan sekarang (no band, urut Order) — backward-compat"
    - "Huruf opsi A-F dinamis konsisten (BuildAnswerCell/IsQuestionCorrect tak diubah — kill-drift)"
  artifacts:
    - path: "Helpers/ExcelExportHelper.cs"
      provides: "AddDetailPerSoalSheet dengan reorder Section-aware + band-header"
      contains: "SectionNumber"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Eager-load Include(q => q.Section) di 2 export load site + grouping heading di GeneratePerPesertaPdf"
      contains: "Include(q => q.Section)"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (export load :5425, :5673)"
      to: "PackageQuestion.Section"
      via: "Include(q => q.Section) eager-load (else q.Section null -> semua Lainnya silent)"
      pattern: "Include\\(q => q\\.Section\\)"
    - from: "Helpers/ExcelExportHelper.cs"
      to: "AssessmentScoreAggregator.BuildAnswerCell / IsQuestionCorrect"
      via: "reuse verbatim (kill-drift)"
      pattern: "BuildAnswerCell|IsQuestionCorrect"
---

<objective>
Implementasi PAG-04: label/header Section di dua jalur export bukti hasil per-soal. (1) Excel "Detail Per Soal" — reorder kolom soal per (SectionNumber, Order) + baris band-header merged "Section {n}: {Nama}". (2) PDF per-peserta — heading "Section {n}: {Nama}" sebelum tiap blok soal. WAJIB tambah eager-load Section di KEDUA call-site load (else silent all-Lainnya, Pitfall 1). Backward-compat: assessment tanpa Section -> output praktis identik sekarang. Huruf A-F dinamis (Phase 418) otomatis konsisten karena reuse BuildAnswerCell (letter-agnostik) — TIDAK boleh diubah (kill-drift).

Purpose: Bukti hasil per-soal menampilkan organisasi Section sesuai SEC-03 — satu-satunya REQ formal fase 419 (PAG-04).
Output: ExcelExportHelper.AddDetailPerSoalSheet Section-aware + GeneratePerPesertaPdf grouping + 2 eager-load fix. Test PAG-04 (Plan 01) jadi GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-CONTEXT.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md

<interfaces>
Helpers/ExcelExportHelper.cs:50-112 — AddDetailPerSoalSheet (saat ini):
  L58 ordering yang DIGANTI: var sortedQuestions = questions.OrderBy(q => q.Order).ThenBy(q => q.Id).ToList();
  L60-69: kolom 1=No, 2=Nama, 3=NIP; kolom soal mulai col=4, tiap soal 2 kolom (Jawaban + Benar?); 1 kolom "Skor Total" terakhir.
  L71-74: header-styling idiom (bold + LightGray fill + center) untuk row 1.
  L76 rowIdx=2 (data mulai row 2); L93-94: BuildAnswerCell + IsQuestionCorrect (JANGAN ubah); L111 FreezeRows(1).

Canonical ordering (Pattern 1 — ShuffleEngine/SectionPaginator 416/417):
  var ordered = questions.OrderBy(q => q.Section?.SectionNumber ?? int.MaxValue).ThenBy(q => q.Order).ThenBy(q => q.Id).ToList();

ClosedXML band-header merged (RESEARCH Code Examples):
  var band = ws.Range(bandRow, startCol, bandRow, endCol);
  band.Merge();
  band.Value = sectionNumber.HasValue ? $"Section {sectionNumber}: {sectionName}" : "Lainnya";
  band.Style.Font.Bold = true;
  band.Style.Alignment.Horizontal = XLAlignmentHorizontalValues.Center;
  band.Style.Fill.BackgroundColor = XLColor.LightBlue;

AssessmentAdminController.cs export load (BUG — no Section eager-load) di :5425-5428 (Excel) DAN :5673-5676 (PDF):
  var allQuestions = await _context.PackageQuestions
      .Include(q => q.Options)
      .Where(q => packageIds.Contains(q.AssessmentPackageId))
      .ToListAsync();
  FIX: tambah .Include(q => q.Section) (mirror CMPController.StartExam:1088-1092 .ThenInclude(q=>q.Section)).

AssessmentAdminController.cs:5703 GeneratePerPesertaPdf ordering yang DIGANTI di :5732-5735:
  var sessionQuestions = allQuestions.Where(q => sessionPkgIds.Contains(q.AssessmentPackageId)).OrderBy(q => q.Order).ThenBy(q => q.Id).ToList();
  Per-question block :5818-5841 (BuildAnswerCell + IsQuestionCorrect) — JANGAN ubah, bungkus dengan group loop.

Model — Models/AssessmentPackage.cs: PackageQuestion.Section -> AssessmentPackageSection { int SectionNumber; string? Name; }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Excel AddDetailPerSoalSheet — reorder Section-aware + band-header merged (PAG-04 Excel)</name>
  <behavior>
    - Test BandHeader_OrdersBySectionNumberThenOrder (Plan 01): kolom soal urut Section 1 (soal Order asc) -> Section 2 -> Lainnya terakhir.
    - Test BandHeader_RendersSectionLabelRow (Plan 01): baris band merged "Section {n}: {Nama}" di atas grup kolom tiap Section.
    - Test NoSection_BackwardCompat (Plan 01): semua null-Section -> TIDAK ada band (data row 2, FreezeRows(1)) — output identik sekarang.
  </behavior>
  <read_first>
    - Helpers/ExcelExportHelper.cs:50-112 (FULL AddDetailPerSoalSheet — struktur kolom, rowIdx, freeze, kill-drift block :93-94)
    - Helpers/ExcelExportHelper.cs:14-23 (CreateSheet header-styling idiom), :71-74 (bold+fill+center)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md section AddDetailPerSoalSheet (off-by-one Pitfall 4, band placement)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md Pitfall 4 (band geser baris data -> off-by-one)
  </read_first>
  <files>Helpers/ExcelExportHelper.cs</files>
  <action>
Ubah AddDetailPerSoalSheet jadi Section-aware. Reuse semua per-cell logic existing (BuildAnswerCell/IsQuestionCorrect/Skor Total) VERBATIM (kill-drift v30.0/386) — hanya urutan + band-header yang berubah.

1. GANTI ordering L58 dengan canonical: OrderBy(q => q.Section?.SectionNumber ?? int.MaxValue).ThenBy(q => q.Order).ThenBy(q => q.Id).

2. Deteksi backward-compat: bool anySection = sortedQuestions.Any(q => q.Section != null);
   - Bila !anySection (legacy semua null): RENDER TANPA band (path lama — data mulai rowIdx=2, FreezeRows(1)). Tampilan byte-mirip sekarang (Claude's Discretion D-01 + Open Q-3: suppress band saat 0 Section).
   - Bila anySection: render band-header di row 1, kolom-header di row 2, data mulai rowIdx=3, FreezeRows(2) (Pitfall 4 off-by-one — hitung ulang semua indeks).

3. Band-header (hanya saat anySection): iterasi grup sortedQuestions.GroupBy(q => q.Section?.SectionNumber).OrderBy(g => g.Key ?? int.MaxValue). Untuk tiap grup hitung rentang kolom yang ditempati soal grup itu (tiap soal = 2 kolom Jawaban+Benar?; kolom No/Nama/NIP 1-3 tetap TANPA band). Merge ws.Range(bandRow, startCol, bandRow, endCol), Value = g.Key.HasValue ? $"Section {g.Key}: {g.First().Section?.Name}" : "Lainnya", bold + center + LightBlue fill. Kolom soal global tetap berurut (grup kontigu karena sudah OrderBy).

4. Kolom-header "Soal {i+1} Jawaban"/"Soal {i+1} Benar?" + "Skor Total" tetap (nomor soal global 1..N mengikuti urutan baru). Sesuaikan baris kolom-header ke row 2 saat band hadir.

5. Sesuaikan rowIdx awal + FreezeRows sesuai cabang. Data per-peserta loop TAK berubah selain offset baris.

JANGAN sentuh BuildAnswerCell/IsQuestionCorrect (L93-94). JANGAN tambah skor per-Section (D-12 — label organisasi saja).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ExportSectionLabelTests"</automated>
  </verify>
  <acceptance_criteria>
    - grep "SectionNumber" Helpers/ExcelExportHelper.cs match (OrderBy section-aware)
    - grep "Merge()" Helpers/ExcelExportHelper.cs match (band-header)
    - grep "Section {" Helpers/ExcelExportHelper.cs match (label format Section {n}: {Nama})
    - grep -c "BuildAnswerCell" Helpers/ExcelExportHelper.cs >= 1 DAN baris BuildAnswerCell/IsQuestionCorrect tak berubah (kill-drift)
    - Test BandHeader_OrdersBySectionNumberThenOrder + BandHeader_RendersSectionLabelRow + NoSection_BackwardCompat PASS
  </acceptance_criteria>
  <done>AddDetailPerSoalSheet reorder per (SectionNumber, Order) + band merged "Section {n}: {Nama}"; legacy no-Section tanpa band (identik sekarang); kill-drift block utuh. 3 test Excel GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDF heading per Section + 2 eager-load fix (PAG-04 PDF + Pitfall 1)</name>
  <behavior>
    - Export PDF per-peserta: heading "Section {n}: {Nama}" sebelum tiap blok soal Section; "Lainnya" terakhir; legacy 1-grup-null suppress heading.
    - Kedua export load site eager-load q.Section (else semua Lainnya silent).
  </behavior>
  <read_first>
    - Controllers/AssessmentAdminController.cs:5418-5435 (Excel export load + call site), :5665-5694 (PDF export load + call site)
    - Controllers/AssessmentAdminController.cs:5703-5745 (GeneratePerPesertaPdf head + ordering :5732-5735), :5810-5843 (per-question QuestPDF loop)
    - Controllers/CMPController.cs:1088-1092 (.ThenInclude(q => q.Section) load idiom)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md section GeneratePerPesertaPdf (heading-between-blocks, preserve block :5823-5825)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
A. FIX Pitfall 1 — tambah .Include(q => q.Section) di KEDUA export load site (else q.Section null -> semua "Lainnya" senyap):
   - :5425-5428 (Excel export load): tambah .Include(q => q.Section) setelah .Include(q => q.Options).
   - :5673-5676 (PDF export load): tambah .Include(q => q.Section) setelah .Include(q => q.Options).

B. GeneratePerPesertaPdf (:5703) — group + heading:
   - GANTI ordering :5732-5735 ke canonical OrderBy(q.Section?.SectionNumber ?? int.MaxValue).ThenBy(q.Order).ThenBy(q.Id).
   - Bungkus per-question loop existing (:5818-5841) dengan group loop GroupBy(q.Section?.SectionNumber).OrderBy(g => g.Key ?? int.MaxValue). Untuk tiap grup: label = g.Key.HasValue ? $"Section {g.Key}: {g.First().Section?.Name}" : "Lainnya". Backward-compat (Claude's Discretion D-01): bila TIDAK ada satupun soal ber-Section (semua null) -> SUPPRESS heading (legacy identik). Bila ada Section, emit heading: col.Item().PaddingTop(6).Text(label).Bold().FontSize(12).FontColor(QuestPDF.Helpers.Colors.Blue.Darken2). Lalu loop soal dalam grup OrderBy(q.Order).ThenBy(q.Id) memakai blok per-question EXISTING tanpa perubahan.
   - PERTAHANKAN verbatim blok kill-drift (:5823-5825): IsQuestionCorrect + BuildAnswerCell. JANGAN ubah.
   - Pertahankan guard DoS max 50 peserta (:5660 existing) — tak disentuh.

JANGAN tambah skor/breakdown per-Section (D-12). Pastikan dotnet build HcPortal.csproj 0 error sebelum commit.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ExportSectionLabelTests|FullyQualifiedName~PdfAnswerCellTests|FullyQualifiedName~IsQuestionCorrectTests"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Include(q => q.Section)" Controllers/AssessmentAdminController.cs >= 2 (kedua export load site)
    - grep "Section {" Controllers/AssessmentAdminController.cs match (PDF heading format)
    - grep -E "GroupBy\(q => q.Section" Controllers/AssessmentAdminController.cs match (PDF grouping)
    - `dotnet build HcPortal.csproj` exit 0
    - Test ExportSectionLabelTests + PdfAnswerCellTests + IsQuestionCorrectTests PASS (kill-drift regresi hijau)
  </acceptance_criteria>
  <done>2 export load site eager-load Section; GeneratePerPesertaPdf emit heading per Section; legacy suppress; build hijau; kill-drift regresi PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC -> export endpoint | Admin/HC trigger export (read-only); data dari DB ter-validasi (Section Name editorial HC, Phase 415). |
| DB (Section.Name) -> .xlsx/.pdf cell | Nilai Section ditulis ke file dokumen; potensi formula-injection Excel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-419-02-01 | Injection | Section.Name ditulis ke cell Excel (band-header) | accept | ClosedXML menulis sebagai string VALUE (band.Value=...), BUKAN formula; tak ter-evaluasi. Section Name editorial HC (ter-validasi 415). Mirror cell writes existing (RESEARCH Security Domain — akui-by-design, LOW). |
| T-419-02-02 | Injection/XSS | Section.Name ke heading PDF (QuestPDF .Text()) | mitigate | QuestPDF .Text() render plain text (bukan HTML/markup) -> tak eval. Pakai .Text(label) langsung. |
| T-419-02-03 | Elevation/IDOR | Export sesi orang lain | accept | Endpoint export sudah [Authorize(Roles="Admin, HC")] (verified :5640) — admin/HC by design lihat semua. TIDAK diubah/dilonggarkan. |
| T-419-02-04 | DoS | Export PDF besar (banyak peserta) | mitigate | Guard max 50 peserta/batch PDF (:5660 existing) DIPERTAHANKAN saat tambah grouping. |
| T-419-02-05 | Tampering | Export menampilkan skor manipulasi | accept | Skor via Skor Total existing + BuildAnswerCell tak diubah (kill-drift). Tak ada sink baru. |
</threat_model>

<verification>
- dotnet build HcPortal.csproj exit 0.
- ExportSectionLabelTests (Plan 01) semua GREEN (band-header, ordering, backward-compat).
- PdfAnswerCellTests + IsQuestionCorrectTests + AssessmentScoreAggregatorTests tetap GREEN (kill-drift — helper tak berubah).
- grep konfirmasi 2 Include Section + band-header Merge + heading PDF.
- Full suite dijalankan saat wave merge — 0 regresi.
</verification>

<success_criteria>
- PAG-04 Excel band-header + PDF heading hidup, urut (SectionNumber, Order), Lainnya terakhir.
- Backward-compat: assessment tanpa Section identik sekarang.
- Huruf A-F dinamis konsisten (kill-drift terjaga).
- 2 eager-load Section ditambahkan (Pitfall 1 ditutup).
</success_criteria>

<output>
After completion, create `.planning/phases/419-export-label-section-polish-test-uat-milestone/419-02-SUMMARY.md` (@$HOME/.claude/get-shit-done/templates/summary.md). Catat: cabang band vs no-band (rowIdx/freeze), rentang merge per Section, konfirmasi kill-drift block utuh, 2 site Include yang diubah, hasil test PAG-04.
</output>
