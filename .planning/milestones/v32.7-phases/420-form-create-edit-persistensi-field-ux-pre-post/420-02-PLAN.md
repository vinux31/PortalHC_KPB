---
phase: 420-form-create-edit-persistensi-field-ux-pre-post
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/EditGuardRedirect420Tests.cs
autonomous: true
requirements: [FORM-05, FORM-06]
migration: false

must_haves:
  truths:
    - "Membuka POST Edit untuk sesi (atau grup Pre-Post) yang sudah Completed ditolak — redirect ManageAssessment + TempData Error, metadata tidak dimutasi"
    - "Guard Completed group-aware: bila SATU sesi dalam grup Pre-Post berstatus Completed, seluruh grup terkunci dari Edit"
    - "Membuka GET Edit untuk sesi IsManualEntry → redirect ke EditManualAssessment (TrainingAdmin), bukan form online"
    - "Sesi standard non-Completed & non-manual tetap dapat di-Edit seperti sekarang (backward-compat)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Guard Completed group-aware diangkat ke atas cabang Pre-Post (POST Edit) + redirect IsManualEntry di GET Edit"
      contains: "RedirectToAction(\"EditManualAssessment\", \"TrainingAdmin\""
    - path: "HcPortal.Tests/EditGuardRedirect420Tests.cs"
      provides: "Test guard lock Completed (group-aware) + redirect manual"
      contains: "[Trait(\"Category\", \"Integration\")]"
  key_links:
    - from: "POST EditAssessment (sebelum cabang Pre-Post :1821)"
      to: "guard isCompleted (group-aware AnyAsync) → RedirectToAction ManageAssessment"
      via: "guard server-side mendahului semua cabang mutasi"
      pattern: "LinkedGroupId == assessment.LinkedGroupId && a.Status == \"Completed\""
    - from: "GET EditAssessment (:1692, setelah null-check)"
      to: "RedirectToAction EditManualAssessment TrainingAdmin"
      via: "if assessment.IsManualEntry redirect"
      pattern: "assessment.IsManualEntry"
---

<objective>
Tutup dua celah guard/redirect di form Edit assessment — **server-authoritative, tanpa mengubah perilaku sesi standard yang sah**:

1. **FORM-05 (E-04):** Sesi Pre/Post yang sudah Completed BISA diedit metadata-nya karena cabang Pre-Post `return :2001` mendahului guard `Status=="Completed"` `:2006` (guard tak pernah tercapai untuk Pre-Post). FIX = ANGKAT guard ke atas cabang Pre-Post (`:1821`), buat **group-aware** (blokir bila ada SATU sesi grup Completed). Data-integrity risk: mengubah metadata pasca-Completed merusak konsistensi cert/skor.
2. **FORM-06 (E-08):** GET `EditAssessment` tidak memfilter `IsManualEntry` → sesi entry-manual diarahkan ke form online (salah). FIX = redirect ke `EditManualAssessment` (TrainingAdmin) di GET, mirror filter `TrainingAdminController.cs:994`.

Purpose: Lock metadata Completed = access-control (V4 ASVS) yang melindungi integritas data assessment; redirect manual = routing-correctness agar HC tidak salah-edit di form yang keliru.
Output: 1 controller (1 guard diangkat group-aware + 1 redirect GET), 1 file test xUnit baru.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-CONTEXT.md
@.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-RESEARCH.md
@.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-PATTERNS.md

<interfaces>
<!-- Kontrak dari codebase 2026-06-22, JANGAN eksplorasi ulang. -->

POST EditAssessment signature (Controllers/AssessmentAdminController.cs:1808):
```csharp
[HttpPost] [Authorize(Roles="Admin, HC")] [ValidateAntiForgeryToken]
public async Task<IActionResult> EditAssessment(int id, AssessmentSession model, List<string> NewUserIds, ...)
// :1813 var assessment = await _context.AssessmentSessions.FindAsync(id);
// :1818 null-check → return RedirectToAction("ManageAssessment")
// :1821 if ((AssessmentType=="PreTest"||"PostTest") && LinkedGroupId.HasValue) { ...mutasi...; return :2001 }
// :2006 if (assessment.Status == "Completed") { ...; return; }  ← TAK terjangkau Pre-Post (bug E-04)
```

GET EditAssessment (Controllers/AssessmentAdminController.cs:1682):
```csharp
[Authorize(Roles="Admin, HC")]
public async Task<IActionResult> EditAssessment(int id)
// :1684 var assessment = await ...FirstOrDefaultAsync(a => a.Id == id);
// :1688 null-check
// :1694 bool isPrePost = ...  ← SISIP redirect IsManualEntry SEBELUM baris ini
```

Pola filter manual (TrainingAdminController.cs:992-994 — VERIFIED):
```csharp
var session = await _context.AssessmentSessions
    .Include(s => s.User)
    .FirstOrDefaultAsync(s => s.Id == id && s.IsManualEntry);
```

Guard existing (single-mode, AssessmentAdminController.cs:2006-2010):
```csharp
if (assessment.Status == "Completed") {
    TempData["Error"] = "Cannot edit completed assessments.";
    return RedirectToAction("ManageAssessment");
}
```

JANGAN reuse `Helpers/AssessmentEditEligibility.IsEditableAsync` — semantik TERBALIK (return true HANYA bila Completed). Tulis guard sendiri.

Template test: `HcPortal.Tests/RetakeExamEndpointTests.cs` (action-invoke + FakeUserStore + StubSession + real-SQL fixture).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave-0 test stubs FORM-05 (lock Completed group-aware) + FORM-06 (redirect manual)</name>
  <files>HcPortal.Tests/EditGuardRedirect420Tests.cs</files>
  <read_first>
    - HcPortal.Tests/RetakeExamEndpointTests.cs (template action-invoke + FakeUserStore + StubSession + null!-substitute deps)
    - HcPortal.Tests/RetakeSettingsEndpointTests.cs (alternatif: replika body guard atas grup real :91-110)
    - 420-PATTERNS.md §EditGuardRedirect420Tests.cs (analog file:line + 2 pendekatan)
    - 420-RESEARCH.md §Open Questions #1 (group-aware default)
  </read_first>
  <behavior>
    File `EditGuardRedirect420Tests.cs` dengan `[Trait("Category","Integration")]` + fixture real-SQL. Test (boleh RED sebelum Task 1):
    - Test 1 (FORM-05 group-aware lock): seed grup Pre-Post (LinkedGroupId sama) dengan SATU sesi Status="Completed", sesi lain "Open" → invoke POST EditAssessment(id=sesi Open, model) → assert hasil = `RedirectToActionResult` ke "ManageAssessment" + `TempData["Error"]` ada + metadata sesi TIDAK berubah (mis. Title tetap nilai lama, bukan model.Title). Pendekatan boleh action-invoke ATAU replika-body guard (`AnyAsync(a=>a.LinkedGroupId==g && a.Status=="Completed")` == true → assert true).
    - Test 2 (FORM-05 standard lock, regresi): seed sesi standard tunggal Status="Completed" → invoke POST EditAssessment → assert redirect + tidak mutasi (membuktikan guard standard tetap jalan).
    - Test 3 (FORM-05 negatif/backward-compat): seed grup Pre-Post SEMUA "Open" (tak ada Completed) → guard TIDAK menolak (isCompleted==false) → boleh lanjut mutasi. Assert guard tidak memblokir sesi yang sah.
    - Test 4 (FORM-06 redirect manual): seed sesi IsManualEntry=true → invoke GET EditAssessment(id) → assert `RedirectToActionResult` controller="TrainingAdmin" action="EditManualAssessment" route id=id.
    - Test 5 (FORM-06 negatif): seed sesi IsManualEntry=false (online) → GET EditAssessment → assert TIDAK redirect ke EditManualAssessment (lanjut render form online / ViewResult).
  </behavior>
  <action>
    Buat `HcPortal.Tests/EditGuardRedirect420Tests.cs` mengikuti idiom `RetakeExamEndpointTests.cs`: `[Trait("Category","Integration")]`, ctor terima fixture real-SQL, FakeUserStore (FindByIdAsync key-by-id, implement `IUserRoleStore` bila GetRolesAsync dipanggil — lesson 407-02), StubSession, `DefaultHttpContext`. Deps controller lain yang tak di-deref → `null!`-substitute (lihat pola RetakeExamEndpointTests ctor). 
    Untuk FORM-05 group-aware query, pendekatan replika-body guard lebih ringan (seed grup real, assert `AnyAsync(...Completed)`); pakai action-invoke bila ingin assert `RedirectToActionResult` aktual + non-mutasi.
    Gunakan data deterministik unik (Title bertimestamp). JANGAN bangun harness baru.
    Jika RED sebelum Task 1, wajar — Task 1 menghijaukan.
  </action>
  <acceptance_criteria>
    - `HcPortal.Tests/EditGuardRedirect420Tests.cs` ada, `[Trait("Category","Integration")]` hadir.
    - Berisi 5 test method (FORM-05: group-aware/standard/negatif; FORM-06: redirect/negatif) — grep `EditManualAssessment` ≥1, `Completed` ≥2, `IsManualEntry` ≥1.
    - `dotnet build` 0 error.
    - Test ter-discover: `dotnet test --filter "FullyQualifiedName~EditGuardRedirect420"` (boleh RED sebelum Task 1).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>File test ada dengan 5 method real-SQL (lock group-aware + redirect manual + negatif/backward-compat); build 0 error.</done>
</task>

<task type="auto">
  <name>Task 1: FORM-05 angkat guard Completed group-aware + FORM-06 redirect manual di GET Edit</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (:1808-1822 POST Edit head + cabang Pre-Post; :2003-2010 guard existing; :1682-1695 GET Edit head)
    - Controllers/TrainingAdminController.cs (:990-994 filter manual analog)
    - 420-PATTERNS.md §FORM-05 + §FORM-06 (snippet penempatan)
    - 420-RESEARCH.md §Pattern 3 + §Pattern 4 + §Open Questions #1 (group-aware default)
    - 420-CONTEXT.md "Claude's Discretion FORM-05" (group-aware default, posisi sebelum cabang Pre-Post)
  </read_first>
  <action>
    **(A) FORM-05 — angkat guard Completed group-aware (POST Edit):**
    Di POST `EditAssessment` (:1808), SETELAH null-check `:1818` dan SEBELUM cabang Pre-Post `:1821`, sisipkan guard group-aware (default group-aware per Open Question #1 rec — blokir bila ada SATU sesi grup Completed):
    ```csharp
    // FORM-05 (E-04): lock metadata sesi/grup Completed — diangkat ke atas cabang Pre-Post
    // (bug asal: cabang Pre-Post return :2001 mendahului guard :2006 → guard tak terjangkau).
    bool isCompleted = assessment.Status == "Completed";
    if (assessment.LinkedGroupId.HasValue)
    {
        isCompleted = await _context.AssessmentSessions
            .AnyAsync(a => a.LinkedGroupId == assessment.LinkedGroupId && a.Status == "Completed");
    }
    if (isCompleted)
    {
        TempData["Error"] = "Tidak dapat mengubah assessment yang sudah Completed.";
        return RedirectToAction("ManageAssessment");
    }
    ```
    HAPUS atau biarkan guard lama `:2006-2010`? → BIARKAN guard lama `:2006-2010` (defense-in-depth untuk jalur standard yang lewat di bawah; tidak duplikat-berbahaya karena guard baru di atas sudah meng-cover standard juga — keduanya identik untuk standard non-grup). Jika executor menilai redundan, boleh hapus guard `:2006-2010`, TAPI hanya bila guard baru pasti meng-cover jalur standard (assessment.LinkedGroupId null → isCompleted = assessment.Status=="Completed", sama persis). Default: ANGKAT guard baru, biarkan guard lama (aman). Catat pilihan di SUMMARY.
    JANGAN gunakan `AssessmentEditEligibility.IsEditableAsync` (semantik terbalik).

    **(B) FORM-06 — redirect manual di GET Edit:**
    Di GET `EditAssessment(int id)` (:1682), SETELAH null-check `:1692` dan SEBELUM `bool isPrePost = ...` `:1694`, sisipkan:
    ```csharp
    // FORM-06 (E-08): sesi entry-manual diarahkan ke form edit manual (bukan form online).
    if (assessment.IsManualEntry)
        return RedirectToAction("EditManualAssessment", "TrainingAdmin", new { id });
    ```
    JANGAN hapus `[Authorize(Roles="Admin, HC")]` GET `:1681` / POST `:1806` / `[ValidateAntiForgeryToken]` `:1807`.
  </action>
  <acceptance_criteria>
    - POST EditAssessment mengandung guard group-aware: `LinkedGroupId == assessment.LinkedGroupId && a.Status == "Completed"` DAN penempatannya SEBELUM cabang Pre-Post (`if ((assessment.AssessmentType == "PreTest" || assessment.AssessmentType == "PostTest")`).
    - Guard menghasilkan `RedirectToAction("ManageAssessment")` + `TempData["Error"]`.
    - GET EditAssessment mengandung `if (assessment.IsManualEntry)` + `RedirectToAction("EditManualAssessment", "TrainingAdmin", new { id })`, ditempatkan SEBELUM `bool isPrePost`.
    - Tidak menggunakan `AssessmentEditEligibility.IsEditableAsync`.
    - `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` masih ada (tidak dihapus).
    - Backward-compat: sesi standard non-Completed & non-manual tidak terblokir (guard hanya menolak Completed; redirect hanya untuk IsManualEntry).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -5 && dotnet test --filter "Category!=Integration" 2>&1 | tail -10</automated>
  </verify>
  <done>Guard Completed group-aware diangkat sebelum cabang Pre-Post; redirect IsManualEntry di GET; antiforgery/authz utuh; build 0 error; EditGuardRedirect420Tests hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST/GET EditAssessment (Admin/HC) | Permintaan edit metadata menyeberang ke server; server harus menolak edit sesi Completed & meng-route sesi manual dengan benar. |
| Routing decision (GET) | Keputusan form mana yang ditampilkan = server-authoritative (IsManualEntry). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-420-lock | Tampering | Mutasi metadata sesi/grup Completed (E-04) | mitigate | Guard server-side group-aware (`AnyAsync(...Completed)`) DIANGKAT ke atas cabang Pre-Post (sebelum mutasi). Menolak edit bila ada sesi grup Completed → integritas cert/skor terjaga. Bukan client-only. |
| T-420-manual | Tampering | Bypass redirect manual via direct POST online | mitigate | FORM-06 redirect di GET = UX/routing. Defense-in-depth: guard POST Completed (`:2006` / guard baru) tetap mencegah mutasi (sesi manual umumnya Completed; audit E-08). FORM-05 guard juga melindungi POST. |
| T-420-csrf | Spoofing | CSRF pada POST Edit | mitigate | `[ValidateAntiForgeryToken]` (`:1807`) DIPERTAHANKAN saat menambah guard. |
| T-420-authz | Elevation of Privilege | Akses Edit oleh non-Admin/HC | mitigate | `[Authorize(Roles="Admin, HC")]` (`:1681/:1806`) DIPERTAHANKAN — tidak diubah plan ini. |
| T-420-idor-edit | Information Disclosure | Akses sesi orang lain via id | accept | Edit assessment-level (bukan per-jawaban-peserta); authz role-based Admin/HC sudah membatasi ke pengelola; tidak ada eskalasi baru. Konsisten perilaku existing. |

**Catatan:** Plan ini menambah guard + redirect — tidak ada endpoint baru. Risiko utama = data-integrity (Tampering metadata Completed) dimitigasi server-side. Secure-phase formal (`gsd-secure-phase 420`) = gerbang terpisah.
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "FullyQualifiedName~EditGuardRedirect420"` (real-SQL) — 5 test hijau setelah Task 1.
- `dotnet test --filter "Category!=Integration"` — tidak ada regresi.
- Grep gate: guard `LinkedGroupId == assessment.LinkedGroupId && a.Status == "Completed"` hadir SEBELUM cabang Pre-Post.
- Grep gate: `RedirectToAction("EditManualAssessment", "TrainingAdmin"` hadir di GET Edit.
- Backward-compat: sesi standard non-Completed non-manual tetap dapat di-Edit (Test 3 + Test 5 negatif hijau).
</verification>

<success_criteria>
- FORM-05: POST Edit untuk sesi/grup Completed ditolak (group-aware) → redirect + TempData Error, metadata tak dimutasi.
- FORM-06: GET Edit sesi IsManualEntry → redirect EditManualAssessment (TrainingAdmin).
- Sesi standard yang sah (non-Completed, non-manual) tetap dapat di-Edit (backward-compat).
- Tidak menghapus antiforgery/authz; tidak reuse helper bersemantik terbalik.
</success_criteria>

<output>
After completion, create `.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-02-SUMMARY.md`
</output>
