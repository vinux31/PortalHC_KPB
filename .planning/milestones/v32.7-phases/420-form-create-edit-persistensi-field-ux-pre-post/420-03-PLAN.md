---
phase: 420-form-create-edit-persistensi-field-ux-pre-post
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
autonomous: false
files_modified:
  - Views/Admin/CreateAssessment.cshtml
  - Controllers/AssessmentAdminController.cs
  - Models/AssessmentSession.cs
  - tests/e2e/form-prepost-ux-420.spec.ts
  - tests/e2e/form-persistence-420.spec.ts
requirements: [FORM-01, FORM-07, FORM-08, FORM-09, FORM-10, FORM-11]
migration: false

must_haves:
  truths:
    - "Mode Pre-Post: checkbox SamePackage tampil di header section Pre-Post (di bawah pemilih Tipe Assessment), bukan di dalam kartu Post"
    - "Mode Pre-Post: Group 'Pengaturan Ujian' disajikan sebagai dua sub-kartu — 'Setelan Post-Test' + 'Setelan Bersama Pre & Post'"
    - "Mode Pre-Post: blok Ujian Ulang DISEMBUNYIKAN dan tidak ter-POST (retake hanya relevan Post; Pre baseline)"
    - "Mode Pre-Post: input jadwal/durasi/EWCD standard + hidden combiner Schedule/ExamWindowCloseDate disabled (tidak ter-POST)"
    - "Penanda mode internal di-rename dari AssessmentTypeInput → CreationMode (binding tetap utuh: pilih Pre-Post → 2 sesi terbuat)"
    - "Mode Standard: DOM + payload TIDAK berubah perilaku (sub-kartu absen, retake tampil & ter-POST, input standard ter-POST, layout tunggal)"
    - "FORM-01 lifecycle ter-cover e2e: create assessment standard shuffle ON → buka Edit → submit → buka ulang → ShuffleQuestions/ShuffleOptions MASIH checked (tidak reset OFF)"
  artifacts:
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "SamePackage header + dua sub-kartu Pre-Post + retake hidden + std input disabled saat Pre-Post + rename name/id"
      contains: "Setelan Bersama Pre &amp; Post"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Rename param AssessmentTypeInput → CreationMode (8 ref) + validasi/ModelState key"
      contains: "string? CreationMode"
    - path: "Models/AssessmentSession.cs"
      provides: "XML-doc AssessmentType diperbarui ke 4-nilai (PreTest/PostTest/Standard/Manual)"
      contains: "Manual"
    - path: "tests/e2e/form-prepost-ux-420.spec.ts"
      provides: "Playwright per-mode render (FORM-07/08/09/10/11) + regresi Standard"
      contains: "Setelan Bersama Pre"
    - path: "tests/e2e/form-persistence-420.spec.ts"
      provides: "Playwright FORM-01 lifecycle (create shuffle ON → Edit → reopen → masih ON) + SEED_WORKFLOW snapshot/restore"
      contains: "ShuffleQuestions"
  key_links:
    - from: "CreateAssessment.cshtml select name=CreationMode (:215)"
      to: "POST CreateAssessment param string? CreationMode"
      via: "model binding name↔param (rename atomik)"
      pattern: "name=\"CreationMode\""
    - from: "JS toggle change (CreationMode == 'PrePostTest')"
      to: "sub-kartu render + retake hidden + std input disabled + SamePackage header"
      via: "classList + disabled toggle dengan aksi-balik di else"
      pattern: "PrePostTest"
    - from: "std input section + #ewcdHidden disabled saat Pre-Post"
      to: "POST payload TANPA Schedule/ExamWindowCloseDate standard"
      via: "HTML disabled attribute tak ikut submit"
      pattern: "disabled"
    - from: "tests/e2e/form-persistence-420.spec.ts (FORM-01 lifecycle)"
      to: "EditAssessment.cshtml render shuffle (Plan 01) → state tersimpan setelah Edit"
      via: "create shuffle ON → buka Edit → submit → reopen → assert checked"
      pattern: "form-persistence-420"
---

<objective>
Redesign tata-letak form Create mode Pre-Post agar scope tiap setelan jelas dan tidak ada field duplikat tersembunyi yang ikut ter-POST — **mode Standard WAJIB tidak berubah perilaku** (backward-compat = risiko utama fase). Plus rename atomik penanda mode internal.

1. **FORM-07 (L-4):** Pindahkan checkbox SamePackage dari DALAM kartu Post → ke header section Pre-Post (di bawah pemilih Tipe Assessment, di atas pasangan kartu Pre & Post).
2. **FORM-08 + FORM-11 (L-2/L-3):** Saat Pre-Post, sajikan Group "Pengaturan Ujian" sebagai DUA sub-kartu: "Setelan Post-Test" (PassPercentage relabel + Sertifikat + retake-disembunyikan) dan "Setelan Bersama Pre & Post" (Shuffle + AllowAnswerReview + Token). Rapikan Status/PassPercentage yang timpang.
3. **FORM-09 (L-5):** Input jadwal/durasi/EWCD standard + hidden combiner `Schedule`/`ExamWindowCloseDate` di-`disabled` saat Pre-Post (BUKAN `d-none`) → tidak ter-POST. Aksi-balik saat Standard.
4. **FORM-10 (L-6):** Rename atomik `AssessmentTypeInput` → `CreationMode` (8 ref controller + 9 ref view) + perbarui XML-doc `AssessmentSession.AssessmentType` usang.

Purpose: Form Pre-Post yang jelas scope-nya mencegah HC salah-isi (mengira retake/pass berlaku ke Pre baseline) dan menghilangkan field duplikat ter-POST yang membingungkan grading. Rename menutup kerancuan `AssessmentTypeInput` (input form Standard/PrePostTest) vs kolom DB `AssessmentType` (PreTest/PostTest/Standard/Manual).
Output: 1 view (redesign Pre-Post + JS toggle + rename), 1 controller (rename param), 1 model (XML-doc), 1 spec Playwright baru. Plan punya **checkpoint:human-verify** (UAT @5270).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-CONTEXT.md
@.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-RESEARCH.md
@.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-PATTERNS.md
@.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-UI-SPEC.md

<interfaces>
<!-- Kontrak dari codebase 2026-06-22, JANGAN eksplorasi ulang. -->

JS toggle existing (CreateAssessment.cshtml:1996-2018) — SATU listener change, PERLUAS:
```javascript
typeSelect.addEventListener('change', function() {
    if (this.value === 'PrePostTest') {
        pptSection.classList.add('show');
        if (stdSection) stdSection.classList.add('d-none');
        if (statusWrapper) statusWrapper.classList.add('d-none');
        if (certNote) certNote.classList.remove('d-none');
        if (statusEl) statusEl.value = 'Upcoming';
        // [BARU] sub-kartu (L-2), hide+disable retake (L-3), SamePackage header (L-4), disable std inputs (L-5)
    } else {
        // [BARU] AKSI-BALIK SEMUA → DOM+payload Standard identik (Pitfall #2)
    }
});
```

Rename surface FORM-10 (VERIFIED grep 2026-06-22):
- Controller (Controllers/AssessmentAdminController.cs): :840 ViewBag, :867 param `string? AssessmentTypeInput`, :878 compare, :925 `isPrePostMode`, :1040-1043 validasi + `ModelState.AddModelError("AssessmentTypeInput",...)`.
- View (CreateAssessment.cshtml): :215 `<select name="AssessmentTypeInput" id="assessmentTypeInput">` + 8 JS `getElementById('assessmentTypeInput')` (:1013,:1140,:1403,:1431,:1565,:1934,:1986,:2078).
- Binding pair WAJIB rename bersamaan: name (HTML) ↔ param (controller).

Markup yang dipindah/di-toggle (CreateAssessment.cshtml):
- SamePackage checkbox :473-481 (di kartu Post) + `#samePackageBadge` :478 + listener :2027-2032 → PINDAH ke header.
- `#standard-jadwal-section` :382-421 + hidden combiner `asp-for="Schedule"` :424 (`#schedHidden`) + `asp-for="ExamWindowCloseDate"` :425 (`#ewcdHidden`).
- Group B "Pengaturan Ujian" :488-579 (Status/PassPercentage :496-512, Token :516-533, Shuffle :536-551, Retake :552-576).
- Group C cert :582-627, Group D AllowAnswerReview :630-643.
- `#statusFieldWrapper` :496, `#prePostCertNote` (text-info).

Idiom sub-kartu nested (analog kartu Pre/Post :429-433):
```html
<div class="card mb-3">
  <div class="card-header bg-light fw-semibold"><i class="bi bi-clock-history"></i> Setelan Post-Test</div>
  <div class="card-body">...</div>
</div>
```

Template e2e: tests/e2e/shuffle.spec.ts (serial + db.backup/restore + login admin). Branch ITHandoff: E2E_BASE_URL=http://localhost:5270, --workers=1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FORM-10 rename atomik AssessmentTypeInput → CreationMode (8 ref controller + 9 ref view + XML-doc)</name>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/CreateAssessment.cshtml, Models/AssessmentSession.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (:840, :867, :878, :925, :1040-1043 — semua ref AssessmentTypeInput)
    - Views/Admin/CreateAssessment.cshtml (:215 select name/id + 8 JS getElementById :1013,:1140,:1403,:1431,:1565,:1934,:1986,:2078)
    - Models/AssessmentSession.cs (:165-177 XML-doc AssessmentType usang)
    - 420-PATTERNS.md §FORM-10 (surface PERSIS + Pitfall #3 atomik)
    - 420-RESEARCH.md §Runtime State Inventory (rename surface) + §Pitfall 3
  </read_first>
  <action>
    Rename ATOMIK dalam satu sapuan (Pitfall #3: rename parsial = binding putus → mode selalu ke-baca Standard). Lakukan rename `AssessmentTypeInput` → `CreationMode` di SEMUA ref:
    - **Controller param + binding:** `:867` `string? AssessmentTypeInput` → `string? CreationMode`; `:878` compare `AssessmentTypeInput != "PrePostTest"` → `CreationMode != "PrePostTest"`; `:925` `AssessmentTypeInput == "PrePostTest"` → `CreationMode == "PrePostTest"`; `:840` `ViewBag.AssessmentTypeInput` → `ViewBag.CreationMode`; `:1040-1043` validasi + `ModelState.AddModelError("AssessmentTypeInput", ...)` → key `"CreationMode"`.
    - **View HTML binding:** `:215` `name="AssessmentTypeInput"` → `name="CreationMode"`; `id="assessmentTypeInput"` → `id="creationMode"` (rename id konsisten — RESEARCH Open Q3 rec: rename keduanya untuk kebersihan).
    - **View JS:** 8 `getElementById('assessmentTypeInput')` (:1013,:1140,:1403,:1431,:1565,:1934,:1986,:2078) → `getElementById('creationMode')`.
    - **Label UI "Tipe Assessment"** + option "Standard"/"Pre-Post Test" → BIARKAN (tidak ada perubahan visual wajib, L-6).
    - **XML-doc** `AssessmentSession.cs:170-171` usang (hanya PreTest/PostTest/null) → perbarui jadi 4-nilai: PreTest / PostTest / Standard / Manual.
    JANGAN rename kolom DB `AssessmentType` (tak tersentuh — RESEARCH Runtime State Inventory: no stored data bernama AssessmentTypeInput). Gunakan grep akhir untuk memastikan 0 sisa `AssessmentTypeInput`/`assessmentTypeInput`.
    Setelah rename, `dotnet build` 0 error WAJIB; gate utama = e2e smoke Task 4 (pilih Pre-Post → sub-kartu muncul = binding utuh).
  </action>
  <acceptance_criteria>
    - Grep `AssessmentTypeInput` (PascalCase) di Controllers/ + Views/ = 0 (kecuali komentar/docs historis bila ada — produksi 0).
    - Grep `assessmentTypeInput` (camelCase id) di Views/ = 0.
    - `AssessmentAdminController.cs` mengandung `string? CreationMode` (param) + `ModelState.AddModelError("CreationMode"`.
    - `CreateAssessment.cshtml` mengandung `name="CreationMode"` + `getElementById('creationMode')` (≥8 kemunculan JS).
    - `AssessmentSession.cs` XML-doc AssessmentType menyebut keempat nilai: PreTest, PostTest, Standard, Manual.
    - Kolom DB `AssessmentType` TIDAK di-rename (grep `AssessmentType = "Standard"` dll tetap ada di build path).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>Rename atomik selesai (0 sisa AssessmentTypeInput/assessmentTypeInput di produksi); CreationMode konsisten name/param/id/JS; XML-doc 4-nilai; kolom DB tak tersentuh; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: FORM-07 SamePackage header + FORM-08/11 dua sub-kartu Pre-Post + FORM-09 disable std input (markup + JS toggle)</name>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml (:213-223 type header; :382-425 std jadwal + hidden combiner; :429-484 kartu Pre/Post + SamePackage :473-481; :488-643 Group B/C/D; :1986-2033 JS toggle)
    - 420-UI-SPEC.md §L-2/L-3/L-4/L-5 + §Visibility Matrix + §Copywriting Contract (load-bearing)
    - 420-PATTERNS.md §CreateAssessment.cshtml (idiom sub-kartu + FORM-07/08/09 + JS toggle perluas)
    - 420-RESEARCH.md §Pitfall 2 (backward-compat Standard) + §Pitfall 4/5 (disabled bukan d-none, hidden combiner)
  </read_first>
  <action>
    Ikuti UI-SPEC L-2..L-5 + Visibility Matrix PERSIS. Mode **Standard markup/payload TIDAK boleh berubah** (Pitfall #2: tiap elemen yang di-toggle di cabang PrePostTest WAJIB punya aksi-balik di cabang else).

    **(A) FORM-07 / L-4 — SamePackage ke header:** PINDAHKAN markup checkbox `SamePackage` + `#samePackageBadge` (`:473-481`) dari DALAM kartu Post → ke header section Pre-Post (di bawah Type select `:213-223`, di atas `#ppt-jadwal-section`). Bungkus dalam wrapper ber-id (mis. `#samePackageHeaderWrapper`) yang default `d-none`, hanya tampil saat Pre-Post via JS. Pertahankan label "Gunakan paket soal yang sama untuk Pre dan Post" + badge `bg-info` "Paket soal Post-Test akan otomatis disinkronkan dari Pre-Test" + listener toggle badge `:2027-2032` (sesuaikan referensi jika id berubah).

    **(B) FORM-08/11 / L-2/L-3 — dua sub-kartu (HANYA tampil Pre-Post):** Tambah DUA sub-kartu nested (idiom `card mb-3` + `card-header bg-light fw-semibold`) di dalam Group B, ber-wrapper id (mis. `#prePostSettingsCards`) default `d-none`:
    - **Sub-kartu "Setelan Post-Test"** (`bi-clock-history`): PassPercentage (relabel "Nilai Lulus Post-Test (%)" + form-text "Nilai minimum untuk lulus Post-Test (0–100)"), Sertifikat (GenerateCertificate switch + ValidUntil) + note `#prePostCertNote` text-info "Sertifikat hanya diterbitkan untuk peserta yang lulus Post-Test". (Retake DISEMBUNYIKAN — lihat C.)
    - **Sub-kartu "Setelan Bersama Pre & Post"** (`bi-arrow-left-right` atau `bi-link-45deg`): Shuffle (dua form-switch), AllowAnswerReview (pindah-render dari Group D saat Pre-Post), Token.
    **PENTING (backward-compat):** Pendekatan AMAN = sub-kartu adalah WRAPPER baru yang HANYA dipakai mode Pre-Post; layout existing Group B/C/D `row g-3` tunggal TETAP untuk mode Standard. JS toggle yang menampilkan/menyembunyikan wrapper sub-kartu vs layout tunggal. Hindari memindahkan field secara permanen yang mengubah DOM Standard. Bila perlu duplikasi markup field di sub-kartu, pastikan HANYA SATU yang aktif (yang lain `disabled`) agar tidak ada field ganda ter-POST. Executor pilih pendekatan paling aman yang menjaga Standard payload identik — dokumentasikan di SUMMARY.

    **(C) FORM-11 / L-3 — retake hidden + tidak ter-POST saat Pre-Post:** Blok Ujian Ulang (`:552-576`) bungkus ber-id (mis. `#retakeBlockCreate`). Saat Pre-Post → `d-none` + `disabled` pada input retake (AllowRetake/MaxAttempts/RetakeCooldownHours) sehingga tidak ter-POST. Saat Standard → tampil + enabled (aksi-balik). Status/PassPercentage dirapikan: `#statusFieldWrapper` sudah di-`d-none` Pre-Post (:2005), pastikan PassPercentage tidak tertinggal sebagai kolom setengah-baris asimetris (di sub-kartu Post ia field penuh/rapi).

    **(D) FORM-09 / L-5 — disable std input (BUKAN d-none):** Perluas JS toggle: saat Pre-Post, set `disabled=true` pada SEMUA input/select di `#standard-jadwal-section` + hidden combiner `#schedHidden`(:424) + `#ewcdHidden`(:425). Saat Standard, `disabled=false` (KEMBALIKAN semua — Pitfall #5: jangan lupa hidden combiner). `d-none` boleh tetap untuk visual, tapi `disabled` yang mencegah POST.

    **(E) JS toggle — perluas listener `:1996-2018`:** Di cabang `if (this.value === 'PrePostTest')` tambah: tampilkan `#samePackageHeaderWrapper`, tampilkan `#prePostSettingsCards`, sembunyikan layout Group B/C/D tunggal (bila pendekatan wrapper), sembunyikan+disable `#retakeBlockCreate`, disable std inputs + combiner. Di cabang `else` (Standard): AKSI-BALIK SEMUA (sembunyikan header SamePackage, sembunyikan sub-kartu + tampilkan layout tunggal, tampilkan+enable retake, enable std inputs). Visibility Matrix UI-SPEC = sumber kebenaran. JANGAN ubah perilaku Status/certNote existing (sudah ada).
    Semua copy Bahasa Indonesia per Copywriting Contract. Razor `@`-encode untuk teks (tidak ada Html.Raw baru — XSS-safe).
  </action>
  <acceptance_criteria>
    - SamePackage checkbox berada di header (di luar kartu Post): markup `name="SamePackage"` muncul SEBELUM `#ppt-jadwal-section` di urutan DOM; tidak lagi di dalam kartu Post.
    - `CreateAssessment.cshtml` mengandung heading "Setelan Post-Test" DAN "Setelan Bersama Pre &amp; Post" (atau "Setelan Bersama Pre & Post").
    - Wrapper sub-kartu Pre-Post default `d-none` (hanya tampil via JS saat Pre-Post).
    - Blok retake ber-id wrapper; JS men-`disabled` input retake saat Pre-Post.
    - JS toggle men-`disabled` `#standard-jadwal-section` inputs + `#schedHidden` + `#ewcdHidden` saat Pre-Post, dan `disabled=false` saat Standard (aksi-balik ada di cabang else).
    - Cabang `else` (Standard) di listener mengembalikan SEMUA elemen (grep: cabang else berisi aksi balik untuk header/sub-kartu/retake/std-inputs).
    - PassPercentage di sub-kartu Post berlabel "Nilai Lulus Post-Test".
    - Tidak ada `Html.Raw` baru; copy Bahasa Indonesia.
    - Backward-compat: layout Group B/C/D tunggal Standard tetap ada (tidak dihapus permanen).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>SamePackage di header; dua sub-kartu Pre-Post (default hidden); retake hidden+disabled saat Pre-Post; std input+combiner disabled saat Pre-Post dengan aksi-balik; mode Standard layout tunggal utuh; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: DUA spec Playwright — (A) form-persistence-420 (FORM-01 lifecycle WAJIB) + (B) form-prepost-ux-420 (per-mode FORM-07..11 + regresi Standard)</name>
  <files>tests/e2e/form-persistence-420.spec.ts, tests/e2e/form-prepost-ux-420.spec.ts</files>
  <read_first>
    - tests/e2e/shuffle.spec.ts (template serial + db.backup/restore SEED_WORKFLOW + login admin + assert DOM checkbox checked)
    - tests/helpers/auth.ts (login admin@pertamina.com dev lokal)
    - 420-PATTERNS.md §form-prepost-ux-420.spec.ts (pola assert per-FORM)
    - 420-RESEARCH.md §Validation Architecture (Test→Map FORM-01 + FORM-07..11 + regresi Standard) + §Wave 0 Gaps (KEDUA spec WAJIB) + §Environment Availability (5270)
    - 420-UI-SPEC.md §Visibility Matrix (assert per-mode)
  </read_first>
  <behavior>
    Spec A `form-persistence-420.spec.ts` (FORM-01 = HIGH/E-01 + Success Criteria #1) — lifecycle WAJIB:
    - Lifecycle: login admin → create assessment standard dengan Acak Soal & Acak Pilihan ON → buka `/Admin/EditAssessment/{id}` → assert checkbox `#ShuffleQuestions` + `#ShuffleOptions` `toBeChecked` (membuktikan render Edit Plan 01 mengisi dari state tersimpan) → submit form Edit → buka ulang `/Admin/EditAssessment/{id}` → assert KEDUA masih `toBeChecked` (TIDAK reset OFF). Ini menutup akar bug E-01 end-to-end (render+write+reopen).
    Spec B `form-prepost-ux-420.spec.ts` (FORM-07/08/09/10/11 + regresi Standard) — per-mode render.
  </behavior>
  <action>
    **(A) Buat `tests/e2e/form-persistence-420.spec.ts` (FORM-01 lifecycle — WAJIB, RESEARCH §Wave 0 Gaps):**
    Ikuti idiom `shuffle.spec.ts` PERSIS: `test.describe.configure({ mode: 'serial' })`, `beforeAll` db.backup(snapshot) / `afterAll` db.restore(snapshot) (SEED_WORKFLOW — CLAUDE.md), `login(page,'admin')`. `E2E_BASE_URL=http://localhost:5270`, `--workers=1`. Skenario lifecycle:
    1. Create assessment standard via UI (`/Admin/CreateAssessment`) dengan toggle Acak Soal + Acak Pilihan ON; submit → dapatkan id (dari redirect URL / list).
    2. Buka `/Admin/EditAssessment/{id}` → `await expect(page.locator('#ShuffleQuestions')).toBeChecked()` + `#ShuffleOptions` `toBeChecked()` (membuktikan Plan 01 render shuffle dari Model).
    3. Submit form Edit (tanpa mengubah shuffle) → buka ULANG `/Admin/EditAssessment/{id}` → assert KEDUA checkbox MASIH `toBeChecked()` (E-01 closed: tidak reset OFF).
    Bila pembuatan via UI penuh terlalu berat untuk satu skenario, boleh seed sesi standard shuffle-ON langsung ke DB di `beforeAll` (snapshot dulu) lalu fokus assert reopen-after-edit; dokumentasikan pendekatan di SUMMARY. INTI yang tidak boleh dikompromikan: assert "buka Edit → submit → buka ulang → shuffle MASIH checked".

    **(B) Buat `tests/e2e/form-prepost-ux-420.spec.ts` (per-mode FORM-07..11 + regresi Standard):**
    Idiom sama (`mode: 'serial'`, db.backup/restore, login admin, 5270, `--workers=1`). Skenario:
    - **FORM-08:** buka /Admin/CreateAssessment → pilih "Pre-Post Test" → assert dua sub-kartu ("Setelan Post-Test" + "Setelan Bersama Pre & Post") `toBeVisible`. Pilih kembali "Standard" → sub-kartu `toHaveCount(0)` atau `not.toBeVisible` (regresi).
    - **FORM-07:** mode Pre-Post → SamePackage checkbox `toBeVisible` dan berada di header (di luar kartu Post — assert via locator path/urutan); mode Standard → header SamePackage hidden.
    - **FORM-11:** mode Pre-Post → blok Ujian Ulang hidden/`toHaveCount(0)` visible-wise; input retake `toBeDisabled`. Mode Standard → blok retake `toBeVisible` + enabled.
    - **FORM-09:** mode Pre-Post → input `#standard-jadwal-section` + `#ewcdHidden` + `#schedHidden` `toBeDisabled`. Mode Standard → enabled.
    - **FORM-10:** smoke binding — pilih Pre-Post → sub-kartu muncul (membuktikan `name="CreationMode"` ter-bind & JS `creationMode` jalan). Idealnya isi minimal form Pre-Post valid → submit → assert 2 sesi/redirect sukses (bila feasible dengan seed; bila tidak, smoke render cukup + catat di SUMMARY).
    - **Regresi Standard (Pitfall #2):** mode Standard render → assert layout tunggal, retake tampil, std input enabled, sub-kartu absen (DOM/perilaku identik). Plus jalankan suite existing `shuffle.spec.ts assessment.spec.ts` sebagai regresi terpisah (di checkpoint Task 4).

    Executor TIDAK menjalankan live e2e (delegasi ke checkpoint Task 4 / orchestrator UAT gate). Pastikan KEDUA spec `--list` parse OK + repo build 0 error.
  </action>
  <acceptance_criteria>
    - `tests/e2e/form-persistence-420.spec.ts` ADA (FORM-01 lifecycle), `mode: 'serial'` + db.backup/restore + login admin; mengandung assert `#ShuffleQuestions`/`#ShuffleOptions` `toBeChecked` SETELAH submit+reopen (grep "ShuffleQuestions" ≥1, "toBeChecked" ≥1, "EditAssessment" ≥1).
    - `tests/e2e/form-prepost-ux-420.spec.ts` ADA, `mode: 'serial'` + db.backup/restore + login admin; mengandung assert FORM-07/08/09/10/11 + regresi Standard (grep "Setelan Bersama Pre" ≥1, "toBeDisabled" ≥1, "Standard" regresi assert).
    - `npx playwright test form-persistence-420 form-prepost-ux-420 --list` parse OK (KEDUA spec; tidak dijalankan live oleh executor).
    - `dotnet build` 0 error (pastikan repo build).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff/tests" && npx playwright test form-persistence-420 form-prepost-ux-420 --list 2>&1 | tail -15</automated>
  </verify>
  <done>DUA spec ada: form-persistence-420 (FORM-01 lifecycle: create shuffle ON → Edit → submit → reopen → MASIH checked) + form-prepost-ux-420 (per-mode FORM-07..11 + regresi Standard); KEDUA parse `--list` OK; SEED_WORKFLOW snapshot/restore. Live run = checkpoint Task 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT live @5270 — render per-mode + regresi Standard + grep gate</name>
  <action>
    Checkpoint UAT manusia (blocking) — executor TIDAK menjalankan live run; ini gerbang verifikasi visual/fungsional yang dipegang orchestrator/user. Lakukan langkah persis di <how-to-verify> (build + dotnet test full + Playwright per-mode & regresi + browser manual @5270 + SEED_WORKFLOW snapshot→restore). Resume hanya setelah semua PASS.
  </action>
  <what-built>
    Redesign form Create Pre-Post: SamePackage di header (FORM-07), dua sub-kartu "Setelan Post-Test" + "Setelan Bersama Pre & Post" (FORM-08), retake disembunyikan saat Pre-Post (FORM-11), input standard disabled tak ter-POST saat Pre-Post (FORM-09), rename CreationMode (FORM-10). Plus persistensi Plan 01 (shuffle Edit render, retake/ValidUntil) + guard Plan 02 (lock Completed, redirect manual). Semua via Bootstrap 5.3 existing, Bahasa Indonesia, migration=FALSE.
  </what-built>
  <how-to-verify>
    Jalankan lokal di branch ITHandoff (port 5270 — `dotnet run --urls http://localhost:5270`). SEED_WORKFLOW: snapshot DB lokal dulu (CLAUDE.md). Lalu:
    1. **dotnet build** 0 error + **dotnet test** full (`HcPortalDB_Dev` @SQLEXPRESS) — FormPersistence420 + EditGuardRedirect420 + suite existing hijau, no regresi.
    2. **Playwright**: `cd tests && npx playwright test form-persistence-420 form-prepost-ux-420 shuffle assessment --workers=1` (E2E_BASE_URL=http://localhost:5270) — FORM-01 lifecycle + per-mode baru + regresi Standard hijau, 0 pageerror.
    3. **Browser manual @5270**: buka /Admin/CreateAssessment →
       (a) default Standard: layout tunggal, blok Ujian Ulang tampil, input jadwal standard aktif, TIDAK ada sub-kartu (regresi PASS).
       (b) pilih "Pre-Post Test": SamePackage muncul di header (bukan di kartu Post); dua sub-kartu "Setelan Post-Test" + "Setelan Bersama Pre & Post"; blok Ujian Ulang HILANG; input jadwal standard nonaktif (disabled).
       (c) submit Pre-Post valid → DevTools Network: payload TIDAK mengandung Schedule/ExamWindowCloseDate standard maupun field retake; 2 sesi (Pre+Post) terbuat di DB.
       (d) submit Standard valid → 1 sesi standard, retake config + shuffle tersimpan (cek DB).
       (e) Edit assessment standard shuffle ON → simpan → buka ulang → Acak Soal/Pilihan masih ON (FORM-01). Edit ValidUntil → tersimpan (FORM-04).
       (f) buka Edit sesi manual → ter-redirect ke EditManualAssessment (FORM-06). Coba Edit sesi/grup Completed → ditolak (FORM-05).
    4. **Cleanup**: restore DB ke snapshot; tandai SEED_JOURNAL `cleaned`.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila semua PASS (per-mode + regresi Standard + persistensi + guard), atau jelaskan temuan untuk diperbaiki.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form (mode toggle) → POST CreateAssessment (Admin/HC) | Mode + field config menyeberang ke server; payload harus bebas field standard stale saat Pre-Post & sebaliknya. |
| Rename binding name↔param | Kontrak binding form↔controller; rename parsial = mode salah-baca (Tampering/availability). |
| Razor SSR → DOM (copy Bahasa Indonesia) | Teks UI dirender ke browser; harus `@`-encode (no Html.Raw). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-420-stale | Tampering | Input standard jadwal/EWCD ter-POST saat Pre-Post (FORM-09) | mitigate | `disabled` attribute pada `#standard-jadwal-section` + `#schedHidden` + `#ewcdHidden` saat Pre-Post (HTML standar: disabled tak ter-submit) — bukan `d-none` (Pitfall #4/#5). Server cabang Pre-Post tak baca field standard. |
| T-420-massassign | Tampering | Mass-assignment via rename CreationMode (over-posting kolom sensitif) | mitigate | Rename murni penanda mode (`AssessmentTypeInput`→`CreationMode`) sebagai param scalar eksplisit; TIDAK mem-bind kolom DB sensitif. `name`/`param` rename atomik menjaga binding eksplisit (bukan model penuh). Kolom DB `AssessmentType` tak di-rename/bind dari form. |
| T-420-csrf | Spoofing | CSRF pada POST Create | mitigate | `[ValidateAntiForgeryToken]` existing DIPERTAHANKAN saat refactor view/rename. |
| T-420-xss | Information Disclosure / Tampering | Copy Bahasa Indonesia via Razor | mitigate | Semua teks via Razor `@`-encode default; tidak ada `Html.Raw` baru (gate grep = 0 Html.Raw baru). |
| T-420-binding-break | Denial of Service (availability) | Rename parsial → mode selalu Standard / form 500 | mitigate | Rename ATOMIK 1 commit (Pitfall #3) + gate: `dotnet build` 0 error + e2e smoke "pilih Pre-Post → sub-kartu muncul" (Task 3/4) membuktikan binding utuh. |

**Catatan:** Tidak ada endpoint baru, tidak ada eskalasi privilege. Risiko = data-integrity (stale payload) + availability (binding rename) — dimitigasi `disabled` + rename atomik + antiforgery dipertahankan. Secure-phase formal (`gsd-secure-phase 420`) = gerbang terpisah.
</threat_model>

<verification>
- `dotnet build` 0 error.
- Grep gate: 0 sisa `AssessmentTypeInput`/`assessmentTypeInput` di produksi; `name="CreationMode"` + `getElementById('creationMode')` hadir.
- Grep gate: heading "Setelan Bersama Pre" + "Setelan Post-Test" hadir; SamePackage di luar kartu Post.
- `npx playwright test form-persistence-420 form-prepost-ux-420 --list` parse OK (KEDUA spec).
- Checkpoint Task 4: UAT live @5270 PASS (per-mode + regresi Standard `shuffle.spec.ts`/`assessment.spec.ts` hijau + persistensi + guard) + DB snapshot→restore (SEED_JOURNAL cleaned).
- Backward-compat: mode Standard DOM+payload tidak berubah perilaku (regresi suite hijau).
</verification>

<success_criteria>
- FORM-07: SamePackage di header section Pre-Post (bukan kartu Post).
- FORM-08: dua sub-kartu scope-explicit muncul saat Pre-Post.
- FORM-09: input standard jadwal/EWCD tidak ter-POST saat Pre-Post (disabled).
- FORM-10: rename CreationMode atomik, binding utuh (Pre-Post → 2 sesi).
- FORM-11: retake hidden + Status/PassPercentage rapi saat Pre-Post.
- Mode Standard tidak berubah perilaku (DOM + payload identik; regresi hijau).
</success_criteria>

<output>
After completion, create `.planning/phases/420-form-create-edit-persistensi-field-ux-pre-post/420-03-SUMMARY.md`
</output>
