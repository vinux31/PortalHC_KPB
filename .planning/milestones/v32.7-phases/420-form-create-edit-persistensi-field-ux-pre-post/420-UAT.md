---
status: complete
phase: 420-form-create-edit-persistensi-field-ux-pre-post
source: [420-01-SUMMARY.md, 420-02-SUMMARY.md, 420-03-SUMMARY.md]
started: 2026-06-23T01:55:00Z
updated: 2026-06-23T02:15:00Z
---

## Current Test

[testing complete]

<!--
METODE: UAT live @localhost:5270 (branch ITHandoff). Verifikasi PRODUK authoritative via
Playwright-MCP (DOM-state eval render-only + lifecycle Edit nyata) DITAMBAH e2e spec hijau.
Ini menutup Task 4 (checkpoint:human-verify) Plan 420-03 yang AWAITING.

Setup env: SQLBrowser started, app @5270 (conn override lpc:Lenovo\SQLEXPRESS + AD-off),
build 0 error. Stale matrix seed (9001-9018, 18 row) dari run lama dibersihkan (backup dulu,
6-step FK cleanup by Title prefix). DB akhir pristine (matrix=0).
-->

## Tests

### 1. FORM-01 — Acak Soal/Pilihan TIDAK reset OFF setelah Edit (akar bug E-01, HIGH)
expected: EditAssessment merender #ShuffleQuestions/#ShuffleOptions checked dari Model; submit (tanpa ubah) → reopen → KEDUA tetap checked. DB tetap ShuffleQuestions=1/ShuffleOptions=1.
result: pass
evidence: Live @5270 assessment Id 172 (Standard, shuffle ON) → Edit render "Acak Soal"+"Acak Pilihan Jawaban" [checked] → "Simpan Perubahan" ("updated successfully") → reopen MASIH [checked]; DB 172 ShuffleQuestions=1 ShuffleOptions=1 post-edit. e2e form-persistence-420 hijau.

### 2. FORM-07 — SamePackage di header section (Pre-Post), bukan kartu Post
expected: Pre-Post → #samePackageHeaderWrapper tampil (d-none lepas), input SamePackage ada di header, 0 SamePackage di #ppt-jadwal-section. Standard → header hidden.
result: pass
evidence: DOM eval — Standard: samePackageHeaderWrapper d-none=true; Pre-Post: d-none=false, sameInPptCount=0, sameInHeaderCount=1. e2e FORM-07 hijau.

### 3. FORM-08 — Dua sub-kartu Pre-Post ("Setelan Post-Test" + "Setelan Bersama Pre & Post")
expected: Pre-Post → #prePostSettingsCards tampil + dua sub-kartu hadir. Standard → wrapper hidden (DOM tunggal).
result: pass
evidence: DOM eval — Standard prePostSettingsCards d-none=true; Pre-Post d-none=false; 2 sub-kartu hadir (scoped count). e2e FORM-08/10 hijau.

### 4. FORM-09 — Input std jadwal + #schedHidden/#ewcdHidden disabled saat Pre-Post (tak ter-POST)
expected: Pre-Post → #schedDateInput/#schedHidden/#ewcdHidden disabled. Standard → enabled (aksi-balik).
result: pass
evidence: DOM eval — Pre-Post: ketiga disabled=true; Standard (default + round-trip): disabled=false. e2e FORM-09 hijau.

### 5. FORM-10 — Rename atomik AssessmentTypeInput → CreationMode (binding utuh + backward-compat)
expected: select name/id #creationMode mengikat; change handler men-toggle layout; regresi mode-switch Status (assessment.spec 8.3/8.4) tetap hijau.
result: pass
evidence: DOM eval — change #creationMode menggerakkan SELURUH toggle (sub-kartu/header/retake/jadwal/groups). assessment.spec 8.3 + 8.4 hijau (Status auto-Upcoming Pre-Post, clear Standard, idempotent).

### 6. FORM-11 — Blok Ujian Ulang hidden + input retake disabled saat Pre-Post
expected: Pre-Post → #retakeBlockCreate d-none + #AllowRetake/#MaxAttempts/#RetakeCooldownHours disabled. Standard → visible + enabled.
result: pass
evidence: DOM eval — Pre-Post: retakeBlockCreate d-none=true, 3 input disabled=true; Standard: d-none=false, enabled. e2e FORM-11 hijau.

### 7. Regresi Standard (Pitfall #2) — round-trip Pre-Post→Standard mengembalikan DOM tunggal
expected: Standard → Group B/C/D visible, sub-kartu absent, PassPercentage/Shuffle/AllowAnswerReview enabled; setelah round-trip semua kembali persis.
result: pass
evidence: DOM eval standard_back identik standard_default (groupB/C/D d-none=false, prePostSettingsCards d-none=true, field enabled). e2e "Regresi Standard" hijau.

### 8. Backward-compat regresi suite (assessment.spec Phase 308 mode-switch)
expected: rename FORM-10 tidak meregresi guard mode-switch Status.
result: pass
reason: 8.3 + 8.4 hijau (rename-critical, class/value-based). CATATAN: 8.1 + 8.2 GAGAL pada `toBeVisible`/`selectOption` terhadap #Status (ada di step-3 display:none) saat masih di step-1 — flaw scaffold WAVE-0 PRE-EXISTING (komentar tes sendiri: "RED state expected", "full wizard navigation defer ke Wave 1/manual UAT"), BUKAN regresi 420. Kontrak produk Status (d-none toggle + auto-Upcoming/clear) terbukti utuh via DOM eval.

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — 0 product issues]

## Notes (test-harness — bukan product gap)

- **Fixed (committed):** 2 spec Task-4 punya bug harness (bukan produk), diperbaiki agar live-green:
  - `form-persistence-420.spec.ts`: route Edit `/Admin/EditAssessment/{id}` (path) → 404 karena controller attribute-routed `[Route("Admin/[action]")]` tanpa segmen `{id}`. Fix → `?id={id}`.
  - `form-prepost-ux-420.spec.ts`: assertion `toBeVisible` atas sub-kartu/input yang berada di step-3 (collapsed saat step-1) + selektor `.card` terlalu lebar (strict-mode 3 match). Fix → scope `#prePostSettingsCards` + `toHaveCount(1)` (kontrak nyata = toggle kelas d-none, bukan visibilitas step-3).
  - Hasil re-run: **7/7 e2e hijau** (form-persistence FORM-01 + form-prepost FORM-07..11 + regresi Standard) + assessment 8.3/8.4 hijau.
- **Pre-existing (out of scope 420):** assessment.spec 8.1/8.2 (WAVE-0 scaffold RED-by-design, interaksi #Status step-3 dari step-1) + SEED_JOURNAL.md 2 entri `active` menggantung di HEAD (bukan dari sesi ini).
- **Security:** 420-SECURITY.md belum ada; security_enforcement aktif → `/gsd-secure-phase 420` disarankan sebelum advance (redesign menyentuh POST binding/disabled-anti-POST/antiforgery — perlu verifikasi).
