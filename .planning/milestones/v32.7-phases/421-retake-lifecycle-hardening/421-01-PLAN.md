---
phase: 421-retake-lifecycle-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/RetakeRules.cs
  - Services/RetakeService.cs
  - Controllers/CMPController.cs
  - Views/CMP/Results.cshtml
  - HcPortal.Tests/RetakeRulesTests.cs
  - HcPortal.Tests/RetakeServiceTests.cs
autonomous: true
requirements: [RTH-01, RTH-02]
must_haves:
  truths:
    - "Ujian ulang ditolak bila ExamWindowCloseDate sudah lewat — sesi live TIDAK dihapus (tidak ada dead-end destruktif)"
    - "Tombol 'Ujian Ulang' otomatis tidak render saat window tutup (eligibility-driven)"
    - "Saat window tutup, tombol countdown cooldown tidak muncul + tier feedback membuka full review (retake mustahil)"
    - "Reset HC menol-kan NomorSertifikat sehingga sesi non-lulus tidak menyandang nomor sertifikat menggantung"
  artifacts:
    - path: "Helpers/RetakeRules.cs"
      provides: "CanRetake dengan param examWindowCloseDate (window gate eligibility)"
      contains: "examWindowCloseDate"
    - path: "Services/RetakeService.cs"
      provides: "Abort-before-destroy window gate + SetProperty NomorSertifikat=null"
      contains: "NomorSertifikat"
    - path: "HcPortal.Tests/RetakeRulesTests.cs"
      provides: "Boundary test +7h WIB window-open/closed/null"
    - path: "HcPortal.Tests/RetakeServiceTests.cs"
      provides: "abort-before-destroy + cert-null integration tests"
  key_links:
    - from: "Services/RetakeService.cs (CanRetakeAsync:244)"
      to: "RetakeRules.CanRetake"
      via: "pass s.ExamWindowCloseDate as examWindowCloseDate"
      pattern: "CanRetake\\([^)]*ExamWindowCloseDate"
    - from: "Services/RetakeService.cs (ExecuteAsync)"
      to: "abort BEFORE ExecuteUpdateAsync/RemoveRange"
      via: "window check returns RetakeResult(false, ...)"
      pattern: "ExamWindowCloseDate.*AddHours\\(7\\)"
---

<objective>
Tutup RTH-01 (window gate dua-lapis, HIGH) dan RTH-02 (nol-kan NomorSertifikat saat reset). Ini keystone fase: perubahan signature `RetakeRules.CanRetake` + abort-before-destroy di `RetakeService.ExecuteAsync` + cert-null satu-baris. Window gate eligibility otomatis menyembunyikan tombol "Ujian Ulang" dan menyelaraskan tier feedback; abort eksekusi mencegah race destruktif.

Purpose: Worker yang gagal ujian saat window sudah tutup TIDAK boleh masuk jalur destruktif (retake menghapus sesi live lalu StartExam memblokir → shell kosong non-completable). HC reset sesi lulus tidak boleh meninggalkan nomor sertifikat menggantung yang mengisi unique index + meng-inflasi proxy `certCount`.
Output: `RetakeRules.CanRetake` ber-param window; `RetakeService.ExecuteAsync` abort sebelum destruksi + cert-null; `Results.cshtml` window-aware (cooldown button + closed-window copy); test boundary +7h WIB + abort-before-destroy + cert-null.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/421-retake-lifecycle-hardening/421-CONTEXT.md
@.planning/phases/421-retake-lifecycle-hardening/421-RESEARCH.md
@.planning/phases/421-retake-lifecycle-hardening/421-PATTERNS.md
@.planning/phases/421-retake-lifecycle-hardening/421-UI-SPEC.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diambil dari source aktual (VERIFIED 2026-06-23). -->

CanRetake signature SAAT INI — Helpers/RetakeRules.cs:29-52 (ubah: +param examWindowCloseDate):
```csharp
public static bool CanRetake(
    bool allowRetake, string? assessmentType, bool isManualEntry,
    string status, bool? isPassed, int attemptsUsed, int maxAttempts,
    int retakeCooldownHours, DateTime? completedAt, DateTime nowUtc)
{
    if (!allowRetake) return false;
    if (assessmentType == "PreTest") return false;
    if (isManualEntry) return false;
    if (status != "Completed") return false;
    if (isPassed != false) return false;
    if (attemptsUsed >= maxAttempts) return false;
    // ★ D-01: window gate DI SINI (SEBELUM cooldown — window = hard-close, fundamental) ★
    if (retakeCooldownHours <= 0) return true;
    if (completedAt == null) return false;
    return nowUtc >= completedAt.Value.AddHours(retakeCooldownHours);
}
```

Konvensi +7h WIB — HARUS byte-identik dengan CMPController.cs:956 (VERIFIED):
```csharp
DateTime.UtcNow.AddHours(7) > assessment.ExamWindowCloseDate.Value
// Di RetakeRules (pure, nowUtc injected): nowUtc.AddHours(7) > examWindowCloseDate.Value
```

Satu-satunya pemanggil produksi CanRetake — Services/RetakeService.cs:244 (VERIFIED). Suplai s.ExamWindowCloseDate.

Idiom abort RetakeService.ExecuteAsync — RetakeService.cs:75/79 (VERIFIED):
```csharp
return new RetakeResult(false, "Sesi tidak ditemukan.");  // RetakeResult record di :17
```

Blok ExecuteUpdateAsync claim — RetakeService.cs:103-112 (VERIFIED, 9 SetProperty, tambah 1):
```csharp
.ExecuteUpdateAsync(s => s
    .SetProperty(r => r.Status, "Open")
    .SetProperty(r => r.Score, (int?)null)
    .SetProperty(r => r.IsPassed, (bool?)null)
    .SetProperty(r => r.Progress, 0)
    .SetProperty(r => r.StartedAt, (DateTime?)null)
    .SetProperty(r => r.CompletedAt, (DateTime?)null)
    .SetProperty(r => r.ElapsedSeconds, (int)0)
    .SetProperty(r => r.LastActivePage, (int?)null)
    // ★ D-03: + .SetProperty(r => r.NomorSertifikat, (string?)null) ★
    .SetProperty(r => r.UpdatedAt, DateTime.UtcNow));
```

Field model (VERIFIED, migration=FALSE — sudah ada): AssessmentSession.ExamWindowCloseDate (DateTime?, :77), AssessmentSession.NomorSertifikat (string?, :91).

Test pure helper — RetakeRulesTests.cs `Can(...)` (lines 21-35), fixed clock `Now = 2026-06-19 12:00 UTC`.
Test integration — RetakeServiceTests.cs `RetakeServiceFixture` (disposable HcPortalDB_Test_{guid} @ localhost\SQLEXPRESS, MigrateAsync full chain), `NoOpHubContext` (hand-stub, NO Moq), `[Trait("Category","Integration")]`, seed helpers `SeedSessionAsync`/`SeedPackageWithResponsesAsync`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Window gate eligibility di RetakeRules.CanRetake + boundary test (RTH-01)</name>
  <read_first>
    - Helpers/RetakeRules.cs (full, 85 lines — CanRetake :29-52, ShouldHideRetakeToggle :59-60, ResolveReviewMode :71-77)
    - Helpers/ShuffleToggleRules.cs (full — pola kill-drift sibling)
    - Controllers/CMPController.cs:955-960 (+7h WIB expression untuk di-mirror byte-identik)
    - HcPortal.Tests/RetakeRulesTests.cs (full — `Can(...)` helper :21-35, fixed clock `Now` :17)
  </read_first>
  <behavior>
    - Test 1: window terbuka (examWindowCloseDate di masa depan jauh) + semua-eligible → CanRetake == true (backward-compat).
    - Test 2: window tutup (now+7h > examWindowCloseDate) → CanRetake == false.
    - Test 3: examWindowCloseDate == null → tidak ada gate (return mengikuti guard lain; backward-compat sesi tanpa window).
    - Test 4 (boundary): examWindowCloseDate == Now (fixed clock UTC) → now+7h > Now → false (gate aktif, sisi +7h).
  </behavior>
  <action>
    Tambah parameter `DateTime? examWindowCloseDate` ke signature `RetakeRules.CanRetake`. Sisipkan SEBELUM cek cooldown (`if (retakeCooldownHours <= 0)`), SETELAH `if (attemptsUsed >= maxAttempts) return false;`:
    ```csharp
    if (examWindowCloseDate.HasValue && nowUtc.AddHours(7) > examWindowCloseDate.Value) return false;
    ```
    WAJIB pakai ekspresi `nowUtc.AddHours(7) > examWindowCloseDate.Value` PERSIS (byte-identik dengan StartExam CMPController:956 yang memakai `DateTime.UtcNow.AddHours(7)`). JANGAN pakai `DateTime.Now`, `TimeZoneInfo`, `+8h`, atau UTC-only.
    Posisi param baru: tambahkan setelah `int retakeCooldownHours` dan sebelum `DateTime? completedAt` ATAU sebagai param terakhir bertanda default `= null` — pilih yang membuat call-site test minimal berubah; karena `completedAt`/`nowUtc` adalah dua param terakhir non-default, tambahkan `examWindowCloseDate` sebagai param terakhir `DateTime? examWindowCloseDate` (TANPA default, agar call-site produksi tunggal di RetakeService wajib suplai fakta — cegah lupa-wire). Sesuaikan urutan argumen di test helper `Can(...)`.
    Di RetakeRulesTests.cs: tambah param `DateTime? examWindowCloseDate = null` ke helper `Can(...)`, teruskan ke `CanRetake(...)`. Tambah 4 `[Fact]` sesuai behavior di atas memakai fixed clock `Now`.
    JANGAN ubah `ShouldHideRetakeToggle` / `ResolveReviewMode` di task ini (window-awareness tier ditangani Task 3).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeRulesTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "examWindowCloseDate" Helpers/RetakeRules.cs` >= 1
    - `grep "nowUtc.AddHours(7)" Helpers/RetakeRules.cs` cocok (window gate hadir, +7h verbatim)
    - RetakeRulesTests.cs berisi minimal 4 test baru window (open/closed/null/boundary) — `grep -c "examWindowCloseDate\|WindowClosed\|WindowOpen" HcPortal.Tests/RetakeRulesTests.cs` >= 4
    - `dotnet test --filter "FullyQualifiedName~RetakeRulesTests"` exit 0
  </acceptance_criteria>
  <done>CanRetake menolak retake saat window tutup (now+7h > EWCD) memakai konvensi +7h verbatim; EWCD null = no gate; 4 test boundary hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CanRetake caller + abort-before-destroy + cert-null di RetakeService (RTH-01 eksekusi, RTH-02)</name>
  <read_first>
    - Services/RetakeService.cs (full, ~250 lines — ExecuteAsync :69-223 termasuk guard :75/79, BeginTransactionAsync :95, claim ExecuteUpdateAsync :103-112; CanRetakeAsync :232-248 caller :244)
    - HcPortal.Tests/RetakeServiceTests.cs (full — RetakeServiceFixture :34-67, NoOpHubContext :70-100, NewService helper, SeedSessionAsync :122-140, SeedPackageWithResponsesAsync :143-174, [Trait Integration] :102)
    - 421-PATTERNS.md §"Services/RetakeService.cs" (abort idiom + D-03 SetProperty)
  </read_first>
  <behavior>
    - Test 1 (RTH-01 abort-before-destroy): seed sesi Completed + responses + assignment + ExamWindowCloseDate di masa lalu (window tutup) → ExecuteAsync return RetakeResult dengan Success=false → assert responses & assignment & ExtraTime MASIH ADA + Status sesi TIDAK berubah (bukan shell kosong).
    - Test 2 (RTH-02 cert-null): seed sesi lulus (IsPassed=true) + NomorSertifikat="KPB/xxx" + window terbuka → ExecuteAsync → assert sesi.NomorSertifikat == null pasca-reset.
    - Test 3 (regresi backward-compat): seed sesi non-lulus, window terbuka (atau EWCD null) → ExecuteAsync sukses → responses ter-RemoveRange, Status=Open (perilaku existing tak berubah).
  </behavior>
  <action>
    A) Wire caller (RTH-01 eligibility): di `CanRetakeAsync` (:244), tambahkan argumen `s.ExamWindowCloseDate` ke pemanggilan `RetakeRules.CanRetake(...)` pada posisi param yang ditambahkan Task 1 (terakhir). Pastikan `s` (AssessmentSession) ter-load dengan kolom ExamWindowCloseDate (sudah di entity, no Include khusus).
    B) Abort-before-destroy (RTH-01 eksekusi, defense-in-depth): di `ExecuteAsync`, SETELAH guard existing `Cancelled`/`Open` (sekitar :78-90) dan SEBELUM `BeginTransactionAsync` (:95) dan SEBELUM claim `ExecuteUpdateAsync` (:103), tambahkan:
    ```csharp
    if (assessment.ExamWindowCloseDate.HasValue && DateTime.UtcNow.AddHours(7) > assessment.ExamWindowCloseDate.Value)
        return new RetakeResult(false, "Masa ujian sudah ditutup — ujian ulang tidak bisa dijalankan.");
    ```
    Gunakan variabel sesi yang sudah ter-load di awal ExecuteAsync (nama variabel sesuai source — cek apakah `assessment`/`session`/`s`; sesuaikan). Ekspresi window +7h verbatim sama dengan Task 1. Idiom return = `new RetakeResult(false, "...")` (mirror :75/:79). Abort HARUS sebelum RemoveRange agar sesi live utuh.
    C) Cert-null (RTH-02): tambahkan SATU baris ke blok claim `ExecuteUpdateAsync` (:103-112), sebelum `.SetProperty(r => r.UpdatedAt, ...)`:
    ```csharp
    .SetProperty(r => r.NomorSertifikat, (string?)null)
    ```
    D) Test: di RetakeServiceTests.cs tambah param `DateTime? examWindowCloseDate = null` dan `string? nomorSertifikat = null` ke `SeedSessionAsync` (set ke sesi yang di-seed). Tulis 3 test integration sesuai behavior, `[Trait("Category","Integration")]`, pakai `NewService(ctx)` + fixture existing. Assert state DB pasca-ExecuteAsync via re-query.
    CATATAN: `ResetAssessment` controller (:4286/:4338) mendelegasi penuh ke ExecuteAsync — cert-null + window-abort otomatis berlaku untuk jalur HC. JANGAN tambah cert-null/abort terpisah di controller (Don't Hand-Roll D-03).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeServiceTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ExamWindowCloseDate" Services/RetakeService.cs` cocok di CanRetakeAsync (caller wire) DAN ExecuteAsync (abort) — minimal 2 hit
    - `grep "new RetakeResult(false, \"Masa ujian sudah ditutup" Services/RetakeService.cs` cocok (abort message)
    - `grep "SetProperty(r => r.NomorSertifikat" Services/RetakeService.cs` cocok (cert-null)
    - Abort muncul SEBELUM `BeginTransactionAsync` / `ExecuteUpdateAsync` (cek urutan baris secara visual saat edit)
    - RetakeServiceTests.cs berisi test abort-before-destroy + cert-null — `grep -c "ExamWindowCloseDate\|NomorSertifikat" HcPortal.Tests/RetakeServiceTests.cs` >= 2
    - `dotnet test --filter "FullyQualifiedName~RetakeServiceTests"` exit 0 (butuh SQLEXPRESS live)
  </acceptance_criteria>
  <done>ExecuteAsync abort + return RetakeResult(false) saat window tutup TANPA menyentuh responses/assignment (sesi live utuh); cert-null pada claim; CanRetakeAsync suplai EWCD ke RetakeRules; 3 test integration hijau; jalur HC ResetAssessment tercakup via delegasi.</done>
</task>

<task type="auto">
  <name>Task 3: Window-aware tier feedback + cooldown button + closed-window copy di Results (RTH-01 UX, Pitfall 1 & 2)</name>
  <read_first>
    - Controllers/CMPController.cs:2460-2555 (Results VM: attemptsRemaining :2480-2482, IsInCooldown :2493-2494, RetakeExam POST + ownership Forbid :2534)
    - Views/CMP/Results.cshtml:485-553 (cabang IsCapReached / CanRetake :497 / IsInCooldown :507 + confirm modal :534-553)
    - Helpers/RetakeRules.cs (ResolveReviewMode :71-77 — tier feedback leak-safe)
    - 421-RESEARCH.md §Common Pitfalls (Pitfall 1 countdown, Pitfall 2 tier; Open Question 1 rekomendasi: gate KEDUA-nya)
    - 421-UI-SPEC.md §"RTH-01 / D-01" + Copywriting "closed-window state"
  </read_first>
  <action>
    Tujuan: saat window tutup, JANGAN ada dead-end UX baru. Terapkan rekomendasi RESEARCH Open Question 1 (gate countdown + tier pada window).
    A) Hitung flag window-tutup di CMPController Results action (sekitar :2460-2495), reuse ekspresi +7h verbatim:
    ```csharp
    bool windowClosed = assessment.ExamWindowCloseDate.HasValue && DateTime.UtcNow.AddHours(7) > assessment.ExamWindowCloseDate.Value;
    ```
    (cek nama variabel assessment/session di scope action; sesuaikan.)
    B) `IsInCooldown` (:2493-2494): AND-kan dengan `!windowClosed` — saat window tutup, cooldown button countdown TIDAK muncul (Pitfall 1). Mis. ubah perhitungan agar `IsInCooldown = ... && !windowClosed`.
    C) `attemptsRemaining` (:2480-2482) yang men-drive `ResolveReviewMode`: saat `windowClosed`, set `attemptsRemaining = false` (atau setara) sehingga tier feedback membuka FULL review (retake sudah mustahil — tak ada gunanya menahan kunci jawaban; Pitfall 2). PERTAHANKAN perilaku leak-safe A1 untuk kasus non-window (attempt-sisa + cooldown transient tetap WrongFlagsOnly seperti existing). Hanya window-tutup yang men-trigger full review.
    D) Closed-window copy (Results.cshtml): tambahkan cabang informasional bila pekerja membuka Hasil saat `windowClosed` & tak ada jalur retake (CanRetake=false, bukan cap-reached). Render `alert alert-secondary` (BUKAN tombol disabled baru) dengan copy UI-SPEC PERSIS: "Masa ujian sudah ditutup. Ujian ulang tidak tersedia untuk assessment ini. Hubungi HC bila perlu kesempatan tambahan." Pakai flag window-tutup yang diteruskan ke VM/ViewBag. Letakkan selaras cabang IsCapReached/cooldown existing (:489-517). Semua teks Bahasa Indonesia.
    JANGAN buat komponen/CSS/JS baru — reuse alert Bootstrap existing (UI-SPEC Consistency Rule 1). Tombol "Ujian Ulang" hilang otomatis karena CanRetake=false (Task 1+2) — JANGAN tulis hide manual.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "windowClosed\|!windowClosed" Controllers/CMPController.cs` cocok (flag dihitung + dipakai di IsInCooldown/attemptsRemaining)
    - `grep "AddHours(7)" Controllers/CMPController.cs` tetap memakai +7h (tidak ada DateTime.Now baru)
    - `grep "Masa ujian sudah ditutup. Ujian ulang tidak tersedia" Views/CMP/Results.cshtml` cocok (closed-window copy)
    - `grep "alert alert-secondary" Views/CMP/Results.cshtml` cocok (pakai alert, bukan tombol disabled baru)
    - `dotnet build` exit 0 (kompilasi VM/view bersih)
  </acceptance_criteria>
  <done>Saat window tutup: tombol countdown cooldown tak muncul, tier feedback membuka full review, dan Hasil menampilkan alert-secondary "Masa ujian sudah ditutup..."; perilaku leak-safe non-window tak berubah; build 0-err.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (CMP) → server | Worker meminta Results/RetakeExam; input untrusted; ownership + eligibility harus server-authoritative |
| HC (Admin) → server | HC men-trigger ResetAssessment (delegasi ke RetakeService); RBAC + antiforgery existing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-421-01 | Denial of Service (self-inflicted) | RetakeService.ExecuteAsync — dead-end destruktif (retake hapus sesi live lalu StartExam blok window) | mitigate | D-01 dua-lapis: eligibility gate di RetakeRules.CanRetake (button hide) + abort-before-RemoveRange di ExecuteAsync (sesi live utuh). Test abort-before-destroy membuktikan responses/assignment tak terhapus. |
| T-421-02 | Tampering | CMP RetakeExam POST — client bypass window via JS (countdown/disable hanya UI) | mitigate | Server-authoritative re-check `CanRetakeAsync` (yang kini window-aware via Task 2A) di RetakeExam POST + abort defense-in-depth di ExecuteAsync. Countdown/hide adalah lapis UX, BUKAN gate. PRESERVE — re-check existing dipertahankan. |
| T-421-03 | Elevation of Privilege (IDOR) | CMP RetakeExam — worker retake sesi orang lain | accept (existing mitigation PRESERVED) | `assessment.UserId != user.Id → Forbid()` existing (CMPController:2534). Plan ini TIDAK mengubah ownership check — jangan regresi. |
| T-421-04 | Tampering (integritas data) | RetakeService claim — NomorSertifikat menggantung di sesi non-lulus (inflasi cert proxy + unique index) | mitigate | D-03 `SetProperty(NomorSertifikat, null)` di claim ExecuteUpdateAsync. Download PDF sudah di-guard `IsPassed!=true→NotFound` (bukan cert-leak; ini integritas index). Data lama pre-fix = DEFER (lihat catatan, di luar 5 REQ). |
| T-421-05 | Information Disclosure | Results tier feedback — kunci jawaban ditahan padahal retake mustahil (window tutup) | accept-by-design / mitigate | Task 3: saat window tutup, `attemptsRemaining=false` → full review (retake mustahil = perlakukan exhausted). Untuk non-window (cooldown transient) tetap WrongFlagsOnly leak-safe A1 (PRESERVE perilaku v32.4). |

**Catatan keamanan:** RBAC `[Authorize(Roles="Admin, HC")]` pada ResetAssessment + antiforgery POST = milik Plan 03 (ResetAssessment view) & existing — TIDAK diubah di Plan 01. Worker ownership Forbid CMPController:2534 = PRESERVE.
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~RetakeRulesTests"` exit 0 (window boundary +7h)
- `dotnet test --filter "FullyQualifiedName~RetakeServiceTests"` exit 0 (abort-before-destroy + cert-null; butuh SQLEXPRESS)
- `dotnet build` exit 0 (Results VM/view window-aware kompilasi)
- Grep guard: `nowUtc.AddHours(7)` di RetakeRules, `DateTime.UtcNow.AddHours(7)` di RetakeService/CMPController — tidak ada `DateTime.Now`/`+8`/`TimeZoneInfo` baru.
- Abort di ExecuteAsync muncul SEBELUM ExecuteUpdateAsync/RemoveRange (urutan baris).
</verification>

<success_criteria>
- RTH-01: CanRetake=false saat window tutup (eligibility) + ExecuteAsync abort tanpa destruksi (eksekusi); tombol Ujian Ulang auto-hide; cooldown/ tier window-aware.
- RTH-02: NomorSertifikat null pasca reset (HC via delegasi + worker retake).
- Backward-compat: jalur retake existing (non-window) tak berubah; Standard tetap retakeable; leak-safe A1 dipertahankan.
- Semua test Plan 01 hijau, build 0-err.
</success_criteria>

<output>
After completion, create `.planning/phases/421-retake-lifecycle-hardening/421-01-SUMMARY.md`
</output>
