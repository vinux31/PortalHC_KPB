---
phase: 421-retake-lifecycle-hardening
plan: 02
type: execute
wave: 2
depends_on: ["421-01"]
files_modified:
  - Helpers/RetakeCountingRules.cs
  - Services/RetakeService.cs
  - Controllers/CMPController.cs
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/RetakeCountingRulesTests.cs
autonomous: true
requirements: [RTH-03]
must_haves:
  truths:
    - "Penghitungan jumlah percobaan konsisten antara batas (cap) dan peringatan di ManagePackages — satu sumber counting"
    - "Predikat snapshot-presence identik dipakai keempat situs hitung (3 cap + 1 warning)"
    - "Arsip legacy HC-reset pre-v32.4 (tanpa snapshot child) tidak ikut dihitung di SEMUA situs"
  artifacts:
    - path: "Helpers/RetakeCountingRules.cs"
      provides: "Helper CountEraRetakeArchives — snapshot-presence base query (per-user count + max-in-group)"
      contains: "AttemptHistoryId"
    - path: "HcPortal.Tests/RetakeCountingRulesTests.cs"
      provides: "Parity test cap==warning predikat + legacy tak dihitung"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (warning :5798)"
      to: "Helpers/RetakeCountingRules.CountEraRetakeArchives"
      via: "max-in-group form, snapshot-presence filter ditambahkan"
      pattern: "CountEraRetakeArchives|AttemptHistoryId == h.Id"
    - from: "Services/RetakeService.cs (CanRetakeAsync + ExecuteAsync count)"
      to: "Helpers/RetakeCountingRules.CountEraRetakeArchives"
      via: "per-user count form"
      pattern: "CountEraRetakeArchives|RetakeCountingRules"
---

<objective>
Tutup RTH-03 (RTK-LOGIC-03): satukan penghitungan percobaan ke satu sumber kebenaran. Ekstrak helper `CountEraRetakeArchives` (D-05) yang mengkapsulasi predikat snapshot-presence, lalu wire keempat situs hitung — 3 situs cap yang SUDAH identik (`RetakeService:145-150`, `RetakeService:237-242`, `CMPController:2472-2475`) + 1 situs warning ManagePackages yang DIVERGEN (`AssessmentAdminController:5795-5798`, tanpa snapshot filter).

Purpose: Situs warning ManagePackages saat ini menghitung TANPA filter snapshot-presence → meng-over-count percobaan (termasuk arsip legacy HC-reset pre-v32.4) sehingga peringatan MaxAttempts<terpakai bisa salah trigger. Menyamakan predikat = kill-drift permanen (pola RetakeRules/ShuffleToggleRules).
Output: helper baru `Helpers/RetakeCountingRules.cs`; 3 situs cap re-wire ke helper (perilaku identik); situs warning ditambah filter snapshot-presence via helper (perbaikan); parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/421-retake-lifecycle-hardening/421-CONTEXT.md
@.planning/phases/421-retake-lifecycle-hardening/421-RESEARCH.md
@.planning/phases/421-retake-lifecycle-hardening/421-PATTERNS.md

<interfaces>
<!-- Diambil dari source aktual (VERIFIED 2026-06-23). -->

Predikat snapshot-presence IDENTIK di 3 situs cap (RetakeService.cs:145-150 & :237-242, CMPController.cs:2472-2475):
```csharp
int eraRetakeArchives = await _context.AssessmentAttemptHistory
    .Where(h => h.UserId == s.UserId && h.Title == s.Title && h.Category == s.Category
             && _context.AssessmentAttemptResponseArchives.Any(a => a.AttemptHistoryId == h.Id))
    .CountAsync();
// attemptsUsed = eraRetakeArchives + 1   (PERTAHANKAN +1)
```

Situs ke-4 DIVERGEN (AssessmentAdminController.cs:5795-5798) — TIDAK ada snapshot filter:
```csharp
int retakeMaxArchivedForGroup = await _context.AssessmentAttemptHistory
    .Where(h => h.Title == assessment.Title && h.Category == assessment.Category)  // ❌ no snapshot-presence, no UserId-scope
    .GroupBy(h => h.UserId).Select(g => g.Count()).OrderByDescending(c => c).FirstOrDefaultAsync();
ViewBag.RetakeMaxAttemptsUsedInGroup = retakeMaxArchivedForGroup + 1;
```

SEMANTIK KRITIS (RESEARCH Pitfall 3 — JANGAN collapse ke satu bentuk):
- Situs cap (a/b/c) = per-user count untuk (UserId, Title, Category). Bentuk: `.Where(UserId==x).CountAsync()`.
- Situs warning (d) = MAX di antara SEMUA user di grup (Title, Category). Bentuk: `.GroupBy(UserId).Select(g=>g.Count()).Max()` (atau OrderByDesc.First). GroupBy BENAR — yang hilang HANYA filter snapshot-presence.
- PERTAHANKAN `+1` (current attempt = arsip + 1) di KEEMPAT situs.

Pola helper static — Helpers/ShuffleToggleRules.cs (sibling kill-drift). Helper INI DB-aware (sentuh _context) — terima IQueryable<AssessmentAttemptHistory> / DbSet + (title, category), expose base query snapshot-presence; caller pilih per-user vs max-in-group.

Model relasi (VERIFIED): AssessmentAttemptResponseArchive.AttemptHistoryId → AssessmentAttemptHistory.Id. AssessmentAttemptHistory punya UserId, Title, Category, Id.

Test fixture integration — RetakeServiceTests.cs `RetakeServiceFixture` (disposable SQL), seed helpers `SeedEraRetakeArchiveAsync` (:177-187, arsip ber-snapshot), `SeedLegacyArchiveAsync` (:190-194, arsip TANPA snapshot child).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ekstrak helper CountEraRetakeArchives + parity test (RTH-03)</name>
  <read_first>
    - Helpers/ShuffleToggleRules.cs (full — pola helper static sibling)
    - Services/RetakeService.cs:140-152 (situs cap dalam ExecuteAsync) + :232-248 (CanRetakeAsync predikat per-user)
    - Controllers/CMPController.cs:2470-2480 (situs cap Results VM)
    - Controllers/AssessmentAdminController.cs:5789-5800 (situs warning DIVERGEN)
    - HcPortal.Tests/RetakeServiceTests.cs:170-195 (SeedEraRetakeArchiveAsync, SeedLegacyArchiveAsync, fixture)
    - 421-PATTERNS.md §"CountEraRetakeArchives helper" (semantik split + diskresi nama/posisi)
  </read_first>
  <behavior>
    - Test 1 (per-user cap): seed 2 arsip ber-snapshot untuk (UserA, Title, Cat) + 0 legacy → helper per-user count == 2.
    - Test 2 (legacy excluded): seed 1 arsip ber-snapshot + 2 legacy (tanpa snapshot child) untuk UserA → per-user count == 1 (legacy TIDAK dihitung).
    - Test 3 (max-in-group warning): seed UserA=3 snapshot-arsip, UserB=1 snapshot-arsip (Title,Cat sama) → max-in-group == 3 (bukan 4, bukan per-user).
    - Test 4 (parity): untuk satu user, nilai per-user helper == komponen yang dihitung situs cap existing (predikat identik) → cap dan warning memakai filter snapshot-presence yang SAMA.
  </behavior>
  <action>
    Buat `Helpers/RetakeCountingRules.cs` (nama file & posisi = diskresi Claude per D-05; nama kelas saran `RetakeCountingRules`, method `CountEraRetakeArchives`). Karena DB-aware, desain agar testable via real-SQL: terima `IQueryable<AssessmentAttemptHistory> historyQuery` + `IQueryable<AssessmentAttemptResponseArchive> archiveQuery` (atau `ApplicationDbContext`) + `(string title, string category)`, dan expose base query snapshot-presence:
    ```csharp
    // base predicate (snapshot-presence + Title/Category) — IDENTIK di 4 situs:
    historyQuery.Where(h => h.Title == title && h.Category == category
        && archiveQuery.Any(a => a.AttemptHistoryId == h.Id))
    ```
    Sediakan dua bentuk konsumsi (JANGAN collapse — Pitfall 3):
    - `CountForUserAsync(... , string userId)` → base + `.Where(h => h.UserId == userId)` + `.CountAsync()` (untuk cap a/b/c).
    - `MaxInGroupAsync(...)` → base + `.GroupBy(h => h.UserId).Select(g => g.Count())` + ambil maksimum (`.OrderByDescending(c => c).FirstOrDefaultAsync()` atau `.MaxAsync()` dengan guard empty→0) (untuk warning d).
    JANGAN sertakan `+1` di dalam helper — caller tetap menambah `+1` (current attempt) seperti existing, agar semantik "arsip vs percobaan" eksplisit di call-site. Mirror `.Any(a => a.AttemptHistoryId == h.Id)` PERSIS dari predikat existing.
    Buat `HcPortal.Tests/RetakeCountingRulesTests.cs` (file baru, `[Trait("Category","Integration")]`, reuse `RetakeServiceFixture` via IClassFixture, seed `SeedEraRetakeArchiveAsync`/`SeedLegacyArchiveAsync`). Tulis 4 test sesuai behavior. Gunakan disposable DB pola existing.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeCountingRules" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - File `Helpers/RetakeCountingRules.cs` ada — `ls Helpers/RetakeCountingRules.cs` sukses
    - `grep "AttemptHistoryId == h.Id" Helpers/RetakeCountingRules.cs` cocok (predikat snapshot-presence verbatim)
    - Helper expose dua bentuk: `grep -c "CountForUser\|MaxInGroup" Helpers/RetakeCountingRules.cs` >= 2
    - File `HcPortal.Tests/RetakeCountingRulesTests.cs` ada dengan >=4 test — `grep -c "Fact\|Theory" HcPortal.Tests/RetakeCountingRulesTests.cs` >= 4
    - `dotnet test --filter "FullyQualifiedName~RetakeCountingRules"` exit 0 (butuh SQLEXPRESS)
  </acceptance_criteria>
  <done>Helper CountEraRetakeArchives ada dengan dua bentuk (per-user cap, max-in-group warning), predikat snapshot-presence verbatim; legacy excluded; 4 parity test hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Wire keempat situs hitung ke helper — fix situs warning DIVERGEN (RTH-03)</name>
  <read_first>
    - Helpers/RetakeCountingRules.cs (helper baru dari Task 1)
    - Services/RetakeService.cs:140-152 & :232-248 (2 situs cap)
    - Controllers/CMPController.cs:2470-2480 (situs cap Results VM)
    - Controllers/AssessmentAdminController.cs:5789-5800 (situs warning — yang diperbaiki)
    - HcPortal.Tests/RetakeServiceTests.cs (regresi cap existing harus tetap hijau)
  </read_first>
  <action>
    Ganti predikat inline di KEEMPAT situs dengan pemanggilan helper, PERTAHANKAN `+1` di tiap situs dan PERTAHANKAN semantik (per-user untuk cap, max-in-group untuk warning):
    A) `RetakeService.cs:145-150` (cap dalam ExecuteAsync) → `CountForUserAsync(...)` untuk (s.UserId, s.Title, s.Category); `attemptsUsed = result + 1`.
    B) `RetakeService.cs:237-242` (CanRetakeAsync) → `CountForUserAsync(...)` sama; `attemptsUsed = result + 1`.
    C) `CMPController.cs:2472-2475` (Results VM cap) → `CountForUserAsync(...)`; pertahankan `+1` dan penggunaan downstream VM.
    D) `AssessmentAdminController.cs:5795-5798` (warning DIVERGEN) → `MaxInGroupAsync(...)` untuk (assessment.Title, assessment.Category); `ViewBag.RetakeMaxAttemptsUsedInGroup = result + 1`. INI PERBAIKAN: tambah filter snapshot-presence (lewat helper) sambil PERTAHANKAN `GroupBy(UserId)` max semantics. JANGAN ubah jadi per-user (akan under-count).
    Lewatkan `_context.AssessmentAttemptHistory` + `_context.AssessmentAttemptResponseArchives` (atau `_context`) ke helper sesuai signature Task 1. Pastikan semua call-site kompilasi. Situs a/b/c HARUS menghasilkan nilai numerik IDENTIK dengan sebelumnya (predikat tak berubah) — dibuktikan oleh regresi RetakeServiceTests existing tetap hijau.
    Hanya situs (d) yang berubah perilaku (kini snapshot-aware). Verifikasi via parity test Task 1 + grep tak ada lagi predikat inline `.Any(a => a.AttemptHistoryId == h.Id)` yang tersisa di keempat call-site (semua lewat helper).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeService|FullyQualifiedName~RetakeCountingRules" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CountForUser\|MaxInGroup\|RetakeCountingRules\|CountEraRetakeArchives" Services/RetakeService.cs` >= 2 (2 situs cap wired)
    - `grep "CountForUser\|RetakeCountingRules\|CountEraRetakeArchives" Controllers/CMPController.cs` cocok (situs cap Results wired)
    - `grep "MaxInGroup\|RetakeCountingRules\|CountEraRetakeArchives" Controllers/AssessmentAdminController.cs` cocok di sekitar warning count
    - Situs warning kini ber-snapshot: `grep "AttemptHistoryId" Controllers/AssessmentAdminController.cs` cocok (langsung atau via helper) ATAU helper-call menggantikan `.Where(h => h.Title ...).GroupBy` lama tanpa snapshot
    - `ViewBag.RetakeMaxAttemptsUsedInGroup` masih `+ 1` — `grep "RetakeMaxAttemptsUsedInGroup" Controllers/AssessmentAdminController.cs` cocok dan ada `+ 1`
    - `dotnet build` exit 0; `dotnet test --filter "FullyQualifiedName~RetakeService"` exit 0 (regresi cap hijau — nilai cap tak berubah)
  </acceptance_criteria>
  <done>Keempat situs hitung memakai helper; cap a/b/c menghasilkan nilai identik (regresi hijau); warning d kini snapshot-aware (perbaikan) dengan GroupBy-max + `+1` dipertahankan; tak ada predikat inline drift tersisa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC (Admin) → server | HC membuka ManagePackages (GET) → warning count dihitung server-side dari DB |
| worker (CMP) → server | Results VM cap dihitung server-side; cap menentukan eligibility retake |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-421-06 | Tampering (integritas data) | Counting drift — situs warning over-count (legacy + cross-snapshot) menyebabkan peringatan/cap tak konsisten | mitigate | D-05: ekstrak `CountEraRetakeArchives` snapshot-presence + wire 4 situs. Parity test membuktikan cap==warning predikat & legacy excluded. Kill-drift permanen. |
| T-421-07 | Information Disclosure | Helper DB-aware membaca AttemptHistory/Archive lintas user (max-in-group) | accept | Warning count = agregat angka (max attempt count di grup) yang dikonsumsi HC (RBAC Admin,HC existing pada ManagePackages/UpdateRetakeSettings) — TIDAK mengekspos jawaban/PII. Hanya angka percobaan. |
| T-421-08 | Elevation of Privilege | Akses helper count dari controller tanpa RBAC | accept (existing PRESERVED) | Helper hanya dipanggil dari action ber-`[Authorize(Roles="Admin, HC")]` (ManagePackages/UpdateRetakeSettings, VERIFIED :5652) atau Results worker (ownership existing). Plan ini TIDAK menambah endpoint baru — tak ada surface RBAC baru. |

**Catatan:** Tidak ada POST/mutasi baru di plan ini (helper read-only + re-wire count). Antiforgery N/A (GET ViewBag + read paths existing).
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~RetakeCountingRules"` exit 0 (parity + legacy-excluded)
- `dotnet test --filter "FullyQualifiedName~RetakeService"` exit 0 (regresi cap — nilai tak berubah)
- `dotnet build` exit 0 (4 call-site kompilasi)
- Grep: tak ada predikat snapshot inline tersisa di 4 call-site (semua via helper); `+1` dipertahankan tiap situs.
</verification>

<success_criteria>
- RTH-03: keempat situs hitung memakai satu helper snapshot-presence; cap (per-user) konsisten dengan warning (max-in-group) dalam hal filter; legacy pre-v32.4 tak dihitung di semua situs.
- Situs warning ManagePackages diperbaiki (kini snapshot-aware) tanpa mengubah GroupBy-max semantics + `+1`.
- Regresi cap existing hijau (nilai cap identik).
</success_criteria>

<output>
After completion, create `.planning/phases/421-retake-lifecycle-hardening/421-02-SUMMARY.md`
</output>
