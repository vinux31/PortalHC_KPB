---
phase: 422-samepackage-shuffle-integrity
plan: 02
type: execute
wave: 2
depends_on: [422-01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/SamePackageSyncTests.cs
  - HcPortal.Tests/SessionEditLockTests.cs
  - HcPortal.Tests/SamePackageInheritTests.cs
  - HcPortal.Tests/SiblingTypeAwareLockTests.cs
autonomous: true
migration: false
requirements: [SHFX-01, SHFX-03, SHFX-04, SHFX-06]
must_haves:
  truths:
    - "Import Excel ke paket Pre ber-SamePackage memicu sync otomatis ke Post (parity dengan jalur form) — SHUF-ISS-03 HIGH ditutup"
    - "Import LANGSUNG ke paket Post terkunci ditolak (IsSessionEditLocked guard di AWAL); Import ke Pre lolos lalu sync di AKHIR"
    - "5 endpoint POST (CreatePackage/DeletePackage/CreateQuestion/EditQuestion/DeleteQuestion) menolak edit pada Post terkunci SamePackage (server-side, no-write)"
    - "Peserta baru di grup Pre-Post mewarisi SamePackage = repPost.SamePackage (bukan default false)"
    - "Lock-detection sibling pakai SiblingPrePostAwarePredicate type-aware (Pre mulai -> Post TIDAK terkunci); propagation shuffle Pre<->Post TETAP cross-type (tidak berubah)"
    - "Satu helper SyncToLinkedPostIfSamePackageAsync dipakai 6 jalur (kill-drift; tidak ada 6 blok copy-paste terpisah)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "SyncToLinkedPostIfSamePackageAsync (private) + guard IsSessionEditLocked di 5 endpoint + newPost inherit + sibling key type-aware"
      contains: "SyncToLinkedPostIfSamePackageAsync"
    - path: "HcPortal.Tests/SamePackageSyncTests.cs"
      provides: "Integration: Import ke Pre+SamePackage -> Post ter-sync; no-op bila bukan PreTest/!SamePackage"
    - path: "HcPortal.Tests/SessionEditLockTests.cs"
      provides: "Integration: 5 endpoint POST reject saat locked (no-write) + lolos saat tak-locked"
    - path: "HcPortal.Tests/SiblingTypeAwareLockTests.cs"
      provides: "Integration: Pre mulai -> Post TIDAK terkunci (type-aware lock-detection)"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs ImportPackageQuestions (terminal :6486+)"
      to: "SyncToLinkedPostIfSamePackageAsync(preSessionId)"
      via: "await sync SEBELUM return RedirectToAction (success path)"
      pattern: "SyncToLinkedPostIfSamePackageAsync"
    - from: "5 endpoint POST awal"
      to: "SessionEditLockRules.IsSessionEditLocked(session)"
      via: "guard reject TempData[Error] + redirect"
      pattern: "IsSessionEditLocked"
    - from: "UpdateShuffleSettings/UpdateRetakeSettings/GET ManagePackages lock-detection"
      to: "SiblingSessionQuery.SiblingPrePostAwarePredicate"
      via: "type-aware anyStarted/anyAssignment (BUKAN propagation loop)"
      pattern: "SiblingPrePostAwarePredicate"
---

<objective>
Tutup SHFX-01 (auto-sync Import, SHUF-ISS-03 HIGH), SHFX-03 (lock server-side, SHUF-ISS-02), SHFX-04 (pewarisan SamePackage peserta baru, PA-02), SHFX-06 (sibling key type-aware lock-detection, SHUF-ISS-01) — semua di `AssessmentAdminController.cs`. Ini wave WIRING: konsolidasi 6 blok sync copy-paste jadi satu helper + tambah ke jalur Import yang bocor, guard 5 endpoint POST dengan helper lock dari Wave 1, plus dua fix mekanis (inherit + sibling key).

Purpose: SHUF-ISS-03 = HIGH — jalur Import (impor bank-soal massal, paling lazim dipakai HC) bocor: Post jalankan paket lama/kosong tanpa peringatan, komparasi Pre/Post gagal senyap. Lock view-only (SHUF-ISS-02) bisa di-bypass via POST langsung -> butuh guard server-authoritative. Peserta baru default SamePackage=false = inkonsisten grup. Sibling key type-agnostic = over-lock (Pre mulai -> Post ikut terkunci).
Output: helper `SyncToLinkedPostIfSamePackageAsync` (wrap `SyncPackagesToPost` existing) di 6 jalur + guard `IsSessionEditLocked` di 5 endpoint + newPost inherit 1-baris + sibling key type-aware di 3 lock-detection site + 4 file test integration.

⚠️ CRITICAL SCOPE (Pitfall 4 / RESEARCH A3 / Open Q1): SHFX-06 type-aware HANYA untuk LOCK-DETECTION (`anyStarted`/`anyAssignment`). JANGAN ubah propagation write shuffle di `UpdateShuffleSettings:5649-5659` ("Propagate ke SEMUA sibling grup") yang SENGAJA cross-type Pre+Post. Mengubahnya = REGRESI propagation shuffle. Acceptance criteria di Task 3 meng-assert propagation tak berubah.

⚠️ OVERLAP v32.6 (branch main, Scoped Shuffle): perubahan sibling key + sync di controller bisa bentrok saat merge. Catat tiap perubahan shuffle/sibling untuk rekonsiliasi — JANGAN tarik kode Scoped-Shuffle dari main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/422-samepackage-shuffle-integrity/422-CONTEXT.md
@.planning/phases/422-samepackage-shuffle-integrity/422-RESEARCH.md
@.planning/phases/422-samepackage-shuffle-integrity/422-PATTERNS.md
@.planning/phases/422-samepackage-shuffle-integrity/422-01-SUMMARY.md

<interfaces>
<!-- Kontrak dari Wave 1 + source aktual (VERIFIED 2026-06-23). -->

Wave 1 helper (PANGGIL dari controller):
- Helpers/SessionEditLockRules.cs -> `SessionEditLockRules.IsSessionEditLocked(AssessmentSession s)` -> bool (true bila PostTest && SamePackage)

EXISTING SyncPackagesToPost (AssessmentAdminController.cs:5875-5933) — deep-clone Pre->Post, SUDAH BENAR (jangan ubah). Helper baru hanya membungkusnya:
```csharp
private async Task SyncPackagesToPost(int preSessionId, int postSessionId) { ... } // existing
```

EXISTING sync-block template (CreateQuestion :6773-6786) — pola yang di-EKSTRAK:
```csharp
var parentSession = await _context.AssessmentSessions.FindAsync(parentPkg.AssessmentSessionId);
if (parentSession?.AssessmentType == "PreTest" && parentSession.LinkedSessionId.HasValue) {
    var linkedPost = await _context.AssessmentSessions.FindAsync(parentSession.LinkedSessionId.Value);
    if (linkedPost != null && linkedPost.SamePackage)
        await SyncPackagesToPost(parentSession.Id, linkedPost.Id);
}
```

EXISTING 6 sync call-site (5 ganti blok inline -> panggil helper; 1 BARU = Import):
- CopyPackagesFromPre:5948 (panggil SyncPackagesToPost langsung — seragamkan ke helper opsional)
- CreatePackage:5983-5991
- DeletePackage:6067-6076
- CreateQuestion:6773-6786
- EditQuestion:7036-7047
- DeleteQuestion:7129-7140
- ImportPackageQuestions terminal :6472-6486 (BOCOR — sisip sync SEBELUM return)

EXISTING 5 endpoint POST (guard di AWAL):
- CreatePackage:5958 (session via assessmentId)
- DeletePackage:5999 (session via packageId -> pkg.AssessmentSessionId)
- CreateQuestion:6625 (session via packageId -> pkg.AssessmentSessionId)
- EditQuestion(POST):6842 (session via packageId -> pkg.AssessmentSessionId)
- DeleteQuestion:7084 (session via packageId arg -> pkg.AssessmentSessionId)

EXISTING newPost init (AssessmentAdminController.cs:2024-2045) — repPost = postGroup.First() (:1999). Tambah 1 baris analog `BannerColor = repPost.BannerColor`.

EXISTING sibling key TYPE-AGNOSTIC (3 lock-detection site DIGANTI ke type-aware):
- UpdateShuffleSettings:5630-5635 (komentar :5629 SALAH "key identik StartExam/Reshuffle" — koreksi)
- UpdateRetakeSettings:5705 (.Where Title/Category/Schedule.Date)
- GET ManagePackages shuffle state :5814-5819
Predicate kanonik: Helpers/SiblingSessionQuery.cs:14-24 SiblingPrePostAwarePredicate(title, category, scheduleDate, assessmentType).

⚠️ JANGAN ubah UpdateShuffleSettings:5649-5659 propagation foreach (cross-type by design).

Fixture integration: HcPortal.Tests/ShuffleLockGuardTests.cs:22-30 (IClassFixture<ProtonCompletionFixture> + [Trait Integration] + seed sibling/AssessmentPackage + marker GUID).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ekstrak SyncToLinkedPostIfSamePackageAsync + wire 6 jalur (incl Import BOCOR, guard-ordering Pitfall 3) + SamePackageSyncTests</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:5875-5933 (SyncPackagesToPost — wrap, jangan ubah)
    - Controllers/AssessmentAdminController.cs:6773-6786 (sync-block template paling lengkap)
    - Controllers/AssessmentAdminController.cs:5983-5991, 6067-6076, 7036-7047, 7129-7140 (4 blok inline lain yang diganti)
    - Controllers/AssessmentAdminController.cs:6199-6225 (ImportPackageQuestions signature + packageId arg) + :6472-6486 (terminal — sisip sync di sini)
    - 422-PATTERNS.md §"SyncToLinkedPostIfSamePackageAsync" (helper + 6-jalur wire) + 422-RESEARCH.md Pitfall 3 (lock-guard ordering Import)
    - HcPortal.Tests/ShuffleLockGuardTests.cs (fixture analog)
  </read_first>
  <action>
    1. Tambah private helper di `AssessmentAdminController.cs` (dekat SyncPackagesToPost):
    ```csharp
    /// SHFX-01/D-06 — satu sumber sync Pre->Post (kill-drift; ganti 6 blok copy-paste).
    private async Task SyncToLinkedPostIfSamePackageAsync(int preSessionId) {
        var pre = await _context.AssessmentSessions.FindAsync(preSessionId);
        if (pre?.AssessmentType == "PreTest" && pre.LinkedSessionId.HasValue) {
            var post = await _context.AssessmentSessions.FindAsync(pre.LinkedSessionId.Value);
            if (post != null && post.SamePackage)
                await SyncPackagesToPost(pre.Id, post.Id);   // existing :5875-5933, sudah benar
        }
    }
    ```
    2. Ganti 5 blok inline (`:5983-5991`, `:6067-6076`, `:6773-6786`, `:7036-7047`, `:7129-7140`) -> `await SyncToLinkedPostIfSamePackageAsync(<preSessionId>);`. preSessionId = session-dari-package yang diedit (Pre). Untuk CreatePackage = `assessment.Id`; untuk Delete/Create/Edit Question = `parentSession.Id` (Pre). (CopyPackagesFromPre `:5948` boleh tetap panggil SyncPackagesToPost langsung atau seragamkan.)
    3. **Import wire (SHFX-01, BOCOR)** — sisip SEBELUM `return RedirectToAction` di terminal (`:6486+`, success path): resolve `pkg = await _context.AssessmentPackages.FindAsync(packageId)`, lalu `await SyncToLinkedPostIfSamePackageAsync(pkg.AssessmentSessionId);`. (Pitfall 3: sync di AKHIR untuk Pre; guard lock di AWAL = Task 2.)
    4. Buat `HcPortal.Tests/SamePackageSyncTests.cs` (`[Trait("Category","Integration")]`, IClassFixture<ProtonCompletionFixture>):
       - Test A (Import sync): seed Pre+Post linked, Post.SamePackage=true, Pre punya paket+soal -> panggil `SyncToLinkedPostIfSamePackageAsync(preId)` (atau jalankan Import path) -> assert paket Post == paket Pre (count + question count match).
       - Test B (no-op !SamePackage): Post.SamePackage=false -> sync -> Post TIDAK berubah.
       - Test C (no-op bukan PreTest): panggil dgn Post id / Standard id -> no-op.
       - Test D (no-op linkedPost null): Pre tanpa LinkedSessionId -> no-op.
       Marker GUID isolasi seed.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test --filter "FullyQualifiedName~SamePackageSyncTests" 2>&1 | tail -8</automated>
  </verify>
  <done>
    `dotnet build` 0 error; SamePackageSyncTests hijau. `grep -c "await SyncToLinkedPostIfSamePackageAsync(" Controllers/AssessmentAdminController.cs` >= 6 (Import + 5 endpoint); helper definition ada (1×). ImportPackageQuestions terminal memanggil sync sebelum return.
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard IsSessionEditLocked di awal 5 endpoint POST (server-side hard-reject) + Import lock-guard awal + SessionEditLockTests</name>
  <read_first>
    - Helpers/SessionEditLockRules.cs (Wave 1 — IsSessionEditLocked)
    - Controllers/AssessmentAdminController.cs:5958 (CreatePackage), :5999 (DeletePackage), :6625 (CreateQuestion), :6842 (EditQuestion POST), :7084 (DeleteQuestion) — resolve session per endpoint
    - Controllers/AssessmentAdminController.cs:6199 (ImportPackageQuestions POST — guard awal, lihat Pitfall 3)
    - Controllers/AssessmentAdminController.cs:5643-5647 (guard reject TempData idiom)
    - 422-PATTERNS.md §"IsSessionEditLocked + guard 5 endpoint" + 422-RESEARCH.md Pitfall 3
    - 422-UI-SPEC.md Surface 2 (reject toast copy)
    - HcPortal.Tests/ShuffleLockGuardTests.cs (endpoint no-write guard analog)
  </read_first>
  <action>
    Di AWAL tiap 5 endpoint POST (sebelum mutasi), resolve session lalu guard. Pakai helper Wave 1:
    ```csharp
    // CreatePackage: session via assessmentId. Delete*/Create*/Edit* Question + DeletePackage: packageId -> pkg.AssessmentSessionId.
    var lockSession = await _context.AssessmentSessions.FindAsync(<assessmentId atau resolved>);
    if (lockSession != null && SessionEditLockRules.IsSessionEditLocked(lockSession)) {
        TempData["Error"] = "Paket Post-Test ini terkunci (paket-sama). Edit soal harus dilakukan di sesi Pre-Test.";
        return RedirectToAction("ManagePackages", new { assessmentId = lockSession.Id });
    }
    ```
    Catatan resolusi session per endpoint:
    - CreatePackage(`int assessmentId`) -> FindAsync(assessmentId).
    - DeletePackage(`int packageId`) -> pkg = FindAsync(packageId) -> session = FindAsync(pkg.AssessmentSessionId).
    - CreateQuestion / EditQuestion(POST) / DeleteQuestion -> resolve packageId -> pkg -> session.
    **Import (Pitfall 3):** guard `IsSessionEditLocked(packageSession)` di AWAL ImportPackageQuestions juga — tolak Import LANGSUNG ke paket Post terkunci. (Import ke Pre -> session Pre -> lock false -> lolos -> sync Task 1. Import ke Post terkunci -> reject.)
    Backward-compat: guard hanya trigger PostTest && SamePackage; Pre/Standard/Post-non-same LOLOS (perilaku tak berubah).

    Buat `HcPortal.Tests/SessionEditLockTests.cs` (`[Trait("Category","Integration")]`, fixture):
    - Untuk tiap dari 5 endpoint (+ Import): seed Post terkunci (PostTest+SamePackage) -> POST edit -> assert NO-WRITE (paket/soal tak berubah) + (bila feasible test return) TempData Error / redirect.
    - Counter-test: seed Pre / Post-non-same -> POST edit -> assert WRITE sukses (tak ter-reject).
    (Ikuti pola ShuffleLockGuardTests: replikasi guard-body atau panggil endpoint via controller instance fixture.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test --filter "FullyQualifiedName~SessionEditLockTests" 2>&1 | tail -8</automated>
  </verify>
  <done>
    `dotnet build` 0 error; SessionEditLockTests hijau (5 endpoint + Import reject saat locked, lolos saat tak-locked). `grep -c "SessionEditLockRules.IsSessionEditLocked" Controllers/AssessmentAdminController.cs` >= 5; reject copy "terkunci (paket-sama)" ada.
  </done>
</task>

<task type="auto">
  <name>Task 3: newPost inherit SamePackage (SHFX-04) + sibling key type-aware lock-detection (SHFX-06, scope LOCK-ONLY) + tests</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:1999 (repPost = postGroup.First()) + :2024-2045 (newPost init — analog BannerColor=repPost.BannerColor)
    - Controllers/AssessmentAdminController.cs:5629-5635 (UpdateShuffleSettings sibling key + komentar SALAH :5629) + :5649-5659 (propagation foreach — JANGAN ubah)
    - Controllers/AssessmentAdminController.cs:5705 (UpdateRetakeSettings key) + :5814-5819 (GET ManagePackages shuffle state key)
    - Helpers/SiblingSessionQuery.cs:14-24 (SiblingPrePostAwarePredicate kanonik — reuse, jangan edit)
    - 422-RESEARCH.md Pitfall 4 + Open Q1/A3 (type-aware HANYA lock-detection, propagation cross-type tetap) + 422-PATTERNS.md §"SiblingSessionQuery REUSE"
    - HcPortal.Tests/SiblingPrePostFilterTests.cs (predicate test analog, jika ada)
  </read_first>
  <action>
    1. **SHFX-04 inherit** — di `newPost` init (`:2024-2045`), tambah 1 baris (mekanis, analog `BannerColor = repPost.BannerColor`):
    ```csharp
    SamePackage = repPost.SamePackage,
    ```
    2. **SHFX-06 sibling key type-aware (LOCK-DETECTION SAJA)** — ganti 3 site type-agnostic ke `SiblingPrePostAwarePredicate`:
       - UpdateShuffleSettings `:5630-5635`: ganti `.Where(s => s.Title==... Category==... Schedule.Date==...)` -> `.Where(SiblingSessionQuery.SiblingPrePostAwarePredicate(assessment.Title, assessment.Category, assessment.Schedule.Date, assessment.AssessmentType))`. Koreksi komentar `:5629` ("key identik StartExam/Reshuffle" -> sekarang BENAR type-aware).
       - UpdateRetakeSettings `:5705`: idem (gunakan assessment.AssessmentType).
       - GET ManagePackages shuffle-state lock-detection `:5814-5819`: idem.
       ⚠️ **JANGAN ubah** propagation foreach `UpdateShuffleSettings:5649-5659` — biarkan tetap mem-propagate shuffle ke siblingSessionIds. Setelah ganti predicate, siblingSessionIds kini hanya same-type; itu mempengaruhi BAIK lock-detection (benar) MAUPUN propagation scope. Karena propagation harus tetap cross-type (Pre<->Post share shuffle by design), PISAHKAN: hitung `lockSiblingIds` (type-aware, untuk anyStarted/anyAssignment) TERPISAH dari `propagationSiblingIds` (type-agnostic existing key, untuk foreach write). Jangan gabung satu list.
    3. Buat `HcPortal.Tests/SiblingTypeAwareLockTests.cs` (`[Trait("Category","Integration")]` atau unit predicate compile bila feasible):
       - Test: seed Pre+Post linked (same Title/Category/Date). Pre.StartedAt set (peserta Pre mulai). Lock-detection untuk POST (assessmentType="PostTest") via SiblingPrePostAwarePredicate -> anyStarted Post == FALSE (Pre mulai TIDAK mengunci Post). Counter: type-agnostic lama -> anyStarted == TRUE (over-lock).
       - Test propagation tak-regresi: assert propagation shuffle masih menyentuh BAIK Pre maupun Post (siblingIds propagation cross-type tetap mencakup keduanya).
    4. Buat `HcPortal.Tests/SamePackageInheritTests.cs` (`[Trait("Category","Integration")]`, fixture): seed grup Pre-Post dgn repPost.SamePackage=true -> jalankan tambah-peserta path -> assert newPost.SamePackage == true (warisi). Counter: repPost.SamePackage=false -> newPost.SamePackage == false.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test --filter "FullyQualifiedName~SiblingTypeAwareLockTests|FullyQualifiedName~SamePackageInheritTests" 2>&1 | tail -8</automated>
  </verify>
  <done>
    `dotnet build` 0 error; kedua test hijau. `grep "SamePackage = repPost.SamePackage" Controllers/AssessmentAdminController.cs` count == 1; `grep -c "SiblingPrePostAwarePredicate" Controllers/AssessmentAdminController.cs` >= 3 (3 lock-detection site baru, di luar yang sudah ada di StartExam); propagation foreach `:5649-5659` TIDAK diubah (assert via test propagation cross-type). Komentar :5629 dikoreksi.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC browser -> AssessmentAdmin POST (5 endpoint paket/soal + Import) | Untrusted edit request; lock harus ditegakkan server-side bukan view |
| HC browser -> Import file upload | .xlsx/.xls + 5MB guard existing (:6203-6219, tak diubah) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-422-05 | Elevation of Privilege / Tampering | Bypass view-only lock via direct POST (SHUF-ISS-02 root) | mitigate | `IsSessionEditLocked` guard di AWAL 5 endpoint POST + Import -> hard-reject TempData + redirect (no-write). Server-authoritative. |
| T-422-06 | Tampering | Import langsung ke Post terkunci | mitigate | Guard lock di AWAL ImportPackageQuestions (Pitfall 3) — reject Import ke Post terkunci; sync hanya Pre->Post di akhir. |
| T-422-07 | Tampering | IDOR edit paket sesi lain | mitigate | Session di-resolve via FindAsync + `[Authorize(Roles="Admin, HC")]` existing di tiap endpoint (tak diubah). Admin/HC global scope. |
| T-422-08 | Information Disclosure | Sibling key type-aware exposes cross-type | accept | Predicate hanya filter session by Title/Category/Date/Type; tidak ekspos data baru. Low-value. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` full hijau (SamePackageSync + SessionEditLock + SiblingTypeAwareLock + SamePackageInherit + suite existing tak regresi).
- Import ke Pre+SamePackage -> Post ter-sync; Import ke Post terkunci -> reject.
- 5 endpoint POST reject saat locked (no-write); lolos saat tak-locked.
- Pre mulai -> Post TIDAK terkunci (type-aware); propagation shuffle Pre<->Post TETAP cross-type (no regresi).
- Backward-compat: grup tanpa SamePackage + Standard tak tersentuh.
</verification>

<success_criteria>
- SHFX-01: Import sync wired (SHUF-ISS-03 HIGH ditutup) via helper 6-jalur.
- SHFX-03: lock server-side di 5 endpoint + Import (no-write saat locked).
- SHFX-04: newPost inherit SamePackage.
- SHFX-06: sibling lock-detection type-aware; propagation tak berubah.
</success_criteria>

<output>
After completion, create `.planning/phases/422-samepackage-shuffle-integrity/422-02-SUMMARY.md`.
Catat: tiap perubahan shuffle/sibling-key untuk rekonsiliasi merge v32.6; konfirmasi propagation foreach :5649-5659 TIDAK diubah.
</output>
