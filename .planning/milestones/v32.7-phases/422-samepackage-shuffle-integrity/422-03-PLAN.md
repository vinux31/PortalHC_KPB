---
phase: 422-samepackage-shuffle-integrity
plan: 03
type: execute
wave: 3
depends_on: [422-01, 422-02]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ManagePackages.cshtml
  - Views/Admin/ManagePackageQuestions.cshtml
  - HcPortal.Tests/SamePackageToggleGuardTests.cs
  - HcPortal.Tests/e2e/same-package-toggle.spec.ts
autonomous: false
migration: false
requirements: [SHFX-02, SHFX-07]
must_haves:
  truths:
    - "HC dapat ON-kan SamePackage pasca-create -> paket Post ter-sync dari Pre + terkunci; OFF-kan -> kunci dilepas + paket salinan DIPERTAHANKAN (tidak dikosongkan)"
    - "Toggle ditolak (TempData Error, non-blocking) bila ADA peserta sudah-mulai di grup (StartedAt/InProgress/Completed); belum-mulai = boleh"
    - "Peringatan Acak ON pada Post SamePackage tampil (non-blocking, HC tetap bisa simpan)"
    - "Peringatan K=min muncul juga di ON-path (D-04) via ShuffleToggleRules.ShouldShowKMinTruncationWarning"
    - "Indikator mismatch dirender dari ViewBag (PackageSizeAnalysis) — duplikasi view :72-78 DIHAPUS (kill-drift)"
    - "Tombol Kelola Soal/Create/Delete paket disembunyikan/disable saat IsSamePackageLocked (UX layer; server sudah hard-reject)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ToggleSamePackage endpoint (RBAC+antiforgery+guard anyStarted+ON sync/OFF keep+audit) + ViewBag PackageSizeAnalysis + warning flags"
      contains: "ToggleSamePackage"
    - path: "Views/Admin/ManagePackages.cshtml"
      provides: "Toggle card + warnings (D-03/D-04) + mismatch single-source (D-05) + lock disable; HAPUS dup :72-78"
      contains: "ToggleSamePackage"
    - path: "Views/Admin/ManagePackageQuestions.cshtml"
      provides: "Friendly disable layer + lock banner saat IsSamePackageLocked"
    - path: "HcPortal.Tests/SamePackageToggleGuardTests.cs"
      provides: "Integration: toggle ON sync/OFF keep clone; guard anyStarted reject"
  key_links:
    - from: "Views/Admin/ManagePackages.cshtml toggle form"
      to: "AssessmentAdminController.ToggleSamePackage"
      via: "POST form-switch + @Html.AntiForgeryToken()"
      pattern: "asp-action=\"ToggleSamePackage\""
    - from: "AssessmentAdminController.ToggleSamePackage ON-path"
      to: "SyncToLinkedPostIfSamePackageAsync (Wave 2)"
      via: "SaveChanges SamePackage=true SEBELUM panggil helper"
      pattern: "SyncToLinkedPostIfSamePackageAsync"
    - from: "GET ManagePackages"
      to: "PackageSizeAnalysis.Compute -> ViewBag"
      via: "single-source mismatch (hapus view re-derive)"
      pattern: "PackageSizeAnalysis.Compute"
---

<objective>
Tutup SHFX-02 (toggle SamePackage editable pasca-create, FLOW-07, keputusan bisnis b) + lengkapi SHFX-07 UI (render peringatan D-03/D-04 + mismatch single-source D-05). Wave terakhir: endpoint `ToggleSamePackage` baru + seluruh permukaan UI aditif di `ManagePackages.cshtml` + friendly-disable di `ManagePackageQuestions.cshtml`, ditutup checkpoint UAT live @5270.

Purpose: HC perlu fleksibilitas mengubah SamePackage setelah grup dibuat (keputusan bisnis b) — ON menyalin+mengunci, OFF melepas kunci TAPI mempertahankan paket salinan (agar bisa diedit manual). Guard pra-peserta mencegah mutasi paket saat ujian sudah jalan. UI merender peringatan shuffle lengkap dari satu sumber (kill-drift) dan menyembunyikan kontrol edit saat terkunci (UX di atas server-authoritative guard Wave 2).
Output: endpoint `ToggleSamePackage` (mirror UpdateShuffleSettings: RBAC+antiforgery+guard anyStarted+ON sync/OFF keep+audit) + ViewBag (PackageSizeAnalysis + warning flags) di GET ManagePackages + toggle card + 3 warning + lock disable di view + friendly disable view soal + test integration + Playwright + checkpoint UAT.

⚠️ Pitfall 5 (D-01): OFF = lepas kunci SAJA (SamePackage=false + SaveChanges) — JANGAN panggil sync/delete; paket clone DIPERTAHANKAN.
⚠️ Open Q2 (RESEARCH): ON-sync via SyncPackagesToPost menghapus paket Post lama tapi TIDAK eksplisit hapus UserPackageAssignment -> potensi dangling. Guard anyStarted mengurangi risiko (belum-mulai biasanya belum ada assignment), TAPI edge belum-mulai-tapi-ada-assignment WAJIB di-test (Task 1 test E). Bila dangling terdeteksi, ON-path harus defensif clear stale Post UPA.
⚠️ Lesson 390.1/421: JS modal/confirm WAJIB wrap DOMContentLoaded + `typeof bootstrap !== 'undefined'` guard + single-quote confirm string (Pitfall 6).
⚠️ OVERLAP v32.6: card shuffle/warning di ManagePackages.cshtml bisa bentrok merge — catat tiap perubahan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/422-samepackage-shuffle-integrity/422-CONTEXT.md
@.planning/phases/422-samepackage-shuffle-integrity/422-RESEARCH.md
@.planning/phases/422-samepackage-shuffle-integrity/422-PATTERNS.md
@.planning/phases/422-samepackage-shuffle-integrity/422-UI-SPEC.md
@.planning/phases/422-samepackage-shuffle-integrity/422-01-SUMMARY.md
@.planning/phases/422-samepackage-shuffle-integrity/422-02-SUMMARY.md

<interfaces>
<!-- Kontrak dari Wave 1+2 + source aktual (VERIFIED 2026-06-23). -->

Wave 1 helper:
- Helpers/PackageSizeAnalysis.cs -> `PackageSizeAnalysis.Compute(IEnumerable<AssessmentPackage>)` -> Result(int PackagesWithQuestions, int? ReferenceCount, bool HasMismatch)
- Helpers/ShuffleToggleRules.cs -> `ShouldShowKMinTruncationWarning(int, bool, bool)` (ON-path) + `ShouldShowSizeMismatchWarning(int, bool, bool)` (OFF-path)
- Helpers/SessionEditLockRules.cs -> `IsSessionEditLocked(AssessmentSession)`

Wave 2 helper:
- AssessmentAdminController.SyncToLinkedPostIfSamePackageAsync(int preSessionId) (private)

EXISTING template endpoint (mirror lengkap): UpdateShuffleSettings AssessmentAdminController.cs:5623-5679 (RBAC [Authorize(Roles="Admin, HC")] + [ValidateAntiForgeryToken] + PRG + guard anyStarted :5638-5641 + audit try/catch :5663-5675).

EXISTING anyStarted idiom (:5638-5641):
```csharp
bool anyStarted = await _context.AssessmentSessions
    .AnyAsync(s => siblingSessionIds.Contains(s.Id) && s.StartedAt != null);
```
Untuk D-01 guard grup = pasangan Pre+Post (LinkedSessionId). anyStarted juga harus mencakup status InProgress/Completed (StartedAt!=null sudah implikasinya, tapi cek status eksplisit bila StartedAt bisa null pada Completed).

EXISTING ViewBag.IsSamePackageLocked (:5811) + view guard `ViewBag.IsSamePackageLocked != true` (:312 Kelola Soal, :379 Delete) — extend ke create-package + ManagePackageQuestions.

EXISTING duplikasi mismatch yang DIHAPUS:
- View Views/Admin/ManagePackages.cshtml:72-78 (re-derive — HAPUS, render ViewBag saja)
- Controller :5844-5856 (ganti panggil PackageSizeAnalysis.Compute -> ViewBag)

UI idiom (Views/Admin/ManagePackages.cshtml — VERIFIED line di UI-SPEC):
- Shuffle card form-switch + antiforgery + Simpan: :104-129
- Retake card / modal max-confirm: :143-173 / :178-222
- Lock banner alert-info bi-lock-fill: :29-44 / :96-102
- Warning alert-warning d-flex bi-exclamation-triangle: :119-127
- confirmDeletePackage() single-quote: :405-412
- TempData Error/Success dismissible: :20-27
- Inline confirm panel (CopyPackagesFromPre): :46-68

COPY Bahasa Indonesia (422-UI-SPEC.md §Copywriting — VERBATIM):
- Switch label: "Samakan paket soal dengan Pre-Test"
- Helper text: "Saat aktif, paket soal Post-Test disalin penuh dari Pre-Test dan dikunci agar komparasi Pre↔Post identik. Saat nonaktif, kunci dilepas dan paket hasil salinan tetap dipertahankan agar bisa Anda edit manual."
- Confirm ON: "Mengaktifkan ini akan MENIMPA seluruh paket soal Post-Test dengan salinan dari Pre-Test. Lanjutkan?"
- Confirm OFF: "Menonaktifkan ini akan melepas kunci paket Post-Test. Paket hasil salinan tetap dipertahankan dan bisa Anda edit. Lanjutkan?"
- Disabled-state: "Pengaturan paket-sama tidak dapat diubah karena sudah ada peserta yang memulai ujian di grup ini."
- Reject toast: "Gagal mengubah pengaturan paket-sama: sudah ada peserta yang memulai ujian di grup ini."
- Success ON: "Pengaturan paket-sama diaktifkan. Paket Post-Test telah disinkronkan dari Pre-Test dan dikunci."
- Success OFF: "Pengaturan paket-sama dinonaktifkan. Kunci dilepas; paket salinan dipertahankan untuk diedit."
- Lock reject (Wave 2, sudah ada): "Paket Post-Test ini terkunci (paket-sama). Edit soal harus dilakukan di sesi Pre-Test."
- Disabled Kelola Soal tooltip: "Terkunci: paket disinkronkan dari Pre-Test. Edit soal di sesi Pre-Test."
- Warning Acak ON SamePackage (D-03): "Acak Soal aktif pada Post-Test paket-sama. Pengacakan mengaburkan komparasi item-level Pre/Post — disarankan nonaktifkan untuk komparasi yang setara. Pengaturan tetap bisa disimpan."
- Warning K=min ON (D-04): "Acak Soal aktif: jumlah soal yang ditampilkan dipangkas ke K=min (paket terkecil) agar setiap peserta menerima jumlah soal yang sama."
- Mismatch (D-05, existing :126): "Jumlah soal antar paket berbeda — nilai maksimal & pass% bisa tak setara antar peserta."

Fixture integration: HcPortal.Tests/ShuffleLockGuardTests.cs (IClassFixture<ProtonCompletionFixture> + seed sibling/anyStarted + marker GUID).
Playwright: konvensi baseURL :5270 (branch ITHandoff). Dev admin: admin@pertamina.com.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint ToggleSamePackage (D-01: RBAC+antiforgery+guard anyStarted+ON sync/OFF keep+audit) + GET ManagePackages ViewBag (PackageSizeAnalysis + warning flags) + SamePackageToggleGuardTests</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:5623-5679 (UpdateShuffleSettings — template endpoint lengkap)
    - Controllers/AssessmentAdminController.cs:5638-5641 (anyStarted idiom) + :5663-5675 (audit try/catch)
    - Controllers/AssessmentAdminController.cs:5754-5868 (GET ManagePackages — :5811 IsSamePackageLocked, :5844-5856 mismatch compute yang diganti)
    - AssessmentAdminController.SyncToLinkedPostIfSamePackageAsync (Wave 2) + Helpers/PackageSizeAnalysis.cs + Helpers/ShuffleToggleRules.cs (Wave 1)
    - 422-RESEARCH.md §"Code Examples D-04 toggle endpoint" + Pitfall 5 (OFF keep) + Open Q2 (dangling UPA)
    - 422-PATTERNS.md §"ToggleSamePackage" (struktur 6 langkah)
    - 422-UI-SPEC.md §Copywriting (toast copy verbatim)
  </read_first>
  <action>
    1. Tambah endpoint `ToggleSamePackage` (mirror UpdateShuffleSettings):
    ```csharp
    [HttpPost][Authorize(Roles = "Admin, HC")][ValidateAntiForgeryToken]
    public async Task<IActionResult> ToggleSamePackage(int assessmentId, bool samePackage)
    {
        var post = await _context.AssessmentSessions.FindAsync(assessmentId);
        if (post == null) return NotFound();
        if (post.AssessmentType != "PostTest" || !post.LinkedSessionId.HasValue) {
            TempData["Error"] = "Pengaturan paket-sama hanya berlaku untuk Post-Test berpasangan.";
            return RedirectToAction("ManagePackages", new { assessmentId });
        }
        // GUARD D-01: grup = pasangan Pre+Post (LinkedSessionId). Tolak bila ADA peserta sudah-mulai.
        var groupIds = new[] { post.Id, post.LinkedSessionId.Value };
        bool anyStarted = await _context.AssessmentSessions
            .AnyAsync(s => groupIds.Contains(s.Id) && (s.StartedAt != null || s.Status == "InProgress" || s.Status == "Completed"));
        if (anyStarted) {
            TempData["Error"] = "Gagal mengubah pengaturan paket-sama: sudah ada peserta yang memulai ujian di grup ini.";
            return RedirectToAction("ManagePackages", new { assessmentId });
        }
        post.SamePackage = samePackage;
        post.UpdatedAt = DateTime.UtcNow;
        await _context.SaveChangesAsync();   // SamePackage tersimpan SEBELUM panggil helper
        if (samePackage) {
            await SyncToLinkedPostIfSamePackageAsync(post.LinkedSessionId.Value);  // ON -> sync Pre->Post
            TempData["Success"] = "Pengaturan paket-sama diaktifkan. Paket Post-Test telah disinkronkan dari Pre-Test dan dikunci.";
        } else {
            // OFF -> KEEP paket clone (Pitfall 5: JANGAN sync/delete)
            TempData["Success"] = "Pengaturan paket-sama dinonaktifkan. Kunci dilepas; paket salinan dipertahankan untuk diedit.";
        }
        // audit try/catch warn-only (pola :5663-5675), ActionType "ToggleSamePackage"
        return RedirectToAction("ManagePackages", new { assessmentId });
    }
    ```
    ⚠️ Open Q2 (dangling UPA): `SyncPackagesToPost` menghapus paket Post lama tapi tak eksplisit hapus UserPackageAssignment. Untuk ON-path, SEBELUM/SESUDAH sync, defensif clear stale Post UPA bila ada (mis. `_context.UserPackageAssignments.Where(a => a.AssessmentSessionId == post.Id)` -> RemoveRange) — verifikasi via test E. Bila SyncPackagesToPost sudah meng-handle, cukup assert tak ada dangling.

    2. GET ManagePackages (`:5844-5856`): GANTI compute mismatch inline -> `var sizeAnalysis = PackageSizeAnalysis.Compute(packages);` lalu set ViewBag:
       - `ViewBag.PackagesWithQuestions = sizeAnalysis.PackagesWithQuestions;`
       - `ViewBag.HasSizeMismatch = sizeAnalysis.HasMismatch;`
       - `ViewBag.ReferenceCount = sizeAnalysis.ReferenceCount;`
       - `ViewBag.ShowKMinWarning = ShuffleToggleRules.ShouldShowKMinTruncationWarning(sizeAnalysis.PackagesWithQuestions, assessment.ShuffleQuestions, sizeAnalysis.HasMismatch);`
       - `ViewBag.ShowSizeMismatchWarning = ShuffleToggleRules.ShouldShowSizeMismatchWarning(sizeAnalysis.PackagesWithQuestions, assessment.ShuffleQuestions, sizeAnalysis.HasMismatch);`
       - `ViewBag.ShowAcakOnSamePackageWarning = (assessment.AssessmentType == "PostTest" && assessment.SamePackage && assessment.ShuffleQuestions);` (D-03)
       - `ViewBag.AnyStartedInGroup` (untuk disable toggle di view — reuse anyStarted compute grup).

    3. Buat `HcPortal.Tests/SamePackageToggleGuardTests.cs` (`[Trait("Category","Integration")]`, IClassFixture<ProtonCompletionFixture>):
       - Test A (ON sync): grup Pre-Post belum-mulai, Post.SamePackage=false, Pre punya paket -> toggle ON -> Post.SamePackage==true + paket Post == Pre.
       - Test B (OFF keep): Post.SamePackage=true + paket clone ada -> toggle OFF -> SamePackage==false + paket Post MASIH ADA (tidak dikosongkan).
       - Test C (guard reject anyStarted): seed peserta StartedAt!=null di grup -> toggle -> SamePackage TIDAK berubah (reject).
       - Test D (allow belum-mulai): grup belum-mulai -> toggle ALLOW.
       - Test E (Open Q2 dangling): belum-mulai TAPI ada UserPackageAssignment Post -> toggle ON sync -> assert TIDAK ada UPA dangling (assignment lama ke paket yang dihapus harus bersih).
       - Test F (non-Post / non-paired reject): toggle pada Pre/Standard -> TempData Error, no change.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test --filter "FullyQualifiedName~SamePackageToggleGuardTests" 2>&1 | tail -8</automated>
  </verify>
  <done>
    `dotnet build` 0 error; SamePackageToggleGuardTests hijau (6 test incl OFF-keep + dangling). `grep "ToggleSamePackage" Controllers/AssessmentAdminController.cs` mencakup [HttpPost]+[ValidateAntiForgeryToken]+[Authorize(Roles="Admin, HC")]; `grep "PackageSizeAnalysis.Compute" Controllers/AssessmentAdminController.cs` count == 1; mismatch inline compute :5844-5856 lama dihapus.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI ManagePackages.cshtml (toggle card + 3 warning + mismatch single-source D-05 hapus dup :72-78 + lock disable) + ManagePackageQuestions.cshtml friendly disable + Playwright</name>
  <read_first>
    - Views/Admin/ManagePackages.cshtml:20-44 (TempData + lock banner), :72-78 (mismatch dup — HAPUS), :104-129 (shuffle card mirror), :117-127 (warning idiom), :312/364-367/379 (Kelola Soal + Delete conditional), :405-412 (confirmDeletePackage)
    - Views/Admin/ManagePackageQuestions.cshtml (0 lock awareness — tambah banner + disable)
    - 422-UI-SPEC.md (SELURUH §Copywriting + §Component Inventory + §Interaction Contracts 1-8 — copy VERBATIM, idiom reuse)
    - 422-RESEARCH.md Pitfall 6 (confirm single-quote) + Anti-Patterns (DOMContentLoaded guard)
    - ViewBag dari Task 1 (PackagesWithQuestions/HasSizeMismatch/ReferenceCount/ShowKMinWarning/ShowSizeMismatchWarning/ShowAcakOnSamePackageWarning/AnyStartedInGroup/IsSamePackageLocked)
  </read_first>
  <action>
    Di `Views/Admin/ManagePackages.cshtml` (HANYA untuk Post-Test berpasangan — backward-compat: Standard/Pre tak menampilkan toggle):
    1. **Toggle SamePackage card** (mirror shuffle card :104-129): `<form method="post" asp-action="ToggleSamePackage" asp-controller="AssessmentAdmin">` + `@Html.AntiForgeryToken()` + hidden `assessmentId` + `form-check form-switch` checkbox `name="samePackage" value="true"` (checked bila SamePackage) + label "Samakan paket soal dengan Pre-Test" + helper-text (copy verbatim) + `btn-primary` "Simpan Pengaturan". Bila `ViewBag.AnyStartedInGroup == true`: switch + button `disabled` + tampilkan disabled-state message (copy verbatim) sebagai `form-text text-muted`.
    2. **Confirm-before** (Interaction Contract 2/7): onsubmit confirm — single-quote string, ON pakai copy confirm-ON, OFF pakai copy confirm-OFF (deteksi state checkbox). Bila pakai modal/inline JS: WAJIB wrap `document.addEventListener('DOMContentLoaded', ...)` + `if (typeof bootstrap !== 'undefined')` guard (lesson 390.1/421). Atau inline `confirm()` mirror confirmDeletePackage :405-412.
    3. **Warning D-03** (Acak ON SamePackage): `@if (ViewBag.ShowAcakOnSamePackageWarning == true)` -> `alert alert-warning d-flex` + `bi-exclamation-triangle` + copy verbatim (non-blocking).
    4. **Warning D-04** (K=min ON-path): `@if (ViewBag.ShowKMinWarning == true)` -> alert-warning + copy verbatim. Pertahankan OFF-path warning existing (`ViewBag.ShowSizeMismatchWarning`).
    5. **Mismatch single-source D-05**: HAPUS blok re-derive `:72-78`. Render dari `ViewBag.HasSizeMismatch`/`ViewBag.ReferenceCount` saja (copy existing :126).
    6. **Lock disable** (D-07 UX layer): extend guard existing `ViewBag.IsSamePackageLocked != true` ke tombol create-package + Kelola Soal (:312/364-367) + Delete (:379). Disabled Kelola Soal -> tooltip "Terkunci: paket disinkronkan dari Pre-Test. Edit soal di sesi Pre-Test."

    Di `Views/Admin/ManagePackageQuestions.cshtml`:
    7. Tambah lock banner (mirror ManagePackages :29-44 `alert-info bi-lock-fill`) + disable tombol Create/Edit/Delete soal saat `ViewBag.IsSamePackageLocked == true` (server SUDAH hard-reject Wave 2 — ini friendly layer). (Controller GET ManagePackageQuestions harus set ViewBag.IsSamePackageLocked — bila belum, tambah di action GET-nya mirror :5811.)

    8. Buat `HcPortal.Tests/e2e/same-package-toggle.spec.ts` (Playwright, baseURL :5270, login admin@pertamina.com): smoke — buka ManagePackages Post berpasangan -> toggle card render + switch present; toggle ON -> Success toast + lock banner muncul; tombol Kelola Soal disabled saat locked. (e2e = smoke render + happy path; full lifecycle = checkpoint Task 3.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -c "ToggleSamePackage" Views/Admin/ManagePackages.cshtml && grep -c "ShowKMinWarning\|ShowAcakOnSamePackageWarning" Views/Admin/ManagePackages.cshtml</automated>
  </verify>
  <done>
    `dotnet build` 0 error. ManagePackages.cshtml: toggle form `asp-action="ToggleSamePackage"` ada; warning D-03/D-04 dirender dari ViewBag; blok mismatch dup :72-78 DIHAPUS (grep re-derive `referenceCount = packages.Where` -> 0); lock disable extend ke create+Kelola Soal. ManagePackageQuestions.cshtml: lock banner + disable saat IsSamePackageLocked. Playwright spec file ada. Confirm string single-quote; modal/JS wrap DOMContentLoaded+bootstrap guard.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint UAT live @5270 — toggle ON/OFF + lock reject + Import sync + warning (SEED_WORKFLOW snapshot->seed->restore)</name>
  <what-built>
    Endpoint ToggleSamePackage + UI toggle card/warning/lock-disable + (Wave 2) Import auto-sync + server-side lock + (Wave 1) PackageNumber unik. Semua xUnit + Playwright smoke hijau. Checkpoint ini = verifikasi visual/fungsional live oleh HC sebelum gerbang formal.
  </what-built>
  <how-to-verify>
    Ikuti CLAUDE.md SEED_WORKFLOW: (1) snapshot DB lokal (sqlcmd BACKUP), (2) catat docs/SEED_JOURNAL.md, (3) UAT, (4) restore + tandai cleaned.
    Jalankan app: `dotnet run` (branch ITHandoff -> http://localhost:5270).
    Langkah UAT:
    1. **Toggle ON:** Buka /Admin/ManagePackages (Post-Test berpasangan, grup BELUM ada peserta mulai). Aktifkan switch "Samakan paket soal dengan Pre-Test" -> confirm ON muncul -> setuju -> Success toast "diaktifkan...disinkronkan...dikunci" + lock banner + paket Post == paket Pre (cek konten soal). Verifikasi DB (sqlcmd): AssessmentSessions.SamePackage=1 untuk Post; AssessmentPackages Post identik Pre.
    2. **Lock reject server-side:** Saat terkunci, tombol "Kelola Soal" disabled (tooltip lock). Coba POST langsung (mis. via devtools/refresh form Create paket) -> TempData Error "terkunci (paket-sama)" + no-write.
    3. **Import sync (SHFX-01 HIGH):** Import Excel bank-soal ke paket PRE ber-SamePackage -> setelah sukses, buka Post -> paket Post otomatis ter-update sama dengan Pre (sebelumnya BOCOR).
    4. **Toggle OFF keep:** Nonaktifkan switch -> confirm OFF -> Success "dinonaktifkan...paket salinan dipertahankan" -> paket Post MASIH ADA + tombol Kelola Soal kembali aktif (editable).
    5. **Guard anyStarted:** Pada grup yang SUDAH ada peserta mulai -> switch disabled + pesan disabled-state; bila POST dipaksa -> reject toast (non-blocking).
    6. **Warning shuffle:** Aktifkan Acak Soal pada Post SamePackage -> peringatan D-03 muncul (non-blocking, tetap bisa Simpan); bila ukuran paket beda + Acak ON -> peringatan K=min (D-04).
    7. **PackageNumber:** Hapus paket tengah -> buat paket baru -> nomor TIDAK bentrok (MAX+1). sqlcmd cek 0 duplikat (AssessmentSessionId, PackageNumber).
    8. **Backward-compat:** Buka assessment Standard -> TIDAK ada toggle card SamePackage (tak tersentuh).
  </how-to-verify>
  <action>Checkpoint manual: pause untuk verifikasi HC live @5270 (lihat how-to-verify 8 langkah). Snapshot DB sebelum, restore sesudah (SEED_WORKFLOW). Tidak ada perubahan kode di task ini.</action>
  <verify>HC mengeksekusi 8 langkah UAT live; setiap langkah memenuhi expected outcome (toast/lock/sync/no-write/PackageNumber unik/backward-compat) dengan bukti DB sqlcmd.</verify>
  <done>8 langkah UAT lulus + DB pristine pasca-restore + HC ketik "approved".</done>
  <resume-signal>Ketik "approved" bila 8 langkah lulus, atau jelaskan isu (sertakan langkah # + bukti DB/screenshot) untuk perbaikan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC browser -> AssessmentAdmin POST ToggleSamePackage | Untrusted toggle request; mutasi paket + lock = server-authoritative |
| Client JS confirm/modal | UX only; server hard-rejects guard regardless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-422-09 | Tampering / CSRF | ToggleSamePackage endpoint | mitigate | `[ValidateAntiForgeryToken]` + `@Html.AntiForgeryToken()` di form + `[Authorize(Roles="Admin, HC")]` (mirror UpdateShuffleSettings). |
| T-422-10 | Tampering | Bypass toggle guard via direct POST (skip JS disable) | mitigate | Guard anyStarted server-side di endpoint (bukan hanya switch disabled di view). Reject TempData + no-write. |
| T-422-11 | Tampering | Mass-assignment SamePackage via form | mitigate | Endpoint terima `bool samePackage` eksplisit, server set kolom; bukan model-bind seluruh entity. |
| T-422-12 | Information Disclosure | XSS pada toast/warning Bahasa Indonesia | mitigate | Razor auto-encode; copy statik (bukan user input). Confirm string single-quote, no Json.Serialize di attribute (Pitfall 6). |
| T-422-13 | Tampering | Dangling UserPackageAssignment setelah ON-sync (Open Q2) | mitigate | ON-path defensif clear stale Post UPA + test E assert no-dangling; guard anyStarted mengurangi paparan. |
</threat_model>

<verification>
- `dotnet build` 0 error; `dotnet test` full hijau (SamePackageToggleGuard incl OFF-keep + dangling + suite tak regresi).
- Playwright smoke hijau (toggle render + ON success + lock disabled).
- Checkpoint UAT live @5270: 8 langkah lulus (toggle ON/OFF, lock reject server-side, Import sync, guard anyStarted, warning D-03/D-04, PackageNumber MAX+1, backward-compat Standard).
- SEED_WORKFLOW: snapshot->seed->restore (DB pristine pasca-UAT).
</verification>

<success_criteria>
- SHFX-02: ToggleSamePackage ON sync+lock / OFF keep clone + guard anyStarted (keputusan bisnis b).
- SHFX-07 (lengkap): warning D-03 + D-04 dirender; mismatch single-source D-05 (dup view dihapus).
- Lock UX layer (D-07) di kedua view di atas server-authoritative guard Wave 2.
- UAT live PASS; backward-compat terjaga.
</success_criteria>

<output>
After completion, create `.planning/phases/422-samepackage-shuffle-integrity/422-03-SUMMARY.md`.
Catat: hasil UAT 8 langkah + bukti DB; Open Q2 resolusi (dangling UPA handled/none); tiap perubahan shuffle/UI untuk rekonsiliasi merge v32.6; konfirmasi migration=TRUE (422-01 AddPackageNumberUniqueIndex) untuk notify IT akhir milestone.
</output>
