---
status: passed
phase: 422-samepackage-shuffle-integrity
plan: 03
source: [422-03-PLAN.md checkpoint:human-verify]
method: Playwright MCP live @localhost:5270 (orchestrator-driven) + 57 xUnit integration (backend authoritative)
started: 2026-06-23
updated: 2026-06-23
---

# Phase 422 — Checkpoint UAT (SHFX-02/03/07 render layer)

**Driver:** milestone-autopilot §5. Backend behavior dibuktikan oleh **57 xUnit integration test** (real-SQLEXPRESS, Wave 1+2+3). UAT live = lapisan **render UI** (kelas bug yang xUnit tak jangkau: quote-break confirm, bootstrap-timing ReferenceError, TempData serialization) — pelajaran fase 421.

**Setup:** App @5270 (branch ITHandoff, AD off). Login admin@pertamina.com. Target: grup Pre-Post existing **161 (PostTest) ↔ 160 (PreTest)**, SamePackage awal=0, 0 peserta sudah-mulai. SEED_WORKFLOW: BACKUP `HcPortalDB_Dev_422uat.bak` (lpc:) → UAT → RESTORE WITH REPLACE (baseline verified).

## Tests

### 1. Toggle card render (SHFX-02 UI)
expected: kartu "Paket Soal Sama dengan Pre-Test" + checkbox + helper text + Simpan render di /Admin/ManagePackages Post-Test.
result: **PASS** — kartu render lengkap, checkbox enabled (guard allow, 0 peserta mulai), 0 console error saat load.

### 2. Toggle ON — confirm-before (quote-break class) + sync + lock
expected: klik Simpan → confirm dialog (pesan benar, tak rusak) → accept → success toast → paket Post ter-sync dari Pre → lock banner alert-info → "Kelola Soal" disabled.
result: **PASS** — dialog "Mengaktifkan ini akan MENIMPA seluruh paket soal Post-Test dengan salinan dari Pre-Test. Lanjutkan?" (single-quote confirm benar, **quote-break ABSENT**). Accept → Success "...diaktifkan. Paket Post-Test telah disinkronkan dari Pre-Test dan dikunci." (**TempData OK, no JsonElement bug**). Packages 0→1 ("Paket A" ter-sync). Lock banner alert-info "Paket soal disinkronkan dari Pre-Test...[Buka Pre-Test]". "Kelola Soal" jadi non-link disabled + tooltip "Terkunci: paket disinkronkan dari Pre-Test...".

### 3. SHFX-07 D-03 warning shuffle ON + SamePackage (non-blocking)
expected: saat Acak Soal ON + SamePackage ON → alert-warning non-blocking.
result: **PASS** — "Acak Soal aktif pada Post-Test paket-sama. Pengacakan mengaburkan komparasi item-level Pre/Post — disarankan nonaktifkan... Pengaturan tetap bisa disimpan." (alert-warning render, non-blocking).

### 4. Toggle OFF — confirm + keep packages (D-01 OFF-keep)
expected: uncheck → Simpan → confirm OFF → accept → success → paket DIPERTAHANKAN (tak dikosongkan) → lock dilepas.
result: **PASS** — dialog "Menonaktifkan ini akan melepas kunci paket Post-Test. Paket hasil salinan tetap dipertahankan dan bisa Anda edit. Lanjutkan?". Accept → Success "...dinonaktifkan. Kunci dilepas; paket salinan dipertahankan untuk diedit." Packages **(1) "Paket A" TETAP ADA** (preserved). "Kelola Soal"+"Import Questions" kembali ENABLED. Warning shuffle hilang.

### 5. SHFX-03 friendly-disable (lock UX)
expected: saat SamePackage locked, "Kelola Soal" disabled dengan tooltip; saat unlocked, enabled.
result: **PASS** — terlihat di Test 2 (disabled+tooltip saat locked) & Test 4 (enabled saat unlocked). Server hard-reject = lapisan Wave 2 (xUnit SessionEditLockTests).

### 6. Backward-compat — Standard assessment tanpa toggle card
expected: assessment Standard (non Post-Test) tak menampilkan kartu SamePackage.
result: **PASS** — sesi Standard 9025: hanya kartu "Pengacakan" + "Ujian Ulang", **TANPA** kartu "Paket Soal Sama dengan Pre-Test". (Bonus: started-guard render — "Pengaturan pengacakan terkunci" karena ada peserta mulai.)

## Summary

total: 6
passed: 6
issues: 0
pending: 0

**0 JS console error** sepanjang UAT (load + 2× POST round-trip) → tak ada bootstrap-timing ReferenceError. Tiga kelas bug render fase 421 (quote-break confirm, bootstrap-timing, TempData JsonElement) **semua ABSENT**.

## Covered by xUnit (tidak di-live-click, backend authoritative — 57 test green)
- SHFX-01 Import auto-sync (SamePackageSyncTests) · SHFX-03 server-side reject 5 endpoint (SessionEditLockTests) · SHFX-02 guard anyStarted reject + dangling-UPA Open Q2 (SamePackageToggleGuardTests) · SHFX-04 inherit (SamePackageInheritTests) · SHFX-05 MAX+1 + unique index + dedup (PackageNumberUnique/MigrationTests) · SHFX-06 sibling type-aware lock-only + propagation no-regress (SiblingTypeAwareLockTests) · SHFX-07 K=min ON-path + mismatch single-source (ShuffleToggleRules/PackageSizeAnalysisTests).

## DB
snapshot→UAT→RESTORE WITH REPLACE (lpc:); baseline verified (session 161 SamePackage=0, pkg161=0, leftover pkg 9021 gone); SEED_JOURNAL cleaned.

## Gaps
None.
