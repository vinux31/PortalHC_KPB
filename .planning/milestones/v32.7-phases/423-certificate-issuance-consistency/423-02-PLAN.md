---
phase: 423-certificate-issuance-consistency
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Services/GradingService.cs
  - Controllers/AssessmentAdminController.cs
  - Controllers/TrainingAdminController.cs
  - HcPortal.Tests/CertIssuanceIntegrationTests.cs
autonomous: true
migration: false
requirements: [CERT-01, CERT-03, CERT-04, CERT-05]
must_haves:
  truths:
    - "Keempat cert-issue site (GradingService.GradeAndCompleteAsync, GradingService.RegradeAfterEditAsync, AssessmentAdminController.FinalizeEssayGrading, TrainingAdminController.AddManualAssessment) memutuskan kelayakan cert HANYA via CertIssuanceRules.ShouldIssueCertificate — tidak ada lagi gate inline divergen"
    - "PreTest + GenerateCertificate=true + lulus -> graded sukses TAPI NomorSertifikat tetap null di SEMUA 4 site (cert tidak terbit)"
    - "Tiga site grading-time auto-gen seq memanggil CertNumberHelper.TryAssignNextSeqAsync (loop terpusat) — bukan loop inline"
    - "Saat seq gagal terbit setelah retry: sesi tetap Completed (non-destruktif) + UpdatedAt di-stamp + audit 'CertIssuanceFailed' -> queryable predicate (IsPassed==true && GenerateCertificate && AssessmentType!=PreTest && NomorSertifikat==null)"
    - "Manual AddManualAssessment: GenerateCertificate berhenti hardcode (derive dari aturan); nomor manual yang menyerupai format auto ditolak (ResemblesAutoCertFormat); SaveChanges dibungkus try/catch DbUpdateException -> error ramah (bukan 500); Permanent && ValidUntil!=null ditolak"
    - "Anti double-cert guard UNCONDITIONAL (di luar if(!ConfirmDuplicateTitle)) blok dua cert AKTIF (peserta, judul) — renewal (RenewsSessionId terisi) lolos; tidak bisa di-bypass via ConfirmDuplicateTitle"
  artifacts:
    - path: "Services/GradingService.cs"
      provides: "SITE 1 (:287) + SITE 2 (:520) wire ShouldIssueCertificate + TryAssignNextSeqAsync + ValidUntil derive + sinyal-gagal"
      contains: "CertIssuanceRules.ShouldIssueCertificate"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "SITE 3 FinalizeEssayGrading wire helper + anti-dup guard unconditional HasActiveCertForTitleAsync"
      contains: "HasActiveCertForTitleAsync"
    - path: "Controllers/TrainingAdminController.cs"
      provides: "SITE 4 AddManualAssessment: stop hardcode + ResemblesAutoCertFormat reject + try/catch friendly + Permanent⊥ValidUntil"
      contains: "ResemblesAutoCertFormat"
    - path: "HcPortal.Tests/CertIssuanceIntegrationTests.cs"
      provides: "real-SQL: PreTest no-cert di site nyata; exactly-1-cert format; anti-dup block + renewal exempt + ConfirmDuplicateTitle tak bypass"
      contains: "IClassFixture<RetakeServiceFixture>"
  key_links:
    - from: "Services/GradingService.cs:287 + :520"
      to: "CertIssuanceRules.ShouldIssueCertificate(session) + CertNumberHelper.TryAssignNextSeqAsync"
      via: "ganti gate inline + loop inline"
      pattern: "TryAssignNextSeqAsync"
    - from: "Controllers/AssessmentAdminController.cs FinalizeEssayGrading:3887"
      to: "CertIssuanceRules.ShouldIssueCertificate + anti-dup guard"
      via: "wire helper, guard unconditional di luar ConfirmDuplicateTitle"
      pattern: "CertIssuanceRules.ShouldIssueCertificate"
    - from: "Controllers/TrainingAdminController.cs AddManualAssessment:759-765"
      to: "ResemblesAutoCertFormat reject + try/catch IsDuplicateKeyException"
      via: "stop hardcode GenerateCertificate + namespace validate + friendly error"
      pattern: "IsDuplicateKeyException"
---

<objective>
Wave 2: pasang helper Wave 1 sebagai GATE TUNGGAL di keempat cert-issue site + harden penomoran + tambah guard anti double-cert server-side. Ini inti CERT-01 (konsolidasi gate), CERT-03 (seq atomik + sinyal-gagal), CERT-04 (namespace manual + error ramah), CERT-05 (anti-dup tak bisa di-bypass).

Purpose: Sebelum fase ini, gate cert divergen (SITE 1 & 3 TANPA cek PreTest, SITE 4 hardcode `GenerateCertificate=true`, SITE 2 saja yang konsisten) = root-cause GRD-01/FLD-5.2-02/FLD-5.2-10. Manual `SaveChanges` tanpa try/catch -> kolisi nomor = HTTP 500 (FLD-5.2-07). Anti-dup hanya soft-block judul yang bisa di-bypass via `ConfirmDuplicateTitle` (VAL-04/GRD-10). Plan ini menutup keempat divergensi + menambah guard cert-aktif unconditional.
Output: GradingService SITE 1 (:287) & SITE 2 (:520) wire helper + `TryAssignNextSeqAsync` + derive ValidUntil + sinyal-gagal; AssessmentAdminController SITE 3 (FinalizeEssayGrading :3887) wire helper + anti-dup guard `HasActiveCertForTitleAsync` unconditional; TrainingAdminController SITE 4 (AddManualAssessment :759-765) stop hardcode + `ResemblesAutoCertFormat` reject + try/catch friendly + Permanent⊥ValidUntil; + integration tests real-SQL.

migration=FALSE — refactor + guard saja; tidak ada schema/write DB baru. Sinyal-gagal pakai `UpdatedAt` existing + audit (zero-schema, D-03 discretion #1).

⚠️ PITFALL (research §Pitfall 1): nama `RecomputeAfterEssayGradingAsync` di CONTEXT TIDAK ADA. Site nyata = `RegradeAfterEditAsync` (:520) + `FinalizeEssayGrading` (:3887, controller). JANGAN cari nama CONTEXT verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/423-certificate-issuance-consistency/423-CONTEXT.md
@.planning/phases/423-certificate-issuance-consistency/423-RESEARCH.md
@.planning/phases/423-certificate-issuance-consistency/423-PATTERNS.md
@.planning/phases/423-certificate-issuance-consistency/423-01-SUMMARY.md

<interfaces>
<!-- Kontrak dari Wave 1 (423-01) + source aktual (VERIFIED 2026-06-24). -->

DARI WAVE 1 (Helpers/CertIssuanceRules.cs — pure):
```csharp
public static bool ShouldIssueCertificate(AssessmentSession s);              // CERT-01 gate tunggal
public static DateOnly? DeriveValidUntil(string? certificateType, DateTime? completedAt);  // CERT-02/06
public static bool ResemblesAutoCertFormat(string? nomor);                   // CERT-04 namespace
```
DARI WAVE 1 (Helpers/CertNumberHelper.cs):
```csharp
public static async Task<bool> TryAssignNextSeqAsync(ApplicationDbContext context, int sessionId, DateTime certNow, int maxAttempts = 8);  // CERT-03 loop terpusat
public static bool IsDuplicateKeyException(DbUpdateException ex);            // existing — reuse try/catch
```

SITE 1 (VERIFIED Services/GradingService.cs:287) — gate TANPA cek PreTest, loop inline :295-318, log-only fail :316:
```csharp
if (session.GenerateCertificate && isPassed)        // ❌ TANPA PreTest -> ganti CertIssuanceRules.ShouldIssueCertificate(session)
{ /* loop retry-3x inline -> ganti panggil TryAssignNextSeqAsync; ValidUntil TIDAK di-set di sini (GRD-06) */ }
```

SITE 2 (VERIFIED Services/GradingService.cs:520, dalam RegradeAfterEditAsync, cabang Fail->Pass) — pola REFERENSI sudah cek PreTest:
```csharp
if (session.GenerateCertificate && session.AssessmentType != "PreTest")   // -> seragamkan ke ShouldIssueCertificate
{ /* loop retry-3x inline :527-545 -> ganti panggil TryAssignNextSeqAsync */ }
```
ValidUntil-on-revoke cast idiom (VERIFIED :500-501): `.SetProperty(r => r.ValidUntil, (DateOnly?)null)` — pakai cast eksplisit saat set ValidUntil derived.

SITE 3 (VERIFIED Controllers/AssessmentAdminController.cs:3887 FinalizeEssayGrading) — gate TANPA cek PreTest, loop inline :3894-3910; PXF-08 certError precedent (:3972-3974):
```csharp
if (session.GenerateCertificate && isPassed)        // ❌ TANPA PreTest -> ShouldIssueCertificate
// ... :3972
var certError = (session.GenerateCertificate && isPassed && string.IsNullOrEmpty(updatedSession?.NomorSertifikat))
    ? "Nomor sertifikat gagal dibuat, coba lagi." : null;   // sudah precedent sinyal — konsisten-kan
```

ANTI-DUP GUARD — POLA BENAR (unconditional double-renewal guard, VERIFIED :1015-1027). Tiru struktur ini (di LUAR ConfirmDuplicateTitle):
```csharp
if (model.RenewsSessionId.HasValue)
{
    var srcAlreadyRenewed = await _context.AssessmentSessions.AnyAsync(a => a.RenewsSessionId == model.RenewsSessionId && a.IsPassed == true) || ...;
    if (srcAlreadyRenewed) ModelState.AddModelError("", "Sertifikat ini sudah di-renew sebelumnya.");
}
```
POLA SALAH (soft-block judul dibungkus !ConfirmDuplicateTitle, VERIFIED :997 — JANGAN taruh guard cert di sini):
```csharp
if (!string.IsNullOrWhiteSpace(model.Title) && !isRenewalModePost && !ConfirmDuplicateTitle)
{ var dupMatches = await FindTitleDuplicatesAsync(_context, model.Title); ... }   // ⚠️ bypass-prone
```
Normalizer (VERIFIED Controllers/AdminBaseController.cs:271): `public static string NormalizeTitleForDup(string? s)` (trim+collapse-ws+lowercase). C#-only -> normalisasi in-memory.

SITE 4 (VERIFIED Controllers/TrainingAdminController.cs:739-765 AddManualAssessment, foreach per-worker). `_auditLog` tersedia (dipakai :769). Insert manual pakai free-text `wc.NomorSertifikat` (BUKAN auto-gen seq):
```csharp
NomorSertifikat = wc.NomorSertifikat,   // :750 ❌ free-text, namespace tak divalidasi (CERT-04)
GenerateCertificate = true,             // :759 ❌ HARDCODE (D-01)
// :765 await _context.SaveChangesAsync();  ❌ TANPA try/catch DbUpdateException -> kolisi = 500
```
Permanent⊥ValidUntil analog (VERIFIED AddManualTraining :269 + :499): `if (model.CertificateType == "Permanent" && model.ValidUntil != null) ModelState.AddModelError("ValidUntil", "...")`.
ManualDuplicatePredicate (VERIFIED AdminBaseController.cs:265) = UserId+Title+CompletedAt+IsManualEntry — BUKAN guard cert-aktif (CERT-05 tetap perlu guard terpisah).

INTEGRATION TEST RECIPE (VERIFIED HcPortal.Tests/RetakeThenPassCertTests.cs):
```csharp
[Trait("Category", "Integration")]
public class CertIssuanceIntegrationTests : IClassFixture<RetakeServiceFixture>
{
    private readonly RetakeServiceFixture _fixture;
    public CertIssuanceIntegrationTests(RetakeServiceFixture f) => _fixture = f;
    private ApplicationDbContext NewCtx() => new ApplicationDbContext(_fixture.Options);
    private static GradingService NewGrading(ApplicationDbContext ctx) {
        var fakeNotif = new FakeNotificationService();
        var audit = new AuditLogService(ctx);
        var completion = new ProtonCompletionService(ctx, NullLogger<ProtonCompletionService>.Instance, fakeNotif, audit);
        var bypass = new ProtonBypassService(ctx, completion, fakeNotif, audit, NullLogger<ProtonBypassService>.Instance);
        var worker = new FakeWorkerDataService();
        return new GradingService(ctx, worker, NullLogger<GradingService>.Instance, completion, bypass);
    }
    // SeedSessionAsync(ctx, userId, title, "Post", generateCertificate:true, passPercentage:...) + SeedPackageWithResponsesAsync
    // exactly-1-cert assert: CountAsync(a => a.Id==sid && a.NomorSertifikat != null) == 1; Assert.Matches(@"^KPB/\d{3}/[IVX]+/\d{4}$", cert)
}
```
Regression existing (JANGAN rusak): RetakeThenPassCertTests, CertDedupTests, CertificateStatusTests, CertGateAuditTests, CertAlertConsistencyTests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire helper di GradingService SITE 1 (:287) & SITE 2 (:520) + TryAssignNextSeqAsync + derive ValidUntil + sinyal-gagal non-destruktif</name>
  <read_first>
    - Services/GradingService.cs:280-336 (SITE 1 — gate + loop inline + log-only fail; konteks isPassed/certNow/certYear)
    - Services/GradingService.cs:490-560 (SITE 2 RegradeAfterEditAsync — gate cek PreTest + loop + ValidUntil revoke cast :500-501)
    - Helpers/CertIssuanceRules.cs (Wave 1 — ShouldIssueCertificate, DeriveValidUntil) + Helpers/CertNumberHelper.cs (TryAssignNextSeqAsync)
    - 423-PATTERNS.md "Services/GradingService.cs — SITE 1 & SITE 2" (excerpt verbatim) + 423-RESEARCH.md Pattern 2 + §"Sinyal cert gagal terbit"
  </read_first>
  <behavior>
    - PreTest + GenerateCertificate=true + lulus (all-correct) via GradeAndCompleteAsync -> graded TRUE tetapi NomorSertifikat == null (CERT-01, SITE 1)
    - PostTest lulus + GenerateCertificate=true via GradeAndCompleteAsync -> tepat 1 cert, format ^KPB/\d{3}/[IVX]+/\d{4}$ (regresi tetap hijau)
    - PostTest, CertificateType "Annual", lulus -> ValidUntil == CompletedAt.AddYears(1) (CERT-02 via DeriveValidUntil)
    - PostTest, CertificateType "Permanent", lulus -> ValidUntil tetap null (CERT-06)
    - RegradeAfterEditAsync flip Fail->Pass pada PreTest -> NomorSertifikat tetap null (CERT-01, SITE 2 sudah konsisten, verifikasi via helper)
    - Seq-fail (simulasi/burst) -> sesi tetap Completed/IsPassed (non-destruktif) + UpdatedAt ter-stamp + predikat queryable lulus&NomorSertifikat==null benar
  </behavior>
  <action>
    SITE 1 (`GradeAndCompleteAsync` :287):
    1. Ganti gate `if (session.GenerateCertificate && isPassed)` -> `if (CertIssuanceRules.ShouldIssueCertificate(session))`. (Tambah `using HcPortal.Helpers;` bila belum.)
    2. Ganti seluruh loop retry inline (`:295-318`) dengan panggilan terpusat:
    ```csharp
    var certNow = DateTime.Now;
    bool certSaved = await CertNumberHelper.TryAssignNextSeqAsync(_context, session.Id, certNow);
    ```
    3. Derive + set ValidUntil (CERT-02/06) — HANYA bila cert tersimpan, gunakan DeriveValidUntil dari CompletedAt (cast `(DateOnly?)`):
    ```csharp
    if (certSaved)
    {
        var validUntil = CertIssuanceRules.DeriveValidUntil(session.CertificateType, session.CompletedAt);
        if (validUntil != null)
            await _context.AssessmentSessions.Where(s => s.Id == session.Id)
                .ExecuteUpdateAsync(s => s.SetProperty(r => r.ValidUntil, (DateOnly?)validUntil));
    }
    ```
    4. Sinyal-gagal non-destruktif (ganti log-only `:316`): JANGAN rollback. Stamp `UpdatedAt` + log + (best-effort) audit:
    ```csharp
    else   // !certSaved
    {
        _logger.LogError("Cert gagal terbit SessionId={SessionId} setelah retry — sesi tetap Completed (non-destruktif), tandai HC.", session.Id);
        await _context.AssessmentSessions.Where(s => s.Id == session.Id)
            .ExecuteUpdateAsync(s => s.SetProperty(r => r.UpdatedAt, DateTime.UtcNow));
        // predikat queryable: IsPassed==true && GenerateCertificate && AssessmentType!=PreTest && NomorSertifikat==null
    }
    ```
    SITE 2 (`RegradeAfterEditAsync` :520):
    5. Ganti gate `if (session.GenerateCertificate && session.AssessmentType != "PreTest")` -> `if (CertIssuanceRules.ShouldIssueCertificate(session))`. Catatan: SITE 2 sudah cek PreTest tapi pakai literal + tidak cek IsPassed lewat helper; seragamkan ke helper (cabang ini sudah dalam flip Fail->Pass jadi IsPassed==true).
    6. Ganti loop inline (`:527-545`) dengan `await CertNumberHelper.TryAssignNextSeqAsync(_context, session.Id, certNow)`; pertahankan komentar paritas ValidUntil (:534) — derive ValidUntil seperti SITE 1 bila certSaved.
    Pastikan tidak ada loop retry inline tersisa di kedua site (grep `maxCertAttempts` di GradingService -> 0).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -c "CertIssuanceRules.ShouldIssueCertificate\|TryAssignNextSeqAsync" Services/GradingService.cs && grep -c "maxCertAttempts" Services/GradingService.cs</automated>
  </verify>
  <done>
    `dotnet build` 0 error. `grep -c "CertIssuanceRules.ShouldIssueCertificate" Services/GradingService.cs` >= 2 (SITE 1 + SITE 2); `grep -c "TryAssignNextSeqAsync" Services/GradingService.cs` >= 2; `grep -c "maxCertAttempts" Services/GradingService.cs` == 0 (loop inline dihapus); `grep "GenerateCertificate && isPassed" Services/GradingService.cs` == 0; `grep "DeriveValidUntil" Services/GradingService.cs` >= 1. Suite pure tak regresi.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SITE 3 FinalizeEssayGrading wire helper + anti-dup guard unconditional HasActiveCertForTitleAsync + SITE 4 manual stop-hardcode + namespace + try/catch + Permanent⊥ValidUntil</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:3880-3980 (SITE 3 FinalizeEssayGrading gate + loop + certError PXF-08)
    - Controllers/AssessmentAdminController.cs:1010-1028 (POLA BENAR guard unconditional double-renewal — tiru) + :995-1007 (POLA SALAH soft-block — JANGAN)
    - Controllers/AdminBaseController.cs:265-294 (NormalizeTitleForDup + ManualDuplicatePredicate + FindTitleDuplicatesAsync — reuse normalizer)
    - Controllers/TrainingAdminController.cs:739-769 (SITE 4 insert hardcode + SaveChanges no try/catch + _auditLog) + :269 (Permanent⊥ValidUntil analog)
    - Helpers/CertIssuanceRules.cs (ShouldIssueCertificate, ResemblesAutoCertFormat) + Helpers/CertNumberHelper.cs (TryAssignNextSeqAsync, IsDuplicateKeyException)
    - 423-RESEARCH.md Pattern 3 (:236-255 anti-dup query template) + Pitfall 3/4 + 423-PATTERNS.md "SITE 3" + "SITE 4"
  </read_first>
  <behavior>
    - PreTest essay + GenerateCertificate=true + lulus via FinalizeEssayGrading -> NomorSertifikat null (CERT-01, SITE 3)
    - PostTest essay lulus via FinalizeEssayGrading -> 1 cert + certError null
    - Anti-dup: peserta sudah punya cert AKTIF (ValidUntil null atau >=today) judul-sama -> issue baru DIBLOK (ModelState/error), MESKIPUN ConfirmDuplicateTitle=true (CERT-05/VAL-04)
    - Anti-dup: cert lama sudah kedaluwarsa (ValidUntil < today) -> issue baru LOLOS
    - Anti-dup: sesi renewal (RenewsSessionId terisi) -> LOLOS guard (CERT-05 pengecualian)
    - Manual: GenerateCertificate tidak lagi hardcode true (derive aturan helper)
    - Manual: nomor menyerupai "KPB/005/VI/2026" -> ditolak ResemblesAutoCertFormat dengan error ramah (CERT-04)
    - Manual: kolisi unique index saat SaveChanges -> ModelState error ramah (bukan HTTP 500)
    - Manual: CertificateType "Permanent" + ValidUntil terisi -> ditolak (CERT-06)
  </behavior>
  <action>
    SITE 3 (`FinalizeEssayGrading` :3887):
    1. Ganti gate `if (session.GenerateCertificate && isPassed)` -> `if (CertIssuanceRules.ShouldIssueCertificate(session))`.
    2. Ganti loop inline (`:3894-3910`) dengan `await CertNumberHelper.TryAssignNextSeqAsync(_context, session.Id, certNow)`; derive+set ValidUntil bila certSaved (sama pola SITE 1). Pertahankan `certError` PXF-08 (:3972-3974) — itu sinyal HC; konsisten-kan kondisinya jadi `CertIssuanceRules.ShouldIssueCertificate(session) && string.IsNullOrEmpty(updatedSession?.NomorSertifikat)`.

    ANTI-DUP GUARD (CERT-05) — tambah helper private + pasang UNCONDITIONAL di create-issue path HC (di POST yang menerbitkan cert, di LUAR cabang `if (!ConfirmDuplicateTitle)`):
    3. Tambah method private di AssessmentAdminController (atau AdminBaseController bila lebih cocok di-reuse):
    ```csharp
    // CERT-05 (D-07) — ada cert AKTIF (ValidUntil null OR >=today) utk (peserta, judul-ternormalisasi)?
    // Renewal (RenewsSessionId terisi) dikecualikan. UNCONDITIONAL — tak boleh dibungkus ConfirmDuplicateTitle.
    private async Task<bool> HasActiveCertForTitleAsync(string userId, string? title, int? excludeSessionId)
    {
        var today = DateOnly.FromDateTime(DateTime.UtcNow);
        var norm = AdminBaseController.NormalizeTitleForDup(title);
        var candidates = await _context.AssessmentSessions
            .Where(s => s.UserId == userId
                     && s.NomorSertifikat != null
                     && s.IsPassed == true
                     && s.RenewsSessionId == null
                     && (excludeSessionId == null || s.Id != excludeSessionId)
                     && (s.ValidUntil == null || s.ValidUntil >= today))
            .Select(s => s.Title)
            .ToListAsync();
        return candidates.Any(t => AdminBaseController.NormalizeTitleForDup(t) == norm);
    }
    ```
    4. Pasang panggilan guard di create-issue path (HC menerbitkan cert via form) SETARA pola double-renewal `:1015-1027` — unconditional, kecuali `model.RenewsSessionId.HasValue` (renewal lolos):
    ```csharp
    if (!model.RenewsSessionId.HasValue && await HasActiveCertForTitleAsync(targetUserId, model.Title, /*excludeSessionId*/ null))
        ModelState.AddModelError("", "Peserta sudah memiliki sertifikat aktif untuk judul ini.");
    ```
    JANGAN taruh ini di dalam `if (!ConfirmDuplicateTitle)` (Pitfall 3). Soft-block judul existing tetap (itu beda concern).

    SITE 4 (`AddManualAssessment` :739-765, TrainingAdminController):
    5. Tambah validasi awal POST (sebelum insert): `if (model.CertificateType == "Permanent" && model.ValidUntil != null) ModelState.AddModelError("ValidUntil", "Sertifikat Permanent tidak boleh punya tanggal expired.");` (CERT-06, analog :269).
    6. Tambah namespace reject (sebelum insert/loop, per worker `wc.NomorSertifikat`): bila `CertIssuanceRules.ResemblesAutoCertFormat(wc.NomorSertifikat)` -> `ModelState.AddModelError("", "Nomor sertifikat manual tidak boleh menyerupai format otomatis (KPB/NNN/ROMAN/TAHUN).");` dan jangan lanjut commit (return View(model) dengan ModelState invalid).
    7. Stop hardcode (`:759`): ganti `GenerateCertificate = true,` -> derive. Manual = `IsManualEntry=true`, AssessmentType=Manual; cert dianggap diterbitkan bila HC mengisi `wc.NomorSertifikat`. Set `GenerateCertificate = !string.IsNullOrWhiteSpace(wc.NomorSertifikat)` (manual cert ada bila nomor diisi) — TIDAK hardcode true. (Manual tidak melewati ShouldIssueCertificate auto-gate karena nomor manual; tapi GenerateCertificate harus merefleksikan kenyataan, bukan selalu true.)
    8. Bungkus `await _context.SaveChangesAsync();` (`:765`) dengan try/catch friendly:
    ```csharp
    try { await _context.SaveChangesAsync(); }
    catch (DbUpdateException ex) when (CertNumberHelper.IsDuplicateKeyException(ex))
    {
        ModelState.AddModelError("", "Nomor sertifikat sudah dipakai. Gunakan nomor lain.");
        return View(model);   // bukan 500
    }
    ```
    (Tambah `using HcPortal.Helpers;` + `using Microsoft.EntityFrameworkCore;` bila belum.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -c "CertIssuanceRules.ShouldIssueCertificate\|HasActiveCertForTitleAsync" Controllers/AssessmentAdminController.cs && grep -c "ResemblesAutoCertFormat\|IsDuplicateKeyException" Controllers/TrainingAdminController.cs</automated>
  </verify>
  <done>
    `dotnet build` 0 error. `grep "CertIssuanceRules.ShouldIssueCertificate" Controllers/AssessmentAdminController.cs` >= 1; `grep "HasActiveCertForTitleAsync" Controllers/AssessmentAdminController.cs` >= 2 (definisi + panggilan); `grep "GenerateCertificate = true" Controllers/TrainingAdminController.cs` (di AddManualAssessment) == 0; `grep "ResemblesAutoCertFormat" Controllers/TrainingAdminController.cs` >= 1; `grep "IsDuplicateKeyException" Controllers/TrainingAdminController.cs` >= 1. Anti-dup guard TIDAK di dalam blok `if (!ConfirmDuplicateTitle)` (verifikasi manual penempatan). RBAC `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` di keempat endpoint TETAP utuh.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration tests real-SQL — PreTest no-cert (site nyata) + exactly-1-cert + anti-dup block/renewal-exempt/ConfirmDuplicateTitle-tak-bypass + seq-fail signal</name>
  <read_first>
    - HcPortal.Tests/RetakeThenPassCertTests.cs (full — recipe fixture + ctor + SeedSessionAsync + SeedPackageWithResponsesAsync + assert exactly-1-cert; SALIN VERBATIM)
    - HcPortal.Tests/RetakeServiceTests.cs:34-70 (RetakeServiceFixture + NoOpHubContext)
    - HcPortal.Tests/CertDedupTests.cs + CertGateAuditTests.cs (pola assert anti-dup/audit existing — JANGAN duplikasi/rusak)
    - Helpers/CertIssuanceRules.cs + Helpers/CertNumberHelper.cs (kontrak Wave 1)
    - 423-RESEARCH.md §"Integration real-SQL recipe" (:381-404) + §Validation Wave-0 Gaps
  </read_first>
  <action>
    Buat `HcPortal.Tests/CertIssuanceIntegrationTests.cs` (`[Trait("Category","Integration")]`, `IClassFixture<RetakeServiceFixture>`). Salin recipe `NewCtx`/`NewGrading`/seed helper dari `RetakeThenPassCertTests.cs` (file self-contained — duplikasi seed helper OK karena cross-class tak terjangkau). Tambah test (marker `"TAG-" + Guid.NewGuid().ToString("N").Substring(0,8)` untuk isolasi seed):
    1. **CERT-01 PreTest no-cert (site nyata):** seed session AssessmentType="PreTest", GenerateCertificate=true, paket all-correct -> `await NewGrading(ctx).GradeAndCompleteAsync(session)` -> assert graded==true TETAPI `NomorSertifikat == null` (CountAsync NomorSertifikat!=null == 0).
    2. **CERT-03 exactly-1-cert (regresi non-double):** seed PostTest lulus + GenerateCertificate=true -> grade -> `CountAsync(a => a.Id==sid && a.NomorSertifikat != null) == 1` + `Assert.Matches(@"^KPB/\d{3}/[IVX]+/\d{4}$", cert)`.
    3. **CERT-02 ValidUntil derive:** seed PostTest CertificateType="Annual" lulus -> grade -> `ValidUntil == DateOnly.FromDateTime(CompletedAt).AddYears(1)`.
    4. **CERT-05 anti-dup block + renewal exempt:** panggil `HasActiveCertForTitleAsync` (atau replikasi predikat query bila method private — gunakan query identik di test) untuk: (a) user dengan cert aktif judul-X -> true (blok); (b) user dengan cert kedaluwarsa ValidUntil<today -> false (lolos); (c) sesi RenewsSessionId terisi -> dikecualikan (predikat RenewsSessionId==null -> tidak terhitung). Bila method private, test boleh menjalankan query predikat yang sama langsung pada `ctx.AssessmentSessions` untuk membuktikan logika (D-07).
    5. **CERT-03 seq-fail signal (opsional bila feasible):** verifikasi predikat queryable: seed PostTest lulus GenerateCertificate=true TANPA NomorSertifikat (simulasi gagal) -> assert query `IsPassed==true && GenerateCertificate && AssessmentType!="PreTest" && NomorSertifikat==null` menemukan sesi (sinyal HC). (Tidak perlu memaksa kolisi nyata; cukup buktikan predikat sinyal benar.)
    JANGAN ubah/regress test cert existing.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test HcPortal.Tests --filter "FullyQualifiedName~CertIssuanceIntegrationTests" 2>&1 | tail -10</automated>
  </verify>
  <done>
    `dotnet build` 0 error; `CertIssuanceIntegrationTests` semua hijau (real-SQLEXPRESS). Mencakup: PreTest no-cert (CERT-01), exactly-1-cert format (CERT-03), ValidUntil Annual (CERT-02), anti-dup block+renewal-exempt+expired-lolos (CERT-05). Test cert existing (RetakeThenPassCertTests/CertDedupTests/CertGateAuditTests) TETAP hijau (`dotnet test HcPortal.Tests` full).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker submit / HC form -> grading/controller | Input jawaban + metadata cert; kelayakan & nomor server-authoritative |
| HC form -> AddManualAssessment POST | Free-text NomorSertifikat + CertificateType + ValidUntil dari HC (untrusted-ish, validasi server) |
| Controller -> SQL Server | ExecuteUpdateAsync seq + SaveChanges manual; filtered unique index race control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-423-05 | Spoofing | PreTest -> cert palsu via 4 site | mitigate | `CertIssuanceRules.ShouldIssueCertificate` server-side di SEMUA 4 site (CERT-01); integration test verifikasi NomorSertifikat==null di site nyata. |
| T-423-06 | Elevation/Tampering | Anti-dup bypass via ConfirmDuplicateTitle | mitigate | `HasActiveCertForTitleAsync` UNCONDITIONAL di luar cabang `!ConfirmDuplicateTitle` (Pitfall 3); renewal exempt via RenewsSessionId==null. |
| T-423-07 | Tampering | Manual nomor tabrak namespace auto | mitigate | `ResemblesAutoCertFormat` reject + try/catch `IsDuplicateKeyException` -> ModelState ramah (bukan 500). |
| T-423-08 | Tampering/DoS-soft | Seq race burst finalize | mitigate | `TryAssignNextSeqAsync` (filtered WHERE + jitter + cap 8); non-destruktif fallback (UpdatedAt + predikat sinyal), worker tak terblok. |
| T-423-09 | Elevation | RBAC/antiforgery hilang saat refactor | mitigate | `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` di FinalizeEssayGrading/AddManualAssessment WAJIB utuh — verifikasi di done. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests` full (incl Integration real-SQLEXPRESS) hijau; `--filter "Category!=Integration"` hijau.
- Keempat site: `grep` membuktikan gate via `ShouldIssueCertificate`; `maxCertAttempts` == 0 di GradingService; `GenerateCertificate = true` hardcode di AddManualAssessment == 0.
- Anti-dup guard unconditional (bukan di dalam `!ConfirmDuplicateTitle`); renewal exempt.
- Test cert existing tak regresi (RetakeThenPassCertTests/CertDedupTests/CertGateAuditTests/CertificateStatusTests/CertAlertConsistencyTests).
- RBAC + antiforgery di endpoint cert utuh.
</verification>

<success_criteria>
- CERT-01: keempat site memutuskan via `ShouldIssueCertificate`; PreTest no-cert terbukti integration.
- CERT-03: 3 site grading-time pakai `TryAssignNextSeqAsync`; seq-fail non-destruktif + sinyal queryable.
- CERT-04: manual namespace reject + try/catch friendly (bukan 500).
- CERT-05: anti-dup guard server-side unconditional, renewal exempt, tak bisa di-bypass ConfirmDuplicateTitle.
- Build 0 error; full suite hijau; no regresi cert tests.
</success_criteria>

<output>
After completion, create `.planning/phases/423-certificate-issuance-consistency/423-02-SUMMARY.md`.
Catat: (1) penempatan persis anti-dup guard (file:method) untuk audit secure-phase; (2) keputusan GenerateCertificate manual (derive dari nomor diisi, bukan hardcode); (3) konfirmasi RBAC/antiforgery utuh; (4) migration=FALSE.
</output>
