---
phase: 423-certificate-issuance-consistency
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - Views/Admin/EssayGrading.cshtml
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - tests/e2e/cert-pending-age-badge.spec.ts
autonomous: false
migration: false
requirements: [CERT-07]
must_haves:
  truths:
    - "Halaman EssayGrading menampilkan badge umur 'Menunggu N hari' untuk sesi PendingGrading (belum finalized)"
    - "AssessmentMonitoringDetail menampilkan badge umur di baris sesi/essay-pending ber-status 'Menunggu Penilaian'"
    - "Warna badge dari CertIssuanceRules.PendingAgeBadgeClass: >3 hari kuning (bg-warning text-dark), >7 hari merah (bg-danger)"
    - "TIDAK ada auto-finalize — badge hanya penanda visibilitas, HC tetap finalize manual"
    - "Umur dihitung dari CompletedAt (timestamp 'menunggu sejak' yang sudah di-set saat status->PendingGrading di GradingService:224)"
  artifacts:
    - path: "Views/Admin/EssayGrading.cshtml"
      provides: "Badge umur PendingGrading di header (transplant idiom badge)"
      contains: "PendingAgeBadgeClass"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Badge umur PendingGrading di list (status + essay-pending row)"
      contains: "PendingAgeBadgeClass"
    - path: "tests/e2e/cert-pending-age-badge.spec.ts"
      provides: "Playwright smoke — badge umur render di 2 view, warna sesuai ambang"
      contains: "Menunggu"
  key_links:
    - from: "Views/Admin/EssayGrading.cshtml"
      to: "CertIssuanceRules.PendingAgeBadgeClass(Model.CompletedAt, DateTime.UtcNow)"
      via: "@using HcPortal.Helpers + render badge"
      pattern: "PendingAgeBadgeClass"
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      to: "CertIssuanceRules.PendingAgeBadgeClass(session.CompletedAt, DateTime.UtcNow)"
      via: "render badge saat UserStatus/Status == Menunggu Penilaian"
      pattern: "PendingAgeBadgeClass"
---

<objective>
Wave 3: tampilkan umur sesi "Menunggu Penilaian" (PendingGrading) di DUA tempat HC bekerja — halaman EssayGrading + daftar AssessmentMonitoringDetail — dengan badge warna ber-ambang (>3 kuning, >7 merah) TANPA auto-finalize. Ini menutup CERT-07 (GRD-05: sesi pending menggantung tanpa visibilitas).

Purpose: Sesi ber-essay yang masuk PendingGrading bisa menggantung lama menunggu HC finalize (GRD-05). Tidak ada visibilitas umur -> HC tak tahu mana yang terlalu lama. Plan ini menambah penanda umur (data sudah ada: `CompletedAt` di-set saat ->PendingGrading di GradingService:224; sudah ter-expose di `EssayGradingPageViewModel.CompletedAt` & `MonitoringSessionViewModel.CompletedAt`) memakai pure helper `PendingAgeBadgeClass` dari Wave 1. Murni presentasi — TIDAK ada write/auto-finalize (D-08).
Output: badge umur di `EssayGrading.cshtml` (header) + `AssessmentMonitoringDetail.cshtml` (list, transplant idiom badge in-file) + Playwright smoke + checkpoint UAT @5270.

migration=FALSE — view-only, tidak ada schema/write. autonomous=false (ada checkpoint UAT browser).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/423-certificate-issuance-consistency/423-CONTEXT.md
@.planning/phases/423-certificate-issuance-consistency/423-RESEARCH.md
@.planning/phases/423-certificate-issuance-consistency/423-PATTERNS.md
@.planning/phases/423-certificate-issuance-consistency/423-01-SUMMARY.md

<interfaces>
<!-- Kontrak Wave 1 + source view aktual (VERIFIED 2026-06-24). -->

DARI WAVE 1 (Helpers/CertIssuanceRules.cs — pure):
```csharp
public static string PendingAgeBadgeClass(DateTime? completedAtUtc, DateTime nowUtc);  // >7 bg-danger, >3 bg-warning text-dark, else bg-secondary
```

VIEW MODELS (VERIFIED Models/AssessmentMonitoringViewModel.cs):
- EssayGradingPageViewModel.CompletedAt = DateTime? (:99); .EssayPendingCount; .IsFinalized; .SessionId
- MonitoringSessionViewModel.CompletedAt = DateTime? (:56); .UserStatus (:53, remap label); .Status (:66, raw mirror AssessmentSession.Status); per-essay row punya s.CompletedAt + s.Status + s.EssayPendingCount

EssayGrading.cshtml — EXISTING idiom konsumsi CompletedAt (VERIFIED :100-104):
```cshtml
var tooltipText = Model.CompletedAt.HasValue
    ? $"Sudah selesai pada {Model.CompletedAt.Value:dd MMM yyyy HH:mm} WIB"
    : "Penilaian sudah diselesaikan";
```
> Badge umur dirender saat sesi MASIH pending (Model.EssayPendingCount > 0 / belum IsFinalized), bukan saat sudah selesai.

AssessmentMonitoringDetail.cshtml — EXISTING badge idiom (transplant). Status badge switch (VERIFIED :243-256):
```cshtml
var statusClass = session.UserStatus switch
{
    "Completed"          => "bg-success",
    "Menunggu Penilaian" => "bg-warning text-dark",
    _                    => "bg-light text-dark border"
};
```
Essay-pending row badge (VERIFIED :426-441):
```cshtml
var isFinalized = s.Status == AssessmentConstants.AssessmentStatus.Completed && ...;
@if (s.EssayPendingCount > 0) { <span class="badge bg-warning text-dark">@s.EssayPendingCount belum dinilai</span> }
else if (!isFinalized) { <span class="badge bg-info">Siap difinalisasi</span> }
else { <span class="badge bg-success">Selesai</span> }
```
PendingGrading status literal = AssessmentConstants.AssessmentStatus.PendingGrading = "Menunggu Penilaian" (VERIFIED AssessmentConstants.cs:18).

Playwright: branch ITHandoff app @ http://localhost:5270 (CLAUDE.md / reference_ithandoff_test_port_5270). Admin login admin@pertamina.com (reference_dev_credentials). Existing e2e di tests/e2e/ — ikuti pola spec existing (baseURL 5270, --workers=1 per reference_local_e2e_sql_env_fix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge umur PendingGrading di EssayGrading.cshtml + AssessmentMonitoringDetail.cshtml</name>
  <read_first>
    - Views/Admin/EssayGrading.cshtml:1-110 (top: @model + @using; :95-110 blok CompletedAt/IsFinalized/EssayPendingCount — sisipkan badge dekat header sesi)
    - Views/Admin/AssessmentMonitoringDetail.cshtml:1-30 (@using header) + :243-280 (status badge per-session) + :420-446 (essay-pending row)
    - Helpers/CertIssuanceRules.cs (Wave 1 — PendingAgeBadgeClass signature + ambang)
    - Models/AssessmentMonitoringViewModel.cs:48-104 (field yang tersedia di kedua VM)
    - 423-PATTERNS.md "EssayGrading.cshtml" + "AssessmentMonitoringDetail.cshtml" (excerpt + delta verbatim) + 423-RESEARCH.md §"CERT-07 badge di view" (:406-417)
  </read_first>
  <action>
    Pastikan kedua view punya `@using HcPortal.Helpers` (tambah di top bila belum) supaya `CertIssuanceRules.PendingAgeBadgeClass(...)` terjangkau.

    A) `Views/Admin/EssayGrading.cshtml` (CERT-07, halaman kerja HC per-sesi):
    Sisipkan badge umur dekat header/judul sesi, DIRENDER hanya saat sesi masih pending (belum finalized — yaitu `Model.EssayPendingCount > 0` atau `!Model.IsFinalized`) dan `Model.CompletedAt.HasValue`:
    ```cshtml
    @if (Model.CompletedAt.HasValue && !Model.IsFinalized)
    {
        var pendingDays = (int)(DateTime.UtcNow - Model.CompletedAt.Value).TotalDays;
        var pendingCls = CertIssuanceRules.PendingAgeBadgeClass(Model.CompletedAt, DateTime.UtcNow);
        <span class="badge @pendingCls ms-2" title="Menunggu penilaian sejak @Model.CompletedAt.Value.ToString("dd MMM yyyy HH:mm") WIB">
            <i class="bi bi-hourglass-split me-1"></i>Menunggu @pendingDays hari
        </span>
    }
    ```
    Tempatkan dekat NIP/judul (sekitar :40-42 area `<p class="text-muted">NIP ...`) atau di blok finalizeSection header — pilih lokasi yang terlihat HC saat membuka halaman. JANGAN ubah logika finalize/tooltip existing.

    B) `Views/Admin/AssessmentMonitoringDetail.cshtml` (CERT-07, daftar HC):
    Di baris per-sesi (dekat status badge `:280` `<td class="status-cell">`), tambahkan badge umur HANYA saat status pending. Pakai `session.Status` (raw) atau `session.UserStatus == "Menunggu Penilaian"`:
    ```cshtml
    @if (session.UserStatus == "Menunggu Penilaian" && session.CompletedAt.HasValue)
    {
        var pCls = CertIssuanceRules.PendingAgeBadgeClass(session.CompletedAt, DateTime.UtcNow);
        var pDays = (int)(DateTime.UtcNow - session.CompletedAt.Value).TotalDays;
        <span class="badge @pCls ms-1">Menunggu @pDays hari</span>
    }
    ```
    Juga di essay-pending row (`:435-441`), tambah badge umur berdampingan dengan "@s.EssayPendingCount belum dinilai" saat `s.Status == AssessmentConstants.AssessmentStatus.PendingGrading && s.CompletedAt.HasValue`:
    ```cshtml
    @if (s.Status == AssessmentConstants.AssessmentStatus.PendingGrading && s.CompletedAt.HasValue)
    {
        <span class="badge @CertIssuanceRules.PendingAgeBadgeClass(s.CompletedAt, DateTime.UtcNow) ms-1">Menunggu @((int)(DateTime.UtcNow - s.CompletedAt.Value).TotalDays) hari</span>
    }
    ```
    KRITIS: NO auto-finalize, NO write — murni render (D-08). Jangan ubah JS DataTable column logic (:1401-1414) — badge ini server-rendered Razor.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -c "PendingAgeBadgeClass" Views/Admin/EssayGrading.cshtml && grep -c "PendingAgeBadgeClass" Views/Admin/AssessmentMonitoringDetail.cshtml</automated>
  </verify>
  <done>
    `dotnet build` 0 error (Razor compile). `grep "PendingAgeBadgeClass" Views/Admin/EssayGrading.cshtml` >= 1; `grep "PendingAgeBadgeClass" Views/Admin/AssessmentMonitoringDetail.cshtml` >= 1; `grep "@using HcPortal.Helpers" Views/Admin/EssayGrading.cshtml` >= 1 (atau already-imported via _ViewImports — verifikasi terjangkau). Tidak ada perubahan di luar render (no controller/service edit di plan ini).
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright smoke — badge umur PendingGrading render di 2 view</name>
  <read_first>
    - tests/e2e/ (spec existing terdekat — pola login admin + baseURL 5270 + selector badge; salin struktur)
    - reference_ithandoff_test_port_5270 + reference_dev_credentials + reference_local_e2e_sql_env_fix (--workers=1, SQL env)
    - Views/Admin/EssayGrading.cshtml + AssessmentMonitoringDetail.cshtml (markup badge dari Task 1 — selector `text=Menunggu`)
  </read_first>
  <action>
    Buat `tests/e2e/cert-pending-age-badge.spec.ts` (ikuti pola spec e2e existing: baseURL http://localhost:5270, login admin@pertamina.com). Smoke (tidak harus seed kompleks — boleh memakai data PendingGrading existing bila ada, atau skip-gracefully bila tak ada):
    - Login admin -> buka AssessmentMonitoringDetail untuk grup yang punya sesi "Menunggu Penilaian" -> assert badge teks `Menunggu` + kelas warna ada (locator `.badge:has-text("Menunggu")` visible).
    - Buka EssayGrading untuk sesi pending -> assert badge `Menunggu N hari` visible saat belum finalized.
    - Assert TIDAK ada auto-finalize: status sesi tetap "Menunggu Penilaian" setelah load (tidak berubah jadi Completed).
    Jalankan dengan `--workers=1`. Bila tidak ada data PendingGrading di DB lokal, test boleh `test.skip` dengan pesan jelas (UAT manual Task 3 yang autoritative untuk seed terkontrol).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && ls tests/e2e/cert-pending-age-badge.spec.ts && npx playwright test tests/e2e/cert-pending-age-badge.spec.ts --workers=1 2>&1 | tail -12</automated>
  </verify>
  <done>
    Spec file ada; `npx playwright test` lulus atau skip-gracefully (bukan error/fail). Badge `Menunggu` ter-assert visible saat data pending tersedia. Status tidak berubah (no auto-finalize).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint UAT @5270 — badge umur PendingGrading di 2 view (SEED_WORKFLOW snapshot->seed->test->restore)</name>
  <what-built>
    Badge umur "Menunggu N hari" untuk sesi PendingGrading di halaman EssayGrading + daftar AssessmentMonitoringDetail, warna ber-ambang (>3 kuning, >7 merah) via CertIssuanceRules.PendingAgeBadgeClass. TANPA auto-finalize (CERT-07/D-08/D-09).
  </what-built>
  <files>Views/Admin/EssayGrading.cshtml, Views/Admin/AssessmentMonitoringDetail.cshtml, docs/SEED_JOURNAL.md</files>
  <action>
    Persiapan checkpoint (yang Claude lakukan sebelum menyerahkan ke HC): `dotnet build` 0 error, `dotnet run --urls http://localhost:5270` (branch ITHandoff, JANGAN commit launchSettings). Ikuti SEED_WORKFLOW (CLAUDE.md): snapshot DB lokal DULU + catat docs/SEED_JOURNAL.md (klasifikasi temporary+local-only). Seed 2-3 sesi ber-essay status "Menunggu Penilaian" dengan CompletedAt berbeda (2 hari=abu, 5 hari=kuning, 8 hari=merah). Setelah verifikasi manusia selesai, RESTORE DB dari snapshot + tandai SEED_JOURNAL `cleaned`. Lalu serahkan langkah verifikasi visual ke HC (lihat how-to-verify).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3</automated>
  </verify>
  <done>HC mengetik "approved" setelah badge umur + warna ambang benar di 2 view dan tidak ada auto-finalize; DB lokal di-restore (SEED_JOURNAL cleaned).</done>
  <how-to-verify>
    Ikuti SEED_WORKFLOW (CLAUDE.md): snapshot DB lokal DULU, catat di docs/SEED_JOURNAL.md (klasifikasi temporary+local-only), seed, test, RESTORE setelah selesai.
    1. Jalankan app branch ITHandoff: `dotnet run --urls http://localhost:5270` (JANGAN commit launchSettings).
    2. Seed (temporary, local-only) 2-3 sesi ber-essay status "Menunggu Penilaian" dengan CompletedAt berbeda: 1 sesi CompletedAt 2 hari lalu (badge abu/secondary), 1 sesi 5 hari lalu (kuning), 1 sesi 8 hari lalu (merah). Catat di SEED_JOURNAL.md.
    3. Login admin (admin@pertamina.com) -> buka AssessmentMonitoringDetail grup terkait. VERIFIKASI: tiap sesi PendingGrading menampilkan "Menunggu N hari"; sesi 5-hari = kuning (bg-warning), 8-hari = merah (bg-danger), 2-hari = abu (bg-secondary).
    4. Klik ke EssayGrading salah satu sesi pending -> VERIFIKASI badge "Menunggu N hari" tampil di header (belum finalized).
    5. VERIFIKASI TIDAK ada auto-finalize: refresh -> status tetap "Menunggu Penilaian", sesi TIDAK berubah jadi Completed otomatis.
    6. (Opsional sanity Wave 2) Finalize satu sesi essay lulus PostTest -> cert terbit 1× format KPB/NNN/ROMAN/TAHUN; finalize sesi PreTest -> TIDAK ada cert.
    7. RESTORE DB lokal dari snapshot (CLAUDE.md SEED_WORKFLOW); tandai SEED_JOURNAL.md `cleaned`.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila badge umur + warna ambang benar di 2 view dan tidak ada auto-finalize; atau jelaskan masalah.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC browser -> view render | Badge umur server-rendered Razor (read-only); tidak ada input/write dari view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-423-10 | Information Disclosure | Badge umur PendingGrading | accept | Hanya menampilkan jumlah hari menunggu (CompletedAt) ke HC yang sudah ber-RBAC akses halaman; tidak ekspos data sensitif baru. Low-value. |
| T-423-11 | Tampering | Auto-finalize tak sengaja | mitigate | Plan view-only — NO write/auto-finalize (D-08); verifikasi di UAT status tetap PendingGrading. |
| T-423-12 | Elevation | Akses halaman EssayGrading/Monitoring | mitigate | Halaman sudah `[Authorize(Roles="Admin, HC")]` existing — tidak diubah; badge tak menambah surface. |
</threat_model>

<verification>
- `dotnet build` 0 error (Razor compile kedua view).
- `grep "PendingAgeBadgeClass"` >= 1 di kedua view.
- Playwright spec lulus/skip-gracefully (badge visible saat data pending).
- UAT @5270: 3 sesi seed -> badge umur + warna ambang benar (abu/kuning/merah); no auto-finalize; (sanity) cert PostTest 1× + PreTest no-cert.
- DB lokal di-restore pasca-UAT (SEED_JOURNAL.md cleaned).
</verification>

<success_criteria>
- CERT-07: badge umur PendingGrading tampil di EssayGrading + AssessmentMonitoringDetail, warna >3 kuning / >7 merah, TANPA auto-finalize.
- Build 0 error; Playwright smoke hijau/skip-gracefully; UAT approved.
- View-only (no schema/write); DB lokal pristine pasca-test.
</success_criteria>

<output>
After completion, create `.planning/phases/423-certificate-issuance-consistency/423-03-SUMMARY.md`.
Catat: (1) lokasi render badge final (file:line) untuk verify; (2) hasil UAT @5270 (3 ambang + no auto-finalize); (3) konfirmasi DB restore + SEED_JOURNAL cleaned; (4) migration=FALSE.
Phase 423 complete -> notify IT migration=FALSE (bundle v32.7).
</output>
