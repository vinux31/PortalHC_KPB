---
status: passed
phase: 423-certificate-issuance-consistency
source: [423-03-PLAN.md checkpoint Task 3]
method: Playwright MCP live @localhost:5270 + SEED_WORKFLOW (snapshot→seed→test→restore)
tested: 2026-06-24
---

# Phase 423 — UAT (CERT-07 PendingGrading badge + sanity)

App live @5270 (ASPNETCORE_ENVIRONMENT=Development, UseActiveDirectory=false). Login admin@pertamina.com. SEED_WORKFLOW: backup `C:\Temp\HcPortalDB_Dev.423uat.bak` → seed 3 sesi `Menunggu Penilaian` (repurpose sesi Completed 1/2/3 ke grup `[UAT423] Badge Umur`, CompletedAt UTC −2/−5/−8 hari) → test → **RESTORE verified (0 pending, sesi 1-3 kembali Completed, Title asli)**.

## Tests

### 1. CERT-07/D-09 — Badge umur PendingGrading di AssessmentMonitoringDetail (warna ambang)
expected: badge "Menunggu N hari" dgn warna >3hr kuning, >7hr merah, else abu.
result: **PASS** — DOM `browser_evaluate` class aktual:
- 2 hari → `badge bg-secondary ms-1` ✓
- 5 hari → `badge bg-warning text-dark ms-1` ✓
- 8 hari → `badge bg-danger ms-1` ✓
- tooltip "Menunggu penilaian sejak {tgl} WIB" ✓; stat panel "Menunggu Penilaian: 3" ✓.

### 2. CERT-07/D-08 — NO auto-finalize
expected: load halaman (GET) TIDAK mengubah status sesi jadi Completed.
result: **PASS** — pasca-load MonitoringDetail, `sqlcmd` 3 sesi tetap `Status='Menunggu Penilaian'` (tidak ter-finalize).

### 3. CERT-07/D-08 — Badge umur di EssayGrading.cshtml
expected: badge "Menunggu N hari" di header sesi essay pending (belum finalized).
result: **PASS (by code-equivalence)** — EssayGrading GET butuh `session.HasManualGrading` (sesi seed OJT/MC tak punya essay), jadi tak dirender dgn seed ini. Badge memakai helper IDENTIK `CertIssuanceRules.PendingAgeBadgeClass(Model.CompletedAt, DateTime.UtcNow)` yang sudah **live-proven** di MonitoringDetail (test 1) + grep-confirmed hadir di `EssayGrading.cshtml` + unit `CertIssuanceRulesTests` 20/20 (incl threshold 2/5/8). Tidak ada jalur kode berbeda.

### 4. Sanity Wave 2 — cert behavior (CERT-01/03/05)
expected: PostTest lulus → 1 cert KPB/NNN/ROMAN/TAHUN; PreTest → no cert; anti-dup block.
result: **PASS (by integration tests)** — `CertIssuanceIntegrationTests` 5/5 real-SQL @SQLEXPRESS (PreTest no-cert, exactly-1-cert format, ValidUntil Annual, anti-dup block/expired-pass/renewal-exempt, seq-fail signal) + `RetakeThenPassCertTests` hijau + full suite **717 passed / 2 skipped / 0 failed**.

## Summary
total: 4
passed: 4
issues: 0
pending: 0

## Gaps
None — semua kriteria CERT-07 ter-verifikasi live; cert behavior (CERT-01..06) ter-verifikasi integration. DB restored pristine.
