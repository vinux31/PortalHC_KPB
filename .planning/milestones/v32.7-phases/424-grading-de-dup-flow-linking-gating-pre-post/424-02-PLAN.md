---
phase: 424-grading-de-dup-flow-linking-gating-pre-post
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Controllers/CMPController.cs
  - HcPortal.Tests/PrePostGatingTests.cs
  - HcPortal.Tests/EnsureCanSubmitStandardTests.cs
autonomous: true
migration: false
requirements: [GRDF-01, GRDF-03, GRDF-05, GRDF-07]

must_haves:
  truths:
    - "Peserta worker tak dapat StartExam Post-Test bila pasangan Pre-nya ADA tapi belum Completed (redirect + pesan ramah)"
    - "Post tanpa pasangan Pre (orphan/Standard) atau Pre milik user lain → LEWAT (tak diblok)"
    - "Pairing display memakai FindPairedPreAsync terfilter UserId (tak pasangkan Pre pekerja lain)"
    - "ElapsedSeconds clamp di UpdateSessionProgress memperhitungkan ExtraTimeMinutes (tak over-clamp)"
    - "Submit on-time dengan ≥1 essay kosong → blokir SELURUH submit + pesan ramah; timeout tetap finalize PendingGrading"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Gate GRDF-01 di StartExam (~:941) + pairing GRDF-03 + clamp GRDF-05 (:469) + essay-empty GRDF-07 (:1630-1656)"
      contains: "FindPairedPreAsync"
    - path: "HcPortal.Tests/PrePostGatingTests.cs"
      provides: "real-SQL gate test: block Pre-not-Completed, pass orphan/Standard/user-lain"
      contains: "PrePostGating"
  key_links:
    - from: "Controllers/CMPController.cs StartExam"
      to: "PrePostPairing.FindPairedPreAsync"
      via: "gate setelah Completed check, sebelum token gate"
      pattern: "FindPairedPreAsync"
    - from: "Controllers/CMPController.cs UpdateSessionProgress clamp :469"
      to: "ExamTimeRules.AllowedExamSeconds"
      via: "Math.Min(clampedElapsed, AllowedExamSeconds(...))"
      pattern: "ExamTimeRules\\.AllowedExamSeconds"
    - from: "Controllers/CMPController.cs SubmitExam :1640-1648"
      to: "essay TextAnswer non-kosong"
      via: "join QuestionType==Essay + !IsNullOrWhiteSpace(TextAnswer)"
      pattern: "IsNullOrWhiteSpace"
---

<objective>
Konsumsi dua helper dari Plan 01 untuk mengunci empat perilaku server-authoritative di `CMPController.cs`: (1) gate gating Pre→Post di StartExam (GRDF-01, HIGH/FLOW-04), (2) pairing satu sumber terfilter UserId di display (GRDF-03/FLOW-01), (3) clamp ElapsedSeconds dengan ExtraTime (GRDF-05/FLOW-02), (4) tolak essay kosong on-time server-side (GRDF-07/VAL-03).

Purpose: `CMPController.cs` adalah file besar yang disentuh keempat REQ — disatukan dalam SATU plan agar tak ada edit paralel pada file sama dalam fase (hindari konflik intra-phase). Plan 01 (helper) menjadi dependency.

Output: gate + pairing + clamp + essay-validasi terpasang; real-SQL gate test + essay test hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-CONTEXT.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-RESEARCH.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-PATTERNS.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-VALIDATION.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-01-SUMMARY.md

<interfaces>
<!-- Kontrak dari Plan 01 (Helpers/) + existing CMPController sites — pakai langsung. -->

Helpers/PrePostPairing.cs (DARI PLAN 01):
- static Task<AssessmentSession?> FindPairedPreAsync(ApplicationDbContext ctx, AssessmentSession post)
  → return null untuk Standard/Pre/orphan (pass-through D-02); paired Pre terfilter UserId untuk PostTest ber-link.

Helpers/ExamTimeRules.cs (DARI PLAN 01):
- static int AllowedExamSeconds(int durationMinutes, int? extraTimeMinutes) => (d + (e ?? 0)) * 60

CMPController.cs anchors (VERIFIED sesi ini — pakai INI):
- StartExam(int id) :904; owner-check :914 (PERTAHANKAN, V4); Completed check :936-940; token gate :945-953; insertion gate GRDF-01 = SETELAH :940, SEBELUM :942.
  Pola gate existing (window) :955-960: `TempData["Error"]=...; return RedirectToAction("Assessment");`
- UpdateSessionProgress clamp :468-469: `clampedElapsed = Math.Min(clampedElapsed, session.DurationMinutes * 60);` ❌ no ExtraTime
- pairing display smell :292-297: query `completedPreSessions` filter `AssessmentType=="PreTest" && LinkedGroupId.HasValue && postGroupIds.Contains(...) && Status=="Completed"` — TANPA `s.UserId == userId` ❌ (FLOW-01 root)
- SubmitExam :1576; serverTimerExpired :1622-1628; on-time incomplete gate :1630-1656; dbResponses :1640-1644 `Select(r => r.PackageQuestionId)` (tanpa TextAnswer/QuestionType ❌); blok :1649-1654 `TempData["Error"]=...; return RedirectToAction("ExamSummary", new { id });`
- timeout path (serverTimerExpired==true) → fall-through finalize → PendingGrading (D-04 PXF-04 — JANGAN sentuh)
- ShouldEnforceSubmitTimer(...) = pure static helper existing (pola ekstraksi untuk test, lihat EnsureCanSubmitStandardTests.cs:21-25)

Test fixture: HcPortal.Tests/GradingDedupeTests.cs `GradingDedupeFixture` (:29-83) disposable real-SQL HcPortalDB_Test_{guid} + MigrateAsync + drop-on-dispose, `[Trait("Category","Integration")]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pairing satu sumber terfilter UserId (GRDF-03) + gate gating Pre→Post di StartExam (GRDF-01)</name>
  <files>Controllers/CMPController.cs, HcPortal.Tests/PrePostGatingTests.cs</files>
  <read_first>
    - Controllers/CMPController.cs:257-304 (pairing display + smell :292-297 NO UserId) + :904-993 (StartExam gate chain — insertion ~:941)
    - Helpers/PrePostPairing.cs (FindPairedPreAsync dari Plan 01)
    - HcPortal.Tests/GradingDedupeTests.cs:29-83 (fixture recipe untuk PrePostGatingTests)
    - 424-RESEARCH.md §"Pattern 1"/"Pattern 2"/Pitfall 4/Pitfall 5 + Open Question #1 (gate worker-only) + 424-PATTERNS.md §1/§4
  </read_first>
  <behavior>
    - Gate GRDF-01: Post (`AssessmentType=="PostTest"`) ber-pasangan Pre `Status != "Completed"` + worker (`assessment.UserId == user.Id`) → StartExam redirect "Assessment" + `TempData["Error"]="Selesaikan Pre-Test dulu sebelum mulai Post-Test."`.
    - Pre `== "Completed"` → lewat (no block).
    - Orphan (no LinkedSessionId/LinkedGroupId) → FindPairedPreAsync==null → pass-through (D-02).
    - Standard (`AssessmentType!="PostTest"`) → pass-through.
    - Pre milik USER LAIN (UserId beda) → FindPairedPreAsync==null (filter UserId) → pass-through.
    - Admin/HC (`assessment.UserId != user.Id`) → bypass gate (mirror token gate :945 — Open Question #1 recommendation: worker-only).
    - Pairing display :292-297 → terfilter UserId (tak pasangkan Pre pekerja lain).
  </behavior>
  <action>
    GRDF-03 — di pairing display `CMPController.cs:292-297`, TAMBAH `&& s.UserId == userId` pada query `completedPreSessions` (fix FLOW-01 root: cross-user pairing). Pastikan `userId` di scope = peserta yang sedang dilihat. (Konvergensi penuh ke `FindPairedPreAsync` opsional bila bentuk query memungkinkan; minimal WAJIB tambah filter UserId di SEMUA cabang pairing yang tak terfilter.) Jangan ubah :3505-3523 (sudah filter UserId) & :2404-2413 (LinkedSessionId).

    GRDF-01 — sisipkan gate di `StartExam` SETELAH cek `Completed` (`:940`), SEBELUM token gate (`:942/:945`). Pola gate existing (TempData+Redirect). Worker-only (mirror token gate :945):
    ```csharp
    // GRDF-01 (FLOW-04, keputusan bisnis a): Post tak boleh mulai bila pasangan Pre ADA tapi belum Completed.
    // Worker-only — Admin/HC bypass (monitoring/impersonate), konsisten token gate :945.
    if (assessment.UserId == user.Id)
    {
        var pairedPre = await PrePostPairing.FindPairedPreAsync(_context, assessment);
        if (pairedPre != null && pairedPre.Status != "Completed")   // D-01: Completed saja, BUKAN IsPassed
        {
            TempData["Error"] = "Selesaikan Pre-Test dulu sebelum mulai Post-Test.";
            return RedirectToAction("Assessment");
        }
        // pairedPre == null (orphan/Standard) → lewat (D-02 non-destruktif).
    }
    ```
    PENEMPATAN KRITIS: setelah Completed (`:936`) agar reload Post selesai tak ke-gate; sebelum `StartedAt` write (`:977`) agar sesi terblok tak write-on-GET. Owner-check :914 tetap di ATAS gate (V4 access control, JANGAN pindah/hapus). JANGAN cek `IsPassed` (langgar D-01). JANGAN pakai pola judul (langgar D-02).

    Buat `HcPortal.Tests/PrePostGatingTests.cs` — real-SQL `IClassFixture<GradingDedupeFixture>`, `[Trait("Category","Integration")]`. Test `FindPairedPreAsync` + skenario gate (seed via ctx):
    - Post→Pre(InProgress) link → FindPairedPreAsync != null & != Completed (gate akan block).
    - Post→Pre(Completed) → pass.
    - Orphan (no link) → null → pass-through.
    - Standard type → null → pass-through.
    - Pre milik UserId LAIN → null → pass-through (filter UserId).
    - LinkedSessionId diprioritaskan di atas LinkedGroupId.
    (Gate redirect penuh di-UAT browser Plan 03; test ini mengunci helper + keputusan block/pass.)
  </action>
  <acceptance_criteria>
    - `grep "FindPairedPreAsync" Controllers/CMPController.cs` → ada di StartExam.
    - `grep -n "pairedPre.Status != \"Completed\"" Controllers/CMPController.cs` → ada; TIDAK ada `IsPassed` pada blok gate baru.
    - Gate berada SETELAH baris `assessment.Status == "Completed"` block dan SEBELUM token gate (`assessment.IsTokenRequired`) — verifikasi urutan.
    - `grep -n "s.UserId == userId" Controllers/CMPController.cs` mencakup query pairing :292-297 (filter UserId ditambahkan).
    - Owner-check `assessment.UserId != user.Id ... return Forbid()` (:914) masih ada (tak terhapus).
    - `dotnet build` exits 0.
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~PrePostGating"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~PrePostGating"</automated>
  </verify>
  <done>Gate Pre→Post worker-only terpasang setelah Completed/sebelum token; pairing display terfilter UserId; orphan/Standard/user-lain pass-through; real-SQL gate test hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clamp ElapsedSeconds dengan ExtraTime (GRDF-05) + tolak essay kosong on-time server-side (GRDF-07)</name>
  <files>Controllers/CMPController.cs, HcPortal.Tests/EnsureCanSubmitStandardTests.cs</files>
  <read_first>
    - Controllers/CMPController.cs:455-484 (UpdateSessionProgress clamp :468-469) + :1576-1665 (SubmitExam serverTimerExpired :1622-1628 + on-time incomplete gate :1630-1656)
    - Helpers/ExamTimeRules.cs (AllowedExamSeconds dari Plan 01)
    - HcPortal.Tests/EnsureCanSubmitStandardTests.cs:17-33 (pola pure-static-helper test) + HcPortal.Tests/EssayEmptyPendingParityTests.cs (DO-NOT-REGRESS — timeout+empty essay tetap finalize)
    - 424-RESEARCH.md §"Pattern 4"/Pitfall 6/Pitfall 7/Pitfall 8 + Open Question #2 + 424-PATTERNS.md §3/§5
  </read_first>
  <behavior>
    - UpdateSessionProgress clamp: ElapsedSeconds di-cap pada `AllowedExamSeconds(DurationMinutes, ExtraTimeMinutes)` BUKAN `DurationMinutes*60` — sesi ber-ExtraTime tak over-clamp (export "Durasi Aktual" jadi benar tanpa ubah math export).
    - Essay on-time: `!serverTimerExpired` + ≥1 soal essay dgn `IsNullOrWhiteSpace(TextAnswer)` → blok SELURUH submit + `TempData["Error"]` pesan ramah → redirect ExamSummary (D-05).
    - Essay on-time semua terisi → lolos (no block).
    - Timeout (`serverTimerExpired==true`) + essay kosong → fall-through finalize → PendingGrading (D-04, PXF-04 — JANGAN ubah).
    - Pesan essay-rejection TIDAK membocorkan kunci/jawaban-benar (V5).
  </behavior>
  <action>
    GRDF-05 — `CMPController.cs:469`, GANTI:
    `clampedElapsed = Math.Min(clampedElapsed, session.DurationMinutes * 60);`
    MENJADI:
    `clampedElapsed = Math.Min(clampedElapsed, ExamTimeRules.AllowedExamSeconds(session.DurationMinutes, session.ExtraTimeMinutes));`
    Export "Durasi Aktual" (`AssessmentAdminController.cs:4929-4931`, `ElapsedSeconds/60`) TIDAK diubah di sini (benar otomatis setelah clamp fix — root cause = write-side clamp, Pitfall 8; konfirmasi math export di Plan 03).

    GRDF-07 — di cabang on-time `!serverTimerExpired` (`:1630-1656`), perketat hitung "terjawab" untuk soal Essay agar berbasis ISI bukan baris-ada (Pitfall 7 + Open Question #2):
    - Perluas projeksi `dbResponses` (`:1640-1644`) agar ambil `{ PackageQuestionId, TextAnswer }` (bukan hanya `PackageQuestionId`).
    - Lookup mana `qId` ber-`QuestionType=="Essay"` dari packageQuestions (sesuai shuffledQIds).
    - Untuk soal Essay: "terjawab" HANYA bila `!string.IsNullOrWhiteSpace(TextAnswer)`. MC/MA: hitung seperti sekarang.
    - Jika on-time + ada ≥1 essay (atau soal lain) yang belum terjawab/essay kosong → pakai blok existing (`TempData["Error"]=...; return RedirectToAction("ExamSummary", new { id });`). Gunakan pesan ramah untuk essay kosong (mis. "Isi semua jawaban essay terlebih dahulu sebelum submit.") — server authoritative, client `flushEssay` tetap UX (lesson Phase 413). JANGAN bocorkan kunci.
    - PERTAHANKAN cabang timeout (`serverTimerExpired==true`) apa adanya — fall-through finalize → PendingGrading (D-04). JANGAN taruh reject di luar `!serverTimerExpired` (Pitfall 6 dead-end).
    - Opsional (mirror pola EnsureCanSubmitStandard): ekstrak keputusan "essay kosong on-time?" ke pure static helper di CMPController (mis. `bool AnyEmptyEssayOnTime(...)` / decision) agar unit-testable tanpa konstruksi controller; integrasi penuh dijaga real-SQL.

    Extend `HcPortal.Tests/EnsureCanSubmitStandardTests.cs` — tambah test (pure bila helper diekstrak; jika butuh DB, `[Trait("Category","Integration")]` reuse GradingDedupeFixture):
    - on-time + 1 essay TextAnswer="" → harus block.
    - on-time + semua essay terisi → lolos.
    - (DO-NOT-REGRESS) timeout + essay kosong → finalize/PendingGrading (jangan rusak `EssayEmptyPendingParityTests`).
  </action>
  <acceptance_criteria>
    - `grep -n "ExamTimeRules.AllowedExamSeconds(session.DurationMinutes" Controllers/CMPController.cs` → ada (clamp :469 fixed).
    - `grep -n "session.DurationMinutes \* 60" Controllers/CMPController.cs` pada clamp :469 → 0 (sudah diganti).
    - `grep "IsNullOrWhiteSpace" Controllers/CMPController.cs` → ada di blok submit on-time (essay content check).
    - Blok reject essay berada di DALAM `if (!serverTimerExpired)` (verifikasi — bukan di luar).
    - Pesan essay-rejection tak mengandung opsi/kunci jawaban (review string literal).
    - `dotnet build` exits 0.
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~EnsureCanSubmit|FullyQualifiedName~EssayEmptyPendingParity"` exits 0 (block on-time + timeout-finalize tak regress).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~EnsureCanSubmit|FullyQualifiedName~EssayEmptyPendingParity|FullyQualifiedName~ExamTimeRules"</automated>
  </verify>
  <done>Clamp :469 pakai ExamTimeRules.AllowedExamSeconds (ExtraTime); essay kosong on-time ditolak server-side seluruh submit dgn pesan ramah tanpa leak; timeout tetap finalize PendingGrading (D-04 utuh).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker browser → StartExam | entry-point ujian; akses Post harus di-gate server-side |
| worker browser → SubmitExam (POST) | submit ujian; antiforgery + server-authoritative validation |
| display pairing query → DB | grouping Pre/Post; harus terfilter UserId (cegah cross-user) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-424-04 | Elevation of Privilege | StartExam gate GRDF-01 | mitigate | Gate pakai link EKSPLISIT terfilter UserId (FindPairedPreAsync), BUKAN pola judul; bypass judul/type tak melewati gate (D-02). Owner-check :914 dipertahankan di atas gate. |
| T-424-05 | Information Disclosure | pairing display :292-297 | mitigate | Tambah `s.UserId == userId` di SEMUA cabang pairing — cegah pasangkan Pre pekerja lain (FLOW-01 fix) |
| T-424-06 | Tampering | SubmitExam essay validation | mitigate | Server-side reject essay kosong on-time (D-05); `flushEssay` client non-authoritative (lesson Phase 413). Antiforgery POST existing dipertahankan. |
| T-424-07 | Information Disclosure | pesan essay-rejection | mitigate | Pesan ramah TANPA kunci/answer-key (V5); hanya instruksi "isi essay". |
| T-424-08 | Tampering | submit replay sesi terminal | accept | STAT-01 guard existing (Completed/Abandoned/Cancelled/PendingGrading :1608) dipertahankan — tak diubah di plan ini. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests` (full, SQLEXPRESS) hijau — PrePostGating (block/pass/orphan/user-lain) + EnsureCanSubmit (essay on-time block/timeout finalize) + ExamTimeRules + regression guard (TokenGateTests, EssayEmptyPendingParityTests, GradingDedupeTests) tak ada yang merah.
- Owner-check + antiforgery + STAT-01 guard masih utuh (grep).
</verification>

<success_criteria>
- GRDF-01: Post StartExam ter-gate (worker-only) bila Pre belum Completed; orphan/Standard/user-lain pass-through (D-02).
- GRDF-03: pairing display terfilter UserId (FLOW-01 ditutup).
- GRDF-05: clamp ElapsedSeconds memperhitungkan ExtraTimeMinutes (under-report export ditutup di root).
- GRDF-07: essay kosong on-time ditolak server-side seluruh submit; timeout finalize PendingGrading utuh (D-04).
- 0 regresi guard test.
</success_criteria>

<output>
After completion, create `.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-02-SUMMARY.md`
</output>
