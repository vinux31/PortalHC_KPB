---
phase: 425-cosmetic-naming-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentSession.cs
  - Models/UserPackageAssignment.cs
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/EditAssessment.cshtml
  - Views/Admin/AddManualAssessment.cshtml
autonomous: true
requirements: [CLN-01, CLN-03]
migration: false

must_haves:
  truths:
    - "Kolom AssessmentPhase ditandai RESERVED di XML-doc, tidak di-drop, tidak ada migration"
    - "Komentar Status mencantumkan 7 nilai kanonik (bukan 3)"
    - "ValidUntil punya satu sumber label via [Display]; label cshtml selaras"
    - "XML-doc LinkedSessionId tidak lagi mengklaim 'ON DELETE SET NULL' (dikoreksi ke app-level)"
    - "AssessmentPackageId punya komentar sentinel yang menjelaskan ini paket seed, bukan paket aktual"
  artifacts:
    - path: "Models/AssessmentSession.cs"
      provides: "RESERVED XML-doc AssessmentPhase + komentar Status 7-nilai + [Display] ValidUntil + koreksi XML-doc LinkedSessionId"
      contains: "RESERVED"
    - path: "Models/UserPackageAssignment.cs"
      provides: "Komentar sentinel AssessmentPackageId"
      contains: "SENTINEL"
  key_links:
    - from: "Models/AssessmentSession.cs ValidUntil [Display]"
      to: "Views/Admin/*.cshtml label"
      via: "Html.DisplayNameFor / istilah label tunggal"
      pattern: "Display\\(Name = \"Berlaku Sampai\"\\)"
---

<objective>
CLN-01 + CLN-03 — penyelarasan label & dokumentasi (kosmetik, pure text/komentar/XML-doc) tanpa mengubah perilaku, binding, atau skema. Menandai dead-field `AssessmentPhase` sebagai RESERVED (BUKAN drop), mengoreksi komentar Status (3→7 nilai), menambah `[Display]` ke `ValidUntil` sebagai satu sumber label, mengoreksi XML-doc `LinkedSessionId` (klaim "ON DELETE SET NULL" keliru → app-level), dan menambah komentar sentinel ke `AssessmentPackageId`.

Purpose: Membayar tech-debt dokumentasi usang/menyesatkan yang teridentifikasi di audit Pre/Post (FLD-5.2-06, FLOW-05, FLOW-06, PA-04, PA-05). Dokumentasi akurat mencegah kesalahan interpretasi di fase mendatang.
Output: `AssessmentSession.cs`, `UserPackageAssignment.cs`, dan 3 view cshtml dengan label/komentar/XML-doc selaras. ZERO perubahan perilaku/skema. migration=FALSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-CONTEXT.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md

<interfaces>
<!-- Lokasi terverifikasi sesi planning 2026-06-24 (re-grep WAJIB sebelum edit; line bisa drift). -->

Models/AssessmentSession.cs (current):
```csharp
:18  public DateTime Schedule { get; set; }
:19  public int DurationMinutes { get; set; }
:20  public string Status { get; set; } = "";   // "Open", "Upcoming", "Completed"   ← komentar usang 3-nilai
:84  public DateOnly? ValidUntil { get; set; }   ← belum ada [Display]
:177-180  XML-doc "Fase assessment ... 'Phase1','Phase2'" + public string? AssessmentPhase { get; set; }   ← RESERVED target
:188-192  XML-doc "ON DELETE SET NULL ..." + public int? LinkedSessionId { get; set; }   ← klaim keliru
```

Models/UserPackageAssignment.cs (current):
```csharp
:17  // FK to the package assigned to this user (Restrict — don't cascade-delete assignments when package deleted)
:18  public int AssessmentPackageId { get; set; }   ← target komentar sentinel
```

Models/AssessmentConstants.cs — 7 status kanonik (AssessmentConstants.AssessmentStatus): Open, Upcoming, Completed,
"Menunggu Penilaian" (PendingGrading), InProgress, Cancelled, Abandoned.

Bukti app-level null-clear LinkedSessionId: Services/RecordCascadeDeleteService.cs:235-237 (Delta #8).

Label cshtml ValidUntil (verifikasi via grep "Expired"/"Berlaku"):
- Views/Admin/CreateAssessment.cshtml (~:637 "Tanggal Expired Sertifikat")
- Views/Admin/EditAssessment.cshtml (~:498)
- Views/Admin/AddManualAssessment.cshtml (~:218)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RESERVED XML-doc AssessmentPhase (CLN-03) + komentar Status 7-nilai (CLN-01/FLOW-05)</name>
  <files>Models/AssessmentSession.cs</files>
  <read_first>
    - Models/AssessmentSession.cs (re-grep "AssessmentPhase" dan baris `public string Status` SEBELUM edit — line bisa drift)
    - Models/AssessmentConstants.cs (konfirmasi 7 nilai AssessmentStatus kanonik)
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md §"Code Examples" (teks before/after persis)
  </read_first>
  <action>
    Re-grep `AssessmentPhase` di Models/AssessmentSession.cs untuk menemukan line aktual (verifikasi 2026-06-24 = :177-180; bisa drift).

    1) Ganti XML-doc 3-baris di atas `public string? AssessmentPhase` dengan teks RESERVED PERSIS berikut (Bahasa Indonesia, salin verbatim):
    ```csharp
    /// <summary>
    /// RESERVED — tidak dipakai. Dideklarasikan di v14 (AddAssessmentV14Columns) untuk konsep
    /// 'Phase1'/'Phase2' yang TIDAK PERNAH diimplementasikan. Linking Pre/Post nyata bertumpu pada
    /// AssessmentType + LinkedGroupId + LinkedSessionId. 0 referensi di app (FLOW-06). Dipertahankan
    /// di skema (kolom nullable, aman) untuk hindari migration destruktif. Jangan baca/tulis.
    /// </summary>
    public string? AssessmentPhase { get; set; }
    ```
    JANGAN drop kolom. JANGAN generate migration (D-01).

    2) Ganti komentar inline usang pada baris `public string Status { get; set; } = "";   // "Open", "Upcoming", "Completed"` menjadi komentar 7-nilai kanonik (salin verbatim):
    ```csharp
    public string Status { get; set; } = "";
    // Nilai kanonik (lihat AssessmentConstants.AssessmentStatus): Open, Upcoming, Completed,
    // "Menunggu Penilaian" (PendingGrading), InProgress, Cancelled, Abandoned (7 status).
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && grep -c "RESERVED — tidak dipakai" Models/AssessmentSession.cs && grep -c "7 status" Models/AssessmentSession.cs && dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RESERVED" Models/AssessmentSession.cs` cocok (XML-doc RESERVED ada di atas AssessmentPhase).
    - `grep "AddAssessmentV14Columns" Models/AssessmentSession.cs` cocok.
    - `grep "Menunggu Penilaian" Models/AssessmentSession.cs` cocok DAN `grep "7 status" Models/AssessmentSession.cs` cocok.
    - `grep -c "public string? AssessmentPhase" Models/AssessmentSession.cs` == 1 (kolom MASIH ada, tidak di-drop).
    - Tidak ada file baru di Migrations/ (`git status` bersih dari migration baru).
    - `dotnet build HcPortal.csproj` exit 0, tanpa error baru.
  </acceptance_criteria>
  <done>AssessmentPhase ber-XML-doc RESERVED (kolom tetap ada, no migration); komentar Status mencantumkan 7 nilai kanonik; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: [Display] ValidUntil + koreksi XML-doc LinkedSessionId (CLN-01/FLD-5.2-06, PA-04)</name>
  <files>Models/AssessmentSession.cs</files>
  <read_first>
    - Models/AssessmentSession.cs (re-grep `ValidUntil` dan `LinkedSessionId` — line bisa drift)
    - Services/RecordCascadeDeleteService.cs (konfirmasi null-clear app-level di ~:235-237, bukti PA-04)
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md §"Code Examples" (teks before/after)
  </read_first>
  <action>
    Re-grep simbol sebelum edit.

    1) Tambahkan atribut `[Display(Name = "Berlaku Sampai")]` tepat di atas `public DateOnly? ValidUntil { get; set; }` (PERTAHANKAN XML-doc existing di atasnya). Hasil:
    ```csharp
    /// <summary>
    /// Optional certificate expiry date. Null = certificate has no expiry.
    /// Only relevant when GenerateCertificate = true.
    /// Phase 327 — DateOnly migrasi P04 (eliminasi tz drift permanen).
    /// </summary>
    [Display(Name = "Berlaku Sampai")]   // CLN-01 (FLD-5.2-06): satu sumber label; selaraskan cshtml ke istilah ini
    public DateOnly? ValidUntil { get; set; }
    ```
    Pastikan `using System.ComponentModel.DataAnnotations;` sudah di-import di file (cek; tambahkan bila belum — model lain pakai [Required] jadi kemungkinan sudah ada).

    2) Ganti XML-doc `LinkedSessionId` yang mengklaim "ON DELETE SET NULL" (KELIRU) dengan teks koreksi PA-04 PERSIS:
    ```csharp
    /// <summary>
    /// FK ke AssessmentSession lain yang terhubung (misal: PreTest terhubung ke PostTest-nya).
    /// CATATAN (PA-04): TIDAK ada FK cascade terkonfigurasi di DB. Null-clear saat pasangan
    /// dihapus dilakukan di level aplikasi — RecordCascadeDeleteService.cs:235-237 (Delta #8).
    /// </summary>
    public int? LinkedSessionId { get; set; }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && grep -c "Display(Name = \"Berlaku Sampai\")" Models/AssessmentSession.cs && grep -c "TIDAK ada FK cascade" Models/AssessmentSession.cs && ! grep -q "ON DELETE SET NULL" Models/AssessmentSession.cs && dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Display(Name = "Berlaku Sampai")' Models/AssessmentSession.cs` cocok, tepat di atas `public DateOnly? ValidUntil`.
    - `grep "RecordCascadeDeleteService.cs:235-237" Models/AssessmentSession.cs` cocok (koreksi PA-04 ada).
    - `grep "ON DELETE SET NULL" Models/AssessmentSession.cs` TIDAK menghasilkan match (klaim keliru terhapus).
    - Tipe `ValidUntil` TETAP `DateOnly?` (tidak diubah); `LinkedSessionId` TETAP `int?`.
    - `dotnet build HcPortal.csproj` exit 0.
  </acceptance_criteria>
  <done>ValidUntil punya [Display] sumber-tunggal; XML-doc LinkedSessionId dikoreksi ke app-level; build hijau; tipe field tak berubah.</done>
</task>

<task type="auto">
  <name>Task 3: Komentar sentinel AssessmentPackageId (CLN-01/PA-05) + selaraskan label ValidUntil di 3 cshtml</name>
  <files>Models/UserPackageAssignment.cs, Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml, Views/Admin/AddManualAssessment.cshtml</files>
  <read_first>
    - Models/UserPackageAssignment.cs (re-grep `AssessmentPackageId` — :17-18)
    - Views/Admin/CreateAssessment.cshtml, EditAssessment.cshtml, AddManualAssessment.cshtml (grep "Expired"/"Berlaku" untuk lokasi label ValidUntil aktual)
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md §"Code Examples" (teks sentinel)
  </read_first>
  <action>
    1) Di Models/UserPackageAssignment.cs, ganti komentar 1-baris di atas `public int AssessmentPackageId { get; set; }` (saat ini `// FK to the package assigned to this user (Restrict — ...)`) dengan komentar sentinel PERSIS:
    ```csharp
    // SENTINEL (PA-05): bukan "paket aktual" peserta — ini paket PERTAMA (seed) yang dipilih saat
    // build assignment (CMPController.cs:1087-1093). Soal aktual ditentukan oleh ShuffledQuestionIds
    // (bisa lintas paket). FK Restrict — assignment tak ikut terhapus saat package dihapus.
    public int AssessmentPackageId { get; set; }
    ```
    JANGAN ubah nama field/properti `AssessmentPackageId` (rename = sentuh runtime/binding; CLN-01 murni komentar).

    2) Selaraskan label ValidUntil ke istilah tunggal "Berlaku Sampai" (atau "Tanggal Berlaku Sampai" bila konteks butuh) di 3 view. Grep masing-masing file untuk teks label expired/berlaku pada field ValidUntil, lalu samakan teks label yang tampil ke pengguna agar konsisten dengan [Display(Name="Berlaku Sampai")]. JANGAN ubah `asp-for`, name attribute, id, atau binding — HANYA teks label yang tampil (`<label>...</label>` / teks heading kolom). Bila view memakai `asp-for="ValidUntil"` dengan `<label asp-for>`, label otomatis ikut [Display] — cukup pastikan tak ada teks hardcode "Tanggal Expired Sertifikat" yang menimpa. Bila ada teks hardcode, ganti ke "Berlaku Sampai".
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && grep -c "SENTINEL (PA-05)" Models/UserPackageAssignment.cs && grep -rn "Berlaku Sampai" Views/Admin/CreateAssessment.cshtml Views/Admin/EditAssessment.cshtml Views/Admin/AddManualAssessment.cshtml ; dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SENTINEL (PA-05)" Models/UserPackageAssignment.cs` cocok.
    - `grep -c "public int AssessmentPackageId" Models/UserPackageAssignment.cs` == 1 (nama field tak berubah).
    - Tidak ada lagi teks label hardcode "Tanggal Expired Sertifikat" untuk field ValidUntil di 3 view (grep "Tanggal Expired" tidak match pada konteks ValidUntil), ATAU label sudah memakai asp-for ([Display]) — istilah tunggal "Berlaku Sampai" konsisten.
    - Tidak ada perubahan pada `asp-for`, `name=`, atau `id=` field ValidUntil di view (binding utuh).
    - `dotnet build HcPortal.csproj` exit 0.
  </acceptance_criteria>
  <done>AssessmentPackageId ber-komentar sentinel (nama field tak berubah); label ValidUntil konsisten "Berlaku Sampai" di 3 view tanpa ubah binding; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan ini pure text/komentar/XML-doc/label. Tidak ada input baru menyeberang batas; tidak ada perubahan binding/skema/perilaku. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-425-01 | Tampering | Models/AssessmentSession.cs (CLN-03 RESERVED) | accept | Hanya XML-doc; kolom TIDAK di-drop, TIDAK ada migration → zero perubahan skema/data. Acceptance criteria memverifikasi kolom masih ada + tidak ada file migration baru. |
| T-425-02 | Information Disclosure | Views/Admin/*.cshtml (label ValidUntil) | accept | Label statis non-sensitif; tidak meng-echo input pengguna. Razor auto-encode. Tidak ada data leak. |
| T-425-03 | Tampering | Models/UserPackageAssignment.cs (sentinel) | accept | Komentar saja; nama field/binding tak berubah (acceptance criteria menjaga nama field). Tidak ada perubahan FK/relasi. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exit 0, 0 error baru, 0 warning baru.
- `git diff --stat` hanya menyentuh 5 file di files_modified; tidak ada file Migrations/ baru.
- Full suite tetap hijau (baseline 748/0/2) — plan ini tak menyentuh logika, tapi jalankan untuk konfirmasi zero regresi: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj`.
- UAT manual @ http://localhost:5270 (branch ITHandoff): buka form Create/Edit/AddManual assessment → label expiry sertifikat tampil "Berlaku Sampai" konsisten.
</verification>

<success_criteria>
- AssessmentPhase ber-XML-doc RESERVED, kolom tetap ada, ZERO migration baru (migration=FALSE).
- Komentar Status mencantumkan 7 nilai kanonik.
- ValidUntil punya [Display(Name="Berlaku Sampai")]; label cshtml selaras.
- XML-doc LinkedSessionId tidak lagi mengklaim "ON DELETE SET NULL"; mengarah ke app-level.
- AssessmentPackageId ber-komentar sentinel; nama field tak berubah.
- Build hijau; full suite 748/0/2 (0 regresi).
</success_criteria>

<output>
After completion, create `.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-01-SUMMARY.md`
</output>
