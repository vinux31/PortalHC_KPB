---
phase: 425-cosmetic-naming-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/ExamTimeRulesTests.cs
  - Controllers/CMPController.cs
autonomous: true
requirements: [CLN-04]
migration: false

must_haves:
  truths:
    - "4 situs formula timer di CMPController memanggil ExamTimeRules.AllowedExamSeconds (bukan formula inline)"
    - "Hasil numerik 4 situs IDENTIK dengan formula lama (parity terbukti via test)"
    - "Situs :4661 (menit→double) tetap menghasilkan allowedSec numerik identik (int→double implicit)"
    - "Token gate (FLOW-08) dan side-effect GET StartExam (FLOW-10) TIDAK disentuh (D-03 defer)"
  artifacts:
    - path: "HcPortal.Tests/ExamTimeRulesTests.cs"
      provides: "Parity test [Theory] untuk (d,e) representatif 4 situs"
      contains: "Parity"
    - path: "Controllers/CMPController.cs"
      provides: "4 call-site ExamTimeRules.AllowedExamSeconds menggantikan formula inline"
      contains: "ExamTimeRules.AllowedExamSeconds"
  key_links:
    - from: "Controllers/CMPController.cs (4 situs timer)"
      to: "Helpers/ExamTimeRules.AllowedExamSeconds"
      via: "static helper call"
      pattern: "ExamTimeRules\\.AllowedExamSeconds\\(assessment\\.DurationMinutes"
---

<objective>
CLN-04 — konsolidasi 4 situs formula durasi timer `(DurationMinutes + (ExtraTimeMinutes ?? 0)) * 60` yang berulang ke single-source `ExamTimeRules.AllowedExamSeconds(...)` (helper sudah dibuat di Phase 424). PARITAS WAJIB: hasil numerik IDENTIK. No migration, no behavior-change.

Purpose: Menutup tech-debt FLOW-09 (redundansi formula timing = akar drift; Phase 424 sudah memperbaiki :469, plan ini menyelesaikan konvergensi 4 situs sisa). Mencegah formula inline ber-divergensi lagi.
Output: `CMPController.cs` dengan 4 call-site helper + `ExamTimeRulesTests.cs` diperluas dengan parity test eksplisit. FLOW-08 token gate & FLOW-10 write-on-GET DI-DEFER (D-03 — JANGAN sentuh).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-CONTEXT.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md

<interfaces>
<!-- Helper existing (Phase 424). Lokasi situs terverifikasi 2026-06-24 (re-grep WAJIB; line bisa drift). -->

Helpers/ExamTimeRules.cs (existing — Phase 424, JANGAN ubah):
```csharp
public static class ExamTimeRules
{
    public static int AllowedExamSeconds(int durationMinutes, int? extraTimeMinutes)
        => (durationMinutes + (extraTimeMinutes ?? 0)) * 60;
}
```

HcPortal.Tests/ExamTimeRulesTests.cs (existing — 3 [Fact], EXTEND bukan rewrite):
```csharp
[Fact] WithExtraTime_AddsBeforeMultiply => Assert.Equal(900, AllowedExamSeconds(10, 5));
[Fact] NullExtraTime_TreatedAsZero      => Assert.Equal(600, AllowedExamSeconds(10, null));
[Fact] ZeroAll_IsZero                   => Assert.Equal(0,   AllowedExamSeconds(0, 0));
```

Controllers/CMPController.cs — 4 situs terverifikasi (re-grep "ExtraTimeMinutes ?? 0" sebelum edit):
```csharp
:1191  int durationSeconds = (assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)) * 60;   // detik, int
:1564  var allowed = (assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)) * 60;            // detik, int
:1642  var allowed = (assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)) * 60;            // detik, int
:4661  int allowedMinutes = assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0);            // menit, int
:4663  double allowedSec  = allowedMinutes * 60.0;                                                       // double
:4664  double graceLimitSec = allowedSec + 120.0;   // 2-min grace (existing tier-2) — JANGAN ubah
```

CMPController.cs:469 — SUDAH di-wire ke ExamTimeRules (Phase 424 GRDF-05). Pola call-site = contoh.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): EXTEND ExamTimeRulesTests.cs dengan parity test 4 situs (CLN-04)</name>
  <files>HcPortal.Tests/ExamTimeRulesTests.cs</files>
  <read_first>
    - HcPortal.Tests/ExamTimeRulesTests.cs (existing 3 [Fact] — tambah, jangan timpa)
    - Helpers/ExamTimeRules.cs (signature AllowedExamSeconds)
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md §"ExamTimeRulesTests.cs (EXTEND)"
  </read_first>
  <behavior>
    - Test parity: AllowedExamSeconds(10, 5) == 900  ((10+5)*60)
    - Test parity: AllowedExamSeconds(10, null) == 600  ((10+0)*60)
    - Test parity: AllowedExamSeconds(0, 0) == 0
    - Test parity: AllowedExamSeconds(60, 15) == 4500  ((60+15)*60)
    - Test parity (double-site :4661): (double)AllowedExamSeconds(10, 5) == 900.0 (int→double identik dgn formula lama allowedMinutes*60.0)
  </behavior>
  <action>
    Tambahkan ke class ExamTimeRulesTests (PERTAHANKAN 3 [Fact] existing) sebuah [Theory] parity yang membuktikan helper == formula lama untuk nilai representatif dari 4 situs:
    ```csharp
    [Theory]
    [InlineData(10, 5, 900)]      // :1191/:1564/:1642 dgn extra
    [InlineData(10, null, 600)]   // null extra
    [InlineData(0, 0, 0)]         // batas bawah
    [InlineData(60, 15, 4500)]    // durasi besar
    public void Parity_AllTimerSites(int duration, int? extra, int expected) =>
        Assert.Equal(expected, ExamTimeRules.AllowedExamSeconds(duration, extra));

    // :4661 site — helper int di-assign ke double allowedSec; buktikan numerik identik dgn allowedMinutes*60.0 lama.
    [Theory]
    [InlineData(10, 5, 900.0)]
    [InlineData(10, null, 600.0)]
    public void Parity_DoubleSite_4661(int duration, int? extra, double expected)
    {
        double allowedSec = ExamTimeRules.AllowedExamSeconds(duration, extra);   // int→double implicit
        int allowedMinutesOld = duration + (extra ?? 0);
        double allowedSecOld = allowedMinutesOld * 60.0;                          // formula lama
        Assert.Equal(expected, allowedSec);
        Assert.Equal(allowedSecOld, allowedSec);                                  // parity eksplisit
    }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ExamTimeRulesTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Parity_AllTimerSites" HcPortal.Tests/ExamTimeRulesTests.cs` == 1.
    - `grep -c "Parity_DoubleSite_4661" HcPortal.Tests/ExamTimeRulesTests.cs` == 1.
    - 3 [Fact] existing TETAP ada (`grep -c "WithExtraTime_AddsBeforeMultiply"` == 1).
    - `dotnet test --filter "FullyQualifiedName~ExamTimeRulesTests"` exit 0 (semua hijau).
  </acceptance_criteria>
  <done>Parity test [Theory] untuk 4 situs (incl double-site :4661) hijau; 3 [Fact] existing utuh.</done>
</task>

<task type="auto">
  <name>Task 2: Konsolidasi 4 situs CMPController ke ExamTimeRules.AllowedExamSeconds (CLN-04)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (re-grep "ExtraTimeMinutes ?? 0" → harus ketemu 4 site; verifikasi line aktual sebelum edit)
    - Controllers/CMPController.cs:469 (contoh call-site Phase 424 yang sudah benar)
    - Helpers/ExamTimeRules.cs (signature)
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md §"Pitfall 1" + §"Anti-Patterns to Avoid"
  </read_first>
  <action>
    Re-grep `ExtraTimeMinutes ?? 0` di CMPController.cs untuk menemukan line aktual (verifikasi 2026-06-24: :1191, :1564, :1642, :4661). Pastikan `using HcPortal.Helpers;` ada di file (kemungkinan sudah, karena :469 memakai ExamTimeRules).

    Situs DETIK (int) — 3 tempat, ganti formula inline → helper:
    ```csharp
    // :1191  int durationSeconds = (assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)) * 60;
    //   →
    int durationSeconds = ExamTimeRules.AllowedExamSeconds(assessment.DurationMinutes, assessment.ExtraTimeMinutes);

    // :1564 & :1642  var allowed = (assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)) * 60;
    //   →
    var allowed = ExamTimeRules.AllowedExamSeconds(assessment.DurationMinutes, assessment.ExtraTimeMinutes);
    ```

    Situs MENIT→DOUBLE (:4661) — jaga parity tipe (Pitfall #1). Ganti DUA baris (`int allowedMinutes = ...` + `double allowedSec = allowedMinutes * 60.0;`) menjadi:
    ```csharp
    double allowedSec = ExamTimeRules.AllowedExamSeconds(assessment.DurationMinutes, assessment.ExtraTimeMinutes);
    ```
    (int→double implicit; numerik identik). Hapus baris `int allowedMinutes = ...` HANYA bila `allowedMinutes` tidak dipakai lagi di scope itu — grep `allowedMinutes` di sekitar :4655-4720 dulu; bila masih dipakai di tempat lain, pertahankan deklarasinya dan jangan hapus. JANGAN ubah `graceLimitSec = allowedSec + 120.0;` (tetap apa adanya).

    HARD CONSTRAINT (D-03): JANGAN sentuh token gate `TempData.Peek` (FLOW-08), JANGAN ubah side-effect GET StartExam (FLOW-10, Upcoming→Open). Hanya konsolidasi formula durasi. Tidak ada perubahan perilaku.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && test "$(grep -c 'ExamTimeRules.AllowedExamSeconds(assessment.DurationMinutes' Controllers/CMPController.cs)" -ge 5 && ! grep -q "(assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)) \* 60" Controllers/CMPController.cs && dotnet build HcPortal.csproj -clp:ErrorsOnly && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ExamTimeRulesTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ExamTimeRules.AllowedExamSeconds(assessment.DurationMinutes" Controllers/CMPController.cs` >= 5 (4 situs baru + :469 existing).
    - `grep "(assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)) \* 60" Controllers/CMPController.cs` TIDAK match (formula inline detik habis).
    - `grep -c "int allowedMinutes = assessment.DurationMinutes + (assessment.ExtraTimeMinutes ?? 0)" Controllers/CMPController.cs` == 0 (situs :4661 menit dikonsolidasi) ATAU allowedMinutes dipertahankan karena dipakai di tempat lain (catat di SUMMARY bila demikian).
    - `grep "graceLimitSec = allowedSec + 120.0" Controllers/CMPController.cs` masih cocok (grace tak berubah).
    - Token gate utuh: `grep -c "TempData.Peek" Controllers/CMPController.cs` tidak berkurang dari baseline; tidak ada edit pada blok GET StartExam.
    - `dotnet build HcPortal.csproj` exit 0; `dotnet test --filter ExamTimeRulesTests` hijau.
  </acceptance_criteria>
  <done>4 situs timer memanggil ExamTimeRules.AllowedExamSeconds; formula inline detik habis; :4661 parity double terjaga; token gate/StartExam tak disentuh; build + parity test hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→exam timer (existing) | Durasi ujian dihitung server-side; plan ini hanya mengganti formula inline → helper identik. Tidak ada input baru menyeberang. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-425-04 | Tampering | CMPController timer (allowedSec/graceLimit) | mitigate | Parity test [Theory] (Task 1) membuktikan output helper == formula lama (incl double-site :4661). Acceptance criteria menjaga `graceLimitSec` tak berubah. Timer "habis mendadak" tercegah. |
| T-425-05 | Spoofing | Token gate FLOW-08 (TempData.Peek) | accept | OUT OF SCOPE (D-03 defer). JANGAN sentuh — acceptance criteria memverifikasi `TempData.Peek` count tak berkurang + tidak ada edit di blok StartExam. Impersonation guard existing tetap aktif. |
| T-425-06 | Elevation | GET StartExam side-effect (FLOW-10) | accept | OUT OF SCOPE (D-03 defer). Tidak disentuh; mitigasi existing (impersonation guard) tetap berlaku. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exit 0, 0 error baru.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full suite tetap 748/0/2 (atau +N test parity baru, 0 fail) — 0 regresi.
- `git diff Controllers/CMPController.cs` hanya menyentuh 4 situs timer; TIDAK menyentuh token gate / StartExam GET.
- UAT manual @ http://localhost:5270 (opsional, low-risk): ambil ujian dengan ExtraTime → timer hitung-mundur benar, auto-submit pada waktu yang sama seperti sebelumnya.
</verification>

<success_criteria>
- 4 situs timer memanggil ExamTimeRules.AllowedExamSeconds; formula inline tidak tersisa untuk detik.
- Parity terbukti via test (4 situs + double-site :4661); hasil numerik identik.
- Token gate (FLOW-08) & write-on-GET StartExam (FLOW-10) TIDAK tersentuh (D-03).
- Build hijau; full suite 748/0/2 + test parity baru hijau (0 regresi).
- migration=FALSE (tak ada migration baru).
</success_criteria>

<output>
After completion, create `.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-02-SUMMARY.md`
</output>
