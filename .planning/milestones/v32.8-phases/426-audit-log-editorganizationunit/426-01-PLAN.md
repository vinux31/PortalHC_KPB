---
phase: 426-audit-log-editorganizationunit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/OrganizationController.cs
  - HcPortal.Tests/OrganizationControllerTests.cs
autonomous: true
requirements: [AUDIT-01]
tags: [audit, organization, traceability]

must_haves:
  truths:
    - "HC/Admin yang me-rename atau me-reparent unit via EditOrganizationUnit meninggalkan satu baris AuditLog ActionType='EditOrganizationUnit' dengan actor + ringkasan oldName→newName + oldParentId→parentId"
    - "Baris audit menyertakan cascade counts (cascadedUsers/cascadedMappings/cascadedUserUnits) di dalam Description"
    - "Audit ditulis SETELAH tx.CommitAsync() dan swallow-on-failure — kegagalan audit tidak memblokir respons sukses edit"
    - "Edit valid tetap sukses; cascade UserUnits-aware (ph403) tak berubah; authz/CSRF existing utuh"
    - "No-op edit (nama+parent identik) commit sukses TANPA menulis baris audit (only-on-change)"
  artifacts:
    - path: "Controllers/OrganizationController.cs"
      provides: "Blok audit aditif di EditOrganizationUnit, guard if(oldName != name.Trim() || oldParentId != parentId) setelah tx.CommitAsync()"
      contains: "EditOrganizationUnit"
    - path: "HcPortal.Tests/OrganizationControllerTests.cs"
      provides: "FakeUserStore + MakeUserManager + MakeControllerWithUser factory + 5 test cases T1-T5"
      contains: "EditOrganizationUnit_NoChange_WritesZeroAuditRows"
  key_links:
    - from: "OrganizationController.EditOrganizationUnit"
      to: "_auditLog.LogAsync"
      via: "panggilan dalam try/catch swallow, guarded by only-on-change, setelah commit"
      pattern: "_auditLog\\.LogAsync\\([^)]*\"EditOrganizationUnit\""
    - from: "HcPortal.Tests/OrganizationControllerTests.cs"
      to: "ctx.AuditLogs"
      via: "read-back assertion ActionType == 'EditOrganizationUnit'"
      pattern: "AuditLogs.*EditOrganizationUnit"
---

<objective>
Tambahkan jejak `AuditLog` pada `OrganizationController.EditOrganizationUnit` (rename/reparent unit organisasi), me-mirror pola audit yang sudah ada di `DeleteOrganizationUnit`. Menutup asimetri traceability pre-existing: `DeleteOrganizationUnit` menulis audit, `EditOrganizationUnit` tidak.

Purpose: Setiap admin/HC yang me-rename atau me-reparent unit meninggalkan jejak yang dapat ditelusuri — siapa (actor NIP/nama), perubahan apa (nama lama→baru, parent lama→baru), dan dampak cascade-nya — memenuhi AUDIT-01.

Output:
- `Controllers/OrganizationController.cs` — satu blok audit aditif (try/catch swallow) setelah `await tx.CommitAsync();`, di-guard only-on-change (D-01).
- `HcPortal.Tests/OrganizationControllerTests.cs` — factory `MakeControllerWithUser` + `FakeUserStore`/`MakeUserManager` (disalin dari `RetakeExamEndpointTests.cs:47-99`) + 5 test cases T1-T5.

Scope: SMALL additive change. migration=FALSE (tabel `AuditLog` + `AuditLogService` sudah ada, tidak ada schema/kolom baru). TIDAK mengubah perilaku cascade ph403, validasi, authz, atau CSRF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/426-audit-log-editorganizationunit/426-CONTEXT.md
@.planning/phases/426-audit-log-editorganizationunit/426-RESEARCH.md
@.planning/phases/426-audit-log-editorganizationunit/426-VALIDATION.md

# Surface yang dimodifikasi + analog yang di-mirror
@Controllers/OrganizationController.cs
@HcPortal.Tests/OrganizationControllerTests.cs
@HcPortal.Tests/RetakeExamEndpointTests.cs

<interfaces>
<!-- Kontrak kunci — executor pakai langsung, no codebase exploration. Source-verified 2026-06-24. -->

From Services/AuditLogService.cs:21 (signature — SaveChanges internal, caller tidak save; CreatedAt=UtcNow di-set di service):
```csharp
public async Task LogAsync(
    string actorUserId, string actorName, string actionType, string description,
    int? targetId = null, string? targetType = null)
```

From Models/AuditLog.cs (⚠️ ActionType MaxLength(50) — "EditOrganizationUnit" = 19 chars ✓):
```
Id · ActorUserId [Required] · ActorName [Required] · ActionType [Required][MaxLength(50)]
· Description [Required] · TargetId int? · TargetType string? [MaxLength(100)] · CreatedAt
```

From Controllers/OrganizationController.cs — EditOrganizationUnit (129-317), variabel siap-pakai:
- `oldName` (line 139), `oldParentId` (line 140) — captured sebelum cascade.
- `cascadedUsers`, `cascadedMappings`, `cascadedUserUnits` (declared 198-200, populated 209-242 & 292-296).
- `unit.Id` = entityId. `await tx.CommitAsync();` di line 308; `var msg = ...` di line 310. SISIPKAN di antara keduanya.
- Endpoint: `[HttpPost] [Authorize(Roles="Admin, HC")] [ValidateAntiForgeryToken]` — sudah ada, JANGAN diubah.

Pola kanonik yang di-mirror — Controllers/OrganizationController.cs:539-549 (DeleteOrganizationUnit):
```csharp
try
{
    var currentUser = await _userManager.GetUserAsync(User);
    var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
        ? (currentUser?.FullName ?? "Unknown")
        : $"{currentUser.NIP} - {currentUser.FullName}";
    await _auditLog.LogAsync(currentUser?.Id ?? "", actorName, "DeleteOrganizationUnit",
        $"Deleted organization unit '{unit.Name}' [ID={unit.Id}] (Level={unit.Level})",
        unit.Id, "OrganizationUnit");
}
catch { /* audit log failure tidak block response */ }
```

Test harness — HcPortal.Tests/OrganizationControllerTests.cs:
- `MakeController()` (line 19-38): factory existing, passes `null!` untuk `_userManager` (param 2). Helper `GetSuccess(IActionResult)` (40), `GetMessage(IActionResult)` (55). InMemory DB Guid + suppress TransactionIgnoredWarning. JANGAN ubah factory ini — null-userManager-nya adalah mekanisme bukti SC#3/SC#4.
- Analog seed pattern (rename Level1): `EditOrganizationUnit_RenameLevel1_RenamesAllUserUnitsRows` (line 250).
- Analog reparent allowed: `EditOrganizationUnit_ReparentSingleUnitWorker_Allowed` (line 334).

UserManager fake store — HcPortal.Tests/RetakeExamEndpointTests.cs:47-99 (SALIN verbatim):
- `FakeUserStore : IUserStore<ApplicationUser>, IUserRoleStore<ApplicationUser>` (47-72)
- `MakeUserManager(FakeUserStore store)` (89-99)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sisipkan blok audit AUDIT-01 di EditOrganizationUnit</name>
  <files>Controllers/OrganizationController.cs</files>
  <read_first>
    - Controllers/OrganizationController.cs §EditOrganizationUnit (line 129-317) — TARGET; pahami posisi `oldName` (139), `oldParentId` (140), cascade counters (198-200), commit (308), `var msg` (310).
    - Controllers/OrganizationController.cs §DeleteOrganizationUnit (line 539-549) — pola audit kanonik yang DI-MIRROR (signature + swallow + actorName format).
    - .planning/phases/426-audit-log-editorganizationunit/426-RESEARCH.md §3 (kode persis) + §4 (gotchas).
    - .planning/phases/426-audit-log-editorganizationunit/426-CONTEXT.md §decisions (D-01/D-02/D-03).
  </read_first>
  <action>
Sisipkan blok berikut PERSIS setelah `await tx.CommitAsync();` (line 308) dan SEBELUM `var msg = ...` (line 310). Guard berada DI LUAR try (perbandingan murah; tak perlu di-swallow). Try membungkus HANYA actor-resolution + audit I/O. Salin dari RESEARCH.md §3:

```csharp
// AUDIT-01 (Phase 426): mirror DeleteOrganizationUnit audit. Only-on-change (D-01),
// single combined row (D-02), raw parent IDs (D-03). Swallow-on-failure.
if (oldName != name.Trim() || oldParentId != parentId)
{
    try
    {
        var currentUser = await _userManager.GetUserAsync(User);
        var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
            ? (currentUser?.FullName ?? "Unknown")
            : $"{currentUser.NIP} - {currentUser.FullName}";
        await _auditLog.LogAsync(currentUser?.Id ?? "", actorName, "EditOrganizationUnit",
            $"Edited organization unit '{oldName}'→'{name.Trim()}' [ID={unit.Id}] " +
            $"parent {(oldParentId?.ToString() ?? "null")}→{(parentId?.ToString() ?? "null")} " +
            $"(cascade: {cascadedUsers} users, {cascadedMappings} mappings, {cascadedUserUnits} UserUnits)",
            unit.Id, "OrganizationUnit");
    }
    catch { /* audit log failure tidak block response */ }
}
```

Decision fidelity (NON-NEGOTIABLE):
- Per D-01 (only-on-change): guard `if (oldName != name.Trim() || oldParentId != parentId)` — no-op edit (nilai identik) commit sukses TAPI tidak menulis audit. `name.Trim()` konsisten dengan yang di-assign di line 306 (`unit.Name = name.Trim();`).
- Per D-02 (single combined row): SATU pemanggilan `LogAsync`, satu baris — rename+reparent sekaligus tetap satu baris. Description = single string memuat nama lama→baru, parent lama→baru, dan cascade counts.
- Per D-03 (raw parent IDs): tulis ID mentah `oldParentId`/`parentId`; `null` parent render `"null"`. JANGAN query DB untuk resolve nama parent (jaga blok swallow ringan).
- ActionType PERSIS string `"EditOrganizationUnit"` (19 chars, di bawah MaxLength(50)). JANGAN append apa pun ke ActionType.
- `entityId = unit.Id`, `entityType = "OrganizationUnit"`. Actor idiom identik Delete (`{NIP} - {FullName}`, fallback FullName/"Unknown").

JANGAN ubah: atribut endpoint (`[Authorize]`/`[ValidateAntiForgeryToken]`), logika cascade/validasi/return-early, atau urutan commit. Return-early branch (unit null/nama kosong/duplikat/circular/split-block) terjadi SEBELUM commit → by-design tidak menulis audit (benar).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -c Debug --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `OrganizationController.cs` memuat string literal `"EditOrganizationUnit"` di dalam pemanggilan `_auditLog.LogAsync` (grep: `_auditLog\.LogAsync\([^)]*"EditOrganizationUnit"` cocok).
    - `OrganizationController.cs` memuat guard `if (oldName != name.Trim() || oldParentId != parentId)` di dalam method `EditOrganizationUnit`.
    - Blok audit berada SETELAH `await tx.CommitAsync();` dan SEBELUM `var msg =` (grep urutan baris terjaga).
    - Description string memuat substring `cascade:` dan `parent ` (referensi cascade counts + parent IDs).
    - `dotnet build HcPortal.csproj` exit 0 (0 error).
  </acceptance_criteria>
  <done>EditOrganizationUnit menulis satu baris AuditLog ActionType="EditOrganizationUnit" hanya saat ada perubahan nyata, setelah commit, swallow-on-failure; build hijau; tidak ada perubahan perilaku cascade/authz/CSRF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tambah factory user + 5 test cases T1-T5 (AUDIT-01)</name>
  <files>HcPortal.Tests/OrganizationControllerTests.cs</files>
  <read_first>
    - HcPortal.Tests/OrganizationControllerTests.cs — keseluruhan; khususnya `MakeController()` (19-38), helper `GetSuccess`/`GetMessage` (40-59), analog `EditOrganizationUnit_RenameLevel1_RenamesAllUserUnitsRows` (250) + `EditOrganizationUnit_ReparentSingleUnitWorker_Allowed` (334) untuk pola seed.
    - HcPortal.Tests/RetakeExamEndpointTests.cs:47-99 — `FakeUserStore` + `MakeUserManager` (SALIN verbatim, sesuaikan namespace bila perlu).
    - .planning/phases/426-audit-log-editorganizationunit/426-RESEARCH.md §Validation Architecture (tabel T1-T6 + spesifikasi factory `MakeControllerWithUser`).
    - .planning/phases/426-audit-log-editorganizationunit/426-VALIDATION.md (Wave 0 + per-task map).
  </read_first>
  <behavior>
    - T1 (SC#1): rename Level1 → tepat 1 baris ActionType="EditOrganizationUnit", TargetId==unitId, TargetType=="OrganizationUnit", ActorName=="{NIP} - {FullName}" actor ter-seed, Description memuat `'OldName'→'NewName'`.
    - T2 (SC#1+SC#2): reparent single-unit worker (jalur allowed, mirror `...ReparentSingleUnitWorker_Allowed`) → 1 baris; Description memuat `parent {oldId}→{newId}` (raw IDs, D-03).
    - T3 (D-02): rename+reparent gabungan → `Count == 1` (BUKAN 2) — bukti single combined row.
    - T4 (D-01): EditOrganizationUnit(id, sameName, sameParent) → `GetSuccess==true` DAN `Count(ActionType=="EditOrganizationUnit")==0` — bukti only-on-change.
    - T5 (SC#3): pakai factory EXISTING null-`_userManager` `MakeController()`; lakukan rename nyata; assert `GetSuccess==true` DAN baris UserUnits/Users ter-rename (cascade sukses) — bukti swallow-on-failure (audit lempar NRE, respons tak terblokir).
    - Regression (SC#4): test existing `EditOrganizationUnit_RenameLevel1_RenamesAllUserUnitsRows` + `...ReparentSingleUnitWorker_Allowed` + `PreviewEditCascade_*` tetap GREEN (swallow melindungi harness null-userManager).
  </behavior>
  <action>
1. Tambahkan ke `OrganizationControllerTests` (private static, di dalam class):
   - `FakeUserStore : IUserStore<ApplicationUser>, IUserRoleStore<ApplicationUser>` — SALIN verbatim dari `RetakeExamEndpointTests.cs:47-72`.
   - `MakeUserManager(FakeUserStore store)` — SALIN verbatim dari `RetakeExamEndpointTests.cs:89-99`.
   - Tambahkan using yang diperlukan: `System.Security.Claims`, `System.Threading`, `Microsoft.AspNetCore.Identity`, `Microsoft.Extensions.Logging.Abstractions`, `Microsoft.Extensions.Options` (cek mana yang belum ada di header file; jangan duplikat).
   - Factory baru `MakeControllerWithUser(ApplicationUser actor)` → `(OrganizationController ctrl, ApplicationDbContext ctx)`:
     - `var store = new FakeUserStore(); store.Add(actor);`
     - `var um = MakeUserManager(store);`
     - bangun `ctx` InMemory Guid + `ConfigureWarnings(...Ignore(InMemoryEventId.TransactionIgnoredWarning))` (sama seperti `MakeController()`).
     - `new OrganizationController(ctx, um, new AuditLogService(ctx), null!)`.
     - `httpContext.User = new ClaimsPrincipal(new ClaimsIdentity(new[]{ new Claim(ClaimTypes.NameIdentifier, actor.Id) }, "TestAuth"));`
     - set header `X-Requested-With: XMLHttpRequest` (→ respons JSON, reuse `GetSuccess`).

2. Tambahkan 5 test `[Fact]` dengan nama PERSIS (dipakai grep verifikasi):
   - `EditOrganizationUnit_RenameLevel1_WritesOneAuditRow` (T1)
   - `EditOrganizationUnit_Reparent_WritesParentIdsInDescription` (T2)
   - `EditOrganizationUnit_RenameAndReparent_WritesExactlyOneRow` (T3)
   - `EditOrganizationUnit_NoChange_WritesZeroAuditRows` (T4)
   - `EditOrganizationUnit_AuditFailure_DoesNotBlockEdit` (T5)
   - Seed actor: `new ApplicationUser { Id="hc1", UserName="hc1", NIP="99001", FullName="Admin HC" }` → ActorName harapan = `"99001 - Admin HC"`.
   - Read-back audit: `ctx.AuditLogs.Where(a => a.ActionType == "EditOrganizationUnit").ToList()`.
   - Pola seed unit/UserUnits ambil dari analog (rename Level1: line 250-263; reparent allowed: line 334-343).
   - T4 & T5 catatan: T4 gunakan `MakeControllerWithUser` lalu Edit dengan nilai identik (id, sameName, sameParent) → 0 baris audit. T5 gunakan `MakeController()` existing (null userManager), rename nyata → assert sukses + cascade ter-rename.

3. JANGAN ubah `MakeController()` existing atau test existing apa pun — itu adalah regression guard SC#4 (T6).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~OrganizationControllerTests" --nologo 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `OrganizationControllerTests.cs` memuat `EditOrganizationUnit_RenameLevel1_WritesOneAuditRow`, `EditOrganizationUnit_Reparent_WritesParentIdsInDescription`, `EditOrganizationUnit_RenameAndReparent_WritesExactlyOneRow`, `EditOrganizationUnit_NoChange_WritesZeroAuditRows`, `EditOrganizationUnit_AuditFailure_DoesNotBlockEdit` (grep masing-masing nama cocok).
    - `OrganizationControllerTests.cs` memuat `FakeUserStore`, `MakeUserManager`, dan `MakeControllerWithUser`.
    - `OrganizationControllerTests.cs` memuat read-back `ctx.AuditLogs` ter-filter `ActionType == "EditOrganizationUnit"`.
    - `dotnet test --filter "FullyQualifiedName~OrganizationControllerTests"` exit 0 (semua test class hijau, termasuk 5 baru + regression existing).
  </acceptance_criteria>
  <done>5 test T1-T5 + factory user-aware ditambahkan; semua test OrganizationControllerTests hijau (5 baru + regression existing); AUDIT-01 SC#1-SC#4 punya minimal satu discriminating test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Admin/HC) → POST /Admin/EditOrganizationUnit | Input rename/reparent unit. Sudah `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` (TIDAK diubah oleh fase ini). |
| controller → AuditLog DB | Penulisan baris audit. Description di-construct SERVER-SIDE (string interpolation atas variabel server: oldName, name.Trim(), unit.Id, parent IDs, cascade counts). Actor di-resolve server-side dari principal terautentikasi. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-426-01 | Tampering | Description AuditLog (data masuk dari `name` input HC) | accept | Description disimpan sebagai data teks (kolom `Description`), TIDAK dipakai sebagai HTML/SQL sink; tidak ada eksekusi/render-HTML atas string ini di blok audit. EF parameterized (no SQL injection). Sama-aman dengan pola Delete existing yang sudah ship. |
| T-426-02 | Repudiation | Actor identity pada baris audit | mitigate | Actor di-resolve server-side via `_userManager.GetUserAsync(User)` (principal terautentikasi), BUKAN dari input client. ActorUserId+ActorName+CreatedAt(UtcNow) di-stamp service. Justru fase ini MENGURANGI repudiation (menutup gap: Edit sebelumnya tak ber-audit). |
| T-426-03 | Denial of Service | Blok audit menambah satu SaveChanges I/O | accept | I/O ekstra minor (satu insert). Swallow-on-failure: kegagalan audit (mis. DB hiccup) di-catch dan tidak memblokir/menggagalkan respons sukses edit (SC#3). Tidak ada amplifikasi (satu baris per Edit ber-perubahan, only-on-change D-01). |
| T-426-04 | Elevation of Privilege | Surface authz/CSRF | accept (no new surface) | Fase murni aditif-traceability: TIDAK menambah endpoint, parameter, atau jalur authz baru. `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` existing tetap utuh (Task 1 melarang ubah atribut). Tidak ada elevasi baru. |

**Ringkasan:** Tidak ada threat High. Perubahan murni aditif-traceability tanpa external input baru, tanpa surface authz/CSRF baru. Description server-constructed (bukan user-controlled HTML sink); actor server-resolved dari principal terautentikasi. Blok audit swallow-on-failure → tak dapat memengaruhi response edit.
</threat_model>

<verification>
1. `dotnet build HcPortal.csproj -c Debug` → 0 error (Task 1).
2. `dotnet test HcPortal.Tests --filter "FullyQualifiedName~OrganizationControllerTests"` → semua hijau: 5 test baru (T1-T5) + regression existing (`...RenamesAllUserUnitsRows`, `...ReparentSingleUnitWorker_Allowed`, `PreviewEditCascade_*`) = SC#4 guard.
3. Grep konfirmasi: `_auditLog.LogAsync(...)` dengan `"EditOrganizationUnit"` hadir di `OrganizationController.cs`; guard `oldName != name.Trim() || oldParentId != parentId` hadir.
4. (Opsional sebelum complete) Full suite filter `Category!=Integration` hijau untuk memastikan tak ada regresi lintas-area.
5. migration=FALSE — tidak ada perubahan schema/snapshot; tidak ada `dotnet ef` yang perlu dijalankan.
</verification>

<success_criteria>
- SC#1 (AUDIT-01): rename/reparent via `EditOrganizationUnit` → satu baris `AuditLog` ActionType="EditOrganizationUnit" + actor benar + ringkasan oldName→newName + oldParentId→parentId. (T1, T2, T3)
- SC#2 (AUDIT-01): baris audit memuat cascade counts (cascadedUsers/cascadedMappings/cascadedUserUnits) di Description. (T1, T2 assert substring counts/parent)
- SC#3 (AUDIT-01): audit ditulis SETELAH `tx.CommitAsync()` + swallow-on-failure → kegagalan audit tak memblokir respons sukses. (T5 — null userManager → NRE di-swallow → edit tetap sukses)
- SC#4: edit valid tetap berhasil, cascade ph403 tak berubah, authz/CSRF utuh. (T6 regression: test existing tetap GREEN)
- D-01 only-on-change: no-op edit → 0 baris audit. (T4)
- D-02 single combined row: rename+reparent → tepat 1 baris. (T3)
- D-03 raw parent IDs: Description memuat `parent {oldId}→{newId}` mentah, `null` render "null". (T2)
- migration=FALSE: tidak ada schema/kolom/snapshot baru.
</success_criteria>

<output>
After completion, create `.planning/phases/426-audit-log-editorganizationunit/426-01-SUMMARY.md`
</output>
