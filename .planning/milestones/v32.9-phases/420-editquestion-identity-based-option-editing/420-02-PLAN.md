---
phase: 420-editquestion-identity-based-option-editing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/ManagePackageQuestions.cshtml
autonomous: true
requirements: [OPTEDIT-05]
tags: [authoring, view, razor, javascript]

must_haves:
  truths:
    - "Tiap baris opsi punya hidden carrier name=options[i].Id yang ikut di-reindex saat tambah/hapus baris"
    - "Saat buka modal edit, hidden Id tiap baris terisi dari GET JSON opt.id (baris padding di luar opts.length → Id kosong)"
    - "Baris BARU dari Tambah Opsi → hidden Id KOSONG (clone reset bersihkan type=hidden) → ADD path, BUKAN overwrite opsi A"
    - "Hapus baris → node hilang → Id-nya absen dari submit → kandidat removed (correct by construction)"
  artifacts:
    - path: "Views/Admin/ManagePackageQuestions.cshtml"
      provides: "Hidden options[i].Id per baris + reindex + populate + clone-reset clear hidden"
      contains: "opt-id-input"
  key_links:
    - from: "GET JSON opt.id"
      to: "hidden option_{L}_id value"
      via: "populateEditForm set idEl.value"
      pattern: "option_'\\s*\\+\\s*L\\s*\\+\\s*'_id"
    - from: "reletterRows reindex"
      to: "hidden name options[i].Id"
      via: "idInput.name rename"
      pattern: "options\\['\\s*\\+\\s*i\\s*\\+\\s*'\\]\\.Id"
    - from: "addOptionRow clone reset"
      to: "hidden Id cleared"
      via: "if (inp.type === 'hidden') inp.value = ''"
      pattern: "type === 'hidden'"
---

<objective>
Tambah hidden carrier `options[i].Id` per baris opsi di form authoring `ManagePackageQuestions.cshtml`, dan pastikan JS reindex/populate/clone menangani Id dengan benar: reletterRows me-rename name+id hidden (value preserved), populateEditForm mengisi Id dari GET JSON `opt.id`, dan — **GOTCHA TERTINGGI** — `addOptionRow` clone-reset membersihkan input `type='hidden'` agar baris baru tidak mewarisi Id baris[0].

Purpose: Form mengirim `options[i].Id` ke controller (Plan 01) untuk identity-matching. Tanpa carrier ini, controller jatuh ke contract legacy (semua Id null → semua existing jadi removed-candidate → over-block). Tanpa clone-reset fix, baris opsi baru diam-diam mewarisi Id opsi A → meng-UPDATE A alih-alih ADD opsi baru (RESEARCH §2c — risiko implementasi tertinggi).

Output: `Views/Admin/ManagePackageQuestions.cshtml` (hidden Id per baris + 3 patch JS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/420-editquestion-identity-based-option-editing/420-CONTEXT.md
@.planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Struktur DOM + JS yang executor patch. Kutipan persis dari Views/Admin/ManagePackageQuestions.cshtml. -->

Template baris opsi (`:403-439`) — id berbasis letter, name berbasis index. Tambah hidden Id DI SINI:
```html
<div class="option-row" data-option-row data-index="@i">
    <div class="input-group input-group-sm mb-2">
        <div class="input-group-text" style="width:36px">
            <input class="form-check-input mt-0 correct-input" type="radio"
                   name="correctIndex" id="correct_@letter" value="@i" aria-label="Opsi @letter benar" />
        </div>
        <span class="input-group-text fw-bold" data-letter>@letter</span>
        <input type="text" class="form-control" name="options[@i].Text"
               id="option_@letter" placeholder="Opsi @letter" aria-label="Teks opsi @letter" />
        <button type="button" class="btn btn-outline-danger remove-option-btn ...">...</button>
    </div>
    <!-- blok gambar opsi ... -->
</div>
```

reletterRows() (`:769-835`) — me-rename input teks per baris (POLA yang ditiru untuk hidden Id):
```javascript
var txt = row.querySelector('input[type=text].form-control');
if (txt) {
    txt.name = 'options[' + i + '].Text';
    txt.id = 'option_' + L;
    ...
}
```

populateEditForm() loop opts (`:713-722`) — isi tiap baris dari GET JSON (TAMBAH set hidden Id di sini):
```javascript
opts.forEach(function (opt, i) {
    var L = LETTERS[i];
    var textEl = document.getElementById('option_' + L);
    if (textEl) textEl.value = opt.optionText || '';
    var correctEl = document.getElementById('correct_' + L);
    if (correctEl) correctEl.checked = opt.isCorrect === true;
    prefillImage(IMAGE_FIELDS[i + 1], opt.imagePath, opt.imageAlt);
});
```

addOptionRow() clone-reset loop (`:843-848`) — ⚠️ HANYA reset text/radio/checkbox/file, BUKAN hidden:
```javascript
clone.querySelectorAll('input').forEach(function (inp) {
    if (inp.type === 'text') inp.value = '';
    if (inp.type === 'radio' || inp.type === 'checkbox') inp.checked = false;
    if (inp.type === 'file') inp.value = '';
    inp.removeAttribute('data-wired');
});
```
LETTERS = ['A','B','C','D','E','F']; MIN_OPTIONS=2; MAX_OPTIONS=6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah hidden options[i].Id per baris + reletterRows rename hidden Id</name>
  <files>Views/Admin/ManagePackageQuestions.cshtml</files>
  <read_first>
    - Views/Admin/ManagePackageQuestions.cshtml:398-446 (template #optionRows + tombol Tambah Opsi)
    - Views/Admin/ManagePackageQuestions.cshtml:762-835 (optionRowEls + reletterRows)
    - .planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md §2c
  </read_first>
  <action>
    **PATCH A — template baris opsi (`:403-439`).** Tambahkan satu hidden input DI DALAM `.option-row`, di awal `<div class="input-group...">` (sebelum `correct-input` radio div). Markup persis:
    ```html
    <input type="hidden" class="opt-id-input" name="options[@i].Id" id="option_@(letter)_id" value="" />
    ```
    Letakkan sebagai child PERTAMA `.option-row` (di atas `<div class="input-group input-group-sm mb-2">`) sehingga ikut ter-clone oleh `addOptionRow` (`rows[0].cloneNode(true)`).

    **PATCH B — reletterRows() (`:769-835`).** Setelah blok yang me-rename input teks (`var txt = ...; if (txt) { txt.name = 'options[' + i + '].Text'; ... }`, ~`:779-785`), TAMBAHKAN blok rename hidden Id (value TIDAK disentuh — reletter hanya rename):
    ```javascript
    // Phase 420: hidden carrier identity — rename name+id ikut posisi (value preserved, tak di-clear).
    var idInput = row.querySelector('.opt-id-input');
    if (idInput) {
        idInput.name = 'options[' + i + '].Id';
        idInput.id = 'option_' + L + '_id';
    }
    ```
    JANGAN ubah logika lain di reletterRows (correct-input, remove-btn, blok gambar, toggle addBtn, IMAGE_FIELDS rebuild) — biarkan apa adanya.
  </action>
  <acceptance_criteria>
    - `grep -n "opt-id-input" Views/Admin/ManagePackageQuestions.cshtml` returns ≥2 matches (template + reletterRows selector)
    - `grep -n "options\[@i\].Id" Views/Admin/ManagePackageQuestions.cshtml` returns 1 match (template hidden)
    - `grep -n "idInput.name = 'options\[' + i + '\].Id'" Views/Admin/ManagePackageQuestions.cshtml` returns 1 match (reletter rename)
    - `grep -n "option_@(letter)_id" Views/Admin/ManagePackageQuestions.cshtml` returns 1 match
    - `dotnet build` exits 0 (Razor compile OK)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>Setiap baris opsi punya hidden `options[i].Id` (id `option_{letter}_id`, value awal kosong); reletterRows me-rename name+id hidden mengikuti posisi tanpa mengubah value; Razor compile hijau.</done>
</task>

<task type="auto">
  <name>Task 2: populateEditForm isi hidden Id dari GET JSON + addOptionRow clone-reset bersihkan hidden (GOTCHA §2c)</name>
  <files>Views/Admin/ManagePackageQuestions.cshtml</files>
  <read_first>
    - Views/Admin/ManagePackageQuestions.cshtml:708-731 (populateEditForm loop opts)
    - Views/Admin/ManagePackageQuestions.cshtml:837-863 (addOptionRow clone-reset)
    - Views/Admin/ManagePackageQuestions.cshtml:865-891 (removeOptionRow + ensureRowCount — konfirmasi tak butuh patch)
    - .planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md §2c (clone gotcha)
  </read_first>
  <action>
    **PATCH C — populateEditForm() loop opts (`:713-722`).** Di dalam `opts.forEach(function (opt, i) { ... })`, TAMBAHKAN baris yang mengisi hidden Id dari `opt.id` (field baru di GET JSON dari Plan 01). Letakkan setelah set `textEl.value`:
    ```javascript
    // Phase 420: isi hidden carrier identity dari GET JSON (opt.id). Baris padding di luar opts.length → Id kosong (baru).
    var idEl = document.getElementById('option_' + L + '_id');
    if (idEl) idEl.value = (opt.id != null) ? String(opt.id) : '';
    ```
    (Baris yang di-`ensureRowCount` melebihi `opts.length` tidak masuk loop ini → hidden Id-nya tetap value awal kosong = baris baru. Benar by construction.)

    **PATCH D — addOptionRow() clone-reset (`:843-848`) — ⚠️ GOTCHA TERTINGGI (RESEARCH §2c).** Di dalam `clone.querySelectorAll('input').forEach(function (inp) { ... })`, TAMBAHKAN penanganan `type==='hidden'` agar baris baru TIDAK mewarisi Id baris[0]:
    ```javascript
    if (inp.type === 'hidden') inp.value = '';
    ```
    Letakkan sebaris dengan reset text/radio/checkbox/file yang sudah ada. Tanpa baris ini, opsi baru akan diam-diam meng-UPDATE opsi A (Id baris[0] terbawa) alih-alih ADD opsi baru — bug yang HARUS ditangkap Playwright add-option test (Plan 03).

    Konfirmasi (TANPA patch): `removeOptionRow` (`:866-878`) menghapus node utuh → hidden Id-nya hilang dari submit otomatis (kandidat removed) — benar by construction. `ensureRowCount` (`:881-891`) memakai addOptionRow/removeChild + reletterRows — sudah ter-cover patch di atas. JANGAN ubah keduanya.
  </action>
  <acceptance_criteria>
    - `grep -n "option_' + L + '_id" Views/Admin/ManagePackageQuestions.cshtml` returns ≥2 matches (reletter + populate)
    - `grep -n "(opt.id != null)" Views/Admin/ManagePackageQuestions.cshtml` returns 1 match (populate)
    - `grep -n "inp.type === 'hidden'" Views/Admin/ManagePackageQuestions.cshtml` returns 1 match (clone-reset gotcha)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>populateEditForm mengisi hidden Id dari `opt.id` (null → kosong); addOptionRow clone-reset membersihkan input hidden (baris baru → Id kosong → ADD, bukan overwrite A); removeOptionRow/ensureRowCount tak diubah (benar by construction); build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM/JS → form submit | Hidden `options[i].Id` di-render & di-reindex client-side; value akhir dikirim ke EditQuestion POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-420-04 | Tampering (silent data corruption) | addOptionRow clone mewarisi Id baris[0] | mitigate | Clone-reset `if (inp.type === 'hidden') inp.value = ''` → baris baru Id kosong → ADD, bukan UPDATE opsi A. Playwright add-option-then-save (Plan 03) mengunci. |
| T-420-01 | Tampering (client-supplied Id) | hidden Id dikirim ke server | transfer | Validasi anti-tamper (Id ∈ q.Options, fail-closed) di server (Plan 01 T-420-01). View hanya carrier; server otoritatif — client tak boleh dipercaya. |
| T-420-06 | Information disclosure (XSS) | hidden Id value | accept | Value = integer Id dari GET JSON via `String(opt.id)`; bukan HTML sink (set via `.value`, bukan innerHTML). Tak ada teks bebas pengguna di carrier ini. |
</threat_model>

<verification>
- `dotnet build` hijau (Razor compile).
- Perilaku reindex/clone/populate diverifikasi real-browser oleh Playwright di Plan 03 (controller test tak bisa menguji reindex client).
- Sanity grep: `opt-id-input` ada di template + reletter; `inp.type === 'hidden'` ada di clone-reset; `(opt.id != null)` ada di populate.
</verification>

<success_criteria>
- Hidden `options[i].Id` per baris opsi (template), di-reindex reletterRows.
- populateEditForm mengisi Id dari GET JSON `opt.id`.
- addOptionRow clone-reset membersihkan hidden → baris baru ADD (bukan overwrite A).
- Build hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/420-editquestion-identity-based-option-editing/420-02-SUMMARY.md`
</output>
