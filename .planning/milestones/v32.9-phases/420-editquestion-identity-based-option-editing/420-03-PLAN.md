---
phase: 420-editquestion-identity-based-option-editing
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - HcPortal.Tests/EditShrinkGuardIntegrationTests.cs
  - tests/e2e/identity-option-edit-420.spec.ts
autonomous: false
requirements: [VRF-01, OPTEDIT-05]
tags: [test, integration, playwright, uat, data-integrity]

must_haves:
  truths:
    - "Integration test mereproduksi relabel-senyap (hapus opsi TENGAH terjawab) lalu membuktikan kini DIBLOKIR (RED-on-main → GREEN)"
    - "Hapus opsi tengah BELUM dijawab → sukses, opsi tersisa TIDAK ter-relabel (match by Id)"
    - "Edit teks/kebenaran opsi terjawab → record by Id ter-update, response peserta tetap merujuk opsi sama"
    - "Regression-lock 999.14: konversi MC→Essay + shrink opsi terjawab → diblokir TANPA DbUpdateException 500"
    - "Anti-tamper: OptionId asing (opsi soal/paket lain) → seluruh edit ditolak, 0 mutasi"
    - "Tambah opsi (Id null) → opsi BARU di-ADD, opsi A TIDAK ter-overwrite (clone gotcha §2c)"
    - "Pure-helper EditShrinkGuardLogicTests tetap hijau (signature OptionShrinkGuard tak berubah)"
    - "Playwright real-browser: delete-middle-answered → pesan ramah; add-option-then-save → opsi baru (lesson 354)"
  artifacts:
    - path: "HcPortal.Tests/EditShrinkGuardIntegrationTests.cs"
      provides: "TEST1/TEST2 di-port ke identity contract + 6 test identity baru"
      contains: "Id = optionIds"
    - path: "tests/e2e/identity-option-edit-420.spec.ts"
      provides: "UAT real-browser form authoring identity edit"
      contains: "identity"
  key_links:
    - from: "EditQuestion POST identity upsert (Plan 01)"
      to: "integration test asserts no-relabel + blocked"
      via: "ctrl.EditQuestion with OptionInput.Id"
      pattern: "Id = optionIds\\["
    - from: "hidden options[i].Id carrier (Plan 02)"
      to: "Playwright add-option-then-save asserts new option not overwrite A"
      via: "browser form submit"
      pattern: "identity-option-edit-420"
---

<objective>
Membuktikan fix identity-based (Plan 01 + 02) lewat test: (1) UPDATE 2 test integrasi existing ke identity contract, (2) tambah 6 test integrasi identity baru (RESEARCH §4), (3) verifikasi pure-helper tetap hijau, (4) Playwright UAT real-browser (lesson 354). Memenuhi VRF-01 + regresi OPTEDIT-05.

Purpose: Mengunci bahwa hapus/edit opsi (termasuk TENGAH) pada soal terjawab tak lagi me-relabel jawaban peserta senyap, guard menyala posisi manapun, anti-tamper fail-closed, dan alur existing (add opsi, CreateQuestion) tak regresi.

Output: `HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` (port + tests baru) + `tests/e2e/identity-option-edit-420.spec.ts` (UAT).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/420-editquestion-identity-based-option-editing/420-CONTEXT.md
@.planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md
@.planning/phases/420-editquestion-identity-based-option-editing/420-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Harness existing yang DIPAKAI ULANG apa adanya. Test identity baru DITAMBAHKAN ke class yang sama
     (hindari duplikasi harness 90-baris). RESEARCH §4 menyebut file baru IdentityOptionEditTests.cs —
     DEVIASI sengaja: tulis ke class existing untuk reuse harness + seed helper. -->

HcPortal.Tests/EditShrinkGuardIntegrationTests.cs sudah punya (REUSE):
- [Trait("Category","Integration")] class EditShrinkGuardIntegrationTests : IClassFixture<SectionFixture>
- private AssessmentAdminController MakeController(ctx, actor, actionName)  // harness lengkap (StubUserManager/UserStore/WebHostEnv/TempData/NoopHubContext)
- SeedActorAsync(ctx) → ApplicationUser
- SeedSessionPackageAsync(ctx, name) → (sessionId, packageId)
- SeedFourOptionQuestionAsync(ctx, sessionId, packageId) → (questionId, List<int> optionIds [OrderBy Id], sessionId)
  // soal MC 4-opsi A(correct),B,C,D

Signature EditQuestion POST (yang dipanggil test):
  Task<IActionResult> EditQuestion(int questionId, int packageId, string questionText, string questionType,
    int scoreValue, string? elemenTeknis, string? rubrik, int maxCharacters,
    IFormFile? questionImage, string? questionImageAlt, bool removeQuestionImage,
    List<OptionInput> options, int? correctIndex = null, int? sectionId = null)

OptionInput (SETELAH Plan 01): { int? Id; string? Text; bool IsCorrect; IFormFile? Image; string? ImageAlt; bool RemoveImage }

Playwright: project di tests/ (tests/playwright.config.ts), spec di tests/e2e/. Analog terdekat: tests/e2e/option-dynamic-418.spec.ts (authoring opsi dinamis). Login admin@pertamina.com (memory reference_dev_credentials). App @ http://localhost:5277. Combined run --workers=1 (memory reference_local_e2e_sql_env_fix).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port TEST1/TEST2 ke identity contract + tambah 6 test identity baru (+ verifikasi pure-helper hijau)</name>
  <files>HcPortal.Tests/EditShrinkGuardIntegrationTests.cs</files>
  <read_first>
    - HcPortal.Tests/EditShrinkGuardIntegrationTests.cs (seluruh file — harness + TEST1/TEST2 + seed helpers)
    - HcPortal.Tests/EditShrinkGuardLogicTests.cs (pure helper — KONFIRMASI tak perlu diubah)
    - .planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md §3 (backward-compat) + §4 (daftar 6 test)
    - Controllers/AssessmentAdminController.cs:7923-8262 (EditQuestion POST identity — hasil Plan 01)
  </read_first>
  <behavior>
    - TEST 1 (port): hapus opsi TENGAH B (answered) via identity (kirim A,C,D dgn Id, omit B) → diblokir, pesan "sudah dijawab" + "B", B utuh, 4 opsi tetap, no-throw.
    - TEST 2 (port): hapus opsi D (unanswered) via identity (kirim A,B,C dgn Id, omit D) → sukses, D terhapus, 3 opsi A,B,C, A response utuh.
    - Baru #2: hapus tengah B (UNanswered, answered=A) → sukses, B terhapus, opsi tersisa A,C,D Id+teks UTUH (C masih "C", tak ter-relabel jadi "B").
    - Baru #3: edit answered B → ubah teks "B"→"B-rev" + flip correctIndex (kirim 4 dgn Id) → sukses, B.Id stabil, teks ter-update, response→B.Id utuh.
    - Baru #4a: konversi answered-soal MC→Essay → diblokir (removedOptionIds=semua, B answered) no-throw no-500.
    - Baru #4b: shrink answered B dari ekor-pattern (kirim hanya A dgn Id + C,D dgn Id, omit B) identik konsep #1 tapi tegaskan no DbUpdateException.
    - Baru #5: anti-tamper — kirim row Id = opsi milik soal/paket LAIN → ditolak "tidak valid", 0 mutasi (DB soal asli utuh).
    - Baru #6: tambah opsi — kirim 4 existing (dgn Id) + 1 row Id null teks "E" → 5 opsi, E ter-ADD, A..D Id utuh.
  </behavior>
  <action>
    **PATCH A — port TEST 1** (`EditShrinkGuard_AnsweredOption_NotRemoved_NoException`). Ganti payload `List<OptionInput>` (yang saat ini A,"",C,D tanpa Id) menjadi identity contract: kirim A,C,D DENGAN Id, OMIT baris B (B = optionIds[1], opsi tengah yang answered). Ganti komentar mekanika shrink lama. Payload:
    ```csharp
    new List<OptionInput>
    {
        new OptionInput { Id = optionIds[0], Text = "A" },
        new OptionInput { Id = optionIds[2], Text = "C" },   // B (optionIds[1]) DIOMIT → removed candidate
        new OptionInput { Id = optionIds[3], Text = "D" },
    },
    correctIndex: 0,
    ```
    Assertion tetap: `Assert.Null(ex)`, redirect ManagePackageQuestions, error not-empty, `Assert.Contains("sudah dijawab", error!)`, `Assert.Contains("B", error!)`. Verify DB: 4 opsi tetap, opsi B (optionIds[1]) text masih "B", response→B masih ada.

    **PATCH B — port TEST 2** (`EditShrinkGuard_UnansweredOption_Removed_Succeeds`). answered=A (optionIds[0]). Hapus D via identity: kirim A,B,C DENGAN Id, OMIT D (optionIds[3]). Payload:
    ```csharp
    new List<OptionInput>
    {
        new OptionInput { Id = optionIds[0], Text = "A" },
        new OptionInput { Id = optionIds[1], Text = "B" },
        new OptionInput { Id = optionIds[2], Text = "C" },   // D (optionIds[3]) DIOMIT → removed (unanswered → boleh)
    },
    correctIndex: 0,
    ```
    Assertion tetap: no-throw, redirect, TempData["Error"] kosong (sukses); DB 3 opsi A,B,C, D (optionIds[3]) hilang, response→A utuh.

    **PATCH C — tambah 6 [Fact] baru** ke class yang sama (setelah TEST 2). Pakai seed helper existing. Bodi ringkas (ikut pola TEST1/2 — 3 blok: seed / act via MakeController+EditQuestion / verify NewCtx). Daftar + inti assert:

    1. `MiddleDelete_Unanswered_NoRelabel_Succeeds` (OPTEDIT-01): seed 4-opt, answer A (optionIds[0]); hapus tengah B (kirim A,C,D dgn Id, omit B). Assert sukses; DB 3 opsi; OrderBy(Id) → Id [0,2,3] dgn teks ["A","C","D"] (BUKAN ter-shift jadi ["A","B","C"]); opsi C (optionIds[2]) teks masih "C".
    2. `EditAnsweredOption_TextAndCorrectness_UpdatesById` (OPTEDIT-03): seed 4-opt, answer B (optionIds[1]); kirim 4 dgn Id, B.Text="B-rev", correctIndex=1 (B benar). Assert sukses; DB opsi optionIds[1] OptionText=="B-rev" & IsCorrect==true; response.PackageOptionId masih == optionIds[1] (identitas semantik utuh).
    3. `ConvertAnsweredMcToEssay_Blocked_NoException` (OPTEDIT-04a): seed 4-opt, answer B; EditQuestion questionType="Essay", rubrik="kunci", options=empty list (atau 4 dgn Id — Essay branch hapus semua). Assert `Assert.Null(ex)` (no 500), redirect+error "sudah dijawab"; DB soal masih MultipleChoice 4 opsi (tak terkonversi), response→B utuh.
    4. `AntiTamper_ForeignOptionId_Rejected_NoMutation` (T-420-01/D-01a): seed soal-A 4-opt (questionId1) + soal-B di paket lain (SeedSessionPackageAsync+SeedFourOptionQuestion → optionIdsOther). Edit soal-A kirim row dgn Id = optionIdsOther[0] (asing). Assert redirect + error "tidak valid"; DB soal-A 4 opsi utuh (0 mutasi), teks tak berubah.
    5. `AddOption_NullId_Adds_NotOverwriteExisting` (OPTEDIT-05): seed 4-opt unanswered; kirim 4 existing dgn Id + 1 row `new OptionInput { Text = "E" }` (Id null). MA? pakai MultipleChoice correctIndex=0 (perlu ≥? validator: MC butuh tepat 1 benar). Assert sukses; DB 5 opsi; opsi baru teks "E"; opsi A (optionIds[0]) teks masih "A" (tak ter-overwrite).
    6. `DuplicateSubmittedId_Rejected` (T-420-02): seed 4-opt; kirim 2 row dgn Id sama (optionIds[0] dua kali) + lainnya. Assert redirect + error "duplikat"; DB utuh.

    Catatan: untuk soal MC, sertakan `correctIndex` yang valid (tepat 1 benar) agar lolos validator per-type sebelum guard. Untuk konversi Essay (#3) tak perlu correctIndex. Pakai `Record.ExceptionAsync` untuk semua agar buktikan no-500.
  </action>
  <acceptance_criteria>
    - `grep -c "Id = optionIds\[" HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` returns ≥6 (port + tests baru pakai Id)
    - `grep -n "MiddleDelete_Unanswered_NoRelabel_Succeeds" HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` returns 1
    - `grep -n "EditAnsweredOption_TextAndCorrectness_UpdatesById" HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` returns 1
    - `grep -n "ConvertAnsweredMcToEssay_Blocked_NoException" HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` returns 1
    - `grep -n "AntiTamper_ForeignOptionId_Rejected_NoMutation" HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` returns 1
    - `grep -n "AddOption_NullId_Adds_NotOverwriteExisting" HcPortal.Tests/EditShrinkGuardIntegrationTests.cs` returns 1
    - `dotnet build HcPortal.Tests` exits 0
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~EditShrinkGuardIntegrationTests"` exits 0 (butuh SQLEXPRESS)
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~EditShrinkGuardLogicTests"` exits 0 (pure helper tetap hijau)
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~EditShrinkGuard"</automated>
  </verify>
  <done>TEST1/TEST2 di-port ke identity contract (pass Id, omit deleted row); 6 test identity baru ditambahkan (OPTEDIT-01/03/04/05 + anti-tamper + duplikat); pure-helper EditShrinkGuardLogicTests tetap hijau; full filter green di SQLEXPRESS.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 2: Playwright UAT real-browser — identity option edit (SEED_WORKFLOW snapshot→test→restore)</name>
  <files>tests/e2e/identity-option-edit-420.spec.ts</files>
  <read_first>
    - tests/e2e/option-dynamic-418.spec.ts (analog authoring opsi dinamis — pola login/navigate/edit form)
    - tests/playwright.config.ts (baseURL, project)
    - Views/Admin/ManagePackageQuestions.cshtml (hasil Plan 02 — hidden Id + reletter + clone-reset)
    - docs/SEED_WORKFLOW.md (snapshot→seed→restore) + CLAUDE.md
  </read_first>
  <action>
    Tulis spec `tests/e2e/identity-option-edit-420.spec.ts` (pola dari option-dynamic-418.spec.ts). Skenario real-browser @5277 (login admin@pertamina.com):

    1. **delete-middle-answered → pesan ramah:** prasyarat seed (SQL via SEED_WORKFLOW, BUKAN UI): 1 sesi+paket+soal MC 4-opsi + `PackageUserResponse` menjawab opsi B (tengah). Di browser: buka ManagePackageQuestions paket itu → klik edit soal → klik tombol hapus baris opsi B (tengah) → Simpan → assert muncul pesan error memuat "sudah dijawab" + "B" (TempData error / alert). Verify (SQL): opsi B masih ada, teks "B".
    2. **delete-middle-unanswered → no-relabel:** soal MC 4-opsi BELUM dijawab → hapus opsi tengah B → Simpan → sukses; reload form edit → assert opsi tersisa = A,C,D dengan teks benar (C BUKAN ter-relabel). (Atau verify via SQL: 3 opsi, teks ["A","C","D"].)
    3. **add-option-then-save (GOTCHA §2c):** soal MC 4-opsi → klik "Tambah Opsi" → isi teks "E" → set salah satu benar valid → Simpan → assert 5 opsi tersimpan; opsi A teks TETAP "A" (BUKAN ter-overwrite oleh opsi baru). Verify SQL: 5 PackageOption, opsi A.Id stabil teks "A", opsi baru teks "E".

    WAJIB SEED_WORKFLOW: snapshot DB lokal (sqlcmd BACKUP) SEBELUM seed; catat docs/SEED_JOURNAL.md (tujuan=UAT 420, temporary+local-only); RESTORE DB setelah test (sukses/gagal); tandai journal cleaned. Combined run `--workers=1`.

    Karena butuh app live @5277 + SQLEXPRESS + seed/restore manual → task ini autonomous:false (dijalankan di gerbang UAT milestone-autopilot, hasil tulis 420-UAT.md).
  </action>
  <acceptance_criteria>
    - File `tests/e2e/identity-option-edit-420.spec.ts` ada (`test -f`)
    - `grep -n "sudah dijawab" tests/e2e/identity-option-edit-420.spec.ts` returns ≥1 (assert pesan blok)
    - `grep -n "Tambah Opsi\|addOptionBtn\|Tambah" tests/e2e/identity-option-edit-420.spec.ts` returns ≥1 (skenario add-option)
    - Playwright run 3/3 PASS @5277 (dibuktikan di 420-UAT.md), DB ter-restore (SEED_JOURNAL cleaned)
  </acceptance_criteria>
  <verify>
    <manual>Jalankan: app @5277 hidup → snapshot DB → seed → npx playwright test tests/e2e/identity-option-edit-420.spec.ts --workers=1 → restore DB. Pass 3/3.</manual>
  </verify>
  <done>Spec identity-option-edit-420.spec.ts dibuat + 3/3 PASS real-browser (delete-middle-answered blocked, delete-middle-unanswered no-relabel, add-option no-overwrite); DB ter-restore; bukti di 420-UAT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → controller/DB | Test integrasi memanggil action asli atas SQL nyata (SectionFixture) — bukan trust boundary produksi, tapi MEMBUKTIKAN mitigasi T-420-01/04/05 nyata |
| browser → form (Playwright) | UAT memverifikasi carrier hidden Id + clone-reset (T-420-04) di DOM nyata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-420-01 | Tampering (IDOR) | test #4 anti-tamper foreign Id | verify | Test membuktikan foreign Id → reject + 0 mutasi (mitigasi Plan 01 nyata). |
| T-420-04 | Tampering (silent relabel) | test #1/#2 + Playwright | verify | Test membuktikan hapus tengah terjawab→blok, unanswered→no-relabel; Playwright add-option→no-overwrite A. |
| T-420-05 | DoS (DbUpdateException 500) | test #3 convert + port TEST1 | verify | `Record.ExceptionAsync==null` membuktikan guard pre-SaveChanges, no FK-Restrict 500. |
| T-420-07 | Data hygiene (seed bocor) | Playwright seed | mitigate | SEED_WORKFLOW snapshot→restore WAJIB; SEED_JOURNAL cleaned (CLAUDE.md). |
</threat_model>

<verification>
- `dotnet build` + `dotnet test HcPortal.Tests --filter "FullyQualifiedName~EditShrinkGuard"` hijau (SQLEXPRESS) — 2 port + 6 baru + pure-helper.
- Playwright 3/3 PASS @5277 (420-UAT.md), DB restored.
- Sanity: `Id = optionIds[` muncul di test; spec file ada dgn assert "sudah dijawab" + skenario add-option.
</verification>

<success_criteria>
- 2 test integrasi existing di-port ke identity contract (pass Id) + tetap mengunci guard.
- 6 test integrasi identity baru hijau (OPTEDIT-01/03/04/05 + anti-tamper + duplikat) — termasuk reproduksi relabel-senyap→blokir (VRF-01).
- Pure-helper EditShrinkGuardLogicTests tetap hijau (tak diubah).
- Playwright UAT 3/3 PASS real-browser; DB ter-restore (SEED_JOURNAL cleaned).
</success_criteria>

<output>
After completion, create `.planning/phases/420-editquestion-identity-based-option-editing/420-03-SUMMARY.md`
</output>
