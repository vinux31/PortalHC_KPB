---
phase: 153-assessment-flow-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ASSESS-03, ASSESS-04, ASSESS-05]

must_haves:
  truths:
    - "Worker sees only assessments filtered by correct status and cannot access unassigned ones"
    - "Worker can start exam with token, auto-save answers, resume after disconnect, submit without data loss"
    - "Worker can view results and review answers after submission"
  artifacts:
    - path: ".planning/phases/153-assessment-flow-audit/153-02-AUDIT-REPORT.md"
      provides: "Audit findings for worker exam flow"
  key_links:
    - from: "Controllers/CMPController.cs (Assessment)"
      to: "Views/CMP/Assessment.cshtml"
      via: "Status-filtered assessment list"
    - from: "Controllers/CMPController.cs (StartExam)"
      to: "Views/CMP/StartExam.cshtml"
      via: "Exam session with auto-save JS"
    - from: "Controllers/CMPController.cs (SubmitExam)"
      to: "Controllers/CMPController.cs (Results)"
      via: "Score calculation and redirect"
---

<objective>
Audit worker-facing exam flow: assessment listing, token verification, exam taking with auto-save, submission, and results viewing.

Purpose: Ensure workers can complete exams without data loss and cannot access unauthorized assessments.
Output: Audit report with findings, plus bug fixes if issues found.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Worker exam lifecycle</name>
  <files>Controllers/CMPController.cs, Views/CMP/Assessment.cshtml, Views/CMP/StartExam.cshtml, Views/CMP/ExamSummary.cshtml, Views/CMP/Results.cshtml</files>
  <action>
Read and analyze the following action methods in CMPController.cs:

**ASSESS-03 (Assessment List & Access Control):**
- `Assessment()` (line ~160) — check status filtering logic (Open/Upcoming/Completed), verify worker only sees assigned assessments, check direct URL access to unassigned assessment IDs
- Check Views/CMP/Assessment.cshtml for correct status display and link generation

**ASSESS-04 (Exam Taking):**
- `VerifyToken()` (line ~931) — check token validation, timing (can worker start before schedule?), already-completed prevention
- `StartExam()` (line ~969) — check session creation, question loading, package assignment, resume logic (what if worker refreshes or disconnects?)
- `SaveAnswer()` (line ~245) and `SaveLegacyAnswer()` (line ~293) — check auto-save correctness, concurrent save handling, what if session expired
- `CheckExamStatus()` (line ~338) — check timer sync, force-close detection
- `UpdateSessionProgress()` (line ~379) — check elapsed time tracking
- `AbandonExam()` (line ~1264) — check what state the exam is left in
- Check Views/CMP/StartExam.cshtml for JavaScript auto-save logic, timer, disconnect handling

**ASSESS-05 (Submit & Results):**
- `ExamSummary()` GET (line ~1397 and ~1425) — check answer collection before submit
- `SubmitExam()` (line ~1540) — check score calculation, double-submit prevention, what if answers are incomplete, race condition with force-close
- `Results()` (line ~1790) — check score display, answer review access control
- Check Views/CMP/Results.cshtml and ExamSummary.cshtml

**Security checks across all actions:**
- Can worker A access worker B's exam session?
- Can worker re-submit a completed exam?
- Can worker manipulate timer or elapsed seconds?
- Is there SQL injection or XSS risk in any parameter?

**Produce audit report** at `.planning/phases/153-assessment-flow-audit/153-02-AUDIT-REPORT.md` with:
- Section per requirement (ASSESS-03, ASSESS-04, ASSESS-05)
- Each finding: severity (bug/edge-case/security/cosmetic), description, file:line, suggested fix
- Summary: pass/fail per requirement

If any bugs with severity "bug" or "security" are found, fix them immediately.
  </action>
  <verify>
    <automated>test -f .planning/phases/153-assessment-flow-audit/153-02-AUDIT-REPORT.md && grep -c "ASSESS-03\|ASSESS-04\|ASSESS-05" .planning/phases/153-assessment-flow-audit/153-02-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report exists covering ASSESS-03, ASSESS-04, ASSESS-05 with findings documented. Any bugs/security issues are fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review audit of worker exam lifecycle (ASSESS-03, ASSESS-04, ASSESS-05)</what-built>
  <how-to-verify>
    1. Read the audit report at `.planning/phases/153-assessment-flow-audit/153-02-AUDIT-REPORT.md`
    2. As a Worker, verify in browser:
       - Go to CMP > Assessment — check status filters show correct assessments
       - Try accessing an assessment you're not assigned to via direct URL
       - Start an exam with token, answer questions, check auto-save works
       - Try refreshing mid-exam — verify resume works
       - Submit and check results page
    3. Confirm fixes (if any) work correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found during verification</resume-signal>
</task>

</tasks>

<verification>
- Audit report covers ASSESS-03, ASSESS-04, ASSESS-05
- All exam lifecycle actions reviewed for security and correctness
- User has verified key flows in browser
</verification>

<success_criteria>
- Audit report exists with structured findings
- No unfixed bugs or security issues remain
- User has approved the audit findings
</success_criteria>

<output>
After completion, create `.planning/phases/153-assessment-flow-audit/153-02-SUMMARY.md`
</output>
