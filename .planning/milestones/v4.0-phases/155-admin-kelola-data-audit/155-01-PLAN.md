---
phase: 155-admin-kelola-data-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ADMIN-01, ADMIN-02]

must_haves:
  truths:
    - "HC/Admin can create, edit, deactivate, and delete workers with correct cascade"
    - "Delete removes all FK-related records without orphan leaks"
    - "Deactivate blocks login but preserves data; reactivate restores access"
    - "Bulk import via Excel rejects malformed rows and reports errors clearly"
  artifacts:
    - path: ".planning/phases/155-admin-kelola-data-audit/155-01-AUDIT-REPORT.md"
      provides: "Audit findings for worker CRUD and import (ADMIN-01, ADMIN-02)"
  key_links:
    - from: "Controllers/AdminController.cs (ManageWorkers, CreateWorker, EditWorker, DeleteWorker, DeactivateWorker, ReactivateWorker)"
      to: "ApplicationUser + all FK tables"
      via: "EF Core cascade/manual delete"
    - from: "Controllers/AdminController.cs (ImportWorkers, DownloadImportTemplate)"
      to: "Excel file parsing"
      via: "EPPlus/ClosedXML"
---

<objective>
Audit worker management flows (ADMIN-01, ADMIN-02): CRUD lifecycle including delete cascade trace, deactivate/reactivate edge cases, and bulk Excel import with error handling.

Purpose: Ensure HC/Admin worker management is bug-free with proper cascade and import validation.
Output: Audit report with findings, plus inline bug fixes for any bug/security issues.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Worker CRUD and import (ADMIN-01, ADMIN-02)</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManageWorkers.cshtml, Views/Admin/CreateWorker.cshtml, Views/Admin/EditWorker.cshtml, Views/Admin/ImportWorkers.cshtml, Views/Admin/WorkerDetail.cshtml</files>
  <action>
Read and analyze worker management actions in AdminController.cs for bugs, validation gaps, security issues, and edge cases.

**ADMIN-01 (Worker CRUD):**
- `ManageWorkers()` — check role authorization [Authorize(Roles = "Admin, HC")], filtering, pagination
- `CreateWorker()` GET+POST — check model validation (required fields: NIP, FullName, Position, Section, Unit, Role), duplicate NIP prevention, password handling
- `EditWorker()` GET+POST — check same validation as create, concurrent edit, role change implications
- `WorkerDetail()` — check authorization, data completeness
- `DeactivateWorker()` — check: sets IsActive=false, does NOT delete data, user blocked at login after deactivation
- `ReactivateWorker()` — check: sets IsActive=true, does NOT restore deleted related records (Phase 154 finding pattern), edge case: reactivating already-active user
- `DeleteWorker()` — **FULL CASCADE TRACE**: enumerate every FK relationship (AssessmentSessions, AssessmentAnswers, CoachCoacheeMappings, ProtonTrackAssignments, ProtonDeliverableProgresses, TrainingRecords, Notifications, AuditLogs, etc.) and verify each is either cascade-deleted or explicitly removed. Flag any orphan leak paths.

**ADMIN-02 (Bulk Import):**
- `DownloadImportTemplate()` — check template columns match CreateWorker fields
- `ImportWorkers()` POST — check edge cases: empty file, empty rows, wrong column count, duplicate NIP in file, duplicate NIP vs existing DB, special characters in names, missing required fields, max row limit, error reporting to user (which rows failed and why)

Also check Views for: missing CSRF tokens, client-side validation gaps, role-gated visibility.

**Produce audit report** at `.planning/phases/155-admin-kelola-data-audit/155-01-AUDIT-REPORT.md` with:
- Section per requirement (ADMIN-01, ADMIN-02)
- Each finding: severity (bug/edge-case/security/cosmetic), description, file:line, suggested fix
- Summary: pass/fail per requirement, total findings count

Fix any findings with severity "bug" or "security" immediately and list fixes in the report.
  </action>
  <verify>
    <automated>test -f .planning/phases/155-admin-kelola-data-audit/155-01-AUDIT-REPORT.md && grep -c "ADMIN-01\|ADMIN-02" .planning/phases/155-admin-kelola-data-audit/155-01-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report exists covering ADMIN-01 and ADMIN-02 with full delete cascade trace documented. Any bugs/security issues are fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — Worker CRUD and import</name>
  <what-built>Code review and bug fixes for worker management (ADMIN-01, ADMIN-02)</what-built>
  <how-to-verify>
1. Login as HC role
2. Navigate to Admin/Index > Manajemen Pekerja
3. **Create:** Add a test worker with all fields, verify success
4. **Edit:** Edit the test worker, change fields, verify changes saved
5. **Deactivate:** Deactivate the test worker, verify they cannot login
6. **Reactivate:** Reactivate, verify they can login again
7. **Delete:** Delete the test worker, verify no orphan records remain in related tables
8. **Import:** Download template, fill in 2-3 rows (include one malformed row), upload, verify valid rows imported and malformed row rejected with clear error message
9. Review audit report at `.planning/phases/155-admin-kelola-data-audit/155-01-AUDIT-REPORT.md` for any findings that need attention
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Audit report covers both ADMIN-01 and ADMIN-02 with findings per requirement
- Delete cascade trace is complete with no orphan leak paths
- All bug/security severity findings are fixed
- UAT confirms worker CRUD and import work correctly
</verification>

<success_criteria>
HC/Admin can create, edit, deactivate, reactivate, delete workers with correct cascade, and bulk import via Excel with proper error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/155-admin-kelola-data-audit/155-01-SUMMARY.md`
</output>
