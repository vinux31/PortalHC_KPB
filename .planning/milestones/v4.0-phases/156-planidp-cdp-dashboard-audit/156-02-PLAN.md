---
phase: 156-planidp-cdp-dashboard-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CDP-04]

must_haves:
  truths:
    - "Coachee dashboard shows correct deliverable counts matching ProtonDeliverableProgress records"
    - "Coach dashboard shows only their mapped coachees"
    - "SectionHead sees only their section's data"
    - "HC/Admin see all sections' data"
    - "AJAX filtering returns correctly scoped data with no cross-role leakage"
    - "Empty states (no deliverables, no coachees) display gracefully without crashes"
  artifacts:
    - path: ".planning/phases/156-planidp-cdp-dashboard-audit/156-02-AUDIT-REPORT.md"
      provides: "Audit findings for CDP Dashboard (CDP-04)"
  key_links:
    - from: "Controllers/CDPController.cs (Dashboard lines 237-260)"
      to: "CDPDashboardViewModel, CoacheeDashboardSubModel, ProtonProgressSubModel"
      via: "BuildCoacheeSubModelAsync, BuildProtonProgressSubModelAsync"
    - from: "Controllers/CDPController.cs (FilterCoachingProton lines 265-280)"
      to: "ProtonDeliverableProgress, CoachCoacheeMappings"
      via: "AJAX with server-side role enforcement"
---

<objective>
Audit CDP Dashboard role-scoped metrics and filtering (CDP-04): coachee own data, coach mapped coachees, SectionHead section scope, HC/Admin all data, AJAX filtering, empty states.

Purpose: Ensure dashboard shows accurate, correctly scoped data for every role with no cross-role data leakage.
Output: Audit report with findings, plus inline bug fixes.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-planidp-cdp-dashboard-audit/156-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — CDP Dashboard and filtering (CDP-04)</name>
  <files>Controllers/CDPController.cs, Views/CDP/Dashboard.cshtml, Views/CDP/_ProtonProgress.cshtml</files>
  <action>
Read and analyze Dashboard, FilterCoachingProton, and GetCascadeOptions actions in CDPController.cs for bugs, metric accuracy, role scoping, and edge cases.

**CDP-04 (Dashboard role-scoped metrics):**

1. **Dashboard() lines 237-260** — trace ViewModel construction:
   - BuildCoacheeSubModelAsync: verify TotalDeliverables, ApprovedDeliverables, ActiveDeliverables counts match actual ProtonDeliverableProgress query logic. Cross-reference Phase 87-02 fix (ActiveDeliverables status filter) — confirm fix still holds.
   - BuildProtonProgressSubModelAsync: verify role branching (HC=all sections, SectionHead=own section, Coach=mapped coachees). Cross-reference Phase 87-02 fix (IsActive filters) — confirm fix still holds.
   - CompetencyLevelGranted: verify it reads from ProtonFinalAssessment correctly

2. **FilterCoachingProton() lines 265-280** — AJAX endpoint:
   - Verify server-side enforcement of role scoping (lines 273-276) — Coach can only see mapped coachees, SectionHead only own section
   - Test: what happens if Coach sends bagian=OtherSection via DevTools? Verify server overrides.
   - Verify cascade dropdown behavior (GetCascadeOptions lines 286-292): section -> units population

3. **Edge cases:**
   - Coachee with no deliverables (empty ProtonDeliverableProgress) — verify 0 counts, no divide-by-zero in percentage
   - HC with no coachees in system — verify empty table, no crash
   - Coach with no active mappings — verify graceful empty state
   - Role with no ProtonFinalAssessment — verify CompetencyLevelGranted shows appropriate default

4. **Views:** Check Dashboard.cshtml and _ProtonProgress.cshtml for null-reference in Razor, role-gated visibility, correct data binding.

**Produce audit report** at `.planning/phases/156-planidp-cdp-dashboard-audit/156-02-AUDIT-REPORT.md` with:
- Subsections: Dashboard metrics, Role scoping, AJAX filtering, Edge cases
- Each finding: severity (bug/edge-case/security/cosmetic), description, file:line, suggested fix
- Summary: pass/fail, total findings count

Fix any findings with severity "bug" or "security" immediately and list fixes in the report.
  </action>
  <verify>
    <automated>test -f .planning/phases/156-planidp-cdp-dashboard-audit/156-02-AUDIT-REPORT.md && grep -c "CDP-04" .planning/phases/156-planidp-cdp-dashboard-audit/156-02-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report exists covering CDP-04 with role scoping analysis and metric accuracy check. Any bugs/security issues are fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — CDP Dashboard</name>
  <what-built>Code review and bug fixes for CDP Dashboard metrics and filtering (CDP-04)</what-built>
  <how-to-verify>
1. **Coachee dashboard:** Login as coachee, navigate to CDP > Dashboard, verify deliverable counts (Total, Approved, Active) look reasonable against known data
2. **Coach dashboard:** Login as coach, verify only mapped coachees appear in ProtonProgress table
3. **SectionHead dashboard:** Login as SectionHead, verify only own section's coachees appear
4. **HC dashboard:** Login as HC, verify all sections visible
5. **AJAX scoping test:** As Coach, open browser DevTools Network tab, trigger FilterCoachingProton with modified bagian parameter — verify server returns only coach's scoped data (not other section)
6. **Cascade dropdown:** Select a section, verify units dropdown populates correctly
7. **Empty state:** If possible, test a role with no relevant data — verify graceful display
8. Review audit report at `.planning/phases/156-planidp-cdp-dashboard-audit/156-02-AUDIT-REPORT.md`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Audit report covers CDP-04 with role scoping, metric accuracy, AJAX filtering analysis
- Phase 87-02 fixes confirmed still in place
- All role branches verified for correct data scoping
- All bug/security severity findings are fixed
- UAT confirms dashboard works correctly for all roles
</verification>

<success_criteria>
CDP Dashboard shows accurate, role-scoped metrics for coachee/coach/SectionHead/HC with correct AJAX filtering and graceful empty states.
</success_criteria>

<output>
After completion, create `.planning/phases/156-planidp-cdp-dashboard-audit/156-02-SUMMARY.md`
</output>
