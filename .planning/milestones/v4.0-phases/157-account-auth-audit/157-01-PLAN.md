---
phase: 157-account-auth-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".planning/phases/157-account-auth-audit/157-01-AUDIT-REPORT.md", ".planning/phases/157-account-auth-audit/157-01-SUMMARY.md"]
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Login with valid local credentials succeeds and redirects to home"
    - "Login with invalid credentials shows error message"
    - "Inactive user is blocked at login with clear error"
    - "Profile page shows correct role, section, unit, position with no null-reference errors"
    - "Password change persists — old password rejected, new password works after re-login"
    - "Profile field edits (FullName, Position) persist after re-login"
  artifacts:
    - path: "Controllers/AccountController.cs"
      provides: "Login, Profile, Settings actions"
    - path: "Views/Account/Profile.cshtml"
      provides: "Profile display"
    - path: "Views/Account/Settings.cshtml"
      provides: "Password change and profile edit"
  key_links:
    - from: "AccountController.Login"
      to: "SignInManager/PasswordHasher"
      via: "local auth check + IsActive gate"
      pattern: "IsActive.*false"
    - from: "AccountController.Profile"
      to: "ViewBag properties"
      via: "user data population"
      pattern: "ViewBag\\."
---

<objective>
Audit login flow (local + AD), profile display, and settings (password change, profile edit) for AUTH-01, AUTH-02, AUTH-03.

Purpose: Verify authentication and profile/settings work correctly for all roles and edge cases, fix any bugs found.
Output: 157-01-AUDIT-REPORT.md with findings and fixes, 157-01-SUMMARY.md
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Login, Profile, Settings</name>
  <files>.planning/phases/157-account-auth-audit/157-01-AUDIT-REPORT.md</files>
  <action>
Review AccountController.cs focusing on:

AUTH-01 (Login):
- Login POST: verify local password check (PasswordHasher), AD path (if configured), IsActive check blocks inactive users with clear error message
- Check for open redirect vulnerability in returnUrl parameter
- Verify logout clears session/cookies properly
- Check error messages don't leak sensitive info (e.g., "user exists but wrong password")

AUTH-02 (Profile):
- Profile GET: verify ViewBag population for Role, Section, Unit, Position — check for null-reference if user has no unit or position
- Check that multi-unit users display correctly
- Verify [Authorize] on Profile action (should require auth, not specific role)

AUTH-03 (Settings):
- ChangePassword POST: verify old password validation, new password hashing, re-sign-in after change
- EditProfile POST: verify FullName/Position update persists to database
- Check for CSRF tokens on POST forms

Also review Views/Account/Profile.cshtml and Views/Account/Settings.cshtml for:
- Null-safe access on ViewBag/Model properties
- Correct form field bindings

Write findings to 157-01-AUDIT-REPORT.md. Fix any major bugs (crash/data loss) inline. Minor issues document only.
  </action>
  <verify>
    <automated>ls .planning/phases/157-account-auth-audit/157-01-AUDIT-REPORT.md &amp;&amp; grep -c "AUTH-0" .planning/phases/157-account-auth-audit/157-01-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report covers AUTH-01, AUTH-02, AUTH-03 with findings per requirement. Major bugs fixed in code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review and bug fixes for login, profile, and settings flows (AUTH-01, AUTH-02, AUTH-03)</what-built>
  <how-to-verify>
    1. Login flow (AUTH-01):
       - Log in with valid local credentials — should succeed and redirect to home
       - Log in with wrong password — should show error, not crash
       - If an inactive user exists in DB, try logging in — should be blocked with clear message
       - Log out and confirm session is cleared
    2. Profile page (AUTH-02):
       - Visit /Account/Profile — should show correct Role, Section, Unit, Position
       - No blank fields or error pages
    3. Settings (AUTH-03):
       - Change password via /Account/Settings — log out, log in with new password — should work
       - Edit FullName or Position — log out, log back in, check Profile — changes should persist
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- AUTH-01: Login works for valid credentials, rejects invalid, blocks inactive users
- AUTH-02: Profile displays all user data without null errors
- AUTH-03: Password change and profile edit persist across sessions
</verification>

<success_criteria>
All three AUTH requirements verified via code review and browser UAT with no major bugs remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/157-account-auth-audit/157-01-SUMMARY.md`
</output>
