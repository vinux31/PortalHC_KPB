# Milestone v6.0: Deployment Preparation

**Status:** ⚠️ CLOSED (no work executed) 2026-03-16
**Phases:** 173-174
**Total Plans:** 0

## Overview

Prepare production-ready release package and documentation so the IT team can deploy Portal HC KPB to IIS with Active Directory integration. Milestone was defined but closed before any phases were executed.

## Phases

### Phase 173: Release Package

**Goal**: A clean, deployable release folder exists at the target path with production configuration enabled
**Depends on**: Nothing (first phase of v6.0)
**Plans**: 0 plans (not started)

**Success Criteria:**
1. Folder `C:\Users\Administrator\Desktop\PortalHC_KPB_Release` exists with all project source files
2. Dev artifacts excluded (`.planning/`, `.playwright-mcp/`, `docs/`, `bin/`, `obj/`, `HcPortal.db`, root `.md/.ps1/.bat`)
3. `appsettings.json` has `UseActiveDirectory` set to `true`
4. Original source project folder unchanged

### Phase 174: Deployment Documentation

**Goal**: IT team has step-by-step written guides to export the database from SSMS and deploy the application to IIS with AD integration
**Depends on**: Phase 173
**Plans**: 0 plans (not started)

**Success Criteria:**
1. SSMS database export guide with numbered steps
2. Deployment guide covers IIS site setup
3. Deployment guide covers production `appsettings.json` configuration
4. Deployment guide covers database restore on production SQL Server
5. IT team can follow both guides without developer clarification

---

## Milestone Summary

**Key Decisions:**
- Terminal deployment milestone — no code changes to source project
- Manual SSMS export, IIS deployment target

**Issues Deferred:**
- All 4 requirements deferred — milestone closed without execution

**Technical Debt Incurred:**
- None (no code was written)

---

_For current project status, see .planning/ROADMAP.md_
