---
phase: 210-critical-renewal-chain-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03

must_haves:
  truths:
    - "Bulk renew pada N pekerja menghasilkan N AssessmentSession baru yang semua memiliki RenewsSessionId/RenewsTrainingId terisi"
    - "Badge count di Admin/Index identik dengan jumlah baris RenewalCertificate"
    - "Set 2 dan Set 4 di BuildRenewalRowsAsync tidak memfilter IsPassed (karena TR tidak punya IsPassed)"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "Fixed bulk FK assignment + badge count via BuildRenewalRowsAsync"
      contains: "RenewsSessionId = model.RenewsSessionId"
  key_links:
    - from: "Admin/Index action"
      to: "BuildRenewalRowsAsync"
      via: "method call for badge count"
      pattern: "BuildRenewalRowsAsync.*Count"
---

<objective>
Fix 3 critical renewal chain bugs: (1) bulk FK hanya di-assign ke user[0], (2) badge count tidak sinkron dengan halaman RenewalCertificate, (3) verifikasi bahwa TR sets sudah benar tanpa IsPassed filter.

Purpose: Renewal chain harus berfungsi benar untuk semua user, bukan hanya user pertama, dan badge count harus akurat.
Output: AdminController.cs yang sudah diperbaiki.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/210-critical-renewal-chain-fixes/210-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bulk FK assignment dan badge count sync</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 1315-1345 untuk FIX-01, lines 55-85 untuk FIX-02, lines 6605-6665 untuk FIX-03)
  </read_first>
  <action>
**FIX-01 — Bulk FK Assignment (line 1330-1331):**

Ubah baris 1330-1331 dari:
```csharp
RenewsSessionId = (i == 0) ? model.RenewsSessionId : null,
RenewsTrainingId = (i == 0) ? model.RenewsTrainingId : null
```
menjadi:
```csharp
RenewsSessionId = model.RenewsSessionId,
RenewsTrainingId = model.RenewsTrainingId
```

Hapus kondisi `(i == 0)` sehingga semua AssessmentSession dalam loop bulk renew mendapat FK yang sama.

**FIX-02 — Badge Count Sync (line 55-85):**

Ganti seluruh blok badge count (line 59-82) di method `Index()` dengan:
```csharp
var renewalRows = await BuildRenewalRowsAsync();
ViewBag.RenewalCount = renewalRows.Count;
```

Hapus 4 query terpisah (renewedSessionIds, renewedTrainingIds, expiredTrainingCount, expiredAssessmentCount) dan ganti dengan satu panggilan ke BuildRenewalRowsAsync().Count. Ini menjamin badge count identik dengan jumlah baris di halaman RenewalCertificate.

**FIX-03 — IsPassed Filter Verification:**

Verifikasi saja (tidak perlu ubah kode). Pastikan:
- Set 2 (renewedByTrSessionIds, line ~6637) TIDAK memfilter IsPassed — sudah benar karena TrainingRecord tidak punya field IsPassed
- Set 4 (renewedByTrTrainingIds, line ~6651) TIDAK memfilter IsPassed — sudah benar
- Set 1 dan Set 3 memfilter `IsPassed == true` karena berasal dari AssessmentSession yang memang punya field IsPassed

Tidak ada perubahan kode untuk FIX-03.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Line "RenewsSessionId = model.RenewsSessionId," exists WITHOUT "(i == 0)" ternary
    - Line "RenewsTrainingId = model.RenewsTrainingId" exists WITHOUT "(i == 0)" ternary
    - Admin/Index method contains "BuildRenewalRowsAsync()" call
    - Admin/Index method contains "ViewBag.RenewalCount = renewalRows.Count" (or equivalent)
    - Admin/Index method does NOT contain "expiredTrainingCount" or "expiredAssessmentCount" variables
    - Set 2 query (renewedByTrSessionIds) does NOT contain "IsPassed"
    - Set 4 query (renewedByTrTrainingIds) does NOT contain "IsPassed"
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Bulk FK assignment memperbaiki semua user (bukan hanya user[0]), badge count menggunakan BuildRenewalRowsAsync sebagai single source of truth, dan TR sets terverifikasi tidak memfilter IsPassed.</done>
</task>

</tasks>

<verification>
1. `dotnet build` sukses tanpa error
2. Grep: `RenewsSessionId = model.RenewsSessionId` tanpa `(i == 0)` di CreateAssessment POST
3. Grep: `BuildRenewalRowsAsync` dipanggil di Admin/Index
4. Grep: tidak ada `expiredTrainingCount` atau `expiredAssessmentCount` di Admin/Index
</verification>

<success_criteria>
- Build berhasil
- FIX-01: Semua AssessmentSession dalam bulk renew mendapat RenewsSessionId/RenewsTrainingId
- FIX-02: Badge count menggunakan BuildRenewalRowsAsync().Count
- FIX-03: Set 2 dan Set 4 tidak memfilter IsPassed (terverifikasi, tidak diubah)
</success_criteria>

<output>
After completion, create `.planning/phases/210-critical-renewal-chain-fixes/210-01-SUMMARY.md`
</output>
