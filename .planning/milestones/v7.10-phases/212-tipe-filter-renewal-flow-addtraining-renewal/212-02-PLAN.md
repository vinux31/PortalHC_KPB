---
phase: 212-tipe-filter-renewal-flow-addtraining-renewal
plan: 02
type: execute
wave: 2
depends_on: [212-01]
files_modified:
  - Models/CreateTrainingRecordViewModel.cs
  - Controllers/AdminController.cs
  - Views/Admin/AddTraining.cshtml
autonomous: true
requirements: [ENH-04, FIX-04]

must_haves:
  truths:
    - "AddTraining GET menerima renewSessionId dan renewTrainingId query params"
    - "AddTraining menampilkan banner kuning saat mode renewal"
    - "Field Title, Category, dan Peserta ter-prefill dari sertifikat asal"
    - "Submit AddTraining menyimpan RenewsTrainingId atau RenewsSessionId di TrainingRecord"
    - "Bulk renew via Training mendukung multi-peserta dengan per-user FK map"
  artifacts:
    - path: "Models/CreateTrainingRecordViewModel.cs"
      provides: "FK fields RenewsTrainingId dan RenewsSessionId"
      contains: "RenewsTrainingId"
    - path: "Controllers/AdminController.cs"
      provides: "AddTraining GET renewal mode + POST FK assignment"
      contains: "renewSessionId"
    - path: "Views/Admin/AddTraining.cshtml"
      provides: "Renewal banner + hidden FK inputs + prefill"
      contains: "IsRenewalMode"
  key_links:
    - from: "RenewalCertificate.cshtml btn-renew-via-training"
      to: "AdminController AddTraining GET"
      via: "window.location.href with query string"
      pattern: "AddTraining\\?renew"
    - from: "AddTraining.cshtml form POST"
      to: "AdminController AddTraining POST"
      via: "hidden input RenewsTrainingId/RenewsSessionId"
      pattern: "RenewsTrainingId"
---

<objective>
Implementasi AddTraining renewal mode — GET menerima parameter renewal untuk prefill, view menampilkan banner + hidden FK, POST menyimpan FK ke TrainingRecord. Termasuk dukungan multi-peserta bulk renewal.

Purpose: Melengkapi renewal chain sehingga sertifikat dapat di-renew via Training Record baru (bukan hanya via Assessment).
Output: AddTraining mendukung single dan bulk renewal mode dengan prefill dan FK assignment.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-tipe-filter-renewal-flow-addtraining-renewal/212-CONTEXT.md
@.planning/phases/212-tipe-filter-renewal-flow-addtraining-renewal/212-RESEARCH.md
@.planning/phases/212-tipe-filter-renewal-flow-addtraining-renewal/212-01-SUMMARY.md

<interfaces>
<!-- From CreateAssessment GET (lines 992-1100) — reference implementation for renewal mode -->
<!-- Pattern: check renewSessionId/renewTrainingId params → query DB → set ViewBag → prefill model -->
<!-- ViewBag keys used: IsRenewalMode, RenewalSourceTitle, RenewalSourceUserName, SelectedUserIds, RenewsSessionId, RenewsTrainingId, RenewalFkMap, RenewalFkMapType -->

<!-- From TrainingRecord model — existing FK fields -->
public int? RenewsTrainingId { get; set; }
public int? RenewsSessionId { get; set; }

<!-- From CreateTrainingRecordViewModel — currently NO FK fields (to be added) -->
public string UserId { get; set; }
public string Judul { get; set; }
public string Kategori { get; set; }
<!-- ... other fields ... -->

<!-- AddTraining GET currently: no params, returns View(new CreateTrainingRecordViewModel()) -->
<!-- AddTraining POST currently: single user, creates one TrainingRecord -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewModel FK fields + AddTraining GET renewal mode</name>
  <files>Models/CreateTrainingRecordViewModel.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Models/CreateTrainingRecordViewModel.cs (full file)
    - Controllers/AdminController.cs (lines 5392-5410 — AddTraining GET)
    - Controllers/AdminController.cs (lines 992-1100 — CreateAssessment GET renewal mode as reference)
  </read_first>
  <action>
**ViewModel — CreateTrainingRecordViewModel.cs:**
Tambah dua field di akhir class (sebelum closing brace):
```csharp
public int? RenewsTrainingId { get; set; }
public int? RenewsSessionId { get; set; }
```

**Controller — AddTraining GET (~line 5395):**
Ubah signature menjadi:
```csharp
public async Task<IActionResult> AddTraining(
    [FromQuery] List<int>? renewSessionId = null,
    [FromQuery] List<int>? renewTrainingId = null)
```

Setelah existing ViewBag.Workers setup (sebelum `return View`), tambah renewal mode logic:

```csharp
bool isRenewalMode = false;
var model = new CreateTrainingRecordViewModel();

if (renewTrainingId != null && renewTrainingId.Count > 0)
{
    if (renewTrainingId.Count == 1)
    {
        var src = await _context.TrainingRecords.Include(t => t.User)
            .FirstOrDefaultAsync(t => t.Id == renewTrainingId[0]);
        if (src == null)
        {
            TempData["Warning"] = "Training record asal tidak ditemukan.";
            return RedirectToAction("AddTraining");
        }
        isRenewalMode = true;
        model.Judul = src.Judul ?? "";
        model.Kategori = src.Kategori ?? "";
        model.RenewsTrainingId = src.Id;
        ViewBag.RenewalSourceTitle = src.Judul ?? "";
        ViewBag.RenewalSourceUserName = src.User?.FullName ?? "";
        ViewBag.SelectedUserId = src.UserId;
    }
    else
    {
        var srcs = await _context.TrainingRecords.Include(t => t.User)
            .Where(t => renewTrainingId.Contains(t.Id)).ToListAsync();
        if (srcs.Count == 0)
        {
            TempData["Warning"] = "Training record asal tidak ditemukan.";
            return RedirectToAction("AddTraining");
        }
        isRenewalMode = true;
        var first = srcs[0];
        model.Judul = first.Judul ?? "";
        model.Kategori = first.Kategori ?? "";
        ViewBag.RenewalSourceTitle = first.Judul ?? "";
        ViewBag.RenewalSourceUserName = string.Join(", ", srcs.Select(t => t.User?.FullName ?? ""));
        ViewBag.RenewalFkMap = System.Text.Json.JsonSerializer.Serialize(
            srcs.ToDictionary(t => t.UserId ?? "", t => t.Id));
        ViewBag.RenewalFkMapType = "training";
        ViewBag.SelectedUserIds = srcs.Select(t => t.UserId).ToList();
    }
}
else if (renewSessionId != null && renewSessionId.Count > 0)
{
    if (renewSessionId.Count == 1)
    {
        var src = await _context.AssessmentSessions.Include(s => s.User)
            .FirstOrDefaultAsync(s => s.Id == renewSessionId[0]);
        if (src == null)
        {
            TempData["Warning"] = "Assessment session asal tidak ditemukan.";
            return RedirectToAction("AddTraining");
        }
        isRenewalMode = true;
        model.Judul = src.Title ?? "";
        model.Kategori = MapKategori(src.Category);
        model.RenewsSessionId = src.Id;
        ViewBag.RenewalSourceTitle = src.Title ?? "";
        ViewBag.RenewalSourceUserName = src.User?.FullName ?? "";
        ViewBag.SelectedUserId = src.UserId;
    }
    else
    {
        var srcs = await _context.AssessmentSessions.Include(s => s.User)
            .Where(s => renewSessionId.Contains(s.Id)).ToListAsync();
        if (srcs.Count == 0)
        {
            TempData["Warning"] = "Assessment session asal tidak ditemukan.";
            return RedirectToAction("AddTraining");
        }
        isRenewalMode = true;
        var first = srcs[0];
        model.Judul = first.Title ?? "";
        model.Kategori = MapKategori(first.Category);
        ViewBag.RenewalSourceTitle = first.Title ?? "";
        ViewBag.RenewalSourceUserName = string.Join(", ", srcs.Select(s => s.User?.FullName ?? ""));
        ViewBag.RenewalFkMap = System.Text.Json.JsonSerializer.Serialize(
            srcs.ToDictionary(s => s.UserId, s => s.Id));
        ViewBag.RenewalFkMapType = "session";
        ViewBag.SelectedUserIds = srcs.Select(s => s.UserId).ToList();
    }
}

ViewBag.IsRenewalMode = isRenewalMode;
return View(model);
```

Hapus `return View(new CreateTrainingRecordViewModel());` yang lama — sekarang gunakan `model` yang sudah di-prefill.

**PENTING:** Untuk bulk renewal, AddTraining saat ini single-user (UserId scalar). Bulk via Training akan menggunakan hidden input JSON pattern (Phase 210) — user dipilih via multi-select dan FK map dikirim via hidden field. POST handler akan loop per user. Lihat Task 2.
  </action>
  <verify>
    <automated>grep -n "RenewsTrainingId" Models/CreateTrainingRecordViewModel.cs && grep -n "renewTrainingId" Controllers/AdminController.cs | grep "AddTraining" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Models/CreateTrainingRecordViewModel.cs contains `public int? RenewsTrainingId { get; set; }`
    - Models/CreateTrainingRecordViewModel.cs contains `public int? RenewsSessionId { get; set; }`
    - Controllers/AdminController.cs AddTraining GET contains `List<int>? renewSessionId`
    - Controllers/AdminController.cs AddTraining GET contains `List<int>? renewTrainingId`
    - Controllers/AdminController.cs AddTraining GET contains `ViewBag.IsRenewalMode = isRenewalMode`
    - Controllers/AdminController.cs AddTraining GET contains `ViewBag.RenewalFkMap`
    - Controllers/AdminController.cs AddTraining GET contains `ViewBag.RenewalFkMapType`
    - Application builds without errors: `dotnet build --no-restore`
  </acceptance_criteria>
  <done>AddTraining GET menerima renewal params, query DB, prefill model dan ViewBag untuk single dan bulk</done>
</task>

<task type="auto">
  <name>Task 2: AddTraining view renewal UI + POST FK assignment + bulk multi-user</name>
  <files>Views/Admin/AddTraining.cshtml, Controllers/AdminController.cs</files>
  <read_first>
    - Views/Admin/AddTraining.cshtml (full file)
    - Controllers/AdminController.cs (lines 5415-5475 — AddTraining POST)
    - Controllers/AdminController.cs (lines 1140-1250 — CreateAssessment POST renewal FK handling as reference)
  </read_first>
  <action>
**View — AddTraining.cshtml:**

1. **Banner kuning** — setelah breadcrumb (setelah closing `</nav>`, sebelum Page Header):
```html
@if (ViewBag.IsRenewalMode == true)
{
    <div class="alert alert-warning d-flex align-items-center mb-3">
        <i class="bi bi-arrow-repeat me-2 fs-5"></i>
        <span>Mode Renewal — Training ini akan me-renew sertifikat
            <strong>@ViewBag.RenewalSourceTitle</strong>
            milik <strong>@ViewBag.RenewalSourceUserName</strong>
        </span>
    </div>
}
```

2. **Hidden FK inputs** — di dalam `<form>`, sebelum submit button:
```html
@if (ViewBag.IsRenewalMode == true)
{
    if (Model.RenewsTrainingId != null)
    {
        <input type="hidden" name="RenewsTrainingId" value="@Model.RenewsTrainingId" />
    }
    if (Model.RenewsSessionId != null)
    {
        <input type="hidden" name="RenewsSessionId" value="@Model.RenewsSessionId" />
    }
    if (ViewBag.RenewalFkMap != null)
    {
        <input type="hidden" id="renewal-fk-map" name="renewalFkMap" value='@ViewBag.RenewalFkMap' />
        <input type="hidden" id="renewal-fk-map-type" name="renewalFkMapType" value="@ViewBag.RenewalFkMapType" />
    }
}
```

3. **Prefill Peserta via JS** — di akhir file (sebelum closing section scripts):
```javascript
@if (ViewBag.SelectedUserId != null)
{
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        var sel = document.getElementById('UserId');
        if (sel) sel.value = '@ViewBag.SelectedUserId';
    });
    </script>
}
@if (ViewBag.SelectedUserIds != null)
{
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        var sel = document.getElementById('UserId');
        if (sel) {
            // Untuk bulk, set pertama saja — POST akan loop berdasarkan FK map
            var ids = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.SelectedUserIds));
            if (ids.length === 1) {
                sel.value = ids[0];
            }
            // Untuk multi-user, simpan list di hidden field
            if (ids.length > 1) {
                var hiddenIds = document.createElement('input');
                hiddenIds.type = 'hidden';
                hiddenIds.name = 'UserIds';
                hiddenIds.value = JSON.stringify(ids);
                sel.closest('form').appendChild(hiddenIds);
                // Disable single UserId karena akan pakai UserIds
                sel.closest('.mb-3').style.display = 'none';
                // Tampilkan info jumlah peserta
                var info = document.createElement('div');
                info.className = 'alert alert-info mb-3';
                info.innerHTML = '<i class="bi bi-people me-1"></i>' + ids.length + ' peserta akan di-renew';
                sel.closest('.mb-3').parentNode.insertBefore(info, sel.closest('.mb-3'));
            }
        }
    });
    </script>
}
```

**Controller — AddTraining POST (~line 5415):**

1. Setelah existing ModelState/file validation, sebelum membuat TrainingRecord, tambah bulk check:
```csharp
// Baca bulk renewal params
var fkMapJson = Request.Form["renewalFkMap"].FirstOrDefault();
var fkMapType = Request.Form["renewalFkMapType"].FirstOrDefault();
Dictionary<string, int>? fkMap = null;
if (!string.IsNullOrEmpty(fkMapJson))
    fkMap = System.Text.Json.JsonSerializer.Deserialize<Dictionary<string, int>>(fkMapJson);

var userIdsJson = Request.Form["UserIds"].FirstOrDefault();
List<string>? bulkUserIds = null;
if (!string.IsNullOrEmpty(userIdsJson))
    bulkUserIds = System.Text.Json.JsonSerializer.Deserialize<List<string>>(userIdsJson);
```

2. Jika bulk (bulkUserIds != null dan count > 1), loop per user:
```csharp
if (bulkUserIds != null && bulkUserIds.Count > 1)
{
    foreach (var uid in bulkUserIds)
    {
        var rec = new TrainingRecord
        {
            UserId = uid,
            Judul = model.Judul,
            Penyelenggara = model.Penyelenggara,
            Kota = model.Kota,
            Kategori = model.Kategori,
            Tanggal = model.Tanggal,
            TanggalMulai = model.TanggalMulai,
            TanggalSelesai = model.TanggalSelesai,
            Status = model.Status,
            NomorSertifikat = model.NomorSertifikat,
            ValidUntil = model.ValidUntil,
            CertificateType = model.CertificateType,
            SertifikatUrl = sertifikatUrl
        };

        if (fkMap != null && fkMap.TryGetValue(uid, out var fkId))
        {
            if (fkMapType == "training") rec.RenewsTrainingId = fkId;
            else rec.RenewsSessionId = fkId;
        }

        _context.TrainingRecords.Add(rec);
    }
    await _context.SaveChangesAsync();
    TempData["Success"] = $"Berhasil membuat {bulkUserIds.Count} training record (renewal).";
    return RedirectToAction("ManageAssessment", new { tab = "training" });
}
```

3. Untuk single renew (existing flow), tambah FK ke TrainingRecord creation:
Di blok existing `var record = new TrainingRecord { ... };` (line ~5454), tambah:
```csharp
RenewsTrainingId = model.RenewsTrainingId,
RenewsSessionId = model.RenewsSessionId,
```

4. **ModelState invalid handler** — tambah ViewBag renewal state di blok `!ModelState.IsValid` return View:
```csharp
// Sebelum return View(model) di ModelState invalid block:
ViewBag.IsRenewalMode = (model.RenewsTrainingId != null || model.RenewsSessionId != null || !string.IsNullOrEmpty(fkMapJson));
if (model.RenewsTrainingId != null)
{
    var src = await _context.TrainingRecords.Include(t => t.User).FirstOrDefaultAsync(t => t.Id == model.RenewsTrainingId);
    if (src != null) { ViewBag.RenewalSourceTitle = src.Judul ?? ""; ViewBag.RenewalSourceUserName = src.User?.FullName ?? ""; }
}
else if (model.RenewsSessionId != null)
{
    var src = await _context.AssessmentSessions.Include(s => s.User).FirstOrDefaultAsync(s => s.Id == model.RenewsSessionId);
    if (src != null) { ViewBag.RenewalSourceTitle = src.Title ?? ""; ViewBag.RenewalSourceUserName = src.User?.FullName ?? ""; }
}
```
  </action>
  <verify>
    <automated>grep -n "IsRenewalMode" Views/Admin/AddTraining.cshtml && grep -n "RenewsTrainingId" Controllers/AdminController.cs | grep -i "addtraining\|record\." | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/Admin/AddTraining.cshtml contains `ViewBag.IsRenewalMode == true`
    - Views/Admin/AddTraining.cshtml contains `Mode Renewal`
    - Views/Admin/AddTraining.cshtml contains `RenewalSourceTitle`
    - Views/Admin/AddTraining.cshtml contains `name="RenewsTrainingId"`
    - Views/Admin/AddTraining.cshtml contains `name="RenewsSessionId"`
    - Views/Admin/AddTraining.cshtml contains `renewal-fk-map`
    - Controllers/AdminController.cs AddTraining POST contains `RenewsTrainingId = model.RenewsTrainingId`
    - Controllers/AdminController.cs AddTraining POST contains `RenewsSessionId = model.RenewsSessionId`
    - Controllers/AdminController.cs AddTraining POST contains `renewalFkMap`
    - Controllers/AdminController.cs AddTraining POST contains `bulkUserIds`
    - Application builds without errors: `dotnet build --no-restore`
  </acceptance_criteria>
  <done>AddTraining menampilkan banner renewal, prefill data, simpan FK single dan bulk, multi-user bulk loop berfungsi</done>
</task>

</tasks>

<verification>
1. `dotnet build --no-restore` — no errors
2. Browser: buka `/Admin/AddTraining?renewTrainingId=X` — banner kuning + prefill muncul
3. Browser: submit AddTraining renewal — cek DB TrainingRecord.RenewsTrainingId terisi
4. Browser: bulk renew via Training dari RenewalCertificate — AddTraining multi-peserta berfungsi
</verification>

<success_criteria>
- AddTraining menampilkan banner kuning saat mode renewal
- Title, Category, Peserta ter-prefill dari sertifikat asal
- Single renew submit menyimpan FK di TrainingRecord
- Bulk renew via Training membuat N TrainingRecord dengan FK per-user
- ModelState invalid tetap menampilkan banner renewal
</success_criteria>

<output>
After completion, create `.planning/phases/212-tipe-filter-renewal-flow-addtraining-renewal/212-02-SUMMARY.md`
</output>
