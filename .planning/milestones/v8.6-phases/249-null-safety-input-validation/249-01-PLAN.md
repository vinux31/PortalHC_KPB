---
phase: 249-null-safety-input-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03

must_haves:
  truths:
    - "GetCurrentUserRoleLevelAsync mengembalikan redirect/error jika user null, bukan crash"
    - "DateTime.Parse diganti TryParse di 3 action export/partial CMP"
    - "ToDictionary di bulk renewal tidak crash pada duplicate key"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Null-safe GetCurrentUserRoleLevelAsync + TryParse date parsing"
      contains: "TryParse"
    - path: "Controllers/AdminController.cs"
      provides: "Duplicate-key-safe ToDictionary di bulk renewal"
      contains: "GroupBy"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "5 action yang memanggil GetCurrentUserRoleLevelAsync"
      via: "null check + early return"
      pattern: "user == null"
---

<objective>
Tambah defensive null checks dan safe parse di CMPController dan AdminController.

Purpose: Mencegah unhandled exception crash pada session expired, input tanggal buruk, dan duplicate key di bulk renewal.
Output: 2 controller files yang lebih resilient tanpa perubahan logika bisnis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/249-null-safety-input-validation/249-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Null-safe GetCurrentUserRoleLevelAsync + TryParse di CMPController</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>Controllers/CMPController.cs</read_first>
  <action>
1. **SAFE-01 — GetCurrentUserRoleLevelAsync (line ~2006):** Ubah method agar mengembalikan nullable tuple. Ganti `user!` dengan null check:
```csharp
private async Task<(ApplicationUser? User, int RoleLevel)> GetCurrentUserRoleLevelAsync()
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return (null, 0);
    var userRoles = await _userManager.GetRolesAsync(user);
    var roleLevel = UserRoles.GetRoleLevel(userRoles.FirstOrDefault() ?? "");
    return (user, roleLevel);
}
```

Kemudian di setiap 5 caller (lines ~370, 411, 522, 574, 625), tambahkan guard setelah destructuring:
```csharp
var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
if (user == null) return RedirectToAction("Login", "Account");
```

2. **SAFE-02 — DateTime.TryParse (lines ~533-534, 585-586, 632-633):** Ganti 6 occurrences `DateTime.Parse(dateFrom/dateTo)` dengan pattern:
```csharp
DateTime? from = DateTime.TryParse(dateFrom, out var parsedFrom) ? parsedFrom : null;
DateTime? to = DateTime.TryParse(dateTo, out var parsedTo) ? parsedTo : null;
```

Ini sudah menangani case string kosong karena TryParse return false pada null/empty.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && grep -c "TryParse" Controllers/CMPController.cs && grep -c "user == null" Controllers/CMPController.cs && grep -c "DateTime\.Parse(" Controllers/CMPController.cs</automated>
  </verify>
  <acceptance_criteria>
- `grep "TryParse" Controllers/CMPController.cs` menunjukkan minimal 6 occurrences (3 action x 2 variabel)
- `grep "DateTime\.Parse(" Controllers/CMPController.cs` menunjukkan 0 occurrences
- `grep "user == null" Controllers/CMPController.cs` menunjukkan minimal 5 occurrences (satu per caller)
- `grep "user!" Controllers/CMPController.cs` dalam GetCurrentUserRoleLevelAsync tidak ada lagi
- Build sukses: `dotnet build --no-restore` exit 0
  </acceptance_criteria>
  <done>GetCurrentUserRoleLevelAsync null-safe dengan redirect, semua DateTime.Parse diganti TryParse, build sukses</done>
</task>

<task type="auto">
  <name>Task 2: Guard ToDictionary duplicate key di bulk renewal AdminController</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>Controllers/AdminController.cs (lines 1060-1170)</read_first>
  <action>
**SAFE-03 — per D-05:** Fix 2 lokasi ToDictionary yang bisa crash pada duplicate UserId:

1. **Line ~1085** — `sourceSessions.ToDictionary(s => s.UserId, s => s.Id)`:
```csharp
var fkMap = sourceSessions
    .GroupBy(s => s.UserId)
    .ToDictionary(g => g.Key, g => g.First().Id);
```

2. **Line ~1155** — `sourceTrainings.ToDictionary(t => t.UserId ?? "", t => t.Id)`:
```csharp
var fkMap = sourceTrainings
    .GroupBy(t => t.UserId ?? "")
    .ToDictionary(g => g.Key, g => g.First().Id);
```

Strategi: ambil record pertama per user (skip duplicates). Ini safe karena bulk renewal per user hanya butuh satu source FK.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && grep -n "GroupBy.*UserId" Controllers/AdminController.cs && dotnet build --no-restore 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `grep "sourceSessions.ToDictionary\|sourceTrainings.ToDictionary" Controllers/AdminController.cs` menunjukkan 0 direct ToDictionary (sudah diganti GroupBy pattern)
- `grep "GroupBy.*UserId" Controllers/AdminController.cs` menunjukkan 2 occurrences
- `dotnet build --no-restore` exit 0
  </acceptance_criteria>
  <done>Bulk renewal ToDictionary tidak crash pada duplicate UserId — menggunakan GroupBy + First()</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` sukses tanpa error
- Tidak ada `DateTime.Parse(` tersisa di CMPController
- Tidak ada `user!` di GetCurrentUserRoleLevelAsync
- Tidak ada bare `ToDictionary(s => s.UserId` atau `ToDictionary(t => t.UserId` di AdminController bulk renewal
</verification>

<success_criteria>
- SAFE-01: 5 halaman CMP redirect ke login jika session expired, bukan crash
- SAFE-02: Export dengan tanggal invalid menghasilkan null range (no crash)
- SAFE-03: Bulk renewal dengan duplicate user data tidak throw ArgumentException
</success_criteria>

<output>
After completion, create `.planning/phases/249-null-safety-input-validation/249-01-SUMMARY.md`
</output>
