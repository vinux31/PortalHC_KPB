---
phase: 249-null-safety-input-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/WorkerDetail.cshtml
  - Views/CMP/ExamSummary.cshtml
autonomous: true
requirements:
  - SAFE-04
  - SAFE-05

must_haves:
  truths:
    - "WorkerDetail dengan FullName null menampilkan string kosong, bukan exception"
    - "ExamSummary dengan ViewBag null tidak throw InvalidCastException"
  artifacts:
    - path: "Views/Admin/WorkerDetail.cshtml"
      provides: "Null-safe FullName rendering"
      contains: "??"
    - path: "Views/CMP/ExamSummary.cshtml"
      provides: "Null-safe ViewBag cast"
      contains: "as int?"
  key_links:
    - from: "Views/Admin/WorkerDetail.cshtml"
      to: "Model.FullName"
      via: "null-coalescing operator"
      pattern: 'FullName.*\?\?'
---

<objective>
Fix null-unsafe patterns di 2 view files: WorkerDetail.cshtml dan ExamSummary.cshtml.

Purpose: Mencegah NullReferenceException dan InvalidCastException saat data ViewBag atau Model property null.
Output: 2 view files yang null-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/249-null-safety-input-validation/249-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Null-safe FullName di WorkerDetail + null-safe ViewBag di ExamSummary</name>
  <files>Views/Admin/WorkerDetail.cshtml, Views/CMP/ExamSummary.cshtml</files>
  <read_first>Views/Admin/WorkerDetail.cshtml, Views/CMP/ExamSummary.cshtml</read_first>
  <action>
1. **SAFE-04 — WorkerDetail.cshtml (per D-02):**

Line ~15-16 — Fix initials computation yang crash jika FullName null atau empty:
```csharp
var fullName = Model.FullName ?? "";
var initials = fullName.Length > 0
    ? string.Join("", fullName.Split(' ').Take(2).Select(w => w[0].ToString().ToUpper()))
    : "?";
```

Line ~37 — Ganti `@Model.FullName` dengan `@(Model.FullName ?? "")`.
Line ~58 — Ganti `@Model.FullName` dengan `@(Model.FullName ?? "")`.

2. **SAFE-05 — ExamSummary.cshtml (per D-03):**

Line ~5-6 — Ganti hard cast `(int)ViewBag.X` dengan null-safe pattern:
```csharp
int unanswered = ViewBag.UnansweredCount as int? ?? 0;
int assessmentId = ViewBag.AssessmentId as int? ?? 0;
```
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && grep -n "FullName" Views/Admin/WorkerDetail.cshtml && grep -n "ViewBag" Views/CMP/ExamSummary.cshtml && dotnet build --no-restore 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `grep "(int)ViewBag" Views/CMP/ExamSummary.cshtml` menunjukkan 0 occurrences (hard cast dihapus)
- `grep "as int?" Views/CMP/ExamSummary.cshtml` menunjukkan 2 occurrences
- `grep 'FullName ?? ""' Views/Admin/WorkerDetail.cshtml` menunjukkan minimal 1 occurrence
- `grep "Model\.FullName\.Length" Views/Admin/WorkerDetail.cshtml` menunjukkan 0 occurrences (sudah diganti var)
- `dotnet build --no-restore` exit 0
  </acceptance_criteria>
  <done>WorkerDetail menampilkan "?" untuk null FullName, ExamSummary tidak crash pada null ViewBag, build sukses</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` sukses
- Tidak ada `(int)ViewBag` di ExamSummary.cshtml
- Tidak ada bare `Model.FullName.Length` di WorkerDetail.cshtml
</verification>

<success_criteria>
- SAFE-04: WorkerDetail dengan FullName null menampilkan "?" sebagai initials dan string kosong di display
- SAFE-05: ExamSummary dengan null ViewBag values default ke 0, bukan crash
</success_criteria>

<output>
After completion, create `.planning/phases/249-null-safety-input-validation/249-02-SUMMARY.md`
</output>
