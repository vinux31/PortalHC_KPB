---
phase: 251-data-integrity-logic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Data/ApplicationDbContext.cs
autonomous: true
requirements: [DATA-06, DATA-02]

must_haves:
  truths:
    - "_lastScopeLabel field dihapus dari CDPController — tidak ada shared mutable state"
    - "BuildProtonProgressSubModelAsync mengembalikan scopeLabel via return value"
    - "OrganizationUnit dengan nama sama di parent berbeda dapat dibuat tanpa constraint violation"
    - "AssessmentCategory dengan nama sama di parent berbeda dapat dibuat tanpa constraint violation"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Thread-safe BuildProtonProgressSubModelAsync dengan tuple return"
    - path: "Data/ApplicationDbContext.cs"
      provides: "Composite unique index (ParentId, Name)"
      contains: "new { u.ParentId, u.Name }"
  key_links:
    - from: "Controllers/CDPController.cs:Dashboard"
      to: "Controllers/CDPController.cs:BuildProtonProgressSubModelAsync"
      via: "tuple deconstruction"
      pattern: "var \\(.*scopeLabel\\)"
    - from: "Data/ApplicationDbContext.cs"
      to: "EF Core migration"
      via: "HasIndex composite"
      pattern: "new \\{ [uc]\\.ParentId, [uc]\\.Name \\}"
---

<objective>
Refactor _lastScopeLabel ke tuple return untuk thread-safety (DATA-06), dan ubah unique index OrganizationUnit/AssessmentCategory ke composite (ParentId, Name) via EF Core migration (DATA-02).

Purpose: Menghilangkan shared mutable state di CDPController dan memungkinkan sub-unit/sub-kategori nama sama di parent berbeda.
Output: CDPController thread-safe, composite unique index + migration file.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/251-data-integrity-logic/251-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor _lastScopeLabel ke tuple return (DATA-06)</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>Controllers/CDPController.cs</read_first>
  <action>
Per D-12, D-13, D-14 — refactor `_lastScopeLabel` dari private instance field ke return value tuple.

**Langkah 1:** Cari dan hapus deklarasi field `_lastScopeLabel` (sekitar line 661 atau di bagian atas class). Hapus baris `private string _lastScopeLabel = "";` atau yang sejenis.

**Langkah 2:** Ubah return type `BuildProtonProgressSubModelAsync` dari `Task<ProtonProgressSubModel>` ke `Task<(ProtonProgressSubModel subModel, string scopeLabel)>`:
SEBELUM:
```csharp
private async Task<ProtonProgressSubModel> BuildProtonProgressSubModelAsync(...)
```
SESUDAH:
```csharp
private async Task<(ProtonProgressSubModel subModel, string scopeLabel)> BuildProtonProgressSubModelAsync(...)
```

**Langkah 3:** Di dalam method, ganti assignment `_lastScopeLabel = scopeLabel;` dan ubah return statement:
SEBELUM:
```csharp
_lastScopeLabel = scopeLabel;
return subModel;
```
SESUDAH:
```csharp
return (subModel, scopeLabel);
```

**Langkah 4:** Di caller `Dashboard()` (sekitar line 285-286), ubah:
SEBELUM:
```csharp
model.ProtonProgressData = await BuildProtonProgressSubModelAsync(user, userRole);
model.ScopeLabel = _lastScopeLabel;
```
SESUDAH:
```csharp
var (progressData, scopeLabel) = await BuildProtonProgressSubModelAsync(user, userRole);
model.ProtonProgressData = progressData;
model.ScopeLabel = scopeLabel;
```

PENTING: Sebelum refactor, grep `BuildProtonProgressSubModelAsync` dan `_lastScopeLabel` untuk konfirmasi tidak ada caller lain.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && grep -n "_lastScopeLabel" Controllers/CDPController.cs ; echo "---" ; grep -n "var (progressData, scopeLabel)" Controllers/CDPController.cs ; dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep "_lastScopeLabel" Controllers/CDPController.cs` mengembalikan 0 baris (field dihapus sepenuhnya)
- `grep "var (progressData, scopeLabel)" Controllers/CDPController.cs` mengembalikan 1 baris (tuple deconstruction di Dashboard)
- `grep "return (subModel, scopeLabel)" Controllers/CDPController.cs` mengembalikan 1 baris (tuple return di BuildProtonProgressSubModelAsync)
- `dotnet build` sukses tanpa error
  </acceptance_criteria>
  <done>Field _lastScopeLabel dihapus, BuildProtonProgressSubModelAsync return tuple, Dashboard menggunakan deconstruction</done>
</task>

<task type="auto">
  <name>Task 2: Composite unique index + EF Core migration (DATA-02)</name>
  <files>Data/ApplicationDbContext.cs</files>
  <read_first>Data/ApplicationDbContext.cs</read_first>
  <action>
Per D-03, D-04, D-05 — ubah unique index dan buat migration.

**Langkah 1:** Di `Data/ApplicationDbContext.cs`, ubah 2 index definition:

Lokasi 1 (sekitar line 532, entity OrganizationUnit):
SEBELUM:
```csharp
entity.HasIndex(u => u.Name).IsUnique();
```
SESUDAH:
```csharp
entity.HasIndex(u => new { u.ParentId, u.Name }).IsUnique();
```

Lokasi 2 (sekitar line 559, entity AssessmentCategory):
SEBELUM:
```csharp
entity.HasIndex(c => c.Name).IsUnique();
```
SESUDAH:
```csharp
entity.HasIndex(c => new { c.ParentId, c.Name }).IsUnique();
```

**Langkah 2:** Generate EF Core migration:
```bash
dotnet ef migrations add ChangeUniqueIndexToComposite
```

**Langkah 3:** Verify migration file dibuat di folder `Migrations/` dan berisi `DropIndex` + `CreateIndex` operations.

CATATAN: Jangan jalankan `dotnet ef database update` — migration akan di-apply otomatis saat startup via `Database.Migrate()`.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && grep -n "new { u.ParentId, u.Name }" Data/ApplicationDbContext.cs && grep -n "new { c.ParentId, c.Name }" Data/ApplicationDbContext.cs && ls Migrations/*ChangeUniqueIndexToComposite* 2>/dev/null && echo "PASS" ; dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep "new { u.ParentId, u.Name }" Data/ApplicationDbContext.cs` mengembalikan 1 baris (OrganizationUnit composite index)
- `grep "new { c.ParentId, c.Name }" Data/ApplicationDbContext.cs` mengembalikan 1 baris (AssessmentCategory composite index)
- `grep "entity.HasIndex(u => u.Name).IsUnique()" Data/ApplicationDbContext.cs` mengembalikan 0 baris (old index dihapus)
- `grep "entity.HasIndex(c => c.Name).IsUnique()" Data/ApplicationDbContext.cs` mengembalikan 0 baris (old index dihapus)
- File migration `*ChangeUniqueIndexToComposite*` ada di folder `Migrations/`
- `dotnet build` sukses tanpa error
  </acceptance_criteria>
  <done>Unique index OrganizationUnit dan AssessmentCategory diubah ke composite (ParentId, Name), migration file dibuat</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- Tidak ada `_lastScopeLabel` di CDPController.cs
- Composite unique index terdefinisi di ApplicationDbContext.cs
- Migration file ada di Migrations/
</verification>

<success_criteria>
- _lastScopeLabel dihapus, tuple return digunakan (DATA-06)
- Unique index OrganizationUnit dan AssessmentCategory menjadi composite (ParentId, Name) (DATA-02)
- Migration file ChangeUniqueIndexToComposite dibuat
- Project builds tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/251-data-integrity-logic/251-02-SUMMARY.md`
</output>
