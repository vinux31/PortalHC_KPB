---
phase: 104-develop-page-http-localhost-5277-cmp-records-saya-ingin-kamu-cari-konten-fitur-logic-user-view-akses-paga-page-cmp-records-ini-saya-ingin-develop-page-ini
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "Section filter in Team View filters table by selected section"
    - "Unit filter in Team View filters table by selected unit"
    - "Category filter cascades - each filter change immediately adjusts table data"
    - "Status filter shows workers with/without training records based on Sudah/Belum selection"
    - "Search filter filters table by worker name or NIP"
    - "Filter combinations work together - multiple filters can be applied simultaneously"
    - "Reset button clears all filter controls and updates table to show all workers"
  artifacts:
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "Team View with working client-side filters"
      contains: "filterTeamTable() function with correct logic"
      fixes: "Function name collision (already done) + Status filter 'ALL' case bug"
  key_links:
    - from: "Filter controls (sectionFilter, unitFilter, categoryFilter, statusFilter, searchFilter)"
      to: "filterTeamTable() function"
      via: "onchange/oninput event handlers"
      pattern: "onchange=\"filterTeamTable()\"|oninput=\"filterTeamTable()\""
    - from: "filterTeamTable() function"
      to: "Worker table rows (.worker-row)"
      via: "data-* attribute filtering"
      pattern: "document.querySelectorAll('.worker-row').forEach"
    - from: "Reset button"
      to: "All filter controls + filterTeamTable()"
      via: "resetTeamFilters() function"
      pattern: "onclick=\"resetTeamFilters()\""
---

<objective>
Fix all client-side filtering bugs in Team View to enable cascading filters with proper reset functionality.

Purpose: User reported ALL filters not working (Section, Unit, Category, Status, Search, combinations, Reset). Root cause: TWO bugs - (1) Function name collision between Records.cshtml and RecordsTeam.cshtml (already fixed in code but need to verify), (2) Status filter logic doesn't handle 'ALL' case explicitly.

Output: Working cascading filters with proper reset, all 5 filters functional independently and in combination.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md

@.planning/phases/104-develop-page-http-localhost-5277-cmp-records-saya-ingin-kamu-cari-konten-fitur-logic-user-view-akses-paga-page-cmp-records-ini-saya-ingin-develop-page-ini/104-PLAN-SUMMARY.md
@.planning/phases/104-develop-page-http-localhost-5277-cmp-records-saya-ingin-kamu-cari-konten-fitur-logic-user-view-akses-paga-page-cmp-records-ini-saya-ingin-develop-page-ini/104-UAT.md
@.planning/debug/team-view-filters-bug.md
@.planning/debug/reset-button-issue.md
</execution_context>

<context>
@.planning/phases/104-develop-page-http-localhost-5277-cmp-records-saya-ingin-kamu-cari-konten-fitur-logic-user-view-akses-paga-page-cmp-records-ini-saya-ingin-develop-page-ini/104-PLAN-SUMMARY.md
@Views/CMP/RecordsTeam.cshtml

## Gaps Being Closed

From 104-UAT.md:

**Gap 3 - Section filter not working (Test 4):**
- User: "filter not working"
- Root cause: "filterTable() defined in both Records.cshtml and RecordsTeam.cshtml. Records version overwrites Team View version when partial is loaded"
- Fix: Already renamed to filterTeamTable() in RecordsTeam.cshtml - need to verify and confirm

**Gap 4 - Unit filter not working (Test 5):**
- Same root cause as Gap 3

**Gap 5 - Category filter not working (Test 6):**
- User: "filter not working, saya ingin filternya cascade"
- Same root cause as Gap 3

**Gap 6 - Status filter not working (Test 7):**
- Same root cause as Gap 3

**Gap 7 - Search filter not working (Test 8):**
- Same root cause as Gap 3

**Gap 8 - Filter combinations not working (Test 9):**
- Same root cause as Gap 3

**Gap 9 - Reset button not working (Test 10):**
- User: "tabel reset, tapi table data tidak"
- Root cause: "Status filter logic doesn't properly handle 'ALL' case - when reset sets status to 'ALL', the matchStatus variable logic needs to explicitly handle this case instead of relying on implicit true"
- Fix: Add explicit `matchStatus = true` when status === 'ALL'

## Implementation Notes from Debug Session

From `.planning/debug/team-view-filters-bug.md`:
- Root cause identified: Function name collision
- Fix applied: Renamed filterTable() to filterTeamTable() in RecordsTeam.cshtml
- Need to verify this fix is present in current code

From `.planning/debug/reset-button-issue.md`:
- Status filter at lines 243-247 doesn't explicitly handle 'ALL' case
- Current logic: matchStatus starts as true, but when status is neither 'Sudah' nor 'Belum', it relies on implicit true
- Fix: Add explicit condition for 'ALL'
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify function name fix (filterTable → filterTeamTable)</name>
  <files>Views/CMP/RecordsTeam.cshtml</files>
  <action>
    Verify that the function name collision fix is present in RecordsTeam.cshtml:

    1. Check that filter function is named `filterTeamTable()` (NOT `filterTable`)
    2. Check that reset function is named `resetTeamFilters()` (NOT `resetFilters`)
    3. Check all event handlers call the renamed functions:
       - onchange="filterTeamTable()" on filter dropdowns (lines 36, 53, 64, 74, 87)
       - oninput="filterTeamTable()" on search input (line 95)
       - onclick="resetTeamFilters()" on reset button (line 98)

    If NOT present (i.e., still using old names `filterTable`/`resetFilters`), rename them:
    - Find and replace: function filterTable() → function filterTeamTable()
    - Find and replace: function resetFilters() → function resetTeamFilters()
    - Update all event handler references

    Reference existing code: RecordsTeam.cshtml lines 215-269 contain the JavaScript section
    Gap reason: Function name collision causes Records.cshtml's filterTable() to overwrite RecordsTeam.cshtml's version
  </action>
  <verify>
    <automated>grep -c "function filterTable" Views/CMP/RecordsTeam.cshtml equals 0 AND grep -c "function filterTeamTable" Views/CMP/RecordsTeam.cshtml equals 1</automated>
  </verify>
  <done>All filter functions use unique names (filterTeamTable, resetTeamFilters) to avoid collision with Records.cshtml functions</done>
</task>

<task type="auto">
  <name>Task 2: Fix Status filter logic to explicitly handle 'ALL' case</name>
  <files>Views/CMP/RecordsTeam.cshtml</files>
  <action>
    Fix the status filter logic in filterTeamTable() function to explicitly handle the 'ALL' case.

    Current code (lines 241-247):
    ```javascript
    // Status filter
    let matchStatus = true;
    if (status === 'Sudah') {
        matchStatus = rowHasTraining;
    } else if (status === 'Belum') {
        matchStatus = !rowHasTraining;
    }
    ```
    Problem: When status is 'ALL', the variable matchStatus remains true from initialization, but this is implicit and not robust.

    Fix to:
    ```javascript
    // Status filter
    let matchStatus = true;
    if (status === 'ALL') {
        matchStatus = true;  // Explicit handling
    } else if (status === 'Sudah') {
        matchStatus = rowHasTraining;
    } else if (status === 'Belum') {
        matchStatus = !rowHasTraining;
    }
    ```

    This makes the logic crystal clear and ensures reset button works correctly when status is set to 'ALL'.

    Reference existing code: RecordsTeam.cshtml lines 241-247 contain status filter logic
    Gap reason: User reported "tabel reset, tapi table data tidak" - reset sets status to 'ALL' but table doesn't update
  </action>
  <verify>
    <automated>grep -A 5 "Status filter" Views/CMP/RecordsTeam.cshtml | grep -c "if (status === 'ALL')" equals 1</automated>
  </verify>
  <done>Status filter explicitly handles 'ALL' case, reset button now properly shows all workers</done>
</task>

</tasks>

<verification>
Manual verification steps:
1. Login as Admin, navigate to CMP > Records, click Team View tab
2. Test Section filter: Select "Alkylation" → table should show only Alkylation workers
3. Test Unit filter: Select "Operation" → table should show only Operation workers
4. Test Category filter: Select "PROTON" → table should update immediately (cascading behavior)
5. Test Status filter: Select "Sudah" → shows workers with training; "Belum" → shows workers without; "ALL" → shows all
6. Test Search filter: Type worker name → table should filter by name/NIP
7. Test combination: Section="Alkylation" + Category="PROTON" + Status="Sudah" → shows matching workers only
8. Test Reset button: Click Reset → all filters clear, all workers shown, counter updates
</verification>

<success_criteria>
- [ ] All filter functions use unique names (filterTeamTable, resetTeamFilters)
- [ ] Status filter explicitly handles 'ALL' case
- [ ] Section filter works independently
- [ ] Unit filter works independently
- [ ] Category filter works independently with cascading behavior
- [ ] Status filter works for all three options (ALL, Sudah, Belum)
- [ ] Search filter works on Nama and NIP fields
- [ ] Multiple filters work together in combination
- [ ] Reset button clears all filters and updates table correctly
- [ ] Worker counter updates immediately when filters change
</success_criteria>

<output>
After completion, create `.planning/phases/104-develop-page-http-localhost-5277-cmp-records-saya-ingin-kamu-cari-konten-fitur-logic-user-view-akses-paga-page-cmp-records-ini-saya-ingin-develop-page-ini/104-02-SUMMARY.md`
</output>
