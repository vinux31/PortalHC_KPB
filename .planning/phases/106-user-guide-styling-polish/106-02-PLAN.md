---
phase: 106-user-guide-styling-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/css/guide.css
  - Views/Home/Guide.cshtml
autonomous: true
requirements:
  - GUIDE-STYLE-02
must_haves:
  truths:
    - All interactive elements meet minimum 44x44px touch target size
    - No horizontal scrolling occurs on any mobile screen size
    - Text is readable without zooming on mobile devices
    - Touch interactions feel responsive and provide visual feedback
    - Mobile layouts maintain visual hierarchy and readability
    - Search input is 16px font size to prevent iOS auto-zoom
    - All breakpoints have been tested on actual devices or emulation
  artifacts:
    - path: wwwroot/css/guide.css
      provides: Mobile responsive CSS with touch-friendly sizing
      contains: "@media (max-width: 768px)", ".guide-module-card", ".faq-question"
    - path: Views/Home/Guide.cshtml
      provides: Guide page structure optimized for mobile
      contains: "viewport", "guide-search-input"
  key_links:
    - from: wwwroot/css/guide.css
      to: Bootstrap breakpoints
      via: Media queries matching Bootstrap grid system
      pattern: "@media \\(max-width: [0-9]+px\\)"
    - from: wwwroot/css/guide.css
      to: CSS variables
      via: Using existing variables for mobile consistency
      pattern: "var\\(--gradient-|--shadow-"
    - from: Views/Home/Guide.cshtml
      to: wwwroot/css/guide.css
      via: CSS class selectors for mobile optimization
      pattern: "guide-search-input|guide-module-card|faq-question"
---

# Plan 106-02: Mobile Responsive Polish

## Objective

Optimize User Guide page for mobile devices with enhanced touch targets, refined layouts, and improved typography. Ensure comfortable touch interactions, readable content without zooming, and consistent design across all screen sizes from 320px phones to tablets.

**Purpose:** Deliver polished mobile experience with touch-friendly interactions and readable layouts
**Output:** Enhanced mobile CSS with proper touch targets, responsive breakpoints, and mobile-specific optimizations

## Tasks

<task type="auto">
<name>Task 1: Enhanced Touch Targets for Mobile</name>
<files>wwwroot/css/guide.css</files>
<action>
1. Locate `.faq-question` (line 476) and enhance for touch:
   - Increase padding: `padding: 1.25rem 0` for larger touch area
   - Add `min-height: 48px` to meet WCAG 2.1 AA touch target size
   - Add `cursor: pointer` and `user-select: none` for better mobile UX
2. Locate `.guide-module-card` (line 60) and enhance:
   - Add `min-height: 120px` on mobile using `@media (max-width: 768px)`
   - Ensure entire card is clickable (already using `<a>` tag)
   - Add `display: flex` to ensure full card area is tappable
3. Locate `.guide-search-input` (line 127) and enhance:
   - Increase padding on mobile: `padding: 1rem 1.2rem 1rem 3.2rem` in media query
   - Increase font size to prevent zoom on iOS: `font-size: 16px` (instead of 0.95rem)
4. Add breadcrumb touch targets in mobile media query:
   - Add `.guide-breadcrumb .breadcrumb-item a { padding: 0.5rem; min-height: 44px; display: inline-flex; align-items: center; }`
5. Add active state feedback for touch:
   - Add `.guide-module-card:active { transform: scale(0.98); }`
   - Add `.faq-question:active { background: rgba(102, 126, 234, 0.08); }`
</action>
<verify>
1. Open Chrome DevTools and toggle device emulation (iPhone SE - 375px)
2. Tap FAQ items and verify easy activation without accidental clicks
3. Tap module cards and verify entire card area is responsive
4. Tap search input and verify it's easily tappable
5. Measure touch targets - verify all interactive elements are minimum 44x44px
6. Test on actual mobile device if available
</verify>
<done>
- [ ] FAQ buttons have min-height 48px and increased padding
- [ ] Module cards have min-height 120px on mobile
- [ ] Search input is 16px font size to prevent iOS zoom
- [ ] All interactive elements have minimum 44x44px touch targets
- [ ] Active states provide visual feedback on touch
</done>
</task>

<task type="auto">
<name>Task 2: Mobile Layout Optimizations</name>
<files>wwwroot/css/guide.css</files>
<action>
1. Locate existing `@media (max-width: 768px)` (line 547) and enhance:
2. Optimize hero section:
   - Reduce padding: `padding: 1.5rem 1rem` for compact screens
   - Adjust h1 font size: `font-size: 1.5rem` (instead of 1.75rem)
   - Stack role badge on new line: add `text-align: center; display: flex; flex-direction: column;` to hero content
3. Optimize card grid:
   - Keep existing `grid-template-columns: 1fr` (line 557)
   - Add gap reduction: `gap: 1rem` on mobile (instead of 1.5rem)
   - Add `padding: 0 1rem` to prevent edge touching
4. Optimize FAQ section:
   - Reduce FAQ item padding: `padding: 0.9rem 0` on mobile
   - Reduce font sizes slightly: `font-size: 0.95rem` for FAQ answers
   - Add `overflow-wrap: break-word` to prevent horizontal scroll
5. Add small phone breakpoint for very small screens:
   - Create `@media (max-width: 400px)` for very small phones
   - Further reduce hero heading: `font-size: 1.3rem`
   - Reduce card header padding: `padding: 1rem 0.75rem`
6. Ensure no horizontal scrolling:
   - Add `overflow-x: hidden` to body/container
   - Add `max-width: 100%` to all images and cards
</action>
<verify>
1. Test on iPhone SE emulation (375px width)
2. Verify no horizontal scrolling occurs
3. Verify text is readable without zooming
4. Verify spacing feels comfortable, not cramped
5. Test on very small phone emulation (320px width)
6. Verify all content fits without overflow
7. Test in both portrait and landscape orientations
</verify>
<done>
- [ ] Hero section uses compact padding and font sizes on mobile
- [ ] Card grid uses 1fr layout with 1rem gap on mobile
- [ ] FAQ items have reduced padding and overflow-wrap
- [ ] Small phone breakpoint (< 400px) further optimizes layout
- [ ] No horizontal scrolling on any mobile screen size
</done>
</task>

<task type="auto">
<name>Task 3: Mobile Typography & Spacing Refinements</name>
<files>wwwroot/css/guide.css</files>
<action>
1. Add mobile-specific font scaling in `@media (max-width: 768px)`:
   - Reduce h2 section headings: `font-size: 1.2rem` (from 1.4rem)
   - Reduce card title: `font-size: 1rem` (from 1.1rem)
   - Keep body text at `font-size: 0.95rem` for readability
2. Optimize line heights for mobile:
   - Add `line-height: 1.6` to `.faq-answer` for better readability
   - Add `line-height: 1.5` to `.guide-step-text`
3. Improve spacing consistency:
   - Add consistent `margin-bottom: 1.5rem` between sections
   - Reduce hero margin: `margin-bottom: 1.5rem` on mobile
   - Add section padding: `padding: 1rem 0` for better breathing room
4. Adjust search placeholder text for mobile:
   - Shorten placeholder via CSS content property or JavaScript
   - Change from "Cari panduan... (contoh: KKJ, Coaching)" to "Cari panduan..."
   - Add in mobile media query: `.guide-search-input::placeholder { content: "Cari panduan..."; }`
5. Ensure consistent vertical rhythm:
   - Use spacing scale in increments of 0.5rem (8px)
   - Match spacing to Bootstrap's spacing utilities
</action>
<verify>
1. View guide page on mobile device emulation
2. Verify all text is readable without zooming
3. Verify line heights feel comfortable, not cramped
4. Verify spacing between sections is consistent
5. Verify placeholder text fits without truncation
6. Test in both portrait and landscape orientations
7. Verify vertical rhythm feels consistent
</verify>
<done>
- [ ] Headings scale appropriately on mobile (h2: 1.2rem, h1: 1.5rem)
- [ ] Line heights optimized for mobile readability (1.5-1.6)
- [ ] Spacing uses consistent 0.5rem scale
- [ ] Search placeholder text shortened for mobile
- [ ] Vertical rhythm is consistent across sections
</done>
</task>

<task type="auto">
<name>Task 4: Mobile-Specific Navigation Improvements</name>
<files>wwwroot/css/guide.css, Views/Home/Guide.cshtml</files>
<action>
1. Optimize breadcrumb for mobile (line 25 in guide.css):
   - Hide breadcrumb text on very small screens, keep icon only
   - Add `@media (max-width: 400px) { .guide-breadcrumb .breadcrumb-item:not(:first-child) { display: none; } }`
   - Add centered back button icon when breadcrumb hidden
   - Add `.guide-breadcrumb { justify-content: center; }` on mobile
2. Enhance back button on GuideDetail page (line 657):
   - Add mobile-specific styling in media query: `@media (max-width: 768px) { .guide-back-btn { width: 100%; text-align: center; margin-bottom: 1rem; } }`
   - Add larger touch target: `padding: 0.75rem 1.5rem` on mobile
   - Add `display: block` for full-width button
3. Add smooth scroll to top on navigation (in Guide.cshtml):
   - Add JavaScript to scroll to top when clicking module cards
   - Use `window.scrollTo({ top: 0, behavior: 'smooth' })` in card click handlers
   - Add to existing card click event listeners
4. Add sticky search bar for mobile (optional enhancement):
   - Add `position: sticky; top: 0; z-index: 100;` to search wrapper in mobile media query
   - Add background color to prevent content show-through
5. Ensure tap targets have spacing:
   - Add minimum 8px spacing between adjacent tap targets
   - Add margin between cards and buttons
</action>
<verify>
1. Navigate to Guide page on mobile emulation
2. Click module card and verify smooth scroll behavior
3. Navigate to GuideDetail page and verify back button is full-width on mobile
4. Verify back button is easily tappable with 44px min height
5. Verify breadcrumb doesn't overflow on small screens
6. Test navigation flow: Guide → GuideDetail → back to Guide
7. Verify sticky search bar works if implemented
</verify>
<done>
- [ ] Breadcrumb hidden on very small screens (< 400px)
- [ ] Back button is full-width on mobile with larger touch target
- [ ] Smooth scroll to top when navigating to guide details
- [ ] Tap targets have minimum 8px spacing
- [ ] Navigation flow works smoothly on mobile
</done>
</task>

## Verification

1. Test on iPhone SE emulation (375px) - verify all touch targets are 44x44px minimum
2. Test on standard phone emulation (390px) - verify layouts are readable
3. Test on tablet emulation (768px) - verify tablet layouts work
4. Test on very small phone (320px) - verify no horizontal scroll
5. Test in landscape orientation - verify layouts adapt
6. Test on actual iOS and Android devices if available
7. Verify all breakpoints use CSS variables for consistency
8. Verify no hardcoded colors that conflict with existing design system

## Notes

- Test on both iOS and Android devices if possible (differences in touch handling)
- Use Chrome DevTools device emulation: iPhone SE, iPhone 12, iPad, Samsung Galaxy
- Focus on "thumb-friendly" design - important actions in bottom half of screen
- Keep font sizes at least 16px to prevent iOS auto-zoom on input focus
- Ensure tap targets have at least 8px spacing between them
- Test in both portrait and landscape orientations
- Use Bootstrap breakpoint values: sm (576px), md (768px), lg (992px), xl (1200px)
