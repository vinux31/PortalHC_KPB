---
phase: 106-user-guide-styling-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/css/guide.css
  - Views/Home/Guide.cshtml
  - Views/Home/GuideDetail.cshtml
autonomous: true
requirements:
  - GUIDE-STYLE-03
must_haves:
  truths:
    - All interactive elements have visible focus indicators
    - Focus indicators meet WCAG AAA contrast requirements
    - All text meets WCAG AAA contrast ratios (7:1 minimum)
    - ARIA labels and roles are properly implemented
    - Keyboard navigation works for all functionality
    - Skip links allow jumping to main sections
    - Search results are announced via aria-live regions
  artifacts:
    - path: wwwroot/css/guide.css
      provides: Accessibility styles for focus, contrast, and keyboard navigation
      contains: ":focus-visible", ".skip-link", "aria-"
    - path: Views/Home/Guide.cshtml
      provides: ARIA attributes and landmark regions
      contains: "aria-label", "aria-live", "role=", "aria-expanded"
    - path: Views/Home/GuideDetail.cshtml
      provides: ARIA attributes for detail page
      contains: "aria-current", "role="
  key_links:
    - from: Views/Home/Guide.cshtml
      to: wwwroot/css/guide.css
      via: CSS selectors for focus states
      pattern: ":focus-visible|\\.skip-link"
    - from: Views/Home/Guide.cshtml
      to: ARIA attributes
      via: Semantic HTML with ARIA labels
      pattern: "aria-[a-z]+="
    - from: wwwroot/css/guide.css
      to: WCAG 2.1 AAA standards
      via: Contrast ratios and focus indicator sizing
      pattern: "3px solid|contrast"
---

# Plan 106-03: Accessibility Enhancements

## Objective

Improve accessibility of User Guide page to WCAG 2.1 AAA standards with enhanced focus indicators, ARIA labels, keyboard navigation, and color contrast. Ensure all users, including those using assistive technologies, can navigate and use the guide effectively.

**Purpose:** Achieve WCAG 2.1 AAA accessibility compliance for inclusive user experience
**Output:** Enhanced ARIA attributes, keyboard navigation, focus indicators, and contrast improvements

## Tasks

<task type="auto">
<name>Task 1: Enhanced Focus Indicators</name>
<files>wwwroot/css/guide.css</files>
<action>
1. Create comprehensive focus state system at the end of guide.css:
   - Add `:focus-visible` pseudo-class for all interactive elements
   - Create consistent focus ring: `outline: 3px solid #667eea; outline-offset: 2px;`
   - Add high contrast focus ring for better visibility
2. Apply to FAQ questions (line 476):
   - Add `.faq-question:focus-visible { outline: 3px solid #667eea; outline-offset: 4px; background: rgba(102, 126, 234, 0.08); }`
   - Add `transition: background 0.2s ease, outline 0s;` to prevent outline animation
3. Apply to module card links (line 60):
   - Add `.guide-module-card:focus-visible { outline: 3px solid #667eea; outline-offset: 4px; }`
   - Ensure focus indicator is visible outside the card bounds
4. Apply to search input (line 127):
   - Enhance existing focus state (line 140) with more visible ring
   - Add `.guide-search-input:focus-visible { outline: 3px solid #667eea; outline-offset: 2px; }`
   - Ensure focus ring doesn't get cut off
5. Apply to back button (line 657):
   - Add `.guide-back-btn:focus-visible { outline: 3px solid #667eea; outline-offset: 3px; }`
6. Add skip link for keyboard users:
   - Add `.skip-link { position: absolute; top: -40px; left: 0; background: #667eea; color: white; padding: 8px 16px; z-index: 10000; text-decoration: none; }`
   - Add `.skip-link:focus { top: 0; }`
   - Add `.skip-link:hover { background: #5568d3; }`
7. Add focus indicator for accordion buttons:
   - Add `.guide-list-btn:focus-visible { outline: 3px solid #667eea; outline-offset: 2px; }`
</action>
<verify>
1. Tab through Guide page using keyboard only
2. Verify all interactive elements show clear focus indicators
3. Verify focus indicators have 3:1 contrast ratio against background (WCAG AAA)
4. Verify focus indicators are visible outside element bounds (outline-offset)
5. Test skip link functionality - tab from top should show skip link
6. Verify focus order is logical (top to bottom, left to right)
7. Test focus indicators on all interactive elements: cards, search, FAQ, back button
</verify>
<done>
- [ ] All interactive elements have 3px solid #667eea focus rings
- [ ] Focus indicators have 2-4px outline-offset for visibility
- [ ] Focus indicators have 3:1 contrast ratio against backgrounds
- [ ] Skip link appears when focused and jumps to main content
- [ ] Focus order is logical and predictable
</done>
</task>

<task type="auto">
<name>Task 2: ARIA Labels and Roles Enhancement</name>
<files>Views/Home/Guide.cshtml, Views/Home/GuideDetail.cshtml</files>
<action>
1. Open Views/Home/Guide.cshtml and enhance search input (line 45):
   - Add `aria-label="Cari panduan"` to input element
   - Add `aria-describedby="search-hint"` with hint text
   - Add live region for search results: `<div id="search-results" aria-live="polite" aria-atomic="true" class="sr-only"></div>` after the search wrapper
   - Locate search JavaScript around line 513 in the `<script>` section at bottom of Guide.cshtml
   - Find the search input event listener (inside document.addEventListener('DOMContentLoaded'))
   - Add this line after filtering completes: `document.getElementById('search-results').textContent = 'Ditemukan ' + visibleCards + ' dari ' + totalCards + ' panduan';`
   - Handle no results case: add `if (visibleCards === 0) { document.getElementById('search-results').textContent = 'Tidak ada panduan yang cocok'; }`
2. Enhance module cards (line 60):
   - Add `aria-label="Panduan CMP - 7 panduan tersedia"` to each card (adjust counts per module)
   - Add `aria-describedby="card-desc-cmp"` with description
   - Ensure current structure is semantic (already using `<a>` tags)
   - Add `role="article"` to card wrapper if not present
3. Enhance FAQ section (line 143):
   - Verify `aria-expanded` attributes are present (already on line 144)
   - Add `aria-controls="faq1"` to buttons linking to answer IDs
   - Add `aria-labelledby` to FAQ answers linking to question IDs
   - Verify all FAQ items have proper heading hierarchy (h2 for section, h3 for questions)
4. Add landmark regions:
   - Wrap main content in `<main role="main" id="main-content">`
   - Wrap FAQ in `<section aria-labelledby="faq-heading">`
   - Add navigation landmark for breadcrumb: `<nav role="navigation" aria-label="Breadcrumb">`
   - Add search landmark: `<section aria-labelledby="search-heading">`
5. Open Views/Home/GuideDetail.cshtml and enhance:
   - Add `role="navigation"` and `aria-label="Breadcrumb navigasi"` to breadcrumb
   - Add `aria-current="page"` to current page in breadcrumb
   - Ensure all accordions have proper ARIA attributes: aria-expanded, aria-controls
6. Add visually hidden labels where needed:
   - Add `.sr-only { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); border: 0; }` if not present
</action>
<verify>
1. Test with screen reader (NVDA on Windows or VoiceOver on Mac)
2. Verify search input is announced with label "Cari panduan"
3. Verify module cards are announced with descriptions and guide counts
4. Verify FAQ expand/collapse state is announced correctly
5. Verify search results count is announced via aria-live region
6. Verify landmark regions provide logical navigation structure
7. Verify all interactive elements are reachable via keyboard
8. Use browser accessibility inspector to verify ARIA attributes
</verify>
<done>
- [ ] Search input has aria-label="Cari panduan" and aria-describedby
- [ ] Module cards have aria-label with guide counts
- [ ] FAQ accordions have aria-expanded and aria-controls attributes
- [ ] Landmark regions (main, nav, section) are properly marked
- [ ] Breadcrumb has aria-current="page" for current page
- [ ] Screen reader announces all interactive elements correctly
</done>
</task>

<task type="auto">
<name>Task 3: Color and Contrast Improvements</name>
<files>wwwroot/css/guide.css</files>
<action>
1. Audit current color contrast ratios in guide.css:
   - Check text colors: `#2d3748` (dark gray) on white background - verify 7:1+ ratio
   - Check muted text: `#718096` on white - verify meets 7:1 for body text, darken if needed
   - Check FAQ answers: `#4a5568` on white - verify contrast
2. Enhance low-contrast colors if found:
   - Update `.guide-step-text span` (line 392) if contrast < 7:1: change `#718096` to darker `#4a5568`
   - Update `.guide-card-header-text p` (line 264) if contrast < 7:1
   - Update `.guide-search-muted` (if exists) to darker color
3. Enhance link visibility:
   - Add `text-decoration: underline` to all links on focus
   - Add `.guide-breadcrumb .breadcrumb-item a:focus { text-decoration: underline; }`
   - Add `.guide-step-item a:focus { text-decoration: underline; }`
4. Add focus indicator contrast verification:
   - Ensure focus ring `#667eea` has sufficient contrast against all backgrounds
   - Test focus ring on white: verify 3:1 contrast (WCAG AAA for focus indicators)
   - Test focus ring on gray and gradient backgrounds
5. Verify gradient text readability:
   - Check white text on gradient backgrounds (hero section)
   - Ensure text shadows maintain readability without reducing contrast
   - Verify role badge text is readable on semi-transparent background
6. Add high contrast mode support:
   - Add `@media (prefers-contrast: high)` with forced colors
   - Ensure all text remains readable in high contrast mode
</action>
<verify>
1. Use WebAIM Contrast Checker (https://webaim.org/resources/contrastchecker/) to verify ratios
2. Verify all normal text meets 7:1 contrast ratio (WCAG AAA)
3. Verify all large text (18pt+) meets 4.5:1 contrast ratio
4. Verify all interactive elements have visible focus indicators
5. Test with Windows High Contrast mode enabled
6. Verify all content remains readable when custom colors are disabled
7. Test in grayscale mode to ensure information isn't conveyed by color alone
8. Check gradient backgrounds maintain text readability
</verify>
<done>
- [ ] All normal text meets 7:1 contrast ratio minimum
- [ ] All large text meets 4.5:1 contrast ratio minimum
- [ ] Links show underline on focus
- [ ] Focus indicators have 3:1 contrast against all backgrounds
- [ ] Gradient text maintains readability with proper contrast
- [ ] High contrast mode doesn't break readability
</done>
</task>

<task type="auto">
<name>Task 4: Keyboard Navigation and Shortcuts</name>
<files>Views/Home/Guide.cshtml, Views/Home/GuideDetail.cshtml, wwwroot/css/guide.css</files>
<action>
1. Open Views/Home/Guide.cshtml and add keyboard shortcuts documentation:
   - Add visually hidden instruction box: `<div class="sr-only" id="keyboard-help">Gunakan Tab untuk navigasi, Enter/Space untuk membuka, Escape untuk menutup pencarian</div>`
   - Add `aria-describedby="keyboard-help"` to search input
2. Implement keyboard search enhancement (locate search JavaScript around line 513):
   - Add Escape key to clear search: `if (e.key === 'Escape') { this.value = ''; this.dispatchEvent(new Event('input')); this.blur(); }`
   - Add Enter key to navigate to first result (if desired)
3. Add skip links for major sections:
   - Add "Skip to search" link: `<a href="#guide-search" class="skip-link">Lompat ke pencarian</a>`
   - Add "Skip to modules" link: `<a href="#guide-modules" class="skip-link">Lompat ke modul</a>`
   - Add "Skip to FAQ" link: `<a href="#guide-faq" class="skip-link">Lompat ke FAQ</a>`
   - Add corresponding IDs to target sections
4. Ensure all accordions work with Enter/Space:
   - Verify FAQ buttons trigger on Enter and Space keys
   - Add event listener to handle Space key if needed: `addEventListener('keydown', (e) => { if (e.key === ' ') { e.preventDefault(); this.click(); } })`
5. Add focus management for search:
   - When search yields no results, move focus to "no results" message
   - When search filters results, announce count via aria-live region
   - Add `tabindex="-1"` to filtered out items to remove from tab order
6. Add focus trap for modals (if any exist):
   - Ensure keyboard focus cycles within modal
   - Add Escape key to close modal
7. Enhance GuideDetail.cshtml keyboard navigation:
   - Ensure back button is first focusable element
   - Add keyboard shortcuts for accordion navigation (Arrow keys)
</action>
<verify>
1. Tab through entire Guide page without mouse
2. Verify all interactive elements receive focus in logical order
3. Verify Enter/Space keys activate all buttons and links
4. Verify Escape key clears search input and closes modals
5. Verify skip links work and jump to correct sections
6. Verify focus moves to search results when search completes
7. Test with keyboard only - unplug mouse for testing
8. Verify focus doesn't get trapped in any component
</verify>
<done>
- [ ] Skip links allow jumping to main sections (search, modules, FAQ)
- [ ] Escape key clears search input
- [ ] Enter/Space keys activate all interactive elements
- [ ] Search result count is announced via aria-live
- [ ] Focus order is logical and predictable
- [ ] No keyboard traps exist on the page
</done>
</task>

## Verification

1. Run automated accessibility audit (WAVE, axe DevTools, or Lighthouse)
2. Test with screen reader (NVDA, JAWS, or VoiceOver)
3. Test keyboard navigation without mouse
4. Verify all focus indicators are visible (3:1 contrast minimum)
5. Verify all text meets WCAG AAA contrast (7:1 for normal text)
6. Test with Windows High Contrast mode
7. Test with browser zoom (200% zoom)
8. Verify ARIA attributes are correctly implemented
9. Test all functionality with keyboard only

## Notes

- Test with multiple screen readers: NVDA (Windows), VoiceOver (Mac), JAWS (if available)
- Use browser accessibility tools: Chrome Accessibility Inspector, Firefox Accessibility Toolbar
- Test keyboard navigation in different browsers (Chrome, Firefox, Safari, Edge)
- Consider adding "reduced motion" preference detection for animations
- Ensure custom focus indicators don't interfere with browser default focus styles
- Test with Windows High Contrast mode to ensure colors don't hardcode
- Verify all functionality works with keyboard alone (unplug mouse for testing)
- Consider adding keyboard shortcut hints in UI (e.g., "Press / to search")
- WCAG 2.1 AAA requires 7:1 contrast for normal text, 4.5:1 for large text (18pt+)
- Focus indicators must have 3:1 contrast against background
