---
phase: 108-timeline-detail-page-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/HistoriProtonDetailViewModel.cs
  - Controllers/CDPController.cs
autonomous: true
requirements:
  - HIST-10
  - HIST-11
  - HIST-12
  - HIST-13
  - HIST-14
  - HIST-15

must_haves:
  truths:
    - "HistoriProtonDetail action returns a populated ViewModel with timeline nodes"
    - "Nodes are ordered chronologically by Tahun (1 -> 2 -> 3)"
    - "Each node has coach name, status, competency level, dates"
  artifacts:
    - path: "Models/HistoriProtonDetailViewModel.cs"
      provides: "ViewModel with worker header + timeline nodes"
      contains: "HistoriProtonDetailViewModel"
    - path: "Controllers/CDPController.cs"
      provides: "Populated HistoriProtonDetail action"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Models/HistoriProtonDetailViewModel.cs"
      via: "new HistoriProtonDetailViewModel"
      pattern: "HistoriProtonDetailViewModel"
---

<objective>
Create the HistoriProtonDetailViewModel and populate it in the CDPController HistoriProtonDetail action by querying ProtonTrackAssignment, ProtonFinalAssessment, CoachCoacheeMapping, and User data.

Purpose: Provide the data layer for the timeline detail page (Plan 02 builds the view).
Output: ViewModel class + fully populated controller action.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-timeline-detail-page-styling/108-CONTEXT.md
@.planning/phases/108-timeline-detail-page-styling/108-RESEARCH.md

@Models/HistoriProtonViewModel.cs
@Controllers/CDPController.cs
@Models/ProtonModels.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HistoriProtonDetailViewModel</name>
  <files>Models/HistoriProtonDetailViewModel.cs</files>
  <action>
Create new file Models/HistoriProtonDetailViewModel.cs with two classes:

1. HistoriProtonDetailViewModel:
   - string Nama, NIP, Unit, Section, Jalur (worker header info)
   - List&lt;ProtonTimelineNode&gt; Nodes (timeline data)

2. ProtonTimelineNode:
   - int AssignmentId
   - string TahunKe (e.g. "Tahun 1")
   - int TahunUrutan (1, 2, 3 for sorting)
   - string Unit (worker's unit)
   - string CoachName (from CoachCoacheeMapping)
   - string Status ("Lulus" or "Dalam Proses")
   - int? CompetencyLevel (0-5, null if not lulus)
   - DateTime StartDate (AssignedAt)
   - DateTime? EndDate (CompletedAt from ProtonFinalAssessment, null if in progress)

Follow the same patterns as HistoriProtonViewModel.cs (namespace, using statements).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>HistoriProtonDetailViewModel.cs exists with both classes, project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Populate HistoriProtonDetail controller action</name>
  <files>Controllers/CDPController.cs</files>
  <action>
Modify the existing HistoriProtonDetail action in CDPController.cs (currently returns empty View()) to:

1. Query target user: `_context.Users.FindAsync(userId)` — return NotFound if null.
2. Query ProtonTrackAssignments for userId with Include(ProtonTrack), ordered by ProtonTrack.Urutan.
3. If no assignments, return NotFound (no Proton history).
4. Query ProtonFinalAssessments where ProtonTrackAssignmentId is in the assignment IDs, build dictionary keyed by assignment ID.
5. Query CoachCoacheeMapping: find coach for this coachee. Use CoachCoacheeMapping where CoacheeId == userId, order by IsActive descending then by Id descending, take first. Look up coach FullName. Default to "N/A".
6. Determine Jalur from first assignment's ProtonTrack.TrackType.
7. Build List&lt;ProtonTimelineNode&gt; from assignments: for each assignment, check if assessment exists to determine Status ("Lulus" vs "Dalam Proses"), populate CompetencyLevel and EndDate from assessment if exists.
8. Order nodes by TahunUrutan.
9. Build HistoriProtonDetailViewModel with worker header + nodes, return View(viewModel).

Use the exact query pattern from 108-RESEARCH.md code example. Keep existing auth checks intact — only replace the `return View()` with the data query + populated view.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>HistoriProtonDetail action queries all data sources and returns populated ViewModel. Builds successfully.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- HistoriProtonDetailViewModel.cs has both classes with all required properties
- CDPController.HistoriProtonDetail queries assignments, assessments, coach mapping
- Nodes ordered by TahunUrutan (chronological)
</verification>

<success_criteria>
- ViewModel exists with worker header fields and timeline node list
- Controller action populates all fields from database
- Each node has: TahunKe, Unit, CoachName, Status, CompetencyLevel (if lulus), StartDate, EndDate (if lulus)
- Chronological ordering enforced
</success_criteria>

<output>
After completion, create `.planning/phases/108-timeline-detail-page-styling/108-01-SUMMARY.md`
</output>
