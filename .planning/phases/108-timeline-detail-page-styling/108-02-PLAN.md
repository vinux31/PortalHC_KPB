---
phase: 108-timeline-detail-page-styling
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - Views/CDP/HistoriProtonDetail.cshtml
autonomous: false
requirements:
  - HIST-09
  - HIST-16
  - HIST-17

must_haves:
  truths:
    - "Vertical timeline renders with left-aligned line and cards to the right"
    - "Each node shows collapsed summary (Tahun + Jalur + Status badge) and expands to show details"
    - "Status circles are colored: green=Lulus, yellow=Dalam Proses"
    - "Page is responsive on mobile"
    - "Worker header card shows Nama, NIP, Unit, Section, Jalur"
  artifacts:
    - path: "Views/CDP/HistoriProtonDetail.cshtml"
      provides: "Full timeline detail page"
      min_lines: 80
  key_links:
    - from: "Views/CDP/HistoriProtonDetail.cshtml"
      to: "Models/HistoriProtonDetailViewModel.cs"
      via: "@model directive"
      pattern: "HistoriProtonDetailViewModel"
---

<objective>
Replace the placeholder HistoriProtonDetail.cshtml with the full vertical timeline view: worker header card, left-aligned timeline with expandable nodes, status-colored circles, and responsive styling.

Purpose: Complete the user-facing timeline detail page for Proton history.
Output: Fully styled, responsive timeline page.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-timeline-detail-page-styling/108-CONTEXT.md
@.planning/phases/108-timeline-detail-page-styling/108-RESEARCH.md
@.planning/phases/108-timeline-detail-page-styling/108-01-SUMMARY.md

@Views/CDP/HistoriProton.cshtml
@Models/HistoriProtonDetailViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build timeline detail view with CSS and responsive layout</name>
  <files>Views/CDP/HistoriProtonDetail.cshtml</files>
  <action>
Replace the placeholder HistoriProtonDetail.cshtml with a complete Razor view. Use @model HistoriProtonDetailViewModel.

**Page structure:**

1. **Breadcrumb** (top): CDP > Histori Proton (link to list) > Detail. Use standard breadcrumb pattern from other CDP views.

2. **Page title**: "Detail Histori Proton" in ViewData["Title"]. No step indicator.

3. **Worker header card** (Bootstrap card, shadow-sm): Display Nama, NIP, Unit, Section, Jalur in a compact layout. Use a dl/dt/dd or simple grid. Centered in col-lg-8 offset-lg-2.

4. **Timeline section** (centered col-lg-8 offset-lg-2):
   - Container div with class "timeline" and CSS pseudo-element for the vertical line on the left.
   - For each node in Model.Nodes, render a "timeline-node" div with status class (status-lulus or status-proses).
   - Each node has a circle pseudo-element (::before) colored by status.
   - Inside each node: Bootstrap card (shadow-sm) with:
     - Card header (clickable, data-bs-toggle="collapse"): "Tahun {TahunKe}" + Jalur + status badge
     - Collapsible card body with: Unit, Coach name, Competency Level (if lulus), Start date, End date (if available)
   - Use Bootstrap Collapse for expand/toggle. No animation on page load — all collapsed by default.

5. **Connector line logic:**
   - Each timeline-node controls the line segment below it via an ::after pseudo-element.
   - Lulus nodes: solid line segment below.
   - Dalam Proses nodes: dashed line segment below.
   - Last node: no line segment below (use :last-child to hide).

6. **Status badge colors** (match list page): Lulus = bg-success, Dalam Proses = bg-warning text-dark.

7. **Circle colors:** Lulus = filled green (var(--bs-success)), Dalam Proses = filled yellow (var(--bs-warning)).

**CSS** (in a style block within the view):
- .timeline: position relative, padding-left 40px
- .timeline::before: vertical line (2px, gray-400, absolute left 15px)
- .timeline-node: position relative, margin-bottom 1.5rem
- .timeline-node::before: circle (14px, border-radius 50%, positioned left of line)
- .timeline-node.status-lulus::before: bg-success, border-success
- .timeline-node.status-proses::before: bg-warning, border-warning
- .timeline-node::after: line segment below (2px wide, absolute)
- .timeline-node.status-lulus::after: solid line
- .timeline-node.status-proses::after: dashed line
- .timeline-node:last-child::after: display none
- Clip the main ::before line to not extend past last node

**Date formatting:** Use .ToString("dd MMM yyyy") for dates. Show "-" for null EndDate.

**Empty state:** If Model.Nodes is empty (shouldn't happen per controller logic, but defensive), show "Tidak ada riwayat Proton."

**Mobile:** col-lg-8 naturally becomes full-width on smaller screens. No special mobile CSS needed per user decision.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>Timeline view renders with left-aligned vertical line, colored circles, expandable cards, worker header, breadcrumb, and responsive layout. Builds successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify timeline detail page in browser</name>
  <action>Human verifies the complete Histori Proton detail page in browser.</action>
  <what-built>Complete HistoriProton detail page with vertical timeline, worker header card, expandable nodes with status badges, and responsive styling.</what-built>
  <how-to-verify>
    1. Run `dotnet run` and navigate to CDP > Histori Proton
    2. Click on a worker row to open their detail page
    3. Verify: breadcrumb shows "CDP > Histori Proton > Detail"
    4. Verify: worker header card shows Nama, NIP, Unit, Section, Jalur
    5. Verify: vertical timeline with left-aligned line and colored circles
    6. Verify: green circles for Lulus, yellow for Dalam Proses
    7. Verify: collapsed summary shows Tahun + Jalur + status badge
    8. Click a node header to expand — verify Unit, Coach, Competency Level, Dates appear
    9. Verify: solid connector line between Lulus nodes, dashed before incomplete
    10. Resize browser to mobile width — verify layout remains usable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- Timeline page renders with all required data fields per node
- Expand/collapse works via Bootstrap Collapse
- Status colors match: green=Lulus, yellow=Dalam Proses
- Responsive on mobile
- Breadcrumb navigation works
</verification>

<success_criteria>
- Vertical timeline with colored node circles and connector lines
- Worker header card with Nama, NIP, Unit, Section, Jalur
- Each node expandable: summary shows Tahun + Status, detail shows Unit, Coach, Level, Dates
- Chronological order maintained
- Bootstrap 5 consistent design
- Responsive mobile layout
</success_criteria>

<output>
After completion, create `.planning/phases/108-timeline-detail-page-styling/108-02-SUMMARY.md`
</output>
