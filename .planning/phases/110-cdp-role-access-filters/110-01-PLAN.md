---
phase: 110-cdp-role-access-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/HistoriProton.cshtml
autonomous: true
requirements: [ROLE-07, FILT-03, UX-03]

must_haves:
  truths:
    - "HistoriProton Bagian/Unit dropdowns show all OrganizationStructure sections, not data-driven"
    - "Selecting a Bagian cascades the Unit dropdown to show only that Bagian's units"
    - "L4 user sees Bagian dropdown locked to their section"
    - "Empty filter results show context-specific message instead of empty table"
    - "CoachingProton role scoping, filters, and empty states confirmed correct (no changes needed)"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "HistoriProton action with OrganizationStructure + L4 lock"
      contains: "OrganizationStructure"
    - path: "Views/CDP/HistoriProton.cshtml"
      provides: "Bagian/Unit cascade filters with L4 lock and empty state"
      contains: "SectionUnits"
  key_links:
    - from: "Views/CDP/HistoriProton.cshtml"
      to: "OrganizationStructure.SectionUnits"
      via: "JSON-serialized cascade data"
      pattern: "SectionUnits"
---

<objective>
Switch HistoriProton filters from data-driven to OrganizationStructure, add Bagian/Unit cascade with L4 lock, and add context-specific empty states. Verify CoachingProton is already correct.

Purpose: ROLE-07 (HistoriProton scoping), FILT-03 (OrganizationStructure filters), UX-03 (empty states)
Output: Updated HistoriProton with consistent filter pattern matching Phase 109
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-cmp-role-access-filters/109-01-SUMMARY.md

<interfaces>
From Controllers/CDPController.cs (HistoriProton action, line 2262-2401):
- Role scoping already correct: L1-3 all, L4 section, L5 mapped, L6 redirect to detail
- Currently builds AvailableSections/AvailableUnits from data (lines 2386-2394) -- MUST CHANGE to OrganizationStructure
- HistoriProtonViewModel has Workers, AvailableSections, AvailableUnits properties

From Views/CDP/HistoriProton.cshtml:
- Client-side JS filtering on data-* attributes (search, section, unit, jalur, status)
- Section/Unit dropdowns populated from Model.AvailableSections/AvailableUnits -- MUST CHANGE
- Labels say "Section"/"Semua Section" -- MUST RENAME to "Bagian"/"Semua Bagian"
- Empty state exists but only for search ("Tidak ada data yang sesuai dengan pencarian")

From Models/OrganizationStructure.cs:
- OrganizationStructure.SectionUnits: Dictionary&lt;string, List&lt;string&gt;&gt;
- OrganizationStructure.GetAllSections(): List&lt;string&gt;
- OrganizationStructure.GetUnitsForSection(string section): List&lt;string&gt;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch HistoriProton filters to OrganizationStructure with cascade, L4 lock, and empty state</name>
  <files>Controllers/CDPController.cs, Views/CDP/HistoriProton.cshtml</files>
  <action>
**Controller (CDPController.cs, HistoriProton action around line 2385-2401):**
1. Remove the data-driven AvailableSections/AvailableUnits building (lines 2386-2394)
2. Replace with OrganizationStructure static lists:
   ```csharp
   var viewModel = new HistoriProtonViewModel
   {
       Workers = workers,
       AvailableSections = HcPortal.Models.OrganizationStructure.GetAllSections(),
       AvailableUnits = new List<string>() // Units populated client-side via cascade
   };
   ```
3. Add L4 lock: if `userLevel == 4`, pass `ViewBag.LockedSection = user.Section;` and `ViewBag.UserLevel = userLevel;`
4. Serialize SectionUnits for client-side cascade: `ViewBag.OrgStructureJson = System.Text.Json.JsonSerializer.Serialize(HcPortal.Models.OrganizationStructure.SectionUnits);`

**View (Views/CDP/HistoriProton.cshtml):**
1. Rename "Section" label to "Bagian", "Semua Section" to "Semua Bagian"
2. Replace `filterSection` dropdown ID to `filterBagian` (update all JS references)
3. Populate Bagian dropdown from OrganizationStructure (already in Model.AvailableSections which now comes from OrganizationStructure)
4. Clear Unit dropdown options (will be populated by cascade JS)
5. Add L4 lock logic at top of view:
   ```csharp
   @{
       var lockedSection = ViewBag.LockedSection as string;
       var userLevel = (int)(ViewBag.UserLevel ?? 0);
       var isLocked = userLevel == 4 && !string.IsNullOrEmpty(lockedSection);
   }
   ```
   If isLocked: disable Bagian dropdown, show only locked section, auto-select it
6. Add Bagian->Unit cascade JS (same pattern as Phase 109 RecordsTeam):
   - Parse OrgStructureJson into JS object
   - On Bagian change: populate Unit dropdown with matching units, reset Unit selection
   - If L4 locked: trigger cascade on page load to populate units for locked section
7. Update data-* attribute from `data-section` to `data-bagian` on table rows (or keep data-section but update JS filter reference)
8. Update applyFilters() to use the renamed filterBagian element
9. Add context-specific empty state message: when no rows match filters, show icon + "Tidak ada pekerja yang sesuai dengan filter." (replacing generic search message)
10. Ensure "Reset" button also resets cascade (clear Unit dropdown back to "Semua Unit", re-enable if was locked by cascade)
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>HistoriProton uses OrganizationStructure for Bagian/Unit, cascade works, L4 sees locked Bagian, empty filter results show context message</done>
</task>

<task type="auto">
  <name>Task 2: Verify CoachingProton role scoping, filters, and empty states are correct</name>
  <files>Views/CDP/CoachingProton.cshtml</files>
  <action>
Read CDPController.cs CoachingProton action (line ~1257-1670) and Views/CDP/CoachingProton.cshtml to verify:
1. Role scoping: L1-3 all coachees, L4 section-only, L5 mapped coachees, L6 self — confirm this is implemented
2. Bagian/Unit filters already use OrganizationStructure (FILT-03) — confirm no data-driven queries for filter options
3. Empty states (select_coachee, no_coachees, no_filter_match, no_deliverables) already implemented (UX-03) — confirm they show context-specific messages

If everything is correct: no code changes needed, just document verification in summary.
If any gap found: fix it in CoachingProton.cshtml or CDPController.cs.

Per CONTEXT.md: "Controller role scoping already correct... Bagian/Unit filters already use OrganizationStructure with cascade — no changes needed... Empty states already implemented — keep as-is. Only verify nothing is missing."
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>CoachingProton confirmed correct for ROLE-03, FILT-03, UX-03 — documented with evidence</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- HistoriProton view renders Bagian/Unit from OrganizationStructure (grep for SectionUnits in view)
- No references to Model.AvailableSections or Model.AvailableUnits in HistoriProton dropdown source (data still passed for ViewModel compat but not used for dropdown population if switched to OrgStructure in view)
- CoachingProton verified unchanged and correct
</verification>

<success_criteria>
- HistoriProton Bagian/Unit dropdowns populated from OrganizationStructure
- Bagian->Unit cascade works client-side
- L4 users see Bagian locked to their section
- Empty filter results show context-specific message
- CoachingProton verified correct (no changes needed)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/110-cdp-role-access-filters/110-01-SUMMARY.md`
</output>
