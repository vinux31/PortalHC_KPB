---
phase: 111-sectionhead-filter-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/CoachingProton.cshtml
  - Views/CDP/Deliverable.cshtml
  - Views/Shared/_Layout.cshtml
autonomous: true
requirements: [SH-01, SH-02, SH-03]

must_haves:
  truths:
    - "SectionHead at level 4 has identical access as SrSupervisor on every CMP/CDP page"
    - "Navbar shows CMP, CDP, Guide for SectionHead — same as SrSupervisor"
    - "SrSpv OR SH approval is sufficient to mark deliverable Approved"
    - "After one L4 role approves, the other can still co-sign"
    - "Both SH and SrSpv can reject deliverables"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Co-sign guard relaxation in ApproveDeliverable, ApproveFromProgress, RejectFromProgress"
      contains: "progress.Status == \"Approved\""
    - path: "Views/CDP/CoachingProton.cshtml"
      provides: "Co-sign approve button visible when own approval is Pending"
    - path: "Views/CDP/Deliverable.cshtml"
      provides: "Co-sign approve button on detail page"
  key_links:
    - from: "Views/CDP/CoachingProton.cshtml"
      to: "Controllers/CDPController.cs"
      via: "AJAX approve/reject calls"
      pattern: "ApproveFromProgress|RejectFromProgress"
---

<objective>
Ensure SectionHead at level 4 has identical access to SrSupervisor across all pages, and enable approval co-sign between SH and SrSpv on deliverables.

Purpose: SH was moved to level 4 but approval co-sign logic and view button conditions don't support the interchangeable approval pattern yet.
Output: Updated CDPController approval logic + CoachingProton/Deliverable view co-sign buttons + verified SH access parity.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-sectionhead-filter-infrastructure/111-CONTEXT.md
@.planning/phases/111-sectionhead-filter-infrastructure/111-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SH access audit and CDP role-name refactor</name>
  <files>Controllers/CDPController.cs, Views/Shared/_Layout.cshtml</files>
  <action>
1. Audit ALL controllers and views for any code that checks role NAME strings ('Sr Supervisor', 'Section Head', 'SrSupervisor', 'SectionHead') instead of level-based checks (`userLevel == 4` or `roleLevel == 4`).

Run grep across Controllers/ and Views/ for these patterns:
- `UserRoles.SrSupervisor` or `UserRoles.SectionHead` used in if/else conditions
- String literals "Sr Supervisor", "Section Head", "SrSupervisor", "SectionHead" in controller logic
- Any `[Authorize(Roles = ...)]` attributes that include one but not both L4 roles

2. For CDPController.cs: where role-name checks like `userRole == UserRoles.SrSupervisor || userRole == UserRoles.SectionHead` exist, refactor to `roleLevel == 4` using the established CMP pattern. This is a functional equivalence refactor — both roles are level 4.

3. For _Layout.cshtml: VERIFY (do not change unless broken) that navbar shows CMP, CDP, Guide to all authenticated users and Kelola Data only to Admin/HC. Research says this is already correct.

4. For any [Authorize(Roles=...)] attributes: verify both SectionHead and SrSupervisor are included where one is. Research says Deliverable already includes both — confirm.

5. Document all findings (what was already correct, what was changed) for the SUMMARY.
  </action>
  <verify>
    <automated>grep -rn "UserRoles.SrSupervisor\|UserRoles.SectionHead" Controllers/CDPController.cs | head -20</automated>
    After refactor, CDP controller should use roleLevel == 4 instead of role-name checks. Remaining role-name references should only be in approval-specific field assignments (SrSpvApprovalStatus vs ShApprovalStatus) which legitimately differ per role.
  </verify>
  <done>All CDP controller access checks use level-based comparisons. Navbar verified correct for SH. No role-name gatekeeping that would differentiate SH from SrSpv.</done>
</task>

<task type="auto">
  <name>Task 2: Approval co-sign logic and view updates</name>
  <files>Controllers/CDPController.cs, Views/CDP/CoachingProton.cshtml, Views/CDP/Deliverable.cshtml</files>
  <action>
1. CDPController.cs — Relax approval guards in THREE endpoints:

   a. `ApproveDeliverable` (~line 868): Change `if (progress.Status != "Submitted")` to allow approval when Status is "Submitted" OR (Status is "Approved" AND the current role's own approval field is still "Pending"). This enables co-sign.

   b. `ApproveFromProgress` (~line 1686): Same guard relaxation as above.

   c. `RejectFromProgress` (~line 1751): Verify rejection works for both SH and SrSpv. After rejection and resubmission, same co-sign rules apply.

   Pattern for the guard:
   ```csharp
   bool isSrSpv = userRole == UserRoles.SrSupervisor;
   bool isSH = userRole == UserRoles.SectionHead;
   bool canApprove = progress.Status == "Submitted" ||
       (progress.Status == "Approved" && (
           (isSrSpv && progress.SrSpvApprovalStatus != "Approved") ||
           (isSH && progress.ShApprovalStatus != "Approved")));
   ```

2. CoachingProton.cshtml — Update approve button visibility:
   - Currently: button shows only when `item.Status == "Submitted"` AND own approval is "Pending"
   - Change to: button shows when `(item.Status == "Submitted" || item.Status == "Approved")` AND own approval is "Pending"
   - When Status is already "Approved" (by the other L4), button text should indicate co-sign context (e.g., "Approve" is fine — same action)
   - Update AJAX success handler to correctly update badge after co-sign (status stays "Approved", but approval cell updates)

3. Deliverable.cshtml — Update approve button on detail page:
   - Same logic: show approve button when own approval is Pending regardless of whether the other L4 already approved
   - Timeline should already show both approval events when both approve (verify existing timeline rendering handles this)

4. After rejection and resubmission: both SrSpvApprovalStatus and ShApprovalStatus should reset to "Pending" — verify this is already handled in the resubmission flow. If not, add reset logic.
  </action>
  <verify>
    <automated>grep -n "Status != \"Submitted\"" Controllers/CDPController.cs | head -10</automated>
    After changes, no hard guard blocking co-sign should remain. The guard should allow "Approved" status when own approval is "Pending".
  </verify>
  <done>SrSpv can approve after SH already approved (and vice versa). CoachingProton and Deliverable views show approve button for co-sign. Rejection works for both roles. Assessment eligibility unchanged (just checks Status == "Approved").</done>
</task>

</tasks>

<verification>
- Grep CDPController for role-name checks — should only remain in approval field assignments, not access gates
- Grep for `Status != "Submitted"` — should be replaced with co-sign-aware guard
- CoachingProton view approve button condition includes `Status == "Approved"` case
</verification>

<success_criteria>
1. SectionHead sees identical pages and navigation as SrSupervisor
2. Either SrSpv or SH can approve a deliverable first; the other can co-sign after
3. Both roles can reject deliverables
4. No role-name string checks gate access differently between SH and SrSpv
</success_criteria>

<output>
After completion, create `.planning/phases/111-sectionhead-filter-infrastructure/111-01-SUMMARY.md`
</output>
