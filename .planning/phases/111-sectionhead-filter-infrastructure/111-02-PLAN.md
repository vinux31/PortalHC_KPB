---
phase: 111-sectionhead-filter-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ManageWorkers.cshtml
autonomous: true
requirements: [FILT-04, FILT-05]

must_haves:
  truths:
    - "ManageWorkers Bagian dropdown populated from OrganizationStructure, not hardcoded array"
    - "ManageWorkers has Unit dropdown that cascades from selected Bagian"
    - "Changing Bagian resets Unit to Semua Unit"
    - "Export respects Bagian + Unit + Role + Search filters"
    - "All existing cascade filters (RecordsTeam, HistoriProton, PlanIdp, CoachingProton) still work correctly"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "ManageWorkers and ExportWorkers with unitFilter parameter and OrganizationStructure"
      contains: "OrganizationStructure.GetAllSections"
    - path: "Views/Admin/ManageWorkers.cshtml"
      provides: "Bagian > Unit > Role > Search filter bar with cascade"
      contains: "ViewBag.AllUnits"
  key_links:
    - from: "Views/Admin/ManageWorkers.cshtml"
      to: "Controllers/AdminController.cs"
      via: "form submit with sectionFilter + unitFilter params"
      pattern: "unitFilter"
---

<objective>
Refactor ManageWorkers filter to use OrganizationStructure with Unit cascade, and audit all existing cascade filters for correctness.

Purpose: ManageWorkers uses a hardcoded sections array and lacks Unit filtering. This brings it in line with the OrganizationStructure pattern established in Phases 109-110.
Output: Updated AdminController + ManageWorkers view with OrganizationStructure filters and cascade.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-sectionhead-filter-infrastructure/111-CONTEXT.md
@.planning/phases/111-sectionhead-filter-infrastructure/111-RESEARCH.md
@.planning/phases/109-cmp-role-access-filters/109-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ManageWorkers filter refactor with Unit cascade</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManageWorkers.cshtml</files>
  <action>
1. AdminController.cs — Update `ManageWorkers` action (~line 3129):
   - Add `string? unitFilter` parameter
   - Replace any hardcoded section references with `OrganizationStructure.GetAllSections()`
   - Add unit filtering: `if (!string.IsNullOrEmpty(unitFilter)) query = query.Where(u => u.Unit == unitFilter);`
   - Set ViewBag values for the view:
     ```csharp
     ViewBag.AllSections = OrganizationStructure.GetAllSections();
     ViewBag.AllUnits = !string.IsNullOrEmpty(sectionFilter)
         ? OrganizationStructure.GetUnitsForSection(sectionFilter)
         : new List<string>();
     ViewBag.SectionFilter = sectionFilter;
     ViewBag.UnitFilter = unitFilter;
     ```
   - Server-side validation: if unitFilter doesn't belong to selected sectionFilter, ignore it (treat as null)

2. AdminController.cs — Update `ExportWorkers` action (~line 3662):
   - Add `string? unitFilter` parameter
   - Apply same unit filtering logic as ManageWorkers
   - Ensure export query matches exactly what ManageWorkers displays

3. ManageWorkers.cshtml — Refactor filter bar (~line 130-150):
   - Reorder filters: Bagian > Unit > Role > Search
   - Bagian dropdown: populate from `ViewBag.AllSections` instead of hardcoded `new[] { "GAST", "RFCC", "NGP", "DHT / HMU" }`
   - Add Unit dropdown: populate from `ViewBag.AllUnits`, selected value from `ViewBag.UnitFilter`
   - Add "Semua Unit" as default option
   - Bagian `onchange`: submit form (server-side cascade — changing Bagian reloads page with new units)
   - Unit `onchange`: submit form
   - When Bagian changes, unitFilter should NOT be sent (so it resets). Use JavaScript: on Bagian change, clear the unitFilter select or remove it from form before submit. Simplest approach: set unitFilter select to empty string before form submit on Bagian change.
   - Style Unit dropdown to match existing Bagian/Role dropdown styling

4. Export link/button: update to include unitFilter in the export URL/form parameters.
  </action>
  <verify>
    <automated>grep -n "GAST\|RFCC\|NGP\|DHT" Views/Admin/ManageWorkers.cshtml | head -5</automated>
    After refactor, no hardcoded section arrays should remain. Should return empty.
  </verify>
  <done>ManageWorkers Bagian uses OrganizationStructure. Unit dropdown cascades from Bagian. Changing Bagian resets Unit. ExportWorkers respects unitFilter. No hardcoded section arrays remain.</done>
</task>

<task type="auto">
  <name>Task 2: Cascade filter audit across all pages</name>
  <files>Controllers/AdminController.cs</files>
  <action>
1. Code audit (NO browser testing) — verify cascade works correctly on these pages by reading the controller + view code:

   a. RecordsTeam (Phase 109): Check that selecting Bagian populates Unit dropdown via OrganizationStructure. Read CMPController RecordsTeam action and Views/CMP/RecordsTeam.cshtml filter section.

   b. HistoriProton (Phase 110): Same check. Read CDPController HistoriProton action and Views/CDP/HistoriProton.cshtml filter section.

   c. PlanIdp (Phase 110): Same check. Read CDPController PlanIdp action and Views/CDP/PlanIdp.cshtml filter section.

   d. CoachingProton (pre-existing): Same check. Read CDPController CoachingProton action and Views/CDP/CoachingProton.cshtml filter section.

   e. ManageWorkers (just implemented in Task 1): Verify cascade wiring is complete.

2. For each page, document:
   - How Bagian dropdown is populated (should be OrganizationStructure)
   - How Unit dropdown cascades (server-side or client-side JS)
   - Whether Bagian change resets Unit
   - Any issues found

3. If any issues found in existing pages, fix them. Research indicates all existing pages should be correct from Phases 109-110.

4. Document findings in the SUMMARY for future reference.
  </action>
  <verify>
    <automated>grep -rn "OrganizationStructure" Controllers/CMPController.cs Controllers/CDPController.cs Controllers/AdminController.cs | head -20</automated>
    All filter pages should reference OrganizationStructure for section/unit data.
  </verify>
  <done>All 5 pages (RecordsTeam, HistoriProton, PlanIdp, CoachingProton, ManageWorkers) use OrganizationStructure for Bagian and cascade Unit correctly. Audit documented.</done>
</task>

</tasks>

<verification>
- No hardcoded section arrays in ManageWorkers view
- AdminController ManageWorkers and ExportWorkers both accept unitFilter
- OrganizationStructure referenced in all filter controllers
- Cascade pattern consistent across all pages
</verification>

<success_criteria>
1. ManageWorkers filter uses OrganizationStructure for Bagian dropdown
2. Unit dropdown appears and cascades from selected Bagian
3. Changing Bagian resets Unit to "Semua Unit"
4. ExportWorkers respects all filters including Unit
5. All existing cascade filters verified correct via code audit
</success_criteria>

<output>
After completion, create `.planning/phases/111-sectionhead-filter-infrastructure/111-02-SUMMARY.md`
</output>
