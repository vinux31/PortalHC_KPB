---
phase: 112-coachingproton-button-badge-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CDP/CoachingProton.cshtml
autonomous: false
requirements:
  - BTN-01
  - BTN-02
  - BTN-03
  - CONS-01
  - CONS-02
  - CONS-03
  - CONS-04
  - TECH-01
  - TECH-02
  - TECH-03

must_haves:
  truths:
    - "Pending badges in SrSpv/SH columns render as outline-warning buttons with text 'Tinjau'"
    - "Non-clickable Pending stays as gray badge (no button styling)"
    - "Resolved status badges (Approved, Rejected, Reviewed) show bold text with border"
    - "Evidence 'Sudah Upload' badge is bold with green border; 'Belum Upload' stays normal gray"
    - "Export PDF uses btn-outline-success (not btn-outline-danger)"
    - "Clicking Tinjau button still opens the approval modal"
    - "After AJAX approve/reject, the replacement badge uses new bold+border styling"
  artifacts:
    - path: "Views/CDP/CoachingProton.cshtml"
      provides: "All button and badge redesign changes"
      contains: "btn-outline-warning btnTinjau"
  key_links:
    - from: "Views/CDP/CoachingProton.cshtml (Tinjau buttons)"
      to: "#tinjaModal"
      via: "data-bs-toggle modal trigger"
      pattern: "btnTinjau.*data-bs-toggle"
    - from: "Views/CDP/CoachingProton.cshtml (JS innerHTML)"
      to: "AJAX badge updates"
      via: "innerHTML assignment after fetch"
      pattern: "innerHTML.*badge.*fw-bold"
---

<objective>
Redesign all buttons and badges on CoachingProton page so interactive elements are visually distinct from status indicators.

Purpose: Users currently cannot distinguish clickable badges from read-only status badges. This redesign converts clickable elements to proper buttons and adds visual weight to resolved status badges.
Output: Updated CoachingProton.cshtml with consistent button/badge styling across server-rendered HTML and JS innerHTML updates.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-coachingproton-button-badge-redesign/112-CONTEXT.md
@.planning/phases/112-coachingproton-button-badge-redesign/112-RESEARCH.md
@Views/CDP/CoachingProton.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Razor helpers, HTML buttons/badges, and JS innerHTML</name>
  <files>Views/CDP/CoachingProton.cshtml</files>
  <action>
All changes in a single file. Follow the 5 categories from RESEARCH.md:

**Category A -- Razor helper functions (~line 920-957):**
Update `GetApprovalBadge` and `GetApprovalBadgeWithTooltip`:
- Approved: add `fw-bold border border-success` to badge classes
- Rejected: add `fw-bold border border-danger` to badge classes
- Reviewed: add `fw-bold border border-success` to badge classes
- Pending (non-clickable): keep `badge bg-secondary` unchanged
- This fixes ALL server-rendered status badges across both table views automatically

**Category B -- Tinjau button conversion (4 locations):**
Lines ~440-448, ~462-471, ~556-564, ~579-588 (approximate -- find by searching for `btnTinjau` in badge spans):
- Change `<span class="badge bg-warning text-dark btnTinjau" role="button" style="cursor:pointer"` to `<button type="button" class="btn btn-sm btn-outline-warning btnTinjau"`
- Change text from "Pending" to "Tinjau"
- Keep ALL data attributes: `data-bs-toggle="modal"`, `data-bs-target="#tinjaModal"`, `data-progress-id`, etc.
- Change closing `</span>` to `</button>`
- Remove `role="button"` and `style="cursor:pointer"` (not needed on actual buttons)
- Do this for ALL 4 locations (2 in multi-coachee view, 2 in single-coachee view)

**Category C -- Evidence badges (4 HTML locations):**
Lines ~429, 433, 545, 549 (find by searching for "Sudah Upload" and "Belum Upload" badge spans):
- "Sudah Upload": add `fw-bold border border-success` to existing badge classes
- "Belum Upload": keep as `badge bg-secondary` (no change)

**Category D -- JS innerHTML updates (6 locations):**
Search the `<script>` section for ALL `innerHTML` assignments that render badges:
- Approval badge (~line 1102): add `fw-bold border border-${color}` where color maps from status
- HC review badge (~lines 1138, 1179): add `fw-bold border border-success` for Reviewed status
- Evidence badges (~lines 1417-1419, 1424, 1428): "Sudah Upload" innerHTML gets `fw-bold border border-success`
- CRITICAL: The JS `badgeClass` variable already maps status to color. For the border, derive from status: Approved->success, Rejected->danger, Reviewed->success

**Category E -- Export PDF button (1 location):**
Line ~294: change `btn-outline-danger` to `btn-outline-success` on the Export PDF link

**DO NOT change:**
- The counter badge in HC Review Panel header (`@pendingCount pending` badge bg-warning) -- this is a notification counter, not a status badge
- Lihat Detail buttons -- already `btn btn-sm btn-outline-secondary` per convention
- HC Review buttons -- already `btn btn-sm btn-outline-success` per convention
- Reset/Kembali buttons -- already correct
- Submit Evidence button -- stays `btn-primary`
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
- All 4 Tinjau locations converted from span badges to button elements with text "Tinjau"
- GetApprovalBadge/GetApprovalBadgeWithTooltip add fw-bold border for resolved statuses
- Evidence "Sudah Upload" badges have fw-bold border border-success
- All JS innerHTML assignments match new styling conventions
- Export PDF uses btn-outline-success
- dotnet build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of button and badge redesign</name>
  <files>Views/CDP/CoachingProton.cshtml</files>
  <action>
User visually verifies the redesigned CoachingProton page across multiple roles. No code changes in this task.

What was built: Complete button/badge redesign -- Tinjau buttons, status badge styling, evidence badges, export button color, and JS innerHTML consistency.

How to verify:
1. Run the app (`dotnet run`) and navigate to CoachingProton page
2. As SrSupervisor role: verify SrSpv column shows "Tinjau" outline-warning buttons (not "Pending" badges) for pending items
3. Click a Tinjau button -- verify modal opens correctly
4. Approve an item -- verify the badge changes to bold green "Approved" with border (AJAX update)
5. As SectionHead role: verify SH column also shows "Tinjau" buttons, modal works
6. As Coach: verify Submit Evidence button is still filled blue (btn-primary)
7. Check Evidence column: "Sudah Upload" should be bold green with border, "Belum Upload" should be plain gray
8. Check Export PDF button -- should be green outline (not red)
9. Check HC Review panel: review buttons should be outline-success, counter badge should still be yellow
10. Verify non-clickable Pending badges (where user has no permission to approve) are plain gray

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&amp;1 | tail -3</automated>
  </verify>
  <done>User confirms all 10 visual/functional checks pass across SrSpv, SH, HC, and Coach roles</done>
</task>

</tasks>

<verification>
- `dotnet build` compiles without errors
- Visual inspection confirms all 10 requirements met
- Approval workflow (Tinjau -> modal -> approve/reject) still functions end-to-end
</verification>

<success_criteria>
- Interactive elements (Tinjau, Submit Evidence, HC Review, Export, etc.) are visually distinct from read-only status badges
- Resolved status badges have bold text with colored border for visual weight
- All AJAX-updated badges match server-rendered styling
- No JS regression in approval/evidence/review workflows
</success_criteria>

<output>
After completion, create `.planning/phases/112-coachingproton-button-badge-redesign/112-01-SUMMARY.md`
</output>
