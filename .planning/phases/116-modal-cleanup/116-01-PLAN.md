---
phase: 116-modal-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/CoachingSession.cs
  - Controllers/CDPController.cs
  - Views/CDP/CoachingProton.cshtml
  - Views/CDP/Deliverable.cshtml
  - Migrations/ClearCoacheeCompetenciesData.cs
autonomous: true
requirements:
  - MOD-01
  - MOD-02

must_haves:
  truths:
    - "Evidence modal in CoachingProton no longer shows Kompetensi Coachee textarea"
    - "Submitting evidence succeeds without koacheeCompetencies data"
    - "Deliverable coaching session table no longer shows Kompetensi Coachee column"
    - "Existing CoacheeCompetencies data in DB is cleared to empty string"
  artifacts:
    - path: "Models/CoachingSession.cs"
      provides: "CoachingSession model without CoacheeCompetencies property"
      contains: "public class CoachingSession"
    - path: "Migrations/ClearCoacheeCompetenciesData.cs"
      provides: "Data-clearing migration"
      contains: "CoacheeCompetencies = ''"
  key_links:
    - from: "Views/CDP/CoachingProton.cshtml"
      to: "Controllers/CDPController.cs"
      via: "formData POST to SubmitEvidenceWithCoaching"
      pattern: "SubmitEvidenceWithCoaching"
---

<objective>
Remove the unused "Kompetensi Coachee" textarea from the CoachingProton evidence modal and clean up all backend/display references.

Purpose: The field is not used in the coaching workflow and will be replaced by structured fields in Phase 120.
Output: Clean evidence modal, updated controller/model, data-clearing migration.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-modal-cleanup/116-CONTEXT.md
@.planning/phases/116-modal-cleanup/116-RESEARCH.md
@Models/CoachingSession.cs
@Controllers/CDPController.cs
@Views/CDP/CoachingProton.cshtml
@Views/CDP/Deliverable.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CoacheeCompetencies from model, controller, and views</name>
  <files>Models/CoachingSession.cs, Controllers/CDPController.cs, Views/CDP/CoachingProton.cshtml, Views/CDP/Deliverable.cshtml</files>
  <action>
Remove all CoacheeCompetencies references across 4 files:

1. **Models/CoachingSession.cs** line 12: Delete the `public string CoacheeCompetencies { get; set; }` property.

2. **Controllers/CDPController.cs**:
   - Line ~1884: Remove `[FromForm] string koacheeCompetencies` parameter from SubmitEvidenceWithCoaching action signature.
   - Line ~1994: Remove `CoacheeCompetencies = koacheeCompetencies` from the CoachingSession object initializer.

3. **Views/CDP/CoachingProton.cshtml**:
   - Lines ~866-870: Remove entire `<div class="mb-3">` block containing textarea `#evidenceKoacheeComp` and its label.
   - Lines ~1342, 1347: Remove JS lines `const koacheeComp = document.getElementById('evidenceKoacheeComp');` and `if (koacheeComp) koacheeComp.value = '';`.
   - Line ~1383: Remove `formData.append('koacheeCompetencies', ...)` line.

4. **Views/CDP/Deliverable.cshtml**:
   - Lines ~385-387: Remove the `<tr>` block displaying Kompetensi Coachee (header cell + data cell).

Note: CoachingLog also has a CoacheeCompetencies property -- this is explicitly OUT OF SCOPE per user decisions.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with zero errors. No references to CoacheeCompetencies remain in the 4 modified files (grep confirms).</done>
</task>

<task type="auto">
  <name>Task 2: Hand-write data-clearing migration</name>
  <files>Migrations/ClearCoacheeCompetenciesData.cs</files>
  <action>
Create a hand-written migration file (do NOT use `dotnet ef migrations add` as it would auto-generate a DropColumn call, violating the locked decision that the column stays in DB).

Create `Migrations/{timestamp}_ClearCoacheeCompetenciesData.cs` with:
- Namespace: `HcPortal.Migrations`
- Class: `ClearCoacheeCompetenciesData : Migration`
- Up method: `migrationBuilder.Sql(@"UPDATE CoachingSessions SET CoacheeCompetencies = ''");`
- Down method: empty (data clearing is irreversible)

Follow the exact pattern from 116-RESEARCH.md code example. Use an appropriate timestamp prefix (e.g., 20260307xxxxxx).

IMPORTANT: Do NOT run `dotnet ef migrations add` after this. The migration is hand-crafted. The DB column remains but is unmapped from EF Core model.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Migration file exists, build succeeds, migration contains SQL UPDATE clearing CoacheeCompetencies data.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with no errors
2. `grep -r "CoacheeCompetencies" Models/CoachingSession.cs` returns nothing
3. `grep -r "koacheeCompetencies" Controllers/CDPController.cs` returns nothing
4. `grep -r "evidenceKoacheeComp" Views/CDP/CoachingProton.cshtml` returns nothing
5. Migration file exists with SQL UPDATE statement
</verification>

<success_criteria>
- Build compiles cleanly
- Evidence modal textarea removed from view
- JS formData no longer sends koacheeCompetencies
- Controller no longer accepts the parameter
- Model property deleted
- Deliverable display column removed
- Data-clearing migration ready to apply
</success_criteria>

<output>
After completion, create `.planning/phases/116-modal-cleanup/116-01-SUMMARY.md`
</output>
