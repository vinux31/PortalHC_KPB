---
phase: 118-p-sign-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/PSignViewModel.cs
  - Views/Shared/_PSign.cshtml
  - Models/SettingsViewModel.cs
  - Controllers/AccountController.cs
  - Views/Account/Settings.cshtml
autonomous: true
requirements:
  - PSIGN-01
  - PSIGN-02
  - PSIGN-03

must_haves:
  truths:
    - "P-Sign badge renders with Logo Pertamina, Position, Unit, and FullName"
    - "Null/empty Position or Unit hides that row; badge still renders with available data"
    - "Settings page shows a live preview of the logged-in user's P-Sign"
  artifacts:
    - path: "Models/PSignViewModel.cs"
      provides: "PSignViewModel with LogoUrl, Position, Unit, FullName"
    - path: "Views/Shared/_PSign.cshtml"
      provides: "Self-contained partial view with inline styles"
    - path: "Views/Account/Settings.cshtml"
      provides: "P-Sign preview section"
  key_links:
    - from: "Controllers/AccountController.cs"
      to: "Models/PSignViewModel.cs"
      via: "Settings action populates PSign property"
      pattern: "new PSignViewModel"
    - from: "Views/Account/Settings.cshtml"
      to: "Views/Shared/_PSign.cshtml"
      via: "Html.PartialAsync"
      pattern: "PartialAsync.*_PSign"
---

<objective>
Create a reusable P-Sign (digital initial badge) component as a Razor partial view with ViewModel, and show a live preview on the Settings page.

Purpose: Foundation for Phase 120 PDF evidence generation which embeds P-Sign badges.
Output: PSignViewModel.cs, _PSign.cshtml partial, Settings page preview.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-p-sign-infrastructure/118-CONTEXT.md
@.planning/phases/118-p-sign-infrastructure/118-RESEARCH.md
@Models/SettingsViewModel.cs
@Controllers/AccountController.cs
@Views/Account/Settings.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PSignViewModel and _PSign.cshtml partial view</name>
  <files>Models/PSignViewModel.cs, Views/Shared/_PSign.cshtml</files>
  <action>
1. Create `Models/PSignViewModel.cs`:
   - Properties: `string LogoUrl` (default "/images/psign-pertamina.png"), `string? Position`, `string? Unit`, `string FullName` (default empty string)
   - Simple POCO class in `HcPortal.Models` namespace (follow existing convention)

2. Create `Views/Shared/_PSign.cshtml`:
   - `@model HcPortal.Models.PSignViewModel`
   - Self-contained with `<style>` block inside the partial (inline styles, no external CSS)
   - All CSS classes prefixed with `psign-` to avoid Bootstrap conflicts
   - Layout per user decisions:
     - Vertical: Logo on top, then Position, then Unit, then FullName
     - Border box with rounded corners, ~180px width
     - Grey border, black text
     - FullName bold, Position+Unit regular weight
     - Logo height ~40px
   - Fallback: use `@if (!string.IsNullOrEmpty(Model.Position))` to hide rows with null/empty values
   - Center-align all content within the badge
   - No NIP, no disclaimer text, no date/timestamp
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>PSignViewModel.cs exists with 4 properties. _PSign.cshtml renders a bordered badge with logo, conditional Position/Unit rows, and bold FullName.</done>
</task>

<task type="auto">
  <name>Task 2: Add P-Sign preview to Settings page</name>
  <files>Models/SettingsViewModel.cs, Controllers/AccountController.cs, Views/Account/Settings.cshtml</files>
  <action>
1. Edit `Models/SettingsViewModel.cs`:
   - Add property: `public PSignViewModel? PSign { get; set; }`

2. Edit `Controllers/AccountController.cs` in the Settings GET action:
   - After loading the current user, create a `PSignViewModel` populated from `user.Position`, `user.Unit`, `user.FullName`
   - Assign it to the SettingsViewModel's PSign property

3. Edit `Views/Account/Settings.cshtml`:
   - Add a "Preview P-Sign" section (use existing page styling conventions — look at how other sections are separated)
   - Place it after the profile info section, before the password section
   - Use `<p class="text-uppercase fw-bold small text-muted mb-3">Preview P-Sign</p>` as section header
   - Render: `@if (Model.PSign != null) { @await Html.PartialAsync("_PSign", Model.PSign) }`
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Settings page displays a P-Sign preview showing the logged-in user's badge with logo, position, unit, and name. Users with null Position/Unit see a badge with only the available fields.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with no errors
2. Navigate to Account/Settings — P-Sign preview visible with current user data
3. Badge shows Logo Pertamina image, Position (if set), Unit (if set), and bold FullName
4. Badge has rounded border, ~180px width, grey/black color scheme
</verification>

<success_criteria>
- PSignViewModel reusable from any controller/view via `@await Html.PartialAsync("_PSign", model)`
- Settings page shows live P-Sign preview for logged-in user
- Component is self-contained (inline styles) for future PDF embedding in Phase 120
</success_criteria>

<output>
After completion, create `.planning/phases/118-p-sign-infrastructure/118-01-SUMMARY.md`
</output>
