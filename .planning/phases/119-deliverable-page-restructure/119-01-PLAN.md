---
phase: 119-deliverable-page-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CDP/Deliverable.cshtml
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03

must_haves:
  truths:
    - "Deliverable detail page shows 4 distinct card sections: Detail Coachee & Kompetensi, Approval Chain, Evidence Coach, Riwayat Status"
    - "Detail Coachee and Approval Chain cards are side-by-side on desktop, stacked on mobile"
    - "Riwayat Status section displays chronological timeline from DeliverableStatusHistory"
    - "Evidence Coach section shows coaching session data and file download button"
    - "Alert banners (Rejected/Submitted/Approved) are removed; status shown as badge in Approval Chain header"
    - "Upload Evidence form is removed from the page"
    - "Breadcrumb simplified to 2 levels"
    - "Approval Chain stepper always visible (even when status is Pending)"
  artifacts:
    - path: "Views/CDP/Deliverable.cshtml"
      provides: "Restructured 4-card Deliverable detail page"
      contains: "card border-0 shadow-sm"
  key_links:
    - from: "Views/CDP/Deliverable.cshtml"
      to: "CDPController.Deliverable()"
      via: "Model binding"
      pattern: "DeliverableViewModel"
---

<objective>
Restructure the Deliverable detail page from a single-card layout into 4 separate Bootstrap cards with a side-by-side desktop layout for the top two cards.

Purpose: Clearly separate coaching data, approval status, and history into distinct visual sections for easier scanning.
Output: Rewritten Views/CDP/Deliverable.cshtml
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/119-deliverable-page-restructure/119-CONTEXT.md
@.planning/phases/119-deliverable-page-restructure/119-RESEARCH.md
@Views/CDP/Deliverable.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Deliverable.cshtml with 4-card sectioned layout</name>
  <files>Views/CDP/Deliverable.cshtml</files>
  <action>
Rewrite the entire Deliverable.cshtml view. Keep the @model directive, @using statements, and ViewData title. The structure must be:

1. **Breadcrumb** (simplified to 2 levels):
   - `Coaching Proton > Deliverable` or `IDP Plan > Deliverable` based on referer
   - Remove the 4-5 level breadcrumb (Track, Kompetensi, SubKompetensi)

2. **TempData alerts** (Success/Error only — keep as-is)

3. **IsAccessible check** (keep as-is — locked deliverable warning + back button)

4. **Inside the else block**, replace the single card with:

   **Badge class computation** — keep existing badgeClass logic (Pending=bg-secondary, Submitted=bg-primary, Approved=bg-success, Rejected=bg-danger)

   **Row 1: Side-by-side cards** (`<div class="row g-3 mb-3">`):
   - `col-md-7`: **Card 1 "Detail Coachee & Kompetensi"**
     - Header: `<h6>` with `bi-person-lines-fill` icon, title "Detail Coachee & Kompetensi"
     - Body: Coachee name, Track (with TahunKe), Kompetensi (`Model.Deliverable?.ProtonSubKompetensi?.ProtonKompetensi?.NamaKompetensi`), SubKompetensi (`Model.Deliverable?.ProtonSubKompetensi?.NamaSubKompetensi`), Deliverable name. Use `<small class="text-muted">Label</small>` + `<div class="fw-semibold">Value</div>` pattern, each in a `mb-3` div. Also keep the "Anda masuk sebagai" role badge and the "Hak akses" info text.
     - Always visible.

   - `col-md-5`: **Card 2 "Approval Chain"**
     - Header: `<h6>` with `bi-diagram-3` icon, title "Approval Chain", PLUS status badge (`@badgeClass`) in the header right corner using `d-flex justify-content-between align-items-center`
     - Body: The existing vertical stepper code (SrSpv > SH > HC steps with icons, badges, names, dates). **CRITICAL: Remove the status gate** — currently gated by `Status == "Submitted" || "Approved" || "Rejected"`. The stepper must ALWAYS render. When status is Pending, define apNames/srSpvStatus/shStatus/hcStatus with defaults and show all steps as Pending.
     - Rejection reason displayed inside the stepper (keep existing code at lines 269-275).
     - Approve/Reject buttons (lines 282-317) and HC Review form (lines 321-335) at bottom of this card (inside card-body, no `<hr>` needed — use `mt-3 pt-3 border-top` separator). Keep all form elements, hidden inputs, and AntiForgeryToken intact.
     - Always visible.

   **Card 3 "Evidence Coach"** (full-width, `mb-3`):
   - Header: `<h6>` with `bi-file-earmark-text` icon, title "Evidence Coach"
   - Body: First show evidence file display (lines 96-114: file name, download button, upload date). Then show coaching session data: for each session in coachingSessions65, display Coach name, Tanggal, Catatan Coach, Kesimpulan, Result badge. Use the existing table layout from lines 384-397.
   - **Conditional**: `@if (!string.IsNullOrEmpty(Model.Progress?.EvidencePath) || coachingSessions65.Any())` — hidden if no evidence AND no coaching sessions.
   - Declare `coachingSessions65` and `coachNames65` variables BEFORE this card (they are also used in Card 4 timeline).

   **Card 4 "Riwayat Status"** (full-width, `mb-3`):
   - Header: `<h6>` with `bi-clock-history` icon, title "Riwayat Status"
   - Body: The existing timeline code (lines 404-467) — timelineEvents list construction and rendering. Keep CreatedAt entry, DeliverableStatusHistory entries, and coaching session entries.
   - **Conditional**: `@if (timelineEvents.Count > 1)` (same as current)

   **Back button** (keep existing referer-based logic, lines 469-484)

5. **Elements to REMOVE entirely:**
   - Alert banners: Rejected notice (lines 117-143), Submitted notice (lines 146-152), Approved notice (lines 155-172)
   - Upload Evidence form (lines 337-358)
   - The old single wrapping card (`<div class="card border-0 shadow-sm">` at line 67 with its card-header showing deliverable name + badge)

All cards use consistent styling: `card border-0 shadow-sm mb-3` with `card-header bg-white border-bottom`.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
  - Deliverable.cshtml compiles without errors
  - Page has 4 Bootstrap cards: Detail Coachee & Kompetensi, Approval Chain, Evidence Coach, Riwayat Status
  - Top two cards are side-by-side on desktop (col-md-7 + col-md-5)
  - Alert banners and upload form are removed
  - Breadcrumb is 2 levels only
  - Approval Chain stepper renders for all statuses including Pending
  - Approve/Reject/HC Review buttons are inside the Approval Chain card
  </done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds (Razor compilation)
- Manual browser check: visit a Deliverable page in each status (Pending, Submitted, Approved, Rejected) and confirm 4-card layout renders correctly
</verification>

<success_criteria>
- Deliverable detail page displays 4 distinct card sections
- Desktop layout shows Detail + Approval Chain side-by-side
- Mobile layout stacks all cards vertically
- No alert banners or upload form present
- Breadcrumb shows only 2 levels
- All approval actions (approve, reject, HC review) functional within Approval Chain card
</success_criteria>

<output>
After completion, create `.planning/phases/119-deliverable-page-restructure/119-01-SUMMARY.md`
</output>
