---
phase: 120-pdf-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/Deliverable.cshtml
autonomous: false
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04
must_haves:
  truths:
    - "Clicking PDF Evidence Report button downloads a PDF file"
    - "PDF contains all coaching fields: Nama Coachee, Track, Kompetensi, Sub Kompetensi, Deliverable, Tanggal, Catatan Coach, Kesimpulan, Result"
    - "PDF displays Coach P-Sign badge at bottom-left"
    - "Button only appears when coaching session exists"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "DownloadEvidencePdf action"
      contains: "DownloadEvidencePdf"
    - path: "Views/CDP/Deliverable.cshtml"
      provides: "Download button in Card 3"
      contains: "DownloadEvidencePdf"
  key_links:
    - from: "Views/CDP/Deliverable.cshtml"
      to: "CDPController.DownloadEvidencePdf"
      via: "asp-action link with progressId"
      pattern: "asp-action=\"DownloadEvidencePdf\""
    - from: "CDPController.DownloadEvidencePdf"
      to: "QuestPDF Document.Create"
      via: "PDF generation and File() return"
      pattern: "Document\\.Create"
---

<objective>
Add a "PDF Evidence Report" download button to the Deliverable detail page that generates a portrait A4 PDF containing coaching session data and the Coach's P-Sign badge using QuestPDF.

Purpose: Coaches need a printable evidence form documenting coaching sessions for each deliverable.
Output: New DownloadEvidencePdf action in CDPController + download button in Deliverable.cshtml Card 3.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/120-pdf-evidence/120-CONTEXT.md
@.planning/phases/120-pdf-evidence/120-RESEARCH.md
@Controllers/CDPController.cs
@Views/CDP/Deliverable.cshtml
@Models/PSignViewModel.cs
@Views/Shared/_PSign.cshtml

<interfaces>
<!-- Existing patterns the executor needs -->

From Controllers/CDPController.cs (ExportCoachingProtonPdf pattern, line 2184-2241):
- Document.Create -> container.Page -> page.Content().Column() pattern
- MemoryStream -> pdf.GeneratePdf(pdfStream) -> File(pdfStream.ToArray(), "application/pdf", filename)
- Colors: QuestPDF.Helpers.Colors.Blue.Lighten4 for headers

From Controllers/CDPController.cs (Deliverable action, line 737-790):
- Access control: load progress with full Include chain, then check isCoachee/hasFullAccess/isSectionScoped/isCoach
- Progress loading: _context.ProtonDeliverableProgresses.Include(p => p.ProtonDeliverable).ThenInclude(d => d.ProtonSubKompetensi).ThenInclude(s => s.ProtonKompetensi).ThenInclude(k => k.ProtonTrack)

From Models/PSignViewModel.cs:
- LogoUrl = "/images/psign-pertamina.png", Position (nullable), Unit (nullable), FullName

From Models/CoachingSession.cs (fields needed):
- CoachId, Date, CatatanCoach, Kesimpulan, Result, ProtonDeliverableProgressId
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DownloadEvidencePdf action and add download button</name>
  <files>Controllers/CDPController.cs, Views/CDP/Deliverable.cshtml</files>
  <action>
**CDPController.cs** -- Add new `DownloadEvidencePdf(int progressId)` action near ExportCoachingProtonPdf (after line ~2241):

1. Load user, check auth (same pattern as Deliverable action lines 739-790):
   - `var user = await _userManager.GetUserAsync(User); if (user == null) return Challenge();`
   - Load progress with full Include chain (ProtonDeliverable -> ProtonSubKompetensi -> ProtonKompetensi -> ProtonTrack)
   - Copy exact access control block from Deliverable action (isCoachee/hasFullAccess/isSectionScoped/isCoach checks)

2. Load latest CoachingSession:
   - `var session = await _context.CoachingSessions.Where(cs => cs.ProtonDeliverableProgressId == progressId).OrderByDescending(cs => cs.CreatedAt).FirstOrDefaultAsync();`
   - If null, return NotFound()

3. Load data for PDF fields:
   - Coachee name: `await _context.Users.Where(u => u.Id == progress.CoacheeId).Select(u => u.FullName).FirstOrDefaultAsync() ?? "Coachee"`
   - Coach user: `await _context.Users.Where(u => u.Id == session.CoachId).Select(u => new { u.FullName, u.Position, u.Unit }).FirstOrDefaultAsync()`
   - Track: `progress.ProtonDeliverable?.ProtonSubKompetensi?.ProtonKompetensi?.ProtonTrack` -- format as `"{TrackType} {TahunKe}"`
   - Kompetensi: `...ProtonKompetensi?.NamaKompetensi`
   - SubKompetensi: `...ProtonSubKompetensi?.NamaSubKompetensi`
   - Deliverable: `...ProtonDeliverable?.NamaDeliverable`

4. Load logo bytes:
   - `var logoBytes = System.IO.File.ReadAllBytes(Path.Combine(_env.WebRootPath, "images", "psign-pertamina.png"));`
   - Need `IWebHostEnvironment _env` -- check if already injected in constructor; if not, add it

5. Build QuestPDF document -- Portrait A4, form-style:
   - accentColor = "#005B96" (Pertamina blue)
   - page.Size(QuestPDF.Helpers.PageSizes.A4) -- Portrait, no .Landscape()
   - page.Margin(2, QuestPDF.Infrastructure.Unit.Centimetre)
   - Header: centered logo (Width 80) + "EVIDENCE COACHING REPORT" title (FontSize 16, Bold, accentColor)
   - Form fields as label-value rows. Create a local helper void AddField(ColumnDescriptor col, string label, string value):
     - Row with RelativeItem(1) for label (background accentColor, white text, Bold, FontSize 9, Padding 6)
     - RelativeItem(2) for value (BorderBottom 1 #dee2e6, Padding 6, FontSize 10)
     - Empty values show "-"
   - Fields in order: Nama Coachee, Track, Kompetensi, Sub Kompetensi, Deliverable, Tanggal Coaching (format dd MMM yyyy), Catatan Coach, Kesimpulan, Result
   - P-Sign at bottom-left: PaddingTop(30), Row with first RelativeItem containing the badge, second RelativeItem empty
     - Badge: Width(170), Border(1.5f), BorderColor("#adb5bd"), Padding(8), Column with:
       - Centered logo (Width 50)
       - Centered Position text (FontSize 8) -- only if not empty
       - Centered Unit text (FontSize 8) -- only if not empty
       - Centered FullName (FontSize 9, Bold)
   - Footer: page.Footer().AlignCenter() with "Generated: {datetime} -- Page 1 of 1 -- PortalHC KPB" (FontSize 7)

6. Return PDF file:
   - MemoryStream, pdf.GeneratePdf(pdfStream)
   - Sanitize filename: replace non-alphanumeric with underscore using Regex
   - Filename: Evidence_{safeName}_{safeDeliverable}_{session.Date:yyyy-MM-dd}.pdf
   - return File(pdfStream.ToArray(), "application/pdf", filename)

**Views/CDP/Deliverable.cshtml** -- Add download button inside Card 3, after the coaching session foreach loop (after line 338, before the closing div of card-body at line 339):

```html
@if (coachingSessions65.Any())
{
    <div class="mt-3">
        <a asp-controller="CDP" asp-action="DownloadEvidencePdf"
           asp-route-progressId="@Model.Progress.Id"
           class="btn btn-outline-secondary">
            <i class="bi bi-file-pdf me-1"></i>PDF Evidence Report
        </a>
    </div>
}
```

Place this AFTER the foreach block and BEFORE the closing div of card-body.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - DownloadEvidencePdf action exists in CDPController, builds without errors
    - Deliverable.cshtml has PDF Evidence Report button inside Card 3 conditional on coachingSessions65.Any()
    - PDF generated as portrait A4 with all 9 fields, P-Sign badge, header with logo, footer
    - Filename follows Evidence_{name}_{deliverable}_{date}.pdf pattern
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PDF Evidence Report</name>
  <files>N/A</files>
  <action>Human verification of PDF output</action>
  <what-built>PDF Evidence Report download feature -- button in Deliverable Card 3 that generates a portrait A4 PDF with coaching session fields and Coach P-Sign badge</what-built>
  <how-to-verify>
    1. Navigate to a Deliverable detail page that has a coaching session (evidence submitted)
    2. Verify the "PDF Evidence Report" button appears inside the Evidence Coach card
    3. Click the button -- a PDF should download
    4. Open the PDF and verify:
       a. Pertamina logo centered at top
       b. "EVIDENCE COACHING REPORT" title
       c. All fields present: Nama Coachee, Track, Kompetensi, Sub Kompetensi, Deliverable, Tanggal Coaching, Catatan Coach, Kesimpulan, Result
       d. Coach P-Sign badge at bottom-left with logo, position, unit, name
       e. Footer with generation timestamp and "PortalHC KPB"
    5. Verify button does NOT appear on a Deliverable page without coaching session
  </how-to-verify>
  <verify>Human confirms PDF output is correct</verify>
  <done>PDF downloads correctly with all required content</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- PDF downloads with correct filename pattern
- PDF contains all 9 required fields
- P-Sign badge renders in PDF
- Button only visible when coaching session exists
</verification>

<success_criteria>
- Coach can download a professional PDF evidence form from the Deliverable detail page
- PDF contains complete coaching session data with P-Sign badge
- Access control matches existing Deliverable page permissions
</success_criteria>

<output>
After completion, create `.planning/phases/120-pdf-evidence/120-01-SUMMARY.md`
</output>
