---
phase: 122-remove-assessment-analytics-tab-from-cdp-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Models/CDPDashboardViewModel.cs
  - Views/CDP/Dashboard.cshtml
  - Views/CDP/Index.cshtml
  - Views/Home/Guide.cshtml
  - Views/Home/GuideDetail.cshtml
  - Views/Shared/_Layout.cshtml
  - Views/CDP/Shared/_AssessmentAnalyticsPartial.cshtml
  - Views/CDP/Shared/_AssessmentAnalyticsContentPartial.cshtml
autonomous: true
requirements:
  - REM-01
  - REM-02
  - REM-03
  - REM-04
  - REM-05

must_haves:
  truths:
    - "CDP Dashboard page shows Coaching Proton content directly with no tab UI"
    - "Page title and navbar say 'Coaching Proton Dashboard'"
    - "No build errors after all analytics code removed"
    - "CDP hub card references Coaching Proton Dashboard, no analytics mention"
    - "Old analytics URLs land on Dashboard without errors"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Dashboard action without analytics params or BuildAnalyticsSubModelAsync"
      contains: "Dashboard()"
    - path: "Models/CDPDashboardViewModel.cs"
      provides: "ViewModel without AssessmentAnalyticsSubModel"
    - path: "Views/CDP/Dashboard.cshtml"
      provides: "Single-section Coaching Proton page without tabs"
  key_links:
    - from: "Views/CDP/Dashboard.cshtml"
      to: "Views/CDP/Shared/_CoachingProtonPartial.cshtml"
      via: "direct partial render (no tab wrapper)"
      pattern: "partial.*_CoachingProtonPartial"
---

<objective>
Remove the Assessment Analytics tab entirely from CDP Dashboard, simplifying it to a single-section Coaching Proton Dashboard page.

Purpose: Analytics functionality is redundant (exists in CMP/Records Team View). Removing it simplifies the codebase and reduces user confusion.
Output: Clean single-section Dashboard with no analytics code remaining.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-remove-assessment-analytics-tab-from-cdp-dashboard/122-CONTEXT.md
@.planning/phases/122-remove-assessment-analytics-tab-from-cdp-dashboard/122-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove analytics backend code and view model classes</name>
  <files>Controllers/CDPController.cs, Models/CDPDashboardViewModel.cs, Views/CDP/Shared/_AssessmentAnalyticsPartial.cshtml, Views/CDP/Shared/_AssessmentAnalyticsContentPartial.cshtml</files>
  <action>
1. Delete files: `Views/CDP/Shared/_AssessmentAnalyticsPartial.cshtml` and `Views/CDP/Shared/_AssessmentAnalyticsContentPartial.cshtml`

2. In `Models/CDPDashboardViewModel.cs`:
   - Remove the `AssessmentAnalyticsSubModel` class and its property from `CDPDashboardViewModel`
   - Remove supporting classes used ONLY by analytics: `AssessmentReportItem`, `ReportFilters`, `CategoryStatistic` (verify no other consumers first via grep)

3. In `Controllers/CDPController.cs`:
   - Remove `FilterAssessmentAnalytics` action method
   - Remove `ExportAnalyticsResults` action method
   - Remove `BuildAnalyticsSubModelAsync` private method
   - Simplify `Dashboard()` action: remove analytics-related parameters (section, unit, category params used only for analytics), remove the `isHCAccess` block that builds analytics sub-model. Keep only user/role detection, CoacheeData or ProtonProgressData building.
   - Remove any using statements that become unused after removal

4. Run `dotnet build` to verify no compile errors.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds. No FilterAssessmentAnalytics, ExportAnalyticsResults, BuildAnalyticsSubModelAsync, or AssessmentAnalyticsSubModel in codebase. Two partial view files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify Dashboard view to single-section and update all references</name>
  <files>Views/CDP/Dashboard.cshtml, Views/CDP/Index.cshtml, Views/Shared/_Layout.cshtml, Views/Home/Guide.cshtml, Views/Home/GuideDetail.cshtml</files>
  <action>
1. In `Views/CDP/Dashboard.cshtml`:
   - Change page title to "Coaching Proton Dashboard"
   - Remove the entire Bootstrap tab nav structure (`ul.nav-tabs`, `div.tab-content`, tab panes)
   - Render Coaching Proton partial directly: if CoacheeData != null render _CoacheeDashboardPartial, else if ProtonProgressData != null render _CoachingProtonPartial
   - Remove ALL analytics-related JavaScript (AJAX handlers, cascade filter logic for analytics tab, export button handlers) -- approximately 110 lines of script code
   - Keep Coaching Proton filter JS intact
   - Remove any tab-related CSS classes that are no longer needed

2. In `Views/CDP/Index.cshtml` (CDP hub page):
   - Update Dashboard card title to "Coaching Proton Dashboard"
   - Update card description to remove any Analytics references

3. In `Views/Shared/_Layout.cshtml` (or navbar partial -- check which file has the nav link):
   - Change navbar label from "Dashboard" (or current text) to "Coaching Proton Dashboard"

4. In `Views/Home/Guide.cshtml` (line ~346):
   - Update text referencing Dashboard to say "Coaching Proton Dashboard" and remove Analytics mentions

5. In `Views/Home/GuideDetail.cshtml` (line ~285+):
   - Update guide steps referencing Dashboard/Analytics tab

6. Run `dotnet build` to verify.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard.cshtml renders single-section layout with no tabs. Page title is "Coaching Proton Dashboard". Hub card, navbar, and guide pages all reference "Coaching Proton Dashboard" with no analytics mentions. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with no errors
2. Grep for "Analytics" in Views/CDP/ returns no results
3. Grep for "FilterAssessmentAnalytics" in entire codebase returns no results
4. Grep for "ExportAnalyticsResults" in entire codebase returns no results
5. Grep for "AssessmentAnalyticsSubModel" in entire codebase returns no results
6. Grep for "nav-tabs" in Dashboard.cshtml returns no results
</verification>

<success_criteria>
- CDP Dashboard is a single-section "Coaching Proton Dashboard" page with no tab UI
- All analytics backend code (3 methods), view model classes, and 2 partial views are deleted
- Navbar, hub card, and guide pages reference "Coaching Proton Dashboard"
- Build compiles cleanly
- No deferred features implemented (no export additions)
</success_criteria>

<output>
After completion, create `.planning/phases/122-remove-assessment-analytics-tab-from-cdp-dashboard/122-01-SUMMARY.md`
</output>
