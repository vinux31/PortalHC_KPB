---
phase: 125-mapping-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/CoachCoacheeMapping.cshtml
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "CoachCoacheeMapping table shows Bagian Penugasan and Unit Penugasan columns per coachee row"
    - "Null AssignmentSection/Unit displays dash (—) in table"
    - "Assign modal has required Bagian Penugasan and Unit Penugasan dropdowns that cascade (unit filtered by section)"
    - "Edit modal has Bagian Penugasan and Unit Penugasan dropdowns pre-filled with current values"
    - "Excel export includes Bagian Penugasan and Unit Penugasan columns with correct data"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "Assignment fields in grouped data, assign modal accepts fields, export includes columns"
    - path: "Views/Admin/CoachCoacheeMapping.cshtml"
      provides: "Table columns, modal dropdowns, cascade JS"
  key_links:
    - from: "Views/Admin/CoachCoacheeMapping.cshtml"
      to: "Controllers/AdminController.cs"
      via: "submitAssign sends AssignmentSection/AssignmentUnit in payload"
      pattern: "AssignmentSection.*AssignmentUnit"
    - from: "Views/Admin/CoachCoacheeMapping.cshtml"
      to: "Models/OrganizationStructure.cs"
      via: "ViewBag.SectionUnits for cascade dropdown data"
      pattern: "SectionUnits"
---

<objective>
Display assignment unit/section columns on the CoachCoacheeMapping table, add required Bagian Penugasan / Unit Penugasan dropdown fields to assign and edit modals with cascade behavior, and include these columns in the Excel export.

Purpose: Admin/HC can see and manage cross-section assignment data when working with coach-coachee mappings.
Output: Updated table, modals, and export with assignment fields.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-mapping-ui/125-CONTEXT.md
@.planning/phases/123-data-model-migration/123-01-SUMMARY.md

<interfaces>
From Models/OrganizationStructure.cs:
```csharp
public static readonly Dictionary<string, List<string>> SectionUnits; // Section -> Units mapping
public static List<string> GetAllSections();
public static List<string> GetUnitsForSection(string section);
```

From Models/CoachCoacheeMapping.cs:
```csharp
public string? AssignmentSection { get; set; }  // line 39
public string? AssignmentUnit { get; set; }      // line 44
```

From Controllers/AdminController.cs (CoachAssignRequest, ~line 5217):
```csharp
public string? AssignmentSection { get; set; }
public string? AssignmentUnit { get; set; }
```
Assign endpoint already validates and persists these fields (Phase 123).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assignment columns to table and cascade dropdowns to assign/edit modals</name>
  <files>Controllers/AdminController.cs, Views/Admin/CoachCoacheeMapping.cshtml</files>
  <action>
**Controller changes (AdminController.cs):**

1. In `CoachCoacheeMapping` GET action (~line 2797):
   - Add `AssignmentSection` and `AssignmentUnit` to the Coachees anonymous type projection (~line 2858-2870). Add:
     ```
     AssignmentSection = r.Mapping.AssignmentSection ?? "",
     AssignmentUnit = r.Mapping.AssignmentUnit ?? ""
     ```
   - Pass `OrganizationStructure.SectionUnits` to the view: `ViewBag.SectionUnits = OrganizationStructure.SectionUnits;`

2. In `CoachCoacheeMappingEdit` POST action (~line 2998):
   - Add `AssignmentSection` and `AssignmentUnit` to `CoachEditRequest` DTO (near line 5217 area — find the CoachEditRequest class).
   - In the edit handler, update mapping's AssignmentSection and AssignmentUnit from the request (trim, allow setting to new values).

**View changes (CoachCoacheeMapping.cshtml):**

1. **Table header** (~line 105-114): Replace "Seksi" with "Bagian Penugasan" and add "Unit Penugasan" after it. Update colspan on coach header row from 8 to 9. New column order: Nama, NIP, Bagian Penugasan, Unit Penugasan, Jabatan, Proton Track, Status, Mulai, Aksi.

2. **Table body** (~line 138-141): Replace `@coachee.CoacheeSection` cell with:
   ```
   <td>@(string.IsNullOrEmpty((string)coachee.AssignmentSection) ? "—" : coachee.AssignmentSection)</td>
   <td>@(string.IsNullOrEmpty((string)coachee.AssignmentUnit) ? "—" : coachee.AssignmentUnit)</td>
   ```

3. **Assign modal** (~line 275, after coachee checklist div, before Proton Track):
   Add two new form groups:
   ```html
   <div class="mb-3">
     <label class="form-label fw-semibold">Bagian Penugasan <span class="text-danger">*</span></label>
     <select id="assignAssignmentSection" class="form-select" onchange="filterAssignmentUnits('assign')">
       <option value="">— Pilih Bagian —</option>
       @foreach sections from ViewBag.SectionUnits keys
     </select>
   </div>
   <div class="mb-3">
     <label class="form-label fw-semibold">Unit Penugasan <span class="text-danger">*</span></label>
     <select id="assignAssignmentUnit" class="form-select">
       <option value="">— Pilih Unit —</option>
     </select>
   </div>
   ```

4. **Edit modal** (~line 327, after Proton Track, before Tanggal Mulai — actually place BEFORE Coach select for better flow, or after Coach select):
   Add same two dropdowns with ids `editAssignmentSection` and `editAssignmentUnit`.

5. **JavaScript — cascade logic:**
   - At top of Scripts section, emit SectionUnits as JSON: `var sectionUnitsMap = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.SectionUnits));`
   - Add `filterAssignmentUnits(prefix)` function: reads `{prefix}AssignmentSection` value, populates `{prefix}AssignmentUnit` options from sectionUnitsMap, clears previous selection.

6. **JavaScript — auto-fill on coachee selection (assign modal):**
   - Need coachee section/unit data. Add `data-section` and `data-unit` attributes to each coachee checkbox item (already has `data-section`; add `data-unit="@coachee.Unit"`). Note: need Unit in eligible coachees data — but the controller query already selects Section. Need to also include Unit field. Actually, looking at the allUsers query (line 2804), it does NOT select Unit. Need to add Unit to the allUsers projection and to eligibleCoachees/allUsers ViewBag items.
   - Update allUsers select (~line 2804): add `u.Unit` to the anonymous type.
   - When a single coachee is checked in assign modal, auto-fill Bagian Penugasan with that coachee's section (via OrganizationStructure.GetSectionForUnit or directly from data attribute) and Unit Penugasan with their unit. When multiple coachees with different sections/units are checked, clear both dropdowns.
   - Add an event listener on `.coachee-checkbox` change to call a function `updateAssignmentDefaults()` that checks all selected coachees, and if they share the same section+unit, fills the dropdowns; otherwise clears them.

7. **JavaScript — submitAssign update:**
   - Add validation: require both `assignAssignmentSection` and `assignAssignmentUnit` to have values.
   - Add to payload: `AssignmentSection: document.getElementById('assignAssignmentSection').value, AssignmentUnit: document.getElementById('assignAssignmentUnit').value`

8. **JavaScript — openEditModal update:**
   - Add parameters for assignmentSection and assignmentUnit to the function signature.
   - Set dropdown values and trigger cascade.
   - Update the onclick in the table row to pass `'@coachee.AssignmentSection'` and `'@coachee.AssignmentUnit'`.

9. **JavaScript — submitEdit update:**
   - Add AssignmentSection and AssignmentUnit to the edit payload.
   - Add validation requiring both fields.

**Important details:**
- "Bagian" = Section in OrganizationStructure (RFCC, DHT/HMU, NGP, GAST)
- "Unit" = specific unit within a section
- Cascade: selecting Bagian populates Unit dropdown from `OrganizationStructure.SectionUnits[selectedSection]`
- Both fields required per CONTEXT.md decisions
- The coachee's home Unit field may need to be added to the controller's user projection (line 2804 — add `u.Unit`)
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Table shows 9 columns: Nama, NIP, Bagian Penugasan, Unit Penugasan, Jabatan, Proton Track, Status, Mulai, Aksi
    - Null assignment values show dash (—)
    - Assign modal has cascading Bagian/Unit dropdowns that auto-fill from coachee selection
    - Edit modal has Bagian/Unit dropdowns pre-filled with current mapping values
    - Both modals send AssignmentSection/AssignmentUnit in their payloads
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add assignment columns to Excel export</name>
  <files>Controllers/AdminController.cs</files>
  <action>
In `CoachCoacheeMappingExport` action (~line 4042):

1. **Update headers array** (~line 4074-4077): Add "Bagian Penugasan" and "Unit Penugasan" columns. Include home section for HR reference (Claude's discretion from CONTEXT.md). New headers:
   ```
   "Coach Name", "Coach Section", "Coachee Name", "Coachee NIP",
   "Coachee Section", "Coachee Position", "Bagian Penugasan", "Unit Penugasan",
   "Current Track", "Status", "Start Date", "End Date"
   ```

2. **Update data rows** (~line 4089-4106): After existing coachee position cell (column 6), add:
   ```
   ws.Cell(row, 7).Value = string.IsNullOrEmpty(m.AssignmentSection) ? "—" : m.AssignmentSection;
   ws.Cell(row, 8).Value = string.IsNullOrEmpty(m.AssignmentUnit) ? "—" : m.AssignmentUnit;
   ```
   Then shift remaining columns (Current Track, Status, Start Date, End Date) to columns 9-12.

3. Keep the flat layout (each row has its own Coach column — no merged cells per CONTEXT.md decision).
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Excel export has 12 columns including Bagian Penugasan and Unit Penugasan
    - Null AssignmentSection/Unit shows dash (—) in export
    - Flat layout with Coach column per row (no merged cells)
  </done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with no errors
2. CoachCoacheeMapping page loads with 9-column table
3. Assign modal shows cascading Bagian/Unit dropdowns, auto-fills from coachee selection
4. Edit modal pre-fills assignment fields from existing mapping data
5. Export Excel contains Bagian Penugasan and Unit Penugasan columns
</verification>

<success_criteria>
- Table displays assignment columns with dash for null values
- Assign and edit modals have required, cascading assignment dropdowns
- Excel export includes assignment columns
- All three requirements (UI-01, UI-02, UI-03) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/125-mapping-ui/125-01-SUMMARY.md`
</output>
