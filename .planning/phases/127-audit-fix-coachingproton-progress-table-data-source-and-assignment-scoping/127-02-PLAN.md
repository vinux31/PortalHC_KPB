---
phase: 127-audit-fix-coachingproton-progress-table-data-source-and-assignment-scoping
plan: 02
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - Controllers/CDPController.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Dashboard shows only coachees with active ProtonTrackAssignment"
    - "Coach sees coachees from active CoachCoacheeMapping that have assignments"
    - "SectionHead/SrSpv sees coachees with assignment in their section"
    - "HC/Admin sees all coachees with active assignments"
    - "CoachingProton page queries progress via ProtonTrackAssignmentId join"
    - "Stats and charts are computed from assignment-linked progress only"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Rewritten BuildProtonProgressSubModelAsync and CoachingProton scoping"
      contains: "ProtonTrackAssignment"
  key_links:
    - from: "CDPController.BuildProtonProgressSubModelAsync"
      to: "ProtonTrackAssignment"
      via: "assignment-based coachee scoping"
      pattern: "ProtonTrackAssignments.*IsActive"
    - from: "CDPController.CoachingProton"
      to: "ProtonDeliverableProgress"
      via: "join through ProtonTrackAssignmentId"
      pattern: "ProtonTrackAssignmentId"
---

<objective>
Rewrite Dashboard (BuildProtonProgressSubModelAsync) and CoachingProton page scoping to be assignment-based instead of user-section-based. Absorbs Phase 126 scope.

Purpose: Currently Dashboard and CoachingProton find coachees by RoleLevel==6 + section match. This must change to: find coachees via active ProtonTrackAssignment, scoped by role.
Output: Rewritten BuildProtonProgressSubModelAsync, updated CoachingProton action, updated FilterCoachingProton, ExportCoachingProton, and HistoriProton scoping.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/127-audit-fix-coachingproton-progress-table-data-source-and-assignment-scoping/127-CONTEXT.md
@.planning/phases/127-audit-fix-coachingproton-progress-table-data-source-and-assignment-scoping/127-01-SUMMARY.md

@Controllers/CDPController.cs (BuildProtonProgressSubModelAsync at line 336, CoachingProton at line 1130, FilterCoachingProton at line 263, HistoriProton at line 2312, ExportCoachingProton — search for it)

<interfaces>
From Models/ProtonModels.cs (after Plan 01):
```csharp
public class ProtonDeliverableProgress
{
    // ... existing fields ...
    public int ProtonTrackAssignmentId { get; set; }
    public ProtonTrackAssignment? ProtonTrackAssignment { get; set; }
}
```

From Models/ProtonViewModels.cs:
```csharp
public class CoacheeProgressRow
{
    public string CoacheeId, CoacheeName, TrackType, TahunKe;
    public int TotalDeliverables, Approved, Submitted, Rejected, Active;
    public bool HasFinalAssessment;
    public int? CompetencyLevelGranted;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite BuildProtonProgressSubModelAsync to assignment-based scoping</name>
  <files>Controllers/CDPController.cs</files>
  <action>
Rewrite `BuildProtonProgressSubModelAsync` (line 336) to use assignment-based scoping per user decisions:

1. Replace the coachee-finding logic (lines 342-377) with assignment-based approach:
   - HC/Admin: Get all active ProtonTrackAssignments, extract coacheeIds
   - SectionHead/SrSpv (HasSectionAccess): Get active assignments where assignment's coachee has matching section (via join to Users), OR use AssignmentSection from CoachCoacheeMapping. Simplest: query ProtonTrackAssignments joined with Users where user.Section == current user's section.
   - Coach: Get coacheeIds from active CoachCoacheeMappings, then filter to only those with active ProtonTrackAssignment

2. Change progress query (line 394) to join via ProtonTrackAssignmentId:
   - Instead of `Where(p => filteredCoacheeIds.Contains(p.CoacheeId))`, use `Where(p => activeAssignmentIds.Contains(p.ProtonTrackAssignmentId))`
   - This ensures only assignment-linked progress is counted

3. The category/track filters (lines 404-407) should filter assignments by track properties — this already works correctly since assignments include ProtonTrack.

4. Build coacheeRows from assignments rather than raw user list — each row maps to an assignment, not just a user. The TotalDeliverables count should come from progress records linked to that assignment.

5. Stats (lines 456-489) should be computed from assignment-linked progress only — this follows naturally from the filtered allProgresses.

6. Keep filter UI logic (sections, units, categories, tracks) intact — just change the data source from "all RoleLevel==6 users" to "users with active assignments".
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard shows only coachees with active ProtonTrackAssignment, scoped by role. Stats computed from assignment-linked progress.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CoachingProton, HistoriProton, and Export scoping</name>
  <files>Controllers/CDPController.cs</files>
  <action>
1. **CoachingProton action** (line 1130): Rewrite STEP 1 (lines 1144-1173) to assignment-based scoping:
   - HC/Admin (level <=3): scopedCoacheeIds = coachees with any active ProtonTrackAssignment
   - SectionHead/SrSpv (level 4): scopedCoacheeIds = coachees with active assignment where coachee's section matches user's section
   - Coach (level 5): scopedCoacheeIds = from active CoachCoacheeMapping, filtered to those with active ProtonTrackAssignment
   - Coachee (level 6): own ID only (unchanged), but only if they have an active assignment

2. **Progress query** (line 1271): Change from `Where(p => activeAssignmentCoacheeIds.Contains(p.CoacheeId))` to use ProtonTrackAssignmentId-based join. Since we now have the FK, query like:
   ```csharp
   var activeAssignmentIds = await _context.ProtonTrackAssignments
       .Where(a => a.IsActive && dataCoacheeIds.Contains(a.CoacheeId))
       .Select(a => a.Id)
       .ToListAsync();
   var query = _context.ProtonDeliverableProgresses
       .Include(p => p.ProtonDeliverable)...
       .Where(p => activeAssignmentIds.Contains(p.ProtonTrackAssignmentId));
   ```

3. Per user decision: "belt and suspenders" — add a .Where clause that also checks deliverable's track matches assignment's track via the navigation chain. This is a safety filter.

4. **HistoriProton** (line 2312): Update the coachee-scoping query at line ~2318 ("Role-scoped coachee IDs same pattern as CoachingProton") to use same assignment-based scoping pattern.

5. **ExportCoachingProton**: Find this action and apply same scoping change — export should respect assignment-based visibility.

6. **FilterCoachingProton** (line 263): This calls BuildProtonProgressSubModelAsync which is already fixed in Task 1. No additional changes needed unless it has its own scoping logic.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>CoachingProton, HistoriProton, and Export all scope data by active ProtonTrackAssignment. Belt-and-suspenders track validation is in place.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- BuildProtonProgressSubModelAsync uses ProtonTrackAssignment for scoping
- CoachingProton STEP 1 queries assignments not raw users
- Progress queries use ProtonTrackAssignmentId
- HistoriProton and Export have matching scoping changes
</verification>

<success_criteria>
- All CDP pages scope coachee visibility through active ProtonTrackAssignment
- Coach role sees only mapped coachees with assignments
- SectionHead sees coachees with assignments in their section
- HC/Admin sees all coachees with any active assignment
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/127-audit-fix-coachingproton-progress-table-data-source-and-assignment-scoping/127-02-SUMMARY.md`
</output>
