---
phase: "130-notification-infrastructure"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["Services/INotificationService.cs", "Services/NotificationService.cs", "ViewComponents/NotificationBellViewComponent.cs", "Views/Shared/Components/NotificationBell/Default.cshtml", "Controllers/NotificationController.cs", "Views/Shared/_Layout.cshtml"]
autonomous: false
requirements: ["INFRA-01", "INFRA-02", "INFRA-03", "INFRA-04", "INFRA-05"]
must_haves:
  truths:
    - "Bell icon with unread count badge renders in navbar for authenticated users"
    - "GET /Notification/List returns JSON array of notifications for current user"
    - "POST /Notification/MarkAsRead/{id} marks notification as read and returns updated unread count"
    - "POST /Notification/MarkAllAsRead marks all as read and returns count=0"
    - "POST /Notification/Dismiss/{id} deletes notification and returns updated unread count"
    - "NotificationService.SendAsync can be called from any controller to create a notification"
  artifacts:
    - path: "Controllers/NotificationController.cs"
      provides: "API endpoints for notification CRUD"
    - path: "ViewComponents/NotificationBellViewComponent.cs"
      provides: "Server-side unread count for navbar badge"
    - path: "Views/Shared/Components/NotificationBell/Default.cshtml"
      provides: "Bell icon HTML with badge and dropdown"
  key_links:
    - from: "Views/Shared/_Layout.cshtml"
      to: "ViewComponents/NotificationBellViewComponent.cs"
      via: "Component.InvokeAsync"
    - from: "Views/Shared/Components/NotificationBell/Default.cshtml"
      to: "Controllers/NotificationController.cs"
      via: "AJAX fetch calls"
---

<objective>
Build notification bell icon with dropdown UI in navbar, API endpoints for list/mark-read/dismiss, and wire everything with AJAX.

Purpose: Complete notification infrastructure so Phase 131/132 triggers can create notifications that users see and interact with.
Output: Working bell icon + dropdown + API endpoints + enhanced NotificationService
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-notification-infrastructure/130-CONTEXT.md

<interfaces>
From Services/INotificationService.cs:
```csharp
public interface INotificationService
{
    Task<bool> SendAsync(string userId, string type, string title, string message, string? actionUrl = null);
    Task<List<UserNotification>> GetAsync(string userId, int count = 50);
    Task<bool> MarkAsReadAsync(int notificationId, string userId);
    Task<int> MarkAllAsReadAsync(string userId);
    Task<int> GetUnreadCountAsync(string userId);
    Task<bool> SendByTemplateAsync(string userId, string type, Dictionary<string, object>? context = null);
    // NOTE: DeleteAsync does NOT exist yet — Task 1 adds it
}
```

From Models/UserNotification.cs:
```csharp
public class UserNotification
{
    public int Id { get; set; }
    public string UserId { get; set; } = "";
    public string Type { get; set; } = "";
    public string Title { get; set; } = "";
    public string Message { get; set; } = "";
    public string? ActionUrl { get; set; }
    public bool IsRead { get; set; } = false;
    public DateTime? ReadAt { get; set; }
    public string DeliveryStatus { get; set; } = "Delivered";
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}
```

_Layout.cshtml navbar structure (line ~81):
- Bell icon should go BEFORE the user avatar div (line 81: `<div class="d-flex align-items-center border-start ps-lg-4 ms-lg-2 mt-3 mt-lg-0">`)
- Insert bell ViewComponent inside the `@if (SignInManager.IsSignedIn(User) && currentUser != null)` block, before the user info div
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DeleteAsync to service and create NotificationController with API endpoints</name>
  <files>Services/INotificationService.cs, Services/NotificationService.cs, Controllers/NotificationController.cs</files>
  <action>
1. Add `Task<bool> DeleteAsync(int notificationId, string userId)` to INotificationService interface.

2. Implement DeleteAsync in NotificationService — find notification by id, verify userId matches, call _context.UserNotifications.Remove(), SaveChangesAsync. Same try-catch pattern as other methods.

3. Create Controllers/NotificationController.cs with [Authorize] and [IgnoreAntiforgeryToken] class-level attributes. Inject INotificationService. Use User.FindFirstValue(ClaimTypes.NameIdentifier) for userId (same pattern as other controllers). Endpoints:

   - `[HttpGet] List()` — calls _notificationService.GetAsync(userId, 20), returns Json with array of {id, type, title, message, actionUrl, isRead, createdAt (formatted as relative time string like "5 menit lalu", "1 jam lalu", "Kemarin", or date)}. Limit 20 for dropdown.

   - `[HttpPost] MarkAsRead(int id)` — calls MarkAsReadAsync(id, userId), then GetUnreadCountAsync(userId), returns Json {success, unreadCount}.

   - `[HttpPost] MarkAllAsRead()` — calls MarkAllAsReadAsync(userId), returns Json {success, unreadCount: 0}.

   - `[HttpPost] Dismiss(int id)` — calls DeleteAsync(id, userId), then GetUnreadCountAsync(userId), returns Json {success, unreadCount}.

   - `[HttpGet] UnreadCount()` — calls GetUnreadCountAsync(userId), returns Json {count}. (For badge refresh after AJAX actions.)

For relative time formatting: create a private static method `FormatRelativeTime(DateTime createdAt)` that returns Indonesian relative time strings. Use DateTime.UtcNow comparison. Thresholds: less than 1min="Baru saja", less than 60min="{n} menit lalu", less than 24h="{n} jam lalu", less than 2d="Kemarin", less than 7d="{n} hari lalu", else=createdAt.ToString("dd MMM yyyy").
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; dotnet build 2>&amp;1 | tail -5</automated>
  </verify>
  <done>NotificationController compiles with all 5 endpoints. DeleteAsync added to interface and implementation. Relative time formatting in Indonesian.</done>
</task>

<task type="auto">
  <name>Task 2: Create NotificationBell ViewComponent and wire bell icon into _Layout.cshtml with dropdown UI and AJAX</name>
  <files>ViewComponents/NotificationBellViewComponent.cs, Views/Shared/Components/NotificationBell/Default.cshtml, Views/Shared/_Layout.cshtml</files>
  <action>
1. Create ViewComponents/NotificationBellViewComponent.cs:
   - Inject INotificationService
   - InvokeAsync gets userId from UserClaimsPrincipal, calls GetUnreadCountAsync, returns View with unread count as model (int)

2. Create Views/Shared/Components/NotificationBell/Default.cshtml:
   - Model: int (unread count)
   - Bell icon using Bootstrap Icons (bi-bell / bi-bell-fill)
   - Badge: red circle with count, positioned top-right of bell. Use `position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger`. Hidden when Model == 0.
   - Dropdown container (not Bootstrap dropdown — custom div toggled by JS click):
     - Header row: "Notifikasi" title + "Tandai semua dibaca" link (calls markAllAsRead)
     - Notification list div (id="notificationList") — populated by AJAX on bell click
     - Loading spinner shown while fetching
     - Empty state: "Tidak ada notifikasi" text
   - Each notification item:
     - Unread: default background. Read: bg-light (gray).
     - Title (bold), message (small text), relative time (small text-muted)
     - Clicking item: if actionUrl, navigate to it; always call markAsRead first
     - X dismiss button on hover (position-absolute end-0) — calls dismiss endpoint
   - Inline script at bottom with AJAX functions:
     - `loadNotifications()`: fetch GET /Notification/List, render items into #notificationList
     - `markAsRead(id, actionUrl)`: fetch POST /Notification/MarkAsRead/{id}, update badge count, then navigate if actionUrl
     - `markAllAsRead()`: fetch POST /Notification/MarkAllAsRead, update badge, re-render list
     - `dismissNotification(id, event)`: event.stopPropagation(), fetch POST /Notification/Dismiss/{id}, remove item from DOM, update badge
     - `updateBadge(count)`: update badge text and visibility (hide when 0)
     - Toggle dropdown open/close on bell click, close on click outside
   - Dropdown width: 360px, max-height: 400px with overflow-y auto
   - Dropdown position: absolute, right-aligned, z-index 1050 (above navbar)

3. In _Layout.cshtml, inside the `@if (SignInManager.IsSignedIn(User) &amp;&amp; currentUser != null)` block at line ~83, BEFORE the text-end div (line 84), insert:
   ```html
   &lt;div class="position-relative me-3"&gt;
       @await Component.InvokeAsync("NotificationBell")
   &lt;/div&gt;
   ```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; dotnet build 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Bell icon visible in navbar with badge count. Clicking bell opens dropdown with AJAX-loaded notifications. Mark read (click), mark all read, and dismiss (X button) all work via AJAX and update badge count without page reload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify notification bell UI and interactions</name>
  <what-built>Complete notification bell infrastructure: bell icon in navbar, dropdown with AJAX, mark read/dismiss functionality</what-built>
  <how-to-verify>
    1. Run app, login as any user
    2. Verify bell icon appears in navbar (to the left of user avatar)
    3. Badge should show 0 or be hidden (no notifications yet)
    4. To test with data: use browser dev tools console — fetch('/Notification/List').then(r=>r.json()).then(console.log) should return empty array
    5. Manually insert a UserNotification via SQL or add a test seed, then refresh page
    6. Badge should show count, clicking bell should open dropdown
    7. Click a notification — should mark as read (background changes to gray)
    8. Click X on a notification — should dismiss (disappear from list, badge updates)
    9. Click "Tandai semua dibaca" — all should become read, badge goes to 0
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- NotificationController has 5 endpoints: List, MarkAsRead, MarkAllAsRead, Dismiss, UnreadCount
- Bell icon renders in navbar for authenticated users
- AJAX dropdown loads notifications and supports mark-read/dismiss
- DeleteAsync exists on INotificationService and NotificationService
</verification>

<success_criteria>
- Authenticated user sees bell icon with unread count badge in navbar
- Clicking bell opens dropdown showing notifications with title, message, relative timestamp
- Clicking notification marks as read and navigates to ActionUrl if present
- X button dismisses notification (hard delete)
- "Tandai semua dibaca" marks all as read
- Badge count updates after all actions without page reload
- NotificationService.SendAsync callable from any controller (already works, confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/130-notification-infrastructure/130-01-SUMMARY.md`
</output>
