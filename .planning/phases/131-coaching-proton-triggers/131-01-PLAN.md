---
phase: 131-coaching-proton-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - COACH-01
  - COACH-02
  - COACH-03

must_haves:
  truths:
    - "Coach receives notification when assigned a new coachee"
    - "Coach and coachee both receive notification when mapping is edited"
    - "Coach and coachee both receive notification when mapping is deactivated"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "INotificationService injection + notification triggers in mapping actions"
      contains: "_notificationService.SendAsync"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Services/NotificationService.cs"
      via: "INotificationService DI injection"
      pattern: "_notificationService\\.SendAsync"
---

<objective>
Wire notification triggers into AdminController mapping actions (assign, edit, deactivate) so coaches and coachees receive in-app notifications for coaching mapping lifecycle events.

Purpose: Coaching Proton mapping changes need to notify affected users immediately.
Output: AdminController with INotificationService injected and 3 trigger points active.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/131-coaching-proton-triggers/131-CONTEXT.md
@.planning/phases/131-coaching-proton-triggers/131-RESEARCH.md
@.planning/phases/130-notification-infrastructure/130-01-SUMMARY.md

<interfaces>
From Services/INotificationService.cs:
```csharp
Task<bool> SendAsync(string userId, string type, string title, string message, string? actionUrl = null);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject INotificationService into AdminController</name>
  <files>Controllers/AdminController.cs</files>
  <action>
Add INotificationService to AdminController constructor:
1. Add `private readonly INotificationService _notificationService;` field
2. Add `INotificationService notificationService` parameter to constructor
3. Assign `_notificationService = notificationService;`
  </action>
  <verify>dotnet build --no-restore 2>&1 | tail -5</verify>
  <done>AdminController compiles with INotificationService injected</done>
</task>

<task type="auto">
  <name>Task 2: Add mapping notification triggers (COACH-01, COACH-02, COACH-03)</name>
  <files>Controllers/AdminController.cs</files>
  <action>
Add notification calls AFTER the final SaveChangesAsync in each action. All notifications use SendAsync with actionUrl="/CDP/CoachingProton". Bahasa Indonesia, semi-formal tone. Wrap each notification block in try-catch that silently catches (fail-silent pattern).

**COACH-01 - CoachCoacheeMappingAssign (after final SaveChangesAsync around line 2998):**
- This action assigns multiple coachees (CoacheeIds). For EACH coachee assigned, send one notification to the coach:
  - Type: "COACH_ASSIGNED"
  - Title: "Coach Ditunjuk"
  - Message: "Anda ditunjuk sebagai coach untuk {coacheeName}"
- Look up coachee name from the User entity (already loaded or query _context.Users).

**COACH-02 - CoachCoacheeMappingEdit (after SaveChangesAsync around line 3076):**
- Send notification to BOTH coach and coachee:
  - Type: "COACH_MAPPING_EDITED"
  - Title: "Mapping Coaching Diubah"
  - Message to coach: "Mapping coaching Anda dengan {coacheeName} telah diubah"
  - Message to coachee: "Mapping coaching Anda dengan {coachName} telah diubah"
- Get coach and coachee IDs from the mapping entity being edited.

**COACH-03 - CoachCoacheeMappingDeactivate (after SaveChangesAsync around line 3167):**
- Send notification to BOTH coach and coachee:
  - Type: "COACH_MAPPING_DEACTIVATED"
  - Title: "Mapping Coaching Dinonaktifkan"
  - Message to coach: "Mapping coaching Anda dengan {coacheeName} telah dinonaktifkan"
  - Message to coachee: "Mapping coaching Anda dengan {coachName} telah dinonaktifkan"
- Get coach and coachee IDs from the mapping entity being deactivated.

For all triggers: look up user FullName (fallback to UserName) for the name placeholders.
  </action>
  <verify>dotnet build --no-restore 2>&1 | tail -5</verify>
  <done>Assign creates notification for coach per coachee. Edit and deactivate create notifications for both coach and coachee. All messages in Bahasa Indonesia linking to /CDP/CoachingProton.</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` succeeds
- Grep confirms: `grep -c "SendAsync" Controllers/AdminController.cs` returns 4+ (1 per coachee in assign loop + 2 for edit + 2 for deactivate)
</verification>

<success_criteria>
- AdminController has INotificationService injected
- CoachCoacheeMappingAssign sends COACH_ASSIGNED notification to coach for each coachee
- CoachCoacheeMappingEdit sends COACH_MAPPING_EDITED to both coach and coachee
- CoachCoacheeMappingDeactivate sends COACH_MAPPING_DEACTIVATED to both coach and coachee
- All messages are Bahasa Indonesia with user names
- All actionUrls point to /CDP/CoachingProton
</success_criteria>

<output>
After completion, create `.planning/phases/131-coaching-proton-triggers/131-01-SUMMARY.md`
</output>
