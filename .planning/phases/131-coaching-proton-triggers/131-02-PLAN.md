---
phase: 131-coaching-proton-triggers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
autonomous: true
requirements:
  - COACH-04
  - COACH-05
  - COACH-06
  - COACH-07

must_haves:
  truths:
    - "SrSpv/SectionHead receives notification when deliverable is submitted for review"
    - "Coach and coachee receive notification when deliverable is approved"
    - "Coach and coachee receive notification when deliverable is rejected"
    - "All HC users receive notification when all deliverables for a coachee are complete via UserNotification"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "INotificationService injection + deliverable triggers + ProtonNotification migration"
      contains: "_notificationService.SendAsync"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Services/NotificationService.cs"
      via: "INotificationService DI injection"
      pattern: "_notificationService\\.SendAsync"
---

<objective>
Wire notification triggers into CDPController deliverable actions (submit, approve, reject) and migrate ProtonNotification all-complete logic to UserNotification.

Purpose: Deliverable lifecycle events and completion milestones need to notify relevant users.
Output: CDPController with INotificationService injected, 4 trigger types active, ProtonNotification code replaced.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/131-coaching-proton-triggers/131-CONTEXT.md
@.planning/phases/131-coaching-proton-triggers/131-RESEARCH.md
@.planning/phases/130-notification-infrastructure/130-01-SUMMARY.md

<interfaces>
From Services/INotificationService.cs:
```csharp
Task<bool> SendAsync(string userId, string type, string title, string message, string? actionUrl = null);
```

From Models/UserRoles.cs:
```csharp
public const string HC = "HC";
// UserManager.GetUsersInRoleAsync(UserRoles.HC) returns all HC users
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject INotificationService and add deliverable triggers (COACH-04, COACH-05, COACH-06)</name>
  <files>Controllers/CDPController.cs</files>
  <action>
**DI Injection:**
1. Add `private readonly INotificationService _notificationService;` field
2. Add `INotificationService notificationService` parameter to CDPController constructor
3. Assign `_notificationService = notificationService;`

**COACH-04 - Submit deliverable (TWO entry points):**
Create a private helper method `NotifyReviewersAsync(string coacheeId, string coacheeName)`:
- Get coachee's active mapping to find AssignmentSection: `_context.CoachCoacheeMappings.FirstOrDefaultAsync(m => m.CoacheeId == coacheeId && m.IsActive)`
- Find users at Level 4 (SrSpv/SectionHead) in same section: `_context.Users.Where(u => u.IsActive && u.Section == section && u.RoleLevel == 4)`
- Send to each reviewer:
  - Type: "COACH_EVIDENCE_SUBMITTED"
  - Title: "Deliverable Disubmit"
  - Message: "Deliverable {coacheeName} telah disubmit untuk review"
  - ActionUrl: "/CDP/CoachingProton"

Call this helper from BOTH:
- `UploadEvidence` (around line 1000) — after successful SaveChangesAsync
- `SubmitEvidenceWithCoaching` (around line 1820) — after successful SaveChangesAsync
For SubmitEvidenceWithCoaching (batch), group by coacheeId and send one notification per unique coachee.

**COACH-05 - ApproveDeliverable (after SaveChangesAsync around line 837):**
- Look up coach from active mapping: `_context.CoachCoacheeMappings.FirstOrDefaultAsync(m => m.CoacheeId == progress.CoacheeId && m.IsActive)`
- Send to coach:
  - Type: "COACH_EVIDENCE_APPROVED", Title: "Deliverable Disetujui"
  - Message: "Deliverable {coacheeName} telah disetujui"
- Send to coachee:
  - Same type/title, Message: "Deliverable Anda telah disetujui"
- ActionUrl: "/CDP/CoachingProton"

**COACH-06 - RejectDeliverable (after SaveChangesAsync around line 917):**
- Same coach lookup as COACH-05
- Send to coach:
  - Type: "COACH_EVIDENCE_REJECTED", Title: "Deliverable Ditolak"
  - Message: "Deliverable {coacheeName} telah ditolak"
- Send to coachee:
  - Same type/title, Message: "Deliverable Anda telah ditolak"
- ActionUrl: "/CDP/CoachingProton"
- Do NOT include rejection reason in notification (per user decision)

All notification blocks wrapped in try-catch (fail-silent). Look up FullName (fallback UserName) for name placeholders.
  </action>
  <verify>dotnet build --no-restore 2>&1 | tail -5</verify>
  <done>CDPController has INotificationService injected. Submit triggers notify section reviewers. Approve/reject triggers notify coach and coachee. All Bahasa Indonesia, no rejection reason in reject message.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ProtonNotification to UserNotification (COACH-07)</name>
  <files>Controllers/CDPController.cs</files>
  <action>
Rewrite `CreateHCNotificationAsync` method (around line 923) to use UserNotification via NotificationService instead of ProtonNotification:

1. Replace the existing method body:
   - Change dedup check from `_context.ProtonNotifications.AnyAsync(...)` to `_context.UserNotifications.AnyAsync(n => n.Type == "COACH_ALL_COMPLETE" && n.Message.Contains(coacheeId))`
   - Replace `new ProtonNotification { ... }` and `_context.ProtonNotifications.AddRange(...)` with a loop calling `_notificationService.SendAsync()` for each HC user
   - Use `_userManager.GetUsersInRoleAsync(UserRoles.HC)` to get HC users (same as existing pattern)
   - Type: "COACH_ALL_COMPLETE"
   - Title: "Semua Deliverable Selesai"
   - Message: "Semua deliverable {coacheeName} telah selesai"
   - ActionUrl: "/CDP/CoachingProton"

2. Remove ProtonNotification code references in CDPController:
   - Remove the lines that reference `_context.ProtonNotifications` (lines ~926, 938, 949)
   - Remove any `using` statement for ProtonNotification if it becomes unused
   - Do NOT delete the ProtonNotification model class itself (keep for now per user decision)
   - Do NOT drop the ProtonNotifications table

3. Check for any other ProtonNotification references in CDPController and clean them up (replace with UserNotification equivalents or remove if no longer needed).
  </action>
  <verify>dotnet build --no-restore 2>&1 | tail -5</verify>
  <done>CreateHCNotificationAsync uses UserNotification via NotificationService. No ProtonNotification code references remain in CDPController. ProtonNotification model class still exists. Build succeeds.</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` succeeds
- `grep -c "ProtonNotification" Controllers/CDPController.cs` returns 0
- `grep -c "SendAsync" Controllers/CDPController.cs` returns 6+ (reviewer notifications + approve coach/coachee + reject coach/coachee + HC all-complete)
</verification>

<success_criteria>
- CDPController has INotificationService injected
- UploadEvidence and SubmitEvidenceWithCoaching both trigger COACH-04 notifications to section SrSpv/SH
- ApproveDeliverable triggers COACH-05 notifications to coach and coachee
- RejectDeliverable triggers COACH-06 notifications to coach and coachee (no rejection reason)
- CreateHCNotificationAsync uses UserNotification via NotificationService (COACH-07)
- No ProtonNotification code references remain in CDPController
- All messages Bahasa Indonesia, all actionUrls /CDP/CoachingProton
</success_criteria>

<output>
After completion, create `.planning/phases/131-coaching-proton-triggers/131-02-SUMMARY.md`
</output>
