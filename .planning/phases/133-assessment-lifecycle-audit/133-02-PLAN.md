---
phase: 133-assessment-lifecycle-audit
plan: 02
type: execute
wave: 2
depends_on: [133-01]
files_modified:
  - Controllers/AdminController.cs
  - Controllers/CMPController.cs
  - Views/Admin/CreateAssessment.cshtml
  - Views/CMP/Assessment.cshtml
  - Views/CMP/ExamSummary.cshtml
  - Views/CMP/Results.cshtml
autonomous: false
requirements: [ASMT-01, ASMT-02, ASMT-03]
must_haves:
  truths:
    - "Admin can create assessment with question package, assign workers, set schedule without errors"
    - "Worker can start exam, see questions, auto-save works, and submit successfully"
    - "Results page shows correct score, pass/fail, and competency earned after submission"
    - "Assessment Proton (Tahun 1/2/3 including interview) works with equal depth"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "CreateAssessment, EditAssessment working correctly"
    - path: "Controllers/CMPController.cs"
      provides: "StartExam, SaveAnswer, SubmitExam, Results working correctly"
  key_links:
    - from: "Views/Admin/CreateAssessment.cshtml"
      to: "AdminController.CreateAssessment POST"
      via: "form submission"
      pattern: "asp-action.*CreateAssessment"
    - from: "Views/CMP/Assessment.cshtml"
      to: "CMPController.StartExam"
      via: "start exam button"
      pattern: "StartExam"
    - from: "CMPController.SubmitExam"
      to: "CMPController.Results"
      via: "redirect after submit"
      pattern: "RedirectToAction.*Results"
---

<objective>
Audit and fix the assessment creation and exam-taking flows end-to-end.

Purpose: Ensure admin can create/assign assessments and workers can take exams with correct results.
Output: All bugs found and fixed in create, exam, and results flows.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/133-assessment-lifecycle-audit/133-CONTEXT.md
@.planning/phases/133-assessment-lifecycle-audit/133-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix Create/Assign/Edit assessment flow</name>
  <files>Controllers/AdminController.cs, Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml</files>
  <action>
**Code audit of the Create Assessment flow (ASMT-01):**

1. Read AdminController CreateAssessment GET and POST actions fully. Check:
   - Question package selection works (dropdown populated, value saved correctly)
   - Worker assignment works (multi-select, saved to AssessmentSession records)
   - Schedule date/time validation (no past dates, proper format)
   - Error handling (what happens on validation failure, duplicate assignment)
   - Assessment Proton creation (Tahun 1/2/3 types, interview type for Tahun 3)

2. Read CreateAssessment.cshtml fully. Check:
   - Form fields match controller expectations
   - Client-side validation present and correct
   - JavaScript for dynamic worker selection works
   - All form data posted correctly (no missing hidden fields)

3. Read EditAssessment flow. Check same concerns.

4. Fix any bugs found. Document each bug and fix clearly in the summary.

**Per user decision:** Investigate which exam engine (Legacy vs Package) is active. The active one gets full audit in the exam task; inactive gets basic sanity check.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Create/Edit assessment flows audited. All bugs found are fixed. Exam engine determination documented.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix exam-taking and results flow</name>
  <files>Controllers/CMPController.cs, Views/CMP/Assessment.cshtml, Views/CMP/StartExam.cshtml, Views/CMP/ExamSummary.cshtml, Views/CMP/Results.cshtml</files>
  <action>
**Code audit of the Exam flow (ASMT-02):**

1. Read CMPController exam actions: Assessment list, StartExam, SaveAnswer, ExamSummary, SubmitExam. Check:
   - StartExam creates attempt correctly, handles re-attempts
   - SaveAnswer auto-save works (AJAX endpoint, saves to correct response table)
   - Timer/heartbeat logic (code review only per user decision — no real-time manual testing)
   - ExamSummary shows answered/unanswered count correctly
   - SubmitExam calculates score, determines pass/fail, records competency
   - Edge cases: submitting twice, expired timer, abandoned exam

2. Read exam views. Check:
   - Questions render correctly (multiple choice, options displayed)
   - Auto-save JavaScript fires on answer selection
   - Navigation between questions works
   - Submit confirmation dialog present

**Code audit of the Results flow (ASMT-03):**

3. Read CMPController Results action. Check:
   - Score calculation is correct (correct answers / total questions)
   - Pass/fail threshold applied correctly
   - Competency earned displayed (what competency, from which assessment)
   - Assessment Proton results (Tahun 3 interview scoring with 5-aspect 1-5 scale)

4. Fix any bugs found.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Exam and results flows audited. Auto-save, submission, score calculation, and pass/fail all verified correct or fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies create, exam, and results flows</name>
  <what-built>Audited and fixed assessment creation, exam-taking, and results flows. Any bugs found have been fixed.</what-built>
  <how-to-verify>
    1. As Admin: Create a new assessment — select question package, assign workers, set schedule, submit. Should succeed.
    2. As Admin: Edit the assessment — change schedule or workers. Should save correctly.
    3. As Worker: Go to Assessment page — see assigned assessment. Click Start Exam.
    4. As Worker: Answer questions — verify auto-save indicator works. Navigate between questions.
    5. As Worker: Submit exam — see ExamSummary, confirm submission.
    6. As Worker: View Results — correct score, pass/fail, competency earned displayed.
    7. If Assessment Proton exists: test Tahun 1/2/3 flows similarly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Create, exam, and results flows all work end-to-end without errors. Assessment Proton covered.
</verification>

<success_criteria>
- Admin creates assessment with all fields populated correctly
- Worker completes exam with working auto-save and successful submission
- Results show correct score, pass/fail, and competency earned
- No JS errors in browser console during exam flow
</success_criteria>

<output>
After completion, create `.planning/phases/133-assessment-lifecycle-audit/133-02-SUMMARY.md`
</output>
