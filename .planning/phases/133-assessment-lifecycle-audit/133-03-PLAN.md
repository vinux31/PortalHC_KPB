---
phase: 133-assessment-lifecycle-audit
plan: 03
type: execute
wave: 2
depends_on: [133-01]
files_modified:
  - Controllers/AdminController.cs
  - Controllers/CMPController.cs
  - Controllers/NotificationController.cs
  - Views/Admin/AssessmentMonitoring.cshtml
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/CMP/Records.cshtml
  - Services/NotificationService.cs
autonomous: false
requirements: [ASMT-04, ASMT-05, ASMT-06]
must_haves:
  truths:
    - "Records page shows accurate assessment and training history per worker"
    - "Records filters work individually (by type, status, date)"
    - "HC monitoring shows live progress with correct status per worker"
    - "Reset and force close actions work on monitoring page"
    - "Notifications reach correct users on assessment assign and group completion"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Records action with working filters"
    - path: "Controllers/AdminController.cs"
      provides: "AssessmentMonitoring, MonitoringDetail with reset/force close"
    - path: "Services/NotificationService.cs"
      provides: "Assessment notification templates"
  key_links:
    - from: "AdminController.AssessmentMonitoringDetail"
      to: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      via: "live polling for progress"
      pattern: "polling|setInterval|fetch"
    - from: "AdminController.CreateAssessment"
      to: "NotificationService"
      via: "notification on assign"
      pattern: "NotificationService|CreateNotification"
---

<objective>
Audit and fix records/history, HC monitoring, and assessment notifications.

Purpose: Ensure downstream assessment data (records, monitoring, notifications) is accurate and functional.
Output: All bugs found and fixed in records, monitoring, and notification flows.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/133-assessment-lifecycle-audit/133-CONTEXT.md
@.planning/phases/133-assessment-lifecycle-audit/133-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix Records/History page and filters</name>
  <files>Controllers/CMPController.cs, Views/CMP/Records.cshtml, Controllers/AdminController.cs, Views/Admin/UserAssessmentHistory.cshtml</files>
  <action>
**Code audit of Records flow (ASMT-04):**

1. Read CMPController Records action. Check:
   - Assessment history query is correct (joins, filters, ordering)
   - Training history included alongside assessment history
   - Data displayed per worker matches what's in the database
   - AssessmentAttemptHistory model correctly tracks re-attempts

2. Read Records.cshtml view. Check:
   - Filters: type filter (assessment type/category), status filter (pass/fail/pending), date filter
   - Test each filter individually per user decision (no combinatorial testing)
   - Filter state preserved after applying (selected values retained)
   - Empty state handling (no results message)
   - Pagination if applicable

3. Read AdminController UserAssessmentHistory (HC view of worker history). Check:
   - Shows same data as worker's own Records but from admin perspective
   - Statistics cards (total assessments, pass rate, etc.) calculate correctly

4. Fix any bugs found.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Records page shows accurate history. Each filter works individually. UserAssessmentHistory accessible and correct.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix HC monitoring and assessment notifications</name>
  <files>Controllers/AdminController.cs, Views/Admin/AssessmentMonitoring.cshtml, Views/Admin/AssessmentMonitoringDetail.cshtml, Services/NotificationService.cs</files>
  <action>
**Code audit of HC Monitoring flow (ASMT-05):**

1. Read AdminController AssessmentMonitoring and AssessmentMonitoringDetail actions (post Plan 01 fix). Check:
   - Monitoring list shows all active assessment groups with correct counts
   - MonitoringDetail shows per-worker progress (not started/in progress/completed)
   - Live polling JavaScript updates status without page refresh
   - Countdown timer displays correctly based on schedule
   - Reset action: resets a worker's attempt (clears responses, resets status)
   - Force close action: marks exam as completed/abandoned, calculates score

2. Read monitoring views. Check:
   - Status badges/colors correct per state
   - Action buttons (reset, force close) have confirmation dialogs
   - AJAX calls for actions use correct endpoints and handle errors

**Code audit of Notifications (ASMT-06):**

3. Read NotificationService assessment-related code. Check:
   - Assessment assign: notification created for each assigned worker
   - Group completion: notification sent to HC when all workers in group complete
   - Notification templates use correct data (assessment title, schedule date)
   - No duplicate notifications on re-assignment or edit

4. Read NotificationController. Check:
   - List, MarkAsRead, Dismiss, UnreadCount all work
   - Notification links navigate to correct assessment pages

5. Fix any bugs found.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Monitoring shows live progress, reset/force close work. Notifications reach correct users with correct content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies records, monitoring, and notifications</name>
  <what-built>Audited and fixed records/history, HC monitoring, and notification flows. Any bugs found have been fixed.</what-built>
  <how-to-verify>
    1. As Worker: Go to Records page — verify assessment history shows correct entries
    2. As Worker: Apply each filter individually (type, status, date) — results should filter correctly
    3. As Admin/HC: Go to AssessmentMonitoring — see active assessment groups with worker counts
    4. As Admin/HC: Click into MonitoringDetail — see per-worker progress status
    5. As Admin/HC: Test Reset on a worker's attempt (if safe to do so with test data)
    6. As Admin/HC: Test Force Close on an in-progress exam (if safe to do so)
    7. Check notification bell — assessment-related notifications should appear for assigned workers
    8. Click a notification — should navigate to the correct assessment page
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Records, monitoring, and notifications all function correctly. Filters work. Admin actions (reset, force close) work. Notifications reach correct users.
</verification>

<success_criteria>
- Records page displays accurate history with working individual filters
- MonitoringDetail shows correct per-worker progress with live updates
- Reset and force close actions complete without errors
- Assessment notifications created and delivered to correct users
</success_criteria>

<output>
After completion, create `.planning/phases/133-assessment-lifecycle-audit/133-03-SUMMARY.md`
</output>
