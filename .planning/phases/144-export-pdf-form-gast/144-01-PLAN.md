---
phase: 144-export-pdf-form-gast
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
autonomous: false
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04
must_haves:
  truths:
    - "PDF downloads as landscape A4 with 3-column table layout"
    - "Left column shows Kompetensi, Sub Kompetensi, Deliverable, and 4 Acuan fields"
    - "Center column shows CatatanCoach text"
    - "Right column shows Kesimpulan checkboxes, Result checkboxes, and TTD Coach with P-Sign"
    - "Header has logo top-right and date top-left"
    - "Footer has red wave with ptkpi.pertamina.com text and logo-135"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Redesigned DownloadEvidencePdf method"
      contains: "PageSizes.A4.Landscape"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "CoachingSession"
      via: "session.AcuanPedoman, AcuanTko, AcuanBestPractice, AcuanDokumen"
      pattern: "session\\.Acuan"
---

<objective>
Redesign the DownloadEvidencePdf action in CDPController to produce a landscape A4 PDF matching the Form Coaching GAST Pertamina layout with 3-column table, checkbox Kesimpulan/Result, TTD Coach P-Sign, and Pertamina branding footer.

Purpose: Replace the current vertical label-value PDF with the official Pertamina form layout.
Output: Updated CDPController.cs with new PDF generation code.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-export-pdf-form-gast/144-CONTEXT.md

<interfaces>
<!-- From Controllers/CDPController.cs (lines 2268-2409): existing DownloadEvidencePdf method to replace -->
<!-- Keep: access control (lines 2270-2306), data loading (lines 2275-2335), filename generation (2405-2408) -->
<!-- Replace: everything from line 2337 (accentColor) through line 2401 (page footer) -->

From Models/CoachingSession.cs:
```csharp
public string CatatanCoach { get; set; } = "";
public string Kesimpulan { get; set; } = "";    // "Kompeten" or "Perlu Pengembangan"
public string Result { get; set; } = "";         // "Need Improvement" / "Suitable" / "Good" / "Excellence"
public string? AcuanPedoman { get; set; }
public string? AcuanTko { get; set; }
public string? AcuanBestPractice { get; set; }
public string? AcuanDokumen { get; set; }
```

Coach user fields available: FullName, NIP, Position, Section, Unit, RoleLevel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DownloadEvidencePdf with GAST landscape layout</name>
  <files>Controllers/CDPController.cs</files>
  <action>
Replace the PDF generation code in `DownloadEvidencePdf` (lines ~2337-2401) while keeping access control and data loading intact.

**Changes to data loading section:**
- Update coachInfo query (line 2323) to also select `u.NIP, u.Section, u.RoleLevel` (need NIP for P-Sign)

**New PDF layout — Landscape A4:**

```
page.Size(QuestPDF.Helpers.PageSizes.A4.Landscape());
page.Margin(1.5f, QuestPDF.Infrastructure.Unit.Centimetre);
```

**Header (outside table):**
- Row: left side = date formatted "dd MMMM yyyy" Indonesian culture; right side = psign-pertamina.png logo (width ~120)

**Main 3-column table** using QuestPDF `.Table()` API:
- Define 3 columns with proportional widths (e.g., 3:4:3 ratio — Claude's discretion)
- All cells with border, padding 6-8

**Left column (ACUAN) — multiple rows spanning the column:**
- Row 1: Header "ACUAN" bold, centered, background color
- Row 2: "Kompetensi" label bold + value from `kompetensi` variable
- Row 3: "Sub Kompetensi" label bold + value from `subKompetensi`
- Row 4: "Deliverable" label bold + value from `deliverable`
- Row 5: "Acuan" section header
- Row 6: "Pedoman:" bold + `session.AcuanPedoman ?? "-"`
- Row 7: "TKO/TKI/TKPA:" bold + `session.AcuanTko ?? "-"`
- Row 8: "Best Practice:" bold + `session.AcuanBestPractice ?? "-"`
- Row 9: "Dokumen:" bold + `session.AcuanDokumen ?? "-"`

**Center column (CATATAN COACH) — single large cell spanning all rows:**
- Header row: "CATATAN COACH" bold, centered, background color
- Content: `session.CatatanCoach` or "-" if empty, in one big cell

**Right column (KESIMPULAN DARI COACH) — stacked sections:**
- Header row: "KESIMPULAN DARI COACH" bold, centered, background color
- Section 1 — "Kesimpulan:" label bold, then vertical checkboxes:
  - Use Unicode: checked = "\u2611" unchecked = "\u2610"
  - `session.Kesimpulan == "Kompeten"` → check "Kompeten", uncheck "Perlu Pengembangan"
  - `session.Kesimpulan == "Perlu Pengembangan"` → vice versa
  - Format each: `{checkbox} Kompeten` and `{checkbox} Perlu Pengembangan`
- Section 2 — "Result:" label bold, then vertical checkboxes:
  - 4 options: "Need Improvement", "Suitable", "Good", "Excellence"
  - Check the one matching `session.Result`, uncheck others
- Section 3 — TTD Coach P-Sign box:
  - Border box containing:
    - psign-pertamina.png logo (width ~50), centered
    - Coach role description: combine Position + Unit (or Section if Unit empty)
    - Coach FullName bold
    - "NIP: {coachInfo.NIP}" or "NIP: -" if null/empty

**Footer:**
- Left side: Red wave shape using QuestPDF Canvas API
  - Draw a red (#CC0000) trapezoid/polygon shape from left edge
  - Overlay white text "ptkpi.pertamina.com" on the red shape
- Right side: logo-135.png (width ~80)
  - Load from `wwwroot/images/logo-135.png`

**Empty field handling:** All Acuan fields, CatatanCoach, NIP — show "-" when null/empty.

IMPORTANT: The table layout is the most complex part. Consider using QuestPDF's `.Table()` with `RowSpan()` for the center and right columns if needed, OR use a simpler approach: a Row with 3 RelativeItems, each containing a Column of content. Choose whichever produces cleaner code — Claude's discretion.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
- PDF generates as landscape A4 with 3-column layout
- Left column shows Kompetensi, Sub Kompetensi, Deliverable, and 4 Acuan fields
- Center column shows CatatanCoach
- Right column shows Kesimpulan checkboxes (checked per value), Result checkboxes (checked per value), TTD Coach P-Sign with NIP
- Header has date left + logo right
- Footer has red wave with URL text left + logo-135 right
- Empty fields display "-"
- Build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PDF output matches Form GAST layout</name>
  <files>Controllers/CDPController.cs</files>
  <action>Human verifies the generated PDF visually against Form GAST Pertamina layout requirements.</action>
  <what-built>Redesigned PDF export with landscape 3-column layout, checkboxes, P-Sign, and Pertamina branding</what-built>
  <how-to-verify>
    1. Open the app and navigate to CDP > Deliverable for a coachee with coaching session data (including Acuan fields)
    2. Click the PDF download button on a deliverable that has evidence
    3. Open the downloaded PDF and verify:
       - Landscape A4 orientation
       - Header: date on left, Pertamina logo on right
       - 3-column table: Acuan (left), Catatan Coach (center), Kesimpulan (right)
       - Left column: Kompetensi, Sub Kompetensi, Deliverable, then Acuan sub-fields
       - Center column: CatatanCoach text
       - Right column: Kesimpulan checkboxes (correct one checked), Result checkboxes (correct one checked), P-Sign box with logo + name + NIP
       - Footer: red wave shape with white "ptkpi.pertamina.com" text, logo-135 on right
    4. Test with a session that has empty Acuan fields — should show "-"
  </how-to-verify>
  <verify>
    <automated>echo "Manual visual verification required"</automated>
  </verify>
  <done>User confirms PDF layout matches Form GAST Pertamina design</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- PDF downloads without error
- Visual inspection confirms Form GAST layout
</verification>

<success_criteria>
- PDF output matches Form Coaching GAST Pertamina layout with all 4 requirements (PDF-01 through PDF-04) satisfied
- Build passes, no regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/144-export-pdf-form-gast/144-01-SUMMARY.md`
</output>
