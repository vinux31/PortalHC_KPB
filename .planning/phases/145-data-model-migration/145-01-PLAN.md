---
phase: 145-data-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentPackage.cs
  - Migrations/AddSubCompetencyToPackageQuestion.cs
  - Migrations/AddSubCompetencyToPackageQuestion.Designer.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
autonomous: true
requirements:
  - SUBTAG-02
user_setup: []

must_haves:
  truths:
    - "PackageQuestion has a nullable string SubCompetency property"
    - "EF Core migration adds SubCompetency column to PackageQuestions table"
    - "Existing rows have NULL SubCompetency after migration (backward compatible)"
  artifacts:
    - path: "Models/AssessmentPackage.cs"
      provides: "SubCompetency property on PackageQuestion"
      contains: "SubCompetency"
    - path: "Migrations/*AddSubCompetencyToPackageQuestion.cs"
      provides: "Migration adding nullable column"
      contains: "AddColumn"
  key_links:
    - from: "Models/AssessmentPackage.cs"
      to: "Migrations/*AddSubCompetencyToPackageQuestion.cs"
      via: "EF Core migration scaffold"
      pattern: "SubCompetency.*nvarchar"
---

<objective>
Add SubCompetency nullable string field to PackageQuestion and create the EF Core migration.

Purpose: Enable sub-competency tagging on assessment questions for later import and analysis.
Output: Updated model + migration files ready to apply.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-data-model-migration/145-RESEARCH.md

@Models/AssessmentPackage.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SubCompetency property and scaffold migration</name>
  <files>Models/AssessmentPackage.cs, Migrations/ (scaffolded files)</files>
  <action>
1. In Models/AssessmentPackage.cs, add this property to the PackageQuestion class after ScoreValue (line 41), before the Navigation comment:

```csharp
public string? SubCompetency { get; set; }
```

2. Run `dotnet build` to confirm the model compiles.

3. Scaffold the migration:
```bash
dotnet ef migrations add AddSubCompetencyToPackageQuestion
```

4. Verify the generated migration's Up method contains:
```csharp
migrationBuilder.AddColumn<string>(
    name: "SubCompetency",
    table: "PackageQuestions",
    type: "nvarchar(max)",
    nullable: true);
```
And the Down method drops the column.

5. Run `dotnet build` again to confirm migration compiles.

6. Apply the migration:
```bash
dotnet ef database update
```
  </action>
  <verify>
    <automated>dotnet build && dotnet ef database update</automated>
  </verify>
  <done>PackageQuestion model has SubCompetency nullable string property, migration applied successfully, existing rows unaffected (NULL values)</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- `dotnet ef database update` applies without error
- PackageQuestions table has SubCompetency column (nullable nvarchar)
</verification>

<success_criteria>
- PackageQuestion.SubCompetency property exists as nullable string
- Migration file scaffolded and applied
- Build green, no runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/145-data-model-migration/145-01-SUMMARY.md`
</output>
