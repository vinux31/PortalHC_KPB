---
phase: 146-excel-import-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ImportPackageQuestions.cshtml
autonomous: true
requirements:
  - SUBTAG-01
  - SUBTAG-03
user_setup: []
must_haves:
  truths:
    - "Downloaded template has 7 columns with 'Sub Kompetensi' as column G"
    - "Import with 7-column Excel saves normalized SubCompetency per question"
    - "Import with old 6-column Excel still works — SubCompetency is NULL"
    - "Paste-text with 7 tab-separated columns saves SubCompetency"
    - "Paste-text with 6 columns still works — SubCompetency is NULL"
    - "Values are Title Case normalized and whitespace-collapsed"
    - "Blank Sub Kompetensi cells become NULL, not empty string"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "NormalizeSubCompetency helper, extended tuple, template column G, cross-package warning"
      contains: "NormalizeSubCompetency"
    - path: "Views/Admin/ImportPackageQuestions.cshtml"
      provides: "Updated paste-text format reference showing optional 7th column"
      contains: "Sub Kompetensi"
  key_links:
    - from: "AdminController.cs (import loop)"
      to: "PackageQuestion.SubCompetency"
      via: "NormalizeSubCompetency(subComp) assignment"
      pattern: "SubCompetency.*=.*NormalizeSubCompetency"
---

<objective>
Update the Excel question import system to support an optional 7th column "Sub Kompetensi".

Purpose: Enable HC to tag questions with sub-competency labels during import for downstream radar chart analysis.
Output: Modified AdminController (template + parsing + normalization) and updated import view.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-excel-import-update/146-CONTEXT.md
@.planning/phases/146-excel-import-update/146-RESEARCH.md
@Controllers/AdminController.cs
@Views/Admin/ImportPackageQuestions.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NormalizeSubCompetency helper and update DownloadQuestionTemplate</name>
  <files>Controllers/AdminController.cs</files>
  <action>
1. Add a private static helper method `NormalizeSubCompetency(string? raw)` near the existing helpers (around line 5368):
   - Return null if `string.IsNullOrWhiteSpace(raw)`
   - Trim, collapse whitespace via `Regex.Replace(raw.Trim(), @"\s+", " ")`
   - Apply Title Case: `CultureInfo.InvariantCulture.TextInfo.ToTitleCase(cleaned.ToLowerInvariant())`
   - IMPORTANT: call `.ToLowerInvariant()` FIRST to handle ALL-CAPS input (per .NET ToTitleCase behavior)

2. Update `DownloadQuestionTemplate()` (around line 5068):
   - Add 7th header "Sub Kompetensi" in column G with same bold/green style as existing headers
   - Add 7th example value "Sub Kompetensi x.x" in column G with same italic/gray style
   - Add instruction row (row 4 or append to existing instruction row): "Kolom Sub Kompetensi: opsional, isi nama sub-kompetensi. Kosongkan jika tidak ada." — italic, dark red font, matching existing instruction style
   - Adjust column widths to accommodate 7th column
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>NormalizeSubCompetency helper exists. Template downloads with 7 columns including Sub Kompetensi header, example, and instruction.</done>
</task>

<task type="auto">
  <name>Task 2: Extend import parsing for Excel, paste-text, and cross-package warning</name>
  <files>Controllers/AdminController.cs, Views/Admin/ImportPackageQuestions.cshtml</files>
  <action>
1. Extend the row tuple from 6 to 7 elements — add `string? SubCompetency` as 7th element:
   `List<(string Question, string OptA, string OptB, string OptC, string OptD, string Correct, string? SubCompetency)>`

2. Update Excel parsing (inside ImportPackageQuestions POST):
   - After parsing cells 1-6, read cell 7: `var cell7 = row.Cell(7).GetString().Trim();`
   - If `string.IsNullOrWhiteSpace(cell7)` set to null, otherwise keep raw value
   - Add as 7th tuple element. Old 6-column files will have empty cell 7 = null. No special detection needed.

3. Update paste-text parsing:
   - Change minimum column check from `cells.Length < 6` error to keep `< 6` as error threshold
   - Parse 7th column: `string? subComp = cells.Length >= 7 ? cells[6].Trim() : null;`
   - If `string.IsNullOrWhiteSpace(subComp)` set to null
   - Add as 7th tuple element

4. Update the "expected columns" error messages to say "at least 6 columns" instead of hardcoded "6 columns"

5. Update PackageQuestion creation (around line 5293):
   - Set `SubCompetency = NormalizeSubCompetency(row.SubCompetency)` on the new PackageQuestion object

6. Add cross-package SubCompetency mismatch warning (after import loop, only if rows were added):
   - Query sibling packages (same AssessmentId, different PackageId) for distinct non-null SubCompetency values
   - Compare with current import batch's distinct normalized SubCompetency values
   - If sets differ, add TempData["Warning"] with a message like: "Sub Kompetensi pada paket ini berbeda dari paket lain dalam assessment yang sama. Pastikan konsistensi antar paket."
   - Use the existing sibling query pattern from lines 5220-5258

7. Update Views/Admin/ImportPackageQuestions.cshtml:
   - Update the paste-text format reference `<code>` to include: `Question | Option A | Option B | Option C | Option D | Correct | Sub Kompetensi (opsional)`
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Import accepts both 6-column (backward compatible, SubCompetency=NULL) and 7-column input (both Excel and paste-text). SubCompetency values are normalized via NormalizeSubCompetency. Cross-package mismatch produces a warning. View shows updated format reference.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with no errors
2. Download template — verify column G header "Sub Kompetensi", example row, instruction row
3. Import a 7-column Excel — verify SubCompetency saved with Title Case normalization
4. Import a 6-column Excel — verify no errors, SubCompetency is NULL
5. Paste 7-column tab-separated text — verify SubCompetency saved
6. Paste 6-column tab-separated text — verify no errors, SubCompetency is NULL
7. Import with different Sub Kompetensi set than sibling package — verify warning appears
</verification>

<success_criteria>
- Template has 7 columns with Sub Kompetensi as column G
- Both Excel and paste-text import support optional 7th column
- Old 6-column input remains backward compatible
- SubCompetency values normalized (trim, collapse whitespace, Title Case)
- Blank/empty values stored as NULL
- Cross-package mismatch warning displayed
</success_criteria>

<output>
After completion, create `.planning/phases/146-excel-import-update/146-01-SUMMARY.md`
</output>
