---
phase: 148-css-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/css/home.css
  - Views/Home/Index.cshtml
autonomous: true
requirements:
  - CSS-01
  - CSS-02
  - CSS-03
must_haves:
  truths:
    - "home.css contains no glass-card, backdrop-filter, or blur pseudo-element rules"
    - "home.css contains no timeline or deadline-card styling rules"
    - "Home/Index.cshtml has no data-aos attributes on any element"
    - "CMP, CDP, and Admin pages remain visually intact — no shared classes were removed"
  artifacts:
    - path: "wwwroot/css/home.css"
      provides: "Cleaned homepage stylesheet — glassmorphism and timeline sections removed"
      contains: ".hero-section {"
    - path: "Views/Home/Index.cshtml"
      provides: "Homepage view with data-aos attributes stripped"
  key_links:
    - from: "wwwroot/css/home.css"
      to: "Hero section base styles"
      via: "Must retain .hero-section rule block (background gradient, padding, border-radius)"
      pattern: ".hero-section {"
    - from: "Views/Shared/_Layout.cshtml"
      to: "AOS library (CDN)"
      via: "AOS stays loaded for Guide.cshtml — only Homepage data-aos attributes removed"
      pattern: "data-aos"
---

<objective>
Strip glassmorphism styles, animation attributes, and unused layout component CSS from home.css and Home/Index.cshtml in preparation for Phase 149's HTML redesign.

Purpose: Phase 149 will rebuild the homepage HTML using Bootstrap card patterns. Before that HTML restructuring, home.css must be cleaned of all rules that will never be referenced again — glass-card, blur pseudo-elements, timeline, and deadline-card blocks. This prevents dead CSS accumulating and confirms no CMP/CDP/Admin pages share these class names.

Output:
- wwwroot/css/home.css — reduced from ~467 lines, glassmorphism and timeline sections deleted
- Views/Home/Index.cshtml — all data-aos attributes removed, HTML structure otherwise untouched
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/148-css-audit-cleanup/148-RESEARCH.md
@.planning/phases/148-css-audit-cleanup/148-VALIDATION.md

<interfaces>
<!-- Key structural facts confirmed by research — no codebase exploration needed. -->

From wwwroot/css/home.css (current 467 lines):

REMOVE — glassmorphism blocks:
  Lines 34-56:   .hero-section::before { } and .hero-section::after { } (filter: blur pseudo-elements)
  Lines ~93:     .hero-badge { backdrop-filter: blur(10px); } — REMOVE the backdrop-filter line
  Lines ~111:    .hero-stat-item { backdrop-filter: blur(10px); } — REMOVE the backdrop-filter line
  Lines 131-186: .glass-card { } entire block including ::before, ::after, :hover, .card-primary/warning/success variants
  Lines 187-214: .card-icon-wrapper { } entire block (only used inside glass-cards, removed in Phase 149)

REMOVE — timeline and deadline blocks:
  Lines 309-405: .timeline, .timeline::before, .timeline-item, .timeline-item:last-child,
                 .timeline-marker, .timeline-content, .timeline-content:hover,
                 .timeline-title, .timeline-description, .timeline-time,
                 .deadline-card, .deadline-card:hover, .deadline-icon, .deadline-content,
                 .deadline-title, .deadline-date, .deadline-badge, .deadline-badge.urgent,
                 .deadline-badge.normal

KEEP — hero base rule (Phase 149 requires this):
  .hero-section {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 24px;
      padding: 3rem 2.5rem;
      color: white;
      position: relative;
      overflow: hidden;
      box-shadow: var(--shadow-lg);
      margin-bottom: 2.5rem;
  }

KEEP — quick-access-card rules (Phase 149 will use these as base)

From Views/Home/Index.cshtml (current 299 lines):

REMOVE — all data-aos attributes found on lines:
  Line 10:  data-aos="fade-down" (hero-section div)
  Line 46:  data-aos="fade-up" data-aos-delay="100" (IDP Status col)
  Line 84:  data-aos="fade-up" data-aos-delay="200" (Assessment col)
  Line 118: data-aos="fade-up" data-aos-delay="300" (Training col)
  Lines 169, 177, 185, 194: data-aos="fade-right" (Quick Access section elements)
  Lines 207, 243: data-aos attributes on Timeline and Deadlines sections

Verification: class names glass-card, timeline, deadline-card are ONLY in Home/Index.cshtml
(grep confirmed — CMP/CDP/Admin pages use Bootstrap only, zero home.css classes)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove glassmorphism CSS rules from home.css (CSS-01)</name>
  <files>wwwroot/css/home.css</files>
  <action>
Open wwwroot/css/home.css and delete the following rule blocks. Do NOT touch the .hero-section { } base rule — only its ::before and ::after pseudo-elements.

Blocks to delete (use class names as anchors, line numbers are approximate):

1. .hero-section::before { } pseudo-element block (the one with filter: blur(60px))
   Remove the entire rule from ".hero-section::before {" to its closing "}"

2. .hero-section::after { } pseudo-element block (the one with filter: blur(50px))
   Remove the entire rule from ".hero-section::after {" to its closing "}"

3. Inside the .hero-badge rule: remove the "backdrop-filter: blur(10px);" declaration line only.
   Inside the .hero-stat-item rule: remove the "backdrop-filter: blur(10px);" declaration line only.
   If removing that declaration leaves a completely empty rule block, remove the whole rule too.

4. The entire .glass-card block — remove from ".glass-card {" through to the end of ".glass-card.card-success::before { }" including all nested variants:
   .glass-card, .glass-card::before, .glass-card:hover, .glass-card:hover::before,
   .glass-card.card-primary, .glass-card.card-primary::before,
   .glass-card.card-warning, .glass-card.card-warning::before,
   .glass-card.card-success, .glass-card.card-success::before

5. The entire .card-icon-wrapper block — remove from ".card-icon-wrapper {" through to the end of its rule group (all .card-icon-wrapper variants).

After deletion, confirm the file still contains ".hero-section {" (the base rule with background gradient).

Do NOT modify guide.css. Do NOT modify any other CSS files.
  </action>
  <verify>
    <automated>grep -cE "glass-card|backdrop-filter: blur|filter: blur" /c/Users/Administrator/Desktop/PortalHC_KPB/wwwroot/css/home.css && echo "FAIL: glassmorphism rules remain" || echo "PASS: no glassmorphism rules found"</automated>
  </verify>
  <done>
    grep returns 0 matches for glass-card, backdrop-filter: blur, and filter: blur in home.css.
    .hero-section { } base rule still present (background gradient, padding, border-radius intact).
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove timeline and deadline-card CSS rules from home.css (CSS-02)</name>
  <files>wwwroot/css/home.css</files>
  <action>
Open wwwroot/css/home.css and delete the entire timeline and deadline section. Remove all rules matching these selectors (the entire block from first .timeline rule to last .deadline-badge.normal closing brace):

  .timeline { }
  .timeline::before { }
  .timeline-item { }
  .timeline-item:last-child { }
  .timeline-marker { }
  .timeline-content { }
  .timeline-content:hover { }
  .timeline-title { }
  .timeline-description { }
  .timeline-time { }
  .deadline-card { }
  .deadline-card:hover { }
  .deadline-icon { }
  .deadline-content { }
  .deadline-title { }
  .deadline-date { }
  .deadline-badge { }
  .deadline-badge.urgent { }
  .deadline-badge.normal { }

If there is a CSS comment header above this block (e.g., "/* Activity Timeline */" or "/* Upcoming Deadlines */"), remove those comment lines too.

Do NOT remove any other rules. The .quick-access-card rules that appear elsewhere must remain.
  </action>
  <verify>
    <automated>grep -cE "\.timeline|\.deadline-card" /c/Users/Administrator/Desktop/PortalHC_KPB/wwwroot/css/home.css && echo "FAIL: timeline/deadline rules remain" || echo "PASS: no timeline/deadline rules found"</automated>
  </verify>
  <done>
    grep returns 0 matches for .timeline and .deadline-card selectors in home.css.
    Timeline and deadline CSS blocks are fully removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip data-aos attributes from Home/Index.cshtml (CSS-03)</name>
  <files>Views/Home/Index.cshtml</files>
  <action>
Open Views/Home/Index.cshtml and remove all data-aos="...", data-aos-delay="...", and data-aos-duration="..." attributes from every HTML element on the page. Remove only the attributes — preserve the surrounding HTML tags and class attributes unchanged.

The 11 data-aos occurrences are on these elements (use attribute presence as anchors, not exact line numbers):
  - The outer div wrapping the hero-section (data-aos="fade-down")
  - The 3 glass-card column divs (data-aos="fade-up" data-aos-delay="100/200/300")
  - The Quick Access section header and card divs (data-aos="fade-right" and similar)
  - The Timeline section container (data-aos="fade-up")
  - The Deadlines section container (data-aos="fade-up")

After removal, all divs and their class attributes remain. Only data-aos and data-aos-delay/duration attributes are deleted. Do not restructure the HTML in any other way — Phase 149 handles all HTML changes.

Do NOT touch guide.css. Do NOT touch _Layout.cshtml. Do NOT touch Guide.cshtml (it has its own data-aos attributes that must stay).
  </action>
  <verify>
    <automated>grep -c "data-aos" /c/Users/Administrator/Desktop/PortalHC_KPB/Views/Home/Index.cshtml && echo "FAIL: data-aos attributes remain" || echo "PASS: no data-aos attributes found"</automated>
  </verify>
  <done>
    grep -c "data-aos" in Views/Home/Index.cshtml returns 0.
    HTML structure of all sections is otherwise unchanged.
    Guide.cshtml still contains its own data-aos attributes (not touched).
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run this combined verification:

```bash
cd /c/Users/Administrator/Desktop/PortalHC_KPB

echo "=== CSS-01: Glassmorphism check ==="
grep -cE "glass-card|backdrop-filter: blur|filter: blur" wwwroot/css/home.css && echo "FAIL" || echo "PASS"

echo "=== CSS-02: Timeline/deadline check ==="
grep -cE "\.timeline|\.deadline-card" wwwroot/css/home.css && echo "FAIL" || echo "PASS"

echo "=== CSS-03: data-aos on Homepage check ==="
grep -c "data-aos" Views/Home/Index.cshtml && echo "FAIL" || echo "PASS"

echo "=== Hero base preserved ==="
grep -c "\.hero-section {" wwwroot/css/home.css && echo "PASS (hero base present)" || echo "FAIL (hero base missing)"

echo "=== Guide.cshtml data-aos untouched ==="
grep -c "data-aos" Views/Home/Guide.cshtml && echo "PASS (Guide data-aos intact)" || echo "WARNING: Guide may have lost data-aos"
```

Visual smoke test (manual):
1. Run local dev server
2. Navigate to CMP Index — verify cards and styling intact
3. Navigate to CDP Index — verify cards and styling intact
4. Navigate to Admin Index — verify no broken styling
5. Navigate to Homepage — verify page loads without browser console errors
</verification>

<success_criteria>
Phase 148 is complete when:
- home.css contains zero references to glass-card, backdrop-filter: blur, or filter: blur
- home.css contains zero references to .timeline or .deadline-card selectors
- Views/Home/Index.cshtml contains zero data-aos attributes
- .hero-section { } base rule still present in home.css (required by Phase 149)
- CMP, CDP, and Admin pages render correctly after the CSS changes
- Homepage loads without JavaScript console errors
</success_criteria>

<output>
After completion, create `.planning/phases/148-css-audit-cleanup/148-01-SUMMARY.md` documenting:
- Lines removed from home.css (approximate count)
- Confirmation that hero-section base rule was preserved
- Confirmation that guide.css and Guide.cshtml were not touched
- grep verification results (all 3 checks passing)
- Any unexpected findings during cleanup
</output>
