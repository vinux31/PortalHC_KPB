---
phase: 149-homepage-view-controller-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/DashboardHomeViewModel.cs
  - Controllers/HomeController.cs
  - Views/Home/Index.cshtml
  - wwwroot/css/home.css
autonomous: false
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HERO-01, HERO-02, QUICK-01]

must_haves:
  truths:
    - "Homepage shows only the hero greeting section and three Quick Access cards — no glass cards, no timeline, no deadlines section for any role"
    - "Hero section displays greeting, user full name, position badge, unit badge, and current date — with gradient background and no glassmorphism pseudo-elements"
    - "Quick Access cards (CDP, Assessment, CMP) use Bootstrap card border-0 shadow-sm h-100 pattern matching CMP/CDP Index pages"
    - "HomeController.Index() fetches only the current user — no IDP, assessment, activity, or deadline database queries"
    - "DashboardHomeViewModel contains only CurrentUser and Greeting properties — all other properties and helper types removed"
  artifacts:
    - path: "Models/DashboardHomeViewModel.cs"
      provides: "Simplified ViewModel with 2 properties"
      contains: "CurrentUser, Greeting"
    - path: "Controllers/HomeController.cs"
      provides: "Simplified controller Index action"
      exports: ["Index", "Guide", "GuideDetail", "Error"]
    - path: "Views/Home/Index.cshtml"
      provides: "Hero + Quick Access only view"
      min_lines: 30
    - path: "wwwroot/css/home.css"
      provides: "Stripped CSS — hero rules only"
  key_links:
    - from: "Views/Home/Index.cshtml"
      to: "Models/DashboardHomeViewModel.cs"
      via: "@model DashboardHomeViewModel"
      pattern: "Model\\.CurrentUser|Model\\.Greeting"
    - from: "Controllers/HomeController.cs"
      to: "Models/DashboardHomeViewModel.cs"
      via: "new DashboardHomeViewModel { CurrentUser = ..., Greeting = ... }"
      pattern: "new DashboardHomeViewModel"
---

<objective>
Replace the current homepage with a clean hero greeting section + three Bootstrap Quick Access cards only.

Purpose: The current homepage fetches 6+ database queries and renders glassmorphism cards (IDP Status, Pending Assessment, Mandatory Training), a Recent Activity timeline, and an Upcoming Deadlines section — none of which align with the v3.18 minimalist redesign goal. Phase 148 already removed the glassmorphism CSS; this plan completes the redesign by removing the HTML sections, simplifying the ViewModel, and stripping unused controller queries.

Output: Four modified files — DashboardHomeViewModel.cs (2 properties), HomeController.cs (~50 lines), Views/Home/Index.cshtml (hero + Quick Access only), wwwroot/css/home.css (hero rules only).
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-homepage-view-controller-redesign/149-CONTEXT.md
@.planning/phases/149-homepage-view-controller-redesign/149-RESEARCH.md

<interfaces>
<!-- Current DashboardHomeViewModel (Models/DashboardHomeViewModel.cs) -->
<!-- KEEP: CurrentUser, Greeting -->
<!-- REMOVE: IdpTotalCount, IdpCompletedCount, IdpProgressPercentage, PendingAssessmentCount, HasUrgentAssessments, MandatoryTrainingStatus, RecentActivities, UpcomingDeadlines -->
<!-- REMOVE TYPES: TrainingStatusInfo, RecentActivityItem, DeadlineItem -->

<!-- Target ViewModel after cleanup: -->
```csharp
public class DashboardHomeViewModel
{
    public ApplicationUser CurrentUser { get; set; } = null!;
    public string Greeting { get; set; } = string.Empty;
}
```

<!-- Current HomeController.Index() at lines 23-78: -->
<!-- Fetches user, roles, runs 6 DB queries (IdpItems x2, AssessmentSessions x2, GetMandatoryTrainingStatus, GetRecentActivities, GetUpcomingDeadlines) -->
<!-- KEEP methods: GetTimeBasedGreeting(), Guide(), GuideDetail(), Error() -->
<!-- REMOVE methods: GetMandatoryTrainingStatus(), GetRecentActivities(), GetUpcomingDeadlines(), GetTimeAgo() -->

<!-- Target controller Index() after cleanup: -->
```csharp
public async Task<IActionResult> Index()
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();

    var viewModel = new DashboardHomeViewModel
    {
        CurrentUser = user,
        Greeting = GetTimeBasedGreeting()
    };

    return View(viewModel);
}
```

<!-- Quick Access Bootstrap pattern (from Views/CMP/Index.cshtml): -->
```html
<a asp-controller="CDP" asp-action="Index" class="card border-0 shadow-sm h-100 text-decoration-none">
    <div class="card-body d-flex flex-column align-items-center justify-content-center">
        <div class="bg-primary bg-opacity-10 text-primary rounded-3 p-3 mb-3">
            <i class="bi bi-calendar-check fs-3"></i>
        </div>
        <h6 class="text-center mb-0 text-dark">CDP</h6>
    </div>
</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify ViewModel and Controller</name>
  <files>Models/DashboardHomeViewModel.cs, Controllers/HomeController.cs</files>
  <action>
**Models/DashboardHomeViewModel.cs** — Rewrite the entire file to contain only the two properties still used by the view. Remove the class body of DashboardHomeViewModel down to just CurrentUser and Greeting. Delete the TrainingStatusInfo, RecentActivityItem, and DeadlineItem class definitions entirely. The resulting file should be ~10 lines.

**Controllers/HomeController.cs** — Simplify the Index() action and remove unused helper methods:

1. Remove from the constructor: `_context` is no longer needed if Index() is the only action using it. HOWEVER, keep `_context` field and DI injection intact because Guide/GuideDetail may be extended later — actually check: Guide() and GuideDetail() do NOT use _context. Safe to remove `_context` field and `ApplicationDbContext` parameter from constructor, and remove `using HcPortal.Data;` if no other action uses it. Verify by grepping the file first before removing.

2. Replace Index() body with the simplified version:
```csharp
public async Task<IActionResult> Index()
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();

    var viewModel = new DashboardHomeViewModel
    {
        CurrentUser = user,
        Greeting = GetTimeBasedGreeting()
    };

    return View(viewModel);
}
```

3. Delete these private methods entirely (lines ~119-314 in current file):
   - GetMandatoryTrainingStatus() (lines 119-150)
   - GetRecentActivities() (lines 152-210)
   - GetUpcomingDeadlines() (lines 212-286)
   - GetTimeAgo() (lines 288-314)

4. Keep unchanged: Guide(), GuideDetail(), GetTimeBasedGreeting(), Error()

After editing, verify: no compile-time references to removed properties remain. The controller should be ~50 lines.

**Pitfall guard:** Check that no assignment like `IdpTotalCount =` or `RecentActivities =` remains in the file after removal. Also verify that `using Microsoft.EntityFrameworkCore;` is removed if _context is removed (it's only used for EF extension methods like CountAsync, AnyAsync).
  </action>
  <verify>
    <automated>grep -c "IdpTotalCount\|RecentActivities\|UpcomingDeadlines\|TrainingStatusInfo\|RecentActivityItem\|DeadlineItem\|GetRecentActivities\|GetUpcomingDeadlines\|GetMandatoryTrainingStatus\|GetTimeAgo" "C:/Users/Administrator/Desktop/PortalHC_KPB/Models/DashboardHomeViewModel.cs" "C:/Users/Administrator/Desktop/PortalHC_KPB/Controllers/HomeController.cs"</automated>
  </verify>
  <done>Both files contain zero references to removed properties and methods. DashboardHomeViewModel.cs has only CurrentUser and Greeting. HomeController.cs Index() action contains only user fetch + ViewModel construction + return View.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite View and strip unused CSS</name>
  <files>Views/Home/Index.cshtml, wwwroot/css/home.css</files>
  <action>
**Views/Home/Index.cshtml** — Rewrite the entire file. The new view has two sections only: hero and Quick Access. Structure:

```html
@model DashboardHomeViewModel
@using System.Globalization

@{
    ViewData["Title"] = "Home";
}

<div class="hero-section mb-4">
    <div class="hero-content">
        <div class="row align-items-center">
            <div class="col-md-9">
                <div class="d-flex align-items-center gap-4">
                    <div class="hero-avatar">
                        <i class="fas fa-user"></i>
                    </div>
                    <div>
                        <h1 class="hero-greeting mb-2">
                            @Model.Greeting, @Model.CurrentUser.FullName!
                        </h1>
                        <p class="hero-subtitle mb-0">
                            <span class="hero-badge">
                                <i class="fas fa-briefcase"></i>
                                @(Model.CurrentUser.Position ?? "Staff")
                            </span>
                            <span class="hero-badge ms-2">
                                <i class="fas fa-building"></i>
                                @(Model.CurrentUser.Unit ?? "N/A")
                            </span>
                        </p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 text-end">
                <p class="mb-0 fs-5 fw-semibold">@DateTime.Now.ToString("dddd", CultureInfo.GetCultureInfo("id-ID"))</p>
                <p class="mb-0 fs-6 opacity-75">@DateTime.Now.ToString("dd MMMM yyyy", CultureInfo.GetCultureInfo("id-ID"))</p>
            </div>
        </div>
    </div>
</div>

<div class="row g-4">
    <div class="col-md-4 col-6">
        <a asp-controller="CDP" asp-action="Index" class="card border-0 shadow-sm h-100 text-decoration-none">
            <div class="card-body d-flex flex-column align-items-center justify-content-center py-4">
                <div class="bg-primary bg-opacity-10 text-primary rounded-3 p-3 mb-3">
                    <i class="bi bi-calendar-check fs-3"></i>
                </div>
                <h6 class="text-center mb-0 text-dark">CDP</h6>
            </div>
        </a>
    </div>
    <div class="col-md-4 col-6">
        <a asp-controller="CMP" asp-action="Assessment" class="card border-0 shadow-sm h-100 text-decoration-none">
            <div class="card-body d-flex flex-column align-items-center justify-content-center py-4">
                <div class="bg-success bg-opacity-10 text-success rounded-3 p-3 mb-3">
                    <i class="bi bi-clipboard-check fs-3"></i>
                </div>
                <h6 class="text-center mb-0 text-dark">Assessment</h6>
            </div>
        </a>
    </div>
    <div class="col-md-4 col-6">
        <a asp-controller="CMP" asp-action="Index" class="card border-0 shadow-sm h-100 text-decoration-none">
            <div class="card-body d-flex flex-column align-items-center justify-content-center py-4">
                <div class="bg-info bg-opacity-10 text-info rounded-3 p-3 mb-3">
                    <i class="bi bi-book fs-3"></i>
                </div>
                <h6 class="text-center mb-0 text-dark">CMP</h6>
            </div>
        </a>
    </div>
</div>
```

Use Bootstrap Icons (bi bi-*) for Quick Access cards to match CMP/CDP Index pages. Keep Font Awesome (fas fa-*) for hero avatar and badges since those are already in the hero markup and consistent with the existing hero styling.

**wwwroot/css/home.css** — Remove unused CSS rule blocks. The current file (after Phase 148) is ~467 lines. Target: keep only hero rules and minimal quick-access responsive rules.

Rules to REMOVE entirely (delete the entire block including comment):
1. `.circular-progress` and related (`.circular-progress-text`, `.circle-bg`, `.circle-progress`) — lines ~106-143 — these were for the IDP card circular progress chart
2. `.section-header`, `.section-icon` — lines ~197-220 — these were for Timeline/Deadlines section headers
3. `.gradient-text` — lines ~225-229 — used only by Pending Assessment count display
4. `.badge-gradient` — lines ~232-236 — glassmorphism badge, no longer used
5. `.quick-access-card`, `.quick-access-icon`, `.quick-access-title` — lines ~148-191 — replaced by Bootstrap card classes

Rules to KEEP (hero section, responsive):
- `.hero-section` base rule (gradient background, padding, border-radius, box-shadow)
- `.hero-content`, `.hero-avatar`, `.hero-greeting`, `.hero-subtitle`, `.hero-badge`
- `@media (max-width: 768px)` responsive rules for `.hero-section` and `.hero-greeting`

After removing, the file should be approximately 80-100 lines (down from ~467).

**Pitfall guard:** Do NOT remove `.hero-section` base styles — they define the purple gradient background that makes the hero section work. Only remove the named rule blocks listed above.
  </action>
  <verify>
    <automated>grep -c "glass-card\|IdpTotalCount\|PendingAssessmentCount\|RecentActivities\|UpcomingDeadlines\|timeline-item\|deadline-card\|circular-progress\|gradient-text\|section-header" "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/Home/Index.cshtml" "C:/Users/Administrator/Desktop/PortalHC_KPB/wwwroot/css/home.css"</automated>
  </verify>
  <done>Views/Home/Index.cshtml contains only hero section and 3 Quick Access cards (zero references to removed sections). wwwroot/css/home.css contains zero references to circular-progress, gradient-text, section-header, or deadline-related rules. Page builds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of redesigned homepage</name>
  <what-built>Complete homepage redesign: simplified ViewModel (2 props), simplified Controller (1 query), rewritten View (hero + 3 Quick Access cards), stripped CSS (hero rules only).</what-built>
  <how-to-verify>
    1. Run the app (dotnet run or F5 in IDE)
    2. Navigate to Home/Index (the homepage after login)
    3. Verify hero section: gradient purple background displays, greeting text shows (e.g. "Selamat Pagi, [Name]!"), position badge and unit badge appear, date and day name display on the right
    4. Verify Quick Access: three cards display — CDP, Assessment, CMP — each with a Bootstrap icon and label. Cards have matching shadow-sm style (no colorful gradient backgrounds, no subtitle/description text)
    5. Verify removals: no IDP Status card, no Pending Assessment card, no Mandatory Training card, no Recent Activity section, no Upcoming Deadlines section
    6. Navigate to CMP/Index and CDP/Index — confirm those pages still load and their card styling matches the Quick Access cards on the homepage
    7. Check browser console — no JavaScript errors
  </how-to-verify>
  <action>Run the application and navigate to Home/Index. Verify the redesigned homepage visually against the criteria in how-to-verify.</action>
  <files>Views/Home/Index.cshtml</files>
  <verify>Human visual inspection per how-to-verify steps above</verify>
  <done>User confirms hero + Quick Access display correctly, no removed sections visible, CMP/CDP pages unaffected</done>
  <resume-signal>Type "approved" if homepage displays correctly, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
# Verify no removed symbols remain in any modified file
grep -rn "IdpTotalCount\|RecentActivities\|UpcomingDeadlines\|glass-card\|timeline-item\|deadline-card\|GetRecentActivities\|GetUpcomingDeadlines\|GetMandatoryTrainingStatus\|TrainingStatusInfo\|RecentActivityItem\|DeadlineItem" \
  "C:/Users/Administrator/Desktop/PortalHC_KPB/Models/DashboardHomeViewModel.cs" \
  "C:/Users/Administrator/Desktop/PortalHC_KPB/Controllers/HomeController.cs" \
  "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/Home/Index.cshtml"

# Should return 0 results

# Verify Quick Access uses Bootstrap card pattern
grep -c "card border-0 shadow-sm" "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/Home/Index.cshtml"
# Should return 3 (one per card)

# Verify hero still in view
grep -c "hero-section\|hero-greeting" "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/Home/Index.cshtml"
# Should be > 0
```
</verification>

<success_criteria>
1. Homepage displays hero section (purple gradient, greeting, name, position, unit, date) and exactly three Quick Access cards (CDP, Assessment, CMP) — nothing else
2. Quick Access cards use `card border-0 shadow-sm h-100` Bootstrap pattern with `bg-{color} bg-opacity-10` icon boxes — matching CMP/CDP Index styling
3. HomeController.Index() contains only: user fetch, ViewModel construction with CurrentUser + Greeting, return View
4. DashboardHomeViewModel contains only CurrentUser and Greeting properties
5. home.css contains zero references to circular-progress, gradient-text, badge-gradient, section-header
6. grep across all four modified files returns 0 hits for: IdpTotalCount, RecentActivities, UpcomingDeadlines, glass-card, timeline-item, deadline-card
7. CMP/Index, CDP/Index, Admin pages all continue to load without errors
</success_criteria>

<output>
After completion, create `.planning/phases/149-homepage-view-controller-redesign/149-01-SUMMARY.md` with:
- What was implemented
- Files modified and key changes per file
- Line count delta (before → after) for each file
- Any discretion decisions made (icon colors, CSS kept/removed)
- Verification results (grep counts)
</output>
