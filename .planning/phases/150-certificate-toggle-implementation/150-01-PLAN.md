---
phase: 150-certificate-toggle-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentSession.cs
  - Models/AssessmentResultsViewModel.cs
  - Controllers/AdminController.cs
  - Controllers/CMPController.cs
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/EditAssessment.cshtml
  - Views/CMP/Results.cshtml
autonomous: true
requirements: [CERT-01, CERT-02, CERT-03, CERT-04, CERT-05]

must_haves:
  truths:
    - "AssessmentSession has GenerateCertificate bool field defaulting to false for new assessments"
    - "Existing assessments via migration have GenerateCertificate = true"
    - "CreateAssessment form shows 'Terbitkan Sertifikat' toggle (default OFF)"
    - "EditAssessment form shows 'Terbitkan Sertifikat' toggle pre-filled with current value"
    - "Results page 'View Certificate' button only shows when GenerateCertificate AND IsPassed are both true"
    - "Certificate action returns NotFound when GenerateCertificate is false"
  artifacts:
    - path: "Models/AssessmentSession.cs"
      provides: "GenerateCertificate bool property"
      contains: "public bool GenerateCertificate"
    - path: "Models/AssessmentResultsViewModel.cs"
      provides: "GenerateCertificate passed to Results view"
      contains: "public bool GenerateCertificate"
    - path: "Controllers/AdminController.cs"
      provides: "Bind + persist GenerateCertificate in Create/Edit"
    - path: "Controllers/CMPController.cs"
      provides: "Certificate action guard + Results ViewModel population"
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "Terbitkan Sertifikat toggle UI"
    - path: "Views/Admin/EditAssessment.cshtml"
      provides: "Terbitkan Sertifikat toggle UI (pre-filled)"
    - path: "Views/CMP/Results.cshtml"
      provides: "Conditional View Certificate button"
  key_links:
    - from: "Views/Admin/CreateAssessment.cshtml"
      to: "Controllers/AdminController.cs (CreateAssessment POST)"
      via: "asp-for binding of GenerateCertificate"
      pattern: "asp-for=\"GenerateCertificate\""
    - from: "Controllers/CMPController.cs (Results)"
      to: "Models/AssessmentResultsViewModel.cs"
      via: "GenerateCertificate = assessment.GenerateCertificate"
      pattern: "GenerateCertificate = assessment.GenerateCertificate"
    - from: "Controllers/CMPController.cs (Certificate)"
      to: "AssessmentSession.GenerateCertificate"
      via: "NotFound guard when false"
      pattern: "if (!assessment.GenerateCertificate)"
---

<objective>
Add a GenerateCertificate boolean toggle to AssessmentSession so HC can control whether an assessment produces certificates. Results page button and Certificate action both respect this flag. Existing assessments default to true (backward compatible); new assessments default to false.

Purpose: Prevent "View Certificate" from appearing on trainings/assessments that do not require certificates.
Output: Migration, updated model, toggle in Create/Edit forms, guarded Results button and Certificate action.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-certificate-toggle-implementation/150-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Models/AssessmentSession.cs (existing config fields — add GenerateCertificate alongside these):
```csharp
public int PassPercentage { get; set; } = 70;
public bool AllowAnswerReview { get; set; } = true;
public DateTime? ExamWindowCloseDate { get; set; }
// ADD HERE:
// public bool GenerateCertificate { get; set; } = false;
```

From Models/AssessmentResultsViewModel.cs (add field alongside AllowAnswerReview):
```csharp
public bool IsPassed { get; set; }
public bool AllowAnswerReview { get; set; }
// ADD:
// public bool GenerateCertificate { get; set; }
```

From Views/Admin/CreateAssessment.cshtml (existing toggle pattern at lines 292-304 to replicate exactly):
```html
<!-- Allow Answer Review Toggle -->
<div class="col-md-6 mb-3">
    <label class="form-label fw-semibold">Allow Answer Review</label>
    <div class="form-check form-switch">
        <input asp-for="AllowAnswerReview" class="form-check-input" type="checkbox" id="AllowAnswerReview">
        <label class="form-check-label" for="AllowAnswerReview">Enable answer review</label>
    </div>
    <div class="form-text text-muted">When enabled, users can see which questions they answered correctly after completing the assessment</div>
</div>
```

From Controllers/AdminController.cs (existing pattern for persisting config fields in CreateAssessment POST ~line 914):
```csharp
PassPercentage = model.PassPercentage,
AllowAnswerReview = model.AllowAnswerReview,
ExamWindowCloseDate = model.ExamWindowCloseDate,
// ADD: GenerateCertificate = model.GenerateCertificate,
```

From Controllers/CMPController.cs — Certificate action (lines 1755-1782) existing guard pattern:
```csharp
// Only generate if Completed
if (assessment.Status != "Completed")
{
    TempData["Error"] = "Assessment not completed yet.";
    return RedirectToAction("Assessment");
}
// ADD BEFORE return View(assessment):
// if (!assessment.GenerateCertificate) return NotFound();
```

From Views/CMP/Results.cshtml (lines 324-329) — existing certificate button condition:
```html
@if (Model.IsPassed)
{
    <a asp-action="Certificate" asp-route-id="@Model.AssessmentId" class="btn btn-primary" target="_blank">
        <i class="bi bi-award me-1"></i>View Certificate
    </a>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GenerateCertificate to model and create migration</name>
  <files>Models/AssessmentSession.cs, Models/AssessmentResultsViewModel.cs</files>
  <action>
In Models/AssessmentSession.cs, add the GenerateCertificate property alongside AllowAnswerReview:

```csharp
[Display(Name = "Terbitkan Sertifikat")]
public bool GenerateCertificate { get; set; } = false;
```

Place it immediately after the AllowAnswerReview property (line ~33).

In Models/AssessmentResultsViewModel.cs, add to the class (after AllowAnswerReview):
```csharp
public bool GenerateCertificate { get; set; }
```

Then run the EF Core migration:
```
dotnet ef migrations add AddGenerateCertificateToAssessmentSession
```

Open the generated migration file and verify the Up() method adds the column with a defaultValue of true (for backward compatibility of existing rows):

```csharp
migrationBuilder.AddColumn<bool>(
    name: "GenerateCertificate",
    table: "AssessmentSessions",
    type: "INTEGER",
    nullable: false,
    defaultValue: true);  // existing rows default TRUE (backward compatible)
```

If the scaffolded default is false, manually change it to true in the migration file. The model default (false) is only for C# object instantiation; the SQL default (true) ensures existing rows pass the guard.

Run: `dotnet build` — must compile cleanly before proceeding.
  </action>
  <verify>
    <automated>dotnet build /c/Users/Administrator/Desktop/PortalHC_KPB/PortalHC_KPB.csproj</automated>
  </verify>
  <done>Build succeeds; migration file exists with defaultValue: true; model and ViewModel both have GenerateCertificate property.</done>
</task>

<task type="auto">
  <name>Task 2: Bind GenerateCertificate in AdminController Create/Edit and guard CMPController Certificate action</name>
  <files>Controllers/AdminController.cs, Controllers/CMPController.cs</files>
  <action>
**AdminController.cs — CreateAssessment POST (~line 914):**

In the section where AssessmentSession objects are populated from model, add GenerateCertificate binding alongside AllowAnswerReview:
```csharp
GenerateCertificate = model.GenerateCertificate,
```

Also check for any sibling session creation loop (around line 1169-1255) that copies fields to sibling sessions — add the same binding there:
```csharp
sibling.GenerateCertificate = model.GenerateCertificate;
```

And in the "saved assessment to viewmodel" mapping (~line 1253):
```csharp
GenerateCertificate = savedAssessment.GenerateCertificate,
```

**AdminController.cs — EditAssessment POST (around line 1169-1171):**
Ensure GenerateCertificate is updated when HC saves edits:
```csharp
sibling.GenerateCertificate = model.GenerateCertificate;
```
Search for where AllowAnswerReview is assigned on the edit path — add the same pattern for GenerateCertificate.

**CMPController.cs — Certificate action (~line 1778, after the Status "Completed" check):**
Add the GenerateCertificate guard immediately before `return View(assessment)`:
```csharp
// Guard: certificate generation disabled for this assessment
if (!assessment.GenerateCertificate)
    return NotFound();
```

**CMPController.cs — Results action:**
Find where AssessmentResultsViewModel is constructed (after line 1808). Add:
```csharp
GenerateCertificate = assessment.GenerateCertificate,
```
alongside the other fields mapped from assessment.

Run: `dotnet build` — must compile cleanly.
  </action>
  <verify>
    <automated>dotnet build /c/Users/Administrator/Desktop/PortalHC_KPB/PortalHC_KPB.csproj</automated>
  </verify>
  <done>Build succeeds; Certificate action returns NotFound when GenerateCertificate=false; Results ViewModel carries GenerateCertificate flag.</done>
</task>

<task type="auto">
  <name>Task 3: Add toggle UI to Create/Edit forms and conditional button in Results view</name>
  <files>Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml, Views/CMP/Results.cshtml</files>
  <action>
**Views/Admin/CreateAssessment.cshtml:**

Locate the row containing PassPercentage and AllowAnswerReview (around line 280-305). After that row, insert a new row with two columns: GenerateCertificate toggle on the left, ExamWindowCloseDate on the right. Currently ExamWindowCloseDate sits in its own solo row — move it into this new two-column row:

```html
<!-- Terbitkan Sertifikat & Tanggal Tutup Ujian -->
<div class="row mb-3">
    <div class="col-md-6 mb-3">
        <label class="form-label fw-semibold">Terbitkan Sertifikat</label>
        <div class="form-check form-switch">
            <input asp-for="GenerateCertificate" class="form-check-input" type="checkbox" id="GenerateCertificate">
            <label class="form-check-label" for="GenerateCertificate">Aktifkan</label>
        </div>
        <div class="form-text text-muted">Peserta yang lulus dapat melihat dan mencetak sertifikat</div>
    </div>
    <div class="col-md-6">
        <label asp-for="ExamWindowCloseDate" class="form-label fw-semibold">
            <i class="bi bi-calendar-x text-danger me-1"></i>
            Tanggal Tutup Ujian (Opsional)
        </label>
        <input asp-for="ExamWindowCloseDate" type="date" class="form-control" />
        <div class="form-text text-muted">
            Jika diisi, peserta tidak dapat memulai ujian setelah tanggal ini.
            Kosongkan jika tidak ada batas waktu.
        </div>
    </div>
</div>
```

Remove the now-redundant standalone ExamWindowCloseDate row that was previously a solo col-md-6 row.

Note: GenerateCertificate defaults to false in the model, so the toggle will be OFF by default for new assessments (correct per user decision).

**Views/Admin/EditAssessment.cshtml:**

Apply the same two-column row pattern (same HTML structure). The `asp-for="GenerateCertificate"` binding will pre-fill the checkbox from the existing session value. Find the PassPercentage/AllowAnswerReview section in EditAssessment and insert this row in the same relative position as in CreateAssessment.

**Views/CMP/Results.cshtml (lines 324-329):**

Change the existing certificate button condition from:
```html
@if (Model.IsPassed)
```
to:
```html
@if (Model.IsPassed && Model.GenerateCertificate)
```

The button content and styling remain unchanged.

Run: `dotnet build` — must compile cleanly.
  </action>
  <verify>
    <automated>dotnet build /c/Users/Administrator/Desktop/PortalHC_KPB/PortalHC_KPB.csproj</automated>
  </verify>
  <done>Build succeeds; CreateAssessment and EditAssessment show the toggle; Results page conditionally renders View Certificate button only when both IsPassed and GenerateCertificate are true.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `dotnet ef database update` — applies migration, existing rows get GenerateCertificate = true
2. Run the app and log in as HC
3. Create a new assessment — toggle is OFF by default
4. Enable the toggle, save — assessment has GenerateCertificate = true
5. As a worker, complete an assessment with toggle ON and pass — "View Certificate" button appears in Results
6. Edit assessment, toggle OFF — "View Certificate" disappears from Results; Certificate URL returns 404
7. Create assessment with toggle OFF — no certificate button in Results regardless of pass status
</verification>

<success_criteria>
- AssessmentSessions table has GenerateCertificate column (default 1 for existing rows)
- New assessments default to GenerateCertificate = false (toggle OFF in form)
- EditAssessment form shows current value of GenerateCertificate
- Results page only shows "View Certificate" when Model.IsPassed AND Model.GenerateCertificate
- Certificate action returns NotFound when GenerateCertificate = false
- dotnet build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/150-certificate-toggle-implementation/150-01-SUMMARY.md`
</output>
