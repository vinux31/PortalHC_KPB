---
phase: 151-homepage-progress-overview-and-upcoming-events-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/HomeController.cs
  - Models/DashboardHomeViewModel.cs
  - Views/Home/Index.cshtml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Upcoming Events only shows coaching sessions scheduled for today or tomorrow"
    - "Upcoming Events only shows assessments scheduled for today or tomorrow"
    - "Progress Overview shows a percentage progress bar for Coaching Sessions (consistent with CDP and Assessment bars)"
  artifacts:
    - path: "Controllers/HomeController.cs"
      provides: "Fixed GetUpcomingEvents with today/tomorrow date filter for both coaching and assessment queries"
    - path: "Models/DashboardHomeViewModel.cs"
      provides: "CoachingProgress property on ProgressViewModel"
    - path: "Views/Home/Index.cshtml"
      provides: "Coaching section renders progress bar using CoachingProgress percentage"
  key_links:
    - from: "Controllers/HomeController.cs"
      to: "Models/DashboardHomeViewModel.cs"
      via: "CoachingProgress assigned in GetProgress"
      pattern: "CoachingProgress"
    - from: "Views/Home/Index.cshtml"
      to: "Model.Progress.CoachingProgress"
      via: "progress-bar width style binding"
      pattern: "CoachingProgress"
---

<objective>
Fix two bugs in the homepage dashboard:

1. Upcoming Events shows all future events — must be restricted to today and tomorrow only.
2. Progress Overview Coaching section shows only a badge count with no progress bar — must add a percentage bar consistent with CDP and Assessment sections.

Purpose: Dashboard should surface only immediately relevant events, and all three progress metrics should display consistently.
Output: HomeController with corrected date filters, ProgressViewModel with CoachingProgress, updated view.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Upcoming Events date filter and add CoachingProgress to model</name>
  <files>Controllers/HomeController.cs, Models/DashboardHomeViewModel.cs</files>
  <action>
In Controllers/HomeController.cs, fix GetUpcomingEvents:

- Coaching filter: change `c.Date > DateTime.Today` to restrict to today and tomorrow only:
  ```csharp
  var tomorrow = DateTime.Today.AddDays(2).AddTicks(-1); // end of tomorrow
  var today = DateTime.Today;
  ```
  Then: `.Where(c => c.CoacheeId == userId && c.Date >= today && c.Date <= tomorrow)`

- Assessment filter: add the same date window restriction on Schedule:
  ```csharp
  .Where(a => a.UserId == userId &&
         (a.Status == "Open" || a.Status == "Upcoming") &&
         (a.ExamWindowCloseDate == null || a.ExamWindowCloseDate > DateTime.Now) &&
         a.Schedule >= today && a.Schedule <= tomorrow)
  ```
  Remove the `.Take(3)` on each individual query (already limited by the date window), keep the final `.OrderBy(e => e.Date).Take(5).ToList()`.

In GetProgress method, compute CoachingProgress after the coaching query:
  ```csharp
  progress.CoachingProgress = progress.CoachingTotal > 0
      ? (int)Math.Round((double)progress.CoachingCompleted / progress.CoachingTotal * 100)
      : 0;
  ```

In Models/DashboardHomeViewModel.cs, add to ProgressViewModel:
  ```csharp
  public int CoachingProgress { get; set; }
  ```
  Place it after CoachingCompleted.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds. GetUpcomingEvents filters both coaching and assessment to today+tomorrow only. ProgressViewModel has CoachingProgress field populated in controller.</done>
</task>

<task type="auto">
  <name>Task 2: Update homepage view to show Coaching progress bar</name>
  <files>Views/Home/Index.cshtml</files>
  <action>
In Views/Home/Index.cshtml, replace the Coaching Sessions progress-item section (currently lines 108-118) with a version that includes a progress bar matching the CDP and Assessment sections:

```html
<div class="progress-item">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <span class="text-muted small">Coaching Sessions</span>
        <span class="fw-bold text-warning">@Model.Progress.CoachingProgress%</span>
    </div>
    <div class="progress mb-2" style="height: 8px;">
        <div class="progress-bar bg-warning" role="progressbar" style="width: @Model.Progress.CoachingProgress%" aria-valuenow="@Model.Progress.CoachingProgress" aria-valuemin="0" aria-valuemax="100"></div>
    </div>
    <small class="text-muted">@Model.Progress.CoachingCompleted / @Model.Progress.CoachingTotal submitted</small>
</div>
```

Remove the badge span — the progress bar and fraction text provide sufficient information.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds. Coaching Sessions section in Progress Overview has a yellow progress bar and percentage, matching the layout of CDP Deliverables and Assessment sections.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `dotnet build` passes with no errors.
2. Review GetUpcomingEvents: both coaching and assessment Where clauses include `>= DateTime.Today && <= DateTime.Today.AddDays(2).AddTicks(-1)`.
3. Review GetProgress: CoachingProgress is computed and assigned.
4. Review Index.cshtml: Coaching section has `<div class="progress">` with `bg-warning` bar and `@Model.Progress.CoachingProgress%` text.
</verification>

<success_criteria>
- Upcoming Events shows zero events when no coaching/assessment is scheduled for today or tomorrow.
- Upcoming Events shows today's and tomorrow's events correctly.
- Progress Overview renders three consistent progress bars (blue CDP, green Assessment, yellow Coaching).
- Build is clean with no warnings introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/151-homepage-progress-overview-and-upcoming-events-fix/151-01-SUMMARY.md`
</output>
