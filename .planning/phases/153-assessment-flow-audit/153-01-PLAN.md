---
phase: 153-assessment-flow-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ASSESS-01, ASSESS-02]

must_haves:
  truths:
    - "HC can create assessment with all fields and no validation gaps"
    - "HC can import questions via Excel template without crashes on edge cases"
  artifacts:
    - path: ".planning/phases/153-assessment-flow-audit/153-01-AUDIT-REPORT.md"
      provides: "Audit findings for assessment creation and question import"
  key_links:
    - from: "Controllers/AdminController.cs (CreateAssessment)"
      to: "Views/Admin/CreateAssessment.cshtml"
      via: "Form POST with model binding"
    - from: "Controllers/AdminController.cs (ImportPackageQuestions)"
      to: "Excel template parsing"
      via: "EPPlus/ClosedXML file read"
---

<objective>
Audit HC assessment creation and question import flows for bugs, validation gaps, and edge cases.

Purpose: Ensure HC can create assessments with all fields and import questions without crashes.
Output: Audit report with findings, plus bug fixes if issues found.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Assessment creation and question import</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManageAssessment.cshtml, Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml, Views/Admin/ManageQuestions.cshtml, Views/Admin/ManagePackages.cshtml</files>
  <action>
Read and analyze the following action methods in AdminController.cs for bugs, validation gaps, security issues, and edge cases:

**ASSESS-01 (Assessment Creation):**
- `CreateAssessment()` GET (line ~697) and POST (line ~733) — check model validation, required fields (title, category, schedule, duration, pass%, packages, users, token, certificate toggle), null handling, duplicate prevention
- `EditAssessment()` GET (line ~1049) and POST (line ~1114) — check same validation as create, plus concurrent edit issues
- `DeleteAssessment()` (line ~1323) and `DeleteAssessmentGroup()` (line ~1429) — check cascade safety, what happens if exam in progress
- `ManageAssessment()` (line ~575) — check filtering, pagination, role authorization

**ASSESS-02 (Question Import):**
- `ManageQuestions()` (line ~4818) — check question listing
- `AddQuestion()` (line ~4833) — check validation (empty text, no options, duplicate)
- `DeleteQuestion()` (line ~4896) — check cascade to exam answers
- `ManagePackages()` (line ~4934), `CreatePackage()` (line ~4965), `DeletePackage()` (line ~4995) — check package lifecycle
- `DownloadQuestionTemplate()` (line ~5072) — check template format
- `ImportPackageQuestions()` POST (line ~5141) — check edge cases: empty rows, duplicate questions, wrong column count, empty file, too many questions, special characters

Also read the corresponding Views to check for client-side validation gaps, missing CSRF tokens, and UI issues.

**Produce audit report** at `.planning/phases/153-assessment-flow-audit/153-01-AUDIT-REPORT.md` with:
- Section per requirement (ASSESS-01, ASSESS-02)
- Each finding: severity (bug/edge-case/security/cosmetic), description, file:line, suggested fix
- Summary: pass/fail per requirement, total findings count

If any bugs with severity "bug" or "security" are found, fix them immediately and list fixes in the report.
  </action>
  <verify>
    <automated>test -f .planning/phases/153-assessment-flow-audit/153-01-AUDIT-REPORT.md && grep -c "ASSESS-01\|ASSESS-02" .planning/phases/153-assessment-flow-audit/153-01-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report exists covering ASSESS-01 and ASSESS-02 with findings documented per requirement. Any bugs/security issues are fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review audit of assessment creation (ASSESS-01) and question import (ASSESS-02) flows</what-built>
  <how-to-verify>
    1. Read the audit report at `.planning/phases/153-assessment-flow-audit/153-01-AUDIT-REPORT.md`
    2. For each finding marked "bug" or "security", verify in browser:
       - Go to Admin > ManageAssessment
       - Try creating an assessment — check all field validations
       - Try importing questions with edge cases (empty file, wrong format)
    3. Confirm fixes (if any) work correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found during verification</resume-signal>
</task>

</tasks>

<verification>
- Audit report covers both ASSESS-01 and ASSESS-02
- Every action method in scope has been reviewed
- Bug/security findings have been fixed
- User has verified findings in browser
</verification>

<success_criteria>
- Audit report exists with structured findings
- No unfixed bugs or security issues remain
- User has approved the audit findings
</success_criteria>

<output>
After completion, create `.planning/phases/153-assessment-flow-audit/153-01-SUMMARY.md`
</output>
