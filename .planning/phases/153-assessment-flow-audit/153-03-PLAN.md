---
phase: 153-assessment-flow-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ASSESS-06, ASSESS-07, ASSESS-08]

must_haves:
  truths:
    - "Worker can download certificate only when GenerateCertificate=true and IsPassed=true"
    - "HC can monitor live exam, reset, force-close, and regenerate token"
    - "Assessment completion creates TrainingRecord and updates competency level"
  artifacts:
    - path: ".planning/phases/153-assessment-flow-audit/153-03-AUDIT-REPORT.md"
      provides: "Audit findings for certificate, monitoring, and training record creation"
  key_links:
    - from: "Controllers/CMPController.cs (Certificate)"
      to: "Views/CMP/Certificate.cshtml"
      via: "Certificate generation with pass+toggle check"
    - from: "Controllers/AdminController.cs (AssessmentMonitoring)"
      to: "Views/Admin/AssessmentMonitoring.cshtml"
      via: "Live monitoring with SignalR or polling"
    - from: "Controllers/CMPController.cs (SubmitExam)"
      to: "TrainingRecord creation"
      via: "Post-submit DB write"
---

<objective>
Audit certificate download, HC monitoring/admin actions, and post-assessment TrainingRecord creation.

Purpose: Ensure certificate access control, HC monitoring tools work, and assessment results flow into training records.
Output: Audit report with findings, plus bug fixes if issues found.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Certificate, monitoring, and training records</name>
  <files>Controllers/CMPController.cs, Controllers/AdminController.cs, Views/CMP/Certificate.cshtml, Views/Admin/AssessmentMonitoring.cshtml, Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <action>
Read and analyze the following action methods:

**ASSESS-06 (Certificate):**
- `Certificate()` in CMPController.cs (line ~1756) — check: does it verify both GenerateCertificate=true AND IsPassed=true? Can worker access certificate for failed exam? Can worker A download worker B's certificate? What if certificate data is null?
- Check Views/CMP/Certificate.cshtml for display correctness

**ASSESS-07 (HC Monitoring & Admin Actions):**
- `AssessmentMonitoring()` in AdminController.cs (line ~1588) — check grouping, filtering, role authorization
- `AssessmentMonitoringDetail()` (line ~1688) — check per-participant data, live progress display
- `GetMonitoringProgress()` (line ~1916) — check real-time data accuracy
- `ResetAssessment()` (line ~2013) — check: what gets reset (answers? timer? status?), can it reset a non-started exam? Race condition with active exam?
- `ForceCloseAssessment()` (line ~2111) — check: does it properly score partial answers? Does worker see correct status after force-close?
- `RegenerateToken()` (line ~1530) — check: does old token become invalid? What if exam in progress?
- `ReshufflePackage()` (line ~2613) — check: when is reshuffle allowed?
- `ExportAssessmentResults()` (line ~2208) — check data accuracy, role authorization

**ASSESS-08 (TrainingRecord Creation):**
- In `SubmitExam()` (CMPController.cs line ~1540) — find the code that creates TrainingRecord after submission. Check:
  - Is TrainingRecord always created on submit?
  - Does it handle the case where TrainingRecord already exists (re-submit)?
  - Does it update competency level correctly?
  - What if the assessment category doesn't match any competency?
- Also check if ForceClose creates TrainingRecord
- Check for PositionTargetHelper usage (known potential gap from STATE.md)

**Produce audit report** at `.planning/phases/153-assessment-flow-audit/153-03-AUDIT-REPORT.md` with:
- Section per requirement (ASSESS-06, ASSESS-07, ASSESS-08)
- Each finding: severity, description, file:line, suggested fix
- Special attention to PositionTargetHelper status (exists or missing?)
- Summary: pass/fail per requirement

If any bugs with severity "bug" or "security" are found, fix them immediately.
  </action>
  <verify>
    <automated>test -f .planning/phases/153-assessment-flow-audit/153-03-AUDIT-REPORT.md && grep -c "ASSESS-06\|ASSESS-07\|ASSESS-08" .planning/phases/153-assessment-flow-audit/153-03-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report exists covering ASSESS-06, ASSESS-07, ASSESS-08 with findings documented. PositionTargetHelper status clarified. Any bugs/security issues are fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review audit of certificate (ASSESS-06), monitoring (ASSESS-07), and training records (ASSESS-08)</what-built>
  <how-to-verify>
    1. Read the audit report at `.planning/phases/153-assessment-flow-audit/153-03-AUDIT-REPORT.md`
    2. As HC, verify in browser:
       - Go to Admin > Assessment Monitoring — check live data accuracy
       - Try Reset and Force-Close on a test assessment
       - Try Regenerate Token
    3. As Worker, verify:
       - Try accessing certificate for a passed exam with GenerateCertificate=true
       - Try accessing certificate for a failed exam (should be blocked)
    4. Check TrainingRecord table after exam submission
    5. Confirm fixes (if any) work correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found during verification</resume-signal>
</task>

</tasks>

<verification>
- Audit report covers ASSESS-06, ASSESS-07, ASSESS-08
- Certificate access control verified
- Monitoring actions reviewed for correctness and security
- TrainingRecord creation flow traced end-to-end
- PositionTargetHelper status documented
</verification>

<success_criteria>
- Audit report exists with structured findings
- No unfixed bugs or security issues remain
- User has approved the audit findings
</success_criteria>

<output>
After completion, create `.planning/phases/153-assessment-flow-audit/153-03-SUMMARY.md`
</output>
