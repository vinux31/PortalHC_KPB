---
phase: 154-coaching-proton-flow-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PROTON-01]

must_haves:
  truths:
    - "HC can create a coach-coachee mapping with correct section/unit assignment"
    - "Coachee sees assigned deliverables immediately after mapping is created"
    - "Multi-unit users are handled correctly in mapping and deliverable assignment"
  artifacts:
    - path: ".planning/phases/154-coaching-proton-flow-audit/154-01-AUDIT-REPORT.md"
      provides: "Audit findings for coach-coachee mapping flow (PROTON-01)"
  key_links:
    - from: "Controllers/AdminController.cs (CoachCoacheeMapping, CoachCoacheeMappingAssign)"
      to: "CoachCoacheeMappings table + ProtonTrackAssignments table"
      via: "POST JSON body → EF Core insert"
    - from: "Controllers/CDPController.cs (CoachingProton)"
      to: "ProtonDeliverableProgresses table"
      via: "coachee filtering by UserId"
---

<objective>
Audit the coach-coachee mapping setup flow (PROTON-01) for bugs, authorization gaps, and edge cases.

Purpose: Ensure HC can create mappings with correct section/unit assignment and that coachees immediately see their assigned deliverables.
Output: Audit report with findings, plus inline bug fixes for any bug/security severity issues found.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Coach-coachee mapping (PROTON-01)</name>
  <files>Controllers/AdminController.cs, Controllers/CDPController.cs, Views/Admin/CoachCoacheeMapping.cshtml</files>
  <action>
Read and analyze the coach-coachee mapping flow for bugs, authorization gaps, and edge cases.

**PROTON-01 (Coach-Coachee Mapping):**

In `AdminController.cs`:
- `CoachCoacheeMapping()` GET (~line 2883) — check role authorization (`[Authorize]` attribute or role check), filtering logic, null handling in query, correct join to coachee users
- `CoachCoacheeMappingAssign()` POST (~line 3006) — check: authorization (HC-only?), duplicate mapping prevention logic, whether `AssignmentSection` and `AssignmentUnit` are correctly stored, whether ProtonTrackAssignment is created alongside the mapping, what happens if coacheeId is invalid/not found, CSRF protection (FromBody vs AntiForgery)
- `CoachCoacheeMappingEdit()` POST (~line 3115) — check: same auth, whether edit creates duplicate, what fields are editable
- Any `CoachCoacheeMappingDelete()` or deactivate action — check cascade (does deactivating mapping hide deliverables from coachee? does it delete ProtonDeliverableProgresses?)
- `SearchUsers()` (~line 583) — check if it leaks users from other sections/units to non-HC roles

In `CDPController.cs`:
- `CoachingProton()` (~line 1257) — check how coachee's deliverables are loaded after mapping: does a newly mapped coachee immediately see deliverables without re-seeding, or is an explicit ProtonTrackAssignment creation needed?
- Multi-unit user handling: if a user belongs to 2+ units, does the mapping correctly use `AssignmentSection`/`AssignmentUnit` instead of the user's default unit?

Check `Views/Admin/CoachCoacheeMapping.cshtml` for:
- Missing client-side validation
- Section/unit cascade dropdowns correctness
- CSRF token presence on AJAX POSTs

**Produce audit report** at `.planning/phases/154-coaching-proton-flow-audit/154-01-AUDIT-REPORT.md` with:
- Section: PROTON-01
- Each finding: severity (bug/edge-case/security/cosmetic), description, file:line, suggested fix
- Summary: pass/fail for PROTON-01, total findings count

Fix any findings with severity "bug" or "security" immediately and list fixes applied in the report.
  </action>
  <verify>
    <automated>test -f .planning/phases/154-coaching-proton-flow-audit/154-01-AUDIT-REPORT.md && grep -c "PROTON-01" .planning/phases/154-coaching-proton-flow-audit/154-01-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report exists covering PROTON-01 with findings documented. Any bugs/security issues are fixed inline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review of coach-coachee mapping flow (PROTON-01). Audit report created. Any bug/security fixes applied.</what-built>
  <how-to-verify>
1. Read `.planning/phases/154-coaching-proton-flow-audit/154-01-AUDIT-REPORT.md` to understand findings.
2. Log in as HC. Navigate to Kelola Data → Coach-Coachee Mapping.
3. Create a new mapping: assign a coach and a coachee (pick a coachee user), set section/unit.
4. Log in as that coachee. Navigate to CDP → Coaching Proton. Verify their assigned deliverables appear.
5. If the coachee is in multiple units, verify the correct unit's deliverables are shown (not from another unit).
6. Try accessing `/Admin/CoachCoacheeMapping` as a Worker role — confirm redirect to AccessDenied.
7. Report any bugs seen in browser that were not caught in code review.
  </how-to-verify>
  <resume-signal>Type "approved" if UAT passes, or describe any issues found so they can be fixed.</resume-signal>
</task>

</tasks>

<verification>
- 154-01-AUDIT-REPORT.md exists with PROTON-01 section
- All bug/security findings are fixed
- UAT: HC can create mapping, coachee sees deliverables, auth gate works
</verification>

<success_criteria>
PROTON-01 audited: HC can create coach-coachee mapping with correct section/unit assignment and coachee sees deliverables immediately. No unauthorized access to mapping UI.
</success_criteria>

<output>
After completion, create `.planning/phases/154-coaching-proton-flow-audit/154-01-SUMMARY.md`
</output>
