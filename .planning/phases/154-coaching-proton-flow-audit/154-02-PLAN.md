---
phase: 154-coaching-proton-flow-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PROTON-02, PROTON-03, PROTON-04]

must_haves:
  truths:
    - "Coachee can upload evidence for a deliverable and it is visible to coach and approvers"
    - "Coach can create a coaching session with notes, conclusion, and action items linked to the correct coachee/deliverable"
    - "SrSpv and SectionHead each see only their scoped deliverables for approval"
    - "Approval or rejection with reason is visible to the coachee"
  artifacts:
    - path: ".planning/phases/154-coaching-proton-flow-audit/154-02-AUDIT-REPORT.md"
      provides: "Audit findings for evidence upload, coaching sessions, and approval chain (PROTON-02, PROTON-03, PROTON-04)"
  key_links:
    - from: "Controllers/CDPController.cs (UploadEvidence / SubmitEvidenceWithCoaching)"
      to: "ProtonDeliverableProgresses table + evidence file storage"
      via: "IFormFile upload + EF Core update"
    - from: "Controllers/CDPController.cs (ApproveDeliverable / ApproveFromProgress)"
      to: "ProtonDeliverableProgresses.Status field"
      via: "role-scoped authorization check + EF Core update"
    - from: "Controllers/CDPController.cs (CoachingProton)"
      to: "CoachingSessions table"
      via: "SubmitEvidenceWithCoaching POST"
---

<objective>
Audit evidence upload, coaching session creation, and approval chain flows (PROTON-02, PROTON-03, PROTON-04) for bugs, authorization gaps, and edge cases.

Purpose: Ensure the core execution loop of the coaching workflow — coachee uploads evidence, coach adds a session, approvers act on the deliverable — works correctly with proper role scoping.
Output: Audit report with findings, plus inline bug fixes for any bug/security severity issues found.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Evidence upload, coaching sessions, and approval chain (PROTON-02, PROTON-03, PROTON-04)</name>
  <files>Controllers/CDPController.cs, Views/CDP/CoachingProton.cshtml, Views/CDP/Deliverable.cshtml</files>
  <action>
Read and analyze the evidence upload, coaching session, and approval flows for bugs, authorization gaps, and edge cases.

**PROTON-02 (Evidence Upload):**
In `CDPController.cs`:
- `UploadEvidence()` (~line 1086) — check: authorization (coachee-only or coach also allowed?), file type/size validation, path traversal prevention, what happens if progressId doesn't belong to the requesting coachee (IDOR risk), does upload reset approval status correctly?
- `SubmitEvidenceWithCoaching()` (~line 1914) — check: same auth, file handling, whether coaching session is correctly linked, what happens on re-submit (duplicate session creation?)
- `DownloadEvidence()` (~line 1183) — check: authorization (who can download? coach? SrSpv? SectionHead?), path traversal prevention, file not found handling
- `Deliverable()` (~line 603) — check: authorization to view this deliverable (does it verify the coachee/coach relationship?), evidence visibility to coach and approvers

**PROTON-03 (Coaching Sessions):**
In `CDPController.cs`:
- `CoachingProton()` POST (within ~line 1257 block or `SubmitEvidenceWithCoaching`) — check: authorization (coach-only for session creation?), validation of required fields (notes/CatatanCoach, conclusion/Kesimpulan, action items), whether session is linked to correct coachee and deliverable progress, what happens if same deliverable gets multiple sessions (which one is shown?)
- `DownloadEvidencePdf()` (~line 2268) — check: authorization, does the PDF correctly include all session fields (notes, conclusion, action items)?
- Check for edit/delete session actions: if they exist, audit them; if not, note the gap

**PROTON-04 (Approval Chain):**
In `CDPController.cs`:
- `ApproveDeliverable()` (~line 746) and `ApproveFromProgress()` (~line 1718) — check: which roles are authorized (SrSpv, SectionHead, HC?), does SrSpv only see deliverables from their section, does SectionHead only see their section, is there a sequential approval (SrSpv first, then SectionHead, then HC) or any-approver?
- `RejectDeliverable()` (~line 884) and `RejectFromProgress()` (~line 1796) — check: same role scoping, is rejection reason stored and visible to coachee?
- `HCReviewDeliverable()` (~line 1048) and `HCReviewFromProgress()` (~line 1873) — check: HC-specific action vs approve, what state transition does it cause?
- Check filtering in `CoachingProton()` and `FilterCoachingProton()` (~line 266): does SrSpv see ONLY their section's deliverables? Does SectionHead? Test for cross-section data leakage.

Also check the relevant Views for approval UI: are rejection reasons displayed back to the coachee? Are approval buttons correctly gated by role?

**Produce audit report** at `.planning/phases/154-coaching-proton-flow-audit/154-02-AUDIT-REPORT.md` with:
- Section per requirement (PROTON-02, PROTON-03, PROTON-04)
- Each finding: severity (bug/edge-case/security/cosmetic), description, file:line, suggested fix
- Summary: pass/fail per requirement, total findings count

Fix any findings with severity "bug" or "security" immediately and list fixes applied in the report.
  </action>
  <verify>
    <automated>test -f .planning/phases/154-coaching-proton-flow-audit/154-02-AUDIT-REPORT.md && grep -c "PROTON-02\|PROTON-03\|PROTON-04" .planning/phases/154-coaching-proton-flow-audit/154-02-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report exists covering PROTON-02, PROTON-03, PROTON-04 with findings documented. Any bugs/security issues are fixed inline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review of evidence upload, coaching sessions, and approval chain (PROTON-02, PROTON-03, PROTON-04). Audit report created. Any bug/security fixes applied.</what-built>
  <how-to-verify>
1. Read `.planning/phases/154-coaching-proton-flow-audit/154-02-AUDIT-REPORT.md` to understand findings.
2. Log in as a coachee. Navigate to CDP → Coaching Proton → open a deliverable. Upload a file as evidence.
3. Log in as the coach for that coachee. Verify the uploaded evidence is visible. Create a coaching session: fill in notes (CatatanCoach), conclusion (Kesimpulan), action items. Save.
4. Log in as SrSpv. Navigate to CDP → Coaching Proton. Verify only deliverables from their section are visible. Approve one deliverable.
5. Log in as SectionHead. Verify same section scoping. Reject a deliverable with a rejection reason.
6. Log in as the coachee again. Verify the rejection reason is visible on the deliverable.
7. Try accessing `/CDP/ApproveDeliverable/{id}` as a Worker with no approval role — confirm it is blocked.
8. Report any bugs seen in browser that were not caught in code review.
  </how-to-verify>
  <resume-signal>Type "approved" if UAT passes, or describe any issues found so they can be fixed.</resume-signal>
</task>

</tasks>

<verification>
- 154-02-AUDIT-REPORT.md exists with PROTON-02, PROTON-03, PROTON-04 sections
- All bug/security findings are fixed
- UAT: evidence upload works, coaching session saved, approval scoping correct, rejection reason visible to coachee
</verification>

<success_criteria>
PROTON-02/03/04 audited: Evidence upload flows correctly, coaching sessions link to correct coachee/deliverable, SrSpv and SectionHead see only scoped deliverables, rejection reasons visible to coachee.
</success_criteria>

<output>
After completion, create `.planning/phases/154-coaching-proton-flow-audit/154-02-SUMMARY.md`
</output>
