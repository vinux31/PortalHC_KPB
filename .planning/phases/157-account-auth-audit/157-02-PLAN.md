---
phase: 157-account-auth-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [".planning/phases/157-account-auth-audit/157-02-AUDIT-REPORT.md", ".planning/phases/157-account-auth-audit/157-02-SUMMARY.md"]
autonomous: false
requirements: [AUTH-04]

must_haves:
  truths:
    - "Accessing Admin-only URL as Worker redirects to AccessDenied, not 500"
    - "Accessing HC-only URL as Worker redirects to AccessDenied, not 500"
    - "AccessDenied page renders cleanly with informative message"
    - "Unauthenticated user accessing protected URL redirects to Login"
  artifacts:
    - path: "Controllers/AccountController.cs"
      provides: "AccessDenied action"
    - path: "Views/Account/AccessDenied.cshtml"
      provides: "Access denied page"
    - path: "Program.cs"
      provides: "Auth middleware configuration"
  key_links:
    - from: "Program.cs"
      to: "AccountController.AccessDenied"
      via: "cookie auth AccessDeniedPath config"
      pattern: "AccessDeniedPath"
    - from: "[Authorize(Roles=...)]"
      to: "AccessDenied redirect"
      via: "ASP.NET authorization middleware"
      pattern: "Authorize.*Roles"
---

<objective>
Audit authorization enforcement across all controllers — verify role-restricted URLs redirect to AccessDenied, not 500 errors (AUTH-04).

Purpose: Confirm the authorization gate works for every controller's role attributes and the AccessDenied page renders correctly.
Output: 157-02-AUDIT-REPORT.md with findings and fixes, 157-02-SUMMARY.md
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review — Authorization enforcement and AccessDenied</name>
  <files>.planning/phases/157-account-auth-audit/157-02-AUDIT-REPORT.md</files>
  <action>
Review authorization configuration and enforcement for AUTH-04:

1. Program.cs auth setup:
   - Verify cookie auth is configured with AccessDeniedPath pointing to /Account/AccessDenied
   - Verify LoginPath is set correctly
   - Check middleware order (UseAuthentication before UseAuthorization)

2. Controller [Authorize] attributes — audit every controller:
   - AccountController: class-level [Authorize], [AllowAnonymous] on Login/AccessDenied
   - HomeController: class-level [Authorize]
   - CMPController: class-level [Authorize]
   - CDPController: class-level [Authorize]
   - AdminController: class-level [Authorize], per-action Roles="Admin" or "Admin, HC"
   - ProtonDataController: [Authorize(Roles="Admin,HC")]
   - Verify no action is accidentally unprotected (missing attribute on a public-facing action)

3. AccessDenied page:
   - Verify AccountController.AccessDenied action exists with [AllowAnonymous]
   - Verify Views/Account/AccessDenied.cshtml renders a user-friendly message
   - Check it doesn't throw exceptions (no ViewBag dependencies that could be null)

4. Edge cases:
   - What happens if an unauthenticated user hits a protected URL? Should redirect to Login.
   - What happens if authenticated Worker hits /Admin/Index? Should redirect to AccessDenied.

Write findings to 157-02-AUDIT-REPORT.md. Fix any major bugs inline.
  </action>
  <verify>
    <automated>ls .planning/phases/157-account-auth-audit/157-02-AUDIT-REPORT.md &amp;&amp; grep -c "AUTH-04" .planning/phases/157-account-auth-audit/157-02-AUDIT-REPORT.md</automated>
  </verify>
  <done>Audit report covers AUTH-04 with authorization matrix for all controllers. Major bugs fixed in code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review and bug fixes for authorization enforcement across all controllers (AUTH-04)</what-built>
  <how-to-verify>
    1. As a Worker (non-Admin, non-HC role):
       - Try accessing /Admin/Index directly — should redirect to AccessDenied page, not 500
       - Try accessing /ProtonData/Silabus directly — should redirect to AccessDenied
       - AccessDenied page should show a clear message
    2. While logged out:
       - Try accessing /Home/Index — should redirect to Login page
       - Try accessing /Account/Profile — should redirect to Login page
    3. As Admin or HC:
       - Access /Admin/Index — should work normally (verify role gate passes)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- AUTH-04: Role-restricted URLs redirect to AccessDenied for unauthorized users, Login for unauthenticated users
</verification>

<success_criteria>
Authorization enforcement verified across all controllers with no 500 errors on unauthorized access.
</success_criteria>

<output>
After completion, create `.planning/phases/157-account-auth-audit/157-02-SUMMARY.md`
</output>
