---
phase: 158-homepage-navigation-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/HomeController.cs
  - Views/Home/Index.cshtml
  - Views/Shared/_Layout.cshtml
autonomous: false
requirements:
  - NAV-01
  - NAV-02
must_haves:
  truths:
    - "Homepage progress bars show real Proton deliverable, assessment, and coaching session counts for the logged-in user"
    - "Upcoming events show only today/tomorrow coaching sessions and assessments for the logged-in user"
    - "Navbar shows CMP, CDP, Panduan for all roles; Kelola Data only for Admin and HC roles"
    - "No navbar items are visible that the user's role should not see"
  artifacts:
    - path: "Controllers/HomeController.cs"
      provides: "Dashboard data queries"
      contains: "GetProgress"
    - path: "Views/Home/Index.cshtml"
      provides: "Dashboard rendering"
    - path: "Views/Shared/_Layout.cshtml"
      provides: "Navbar with role-conditional items"
      contains: "User.IsInRole"
  key_links:
    - from: "Views/Home/Index.cshtml"
      to: "Controllers/HomeController.cs"
      via: "DashboardHomeViewModel binding"
      pattern: "DashboardHomeViewModel"
    - from: "Views/Shared/_Layout.cshtml"
      to: "Role claims"
      via: "User.IsInRole checks"
      pattern: "User\\.IsInRole"
---

<objective>
Audit the homepage dashboard data accuracy and navbar role-scoping correctness.

Purpose: Verify that progress bars reflect real user data, upcoming events are correctly filtered, and the navbar shows exactly the right items per role.
Output: Verified NAV-01 and NAV-02 with any bugs fixed inline.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@Controllers/HomeController.cs
@Views/Home/Index.cshtml
@Views/Shared/_Layout.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code audit — Homepage dashboard and navbar</name>
  <files>Controllers/HomeController.cs, Views/Home/Index.cshtml, Views/Shared/_Layout.cshtml</files>
  <action>
Audit the following for correctness, edge cases, and bugs:

**NAV-01 — Homepage Dashboard:**
1. Review `GetProgress()` in HomeController.cs:
   - CDP progress: Verify the ProtonTrackAssignment -> ProtonKompetensiList -> SubKompetensiList -> Deliverables chain correctly counts all deliverables for the user's track. Check if multiple active assignments cause issues.
   - Assessment progress: Verify AssessmentSessions query counts are correct (total vs completed).
   - Coaching progress: Verify CoachingSessions query — check if "Submitted" is the correct terminal status (vs "Completed" or other values used elsewhere in the codebase).
2. Review `GetUpcomingEvents()`:
   - Check the "tomorrow" calculation: `AddDays(2).AddTicks(-1)` gives end of tomorrow — verify this is intentional and correct.
   - Check coaching session Date field — is it the session date or creation date?
   - Check assessment filtering: Status "Open" or "Upcoming" with Schedule in today/tomorrow window — verify these status values match what the system actually sets.
3. Review `Views/Home/Index.cshtml`:
   - Verify progress bars render correctly with 0% (no data) and 100% (all complete).
   - Verify upcoming events section handles empty list gracefully.
   - Check for any null reference risks in the view model binding.

**NAV-02 — Navbar Role Scoping:**
4. Review `Views/Shared/_Layout.cshtml` navbar section:
   - CMP link: visible to all authenticated users — correct.
   - CDP link: visible to all authenticated users — verify this is intended (should non-coaching users see CDP?).
   - Panduan link: visible to all — correct.
   - Kelola Data: `User.IsInRole("Admin") || User.IsInRole("HC")` — verify this matches AdminController authorization.
   - Verify NO other role-conditional items are missing (e.g., should SectionHead or Coach have any special nav items?).
5. Check dropdown menu items (Profile, Settings, Logout) — should be role-independent, verify.

Fix any bugs found inline. Document findings in the summary.

Search the codebase for CoachingSession status values to verify "Submitted" is the correct completed status:
```bash
grep -rn "Status.*=" Controllers/ Views/ --include="*.cs" --include="*.cshtml" | grep -i "coaching\|session" | head -20
```
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>All HomeController dashboard queries verified correct or bugs fixed. Navbar role-scoping confirmed correct for all roles. Findings documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — Homepage and navbar per role</name>
  <files>Controllers/HomeController.cs, Views/Home/Index.cshtml, Views/Shared/_Layout.cshtml</files>
  <action>
User verifies homepage dashboard and navbar in browser across multiple roles.
  </action>
  <what-built>Code audit of homepage dashboard and navbar. Any bugs found have been fixed.</what-built>
  <how-to-verify>
    1. Login as a Worker (coachee with active Proton assignment):
       - Homepage shows CDP progress bar with correct deliverable count
       - Homepage shows Assessment progress bar matching their exam history
       - Homepage shows Coaching Sessions progress bar
       - Upcoming events show only today/tomorrow items (or empty if none)
       - Navbar shows: CMP, CDP, Panduan — NO Kelola Data
    2. Login as HC:
       - Homepage shows HC's own progress data (may be 0 if HC has no assignments)
       - Navbar shows: CMP, CDP, Panduan, Kelola Data
    3. Login as Admin:
       - Navbar shows: CMP, CDP, Panduan, Kelola Data
    4. Login as Coach (non-HC):
       - Navbar shows: CMP, CDP, Panduan — NO Kelola Data
    5. Login as SectionHead:
       - Navbar shows: CMP, CDP, Panduan — NO Kelola Data
  </how-to-verify>
  <verify>User confirms all roles show correct dashboard data and navbar items</verify>
  <done>All roles verified: correct progress bars, correct navbar items, no extras or missing items.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Homepage progress bars match real database data for the logged-in user
- Upcoming events are correctly filtered to today/tomorrow only
- Navbar items are role-appropriate with no extras or missing items
- Build succeeds with no errors
</verification>

<success_criteria>
NAV-01: Homepage dashboard shows personalized, accurate progress and events.
NAV-02: Navbar shows exactly correct items per role.
</success_criteria>

<output>
After completion, create `.planning/phases/158-homepage-navigation-audit/158-01-SUMMARY.md`
</output>
