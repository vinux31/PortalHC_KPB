---
phase: "161-fix-deliverable-ordering-in-coachingproton-table"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "Controllers/CDPController.cs"
autonomous: false
requirements: []
must_haves:
  truths:
    - "CoachingProton table displays deliverables in numerical order (1, 2, 3, ...) within each sub-competency"
    - "ProtonData/Index silabus tab shows deliverables in same consistent order"
    - "Ordering is stable across pagination boundaries"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Stable deliverable ordering in CoachingProton query"
      contains: "OrderBy"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Views/CDP/CoachingProton.cshtml"
      via: "TrackingItem list ordering preserved through pagination"
      pattern: "OrderBy.*Urutan"
---

<objective>
Fix deliverable ordering in the CoachingProton table so rows display in correct numerical sequence (1, 2, 3, 4, 5, 6, 7) instead of current broken order (3, 4, 5, 6, 7, 1, 2).

Purpose: User sees scrambled deliverable rows making the coaching tracking table confusing and inconsistent with the silabus page.
Output: Correctly ordered deliverable rows in CoachingProton and verified consistent ordering in ProtonData silabus.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-fix-deliverable-ordering-in-coachingproton-table/161-CONTEXT.md
@Controllers/CDPController.cs
@Controllers/ProtonDataController.cs
</context>

<interfaces>
<!-- Key code locations for the executor -->

From Controllers/CDPController.cs (lines 1450-1454) — DB query ordering:
```csharp
var progresses = await query
    .OrderBy(p => p.ProtonDeliverable!.ProtonSubKompetensi!.ProtonKompetensi!.Urutan)
    .ThenBy(p => p.ProtonDeliverable!.ProtonSubKompetensi!.Urutan)
    .ThenBy(p => p.ProtonDeliverable!.Urutan)
    .ToListAsync();
```

From Controllers/CDPController.cs (lines 1476-1481) — In-memory filter (preserves List order):
```csharp
progresses = progresses.Where(p => { ... }).ToList();
```

From Controllers/CDPController.cs (lines 1532-1534) — GroupBy for pagination:
```csharp
var finestGroups = data
    .GroupBy(item => new { item.CoacheeName, item.Kompetensi, item.SubKompetensi })
    .ToList();
```

From Controllers/ProtonDataController.cs (lines 776-778) — Silabus ordering:
```csharp
.OrderBy(d => d.ProtonSubKompetensi.ProtonKompetensi.Urutan)
    .ThenBy(d => d.ProtonSubKompetensi.Urutan)
    .ThenBy(d => d.Urutan)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix deliverable ordering</name>
  <files>Controllers/CDPController.cs</files>
  <action>
The DB query at line 1450-1453 orders by Kompetensi.Urutan, SubKompetensi.Urutan, Deliverable.Urutan which is correct. Two possible root causes to investigate in order:

1. **Check DB Urutan values** — Run a quick EF query or SQL to check if ProtonDeliverable Urutan values within the affected sub-competency actually match expected 1-7 order. If they are wrong (e.g., some deliverables have Urutan 3,4,5,6,7 and others 1,2 due to being added in different batches via the Catalog UI), the fix is to correct the Urutan values. BUT this is a data issue, not a code issue — skip to code fix below.

2. **Fix the code path** — The `data` list (line 1501-1522) maps `progresses` to `TrackingItem` via `.Select()` which preserves order. However, `TrackingItem` does NOT carry any Urutan fields — only string fields like `Kompetensi`, `SubKompetensi`, `Deliverable`. The pagination `GroupBy` at line 1532 groups by `(CoacheeName, Kompetensi, SubKompetensi)` which preserves first-seen order, BUT the items within each group lose their explicit ordering guarantee since GroupBy collects by key.

   **Most likely fix:** Add an explicit `.OrderBy()` on the `data` list AFTER building TrackingItems, to ensure the order is preserved regardless of any upstream scrambling. Add `int DeliverableUrutan`, `int KompetensiUrutan`, `int SubKompetensiUrutan` fields to `TrackingItem` so we can sort after mapping.

   Specifically:
   a. In the TrackingItem model (find it in Models/ or ViewModels/), add three int properties: `KompetensiUrutan`, `SubKompetensiUrutan`, `DeliverableUrutan`
   b. In the `.Select()` mapping at line 1501, populate them from `p.ProtonDeliverable.ProtonSubKompetensi.ProtonKompetensi.Urutan`, etc.
   c. After building `data` (line 1522), add: `data = data.OrderBy(d => d.CoacheeName).ThenBy(d => d.KompetensiUrutan).ThenBy(d => d.SubKompetensiUrutan).ThenBy(d => d.DeliverableUrutan).ToList();`
   d. This ensures the data is deterministically sorted before pagination GroupBy, making the ordering bulletproof regardless of DB query behavior.

Also verify ProtonDataController silabus query (lines 776-778) already has the same OrderBy chain — it does, so no change needed there.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>CoachingProton data list is explicitly sorted by Urutan fields before pagination. TrackingItem carries Urutan values for stable sorting. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deliverable ordering in browser</name>
  <what-built>Deliverable ordering fix in CoachingProton table — rows now sorted by Kompetensi/SubKompetensi/Deliverable Urutan values explicitly after mapping to TrackingItem</what-built>
  <how-to-verify>
    1. Run the app: dotnet run
    2. Log in as a Coach or HC user
    3. Navigate to CDP > Coaching Proton
    4. Verify deliverables display in numerical order (1, 2, 3, 4, 5, 6, 7) within each sub-competency
    5. Compare with ProtonData > Index silabus tab — ordering should be consistent
    6. If multiple pages exist, check that ordering is consistent across page boundaries
  </how-to-verify>
  <resume-signal>Type "approved" or describe any ordering issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- CoachingProton page shows deliverables in correct 1-7 order
- ProtonData silabus tab shows same order (already correct, just verify)
</verification>

<success_criteria>
Deliverable rows in CoachingProton table display in correct numerical order matching the silabus definition, stable across pagination.
</success_criteria>

<output>
After completion, create `.planning/phases/161-fix-deliverable-ordering-in-coachingproton-table/161-01-SUMMARY.md`
</output>
