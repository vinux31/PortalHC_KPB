---
phase: "162-simplifikasi-action-close-auto-grade"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "Controllers/AdminController.cs"
  - "Controllers/CMPController.cs"
  - "Models/AssessmentMonitoringViewModel.cs"
  - "Models/AuditLog.cs"
autonomous: true
requirements:
  - "CLOSE-01"
  - "CLOSE-02"
  - "CLOSE-03"
  - "CLOSE-04"
must_haves:
  truths:
    - "AkhiriUjian auto-grades saved answers for individual InProgress worker — real score, not hardcoded 0"
    - "AkhiriSemuaUjian auto-grades all InProgress workers and sets Open/Not-started to Cancelled"
    - "ForceCloseAssessment, ForceCloseAll, CloseEarly actions no longer exist in controller"
    - "CheckExamStatus detects Completed (from auto-grade) and Cancelled statuses correctly"
    - "TrainingRecord is created and group completion notification fires on AkhiriUjian"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "AkhiriUjian, AkhiriSemuaUjian actions; GradeFromSavedAnswers shared method"
      contains: "AkhiriUjian"
    - path: "Controllers/CMPController.cs"
      provides: "CheckExamStatus updated for Cancelled status"
      contains: "Cancelled"
    - path: "Models/AssessmentMonitoringViewModel.cs"
      provides: "CancelledCount property"
      contains: "CancelledCount"
  key_links:
    - from: "AdminController.AkhiriUjian"
      to: "GradeFromSavedAnswers"
      via: "shared method call"
      pattern: "GradeFromSavedAnswers"
    - from: "AdminController.AkhiriSemuaUjian"
      to: "GradeFromSavedAnswers"
      via: "shared method call for each InProgress session"
      pattern: "GradeFromSavedAnswers"
---

<objective>
Replace 3 inconsistent close actions with 2 auto-grading actions in the backend.

Purpose: Eliminate the confusing ForceClose (score=0), ForceCloseAll (Abandoned), CloseEarly (partial grade) trio with consistent AkhiriUjian (individual auto-grade) and AkhiriSemuaUjian (bulk auto-grade + cancel not-started).
Output: Working backend actions that auto-grade from saved answers, create TrainingRecords, and handle Cancelled status.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-simplifikasi-action-close-auto-grade/162-CONTEXT.md

@Controllers/AdminController.cs (lines 2233-2280: ForceCloseAssessment, lines 2287-2325: ForceCloseAll, lines 2575-2729: CloseEarly)
@Controllers/CMPController.cs (lines 338-374: CheckExamStatus, lines 1540-1690: SubmitExam grading logic)
@Models/AssessmentMonitoringViewModel.cs
</context>

<interfaces>
<!-- Key types the executor needs -->

From Models/AssessmentMonitoringViewModel.cs:
```csharp
public class MonitoringGroupViewModel {
    public int TotalCount { get; set; }
    public int CompletedCount { get; set; }
    public int PassedCount { get; set; }
    public int PendingCount { get; set; }
    public List<MonitoringSessionViewModel> Sessions { get; set; } = new();
}
```

From Controllers/CMPController.cs (SubmitExam grading pattern):
```csharp
// Package path: load PackageUserResponses, grade against PackageQuestion.Options.IsCorrect
// Legacy path: load UserResponses, grade against AssessmentQuestion.Options.IsCorrect
// Both: finalPercentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0
// Both: set Score, Status="Completed", Progress=100, IsPassed, CompletedAt
// Both: create TrainingRecord + call NotifyIfGroupCompleted
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared grading method and create AkhiriUjian + AkhiriSemuaUjian actions</name>
  <files>Controllers/AdminController.cs, Models/AssessmentMonitoringViewModel.cs, Models/AuditLog.cs</files>
  <action>
1. **Extract GradeFromSavedAnswers private method** in AdminController. This method takes an AssessmentSession (already loaded), detects package vs legacy mode, loads saved responses from DB, grades them, sets Score/Status/Progress/IsPassed/CompletedAt, creates TrainingRecord (with duplicate guard), and calls NotifyIfGroupCompleted. Pattern: copy grading logic from CloseEarly (lines 2590-2708) but for a SINGLE session. The method signature should be:
   `private async Task GradeFromSavedAnswers(AssessmentSession session)`
   - For package mode: load UserPackageAssignment, get ShuffledQuestionIds, load PackageUserResponses, load PackageQuestions with Options, grade
   - For legacy mode: find sibling session with Questions, load UserResponses, grade
   - Set session.Score, Status="Completed", Progress=100, IsPassed, CompletedAt=DateTime.UtcNow
   - For package mode: also set assignment.IsCompleted = true
   - Create TrainingRecord (duplicate guard: check existing by UserId + Judul + Tanggal, same as SubmitExam lines 1664-1683)
   - Call NotifyIfGroupCompleted(session) — same as SubmitExam line 1686
   - Do NOT call SaveChangesAsync inside this method — caller handles it

2. **Create AkhiriUjian action** (replaces ForceCloseAssessment):
   - `[HttpPost] [Authorize(Roles = "Admin, HC")] [ValidateAntiForgeryToken]`
   - Parameter: `int id` (session ID)
   - Load session, validate status is "InProgress" only (per decision: only InProgress, not Open)
   - Call `GradeFromSavedAnswers(session)`
   - SaveChangesAsync, invalidate cache `_cache.Remove($"exam-status-{id}")`
   - Audit log with action "AkhiriUjian"
   - TempData["Success"] = "Ujian telah diakhiri dan dinilai dari jawaban tersimpan."
   - Redirect to AssessmentMonitoringDetail

3. **Create AkhiriSemuaUjian action** (replaces ForceCloseAll + CloseEarly):
   - `[HttpPost] [Authorize(Roles = "Admin, HC")] [ValidateAntiForgeryToken]`
   - Parameters: `string title, string category, DateTime scheduleDate`
   - Load all sessions in group where Status is "Open" or "InProgress"
   - For each InProgress session: call `GradeFromSavedAnswers(session)`
   - For each Open (not-started) session: set Status = "Cancelled", UpdatedAt = DateTime.UtcNow
   - SaveChangesAsync once after all processing
   - Invalidate cache for all affected sessions
   - Audit log with action "AkhiriSemuaUjian", include counts in description
   - TempData["Success"] with counts of graded + cancelled
   - Redirect to AssessmentMonitoringDetail

4. **Delete ForceCloseAssessment, ForceCloseAll, and CloseEarly actions** entirely from AdminController.

5. **Add CancelledCount** to MonitoringGroupViewModel:
   - Add `public int CancelledCount { get; set; }` property
   - In AssessmentMonitoringDetail action (~line 1862 area), compute CancelledCount from sessions where UserStatus == "Cancelled"

6. **Update AssessmentMonitoringDetail action** status mapping: add handling for Status == "Cancelled" → UserStatus = "Dibatalkan" (around line 1830 where Abandoned is mapped)

7. **Update ExportAssessmentResults** (around line 2380): add Cancelled → "Dibatalkan" status mapping, leave score column empty for Cancelled

8. **Update ResetAssessment** validation (around line 2142): ensure "Cancelled" is NOT resettable (per decision: Cancelled is final). Keep Abandoned as resettable.

9. **Update AuditLog.cs** comment to include AkhiriUjian and AkhiriSemuaUjian action types
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>ForceCloseAssessment/ForceCloseAll/CloseEarly removed. AkhiriUjian and AkhiriSemuaUjian exist with auto-grading via shared GradeFromSavedAnswers method. CancelledCount in ViewModel. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update CheckExamStatus for new statuses and add GetAkhiriSemuaCounts endpoint</name>
  <files>Controllers/CMPController.cs, Controllers/AdminController.cs, Models/AssessmentMonitoringViewModel.cs</files>
  <action>
1. **Update CheckExamStatus** (CMPController line 338):
   - Add check: if session.Status == "Cancelled", return `{ closed = true, redirectUrl = Assessment page }` (Cancelled workers should be redirected away from exam, not to Results since they have no score)
   - Keep existing Completed detection (now covers auto-graded sessions)
   - Keep ExamWindowCloseDate check for backward compat
   - Update comments to reference AkhiriUjian/AkhiriSemuaUjian instead of CloseEarly/ForceClose

2. **Add GetAkhiriSemuaCounts endpoint** in AdminController for the confirmation modal to show impact summary:
   - `[HttpGet] [Authorize(Roles = "Admin, HC")]`
   - Parameters: `string title, string category, DateTime scheduleDate`
   - Query sessions in group, count InProgress (StartedAt != null and CompletedAt == null and Score == null) and Open/not-started (StartedAt == null)
   - Return JSON: `{ inProgressCount, notStartedCount }`
   - This feeds the confirmation modal with "3 peserta InProgress akan dinilai, 2 peserta belum mulai akan dibatalkan"

3. **Add InProgressCount** to MonitoringGroupViewModel and compute it in AssessmentMonitoringDetail action (~line 1862):
   - Add `public int InProgressCount { get; set; }` property
   - Compute from sessions where UserStatus == "Sedang Mengerjakan"
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>CheckExamStatus handles Cancelled status. GetAkhiriSemuaCounts returns impact summary for modal. InProgressCount available in ViewModel. Build succeeds.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes with no errors
- grep confirms no references to ForceCloseAssessment, ForceCloseAll, or CloseEarly in Controllers/
- grep confirms AkhiriUjian and AkhiriSemuaUjian exist in AdminController.cs
- grep confirms GradeFromSavedAnswers exists as private method
- grep confirms CancelledCount in MonitoringGroupViewModel
</verification>

<success_criteria>
- All 3 old close actions removed from AdminController
- 2 new actions (AkhiriUjian, AkhiriSemuaUjian) exist with auto-grading logic
- Grading logic extracted to shared method matching SubmitExam behavior
- Cancelled status handled in CheckExamStatus, monitoring detail, and export
- TrainingRecord creation and group completion notification included in grading flow
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/162-simplifikasi-action-close-auto-grade/162-01-SUMMARY.md`
</output>
