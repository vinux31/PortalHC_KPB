---
phase: "162-simplifikasi-action-close-auto-grade"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "162-01"
files_modified:
  - "Views/Admin/AssessmentMonitoringDetail.cshtml"
  - "Views/CMP/StartExam.cshtml"
autonomous: false
requirements:
  - "CLOSE-01"
  - "CLOSE-02"
  - "CLOSE-03"
  - "CLOSE-04"
must_haves:
  truths:
    - "HC sees 'Akhiri Ujian' button only for InProgress workers — not Open/Not-started"
    - "HC sees 'Akhiri Semua Ujian' button at group level with informative confirmation modal"
    - "Cancelled workers show grey 'Dibatalkan' badge in monitoring table"
    - "Worker sees 5-second notification modal when exam is closed by HC, then redirects to results"
    - "Old ForceClose/CloseEarly buttons and modals no longer exist in the view"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Updated buttons, modals, JS for new actions"
      contains: "AkhiriUjian"
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Worker close notification modal"
      contains: "Ujian Anda telah diakhiri"
  key_links:
    - from: "AssessmentMonitoringDetail.cshtml"
      to: "AdminController.AkhiriUjian"
      via: "form POST"
      pattern: "AkhiriUjian"
    - from: "AssessmentMonitoringDetail.cshtml"
      to: "AdminController.AkhiriSemuaUjian"
      via: "form POST"
      pattern: "AkhiriSemuaUjian"
    - from: "StartExam.cshtml CheckExamStatus poll"
      to: "CMPController.CheckExamStatus"
      via: "fetch JSON every 10s"
      pattern: "CheckExamStatus"
---

<objective>
Update monitoring detail view and worker exam page for the new close actions.

Purpose: Replace old ForceClose/CloseEarly buttons with AkhiriUjian/AkhiriSemuaUjian, add Cancelled badge, and add worker notification modal when exam is closed by HC.
Output: Working UI for HC to close exams and workers to receive close notifications.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/162-simplifikasi-action-close-auto-grade/162-CONTEXT.md
@.planning/phases/162-simplifikasi-action-close-auto-grade/162-01-SUMMARY.md

@Views/Admin/AssessmentMonitoringDetail.cshtml
@Views/CMP/StartExam.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace buttons and modals in AssessmentMonitoringDetail</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <action>
1. **Replace individual ForceClose button** (per-row, ~line 278 and JS ~line 664):
   - Change form action from `ForceCloseAssessment` to `AkhiriUjian`
   - Button label: "Akhiri Ujian", class btn-danger btn-sm
   - Show ONLY for InProgress workers (not Open) — update the JS condition that renders this button (~line 664: check status === 'Sedang Mengerjakan' only)
   - Add onclick confirmation: `confirm('Akhiri ujian untuk {workerName}? Jawaban tersimpan akan dinilai otomatis.')`
   - Remove the old ForceCloseAll inline form (~line 174)

2. **Replace bulk CloseEarly button and modal** (~line 168 closeEarlyBtn, ~line 369 closeEarlyModal):
   - Replace closeEarlyBtn with "Akhiri Semua Ujian" button, btn-danger, opens new modal
   - New modal ID: `akhiriSemuaModal`
   - Modal title: "Akhiri Semua Ujian"
   - Modal body: fetch counts from `/Admin/GetAkhiriSemuaCounts?title=...&category=...&scheduleDate=...` on modal open, then display:
     "Akhiri semua ujian?\n{inProgressCount} peserta InProgress akan dinilai dari jawaban tersimpan.\n{notStartedCount} peserta belum mulai akan dibatalkan.\nTindakan ini tidak dapat dibatalkan."
   - Form action: `AkhiriSemuaUjian` with hidden fields for title, category, scheduleDate
   - Submit button: "Ya, Akhiri Semua", btn-danger
   - Cancel button: "Batal"

3. **Add Cancelled/Dibatalkan badge** in the status column:
   - In the Razor table rows (~line 270 area): add `else if (session.UserStatus == "Dibatalkan")` → grey badge `<span class="badge bg-secondary">Dibatalkan</span>`
   - In the JS dynamic row rendering (~line 660 area): add same badge for "Dibatalkan" status
   - Cancelled rows should NOT show any action buttons (no Reset, no Akhiri Ujian)

4. **Update summary cards** to show CancelledCount:
   - Add a card or modify existing display to show "Dibatalkan: X" count using Model.CancelledCount
   - Use grey/secondary styling consistent with the badge

5. **Remove all old references**: delete closeEarlyModal, closeEarlyBtn, ForceCloseAll form, ForceCloseAssessment form references. Clean up any related JS event handlers.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Old ForceClose/CloseEarly buttons removed. AkhiriUjian per-row button for InProgress only. AkhiriSemuaUjian bulk button with informative modal. Cancelled badge shown. Summary cards include CancelledCount.</done>
</task>

<task type="auto">
  <name>Task 2: Add worker close notification in StartExam</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <action>
1. **Update CheckExamStatus polling handler** in StartExam.cshtml:
   - Currently when `closed=true`, it redirects immediately. Change to show a modal first.
   - When response has `closed: true`:
     a. Stop the polling interval
     b. Show a Bootstrap modal with message: "Ujian Anda telah diakhiri oleh penyelenggara."
     c. Modal has a 5-second countdown displayed: "Anda akan diarahkan ke halaman hasil dalam 5 detik..."
     d. "Lihat Hasil" button for immediate redirect to `redirectUrl`
     e. After 5 seconds, auto-redirect to `redirectUrl`
     f. Modal should be non-dismissable (no close X, no backdrop click, keyboard=false)
   - If `redirectUrl` is empty (Cancelled case — worker never started, shouldn't normally be on exam page, but handle gracefully): redirect to `/CMP/Assessment` instead

2. **Reuse expired exam modal pattern**: The existing expired exam modal in StartExam.cshtml already has a similar 5-second countdown + redirect. Follow the same visual pattern but with different text. Do NOT merge them — keep separate modals for clarity (expired vs HC-closed are different events).

3. **Ensure the close notification works even if worker is mid-question**: The modal should overlay everything. Use `z-index: 9999` and fixed position if needed to ensure it appears above any exam UI.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Worker sees non-dismissable "Ujian Anda telah diakhiri oleh penyelenggara" modal with 5-second countdown when HC closes their exam. Lihat Hasil button for early redirect. Cancelled workers redirected to Assessment page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete close action simplification</name>
  <what-built>Complete close action simplification: AkhiriUjian (individual auto-grade), AkhiriSemuaUjian (bulk auto-grade + cancel), worker notification modal, Cancelled status handling</what-built>
  <how-to-verify>
    1. Run `dotnet run` and login as HC
    2. Go to Assessment Monitoring, pick an assessment group with InProgress and Open workers
    3. Test "Akhiri Ujian" on an InProgress worker:
       - Verify button only appears for InProgress (not Open/Not-started)
       - Click it, confirm — worker should get a real score (not 0), status Completed
    4. Open another browser as a worker who is InProgress in same group
       - As HC, click "Akhiri Semua Ujian" — verify confirmation modal shows correct counts
       - Confirm — check worker browser shows "Ujian Anda telah diakhiri" modal with 5-sec countdown
       - Verify InProgress workers got real scores, Open workers got "Dibatalkan" badge
    5. Check Export Excel includes Cancelled workers with "Dibatalkan" status, empty score
    6. Verify no ForceClose/CloseEarly buttons remain anywhere
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` passes
- grep confirms no ForceClose/CloseEarly references in Views/
- grep confirms AkhiriUjian and AkhiriSemuaUjian in AssessmentMonitoringDetail.cshtml
- grep confirms "Ujian Anda telah diakhiri" in StartExam.cshtml
</verification>

<success_criteria>
- HC monitoring detail shows AkhiriUjian for InProgress workers only
- HC can bulk-close with AkhiriSemuaUjian, modal shows impact summary
- Cancelled workers show grey Dibatalkan badge, no action buttons
- Worker receives 5-second notification modal when exam is closed by HC
- Excel export handles Cancelled status
- No old ForceClose/CloseEarly UI remains
</success_criteria>

<output>
After completion, create `.planning/phases/162-simplifikasi-action-close-auto-grade/162-02-SUMMARY.md`
</output>
