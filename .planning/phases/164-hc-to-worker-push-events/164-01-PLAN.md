---
phase: 164-hc-to-worker-push-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - PUSH-01
  - PUSH-02
  - PUSH-03

must_haves:
  truths:
    - "AdminController injects IHubContext<AssessmentHub> via constructor"
    - "Reset action sends sessionReset event to the specific worker after DB write succeeds"
    - "AkhiriUjian action sends examClosed event to the specific worker after DB write succeeds"
    - "AkhiriSemuaUjian action sends examClosed event to the batch group after DB write succeeds"
    - "No SendAsync call fires when rowsAffected == 0 (race condition guard respected)"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "IHubContext injection + 3 SendAsync calls"
      contains: "IHubContext<AssessmentHub>"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Hubs/AssessmentHub.cs"
      via: "IHubContext<AssessmentHub> DI injection"
      pattern: "IHubContext<AssessmentHub>"
    - from: "AdminController.AkhiriSemuaUjian"
      to: "SignalR group"
      via: "Clients.Group(batch-{batchKey})"
      pattern: "Clients\\.Group.*batch-"
---

<objective>
Inject IHubContext into AdminController and add SignalR push calls after the three HC assessment actions (Reset, AkhiriUjian, AkhiriSemuaUjian).

Purpose: Enable server-side push so worker exam pages receive instant notification when HC takes action, instead of waiting up to 10 seconds for polling.
Output: AdminController with 3 SendAsync calls wired after successful DB writes.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-hc-to-worker-push-events/164-CONTEXT.md
@.planning/phases/164-hc-to-worker-push-events/164-RESEARCH.md
@.planning/phases/163-hub-infrastructure-safety-foundations/163-01-SUMMARY.md

<interfaces>
<!-- From Hubs/AssessmentHub.cs (Phase 163): -->
<!-- Hub registered at /hubs/assessment, has JoinBatch/LeaveBatch methods -->
<!-- Group naming: "batch-{batchKey}" where batchKey = "{title}|{category}|{date:yyyy-MM-dd}" -->

<!-- IHubContext injection pattern: -->
<!-- using Microsoft.AspNetCore.SignalR; -->
<!-- using HcPortal.Hubs; -->
<!-- IHubContext<AssessmentHub> hubContext in constructor -->
<!-- Clients.User(userId).SendAsync("eventName", payload) for single user -->
<!-- Clients.Group("batch-{key}").SendAsync("eventName", payload) for group -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject IHubContext into AdminController</name>
  <files>Controllers/AdminController.cs</files>
  <action>
    1. Add using directives: `using Microsoft.AspNetCore.SignalR;` and `using HcPortal.Hubs;`
    2. Add private field: `private readonly IHubContext<AssessmentHub> _hubContext;`
    3. Add `IHubContext<AssessmentHub> hubContext` parameter to the constructor
    4. Add `_hubContext = hubContext;` in constructor body

    This is the 9th injected service. DI container resolves it automatically since AddSignalR() was registered in Phase 163.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>AdminController compiles with IHubContext<AssessmentHub> injected</done>
</task>

<task type="auto">
  <name>Task 2: Add SendAsync calls to Reset, AkhiriUjian, AkhiriSemuaUjian</name>
  <files>Controllers/AdminController.cs</files>
  <action>
    IMPORTANT: Read the full Reset action (around line 2160-2240), AkhiriUjian (around line 2254-2330), and AkhiriSemuaUjian (around line 2336-2400) BEFORE making changes. Confirm variable names for session entity and UserId.

    **Reset action** (PUSH-01):
    - After the `rsRowsAffected > 0` path completes (after audit log), before TempData/redirect:
    - Add: `await _hubContext.Clients.User(session.UserId).SendAsync("sessionReset", new { reason = "hc_reset" });`
    - VERIFY: Confirm the actual variable name for the session entity (may be `assessment` not `session`) — use whatever the action uses for `.UserId`
    - Only send if the DB write succeeded (respect existing rowsAffected guard)

    **AkhiriUjian action** (PUSH-02):
    - After `rowsAffected > 0` path completes (after audit log), before TempData/redirect:
    - Add: `await _hubContext.Clients.User(session.UserId).SendAsync("examClosed", new { reason = "hc_closed" });`
    - VERIFY: Confirm `session.UserId` is the correct accessor

    **AkhiriSemuaUjian action** (PUSH-03):
    - After SaveChangesAsync() and cache invalidation, before TempData/redirect:
    - Reconstruct batchKey: `var batchKey = $"{title}|{category}|{scheduleDate.Date:yyyy-MM-dd}";`
    - Add: `await _hubContext.Clients.Group($"batch-{batchKey}").SendAsync("examClosed", new { reason = "hc_closed" });`
    - The batchKey format MUST match exactly what JoinBatch uses in assessment-hub.js / CMPController ViewBag assignment

    **Anti-patterns to avoid:**
    - Never send push BEFORE DB write confirms
    - Never use connectionId — always Clients.User() or Clients.Group()
    - Never wrap SendAsync in try/catch that silently swallows — let it propagate (SignalR is fire-and-forget to disconnected clients anyway)
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 actions compile and have SendAsync calls gated behind successful DB writes. Reset sends "sessionReset" to user, AkhiriUjian sends "examClosed" to user, AkhiriSemuaUjian sends "examClosed" to batch group.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds with no errors
- Grep confirms: `SendAsync.*sessionReset` appears once in AdminController (Reset action)
- Grep confirms: `SendAsync.*examClosed` appears twice in AdminController (AkhiriUjian + AkhiriSemuaUjian)
- Grep confirms: `IHubContext<AssessmentHub>` appears in AdminController constructor
- No `Dictionary.*connectionId` patterns exist
</verification>

<success_criteria>
AdminController compiles and contains 3 SendAsync calls — one per HC action — all gated behind successful DB writes. No new packages or files needed.
</success_criteria>

<output>
After completion, create `.planning/phases/164-hc-to-worker-push-events/164-01-SUMMARY.md`
</output>
