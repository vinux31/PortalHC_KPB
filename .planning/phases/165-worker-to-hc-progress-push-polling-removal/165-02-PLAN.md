---
phase: 165-worker-to-hc-progress-push-polling-removal
plan: 02
type: execute
wave: 2
depends_on: ["165-01"]
files_modified:
  - Views/CMP/StartExam.cshtml
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Controllers/CMPController.cs
  - Controllers/AdminController.cs
autonomous: false
requirements: [CLEAN-01]

must_haves:
  truths:
    - "No setInterval calls for CheckExamStatus or GetMonitoringProgress remain in JS"
    - "CheckExamStatus and GetMonitoringProgress controller actions are deleted"
    - "Countdown timer and auto-save intervals still work (not removed)"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Polling-free exam page"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Polling-free monitoring page"
  key_links:
    - from: "Views/CMP/StartExam.cshtml"
      to: "assessment-hub.js"
      via: "SignalR push replaces polling"
      pattern: "assessmentHub"
---

<objective>
Remove legacy polling code after UAT confirms SignalR push events work correctly.

Purpose: Clean codebase — no dead polling code coexisting with live push events.
Output: CheckExamStatus and GetMonitoringProgress endpoints deleted, their JS polling intervals removed, all polling-related variables cleaned up.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-worker-to-hc-progress-push-polling-removal/165-CONTEXT.md
@.planning/phases/165-worker-to-hc-progress-push-polling-removal/165-RESEARCH.md
@.planning/phases/165-worker-to-hc-progress-push-polling-removal/165-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Plan 01 added real-time SignalR push events for worker progress, start, and submit to the HC monitoring page. Before removing polling, please verify push events work correctly.</what-built>
  <how-to-verify>
    1. Open two browser tabs: HC monitoring detail page + worker exam page (same assessment batch)
    2. On worker tab, answer a question → HC monitoring row should update progress within 1-2s with blue flash
    3. Open a fresh worker exam (different user or incognito) → HC monitoring should show "Dalam Pengerjaan" with flash + toast
    4. On worker tab, submit the exam → HC monitoring should show "Selesai" + score with green flash + toast
    5. Check connection badge shows "Live" on HC monitoring page
    6. Confirm all 3 events work before approving polling removal
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with polling removal, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove polling code from StartExam, AssessmentMonitoringDetail, and delete polling endpoints</name>
  <files>Views/CMP/StartExam.cshtml, Views/Admin/AssessmentMonitoringDetail.cshtml, Controllers/CMPController.cs, Controllers/AdminController.cs</files>
  <action>
1. **StartExam.cshtml** — Remove all CheckExamStatus polling code:
   - Remove the `checkExamStatus` function (lines ~740-784)
   - Remove `const CHECK_STATUS_URL` declaration (line ~742)
   - Remove `var statusPollInterval = null` declaration (line ~743)
   - Remove `statusPollInterval = setInterval(checkExamStatus, 10000)` (line ~786)
   - KEEP: timerInterval countdown (line 331), 30-second auto-save (line 547), navPoll (line 588), rPoll (line 627) — these are local timers/save guards, NOT server polls

2. **AssessmentMonitoringDetail.cshtml** — Remove all GetMonitoringProgress polling code:
   - Remove `fetchProgress()` function (lines ~807-837)
   - Remove `var pollingTimer = null` (line ~626)
   - Remove `var pollingActive = true` (line ~625)
   - Remove `pollingTimer = setInterval(fetchProgress, 10000)` (line ~840)
   - Remove `fetchProgress()` initial call (line ~844)
   - Remove `id="poll-error"` span element (line ~326)
   - KEEP: `setInterval(tickCountdowns, 1000)` (line 841) — local countdown timer
   - KEEP: "Last updated" timestamp element (line ~324) — still updated by push events
   - Port terminal-state detection from fetchProgress to the workerSubmitted handler (if not already done in Plan 01): when all sessions are Completed/Dibatalkan/Abandoned, clearInterval(countdownTimer) and hide akhiriSemuaBtn

3. **CMPController.cs** — Delete `CheckExamStatus` action method entirely (lines ~336-380)

4. **AdminController.cs** — Delete `GetMonitoringProgress` action method entirely (lines ~2046-2140)

IMPORTANT: Do NOT remove navPoll, rPoll, countdown timer, or auto-save intervals. These are NOT polling — they are local client-side guards.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5 && echo "---" && grep -n "setInterval" Views/CMP/StartExam.cshtml Views/Admin/AssessmentMonitoringDetail.cshtml 2>/dev/null | grep -iv "countdown\|tickCountdown\|autoSave\|navPoll\|rPoll\|timer" || echo "No polling setIntervals found (GOOD)"</automated>
  </verify>
  <done>No setInterval calls for CheckExamStatus or GetMonitoringProgress remain. Both polling endpoints deleted from controllers. Countdown timer, auto-save, navPoll, rPoll still intact. Project compiles.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds
2. `grep -rn "CheckExamStatus\|GetMonitoringProgress" Controllers/` returns nothing
3. `grep -n "setInterval" Views/CMP/StartExam.cshtml` shows only countdown, auto-save, navPoll, rPoll — no checkExamStatus
4. `grep -n "setInterval" Views/Admin/AssessmentMonitoringDetail.cshtml` shows only tickCountdowns — no fetchProgress
5. Manual test: worker exam still works (countdown, auto-save, navigation guards functional)
6. Manual test: HC monitoring still receives live push events
</verification>

<success_criteria>
All polling code removed. Both polling endpoints deleted. No JS callers remain. Non-polling intervals preserved. Push events confirmed still working after removal.
</success_criteria>

<output>
After completion, create `.planning/phases/165-worker-to-hc-progress-push-polling-removal/165-02-SUMMARY.md`
</output>
