---
phase: 166-activity-log-per-worker
plan: 02
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/CMP/StartExam.cshtml
autonomous: false
requirements: [LOG-01, LOG-02]

must_haves:
  truths:
    - "HC sees a log button on each worker row that has logged events"
    - "Clicking the log button opens a modal with chronological timeline"
    - "Modal shows summary header with answered count, time spent, disconnect count"
    - "Worker page navigation triggers LogPageNav hub call"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Log button per worker row + activity log modal + AJAX fetch"
      contains: "GetActivityLog"
    - path: "Views/CMP/StartExam.cshtml"
      provides: "hub.invoke LogPageNav on page change"
      contains: "LogPageNav"
  key_links:
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      to: "AdminController.GetActivityLog"
      via: "fetch/AJAX call on button click"
      pattern: "GetActivityLog"
    - from: "Views/CMP/StartExam.cshtml"
      to: "AssessmentHub.LogPageNav"
      via: "hub.invoke on page navigation"
      pattern: "LogPageNav"
---

<objective>
Wire the frontend: add log button to monitoring detail worker rows, activity log modal with AJAX fetch, and page navigation logging from the worker exam page.

Purpose: HC can view the audit trail; worker page changes are captured in real-time.
Output: Functional activity log modal on monitoring detail + page nav logging on exam page.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-activity-log-per-worker/166-CONTEXT.md
@.planning/phases/166-activity-log-per-worker/166-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker exam page nav logging + monitoring detail modal</name>
  <files>Views/CMP/StartExam.cshtml, Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <action>
1. In `Views/CMP/StartExam.cshtml`:
   - Find the existing page navigation JS (where user clicks page number buttons or next/prev to change question pages)
   - After each page change, call `hub.invoke('LogPageNav', sessionId, currentPageNumber)` where sessionId is the current assessment session ID (should be available in the view model or a data attribute)
   - Wrap in try-catch — never block navigation if logging fails

2. In `Views/Admin/AssessmentMonitoringDetail.cshtml`:
   - Add a small icon button (e.g., `<i class="bi bi-clock-history"></i>`) in each worker's table row. The button should be conditionally shown — pass an `HasActivityLogs` flag or event count from the controller (add this to the AssessmentMonitoringDetail action's query: check if any ExamActivityLogs exist for each session). Alternatively, always show the button but disable it if no logs exist.
   - Add a Bootstrap modal at the bottom of the page:
     ```html
     <div class="modal fade" id="activityLogModal">
       <div class="modal-dialog modal-lg">
         <div class="modal-content">
           <div class="modal-header">
             <h5 class="modal-title">Activity Log: <span id="logWorkerName"></span></h5>
             <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
           </div>
           <div class="modal-body">
             <div id="logSummary" class="mb-3 p-2 bg-light rounded"></div>
             <div id="logTimeline"></div>
             <div id="logLoading" class="text-center d-none"><div class="spinner-border spinner-border-sm"></div> Memuat...</div>
           </div>
         </div>
       </div>
     </div>
     ```
   - Add JS function `showActivityLog(sessionId, workerName)`:
     - Show modal, set worker name, show loading spinner
     - Fetch `/Admin/GetActivityLog?sessionId={sessionId}`
     - Render summary: "Dijawab: {answered}/{total} | Waktu: {minutes} menit | Terputus: {disconnectCount}x"
     - Render timeline: each event as a row with time (HH:mm:ss) and label in Bahasa Indonesia:
       - "started" → "Memulai Ujian"
       - "page_nav" → "Pindah ke Halaman X" (from Detail field)
       - "disconnected" → "Terputus"
       - "reconnected" → "Tersambung Kembali"
       - "submitted" → "Menyelesaikan Ujian"
     - Plain text list with timestamp on left, description on right. Use `<table class="table table-sm">` or simple div rows.
   - Wire button click: `onclick="showActivityLog(@session.Id, '@session.User.FullName')"`
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Worker page navigation calls LogPageNav on hub. Monitoring detail shows log button per worker, clicking it opens modal with summary header and chronological timeline fetched via AJAX.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify activity log end-to-end</name>
  <what-built>Complete activity log feature: server-side event capture (start, page nav, disconnect, reconnect, submit) and HC-facing activity log modal on monitoring detail page</what-built>
  <how-to-verify>
    1. Open an assessment as a worker, navigate through question pages, then submit
    2. As HC, go to Assessment Monitoring Detail for that assessment
    3. Find the worker's row — a log icon/button should be visible
    4. Click the log button — modal should open showing:
       - Summary: "Dijawab: X/Y | Waktu: Z menit | Terputus: 0x"
       - Timeline with "Memulai Ujian", "Pindah ke Halaman X" entries, "Menyelesaikan Ujian"
       - Timestamps in HH:mm:ss format
    5. Verify disconnect logging: toggle DevTools offline briefly during exam, then reconnect — "Terputus" and "Tersambung Kembali" should appear in log
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- Worker exam page calls LogPageNav on page changes
- Monitoring detail has log button per worker row
- Modal shows summary + chronological timeline
- Events display in Bahasa Indonesia labels
</verification>

<success_criteria>
HC can click a worker in monitoring detail and see a complete activity timeline with timestamps. Page navigation events from the worker exam are captured in real-time via SignalR.
</success_criteria>

<output>
After completion, create `.planning/phases/166-activity-log-per-worker/166-02-SUMMARY.md`
</output>
