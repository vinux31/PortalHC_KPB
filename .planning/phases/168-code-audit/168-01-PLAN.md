---
phase: 168-code-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AccountController.cs
  - Controllers/HomeController.cs
  - Controllers/NotificationController.cs
  - Controllers/CMPController.cs
  - Controllers/CDPController.cs
  - Controllers/AdminController.cs
  - Controllers/ProtonDataController.cs
  - Views/**/*.cshtml
autonomous: true
requirements: [CODE-01, CODE-04]

must_haves:
  truths:
    - "Every controller action is reachable through a route, link, or form in the application"
    - "Every .cshtml view file has a corresponding controller action that returns it"
    - "No helper method or private method exists without being called"
  artifacts:
    - path: ".planning/phases/168-code-audit/168-01-SUMMARY.md"
      provides: "Audit results and list of removals"
  key_links:
    - from: "Controllers/*.cs"
      to: "Views/**/*.cshtml"
      via: "return View() calls"
      pattern: "return View\\("
---

<objective>
Identify and remove dead code (unreachable actions, unused helpers) and orphaned views across the entire codebase.

Purpose: Eliminate code that cannot be reached through any route or call chain, and views with no controller action.
Output: Clean controllers and views directory with all dead code removed.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and remove dead controller actions and unused helpers</name>
  <files>Controllers/AccountController.cs, Controllers/HomeController.cs, Controllers/NotificationController.cs, Controllers/CMPController.cs, Controllers/CDPController.cs, Controllers/AdminController.cs, Controllers/ProtonDataController.cs</files>
  <action>
Systematically audit every controller for dead code:

1. For each controller, list ALL public actions (methods returning IActionResult/Task&lt;IActionResult&gt;).
2. For each action, search the ENTIRE codebase for references: grep for the action name in all .cshtml files (asp-action, Html.ActionLink, Url.Action), in all .cs files (RedirectToAction, return View), in all .js files, and in any route configuration.
3. An action is DEAD if it has zero references from views, other controllers, JavaScript, or route configs.
4. For each controller, list ALL private/helper methods. Search for callers within the same file and across the codebase.
5. A helper is DEAD if no method in the codebase calls it.
6. Remove all confirmed dead actions and helpers. Do NOT remove actions that serve as HTTP endpoints even if not linked from views (they may be called via AJAX — check .js files and inline scripts in views).
7. Document every removal with method name and reason.

IMPORTANT: Be conservative. If uncertain whether something is used (e.g., called dynamically, used in JavaScript string construction), keep it and document the uncertainty.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All dead controller actions and unused helper methods are removed. Build succeeds. Each removal is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and remove orphaned views</name>
  <files>Views/**/*.cshtml</files>
  <action>
1. List every .cshtml file under Views/ (excluding Shared/, _ViewImports.cshtml, _ViewStart.cshtml).
2. For each view file, determine the expected controller and action from the directory structure (e.g., Views/Admin/ImportWorkers.cshtml expects AdminController.ImportWorkers action).
3. Verify that the corresponding controller action exists AND calls return View() for that view name (either explicitly or by convention — action name matches view file name).
4. Also check for views referenced via return View("ViewName") from a different action name.
5. Also check for views used as partial views via @Html.Partial, @await Html.PartialAsync, or &lt;partial name=&gt; tag helpers.
6. A view is ORPHANED if no controller action or partial reference points to it.
7. Delete all confirmed orphaned view files.
8. Check Shared/ views — verify each is referenced as a layout (_Layout), partial, or error view.
9. Document every removal.

IMPORTANT: Views in Shared/ that serve as layouts (_Layout*.cshtml) or error pages are NOT orphaned even without direct controller references.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All orphaned view files are removed. Build succeeds. Every remaining view has a reachable controller action or is a used partial/layout.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds with no errors
- Every remaining controller action can be traced to a route, link, or AJAX call
- Every remaining .cshtml file can be traced to a controller action or partial/layout reference
</verification>

<success_criteria>
No unreachable controller actions, helper methods, or orphaned views remain in the codebase. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/168-code-audit/168-01-SUMMARY.md`
</output>
