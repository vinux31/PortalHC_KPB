---
phase: 169-file-database-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [".gitignore"]
autonomous: true
requirements: [FILE-02, FILE-03]
must_haves:
  truths:
    - "No temporary files (screenshots, debug logs, test artifacts) remain in the project directory tree"
    - "No orphaned JS or CSS file exists that is not referenced by any view or layout"
  artifacts:
    - path: ".gitignore"
      provides: "Patterns to prevent future temp file accumulation"
      contains: "*.png"
  key_links: []
---

<objective>
Remove all temporary/leftover files (screenshots, build logs, playwright artifacts, SQLite WAL files) from the project root and ensure all JS/CSS files in wwwroot are actually referenced.

Purpose: Clean the repository of 40+ accumulated test screenshots, build logs, and debug artifacts that should never be committed.
Output: Clean project tree, updated .gitignore
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove temporary files and update .gitignore</name>
  <files>.gitignore</files>
  <action>
1. Identify ALL temp files in project root and subdirs (excluding .claude/, .planning/, node_modules/):
   - `*.png` screenshots in project root (~30+ uat-*.png, homepage-*.png, verify-*.png files)
   - `*.log` files in project root (build.log, build_normal.log, build_output.log, error.log, msbuild.log)
   - `HcPortal.db-shm`, `HcPortal.db-wal` (SQLite WAL temp files)
   - `.playwright-mcp/` directory and contents
   - Any other temp artifacts (*.tmp, *.bak)

2. Delete all identified temp files using `git rm` for tracked files, `rm` for untracked.

3. Update `.gitignore` to prevent future accumulation. Add patterns:
   ```
   # Temp/debug artifacts
   *.log
   *.png
   !wwwroot/**/*.png
   *.db-shm
   *.db-wal
   .playwright-mcp/
   ```

4. Verify custom JS/CSS files in wwwroot/ are referenced:
   - `wwwroot/css/assessment-hub.css` — grep for reference in views
   - `wwwroot/css/guide.css` — grep for reference in views
   - `wwwroot/css/home.css` — grep for reference in views
   - `wwwroot/js/assessment-hub.js` — grep for reference in views
   If any are unreferenced, remove them. If all referenced, document findings.
  </action>
  <verify>
    <automated>bash -c "find . -maxdepth 1 -name '*.png' -o -name '*.log' | head -1 | grep -c . | grep -q 0 && echo PASS || echo FAIL"</automated>
  </verify>
  <done>Zero temp files in project root, .gitignore updated, all wwwroot JS/CSS verified as referenced or removed</done>
</task>

</tasks>

<verification>
- `find . -maxdepth 1 \( -name "*.png" -o -name "*.log" \) | wc -l` returns 0
- `ls .playwright-mcp/ 2>/dev/null` returns error (directory gone)
- `ls HcPortal.db-shm HcPortal.db-wal 2>/dev/null` returns error
- `.gitignore` contains patterns for png, log, db-shm, db-wal
</verification>

<success_criteria>
No temporary files remain in the project tree. All JS/CSS files in wwwroot are verified as referenced.
</success_criteria>

<output>
After completion, create `.planning/phases/169-file-database-audit/169-01-SUMMARY.md`
</output>
