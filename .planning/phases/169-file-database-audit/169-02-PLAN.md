---
phase: 169-file-database-audit
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [FILE-01, FILE-04]
must_haves:
  truths:
    - "No .cshtml file exists without a corresponding reachable controller action or partial reference"
    - "No duplicate or near-duplicate code blocks exist that could be unified into a shared partial or method"
  artifacts: []
  key_links: []
---

<objective>
Audit all .cshtml views for orphaned files and scan controllers+views for duplicate/near-duplicate code blocks that should be unified.

Purpose: Ensure every view is reachable and identify consolidation opportunities to reduce maintenance burden.
Output: List of findings with removals or documented justifications; duplicate blocks unified into partials/methods where beneficial.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-code-audit/168-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit views for orphaned files (post-Phase 168 re-verify)</name>
  <files></files>
  <action>
Phase 168-01 already confirmed all 53 views have controller actions. This task re-verifies after Phase 168's code removals and checks for any new gaps:

1. List all .cshtml files under Views/ (excluding Shared/).
2. For each, confirm a matching controller action exists (controller name = folder, action name = file name).
3. For Shared/ views, confirm each is referenced via `@Html.Partial`, `@await Html.PartialAsync`, `@await Component.InvokeAsync`, `Layout =`, or `_ViewImports`/`_ViewStart`.
4. If any orphaned views found, remove them. If none, document "re-verified, zero orphaned views".

Note: Phase 168-01 found zero orphaned views. This is a post-cleanup re-verification to satisfy FILE-01.
  </action>
  <verify>
    <automated>bash -c "echo 'Manual audit — documented in summary'"</automated>
  </verify>
  <done>All .cshtml files verified as reachable. Zero orphaned views, or orphaned views removed.</done>
</task>

<task type="auto">
  <name>Task 2: Identify and unify duplicate code blocks</name>
  <files></files>
  <action>
Scan for duplicate/near-duplicate patterns across views and controllers:

1. **Views**: Compare all .cshtml files for repeated HTML blocks (>10 lines identical or near-identical). Common candidates:
   - Table headers/structures repeated across list views
   - Form layouts repeated across create/edit views
   - Alert/notification markup
   - Pagination controls

2. **Controllers**: Scan all controllers for repeated logic blocks (>10 lines). Common candidates:
   - Similar LINQ query patterns repeated across actions
   - Repeated authorization/validation logic
   - Similar ViewBag/ViewData setup

3. For each duplicate found, assess:
   - Is it >10 lines repeated in 3+ places? -> Extract to partial view or helper method
   - Is it 2 places only and context-specific? -> Document but leave (extraction cost > benefit)

4. Extract beneficial duplicates into shared partials (`Views/Shared/`) or helper methods. Update all consumers.

5. Document all findings: what was duplicated, what was extracted, what was intentionally left.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>All significant duplicate code blocks (>10 lines, 3+ occurrences) either extracted into shared partials/methods or documented as intentionally separate. Build passes.</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` succeeds
- Summary documents all duplicate patterns found and actions taken
</verification>

<success_criteria>
Every .cshtml file is reachable. Duplicate code blocks are either unified or documented with justification for keeping separate.
</success_criteria>

<output>
After completion, create `.planning/phases/169-file-database-audit/169-02-SUMMARY.md`
</output>
