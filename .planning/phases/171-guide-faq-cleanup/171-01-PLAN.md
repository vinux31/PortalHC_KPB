---
phase: 171-guide-faq-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Home/GuideDetail.cshtml
  - wwwroot/css/guide.css
  - wwwroot/documents/guides/ActiveDirectory-Guide.html
  - docs/ActiveDirectory-Guide.html
autonomous: true
requirements: [GUIDE-01, GUIDE-02, GUIDE-04]

must_haves:
  truths:
    - "CMP GuideDetail shows only 4 accordion items for workers (Library KKJ, Mapping KKJ-CPDP, Training Records, Monitoring Records Tim) plus 1 PDF tutorial card"
    - "CDP GuideDetail has redundant items removed (items covered by PDF Coaching Proton tutorial)"
    - "CDP 5 (Approve/Reject Deliverable) is only visible to Admin/HC roles"
    - "Tutorial PDF cards use CSS classes from guide.css instead of inline styles"
    - "AD tutorial card appears in admin module GuideDetail for Admin/HC only"
  artifacts:
    - path: "Views/Home/GuideDetail.cshtml"
      provides: "Simplified accordion items, role-gated content, styled tutorial cards"
    - path: "wwwroot/css/guide.css"
      provides: "CSS classes for tutorial card variants (CMP purple, CDP green, Admin pink)"
    - path: "wwwroot/documents/guides/ActiveDirectory-Guide.html"
      provides: "AD guide file in correct location"
  key_links:
    - from: "Views/Home/GuideDetail.cshtml"
      to: "wwwroot/css/guide.css"
      via: "CSS classes for tutorial cards"
      pattern: "guide-tutorial-card--(cmp|cdp|admin)"
---

<objective>
Simplify GuideDetail accordion items by removing CMP/CDP items covered by PDF tutorials, fix tutorial card inline styles to use CSS classes, add AD tutorial card for admin module, and role-gate CDP 5.

Purpose: Reduce redundancy — PDF tutorials already cover assessment and coaching flows in detail.
Output: Cleaner GuideDetail.cshtml with fewer accordion items, proper CSS classes, AD integration.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-guide-faq-cleanup/171-CONTEXT.md
@Views/Home/GuideDetail.cshtml
@wwwroot/css/guide.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tutorial card CSS classes and move AD guide file</name>
  <files>wwwroot/css/guide.css, wwwroot/documents/guides/ActiveDirectory-Guide.html, docs/ActiveDirectory-Guide.html</files>
  <action>
1. In guide.css, add three tutorial card variant classes to replace inline styles. Each needs: background gradient, border color, icon background color, title color. Variants:
   - `.guide-tutorial-card--cmp`: background `linear-gradient(135deg, #e3f2fd 0%, #f5f5f5 100%)`, border `#bbdefb`, icon bg `#1565c0`, title color `#1565c0`
   - `.guide-tutorial-card--cdp`: background `linear-gradient(135deg, #e8f5e9 0%, #f5f5f5 100%)`, border `#a5d6a7`, icon bg `#2e7d32`, title color `#2e7d32`
   - `.guide-tutorial-card--admin`: background `linear-gradient(135deg, #fce4ec 0%, #f5f5f5 100%)`, border `#f48fb1`, icon bg `#c2185b`, title color `#c2185b`

   Each class targets the inner `.d-flex` container for background/border-radius/border, a `.guide-tutorial-icon` div for the icon box (48x48, 12px radius, centered flex), and a `.guide-tutorial-title` for the h5 color. Structure:
   ```css
   .guide-tutorial-card--cmp > .guide-tutorial-inner { background: ...; border-radius: 10px; border: 1px solid ...; }
   .guide-tutorial-card--cmp .guide-tutorial-icon { width:48px; height:48px; border-radius:12px; background:#1565c0; display:flex; align-items:center; justify-content:center; }
   .guide-tutorial-card--cmp .guide-tutorial-title { color:#1565c0; }
   ```
   Also add a shared base: `.guide-tutorial-inner { padding: 1rem; }` and `.guide-tutorial-icon i { color: #fff; }`.

2. Copy `docs/ActiveDirectory-Guide.html` to `wwwroot/documents/guides/ActiveDirectory-Guide.html` using `cp`. Keep the original in docs/ (do NOT delete it — git already tracks the deletion separately).
  </action>
  <verify>
    <automated>grep -c "guide-tutorial-card--cmp\|guide-tutorial-card--cdp\|guide-tutorial-card--admin" wwwroot/css/guide.css && ls wwwroot/documents/guides/ActiveDirectory-Guide.html</automated>
  </verify>
  <done>Three tutorial card CSS variant classes exist in guide.css. AD guide file exists in wwwroot/documents/guides/.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify GuideDetail accordions, fix tutorial cards, add AD card, role-gate CDP 5</name>
  <files>Views/Home/GuideDetail.cshtml</files>
  <action>
**CMP section (GUIDE-01):**
- Remove CMP 3 (Mengerjakan Assessment), CMP 4 (Melihat Hasil Assessment), CMP 5 (Download Sertifikat) — these are lines ~155-207 approximately. They are fully covered by the PDF Assessment tutorial.
- Keep: CMP 1 (Library KKJ), CMP 2 (Mapping KKJ-CPDP), CMP 6 (Training Records), CMP 7 (Monitoring Records Tim — already role-gated to Admin/HC).
- Renumber remaining items if needed for clean accordion IDs.

**CDP section (GUIDE-02):**
- Read the CDP accordion items (starting ~line 250). Analyze which items are covered by the PDF Coaching Proton tutorial (which covers: silabus viewing, evidence upload, approval flow, final assessment).
- Remove CDP items that duplicate the PDF tutorial content. Per user decision: keep CDP 1 (Plan IDP/Silabus) and CDP 6 (Dashboard) at minimum.
- CDP 5 (Approve/Reject Deliverable): wrap in `@if (userRole == "Admin" || userRole == "HC")` role gate — do NOT remove it, just hide from regular workers.

**Tutorial card styling (GUIDE-04):**
- Replace all inline styles on the CMP tutorial card (lines ~66-86) with CSS classes:
  - Outer div: keep `class="guide-tutorial-card"`, add variant `guide-tutorial-card--cmp`
  - Inner div: replace `style="background:..."` with `class="guide-tutorial-inner d-flex align-items-center justify-content-between flex-wrap gap-3"`
  - Icon div: replace `style="width:48px..."` with `class="guide-tutorial-icon"`
  - Title h5: replace `style="color:..."` with `class="guide-tutorial-title mb-1 fw-bold"`
- Do the same for the CDP tutorial card (lines ~90-111) using `guide-tutorial-card--cdp` variant.

**AD tutorial card integration:**
- Add a new `@else if (module == "admin")` block after the CDP tutorial card block.
- Wrap it in `@if (userRole == "Admin" || userRole == "HC")` (admin module is already Admin/HC only, but be explicit).
- Use the same HTML structure as CMP/CDP cards but with `guide-tutorial-card--admin` variant.
- Title: "Panduan Konfigurasi Active Directory"
- Description: "Tutorial lengkap konfigurasi Active Directory untuk integrasi autentikasi HC Portal."
- Link: `/documents/guides/ActiveDirectory-Guide.html`
- Buttons: Lihat (outline-danger) and Download (btn-danger) to match admin/pink theme.
  </action>
  <verify>
    <automated>grep -c "cmpCollapse3\|cmpCollapse4\|cmpCollapse5" Views/Home/GuideDetail.cshtml | grep "^0$" && grep -c "guide-tutorial-card--cmp\|guide-tutorial-card--cdp\|guide-tutorial-card--admin" Views/Home/GuideDetail.cshtml && grep -c "ActiveDirectory-Guide" Views/Home/GuideDetail.cshtml</automated>
  </verify>
  <done>CMP 3/4/5 removed. CDP simplified with PDF-covered items removed. CDP 5 role-gated. Tutorial cards use CSS classes (no inline styles). AD tutorial card renders for admin module. Zero inline style attributes on tutorial card sections.</done>
</task>

</tasks>

<verification>
- `dotnet build` compiles without errors
- Browse to /Home/GuideDetail?module=cmp as worker: see 4 accordion items + 1 PDF card
- Browse to /Home/GuideDetail?module=cmp as Admin: see 5 accordion items + 1 PDF card
- Browse to /Home/GuideDetail?module=cdp as worker: reduced accordion items, no CDP 5
- Browse to /Home/GuideDetail?module=cdp as Admin: includes CDP 5
- Browse to /Home/GuideDetail?module=admin as Admin: see AD tutorial card
- No inline `style=` attributes on any tutorial card div
</verification>

<success_criteria>
- CMP accordion reduced from 7 to 4 items (5 for Admin/HC)
- CDP accordion simplified with PDF-covered items removed
- CDP 5 visible only to Admin/HC
- All tutorial cards styled via guide.css classes
- AD tutorial card integrated in admin module
</success_criteria>

<output>
After completion, create `.planning/phases/171-guide-faq-cleanup/171-01-SUMMARY.md`
</output>
