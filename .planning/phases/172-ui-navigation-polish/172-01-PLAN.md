---
phase: 172-ui-navigation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/css/guide.css
  - Views/Home/Guide.cshtml
  - Views/Home/GuideDetail.cshtml
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "Role badges look identical on Guide.cshtml hero and GuideDetail.cshtml accordion headers"
    - "FAQ question buttons and GuideDetail accordion buttons share consistent base styling"
    - "CMP step badges use blue color matching the CMP module icon"
  artifacts:
    - path: "wwwroot/css/guide.css"
      provides: "Unified .guide-role-badge class, .step-variant-blue, unified accordion base styles"
      contains: "guide-role-badge"
    - path: "Views/Home/Guide.cshtml"
      provides: "Updated role badge class usage"
      contains: "guide-role-badge"
    - path: "Views/Home/GuideDetail.cshtml"
      provides: "Updated role badge class usage"
      contains: "guide-role-badge"
  key_links:
    - from: "Views/Home/Guide.cshtml"
      to: "wwwroot/css/guide.css"
      via: "guide-role-badge class"
      pattern: "guide-role-badge"
    - from: "Views/Home/GuideDetail.cshtml"
      to: "wwwroot/css/guide.css"
      via: "guide-role-badge and step-variant-blue classes"
      pattern: "guide-role-badge|step-variant-blue"
---

<objective>
Unify visual styling across Guide pages: consolidate role badges into one class, align accordion/FAQ button base styles, add blue step variant for CMP, remove dead CSS.

Purpose: Eliminate inconsistent inline overrides and divergent badge styling between Guide and GuideDetail pages.
Output: Updated guide.css with unified classes, updated Razor views using new classes.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-ui-navigation-polish/172-CONTEXT.md
@wwwroot/css/guide.css
@Views/Home/Guide.cshtml
@Views/Home/GuideDetail.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify role badges and step badge colors in CSS</name>
  <files>wwwroot/css/guide.css</files>
  <action>
1. Create a new `.guide-role-badge` class that combines the best of `.role-badge` (guide.css:100) and `.guide-step-badge-role` (guide.css:465). Use the same neutral styling for both contexts (background, padding, border-radius, font-size). Keep the existing `.role-badge` and `.guide-step-badge-role` rules but make them simple aliases or remove them after views are updated in Task 2.

2. Add `.step-variant-blue` following the exact pattern of `.step-variant-green` (guide.css:399-410):
   - `.guide-step-item.step-variant-blue .guide-step-badge` — blue background (#0d6efd or Bootstrap primary blue)
   - `.guide-step-item.step-variant-blue:hover .guide-step-badge` — slightly darker blue
   - Add corresponding print media and responsive rules matching the green/teal/orange pattern

3. Remove `.step-variant-pink` and its hover rule (guide.css:421-442 pink entries) — confirmed dead code per user decision.

4. Unify accordion base styles: Add a shared base rule that applies to both `.faq-question` and `.accordion-button` within guide pages. Set consistent font-size (0.95rem), padding (0.75rem 1rem), border-radius (0.5rem), and transition (all 0.2s ease). Use a shared class or comma-separated selector. Keep module-specific color accents via existing variant classes.

5. Update responsive breakpoint sections (~line 816 for .role-badge, ~line 1065-1080 for step variants) to reference the new classes.
  </action>
  <verify>
    <automated>grep -c "guide-role-badge" wwwroot/css/guide.css | grep -v "^0$" && grep -c "step-variant-blue" wwwroot/css/guide.css | grep -v "^0$" && ! grep -q "step-variant-pink" wwwroot/css/guide.css && echo "PASS"</automated>
  </verify>
  <done>guide.css has .guide-role-badge unified class, .step-variant-blue with hover/responsive/print rules, .step-variant-pink removed, and shared accordion base styling for .faq-question and .accordion-button</done>
</task>

<task type="auto">
  <name>Task 2: Update Razor views to use unified classes</name>
  <files>Views/Home/Guide.cshtml, Views/Home/GuideDetail.cshtml</files>
  <action>
1. In Guide.cshtml (~line 42): Replace `class="role-badge"` with `class="guide-role-badge"`. Standardize badge text to "Admin / HC" if any variant text exists.

2. In GuideDetail.cshtml: Replace all occurrences of `class="guide-step-badge-role"` with `class="guide-role-badge"`. Remove any inline style overrides on role badges (e.g., `bg-success text-white border-0` on CDP step 5). Standardize all badge text to "Admin / HC".

3. In GuideDetail.cshtml: Change CMP step items from their current variant class (likely step-variant-pink or no variant) to `step-variant-blue`. Verify the switch/case section around CMP to ensure all CMP accordion items use `step-variant-blue`.

4. Verify no remaining references to the old `.role-badge` or `.guide-step-badge-role` classes in either view file.
  </action>
  <verify>
    <automated>! grep -q "guide-step-badge-role" Views/Home/GuideDetail.cshtml && grep -q "guide-role-badge" Views/Home/Guide.cshtml && grep -q "guide-role-badge" Views/Home/GuideDetail.cshtml && grep -q "step-variant-blue" Views/Home/GuideDetail.cshtml && echo "PASS"</automated>
  </verify>
  <done>Both views use .guide-role-badge exclusively, CMP steps use step-variant-blue, no inline badge overrides remain, badge text is "Admin / HC" everywhere</done>
</task>

</tasks>

<verification>
- Build project: `dotnet build` passes
- Visual check: Role badges identical on Guide and GuideDetail pages
- CMP steps show blue badges
- FAQ and accordion buttons have consistent base styling
</verification>

<success_criteria>
1. Single .guide-role-badge class used in both views — no divergent badge styling
2. .step-variant-blue exists and is applied to CMP steps
3. .step-variant-pink CSS removed
4. FAQ and accordion buttons share unified base styling
5. No inline style overrides on role badges
</success_criteria>

<output>
After completion, create `.planning/phases/172-ui-navigation-polish/172-01-SUMMARY.md`
</output>
