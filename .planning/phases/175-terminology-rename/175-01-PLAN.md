---
phase: 175-terminology-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/Results.cshtml
  - Controllers/AdminController.cs
  - Views/Admin/ImportPackageQuestions.cshtml
autonomous: true
requirements:
  - TERM-01
  - TERM-02
  - TERM-03
  - TERM-04
  - TERM-05
  - TERM-06
  - TERM-07
must_haves:
  truths:
    - "Results page spider web section title reads 'Analisis Elemen Teknis'"
    - "Results page table column header reads 'Elemen Teknis'"
    - "Excel template header cell reads 'Elemen Teknis'"
    - "Excel template example row reads 'Elemen Teknis x.x'"
    - "Excel template help text reads 'Kolom Elemen Teknis'"
    - "Import page hint text reads 'Elemen Teknis (opsional)'"
    - "Cross-package warning message contains 'Elemen Teknis'"
  artifacts:
    - path: "Views/CMP/Results.cshtml"
      provides: "Assessment results display with renamed labels"
      contains: "Elemen Teknis"
    - path: "Controllers/AdminController.cs"
      provides: "Template generation and warning with renamed labels"
      contains: "Elemen Teknis"
    - path: "Views/Admin/ImportPackageQuestions.cshtml"
      provides: "Import hint with renamed label"
      contains: "Elemen Teknis"
  key_links: []
---

<objective>
Replace all user-facing "Sub Kompetensi" text with "Elemen Teknis" in assessment-related UI.

Purpose: Align assessment terminology with current organizational standards.
Output: 3 modified files with 8 string replacements total.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/175-terminology-rename/175-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename labels in Results.cshtml</name>
  <files>Views/CMP/Results.cshtml</files>
  <read_first>Views/CMP/Results.cshtml</read_first>
  <action>
Make 3 exact string replacements in Views/CMP/Results.cshtml:

1. Line 106 HTML comment: change `<!-- Analisis Sub Kompetensi -->` to `<!-- Analisis Elemen Teknis -->`
2. Line 111 section title: change `Analisis Sub Kompetensi` to `Analisis Elemen Teknis` (inside the h5 tag)
3. Line 124 table header: change `<th>Sub Kompetensi</th>` to `<th>Elemen Teknis</th>`

Do NOT change any C# variable names like `SubCompetencyScores` — only user-facing display strings.
  </action>
  <verify>
    <automated>grep -n "Sub Kompetensi" Views/CMP/Results.cshtml | head -5; grep -n "Elemen Teknis" Views/CMP/Results.cshtml</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "Sub Kompetensi" Views/CMP/Results.cshtml` returns 0
- `grep -c "Elemen Teknis" Views/CMP/Results.cshtml` returns 3
- `grep "SubCompetencyScores" Views/CMP/Results.cshtml` still finds C# variable references (unchanged)
  </acceptance_criteria>
  <done>All 3 user-facing "Sub Kompetensi" strings in Results.cshtml replaced with "Elemen Teknis"</done>
</task>

<task type="auto">
  <name>Task 2: Rename labels in AdminController and ImportPackageQuestions view</name>
  <files>Controllers/AdminController.cs, Views/Admin/ImportPackageQuestions.cshtml</files>
  <read_first>Controllers/AdminController.cs, Views/Admin/ImportPackageQuestions.cshtml</read_first>
  <action>
Make 4 exact string replacements in Controllers/AdminController.cs:

1. Line ~5398 template header array: change `"Sub Kompetensi"` to `"Elemen Teknis"`
2. Line ~5416 example row: change `"Sub Kompetensi x.x"` to `"Elemen Teknis x.x"`
3. Line ~5430 help text: change `"Kolom Sub Kompetensi: opsional, isi nama sub-kompetensi. Kosongkan jika tidak ada."` to `"Kolom Elemen Teknis: opsional, isi nama elemen teknis. Kosongkan jika tidak ada."`
4. Line ~5738 warning message: change `"Sub Kompetensi pada paket ini berbeda dari paket lain dalam assessment yang sama. Pastikan konsistensi antar paket."` to `"Elemen Teknis pada paket ini berbeda dari paket lain dalam assessment yang sama. Pastikan konsistensi antar paket."`

Make 1 exact string replacement in Views/Admin/ImportPackageQuestions.cshtml:

5. Line ~27: change `Sub Kompetensi (opsional)` to `Elemen Teknis (opsional)`

Do NOT change any C# variable/property names — only user-facing display strings.
  </action>
  <verify>
    <automated>grep -n "Sub Kompetensi" Controllers/AdminController.cs Views/Admin/ImportPackageQuestions.cshtml | head -10; grep -n "Elemen Teknis" Controllers/AdminController.cs Views/Admin/ImportPackageQuestions.cshtml</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "Sub Kompetensi" Controllers/AdminController.cs` returns 0
- `grep -c "Elemen Teknis" Controllers/AdminController.cs` returns 4
- `grep -c "Sub Kompetensi" Views/Admin/ImportPackageQuestions.cshtml` returns 0
- `grep -c "Elemen Teknis" Views/Admin/ImportPackageQuestions.cshtml` returns 1
- `grep "sub-kompetensi" Controllers/AdminController.cs` returns 0 (help text lowercase also renamed)
  </acceptance_criteria>
  <done>All 5 user-facing "Sub Kompetensi" strings in AdminController.cs and ImportPackageQuestions.cshtml replaced with "Elemen Teknis"</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "Sub Kompetensi" Views/CMP/Results.cshtml Controllers/AdminController.cs Views/Admin/ImportPackageQuestions.cshtml` returns NO results
2. `grep -rn "Elemen Teknis" Views/CMP/Results.cshtml Controllers/AdminController.cs Views/Admin/ImportPackageQuestions.cshtml` returns exactly 8 matches
3. `dotnet build --no-restore` compiles without errors
</verification>

<success_criteria>
Zero occurrences of "Sub Kompetensi" in the 3 target files. Eight occurrences of "Elemen Teknis" across those files. Project builds successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/175-terminology-rename/175-01-SUMMARY.md`
</output>
