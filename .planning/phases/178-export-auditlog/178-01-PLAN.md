---
phase: 178-export-auditlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/AuditLog.cshtml
autonomous: true
requirements:
  - EXP-03

must_haves:
  truths:
    - "Admin/HC clicks Export Excel on AuditLog page and receives an .xlsx file"
    - "Export respects current date filter (startDate/endDate) so only filtered entries are exported"
    - "Exported file contains four columns: Waktu, Aktor, Aksi, Detail"
    - "Date filter filters both the on-screen table and the export"
    - "Empty date pickers show all records (no default range)"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "AuditLog action with date filter params + ExportAuditLog action"
      contains: "ExportAuditLog"
    - path: "Views/Admin/AuditLog.cshtml"
      provides: "Date filter toolbar with export button"
      contains: "ExportAuditLog"
  key_links:
    - from: "Views/Admin/AuditLog.cshtml"
      to: "AdminController.ExportAuditLog"
      via: "anchor href with startDate/endDate query params"
      pattern: "ExportAuditLog.*startDate"
    - from: "Views/Admin/AuditLog.cshtml"
      to: "AdminController.AuditLog"
      via: "form submit with startDate/endDate"
      pattern: "asp-action=\"AuditLog\""
---

<objective>
Add date filtering and Excel export to the AuditLog page.

Purpose: Admin/HC can filter audit trail by date range and download filtered results as Excel for compliance review.
Output: Updated AuditLog action with date params, new ExportAuditLog action, updated view with filter toolbar and export button.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-export-auditlog/178-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add date filter to AuditLog action and create ExportAuditLog action</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 2693-2718 for current AuditLog action)
    - Controllers/AdminController.cs (lines 3891-3975 for ExportWorkers pattern)
    - Models/AuditLog.cs (model fields)
  </read_first>
  <action>
1. Modify the existing AuditLog action (line ~2696) to accept date filter parameters:
   - Change signature to: `public async Task<IActionResult> AuditLog(int page = 1, DateTime? startDate = null, DateTime? endDate = null)`
   - Build query as `var query = _context.AuditLogs.AsQueryable();`
   - If startDate has value: `query = query.Where(a => a.CreatedAt >= startDate.Value);`
   - If endDate has value: `query = query.Where(a => a.CreatedAt < endDate.Value.AddDays(1));` (inclusive end date)
   - Apply `.CountAsync()` on filtered query for totalCount
   - Apply `.OrderByDescending(a => a.CreatedAt).Skip().Take()` on filtered query
   - Pass startDate and endDate to ViewBag: `ViewBag.StartDate = startDate?.ToString("yyyy-MM-dd");` and `ViewBag.EndDate = endDate?.ToString("yyyy-MM-dd");`
   - Keep existing pagination logic (pageSize 25)

2. Add new ExportAuditLog action right after AuditLog action (~line 2718):
   ```
   // GET /Admin/ExportAuditLog
   [HttpGet]
   [Authorize(Roles = "Admin, HC")]
   public async Task<IActionResult> ExportAuditLog(DateTime? startDate = null, DateTime? endDate = null)
   ```
   - Build same date filter query as AuditLog (no pagination — export ALL matching)
   - Order by CreatedAt descending
   - Create workbook with ClosedXML: `using var workbook = new XLWorkbook();`
   - Worksheet name: "AuditLog"
   - Four header cells: row 1 cols 1-4 = "Waktu", "Aktor", "Aksi", "Detail"
   - Header style: Bold + LightBlue background (same as ExportWorkers pattern)
   - Data rows: CreatedAt formatted as `log.CreatedAt.ToLocalTime().ToString("dd MMM yyyy HH:mm")`, ActorName, ActionType, Description
   - `ws.Columns().AdjustToContents();`
   - Return File with filename `AuditLog_{DateTime.Now:yyyyMMdd_HHmmss}.xlsx` and MIME `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
  </action>
  <verify>
    <automated>grep -n "ExportAuditLog\|startDate\|endDate" Controllers/AdminController.cs | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "public async Task<IActionResult> ExportAuditLog" Controllers/AdminController.cs returns a match
    - grep "public async Task<IActionResult> AuditLog(int page = 1, DateTime? startDate = null, DateTime? endDate = null)" Controllers/AdminController.cs returns a match
    - grep "Waktu" Controllers/AdminController.cs finds the header cell in ExportAuditLog
    - grep "Aktor" Controllers/AdminController.cs finds the header cell in ExportAuditLog
    - grep "ViewBag.StartDate" Controllers/AdminController.cs returns a match
    - grep "ViewBag.EndDate" Controllers/AdminController.cs returns a match
    - grep "AuditLog_" Controllers/AdminController.cs finds the filename pattern
  </acceptance_criteria>
  <done>AuditLog action accepts startDate/endDate and filters query; ExportAuditLog action generates Excel with 4 columns for all filtered records</done>
</task>

<task type="auto">
  <name>Task 2: Add date filter toolbar and export button to AuditLog view</name>
  <files>Views/Admin/AuditLog.cshtml</files>
  <read_first>
    - Views/Admin/AuditLog.cshtml (current view)
    - Controllers/AdminController.cs (the updated AuditLog action from Task 1 for ViewBag fields)
  </read_first>
  <action>
1. Add ViewBag variables at top of the @{} block:
   ```
   var startDate = ViewBag.StartDate as string ?? "";
   var endDate = ViewBag.EndDate as string ?? "";
   ```

2. Add a filter toolbar between the Header div and the Table section (before `@if (Model.Any())`). Structure:
   ```html
   <div class="card shadow-sm mb-3">
       <div class="card-body py-2">
           <form method="get" asp-controller="Admin" asp-action="AuditLog" class="row g-2 align-items-end">
               <div class="col-auto">
                   <label class="form-label mb-0 small">Dari Tanggal</label>
                   <input type="date" name="startDate" class="form-control form-control-sm" value="@startDate" />
               </div>
               <div class="col-auto">
                   <label class="form-label mb-0 small">Sampai Tanggal</label>
                   <input type="date" name="endDate" class="form-control form-control-sm" value="@endDate" />
               </div>
               <div class="col-auto">
                   <button type="submit" class="btn btn-primary btn-sm">
                       <i class="bi bi-funnel me-1"></i>Filter
                   </button>
               </div>
               <div class="col-auto">
                   <a href="@Url.Action("AuditLog", "Admin")" class="btn btn-outline-secondary btn-sm">
                       <i class="bi bi-x-circle me-1"></i>Reset
                   </a>
               </div>
               <div class="col-auto ms-auto">
                   <a href="@Url.Action("ExportAuditLog", "Admin", new { startDate = startDate, endDate = endDate })" class="btn btn-success btn-sm">
                       <i class="bi bi-file-earmark-excel me-1"></i>Export Excel
                   </a>
               </div>
           </form>
       </div>
   </div>
   ```

3. Update pagination links to preserve date filter parameters. In each pagination `<a>` tag, add:
   - `asp-route-startDate="@startDate"`
   - `asp-route-endDate="@endDate"`
   This applies to: previous page link, page number links (in the @for loop), and next page link.

4. Update the export button's href to dynamically build from current filter values using a small JS snippet at the bottom:
   Actually, the Url.Action approach above is sufficient since startDate/endDate are server-rendered.
  </action>
  <verify>
    <automated>grep -n "ExportAuditLog\|startDate\|endDate\|Dari Tanggal\|Sampai Tanggal" Views/Admin/AuditLog.cshtml | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "ExportAuditLog" Views/Admin/AuditLog.cshtml returns a match
    - grep "startDate" Views/Admin/AuditLog.cshtml returns at least 3 matches (input, pagination, export link)
    - grep "endDate" Views/Admin/AuditLog.cshtml returns at least 3 matches
    - grep "Dari Tanggal" Views/Admin/AuditLog.cshtml returns a match
    - grep "Sampai Tanggal" Views/Admin/AuditLog.cshtml returns a match
    - grep "asp-route-startDate" Views/Admin/AuditLog.cshtml returns matches in pagination links
    - grep "bi-file-earmark-excel" Views/Admin/AuditLog.cshtml returns a match
  </acceptance_criteria>
  <done>AuditLog view has date filter toolbar with start/end date pickers, Filter button, Reset button, and Export Excel button; pagination preserves filter state</done>
</task>

</tasks>

<verification>
1. `dotnet build` compiles without errors
2. Navigate to /Admin/AuditLog — page loads with filter toolbar, date pickers empty, all records shown
3. Set date range and click Filter — table shows only records in range, badge count updates
4. Click Export Excel with date filter active — downloads .xlsx with only filtered records
5. Click Export Excel with no filter — downloads .xlsx with all records
6. Pagination links preserve date filter parameters
</verification>

<success_criteria>
- AuditLog page has date filter toolbar with Dari Tanggal / Sampai Tanggal inputs
- Filter button narrows both table and export scope
- Export Excel button downloads .xlsx with columns: Waktu, Aktor, Aksi, Detail
- Empty date pickers = all records (no default range)
- Pagination preserves filter state
</success_criteria>

<output>
After completion, create `.planning/phases/178-export-auditlog/178-01-SUMMARY.md`
</output>
