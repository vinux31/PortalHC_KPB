---
phase: 182-page-cdp-coachingproton-kolom-evidence-jelaskan-status-yang-ada-di-kolom-ini
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/CoachingProton.cshtml
autonomous: true
requirements: []

must_haves:
  truths:
    - "When coach submits evidence without a file, the Evidence column shows 'Sudah Upload' (green badge), not 'Belum Upload'"
    - "When status is Approved, the Evidence column shows 'Approved' badge (green+bold)"
    - "When status is Rejected, the Evidence column shows 'Rejected' badge (red)"
    - "When status is Pending, the Evidence column shows 'Belum Upload' (grey badge)"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "EvidenceStatus derived from p.Status instead of p.EvidencePath"
      contains: "EvidenceStatus = p.Status"
    - path: "Views/CDP/CoachingProton.cshtml"
      provides: "Evidence column badges for all four statuses"
      contains: "Approved"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Views/CDP/CoachingProton.cshtml"
      via: "EvidenceStatus property on TrackingItem"
      pattern: "EvidenceStatus"
---

<objective>
Fix the Evidence column in CoachingProton page to display status based on the workflow Status field instead of EvidencePath file presence.

Purpose: When a coach submits evidence without attaching a file, the column currently stays "Belum Upload" even though status changed to "Submitted". This fix maps the Evidence column display to the Status field (Pending/Submitted/Approved/Rejected).
Output: Corrected controller mapping and updated view badges in both multi-coachee and single-coachee tables.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-page-cdp-coachingproton-kolom-evidence-jelaskan-status-yang-ada-di-kolom-ini/182-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix EvidenceStatus mapping in controller and update Evidence column badges in view</name>
  <files>Controllers/CDPController.cs, Views/CDP/CoachingProton.cshtml</files>
  <read_first>
    - Controllers/CDPController.cs (lines 1477-1500 — TrackingItem mapping)
    - Views/CDP/CoachingProton.cshtml (lines 419-437 — coach view evidence column)
    - Views/CDP/CoachingProton.cshtml (lines 535-553 — single-coachee view evidence column)
  </read_first>
  <action>
1. In Controllers/CDPController.cs at line ~1483, change:
   `EvidenceStatus = p.EvidencePath != null ? "Uploaded" : "Pending",`
   to:
   `EvidenceStatus = p.Status,`
   This makes EvidenceStatus carry the actual workflow status (Pending/Submitted/Approved/Rejected).

2. In Views/CDP/CoachingProton.cshtml, update BOTH evidence column blocks (lines ~419-437 AND lines ~535-553). Replace the `else if (item.EvidenceStatus == "Uploaded")` / `else` pattern with a status-aware pattern. The full replacement for each block (the content inside the `<td>` after the coach button `@if` block):

```
else if (item.EvidenceStatus == "Submitted")
{
    <span class="badge bg-success">Sudah Upload</span>
}
else if (item.EvidenceStatus == "Approved")
{
    <span class="badge bg-success fw-bold border border-success">Approved</span>
}
else if (item.EvidenceStatus == "Rejected")
{
    <span class="badge bg-danger">Rejected</span>
}
else
{
    <span class="badge bg-secondary">Belum Upload</span>
}
```

This covers all four statuses:
- Pending → "Belum Upload" (grey) — falls to else since coach sees Submit button when Pending
- Submitted → "Sudah Upload" (green)
- Approved → "Approved" (green bold with border)
- Rejected → "Rejected" (red) — coach sees Submit button, non-coach sees badge

Both evidence column blocks (multi-coachee table ~line 419 and single-coachee table ~line 535) must be updated identically.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs contains `EvidenceStatus = p.Status,` (not `p.EvidencePath != null`)
    - CoachingProton.cshtml contains `item.EvidenceStatus == "Submitted"` (two occurrences)
    - CoachingProton.cshtml contains `item.EvidenceStatus == "Approved"` (two occurrences)
    - CoachingProton.cshtml contains `item.EvidenceStatus == "Rejected"` (two occurrences)
    - CoachingProton.cshtml contains `badge bg-danger` in evidence column context (two occurrences)
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Evidence column shows status-based badges: Pending→"Belum Upload" grey, Submitted→"Sudah Upload" green, Approved→"Approved" green bold, Rejected→"Rejected" red. Both table blocks updated. Build passes.</done>
</task>

</tasks>

<verification>
- `dotnet build` completes with 0 errors
- grep confirms `EvidenceStatus = p.Status` in CDPController.cs
- grep confirms two occurrences each of `EvidenceStatus == "Submitted"`, `EvidenceStatus == "Approved"`, `EvidenceStatus == "Rejected"` in CoachingProton.cshtml
</verification>

<success_criteria>
The Evidence column in CoachingProton page derives its display from the Status field. A coach who submits evidence without a file sees "Sudah Upload" instead of "Belum Upload". All four statuses render with correct badge colors.
</success_criteria>

<output>
After completion, create `.planning/phases/182-page-cdp-coachingproton-kolom-evidence-jelaskan-status-yang-ada-di-kolom-ini/182-01-SUMMARY.md`
</output>
