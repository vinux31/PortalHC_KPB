---
phase: 183-internal-rename-subcompetency-elementeknis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentPackage.cs
  - Models/AssessmentResultsViewModel.cs
  - Controllers/CMPController.cs
  - Controllers/AdminController.cs
  - Views/CMP/Results.cshtml
  - Migrations/
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02
  - RENAME-03
must_haves:
  truths:
    - "PackageQuestion table has column named ElemenTeknis in the database"
    - "No C# source file contains SubCompetency as a property, variable, or method name"
    - "The class previously named SubCompetencyScore is now named ElemenTeknisScore"
    - "Application builds with zero errors"
  artifacts:
    - path: "Models/AssessmentPackage.cs"
      provides: "PackageQuestion.ElemenTeknis property"
      contains: "public string? ElemenTeknis"
    - path: "Models/AssessmentResultsViewModel.cs"
      provides: "ElemenTeknisScore class and ElemenTeknisScores list"
      contains: "class ElemenTeknisScore"
    - path: "Controllers/CMPController.cs"
      provides: "Renamed references to ElemenTeknis"
    - path: "Controllers/AdminController.cs"
      provides: "NormalizeElemenTeknis method and renamed references"
    - path: "Views/CMP/Results.cshtml"
      provides: "Model.ElemenTeknisScores references"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "Models/AssessmentResultsViewModel.cs"
      via: "ElemenTeknisScore class usage"
      pattern: "ElemenTeknisScore"
    - from: "Views/CMP/Results.cshtml"
      to: "Models/AssessmentResultsViewModel.cs"
      via: "Model.ElemenTeknisScores"
      pattern: "Model\\.ElemenTeknisScores"
---

<objective>
Rename all internal C# references from SubCompetency to ElemenTeknis, including the DB column via EF Core migration.

Purpose: Align internal code with the user-facing terminology change done in v7.0, preparing clean naming for Phase 184 shuffle algorithm work.
Output: Renamed model properties, ViewModel class, controller code, view references, and a new EF Core migration.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename model properties, ViewModel class, and all C# references</name>
  <files>Models/AssessmentPackage.cs, Models/AssessmentResultsViewModel.cs, Controllers/CMPController.cs, Controllers/AdminController.cs, Views/CMP/Results.cshtml</files>
  <read_first>Models/AssessmentPackage.cs, Models/AssessmentResultsViewModel.cs, Controllers/CMPController.cs, Controllers/AdminController.cs, Views/CMP/Results.cshtml</read_first>
  <action>
Perform the following renames across all listed files:

**Models/AssessmentPackage.cs (line 44):**
- `public string? SubCompetency { get; set; }` → `public string? ElemenTeknis { get; set; }`

**Models/AssessmentResultsViewModel.cs:**
- Rename class `SubCompetencyScore` → `ElemenTeknisScore`
- Rename property `List<SubCompetencyScore>? SubCompetencyScores` → `List<ElemenTeknisScore>? ElemenTeknisScores`

**Controllers/CMPController.cs (6 occurrences):**
- `List<SubCompetencyScore>? subCompScores` → `List<ElemenTeknisScore>? elemenTeknisScores`
- `hasRealSubCompetency` variable → `hasRealElemenTeknis`
- All `.SubCompetency` property access → `.ElemenTeknis`
- `SubCompetencyScores = subCompScores` → `ElemenTeknisScores = elemenTeknisScores`
- `new SubCompetencyScore` → `new ElemenTeknisScore`

**Controllers/AdminController.cs (7 occurrences):**
- In tuple type: `string? SubCompetency` → `string? ElemenTeknis`
- `SubCompetency = NormalizeSubCompetency(rawSubComp)` → `ElemenTeknis = NormalizeElemenTeknis(rawSubComp)`
- Method `NormalizeSubCompetency` → `NormalizeElemenTeknis`
- All `.SubCompetency` property access → `.ElemenTeknis`
- Update comment "Cross-package SubCompetency mismatch warning" → "Cross-package ElemenTeknis mismatch warning"
- `r.SubCompetency` → `r.ElemenTeknis`
- `pq.SubCompetency` → `pq.ElemenTeknis`

**Views/CMP/Results.cshtml (8 occurrences):**
- All `Model.SubCompetencyScores` → `Model.ElemenTeknisScores`

Do NOT rename anything in .planning/ docs or Migration files. ProtonSubKompetensi is explicitly out of scope.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -rn "SubCompetency" Models/ Controllers/ Views/ --include="*.cs" --include="*.cshtml" | grep -v "ProtonSubKompetensi" | grep -v ".planning/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
- `grep -rn "SubCompetency" Models/ Controllers/ Views/ --include="*.cs" --include="*.cshtml" | grep -v ProtonSubKompetensi` returns 0 lines
- Models/AssessmentPackage.cs contains `public string? ElemenTeknis { get; set; }`
- Models/AssessmentResultsViewModel.cs contains `class ElemenTeknisScore`
- Models/AssessmentResultsViewModel.cs contains `List<ElemenTeknisScore>? ElemenTeknisScores`
- Controllers/AdminController.cs contains `NormalizeElemenTeknis`
- Views/CMP/Results.cshtml contains `Model.ElemenTeknisScores`
  </acceptance_criteria>
  <done>All C# source files and views use ElemenTeknis instead of SubCompetency. Zero SubCompetency references remain in Models/, Controllers/, Views/ (excluding ProtonSubKompetensi).</done>
</task>

<task type="auto">
  <name>Task 2: Create EF Core migration to rename DB column and verify build</name>
  <files>Migrations/</files>
  <read_first>Models/AssessmentPackage.cs, Migrations/20260310014410_AddSubCompetencyToPackageQuestion.cs</read_first>
  <action>
1. Generate EF Core migration to rename the PackageQuestion.SubCompetency column to ElemenTeknis:
   ```
   dotnet ef migrations add RenameSubCompetencyToElemenTeknis
   ```

2. Verify the generated migration contains a `RenameColumn` call (not drop+add):
   - Should contain: `migrationBuilder.RenameColumn(name: "SubCompetency", table: "PackageQuestion", newName: "ElemenTeknis")`
   - If EF generated drop+add instead, manually edit the migration Up/Down methods to use RenameColumn/RenameColumn respectively

3. Apply the migration:
   ```
   dotnet ef database update
   ```

4. Build the entire solution:
   ```
   dotnet build
   ```

Ensure zero build errors and zero warnings related to the rename.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- A migration file exists in Migrations/ with name containing "RenameSubCompetencyToElemenTeknis"
- The migration file contains the string `ElemenTeknis`
- `dotnet build` output contains "Build succeeded" and "0 Warning(s)" or only pre-existing warnings
- `dotnet ef database update` completes without error
- ApplicationDbContextModelSnapshot.cs contains `ElemenTeknis` (not `SubCompetency` for PackageQuestion)
  </acceptance_criteria>
  <done>DB column renamed from SubCompetency to ElemenTeknis via EF Core migration. Application builds successfully with zero errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "SubCompetency" Models/ Controllers/ Views/ --include="*.cs" --include="*.cshtml" | grep -v ProtonSubKompetensi` returns empty
2. `dotnet build` succeeds with 0 errors
3. Migration file exists and contains RenameColumn with ElemenTeknis
4. ApplicationDbContextModelSnapshot.cs references ElemenTeknis for PackageQuestion
</verification>

<success_criteria>
- All RENAME-01, RENAME-02, RENAME-03 requirements satisfied
- Zero SubCompetency references remain in production C# code (excluding ProtonSubKompetensi and old migration files)
- Application compiles and runs
</success_criteria>

<output>
After completion, create `.planning/phases/183-internal-rename-subcompetency-elementeknis/183-01-SUMMARY.md`
</output>
