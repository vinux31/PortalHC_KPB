---
phase: 184-shuffle-algorithm-guaranteed-elemen-teknis-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
autonomous: true
requirements: [SHUF-01, SHUF-03]
gap_closure: true

must_haves:
  truths:
    - "Reshuffle (single + bulk) produces question sets with same ET coverage guarantee as exam start"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "ET-aware BuildCrossPackageAssignment replacing old slot-list algorithm"
      contains: "etGroups"
  key_links:
    - from: "Controllers/AdminController.cs (BuildCrossPackageAssignment)"
      to: "Controllers/AdminController.cs (ReshufflePackage L2828)"
      via: "direct call"
      pattern: "BuildCrossPackageAssignment\\(packages, rng\\)"
    - from: "Controllers/AdminController.cs (BuildCrossPackageAssignment)"
      to: "Controllers/AdminController.cs (ReshuffleAll L2919)"
      via: "direct call"
      pattern: "BuildCrossPackageAssignment\\(packages, rng\\)"
---

<objective>
Replace AdminController's stale BuildCrossPackageAssignment (old slot-list algorithm at L2968-3012) with the ET-aware 3-phase algorithm from CMPController (L1688-1830), so that ReshufflePackage and ReshuffleAll produce ET-guaranteed question sets.

Purpose: Verification found that reshuffle paths call AdminController's local copy which was never updated with Phase 184's ET-aware logic. This is a blocker for SHUF-01 and SHUF-03.
Output: AdminController.BuildCrossPackageAssignment identical to CMPController's version.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-shuffle-algorithm-guaranteed-elemen-teknis-distribution/184-CONTEXT.md
@.planning/phases/184-shuffle-algorithm-guaranteed-elemen-teknis-distribution/184-VERIFICATION.md

<interfaces>
<!-- Source of truth: CMPController.BuildCrossPackageAssignment L1688-1830 -->
<!-- Target: AdminController.BuildCrossPackageAssignment L2968-3012 -->
<!-- Both have identical signature: private static List<int> BuildCrossPackageAssignment(List<AssessmentPackage> packages, Random rng) -->
<!-- AdminController also has Shuffle<T> helper at L2957-2966, same as CMPController L1670-1677 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AdminController.BuildCrossPackageAssignment with ET-aware algorithm</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (lines 1688-1831: the ET-aware BuildCrossPackageAssignment — this is the SOURCE OF TRUTH to copy)
    - Controllers/AdminController.cs (lines 2968-3012: the OLD algorithm to replace)
    - Controllers/AdminController.cs (lines 2955-2966: Shuffle helper — confirm it exists and matches CMPController's version)
    - Controllers/AdminController.cs (lines 2825-2835: ReshufflePackage calling BuildCrossPackageAssignment)
    - Controllers/AdminController.cs (lines 2915-2925: ReshuffleAll calling BuildCrossPackageAssignment)
  </read_first>
  <action>
Replace the ENTIRE body of AdminController.BuildCrossPackageAssignment (L2968-3012) with the exact same logic from CMPController.BuildCrossPackageAssignment (L1688-1830). The method signature stays identical:

```csharp
private static List<int> BuildCrossPackageAssignment(List<AssessmentPackage> packages, Random rng)
```

Copy the full method body from CMPController L1689-1831, which includes:
- L1690-1691: empty packages check returning empty list
- L1694-1702: single-package path (shuffle all question IDs)
- L1704-1707: K computation (min question count) with K==0 early return
- L1709-1711: allQuestions flat list with PackageIndex
- L1713-1718: etGroups distinct non-null ElemenTeknis values
- L1720-1750: fallback to original slot-list when etGroups.Count == 0
- L1752-1774: Phase 1 — one question per ET group (best-effort, capped at K)
- L1776-1826: Phase 2 — fill remaining with balanced package distribution
- L1828-1830: Phase 3 — Fisher-Yates shuffle and return

Include ALL comments from the CMPController version (they document the algorithm phases and edge cases).

Do NOT modify any other code in AdminController. The Shuffle helper at L2957-2966 already exists and matches CMPController's version.

Do NOT extract to a shared class — keep as private static in AdminController (matching CMPController pattern). The duplication is acceptable for this codebase.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs BuildCrossPackageAssignment method contains `etGroups` variable
    - Controllers/AdminController.cs BuildCrossPackageAssignment method contains comment "Phase 1" and "Phase 2"
    - Controllers/AdminController.cs BuildCrossPackageAssignment method contains `ElemenTeknis` reference
    - Controllers/AdminController.cs BuildCrossPackageAssignment method contains `if (etGroups.Count == 0)` fallback check
    - Controllers/AdminController.cs BuildCrossPackageAssignment method contains `selectedIds` HashSet
    - The old slot-list-only code (baseCount/remainder/slots without etGroups check) is gone from BuildCrossPackageAssignment
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>AdminController.BuildCrossPackageAssignment uses the same ET-aware 3-phase algorithm as CMPController, so ReshufflePackage and ReshuffleAll now guarantee ET distribution.</done>
</task>

</tasks>

<verification>
1. `dotnet build` compiles with 0 errors
2. AdminController.BuildCrossPackageAssignment contains Phase 1 ET guarantee logic (grep for "Phase 1" in method)
3. AdminController.BuildCrossPackageAssignment contains fallback for no-ET-data case (grep for "etGroups.Count == 0")
4. ReshufflePackage and ReshuffleAll call sites unchanged (they already call BuildCrossPackageAssignment)
</verification>

<success_criteria>
- AdminController.BuildCrossPackageAssignment body matches CMPController's ET-aware algorithm
- Both reshuffle paths (single + bulk) now produce ET-guaranteed question sets
- Application builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/184-shuffle-algorithm-guaranteed-elemen-teknis-distribution/184-03-SUMMARY.md`
</output>
