---
phase: 185-viewmodel-and-data-model-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/CertificationManagementViewModel.cs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "CertificateStatus enum has exactly 4 values: Aktif, AkanExpired, Expired, Permanent"
    - "SertifikatRow holds unified fields from TrainingRecord and AssessmentSession"
    - "CertificationManagementViewModel wraps a list of SertifikatRow with summary counts"
    - "Status derivation uses 30-day threshold consistent with TrainingRecord.IsExpiringSoon"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "CertificateStatus enum, SertifikatRow, RecordType enum, CertificationManagementViewModel"
      contains: "public enum CertificateStatus"
  key_links:
    - from: "Models/CertificationManagementViewModel.cs"
      to: "Models/TrainingRecord.cs"
      via: "RecordType.Training maps TrainingRecord fields to SertifikatRow"
      pattern: "RecordType.Training"
    - from: "Models/CertificationManagementViewModel.cs"
      to: "Models/AssessmentSession.cs"
      via: "RecordType.Assessment maps AssessmentSession fields to SertifikatRow"
      pattern: "RecordType.Assessment"
---

<objective>
Define the data model foundation for Certification Management: CertificateStatus enum, RecordType enum, SertifikatRow class, and CertificationManagementViewModel with summary card counts.

Purpose: Phase 186-189 depend on these types for query building, view rendering, filtering, and export.
Output: Models/CertificationManagementViewModel.cs with all types needed by downstream phases.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/185-viewmodel-and-data-model-foundation/185-CONTEXT.md

<interfaces>
<!-- Source models the executor must understand for field mapping -->

From Models/TrainingRecord.cs:
- UserId, User (ApplicationUser)
- Judul, Kategori, Tanggal (DateTime), SertifikatUrl
- ValidUntil (DateTime?), CertificateType ("Permanent"/"Annual"/"3-Year")
- NomorSertifikat (string?)
- IsExpiringSoon: ValidUntil within 30 days AND Status == "Valid"

From Models/AssessmentSession.cs:
- UserId, User (ApplicationUser)
- Title, Category, CompletedAt (DateTime?)
- ValidUntil (DateTime?), NomorSertifikat (string?)
- GenerateCertificate (bool), IsPassed (bool?)

From Models/ApplicationUser.cs:
- FullName (string), Section (string?), Unit (string?)

ViewModel pattern from Models/CDPDashboardViewModel.cs:
- Namespace: HcPortal.Models
- File-scoped namespace syntax: namespace HcPortal.Models;
- POCO classes, no constructor logic
- Summary counts as int properties
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CertificationManagementViewModel.cs with all types</name>
  <files>Models/CertificationManagementViewModel.cs</files>
  <read_first>
    - Models/CDPDashboardViewModel.cs (pattern reference for namespace, style)
    - Models/TrainingRecord.cs (source fields for mapping)
    - Models/AssessmentSession.cs (source fields for mapping)
  </read_first>
  <action>
Create Models/CertificationManagementViewModel.cs with file-scoped namespace HcPortal.Models containing:

1. RecordType enum: Training, Assessment

2. CertificateStatus enum: Aktif, AkanExpired, Expired, Permanent

3. SertifikatRow class with these exact properties:
   - int SourceId — TrainingRecord.Id or AssessmentSession.Id
   - RecordType RecordType
   - string NamaWorker — from User.FullName
   - string? Bagian — from User.Section
   - string? Unit — from User.Unit
   - string Judul — TrainingRecord.Judul or AssessmentSession.Title
   - string? Kategori — TrainingRecord.Kategori or AssessmentSession.Category
   - string? NomorSertifikat
   - DateTime? TanggalTerbit — TrainingRecord.Tanggal or AssessmentSession.CompletedAt
   - DateTime? ValidUntil
   - CertificateStatus Status — computed via DeriveCertificateStatus
   - string? SertifikatUrl — TrainingRecord.SertifikatUrl (null for Assessment)

4. Static method on SertifikatRow (or a static helper class):

   public static CertificateStatus DeriveCertificateStatus(DateTime? validUntil, string? certificateType)
   {
       if (certificateType == "Permanent" || validUntil == null)
           return CertificateStatus.Permanent;
       var days = (validUntil.Value - DateTime.Now).Days;
       if (days &lt; 0) return CertificateStatus.Expired;
       if (days &lt;= 30) return CertificateStatus.AkanExpired;
       return CertificateStatus.Aktif;
   }

   For AssessmentSession rows, pass certificateType: null so ValidUntil==null yields Permanent.

5. CertificationManagementViewModel class:
   - List of SertifikatRow Rows (defaults new())
   - int TotalCount
   - int AktifCount
   - int AkanExpiredCount
   - int ExpiredCount
   - int PermanentCount
   - int CurrentPage
   - int TotalPages
   - int PageSize (default 20)

Follow CDPDashboardViewModel style: file-scoped namespace, no constructors, default values via = new() or = 0.
  </action>
  <verify>
    <automated>grep -c "public enum CertificateStatus" Models/CertificationManagementViewModel.cs &amp;&amp; grep -c "public enum RecordType" Models/CertificationManagementViewModel.cs &amp;&amp; grep -c "public class SertifikatRow" Models/CertificationManagementViewModel.cs &amp;&amp; grep -c "public class CertificationManagementViewModel" Models/CertificationManagementViewModel.cs &amp;&amp; grep -c "DeriveCertificateStatus" Models/CertificationManagementViewModel.cs &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Models/CertificationManagementViewModel.cs contains "public enum CertificateStatus" with values Aktif, AkanExpired, Expired, Permanent
    - Models/CertificationManagementViewModel.cs contains "public enum RecordType" with values Training, Assessment
    - Models/CertificationManagementViewModel.cs contains "public class SertifikatRow" with properties: SourceId, RecordType, NamaWorker, Bagian, Unit, Judul, Kategori, NomorSertifikat, TanggalTerbit, ValidUntil, Status, SertifikatUrl
    - Models/CertificationManagementViewModel.cs contains "DeriveCertificateStatus(DateTime? validUntil, string? certificateType)" returning CertificateStatus
    - Models/CertificationManagementViewModel.cs contains "public class CertificationManagementViewModel" with Rows, TotalCount, AktifCount, AkanExpiredCount, ExpiredCount, PermanentCount, CurrentPage, TotalPages, PageSize
    - dotnet build --no-restore completes with 0 errors
  </acceptance_criteria>
  <done>All 4 types (2 enums, SertifikatRow, CertificationManagementViewModel) defined and project compiles successfully</done>
</task>

</tasks>

<verification>
- dotnet build --no-restore succeeds with 0 errors
- grep "CertificateStatus" Models/CertificationManagementViewModel.cs shows enum and usage in SertifikatRow
- All field names match CONTEXT.md specification exactly
</verification>

<success_criteria>
- Models/CertificationManagementViewModel.cs exists with CertificateStatus enum, RecordType enum, SertifikatRow class, and CertificationManagementViewModel class
- Project compiles without errors
- Status derivation logic uses 30-day threshold matching TrainingRecord.IsExpiringSoon
</success_criteria>

<output>
After completion, create .planning/phases/185-viewmodel-and-data-model-foundation/185-01-SUMMARY.md
</output>
