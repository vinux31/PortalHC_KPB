---
phase: 187-full-page-controller-action-and-static-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/CertificationManagement.cshtml
  - Views/CDP/Index.cshtml
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]
must_haves:
  truths:
    - "User bisa klik card Certification Management di CDP/Index dan masuk ke halaman baru"
    - "Halaman menampilkan 4 summary cards: Total, Aktif, Akan Expired, Expired"
    - "Tabel sertifikat menampilkan data dengan status badge berwarna"
    - "Tabel memiliki pagination 20 baris per halaman"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "CertificationManagement GET action"
      contains: "CertificationManagement"
    - path: "Views/CDP/CertificationManagement.cshtml"
      provides: "Full page view with summary cards, table, pagination"
      min_lines: 80
    - path: "Views/CDP/Index.cshtml"
      provides: "Entry card linking to CertificationManagement"
      contains: "CertificationManagement"
  key_links:
    - from: "Views/CDP/Index.cshtml"
      to: "CDPController.CertificationManagement"
      via: "Url.Action link"
      pattern: "Url.Action.*CertificationManagement.*CDP"
    - from: "CDPController.CertificationManagement"
      to: "BuildSertifikatRowsAsync"
      via: "method call"
      pattern: "BuildSertifikatRowsAsync"
    - from: "CDPController.CertificationManagement"
      to: "PaginationHelper.Calculate"
      via: "pagination call"
      pattern: "PaginationHelper.Calculate"
---

<objective>
Buat halaman Certification Management lengkap: controller action, view dengan summary cards + tabel + pagination, dan entry card di CDP/Index.

Purpose: User bisa melihat semua sertifikat (Training + Assessment) dalam satu halaman terpusat dengan overview status.
Output: Halaman CertificationManagement yang berfungsi penuh dengan navigasi dari CDP hub.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From Helpers/PaginationHelper.cs:
```csharp
public record PaginationResult(int CurrentPage, int TotalPages, int TotalCount, int Skip, int Take);
public static class PaginationHelper
{
    public static PaginationResult Calculate(int totalCount, int page, int pageSize);
}
```

From Models/CertificationManagementViewModel.cs:
```csharp
public enum RecordType { Training, Assessment }
public enum CertificateStatus { Aktif, AkanExpired, Expired, Permanent }

public class SertifikatRow
{
    public int SourceId { get; set; }
    public RecordType RecordType { get; set; }
    public string NamaWorker { get; set; }
    public string? Bagian { get; set; }
    public string? Unit { get; set; }
    public string Judul { get; set; }
    public string? Kategori { get; set; }
    public string? NomorSertifikat { get; set; }
    public DateTime? TanggalTerbit { get; set; }
    public DateTime? ValidUntil { get; set; }
    public CertificateStatus Status { get; set; }
    public string? SertifikatUrl { get; set; }
}

public class CertificationManagementViewModel
{
    public List<SertifikatRow> Rows { get; set; }
    public int TotalCount { get; set; }
    public int AktifCount { get; set; }
    public int AkanExpiredCount { get; set; }
    public int ExpiredCount { get; set; }
    public int PermanentCount { get; set; }
    public int CurrentPage { get; set; }
    public int TotalPages { get; set; }
    public int PageSize { get; set; } = 20;
}
```

From Controllers/CDPController.cs (Phase 186):
```csharp
private async Task<List<SertifikatRow>> BuildSertifikatRowsAsync();
```

CDP/Index card pattern: Bootstrap card with icon-box, h5 title, small subtitle, paragraph description, full-width button.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller action + entry card di CDP/Index</name>
  <files>Controllers/CDPController.cs, Views/CDP/Index.cshtml</files>
  <read_first>Controllers/CDPController.cs, Views/CDP/Index.cshtml, Models/CertificationManagementViewModel.cs, Helpers/PaginationHelper.cs</read_first>
  <action>
1. Di CDPController.cs, tambahkan action method CertificationManagement sebelum region private helpers (atau sebelum BuildSertifikatRowsAsync). Action menerima parameter `int page = 1`:

```csharp
public async Task<IActionResult> CertificationManagement(int page = 1)
{
    var allRows = await BuildSertifikatRowsAsync();

    // Sort: TanggalTerbit descending (terbaru dulu)
    allRows = allRows.OrderByDescending(r => r.TanggalTerbit).ToList();

    // Summary counts dari FULL dataset (sebelum pagination)
    var vm = new CertificationManagementViewModel
    {
        TotalCount = allRows.Count,
        AktifCount = allRows.Count(r => r.Status == CertificateStatus.Aktif),
        AkanExpiredCount = allRows.Count(r => r.Status == CertificateStatus.AkanExpired),
        ExpiredCount = allRows.Count(r => r.Status == CertificateStatus.Expired),
        PermanentCount = allRows.Count(r => r.Status == CertificateStatus.Permanent),
    };

    // Pagination
    var paging = PaginationHelper.Calculate(allRows.Count, page, vm.PageSize);
    vm.Rows = allRows.Skip(paging.Skip).Take(paging.Take).ToList();
    vm.CurrentPage = paging.CurrentPage;
    vm.TotalPages = paging.TotalPages;

    return View(vm);
}
```

2. Di Views/CDP/Index.cshtml, tambahkan card baru di dalam row kedua (setelah Dashboard Monitoring card, sebelum closing `</div>` row). Ikuti pattern card yang sudah ada:

```html
<!-- Certification Management -->
<div class="col-12 col-md-6 col-lg-4">
    <div class="card border-0 shadow-sm h-100">
        <div class="card-body d-flex flex-column">
            <div class="d-flex align-items-center mb-3">
                <div class="icon-box bg-success bg-opacity-10 text-success rounded-3 p-3 me-3">
                    <i class="bi bi-patch-check fs-3"></i>
                </div>
                <div>
                    <h5 class="mb-0">Certification Management</h5>
                    <small class="text-muted">Kelola Sertifikat</small>
                </div>
            </div>
            <p class="text-muted mb-3">Lihat dan kelola semua sertifikat pelatihan dan asesmen pekerja</p>
            <a href="@Url.Action("CertificationManagement", "CDP")" class="btn btn-success w-100 mt-auto">
                <i class="bi bi-arrow-right-circle me-2"></i>Kelola Sertifikat
            </a>
        </div>
    </div>
</div>
```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs contains "public async Task<IActionResult> CertificationManagement(int page = 1)"
    - CDPController.cs contains "BuildSertifikatRowsAsync()"
    - CDPController.cs contains "PaginationHelper.Calculate(allRows.Count, page, vm.PageSize)"
    - CDPController.cs contains "OrderByDescending(r => r.TanggalTerbit)"
    - Views/CDP/Index.cshtml contains "CertificationManagement"
    - Views/CDP/Index.cshtml contains "bi-patch-check"
    - Views/CDP/Index.cshtml contains "Kelola Sertifikat"
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>CertificationManagement action mengembalikan ViewModel dengan summary counts dan paginated rows; CDP/Index punya card navigasi ke halaman baru</done>
</task>

<task type="auto">
  <name>Task 2: View CertificationManagement.cshtml dengan summary cards, tabel, dan pagination</name>
  <files>Views/CDP/CertificationManagement.cshtml</files>
  <read_first>Models/CertificationManagementViewModel.cs, Views/Admin/AuditLog.cshtml, Views/CDP/Index.cshtml</read_first>
  <action>
Buat file Views/CDP/CertificationManagement.cshtml baru. Struktur:

1. **Header** — model directive `@model CertificationManagementViewModel`, title, back button ke CDP/Index.

2. **4 Summary Cards** (row, col-md-3 masing-masing):
   - Total Sertifikat: `Model.TotalCount`, warna primary, icon bi-award
   - Aktif: `Model.AktifCount`, warna success (hijau), icon bi-check-circle
   - Akan Expired: `Model.AkanExpiredCount`, warna warning (kuning), icon bi-exclamation-triangle
   - Expired: `Model.ExpiredCount`, warna danger (merah), icon bi-x-circle

3. **Tabel** (table-responsive, table-striped, table-hover):
   - Header: No, Nama, Bagian (d-none d-md-table-cell), Unit (d-none d-md-table-cell), Judul, Kategori (d-none d-md-table-cell), Nomor Sertifikat, Tanggal Terbit, Valid Until, Tipe, Status
   - Body: loop `Model.Rows` dengan index `(Model.CurrentPage - 1) * Model.PageSize + i + 1` untuk nomor urut
   - Status badge:
     - Aktif → `<span class="badge bg-success">Aktif</span>`
     - AkanExpired → `<span class="badge bg-warning text-dark">Akan Expired</span>`
     - Expired → `<span class="badge bg-danger">Expired</span>`
     - Permanent → `<span class="badge bg-secondary">Permanent</span>`
   - RecordType badge:
     - Training → `<span class="badge bg-primary">Training</span>`
     - Assessment → `<span class="badge bg-purple" style="background-color: #6f42c1 !important;">Assessment</span>`
   - TanggalTerbit dan ValidUntil format: `row.TanggalTerbit?.ToString("dd MMM yyyy")` — ValidUntil tampilkan "-" jika null
   - Empty state: jika `Model.Rows.Count == 0`, tampilkan `<tr><td colspan="11" class="text-center text-muted py-4">Belum ada data sertifikat</td></tr>`

4. **Pagination** (jika TotalPages > 1):
   - `<nav>` dengan `<ul class="pagination justify-content-center">`
   - Previous button (disabled jika page 1)
   - Page numbers loop 1..TotalPages
   - Next button (disabled jika last page)
   - Semua link pakai `asp-action="CertificationManagement" asp-route-page="@i"`
   - Active page: `class="page-item active"`

5. **Info text** di bawah pagination: "Menampilkan X - Y dari Z sertifikat" dimana X = `(Model.CurrentPage-1)*Model.PageSize+1`, Y = `Math.Min(Model.CurrentPage*Model.PageSize, Model.TotalCount)`.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/CDP/CertificationManagement.cshtml exists and has more than 80 lines
    - File contains "@model HcPortal.Models.CertificationManagementViewModel" or "@model CertificationManagementViewModel"
    - File contains "Model.TotalCount"
    - File contains "Model.AktifCount"
    - File contains "Model.AkanExpiredCount"
    - File contains "Model.ExpiredCount"
    - File contains "badge bg-success" (Aktif badge)
    - File contains "badge bg-warning" (AkanExpired badge)
    - File contains "badge bg-danger" (Expired badge)
    - File contains "badge bg-secondary" (Permanent badge)
    - File contains "d-none d-md-table-cell" (responsive columns)
    - File contains "Belum ada data sertifikat" (empty state)
    - File contains "pagination justify-content-center"
    - File contains "asp-route-page"
    - File contains "CertificationManagement" in asp-action
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>View menampilkan 4 summary cards, tabel dengan status/tipe badges, responsive columns, empty state, dan pagination lengkap</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` sukses tanpa error
- `grep -c "CertificationManagement" Controllers/CDPController.cs` menunjukkan minimal 2 hits
- `grep -c "CertificationManagement" Views/CDP/Index.cshtml` menunjukkan minimal 1 hit
- `wc -l Views/CDP/CertificationManagement.cshtml` menunjukkan lebih dari 80 baris
- `grep "PaginationHelper.Calculate" Controllers/CDPController.cs` ada hit
- `grep "badge bg-success" Views/CDP/CertificationManagement.cshtml` ada hit
- `grep "badge bg-danger" Views/CDP/CertificationManagement.cshtml` ada hit
</verification>

<success_criteria>
- Halaman CDP/Index menampilkan card "Certification Management" yang bisa diklik
- CDPController.CertificationManagement action mengembalikan view dengan data terisi
- View menampilkan 4 summary cards dengan angka yang benar
- Tabel menampilkan sertifikat dengan badge status berwarna dan badge tipe
- Kolom Bagian, Unit, Kategori hidden di mobile
- Empty state ditampilkan jika tidak ada data
- Pagination berfungsi dengan 20 baris per halaman
- Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/187-full-page-controller-action-and-static-view/187-01-SUMMARY.md`
</output>
