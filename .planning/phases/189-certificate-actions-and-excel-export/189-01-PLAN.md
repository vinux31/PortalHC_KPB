---
phase: 189-certificate-actions-and-excel-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml
  - Views/CDP/CertificationManagement.cshtml
  - Controllers/CDPController.cs
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03]

must_haves:
  truths:
    - "Kolom Aksi muncul di tabel sertifikat dengan icon per baris"
    - "Training dengan SertifikatUrl menampilkan icon eye yang membuka URL di tab baru"
    - "Training tanpa SertifikatUrl menampilkan dash tanpa link"
    - "Assessment menampilkan icon download yang membuka CMP/CertificatePdf di tab baru"
    - "Admin/HC melihat tombol Export Excel di header halaman"
    - "User biasa tidak melihat tombol Export Excel"
    - "Export menghasilkan file Excel dengan data sesuai filter aktif"
  artifacts:
    - path: "Views/CDP/Shared/_CertificationManagementTablePartial.cshtml"
      provides: "Kolom Aksi dengan conditional icon links"
      contains: "Aksi"
    - path: "Views/CDP/CertificationManagement.cshtml"
      provides: "Export button role-gated + exportExcel JS function"
      contains: "ExportSertifikatExcel"
    - path: "Controllers/CDPController.cs"
      provides: "ExportSertifikatExcel action"
      contains: "ExportSertifikatExcel"
  key_links:
    - from: "Views/CDP/CertificationManagement.cshtml"
      to: "Controllers/CDPController.cs"
      via: "exportExcel JS -> window.location.href /CDP/ExportSertifikatExcel"
      pattern: "ExportSertifikatExcel"
    - from: "Views/CDP/Shared/_CertificationManagementTablePartial.cshtml"
      to: "Controllers/CMPController.cs"
      via: "asp-action=CertificatePdf link"
      pattern: "CertificatePdf"
---

<objective>
Tambah kolom Aksi di tabel sertifikat untuk view/download individual, dan tombol Export Excel (Admin/HC only) yang mengexport filtered data menggunakan ExcelExportHelper.

Purpose: Melengkapi halaman CertificationManagement dengan aksi per-baris dan bulk export.
Output: Kolom Aksi di tabel, export button di header, ExportSertifikatExcel action di CDPController.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-certificate-actions-and-excel-export/189-CONTEXT.md
@.planning/phases/189-certificate-actions-and-excel-export/189-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah kolom Aksi di partial + export button dan JS di main view</name>
  <files>Views/CDP/Shared/_CertificationManagementTablePartial.cshtml, Views/CDP/CertificationManagement.cshtml</files>
  <read_first>Views/CDP/Shared/_CertificationManagementTablePartial.cshtml, Views/CDP/CertificationManagement.cshtml, Models/CertificationManagementViewModel.cs</read_first>
  <action>
**Partial view (_CertificationManagementTablePartial.cshtml):**

1. Tambah `<th>Aksi</th>` sebagai kolom terakhir di thead.

2. Di dalam foreach loop baris data, tambah `<td>` terakhir dengan logic:
   - Jika `row.RecordType == RecordType.Training`:
     - Jika `!string.IsNullOrEmpty(row.SertifikatUrl)`: tampilkan `<a href="@row.SertifikatUrl" target="_blank" class="btn btn-sm btn-outline-primary" title="Lihat Sertifikat"><i class="bi bi-eye"></i></a>`
     - Else: tampilkan `<span class="text-muted">-</span>`
   - Else (Assessment): tampilkan `<a asp-controller="CMP" asp-action="CertificatePdf" asp-route-id="@row.SourceId" target="_blank" class="btn btn-sm btn-outline-primary" title="Download Sertifikat"><i class="bi bi-download"></i></a>`

3. Update colspan di row "Belum ada data sertifikat" dari 11 menjadi 12.

**Main view (CertificationManagement.cshtml):**

4. Di header area (dekat tombol "Kembali ke CDP"), tambah export button SEBELUM tombol kembali, wrapped dalam `<div class="d-flex gap-2">`:
```razor
@if (User.IsInRole("Admin") || User.IsInRole("HC"))
{
    <a id="btn-export" href="#" class="btn btn-outline-success" onclick="exportExcel(event)">
        <i class="bi bi-file-earmark-excel me-1"></i>Export Excel
    </a>
}
```

5. Tambahkan function `exportExcel` DI DALAM IIFE yang sudah ada (agar variabel filter bagianEl, unitEl, statusEl, tipeEl, searchEl dalam scope):
```javascript
function exportExcel(e) {
    e.preventDefault();
    var params = new URLSearchParams();
    if (bagianEl.value) params.set('bagian', bagianEl.value);
    if (unitEl.value) params.set('unit', unitEl.value);
    if (statusEl.value) params.set('status', statusEl.value);
    if (tipeEl.value) params.set('tipe', tipeEl.value);
    if (searchEl.value.trim()) params.set('search', searchEl.value.trim());
    window.location.href = '/CDP/ExportSertifikatExcel?' + params.toString();
}
```
Pastikan function ini di DALAM IIFE, bukan di luar, agar bisa akses variabel filter. Karena onclick="exportExcel(event)" butuh akses global, expose function: `window.exportExcel = exportExcel;` di akhir IIFE, atau gunakan event listener approach. Rekomendasi: tambahkan `window.exportExcel = exportExcel;` di dalam IIFE setelah definisi function.
  </action>
  <verify>
    <automated>grep -c "Aksi" Views/CDP/Shared/_CertificationManagementTablePartial.cshtml && grep -c "ExportSertifikatExcel" Views/CDP/CertificationManagement.cshtml && grep -c "exportExcel" Views/CDP/CertificationManagement.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - grep "Aksi" di partial view menemukan minimal 1 match (th header)
    - grep "bi-eye" di partial view menemukan match (icon Training)
    - grep "bi-download" di partial view menemukan match (icon Assessment)
    - grep "CertificatePdf" di partial view menemukan match (asp-action link)
    - grep 'colspan="12"' di partial view menemukan match (updated colspan)
    - grep "btn-outline-success" di main view menemukan match (export button)
    - grep "ExportSertifikatExcel" di main view menemukan match (JS URL)
    - grep "exportExcel" di main view menemukan match (JS function)
    - grep 'User.IsInRole("Admin")' di main view menemukan match (role gate)
  </acceptance_criteria>
  <done>Kolom Aksi muncul di tabel dengan conditional icon links per RecordType. Export button role-gated tampil di header dengan JS yang membaca filter aktif.</done>
</task>

<task type="auto">
  <name>Task 2: Tambah ExportSertifikatExcel action di CDPController</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>Controllers/CDPController.cs (cari FilterCertificationManagement action untuk duplikasi filter logic, dan cari contoh ExcelExportHelper usage di file yang sama), Helpers/ExcelExportHelper.cs</read_first>
  <action>
Tambah action baru `ExportSertifikatExcel` di CDPController, dekat action `FilterCertificationManagement`.

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ExportSertifikatExcel(
    string? bagian = null,
    string? unit = null,
    string? status = null,
    string? tipe = null,
    string? search = null)
{
    var allRows = await BuildSertifikatRowsAsync();

    // Filter logic duplikasi dari FilterCertificationManagement
    if (!string.IsNullOrEmpty(bagian))
        allRows = allRows.Where(r => r.Bagian == bagian).ToList();
    if (!string.IsNullOrEmpty(unit))
        allRows = allRows.Where(r => r.Unit == unit).ToList();
    if (!string.IsNullOrEmpty(status) && Enum.TryParse<CertificateStatus>(status, out var st))
        allRows = allRows.Where(r => r.Status == st).ToList();
    if (!string.IsNullOrEmpty(tipe) && Enum.TryParse<RecordType>(tipe, out var rt))
        allRows = allRows.Where(r => r.RecordType == rt).ToList();
    if (!string.IsNullOrEmpty(search))
    {
        var q = search.ToLower();
        allRows = allRows.Where(r =>
            r.NamaWorker.ToLower().Contains(q) ||
            r.Judul.ToLower().Contains(q) ||
            (r.NomorSertifikat?.ToLower().Contains(q) ?? false)
        ).ToList();
    }
    allRows = allRows.OrderByDescending(r => r.TanggalTerbit).ToList();

    using var workbook = new XLWorkbook();
    var ws = ExcelExportHelper.CreateSheet(workbook, "Sertifikat", new[]
    {
        "No", "Nama", "Bagian", "Unit", "Judul", "Kategori",
        "Nomor Sertifikat", "Tgl Terbit", "Valid Until", "Tipe", "Status", "Sertifikat URL"
    });

    for (int i = 0; i < allRows.Count; i++)
    {
        var r = allRows[i];
        var row = i + 2;
        ws.Cell(row, 1).Value = i + 1;
        ws.Cell(row, 2).Value = r.NamaWorker;
        ws.Cell(row, 3).Value = r.Bagian ?? "";
        ws.Cell(row, 4).Value = r.Unit ?? "";
        ws.Cell(row, 5).Value = r.Judul;
        ws.Cell(row, 6).Value = r.Kategori ?? "";
        ws.Cell(row, 7).Value = r.NomorSertifikat ?? "";
        ws.Cell(row, 8).Value = r.TanggalTerbit?.ToString("dd MMM yyyy") ?? "";
        ws.Cell(row, 9).Value = r.ValidUntil?.ToString("dd MMM yyyy") ?? "";
        ws.Cell(row, 10).Value = r.RecordType.ToString();
        ws.Cell(row, 11).Value = r.Status.ToString();
        ws.Cell(row, 12).Value = r.SertifikatUrl ?? "";
    }

    var fileName = $"Sertifikat_Export_{DateTime.Now:yyyy-MM-dd}.xlsx";
    return ExcelExportHelper.ToFileResult(workbook, fileName, this);
}
```

Pastikan `using ClosedXML.Excel;` dan `using PortalHC_KPB.Helpers;` sudah ada di file (kemungkinan sudah ada karena ExcelExportHelper dipakai di action lain). Periksa juga `using PortalHC_KPB.Models;` untuk CertificateStatus dan RecordType enum.
  </action>
  <verify>
    <automated>grep -c "ExportSertifikatExcel" Controllers/CDPController.cs && grep -c 'Authorize(Roles = "Admin, HC")' Controllers/CDPController.cs && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "ExportSertifikatExcel" di CDPController.cs menemukan minimal 1 match
    - grep 'Authorize(Roles = "Admin, HC")' di CDPController.cs menemukan match pada action export
    - grep "ExcelExportHelper.CreateSheet" di CDPController.cs menemukan match
    - grep "ExcelExportHelper.ToFileResult" di CDPController.cs menemukan match
    - grep "Sertifikat_Export_" di CDPController.cs menemukan match (filename pattern)
    - grep "BuildSertifikatRowsAsync" di action ExportSertifikatExcel menemukan match
    - dotnet build berhasil tanpa error
  </acceptance_criteria>
  <done>Action ExportSertifikatExcel tersedia di CDPController, di-authorize untuk Admin/HC, menggunakan ExcelExportHelper, dan menghasilkan file Excel dengan 12 kolom sesuai filter aktif. Build sukses.</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil tanpa error
- Kolom Aksi muncul di tabel partial view
- Export button role-gated di main view
- ExportSertifikatExcel action ada di CDPController dengan [Authorize(Roles = "Admin, HC")]
- JS exportExcel function dalam scope yang benar (di dalam IIFE)
</verification>

<success_criteria>
- Tabel sertifikat menampilkan kolom Aksi dengan icon eye (Training+URL), dash (Training tanpa URL), atau download (Assessment)
- Admin/HC melihat tombol Export Excel di header
- Klik export menghasilkan file Sertifikat_Export_{tanggal}.xlsx dengan data ter-filter
- Build project sukses
</success_criteria>

<output>
After completion, create `.planning/phases/189-certificate-actions-and-excel-export/189-01-SUMMARY.md`
</output>
