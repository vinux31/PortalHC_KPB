---
phase: 190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/CertificationManagementViewModel.cs
  - Controllers/CDPController.cs
autonomous: true
requirements:
  - ROLE-SCOPE
  - CATEGORY-FILTER
  - SUBCATEGORY-COL
  - EXPORT-SUBCOL

must_haves:
  truths:
    - "SertifikatRow memiliki property SubKategori"
    - "CertificationManagementViewModel memiliki property RoleLevel"
    - "BuildSertifikatRowsAsync menerima parameter l5OwnDataOnly dan saat true, L5 hanya return data diri sendiri"
    - "GetSubCategories endpoint mengembalikan children AssessmentCategory berdasarkan parent name"
    - "FilterCertificationManagement menerima parameter category dan subCategory"
    - "ExportSertifikatExcel menerima parameter category dan subCategory serta menambah kolom Sub Kategori"
    - "CertificationManagement dan FilterCertificationManagement set vm.RoleLevel dari GetCurrentUserRoleLevelAsync"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "SubKategori field di SertifikatRow, RoleLevel di ViewModel"
      contains: "SubKategori"
    - path: "Controllers/CDPController.cs"
      provides: "GetSubCategories endpoint, l5OwnDataOnly parameter, category/subCategory filter params, RoleLevel propagation"
      contains: "GetSubCategories"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Models/CertificationManagementViewModel.cs"
      via: "vm.RoleLevel assignment dan SubKategori field population"
      pattern: "RoleLevel = roleLevel"
---

<objective>
Modifikasi backend: tambah SubKategori ke SertifikatRow, RoleLevel ke ViewModel, override L5 scope di BuildSertifikatRowsAsync, endpoint GetSubCategories untuk cascade AJAX, parameter category/subCategory di filter dan export, serta kolom Sub Kategori di Excel export.

Purpose: Menyiapkan semua perubahan model dan controller yang dibutuhkan view layer.
Output: Model dan controller siap untuk rendering role-based di Plan 02.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic/190-CONTEXT.md
@.planning/phases/190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Model — SubKategori dan RoleLevel</name>
  <files>Models/CertificationManagementViewModel.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs
  </read_first>
  <action>
1. Tambah property ke SertifikatRow (setelah Kategori):
   ```csharp
   public string? SubKategori { get; set; }
   ```

2. Tambah property ke CertificationManagementViewModel (setelah PageSize):
   ```csharp
   public int RoleLevel { get; set; } = 1;
   ```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - Models/CertificationManagementViewModel.cs contains `public string? SubKategori { get; set; }`
    - Models/CertificationManagementViewModel.cs contains `public int RoleLevel { get; set; } = 1;`
  </acceptance_criteria>
  <done>SertifikatRow punya SubKategori, ViewModel punya RoleLevel</done>
</task>

<task type="auto">
  <name>Task 2: Update CDPController — L5 override, GetSubCategories, filter params, RoleLevel propagation, export column</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs (lines 3026-3280 — CertificationManagement actions dan BuildSertifikatRowsAsync)
    - Controllers/CDPController.cs (line 288 — GetCascadeOptions pattern)
    - Models/AssessmentCategory.cs
  </read_first>
  <action>
**A. BuildSertifikatRowsAsync — L5 override + SubKategori population + return roleLevel**

Ubah signature menjadi:
```csharp
private async Task<(List<SertifikatRow> rows, int roleLevel)> BuildSertifikatRowsAsync(bool l5OwnDataOnly = false)
```

Di blok L5 (roleLevel == 5), tambah branching:
```csharp
else if (roleLevel == 5)
{
    if (l5OwnDataOnly)
    {
        scopedUserIds = new List<string> { user.Id };
    }
    else
    {
        var coacheeIds = await _context.CoachCoacheeMappings
            .Where(m => m.CoachId == user.Id && m.IsActive)
            .Select(m => m.CoacheeId)
            .ToListAsync();
        coacheeIds.Add(user.Id);
        scopedUserIds = coacheeIds;
    }
}
```

Sebelum merge, load lookup sub-category dan populate SubKategori untuk Assessment rows:
```csharp
// Load category -> sub-categories lookup (untuk Assessment rows)
var categoryChildrenLookup = await _context.AssessmentCategories
    .Include(c => c.Children)
    .Where(c => c.ParentId == null && c.IsActive)
    .ToDictionaryAsync(
        c => c.Name,
        c => c.Children.Where(ch => ch.IsActive).Select(ch => ch.Name).ToList());
```

Saat mapping assessmentAnon ke SertifikatRow, set:
```csharp
SubKategori = null // AssessmentSession tidak menyimpan sub-category
```

Saat mapping trainingAnon ke SertifikatRow, set:
```csharp
SubKategori = null // Training tidak punya sub-category
```

Return `(rows, roleLevel)` di akhir method.

**B. CertificationManagement action — call dengan l5OwnDataOnly: true + set RoleLevel + ViewBag.AllCategories**

```csharp
var (allRows, roleLevel) = await BuildSertifikatRowsAsync(l5OwnDataOnly: true);
```

Set di vm:
```csharp
vm.RoleLevel = roleLevel;
```

Tambah ViewBag sebelum return:
```csharp
ViewBag.AllCategories = await _context.AssessmentCategories
    .Where(c => c.ParentId == null && c.IsActive)
    .OrderBy(c => c.SortOrder)
    .Select(c => c.Name)
    .ToListAsync();
ViewBag.UserBagian = (await GetCurrentUserRoleLevelAsync()).user.Section;
```

**C. FilterCertificationManagement — tambah parameter category, subCategory + RoleLevel**

Tambah parameter:
```csharp
string? category = null,
string? subCategory = null,
```

Call:
```csharp
var (allRows, roleLevel) = await BuildSertifikatRowsAsync(l5OwnDataOnly: true);
```

Tambah filter setelah existing filters (sebelum sort):
```csharp
if (!string.IsNullOrEmpty(category))
    allRows = allRows.Where(r => r.RecordType == RecordType.Assessment && r.Kategori == category).ToList();
if (!string.IsNullOrEmpty(subCategory))
    allRows = allRows.Where(r => r.RecordType == RecordType.Assessment && r.SubKategori == subCategory).ToList();
```

Set `vm.RoleLevel = roleLevel;` sebelum return partial.

**D. ExportSertifikatExcel — tambah parameter category, subCategory + kolom Sub Kategori**

Tambah parameter:
```csharp
string? category = null,
string? subCategory = null,
```

Call:
```csharp
var (allRows, _) = await BuildSertifikatRowsAsync(l5OwnDataOnly: true);
```

Tambah filter yang sama dengan FilterCertificationManagement (category, subCategory).

Update header array — tambah "Sub Kategori" setelah "Kategori":
```csharp
"No", "Nama", "Bagian", "Unit", "Judul", "Kategori", "Sub Kategori",
"Nomor Sertifikat", "Tgl Terbit", "Valid Until", "Tipe", "Status", "Sertifikat URL"
```

Shift cell indices setelah Kategori (kolom 7 = Sub Kategori, sisanya geser +1):
```csharp
ws.Cell(row, 6).Value = r.Kategori ?? "";
ws.Cell(row, 7).Value = r.SubKategori ?? "";
ws.Cell(row, 8).Value = r.NomorSertifikat ?? "";
ws.Cell(row, 9).Value = r.TanggalTerbit?.ToString("dd MMM yyyy") ?? "";
ws.Cell(row, 10).Value = r.ValidUntil?.ToString("dd MMM yyyy") ?? "";
ws.Cell(row, 11).Value = r.RecordType.ToString();
ws.Cell(row, 12).Value = r.Status.ToString();
ws.Cell(row, 13).Value = r.SertifikatUrl ?? "";
```

**E. GetSubCategories endpoint — setelah GetCascadeOptions**

```csharp
[HttpGet]
public async Task<IActionResult> GetSubCategories(string? category)
{
    if (string.IsNullOrEmpty(category))
        return Json(new List<string>());

    var subCategories = await _context.AssessmentCategories
        .Where(c => c.ParentId != null && c.Parent!.Name == category && c.IsActive)
        .OrderBy(c => c.SortOrder)
        .Select(c => c.Name)
        .ToListAsync();

    return Json(subCategories);
}
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/CDPController.cs contains `private async Task<(List<SertifikatRow> rows, int roleLevel)> BuildSertifikatRowsAsync(bool l5OwnDataOnly = false)`
    - Controllers/CDPController.cs contains `if (l5OwnDataOnly)`
    - Controllers/CDPController.cs contains `public async Task<IActionResult> GetSubCategories(string? category)`
    - Controllers/CDPController.cs contains `string? category = null` in FilterCertificationManagement signature
    - Controllers/CDPController.cs contains `string? subCategory = null` in FilterCertificationManagement signature
    - Controllers/CDPController.cs contains `vm.RoleLevel = roleLevel` (at least 2 occurrences — CertificationManagement + FilterCertificationManagement)
    - Controllers/CDPController.cs contains `"Sub Kategori"` in ExportSertifikatExcel header array
    - Controllers/CDPController.cs contains `l5OwnDataOnly: true` (3 occurrences — CertificationManagement, Filter, Export)
    - Controllers/CDPController.cs contains `ViewBag.AllCategories`
    - Controllers/CDPController.cs contains `ViewBag.UserBagian`
  </acceptance_criteria>
  <done>Semua backend perubahan selesai: L5 override, GetSubCategories, filter params, RoleLevel propagation, export kolom baru</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil tanpa error
- BuildSertifikatRowsAsync return tuple (rows, roleLevel)
- GetSubCategories endpoint terdaftar
- Filter dan export menerima category/subCategory
</verification>

<success_criteria>
- Build sukses
- Model punya SubKategori dan RoleLevel
- Controller punya semua perubahan yang dibutuhkan Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic/190-01-SUMMARY.md`
</output>
