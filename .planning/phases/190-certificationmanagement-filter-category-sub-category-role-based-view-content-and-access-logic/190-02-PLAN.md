---
phase: 190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic
plan: 02
type: execute
wave: 2
depends_on: ["190-01"]
files_modified:
  - Views/CDP/CertificationManagement.cshtml
  - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml
autonomous: false
requirements:
  - ROLE-VIEW
  - FILTER-CASCADE
  - SUBCATEGORY-DISPLAY

must_haves:
  truths:
    - "L5/L6 tidak melihat summary cards"
    - "L5/L6 tidak melihat kolom Nama, Bagian, Unit di tabel"
    - "L4 melihat filter Bagian disabled dan pre-filled dengan bagian sendiri"
    - "L5/L6 melihat filter Bagian dan Unit disabled"
    - "Kolom Sub Kategori tampil di tabel untuk semua role"
    - "Filter Category dropdown terisi dari AssessmentCategory parents"
    - "Pilih Category memicu cascade fetch Sub-Category"
    - "Filter category/subCategory dikirim ke AJAX refresh dan export"
  artifacts:
    - path: "Views/CDP/CertificationManagement.cshtml"
      provides: "Filter Category/Sub-Category dropdowns, role-based filter disabled state, role-based summary card visibility, JS cascade"
      contains: "filter-category"
    - path: "Views/CDP/Shared/_CertificationManagementTablePartial.cshtml"
      provides: "Kolom Sub Kategori, role-based column visibility"
      contains: "SubKategori"
  key_links:
    - from: "Views/CDP/CertificationManagement.cshtml"
      to: "Controllers/CDPController.cs"
      via: "AJAX fetch GetSubCategories + FilterCertificationManagement with category/subCategory params"
      pattern: "GetSubCategories"
---

<objective>
Modifikasi frontend: tambah filter Category/Sub-Category dengan cascade AJAX, conditional rendering berdasarkan RoleLevel (summary cards, kolom tabel, filter disabled state), dan kolom Sub Kategori di tabel.

Purpose: Implementasi semua perubahan UI sesuai keputusan user untuk role-based view content.
Output: Halaman CertificationManagement dengan filter dan tampilan yang sesuai role level user.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic/190-CONTEXT.md
@.planning/phases/190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic/190-RESEARCH.md
@.planning/phases/190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic/190-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CertificationManagement.cshtml — Filter Category/Sub-Category, role-based rendering, JS cascade</name>
  <files>Views/CDP/CertificationManagement.cshtml</files>
  <read_first>
    - Views/CDP/CertificationManagement.cshtml
    - Models/CertificationManagementViewModel.cs
    - Controllers/CDPController.cs (lines 3026-3060 — CertificationManagement action, untuk melihat ViewBag yang tersedia)
  </read_first>
  <action>
**A. Summary Cards — wrap dengan conditional RoleLevel <= 4:**

```razor
@if (Model.RoleLevel <= 4)
{
    <!-- Summary Cards row (existing 4 cards) -->
    <div class="row g-3 mb-4">
        ... (existing summary cards HTML, tidak diubah) ...
    </div>
}
```

**B. Filter Bar — tambah Category dan Sub-Category dropdowns + role-based disabled state:**

Ubah layout filter bar dari row yang ada. Tambah 2 dropdown baru dan atur disabled state:

Setelah filter Tipe (col-md-2), sebelum Cari, tambah:
```razor
<div class="col-md-2">
    <label class="form-label small mb-1">Kategori</label>
    <select id="filter-category" class="form-select form-select-sm">
        <option value="">Semua Kategori</option>
        @foreach (var cat in (List<string>)ViewBag.AllCategories)
        {
            <option value="@cat">@cat</option>
        }
    </select>
</div>
<div class="col-md-2">
    <label class="form-label small mb-1">Sub Kategori</label>
    <select id="filter-subcategory" class="form-select form-select-sm" disabled>
        <option value="">Semua Sub Kategori</option>
    </select>
</div>
```

Ubah filter Bagian — disabled untuk L4+ dan pre-fill untuk L4:
```razor
<select id="filter-bagian" class="form-select form-select-sm" @(Model.RoleLevel >= 4 ? "disabled" : "")>
    @if (Model.RoleLevel == 4)
    {
        <option value="@ViewBag.UserBagian" selected>@ViewBag.UserBagian</option>
    }
    else if (Model.RoleLevel <= 3)
    {
        <option value="">Semua Bagian</option>
        @foreach (var s in (List<string>)ViewBag.AllBagian)
        {
            <option value="@s">@s</option>
        }
    }
    else
    {
        <option value="">Semua Bagian</option>
    }
</select>
```

Ubah filter Unit — disabled untuk L5/L6:
```razor
<select id="filter-unit" class="form-select form-select-sm" @(Model.RoleLevel >= 5 ? "disabled" : "") disabled>
    <option value="">Semua Unit</option>
</select>
```
Catatan: untuk L4, Unit mulai disabled tapi enabled setelah cascade load (karena Bagian sudah pre-filled). Untuk L5/L6, Unit tetap disabled.

Sesuaikan col sizing agar 8 filter + reset muat. Gunakan:
- Bagian: col-md-2, Unit: col-md-2, Status: col-md-1, Tipe: col-md-1, Kategori: col-md-2, Sub Kategori: col-md-2, Cari: col-md-1, Reset: col-md-1.
Atau buat 2 baris filter jika tidak muat.

**C. JS — Category cascade + include category/subCategory in refreshTable + auto-load units for L4:**

Tambah variable references:
```javascript
var categoryEl = document.getElementById('filter-category');
var subCategoryEl = document.getElementById('filter-subcategory');
```

Tambah cascade Category -> Sub-Category (setelah cascade Bagian -> Unit):
```javascript
categoryEl.addEventListener('change', function () {
    var cat = categoryEl.value;
    subCategoryEl.innerHTML = '<option value="">Semua Sub Kategori</option>';
    subCategoryEl.disabled = true;
    if (!cat) { refreshTable(); return; }
    fetch('/CDP/GetSubCategories?category=' + encodeURIComponent(cat))
        .then(function (r) { return r.json(); })
        .then(function (data) {
            data.forEach(function (sc) {
                var opt = document.createElement('option');
                opt.value = sc; opt.textContent = sc;
                subCategoryEl.appendChild(opt);
            });
            if (data.length > 0) subCategoryEl.disabled = false;
            refreshTable();
        });
});
subCategoryEl.addEventListener('change', refreshTable);
```

Update refreshTable — tambah category dan subCategory ke params:
```javascript
if (categoryEl.value) params.set('category', categoryEl.value);
if (subCategoryEl.value) params.set('subCategory', subCategoryEl.value);
```

Update exportExcel — tambah category dan subCategory ke params:
```javascript
if (categoryEl.value) params.set('category', categoryEl.value);
if (subCategoryEl.value) params.set('subCategory', subCategoryEl.value);
```

Update reset button — reset category dan subCategory:
```javascript
categoryEl.value = '';
subCategoryEl.innerHTML = '<option value="">Semua Sub Kategori</option>';
subCategoryEl.disabled = true;
```

Untuk L4 auto-load units pada initial load — tambah di akhir IIFE:
```javascript
// L4: auto-load units karena Bagian sudah pre-filled
var roleLevel = @Model.RoleLevel;
if (roleLevel === 4 && bagianEl.value) {
    fetch('/CDP/GetCascadeOptions?section=' + encodeURIComponent(bagianEl.value))
        .then(function (r) { return r.json(); })
        .then(function (data) {
            data.units.forEach(function (u) {
                var opt = document.createElement('option');
                opt.value = u; opt.textContent = u;
                unitEl.appendChild(opt);
            });
            unitEl.disabled = false;
        });
}

// L5/L6: disable Bagian dan Unit dan reset tidak re-enable
if (roleLevel >= 5) {
    bagianEl.disabled = true;
    unitEl.disabled = true;
}
```

**D. Reset button — L4/L5/L6 tidak boleh re-enable Bagian/Unit:**

Update reset handler:
```javascript
resetBtn.addEventListener('click', function () {
    if (roleLevel <= 3) {
        bagianEl.value = '';
        unitEl.innerHTML = '<option value="">Semua Unit</option>';
        unitEl.disabled = true;
    }
    // L4: keep bagian, reset unit
    if (roleLevel === 4) {
        unitEl.value = '';
    }
    statusEl.value = '';
    tipeEl.value = '';
    categoryEl.value = '';
    subCategoryEl.innerHTML = '<option value="">Semua Sub Kategori</option>';
    subCategoryEl.disabled = true;
    searchEl.value = '';
    refreshTable();
});
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - Views/CDP/CertificationManagement.cshtml contains `@if (Model.RoleLevel <= 4)` before summary cards div
    - Views/CDP/CertificationManagement.cshtml contains `id="filter-category"`
    - Views/CDP/CertificationManagement.cshtml contains `id="filter-subcategory"`
    - Views/CDP/CertificationManagement.cshtml contains `GetSubCategories`
    - Views/CDP/CertificationManagement.cshtml contains `@(Model.RoleLevel >= 4 ? "disabled" : "")`
    - Views/CDP/CertificationManagement.cshtml contains `params.set('category'`
    - Views/CDP/CertificationManagement.cshtml contains `params.set('subCategory'`
    - Views/CDP/CertificationManagement.cshtml contains `ViewBag.AllCategories`
    - Views/CDP/CertificationManagement.cshtml contains `ViewBag.UserBagian`
  </acceptance_criteria>
  <done>Halaman CertificationManagement punya filter Category/Sub-Category, role-based conditional rendering untuk summary cards dan filter disabled state</done>
</task>

<task type="auto">
  <name>Task 2: Update _CertificationManagementTablePartial.cshtml — kolom Sub Kategori dan role-based column visibility</name>
  <files>Views/CDP/Shared/_CertificationManagementTablePartial.cshtml</files>
  <read_first>
    - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml
    - Models/CertificationManagementViewModel.cs
  </read_first>
  <action>
**A. Tambah kolom Sub Kategori di header — setelah Kategori, sebelum Nomor Sertifikat:**

```razor
<th class="d-none d-md-table-cell">Kategori</th>
<th class="d-none d-md-table-cell">Sub Kategori</th>
```

**B. Tambah data Sub Kategori di tbody — setelah Kategori td:**

```razor
<td class="d-none d-md-table-cell">@(row.Kategori ?? "-")</td>
<td class="d-none d-md-table-cell">@(row.SubKategori ?? "-")</td>
```

**C. Role-based column visibility — wrap Nama, Bagian, Unit kolom dengan conditional:**

Header:
```razor
@if (Model.RoleLevel <= 4)
{
    <th>Nama</th>
    <th class="d-none d-md-table-cell">Bagian</th>
    <th class="d-none d-md-table-cell">Unit</th>
}
```

Body:
```razor
@if (Model.RoleLevel <= 4)
{
    <td>@row.NamaWorker</td>
    <td class="d-none d-md-table-cell">@(row.Bagian ?? "-")</td>
    <td class="d-none d-md-table-cell">@(row.Unit ?? "-")</td>
}
```

**D. Update colspan di empty state row — dinamis berdasarkan RoleLevel:**

Hitung: L1-4 = 13 kolom (No + Nama + Bagian + Unit + Judul + Kategori + Sub Kategori + Nomor + Tgl + Valid + Tipe + Status + Aksi), L5/L6 = 10 kolom (tanpa Nama, Bagian, Unit).

```razor
@{
    var colCount = Model.RoleLevel <= 4 ? 13 : 10;
}
<td colspan="@colCount" class="text-center text-muted py-4">Belum ada data sertifikat</td>
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml contains `Sub Kategori` in thead
    - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml contains `row.SubKategori`
    - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml contains `@if (Model.RoleLevel <= 4)` (at least 2 occurrences — header and body)
    - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml contains `var colCount = Model.RoleLevel <= 4 ? 13 : 10`
  </acceptance_criteria>
  <done>Tabel partial view punya kolom Sub Kategori dan conditional rendering kolom Nama/Bagian/Unit berdasarkan RoleLevel</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi visual role-based rendering dan filter cascade</name>
  <what-built>
    Filter cascade Category -> Sub-Category, role-based summary cards visibility, role-based column visibility, role-based filter disabled state, kolom Sub Kategori di tabel.
  </what-built>
  <how-to-verify>
    1. Login sebagai Admin (L1-3): Buka CDP > Certification Management. Pastikan semua filter aktif, summary cards tampil, semua kolom tampil termasuk Sub Kategori. Pilih Category dari dropdown — Sub-Category harus populate otomatis. Apply filter, pastikan tabel refresh via AJAX.
    2. Login sebagai L4: Pastikan filter Bagian disabled dan pre-filled, Unit aktif (auto-loaded). Summary cards tampil. Semua kolom tampil.
    3. Login sebagai L5: Pastikan hanya data diri sendiri (bukan coachee). Summary cards TIDAK tampil. Kolom Nama, Bagian, Unit TIDAK tampil. Filter Bagian dan Unit disabled.
    4. Export Excel: Cek ada kolom "Sub Kategori" setelah "Kategori" (total 13 kolom).
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` sukses
- Login sebagai berbagai role level menghasilkan tampilan yang sesuai spesifikasi
- Filter Category/Sub-Category cascade berjalan
- Export Excel punya 13 kolom termasuk Sub Kategori
</verification>

<success_criteria>
- Summary cards hidden untuk L5/L6, visible untuk L1-4
- Kolom Nama/Bagian/Unit hidden untuk L5/L6
- Filter Bagian disabled untuk L4+ dengan pre-fill untuk L4
- Filter Unit disabled untuk L5/L6
- Cascade Category -> Sub-Category berfungsi
- Kolom Sub Kategori tampil di tabel
- Export Excel punya kolom Sub Kategori
</success_criteria>

<output>
After completion, create `.planning/phases/190-certificationmanagement-filter-category-sub-category-role-based-view-content-and-access-logic/190-02-SUMMARY.md`
</output>
