---
phase: 190-db-categories-foundation
plan: 02
type: execute
wave: 2
depends_on: ["190-01"]
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ManageCategories.cshtml
  - Views/Admin/Index.cshtml
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/EditAssessment.cshtml
autonomous: true
requirements:
  - FORM-02
must_haves:
  truths:
    - "Admin/HC can navigate to ManageCategories page and see all categories in a table"
    - "Admin/HC can add, edit, delete, and toggle-active categories"
    - "CreateAssessment dropdown loads categories from DB with data-pass-percentage attributes"
    - "EditAssessment dropdown loads categories from DB via ViewBag"
    - "No hardcoded category lists remain in any view"
    - "The categoryDefaults JS object no longer exists in CreateAssessment.cshtml"
  artifacts:
    - path: "Views/Admin/ManageCategories.cshtml"
      provides: "Category CRUD page"
      contains: "Kategori Assessment"
    - path: "Controllers/AdminController.cs"
      provides: "ManageCategories, AddCategory, EditCategory, DeleteCategory, ToggleCategoryActive actions"
      contains: "ManageCategories"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "_context.AssessmentCategories"
      pattern: "_context\\.AssessmentCategories"
    - from: "Views/Admin/CreateAssessment.cshtml"
      to: "Controllers/AdminController.cs"
      via: "ViewBag.Categories"
      pattern: "ViewBag\\.Categories"
    - from: "Views/Admin/Index.cshtml"
      to: "Controllers/AdminController.cs"
      via: "Url.Action ManageCategories"
      pattern: "ManageCategories"
---

<objective>
Build the Admin CRUD page for categories, add the hub card to Admin/Index, and rewire both CreateAssessment and EditAssessment forms to load categories from DB instead of hardcoded strings.

Purpose: Completes FORM-02 — Admin can manage categories without code deployment, and both assessment forms use DB-driven dropdowns.
Output: ManageCategories view, 6 new controller actions, updated assessment forms, hub card.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-db-categories-foundation/190-CONTEXT.md
@.planning/phases/190-db-categories-foundation/190-RESEARCH.md
@.planning/phases/190-db-categories-foundation/190-UI-SPEC.md
@.planning/phases/190-db-categories-foundation/190-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ManageCategories CRUD actions to AdminController + ManageCategories view + Admin/Index hub card</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManageCategories.cshtml, Views/Admin/Index.cshtml</files>
  <read_first>Controllers/AdminController.cs, Views/Admin/Index.cshtml, Views/Admin/ManageWorkers.cshtml</read_first>
  <action>
**AdminController.cs — Add 6 new actions (all with `[Authorize(Roles = "Admin, HC")]`):**

1. `GET ManageCategories` — Query `_context.AssessmentCategories.OrderBy(c => c.SortOrder).ThenBy(c => c.Name).ToListAsync()`, return View with list.

2. `POST AddCategory(string name, int defaultPassPercentage, int sortOrder)` — Validate name not empty, check for duplicate name (`_context.AssessmentCategories.AnyAsync(c => c.Name == name)`), create new AssessmentCategory with IsActive=true, save, log to AuditLog:
```csharp
var currentUser = await _userManager.GetUserAsync(User);
var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
    ? (currentUser?.FullName ?? "Unknown")
    : $"{currentUser.NIP} - {currentUser.FullName}";
await _auditLog.LogAsync(currentUser?.Id ?? "", actorName, "AddCategory",
    $"Added assessment category '{category.Name}' (DefaultPass: {category.DefaultPassPercentage}%)",
    category.Id, "AssessmentCategory");
```
Set `TempData["Success"] = "Kategori berhasil ditambahkan."`, redirect to ManageCategories.
On duplicate: `TempData["Error"] = "Nama kategori sudah digunakan. Gunakan nama yang berbeda."`, redirect.

3. `GET EditCategory(int id)` — Find by id, return NotFound if null, return View("ManageCategories") with edit model in ViewBag or use a separate partial. Simpler approach: return the ManageCategories view with `ViewBag.EditCategory` set to the category being edited, and the full list in the model.

4. `POST EditCategory(int id, string name, int defaultPassPercentage, int sortOrder)` — Find by id, check duplicate name (excluding current id), update fields, save, AuditLog "EditCategory", TempData success "Kategori berhasil diperbarui.", redirect.

5. `POST DeleteCategory(int id)` — Find by id, remove, save, AuditLog "DeleteCategory", TempData success "Kategori berhasil dihapus.", redirect.

6. `POST ToggleCategoryActive(int id)` — Find by id, flip `IsActive = !IsActive`, save, AuditLog "ToggleCategoryActive", TempData success "Status kategori berhasil diubah.", redirect.

**Views/Admin/ManageCategories.cshtml — New file following ManageWorkers layout:**

- `@model IEnumerable<HcPortal.Models.AssessmentCategory>`
- Breadcrumb: Kelola Data > Kategori Assessment
- Page header row: `h2.fw-bold` "Kategori Assessment" + `btn-primary` "Tambah Kategori"
- TempData alerts: `alert-success` for Success, `alert-danger` for Error
- Add form (shown inline or always visible): Name input, DefaultPassPercentage input (type=number, min=0, max=100), SortOrder input (type=number)
- Edit form: same fields, pre-filled, shown when `ViewBag.EditCategory != null`
- Table with columns: # (row number), Nama Kategori, Nilai Lulus Default (%), Urutan, Status (badge: `badge bg-success` "Aktif" / `badge bg-secondary` "Nonaktif"), Aksi (Edit btn-outline-primary, Toggle btn-outline-warning, Delete btn-danger)
- Delete confirmation modal using Bootstrap `data-bs-toggle="modal"` pattern
- Empty state: "Belum ada kategori" heading + "Tambahkan kategori assessment menggunakan tombol 'Tambah Kategori' di atas." body
- Table classes: `table table-hover table-bordered align-middle`

**Views/Admin/Index.cshtml — Add hub card to Section C:**

After the existing ManageAssessment/AssessmentMonitoring cards in Section C, add (wrapped in `@if (User.IsInRole("Admin") || User.IsInRole("HC"))`):
```html
<div class="col-md-4">
    <a href="@Url.Action("ManageCategories", "Admin")" class="text-decoration-none">
        <div class="card shadow-sm h-100 border-0">
            <div class="card-body">
                <div class="d-flex align-items-center gap-2 mb-2">
                    <i class="bi bi-tags fs-5 text-primary"></i>
                    <span class="fw-bold">Kategori Assessment</span>
                </div>
                <small class="text-muted">Kelola kategori assessment dan nilai lulus default</small>
            </div>
        </div>
    </a>
</div>
```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs contains `public async Task<IActionResult> ManageCategories()`
    - Controllers/AdminController.cs contains `public async Task<IActionResult> AddCategory(`
    - Controllers/AdminController.cs contains `public async Task<IActionResult> EditCategory(`
    - Controllers/AdminController.cs contains `public async Task<IActionResult> DeleteCategory(`
    - Controllers/AdminController.cs contains `public async Task<IActionResult> ToggleCategoryActive(`
    - Controllers/AdminController.cs contains `_auditLog.LogAsync` within AddCategory action
    - Views/Admin/ManageCategories.cshtml exists and contains `Kategori Assessment`
    - Views/Admin/ManageCategories.cshtml contains `table-hover table-bordered`
    - Views/Admin/ManageCategories.cshtml contains `Tambah Kategori`
    - Views/Admin/ManageCategories.cshtml contains `badge bg-success`
    - Views/Admin/ManageCategories.cshtml contains `Belum ada kategori`
    - Views/Admin/Index.cshtml contains `ManageCategories`
    - Views/Admin/Index.cshtml contains `bi bi-tags`
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>ManageCategories page functional with full CRUD + AuditLog. Hub card visible in Admin/Index Section C for Admin/HC roles.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire CreateAssessment and EditAssessment dropdowns to use DB categories</name>
  <files>Controllers/AdminController.cs, Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml</files>
  <read_first>Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml, Controllers/AdminController.cs</read_first>
  <action>
**AdminController.cs — Update existing GET actions:**

1. In `CreateAssessment GET` (around line 759-789), add before `return View()`:
```csharp
ViewBag.Categories = await _context.AssessmentCategories
    .Where(c => c.IsActive)
    .OrderBy(c => c.SortOrder)
    .ThenBy(c => c.Name)
    .ToListAsync();
```

2. In `EditAssessment GET` (around line 1107-1170), add same ViewBag.Categories query before `return View()`.

3. CRITICAL — Find ALL `return View(model)` calls inside `CreateAssessment POST` and `EditAssessment POST` (validation failure re-renders). Before EACH one, add the same `ViewBag.Categories = await _context.AssessmentCategories...` query. This prevents NullReferenceException when form re-renders after validation failure.

**Views/Admin/CreateAssessment.cshtml:**

1. Remove the hardcoded category list at lines 7-15 (the `@{ var categories = new List<SelectListItem> { ... } }` block or equivalent hardcoded `<option>` elements).

2. Replace the category `<select>` with:
```html
<select asp-for="Category" class="form-select" id="Category" required>
    <option value="">-- Pilih Kategori --</option>
    @foreach (var cat in (IEnumerable<HcPortal.Models.AssessmentCategory>)ViewBag.Categories)
    {
        <option value="@cat.Name"
                data-pass-percentage="@cat.DefaultPassPercentage"
                selected="@(Model.Category == cat.Name)">
            @cat.Name
        </option>
    }
</select>
```

3. Remove the `categoryDefaults` JS object (around line 538-546). Replace the category change handler with:
```javascript
categorySelect.addEventListener('change', function() {
    var selected = this.options[this.selectedIndex];
    var defaultPct = selected ? parseInt(selected.getAttribute('data-pass-percentage') || '0') : 0;
    if (!passPercentageManuallySet && defaultPct > 0) {
        passPercentageInput.value = defaultPct;
    }
});
```
Keep the existing `input` event listener on passPercentageInput that sets `passPercentageManuallySet = true`.

**Views/Admin/EditAssessment.cshtml:**

1. Remove the hardcoded category list at lines 9-17.

2. Replace the category `<select>` with the same DB-driven pattern:
```html
<select asp-for="Category" class="form-select" id="Category" required>
    <option value="">-- Pilih Kategori --</option>
    @foreach (var cat in (IEnumerable<HcPortal.Models.AssessmentCategory>)ViewBag.Categories)
    {
        <option value="@cat.Name"
                data-pass-percentage="@cat.DefaultPassPercentage"
                selected="@(Model.Category == cat.Name)">
            @cat.Name
        </option>
    }
    @if (Model.Category != null && !((IEnumerable<HcPortal.Models.AssessmentCategory>)ViewBag.Categories).Any(c => c.Name == Model.Category))
    {
        <option value="@Model.Category" selected disabled>@Model.Category (tidak aktif)</option>
    }
</select>
```
Note the defensive fallback option for orphaned categories.

3. If EditAssessment has a `categoryDefaults` JS object, remove it and apply the same `data-pass-percentage` change handler pattern.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/Admin/CreateAssessment.cshtml contains `ViewBag.Categories`
    - Views/Admin/CreateAssessment.cshtml contains `data-pass-percentage`
    - Views/Admin/CreateAssessment.cshtml does NOT contain `categoryDefaults`
    - Views/Admin/CreateAssessment.cshtml contains `-- Pilih Kategori --`
    - Views/Admin/EditAssessment.cshtml contains `ViewBag.Categories`
    - Views/Admin/EditAssessment.cshtml contains `data-pass-percentage`
    - Views/Admin/EditAssessment.cshtml does NOT contain hardcoded `SelectListItem` for categories
    - Views/Admin/EditAssessment.cshtml contains `tidak aktif` (orphan fallback)
    - Controllers/AdminController.cs contains `ViewBag.Categories = await _context.AssessmentCategories` (at least 3 occurrences — CreateAssessment GET, EditAssessment GET, and at least one POST re-render path)
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>Both assessment forms load categories from DB. Zero hardcoded category lists remain. categoryDefaults JS removed. passPercentageManuallySet logic preserved. Orphan category fallback in EditAssessment.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes with 0 errors
- ManageCategories page accessible at /Admin/ManageCategories
- CreateAssessment and EditAssessment dropdowns populated from DB
- No hardcoded category strings remain in any .cshtml view
- categoryDefaults JS object removed from CreateAssessment.cshtml
</verification>

<success_criteria>
- Full CRUD for categories with AuditLog on all mutations
- Hub card in Admin/Index Section C for Admin/HC
- Both assessment forms use ViewBag.Categories with data-pass-percentage
- Zero hardcoded category lists in views
- Orphan category fallback in EditAssessment
</success_criteria>

<output>
After completion, create `.planning/phases/190-db-categories-foundation/190-02-SUMMARY.md`
</output>
