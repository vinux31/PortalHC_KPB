---
phase: 191-wizard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentSession.cs
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - FORM-01
must_haves:
  truths:
    - "ValidUntil property exists on AssessmentSession and EF migration is applied"
    - "POST action does not reject form submissions with empty ValidUntil"
  artifacts:
    - path: "Models/AssessmentSession.cs"
      provides: "ValidUntil DateTime? property"
      contains: "public DateTime? ValidUntil"
    - path: "Controllers/AdminController.cs"
      provides: "ModelState.Remove for ValidUntil"
      contains: "ModelState.Remove(\"ValidUntil\")"
  key_links:
    - from: "Models/AssessmentSession.cs"
      to: "Controllers/AdminController.cs"
      via: "model binding"
      pattern: "ModelState\\.Remove.*ValidUntil"
---

<objective>
Add the ValidUntil property to AssessmentSession and create an EF migration, so the wizard view (Plan 02) can bind to it without compile errors.

Purpose: The wizard Step 3 includes a ValidUntil datepicker. The model property must exist before the view can use asp-for="ValidUntil".
Output: Updated model, new migration, POST guard for optional field.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/191-wizard-ui/191-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ValidUntil property to AssessmentSession and create EF migration</name>
  <files>Models/AssessmentSession.cs, Controllers/AdminController.cs</files>
  <read_first>
    Models/AssessmentSession.cs
    Controllers/AdminController.cs (lines 1015-1020 for ExamWindowCloseDate pattern)
  </read_first>
  <action>
1. In `Models/AssessmentSession.cs`, add after the `ExamWindowCloseDate` property (line 59):

```csharp
/// <summary>
/// Optional certificate expiry date. Null = certificate has no expiry.
/// Only relevant when GenerateCertificate = true.
/// </summary>
public DateTime? ValidUntil { get; set; }
```

2. Run: `dotnet ef migrations add AddValidUntilToAssessmentSession`

3. Run: `dotnet ef database update`

4. In `Controllers/AdminController.cs`, add after line 1017 (`ModelState.Remove("ExamWindowCloseDate");`):

```csharp
// ValidUntil is optional — remove from ModelState to prevent accidental validation failure
ModelState.Remove("ValidUntil");
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Models/AssessmentSession.cs contains `public DateTime? ValidUntil { get; set; }`
    - Controllers/AdminController.cs contains `ModelState.Remove("ValidUntil")`
    - A migration file exists in Migrations/ with name containing "AddValidUntilToAssessmentSession"
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>ValidUntil property on model, migration applied, POST action guards optional field</done>
</task>

</tasks>

<verification>
- `dotnet build` passes with zero errors
- grep confirms `ValidUntil` in AssessmentSession.cs and AdminController.cs
</verification>

<success_criteria>
ValidUntil column exists in database, model property compiles, POST action won't reject empty ValidUntil
</success_criteria>

<output>
After completion, create `.planning/phases/191-wizard-ui/191-01-SUMMARY.md`
</output>
