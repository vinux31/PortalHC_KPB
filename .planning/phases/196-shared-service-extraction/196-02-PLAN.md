---
phase: 196-shared-service-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 196-01
files_modified:
  - Controllers/AdminController.cs
  - Controllers/CMPController.cs
autonomous: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-03
  - SVC-04

must_haves:
  truths:
    - "AdminController has zero private GetUnifiedRecords/GetAllWorkersHistory/GetWorkersInSection/NotifyIfGroupCompleted methods"
    - "CMPController has zero private GetUnifiedRecords/GetAllWorkersHistory/GetWorkersInSection/NotifyIfGroupCompleted methods"
    - "Both controllers inject IWorkerDataService and call service methods instead"
    - "All existing call sites in both controllers use _workerDataService.MethodName()"
    - "Project compiles with zero errors"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "Delegated calls to IWorkerDataService, no duplicate methods"
    - path: "Controllers/CMPController.cs"
      provides: "Delegated calls to IWorkerDataService, no duplicate methods"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Services/IWorkerDataService.cs"
      via: "constructor injection + method calls"
      pattern: "_workerDataService"
    - from: "Controllers/CMPController.cs"
      to: "Services/IWorkerDataService.cs"
      via: "constructor injection + method calls"
      pattern: "_workerDataService"
---

<objective>
Replace all 4 duplicated private helper methods in AdminController and CMPController with calls to the shared IWorkerDataService, then delete the private methods.

Purpose: Complete the extraction — zero duplicate logic remains in either controller.
Output: Updated AdminController.cs and CMPController.cs delegating to IWorkerDataService.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/196-shared-service-extraction/196-CONTEXT.md
@.planning/phases/196-shared-service-extraction/196-01-SUMMARY.md

<interfaces>
From Services/IWorkerDataService.cs (created in plan 01):
```csharp
public interface IWorkerDataService
{
    Task<List<UnifiedTrainingRecord>> GetUnifiedRecords(string userId);
    Task<(List<AllWorkersHistoryRow> assessment, List<AllWorkersHistoryRow> training)> GetAllWorkersHistory();
    Task<List<WorkerTrainingStatus>> GetWorkersInSection(string? section, string? unitFilter = null, string? category = null, string? search = null, string? statusFilter = null);
    Task NotifyIfGroupCompleted(AssessmentSession completedSession);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire IWorkerDataService into AdminController and replace private methods</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    Controllers/AdminController.cs (constructor — around line 25-50 for existing DI fields)
    Controllers/AdminController.cs (line 740 — GetWorkersInSection call site)
    Controllers/AdminController.cs (line 742 — GetAllWorkersHistory call site)
    Controllers/AdminController.cs (line 2715 — NotifyIfGroupCompleted call site)
    Controllers/AdminController.cs (lines 2718-2752 — NotifyIfGroupCompleted private method to DELETE)
    Controllers/AdminController.cs (lines 5543-5760 — all 3 helper methods to DELETE)
    Services/IWorkerDataService.cs (interface created in plan 01)
  </read_first>
  <action>
**Step 1: Add constructor injection.**

Add field: `private readonly IWorkerDataService _workerDataService;`

Add `IWorkerDataService workerDataService` parameter to the constructor. Assign: `_workerDataService = workerDataService;`

**Step 2: Replace call sites (3 locations).**

All existing calls are already `await MethodName(args)` — change to `await _workerDataService.MethodName(args)`:

- Line 740: `workers = await GetWorkersInSection(section, unit, category, search, statusFilter);`
  → `workers = await _workerDataService.GetWorkersInSection(section, unit, category, search, statusFilter);`

- Line 742: `var (assessmentHistory, trainingHistory) = await GetAllWorkersHistory();`
  → `var (assessmentHistory, trainingHistory) = await _workerDataService.GetAllWorkersHistory();`

- Line 2715: `await NotifyIfGroupCompleted(session);`
  → `await _workerDataService.NotifyIfGroupCompleted(session);`

Note: GetUnifiedRecords is NOT called in AdminController (only defined) — just delete the method.

**Step 3: Delete private methods (4 blocks).**

Delete these blocks entirely:
1. Lines 2718-2752: `private async Task NotifyIfGroupCompleted(...)` and its XML comment (line 2718-2752)
2. Lines 5543-5591: The comment `// --- WORKER TRAINING HELPERS ...` + `private async Task<List<UnifiedTrainingRecord>> GetUnifiedRecords(...)`
3. Lines 5593-5665: `private async Task<(List<AllWorkersHistoryRow>...> GetAllWorkersHistory()`
4. Lines 5667-5760: `private async Task<List<WorkerTrainingStatus>> GetWorkersInSection(...)`
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs contains `private readonly IWorkerDataService _workerDataService`
    - Controllers/AdminController.cs contains `_workerDataService.GetWorkersInSection(`
    - Controllers/AdminController.cs contains `_workerDataService.GetAllWorkersHistory()`
    - Controllers/AdminController.cs contains `_workerDataService.NotifyIfGroupCompleted(`
    - Controllers/AdminController.cs does NOT contain `private async Task<List<UnifiedTrainingRecord>> GetUnifiedRecords`
    - Controllers/AdminController.cs does NOT contain `private async Task<(List<AllWorkersHistoryRow>` (the private method signature)
    - Controllers/AdminController.cs does NOT contain `private async Task<List<WorkerTrainingStatus>> GetWorkersInSection`
    - Controllers/AdminController.cs does NOT contain `private async Task NotifyIfGroupCompleted`
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>AdminController delegates all 4 methods to IWorkerDataService with zero private duplicates remaining</done>
</task>

<task type="auto">
  <name>Task 2: Wire IWorkerDataService into CMPController and replace private methods</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    Controllers/CMPController.cs (constructor — first 30 lines for existing DI fields)
    Controllers/CMPController.cs (lines 407, 423, 454, 489, 518 — GetUnifiedRecords/GetWorkersInSection call sites)
    Controllers/CMPController.cs (lines 602-605, 667-670 — GetAllWorkersHistory + GetWorkersInSection call sites)
    Controllers/CMPController.cs (lines 2144, 2252 — NotifyIfGroupCompleted call sites)
    Controllers/CMPController.cs (lines 1037-1087 — GetUnifiedRecords private method to DELETE)
    Controllers/CMPController.cs (lines 1090-1173 — GetAllWorkersHistory private method to DELETE)
    Controllers/CMPController.cs (lines 1176-1291 — GetWorkersInSection private method to DELETE)
    Controllers/CMPController.cs (lines 2866-2895 — NotifyIfGroupCompleted private method to DELETE)
    Services/IWorkerDataService.cs (interface)
  </read_first>
  <action>
**Step 1: Add constructor injection.**

Add field: `private readonly IWorkerDataService _workerDataService;`

Add `IWorkerDataService workerDataService` parameter to the constructor. Assign: `_workerDataService = workerDataService;`

**Step 2: Replace all call sites (9 locations).**

Prefix each existing call with `_workerDataService.`:

- Line 407: `await GetUnifiedRecords(user.Id)` → `await _workerDataService.GetUnifiedRecords(user.Id)`
- Line 423: `await GetWorkersInSection(sectionFilter)` → `await _workerDataService.GetWorkersInSection(sectionFilter)`
- Line 454: `await GetWorkersInSection(sectionFilter)` → `await _workerDataService.GetWorkersInSection(sectionFilter)`
- Line 489: `await GetUnifiedRecords(workerId)` → `await _workerDataService.GetUnifiedRecords(workerId)`
- Line 518: `await GetUnifiedRecords(user.Id)` → `await _workerDataService.GetUnifiedRecords(user.Id)`
- Line 602: `await GetAllWorkersHistory()` → `await _workerDataService.GetAllWorkersHistory()`
- Line 605: `await GetWorkersInSection(sectionFilter, unit, null, search, statusFilter)` → `await _workerDataService.GetWorkersInSection(sectionFilter, unit, null, search, statusFilter)`
- Line 667: `await GetAllWorkersHistory()` → `await _workerDataService.GetAllWorkersHistory()`
- Line 670: `await GetWorkersInSection(sectionFilter, unit, category, search, statusFilter)` → `await _workerDataService.GetWorkersInSection(sectionFilter, unit, category, search, statusFilter)`
- Line 2144: `await NotifyIfGroupCompleted(assessment)` → `await _workerDataService.NotifyIfGroupCompleted(assessment)`
- Line 2252: `await NotifyIfGroupCompleted(assessment)` → `await _workerDataService.NotifyIfGroupCompleted(assessment)`

**Step 3: Delete private methods (4 blocks).**

Delete these blocks entirely:
1. Lines 1037-1087: `private async Task<List<UnifiedTrainingRecord>> GetUnifiedRecords(...)`
2. Lines 1090-1173: `private async Task<(List<AllWorkersHistoryRow>...> GetAllWorkersHistory()`
3. Lines 1176-1291: `private async Task<List<WorkerTrainingStatus>> GetWorkersInSection(...)`
4. Lines 2865-2895: The comment + `private async Task NotifyIfGroupCompleted(...)`
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/CMPController.cs contains `private readonly IWorkerDataService _workerDataService`
    - Controllers/CMPController.cs contains `_workerDataService.GetUnifiedRecords(`
    - Controllers/CMPController.cs contains `_workerDataService.GetWorkersInSection(`
    - Controllers/CMPController.cs contains `_workerDataService.GetAllWorkersHistory()`
    - Controllers/CMPController.cs contains `_workerDataService.NotifyIfGroupCompleted(`
    - Controllers/CMPController.cs does NOT contain `private async Task<List<UnifiedTrainingRecord>> GetUnifiedRecords`
    - Controllers/CMPController.cs does NOT contain `private async Task<(List<AllWorkersHistoryRow>` (the private method signature)
    - Controllers/CMPController.cs does NOT contain `private async Task<List<WorkerTrainingStatus>> GetWorkersInSection`
    - Controllers/CMPController.cs does NOT contain `private async Task NotifyIfGroupCompleted`
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>CMPController delegates all 4 methods to IWorkerDataService with zero private duplicates remaining</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds with 0 errors
- grep for `private async Task.*GetUnifiedRecords\|private async Task.*GetAllWorkersHistory\|private async Task.*GetWorkersInSection\|private async Task.*NotifyIfGroupCompleted` in Controllers/ returns 0 matches
- grep for `_workerDataService` in AdminController.cs returns 4+ matches (field + 3 calls)
- grep for `_workerDataService` in CMPController.cs returns 12+ matches (field + 11 calls)
</verification>

<success_criteria>
- Zero private duplicate helper methods remain in either controller
- All existing call sites delegate to IWorkerDataService
- Project compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/196-shared-service-extraction/196-02-SUMMARY.md`
</output>
