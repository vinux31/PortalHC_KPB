---
phase: 198-crud-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - Controllers/AdminController.cs
  - Views/Admin/ImportTraining.cshtml
  - Views/Admin/ManageAssessment.cshtml
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements: [CRUD-01, CRUD-02, CRUD-03]

must_haves:
  truths:
    - "CMPController tidak lagi memiliki action EditTrainingRecord atau DeleteTrainingRecord"
    - "CMPController tidak lagi memiliki action ImportTraining atau DownloadImportTrainingTemplate"
    - "AdminController memiliki action ImportTraining (GET+POST) dan DownloadImportTrainingTemplate"
    - "Tombol Import Excel muncul di ManageAssessment tab=training"
    - "Tombol import tidak muncul di CMP/RecordsTeam"
    - "Project compile tanpa error"
  artifacts:
    - path: "Views/Admin/ImportTraining.cshtml"
      provides: "Import Training view mengikuti pattern ImportWorkers"
      min_lines: 50
    - path: "Controllers/AdminController.cs"
      provides: "ImportTraining + DownloadImportTrainingTemplate actions"
      contains: "ImportTraining"
    - path: "Controllers/CMPController.cs"
      provides: "Tanpa EditTrainingRecord, DeleteTrainingRecord, ImportTraining, DownloadImportTrainingTemplate"
  key_links:
    - from: "Views/Admin/ManageAssessment.cshtml"
      to: "Admin/ImportTraining"
      via: "Url.Action link button"
      pattern: 'Url.Action\("ImportTraining", "Admin"\)'
---

<objective>
Konsolidasi Training Record CRUD: hapus action orphan dari CMPController, pindahkan import ke AdminController, bersihkan referensi view.

Purpose: Training Record CRUD punya satu entry point (Admin), mengurangi duplikasi controller code
Output: CMPController lebih lean, Admin memiliki full training management termasuk import
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-crud-consolidation/198-CONTEXT.md
@.planning/phases/198-crud-consolidation/198-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hapus CMP orphan actions + pindahkan import logic ke AdminController</name>
  <files>Controllers/CMPController.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (baca full — cari EditTrainingRecord ~line 845, DeleteTrainingRecord ~line 930, ImportTraining ~line 716, DownloadImportTrainingTemplate ~line 673)
    - Controllers/AdminController.cs (baca ImportWorkers + DownloadImportTemplate actions sebagai pattern, dan AddTraining area ~line 5221)
    - Models/ImportTrainingResult.cs (model yang dipakai import logic)
    - Views/Admin/ImportWorkers.cshtml (target pattern untuk view nanti)
  </read_first>
  <action>
Di CMPController.cs:
1. HAPUS action `EditTrainingRecord` (GET + POST) — seluruh method body
2. HAPUS action `DeleteTrainingRecord` (POST) — seluruh method body
3. HAPUS action `DownloadImportTrainingTemplate` (GET) — seluruh method body
4. HAPUS action `ImportTraining` (GET + POST) — seluruh method body
5. Hapus using statements yang jadi unused setelah penghapusan (jika ada)

Di AdminController.cs:
1. TAMBAH action `DownloadImportTrainingTemplate` dengan attribute `[Authorize(Roles = "Admin, HC")]` — copy logic dari CMP yang baru dihapus (Excel template generation untuk training import)
2. TAMBAH action `ImportTraining` GET dengan attribute `[Authorize(Roles = "Admin, HC")]` — return View dengan model kosong (List of ImportTrainingResult)
3. TAMBAH action `ImportTraining` POST dengan attribute `[Authorize(Roles = "Admin, HC")]` — copy parsing logic dari CMP (Excel read, validate NIP exists, create TrainingRecord, return results). Ubah redirect target ke `ManageAssessment` dengan `tab=training`. Ubah TempData messages sesuai Admin context.

PENTING:
- ImportTrainingResult model TETAP di Models/ — tidak perlu dipindah
- EditTrainingRecordViewModel JANGAN dihapus — masih dipakai Admin/EditTraining
- Pastikan semua _context (ApplicationDbContext) dan _env (IWebHostEnvironment) references tersedia di AdminController (sudah ada dari existing code)
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CMPController.cs TIDAK mengandung string "EditTrainingRecord"
    - CMPController.cs TIDAK mengandung string "DeleteTrainingRecord"
    - CMPController.cs TIDAK mengandung string "ImportTraining"
    - CMPController.cs TIDAK mengandung string "DownloadImportTrainingTemplate"
    - AdminController.cs mengandung "public async Task<IActionResult> ImportTraining()" (GET)
    - AdminController.cs mengandung "public async Task<IActionResult> ImportTraining(IFormFile" (POST)
    - AdminController.cs mengandung "public IActionResult DownloadImportTrainingTemplate()"
    - AdminController.cs mengandung "[Authorize(Roles" pada ketiga action baru
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>CMPController tidak punya action training CRUD/import, AdminController punya ImportTraining + DownloadImportTrainingTemplate, project compile clean</done>
</task>

<task type="auto">
  <name>Task 2: Buat view ImportTraining + update ManageAssessment + bersihkan RecordsTeam</name>
  <files>Views/Admin/ImportTraining.cshtml, Views/Admin/ManageAssessment.cshtml, Views/CMP/RecordsTeam.cshtml</files>
  <read_first>
    - Views/Admin/ImportWorkers.cshtml (pattern yang HARUS diikuti untuk ImportTraining view)
    - Views/Admin/ManageAssessment.cshtml (cari tab=training section ~line 356-365, tombol "Tambah Training")
    - Views/CMP/RecordsTeam.cshtml (cari import button block ~line 115-127)
    - Models/ImportTrainingResult.cs (fields: NIP, Judul, Status, Message)
  </read_first>
  <action>
1. BUAT Views/Admin/ImportTraining.cshtml mengikuti EXACT structure dari ImportWorkers.cshtml:
   - @model List&lt;ImportTrainingResult&gt;
   - Breadcrumb: Kelola Data > Manajemen Assessment > Import Training
   - Header "Import Training" dengan tombol "Kembali" yang link ke ManageAssessment?tab=training
   - Alert section untuk TempData["Error"]
   - Results summary cards (jika Model.Count > 0): Success count, Skip count, Error count — filter by Status field
   - Results table dengan kolom: NIP, Judul, Status (badge: success=green, skip=warning, error=danger), Message
   - Upload card dengan 2 steps: Step 1 "Download Template" (link ke Admin/DownloadImportTrainingTemplate), Step 2 "Upload File" (form POST ke Admin/ImportTraining)
   - Format notes table: kolom NIP (wajib, harus terdaftar), Judul (wajib), TanggalPelaksanaan (dd/MM/yyyy), TanggalExpired (dd/MM/yyyy, opsional), FilePath (opsional)
   - Drag & drop JS + file validation (copy dari ImportWorkers)

2. EDIT Views/Admin/ManageAssessment.cshtml:
   - Cari section tab=training header (~line 360-364) yang berisi d-flex dengan "Tambah Training" button
   - Ganti single button dengan button group menggunakan d-flex gap-2:
     ```html
     <div class="d-flex gap-2">
         <a href="@Url.Action("ImportTraining", "Admin")" class="btn btn-outline-primary">
             <i class="bi bi-file-earmark-arrow-up me-2"></i>Import Excel
         </a>
         <a href="@Url.Action("AddTraining", "Admin")" class="btn btn-primary">
             <i class="bi bi-plus-lg me-2"></i>Tambah Training
         </a>
     </div>
     ```

3. EDIT Views/CMP/RecordsTeam.cshtml:
   - HAPUS block import buttons (~line 115-127): seluruh @if (userRole == "Admin" || userRole == "HC") block yang berisi link ke CMP/ImportTraining dan CMP/DownloadImportTrainingTemplate
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File Views/Admin/ImportTraining.cshtml exists dan mengandung "@model List<PortalHC_KPB.Models.ImportTrainingResult>" atau "@model List<ImportTrainingResult>"
    - ImportTraining.cshtml mengandung "DownloadImportTrainingTemplate"
    - ImportTraining.cshtml mengandung "Kelola Data"  (breadcrumb)
    - ManageAssessment.cshtml mengandung 'Url.Action("ImportTraining", "Admin")'
    - ManageAssessment.cshtml mengandung "Import Excel"
    - RecordsTeam.cshtml TIDAK mengandung "ImportTraining"
    - RecordsTeam.cshtml TIDAK mengandung "DownloadImportTrainingTemplate"
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>View ImportTraining baru mengikuti pattern ImportWorkers, tombol Import muncul di ManageAssessment tab=training, tombol import dihapus dari RecordsTeam</done>
</task>

</tasks>

<verification>
1. `dotnet build` — compile clean, zero errors
2. Grep CMPController.cs untuk "EditTrainingRecord|DeleteTrainingRecord|ImportTraining" — zero matches
3. Grep RecordsTeam.cshtml untuk "ImportTraining" — zero matches
4. Grep ManageAssessment.cshtml untuk "ImportTraining.*Admin" — match found
5. File Views/Admin/ImportTraining.cshtml exists
</verification>

<success_criteria>
- CMPController tidak memiliki action edit/delete/import training — Admin satu-satunya entry point
- Import Training accessible dari Admin/ManageAssessment tab=training
- View ImportTraining mengikuti pattern ImportWorkers (breadcrumb, summary cards, upload card, format notes)
- Tidak ada referensi CMP import di RecordsTeam
- Project compile tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/198-crud-consolidation/198-01-SUMMARY.md`
</output>
