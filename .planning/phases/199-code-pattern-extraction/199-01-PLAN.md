---
phase: 199-code-pattern-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/FileUploadHelper.cs
  - Helpers/PaginationHelper.cs
  - Controllers/AdminController.cs
  - Controllers/CMPController.cs
  - Controllers/CDPController.cs
autonomous: true
requirements: [PAT-01, PAT-03]

must_haves:
  truths:
    - "File upload untuk KKJ dan CPDP menggunakan FileUploadHelper — tidak ada inline upload logic di AdminController"
    - "Pagination calculation di semua controller menggunakan PaginationHelper — tidak ada inline Math.Ceiling/Skip/Take pattern"
  artifacts:
    - path: "Helpers/FileUploadHelper.cs"
      provides: "Static helper untuk file upload: validasi, safe filename, save, return relative URL"
      contains: "public static class FileUploadHelper"
    - path: "Helpers/PaginationHelper.cs"
      provides: "Static helper untuk pagination calculation: skip, take, totalPages, currentPage clamping"
      contains: "public static class PaginationHelper"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Helpers/FileUploadHelper.cs"
      via: "FileUploadHelper.SaveFileAsync call"
      pattern: "FileUploadHelper\\.SaveFileAsync"
    - from: "Controllers/AdminController.cs"
      to: "Helpers/PaginationHelper.cs"
      via: "PaginationHelper.Calculate call"
      pattern: "PaginationHelper\\.Calculate"
    - from: "Controllers/CMPController.cs"
      to: "Helpers/PaginationHelper.cs"
      via: "PaginationHelper.Calculate call"
      pattern: "PaginationHelper\\.Calculate"
---

<objective>
Extract FileUploadHelper dan PaginationHelper ke Helpers/ folder, lalu refactor semua controller yang pakai inline pattern.

Purpose: Menghilangkan duplikasi inline file upload (2 tempat di AdminController) dan pagination (5+ tempat di 3 controller).
Output: Dua static helper class baru + semua controller ter-refactor.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-code-pattern-extraction/199-CONTEXT.md

<interfaces>
From Helpers/ExcelExportHelper.cs (reference pattern):
```csharp
namespace HcPortal.Helpers
{
    public static class ExcelExportHelper
    {
        public static IXLWorksheet CreateSheet(XLWorkbook workbook, string sheetName, string[] headers);
        public static FileContentResult ToFileResult(XLWorkbook workbook, string fileName, ControllerBase controller);
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat FileUploadHelper dan PaginationHelper</name>
  <files>Helpers/FileUploadHelper.cs, Helpers/PaginationHelper.cs</files>
  <read_first>
    - Helpers/ExcelExportHelper.cs (reference pattern untuk static helper class)
    - Controllers/AdminController.cs lines 5273-5285 (inline file upload pattern — CreateTraining)
    - Controllers/AdminController.cs lines 5390-5400 (inline file upload pattern — EditTraining)
    - Controllers/AdminController.cs lines 714-723 (inline pagination pattern)
    - Controllers/CMPController.cs lines 228-234 (inline pagination pattern)
    - Controllers/CDPController.cs lines 1585-1590 (inline pagination pattern)
  </read_first>
  <action>
Buat `Helpers/FileUploadHelper.cs`:
```csharp
namespace HcPortal.Helpers
{
    public static class FileUploadHelper
    {
        /// Saves uploaded file to wwwroot/{subFolder}, returns relative URL like "/uploads/certificates/20260318..._{name}"
        /// Returns null if file is null or empty.
        public static async Task<string?> SaveFileAsync(IFormFile? file, string webRootPath, string subFolder)
        {
            if (file == null || file.Length == 0) return null;
            var uploadDir = Path.Combine(webRootPath, subFolder);
            Directory.CreateDirectory(uploadDir);
            var safeFileName = $"{DateTime.UtcNow:yyyyMMddHHmmss}_{Path.GetFileName(file.FileName)}";
            var filePath = Path.Combine(uploadDir, safeFileName);
            using (var stream = new FileStream(filePath, FileMode.Create))
            {
                await file.CopyToAsync(stream);
            }
            return $"/{subFolder.Replace('\\', '/')}/{safeFileName}";
        }
    }
}
```

Buat `Helpers/PaginationHelper.cs`:
```csharp
namespace HcPortal.Helpers
{
    public record PaginationResult(int CurrentPage, int TotalPages, int TotalCount, int Skip, int Take);

    public static class PaginationHelper
    {
        public static PaginationResult Calculate(int totalCount, int page, int pageSize)
        {
            var totalPages = (int)Math.Ceiling(totalCount / (double)pageSize);
            var currentPage = Math.Max(1, Math.Min(page, Math.Max(1, totalPages)));
            var skip = (currentPage - 1) * pageSize;
            return new PaginationResult(currentPage, totalPages, totalCount, skip, pageSize);
        }
    }
}
```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Helpers/FileUploadHelper.cs contains "public static class FileUploadHelper"
    - Helpers/FileUploadHelper.cs contains "public static async Task<string?> SaveFileAsync(IFormFile? file, string webRootPath, string subFolder)"
    - Helpers/PaginationHelper.cs contains "public static class PaginationHelper"
    - Helpers/PaginationHelper.cs contains "public static PaginationResult Calculate(int totalCount, int page, int pageSize)"
    - Helpers/PaginationHelper.cs contains "public record PaginationResult"
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Kedua helper class terbuat dan compile tanpa error</done>
</task>

<task type="auto">
  <name>Task 2: Refactor semua controller pakai helper</name>
  <files>Controllers/AdminController.cs, Controllers/CMPController.cs, Controllers/CDPController.cs</files>
  <read_first>
    - Helpers/FileUploadHelper.cs (baru dibuat Task 1)
    - Helpers/PaginationHelper.cs (baru dibuat Task 1)
    - Controllers/AdminController.cs (full — cari semua inline upload dan pagination)
    - Controllers/CMPController.cs (cari semua inline pagination)
    - Controllers/CDPController.cs (cari semua inline pagination)
  </read_first>
  <action>
**FileUploadHelper refactor di AdminController.cs:**

Cari semua blok yang match pattern:
```
var uploadDir = Path.Combine(_env.WebRootPath, "uploads", "certificates");
Directory.CreateDirectory(uploadDir);
var safeFileName = ...
var filePath = Path.Combine(uploadDir, safeFileName);
using (var stream = new FileStream(filePath, FileMode.Create)) { await ...CopyToAsync(stream); }
sertifikatUrl = $"/uploads/certificates/{safeFileName}";
```

Ganti dengan satu baris:
```csharp
sertifikatUrl = await FileUploadHelper.SaveFileAsync(model.CertificateFile, _env.WebRootPath, "uploads/certificates");
```

Minimal 2 tempat: CreateTraining POST (~line 5273) dan EditTraining POST (~line 5390).

Tambah `using HcPortal.Helpers;` di atas file jika belum ada.

**PaginationHelper refactor di semua 3 controller:**

Cari semua blok yang match pattern:
```
var totalPages = (int)Math.Ceiling(totalCount / (double)pageSize);
... .Skip((page - 1) * pageSize).Take(pageSize) ...
ViewBag.CurrentPage = page;
ViewBag.TotalPages = totalPages;
```

Ganti dengan:
```csharp
var paging = PaginationHelper.Calculate(totalCount, page, pageSize);
... .Skip(paging.Skip).Take(paging.Take) ...
ViewBag.CurrentPage = paging.CurrentPage;
ViewBag.TotalPages = paging.TotalPages;
ViewBag.TotalCount = paging.TotalCount;
```

Tempat yang perlu di-refactor:
- AdminController: ManageAssessment (~line 715), AuditLog (~line 2952), CoachingProtonDashboard (~line 3430), dan lainnya yang pakai pattern yang sama
- CMPController: ~line 228
- CDPController: ~line 1587

Tambah `using HcPortal.Helpers;` di setiap controller yang belum ada.

PENTING: Pastikan behavior IDENTIK — jangan ubah pageSize default, jangan ubah parameter name, jangan ubah ViewBag key names.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains "FileUploadHelper.SaveFileAsync" (minimal 2 occurrences)
    - AdminController.cs does NOT contain "var safeFileName = $\"{DateTime.UtcNow:yyyyMMddHHmmss}" (inline upload removed)
    - AdminController.cs contains "PaginationHelper.Calculate" (minimal 3 occurrences)
    - CMPController.cs contains "PaginationHelper.Calculate"
    - CDPController.cs contains "PaginationHelper.Calculate"
    - No controller contains "(int)Math.Ceiling(totalCount / (double)pageSize)" (inline pagination removed)
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Semua inline file upload dan pagination pattern diganti helper calls, build sukses</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- `grep -r "FileUploadHelper.SaveFileAsync" Controllers/` menunjukkan minimal 2 hit di AdminController
- `grep -r "PaginationHelper.Calculate" Controllers/` menunjukkan hit di 3 controller
- `grep -r "Math.Ceiling(totalCount" Controllers/` menunjukkan 0 hit (inline dihapus)
- `grep -r "var safeFileName.*DateTime.UtcNow.*yyyyMMddHHmmss" Controllers/` menunjukkan 0 hit (inline dihapus)
</verification>

<success_criteria>
- FileUploadHelper dan PaginationHelper ada di Helpers/ folder
- Semua inline file upload dan pagination di-replace oleh helper calls
- Build sukses, zero behavior change
</success_criteria>

<output>
After completion, create `.planning/phases/199-code-pattern-extraction/199-01-SUMMARY.md`
</output>
