---
phase: 199-code-pattern-extraction
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - Controllers/CMPController.cs
autonomous: true
requirements: [PAT-02]

must_haves:
  truths:
    - "Role-scoping logic di CMPController ada di satu private helper — tidak ada duplikasi GetRolesAsync + GetRoleLevel pattern"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Private helper method untuk role-scoping"
      contains: "GetCurrentUserRoleLevel"
  key_links:
    - from: "CMPController actions"
      to: "GetCurrentUserRoleLevel helper"
      via: "private method call"
      pattern: "await GetCurrentUserRoleLevel"
---

<objective>
Extract role-scoping enforcement logic yang berulang di CMPController ke private helper method.

Purpose: 3+ tempat di CMPController punya blok identik: GetRolesAsync -> GetRoleLevel -> switch roleLevel. Extract ke satu method.
Output: CMPController dengan private helper, zero behavior change.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-code-pattern-extraction/199-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract role-scoping helper di CMPController</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (full file — identifikasi semua repeated role-scoping blocks)
  </read_first>
  <action>
Identifikasi blok yang berulang di CMPController (3+ tempat). Pattern yang berulang:

```csharp
var user = await _userManager.GetUserAsync(User);
var userRoles = await _userManager.GetRolesAsync(user);
var userRole = userRoles.FirstOrDefault();
var roleLevel = UserRoles.GetRoleLevel(userRole ?? "");
```

Buat private helper method di CMPController:

```csharp
/// <summary>
/// Returns (user, roleLevel) for the current authenticated user.
/// Extracts the repeated role-scoping pattern used across multiple actions.
/// </summary>
private async Task<(ApplicationUser User, int RoleLevel)> GetCurrentUserRoleLevelAsync()
{
    var user = await _userManager.GetUserAsync(User);
    var userRoles = await _userManager.GetRolesAsync(user!);
    var roleLevel = UserRoles.GetRoleLevel(userRoles.FirstOrDefault() ?? "");
    return (user!, roleLevel);
}
```

Lalu ganti SEMUA blok yang match pattern di atas dengan:
```csharp
var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
```

Cari di sekitar line 414, 440, 469, 571, 623 dan tempat lain yang pakai pattern yang sama.

PENTING:
- Jangan ubah logic setelah roleLevel assignment — hanya extract assignment-nya
- Jika ada tempat yang pakai `userRole` variable (bukan hanya `roleLevel`), pertahankan variable itu dari `userRoles.FirstOrDefault()` atau tambahkan ke return tuple
- Pastikan setiap action yang pakai pattern ini tetap berperilaku identik
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CMPController.cs contains "private async Task<" and "GetCurrentUserRoleLevelAsync"
    - CMPController.cs contains "await GetCurrentUserRoleLevelAsync()" (minimal 3 occurrences)
    - CMPController.cs does NOT contain more than 1 occurrence of "UserRoles.GetRoleLevel" (the one inside the helper)
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Role-scoping blocks di CMPController di-extract ke satu private helper, build sukses</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- `grep -c "GetCurrentUserRoleLevelAsync" Controllers/CMPController.cs` menunjukkan 4+ (1 definition + 3+ calls)
- `grep -c "UserRoles.GetRoleLevel" Controllers/CMPController.cs` menunjukkan 1 (hanya di helper)
</verification>

<success_criteria>
- Private helper method ada di CMPController
- Semua inline role-scoping blocks diganti helper call
- Build sukses, zero behavior change
</success_criteria>

<output>
After completion, create `.planning/phases/199-code-pattern-extraction/199-02-SUMMARY.md`
</output>
