---
phase: 200-renewal-chain-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentSession.cs
  - Models/TrainingRecord.cs
  - Data/ApplicationDbContext.cs
  - Migrations/<generated>_AddRenewalChainFKs.cs
autonomous: true
requirements:
  - RENEW-01
user_setup: []

must_haves:
  truths:
    - "AssessmentSession memiliki kolom RenewsSessionId dan RenewsTrainingId nullable di database"
    - "TrainingRecord memiliki kolom RenewsTrainingId dan RenewsSessionId nullable di database"
    - "Menghapus sertifikat asal tidak error — FK di-set NULL"
  artifacts:
    - path: "Models/AssessmentSession.cs"
      provides: "RenewsSessionId dan RenewsTrainingId properties"
      contains: "public int? RenewsSessionId"
    - path: "Models/TrainingRecord.cs"
      provides: "RenewsTrainingId dan RenewsSessionId properties"
      contains: "public int? RenewsTrainingId"
    - path: "Data/ApplicationDbContext.cs"
      provides: "FK configuration dengan OnDelete SetNull"
      contains: "DeleteBehavior.SetNull"
  key_links:
    - from: "Models/AssessmentSession.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "FK configuration in OnModelCreating"
      pattern: "HasForeignKey.*RenewsSessionId"
    - from: "Models/TrainingRecord.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "FK configuration in OnModelCreating"
      pattern: "HasForeignKey.*RenewsTrainingId"
---

<objective>
Menambahkan kolom FK renewal chain ke AssessmentSession dan TrainingRecord, beserta konfigurasi EF Core dan migration.

Purpose: Infrastruktur data yang mendasari seluruh fitur renewal di milestone v7.7.
Output: Migration yang menambah 4 kolom FK (2 per tabel) dengan index dan ON DELETE SET NULL.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-renewal-chain-foundation/200-CONTEXT.md
@.planning/phases/200-renewal-chain-foundation/200-RESEARCH.md

<interfaces>
From Models/AssessmentSession.cs (last property before navigation):
```csharp
public string? InterviewResultsJson { get; set; }

// Navigation Properties for Exam Engine
public virtual ICollection<AssessmentQuestion> Questions { get; set; } = new List<AssessmentQuestion>();
```

From Models/TrainingRecord.cs (last properties):
```csharp
public string? Kota { get; set; }

public bool IsExpiringSoon { get { ... } }
public int? DaysUntilExpiry { get { ... } }
```

From Data/ApplicationDbContext.cs - TrainingRecord config (line 97):
```csharp
builder.Entity<TrainingRecord>(entity =>
{
    entity.HasOne(t => t.User)
        .WithMany(u => u.TrainingRecords)
        .HasForeignKey(t => t.UserId)
        .OnDelete(DeleteBehavior.Cascade);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah FK properties ke model + konfigurasi OnModelCreating + generate migration</name>
  <files>Models/AssessmentSession.cs, Models/TrainingRecord.cs, Data/ApplicationDbContext.cs</files>
  <read_first>
    - Models/AssessmentSession.cs
    - Models/TrainingRecord.cs
    - Data/ApplicationDbContext.cs
  </read_first>
  <action>
1. **Models/AssessmentSession.cs** — Tambahkan 2 property SEBELUM baris `// Navigation Properties for Exam Engine`:

```csharp
/// <summary>
/// FK ke AssessmentSession lain yang di-renew oleh session ini.
/// Nullable. Hanya salah satu dari RenewsSessionId/RenewsTrainingId yang boleh diisi.
/// ON DELETE SET NULL — jika sertifikat asal dihapus, FK jadi NULL.
/// </summary>
public int? RenewsSessionId { get; set; }

/// <summary>
/// FK ke TrainingRecord yang di-renew oleh session ini.
/// Nullable. Hanya salah satu dari RenewsSessionId/RenewsTrainingId yang boleh diisi.
/// ON DELETE SET NULL — jika sertifikat asal dihapus, FK jadi NULL.
/// </summary>
public int? RenewsTrainingId { get; set; }
```

2. **Models/TrainingRecord.cs** — Tambahkan 2 property SEBELUM property `IsExpiringSoon`:

```csharp
/// <summary>
/// FK ke TrainingRecord lain yang di-renew oleh record ini (self-FK).
/// Nullable. Hanya salah satu dari RenewsTrainingId/RenewsSessionId yang boleh diisi.
/// </summary>
public int? RenewsTrainingId { get; set; }

/// <summary>
/// FK ke AssessmentSession yang di-renew oleh record ini.
/// Nullable. Hanya salah satu dari RenewsTrainingId/RenewsSessionId yang boleh diisi.
/// </summary>
public int? RenewsSessionId { get; set; }
```

3. **Data/ApplicationDbContext.cs** — Di dalam method `OnModelCreating`:

   a. Cari blok `builder.Entity<TrainingRecord>(entity =>` (line ~97). Tambahkan SETELAH konfigurasi User FK yang ada, MASIH di dalam lambda `entity =>`:

```csharp
entity.Property(t => t.RenewsTrainingId).IsRequired(false);
entity.Property(t => t.RenewsSessionId).IsRequired(false);

entity.HasOne<TrainingRecord>()
    .WithMany()
    .HasForeignKey(t => t.RenewsTrainingId)
    .OnDelete(DeleteBehavior.SetNull);

entity.HasOne<AssessmentSession>()
    .WithMany()
    .HasForeignKey(t => t.RenewsSessionId)
    .OnDelete(DeleteBehavior.SetNull);
```

   b. Tambahkan blok BARU untuk AssessmentSession renewal FKs (setelah blok TrainingRecord, atau di tempat yang sesuai secara alfabet):

```csharp
builder.Entity<AssessmentSession>(entity =>
{
    entity.Property(a => a.RenewsSessionId).IsRequired(false);
    entity.Property(a => a.RenewsTrainingId).IsRequired(false);

    entity.HasOne<AssessmentSession>()
        .WithMany()
        .HasForeignKey(a => a.RenewsSessionId)
        .OnDelete(DeleteBehavior.SetNull);

    entity.HasOne<TrainingRecord>()
        .WithMany()
        .HasForeignKey(a => a.RenewsTrainingId)
        .OnDelete(DeleteBehavior.SetNull);
});
```

   PENTING: Jika sudah ada blok `builder.Entity<AssessmentSession>`, tambahkan konfigurasi di dalam blok yang ada, bukan buat blok baru.

4. **Generate migration:**
```bash
dotnet ef migrations add AddRenewalChainFKs
```

5. **Apply migration:**
```bash
dotnet ef database update
```
  </action>
  <verify>
    <automated>dotnet build && dotnet ef database update && dotnet ef migrations list | grep AddRenewalChainFKs</automated>
  </verify>
  <acceptance_criteria>
    - Models/AssessmentSession.cs contains `public int? RenewsSessionId { get; set; }`
    - Models/AssessmentSession.cs contains `public int? RenewsTrainingId { get; set; }`
    - Models/TrainingRecord.cs contains `public int? RenewsTrainingId { get; set; }`
    - Models/TrainingRecord.cs contains `public int? RenewsSessionId { get; set; }`
    - Data/ApplicationDbContext.cs contains `HasForeignKey(a => a.RenewsSessionId)`
    - Data/ApplicationDbContext.cs contains `HasForeignKey(a => a.RenewsTrainingId)`
    - Data/ApplicationDbContext.cs contains `HasForeignKey(t => t.RenewsTrainingId)`
    - Data/ApplicationDbContext.cs contains `HasForeignKey(t => t.RenewsSessionId)`
    - Data/ApplicationDbContext.cs contains `DeleteBehavior.SetNull` for all 4 renewal FKs
    - Migration file exists matching pattern `*_AddRenewalChainFKs.cs`
    - `dotnet build` exits 0
    - `dotnet ef database update` exits 0
  </acceptance_criteria>
  <done>4 kolom FK renewal (RenewsSessionId + RenewsTrainingId di kedua tabel) terbuat di database dengan index dan ON DELETE SET NULL. Build sukses tanpa error.</done>
</task>

</tasks>

<verification>
- `dotnet build` — zero errors
- `dotnet ef migrations list` — migration AddRenewalChainFKs tercantum dan applied
- Database memiliki 4 kolom baru: AssessmentSessions.RenewsSessionId, AssessmentSessions.RenewsTrainingId, TrainingRecords.RenewsTrainingId, TrainingRecords.RenewsSessionId
</verification>

<success_criteria>
- Build sukses
- Migration applied tanpa error
- Semua 4 FK columns nullable dengan index
</success_criteria>

<output>
After completion, create `.planning/phases/200-renewal-chain-foundation/200-01-SUMMARY.md`
</output>
