---
phase: 200-renewal-chain-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 200-01
files_modified:
  - Models/CertificationManagementViewModel.cs
  - Controllers/CDPController.cs
autonomous: true
requirements:
  - RENEW-02
user_setup: []

must_haves:
  truths:
    - "Sertifikat yang memiliki renewal lulus mengembalikan IsRenewed=true"
    - "Sertifikat tanpa renewal yang lulus tetap IsRenewed=false meskipun ada attempt gagal"
    - "Sertifikat dari TrainingRecord yang di-renew oleh TrainingRecord lain juga IsRenewed=true"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "IsRenewed property pada SertifikatRow"
      contains: "public bool IsRenewed"
    - path: "Controllers/CDPController.cs"
      provides: "Renewal chain resolution batch queries"
      contains: "renewedAssessmentSessionIds"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Models/CertificationManagementViewModel.cs"
      via: "IsRenewed assignment in SertifikatRow mapping"
      pattern: "IsRenewed = renewed"
---

<objective>
Menambahkan property IsRenewed ke SertifikatRow dan meng-enhance BuildSertifikatRowsAsync untuk menghitung flag tersebut via batch lookup.

Purpose: Downstream phases (202, 204) membutuhkan informasi renewal status per sertifikat.
Output: BuildSertifikatRowsAsync menghasilkan SertifikatRow dengan IsRenewed yang akurat.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-renewal-chain-foundation/200-CONTEXT.md
@.planning/phases/200-renewal-chain-foundation/200-RESEARCH.md
@.planning/phases/200-renewal-chain-foundation/200-01-SUMMARY.md

<interfaces>
From Models/CertificationManagementViewModel.cs - SertifikatRow class:
```csharp
public class SertifikatRow
{
    public int SourceId { get; set; }
    public RecordType RecordType { get; set; }
    public string NamaWorker { get; set; } = "";
    public string? Bagian { get; set; }
    public string? Unit { get; set; }
    public string Judul { get; set; } = "";
    public string? Kategori { get; set; }
    public string? SubKategori { get; set; }
    public string? NomorSertifikat { get; set; }
    public DateTime? TanggalTerbit { get; set; }
    public DateTime? ValidUntil { get; set; }
    public CertificateStatus Status { get; set; }
    public string? SertifikatUrl { get; set; }
}
```

From Controllers/CDPController.cs - BuildSertifikatRowsAsync (line 3187-3336):
- Returns `(List<SertifikatRow> rows, int roleLevel)`
- trainingRows mapping at line 3253-3268
- assessmentRows mapping at line 3303-3329
- Merge at line 3332-3334
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah IsRenewed ke SertifikatRow + enhance BuildSertifikatRowsAsync dengan batch renewal lookup</name>
  <files>Models/CertificationManagementViewModel.cs, Controllers/CDPController.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs
    - Controllers/CDPController.cs (lines 3187-3336 — BuildSertifikatRowsAsync)
  </read_first>
  <action>
1. **Models/CertificationManagementViewModel.cs** — Tambahkan property ke class `SertifikatRow`, SETELAH `SertifikatUrl`:

```csharp
/// <summary>
/// True jika ada sesi/record renewal yang lulus mengarah ke sertifikat ini.
/// Dihitung oleh BuildSertifikatRowsAsync. Orthogonal terhadap Status.
/// </summary>
public bool IsRenewed { get; set; }
```

2. **Controllers/CDPController.cs** — Di method `BuildSertifikatRowsAsync`, tambahkan batch renewal lookup SETELAH `var assessmentAnon = await asQuery...ToListAsync();` (sekitar line 3301) dan SEBELUM `var assessmentRows = assessmentAnon.Select(...)` (line 3303):

```csharp
// ===== Renewal chain resolution: batch lookup =====
// Set 1: AS IDs renewed by another AS (IsPassed==true)
var renewedByAsSessionIds = await _context.AssessmentSessions
    .Where(a => a.RenewsSessionId.HasValue && a.IsPassed == true)
    .Select(a => a.RenewsSessionId!.Value)
    .Distinct()
    .ToListAsync();

// Set 2: AS IDs renewed by a TR
var renewedByTrSessionIds = await _context.TrainingRecords
    .Where(t => t.RenewsSessionId.HasValue)
    .Select(t => t.RenewsSessionId!.Value)
    .Distinct()
    .ToListAsync();

// Set 3: TR IDs renewed by an AS (IsPassed==true)
var renewedByAsTrainingIds = await _context.AssessmentSessions
    .Where(a => a.RenewsTrainingId.HasValue && a.IsPassed == true)
    .Select(a => a.RenewsTrainingId!.Value)
    .Distinct()
    .ToListAsync();

// Set 4: TR IDs renewed by another TR
var renewedByTrTrainingIds = await _context.TrainingRecords
    .Where(t => t.RenewsTrainingId.HasValue)
    .Select(t => t.RenewsTrainingId!.Value)
    .Distinct()
    .ToListAsync();

// Merge: all AS IDs that have been renewed
var renewedAssessmentSessionIds = new HashSet<int>(renewedByAsSessionIds);
renewedAssessmentSessionIds.UnionWith(renewedByTrSessionIds);

// Merge: all TR IDs that have been renewed
var renewedTrainingRecordIds = new HashSet<int>(renewedByAsTrainingIds);
renewedTrainingRecordIds.UnionWith(renewedByTrTrainingIds);
```

CATATAN: Gunakan `.ToListAsync()` lalu `new HashSet<int>(...)` karena `ToHashSetAsync()` mungkin tidak tersedia. Tambahkan `.Distinct()` untuk efisiensi.

3. **Pada mapping trainingRows** (sekitar line 3253-3268), tambahkan `IsRenewed` di SertifikatRow initializer:
```csharp
IsRenewed = renewedTrainingRecordIds.Contains(t.Id)
```

4. **Pada mapping assessmentRows** (sekitar line 3303-3329), tambahkan `IsRenewed` di SertifikatRow initializer (di dalam `return new SertifikatRow { ... }`):
```csharp
IsRenewed = renewedAssessmentSessionIds.Contains(a.Id)
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - Models/CertificationManagementViewModel.cs contains `public bool IsRenewed { get; set; }`
    - Controllers/CDPController.cs contains `var renewedAssessmentSessionIds = new HashSet<int>`
    - Controllers/CDPController.cs contains `var renewedTrainingRecordIds = new HashSet<int>`
    - Controllers/CDPController.cs contains `IsRenewed = renewedTrainingRecordIds.Contains(t.Id)`
    - Controllers/CDPController.cs contains `IsRenewed = renewedAssessmentSessionIds.Contains(a.Id)`
    - Controllers/CDPController.cs contains `a.RenewsSessionId.HasValue && a.IsPassed == true`
    - Controllers/CDPController.cs contains `t.RenewsSessionId.HasValue` (TR renewal — no IsPassed check)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>BuildSertifikatRowsAsync mengembalikan IsRenewed=true hanya untuk sertifikat yang memiliki renewal lulus. Build sukses.</done>
</task>

</tasks>

<verification>
- `dotnet build` — zero errors
- SertifikatRow memiliki property IsRenewed
- BuildSertifikatRowsAsync memiliki 4 batch queries untuk renewal lookup
- Mapping trainingRows dan assessmentRows menggunakan HashSet.Contains untuk set IsRenewed
</verification>

<success_criteria>
- Build sukses
- IsRenewed flag dihitung dari batch lookup, bukan per-row query
- Logika: AS renewed jika ada AS(IsPassed==true) atau TR yang menunjuk ke-nya; TR renewed jika ada AS(IsPassed==true) atau TR yang menunjuk ke-nya
</success_criteria>

<output>
After completion, create `.planning/phases/200-renewal-chain-foundation/200-02-SUMMARY.md`
</output>
