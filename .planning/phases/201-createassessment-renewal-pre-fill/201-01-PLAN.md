---
phase: 201-createassessment-renewal-pre-fill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/CreateAssessment.cshtml
autonomous: true
requirements:
  - RENEW-03
must_haves:
  truths:
    - "GET /Admin/CreateAssessment?renewSessionId={id} menampilkan form dengan Title, Category, peserta ter-pre-fill dari AssessmentSession asal"
    - "GET /Admin/CreateAssessment?renewTrainingId={id} menampilkan form dengan Title ter-pre-fill dari TrainingRecord asal"
    - "Banner alert-info Mode Renewal muncul di atas wizard saat renewal mode aktif"
    - "GenerateCertificate otomatis checked di renewal mode"
    - "ValidUntil wajib diisi di renewal mode — submit tanpa ValidUntil menghasilkan validation error"
    - "POST menyimpan RenewsSessionId atau RenewsTrainingId ke AssessmentSession yang dibuat"
    - "Query param invalid redirect ke CreateAssessment biasa + TempData warning"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "CreateAssessment GET dengan renewSessionId/renewTrainingId params, POST dengan renewal FK save"
      contains: "renewSessionId"
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "Renewal banner, hidden fields, ValidUntil required marker, auto-check GenerateCertificate"
      contains: "IsRenewalMode"
  key_links:
    - from: "Views/Admin/CreateAssessment.cshtml"
      to: "Controllers/AdminController.cs"
      via: "hidden input RenewsSessionId/RenewsTrainingId POST binding"
      pattern: "name=\"RenewsSessionId\""
    - from: "Controllers/AdminController.cs"
      to: "Models/AssessmentSession.cs"
      via: "session.RenewsSessionId = model.RenewsSessionId"
      pattern: "RenewsSessionId = model\\.RenewsSessionId"
---

<objective>
CreateAssessment GET menerima query param renewSessionId/renewTrainingId, pre-fill form dari sertifikat asal, dan POST menyimpan renewal FK. Banner renewal mode muncul di view dengan tombol Batalkan Renewal.

Purpose: Memungkinkan HC/Admin memulai renewal sertifikat via URL — fondasi untuk tombol Renew di Phase 202.
Output: Controller GET/POST yang renewal-aware + view dengan banner dan conditional fields.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-createassessment-renewal-pre-fill/201-CONTEXT.md
@.planning/phases/201-createassessment-renewal-pre-fill/201-RESEARCH.md
@.planning/phases/201-createassessment-renewal-pre-fill/201-UI-SPEC.md

<interfaces>
<!-- From Models/AssessmentSession.cs — FK renewal sudah ada dari Phase 200 -->
public int? RenewsSessionId { get; set; }   // line 107
public int? RenewsTrainingId { get; set; }  // line 114

<!-- From Controllers/AdminController.cs — CreateAssessment GET (line 947-989) -->
<!-- Existing signature: public async Task<IActionResult> CreateAssessment() -->
<!-- POST signature (line 995): public async Task<IActionResult> CreateAssessment(AssessmentSession model, List<string> UserIds) -->

<!-- ModelState.Remove("ValidUntil") at line 1064 — MUST be conditional in renewal mode -->
<!-- Session creation loop starts at line 1196 — RenewsSessionId/RenewsTrainingId must be assigned -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller GET/POST renewal-aware logic</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 945-1230 — full CreateAssessment GET+POST)
    - Models/AssessmentSession.cs (RenewsSessionId, RenewsTrainingId fields)
    - Models/TrainingRecord.cs (Judul, ValidUntil, UserId fields)
    - .planning/phases/201-createassessment-renewal-pre-fill/201-RESEARCH.md
  </read_first>
  <action>
Modifikasi CreateAssessment GET action (line 947):

1. Ubah signature menjadi:
   `public async Task<IActionResult> CreateAssessment(int? renewSessionId = null, int? renewTrainingId = null)`

2. Setelah model dibuat (setelah line 986), tambahkan blok renewal:

```csharp
bool isRenewalMode = false;

if (renewSessionId.HasValue)
{
    var sourceSession = await _context.AssessmentSessions
        .Include(s => s.User)
        .FirstOrDefaultAsync(s => s.Id == renewSessionId.Value);

    if (sourceSession == null)
    {
        TempData["Warning"] = "Sertifikat asal tidak ditemukan.";
        return RedirectToAction("CreateAssessment");
    }

    isRenewalMode = true;
    model.Title = sourceSession.Title;
    model.Category = sourceSession.Category;
    model.GenerateCertificate = true;
    if (sourceSession.ValidUntil.HasValue)
        model.ValidUntil = sourceSession.ValidUntil.Value.AddYears(1);

    ViewBag.SelectedUserIds = new List<string> { sourceSession.UserId };
    ViewBag.RenewalSourceTitle = sourceSession.Title;
    ViewBag.RenewalSourceUserName = sourceSession.User?.FullName ?? "";
    ViewBag.RenewsSessionId = renewSessionId.Value;
}
else if (renewTrainingId.HasValue)
{
    var sourceTraining = await _context.TrainingRecords
        .Include(t => t.User)
        .FirstOrDefaultAsync(t => t.Id == renewTrainingId.Value);

    if (sourceTraining == null)
    {
        TempData["Warning"] = "Sertifikat asal tidak ditemukan.";
        return RedirectToAction("CreateAssessment");
    }

    isRenewalMode = true;
    model.Title = sourceTraining.Judul ?? "";
    model.GenerateCertificate = true;
    if (sourceTraining.ValidUntil.HasValue)
        model.ValidUntil = sourceTraining.ValidUntil.Value.AddYears(1);

    ViewBag.RenewalSourceTitle = sourceTraining.Judul ?? "";
    ViewBag.RenewalSourceUserName = sourceTraining.User?.FullName ?? "";
    ViewBag.RenewsTrainingId = renewTrainingId.Value;
}

ViewBag.IsRenewalMode = isRenewalMode;
```

PENTING: blok `ViewBag.SelectedUserIds = new List<string>();` di line 957 harus dipindah SEBELUM blok renewal, agar renewal bisa override. Atau cukup pindahkan assignment SelectedUserIds untuk renewal ke SETELAH existing line 957.

3. Modifikasi POST action (line 995+):

a. Setelah line 1064 `ModelState.Remove("ValidUntil")`, ubah menjadi CONDITIONAL:
```csharp
// ValidUntil: opsional di normal mode, wajib di renewal mode
bool isRenewalModePost = model.RenewsSessionId.HasValue || model.RenewsTrainingId.HasValue;
if (!isRenewalModePost)
{
    ModelState.Remove("ValidUntil");
}
else
{
    // Tetap remove default ModelState error, lalu tambah custom validation
    ModelState.Remove("ValidUntil");
    if (!model.ValidUntil.HasValue)
    {
        ModelState.AddModelError("ValidUntil", "Tanggal expired sertifikat wajib diisi untuk renewal.");
    }
}

// XOR validation
if (model.RenewsSessionId.HasValue && model.RenewsTrainingId.HasValue)
{
    ModelState.AddModelError("", "Hanya satu renewal FK yang boleh diisi.");
}
```

b. Di SETIAP `return View(model)` dalam POST action (ada 3 tempat: line 1087, 1157, 1175), tambahkan reload ViewBag renewal data:
```csharp
if (model.RenewsSessionId.HasValue || model.RenewsTrainingId.HasValue)
{
    ViewBag.IsRenewalMode = true;
    ViewBag.RenewsSessionId = model.RenewsSessionId;
    ViewBag.RenewsTrainingId = model.RenewsTrainingId;
    ViewBag.RenewalSourceTitle = model.Title ?? "";
    ViewBag.RenewalSourceUserName = "";
}
```

c. Di session creation loop (line 1199-1218), tambahkan setelah `CreatedBy = currentUser?.Id`:
```csharp
RenewsSessionId = model.RenewsSessionId,
RenewsTrainingId = model.RenewsTrainingId,
```

CATATAN: Hanya session PERTAMA (i==0) yang mendapat renewal FK, karena renewal 1-to-1. Ubah assignment menjadi:
```csharp
RenewsSessionId = (i == 0) ? model.RenewsSessionId : null,
RenewsTrainingId = (i == 0) ? model.RenewsTrainingId : null,
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `int? renewSessionId = null, int? renewTrainingId = null` in GET signature
    - AdminController.cs contains `ViewBag.IsRenewalMode = isRenewalMode;`
    - AdminController.cs contains `TempData["Warning"] = "Sertifikat asal tidak ditemukan.";`
    - AdminController.cs contains `ModelState.AddModelError("ValidUntil", "Tanggal expired sertifikat wajib diisi untuk renewal.");`
    - AdminController.cs contains `RenewsSessionId = (i == 0) ? model.RenewsSessionId : null`
    - AdminController.cs contains `model.RenewsSessionId.HasValue && model.RenewsTrainingId.HasValue` (XOR check)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>CreateAssessment GET pre-fills form dari renewal source, POST menyimpan FK dan memvalidasi ValidUntil required. Build sukses.</done>
</task>

<task type="auto">
  <name>Task 2: View renewal banner, hidden fields, conditional ValidUntil</name>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml (full file — banner position, form structure, ValidUntil field, GenerateCertificate checkbox)
    - .planning/phases/201-createassessment-renewal-pre-fill/201-UI-SPEC.md
    - .planning/phases/201-createassessment-renewal-pre-fill/201-RESEARCH.md (Pattern 5, 6, hidden fields)
  </read_first>
  <action>
Modifikasi CreateAssessment.cshtml:

1. BANNER RENEWAL — Setelah blok TempData alert (alert-success/warning/error), sebelum wizard `<nav>` nav-pills, tambahkan:
```html
@if (ViewBag.IsRenewalMode == true)
{
    <div class="alert alert-info d-flex align-items-center justify-content-between mb-3" role="alert">
        <div>
            <i class="bi bi-arrow-repeat me-2"></i>
            <strong>Mode Renewal:</strong> @ViewBag.RenewalSourceTitle
            @if (!string.IsNullOrEmpty(ViewBag.RenewalSourceUserName as string))
            {
                <span> — @ViewBag.RenewalSourceUserName</span>
            }
        </div>
        <a href="@Url.Action("CreateAssessment", "Admin")" class="btn btn-sm btn-outline-secondary ms-3">
            Batalkan Renewal
        </a>
    </div>
}
```

2. HIDDEN FIELDS — Di dalam `<form>`, langsung setelah `@Html.AntiForgeryToken()`:
```html
@if (ViewBag.RenewsSessionId != null)
{
    <input type="hidden" name="RenewsSessionId" value="@ViewBag.RenewsSessionId" />
}
@if (ViewBag.RenewsTrainingId != null)
{
    <input type="hidden" name="RenewsTrainingId" value="@ViewBag.RenewsTrainingId" />
}
```

3. VALIDUNTIL LABEL — Cari label field ValidUntil di Step 3. Ubah menjadi:
```html
<label asp-for="ValidUntil" class="form-label fw-bold">
    Tanggal Expired Sertifikat
    @if (ViewBag.IsRenewalMode == true)
    {
        <span class="text-danger">*</span>
    }
    else
    {
        <span class="text-muted">(opsional)</span>
    }
</label>
```

4. GENERATECERTIFICATE AUTO-CHECK — Cari checkbox GenerateCertificate. Jika view menggunakan `asp-for="GenerateCertificate"` maka pre-fill dari model.GenerateCertificate=true di controller sudah cukup (checked otomatis). Tidak perlu modifikasi view.

5. SUBCATEGORY JS TRIGGER — Di bagian bawah file (script section), tambahkan inline script conditional:
```html
@if (ViewBag.IsRenewalMode == true)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var catSelect = document.getElementById('Category');
            if (catSelect && catSelect.value) {
                catSelect.dispatchEvent(new Event('change'));
            }
        });
    </script>
}
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - CreateAssessment.cshtml contains `ViewBag.IsRenewalMode == true`
    - CreateAssessment.cshtml contains `alert alert-info`
    - CreateAssessment.cshtml contains `bi bi-arrow-repeat`
    - CreateAssessment.cshtml contains `Mode Renewal:`
    - CreateAssessment.cshtml contains `Batalkan Renewal`
    - CreateAssessment.cshtml contains `name="RenewsSessionId"` (hidden input)
    - CreateAssessment.cshtml contains `name="RenewsTrainingId"` (hidden input)
    - CreateAssessment.cshtml contains `text-danger` within ValidUntil label block
    - CreateAssessment.cshtml contains `dispatchEvent(new Event('change'))`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>View menampilkan banner renewal, hidden fields untuk POST binding, ValidUntil required marker, dan SubCategory JS trigger. Build sukses.</done>
</task>

</tasks>

<verification>
1. `dotnet build` — kompilasi sukses tanpa error
2. Manual browser: GET `/Admin/CreateAssessment?renewSessionId={valid_id}` — banner muncul, Title/Category/peserta ter-pre-fill, GenerateCertificate checked, ValidUntil pre-filled +1 tahun
3. Manual browser: GET `/Admin/CreateAssessment?renewTrainingId={valid_id}` — banner muncul, Title ter-pre-fill, Category/peserta kosong
4. Manual browser: GET `/Admin/CreateAssessment?renewSessionId=99999` — redirect ke CreateAssessment biasa + warning
5. Manual browser: Submit renewal tanpa ValidUntil — validation error muncul
6. Manual browser: Submit renewal lengkap — cek DB AssessmentSession.RenewsSessionId terisi
7. Manual browser: Klik Batalkan Renewal — kembali ke form biasa tanpa param
</verification>

<success_criteria>
- CreateAssessment GET menerima renewSessionId/renewTrainingId dan pre-fill form
- Banner alert-info Mode Renewal tampil dengan tombol Batalkan
- ValidUntil wajib di renewal mode dengan tanda *
- POST menyimpan RenewsSessionId/RenewsTrainingId ke AssessmentSession
- Query param invalid redirect + warning
- dotnet build sukses
</success_criteria>

<output>
After completion, create `.planning/phases/201-createassessment-renewal-pre-fill/201-01-SUMMARY.md`
</output>
