---
phase: 202-renewal-certificate-page-kelola-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/RenewalCertificate.cshtml
  - Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml
autonomous: true
requirements: [RNPAGE-01, RNPAGE-02, RNPAGE-03, RNPAGE-04]

must_haves:
  truths:
    - "Halaman /Admin/RenewalCertificate menampilkan hanya sertifikat Expired dan AkanExpired yang belum di-renew"
    - "Filter Bagian, Unit, Kategori, Status mempersempit daftar via AJAX tanpa reload"
    - "Klik Renew pada satu baris redirect ke CreateAssessment dengan renewSessionId atau renewTrainingId"
    - "Checkbox bulk select hanya mengizinkan kategori sama, Renew Selected redirect multi-param"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "RenewalCertificate, FilterRenewalCertificate actions, BuildRenewalRowsAsync helper"
      contains: "BuildRenewalRowsAsync"
    - path: "Views/Admin/RenewalCertificate.cshtml"
      provides: "Halaman utama dengan filter bar, summary badges, bulk toolbar"
      contains: "cert-table-container"
    - path: "Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml"
      provides: "Partial view tabel dengan checkbox, badge status, tombol Renew"
      contains: "cb-select"
  key_links:
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "/Admin/FilterRenewalCertificate"
      via: "fetch AJAX call"
      pattern: "FilterRenewalCertificate"
    - from: "Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml"
      to: "/Admin/CreateAssessment"
      via: "Renew button href and JS renewSelected()"
      pattern: "CreateAssessment"
---

<objective>
Membangun halaman Renewal Sertifikat lengkap di AdminController — controller actions (GET + AJAX filter), view utama dengan filter bar dan summary badges, partial view tabel dengan checkbox bulk select dan tombol Renew.

Purpose: HC/Admin memerlukan halaman khusus untuk melihat dan mengelola sertifikat expired/akan expired yang belum di-renew.
Output: 3 file — controller actions, view utama, partial view tabel.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-renewal-certificate-page-kelola-data/202-CONTEXT.md
@.planning/phases/202-renewal-certificate-page-kelola-data/202-RESEARCH.md
@.planning/phases/202-renewal-certificate-page-kelola-data/202-UI-SPEC.md

<interfaces>
From Models/CertificationManagementViewModel.cs:
```csharp
public enum RecordType { Training, Assessment }
public enum CertificateStatus { Aktif, AkanExpired, Expired, Permanent }

public class SertifikatRow
{
    public int SourceId { get; set; }
    public RecordType RecordType { get; set; }
    public string NamaWorker { get; set; } = "";
    public string? Bagian { get; set; }
    public string? Unit { get; set; }
    public string Judul { get; set; } = "";
    public string? Kategori { get; set; }
    public string? SubKategori { get; set; }
    public DateTime? ValidUntil { get; set; }
    public CertificateStatus Status { get; set; }
    public bool IsRenewed { get; set; }
    public static CertificateStatus DeriveCertificateStatus(DateTime? validUntil, string? certificateType) { ... }
}

public class CertificationManagementViewModel
{
    public List<SertifikatRow> Rows { get; set; } = new();
    public int TotalCount { get; set; }
    public int AkanExpiredCount { get; set; }
    public int ExpiredCount { get; set; }
    public int CurrentPage { get; set; }
    public int TotalPages { get; set; }
    public int PageSize { get; set; } = 20;
}
```

From Helpers/PaginationHelper.cs:
```csharp
public static (int Skip, int Take, int CurrentPage, int TotalPages) Calculate(int totalItems, int page, int pageSize);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller — BuildRenewalRowsAsync + RenewalCertificate + FilterRenewalCertificate</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (full file — find existing patterns, Index action for ViewBag, end of file for insertion point)
    - Controllers/CDPController.cs lines 3082-3350 (BuildSertifikatRowsAsync + FilterCertificationManagement — copy/adapt pattern)
    - Models/CertificationManagementViewModel.cs (SertifikatRow, CertificationManagementViewModel, DeriveCertificateStatus)
    - Helpers/PaginationHelper.cs (Calculate signature)
  </read_first>
  <action>
Add 3 members to AdminController:

**1. `BuildRenewalRowsAsync()` private async method:**
- Copy logic from CDPController.BuildSertifikatRowsAsync but WITHOUT role-level scoping (Admin/HC = full access)
- Query TrainingRecords with SertifikatUrl != null, Include User
- Query AssessmentSessions with GenerateCertificate && IsPassed == true, Include User
- Batch renewal lookup: 4 sets (renewedByAsSessionIds, renewedByAsTrRecordIds, renewedByTrSessionIds, renewedByTrTrRecordIds) — identical to CDPController
- Map to SertifikatRow list with IsRenewed calculated from the 4 sets
- POST-FILTER: keep only rows where `!r.IsRenewed && (r.Status == CertificateStatus.Expired || r.Status == CertificateStatus.AkanExpired)`
- Apply default sorting: `.OrderBy(r => r.Status == CertificateStatus.Expired ? 0 : 1).ThenBy(r => r.ValidUntil ?? DateTime.MaxValue)`
- Return `List<SertifikatRow>`

**2. `RenewalCertificate(int page = 1)` GET action:**
- `[HttpGet]`, `[Authorize(Roles = "Admin, HC")]`
- Call `BuildRenewalRowsAsync()`
- Build `CertificationManagementViewModel` with TotalCount, ExpiredCount, AkanExpiredCount
- Apply pagination via `PaginationHelper.Calculate(allRows.Count, page, vm.PageSize)`
- Set `vm.Rows`, `vm.CurrentPage`, `vm.TotalPages`
- Populate ViewBag for filter dropdowns:
  - `ViewBag.AllBagian` = distinct Bagian from allRows (non-null)
  - `ViewBag.AllCategories` = distinct Kategori from allRows (non-null)
  - `ViewBag.SelectedView = "RenewalCertificate"`
- Return `View(vm)`

**3. `FilterRenewalCertificate(string? bagian, string? unit, string? status, string? category, int page = 1)` GET action:**
- `[HttpGet]`, `[Authorize(Roles = "Admin, HC")]`
- Call `BuildRenewalRowsAsync()`
- Apply filters in-memory: bagian, unit, status (parse to CertificateStatus enum), category
- Apply same default sorting
- Build CertificationManagementViewModel with counts from filtered list
- Paginate
- Return `PartialView("Shared/_RenewalCertificateTablePartial", vm)`

Place all 3 members at the end of AdminController (before closing brace).
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `private async Task<List<SertifikatRow>> BuildRenewalRowsAsync()`
    - AdminController.cs contains `public async Task<IActionResult> RenewalCertificate(int page = 1)`
    - AdminController.cs contains `[Authorize(Roles = "Admin, HC")]` before RenewalCertificate
    - AdminController.cs contains `public async Task<IActionResult> FilterRenewalCertificate(`
    - AdminController.cs contains `!r.IsRenewed`
    - AdminController.cs contains `CertificateStatus.Expired ? 0 : 1`
    - AdminController.cs contains `PaginationHelper.Calculate`
    - AdminController.cs contains `PartialView("Shared/_RenewalCertificateTablePartial"`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>BuildRenewalRowsAsync queries and filters correctly, RenewalCertificate returns full page, FilterRenewalCertificate returns partial view, project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Views — RenewalCertificate.cshtml + _RenewalCertificateTablePartial.cshtml</name>
  <files>Views/Admin/RenewalCertificate.cshtml, Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml</files>
  <read_first>
    - Views/CDP/CertificationManagement.cshtml (AJAX filter pattern, summary badges, loading overlay — reference implementation)
    - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml (table structure, badge styling, pagination, empty state — reference implementation)
    - .planning/phases/202-renewal-certificate-page-kelola-data/202-UI-SPEC.md (visual/interaction contract)
    - Controllers/AdminController.cs (verify action names match — RenewalCertificate, FilterRenewalCertificate)
  </read_first>
  <action>
**RenewalCertificate.cshtml:**
- `@model CertificationManagementViewModel`
- Layout structure per UI-SPEC: `container-fluid py-4`
- Header: `h4` with `bi-arrow-repeat text-warning` icon + "Renewal Sertifikat"
- Breadcrumb: `Kelola Data > Renewal Sertifikat` (Kelola Data links to `/Admin/Index`)
- Summary row: 2 cards in `row g-3 mb-4` — Expired (count `fs-2 fw-bold text-danger`) and Akan Expired (count `fs-2 fw-bold text-warning`)
- Toolbar row: `div` containing `btn btn-warning d-none` id `btn-renew-selected` with text "Renew Selected (0)" — shown/hidden by JS
- Filter bar card: `card border-0 shadow-sm mb-4` with `row g-2` containing:
  - Bagian dropdown (`form-select form-select-sm`), options from `ViewBag.AllBagian`, default "Semua Bagian"
  - Unit dropdown (disabled initially), populated via AJAX `/CDP/GetCascadeOptions?section=`
  - Kategori dropdown from `ViewBag.AllCategories`, default "Semua Kategori"
  - Status dropdown: hardcoded "Semua Status" / "Expired" / "AkanExpired"
  - Reset button: `btn btn-outline-secondary btn-sm w-100`
- Table container: `card border-0 shadow-sm` with `div#cert-table-container` that renders `@Html.Partial("Shared/_RenewalCertificateTablePartial", Model)`
- Loading overlay: `.dashboard-loading` div (same as CertificationManagement)

**JavaScript (inline @section Scripts or bottom of file):**
- AJAX `refreshTable(page)` function: build URLSearchParams from filter values, fetch `/Admin/FilterRenewalCertificate?` + params, replace `cert-table-container` innerHTML, call `wireCheckboxes()` and `wirePagination()`
- AbortController pattern (identical to CertificationManagement)
- Cascade Bagian→Unit: on Bagian change, fetch `/CDP/GetCascadeOptions?section=` to populate Unit dropdown
- Reset button: clear all filters, disable Unit, call refreshTable
- `wireCheckboxes()`: attach change listeners to `.cb-select`, implement category lock logic:
  - Track `selectedKategori` (null initially)
  - On check: set selectedKategori if null, disable checkboxes where `dataset.kategori !== selectedKategori`, add title "Hanya sertifikat kategori sama"
  - On uncheck: if no more checked, reset selectedKategori to null, enable all checkboxes
  - Treat empty/null kategori as string `""` — can only bulk with other empty kategori
  - Call `updateRenewSelectedButton()`
- `updateRenewSelectedButton()`: count checked `.cb-select:checked`, toggle `d-none` on `btn-renew-selected`, update text with count
- `renewSelected()`: gather checked checkboxes, build URLSearchParams with `params.append('renewSessionId', sourceid)` or `params.append('renewTrainingId', sourceid)` based on `dataset.recordtype`, redirect to `/Admin/CreateAssessment?` + params
- `wirePagination()`: attach click to `.page-link[data-page]`, call refreshTable(page)
- On page load: call `wireCheckboxes()` and `wirePagination()`
- On any filter change: reset selectedKategori, hide Renew Selected button, call refreshTable(1)

**_RenewalCertificateTablePartial.cshtml:**
- `@model CertificationManagementViewModel`
- Summary badges update: hidden inputs or data attributes for ExpiredCount, AkanExpiredCount (so parent page JS can update summary cards after AJAX)
- Table: `table table-striped table-hover mb-0`
- Thead `table-light` with columns: Checkbox (header checkbox for select-all same page), No, Nama, Judul Sertifikat, Kategori (`d-none d-md-table-cell`), Sub Kategori (`d-none d-md-table-cell`), Valid Until, Status, Aksi
- Tbody: foreach row with index, render:
  - Checkbox: `input.form-check-input.cb-select` with `data-kategori="@(row.Kategori ?? "")"` `data-sourceid="@row.SourceId"` `data-recordtype="@row.RecordType"` `aria-label="Pilih @row.NamaWorker - @row.Judul"`
  - No: `((Model.CurrentPage - 1) * Model.PageSize) + index + 1`
  - Nama: `@row.NamaWorker`
  - Judul: `@row.Judul`
  - Kategori: `@(row.Kategori ?? "-")`
  - Sub Kategori: `@(row.SubKategori ?? "-")`
  - Valid Until: `@row.ValidUntil?.ToString("dd MMM yyyy")`
  - Status: `<span class="badge @(row.Status == CertificateStatus.Expired ? "bg-danger" : "bg-warning text-dark")">@(row.Status == CertificateStatus.Expired ? "Expired" : "Akan Expired")</span>`
  - Aksi: Renew button `btn btn-sm btn-warning` with href to CreateAssessment using renewSessionId or renewTrainingId based on RecordType, `aria-label="Renew sertifikat @row.Judul untuk @row.NamaWorker"`
- Empty state: if `Model.Rows.Count == 0`, show `tr` with full colspan, centered `bi-patch-check-fill fs-1 text-success`, text "Tidak ada sertifikat yang perlu di-renew", subtitle "Semua sertifikat aktif atau sudah di-renew.", link "Kembali ke Kelola Data" (`btn btn-outline-secondary`)
- Pagination: if `Model.TotalPages > 1`, render `nav` with `ul.pagination.justify-content-center`, page items with `data-page` attribute, active page highlighted
- Hidden data for parent JS: `<span id="partial-expired-count" class="d-none">@Model.ExpiredCount</span>` and `<span id="partial-akan-expired-count" class="d-none">@Model.AkanExpiredCount</span>`
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File Views/Admin/RenewalCertificate.cshtml exists and contains `@model HcPortal.Models.CertificationManagementViewModel` or `@model CertificationManagementViewModel`
    - RenewalCertificate.cshtml contains `FilterRenewalCertificate`
    - RenewalCertificate.cshtml contains `btn-renew-selected`
    - RenewalCertificate.cshtml contains `GetCascadeOptions`
    - RenewalCertificate.cshtml contains `selectedKategori`
    - RenewalCertificate.cshtml contains `renewSelected`
    - File Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml exists
    - _RenewalCertificateTablePartial.cshtml contains `cb-select`
    - _RenewalCertificateTablePartial.cshtml contains `data-kategori`
    - _RenewalCertificateTablePartial.cshtml contains `data-sourceid`
    - _RenewalCertificateTablePartial.cshtml contains `data-recordtype`
    - _RenewalCertificateTablePartial.cshtml contains `CreateAssessment`
    - _RenewalCertificateTablePartial.cshtml contains `bi-patch-check-fill`
    - _RenewalCertificateTablePartial.cshtml contains `pagination`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Halaman Renewal Sertifikat menampilkan tabel dengan filter AJAX, checkbox bulk select dengan category lock, tombol Renew satuan dan bulk, empty state, pagination</done>
</task>

</tasks>

<verification>
- `dotnet build` compiles without errors
- Navigate to /Admin/RenewalCertificate — page loads with filter bar, summary badges, table
- Filter dropdown changes trigger AJAX table refresh
- Checkbox category lock works — different category checkboxes get disabled
- Renew button links to correct CreateAssessment URL
- Empty state shows when no rows match
</verification>

<success_criteria>
- Halaman /Admin/RenewalCertificate menampilkan hanya sertifikat Expired dan AkanExpired yang belum di-renew
- Filter Bagian, Unit, Kategori, Status mempersempit daftar via AJAX
- Tombol Renew satuan redirect ke CreateAssessment dengan param yang benar
- Bulk select hanya mengizinkan kategori sama, Renew Selected redirect multi-param
- Pagination 20 baris per halaman
</success_criteria>

<output>
After completion, create `.planning/phases/202-renewal-certificate-page-kelola-data/202-01-SUMMARY.md`
</output>
