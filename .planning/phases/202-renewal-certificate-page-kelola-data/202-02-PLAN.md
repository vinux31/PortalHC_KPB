---
phase: 202-renewal-certificate-page-kelola-data
plan: 02
type: execute
wave: 2
depends_on: [202-01]
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/Index.cshtml
autonomous: true
requirements: [RNPAGE-05]

must_haves:
  truths:
    - "Card Renewal Sertifikat muncul di Kelola Data Section C dengan badge count"
    - "Badge count menampilkan jumlah total sertifikat expired/akan expired yang belum di-renew"
  artifacts:
    - path: "Views/Admin/Index.cshtml"
      provides: "Card Renewal Sertifikat di Section C"
      contains: "bi-arrow-repeat"
    - path: "Controllers/AdminController.cs"
      provides: "ViewBag.RenewalCount di Index action"
      contains: "RenewalCount"
  key_links:
    - from: "Views/Admin/Index.cshtml"
      to: "/Admin/RenewalCertificate"
      via: "Card href"
      pattern: "RenewalCertificate"
---

<objective>
Menambahkan card "Renewal Sertifikat" di Kelola Data Section C dengan badge count, dan query ringan di AdminController.Index() untuk menghitung jumlah sertifikat perlu renew.

Purpose: Entry point dari Kelola Data hub ke halaman Renewal Sertifikat.
Output: Card di Index.cshtml + ViewBag.RenewalCount di controller.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/202-renewal-certificate-page-kelola-data/202-CONTEXT.md
@.planning/phases/202-renewal-certificate-page-kelola-data/202-UI-SPEC.md
@.planning/phases/202-renewal-certificate-page-kelola-data/202-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card Renewal Sertifikat di Section C + badge count query di Index</name>
  <files>Controllers/AdminController.cs, Views/Admin/Index.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (find Index GET action — locate existing ViewBag assignments to add RenewalCount query)
    - Views/Admin/Index.cshtml (find Section C closing `</div>` at line ~196 to insert card before it)
    - .planning/phases/202-renewal-certificate-page-kelola-data/202-RESEARCH.md (badge count query pattern — lightweight query without full BuildRenewalRowsAsync)
  </read_first>
  <action>
**AdminController.cs — Index GET action:**

Add lightweight renewal count query inside the existing `Index()` action, BEFORE the `return View()`. Do NOT call BuildRenewalRowsAsync (too expensive for hub page). Instead use direct DB queries:

```csharp
// Renewal badge count (lightweight)
var renewedSessionIds = await _context.AssessmentSessions
    .Where(a => a.RenewsSessionId.HasValue && a.IsPassed == true)
    .Select(a => a.RenewsSessionId!.Value).Distinct().ToListAsync();
var renewedTrainingIds = await _context.AssessmentSessions
    .Where(a => a.RenewsTrainingId.HasValue && a.IsPassed == true)
    .Select(a => a.RenewsTrainingId!.Value).Distinct().ToListAsync();

var expiredTrainingCount = await _context.TrainingRecords
    .Where(t => t.SertifikatUrl != null &&
                t.ValidUntil != null &&
                t.CertificateType != "Permanent" &&
                t.ValidUntil < DateTime.Now.AddDays(30) &&
                !renewedTrainingIds.Contains(t.Id))
    .CountAsync();

var expiredAssessmentCount = await _context.AssessmentSessions
    .Where(a => a.GenerateCertificate && a.IsPassed == true &&
                a.ValidUntil != null &&
                a.ValidUntil < DateTime.Now.AddDays(30) &&
                !renewedSessionIds.Contains(a.Id))
    .CountAsync();

ViewBag.RenewalCount = expiredTrainingCount + expiredAssessmentCount;
```

**Views/Admin/Index.cshtml — Insert card BEFORE the closing `</div>` of Section C row (before line 196):**

Insert this card block inside the existing `<div class="row g-3 mb-2">` of Section C, right before its closing `</div>`:

```razor
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("RenewalCertificate", "Admin")" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-arrow-repeat fs-5 text-warning"></i>
                        <span class="fw-bold">Renewal Sertifikat</span>
                        @if (ViewBag.RenewalCount != null && (int)ViewBag.RenewalCount > 0)
                        {
                            <span class="badge bg-warning text-dark ms-auto">@ViewBag.RenewalCount</span>
                        }
                    </div>
                    <small class="text-muted">Kelola sertifikat expired dan akan expired yang perlu di-renew</small>
                </div>
            </div>
        </a>
    </div>
    }
```

Card uses `bi-arrow-repeat` icon with `text-warning` (orange) per CONTEXT.md locked decision. Badge only shows when count > 0.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `ViewBag.RenewalCount = expiredTrainingCount + expiredAssessmentCount`
    - AdminController.cs contains `renewedSessionIds` in Index action
    - Views/Admin/Index.cshtml contains `bi-arrow-repeat`
    - Views/Admin/Index.cshtml contains `RenewalCertificate`
    - Views/Admin/Index.cshtml contains `RenewalCount`
    - Views/Admin/Index.cshtml contains `bg-warning text-dark ms-auto`
    - Views/Admin/Index.cshtml contains `Renewal Sertifikat`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Card Renewal Sertifikat visible di Section C Kelola Data dengan badge count yang akurat, link ke halaman /Admin/RenewalCertificate berfungsi</done>
</task>

</tasks>

<verification>
- `dotnet build` compiles without errors
- Navigate to /Admin (Kelola Data) — card "Renewal Sertifikat" muncul di Section C
- Badge count menampilkan jumlah total sertifikat yang perlu di-renew
- Klik card redirect ke /Admin/RenewalCertificate
</verification>

<success_criteria>
- Card "Renewal Sertifikat" muncul di Kelola Data Section C dengan icon bi-arrow-repeat text-warning
- Badge count menampilkan angka > 0 jika ada sertifikat perlu renew
- Klik card membuka halaman Renewal Sertifikat
</success_criteria>

<output>
After completion, create `.planning/phases/202-renewal-certificate-page-kelola-data/202-02-SUMMARY.md`
</output>
