---
phase: 202-renewal-certificate-page-kelola-data
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/RenewalCertificate.cshtml
autonomous: true
gap_closure: true
requirements:
  - RNPAGE-03

must_haves:
  truths:
    - "Filter Bagian menampilkan semua section dari OrganizationStructure, bukan hanya yang punya data renewal"
    - "Filter Kategori menampilkan parent categories dari AssessmentCategories table"
    - "Filter Sub Kategori muncul sebagai dropdown cascade dari Kategori, menggunakan endpoint GetSubCategories yang sudah ada"
    - "Memilih Bagian tanpa data renewal menampilkan empty state, bukan tidak muncul di dropdown"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "ViewBag.AllBagian dari OrganizationStructure, ViewBag.AllCategories dari DB, subCategory filter param"
    - path: "Views/Admin/RenewalCertificate.cshtml"
      provides: "Dropdown sub kategori dengan cascade JS"
  key_links:
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "/CDP/GetSubCategories"
      via: "fetch on category change"
      pattern: "GetSubCategories.*category"
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "/Admin/FilterRenewalCertificate"
      via: "refreshTable with subCategory param"
      pattern: "subCategory"
---

<objective>
Fix filter Bagian/Unit agar menampilkan semua opsi dari OrganizationStructure (bukan hanya dari data renewal), fix filter Kategori agar dari AssessmentCategories table (parent only), dan tambah filter Sub Kategori dengan cascade dari Kategori.

Purpose: Menutup 2 gap dari UAT test #3 dan #7
Output: Filter yang berfungsi benar dengan semua opsi tersedia
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Pattern dari CDPController.cs CertificationManagement (baris ~3072): -->
```csharp
ViewBag.AllBagian = OrganizationStructure.GetAllSections();
ViewBag.AllCategories = await _context.AssessmentCategories
    .Where(c => c.ParentId == null && c.IsActive)
    .OrderBy(c => c.SortOrder)
    .Select(c => c.Name)
    .ToListAsync();
```

<!-- Pattern filter subCategory di CDPController FilterCertificationManagement (baris ~3102): -->
```csharp
if (!string.IsNullOrEmpty(subCategory))
    allRows = allRows.Where(r => r.SubKategori == subCategory).ToList();
```

<!-- Endpoint cascade sub kategori sudah ada: CDPController.GetSubCategories (baris ~300) -->
<!-- View pattern dari CertificationManagement.cshtml: fetch('/CDP/GetSubCategories?category=' + ...) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix ViewBag populasi dan tambah subCategory filter di Controller</name>
  <files>Controllers/AdminController.cs</files>
  <action>
Di action RenewalCertificate (~baris 6762-6774):
1. Ganti `ViewBag.AllBagian` dari allRows ke `OrganizationStructure.GetAllSections()` — exact pattern dari CDPController baris 3072
2. Ganti `ViewBag.AllCategories` dari allRows ke query DB: `await _context.AssessmentCategories.Where(c => c.ParentId == null && c.IsActive).OrderBy(c => c.SortOrder).Select(c => c.Name).ToListAsync()` — exact pattern dari CDPController baris 3073-3077

Di action FilterRenewalCertificate (~baris 6783):
3. Tambah parameter `string? subCategory = null` di method signature (setelah category)
4. Tambah filter: `if (!string.IsNullOrEmpty(subCategory)) allRows = allRows.Where(r => r.SubKategori == subCategory).ToList();` — letakkan setelah filter category (~baris 6799)
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>ViewBag.AllBagian dari OrganizationStructure, ViewBag.AllCategories dari DB parent categories, FilterRenewalCertificate menerima dan memproses subCategory parameter</done>
</task>

<task type="auto">
  <name>Task 2: Tambah dropdown Sub Kategori dan update JS di View</name>
  <files>Views/Admin/RenewalCertificate.cshtml</files>
  <action>
Di filter bar (setelah dropdown Kategori ~baris 86):
1. Tambah col-md-2 dengan dropdown Sub Kategori — id="filter-subcategory", disabled by default, option default "Semua Sub Kategori". Copy exact pattern dari CertificationManagement.cshtml.

2. Sesuaikan col-md widths: Bagian(2), Unit(2), Kategori(2), Sub Kategori(2), Status(2), Reset button(2) — total 12 cols.

Di script section:
3. Tambah `var subCategoryEl = document.getElementById('filter-subcategory');`
4. Tambah cascade event pada categoryEl change: fetch('/CDP/GetSubCategories?category=' + encodeURIComponent(cat)), populate subCategoryEl options, enable/disable. Pattern exact dari CertificationManagement.cshtml baris 196-212.
5. Tambah `subCategoryEl.addEventListener('change', function () { resetKategoriLock(); refreshTable(1); });`
6. Di refreshTable function: tambah `if (subCategoryEl.value) params.set('subCategory', subCategoryEl.value);`
7. Di resetBtn click handler: tambah reset subCategoryEl (innerHTML reset + disabled = true). Pattern dari CertificationManagement.cshtml baris 232-233.
8. Di categoryEl change handler yang sudah ada: reset subCategoryEl sebelum fetch sub categories (clear options, disable).
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Dropdown Sub Kategori muncul di filter bar, cascade dari Kategori via GetSubCategories endpoint, refreshTable mengirim subCategory param, reset membersihkan Sub Kategori</done>
</task>

</tasks>

<verification>
1. `dotnet build` sukses tanpa error
2. Buka /Admin/RenewalCertificate — filter Bagian menampilkan semua section (termasuk yang tidak punya data renewal)
3. Filter Kategori menampilkan parent categories dari DB
4. Pilih Kategori — dropdown Sub Kategori terisi otomatis via cascade
5. Pilih Sub Kategori — tabel ter-filter
6. Pilih Bagian yang tidak punya data — tabel menampilkan empty state
7. Reset mengembalikan semua filter termasuk Sub Kategori
</verification>

<success_criteria>
- Semua section OrganizationStructure tampil di dropdown Bagian
- Kategori dari AssessmentCategories (parent only, active)
- Sub Kategori cascade berfungsi
- Filter subCategory memfilter tabel dengan benar
- Kedua UAT gap (test #3 dan #7) tertutup
</success_criteria>

<output>
After completion, create `.planning/phases/202-renewal-certificate-page-kelola-data/202-03-SUMMARY.md`
</output>
