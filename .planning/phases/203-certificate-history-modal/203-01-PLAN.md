---
phase: 203-certificate-history-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/CertificationManagementViewModel.cs
  - Controllers/AdminController.cs
  - Views/Shared/_CertificateHistoryModalContent.cshtml
autonomous: true
requirements: [HIST-01, HIST-02]

must_haves:
  truths:
    - "Endpoint /Admin/CertificateHistory?workerId=X&mode=renewal mengembalikan HTML partial dengan sertifikat grouped by renewal chain"
    - "Sertifikat terbaru di atas per group, group terbaru di atas"
    - "Mode renewal menampilkan tombol Renew pada sertifikat expired/akan expired yang belum di-renew"
    - "Mode readonly tidak menampilkan tombol Renew"
    - "SertifikatRow memiliki property WorkerId"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "WorkerId property di SertifikatRow, CertificateChainGroup class"
      contains: "public string WorkerId"
    - path: "Controllers/AdminController.cs"
      provides: "CertificateHistoryAsync action"
      contains: "CertificateHistory"
    - path: "Views/Shared/_CertificateHistoryModalContent.cshtml"
      provides: "Partial view tabel grouped by chain dengan mode support"
      contains: "CertificateChainGroup"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Views/Shared/_CertificateHistoryModalContent.cshtml"
      via: "PartialView return"
      pattern: 'PartialView.*_CertificateHistoryModalContent'
---

<objective>
Membangun endpoint controller CertificateHistoryAsync dan shared partial view _CertificateHistoryModalContent yang menampilkan sertifikat per pekerja grouped by renewal chain. Termasuk penambahan WorkerId ke SertifikatRow dan CertificateChainGroup ViewModel.

Purpose: Menyediakan backend + view untuk modal history sertifikat yang akan diintegrasikan di Plan 02.
Output: Endpoint yang mengembalikan HTML partial, siap dikonsumsi via AJAX.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-certificate-history-modal/203-CONTEXT.md
@.planning/phases/203-certificate-history-modal/203-RESEARCH.md
@.planning/phases/203-certificate-history-modal/203-UI-SPEC.md

<interfaces>
From Models/CertificationManagementViewModel.cs:
```csharp
public class SertifikatRow
{
    public int SourceId { get; set; }
    public RecordType RecordType { get; set; }
    public string NamaWorker { get; set; } = "";
    public string? Bagian { get; set; }
    public string? Unit { get; set; }
    public string Judul { get; set; } = "";
    public string? Kategori { get; set; }
    public string? SubKategori { get; set; }
    public string? NomorSertifikat { get; set; }
    public DateTime? TanggalTerbit { get; set; }
    public DateTime? ValidUntil { get; set; }
    public CertificateStatus Status { get; set; }
    public string? SertifikatUrl { get; set; }
    public bool IsRenewed { get; set; }
    // WorkerId BELUM ADA — harus ditambah
}

public enum CertificateStatus { Aktif, AkanExpired, Expired, Permanent }
```

From Controllers/AdminController.cs (BuildRenewalRowsAsync pattern):
- trainingAnon query: `_context.TrainingRecords.Include(t => t.User).Where(t => t.SertifikatUrl != null).Select(...)` — TIDAK ada UserId di Select
- assessmentAnon query: `_context.AssessmentSessions.Include(a => a.User).Where(a => a.GenerateCertificate && a.IsPassed == true).Select(...)` — TIDAK ada UserId di Select
- Renewal chain batch lookup: 4 sets (renewedByAsSessionIds, renewedByTrSessionIds, renewedByAsTrainingIds, renewedByTrTrainingIds)
- User FK: `t.User.Id` (TrainingRecord), `a.UserId` (AssessmentSession)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah WorkerId ke SertifikatRow + CertificateChainGroup ViewModel + Update BuildRenewalRowsAsync</name>
  <files>Models/CertificationManagementViewModel.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs (current SertifikatRow model)
    - Controllers/AdminController.cs lines 6604-6750 (BuildRenewalRowsAsync — perlu tambah UserId di anonymous Select dan mapping)
    - Controllers/CDPController.cs lines 3194-3382 (BuildSertifikatRowsAsync — juga perlu tambah WorkerId di mapping)
  </read_first>
  <action>
**1. Models/CertificationManagementViewModel.cs:**

Tambah property `WorkerId` ke `SertifikatRow` setelah `SourceId`:
```csharp
public string WorkerId { get; set; } = "";
```

Tambah class `CertificateChainGroup` di bawah `SertifikatRow` class (dalam file yang sama):
```csharp
public class CertificateChainGroup
{
    public string ChainTitle { get; set; } = "";
    public List<SertifikatRow> Certificates { get; set; } = new();
    public DateTime? LatestValidUntil { get; set; }
}
```

**2. Controllers/AdminController.cs — BuildRenewalRowsAsync:**

Di `trainingAnon` Select, tambah:
```csharp
UserId = t.User != null ? t.User.Id : "",
```

Di `trainingRows` mapping (SertifikatRow construction), tambah:
```csharp
WorkerId = t.UserId,
```

Di `assessmentAnon` Select, tambah:
```csharp
a.UserId,
```

Di `assessmentRows` mapping (SertifikatRow construction), tambah:
```csharp
WorkerId = a.UserId,
```

**3. Controllers/CDPController.cs — BuildSertifikatRowsAsync:**

Sama — tambah `WorkerId` di anonymous Select dan SertifikatRow mapping untuk kedua query (training dan assessment). CDPController sudah punya `a.UserId` / `t.UserId` di scope — cukup tambah ke Select dan mapping.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Models/CertificationManagementViewModel.cs mengandung `public string WorkerId { get; set; } = "";` di dalam class SertifikatRow
    - Models/CertificationManagementViewModel.cs mengandung `public class CertificateChainGroup`
    - CertificateChainGroup memiliki property `ChainTitle`, `Certificates`, `LatestValidUntil`
    - AdminController.cs BuildRenewalRowsAsync trainingRows mapping mengandung `WorkerId = t.UserId`
    - AdminController.cs BuildRenewalRowsAsync assessmentRows mapping mengandung `WorkerId = a.UserId`
    - CDPController.cs BuildSertifikatRowsAsync mapping mengandung `WorkerId =`
    - `dotnet build` sukses tanpa error
  </acceptance_criteria>
  <done>SertifikatRow memiliki WorkerId, CertificateChainGroup ViewModel ada, BuildRenewalRowsAsync dan BuildSertifikatRowsAsync mengisi WorkerId</done>
</task>

<task type="auto">
  <name>Task 2: CertificateHistoryAsync action + _CertificateHistoryModalContent partial view</name>
  <files>Controllers/AdminController.cs, Views/Shared/_CertificateHistoryModalContent.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs lines 6604-6750 (BuildRenewalRowsAsync — reuse query pattern)
    - Models/CertificationManagementViewModel.cs (SertifikatRow, CertificateChainGroup setelah Task 1)
    - Models/AssessmentSession.cs (RenewsSessionId, RenewsTrainingId FK)
    - Models/TrainingRecord.cs (RenewsSessionId, RenewsTrainingId FK)
    - Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml (badge pattern)
    - .planning/phases/203-certificate-history-modal/203-UI-SPEC.md (layout dan interaction contract)
    - .planning/phases/203-certificate-history-modal/203-RESEARCH.md (chain grouping algorithm)
  </read_first>
  <action>
**1. Controllers/AdminController.cs — Tambah action CertificateHistoryAsync:**

Letakkan di dekat BuildRenewalRowsAsync. Attribute: `[HttpGet]` dan `[Authorize(Roles = "Admin, HC")]`.

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> CertificateHistory(string workerId, string mode = "readonly")
{
    if (string.IsNullOrEmpty(workerId))
        return BadRequest("workerId required");

    // 1. Query semua sertifikat pekerja ini
    // TrainingRecords with certificate
    var trainingCerts = await _context.TrainingRecords
        .Where(t => t.UserId == workerId && t.SertifikatUrl != null)
        .Select(t => new {
            t.Id,
            Judul = t.Judul ?? "",
            t.Kategori,
            t.NomorSertifikat,
            TanggalTerbit = (DateTime?)t.Tanggal,
            t.ValidUntil,
            t.CertificateType,
            t.SertifikatUrl,
            t.RenewsSessionId,
            t.RenewsTrainingId
        })
        .ToListAsync();

    // AssessmentSessions with certificate
    var assessmentCerts = await _context.AssessmentSessions
        .Where(a => a.UserId == workerId && a.GenerateCertificate && a.IsPassed == true)
        .Select(a => new {
            a.Id,
            Judul = a.Title,
            a.Category,
            a.NomorSertifikat,
            TanggalTerbit = a.CompletedAt,
            a.ValidUntil,
            a.RenewsSessionId,
            a.RenewsTrainingId
        })
        .ToListAsync();

    // 2. Category resolution (sama pattern dengan BuildRenewalRowsAsync)
    var allCategories = await _context.AssessmentCategories
        .Where(c => c.IsActive)
        .Select(c => new { c.Id, c.Name, c.ParentId })
        .ToListAsync();
    var categoryById = allCategories.ToDictionary(c => c.Id);
    var categoryNameLookup = allCategories
        .Where(c => c.ParentId != null && categoryById.ContainsKey(c.ParentId.Value))
        .ToDictionary(c => c.Name, c => categoryById[c.ParentId!.Value].Name);

    // 3. Renewal chain batch lookup (4 sets) — scoped to this worker's certs
    var mySessionIds = assessmentCerts.Select(a => a.Id).ToHashSet();
    var myTrainingIds = trainingCerts.Select(t => t.Id).ToHashSet();

    // Check which of MY certs have been renewed by ANY other cert
    var renewedSessionIds = new HashSet<int>(
        await _context.AssessmentSessions
            .Where(a => a.RenewsSessionId.HasValue && mySessionIds.Contains(a.RenewsSessionId.Value) && a.IsPassed == true)
            .Select(a => a.RenewsSessionId!.Value).ToListAsync());
    renewedSessionIds.UnionWith(
        await _context.TrainingRecords
            .Where(t => t.RenewsSessionId.HasValue && mySessionIds.Contains(t.RenewsSessionId.Value))
            .Select(t => t.RenewsSessionId!.Value).ToListAsync());

    var renewedTrainingIds = new HashSet<int>(
        await _context.AssessmentSessions
            .Where(a => a.RenewsTrainingId.HasValue && myTrainingIds.Contains(a.RenewsTrainingId.Value) && a.IsPassed == true)
            .Select(a => a.RenewsTrainingId!.Value).ToListAsync());
    renewedTrainingIds.UnionWith(
        await _context.TrainingRecords
            .Where(t => t.RenewsTrainingId.HasValue && myTrainingIds.Contains(t.RenewsTrainingId.Value))
            .Select(t => t.RenewsTrainingId!.Value).ToListAsync());

    // 4. Build SertifikatRow list
    var rows = new List<SertifikatRow>();

    foreach (var t in trainingCerts)
    {
        rows.Add(new SertifikatRow
        {
            SourceId = t.Id,
            RecordType = RecordType.Training,
            WorkerId = workerId,
            Judul = t.Judul,
            Kategori = MapKategori(t.Kategori),
            SubKategori = null,
            NomorSertifikat = t.NomorSertifikat,
            TanggalTerbit = t.TanggalTerbit,
            ValidUntil = t.ValidUntil,
            Status = SertifikatRow.DeriveCertificateStatus(t.ValidUntil, t.CertificateType),
            SertifikatUrl = t.SertifikatUrl,
            IsRenewed = renewedTrainingIds.Contains(t.Id)
        });
    }

    foreach (var a in assessmentCerts)
    {
        string kategori = a.Category;
        string? subKategori = null;
        if (categoryNameLookup.TryGetValue(a.Category, out var parentName))
        {
            kategori = parentName;
            subKategori = a.Category;
        }
        rows.Add(new SertifikatRow
        {
            SourceId = a.Id,
            RecordType = RecordType.Assessment,
            WorkerId = workerId,
            Judul = a.Judul,
            Kategori = kategori,
            SubKategori = subKategori,
            NomorSertifikat = a.NomorSertifikat,
            TanggalTerbit = a.TanggalTerbit,
            ValidUntil = a.ValidUntil,
            Status = SertifikatRow.DeriveCertificateStatus(a.ValidUntil, null),
            IsRenewed = renewedSessionIds.Contains(a.Id)
        });
    }

    // 5. Build renewal chain graph using Union-Find
    // Node key: "AS:{id}" or "TR:{id}"
    var parent = new Dictionary<string, string>();
    string Find(string x) {
        if (!parent.ContainsKey(x)) parent[x] = x;
        while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }
        return x;
    }
    void Union(string a, string b) {
        var ra = Find(a); var rb = Find(b);
        if (ra != rb) parent[ra] = rb;
    }

    // Register all cert nodes
    foreach (var r in rows)
        Find(r.RecordType == RecordType.Assessment ? $"AS:{r.SourceId}" : $"TR:{r.SourceId}");

    // Build edges from renewal FKs
    foreach (var a in assessmentCerts)
    {
        var key = $"AS:{a.Id}";
        if (a.RenewsSessionId.HasValue) Union(key, $"AS:{a.RenewsSessionId.Value}");
        if (a.RenewsTrainingId.HasValue) Union(key, $"TR:{a.RenewsTrainingId.Value}");
    }
    foreach (var t in trainingCerts)
    {
        var key = $"TR:{t.Id}";
        if (t.RenewsSessionId.HasValue) Union(key, $"AS:{t.RenewsSessionId.Value}");
        if (t.RenewsTrainingId.HasValue) Union(key, $"TR:{t.RenewsTrainingId.Value}");
    }

    // Group rows by chain
    var groups = rows.GroupBy(r => Find(r.RecordType == RecordType.Assessment ? $"AS:{r.SourceId}" : $"TR:{r.SourceId}"))
        .Select(g =>
        {
            var certs = g.OrderByDescending(c => c.ValidUntil ?? DateTime.MaxValue).ToList();
            var oldest = g.OrderBy(c => c.ValidUntil ?? DateTime.MaxValue).First();
            var chainTitle = !string.IsNullOrEmpty(oldest.SubKategori) ? oldest.SubKategori
                           : !string.IsNullOrEmpty(oldest.Kategori) ? oldest.Kategori
                           : oldest.Judul;
            return new CertificateChainGroup
            {
                ChainTitle = chainTitle,
                Certificates = certs,
                LatestValidUntil = certs.First().ValidUntil
            };
        })
        .OrderByDescending(g => g.LatestValidUntil ?? DateTime.MaxValue)
        .ToList();

    ViewBag.Mode = mode;
    return PartialView("Shared/_CertificateHistoryModalContent", groups);
}
```

**Catatan penting:**
- `MapKategori` sudah ada sebagai private method di AdminController
- Union-Find didefinisikan sebagai local functions di dalam action method
- Filter renewal lookup scoped ke workerId certs saja (efisien)

**2. Views/Shared/_CertificateHistoryModalContent.cshtml:**

Buat file baru. Model: `List<CertificateChainGroup>`. Layout sesuai UI-SPEC.md:

- `@model List<HcPortal.Models.CertificateChainGroup>`
- Ambil mode dari `ViewBag.Mode`
- Jika model kosong: tampilkan empty state (bi-journal-x icon + "Belum ada sertifikat" + "Pekerja ini belum memiliki sertifikat yang tercatat.")
- Jika ada data: tabel `table table-striped table-hover`
- Thead: Judul, Kategori, Sub Kategori, Valid, Status, Sumber + (Aksi jika mode==renewal)
- Grouping header row per chain: `table-secondary`, colspan sesuai jumlah kolom, `bi-link-45deg` icon + ChainTitle + badge count
- Data rows: Judul, Kategori ?? "-", SubKategori ?? "-", ValidUntil?.ToString("dd MMM yyyy") ?? "Permanent", badge status (bg-danger Expired / bg-warning text-dark Akan Expired / bg-success Aktif / bg-info text-dark Permanent), badge sumber (bg-info text-dark Assessment / bg-secondary Training)
- Kolom Aksi (mode renewal only): tombol Renew `btn btn-sm btn-warning` dengan href ke `/Admin/CreateAssessment?renew{SessionId|TrainingId}={SourceId}` — hanya tampil jika (Status == Expired || Status == AkanExpired) && !IsRenewed
- Tombol Renew: `<a href="..." class="btn btn-sm btn-warning"><i class="bi bi-arrow-repeat me-1"></i>Renew</a>`
- aria-label pada tombol Renew: "Renew sertifikat {Judul}"
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs mengandung `public async Task<IActionResult> CertificateHistory(string workerId, string mode = "readonly")`
    - Controllers/AdminController.cs mengandung `PartialView("Shared/_CertificateHistoryModalContent", groups)`
    - Controllers/AdminController.cs mengandung `[Authorize(Roles = "Admin, HC")]` sebelum CertificateHistory
    - Views/Shared/_CertificateHistoryModalContent.cshtml ada dan mengandung `@model List<HcPortal.Models.CertificateChainGroup>`
    - Partial view mengandung `ViewBag.Mode`
    - Partial view mengandung `bi-clock-history` atau `bi-link-45deg` (grouping icon)
    - Partial view mengandung `btn-warning` (tombol Renew)
    - Partial view mengandung `Belum ada sertifikat` (empty state)
    - Partial view mengandung `renew` dalam href (link ke CreateAssessment)
    - `dotnet build` sukses tanpa error
  </acceptance_criteria>
  <done>Endpoint CertificateHistory mengembalikan partial view dengan sertifikat grouped by renewal chain, mode renewal menampilkan tombol Renew, mode readonly tanpa tombol</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses
- Endpoint `/Admin/CertificateHistory?workerId=XXX&mode=renewal` mengembalikan HTML partial (manual test)
- Endpoint `/Admin/CertificateHistory?workerId=XXX&mode=readonly` mengembalikan HTML tanpa tombol Renew
</verification>

<success_criteria>
- CertificateHistoryAsync action ada di AdminController dan mengembalikan PartialView
- _CertificateHistoryModalContent.cshtml ada di Views/Shared/
- Sertifikat di-group by renewal chain, newest first per group, groups sorted newest first
- Mode renewal menampilkan tombol Renew pada sertifikat eligible
- Mode readonly tanpa tombol aksi
- Build sukses
</success_criteria>

<output>
After completion, create `.planning/phases/203-certificate-history-modal/203-01-SUMMARY.md`
</output>
