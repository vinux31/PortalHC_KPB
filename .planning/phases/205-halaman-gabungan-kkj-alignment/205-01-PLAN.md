---
phase: 205-halaman-gabungan-kkj-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/DokumenKkj.cshtml
autonomous: true
requirements:
  - CMP-02
  - CMP-03
  - CMP-04
  - CMP-05

must_haves:
  truths:
    - "Halaman /CMP/DokumenKkj menampilkan 2 tab: Kebutuhan Kompetensi Jabatan dan Alignment KKJ & IDP"
    - "Tab KKJ menampilkan semua bagian stacked dengan file table masing-masing"
    - "Tab Alignment menampilkan semua bagian stacked dengan file table masing-masing"
    - "User L5-L6 hanya melihat bagian milik section-nya di kedua tab"
    - "User L1-L4 melihat semua bagian di kedua tab"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "DokumenKkj action"
      contains: "public async Task<IActionResult> DokumenKkj"
    - path: "Views/CMP/DokumenKkj.cshtml"
      provides: "Combined KKJ + Alignment view with 2 tabs"
      contains: "nav-tabs"
  key_links:
    - from: "Views/CMP/DokumenKkj.cshtml"
      to: "Controllers/CMPController.cs"
      via: "DokumenKkj action returns View with ViewBag data"
      pattern: "ViewBag\\.KkjFilesByBagian"
    - from: "Views/CMP/DokumenKkj.cshtml"
      to: "AdminController"
      via: "KkjFileDownload and CpdpFileDownload endpoints"
      pattern: "KkjFileDownload|CpdpFileDownload"
---

<objective>
Buat halaman gabungan KKJ & Alignment dengan 2 tab, menampilkan semua bagian stacked per tab beserta file table, dengan role-based filtering.

Purpose: Menggabungkan 2 halaman terpisah menjadi 1 halaman yang lebih mudah diakses
Output: Action DokumenKkj di CMPController + View DokumenKkj.cshtml
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-halaman-gabungan-kkj-alignment/205-CONTEXT.md

<interfaces>
From Controllers/CMPController.cs (lines 75-173):
- Kkj(string? section) — loads KkjBagians filtered by role, KkjFiles for selected bagian
- Mapping() — loads all KkjBagians filtered by role, CpdpFiles grouped by BagianId
- Role filtering pattern: userLevel >= 5 && user.Section != null → filter bagians by Section name match
- ViewBag pattern: Bagians list, FilesByBagian dictionary

From Views/CMP/Mapping.cshtml:
- Tab-per-bagian layout with Bootstrap nav-tabs + tab-content
- File table: table-sm table-hover align-middle
- Empty state: alert alert-light border with inbox icon
- File icons: bi-file-earmark-pdf text-danger / bi-file-earmark-excel text-success
- Download: Url.Action("CpdpFileDownload", "Admin", new { id = file.Id })

From Views/CMP/Kkj.cshtml:
- Download: Url.Action("KkjFileDownload", "Admin", new { id = file.Id })
- btn-sm btn-primary for download button with "Unduh" label
- Date format: dd MMM yyyy (without time)
- Header column "Unduh" (not "Aksi")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah action DokumenKkj di CMPController</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (full file — lihat Kkj() lines 75-129 dan Mapping() lines 132-173 untuk pattern)
  </read_first>
  <action>
Tambahkan action baru `DokumenKkj(string? tab)` di CMPController, tepat sebelum comment `// --- HALAMAN 3:` (sebelum line 174).

Action harus:
1. Set `ViewData["Title"] = "Dokumen KKJ & Alignment KKJ/IDP";`
2. Get current user dan roleLevel (sama seperti Kkj() lines 79-80):
   ```csharp
   var currentUser = await _userManager.GetUserAsync(User) as ApplicationUser;
   var userLevel = currentUser?.RoleLevel ?? 6;
   ```
3. Load semua KkjBagians ordered by DisplayOrder
4. Apply role-based filtering (reuse pattern dari Mapping() lines 150-164):
   - Jika userLevel >= 5 dan user.Section != null, filter bagians yang Name.ToLower() == user.Section.ToLower()
   - Jika filter menghasilkan 0 results, fallback ke semua bagians
5. Load KkjFiles (non-archived) grouped by BagianId:
   ```csharp
   var kkjFiles = await _context.KkjFiles
       .Where(f => !f.IsArchived)
       .OrderByDescending(f => f.UploadedAt)
       .ToListAsync();
   var kkjFilesByBagian = kkjFiles
       .GroupBy(f => f.BagianId)
       .ToDictionary(g => g.Key, g => g.ToList());
   ```
6. Load CpdpFiles (non-archived) grouped by BagianId:
   ```csharp
   var cpdpFiles = await _context.CpdpFiles
       .Where(f => !f.IsArchived)
       .OrderBy(f => f.UploadedAt)
       .ToListAsync();
   var cpdpFilesByBagian = cpdpFiles
       .GroupBy(f => f.BagianId)
       .ToDictionary(g => g.Key, g => g.ToList());
   ```
7. Set ViewBag:
   ```csharp
   ViewBag.Bagians = filteredBagians;
   ViewBag.KkjFilesByBagian = kkjFilesByBagian;
   ViewBag.CpdpFilesByBagian = cpdpFilesByBagian;
   ViewBag.ActiveTab = tab == "alignment" ? "alignment" : "kkj";
   ```
8. Return View();

Filter KkjFiles dan CpdpFiles hanya untuk bagians yang user boleh lihat: setelah grouping, tidak perlu filter lagi karena view hanya iterasi `ViewBag.Bagians` yang sudah di-filter.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/CMPController.cs contains `public async Task<IActionResult> DokumenKkj(string? tab)`
    - Controllers/CMPController.cs contains `ViewBag.KkjFilesByBagian = kkjFilesByBagian`
    - Controllers/CMPController.cs contains `ViewBag.CpdpFilesByBagian = cpdpFilesByBagian`
    - Controllers/CMPController.cs contains `ViewBag.ActiveTab = tab == "alignment" ? "alignment" : "kkj"`
    - Controllers/CMPController.cs contains `userLevel >= 5` role filtering in DokumenKkj method
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Action DokumenKkj terdaftar di CMPController, build sukses, ViewBag berisi Bagians + KkjFilesByBagian + CpdpFilesByBagian + ActiveTab</done>
</task>

<task type="auto">
  <name>Task 2: Buat view DokumenKkj.cshtml dengan 2 tab stacked sections</name>
  <files>Views/CMP/DokumenKkj.cshtml</files>
  <read_first>
    - Views/CMP/Mapping.cshtml (reference: tab layout, file table markup, empty state alert)
    - Views/CMP/Kkj.cshtml (reference: download button style btn-sm btn-primary, "Unduh" label, date format dd MMM yyyy tanpa jam)
    - Controllers/CMPController.cs (verify ViewBag names dari Task 1)
  </read_first>
  <action>
Buat file baru `Views/CMP/DokumenKkj.cshtml` dengan struktur:

**Header section:**
```razor
@{
    ViewData["Title"] = "Dokumen KKJ & Alignment KKJ/IDP";
    var bagians = ViewBag.Bagians as List<HcPortal.Models.KkjBagian> ?? new List<HcPortal.Models.KkjBagian>();
    var kkjFilesByBagian = ViewBag.KkjFilesByBagian as Dictionary<int, List<HcPortal.Models.KkjFile>> ?? new Dictionary<int, List<HcPortal.Models.KkjFile>>();
    var cpdpFilesByBagian = ViewBag.CpdpFilesByBagian as Dictionary<int, List<HcPortal.Models.CpdpFile>> ?? new Dictionary<int, List<HcPortal.Models.CpdpFile>>();
    var activeTab = ViewBag.ActiveTab as string ?? "kkj";
    string FormatSize(long bytes) => bytes < 1024 * 1024 ? $"{bytes / 1024}KB" : $"{bytes / (1024 * 1024.0):F1}MB";
}
@using System.Globalization
```

**Breadcrumb:** CMP > Dokumen KKJ & Alignment KKJ/IDP (active)
- Breadcrumb pattern: sama seperti Mapping.cshtml lines 12-18, tapi dengan `Beranda` link juga
- `<li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Beranda</a></li>`
- `<li class="breadcrumb-item"><a href="@Url.Action("Index", "CMP")">CMP</a></li>`
- `<li class="breadcrumb-item active">Dokumen KKJ & Alignment KKJ/IDP</li>`

**Page header:**
- `<h2 class="fw-bold mb-1"><i class="bi bi-file-earmark-richtext me-2 text-primary"></i>Dokumen KKJ & Alignment KKJ/IDP</h2>`
- `<p class="text-muted mb-0">Dokumen Kebutuhan Kompetensi Jabatan dan Alignment KKJ & IDP</p>`

**TANPA `div.container`** — langsung `<div class="py-4">` sebagai wrapper utama (per user decision).

**Card wrapper:** `<div class="card border-0 shadow-sm">`

**2 Tab utama** (Bootstrap nav-tabs di dalam card):
```html
<ul class="nav nav-tabs px-3 pt-3" id="mainTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <button class="nav-link @(activeTab == "kkj" ? "active" : "")"
                id="tab-kkj" data-bs-toggle="tab" data-bs-target="#pane-kkj"
                type="button" role="tab">
            Kebutuhan Kompetensi Jabatan
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button class="nav-link @(activeTab == "alignment" ? "active" : "")"
                id="tab-alignment" data-bs-toggle="tab" data-bs-target="#pane-alignment"
                type="button" role="tab">
            Alignment KKJ &amp; IDP
        </button>
    </li>
</ul>
```

**Tab content — Tab KKJ (`#pane-kkj`):**
Iterate `bagians.OrderBy(b => b.DisplayOrder)`. Per bagian, render:
- Section header: `<div class="px-4 py-3 border-bottom bg-light"><h6 class="mb-0 fw-bold">@bagian.Name <span class="badge bg-primary ms-2">@kkjCount file</span></h6></div>` (dimana kkjCount = jumlah file di bagian tersebut, atau 0)
- Jika tidak ada file: `<div class="alert alert-light border text-muted text-center py-4 mx-4 my-3"><i class="bi bi-inbox fs-2 d-block mb-2 text-muted"></i>Belum ada dokumen untuk bagian ini</div>`
- Jika ada file: tabel dengan class `table table-sm table-hover align-middle mb-0`
  - Kolom: Nama File, Tipe, Ukuran, Keterangan, Tanggal Upload, Unduh (header terakhir: `<th class="text-center">Unduh</th>`)
  - Row per file:
    - Nama: icon (`bi-file-earmark-pdf text-danger` atau `bi-file-earmark-excel text-success`) + `@file.FileName`
    - Tipe: `<span class="badge @(file.FileType == "pdf" ? "bg-danger" : "bg-success")">@file.FileType.ToUpper()</span>`
    - Ukuran: `<small>@FormatSize(file.FileSizeBytes)</small>`
    - Keterangan: `<small class="text-muted">@(file.Keterangan ?? "-")</small>`
    - Tanggal: `<small>@file.UploadedAt.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID"))</small>` (TANPA jam)
    - Unduh: `<a href="@Url.Action("KkjFileDownload", "Admin", new { id = file.Id })" class="btn btn-sm btn-primary" title="Unduh"><i class="bi bi-download me-1"></i>Unduh</a>`

**Tab content — Tab Alignment (`#pane-alignment`):**
Sama persis dengan Tab KKJ, kecuali:
- Data source: `cpdpFilesByBagian` (bukan `kkjFilesByBagian`)
- Model type: `CpdpFile` (bukan `KkjFile`)
- Download endpoint: `CpdpFileDownload` (bukan `KkjFileDownload`)

**Script untuk deep-link tab via query param:**
```html
<script>
    // Support ?tab=alignment deep link — tab already set server-side via activeTab
</script>
```
Tidak perlu script tambahan karena active tab sudah di-set server-side via `activeTab` variable.

**Jika bagians kosong:** tampilkan empty state di dalam card: `<div class="p-4 text-center text-muted"><i class="bi bi-inbox fs-2 d-block mb-2"></i>Belum ada bagian tersedia.</div>`
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/CMP/DokumenKkj.cshtml exists
    - File contains `nav-tabs` and `id="mainTabs"`
    - File contains `id="tab-kkj"` and `id="tab-alignment"`
    - File contains `id="pane-kkj"` and `id="pane-alignment"`
    - File contains `Kebutuhan Kompetensi Jabatan` as tab label
    - File contains `Alignment KKJ` as tab label
    - File contains `KkjFileDownload` for KKJ tab downloads
    - File contains `CpdpFileDownload` for Alignment tab downloads
    - File contains `dd MMM yyyy` date format (without HH:mm)
    - File contains `btn btn-sm btn-primary` for download button (solid, not outline)
    - File contains `Unduh` as download button label (not "Download")
    - File contains `class="text-center">Unduh</th>` as last column header
    - File contains `Belum ada dokumen untuk bagian ini` as empty state
    - File does NOT contain `div class="container`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>View DokumenKkj.cshtml menampilkan 2 tab dengan semua bagian stacked, file tables, dan role filtering via ViewBag.Bagians yang sudah di-filter controller</done>
</task>

</tasks>

<verification>
1. `dotnet build` sukses tanpa error
2. Navigasi ke /CMP/DokumenKkj — tampil 2 tab
3. Tab KKJ menampilkan semua bagian stacked dengan file masing-masing
4. Tab Alignment menampilkan semua bagian stacked dengan file masing-masing
5. /CMP/DokumenKkj?tab=alignment — langsung buka tab Alignment
6. Login sebagai L5-L6 — hanya melihat bagian sendiri di kedua tab
</verification>

<success_criteria>
- Halaman /CMP/DokumenKkj accessible dan menampilkan 2 tab
- Kedua tab menampilkan semua bagian stacked tanpa dropdown
- Download button menggunakan endpoint yang benar per tab
- Role filtering berfungsi (L5-L6 hanya lihat bagian sendiri)
- Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/205-halaman-gabungan-kkj-alignment/205-01-SUMMARY.md`
</output>
