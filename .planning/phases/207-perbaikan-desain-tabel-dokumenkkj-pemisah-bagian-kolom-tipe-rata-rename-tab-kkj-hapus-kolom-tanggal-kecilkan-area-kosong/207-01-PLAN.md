---
phase: 207-perbaikan-desain-tabel-dokumenkkj
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/DokumenKkj.cshtml
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Tiap bagian (section) terpisah secara visual dengan border-top dan margin-top"
    - "Kolom Tipe (badge PDF/Excel) sejajar vertikal dengan header"
    - "Tab pertama bertuliskan 'Kebutuhan Kompetensi Jabatan (KKJ)'"
    - "Kolom Tanggal Upload tidak ada di kedua tab"
    - "Empty state compact tanpa icon besar dan whitespace berlebih"
  artifacts:
    - path: "Views/CMP/DokumenKkj.cshtml"
      provides: "Halaman gabungan KKJ & Alignment dengan perbaikan desain"
  key_links: []
---

<objective>
Perbaiki tampilan tabel dan layout di halaman /CMP/DokumenKkj: pemisah antar bagian, alignment kolom Tipe, rename tab KKJ, hapus kolom Tanggal Upload, dan kecilkan area empty state.

Purpose: Visual polish agar tabel lebih rapi dan mudah dibaca
Output: Views/CMP/DokumenKkj.cshtml yang sudah diperbaiki
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Perbaiki desain tabel DokumenKkj — 5 perubahan visual</name>
  <files>Views/CMP/DokumenKkj.cshtml</files>
  <read_first>Views/CMP/DokumenKkj.cshtml</read_first>
  <action>
Buka Views/CMP/DokumenKkj.cshtml dan lakukan 5 perubahan berikut:

**1. Pemisah antar bagian:**
Pada setiap div header bagian (class `px-4 py-3 border-bottom bg-light`), tambahkan `mt-3 border-top` KECUALI untuk bagian pertama di tiap tab. Gunakan conditional: jika bukan iterasi pertama, tambahkan `mt-3 border-top`. Ini berlaku di KEDUA tab (KKJ loop di ~line 69 dan Alignment loop di ~line 137).

Cara implementasi: gunakan counter atau `@{ var isFirstKkj = true; }` sebelum foreach, lalu di dalam loop cek `isFirstKkj` untuk menentukan apakah tambah `mt-3 border-top`. Set `isFirstKkj = false` di akhir iterasi. Lakukan hal sama untuk tab Alignment dengan variable `isFirstAlignment`.

**2. Kolom Tipe alignment:**
Pada kedua `<th>Tipe</th>` (line ~92 dan ~160), tambahkan `class="text-center"` sehingga menjadi `<th class="text-center">Tipe</th>`.
Pada kedua `<td>` yang berisi badge Tipe (line ~108 dan ~176), tambahkan `class="text-center"` sehingga menjadi `<td class="text-center">`.

**3. Rename tab KKJ:**
Pada button tab KKJ (line ~47), ubah teks dari:
```
Kebutuhan Kompetensi Jabatan
```
menjadi:
```
Kebutuhan Kompetensi Jabatan (KKJ)
```

**4. Hapus kolom Tanggal Upload:**
Hapus `<th>Tanggal Upload</th>` dari KEDUA thead (line ~95 dan ~163).
Hapus `<td>` yang berisi `file.UploadedAt.ToString(...)` dari KEDUA tbody (line ~114 dan ~182).

**5. Kecilkan empty state:**
Pada KEDUA empty state div (line ~80 dan ~148), ubah class dari:
```
alert alert-light border text-muted text-center py-4 mx-4 my-3
```
menjadi:
```
alert alert-light border text-muted text-center py-2 mx-4 my-2
```
Dan hapus icon `<i class="bi bi-inbox fs-2 d-block mb-2 text-muted"></i>` dari kedua empty state, sisakan hanya teks "Belum ada dokumen untuk bagian ini".
  </action>
  <verify>
    <automated>grep -c "Tanggal Upload" Views/CMP/DokumenKkj.cshtml | grep "^0$" &amp;&amp; grep -c "Kebutuhan Kompetensi Jabatan (KKJ)" Views/CMP/DokumenKkj.cshtml | grep "^1$" &amp;&amp; grep -c "text-center.*Tipe\|Tipe.*text-center" Views/CMP/DokumenKkj.cshtml | grep "^2$" &amp;&amp; grep -c "mt-3 border-top" Views/CMP/DokumenKkj.cshtml | grep -v "^0$" &amp;&amp; grep -c "py-2 mx-4 my-2" Views/CMP/DokumenKkj.cshtml | grep "^2$" &amp;&amp; echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
- File Views/CMP/DokumenKkj.cshtml TIDAK mengandung string "Tanggal Upload"
- File mengandung tepat 1x string "Kebutuhan Kompetensi Jabatan (KKJ)"
- File mengandung 2x `th` dengan class text-center untuk kolom Tipe
- File mengandung `mt-3 border-top` pada div pemisah bagian (minimal 1x)
- File mengandung 2x `py-2 mx-4 my-2` (compact empty state)
- File TIDAK mengandung `bi-inbox fs-2` (icon besar dihapus dari empty state)
  </acceptance_criteria>
  <done>Semua 5 perubahan visual diterapkan: pemisah bagian, kolom Tipe rata tengah, tab KKJ renamed, kolom tanggal dihapus, empty state compact</done>
</task>

</tasks>

<verification>
- Buka /CMP/DokumenKkj di browser
- Pastikan tiap bagian terpisah jelas dengan garis
- Pastikan kolom Tipe sejajar vertikal
- Pastikan tab pertama bertuliskan "Kebutuhan Kompetensi Jabatan (KKJ)"
- Pastikan tidak ada kolom Tanggal Upload
- Pastikan empty state compact tanpa icon besar
</verification>

<success_criteria>
Semua 5 perubahan visual diterapkan di Views/CMP/DokumenKkj.cshtml dan halaman tampil rapi di browser.
</success_criteria>

<output>
After completion, create `.planning/phases/207-perbaikan-desain-tabel-dokumenkkj-pemisah-bagian-kolom-tipe-rata-rename-tab-kkj-hapus-kolom-tanggal-kecilkan-area-kosong/207-01-SUMMARY.md`
</output>
