---
phase: 208-grouped-view-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/CertificationManagementViewModel.cs
  - Controllers/AdminController.cs
  - Views/Admin/Shared/_RenewalGroupedPartial.cshtml
  - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml
  - Views/Admin/RenewalCertificate.cshtml
autonomous: true
requirements: [GRP-01, GRP-02, GRP-03, GRP-04]

must_haves:
  truths:
    - "Halaman RenewalCertificate menampilkan accordion cards per judul sertifikat, bukan flat table"
    - "Setiap group header menampilkan judul, kategori/sub-kategori, dan badge count (total, expired, akan expired)"
    - "Setiap group bisa di-collapse/expand dengan klik header, default semua collapsed"
    - "Tabel dalam group hanya menampilkan kolom: Checkbox, Nama, Kategori, Sub Kategori, Valid Until, Status, Aksi (tanpa No dan Judul Sertifikat)"
    - "Pagination per group berfungsi via AJAX tanpa reload seluruh halaman"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "RenewalGroup dan RenewalGroupViewModel classes"
      contains: "class RenewalGroup"
    - path: "Views/Admin/Shared/_RenewalGroupedPartial.cshtml"
      provides: "Accordion grouped view partial"
      contains: "data-bs-toggle"
    - path: "Views/Admin/Shared/_RenewalGroupTablePartial.cshtml"
      provides: "Single group table body partial untuk pagination per group"
      contains: "RenewalGroup"
    - path: "Controllers/AdminController.cs"
      provides: "FilterRenewalCertificateGroup endpoint"
      contains: "FilterRenewalCertificateGroup"
  key_links:
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "/Admin/FilterRenewalCertificate"
      via: "AJAX fetch di refreshTable()"
      pattern: "FilterRenewalCertificate"
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "/Admin/FilterRenewalCertificateGroup"
      via: "AJAX fetch di group pagination click"
      pattern: "FilterRenewalCertificateGroup"
    - from: "Controllers/AdminController.cs"
      to: "Shared/_RenewalGroupedPartial"
      via: "PartialView return dari FilterRenewalCertificate"
      pattern: "_RenewalGroupedPartial"
---

<objective>
Mengubah halaman RenewalCertificate dari flat table menjadi grouped accordion per judul sertifikat.

Purpose: Admin dapat langsung melihat sertifikat mana yang paling mendesak di-renew tanpa scan baris per baris.
Output: Grouped accordion view dengan header (judul, kategori, badge count), collapse/expand, pagination per group.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/208-grouped-view-structure/208-CONTEXT.md
@.planning/phases/208-grouped-view-structure/208-RESEARCH.md
@.planning/phases/208-grouped-view-structure/208-UI-SPEC.md

<interfaces>
<!-- Existing types and signatures the executor needs -->

From Models/CertificationManagementViewModel.cs:
```csharp
public enum CertificateStatus { Aktif, AkanExpired, Expired, Permanent }

public class SertifikatRow
{
    public int SourceId { get; set; }
    public string WorkerId { get; set; } = "";
    public RecordType RecordType { get; set; }
    public string NamaWorker { get; set; } = "";
    public string? Bagian { get; set; }
    public string? Unit { get; set; }
    public string Judul { get; set; } = "";
    public string? Kategori { get; set; }
    public string? SubKategori { get; set; }
    public DateTime? ValidUntil { get; set; }
    public CertificateStatus Status { get; set; }
    public bool IsRenewed { get; set; }
}

public class CertificationManagementViewModel
{
    public List<SertifikatRow> Rows { get; set; } = new();
    public int TotalCount { get; set; } = 0;
    public int AkanExpiredCount { get; set; } = 0;
    public int ExpiredCount { get; set; } = 0;
    public int CurrentPage { get; set; } = 0;
    public int TotalPages { get; set; } = 0;
    public int PageSize { get; set; } = 20;
}
```

From Helpers/PaginationHelper.cs:
```csharp
public record PaginationResult(int CurrentPage, int TotalPages, int TotalCount, int Skip, int Take);
public static PaginationResult Calculate(int totalCount, int page, int pageSize)
```

From Controllers/AdminController.cs (lines 6929-6995):
- `RenewalCertificate(int page = 1)` — calls `BuildRenewalRowsAsync()`, returns View with CertificationManagementViewModel
- `FilterRenewalCertificate(bagian, unit, status, category, subCategory, page)` — filters rows, returns PartialView("Shared/_RenewalCertificateTablePartial")
- `BuildRenewalRowsAsync()` — private method returning List<SertifikatRow>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewModel + Controller — grouping layer dan endpoint pagination per group</name>
  <files>Models/CertificationManagementViewModel.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs (current ViewModel classes)
    - Controllers/AdminController.cs lines 6900-6999 (RenewalCertificate + FilterRenewalCertificate + BuildRenewalRowsAsync)
    - Helpers/PaginationHelper.cs (Calculate signature)
    - .planning/phases/208-grouped-view-structure/208-RESEARCH.md (Pattern 1-2 dan Pattern 5)
  </read_first>
  <action>
**1. Tambah classes baru di `Models/CertificationManagementViewModel.cs`** (append setelah class CertificationManagementViewModel):

```csharp
public class RenewalGroup
{
    public string GroupKey { get; set; } = "";
    public string Judul { get; set; } = "";
    public string? Kategori { get; set; }
    public string? SubKategori { get; set; }
    public int TotalCount { get; set; }
    public int ExpiredCount { get; set; }
    public int AkanExpiredCount { get; set; }
    public DateTime? MinValidUntil { get; set; }
    public List<SertifikatRow> Rows { get; set; } = new();
    public int CurrentPage { get; set; } = 1;
    public int TotalPages { get; set; } = 1;
    public int PageSize { get; set; } = 10;
}

public class RenewalGroupViewModel
{
    public List<RenewalGroup> Groups { get; set; } = new();
    public int TotalExpiredCount { get; set; }
    public int TotalAkanExpiredCount { get; set; }
}
```

**2. Ubah `FilterRenewalCertificate` di `AdminController.cs`** agar return grouped partial:

Setelah filtering existing (baris 6966-6975) dan sorting (6977-6980), GANTI bagian pagination + return (baris 6982-6994) dengan:

```csharp
// Group by judul sertifikat
var grouped = allRows
    .GroupBy(r => r.Judul)
    .Select(g => new RenewalGroup
    {
        GroupKey = Convert.ToBase64String(System.Text.Encoding.UTF8.GetBytes(g.Key))
                         .Replace("+", "_").Replace("/", "-").Replace("=", ""),
        Judul = g.Key,
        Kategori = g.First().Kategori,
        SubKategori = g.First().SubKategori,
        TotalCount = g.Count(),
        ExpiredCount = g.Count(r => r.Status == CertificateStatus.Expired),
        AkanExpiredCount = g.Count(r => r.Status == CertificateStatus.AkanExpired),
        MinValidUntil = g.Min(r => r.ValidUntil)
    })
    .OrderBy(g => g.MinValidUntil ?? DateTime.MaxValue)
    .ToList();

foreach (var group in grouped)
{
    var groupRows = allRows
        .Where(r => r.Judul == group.Judul)
        .OrderBy(r => r.Status == CertificateStatus.Expired ? 0 : 1)
        .ThenBy(r => r.ValidUntil ?? DateTime.MaxValue)
        .ToList();
    var paging = PaginationHelper.Calculate(groupRows.Count, 1, group.PageSize);
    group.Rows = groupRows.Skip(paging.Skip).Take(paging.Take).ToList();
    group.CurrentPage = paging.CurrentPage;
    group.TotalPages = paging.TotalPages;
}

var gvm = new RenewalGroupViewModel
{
    Groups = grouped,
    TotalExpiredCount = allRows.Count(r => r.Status == CertificateStatus.Expired),
    TotalAkanExpiredCount = allRows.Count(r => r.Status == CertificateStatus.AkanExpired)
};

return PartialView("Shared/_RenewalGroupedPartial", gvm);
```

**3. Tambah endpoint baru `FilterRenewalCertificateGroup`** di AdminController.cs (sebelum `#endregion` di baris 6997):

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> FilterRenewalCertificateGroup(
    string groupKey,
    string judul,
    int page = 1,
    string? bagian = null, string? unit = null,
    string? status = null, string? category = null, string? subCategory = null)
{
    var allRows = await BuildRenewalRowsAsync();

    if (!string.IsNullOrEmpty(bagian))
        allRows = allRows.Where(r => r.Bagian == bagian).ToList();
    if (!string.IsNullOrEmpty(unit))
        allRows = allRows.Where(r => r.Unit == unit).ToList();
    if (!string.IsNullOrEmpty(status) && Enum.TryParse<CertificateStatus>(status, out var st))
        allRows = allRows.Where(r => r.Status == st).ToList();
    if (!string.IsNullOrEmpty(category))
        allRows = allRows.Where(r => r.Kategori == category).ToList();
    if (!string.IsNullOrEmpty(subCategory))
        allRows = allRows.Where(r => r.SubKategori == subCategory).ToList();

    var groupRows = allRows
        .Where(r => r.Judul == judul)
        .OrderBy(r => r.Status == CertificateStatus.Expired ? 0 : 1)
        .ThenBy(r => r.ValidUntil ?? DateTime.MaxValue)
        .ToList();

    var paging = PaginationHelper.Calculate(groupRows.Count, page, 10);
    var group = new RenewalGroup
    {
        GroupKey = groupKey,
        Judul = judul,
        Rows = groupRows.Skip(paging.Skip).Take(paging.Take).ToList(),
        CurrentPage = paging.CurrentPage,
        TotalPages = paging.TotalPages,
        TotalCount = groupRows.Count,
        ExpiredCount = groupRows.Count(r => r.Status == CertificateStatus.Expired),
        AkanExpiredCount = groupRows.Count(r => r.Status == CertificateStatus.AkanExpired)
    };

    return PartialView("Shared/_RenewalGroupTablePartial", group);
}
```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Models/CertificationManagementViewModel.cs contains `public class RenewalGroup`
    - Models/CertificationManagementViewModel.cs contains `public class RenewalGroupViewModel`
    - Models/CertificationManagementViewModel.cs contains `public int PageSize { get; set; } = 10;`
    - Controllers/AdminController.cs contains `FilterRenewalCertificateGroup`
    - Controllers/AdminController.cs contains `_RenewalGroupedPartial`
    - Controllers/AdminController.cs contains `_RenewalGroupTablePartial`
    - Controllers/AdminController.cs contains `.GroupBy(r => r.Judul)`
    - Controllers/AdminController.cs contains `.OrderBy(g => g.MinValidUntil`
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>Build succeeds. RenewalGroup + RenewalGroupViewModel classes exist. FilterRenewalCertificate returns grouped partial. FilterRenewalCertificateGroup endpoint exists for per-group pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Partial views + JS — accordion grouped view dan pagination per group</name>
  <files>Views/Admin/Shared/_RenewalGroupedPartial.cshtml, Views/Admin/Shared/_RenewalGroupTablePartial.cshtml, Views/Admin/RenewalCertificate.cshtml</files>
  <read_first>
    - Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml (existing flat table — pattern reference)
    - Views/Admin/RenewalCertificate.cshtml (existing JS: refreshTable, wirePagination, wireCheckboxes, updateSummaryCards)
    - .planning/phases/208-grouped-view-structure/208-UI-SPEC.md (component inventory, interaction contract, copywriting)
    - .planning/phases/208-grouped-view-structure/208-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 2-4)
    - Models/CertificationManagementViewModel.cs (setelah Task 1 — RenewalGroup, RenewalGroupViewModel)
  </read_first>
  <action>
**1. Buat `Views/Admin/Shared/_RenewalGroupedPartial.cshtml`** (partial baru untuk seluruh grouped view):

```html
@model HcPortal.Models.RenewalGroupViewModel

<span id="partial-expired-count" class="d-none">@Model.TotalExpiredCount</span>
<span id="partial-akan-expired-count" class="d-none">@Model.TotalAkanExpiredCount</span>

@if (Model.Groups.Count == 0)
{
    <div class="text-center py-5">
        <i class="bi bi-patch-check-fill fs-1 text-success d-block mb-3"></i>
        <strong>Tidak ada sertifikat yang perlu di-renew</strong>
        <p class="text-muted mt-1 mb-3">Semua sertifikat aktif atau sudah di-renew.</p>
        <a href="/Admin/Index" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left me-1"></i>Kembali ke Kelola Data
        </a>
    </div>
}
else
{
    @foreach (var group in Model.Groups)
    {
        <div class="card border-0 shadow-sm mb-3">
            <div class="card-header d-flex align-items-center justify-content-between"
                 role="button"
                 data-bs-toggle="collapse"
                 data-bs-target="#group-@group.GroupKey-body"
                 aria-expanded="false"
                 aria-controls="group-@group.GroupKey-body"
                 aria-label="Tampilkan daftar pekerja untuk @group.Judul"
                 style="cursor:pointer; min-height:48px">
                <div class="d-flex align-items-center gap-2">
                    <i class="bi bi-chevron-right group-chevron" id="chevron-@group.GroupKey"></i>
                    <span class="fw-semibold">@group.Judul</span>
                    <div class="d-flex gap-1" aria-label="@group.TotalCount orang, @group.ExpiredCount expired, @group.AkanExpiredCount akan expired">
                        <span class="badge bg-secondary">@group.TotalCount orang</span>
                        @if (group.ExpiredCount > 0)
                        {
                            <span class="badge bg-danger">@group.ExpiredCount Expired</span>
                        }
                        @if (group.AkanExpiredCount > 0)
                        {
                            <span class="badge bg-warning text-dark">@group.AkanExpiredCount Akan Expired</span>
                        }
                    </div>
                </div>
                <small class="text-muted">
                    @(string.IsNullOrEmpty(group.SubKategori) ? group.Kategori : $"{group.Kategori} / {group.SubKategori}")
                </small>
            </div>
            <div class="collapse" id="group-@group.GroupKey-body">
                <div class="card-body p-0" id="group-@group.GroupKey-table">
                    @await Html.PartialAsync("Shared/_RenewalGroupTablePartial", group)
                </div>
            </div>
        </div>
    }
}
```

**2. Buat `Views/Admin/Shared/_RenewalGroupTablePartial.cshtml`** (partial untuk isi satu group — digunakan juga oleh FilterRenewalCertificateGroup):

```html
@model HcPortal.Models.RenewalGroup

<div class="table-responsive">
    <table class="table table-striped table-hover mb-0">
        <caption class="visually-hidden">Daftar pekerja untuk @Model.Judul</caption>
        <thead class="table-light">
            <tr>
                <th class="ps-3" style="width:40px">
                    <input type="checkbox" class="form-check-input cb-group-select-all"
                           data-group-key="@Model.GroupKey"
                           aria-label="Pilih semua di group @Model.Judul" />
                </th>
                <th>Nama</th>
                <th class="d-none d-md-table-cell">Kategori</th>
                <th class="d-none d-md-table-cell">Sub Kategori</th>
                <th style="width:110px">Valid Until</th>
                <th style="width:120px">Status</th>
                <th style="width:90px">Aksi</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var row in Model.Rows)
            {
                <tr>
                    <td class="ps-3">
                        <input type="checkbox"
                               class="form-check-input cb-select"
                               data-kategori="@(row.Kategori ?? "")"
                               data-sourceid="@row.SourceId"
                               data-recordtype="@row.RecordType"
                               data-group-key="@Model.GroupKey"
                               aria-label="Pilih @row.NamaWorker - @row.Judul" />
                    </td>
                    <td>@row.NamaWorker</td>
                    <td class="d-none d-md-table-cell">@(row.Kategori ?? "-")</td>
                    <td class="d-none d-md-table-cell">@(row.SubKategori ?? "-")</td>
                    <td>@(row.ValidUntil?.ToString("dd MMM yyyy") ?? "-")</td>
                    <td>
                        @if (row.Status == CertificateStatus.Expired)
                        {
                            <span class="badge bg-danger">Expired</span>
                        }
                        else
                        {
                            <span class="badge bg-warning text-dark">Akan Expired</span>
                        }
                    </td>
                    <td>
                        <button type="button"
                                class="btn btn-sm btn-outline-secondary btn-history me-1"
                                data-worker-id="@row.WorkerId"
                                data-worker-name="@row.NamaWorker"
                                data-mode="renewal"
                                title="Lihat riwayat sertifikat @row.NamaWorker"
                                aria-label="Lihat riwayat sertifikat @row.NamaWorker">
                            <i class="bi bi-clock-history"></i>
                        </button>
                        @if (row.RecordType == RecordType.Assessment)
                        {
                            <a href="/Admin/CreateAssessment?renewSessionId=@row.SourceId"
                               class="btn btn-sm btn-warning"
                               aria-label="Renew sertifikat @row.Judul untuk @row.NamaWorker">
                                <i class="bi bi-arrow-repeat me-1"></i>Renew
                            </a>
                        }
                        else
                        {
                            <a href="/Admin/CreateAssessment?renewTrainingId=@row.SourceId"
                               class="btn btn-sm btn-warning"
                               aria-label="Renew sertifikat @row.Judul untuk @row.NamaWorker">
                                <i class="bi bi-arrow-repeat me-1"></i>Renew
                            </a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (Model.TotalPages > 1)
{
    <nav class="mt-3 px-3" aria-label="Pagination group @Model.Judul">
        <ul class="pagination pagination-sm justify-content-center">
            <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                <a href="#" class="page-link" data-page="@(Model.CurrentPage - 1)"
                   data-group-key="@Model.GroupKey" data-group-judul="@Model.Judul">
                    <i class="bi bi-chevron-left"></i>
                </a>
            </li>
            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                    <a href="#" class="page-link" data-page="@i"
                       data-group-key="@Model.GroupKey" data-group-judul="@Model.Judul">@i</a>
                </li>
            }
            <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                <a href="#" class="page-link" data-page="@(Model.CurrentPage + 1)"
                   data-group-key="@Model.GroupKey" data-group-judul="@Model.Judul">
                    <i class="bi bi-chevron-right"></i>
                </a>
            </li>
        </ul>
    </nav>
    <p class="text-center text-muted small mt-1 pb-3">
        Menampilkan @((Model.CurrentPage - 1) * Model.PageSize + 1) - @(Math.Min(Model.CurrentPage * Model.PageSize, Model.TotalCount)) dari @Model.TotalCount pekerja
    </p>
}
```

**3. Ubah `Views/Admin/RenewalCertificate.cshtml`:**

3a. Ganti baris 109-111 (container + partial) dari:
```html
<div id="cert-table-container" class="card border-0 shadow-sm">
    @await Html.PartialAsync("Shared/_RenewalCertificateTablePartial", Model)
</div>
```
menjadi:
```html
<div id="cert-table-container">
</div>
```
(Container tanpa card class — setiap group sudah punya card sendiri. Isi di-load via AJAX `refreshTable(1)` on DOMContentLoaded.)

3b. Ubah `wirePagination()` function (baris 359-367) untuk handle per-group pagination:

```javascript
function wirePagination() {
    container.querySelectorAll('a[data-page]').forEach(function (link) {
        link.addEventListener('click', function (e) {
            e.preventDefault();
            var p = parseInt(this.dataset.page);
            if (p < 1) return;
            var groupKey = this.dataset.groupKey;
            var groupJudul = this.dataset.groupJudul;
            if (groupKey && groupJudul) {
                // Per-group pagination
                refreshGroupTable(groupKey, groupJudul, p);
            } else {
                // Global pagination (fallback, not used in grouped view)
                resetKategoriLock();
                refreshTable(p);
            }
        });
    });
}
```

3c. Tambah function `refreshGroupTable` setelah `refreshTable`:

```javascript
function refreshGroupTable(groupKey, judul, page) {
    var tableContainer = document.getElementById('group-' + groupKey + '-table');
    if (!tableContainer) return;
    tableContainer.innerHTML = '<div class="text-center py-3"><div class="spinner-border spinner-border-sm text-primary" role="status"><span class="visually-hidden">Memuat...</span></div></div>';

    var params = new URLSearchParams();
    params.set('groupKey', groupKey);
    params.set('judul', judul);
    params.set('page', page.toString());
    if (bagianEl.value) params.set('bagian', bagianEl.value);
    if (unitEl.value) params.set('unit', unitEl.value);
    if (categoryEl.value) params.set('category', categoryEl.value);
    if (subCategoryEl.value) params.set('subCategory', subCategoryEl.value);
    if (statusEl.value) params.set('status', statusEl.value);

    fetch('/Admin/FilterRenewalCertificateGroup?' + params)
        .then(function (resp) { return resp.text(); })
        .then(function (html) {
            tableContainer.innerHTML = html;
            wireCheckboxes();
            wirePagination();
        })
        .catch(function (e) {
            tableContainer.innerHTML = '<div class="alert alert-danger m-3"><i class="bi bi-exclamation-triangle-fill me-2"></i>Gagal memuat data. Silakan muat ulang halaman.</div>';
        });
}
```

3d. Tambah function `wireGroupChevrons` dan panggil di callback `refreshTable`:

```javascript
function wireGroupChevrons() {
    container.querySelectorAll('.collapse').forEach(function (collapseEl) {
        collapseEl.addEventListener('show.bs.collapse', function () {
            var key = collapseEl.id.replace('group-', '').replace('-body', '');
            var chevron = document.getElementById('chevron-' + key);
            if (chevron) chevron.classList.replace('bi-chevron-right', 'bi-chevron-down');
        });
        collapseEl.addEventListener('hide.bs.collapse', function () {
            var key = collapseEl.id.replace('group-', '').replace('-body', '');
            var chevron = document.getElementById('chevron-' + key);
            if (chevron) chevron.classList.replace('bi-chevron-down', 'bi-chevron-right');
        });
    });
}
```

3e. Update callback `.then` di `refreshTable` (baris 241-246) — tambah `wireGroupChevrons()`:
```javascript
.then(function (html) {
    container.innerHTML = html;
    wireCheckboxes();
    wirePagination();
    wireGroupChevrons();
    updateSummaryCards();
    container.classList.remove('dashboard-loading');
})
```

3f. Di bagian "Wire on initial load" (baris 382-384), GANTI dari:
```javascript
wireCheckboxes();
wirePagination();
```
menjadi:
```javascript
refreshTable(1);
```
Ini memastikan grouped view di-load via AJAX saat halaman pertama dibuka (container kosong, diisi oleh refreshTable).

3g. Hapus baris `var headerCb = container.querySelector('#cb-select-all');` dan blok event listener `#cb-select-all` di dalam `wireCheckboxes()` (baris 288-325) — karena select-all global diganti ke per-group di Phase 209. Untuk saat ini, `cb-group-select-all` checkbox ada di HTML tapi belum di-wire (Phase 209 scope).
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/Admin/Shared/_RenewalGroupedPartial.cshtml exists and contains `@model HcPortal.Models.RenewalGroupViewModel`
    - Views/Admin/Shared/_RenewalGroupedPartial.cshtml contains `data-bs-toggle="collapse"`
    - Views/Admin/Shared/_RenewalGroupedPartial.cshtml contains `bi-chevron-right group-chevron`
    - Views/Admin/Shared/_RenewalGroupedPartial.cshtml contains `badge bg-secondary`
    - Views/Admin/Shared/_RenewalGroupedPartial.cshtml contains `badge bg-danger`
    - Views/Admin/Shared/_RenewalGroupedPartial.cshtml contains `badge bg-warning text-dark`
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml exists and contains `@model HcPortal.Models.RenewalGroup`
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml does NOT contain `<th>No</th>` or `<th style="width:48px">No</th>`
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml does NOT contain `<th>Judul Sertifikat</th>`
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml contains `data-group-key` on pagination links
    - Views/Admin/RenewalCertificate.cshtml contains `refreshGroupTable`
    - Views/Admin/RenewalCertificate.cshtml contains `wireGroupChevrons`
    - Views/Admin/RenewalCertificate.cshtml contains `FilterRenewalCertificateGroup`
    - Views/Admin/RenewalCertificate.cshtml does NOT contain `_RenewalCertificateTablePartial`
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>Grouped accordion view berfungsi: setiap judul sertifikat tampil sebagai card terpisah dengan header (judul, kategori, badge count), collapse/expand via klik header, tabel per group tanpa kolom No dan Judul Sertifikat, pagination per group via AJAX.</done>
</task>

</tasks>

<verification>
1. `dotnet build` berhasil tanpa error
2. Buka browser /Admin/RenewalCertificate — halaman menampilkan accordion cards (semua collapsed)
3. Klik header card — expand menampilkan tabel pekerja, chevron berubah dari right ke down
4. Tabel per group memiliki 7 kolom: Checkbox, Nama, Kategori, Sub Kategori, Valid Until, Status, Aksi
5. Group diurutkan berdasarkan sertifikat paling mendesak (MinValidUntil terkecil di atas)
6. Badge count tampil: total orang (abu-abu), expired (merah, jika > 0), akan expired (kuning, jika > 0)
7. Jika group punya > 10 anggota, pagination per group muncul dan berfungsi
8. Summary cards (Expired count, Akan Expired count) tetap update sesuai data
9. Filter existing (Bagian, Unit, Kategori, Sub Kategori, Status) tetap berfungsi
10. Tombol Renew per baris dan History modal tetap berfungsi
</verification>

<success_criteria>
- Halaman RenewalCertificate menampilkan grouped accordion view, bukan flat table
- Setiap group header menampilkan judul sertifikat, kategori/sub-kategori, dan badge count
- Collapse/expand berfungsi, default semua collapsed
- Tabel per group hanya 7 kolom (tanpa No dan Judul Sertifikat)
- Pagination per group berfungsi via AJAX
- Build berhasil tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/208-grouped-view-structure/208-01-SUMMARY.md`
</output>
