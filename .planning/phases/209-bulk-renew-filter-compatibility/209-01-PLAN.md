---
phase: 209-bulk-renew-filter-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/Shared/_RenewalGroupedPartial.cshtml
  - Views/Admin/RenewalCertificate.cshtml
  - Models/CertificationManagementViewModel.cs
  - Controllers/AdminController.cs
autonomous: false
requirements: [BULK-01, BULK-02, FILT-01, FILT-02]

must_haves:
  truths:
    - "Admin dapat mencentang select-all per group dan semua checkbox di group tersebut tercentang"
    - "Centang checkbox di group A membuat checkbox di group lain disabled"
    - "Tombol Renew N Pekerja muncul di header accordion group saat ada checkbox tercentang"
    - "Tombol Renew N Pekerja hilang saat tidak ada checkbox tercentang di group"
    - "Modal konfirmasi muncul sebelum redirect ke CreateAssessment"
    - "Filter aktif yang menghasilkan 0 group menampilkan empty state khusus filter dengan tombol Reset Filter"
    - "Summary cards Expired dan Akan Expired update sesuai filter aktif"
  artifacts:
    - path: "Views/Admin/Shared/_RenewalGroupedPartial.cshtml"
      provides: "Tombol renew per group di header accordion + empty state filter"
      contains: "btn-renew-group"
    - path: "Views/Admin/RenewalCertificate.cshtml"
      provides: "JS wiring checkbox lock per group, select-all, modal konfirmasi, updateGroupRenewButton"
      contains: "selectedGroupKey"
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "IsFiltered property pada RenewalGroupViewModel"
      contains: "IsFiltered"
    - path: "Controllers/AdminController.cs"
      provides: "Set IsFiltered pada RenewalGroupViewModel"
      contains: "IsFiltered"
  key_links:
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "Views/Admin/Shared/_RenewalGroupedPartial.cshtml"
      via: "JS querySelector .btn-renew-group dan .cb-group-select-all"
      pattern: "btn-renew-group|cb-group-select-all"
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "/Admin/CreateAssessment"
      via: "window.location.href redirect setelah modal konfirmasi"
      pattern: "CreateAssessment"
---

<objective>
Implementasi bulk renew per group sertifikat dan kompatibilitas filter pada tampilan grouped RenewalCertificate.

Purpose: Admin dapat memilih pekerja per group sertifikat dan melakukan bulk renew dengan konfirmasi, sementara semua filter existing tetap berfungsi pada tampilan grouped.
Output: Checkbox select-all per group, tombol renew per group, modal konfirmasi, empty state filter, summary cards yang update sesuai filter.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-bulk-renew-filter-compatibility/209-CONTEXT.md
@.planning/phases/209-bulk-renew-filter-compatibility/209-RESEARCH.md
@.planning/phases/209-bulk-renew-filter-compatibility/209-UI-SPEC.md
@.planning/phases/208-grouped-view-structure/208-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side — Tombol renew per group + IsFiltered + empty state filter</name>
  <files>Views/Admin/Shared/_RenewalGroupedPartial.cshtml, Models/CertificationManagementViewModel.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Views/Admin/Shared/_RenewalGroupedPartial.cshtml (current accordion partial — lihat structure card-header untuk posisi tombol)
    - Models/CertificationManagementViewModel.cs (RenewalGroupViewModel class — tambah IsFiltered property)
    - Controllers/AdminController.cs (FilterRenewalCertificate action — set IsFiltered)
    - .planning/phases/209-bulk-renew-filter-compatibility/209-RESEARCH.md (Pattern 3, Pattern 7)
  </read_first>
  <action>
**1. Tambah property `IsFiltered` ke `RenewalGroupViewModel`** di `Models/CertificationManagementViewModel.cs`:

```csharp
public bool IsFiltered { get; set; }
```

Tambahkan di class `RenewalGroupViewModel` setelah property `TotalAkanExpiredCount`.

**2. Set `IsFiltered` di controller** di `Controllers/AdminController.cs`, dalam action `FilterRenewalCertificate`:

Setelah membuat instance `RenewalGroupViewModel gvm`, tambahkan:
```csharp
gvm.IsFiltered = !string.IsNullOrEmpty(bagian) || !string.IsNullOrEmpty(unit)
    || !string.IsNullOrEmpty(kategori) || !string.IsNullOrEmpty(subKategori)
    || !string.IsNullOrEmpty(status);
```

**3. Tambah tombol "Renew N Pekerja" di header accordion** di `_RenewalGroupedPartial.cshtml`:

Di dalam `div.card-header`, setelah div yang berisi badge count (bg-danger, bg-warning), tambahkan tombol SEBELUM chevron icon:

```html
<button class="btn btn-warning btn-sm btn-renew-group d-none ms-auto me-2"
        data-group-key="@group.GroupKey"
        data-group-judul="@group.Judul"
        onclick="event.stopPropagation(); renewGroup('@group.GroupKey', '@group.Judul')">
    <i class="bi bi-arrow-repeat me-1"></i>Renew <span class="renew-count-@group.GroupKey">0</span> Pekerja
</button>
```

PENTING: `event.stopPropagation()` WAJIB ada di onclick agar klik tombol tidak trigger toggle accordion.

**4. Ubah empty state** di `_RenewalGroupedPartial.cshtml`:

Ganti blok empty state existing (`@if (Model.Groups.Count == 0)`) menjadi dua varian:

```html
@if (Model.Groups.Count == 0)
{
    @if (Model.IsFiltered)
    {
        <div class="text-center py-5">
            <i class="bi bi-funnel fs-1 text-muted d-block mb-3"></i>
            <strong>Tidak ada sertifikat yang sesuai filter</strong>
            <p class="text-muted mt-1 mb-3">Coba ubah atau hapus filter untuk melihat semua sertifikat.</p>
            <button class="btn btn-outline-secondary" onclick="document.getElementById('btn-reset').click()">
                <i class="bi bi-x-circle me-1"></i>Reset Filter
            </button>
        </div>
    }
    else
    {
        <!-- existing empty state content tetap sama -->
    }
}
```

**5. Hapus tombol global "Renew Selected"** yang ada di atas filter bar di `Views/Admin/RenewalCertificate.cshtml` (elemen `#btn-renew-selected` jika masih ada). Jika sudah tidak ada, skip langkah ini.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -n "IsFiltered" Models/CertificationManagementViewModel.cs && grep -n "IsFiltered" Controllers/AdminController.cs && grep -n "btn-renew-group" Views/Admin/Shared/_RenewalGroupedPartial.cshtml && grep -n "bi-funnel" Views/Admin/Shared/_RenewalGroupedPartial.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Models/CertificationManagementViewModel.cs mengandung `public bool IsFiltered { get; set; }` di class RenewalGroupViewModel
    - Controllers/AdminController.cs mengandung `gvm.IsFiltered =` di action FilterRenewalCertificate
    - _RenewalGroupedPartial.cshtml mengandung `btn-renew-group` dengan `data-group-key` dan `event.stopPropagation()`
    - _RenewalGroupedPartial.cshtml mengandung `bi bi-funnel` di empty state filter
    - _RenewalGroupedPartial.cshtml mengandung `Model.IsFiltered` untuk membedakan dua jenis empty state
    - _RenewalGroupedPartial.cshtml mengandung `renewGroup(` di onclick tombol
  </acceptance_criteria>
  <done>Tombol renew per group di-render server-side (hidden by default), empty state filter berbeda dari empty state no-data, IsFiltered property ada dan di-set di controller.</done>
</task>

<task type="auto">
  <name>Task 2: JS wiring — Checkbox lock per group, select-all, modal konfirmasi, cleanup</name>
  <files>Views/Admin/RenewalCertificate.cshtml</files>
  <read_first>
    - Views/Admin/RenewalCertificate.cshtml (seluruh blok script — wireCheckboxes, refreshTable, refreshGroupTable, updateRenewSelectedButton, renewSelected, resetKategoriLock)
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml (cb-group-select-all element, data-group-key pada .cb-select, data-recordtype, data-sourceid)
    - .planning/phases/209-bulk-renew-filter-compatibility/209-RESEARCH.md (Pattern 1-6, Pitfall 1-5)
    - .planning/phases/209-bulk-renew-filter-compatibility/209-UI-SPEC.md (IC-01 sampai IC-05)
  </read_first>
  <action>
Semua perubahan di `Views/Admin/RenewalCertificate.cshtml` dalam blok `<script>`:

**1. Ganti variabel state** — hapus `var selectedKategori = null;` dan ganti dengan:
```javascript
var selectedGroupKey = null;
var pendingRenewParams = null;
```

**2. Refactor `wireCheckboxes()`** — hapus seluruh isi fungsi lama (yang menggunakan `data-kategori` lock). Ganti dengan implementasi lock-per-group:

```javascript
function wireCheckboxes() {
    var checkboxes = container.querySelectorAll('.cb-select');
    checkboxes.forEach(function (cb) {
        cb.addEventListener('change', function () {
            var groupKey = cb.dataset.groupKey || '';
            if (cb.checked) {
                if (selectedGroupKey === null) { selectedGroupKey = groupKey; }
                checkboxes.forEach(function (other) {
                    if (!other.checked && (other.dataset.groupKey || '') !== selectedGroupKey) {
                        other.disabled = true;
                    }
                });
            } else {
                var anyChecked = container.querySelectorAll('.cb-select:checked').length > 0;
                if (!anyChecked) {
                    selectedGroupKey = null;
                    checkboxes.forEach(function (other) {
                        other.disabled = false;
                    });
                }
            }
            updateGroupRenewButton(groupKey);
        });
    });
    wireGroupSelectAll();
}
```

**3. Tambah fungsi `wireGroupSelectAll()`:**

```javascript
function wireGroupSelectAll() {
    container.querySelectorAll('.cb-group-select-all').forEach(function (selectAll) {
        selectAll.addEventListener('change', function () {
            var groupKey = selectAll.dataset.groupKey;
            var groupCheckboxes = container.querySelectorAll('.cb-select[data-group-key="' + groupKey + '"]');
            if (selectedGroupKey !== null && selectedGroupKey !== groupKey) {
                selectAll.checked = false;
                return;
            }
            groupCheckboxes.forEach(function (cb) {
                if (!cb.disabled) cb.checked = selectAll.checked;
            });
            if (selectAll.checked && groupCheckboxes.length > 0) {
                selectedGroupKey = groupKey;
                container.querySelectorAll('.cb-select').forEach(function (other) {
                    if ((other.dataset.groupKey || '') !== groupKey && !other.checked) {
                        other.disabled = true;
                    }
                });
            } else if (!selectAll.checked) {
                var anyChecked = container.querySelectorAll('.cb-select:checked').length > 0;
                if (!anyChecked) {
                    selectedGroupKey = null;
                    container.querySelectorAll('.cb-select').forEach(function (other) {
                        other.disabled = false;
                    });
                }
            }
            updateGroupRenewButton(groupKey);
        });
    });
}
```

**4. Tambah fungsi `updateGroupRenewButton(groupKey)`:**

```javascript
function updateGroupRenewButton(groupKey) {
    var checked = container.querySelectorAll('.cb-select[data-group-key="' + groupKey + '"]:checked').length;
    var btn = container.querySelector('.btn-renew-group[data-group-key="' + groupKey + '"]');
    var countSpan = container.querySelector('.renew-count-' + groupKey);
    if (!btn) return;
    if (checked > 0) {
        btn.classList.remove('d-none');
        if (countSpan) countSpan.textContent = checked;
    } else {
        btn.classList.add('d-none');
        if (countSpan) countSpan.textContent = '0';
    }
}
```

**5. Tambah fungsi `renewGroup(groupKey, judul)`:**

```javascript
function renewGroup(groupKey, judul) {
    var checked = container.querySelectorAll('.cb-select[data-group-key="' + groupKey + '"]:checked');
    if (checked.length === 0) return;
    var params = new URLSearchParams();
    checked.forEach(function (cb) {
        if (cb.dataset.recordtype === 'Assessment') {
            params.append('renewSessionId', cb.dataset.sourceid);
        } else {
            params.append('renewTrainingId', cb.dataset.sourceid);
        }
    });
    pendingRenewParams = params.toString();
    document.getElementById('bulk-renew-count').textContent = checked.length;
    document.getElementById('bulk-renew-judul').textContent = judul;
    var modal = new bootstrap.Modal(document.getElementById('bulkRenewConfirmModal'));
    modal.show();
}
window.renewGroup = renewGroup;
```

**6. Wire tombol "Lanjutkan" di modal:**

```javascript
document.getElementById('btn-bulk-renew-confirm').addEventListener('click', function () {
    if (pendingRenewParams) {
        window.location.href = '/Admin/CreateAssessment?' + pendingRenewParams;
    }
});
```

**7. Tambah modal HTML** sebelum closing `</section>` atau di akhir halaman, SEBELUM blok `<script>`:

```html
<div class="modal fade" id="bulkRenewConfirmModal" tabindex="-1" aria-labelledby="bulkRenewConfirmLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="bulkRenewConfirmLabel">
                    <i class="bi bi-arrow-repeat me-2 text-warning"></i>Konfirmasi Bulk Renew
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Tutup"></button>
            </div>
            <div class="modal-body">
                Anda akan me-renew <strong id="bulk-renew-count">0</strong> pekerja untuk sertifikat
                <strong id="bulk-renew-judul"></strong>. Lanjutkan?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
                <button type="button" class="btn btn-warning" id="btn-bulk-renew-confirm">
                    <i class="bi bi-arrow-repeat me-1"></i>Lanjutkan
                </button>
            </div>
        </div>
    </div>
</div>
```

**8. Update `refreshGroupTable()`** — setelah `tableContainer.innerHTML = html;`, tambahkan reset state:

```javascript
if (selectedGroupKey === groupKey) {
    selectedGroupKey = null;
    container.querySelectorAll('.cb-select').forEach(function (cb) {
        cb.disabled = false;
    });
}
updateGroupRenewButton(groupKey);
var selectAllCb = container.querySelector('.cb-group-select-all[data-group-key="' + groupKey + '"]');
if (selectAllCb) selectAllCb.checked = false;
```

**9. Update `refreshTable()`** — setelah `container.innerHTML = html;`, pastikan ada reset:

```javascript
selectedGroupKey = null;
```

Jika ada `selectedKategori = null;` ganti menjadi `selectedGroupKey = null;`.

**10. Hapus fungsi lama yang tidak lagi dipakai:**
- `updateRenewSelectedButton()` (diganti oleh `updateGroupRenewButton`)
- `renewSelected()` (diganti oleh `renewGroup`)
- `resetKategoriLock()` (logic sudah inline di wireCheckboxes)
- Referensi ke `selectedKategori`
- Referensi ke `#btn-renew-selected` (tombol global sudah dihapus)

**11. Hapus tombol global `#btn-renew-selected`** dari HTML (jika masih ada di halaman).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -c "selectedGroupKey" Views/Admin/RenewalCertificate.cshtml && grep -c "wireGroupSelectAll" Views/Admin/RenewalCertificate.cshtml && grep -c "updateGroupRenewButton" Views/Admin/RenewalCertificate.cshtml && grep -c "renewGroup" Views/Admin/RenewalCertificate.cshtml && grep -c "bulkRenewConfirmModal" Views/Admin/RenewalCertificate.cshtml && grep -c "selectedKategori" Views/Admin/RenewalCertificate.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - RenewalCertificate.cshtml mengandung `var selectedGroupKey = null` (bukan selectedKategori)
    - RenewalCertificate.cshtml mengandung `function wireGroupSelectAll()`
    - RenewalCertificate.cshtml mengandung `function updateGroupRenewButton(groupKey)`
    - RenewalCertificate.cshtml mengandung `function renewGroup(groupKey, judul)`
    - RenewalCertificate.cshtml mengandung `bulkRenewConfirmModal` (modal HTML)
    - RenewalCertificate.cshtml mengandung `btn-bulk-renew-confirm` (tombol Lanjutkan di modal)
    - RenewalCertificate.cshtml mengandung `pendingRenewParams`
    - RenewalCertificate.cshtml TIDAK mengandung `selectedKategori` (grep count = 0)
    - RenewalCertificate.cshtml TIDAK mengandung `updateRenewSelectedButton` (grep count = 0)
    - RenewalCertificate.cshtml TIDAK mengandung `btn-renew-selected` (grep count = 0)
  </acceptance_criteria>
  <done>Checkbox lock per group berfungsi, select-all per group berfungsi, tombol renew per group muncul/hilang sesuai centang, modal konfirmasi tampil sebelum redirect, fungsi lama (kategori lock, global renew button) sudah dihapus.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi manual bulk renew dan filter</name>
  <what-built>Bulk renew per group sertifikat dengan checkbox lock, select-all, modal konfirmasi, dan filter compatibility (empty state filter + summary cards update)</what-built>
  <how-to-verify>
    1. Buka /Admin/RenewalCertificate
    2. Expand satu group sertifikat
    3. Centang satu checkbox pekerja — pastikan:
       a. Checkbox di group lain menjadi disabled
       b. Tombol "Renew 1 Pekerja" muncul di header accordion group tersebut
    4. Centang checkbox "select all" di header tabel group — pastikan semua row tercentang dan count di tombol update
    5. Klik tombol "Renew N Pekerja" — pastikan:
       a. Accordion TIDAK toggle (stopPropagation bekerja)
       b. Modal konfirmasi muncul dengan judul sertifikat dan jumlah pekerja yang benar
    6. Klik "Batal" — modal tertutup, checkbox state tidak berubah
    7. Klik tombol Renew lagi, klik "Lanjutkan" — redirect ke CreateAssessment dengan parameter yang benar
    8. Kembali ke RenewalCertificate, pilih filter Bagian yang hanya cocok sebagian group — pastikan group tanpa anggota tidak muncul
    9. Set filter yang tidak cocok sama sekali — pastikan muncul empty state "Tidak ada sertifikat yang sesuai filter" dengan tombol "Reset Filter"
    10. Klik "Reset Filter" — halaman kembali normal
    11. Verifikasi summary cards (Expired, Akan Expired) update sesuai filter aktif
    12. Test pagination per group — centang checkbox, ganti halaman, pastikan checkbox di-reset
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan masalah yang ditemukan</resume-signal>
  <action>User melakukan verifikasi manual di browser sesuai langkah how-to-verify di atas.</action>
  <verify>User confirms all 12 verification steps pass</verify>
  <done>User meng-approve atau melaporkan masalah</done>
</task>

</tasks>

<verification>
- Tombol "Renew N Pekerja" muncul per group di header accordion, bukan global
- Checkbox lock berdasarkan group key, bukan kategori
- Select-all per group mencentang semua checkbox di group-nya
- Modal konfirmasi muncul sebelum redirect ke CreateAssessment
- Filter yang menghasilkan 0 group menampilkan empty state khusus dengan tombol Reset Filter
- Summary cards update sesuai filter aktif (sudah berfungsi dari Phase 208)
- Tidak ada referensi ke selectedKategori, updateRenewSelectedButton, btn-renew-selected yang tersisa
</verification>

<success_criteria>
1. BULK-01: Select-all per group berfungsi, checkbox lock per group berfungsi
2. BULK-02: Tombol renew per group muncul/hilang sesuai checkbox state, modal konfirmasi tampil
3. FILT-01: Group tanpa anggota tidak muncul saat filter aktif, empty state filter tampil jika semua group kosong
4. FILT-02: Summary cards Expired dan Akan Expired update sesuai filter (sudah berfungsi dari Phase 208)
</success_criteria>

<output>
After completion, create `.planning/phases/209-bulk-renew-filter-compatibility/209-01-SUMMARY.md`
</output>
