---
phase: 211-data-display-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/CertificationManagementViewModel.cs
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - FIX-05
  - FIX-06
  - FIX-07
  - FIX-08
  - FIX-09
  - FIX-10

must_haves:
  truths:
    - "Sertifikat dengan ValidUntil=null dan CertificateType bukan Permanent muncul di renewal list sebagai Expired"
    - "Renew dari TrainingRecord menghasilkan CreateAssessment form dengan Category ter-prefill"
    - "Group header menampilkan nama kategori konsisten dengan AssessmentCategories.Name"
    - "Judul MIGAS dan Migas masuk ke group yang sama (case-insensitive)"
    - "Judul dengan karakter / & # tidak menyebabkan group terpisah atau URL error"
    - "ValidUntil=null di renewal mode menampilkan warning message informatif"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "Fixed DeriveCertificateStatus — ValidUntil=null non-Permanent returns Expired"
      contains: "certificateType != \"Permanent\""
    - path: "Controllers/AdminController.cs"
      provides: "Category prefill, case-insensitive grouping, URL-safe grouping, MapKategori audit"
  key_links:
    - from: "DeriveCertificateStatus"
      to: "BuildRenewalRowsAsync post-filter"
      via: "Status == Expired/AkanExpired filter at line 6727"
      pattern: "DeriveCertificateStatus"
    - from: "FilterRenewalCertificate GroupBy"
      to: "FilterRenewalCertificateGroup Where"
      via: "case-insensitive Judul matching"
      pattern: "StringComparer.OrdinalIgnoreCase"
---

<objective>
Fix 6 data/display bugs pada RenewalCertificate: ValidUntil=null handling, category prefill dari TrainingRecord, MapKategori konsistensi, grouping case-insensitive, URL-safe karakter khusus, dan error message informatif.

Purpose: Memastikan semua data renewal akurat dan tampilan grouped view konsisten
Output: AdminController.cs dan CertificationManagementViewModel.cs yang diperbaiki
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/211-data-display-fixes/211-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DeriveCertificateStatus dan ValidUntil=null warning (FIX-05, FIX-10)</name>
  <files>Models/CertificationManagementViewModel.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs (lines 50-62 — current DeriveCertificateStatus)
    - Controllers/AdminController.cs (lines 992-1044 — CreateAssessment GET renewal prefill)
  </read_first>
  <action>
**FIX-05: DeriveCertificateStatus** di `Models/CertificationManagementViewModel.cs` line 53-61.

Ubah logic dari:
```csharp
if (certificateType == "Permanent" || validUntil == null)
    return CertificateStatus.Permanent;
```

Menjadi:
```csharp
if (certificateType == "Permanent")
    return CertificateStatus.Permanent;
if (validUntil == null)
    return CertificateStatus.Expired; // non-Permanent with no expiry → treat as expired (needs renewal)
```

Ini memastikan TrainingRecord dengan ValidUntil=null tapi CertificateType bukan "Permanent" tetap muncul di renewal list.

**FIX-10: ValidUntil=null warning di CreateAssessment renewal mode** di `Controllers/AdminController.cs`.

Di block `renewSessionId.HasValue` (sekitar line 1011-1012), tambahkan else clause:
```csharp
if (sourceSession.ValidUntil.HasValue)
    model.ValidUntil = sourceSession.ValidUntil.Value.AddYears(1);
else
    ViewBag.RenewalValidUntilWarning = "Tanggal expired sertifikat asal kosong. Silakan isi ValidUntil secara manual.";
```

Di block `renewTrainingId.HasValue` (sekitar line 1034-1035), tambahkan else clause yang sama:
```csharp
if (sourceTraining.ValidUntil.HasValue)
    model.ValidUntil = sourceTraining.ValidUntil.Value.AddYears(1);
else
    ViewBag.RenewalValidUntilWarning = "Tanggal expired sertifikat asal kosong. Silakan isi ValidUntil secara manual.";
```

Lalu di `Views/Admin/CreateAssessment.cshtml`, cari area ValidUntil input field dan tambahkan warning alert SEBELUM field tersebut:
```html
@if (ViewBag.RenewalValidUntilWarning != null)
{
    <div class="alert alert-warning py-2 mb-2">
        <i class="bi bi-exclamation-triangle me-1"></i>@ViewBag.RenewalValidUntilWarning
    </div>
}
```
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\Desktop\PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CertificationManagementViewModel.cs contains `if (certificateType == "Permanent")` as separate check (NOT `|| validUntil == null`)
    - CertificationManagementViewModel.cs contains `return CertificateStatus.Expired;` for the validUntil==null non-Permanent case
    - AdminController.cs contains `ViewBag.RenewalValidUntilWarning = "Tanggal expired sertifikat asal kosong` in both renewSessionId and renewTrainingId blocks
    - CreateAssessment.cshtml contains `ViewBag.RenewalValidUntilWarning`
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>DeriveCertificateStatus correctly returns Expired for ValidUntil=null non-Permanent certificates; CreateAssessment shows warning when source cert has no expiry date</done>
</task>

<task type="auto">
  <name>Task 2: Fix Category prefill, MapKategori, case-insensitive grouping, URL-safe grouping (FIX-06, FIX-07, FIX-08, FIX-09)</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 1019-1040 — renewTrainingId prefill block)
    - Controllers/AdminController.cs (lines 6576-6582 — MapKategori)
    - Controllers/AdminController.cs (lines 6644-6661 — trainingRows mapping with MapKategori)
    - Controllers/AdminController.cs (lines 6960-6990 — FilterRenewalCertificate GroupBy logic)
    - Controllers/AdminController.cs (lines 7027-7046 — FilterRenewalCertificateGroup judul matching)
  </read_first>
  <action>
**FIX-06: Category prefill dari TrainingRecord** di `Controllers/AdminController.cs` sekitar line 1031.

Di block `renewTrainingId.HasValue`, setelah `model.Title = sourceTraining.Judul ?? "";`, tambahkan:
```csharp
model.Category = MapKategori(sourceTraining.Kategori);
```

Ini memastikan saat renew dari TrainingRecord, Category ter-prefill sesuai mapping (misal "MANDATORY" → "Mandatory HSSE Training").

**FIX-07: MapKategori konsistensi** di line 6576-6582.

Audit MapKategori dan pastikan output names match AssessmentCategories.Name di database. Current mapping:
- `"MANDATORY"` → `"Mandatory HSSE Training"` — verify ini sesuai AssessmentCategories.Name
- `"PROTON"` → `"Assessment Proton"` — verify ini sesuai AssessmentCategories.Name
- Default `_` → `raw` — pass-through, OK

Jika ada mismatch, update mapping. Juga tambahkan case-insensitive matching pada input:
```csharp
private static string MapKategori(string? raw) => raw?.Trim().ToUpperInvariant() switch
{
    null or "" => "-",
    "MANDATORY" => "Mandatory HSSE Training",
    "PROTON" => "Assessment Proton",
    _ => raw!.Trim()
};
```

**FIX-08: Case-insensitive GroupBy** di FilterRenewalCertificate sekitar line 6963.

Ubah:
```csharp
.GroupBy(r => r.Judul)
```
Menjadi:
```csharp
.GroupBy(r => r.Judul, StringComparer.OrdinalIgnoreCase)
```

Dan di line 6982 (group rows filter inside foreach):
```csharp
.Where(r => r.Judul == group.Judul)
```
Menjadi:
```csharp
.Where(r => string.Equals(r.Judul, group.Judul, StringComparison.OrdinalIgnoreCase))
```

**FIX-08 juga di FilterRenewalCertificateGroup** sekitar line 7028:
```csharp
.Where(r => r.Judul == judul)
```
Menjadi:
```csharp
.Where(r => string.Equals(r.Judul, judul, StringComparison.OrdinalIgnoreCase))
```

**FIX-09: URL-safe karakter khusus** — GroupKey sudah menggunakan Base64 encoding di line 6966-6967 yang menangani karakter khusus. Namun `judul` parameter di FilterRenewalCertificateGroup datang dari URL query string. Pastikan di view yang memanggil endpoint ini, judul di-encode dengan `Uri.EscapeDataString`.

Di `AdminController.cs` FilterRenewalCertificateGroup, tambahkan URL-decode pada `judul` parameter:
```csharp
judul = Uri.UnescapeDataString(judul ?? "");
```
Di awal method (setelah parameter list, sebelum BuildRenewalRowsAsync call). Ini memastikan judul dengan `/`, `&`, `#` tidak gagal match.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\Desktop\PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `model.Category = MapKategori(sourceTraining.Kategori)` in the renewTrainingId block
    - AdminController.cs MapKategori contains `raw?.Trim().ToUpperInvariant() switch` (case-insensitive input)
    - AdminController.cs contains `StringComparer.OrdinalIgnoreCase` in GroupBy call
    - AdminController.cs contains `string.Equals(r.Judul, group.Judul, StringComparison.OrdinalIgnoreCase)` in FilterRenewalCertificate
    - AdminController.cs contains `string.Equals(r.Judul, judul, StringComparison.OrdinalIgnoreCase)` in FilterRenewalCertificateGroup
    - AdminController.cs contains `Uri.UnescapeDataString(judul` in FilterRenewalCertificateGroup
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>Category prefill bekerja untuk TrainingRecord renewal, MapKategori case-insensitive, grouping case-insensitive, dan URL karakter khusus aman</done>
</task>

</tasks>

<verification>
1. `dotnet build` berhasil tanpa error
2. Grep confirm: `DeriveCertificateStatus` tidak lagi menggabungkan `certificateType == "Permanent"` dengan `validUntil == null` dalam satu condition
3. Grep confirm: `StringComparer.OrdinalIgnoreCase` ada di GroupBy
4. Grep confirm: `model.Category = MapKategori` ada di renewTrainingId block
5. Grep confirm: `Uri.UnescapeDataString` ada di FilterRenewalCertificateGroup
</verification>

<success_criteria>
- Build sukses
- Semua 6 requirement (FIX-05 through FIX-10) ter-address dalam kode
- Tidak ada regresi pada existing renewal functionality
</success_criteria>

<output>
After completion, create `.planning/phases/211-data-display-fixes/211-01-SUMMARY.md`
</output>
