---
phase: 212-tipe-filter-renewal-flow-addtraining-renewal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/RenewalCertificate.cshtml
  - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml
autonomous: true
requirements: [ENH-01, ENH-02, ENH-03]

must_haves:
  truths:
    - "Dropdown Tipe (Semua Tipe / Assessment / Training) muncul di filter bar sebelum Status"
    - "Memilih tipe memfilter tabel dan summary cards sesuai RecordType"
    - "Klik Renew pada baris manapun menampilkan modal pilihan metode (Assessment atau Training)"
    - "Bulk renew campuran tipe menampilkan error di modal dan disable tombol Lanjutkan"
    - "Bulk renew tipe seragam menampilkan modal pilihan metode"
  artifacts:
    - path: "Views/Admin/RenewalCertificate.cshtml"
      provides: "Tipe filter dropdown, renewMethodModal, bulk mixed-type logic"
      contains: "renewMethodModal"
    - path: "Views/Admin/Shared/_RenewalGroupTablePartial.cshtml"
      provides: "Tombol Renew sebagai button trigger modal (bukan anchor)"
      contains: "btn-renew-single"
    - path: "Controllers/AdminController.cs"
      provides: "Parameter tipe di FilterRenewalCertificate dan FilterRenewalCertificateGroup"
      contains: "string? tipe"
  key_links:
    - from: "RenewalCertificate.cshtml"
      to: "AdminController FilterRenewalCertificate"
      via: "fetch with tipe param"
      pattern: "params.set.*tipe"
    - from: "_RenewalGroupTablePartial.cshtml"
      to: "RenewalCertificate.cshtml renewMethodModal"
      via: "JS click handler on btn-renew-single"
      pattern: "btn-renew-single"
---

<objective>
Tambah filter tipe (Assessment/Training) pada RenewalCertificate dan ubah alur renewal dari direct-link menjadi modal pilihan metode, termasuk validasi mixed-type pada bulk renew.

Purpose: Admin dapat memfilter berdasarkan tipe sertifikat dan memilih metode renewal (via Assessment atau via Training) untuk setiap renew action.
Output: Filter tipe berfungsi, modal pilihan metode untuk single dan bulk renew, blokir bulk mixed-type.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-tipe-filter-renewal-flow-addtraining-renewal/212-CONTEXT.md
@.planning/phases/212-tipe-filter-renewal-flow-addtraining-renewal/212-RESEARCH.md

<interfaces>
<!-- From AdminController.cs — existing filter signatures -->
FilterRenewalCertificate(string? bagian, string? unit, string? status, string? category, string? subCategory, int page = 1)
FilterRenewalCertificateGroup(string? bagian, string? unit, string? status, string? category, string? subCategory, string groupKey, int page = 1)

<!-- From _RenewalGroupTablePartial.cshtml — existing Renew button is anchor -->
<a href="/Admin/CreateAssessment?renewSessionId=@row.SourceId">Renew</a>

<!-- From RenewalCertificate.cshtml — existing JS functions -->
refreshTable() — calls /Admin/FilterRenewalCertificate with URLSearchParams
refreshGroupTable() — calls /Admin/FilterRenewalCertificateGroup
renewGroup(groupKey, judul) — reads checked .cb-select checkboxes, shows bulkRenewConfirmModal

<!-- SertifikatRow has RecordType enum (Training/Assessment) — already available per row -->
<!-- Checkbox already has data-sourceid and data-recordtype attributes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipe filter di controller + view + JS</name>
  <files>Controllers/AdminController.cs, Views/Admin/RenewalCertificate.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (lines 6956-7100 — FilterRenewalCertificate and FilterRenewalCertificateGroup)
    - Views/Admin/RenewalCertificate.cshtml (lines 51-120 — filter bar, lines 300+ — JS refreshTable/refreshGroupTable)
  </read_first>
  <action>
**Controller — FilterRenewalCertificate (~line 6956):**
1. Tambah parameter `string? tipe = null` ke signature `FilterRenewalCertificate`
2. Setelah filter existing (category, subCategory, status), tambah:
```csharp
if (!string.IsNullOrEmpty(tipe) && Enum.TryParse<RecordType>(tipe, out var rt))
    allRows = allRows.Where(r => r.RecordType == rt).ToList();
```
3. Update `IsFiltered` check untuk include tipe:
```csharp
gvm.IsFiltered = ... || !string.IsNullOrEmpty(tipe);
```

**Controller — FilterRenewalCertificateGroup (~line 7028):**
1. Tambah parameter `string? tipe = null` ke signature
2. Tambah filter yang sama setelah existing filters:
```csharp
if (!string.IsNullOrEmpty(tipe) && Enum.TryParse<RecordType>(tipe, out var rt))
    allRows = allRows.Where(r => r.RecordType == rt).ToList();
```

**View — Filter bar di RenewalCertificate.cshtml:**
Insert dropdown Tipe SEBELUM dropdown Status (sebelum col-md untuk Status). HTML:
```html
<div class="col-md-2">
    <label class="form-label small mb-1">Tipe</label>
    <select id="filter-tipe" class="form-select form-select-sm">
        <option value="">Semua Tipe</option>
        <option value="Assessment">Assessment</option>
        <option value="Training">Training</option>
    </select>
</div>
```
Tambah event listener pada `filter-tipe` yang memanggil `refreshTable()` (sama seperti dropdown filter lainnya).

**View — JS refreshTable():**
Tambah baris untuk menyertakan tipe:
```javascript
var tipeEl = document.getElementById('filter-tipe');
if (tipeEl && tipeEl.value) params.set('tipe', tipeEl.value);
```

**View — JS refreshGroupTable():**
Tambah baris yang sama untuk tipe param.

**View — JS reset handler:**
Tambah `document.getElementById('filter-tipe').value = '';` di reset filter function.
  </action>
  <verify>
    <automated>grep -n "string? tipe" Controllers/AdminController.cs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs contains `string? tipe = null` in FilterRenewalCertificate signature
    - Controllers/AdminController.cs contains `string? tipe = null` in FilterRenewalCertificateGroup signature
    - Controllers/AdminController.cs contains `Enum.TryParse<RecordType>(tipe, out var rt)`
    - Views/Admin/RenewalCertificate.cshtml contains `id="filter-tipe"`
    - Views/Admin/RenewalCertificate.cshtml contains `params.set('tipe'`
    - Views/Admin/RenewalCertificate.cshtml contains `Semua Tipe`
    - Application builds without errors: `dotnet build --no-restore`
  </acceptance_criteria>
  <done>Dropdown Tipe muncul di filter bar, memfilter tabel dan summary cards, bekerja di flat dan grouped view</done>
</task>

<task type="auto">
  <name>Task 2: Modal pilihan metode renewal (single + bulk)</name>
  <files>Views/Admin/RenewalCertificate.cshtml, Views/Admin/Shared/_RenewalGroupTablePartial.cshtml</files>
  <read_first>
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml (full file — find existing Renew anchor/button)
    - Views/Admin/RenewalCertificate.cshtml (lines 200+ — existing modals bulkRenewConfirmModal, certificateHistoryModal; lines 400+ — JS renewGroup function, bulkRenew confirm handler)
  </read_first>
  <action>
**Partial — _RenewalGroupTablePartial.cshtml:**
Ganti anchor Renew (yang saat ini `<a href="/Admin/CreateAssessment?...">`) dengan button:
```html
<button type="button"
        class="btn btn-sm btn-warning btn-renew-single"
        data-sourceid="@row.SourceId"
        data-recordtype="@row.RecordType"
        data-judul="@row.Judul"
        data-namaworker="@row.NamaWorker">
    <i class="bi bi-arrow-repeat me-1"></i>Renew
</button>
```
Pastikan `data-recordtype` menggunakan `@row.RecordType` (menghasilkan "Assessment" atau "Training" PascalCase).

**View — RenewalCertificate.cshtml — Tambah renewMethodModal HTML:**
Tambahkan sebelum closing `</div>` terakhir (atau setelah modal existing):
```html
<div class="modal fade" id="renewMethodModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Pilih Metode Renewal</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <p>Sertifikat: <strong id="renew-method-judul"></strong></p>
        <p>Pekerja: <strong id="renew-method-nama"></strong></p>
      </div>
      <div class="modal-footer justify-content-between">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
        <div>
          <button type="button" class="btn btn-outline-primary me-2" id="btn-renew-via-assessment">
            <i class="bi bi-clipboard-check me-1"></i>Renew via Assessment
          </button>
          <button type="button" class="btn btn-warning" id="btn-renew-via-training">
            <i class="bi bi-journal-plus me-1"></i>Renew via Training
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
```

**View — JS — Single renew click handler:**
```javascript
document.addEventListener('click', function(e) {
    var btn = e.target.closest('.btn-renew-single');
    if (!btn) return;
    var sourceId = btn.dataset.sourceid;
    var recordType = btn.dataset.recordtype;
    var judul = btn.dataset.judul;
    var namaWorker = btn.dataset.namaworker;

    document.getElementById('renew-method-judul').textContent = judul;
    document.getElementById('renew-method-nama').textContent = namaWorker;

    document.getElementById('btn-renew-via-assessment').onclick = function() {
        var param = recordType === 'Assessment' ? 'renewSessionId' : 'renewTrainingId';
        window.location.href = '/Admin/CreateAssessment?' + param + '=' + sourceId;
    };
    document.getElementById('btn-renew-via-training').onclick = function() {
        var param = recordType === 'Assessment' ? 'renewSessionId' : 'renewTrainingId';
        window.location.href = '/Admin/AddTraining?' + param + '=' + sourceId;
    };

    new bootstrap.Modal(document.getElementById('renewMethodModal')).show();
});
```

**View — JS — Bulk renew mixed-type validation:**
Modifikasi fungsi `renewGroup(groupKey, judul)`:
1. Setelah membaca checked checkboxes, hitung unique types:
```javascript
var types = Array.from(checked).map(function(cb) { return cb.dataset.recordtype; });
var uniqueTypes = [...new Set(types)];
var isMixed = uniqueTypes.length > 1;
```
2. Simpan ke variabel global: `pendingRenewType = isMixed ? 'Mixed' : uniqueTypes[0];`
3. Build params seperti existing tapi dengan tipe-aware param names:
```javascript
var params = new URLSearchParams();
checked.forEach(function(cb) {
    if (cb.dataset.recordtype === 'Assessment') {
        params.append('renewSessionId', cb.dataset.sourceid);
    } else {
        params.append('renewTrainingId', cb.dataset.sourceid);
    }
});
pendingRenewParams = params.toString();
```

**View — Modifikasi bulkRenewConfirmModal body:**
Tambah elemen di modal body:
```html
<div id="bulk-renew-mixed-error" class="alert alert-danger d-none">
    <i class="bi bi-exclamation-triangle me-1"></i>
    Tidak dapat bulk renew campuran tipe Assessment dan Training. Gunakan filter Tipe untuk memisahkan terlebih dahulu.
</div>
<div id="bulk-renew-method-choice" class="d-none mt-3">
    <p class="fw-semibold">Pilih metode renewal:</p>
    <div class="d-flex gap-2">
        <button type="button" class="btn btn-outline-primary flex-fill" id="bulk-btn-via-assessment">
            <i class="bi bi-clipboard-check me-1"></i>Renew via Assessment
        </button>
        <button type="button" class="btn btn-warning flex-fill" id="bulk-btn-via-training">
            <i class="bi bi-journal-plus me-1"></i>Renew via Training
        </button>
    </div>
</div>
```

**View — JS — updateBulkModalState function (new):**
```javascript
function updateBulkModalState() {
    var mixedErr = document.getElementById('bulk-renew-mixed-error');
    var methodChoice = document.getElementById('bulk-renew-method-choice');
    var confirmBtn = document.getElementById('btn-confirm-bulk-renew'); // existing confirm button

    if (pendingRenewType === 'Mixed') {
        mixedErr.classList.remove('d-none');
        methodChoice.classList.add('d-none');
        if (confirmBtn) confirmBtn.disabled = true;
    } else {
        mixedErr.classList.add('d-none');
        methodChoice.classList.remove('d-none');
        if (confirmBtn) confirmBtn.disabled = true; // disable old confirm, use method buttons instead
    }
}
```
Call `updateBulkModalState()` di akhir `renewGroup()` setelah `modal.show()`.

**View — JS — Bulk method button handlers:**
```javascript
document.getElementById('bulk-btn-via-assessment').onclick = function() {
    window.location.href = '/Admin/CreateAssessment?' + pendingRenewParams;
};
document.getElementById('bulk-btn-via-training').onclick = function() {
    window.location.href = '/Admin/AddTraining?' + pendingRenewParams;
};
```

Deklarasikan variabel di scope IIFE: `var pendingRenewParams = ''; var pendingRenewType = '';`
  </action>
  <verify>
    <automated>grep -c "renewMethodModal" Views/Admin/RenewalCertificate.cshtml && grep -c "btn-renew-single" Views/Admin/Shared/_RenewalGroupTablePartial.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml contains `btn-renew-single` (button, not anchor)
    - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml does NOT contain `href="/Admin/CreateAssessment` as Renew link (old anchor removed)
    - Views/Admin/RenewalCertificate.cshtml contains `id="renewMethodModal"`
    - Views/Admin/RenewalCertificate.cshtml contains `btn-renew-via-assessment`
    - Views/Admin/RenewalCertificate.cshtml contains `btn-renew-via-training`
    - Views/Admin/RenewalCertificate.cshtml contains `bulk-renew-mixed-error`
    - Views/Admin/RenewalCertificate.cshtml contains `bulk-renew-method-choice`
    - Views/Admin/RenewalCertificate.cshtml contains `pendingRenewType`
    - Views/Admin/RenewalCertificate.cshtml contains `updateBulkModalState`
    - Application builds without errors: `dotnet build --no-restore`
  </acceptance_criteria>
  <done>Single renew menampilkan modal pilihan metode, bulk renew mixed-type menampilkan error, bulk renew tipe seragam menampilkan pilihan metode</done>
</task>

</tasks>

<verification>
1. `dotnet build --no-restore` — no errors
2. Grep semua acceptance criteria terpenuhi
3. Browser test: filter Tipe memfilter tabel, single Renew menampilkan modal, bulk mixed-type menampilkan error
</verification>

<success_criteria>
- Dropdown Tipe berfungsi memfilter flat dan grouped view termasuk summary cards
- Semua Renew button menampilkan modal pilihan metode (bukan direct link)
- Bulk renew campuran tipe diblokir dengan pesan error di modal
- Bulk renew tipe seragam menampilkan pilihan metode
</success_criteria>

<output>
After completion, create `.planning/phases/212-tipe-filter-renewal-flow-addtraining-renewal/212-01-SUMMARY.md`
</output>
