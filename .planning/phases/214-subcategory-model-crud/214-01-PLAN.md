---
phase: 214-subcategory-model-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/TrainingRecord.cs
  - Models/CreateTrainingRecordViewModel.cs
  - Models/EditTrainingRecordViewModel.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [MDL-01]

must_haves:
  truths:
    - "TrainingRecord memiliki kolom SubKategori nullable di database"
    - "AddTraining GET menyediakan data parent categories dan subcategories via ViewBag"
    - "EditTraining GET menyediakan data parent categories dan subcategories via ViewBag"
    - "AddTraining POST menyimpan SubKategori ke TrainingRecord"
    - "EditTraining POST menyimpan SubKategori ke TrainingRecord"
    - "ImportTraining template punya kolom ke-9 SubKategori"
    - "ImportTraining POST membaca kolom SubKategori dan menyimpan ke record"
  artifacts:
    - path: "Models/TrainingRecord.cs"
      provides: "SubKategori nullable string property"
      contains: "public string? SubKategori"
    - path: "Models/CreateTrainingRecordViewModel.cs"
      provides: "SubKategori field untuk AddTraining form"
      contains: "public string? SubKategori"
    - path: "Models/EditTrainingRecordViewModel.cs"
      provides: "SubKategori field untuk EditTraining form"
      contains: "public string? SubKategori"
  key_links:
    - from: "Controllers/AdminController.cs (AddTraining POST)"
      to: "TrainingRecord.SubKategori"
      via: "model.SubKategori assignment"
      pattern: "SubKategori = model\\.SubKategori"
    - from: "Controllers/AdminController.cs (ImportTraining POST)"
      to: "TrainingRecord.SubKategori"
      via: "row.Cell(9) mapping"
      pattern: "Cell\\(9\\)"
---

<objective>
Tambah field SubKategori ke model, ViewModel, controller actions (ViewBag + POST handlers), dan import logic.

Purpose: Foundation untuk dropdown SubKategori di views (Plan 02) — semua backend siap sebelum UI.
Output: Migration database, model updates, controller ViewBag + POST mapping, import template + logic update.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-subcategory-model-crud/214-CONTEXT.md
@.planning/phases/214-subcategory-model-crud/214-RESEARCH.md

@Models/TrainingRecord.cs
@Models/CreateTrainingRecordViewModel.cs
@Models/EditTrainingRecordViewModel.cs
@Models/AssessmentCategory.cs
@Controllers/AdminController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model + Migration + ViewModel SubKategori</name>
  <files>Models/TrainingRecord.cs, Models/CreateTrainingRecordViewModel.cs, Models/EditTrainingRecordViewModel.cs</files>
  <read_first>Models/TrainingRecord.cs, Models/CreateTrainingRecordViewModel.cs, Models/EditTrainingRecordViewModel.cs</read_first>
  <action>
1. Di `Models/TrainingRecord.cs`, tambah property baru di bawah `Kota`:
```csharp
public string? SubKategori { get; set; }         // Sub category from AssessmentCategories
```

2. Di `Models/CreateTrainingRecordViewModel.cs`, tambah setelah field `Kategori` (line 25):
```csharp
[Display(Name = "Sub Kategori")]
public string? SubKategori { get; set; }
```

3. Di `Models/EditTrainingRecordViewModel.cs`, tambah setelah field `Kategori` (line 26):
```csharp
[Display(Name = "Sub Kategori")]
public string? SubKategori { get; set; }
```

4. Jalankan EF Core migration:
```bash
dotnet ef migrations add AddSubKategoriToTrainingRecord
dotnet ef database update
```

Per D-01: kolom SubKategori nullable string di TrainingRecords.
Per D-02: SubKategori opsional — tidak ada [Required] attribute.
  </action>
  <verify>
    <automated>grep -n "SubKategori" Models/TrainingRecord.cs Models/CreateTrainingRecordViewModel.cs Models/EditTrainingRecordViewModel.cs</automated>
  </verify>
  <acceptance_criteria>
- Models/TrainingRecord.cs contains `public string? SubKategori { get; set; }`
- Models/CreateTrainingRecordViewModel.cs contains `public string? SubKategori { get; set; }`
- Models/EditTrainingRecordViewModel.cs contains `public string? SubKategori { get; set; }`
- Migration file exists in Migrations/ folder with name containing `AddSubKategoriToTrainingRecord`
- `dotnet ef database update` exits 0
  </acceptance_criteria>
  <done>SubKategori ada di model, ViewModel, dan database — migrasi sukses</done>
</task>

<task type="auto">
  <name>Task 2: Controller — ViewBag, POST handlers, Import logic</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>Controllers/AdminController.cs, Models/AssessmentCategory.cs</read_first>
  <action>
**A. Helper method** — Tambah private method di AdminController (dekat SetCategoriesViewBag):
```csharp
private async Task SetTrainingCategoryViewBag()
{
    ViewBag.KategoriOptions = await _context.AssessmentCategories
        .Where(c => c.ParentId == null && c.IsActive)
        .OrderBy(c => c.SortOrder).ThenBy(c => c.Name)
        .Select(c => new { c.Id, c.Name })
        .ToListAsync();

    ViewBag.SubKategoriOptions = await _context.AssessmentCategories
        .Where(c => c.ParentId != null && c.IsActive)
        .OrderBy(c => c.SortOrder).ThenBy(c => c.Name)
        .Select(c => new { c.Id, c.Name, c.ParentId })
        .ToListAsync();
}
```

**B. AddTraining GET** — Panggil `await SetTrainingCategoryViewBag();` di action AddTraining GET (sekitar line 5392-5496). Tambahkan sebelum return View().

**C. AddTraining POST** — Di bagian `new TrainingRecord { ... }` (sekitar line 5600-5627), tambah:
```csharp
SubKategori = model.SubKategori,
```
Juga panggil `await SetTrainingCategoryViewBag();` sebelum `return View(model)` di validation failure path.

**D. EditTraining GET** — Panggil `await SetTrainingCategoryViewBag();` sebelum return View(). Juga set `model.SubKategori = record.SubKategori;` saat mapping record ke ViewModel.

**E. EditTraining POST** — Di bagian update record fields, tambah:
```csharp
record.SubKategori = model.SubKategori;
```
Juga panggil `await SetTrainingCategoryViewBag();` sebelum `return View(model)` di validation failure path.

**F. DownloadImportTrainingTemplate** (sekitar line 5771) — Tambah "SubKategori (opsional)" sebagai header kolom ke-9 di array headers.

**G. ImportTraining POST** — Di bagian loop baca row Excel, setelah baca NomorSertifikat (cell 8), tambah:
```csharp
var subKategori = row.Cell(9).GetString().Trim();
```
Di bagian `new TrainingRecord { ... }`, tambah:
```csharp
SubKategori = string.IsNullOrWhiteSpace(subKategori) ? null : subKategori,
```

Per D-03, D-06: Kategori dari AssessmentCategories (parent, IsActive).
Per D-07, D-08: Import template + logic mendukung SubKategori.
  </action>
  <verify>
    <automated>grep -n "SetTrainingCategoryViewBag\|SubKategori" Controllers/AdminController.cs | head -20</automated>
  </verify>
  <acceptance_criteria>
- AdminController.cs contains method `SetTrainingCategoryViewBag`
- AdminController.cs contains `ViewBag.KategoriOptions` assignment with `ParentId == null && c.IsActive`
- AdminController.cs contains `ViewBag.SubKategoriOptions` assignment with `ParentId != null && c.IsActive`
- AddTraining POST contains `SubKategori = model.SubKategori`
- EditTraining POST contains `record.SubKategori = model.SubKategori`
- DownloadImportTrainingTemplate headers array contains `SubKategori (opsional)`
- ImportTraining POST contains `row.Cell(9)`
  </acceptance_criteria>
  <done>Controller siap: ViewBag categories untuk dropdown, POST handlers simpan SubKategori, import template+logic mendukung kolom ke-9</done>
</task>

</tasks>

<verification>
- `dotnet build` exits 0
- Migration applied successfully
- grep confirms SubKategori in model, ViewModels, and controller
</verification>

<success_criteria>
Backend lengkap: model, migration, ViewModel, controller ViewBag + POST + import — siap untuk view updates di Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/214-subcategory-model-crud/214-01-SUMMARY.md`
</output>
