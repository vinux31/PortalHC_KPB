---
phase: 215-team-view-filter-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/WorkerTrainingStatus.cs
  - Services/WorkerDataService.cs
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements:
  - FLT-04
must_haves:
  truths:
    - "Filter Category di Team View memfilter worker berdasarkan training DAN assessment records"
    - "Dropdown Sub Category muncul di Team View setelah Category, dependent pada Category"
    - "Sub Category di-disable saat Category belum dipilih"
    - "Memilih Sub Category memfilter daftar worker sesuai SubKategori training"
  artifacts:
    - path: "Models/WorkerTrainingStatus.cs"
      provides: "AssessmentSessions list property"
      contains: "List<AssessmentSession> AssessmentSessions"
    - path: "Services/WorkerDataService.cs"
      provides: "Batch query AssessmentSessions per user"
      contains: "assessmentSessionLookup"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "SubCategory dropdown + updated data attributes + JS filter"
      contains: "subCategoryFilter"
  key_links:
    - from: "Services/WorkerDataService.cs"
      to: "Models/WorkerTrainingStatus.cs"
      via: "worker.AssessmentSessions = sessions"
      pattern: "AssessmentSessions ="
    - from: "Views/CMP/RecordsTeam.cshtml"
      to: "data-categories"
      via: "training + assessment union"
      pattern: "assessmentCats"
    - from: "Views/CMP/RecordsTeam.cshtml"
      to: "subCategoryFilter"
      via: "dependent dropdown from subCategoryMap"
      pattern: "subCategoryMap"
---

<objective>
Menambahkan assessment records ke data filterable di Team View dan menambahkan dropdown Sub Category yang dependent pada Category.

Purpose: Saat ini filter Category di Team View hanya melihat TrainingRecords — worker yang hanya punya assessment di kategori tertentu tidak muncul. Juga belum ada Sub Category filter untuk drill-down lebih spesifik.

Output: RecordsTeam.cshtml yang filter Category-nya gabungkan training + assessment, plus dropdown Sub Category dependent.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-team-view-filter-enhancement/215-RESEARCH.md

<interfaces>
<!-- WorkerTrainingStatus model (current) -->
From Models/WorkerTrainingStatus.cs:
```csharp
public class WorkerTrainingStatus
{
    public string WorkerId { get; set; }
    public string WorkerName { get; set; }
    public string? NIP { get; set; }
    public string Position { get; set; }
    public string? Section { get; set; }
    public string? Unit { get; set; }
    public int TotalTrainings { get; set; }
    public int CompletedTrainings { get; set; }
    public int PendingTrainings { get; set; }
    public int ExpiringSoonTrainings { get; set; }
    public List<TrainingRecord> TrainingRecords { get; set; }
    public int CompletedAssessments { get; set; }
    // MISSING: List<AssessmentSession> AssessmentSessions — needs to be added
}
```

From Models/AssessmentSession.cs (relevant fields):
```csharp
public string UserId { get; set; }
public string Category { get; set; }
public bool? IsPassed { get; set; }
// NOTE: No SubKategori field — sub category filter only applies to training
```

From Models/AssessmentCategory.cs:
```csharp
public int Id { get; set; }
public string Name { get; set; }
public int? ParentId { get; set; }
public bool IsActive { get; set; }
public ICollection<AssessmentCategory> Children { get; set; }
```

From Services/WorkerDataService.cs GetWorkersInSection (line 192-198):
```csharp
// Existing batch pattern for assessment count
var passedAssessmentsByUser = await _context.AssessmentSessions
    .Where(a => userIds.Contains(a.UserId) && a.IsPassed == true)
    .GroupBy(a => a.UserId)
    .Select(g => new { UserId = g.Key, Count = g.Count() })
    .ToListAsync();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — AssessmentSessions di WorkerTrainingStatus + populate di GetWorkersInSection + subCategoryMap via ViewBag</name>
  <files>Models/WorkerTrainingStatus.cs, Services/WorkerDataService.cs, Controllers/CMPController.cs</files>
  <read_first>
    - Models/WorkerTrainingStatus.cs (current model — add AssessmentSessions property)
    - Services/WorkerDataService.cs (GetWorkersInSection method — add batch query)
    - Controllers/CMPController.cs (RecordsTeam action — add ViewBag.SubCategoryMapJson)
    - Models/AssessmentCategory.cs (ParentId hierarchy for subCategoryMap)
    - Models/AssessmentSession.cs (Category field confirmation)
  </read_first>
  <action>
1. **Models/WorkerTrainingStatus.cs** — Tambah property setelah `CompletedAssessments`:
   ```csharp
   public List<AssessmentSession> AssessmentSessions { get; set; } = new List<AssessmentSession>();
   ```
   Tambah `using HcPortal.Models;` jika belum ada (AssessmentSession di namespace yang sama, kemungkinan sudah).

2. **Services/WorkerDataService.cs** di `GetWorkersInSection()`:
   - Ubah query `passedAssessmentsByUser` menjadi query SEMUA AssessmentSessions (bukan hanya IsPassed==true), karena view butuh semua assessment untuk data-categories:
     ```csharp
     var assessmentSessionsByUser = await _context.AssessmentSessions
         .Where(a => userIds.Contains(a.UserId))
         .ToListAsync();
     var assessmentSessionLookup = assessmentSessionsByUser
         .GroupBy(a => a.UserId)
         .ToDictionary(g => g.Key, g => g.ToList());
     ```
   - Update `passedAssessmentLookup` agar tetap berfungsi — derive dari assessmentSessionsByUser:
     ```csharp
     var passedAssessmentLookup = assessmentSessionsByUser
         .Where(a => a.IsPassed == true)
         .GroupBy(a => a.UserId)
         .ToDictionary(g => g.Key, g => g.Count());
     ```
   - Di foreach user loop, set `AssessmentSessions`:
     ```csharp
     AssessmentSessions = assessmentSessionLookup.TryGetValue(user.Id, out var sessions)
         ? sessions : new List<AssessmentSession>()
     ```

3. **Controllers/CMPController.cs** di action `RecordsTeam`:
   - Query AssessmentCategories hierarchy dan build subCategoryMap:
     ```csharp
     var allCats = await _context.AssessmentCategories
         .Where(c => c.IsActive && c.ParentId == null)
         .Include(c => c.Children)
         .ToListAsync();
     var subCategoryMap = allCats.ToDictionary(
         p => p.Name,
         p => p.Children.Where(ch => ch.IsActive).Select(ch => ch.Name).OrderBy(n => n).ToList()
     );
     ViewBag.SubCategoryMapJson = System.Text.Json.JsonSerializer.Serialize(subCategoryMap);
     ```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep "List<AssessmentSession> AssessmentSessions" Models/WorkerTrainingStatus.cs returns match
    - grep "assessmentSessionLookup" Services/WorkerDataService.cs returns match
    - grep "SubCategoryMapJson" Controllers/CMPController.cs returns match
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>WorkerTrainingStatus membawa AssessmentSessions, GetWorkersInSection mengisi data tersebut, dan RecordsTeam controller menyediakan subCategoryMap JSON via ViewBag</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — data attributes gabung training+assessment, dropdown Sub Category dependent, JS filter update</name>
  <files>Views/CMP/RecordsTeam.cshtml</files>
  <read_first>
    - Views/CMP/RecordsTeam.cshtml (full file — current data attributes, filter controls, JS functions)
  </read_first>
  <action>
1. **Category dropdown options** — Ubah variabel `categories` di atas (line 16-21) agar gabungkan training + assessment:
   ```csharp
   var trainingCats = Model.SelectMany(w => w.TrainingRecords)
       .Select(t => t.Kategori)
       .Where(k => !string.IsNullOrEmpty(k));
   var assessmentCats = Model.SelectMany(w => w.AssessmentSessions)
       .Select(a => a.Category)
       .Where(c => !string.IsNullOrEmpty(c));
   var categories = trainingCats.Union(assessmentCats)
       .Distinct()
       .OrderBy(k => k)
       .ToList();
   ```

2. **Sub Category dropdown** — Tambah setelah Category dropdown div (setelah line 78), di Row 1. Ubah layout Row 1 menjadi 4 kolom (col-md-3 masing-masing) untuk muat 4 dropdown: Bagian, Unit, Category, SubCategory:
   - Ubah col-12 col-md-4 pada Bagian, Unit, Category menjadi `col-12 col-md-3`
   - Tambah div baru:
     ```html
     <div class="col-12 col-md-3">
         <label class="form-label small text-muted mb-1">Sub Category</label>
         <select id="subCategoryFilter" class="form-select" disabled onchange="filterTeamTable()">
             <option value="">All Sub Categories</option>
         </select>
     </div>
     ```

3. **data-* attributes di worker row** — Ubah block Razor (line 155-176) untuk gabungkan assessment:
   ```csharp
   var trainingCats2 = worker.TrainingRecords
       .Select(t => t.Kategori).Where(k => !string.IsNullOrEmpty(k)).Distinct();
   var assessmentCats2 = worker.AssessmentSessions
       .Select(a => a.Category).Where(c => !string.IsNullOrEmpty(c)).Distinct();
   var allCats = trainingCats2.Union(assessmentCats2).ToList();
   var allCatsStr = string.Join(",", allCats);

   var completedTrainingCats = worker.TrainingRecords
       .Where(t => !string.IsNullOrEmpty(t.Kategori) &&
           (t.Status == "Passed" || t.Status == "Valid" || t.Status == "Permanent"))
       .Select(t => t.Kategori).Distinct();
   var passedAssessmentCats = worker.AssessmentSessions
       .Where(a => a.IsPassed == true)
       .Select(a => a.Category).Where(c => !string.IsNullOrEmpty(c)).Distinct();
   var completedCatsStr = string.Join(",", completedTrainingCats.Union(passedAssessmentCats));

   var allSubCats = worker.TrainingRecords
       .Select(t => t.SubKategori).Where(s => !string.IsNullOrEmpty(s)).Distinct();
   var allSubCatsStr = string.Join(",", allSubCats);

   var completedSubCats = worker.TrainingRecords
       .Where(t => !string.IsNullOrEmpty(t.SubKategori) &&
           (t.Status == "Passed" || t.Status == "Valid" || t.Status == "Permanent"))
       .Select(t => t.SubKategori).Distinct();
   var completedSubCatsStr = string.Join(",", completedSubCats);

   var hasTraining = allCats.Any() || worker.TrainingRecords.Any(t =>
       !string.IsNullOrEmpty(t.Kategori) &&
       (t.Status == "Passed" || t.Status == "Valid" || t.Status == "Permanent"));
   ```
   Update TR tag:
   ```html
   <tr class="worker-row"
       data-section="@worker.Section"
       data-unit="@worker.Unit"
       data-categories="@allCatsStr.ToLower()"
       data-has-training="@hasTraining.ToString().ToLower()"
       data-completed-categories="@completedCatsStr.ToLower()"
       data-subcategories="@allSubCatsStr.ToLower()"
       data-completed-subcategories="@completedSubCatsStr.ToLower()"
       data-name="@worker.WorkerName.ToLower()"
       data-nip="@((worker.NIP ?? "").ToLower())">
   ```

4. **JS subCategoryMap** — Tambah di awal script section:
   ```javascript
   var subCategoryMap = @Html.Raw(ViewBag.SubCategoryMapJson ?? "{}");
   ```

5. **JS categoryFilter change handler** — Tambah event listener untuk populate Sub Category:
   ```javascript
   document.getElementById('categoryFilter').addEventListener('change', function() {
       var cat = this.value;
       var subSelect = document.getElementById('subCategoryFilter');
       subSelect.innerHTML = '<option value="">All Sub Categories</option>';
       subSelect.disabled = !cat;
       if (cat && subCategoryMap[cat]) {
           subCategoryMap[cat].forEach(function(sub) {
               var opt = document.createElement('option');
               opt.value = sub;
               opt.textContent = sub;
               subSelect.appendChild(opt);
           });
       }
   });
   ```

6. **filterTeamTable()** — Tambah subCategory logic:
   - Tambah line: `const subCategory = document.getElementById('subCategoryFilter').value.toLowerCase();`
   - Tambah: `const rowSubCategories = row.getAttribute('data-subcategories') || '';`
   - Tambah: `const matchSubCategory = !subCategory || rowSubCategories.split(',').some(s => s.trim() === subCategory);`
   - Update matchAll: tambah `&& matchSubCategory`
   - Untuk status "Sudah" dengan subCategory aktif, cek juga `data-completed-subcategories`:
     ```javascript
     if (subCategory) {
         const completedSubCats = row.getAttribute('data-completed-subcategories') || '';
         matchStatus = completedSubCats.split(',').some(s => s.trim() === subCategory);
     }
     ```

7. **resetTeamFilters()** — Tambah reset Sub Category:
   ```javascript
   document.getElementById('subCategoryFilter').value = '';
   document.getElementById('subCategoryFilter').disabled = true;
   ```

8. **updateExportLinks()** — Tambah subCategory ke params:
   ```javascript
   const subCategory = document.getElementById('subCategoryFilter')?.value || '';
   if (subCategory) params.set('subCategory', subCategory);
   ```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep "subCategoryFilter" Views/CMP/RecordsTeam.cshtml returns multiple matches
    - grep "data-subcategories" Views/CMP/RecordsTeam.cshtml returns match
    - grep "data-completed-subcategories" Views/CMP/RecordsTeam.cshtml returns match
    - grep "assessmentCats" Views/CMP/RecordsTeam.cshtml returns match (assessment categories in union)
    - grep "subCategoryMap" Views/CMP/RecordsTeam.cshtml returns match
    - grep "matchSubCategory" Views/CMP/RecordsTeam.cshtml returns match
    - grep "AssessmentSessions" Views/CMP/RecordsTeam.cshtml returns match
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Team View filter Category gabungkan training+assessment, dropdown Sub Category muncul dependent pada Category, filter JS bekerja untuk subcategory, reset menghandle subcategory</done>
</task>

</tasks>

<verification>
1. `dotnet build` — 0 errors
2. Browser: Buka Team View → Category dropdown berisi kategori dari training DAN assessment
3. Browser: Sub Category dropdown disabled saat Category kosong
4. Browser: Pilih Category → Sub Category enabled dan berisi children dari category tersebut
5. Browser: Pilih Sub Category → worker difilter sesuai SubKategori training
6. Browser: Reset → semua filter kembali default, Sub Category disabled
</verification>

<success_criteria>
- Filter Category di Team View memfilter berdasarkan training + assessment records
- Dropdown Sub Category muncul setelah Category, dependent dan disabled saat Category kosong
- Memilih Sub Category memfilter worker sesuai SubKategori training
- Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/215-team-view-filter-enhancement/215-01-SUMMARY.md`
</output>
