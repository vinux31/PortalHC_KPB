---
phase: 217-fix-category-dropdown-di-recordsteam-agar-ambil-dari-master-assessmentcategories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements:
  - CAT-01

must_haves:
  truths:
    - "Dropdown Category di RecordsTeam menampilkan kategori dari tabel AssessmentCategories (master), bukan dari string records"
    - "Sub Category dropdown tetap dependent dan berfungsi"
    - "Filter Category, Sub Category, Status, dan Export tetap berfungsi setelah perubahan"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "ViewBag.MasterCategoriesJson dengan data dari AssessmentCategories"
      contains: "MasterCategoriesJson"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "Dropdown Category dari master JSON, bukan union strings"
      contains: "masterCategories"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "Views/CMP/RecordsTeam.cshtml"
      via: "ViewBag.MasterCategoriesJson"
      pattern: "MasterCategoriesJson"
---

<objective>
Fix dropdown Category di RecordsTeam agar mengambil data dari tabel master AssessmentCategories (sama dengan Admin/ManageCategories), bukan dari union string TrainingRecord.Kategori + AssessmentSession.Category.

Purpose: Dropdown saat ini menampilkan kategori "liar" dari data records yang bisa tidak konsisten dengan master. Harus sinkron dengan ManageCategories.
Output: Dropdown Category yang reliable dari master table, filter tetap berfungsi.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-fix-category-dropdown-di-recordsteam-agar-ambil-dari-master-assessmentcategories/217-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah MasterCategoriesJson di Controller dan ganti dropdown source di View</name>
  <files>Controllers/CMPController.cs, Views/CMP/RecordsTeam.cshtml</files>
  <read_first>
    - Controllers/CMPController.cs (RecordsTeam action, sekitar line 435-467)
    - Views/CMP/RecordsTeam.cshtml (seluruh file — lihat lines 16-26 union logic dan lines 74-80 dropdown rendering)
  </read_first>
  <action>
**Controller (CMPController.cs) — RecordsTeam action:**

Setelah line 464 (`ViewBag.SubCategoryMapJson = ...`), tambahkan:

```csharp
// Phase 217: Master categories from AssessmentCategories table (D-01)
var masterCategories = allCats.Select(c => c.Name).OrderBy(n => n).ToList();
ViewBag.MasterCategoriesJson = System.Text.Json.JsonSerializer.Serialize(masterCategories);
```

Variable `allCats` sudah ada di line 456-459 — reuse, JANGAN query ulang.

**View (RecordsTeam.cshtml):**

1. HAPUS lines 16-26 (blok Razor yang union TrainingRecord.Kategori + AssessmentSession.Category):
```csharp
// HAPUS SELURUH BLOK INI:
var trainingCats = Model.SelectMany(w => w.TrainingRecords)...
var assessmentCats = Model.SelectMany(w => w.AssessmentSessions)...
var categories = trainingCats.Union(assessmentCats)...
```

2. GANTI dropdown Category rendering (lines 76-80) dari Razor foreach ke JavaScript. Ubah bagian select element menjadi:
```html
<select id="categoryFilter" class="form-select" onchange="filterTeamTable()">
    <option value="">All Categories</option>
</select>
```

3. Di bagian script (di bawah, dekat script lainnya), tambahkan populasi dropdown dari JSON:
```javascript
// Phase 217: Populate category dropdown from master AssessmentCategories (D-01)
(function() {
    var masterCategories = @Html.Raw(ViewBag.MasterCategoriesJson);
    var sel = document.getElementById('categoryFilter');
    masterCategories.forEach(function(cat) {
        var opt = document.createElement('option');
        opt.value = cat;
        opt.textContent = cat;
        sel.appendChild(opt);
    });
})();
```

4. JANGAN ubah `data-categories` attribute per worker row (D-06) — itu tetap dari actual records.
5. JANGAN ubah fungsi `filterTeamTable()`, `updateExportLinks()`, atau Sub Category logic — mereka tetap bekerja karena dropdown value format sama (string category name).
  </action>
  <verify>
    <automated>grep -n "MasterCategoriesJson" Controllers/CMPController.cs && grep -n "masterCategories" Views/CMP/RecordsTeam.cshtml && ! grep -n "trainingCats\|assessmentCats" Views/CMP/RecordsTeam.cshtml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/CMPController.cs contains `ViewBag.MasterCategoriesJson`
    - Controllers/CMPController.cs contains `allCats.Select(c => c.Name)` (reuse existing query)
    - Views/CMP/RecordsTeam.cshtml does NOT contain `trainingCats` or `assessmentCats` (union logic removed)
    - Views/CMP/RecordsTeam.cshtml contains `masterCategories` (JS population from master)
    - Views/CMP/RecordsTeam.cshtml contains `MasterCategoriesJson` (ViewBag reference)
    - Views/CMP/RecordsTeam.cshtml still contains `data-categories` (per D-06, unchanged)
    - Views/CMP/RecordsTeam.cshtml still contains `filterTeamTable` (filter logic unchanged)
    - Views/CMP/RecordsTeam.cshtml still contains `SubCategoryMapJson` (sub category unchanged)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Dropdown Category di RecordsTeam mengambil data dari master AssessmentCategories via JSON, union string logic dihapus, semua filter tetap berfungsi, build berhasil</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil tanpa error
- Dropdown Category di RecordsTeam menampilkan kategori yang sama dengan Admin/ManageCategories
- Filter Category + Sub Category + Status tetap berfungsi
- Export tetap include category parameter
</verification>

<success_criteria>
- Dropdown Category populated dari master AssessmentCategories, bukan union records
- Tidak ada regression pada filter atau export
- Build berhasil
</success_criteria>

<output>
After completion, create `.planning/phases/217-fix-category-dropdown-di-recordsteam-agar-ambil-dari-master-assessmentcategories/217-01-SUMMARY.md`
</output>
