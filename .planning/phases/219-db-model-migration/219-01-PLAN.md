---
phase: 219-db-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/OrganizationUnit.cs
  - Models/KkjModels.cs
  - Data/ApplicationDbContext.cs
autonomous: true
requirements: [DB-01, DB-02, DB-03]

must_haves:
  truths:
    - "Tabel OrganizationUnits ada di database dengan 21 baris (4 Bagian + 17 Unit)"
    - "KkjFile dan CpdpFile memiliki FK OrganizationUnitId ke OrganizationUnits"
    - "Tabel KkjBagians sudah tidak ada di database"
    - "Aplikasi build tanpa error"
  artifacts:
    - path: "Models/OrganizationUnit.cs"
      provides: "Entity class OrganizationUnit"
      contains: "public class OrganizationUnit"
    - path: "Models/KkjModels.cs"
      provides: "Updated KkjFile/CpdpFile dengan OrganizationUnitId, tanpa KkjBagian"
      contains: "OrganizationUnitId"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet<OrganizationUnit>, konfigurasi self-referential FK"
      contains: "DbSet<OrganizationUnit>"
  key_links:
    - from: "Models/KkjModels.cs"
      to: "Models/OrganizationUnit.cs"
      via: "FK navigation property"
      pattern: "public OrganizationUnit OrganizationUnit"
    - from: "Data/ApplicationDbContext.cs"
      to: "Models/OrganizationUnit.cs"
      via: "Entity configuration"
      pattern: "builder\\.Entity<OrganizationUnit>"
---

<objective>
Membuat entity OrganizationUnit, migrasi data 4 Bagian + 17 Unit dari static class, dan konsolidasi KkjBagian ke OrganizationUnit.

Purpose: Foundation database untuk CRUD Struktur Organisasi (Phase 220) dan integrasi codebase (Phase 221).
Output: Model OrganizationUnit.cs, updated KkjModels.cs, updated ApplicationDbContext.cs, migration file.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-db-model-migration/219-CONTEXT.md
@.planning/phases/219-db-model-migration/219-RESEARCH.md

<interfaces>
<!-- Existing entities yang akan dimodifikasi -->

From Models/KkjModels.cs:
```csharp
public class KkjBagian
{
    public int Id { get; set; }
    public string Name { get; set; } = "";
    public int DisplayOrder { get; set; } = 0;
    public ICollection<KkjFile> Files { get; set; } = new List<KkjFile>();
}

public class KkjFile
{
    public int Id { get; set; }
    public int BagianId { get; set; }
    public KkjBagian Bagian { get; set; } = null!;
    // ... other fields
}

public class CpdpFile
{
    public int Id { get; set; }
    public int BagianId { get; set; }
    public KkjBagian Bagian { get; set; } = null!;
    // ... other fields
}
```

From Data/ApplicationDbContext.cs (line 28-30):
```csharp
public DbSet<KkjBagian> KkjBagians { get; set; }
public DbSet<KkjFile> KkjFiles { get; set; }
public DbSet<CpdpFile> CpdpFiles { get; set; }
```

From Data/ApplicationDbContext.cs (line 219-244) — KkjBagian/KkjFile/CpdpFile config:
```csharp
builder.Entity<KkjBagian>(entity =>
{
    entity.ToTable("KkjBagians");
    entity.HasIndex(b => b.Name).IsUnique();
    entity.HasIndex(b => b.DisplayOrder);
});

builder.Entity<KkjFile>(entity =>
{
    entity.ToTable("KkjFiles");
    entity.HasOne(f => f.Bagian)
          .WithMany(b => b.Files)
          .HasForeignKey(f => f.BagianId)
          .OnDelete(DeleteBehavior.Cascade);
});

builder.Entity<CpdpFile>(entity =>
{
    entity.ToTable("CpdpFiles");
    entity.HasOne(f => f.Bagian)
          .WithMany()
          .HasForeignKey(f => f.BagianId)
          .OnDelete(DeleteBehavior.Cascade);
});
```

From Data/ApplicationDbContext.cs (line 571-575) — Self-referential FK pattern (AssessmentCategory):
```csharp
entity.HasOne(c => c.Parent)
      .WithMany(c => c.Children)
      .HasForeignKey(c => c.ParentId)
      .OnDelete(DeleteBehavior.Restrict);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat model OrganizationUnit dan update KkjModels + DbContext</name>
  <files>Models/OrganizationUnit.cs, Models/KkjModels.cs, Data/ApplicationDbContext.cs</files>
  <read_first>
    - Models/KkjModels.cs (current KkjBagian, KkjFile, CpdpFile entities)
    - Data/ApplicationDbContext.cs (current DbSets dan entity configuration)
    - Models/AssessmentCategory.cs (self-referential FK pattern reference)
  </read_first>
  <action>
1. Buat file `Models/OrganizationUnit.cs` dengan class:
```csharp
namespace HcPortal.Models
{
    public class OrganizationUnit
    {
        public int Id { get; set; }
        public string Name { get; set; } = "";
        public int? ParentId { get; set; }
        public int Level { get; set; } = 0;
        public int DisplayOrder { get; set; } = 0;
        public bool IsActive { get; set; } = true;

        // Navigation properties
        public OrganizationUnit? Parent { get; set; }
        public ICollection<OrganizationUnit> Children { get; set; } = new List<OrganizationUnit>();
        public ICollection<KkjFile> KkjFiles { get; set; } = new List<KkjFile>();
        public ICollection<CpdpFile> CpdpFiles { get; set; } = new List<CpdpFile>();
    }
}
```

2. Update `Models/KkjModels.cs`:
   - HAPUS seluruh class `KkjBagian` (baris 3-12)
   - Di class `KkjFile`: ganti `public int BagianId` → `public int OrganizationUnitId`, ganti `public KkjBagian Bagian { get; set; } = null!;` → `public OrganizationUnit OrganizationUnit { get; set; } = null!;`
   - Di class `CpdpFile`: sama — ganti `BagianId` → `OrganizationUnitId`, `KkjBagian Bagian` → `OrganizationUnit OrganizationUnit`

3. Update `Data/ApplicationDbContext.cs`:
   - Ganti `public DbSet<KkjBagian> KkjBagians { get; set; }` → `public DbSet<OrganizationUnit> OrganizationUnits { get; set; }` (baris 28)
   - HAPUS blok `builder.Entity<KkjBagian>(...)` (baris 219-224)
   - TAMBAH blok OrganizationUnit config (setelah blok AssessmentCategory sekitar baris 582):
```csharp
builder.Entity<OrganizationUnit>(entity =>
{
    entity.ToTable("OrganizationUnits");
    entity.HasIndex(u => u.Name).IsUnique();
    entity.HasIndex(u => new { u.ParentId, u.DisplayOrder });
    entity.Property(u => u.IsActive).HasDefaultValue(true);

    entity.HasOne(u => u.Parent)
          .WithMany(u => u.Children)
          .HasForeignKey(u => u.ParentId)
          .OnDelete(DeleteBehavior.Restrict);
});
```
   - Update blok `builder.Entity<KkjFile>(...)` (baris 227-234): ganti `f.Bagian` → `f.OrganizationUnit`, ganti `b => b.Files` → `b => b.KkjFiles`, ganti `f.BagianId` → `f.OrganizationUnitId`, ganti `DeleteBehavior.Cascade` → `DeleteBehavior.Restrict`
   - Update blok `builder.Entity<CpdpFile>(...)` (baris 237-244): ganti `f.Bagian` → `f.OrganizationUnit`, ganti `.WithMany()` → `.WithMany(b => b.CpdpFiles)`, ganti `f.BagianId` → `f.OrganizationUnitId`, ganti `DeleteBehavior.Cascade` → `DeleteBehavior.Restrict`
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Models/OrganizationUnit.cs exists dan contains `public class OrganizationUnit`
    - Models/OrganizationUnit.cs contains `public int? ParentId`
    - Models/OrganizationUnit.cs contains `public ICollection<KkjFile> KkjFiles`
    - Models/KkjModels.cs TIDAK contains `class KkjBagian`
    - Models/KkjModels.cs contains `public int OrganizationUnitId { get; set; }`
    - Models/KkjModels.cs contains `public OrganizationUnit OrganizationUnit { get; set; }`
    - Data/ApplicationDbContext.cs contains `DbSet<OrganizationUnit> OrganizationUnits`
    - Data/ApplicationDbContext.cs TIDAK contains `DbSet<KkjBagian>`
    - Data/ApplicationDbContext.cs contains `DeleteBehavior.Restrict` untuk KkjFile dan CpdpFile
    - `dotnet build` succeeds (exit code 0)
  </acceptance_criteria>
  <done>Model OrganizationUnit tersedia, KkjBagian dihapus dari codebase, KkjFile/CpdpFile mengarah ke OrganizationUnit, build sukses</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration dan apply — seed 21 baris + remap FK + drop KkjBagians</name>
  <files>Migrations/[timestamp]_AddOrganizationUnitsAndConsolidateKkjBagian.cs</files>
  <read_first>
    - Models/OrganizationUnit.cs (baru dibuat Task 1)
    - Models/KkjModels.cs (updated Task 1)
    - Data/ApplicationDbContext.cs (updated Task 1)
    - Migrations/20260302125630_DropKkjTablesAddKkjFiles.cs (reference pattern untuk drop table + remap FK)
  </read_first>
  <action>
1. Generate migration:
```bash
cd "C:/Users/Administrator/Desktop/PortalHC_KPB"
dotnet ef migrations add AddOrganizationUnitsAndConsolidateKkjBagian
```

2. EDIT file migration yang di-generate. EF Core akan generate DDL (create table, add column, drop column, drop table) tapi TIDAK akan generate SQL seed dan remap. Tambahkan SQL statements di method `Up()` SETELAH `CreateTable("OrganizationUnits", ...)` dan SEBELUM `DropTable("KkjBagians")`. Urutan operasi dalam `Up()`:

   a. Setelah `migrationBuilder.CreateTable("OrganizationUnits", ...)` yang sudah di-generate, tambahkan seed SQL:
```csharp
migrationBuilder.Sql(@"
    -- Seed Bagian (Level 0)
    INSERT INTO OrganizationUnits (Name, ParentId, Level, DisplayOrder, IsActive)
    VALUES
        ('RFCC',      NULL, 0, 1, 1),
        ('DHT / HMU', NULL, 0, 2, 1),
        ('NGP',       NULL, 0, 3, 1),
        ('GAST',      NULL, 0, 4, 1);

    -- Seed Unit RFCC (Level 1)
    INSERT INTO OrganizationUnits (Name, ParentId, Level, DisplayOrder, IsActive)
    VALUES
        ('RFCC LPG Treating Unit (062)',  (SELECT Id FROM OrganizationUnits WHERE Name = 'RFCC'), 1, 1, 1),
        ('Propylene Recovery Unit (063)', (SELECT Id FROM OrganizationUnits WHERE Name = 'RFCC'), 1, 2, 1);

    -- Seed Unit DHT / HMU (Level 1)
    INSERT INTO OrganizationUnits (Name, ParentId, Level, DisplayOrder, IsActive)
    VALUES
        ('Diesel Hydrotreating Unit I & II (054 & 083)', (SELECT Id FROM OrganizationUnits WHERE Name = 'DHT / HMU'), 1, 1, 1),
        ('Hydrogen Manufacturing Unit (068)',            (SELECT Id FROM OrganizationUnits WHERE Name = 'DHT / HMU'), 1, 2, 1),
        ('Common DHT H2 Compressor (085)',               (SELECT Id FROM OrganizationUnits WHERE Name = 'DHT / HMU'), 1, 3, 1);

    -- Seed Unit NGP (Level 1)
    INSERT INTO OrganizationUnits (Name, ParentId, Level, DisplayOrder, IsActive)
    VALUES
        ('Saturated Gas Concentration Unit (060)', (SELECT Id FROM OrganizationUnits WHERE Name = 'NGP'), 1, 1, 1),
        ('Saturated LPG Treating Unit (064)',      (SELECT Id FROM OrganizationUnits WHERE Name = 'NGP'), 1, 2, 1),
        ('Isomerization Unit (082)',               (SELECT Id FROM OrganizationUnits WHERE Name = 'NGP'), 1, 3, 1),
        ('Common Facilities For NLP (160)',         (SELECT Id FROM OrganizationUnits WHERE Name = 'NGP'), 1, 4, 1),
        ('Naphtha Hydrotreating Unit II (084)',    (SELECT Id FROM OrganizationUnits WHERE Name = 'NGP'), 1, 5, 1);

    -- Seed Unit GAST (Level 1)
    INSERT INTO OrganizationUnits (Name, ParentId, Level, DisplayOrder, IsActive)
    VALUES
        ('RFCC NHT (053)',                         (SELECT Id FROM OrganizationUnits WHERE Name = 'GAST'), 1, 1, 1),
        ('Alkylation Unit (065)',                   (SELECT Id FROM OrganizationUnits WHERE Name = 'GAST'), 1, 2, 1),
        ('Wet Gas Sulfuric Acid Unit (066)',        (SELECT Id FROM OrganizationUnits WHERE Name = 'GAST'), 1, 3, 1),
        ('SWS RFCC & Non RFCC (067 & 167)',        (SELECT Id FROM OrganizationUnits WHERE Name = 'GAST'), 1, 4, 1),
        ('Amine Regeneration Unit I & II (069 & 079)', (SELECT Id FROM OrganizationUnits WHERE Name = 'GAST'), 1, 5, 1),
        ('Flare System (319)',                     (SELECT Id FROM OrganizationUnits WHERE Name = 'GAST'), 1, 6, 1),
        ('Sulfur Recovery Unit (169)',              (SELECT Id FROM OrganizationUnits WHERE Name = 'GAST'), 1, 7, 1);
");
```

   b. Setelah EF-generated `AddColumn("OrganizationUnitId", ...)` pada KkjFiles dan CpdpFiles, tambahkan remap SQL dan orphan guard:
```csharp
// Remap KkjFiles.OrganizationUnitId dari BagianId
migrationBuilder.Sql(@"
    UPDATE kf
    SET kf.OrganizationUnitId = ou.Id
    FROM KkjFiles kf
    INNER JOIN KkjBagians kb ON kf.BagianId = kb.Id
    INNER JOIN OrganizationUnits ou ON ou.Name = kb.Name AND ou.ParentId IS NULL;

    -- Guard: hapus orphans yang tidak ter-remap
    DELETE FROM KkjFiles WHERE OrganizationUnitId IS NULL;
");

// Remap CpdpFiles.OrganizationUnitId dari BagianId
migrationBuilder.Sql(@"
    UPDATE cf
    SET cf.OrganizationUnitId = ou.Id
    FROM CpdpFiles cf
    INNER JOIN KkjBagians kb ON cf.BagianId = kb.Id
    INNER JOIN OrganizationUnits ou ON ou.Name = kb.Name AND ou.ParentId IS NULL;

    -- Guard: hapus orphans yang tidak ter-remap
    DELETE FROM CpdpFiles WHERE OrganizationUnitId IS NULL;
");
```

   c. Pastikan urutan EF-generated operations benar: CREATE TABLE OrganizationUnits → seed SQL → ADD COLUMN OrganizationUnitId (nullable) → remap SQL → ALTER COLUMN NOT NULL → ADD FK baru → DROP FK lama → DROP INDEX BagianId → DROP COLUMN BagianId → DROP TABLE KkjBagians. Jika EF meng-generate urutan yang salah, pindahkan statements secara manual.

   d. Untuk method `Down()`: biarkan EF-generated reverse. Tambah comment `// Note: data loss on Down() is expected — seed data dan remap tidak reversible`.

3. Apply migration:
```bash
dotnet ef database update
```

4. Verifikasi data:
```bash
dotnet ef database update  # should complete without error
dotnet build               # should compile
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet ef database update 2>&1 | tail -5 && dotnet build --no-restore 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists di folder Migrations/ dengan nama yang mengandung "AddOrganizationUnitsAndConsolidateKkjBagian"
    - Migration file contains "INSERT INTO OrganizationUnits"
    - Migration file contains "('RFCC'," dan "('DHT / HMU'," dan "('NGP'," dan "('GAST',"
    - Migration file contains "UPDATE kf" (remap KkjFiles)
    - Migration file contains "UPDATE cf" (remap CpdpFiles)
    - Migration file contains "DropTable" dengan "KkjBagians"
    - `dotnet ef database update` exits 0 (no error)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Migration berhasil di-apply: tabel OrganizationUnits ada dengan 21 baris seed data, KkjFiles/CpdpFiles sudah punya OrganizationUnitId, tabel KkjBagians sudah di-drop</done>
</task>

</tasks>

<verification>
1. `dotnet build` sukses tanpa error
2. `dotnet ef database update` sukses tanpa error
3. Query manual: `SELECT COUNT(*) FROM OrganizationUnits` = 21
4. Query manual: `SELECT COUNT(*) FROM OrganizationUnits WHERE ParentId IS NULL` = 4 (Bagian)
5. Query manual: `SELECT COUNT(*) FROM OrganizationUnits WHERE ParentId IS NOT NULL` = 17 (Unit)
6. Tabel KkjBagians tidak ada lagi
7. KkjFiles dan CpdpFiles punya kolom OrganizationUnitId (NOT NULL)
</verification>

<success_criteria>
- Entity OrganizationUnit ada di database dengan hierarki parent-child yang benar (4 Bagian, 17 Unit)
- KkjFile dan CpdpFile memiliki FK OrganizationUnitId ke OrganizationUnits
- Entity KkjBagian dihapus dari codebase dan tabel KkjBagians di-drop dari database
- Aplikasi build dan run tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/219-db-model-migration/219-01-SUMMARY.md`
</output>
