---
phase: 221-integrasi-codebase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Data/ApplicationDbContext.cs
  - Controllers/AdminController.cs
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements: [INT-01, INT-03, INT-05]

must_haves:
  truths:
    - "Helper methods GetAllSectionsAsync, GetUnitsForSectionAsync, GetSectionUnitsDictAsync tersedia di ApplicationDbContext"
    - "AdminController tidak lagi mereferensi OrganizationStructure — semua dropdown Bagian/Unit dari DB"
    - "Validasi Section/Unit saat create/edit worker mengecek terhadap OrganizationUnit aktif"
    - "RecordsTeam.cshtml mengambil SectionUnits dari ViewBag (bukan static class)"
  artifacts:
    - path: "Data/ApplicationDbContext.cs"
      provides: "3 helper methods untuk query OrganizationUnit"
      contains: "GetAllSectionsAsync"
    - path: "Controllers/AdminController.cs"
      provides: "Semua referensi OrganizationStructure diganti ke DB query"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "await _context.GetAllSectionsAsync()"
      pattern: "_context\\.Get(AllSections|UnitsForSection|SectionUnitsDict)Async"
---

<objective>
Tambahkan helper methods ke ApplicationDbContext dan ganti semua referensi OrganizationStructure di AdminController + RecordsTeam view ke database query.

Purpose: AdminController punya ~11 referensi OrganizationStructure (terbanyak) — menyelesaikan ini duluan membuka jalan untuk controller lain. Helper methods yang dibuat di sini akan dipakai oleh Plan 02.
Output: 3 helper methods di DbContext, AdminController bebas dari OrganizationStructure, RecordsTeam view pakai ViewBag.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-integrasi-codebase/221-CONTEXT.md
@.planning/phases/221-integrasi-codebase/221-RESEARCH.md

<interfaces>
<!-- OrganizationUnit entity dari Phase 219 -->
From Models/OrganizationUnit.cs:
```csharp
public class OrganizationUnit
{
    public int Id { get; set; }
    public string Name { get; set; }
    public int? ParentId { get; set; }
    public int Level { get; set; }
    public int DisplayOrder { get; set; }
    public bool IsActive { get; set; }
    public OrganizationUnit? Parent { get; set; }
    public ICollection<OrganizationUnit> Children { get; set; }
}
```

From Models/OrganizationStructure.cs (yang akan diganti):
```csharp
public static List<string> GetAllSections() // returns Bagian names
public static List<string> GetUnitsForSection(string section) // returns Unit names for a Bagian
public static Dictionary<string, List<string>> SectionUnits // property: Bagian -> List<Unit>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper methods di ApplicationDbContext + AdminController integrasi</name>
  <files>Data/ApplicationDbContext.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Data/ApplicationDbContext.cs
    - Controllers/AdminController.cs
    - Models/OrganizationStructure.cs
    - Models/OrganizationUnit.cs
  </read_first>
  <action>
**Langkah A — Tambah 3 helper methods di ApplicationDbContext:**

```csharp
public async Task<List<string>> GetAllSectionsAsync()
{
    return await OrganizationUnits
        .Where(u => u.ParentId == null && u.IsActive)
        .OrderBy(u => u.DisplayOrder)
        .Select(u => u.Name)
        .ToListAsync();
}

public async Task<List<string>> GetUnitsForSectionAsync(string sectionName)
{
    var parent = await OrganizationUnits
        .FirstOrDefaultAsync(u => u.ParentId == null && u.Name == sectionName && u.IsActive);
    if (parent == null) return new List<string>();
    return await OrganizationUnits
        .Where(u => u.ParentId == parent.Id && u.IsActive)
        .OrderBy(u => u.DisplayOrder)
        .Select(u => u.Name)
        .ToListAsync();
}

public async Task<Dictionary<string, List<string>>> GetSectionUnitsDictAsync()
{
    var bagians = await OrganizationUnits
        .Where(u => u.ParentId == null && u.IsActive)
        .OrderBy(u => u.DisplayOrder)
        .ToListAsync();
    var bagianIds = bagians.Select(b => b.Id).ToList();
    var units = await OrganizationUnits
        .Where(u => u.ParentId != null && bagianIds.Contains(u.ParentId!.Value) && u.IsActive)
        .OrderBy(u => u.DisplayOrder)
        .ToListAsync();
    return bagians.ToDictionary(
        b => b.Name,
        b => units.Where(u => u.ParentId == b.Id).Select(u => u.Name).ToList()
    );
}
```

**Langkah B — Ganti semua referensi OrganizationStructure di AdminController.cs:**

Berdasarkan research, ada ~11 referensi. Ganti setiap `OrganizationStructure.GetAllSections()` dengan `await _context.GetAllSectionsAsync()`, `OrganizationStructure.GetUnitsForSection(x)` dengan `await _context.GetUnitsForSectionAsync(x)`, dan `OrganizationStructure.SectionUnits` dengan `await _context.GetSectionUnitsDictAsync()`.

Lokasi spesifik (dari research):
- Line ~968: CreateAssessment GET — `GetAllSections()`
- Line ~1234: CreateAssessment POST validation error — `GetAllSections()`
- Line ~1312: CreateAssessment POST renewal path — `GetAllSections()`
- Line ~1546: EditAssessment POST — `GetAllSections()`
- Line ~1621: EditAssessment GET — `GetAllSections()`
- Line ~3629-3630: CoachingProton — `GetAllSections()` + `GetSectionUnitsDictAsync()`
- Line ~4374: ExportWorkers validation — `GetUnitsForSectionAsync()`
- Line ~4442-4444: ManageWorkers — `GetAllSections()` + `GetUnitsForSectionAsync()`
- Line ~4458: ExportWorkers validation — `GetUnitsForSectionAsync()`
- Line ~7187: RenewalCertificate — `GetAllSections()`

Tambahkan validasi server-side di AddWorker/EditWorker POST actions (D-07, D-08):
```csharp
var validSections = await _context.GetAllSectionsAsync();
if (!string.IsNullOrEmpty(model.Section) && !validSections.Contains(model.Section))
{
    ModelState.AddModelError("Section", "Bagian tidak valid.");
}
var validUnits = await _context.GetUnitsForSectionAsync(model.Section ?? "");
if (!string.IsNullOrEmpty(model.Unit) && !validUnits.Contains(model.Unit))
{
    ModelState.AddModelError("Unit", "Unit tidak valid.");
}
```

Untuk setiap action yang mengirim data dropdown ke view, serialize ke ViewBag:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
ViewBag.Sections = sectionUnitsDict.Keys.ToList();
```

Hapus semua `using HcPortal.Models` yang HANYA dipakai untuk OrganizationStructure (cek apakah ada penggunaan model lain sebelum hapus).

**Langkah C — Verifikasi INT-05 (DokumenKkj):**
Research menunjukkan DokumenKkj di AdminController SUDAH pakai `_context.OrganizationUnits` sejak Phase 219. Verifikasi ini benar dengan membaca method DokumenKkj. Jika sudah benar, tidak perlu perubahan. Jika masih ada referensi OrganizationStructure, ganti.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "OrganizationStructure" Controllers/AdminController.cs | head -20; echo "---"; grep -n "GetAllSectionsAsync\|GetUnitsForSectionAsync\|GetSectionUnitsDictAsync" Data/ApplicationDbContext.cs | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OrganizationStructure" Controllers/AdminController.cs` returns 0
    - `grep -c "GetAllSectionsAsync" Data/ApplicationDbContext.cs` returns at least 1
    - `grep -c "GetUnitsForSectionAsync" Data/ApplicationDbContext.cs` returns at least 1
    - `grep -c "GetSectionUnitsDictAsync" Data/ApplicationDbContext.cs` returns at least 1
    - `dotnet build` compiles without errors
  </acceptance_criteria>
  <done>AdminController tidak mereferensi OrganizationStructure, 3 helper methods tersedia di DbContext, validasi server-side Section/Unit aktif</done>
</task>

<task type="auto">
  <name>Task 2: RecordsTeam view + CMPController verifikasi</name>
  <files>Views/CMP/RecordsTeam.cshtml, Controllers/CMPController.cs</files>
  <read_first>
    - Views/CMP/RecordsTeam.cshtml
    - Controllers/CMPController.cs
  </read_first>
  <action>
**Langkah A — CMPController.cs:**
Research menunjukkan grep tidak menemukan referensi `OrganizationStructure` di CMPController. Verifikasi ini benar. Jika ada referensi, ganti ke helper methods. Jika CMPController sudah mengirim SectionUnits via ViewBag untuk RecordsTeam, pastikan data diambil dari DB.

**Langkah B — RecordsTeam.cshtml:**
View ini sudah pakai pattern embedded JSON + JS cascade. Cek code-block `@{ }` di atas — jika ada referensi `OrganizationStructure.SectionUnits` untuk serialize ke JSON, ini harus dipindah ke controller.

Jika RecordsTeam code-block punya:
```csharp
@{
    var sectionUnits = HcPortal.Models.OrganizationStructure.SectionUnits;
    var sectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnits);
}
```

Ganti ke:
```csharp
@{
    var sectionUnitsJson = ViewBag.SectionUnitsJson as string ?? "{}";
}
```

Dan pastikan CMPController action yang merender RecordsTeam sudah mengirim `ViewBag.SectionUnitsJson`:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
```

Hapus `@using HcPortal.Models` jika hanya dipakai untuk OrganizationStructure.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "OrganizationStructure" Views/CMP/RecordsTeam.cshtml Controllers/CMPController.cs 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OrganizationStructure" Views/CMP/RecordsTeam.cshtml` returns 0
    - `grep -c "OrganizationStructure" Controllers/CMPController.cs` returns 0
    - RecordsTeam.cshtml contains `ViewBag.SectionUnitsJson` (grep confirms)
    - `dotnet build` compiles without errors
  </acceptance_criteria>
  <done>RecordsTeam view mengambil SectionUnits dari ViewBag (controller query DB), CMPController verified bebas dari OrganizationStructure</done>
</task>

</tasks>

<verification>
```bash
# Verifikasi tidak ada OrganizationStructure tersisa di file yang dimodifikasi
grep -rn "OrganizationStructure" Controllers/AdminController.cs Controllers/CMPController.cs Views/CMP/RecordsTeam.cshtml

# Verifikasi helper methods ada
grep -c "GetAllSectionsAsync\|GetUnitsForSectionAsync\|GetSectionUnitsDictAsync" Data/ApplicationDbContext.cs

# Build test
dotnet build
```
</verification>

<success_criteria>
- AdminController.cs: 0 referensi OrganizationStructure
- CMPController.cs: 0 referensi OrganizationStructure
- RecordsTeam.cshtml: 0 referensi OrganizationStructure
- ApplicationDbContext.cs: 3 helper methods tersedia
- `dotnet build` sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/221-integrasi-codebase/221-01-SUMMARY.md`
</output>
