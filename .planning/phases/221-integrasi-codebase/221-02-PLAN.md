---
phase: 221-integrasi-codebase
plan: 02
type: execute
wave: 2
depends_on: ["221-01"]
files_modified:
  - Controllers/CDPController.cs
  - Controllers/ProtonDataController.cs
  - Views/CDP/PlanIdp.cshtml
  - Views/CDP/HistoriProton.cshtml
  - Views/ProtonData/Index.cshtml
  - Views/ProtonData/Override.cshtml
autonomous: true
requirements: [INT-01, INT-02, INT-04, INT-06]

must_haves:
  truths:
    - "CDPController tidak lagi mereferensi OrganizationStructure — semua dropdown dari DB"
    - "ProtonDataController tidak lagi mereferensi OrganizationStructure"
    - "Cascade dropdown Bagian->Unit berfungsi di semua view CDP dan ProtonData"
    - "L4/L5 section locking tetap berfungsi dengan graceful fallback"
    - "Semua views pakai JS populate dari embedded JSON (bukan Razor foreach loop)"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Semua referensi OrganizationStructure diganti ke DB query"
    - path: "Controllers/ProtonDataController.cs"
      provides: "Referensi OrganizationStructure diganti ke DB query"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "await _context.GetSectionUnitsDictAsync()"
      pattern: "_context\\.Get(AllSections|UnitsForSection|SectionUnitsDict)Async"
    - from: "Views/CDP/PlanIdp.cshtml"
      to: "Controllers/CDPController.cs"
      via: "ViewBag.SectionUnitsJson"
      pattern: "ViewBag\\.SectionUnitsJson"
---

<objective>
Ganti semua referensi OrganizationStructure di CDPController, ProtonDataController, dan views terkait (PlanIdp, HistoriProton, ProtonData/Index, ProtonData/Override) ke database query via helper methods.

Purpose: Menyelesaikan integrasi codebase — setelah plan ini, tidak ada controller/view yang mereferensi OrganizationStructure.
Output: 2 controller dan 4 view bebas dari OrganizationStructure, cascade dropdown berfungsi, L4/L5 locking tetap aman.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/221-integrasi-codebase/221-CONTEXT.md
@.planning/phases/221-integrasi-codebase/221-RESEARCH.md
@.planning/phases/221-integrasi-codebase/221-01-SUMMARY.md

<interfaces>
<!-- Helper methods dari Plan 01 -->
From Data/ApplicationDbContext.cs:
```csharp
public async Task<List<string>> GetAllSectionsAsync();
public async Task<List<string>> GetUnitsForSectionAsync(string sectionName);
public async Task<Dictionary<string, List<string>>> GetSectionUnitsDictAsync();
```

<!-- Pattern ViewBag yang sudah diterapkan di Plan 01 -->
Controller pattern:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
ViewBag.Sections = sectionUnitsDict.Keys.ToList();
```

View JS populate pattern (dari RecordsTeam — sudah berfungsi):
```javascript
var sectionUnits = @Html.Raw(ViewBag.SectionUnitsJson);
document.getElementById('sectionFilter').addEventListener('change', function() {
    var selected = this.value;
    var unitSelect = document.getElementById('unitFilter');
    unitSelect.innerHTML = '<option value="">Semua Unit</option>';
    if (selected && sectionUnits[selected]) {
        sectionUnits[selected].forEach(function(unit) {
            var opt = document.createElement('option');
            opt.value = unit;
            opt.textContent = unit;
            unitSelect.appendChild(opt);
        });
    }
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDPController + CDP views integrasi</name>
  <files>Controllers/CDPController.cs, Views/CDP/PlanIdp.cshtml, Views/CDP/HistoriProton.cshtml</files>
  <read_first>
    - Controllers/CDPController.cs
    - Views/CDP/PlanIdp.cshtml
    - Views/CDP/HistoriProton.cshtml
    - Data/ApplicationDbContext.cs
  </read_first>
  <action>
**Langkah A — CDPController.cs (~12 referensi OrganizationStructure):**

Ganti setiap referensi sesuai peta dari research:

1. Line ~206 (HistoriProton): Ganti `OrganizationStructure.SectionUnits` ke `await _context.GetSectionUnitsDictAsync()`, serialize ke `ViewBag.OrgStructureJson`
2. Line ~290 (GetUnitsForSection endpoint): Ganti `OrganizationStructure.GetUnitsForSection(section)` ke `await _context.GetUnitsForSectionAsync(section)`
3. Line ~556-559 (CoachingProton section locking L4/L5):
   ```csharp
   var availableSections = await _context.GetAllSectionsAsync();
   var availableUnits = !string.IsNullOrEmpty(effectiveSection)
       ? await _context.GetUnitsForSectionAsync(effectiveSection)
       : new List<string>();
   // Graceful fallback: jika availableSections kosong, tampilkan semua (jangan block user)
   ```
4. Line ~1317,1343 (CoachingProton validation): Ganti ke async versions
5. Line ~1601,1604,1609 (Deliverable): Ganti ke async versions
6. Line ~2760,2768 (Dashboard): Ganti ke async versions
7. Line ~3072 (PlanIdp): Ganti `OrganizationStructure.GetAllSections()` ke `await _context.GetAllSectionsAsync()`, dan kirim `ViewBag.SectionUnitsJson` untuk cascade

Pastikan setiap action yang mengirim data Bagian/Unit ke view juga mengirim `ViewBag.SectionUnitsJson`:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
```

Hapus `using HcPortal.Models;` jika hanya dipakai untuk OrganizationStructure (cek penggunaan lain dulu).

**Langkah B — PlanIdp.cshtml:**
View ini pakai Razor `@foreach` loop untuk dropdown Bagian/Unit. Refactor ke JS populate:

Hapus Razor `@foreach` loop yang generate `<option>` untuk Bagian dropdown. Ganti dengan:
```html
<select id="sectionFilter">
    <option value="">Semua Bagian</option>
</select>
<select id="unitFilter" disabled>
    <option value="">Semua Unit</option>
</select>
```

Tambah JS di section Scripts:
```javascript
var sectionUnits = @Html.Raw(ViewBag.SectionUnitsJson ?? "{}");

// Populate Bagian dropdown
Object.keys(sectionUnits).forEach(function(section) {
    var opt = document.createElement('option');
    opt.value = section;
    opt.textContent = section;
    document.getElementById('sectionFilter').appendChild(opt);
});

// Cascade Unit
document.getElementById('sectionFilter').addEventListener('change', function() {
    var selected = this.value;
    var unitSelect = document.getElementById('unitFilter');
    unitSelect.innerHTML = '<option value="">Semua Unit</option>';
    unitSelect.disabled = !selected;
    if (selected && sectionUnits[selected]) {
        sectionUnits[selected].forEach(function(unit) {
            var opt = document.createElement('option');
            opt.value = unit;
            opt.textContent = unit;
            unitSelect.appendChild(opt);
        });
    }
});
```

Catatan: Sesuaikan element ID dan logic filter yang ada di view saat ini — baca view dulu untuk melihat ID dropdown dan mekanisme filter yang sudah ada.

**Langkah C — HistoriProton.cshtml:**
View ini SUDAH pakai embedded JSON pattern (`var sectionUnits = @Html.Raw(orgStructureJson)`). Cek apakah source `orgStructureJson` sudah dari ViewBag (bukan dari code-block yang referensi OrganizationStructure). Jika ada code-block referensi, hapus dan ganti ke ViewBag. Controller sudah diubah di Langkah A.

Hapus `@using HcPortal.Models` jika hanya dipakai untuk OrganizationStructure.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "OrganizationStructure" Controllers/CDPController.cs Views/CDP/PlanIdp.cshtml Views/CDP/HistoriProton.cshtml 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OrganizationStructure" Controllers/CDPController.cs` returns 0
    - `grep -c "OrganizationStructure" Views/CDP/PlanIdp.cshtml` returns 0
    - `grep -c "OrganizationStructure" Views/CDP/HistoriProton.cshtml` returns 0
    - PlanIdp.cshtml contains `ViewBag.SectionUnitsJson` (JS populate, bukan Razor foreach)
    - `dotnet build` compiles without errors
  </acceptance_criteria>
  <done>CDPController dan kedua view CDP bebas dari OrganizationStructure, cascade dropdown berfungsi via JS populate, L4/L5 locking tetap aktif dengan graceful fallback</done>
</task>

<task type="auto">
  <name>Task 2: ProtonDataController + ProtonData views integrasi</name>
  <files>Controllers/ProtonDataController.cs, Views/ProtonData/Index.cshtml, Views/ProtonData/Override.cshtml</files>
  <read_first>
    - Controllers/ProtonDataController.cs
    - Views/ProtonData/Index.cshtml
    - Views/ProtonData/Override.cshtml
    - Data/ApplicationDbContext.cs
  </read_first>
  <action>
**Langkah A — ProtonDataController.cs:**
Research menunjukkan 1 referensi di line ~120 (Index action): `OrganizationStructure.SectionUnits` dalam foreach loop untuk grid status.

Ganti ke:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
// Gunakan sectionUnitsDict dalam foreach loop yang ada (menggantikan OrganizationStructure.SectionUnits)
```

Juga kirim ke ViewBag untuk views:
```csharp
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
ViewBag.Sections = sectionUnitsDict.Keys.ToList();
```

Cek action Override juga — jika ada referensi OrganizationStructure, ganti ke helper methods.

Hapus `using HcPortal.Models;` jika hanya dipakai untuk OrganizationStructure.

**Langkah B — ProtonData/Index.cshtml:**
View ini pakai Razor `@foreach` loop untuk Bagian/Unit dropdown (2 tempat per research). Refactor ke JS populate dari `ViewBag.SectionUnitsJson`:

Hapus Razor `@foreach` loops. Ganti ke JS populate pattern (sama seperti PlanIdp di Task 1). Sesuaikan element ID dengan yang ada di view saat ini.

**Langkah C — ProtonData/Override.cshtml:**
View ini pakai Razor `@foreach` + JSON serialize. Refactor:
- Hapus code-block yang serialize OrganizationStructure ke JSON
- Ganti ke `var sectionUnits = @Html.Raw(ViewBag.SectionUnitsJson ?? "{}");`
- Hapus Razor `@foreach` loops, ganti ke JS populate

Hapus `@using HcPortal.Models` dari kedua views jika hanya dipakai untuk OrganizationStructure.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "OrganizationStructure" Controllers/ProtonDataController.cs Views/ProtonData/Index.cshtml Views/ProtonData/Override.cshtml 2>/dev/null; echo "Exit: $?"; echo "---"; dotnet build --nologo -v q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OrganizationStructure" Controllers/ProtonDataController.cs` returns 0
    - `grep -c "OrganizationStructure" Views/ProtonData/Index.cshtml` returns 0
    - `grep -c "OrganizationStructure" Views/ProtonData/Override.cshtml` returns 0
    - Both views contain `ViewBag.SectionUnitsJson` (JS populate pattern)
    - `dotnet build` compiles without errors
  </acceptance_criteria>
  <done>ProtonDataController dan kedua view ProtonData bebas dari OrganizationStructure, dropdown Bagian/Unit dari database, cascade berfungsi via JS</done>
</task>

</tasks>

<verification>
```bash
# Final check — tidak ada OrganizationStructure tersisa di semua file yang dimodifikasi phase ini
grep -rn "OrganizationStructure" Controllers/CDPController.cs Controllers/ProtonDataController.cs Views/CDP/ Views/ProtonData/

# Build test
dotnet build
```
</verification>

<success_criteria>
- CDPController.cs: 0 referensi OrganizationStructure
- ProtonDataController.cs: 0 referensi OrganizationStructure
- Semua 4 views: 0 referensi OrganizationStructure, pakai JS populate dari ViewBag.SectionUnitsJson
- `dotnet build` sukses
</success_criteria>

<output>
After completion, create `.planning/phases/221-integrasi-codebase/221-02-SUMMARY.md`
</output>
