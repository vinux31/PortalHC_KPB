---
phase: 221-integrasi-codebase
plan: 03
type: execute
wave: 1
depends_on: ["221-01", "221-02"]
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/CreateWorker.cshtml
  - Views/Admin/EditWorker.cshtml
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, INT-06]
gap_closure: true

must_haves:
  truths:
    - "CreateWorker dan EditWorker dropdown Bagian/Unit mengambil data dari DB OrganizationUnits"
    - "Validasi server-side menolak Section/Unit yang tidak ada di OrganizationUnit aktif"
    - "Dropdown Bagian di-populate via JS dari SectionUnitsJson, bukan hardcoded HTML options"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "CreateWorker/EditWorker GET set ViewBag.SectionUnitsJson, POST validate Section/Unit"
      contains: "GetSectionUnitsDictAsync"
    - path: "Views/Admin/CreateWorker.cshtml"
      provides: "Dynamic dropdown dari ViewBag"
      contains: "ViewBag.SectionUnitsJson"
    - path: "Views/Admin/EditWorker.cshtml"
      provides: "Dynamic dropdown dari ViewBag"
      contains: "ViewBag.SectionUnitsJson"
  key_links:
    - from: "Controllers/AdminController.cs (CreateWorker GET)"
      to: "ApplicationDbContext.GetSectionUnitsDictAsync()"
      via: "ViewBag.SectionUnitsJson"
      pattern: "GetSectionUnitsDictAsync"
    - from: "Controllers/AdminController.cs (EditWorker GET)"
      to: "ApplicationDbContext.GetSectionUnitsDictAsync()"
      via: "ViewBag.SectionUnitsJson"
      pattern: "GetSectionUnitsDictAsync"
    - from: "Controllers/AdminController.cs (CreateWorker POST)"
      to: "ApplicationDbContext.GetAllSectionsAsync()"
      via: "server-side validation"
      pattern: "validSections.*Contains.*Section"
---

<objective>
Menutup 2 gap dari VERIFICATION.md Phase 221: (1) CreateWorker/EditWorker view masih hardcoded dropdown, (2) tidak ada validasi server-side Section/Unit di POST actions.

Purpose: Semua form create/edit worker mengambil data Bagian/Unit dari database OrganizationUnits, konsisten dengan seluruh portal.
Output: Controller actions dan views yang terintegrasi penuh dengan DB.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-integrasi-codebase/221-01-SUMMARY.md
@.planning/phases/221-integrasi-codebase/221-VERIFICATION.md

<interfaces>
From Data/ApplicationDbContext.cs:
```csharp
public async Task<List<string>> GetAllSectionsAsync()
// Returns list of active section names from OrganizationUnits where Level == "Bagian"

public async Task<List<string>> GetUnitsForSectionAsync(string sectionName)
// Returns list of active unit names under given section

public async Task<Dictionary<string, List<string>>> GetSectionUnitsDictAsync()
// Returns { "RFCC": ["unit1", "unit2"], "DHT / HMU": [...] } from DB
```

Existing pattern in AdminController.cs line 3629-3632 (ManageWorkers action):
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
```

Existing pattern in Views/CMP/RecordsTeam.cshtml line 13:
```csharp
var sectionUnitsJson = ViewBag.SectionUnitsJson as string ?? "{}";
```
Then in JS: `const sectionUnits = @Html.Raw(sectionUnitsJson);`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller — ViewBag + Validasi Server-Side</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 4519-4530 CreateWorker GET, lines 4535-4610 CreateWorker POST, lines 4614-4641 EditWorker GET, lines 4643-4770 EditWorker POST)
    - Data/ApplicationDbContext.cs (lines 86-120 helper methods)
  </read_first>
  <action>
Ubah 4 action di AdminController.cs:

**A. CreateWorker GET (line 4522):**
- Ubah signature dari `public IActionResult CreateWorker()` menjadi `public async Task<IActionResult> CreateWorker()`
- Tambah sebelum `return View(model)`:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
```

**B. EditWorker GET (line 4617):**
- Tambah sebelum `return View(model)` (line 4640):
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDict);
```

**C. CreateWorker POST (line 4535):**
- Tambah validasi Section/Unit SEBELUM blok `if (!ModelState.IsValid)` (setelah line 4542):
```csharp
// Validate Section/Unit against active OrganizationUnits in DB
if (!string.IsNullOrEmpty(model.Section))
{
    var validSections = await _context.GetAllSectionsAsync();
    if (!validSections.Contains(model.Section))
    {
        ModelState.AddModelError("Section", $"Bagian '{model.Section}' tidak ditemukan di data organisasi");
    }
    else if (!string.IsNullOrEmpty(model.Unit))
    {
        var validUnits = await _context.GetUnitsForSectionAsync(model.Section);
        if (!validUnits.Contains(model.Unit))
        {
            ModelState.AddModelError("Unit", $"Unit '{model.Unit}' tidak valid untuk bagian '{model.Section}'");
        }
    }
}
```
- Di dalam blok `if (!ModelState.IsValid)` (line 4544-4548), tambah re-populate ViewBag SEBELUM `return View(model)`:
```csharp
var sectionUnitsDictErr = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDictErr);
```

**D. EditWorker POST (line 4647):**
- Tambah validasi Section/Unit yang sama SEBELUM blok `if (!ModelState.IsValid)` (setelah line 4656):
```csharp
// Validate Section/Unit against active OrganizationUnits in DB
if (!string.IsNullOrEmpty(model.Section))
{
    var validSections = await _context.GetAllSectionsAsync();
    if (!validSections.Contains(model.Section))
    {
        ModelState.AddModelError("Section", $"Bagian '{model.Section}' tidak ditemukan di data organisasi");
    }
    else if (!string.IsNullOrEmpty(model.Unit))
    {
        var validUnits = await _context.GetUnitsForSectionAsync(model.Section);
        if (!validUnits.Contains(model.Unit))
        {
            ModelState.AddModelError("Unit", $"Unit '{model.Unit}' tidak valid untuk bagian '{model.Section}'");
        }
    }
}
```
- Di dalam blok `if (!ModelState.IsValid)` (line 4658-4663), tambah re-populate ViewBag SEBELUM `return View(model)`:
```csharp
var sectionUnitsDictErr = await _context.GetSectionUnitsDictAsync();
ViewBag.SectionUnitsJson = System.Text.Json.JsonSerializer.Serialize(sectionUnitsDictErr);
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -n "GetSectionUnitsDictAsync" Controllers/AdminController.cs | grep -E "CreateWorker|EditWorker" -A2 && grep -c "validSections.Contains" Controllers/AdminController.cs && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GetSectionUnitsDictAsync" Controllers/AdminController.cs` returns >= 5 (was 1, now +4 calls in CreateWorker GET/POST error, EditWorker GET/POST error)
    - `grep -c "validSections.Contains" Controllers/AdminController.cs` returns 2 (CreateWorker POST + EditWorker POST)
    - `grep "async Task.*CreateWorker\b" Controllers/AdminController.cs` matches the GET action (was sync IActionResult)
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>CreateWorker/EditWorker GET actions set ViewBag.SectionUnitsJson dari DB. POST actions validasi Section/Unit terhadap OrganizationUnit aktif dan re-populate ViewBag saat validation error.</done>
</task>

<task type="auto">
  <name>Task 2: Views — Replace Hardcoded Dropdown dengan DB Data</name>
  <files>Views/Admin/CreateWorker.cshtml, Views/Admin/EditWorker.cshtml</files>
  <read_first>
    - Views/Admin/CreateWorker.cshtml (lines 114-127 dropdown HTML, lines 198-265 JS section)
    - Views/Admin/EditWorker.cshtml (lines 118-132 dropdown HTML, lines 210-270 JS section)
  </read_first>
  <action>
**A. CreateWorker.cshtml:**

1. Ganti hardcoded Bagian dropdown (lines 115-121):
```html
<select asp-for="Section" class="form-select" id="sectionSelect">
    <option value="">-- Pilih Bagian --</option>
</select>
```
Hapus 4 `<option value="RFCC">` dst. Options akan di-populate oleh JS.

2. Ganti hardcoded JS `const sectionUnits = { "RFCC": [...] }` (lines 200-226) dengan:
```javascript
const sectionUnits = @Html.Raw(ViewBag.SectionUnitsJson ?? "{}");
```

3. Tambah JS untuk populate Bagian dropdown dari sectionUnits keys. Setelah `const sectionSelect = ...` dan `const unitSelect = ...`:
```javascript
// Populate Bagian dropdown dari DB
Object.keys(sectionUnits).forEach(section => {
    const opt = document.createElement('option');
    opt.value = section;
    opt.textContent = section;
    sectionSelect.appendChild(opt);
});
```

4. Setelah populate Bagian, jika `currentUnit` ada, set selected value:
```javascript
// Restore selected section if editing/returning from validation error
const currentSection = "@(Model.Section ?? "")";
if (currentSection) {
    sectionSelect.value = currentSection;
    updateUnits(currentSection);
}
```

**B. EditWorker.cshtml:**

Terapkan perubahan yang sama persis seperti CreateWorker.cshtml:
1. Hapus hardcoded `<option>` dari Bagian dropdown (sisakan hanya placeholder `-- Pilih Bagian --`)
2. Ganti hardcoded `const sectionUnits = {...}` dengan `@Html.Raw(ViewBag.SectionUnitsJson ?? "{}")`
3. Tambah JS populate Bagian dropdown dari keys
4. Restore selected section/unit (EditWorker sudah punya `currentUnit` logic, pastikan `currentSection` juga dihandle untuk pre-select saat load)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && echo "=== CreateWorker ===" && grep -c "RFCC" Views/Admin/CreateWorker.cshtml && grep -c "SectionUnitsJson" Views/Admin/CreateWorker.cshtml && echo "=== EditWorker ===" && grep -c "RFCC" Views/Admin/EditWorker.cshtml && grep -c "SectionUnitsJson" Views/Admin/EditWorker.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RFCC" Views/Admin/CreateWorker.cshtml` returns 0 (semua hardcoded data dihapus)
    - `grep -c "RFCC" Views/Admin/EditWorker.cshtml` returns 0
    - `grep -c "SectionUnitsJson" Views/Admin/CreateWorker.cshtml` returns >= 1
    - `grep -c "SectionUnitsJson" Views/Admin/EditWorker.cshtml` returns >= 1
    - `grep "Object.keys(sectionUnits)" Views/Admin/CreateWorker.cshtml` matches (JS populate Bagian)
    - `grep "Object.keys(sectionUnits)" Views/Admin/EditWorker.cshtml` matches
    - `dotnet build --no-restore` succeeds
  </acceptance_criteria>
  <done>Dropdown Bagian dan Unit di CreateWorker dan EditWorker sepenuhnya dari database. Tidak ada hardcoded section/unit data tersisa di kedua view.</done>
</task>

</tasks>

<verification>
1. `dotnet build` berhasil tanpa error
2. `grep -rn "RFCC" Views/Admin/CreateWorker.cshtml Views/Admin/EditWorker.cshtml` returns 0 matches
3. `grep -c "GetSectionUnitsDictAsync" Controllers/AdminController.cs` >= 5
4. `grep -c "validSections.Contains" Controllers/AdminController.cs` == 2
5. `grep "SectionUnitsJson" Views/Admin/CreateWorker.cshtml Views/Admin/EditWorker.cshtml` returns matches di kedua file
</verification>

<success_criteria>
- Semua dropdown Bagian/Unit di CreateWorker dan EditWorker mengambil data dari DB OrganizationUnits via ViewBag.SectionUnitsJson
- Server-side validation menolak Section/Unit yang tidak ada di OrganizationUnit aktif
- Tidak ada hardcoded section/unit data (RFCC, DHT/HMU, NGP, GAST) di kedua view
- Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/221-integrasi-codebase/221-03-SUMMARY.md`
</output>
