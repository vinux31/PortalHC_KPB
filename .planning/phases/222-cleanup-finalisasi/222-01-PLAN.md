---
phase: 222-cleanup-finalisasi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/OrganizationStructure.cs
  - Controllers/CMPController.cs
  - Views/CMP/RecordsTeam.cshtml
  - Views/CDP/HistoriProton.cshtml
  - Data/SeedData.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [INT-07, CLN-01, CLN-02]

must_haves:
  truths:
    - "File OrganizationStructure.cs tidak ada di codebase"
    - "Aplikasi compile tanpa error setelah file dihapus"
    - "SeedData memastikan OrganizationUnits exist di database"
    - "ImportWorkers menolak baris dengan Section/Unit yang tidak ada di OrganizationUnit"
  artifacts:
    - path: "Data/SeedData.cs"
      provides: "Seed OrganizationUnits method"
      contains: "SeedOrganizationUnitsAsync"
    - path: "Controllers/AdminController.cs"
      provides: "ImportWorkers validasi Section/Unit terhadap DB"
      contains: "tidak ditemukan di database"
  key_links:
    - from: "Data/SeedData.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "DbSet OrganizationUnits query"
      pattern: "OrganizationUnits"
    - from: "Controllers/AdminController.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "GetSectionUnitsDictAsync untuk validasi"
      pattern: "GetSectionUnitsDictAsync"
---

<objective>
Cleanup akhir milestone v7.12: hapus static class OrganizationStructure.cs, tambah seed OrganizationUnits di SeedData.cs, dan tambah validasi Section/Unit di ImportWorkers terhadap database.

Purpose: Menyelesaikan migrasi dari static class ke database-driven OrganizationUnit
Output: Codebase bersih tanpa OrganizationStructure.cs, seed data lengkap, ImportWorkers tervalidasi
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-cleanup-finalisasi/222-CONTEXT.md

<interfaces>
<!-- From Data/ApplicationDbContext.cs -->
```csharp
// Line 107
public async Task<Dictionary<string, List<string>>> GetSectionUnitsDictAsync()
```

<!-- From Data/SeedData.cs — existing pattern -->
```csharp
public static class SeedData
{
    public static async Task InitializeAsync(IServiceProvider serviceProvider, IWebHostEnvironment environment)
    // Step 3: DeduplicateProtonTrackAssignments(context)
    // Step 4: MergeProtonCatalogDuplicates(context)
    // -> Add Step 5: SeedOrganizationUnitsAsync(context)
}
```

<!-- ImportWorkers existing error pattern (AdminController.cs ~line 5193) -->
```csharp
var errors = new List<string>();
if (string.IsNullOrWhiteSpace(nama)) errors.Add("Nama kosong");
// -> Add Section/Unit validation here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hapus OrganizationStructure.cs dan komentar referensi, tambah seed OrganizationUnits</name>
  <files>Models/OrganizationStructure.cs, Controllers/CMPController.cs, Views/CMP/RecordsTeam.cshtml, Views/CDP/HistoriProton.cshtml, Data/SeedData.cs</files>
  <read_first>
    - Models/OrganizationStructure.cs
    - Data/SeedData.cs
    - Controllers/CMPController.cs (line 440-450)
    - Views/CMP/RecordsTeam.cshtml (line 10-15)
    - Views/CDP/HistoriProton.cshtml (line 210-215)
  </read_first>
  <action>
1. DELETE file `Models/OrganizationStructure.cs` entirely.

2. Hapus komentar referensi OrganizationStructure di 3 file:
   - `Controllers/CMPController.cs` line 444: hapus baris `// Phase 221: SectionUnits dari DB (menggantikan OrganizationStructure static class)`
   - `Views/CMP/RecordsTeam.cshtml` line 12: hapus baris `// Phase 221: SectionUnits dari DB via ViewBag (menggantikan OrganizationStructure static class)`
   - `Views/CDP/HistoriProton.cshtml` line 212: hapus baris `// OrganizationStructure cascade data`

3. Di `Data/SeedData.cs`, tambah method baru `SeedOrganizationUnitsAsync`:
   ```csharp
   /// <summary>
   /// Ensures OrganizationUnits exist in DB (safety net for fresh deployment).
   /// Idempotent — skips if any OrganizationUnit rows already exist.
   /// </summary>
   public static async Task SeedOrganizationUnitsAsync(ApplicationDbContext context)
   {
       if (await context.OrganizationUnits.AnyAsync())
           return;

       var sections = new[]
       {
           new { Name = "RFCC", Order = 1, Units = new[] { "RFCC LPG Treating Unit (062)", "Propylene Recovery Unit (063)" } },
           new { Name = "DHT / HMU", Order = 2, Units = new[] { "Diesel Hydrotreating Unit I & II (054 & 083)", "Hydrogen Manufacturing Unit (068)", "Common DHT H2 Compressor (085)" } },
           new { Name = "NGP", Order = 3, Units = new[] { "Saturated Gas Concentration Unit (060)", "Saturated LPG Treating Unit (064)", "Isomerization Unit (082)", "Common Facilities For NLP (160)", "Naphtha Hydrotreating Unit II (084)" } },
           new { Name = "GAST", Order = 4, Units = new[] { "RFCC NHT (053)", "Alkylation Unit (065)", "Wet Gas Sulfuric Acid Unit (066)", "SWS RFCC & Non RFCC (067 & 167)", "Amine Regeneration Unit I & II (069 & 079)", "Flare System (319)", "Sulfur Recovery Unit (169)" } }
       };

       foreach (var section in sections)
       {
           var parent = new OrganizationUnit { Name = section.Name, Level = 1, DisplayOrder = section.Order, IsActive = true };
           context.OrganizationUnits.Add(parent);
           await context.SaveChangesAsync();

           int unitOrder = 1;
           foreach (var unitName in section.Units)
           {
               context.OrganizationUnits.Add(new OrganizationUnit
               {
                   Name = unitName, ParentId = parent.Id, Level = 2, DisplayOrder = unitOrder++, IsActive = true
               });
           }
           await context.SaveChangesAsync();
       }
   }
   ```

4. Di `InitializeAsync`, tambah call setelah step 4:
   ```csharp
   // 5. Seed OrganizationUnits (safety net for fresh deployment)
   await SeedOrganizationUnitsAsync(context);
   ```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File Models/OrganizationStructure.cs tidak ada (ls returns error)
    - grep -r "OrganizationStructure" pada file .cs dan .cshtml hanya mengembalikan hasil dari folder .planning (bukan source code)
    - SeedData.cs mengandung method "SeedOrganizationUnitsAsync"
    - SeedData.cs InitializeAsync memanggil "SeedOrganizationUnitsAsync"
    - dotnet build berhasil tanpa error
  </acceptance_criteria>
  <done>OrganizationStructure.cs terhapus, tidak ada referensi tersisa di source code, seed OrganizationUnits tersedia</done>
</task>

<task type="auto">
  <name>Task 2: Tambah validasi Section/Unit di ImportWorkers terhadap database OrganizationUnit</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (line 5129-5287)
    - Data/ApplicationDbContext.cs (line 107-130, GetSectionUnitsDictAsync method)
  </read_first>
  <action>
Di method `ImportWorkers(IFormFile? excelFile)` di `AdminController.cs`:

1. Sebelum loop `foreach (var row in ws.RowsUsed().Skip(1))` (sekitar line 5165), load section-units dictionary dari DB:
   ```csharp
   var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
   ```

2. Di dalam loop, setelah validasi existing (line 5197-5198 role validation), tambah validasi Section dan Unit SEBELUM block `if (errors.Any())`:
   ```csharp
   // Validasi Section terhadap OrganizationUnit database
   if (!string.IsNullOrWhiteSpace(bagian) && !sectionUnitsDict.ContainsKey(bagian))
       errors.Add($"Section '{bagian}' tidak ditemukan di database");

   // Validasi Unit: harus child dari Section yang dipilih
   if (!string.IsNullOrWhiteSpace(unit))
   {
       if (string.IsNullOrWhiteSpace(bagian))
           errors.Add("Unit tidak boleh diisi tanpa Section");
       else if (sectionUnitsDict.TryGetValue(bagian, out var validUnits) && !validUnits.Contains(unit))
           errors.Add($"Unit '{unit}' bukan child dari Section '{bagian}'");
   }
   ```

Catatan: Validasi hanya dilakukan jika field tidak kosong (Section dan Unit opsional di ImportWorkers).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "tidak ditemukan di database" Controllers/AdminController.cs mengembalikan hasil
    - grep "bukan child dari Section" Controllers/AdminController.cs mengembalikan hasil
    - grep "GetSectionUnitsDictAsync" Controllers/AdminController.cs mengembalikan hasil di method ImportWorkers
    - dotnet build berhasil tanpa error
  </acceptance_criteria>
  <done>ImportWorkers memvalidasi Section/Unit terhadap OrganizationUnit database, menolak baris dengan nilai tidak valid dengan pesan error yang jelas</done>
</task>

</tasks>

<verification>
1. `dotnet build` berhasil tanpa error atau warning terkait OrganizationStructure
2. File `Models/OrganizationStructure.cs` tidak ada
3. Tidak ada referensi `OrganizationStructure` di source code (.cs/.cshtml) selain folder .planning
4. `SeedData.cs` memiliki method `SeedOrganizationUnitsAsync` yang dipanggil dari `InitializeAsync`
5. `AdminController.cs` ImportWorkers memvalidasi Section/Unit terhadap database
</verification>

<success_criteria>
- Codebase compile clean tanpa OrganizationStructure.cs
- Seed data mencakup OrganizationUnits sebagai safety net
- ImportWorkers menolak Section/Unit yang tidak ada di database dengan pesan error yang informatif
</success_criteria>

<output>
After completion, create `.planning/phases/222-cleanup-finalisasi/222-01-SUMMARY.md`
</output>
