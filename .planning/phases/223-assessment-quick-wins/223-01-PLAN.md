---
phase: 223-assessment-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/SessionElemenTeknisScore.cs
  - Models/UserResponse.cs
  - Data/ApplicationDbContext.cs
  - Controllers/CMPController.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [AINT-01, AINT-04]
must_haves:
  truths:
    - "Setelah peserta submit exam (package path), skor per ElemenTeknis tersimpan di tabel SessionElemenTeknisScores"
    - "Setelah admin GradeFromSavedAnswers (package path), skor per ElemenTeknis tersimpan di tabel SessionElemenTeknisScores"
    - "Setelah SaveLegacyAnswer dipanggil, field SubmittedAt pada UserResponse terisi"
    - "Setelah SubmitExam legacy path, field SubmittedAt pada UserResponse terisi"
  artifacts:
    - path: "Models/SessionElemenTeknisScore.cs"
      provides: "Model ET score per session"
      contains: "public class SessionElemenTeknisScore"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet SessionElemenTeknisScores"
      contains: "DbSet<SessionElemenTeknisScore> SessionElemenTeknisScores"
    - path: "Models/UserResponse.cs"
      provides: "SubmittedAt field"
      contains: "public DateTime? SubmittedAt"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "SessionElemenTeknisScores"
      via: "SubmitExam package path — persist ET scores sebelum SaveChanges"
      pattern: "SessionElemenTeknisScores\\.Add"
    - from: "Controllers/AdminController.cs"
      to: "SessionElemenTeknisScores"
      via: "GradeFromSavedAnswers — persist ET scores"
      pattern: "SessionElemenTeknisScores\\.Add"
    - from: "Controllers/CMPController.cs"
      to: "UserResponse.SubmittedAt"
      via: "SaveLegacyAnswer dan SubmitExam legacy path"
      pattern: "SubmittedAt = DateTime\\.UtcNow"
---

<objective>
Persist skor ElemenTeknis per session ke database dan tambah timestamp SubmittedAt pada UserResponse.

Purpose: Data integritas assessment — skor ET tersimpan permanen untuk analytics Phase 224, dan legacy response punya audit trail waktu submit.
Output: Model baru SessionElemenTeknisScore, field baru UserResponse.SubmittedAt, EF migration, logic persist di SubmitExam dan GradeFromSavedAnswers.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-assessment-quick-wins/223-CONTEXT.md
@.planning/phases/223-assessment-quick-wins/223-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model + Migration — SessionElemenTeknisScore dan UserResponse.SubmittedAt</name>
  <files>Models/SessionElemenTeknisScore.cs, Models/UserResponse.cs, Data/ApplicationDbContext.cs</files>
  <read_first>
    - Models/UserResponse.cs (current state — perlu tambah SubmittedAt)
    - Models/AssessmentSession.cs (FK reference untuk SessionElemenTeknisScore)
    - Models/AssessmentPackage.cs (referensi PackageQuestion.ElemenTeknis field)
    - Data/ApplicationDbContext.cs (DbSet inventory — perlu tambah DbSet baru)
  </read_first>
  <action>
1. Buat file `Models/SessionElemenTeknisScore.cs`:
```csharp
using System.ComponentModel.DataAnnotations.Schema;

namespace PortalHC_KPB.Models;

public class SessionElemenTeknisScore
{
    public int Id { get; set; }

    public int AssessmentSessionId { get; set; }
    [ForeignKey("AssessmentSessionId")]
    public virtual AssessmentSession AssessmentSession { get; set; } = null!;

    /// <summary>Nama elemen teknis. "Lainnya" untuk soal tanpa tag ET.</summary>
    public string ElemenTeknis { get; set; } = "";

    public int CorrectCount { get; set; }
    public int QuestionCount { get; set; }
}
```

2. Tambah field di `Models/UserResponse.cs`:
```csharp
public DateTime? SubmittedAt { get; set; }
```

3. Tambah DbSet di `Data/ApplicationDbContext.cs`:
```csharp
public DbSet<SessionElemenTeknisScore> SessionElemenTeknisScores { get; set; }
```

4. Jalankan `dotnet ef migrations add AddSessionETScoreAndUserResponseSubmittedAt` — migration ini harus berisi:
   - CreateTable SessionElemenTeknisScores (Id, AssessmentSessionId FK, ElemenTeknis, CorrectCount, QuestionCount)
   - Unique index `IX_SessionElemenTeknisScores_AssessmentSessionId_ElemenTeknis` pada (AssessmentSessionId, ElemenTeknis)
   - AddColumn SubmittedAt (DateTime? nullable) ke UserResponses

5. Jika unique index tidak otomatis ter-generate oleh EF, tambahkan manual di migration:
```csharp
migrationBuilder.CreateIndex(
    name: "IX_SessionElemenTeknisScores_AssessmentSessionId_ElemenTeknis",
    table: "SessionElemenTeknisScores",
    columns: new[] { "AssessmentSessionId", "ElemenTeknis" },
    unique: true);
```

6. Jalankan `dotnet ef database update` untuk apply migration.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - File `Models/SessionElemenTeknisScore.cs` ada dan berisi `public class SessionElemenTeknisScore`
    - `Models/UserResponse.cs` berisi `public DateTime? SubmittedAt { get; set; }`
    - `Data/ApplicationDbContext.cs` berisi `DbSet<SessionElemenTeknisScore> SessionElemenTeknisScores`
    - Migration file terbuat di folder `Migrations/`
    - `dotnet build` exit code 0
  </acceptance_criteria>
  <done>Model SessionElemenTeknisScore terbuat dengan unique index, UserResponse punya SubmittedAt, migration applied, build green.</done>
</task>

<task type="auto">
  <name>Task 2: Persist ET Scores di SubmitExam dan GradeFromSavedAnswers + SubmittedAt di SaveLegacyAnswer</name>
  <files>Controllers/CMPController.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (SubmitExam ~line 1381 — package path grading loop; SaveLegacyAnswer ~line 327 — upsert; SubmitExam legacy path upsert)
    - Controllers/AdminController.cs (GradeFromSavedAnswers ~line 2804 — package path grading)
    - Models/SessionElemenTeknisScore.cs (model baru dari Task 1)
  </read_first>
  <action>
**A. CMPController.cs — SubmitExam package path (setelah grading loop, sebelum SaveChangesAsync pertama):**

Tambahkan ET score persist logic:
```csharp
// Persist ET scores per session
var etGroups = packageQuestions
    .GroupBy(q => string.IsNullOrWhiteSpace(q.ElemenTeknis) ? "Lainnya" : q.ElemenTeknis);

foreach (var etGroup in etGroups)
{
    int etCorrect = 0;
    int etTotal = etGroup.Count();
    foreach (var q in etGroup)
    {
        if (answers.ContainsKey(q.Id))
        {
            var sel = q.Options.FirstOrDefault(o => o.Id == answers[q.Id]);
            if (sel != null && sel.IsCorrect) etCorrect++;
        }
    }
    _context.SessionElemenTeknisScores.Add(new SessionElemenTeknisScore
    {
        AssessmentSessionId = id,
        ElemenTeknis = etGroup.Key,
        CorrectCount = etCorrect,
        QuestionCount = etTotal
    });
}
```

**B. AdminController.cs — GradeFromSavedAnswers (package path only, sebelum return):**

Tambahkan ET score persist logic serupa. Gunakan `responses` dictionary (PackageUserResponses) yang sudah di-load untuk lookup jawaban benar. Legacy path di GradeFromSavedAnswers: SKIP persist ET (AssessmentQuestion tidak punya ElemenTeknis). Tambahkan komentar:
```csharp
// Legacy path: ET scores tidak dipersist karena AssessmentQuestion tidak memiliki field ElemenTeknis
```

**C. CMPController.cs — SaveLegacyAnswer (ExecuteUpdateAsync path):**

Tambah `.SetProperty(r => r.SubmittedAt, DateTime.UtcNow)` ke chain ExecuteUpdateAsync yang sudah ada. Juga tambah `SubmittedAt = DateTime.UtcNow` pada `new UserResponse { ... }` di insert path.

**D. CMPController.cs — SubmitExam legacy path (upsert loop):**

Pada update path (`existingLegacyDict.TryGetValue`): tambah `existingLegacyResponse.SubmittedAt = DateTime.UtcNow;`
Pada insert path (`_context.UserResponses.Add`): tambah `SubmittedAt = DateTime.UtcNow` di object initializer.

PENTING: SubmitExam legacy path TIDAK perlu ET score persist (sama alasan dengan GradeFromSavedAnswers legacy path).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/CMPController.cs` berisi `SessionElemenTeknisScores.Add` di SubmitExam package path
    - `Controllers/CMPController.cs` berisi `SubmittedAt = DateTime.UtcNow` di SaveLegacyAnswer (minimal 2 lokasi: update dan insert)
    - `Controllers/CMPController.cs` berisi `SubmittedAt = DateTime.UtcNow` di SubmitExam legacy path (minimal 2 lokasi: update dan insert)
    - `Controllers/AdminController.cs` berisi `SessionElemenTeknisScores.Add` di GradeFromSavedAnswers
    - `Controllers/AdminController.cs` berisi komentar "Legacy path: ET scores tidak dipersist"
    - `dotnet build` exit code 0
  </acceptance_criteria>
  <done>ET scores dipersist di SubmitExam (package) dan GradeFromSavedAnswers (package). SubmittedAt terisi di SaveLegacyAnswer dan SubmitExam legacy path. Build green.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes
- grep "SessionElemenTeknisScores" di CMPController.cs dan AdminController.cs menghasilkan Add calls
- grep "SubmittedAt" di CMPController.cs menghasilkan minimal 4 assignment (2 di SaveLegacyAnswer, 2 di SubmitExam legacy)
- Model file SessionElemenTeknisScore.cs ada dengan 5 properties
</verification>

<success_criteria>
1. SessionElemenTeknisScore tabel ada di DB dengan unique index pada (SessionId, ElemenTeknis)
2. SubmitExam package path persist ET scores
3. GradeFromSavedAnswers package path persist ET scores
4. SaveLegacyAnswer mengisi SubmittedAt pada UserResponse (update + insert)
5. SubmitExam legacy path mengisi SubmittedAt pada UserResponse (update + insert)
6. Build green
</success_criteria>

<output>
After completion, create `.planning/phases/223-assessment-quick-wins/223-01-SUMMARY.md`
</output>
