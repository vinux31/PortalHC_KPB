---
phase: 223-assessment-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/TrainingRecord.cs
  - Models/AssessmentSession.cs
  - Views/Admin/EditTraining.cshtml
  - Views/Admin/AddTraining.cshtml
  - Views/Admin/ImportTraining.cshtml
  - Views/Admin/ManageAssessment.cshtml
  - Views/CMP/Records.cshtml
  - Views/CMP/RecordsWorkerDetail.cshtml
  - Services/WorkerDataService.cs
autonomous: true
requirements: [CLEN-01, CLEN-05, AINT-02, AINT-03]
must_haves:
  truths:
    - "Status 'Wait Certificate' tidak muncul di dropdown form manapun"
    - "Status 'Wait Certificate' tidak muncul di badge/switch expression manapun"
    - "Status 'Failed' tersedia di dropdown form AddTraining dan EditTraining"
    - "TrainingRecord.Status lifecycle terdokumentasi di model class"
    - "AssessmentSession.AccessToken memiliki XML doc comment yang menjelaskan shared token pattern"
    - "Data existing dengan Status 'Wait Certificate' sudah dimigrasikan ke 'Passed'"
  artifacts:
    - path: "Models/TrainingRecord.cs"
      provides: "Lifecycle documentation"
      contains: "Lifecycle Training Manual"
    - path: "Models/AssessmentSession.cs"
      provides: "AccessToken documentation"
      contains: "common exam room pattern"
  key_links:
    - from: "Views/Admin/EditTraining.cshtml"
      to: "TrainingRecord.Status"
      via: "select option values"
      pattern: "option value.*Failed"
    - from: "Services/WorkerDataService.cs"
      to: "TrainingRecord.Status"
      via: "status filter logic"
      pattern: "Wait Certificate"
---

<objective>
Cleanup TrainingRecord status lifecycle dan dokumentasi AccessToken.

Purpose: Hapus ambiguitas status "Wait Certificate", definisikan lifecycle yang jelas (Passed/Valid/Expired/Failed), dan dokumentasikan keputusan desain AccessToken shared.
Output: Views dibersihkan, data dimigrasikan, model terdokumentasi.

NOTE: AINT-02 dan AINT-03 (tab-switch detection) DEFERRED per user decision — sudah ada mekanisme warning di sistem. Requirement IDs disertakan untuk traceability tapi tidak ada implementasi.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-assessment-quick-wins/223-CONTEXT.md
@.planning/phases/223-assessment-quick-wins/223-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status Lifecycle Cleanup — Views, Service, Data Migration</name>
  <files>Views/Admin/EditTraining.cshtml, Views/Admin/AddTraining.cshtml, Views/Admin/ImportTraining.cshtml, Views/Admin/ManageAssessment.cshtml, Views/CMP/Records.cshtml, Views/CMP/RecordsWorkerDetail.cshtml, Services/WorkerDataService.cs</files>
  <read_first>
    - Views/Admin/EditTraining.cshtml (line ~117 — "Wait Certificate" option)
    - Views/Admin/AddTraining.cshtml (line ~139 — "Wait Certificate" option)
    - Views/Admin/ImportTraining.cshtml (line ~188 — template help text)
    - Views/CMP/Records.cshtml (line ~178 — switch expression badge)
    - Views/CMP/RecordsWorkerDetail.cshtml (line ~239 — switch expression badge)
    - Views/Admin/ManageAssessment.cshtml (line ~606 — switch expression badge)
    - Services/WorkerDataService.cs (line ~218 — "Wait Certificate" condition)
  </read_first>
  <action>
**A. Views/Admin/EditTraining.cshtml (~line 117):**
- Hapus `<option value="Wait Certificate">Wait Certificate</option>`
- Tambah `<option value="Failed">Failed</option>` di daftar option status (setelah Expired)

**B. Views/Admin/AddTraining.cshtml (~line 139):**
- Hapus `<option value="Wait Certificate">Wait Certificate</option>`
- Tambah `<option value="Failed">Failed</option>` di daftar option status (setelah Expired)

**C. Views/Admin/ImportTraining.cshtml (~line 188):**
- Update keterangan kolom Status di template help text: hapus referensi "Wait Certificate", pastikan status yang valid adalah: Passed, Valid, Expired, Failed

**D. Views/CMP/Records.cshtml (~line 178):**
- Hapus case "Wait Certificate" dari switch expression badge rendering
- Pastikan "Failed" case ada dengan badge `bg-danger`

**E. Views/CMP/RecordsWorkerDetail.cshtml (~line 239):**
- Hapus case "Wait Certificate" dari switch expression badge rendering
- Pastikan "Failed" case ada dengan badge `bg-danger`

**F. Views/Admin/ManageAssessment.cshtml (~line 606):**
- Hapus case "Wait Certificate" dari switch expression badge rendering
- Pastikan "Failed" case ada dengan badge `bg-danger`

**G. Services/WorkerDataService.cs (~line 218):**
- Hapus `tr.Status == "Wait Certificate"` dari kondisi. Jika ini bagian dari OR condition, hapus hanya clause ini dan pertahankan sisanya.

**H. EF Migration untuk data cleanup:**
Jalankan `dotnet ef migrations add CleanupWaitCertificateStatus` lalu edit migration file — tambahkan di method Up():
```csharp
migrationBuilder.Sql("UPDATE TrainingRecords SET Status = 'Passed' WHERE Status = 'Wait Certificate';");
```
Dan di method Down():
```csharp
migrationBuilder.Sql("UPDATE TrainingRecords SET Status = 'Wait Certificate' WHERE Status = 'Passed';");
```
Note: Down() bersifat best-effort — tidak semua "Passed" berasal dari "Wait Certificate".

Jalankan `dotnet ef database update`.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep "Wait Certificate" di semua file .cshtml dan .cs menghasilkan ZERO results (kecuali migration file dan komentar)
    - `Views/Admin/EditTraining.cshtml` berisi `<option value="Failed">`
    - `Views/Admin/AddTraining.cshtml` berisi `<option value="Failed">`
    - `Services/WorkerDataService.cs` TIDAK berisi string "Wait Certificate"
    - Migration file terbuat dan berisi `UPDATE TrainingRecords SET Status = 'Passed' WHERE Status = 'Wait Certificate'`
    - `dotnet build` exit code 0
  </acceptance_criteria>
  <done>"Wait Certificate" dihapus dari seluruh views dan services. "Failed" ditambahkan ke dropdown. Data dimigrasikan ke "Passed". Build green.</done>
</task>

<task type="auto">
  <name>Task 2: Dokumentasi Model — TrainingRecord Lifecycle dan AccessToken</name>
  <files>Models/TrainingRecord.cs, Models/AssessmentSession.cs</files>
  <read_first>
    - Models/TrainingRecord.cs (field Status — perlu tambah XML doc comment lifecycle)
    - Models/AssessmentSession.cs (field AccessToken — perlu tambah XML doc comment)
  </read_first>
  <action>
**A. Models/TrainingRecord.cs — tambah XML doc comment pada field Status:**
```csharp
/// <summary>
/// Status lifecycle TrainingRecord:
///
/// Training Manual (Import/Add):
///   - "Passed" — training selesai, tanpa sertifikat (atau tanpa ValidUntil)
///   - "Valid" — training selesai dengan sertifikat (ValidUntil terisi)
///   - "Expired" — saat ValidUntil sudah lewat
///
/// Assessment:
///   - "Failed" — peserta gagal ujian
///   - "Passed" — peserta lulus, tanpa sertifikat (GenerateCertificate = false)
///   - "Valid" — peserta lulus dengan sertifikat (GenerateCertificate = true, ValidUntil terisi)
///   - "Expired" — saat ValidUntil sudah lewat
///
/// Status yang valid: Passed, Valid, Expired, Failed
/// "Wait Certificate" sudah dihapus dan dimigrasikan ke "Passed".
/// </summary>
public string Status { get; set; } = "";
```

**B. Models/AssessmentSession.cs — tambah XML doc comment pada field AccessToken:**
```csharp
/// <summary>
/// Token akses untuk masuk ke sesi ujian.
/// DESAIN DISENGAJA: Token ini di-share ke semua peserta dalam satu batch ujian yang sama
/// (common exam room pattern). Ini bukan security vulnerability — peserta ujian memang
/// berada di ruangan yang sama dan mendapat token yang sama dari pengawas.
/// Token hanya mengontrol akses masuk, bukan identitas peserta (identity ditangani ASP.NET Core Identity).
/// </summary>
public string AccessToken { get; set; } = "";
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Models/TrainingRecord.cs` berisi string "Lifecycle TrainingRecord" dalam XML comment
    - `Models/TrainingRecord.cs` berisi string "Wait Certificate sudah dihapus"
    - `Models/TrainingRecord.cs` berisi string "Passed, Valid, Expired, Failed"
    - `Models/AssessmentSession.cs` berisi string "common exam room pattern"
    - `Models/AssessmentSession.cs` berisi string "DESAIN DISENGAJA"
    - `dotnet build` exit code 0
  </acceptance_criteria>
  <done>TrainingRecord.Status punya dokumentasi lifecycle lengkap. AssessmentSession.AccessToken punya dokumentasi keputusan desain shared token. Build green.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes
- grep -r "Wait Certificate" pada Views/ dan Services/ menghasilkan zero matches
- grep "common exam room pattern" pada Models/AssessmentSession.cs menghasilkan match
- grep "Lifecycle TrainingRecord" pada Models/TrainingRecord.cs menghasilkan match
</verification>

<success_criteria>
1. "Wait Certificate" dihapus dari seluruh views dan service code
2. "Failed" tersedia di dropdown status pada AddTraining dan EditTraining
3. Data existing "Wait Certificate" dimigrasikan ke "Passed"
4. TrainingRecord.Status punya XML doc comment lifecycle lengkap
5. AssessmentSession.AccessToken punya XML doc comment shared token pattern
6. Build green
</success_criteria>

<output>
After completion, create `.planning/phases/223-assessment-quick-wins/223-02-SUMMARY.md`
</output>
