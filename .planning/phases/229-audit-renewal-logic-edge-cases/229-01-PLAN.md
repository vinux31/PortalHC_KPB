---
phase: 229-audit-renewal-logic-edge-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Controllers/CDPController.cs
autonomous: true
requirements: [LDAT-01, LDAT-02, LDAT-03, LDAT-04, LDAT-05, EDGE-02]

must_haves:
  truths:
    - "MapKategori menggunakan DB lookup dari AssessmentCategories, bukan hardcode"
    - "Double renewal dicegah server-side di CreateAssessment POST dan AddTraining POST"
    - "FK mutual exclusion guard ada di AddTraining POST (XOR RenewsTrainingId/RenewsSessionId)"
    - "Badge count Admin/Index hanya menggunakan BuildRenewalRowsAsync (tidak ada counting lain)"
    - "CDPController MapKategori di-update konsisten dengan AdminController"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "MapKategori DB lookup, double renewal guard, FK mutual exclusion guard di AddTraining"
    - path: "Controllers/CDPController.cs"
      provides: "MapKategori DB lookup konsisten"
  key_links:
    - from: "AdminController.MapKategori"
      to: "AssessmentCategories table"
      via: "Dictionary lookup parameter"
      pattern: "categoryMappingLookup"
    - from: "AdminController.AddTraining POST"
      to: "RenewsTrainingId/RenewsSessionId"
      via: "XOR validation guard"
      pattern: "RenewsTrainingId.HasValue && model.RenewsSessionId.HasValue"
---

<objective>
Audit dan fix renewal logic: refactor MapKategori ke DB lookup, tambahkan double renewal server-side guard, tambahkan FK mutual exclusion di AddTraining, verifikasi badge count dan GroupKey decode consistency.

Purpose: Fix bug nyata pada renewal logic — MapKategori hardcode tidak sinkron DB, tidak ada server-side guard untuk double renewal, AddTraining tidak punya FK XOR validation.
Output: AdminController.cs dan CDPController.cs dengan fix lengkap.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/229-audit-renewal-logic-edge-cases/229-CONTEXT.md
@.planning/phases/229-audit-renewal-logic-edge-cases/229-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor MapKategori ke DB lookup + double renewal guard + FK XOR di AddTraining</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (baris 6696-6702 untuk MapKategori, baris 5539-5650 untuk AddTraining POST, baris 1205-1397 untuk CreateAssessment POST, baris 55-64 untuk Index badge, baris 6704-6853 untuk BuildRenewalRowsAsync, baris 7080-7176 untuk grouping/decode)
    - Models/AssessmentCategory.cs
  </read_first>
  <action>
**A. Refactor MapKategori (per D-08, LDAT-05):**

1. Ubah method signature dari `private static string MapKategori(string? raw)` menjadi `private static string MapKategori(string? raw, Dictionary<string, string>? rawToDisplayMap)`.
2. Logic baru:
   ```csharp
   private static string MapKategori(string? raw, Dictionary<string, string>? rawToDisplayMap)
   {
       if (string.IsNullOrWhiteSpace(raw)) return "-";
       var trimmed = raw.Trim();
       if (rawToDisplayMap != null && rawToDisplayMap.TryGetValue(trimmed.ToUpperInvariant(), out var displayName))
           return displayName;
       return trimmed;
   }
   ```
3. Di `BuildRenewalRowsAsync`, sebelum `trainingRows` construction (sekitar baris 6764), build dictionary dari `allCategories` yang sudah di-query di baris 6784:
   - PINDAHKAN query `allCategories` (baris 6784-6791) ke SEBELUM trainingRows construction.
   - Build `rawToDisplayMap`:
     ```csharp
     var rawToDisplayMap = allCategories
         .Where(c => c.ParentId == null) // hanya parent categories
         .ToDictionary(c => c.Name.ToUpperInvariant(), c => c.Name);
     ```
   - Perhatikan: TrainingRecord.Kategori berisi raw codes seperti "MANDATORY", "PROTON". AssessmentCategories.Name berisi display names "Mandatory HSSE Training", "Assessment Proton". Karena raw codes TIDAK ADA di AssessmentCategories, kita perlu mempertahankan mapping lama sebagai fallback. Tambahkan manual entries jika belum ada di DB:
     ```csharp
     if (!rawToDisplayMap.ContainsKey("MANDATORY"))
         rawToDisplayMap["MANDATORY"] = "Mandatory HSSE Training";
     if (!rawToDisplayMap.ContainsKey("PROTON"))
         rawToDisplayMap["PROTON"] = "Assessment Proton";
     ```
   - Pass `rawToDisplayMap` ke semua call `MapKategori(t.Kategori, rawToDisplayMap)` di trainingRows (baris 6773).
4. Update semua caller MapKategori di AdminController (search semua `MapKategori(` calls — ada di baris 6773, 1082, 1110). Untuk callers di luar BuildRenewalRowsAsync (CreateAssessment GET baris 1082, 1110), query categories inline:
   ```csharp
   var cats = await _context.AssessmentCategories.Where(c => c.IsActive && c.ParentId == null).ToDictionaryAsync(c => c.Name.ToUpperInvariant(), c => c.Name);
   if (!cats.ContainsKey("MANDATORY")) cats["MANDATORY"] = "Mandatory HSSE Training";
   if (!cats.ContainsKey("PROTON")) cats["PROTON"] = "Assessment Proton";
   model.Category = MapKategori(sourceTraining.Kategori, cats);
   ```

**B. Double Renewal Server-Side Guard (per D-10, EDGE-02):**

1. Di `CreateAssessment POST` (setelah XOR validation baris 1215-1218, sebelum ModelState.IsValid check baris 1223), tambahkan:
   ```csharp
   // Double renewal prevention (per D-10): check if source cert already renewed
   if (model.RenewsSessionId.HasValue)
   {
       var srcAlreadyRenewed = await _context.AssessmentSessions.AnyAsync(a => a.RenewsSessionId == model.RenewsSessionId && a.IsPassed == true)
           || await _context.TrainingRecords.AnyAsync(t => t.RenewsSessionId == model.RenewsSessionId);
       if (srcAlreadyRenewed)
           ModelState.AddModelError("", "Sertifikat ini sudah di-renew sebelumnya.");
   }
   if (model.RenewsTrainingId.HasValue)
   {
       var srcAlreadyRenewed = await _context.AssessmentSessions.AnyAsync(a => a.RenewsTrainingId == model.RenewsTrainingId && a.IsPassed == true)
           || await _context.TrainingRecords.AnyAsync(t => t.RenewsTrainingId == model.RenewsTrainingId);
       if (srcAlreadyRenewed)
           ModelState.AddModelError("", "Sertifikat ini sudah di-renew sebelumnya.");
   }
   ```
2. Di `AddTraining POST` (setelah fkMap parsing baris 5544, sebelum ModelState.IsValid check baris 5551), tambahkan guard serupa:
   ```csharp
   // Double renewal prevention (per D-10)
   if (model.RenewsTrainingId.HasValue)
   {
       var srcAlreadyRenewed = await _context.TrainingRecords.AnyAsync(t => t.RenewsTrainingId == model.RenewsTrainingId)
           || await _context.AssessmentSessions.AnyAsync(a => a.RenewsTrainingId == model.RenewsTrainingId && a.IsPassed == true);
       if (srcAlreadyRenewed)
           ModelState.AddModelError("", "Sertifikat ini sudah di-renew sebelumnya.");
   }
   if (model.RenewsSessionId.HasValue)
   {
       var srcAlreadyRenewed = await _context.TrainingRecords.AnyAsync(t => t.RenewsSessionId == model.RenewsSessionId)
           || await _context.AssessmentSessions.AnyAsync(a => a.RenewsSessionId == model.RenewsSessionId && a.IsPassed == true);
       if (srcAlreadyRenewed)
           ModelState.AddModelError("", "Sertifikat ini sudah di-renew sebelumnya.");
   }
   ```

**C. FK Mutual Exclusion Guard di AddTraining POST (per D-04):**

Di `AddTraining POST` (sebelum double renewal guard), tambahkan:
```csharp
// FK mutual exclusion (per D-04)
if (model.RenewsTrainingId.HasValue && model.RenewsSessionId.HasValue)
{
    ModelState.AddModelError("", "Renewal FK tidak valid: hanya boleh mengisi salah satu dari RenewsTrainingId atau RenewsSessionId.");
}
```
CATATAN: CreateAssessment POST sudah punya guard ini di baris 1215-1218. AddTraining POST belum punya.

**D. Verifikasi LDAT-01 (FK 4 kombinasi):**
- CreateAssessment POST baris 1395-1396 sudah handle AS→AS dan AS→TR via `fkMap` dan `model.RenewsSessionId`/`model.RenewsTrainingId`.
- AddTraining POST baris 5607-5611 sudah handle TR→TR dan TR→AS via `fkMap` dan `fkMapType`.
- Single AddTraining baris 5636-5637 sudah set `RenewsTrainingId` dan `RenewsSessionId` dari model.
- Verifikasi: semua 4 path sudah benar. Tidak perlu fix — hanya document di SUMMARY.

**E. Verifikasi LDAT-02 (Badge count):**
- Admin/Index baris 59-61: `var renewalRows = await BuildRenewalRowsAsync(); ViewBag.RenewalCount = renewalRows.Count;`
- Grep seluruh codebase untuk `RenewalCount` — pastikan tidak ada counting independen lain.
- Verifikasi: sudah single source. Tidak perlu fix — hanya document di SUMMARY.

**F. Verifikasi LDAT-03 (DeriveCertificateStatus):**
- Keep as-is per D-06. Document bahwa edge cases sudah di-handle.

**G. Verifikasi LDAT-04 (GroupKey decode):**
- `FilterRenewalCertificateGroup` baris 7138 menggunakan `judul` parameter langsung (URL-decoded via `Uri.UnescapeDataString`), BUKAN decode GroupKey.
- GroupKey hanya digunakan sebagai identifier untuk HTML element IDs, bukan di-decode di server.
- Verifikasi: konsisten. Tidak perlu fix — document di SUMMARY.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `private static string MapKategori(string? raw, Dictionary<string, string>? rawToDisplayMap)`
    - AdminController.cs contains `rawToDisplayMap.TryGetValue(trimmed.ToUpperInvariant()`
    - AdminController.cs contains `Sertifikat ini sudah di-renew sebelumnya` (double renewal guard) in at least 2 locations (CreateAssessment POST and AddTraining POST)
    - AdminController.cs AddTraining POST area contains `RenewsTrainingId.HasValue && model.RenewsSessionId.HasValue` (FK XOR guard)
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>MapKategori refactored ke DB lookup dengan fallback, double renewal guard di CreateAssessment POST dan AddTraining POST, FK mutual exclusion guard di AddTraining POST, LDAT-01/02/03/04 diverifikasi dan didokumentasikan.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror fix MapKategori ke CDPController</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs (baris 3192-3196 untuk MapKategori, baris 3200-3390 untuk BuildSertifikatRowsAsync)
    - Controllers/AdminController.cs (baris 6696 area — untuk referensi new MapKategori signature)
  </read_first>
  <action>
**Mirror MapKategori refactor ke CDPController (per Pitfall 1 dari RESEARCH.md):**

1. Ubah CDPController `MapKategori` (baris 3192) dari:
   ```csharp
   private static string MapKategori(string raw) => raw switch
   {
       "MANDATORY" => "Mandatory HSSE Training",
       "PROTON"    => "Assessment Proton",
       _ => raw
   };
   ```
   Menjadi signature identik dengan AdminController:
   ```csharp
   private static string MapKategori(string? raw, Dictionary<string, string>? rawToDisplayMap)
   {
       if (string.IsNullOrWhiteSpace(raw)) return "-";
       var trimmed = raw.Trim();
       if (rawToDisplayMap != null && rawToDisplayMap.TryGetValue(trimmed.ToUpperInvariant(), out var displayName))
           return displayName;
       return trimmed;
   }
   ```

2. Di `BuildSertifikatRowsAsync`, sebelum training rows construction, build `rawToDisplayMap` dari AssessmentCategories sama seperti AdminController:
   ```csharp
   var allCategories = await _context.AssessmentCategories
       .Where(c => c.IsActive && c.ParentId == null)
       .Select(c => new { c.Name })
       .ToListAsync();
   var rawToDisplayMap = allCategories.ToDictionary(c => c.Name.ToUpperInvariant(), c => c.Name);
   if (!rawToDisplayMap.ContainsKey("MANDATORY"))
       rawToDisplayMap["MANDATORY"] = "Mandatory HSSE Training";
   if (!rawToDisplayMap.ContainsKey("PROTON"))
       rawToDisplayMap["PROTON"] = "Assessment Proton";
   ```

3. Update semua caller `MapKategori(t.Kategori)` di CDPController (baris 3312) menjadi `MapKategori(t.Kategori, rawToDisplayMap)`.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs contains `private static string MapKategori(string? raw, Dictionary<string, string>? rawToDisplayMap)`
    - CDPController.cs contains `rawToDisplayMap.TryGetValue(trimmed.ToUpperInvariant()`
    - CDPController.cs contains `AssessmentCategories` query in BuildSertifikatRowsAsync
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>CDPController MapKategori identik dengan AdminController — DB lookup dengan fallback.</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil tanpa error
- `grep -n "MapKategori" Controllers/AdminController.cs Controllers/CDPController.cs` menunjukkan signature baru dengan Dictionary parameter
- `grep -n "sudah di-renew sebelumnya" Controllers/AdminController.cs` menunjukkan guard di 2+ lokasi
- `grep -n "RenewsTrainingId.HasValue && model.RenewsSessionId.HasValue" Controllers/AdminController.cs` menunjukkan guard di AddTraining POST area (selain CreateAssessment POST)
</verification>

<success_criteria>
- MapKategori di kedua controller menggunakan DB lookup dari AssessmentCategories
- Double renewal server-side guard aktif di CreateAssessment POST dan AddTraining POST
- FK mutual exclusion guard ada di AddTraining POST
- LDAT-01 sampai LDAT-05 dan EDGE-02 terverifikasi atau di-fix
- `dotnet build` sukses
</success_criteria>

<output>
After completion, create `.planning/phases/229-audit-renewal-logic-edge-cases/229-01-SUMMARY.md`
</output>
