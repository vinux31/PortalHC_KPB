---
phase: 230-audit-renewal-ui-cross-page-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/RenewalCertificate.cshtml
  - Views/Admin/Shared/_RenewalGroupedPartial.cshtml
  - Views/Admin/Shared/_RenewalGroupTablePartial.cshtml
  - Views/Shared/_CertificateHistoryModalContent.cshtml
  - Controllers/AdminController.cs
  - Controllers/CDPController.cs
autonomous: true
requirements: [UIUX-01, UIUX-02, UIUX-03, UIUX-04]

must_haves:
  truths:
    - "Grouped view accordion menampilkan badge count (total, expired merah, akan expired kuning) yang akurat per grup"
    - "Filter Kategori mengaktifkan SubKategori dropdown, SubKategori disabled saat Kategori belum dipilih"
    - "Endpoint GetSubCategories ada dan mengembalikan data yang benar"
    - "Certificate history modal menampilkan chain renewal yang akurat untuk semua 4 FK kombinasi"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "GetSubCategories endpoint"
      contains: "GetSubCategories"
    - path: "Views/Admin/RenewalCertificate.cshtml"
      provides: "Filter cascade JS dengan SubKategori"
      contains: "GetSubCategories"
    - path: "Views/Shared/_CertificateHistoryModalContent.cshtml"
      provides: "Chain grouping visual"
  key_links:
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "CDPController.GetSubCategories"
      via: "fetch AJAX call"
      pattern: "GetSubCategories"
    - from: "Controllers/AdminController.cs"
      to: "Views/Admin/Shared/_RenewalGroupedPartial.cshtml"
      via: "FilterRenewalCertificate returns partial"
      pattern: "FilterRenewalCertificate"
---

<objective>
Audit dan fix UI halaman RenewalCertificate: grouped view accordion, filter cascade (termasuk Kategori→SubKategori), renewal modals, dan certificate history modal.

Purpose: Memastikan semua elemen UI renewal berfungsi benar dengan data aktual, warna konsisten, dan filter cascade lengkap.
Output: UI renewal yang sudah diaudit dan diperbaiki, endpoint GetSubCategories terverifikasi/dibuat.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/230-audit-renewal-ui-cross-page-integration/230-CONTEXT.md
@.planning/phases/230-audit-renewal-ui-cross-page-integration/230-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dan fix grouped view, filter cascade, dan GetSubCategories endpoint</name>
  <files>
    Controllers/CDPController.cs,
    Controllers/AdminController.cs,
    Views/Admin/RenewalCertificate.cshtml,
    Views/Admin/Shared/_RenewalGroupedPartial.cshtml,
    Views/Admin/Shared/_RenewalGroupTablePartial.cshtml
  </files>
  <read_first>
    Controllers/CDPController.cs (grep for GetSubCategories — verify endpoint exists),
    Controllers/AdminController.cs (lines 7109-7260 — FilterRenewalCertificate, verify subCategory param applied),
    Views/Admin/RenewalCertificate.cshtml (lines 240-290 — filter cascade JS, reset button),
    Views/Admin/Shared/_RenewalGroupedPartial.cshtml (full file — accordion header badges),
    Views/Admin/Shared/_RenewalGroupTablePartial.cshtml (full file — row colors)
  </read_first>
  <action>
    **UIUX-01 (per D-01, D-02, D-03):** Audit grouped view accordion:
    1. Verify `_RenewalGroupedPartial.cshtml` accordion header shows: judul sertifikat, badge TotalCount, badge ExpiredCount with `bg-danger`, badge AkanExpiredCount with `bg-warning text-dark`
    2. Verify accordion default collapsed (accordion-collapse without `show` class)
    3. Audit color consistency: check summary cards in RenewalCertificate.cshtml use same `bg-danger`/`bg-warning` classes as group badges and row badges in `_RenewalGroupTablePartial.cshtml`. Fix any inconsistency.

    **UIUX-02 (per D-04, D-05, D-06):** Audit dan fix filter cascade:
    1. Grep CDPController.cs for `GetSubCategories` — if endpoint MISSING, create it:
       - Action method `[HttpGet] public async Task<IActionResult> GetSubCategories(string category)`
       - Query `_context.AssessmentCategories.Where(c => c.ParentId != null && c.Parent.Name == category).Select(c => new { c.Name }).ToListAsync()`
       - Return `Json(new { subCategories = results })`
    2. If endpoint EXISTS, verify it returns correct data matching AssessmentCategories with ParentId != null
    3. Verify `FilterRenewalCertificate` in AdminController.cs accepts `subCategory` parameter and applies it to filter results (check if `SertifikatRow.SubKategori` is matched)
    4. Verify RenewalCertificate.cshtml JS: Kategori change handler fetches GetSubCategories, populates SubKategori dropdown, enables it. Reset button disables SubKategori and clears value.
    5. Verify auto-reload: every filter change calls `refreshTable(1)` per D-04

    **UIUX-03 (per D-07, D-08):** Audit renewal modals:
    1. Verify single renew modal shows both "Renew via Assessment" and "Renew via Training" buttons always (per D-07)
    2. Verify bulk renew modal shows both options for homogeneous selections and error for mixed
    3. For D-08: Research confirms BuildRenewalRowsAsync filters IsRenewed=false, so already-renewed certs never appear in table. D-08 skip warning is NOT needed — add a JS comment in bulk renew section: `// D-08: Skip warning not needed — BuildRenewalRowsAsync excludes IsRenewed=true rows`
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "GetSubCategories" Controllers/CDPController.cs && grep -n "subCategory" Controllers/AdminController.cs && grep -n "bg-danger\|bg-warning" Views/Admin/Shared/_RenewalGroupedPartial.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs contains `public.*GetSubCategories` method
    - AdminController.cs FilterRenewalCertificate contains parameter or variable `subCategory`
    - _RenewalGroupedPartial.cshtml contains both `bg-danger` and `bg-warning text-dark` for badges
    - _RenewalGroupTablePartial.cshtml color classes match _RenewalGroupedPartial.cshtml (both use bg-danger for expired, bg-warning for akan expired)
    - RenewalCertificate.cshtml contains `GetSubCategories` in JS fetch call
    - RenewalCertificate.cshtml contains JS comment mentioning D-08
  </acceptance_criteria>
  <done>Grouped view badges akurat dan warna konsisten, filter cascade Kategori→SubKategori berfungsi end-to-end, GetSubCategories endpoint terverifikasi/dibuat, modals sesuai D-07</done>
</task>

<task type="auto">
  <name>Task 2: Audit dan fix certificate history modal chain grouping</name>
  <files>
    Controllers/AdminController.cs,
    Views/Shared/_CertificateHistoryModalContent.cshtml
  </files>
  <read_first>
    Controllers/AdminController.cs (lines 6933-7105 — CertificateHistory action, chain building logic),
    Views/Shared/_CertificateHistoryModalContent.cshtml (full file — chain display)
  </read_first>
  <action>
    **UIUX-04:** Audit certificate history modal chain grouping accuracy:
    1. Read CertificateHistory action — verify it builds chains by following 4 FK combinations:
       - AssessmentSession.RenewsSessionId (AS→AS)
       - AssessmentSession.RenewsTrainingId (AS→TR)
       - TrainingRecord.RenewsSessionId (TR→AS)
       - TrainingRecord.RenewsTrainingId (TR→TR)
    2. Verify chains are grouped by Judul (case-insensitive) and ordered chronologically
    3. Read `_CertificateHistoryModalContent.cshtml` — verify it displays:
       - Each chain as a visual timeline or grouped section
       - Each cert shows: Judul, Tipe (Assessment/Training), ValidUntil/Status, relationship arrow or label (e.g., "renewal dari...")
       - Correct color coding for status (expired=red, active=green, permanent=blue or similar)
    4. Fix any issues found:
       - If chain FK traversal is incomplete (doesn't follow all 4 FK types), add missing FK lookups
       - If display doesn't show renewal relationship clearly, add "Renewal dari [judul]" label
       - If color coding is missing or inconsistent, add appropriate badge classes
    5. Verify modal opens correctly from RenewalCertificate page (check JS handler for history button click)
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "RenewsSessionId\|RenewsTrainingId" Controllers/AdminController.cs | grep -i "history\|chain" | head -20; grep -n "renewal\|chain\|Renewal" Views/Shared/_CertificateHistoryModalContent.cshtml | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs CertificateHistory action references all 4 FK fields: RenewsSessionId, RenewsTrainingId (on both AssessmentSession and TrainingRecord)
    - _CertificateHistoryModalContent.cshtml contains visual chain display (timeline, grouped, or list with renewal relationship labels)
    - _CertificateHistoryModalContent.cshtml contains status color coding (bg-danger or text-danger for expired)
    - RenewalCertificate.cshtml contains JS that opens history modal via fetch to CertificateHistory endpoint
  </acceptance_criteria>
  <done>Certificate history modal menampilkan chain grouping yang akurat untuk semua 4 FK kombinasi renewal, dengan visual relationship dan color coding status</done>
</task>

</tasks>

<verification>
- Grouped view: accordion badges show correct counts with red/yellow colors
- Filter: Kategori→SubKategori cascade works, auto-reload on change, reset clears all
- Modals: both renewal options always shown (D-07), D-08 documented as not needed
- History: chain grouping accurate across all 4 FK types
</verification>

<success_criteria>
- GetSubCategories endpoint exists and returns correct subcategories
- Color coding consistent across summary cards, group badges, and row badges
- Filter cascade Kategori→SubKategori functional end-to-end
- Certificate history shows accurate renewal chains
- All UIUX-01 through UIUX-04 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/230-audit-renewal-ui-cross-page-integration/230-01-SUMMARY.md`
</output>
