---
phase: 230-audit-renewal-ui-cross-page-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Controllers/CDPController.cs
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/AddTraining.cshtml
  - Views/CDP/CertificationManagement.cshtml
  - Views/CDP/Shared/_CertificationManagementTablePartial.cshtml
  - Views/Admin/Index.cshtml
autonomous: true
requirements: [XPAG-01, XPAG-02, XPAG-03, XPAG-04]

must_haves:
  truths:
    - "CreateAssessment pre-fill dari renewal menampilkan judul, kategori, dan peserta yang benar"
    - "AddTraining pre-fill dari renewal menampilkan judul dan peserta yang benar"
    - "CDP CertificationManagement toggle menyembunyikan renewed certs by default dan mempertahankan state saat AJAX reload"
    - "Admin/Index badge count sama dengan jumlah rows di RenewalCertificate"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "CreateAssessment dan AddTraining renewal pre-fill logic"
      contains: "renewSessionId"
    - path: "Views/CDP/CertificationManagement.cshtml"
      provides: "Toggle renewed certs"
      contains: "applyRenewedToggle"
    - path: "Views/Admin/Index.cshtml"
      provides: "Badge count display"
      contains: "RenewalCount"
  key_links:
    - from: "Views/Admin/RenewalCertificate.cshtml"
      to: "Controllers/AdminController.cs CreateAssessment"
      via: "redirect with renewSessionId/renewTrainingId params"
      pattern: "renewSessionId|renewTrainingId"
    - from: "Controllers/CDPController.cs"
      to: "Views/CDP/Shared/_CertificationManagementTablePartial.cshtml"
      via: "renewed-row class and IsRenewed calculation"
      pattern: "renewed-row|IsRenewed"
---

<objective>
Audit dan fix integrasi lintas halaman renewal: CreateAssessment pre-fill, AddTraining pre-fill, CDP CertificationManagement toggle renewed certs, dan Admin/Index badge count sync.

Purpose: Memastikan renewal flow terintegrasi dengan benar ke semua halaman terkait — pre-fill akurat, toggle berfungsi, badge sinkron.
Output: Cross-page integration yang sudah diaudit dan diperbaiki, HTML audit report.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/230-audit-renewal-ui-cross-page-integration/230-CONTEXT.md
@.planning/phases/230-audit-renewal-ui-cross-page-integration/230-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dan fix CreateAssessment dan AddTraining renewal pre-fill</name>
  <files>
    Controllers/AdminController.cs,
    Views/Admin/CreateAssessment.cshtml,
    Views/Admin/AddTraining.cshtml
  </files>
  <read_first>
    Controllers/AdminController.cs (lines 957-1063 — CreateAssessment GET renewal pre-fill),
    Controllers/AdminController.cs (lines 5444-5550 — AddTraining GET renewal pre-fill),
    Views/Admin/CreateAssessment.cshtml (full file — form fields, category dropdown, user picker),
    Views/Admin/AddTraining.cshtml (full file — form fields, renewal mode alert, hidden FK fields)
  </read_first>
  <action>
    **XPAG-01 (per D-09):** Audit CreateAssessment renewal pre-fill:
    1. Verify controller sets: model.Title (from cert judul), model.Category (from MapKategori for training, from session.Category for assessment), model.SelectedUserIds (worker IDs)
    2. Check CreateAssessment.cshtml: verify category dropdown `<select>` has `<option>` values that match what controller sets in model.Category. If MapKategori returns display name like "Mandatory HSSE Training" but dropdown uses different value — fix mismatch by ensuring dropdown option values match AssessmentCategories.Name
    3. Check user multi-select: verify SelectedUserIds pre-selects the correct workers. Look for how the multi-select is populated — if it uses `selected` attribute or JS initialization
    4. Verify ViewBag.RenewalSourceUserName is displayed in alert
    5. Verify ViewBag.RenewalFkMap and ViewBag.RenewalFkMapType are passed as hidden inputs for POST

    **XPAG-02 (per D-10):** Audit AddTraining renewal pre-fill:
    1. Verify controller sets: model.Judul or ViewBag.RenewalSourceTitle (from cert judul), IsRenewalMode=true, SelectedUserIds
    2. Check if Kategori is pre-filled — if AddTraining has a Kategori field, verify it's set. If not, this is OK (training kategori is free text, different from assessment categories)
    3. Verify AddTraining.cshtml: alert "Mode Renewal" shows when ViewBag.IsRenewalMode == true
    4. Verify hidden fields: RenewalFkMap, RenewalFkMapType are present in form for POST submission
    5. Verify POST action processes RenewalFkMap correctly — sets appropriate FK (RenewsSessionId or RenewsTrainingId) on the new TrainingRecord

    **Fix any issues found** — specifically watch for:
    - Category dropdown value mismatch (Pitfall 4 from research)
    - SelectedUserIds not pre-selecting in multi-select
    - Missing hidden fields for RenewalFkMap/RenewalFkMapType
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "RenewalFkMap\|RenewalFkMapType\|RenewalSourceTitle\|IsRenewalMode" Controllers/AdminController.cs | head -20 && grep -n "RenewalFkMap\|IsRenewalMode\|renewal" Views/Admin/AddTraining.cshtml | head -10 && grep -n "RenewalFkMap\|RenewalSource" Views/Admin/CreateAssessment.cshtml | head -10</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs CreateAssessment GET (around line 957) sets model.Category using MapKategori or session.Category
    - AdminController.cs AddTraining GET (around line 5444) sets IsRenewalMode and RenewalSourceTitle
    - CreateAssessment.cshtml contains hidden input for RenewalFkMap and RenewalFkMapType
    - AddTraining.cshtml contains hidden input for RenewalFkMap and RenewalFkMapType
    - AddTraining.cshtml contains IsRenewalMode alert div
    - Category dropdown values in CreateAssessment.cshtml match AssessmentCategories.Name values
  </acceptance_criteria>
  <done>CreateAssessment dan AddTraining renewal pre-fill berfungsi: judul, kategori (jika applicable), peserta ter-pre-fill dengan benar, hidden FK fields diteruskan ke POST</done>
</task>

<task type="auto">
  <name>Task 2: Audit CDP CertificationManagement toggle, Admin/Index badge, dan generate HTML audit report</name>
  <files>
    Controllers/CDPController.cs,
    Views/CDP/CertificationManagement.cshtml,
    Views/CDP/Shared/_CertificationManagementTablePartial.cshtml,
    Views/Admin/Index.cshtml,
    docs/audit-renewal-ui-v8.1.html
  </files>
  <read_first>
    Controllers/CDPController.cs (lines 3047-3132 — CertificationManagement, BuildSertifikatRowsAsync IsRenewed calc),
    Views/CDP/CertificationManagement.cshtml (lines 130-302 — toggle switch, applyRenewedToggle function, AJAX reload),
    Views/CDP/Shared/_CertificationManagementTablePartial.cshtml (line 49 — renewed-row class),
    Views/Admin/Index.cshtml (lines 215-230 — badge count display),
    Controllers/AdminController.cs (lines 59-61 — Index action BuildRenewalRowsAsync)
  </read_first>
  <action>
    **XPAG-03 (per D-11):** Audit CDP CertificationManagement toggle:
    1. Verify toggle switch exists with id `toggle-renewed` or similar
    2. Verify `applyRenewedToggle()` hides rows with class `renewed-row` when toggle is OFF (default)
    3. Verify after AJAX filter reload, `applyRenewedToggle()` is called to re-apply toggle state
    4. Verify `_CertificationManagementTablePartial.cshtml` adds `renewed-row` class and `style="display:none"` to renewed rows
    5. Verify IsRenewed calculation in CDPController uses same 4 FK sets as AdminController:
       - Check if BuildSertifikatRowsAsync checks: AssessmentSession with RenewsSessionId matching, AssessmentSession with RenewsTrainingId matching, TrainingRecord with RenewsSessionId matching, TrainingRecord with RenewsTrainingId matching
    6. If IsRenewed calculation is incomplete (missing any of 4 FK sets), add the missing FK checks

    **XPAG-04 (per D-12):** Audit Admin/Index badge count:
    1. Verify Index() action calls BuildRenewalRowsAsync() and sets ViewBag.RenewalCount = renewalRows.Count
    2. Verify Index.cshtml displays badge with RenewalCount on the Renewal Sertifikat card
    3. Verify badge uses appropriate styling (e.g., `badge bg-danger` or `badge bg-warning`)
    4. This should already be correct from Phase 229 — verify only

    **HTML Audit Report:** Generate `docs/audit-renewal-ui-v8.1.html` — a standalone HTML report summarizing:
    - All 8 requirements (UIUX-01 to UIUX-04, XPAG-01 to XPAG-04)
    - Per requirement: status (OK/Fixed/N/A), what was found, what was fixed
    - Summary of D-01 through D-12 decision compliance
    - Use same HTML template style as previous audit reports (Bootstrap 5 CDN, single file)
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "applyRenewedToggle\|renewed-row\|toggle-renewed" Views/CDP/CertificationManagement.cshtml | head -10 && grep -n "RenewalCount" Views/Admin/Index.cshtml && test -f docs/audit-renewal-ui-v8.1.html && echo "audit report exists"</automated>
  </verify>
  <acceptance_criteria>
    - CertificationManagement.cshtml contains `applyRenewedToggle` function
    - CertificationManagement.cshtml calls `applyRenewedToggle()` after AJAX innerHTML replacement
    - _CertificationManagementTablePartial.cshtml contains `renewed-row` class on renewed cert rows
    - CDPController.cs IsRenewed calculation checks all 4 FK combinations (RenewsSessionId and RenewsTrainingId on both AssessmentSession and TrainingRecord)
    - Index.cshtml contains `ViewBag.RenewalCount` or `RenewalCount` display
    - File docs/audit-renewal-ui-v8.1.html exists and contains sections for all 8 requirement IDs
  </acceptance_criteria>
  <done>CDP toggle renewed certs berfungsi dengan state preservation saat AJAX reload, Admin/Index badge sinkron, HTML audit report dihasilkan</done>
</task>

</tasks>

<verification>
- CreateAssessment pre-fill: judul, kategori, peserta benar dari renewal flow
- AddTraining pre-fill: judul, peserta benar, mode renewal alert tampil
- CDP toggle: renewed rows hidden by default, state preserved on AJAX reload
- Badge count: Admin/Index shows correct pending renewal count
- Audit report: docs/audit-renewal-ui-v8.1.html exists with all 8 requirements
</verification>

<success_criteria>
- All 4 cross-page integrations (XPAG-01 through XPAG-04) verified and fixed
- Category pre-fill matches dropdown values (no mismatch)
- IsRenewed uses complete 4 FK set calculation
- HTML audit report documents all findings and fixes
</success_criteria>

<output>
After completion, create `.planning/phases/230-audit-renewal-ui-cross-page-integration/230-02-SUMMARY.md`
</output>
