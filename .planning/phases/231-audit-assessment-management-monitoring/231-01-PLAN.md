---
phase: 231-audit-assessment-management-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ManageAssessment.cshtml
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/EditAssessment.cshtml
  - docs/audit-assessment-management-v8.html
autonomous: true
requirements:
  - AMGT-01
  - AMGT-02
  - AMGT-03
  - AMGT-04
  - AMGT-05

must_haves:
  truths:
    - "ManageAssessment list view memiliki filter dropdown kategori dan status yang berfungsi"
    - "CreateAssessment POST menolak submit tanpa kategori dengan pesan error"
    - "EditAssessment menampilkan warning jika assessment memiliki packages terkait"
    - "DeleteAssessment menghapus packages/questions/options secara eksplisit sebelum hapus session"
    - "Semua assessment CRUD actions memiliki [Authorize(Roles = \"Admin, HC\")]"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "Filter kategori/status di ManageAssessment GET, validasi Category di CreateAssessment POST, explicit cascade delete"
      contains: "managementQuery.Where(a => a.Category == category)"
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Filter dropdown UI untuk kategori dan status"
      contains: "id=\"categoryFilter\""
    - path: "docs/audit-assessment-management-v8.html"
      provides: "HTML audit report Plan 1"
  key_links:
    - from: "Views/Admin/ManageAssessment.cshtml"
      to: "AdminController.ManageAssessment GET"
      via: "form submit/query params category + statusFilter"
      pattern: "category.*statusFilter"
    - from: "Controllers/AdminController.cs (DeleteAssessment)"
      to: "AssessmentPackages + PackageQuestions + PackageOptions"
      via: "explicit RemoveRange before session delete"
      pattern: "RemoveRange.*packages"
---

<objective>
Audit dan fix ManageAssessment CRUD — filter list view, CreateAssessment validasi, EditAssessment edge cases, DeleteAssessment cascade, package assignment audit.

Purpose: Fix must-fix gaps (filter kategori/status tidak ada, validasi kategori kosong) dan audit CRUD completeness per D-04.
Output: Fixed controller + views, HTML audit report.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-audit-assessment-management-monitoring/231-CONTEXT.md
@.planning/phases/231-audit-assessment-management-monitoring/231-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dan fix ManageAssessment list view filter + CreateAssessment validasi + EditAssessment edge case</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManageAssessment.cshtml, Views/Admin/CreateAssessment.cshtml, Views/Admin/EditAssessment.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (lines 633-750 untuk ManageAssessment GET, lines 1145-1250 untuk CreateAssessment POST, lines 1581-1700 untuk EditAssessment, lines 1899-2060 untuk DeleteAssessment + DeleteAssessmentGroup, lines 2125-2220 untuk AssessmentMonitoring GET sebagai referensi filter pattern)
    - Views/Admin/ManageAssessment.cshtml
    - Views/Admin/CreateAssessment.cshtml
    - Views/Admin/EditAssessment.cshtml
    - .planning/phases/231-audit-assessment-management-monitoring/231-RESEARCH.md (Code Examples section)
  </read_first>
  <action>
**A. ManageAssessment filter kategori/status (AMGT-05, per D-05, D-06 must-fix):**

1. Di AdminController.cs ManageAssessment GET action (~line 636), SETELAH search filter dan SEBELUM grouping:
   - Tambahkan filter kategori:
     ```csharp
     if (!string.IsNullOrEmpty(category))
         managementQuery = managementQuery.Where(a => a.Category == category);
     ```
   - Pass `ViewBag.SelectedCategory = category;` dan `ViewBag.SelectedStatus = statusFilter;`
   - Ambil distinct categories untuk dropdown: `ViewBag.Categories = await _context.AssessmentSessions.Select(a => a.Category).Distinct().OrderBy(c => c).ToListAsync();`

2. SETELAH grouping ke list (sama pattern dengan AssessmentMonitoring lines 2202-2213), tambahkan status filter:
   ```csharp
   if (string.IsNullOrEmpty(statusFilter))
       grouped = grouped.Where(g => g.GroupStatus != "Closed").ToList();
   else if (statusFilter == "Open" || statusFilter == "Upcoming" || statusFilter == "Closed")
       grouped = grouped.Where(g => g.GroupStatus == statusFilter).ToList();
   // statusFilter == "All" → no filter
   ```

3. Di Views/Admin/ManageAssessment.cshtml, di area tab "Assessment Groups" SEBELUM tabel, tambahkan filter row:
   ```html
   <div class="row mb-3">
     <div class="col-md-3">
       <select id="categoryFilter" class="form-select form-select-sm" onchange="applyFilters()">
         <option value="">Semua Kategori</option>
         @foreach (var cat in (List<string>)ViewBag.Categories)
         {
           <option value="@cat" @(ViewBag.SelectedCategory == cat ? "selected" : "")>@cat</option>
         }
       </select>
     </div>
     <div class="col-md-3">
       <select id="statusFilter" class="form-select form-select-sm" onchange="applyFilters()">
         <option value="">Aktif (Open/Upcoming)</option>
         <option value="Open" @(ViewBag.SelectedStatus == "Open" ? "selected" : "")>Open</option>
         <option value="Upcoming" @(ViewBag.SelectedStatus == "Upcoming" ? "selected" : "")>Upcoming</option>
         <option value="Closed" @(ViewBag.SelectedStatus == "Closed" ? "selected" : "")>Closed</option>
         <option value="All" @(ViewBag.SelectedStatus == "All" ? "selected" : "")>Semua Status</option>
       </select>
     </div>
   </div>
   ```
   Dan tambahkan JS function `applyFilters()` yang redirect ke URL dengan query params `category` dan `statusFilter`.

**B. CreateAssessment validasi kategori (AMGT-01, per D-08):**

4. Di AdminController.cs CreateAssessment POST (~line 1145), SETELAH validasi Title, tambahkan:
   ```csharp
   if (string.IsNullOrWhiteSpace(model.Category))
   {
       ModelState.AddModelError("Category", "Kategori wajib dipilih.");
   }
   ```

5. Di CreateAssessment.cshtml, pastikan field Category menampilkan validation message: tambahkan `<span asp-validation-for="Category" class="text-danger"></span>` di bawah Category select jika belum ada.

6. Audit PassPercentage validation — jika tidak ada explicit check di POST, tambahkan:
   ```csharp
   if (model.PassPercentage < 0 || model.PassPercentage > 100)
   {
       ModelState.AddModelError("PassPercentage", "Passing grade harus antara 0-100.");
   }
   ```

**C. EditAssessment edge case (AMGT-02, per D-09):**

7. Di AdminController.cs EditAssessment POST (~line 1668), SETELAH check `Status == "Completed"`, tambahkan warning untuk InProgress:
   ```csharp
   var hasInProgress = await _context.AssessmentSessions
       .AnyAsync(s => s.Title == assessment.Title && s.Category == assessment.Category
           && s.Schedule == assessment.Schedule && s.StartedAt != null && s.CompletedAt == null);
   if (hasInProgress)
   {
       TempData["Warning"] = "Perhatian: Ada peserta yang sedang mengerjakan ujian. Perubahan Title/Category/Schedule tidak akan berlaku untuk sesi yang sedang berjalan.";
   }
   ```

8. Di EditAssessment.cshtml, audit apakah `ViewBag.PackageCount` ditampilkan sebagai warning — jika tidak, tambahkan alert:
   ```html
   @if (ViewBag.PackageCount > 0)
   {
     <div class="alert alert-warning">Assessment ini memiliki @ViewBag.PackageCount paket soal. Mengubah peserta dapat mempengaruhi assignment paket.</div>
   }
   ```

**D. DeleteAssessment cascade (AMGT-03, per D-10):**

9. Di AdminController.cs DeleteAssessment (~line 1899), SEBELUM `_context.AssessmentSessions.Remove(assessment)`, tambahkan explicit package cleanup:
   ```csharp
   var packages = await _context.AssessmentPackages
       .Include(p => p.Questions).ThenInclude(q => q.Options)
       .Where(p => p.AssessmentSessionId == id)
       .ToListAsync();
   foreach (var pkg in packages)
   {
       foreach (var q in pkg.Questions)
           _context.PackageOptions.RemoveRange(q.Options);
       _context.PackageQuestions.RemoveRange(pkg.Questions);
   }
   _context.AssessmentPackages.RemoveRange(packages);
   ```

10. Terapkan pattern yang sama di DeleteAssessmentGroup (~line 1979) — untuk setiap sibling session, hapus packages/questions/options secara eksplisit sebelum hapus sessions.

**E. Authorization audit (per D-24):**

11. Verify semua assessment actions memiliki `[Authorize(Roles = "Admin, HC")]`. Catat hasilnya untuk report. Jika ada yang missing, tambahkan.

**F. Audit log audit (per D-23):**

12. Verify semua CRUD actions (Create, Edit, Delete, DeleteGroup) memiliki `_auditLog.LogAsync` call. Jika ada yang missing, tambahkan dengan pattern:
    ```csharp
    try { await _auditLog.LogAsync(User.Identity.Name, "ActionName", $"Detail: {assessment.Title}"); }
    catch { /* warning-only */ }
    ```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `managementQuery = managementQuery.Where(a => a.Category == category)` in ManageAssessment GET
    - AdminController.cs contains `grouped = grouped.Where(g => g.GroupStatus == statusFilter).ToList()` in ManageAssessment GET
    - ManageAssessment.cshtml contains `id="categoryFilter"` and `id="statusFilter"`
    - AdminController.cs contains `ModelState.AddModelError("Category", "Kategori wajib dipilih.")` in CreateAssessment POST
    - AdminController.cs contains `_context.AssessmentPackages.RemoveRange(packages)` in DeleteAssessment
    - AdminController.cs contains `hasInProgress` check in EditAssessment POST
    - EditAssessment.cshtml contains `ViewBag.PackageCount > 0` alert
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>ManageAssessment filter kategori/status berfungsi, CreateAssessment validasi kategori + PassPercentage, EditAssessment warning InProgress + package count, DeleteAssessment explicit cascade, authorization + audit log verified</done>
</task>

<task type="auto">
  <name>Task 2: Generate HTML audit report dan audit package assignment flow</name>
  <files>docs/audit-assessment-management-v8.html, Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 6050-6420 untuk ManagePackages, ReshufflePackage ~3183, AssignPackage area)
    - Controllers/AdminController.cs (lines 1820-1870 untuk BulkAssign di EditAssessment POST)
    - docs/audit-assessment-training-v8.html (sebagai template HTML report format)
    - Views/Admin/ManagePackages.cshtml
    - Views/Admin/ImportPackageQuestions.cshtml
    - Views/Admin/PreviewPackage.cshtml
  </read_first>
  <action>
**A. Package assignment audit (AMGT-04, per D-15 s/d D-18):**

1. Baca dan audit flow package assignment:
   - BulkAssign di EditAssessment POST: saat `NewUserIds` ditambahkan, bagaimana packages di-assign? Audit `ReshufflePackage` action (~line 3183) dan `AssignPackage` jika ada.
   - Cek assignment conflict: apa behavior jika assign ke peserta yang sudah punya active session?
   - Audit ImportPackageQuestions: validasi format Excel, error handling, duplicate detection.
   - Audit PreviewPackage: soal render, gambar/media, jawaban tersembunyi.

2. Jika ditemukan bug pada assignment flow, fix di AdminController.cs. Catat temuan untuk report.

**B. HTML audit report (per D-03):**

3. Buat file `docs/audit-assessment-management-v8.html` — self-contained HTML report (sama format dengan `docs/audit-assessment-training-v8.html`). Isi report:

   - **Header:** "Audit Assessment Management — Phase 231 Plan 01"
   - **Section 1: ManageAssessment List View** — temuan (filter missing → fixed), status filter added, search audit
   - **Section 2: CreateAssessment** — validasi audit (kategori gap → fixed, PassPercentage → fixed), renewal integration audit per D-07
   - **Section 3: EditAssessment** — data preservation audit, InProgress warning added, package warning audit
   - **Section 4: DeleteAssessment** — cascade audit (explicit package cleanup added), DeleteAssessmentGroup audit
   - **Section 5: Package Management** — assignment flow audit, ImportPackageQuestions audit, PreviewPackage audit
   - **Section 6: Authorization & Audit Log** — per-action [Authorize] verification, audit log consistency
   - **Appendix:** Tabel semua temuan dengan severity (must-fix/should-improve/info), status (fixed/verified-ok/deferred)

   Style: dark theme, collapsible sections, Bootstrap CDN, consistent with existing audit reports.

4. Audit renewal integration dari perspektif CreateAssessment (per D-07):
   - Baca CreateAssessment GET untuk parameter `renewSessionId`/`renewTrainingId`
   - Verify pre-fill behavior (judul, kategori, peserta dari renewal source)
   - Catat temuan di report Section 2

5. Audit UserAssessmentHistory (per D-25):
   - Baca Views/Admin/UserAssessmentHistory.cshtml
   - Verify data accuracy (score, status, tanggal), link dari monitoring detail
   - Catat temuan di report sebagai sub-section
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5 && test -f docs/audit-assessment-management-v8.html && echo "Report exists"</automated>
  </verify>
  <acceptance_criteria>
    - docs/audit-assessment-management-v8.html exists and contains "Audit Assessment Management"
    - docs/audit-assessment-management-v8.html contains sections for ManageAssessment, CreateAssessment, EditAssessment, DeleteAssessment, Package Management
    - docs/audit-assessment-management-v8.html contains severity table with "must-fix" and "fixed" entries
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Package assignment flow audited, HTML report generated covering all AMGT findings, renewal integration audited per D-07, UserAssessmentHistory verified per D-25</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil tanpa error
- ManageAssessment.cshtml memiliki dropdown filter kategori dan status
- AdminController.cs memiliki filter logic di ManageAssessment GET
- CreateAssessment POST menolak Category kosong
- DeleteAssessment memiliki explicit package/question/option cleanup
- HTML report ada di docs/audit-assessment-management-v8.html
</verification>

<success_criteria>
- AMGT-01: CreateAssessment validasi lengkap termasuk kategori dan PassPercentage
- AMGT-02: EditAssessment menampilkan warning InProgress dan package count
- AMGT-03: DeleteAssessment cascade cleanup eksplisit (packages, questions, options)
- AMGT-04: Package assignment flow teraudit dan terdokumentasi
- AMGT-05: ManageAssessment filter kategori dan status berfungsi
</success_criteria>

<output>
After completion, create `.planning/phases/231-audit-assessment-management-monitoring/231-01-SUMMARY.md`
</output>
