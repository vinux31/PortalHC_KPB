---
phase: 232-audit-assessment-flow-worker-side
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/Assessment.cshtml
  - Views/CMP/StartExam.cshtml
  - Hubs/AssessmentHub.cs
  - wwwroot/js/assessment-hub.js
  - Controllers/CMPController.cs
autonomous: true
requirements:
  - AFLW-01
  - AFLW-02
  - AFLW-04
must_haves:
  truths:
    - "Worker melihat daftar assessment dengan badge warna benar: Open=hijau, Upcoming=biru, Completed=abu-abu, Expired=merah"
    - "Token entry modal auto-focus, mendukung Enter key, clear error saat ketik ulang, pesan error Bahasa Indonesia"
    - "Timer menggunakan wall clock anchor (Date.now) bukan setInterval decrement, tidak drift saat tab minimize"
    - "Timer habis menampilkan warning modal 'Waktu habis' sebelum auto-submit"
    - "SignalR worker push berfungsi: HC Reset/Force Close mengirim notifikasi real-time ke worker exam page"
    - "Session resume memulihkan ElapsedSeconds, LastActivePage, jawaban pre-populated, timer lanjut akurat"
    - "Network disconnect menampilkan indikator persisten dan retry jawaban gagal saat reconnect"
  artifacts:
    - path: "Views/CMP/Assessment.cshtml"
      provides: "Assessment list dengan badge warna benar dan empty state Bahasa Indonesia"
      contains: "bg-primary"
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Exam page dengan timer wall-clock, modal warning, token UX fixes, network indicator"
      contains: "timeUpWarningModal"
    - path: "Hubs/AssessmentHub.cs"
      provides: "JoinWorkerSession method untuk per-worker SignalR group"
      contains: "JoinWorkerSession"
    - path: "wwwroot/js/assessment-hub.js"
      provides: "assessmentHubStartPromise exposed untuk worker page"
      contains: "assessmentHubStartPromise"
  key_links:
    - from: "Views/CMP/StartExam.cshtml"
      to: "Hubs/AssessmentHub.cs"
      via: "SignalR JoinWorkerSession invocation"
      pattern: "invoke.*JoinWorkerSession"
    - from: "wwwroot/js/assessment-hub.js"
      to: "Views/CMP/StartExam.cshtml"
      via: "assessmentHubStartPromise"
      pattern: "assessmentHubStartPromise"
---

<objective>
Audit dan fix worker-side assessment flow pre-submit: assessment list, token entry, exam page (timer, auto-save, navigation), SignalR worker push, dan session resume.

Purpose: Memastikan seluruh flow dari worker melihat daftar assessment hingga mengerjakan ujian berjalan benar, dengan UX improvements berdasarkan riset Phase 228.
Output: Assessment list dengan badge/empty state benar, exam page dengan timer akurat + modal warning + SignalR real-time + network indicator, session resume lengkap.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-audit-assessment-flow-worker-side/232-CONTEXT.md
@.planning/phases/232-audit-assessment-flow-worker-side/232-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dan fix Assessment List + Token Entry UX</name>
  <read_first>
    - Views/CMP/Assessment.cshtml
    - Controllers/CMPController.cs (offset 1-300, Assessment action area)
    - .planning/phases/232-audit-assessment-flow-worker-side/232-RESEARCH.md (Area 1 dan Area 2 gaps)
  </read_first>
  <files>Views/CMP/Assessment.cshtml, Controllers/CMPController.cs</files>
  <action>
**Assessment List Fixes (D-01 s/d D-05, AFLW-01):**

1. **D-02: Status badge warna** — Ubah `statusBadgeClass` mapping di Assessment.cshtml:
   - `"Open"` → `"bg-success"` (hijau — sudah benar, verifikasi)
   - `"Upcoming"` → `"bg-primary"` (biru — ubah dari `bg-warning`)
   - `"Completed"` → `"bg-secondary"` (abu-abu)
   - `"InProgress"` → `"bg-warning text-dark"` (kuning — tetap)
   - Tambah display-state `Expired`: jika status masih `Open` tapi `ExamWindowCloseDate < DateTime.Now`, tampilkan badge `bg-danger` dengan teks "Expired"

2. **D-03: Empty state Bahasa Indonesia** — Cari teks "No assessments assigned to you" dan ganti dengan "Belum ada assessment yang ditugaskan untuk Anda."

3. **D-05: Completed IsPassed null handling** — Di Riwayat Ujian section, cari kondisi `IsPassed` dan tambah handling untuk `IsPassed == null`: tampilkan badge `bg-secondary` "Belum Dinilai" (bukan "Tidak Lulus").

**Token Entry Fixes (D-06):**

4. **Auto-focus** — Tambah event listener `shown.bs.modal` pada token modal yang focus ke `#tokenInput`:
   ```javascript
   document.getElementById('tokenModal').addEventListener('shown.bs.modal', function() {
       document.getElementById('tokenInput').focus();
   });
   ```

5. **Enter key submit** — Tambah `keydown` listener di `#tokenInput`:
   ```javascript
   document.getElementById('tokenInput').addEventListener('keydown', function(e) {
       if (e.key === 'Enter') { e.preventDefault(); verifyToken(); }
   });
   ```

6. **Clear error + auto-uppercase** — Tambah `input` listener di `#tokenInput`:
   ```javascript
   document.getElementById('tokenInput').addEventListener('input', function() {
       document.getElementById('tokenError').classList.add('d-none');
       this.value = this.value.toUpperCase().replace(/\s/g, '');
   });
   ```

7. **Error message Bahasa Indonesia** — Di CMPController `VerifyToken` action, ubah error messages ke Bahasa Indonesia:
   - "Invalid Token. Please check and try again." → "Token tidak valid. Silakan periksa dan coba lagi."
   - Cari semua string error response terkait token dan Indonesiakan.
  </action>
  <verify>
    <automated>grep -n "bg-primary" Views/CMP/Assessment.cshtml | head -5 &amp;&amp; grep -n "Belum ada assessment" Views/CMP/Assessment.cshtml &amp;&amp; grep -n "shown.bs.modal" Views/CMP/Assessment.cshtml &amp;&amp; grep -n "Token tidak valid" Controllers/CMPController.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assessment.cshtml mengandung `"bg-primary"` untuk Upcoming badge (bukan bg-warning)
    - Assessment.cshtml mengandung `"bg-danger"` untuk Expired display-state
    - Assessment.cshtml mengandung teks "Belum ada assessment yang ditugaskan untuk Anda"
    - Assessment.cshtml mengandung `"Belum Dinilai"` untuk IsPassed == null handling
    - Assessment.cshtml mengandung `shown.bs.modal` listener untuk token auto-focus
    - Assessment.cshtml mengandung `e.key === 'Enter'` untuk Enter key submit
    - Assessment.cshtml mengandung `tokenError.*d-none` pattern untuk clear error on input
    - CMPController.cs mengandung "Token tidak valid" (bukan "Invalid Token")
  </acceptance_criteria>
  <done>Assessment list badge warna benar (Open=hijau, Upcoming=biru, Expired=merah), empty state Bahasa Indonesia, IsPassed null handled, token modal UX lengkap (auto-focus, Enter, clear error, uppercase, pesan Indonesia).</done>
</task>

<task type="auto">
  <name>Task 2: Audit dan fix Timer + SignalR Worker Push + Session Resume + Network Indicator</name>
  <read_first>
    - Views/CMP/StartExam.cshtml
    - Hubs/AssessmentHub.cs
    - wwwroot/js/assessment-hub.js
    - Controllers/CMPController.cs (offset 600-1000, StartExam area)
    - Controllers/AdminController.cs (grep "ResetAssessment\|AkhiriUjian\|sessionReset\|examClosed" untuk verifikasi group name)
    - .planning/phases/232-audit-assessment-flow-worker-side/232-RESEARCH.md (Area 3, 4, 5, 6 gaps)
  </read_first>
  <files>Views/CMP/StartExam.cshtml, Hubs/AssessmentHub.cs, wwwroot/js/assessment-hub.js, Controllers/CMPController.cs</files>
  <action>
**Timer Fixes (D-07, D-08):**

1. **D-07: Timer drift fix — wall clock anchor** — Di StartExam.cshtml, ganti `setInterval` decrement pattern dengan `Date.now()` anchor:
   ```javascript
   var timerStartWallClock = Date.now();
   var timerStartRemaining = REMAINING_SECONDS_FROM_DB;

   function updateTimer() {
       var elapsed = Math.floor((Date.now() - timerStartWallClock) / 1000);
       var remaining = Math.max(0, timerStartRemaining - elapsed);
       timeRemaining = remaining;
       elapsedSeconds = ELAPSED_SECONDS_FROM_DB + elapsed;
       // ... display update (existing code) ...
   }
   ```
   Pertahankan `setInterval(updateTimer, 1000)` untuk display refresh tapi hitung waktu dari wall clock.

2. **D-08: Timer expiry warning modal** — Tambah modal HTML `timeUpWarningModal` di StartExam.cshtml:
   ```html
   <div class="modal fade" id="timeUpWarningModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1">
     <div class="modal-dialog modal-dialog-centered">
       <div class="modal-content">
         <div class="modal-body text-center p-4">
           <i class="bi bi-clock-history text-warning" style="font-size:3rem"></i>
           <h5 class="mt-3">Waktu Habis!</h5>
           <p>Waktu pengerjaan ujian telah habis. Jawaban Anda akan dikirim.</p>
           <button type="button" class="btn btn-primary" id="timeUpOkBtn">OK — Kirim Jawaban</button>
         </div>
       </div>
     </div>
   </div>
   ```
   Ubah timer expiry handler: hapus `document.getElementById('examForm').submit()` langsung, ganti dengan show modal + button click handler + 10 detik auto-submit fallback. Tambah `submitted` guard flag untuk mencegah double submit (lihat Pitfall 4 di RESEARCH.md).

3. **D-09: beforeunload** — Verifikasi `window.onbeforeunload` sudah ada (RESEARCH bilang sudah OK). Pastikan `window.onbeforeunload = null` dipanggil sebelum submit di timeUpOkBtn handler.

**SignalR Worker Push (D-11, D-12):**

4. **Verifikasi AdminController group name** — Baca AdminController.cs, grep `ResetAssessment` dan `AkhiriUjian` untuk mengetahui group name yang digunakan saat kirim `sessionReset`/`examClosed`. Berdasarkan temuan:
   - Jika mengirim ke `"batch-{batchKey}"` → worker sudah join via `JoinBatch` di assessment-hub.js, tidak perlu perubahan hub
   - Jika mengirim ke per-session group → tambah `JoinWorkerSession` method ke AssessmentHub.cs:
     ```csharp
     public async Task JoinWorkerSession(int sessionId)
     {
         await Groups.AddToGroupAsync(Context.ConnectionId, $"worker-session-{sessionId}");
     }
     ```
     Dan panggil dari StartExam.cshtml setelah hub connect.

5. **assessmentHubStartPromise** — Di assessment-hub.js, expose `window.assessmentHubStartPromise` (promise dari `connection.start()`). Di StartExam.cshtml, ganti `setTimeout(2000)` fallback dengan:
   ```javascript
   window.assessmentHubStartPromise.then(function() {
       // join worker session group
       // set hub badge to 'Live'
   });
   ```

**Network Disconnect Indicator (D-14):**

6. **Persistent offline indicator** — Di StartExam.cshtml, tambah elemen badge di header exam (dekat timer):
   ```html
   <span id="networkStatusBadge" class="badge bg-success ms-2">Tersimpan</span>
   ```
   Update badge state:
   - Saat `saveAnswerAsync` berhasil → `bg-success` "Tersimpan"
   - Saat `saveAnswerAsync` gagal → `bg-danger` "Offline"
   - Saat saving → `bg-warning text-dark` "Menyimpan..."

7. **Auto-retry gagal save saat reconnect** — Tambah array `pendingAnswers` yang menampung `{questionId, optionId}` dari save yang gagal. Di `connection.onreconnected()` di assessment-hub.js (atau di StartExam.cshtml reconnect handler), retry semua pending answers:
   ```javascript
   var pendingAnswers = [];
   // Di saveAnswerAsync catch (setelah semua retry gagal):
   pendingAnswers.push({qId: questionId, optId: optionId});
   // Di reconnect:
   if (pendingAnswers.length > 0) {
       pendingAnswers.forEach(function(pa) { saveAnswerAsync(pa.qId, pa.optId); });
       pendingAnswers = [];
   }
   ```

**Exam Navigation (D-10):**

8. **Verifikasi** jump-to-question panel. Jika panel soal hanya menampilkan soal halaman aktif, pertimbangkan apakah menambah overview seluruh soal layak (bisa dilakukan dengan menampilkan semua nomor soal dari semua halaman di sidebar, grouped by page, masing-masing clickable untuk navigate). Jika terlalu kompleks, catat di SUMMARY bahwa ExamSummary page sudah menyediakan overview.

**Session Resume (D-13, AFLW-04):**

9. **Verifikasi elapsedSeconds increment** — Baca `UpdateSessionProgress` action di CMPController (mungkin di offset 1528+). Pastikan `ElapsedSeconds` di-update dengan `elapsedSeconds` yang dikirim dari client (yang sekarang menggunakan wall clock: `ELAPSED_SECONDS_FROM_DB + wallClockElapsed`), bukan increment naif.

10. **Stale question message** — Jika ada redirect saat `SavedQuestionCount != currentQuestionCount`, ubah pesan TempData error ke Bahasa Indonesia yang informatif: "Soal ujian telah diperbarui oleh admin. Silakan mulai ulang ujian."
  </action>
  <verify>
    <automated>grep -n "timerStartWallClock\|Date.now" Views/CMP/StartExam.cshtml | head -3 &amp;&amp; grep -n "timeUpWarningModal" Views/CMP/StartExam.cshtml | head -2 &amp;&amp; grep -n "assessmentHubStartPromise" wwwroot/js/assessment-hub.js | head -2 &amp;&amp; grep -n "networkStatusBadge\|Tersimpan\|Offline" Views/CMP/StartExam.cshtml | head -3 &amp;&amp; grep -n "pendingAnswers" Views/CMP/StartExam.cshtml | head -2</automated>
  </verify>
  <acceptance_criteria>
    - StartExam.cshtml mengandung `Date.now()` untuk timer wall clock anchor
    - StartExam.cshtml mengandung `timerStartWallClock` variabel
    - StartExam.cshtml mengandung `timeUpWarningModal` modal HTML
    - StartExam.cshtml mengandung `timeUpOkBtn` button di modal
    - StartExam.cshtml mengandung `var submitted = false` guard flag
    - StartExam.cshtml mengandung `networkStatusBadge` element
    - StartExam.cshtml mengandung `pendingAnswers` array untuk retry
    - assessment-hub.js mengandung `assessmentHubStartPromise`
    - AssessmentHub.cs mengandung `JoinWorkerSession` (jika per-session group diperlukan berdasarkan audit AdminController)
    - StartExam.cshtml mengandung `assessmentHubStartPromise.then` (bukan setTimeout fallback)
  </acceptance_criteria>
  <done>Timer wall-clock akurat tanpa drift, modal warning sebelum auto-submit, SignalR worker push berfungsi end-to-end, network indicator persisten, auto-retry pending answers saat reconnect, session resume state lengkap.</done>
</task>

</tasks>

<verification>
1. `grep -c "bg-primary" Views/CMP/Assessment.cshtml` — Upcoming badge biru
2. `grep -c "timeUpWarningModal" Views/CMP/StartExam.cshtml` — Modal warning ada
3. `grep -c "Date.now" Views/CMP/StartExam.cshtml` — Timer wall clock
4. `grep -c "assessmentHubStartPromise" wwwroot/js/assessment-hub.js` — Promise exposed
5. `grep -c "networkStatusBadge" Views/CMP/StartExam.cshtml` — Network indicator
6. `grep -c "Token tidak valid" Controllers/CMPController.cs` — Pesan Indonesia
</verification>

<success_criteria>
- Assessment list menampilkan badge warna sesuai D-02 (Open=hijau, Upcoming=biru, Expired=merah)
- Token modal auto-focus, Enter key, clear error, pesan Indonesia
- Timer menggunakan wall clock, tidak drift saat tab minimize
- Timer habis menampilkan warning modal sebelum submit
- SignalR push dari HC ke worker berfungsi end-to-end
- Network disconnect menampilkan indikator persisten + auto-retry
- Session resume memulihkan semua state dengan akurat
</success_criteria>

<output>
After completion, create `.planning/phases/232-audit-assessment-flow-worker-side/232-01-SUMMARY.md`
</output>
