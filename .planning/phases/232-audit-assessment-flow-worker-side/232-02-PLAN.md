---
phase: 232-audit-assessment-flow-worker-side
plan: 02
type: execute
wave: 2
depends_on: ["232-01"]
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/Results.cshtml
  - Views/CMP/ExamSummary.cshtml
  - docs/audit-worker-flow-v81.html
autonomous: true
requirements:
  - AFLW-03
  - AFLW-05
must_haves:
  truths:
    - "SubmitExam menghasilkan score, IsPassed, NomorSertifikat (jika lulus), dan ElemenTeknis scores yang benar"
    - "Results page menampilkan score, pass/fail badge, NomorSertifikat, dan answer review dengan warna hijau/merah"
    - "HC toggle AllowAnswerReview berfungsi — jawaban hanya tampil jika toggle aktif"
    - "Proton Tahun 1-2 exam flow end-to-end identik atau ada handling khusus yang benar"
    - "Proton scoring per aspek dan NomorSertifikat generation berfungsi benar"
  artifacts:
    - path: "Views/CMP/Results.cshtml"
      provides: "Results page dengan NomorSertifikat display dan CompetencyGains cleanup"
      contains: "NomorSertifikat"
    - path: "Controllers/CMPController.cs"
      provides: "Scoring chain dan Results action yang benar"
      contains: "AllowAnswerReview"
    - path: "docs/audit-worker-flow-v81.html"
      provides: "HTML audit report lengkap"
      contains: "Phase 232"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "Views/CMP/Results.cshtml"
      via: "AssessmentResultsViewModel"
      pattern: "AllowAnswerReview"
    - from: "Controllers/CMPController.cs"
      to: "Models (NomorSertifikat)"
      via: "CertNumberHelper"
      pattern: "CertNumberHelper"
---

<objective>
Audit dan fix worker-side post-submit flow: scoring chain, results page, Proton special handling, dan generate HTML audit report lengkap.

Purpose: Memastikan scoring benar, results page informatif, Proton path end-to-end benar, dan seluruh audit terdokumentasi dalam HTML report.
Output: Scoring chain verified, results page improved, Proton audit selesai, HTML report.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-audit-assessment-flow-worker-side/232-CONTEXT.md
@.planning/phases/232-audit-assessment-flow-worker-side/232-RESEARCH.md
@.planning/phases/232-audit-assessment-flow-worker-side/232-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dan fix Scoring Chain + Results Page + Proton Handling</name>
  <read_first>
    - Controllers/CMPController.cs (offset 1100-1528, SubmitExam + Results area; juga grep "Assessment Proton" untuk Proton handling)
    - Views/CMP/Results.cshtml
    - Views/CMP/ExamSummary.cshtml
    - .planning/phases/232-audit-assessment-flow-worker-side/232-RESEARCH.md (Area 7 dan Area 8 gaps)
    - .planning/phases/231-audit-assessment-management-monitoring/231-02-SUMMARY.md (Proton SubmitInterviewResults context)
  </read_first>
  <files>Controllers/CMPController.cs, Views/CMP/Results.cshtml, Views/CMP/ExamSummary.cshtml</files>
  <action>
**Scoring Chain Audit (D-15, AFLW-03):**

1. **Audit SubmitExam action** — Verifikasi:
   - `totalScore / maxScore * 100` → `finalPercentage` kalkulasi benar
   - `IsPassed = finalPercentage >= assessment.PassPercentage` logic benar
   - `CertNumberHelper` generate NomorSertifikat saat `IsPassed && GenerateCertificate`
   - `SessionElemenTeknisScores` tersimpan per session
   - Status-guarded write (`Status != "Completed"`) mencegah double submit
   - Jika ada bug atau edge case, fix langsung

2. **CompetencyGains audit (D-15)** — Baca Results GET action di CMPController. Cek apakah `AssessmentResultsViewModel.CompetencyGains` diisi dari data atau selalu empty collection. Jika selalu empty (karena competency auto-update dihapus Phase 90):
   - Hapus blok `CompetencyGains` dari Results.cshtml (dead code)
   - Atau jika property diisi dari sumber lain, verifikasi dan pertahankan

**Results Page Fixes (D-16, D-17, AFLW-05):**

3. **D-16: NomorSertifikat display** — Tambah display NomorSertifikat di Results.cshtml untuk worker yang lulus:
   ```html
   @if (Model.IsPassed && !string.IsNullOrEmpty(Model.NomorSertifikat))
   {
       <div class="alert alert-success">
           <i class="bi bi-award"></i> Nomor Sertifikat: <strong>@Model.NomorSertifikat</strong>
       </div>
   }
   ```
   Verifikasi bahwa `AssessmentResultsViewModel` punya property `NomorSertifikat` — jika belum, tambah property dan isi di Results GET action dari `session.NomorSertifikat`.

4. **D-17: AllowAnswerReview audit** — Verifikasi di Results GET action bahwa `Model.AllowAnswerReview` diset dari `assessment.AllowAnswerReview` DB field. Jika mapping salah atau missing, fix.

5. **D-16: Section breakdown** — Verifikasi ElemenTeknis table di Results.cshtml sudah menampilkan per-section breakdown (benar/total/persen). Jika answer review ditampilkan flat tanpa grouping per section, pertimbangkan apakah grouping layak ditambah. Jika terlalu kompleks (view model restructure), catat di SUMMARY.

6. **Pesan Bahasa Indonesia** — Cari semua teks Inggris di Results.cshtml dan ExamSummary.cshtml, terjemahkan ke Bahasa Indonesia. Contoh: "Score", "Pass", "Fail", "Answer Review", dll.

**Proton Special Handling (D-18, D-19):**

7. **Proton Tahun 1-2 StartExam path** — Grep `Assessment Proton` atau `Proton` di CMPController. Verifikasi apakah ada handling khusus untuk kategori Proton di StartExam/SubmitExam. Dokumentasikan temuan:
   - Jika identik dengan assessment reguler → catat di report
   - Jika ada branching logic → audit end-to-end, fix jika ada bug

8. **Proton NomorSertifikat** — Verifikasi bahwa `GenerateCertificate` diset benar untuk Assessment Proton saat create. Cek apakah SubmitExam generate NomorSertifikat untuk Proton assessment yang lulus.

9. **Proton scoring per aspek** — Verifikasi Proton Tahun 3 interview scoring (audit dari Phase 231-02 SUMMARY: `SubmitInterviewResults` sudah verified). Untuk Tahun 1-2: verifikasi scoring formula identik dengan reguler atau ada formula khusus.
  </action>
  <verify>
    <automated>grep -n "NomorSertifikat" Views/CMP/Results.cshtml | head -3 &amp;&amp; grep -n "AllowAnswerReview" Controllers/CMPController.cs | head -3 &amp;&amp; grep -n "CompetencyGains" Views/CMP/Results.cshtml | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Results.cshtml mengandung display `NomorSertifikat` untuk worker yang lulus
    - Results.cshtml: blok `CompetencyGains` dihapus (jika confirmed dead code) ATAU diverifikasi terisi
    - CMPController.cs Results action: `AllowAnswerReview` diset dari `assessment.AllowAnswerReview`
    - Teks di Results.cshtml dalam Bahasa Indonesia (tidak ada "Score:", "Pass", "Fail" dalam Inggris)
    - Proton Tahun 1-2 exam path diaudit dan didokumentasikan (bug fix jika ada)
  </acceptance_criteria>
  <done>Scoring chain verified benar, Results page menampilkan NomorSertifikat + answer review toggle berfungsi, CompetencyGains dead code dibersihkan, Proton path diaudit end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Generate HTML Audit Report</name>
  <read_first>
    - .planning/phases/232-audit-assessment-flow-worker-side/232-01-SUMMARY.md
    - docs/audit-v7.7.html (referensi format HTML report sebelumnya)
  </read_first>
  <files>docs/audit-worker-flow-v81.html</files>
  <action>
Generate HTML audit report `docs/audit-worker-flow-v81.html` yang mendokumentasikan seluruh audit Phase 232. Format mengikuti pola report HTML sebelumnya (`docs/audit-v7.7.html`). Konten mencakup:

1. **Header**: "Audit Report: Assessment Flow — Worker Side (Phase 232, v8.1)"
2. **Ringkasan Eksekutif**: Jumlah temuan, jumlah fix, status per requirement (AFLW-01 s/d AFLW-05)
3. **Temuan Per Area** (8 area dari RESEARCH.md):
   - Assessment List (D-01 s/d D-05)
   - Token Entry (D-06)
   - Timer & Auto-Save (D-07, D-08)
   - Browser Close (D-09)
   - Exam Navigation (D-10)
   - SignalR Worker Push (D-11, D-12)
   - Session Resume (D-13, D-14)
   - Scoring & Results (D-15 s/d D-17)
   - Proton Special Handling (D-18, D-19)
4. **Tabel Temuan**: Kolom — Area, Decision ID, Gap Ditemukan, Status (Fixed/Verified OK/Noted), File
5. **Rekomendasi**: Item yang tidak di-fix di phase ini (jika ada) dengan alasan

Gunakan Bootstrap CDN untuk styling (konsisten dengan report sebelumnya). Tabel menggunakan `table-striped table-bordered`. Badge status: `bg-success` Fixed, `bg-info` Verified OK, `bg-warning` Noted.
  </action>
  <verify>
    <automated>test -f docs/audit-worker-flow-v81.html &amp;&amp; grep -c "Phase 232" docs/audit-worker-flow-v81.html</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/audit-worker-flow-v81.html` ada
    - File mengandung "Phase 232"
    - File mengandung tabel dengan kolom Area, Decision ID, Gap, Status, File
    - File mengandung AFLW-01 sampai AFLW-05
    - File menggunakan Bootstrap CDN untuk styling
  </acceptance_criteria>
  <done>HTML audit report lengkap mencakup semua 8+ area audit, tabel temuan per decision ID, status per requirement AFLW.</done>
</task>

</tasks>

<verification>
1. `grep -c "NomorSertifikat" Views/CMP/Results.cshtml` — NomorSertifikat ditampilkan
2. `test -f docs/audit-worker-flow-v81.html` — Report ada
3. `grep -c "AFLW-0" docs/audit-worker-flow-v81.html` — Semua requirement tercakup
</verification>

<success_criteria>
- Scoring chain SubmitExam diaudit dan verified/fixed
- Results page menampilkan NomorSertifikat, answer review toggle berfungsi
- Proton Tahun 1-2 dan Tahun 3 path diaudit end-to-end
- HTML audit report lengkap dengan semua temuan dan status
</success_criteria>

<output>
After completion, create `.planning/phases/232-audit-assessment-flow-worker-side/232-02-SUMMARY.md`
</output>
