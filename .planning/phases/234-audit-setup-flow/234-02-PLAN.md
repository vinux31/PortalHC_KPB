---
phase: 234-audit-setup-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "CoachCoacheeMappingDeactivate cascade dibungkus dalam explicit DB transaction — rollback jika cascade gagal"
    - "CoachCoacheeMappingReactivate menyimpan originalEndDate sebelum modifikasi dan dibungkus transaction"
    - "Assign Tahun 2/3 menampilkan warning jika Tahun sebelumnya belum selesai — warning only, bukan block"
    - "Reactivated coachee bisa langsung assign Tahun 2/3 tanpa warning"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "Transaction wrapping deactivate/reactivate, progression warning di assign"
      contains: "BeginTransactionAsync"
  key_links:
    - from: "AdminController.CoachCoacheeMappingAssign"
      to: "ProtonDeliverableProgresses"
      via: "progression check query"
      pattern: "Status != .Approved."
    - from: "AdminController.CoachCoacheeMappingDeactivate"
      to: "BeginTransactionAsync"
      via: "transaction wrapping"
      pattern: "BeginTransactionAsync"
---

<objective>
Audit dan fix coach-coachee mapping cascade integrity + track assignment progression validation di AdminController.

Purpose: Mencegah state inconsistent pada cascade deactivation dan memastikan progression Tahun 1→2→3 divalidasi dengan warning.
Output: AdminController dengan transaction wrapping pada deactivate/reactivate dan progression warning pada assign.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-audit-setup-flow/234-CONTEXT.md
@.planning/phases/234-audit-setup-flow/234-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transaction Wrapping Deactivate + Reactivate (D-05, D-08)</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (baca CoachCoacheeMappingDeactivate sekitar L4230-4283, CoachCoacheeMappingReactivate sekitar L4289-4350, dan DeleteKompetensi di ProtonDataController.cs L1278 sebagai transaction template)
    - Models/CoachCoacheeMapping.cs (baca model definition)
  </read_first>
  <action>
**A. Wrap CoachCoacheeMappingDeactivate (L4230) dalam transaction (per D-05):**

Tambah `BeginTransactionAsync` di awal method setelah validasi, wrap semua operasi cascade:
```csharp
using var transaction = await _context.Database.BeginTransactionAsync();
try
{
    // existing logic: mapping.IsActive = false, mapping.EndDate = DateTime.Now
    // cascade: deactivate ProtonTrackAssignments terkait
    // ... existing cascade code ...
    await _context.SaveChangesAsync();
    await transaction.CommitAsync();
    return Json(new { success = true });
}
catch (Exception ex)
{
    await transaction.RollbackAsync();
    _logger.LogError(ex, "CoachCoacheeMappingDeactivate transaction failed for mapping {Id}", mappingId);
    return Json(new { success = false, message = "Operasi gagal. Semua perubahan dibatalkan." });
}
```

**B. Fix CoachCoacheeMappingReactivate (L4289) — capture originalEndDate + transaction (per D-08):**

1. Capture `originalEndDate` SEBELUM modifikasi (mengganti fragile `entry.OriginalValues["EndDate"]` pattern):
```csharp
var originalEndDate = mapping.EndDate; // capture BEFORE modifying
mapping.IsActive = true;
mapping.EndDate = null;
```

2. Gunakan `originalEndDate` variable (bukan `entry.OriginalValues`) untuk timestamp matching ±5 detik pada reactivation assignments.

3. Wrap dalam `BeginTransactionAsync`:
```csharp
using var transaction = await _context.Database.BeginTransactionAsync();
try
{
    var originalEndDate = mapping.EndDate;
    mapping.IsActive = true;
    mapping.EndDate = null;
    // reactivate assignments yang DeactivatedAt within 5 seconds of originalEndDate
    // ... existing reactivation logic using originalEndDate instead of entry.OriginalValues ...
    await _context.SaveChangesAsync();
    await transaction.CommitAsync();
    return Json(new { success = true });
}
catch (Exception ex)
{
    await transaction.RollbackAsync();
    _logger.LogError(ex, "CoachCoacheeMappingReactivate transaction failed");
    return Json(new { success = false, message = "Operasi gagal. Semua perubahan dibatalkan." });
}
```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "BeginTransactionAsync" Controllers/AdminController.cs | head -10 && grep -n "originalEndDate" Controllers/AdminController.cs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs CoachCoacheeMappingDeactivate method contains `BeginTransactionAsync`
    - AdminController.cs CoachCoacheeMappingDeactivate method contains `CommitAsync` and `RollbackAsync`
    - AdminController.cs CoachCoacheeMappingReactivate method contains `var originalEndDate = mapping.EndDate`
    - AdminController.cs CoachCoacheeMappingReactivate method contains `BeginTransactionAsync`
    - AdminController.cs does NOT contain `OriginalValues["EndDate"]` (replaced with originalEndDate variable)
  </acceptance_criteria>
  <done>Deactivate dan reactivate dibungkus transaction atomik. OriginalValues fragile pattern diganti dengan explicit variable capture.</done>
</task>

<task type="auto">
  <name>Task 2: Progression Warning Tahun 1→2→3 di Assign (D-09, D-10, D-11, D-12)</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (baca CoachCoacheeMappingAssign sekitar L3944-4000, fokus pada parameter request dan flow create assignment + auto-create progress)
    - Models/ProtonModels.cs (baca ProtonTrack — cari field TahunKe atau Urutan, ProtonTrackAssignment, ProtonDeliverableProgress)
  </read_first>
  <action>
**Tambah progression warning check di CoachCoacheeMappingAssign sebelum create assignment:**

Setelah validasi input dan duplikasi check, tambahkan:
```csharp
// Progression warning (D-09: warning only, bukan block)
if (req.ProtonTrackId.HasValue)
{
    var requestedTrack = await _context.ProtonTracks.FindAsync(req.ProtonTrackId.Value);
    if (requestedTrack != null && requestedTrack.Urutan > 1)
    {
        // Cari track sebelumnya
        var prevTrack = await _context.ProtonTracks
            .Where(t => t.Urutan == requestedTrack.Urutan - 1 && t.IsActive)
            .FirstOrDefaultAsync();
        if (prevTrack != null)
        {
            var incompleteCoachees = new List<string>();
            foreach (var coacheeId in coacheeIds) // gunakan variable coachee IDs dari request
            {
                // D-11: Skip jika coachee sudah pernah punya assignment di track ini (reactivated)
                var hasExistingAssignment = await _context.ProtonTrackAssignments
                    .AnyAsync(a => a.CoacheeId == coacheeId
                               && a.ProtonTrackId == req.ProtonTrackId.Value);
                if (hasExistingAssignment) continue;

                // Cek apakah tahun sebelumnya sudah selesai (D-10: semua progress Approved)
                var prevAssignment = await _context.ProtonTrackAssignments
                    .FirstOrDefaultAsync(a => a.CoacheeId == coacheeId
                                           && a.ProtonTrackId == prevTrack.Id);
                if (prevAssignment == null)
                {
                    incompleteCoachees.Add(coacheeId);
                    continue;
                }

                var allApproved = !await _context.ProtonDeliverableProgresses
                    .AnyAsync(p => p.ProtonTrackAssignmentId == prevAssignment.Id
                               && p.Status != "Approved");
                if (!allApproved)
                    incompleteCoachees.Add(coacheeId);
            }

            // D-09: Warning only — jika ada incomplete dan user belum konfirmasi
            if (incompleteCoachees.Any() && !req.ConfirmProgressionWarning)
            {
                return Json(new { success = false, warning = true,
                    message = $"{incompleteCoachees.Count} coachee belum menyelesaikan {prevTrack.DisplayName ?? "Tahun " + prevTrack.Urutan}. Tetap lanjutkan?",
                    incompleteCount = incompleteCoachees.Count });
            }
        }
    }
}
```

**Tambah `ConfirmProgressionWarning` ke request model:**

Di model request yang digunakan oleh CoachCoacheeMappingAssign (cari class-nya di file), tambahkan:
```csharp
public bool ConfirmProgressionWarning { get; set; }
```

**Update JS caller** di view yang memanggil assign (kemungkinan di Views/Admin/ area mapping):
- Jika response `warning == true`, tampilkan confirm dialog dengan pesan dari server
- Jika user konfirmasi, re-send request dengan `ConfirmProgressionWarning = true`

Catatan: Cek apakah ProtonTrack punya field `Urutan` — jika pakai nama lain (misal `TahunKe` sebagai int), sesuaikan query. Jika `TahunKe` adalah string "Tahun 1", parse angka terakhir untuk comparison.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -n "ConfirmProgressionWarning\|incompleteCoachees\|Urutan.*>" Controllers/AdminController.cs | head -10</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `ConfirmProgressionWarning` property check
    - AdminController.cs contains `incompleteCoachees` list logic in CoachCoacheeMappingAssign
    - AdminController.cs contains query checking `Status != "Approved"` for previous track progress
    - AdminController.cs contains `warning = true` JSON response for incomplete progression
    - Request model contains `public bool ConfirmProgressionWarning { get; set; }`
  </acceptance_criteria>
  <done>Assign Tahun 2/3 menampilkan warning jika tahun sebelumnya belum selesai. Reactivated coachee di-skip dari warning. Warning bisa di-override oleh user.</done>
</task>

</tasks>

<verification>
- `dotnet build` harus berhasil tanpa error
- CoachCoacheeMappingDeactivate dan Reactivate dibungkus transaction
- CoachCoacheeMappingAssign memiliki progression warning check
</verification>

<success_criteria>
1. Cascade deactivation atomik — rollback semua jika gagal
2. Reactivation tidak bergantung pada EF OriginalValues fragile API
3. Warning muncul saat assign Tahun 2/3 sebelum Tahun 1 selesai
4. Reactivated coachee tidak kena warning (D-11)
5. Warning bisa di-override oleh user (D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/234-audit-setup-flow/234-02-SUMMARY.md`
</output>
