---
phase: 235-audit-execution-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Controllers/AdminController.cs
  - Controllers/ProtonDataController.cs
  - Models/ProtonModels.cs
autonomous: true
requirements: [EXEC-01, EXEC-03]
must_haves:
  truths:
    - "Coach upload evidence via UploadEvidence menghasilkan DeliverableStatusHistory entry 'Submitted' atau 'Re-submitted'"
    - "Seed ProtonDeliverableProgress menghasilkan DeliverableStatusHistory entry 'Pending' awal"
    - "Resubmit setelah reject mempertahankan path file lama di EvidencePathHistory"
    - "Upload gagal tidak mengubah status deliverable — rollback bersih"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "RecordStatusHistory call di UploadEvidence, evidence path history, upload rollback"
    - path: "Controllers/AdminController.cs"
      provides: "StatusHistory 'Pending' insert di SeedProtonDeliverableProgressAsync"
    - path: "Models/ProtonModels.cs"
      provides: "EvidencePathHistory column di ProtonDeliverableProgress"
  key_links:
    - from: "CDPController.UploadEvidence"
      to: "RecordStatusHistory"
      via: "call before SaveChangesAsync"
      pattern: "RecordStatusHistory.*Submitted"
    - from: "AdminController.SeedProtonDeliverableProgressAsync"
      to: "DeliverableStatusHistories.Add"
      via: "loop after initial SaveChanges"
      pattern: "StatusType.*Pending"
---

<objective>
Audit dan fix evidence submission flow (EXEC-01) serta DeliverableStatusHistory completeness (EXEC-03).

Purpose: Memastikan setiap transisi state di Proton deliverable memiliki audit trail lengkap, dan evidence path history tersimpan untuk traceability.
Output: CDPController.UploadEvidence dengan StatusHistory + rollback + path history, seed dengan initial Pending history.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-audit-execution-flow/235-CONTEXT.md
@.planning/phases/235-audit-execution-flow/235-RESEARCH.md

@Controllers/CDPController.cs
@Controllers/AdminController.cs
@Controllers/ProtonDataController.cs
@Models/ProtonModels.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah EvidencePathHistory column + StatusHistory di UploadEvidence + upload rollback</name>
  <files>Models/ProtonModels.cs, Controllers/CDPController.cs</files>
  <read_first>
    - Models/ProtonModels.cs (lihat ProtonDeliverableProgress model — field EvidencePath saat ini)
    - Controllers/CDPController.cs (UploadEvidence sekitar L1086-1177, RecordStatusHistory sekitar L3021-3033, SubmitEvidenceWithCoaching L1943-2106 sebagai referensi pattern)
  </read_first>
  <action>
**A. Tambah EvidencePathHistory di ProtonDeliverableProgress (Models/ProtonModels.cs):**
Tambah property baru di class ProtonDeliverableProgress:
```csharp
public string? EvidencePathHistory { get; set; } // JSON array: ["/uploads/evidence/5/v1.pdf", "/uploads/evidence/5/v2.pdf"]
```

**B. Fix UploadEvidence di CDPController.cs (sekitar L1086-1177) — per D-02, D-09, D-15, D-16:**

1. Sebelum overwrite EvidencePath (sekitar L1145-1150), simpan path lama ke history:
```csharp
// Simpan path lama ke history (D-02)
if (!string.IsNullOrEmpty(progress.EvidencePath))
{
    var history = string.IsNullOrEmpty(progress.EvidencePathHistory)
        ? new List<string>()
        : System.Text.Json.JsonSerializer.Deserialize<List<string>>(progress.EvidencePathHistory) ?? new List<string>();
    history.Add(progress.EvidencePath);
    progress.EvidencePathHistory = System.Text.Json.JsonSerializer.Serialize(history);
}
```

2. Wrap FileUploadHelper.SaveFileAsync dan status update dalam try-catch untuk rollback (D-09):
```csharp
string savedPath;
try
{
    savedPath = await FileUploadHelper.SaveFileAsync(file, ...);
}
catch (Exception)
{
    TempData["Error"] = "Gagal menyimpan file. Silakan coba lagi.";
    return RedirectToAction("Deliverable", new { id = progressId });
}
```
Hanya setelah file berhasil disimpan, lanjutkan update progress fields.

3. Tambah RecordStatusHistory sebelum SaveChangesAsync (D-16):
```csharp
bool wasRejected = progress.Status == "Rejected";
// ... existing field updates ...
string uploadStatusType = wasRejected ? "Re-submitted" : "Submitted";
RecordStatusHistory(progress.Id, uploadStatusType, user.Id, user.FullName, "Coach");
await _context.SaveChangesAsync();
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -n "EvidencePathHistory" Models/ProtonModels.cs && grep -n "RecordStatusHistory" Controllers/CDPController.cs | grep -i "upload" && grep -n "EvidencePathHistory" Controllers/CDPController.cs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Models/ProtonModels.cs contains `public string? EvidencePathHistory { get; set; }`
    - CDPController.cs UploadEvidence method contains `RecordStatusHistory(progress.Id, uploadStatusType,` before `SaveChangesAsync`
    - CDPController.cs UploadEvidence contains `EvidencePathHistory` assignment with JsonSerializer.Serialize
    - CDPController.cs UploadEvidence contains try-catch around FileUploadHelper.SaveFileAsync with TempData["Error"] on failure
    - Variable `wasRejected` determines statusType: "Re-submitted" vs "Submitted"
  </acceptance_criteria>
  <done>UploadEvidence memiliki StatusHistory insert, evidence path history tracking, dan upload rollback</done>
</task>

<task type="auto">
  <name>Task 2: Tambah StatusHistory "Pending" di kedua seed locations</name>
  <files>Controllers/AdminController.cs, Controllers/ProtonDataController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (SeedProtonDeliverableProgressAsync sekitar L6800-6840)
    - Controllers/ProtonDataController.cs (cari `ProtonDeliverableProgresses.Add` sekitar L492)
    - Controllers/CDPController.cs (RecordStatusHistory L3021-3033 — untuk signature reference)
  </read_first>
  <action>
**A. AdminController.SeedProtonDeliverableProgressAsync (sekitar L6829-6838):**

Setelah `AddRange(progresses)` dan `SaveChangesAsync()` (yang men-generate IDs), tambahkan loop insert StatusHistory:
```csharp
// Insert initial "Pending" StatusHistory untuk setiap progress baru (D-17)
foreach (var p in progresses)
{
    _context.DeliverableStatusHistories.Add(new DeliverableStatusHistory
    {
        ProtonDeliverableProgressId = p.Id,
        StatusType = "Pending",
        ActorId = assignedById ?? "system",
        ActorName = "System",
        ActorRole = "System",
        Timestamp = DateTime.UtcNow
    });
}
await _context.SaveChangesAsync();
```

Catatan: variabel `assignedById` — gunakan user ID yang tersedia di method scope. Jika tidak ada, gunakan string literal "system".

**B. ProtonDataController seed (sekitar L492):**

Terapkan pattern yang sama — setelah `AddRange` + `SaveChangesAsync`, loop insert DeliverableStatusHistory "Pending" per progress entry. Gunakan user ID dari `User.FindFirst(ClaimTypes.NameIdentifier)?.Value ?? "system"` sebagai ActorId.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -A5 "StatusType.*Pending" Controllers/AdminController.cs | head -10 && grep -A5 "StatusType.*Pending" Controllers/ProtonDataController.cs | head -10</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `StatusType = "Pending"` inside a foreach loop after `SaveChangesAsync` in SeedProtonDeliverableProgressAsync
    - ProtonDataController.cs contains `StatusType = "Pending"` with similar pattern near L492 seed location
    - Both locations use `DeliverableStatusHistories.Add(new DeliverableStatusHistory` pattern
    - Both have a second `SaveChangesAsync()` call after the history insert loop
  </acceptance_criteria>
  <done>Kedua seed locations menghasilkan DeliverableStatusHistory "Pending" awal untuk setiap progress baru</done>
</task>

</tasks>

<verification>
- grep "RecordStatusHistory" CDPController.cs menunjukkan call di UploadEvidence (baru) plus existing calls
- grep "EvidencePathHistory" Models/ProtonModels.cs menunjukkan property baru
- grep "StatusType.*Pending" AdminController.cs dan ProtonDataController.cs menunjukkan seed history
- Build: `dotnet build` berhasil tanpa error
</verification>

<success_criteria>
- Setiap UploadEvidence call menghasilkan StatusHistory entry (Submitted atau Re-submitted)
- Setiap seed menghasilkan StatusHistory "Pending" awal
- File lama tersimpan di EvidencePathHistory saat resubmit
- Upload gagal tidak mengubah status deliverable
- `dotnet build` berhasil
</success_criteria>

<output>
After completion, create `.planning/phases/235-audit-execution-flow/235-01-SUMMARY.md`
</output>
