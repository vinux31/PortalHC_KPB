---
phase: 235-audit-execution-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
autonomous: true
requirements: [EXEC-02, EXEC-04]
must_haves:
  truths:
    - "Dua approver yang approve bersamaan — hanya satu sukses, kedua mendapat error 'sudah diproses'"
    - "HC Review menghasilkan notifikasi ke Coach"
    - "Resubmit setelah reject menghasilkan notifikasi khusus 'Re-submitted' ke reviewers"
    - "CreateHCNotificationAsync dedup menggunakan exact message match bukan Contains"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Race condition guard, HC Review notif, resubmit notif, dedup fix"
  key_links:
    - from: "CDPController.ApproveDeliverable"
      to: "ProtonDeliverableProgresses reload"
      via: "fresh DB query before status write"
      pattern: "sudah diproses oleh approver lain"
    - from: "CDPController.HCReviewDeliverable"
      to: "_notificationService"
      via: "SendAsync call"
      pattern: "HC_REVIEW_COMPLETE"
---

<objective>
Audit dan fix approval chain race condition (EXEC-02) serta notifikasi gaps (EXEC-04).

Purpose: Memastikan concurrent approve tidak menghasilkan state inkonsisten, dan semua transisi state mengirim notifikasi yang tepat.
Output: CDPController dengan race condition guard, HC Review notif, resubmit notif, dan dedup fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-audit-execution-flow/235-CONTEXT.md
@.planning/phases/235-audit-execution-flow/235-RESEARCH.md

@Controllers/CDPController.cs
@Models/UserNotification.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Race condition guard di ApproveDeliverable + audit Admin override flow</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs (ApproveDeliverable sekitar L746-880 — khususnya canApprove logic L771-779, section check L782-789, per-role approval field setting, dan SaveChangesAsync)
  </read_first>
  <action>
**A. Race condition guard (D-10 — first-write-wins):**

Di ApproveDeliverable, setelah per-role approval fields sudah di-set tapi SEBELUM set overall Status dan SaveChangesAsync, tambahkan reload-before-write:

```csharp
// Race condition guard — reload fresh status (D-10)
var freshStatus = await _context.ProtonDeliverableProgresses
    .Where(p => p.Id == progressId)
    .Select(p => new { p.Status, p.SrSpvApprovalStatus, p.ShApprovalStatus })
    .AsNoTracking()
    .FirstOrDefaultAsync();

if (freshStatus == null) return NotFound();

// Re-check: apakah role ini masih bisa approve?
bool isSrSpv = /* existing role check logic */;
bool isSH = /* existing role check logic */;
bool stillCanApprove = freshStatus.Status == "Submitted" ||
    (freshStatus.Status == "Approved" && (
        (isSrSpv && freshStatus.SrSpvApprovalStatus != "Approved") ||
        (isSH && freshStatus.ShApprovalStatus != "Approved")));

if (!stillCanApprove)
{
    TempData["Error"] = "Deliverable sudah diproses oleh approver lain.";
    return RedirectToAction("CoachingProton");
}
```

Catatan: `AsNoTracking()` penting agar tidak conflict dengan entity yang sudah di-track. Variabel `isSrSpv`/`isSH` sudah ada di method — reuse.

**B. Audit Admin override (D-11):**

Investigasi: tidak ada dedicated "AdminOverride" action di CDPController (grep "Override" = 0 results). Admin bypass hanya terjadi via section check skip di L786. Ini adalah by-design — Admin tidak punya separate override path. Dokumentasikan temuan ini di SUMMARY: "Admin override = section bypass only, no dedicated action, acceptable."
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -n "sudah diproses oleh approver lain" Controllers/CDPController.cs && grep -n "AsNoTracking" Controllers/CDPController.cs | grep -i "approve"</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs ApproveDeliverable contains string `"Deliverable sudah diproses oleh approver lain."`
    - CDPController.cs ApproveDeliverable contains `AsNoTracking()` query to reload fresh status before write
    - CDPController.cs ApproveDeliverable contains `stillCanApprove` boolean check with redirect on false
    - Race guard is placed AFTER per-role field sets but BEFORE overall Status assignment and SaveChangesAsync
  </acceptance_criteria>
  <done>ApproveDeliverable memiliki first-write-wins race condition guard, Admin override flow diaudit dan didokumentasikan</done>
</task>

<task type="auto">
  <name>Task 2: Fix notifikasi gaps — HC Review notif, resubmit notif, dedup fix</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs (HCReviewDeliverable sekitar L1048-1082, NotifyReviewersAsync sekitar L988-1014, CreateHCNotificationAsync sekitar L1016-1044, UploadEvidence sekitar L1170-1177 notif section)
    - Models/UserNotification.cs (field list — konfirmasi tidak ada RelatedEntityId)
  </read_first>
  <action>
**A. HC Review notifikasi (D-14, EXEC-04 gap):**

Di HCReviewDeliverable (sekitar L1048-1082), setelah SaveChangesAsync dan RecordStatusHistory, tambahkan notifikasi ke Coach:

```csharp
// Notify Coach bahwa HC sudah review (EXEC-04 gap fix)
try
{
    var coachMapping = await _context.CoachCoacheeMappings
        .FirstOrDefaultAsync(m => m.CoacheeId == progress.CoacheeId && m.IsActive);
    if (coachMapping != null)
    {
        await _notificationService.SendAsync(new UserNotification
        {
            UserId = coachMapping.CoachId,
            Type = "HC_REVIEW_COMPLETE",
            Title = "HC Review Selesai",
            Message = $"Deliverable '{deliverableName}' untuk {coacheeName} telah di-review oleh HC.",
            ActionUrl = $"/CDP/Deliverable/{progress.Id}",
            CreatedAt = DateTime.UtcNow
        });
    }
}
catch { /* Notification failure must not break main operation */ }
```

Catatan: ambil `deliverableName` dan `coacheeName` dari data yang sudah di-load di method. Cek variable names yang ada.

**B. Resubmit notifikasi (D-18):**

Di UploadEvidence, sekitar L1170-1177 di mana `NotifyReviewersAsync` dipanggil, tambahkan conditional message:

```csharp
// Kirim notif khusus resubmit jika wasRejected (D-18)
string notifType = wasRejected ? "COACH_EVIDENCE_RESUBMITTED" : "COACH_EVIDENCE_SUBMITTED";
string notifMessage = wasRejected
    ? $"Evidence deliverable '{deliverableName}' telah diresubmit setelah ditolak oleh {coachName}."
    : $"Evidence deliverable '{deliverableName}' telah disubmit oleh {coachName}.";
```

Modifikasi `NotifyReviewersAsync` call untuk menerima type dan message parameter, ATAU buat notifikasi resubmit terpisah langsung di UploadEvidence sebelum/sesudah existing NotifyReviewersAsync call. Pilih approach yang paling minimal perubahan.

**C. Dedup fix CreateHCNotificationAsync (D-14):**

Di CreateHCNotificationAsync (sekitar L1016-1044), ganti:
```csharp
// SEBELUM (fragile):
.AnyAsync(n => n.Type == "COACH_ALL_COMPLETE" && n.Message.Contains(coacheeId));

// SESUDAH (exact match):
var expectedMessage = $"Semua deliverable {coacheeName} telah selesai";
.AnyAsync(n => n.UserId == hcUserId && n.Type == "COACH_ALL_COMPLETE" && n.Message == expectedMessage);
```

Catatan: `hcUserId` dan `coacheeName` — gunakan variable names yang sudah ada di method scope. Cek exact message format yang digunakan saat create notification di method yang sama.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -n "HC_REVIEW_COMPLETE" Controllers/CDPController.cs && grep -n "RESUBMIT\|Re-submit\|resubmit" Controllers/CDPController.cs | head -5 && grep -n "Message ==" Controllers/CDPController.cs | head -3</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs HCReviewDeliverable contains `Type = "HC_REVIEW_COMPLETE"` notification send
    - CDPController.cs UploadEvidence contains conditional notif type for resubmit (either "COACH_EVIDENCE_RESUBMITTED" string or wasRejected conditional)
    - CDPController.cs CreateHCNotificationAsync uses `n.Message ==` (exact match) instead of `n.Message.Contains(` for dedup check
    - All notification sends are wrapped in try-catch
  </acceptance_criteria>
  <done>HC Review mengirim notif ke Coach, resubmit mengirim notif khusus, dedup menggunakan exact match</done>
</task>

</tasks>

<verification>
- grep "HC_REVIEW_COMPLETE" CDPController.cs — notif HC Review ada
- grep "RESUBMITTED" CDPController.cs — notif resubmit ada
- grep "Message ==" CDPController.cs — dedup pakai exact match
- grep "sudah diproses" CDPController.cs — race condition guard ada
- Build: `dotnet build` berhasil tanpa error
</verification>

<success_criteria>
- Race condition guard aktif di ApproveDeliverable
- HC Review menghasilkan notifikasi
- Resubmit menghasilkan notifikasi khusus
- Dedup notification menggunakan exact match
- `dotnet build` berhasil
</success_criteria>

<output>
After completion, create `.planning/phases/235-audit-execution-flow/235-02-SUMMARY.md`
</output>
