---
phase: 235-audit-execution-flow
plan: 03
type: execute
wave: 2
depends_on: [235-01, 235-02]
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/PlanIdp.cshtml
autonomous: false
requirements: [EXEC-05]
must_haves:
  truths:
    - "PlanIdp menampilkan silabus sesuai track assignment coachee — tidak ada data track lain"
    - "Coach hanya melihat data coachee yang di-map ke mereka"
    - "Coachee tidak bisa akses admin guidance management tab"
    - "Inactive kompetensi tidak tampil di PlanIdp"
  artifacts:
    - path: "Views/CDP/PlanIdp.cshtml"
      provides: "Tab access control berdasarkan UserLevel"
    - path: "Controllers/CDPController.cs"
      provides: "PlanIdp action dengan correct role filtering"
  key_links:
    - from: "Views/CDP/PlanIdp.cshtml"
      to: "ViewBag.UserLevel"
      via: "Razor conditional rendering"
      pattern: "UserLevel.*Admin"
---

<objective>
Audit PlanIdp view — silabus display accuracy, guidance tabs, dan role-based access correctness (EXEC-05).

Purpose: Memastikan PlanIdp menampilkan data yang benar per role dan tidak ada unauthorized access ke admin tabs.
Output: PlanIdp yang diaudit dan diperbaiki jika ada gap, plus human verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-audit-execution-flow/235-CONTEXT.md
@.planning/phases/235-audit-execution-flow/235-RESEARCH.md
@.planning/phases/235-audit-execution-flow/235-01-SUMMARY.md
@.planning/phases/235-audit-execution-flow/235-02-SUMMARY.md

@Controllers/CDPController.cs
@Views/CDP/PlanIdp.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dan fix PlanIdp — role filtering, inactive filter, guidance tab access</name>
  <files>Controllers/CDPController.cs, Views/CDP/PlanIdp.cshtml</files>
  <read_first>
    - Controllers/CDPController.cs (PlanIdp action sekitar L57-208 — khususnya: coachee track lock L82-86, L4 section lock L102-107, guidanceQuery L148-154, ViewBag assignments)
    - Views/CDP/PlanIdp.cshtml (tab rendering logic — cari conditional tabs berdasarkan ViewBag.UserLevel, guidance management tab, admin-specific sections)
  </read_first>
  <action>
**Audit checklist per D-19 sampai D-22:**

**D-19 General audit:**
- Verifikasi silabus query memfilter `IsActive` di level ProtonKompetensi (sudah PASS per research L121)
- Verifikasi SubKompetensi dan Deliverable tidak punya IsActive field (confirmed N/A — model tidak punya)
- Jika ada bug ditemukan saat baca kode, fix langsung

**D-20 Coach role access:**
- Baca guidanceQuery (L148-154) — saat ini hanya handle `isCoachee` dan `isL4`, tidak ada coach-specific filter
- Per research: Coach melihat semua guidance untuk bagiannya — ini acceptable karena Coach perlu akses guidance
- Verifikasi Coach tidak bisa lihat data coachee lain yang bukan mapping-nya — cek query di PlanIdp action apakah ada coach-to-coachee scoping
- Jika Coach bisa lihat semua coachee (bukan hanya mapping-nya), tambah filter:
```csharp
if (isCoach && !isAdmin && !isHC)
{
    var myCoacheeIds = await _context.CoachCoacheeMappings
        .Where(m => m.CoachId == userId && m.IsActive)
        .Select(m => m.CoacheeId)
        .ToListAsync();
    // Filter viewmodel to only show mapped coachees
}
```

**D-21 Inactive silabus filtering:**
- Sudah PASS di Kompetensi level. Tidak perlu tambahan.

**D-22 Guidance tab access — admin management tab:**
- Baca PlanIdp.cshtml — cari tab/section yang menampilkan guidance management (CRUD guidance)
- Pastikan ada Razor conditional: hanya Admin/HC yang bisa lihat management tab
- Jika coachee bisa lihat admin guidance management tab, tambah conditional:
```html
@if (ViewBag.UserLevel == "Admin" || ViewBag.UserLevel == "HC")
{
    <!-- Guidance management tab content -->
}
```

**Fix semua gap yang ditemukan.** Jika tidak ada gap selain yang sudah diketahui, dokumentasikan "PASS" di SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -n "UserLevel" Views/CDP/PlanIdp.cshtml | head -10 && grep -n "IsActive\|isCoach\|CoachId" Controllers/CDPController.cs | grep -i "plan" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - PlanIdp.cshtml guidance management tab (if exists) has conditional check on ViewBag.UserLevel for Admin/HC only
    - CDPController.cs PlanIdp action either already scopes Coach to their mapped coachees OR fix is added
    - No compilation errors from `dotnet build`
    - All D-19 through D-22 findings documented in code comments or SUMMARY
  </acceptance_criteria>
  <done>PlanIdp diaudit — role filtering, inactive filter, dan tab access semuanya benar atau sudah diperbaiki</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Audit execution flow lengkap: evidence submission + StatusHistory + approval chain race guard + notifikasi + PlanIdp view</what-built>
  <how-to-verify>
    1. Login sebagai Coach, upload evidence ke deliverable Pending — pastikan berhasil
    2. Reject deliverable tersebut (login SrSpv), lalu resubmit sebagai Coach — pastikan berhasil
    3. Cek di database: DeliverableStatusHistory memiliki entry untuk semua transisi (Pending, Submitted, Rejected, Re-submitted)
    4. Login sebagai Coachee, buka PlanIdp — pastikan hanya lihat track miliknya dan tidak ada admin tab
    5. Verifikasi `dotnet build` berhasil tanpa error
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- PlanIdp menampilkan data yang benar per role
- Admin guidance tab tidak visible untuk Coachee
- `dotnet build` berhasil
- Semua D-19 sampai D-22 diaudit dan didokumentasikan
</verification>

<success_criteria>
- PlanIdp role filtering diverifikasi dan diperbaiki jika ada gap
- Human verification approved untuk keseluruhan Phase 235
- Build berhasil
</success_criteria>

<output>
After completion, create `.planning/phases/235-audit-execution-flow/235-03-SUMMARY.md`
</output>
