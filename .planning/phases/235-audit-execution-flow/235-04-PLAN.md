---
phase: 235
plan: "04"
title: "Gap closure: COACH_EVIDENCE_RESUBMITTED notification di SubmitEvidenceWithCoaching"
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
requirements_addressed: [EXEC-04]
autonomous: true
type: gap_closure
---

# Plan 235-04: Fix resubmit notification di SubmitEvidenceWithCoaching

## Objective

`SubmitEvidenceWithCoaching` (batch endpoint) harus mengirim `COACH_EVIDENCE_RESUBMITTED` (bukan `COACH_EVIDENCE_SUBMITTED`) ketika deliverable yang disubmit berstatus `Rejected`. Saat ini hanya memanggil `NotifyReviewersAsync` yang selalu mengirim `COACH_EVIDENCE_SUBMITTED`.

## Source

- UAT test #6 (235-UAT.md): SubmitEvidenceWithCoaching mengirim COACH_EVIDENCE_SUBMITTED saat re-submit deliverable rejected
- Reference implementation: `UploadEvidence` endpoint (CDPController.cs:1265-1294) sudah memiliki logika resubmit notification yang benar

## Tasks

<task id="1">
<title>Tambah logika resubmit notification di SubmitEvidenceWithCoaching</title>
<read_first>
- Controllers/CDPController.cs (lines 2150-2218 — SubmitEvidenceWithCoaching processing loop + notification block)
- Controllers/CDPController.cs (lines 1265-1294 — UploadEvidence resubmit notification reference)
</read_first>
<action>
Di `SubmitEvidenceWithCoaching`, dalam foreach loop (line 2153), variabel `isResubmit` sudah ada (line 2156). Kumpulkan info resubmit per coachee.

Setelah `SaveChangesAsync()` (line 2201), dalam blok notifikasi (line 2203-2218):

1. Track mana coachee yang punya deliverable resubmit: sebelum loop notification, buat dictionary resubmit coachee IDs dari `progresses`:
```csharp
var resubmittedCoacheeIds = progresses
    .Where(p => p.Status == "Submitted") // sudah diubah, tapi cek wasRejected flag
    .Select(p => p.CoacheeId)
    .Distinct()
    .ToHashSet();
```

Perlu track `wasRejected` state sebelum status diubah. Tambahkan tracking sebelum foreach loop:
```csharp
var resubmitFlags = progresses.ToDictionary(p => p.Id, p => p.Status == "Rejected");
```
Pindahkan ini ke SEBELUM foreach loop (sebelum line 2153), karena setelah loop status sudah berubah ke "Submitted".

Lalu setelah `NotifyReviewersAsync` calls, tambahkan blok resubmit notification:
```csharp
// Notify reviewers with COACH_EVIDENCE_RESUBMITTED for resubmitted deliverables
var resubmittedCoacheeIds = progresses
    .Where(p => resubmitFlags.ContainsKey(p.Id) && resubmitFlags[p.Id])
    .Select(p => p.CoacheeId)
    .Distinct()
    .ToList();

foreach (var cid in resubmittedCoacheeIds)
{
    var c = coacheeNames.FirstOrDefault(x => x.Id == cid);
    var cName = c?.FullName ?? c?.UserName ?? cid;

    // Get deliverable names for this coachee's resubmitted items
    var resubDeliverables = progresses
        .Where(p => p.CoacheeId == cid && resubmitFlags.ContainsKey(p.Id) && resubmitFlags[p.Id])
        .Select(p => p.ProtonDeliverable?.NamaDeliverable ?? $"#{p.Id}")
        .ToList();

    var mapping = await _context.CoachCoacheeMappings
        .FirstOrDefaultAsync(m => m.CoacheeId == cid && m.IsActive);
    if (mapping == null) continue;

    var reviewerIds = await _context.Users
        .Where(u => u.IsActive && u.Section == mapping.AssignmentSection && u.RoleLevel == 4)
        .Select(u => u.Id)
        .ToListAsync();

    foreach (var reviewerId in reviewerIds)
    {
        await _notificationService.SendAsync(
            reviewerId,
            "COACH_EVIDENCE_RESUBMITTED",
            "Evidence Diresubmit",
            $"Evidence deliverable '{string.Join(", ", resubDeliverables)}' untuk {cName} telah diresubmit setelah ditolak.",
            "/CDP/CoachingProton"
        );
    }
}
```

Letakkan blok ini di dalam try-catch yang sama (sebelum closing `catch` di line 2218).
</action>
<acceptance_criteria>
- `CDPController.cs` contains `resubmitFlags = progresses.ToDictionary` before the foreach processing loop
- `CDPController.cs` contains `"COACH_EVIDENCE_RESUBMITTED"` in the SubmitEvidenceWithCoaching method (after line 2200)
- `CDPController.cs` contains `resubmittedCoacheeIds` variable in SubmitEvidenceWithCoaching
- Build succeeds: `dotnet build` exits 0 (excluding MSB3027 locked-file warnings)
</acceptance_criteria>
</task>

## Verification

```bash
grep -n "COACH_EVIDENCE_RESUBMITTED" Controllers/CDPController.cs
# Expected: at least 2 matches — one in UploadEvidence (~1286), one in SubmitEvidenceWithCoaching (new)
```

## must_haves

- [ ] SubmitEvidenceWithCoaching sends COACH_EVIDENCE_RESUBMITTED (not COACH_EVIDENCE_SUBMITTED) when deliverable was previously Rejected
- [ ] Resubmit flag captured BEFORE status is overwritten to "Submitted"
- [ ] Build compiles without errors
