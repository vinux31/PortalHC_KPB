---
phase: 236-audit-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Data/ApplicationDbContext.cs
  - Models/CoachCoacheeMapping.cs
  - Migrations/
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04]

must_haves:
  truths:
    - "ProtonFinalAssessment tidak bisa punya dua record dengan ProtonTrackAssignmentId yang sama di DB"
    - "CoachCoacheeMapping punya field IsCompleted dan CompletedAt untuk menandai graduated"
  artifacts:
    - path: "Data/ApplicationDbContext.cs"
      provides: "Unique index pada ProtonFinalAssessment.ProtonTrackAssignmentId"
      contains: "entity.HasIndex(fa => fa.ProtonTrackAssignmentId).IsUnique()"
    - path: "Models/CoachCoacheeMapping.cs"
      provides: "IsCompleted dan CompletedAt fields"
      contains: "public bool IsCompleted"
    - path: "Migrations/"
      provides: "Migration file untuk unique index dan field baru"
  key_links:
    - from: "Data/ApplicationDbContext.cs"
      to: "Models/ProtonModels.cs"
      via: "HasIndex unique constraint config"
      pattern: "HasIndex.*ProtonTrackAssignmentId.*IsUnique"
---

<objective>
Tambah DB migration untuk: (1) unique index ProtonFinalAssessment.ProtonTrackAssignmentId (COMP-01, D-01), (2) field IsCompleted + CompletedAt di CoachCoacheeMapping (COMP-04, D-15).

Purpose: Fondasi DB-level enforcement sebelum controller fixes di Plan 02/03.
Output: Migration file, updated DbContext config, updated model.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/236-audit-completion/236-CONTEXT.md
@.planning/phases/236-audit-completion/236-RESEARCH.md

<interfaces>
From Data/ApplicationDbContext.cs L352-361:
```csharp
builder.Entity<ProtonFinalAssessment>(entity =>
{
    entity.HasOne(fa => fa.ProtonTrackAssignment)
        .WithMany()
        .HasForeignKey(fa => fa.ProtonTrackAssignmentId)
        .OnDelete(DeleteBehavior.Restrict);
    entity.HasIndex(fa => fa.CoacheeId);
    entity.HasIndex(fa => new { fa.CoacheeId, fa.Status });
});
```

From Models/CoachCoacheeMapping.cs:
```csharp
public class CoachCoacheeMapping
{
    public int Id { get; set; }
    public string CoachId { get; set; } = "";
    public string CoacheeId { get; set; } = "";
    public bool IsActive { get; set; } = true;
    public DateTime StartDate { get; set; }
    public DateTime? EndDate { get; set; }
    public string? AssignmentSection { get; set; }
    public string? AssignmentUnit { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update model dan DbContext config</name>
  <read_first>
    - Models/CoachCoacheeMapping.cs
    - Data/ApplicationDbContext.cs (L352-361 ProtonFinalAssessment config)
  </read_first>
  <files>Models/CoachCoacheeMapping.cs, Data/ApplicationDbContext.cs</files>
  <action>
1. Di Models/CoachCoacheeMapping.cs, tambah 2 property sebelum closing brace class:
```csharp
/// <summary>True jika coachee sudah selesai Tahun 3 (graduated). Per D-15.</summary>
public bool IsCompleted { get; set; } = false;
/// <summary>Tanggal completion dikonfirmasi oleh HC/Admin.</summary>
public DateTime? CompletedAt { get; set; }
```

2. Di Data/ApplicationDbContext.cs, dalam blok `builder.Entity<ProtonFinalAssessment>(entity =>` (sekitar L352-361), tambah baris baru setelah `entity.HasIndex(fa => new { fa.CoacheeId, fa.Status });`:
```csharp
// Phase 236 COMP-01: unique constraint per D-01
entity.HasIndex(fa => fa.ProtonTrackAssignmentId).IsUnique();
```
  </action>
  <verify>
    <automated>grep -n "IsCompleted" Models/CoachCoacheeMapping.cs && grep -n "ProtonTrackAssignmentId.*IsUnique" Data/ApplicationDbContext.cs</automated>
  </verify>
  <acceptance_criteria>
    - Models/CoachCoacheeMapping.cs contains `public bool IsCompleted { get; set; } = false;`
    - Models/CoachCoacheeMapping.cs contains `public DateTime? CompletedAt { get; set; }`
    - Data/ApplicationDbContext.cs contains `entity.HasIndex(fa => fa.ProtonTrackAssignmentId).IsUnique();`
  </acceptance_criteria>
  <done>Model dan DbContext config updated — siap untuk migration generation</done>
</task>

<task type="auto">
  <name>Task 2: Generate dan apply EF Core migration</name>
  <read_first>
    - Data/ApplicationDbContext.cs (verify changes from Task 1)
    - Models/CoachCoacheeMapping.cs (verify changes from Task 1)
  </read_first>
  <files>Migrations/</files>
  <action>
1. Jalankan: `dotnet ef migrations add Phase236_UniqueAssignment_CompletedMapping`
2. Jalankan: `dotnet ef database update`
3. Jika migration gagal karena duplikat ProtonFinalAssessment data, jalankan SQL query audit dulu:
```sql
SELECT ProtonTrackAssignmentId, COUNT(*) as cnt FROM ProtonFinalAssessments GROUP BY ProtonTrackAssignmentId HAVING COUNT(*) > 1;
```
Jika ada duplikat, hapus record duplikat yang lebih lama (keep yang terbaru berdasarkan CreatedAt) sebelum apply migration.
  </action>
  <verify>
    <automated>dotnet ef migrations list 2>&1 | grep -i "Phase236"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists in Migrations/ with name containing "Phase236_UniqueAssignment_CompletedMapping"
    - `dotnet ef database update` completes without error
    - Migrations/ApplicationDbContextModelSnapshot.cs contains `IsCompleted` and unique index on ProtonTrackAssignmentId
  </acceptance_criteria>
  <done>Migration applied — unique constraint dan IsCompleted/CompletedAt field aktif di DB</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- Migration snapshot contains unique index on ProtonTrackAssignmentId
- Migration snapshot contains IsCompleted column on CoachCoacheeMappings
</verification>

<success_criteria>
- DB-level unique constraint pada ProtonFinalAssessment.ProtonTrackAssignmentId aktif
- CoachCoacheeMapping punya field IsCompleted (bool) dan CompletedAt (DateTime?)
- Project builds tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/236-audit-completion/236-01-SUMMARY.md`
</output>
