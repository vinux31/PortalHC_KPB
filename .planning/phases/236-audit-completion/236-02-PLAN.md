---
phase: 236-audit-completion
plan: 02
type: execute
wave: 2
depends_on: [236-01]
files_modified:
  - Controllers/CDPController.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04]

must_haves:
  truths:
    - "Query final assessment di BuildCoacheeProgressSubModelAsync scope ke assignment aktif, bukan ambil terbaru per coachee"
    - "Coach bisa edit/delete coaching session miliknya dengan audit log"
    - "HC/Admin bisa mark mapping sebagai completed setelah Tahun 3 selesai"
    - "Completion criteria = semua deliverable Approved DAN final assessment ada"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "EditCoachingSession, DeleteCoachingSession actions + fixed L365 query"
      contains: "EditCoachingSession"
    - path: "Controllers/AdminController.cs"
      provides: "MarkMappingCompleted action + IsYearCompletedAsync helper"
      contains: "MarkMappingCompleted"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "IAuditLogService"
      via: "_auditLog.LogAsync"
      pattern: "_auditLog.LogAsync.*EditCoachingSession"
    - from: "Controllers/AdminController.cs"
      to: "Models/CoachCoacheeMapping.cs"
      via: "IsCompleted = true"
      pattern: "IsCompleted = true"
---

<objective>
Controller-level fixes: (1) fix query L365 scope per assignment (COMP-01, D-02), (2) tambah EditCoachingSession/DeleteCoachingSession dengan audit log (COMP-02, D-07), (3) tambah MarkMappingCompleted + IsYearCompletedAsync helper (COMP-04, D-13/D-15).

Purpose: Controller logic fixes setelah DB schema siap dari Plan 01.
Output: Fixed query, session CRUD actions, completion flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/236-audit-completion/236-CONTEXT.md
@.planning/phases/236-audit-completion/236-RESEARCH.md
@.planning/phases/236-audit-completion/236-01-SUMMARY.md

<interfaces>
From Models/CoachingSession.cs:
```csharp
public class CoachingSession
{
    public int Id { get; set; }
    public string CoachId { get; set; } = "";
    public string CoacheeId { get; set; } = "";
    public DateTime Date { get; set; }
    public string Kompetensi { get; set; } = "";
    public string SubKompetensi { get; set; } = "";
    public string Deliverable { get; set; } = "";
    public string CatatanCoach { get; set; } = "";
    public string Kesimpulan { get; set; } = "";
    public string Result { get; set; } = "";
    public string? AcuanPedoman { get; set; }
    public string? AcuanTko { get; set; }
    public string? AcuanBestPractice { get; set; }
    public string? AcuanDokumen { get; set; }
    public string Status { get; set; } = "Draft";
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime? UpdatedAt { get; set; }
    public int? ProtonDeliverableProgressId { get; set; }
    public ICollection<ActionItem> ActionItems { get; set; } = new List<ActionItem>();
}
```

From CDPController.cs L365-373 (BuildCoacheeProgressSubModelAsync — buggy query):
```csharp
var finalAssessment = await _context.ProtonFinalAssessments
    .Where(fa => fa.CoacheeId == userId)
    .OrderByDescending(fa => fa.CreatedAt)
    .FirstOrDefaultAsync();
subModel.CompetencyLevelGranted = finalAssessment?.CompetencyLevelGranted;
subModel.CurrentStatus = finalAssessment != null ? "Completed" : "In Progress";
```

From AdminController.cs L2526-2543 (existing guard pattern):
```csharp
var alreadyExists = await _context.ProtonFinalAssessments
    .AnyAsync(fa => fa.ProtonTrackAssignmentId == assignment.Id);
if (!alreadyExists) { /* create */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix query L365 + tambah EditCoachingSession/DeleteCoachingSession di CDPController</name>
  <read_first>
    - Controllers/CDPController.cs (L360-374 BuildCoacheeProgressSubModelAsync, L2177-2204 SubmitEvidenceWithCoaching untuk session creation pattern, L707 area untuk session reference)
  </read_first>
  <files>Controllers/CDPController.cs</files>
  <action>
**Fix 1 — Query L365 (COMP-01, D-02):**
Di `BuildCoacheeProgressSubModelAsync`, ganti query di L365-368:
```csharp
// SEBELUM (buggy — mengambil assessment terbaru per coachee, bukan per assignment):
var finalAssessment = await _context.ProtonFinalAssessments
    .Where(fa => fa.CoacheeId == userId)
    .OrderByDescending(fa => fa.CreatedAt)
    .FirstOrDefaultAsync();
```
Dengan query yang scope ke assignment aktif. Perlu cari assignment ID di method ini — lihat bagaimana `progresses` di-query (pasti ada `assignmentId` atau `ProtonTrackAssignmentId`). Ganti menjadi:
```csharp
// Phase 236 COMP-01 fix: scope ke assignment aktif per D-02
var activeAssignmentId = progresses.FirstOrDefault()?.ProtonTrackAssignmentId ?? 0;
var finalAssessment = await _context.ProtonFinalAssessments
    .FirstOrDefaultAsync(fa => fa.ProtonTrackAssignmentId == activeAssignmentId);
```

**Fix 2 — Tambah EditCoachingSession (COMP-02, D-07):**
Tambah setelah area coaching session existing (cari tempat yang natural, dekat SubmitEvidenceWithCoaching). Tambah GET dan POST actions:

```csharp
// Phase 236 COMP-02: Edit coaching session per D-07
[HttpGet]
public async Task<IActionResult> EditCoachingSession(int id)
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();
    var session = await _context.CoachingSessions
        .Include(s => s.ActionItems)
        .FirstOrDefaultAsync(s => s.Id == id);
    if (session == null) return NotFound();
    bool isHcOrAdmin = User.IsInRole("HC") || User.IsInRole("Admin");
    if (!isHcOrAdmin && session.CoachId != user.Id) return Forbid();
    return View(session);
}

[HttpPost]
[ValidateAntiForgeryToken]
public async Task<IActionResult> EditCoachingSession(int id, string catatanCoach, string kesimpulan, string result)
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();
    var session = await _context.CoachingSessions.FindAsync(id);
    if (session == null) return NotFound();
    bool isHcOrAdmin = User.IsInRole("HC") || User.IsInRole("Admin");
    if (!isHcOrAdmin && session.CoachId != user.Id) return Forbid();
    session.CatatanCoach = catatanCoach;
    session.Kesimpulan = kesimpulan;
    session.Result = result;
    session.UpdatedAt = DateTime.UtcNow;
    await _context.SaveChangesAsync();
    var actorName = string.IsNullOrWhiteSpace(user.NIP)
        ? (user.FullName ?? "Unknown")
        : $"{user.NIP} - {user.FullName}";
    await _auditLog.LogAsync(user.Id, actorName, "EditCoachingSession",
        $"Session ID={id} diubah. Kesimpulan={kesimpulan}, Result={result}", id, "CoachingSession");
    TempData["Success"] = "Sesi coaching berhasil diperbarui.";
    return RedirectToAction("Deliverable", new { id = session.ProtonDeliverableProgressId });
}
```

**Fix 3 — Tambah DeleteCoachingSession (COMP-02, D-07):**
```csharp
// Phase 236 COMP-02: Delete coaching session per D-07
[HttpPost]
[ValidateAntiForgeryToken]
public async Task<IActionResult> DeleteCoachingSession(int id)
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();
    var session = await _context.CoachingSessions
        .Include(s => s.ActionItems)
        .FirstOrDefaultAsync(s => s.Id == id);
    if (session == null) return NotFound();
    bool isHcOrAdmin = User.IsInRole("HC") || User.IsInRole("Admin");
    if (!isHcOrAdmin && session.CoachId != user.Id) return Forbid();
    var progressId = session.ProtonDeliverableProgressId;
    await using var tx = await _context.Database.BeginTransactionAsync();
    try
    {
        _context.ActionItems.RemoveRange(session.ActionItems);
        _context.CoachingSessions.Remove(session);
        await _context.SaveChangesAsync();
        var actorName = string.IsNullOrWhiteSpace(user.NIP)
            ? (user.FullName ?? "Unknown")
            : $"{user.NIP} - {user.FullName}";
        await _auditLog.LogAsync(user.Id, actorName, "DeleteCoachingSession",
            $"Session ID={id} dihapus.", id, "CoachingSession");
        await tx.CommitAsync();
    }
    catch { await tx.RollbackAsync(); throw; }
    TempData["Success"] = "Sesi coaching berhasil dihapus.";
    return RedirectToAction("Deliverable", new { id = progressId });
}
```
  </action>
  <verify>
    <automated>grep -n "EditCoachingSession\|DeleteCoachingSession\|Phase 236 COMP-01 fix" Controllers/CDPController.cs | head -10</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs contains `public async Task<IActionResult> EditCoachingSession(int id`
    - CDPController.cs contains `public async Task<IActionResult> DeleteCoachingSession(int id)`
    - CDPController.cs L365 area no longer contains `.Where(fa => fa.CoacheeId == userId)` for final assessment query — replaced with ProtonTrackAssignmentId scope
    - CDPController.cs contains `_auditLog.LogAsync(user.Id, actorName, "EditCoachingSession"`
    - CDPController.cs contains `_auditLog.LogAsync(user.Id, actorName, "DeleteCoachingSession"`
  </acceptance_criteria>
  <done>Query L365 fixed per assignment, session edit/delete with audit log added</done>
</task>

<task type="auto">
  <name>Task 2: Tambah MarkMappingCompleted + IsYearCompletedAsync di AdminController</name>
  <read_first>
    - Controllers/AdminController.cs (L2515-2546 SubmitInterviewResults final assessment creation, search for CoachCoacheeMapping actions)
    - Models/CoachCoacheeMapping.cs (verify IsCompleted field from Plan 01)
  </read_first>
  <files>Controllers/AdminController.cs</files>
  <action>
**Fix 1 — Tambah helper IsYearCompletedAsync (COMP-04, D-13):**
Tambah private method di AdminController:
```csharp
// Phase 236 COMP-04: completion criteria helper per D-13
private async Task<bool> IsYearCompletedAsync(int assignmentId)
{
    var progresses = await _context.ProtonDeliverableProgresses
        .Where(p => p.ProtonTrackAssignmentId == assignmentId)
        .ToListAsync();
    if (!progresses.Any()) return false;
    bool allApproved = progresses.All(p => p.Status == "Approved");
    bool hasFinalAssessment = await _context.ProtonFinalAssessments
        .AnyAsync(fa => fa.ProtonTrackAssignmentId == assignmentId);
    return allApproved && hasFinalAssessment;
}
```

**Fix 2 — Tambah MarkMappingCompleted action (COMP-04, D-15):**
```csharp
// Phase 236 COMP-04: Mark mapping as completed/graduated per D-15
[HttpPost]
[ValidateAntiForgeryToken]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> MarkMappingCompleted(int mappingId)
{
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return Challenge();
    var mapping = await _context.CoachCoacheeMappings.FindAsync(mappingId);
    if (mapping == null) return NotFound();
    // Validate: coachee harus punya semua tahun completed
    var assignments = await _context.ProtonTrackAssignments
        .Include(a => a.ProtonTrack)
        .Where(a => a.CoacheeId == mapping.CoacheeId && a.IsActive)
        .ToListAsync();
    var tahun3Assignment = assignments
        .FirstOrDefault(a => a.ProtonTrack != null && a.ProtonTrack.TahunKe == "Tahun 3");
    if (tahun3Assignment == null)
    {
        TempData["Error"] = "Coachee belum memiliki assignment Tahun 3.";
        return RedirectToAction("CoachCoacheeMapping");
    }
    bool tahun3Complete = await IsYearCompletedAsync(tahun3Assignment.Id);
    if (!tahun3Complete)
    {
        TempData["Error"] = "Tahun 3 belum selesai — semua deliverable harus Approved dan final assessment harus ada.";
        return RedirectToAction("CoachCoacheeMapping");
    }
    mapping.IsCompleted = true;
    mapping.CompletedAt = DateTime.UtcNow;
    await _context.SaveChangesAsync();
    var actorName = string.IsNullOrWhiteSpace(user.NIP)
        ? (user.FullName ?? "Unknown")
        : $"{user.NIP} - {user.FullName}";
    await _auditLog.LogAsync(user.Id, actorName, "MarkMappingCompleted",
        $"Mapping ID={mappingId} ditandai graduated. CoacheeId={mapping.CoacheeId}", mappingId, "CoachCoacheeMapping");
    TempData["Success"] = "Coachee berhasil ditandai sebagai graduated.";
    return RedirectToAction("CoachCoacheeMapping");
}
```
  </action>
  <verify>
    <automated>grep -n "MarkMappingCompleted\|IsYearCompletedAsync" Controllers/AdminController.cs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `private async Task<bool> IsYearCompletedAsync(int assignmentId)`
    - AdminController.cs contains `public async Task<IActionResult> MarkMappingCompleted(int mappingId)`
    - AdminController.cs contains `[Authorize(Roles = "Admin, HC")]` before MarkMappingCompleted
    - AdminController.cs contains `mapping.IsCompleted = true;`
    - AdminController.cs contains `mapping.CompletedAt = DateTime.UtcNow;`
  </acceptance_criteria>
  <done>Completion helper dan MarkMappingCompleted action tersedia, role-guarded, dengan audit log</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- CDPController has EditCoachingSession and DeleteCoachingSession actions
- AdminController has MarkMappingCompleted and IsYearCompletedAsync
- L365 query scoped to assignment, not coachee
</verification>

<success_criteria>
- Final assessment query di BuildCoacheeProgressSubModelAsync scope ke ProtonTrackAssignmentId
- Coach bisa edit/delete session dengan ownership check dan audit log
- HC/Admin bisa mark mapping completed setelah validasi Tahun 3
- Completion criteria (all Approved + final assessment) di-enforce via helper
</success_criteria>

<output>
After completion, create `.planning/phases/236-audit-completion/236-02-SUMMARY.md`
</output>
