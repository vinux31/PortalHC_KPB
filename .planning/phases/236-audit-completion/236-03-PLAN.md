---
phase: 236-audit-completion
plan: 03
type: execute
wave: 2
depends_on: [236-01]
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/HistoriProtonDetail.cshtml
autonomous: true
requirements: [COMP-03, COMP-04]

must_haves:
  truths:
    - "Status Lulus di HistoriProton berdasarkan completion criteria lengkap (semua deliverable Approved + final assessment)"
    - "HistoriProtonDetail menampilkan section separator per tahun"
    - "HistoriProton view dan ExportHistoriProton menghasilkan data yang konsisten"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "Fixed Lulus logic di HistoriProton, HistoriProtonDetail, ExportHistoriProton"
      contains: "allDeliverableApproved"
    - path: "Views/CDP/HistoriProtonDetail.cshtml"
      provides: "Section separator visual per tahun (D-12)"
      contains: "TahunKe"
  key_links:
    - from: "Controllers/CDPController.cs (HistoriProton)"
      to: "ProtonDeliverableProgresses"
      via: "completion criteria check"
      pattern: "allDeliverableApproved.*hasAssessment"
---

<objective>
Fix status "Lulus" logic di HistoriProton/HistoriProtonDetail/ExportHistoriProton agar berdasarkan completion criteria lengkap (COMP-03, D-13), tambah section separator per tahun di detail view (COMP-03, D-12).

Purpose: Akurasi timeline dan status display di HistoriProton.
Output: Fixed status logic, section separators, konsisten view/export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/236-audit-completion/236-CONTEXT.md
@.planning/phases/236-audit-completion/236-RESEARCH.md
@.planning/phases/236-audit-completion/236-01-SUMMARY.md

<interfaces>
From CDPController.cs L2870-2898 (HistoriProton — current "Lulus" logic per tahun):
```csharp
foreach (var a in coacheeAssignments)
{
    if (a.ProtonTrack == null) continue;
    string tahunKe = a.ProtonTrack.TahunKe;
    bool hasAssessment = assessmentsByAssignmentId.ContainsKey(a.Id);
    if (tahunKe == "Tahun 1")
    {
        if (hasAssessment) tahun1Done = true;
        else tahun1InProgress = true;
    }
    // ... same for Tahun 2, 3
}
// Overall status: hanya cek latest assignment punya assessment
string status = assessmentsByAssignmentId.ContainsKey(latestAssignment.Id) ? "Lulus" : "Dalam Proses";
```

From CDPController.cs L3153-3162 (HistoriProtonDetail — node status):
```csharp
var nodes = assignments.Select(a => {
    var hasAssessment = assessments.TryGetValue(a.Id, out var fa);
    return new ProtonTimelineNode
    {
        Status = hasAssessment ? "Lulus" : "Dalam Proses",
        CompetencyLevel = hasAssessment ? fa!.CompetencyLevelGranted : null,
        // ...
    };
}).OrderBy(n => n.TahunUrutan).ToList();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Lulus logic di HistoriProton, HistoriProtonDetail, ExportHistoriProton</name>
  <read_first>
    - Controllers/CDPController.cs (L2840-2920 HistoriProton, L2975-3040 ExportHistoriProton, L3095-3180 HistoriProtonDetail)
  </read_first>
  <files>Controllers/CDPController.cs</files>
  <action>
**Konteks:** Saat ini status "Lulus" hanya cek ada/tidaknya ProtonFinalAssessment. Per D-13, harus juga cek semua deliverable Approved.

**Fix di 3 tempat:**

1. **HistoriProton (L2867-2898):** Setelah `assessmentsByAssignmentId` sudah di-build (L2844), tambah query ProtonDeliverableProgresses per assignment:
```csharp
// Phase 236 COMP-03/COMP-04: load progresses untuk completion criteria per D-13
var allProgresses = await _context.ProtonDeliverableProgresses
    .Where(p => assignmentIds.Contains(p.ProtonTrackAssignmentId))
    .ToListAsync();
var progressesByAssignment = allProgresses.GroupBy(p => p.ProtonTrackAssignmentId)
    .ToDictionary(g => g.Key, g => g.ToList());
```
Kemudian di loop `foreach (var a in coacheeAssignments)`, ganti `bool hasAssessment = assessmentsByAssignmentId.ContainsKey(a.Id);` menjadi:
```csharp
bool hasAssessment = assessmentsByAssignmentId.ContainsKey(a.Id);
bool allDeliverableApproved = progressesByAssignment.TryGetValue(a.Id, out var progs)
    && progs.Any() && progs.All(p => p.Status == "Approved");
bool yearComplete = hasAssessment && allDeliverableApproved;
```
Dan ganti `if (hasAssessment) tahun1Done = true;` menjadi `if (yearComplete) tahun1Done = true;` (sama untuk Tahun 2, 3).

Untuk overall status (L2894-2898), ganti:
```csharp
// Phase 236: completion criteria = deliverable approved + final assessment per D-13
bool latestHasAssessment = assessmentsByAssignmentId.ContainsKey(latestAssignment.Id);
bool latestAllApproved = progressesByAssignment.TryGetValue(latestAssignment.Id, out var latestProgs)
    && latestProgs.Any() && latestProgs.All(p => p.Status == "Approved");
string status = (latestHasAssessment && latestAllApproved) ? "Lulus" : "Dalam Proses";
```

2. **ExportHistoriProton (L3006-3032):** Terapkan perubahan yang IDENTIK — logic ini duplikat dari HistoriProton. Tambah query `allProgresses`/`progressesByAssignment` setelah `assessmentsByAssignmentId` (L2987), dan ganti `hasAssessment` checks yang sama.

3. **HistoriProtonDetail (L3133-3167):** Tambah query progresses:
```csharp
// Phase 236 COMP-03: load progresses untuk completion criteria per D-13
var allProgresses = await _context.ProtonDeliverableProgresses
    .Where(p => assignmentIds.Contains(p.ProtonTrackAssignmentId))
    .ToListAsync();
var progressesByAssignment = allProgresses.GroupBy(p => p.ProtonTrackAssignmentId)
    .ToDictionary(g => g.Key, g => g.ToList());
```
Di node builder (L3153), ganti:
```csharp
var hasAssessment = assessments.TryGetValue(a.Id, out var fa);
bool allDeliverableApproved = progressesByAssignment.TryGetValue(a.Id, out var progs)
    && progs.Any() && progs.All(p => p.Status == "Approved");
bool yearComplete = hasAssessment && allDeliverableApproved;
return new ProtonTimelineNode
{
    // ...
    Status = yearComplete ? "Lulus" : "Dalam Proses",
    CompetencyLevel = hasAssessment ? fa!.CompetencyLevelGranted : null,
    // ...
};
```
  </action>
  <verify>
    <automated>grep -c "allDeliverableApproved" Controllers/CDPController.cs</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs contains `allDeliverableApproved` in at least 3 places (HistoriProton, ExportHistoriProton, HistoriProtonDetail)
    - CDPController.cs HistoriProton area contains `bool yearComplete = hasAssessment && allDeliverableApproved;`
    - CDPController.cs ExportHistoriProton area contains same `yearComplete` pattern
    - CDPController.cs HistoriProtonDetail area contains `Status = yearComplete ? "Lulus" : "Dalam Proses"`
    - `dotnet build` succeeds
  </acceptance_criteria>
  <done>Status Lulus berdasarkan completion criteria lengkap (semua deliverable Approved + final assessment) di ketiga action</done>
</task>

<task type="auto">
  <name>Task 2: Tambah section separator per tahun di HistoriProtonDetail view</name>
  <read_first>
    - Views/CDP/HistoriProtonDetail.cshtml
    - Models/HistoriProtonDetailViewModel.cs (ProtonTimelineNode structure)
  </read_first>
  <files>Views/CDP/HistoriProtonDetail.cshtml</files>
  <action>
Per D-12: multi-year coachee harus tampilkan data terpisah per tahun dengan section header.

Di view HistoriProtonDetail.cshtml, cari loop yang iterasi `Model.Nodes`. Ganti dari flat loop menjadi grouped by TahunKe:

```razor
@{
    var groupedByTahun = Model.Nodes.GroupBy(n => n.TahunKe).OrderBy(g => g.First().TahunUrutan);
}
@foreach (var tahunGroup in groupedByTahun)
{
    <div class="card mb-4">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">@tahunGroup.Key</h5>
        </div>
        <div class="card-body">
            @foreach (var node in tahunGroup)
            {
                <!-- existing node rendering code here -->
            }
        </div>
    </div>
}
```

Pastikan existing node rendering (status badge, competency level, dates) tetap sama — hanya bungkus dalam card per tahun group.
  </action>
  <verify>
    <automated>grep -c "GroupBy.*TahunKe\|groupedByTahun\|tahunGroup" Views/CDP/HistoriProtonDetail.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Views/CDP/HistoriProtonDetail.cshtml contains `GroupBy(n => n.TahunKe)`
    - Views/CDP/HistoriProtonDetail.cshtml contains `card-header` with TahunKe display
    - Views/CDP/HistoriProtonDetail.cshtml contains `@tahunGroup.Key` for section header text
    - `dotnet build` succeeds
  </acceptance_criteria>
  <done>HistoriProtonDetail menampilkan section separator per tahun — Tahun 1, Tahun 2, Tahun 3 sebagai card terpisah</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- Status "Lulus" hanya muncul jika deliverable semua Approved DAN final assessment ada
- HistoriProtonDetail menampilkan section per tahun
- Logic HistoriProton dan ExportHistoriProton identik (konsisten)
</verification>

<success_criteria>
- Completion criteria (D-13) diterapkan di HistoriProton, ExportHistoriProton, dan HistoriProtonDetail
- Section separator per tahun visible di HistoriProtonDetail (D-12)
- View dan export tetap konsisten (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/236-audit-completion/236-03-SUMMARY.md`
</output>
