---
phase: 236-audit-completion
plan: 04
type: execute
wave: 1
depends_on: ["236-02", "236-03"]
files_modified:
  - Views/CDP/EditCoachingSession.cshtml
  - Views/Admin/CoachCoacheeMapping.cshtml
  - Controllers/AdminController.cs
autonomous: true
gap_closure: true
requirements: [COMP-02, COMP-04]

must_haves:
  truths:
    - "Coach bisa edit coaching session miliknya via form UI tanpa runtime error"
    - "HC/Admin bisa trigger MarkMappingCompleted dari UI untuk mapping yang eligible"
  artifacts:
    - path: "Views/CDP/EditCoachingSession.cshtml"
      provides: "Form edit CatatanCoach, Kesimpulan, Result"
      contains: "EditCoachingSession"
    - path: "Views/Admin/CoachCoacheeMapping.cshtml"
      provides: "Button MarkMappingCompleted untuk mapping eligible"
      contains: "MarkMappingCompleted"
  key_links:
    - from: "Views/CDP/EditCoachingSession.cshtml"
      to: "Controllers/CDPController.cs"
      via: "form POST asp-action=EditCoachingSession"
      pattern: "asp-action.*EditCoachingSession"
    - from: "Views/Admin/CoachCoacheeMapping.cshtml"
      to: "Controllers/AdminController.cs"
      via: "form POST MarkMappingCompleted"
      pattern: "MarkMappingCompleted"
---

<objective>
Menutup 2 gap dari verifikasi Phase 236: (1) buat view EditCoachingSession.cshtml yang hilang, (2) tambah tombol MarkMappingCompleted di CoachCoacheeMapping view.

Purpose: Kedua backend action sudah ada tapi tidak bisa diakses user karena view/button tidak ada.
Output: Dua file view yang menutup gap COMP-02 dan COMP-04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/236-audit-completion/236-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat Views/CDP/EditCoachingSession.cshtml</name>
  <files>Views/CDP/EditCoachingSession.cshtml</files>
  <read_first>
    - Controllers/CDPController.cs (line 2275-2310: EditCoachingSession GET returns View(session) with CoachingSession model, POST accepts id, catatanCoach, kesimpulan, result)
    - Models/CoachingSession.cs (model properties: CatatanCoach, Kesimpulan, Result, Kompetensi, SubKompetensi, Deliverable, Date, Status)
    - Views/CDP/Deliverable.cshtml (referensi pattern view CDP yang sudah ada)
  </read_first>
  <action>
    Buat file Views/CDP/EditCoachingSession.cshtml dengan:

    1. Model directive: `@model HcPortal.Models.CoachingSession`
    2. Title: `ViewData["Title"] = "Edit Sesi Coaching";`
    3. Breadcrumb: Coaching Proton > Deliverable > Edit Sesi Coaching
    4. Form POST ke CDPController.EditCoachingSession:
       - `asp-controller="CDP"` `asp-action="EditCoachingSession"` `asp-route-id="@Model.Id"`
       - Include `@Html.AntiForgeryToken()`
    5. Read-only info display (tidak bisa diedit):
       - Tanggal: `Model.Date.ToString("dd MMM yyyy")`
       - Kompetensi: `Model.Kompetensi`
       - Sub Kompetensi: `Model.SubKompetensi`
       - Deliverable: `Model.Deliverable`
    6. Editable fields (name harus match parameter POST action):
       - `<textarea name="catatanCoach" class="form-control" rows="5">@Model.CatatanCoach</textarea>` dengan label "Catatan Coach"
       - `<select name="kesimpulan" class="form-select">` dengan options "Kompeten" dan "Perlu Pengembangan", selected berdasarkan `Model.Kesimpulan`
       - `<select name="result" class="form-select">` dengan options "Need Improvement", "Suitable", "Good", "Excellence", selected berdasarkan `Model.Result`
    7. Buttons: Submit "Simpan Perubahan" (btn-primary) + Cancel link ke `Url.Action("Deliverable", "CDP", new { id = Model.ProtonDeliverableProgressId })`
    8. Styling: gunakan Bootstrap 5 card layout konsisten dengan view CDP lainnya
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path 'Views/CDP/EditCoachingSession.cshtml') { $c = Get-Content 'Views/CDP/EditCoachingSession.cshtml' -Raw; if ($c -match 'EditCoachingSession' -and $c -match 'catatanCoach' -and $c -match 'kesimpulan' -and $c -match 'result' -and $c -match 'AntiForgeryToken') { 'PASS' } else { 'FAIL: missing required elements' } } else { 'FAIL: file not found' }"</automated>
  </verify>
  <acceptance_criteria>
    - File Views/CDP/EditCoachingSession.cshtml exists
    - Contains `@model HcPortal.Models.CoachingSession`
    - Contains `asp-action="EditCoachingSession"`
    - Contains `name="catatanCoach"` textarea
    - Contains `name="kesimpulan"` select with options "Kompeten" and "Perlu Pengembangan"
    - Contains `name="result"` select with options "Need Improvement", "Suitable", "Good", "Excellence"
    - Contains `@Html.AntiForgeryToken()`
    - Contains cancel link to Deliverable action
  </acceptance_criteria>
  <done>View EditCoachingSession.cshtml ada dan wired ke POST action CDPController — coach bisa edit CatatanCoach, Kesimpulan, Result via form</done>
</task>

<task type="auto">
  <name>Task 2: Tambah tombol MarkMappingCompleted + IsCompleted property di CoachCoacheeMapping view</name>
  <files>Views/Admin/CoachCoacheeMapping.cshtml, Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (line 3679-3693: coachee projection — perlu tambah IsCompleted; line 4468-4502: MarkMappingCompleted POST action accepts mappingId)
    - Views/Admin/CoachCoacheeMapping.cshtml (line 260-285: existing action buttons area per coachee row)
    - Models/CoachCoacheeMapping.cs (IsCompleted property sudah ada dari Plan 01)
  </read_first>
  <action>
    Dua perubahan:

    **A. Controllers/AdminController.cs line ~3679-3693** — tambah `IsCompleted` ke coachee projection:
    Dalam anonymous object di `g.Select(r => new { ... })` (sekitar line 3679), tambah:
    ```csharp
    IsCompleted = r.Mapping.IsCompleted,
    ```
    Setelah `AssignmentUnit = r.Mapping.AssignmentUnit ?? ""` (line 3692).

    **B. Views/Admin/CoachCoacheeMapping.cshtml line ~266-283** — tambah tombol MarkMappingCompleted:
    Di dalam blok `@if (coachee.IsActive)` (line 266), SETELAH tombol Nonaktifkan (line 271), tambah:
    ```html
    @if (!coachee.IsCompleted)
    {
        <form method="post" asp-action="MarkMappingCompleted" asp-controller="Admin" class="d-inline">
            @Html.AntiForgeryToken()
            <input type="hidden" name="mappingId" value="@coachee.Id" />
            <button type="submit" class="btn btn-sm btn-outline-info"
                    onclick="return confirm('Tandai coachee ini sebagai graduated?')">
                <i class="bi bi-award"></i> Graduated
            </button>
        </form>
    }
    else
    {
        <span class="badge bg-info"><i class="bi bi-award me-1"></i>Graduated</span>
    }
    ```
    Ini menampilkan tombol "Graduated" untuk mapping aktif yang belum completed, atau badge "Graduated" untuk yang sudah completed.
  </action>
  <verify>
    <automated>powershell -Command "$v = Get-Content 'Views/Admin/CoachCoacheeMapping.cshtml' -Raw; $c = Get-Content 'Controllers/AdminController.cs' -Raw; $ok = $true; if ($v -notmatch 'MarkMappingCompleted') { Write-Host 'FAIL: view missing MarkMappingCompleted'; $ok = $false }; if ($v -notmatch 'IsCompleted') { Write-Host 'FAIL: view missing IsCompleted check'; $ok = $false }; if ($c -notmatch 'IsCompleted = r\.Mapping\.IsCompleted') { Write-Host 'FAIL: controller missing IsCompleted projection'; $ok = $false }; if ($ok) { 'PASS' }"</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs coachee projection contains `IsCompleted = r.Mapping.IsCompleted`
    - Views/Admin/CoachCoacheeMapping.cshtml contains form with `asp-action="MarkMappingCompleted"`
    - View contains `name="mappingId"` hidden input
    - View contains `@Html.AntiForgeryToken()` inside the MarkMappingCompleted form
    - View shows "Graduated" badge when `coachee.IsCompleted` is true
    - View shows "Graduated" button when `coachee.IsCompleted` is false and `coachee.IsActive` is true
  </acceptance_criteria>
  <done>Tombol MarkMappingCompleted tampil di UI untuk mapping eligible, badge Graduated tampil untuk yang sudah completed</done>
</task>

</tasks>

<verification>
1. `dotnet build` harus sukses tanpa error
2. Views/CDP/EditCoachingSession.cshtml exists dan contains form elements
3. Views/Admin/CoachCoacheeMapping.cshtml contains MarkMappingCompleted form
4. Controllers/AdminController.cs coachee projection includes IsCompleted
</verification>

<success_criteria>
- EditCoachingSession GET action tidak lagi throw runtime error — view template ada
- MarkMappingCompleted bisa di-trigger dari UI oleh HC/Admin
- Kedua gap dari 236-VERIFICATION.md tertutup
</success_criteria>

<output>
After completion, create `.planning/phases/236-audit-completion/236-04-SUMMARY.md`
</output>
