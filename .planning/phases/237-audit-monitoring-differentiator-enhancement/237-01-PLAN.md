---
phase: 237-audit-monitoring-differentiator-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Controllers/ProtonDataController.cs
  - Views/CDP/CoachingProton.cshtml
autonomous: true
requirements: [MON-02, MON-03]
must_haves:
  truths:
    - "Filter cascade CoachingProton menampilkan data konsisten dengan filter aktif"
    - "Pagination tidak memecah group SubKompetensi"
    - "Override dari Approved ke Pending diblokir oleh server"
    - "Setiap override tercatat di audit log dengan detail transisi"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "Status transition validation di OverrideSave"
      contains: "illegalTransitions"
    - path: "Controllers/CDPController.cs"
      provides: "CoachingProton tracking audit fixes"
  key_links:
    - from: "Controllers/ProtonDataController.cs"
      to: "AuditLogService"
      via: "_auditLog.LogAsync"
      pattern: "_auditLog\\.LogAsync"
---

<objective>
Audit dan fix CoachingProton tracking (MON-02) serta Override flow (MON-03).

Purpose: Memastikan monitoring view dan override admin aman dari bug filter, pagination, dan transisi status ilegal.
Output: CoachingProton tracking teraudit, OverrideSave punya transition validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-audit-monitoring-differentiator-enhancement/237-CONTEXT.md
@.planning/phases/237-audit-monitoring-differentiator-enhancement/237-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit CoachingProton tracking — filter, pagination, role-based columns (MON-02)</name>
  <files>Controllers/CDPController.cs, Views/CDP/CoachingProton.cshtml</files>
  <read_first>
    - Controllers/CDPController.cs (L1386-1685 — CoachingProton action, filter cascade, pagination)
    - Views/CDP/CoachingProton.cshtml (role-based column visibility)
    - .planning/phases/237-audit-monitoring-differentiator-enhancement/237-RESEARCH.md (MON-02 findings)
  </read_first>
  <action>
Audit CoachingProton tracking per D-12:

1. **Filter cascade audit** — Periksa filter Bagian (L1438-1452), Unit (L1454-1480), Track (L1482-1491). Verify bahwa setiap filter downstream di-cascade dari filter upstream. Fix jika ada filter yang tidak konsisten.

2. **Tahun filter vs dropdown coachee** — Research menemukan potensi bug: Tahun filter (L1553-1556) di-apply ke query `progresses` tapi bukan ke `scopedCoacheeIds`. Artinya dropdown coachee menampilkan semua coachee meskipun mereka tidak punya deliverable di tahun yang dipilih. Investigasi dan fix jika memang bug — filter `scopedCoacheeIds` berdasarkan tahun yang dipilih juga.

3. **Pagination group-boundary** — Verify pagination (L1646-1685) tidak memecah SubKompetensi group. Ini sudah diimplementasi — confirm dan report di summary.

4. **Role-based column visibility** — Di View CoachingProton.cshtml, periksa apakah kolom approval (SrSpv approval, SH approval, HC review) hanya tampil untuk role yang berwenang. Fix visibility jika ada kolom yang tampil untuk role yang tidak seharusnya.

Catat semua temuan dan fix di summary.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build berhasil tanpa error
    - Jika tahun filter bug ditemukan: CDPController.cs contains filter logic yang menghubungkan tahun ke scopedCoacheeIds
    - Summary mencatat semua temuan audit MON-02
  </acceptance_criteria>
  <done>CoachingProton tracking teraudit — filter cascade, pagination, dan role-based columns diverifikasi dan difix jika ada bug</done>
</task>

<task type="auto">
  <name>Task 2: Fix OverrideSave — tambah status transition validation (MON-03)</name>
  <files>Controllers/ProtonDataController.cs</files>
  <read_first>
    - Controllers/ProtonDataController.cs (L1365-1421 — OverrideSave action)
    - .planning/phases/237-audit-monitoring-differentiator-enhancement/237-RESEARCH.md (MON-03 findings, code example)
  </read_first>
  <action>
Fix OverrideSave per D-13 dan D-14:

1. **Status transition validation** — Setelah `FindAsync` progress (sekitar L1386), tambahkan illegal transition check SEBELUM status di-update:

```csharp
// Illegal transitions — per D-14
var illegalTransitions = new Dictionary<string, HashSet<string>>
{
    { "Approved", new HashSet<string> { "Pending" } },
};
if (illegalTransitions.TryGetValue(progress.Status, out var blockedTargets)
    && blockedTargets.Contains(req.NewStatus))
{
    return Json(new { success = false,
        message = $"Transisi dari '{progress.Status}' ke '{req.NewStatus}' tidak diizinkan. Approved tidak bisa langsung kembali ke Pending." });
}
```

2. **HCApprovalStatus consistency** — Tambah validasi: jika `NewStatus` bukan `"Approved"`, maka `HCApprovalStatus` harus di-reset ke `"Pending"` (jangan biarkan status HC review untuk deliverable yang belum approved). Periksa apakah OverrideSave sudah handle field ini — jika tidak, tambahkan logic reset.

3. **Audit trail verification** — Confirm bahwa `_auditLog.LogAsync` sudah dipanggil di L1416-1418 dengan detail transisi (oldStatus → newStatus + reason). Ini sudah ada per research — verify dan report.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build berhasil tanpa error
    - ProtonDataController.cs contains "illegalTransitions"
    - ProtonDataController.cs contains "Transisi dari" (error message untuk blocked transition)
    - ProtonDataController.cs contains "_auditLog.LogAsync" di OverrideSave (sudah ada, verify tetap ada)
  </acceptance_criteria>
  <done>OverrideSave memblokir transisi Approved→Pending, audit trail lengkap terverifikasi</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil
- grep "illegalTransitions" ProtonDataController.cs mengembalikan hasil
- CoachingProton tracking audit findings terdokumentasi di summary
</verification>

<success_criteria>
1. OverrideSave menolak transisi Approved→Pending dengan pesan error JSON
2. CoachingProton tracking teraudit — semua bug yang ditemukan sudah difix
3. Build berhasil tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/237-audit-monitoring-differentiator-enhancement/237-01-SUMMARY.md`
</output>
