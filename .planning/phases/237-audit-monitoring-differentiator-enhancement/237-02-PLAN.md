---
phase: 237-audit-monitoring-differentiator-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CDPController.cs
  - Controllers/AdminController.cs
  - Models/CDPDashboardViewModel.cs
  - Views/CDP/Shared/_CoachingProtonContentPartial.cshtml
  - Views/Admin/CoachCoacheeMapping.cshtml
autonomous: true
requirements: [MON-01, DIFF-01, DIFF-03]
must_haves:
  truths:
    - "Dashboard stats konsisten dengan filter yang aktif"
    - "Bottleneck horizontal bar chart menampilkan top 10 deliverable pending >30 hari"
    - "Kolom Jumlah Coachee Aktif tampil di tabel CoachCoacheeMapping"
  artifacts:
    - path: "Models/CDPDashboardViewModel.cs"
      provides: "BottleneckLabels dan BottleneckValues properties"
      contains: "BottleneckLabels"
    - path: "Views/CDP/Shared/_CoachingProtonContentPartial.cshtml"
      provides: "Horizontal bar chart bottleneck"
      contains: "bottleneckChart"
    - path: "Views/Admin/CoachCoacheeMapping.cshtml"
      provides: "Kolom workload indicator"
      contains: "Coachee Aktif"
  key_links:
    - from: "Controllers/CDPController.cs"
      to: "Models/CDPDashboardViewModel.cs"
      via: "BuildProtonProgressSubModelAsync populates BottleneckLabels/Values"
      pattern: "BottleneckLabels"
    - from: "Views/CDP/Shared/_CoachingProtonContentPartial.cshtml"
      to: "Models/CDPDashboardViewModel.cs"
      via: "Model.BottleneckLabels rendered as Chart.js data"
      pattern: "bottleneckChart"
---

<objective>
Audit dashboard accuracy (MON-01), tambah bottleneck chart (DIFF-03), dan workload indicator (DIFF-01).

Purpose: Dashboard menampilkan data akurat + dua fitur differentiator baru untuk visibility.
Output: Dashboard teraudit, bottleneck chart horizontal bar, kolom workload di mapping page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-audit-monitoring-differentiator-enhancement/237-CONTEXT.md
@.planning/phases/237-audit-monitoring-differentiator-enhancement/237-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dashboard stats + tambah bottleneck chart (MON-01, DIFF-03)</name>
  <files>Controllers/CDPController.cs, Models/CDPDashboardViewModel.cs, Views/CDP/Shared/_CoachingProtonContentPartial.cshtml</files>
  <read_first>
    - Controllers/CDPController.cs (L260-619 — BuildProtonProgressSubModelAsync, stat calculations L537-538)
    - Models/CDPDashboardViewModel.cs (ProtonProgressSubModel class — L35+)
    - Views/CDP/Shared/_CoachingProtonContentPartial.cshtml (L200-279 — existing Chart.js pattern)
    - .planning/phases/237-audit-monitoring-differentiator-enhancement/237-RESEARCH.md (MON-01 findings, bottleneck code example)
  </read_first>
  <action>
**A. Dashboard stats audit (MON-01, D-01):**

1. Periksa `allProgresses` scope — research menemukan potensi bug: ketika filter category/track aktif, `allProgresses` mungkin belum difilter ulang setelah assignment filter. Verify bahwa `pendingSpv` dan `pendingHC` stats (L537-538) dihitung dari progresses yang sudah difilter sesuai filter aktif. Fix jika inkonsisten.

2. Verify Chart.js TrendLabels/TrendValues dan StatusLabels/StatusData dihitung dari data yang sama dengan stats cards. Fix jika ada discrepancy.

**B. Tambah BottleneckLabels/BottleneckValues ke ProtonProgressSubModel (DIFF-03, D-02, D-10, D-11):**

Di `Models/CDPDashboardViewModel.cs`, tambah properties ke class `ProtonProgressSubModel`:
```csharp
public List<string> BottleneckLabels { get; set; } = new();  // "CoacheeName - DeliverableName"
public List<int> BottleneckValues { get; set; } = new();      // jumlah hari pending
```

**C. Populate bottleneck data di BuildProtonProgressSubModelAsync:**

Di `Controllers/CDPController.cs`, setelah stats calculation, tambah query bottleneck dari `allProgresses` yang SUDAH difilter (bukan query baru terpisah — per Pitfall 3):
```csharp
var bottleneckItems = allProgresses
    .Where(p => p.Status == "Submitted" && p.SubmittedAt.HasValue
                && (DateTime.UtcNow - p.SubmittedAt.Value).TotalDays > 30)
    .OrderByDescending(p => (DateTime.UtcNow - p.SubmittedAt.Value).TotalDays)
    .Take(10)
    .ToList();
```
Populate `BottleneckLabels` dengan format "{CoacheeName} - {DeliverableName}" dan `BottleneckValues` dengan jumlah hari (int). Gunakan navigation properties yang sudah di-Include untuk mendapatkan nama.

**D. Render horizontal bar chart di partial view (D-03):**

Di `Views/CDP/Shared/_CoachingProtonContentPartial.cshtml`, setelah existing chart section, tambah:
- Card container dengan heading "Bottleneck — Deliverable Pending > 30 Hari"
- `<canvas id="bottleneckChart"></canvas>`
- Chart.js script mengikuti pattern existing (menggunakan `@Json.Serialize`):
```javascript
var bottleneckLabels = @Json.Serialize(Model.BottleneckLabels);
var bottleneckValues = @Json.Serialize(Model.BottleneckValues);
if (bottleneckCtx) {
    new Chart(bottleneckCtx.getContext('2d'), {
        type: 'bar',
        data: {
            labels: bottleneckLabels,
            datasets: [{ label: 'Hari Pending', data: bottleneckValues,
                backgroundColor: 'rgba(220, 53, 69, 0.7)', borderColor: 'rgba(220, 53, 69, 1)', borderWidth: 1 }]
        },
        options: { indexAxis: 'y', responsive: true,
            plugins: { legend: { display: false } },
            scales: { x: { beginAtZero: true, title: { display: true, text: 'Jumlah Hari Pending' } } }
        }
    });
}
```
- Wrap dalam `@if (Model.BottleneckLabels.Any())` agar tidak render chart kosong
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build berhasil tanpa error
    - CDPDashboardViewModel.cs contains "BottleneckLabels"
    - CDPDashboardViewModel.cs contains "BottleneckValues"
    - CDPController.cs contains "bottleneckItems" atau "BottleneckLabels" (population logic)
    - _CoachingProtonContentPartial.cshtml contains "bottleneckChart"
    - _CoachingProtonContentPartial.cshtml contains "indexAxis"
  </acceptance_criteria>
  <done>Dashboard stats teraudit dan difix, bottleneck horizontal bar chart menampilkan top 10 deliverable pending >30 hari</done>
</task>

<task type="auto">
  <name>Task 2: Workload indicator di CoachCoacheeMapping page (DIFF-01)</name>
  <files>Controllers/AdminController.cs, Views/Admin/CoachCoacheeMapping.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (L3615-3735 — CoachCoacheeMapping action, grouped variable L3672-3700)
    - Views/Admin/CoachCoacheeMapping.cshtml (tabel mapping — kolom yang ada)
    - .planning/phases/237-audit-monitoring-differentiator-enhancement/237-RESEARCH.md (DIFF-01 workload count)
  </read_first>
  <action>
Tambah workload indicator per D-08 dan D-09:

1. **Controller** — `grouped` variable di AdminController L3672-3678 sudah memiliki `ActiveCount = g.Count(r => r.Mapping.IsActive)`. Data ini sudah tersedia. Verify bahwa `ActiveCount` di-pass ke View (via model atau ViewBag). Jika belum ada di anonymous type yang di-pass ke View, tambahkan.

2. **View** — Di tabel CoachCoacheeMapping.cshtml, tambah kolom header "Coachee Aktif" setelah kolom Coach Name. Tampilkan `@coach.ActiveCount` untuk setiap row. Style dengan badge: jika ActiveCount >= 5, gunakan `badge bg-warning` (beban tinggi); jika >= 8, `badge bg-danger`; default `badge bg-info`.

Tidak perlu query baru — data sudah ada di `grouped` list.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build berhasil tanpa error
    - CoachCoacheeMapping.cshtml contains "Coachee Aktif"
    - CoachCoacheeMapping.cshtml contains "ActiveCount"
    - CoachCoacheeMapping.cshtml contains "bg-warning" atau "bg-danger" (badge styling)
  </acceptance_criteria>
  <done>Kolom "Coachee Aktif" tampil di tabel mapping dengan badge warna sesuai beban coach</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil
- grep "BottleneckLabels" CDPDashboardViewModel.cs mengembalikan hasil
- grep "bottleneckChart" _CoachingProtonContentPartial.cshtml mengembalikan hasil
- grep "Coachee Aktif" CoachCoacheeMapping.cshtml mengembalikan hasil
</verification>

<success_criteria>
1. Dashboard stats akurat per filter aktif
2. Bottleneck horizontal bar chart tampil di dashboard dengan top 10 deliverable pending >30 hari
3. Kolom workload indicator tampil di mapping page
4. Build berhasil tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/237-audit-monitoring-differentiator-enhancement/237-02-SUMMARY.md`
</output>
