---
phase: 239-date-range-filter-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/IWorkerDataService.cs
  - Services/WorkerDataService.cs
  - Controllers/CMPController.cs
  - Views/CMP/_RecordsTeamBody.cshtml
autonomous: true
requirements: [FILT-02, FILT-03, FILT-04, FILT-05, EXP-01, EXP-02]

must_haves:
  truths:
    - "GetWorkersInSection menerima dateFrom/dateTo dan memfilter workers + count berdasarkan date range"
    - "RecordsTeamPartial action mengembalikan HTML partial tbody untuk AJAX call"
    - "Export Assessment dan Training menerima dateFrom/dateTo dan memfilter rows sesuai date range"
    - "Jika dateFrom/dateTo kosong, behavior identik dengan sebelumnya (semua records tampil)"
  artifacts:
    - path: "Services/IWorkerDataService.cs"
      provides: "Interface dengan parameter dateFrom/dateTo"
      contains: "DateTime? dateFrom"
    - path: "Services/WorkerDataService.cs"
      provides: "Filter logic date range pada workers dan count"
      contains: "dateFrom"
    - path: "Controllers/CMPController.cs"
      provides: "RecordsTeamPartial action + export date params"
      contains: "RecordsTeamPartial"
    - path: "Views/CMP/_RecordsTeamBody.cshtml"
      provides: "Partial view tbody untuk AJAX response"
      contains: "worker-row"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "Services/WorkerDataService.cs"
      via: "GetWorkersInSection with dateFrom/dateTo"
      pattern: "GetWorkersInSection.*dateFrom.*dateTo"
    - from: "Controllers/CMPController.cs"
      to: "Views/CMP/_RecordsTeamBody.cshtml"
      via: "PartialView return"
      pattern: "PartialView.*_RecordsTeamBody"
---

<objective>
Backend: Tambah date range filter ke service layer, buat AJAX partial endpoint, dan update export actions.

Purpose: Menyiapkan semua server-side logic agar frontend (Plan 02) bisa memanggil AJAX dan mendapat data ter-filter.
Output: Service menerima dateFrom/dateTo, controller punya RecordsTeamPartial action, export actions filter by date, partial view _RecordsTeamBody.cshtml siap.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-date-range-filter-export/239-CONTEXT.md
@.planning/phases/239-date-range-filter-export/239-RESEARCH.md

@Services/IWorkerDataService.cs
@Services/WorkerDataService.cs
@Controllers/CMPController.cs
@Views/CMP/RecordsTeam.cshtml

<interfaces>
<!-- Current IWorkerDataService signature to be extended -->
From Services/IWorkerDataService.cs:
```csharp
Task<List<WorkerTrainingStatus>> GetWorkersInSection(string? section, string? unitFilter = null, string? category = null, string? search = null, string? statusFilter = null);
```

From Controllers/CMPController.cs (export actions):
```csharp
// line 520
public async Task<IActionResult> ExportRecordsTeamAssessment(string? section, string? unit, string? search, string? statusFilter)
// line 567
public async Task<IActionResult> ExportRecordsTeamTraining(string? section, string? unit, string? search, string? statusFilter, string? category)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service layer — tambah dateFrom/dateTo ke GetWorkersInSection</name>
  <files>Services/IWorkerDataService.cs, Services/WorkerDataService.cs</files>
  <read_first>Services/IWorkerDataService.cs, Services/WorkerDataService.cs</read_first>
  <action>
1. **IWorkerDataService.cs** — Ubah signature `GetWorkersInSection` menjadi:
```csharp
Task<List<WorkerTrainingStatus>> GetWorkersInSection(string? section, string? unitFilter = null, string? category = null, string? search = null, string? statusFilter = null, DateTime? dateFrom = null, DateTime? dateTo = null);
```

2. **WorkerDataService.cs** — Update implementasi `GetWorkersInSection`:
   - Tambah parameter `DateTime? dateFrom = null, DateTime? dateTo = null` ke method signature
   - Setelah fetch training records dan assessment sessions, filter berdasarkan date range:
     - Training: gunakan `tr.TanggalMulai ?? tr.Tanggal` sebagai effective date (konsisten dengan pola existing di GetAllWorkersHistory)
     - Assessment: gunakan `a.CompletedAt ?? a.Schedule` sebagai effective date
   - Date comparison: gunakan `.Date` property untuk perbandingan tanggal saja (menghindari end-of-day boundary issue):
     ```csharp
     var effectiveDate = (tr.TanggalMulai ?? tr.Tanggal).Date;
     bool inRange = (dateFrom == null || effectiveDate >= dateFrom.Value.Date)
                  && (dateTo == null || effectiveDate <= dateTo.Value.Date);
     ```
   - Jika dateFrom atau dateTo aktif DAN worker tidak punya record dalam range → skip worker (jangan masukkan ke result list). Per FILT-02.
   - Count `CompletedAssessments` dan `CompletedTrainings` dihitung HANYA dari records dalam date range. Per FILT-03, D-03.
   - Jika dateFrom dan dateTo keduanya null → behavior identik dengan sekarang (tidak ada perubahan). Per FILT-05.

CATATAN: Parameter `search` tetap ada di signature untuk backward compatibility tapi tidak digunakan lagi oleh UI baru. Jangan hapus parameter ini.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- IWorkerDataService.cs berisi `DateTime? dateFrom = null, DateTime? dateTo = null` di signature GetWorkersInSection
- WorkerDataService.cs berisi `dateFrom` dan `dateTo` parameter di GetWorkersInSection method
- WorkerDataService.cs berisi `TanggalMulai ?? tr.Tanggal` untuk training date filtering
- WorkerDataService.cs berisi `CompletedAt ?? a.Schedule` untuk assessment date filtering
- WorkerDataService.cs berisi logic skip worker jika date filter aktif dan tidak ada record dalam range
- `dotnet build` berhasil tanpa error
  </acceptance_criteria>
  <done>GetWorkersInSection menerima dateFrom/dateTo, filter workers dan count berdasarkan date range, build sukses</done>
</task>

<task type="auto">
  <name>Task 2: Controller — RecordsTeamPartial action, export date params, partial view</name>
  <files>Controllers/CMPController.cs, Views/CMP/_RecordsTeamBody.cshtml</files>
  <read_first>Controllers/CMPController.cs, Views/CMP/RecordsTeam.cshtml</read_first>
  <action>
1. **CMPController.cs** — Tambah action `RecordsTeamPartial`:
```csharp
[HttpGet]
public async Task<IActionResult> RecordsTeamPartial(
    string? section, string? unit, string? category, string? subCategory,
    string? statusFilter, string? dateFrom, string? dateTo)
{
    var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
    if (roleLevel >= 5) return Forbid();

    // L4 section lock — enforce server-side (pitfall 6)
    string? sectionFilter = (roleLevel == 4 && !string.IsNullOrEmpty(user.Section))
        ? user.Section : section;

    DateTime? from = string.IsNullOrEmpty(dateFrom) ? null : DateTime.Parse(dateFrom);
    DateTime? to = string.IsNullOrEmpty(dateTo) ? null : DateTime.Parse(dateTo);

    var workerList = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, null, statusFilter, from, to);

    return PartialView("_RecordsTeamBody", workerList);
}
```

2. **CMPController.cs** — Update `ExportRecordsTeamAssessment` (line ~520):
   - Tambah parameter `string? dateFrom, string? dateTo` ke method signature
   - Parse ke DateTime nullable: `DateTime? from = string.IsNullOrEmpty(dateFrom) ? null : DateTime.Parse(dateFrom);`
   - Pass `from, to` ke `GetWorkersInSection` call
   - Filter `assessmentRows` by date: `.Where(r => from == null || r.Date.Date >= from.Value.Date).Where(r => to == null || r.Date.Date <= to.Value.Date)`

3. **CMPController.cs** — Update `ExportRecordsTeamTraining` (line ~567):
   - Sama: tambah `string? dateFrom, string? dateTo`, parse, pass ke GetWorkersInSection, filter `trainingRows` by date

4. **Views/CMP/_RecordsTeamBody.cshtml** — Buat file baru. Extract tbody content dari RecordsTeam.cshtml:
   - Model: `@model List<HcPortal.Models.WorkerTrainingStatus>`
   - Render foreach worker in Model → `<tr class="worker-row" data-section="..." data-unit="..." data-status="..." data-nip="..." data-name="...">` dengan semua kolom (No, Nama, NIP, Bagian, Unit, Assessment count, Training count, Status badge)
   - Pastikan setiap `<tr>` punya class `worker-row` (digunakan oleh JS untuk count)
   - Copy markup persis dari `<tbody>` di RecordsTeam.cshtml, hanya bungkus sebagai partial (tanpa `<table>` atau `<tbody>` tag — hanya isi rows)
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- CMPController.cs berisi method `RecordsTeamPartial` dengan parameter `dateFrom` dan `dateTo`
- CMPController.cs: RecordsTeamPartial berisi `PartialView("_RecordsTeamBody"`
- CMPController.cs: RecordsTeamPartial berisi section lock logic `roleLevel == 4`
- CMPController.cs: ExportRecordsTeamAssessment signature berisi `string? dateFrom, string? dateTo`
- CMPController.cs: ExportRecordsTeamTraining signature berisi `string? dateFrom, string? dateTo`
- File Views/CMP/_RecordsTeamBody.cshtml ada dan berisi `@model List<HcPortal.Models.WorkerTrainingStatus>`
- _RecordsTeamBody.cshtml berisi `class="worker-row"`
- `dotnet build` berhasil tanpa error
  </acceptance_criteria>
  <done>RecordsTeamPartial action siap melayani AJAX request, export actions menerima date params, partial view _RecordsTeamBody.cshtml merender tbody rows</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil tanpa error
- Grep: `RecordsTeamPartial` ada di CMPController.cs
- Grep: `dateFrom` ada di IWorkerDataService.cs, WorkerDataService.cs, CMPController.cs
- File `_RecordsTeamBody.cshtml` ada
</verification>

<success_criteria>
- Service layer menerima dan memproses dateFrom/dateTo
- Controller punya AJAX endpoint RecordsTeamPartial
- Export actions menerima dateFrom/dateTo dan filter rows
- Partial view siap untuk AJAX response
- Build sukses
</success_criteria>

<output>
After completion, create `.planning/phases/239-date-range-filter-export/239-01-SUMMARY.md`
</output>
