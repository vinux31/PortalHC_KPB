---
phase: 239-date-range-filter-export
plan: 02
type: execute
wave: 2
depends_on: ["239-01"]
files_modified:
  - Views/CMP/RecordsTeam.cshtml
autonomous: false
requirements: [FILT-01, FILT-04, FILT-06]

must_haves:
  truths:
    - "User melihat 2 input date (Tanggal Awal & Tanggal Akhir) menggantikan Search Nama/NIP di filter bar"
    - "Saat tanggal diisi, tabel ter-update via AJAX menampilkan hanya workers dengan records dalam rentang"
    - "Tombol Reset mengosongkan date range bersama semua filter lain"
    - "Filter date bekerja bersama filter Bagian, Unit, Category, Sub Category, Status"
  artifacts:
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "Date inputs, AJAX filter, reset, export link update"
      contains: "dateFrom"
  key_links:
    - from: "Views/CMP/RecordsTeam.cshtml"
      to: "/CMP/RecordsTeamPartial"
      via: "fetch() AJAX call"
      pattern: "RecordsTeamPartial"
    - from: "Views/CMP/RecordsTeam.cshtml"
      to: "export links"
      via: "updateExportLinks with dateFrom/dateTo"
      pattern: "dateFrom.*dateTo"
---

<objective>
Frontend: Refactor RecordsTeam.cshtml — hapus search, tambah date inputs, ubah filtering ke AJAX, update reset dan export links.

Purpose: User dapat memfilter Team View berdasarkan rentang tanggal dengan UI yang responsif.
Output: RecordsTeam.cshtml ter-update dengan date inputs, AJAX filtering, dan export link yang menyertakan date params.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-date-range-filter-export/239-CONTEXT.md
@.planning/phases/239-date-range-filter-export/239-RESEARCH.md
@.planning/phases/239-date-range-filter-export/239-01-SUMMARY.md

@Views/CMP/RecordsTeam.cshtml

<interfaces>
<!-- From Plan 01: new AJAX endpoint -->
RecordsTeamPartial accepts query params: section, unit, category, subCategory, statusFilter, dateFrom, dateTo
Returns: HTML string of <tr class="worker-row"> rows (no <tbody> wrapper)
Replace innerHTML of #workerTableBody with response

<!-- From Plan 01: export actions now accept dateFrom, dateTo -->
ExportRecordsTeamAssessment?section=...&unit=...&statusFilter=...&category=...&subCategory=...&dateFrom=...&dateTo=...
ExportRecordsTeamTraining?section=...&unit=...&statusFilter=...&category=...&subCategory=...&dateFrom=...&dateTo=...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor RecordsTeam.cshtml — UI + JS AJAX filtering</name>
  <files>Views/CMP/RecordsTeam.cshtml</files>
  <read_first>Views/CMP/RecordsTeam.cshtml, Views/CMP/_RecordsTeamBody.cshtml</read_first>
  <action>
Perubahan di RecordsTeam.cshtml:

**A. Filter Bar Row 2 — per D-01:**
Hapus kolom Search Nama/NIP (`#searchFilter` input dan `oninput="filterTeamTable()"`). Ganti Row 2 menjadi 4 kolom:
```html
<div class="row g-3">
    <!-- Status (existing, col-md-3) -->
    <div class="col-12 col-md-3">
        <label class="form-label small text-muted mb-1">Status</label>
        <select id="statusFilter" class="form-select" onchange="filterTeamTable()">
            <option value="ALL">ALL</option>
            <option value="Sudah">Sudah</option>
            <option value="Belum">Belum</option>
        </select>
    </div>
    <!-- Tanggal Awal (baru) -->
    <div class="col-12 col-md-3">
        <label class="form-label small text-muted mb-1">Tanggal Awal</label>
        <input type="date" id="dateFrom" class="form-control" onchange="filterTeamTable()">
    </div>
    <!-- Tanggal Akhir (baru) -->
    <div class="col-12 col-md-3">
        <label class="form-label small text-muted mb-1">Tanggal Akhir</label>
        <input type="date" id="dateTo" class="form-control" onchange="filterTeamTable()">
    </div>
    <!-- Reset -->
    <div class="col-12 col-md-3 d-flex align-items-end">
        <button class="btn btn-outline-secondary w-100" onclick="resetTeamFilters()">
            <i class="bi bi-x-circle me-1"></i>Reset
        </button>
    </div>
</div>
```

**B. Table tbody — gunakan partial:**
Ganti isi `<tbody id="workerTableBody">` dengan `@Html.Partial("_RecordsTeamBody", workerList)` (di mana workerList = ViewData["WorkerList"] cast ke `List<WorkerTrainingStatus>`). Ini memastikan initial render dan AJAX render pakai markup yang sama.

**C. Loading indicator — tambah di atas table:**
```html
<div id="tableLoadingIndicator" style="display:none" class="text-center py-3">
    <div class="spinner-border spinner-border-sm text-primary" role="status"></div>
    <span class="ms-2 text-muted">Memuat data...</span>
</div>
```

**D. JavaScript — refactor filterTeamTable() ke AJAX (per D-02, D-05):**
Hapus seluruh body fungsi `filterTeamTable()` yang lama (client-side DOM show/hide). Ganti dengan:
```javascript
let debounceTimer;
function filterTeamTable() {
    clearTimeout(debounceTimer);
    debounceTimer = setTimeout(doFetch, 300);
}

async function doFetch() {
    const params = new URLSearchParams();
    const section = document.getElementById('sectionFilter').value;
    const unit = document.getElementById('unitFilter').value;
    const category = document.getElementById('categoryFilter').value;
    const subCategory = document.getElementById('subCategoryFilter').value;
    const status = document.getElementById('statusFilter').value;
    const dateFrom = document.getElementById('dateFrom').value;
    const dateTo = document.getElementById('dateTo').value;

    if (section) params.set('section', section);
    if (unit) params.set('unit', unit);
    if (category) params.set('category', category);
    if (subCategory) params.set('subCategory', subCategory);
    if (status && status !== 'ALL') params.set('statusFilter', status);
    if (dateFrom) params.set('dateFrom', dateFrom);
    if (dateTo) params.set('dateTo', dateTo);

    document.getElementById('tableLoadingIndicator').style.display = '';

    try {
        const resp = await fetch('@Url.Action("RecordsTeamPartial", "CMP")?' + params.toString(),
            { headers: { 'X-Requested-With': 'XMLHttpRequest' } });
        if (!resp.ok) throw new Error('Server error');
        const html = await resp.text();
        document.getElementById('workerTableBody').innerHTML = html;
        const count = document.querySelectorAll('#workerTableBody .worker-row').length;
        document.getElementById('workerCount').textContent = count;
    } catch (e) {
        document.getElementById('workerTableBody').innerHTML =
            '<tr><td colspan="8" class="text-center text-danger p-4">Gagal memuat data. Coba lagi.</td></tr>';
    } finally {
        document.getElementById('tableLoadingIndicator').style.display = 'none';
    }
    updateExportLinks();
}
```

**E. resetTeamFilters() — per FILT-06:**
Update fungsi reset: tambah clear `dateFrom.value = ''` dan `dateTo.value = ''`. Hapus `searchFilter` references. Panggil `filterTeamTable()` di akhir (yang sekarang trigger AJAX).

**F. updateExportLinks() — per EXP-01, EXP-02:**
Tambah `dateFrom` dan `dateTo` ke URLSearchParams dalam fungsi `updateExportLinks()`. Hapus `search` parameter. Kode lengkap sesuai RESEARCH.md code example.

**G. Hapus semua referensi ke `#searchFilter`** — termasuk variable declarations, event handlers, dan filter logic yang mention searchFilter.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- RecordsTeam.cshtml TIDAK berisi `searchFilter` (grep returns 0 matches)
- RecordsTeam.cshtml berisi `id="dateFrom"` dan `id="dateTo"`
- RecordsTeam.cshtml berisi `type="date"` (2 kali)
- RecordsTeam.cshtml berisi `RecordsTeamPartial` dalam fetch URL
- RecordsTeam.cshtml berisi `doFetch` function
- RecordsTeam.cshtml berisi `debounceTimer`
- RecordsTeam.cshtml berisi `tableLoadingIndicator`
- RecordsTeam.cshtml berisi `dateFrom` dan `dateTo` dalam updateExportLinks
- RecordsTeam.cshtml berisi `dateFrom` dan `dateTo` dalam resetTeamFilters
- `dotnet build` berhasil tanpa error
  </acceptance_criteria>
  <done>Search Nama/NIP dihapus, 2 input date ditambahkan, filtering via AJAX, reset clear dates, export links include date params</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifikasi fungsional Team View</name>
  <files>Views/CMP/RecordsTeam.cshtml</files>
  <action>Human verification — tidak ada perubahan kode. User memverifikasi fungsionalitas di browser.</action>
  <what-built>Date range filter menggantikan search di Team View CMP/Records. Filtering via AJAX, count ter-filter, export include date params.</what-built>
  <how-to-verify>
1. Buka halaman CMP/Records, klik tab Team View
2. Pastikan Search Nama/NIP TIDAK ADA, digantikan oleh 2 input date (Tanggal Awal, Tanggal Akhir)
3. Isi Tanggal Awal dan Tanggal Akhir → tabel ter-update otomatis (AJAX), hanya workers dengan records dalam rentang tampil
4. Perhatikan count Assessment dan Training di tabel — harus hanya menghitung records dalam rentang
5. Kombinasikan date filter dengan filter Bagian, Unit, Category, Status → semua bekerja bersama
6. Klik Reset → semua filter termasuk date range terkosongkan, tabel kembali menampilkan semua workers
7. Isi date range lalu klik tombol Export Assessment → download Excel, periksa hasilnya hanya berisi records dalam rentang
8. Sama untuk Export Training
  </how-to-verify>
  <verify>Human verification di browser</verify>
  <done>User mengkonfirmasi semua 8 langkah verifikasi berfungsi dengan benar</done>
  <resume-signal>Ketik "approved" atau jelaskan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` berhasil
- Grep: `searchFilter` TIDAK ada di RecordsTeam.cshtml
- Grep: `dateFrom` ada di RecordsTeam.cshtml
- Grep: `RecordsTeamPartial` ada di RecordsTeam.cshtml
- Human verification: filter, count, reset, export berfungsi
</verification>

<success_criteria>
- Input date Tanggal Awal dan Tanggal Akhir tampil di filter bar menggantikan Search
- AJAX filtering berfungsi dengan debounce
- Reset membersihkan semua filter termasuk date range
- Export links menyertakan dateFrom/dateTo
- User mengkonfirmasi semua berfungsi via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/239-date-range-filter-export/239-02-SUMMARY.md`
</output>
