---
phase: 240-alarm-sertifikat-expired
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/DashboardHomeViewModel.cs
  - Controllers/HomeController.cs
  - Views/Home/_CertAlertBanner.cshtml
  - Views/Home/Index.cshtml
autonomous: true
requirements: [ALRT-01, ALRT-02, ALRT-03, ALRT-04]

must_haves:
  truths:
    - "HC/Admin melihat banner merah dengan count sertifikat expired di Home/Index"
    - "HC/Admin melihat banner kuning dengan count sertifikat akan expired di Home/Index"
    - "Klik Lihat Detail navigasi ke /Admin/RenewalCertificate"
    - "Banner tidak muncul jika tidak ada sertifikat bermasalah"
    - "User biasa (bukan HC/Admin) tidak melihat banner"
  artifacts:
    - path: "Models/DashboardHomeViewModel.cs"
      provides: "ExpiredCount dan AkanExpiredCount properties"
      contains: "public int ExpiredCount"
    - path: "Controllers/HomeController.cs"
      provides: "GetCertAlertCountsAsync helper method"
      contains: "GetCertAlertCountsAsync"
    - path: "Views/Home/_CertAlertBanner.cshtml"
      provides: "Banner partial view dua baris merah/kuning"
      contains: "cert-alert-banner"
    - path: "Views/Home/Index.cshtml"
      provides: "Banner insertion setelah hero section"
      contains: "_CertAlertBanner"
  key_links:
    - from: "Controllers/HomeController.cs"
      to: "Models/DashboardHomeViewModel.cs"
      via: "ExpiredCount dan AkanExpiredCount assignment"
      pattern: "ExpiredCount ="
    - from: "Views/Home/Index.cshtml"
      to: "Views/Home/_CertAlertBanner.cshtml"
      via: "partial rendering"
      pattern: "_CertAlertBanner"
---

<objective>
Implementasi banner alert sertifikat expired di Home/Index untuk HC/Admin.

Purpose: HC dan Admin dapat langsung melihat status sertifikat bermasalah saat membuka dashboard, sehingga tindakan renewal dapat segera diambil.
Output: Banner dua baris (merah: expired, kuning: akan expired) dengan link ke RenewalCertificate, hanya tampil untuk HC/Admin.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/240-alarm-sertifikat-expired/240-CONTEXT.md
@.planning/phases/240-alarm-sertifikat-expired/240-RESEARCH.md
@.planning/phases/240-alarm-sertifikat-expired/240-UI-SPEC.md

<interfaces>
<!-- DashboardHomeViewModel saat ini -->
From Models/DashboardHomeViewModel.cs:
```csharp
public class DashboardHomeViewModel
{
    public ApplicationUser CurrentUser { get; set; } = null!;
    public string Greeting { get; set; } = string.Empty;
    public List<UpcomingEventViewModel> UpcomingEvents { get; set; } = new();
    public ProgressViewModel Progress { get; set; } = new();
}
```

<!-- HomeController constructor saat ini -->
From Controllers/HomeController.cs:
```csharp
private readonly UserManager<ApplicationUser> _userManager;
private readonly ApplicationDbContext _context;

public HomeController(UserManager<ApplicationUser> userManager, ApplicationDbContext context)
{
    _userManager = userManager;
    _context = context;
}
```

<!-- INotificationService interface (untuk plan 02, tapi referensi) -->
From Services/INotificationService.cs:
```csharp
Task<bool> SendAsync(string userId, string type, string title, string message, string? actionUrl = null);
```

<!-- Index.cshtml insertion point — setelah hero-section div, sebelum card row -->
From Views/Home/Index.cshtml line 10:
```razor
<div class="hero-section mb-4">
    ...
</div>
<!-- BANNER INSERTION POINT -->
<!-- next: card progress/upcoming events rows -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewModel + Query + Banner Partial</name>
  <files>Models/DashboardHomeViewModel.cs, Controllers/HomeController.cs, Views/Home/_CertAlertBanner.cshtml</files>
  <read_first>
    Models/DashboardHomeViewModel.cs
    Controllers/HomeController.cs
    Controllers/AdminController.cs (baris 7022-7170 — logika BuildRenewalRowsAsync untuk memahami query pattern expired/akan expired, termasuk AssessmentSession dan IsRenewed exclusion)
    Models/TrainingRecord.cs (field ValidUntil, CertificateType, SertifikatUrl)
    Models/AssessmentSession.cs (field ValidUntil, GenerateCertificate)
    .planning/phases/240-alarm-sertifikat-expired/240-UI-SPEC.md
  </read_first>
  <action>
1. **DashboardHomeViewModel.cs** — Tambahkan 2 property baru:
   ```csharp
   public int ExpiredCount { get; set; }
   public int AkanExpiredCount { get; set; }
   ```

2. **HomeController.cs** — Tambahkan private helper `GetCertAlertCountsAsync()`:
   - Query TrainingRecords: `SertifikatUrl != null && CertificateType != "Permanent" && ValidUntil != null`
     - Expired: `ValidUntil < today` — EXCLUDE yang sudah di-renew (cek apakah ada TrainingRecord lain yang `RenewsTrainingId == thisRecord.Id`)
     - Akan expired: `ValidUntil >= today && ValidUntil <= today.AddDays(30)` — sama, exclude yang sudah di-renew
   - Query AssessmentSessions: ikuti logika BuildRenewalRowsAsync baris 7120+ untuk menentukan sertifikat assessment expired
     - EXCLUDE yang sudah di-renew (ada AssessmentSession lain dengan `RenewsSessionId == thisSession.Id`)
   - Return `(int expiredCount, int akanExpiredCount)`

3. **HomeController.Index** — Sebelum `return View(viewModel)`, tambahkan:
   ```csharp
   if (User.IsInRole("HC") || User.IsInRole("Admin"))
   {
       var (expiredCount, akanExpiredCount) = await GetCertAlertCountsAsync();
       viewModel.ExpiredCount = expiredCount;
       viewModel.AkanExpiredCount = akanExpiredCount;
   }
   ```

4. **Views/Home/_CertAlertBanner.cshtml** — Buat partial baru sesuai UI-SPEC:
   ```razor
   @model HcPortal.Models.DashboardHomeViewModel

   <div class="cert-alert-banner mb-3" role="region" aria-label="Peringatan Sertifikat">
       @if (Model.ExpiredCount > 0)
       {
           <div class="alert bg-danger bg-opacity-10 border border-danger border-opacity-25 d-flex align-items-center py-2 px-3 mb-2">
               <i class="bi bi-x-circle me-2 text-danger" aria-hidden="true"></i>
               <span><strong class="text-danger">@Model.ExpiredCount</strong> sertifikat telah Expired</span>
               <a href="/Admin/RenewalCertificate" class="ms-auto text-danger fw-semibold text-decoration-none"
                  aria-label="Lihat detail sertifikat expired">
                   Lihat Detail <i class="bi bi-arrow-right" aria-hidden="true"></i>
               </a>
           </div>
       }
       @if (Model.AkanExpiredCount > 0)
       {
           <div class="alert bg-warning bg-opacity-10 border border-warning border-opacity-25 d-flex align-items-center py-2 px-3 mb-0">
               <i class="bi bi-exclamation-triangle me-2 text-warning" aria-hidden="true"></i>
               <span><strong class="text-warning">@Model.AkanExpiredCount</strong> sertifikat akan Expired dalam 30 hari</span>
               <a href="/Admin/RenewalCertificate" class="ms-auto text-warning fw-semibold text-decoration-none"
                  aria-label="Lihat detail sertifikat akan expired">
                   Lihat Detail <i class="bi bi-arrow-right" aria-hidden="true"></i>
               </a>
           </div>
       }
   </div>
   ```

PENTING per D-02: TIDAK ada tombol dismiss/close di banner. Per D-10: query dan render hanya untuk HC/Admin.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\Desktop\PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Models/DashboardHomeViewModel.cs contains `public int ExpiredCount { get; set; }`
    - Models/DashboardHomeViewModel.cs contains `public int AkanExpiredCount { get; set; }`
    - Controllers/HomeController.cs contains `GetCertAlertCountsAsync`
    - Controllers/HomeController.cs contains `User.IsInRole("HC") || User.IsInRole("Admin")`
    - Views/Home/_CertAlertBanner.cshtml exists and contains `cert-alert-banner`
    - Views/Home/_CertAlertBanner.cshtml contains `bg-danger bg-opacity-10`
    - Views/Home/_CertAlertBanner.cshtml contains `bg-warning bg-opacity-10`
    - Views/Home/_CertAlertBanner.cshtml contains `/Admin/RenewalCertificate`
    - Views/Home/_CertAlertBanner.cshtml contains `aria-label="Peringatan Sertifikat"`
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>DashboardHomeViewModel punya ExpiredCount dan AkanExpiredCount. HomeController query count sertifikat bermasalah hanya untuk HC/Admin. Partial banner merah/kuning terbuat sesuai UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Banner ke Index.cshtml</name>
  <files>Views/Home/Index.cshtml</files>
  <read_first>
    Views/Home/Index.cshtml
    Views/Home/_CertAlertBanner.cshtml
  </read_first>
  <action>
Sisipkan partial banner di Views/Home/Index.cshtml tepat setelah closing `</div>` dari `div.hero-section.mb-4` (sekitar line 40-41), sebelum card progress row:

```razor
@if (Model.ExpiredCount > 0 || Model.AkanExpiredCount > 0)
{
    <partial name="_CertAlertBanner" model="Model" />
}
```

Per D-01: posisi setelah greeting/hero, sebelum card progress.
Per ALRT-04: container tidak dirender jika keduanya 0.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\Desktop\PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/Home/Index.cshtml contains `_CertAlertBanner`
    - Views/Home/Index.cshtml contains `Model.ExpiredCount > 0 || Model.AkanExpiredCount > 0`
    - The partial tag appears AFTER hero-section div closing tag
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Banner partial terwire di Index.cshtml setelah hero section. Banner hanya tampil jika ada sertifikat bermasalah.</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- Grep: `_CertAlertBanner` ada di Index.cshtml
- Grep: `ExpiredCount` ada di DashboardHomeViewModel.cs dan HomeController.cs
- Grep: `GetCertAlertCountsAsync` ada di HomeController.cs
</verification>

<success_criteria>
- Build sukses
- Banner partial view terbuat dengan styling merah/kuning sesuai UI-SPEC
- HomeController query count sertifikat expired/akan expired hanya untuk role HC/Admin
- Index.cshtml merender banner setelah hero section, sebelum card progress
- Banner tidak dirender jika kedua count = 0
</success_criteria>

<output>
After completion, create `.planning/phases/240-alarm-sertifikat-expired/240-01-SUMMARY.md`
</output>
