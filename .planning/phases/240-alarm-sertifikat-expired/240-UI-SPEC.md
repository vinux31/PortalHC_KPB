---
phase: 240
slug: alarm-sertifikat-expired
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-23
---

# Phase 240 — UI Design Contract

> Visual dan interaction contract untuk Banner Alert Sertifikat Expired + Bell Notification CERT_EXPIRED. Generated by gsd-ui-researcher.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Bootstrap 5.3 native |
| Preset | not applicable |
| Component library | Bootstrap 5.3 (cdn.jsdelivr.net/npm/bootstrap@5.3.0) |
| Icon library | Bootstrap Icons 1.10.0 (bi-x-circle, bi-exclamation-triangle) |
| Font | Inter (Google Fonts, weights 400/600) |

Sumber: `Views/Shared/_Layout.cshtml` line 21–24. Proyek tidak menggunakan shadcn/tailwind.

---

## Spacing Scale

Declared values (multiples of 4 — menggunakan Bootstrap spacing utilities):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gap dalam alert (gap-1, me-1) |
| sm | 8px | Padding inline alert icon + teks (me-2) |
| md | 16px | Padding horizontal banner (px-3) |
| lg | 24px | Margin bawah banner sebelum card progress (mb-3) |
| xl | 32px | — (tidak digunakan di komponen ini) |
| 2xl | 48px | — (tidak digunakan di komponen ini) |
| 3xl | 64px | — (tidak digunakan di komponen ini) |

Exceptions: Banner ditampilkan dengan `py-2 px-3` — touch target minimal 44px terpenuhi karena elemen bukan button standalone.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (0.875rem) | 400 | 1.5 |
| Label/Badge | 13px (0.8125rem) | 600 | 1.4 |
| Alert text | 14px (0.875rem) | 400 | 1.5 |
| Count display | 24px (1.5rem / fs-4) | 600 | 1.2 |

Dua weight saja: 400 (regular) dan 600 (semibold). Tidak ada weight 700.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #ffffff / bg-body | Surface halaman, background utama |
| Secondary (30%) | #f8f9fa (bs-light) | Card backgrounds, nav, sidebar |
| Accent (10%) | #0d6efd (bs-primary) | Badge count pada bell icon navbar |
| Semantic: Expired | #dc3545 (bs-danger) | Baris banner Expired, badge teks danger, link "Lihat Detail" expired |
| Semantic: Akan Expired | #ffc107 (bs-warning) | Baris banner Akan Expired, badge teks warning, link "Lihat Detail" akan expired |
| Destructive | #dc3545 (bs-danger) | Tidak ada aksi destruktif di fase ini |

Accent reserved for: badge count pada bell icon navbar saja. Link "Lihat Detail" di dalam banner mengikuti warna semantic banner-nya masing-masing (`text-danger` untuk expired, `text-warning` untuk akan expired).

Sumber warna: `RenewalCertificate.cshtml` line 31/41 menggunakan `text-danger` dan `text-warning` konsisten — dipertahankan.

---

## Component Inventory

### 1. Banner Alert — Baris Expired (merah)

```
[bi-x-circle] X sertifikat telah Expired   [Lihat Detail →]
```

- Elemen: `div.alert` dengan background `bg-danger bg-opacity-10 border border-danger border-opacity-25`
- Icon: `bi bi-x-circle me-2 text-danger`
- Teks count: `<strong class="text-danger">@Model.ExpiredCount</strong>`
- Teks label: "sertifikat telah Expired" (weight 400, 14px)
- Link: `<a href="/Admin/RenewalCertificate" class="ms-auto text-danger fw-semibold text-decoration-none">Lihat Detail <i class="bi bi-arrow-right"></i></a>`
- Layout: `d-flex align-items-center` dengan `py-2 px-3`
- Tampil hanya jika `Model.ExpiredCount > 0`

### 2. Banner Alert — Baris Akan Expired (kuning)

```
[bi-exclamation-triangle] X sertifikat akan Expired dalam 30 hari   [Lihat Detail →]
```

- Elemen: `div.alert` dengan background `bg-warning bg-opacity-10 border border-warning border-opacity-25`
- Icon: `bi bi-exclamation-triangle me-2 text-warning`
- Teks count: `<strong class="text-warning">@Model.AkanExpiredCount</strong>`
- Teks label: "sertifikat akan Expired dalam 30 hari" (weight 400, 14px)
- Link: `<a href="/Admin/RenewalCertificate" class="ms-auto text-warning fw-semibold text-decoration-none">Lihat Detail <i class="bi bi-arrow-right"></i></a>`
- Layout: `d-flex align-items-center` dengan `py-2 px-3`
- Tampil hanya jika `Model.AkanExpiredCount > 0`

### 3. Banner Container

- `div.cert-alert-banner.mb-3` — wrapper keduanya, `role="region" aria-label="Peringatan Sertifikat"`
- Posisi: setelah hero/greeting section, sebelum card progress/upcoming events (D-01)
- Tidak ada tombol dismiss/close — banner permanent selama data bermasalah (D-02)
- Tidak tampil sama sekali jika keduanya 0 (ALRT-04)

### 4. Bell Notification Item (CERT_EXPIRED)

- Format dropdown item: teks `Sertifikat [Judul] milik [Nama Pekerja] telah expired` (D-06)
- Icon item: `bi bi-shield-exclamation text-danger` (konsisten dengan tema expired)
- ActionUrl: `/Admin/RenewalCertificate` (D-07)
- Tampil dalam `NotificationBellViewComponent` dropdown yang sudah ada — tidak ada perubahan struktur bell

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (link banner expired) | "Lihat Detail" |
| Primary CTA (link banner akan expired) | "Lihat Detail" |
| Banner expired label | "[N] sertifikat telah Expired" |
| Banner akan expired label | "[N] sertifikat akan Expired dalam 30 hari" |
| Empty state | Banner tidak ditampilkan — tidak ada copy kosong (D-02, ALRT-04) |
| Error state | Tidak ada aksi form — error tidak relevan di fase ini |
| Bell notification body | "Sertifikat [Judul] milik [Nama Pekerja] telah expired" (D-06) |
| Destructive confirmation | Tidak ada aksi destruktif di fase ini |

Sumber: D-03, D-06 dari CONTEXT.md.

---

## Interaction Contract

| Interaksi | Perilaku |
|-----------|---------|
| Klik "Lihat Detail" (banner expired) | Navigasi ke `/Admin/RenewalCertificate` — GET, full page load |
| Klik "Lihat Detail" (banner akan expired) | Navigasi ke `/Admin/RenewalCertificate` — GET, full page load |
| Klik bell notification CERT_EXPIRED | Navigasi ke `/Admin/RenewalCertificate` via ActionUrl |
| Page load Home/Index (HC/Admin) | Sistem generate notifikasi CERT_EXPIRED untuk sertifikat expired baru (NOTF-01) |
| Dismiss notifikasi bell | Gunakan mekanisme dismiss bell yang sudah ada — TIDAK ada tombol close di banner |
| User bukan HC/Admin | Banner tidak dirender sama sekali (D-10) |

---

## Accessibility

- Banner region: `role="region" aria-label="Peringatan Sertifikat"`
- Icon dekoratif: `aria-hidden="true"` pada semua `<i>` Bootstrap Icons
- Link "Lihat Detail": tambah `aria-label="Lihat detail sertifikat expired"` agar tidak ambigu
- Color contrast: `text-danger` (#dc3545) on `bg-danger bg-opacity-10` (#fde8eb) — ratio 4.5:1+ untuk body 14px (AA compliant)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — tidak digunakan | not applicable |
| Bootstrap 5.3 CDN | alert, badge, d-flex utilities | not required (existing stack) |
| Bootstrap Icons 1.10.0 | bi-x-circle, bi-exclamation-triangle, bi-shield-exclamation, bi-arrow-right | not required (existing stack) |

Tidak ada third-party registry baru. Semua komponen dari stack Bootstrap yang sudah ada di proyek.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
