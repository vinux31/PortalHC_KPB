---
phase: 241-seed-data-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Data/SeedData.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "SeedUatDataAsync dipanggil dari InitializeAsync saat IsDevelopment"
    - "Coach-coachee mapping Rustam->Rino ter-seed dengan IsActive=true"
    - "ProtonTrackAssignment aktif ter-seed untuk Rino"
    - "AssessmentCategory sub-kategori ter-seed"
    - "Assessment reguler OJT Proses Alkylation Q1-2026 ter-seed dengan 15 soal 4 opsi dan 4 ET"
    - "UserPackageAssignment ter-seed untuk Rino dan Iwan"
    - "Idempotency guard mencegah duplikasi saat restart"
  artifacts:
    - path: "Data/SeedData.cs"
      provides: "SeedUatDataAsync method + sub-methods"
      contains: "SeedUatDataAsync"
  key_links:
    - from: "Data/SeedData.cs"
      to: "InitializeAsync"
      via: "method call in IsDevelopment block"
      pattern: "SeedUatDataAsync"
---

<objective>
Menambahkan method SeedUatDataAsync ke SeedData.cs yang menyediakan: coach-coachee mapping, ProtonTrackAssignment, AssessmentCategory sub-kategori, dan assessment reguler "OJT Proses Alkylation Q1-2026" dengan 15 soal, 4 ET, dan assignment untuk Rino + Iwan.

Purpose: Menyediakan data prasyarat agar fase UAT 242-246 bisa dieksekusi tanpa setup manual.
Output: SeedData.cs yang di-extend dengan SeedUatDataAsync dan sub-methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/241-seed-data-uat/241-CONTEXT.md
@.planning/phases/241-seed-data-uat/241-RESEARCH.md

@Data/SeedData.cs
@Models/AssessmentSession.cs
@Models/AssessmentPackage.cs
@Models/UserPackageAssignment.cs
@Models/PackageUserResponse.cs
@Models/CoachCoacheeMapping.cs
@Models/ProtonModels.cs
@Models/AssessmentCategory.cs
@Models/SessionElemenTeknisScore.cs
@Models/AssessmentAttemptHistory.cs
@Helpers/CertNumberHelper.cs
@Data/ApplicationDbContext.cs

<interfaces>
<!-- Entity models executor needs -->

From Models/AssessmentSession.cs:
```csharp
public class AssessmentSession {
    public int Id { get; set; }
    public string UserId { get; set; } = "";
    public string Title { get; set; } = "";
    public string Category { get; set; } = "";
    public DateTime Schedule { get; set; }
    public int DurationMinutes { get; set; }
    public string Status { get; set; } = "";  // "Open", "Upcoming", "Completed"
    public int Progress { get; set; } = 0;
    public string BannerColor { get; set; } = "bg-primary";
    public int? Score { get; set; }
    public int PassPercentage { get; set; } = 70;
    public bool AllowAnswerReview { get; set; } = true;
    public bool GenerateCertificate { get; set; } = false;
    public bool? IsPassed { get; set; }
    public DateTime? CompletedAt { get; set; }
    public DateTime? StartedAt { get; set; }
    public DateTime? ValidUntil { get; set; }
    public string? NomorSertifikat { get; set; }
    public bool IsTokenRequired { get; set; }
    public string AccessToken { get; set; } = "";
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public int? ProtonTrackId { get; set; }
    public string? TahunKe { get; set; }
}
```

From Models/AssessmentPackage.cs:
```csharp
public class AssessmentPackage { public int Id; public int AssessmentSessionId; public string PackageName; public int PackageNumber; }
public class PackageQuestion { public int Id; public int AssessmentPackageId; public string QuestionText; public int Order; public int ScoreValue = 10; public string? ElemenTeknis; }
public class PackageOption { public int Id; public int PackageQuestionId; public string OptionText; public bool IsCorrect; }
```

From Models/UserPackageAssignment.cs:
```csharp
public class UserPackageAssignment { public int Id; public int AssessmentSessionId; public int AssessmentPackageId; public string UserId; public string ShuffledQuestionIds = "[]"; public string ShuffledOptionIdsPerQuestion = "{}"; public bool IsCompleted = false; public int? SavedQuestionCount; }
```

From Models/CoachCoacheeMapping.cs:
```csharp
public class CoachCoacheeMapping { public int Id; public string CoachId; public string CoacheeId; public bool IsActive = true; public DateTime StartDate; public string? AssignmentSection; public string? AssignmentUnit; }
```

From Models/ProtonModels.cs:
```csharp
public class ProtonTrackAssignment { public int Id; public string CoacheeId; public string AssignedById; public int ProtonTrackId; public bool IsActive = true; public DateTime AssignedAt; }
```

From Models/AssessmentCategory.cs:
```csharp
public class AssessmentCategory { public int Id; public string Name; public int DefaultPassPercentage = 70; public bool IsActive = true; public int SortOrder; public int? ParentId; public string? SignatoryUserId; }
```

From Helpers/CertNumberHelper.cs:
```csharp
public static string Build(int seq, DateTime date) => $"KPB/{seq:D3}/{ToRomanMonth(date.Month)}/{date.Year}";
public static async Task<int> GetNextSeqAsync(ApplicationDbContext context, int year);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah SeedUatDataAsync entry point + coach-coachee + kategori + assessment reguler open</name>
  <read_first>
    - Data/SeedData.cs (current state, InitializeAsync structure, existing patterns)
    - Models/AssessmentSession.cs (all fields)
    - Models/AssessmentPackage.cs (PackageQuestion, PackageOption)
    - Models/UserPackageAssignment.cs (ShuffledQuestionIds, ShuffledOptionIdsPerQuestion JSON format)
    - Models/CoachCoacheeMapping.cs (all fields, unique index constraint)
    - Models/ProtonModels.cs (ProtonTrackAssignment fields, ProtonTrack lookup)
    - Models/AssessmentCategory.cs (ParentId hierarchy)
    - Data/ApplicationDbContext.cs (DbSet names, constraints)
  </read_first>
  <files>Data/SeedData.cs</files>
  <action>
1. Di `InitializeAsync`, setelah `SeedOrganizationUnitsAsync(context)` (line 42), tambahkan blok IsDevelopment:
```csharp
// 6. Seed UAT data (Development only — assessment, coach-coachee, proton)
if (environment.IsDevelopment())
{
    await SeedUatDataAsync(userManager, context);
}
```

2. Tambah method `SeedUatDataAsync` (per D-08):
```csharp
public static async Task SeedUatDataAsync(UserManager<ApplicationUser> userManager, ApplicationDbContext context)
{
    // Idempotency guard (per D-10)
    if (await context.AssessmentSessions.AnyAsync(s => s.Title == "OJT Proses Alkylation Q1-2026"))
    {
        Console.WriteLine("UAT-SEED: Data UAT sudah ada, skip.");
        return;
    }

    var rino = await userManager.FindByEmailAsync("rino.prasetyo@pertamina.com");
    var iwan = await userManager.FindByEmailAsync("iwan3@pertamina.com");
    var rustam = await userManager.FindByEmailAsync("rustam.nugroho@pertamina.com");
    if (rino == null || iwan == null || rustam == null)
    {
        Console.WriteLine("UAT-SEED: User Rino/Iwan/Rustam tidak ditemukan, skip.");
        return;
    }

    var now = DateTime.UtcNow;

    // 1. Coach-Coachee Mapping (per D-14)
    await SeedCoachCoacheeMappingAsync(context, rustam.Id, rino.Id, now);

    // 2. ProtonTrackAssignment (per D-14)
    await SeedProtonTrackAssignmentAsync(context, rino.Id, rustam.Id, now);

    // 3. AssessmentCategory sub-kategori
    await SeedAssessmentCategoriesAsync(context);

    // 4. Assessment reguler open (per D-11)
    var (questions, package) = await SeedRegularAssessmentOpenAsync(context, rino.Id, iwan.Id, now);

    // 5. Completed assessment lulus untuk Rino (per D-04, D-06)
    await SeedCompletedAssessmentPassAsync(context, rino.Id, now, questions);

    // 6. Completed assessment gagal untuk Rino (per D-04, D-07)
    await SeedCompletedAssessmentFailAsync(context, rino.Id, now, questions);

    // 7. Assessment Proton (per D-12)
    await SeedProtonAssessmentsAsync(context, rino.Id, now);

    Console.WriteLine("UAT-SEED: Selesai seed data UAT.");
}
```

3. Tambah `SeedCoachCoacheeMappingAsync` — check `AnyAsync(m => m.CoacheeId == rinoId && m.IsActive)` sebelum insert. Set `CoachId = rustamId`, `CoacheeId = rinoId`, `IsActive = true`, `StartDate = now`, `AssignmentSection = "GAST"`, `AssignmentUnit = "Alkylation Unit (065)"`.

4. Tambah `SeedProtonTrackAssignmentAsync`:
   - Query `ProtonTracks.FirstOrDefaultAsync(t => t.TrackType == "Operator" && t.TahunKe == "Tahun 1")`. Jika null, log warning dan return.
   - Check `AnyAsync(a => a.CoacheeId == rinoId && a.ProtonTrackId == track.Id && a.IsActive)` sebelum insert.
   - Set `CoacheeId = rinoId`, `AssignedById = rustamId`, `ProtonTrackId = track.Id`, `IsActive = true`, `AssignedAt = now`.

5. Tambah `SeedAssessmentCategoriesAsync`:
   - Check `AnyAsync(c => c.Name == "Assessment OJT")` sebelum insert.
   - Seed parent "Assessment OJT" (SortOrder=1) lalu SaveChangesAsync.
   - Seed child "Alkylation" dengan `ParentId = parent.Id`, SortOrder=1.
   - Seed parent "Assessment Proton" (SortOrder=2).
   - SaveChangesAsync.

6. Tambah `SeedRegularAssessmentOpenAsync` yang return `(List<PackageQuestion> questions, AssessmentPackage package)`:
   - Buat AssessmentSession: `Title = "OJT Proses Alkylation Q1-2026"` (per D-11), `UserId = rinoId` (creator), `Category = "Assessment OJT"`, `Schedule = now.AddDays(7)` (per D-13), `DurationMinutes = 60`, `Status = "Open"`, `PassPercentage = 70`, `AllowAnswerReview = true`, `GenerateCertificate = true`, `AccessToken = "UAT-TOKEN-001"`, `IsTokenRequired = false`, `CreatedAt = now`.
   - SaveChangesAsync untuk mendapat session.Id.
   - Buat AssessmentPackage: `AssessmentSessionId = session.Id`, `PackageName = "Paket A"`, `PackageNumber = 1`.
   - SaveChangesAsync untuk mendapat package.Id.
   - Buat 15 PackageQuestion (per D-03) dengan 4 ET (per D-02) merata: soal 1-4 = "Proses Distilasi", 5-8 = "Keselamatan Kerja", 9-12 = "Operasi Pompa", 13-15 = "Instrumentasi". Konten semi-realistis kilang (per D-01):
     - Q1: "Apa fungsi utama kolom distilasi dalam unit Alkylation?" opts: "Memisahkan komponen berdasarkan titik didih" (correct), "Mencampur bahan kimia", "Menurunkan tekanan sistem", "Menghasilkan listrik"
     - Q2: "Suhu operasi normal reboiler distilasi adalah..." opts: "150-200°C" (correct), "50-80°C", "300-400°C", "Suhu kamar"
     - Q3: "Reflux ratio yang terlalu tinggi menyebabkan..." opts: "Konsumsi energi berlebih" (correct), "Produk lebih kotor", "Tekanan turun drastis", "Tidak berpengaruh"
     - Q4: "Indikator flooding pada kolom distilasi adalah..." opts: "Pressure drop naik tajam" (correct), "Level turun", "Suhu naik", "Flow rate stabil"
     - Q5: "APD wajib di area Alkylation meliputi..." opts: "Helm, kacamata safety, sarung tangan asam" (correct), "Hanya helm", "Sepatu biasa dan helm", "Tidak perlu APD"
     - Q6: "Langkah pertama saat terjadi kebocoran HF adalah..." opts: "Evakuasi searah angin dan aktifkan alarm" (correct), "Tutup kebocoran langsung", "Lanjutkan bekerja", "Telepon keluarga"
     - Q7: "Frekuensi safety talk di area proses adalah..." opts: "Setiap shift change" (correct), "Sekali sebulan", "Sekali setahun", "Tidak perlu"
     - Q8: "Tujuan Job Safety Analysis (JSA) adalah..." opts: "Mengidentifikasi bahaya setiap langkah kerja" (correct), "Membuat laporan keuangan", "Menghitung bonus", "Mengganti peralatan"
     - Q9: "Jenis pompa yang umum di unit Alkylation adalah..." opts: "Centrifugal pump" (correct), "Pompa tangan", "Pompa angin", "Water wheel"
     - Q10: "Tanda kavitasi pada pompa sentrifugal adalah..." opts: "Suara gemericik dan getaran abnormal" (correct), "Pompa berjalan normal", "Flow naik drastis", "Motor mati"
     - Q11: "Mechanical seal pada pompa berfungsi untuk..." opts: "Mencegah kebocoran fluida proses" (correct), "Menambah tekanan", "Mendinginkan motor", "Mempercepat putaran"
     - Q12: "Prosedur alignment pompa dilakukan saat..." opts: "Setelah maintenance atau instalasi baru" (correct), "Setiap jam", "Saat pompa berjalan", "Tidak pernah"
     - Q13: "Transmitter tekanan pada reaktor berfungsi untuk..." opts: "Mengukur dan mengirim sinyal tekanan ke DCS" (correct), "Menghasilkan tekanan", "Menyimpan data manual", "Mengganti operator"
     - Q14: "Kalibrasi instrumen dilakukan untuk..." opts: "Memastikan akurasi pembacaan" (correct), "Mengganti instrumen", "Mematikan sistem", "Mengurangi biaya"
     - Q15: "Control valve gagal membuka (fail-open) dipasang pada..." opts: "Cooling water line" (correct), "Fuel gas line", "Vent line", "Drain line"
   - SaveChangesAsync setelah AddRange questions.
   - Untuk setiap question, buat 4 PackageOption sesuai di atas. SaveChangesAsync setelah semua options ditambahkan.
   - Buat 2 UserPackageAssignment untuk Rino dan Iwan:
     - `AssessmentSessionId = session.Id`, `AssessmentPackageId = package.Id`, `UserId = rinoId/iwanId`
     - `ShuffledQuestionIds = JsonSerializer.Serialize(questions.Select(q => q.Id).ToList())`
     - `ShuffledOptionIdsPerQuestion = JsonSerializer.Serialize(questions.ToDictionary(q => q.Id.ToString(), q => q.Options.Select(o => o.Id).ToList()))`
     - `IsCompleted = false`, `SavedQuestionCount = 15`
   - SaveChangesAsync.
   - Return (questions loaded with Options via Include, package).

**PENTING:** Tambah `using System.Text.Json;` dan `using HcPortal.Helpers;` di top SeedData.cs jika belum ada.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SeedData.cs contains `await SeedUatDataAsync(userManager, context);` inside `if (environment.IsDevelopment())` block in InitializeAsync
    - SeedData.cs contains `public static async Task SeedUatDataAsync(`
    - SeedData.cs contains `SeedCoachCoacheeMappingAsync` with `m.CoacheeId == rinoId && m.IsActive` guard
    - SeedData.cs contains `SeedProtonTrackAssignmentAsync` with `ProtonTracks.FirstOrDefaultAsync` lookup
    - SeedData.cs contains `SeedAssessmentCategoriesAsync` with `c.Name == "Assessment OJT"` guard
    - SeedData.cs contains `SeedRegularAssessmentOpenAsync` with Title "OJT Proses Alkylation Q1-2026"
    - SeedData.cs contains 15 PackageQuestion entries with ElemenTeknis values "Proses Distilasi", "Keselamatan Kerja", "Operasi Pompa", "Instrumentasi"
    - SeedData.cs contains `UserPackageAssignment` creation for both Rino and Iwan with ShuffledQuestionIds and ShuffledOptionIdsPerQuestion
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>SeedData.cs compiles, SeedUatDataAsync dipanggil dari InitializeAsync, coach-coachee mapping + ProtonTrackAssignment + kategori + assessment reguler open dengan 15 soal dan 2 assignment ter-seed secara idempotent.</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- SeedData.cs mengandung SeedUatDataAsync dipanggil dari InitializeAsync
- 15 soal dengan 4 ET terdistribusi merata
- Idempotency guard by Title "OJT Proses Alkylation Q1-2026"
</verification>

<success_criteria>
- dotnet build 0 errors
- SeedUatDataAsync, SeedCoachCoacheeMappingAsync, SeedProtonTrackAssignmentAsync, SeedAssessmentCategoriesAsync, SeedRegularAssessmentOpenAsync semua ada sebagai method di SeedData.cs
- Method stubs untuk SeedCompletedAssessmentPassAsync, SeedCompletedAssessmentFailAsync, SeedProtonAssessmentsAsync ada (boleh kosong/throw NotImplementedException — akan diimplementasi Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/241-seed-data-uat/241-01-SUMMARY.md`
</output>
