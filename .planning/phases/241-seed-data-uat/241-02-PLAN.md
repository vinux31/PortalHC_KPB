---
phase: 241-seed-data-uat
plan: 02
type: execute
wave: 2
depends_on: ["241-01"]
files_modified:
  - Data/SeedData.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Rino punya 1 completed assessment lulus (skor ~80) dengan sertifikat dan ValidUntil"
    - "Rino punya 1 completed assessment gagal (skor ~40) tanpa sertifikat"
    - "Kedua completed assessment punya UserResponses per soal"
    - "SessionElemenTeknisScore ter-seed untuk radar chart"
    - "AssessmentAttemptHistory ter-seed untuk riwayat ujian"
    - "Assessment Proton Tahun 1 dan Tahun 3 ter-seed untuk Rino"
  artifacts:
    - path: "Data/SeedData.cs"
      provides: "SeedCompletedAssessmentPassAsync, SeedCompletedAssessmentFailAsync, SeedProtonAssessmentsAsync"
      contains: "SeedCompletedAssessmentPassAsync"
  key_links:
    - from: "Data/SeedData.cs"
      to: "Helpers/CertNumberHelper.cs"
      via: "CertNumberHelper.GetNextSeqAsync + Build"
      pattern: "CertNumberHelper\\.Build"
---

<objective>
Mengimplementasi method SeedCompletedAssessmentPassAsync (lulus, skor ~80, sertifikat), SeedCompletedAssessmentFailAsync (gagal, skor ~40, tanpa sertifikat), dan SeedProtonAssessmentsAsync (Proton Tahun 1 + Tahun 3). Termasuk UserResponses, SessionElemenTeknisScore, dan AssessmentAttemptHistory.

Purpose: Completed assessment data memungkinkan UAT untuk records view, radar chart, review jawaban, dan riwayat ujian.
Output: SeedData.cs lengkap dengan semua seed UAT data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/241-seed-data-uat/241-CONTEXT.md
@.planning/phases/241-seed-data-uat/241-RESEARCH.md
@.planning/phases/241-seed-data-uat/241-01-SUMMARY.md

@Data/SeedData.cs
@Models/AssessmentSession.cs
@Models/AssessmentPackage.cs
@Models/PackageUserResponse.cs
@Models/SessionElemenTeknisScore.cs
@Models/AssessmentAttemptHistory.cs
@Helpers/CertNumberHelper.cs
@Data/ApplicationDbContext.cs

<interfaces>
From Models/PackageUserResponse.cs:
```csharp
public class PackageUserResponse { public int Id; public int AssessmentSessionId; public int PackageQuestionId; public int? PackageOptionId; public DateTime SubmittedAt; }
```

From Models/SessionElemenTeknisScore.cs:
```csharp
public class SessionElemenTeknisScore { public int Id; public int AssessmentSessionId; public string ElemenTeknis; public int CorrectCount; public int QuestionCount; }
```

From Models/AssessmentAttemptHistory.cs:
```csharp
public class AssessmentAttemptHistory { public int Id; public int SessionId; public string UserId; public string Title; public string Category; public int? Score; public bool? IsPassed; public DateTime? StartedAt; public DateTime? CompletedAt; public int AttemptNumber; }
```

From Helpers/CertNumberHelper.cs:
```csharp
public static string Build(int seq, DateTime date) => $"KPB/{seq:D3}/{ToRomanMonth(date.Month)}/{date.Year}";
public static async Task<int> GetNextSeqAsync(ApplicationDbContext context, int year);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementasi completed assessment lulus + gagal dengan responses, ET scores, attempt history</name>
  <read_first>
    - Data/SeedData.cs (current state setelah Plan 01 — melihat method stubs dan questions structure)
    - Helpers/CertNumberHelper.cs (Build, GetNextSeqAsync signature)
    - Models/PackageUserResponse.cs (FK constraints)
    - Models/SessionElemenTeknisScore.cs (unique index per SessionId+ElemenTeknis)
    - Models/AssessmentAttemptHistory.cs (all fields)
    - Data/ApplicationDbContext.cs (DbSet names untuk PackageUserResponses, SessionElemenTeknisScores, AssessmentAttemptHistories)
  </read_first>
  <files>Data/SeedData.cs</files>
  <action>
Implementasi `SeedCompletedAssessmentPassAsync(context, rinoId, now, questions)`:

1. Buat AssessmentSession baru:
   - `Title = "OJT Proses Alkylation Q4-2025 (Lulus)"`, `UserId = rinoId`, `Category = "Assessment OJT"`
   - `Schedule = now.AddDays(-30)` (sebulan lalu, per D-13 relative)
   - `DurationMinutes = 60`, `Status = "Completed"`, `Progress = 100`
   - `Score = 80` (per D-04), `PassPercentage = 70`, `IsPassed = true` (per D-04)
   - `AllowAnswerReview = true`, `GenerateCertificate = true`
   - `StartedAt = now.AddDays(-30)`, `CompletedAt = now.AddDays(-30).AddMinutes(45)`
   - `AccessToken = "UAT-TOKEN-PASS"`, `IsTokenRequired = false`, `CreatedAt = now.AddDays(-30)`
   - SaveChangesAsync untuk mendapat session.Id

2. Generate NomorSertifikat (per D-06):
   ```csharp
   var certDate = now.AddDays(-30);
   var nextSeq = await CertNumberHelper.GetNextSeqAsync(context, certDate.Year);
   session.NomorSertifikat = CertNumberHelper.Build(nextSeq, certDate);
   session.ValidUntil = certDate.AddYears(1);
   await context.SaveChangesAsync();
   ```

3. Buat AssessmentPackage + copy 15 soal yang sama dari parameter `questions` (bukan referensi langsung — buat PackageQuestion baru dengan teks sama karena setiap session punya package sendiri):
   - Buat package: `AssessmentSessionId = session.Id`, `PackageName = "Paket A"`, `PackageNumber = 1`. SaveChangesAsync.
   - Untuk setiap question di `questions`, buat PackageQuestion baru dengan `QuestionText`, `Order`, `ScoreValue`, `ElemenTeknis` yang sama. SaveChangesAsync.
   - Untuk setiap question baru, copy PackageOption dari original. SaveChangesAsync.

4. Buat UserPackageAssignment untuk Rino:
   - `IsCompleted = true`, `SavedQuestionCount = 15`
   - ShuffledQuestionIds dan ShuffledOptionIdsPerQuestion dari newQuestions
   - SaveChangesAsync.

5. Buat PackageUserResponse per soal (per D-05) — distribusi ~12 benar dari 15 (skor 80):
   - ET "Proses Distilasi" (4 soal): 3 benar, 1 salah -> pilih `IsCorrect=true` untuk 3 pertama, `IsCorrect=false` untuk Q4
   - ET "Keselamatan Kerja" (4 soal): 3 benar, 1 salah
   - ET "Operasi Pompa" (4 soal): 3 benar, 1 salah
   - ET "Instrumentasi" (3 soal): 3 benar, 0 salah
   - Total: 12/15 = 80%
   - Untuk setiap soal, pilih option berdasarkan isCorrect flag:
   ```csharp
   // Track per-ET correct counts for scoring
   var etCorrectMap = new Dictionary<string, (int correct, int total)>();
   int answeredCorrect = 0;
   foreach (var q in newQuestions)
   {
       bool shouldBeCorrect = /* distribusi di atas */;
       var chosen = shouldBeCorrect
           ? q.Options.First(o => o.IsCorrect)
           : q.Options.First(o => !o.IsCorrect);
       context.PackageUserResponses.Add(new PackageUserResponse
       {
           AssessmentSessionId = session.Id,
           PackageQuestionId = q.Id,
           PackageOptionId = chosen.Id,
           SubmittedAt = session.CompletedAt!.Value
       });
   }
   await context.SaveChangesAsync();
   ```

6. Buat SessionElemenTeknisScore (per D-05 untuk radar chart):
   ```csharp
   context.SessionElemenTeknisScores.AddRange(new[]
   {
       new SessionElemenTeknisScore { AssessmentSessionId = session.Id, ElemenTeknis = "Proses Distilasi", CorrectCount = 3, QuestionCount = 4 },
       new SessionElemenTeknisScore { AssessmentSessionId = session.Id, ElemenTeknis = "Keselamatan Kerja", CorrectCount = 3, QuestionCount = 4 },
       new SessionElemenTeknisScore { AssessmentSessionId = session.Id, ElemenTeknis = "Operasi Pompa", CorrectCount = 3, QuestionCount = 4 },
       new SessionElemenTeknisScore { AssessmentSessionId = session.Id, ElemenTeknis = "Instrumentasi", CorrectCount = 3, QuestionCount = 3 },
   });
   await context.SaveChangesAsync();
   ```

7. Buat AssessmentAttemptHistory:
   ```csharp
   context.AssessmentAttemptHistories.Add(new AssessmentAttemptHistory
   {
       SessionId = session.Id, UserId = rinoId, Title = session.Title, Category = session.Category,
       Score = 80, IsPassed = true, StartedAt = session.StartedAt, CompletedAt = session.CompletedAt,
       AttemptNumber = 1, ArchivedAt = session.CompletedAt!.Value
   });
   await context.SaveChangesAsync();
   ```

---

Implementasi `SeedCompletedAssessmentFailAsync(context, rinoId, now, questions)`:

Sama seperti Pass tapi:
- `Title = "OJT Proses Alkylation Q3-2025 (Gagal)"`, `Score = 40` (per D-04), `IsPassed = false` (per D-07)
- `Schedule = now.AddDays(-60)`, `StartedAt = now.AddDays(-60)`, `CompletedAt = now.AddDays(-60).AddMinutes(30)`
- TANPA NomorSertifikat (null), TANPA ValidUntil (null) (per D-07)
- `GenerateCertificate = false`
- `AccessToken = "UAT-TOKEN-FAIL"`
- Distribusi jawaban ~6 benar dari 15 (skor 40):
  - ET "Proses Distilasi": 2 benar, 2 salah
  - ET "Keselamatan Kerja": 1 benar, 3 salah
  - ET "Operasi Pompa": 2 benar, 2 salah
  - ET "Instrumentasi": 1 benar, 2 salah
  - Total: 6/15 = 40%
- SessionElemenTeknisScore: CorrectCount sesuai distribusi di atas
- AssessmentAttemptHistory: `Score = 40, IsPassed = false, AttemptNumber = 1`

**CATATAN:** Kedua method harus buat PackageQuestion + PackageOption baru (copy dari parameter `questions`) — JANGAN reuse question IDs dari session open karena setiap session punya package terpisah.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SeedData.cs contains `SeedCompletedAssessmentPassAsync` with `CertNumberHelper.GetNextSeqAsync` and `CertNumberHelper.Build` calls
    - SeedData.cs contains `SeedCompletedAssessmentFailAsync` with `IsPassed = false` and no NomorSertifikat assignment
    - SeedData.cs contains `PackageUserResponse` inserts in both Pass and Fail methods
    - SeedData.cs contains `SessionElemenTeknisScores.AddRange` in both methods with all 4 ET names
    - SeedData.cs contains `AssessmentAttemptHistories.Add` in both methods
    - Pass method: Score = 80, CorrectCount values are 3,3,3,3
    - Fail method: Score = 40, CorrectCount values are 2,1,2,1
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Rino punya 2 completed assessment (1 lulus skor 80 dengan sertifikat, 1 gagal skor 40 tanpa sertifikat), keduanya lengkap dengan UserResponses, SessionElemenTeknisScore, dan AssessmentAttemptHistory.</done>
</task>

<task type="auto">
  <name>Task 2: Implementasi SeedProtonAssessmentsAsync untuk Proton Tahun 1 dan Tahun 3</name>
  <read_first>
    - Data/SeedData.cs (current state setelah Task 1)
    - Models/AssessmentSession.cs (ProtonTrackId, TahunKe fields)
    - Models/ProtonModels.cs (ProtonTrack entity — TrackType, TahunKe fields)
    - Data/ApplicationDbContext.cs (ProtonTracks DbSet)
  </read_first>
  <files>Data/SeedData.cs</files>
  <action>
Implementasi `SeedProtonAssessmentsAsync(context, rinoId, now)`:

1. Lookup ProtonTrack untuk Tahun 1:
   ```csharp
   var trackT1 = await context.ProtonTracks.FirstOrDefaultAsync(t => t.TahunKe == "Tahun 1");
   ```
   Jika null, log warning: `Console.WriteLine("UAT-SEED: ProtonTrack Tahun 1 tidak ditemukan, skip Proton assessments.");` dan return.

2. Buat AssessmentSession Proton Tahun 1 (per D-12):
   - `Title = "Assessment Proton Tahun 1"`, `UserId = rinoId`, `Category = "Assessment Proton"`
   - `Schedule = now.AddDays(14)`, `DurationMinutes = 90`, `Status = "Open"`
   - `PassPercentage = 70`, `AllowAnswerReview = true`, `GenerateCertificate = false`
   - `AccessToken = "UAT-PROTON-T1"`, `IsTokenRequired = false`, `CreatedAt = now`
   - `ProtonTrackId = trackT1.Id`, `TahunKe = "Tahun 1"`
   - SaveChangesAsync.

3. Lookup ProtonTrack untuk Tahun 3:
   ```csharp
   var trackT3 = await context.ProtonTracks.FirstOrDefaultAsync(t => t.TahunKe == "Tahun 3");
   ```
   Jika null, log warning dan skip Tahun 3 saja (jangan return — Tahun 1 sudah ter-seed).

4. Buat AssessmentSession Proton Tahun 3 (per D-12):
   - `Title = "Assessment Proton Tahun 3"`, `UserId = rinoId`, `Category = "Assessment Proton"`
   - `Schedule = now.AddDays(21)`, `DurationMinutes = 120`, `Status = "Open"`
   - `PassPercentage = 70`, `AllowAnswerReview = false`, `GenerateCertificate = false`
   - `AccessToken = "UAT-PROTON-T3"`, `IsTokenRequired = false`, `CreatedAt = now`
   - `ProtonTrackId = trackT3.Id`, `TahunKe = "Tahun 3"`
   - SaveChangesAsync.

5. Log: `Console.WriteLine("UAT-SEED: Assessment Proton Tahun 1 + Tahun 3 untuk Rino selesai.");`

**NOTE:** Assessment Proton tidak perlu paket soal di seed — soal dikelola terpisah via admin. Hanya session + assignment yang perlu ada.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SeedData.cs contains `SeedProtonAssessmentsAsync` method
    - SeedData.cs contains `ProtonTracks.FirstOrDefaultAsync(t => t.TahunKe == "Tahun 1")`
    - SeedData.cs contains `Category = "Assessment Proton"` in two session creations
    - SeedData.cs contains `TahunKe = "Tahun 1"` and `TahunKe = "Tahun 3"`
    - SeedData.cs contains `ProtonTrackId = trackT1.Id` and `ProtonTrackId = trackT3.Id`
    - Both Proton sessions have `UserId = rinoId` (per D-12: hanya Rino)
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Assessment Proton Tahun 1 dan Tahun 3 ter-seed untuk Rino, dengan guard jika ProtonTrack belum ada di DB. Seluruh SeedUatDataAsync lengkap dan app bisa di-`dotnet run` untuk verifikasi.</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- SeedData.cs mengandung semua 7 sub-methods yang dipanggil dari SeedUatDataAsync
- Completed assessment pass punya NomorSertifikat via CertNumberHelper
- Completed assessment fail TIDAK punya NomorSertifikat
- Kedua completed assessment punya PackageUserResponse, SessionElemenTeknisScore, AssessmentAttemptHistory
</verification>

<success_criteria>
- dotnet build 0 errors
- SeedCompletedAssessmentPassAsync lengkap: session + package + questions + options + assignment + responses + ET scores + attempt history + sertifikat
- SeedCompletedAssessmentFailAsync lengkap: sama minus sertifikat, skor 40
- SeedProtonAssessmentsAsync lengkap: 2 Proton sessions (Tahun 1 + Tahun 3) untuk Rino
- Seluruh SeedUatDataAsync bisa dijalankan idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/241-seed-data-uat/241-02-SUMMARY.md`
</output>
