---
phase: 242-uat-setup-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ManageCategories.cshtml
  - Views/Admin/CreateAssessment.cshtml
autonomous: false
requirements: [SETUP-01, SETUP-02]
must_haves:
  truths:
    - "Admin dapat membuat sub-kategori baru dengan parent hierarchy dan indent tampil benar"
    - "Admin/HC dapat membuat assessment baru dengan token, jadwal, durasi, generate certificate"
    - "Assessment baru muncul di daftar ManageAssessment"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "AddCategory, CreateAssessment POST handlers"
    - path: "Views/Admin/ManageCategories.cshtml"
      provides: "Category list with parent-child indent"
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "4-step wizard form"
  key_links:
    - from: "Views/Admin/ManageCategories.cshtml"
      to: "AdminController.AddCategory"
      via: "form POST"
      pattern: "asp-action=\"AddCategory\""
    - from: "Views/Admin/CreateAssessment.cshtml"
      to: "AdminController.CreateAssessment POST"
      via: "form POST"
      pattern: "asp-action=\"CreateAssessment\""
---

<objective>
UAT flow Kategori dan Assessment — code review untuk identifikasi & fix bugs, lalu user verifikasi di browser.

Purpose: Memastikan SETUP-01 (sub-kategori hierarchy) dan SETUP-02 (create assessment) berjalan tanpa error (per D-01 hybrid approach).
Output: Bug fixes (jika ada) + UAT checklist untuk verifikasi browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/242-uat-setup-flow/242-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review & fix — Kategori hierarchy + CreateAssessment flow</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManageCategories.cshtml, Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (lines 800-980 for ManageCategories/AddCategory/EditCategory/DeleteCategory, lines 987-1180 for CreateAssessment GET, lines 1175-1400 for CreateAssessment POST)
    - Views/Admin/ManageCategories.cshtml (full file — category list rendering with indent)
    - Views/Admin/CreateAssessment.cshtml (full file — 4-step wizard)
  </read_first>
  <action>
    Per D-01 (hybrid approach) dan D-02 (happy path + basic validation):

    1. **Kategori flow (SETUP-01):** Baca ManageCategories GET handler + view. Verifikasi:
       - Parent dropdown populated dengan kategori aktif yang belum punya parent (root only)
       - AddCategory POST menyimpan parentId dengan benar
       - View menampilkan indent untuk sub-kategori (child items indented under parent)
       - Validation: nama kosong ditolak, nama duplikat ditolak
       - DeleteCategory menolak jika masih punya children

    2. **Assessment flow (SETUP-02):** Baca CreateAssessment GET + POST handler + view. Verifikasi:
       - Form wizard 4-step: Kategori, Peserta, Settings (token/jadwal/durasi/certificate), Konfirmasi
       - POST menyimpan multi-user (UserIds list) dengan token, schedule, duration, GenerateCertificate
       - Assessment baru muncul di ManageAssessment list setelah create
       - Validation: required fields (title, category, minimal 1 user)

    3. **Fix blocking bugs** langsung (per D-03). Bug minor dicatat di SUMMARY untuk Phase 247.

    4. **Build project** setelah fix untuk memastikan tidak ada compile error:
       ```
       dotnet build
       ```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build menghasilkan "Build succeeded" tanpa error
    - Jika ada bug fix: perubahan sudah diterapkan di file yang relevan
    - Jika tidak ada bug: kode direview dan dikonfirmasi berfungsi
  </acceptance_criteria>
  <done>Code review selesai, blocking bugs (jika ada) diperbaiki, project build sukses</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser verification — Kategori + Assessment</name>
  <files>none</files>
  <what-built>Code review dan fix (jika ada) untuk flow Kategori hierarchy dan CreateAssessment</what-built>
  <action>User melakukan verifikasi manual di browser sesuai checklist berikut</action>
  <how-to-verify>
    **Persiapan:** Login sebagai Admin di https://localhost (atau URL development).

    **SETUP-01: Kategori Hierarchy**
    1. Buka Admin/Index > Kategori Assessment (atau navigasi ke /Admin/ManageCategories)
    2. Verifikasi seed data: "OJT Operasi Kilang" tampil dengan indent di bawah parent OJT (per D-04)
    3. Buat sub-kategori baru: isi nama "Test Sub-Kategori UAT", pilih parent dari dropdown, isi pass percentage dan sort order, klik Simpan
    4. Verifikasi sub-kategori baru tampil dengan indent di bawah parent yang dipilih
    5. Test validation: coba tambah kategori dengan nama kosong — harus muncul pesan error
    6. Test validation: coba tambah kategori dengan nama duplikat — harus muncul pesan error

    **SETUP-02: Create Assessment**
    1. Buka Admin/Index > buat assessment baru (atau navigasi ke /Admin/CreateAssessment)
    2. Step 1 (Kategori): pilih kategori, isi judul "Test Assessment UAT"
    3. Step 2 (Peserta): pilih minimal 2 peserta
    4. Step 3 (Settings): aktifkan Token Required, set jadwal besok, durasi 30 menit, aktifkan Generate Certificate
    5. Step 4 (Konfirmasi): review data, klik Submit
    6. Verifikasi assessment muncul di daftar ManageAssessment
    7. Verifikasi token ter-generate dan tampil

    **Checklist:**
    - [ ] SETUP-01: Seed sub-kategori tampil indent benar
    - [ ] SETUP-01: Create sub-kategori baru berhasil + indent benar
    - [ ] SETUP-01: Validation nama kosong ditolak
    - [ ] SETUP-01: Validation nama duplikat ditolak
    - [ ] SETUP-02: Assessment baru tersimpan dengan semua setting
    - [ ] SETUP-02: Assessment muncul di daftar
    - [ ] SETUP-02: Token ter-generate
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human browser verification"</automated>
  </verify>
  <done>Semua checklist SETUP-01 dan SETUP-02 pass di browser</done>
  <resume-signal>Ketik "approved" jika semua checklist pass, atau deskripsikan issues yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- dotnet build sukses tanpa error
- User mengkonfirmasi semua checklist SETUP-01 dan SETUP-02 pass di browser
</verification>

<success_criteria>
- Sub-kategori hierarchy tampil dengan indent yang benar
- Assessment baru dengan token, jadwal, durasi, certificate berhasil dibuat dan muncul di daftar
- Tidak ada blocking bugs pada kedua flow
</success_criteria>

<output>
After completion, create `.planning/phases/242-uat-setup-flow/242-01-SUMMARY.md`
</output>
