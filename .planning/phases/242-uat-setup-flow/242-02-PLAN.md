---
phase: 242-uat-setup-flow
plan: 02
type: execute
wave: 2
depends_on: [242-01]
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ManagePackages.cshtml
  - Views/Admin/ImportPackageQuestions.cshtml
  - Views/Admin/PreviewPackage.cshtml
autonomous: false
requirements: [SETUP-03, SETUP-04]
must_haves:
  truths:
    - "Admin dapat membuat paket soal baru pada assessment"
    - "Admin dapat import 15 soal via paste Excel dengan kolom Elemen Teknis"
    - "Semua 15 soal tersimpan dengan benar"
    - "ET coverage matrix menampilkan distribusi soal per elemen teknis"
    - "Admin dapat preview soal individual"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "CreatePackage, ImportPackageQuestions, ManagePackages, PreviewPackage handlers"
    - path: "Views/Admin/ManagePackages.cshtml"
      provides: "Package list + ET coverage matrix table"
    - path: "Views/Admin/ImportPackageQuestions.cshtml"
      provides: "Paste/upload import form"
    - path: "Views/Admin/PreviewPackage.cshtml"
      provides: "Question preview per package"
  key_links:
    - from: "Views/Admin/ImportPackageQuestions.cshtml"
      to: "AdminController.ImportPackageQuestions POST"
      via: "form POST with pasteText"
      pattern: "asp-action=\"ImportPackageQuestions\""
    - from: "Views/Admin/ManagePackages.cshtml"
      to: "ViewBag.EtCoverage"
      via: "ET coverage matrix rendering"
      pattern: "EtCoverage"
---

<objective>
UAT flow Paket Soal, Import Soal, ET Coverage Matrix, dan Preview — code review + fix + browser verification.

Purpose: Memastikan SETUP-03 (create paket + import soal) dan SETUP-04 (ET matrix + preview) berjalan tanpa error.
Output: Bug fixes (jika ada) + UAT checklist untuk verifikasi browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/242-uat-setup-flow/242-CONTEXT.md
@.planning/phases/242-uat-setup-flow/242-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review & fix — Paket Soal, Import, ET Matrix, Preview</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManagePackages.cshtml, Views/Admin/ImportPackageQuestions.cshtml, Views/Admin/PreviewPackage.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (lines 6285-6370 for ManagePackages/CreatePackage, lines 6370-6500 for DeletePackage/DownloadTemplate, lines 6496-6700 for ImportPackageQuestions GET+POST)
    - Controllers/AdminController.cs (search for "PreviewPackage" action)
    - Views/Admin/ManagePackages.cshtml (full file — package list + ET coverage matrix)
    - Views/Admin/ImportPackageQuestions.cshtml (full file — paste/upload form)
    - Views/Admin/PreviewPackage.cshtml (full file — question preview)
  </read_first>
  <action>
    Per D-01 (hybrid approach) dan D-02 (happy path + basic validation):

    1. **Create Package (SETUP-03):** Verifikasi CreatePackage POST:
       - packageName required validation
       - PackageNumber auto-increment
       - Redirect kembali ke ManagePackages

    2. **Import soal via paste (SETUP-03):** Verifikasi ImportPackageQuestions POST:
       - Tab-separated paste parsing (6-7 kolom: Question, OptA-D, Correct, ElemenTeknis)
       - Header row detection (skip jika "correct" di kolom 6)
       - Validation: minimal 6 kolom, question text not empty, options not empty, correct A/B/C/D
       - Kolom 7 (ElemenTeknis) optional — disimpan ke PackageQuestion.ElemenTeknis
       - Duplicate detection via fingerprint
       - Cross-package count validation (jumlah soal harus sama dengan sibling package)

    3. **ET Coverage Matrix (SETUP-04):** Verifikasi ManagePackages GET:
       - allEtGroups extracted dari semua questions across packages
       - etCoverage dictionary: ET group -> packageId -> count
       - View merender tabel matrix dengan benar

    4. **Preview (SETUP-04):** Verifikasi PreviewPackage action + view:
       - Questions tampil dengan opsi dan correct answer highlighted
       - ElemenTeknis tampil per soal

    5. **Fix blocking bugs** langsung (per D-03). Bug minor dicatat di SUMMARY.

    6. **Build project:**
       ```
       dotnet build
       ```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build menghasilkan "Build succeeded" tanpa error
    - Jika ada bug fix: perubahan sudah diterapkan di file yang relevan
    - Jika tidak ada bug: kode direview dan dikonfirmasi berfungsi
  </acceptance_criteria>
  <done>Code review selesai, blocking bugs (jika ada) diperbaiki, project build sukses</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser verification — Paket Soal, Import, ET Matrix, Preview</name>
  <files>none</files>
  <what-built>Code review dan fix (jika ada) untuk flow paket soal, import, ET coverage matrix, dan preview</what-built>
  <action>User melakukan verifikasi manual di browser sesuai checklist berikut</action>
  <how-to-verify>
    **Persiapan:** Login sebagai Admin. Gunakan assessment "Test Assessment UAT" yang dibuat di Plan 01, ATAU assessment seed "OJT Proses Alkylation Q1-2026".

    **SETUP-03: Paket Soal + Import**
    1. Buka ManagePackages untuk assessment yang dipilih
    2. Verifikasi seed data: Paket A dengan 15 soal tampil (per D-04)
    3. Buat paket baru: isi nama "Paket UAT Test", klik Create
    4. Verifikasi paket baru muncul di daftar
    5. Klik Import pada paket baru
    6. Siapkan 15 baris data tab-separated di Excel/spreadsheet:
       - Kolom: Question | OptA | OptB | OptC | OptD | Correct (A/B/C/D) | Elemen Teknis
    7. Copy-paste ke textarea, klik Import
    8. Verifikasi: 15 soal berhasil disimpan (tidak ada error, redirect ke ManagePackages)
    9. Verifikasi: jumlah soal "Paket UAT Test" = 15

    **SETUP-04: ET Coverage Matrix + Preview**
    1. Di halaman ManagePackages, lihat bagian ET Coverage Matrix
    2. Verifikasi matrix menampilkan rows = elemen teknis groups, columns = per paket
    3. Verifikasi angka distribusi soal per elemen per paket tampil
    4. Klik Preview pada salah satu paket
    5. Verifikasi soal tampil dengan opsi jawaban
    6. Verifikasi elemen teknis tampil per soal (jika diisi)

    **Checklist:**
    - [ ] SETUP-03: Seed Paket A + 15 soal tampil
    - [ ] SETUP-03: Create paket baru berhasil
    - [ ] SETUP-03: Import 15 soal via paste berhasil
    - [ ] SETUP-03: Semua 15 soal tersimpan dengan Elemen Teknis
    - [ ] SETUP-04: ET coverage matrix tampil dengan distribusi benar
    - [ ] SETUP-04: Preview soal individual tampil dengan opsi + ET
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human browser verification"</automated>
  </verify>
  <done>Semua checklist SETUP-03 dan SETUP-04 pass di browser</done>
  <resume-signal>Ketik "approved" jika semua checklist pass, atau deskripsikan issues yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- dotnet build sukses tanpa error
- User mengkonfirmasi semua checklist SETUP-03 dan SETUP-04 pass di browser
</verification>

<success_criteria>
- Paket soal baru berhasil dibuat
- 15 soal berhasil diimport via paste Excel dengan Elemen Teknis tersimpan
- ET coverage matrix menampilkan distribusi soal per elemen yang akurat
- Preview soal individual menampilkan soal, opsi, dan elemen teknis
</success_criteria>

<output>
After completion, create `.planning/phases/242-uat-setup-flow/242-02-SUMMARY.md`
</output>
