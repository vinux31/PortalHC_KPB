---
phase: 243-uat-exam-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [EXAM-01, EXAM-02, EXAM-03, EXAM-04]

must_haves:
  truths:
    - "Worker melihat assessment yang ditugaskan di halaman Assessment"
    - "Worker input token yang benar dan ujian dimulai (status InProgress)"
    - "Soal ditampilkan dalam urutan acak dengan pagination dan auto-save per klik"
    - "Timer countdown berjalan akurat berdasarkan wall-clock, warning visual saat ≤5 menit"
    - "Timer habis → ujian auto-submit"
    - "Worker disconnect lalu kembali → resume dari page terakhir, sisa waktu akurat, jawaban sebelumnya intact"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "StartExam, TakeExam exam lifecycle"
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Timer JS, auto-save JS, pagination JS, resume logic"
    - path: "Views/CMP/Assessment.cshtml"
      provides: "Daftar assessment + token input"
  key_links:
    - from: "Views/CMP/Assessment.cshtml"
      to: "CMPController.StartExam"
      via: "Token verification → redirect to StartExam"
    - from: "Views/CMP/StartExam.cshtml"
      to: "CMPController.SaveAnswer API"
      via: "AJAX auto-save per click"
    - from: "StartExam timer JS"
      to: "CMPController.SubmitExam"
      via: "Auto-submit when timer reaches 0"
---

<objective>
Code review dan UAT alur ujian bagian pertama: mulai ujian (token → start), mengerjakan ujian (soal acak, pagination, auto-save), timer countdown (warning + auto-submit), dan disconnect/resume.

Purpose: Verifikasi bahwa mekanisme integritas ujian (timer wall-clock, auto-save, resume state) berfungsi benar sebelum lanjut ke grading & sertifikat.
Output: Laporan code review + UAT checkpoint user verifikasi di browser.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/243-uat-exam-flow/243-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review exam start, take, timer, dan resume (EXAM-01 s/d EXAM-04)</name>
  <read_first>
    Controllers/CMPController.cs
    Views/CMP/Assessment.cshtml
    Views/CMP/StartExam.cshtml
    Models/PackageExamViewModel.cs
    Models/AssessmentSession.cs
  </read_first>
  <files></files>
  <action>
Analisis kode untuk memverifikasi logic berikut (per D-03: Claude analisis kode, user verifikasi visual):

**EXAM-01 — Assessment list + token + start:**
- CMPController.Assessment(): Pastikan query memfilter assessment berdasarkan UserId user yang login
- Views/CMP/Assessment.cshtml: Pastikan ada form input token dan mekanisme verifikasi token sebelum redirect ke StartExam
- CMPController.StartExam(): Verifikasi flow token check via TempData["TokenVerified_{id}"], status transition Open→InProgress, StartedAt set

**EXAM-02 — Soal acak, pagination, auto-save:**
- StartExam(): Verifikasi UserPackageAssignment dibuat dengan ShuffledQuestionIds (Fisher-Yates shuffle) dan ShuffledOptionIdsPerQuestion
- Views/CMP/StartExam.cshtml: Verifikasi JS pagination (soal per halaman), auto-save via AJAX pada setiap klik opsi (endpoint SaveAnswer)
- CMPController: Cari endpoint SaveAnswer — verifikasi menerima questionId + optionId, upsert ke PackageUserResponses

**EXAM-03 — Timer countdown + warning + auto-submit:**
- Views/CMP/StartExam.cshtml JS: Verifikasi timer menggunakan wall-clock (Date.now() atau setInterval dengan drift correction), bukan naive setInterval
- Verifikasi warning visual (CSS class change / modal) saat remainingSeconds ≤ 300 (5 menit)
- Verifikasi auto-submit: saat timer = 0, JS trigger form submit ke SubmitExam

**EXAM-04 — Disconnect/resume:**
- StartExam(): Verifikasi isResume logic — ElapsedSeconds digunakan untuk menghitung RemainingSeconds
- Verifikasi ViewBag.LastActivePage diset dari assessment.LastActivePage
- Verifikasi ViewBag.SavedAnswers di-load dari PackageUserResponses dan di-serialize ke JSON
- Views/CMP/StartExam.cshtml JS: Verifikasi pada load, savedAnswers di-apply ke radio buttons, page diset ke LastActivePage

Buat laporan temuan: OK atau BUG per item. Jika ada bug critical/blocking, fix langsung (per D-01).
  </action>
  <verify>
    <automated>grep -n "SaveAnswer\|ElapsedSeconds\|LastActivePage\|RemainingSeconds\|TokenVerified" Controllers/CMPController.cs | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Laporan code review mencakup semua 4 requirement (EXAM-01 s/d EXAM-04) dengan status OK atau BUG per item
    - Jika ada bug critical ditemukan, fix sudah di-apply dan `dotnet build` passes
    - File SUMMARY mencatat setiap temuan
  </acceptance_criteria>
  <done>Code review EXAM-01 s/d EXAM-04 selesai. Setiap requirement punya status OK/BUG. Bug critical sudah di-fix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser — start exam, take exam, timer, resume</name>
  <what-built>Code review selesai untuk exam start/take/timer/resume. Sekarang user verifikasi visual di browser.</what-built>
  <how-to-verify>
Login sebagai Rino (worker). Lakukan skenario berikut:

**Skenario 1 — EXAM-01: Start Exam**
1. Buka /CMP/Assessment
2. Lihat assessment "OJT Proses Alkylation Q1-2026" di daftar
3. Klik assessment → muncul form input token
4. Input token yang benar → ujian dimulai (halaman soal muncul)

**Skenario 2 — EXAM-02: Take Exam**
5. Lihat soal ditampilkan (urutan mungkin berbeda dari seed karena shuffle)
6. Klik salah satu opsi jawaban → jawaban tersimpan (tidak perlu klik Save)
7. Navigasi ke halaman soal berikutnya (pagination)
8. Kembali ke halaman sebelumnya → jawaban yang dipilih masih tercentang

**Skenario 3 — EXAM-03: Timer**
9. Lihat timer countdown di halaman ujian — angka berjalan mundur
10. (Opsional) Jika bisa tunggu hingga ≤5 menit: lihat warning visual muncul

**Skenario 4 — EXAM-04: Resume**
11. Tutup browser tab (disconnect)
12. Buka kembali halaman ujian (akses /CMP/StartExam/{id})
13. Verifikasi: jawaban sebelumnya masih tercentang
14. Verifikasi: timer melanjutkan dari sisa waktu (bukan reset dari awal)
15. Verifikasi: halaman soal kembali ke page terakhir yang dikerjakan

Laporkan: setiap skenario PASS atau FAIL + deskripsi masalah.
  </how-to-verify>
  <resume-signal>Ketik hasil UAT per skenario (PASS/FAIL). Jika ada FAIL, deskripsikan masalah.</resume-signal>
</task>

</tasks>

<verification>
- Code review mencakup semua logic path EXAM-01 s/d EXAM-04
- Bug critical/blocking sudah di-fix (per D-01)
- User telah verifikasi 4 skenario UAT di browser
</verification>

<success_criteria>
- Semua 4 skenario UAT (start, take, timer, resume) PASS di browser
- Atau: bug ditemukan, di-fix, dan diverifikasi ulang
</success_criteria>

<output>
After completion, create `.planning/phases/243-uat-exam-flow/243-01-SUMMARY.md`
</output>
