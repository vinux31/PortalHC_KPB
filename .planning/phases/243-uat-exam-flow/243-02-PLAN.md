---
phase: 243-uat-exam-flow
plan: 02
type: execute
wave: 2
depends_on: [243-01]
files_modified: []
autonomous: false
requirements: [EXAM-05, EXAM-06, EXAM-07]

must_haves:
  truths:
    - "Worker melihat ringkasan jawaban di ExamSummary dengan indikator terjawab/belum"
    - "Worker submit dan sistem menampilkan skor persentase"
    - "Hasil assessment menampilkan radar chart ET dan tinjauan jawaban benar/salah"
    - "Worker yang lulus melihat sertifikat dengan nomor format KPB/SEQ/BULAN/TAHUN"
    - "Sertifikat dapat dicetak/download sebagai PDF"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "ExamSummary, SubmitExam, Results, Certificate, CertificatePdf"
    - path: "Views/CMP/ExamSummary.cshtml"
      provides: "Ringkasan jawaban + submit form"
    - path: "Views/CMP/Results.cshtml"
      provides: "Skor + radar chart ET + answer review"
    - path: "Views/CMP/Certificate.cshtml"
      provides: "Tampilan sertifikat + print/PDF button"
  key_links:
    - from: "ExamSummary submit"
      to: "CMPController.SubmitExam"
      via: "POST form with answers dictionary"
    - from: "SubmitExam"
      to: "SessionElemenTeknisScores"
      via: "ET score persist per group"
    - from: "Results.cshtml"
      to: "SessionElemenTeknisScores"
      via: "Radar chart data from ET scores"
    - from: "SubmitExam"
      to: "CertNumberHelper"
      via: "Auto-generate nomor sertifikat KPB/SEQ/BULAN/TAHUN"
---

<objective>
Code review dan UAT alur ujian bagian kedua: review jawaban (ExamSummary), submit & grading otomatis, hasil assessment (skor + radar chart ET), dan sertifikat (nomor otomatis + PDF).

Purpose: Verifikasi bahwa grading, ET scoring, dan sertifikat generation berfungsi benar end-to-end.
Output: Laporan code review + UAT checkpoint user verifikasi di browser.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/243-uat-exam-flow/243-CONTEXT.md
@.planning/phases/243-uat-exam-flow/243-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review ExamSummary, SubmitExam, Results, dan Certificate (EXAM-05 s/d EXAM-07)</name>
  <read_first>
    Controllers/CMPController.cs
    Views/CMP/ExamSummary.cshtml
    Views/CMP/Results.cshtml
    Views/CMP/Certificate.cshtml
    Models/AssessmentResultsViewModel.cs
    Helpers/CertNumberHelper.cs
  </read_first>
  <files></files>
  <action>
Analisis kode untuk memverifikasi logic berikut (per D-03):

**EXAM-05 — ExamSummary review + submit:**
- CMPController.ExamSummary(POST): Verifikasi answers disimpan ke TempData lalu redirect ke GET
- CMPController.ExamSummary(GET): Verifikasi summary items dibuild dari ShuffledQuestionIds, menampilkan nomor soal + teks soal + jawaban terpilih (atau "Belum dijawab")
- Views/CMP/ExamSummary.cshtml: Verifikasi ada indikator visual terjawab/belum per soal, jumlah belum terjawab (UnansweredCount), tombol Submit, dan hidden form dengan semua answers

**EXAM-05 — SubmitExam grading:**
- CMPController.SubmitExam(): Verifikasi grading loop: iterate shuffledIds, cek selectedOption.IsCorrect, hitung totalScore, finalPercentage = totalScore/maxScore * 100
- Verifikasi IsPassed = finalPercentage >= assessment.PassPercentage
- Verifikasi PackageUserResponses di-upsert (bukan duplicate insert)

**EXAM-06 — Results dengan radar chart ET:**
- CMPController: Cari action Results(int id) — verifikasi load SessionElemenTeknisScores untuk radar chart data
- Views/CMP/Results.cshtml: Verifikasi radar chart (Chart.js atau library lain) render ET scores, dan ada section answer review yang highlight jawaban benar (hijau) vs salah (merah)
- SubmitExam(): Verifikasi ET scores persist ke SessionElemenTeknisScores — groupBy ElemenTeknis, hitung correct/total per group

**EXAM-07 — Certificate + nomor otomatis + PDF:**
- SubmitExam(): Verifikasi GenerateCertificate check + CertNumberHelper.Build() format KPB/SEQ/BULAN/TAHUN
- Helpers/CertNumberHelper.cs: Verifikasi format Build() menghasilkan "KPB/{seq:D4}/{bulan romawi}/{tahun}" atau format serupa
- CMPController.Certificate(): Verifikasi guard (Completed + IsPassed + GenerateCertificate)
- CMPController.CertificatePdf(): Verifikasi PDF generation (QuestPDF atau library lain)
- Views/CMP/Certificate.cshtml: Verifikasi ada tombol Print dan Download PDF

Buat laporan temuan: OK atau BUG per item. Fix bug critical langsung (per D-01).
  </action>
  <verify>
    <automated>grep -n "CertNumberHelper\|SessionElemenTeknisScore\|radar\|IsPassed\|finalPercentage" Controllers/CMPController.cs | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Laporan code review mencakup semua 3 requirement (EXAM-05 s/d EXAM-07) dengan status OK atau BUG per item
    - CertNumberHelper.Build menghasilkan format yang mengandung "KPB/" — verifikasi via grep di Helpers/CertNumberHelper.cs
    - Jika ada bug critical, fix sudah di-apply dan `dotnet build` passes
  </acceptance_criteria>
  <done>Code review EXAM-05 s/d EXAM-07 selesai. Setiap requirement punya status OK/BUG. Bug critical sudah di-fix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser — summary, submit, results, certificate</name>
  <what-built>Code review selesai untuk ExamSummary/SubmitExam/Results/Certificate. Sekarang user verifikasi di browser. (Lanjutan dari Plan 01 — ujian sudah in-progress.)</what-built>
  <how-to-verify>
Login sebagai Rino (worker). Lanjutkan ujian dari Plan 01.

**Skenario 5 — EXAM-05: ExamSummary + Submit**
1. Selesaikan menjawab semua soal (atau sebagian untuk test unanswered count)
2. Klik tombol "Selesai" / navigasi ke ExamSummary
3. Lihat ringkasan: nomor soal, teks soal, jawaban terpilih atau "Belum dijawab"
4. Lihat jumlah soal belum terjawab ditampilkan
5. Klik Submit → sistem memproses grading

**Skenario 6 — EXAM-06: Results + Radar Chart ET**
6. Setelah submit, lihat halaman Results
7. Verifikasi skor persentase ditampilkan (misal "80%")
8. Verifikasi radar chart ET muncul (grafik spider/radar dengan label Elemen Teknis)
9. Verifikasi ada section tinjauan jawaban: jawaban benar ditandai hijau, jawaban salah ditandai merah

**Skenario 7 — EXAM-07: Certificate + PDF**
10. Jika skor lulus (>= passing grade): lihat tombol/link ke sertifikat
11. Buka halaman Certificate → verifikasi nomor sertifikat format "KPB/XXXX/BULAN/TAHUN"
12. Klik Print → dialog print browser muncul
13. Klik Download PDF → file PDF terdownload

Jika skor tidak lulus: verifikasi pesan "Certificate only for passed" dan tidak ada link sertifikat. Gunakan assessment completed dari seed data (SEED-07) yang sudah lulus untuk test sertifikat.

Laporkan: setiap skenario PASS atau FAIL + deskripsi masalah.
  </how-to-verify>
  <resume-signal>Ketik hasil UAT per skenario (PASS/FAIL). Jika ada FAIL, deskripsikan masalah.</resume-signal>
</task>

</tasks>

<verification>
- Code review mencakup semua logic path EXAM-05 s/d EXAM-07
- Bug critical/blocking sudah di-fix (per D-01)
- User telah verifikasi 3 skenario UAT di browser
</verification>

<success_criteria>
- Semua 3 skenario UAT (summary+submit, results+radar, certificate+PDF) PASS di browser
- Atau: bug ditemukan, di-fix, dan diverifikasi ulang
</success_criteria>

<output>
After completion, create `.planning/phases/243-uat-exam-flow/243-02-SUMMARY.md`
</output>
