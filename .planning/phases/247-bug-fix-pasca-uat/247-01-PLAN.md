---
phase: 247-bug-fix-pasca-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "ET distribution Phase 2 membagi soal sisa secara round-robin per-ET, bukan per-package"
    - "Setiap ET mendapat jumlah soal yang seimbang (gap maksimal 1 soal antar ET)"
    - "NULL-ET questions tetap bisa ter-pick sebagai fallback jika kuota ET belum terpenuhi"
    - "COACH_EVIDENCE_RESUBMITTED notification sudah terverifikasi benar di kode"
    - "REQUIREMENTS.md SETUP-01/02 status updated ke Complete"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Fixed BuildCrossPackageAssignment Phase 2 — round-robin per-ET"
      contains: "basePerET"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "BuildCrossPackageAssignment Phase 2"
      via: "round-robin per-ET distribution"
      pattern: "remaining / etGroups\\.Count"
---

<objective>
Fix ET distribution algorithm (BUG-01) agar distribusi soal Phase 2 seimbang per Elemen Teknis, verifikasi BUG-02 (notifikasi resubmit), dan update admin status REQUIREMENTS.md.

Purpose: Memperbaiki bug utama yang ditemukan selama UAT — radar chart ET menjadi representatif setelah distribusi soal balanced.
Output: CMPController.cs dengan Phase 2 per-ET distribution, REQUIREMENTS.md updated.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/247-bug-fix-pasca-uat/247-CONTEXT.md
@.planning/phases/247-bug-fix-pasca-uat/247-RESEARCH.md

<interfaces>
<!-- BuildCrossPackageAssignment Phase 2 — baris 1099-1148 CMPController.cs -->
<!-- Phase 1 (baris 1079-1097): guarantee 1 per ET — JANGAN DIUBAH -->
<!-- Phase 2 (baris 1099-1148): distribusi per-package — HARUS DIGANTI ke per-ET -->
<!-- Phase 3 (baris 1151-1153): Fisher-Yates shuffle — JANGAN DIUBAH -->

From Controllers/CMPController.cs (Phase 1 output variables used by Phase 2):
```csharp
var selectedIds = new HashSet<int>();   // IDs soal yang sudah dipilih Phase 1
var selectedList = new List<int>();      // Ordered list soal yang dipilih
var etGroups = ...;                     // List<string> nama ET groups (non-null)
var allQuestions = ...;                 // Semua soal dari semua packages
int K = ...;                           // Total soal yang dibutuhkan
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ET Distribution Phase 2 — Round-Robin per-ET (D-01)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (baris 1075-1153 — Phase 1, 2, 3 BuildCrossPackageAssignment)
    - .planning/phases/247-bug-fix-pasca-uat/247-RESEARCH.md (Pattern 1: Fix ET Distribution)
  </read_first>
  <action>
Ganti HANYA blok Phase 2 (baris 1099-1148) di method BuildCrossPackageAssignment. Jangan ubah Phase 1 (baris 1079-1097) dan Phase 3 (baris 1151-1153). Per D-01.

Ganti kode lama:
```csharp
// Phase 2 — Fill remaining quota with balanced package distribution
int remaining = K - selectedIds.Count;
if (remaining > 0)
{
    int N = packages.Count;
    // ... distribusi per-package ...
}
```

Dengan kode baru:
```csharp
// Phase 2 — Fill remaining quota with balanced ET distribution (round-robin per-ET)
int remaining = K - selectedIds.Count;
if (remaining > 0)
{
    int M = etGroups.Count;
    int basePerET = remaining / M;
    int extraCount = remaining % M;
    var extraETs = etGroups.OrderBy(_ => rng.Next()).Take(extraCount).ToHashSet();

    foreach (var et in etGroups)
    {
        int quota = basePerET + (extraETs.Contains(et) ? 1 : 0);
        var etCandidates = allQuestions
            .Where(x => x.Question.ElemenTeknis == et && !selectedIds.Contains(x.Question.Id))
            .Select(x => x.Question.Id)
            .ToList();
        Shuffle(etCandidates, rng);
        int toTake = Math.Min(quota, etCandidates.Count);
        foreach (var id in etCandidates.Take(toTake))
        {
            selectedIds.Add(id);
            selectedList.Add(id);
        }
    }

    // Fallback: jika masih kurang (ET kehabisan soal), ambil dari NULL-ET atau sisa soal manapun
    if (selectedIds.Count < K)
    {
        var fallbackCandidates = allQuestions
            .Where(x => !selectedIds.Contains(x.Question.Id))
            .Select(x => x.Question.Id)
            .ToList();
        Shuffle(fallbackCandidates, rng);
        foreach (var id in fallbackCandidates.Take(K - selectedIds.Count))
        {
            selectedIds.Add(id);
            selectedList.Add(id);
        }
    }
}
```

Edge cases yang di-handle:
- ET dengan soal lebih sedikit dari quota: `Math.Min(quota, etCandidates.Count)`
- Total soal kurang dari K: fallback pool dari semua sisa soal (termasuk NULL-ET)
- Extra soal didistribusi random ke ET subset via `extraETs`
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CMPController.cs baris Phase 2 mengandung `int M = etGroups.Count;` (bukan `int N = packages.Count;`)
    - CMPController.cs mengandung `int basePerET = remaining / M;`
    - CMPController.cs mengandung `var extraETs = etGroups.OrderBy`
    - CMPController.cs mengandung fallback block `if (selectedIds.Count < K)`
    - Phase 1 (baris ~1079-1097) TIDAK berubah — masih mengandung `foreach (var etGroup in etGroups)`
    - Phase 3 (baris ~1151-1153) TIDAK berubah — masih mengandung `Shuffle(selectedList, rng)`
    - `dotnet build` berhasil tanpa error
  </acceptance_criteria>
  <done>Phase 2 BuildCrossPackageAssignment menggunakan round-robin per-ET. Build sukses. Phase 1 dan 3 tidak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 2: Verifikasi BUG-02 + Update REQUIREMENTS.md (D-03, D-06)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - Controllers/CDPController.cs (baris 2195-2300 — SubmitEvidenceWithCoaching + resubmitFlags + COACH_EVIDENCE_RESUBMITTED)
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
**Bagian A — Code review BUG-02 notifikasi resubmit:**
Baca CDPController.cs baris 2195-2300. Verifikasi bahwa:
1. `resubmitFlags` dictionary di-populate SEBELUM status deliverable diubah (baris ~2195)
2. Block `COACH_EVIDENCE_RESUBMITTED` (baris 2265-2300) mengirim notifikasi ke coach HANYA jika ada deliverable yang sebelumnya `Rejected`
3. Notification type string persis `"COACH_EVIDENCE_RESUBMITTED"`

Jika kode sudah benar: catat di SUMMARY bahwa BUG-02 verified — no fix needed.
Jika ada bug: fix langsung di CDPController.cs.

**Bagian B — Update REQUIREMENTS.md status (D-03, D-06):**
REQUIREMENTS.md sudah menandai SETUP-01 dan SETUP-02 sebagai `[x]` (checked). Verifikasi ini sudah benar.

Juga di bagian Traceability table, pastikan baris SETUP-01 dan SETUP-02 sudah menunjukkan `Complete`.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -n "COACH_EVIDENCE_RESUBMITTED" Controllers/CDPController.cs | head -5 && grep -c "SETUP-01.*Complete\|SETUP-02.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - CDPController.cs mengandung string "COACH_EVIDENCE_RESUBMITTED" di block SubmitEvidenceWithCoaching
    - REQUIREMENTS.md baris SETUP-01 mengandung `[x]` dan Traceability row mengandung `Complete`
    - REQUIREMENTS.md baris SETUP-02 mengandung `[x]` dan Traceability row mengandung `Complete`
  </acceptance_criteria>
  <done>BUG-02 diverifikasi (fix sudah ada atau diperbaiki). REQUIREMENTS.md SETUP-01/02 confirmed Complete.</done>
</task>

</tasks>

<verification>
- `dotnet build` berhasil tanpa error
- Phase 2 BuildCrossPackageAssignment menggunakan `etGroups.Count` bukan `packages.Count`
- Grep `basePerET` ditemukan di CMPController.cs
- BUG-02 notification code terverifikasi
</verification>

<success_criteria>
- ET distribution algorithm fixed: soal terdistribusi balanced per-ET (gap max 1 antar ET)
- BUG-02 verified atau fixed
- REQUIREMENTS.md status up to date
- Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/247-bug-fix-pasca-uat/247-01-SUMMARY.md`
</output>
