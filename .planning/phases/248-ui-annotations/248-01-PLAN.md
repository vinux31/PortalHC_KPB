---
phase: 248-ui-annotations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/css/site.css
  - Views/Shared/_Layout.cshtml
  - Models/TrainingRecord.cs
  - Models/ProtonModels.cs
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Badge Proton di AssessmentMonitoring tampil dengan warna ungu (bukan transparan/kosong)"
    - "String fields TrainingRecord memiliki MaxLength — EF Core tidak menggunakan nvarchar(MAX)"
    - "CompetencyLevelGranted menolak nilai di luar 0-5 pada validasi model"
  artifacts:
    - path: "wwwroot/css/site.css"
      provides: "CSS global termasuk .bg-purple"
      contains: ".bg-purple"
    - path: "Models/TrainingRecord.cs"
      provides: "MaxLength annotations pada string fields"
      contains: "[MaxLength("
    - path: "Models/ProtonModels.cs"
      provides: "Range annotation pada CompetencyLevelGranted"
      contains: "[Range(0, 5)]"
  key_links:
    - from: "Views/Shared/_Layout.cshtml"
      to: "wwwroot/css/site.css"
      via: "link stylesheet"
      pattern: "site\\.css"
    - from: "Views/Admin/AssessmentMonitoring.cshtml"
      to: "wwwroot/css/site.css"
      via: "CSS class bg-purple loaded globally"
      pattern: "bg-purple"
---

<objective>
Tambah CSS global (.bg-purple) dan data annotations (MaxLength, Range) pada model tanpa mengubah logika apapun.

Purpose: Badge Proton tampil benar di semua halaman, string fields punya batas panjang di database, dan CompetencyLevelGranted tervalidasi range-nya.
Output: site.css baru, _Layout.cshtml updated, TrainingRecord.cs dan ProtonModels.cs dengan annotations.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat site.css global dengan .bg-purple dan link di _Layout.cshtml</name>
  <files>wwwroot/css/site.css, Views/Shared/_Layout.cshtml</files>
  <read_first>
    - Views/CMP/Assessment.cshtml (line 409-414 — existing .bg-purple definition to replicate)
    - Views/Shared/_Layout.cshtml (full file — find where to insert link tag)
    - wwwroot/css/home.css (pattern reference for CSS file style)
  </read_first>
  <action>
Per D-01 dan D-02:

1. Buat file `wwwroot/css/site.css` dengan isi:
```css
/* ===== Global Utility Classes ===== */

/* Purple badge for Proton categories */
.bg-purple {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
    color: white !important;
}
```
Warna diambil dari definisi inline yang sudah ada di Assessment.cshtml line 412.

2. Di `Views/Shared/_Layout.cshtml`, tambahkan link tag SETELAH baris AOS CSS (line 25 `<link href="https://unpkg.com/aos@2.3.1/dist/aos.css"`):
```html
<link rel="stylesheet" href="~/css/site.css" />
```

3. JANGAN hapus definisi .bg-purple inline di Assessment.cshtml — biarkan saja, CSS global akan berlaku di halaman lain yang belum punya definisi inline (AssessmentMonitoring, AssessmentMonitoringDetail, ManageAssessment).
  </action>
  <verify>
    <automated>grep -c "bg-purple" wwwroot/css/site.css && grep -c "site.css" Views/Shared/_Layout.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - wwwroot/css/site.css exists and contains `.bg-purple {`
    - wwwroot/css/site.css contains `background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;`
    - wwwroot/css/site.css contains `color: white !important;`
    - Views/Shared/_Layout.cshtml contains `site.css`
    - Link tag appears AFTER the AOS CSS link line
  </acceptance_criteria>
  <done>File site.css ada dengan .bg-purple, _Layout.cshtml me-link-nya secara global</done>
</task>

<task type="auto">
  <name>Task 2: Tambah MaxLength dan Range annotations pada model</name>
  <files>Models/TrainingRecord.cs, Models/ProtonModels.cs</files>
  <read_first>
    - Models/TrainingRecord.cs (full file — current state of all string fields)
    - Models/ProtonModels.cs (full file — find CompetencyLevelGranted at line ~217)
  </read_first>
  <action>
Per D-03, tambah `[MaxLength]` pada SEMUA string fields di TrainingRecord.cs yang belum punya annotation. Tambah `using System.ComponentModel.DataAnnotations;` jika belum ada. Nilai MaxLength per field:

- `Judul` — `[MaxLength(200)]` (nama training bisa panjang)
- `Kategori` — `[MaxLength(50)]` (enum-like: "PROTON", "OJT", "MANDATORY")
- `Penyelenggara` — `[MaxLength(100)]` (nama organisasi)
- `Status` — `[MaxLength(20)]` (enum-like: "Passed", "Valid", "Expired", "Failed")
- `SertifikatUrl` — `[MaxLength(500)]` (URL bisa panjang)
- `CertificateType` — `[MaxLength(20)]` (enum-like: "Permanent", "Annual", "3-Year")
- `NomorSertifikat` — `[MaxLength(100)]` (certificate number)
- `Kota` — `[MaxLength(100)]` (nama kota)
- `SubKategori` — `[MaxLength(100)]` (sub category name)

Catatan: `UserId` sudah bertipe `string` tapi ini FK ke Identity — JANGAN tambah MaxLength pada UserId.

Per D-04, tambah `[Range(0, 5)]` pada `CompetencyLevelGranted` di Models/ProtonModels.cs (line ~217). Tambah `using System.ComponentModel.DataAnnotations;` jika belum ada.

JANGAN ubah logika, tipe data, atau default value apapun. Hanya tambah attribute annotations.
  </action>
  <verify>
    <automated>grep -c "MaxLength" Models/TrainingRecord.cs && grep -c "Range(0, 5)" Models/ProtonModels.cs</automated>
  </verify>
  <acceptance_criteria>
    - Models/TrainingRecord.cs contains `[MaxLength(200)]` before Judul property
    - Models/TrainingRecord.cs contains `[MaxLength(50)]` before Kategori property
    - Models/TrainingRecord.cs contains `[MaxLength(100)]` before Penyelenggara property
    - Models/TrainingRecord.cs contains `[MaxLength(20)]` before Status property
    - Models/TrainingRecord.cs contains `[MaxLength(500)]` before SertifikatUrl property
    - Models/TrainingRecord.cs contains `[MaxLength(20)]` before CertificateType property
    - Models/TrainingRecord.cs contains `[MaxLength(100)]` before NomorSertifikat property
    - Models/TrainingRecord.cs contains `[MaxLength(100)]` before Kota property
    - Models/TrainingRecord.cs contains `[MaxLength(100)]` before SubKategori property
    - Models/ProtonModels.cs contains `[Range(0, 5)]` before CompetencyLevelGranted property
    - Total grep count for MaxLength in TrainingRecord.cs is 9
  </acceptance_criteria>
  <done>9 string fields di TrainingRecord punya MaxLength, CompetencyLevelGranted punya Range(0,5)</done>
</task>

</tasks>

<verification>
1. `grep "bg-purple" wwwroot/css/site.css` — returns match
2. `grep "site.css" Views/Shared/_Layout.cshtml` — returns match
3. `grep -c "MaxLength" Models/TrainingRecord.cs` — returns 9
4. `grep "Range(0, 5)" Models/ProtonModels.cs` — returns match
5. `dotnet build` — compiles without errors
</verification>

<success_criteria>
- site.css global ada dan ter-link di _Layout.cshtml
- Semua 4 halaman yang menggunakan bg-purple akan mendapat styling dari CSS global
- 9 string fields di TrainingRecord memiliki MaxLength annotation
- CompetencyLevelGranted memiliki Range(0,5) annotation
- Project compiles tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/248-ui-annotations/248-01-SUMMARY.md`
</output>
