---
phase: 250-security-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/Assessment.cshtml
  - Views/CDP/CoachingProton.cshtml
  - Controllers/HomeController.cs
autonomous: true
requirements: [SEC-01, SEC-02, PERF-01]
must_haves:
  truths:
    - "Halaman Assessment tidak menampilkan token atau response payload di console DevTools"
    - "Nama approver dengan karakter HTML tampil sebagai teks literal di tooltip CoachingProton"
    - "Dashboard refresh berulang hanya memicu TriggerCertExpiredNotificationsAsync sekali per jam"
  artifacts:
    - path: "Views/CMP/Assessment.cshtml"
      provides: "Assessment view tanpa console.log sensitif"
      contains: "NO console.log on lines 639, 651, 682, 694"
    - path: "Views/CDP/CoachingProton.cshtml"
      provides: "XSS-safe approval badge tooltip"
      contains: "WebUtility.HtmlEncode"
    - path: "Controllers/HomeController.cs"
      provides: "Throttled cert notification trigger"
      contains: "IMemoryCache"
  key_links:
    - from: "Controllers/HomeController.cs"
      to: "IMemoryCache DI"
      via: "constructor injection"
      pattern: "IMemoryCache cache"
---

<objective>
Hapus kebocoran data sensitif via console.log, tutup XSS vector di CoachingProton tooltip, dan throttle query notifikasi sertifikat expired agar hanya berjalan maksimal 1x per jam.

Purpose: Hardening keamanan dan performa dashboard — tiga fix atomik tanpa fitur baru.
Output: Tiga file existing dimodifikasi, build tetap green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/250-security-performance/250-CONTEXT.md
@.planning/phases/250-security-performance/250-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hapus console.log sensitif di Assessment.cshtml (SEC-01)</name>
  <files>Views/CMP/Assessment.cshtml</files>
  <read_first>Views/CMP/Assessment.cshtml</read_first>
  <action>
Per D-01 dan D-02: Hapus 4 baris console.log berikut dari Assessment.cshtml:

1. Line ~639: `console.log('Verifying token for assessment:', currentAssessmentId, 'with token:', token);` — mengekspos token asli
2. Line ~651: `console.log('VerifyToken response:', response);` — mengekspos response payload
3. Line ~682: `console.log('Starting standard assessment:', id);`
4. Line ~694: `console.log('StartAssessment response:', response);`

Hapus baris sepenuhnya (delete line, bukan comment out). JANGAN hapus `console.error(...)` di line ~663 dan ~703 — ini di error handler dan tidak mengekspos data sensitif.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; grep -n "console.log" Views/CMP/Assessment.cshtml | grep -v "console.error" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "console.log" Views/CMP/Assessment.cshtml` returns 0
- `grep -c "console.error" Views/CMP/Assessment.cshtml` returns 2 (error handlers preserved)
- `dotnet build` exits 0
  </acceptance_criteria>
  <done>Tidak ada console.log tersisa di Assessment.cshtml; console.error di error handler tetap ada</done>
</task>

<task type="auto">
  <name>Task 2: XSS escape di GetApprovalBadgeWithTooltip (SEC-02)</name>
  <files>Views/CDP/CoachingProton.cshtml</files>
  <read_first>Views/CDP/CoachingProton.cshtml</read_first>
  <action>
Per D-03 dan D-04: Di @functions block CoachingProton.cshtml (line ~1034), modifikasi fungsi `GetApprovalBadgeWithTooltip` untuk encode input SEBELUM interpolasi.

Ubah awal fungsi dari:
```csharp
string GetApprovalBadgeWithTooltip(string status, string approverName, string approvedAt)
{
    var tooltipText = $"{approverName} — {approvedAt}";
```

Menjadi:
```csharp
string GetApprovalBadgeWithTooltip(string status, string approverName, string approvedAt)
{
    var safeApproverName = System.Net.WebUtility.HtmlEncode(approverName ?? "");
    var safeApprovedAt = System.Net.WebUtility.HtmlEncode(approvedAt ?? "");
    var tooltipText = $"{safeApproverName} — {safeApprovedAt}";
```

PENTING: Encode SETIAP variabel input sebelum interpolasi, bukan encode hasil tooltipText. Sisa fungsi (switch statement) tidak berubah.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; grep -n "WebUtility.HtmlEncode" Views/CDP/CoachingProton.cshtml</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "WebUtility.HtmlEncode" Views/CDP/CoachingProton.cshtml` returns 2 (satu untuk approverName, satu untuk approvedAt)
- `grep "safeApproverName" Views/CDP/CoachingProton.cshtml` matches
- `grep "safeApprovedAt" Views/CDP/CoachingProton.cshtml` matches
- `dotnet build` exits 0
  </acceptance_criteria>
  <done>approverName dan approvedAt di-encode sebelum masuk ke tooltip HTML attribute; XSS vector tertutup</done>
</task>

<task type="auto">
  <name>Task 3: Throttle TriggerCertExpiredNotificationsAsync via IMemoryCache (PERF-01)</name>
  <files>Controllers/HomeController.cs</files>
  <read_first>Controllers/HomeController.cs, Controllers/AdminController.cs</read_first>
  <action>
Per D-05, D-06, D-07: Inject IMemoryCache ke HomeController dan wrap TriggerCertExpiredNotificationsAsync dengan cache guard.

1. Tambah field setelah field _logger yang sudah ada:
```csharp
private readonly IMemoryCache _cache;
```

2. Tambah parameter `IMemoryCache cache` di constructor HomeController (setelah parameter terakhir yang ada). Tambah assignment `_cache = cache;` di constructor body.

3. Tambah using jika belum ada: `using Microsoft.Extensions.Caching.Memory;`

4. Di method Index() (atau Dashboard action yang memanggil TriggerCertExpiredNotificationsAsync), wrap pemanggilan dengan throttle guard. Cari blok yang memanggil `await TriggerCertExpiredNotificationsAsync()` dan wrap dengan:
```csharp
var cacheKey = "cert-notif-global";
if (!_cache.TryGetValue(cacheKey, out _))
{
    await TriggerCertExpiredNotificationsAsync();
    _cache.Set(cacheKey, true, TimeSpan.FromHours(1));
}
```

Gunakan global cache key (bukan per-user) karena fungsi ini bersifat global — scan semua cert expired dan kirim notif ke semua HC/Admin. AbsoluteExpiration via TimeSpan.FromHours(1) — bukan SlidingExpiration.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; grep -n "IMemoryCache" Controllers/HomeController.cs &amp;&amp; grep -n "cert-notif-global" Controllers/HomeController.cs &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "IMemoryCache" Controllers/HomeController.cs` returns at least 2 (field + constructor param)
- `grep "private readonly IMemoryCache _cache" Controllers/HomeController.cs` matches
- `grep "cert-notif-global" Controllers/HomeController.cs` matches
- `grep "TryGetValue" Controllers/HomeController.cs` matches
- `grep "TimeSpan.FromHours(1)" Controllers/HomeController.cs` matches
- `dotnet build` exits 0
  </acceptance_criteria>
  <done>TriggerCertExpiredNotificationsAsync hanya dipanggil sekali per jam; IMemoryCache ter-inject di HomeController</done>
</task>

</tasks>

<verification>
1. `dotnet build` — kompilasi sukses tanpa error
2. `grep -c "console.log" Views/CMP/Assessment.cshtml` — returns 0
3. `grep -c "WebUtility.HtmlEncode" Views/CDP/CoachingProton.cshtml` — returns 2
4. `grep "cert-notif-global" Controllers/HomeController.cs` — matches
</verification>

<success_criteria>
- Build green (dotnet build exits 0)
- Tidak ada console.log di Assessment.cshtml
- GetApprovalBadgeWithTooltip menggunakan HtmlEncode pada kedua parameter
- HomeController memiliki IMemoryCache injection dan throttle guard dengan TTL 1 jam
</success_criteria>

<output>
After completion, create `.planning/phases/250-security-performance/250-01-SUMMARY.md`
</output>
