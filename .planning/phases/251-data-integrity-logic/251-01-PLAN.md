---
phase: 251-data-integrity-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/TrainingRecord.cs
  - Models/CertificationManagementViewModel.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [DATA-01, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "Status sertifikat dihitung menggunakan DateTime.UtcNow, bukan DateTime.Now"
    - "Bulk renewal tanpa ValidUntil ditolak dengan pesan error"
    - "HC bisa edit assessment yang jadwalnya sudah lewat"
    - "Kegagalan deserialize RenewalFkMap tercatat sebagai warning di log"
  artifacts:
    - path: "Models/TrainingRecord.cs"
      provides: "UTC-based DaysUntilExpiry dan IsExpiringSoon"
      contains: "DateTime.UtcNow"
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "UTC-based DeriveCertificateStatus"
      contains: "DateTime.UtcNow"
    - path: "Controllers/AdminController.cs"
      provides: "Fixed isRenewalModePost, relaxed EditAssessment, logged catch"
  key_links:
    - from: "Models/TrainingRecord.cs"
      to: "CertificationManagement page"
      via: "DaysUntilExpiry and IsExpiringSoon properties"
      pattern: "DateTime\\.UtcNow"
    - from: "Controllers/AdminController.cs"
      to: "CreateAssessment POST bulk renewal"
      via: "isRenewalModePost detection"
      pattern: "IsNullOrEmpty\\(RenewalFkMap\\)"
---

<objective>
Fix 4 bug data integrity di model dan AdminController: timezone consistency (DATA-01), bulk renewal ValidUntil validation (DATA-03), relax past-date di EditAssessment (DATA-04), dan log warning pada bare catch RenewalFkMap (DATA-05).

Purpose: Memperbaiki kalkulasi expiry sertifikat, validasi business rule renewal, dan observability error handling.
Output: 4 file C# diperbaiki, project builds tanpa error.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/251-data-integrity-logic/251-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DateTime.Now ke DateTime.UtcNow di model expiry (DATA-01)</name>
  <files>Models/TrainingRecord.cs, Models/CertificationManagementViewModel.cs</files>
  <read_first>Models/TrainingRecord.cs, Models/CertificationManagementViewModel.cs</read_first>
  <action>
Per D-01 dan D-02, ganti `DateTime.Now` ke `DateTime.UtcNow` di 3 lokasi exact:

1. `Models/TrainingRecord.cs` line 77 — property `DaysUntilExpiry` getter:
   SEBELUM: `var daysUntilExpiry = (ValidUntil.Value - DateTime.Now).Days;`
   SESUDAH: `var daysUntilExpiry = (ValidUntil.Value - DateTime.UtcNow).Days;`

2. `Models/TrainingRecord.cs` line 91 — property `IsExpiringSoon` computed:
   Ganti `DateTime.Now` ke `DateTime.UtcNow` di baris yang menghitung apakah sertifikat akan expired.

3. `Models/CertificationManagementViewModel.cs` line 59 — method `DeriveCertificateStatus`:
   Ganti `DateTime.Now` ke `DateTime.UtcNow` di baris yang membandingkan tanggal untuk menentukan status sertifikat.

HANYA ganti di 3 lokasi ini. Tidak ada perubahan lain.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && grep -n "DateTime\.Now" Models/TrainingRecord.cs Models/CertificationManagementViewModel.cs | grep -v "UtcNow" | grep -v "//" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "DateTime.UtcNow" Models/TrainingRecord.cs` menghasilkan minimal 2
- `grep -c "DateTime.UtcNow" Models/CertificationManagementViewModel.cs` menghasilkan minimal 1
- `grep "DateTime\.Now" Models/TrainingRecord.cs` TIDAK mengembalikan baris aktif (hanya komentar atau nol hasil)
- `grep "DateTime\.Now" Models/CertificationManagementViewModel.cs` TIDAK mengembalikan baris aktif
- `dotnet build` sukses tanpa error
  </acceptance_criteria>
  <done>3 lokasi DateTime.Now diganti ke DateTime.UtcNow, project builds sukses</done>
</task>

<task type="auto">
  <name>Task 2: Fix AdminController — bulk renewal validation, past-date relaxation, log warning (DATA-03, DATA-04, DATA-05)</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>Controllers/AdminController.cs</read_first>
  <action>
3 perubahan di AdminController.cs:

**DATA-03 (D-06, D-07) — Fix isRenewalModePost detection (sekitar line 1254):**
SEBELUM:
```csharp
bool isRenewalModePost = model.RenewsSessionId.HasValue || model.RenewsTrainingId.HasValue;
```
SESUDAH:
```csharp
bool isRenewalModePost = model.RenewsSessionId.HasValue || model.RenewsTrainingId.HasValue || !string.IsNullOrEmpty(RenewalFkMap);
```
Ini memastikan bulk renewal (yang mengirim RenewalFkMap JSON, bukan model FK) juga terdeteksi sebagai renewal mode, sehingga ValidUntil wajib diisi.

**DATA-04 (D-08, D-09) — Relax past-date validation di EditAssessment POST (sekitar line 1727):**
Hapus atau comment out baris validasi past-date:
SEBELUM:
```csharp
if (model.Schedule < DateTime.Today)
    editErrors.Add("Schedule date cannot be in the past.");
```
SESUDAH: Hapus kedua baris tersebut sepenuhnya. Ini adalah EditAssessment (bukan Create), sehingga HC harus bisa edit assessment yang jadwalnya sudah lewat.

**DATA-05 (D-10, D-11) — Log warning pada bare catch RenewalFkMap (sekitar line 1437):**
SEBELUM:
```csharp
catch { /* ignore malformed map — fall back to model value */ }
```
SESUDAH:
```csharp
catch (Exception ex)
{
    _logger.LogWarning(ex, "Failed to deserialize RenewalFkMap");
}
```
`_logger` sudah tersedia di AdminController — tidak perlu inject baru.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && grep -n "IsNullOrEmpty(RenewalFkMap)" Controllers/AdminController.cs && grep -n "LogWarning.*RenewalFkMap" Controllers/AdminController.cs && echo "PASS" ; dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep "IsNullOrEmpty(RenewalFkMap)" Controllers/AdminController.cs` mengembalikan minimal 1 baris
- `grep "Schedule date cannot be in the past" Controllers/AdminController.cs` mengembalikan 0 baris (validasi dihapus)
- `grep "LogWarning.*RenewalFkMap" Controllers/AdminController.cs` mengembalikan minimal 1 baris
- `grep "catch {" Controllers/AdminController.cs` TIDAK mengembalikan baris bare catch di area RenewalFkMap
- `dotnet build` sukses tanpa error
  </acceptance_criteria>
  <done>isRenewalModePost mendeteksi bulk renewal, past-date validasi dihapus di EditAssessment, bare catch diganti LogWarning</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error atau warning baru
- Tidak ada `DateTime.Now` aktif di TrainingRecord.cs dan CertificationManagementViewModel.cs
- AdminController.cs berisi fix untuk 3 bug (bulk renewal, past-date, log warning)
</verification>

<success_criteria>
- 3 lokasi DateTime.Now diganti ke DateTime.UtcNow (DATA-01)
- isRenewalModePost mendeteksi bulk renewal via RenewalFkMap (DATA-03)
- EditAssessment POST tidak lagi menolak past-date schedule (DATA-04)
- Bare catch RenewalFkMap diganti LogWarning (DATA-05)
- Project builds tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/251-data-integrity-logic/251-01-SUMMARY.md`
</output>
