---
phase: 257-setup-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/CoachCoacheeMapping.cshtml
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
must_haves:
  truths:
    - "Halaman CoachCoacheeMapping tampil dengan data, pagination berfungsi, search by name/NIP berfungsi"
    - "Assign coach ke coachee via modal berhasil dan mapping record terbuat di DB"
    - "Import Excel berhasil — row valid di-commit, duplicate di-skip, inactive di-reactivate, error di-report"
    - "Download template Excel menghasilkan file .xlsx dengan kolom NIP Coach dan NIP Coachee"
    - "Assign dengan ProtonTrackId otomatis membuat ProtonTrackAssignment"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "All 13 mapping actions"
      contains: "CoachCoacheeMapping"
  key_links:
    - from: "Views/Admin/CoachCoacheeMapping.cshtml"
      to: "AdminController.CoachCoacheeMapping"
      via: "form submit + AJAX calls"
      pattern: "CoachCoacheeMappingAssign|ImportCoachCoacheeMapping"
---

<objective>
Code review dan bug fix untuk flow mapping utama: list/pagination/search (MAP-01), assign via modal (MAP-02), import Excel (MAP-03), download template (MAP-04), dan track assignment auto-creation (MAP-05).

Purpose: Verifikasi logic code benar dan fix bug yang ditemukan sebelum user test di browser.
Output: Bug fixes committed, test checklist siap untuk user verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/257-setup-mapping/257-CONTEXT.md
@.planning/phases/257-setup-mapping/257-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review MAP-01..05 dan fix bug</name>
  <files>Controllers/AdminController.cs, Views/Admin/CoachCoacheeMapping.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (line 3630-4200 — CoachCoacheeMapping, DownloadMappingImportTemplate, ImportCoachCoacheeMapping, CoachCoacheeMappingAssign, track assignment logic)
    - Views/Admin/CoachCoacheeMapping.cshtml (full file — UI view with modals)
    - Models/CoachCoacheeMapping.cs
    - Models/ImportMappingResult.cs
    - Models/ProtonModels.cs (ProtonTrackAssignment, ProtonDeliverableProgress)
  </read_first>
  <action>
Analisa code untuk 5 flow berikut dan fix bug yang ditemukan (per D-02):

**MAP-01 — List/Pagination/Search** (line ~3632):
- Verifikasi pagination logic (pageSize=20, grouped by coach)
- Verifikasi search by name/NIP berfungsi (case-insensitive)
- Verifikasi section filter berfungsi
- Verifikasi showAll toggle menampilkan inactive mappings

**MAP-02 — Assign via Modal** (line ~3984):
- Verifikasi CoachCoacheeMappingAssign menerima JSON POST dengan CoachId, CoacheeIds[], AssignmentSection, AssignmentUnit, ProtonTrackId (optional)
- Verifikasi duplicate check (coachee sudah punya active mapping)
- Verifikasi AssignmentSection dan AssignmentUnit tersimpan

**MAP-03 — Import Excel** (line ~3789):
- Verifikasi per-row processing: Success (new mapping), Error (NIP not found), Skip (duplicate active), Reactivated (inactive mapping)
- Verifikasi partial commit per D-06 — valid rows committed, error rows reported
- Cek pitfall dari RESEARCH: import reactivate TIDAK reactivate ProtonTrackAssignment — verify ini intentional per D-07

**MAP-04 — Download Template** (line ~3754):
- Verifikasi template menghasilkan .xlsx dengan 2 kolom: NIP Coach, NIP Coachee
- Verifikasi content-type dan filename header benar

**MAP-05 — Track Assignment Auto-Creation** (line ~4083-4135):
- Verifikasi: jika ProtonTrackId dipilih saat assign, ProtonTrackAssignment terbuat
- Verifikasi reuse inactive assignment jika ada (bukan duplicate create)
- Verifikasi AutoCreateProgressForAssignment dipanggil untuk buat ProtonDeliverableProgress records

Untuk setiap bug yang ditemukan: fix in-place, commit dengan message yang jelas. Jika tidak ada bug, catat "code review passed" per requirement.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds tanpa error
    - Setiap MAP-01..05 sudah di-review dengan hasil documented (bug found + fixed, atau "passed")
    - Jika ada fix: git log menunjukkan commit dengan deskripsi bug yang diperbaiki
  </acceptance_criteria>
  <done>Code review lengkap untuk MAP-01..05, semua bug yang ditemukan sudah di-fix dan build sukses</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifikasi MAP-01..05 di browser</name>
  <files>N/A</files>
  <action>
User verifikasi di browser — login sebagai Admin/HC, test 5 flow:

1. MAP-01 — List: Buka /Admin/CoachCoacheeMapping — halaman tampil dengan data, pagination, search by nama/NIP, filter section, toggle Show All
2. MAP-04 — Template: Download template — file .xlsx dengan 2 kolom (NIP Coach, NIP Coachee)
3. MAP-02 — Assign: Assign coach ke coachee via modal — mapping terbuat, duplicate check berfungsi
4. MAP-05 — Track Assignment: Assign dengan ProtonTrackId — ProtonTrackAssignment terbuat
5. MAP-03 — Import Excel: Upload filled template — Success/Error/Skip/Reactivated per row
  </action>
  <verify>User confirms all 5 requirements pass in browser</verify>
  <done>MAP-01..05 verified by user in browser — all pass or issues reported and fixed</done>
</task>

</tasks>

<verification>
- Build sukses tanpa error
- User konfirmasi MAP-01..05 berfungsi di browser
</verification>

<success_criteria>
- MAP-01: List tampil, pagination, search, filter section berfungsi
- MAP-02: Assign via modal berhasil, duplicate check berfungsi
- MAP-03: Import Excel partial commit berfungsi (create, skip, reactivate, error)
- MAP-04: Template download menghasilkan .xlsx valid
- MAP-05: Track assignment auto-created saat assign dengan TrackId
</success_criteria>

<output>
After completion, create `.planning/phases/257-setup-mapping/257-01-SUMMARY.md`
</output>
