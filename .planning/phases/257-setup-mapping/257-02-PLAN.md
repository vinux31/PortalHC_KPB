---
phase: 257-setup-mapping
plan: 02
type: execute
wave: 2
depends_on:
  - 257-01
files_modified:
  - Controllers/AdminController.cs
autonomous: false
requirements:
  - MAP-06
  - MAP-07
  - MAP-08
must_haves:
  truths:
    - "Deactivate mapping cascade deactivate ProtonTrackAssignment dengan DeactivatedAt timestamp"
    - "Reactivate mapping reuse existing ProtonTrackAssignment (correlate by DeactivatedAt within 5s window)"
    - "Progression warning muncul saat assign Tahun 2+ jika Tahun sebelumnya belum selesai, user bisa confirm dan proceed"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "Deactivate, Reactivate, Progression warning actions"
      contains: "CoachCoacheeMappingDeactivate"
  key_links:
    - from: "AdminController.CoachCoacheeMappingDeactivate"
      to: "ProtonTrackAssignment"
      via: "cascade IsActive=false + DeactivatedAt"
      pattern: "IsActive = false.*DeactivatedAt"
    - from: "AdminController.CoachCoacheeMappingReactivate"
      to: "ProtonTrackAssignment"
      via: "correlate by DeactivatedAt within 5s"
      pattern: "DeactivatedAt.*AddSeconds"
---

<objective>
Code review dan bug fix untuk deactivate cascade (MAP-06), reactivate reuse (MAP-07), dan progression warning (MAP-08).

Purpose: Verifikasi logic deactivate/reactivate cascade dan progression warning benar sebelum user test.
Output: Bug fixes committed, user verifikasi di browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/257-setup-mapping/257-CONTEXT.md
@.planning/phases/257-setup-mapping/257-RESEARCH.md
@.planning/phases/257-setup-mapping/257-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review MAP-06..08 dan fix bug</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (line 4300-4470 — CoachCoacheeMappingDeactivate, CoachCoacheeMappingReactivate)
    - Controllers/AdminController.cs (line 3984-4070 — CoachCoacheeMappingAssign, progression warning logic)
    - Models/ProtonModels.cs (ProtonTrackAssignment — IsActive, DeactivatedAt fields)
    - Views/Admin/CoachCoacheeMapping.cshtml (deactivate/reactivate buttons, confirmation modals)
  </read_first>
  <action>
Analisa code untuk 3 flow berikut dan fix bug yang ditemukan (per D-02):

**MAP-06 — Deactivate Cascade** (line ~4320):
- Verifikasi transaction wrapping (BeginTransactionAsync + commit/rollback)
- Verifikasi mapping.IsActive = false
- Verifikasi cascade: semua active ProtonTrackAssignment untuk coachee ini juga di-set IsActive=false
- Verifikasi DeactivatedAt timestamp di-set pada ProtonTrackAssignment (dipakai untuk reactivate correlation)
- Per D-07: cascade hanya sampai ProtonTrackAssignment level, BUKAN DeliverableProgress

**MAP-07 — Reactivate Reuse** (line ~4390):
- Verifikasi mapping.IsActive = true
- Verifikasi reactivate logic: cari ProtonTrackAssignment yang DeactivatedAt within 5 seconds dari mapping deactivation time
- Verifikasi assignment yang ditemukan di-set IsActive=true, DeactivatedAt=null
- Cek pitfall dari RESEARCH: 5-second window correlation — apakah cukup robust?
- Verifikasi transaction wrapping

**MAP-08 — Progression Warning** (line ~4019-4063):
- Verifikasi: saat assign ProtonTrack Tahun 2+, cek apakah Tahun sebelumnya (same TrackType, Urutan-1) sudah completed
- Verifikasi: jika belum completed, return JSON warning (bukan hard block per D-05)
- Verifikasi: ConfirmProgressionWarning flag — jika true, skip warning dan proceed
- Verifikasi: jika coachee sudah punya existing assignment untuk track tersebut, warning di-skip (line 4035-4038)

Untuk setiap bug: fix in-place dan commit. Jika tidak ada bug: catat "code review passed".
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds tanpa error
    - Setiap MAP-06..08 sudah di-review dengan hasil documented
    - Jika ada fix: git log menunjukkan commit dengan deskripsi bug
  </acceptance_criteria>
  <done>Code review lengkap untuk MAP-06..08, semua bug yang ditemukan sudah di-fix dan build sukses</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifikasi MAP-06..08 di browser</name>
  <files>N/A</files>
  <action>
User verifikasi di browser — login sebagai Admin/HC, test di /Admin/CoachCoacheeMapping:

Prerequisite: Minimal 1 active mapping dengan ProtonTrackAssignment (dari Plan 01).

1. MAP-06 — Deactivate: Pilih mapping dengan track assignment, klik deactivate — mapping inactive, ProtonTrackAssignment juga inactive
2. MAP-07 — Reactivate: Dari Show All, reactivate mapping yang baru di-deactivate — mapping active kembali, ProtonTrackAssignment reuse (bukan new record)
3. MAP-08 — Progression Warning: Assign coachee baru ke ProtonTrack Tahun 2 (Tahun 1 belum selesai) — warning muncul, confirm proceed — assignment terbuat
  </action>
  <verify>User confirms all 3 requirements pass in browser</verify>
  <done>MAP-06..08 verified by user in browser — all pass or issues reported and fixed. Phase 257 complete.</done>
</task>

</tasks>

<verification>
- Build sukses tanpa error
- User konfirmasi MAP-06..08 berfungsi di browser
- Semua 8 requirements (MAP-01..08) pass
</verification>

<success_criteria>
- MAP-06: Deactivate cascade ke ProtonTrackAssignment berfungsi
- MAP-07: Reactivate reuse existing assignment berfungsi
- MAP-08: Progression warning muncul untuk Tahun 2+ dan bisa di-confirm
- Seluruh Phase 257 MAP-01..08 verified
</success_criteria>

<output>
After completion, create `.planning/phases/257-setup-mapping/257-02-SUMMARY.md`
</output>
