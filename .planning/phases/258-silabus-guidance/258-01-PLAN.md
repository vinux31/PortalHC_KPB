---
phase: 258-silabus-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/ProtonDataController.cs
autonomous: false
requirements: [SIL-01, SIL-02, SIL-03]

must_haves:
  truths:
    - "SilabusSave (inline edit) berhasil upsert hierarki Kompetensi > SubKompetensi > Deliverable"
    - "ImportSilabus (Excel upload) berhasil dengan 2-pass validation"
    - "Orphan cleanup menghapus deliverable yang tidak ada di payload baru"
    - "Orphan cleanup di-block jika ada active DeliverableProgress"
    - "Deactivate kompetensi menyembunyikan sub+deliverable di UI"
    - "Reactivate kompetensi mengembalikan visibility"
    - "ExportSilabus menghasilkan file .xlsx valid"
    - "DownloadSilabusTemplate menghasilkan template dengan kolom benar"
    - "Status tab menampilkan status Silabus/Guidance per Bagian+Unit+Track"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "Silabus CRUD + orphan cleanup + deactivate/reactivate + export"
  key_links:
    - from: "Views/ProtonData/Index.cshtml"
      to: "ProtonDataController.SilabusSave"
      via: "AJAX POST"
    - from: "Views/ProtonData/ImportSilabus.cshtml"
      to: "ProtonDataController.ImportSilabus"
      via: "form POST"
---

<objective>
Code review dan bug fix untuk silabus management: upload/upsert (SIL-01), orphan cleanup (SIL-02), deactivate/reactivate (SIL-03), plus export silabus (D-09), download template (D-10), dan status tab (D-11).

Purpose: Temukan dan fix bug sebelum user verifikasi di browser
Output: Bug fixes committed, test scenarios untuk UAT browser verification
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/258-silabus-guidance/258-CONTEXT.md
@.planning/phases/258-silabus-guidance/258-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review SilabusSave, ImportSilabus, orphan cleanup, deactivate/reactivate, export, template, status tab — fix bugs</name>
  <files>Controllers/ProtonDataController.cs</files>
  <read_first>
    - Controllers/ProtonDataController.cs (SELURUH file — fokus SilabusSave line ~226, ImportSilabus line ~827, SilabusDelete line ~582, SilabusDeletePreview line ~536, SilabusDeactivate line ~677, SilabusReactivate line ~699, ExportSilabus line ~720, DownloadSilabusTemplate line ~764)
    - Views/ProtonData/Index.cshtml (UI tabs: Status, Silabus, Guidance)
    - Views/ProtonData/ImportSilabus.cshtml (import flow UI)
    - Models/ProtonModels.cs (ProtonKompetensi, ProtonSubKompetensi, ProtonDeliverable)
  </read_first>
  <action>
Analisa code untuk semua requirement SIL-01..SIL-03 + D-09..D-11. Cek secara spesifik:

**SIL-01 (per D-04):** Review SilabusSave (inline bulk upsert) dan ImportSilabus (Excel upload). Pastikan:
- SilabusSave: upsert logic benar (insert baru + update existing)
- ImportSilabus: 2-pass validation berfungsi, header mismatch di-handle (per D-05), duplicate detection ada, required field kosong di-reject, file non-Excel di-reject

**SIL-02 (per D-01, D-02, D-03):** Review orphan cleanup di SilabusSave:
- Deliverable yang tidak ada di payload baru HARUS dihapus (hard delete otomatis, per D-01)
- Hard delete HARUS di-block jika ada active DeliverableProgress (per D-02)
- PITFALL dari RESEARCH.md: SilabusSave orphan cleanup mungkin TIDAK cek DeliverableProgress — verifikasi dan fix jika missing

**SIL-03 (per D-08):** Review SilabusDeactivate dan SilabusReactivate:
- Deactivate set IsActive=false pada ProtonKompetensi
- UI (Index.cshtml) harus filter SubKompetensi/Deliverable berdasarkan parent IsActive
- PITFALL: IsActive hanya di level Kompetensi — pastikan view memfilter benar

**D-09:** Review ExportSilabus — pastikan menghasilkan .xlsx valid dengan data benar
**D-10:** Review DownloadSilabusTemplate — pastikan template punya kolom yang benar
**D-11:** Review Status tab di Index.cshtml — pastikan load status Silabus/Guidance per Bagian+Unit+Track

Fix semua bug yang ditemukan langsung di ProtonDataController.cs atau view files.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build berhasil tanpa error
    - Jika orphan cleanup di SilabusSave tidak cek DeliverableProgress: fix sudah ditambahkan (grep "DeliverableProgress" di area orphan cleanup SilabusSave)
    - Semua bug yang ditemukan sudah di-fix dan di-dokumentasikan
  </acceptance_criteria>
  <done>Code review selesai untuk SIL-01, SIL-02, SIL-03, D-09, D-10, D-11. Semua bug yang ditemukan sudah di-fix. Build berhasil.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser verification — Silabus management</name>
  <files>N/A</files>
  <action>Presentasikan test scenarios ke user untuk browser verification. Tunggu feedback dan fix issues yang dilaporkan.</action>
  <what-built>Code review dan bug fix untuk silabus management (SilabusSave, ImportSilabus, orphan cleanup, deactivate/reactivate, export, template, status tab)</what-built>
  <how-to-verify>
Login sebagai Admin/HC, lalu test di browser:

**SIL-01 — Upload/Upsert Silabus:**
1. Buka ProtonData lalu tab Silabus
2. Pilih Bagian + Unit + Track
3. Test inline edit: tambah Kompetensi baru lalu SubKompetensi lalu Deliverable lalu Save lalu data tersimpan
4. Test import Excel: buka ImportSilabus lalu download template (D-10) lalu isi data lalu upload lalu data terbuat
5. Error scenarios (D-05): upload file non-Excel harus error message jelas; upload Excel dengan header salah harus error

**SIL-02 — Orphan Cleanup:**
6. Upload silabus dengan 5 deliverable via import
7. Upload ulang dengan hanya 3 deliverable lalu 2 deliverable lama HARUS terhapus
8. Jika deliverable punya active progress maka delete harus di-block (D-02)

**SIL-03 — Deactivate/Reactivate:**
9. Deactivate satu Kompetensi lalu SubKompetensi dan Deliverable-nya tersembunyi di UI
10. Reactivate lalu muncul kembali

**D-09 — Export:**
11. Export Silabus lalu download .xlsx lalu buka lalu data benar

**D-11 — Status Tab:**
12. Buka ProtonData lalu tab Status lalu tampil status per Bagian+Unit+Track
  </how-to-verify>
  <verify>User menyatakan approved atau semua issues sudah di-fix</verify>
  <done>User approved semua test scenario SIL-01, SIL-02, SIL-03, D-09, D-10, D-11</done>
  <resume-signal>Ketik "approved" atau deskripsikan issues yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- dotnet build berhasil
- User confirm semua test scenario PASS di browser
</verification>

<success_criteria>
SIL-01, SIL-02, SIL-03 verified via code review + browser UAT. Export (D-09), template (D-10), status tab (D-11) berfungsi.
</success_criteria>

<output>
After completion, create `.planning/phases/258-silabus-guidance/258-01-SUMMARY.md`
</output>
