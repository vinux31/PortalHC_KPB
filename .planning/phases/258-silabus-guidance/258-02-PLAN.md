---
phase: 258-silabus-guidance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/ProtonDataController.cs
  - Controllers/CDPController.cs
autonomous: false
requirements: [SIL-04, SIL-05, SIL-06]

must_haves:
  truths:
    - "Admin/HC bisa upload guidance file per Bagian+Unit+Track"
    - "Coach dan Coachee bisa download guidance file via CDPController"
    - "Admin/HC bisa replace guidance file (upload baru lalu hapus lama)"
    - "Admin/HC bisa delete guidance file"
    - "Path traversal protection ada di KEDUA controller"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "GuidanceUpload, GuidanceDownload, GuidanceReplace, GuidanceDelete"
    - path: "Controllers/CDPController.cs"
      provides: "GuidanceDownload untuk Coach/Coachee"
  key_links:
    - from: "Views/ProtonData/Index.cshtml"
      to: "ProtonDataController.GuidanceUpload"
      via: "form POST multipart"
    - from: "CDPController.GuidanceDownload"
      to: "wwwroot/uploads/guidance/"
      via: "PhysicalFile serve"
---

<objective>
Code review dan bug fix untuk guidance file management: upload (SIL-04), download Coach/Coachee (SIL-05), replace dan delete (SIL-06).

Purpose: Temukan dan fix bug — terutama path traversal vulnerability di ProtonDataController.GuidanceDownload
Output: Bug fixes committed, test scenarios untuk UAT browser verification
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/258-silabus-guidance/258-CONTEXT.md
@.planning/phases/258-silabus-guidance/258-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review GuidanceUpload, GuidanceDownload (kedua controller), GuidanceReplace, GuidanceDelete — fix bugs</name>
  <files>Controllers/ProtonDataController.cs, Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/ProtonDataController.cs (fokus GuidanceUpload line ~1085, GuidanceDownload line ~1138, GuidanceReplace line ~1153, GuidanceDelete line ~1215)
    - Controllers/CDPController.cs (fokus GuidanceDownload line ~238 — perhatikan path traversal protection fullPath.StartsWith)
    - Views/ProtonData/Index.cshtml (Guidance tab UI)
    - Models/ProtonModels.cs (CoachingGuidanceFile model)
  </read_first>
  <action>
Analisa code untuk SIL-04, SIL-05, SIL-06. Cek secara spesifik:

**SIL-04 — Upload guidance (per D-07):**
- GuidanceUpload: file type validation (.pdf, .doc, .docx, .xls, .xlsx, .ppt, .pptx)
- Max 10MB enforcement
- Timestamp+GUID naming convention
- Upload file type yang tidak diizinkan harus return error message jelas

**SIL-05 — Download guidance (per D-06):**
- ProtonDataController.GuidanceDownload (Admin/HC): RESEARCH.md menyebutkan TIDAK ada path traversal protection — ini potensial bug. Tambahkan fullPath.StartsWith(uploadsDir) check seperti CDPController
- CDPController.GuidanceDownload (Coach/Coachee): sudah ada path traversal protection — verifikasi masih benar

**SIL-06 — Replace dan delete (per D-07):**
- GuidanceReplace: verifikasi safe replacement flow (upload baru lalu DB update lalu delete old file)
- GuidanceDelete: verifikasi file fisik juga terhapus, bukan hanya DB record
- File type validation pada replace (same as upload)

**CRITICAL BUG dari RESEARCH.md Pitfall 2:**
ProtonDataController.GuidanceDownload (line ~1138) TIDAK punya path traversal protection. CDPController punya fullPath.StartsWith check. Fix ProtonDataController.GuidanceDownload dengan menambahkan check yang sama:
```csharp
var uploadsDir = Path.Combine(_env.WebRootPath, "uploads", "guidance");
var fullPath = Path.GetFullPath(filePath);
if (!fullPath.StartsWith(uploadsDir))
    return NotFound();
```

Fix semua bug yang ditemukan.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build berhasil tanpa error
    - ProtonDataController.GuidanceDownload mengandung path traversal check (grep "StartsWith" di area GuidanceDownload ProtonDataController)
    - Semua bug yang ditemukan sudah di-fix dan di-dokumentasikan
  </acceptance_criteria>
  <done>Code review selesai untuk SIL-04, SIL-05, SIL-06. Path traversal fix applied. Build berhasil.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser verification — Guidance file management</name>
  <files>N/A</files>
  <action>Presentasikan test scenarios ke user untuk browser verification. Tunggu feedback dan fix issues yang dilaporkan.</action>
  <verify>User menyatakan approved atau semua issues sudah di-fix</verify>
  <done>User approved semua test scenario SIL-04, SIL-05, SIL-06</done>
  <what-built>Code review dan bug fix untuk guidance file management (upload, download kedua role, replace, delete, path traversal fix)</what-built>
  <how-to-verify>
**SIL-04 — Upload Guidance:**
1. Login sebagai Admin/HC lalu buka ProtonData tab Guidance
2. Pilih Bagian + Unit + Track lalu upload file PDF — berhasil tersimpan
3. Upload file type tidak diizinkan (misal .exe atau .txt) — error message muncul

**SIL-05 — Download Guidance (per D-06):**
4. Sebagai Admin/HC: download guidance file yang baru di-upload — file valid
5. Login sebagai Coach/Coachee: buka halaman CDP lalu download guidance file yang sama — file valid

**SIL-06 — Replace dan Delete (per D-07):**
6. Sebagai Admin/HC: replace guidance file dengan file baru — file lama terhapus, file baru tersedia
7. Delete guidance file — file terhapus dari list dan dari disk
  </how-to-verify>
  <resume-signal>Ketik "approved" atau deskripsikan issues yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- dotnet build berhasil
- Path traversal protection ada di kedua controller
- User confirm semua test scenario PASS di browser
</verification>

<success_criteria>
SIL-04, SIL-05, SIL-06 verified via code review + browser UAT. Path traversal vulnerability fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/258-silabus-guidance/258-02-SUMMARY.md`
</output>
