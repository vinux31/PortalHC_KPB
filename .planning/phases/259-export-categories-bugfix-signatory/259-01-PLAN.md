---
phase: 259-export-categories-bugfix-signatory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ManageCategories.cshtml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Kolom Penandatangan di tabel ManageCategories menampilkan nama signatory untuk sub-kategori (bukan dash)"
    - "User bisa download file Excel berisi semua kategori assessment"
    - "User bisa download file PDF berisi semua kategori assessment"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "ExportCategoriesExcel dan ExportCategoriesPdf actions, plus fixed SetCategoriesViewBag"
      contains: "ExportCategoriesExcel"
    - path: "Views/Admin/ManageCategories.cshtml"
      provides: "Dropdown tombol export Excel & PDF"
      contains: "ExportCategoriesExcel"
  key_links:
    - from: "Views/Admin/ManageCategories.cshtml"
      to: "AdminController.ExportCategoriesExcel"
      via: "href link"
      pattern: "ExportCategoriesExcel"
    - from: "Views/Admin/ManageCategories.cshtml"
      to: "AdminController.ExportCategoriesPdf"
      via: "href link"
      pattern: "ExportCategoriesPdf"
---

<objective>
Fix bug signatory sub-kategori dan tambah fitur export Excel + PDF di halaman ManageCategories.

Purpose: Sub-kategori menampilkan "—" untuk penandatangan padahal sudah di-set (bug query Include). User juga butuh export data kategori.
Output: Bug fix + 2 export actions + tombol export di view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/259-export-categories-bugfix-signatory/259-CONTEXT.md

<interfaces>
From Helpers/ExcelExportHelper.cs:
```csharp
public static IXLWorksheet CreateSheet(XLWorkbook workbook, string sheetName, string[] headers)
public static FileContentResult ToFileResult(XLWorkbook workbook, string fileName, ControllerBase controller)
```

From Models/AssessmentCategory.cs:
```csharp
public class AssessmentCategory {
    public int Id { get; set; }
    public string Name { get; set; } = "";
    public int DefaultPassPercentage { get; set; } = 70;
    public bool IsActive { get; set; } = true;
    public int SortOrder { get; set; } = 0;
    public int? ParentId { get; set; }
    public string? SignatoryUserId { get; set; }
    public AssessmentCategory? Parent { get; set; }
    public ICollection<AssessmentCategory> Children { get; set; }
    public ApplicationUser? Signatory { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bug Signatory di SetCategoriesViewBag</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 785-800, SetCategoriesViewBag method)
    - Models/AssessmentCategory.cs
  </read_first>
  <action>
Di method `SetCategoriesViewBag()` (sekitar line 787-794), tambahkan `.ThenInclude` untuk Signatory pada children dan grandchildren. Per D-01.

Ubah query dari:
```csharp
var parentCategories = await _context.AssessmentCategories
    .Include(c => c.Children.OrderBy(ch => ch.SortOrder).ThenBy(ch => ch.Name))
        .ThenInclude(ch => ch.Children.OrderBy(gc => gc.SortOrder).ThenBy(gc => gc.Name))
    .Include(c => c.Signatory)
    .Where(c => c.ParentId == null)
    .OrderBy(c => c.SortOrder).ThenBy(c => c.Name)
    .ToListAsync();
```

Menjadi:
```csharp
var parentCategories = await _context.AssessmentCategories
    .Include(c => c.Children.OrderBy(ch => ch.SortOrder).ThenBy(ch => ch.Name))
        .ThenInclude(ch => ch.Children.OrderBy(gc => gc.SortOrder).ThenBy(gc => gc.Name))
    .Include(c => c.Children)
        .ThenInclude(ch => ch.Signatory)
    .Include(c => c.Children)
        .ThenInclude(ch => ch.Children)
            .ThenInclude(gc => gc.Signatory)
    .Include(c => c.Signatory)
    .Where(c => c.ParentId == null)
    .OrderBy(c => c.SortOrder).ThenBy(c => c.Name)
    .ToListAsync();
```
  </action>
  <verify>
    <automated>grep -n "ThenInclude(ch => ch.Signatory)" Controllers/AdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/AdminController.cs` contains `.ThenInclude(ch => ch.Signatory)` within SetCategoriesViewBag
    - `Controllers/AdminController.cs` contains `.ThenInclude(gc => gc.Signatory)` within SetCategoriesViewBag
    - Project builds without errors: `dotnet build --no-restore` exits 0
  </acceptance_criteria>
  <done>SetCategoriesViewBag memuat Signatory untuk semua level hierarki (root, children, grandchildren)</done>
</task>

<task type="auto">
  <name>Task 2: Tambah ExportCategoriesExcel dan ExportCategoriesPdf actions</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 4703-4768, ExportWorkers sebagai reference Excel)
    - Controllers/CDPController.cs (lines 2447-2515, ExportProgressPdf sebagai reference PDF)
    - Helpers/ExcelExportHelper.cs
  </read_first>
  <action>
Tambahkan 2 action methods di AdminController, setelah method ManageCategories-related yang sudah ada. Per D-02 sampai D-09.

**ExportCategoriesExcel:**
```csharp
// GET /Admin/ExportCategoriesExcel
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ExportCategoriesExcel()
{
    var categories = await _context.AssessmentCategories
        .Include(c => c.Parent)
        .Include(c => c.Signatory)
        .OrderBy(c => c.ParentId == null ? 0 : 1)
            .ThenBy(c => c.SortOrder).ThenBy(c => c.Name)
        .ToListAsync();

    using var workbook = new XLWorkbook();
    var ws = ExcelExportHelper.CreateSheet(workbook, "Kategori Assessment",
        new[] { "No", "Nama Kategori", "Kategori Induk", "Passing Grade (%)", "Penandatangan", "Urutan", "Status" });
    ws.Range(1, 1, 1, 7).Style.Fill.BackgroundColor = XLColor.LightBlue;

    for (int i = 0; i < categories.Count; i++)
    {
        var c = categories[i];
        ws.Cell(i + 2, 1).Value = i + 1;
        ws.Cell(i + 2, 2).Value = c.Name;
        ws.Cell(i + 2, 3).Value = c.Parent?.Name ?? "";
        ws.Cell(i + 2, 4).Value = c.DefaultPassPercentage;
        ws.Cell(i + 2, 5).Value = c.Signatory?.FullName ?? "—";
        ws.Cell(i + 2, 6).Value = c.SortOrder;
        ws.Cell(i + 2, 7).Value = c.IsActive ? "Aktif" : "Nonaktif";
    }

    var fileName = $"KategoriAssessment_{DateTime.Now:yyyyMMdd_HHmmss}.xlsx";
    return ExcelExportHelper.ToFileResult(workbook, fileName, this);
}
```

**ExportCategoriesPdf:**
```csharp
// GET /Admin/ExportCategoriesPdf
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ExportCategoriesPdf()
{
    var categories = await _context.AssessmentCategories
        .Include(c => c.Parent)
        .Include(c => c.Signatory)
        .OrderBy(c => c.ParentId == null ? 0 : 1)
            .ThenBy(c => c.SortOrder).ThenBy(c => c.Name)
        .ToListAsync();

    var pdf = QuestPDF.Fluent.Document.Create(container =>
    {
        container.Page(page =>
        {
            page.Size(QuestPDF.Helpers.PageSizes.A4.Landscape());
            page.Margin(20);
            page.Content().Column(col =>
            {
                col.Item().Text("Kategori Assessment").FontSize(14).Bold();
                col.Item().Text($"Tanggal export: {DateTime.Now:dd MMM yyyy HH:mm}").FontSize(9);
                col.Item().PaddingTop(10).Table(table =>
                {
                    table.ColumnsDefinition(cols =>
                    {
                        cols.ConstantColumn(30);    // No
                        cols.RelativeColumn(3);     // Nama
                        cols.RelativeColumn(2);     // Kategori Induk
                        cols.ConstantColumn(50);    // Passing Grade
                        cols.RelativeColumn(2);     // Penandatangan
                        cols.ConstantColumn(40);    // Urutan
                        cols.ConstantColumn(50);    // Status
                    });

                    foreach (var header in new[] { "No", "Nama Kategori", "Kategori Induk", "PG (%)", "Penandatangan", "Urutan", "Status" })
                    {
                        table.Cell().Border(0.5f).Padding(3).Text(header).FontSize(8).Bold();
                    }

                    for (int i = 0; i < categories.Count; i++)
                    {
                        var c = categories[i];
                        table.Cell().Border(0.5f).Padding(2).Text($"{i + 1}").FontSize(7);
                        table.Cell().Border(0.5f).Padding(2).Text(c.Name).FontSize(7);
                        table.Cell().Border(0.5f).Padding(2).Text(c.Parent?.Name ?? "").FontSize(7);
                        table.Cell().Border(0.5f).Padding(2).Text($"{c.DefaultPassPercentage}").FontSize(7);
                        table.Cell().Border(0.5f).Padding(2).Text(c.Signatory?.FullName ?? "—").FontSize(7);
                        table.Cell().Border(0.5f).Padding(2).Text($"{c.SortOrder}").FontSize(7);
                        table.Cell().Border(0.5f).Padding(2).Text(c.IsActive ? "Aktif" : "Nonaktif").FontSize(7);
                    }
                });
            });
        });
    });

    var stream = new MemoryStream();
    pdf.GeneratePdf(stream);
    stream.Position = 0;
    var fileName = $"KategoriAssessment_{DateTime.Now:yyyyMMdd_HHmmss}.pdf";
    return File(stream.ToArray(), "application/pdf", fileName);
}
```

Pastikan `using QuestPDF.Fluent;` dan `using QuestPDF.Helpers;` sudah ada di top of file (kemungkinan sudah dari export lain, tapi verifikasi).
  </action>
  <verify>
    <automated>grep -n "ExportCategoriesExcel\|ExportCategoriesPdf" Controllers/AdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/AdminController.cs` contains `public async Task<IActionResult> ExportCategoriesExcel()`
    - `Controllers/AdminController.cs` contains `public async Task<IActionResult> ExportCategoriesPdf()`
    - Both methods have `[Authorize(Roles = "Admin, HC")]` attribute
    - `dotnet build --no-restore` exits 0
  </acceptance_criteria>
  <done>Dua action methods export berfungsi: Excel dengan header biru muda dan PDF plain table</done>
</task>

<task type="auto">
  <name>Task 3: Tambah dropdown tombol export di ManageCategories view</name>
  <files>Views/Admin/ManageCategories.cshtml</files>
  <read_first>
    - Views/Admin/ManageCategories.cshtml (lines 20-31, header section)
  </read_first>
  <action>
Di header section (sekitar line 21-31), ubah area tombol. Ganti single button "Tambah Kategori" menjadi button group yang berisi tombol export dropdown + tombol Tambah Kategori.

Ubah dari:
```html
<button type="button" class="btn btn-primary" data-bs-toggle="collapse" data-bs-target="#addCategoryForm">
    <i class="bi bi-plus-circle me-2"></i>Tambah Kategori
</button>
```

Menjadi:
```html
<div class="d-flex gap-2">
    <div class="btn-group">
        <button type="button" class="btn btn-outline-success dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-download me-1"></i>Export
        </button>
        <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="@Url.Action("ExportCategoriesExcel", "Admin")"><i class="bi bi-file-earmark-excel me-2"></i>Excel</a></li>
            <li><a class="dropdown-item" href="@Url.Action("ExportCategoriesPdf", "Admin")"><i class="bi bi-file-earmark-pdf me-2"></i>PDF</a></li>
        </ul>
    </div>
    <button type="button" class="btn btn-primary" data-bs-toggle="collapse" data-bs-target="#addCategoryForm">
        <i class="bi bi-plus-circle me-2"></i>Tambah Kategori
    </button>
</div>
```
  </action>
  <verify>
    <automated>grep -n "ExportCategoriesExcel\|ExportCategoriesPdf" Views/Admin/ManageCategories.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/ManageCategories.cshtml` contains `ExportCategoriesExcel`
    - `Views/Admin/ManageCategories.cshtml` contains `ExportCategoriesPdf`
    - `Views/Admin/ManageCategories.cshtml` contains `btn-outline-success dropdown-toggle`
    - `dotnet build --no-restore` exits 0
  </acceptance_criteria>
  <done>Halaman ManageCategories menampilkan dropdown Export (Excel/PDF) di samping tombol Tambah Kategori</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` berhasil tanpa error
- `grep -n "ThenInclude(ch => ch.Signatory)" Controllers/AdminController.cs` menunjukkan fix bug
- `grep -n "ExportCategoriesExcel\|ExportCategoriesPdf" Controllers/AdminController.cs` menunjukkan 2 action baru
- `grep -n "ExportCategoriesExcel\|ExportCategoriesPdf" Views/Admin/ManageCategories.cshtml` menunjukkan tombol export
</verification>

<success_criteria>
1. Bug fix: Signatory loaded untuk semua level hierarki di SetCategoriesViewBag
2. Export Excel: File .xlsx terdownload dengan header biru muda, kolom auto-fit, data semua kategori
3. Export PDF: File .pdf terdownload dengan plain table, semua kategori
4. UI: Dropdown export muncul di header ManageCategories
5. Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/259-export-categories-bugfix-signatory/259-01-SUMMARY.md`
</output>
