---
phase: 260-auto-cascade-perubahan-nama-organizationunit-ke-semua-user-records-dan-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-09

must_haves:
  truths:
    - "Rename Bagian (Level 0) auto-update ApplicationUser.Section dan CoachCoacheeMapping.AssignmentSection"
    - "Rename Unit (Level 1+) auto-update ApplicationUser.Unit dan CoachCoacheeMapping.AssignmentUnit"
    - "Reparent Unit auto-update Section semua user di unit tersebut ke nama Bagian baru"
    - "Deactivate diblokir jika masih ada user aktif di unit"
    - "Flash message menampilkan jumlah user dan mapping yang terupdate"
    - "DownloadImportTemplate menampilkan nama Bagian dinamis dari database"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "Cascade logic in EditOrganizationUnit, block in ToggleOrganizationUnitActive, dynamic template"
      contains: "oldName"
  key_links:
    - from: "EditOrganizationUnit"
      to: "ApplicationUser.Section, ApplicationUser.Unit"
      via: "LINQ Where + foreach update"
      pattern: "u\\.Section == oldName"
    - from: "EditOrganizationUnit"
      to: "CoachCoacheeMapping.AssignmentSection, AssignmentUnit"
      via: "LINQ Where + foreach update"
      pattern: "m\\.AssignmentSection == oldName"
    - from: "ToggleOrganizationUnitActive"
      to: "ApplicationUser"
      via: "AnyAsync check before deactivate"
      pattern: "hasActiveUsers"
    - from: "DownloadImportTemplate"
      to: "GetAllSectionsAsync"
      via: "async call for dynamic section names"
      pattern: "GetAllSectionsAsync"
---

<objective>
Tambahkan cascade logic ke EditOrganizationUnit agar rename/reparent OrganizationUnit otomatis memperbarui semua field denormalized di ApplicationUser dan CoachCoacheeMapping. Blokir deactivate jika masih ada user aktif. Ubah DownloadImportTemplate dari hardcoded ke dinamis.

Purpose: Menjaga konsistensi data denormalized saat organisasi berubah, tanpa perlu manual update per user.
Output: AdminController.cs yang sudah dilengkapi cascade, block deactivate, dan dynamic template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/260-auto-cascade-perubahan-nama-organizationunit-ke-semua-user-records-dan-template/260-CONTEXT.md
@.planning/phases/260-auto-cascade-perubahan-nama-organizationunit-ke-semua-user-records-dan-template/260-RESEARCH.md

<interfaces>
<!-- From Controllers/AdminController.cs -->
EditOrganizationUnit (line 7793): public async Task<IActionResult> EditOrganizationUnit(int id, string name, int? parentId)
  - unit.Name diset di line 7840: unit.Name = name.Trim();
  - SaveChangesAsync di line 7841
  - Reparent logic lines 7827-7838: updates ParentId, Level, calls UpdateChildrenLevelsAsync

ToggleOrganizationUnitActive (line 7875): public async Task<IActionResult> ToggleOrganizationUnitActive(int id)
  - Existing check: children active (line 7883)
  - Toggle: unit.IsActive = !unit.IsActive (line 7889)

DownloadImportTemplate (line 5420): public IActionResult DownloadImportTemplate()
  - Hardcoded line 5456: ws.Cell(3, 1).Value = "Kolom Bagian: RFCC / DHT / HMU / NGP / GAST"

<!-- From Data/ApplicationDbContext.cs -->
GetAllSectionsAsync(): returns Task<List<string>> — active Level 0 OrganizationUnit names

<!-- From Models/ -->
ApplicationUser: string Section, string Unit, string Directorate
CoachCoacheeMapping: string AssignmentSection, string AssignmentUnit
OrganizationUnit: int Id, string Name, int Level, int? ParentId, bool IsActive
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cascade rename/reparent di EditOrganizationUnit + blokir deactivate di ToggleOrganizationUnitActive</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 7789-7894 — EditOrganizationUnit dan ToggleOrganizationUnitActive)
    - Models/ApplicationUser.cs (fields Section, Unit)
    - Models/CoachCoacheeMapping.cs (fields AssignmentSection, AssignmentUnit)
    - Models/OrganizationUnit.cs (fields Level, ParentId, Name)
    - Data/ApplicationDbContext.cs (GetAllSectionsAsync method)
  </read_first>
  <action>
**EditOrganizationUnit (line 7793):**

1. Di awal method (setelah `var unit = await _context.OrganizationUnits.FindAsync(id);` line 7795), capture old values:
```csharp
string oldName = unit.Name;
int? oldParentId = unit.ParentId;
```

2. Setelah reparent logic (line 7838) dan SEBELUM `unit.Name = name.Trim();` (line 7840), tambahkan cascade logic. Variabel counter untuk flash message:
```csharp
int cascadedUsers = 0;
int cascadedMappings = 0;
```

3. Cascade rename — jika nama berubah (`oldName != name.Trim()`):
   - Jika `unit.Level == 0` (Bagian): query `_context.Users.Where(u => u.Section == oldName).ToListAsync()`, foreach set `u.Section = name.Trim()`. Query `_context.CoachCoacheeMappings.Where(m => m.AssignmentSection == oldName).ToListAsync()`, foreach set `m.AssignmentSection = name.Trim()`.
   - Jika `unit.Level >= 1` (Unit): query `_context.Users.Where(u => u.Unit == oldName).ToListAsync()`, foreach set `u.Unit = name.Trim()`. Query `_context.CoachCoacheeMappings.Where(m => m.AssignmentUnit == oldName).ToListAsync()`, foreach set `m.AssignmentUnit = name.Trim()`.
   - Increment `cascadedUsers` dan `cascadedMappings` sesuai count.

4. Cascade reparent — jika parent berubah (`oldParentId != parentId`) dan unit adalah Level 1+:
   - Cari root ancestor (Level 0) dari new parent: loop naik via ParentId sampai Level == 0, ambil Name-nya sebagai `newSectionName`.
   - Query `_context.Users.Where(u => u.Unit == unit.Name).ToListAsync()`, foreach set `u.Section = newSectionName`. Tambah ke `cascadedUsers`.
   - Query `_context.CoachCoacheeMappings.Where(m => m.AssignmentUnit == unit.Name).ToListAsync()`, foreach set `m.AssignmentSection = newSectionName`. Tambah ke `cascadedMappings`.
   - NOTE: Gunakan `unit.Name` (bukan oldName) untuk WHERE clause reparent, karena saat ini nama unit sudah bisa berubah. Tapi karena `unit.Name` belum diset ke name.Trim() di titik ini (line 7840 belum dieksekusi), gunakan `oldName` juga OK untuk unit name match. Yang penting: field yang diupdate adalah Section (bukan Unit), karena reparent mengubah parent Bagian.

5. Ganti TempData success message (line 7843) menjadi:
```csharp
if (cascadedUsers > 0 || cascadedMappings > 0)
    TempData["Success"] = $"Unit berhasil diperbarui. {cascadedUsers} user dan {cascadedMappings} mapping terupdate.";
else
    TempData["Success"] = "Unit berhasil diperbarui.";
```

Per D-01: semua cascade dalam satu SaveChangesAsync (sudah ada di line 7841).
Per D-02: cascade ke ApplicationUser.Section dan Unit.
Per D-03: cascade ke CoachCoacheeMapping.AssignmentSection dan AssignmentUnit.
Per D-06: flash message dengan jumlah.
Per D-09: reparent auto-update Section.

**ToggleOrganizationUnitActive (line 7875):**

6. Setelah existing children check (line 7883-7887), tambahkan blokir deactivate jika ada user aktif (per D-07). Hanya saat `unit.IsActive` (akan di-deactivate):
```csharp
if (unit.IsActive)
{
    bool hasActiveUsers;
    if (unit.Level == 0)
        hasActiveUsers = await _context.Users.AnyAsync(u => u.Section == unit.Name);
    else
        hasActiveUsers = await _context.Users.AnyAsync(u => u.Unit == unit.Name);

    if (hasActiveUsers)
    {
        TempData["Error"] = "Tidak dapat menonaktifkan unit. Masih ada user aktif yang terdaftar di unit ini. Pindahkan semua user terlebih dahulu.";
        return RedirectToAction("ManageOrganization");
    }
}
```
Letakkan SETELAH children check (line 7887) dan SEBELUM toggle (line 7889).
  </action>
  <verify>
    <automated>grep -n "oldName" Controllers/AdminController.cs | head -5 && grep -n "cascadedUsers" Controllers/AdminController.cs | head -3 && grep -n "hasActiveUsers" Controllers/AdminController.cs | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs contains `string oldName = unit.Name;` before line 7840
    - Controllers/AdminController.cs contains `int? oldParentId = unit.ParentId;`
    - Controllers/AdminController.cs contains `u.Section == oldName`
    - Controllers/AdminController.cs contains `m.AssignmentSection == oldName`
    - Controllers/AdminController.cs contains `u.Unit == oldName`
    - Controllers/AdminController.cs contains `m.AssignmentUnit == oldName`
    - Controllers/AdminController.cs contains `cascadedUsers` and `cascadedMappings`
    - Controllers/AdminController.cs contains `hasActiveUsers`
    - Controllers/AdminController.cs contains `Masih ada user aktif yang terdaftar di unit ini`
    - Controllers/AdminController.cs contains `$"Unit berhasil diperbarui. {cascadedUsers} user dan {cascadedMappings} mapping terupdate."`
    - The cascade code appears BEFORE the `unit.Name = name.Trim();` line
    - The hasActiveUsers check appears AFTER children check and BEFORE `unit.IsActive = !unit.IsActive;`
  </acceptance_criteria>
  <done>Rename Bagian cascades ke User.Section + Mapping.AssignmentSection, rename Unit cascades ke User.Unit + Mapping.AssignmentUnit, reparent cascades Section, deactivate diblokir jika ada user aktif, flash message menampilkan jumlah.</done>
</task>

<task type="auto">
  <name>Task 2: Ubah DownloadImportTemplate dari hardcoded ke dinamis</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 5417-5470 — DownloadImportTemplate)
    - Data/ApplicationDbContext.cs (GetAllSectionsAsync method)
  </read_first>
  <action>
Per D-05: ubah hardcoded Bagian names menjadi query dinamis.

1. Ubah method signature dari:
```csharp
public IActionResult DownloadImportTemplate()
```
menjadi:
```csharp
public async Task&lt;IActionResult&gt; DownloadImportTemplate()
```

2. Ganti line 5456:
```csharp
ws.Cell(3, 1).Value = "Kolom Bagian: RFCC / DHT / HMU / NGP / GAST";
```
menjadi:
```csharp
var sections = await _context.GetAllSectionsAsync();
ws.Cell(3, 1).Value = $"Kolom Bagian: {string.Join(" / ", sections)}";
```

Hanya 2 perubahan: signature async dan query dinamis.
  </action>
  <verify>
    <automated>grep -n "async Task.*DownloadImportTemplate" Controllers/AdminController.cs && grep -n "GetAllSectionsAsync" Controllers/AdminController.cs | grep -i "download\|5[45]"</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs contains `public async Task<IActionResult> DownloadImportTemplate()`
    - Controllers/AdminController.cs contains `var sections = await _context.GetAllSectionsAsync();` inside DownloadImportTemplate
    - Controllers/AdminController.cs contains `string.Join(" / ", sections)` near the GetAllSectionsAsync call
    - Controllers/AdminController.cs does NOT contain `RFCC / DHT / HMU / NGP / GAST`
  </acceptance_criteria>
  <done>DownloadImportTemplate menggunakan nama Bagian dinamis dari database, bukan hardcoded string.</done>
</task>

</tasks>

<verification>
1. Build berhasil: `dotnet build --no-restore` exits 0
2. Grep confirm cascade: `grep -c "oldName" Controllers/AdminController.cs` returns >= 4
3. Grep confirm block: `grep -c "hasActiveUsers" Controllers/AdminController.cs` returns >= 2
4. Grep confirm dynamic: `grep "GetAllSectionsAsync" Controllers/AdminController.cs` shows usage in DownloadImportTemplate area
5. Grep confirm no hardcoded: `grep "RFCC / DHT / HMU / NGP / GAST" Controllers/AdminController.cs` returns nothing
</verification>

<success_criteria>
- dotnet build passes
- EditOrganizationUnit cascade rename Bagian -> User.Section + Mapping.AssignmentSection
- EditOrganizationUnit cascade rename Unit -> User.Unit + Mapping.AssignmentUnit
- EditOrganizationUnit cascade reparent -> User.Section update
- ToggleOrganizationUnitActive blocked when active users exist
- Flash message shows cascaded count
- DownloadImportTemplate uses dynamic section names
- No hardcoded "RFCC / DHT / HMU / NGP / GAST" remains
</success_criteria>

<output>
After completion, create `.planning/phases/260-auto-cascade-perubahan-nama-organizationunit-ke-semua-user-records-dan-template/260-01-SUMMARY.md`
</output>
