---
phase: 261-validasi-konsistensi-field-organisasi-di-coachcoacheemapping-dan-directorate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09

must_haves:
  truths:
    - "CoachCoacheeMapping dengan Section/Unit invalid di-auto-fix dari coachee user record"
    - "Mapping yang coachee user-nya juga invalid dilaporkan sebagai unfixable"
    - "Create mapping gagal jika Section/Unit tidak ada di OrganizationUnit aktif"
    - "Edit mapping gagal jika Section/Unit tidak ada di OrganizationUnit aktif"
    - "Import mapping gagal per-row jika coachee Section/Unit tidak valid"
    - "Reactivate mapping saat import juga update Section/Unit dari coachee user"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "CleanupCoachCoacheeMappingOrg action + runtime validation di Assign/Edit/Import"
      contains: "CleanupCoachCoacheeMappingOrg"
  key_links:
    - from: "Controllers/AdminController.cs (CleanupCoachCoacheeMappingOrg)"
      to: "ApplicationDbContext.GetSectionUnitsDictAsync()"
      via: "dict lookup for validation"
      pattern: "GetSectionUnitsDictAsync"
    - from: "Controllers/AdminController.cs (CoachCoacheeMappingAssign)"
      to: "ApplicationDbContext.GetSectionUnitsDictAsync()"
      via: "runtime validation before save"
      pattern: "sectionUnitsDict.TryGetValue"
---

<objective>
Validasi konsistensi field organisasi di CoachCoacheeMapping: one-time cleanup data existing + runtime validation pada create/edit/import.

Purpose: Mencegah data CoachCoacheeMapping memiliki AssignmentSection/AssignmentUnit yang tidak konsisten dengan OrganizationUnit aktif.
Output: Admin action cleanup + runtime validation di 3 flow (assign, edit, import).
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/261-validasi-konsistensi-field-organisasi-di-coachcoacheemapping-dan-directorate/261-CONTEXT.md
@.planning/phases/261-validasi-konsistensi-field-organisasi-di-coachcoacheemapping-dan-directorate/261-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-time cleanup admin action CleanupCoachCoacheeMappingOrg</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 4092-4257 for CoachCoacheeMappingAssign pattern, lines 7794-7908 for Phase 260 cascade reference)
    - Data/ApplicationDbContext.cs (GetSectionUnitsDictAsync helper)
    - Models/CoachCoacheeMapping.cs (entity fields)
  </read_first>
  <action>
Tambahkan action `CleanupCoachCoacheeMappingOrg` di AdminController (per D-01, D-03, D-04, D-05, D-06):

1. Buat method `[HttpPost] [Authorize(Roles = "Admin, HC")] [ValidateAntiForgeryToken] public async Task&lt;IActionResult&gt; CleanupCoachCoacheeMappingOrg()`

2. Logic:
   - Load `var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();`
   - Load semua `CoachCoacheeMappings.Where(m => m.IsActive).ToListAsync()`
   - Load user dict: `_context.Users.Select(u => new { u.Id, u.Section, u.Unit }).ToDictionaryAsync(u => u.Id)`
   - Untuk setiap mapping, cek apakah `AssignmentSection?.Trim()` dan `AssignmentUnit?.Trim()` valid di sectionUnitsDict (per D-03)
   - Jika invalid: coba fix dari coachee user record (`userDict[m.CoacheeId]`) — jika user Section/Unit valid di dict, update mapping (per D-04)
   - Jika user juga invalid: tambah ke `unfixable` list dengan Id, CoacheeId, AssignmentSection, AssignmentUnit (per D-05)
   - Null AssignmentSection/Unit dianggap invalid (needs fix)
   - Trim semua value sebelum compare
   - `SaveChangesAsync()` setelah loop
   - Set `TempData["CleanupReport"]` sebagai JSON: `{ autoFixed: N, unfixable: [...] }` (per D-06)
   - Redirect ke `CoachCoacheeMapping` index

3. Exact validation logic per mapping:
```csharp
var sec = m.AssignmentSection?.Trim();
var unit = m.AssignmentUnit?.Trim();
bool isValid = !string.IsNullOrEmpty(sec) && !string.IsNullOrEmpty(unit)
    && sectionUnitsDict.TryGetValue(sec, out var vu) && vu.Contains(unit);
```

4. Tempatkan method di dekat method CoachCoacheeMapping lainnya (sekitar line 4400+).
  </action>
  <verify>
    <automated>grep -n "CleanupCoachCoacheeMappingOrg" Controllers/AdminController.cs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs contains `public async Task<IActionResult> CleanupCoachCoacheeMappingOrg()`
    - Controllers/AdminController.cs contains `[Authorize(Roles = "Admin, HC")]` above CleanupCoachCoacheeMappingOrg
    - Controllers/AdminController.cs contains `GetSectionUnitsDictAsync()` inside CleanupCoachCoacheeMappingOrg
    - Controllers/AdminController.cs contains `TempData["CleanupReport"]`
    - Controllers/AdminController.cs contains `autoFixed` in the TempData JSON
    - Controllers/AdminController.cs contains `unfixable` in the cleanup report
    - Method handles null AssignmentSection/Unit (treats as invalid)
    - All string comparisons use Trim()
  </acceptance_criteria>
  <done>Action CleanupCoachCoacheeMappingOrg tersedia, bisa dipanggil POST, menghasilkan report auto-fixed + unfixable via TempData</done>
</task>

<task type="auto">
  <name>Task 2: Runtime validation Section/Unit di Assign, Edit, dan Import</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 4092-4257 for CoachCoacheeMappingAssign, lines 4282-4400 for CoachCoacheeMappingEdit, lines 3901-4090 for ImportCoachCoacheeMapping)
    - Data/ApplicationDbContext.cs (GetSectionUnitsDictAsync)
  </read_first>
  <action>
Tambahkan runtime validation di 3 flow (per D-07, D-08, D-09, D-02):

**A. CoachCoacheeMappingAssign (per D-07):**
Setelah existing null/whitespace checks (~line 4104), tambahkan:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
if (!sectionUnitsDict.TryGetValue(req.AssignmentSection!.Trim(), out var validUnits)
    || !validUnits.Contains(req.AssignmentUnit!.Trim()))
    return Json(new { success = false, message = "Section/Unit tidak ditemukan di data organisasi aktif." });
```

**B. CoachCoacheeMappingEdit (per D-08):**
Sebelum assignment ke mapping (~line 4313), tambahkan:
```csharp
var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();
var secEdit = req.AssignmentSection?.Trim();
var unitEdit = req.AssignmentUnit?.Trim();
if (!string.IsNullOrEmpty(secEdit) && !string.IsNullOrEmpty(unitEdit))
{
    if (!sectionUnitsDict.TryGetValue(secEdit, out var validUnitsEdit) || !validUnitsEdit.Contains(unitEdit))
        return Json(new { success = false, message = "Section/Unit tidak ditemukan di data organisasi aktif." });
}
```

**C. ImportCoachCoacheeMapping (per D-09):**
1. Di awal method (sebelum loop), load: `var sectionUnitsDict = await _context.GetSectionUnitsDictAsync();`
2. Di dalam per-row loop (~line 3940), sebelum create/assign mapping, validasi coachee user Section/Unit:
```csharp
if (string.IsNullOrEmpty(coacheeUser.Section) || string.IsNullOrEmpty(coacheeUser.Unit)
    || !sectionUnitsDict.TryGetValue(coacheeUser.Section.Trim(), out var vuImport)
    || !vuImport.Contains(coacheeUser.Unit.Trim()))
{
    result.Status = "Error";
    result.Message = $"Section/Unit coachee ('{coacheeUser.Section}'/'{coacheeUser.Unit}') tidak valid di OrganizationUnit aktif";
    results.Add(result);
    continue;
}
```
3. **PENTING (Pitfall 3):** Pada blok reactivation (~line 4021-4024), tambahkan update Section/Unit dari coachee user SETELAH validasi di atas:
```csharp
existingMapping.AssignmentSection = coacheeUser.Section.Trim();
existingMapping.AssignmentUnit = coacheeUser.Unit.Trim();
```

Error message yang konsisten: "Section/Unit tidak ditemukan di data organisasi aktif." untuk assign/edit, dan per-row error message untuk import.
  </action>
  <verify>
    <automated>grep -c "Section/Unit tidak ditemukan di data organisasi aktif" Controllers/AdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "Section/Unit tidak ditemukan di data organisasi aktif" returns count >= 2 (assign + edit)
    - grep "tidak valid di OrganizationUnit aktif" returns count >= 1 (import)
    - CoachCoacheeMappingAssign contains `GetSectionUnitsDictAsync()` call
    - CoachCoacheeMappingEdit contains `GetSectionUnitsDictAsync()` call
    - ImportCoachCoacheeMapping contains `GetSectionUnitsDictAsync()` call (loaded once before loop)
    - Reactivation block (~line 4021) contains `AssignmentSection = coacheeUser.Section.Trim()`
    - Reactivation block contains `AssignmentUnit = coacheeUser.Unit.Trim()`
  </acceptance_criteria>
  <done>Runtime validation aktif di assign, edit, dan import. Section/Unit yang tidak valid di OrganizationUnit aktif ditolak. Reactivation juga update Section/Unit.</done>
</task>

</tasks>

<verification>
1. `dotnet build` berhasil tanpa error
2. grep confirm CleanupCoachCoacheeMappingOrg exists
3. grep confirm validation di 3 flow (Assign, Edit, Import)
4. grep confirm reactivation update Section/Unit
</verification>

<success_criteria>
- CleanupCoachCoacheeMappingOrg action tersedia dan menghasilkan report via TempData
- CoachCoacheeMappingAssign menolak Section/Unit yang tidak valid
- CoachCoacheeMappingEdit menolak Section/Unit yang tidak valid
- ImportCoachCoacheeMapping menolak row dengan coachee Section/Unit tidak valid
- Import reactivation juga update Section/Unit dari coachee user
- `dotnet build` berhasil
</success_criteria>

<output>
After completion, create `.planning/phases/261-validasi-konsistensi-field-organisasi-di-coachcoacheemapping-dan-directorate/261-01-SUMMARY.md`
</output>
