---
phase: 262-fix-hardcoded-urls-in-views-for-sub-path-deployment-compatibility
plan: 03
type: execute
wave: 2
depends_on: ["262-01"]
files_modified:
  - Views/CMP/Certificate.cshtml
  - Views/Admin/ManageCategories.cshtml
  - Views/ProtonData/Override.cshtml
  - Views/Home/_CertAlertBanner.cshtml
  - Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
  - Views/Admin/Shared/_RenewalGroupedPartial.cshtml
  - Views/Shared/_CertificateHistoryModalContent.cshtml
  - Views/Shared/Error.cshtml
  - Views/CDP/PlanIdp.cshtml
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/AssessmentMonitoring.cshtml
  - Views/CMP/Assessment.cshtml
autonomous: true
requirements:
  - D-03
  - D-05
  - D-06

must_haves:
  truths:
    - "Tidak ada hardcoded absolute URL tersisa di SELURUH project views"
    - "Build sukses tanpa error"
  artifacts:
    - path: "Views/Shared/Error.cshtml"
      provides: "Error page with PathBase-aware home link"
      contains: "~/"
    - path: "Views/CMP/Certificate.cshtml"
      provides: "Certificate view with PathBase-aware image src"
      contains: "~/images"
  key_links:
    - from: "All remaining view files"
      to: "basePath/appUrl from _Layout.cshtml"
      via: "JS function call or Razor tilde"
      pattern: "appUrl\\(|~/|asp-controller"
---

<objective>
Fix semua hardcoded URL di 12 file view remaining dengan volume rendah (1-2 URL per file, total ~17 fixes) + final sweep seluruh project.

Purpose: Menuntaskan semua hardcoded URL. Per D-06, semua file difix sekaligus.
Output: Zero hardcoded absolute URL di seluruh Views folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/262-fix-hardcoded-urls-in-views-for-sub-path-deployment-compatibility/262-CONTEXT.md
@.planning/phases/262-fix-hardcoded-urls-in-views-for-sub-path-deployment-compatibility/262-RESEARCH.md
@.planning/phases/262-fix-hardcoded-urls-in-views-for-sub-path-deployment-compatibility/262-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hardcoded URL di 12 file low-volume + CMP/Assessment.cshtml</name>
  <files>
    Views/CMP/Certificate.cshtml,
    Views/Admin/ManageCategories.cshtml,
    Views/ProtonData/Override.cshtml,
    Views/Home/_CertAlertBanner.cshtml,
    Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml,
    Views/Admin/Shared/_AssessmentGroupsTab.cshtml,
    Views/Admin/Shared/_RenewalGroupedPartial.cshtml,
    Views/Shared/_CertificateHistoryModalContent.cshtml,
    Views/Shared/Error.cshtml,
    Views/CDP/PlanIdp.cshtml,
    Views/Admin/CreateAssessment.cshtml,
    Views/Admin/AssessmentMonitoring.cshtml,
    Views/CMP/Assessment.cshtml
  </files>
  <read_first>
    Views/CMP/Certificate.cshtml,
    Views/Admin/ManageCategories.cshtml,
    Views/ProtonData/Override.cshtml,
    Views/Home/_CertAlertBanner.cshtml,
    Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml,
    Views/Admin/Shared/_AssessmentGroupsTab.cshtml,
    Views/Admin/Shared/_RenewalGroupedPartial.cshtml,
    Views/Shared/_CertificateHistoryModalContent.cshtml,
    Views/Shared/Error.cshtml,
    Views/CDP/PlanIdp.cshtml,
    Views/Admin/CreateAssessment.cshtml,
    Views/Admin/AssessmentMonitoring.cshtml,
    Views/CMP/Assessment.cshtml
  </read_first>
  <action>
Fix semua hardcoded URL di 13 file ini (masing-masing 1-2 URL):

1. **Views/CMP/Certificate.cshtml** (2): `src="/images/..."` -> `src="~/images/..."`
2. **Views/Admin/ManageCategories.cshtml** (2): `src="/images/..."` -> `src="~/images/..."`
3. **Views/ProtonData/Override.cshtml** (2): fetch calls -> `appUrl()`
4. **Views/Home/_CertAlertBanner.cshtml** (2): `href="/..."` -> tag helpers atau `~/`
5. **Views/Admin/Shared/_RenewalCertificateTablePartial.cshtml** (2): `href="/..."` -> tag helpers
6. **Views/Admin/Shared/_AssessmentGroupsTab.cshtml** (1): `action="/..."` in form -> `asp-controller` + `asp-action`
7. **Views/Admin/Shared/_RenewalGroupedPartial.cshtml** (1): `href="/..."` -> tag helper
8. **Views/Shared/_CertificateHistoryModalContent.cshtml** (1): `href="/..."` -> tag helper
9. **Views/Shared/Error.cshtml** (1): `href="/"` -> `href="~/"`
10. **Views/CDP/PlanIdp.cshtml** (1): JS-built href -> `basePath + '/...'`
11. **Views/Admin/CreateAssessment.cshtml** (1): fetch -> `appUrl()`
12. **Views/Admin/AssessmentMonitoring.cshtml** (1): fetch -> `appUrl()`
13. **Views/CMP/Assessment.cshtml**: Cek file ini juga — ada di git status sebagai modified. Perbaiki hardcoded URL jika ada.

Rules:
- HTML `href="/Controller/Action"` pada `<a>` tag -> `asp-controller` + `asp-action`
- HTML `src="/images/..."` -> `src="~/images/..."`
- HTML `href="/documents/..."` -> `href="~/documents/..."`
- HTML `href="/"` -> `href="~/"`
- HTML `<form action="/...">` in Razor -> `asp-controller` + `asp-action`
- JS fetch/$.get -> `appUrl()`
- JS-built href string -> `basePath + '/...'`
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `dotnet build` succeeds with 0 errors
- `Views/CMP/Certificate.cshtml` contains `~/images` instead of `/images`
- `Views/Shared/Error.cshtml` contains `href="~/"` instead of `href="/"`
- `Views/Admin/Shared/_AssessmentGroupsTab.cshtml` contains `asp-controller` instead of `action="/`
  </acceptance_criteria>
  <done>17+ hardcoded URL di 13 file low-volume sudah diganti, build sukses</done>
</task>

<task type="auto">
  <name>Task 2: Final sweep — verifikasi zero hardcoded absolute URL di seluruh Views</name>
  <files></files>
  <read_first></read_first>
  <action>
Jalankan comprehensive grep di seluruh folder Views/ untuk mencari sisa hardcoded absolute URL:

```bash
# Cari semua hardcoded absolute URL patterns
grep -rn 'href="/' Views/ --include="*.cshtml" | grep -v 'href="~/' | grep -v 'href="https' | grep -v 'href="http' | grep -v 'href="//' | grep -v 'asp-'
grep -rn 'src="/' Views/ --include="*.cshtml" | grep -v 'src="~/' | grep -v 'src="https' | grep -v 'src="http' | grep -v 'src="//'
grep -rn 'action="/' Views/ --include="*.cshtml" | grep -v 'asp-'
grep -rn "fetch('/" Views/ --include="*.cshtml"
grep -rn 'fetch("/' Views/ --include="*.cshtml"
grep -rn "\\$.get('/" Views/ --include="*.cshtml"
grep -rn "\\$.ajax.*url: '/" Views/ --include="*.cshtml"
grep -rn "window.location.href = '/" Views/ --include="*.cshtml"
grep -rn "window.location.href = \"/" Views/ --include="*.cshtml"
grep -rn "postWithToken('/" Views/ --include="*.cshtml"
```

Jika ada yang terlewat, fix langsung. Setiap hasil grep harus dievaluasi:
- False positive (e.g., dalam comment, atau sudah benar) -> skip
- Genuine hardcoded URL -> fix sesuai pattern di atas

Goal: ZERO genuine hardcoded absolute URL di seluruh Views folder.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && echo "=== href hardcoded ===" && grep -rn 'href="/' Views/ --include="*.cshtml" | grep -v 'href="~/' | grep -v 'href="https' | grep -v 'href="http' | grep -v 'href="//' | grep -v 'asp-' | grep -v '@Url' | wc -l && echo "=== src hardcoded ===" && grep -rn 'src="/' Views/ --include="*.cshtml" | grep -v 'src="~/' | grep -v 'src="https' | grep -v 'src="http' | grep -v 'src="//' | wc -l && echo "=== fetch hardcoded ===" && grep -rn "fetch('/\|fetch(\"/" Views/ --include="*.cshtml" | wc -l && echo "=== Expected: all 0 ==="</automated>
  </verify>
  <acceptance_criteria>
- grep for hardcoded `href="/` (excluding ~/,https,http,//,asp-,@Url) returns 0 results
- grep for hardcoded `src="/` (excluding ~/,https,http,//) returns 0 results
- grep for hardcoded `fetch('/` or `fetch("/` returns 0 results
- `dotnet build` succeeds
  </acceptance_criteria>
  <done>Zero hardcoded absolute URL di seluruh Views folder, comprehensive sweep selesai</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses
- Comprehensive grep across ALL Views/*.cshtml menunjukkan zero hardcoded absolute URL
</verification>

<success_criteria>
Seluruh project views bebas dari hardcoded absolute URL. Semua URL menggunakan mekanisme PathBase-aware (tag helpers, ~/, appUrl, basePath).
</success_criteria>

<output>
After completion, create `.planning/phases/262-fix-hardcoded-urls-in-views-for-sub-path-deployment-compatibility/262-03-SUMMARY.md`
</output>
