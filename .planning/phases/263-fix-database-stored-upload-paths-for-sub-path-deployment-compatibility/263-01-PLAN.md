---
phase: 263-fix-database-stored-upload-paths-for-sub-path-deployment-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/ProtonData/Override.cshtml
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "Link SupportingDocPath di AssessmentMonitoringDetail resolve dengan PathBase prefix"
    - "Link evidencePath di Override modal resolve dengan basePath prefix"
    - "Path di database tetap format /uploads/... tanpa perubahan"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Render SupportingDocPath dengan Url.Content"
      contains: "Url.Content"
    - path: "Views/ProtonData/Override.cshtml"
      provides: "Render evidencePath dengan basePath prefix"
      contains: "basePath"
  key_links:
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      to: "Url.Content()"
      via: "Razor helper resolve ~ ke PathBase"
      pattern: 'Url\.Content\("~"'
    - from: "Views/ProtonData/Override.cshtml"
      to: "basePath global"
      via: "JS string concatenation"
      pattern: "basePath.*evidencePath"
---

<objective>
Fix 2 lokasi render file upload path dari database agar kompatibel dengan sub-path deployment /KPB-PortalHC/.

Purpose: Path database (/uploads/...) di-render langsung sebagai href/src tanpa PathBase — akan 404 saat deploy di sub-path.
Output: Kedua lokasi render sudah prefix PathBase secara otomatis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/263-fix-database-stored-upload-paths-for-sub-path-deployment-compatibility/263-CONTEXT.md
@.planning/phases/263-fix-database-stored-upload-paths-for-sub-path-deployment-compatibility/263-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Razor render SupportingDocPath di AssessmentMonitoringDetail (D-02, D-05)</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (lihat line ~525-535 untuk context lengkap)
  </read_first>
  <action>
Di line 529 (atau sekitarnya), ubah:
```csharp
href="@existingDto.SupportingDocPath"
```
menjadi:
```csharp
href="@Url.Content("~" + existingDto.SupportingDocPath)"
```

Per D-02: gunakan Url.Content() untuk resolve ~ ke PathBase. JANGAN gunakan syntax `href="~@path"` — itu tidak bekerja di Razor (lihat RESEARCH.md pitfall 1).

Per D-01/D-04: JANGAN ubah format penyimpanan path di controller — fix hanya di render.

Null-check sudah ada di line ~525 (`@if (!string.IsNullOrEmpty(...)`) jadi Url.Content() aman.
  </action>
  <verify>
    <automated>grep -n "Url.Content" Views/Admin/AssessmentMonitoringDetail.cshtml | grep -i "supporting\|DocPath"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Url.Content.*SupportingDocPath" Views/Admin/AssessmentMonitoringDetail.cshtml returns match
    - grep 'href="@existingDto.SupportingDocPath"' Views/Admin/AssessmentMonitoringDetail.cshtml returns NO match (old pattern removed)
  </acceptance_criteria>
  <done>SupportingDocPath link menggunakan Url.Content("~" + path) untuk resolve PathBase</done>
</task>

<task type="auto">
  <name>Task 2: Fix JS render evidencePath di Override.cshtml (D-03, D-06)</name>
  <files>Views/ProtonData/Override.cshtml</files>
  <read_first>
    - Views/ProtonData/Override.cshtml (lihat line ~350-360 untuk context lengkap)
  </read_first>
  <action>
Di line 354 (atau sekitarnya), cari pattern JS yang render evidencePath tanpa basePath prefix. Ubah dari:
```javascript
href="' + escHtml(data.evidencePath) + '"
```
menjadi:
```javascript
href="' + basePath + escHtml(data.evidencePath) + '"
```

Per D-03: gunakan `basePath` global variable (dari _Layout.cshtml:35). basePath sudah strip trailing slash, dan path DB dimulai dengan `/`, jadi concatenation menghasilkan `/KPB-PortalHC/uploads/...` — correct.

Null-check sudah ada di line ~353 (`if (data.evidenceFileName && data.evidencePath)`) jadi basePath concatenation aman.
  </action>
  <verify>
    <automated>grep -n "basePath.*evidencePath\|evidencePath.*basePath" Views/ProtonData/Override.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - grep "basePath.*escHtml(data.evidencePath)" Views/ProtonData/Override.cshtml returns match
    - grep tanpa basePath prefix langsung ke evidencePath di href context returns NO match
  </acceptance_criteria>
  <done>evidencePath link di Override modal menggunakan basePath prefix untuk resolve sub-path</done>
</task>

</tasks>

<verification>
1. grep "Url.Content.*SupportingDocPath" Views/Admin/AssessmentMonitoringDetail.cshtml — harus ada match
2. grep "basePath.*evidencePath" Views/ProtonData/Override.cshtml — harus ada match
3. Tidak ada perubahan di controller files (path storage tetap /uploads/...)
</verification>

<success_criteria>
- Kedua lokasi render DB path sudah prefix PathBase (via Url.Content atau basePath)
- Zero perubahan di database atau controller logic
- Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/263-fix-database-stored-upload-paths-for-sub-path-deployment-compatibility/263-01-SUMMARY.md`
</output>
