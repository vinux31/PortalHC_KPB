---
phase: 268-monitoring-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - Controllers/AdminController.cs
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/Admin/AssessmentMonitoring.cshtml
  - Hubs/AssessmentHub.cs
autonomous: false
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04

must_haves:
  truths:
    - "Dashboard monitoring menampilkan progress x/total per worker yang update real-time tanpa refresh"
    - "Status lifecycle Open -> InProgress -> Completed berubah sesuai aktivitas worker"
    - "Timer/elapsed tampil, bergerak, dan tidak nol/negatif untuk worker InProgress"
    - "Setelah worker submit, skor dan status pass/fail muncul di monitoring"
  artifacts:
    - path: ".planning/phases/268-monitoring-dashboard/268-01-SUMMARY.md"
      provides: "UAT results dan daftar bug fixes"
  key_links:
    - from: "CMPController.SaveAnswer"
      to: "AssessmentMonitoringDetail.cshtml progressUpdate handler"
      via: "SignalR push"
      pattern: "progressUpdate"
    - from: "CMPController.StartExam"
      to: "AssessmentMonitoringDetail.cshtml workerStarted handler"
      via: "SignalR push"
      pattern: "workerStarted"
    - from: "CMPController.ExamSummary"
      to: "AssessmentMonitoringDetail.cshtml workerSubmitted handler"
      via: "SignalR push"
      pattern: "workerSubmitted"
---

<objective>
UAT monitoring dashboard assessment di server development — verifikasi 4 requirement MON (progress real-time, lifecycle status, timer/elapsed, skor setelah submit). Claude analisa kode untuk identifikasi bug, user test di browser dua bersamaan, kemudian fix batch di project lokal.

Purpose: Fase terakhir v10.0 — pastikan Admin/HC bisa memantau ujian real-time
Output: UAT results, bug fixes (jika ada), SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/268-monitoring-dashboard/268-CONTEXT.md
@.planning/phases/268-monitoring-dashboard/268-RESEARCH.md

@Controllers/CMPController.cs
@Controllers/AdminController.cs
@Views/Admin/AssessmentMonitoringDetail.cshtml
@Views/Admin/AssessmentMonitoring.cshtml
@Hubs/AssessmentHub.cs
@Models/AssessmentMonitoringViewModel.cs
@wwwroot/js/assessment-hub.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analisa kode monitoring dan identifikasi bug potensial</name>
  <files>.planning/phases/268-monitoring-dashboard/268-UAT-ANALYSIS.md</files>
  <read_first>
    - Controllers/CMPController.cs (cari: SaveAnswer, StartExam, ExamSummary — SignalR push calls)
    - Controllers/AdminController.cs (cari: AssessmentMonitoring, AssessmentMonitoringDetail actions)
    - Views/Admin/AssessmentMonitoringDetail.cshtml (SignalR JS handlers: progressUpdate, workerStarted, workerSubmitted, timer logic)
    - Views/Admin/AssessmentMonitoring.cshtml (list view)
    - Hubs/AssessmentHub.cs (JoinMonitor, JoinBatch, group routing)
    - Models/AssessmentMonitoringViewModel.cs (MonitoringGroupViewModel, MonitoringSessionViewModel)
    - wwwroot/js/assessment-hub.js (connection setup, reconnect)
    - .planning/phases/268-monitoring-dashboard/268-RESEARCH.md (potensi bug yang sudah diidentifikasi)
  </read_first>
  <action>
Analisa kode monitoring secara menyeluruh untuk setiap requirement MON. Buat dokumen 268-UAT-ANALYSIS.md berisi:

1. **MON-01 (Progress real-time):** Trace alur dari CMPController.SaveAnswer → SignalR `progressUpdate` → JS handler di AssessmentMonitoringDetail.cshtml. Verifikasi:
   - `progressUpdate` dikirim dengan payload `{ sessionId, progress, totalQuestions }` di SEMUA path (package dan non-package)
   - JS handler menerima dan update `.progress-cell` dengan format "x/total"
   - Cek apakah `totalQuestions` dikirim dengan benar (bukan 0)

2. **MON-02 (Status lifecycle):** Trace:
   - `workerStarted` dari StartExam (hanya saat `justStarted=true`) → JS handler update badge ke "InProgress"
   - `workerSubmitted` dari ExamSummary → JS handler update badge ke "Completed"
   - Cek apakah `workerSubmitted` dikirim di NON-PACKAGE path juga (RESEARCH.md sudah flagging ini sebagai potensi bug)

3. **MON-03 (Timer/elapsed):** Analisa dua mekanisme timer:
   - Load-time: `updateTimeRemaining()` baca `data-started-at` dan `data-duration` dari `<tr>` attributes
   - `countdownMap` dari `updateRow()` — kapan ini dipanggil?
   - Verifikasi: jika worker mulai SETELAH halaman monitoring dibuka, apakah `data-started-at` terisi? (RESEARCH.md flagging ini sebagai bug)
   - Verifikasi: `workerStarted` handler — apakah update `data-started-at` attribute di `<tr>`?

4. **MON-04 (Skor setelah submit):** Trace:
   - `workerSubmitted` payload: `{ sessionId, workerName, score, result, status, totalQuestions }`
   - JS handler update Score column dan Result column
   - Cek apakah ada path di mana score bisa null/undefined

5. **Bug list:** Daftar semua bug yang ditemukan dari analisa kode, termasuk yang sudah diidentifikasi di RESEARCH.md (workerSubmitted non-package, timer tidak muncul setelah worker mulai). Categorize: CONFIRMED (pasti bug), SUSPECTED (perlu verifikasi UAT), INFO (catatan).

6. **Skenario UAT:** Tulis langkah test spesifik untuk user berdasarkan analisa, termasuk:
   - Skenario A: Buka monitoring, verifikasi data existing (Completed sessions) — MON-04
   - Skenario B: Dua browser bersamaan, worker mulai ujian baru, admin pantau — MON-01, MON-02, MON-03
   - Skenario C: Worker submit, admin verifikasi skor muncul real-time — MON-02, MON-04
   - Per D-02: Jika SignalR tidak bekerja, catat bug, test dengan refresh manual

Per D-04: Akun test admin@pertamina.com (monitor) dan rino.prasetyo@pertamina.com (worker).
Per D-05: Timer verifikasi eyeball saja — tampil, bergerak, tidak nol/negatif.
  </action>
  <verify>
    <automated>test -f ".planning/phases/268-monitoring-dashboard/268-UAT-ANALYSIS.md" &amp;&amp; grep -c "MON-0" ".planning/phases/268-monitoring-dashboard/268-UAT-ANALYSIS.md"</automated>
  </verify>
  <acceptance_criteria>
    - File 268-UAT-ANALYSIS.md exists
    - Contains sections for MON-01, MON-02, MON-03, MON-04
    - Contains "Bug List" section with categorized bugs (CONFIRMED/SUSPECTED/INFO)
    - Contains "Skenario UAT" section with step-by-step test instructions
    - References specific code locations (file:line or function names)
  </acceptance_criteria>
  <done>Dokumen analisa lengkap dengan bug list dan skenario UAT siap untuk user test di browser</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT monitoring dashboard di server development</name>
  <files>.planning/phases/268-monitoring-dashboard/268-UAT-ANALYSIS.md</files>
  <what-built>Dokumen analisa kode (268-UAT-ANALYSIS.md) berisi skenario test detail dan daftar bug potensial. User perlu menjalankan skenario UAT di browser server development.</what-built>
  <read_first>
    - .planning/phases/268-monitoring-dashboard/268-UAT-ANALYSIS.md
  </read_first>
  <action>
Presentasikan skenario UAT dari 268-UAT-ANALYSIS.md ke user. User akan menjalankan test di browser dan melaporkan hasil. Tidak ada kode yang perlu diubah di task ini — murni human verification.
  </action>
  <how-to-verify>
Buka 268-UAT-ANALYSIS.md untuk langkah detail. Ringkasan:

**Skenario A — Data Existing (MON-04):**
1. Login admin@pertamina.com di http://10.55.3.3/KPB-PortalHC/
2. Buka menu Kelola Data > Assessment Monitoring
3. Verifikasi: assessment group dari Phase 264-267 muncul di list
4. Klik detail salah satu group — verifikasi worker Completed menampilkan Score% dan Pass/Fail

**Skenario B — Live Two-Browser (MON-01, MON-02, MON-03):**
1. Browser 1 (Admin): Buka AssessmentMonitoringDetail untuk assessment yang ada worker Open/belum mulai
2. Browser 2 (Worker): Login rino.prasetyo@pertamina.com, mulai ujian
3. Browser 1: Verifikasi status berubah ke InProgress (MON-02), timer tampil dan bergerak (MON-03)
4. Browser 2: Jawab beberapa soal
5. Browser 1: Verifikasi progress "x/total" update tanpa refresh (MON-01)
6. Browser 2: Submit ujian
7. Browser 1: Verifikasi status Completed, Score%, Pass/Fail muncul real-time (MON-02, MON-04)

**Jika SignalR tidak bekerja (per D-02):**
- Catat apa yang tidak update real-time
- Refresh halaman monitoring — verifikasi data tetap benar setelah refresh

Laporkan hasil per requirement:
- MON-01: PASS/FAIL + detail
- MON-02: PASS/FAIL + detail
- MON-03: PASS/FAIL + detail
- MON-04: PASS/FAIL + detail
+ Daftar bug tambahan yang ditemukan saat testing
  </how-to-verify>
  <verify>User melaporkan hasil UAT per MON requirement dengan status PASS/FAIL</verify>
  <resume-signal>Laporkan hasil UAT per MON requirement (PASS/FAIL + detail) dan daftar bug. Ketik "all pass" jika semua berfungsi, atau jelaskan issue yang ditemukan untuk di-fix batch.</resume-signal>
  <acceptance_criteria>
    - User telah menjalankan Skenario A dan B di browser server development
    - Setiap MON requirement memiliki status PASS atau FAIL dengan detail
    - Bug list (jika ada) mencakup deskripsi dan langkah reproduksi
  </acceptance_criteria>
  <done>Semua 4 MON requirement di-verifikasi di browser, hasil dilaporkan, bug list dikumpulkan untuk batch fix</done>
</task>

<task type="auto">
  <name>Task 3: Batch fix bug dan dokumentasi hasil UAT</name>
  <files>
    - Controllers/CMPController.cs
    - Controllers/AdminController.cs
    - Views/Admin/AssessmentMonitoringDetail.cshtml
    - .planning/phases/268-monitoring-dashboard/268-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/268-monitoring-dashboard/268-UAT-ANALYSIS.md
    - Controllers/CMPController.cs
    - Views/Admin/AssessmentMonitoringDetail.cshtml
    - Hubs/AssessmentHub.cs
  </read_first>
  <action>
Berdasarkan hasil UAT dari Task 2:

**Jika ada bug yang dilaporkan user:**
1. Fix setiap bug di project lokal sesuai laporan user
2. Untuk bug yang sudah diidentifikasi di analisa (workerSubmitted non-package path, timer tidak update setelah worker mulai), terapkan fix yang sesuai:
   - **workerSubmitted non-package:** Tambahkan SignalR push `workerSubmitted` di non-package path di CMPController.ExamSummary jika belum ada
   - **Timer tidak muncul setelah worker mulai:** Update `workerStarted` JS handler di AssessmentMonitoringDetail.cshtml untuk mengisi `data-started-at` dan `data-duration` attributes pada `<tr>` + inisialisasi `countdownMap` entry
3. Untuk bug baru yang ditemukan user, analisa dan fix sesuai

**Jika semua PASS (tidak ada bug):**
1. Skip code fixes
2. Langsung buat SUMMARY

**SUMMARY (selalu):**
Buat 268-01-SUMMARY.md dengan format standar:
- Objective: UAT monitoring dashboard
- Tasks completed: analisa kode, UAT browser, fix bug (jika ada)
- Hasil per MON requirement: PASS/FAIL
- Bug fixes applied (jika ada): daftar file dan perubahan
- Artifacts: files modified
  </action>
  <verify>
    <automated>test -f ".planning/phases/268-monitoring-dashboard/268-01-SUMMARY.md" &amp;&amp; grep -c "MON-0" ".planning/phases/268-monitoring-dashboard/268-01-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - File 268-01-SUMMARY.md exists
    - Contains status for all 4 requirements: MON-01, MON-02, MON-03, MON-04
    - If bugs were reported: corresponding code files modified with fixes
    - If no bugs: SUMMARY states "no fixes needed"
  </acceptance_criteria>
  <done>Semua bug dari UAT di-fix di project lokal, SUMMARY mendokumentasikan hasil lengkap</done>
</task>

</tasks>

<verification>
- Semua 4 MON requirement terverifikasi di browser server development
- Bug yang ditemukan sudah di-fix di project lokal
- SUMMARY mendokumentasikan hasil UAT dan fix yang diterapkan
</verification>

<success_criteria>
1. Monitoring dashboard menampilkan progress real-time per worker (MON-01)
2. Status lifecycle berubah sesuai aktivitas worker (MON-02)
3. Timer/elapsed tampil dan bergerak untuk worker InProgress (MON-03)
4. Skor dan pass/fail muncul setelah worker submit (MON-04)
5. Semua bug yang ditemukan sudah di-fix di project lokal
</success_criteria>

<output>
After completion, create `.planning/phases/268-monitoring-dashboard/268-01-SUMMARY.md`
</output>
