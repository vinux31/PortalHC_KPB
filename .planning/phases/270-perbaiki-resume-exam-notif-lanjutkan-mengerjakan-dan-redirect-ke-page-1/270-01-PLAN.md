---
phase: 270-perbaiki-resume-exam-notif-lanjutkan-mengerjakan-dan-redirect-ke-page-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/StartExam.cshtml
autonomous: true
requirements:
  - RESUME-01
  - RESUME-02

must_haves:
  truths:
    - "Modal resume menampilkan teks sederhana 'Lanjutkan' tanpa nomor soal"
    - "Klik Lanjutkan di modal resume selalu ke halaman 1 (page 0)"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Modal resume yang disederhanakan + redirect ke page 0"
      contains: "Anda memiliki ujian yang belum selesai"
  key_links:
    - from: "resumeConfirmBtn click handler"
      to: "currentPage = 0"
      via: "event listener"
      pattern: "currentPage = 0"
---

<objective>
Sederhanakan modal resume exam dan ubah redirect agar selalu ke halaman 1.

Purpose: User menginginkan modal resume yang simpel tanpa detail nomor soal, dan resume selalu mulai dari page 1 agar tidak membingungkan.
Output: StartExam.cshtml yang sudah diperbarui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/270-perbaiki-resume-exam-notif-lanjutkan-mengerjakan-dan-redirect-ke-page-1/270-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sederhanakan modal resume dan ubah redirect ke page 0</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - Views/CMP/StartExam.cshtml (full file — modal di line ~182-202, redirect logic di line ~794-812)
  </read_first>
  <action>
Dua perubahan di Views/CMP/StartExam.cshtml (per D-01, D-02, D-03):

**1. Sederhanakan modal HTML (line ~182-202):**
- Ubah teks modal body dari `Lanjutkan dari soal no. <strong id="resumePageNum">--</strong>?` menjadi teks sederhana: `Anda memiliki ujian yang belum selesai. Klik tombol di bawah untuk melanjutkan.`
- Hapus elemen `<strong id="resumePageNum">--</strong>` — tidak diperlukan lagi
- Biarkan header, tombol "Lanjutkan", dan styling yang sudah ada tetap sama

**2. Ubah redirect logic di JavaScript (line ~794-812):**
- Hapus kalkulasi `resumeFromNum` (line 797-799): hapus variabel `resumeFromNum` dan baris `answeredQuestions.size + 1` / fallback
- Hapus baris `document.getElementById('resumePageNum').innerText = resumeFromNum;` (line 800)
- Di dalam `resumeConfirmBtn` click handler (line 804-812): ubah `currentPage = RESUME_PAGE;` menjadi `currentPage = 0;`
- Hapus baris `document.getElementById('page_0').style.display = 'none';` (line 807) karena page_0 sudah ditampilkan dan kita tetap di page 0
- Ubah kondisi `if (IS_RESUME && RESUME_PAGE > 0)` (line 794) menjadi `if (IS_RESUME)` saja — modal harus tampil untuk semua resume, bukan hanya non-first-page

Hasil click handler setelah perubahan:
```javascript
document.getElementById('resumeConfirmBtn').addEventListener('click', function() {
    resumeModal.hide();
    currentPage = 0;
    document.getElementById('page_' + currentPage).style.display = 'block';
    updatePanel();
    window.scrollTo(0, 0);
});
```
  </action>
  <verify>
    <automated>grep -n "resumePageNum" Views/CMP/StartExam.cshtml | wc -l</automated>
    Harus return 0 (tidak ada lagi referensi ke resumePageNum).
    Juga: `grep -n "currentPage = 0" Views/CMP/StartExam.cshtml` harus ada match di dalam click handler.
    Juga: `grep -n "resumeFromNum" Views/CMP/StartExam.cshtml` harus return 0.
  </verify>
  <acceptance_criteria>
    - Views/CMP/StartExam.cshtml TIDAK mengandung string "resumePageNum"
    - Views/CMP/StartExam.cshtml TIDAK mengandung string "resumeFromNum"
    - Views/CMP/StartExam.cshtml mengandung string "currentPage = 0" di dalam resumeConfirmBtn handler
    - Views/CMP/StartExam.cshtml mengandung string "Anda memiliki ujian yang belum selesai"
    - Views/CMP/StartExam.cshtml mengandung "if (IS_RESUME)" tanpa "&& RESUME_PAGE > 0"
  </acceptance_criteria>
  <done>Modal resume menampilkan teks sederhana tanpa nomor soal, dan klik Lanjutkan selalu ke halaman 1 (page 0)</done>
</task>

</tasks>

<verification>
1. `grep -c "resumePageNum" Views/CMP/StartExam.cshtml` returns 0
2. `grep -c "resumeFromNum" Views/CMP/StartExam.cshtml` returns 0
3. `grep "currentPage = 0" Views/CMP/StartExam.cshtml` returns match
4. `grep "Anda memiliki ujian yang belum selesai" Views/CMP/StartExam.cshtml` returns match
</verification>

<success_criteria>
Modal resume exam menampilkan teks sederhana "Anda memiliki ujian yang belum selesai" tanpa info nomor soal, dan klik Lanjutkan selalu redirect ke halaman 1 (page 0).
</success_criteria>

<output>
After completion, create `.planning/phases/270-perbaiki-resume-exam-notif-lanjutkan-mengerjakan-dan-redirect-ke-page-1/270-01-SUMMARY.md`
</output>
