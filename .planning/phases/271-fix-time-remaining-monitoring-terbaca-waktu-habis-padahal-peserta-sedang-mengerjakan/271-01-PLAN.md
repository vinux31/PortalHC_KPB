---
phase: 271-fix-time-remaining-monitoring-terbaca-waktu-habis-padahal-peserta-sedang-mengerjakan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
autonomous: false
requirements:
  - TIMER-01
  - TIMER-02

must_haves:
  truths:
    - "Saat resume setelah navigate away lama, timer menampilkan sisa waktu yang akurat berdasarkan wall-clock elapsed sejak StartedAt"
    - "Timer tidak pernah bertambah setelah resume — selalu berkurang atau tetap"
    - "ElapsedSeconds di DB selalu monotonically increasing dan tidak melebihi wall-clock elapsed"
    - "Jika durasi sudah habis saat resume, ExamExpired = true dan modal waktu habis muncul"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Server-authoritative timer: wall-clock cross-check di StartExam + clamp validation di UpdateSessionProgress"
      contains: "Math.Max(elapsedSec, wallClockElapsed)"
  key_links:
    - from: "Controllers/CMPController.cs (StartExam)"
      to: "ViewBag.RemainingSeconds"
      via: "wall-clock cross-check dengan DateTime.UtcNow - StartedAt"
      pattern: "Math\\.Max\\(elapsedSec, wallClockElapsed\\)"
    - from: "Controllers/CMPController.cs (UpdateSessionProgress)"
      to: "AssessmentSessions.ElapsedSeconds"
      via: "3-step clamp: min(client, wallClock), max(clamped, dbCurrent), min(clamped, duration)"
      pattern: "clampedElapsed"
---

<objective>
Fix dua bug timer ujian yang berakar pada satu root cause: `ElapsedSeconds` di database bisa stale saat worker navigate away.

Purpose: Timer resume harus akurat — tidak boleh tiba-tiba habis (bug 1) atau malah bertambah (bug 2) setelah worker kembali ke exam.
Output: Server-authoritative timer logic di CMPController.cs — cross-check wall-clock di StartExam + clamp validation di UpdateSessionProgress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/271-fix-time-remaining-monitoring-terbaca-waktu-habis-padahal-peserta-sedang-mengerjakan/271-CONTEXT.md
@.planning/phases/271-fix-time-remaining-monitoring-terbaca-waktu-habis-padahal-peserta-sedang-mengerjakan/271-RESEARCH.md

@Controllers/CMPController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-authoritative timer di StartExam + clamp di UpdateSessionProgress</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (lines 900-920 untuk StartExam resume state, lines 326-364 untuk UpdateSessionProgress)
    - .planning/phases/271-fix-time-remaining-monitoring-terbaca-waktu-habis-padahal-peserta-sedang-mengerjakan/271-RESEARCH.md (exact code examples)
  </read_first>
  <action>
**Fix 1 — StartExam resume calculation (sekitar baris 908-917, per D-06, D-07):**

Ganti blok:
```csharp
bool isResume = assessment.StartedAt != null;
int durationSeconds = assessment.DurationMinutes * 60;
int elapsedSec = assessment.ElapsedSeconds;
int remainingSeconds = durationSeconds - elapsedSec;
```

Menjadi:
```csharp
bool isResume = assessment.StartedAt != null;
int durationSeconds = assessment.DurationMinutes * 60;
int elapsedSec = assessment.ElapsedSeconds;

// Server-authoritative: cross-check DB elapsed dengan wall-clock elapsed sejak StartedAt
// Fix untuk bug "waktu habis mendadak" dan "timer bertambah saat resume"
if (!justStarted && assessment.StartedAt.HasValue)
{
    int wallClockElapsed = (int)(DateTime.UtcNow - assessment.StartedAt.Value).TotalSeconds;
    elapsedSec = Math.Max(elapsedSec, wallClockElapsed);
}

// Defensive clamp — tidak boleh melebihi durasi total
elapsedSec = Math.Min(elapsedSec, durationSeconds);

int remainingSeconds = durationSeconds - elapsedSec;
```

Pastikan `ViewBag.ElapsedSeconds = elapsedSec;` tetap menggunakan nilai yang sudah di-cross-check (sudah ada di baris 915).

**Fix 2 — UpdateSessionProgress clamp validation (sekitar baris 351-358, per D-08):**

Tambahkan clamp logic SEBELUM `ExecuteUpdateAsync`. Perlu baca `session.StartedAt`, `session.ElapsedSeconds`, dan `session.DurationMinutes` dari object `session` yang sudah di-fetch di baris 337.

Ganti:
```csharp
var updated = await _context.AssessmentSessions
    .Where(s => s.Id == sessionId)
    .ExecuteUpdateAsync(s => s
        .SetProperty(r => r.ElapsedSeconds, elapsedSeconds)
        .SetProperty(r => r.LastActivePage, currentPage)
        .SetProperty(r => r.UpdatedAt, DateTime.UtcNow)
    );
```

Menjadi:
```csharp
// Clamp elapsedSeconds dari client (per D-08)
int clampedElapsed = elapsedSeconds;

// Clamp 1: tidak boleh melebihi wall-clock elapsed sejak StartedAt
if (session.StartedAt.HasValue)
{
    int wallClockElapsed = (int)(DateTime.UtcNow - session.StartedAt.Value).TotalSeconds;
    clampedElapsed = Math.Min(clampedElapsed, wallClockElapsed);
}

// Clamp 2: tidak boleh mundur (monotonically increasing)
clampedElapsed = Math.Max(clampedElapsed, session.ElapsedSeconds);

// Clamp 3: tidak boleh melebihi durasi total
clampedElapsed = Math.Min(clampedElapsed, session.DurationMinutes * 60);

var updated = await _context.AssessmentSessions
    .Where(s => s.Id == sessionId)
    .ExecuteUpdateAsync(s => s
        .SetProperty(r => r.ElapsedSeconds, clampedElapsed)
        .SetProperty(r => r.LastActivePage, currentPage)
        .SetProperty(r => r.UpdatedAt, DateTime.UtcNow)
    );
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - CMPController.cs contains `Math.Max(elapsedSec, wallClockElapsed)` in StartExam section
    - CMPController.cs contains `int clampedElapsed = elapsedSeconds;` in UpdateSessionProgress
    - CMPController.cs contains `Math.Min(clampedElapsed, wallClockElapsed)` in UpdateSessionProgress
    - CMPController.cs contains `Math.Max(clampedElapsed, session.ElapsedSeconds)` in UpdateSessionProgress
    - CMPController.cs contains `Math.Min(clampedElapsed, session.DurationMinutes * 60)` in UpdateSessionProgress
    - CMPController.cs contains `.SetProperty(r => r.ElapsedSeconds, clampedElapsed)` (not raw `elapsedSeconds`)
    - `dotnet build` exits 0 with no errors
  </acceptance_criteria>
  <done>Server menghitung remainingSeconds menggunakan max(DB elapsed, wall-clock elapsed), dan UpdateSessionProgress mem-clamp nilai dari client agar monotonically increasing dan tidak melebihi wall-clock. Build sukses tanpa error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifikasi manual timer di browser</name>
  <files>Controllers/CMPController.cs</files>
  <action>Checkpoint — tidak ada kode yang diubah. User memverifikasi hasil Task 1 di browser server dev.</action>
  <what-built>Server-authoritative timer: cross-check wall-clock di StartExam + clamp validation di UpdateSessionProgress. Kedua bug (waktu habis mendadak saat resume + timer bertambah) seharusnya teratasi.</what-built>
  <how-to-verify>
    Deploy ke server dev lalu test:

    **Test BUG-01 (waktu habis mendadak saat resume):**
    1. Login sebagai worker, mulai ujian
    2. Kerjakan beberapa soal, catat timer (misal 24:00)
    3. Navigate ke halaman lain (misal Home) — BUKAN close tab
    4. Tunggu minimal 1-2 menit
    5. Kembali ke ujian (resume)
    6. EXPECTED: Timer menunjukkan sisa waktu yang akurat (misal ~22:00 jika 2 menit berlalu), BUKAN "waktu habis"

    **Test BUG-02 (timer bertambah saat resume):**
    1. Saat di ujian, catat timer (misal 23:30)
    2. Navigate away ke halaman lain
    3. Tunggu 30 detik
    4. Resume ujian
    5. EXPECTED: Timer lebih kecil dari 23:30 (misal ~23:00), BUKAN lebih besar

    **Test EDGE-01 (expired saat resume):**
    1. Buat assessment dengan durasi 1 menit
    2. Mulai ujian, navigate away
    3. Tunggu > 1 menit
    4. Resume
    5. EXPECTED: Modal "Waktu Habis" muncul
  </how-to-verify>
  <verify>User confirms all 3 tests pass in browser</verify>
  <acceptance_criteria>
    - BUG-01: Timer resume menunjukkan sisa waktu akurat setelah navigate away 1-2 menit
    - BUG-02: Timer tidak bertambah setelah resume (selalu lebih kecil atau sama)
    - EDGE-01: Modal "Waktu Habis" muncul saat resume setelah durasi habis
  </acceptance_criteria>
  <done>User mengkonfirmasi ketiga test scenario pass di browser server dev.</done>
  <resume-signal>Ketik "approved" jika semua test pass, atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- Manual browser test: resume timer akurat, tidak bertambah, expired terdeteksi
</verification>

<success_criteria>
- Timer resume menampilkan sisa waktu berdasarkan wall-clock elapsed (bukan DB stale)
- Timer tidak pernah bertambah setelah resume
- ElapsedSeconds di DB monotonically increasing via clamp
- ExamExpired = true jika durasi sudah habis saat resume
</success_criteria>

<output>
After completion, create `.planning/phases/271-fix-time-remaining-monitoring-terbaca-waktu-habis-padahal-peserta-sedang-mengerjakan/271-01-SUMMARY.md`
</output>
