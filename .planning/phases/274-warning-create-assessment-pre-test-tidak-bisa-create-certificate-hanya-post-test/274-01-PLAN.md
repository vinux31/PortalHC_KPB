---
phase: 274
plan: 1
title: "Hapus badge score dari sertifikat"
wave: 1
depends_on: []
files_modified:
  - Views/CMP/Certificate.cshtml
requirements_addressed: []
autonomous: true
---

# Plan 274-01: Hapus Badge Score dari Sertifikat

<objective>
Menghapus elemen visual badge score (lingkaran biru di pojok kanan bawah) dari tampilan sertifikat. Badge ini menampilkan score assessment dan harus dihilangkan sepenuhnya — baik CSS maupun HTML-nya.
</objective>

## Tasks

<task id="1">
<title>Hapus CSS dan HTML badge-score dari Certificate.cshtml</title>

<read_first>
- Views/CMP/Certificate.cshtml
</read_first>

<action>
1. Hapus CSS block `.badge-score` beserta sub-selectors (baris 183-205):
   - `.badge-score { ... }` (baris 183-198)
   - `.badge-score span { ... }` (baris 199-202)
   - `.badge-score strong { ... }` (baris 203-205)

2. Hapus HTML conditional render block (baris 298-304):
   ```razor
   @if(Model.Score.HasValue)
   {
       <div class="badge-score">
           <span>Score</span>
           <strong>@Model.Score%</strong>
       </div>
   }
   ```

Pastikan tidak ada blank lines berlebih setelah penghapusan.
</action>

<acceptance_criteria>
- Views/CMP/Certificate.cshtml TIDAK mengandung string `.badge-score`
- Views/CMP/Certificate.cshtml TIDAK mengandung string `Model.Score`
- Views/CMP/Certificate.cshtml TIDAK mengandung string `<span>Score</span>`
- File tetap valid Razor syntax (tidak ada unclosed tags)
- Sertifikat masih bisa di-render tanpa error
</acceptance_criteria>
</task>

## Verification

```bash
# Pastikan badge-score sudah tidak ada
grep -c "badge-score" Views/CMP/Certificate.cshtml
# Expected: 0

grep -c "Model.Score" Views/CMP/Certificate.cshtml
# Expected: 0
```

## must_haves
- Badge score tidak muncul di sertifikat
- Tidak ada CSS orphan `.badge-score` tersisa
- Tidak ada perubahan fungsional lain pada sertifikat
