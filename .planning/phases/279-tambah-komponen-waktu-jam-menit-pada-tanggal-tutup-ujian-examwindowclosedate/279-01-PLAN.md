---
phase: 279-tambah-komponen-waktu-jam-menit-pada-tanggal-tutup-ujian-examwindowclosedate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/EditAssessment.cshtml
  - Controllers/AdminController.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin dapat memilih tanggal DAN waktu tutup ujian saat create assessment"
    - "Admin dapat memilih tanggal DAN waktu tutup ujian saat edit assessment"
    - "ExamWindowCloseDate tersimpan dengan komponen waktu yang benar (bukan 00:00)"
    - "Field ExamWindowCloseDate wajib diisi — form tidak bisa submit tanpa isi"
  artifacts:
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "Date+time+hidden combiner untuk ExamWindowCloseDate"
      contains: "ewcdHidden"
    - path: "Views/Admin/EditAssessment.cshtml"
      provides: "Date+time+hidden combiner + populate dari model"
      contains: "ewcdHidden"
    - path: "Controllers/AdminController.cs"
      provides: "Tidak ada ModelState.Remove untuk ExamWindowCloseDate"
  key_links:
    - from: "Views/Admin/CreateAssessment.cshtml"
      to: "Controllers/AdminController.cs"
      via: "hidden input ewcdHidden bound to ExamWindowCloseDate"
      pattern: "ewcdHidden"
    - from: "Views/Admin/EditAssessment.cshtml"
      to: "Controllers/AdminController.cs"
      via: "hidden input ewcdHidden bound to ExamWindowCloseDate"
      pattern: "ewcdHidden"
---

<objective>
Tambahkan komponen waktu (jam:menit) pada field ExamWindowCloseDate di CreateAssessment dan EditAssessment, mengikuti pola Schedule combiner yang sudah ada. Field berubah dari opsional menjadi wajib.

Purpose: Admin perlu mengatur batas waktu tutup ujian secara presisi (bukan hanya tanggal).
Output: 3 file diubah — 2 view + 1 controller.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/279-tambah-komponen-waktu-jam-menit-pada-tanggal-tutup-ujian-examwindowclosedate/279-CONTEXT.md
@.planning/phases/279-tambah-komponen-waktu-jam-menit-pada-tanggal-tutup-ujian-examwindowclosedate/279-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah date+time combiner di CreateAssessment dan step validation</name>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml (current state — lihat pola Schedule di line ~329-344, step validation di ~757, form submit combiner di ~1133-1140, dan ExamWindowCloseDate input di ~398-405)
  </read_first>
  <action>
1. Ganti single `<input type="date" asp-for="ExamWindowCloseDate">` (sekitar line 398-405) dengan 3 input per D-01/D-02/D-03:
   - `<input type="date" id="ewcdDateInput" class="form-control" required />` dengan label "Tanggal Tutup Ujian"
   - `<input type="time" id="ewcdTimeInput" class="form-control" value="23:59" required />` dengan label "Waktu Tutup Ujian" (per D-04: default 23:59)
   - `<input type="hidden" asp-for="ExamWindowCloseDate" id="ewcdHidden" />`
   Ikuti layout HTML yang sama persis dengan Schedule combiner (date di kiri, time di kanan, hidden di bawah).

2. Tambah JavaScript combiner di form submit handler (setelah schedule combiner, sekitar line ~1133-1140):
   ```javascript
   var ewcdDateInput = document.getElementById('ewcdDateInput');
   var ewcdTimeInput = document.getElementById('ewcdTimeInput');
   var ewcdHidden = document.getElementById('ewcdHidden');
   if (ewcdDateInput && ewcdDateInput.value && ewcdTimeInput && ewcdHidden) {
       ewcdHidden.value = ewcdDateInput.value + 'T' + (ewcdTimeInput.value || '23:59') + ':00';
   }
   ```

3. Tambah validasi step 3 (sekitar line ~757, block `if (n === 3)`):
   - Check ewcdDateInput.value tidak kosong
   - Check ewcdTimeInput.value tidak kosong
   - Tampilkan invalid-feedback jika kosong, ikuti pola schedDate/schedTime validation

4. Jika ada summary panel (Step 4) yang menampilkan ExamWindowCloseDate, update tampilannya agar include waktu. Jika belum ada entry summary untuk ExamWindowCloseDate, tambahkan.

Per D-05/D-06: field wajib — gunakan `required` attribute pada kedua input.
  </action>
  <verify>
    <automated>grep -n "ewcdHidden" Views/Admin/CreateAssessment.cshtml | head -10</automated>
  </verify>
  <acceptance_criteria>
    - CreateAssessment.cshtml contains `id="ewcdDateInput"` with `required`
    - CreateAssessment.cshtml contains `id="ewcdTimeInput"` with `value="23:59"` and `required`
    - CreateAssessment.cshtml contains `id="ewcdHidden"` with `asp-for="ExamWindowCloseDate"`
    - CreateAssessment.cshtml contains `ewcdHidden.value = ewcdDateInput.value + 'T'` in JS combiner
    - Step 3 validation block references `ewcdDateInput` for required check
    - No single `type="date" asp-for="ExamWindowCloseDate"` input remains (replaced by combiner)
  </acceptance_criteria>
  <done>CreateAssessment memiliki date+time combiner untuk ExamWindowCloseDate dengan default 23:59, validasi step 3, dan JS combiner di form submit</done>
</task>

<task type="auto">
  <name>Task 2: Tambah date+time combiner di EditAssessment + hapus ModelState.Remove di controller</name>
  <files>Views/Admin/EditAssessment.cshtml, Controllers/AdminController.cs</files>
  <read_first>
    - Views/Admin/EditAssessment.cshtml (current state — lihat Schedule pattern di line ~209-225, form submit handlers di ~514-524 dan ~565-574, ExamWindowCloseDate input di ~280-288)
    - Controllers/AdminController.cs (cari `ModelState.Remove("ExamWindowCloseDate")` di sekitar line 1360-1361)
  </read_first>
  <action>
1. Di EditAssessment.cshtml, ganti single `<input type="date" asp-for="ExamWindowCloseDate">` (sekitar line 280-288) dengan 3 input:
   ```html
   <input type="date" id="ewcdDateInput"
          value="@Model.ExamWindowCloseDate?.ToString("yyyy-MM-dd")"
          class="form-control" required />
   <input type="time" id="ewcdTimeInput"
          value="@(Model.ExamWindowCloseDate?.ToString("HH:mm") ?? "23:59")"
          class="form-control" required />
   <input asp-for="ExamWindowCloseDate" type="hidden" id="ewcdHidden"
          value="@Model.ExamWindowCloseDate?.ToString("yyyy-MM-ddTHH:mm:ss")" />
   ```
   Ikuti layout Schedule combiner (date kiri, time kanan). Null-safe: gunakan `?.ToString()` dan `?? "23:59"` per pitfall #2.

2. Tambah JS combiner di KEDUA form submit handlers (sekitar line ~514-524 dan ~565-574):
   ```javascript
   var ewcdDateInput = document.getElementById('ewcdDateInput');
   var ewcdTimeInput = document.getElementById('ewcdTimeInput');
   var ewcdHidden = document.getElementById('ewcdHidden');
   if (ewcdDateInput && ewcdDateInput.value && ewcdTimeInput && ewcdHidden) {
       ewcdHidden.value = ewcdDateInput.value + 'T' + (ewcdTimeInput.value || '23:59') + ':00';
   }
   ```
   PENTING: harus di KEDUA handler (pitfall #1 dari research).

3. Di Controllers/AdminController.cs, HAPUS baris `ModelState.Remove("ExamWindowCloseDate");` (sekitar line 1360-1361). Field sekarang wajib — tidak perlu di-remove dari ModelState per D-05.

4. JANGAN ubah tipe property di model (tetap `DateTime?` di AssessmentSession.cs) — biarkan nullable untuk backward compatibility data lama.
  </action>
  <verify>
    <automated>grep -n "ewcdHidden" Views/Admin/EditAssessment.cshtml | head -10 && grep -c "ModelState.Remove.*ExamWindowCloseDate" Controllers/AdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - EditAssessment.cshtml contains `id="ewcdDateInput"` with `required`
    - EditAssessment.cshtml contains `id="ewcdTimeInput"` with `?? "23:59"` and `required`
    - EditAssessment.cshtml contains `id="ewcdHidden"` with `asp-for="ExamWindowCloseDate"`
    - EditAssessment.cshtml contains ewcdHidden combiner JS in TWO separate locations (both form submit handlers)
    - AdminController.cs does NOT contain `ModelState.Remove("ExamWindowCloseDate")`
    - grep -c "ModelState.Remove.*ExamWindowCloseDate" Controllers/AdminController.cs returns 0
  </acceptance_criteria>
  <done>EditAssessment memiliki date+time combiner yang populate dari model value, kedua form submit handler punya combiner JS, dan ModelState.Remove sudah dihapus dari controller</done>
</task>

</tasks>

<verification>
1. `grep -c "ewcdHidden" Views/Admin/CreateAssessment.cshtml` — minimal 3 (HTML + JS)
2. `grep -c "ewcdHidden" Views/Admin/EditAssessment.cshtml` — minimal 4 (HTML + 2x JS handlers)
3. `grep -c "ModelState.Remove.*ExamWindowCloseDate" Controllers/AdminController.cs` — harus 0
4. `grep -c "ewcdDateInput" Views/Admin/CreateAssessment.cshtml` — minimal 2 (HTML + step validation)
5. Build: `dotnet build --no-restore` passes
</verification>

<success_criteria>
- CreateAssessment dan EditAssessment menampilkan 2 field (date + time) untuk ExamWindowCloseDate
- Default waktu 23:59 di kedua view
- Hidden combiner menggabungkan date+time sebelum form submit
- Field wajib (required) di frontend
- ModelState.Remove dihapus di backend
- Build sukses tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/279-tambah-komponen-waktu-jam-menit-pada-tanggal-tutup-ujian-examwindowclosedate/279-01-SUMMARY.md`
</output>
