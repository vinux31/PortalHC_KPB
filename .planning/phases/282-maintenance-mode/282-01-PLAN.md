---
phase: 282-maintenance-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/MaintenanceMode.cs
  - Data/ApplicationDbContext.cs
  - Middleware/MaintenanceModeMiddleware.cs
  - Program.cs
autonomous: true
requirements:
  - MAINT-03
  - MAINT-05
must_haves:
  truths:
    - "MaintenanceMode entity terdaftar di DbContext dan migration bisa dijalankan"
    - "Middleware intercept request non-admin/HC saat maintenance aktif"
    - "Middleware bypass static files, login page, maintenance page, dan SignalR hub"
    - "Middleware mendukung partial maintenance per modul"
  artifacts:
    - path: "Models/MaintenanceMode.cs"
      provides: "Entity model dengan IsEnabled, Message, EstimatedEndTime, Scope, audit fields"
      contains: "public class MaintenanceMode"
    - path: "Middleware/MaintenanceModeMiddleware.cs"
      provides: "Pipeline middleware yang redirect non-admin ke /Home/Maintenance"
      contains: "public class MaintenanceModeMiddleware"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet<MaintenanceMode> registration"
      contains: "DbSet<MaintenanceMode>"
  key_links:
    - from: "Middleware/MaintenanceModeMiddleware.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "EF Core query untuk MaintenanceMode state"
      pattern: "MaintenanceModes"
    - from: "Program.cs"
      to: "Middleware/MaintenanceModeMiddleware.cs"
      via: "app.UseMiddleware<MaintenanceModeMiddleware>()"
      pattern: "UseMiddleware.*MaintenanceMode"
---

<objective>
Backend foundation untuk maintenance mode: entity model, database migration, dan middleware yang intercept request.

Purpose: Menyediakan infrastruktur backend agar Plan 02 bisa langsung membangun UI di atasnya.
Output: MaintenanceMode model, DbContext registration, middleware terdaftar di pipeline.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/282-maintenance-mode/282-CONTEXT.md
@.planning/phases/282-maintenance-mode/282-RESEARCH.md

<interfaces>
From Services/AuditLogService.cs:
```csharp
public async Task LogAsync(
    string actorUserId, string actorName, string actionType,
    string description, int? targetId = null, string? targetType = null)
```

From Data/ApplicationDbContext.cs:
```csharp
// Pattern: public DbSet<EntityName> EntityNames { get; set; }
public DbSet<AuditLog> AuditLogs { get; set; }
```

From Program.cs (pipeline order, line 188-197):
```csharp
app.UseAuthentication();
app.UseAuthorization();
// Middleware HARUS di sini — setelah auth, sebelum MapControllerRoute
app.MapControllerRoute(...);
app.MapHub<AssessmentHub>("/hubs/assessment");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat MaintenanceMode entity dan registrasi DbContext + migration</name>
  <files>Models/MaintenanceMode.cs, Data/ApplicationDbContext.cs</files>
  <read_first>Data/ApplicationDbContext.cs, Models/AuditLog.cs</read_first>
  <action>
1. Buat file `Models/MaintenanceMode.cs` dengan class:

```csharp
namespace HcPortal.Models
{
    public class MaintenanceMode
    {
        public int Id { get; set; }
        public bool IsEnabled { get; set; }

        [Required]
        public string Message { get; set; } = "";

        public DateTime? EstimatedEndTime { get; set; }

        // "All" atau comma-separated module keys: "CMP,CDP,Proton,Admin,Home,Account"
        public string Scope { get; set; } = "All";

        public string? ActivatedByUserId { get; set; }
        public string? ActivatedByName { get; set; }
        public DateTime? ActivatedAt { get; set; }
        public DateTime? DeactivatedAt { get; set; }
    }
}
```

2. Di `Data/ApplicationDbContext.cs`, tambahkan DbSet setelah DbSet terakhir yang ada (setelah SessionElemenTeknisScores):
```csharp
public DbSet<MaintenanceMode> MaintenanceModes { get; set; }
```

3. Jalankan `dotnet ef migrations add AddMaintenanceMode` dan `dotnet ef database update`.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- File `Models/MaintenanceMode.cs` contains `public class MaintenanceMode`
- File `Models/MaintenanceMode.cs` contains `public bool IsEnabled`
- File `Models/MaintenanceMode.cs` contains `public string Scope { get; set; } = "All"`
- File `Data/ApplicationDbContext.cs` contains `DbSet<MaintenanceMode> MaintenanceModes`
- `dotnet build` exits 0
  </acceptance_criteria>
  <done>MaintenanceMode entity exists, DbContext registered, migration applied, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Buat MaintenanceModeMiddleware dan registrasi di Program.cs</name>
  <files>Middleware/MaintenanceModeMiddleware.cs, Program.cs</files>
  <read_first>Program.cs, Data/ApplicationDbContext.cs</read_first>
  <action>
1. Buat folder `Middleware/` jika belum ada. Buat file `Middleware/MaintenanceModeMiddleware.cs`:

```csharp
using HcPortal.Data;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Caching.Memory;

namespace HcPortal.Middleware
{
    public class MaintenanceModeMiddleware
    {
        private readonly RequestDelegate _next;
        private const string CacheKey = "MaintenanceMode_State";

        public MaintenanceModeMiddleware(RequestDelegate next)
        {
            _next = next;
        }

        public async Task InvokeAsync(HttpContext context, IMemoryCache cache, ApplicationDbContext db)
        {
            var path = context.Request.Path;

            // 1. Bypass static files
            if (path.StartsWithSegments("/css") || path.StartsWithSegments("/js") ||
                path.StartsWithSegments("/lib") || path.StartsWithSegments("/images") ||
                path.StartsWithSegments("/favicon.ico"))
            {
                await _next(context);
                return;
            }

            // 2. Bypass maintenance page itself (prevent redirect loop)
            if (path.StartsWithSegments("/Home/Maintenance"))
            {
                await _next(context);
                return;
            }

            // 3. Bypass login and access denied
            if (path.StartsWithSegments("/Account/Login") || path.StartsWithSegments("/Account/AccessDenied"))
            {
                await _next(context);
                return;
            }

            // 4. Bypass SignalR hubs
            if (path.StartsWithSegments("/hubs"))
            {
                await _next(context);
                return;
            }

            // 5. Admin + HC bypass
            if (context.User.IsInRole("Admin") || context.User.IsInRole("HC"))
            {
                await _next(context);
                return;
            }

            // 6. Check maintenance state (cached 5 min)
            var maintenance = await GetCachedState(cache, db);
            if (maintenance == null || !maintenance.IsEnabled)
            {
                await _next(context);
                return;
            }

            // 7. Check partial scope
            if (maintenance.Scope != "All")
            {
                var module = GetModuleFromPath(path);
                var modules = maintenance.Scope.Split(',', StringSplitOptions.RemoveEmptyEntries);
                if (!modules.Contains(module, StringComparer.OrdinalIgnoreCase))
                {
                    await _next(context); // Module ini tidak di-maintenance
                    return;
                }
            }

            // 8. AJAX request: return 503
            if (context.Request.Headers["X-Requested-With"] == "XMLHttpRequest")
            {
                context.Response.StatusCode = 503;
                context.Response.ContentType = "application/json";
                await context.Response.WriteAsync("{\"error\":\"Sistem sedang dalam pemeliharaan\"}");
                return;
            }

            // 9. Redirect ke maintenance page
            context.Response.Redirect("/Home/Maintenance");
        }

        private static async Task<HcPortal.Models.MaintenanceMode?> GetCachedState(
            IMemoryCache cache, ApplicationDbContext db)
        {
            if (!cache.TryGetValue(CacheKey, out HcPortal.Models.MaintenanceMode? state))
            {
                state = await db.MaintenanceModes.FirstOrDefaultAsync();
                if (state != null)
                {
                    cache.Set(CacheKey, state, TimeSpan.FromMinutes(5));
                }
            }
            return state;
        }

        private static string GetModuleFromPath(PathString path)
        {
            var segments = path.Value?.Split('/', StringSplitOptions.RemoveEmptyEntries);
            if (segments == null || segments.Length == 0) return "Home";

            return segments[0] switch
            {
                "CMP" => "CMP",
                "CDP" => "CDP",
                "ProtonData" => "Proton",
                "Admin" => "Admin",
                "Home" => "Home",
                "Account" => "Account",
                "Notification" => "Notification",
                _ => "Home"
            };
        }
    }
}
```

2. Di `Program.cs`, tambahkan `using HcPortal.Middleware;` di bagian atas. Kemudian sisipkan middleware SETELAH `app.UseAuthorization();` (line 190) dan SEBELUM `app.MapControllerRoute(...)` (line 193):

```csharp
app.UseAuthorization();
app.UseMiddleware<MaintenanceModeMiddleware>(); // <-- tambah baris ini
```
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- File `Middleware/MaintenanceModeMiddleware.cs` contains `public class MaintenanceModeMiddleware`
- File `Middleware/MaintenanceModeMiddleware.cs` contains `User.IsInRole("Admin") || context.User.IsInRole("HC")`
- File `Middleware/MaintenanceModeMiddleware.cs` contains `GetModuleFromPath`
- File `Middleware/MaintenanceModeMiddleware.cs` contains `StartsWithSegments("/Home/Maintenance")`
- File `Middleware/MaintenanceModeMiddleware.cs` contains `StartsWithSegments("/hubs")`
- File `Middleware/MaintenanceModeMiddleware.cs` contains `X-Requested-With`
- File `Program.cs` contains `UseMiddleware<MaintenanceModeMiddleware>()`
- `dotnet build` exits 0
  </acceptance_criteria>
  <done>Middleware terdaftar di pipeline, bypass Admin/HC/static/login/hubs, redirect non-admin ke /Home/Maintenance, support partial scope, handle AJAX 503.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes tanpa error
- File Models/MaintenanceMode.cs ada dengan semua property
- File Middleware/MaintenanceModeMiddleware.cs ada dengan semua bypass logic
- Program.cs contains UseMiddleware setelah UseAuthorization
</verification>

<success_criteria>
- Build sukses
- MaintenanceMode entity registered di DbContext
- Migration applied
- Middleware registered di pipeline di posisi yang benar (setelah UseAuthorization)
</success_criteria>

<output>
After completion, create `.planning/phases/282-maintenance-mode/282-01-SUMMARY.md`
</output>
