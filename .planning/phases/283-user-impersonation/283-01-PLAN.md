---
phase: 283-user-impersonation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Middleware/ImpersonationMiddleware.cs
  - Services/ImpersonationService.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [IMP-01, IMP-02, IMP-04, IMP-05, IMP-06, IMP-07, IMP-08]

must_haves:
  truths:
    - "Admin dapat memulai impersonation sebagai role HC atau User via POST endpoint"
    - "Admin dapat memulai impersonation sebagai user spesifik via POST endpoint"
    - "Admin dapat mencari user non-admin via API endpoint"
    - "Semua HTTP POST/PUT/DELETE diblokir saat impersonation aktif kecuali whitelist"
    - "Impersonation otomatis berakhir setelah 30 menit"
    - "Setiap start/stop impersonation tercatat di audit log"
    - "Admin tidak bisa impersonate admin lain"
  artifacts:
    - path: "Middleware/ImpersonationMiddleware.cs"
      provides: "Read-only enforcement + auto-expire"
      contains: "class ImpersonationMiddleware"
    - path: "Services/ImpersonationService.cs"
      provides: "Session management helper + constants"
      contains: "class ImpersonationService"
    - path: "Controllers/AdminController.cs"
      provides: "StartImpersonation, StopImpersonation, SearchUsersApi actions"
  key_links:
    - from: "Middleware/ImpersonationMiddleware.cs"
      to: "HttpContext.Session"
      via: "Session keys Impersonate_Mode, Impersonate_StartedAt"
      pattern: "GetString.*Impersonate_"
    - from: "Controllers/AdminController.cs"
      to: "Services/ImpersonationService.cs"
      via: "DI injection"
      pattern: "ImpersonationService"
    - from: "Middleware/ImpersonationMiddleware.cs"
      to: "HttpContext.Items"
      via: "Set IsImpersonating flag for views"
      pattern: 'Items\["IsImpersonating"\]'
---

<objective>
Backend impersonation: middleware read-only enforcement, session service, dan controller actions.

Purpose: Menyediakan seluruh infrastruktur backend agar admin bisa memulai/menghentikan impersonation, dengan semua write operations diblokir dan audit trail tercatat.
Output: ImpersonationMiddleware.cs, ImpersonationService.cs, 3 action baru di AdminController.cs
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/283-user-impersonation/283-CONTEXT.md
@.planning/phases/283-user-impersonation/283-RESEARCH.md

<interfaces>
<!-- Existing code contracts executor needs -->

From Middleware/MaintenanceModeMiddleware.cs:
```csharp
public class MaintenanceModeMiddleware
{
    private readonly RequestDelegate _next;
    public MaintenanceModeMiddleware(RequestDelegate next) { _next = next; }
    public async Task InvokeAsync(HttpContext context, IMemoryCache cache, ApplicationDbContext db)
    // Bypass patterns: static files, /hubs, /Account/Login, /Account/AccessDenied
    // Admin+HC bypass maintenance
}
```

From Services/AuditLogService.cs:
```csharp
public class AuditLogService
{
    public async Task LogAsync(string actorUserId, string actorName, string actionType, string description, int? targetId = null, string? targetType = null)
}
```

From Models/UserRoles.cs:
```csharp
public static class UserRoles
{
    public const string Admin = "Admin";
    public const string HC = "HC";
    // Level 1=Admin, 2=HC, 3=Management, 4=Section, 5=Coach, 6=Coachee
    public static int GetRoleLevel(string roleName) // Admin=1, HC=2, etc.
    public static bool HasFullAccess(int level) => level <= 3;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImpersonationService + ImpersonationMiddleware + DI Registration</name>
  <files>Services/ImpersonationService.cs, Middleware/ImpersonationMiddleware.cs, Program.cs</files>
  <read_first>
    - Middleware/MaintenanceModeMiddleware.cs (pattern untuk middleware bypass, static files, hubs)
    - Services/AuditLogService.cs (LogAsync signature)
    - Models/UserRoles.cs (GetRoleLevel, role constants)
    - Program.cs (middleware registration order, session config, DI registrations)
  </read_first>
  <action>
1. Buat `Services/ImpersonationService.cs`:
   - Static class `ImpersonationKeys` dengan constants: `Mode = "Impersonate_Mode"`, `TargetRole = "Impersonate_TargetRole"`, `TargetUserId = "Impersonate_TargetUserId"`, `TargetUserName = "Impersonate_TargetUserName"`, `StartedAt = "Impersonate_StartedAt"` (per D-01)
   - Class `ImpersonationService` dengan constructor injection `IHttpContextAccessor`
   - Method `bool IsImpersonating()` — cek session key Mode != null
   - Method `void StartRole(string role)` — set Mode="role", TargetRole=role, StartedAt=DateTime.UtcNow.Ticks.ToString()
   - Method `void StartUser(string userId, string userName)` — set Mode="user", TargetUserId=userId, TargetUserName=userName, StartedAt=DateTime.UtcNow.Ticks.ToString()
   - Method `void Stop()` — remove semua 5 session keys
   - Method `bool IsExpired()` — parse StartedAt, bandingkan dengan DateTime.UtcNow, return true jika > 30 menit (per D-09)
   - Method `string? GetDisplayName()` — return TargetUserName jika mode=user, "HC"/"User" jika mode=role
   - Method `string? GetMode()` — return session Mode value
   - Method `string? GetTargetRole()` — return session TargetRole value
   - Method `string? GetTargetUserId()` — return session TargetUserId value

2. Buat `Middleware/ImpersonationMiddleware.cs` (mengikuti pattern MaintenanceModeMiddleware — per D-04):
   - Constructor: `RequestDelegate _next`
   - `InvokeAsync(HttpContext context)` — resolve ImpersonationService dari DI
   - Bypass jika tidak authenticated atau tidak impersonating — `await _next(context)` langsung
   - Bypass static files (sama pattern: /css, /js, /lib, /images, /favicon.ico)
   - Cek auto-expire: jika `IsExpired()`, panggil `Stop()`, set TempData "Sesi impersonation telah berakhir (batas 30 menit).", redirect ke `/Admin/Index`
   - Cek HTTP method: GET, HEAD → allow
   - POST/PUT/DELETE: cek whitelist. Whitelist paths (HashSet<string>, OrdinalIgnoreCase):
     - "/Account/Login", "/Account/Logout", "/Admin/StopImpersonation", "/Admin/SearchUsersApi"
   - Juga whitelist path yang StartsWithSegments "/hubs" (SignalR)
   - Blocked request: jika header X-Requested-With == "XMLHttpRequest", return JSON `{"error":"Aksi ini diblokir — Mode Read-Only aktif","readOnly":true}` dengan status 403. Jika bukan AJAX, set TempData error "Aksi tidak diizinkan dalam mode impersonation. Semua perubahan data diblokir." dan redirect ke referrer atau Home/Index
   - Set `HttpContext.Items["IsImpersonating"] = true`
   - Set `HttpContext.Items["ImpersonateMode"] = mode`
   - Set `HttpContext.Items["ImpersonateTargetName"] = displayName`
   - Set `HttpContext.Items["ImpersonateTargetRole"] = targetRole` (untuk navigation override per D-07)
   - Panggil `await _next(context)`

3. Di `Program.cs`:
   - Tambah DI: `builder.Services.AddScoped<ImpersonationService>();` (setelah AuditLogService registration)
   - Tambah middleware: `app.UseMiddleware<HcPortal.Middleware.ImpersonationMiddleware>();` SETELAH `app.UseMiddleware<HcPortal.Middleware.MaintenanceModeMiddleware>();`
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Services/ImpersonationService.cs contains `public static class ImpersonationKeys`
    - Services/ImpersonationService.cs contains `public const string Mode = "Impersonate_Mode"`
    - Services/ImpersonationService.cs contains `public bool IsExpired()`
    - Services/ImpersonationService.cs contains `public void StartRole(string role)`
    - Services/ImpersonationService.cs contains `public void StartUser(string userId, string userName)`
    - Services/ImpersonationService.cs contains `public void Stop()`
    - Middleware/ImpersonationMiddleware.cs contains `class ImpersonationMiddleware`
    - Middleware/ImpersonationMiddleware.cs contains `"/Admin/StopImpersonation"`
    - Middleware/ImpersonationMiddleware.cs contains `Items["IsImpersonating"] = true`
    - Middleware/ImpersonationMiddleware.cs contains `StartsWithSegments("/hubs")`
    - Middleware/ImpersonationMiddleware.cs contains `403`
    - Middleware/ImpersonationMiddleware.cs contains `readOnly`
    - Program.cs contains `ImpersonationMiddleware`
    - Program.cs contains `ImpersonationService`
    - dotnet build exits 0
  </acceptance_criteria>
  <done>ImpersonationService dan ImpersonationMiddleware terdaftar di DI dan pipeline, build sukses</done>
</task>

<task type="auto">
  <name>Task 2: Controller Actions — StartImpersonation, StopImpersonation, SearchUsersApi</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (existing actions pattern, DI fields, constructor)
    - Services/ImpersonationService.cs (baru dibuat Task 1 — methods dan keys)
    - Services/AuditLogService.cs (LogAsync signature)
    - Models/UserRoles.cs (GetRoleLevel — untuk validasi non-admin)
  </read_first>
  <action>
1. Di constructor AdminController, tambah parameter `ImpersonationService impersonationService` dan simpan ke field `private readonly ImpersonationService _impersonationService;`

2. Tambah action `StartImpersonation` (per D-01, D-02, D-03, D-08):
```csharp
[HttpPost]
[Authorize(Roles = "Admin")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> StartImpersonation(string mode, string? targetRole, string? targetUserId)
```
   - Validasi mode harus "role" atau "user", jika tidak → TempData error, redirect Admin/Index
   - Jika mode == "role": validasi targetRole harus "HC" atau non-admin (GetRoleLevel >= 2). Panggil `_impersonationService.StartRole(targetRole)`
   - Jika mode == "user": cari user via `_userManager.FindByIdAsync(targetUserId)`. Validasi user exist dan bukan Admin (cek roles via `_userManager.GetRolesAsync(user)`, pastikan tidak mengandung "Admin" — per D-08/IMP-07). Panggil `_impersonationService.StartUser(targetUserId, user.FullName)`
   - Audit log via `_auditLog.LogAsync(currentUserId, currentName, "ImpersonateStart", $"Mulai impersonation sebagai {description}")` (per D-10/IMP-06)
   - Redirect ke Home/Index

3. Tambah action `StopImpersonation` (per D-11/IMP-08):
```csharp
[HttpPost]
[Authorize(Roles = "Admin")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> StopImpersonation()
```
   - Audit log "ImpersonateEnd" dengan deskripsi siapa yang di-impersonate (baca dari service sebelum stop)
   - Panggil `_impersonationService.Stop()`
   - TempData success "Sesi impersonation berakhir."
   - Redirect ke Admin/Index

4. Tambah action `SearchUsersApi` (per D-03/IMP-02):
```csharp
[HttpGet]
[Authorize(Roles = "Admin")]
public async Task<IActionResult> SearchUsersApi(string q)
```
   - Jika q null/empty atau length < 2, return `Json(new List<object>())`
   - Query `_userManager.Users.Where(u => u.FullName.Contains(q) || u.NIP.Contains(q))` — exclude admin: filter roles via subquery atau cek RoleLevel. Simplest: `.Where(u => u.RoleLevel >= 2)` jika ApplicationUser punya RoleLevel field. Jika tidak, filter setelah query via roles.
   - `.Take(10).Select(u => new { u.Id, u.FullName, u.NIP, u.SelectedView }).ToListAsync()`
   - Return `Json(users)`
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `private readonly ImpersonationService _impersonationService`
    - AdminController.cs contains `public async Task<IActionResult> StartImpersonation(string mode, string? targetRole, string? targetUserId)`
    - AdminController.cs contains `public async Task<IActionResult> StopImpersonation()`
    - AdminController.cs contains `public async Task<IActionResult> SearchUsersApi(string q)`
    - AdminController.cs contains `"ImpersonateStart"`
    - AdminController.cs contains `"ImpersonateEnd"`
    - AdminController.cs contains `GetRoleLevel` or `RoleLevel >= 2` (admin exclusion check)
    - AdminController.cs contains `[Authorize(Roles = "Admin")]` pada ketiga action baru
    - dotnet build exits 0
  </acceptance_criteria>
  <done>Tiga endpoint baru (StartImpersonation, StopImpersonation, SearchUsersApi) tersedia di AdminController, build sukses, validasi role + audit log terintegrasi</done>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- File ImpersonationMiddleware.cs, ImpersonationService.cs ada
- AdminController.cs punya 3 action baru
- Middleware terdaftar di Program.cs setelah MaintenanceModeMiddleware
</verification>

<success_criteria>
Backend impersonation lengkap: middleware blokir write, service manage session, controller handle start/stop/search. Build sukses.
</success_criteria>

<output>
After completion, create `.planning/phases/283-user-impersonation/283-01-SUMMARY.md`
</output>
