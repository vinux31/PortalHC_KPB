---
phase: 285-impersonation-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/Impersonate.cshtml
  - Views/Shared/_Layout.cshtml
autonomous: false
requirements:
  - IMP-UI-01
  - IMP-UI-02
  - IMP-UI-03
must_haves:
  truths:
    - "Admin melihat halaman /Admin/Impersonate dengan layout dua kolom"
    - "Kolom kiri berisi 2 card role (HC, User) dan info panel dengan history"
    - "Kolom kanan berisi search user dengan AJAX autocomplete"
    - "Navbar dropdown tidak lagi berisi kontrol impersonation, hanya link Impersonate"
    - "Klik card role atau hasil search menjalankan StartImpersonation yang sudah ada"
  artifacts:
    - path: "Views/Admin/Impersonate.cshtml"
      provides: "Halaman dedicated impersonation"
      min_lines: 80
    - path: "Controllers/AdminController.cs"
      provides: "Action Impersonate() GET + GetImpersonationHistory()"
      contains: "public async Task<IActionResult> Impersonate()"
  key_links:
    - from: "Views/Admin/Impersonate.cshtml"
      to: "/Admin/StartImpersonation"
      via: "form POST"
      pattern: "asp-action=\"StartImpersonation\""
    - from: "Views/Admin/Impersonate.cshtml"
      to: "/Admin/SearchUsersApi"
      via: "fetch AJAX"
      pattern: "SearchUsersApi"
    - from: "Views/Shared/_Layout.cshtml"
      to: "/Admin/Impersonate"
      via: "navbar link"
      pattern: "asp-action=\"Impersonate\""
---

<objective>
Buat halaman dedicated `/Admin/Impersonate` dengan layout dua kolom (role cards + info panel | search user), lalu cleanup navbar dropdown agar hanya berisi satu link ke halaman baru.

Purpose: UX improvement — kontrol impersonation pindah dari dropdown sempit ke halaman lega dengan info panel dan history.
Output: View baru Impersonate.cshtml, action GET baru di AdminController, _Layout.cshtml ter-cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/285-impersonation-page/285-CONTEXT.md
@.planning/phases/285-impersonation-page/285-RESEARCH.md

@Controllers/AdminController.cs
@Views/Shared/_Layout.cshtml
@Models/AuditLog.cs

<interfaces>
<!-- Existing actions di AdminController yang akan di-reuse -->
From Controllers/AdminController.cs:
- StartImpersonation(string mode, string targetRole, string targetUserId) — POST, handles role/user impersonation
- StopImpersonation() — POST, ends impersonation session
- SearchUsersApi(string q) — GET, returns JSON [{id, fullName, nip, selectedView}]

From Models/AuditLog.cs:
- AuditLog { Id, UserId, UserName, ActionType, Description, CreatedAt }
- ActionType values: "ImpersonateStart", "ImpersonateEnd"

From Views/Shared/_Layout.cshtml:
- Variable `isImpersonating` sudah ada (bool dari ViewData atau session check)
- Lines 159-188: impersonation controls dalam dropdown (AKAN DIHAPUS)
- Lines 282-325: JS autocomplete impersonation (AKAN DIHAPUS)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Action Impersonate() GET + View Impersonate.cshtml</name>
  <files>Controllers/AdminController.cs, Views/Admin/Impersonate.cshtml</files>
  <read_first>
    - Controllers/AdminController.cs (cari StartImpersonation ~line 8133, SearchUsersApi, dan AuditLog query patterns)
    - Views/Shared/_Layout.cshtml (lines 159-188 untuk role card pattern, lines 282-325 untuk JS search pattern)
    - Models/AuditLog.cs (entity model)
  </read_first>
  <action>
1. Di AdminController.cs, tambah action baru `Impersonate()` GET dengan attribute `[Authorize(Roles = "Admin")]`:
   - Query `_context.AuditLogs` where `ActionType == "ImpersonateStart" || ActionType == "ImpersonateEnd"`, order by `CreatedAt` descending, take 10
   - Return `View(history)` — model type `List&lt;AuditLog&gt;`

2. Buat `Views/Admin/Impersonate.cshtml` per D-01/D-02/D-03/D-06/D-07:
   - `@model List&lt;PortalHC_KPB.Models.AuditLog&gt;`
   - Page title "Impersonate" dengan breadcrumb
   - Bootstrap row g-4, kolom kiri col-lg-5, kolom kanan col-lg-7
   - **Kolom kiri** (per D-02):
     - Card "View As HC": icon `bi-person-badge`, warna outline-primary, form POST ke `StartImpersonation` dengan hidden inputs `mode=role`, `targetRole=HC`, include AntiForgeryToken
     - Card "View As User": icon `bi-person`, warna outline-success, form POST ke `StartImpersonation` dengan hidden inputs `mode=role`, `targetRole=User`, include AntiForgeryToken
     - **Info panel** (per D-06): card dengan 2 section:
       - Section 1: alert-info dengan aturan: read-only mode, max 30 menit, tidak bisa impersonate admin
       - Section 2: tabel history dari @Model — kolom: Admin, Target, Waktu, Aksi (ImpersonateStart/End) — format CreatedAt dengan `ToString("dd MMM yyyy HH:mm")`
   - **Kolom kanan** (per D-03):
     - Card dengan search input `id="imp-search"`, placeholder "Ketik nama atau NIP..."
     - Div `id="imp-results"` untuk AJAX results, `style="max-height:400px; overflow-y:auto;"`
   - **@section Scripts**: Copy JS pattern dari _Layout.cshtml lines 282-325, adaptasi:
     - Ganti `getElementById('impersonate-search')` → `getElementById('imp-search')`
     - Ganti `getElementById('impersonate-results')` → `getElementById('imp-results')`
     - Fetch URL tetap `/Admin/SearchUsersApi?q=...`
     - Click handler sama: buat form POST ke `/Admin/StartImpersonation` dengan `mode=user` + `targetUserId`
     - Token ambil dari `document.querySelector('input[name=__RequestVerificationToken]').value`
  </action>
  <verify>
    <automated>grep -n "public async Task.IActionResult. Impersonate()" Controllers/AdminController.cs &amp;&amp; test -f Views/Admin/Impersonate.cshtml &amp;&amp; grep -c "StartImpersonation" Views/Admin/Impersonate.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AdminController.cs contains `public async Task<IActionResult> Impersonate()`
    - Controllers/AdminController.cs contains `[Authorize(Roles = "Admin")]` sebelum action Impersonate
    - Controllers/AdminController.cs contains `ActionType == "ImpersonateStart"` dalam query history
    - Views/Admin/Impersonate.cshtml exists dan berisi `@model List<PortalHC_KPB.Models.AuditLog>`
    - Views/Admin/Impersonate.cshtml contains `col-lg-5` dan `col-lg-7` (two column layout)
    - Views/Admin/Impersonate.cshtml contains `targetRole" value="HC"` dan `targetRole" value="User"`
    - Views/Admin/Impersonate.cshtml contains `@Html.AntiForgeryToken()` (minimal 2 kali untuk 2 form role)
    - Views/Admin/Impersonate.cshtml contains `SearchUsersApi` dalam script section
    - Views/Admin/Impersonate.cshtml contains `imp-search` dan `imp-results` element IDs
  </acceptance_criteria>
  <done>Halaman /Admin/Impersonate render dengan layout dua kolom, role cards POST ke StartImpersonation, search AJAX ke SearchUsersApi, info panel tampil history dari AuditLog</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup _Layout.cshtml — hapus impersonation controls, tambah link</name>
  <files>Views/Shared/_Layout.cshtml</files>
  <read_first>
    - Views/Shared/_Layout.cshtml (full file — perhatikan lines 159-188 impersonation controls dan lines 282-325 JS autocomplete)
  </read_first>
  <action>
Per D-08/D-09/D-10/D-11:

1. **Hapus lines 159-188** (atau section yang berisi "Lihat Sebagai" header, 2 form StartImpersonation role, "Impersonate User" header, dan search input+results div). Identifikasi section dengan marker: dimulai dari `@if (User.IsInRole("Admin") &amp;&amp; !isImpersonating)` yang berisi dropdown-header "Lihat Sebagai".

2. **Ganti dengan link tunggal** (per D-11):
```html
@if (User.IsInRole("Admin") &amp;&amp; !isImpersonating)
{
    &lt;li&gt;&lt;hr class="dropdown-divider"&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a class="dropdown-item" asp-controller="Admin" asp-action="Impersonate"&gt;
        &lt;i class="bi bi-incognito me-2"&gt;&lt;/i&gt; Impersonate&lt;/a&gt;&lt;/li&gt;
}
```

3. **Hapus lines 282-325** — blok JS `// Impersonation user search autocomplete` IIFE. Ini dimulai dari komentar `// Impersonation user search autocomplete` dan berakhir di `})();` sebelum `// Read-only mode: disable submit buttons`.

4. **Pertahankan** semua kode lain: `_ImpersonationBanner` partial, read-only JS (line 327+), `isImpersonating` variable declaration.
  </action>
  <verify>
    <automated>grep -c "impersonate-search" Views/Shared/_Layout.cshtml | grep "^0$" &amp;&amp; grep -c "Impersonate User" Views/Shared/_Layout.cshtml | grep "^0$" &amp;&amp; grep "asp-action=\"Impersonate\"" Views/Shared/_Layout.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Views/Shared/_Layout.cshtml does NOT contain `impersonate-search` (search input removed)
    - Views/Shared/_Layout.cshtml does NOT contain `impersonate-results` (results div removed)
    - Views/Shared/_Layout.cshtml does NOT contain `Impersonate User` (dropdown header removed)
    - Views/Shared/_Layout.cshtml does NOT contain `Lihat Sebagai` (dropdown header removed)
    - Views/Shared/_Layout.cshtml does NOT contain `SearchUsersApi` (JS autocomplete removed)
    - Views/Shared/_Layout.cshtml contains `asp-action="Impersonate"` (new link to dedicated page)
    - Views/Shared/_Layout.cshtml contains `bi-incognito` (icon on new link)
    - Views/Shared/_Layout.cshtml still contains `isImpersonating` (variable declaration preserved)
    - Views/Shared/_Layout.cshtml still contains `_ImpersonationBanner` (banner partial preserved)
    - Views/Shared/_Layout.cshtml still contains `data-impersonating` (read-only JS preserved)
  </acceptance_criteria>
  <done>Navbar dropdown hanya berisi link "Impersonate" ke halaman dedicated, semua inline controls dan JS autocomplete terhapus, banner dan read-only JS tetap utuh</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi halaman Impersonate dan navbar cleanup</name>
  <files>Views/Admin/Impersonate.cshtml, Views/Shared/_Layout.cshtml</files>
  <action>Human verification — jalankan aplikasi dan test secara manual di browser sesuai langkah verifikasi.</action>
  <verify>
    <automated>dotnet build --no-restore -q</automated>
  </verify>
  <acceptance_criteria>
    - User mengkonfirmasi navbar dropdown berisi link Impersonate (bukan inline controls)
    - User mengkonfirmasi halaman /Admin/Impersonate render dengan layout dua kolom
    - User mengkonfirmasi role cards dan search user berfungsi untuk impersonation
  </acceptance_criteria>
  <done>User mengkonfirmasi semua 10 langkah verifikasi berhasil</done>
  <what-built>Halaman dedicated /Admin/Impersonate dengan layout dua kolom (role cards + info panel | search user) dan navbar dropdown yang sudah di-cleanup</what-built>
  <how-to-verify>
    1. Login sebagai Admin
    2. Klik profile dropdown di navbar — pastikan ada link "Impersonate" dengan icon incognito, TIDAK ada lagi "Lihat Sebagai" atau "Impersonate User" section
    3. Klik link "Impersonate" — harus masuk ke halaman /Admin/Impersonate
    4. Verifikasi layout dua kolom: kiri ada 2 card role (HC, User) + info panel, kanan ada search input
    5. Info panel: ada penjelasan aturan + tabel history (mungkin kosong jika belum pernah impersonate)
    6. Klik card "View As HC" — harus impersonate sebagai HC (banner muncul)
    7. Klik "Kembali ke Admin" di banner
    8. Balik ke /Admin/Impersonate, coba search user di kolom kanan — ketik nama, lihat hasil AJAX
    9. Klik salah satu user dari hasil search — harus impersonate sebagai user tersebut
    10. Klik "Kembali ke Admin" di banner, verifikasi history di info panel bertambah
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` sukses tanpa error
- Halaman /Admin/Impersonate render dengan layout dua kolom
- Role cards POST ke StartImpersonation existing
- Search AJAX ke SearchUsersApi existing
- Navbar dropdown hanya berisi link Impersonate, bukan inline controls
- Banner impersonation dan read-only JS tetap berfungsi
</verification>

<success_criteria>
- Admin dapat mengakses /Admin/Impersonate dari navbar link
- Layout dua kolom: kiri role cards + info panel, kanan search user
- Semua impersonation tetap berfungsi via halaman baru
- Navbar dropdown bersih — hanya satu link, bukan controls inline
</success_criteria>

<output>
After completion, create `.planning/phases/285-impersonation-page/285-01-SUMMARY.md`
</output>
