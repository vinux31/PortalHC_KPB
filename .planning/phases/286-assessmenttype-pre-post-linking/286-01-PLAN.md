---
phase: 286-assessmenttype-pre-post-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminBaseController.cs
  - Controllers/AdminController.cs
autonomous: true
requirements:
  - BASE-01
  - BASE-02

must_haves:
  truths:
    - "AdminBaseController exists as abstract base class with 4 shared DI dependencies"
    - "AdminController inherits AdminBaseController and compiles without errors"
    - "All existing admin URLs still work (zero regression)"
  artifacts:
    - path: "Controllers/AdminBaseController.cs"
      provides: "Abstract base controller with shared DI and routing"
      contains: "public abstract class AdminBaseController"
    - path: "Controllers/AdminController.cs"
      provides: "Existing controller now inheriting base"
      contains: "AdminController : AdminBaseController"
  key_links:
    - from: "Controllers/AdminController.cs"
      to: "Controllers/AdminBaseController.cs"
      via: "class inheritance + base() constructor call"
      pattern: ": base\\(context, userManager, auditLog, env\\)"
---

<objective>
Membuat AdminBaseController sebagai abstract base class dengan 4 shared dependencies dan routing attributes, lalu mengubah AdminController untuk mewarisinya.

Purpose: Fondasi untuk pecah AdminController menjadi domain controllers di phase berikutnya (287-289)
Output: AdminBaseController.cs baru + AdminController.cs yang sudah inherit base
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/286-assessmenttype-pre-post-linking/286-CONTEXT.md
@.planning/phases/286-assessmenttype-pre-post-linking/286-RESEARCH.md

@Controllers/AdminController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat AdminBaseController dengan shared DI dan routing</name>
  <files>Controllers/AdminBaseController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (lines 1-58 — current constructor, usings, attributes)
  </read_first>
  <action>
Buat file baru `Controllers/AdminBaseController.cs` dengan isi persis berikut (per D-01, D-04, D-07, D-08):

```csharp
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Identity;
using Microsoft.AspNetCore.Authorization;
using HcPortal.Models;
using HcPortal.Data;
using HcPortal.Services;

namespace HcPortal.Controllers
{
    [Authorize]
    [Route("Admin")]
    [Route("Admin/[action]")]
    public abstract class AdminBaseController : Controller
    {
        protected readonly ApplicationDbContext _context;
        protected readonly UserManager<ApplicationUser> _userManager;
        protected readonly AuditLogService _auditLog;
        protected readonly IWebHostEnvironment _env;

        protected AdminBaseController(
            ApplicationDbContext context,
            UserManager<ApplicationUser> userManager,
            AuditLogService auditLog,
            IWebHostEnvironment env)
        {
            _context = context;
            _userManager = userManager;
            _auditLog = auditLog;
            _env = env;
        }
    }
}
```

Perhatikan:
- 4 dependencies saja per D-01: ApplicationDbContext, UserManager, AuditLogService, IWebHostEnvironment
- TIDAK ada helper methods per D-04
- `[Authorize]` class-level per D-08
- `[Route("Admin")]` + `[Route("Admin/[action]")]` per D-07
- Class `abstract` (discretion — tidak perlu instantiate langsung)
- Fields `protected readonly` agar child bisa akses
  </action>
  <verify>
    <automated>dotnet build C:/Users/Administrator/Desktop/PortalHC_KPB/HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File Controllers/AdminBaseController.cs exists
    - File contains `public abstract class AdminBaseController : Controller`
    - File contains `protected readonly ApplicationDbContext _context;`
    - File contains `protected readonly UserManager<ApplicationUser> _userManager;`
    - File contains `protected readonly AuditLogService _auditLog;`
    - File contains `protected readonly IWebHostEnvironment _env;`
    - File contains `[Authorize]`
    - File contains `[Route("Admin")]`
    - File contains `[Route("Admin/[action]")]`
    - File does NOT contain any method besides the constructor
    - `dotnet build` succeeds
  </acceptance_criteria>
  <done>AdminBaseController.cs ada dengan 4 protected DI fields, routing attributes, dan class-level Authorize</done>
</task>

<task type="auto">
  <name>Task 2: Ubah AdminController untuk inherit AdminBaseController</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminBaseController.cs (baru dibuat di Task 1)
    - Controllers/AdminController.cs (lines 1-58 — current state)
  </read_first>
  <action>
Modifikasi `Controllers/AdminController.cs`:

1. **Ubah class declaration** (line 20):
   - DARI: `public class AdminController : Controller`
   - MENJADI: `public class AdminController : AdminBaseController`

2. **Tambah route attributes** pada class (karena base abstract punya [Route] tapi child perlu juga untuk ASP.NET routing):
   - Tambahkan `[Route("Admin")]` dan `[Route("Admin/[action]")]` di atas class declaration AdminController juga (ASP.NET Core: attribute routes pada base class TIDAK otomatis inherited oleh child — child harus punya sendiri atau akan 404)

3. **Hapus 4 field declarations** yang sudah ada di base (lines 22-23, 26):
   - HAPUS: `private readonly ApplicationDbContext _context;`
   - HAPUS: `private readonly UserManager<ApplicationUser> _userManager;`
   - HAPUS: `private readonly AuditLogService _auditLog;`
   - HAPUS: `private readonly IWebHostEnvironment _env;`
   - TETAP: 7 field lainnya (_cache, _config, _logger, _notificationService, _hubContext, _workerDataService, _impersonationService)

4. **Ubah constructor** (lines 34-58):
   - Tetap terima semua 11 parameters
   - Tambahkan `: base(context, userManager, auditLog, env)` setelah parameter list
   - HAPUS 4 assignment lines yang sudah di-handle base: `_context = context;`, `_userManager = userManager;`, `_auditLog = auditLog;`, `_env = env;`
   - TETAP: 7 assignment lainnya

5. **Hapus duplicate `[Authorize]`** class-level attribute (line 19) — sudah di base class

6. **JANGAN ubah** apapun selain yang disebutkan di atas. Semua action methods, helper methods, regions, dan using statements tetap sama persis.

Constructor setelah perubahan:
```csharp
public AdminController(
    ApplicationDbContext context,
    UserManager<ApplicationUser> userManager,
    AuditLogService auditLog,
    IMemoryCache cache,
    IConfiguration config,
    IWebHostEnvironment env,
    ILogger<AdminController> logger,
    INotificationService notificationService,
    IHubContext<AssessmentHub> hubContext,
    IWorkerDataService workerDataService,
    ImpersonationService impersonationService)
    : base(context, userManager, auditLog, env)
{
    _cache = cache;
    _config = config;
    _logger = logger;
    _notificationService = notificationService;
    _hubContext = hubContext;
    _workerDataService = workerDataService;
    _impersonationService = impersonationService;
}
```
  </action>
  <verify>
    <automated>dotnet build C:/Users/Administrator/Desktop/PortalHC_KPB/HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs contains `public class AdminController : AdminBaseController`
    - AdminController.cs contains `: base(context, userManager, auditLog, env)`
    - AdminController.cs does NOT contain `private readonly ApplicationDbContext _context;`
    - AdminController.cs does NOT contain `private readonly UserManager<ApplicationUser> _userManager;`
    - AdminController.cs does NOT contain `private readonly AuditLogService _auditLog;`
    - AdminController.cs does NOT contain `private readonly IWebHostEnvironment _env;`
    - AdminController.cs DOES contain `private readonly IMemoryCache _cache;` (7 remaining fields)
    - AdminController.cs contains `[Route("Admin")]`
    - AdminController.cs contains `[Route("Admin/[action]")]`
    - Class-level `[Authorize]` appears ZERO times directly on AdminController (inherited from base) OR appears once (acceptable — redundant but not harmful)
    - `dotnet build` succeeds with 0 errors
    - Total line count of AdminController.cs is within 10 lines of original (only removed 4 fields + 4 assignments)
  </acceptance_criteria>
  <done>AdminController mewarisi AdminBaseController, 4 DI fields diambil dari base, build sukses tanpa error, semua action methods tidak berubah</done>
</task>

</tasks>

<verification>
1. `dotnet build` — harus sukses tanpa error
2. Grep: `AdminBaseController.cs` contains `public abstract class AdminBaseController : Controller`
3. Grep: `AdminController.cs` contains `AdminController : AdminBaseController`
4. Grep: `AdminController.cs` contains `: base(context, userManager, auditLog, env)`
5. Grep: `AdminController.cs` does NOT contain `private readonly ApplicationDbContext _context`
</verification>

<success_criteria>
- AdminBaseController.cs baru ada dengan 4 protected DI fields + [Authorize] + [Route("Admin")] + [Route("Admin/[action]")]
- AdminController.cs mewarisi AdminBaseController via `: base(...)` constructor call
- 4 field declarations dihapus dari AdminController (sekarang inherited dari base)
- `dotnet build` sukses tanpa error
- Zero regression: semua existing code di AdminController tidak berubah selain constructor/fields/inheritance
</success_criteria>

<output>
After completion, create `.planning/phases/286-assessmenttype-pre-post-linking/286-01-SUMMARY.md`
</output>
