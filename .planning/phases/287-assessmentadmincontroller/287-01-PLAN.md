---
phase: 287-assessmentadmincontroller
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [ASMT-01, ASMT-02, ASMT-03]

must_haves:
  truths:
    - "Semua URL assessment (/Admin/ManageAssessment, /Admin/CreateAssessment, /Admin/EditAssessment, /Admin/DeleteAssessment, /Admin/ManagePackages, dll) tetap bisa diakses"
    - "Assessment actions tidak ada lagi di AdminController"
    - "Authorization [Authorize(Roles = 'Admin, HC')] tetap sama di setiap action"
    - "Aplikasi build tanpa error"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Semua assessment actions"
      contains: "class AssessmentAdminController : AdminBaseController"
    - path: "Controllers/AdminController.cs"
      provides: "Non-assessment actions only"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs"
      to: "Controllers/AdminBaseController.cs"
      via: "inheritance"
      pattern: "class AssessmentAdminController : AdminBaseController"
    - from: "Controllers/AdminController.cs"
      to: "Controllers/AssessmentAdminController.cs"
      via: "cross-controller redirect"
      pattern: "RedirectToAction.*ManageAssessment.*controller.*Assessment"
---

<objective>
Ekstraksi semua assessment-related actions dari AdminController ke AssessmentAdminController baru.

Purpose: Isolasi domain assessment ke controller tersendiri sebagai bagian refactoring AdminController (8,146 baris).
Output: AssessmentAdminController.cs baru, AdminController.cs lebih kecil (~4,400 baris).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/287-assessmentadmincontroller/287-CONTEXT.md
@.planning/phases/286-assessmenttype-pre-post-linking/286-01-SUMMARY.md

<interfaces>
From Controllers/AdminBaseController.cs:
```csharp
[Authorize]
[Route("Admin")]
[Route("Admin/[action]")]
public abstract class AdminBaseController : Controller
{
    protected readonly ApplicationDbContext _context;
    protected readonly UserManager<ApplicationUser> _userManager;
    protected readonly AuditLogService _auditLog;
    protected readonly IWebHostEnvironment _env;

    protected AdminBaseController(
        ApplicationDbContext context,
        UserManager<ApplicationUser> userManager,
        AuditLogService auditLog,
        IWebHostEnvironment env) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat AssessmentAdminController dengan semua assessment actions</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AdminBaseController.cs (base class untuk inheritance)
    - Controllers/AdminController.cs (source — semua assessment code akan dipindahkan dari sini)
  </read_first>
  <action>
Buat file `Controllers/AssessmentAdminController.cs` dengan:

1. **Using statements** — salin dari AdminController, tambahkan yang dibutuhkan:
   ```
   using Microsoft.AspNetCore.Mvc;
   using Microsoft.AspNetCore.Identity;
   using Microsoft.AspNetCore.Authorization;
   using Microsoft.EntityFrameworkCore;
   using Microsoft.Extensions.Caching.Memory;
   using HcPortal.Models;
   using HcPortal.Data;
   using HcPortal.Services;
   using HcPortal.Hubs;
   using Microsoft.AspNetCore.SignalR;
   using ClosedXML.Excel;
   using System.Globalization;
   using HcPortal.Helpers;
   using QuestPDF.Fluent;
   using QuestPDF.Helpers;
   ```

2. **Class declaration** — duplikasi Route attributes (ASP.NET Core TIDAK inherit route attributes):
   ```csharp
   [Route("Admin")]
   [Route("Admin/[action]")]
   public class AssessmentAdminController : AdminBaseController
   ```
   NOTE: JANGAN tambahkan `[Authorize]` di class level — sudah inherited dari AdminBaseController.

3. **Constructor** — 6 additional DI di luar 4 base dependencies:
   ```csharp
   private readonly IMemoryCache _cache;
   private readonly ILogger<AssessmentAdminController> _logger;
   private readonly INotificationService _notificationService;
   private readonly IHubContext<AssessmentHub> _hubContext;
   private readonly IWorkerDataService _workerDataService;

   public AssessmentAdminController(
       ApplicationDbContext context,
       UserManager<ApplicationUser> userManager,
       AuditLogService auditLog,
       IWebHostEnvironment env,
       IMemoryCache cache,
       ILogger<AssessmentAdminController> logger,
       INotificationService notificationService,
       IHubContext<AssessmentHub> hubContext,
       IWorkerDataService workerDataService)
       : base(context, userManager, auditLog, env)
   {
       _cache = cache;
       _logger = logger;
       _notificationService = notificationService;
       _hubContext = hubContext;
       _workerDataService = workerDataService;
   }
   ```
   NOTE: `_config` (IConfiguration) TIDAK diperlukan — tidak dipakai oleh assessment actions.
   NOTE: `_env` (IWebHostEnvironment) diteruskan ke base meskipun assessment code tidak langsung memakainya — ini requirement base constructor.

4. **Pindahkan SEMUA assessment actions** dari AdminController — cut-paste exact, JANGAN ubah logic apapun:
   - Lines 632-3731: ManageAssessment, ManageCategories, AddCategory, EditCategory, DeleteCategory, ToggleCategoryActive, ExportCategoriesExcel, ExportCategoriesPdf, CreateAssessment (GET+POST), EditAssessment (GET+POST), DeleteAssessment, DeleteAssessmentGroup, RegenerateToken, AssessmentMonitoring, AssessmentMonitoringDetail, SubmitInterviewResults, ResetAssessment, AkhiriUjian, AkhiriSemuaUjian, GetAkhiriSemuaCounts, GradeFromSavedAnswers (private), ExportAssessmentResults, UserAssessmentHistory, AuditLog, ExportAuditLog, ReshufflePackage, ReshuffleAll
   - Lines 3575-3731: Helper Methods region (Shuffle, BuildCrossPackageAssignment)
   - Lines 6556-7114: Package Management region (ManagePackages, CreatePackage, DeletePackage, PreviewPackage, DownloadQuestionTemplate, ImportPackageQuestions)
   - Lines 7216-7283: Activity Log region (GetActivityLog)

5. **Authorization** — setiap action HARUS mempertahankan `[Authorize(Roles = "Admin, HC")]` attribute yang sama persis seperti di AdminController. JANGAN ubah role string apapun.

6. **View returns** — semua `return View(...)` calls tetap apa adanya. Views di `Views/Admin/` akan tetap ditemukan karena route prefix sama `/Admin/`.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AssessmentAdminController.cs exists
    - File contains `class AssessmentAdminController : AdminBaseController`
    - File contains `[Route("Admin")]`
    - File contains `[Route("Admin/[action]")]`
    - File does NOT contain class-level `[Authorize]` (inherited from base)
    - File contains `ManageAssessment` action method
    - File contains `CreateAssessment` action method
    - File contains `EditAssessment` action method
    - File contains `DeleteAssessment` action method
    - File contains `ManagePackages` action method
    - File contains `GetActivityLog` action method
    - File contains `Shuffle` private method
    - File contains `BuildCrossPackageAssignment` private method
    - File contains `GradeFromSavedAnswers` private method
    - File contains `ManageCategories` action method
    - File contains `ReshufflePackage` action method
    - File contains `UserAssessmentHistory` action method
    - File contains `ExportAssessmentResults` action method
    - All action methods have `[Authorize(Roles = "Admin, HC")]`
    - Constructor calls `base(context, userManager, auditLog, env)`
  </acceptance_criteria>
  <done>AssessmentAdminController.cs berisi semua assessment actions dengan DI, routing, dan authorization yang benar</done>
</task>

<task type="auto">
  <name>Task 2: Hapus assessment code dari AdminController + fix cross-controller redirects</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (current state — verify assessment code masih ada sebelum hapus)
    - Controllers/AssessmentAdminController.cs (verify assessment code sudah ada di sini)
  </read_first>
  <action>
1. **Hapus dari AdminController** semua code yang sudah dipindahkan ke AssessmentAdminController:
   - Lines 632-3731: Semua assessment actions + helper methods region
   - Lines 6556-7114: Package Management region
   - Lines 7216-7283: Activity Log region
   - Hapus juga comment lines terkait (e.g., `// Question Management region removed...`)

2. **Fix cross-controller redirects** — Training actions (masih di AdminController) melakukan redirect ke ManageAssessment yang sekarang ada di AssessmentAdminController. Update 8 redirect calls di training region (sekitar line 6157-6359 sebelum deletion):

   Ubah setiap:
   ```csharp
   return RedirectToAction("ManageAssessment", new { tab = "training" });
   ```
   Menjadi:
   ```csharp
   return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
   ```

   NOTE: Controller name parameter di RedirectToAction menggunakan nama class TANPA suffix "Controller" — jadi "AssessmentAdmin", bukan "AssessmentAdminController".

3. **Cleanup DI** — Setelah assessment code dihapus, cek apakah AdminController masih menggunakan:
   - `_hubContext` (IHubContext<AssessmentHub>) — HAPUS jika tidak dipakai lagi oleh non-assessment code
   - `_notificationService` — HAPUS jika tidak dipakai lagi
   - `_cache` (IMemoryCache) — cek apakah masih dipakai oleh non-assessment code
   - `_workerDataService` — cek apakah masih dipakai oleh non-assessment code (worker actions juga mungkin pakai)

   Untuk setiap DI yang tidak terpakai: hapus field declaration, hapus parameter dari constructor, dan hapus assignment di constructor body. Hapus juga using statement yang tidak terpakai lagi.

4. **Jangan ubah apapun selain** yang disebut di atas — tidak ada perubahan logic, routing, atau authorization pada action yang tersisa.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs does NOT contain `ManageAssessment` method
    - AdminController.cs does NOT contain `CreateAssessment` method
    - AdminController.cs does NOT contain `ManagePackages` method
    - AdminController.cs does NOT contain `GetActivityLog` method
    - AdminController.cs does NOT contain `Shuffle` method
    - AdminController.cs does NOT contain `BuildCrossPackageAssignment` method
    - AdminController.cs contains `RedirectToAction("ManageAssessment", "AssessmentAdmin"` (cross-controller redirect)
    - AdminController.cs still contains `ManageWorkers` method (not removed)
    - AdminController.cs still contains `CoachCoacheeMapping` method (not removed)
    - AdminController.cs still contains `Index` method (not removed)
    - `dotnet build` exits with 0 errors
  </acceptance_criteria>
  <done>AdminController tidak lagi berisi assessment code, cross-controller redirects sudah diupdate, build bersih tanpa error</done>
</task>

</tasks>

<verification>
1. `dotnet build` — zero errors
2. grep AssessmentAdminController untuk confirm class exists dan inherits AdminBaseController
3. grep ManageAssessment di AdminController — should return 0 matches (kecuali redirect)
4. grep `[Route("Admin")]` di AssessmentAdminController — harus ada
5. Semua URL assessment tetap sama karena route prefix `/Admin/` + `[action]` pattern identik
</verification>

<success_criteria>
- AssessmentAdminController.cs berisi semua assessment actions (~3,700 baris)
- AdminController.cs berkurang ~3,700 baris (dari ~8,146 ke ~4,400)
- `dotnet build` sukses tanpa error
- Semua URL /Admin/ManageAssessment, /Admin/CreateAssessment, dll tetap berfungsi
- Authorization [Authorize(Roles = "Admin, HC")] tetap identik
- Cross-controller redirects dari training actions sudah mengarah ke AssessmentAdmin controller
</success_criteria>

<output>
After completion, create `.planning/phases/287-assessmentadmincontroller/287-01-SUMMARY.md`
</output>
