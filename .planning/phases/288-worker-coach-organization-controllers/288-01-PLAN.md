---
phase: 288-worker-coach-organization-controllers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/WorkerController.cs
  - Controllers/CoachMappingController.cs
  - Controllers/AdminController.cs
autonomous: true
requirements: [WKR-01, WKR-02, CCM-01, CCM-02]

must_haves:
  truths:
    - "WorkerController berisi semua 11 action worker management"
    - "CoachMappingController berisi semua 15 action coach-coachee + 2 Proton helper methods"
    - "Actions sudah dihapus dari AdminController"
    - "Build berhasil tanpa error"
  artifacts:
    - path: "Controllers/WorkerController.cs"
      provides: "Worker management actions"
      contains: "ManageWorkers"
    - path: "Controllers/CoachMappingController.cs"
      provides: "Coach-coachee mapping actions + Proton helpers"
      contains: "AutoCreateProgressForAssignment"
  key_links:
    - from: "Controllers/WorkerController.cs"
      to: "AdminBaseController"
      via: "class inheritance"
      pattern: "class WorkerController : AdminBaseController"
    - from: "Controllers/CoachMappingController.cs"
      to: "AdminBaseController"
      via: "class inheritance"
      pattern: "class CoachMappingController : AdminBaseController"
---

<objective>
Ekstraksi WorkerController dan CoachMappingController dari AdminController.

Purpose: Isolasi domain worker management dan coach-coachee mapping ke controller tersendiri, mengikuti pattern Phase 287.
Output: Dua controller baru yang mewarisi AdminBaseController, actions dihapus dari AdminController.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/288-worker-coach-organization-controllers/288-CONTEXT.md
@.planning/phases/288-worker-coach-organization-controllers/288-RESEARCH.md

<interfaces>
From Controllers/AdminBaseController.cs:
```csharp
[Authorize]
[Route("Admin")]
[Route("Admin/[action]")]
public abstract class AdminBaseController : Controller
{
    protected readonly ApplicationDbContext _context;
    protected readonly UserManager<ApplicationUser> _userManager;
    protected readonly AuditLogService _auditLog;
    protected readonly IWebHostEnvironment _env;

    protected AdminBaseController(
        ApplicationDbContext context,
        UserManager<ApplicationUser> userManager,
        AuditLogService auditLog,
        IWebHostEnvironment env) { ... }

    protected static string MapKategori(string? raw, Dictionary<string, string>? rawToDisplayMap) { ... }
}
```

Model classes at end of AdminController.cs (line 4393-4413) — must move to CoachMappingController:
```csharp
public class CoachAssignRequest
{
    public string CoachId { get; set; } = "";
    public List<string> CoacheeIds { get; set; } = new();
    public int? ProtonTrackId { get; set; }
    public DateTime? StartDate { get; set; }
    public string? AssignmentSection { get; set; }
    public string? AssignmentUnit { get; set; }
    public bool ConfirmProgressionWarning { get; set; }
}

public class CoachEditRequest
{
    public int MappingId { get; set; }
    public string CoachId { get; set; } = "";
    public int? ProtonTrackId { get; set; }
    public DateTime? StartDate { get; set; }
    public string? AssignmentSection { get; set; }
    public string? AssignmentUnit { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat WorkerController dan CoachMappingController</name>
  <files>Controllers/WorkerController.cs, Controllers/CoachMappingController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (source — worker actions line 1682-2608, coach actions line 621-1681, proton helpers line 3460-3558, model classes line 4393-4413)
    - Controllers/AssessmentAdminController.cs (reference pattern — header, constructor, View overrides)
    - Controllers/AdminBaseController.cs (base class signature)
  </read_first>
  <action>
Buat 2 file controller baru mengikuti pattern AssessmentAdminController:

**WorkerController.cs:**
1. Inherit `AdminBaseController`, tambahkan `[Route("Admin")]` dan `[Route("Admin/[action]")]`
2. DI tambahan: `IConfiguration _config`, `ILogger<WorkerController> _logger`
3. Copy 4 View override methods persis dari AssessmentAdminController
4. Copy SEMUA action dari AdminController line 1682-2608:
   - ManageWorkers, ExportWorkers, CreateWorker (GET+POST), EditWorker (GET+POST), DeleteWorker, DeactivateWorker, ReactivateWorker, WorkerDetail, ImportWorkers (GET+POST), DownloadImportTemplate
5. Setiap action HARUS memiliki `[Authorize(Roles = "Admin, HC")]` persis seperti aslinya
6. Using statements: Microsoft.AspNetCore.Mvc, Microsoft.AspNetCore.Identity, Microsoft.AspNetCore.Authorization, Microsoft.EntityFrameworkCore, HcPortal.Models, HcPortal.Data, HcPortal.Services, ClosedXML.Excel, System.Globalization, HcPortal.Helpers, IConfiguration (tambahkan sesuai kebutuhan kode)

**CoachMappingController.cs:**
1. Inherit `AdminBaseController`, tambahkan `[Route("Admin")]` dan `[Route("Admin/[action]")]`
2. DI tambahan: `ILogger<CoachMappingController> _logger`, `INotificationService _notificationService`
3. Copy 4 View override methods persis dari AssessmentAdminController
4. Copy SEMUA action dari AdminController line 621-1681:
   - CoachCoacheeMapping, DownloadMappingImportTemplate, ImportCoachCoacheeMapping, CoachCoacheeMappingAssign, CoachCoacheeMappingEdit, CleanupCoachCoacheeMappingOrg, CoachCoacheeMappingGetSessionCount, CoachCoacheeMappingActiveAssignmentCount, CoachCoacheeMappingDeactivate, CoachCoacheeMappingReactivate, MarkMappingCompleted, CoachCoacheeMappingDeletePreview, CoachCoacheeMappingDelete, CoachCoacheeMappingExport, GetEligibleCoachees
5. Copy Proton Progress Helpers (line 3460-3558) per D-01: `AutoCreateProgressForAssignment()` dan `CleanupProgressForAssignment()` — sebagai private methods
6. Pindahkan `CoachAssignRequest` dan `CoachEditRequest` model classes (line 4393-4413) ke BAWAH file CoachMappingController.cs, di luar class tapi di dalam namespace HcPortal.Controllers (atau buat tanpa namespace seperti aslinya)
7. Setiap action HARUS memiliki `[Authorize(Roles = "Admin, HC")]` persis seperti aslinya
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/WorkerController.cs exists dan contains `class WorkerController : AdminBaseController`
    - Controllers/WorkerController.cs contains `public IActionResult ManageWorkers` atau `public async Task<IActionResult> ManageWorkers`
    - Controllers/WorkerController.cs contains `private readonly IConfiguration _config`
    - Controllers/WorkerController.cs contains `protected new ViewResult View()`
    - Controllers/CoachMappingController.cs exists dan contains `class CoachMappingController : AdminBaseController`
    - Controllers/CoachMappingController.cs contains `CoachCoacheeMapping`
    - Controllers/CoachMappingController.cs contains `AutoCreateProgressForAssignment`
    - Controllers/CoachMappingController.cs contains `private readonly INotificationService _notificationService`
    - Controllers/CoachMappingController.cs contains `class CoachAssignRequest`
    - Controllers/CoachMappingController.cs contains `class CoachEditRequest`
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>Kedua controller baru ada, berisi semua actions dari domain masing-masing, build berhasil</done>
</task>

<task type="auto">
  <name>Task 2: Hapus actions yang dipindahkan dari AdminController</name>
  <files>Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (current state — setelah Task 1, mungkin masih utuh karena Task 1 hanya membuat file baru)
    - Controllers/WorkerController.cs (verify actions sudah ada di sana)
    - Controllers/CoachMappingController.cs (verify actions sudah ada di sana)
  </read_first>
  <action>
Dari AdminController.cs, hapus:
1. Region coach-coachee mapping: line 621-1681 (semua action coach termasuk CoachCoacheeMapping sampai GetEligibleCoachees)
2. Region worker management: line 1682-2608 (semua action worker termasuk ManageWorkers sampai DownloadImportTemplate)
3. Region Proton Progress Helpers: line 3460-3558 (AutoCreateProgressForAssignment dan CleanupProgressForAssignment)
4. Model classes di akhir file: line 4393-4413 (CoachAssignRequest dan CoachEditRequest)

JANGAN hapus actions yang bukan domain worker/coach/org (training, document, renewal, dll — masih di AdminController).
JANGAN hapus using statements yang masih dipakai oleh actions yang tersisa.

Setelah penghapusan, pastikan `dotnet build` berhasil.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs does NOT contain `ManageWorkers`
    - AdminController.cs does NOT contain `CoachCoacheeMapping`
    - AdminController.cs does NOT contain `AutoCreateProgressForAssignment`
    - AdminController.cs does NOT contain `class CoachAssignRequest`
    - AdminController.cs still contains `ManageOrganization` (belum dipindahkan — plan 02)
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>AdminController sudah bersih dari actions worker dan coach, build berhasil tanpa error</done>
</task>

</tasks>

<verification>
1. `dotnet build` berhasil tanpa error
2. WorkerController.cs berisi 11 action methods worker
3. CoachMappingController.cs berisi 15 action methods coach + 2 Proton helpers
4. AdminController.cs tidak lagi berisi action worker atau coach
</verification>

<success_criteria>
- Build clean
- WorkerController dan CoachMappingController inherit AdminBaseController
- Semua action terpindah lengkap dengan authorization attributes
- CoachAssignRequest dan CoachEditRequest sudah di CoachMappingController.cs
</success_criteria>

<output>
After completion, create `.planning/phases/288-worker-coach-organization-controllers/288-01-SUMMARY.md`
</output>
