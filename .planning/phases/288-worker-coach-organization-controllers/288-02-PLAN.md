---
phase: 288-worker-coach-organization-controllers
plan: 02
type: execute
wave: 2
depends_on: [288-01]
files_modified:
  - Controllers/OrganizationController.cs
  - Controllers/AdminController.cs
  - Views/Admin/CoachCoacheeMapping.cshtml
  - Views/Admin/CreateWorker.cshtml
  - Views/Admin/EditWorker.cshtml
  - Views/Admin/ImportWorkers.cshtml
autonomous: true
requirements: [ORG-01, ORG-02]

must_haves:
  truths:
    - "OrganizationController berisi semua 6 action organization management"
    - "Actions organization sudah dihapus dari AdminController"
    - "Semua asp-controller references di views mengarah ke controller baru yang benar"
    - "Build berhasil tanpa error"
  artifacts:
    - path: "Controllers/OrganizationController.cs"
      provides: "Organization management actions"
      contains: "ManageOrganization"
  key_links:
    - from: "Controllers/OrganizationController.cs"
      to: "AdminBaseController"
      via: "class inheritance"
      pattern: "class OrganizationController : AdminBaseController"
    - from: "Views/Admin/CoachCoacheeMapping.cshtml"
      to: "CoachMappingController"
      via: "asp-controller tag helper"
      pattern: 'asp-controller="CoachMapping"'
---

<objective>
Ekstraksi OrganizationController dari AdminController dan update asp-controller references di views.

Purpose: Menyelesaikan isolasi 3 domain people-management dan memastikan view tag helpers mengarah ke controller baru.
Output: OrganizationController baru, AdminController bersih dari 3 domain, views terupdate.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/288-worker-coach-organization-controllers/288-CONTEXT.md
@.planning/phases/288-worker-coach-organization-controllers/288-RESEARCH.md
@.planning/phases/288-worker-coach-organization-controllers/288-01-SUMMARY.md

<interfaces>
From Controllers/AdminBaseController.cs:
```csharp
[Authorize]
[Route("Admin")]
[Route("Admin/[action]")]
public abstract class AdminBaseController : Controller
{
    protected readonly ApplicationDbContext _context;
    protected readonly UserManager<ApplicationUser> _userManager;
    protected readonly AuditLogService _auditLog;
    protected readonly IWebHostEnvironment _env;
    // constructor: (context, userManager, auditLog, env)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat OrganizationController dan hapus dari AdminController</name>
  <files>Controllers/OrganizationController.cs, Controllers/AdminController.cs</files>
  <read_first>
    - Controllers/AdminController.cs (source — organization actions: ManageOrganization, AddOrganizationUnit, EditOrganizationUnit, ToggleOrganizationUnitActive, DeleteOrganizationUnit, ReorderOrganizationUnit)
    - Controllers/AssessmentAdminController.cs (reference pattern — header, View overrides)
    - Controllers/AdminBaseController.cs (base class signature)
  </read_first>
  <action>
**Buat OrganizationController.cs:**
1. Inherit `AdminBaseController`, tambahkan `[Route("Admin")]` dan `[Route("Admin/[action]")]`
2. TIDAK perlu DI tambahan — hanya base DI (context, userManager, auditLog, env)
3. Copy 4 View override methods persis dari AssessmentAdminController
4. Copy SEMUA action organization dari AdminController (sekitar line 4057-4389 setelah penghapusan plan 01):
   - ManageOrganization, AddOrganizationUnit (GET+POST), EditOrganizationUnit (GET+POST), ToggleOrganizationUnitActive, DeleteOrganizationUnit, ReorderOrganizationUnit
5. Setiap action HARUS memiliki `[Authorize(Roles = "Admin, HC")]` persis seperti aslinya
6. Using statements: Microsoft.AspNetCore.Mvc, Microsoft.AspNetCore.Identity, Microsoft.AspNetCore.Authorization, Microsoft.EntityFrameworkCore, HcPortal.Models, HcPortal.Data, HcPortal.Services

**Hapus dari AdminController.cs:**
- Hapus semua action organization yang sudah dipindahkan
- JANGAN hapus actions yang bukan domain ini (training, document, renewal, Index hub, dll)
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/OrganizationController.cs exists dan contains `class OrganizationController : AdminBaseController`
    - Controllers/OrganizationController.cs contains `ManageOrganization`
    - Controllers/OrganizationController.cs contains `AddOrganizationUnit`
    - Controllers/OrganizationController.cs contains `ReorderOrganizationUnit`
    - Controllers/OrganizationController.cs contains `protected new ViewResult View()`
    - AdminController.cs does NOT contain `ManageOrganization`
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>OrganizationController ada dengan semua 6 action, AdminController bersih dari actions organization, build berhasil</done>
</task>

<task type="auto">
  <name>Task 2: Update asp-controller references di views</name>
  <files>Views/Admin/CoachCoacheeMapping.cshtml, Views/Admin/CreateWorker.cshtml, Views/Admin/EditWorker.cshtml, Views/Admin/ImportWorkers.cshtml</files>
  <read_first>
    - Views/Admin/CoachCoacheeMapping.cshtml (has 4 refs asp-controller="Admin" pointing to coach actions)
    - Views/Admin/CreateWorker.cshtml (has 1 ref asp-controller="Admin" pointing to worker action)
    - Views/Admin/EditWorker.cshtml (has 1 ref asp-controller="Admin" pointing to worker action)
    - Views/Admin/ImportWorkers.cshtml (has 1 ref asp-controller="Admin" pointing to worker action)
  </read_first>
  <action>
Update asp-controller tag helper references yang mengarah ke action yang sudah dipindahkan:

**CoachCoacheeMapping.cshtml** — 4 perubahan:
- Line 55: `asp-controller="Admin" asp-action="CoachCoacheeMappingExport"` → `asp-controller="CoachMapping"`
- Line 168: `asp-action="CoachCoacheeMapping" asp-controller="Admin"` → `asp-controller="CoachMapping"`
- Line 307: `asp-action="MarkMappingCompleted" asp-controller="Admin"` → `asp-controller="CoachMapping"`
- Line 958: `asp-action="ImportCoachCoacheeMapping" asp-controller="Admin"` → `asp-controller="CoachMapping"`

**CreateWorker.cshtml** — 1 perubahan:
- Line 48: `asp-action="CreateWorker" asp-controller="Admin"` → `asp-controller="Worker"`

**EditWorker.cshtml** — 1 perubahan:
- Line 48: `asp-action="EditWorker" asp-controller="Admin"` → `asp-controller="Worker"`

**ImportWorkers.cshtml** — 1 perubahan:
- Line 160: `asp-action="ImportWorkers" asp-controller="Admin"` → `asp-controller="Worker"`

PENTING: Hanya ubah `asp-controller` value, JANGAN ubah asp-action atau attribute lainnya.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && grep -rn 'asp-controller="Admin"' Views/Admin/CoachCoacheeMapping.cshtml Views/Admin/CreateWorker.cshtml Views/Admin/EditWorker.cshtml Views/Admin/ImportWorkers.cshtml; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - CoachCoacheeMapping.cshtml contains `asp-controller="CoachMapping"` (4 occurrences)
    - CoachCoacheeMapping.cshtml does NOT contain `asp-controller="Admin"` for coach actions
    - CreateWorker.cshtml contains `asp-controller="Worker"`
    - EditWorker.cshtml contains `asp-controller="Worker"`
    - ImportWorkers.cshtml contains `asp-controller="Worker"`
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>Semua asp-controller references di views mengarah ke controller baru yang benar, tidak ada 404 risk</done>
</task>

</tasks>

<verification>
1. `dotnet build` berhasil tanpa error
2. OrganizationController.cs berisi 6 action methods
3. AdminController.cs tidak lagi berisi actions worker, coach, atau organization
4. Tidak ada `asp-controller="Admin"` yang mengarah ke action yang sudah dipindahkan
</verification>

<success_criteria>
- Build clean
- OrganizationController inherit AdminBaseController dengan semua action
- Semua asp-controller references di views sudah diupdate ke Worker/CoachMapping/Organization
- AdminController hanya berisi actions yang belum dipindahkan (training, document, renewal, Index hub, dll)
</success_criteria>

<output>
After completion, create `.planning/phases/288-worker-coach-organization-controllers/288-02-SUMMARY.md`
</output>
