---
phase: 289-document-training-renewal-controllers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/DocumentAdminController.cs
  - Controllers/TrainingAdminController.cs
  - Controllers/RenewalController.cs
  - Controllers/AdminBaseController.cs
autonomous: true
requirements: [DOC-01, DOC-02, TRN-01, TRN-02, RNW-01, RNW-02]

must_haves:
  truths:
    - "DocumentAdminController berisi 13 action KKJ+CPDP dengan auth Admin"
    - "TrainingAdminController berisi 7 action training dengan auth Admin,HC"
    - "RenewalController berisi 4 action renewal dengan auth Admin,HC"
    - "BuildRenewalRowsAsync tersedia di AdminBaseController sebagai protected"
    - "dotnet build sukses tanpa error"
  artifacts:
    - path: "Controllers/DocumentAdminController.cs"
      provides: "KKJ + CPDP actions"
      contains: "class DocumentAdminController : AdminBaseController"
    - path: "Controllers/TrainingAdminController.cs"
      provides: "Training actions"
      contains: "class TrainingAdminController : AdminBaseController"
    - path: "Controllers/RenewalController.cs"
      provides: "Renewal actions"
      contains: "class RenewalController : AdminBaseController"
  key_links:
    - from: "Controllers/RenewalController.cs"
      to: "Controllers/AdminBaseController.cs"
      via: "BuildRenewalRowsAsync protected method"
      pattern: "BuildRenewalRowsAsync"
    - from: "Controllers/TrainingAdminController.cs"
      to: "Controllers/AssessmentAdminController.cs"
      via: "RedirectToAction ManageAssessment"
      pattern: "RedirectToAction.*AssessmentAdmin"
---

<objective>
Ekstraksi 3 domain terakhir dari AdminController ke controller terpisah: DocumentAdminController, TrainingAdminController, RenewalController.

Purpose: Menyelesaikan refactoring AdminController — setelah ini AdminController hanya tersisa sebagai hub (~60 baris).
Output: 3 controller baru + BuildRenewalRowsAsync dipindah ke base class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Controllers/AdminController.cs
@Controllers/AdminBaseController.cs
@Controllers/OrganizationController.cs

<interfaces>
<!-- Reference implementation pattern dari OrganizationController -->
From Controllers/OrganizationController.cs:
```csharp
[Route("Admin")]
[Route("Admin/[action]")]
public class OrganizationController : AdminBaseController
{
    public OrganizationController(
        ApplicationDbContext context,
        UserManager<ApplicationUser> userManager,
        AuditLogService auditLog,
        IWebHostEnvironment env)
        : base(context, userManager, auditLog, env) { }

    protected new ViewResult View() => base.View("~/Views/Admin/" + ControllerContext.ActionDescriptor.ActionName + ".cshtml");
    protected new ViewResult View(object? model) => base.View("~/Views/Admin/" + ControllerContext.ActionDescriptor.ActionName + ".cshtml", model);
    protected new ViewResult View(string viewName) => base.View(viewName.StartsWith("~/") ? viewName : "~/Views/Admin/" + viewName + ".cshtml");
    protected new ViewResult View(string viewName, object? model) => base.View(viewName.StartsWith("~/") ? viewName : "~/Views/Admin/" + viewName + ".cshtml", model);
}
```

From Controllers/AdminBaseController.cs:
```csharp
public class AdminBaseController : Controller
{
    // 4 shared DI: ApplicationDbContext, UserManager, AuditLogService, IWebHostEnvironment
    // protected static MapKategori helper
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pindahkan BuildRenewalRowsAsync ke AdminBaseController + buat 3 controller baru</name>
  <files>
    Controllers/AdminBaseController.cs,
    Controllers/DocumentAdminController.cs,
    Controllers/TrainingAdminController.cs,
    Controllers/RenewalController.cs
  </files>
  <read_first>
    Controllers/AdminController.cs,
    Controllers/AdminBaseController.cs,
    Controllers/OrganizationController.cs
  </read_first>
  <action>
1. **AdminBaseController.cs** — Tambahkan `BuildRenewalRowsAsync()` sebagai `protected async Task&lt;List&lt;...&gt;&gt;` method. Copy persis dari AdminController region Renewal. Method ini dipakai oleh AdminController.Index() dan RenewalController.

2. **DocumentAdminController.cs** — Buat controller baru:
   - Class: `DocumentAdminController : AdminBaseController`
   - Route attributes: `[Route("Admin")]` dan `[Route("Admin/[action]")]`
   - Constructor: base 4 DI + `ILogger&lt;DocumentAdminController&gt; logger`
   - View override: 4 overload identik dengan OrganizationController
   - Copy SEMUA actions dari AdminController KKJ region (line 48-392): KkjMatrix, KkjUpload (GET+POST), KkjFileDownload, KkjFileDelete, KkjFileHistory, KkjBagianAdd, DeleteBagian
   - Copy SEMUA actions dari AdminController CPDP region (line 394-638): CpdpFiles, CpdpUpload (GET+POST), CpdpFileDownload, CpdpFileArchive, CpdpFileHistory
   - Copy semua private/helper methods dari kedua region
   - VERIFIKASI per-action authorization dari kode aktual — bisa `[Authorize(Roles = "Admin")]` atau `[Authorize(Roles = "Admin, HC")]`, copy persis
   - Using statements: copy yang relevan dari AdminController (Microsoft.EntityFrameworkCore, System.IO, dll)

3. **TrainingAdminController.cs** — Buat controller baru:
   - Class: `TrainingAdminController : AdminBaseController`
   - Route attributes: `[Route("Admin")]` dan `[Route("Admin/[action]")]`
   - Constructor: base 4 DI + `ILogger&lt;TrainingAdminController&gt; logger`
   - View override: 4 overload identik
   - Copy SEMUA actions dari Training region (line 641-1316): AddTraining (GET+POST), EditTraining (GET+POST), DeleteTraining, DownloadImportTrainingTemplate, ImportTraining (GET+POST)
   - Copy private method `SetTrainingCategoryViewBag()`
   - Authorization: `[Authorize(Roles = "Admin, HC")]` per action
   - Cross-controller redirect: pastikan `RedirectToAction("ManageAssessment", "AssessmentAdmin", ...)` sudah benar (bukan "Admin")
   - Using statements tambahan: `using ClosedXML.Excel;`, `using System.Globalization;`, `using HcPortal.Helpers;`

4. **RenewalController.cs** — Buat controller baru:
   - Class: `RenewalController : AdminBaseController`
   - Route attributes: `[Route("Admin")]` dan `[Route("Admin/[action]")]`
   - Constructor: base 4 DI saja (tidak perlu extra)
   - View override: 4 overload identik
   - Copy SEMUA actions dari Renewal region (line 1318-1812): RenewalCertificate, FilterRenewalCertificate, FilterRenewalCertificateGroup, CertificateHistory
   - Copy private methods KECUALI BuildRenewalRowsAsync (sudah di base)
   - Ganti `BuildRenewalRowsAsync()` call menjadi memanggil dari base class (tidak perlu prefix, inherited)
   - Authorization: `[Authorize(Roles = "Admin, HC")]` per action
   - PartialView calls: pastikan path absolute `~/Views/Admin/...` atau shared folder — PartialView TIDAK di-override, gunakan path absolut untuk partial views
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File Controllers/DocumentAdminController.cs exists dan contains `class DocumentAdminController : AdminBaseController`
    - File Controllers/DocumentAdminController.cs contains `[Route("Admin")]`
    - File Controllers/DocumentAdminController.cs contains `KkjMatrix` dan `CpdpFiles`
    - File Controllers/TrainingAdminController.cs exists dan contains `class TrainingAdminController : AdminBaseController`
    - File Controllers/TrainingAdminController.cs contains `AddTraining` dan `ImportTraining`
    - File Controllers/TrainingAdminController.cs contains `using ClosedXML.Excel`
    - File Controllers/RenewalController.cs exists dan contains `class RenewalController : AdminBaseController`
    - File Controllers/RenewalController.cs contains `RenewalCertificate` dan `FilterRenewalCertificate`
    - File Controllers/AdminBaseController.cs contains `BuildRenewalRowsAsync`
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>3 controller baru ada dengan semua actions, BuildRenewalRowsAsync di base class, build sukses</done>
</task>

<task type="auto">
  <name>Task 2: Hapus actions yang sudah diekstrak dari AdminController</name>
  <files>
    Controllers/AdminController.cs
  </files>
  <read_first>
    Controllers/AdminController.cs,
    Controllers/DocumentAdminController.cs,
    Controllers/TrainingAdminController.cs,
    Controllers/RenewalController.cs
  </read_first>
  <action>
1. Buka AdminController.cs dan hapus:
   - KKJ region (line 48-392) — semua actions dan private methods
   - CPDP region (line 394-638) — semua actions dan private methods
   - Training region (line 641-1316) — semua actions dan private methods
   - Renewal region (line 1318-1812) — semua actions dan private methods TERMASUK BuildRenewalRowsAsync (sudah di base)

2. AdminController.cs seharusnya hanya tersisa:
   - Using statements (bersihkan yang tidak dipakai)
   - Class declaration inheriting AdminBaseController
   - Constructor: base 4 DI + `IMemoryCache _cache`
   - `Index()` action — pastikan `BuildRenewalRowsAsync()` call masih bekerja (inherited dari base)
   - `Maintenance()` GET + POST actions
   - Route attributes `[Route("Admin")]` dan `[Route("Admin/[action]")]`

3. Hapus using statements yang tidak lagi dipakai oleh sisa actions (ClosedXML, System.IO jika tidak dipakai oleh Index/Maintenance, dll)

4. Verifikasi AdminController.cs sekarang ~60-80 baris saja
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.cs TIDAK contains `KkjMatrix`
    - AdminController.cs TIDAK contains `CpdpFiles`
    - AdminController.cs TIDAK contains `AddTraining`
    - AdminController.cs TIDAK contains `RenewalCertificate`
    - AdminController.cs contains `Index` dan `Maintenance`
    - AdminController.cs kurang dari 150 baris
    - `dotnet build` exits with code 0
  </acceptance_criteria>
  <done>AdminController hanya berisi Index + Maintenance, build sukses, semua extracted actions ada di controller baru</done>
</task>

</tasks>

<verification>
1. `dotnet build` sukses tanpa error
2. Grep: DocumentAdminController contains KkjMatrix, CpdpFiles
3. Grep: TrainingAdminController contains AddTraining, ImportTraining
4. Grep: RenewalController contains RenewalCertificate
5. Grep: AdminController TIDAK contains KkjMatrix, CpdpFiles, AddTraining, RenewalCertificate
6. AdminBaseController contains BuildRenewalRowsAsync
</verification>

<success_criteria>
- 3 controller baru tercipta dengan semua actions dan authorization identik
- AdminController hanya tersisa Index + Maintenance (~60-80 baris)
- BuildRenewalRowsAsync ada di AdminBaseController sebagai protected method
- `dotnet build` sukses
</success_criteria>

<output>
After completion, create `.planning/phases/289-document-training-renewal-controllers/289-01-SUMMARY.md`
</output>
