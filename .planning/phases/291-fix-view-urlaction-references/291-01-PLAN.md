---
phase: 291-fix-view-urlaction-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/Index.cshtml
  - Views/Admin/ManageWorkers.cshtml
  - Views/Admin/CreateWorker.cshtml
  - Views/Admin/EditWorker.cshtml
  - Views/Admin/ImportWorkers.cshtml
  - Views/Admin/WorkerDetail.cshtml
  - Views/Admin/CoachCoacheeMapping.cshtml
autonomous: true
requirements: [VER-01]
gap_closure: true

must_haves:
  truths:
    - "Index hub links menghasilkan URL valid ke semua domain controller"
    - "Semua link dan form di Worker views menghasilkan URL valid"
    - "CoachCoacheeMapping download template link valid"
  artifacts:
    - path: "Views/Admin/Index.cshtml"
      provides: "Hub links ke semua domain controllers"
      contains: "Url.Action.*Worker|Url.Action.*Organization|Url.Action.*DocumentAdmin|Url.Action.*AssessmentAdmin|Url.Action.*Renewal|Url.Action.*CoachMapping"
    - path: "Views/Admin/ManageWorkers.cshtml"
      provides: "Worker CRUD links dan form actions"
      contains: 'Url.Action("ManageWorkers", "Worker")'
  key_links:
    - from: "Views/Admin/Index.cshtml"
      to: "Multiple domain controllers"
      via: "Url.Action with correct controller names"
      pattern: 'Url\.Action\("ManageWorkers", "Worker"\)'
---

<objective>
Fix broken Url.Action references di Index hub, Worker views, dan CoachMapping views.

Purpose: Index.cshtml adalah hub utama admin — 10 broken links. Worker views memiliki 22 broken references termasuk form POST actions yang kritis.
Output: Semua Url.Action di 7 files menghasilkan URL valid.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v12.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Index hub + Worker views Url.Action references</name>
  <files>
    Views/Admin/Index.cshtml,
    Views/Admin/ManageWorkers.cshtml,
    Views/Admin/CreateWorker.cshtml,
    Views/Admin/EditWorker.cshtml,
    Views/Admin/ImportWorkers.cshtml,
    Views/Admin/WorkerDetail.cshtml
  </files>
  <read_first>
    Views/Admin/Index.cshtml,
    Views/Admin/ManageWorkers.cshtml,
    Views/Admin/CreateWorker.cshtml,
    Views/Admin/EditWorker.cshtml,
    Views/Admin/ImportWorkers.cshtml,
    Views/Admin/WorkerDetail.cshtml
  </read_first>
  <action>
    Replace broken Url.Action("X", "Admin") with correct controller names. DO NOT touch Url.Action("Index", "Admin") or Url.Action("Maintenance", "Admin") — those are VALID (still in AdminController).

    **Index.cshtml** (10 replacements):
    - Url.Action("ManageWorkers", "Admin") → Url.Action("ManageWorkers", "Worker")
    - Url.Action("ManageOrganization", "Admin") → Url.Action("ManageOrganization", "Organization")
    - Url.Action("KkjMatrix", "Admin") → Url.Action("KkjMatrix", "DocumentAdmin")
    - Url.Action("CpdpFiles", "Admin") → Url.Action("CpdpFiles", "DocumentAdmin")
    - Url.Action("CoachCoacheeMapping", "Admin") → Url.Action("CoachCoacheeMapping", "CoachMapping")
    - Url.Action("ManageAssessment", "Admin") → Url.Action("ManageAssessment", "AssessmentAdmin")
    - Url.Action("AssessmentMonitoring", "Admin") → Url.Action("AssessmentMonitoring", "AssessmentAdmin")
    - Url.Action("AuditLog", "Admin") → Url.Action("AuditLog", "AssessmentAdmin")
    - Url.Action("ManageCategories", "Admin") → Url.Action("ManageCategories", "AssessmentAdmin")
    - Url.Action("RenewalCertificate", "Admin") → Url.Action("RenewalCertificate", "Renewal")

    **ManageWorkers.cshtml** — all non-Index Url.Action("X", "Admin") → "Worker":
    - CreateWorker, ImportWorkers, ManageWorkers, DeactivateWorker, ReactivateWorker, DeleteWorker → "Worker"

    **CreateWorker.cshtml** — ManageWorkers → "Worker"

    **EditWorker.cshtml** — ManageWorkers → "Worker"

    **ImportWorkers.cshtml**:
    - ManageWorkers → "Worker"
    - ReactivateWorker → "Worker"
    - DownloadImportTemplate → "Worker"

    **WorkerDetail.cshtml** — ManageWorkers → "Worker"
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep -c 'Url\.Action(".*", "Admin")' Views/Admin/Index.cshtml Views/Admin/ManageWorkers.cshtml Views/Admin/CreateWorker.cshtml Views/Admin/EditWorker.cshtml Views/Admin/ImportWorkers.cshtml Views/Admin/WorkerDetail.cshtml | grep -v ":0$"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'Url.Action.*"Admin"' pada 6 files hanya mengembalikan references ke "Index" atau "Maintenance"
    - Zero Url.Action("ManageWorkers", "Admin") di semua 6 files
    - Zero Url.Action("CreateWorker", "Admin") di semua files
    - Zero Url.Action("DeactivateWorker", "Admin")
    - Index.cshtml: semua 10 hub links sudah pakai controller name yang benar
  </acceptance_criteria>
  <done>Semua Url.Action di Index hub dan Worker views mengarah ke controller yang benar. Hanya Url.Action("Index", "Admin") yang tersisa.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CoachCoacheeMapping Url.Action references</name>
  <files>Views/Admin/CoachCoacheeMapping.cshtml</files>
  <read_first>Views/Admin/CoachCoacheeMapping.cshtml</read_first>
  <action>
    Replace broken references:
    - Url.Action("DownloadMappingImportTemplate", "Admin") → Url.Action("DownloadMappingImportTemplate", "CoachMapping")

    DO NOT touch Url.Action("Index", "Admin") — valid.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep 'Url\.Action("DownloadMappingImportTemplate", "Admin")' Views/Admin/CoachCoacheeMapping.cshtml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for Url.Action("DownloadMappingImportTemplate", "Admin")
    - grep 'DownloadMappingImportTemplate.*CoachMapping' returns 1 match
  </acceptance_criteria>
  <done>CoachCoacheeMapping download template link valid.</done>
</task>

</tasks>

<verification>
grep -rn 'Url\.Action(".*", "Admin")' Views/Admin/Index.cshtml Views/Admin/ManageWorkers.cshtml Views/Admin/CreateWorker.cshtml Views/Admin/EditWorker.cshtml Views/Admin/ImportWorkers.cshtml Views/Admin/WorkerDetail.cshtml Views/Admin/CoachCoacheeMapping.cshtml | grep -v '"Index"' | grep -v '"Maintenance"'
Harus return 0 baris.
</verification>

<success_criteria>
- Zero broken Url.Action di 7 files
- Hanya Url.Action("Index", "Admin") dan Url.Action("Maintenance", "Admin") yang tersisa
- Build sukses: dotnet build --no-restore
</success_criteria>

<output>
After completion, create `.planning/phases/291-fix-view-urlaction-references/291-01-SUMMARY.md`
</output>
