---
phase: 291-fix-view-urlaction-references
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/ManageOrganization.cshtml
  - Views/Admin/KkjMatrix.cshtml
  - Views/Admin/KkjUpload.cshtml
  - Views/Admin/KkjFileHistory.cshtml
  - Views/Admin/CpdpFiles.cshtml
  - Views/Admin/CpdpUpload.cshtml
  - Views/Admin/CpdpFileHistory.cshtml
autonomous: true
requirements: [VER-01]
gap_closure: true

must_haves:
  truths:
    - "Semua form POST di ManageOrganization menghasilkan URL valid"
    - "Semua link di KKJ views menghasilkan URL valid"
    - "Semua link di CPDP views menghasilkan URL valid"
  artifacts:
    - path: "Views/Admin/ManageOrganization.cshtml"
      provides: "Organization CRUD form actions"
      contains: 'Url.Action("AddOrganizationUnit", "Organization")'
    - path: "Views/Admin/KkjMatrix.cshtml"
      provides: "KKJ navigation links"
      contains: 'Url.Action("KkjMatrix", "DocumentAdmin")'
  key_links:
    - from: "Views/Admin/ManageOrganization.cshtml"
      to: "OrganizationController"
      via: "Url.Action with Organization controller"
      pattern: 'Url\.Action\(".*", "Organization"\)'
    - from: "Views/Admin/KkjMatrix.cshtml"
      to: "DocumentAdminController"
      via: "Url.Action with DocumentAdmin controller"
      pattern: 'Url\.Action\("KkjMatrix", "DocumentAdmin"\)'
---

<objective>
Fix broken Url.Action references di Organization dan Document views.

Purpose: ManageOrganization memiliki ~13 broken form POST actions (kritis — toggle, reorder, delete). Document views (KKJ + CPDP) memiliki ~10 broken references.
Output: Semua Url.Action di 7 files menghasilkan URL valid.
</objective>

<execution_context>
@C:\Users\Administrator\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Administrator\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Organization view Url.Action references</name>
  <files>Views/Admin/ManageOrganization.cshtml</files>
  <read_first>Views/Admin/ManageOrganization.cshtml</read_first>
  <action>
    Replace all broken Url.Action("X", "Admin") → Url.Action("X", "Organization"):
    - AddOrganizationUnit → "Organization"
    - EditOrganizationUnit → "Organization"
    - ManageOrganization → "Organization"
    - ReorderOrganizationUnit → "Organization" (multiple occurrences)
    - ToggleOrganizationUnitActive → "Organization" (multiple occurrences)
    - DeleteOrganizationUnit → "Organization"

    DO NOT touch Url.Action("Index", "Admin") — valid.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep 'Url\.Action(".*", "Admin")' Views/Admin/ManageOrganization.cshtml | grep -v '"Index"' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for Url.Action("...Organization...", "Admin")
    - All form actions use "Organization" controller
    - Url.Action("Index", "Admin") tetap ada (valid)
  </acceptance_criteria>
  <done>Semua 13+ Organization form actions dan links menghasilkan URL valid.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Document views (KKJ + CPDP) Url.Action references</name>
  <files>
    Views/Admin/KkjMatrix.cshtml,
    Views/Admin/KkjUpload.cshtml,
    Views/Admin/KkjFileHistory.cshtml,
    Views/Admin/CpdpFiles.cshtml,
    Views/Admin/CpdpUpload.cshtml,
    Views/Admin/CpdpFileHistory.cshtml
  </files>
  <read_first>
    Views/Admin/KkjMatrix.cshtml,
    Views/Admin/KkjUpload.cshtml,
    Views/Admin/KkjFileHistory.cshtml,
    Views/Admin/CpdpFiles.cshtml,
    Views/Admin/CpdpUpload.cshtml,
    Views/Admin/CpdpFileHistory.cshtml
  </read_first>
  <action>
    Replace all broken Url.Action("X", "Admin") → Url.Action("X", "DocumentAdmin"):
    - KkjMatrix → "DocumentAdmin"
    - KkjUpload → "DocumentAdmin" (if present)
    - CpdpFiles → "DocumentAdmin"
    - CpdpUpload → "DocumentAdmin" (if present)

    DO NOT touch Url.Action("Index", "Admin") — valid.

    Specific files:
    - KkjMatrix.cshtml: KkjMatrix refs (line 266 JS template literal)
    - KkjUpload.cshtml: KkjMatrix refs (breadcrumb + back/cancel links)
    - KkjFileHistory.cshtml: KkjMatrix ref (breadcrumb)
    - CpdpFiles.cshtml: CpdpFiles ref (line 244 JS template literal)
    - CpdpUpload.cshtml: CpdpFiles refs (breadcrumb + back/cancel links)
    - CpdpFileHistory.cshtml: CpdpFiles ref (breadcrumb)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && grep 'Url\.Action(".*", "Admin")' Views/Admin/KkjMatrix.cshtml Views/Admin/KkjUpload.cshtml Views/Admin/KkjFileHistory.cshtml Views/Admin/CpdpFiles.cshtml Views/Admin/CpdpUpload.cshtml Views/Admin/CpdpFileHistory.cshtml | grep -v '"Index"' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero non-Index Url.Action("X", "Admin") di 6 document views
    - Semua KkjMatrix refs pakai "DocumentAdmin"
    - Semua CpdpFiles refs pakai "DocumentAdmin"
    - JS template literals di KkjMatrix.cshtml dan CpdpFiles.cshtml juga sudah diupdate
  </acceptance_criteria>
  <done>Semua Document view links dan breadcrumbs menghasilkan URL valid.</done>
</task>

</tasks>

<verification>
grep -rn 'Url\.Action(".*", "Admin")' Views/Admin/ManageOrganization.cshtml Views/Admin/KkjMatrix.cshtml Views/Admin/KkjUpload.cshtml Views/Admin/KkjFileHistory.cshtml Views/Admin/CpdpFiles.cshtml Views/Admin/CpdpUpload.cshtml Views/Admin/CpdpFileHistory.cshtml | grep -v '"Index"' | grep -v '"Maintenance"'
Harus return 0 baris.
</verification>

<success_criteria>
- Zero broken Url.Action di 7 files
- Build sukses: dotnet build --no-restore
</success_criteria>

<output>
After completion, create `.planning/phases/291-fix-view-urlaction-references/291-02-SUMMARY.md`
</output>
