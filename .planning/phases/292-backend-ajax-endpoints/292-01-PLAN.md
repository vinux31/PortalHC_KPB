---
phase: 292-backend-ajax-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminBaseController.cs
  - Controllers/OrganizationController.cs
  - wwwroot/js/orgTree.js
autonomous: true
requirements: [TREE-01, TREE-04]

must_haves:
  truths:
    - "GET /Admin/GetOrganizationTree mengembalikan flat JSON array dengan 6 field"
    - "POST actions mengembalikan JSON {success, message} jika X-Requested-With: XMLHttpRequest"
    - "POST actions tetap redirect seperti biasa jika bukan AJAX request"
    - "NotFound() pada Edit/Toggle/Delete/Reorder juga return JSON jika AJAX"
    - "ajaxPost() di orgTree.js mengirim CSRF token via form-urlencoded"
  artifacts:
    - path: "Controllers/AdminBaseController.cs"
      provides: "IsAjaxRequest() helper method"
      contains: "IsAjaxRequest"
    - path: "Controllers/OrganizationController.cs"
      provides: "GetOrganizationTree endpoint + dual-response pada 5 POST actions"
      contains: "GetOrganizationTree"
    - path: "wwwroot/js/orgTree.js"
      provides: "ajaxPost utility dengan CSRF token"
      contains: "ajaxPost"
  key_links:
    - from: "wwwroot/js/orgTree.js"
      to: "Controllers/OrganizationController.cs"
      via: "fetch POST with X-Requested-With header"
      pattern: "X-Requested-With.*XMLHttpRequest"
    - from: "Controllers/OrganizationController.cs"
      to: "Controllers/AdminBaseController.cs"
      via: "IsAjaxRequest() helper call"
      pattern: "IsAjaxRequest\\(\\)"
---

<objective>
Menambahkan endpoint GetOrganizationTree dan mengubah 5 POST action menjadi dual-response (JSON jika AJAX, redirect jika form POST), plus membuat JS utility ajaxPost di orgTree.js.

Purpose: Backend siap untuk phase 293-295 yang akan mengganti view statis menjadi dynamic tree.
Output: 3 file dimodifikasi/dibuat — AdminBaseController, OrganizationController, orgTree.js
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/292-backend-ajax-endpoints/292-CONTEXT.md
@.planning/phases/292-backend-ajax-endpoints/292-RESEARCH.md

@Controllers/AdminBaseController.cs
@Controllers/OrganizationController.cs
@Models/OrganizationUnit.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: IsAjaxRequest helper + GetOrganizationTree endpoint + dual-response pada semua actions</name>
  <files>Controllers/AdminBaseController.cs, Controllers/OrganizationController.cs</files>
  <action>
**1. AdminBaseController.cs** — Tambahkan helper method (per D-03, research merekomendasikan base controller):

```csharp
protected bool IsAjaxRequest()
    => Request.Headers["X-Requested-With"] == "XMLHttpRequest";
```

Tambahkan setelah constructor, sebelum `MapKategori`.

**2. OrganizationController.cs** — Tambahkan endpoint GET baru (per D-01, D-02):

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> GetOrganizationTree()
{
    var units = await _context.OrganizationUnits
        .OrderBy(u => u.Level)
        .ThenBy(u => u.DisplayOrder)
        .ThenBy(u => u.Name)
        .Select(u => new { u.Id, u.Name, u.ParentId, u.Level, u.DisplayOrder, u.IsActive })
        .ToListAsync();
    return Json(units);
}
```

Letakkan setelah action `ManageOrganization`, sebelum `AddOrganizationUnit`.

**3. OrganizationController.cs** — Ubah 5 POST actions menjadi dual-response (per D-06):

Untuk setiap titik yang saat ini `return RedirectToAction("ManageOrganization")` atau `return NotFound()`:

- **Pattern redirect dengan TempData message:** Tambahkan sebelum TempData + redirect:
  ```csharp
  if (IsAjaxRequest())
      return Json(new { success = true/false, message = "..." });
  ```
  Gunakan message string yang SAMA dengan yang sudah ada di TempData.

- **Pattern NotFound()** (ada di Edit line 112, Toggle line 259, Delete line 301, Reorder line 336): Tambahkan sebelum `return NotFound()`:
  ```csharp
  if (IsAjaxRequest())
      return Json(new { success = false, message = "Unit tidak ditemukan." });
  ```

- **Pattern Reorder tanpa message** (line 357 `return RedirectToAction`): Tambahkan sebelumnya:
  ```csharp
  if (IsAjaxRequest())
      return Json(new { success = true, message = "Urutan berhasil diubah." });
  ```

Daftar lengkap 5 action dan titik-titik perubahannya:

**AddOrganizationUnit:** 3 return points (2 error, 1 success)
**EditOrganizationUnit:** 6 return points (1 NotFound, 4 error redirect, 1 success — note: success message bisa beda tergantung cascade count, gunakan variable `msg`)
**ToggleOrganizationUnitActive:** 3 return points (1 NotFound, 1 error, 1 success)
**DeleteOrganizationUnit:** 5 return points (1 NotFound, 3 error, 1 success)
**ReorderOrganizationUnit:** 2 return points (1 NotFound, 1 success tanpa message)

PENTING: Jangan ubah logic bisnis yang sudah ada. Hanya tambahkan `if (IsAjaxRequest())` branch sebelum setiap return statement yang existing. TempData + redirect path harus tetap identik.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
- Build sukses tanpa error
- AdminBaseController punya method IsAjaxRequest()
- OrganizationController punya action GetOrganizationTree yang return Json flat array
- Semua 5 POST action punya dual-response: JSON jika AJAX, redirect jika bukan
- Semua NotFound() didahului AJAX check
  </done>
</task>

<task type="auto">
  <name>Task 2: Buat orgTree.js dengan ajaxPost utility</name>
  <files>wwwroot/js/orgTree.js</files>
  <action>
Buat file baru `wwwroot/js/orgTree.js` (per D-04, D-05):

```javascript
// Organization Tree — JS utilities
// Phase 292: AJAX utility | Phase 293+: tree rendering & CRUD

function getAntiForgeryToken() {
    const input = document.querySelector('input[name="__RequestVerificationToken"]');
    return input ? input.value : '';
}

async function ajaxPost(url, data = {}) {
    const params = new URLSearchParams(data);
    params.append('__RequestVerificationToken', getAntiForgeryToken());
    const res = await fetch(url, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            'X-Requested-With': 'XMLHttpRequest'
        },
        body: params.toString()
    });
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    return res.json();
}

async function ajaxGet(url) {
    const res = await fetch(url, {
        headers: { 'X-Requested-With': 'XMLHttpRequest' }
    });
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    return res.json();
}
```

Catatan:
- `Content-Type: application/x-www-form-urlencoded` agar `[ValidateAntiForgeryToken]` bisa baca token dari body (per D-05)
- `ajaxGet` ditambahkan sebagai convenience untuk GetOrganizationTree (phase 293 akan pakai)
- Tidak ada DOM manipulation di phase ini — hanya utility functions
  </action>
  <verify>
    <automated>test -f "C:/Users/Administrator/Desktop/PortalHC_KPB/wwwroot/js/orgTree.js" && grep -c "ajaxPost" "C:/Users/Administrator/Desktop/PortalHC_KPB/wwwroot/js/orgTree.js"</automated>
  </verify>
  <done>
- File wwwroot/js/orgTree.js ada
- Berisi function ajaxPost yang mengirim CSRF token via form-urlencoded
- Berisi function ajaxGet untuk GET requests
- Header X-Requested-With: XMLHttpRequest disertakan di kedua function
  </done>
</task>

</tasks>

<verification>
1. `dotnet build` sukses — tidak ada compile error
2. File orgTree.js ada di wwwroot/js/
3. Jalankan aplikasi, buka browser DevTools, hit `GET /Admin/GetOrganizationTree` — harus return JSON array dengan field Id, Name, ParentId, Level, DisplayOrder, IsActive
4. Halaman ManageOrganization tetap berfungsi normal (PRG pattern tidak rusak) — test Add, Edit, Toggle, Delete via form biasa
</verification>

<success_criteria>
- GET /Admin/GetOrganizationTree mengembalikan flat JSON array semua OrganizationUnit
- POST actions return JSON {success, message} jika ada header X-Requested-With: XMLHttpRequest
- POST actions tetap redirect normal jika bukan AJAX
- orgTree.js berisi ajaxPost dan ajaxGet utility
- dotnet build sukses
- Tidak ada regression pada form-based flow
</success_criteria>

<output>
After completion, create `.planning/phases/292-backend-ajax-endpoints/292-01-SUMMARY.md`
</output>
