---
phase: 296-data-foundation-gradingservice-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentSession.cs
  - Models/AssessmentPackage.cs
  - Models/PackageUserResponse.cs
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "AssessmentSession memiliki 5 kolom baru: AssessmentType, AssessmentPhase, LinkedGroupId, LinkedSessionId, HasManualGrading"
    - "PackageQuestion memiliki kolom QuestionType string nullable"
    - "PackageUserResponse memiliki kolom TextAnswer string nullable"
    - "Semua kolom baru nullable atau punya default value — tidak ada breaking change"
  artifacts:
    - path: "Models/AssessmentSession.cs"
      provides: "5 kolom baru pada AssessmentSession"
      contains: "AssessmentType"
    - path: "Models/AssessmentPackage.cs"
      provides: "QuestionType pada PackageQuestion"
      contains: "QuestionType"
    - path: "Models/PackageUserResponse.cs"
      provides: "TextAnswer pada PackageUserResponse"
      contains: "TextAnswer"
  key_links: []
---

<objective>
Tambahkan kolom-kolom baru ke 3 model entity (AssessmentSession, PackageQuestion, PackageUserResponse) sebagai fondasi data untuk v14.0 Assessment Enhancement.

Purpose: Kolom-kolom ini diperlukan agar GradingService (Plan 02) bisa melakukan switch-case per QuestionType dan agar phase-phase selanjutnya bisa menyimpan MultipleAnswer dan Essay.
Output: 3 model files updated dengan property baru, siap untuk migration generation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/296-data-foundation-gradingservice-extraction/296-CONTEXT.md
@.planning/phases/296-data-foundation-gradingservice-extraction/296-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah kolom baru ke 3 model entity</name>
  <files>Models/AssessmentSession.cs, Models/AssessmentPackage.cs, Models/PackageUserResponse.cs</files>
  <read_first>
    - Models/AssessmentSession.cs
    - Models/AssessmentPackage.cs
    - Models/PackageUserResponse.cs
    - .planning/phases/296-data-foundation-gradingservice-extraction/296-RESEARCH.md
  </read_first>
  <action>
Tambahkan property berikut ke model entities:

**Models/AssessmentSession.cs** — tambah 5 property di akhir class (sebelum closing brace), setelah property yang sudah ada:
```csharp
// v14.0 Assessment Enhancement columns
public string? AssessmentType { get; set; }
public string? AssessmentPhase { get; set; }
public int? LinkedGroupId { get; set; }
public int? LinkedSessionId { get; set; }
public bool HasManualGrading { get; set; }
```

**Models/AssessmentPackage.cs** — di dalam class `PackageQuestion` (inner class, baris ~27-48), tambah property:
```csharp
public string? QuestionType { get; set; }
```
QuestionType disimpan sebagai string per D-06: 'MultipleChoice', 'MultipleAnswer', 'Essay'. Null = MultipleChoice untuk backward compat (per A1 assumption).

**Models/PackageUserResponse.cs** — tambah property:
```csharp
public string? TextAnswer { get; set; }
```
TextAnswer nullable — hanya diisi untuk Essay, null untuk MC/MA.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && grep -n "AssessmentType\|AssessmentPhase\|LinkedGroupId\|LinkedSessionId\|HasManualGrading" Models/AssessmentSession.cs && grep -n "QuestionType" Models/AssessmentPackage.cs && grep -n "TextAnswer" Models/PackageUserResponse.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "AssessmentType" Models/AssessmentSession.cs returns "public string? AssessmentType"
    - grep "AssessmentPhase" Models/AssessmentSession.cs returns "public string? AssessmentPhase"
    - grep "LinkedGroupId" Models/AssessmentSession.cs returns "public int? LinkedGroupId"
    - grep "LinkedSessionId" Models/AssessmentSession.cs returns "public int? LinkedSessionId"
    - grep "HasManualGrading" Models/AssessmentSession.cs returns "public bool HasManualGrading"
    - grep "QuestionType" Models/AssessmentPackage.cs returns "public string? QuestionType"
    - grep "TextAnswer" Models/PackageUserResponse.cs returns "public string? TextAnswer"
  </acceptance_criteria>
  <done>7 kolom baru ditambahkan ke 3 model files — semua nullable atau value type dengan default</done>
</task>

<task type="auto">
  <name>Task 2: Generate EF Core migration untuk kolom baru</name>
  <files></files>
  <read_first>
    - Models/AssessmentSession.cs
    - Models/AssessmentPackage.cs
    - Models/PackageUserResponse.cs
  </read_first>
  <action>
Jalankan EF Core migration command untuk generate migration file dari model changes di Task 1:

```bash
dotnet ef migrations add AddAssessmentV14Columns
```

Setelah migration ter-generate, buka file migration baru dan verifikasi:
1. 7 kolom ditambahkan (5 AssessmentSessions, 1 PackageQuestions, 1 PackageUserResponses)
2. AssessmentType, AssessmentPhase: nvarchar nullable
3. LinkedGroupId, LinkedSessionId: int nullable
4. HasManualGrading: bit, nullable false, defaultValue false
5. QuestionType: nvarchar nullable
6. TextAnswer: nvarchar(max) nullable
7. Down() method drops semua 7 kolom

Jika `HasManualGrading` tidak otomatis mendapat `defaultValue: false`, edit migration manual untuk tambahkan `defaultValue: false` pada AddColumn call tersebut.

Satu migration per D-05 — TIDAK split menjadi beberapa migration.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds (exit code 0)
    - File Migrations/*_AddAssessmentV14Columns.cs exists
    - grep "AddColumn.*AssessmentType" in migration file returns match
    - grep "AddColumn.*QuestionType" in migration file returns match
    - grep "AddColumn.*TextAnswer" in migration file returns match
    - grep "defaultValue: false" in migration file returns match (for HasManualGrading)
  </acceptance_criteria>
  <done>Migration file ter-generate dengan 7 kolom baru, build sukses, semua kolom nullable atau punya default</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase ini hanya menambah kolom nullable ke model — tidak ada trust boundary baru |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-296-01 | Tampering | Migration | accept | Kolom nullable/default — tidak mengubah existing data, rollback via Down() tersedia |
</threat_model>

<verification>
- `dotnet build` sukses
- Migration file berisi 7 AddColumn dan 7 DropColumn
- Model files memiliki property baru
</verification>

<success_criteria>
- 3 model files memiliki kolom baru
- 1 migration file ter-generate dan build sukses
- Tidak ada breaking change pada existing data
</success_criteria>

<output>
After completion, create `.planning/phases/296-data-foundation-gradingservice-extraction/296-01-SUMMARY.md`
</output>
