---
phase: 296-data-foundation-gradingservice-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - 296-02
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Controllers/CMPController.cs
autonomous: true
requirements:
  - FOUND-07
  - FOUND-08
  - FOUND-09

must_haves:
  truths:
    - "AssessmentAdminController.AkhiriUjian() memanggil GradingService.GradeAndCompleteAsync(), bukan GradeFromSavedAnswers()"
    - "AssessmentAdminController.AkhiriSemuaUjian() memanggil GradingService.GradeAndCompleteAsync() per session, bukan GradeFromSavedAnswers()"
    - "CMPController.SubmitExam() memanggil GradingService.GradeAndCompleteAsync(), bukan inline grading logic"
    - "GradeFromSavedAnswers private method dihapus dari AssessmentAdminController"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "AkhiriUjian + AkhiriSemuaUjian menggunakan GradingService"
      contains: "_gradingService.GradeAndCompleteAsync"
    - path: "Controllers/CMPController.cs"
      provides: "SubmitExam menggunakan GradingService"
      contains: "_gradingService.GradeAndCompleteAsync"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs"
      to: "Services/GradingService.cs"
      via: "constructor injection + method call"
      pattern: "_gradingService.GradeAndCompleteAsync"
    - from: "Controllers/CMPController.cs"
      to: "Services/GradingService.cs"
      via: "constructor injection + method call"
      pattern: "_gradingService.GradeAndCompleteAsync"
---

<objective>
Wire AssessmentAdminController dan CMPController ke GradingService — ganti logika grading duplikat dengan panggilan ke service terpusat.

Purpose: Menghilangkan duplikasi grading logic di 2 controller, memastikan bug fix di satu tempat berlaku di semua tempat.
Output: 2 controller files updated, GradeFromSavedAnswers dihapus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/296-data-foundation-gradingservice-extraction/296-CONTEXT.md
@.planning/phases/296-data-foundation-gradingservice-extraction/296-RESEARCH.md
@.planning/phases/296-data-foundation-gradingservice-extraction/296-02-SUMMARY.md

<interfaces>
From Services/GradingService.cs (created in Plan 02):
```csharp
public class GradingService
{
    public async Task GradeAndCompleteAsync(AssessmentSession session);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AssessmentAdminController ke GradingService</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs
    - Services/GradingService.cs
    - .planning/phases/296-data-foundation-gradingservice-extraction/296-02-SUMMARY.md
  </read_first>
  <action>
**Step 1 — Tambah GradingService ke constructor:**
- Tambah parameter `GradingService gradingService` ke constructor AssessmentAdminController
- Tambah field `private readonly GradingService _gradingService;`
- Set `_gradingService = gradingService;` di constructor body
- JANGAN ubah base constructor call (GradingService bukan di AdminBaseController)

**Step 2 — Update AkhiriUjian() (baris ~2267-2375):**
- Ganti panggilan ke `GradeFromSavedAnswers(session)` (atau nama aktual) dengan `await _gradingService.GradeAndCompleteAsync(session);`
- PERTAHANKAN: `_cache.Remove($"exam-status-{id}")` — tetap di controller
- PERTAHANKAN: SignalR push `examClosed` — tetap di controller
- PERTAHANKAN: AuditLog call — tetap di controller (atau pindah ke service jika sudah ada di service)
- HAPUS: Logika NomorSertifikat yang inline di AkhiriUjian (sudah di GradingService)
- HAPUS: `SaveChangesAsync()` yang dulu untuk grading results (sudah di GradingService)

**Step 3 — Update AkhiriSemuaUjian() (baris ~2380-2460):**
- Dalam loop per session, ganti panggilan ke `GradeFromSavedAnswers(session)` dengan `await _gradingService.GradeAndCompleteAsync(session);`
- HAPUS: `SaveChangesAsync()` yang dulu di akhir loop untuk grading (sudah di GradingService)
- PERTAHANKAN: SignalR push per session jika ada

**Step 4 — Hapus GradeFromSavedAnswers() private method (baris ~2492-2549):**
- Grep codebase dulu: `grep -rn "GradeFromSavedAnswers" Controllers/` untuk pastikan tidak ada panggilan lain
- Hapus seluruh method setelah konfirmasi

PENTING: Baca AkhiriUjian dan AkhiriSemuaUjian secara detail sebelum edit. Ada nuansa yang perlu dipertahankan (cache removal, SignalR, audit log). Hanya ganti bagian grading calculation + DB writes yang sudah pindah ke GradingService.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && grep -n "GradeFromSavedAnswers" Controllers/AssessmentAdminController.cs; echo "---"; grep -n "_gradingService.GradeAndCompleteAsync" Controllers/AssessmentAdminController.cs; echo "---"; grep -n "private readonly GradingService" Controllers/AssessmentAdminController.cs; echo "---"; dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "GradeFromSavedAnswers" Controllers/AssessmentAdminController.cs returns NO matches (method deleted)
    - grep "_gradingService.GradeAndCompleteAsync" Controllers/AssessmentAdminController.cs returns at least 2 matches (AkhiriUjian + AkhiriSemuaUjian)
    - grep "private readonly GradingService _gradingService" Controllers/AssessmentAdminController.cs returns match
    - grep "_cache.Remove" Controllers/AssessmentAdminController.cs still returns match (not moved to service)
    - dotnet build succeeds (exit code 0)
  </acceptance_criteria>
  <done>AkhiriUjian dan AkhiriSemuaUjian menggunakan GradingService, GradeFromSavedAnswers dihapus, cache/SignalR tetap di controller</done>
</task>

<task type="auto">
  <name>Task 2: Wire CMPController ke GradingService</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (fokus baris 1394-1638 — SubmitExam)
    - Services/GradingService.cs
  </read_first>
  <action>
**Step 1 — Tambah GradingService ke constructor:**
- Tambah parameter `GradingService gradingService` ke constructor CMPController
- Tambah field `private readonly GradingService _gradingService;`
- Set `_gradingService = gradingService;` di constructor body

**Step 2 — Update SubmitExam() (baris ~1394-1638):**

SubmitExam punya 2 bagian: (A) upsert answers dari form POST ke DB, (B) grading logic.

- PERTAHANKAN bagian (A): upsert PackageUserResponses dari `answers` dictionary ke DB + SaveChangesAsync. Ini HARUS tetap di controller karena GradingService grade dari DB (bukan dari form POST).
- GANTI bagian (B) — semua inline grading logic (hitung skor, ET scores, ExecuteUpdateAsync, TrainingRecord, NomorSertifikat, notifikasi) — dengan: `await _gradingService.GradeAndCompleteAsync(session);`
- PERTAHANKAN: SignalR push `workerSubmitted` ke monitor group — tetap di controller setelah grading
- PERTAHANKAN: Response return (redirect atau JSON)

Titik potong: Setelah `await _context.SaveChangesAsync()` yang menyimpan upserted responses, tambah `await _gradingService.GradeAndCompleteAsync(session);` dan hapus semua inline grading code setelahnya (sampai sebelum SignalR push / response return).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && grep -n "_gradingService.GradeAndCompleteAsync" Controllers/CMPController.cs; echo "---"; grep -n "private readonly GradingService" Controllers/CMPController.cs; echo "---"; dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "_gradingService.GradeAndCompleteAsync" Controllers/CMPController.cs returns at least 1 match
    - grep "private readonly GradingService _gradingService" Controllers/CMPController.cs returns match
    - dotnet build succeeds (exit code 0)
    - CMPController.cs line count decreased (inline grading logic removed)
  </acceptance_criteria>
  <done>SubmitExam menggunakan GradingService untuk grading, answer upsert tetap di controller, SignalR tetap di controller</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller -> GradingService | Same trust level — internal service call, no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-296-05 | Repudiation | Controller refactoring | accept | AuditLog calls preserved in controller — no change to audit trail |
| T-296-06 | Tampering | SubmitExam answer upsert | accept | Answer upsert logic unchanged — stays in controller before GradingService call |
</threat_model>

<verification>
- `dotnet build` sukses
- GradeFromSavedAnswers tidak ditemukan di codebase
- Kedua controller punya `_gradingService.GradeAndCompleteAsync` calls
- Cache removal dan SignalR push tetap di controller
</verification>

<success_criteria>
- FOUND-07: AkhiriUjian menggunakan GradingService
- FOUND-08: AkhiriSemuaUjian menggunakan GradingService
- FOUND-09: SubmitExam menggunakan GradingService
- Tidak ada regression — `dotnet build` sukses
- GradeFromSavedAnswers private method dihapus
</success_criteria>

<output>
After completion, create `.planning/phases/296-data-foundation-gradingservice-extraction/296-03-SUMMARY.md`
</output>
