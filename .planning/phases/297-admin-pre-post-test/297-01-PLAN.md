---
phase: 297-admin-pre-post-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentMonitoringViewModel.cs
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/CreateAssessment.cshtml
autonomous: true
requirements: [PPT-01, PPT-02, PPT-03, PPT-08, PPT-09]

must_haves:
  truths:
    - "HC dapat memilih tipe 'Pre-Post Test' di dropdown saat buat assessment baru"
    - "Dual-section jadwal Pre dan Post muncul saat tipe Pre-Post dipilih"
    - "2 session per peserta (Pre+Post) tersimpan di DB dengan LinkedGroupId dan LinkedSessionId benar"
    - "Pre session selalu GenerateCertificate=false"
    - "Checkbox 'Gunakan paket soal yang sama' muncul dan menampilkan info badge"
  artifacts:
    - path: "Models/AssessmentMonitoringViewModel.cs"
      provides: "Pre-Post fields di MonitoringGroupViewModel"
      contains: "IsPrePostGroup"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "CreateAssessment POST Pre-Post logic"
      contains: "PreTest"
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "Dropdown AssessmentType + dual-section jadwal"
      contains: "ppt-jadwal-section"
  key_links:
    - from: "Views/Admin/CreateAssessment.cshtml"
      to: "Controllers/AssessmentAdminController.cs"
      via: "form POST dengan field PreSchedule, PostSchedule, PreDurationMinutes, PostDurationMinutes"
      pattern: "PreSchedule|PostSchedule"
    - from: "Controllers/AssessmentAdminController.cs"
      to: "Models/AssessmentSession"
      via: "AssessmentType = PreTest/PostTest, LinkedGroupId, LinkedSessionId"
      pattern: "LinkedGroupId.*LinkedSessionId"
---

<objective>
Implementasi fondasi Pre-Post Test: ViewModel extension, CreateAssessment POST backend yang membuat 2 session per peserta, dan CreateAssessment view dengan dropdown tipe + dual-section jadwal.

Purpose: Ini adalah fondasi yang dibutuhkan semua plan berikutnya — tanpa CreateAssessment Pre-Post, tidak ada data untuk monitoring, edit, atau delete.
Output: HC dapat membuat assessment Pre-Post Test, menghasilkan Pre+Post sessions di DB.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/297-admin-pre-post-test/297-CONTEXT.md
@.planning/phases/297-admin-pre-post-test/297-RESEARCH.md
@.planning/phases/297-admin-pre-post-test/297-UI-SPEC.md

<interfaces>
<!-- MonitoringGroupViewModel — existing fields (perlu extend) -->
From Models/AssessmentMonitoringViewModel.cs:
```csharp
public class MonitoringGroupViewModel
{
    public int RepresentativeId { get; set; }
    public string Title { get; set; } = "";
    public string Category { get; set; } = "";
    public DateTime Schedule { get; set; }
    public string GroupStatus { get; set; } = "";
    public int TotalCount { get; set; }
    public int CompletedCount { get; set; }
    public int PassedCount { get; set; }
    public bool IsPackageMode { get; set; }
    public int PendingCount { get; set; }
    public int CancelledCount { get; set; }
    public int InProgressCount { get; set; }
    public bool IsTokenRequired { get; set; }
    public string AccessToken { get; set; } = "";
    public List<MonitoringSessionViewModel> Sessions { get; set; } = new();
}
```

From Models/AssessmentSession.cs (v14.0 kolom baru, sudah ada):
```csharp
public string? AssessmentType { get; set; }       // line 131
public string? AssessmentPhase { get; set; }       // line 136
public int? LinkedGroupId { get; set; }            // line 142
public int? LinkedSessionId { get; set; }          // line 148
public bool HasManualGrading { get; set; } = false; // line 154
```

From Controllers/AssessmentAdminController.cs:
```csharp
// CreateAssessment POST signature (line 711):
public async Task<IActionResult> CreateAssessment(AssessmentSession model, List<string> UserIds, string? RenewalFkMap = null, string? RenewalFkMapType = null)

// CreateAssessment GET signature (line 517):
public async Task<IActionResult> CreateAssessment([FromQuery] List<int>? renewSessionId = null, [FromQuery] List<int>? renewTrainingId = null)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MonitoringGroupViewModel dengan field Pre-Post</name>
  <read_first>
    - Models/AssessmentMonitoringViewModel.cs
  </read_first>
  <files>Models/AssessmentMonitoringViewModel.cs</files>
  <action>
Tambahkan 4 property baru di akhir class `MonitoringGroupViewModel` (sebelum closing brace):

```csharp
// Pre-Post Test support (Phase 297)
public bool IsPrePostGroup { get; set; } = false;
public int? LinkedGroupId { get; set; }
public MonitoringSubRowViewModel? PreSubRow { get; set; }
public MonitoringSubRowViewModel? PostSubRow { get; set; }
```

Tambahkan class baru `MonitoringSubRowViewModel` di file yang sama, setelah `MonitoringSessionViewModel`:

```csharp
public class MonitoringSubRowViewModel
{
    public int RepresentativeId { get; set; }
    public string Phase { get; set; } = "";  // "PreTest" atau "PostTest"
    public DateTime Schedule { get; set; }
    public int DurationMinutes { get; set; }
    public int TotalCount { get; set; }
    public int CompletedCount { get; set; }
    public int PassedCount { get; set; }
    public int PendingCount { get; set; }
    public int InProgressCount { get; set; }
    public int CancelledCount { get; set; }
    public string GroupStatus { get; set; } = "";
}
```

Tidak ada perubahan lain di file ini.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - MonitoringGroupViewModel.cs mengandung `public bool IsPrePostGroup { get; set; } = false;`
    - MonitoringGroupViewModel.cs mengandung `public int? LinkedGroupId { get; set; }`
    - MonitoringGroupViewModel.cs mengandung `public MonitoringSubRowViewModel? PreSubRow { get; set; }`
    - MonitoringGroupViewModel.cs mengandung `public MonitoringSubRowViewModel? PostSubRow { get; set; }`
    - MonitoringGroupViewModel.cs mengandung `public class MonitoringSubRowViewModel`
    - MonitoringSubRowViewModel mengandung `public string Phase { get; set; }`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>MonitoringGroupViewModel memiliki field IsPrePostGroup, LinkedGroupId, PreSubRow, PostSubRow. MonitoringSubRowViewModel class baru tersedia.</done>
</task>

<task type="auto">
  <name>Task 2: CreateAssessment POST — buat 2 session per user untuk Pre-Post</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baris 708-1060 — CreateAssessment POST)
    - Models/AssessmentSession.cs
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Modifikasi `CreateAssessment` POST action (mulai baris 711). Perubahan meliputi:

**1. Tambah parameter POST baru** — ubah signature menjadi:
```csharp
public async Task<IActionResult> CreateAssessment(
    AssessmentSession model, List<string> UserIds,
    string? RenewalFkMap = null, string? RenewalFkMapType = null,
    string? AssessmentTypeInput = null,
    DateTime? PreSchedule = null, int? PreDurationMinutes = null, DateTime? PreExamWindowCloseDate = null,
    DateTime? PostSchedule = null, int? PostDurationMinutes = null, DateTime? PostExamWindowCloseDate = null,
    bool SamePackage = false)
```

**2. Tambah validasi Pre-Post** — setelah validasi existing (setelah baris ~797), tambah block:
```csharp
bool isPrePostMode = AssessmentTypeInput == "PrePostTest";

if (isPrePostMode)
{
    // Validasi field Pre wajib
    if (!PreSchedule.HasValue)
        ModelState.AddModelError("PreSchedule", "Jadwal Pre-Test wajib diisi.");
    if (!PreDurationMinutes.HasValue || PreDurationMinutes <= 0)
        ModelState.AddModelError("PreDurationMinutes", "Durasi Pre-Test harus lebih dari 0.");
    if (PreDurationMinutes > 480)
        ModelState.AddModelError("PreDurationMinutes", "Durasi Pre-Test tidak boleh lebih dari 480 menit.");

    // Validasi field Post wajib
    if (!PostSchedule.HasValue)
        ModelState.AddModelError("PostSchedule", "Jadwal Post-Test wajib diisi.");
    if (!PostDurationMinutes.HasValue || PostDurationMinutes <= 0)
        ModelState.AddModelError("PostDurationMinutes", "Durasi Post-Test harus lebih dari 0.");
    if (PostDurationMinutes > 480)
        ModelState.AddModelError("PostDurationMinutes", "Durasi Post-Test tidak boleh lebih dari 480 menit.");

    // D-06: Schedule Post harus setelah Pre
    if (PreSchedule.HasValue && PostSchedule.HasValue && PostSchedule <= PreSchedule)
        ModelState.AddModelError("PostSchedule", "Jadwal Post-Test harus setelah jadwal Pre-Test.");

    // Override model fields agar validasi standar Schedule/Duration tidak gagal
    if (PreSchedule.HasValue) model.Schedule = PreSchedule.Value;
    if (PreDurationMinutes.HasValue) model.DurationMinutes = PreDurationMinutes.Value;
}
```

**3. Setelah `ModelState.IsValid` check dan sebelum loop user session creation** (sekitar baris 975), tambah branch Pre-Post:
```csharp
if (isPrePostMode)
{
    // FASE 1: Buat Pre sessions
    var preSessions = new List<AssessmentSession>();
    foreach (var userId in UserIds)
    {
        var preSession = new AssessmentSession
        {
            Title = model.Title,
            Category = model.Category,
            Schedule = PreSchedule!.Value,
            DurationMinutes = PreDurationMinutes!.Value,
            ExamWindowCloseDate = PreExamWindowCloseDate,
            Status = "Upcoming",
            PassPercentage = model.PassPercentage,
            AllowAnswerReview = model.AllowAnswerReview,
            IsTokenRequired = model.IsTokenRequired,
            AccessToken = model.AccessToken,
            GenerateCertificate = false,  // D-20: Pre TIDAK generate sertifikat
            ValidUntil = null,
            UserId = userId,
            AssessmentType = "PreTest",
            CreatedBy = currentUser?.Id,
            BannerColor = model.BannerColor
        };
        preSessions.Add(preSession);
    }

    using var pptTransaction = await _context.Database.BeginTransactionAsync();
    try
    {
        _context.AssessmentSessions.AddRange(preSessions);
        await _context.SaveChangesAsync(); // Pre sessions get Ids

        int linkedGroupId = preSessions[0].Id;

        // FASE 2: Buat Post sessions
        var postSessions = new List<AssessmentSession>();
        for (int i = 0; i < UserIds.Count; i++)
        {
            var postSession = new AssessmentSession
            {
                Title = model.Title,
                Category = model.Category,
                Schedule = PostSchedule!.Value,
                DurationMinutes = PostDurationMinutes!.Value,
                ExamWindowCloseDate = PostExamWindowCloseDate,
                Status = "Upcoming",
                PassPercentage = model.PassPercentage,
                AllowAnswerReview = model.AllowAnswerReview,
                IsTokenRequired = model.IsTokenRequired,
                AccessToken = model.AccessToken,
                GenerateCertificate = model.GenerateCertificate, // D-21: pilihan HC
                ValidUntil = model.ValidUntil,
                UserId = UserIds[i],
                AssessmentType = "PostTest",
                LinkedGroupId = linkedGroupId,
                CreatedBy = currentUser?.Id,
                BannerColor = model.BannerColor
            };
            postSessions.Add(postSession);
        }

        _context.AssessmentSessions.AddRange(postSessions);
        await _context.SaveChangesAsync(); // Post sessions get Ids

        // FASE 3: Cross-link LinkedSessionId dan set LinkedGroupId pada Pre
        for (int i = 0; i < preSessions.Count; i++)
        {
            preSessions[i].LinkedGroupId = linkedGroupId;
            preSessions[i].LinkedSessionId = postSessions[i].Id;
            postSessions[i].LinkedSessionId = preSessions[i].Id;
        }
        await _context.SaveChangesAsync();
        await pptTransaction.CommitAsync();

        TempData["Success"] = $"Assessment Pre-Post Test '{model.Title}' berhasil dibuat untuk {UserIds.Count} peserta ({preSessions.Count + postSessions.Count} sesi).";
        return RedirectToAction("ManageAssessment");
    }
    catch (Exception ex)
    {
        await pptTransaction.RollbackAsync();
        var pptLogger = HttpContext.RequestServices.GetRequiredService<ILogger<AdminController>>();
        pptLogger.LogError(ex, "Error creating Pre-Post Test assessment");
        TempData["Error"] = "Gagal membuat assessment Pre-Post Test. Silakan coba lagi.";
        // Re-setup ViewBag dan return view (ikuti pola existing di bawah)
        await SetCategoriesViewBag();
        await SetTrainingCategoryViewBag();
        return View(model);
    }
}
// else: flow standard existing continues below...
```

PENTING: Block `if (isPrePostMode)` harus SEBELUM loop standard existing (baris ~975) dan RETURN di dalamnya, sehingga flow standard tidak terpengaruh.

**4. CreateAssessment GET** — tambah `ViewBag.AssessmentTypeInput = "";` sebelum `return View()` (sekitar baris 706).

Pastikan variable `currentUser` sudah ada di scope (biasanya `var currentUser = await _userManager.GetUserAsync(User);` — cek apakah sudah ada di flow existing dan reuse).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs mengandung `string? AssessmentTypeInput = null`
    - AssessmentAdminController.cs mengandung `DateTime? PreSchedule = null`
    - AssessmentAdminController.cs mengandung `DateTime? PostSchedule = null`
    - AssessmentAdminController.cs mengandung `bool isPrePostMode = AssessmentTypeInput == "PrePostTest"`
    - AssessmentAdminController.cs mengandung `AssessmentType = "PreTest"`
    - AssessmentAdminController.cs mengandung `AssessmentType = "PostTest"`
    - AssessmentAdminController.cs mengandung `GenerateCertificate = false,  // D-20`
    - AssessmentAdminController.cs mengandung `LinkedGroupId = linkedGroupId`
    - AssessmentAdminController.cs mengandung `LinkedSessionId = postSessions[i].Id`
    - AssessmentAdminController.cs mengandung `PostSchedule <= PreSchedule`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>CreateAssessment POST dapat membuat 2 session per peserta (Pre+Post) dengan LinkedGroupId dan LinkedSessionId benar. Pre session GenerateCertificate=false. Validasi jadwal Post > Pre diterapkan.</done>
</task>

<task type="auto">
  <name>Task 3: CreateAssessment view — dropdown AssessmentType + dual-section jadwal + checkbox paket soal</name>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml
    - .planning/phases/297-admin-pre-post-test/297-UI-SPEC.md
  </read_first>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <action>
Modifikasi `CreateAssessment.cshtml` — tambahkan elemen berikut per UI-SPEC dan D-01..D-09:

**1. Dropdown AssessmentType** — tambahkan SEBELUM field Title (sebagai field pertama di form, per UI-SPEC focal point):
```html
<div class="mb-3">
    <label class="form-label fw-semibold">Tipe Assessment</label>
    <select name="AssessmentTypeInput" id="assessmentTypeInput" class="form-select">
        <option value="Standard" selected>Standard</option>
        <option value="PrePostTest">Pre-Post Test</option>
    </select>
</div>
```

**2. Dual-section jadwal** — tambahkan SETELAH field jadwal standar existing, dalam collapsible div:
```html
<div id="ppt-jadwal-section" class="collapse">
    <div class="card mb-3">
        <div class="card-header bg-light fw-semibold">
            <i class="bi bi-clock"></i> Jadwal Pre-Test
        </div>
        <div class="card-body">
            <div class="row g-2">
                <div class="col-md-4">
                    <label class="form-label small fw-semibold text-muted">Tanggal & Waktu</label>
                    <input type="datetime-local" name="PreSchedule" id="preSchedule" class="form-control" />
                </div>
                <div class="col-md-4">
                    <label class="form-label small fw-semibold text-muted">Durasi (menit)</label>
                    <input type="number" name="PreDurationMinutes" id="preDurationMinutes" class="form-control" min="1" max="480" />
                </div>
                <div class="col-md-4">
                    <label class="form-label small fw-semibold text-muted">Batas Waktu Pengerjaan</label>
                    <input type="datetime-local" name="PreExamWindowCloseDate" id="preExamWindowCloseDate" class="form-control" />
                </div>
            </div>
        </div>
    </div>

    <div class="card mb-3">
        <div class="card-header bg-light fw-semibold">
            <i class="bi bi-clock-history"></i> Jadwal Post-Test
        </div>
        <div class="card-body">
            <div class="row g-2">
                <div class="col-md-4">
                    <label class="form-label small fw-semibold text-muted">Tanggal & Waktu</label>
                    <input type="datetime-local" name="PostSchedule" id="postSchedule" class="form-control" />
                </div>
                <div class="col-md-4">
                    <label class="form-label small fw-semibold text-muted">Durasi (menit)</label>
                    <input type="number" name="PostDurationMinutes" id="postDurationMinutes" class="form-control" min="1" max="480" />
                </div>
                <div class="col-md-4">
                    <label class="form-label small fw-semibold text-muted">Batas Waktu Pengerjaan</label>
                    <input type="datetime-local" name="PostExamWindowCloseDate" id="postExamWindowCloseDate" class="form-control" />
                </div>
            </div>
            <div class="mt-3">
                <div class="form-check">
                    <input type="checkbox" name="SamePackage" value="true" id="samePackageCheck" class="form-check-input" />
                    <label class="form-check-label" for="samePackageCheck">Gunakan paket soal yang sama untuk Pre dan Post</label>
                </div>
                <div id="samePackageBadge" class="mt-2 d-none">
                    <span class="badge bg-info"><i class="bi bi-info-circle"></i> Paket soal disamakan dengan Pre-Test</span>
                </div>
            </div>
        </div>
    </div>
</div>
```

**3. Sembunyikan field jadwal standar** saat Pre-Post dipilih — field Schedule, DurationMinutes, ExamWindowCloseDate existing harus wrapped dalam div `id="standard-jadwal-section"`.

**4. JavaScript** — tambahkan di bagian bawah file (dalam @section Scripts atau sebelum closing tag):
```javascript
<script>
document.addEventListener('DOMContentLoaded', function() {
    var typeSelect = document.getElementById('assessmentTypeInput');
    var pptSection = document.getElementById('ppt-jadwal-section');
    var stdSection = document.getElementById('standard-jadwal-section');
    var preScheduleInput = document.getElementById('preSchedule');
    var postScheduleInput = document.getElementById('postSchedule');
    var samePackageCheck = document.getElementById('samePackageCheck');
    var samePackageBadge = document.getElementById('samePackageBadge');

    typeSelect.addEventListener('change', function() {
        if (this.value === 'PrePostTest') {
            pptSection.classList.add('show');
            if (stdSection) stdSection.classList.add('d-none');
        } else {
            pptSection.classList.remove('show');
            if (stdSection) stdSection.classList.remove('d-none');
        }
    });

    // D-06: min date pada Post = value Pre
    preScheduleInput.addEventListener('change', function() {
        postScheduleInput.min = this.value;
    });

    // D-09: checkbox paket soal sama
    samePackageCheck.addEventListener('change', function() {
        samePackageBadge.classList.toggle('d-none', !this.checked);
    });
});
</script>
```

**5. Pada field GenerateCertificate dan ValidUntil existing** — pindahkan ke DALAM card Post-Test (setelah ExamWindowCloseDate Post). Ini per D-03: GenerateCertificate dan ValidUntil hanya relevan untuk Post.

PENTING: Field jadwal standar (Schedule, DurationMinutes, ExamWindowCloseDate) tetap ada dan fungsional untuk mode Standard. Hanya disembunyikan secara visual saat Pre-Post dipilih.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - CreateAssessment.cshtml mengandung `id="assessmentTypeInput"`
    - CreateAssessment.cshtml mengandung `id="ppt-jadwal-section"`
    - CreateAssessment.cshtml mengandung `name="PreSchedule"`
    - CreateAssessment.cshtml mengandung `name="PostSchedule"`
    - CreateAssessment.cshtml mengandung `name="PreDurationMinutes"`
    - CreateAssessment.cshtml mengandung `name="PostDurationMinutes"`
    - CreateAssessment.cshtml mengandung `name="AssessmentTypeInput"`
    - CreateAssessment.cshtml mengandung `id="samePackageCheck"`
    - CreateAssessment.cshtml mengandung `Gunakan paket soal yang sama untuk Pre dan Post`
    - CreateAssessment.cshtml mengandung `postScheduleInput.min = this.value`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Form CreateAssessment menampilkan dropdown AssessmentType, dual-section jadwal Pre/Post saat Pre-Post dipilih, validasi min-date Post > Pre, dan checkbox paket soal sama.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form → CreateAssessment POST | User bisa manipulasi AssessmentTypeInput, Schedule values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-297-01 | Tampering | CreateAssessment POST AssessmentTypeInput | mitigate | Validate AssessmentTypeInput hanya "Standard" atau "PrePostTest"; reject lainnya |
| T-297-02 | Tampering | CreateAssessment POST jadwal Pre/Post | mitigate | Backend enforce PostSchedule > PreSchedule di ModelState validation (D-06) |
| T-297-03 | Spoofing | CreateAssessment POST | mitigate | [ValidateAntiForgeryToken] sudah ada pada action |
</threat_model>

<verification>
1. `dotnet build` passes tanpa error
2. Form CreateAssessment menampilkan dropdown tipe dan dual-section saat Pre-Post dipilih
3. POST CreateAssessment Pre-Post menghasilkan 2 session per user di DB
4. Pre session: AssessmentType="PreTest", GenerateCertificate=false
5. Post session: AssessmentType="PostTest", LinkedGroupId dan LinkedSessionId benar
</verification>

<success_criteria>
- HC dapat memilih "Pre-Post Test" di dropdown dan melihat dual-section jadwal
- Submit form menghasilkan Pre+Post sessions di DB dengan linking benar
- Pre session tidak generate sertifikat (GenerateCertificate=false)
- Build berhasil tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/297-admin-pre-post-test/297-01-SUMMARY.md`
</output>
