---
phase: 297-admin-pre-post-test
plan: 02
type: execute
wave: 2
depends_on: [297-01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/AssessmentMonitoring.cshtml
  - Views/Admin/ManageAssessment.cshtml
autonomous: true
requirements: [PPT-05, PPT-10]

must_haves:
  truths:
    - "Monitoring menampilkan Pre-Post group sebagai 1 baris expandable"
    - "Sub-row Pre dan Post menampilkan stat masing-masing"
    - "ManageAssessment menampilkan Pre-Post sebagai 1 card dengan badge"
    - "Standard assessment tidak terpengaruh"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Monitoring grouping by LinkedGroupId + ManageAssessment grouping"
      contains: "IsPrePostGroup = true"
    - path: "Views/Admin/AssessmentMonitoring.cshtml"
      provides: "Expandable parent row + sub-rows"
      contains: "ppt-sub-row"
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Badge Pre-Post pada card"
      contains: "Pre-Post"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs"
      to: "Views/Admin/AssessmentMonitoring.cshtml"
      via: "MonitoringGroupViewModel dengan IsPrePostGroup dan PreSubRow/PostSubRow"
      pattern: "IsPrePostGroup"
---

<objective>
Implementasi monitoring Pre-Post: backend grouping by LinkedGroupId, view expandable rows, dan ManageAssessment badge.

Purpose: HC dapat memonitor assessment Pre-Post Test dari satu tampilan terpadu.
Output: Monitoring menampilkan Pre-Post group sebagai 1 baris expandable. ManageAssessment menampilkan badge Pre-Post.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/297-admin-pre-post-test/297-CONTEXT.md
@.planning/phases/297-admin-pre-post-test/297-RESEARCH.md
@.planning/phases/297-admin-pre-post-test/297-UI-SPEC.md
@.planning/phases/297-admin-pre-post-test/297-01-SUMMARY.md

<interfaces>
From Models/AssessmentMonitoringViewModel.cs (setelah Plan 01):
```csharp
public class MonitoringGroupViewModel
{
    // ... existing fields ...
    public bool IsPrePostGroup { get; set; } = false;
    public int? LinkedGroupId { get; set; }
    public MonitoringSubRowViewModel? PreSubRow { get; set; }
    public MonitoringSubRowViewModel? PostSubRow { get; set; }
}

public class MonitoringSubRowViewModel
{
    public int RepresentativeId { get; set; }
    public string Phase { get; set; } = "";
    public DateTime Schedule { get; set; }
    public int DurationMinutes { get; set; }
    public int TotalCount { get; set; }
    public int CompletedCount { get; set; }
    public int PassedCount { get; set; }
    public int PendingCount { get; set; }
    public int InProgressCount { get; set; }
    public int CancelledCount { get; set; }
    public string GroupStatus { get; set; } = "";
}
```

From Controllers/AssessmentAdminController.cs — AssessmentMonitoring (baris 1763-1858):
- Query `allSessions` di baris 1788-1805 perlu tambah field `a.AssessmentType` dan `a.LinkedGroupId`
- Grouping di baris 1807-1838 perlu split: Pre-Post by LinkedGroupId, Standard by (Title, Category, Schedule.Date)

From Controllers/AssessmentAdminController.cs — ManageAssessment (baris 60-206):
- Query `allSessions` di baris 87-108 perlu tambah field `AssessmentType` dan `LinkedGroupId`
- Grouping di baris 111-144 perlu split serupa monitoring
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AssessmentMonitoring backend — grouping Pre-Post by LinkedGroupId</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baris 1763-1858 — AssessmentMonitoring action)
    - Models/AssessmentMonitoringViewModel.cs
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Modifikasi `AssessmentMonitoring` action (baris 1763-1858):

**1. Tambah field ke Select** (baris 1790-1805) — tambah 2 field:
```csharp
a.AssessmentType,
a.LinkedGroupId,
a.DurationMinutes,
```

**2. Ganti grouping logic** (baris 1807-1838). Hapus grouping lama dan ganti dengan:
```csharp
// Pisahkan Pre-Post dari Standard (D-33)
var prePostSessions = allSessions.Where(a => a.LinkedGroupId != null).ToList();
var standardSessions = allSessions.Where(a => a.LinkedGroupId == null).ToList();

// Group Pre-Post by LinkedGroupId
var prePostGroups = prePostSessions
    .GroupBy(a => a.LinkedGroupId)
    .Select(g =>
    {
        var preSubs = g.Where(a => a.AssessmentType == "PreTest").ToList();
        var postSubs = g.Where(a => a.AssessmentType == "PostTest").ToList();
        var rep = preSubs.OrderBy(a => a.CreatedAt).FirstOrDefault() ?? g.OrderBy(a => a.CreatedAt).First();

        // D-29: status derived
        string groupStatus;
        if (g.Any(a => a.Status == "Open" || a.Status == "InProgress"))
            groupStatus = "Open";
        else if (g.Any(a => a.Status == "Upcoming"))
            groupStatus = "Upcoming";
        else
            groupStatus = "Closed";

        // Helper untuk sub-row status
        string SubRowStatus(IEnumerable<dynamic> subs) {
            if (subs.Any(a => a.Status == "Open" || a.Status == "InProgress")) return "Open";
            if (subs.Any(a => a.Status == "Upcoming")) return "Upcoming";
            return "Closed";
        }

        return new MonitoringGroupViewModel
        {
            RepresentativeId = rep.Id,
            Title = rep.Title,
            Category = rep.Category,
            Schedule = rep.Schedule,
            GroupStatus = groupStatus,
            // D-11: stat gabungan dari Post
            TotalCount = postSubs.Count > 0 ? postSubs.Count : preSubs.Count,
            CompletedCount = postSubs.Count(a => a.IsCompleted),
            PassedCount = postSubs.Count(a => a.IsPassed),
            PendingCount = postSubs.Count(a => !a.IsCompleted && !a.IsStarted),
            InProgressCount = postSubs.Count(a => a.IsStarted && !a.IsCompleted),
            CancelledCount = g.Count(a => a.Status == "Cancelled"),
            IsTokenRequired = rep.IsTokenRequired,
            AccessToken = rep.AccessToken ?? "",
            IsPrePostGroup = true,
            LinkedGroupId = g.Key,
            PreSubRow = preSubs.Any() ? new MonitoringSubRowViewModel
            {
                RepresentativeId = preSubs.OrderBy(a => a.CreatedAt).First().Id,
                Phase = "PreTest",
                Schedule = preSubs.First().Schedule,
                DurationMinutes = preSubs.First().DurationMinutes,
                TotalCount = preSubs.Count,
                CompletedCount = preSubs.Count(a => a.IsCompleted),
                PassedCount = preSubs.Count(a => a.IsPassed),
                PendingCount = preSubs.Count(a => !a.IsCompleted && !a.IsStarted),
                InProgressCount = preSubs.Count(a => a.IsStarted && !a.IsCompleted),
                CancelledCount = preSubs.Count(a => a.Status == "Cancelled"),
                GroupStatus = SubRowStatus(preSubs)
            } : null,
            PostSubRow = postSubs.Any() ? new MonitoringSubRowViewModel
            {
                RepresentativeId = postSubs.OrderBy(a => a.CreatedAt).First().Id,
                Phase = "PostTest",
                Schedule = postSubs.First().Schedule,
                DurationMinutes = postSubs.First().DurationMinutes,
                TotalCount = postSubs.Count,
                CompletedCount = postSubs.Count(a => a.IsCompleted),
                PassedCount = postSubs.Count(a => a.IsPassed),
                PendingCount = postSubs.Count(a => !a.IsCompleted && !a.IsStarted),
                InProgressCount = postSubs.Count(a => a.IsStarted && !a.IsCompleted),
                CancelledCount = postSubs.Count(a => a.Status == "Cancelled"),
                GroupStatus = SubRowStatus(postSubs)
            } : null
        };
    }).ToList();

// Group Standard by (Title, Category, Schedule.Date) — existing logic
var standardGroups = standardSessions
    .GroupBy(a => (a.Title, a.Category, a.Schedule.Date))
    .Select(g =>
    {
        var rep = g.OrderBy(a => a.CreatedAt).First();
        string groupStatus;
        if (g.Any(a => a.Status == "Open" || a.Status == "InProgress"))
            groupStatus = "Open";
        else if (g.Any(a => a.Status == "Upcoming"))
            groupStatus = "Upcoming";
        else
            groupStatus = "Closed";

        return new MonitoringGroupViewModel
        {
            RepresentativeId = rep.Id,
            Title = rep.Title,
            Category = rep.Category,
            Schedule = rep.Schedule,
            GroupStatus = groupStatus,
            TotalCount = g.Count(),
            CompletedCount = g.Count(a => a.IsCompleted),
            PassedCount = g.Count(a => a.IsPassed),
            PendingCount = g.Count(a => !a.IsCompleted && !a.IsStarted),
            IsTokenRequired = rep.IsTokenRequired,
            AccessToken = rep.AccessToken ?? ""
        };
    }).ToList();

// Gabungkan dan sort
var grouped = prePostGroups.Concat(standardGroups)
    .OrderByDescending(g => g.Schedule)
    .ToList();
```

CATATAN: `SubRowStatus` helper bisa jadi local function di dalam action method. Jika compiler complain tentang `dynamic`, gunakan explicit anonymous type cast — atau buat helper yang menerima `IEnumerable` dengan `Status` property.

PENTING: `a.IsStarted` dan `a.IsCompleted` sudah ada di anonymous type dari Select existing. Pastikan `a.DurationMinutes` ditambahkan ke Select (belum ada).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs di area AssessmentMonitoring mengandung `a.LinkedGroupId`
    - AssessmentAdminController.cs mengandung `var prePostSessions = allSessions.Where(a => a.LinkedGroupId != null)`
    - AssessmentAdminController.cs mengandung `IsPrePostGroup = true`
    - AssessmentAdminController.cs mengandung `PreSubRow =`
    - AssessmentAdminController.cs mengandung `PostSubRow =`
    - AssessmentAdminController.cs mengandung `.Concat(standardGroups)`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Monitoring backend mendeteksi Pre-Post via LinkedGroupId dan menghasilkan MonitoringGroupViewModel dengan PreSubRow dan PostSubRow.</done>
</task>

<task type="auto">
  <name>Task 2: AssessmentMonitoring view — expandable parent row + sub-rows Pre/Post</name>
  <read_first>
    - Views/Admin/AssessmentMonitoring.cshtml
    - .planning/phases/297-admin-pre-post-test/297-UI-SPEC.md
  </read_first>
  <files>Views/Admin/AssessmentMonitoring.cshtml</files>
  <action>
Modifikasi `AssessmentMonitoring.cshtml` — di dalam loop yang render setiap `MonitoringGroupViewModel` item:

**1. Pada parent row**, tambahkan conditional badge dan expand button:
```html
@if (item.IsPrePostGroup)
{
    <span class="badge bg-primary"><i class="bi bi-arrow-left-right"></i> Pre-Post</span>
    <button type="button" class="btn btn-sm btn-link text-primary p-0 ms-2 ppt-expand-btn"
            data-bs-toggle="collapse" data-bs-target="#ppt-sub-@item.RepresentativeId"
            aria-label="Lihat detail Pre dan Post">
        <i class="bi bi-chevron-down"></i>
    </button>
}
```

**2. Setelah parent row**, tambahkan sub-rows (collapse):
```html
@if (item.IsPrePostGroup)
{
    <tr id="ppt-sub-@item.RepresentativeId" class="collapse">
        <td colspan="[colspan]" class="p-0">
            <table class="table table-sm mb-0">
                @if (item.PreSubRow != null)
                {
                    <tr class="bg-light">
                        <td style="padding-left: 24px;">
                            <span class="badge bg-info">Pre-Test</span>
                            @item.PreSubRow.Schedule.ToString("dd MMM yyyy HH:mm")
                            — @item.PreSubRow.DurationMinutes menit
                        </td>
                        <td>@item.PreSubRow.CompletedCount / @item.PreSubRow.TotalCount selesai</td>
                        <td>
                            <a href="@Url.Action("AssessmentMonitoringDetail", new { title = item.Title, category = item.Category, scheduleDate = item.PreSubRow.Schedule.Date.ToString("yyyy-MM-dd") })"
                               class="btn btn-sm btn-outline-info">Detail Pre</a>
                        </td>
                    </tr>
                }
                @if (item.PostSubRow != null)
                {
                    <tr class="bg-light">
                        <td style="padding-left: 24px;">
                            <span class="badge bg-secondary">Post-Test</span>
                            @item.PostSubRow.Schedule.ToString("dd MMM yyyy HH:mm")
                            — @item.PostSubRow.DurationMinutes menit
                        </td>
                        <td>@item.PostSubRow.CompletedCount / @item.PostSubRow.TotalCount selesai</td>
                        <td>
                            <a href="@Url.Action("AssessmentMonitoringDetail", new { title = item.Title, category = item.Category, scheduleDate = item.PostSubRow.Schedule.Date.ToString("yyyy-MM-dd") })"
                               class="btn btn-sm btn-outline-secondary">Detail Post</a>
                        </td>
                    </tr>
                }
            </table>
        </td>
    </tr>
}
```

**3. JavaScript** — di bagian bawah view, tambah toggle chevron:
```javascript
document.querySelectorAll('.ppt-expand-btn').forEach(function(btn) {
    var target = document.querySelector(btn.dataset.bsTarget);
    if (target) {
        target.addEventListener('show.bs.collapse', function() {
            btn.querySelector('i').classList.replace('bi-chevron-down', 'bi-chevron-up');
        });
        target.addEventListener('hide.bs.collapse', function() {
            btn.querySelector('i').classList.replace('bi-chevron-up', 'bi-chevron-down');
        });
    }
});
```

CATATAN: Sesuaikan `colspan` dengan jumlah kolom aktual di tabel monitoring. Baca file terlebih dahulu untuk menentukan jumlah kolom yang tepat.

Per D-13: Aksi bulk (Akhiri Semua Ujian, Reshuffle All) hanya muncul di sub-row, BUKAN di parent row Pre-Post.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentMonitoring.cshtml mengandung `item.IsPrePostGroup`
    - AssessmentMonitoring.cshtml mengandung `ppt-expand-btn`
    - AssessmentMonitoring.cshtml mengandung `badge bg-primary` dengan text "Pre-Post"
    - AssessmentMonitoring.cshtml mengandung `badge bg-info` dengan text "Pre-Test"
    - AssessmentMonitoring.cshtml mengandung `badge bg-secondary` dengan text "Post-Test"
    - AssessmentMonitoring.cshtml mengandung `ppt-sub-@item.RepresentativeId`
    - AssessmentMonitoring.cshtml mengandung `bi-chevron-down`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Monitoring menampilkan Pre-Post group sebagai 1 baris expandable dengan badge, sub-row Pre dan Post dengan stat dan link detail.</done>
</task>

<task type="auto">
  <name>Task 3: ManageAssessment — grouping Pre-Post + badge card</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baris 58-206 — ManageAssessment action)
    - Views/Admin/ManageAssessment.cshtml
  </read_first>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/ManageAssessment.cshtml</files>
  <action>
**A. Controller ManageAssessment** (baris 60-206):

1. Tambah `AssessmentType` dan `LinkedGroupId` ke Select (baris 90-108):
```csharp
a.AssessmentType,
a.LinkedGroupId,
```

2. Ganti grouping (baris 111-144) — split Pre-Post dan Standard sama seperti monitoring:
```csharp
var prePostSessions = allSessions.Where(a => a.LinkedGroupId != null).ToList();
var standardSessions = allSessions.Where(a => a.LinkedGroupId == null).ToList();

// Pre-Post: group by LinkedGroupId, anonymous type tambah IsPrePostGroup = true
var prePostGrouped = prePostSessions
    .GroupBy(a => a.LinkedGroupId)
    .Select(g => {
        var rep = g.Where(a => a.AssessmentType == "PreTest").OrderBy(a => a.CreatedAt).FirstOrDefault() ?? g.OrderBy(a => a.CreatedAt).First();
        string groupStatus;
        if (g.Any(a => a.Status == "Open" || a.Status == "InProgress")) groupStatus = "Open";
        else if (g.Any(a => a.Status == "Upcoming")) groupStatus = "Upcoming";
        else groupStatus = "Closed";
        return new {
            rep.Title, rep.Category, rep.Schedule, rep.ExamWindowCloseDate, rep.DurationMinutes,
            rep.Status, rep.IsTokenRequired, rep.AccessToken, rep.PassPercentage, rep.AllowAnswerReview,
            RepresentativeId = rep.Id,
            Users = g.Where(a => a.AssessmentType == "PreTest").Select(a => new { a.UserFullName, a.UserEmail, a.UserId }).ToList(),
            AllIds = g.Select(a => a.Id).ToList(),
            UserCount = g.Where(a => a.AssessmentType == "PreTest").Count(),
            GroupStatus = groupStatus,
            IsPrePostGroup = true,
            LinkedGroupId = g.Key
        };
    }).ToList();

// Standard: existing logic, tambah IsPrePostGroup = false, LinkedGroupId = (int?)null
var standardGrouped = standardSessions
    .GroupBy(a => (a.Title, a.Category, a.Schedule.Date))
    .Select(g => {
        var rep = g.OrderBy(a => a.CreatedAt).First();
        string groupStatus;
        if (g.Any(a => a.Status == "Open" || a.Status == "InProgress")) groupStatus = "Open";
        else if (g.Any(a => a.Status == "Upcoming")) groupStatus = "Upcoming";
        else groupStatus = "Closed";
        return new {
            rep.Title, rep.Category, rep.Schedule, rep.ExamWindowCloseDate, rep.DurationMinutes,
            rep.Status, rep.IsTokenRequired, rep.AccessToken, rep.PassPercentage, rep.AllowAnswerReview,
            RepresentativeId = rep.Id,
            Users = g.Select(a => new { a.UserFullName, a.UserEmail, a.UserId }).ToList(),
            AllIds = g.Select(a => a.Id).ToList(),
            UserCount = g.Count(),
            GroupStatus = groupStatus,
            IsPrePostGroup = false,
            LinkedGroupId = (int?)null
        };
    }).ToList();

var grouped = prePostGrouped.Concat(standardGrouped)
    .OrderByDescending(g => g.Schedule)
    .ToList();
```

CATATAN: Karena anonymous types harus identik untuk Concat, pastikan kedua Select menghasilkan property yang persis sama (nama dan tipe).

**B. View ManageAssessment** — di card rendering loop:

1. Tambah badge setelah judul assessment card:
```html
@if (item.IsPrePostGroup)
{
    <span class="badge bg-primary rounded-pill ms-2"><i class="bi bi-arrow-left-right"></i> Pre-Post Test</span>
}
```

2. Untuk item Pre-Post, sembunyikan tombol delete individual (D-19) dan tampilkan tombol "Hapus Grup":
```html
@if (item.IsPrePostGroup)
{
    <!-- D-19: delete hanya per-grup -->
    <form method="post" asp-action="DeletePrePostGroup" class="d-inline">
        @Html.AntiForgeryToken()
        <input type="hidden" name="linkedGroupId" value="@item.LinkedGroupId" />
        <button type="submit" class="btn btn-sm btn-outline-danger"
                onclick="return confirm('Tindakan ini akan menghapus kedua sesi (Pre dan Post), semua paket soal, jawaban peserta, dan assignment secara permanen. Lanjutkan?')">
            <i class="bi bi-trash3"></i> Hapus Grup
        </button>
    </form>
}
else
{
    <!-- tombol delete standard existing -->
}
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs di area ManageAssessment mengandung `a.LinkedGroupId`
    - AssessmentAdminController.cs mengandung `IsPrePostGroup = true` di area ManageAssessment
    - AssessmentAdminController.cs mengandung `prePostGrouped.Concat(standardGrouped)`
    - ManageAssessment.cshtml mengandung `item.IsPrePostGroup`
    - ManageAssessment.cshtml mengandung `badge bg-primary` dengan "Pre-Post Test"
    - ManageAssessment.cshtml mengandung `DeletePrePostGroup`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>ManageAssessment menampilkan Pre-Post group sebagai 1 card dengan badge. Standard assessment tidak terpengaruh. Tombol delete individual tersembunyi untuk Pre-Post.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Monitoring view | Data display only, no mutation |
| Client → ManageAssessment delete form | linkedGroupId bisa dimanipulasi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-297-04 | Information Disclosure | Monitoring sub-row | accept | Data sudah filtered by [Authorize(Roles = "Admin, HC")] — semua assessment visible to HC |
| T-297-05 | Tampering | DeletePrePostGroup linkedGroupId | mitigate | Plan 04 implementasi DeletePrePostGroup dengan validasi linkedGroupId exists dan belongs to HC-accessible sessions |
</threat_model>

<verification>
1. `dotnet build` passes
2. Monitoring menampilkan Pre-Post group sebagai 1 baris expandable
3. Sub-row Pre menampilkan badge bg-info "Pre-Test" dan stat
4. Sub-row Post menampilkan badge bg-secondary "Post-Test" dan stat
5. ManageAssessment menampilkan badge Pre-Post pada card
6. Standard assessment tetap tampil normal tanpa perubahan
</verification>

<success_criteria>
- Pre-Post group tampil sebagai 1 baris di monitoring dan 1 card di ManageAssessment
- Expand menampilkan sub-row Pre dan Post dengan stat masing-masing
- Standard assessment grouping tidak terpengaruh
</success_criteria>

<output>
After completion, create `.planning/phases/297-admin-pre-post-test/297-02-SUMMARY.md`
</output>
