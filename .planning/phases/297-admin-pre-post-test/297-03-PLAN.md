---
phase: 297-admin-pre-post-test
plan: 03
type: execute
wave: 2
depends_on: [297-01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/EditAssessment.cshtml
  - Views/Admin/ManagePackages.cshtml
autonomous: true
requirements: [PPT-04, PPT-03]

must_haves:
  truths:
    - "EditAssessment Pre-Post menampilkan tab Pre/Post dengan jadwal terpisah"
    - "Tambah peserta via EditAssessment otomatis buat Pre+Post session"
    - "Hapus peserta via EditAssessment hapus kedua session"
    - "ManagePackages Post menampilkan tombol Copy dari Pre-Test"
    - "Copy paket soal deep-clone Questions+Options dari Pre ke Post"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "EditAssessment Pre-Post GET/POST + CopyPackagesFromPre action"
      contains: "CopyPackagesFromPre"
    - path: "Views/Admin/EditAssessment.cshtml"
      provides: "Tab Pre/Post layout"
      contains: "nav-tabs"
    - path: "Views/Admin/ManagePackages.cshtml"
      provides: "Tombol Copy dari Pre-Test"
      contains: "Copy dari Pre-Test"
  key_links:
    - from: "Views/Admin/EditAssessment.cshtml"
      to: "Controllers/AssessmentAdminController.cs"
      via: "form POST EditAssessment dengan field per-tab"
      pattern: "PreSchedule|PostSchedule"
    - from: "Views/Admin/ManagePackages.cshtml"
      to: "Controllers/AssessmentAdminController.cs"
      via: "form POST CopyPackagesFromPre"
      pattern: "CopyPackagesFromPre"
---

<objective>
Implementasi EditAssessment tab Pre/Post, sinkronisasi peserta, dan ManagePackages tombol Copy dari Pre-Test.

Purpose: HC dapat mengedit jadwal Pre/Post terpisah, mengelola peserta sinkron, dan menyalin paket soal Pre ke Post.
Output: EditAssessment menampilkan tab Pre/Post. ManagePackages Post memiliki tombol Copy.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/297-admin-pre-post-test/297-CONTEXT.md
@.planning/phases/297-admin-pre-post-test/297-RESEARCH.md
@.planning/phases/297-admin-pre-post-test/297-UI-SPEC.md
@.planning/phases/297-admin-pre-post-test/297-01-SUMMARY.md

<interfaces>
From Controllers/AssessmentAdminController.cs — EditAssessment GET (baris 1164-1235):
```csharp
public async Task<IActionResult> EditAssessment(int id)
// Query siblings by Title+Category+Schedule.Date
// ViewBag.AssignedUsers, ViewBag.AssignedUserIds, ViewBag.Users, ViewBag.Sections
```

From Controllers/AssessmentAdminController.cs — ManagePackages (baris 3002-3051):
```csharp
public async Task<IActionResult> ManagePackages(int assessmentId)
// Loads packages with Questions, ViewBag.Packages, ViewBag.AssessmentTitle, ViewBag.AssessmentId
```

From Models/AssessmentPackage.cs:
```csharp
public class AssessmentPackage {
    public int Id { get; set; }
    public int AssessmentSessionId { get; set; }
    public string PackageName { get; set; }
    public int PackageNumber { get; set; }
    public List<PackageQuestion> Questions { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EditAssessment GET/POST — Tab Pre/Post + peserta sync (tambah + hapus)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baris 1160-1490 — EditAssessment GET dan POST)
    - Views/Admin/EditAssessment.cshtml
    - Models/AssessmentSession.cs
  </read_first>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/EditAssessment.cshtml</files>
  <action>
**A. EditAssessment GET** (baris 1164-1235):

Setelah load `assessment` (baris 1166-1168), tambah deteksi Pre-Post:
```csharp
bool isPrePost = assessment.AssessmentType == "PreTest" || assessment.AssessmentType == "PostTest";

if (isPrePost && assessment.LinkedGroupId.HasValue)
{
    // Load semua sessions dalam grup Pre-Post
    var groupSessions = await _context.AssessmentSessions
        .Include(a => a.User)
        .Where(a => a.LinkedGroupId == assessment.LinkedGroupId)
        .ToListAsync();

    var preSessions = groupSessions.Where(a => a.AssessmentType == "PreTest").ToList();
    var postSessions = groupSessions.Where(a => a.AssessmentType == "PostTest").ToList();

    ViewBag.IsPrePostGroup = true;
    ViewBag.PreSession = preSessions.FirstOrDefault();
    ViewBag.PostSession = postSessions.FirstOrDefault();
    ViewBag.PreSessionIds = preSessions.Select(s => s.Id).ToList();
    ViewBag.PostSessionIds = postSessions.Select(s => s.Id).ToList();

    // Override siblings: semua users dalam grup (Pre sessions saja untuk avoid duplicates)
    var groupUserIds = preSessions.Where(a => a.User != null).Select(a => a.UserId).Distinct().ToList();
    ViewBag.AssignedUsers = preSessions.Where(a => a.User != null)
        .Select(a => new { Id = a.Id, FullName = a.User!.FullName ?? "", Email = a.User!.Email ?? "", Section = a.User!.Section ?? "" }).ToList();
    ViewBag.AssignedUserIds = groupUserIds;

    // Package count per phase
    var preIds = preSessions.Select(s => s.Id).ToList();
    var postIds = postSessions.Select(s => s.Id).ToList();
    ViewBag.PrePackageCount = await _context.AssessmentPackages.CountAsync(p => preIds.Contains(p.AssessmentSessionId));
    ViewBag.PostPackageCount = await _context.AssessmentPackages.CountAsync(p => postIds.Contains(p.AssessmentSessionId));
}
else
{
    ViewBag.IsPrePostGroup = false;
}
```

Pastikan block ini SEBELUM query siblings existing (baris 1177). Untuk Pre-Post, skip query siblings standard.

**B. EditAssessment POST** (baris 1237+):

Tambah parameter `DateTime? PreSchedule, int? PreDurationMinutes, DateTime? PreExamWindowCloseDate, DateTime? PostSchedule, int? PostDurationMinutes, DateTime? PostExamWindowCloseDate` ke signature.

Setelah load assessment, tambah branch Pre-Post:
```csharp
if (assessment.AssessmentType == "PreTest" || assessment.AssessmentType == "PostTest")
{
    if (assessment.LinkedGroupId.HasValue)
    {
        var allGroupSessions = await _context.AssessmentSessions
            .Where(a => a.LinkedGroupId == assessment.LinkedGroupId)
            .ToListAsync();

        var preGroup = allGroupSessions.Where(a => a.AssessmentType == "PreTest").ToList();
        var postGroup = allGroupSessions.Where(a => a.AssessmentType == "PostTest").ToList();

        // Update shared fields pada semua sessions
        foreach (var s in allGroupSessions)
        {
            s.Title = model.Title;
            s.Category = model.Category;
            s.PassPercentage = model.PassPercentage;
            s.AllowAnswerReview = model.AllowAnswerReview;
            s.IsTokenRequired = model.IsTokenRequired;
            s.AccessToken = model.IsTokenRequired ? model.AccessToken : "";
            s.UpdatedAt = DateTime.UtcNow;
        }

        // Update per-phase fields
        if (PreSchedule.HasValue)
        {
            foreach (var s in preGroup) {
                s.Schedule = PreSchedule.Value;
                if (PreDurationMinutes.HasValue) s.DurationMinutes = PreDurationMinutes.Value;
                s.ExamWindowCloseDate = PreExamWindowCloseDate;
            }
        }
        if (PostSchedule.HasValue)
        {
            foreach (var s in postGroup) {
                s.Schedule = PostSchedule.Value;
                if (PostDurationMinutes.HasValue) s.DurationMinutes = PostDurationMinutes.Value;
                s.ExamWindowCloseDate = PostExamWindowCloseDate;
                s.GenerateCertificate = model.GenerateCertificate;
                s.ValidUntil = model.ValidUntil;
            }
        }

        // D-31: Tambah peserta baru = buat Pre+Post session
        if (UserIds != null && UserIds.Any())
        {
            var existingUserIds = preGroup.Select(s => s.UserId).ToHashSet();
            var newUserIds = UserIds.Where(u => !existingUserIds.Contains(u)).ToList();

            foreach (var newUserId in newUserIds)
            {
                var repPre = preGroup.First();
                var repPost = postGroup.First();
                var linkedGroupId = assessment.LinkedGroupId!.Value;

                var newPre = new AssessmentSession
                {
                    Title = model.Title,
                    Category = model.Category,
                    Schedule = PreSchedule ?? repPre.Schedule,
                    DurationMinutes = PreDurationMinutes ?? repPre.DurationMinutes,
                    ExamWindowCloseDate = PreExamWindowCloseDate ?? repPre.ExamWindowCloseDate,
                    Status = "Upcoming",
                    PassPercentage = model.PassPercentage,
                    AllowAnswerReview = model.AllowAnswerReview,
                    IsTokenRequired = model.IsTokenRequired,
                    AccessToken = model.IsTokenRequired ? model.AccessToken : "",
                    GenerateCertificate = false, // D-20
                    UserId = newUserId,
                    AssessmentType = "PreTest",
                    LinkedGroupId = linkedGroupId,
                    CreatedBy = (await _userManager.GetUserAsync(User))?.Id,
                    BannerColor = repPre.BannerColor
                };
                var newPost = new AssessmentSession
                {
                    Title = model.Title,
                    Category = model.Category,
                    Schedule = PostSchedule ?? repPost.Schedule,
                    DurationMinutes = PostDurationMinutes ?? repPost.DurationMinutes,
                    ExamWindowCloseDate = PostExamWindowCloseDate ?? repPost.ExamWindowCloseDate,
                    Status = "Upcoming",
                    PassPercentage = model.PassPercentage,
                    AllowAnswerReview = model.AllowAnswerReview,
                    IsTokenRequired = model.IsTokenRequired,
                    AccessToken = model.IsTokenRequired ? model.AccessToken : "",
                    GenerateCertificate = model.GenerateCertificate,
                    ValidUntil = model.ValidUntil,
                    UserId = newUserId,
                    AssessmentType = "PostTest",
                    LinkedGroupId = linkedGroupId,
                    CreatedBy = (await _userManager.GetUserAsync(User))?.Id,
                    BannerColor = repPost.BannerColor
                };

                _context.AssessmentSessions.AddRange(newPre, newPost);
                await _context.SaveChangesAsync();

                newPre.LinkedSessionId = newPost.Id;
                newPost.LinkedSessionId = newPre.Id;
            }

            // D-32: Hapus peserta — hapus kedua session Pre+Post untuk user yang dihapus
            var removedUserIds = existingUserIds.Where(u => !UserIds.Contains(u)).ToList();
            foreach (var removedUserId in removedUserIds)
            {
                // Cari Pre+Post sessions milik user ini dalam grup
                var userPreSession = preGroup.FirstOrDefault(s => s.UserId == removedUserId);
                var userPostSession = postGroup.FirstOrDefault(s => s.UserId == removedUserId);

                // D-32 validasi: tidak bisa hapus jika Pre atau Post sudah InProgress/Completed
                if (userPreSession != null && (userPreSession.Status == "InProgress" || userPreSession.Status == "Completed"))
                {
                    TempData["Error"] = $"Tidak dapat menghapus peserta — sesi Pre-Test sudah {userPreSession.Status}.";
                    continue;
                }
                if (userPostSession != null && (userPostSession.Status == "InProgress" || userPostSession.Status == "Completed"))
                {
                    TempData["Error"] = $"Tidak dapat menghapus peserta — sesi Post-Test sudah {userPostSession.Status}.";
                    continue;
                }

                var sessionsToRemove = new List<AssessmentSession>();
                if (userPreSession != null) sessionsToRemove.Add(userPreSession);
                if (userPostSession != null) sessionsToRemove.Add(userPostSession);

                var sessionIdsToRemove = sessionsToRemove.Select(s => s.Id).ToList();

                // Cascade delete data terkait
                var responses = await _context.PackageUserResponses
                    .Where(r => sessionIdsToRemove.Contains(r.AssessmentSessionId))
                    .ToListAsync();
                if (responses.Any()) _context.PackageUserResponses.RemoveRange(responses);

                var attempts = await _context.AssessmentAttemptHistory
                    .Where(h => sessionIdsToRemove.Contains(h.SessionId))
                    .ToListAsync();
                if (attempts.Any()) _context.AssessmentAttemptHistory.RemoveRange(attempts);

                var packages = await _context.AssessmentPackages
                    .Include(p => p.Questions).ThenInclude(q => q.Options)
                    .Where(p => sessionIdsToRemove.Contains(p.AssessmentSessionId))
                    .ToListAsync();
                foreach (var pkg in packages)
                {
                    foreach (var q in pkg.Questions)
                        _context.PackageOptions.RemoveRange(q.Options);
                    _context.PackageQuestions.RemoveRange(pkg.Questions);
                }
                if (packages.Any()) _context.AssessmentPackages.RemoveRange(packages);

                _context.AssessmentSessions.RemoveRange(sessionsToRemove);
            }
        }

        await _context.SaveChangesAsync();
        TempData["Success"] = "Assessment Pre-Post Test berhasil diperbarui.";
        return RedirectToAction("ManageAssessment");
    }
}
// else: standard edit flow continues...
```

**C. View EditAssessment** — wrap form content dalam tab layout saat Pre-Post:
```html
@if (ViewBag.IsPrePostGroup == true)
{
    <ul class="nav nav-tabs mb-3" role="tablist">
        <li class="nav-item">
            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#tab-pre" type="button">
                Pre-Test <span class="badge bg-secondary ms-1">@ViewBag.PrePackageCount paket</span>
            </button>
        </li>
        <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tab-post" type="button">
                Post-Test <span class="badge bg-secondary ms-1">@ViewBag.PostPackageCount paket</span>
            </button>
        </li>
    </ul>
    <div class="tab-content">
        <div class="tab-pane fade show active" id="tab-pre">
            <!-- Schedule Pre, DurationMinutes Pre, ExamWindowCloseDate Pre -->
            <!-- Link ke ManagePackages Pre: assessmentId = PreSession.Id -->
        </div>
        <div class="tab-pane fade" id="tab-post">
            <!-- Schedule Post, DurationMinutes Post, ExamWindowCloseDate Post -->
            <!-- GenerateCertificate, ValidUntil -->
            <!-- Link ke ManagePackages Post: assessmentId = PostSession.Id -->
        </div>
    </div>
}
```

Shared fields (Title, Category, PassPercentage, dll) tetap di luar tab — ditampilkan sekali.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs mengandung `ViewBag.IsPrePostGroup = true`
    - AssessmentAdminController.cs mengandung `ViewBag.PreSession =`
    - AssessmentAdminController.cs mengandung `ViewBag.PostSession =`
    - AssessmentAdminController.cs mengandung D-32 hapus peserta: `var removedUserIds = existingUserIds.Where(u => !UserIds.Contains(u))`
    - AssessmentAdminController.cs mengandung D-32 validasi status: `userPreSession.Status == "InProgress" || userPreSession.Status == "Completed"`
    - AssessmentAdminController.cs mengandung cascade delete untuk removed sessions: `_context.AssessmentSessions.RemoveRange(sessionsToRemove)`
    - EditAssessment.cshtml mengandung `nav nav-tabs`
    - EditAssessment.cshtml mengandung `tab-pre`
    - EditAssessment.cshtml mengandung `tab-post`
    - EditAssessment.cshtml mengandung `Pre-Test`
    - EditAssessment.cshtml mengandung `Post-Test`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>EditAssessment menampilkan tab Pre/Post untuk Pre-Post assessment. Shared fields di luar tab, per-phase fields di dalam tab. Peserta sync berfungsi: tambah peserta buat Pre+Post session baru, hapus peserta hapus kedua session dengan validasi status dan cascade delete data terkait.</done>
</task>

<task type="auto">
  <name>Task 2: ManagePackages — tombol Copy dari Pre-Test + CopyPackagesFromPre action</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baris 3002-3081 — ManagePackages + CreatePackage)
    - Views/Admin/ManagePackages.cshtml
    - Models/AssessmentPackage.cs
  </read_first>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/ManagePackages.cshtml</files>
  <action>
**A. Controller — ManagePackages GET** (baris 3002-3051):

Setelah load assessment, tambah deteksi Post session:
```csharp
bool isPostSession = assessment.AssessmentType == "PostTest";
ViewBag.IsPostSession = isPostSession;
if (isPostSession && assessment.LinkedSessionId.HasValue)
{
    ViewBag.PreSessionId = assessment.LinkedSessionId.Value;
}
```

**B. Controller — tambah action CopyPackagesFromPre:**
```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> CopyPackagesFromPre(int postSessionId)
{
    var postSession = await _context.AssessmentSessions.FindAsync(postSessionId);
    if (postSession == null || postSession.AssessmentType != "PostTest" || !postSession.LinkedSessionId.HasValue)
    {
        TempData["Error"] = "Sesi Post-Test tidak valid.";
        return RedirectToAction("ManagePackages", new { assessmentId = postSessionId });
    }

    int preSessionId = postSession.LinkedSessionId.Value;

    // Hapus paket Post yang ada
    var existingPostPkgs = await _context.AssessmentPackages
        .Include(p => p.Questions).ThenInclude(q => q.Options)
        .Where(p => p.AssessmentSessionId == postSessionId)
        .ToListAsync();

    foreach (var pkg in existingPostPkgs)
    {
        foreach (var q in pkg.Questions)
            _context.PackageOptions.RemoveRange(q.Options);
        _context.PackageQuestions.RemoveRange(pkg.Questions);
    }
    _context.AssessmentPackages.RemoveRange(existingPostPkgs);

    // Deep clone Pre packages ke Post
    var prePkgs = await _context.AssessmentPackages
        .Include(p => p.Questions).ThenInclude(q => q.Options)
        .Where(p => p.AssessmentSessionId == preSessionId)
        .OrderBy(p => p.PackageNumber)
        .ToListAsync();

    foreach (var prePkg in prePkgs)
    {
        var newPkg = new AssessmentPackage
        {
            AssessmentSessionId = postSessionId,
            PackageName = prePkg.PackageName,
            PackageNumber = prePkg.PackageNumber
        };
        foreach (var q in prePkg.Questions)
        {
            var newQ = new PackageQuestion
            {
                QuestionText = q.QuestionText,
                Order = q.Order,
                ScoreValue = q.ScoreValue,
                QuestionType = q.QuestionType,
                ElemenTeknis = q.ElemenTeknis,
                Options = q.Options.Select(o => new PackageOption
                {
                    OptionText = o.OptionText,
                    IsCorrect = o.IsCorrect
                }).ToList()
            };
            newPkg.Questions.Add(newQ);
        }
        _context.AssessmentPackages.Add(newPkg);
    }

    await _context.SaveChangesAsync();

    TempData["Success"] = $"Berhasil menyalin {prePkgs.Count} paket soal dari Pre-Test.";
    return RedirectToAction("ManagePackages", new { assessmentId = postSessionId });
}
```

**C. View ManagePackages** — tambah tombol Copy jika Post session:
```html
@if (ViewBag.IsPostSession == true)
{
    <div class="mb-3">
        <div id="copyPreConfirm" class="d-none">
            <div class="alert alert-warning d-flex align-items-center gap-2">
                <span>Ini akan menimpa semua paket soal Post-Test yang ada. Lanjutkan?</span>
                <form method="post" asp-action="CopyPackagesFromPre" class="d-inline">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="postSessionId" value="@ViewBag.AssessmentId" />
                    <button type="submit" class="btn btn-sm btn-warning">Konfirmasi</button>
                </form>
                <button type="button" class="btn btn-sm btn-secondary" onclick="document.getElementById('copyPreConfirm').classList.add('d-none'); document.getElementById('copyPreBtn').classList.remove('d-none');">Batal</button>
            </div>
        </div>
        <button type="button" id="copyPreBtn" class="btn btn-outline-info"
                onclick="this.classList.add('d-none'); document.getElementById('copyPreConfirm').classList.remove('d-none');">
            <i class="bi bi-clipboard"></i> Copy dari Pre-Test
        </button>
    </div>
}
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs mengandung `public async Task<IActionResult> CopyPackagesFromPre(int postSessionId)`
    - AssessmentAdminController.cs mengandung `ViewBag.IsPostSession = isPostSession`
    - AssessmentAdminController.cs mengandung `[ValidateAntiForgeryToken]` sebelum CopyPackagesFromPre
    - AssessmentAdminController.cs mengandung `postSession.AssessmentType != "PostTest"`
    - ManagePackages.cshtml mengandung `Copy dari Pre-Test`
    - ManagePackages.cshtml mengandung `copyPreConfirm`
    - ManagePackages.cshtml mengandung `CopyPackagesFromPre`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>ManagePackages Post menampilkan tombol "Copy dari Pre-Test" dengan konfirmasi inline. CopyPackagesFromPre action deep-clone paket soal Pre ke Post.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → EditAssessment POST | User bisa manipulasi jadwal Pre/Post |
| Client → CopyPackagesFromPre | postSessionId bisa di-spoof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-297-06 | Tampering | CopyPackagesFromPre postSessionId | mitigate | Validate postSession.AssessmentType == "PostTest" dan LinkedSessionId exists |
| T-297-07 | Tampering | EditAssessment POST schedule | mitigate | Backend validate PostSchedule > PreSchedule |
| T-297-08 | Elevation | D-32 hapus peserta | mitigate | Validate session tidak InProgress/Completed sebelum allow delete |
</threat_model>

<verification>
1. `dotnet build` passes
2. EditAssessment Pre-Post menampilkan tab Pre/Post
3. Shared fields di luar tab, per-phase fields di dalam tab
4. Tambah peserta buat Pre+Post session baru
5. Hapus peserta menghapus kedua session Pre+Post dengan cascade delete data terkait
6. Hapus peserta diblokir jika Pre atau Post sudah InProgress/Completed
7. Copy paket soal dari Pre ke Post berhasil (deep clone)
</verification>

<success_criteria>
- Tab Pre/Post berfungsi di EditAssessment
- Peserta sync (tambah dan hapus) bekerja untuk Pre-Post dengan validasi status
- Copy paket soal deep-clone tanpa referensi ke Pre
</success_criteria>

<output>
After completion, create `.planning/phases/297-admin-pre-post-test/297-03-SUMMARY.md`
</output>
