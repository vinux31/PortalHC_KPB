---
phase: 297-admin-pre-post-test
plan: 04
type: execute
wave: 3
depends_on: [297-02, 297-03]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PPT-06, PPT-07, PPT-11]

must_haves:
  truths:
    - "DeletePrePostGroup menghapus semua Pre+Post sessions + data terkait tanpa orphan"
    - "Reset Pre diblokir jika Post sudah Completed (D-17)"
    - "DeleteAssessment individual menolak jika session bagian Pre-Post (D-19)"
    - "Renewal dari Post sertifikat bebas pilih tipe (D-23)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "DeletePrePostGroup action + ResetAssessment guard + DeleteAssessment guard"
      contains: "DeletePrePostGroup"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs DeletePrePostGroup"
      to: "DB AssessmentSessions"
      via: "LinkedGroupId query"
      pattern: "LinkedGroupId == linkedGroupId"
    - from: "Controllers/AssessmentAdminController.cs ResetAssessment"
      to: "DB AssessmentSessions LinkedSessionId"
      via: "cek Post Completed sebelum allow reset Pre"
      pattern: "LinkedSessionId.*Completed"
---

<objective>
Implementasi DeletePrePostGroup, guard ResetAssessment untuk Pre-Post, guard DeleteAssessment individual, dan verifikasi renewal flow.

Purpose: Memastikan integritas data Pre-Post — delete atomik, reset dengan proteksi, dan tidak ada orphan record.
Output: Delete, reset, dan renewal untuk Pre-Post berfungsi sesuai D-16..D-24.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/297-admin-pre-post-test/297-CONTEXT.md
@.planning/phases/297-admin-pre-post-test/297-RESEARCH.md
@.planning/phases/297-admin-pre-post-test/297-01-SUMMARY.md
@.planning/phases/297-admin-pre-post-test/297-02-SUMMARY.md

<interfaces>
From Controllers/AssessmentAdminController.cs:
```csharp
// DeleteAssessmentGroup (baris 1588-1687) — pola cascade delete existing:
// 1. Load siblings by Title+Category+Schedule.Date
// 2. Delete PackageUserResponses → AttemptHistory → Packages+Questions+Options → Sessions
// 3. SaveChanges + audit log

// ResetAssessment (baris 2123-2219):
// 1. Load assessment, validate status
// 2. Archive attempt if Completed
// 3. Delete PackageUserResponses + UserPackageAssignment
// 4. ExecuteUpdate reset fields to Open
// 5. Audit log + SignalR notification

// DeleteAssessment (baris 1490-1582):
// 1. Load assessment
// 2. Delete PackageUserResponses → AttemptHistory → Packages+Questions+Options → Session
// 3. SaveChanges + audit log
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeletePrePostGroup action — hapus atomik Pre+Post via LinkedGroupId</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baris 1584-1687 — DeleteAssessmentGroup, sebagai template)
    - Controllers/AssessmentAdminController.cs (baris 1490-1582 — DeleteAssessment)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Tambahkan action baru `DeletePrePostGroup` setelah `DeleteAssessmentGroup` (setelah baris 1687):

```csharp
// --- DELETE PRE-POST GROUP (D-18, D-19) ---
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> DeletePrePostGroup(int linkedGroupId)
{
    var logger = HttpContext.RequestServices.GetRequiredService<ILogger<AdminController>>();

    try
    {
        // Find semua sessions dalam Pre-Post group
        var groupSessions = await _context.AssessmentSessions
            .Where(a => a.LinkedGroupId == linkedGroupId)
            .ToListAsync();

        if (!groupSessions.Any())
        {
            TempData["Error"] = "Grup Pre-Post Test tidak ditemukan.";
            return RedirectToAction("ManageAssessment");
        }

        var groupTitle = groupSessions.First().Title;
        var groupIds = groupSessions.Select(s => s.Id).ToList();

        logger.LogInformation($"DeletePrePostGroup: deleting {groupSessions.Count} sessions for '{groupTitle}' (LinkedGroupId={linkedGroupId})");

        // Cascade delete — ikuti pola DeleteAssessmentGroup:
        // 1. PackageUserResponses
        var allPkgResponses = await _context.PackageUserResponses
            .Where(r => groupIds.Contains(r.AssessmentSessionId))
            .ToListAsync();
        if (allPkgResponses.Any())
            _context.PackageUserResponses.RemoveRange(allPkgResponses);

        // 2. AssessmentAttemptHistory
        var allAttemptHistory = await _context.AssessmentAttemptHistory
            .Where(h => groupIds.Contains(h.SessionId))
            .ToListAsync();
        if (allAttemptHistory.Any())
            _context.AssessmentAttemptHistory.RemoveRange(allAttemptHistory);

        // 3. Packages + Questions + Options
        var allPackages = await _context.AssessmentPackages
            .Include(p => p.Questions).ThenInclude(q => q.Options)
            .Where(p => groupIds.Contains(p.AssessmentSessionId))
            .ToListAsync();
        if (allPackages.Any())
        {
            foreach (var pkg in allPackages)
            {
                foreach (var q in pkg.Questions)
                    _context.PackageOptions.RemoveRange(q.Options);
                _context.PackageQuestions.RemoveRange(pkg.Questions);
            }
            _context.AssessmentPackages.RemoveRange(allPackages);
        }

        // 4. Sessions
        _context.AssessmentSessions.RemoveRange(groupSessions);

        await _context.SaveChangesAsync();

        // Audit log
        try
        {
            var dpgUser = await _userManager.GetUserAsync(User);
            var dpgActorName = string.IsNullOrWhiteSpace(dpgUser?.NIP) ? (dpgUser?.FullName ?? "Unknown") : $"{dpgUser.NIP} - {dpgUser.FullName}";
            await _auditLog.LogAsync(
                dpgUser?.Id ?? "",
                dpgActorName,
                "DeletePrePostGroup",
                $"Deleted Pre-Post group '{groupTitle}' — {groupSessions.Count} session(s) (LinkedGroupId={linkedGroupId})",
                linkedGroupId,
                "AssessmentSession");
        }
        catch (Exception auditEx)
        {
            logger.LogWarning(auditEx, "Audit log write failed for DeletePrePostGroup {LinkedGroupId}", linkedGroupId);
        }

        TempData["Success"] = $"Grup Pre-Post Test '{groupTitle}' dan semua {groupSessions.Count} sesi berhasil dihapus.";
        return RedirectToAction("ManageAssessment");
    }
    catch (Exception ex)
    {
        logger.LogError(ex, "DeletePrePostGroup error for LinkedGroupId {LinkedGroupId}", linkedGroupId);
        TempData["Error"] = "Gagal menghapus grup Pre-Post Test. Silakan coba lagi.";
        return RedirectToAction("ManageAssessment");
    }
}
```

Juga tambah guard di `DeleteAssessment` (baris 1490) — setelah load assessment, SEBELUM cascade delete:
```csharp
// D-19: Block delete individual jika bagian Pre-Post group
if (assessment.AssessmentType == "PreTest" || assessment.AssessmentType == "PostTest")
{
    TempData["Error"] = "Sesi ini bagian dari grup Pre-Post Test. Gunakan 'Hapus Grup' untuk menghapus keduanya.";
    return RedirectToAction("ManageAssessment");
}
```
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs mengandung `public async Task<IActionResult> DeletePrePostGroup(int linkedGroupId)`
    - AssessmentAdminController.cs mengandung `[ValidateAntiForgeryToken]` sebelum DeletePrePostGroup
    - AssessmentAdminController.cs mengandung `a.LinkedGroupId == linkedGroupId`
    - AssessmentAdminController.cs mengandung `_context.AssessmentSessions.RemoveRange(groupSessions)`
    - AssessmentAdminController.cs mengandung `DeletePrePostGroup` di audit log
    - AssessmentAdminController.cs di DeleteAssessment mengandung `Gunakan 'Hapus Grup'`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>DeletePrePostGroup menghapus semua sessions + data cascade via LinkedGroupId. DeleteAssessment individual menolak jika bagian Pre-Post.</done>
</task>

<task type="auto">
  <name>Task 2: ResetAssessment guard — block reset Pre jika Post Completed + Renewal verifikasi</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baris 2120-2220 — ResetAssessment action)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
**1. ResetAssessment guard (D-16, D-17):**

Di `ResetAssessment` action (baris 2123), setelah load assessment dan SEBELUM status validation (baris 2131), tambah:

```csharp
// D-17: Block reset Pre jika Post sudah Completed
if (assessment.AssessmentType == "PreTest" && assessment.LinkedSessionId.HasValue)
{
    var linkedPost = await _context.AssessmentSessions
        .FirstOrDefaultAsync(a => a.Id == assessment.LinkedSessionId);

    if (linkedPost != null && linkedPost.Status == "Completed")
    {
        TempData["Error"] = "Post-Test sudah selesai. Reset Post-Test terlebih dahulu sebelum mereset Pre-Test.";
        return RedirectToAction("AssessmentMonitoringDetail", new {
            title = assessment.Title,
            category = assessment.Category,
            scheduleDate = assessment.Schedule.Date.ToString("yyyy-MM-dd")
        });
    }
}
```

Ini memenuhi D-16 (reset Pre TIDAK cascade ke Post — hanya Pre yang di-reset, Post tetap utuh) karena logic reset existing hanya reset 1 session. Dan D-17 (block reset Pre jika Post Completed).

**2. Renewal verifikasi (D-23, D-24, PPT-11):**

Tidak perlu perubahan kode untuk renewal. D-23 menyatakan "HC bebas pilih tipe" — flow renewal existing di CreateAssessment GET (baris 517+) sudah pre-fill dari source session tanpa memaksa tipe. Dropdown AssessmentType yang sudah ditambahkan di Plan 01 otomatis tersedia saat renewal.

Untuk D-24 (RenewsSessionId pada renewal Post = Id Post session lama): ini sudah ter-handle oleh flow existing — saat HC klik renewal dari Post session, `renewSessionId` = Post session Id, yang akan di-set sebagai `RenewsSessionId` pada session baru.

Tambahkan comment di CreateAssessment POST (di area Pre-Post mode) untuk kejelasan:
```csharp
// D-24: Jika ini renewal Pre-Post, RenewsSessionId di-set pada Post session baru saja
// Pre session baru tidak punya RenewsSessionId (sesuai D-24)
// Logic: model.RenewsSessionId sudah di-bind dari form. Hanya set pada Post session:
```

Di block pembuatan Pre session (Plan 01 Task 2), pastikan:
```csharp
// RenewsSessionId TIDAK di-set pada Pre session (D-24)
// preSession.RenewsSessionId = null; (default)
```

Di block pembuatan Post session (Plan 01 Task 2), pastikan:
```csharp
RenewsSessionId = model.RenewsSessionId, // D-24: renewal FK hanya di Post
RenewsTrainingId = model.RenewsTrainingId,
```

PENTING: Jika `model.RenewsSessionId` atau `model.RenewsTrainingId` sudah di-set di Plan 01 Task 2 untuk KEDUA Pre dan Post, ubah agar hanya Post yang memiliki nilai tersebut.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs di ResetAssessment mengandung `assessment.AssessmentType == "PreTest"`
    - AssessmentAdminController.cs di ResetAssessment mengandung `linkedPost.Status == "Completed"`
    - AssessmentAdminController.cs di ResetAssessment mengandung `Reset Post-Test terlebih dahulu`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Reset Pre diblokir jika Post Completed. Reset Pre tidak cascade ke Post (D-16 — logic existing hanya reset 1 session). Renewal flow mendukung Pre-Post tanpa perubahan major.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → DeletePrePostGroup | linkedGroupId dari form hidden input |
| Client → ResetAssessment | id dari form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-297-09 | Tampering | DeletePrePostGroup linkedGroupId | mitigate | Validate groupSessions exist; [Authorize(Roles = "Admin, HC")] + [ValidateAntiForgeryToken] |
| T-297-10 | Tampering | DeleteAssessment bypass guard | mitigate | Backend check AssessmentType before allowing individual delete |
| T-297-11 | Tampering | ResetAssessment bypass guard | mitigate | Backend check LinkedSessionId + Post status before allowing Pre reset |
</threat_model>

<verification>
1. `dotnet build` passes
2. DeletePrePostGroup menghapus semua sessions + cascade data
3. DeleteAssessment individual menolak untuk session Pre-Post
4. ResetAssessment Pre diblokir jika Post Completed
5. ResetAssessment Pre berhasil jika Post belum Completed (dan tidak cascade ke Post)
</verification>

<success_criteria>
- Delete grup atomik tanpa orphan
- Delete individual blocked untuk Pre-Post
- Reset Pre blocked jika Post Completed
- Renewal flow berfungsi untuk Pre-Post
</success_criteria>

<output>
After completion, create `.planning/phases/297-admin-pre-post-test/297-04-SUMMARY.md`
</output>
