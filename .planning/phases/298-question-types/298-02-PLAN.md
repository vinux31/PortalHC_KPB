---
phase: 298-question-types
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ImportPackageQuestions.cshtml
autonomous: true
requirements: [QTYPE-05, QTYPE-06]
must_haves:
  truths:
    - "Template Excel memiliki kolom QuestionType dan Rubrik"
    - "4 tombol download template tersedia (MC, MA, Essay, Universal)"
    - "Upload file campur MC+MA+Essay berhasil parse dengan tipe benar"
    - "File lama tanpa kolom QuestionType default ke MC (backward compat)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "DownloadQuestionTemplate 4 varian + ImportPackageQuestions parser updated"
      contains: "QuestionType"
    - path: "Views/Admin/ImportPackageQuestions.cshtml"
      provides: "4 tombol download template + info kolom baru"
  key_links:
    - from: "Views/Admin/ImportPackageQuestions.cshtml"
      to: "Controllers/AssessmentAdminController.cs"
      via: "download template + upload import"
      pattern: "DownloadQuestionTemplate"
---

<objective>
Update Excel template download (4 varian) dan import parser untuk mendukung kolom QuestionType + Rubrik, termasuk parsing multi-correct untuk MA dan backward compatibility.

Purpose: HC dapat import soal MA dan Essay secara bulk via Excel — metode utama pembuatan soal massal.
Output: 4 template download, parser updated untuk 3 tipe soal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/298-question-types/298-CONTEXT.md
@.planning/phases/298-question-types/298-RESEARCH.md
@Controllers/AssessmentAdminController.cs
@Views/Admin/ImportPackageQuestions.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DownloadQuestionTemplate — 4 Varian Template</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs line 3160-3210 (existing DownloadQuestionTemplate method — lihat kolom apa saja yang sudah ada, format cell, header row)
  </read_first>
  <action>
Per D-10, D-13, D-14: Update `DownloadQuestionTemplate` untuk menerima parameter `string type` (MC/MA/Essay/Universal).

1. Ubah signature: `public IActionResult DownloadQuestionTemplate(string type = "MC")`

2. Header row (semua template sama — 9 kolom):
   Col 1: "Pertanyaan", Col 2: "Opsi A", Col 3: "Opsi B", Col 4: "Opsi C", Col 5: "Opsi D", Col 6: "Jawaban Benar", Col 7: "Elemen Teknis", Col 8: "QuestionType", Col 9: "Rubrik"

3. Contoh baris per tipe template:
   - **MC template**: Row 2: "Contoh soal MC?" | "Opsi A" | "Opsi B" | "Opsi C" | "Opsi D" | "A" | "K3 Dasar" | "MultipleChoice" | ""
   - **MA template**: Row 2: "Contoh soal MA?" | "Opsi A" | "Opsi B" | "Opsi C" | "Opsi D" | "A,C" | "K3 Dasar" | "MultipleAnswer" | ""
   - **Essay template**: Row 2: "Contoh soal Essay?" | "" | "" | "" | "" | "" | "K3 Dasar" | "Essay" | "Rubrik: Jawaban harus mencakup..."
   - **Universal template**: Semua 3 contoh baris di atas

4. Instruksi sheet (row 2 atau sheet kedua):
   - "QuestionType: MultipleChoice (default jika kosong), MultipleAnswer, atau Essay"
   - "Jawaban Benar MA: isi huruf dipisah koma, contoh: A,C atau A,B,D"
   - "Essay: Opsi A-D dan Jawaban Benar dikosongkan. Rubrik wajib diisi"

5. Filename: `Template_Soal_{type}.xlsx`

Per D-12: Jika kolom QuestionType kosong/tidak ada, default ke "MultipleChoice".
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "string type" Controllers/AssessmentAdminController.cs shows parameter in DownloadQuestionTemplate
    - grep -n "QuestionType" Controllers/AssessmentAdminController.cs shows header "QuestionType" in template generation
    - grep -n "Rubrik" Controllers/AssessmentAdminController.cs shows header "Rubrik" in template generation
    - grep -n "Universal" Controllers/AssessmentAdminController.cs shows Universal template variant handling
  </acceptance_criteria>
  <done>4 varian template Excel tersedia: MC, MA, Essay, Universal — masing-masing dengan 9 kolom dan contoh baris</done>
</task>

<task type="auto">
  <name>Task 2: Update ImportPackageQuestions Parser + View 4 Tombol Download</name>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/ImportPackageQuestions.cshtml</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs line 3226-3460 (existing ImportPackageQuestions POST — parser logic, validRowCount, option creation, cross-package validation)
    - Views/Admin/ImportPackageQuestions.cshtml (existing UI — tombol download, form upload, info text)
  </read_first>
  <action>
**Parser update di ImportPackageQuestions POST (per D-10, D-11, D-12):**

1. Baca kolom 8 (QuestionType) dan kolom 9 (Rubrik):
   ```csharp
   var questionType = row.Cell(8).GetString()?.Trim() ?? "";
   if (string.IsNullOrWhiteSpace(questionType)) questionType = "MultipleChoice"; // D-12 backward compat
   var rubrik = row.Cell(9).GetString()?.Trim();
   ```

2. Validasi QuestionType — hanya terima "MultipleChoice", "MultipleAnswer", "Essay":
   ```csharp
   var validTypes = new[] { "MultipleChoice", "MultipleAnswer", "Essay" };
   if (!validTypes.Contains(questionType)) questionType = "MultipleChoice";
   ```

3. Parse multi-correct untuk MA (per D-11):
   ```csharp
   var correctStr = row.Cell(6).GetString()?.Trim().ToUpper() ?? "";
   var correctLetters = correctStr.Split(',', StringSplitOptions.RemoveEmptyEntries)
       .Select(s => s.Trim())
       .Where(s => new[] { "A", "B", "C", "D" }.Contains(s))
       .ToList();
   ```

4. Buat PackageQuestion dengan QuestionType, Rubrik, MaxCharacters:
   ```csharp
   var newQuestion = new PackageQuestion {
       AssessmentPackageId = packageId,
       QuestionText = questionText,
       QuestionType = questionType,
       Rubrik = questionType == "Essay" ? rubrik : null,
       MaxCharacters = 2000, // default
       ScoreValue = 10,
       ElemenTeknis = elemenTeknis,
       Order = questionOrder++
   };
   ```

5. Buat PackageOption per opsi (skip untuk Essay per D-04):
   ```csharp
   if (questionType != "Essay") {
       // Buat 4 opsi seperti sebelumnya
       // MA: set IsCorrect untuk semua huruf di correctLetters
       // MC: set IsCorrect untuk single correct letter
   }
   ```

6. **Update validRowCount** (PITFALL #2 dari RESEARCH.md):
   - Essay row valid meskipun tidak punya opsi dan Correct kosong
   - Kondisi valid: `questionType == "Essay" ? !string.IsNullOrWhiteSpace(questionText) : (existing condition)`

7. **Cross-package count validation**: Essay ikut dihitung sebagai soal valid.

**View update (per D-13):**
Di `Views/Admin/ImportPackageQuestions.cshtml`, ganti 1 tombol download template dengan 4 tombol:
```html
<div class="btn-group mb-3">
    <a href="@Url.Action("DownloadQuestionTemplate", new { type = "MC" })" class="btn btn-outline-primary btn-sm">
        <i class="bi bi-download me-1"></i>Template MC
    </a>
    <a href="@Url.Action("DownloadQuestionTemplate", new { type = "MA" })" class="btn btn-outline-primary btn-sm">
        <i class="bi bi-download me-1"></i>Template MA
    </a>
    <a href="@Url.Action("DownloadQuestionTemplate", new { type = "Essay" })" class="btn btn-outline-primary btn-sm">
        <i class="bi bi-download me-1"></i>Template Essay
    </a>
    <a href="@Url.Action("DownloadQuestionTemplate", new { type = "Universal" })" class="btn btn-outline-primary btn-sm">
        <i class="bi bi-download me-1"></i>Template Universal
    </a>
</div>
```

Update info text di view untuk menjelaskan format baru.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "row.Cell(8)" Controllers/AssessmentAdminController.cs returns QuestionType parsing
    - grep -n "row.Cell(9)" Controllers/AssessmentAdminController.cs returns Rubrik parsing
    - grep -n "correctLetters" Controllers/AssessmentAdminController.cs returns MA multi-correct parsing
    - grep -n "Template MC" Views/Admin/ImportPackageQuestions.cshtml returns 4 download buttons
    - grep -n "Template Universal" Views/Admin/ImportPackageQuestions.cshtml returns match
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Import parser mendukung 3 tipe soal, multi-correct MA, backward compat file lama, dan 4 tombol download template di view</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC upload Excel -> ImportPackageQuestions | Untrusted file content parsed by ClosedXML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-04 | Tampering | Excel QuestionType cell | mitigate | Server-side whitelist: hanya "MultipleChoice"/"MultipleAnswer"/"Essay", default MC jika invalid |
| T-298-05 | Tampering | Excel Correct cell MA | mitigate | Parse hanya huruf A-D, ignore invalid characters |
| T-298-06 | Denial of Service | Malformed Excel upload | accept | ClosedXML existing error handling sudah adequate |
</threat_model>

<verification>
1. `dotnet build` — 0 errors
2. Download 4 template: masing-masing berisi header 9 kolom + contoh baris sesuai tipe
3. Upload file Universal template → soal MC, MA, Essay tersimpan dengan tipe benar
4. Upload file lama tanpa kolom QuestionType → semua default MC
</verification>

<success_criteria>
- 4 tombol download template berfungsi (MC, MA, Essay, Universal)
- Import file campur berhasil: MC opsi + correct single, MA opsi + correct multi, Essay tanpa opsi + rubrik
- File lama backward compatible (default MC)
</success_criteria>

<output>
After completion, create `.planning/phases/298-question-types/298-02-SUMMARY.md`
</output>
