---
phase: 298-question-types
plan: 05
type: execute
wave: 3
depends_on: [298-03, 298-04]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/Admin/AssessmentMonitoring.cshtml
  - Models/AssessmentMonitoringViewModel.cs
autonomous: false
requirements: [QTYPE-10, QTYPE-11]
must_haves:
  truths:
    - "HC dapat input skor per soal Essay (0 s/d ScoreValue) dari AssessmentMonitoringDetail"
    - "Setelah semua Essay dinilai: recalculate total, update IsPassed, generate sertifikat"
    - "Badge 'Menunggu Penilaian' kuning muncul di AssessmentMonitoring dengan counter Essay pending"
    - "Status berubah dari 'Menunggu Penilaian' ke 'Completed' setelah finalize"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "SubmitEssayScore + FinalizeEssayGrading endpoints"
      contains: "SubmitEssayScore"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Inline essay grading cards"
      contains: "essay-grading-card"
    - path: "Views/Admin/AssessmentMonitoring.cshtml"
      provides: "Badge Menunggu Penilaian + counter"
      contains: "Menunggu Penilaian"
  key_links:
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      to: "Controllers/AssessmentAdminController.cs"
      via: "AJAX POST SubmitEssayScore + FinalizeEssayGrading"
      pattern: "SubmitEssayScore|FinalizeEssayGrading"
---

<objective>
Essay grading admin UI — HC menilai Essay inline di AssessmentMonitoringDetail, sistem recalculate skor total setelah semua Essay dinilai, badge status "Menunggu Penilaian" di monitoring list.

Purpose: HC dapat menyelesaikan penilaian Essay sehingga assessment berstatus final (Completed) dan sertifikat digenerate.
Output: 2 AJAX endpoints baru (SubmitEssayScore, FinalizeEssayGrading), inline grading UI, monitoring badge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/298-question-types/298-CONTEXT.md
@.planning/phases/298-question-types/298-RESEARCH.md
@.planning/phases/298-question-types/298-UI-SPEC.md
@.planning/phases/298-question-types/298-04-SUMMARY.md
@Controllers/AssessmentAdminController.cs
@Views/Admin/AssessmentMonitoringDetail.cshtml
@Views/Admin/AssessmentMonitoring.cshtml
@Models/AssessmentMonitoringViewModel.cs
@Services/GradingService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: SubmitEssayScore + FinalizeEssayGrading Endpoints</name>
  <files>Controllers/AssessmentAdminController.cs, Models/AssessmentMonitoringViewModel.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs line 1863-1900 (AssessmentMonitoringDetail GET — bagaimana sessions di-query, apa yang di-pass ke view)
    - Controllers/AssessmentAdminController.cs line 2050-2120 (existing AJAX POST patterns — ResetSession, DeleteSession — untuk pattern Authorize, ValidateAntiForgeryToken, JSON response)
    - Models/AssessmentMonitoringViewModel.cs (MonitoringSessionViewModel fields)
    - Services/GradingService.cs (for reference on sertifikat generation pattern)
    - Models/PackageUserResponse.cs (EssayScore field)
  </read_first>
  <action>
**1. ViewModel update — AssessmentMonitoringViewModel.cs:**
Tambah ke `MonitoringSessionViewModel`:
```csharp
public bool HasManualGrading { get; set; }
public int EssayPendingCount { get; set; }
```

Update query di AssessmentMonitoringDetail GET untuk populate:
```csharp
HasManualGrading = s.HasManualGrading,
EssayPendingCount = ... // count Essay questions where EssayScore is null
```

**2. Endpoint: SubmitEssayScore (per D-15, D-16)**

```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> SubmitEssayScore(int sessionId, int questionId, int score)
{
    // 1. Load response
    var response = await _context.PackageUserResponses
        .FirstOrDefaultAsync(r => r.AssessmentSessionId == sessionId && r.PackageQuestionId == questionId);
    if (response == null) return Json(new { success = false, message = "Jawaban tidak ditemukan" });

    // 2. Load question untuk validasi ScoreValue
    var question = await _context.PackageQuestions.FindAsync(questionId);
    if (question == null) return Json(new { success = false, message = "Soal tidak ditemukan" });

    // 3. Validasi skor range (T-298-03 dari RESEARCH security domain)
    if (score < 0 || score > question.ScoreValue)
        return Json(new { success = false, message = $"Skor harus antara 0 dan {question.ScoreValue}" });

    // 4. Save EssayScore
    response.EssayScore = score;
    await _context.SaveChangesAsync();

    // 5. Cek berapa Essay masih pending
    var pendingCount = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == sessionId)
        .Join(_context.PackageQuestions.Where(q => q.QuestionType == "Essay"),
            r => r.PackageQuestionId, q => q.Id, (r, q) => r)
        .CountAsync(r => r.EssayScore == null);

    return Json(new { success = true, pendingCount, allGraded = pendingCount == 0 });
}
```

**3. Endpoint: FinalizeEssayGrading (per D-17, D-18)**

```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> FinalizeEssayGrading(int sessionId)
{
    var session = await _context.AssessmentSessions.FindAsync(sessionId);
    if (session == null || session.Status != "Menunggu Penilaian")
        return Json(new { success = false, message = "Session tidak dalam status Menunggu Penilaian" });

    // 1. Cek semua Essay sudah dinilai
    var packageAssignment = await _context.UserPackageAssignments
        .FirstOrDefaultAsync(a => a.AssessmentSessionId == sessionId);
    var shuffledIds = packageAssignment!.GetShuffledQuestionIds();

    var essayQuestions = await _context.PackageQuestions
        .Where(q => shuffledIds.Contains(q.Id) && q.QuestionType == "Essay")
        .ToListAsync();

    var essayResponses = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == sessionId &&
               essayQuestions.Select(q => q.Id).Contains(r.PackageQuestionId))
        .ToListAsync();

    if (essayResponses.Any(r => r.EssayScore == null))
        return Json(new { success = false, message = "Masih ada Essay yang belum dinilai" });

    // 2. Recalculate total score (MC+MA auto + Essay manual)
    var allQuestions = await _context.PackageQuestions
        .Include(q => q.Options)
        .Where(q => shuffledIds.Contains(q.Id))
        .ToListAsync();
    var allResponses = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == sessionId)
        .ToListAsync();

    int totalScore = 0;
    int maxScore = 0;
    foreach (var q in allQuestions)
    {
        maxScore += q.ScoreValue;
        switch (q.QuestionType ?? "MultipleChoice")
        {
            case "MultipleChoice":
                var mcResp = allResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue);
                if (mcResp != null)
                {
                    var opt = q.Options.FirstOrDefault(o => o.Id == mcResp.PackageOptionId!.Value);
                    if (opt != null && opt.IsCorrect) totalScore += q.ScoreValue;
                }
                break;
            case "MultipleAnswer":
                var maSelected = allResponses
                    .Where(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue)
                    .Select(r => r.PackageOptionId!.Value).ToHashSet();
                var maCorrect = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
                if (maSelected.SetEquals(maCorrect)) totalScore += q.ScoreValue;
                break;
            case "Essay":
                var essayResp = allResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id);
                if (essayResp?.EssayScore.HasValue == true) totalScore += essayResp.EssayScore.Value;
                break;
        }
    }

    int finalPercentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;
    bool isPassed = finalPercentage >= session.PassPercentage;

    // 3. Update session: Completed + final score + IsPassed
    await _context.AssessmentSessions
        .Where(s => s.Id == sessionId && s.Status == "Menunggu Penilaian")
        .ExecuteUpdateAsync(s => s
            .SetProperty(r => r.Score, finalPercentage)
            .SetProperty(r => r.Status, "Completed")
            .SetProperty(r => r.IsPassed, isPassed)
        );

    // 4. Generate TrainingRecord (duplicate guard — same as GradingService)
    var judul = $"Assessment: {session.Title}";
    bool trExists = await _context.TrainingRecords.AnyAsync(t =>
        t.UserId == session.UserId && t.Judul == judul && t.Tanggal == session.Schedule);
    if (!trExists)
    {
        _context.TrainingRecords.Add(new TrainingRecord
        {
            UserId = session.UserId,
            Judul = judul,
            Kategori = session.Category ?? "Assessment",
            Tanggal = session.Schedule,
            TanggalSelesai = DateTime.UtcNow,
            Penyelenggara = "Internal",
            Status = isPassed ? "Passed" : "Failed"
        });
        await _context.SaveChangesAsync();
    }

    // 5. Generate sertifikat (jika applicable — same pattern as GradingService)
    if (session.GenerateCertificate && isPassed)
    {
        var certNow = DateTime.Now;
        int certYear = certNow.Year;
        try
        {
            var nextSeq = await CertNumberHelper.GetNextSeqAsync(_context, certYear);
            await _context.AssessmentSessions
                .Where(s => s.Id == sessionId && s.NomorSertifikat == null)
                .ExecuteUpdateAsync(s => s
                    .SetProperty(r => r.NomorSertifikat, CertNumberHelper.Build(nextSeq, certNow)));
        }
        catch (DbUpdateException) { /* retry logic if needed */ }
    }

    // 6. Notify group completion
    await _workerDataService.NotifyIfGroupCompleted(session);

    return Json(new { success = true, score = finalPercentage, isPassed });
}
```

Pastikan `_workerDataService` tersedia via DI (sudah ada di controller constructor).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "SubmitEssayScore" Controllers/AssessmentAdminController.cs returns endpoint method
    - grep -n "FinalizeEssayGrading" Controllers/AssessmentAdminController.cs returns endpoint method
    - grep -n "Authorize.*Admin.*HC" Controllers/AssessmentAdminController.cs returns both endpoints with role auth
    - grep -n "ValidateAntiForgeryToken" Controllers/AssessmentAdminController.cs returns both endpoints with CSRF
    - grep -n "score < 0 || score > question.ScoreValue" Controllers/AssessmentAdminController.cs returns validation
    - grep -n "EssayPendingCount" Models/AssessmentMonitoringViewModel.cs returns new field
  </acceptance_criteria>
  <done>2 AJAX endpoints baru berfungsi: SubmitEssayScore (save skor per soal) dan FinalizeEssayGrading (recalculate + Completed + sertifikat)</done>
</task>

<task type="auto">
  <name>Task 2: Essay Grading UI di AssessmentMonitoringDetail + Badge Monitoring</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml, Views/Admin/AssessmentMonitoring.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (full file — lihat layout per session, bagaimana soal ditampilkan jika ada, section yang bisa di-extend untuk essay grading)
    - Views/Admin/AssessmentMonitoring.cshtml (full file — lihat badge status pattern, bagaimana session status ditampilkan)
    - .planning/phases/298-question-types/298-UI-SPEC.md (komponen essay grading, copywriting)
  </read_first>
  <action>
**1. AssessmentMonitoringDetail — Essay Grading Section (per D-15, D-16, UI-SPEC):**

Untuk setiap session yang HasManualGrading, tampilkan section Essay grading setelah info session:

```html
@if (session.HasManualGrading) {
    <div class="mt-4">
        <h5 class="fw-semibold mb-3">Penilaian Essay</h5>

        @foreach (var essayItem in essayQuestions) {
            <div class="card shadow-sm mb-3 essay-grading-card" id="essay_@essayItem.QuestionId">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start mb-2">
                        <h6 class="fw-semibold">Soal @essayItem.DisplayNumber: @essayItem.QuestionText</h6>
                        <span class="badge @(essayItem.EssayScore.HasValue ? "bg-success" : "bg-secondary") essay-status-badge"
                              id="badge_@essayItem.QuestionId">
                            @(essayItem.EssayScore.HasValue ? "Sudah Dinilai" : "Belum Dinilai")
                        </span>
                    </div>
                    <!-- Jawaban pekerja (read-only) -->
                    <div class="border rounded p-3 bg-light mb-2">
                        <small class="text-muted d-block mb-1">Jawaban Pekerja:</small>
                        <p class="mb-0">@essayItem.TextAnswer</p>
                    </div>
                    <!-- Rubrik (collapsed by default) -->
                    <div class="alert alert-info small mb-2">
                        <a class="text-decoration-none" data-bs-toggle="collapse"
                           href="#rubrik_@essayItem.QuestionId">
                            <i class="bi bi-info-circle me-1"></i>Lihat Rubrik
                        </a>
                        <div class="collapse mt-2" id="rubrik_@essayItem.QuestionId">@essayItem.Rubrik</div>
                    </div>
                    <!-- Input skor -->
                    <div class="d-flex align-items-center gap-2">
                        <label class="form-label mb-0 small">Skor:</label>
                        <input type="number" class="form-control essay-score-input"
                               min="0" max="@essayItem.ScoreValue" style="max-width:80px"
                               value="@essayItem.EssayScore"
                               data-question-id="@essayItem.QuestionId"
                               data-session-id="@session.Id" />
                        <span class="text-muted small">/ @essayItem.ScoreValue</span>
                        <button class="btn btn-primary btn-sm btn-save-essay-score"
                                data-question-id="@essayItem.QuestionId"
                                data-session-id="@session.Id">
                            Simpan Skor
                        </button>
                    </div>
                </div>
            </div>
        }

        <!-- Tombol Selesaikan Penilaian (muncul jika semua Essay sudah dinilai) -->
        <div id="finalizeSection_@session.Id"
             style="display: @(session.EssayPendingCount == 0 ? "block" : "none")">
            <button class="btn btn-success btn-finalize-grading" data-session-id="@session.Id">
                <i class="bi bi-check-circle me-1"></i>Selesaikan Penilaian
            </button>
        </div>
    </div>
}
```

**Controller side**: Update AssessmentMonitoringDetail GET action untuk query essay questions per session:
- Join PackageQuestions (QuestionType == "Essay") dengan PackageUserResponses (TextAnswer + EssayScore)
- Pass ke view via ViewBag atau nested ViewModel

**2. JavaScript AJAX handlers:**

```javascript
// Simpan Skor per soal
document.querySelectorAll('.btn-save-essay-score').forEach(btn => {
    btn.addEventListener('click', async function() {
        const sessionId = this.dataset.sessionId;
        const questionId = this.dataset.questionId;
        const scoreInput = this.closest('.essay-grading-card').querySelector('.essay-score-input');
        const score = parseInt(scoreInput.value);
        const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

        const res = await fetch('/Admin/SubmitEssayScore', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'X-Requested-With': 'XMLHttpRequest' },
            body: `sessionId=${sessionId}&questionId=${questionId}&score=${score}&__RequestVerificationToken=${encodeURIComponent(token)}`
        });
        const data = await res.json();
        if (data.success) {
            document.getElementById('badge_' + questionId).className = 'badge bg-success essay-status-badge';
            document.getElementById('badge_' + questionId).textContent = 'Sudah Dinilai';
            if (data.allGraded) {
                document.getElementById('finalizeSection_' + sessionId).style.display = 'block';
            }
        } else {
            alert(data.message);
        }
    });
});

// Selesaikan Penilaian
document.querySelectorAll('.btn-finalize-grading').forEach(btn => {
    btn.addEventListener('click', async function() {
        if (!confirm('Setelah diselesaikan, status peserta akan diperbarui dan sertifikat akan digenerate. Lanjutkan?')) return;
        const sessionId = this.dataset.sessionId;
        const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

        const res = await fetch('/Admin/FinalizeEssayGrading', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'X-Requested-With': 'XMLHttpRequest' },
            body: `sessionId=${sessionId}&__RequestVerificationToken=${encodeURIComponent(token)}`
        });
        const data = await res.json();
        if (data.success) {
            location.reload(); // Refresh page untuk update status
        } else {
            alert(data.message);
        }
    });
});
```

**3. AssessmentMonitoring — Badge "Menunggu Penilaian" (per D-25):**

Di `Views/Admin/AssessmentMonitoring.cshtml`, di bagian status badge rendering, tambahkan case:
```html
@if (session.Status == "Menunggu Penilaian") {
    <span class="badge bg-warning text-dark">Menunggu Penilaian</span>
    @if (session.EssayPendingCount > 0) {
        <small class="text-muted d-block">@session.EssayPendingCount Essay belum dinilai</small>
    }
}
```

Per D-26: Tidak ada notifikasi khusus — HC lihat dari monitoring page.

**4. PITFALL #3 (Status audit):**
Cari semua tempat di view/controller yang switch/check session.Status. Pastikan "Menunggu Penilaian" tidak break existing logic. Minimal update:
- AssessmentMonitoring.cshtml — badge rendering (update di atas)
- AssessmentMonitoringDetail.cshtml — status display
- Jika ada filter dropdown status: tambahkan opsi "Menunggu Penilaian"
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "essay-grading-card" Views/Admin/AssessmentMonitoringDetail.cshtml returns card elements
    - grep -n "btn-save-essay-score" Views/Admin/AssessmentMonitoringDetail.cshtml returns save button
    - grep -n "btn-finalize-grading" Views/Admin/AssessmentMonitoringDetail.cshtml returns finalize button
    - grep -n "Menunggu Penilaian" Views/Admin/AssessmentMonitoring.cshtml returns badge rendering
    - grep -n "Essay belum dinilai" Views/Admin/AssessmentMonitoring.cshtml returns counter text
    - grep -n "SubmitEssayScore" Views/Admin/AssessmentMonitoringDetail.cshtml returns AJAX call
    - grep -n "FinalizeEssayGrading" Views/Admin/AssessmentMonitoringDetail.cshtml returns AJAX call
  </acceptance_criteria>
  <done>HC dapat input skor Essay inline, badge status Menunggu Penilaian + counter muncul di monitoring, Selesaikan Penilaian mengubah status ke Completed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human Verify — Full Question Types End-to-End</name>
  <action>Full question types feature: HC create/edit soal MA+Essay, Excel import 3 tipe, worker exam UI checkbox+textarea, MA all-or-nothing grading, Essay manual grading flow, monitoring status Menunggu Penilaian</action>
  <verify>
    1. Buka halaman manage questions per package — buat soal MA (centang 2 opsi), buat soal Essay (isi rubrik)
    2. Download 4 template Excel — cek kolom QuestionType + Rubrik ada
    3. Upload template Universal — cek soal MC/MA/Essay tersimpan benar
    4. Mulai ujian sebagai worker — cek checkbox untuk MA, textarea untuk Essay, badge tipe muncul
    5. Submit ujian dengan MC+Essay — cek status "Menunggu Penilaian" di monitoring, IsPassed null
    6. Di monitoring detail — input skor Essay, klik Simpan Skor — badge berubah "Sudah Dinilai"
    7. Klik "Selesaikan Penilaian" — status berubah Completed, sertifikat digenerate (jika applicable)
    8. Submit ujian MA only — cek skor all-or-nothing (pilih semua benar — penuh, miss 1 — 0)
  </verify>
  <done>User approved: semua 8 langkah verifikasi lulus</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC browser -> SubmitEssayScore | Score input untrusted — range validation needed |
| HC browser -> FinalizeEssayGrading | State transition — must verify all essays graded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-13 | Tampering | SubmitEssayScore score | mitigate | Server-side: `score < 0 || score > question.ScoreValue` returns BadRequest |
| T-298-14 | Tampering | FinalizeEssayGrading premature | mitigate | Server checks all essay EssayScore != null before proceeding |
| T-298-15 | Elevation | IDOR session grading | mitigate | [Authorize(Roles = "Admin, HC")] + validate session exists |
| T-298-16 | Replay | Double finalize | mitigate | Status guard: `s.Status == "Menunggu Penilaian"` in ExecuteUpdateAsync WHERE clause |
</threat_model>

<verification>
1. `dotnet build` — 0 errors
2. SubmitEssayScore AJAX: skor tersimpan, badge berubah
3. FinalizeEssayGrading: recalculate benar, status Completed, sertifikat generated
4. Badge "Menunggu Penilaian" kuning muncul di monitoring list
5. Human verify: full end-to-end flow
</verification>

<success_criteria>
- HC input skor Essay 0-ScoreValue, simpan per soal
- Setelah semua dinilai: tombol Selesaikan muncul, klik → Completed + sertifikat
- Badge Menunggu Penilaian + counter di monitoring
- End-to-end flow verified by user
</success_criteria>

<output>
After completion, create `.planning/phases/298-question-types/298-05-SUMMARY.md`
</output>
