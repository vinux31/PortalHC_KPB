---
phase: 299-worker-pre-post-test-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
autonomous: true
requirements:
  - WKPPT-01
  - WKPPT-02
  - WKPPT-03
  - WKPPT-04
  - WKPPT-05
  - WKPPT-06
  - WKPPT-07
must_haves:
  truths:
    - "Assessment() action mengembalikan PairedGroups dan StandaloneExams via ViewBag"
    - "Results() action mengembalikan ComparisonData, GainScorePending, HasComparisonSection via ViewBag saat session PostTest"
    - "Gain score formula benar termasuk edge case PreScore=100"
    - "Pre session ownership divalidasi saat query comparison data"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Assessment() pair grouping + Results() comparison data"
      contains: "ViewBag.PairedGroups"
  key_links:
    - from: "CMPController.Assessment()"
      to: "Views/CMP/Assessment.cshtml"
      via: "ViewBag.PairedGroups, ViewBag.StandaloneExams, ViewBag.CompletedPreSessions"
      pattern: "ViewBag\\.PairedGroups"
    - from: "CMPController.Results()"
      to: "Views/CMP/Results.cshtml"
      via: "ViewBag.ComparisonData, ViewBag.GainScorePending, ViewBag.HasComparisonSection"
      pattern: "ViewBag\\.ComparisonData"
---

<objective>
Extend CMPController dengan data Pre-Post pair grouping di Assessment() dan comparison data di Results().

Purpose: Menyediakan data yang dibutuhkan view untuk render card pair Pre-Post dan section perbandingan gain score.
Output: CMPController.cs dengan Assessment() dan Results() yang sudah extended.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/299-worker-pre-post-test-comparison/299-CONTEXT.md
@.planning/phases/299-worker-pre-post-test-comparison/299-RESEARCH.md
@.planning/phases/299-worker-pre-post-test-comparison/299-UI-SPEC.md

<interfaces>
<!-- From Models/AssessmentSession.cs — fields yang sudah ada sejak Phase 296 -->
AssessmentType (string, nullable) — "PreTest" | "PostTest" | null
LinkedGroupId (Guid?, nullable) — shared group ID untuk pair
LinkedSessionId (Guid?, nullable) — Pre points to Post, Post points to Pre
HasManualGrading (bool) — true jika ada soal Essay
IsPassed (bool?, nullable) — null sampai semua grading selesai

<!-- From Models/SessionElemenTeknisScore.cs -->
AssessmentSessionId (Guid)
ElemenTeknis (string)
QuestionCount (int)
CorrectCount (int)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Assessment() — Pre-Post pair grouping + completed Pre query</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (baca Assessment() action sekitar baris 185-266 — lihat query exams, filter status, ViewBag assignment)
    - Models/AssessmentSession.cs (verify field AssessmentType, LinkedGroupId, LinkedSessionId)
  </read_first>
  <action>
Di CMPController.Assessment() action, SETELAH query `exams` yang existing (sekitar baris 225-229), tambahkan logika pair grouping:

1. Kelompokkan sessions dari `exams` menjadi pair dan standalone:
```csharp
// Pre-Post pair grouping (per D-01)
var prePairs = exams
    .Where(e => !string.IsNullOrEmpty(e.AssessmentType) && e.AssessmentType == "PreTest" && e.LinkedGroupId.HasValue)
    .ToList();

var postPairs = exams
    .Where(e => !string.IsNullOrEmpty(e.AssessmentType) && e.AssessmentType == "PostTest" && e.LinkedGroupId.HasValue)
    .ToList();

var pairedGroups = prePairs
    .Select(pre => new {
        Pre = pre,
        Post = postPairs.FirstOrDefault(p => p.LinkedGroupId == pre.LinkedGroupId)
    })
    .ToList();

// IDs yang sudah masuk pair — exclude dari standalone
var pairedIds = new HashSet<Guid>(
    pairedGroups.SelectMany(pg => new[] { pg.Pre.Id }.Concat(pg.Post != null ? new[] { pg.Post.Id } : Array.Empty<Guid>()))
);

var standaloneExams = exams
    .Where(e => !pairedIds.Contains(e.Id))
    .ToList();

ViewBag.PairedGroups = pairedGroups;
ViewBag.StandaloneExams = standaloneExams;
```

2. Query Pre sessions yang sudah Completed (tidak ada dalam `exams` karena filter exclude Completed) — ini diperlukan agar card Post-Test tahu status Pre-nya (per Open Question #2 di RESEARCH.md):
```csharp
// Query Completed Pre sessions yang punya Post di exams
var postGroupIds = postPairs
    .Where(p => p.LinkedGroupId.HasValue)
    .Select(p => p.LinkedGroupId.Value)
    .ToList();

if (postGroupIds.Any())
{
    var completedPreSessions = await _context.AssessmentSessions
        .Where(s => s.AssessmentType == "PreTest"
            && s.LinkedGroupId.HasValue
            && postGroupIds.Contains(s.LinkedGroupId.Value)
            && s.Status == "Completed"
            && !pairedIds.Contains(s.Id))
        .ToListAsync();

    // Gabung completedPre + existing prePairs ke pairedGroups
    foreach (var completedPre in completedPreSessions)
    {
        var matchingPost = postPairs.FirstOrDefault(p => p.LinkedGroupId == completedPre.LinkedGroupId);
        if (matchingPost != null && !pairedGroups.Any(pg => pg.Pre.Id == completedPre.Id))
        {
            pairedGroups.Add(new { Pre = completedPre, Post = matchingPost });
            // Remove post from standalone jika ada
            standaloneExams.RemoveAll(s => s.Id == matchingPost.Id);
        }
    }

    ViewBag.CompletedPreSessions = completedPreSessions;
}
else
{
    ViewBag.CompletedPreSessions = new List<object>();
}

// Re-assign setelah update
ViewBag.PairedGroups = pairedGroups;
ViewBag.StandaloneExams = standaloneExams;
```

CATATAN: Gunakan tipe anonymous object `new { Pre = ..., Post = ... }` — BUKAN ViewModel baru. Ini konsisten dengan pattern ViewBag existing di controller ini.

CATATAN 2: pairedGroups harus berupa `List<dynamic>` atau anonymous type list agar bisa di-Add() setelah deklarasi awal. Jika C# compiler menolak `.Add()` pada anonymous type list, gunakan `dynamic`:
```csharp
var pairedGroups = new List<dynamic>();
// ... populate dengan new { Pre = pre, Post = post }
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CMPController.cs contains `ViewBag.PairedGroups`
    - CMPController.cs contains `ViewBag.StandaloneExams`
    - CMPController.cs contains `ViewBag.CompletedPreSessions`
    - CMPController.cs contains `e.AssessmentType == "PreTest"`
    - CMPController.cs contains `e.AssessmentType == "PostTest"`
    - CMPController.cs contains `e.LinkedGroupId.HasValue`
    - CMPController.cs contains `s.Status == "Completed"`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Assessment() action mengembalikan PairedGroups (list of {Pre, Post}), StandaloneExams, dan CompletedPreSessions via ViewBag. Build berhasil.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Results() — Comparison data dengan gain score per elemen</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (baca Results() action sekitar baris 1891-2076 — lihat query assessment, viewModel build, return View)
    - Models/SessionElemenTeknisScore.cs (verify field AssessmentSessionId, ElemenTeknis, QuestionCount, CorrectCount)
    - Services/GradingService.cs (verify ET score computation pattern sekitar baris 122-145)
  </read_first>
  <action>
Di CMPController.Results() action, SEBELUM `return View(viewModel);` (akhir action), tambahkan comparison data query:

```csharp
// Pre-Post comparison section (per D-11, D-12, D-13, D-14)
ViewBag.HasComparisonSection = false;
ViewBag.GainScorePending = false;
ViewBag.ComparisonData = null;

if (assessment.AssessmentType == "PostTest" && assessment.LinkedSessionId.HasValue)
{
    var preSessionId = assessment.LinkedSessionId.Value;

    // Security: validasi Pre session milik user yang sama (T-299-01)
    var preSession = await _context.AssessmentSessions
        .FirstOrDefaultAsync(s => s.Id == preSessionId);

    if (preSession != null && preSession.UserId == assessment.UserId)
    {
        bool gainPending = assessment.HasManualGrading && assessment.IsPassed == null;

        var preEtScores = await _context.SessionElemenTeknisScores
            .Where(s => s.AssessmentSessionId == preSessionId)
            .ToListAsync();

        var postEtScores = await _context.SessionElemenTeknisScores
            .Where(s => s.AssessmentSessionId == assessment.Id)
            .ToListAsync();

        if (preEtScores.Any() && postEtScores.Any())
        {
            var comparisonRows = postEtScores
                .Select(post => {
                    var pre = preEtScores.FirstOrDefault(p => p.ElemenTeknis == post.ElemenTeknis);
                    double preScore = pre != null && pre.QuestionCount > 0
                        ? Math.Round((double)pre.CorrectCount / pre.QuestionCount * 100, 1) : 0;
                    double postScore = post.QuestionCount > 0
                        ? Math.Round((double)post.CorrectCount / post.QuestionCount * 100, 1) : 0;

                    // Gain score formula (D-16): (Post - Pre) / (100 - Pre) * 100
                    // Edge case D-16: PreScore >= 100 → Gain = 100
                    // Edge case D-17: Essay pending → null
                    double? gainScore = null;
                    if (!gainPending)
                    {
                        gainScore = preScore >= 100
                            ? 100
                            : Math.Round((postScore - preScore) / (100 - preScore) * 100, 1);
                    }

                    return new {
                        ElemenTeknis = post.ElemenTeknis,
                        PreScore = preScore,
                        PostScore = postScore,
                        GainScore = gainScore
                    };
                })
                .OrderBy(r => r.ElemenTeknis)
                .ToList();

            ViewBag.ComparisonData = comparisonRows;
            ViewBag.GainScorePending = gainPending;
            ViewBag.HasComparisonSection = comparisonRows.Any();
        }
    }
}
```

PENTING — Security validation: `preSession.UserId == assessment.UserId` WAJIB ada untuk mencegah IDOR (T-299-01). Tanpa ini, worker bisa manipulasi LinkedSessionId untuk melihat comparison data milik worker lain.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CMPController.cs contains `ViewBag.HasComparisonSection`
    - CMPController.cs contains `ViewBag.GainScorePending`
    - CMPController.cs contains `ViewBag.ComparisonData`
    - CMPController.cs contains `assessment.AssessmentType == "PostTest"`
    - CMPController.cs contains `assessment.LinkedSessionId.HasValue`
    - CMPController.cs contains `preSession.UserId == assessment.UserId`
    - CMPController.cs contains `preScore >= 100`
    - CMPController.cs contains `(postScore - preScore) / (100 - preScore) * 100`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Results() action mengembalikan ComparisonData dengan gain score per elemen, GainScorePending flag, dan HasComparisonSection flag via ViewBag. Security check preSession.UserId == assessment.UserId ada. Build berhasil.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker browser → CMPController | Worker bisa manipulasi request parameter (session ID) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-299-01 | Spoofing | CMPController.Results() | mitigate | Validate `preSession.UserId == assessment.UserId` sebelum return comparison data — mencegah IDOR via LinkedSessionId manipulasi |
| T-299-02 | Information Disclosure | ViewBag.ComparisonData | mitigate | Hanya populate ComparisonData jika preSession milik user yang sama; null-check preSession sebelum akses |
| T-299-03 | Tampering | Assessment() query | accept | Query read-only, tidak ada write operation. LinkedGroupId dari DB, bukan user input |
</threat_model>

<verification>
- `dotnet build` berhasil tanpa error
- CMPController.cs contains ViewBag.PairedGroups, ViewBag.StandaloneExams, ViewBag.ComparisonData
- Gain score formula handles PreScore=100 edge case
- Security check preSession.UserId == assessment.UserId present
</verification>

<success_criteria>
- Assessment() mengembalikan data pair grouping yang benar untuk Pre-Post sessions
- Results() mengembalikan comparison data dengan gain score per elemen kompetensi
- Edge case PreScore=100 menghasilkan Gain=100 (bukan DivisionByZero)
- Essay pending menghasilkan gainScore=null
- Build berhasil tanpa error
</success_criteria>

<output>
After completion, create `.planning/phases/299-worker-pre-post-test-comparison/299-01-SUMMARY.md`
</output>
