---
phase: 299-worker-pre-post-test-comparison
plan: 02
type: execute
wave: 2
depends_on:
  - 299-01
files_modified:
  - Views/CMP/Assessment.cshtml
  - Views/CMP/Results.cshtml
autonomous: false
requirements:
  - WKPPT-01
  - WKPPT-02
  - WKPPT-03
  - WKPPT-04
  - WKPPT-05
  - WKPPT-06
  - WKPPT-07
must_haves:
  truths:
    - "Pre-Post card pair tampil sebagai 2 card terhubung dengan badge dan arrow connector"
    - "Post-Test card disabled saat Pre belum Completed"
    - "Post-Test card menampilkan pesan 'Pre-Test tidak diselesaikan' saat Pre expired"
    - "Riwayat Ujian menampilkan badge Pre-Test/Post-Test"
    - "Results Post-Test menampilkan section perbandingan dengan gain score per elemen"
    - "Gain score positif hijau, negatif merah, 0 abu-abu, pending dash"
    - "Inner cards dalam pair wrapper TIDAK punya class assessment-card"
    - "ARIA label pada arrow connector untuk screen reader"
  artifacts:
    - path: "Views/CMP/Assessment.cshtml"
      provides: "Pre-Post card pair + Riwayat badge"
      contains: "PairedGroups"
    - path: "Views/CMP/Results.cshtml"
      provides: "Comparison section dengan gain score"
      contains: "ComparisonData"
  key_links:
    - from: "Assessment.cshtml"
      to: "ViewBag.PairedGroups"
      via: "Razor loop"
      pattern: "PairedGroups"
    - from: "Results.cshtml"
      to: "ViewBag.ComparisonData"
      via: "Razor conditional"
      pattern: "HasComparisonSection"
---

<objective>
Render Pre-Post card pair di Assessment.cshtml (cards, blocking, tab filtering, riwayat badge) dan comparison section di Results.cshtml.

Purpose: Worker dapat melihat Pre-Post assessment pair dan perbandingan gain score setelah Post-Test selesai.
Output: Assessment.cshtml dan Results.cshtml yang sudah extended.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/299-worker-pre-post-test-comparison/299-CONTEXT.md
@.planning/phases/299-worker-pre-post-test-comparison/299-RESEARCH.md
@.planning/phases/299-worker-pre-post-test-comparison/299-UI-SPEC.md
@.planning/phases/299-worker-pre-post-test-comparison/299-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assessment.cshtml — Pre-Post card pair + blocking + tab filtering + Riwayat badge</name>
  <files>Views/CMP/Assessment.cshtml</files>
  <read_first>
    - Views/CMP/Assessment.cshtml (SELURUH FILE — baca card loop sekitar baris 138-291, tab filtering JS sekitar baris 545, Riwayat Ujian table, dan semua JS event handlers yang menggunakan class .assessment-card atau .assessment-card-item)
    - .planning/phases/299-worker-pre-post-test-comparison/299-UI-SPEC.md (component inventory, state matrix, copywriting contract)
    - .planning/phases/299-worker-pre-post-test-comparison/299-01-SUMMARY.md (verify ViewBag names dari Plan 01)
  </read_first>
  <action>
**REVIEW FIX — CRITICAL: JS event binding audit (concern HIGH dari review)**

Sebelum modifikasi, BACA dan audit semua JavaScript di Assessment.cshtml yang menggunakan:
- `document.querySelectorAll('.assessment-card')` — digunakan di filterCards() baris 545 untuk tab filtering
- `document.querySelectorAll('.assessment-card-item')` — digunakan untuk hover effects di CSS
- `document.querySelectorAll('.btn-start-standard')` — digunakan untuk click handler start assessment
- Cari juga event delegation patterns yang mungkin menggunakan `.closest('.assessment-card')`

HASIL AUDIT (pre-verified): `.assessment-card` digunakan HANYA di `filterCards()` (baris 545) untuk `querySelectorAll` + hidden toggle. `.assessment-card-item` digunakan HANYA di CSS untuk hover effect. `.btn-start-standard` click handler didelegasikan — ini TETAP berfungsi karena inner cards masih punya `.btn-start-standard`.

KESIMPULAN: Inner cards dalam pair wrapper AMAN untuk TIDAK punya class `.assessment-card`. Hanya outer wrapper pair yang punya `.assessment-card`. Inner cards TETAP punya `.assessment-card-item` untuk CSS hover.

---

Modifikasi Assessment.cshtml di 3 area:

**AREA 1: Card rendering — tambah pair loop SEBELUM standalone loop**

Di dalam area card grid (setelah `<div class="row g-4">` atau equivalent), SEBELUM loop `@foreach` existing:

A) Render paired groups DULU (dari `ViewBag.PairedGroups`):
```razor
@* === Pre-Post Card Pairs (D-01, D-02, D-03, D-04, D-05, D-06) === *@
@if (ViewBag.PairedGroups != null)
{
    foreach (var pair in ViewBag.PairedGroups)
    {
        var pre = pair.Pre;
        var post = pair.Post;

        // Blocking detection (D-04, D-05, D-06)
        bool preCompleted = ((string)pre.Status) == "Completed";
        bool preExpired = !preCompleted
            && pre.ExamWindowCloseDate != null
            && ((DateTime)pre.ExamWindowCloseDate) < DateTime.UtcNow.AddHours(7);
        bool postBlocked = !preCompleted;
        bool postBlockedByExpiry = preExpired;

        // Tab filtering (D-08): pair uses Post status for data-status
        string pairStatus = post != null ? ((string)post.Status)?.ToLower() ?? "upcoming" : "upcoming";

        <!-- Wrapper pair - satu entry untuk tab filtering. ARIA label untuk accessibility (review fix) -->
        <div class="col-12 assessment-card" data-status="@pairStatus" role="group" aria-label="Pre-Post Test: @pre.Title">
            <div class="row g-3 align-items-stretch">

                <!-- Pre-Test Card (D-01, D-02, D-03) -->
                <div class="col-12 col-md-5">
                    <div class="card h-100 border-0 shadow-sm assessment-card-item border-start border-4 border-primary" style="border-radius: 12px;">
                        <div class="card-body d-flex flex-column p-4">
                            <div class="mb-3">
                                <span class="badge bg-primary">@pre.Category</span>
                                <span class="badge bg-info text-dark ms-1">Pre-Test</span>
                            </div>
                            <h5 class="card-title fw-bold mb-3">@pre.Title</h5>
                            <div class="assessment-meta mb-3">
                                @* PENTING: copy EXACT meta items pattern dari loop existing *@
                                @* Biasanya: tanggal, durasi, status badge *@
                            </div>
                            <div class="mt-auto">
                                @if (((string)pre.Status) == "Completed")
                                {
                                    <span class="badge bg-success w-100 py-2">Completed</span>
                                }
                                else if (((string)pre.Status) == "Open" || ((string)pre.Status) == "InProgress")
                                {
                                    <button class="btn btn-primary w-100 btn-start-standard" data-id="@pre.Id">
                                        Mulai Pre-Test <i class="bi bi-arrow-right ms-1"></i>
                                    </button>
                                }
                                else
                                {
                                    <button class="btn btn-outline-secondary w-100" disabled>
                                        Opens @(((DateTime?)pre.ExamWindowOpenDate)?.ToString("dd MMM yyyy, HH:mm", new System.Globalization.CultureInfo("id-ID"))) WIB
                                    </button>
                                }
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Arrow Connector (D-03) with ARIA (review fix: accessibility) -->
                <div class="col-12 col-md-2 d-flex align-items-center justify-content-center" aria-label="Pre-Test menuju Post-Test" role="img">
                    <i class="bi bi-arrow-right-circle-fill text-primary fs-4 d-none d-md-flex" aria-hidden="true"></i>
                    <i class="bi bi-arrow-down-circle-fill text-primary fs-4 d-flex d-md-none" aria-hidden="true"></i>
                </div>

                <!-- Post-Test Card (D-04, D-05, D-06) -->
                <div class="col-12 col-md-5">
                    @if (post != null)
                    {
                        <div class="card h-100 border-0 shadow-sm assessment-card-item border-start border-4 border-primary @(postBlocked ? "opacity-50" : "")" style="border-radius: 12px;">
                            <div class="card-body d-flex flex-column p-4">
                                <div class="mb-3">
                                    <span class="badge bg-primary">@post.Category</span>
                                    <span class="badge bg-primary ms-1">Post-Test</span>
                                </div>
                                <h5 class="card-title fw-bold mb-3">@post.Title</h5>
                                <div class="assessment-meta mb-3">
                                    @* PENTING: copy EXACT meta items pattern dari loop existing *@
                                </div>
                                <div class="mt-auto">
                                    @if (postBlockedByExpiry)
                                    {
                                        <span class="badge bg-danger w-100 py-2">Pre-Test tidak diselesaikan</span>
                                    }
                                    else if (postBlocked)
                                    {
                                        <button class="btn btn-secondary w-100" disabled>
                                            Selesaikan Pre-Test terlebih dahulu
                                        </button>
                                    }
                                    else if (((string)post.Status) == "Upcoming" || (post.ExamWindowOpenDate != null && ((DateTime)post.ExamWindowOpenDate) > DateTime.UtcNow.AddHours(7)))
                                    {
                                        <button class="btn btn-outline-secondary w-100" disabled>
                                            Opens @(((DateTime?)post.ExamWindowOpenDate)?.ToString("dd MMM yyyy, HH:mm", new System.Globalization.CultureInfo("id-ID"))) WIB
                                        </button>
                                    }
                                    else
                                    {
                                        <button class="btn btn-primary w-100 btn-start-standard" data-id="@post.Id">
                                            Mulai Post-Test <i class="bi bi-arrow-right ms-1"></i>
                                        </button>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                </div>

            </div>
        </div>
    }
}
```

B) Ubah loop card existing: ganti `foreach (var item in exams)` (atau nama variable existing) menjadi `foreach (var item in ViewBag.StandaloneExams)`. Loop body TETAP SAMA PERSIS — hanya sumber data berubah.

PENTING (Pitfall 3 — REVIEW CRITICAL): Inner cards dalam pair wrapper HANYA punya class `assessment-card-item` (untuk CSS hover). TIDAK punya class `assessment-card`. Outer wrapper `<div class="col-12 assessment-card" ...>` adalah satu-satunya yang punya `assessment-card`.

**AREA 2: Riwayat Ujian table badge (D-09, D-10)**

Di tabel Riwayat Ujian, cari kolom yang menampilkan judul assessment. Tambahkan badge sebelum title:
```razor
<td class="fw-semibold">
    @if (!string.IsNullOrEmpty((string)item.AssessmentType))
    {
        if ((string)item.AssessmentType == "PreTest")
        {
            <span class="badge bg-info text-dark me-1">Pre-Test</span>
        }
        else if ((string)item.AssessmentType == "PostTest")
        {
            <span class="badge bg-primary me-1">Post-Test</span>
        }
    }
    @item.Title
</td>
```

**AREA 3: Tab filtering — TIDAK perlu ubah JS**

JS `filterCards()` existing sudah check `data-status` pada `.assessment-card`. Pair wrapper sudah punya class `assessment-card` dan `data-status` dari status Post — JS filter otomatis bekerja. TIDAK perlu modifikasi JS.

**AREA 4: Mobile responsive (review fix)**

Tambahkan CSS rule di `<style>` section existing di Assessment.cshtml:
```css
/* Pre-Post pair card responsive fix (review) */
@media (max-width: 767.98px) {
    .assessment-card .row.g-3 .col-md-5 {
        /* Full width on mobile — already handled by col-12 */
    }
    .assessment-card .row.g-3 > [role="img"] {
        padding: 0.25rem 0;
    }
}
```

Arrow connector sudah responsive via `d-none d-md-flex` / `d-flex d-md-none` (horizontal on desktop, vertical on mobile).

**CATATAN PENTING tentang meta items:** Baca pola `assessment-meta` existing di card loop dan replicate EXACTLY untuk kedua card Pre dan Post. Item yang biasanya ada: tanggal (ExamWindowOpenDate/CloseDate), durasi, status badge. Copy HTML structure dari loop existing.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Assessment.cshtml contains `ViewBag.PairedGroups`
    - Assessment.cshtml contains `ViewBag.StandaloneExams`
    - Assessment.cshtml contains `badge bg-info text-dark`
    - Assessment.cshtml contains `Pre-Test`
    - Assessment.cshtml contains `Post-Test`
    - Assessment.cshtml contains `Selesaikan Pre-Test terlebih dahulu`
    - Assessment.cshtml contains `Pre-Test tidak diselesaikan`
    - Assessment.cshtml contains `bi-arrow-right-circle-fill`
    - Assessment.cshtml contains `bi-arrow-down-circle-fill`
    - Assessment.cshtml contains `opacity-50`
    - Assessment.cshtml contains `border-start border-4 border-primary`
    - Assessment.cshtml contains `role="group"`
    - Assessment.cshtml contains `aria-label`
    - Inner card divs within pair DO NOT contain class `assessment-card` (only `assessment-card-item`)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Assessment.cshtml menampilkan Pre-Post card pair dengan badge, arrow connector (dengan ARIA label), blocking logic (Pre belum selesai / expired), tab filtering via wrapper data-status, Riwayat badge, dan mobile responsive layout. Inner cards TIDAK punya class assessment-card. Build berhasil.</done>
</task>

<task type="auto">
  <name>Task 2: Results.cshtml — Section perbandingan Pre-Post dengan gain score</name>
  <files>Views/CMP/Results.cshtml</files>
  <read_first>
    - Views/CMP/Results.cshtml (SELURUH FILE — lihat layout existing, elemen teknis section, posisi sebelum detail soal)
    - .planning/phases/299-worker-pre-post-test-comparison/299-UI-SPEC.md (section perbandingan component, gain score rendering)
    - .planning/phases/299-worker-pre-post-test-comparison/299-01-SUMMARY.md (verify ViewBag names: ComparisonData, GainScorePending, HasComparisonSection)
  </read_first>
  <action>
Di Results.cshtml, SEBELUM detail soal/jawaban section (sebelum tabel jawaban atau section ElemenTeknis existing), tambahkan section perbandingan:

```razor
@* === Section Perbandingan Pre-Post Test (D-11, D-13, D-14) === *@
@if (ViewBag.HasComparisonSection == true)
{
    <div class="card border-0 shadow-sm mb-4">
        <div class="card-header bg-light fw-bold">
            <i class="bi bi-bar-chart-line me-2" aria-hidden="true"></i> Perbandingan Pre-Post Test
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-bordered mb-0" aria-label="Perbandingan skor Pre-Test dan Post-Test per elemen kompetensi">
                    <thead class="table-light">
                        <tr>
                            <th>Elemen Kompetensi</th>
                            <th class="text-center" style="width: 150px;">Skor Pre</th>
                            <th class="text-center" style="width: 150px;">Skor Post</th>
                            <th class="text-center" style="width: 150px;">Gain Score</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var row in ViewBag.ComparisonData)
                        {
                            <tr>
                                <td>@row.ElemenTeknis</td>
                                <td class="text-center">@($"{row.PreScore:0.#}%")</td>
                                <td class="text-center">@($"{row.PostScore:0.#}%")</td>
                                <td class="text-center">
                                    @{
                                        var gain = (double?)row.GainScore;
                                        string gainText, gainClass;
                                        if (gain == null)
                                        {
                                            gainText = "\u2014";
                                            gainClass = "text-secondary";
                                        }
                                        else if (gain > 0)
                                        {
                                            gainText = $"+{gain:0.#}%";
                                            gainClass = "text-success";
                                        }
                                        else if (gain < 0)
                                        {
                                            gainText = $"{gain:0.#}%";
                                            gainClass = "text-danger";
                                        }
                                        else
                                        {
                                            gainText = "0%";
                                            gainClass = "text-secondary";
                                        }
                                    }
                                    <span class="fw-bold @gainClass">@gainText</span>
                                    @if (gain == null && ViewBag.GainScorePending == true)
                                    {
                                        <small class="d-block text-muted">Menunggu penilaian Essay</small>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
```

PENTING: Section ini HANYA muncul di Results Post-Test (per D-14). Controller sudah handle ini — `ViewBag.HasComparisonSection` hanya true saat session adalah PostTest + ada ET scores.

PENTING 2: `table-responsive` wrapper untuk mobile support (review fix: mobile responsiveness).

PENTING 3: `aria-label` pada table untuk accessibility (review fix).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Results.cshtml contains `ViewBag.HasComparisonSection`
    - Results.cshtml contains `ViewBag.ComparisonData`
    - Results.cshtml contains `Perbandingan Pre-Post Test`
    - Results.cshtml contains `Elemen Kompetensi`
    - Results.cshtml contains `Skor Pre`
    - Results.cshtml contains `Skor Post`
    - Results.cshtml contains `Gain Score`
    - Results.cshtml contains `text-success`
    - Results.cshtml contains `text-danger`
    - Results.cshtml contains `Menunggu penilaian Essay`
    - Results.cshtml contains `bi-bar-chart-line`
    - Results.cshtml contains `table-responsive`
    - Results.cshtml contains `aria-label`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Results.cshtml menampilkan section perbandingan Pre-Post dengan gain score per elemen — positif hijau, negatif merah, 0 abu-abu, pending Essay dash. Table responsive dan accessible. Build berhasil.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi visual Pre-Post card pair dan comparison section</name>
  <files>Views/CMP/Assessment.cshtml, Views/CMP/Results.cshtml</files>
  <read_first>
    - Views/CMP/Assessment.cshtml (verify final state)
    - Views/CMP/Results.cshtml (verify final state)
  </read_first>
  <action>Human verification — buka browser dan test flow Pre-Post</action>
  <verify><automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated></verify>
  <acceptance_criteria>
    - User confirms Pre-Post card pair visible with badge and arrow
    - User confirms Post-Test blocked when Pre not completed
    - User confirms Riwayat badge visible
    - User confirms comparison section visible in Results
    - User confirms gain score color coding correct
  </acceptance_criteria>
  <done>User approved visual dan functional behavior</done>
  <what-built>
  Pre-Post card pair di halaman My Assessments (badge, arrow connector, blocking logic, ARIA labels) dan section perbandingan gain score di halaman Results Post-Test.
  </what-built>
  <how-to-verify>
  1. Jalankan `dotnet run` dan login sebagai worker yang punya Pre-Post Test session
  2. Buka /CMP/Assessment — verifikasi:
     - Pre-Test dan Post-Test muncul sebagai 2 card terhubung dengan left-border biru dan arrow icon
     - Badge "Pre-Test" dan "Post-Test" tampil di samping badge kategori
     - Jika Pre belum Completed, Post card grayed out (opacity-50) dengan tombol disabled "Selesaikan Pre-Test terlebih dahulu"
     - Tab filtering berfungsi — pair card mengikuti status Post untuk tab placement
     - Di mobile viewport (resize browser ke < 768px): card stack vertikal, arrow berubah ke panah bawah
  3. Selesaikan Pre-Test, lalu buka Assessment lagi — verifikasi Post card aktif (jika jadwal sudah tiba)
  4. Selesaikan Post-Test, buka Riwayat Ujian — verifikasi badge Pre-Test/Post-Test di kolom Judul
  5. Klik "Lihat Detail" pada Post-Test di Riwayat — verifikasi section "Perbandingan Pre-Post Test" muncul dengan tabel Elemen Kompetensi, Skor Pre, Skor Post, Gain Score
  6. Verifikasi warna gain score: hijau jika positif, merah jika negatif, abu-abu jika 0
  7. Jika ada Post-Test dengan Essay belum dinilai: gain score menampilkan dash dengan "Menunggu penilaian Essay"
  </how-to-verify>
  <resume-signal>Ketik "approved" atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker browser -> Assessment.cshtml | Card data dari ViewBag, no user input — read only |
| Worker browser -> Results.cshtml | Comparison data dari ViewBag, no user input — read only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-299-04 | Information Disclosure | Assessment.cshtml | accept | Card data hanya menampilkan assessment milik worker yang login — filter sudah di controller |
| T-299-05 | Information Disclosure | Results.cshtml | mitigate | Comparison data hanya muncul jika preSession.UserId == assessment.UserId (di controller Plan 01) |
</threat_model>

<verification>
- `dotnet build` berhasil
- Assessment.cshtml menampilkan Pre-Post card pair dengan blocking logic
- Assessment.cshtml inner pair cards TIDAK punya class `assessment-card` (hanya `assessment-card-item`)
- Results.cshtml menampilkan comparison section dengan gain score
- Tab filtering bekerja untuk pair cards
- Riwayat Ujian menampilkan badge Pre-Test/Post-Test
- Mobile responsive: card pair stack vertikal, arrow berubah arah
- ARIA labels present pada pair wrapper dan comparison table
</verification>

<success_criteria>
- Worker melihat Pre-Post card pair dengan visual linking (border + arrow)
- Post-Test card disabled saat Pre belum Completed
- Post-Test card menampilkan "Pre-Test tidak diselesaikan" saat Pre expired
- Riwayat Ujian menampilkan badge Pre-Test/Post-Test
- Results Post-Test menampilkan comparison section dengan gain score per elemen
- Gain score: hijau positif, merah negatif, abu-abu 0, dash saat Essay pending
- Mobile responsive dan accessible (ARIA labels)
- Existing JS functionality (tab filtering, start button) tidak broken
</success_criteria>

<output>
After completion, create `.planning/phases/299-worker-pre-post-test-comparison/299-02-SUMMARY.md`
</output>
