---
phase: 300-mobile-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/StartExam.cshtml
autonomous: true
requirements:
  - MOB-02
  - MOB-03
must_haves:
  truths:
    - "Sidebar col-lg-3 tersembunyi di viewport < 992px portrait, diganti offcanvas drawer"
    - "Sticky footer dengan Prev + [≡] + Next/Submit selalu terlihat di mobile"
    - "Klik nomor soal di offcanvas drawer menavigasi ke soal dan menutup drawer"
    - "Mobile menampilkan 5 soal per halaman, desktop tetap 10"
    - "Inline Prev/Next buttons tersembunyi di mobile"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Offcanvas drawer HTML + sticky footer HTML + JS page size + JS sync"
      contains: "questionNavDrawer"
  key_links:
    - from: "sticky footer buttons"
      to: "changePage()"
      via: "onclick handler"
      pattern: "changePage\\(currentPage"
    - from: "offcanvas drawer numbers"
      to: "jumpToQuestion()"
      via: "click event listener in updatePanel sync"
      pattern: "drawerNumbers"
---

<objective>
Tambahkan offcanvas drawer navigasi soal dan sticky footer mobile ke StartExam.cshtml, serta ubah page size menjadi 5 soal di mobile.

Purpose: Pekerja lapangan dapat menavigasi soal ujian dari perangkat mobile dengan nyaman — panel navigasi dan tombol Prev/Next selalu tersedia tanpa scroll.
Output: StartExam.cshtml dengan offcanvas drawer, sticky footer, dan conditional page size.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/300-mobile-optimization/300-CONTEXT.md
@.planning/phases/300-mobile-optimization/300-RESEARCH.md
@.planning/phases/300-mobile-optimization/300-UI-SPEC.md

<interfaces>
<!-- Key structures in StartExam.cshtml yang harus dipahami executor -->

Line 5-6 (Razor constants — AKAN DIUBAH):
```razor
const int questionsPerPage = 10;
int totalPages = (int)Math.Ceiling((double)Model.TotalQuestions / questionsPerPage);
```

Line 63-66 (Grid layout):
```html
<div class="container-fluid py-3" id="examContainer" inert>
    <div class="row">
        <div class="col-lg-9 exam-protected">
```

Line 185-207 (Inline page nav — AKAN DISEMBUNYIKAN di mobile):
```html
<div class="d-flex justify-content-between mt-3 mb-4">
    <button type="button" class="btn btn-outline-secondary" onclick="changePage(@(page - 1))" ...>Previous Page</button>
    <!-- Next atau Submit button -->
</div>
```

Line 213-231 (Sidebar — AKAN DISEMBUNYIKAN di mobile portrait):
```html
<div class="col-lg-3" id="questionPanelWrapper">
    <div class="sticky-top" style="top: 70px;">
        ...
        <div id="panelNumbers" class="d-flex flex-wrap gap-2">
```

Line 366-404 (JS constants — questionsPerPage references):
```javascript
const QUESTIONS_PER_PAGE = @questionsPerPage;
const TOTAL_PAGES = @totalPages;
const pageQuestionIds = @Html.Raw(...Enumerable.Range(0, totalPages)...);
const allQuestionsData = @Html.Raw(...index / questionsPerPage...);
```

Line 876-909 (changePage function — selector for disable):
```javascript
document.querySelectorAll('[onclick^="changePage"], #reviewSubmitBtn').forEach(...)
```

Line 971-991 (updatePanel function — AKAN DI-EXTEND untuk sync drawer):
```javascript
function updatePanel() {
    const container = document.getElementById('panelNumbers');
    container.innerHTML = '';
    allQuestionsData.forEach(function(q) { ... });
}
```

Line 1351-1357 (Existing mobile CSS — AKAN DIGANTI):
```css
@@media (max-width: 991px) {
    #questionPanelWrapper { order: -1; margin-bottom: 1rem; }
    #questionPanel { max-height: 300px; overflow-y: auto; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offcanvas drawer HTML + sidebar hide di mobile portrait</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - Views/CMP/StartExam.cshtml (full file — pahami layout, sidebar di line 213-231, modals di line 236+)
    - .planning/phases/300-mobile-optimization/300-UI-SPEC.md (interaction contract untuk offcanvas)
    - .planning/phases/300-mobile-optimization/300-CONTEXT.md (D-01, D-02, D-03, D-06)
  </read_first>
  <action>
Modifikasi `Views/CMP/StartExam.cshtml`:

**1. Sembunyikan sidebar di mobile portrait (D-01, D-03, D-06):**
Pada line 214, ubah `<div class="col-lg-3" id="questionPanelWrapper">` menjadi:
```html
<div class="col-lg-3 d-none d-lg-block" id="questionPanelWrapper">
```
Ini menyembunyikan sidebar di viewport < 992px. Desktop (lg+) tidak berubah per D-03.

**2. Tambah offcanvas drawer HTML (D-01, D-02):**
Setelah closing `}` dari `@else` block (setelah line 234, sebelum resume modal), tambahkan:
```html
<!-- Mobile Question Navigation Drawer (D-01, D-02) -->
<div class="offcanvas offcanvas-end" tabindex="-1" id="questionNavDrawer" aria-labelledby="questionNavDrawerLabel" data-bs-backdrop="true">
    <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="questionNavDrawerLabel">Navigasi Soal</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Tutup"></button>
    </div>
    <div class="offcanvas-body">
        <div id="drawerNumbers" class="d-flex flex-wrap gap-2">
            <!-- Diisi oleh JS — sync dari updatePanel() -->
        </div>
    </div>
</div>
```

**3. Extend `updatePanel()` JS untuk sync drawer (D-02):**
Di function `updatePanel()` (sekitar line 971-991), setelah loop `allQuestionsData.forEach(...)` dan sebelum closing `}` function, tambahkan:
```javascript
    // Sync ke mobile drawer (D-02)
    var drawerNumbers = document.getElementById('drawerNumbers');
    if (drawerNumbers) {
        drawerNumbers.innerHTML = '';
        allQuestionsData.forEach(function(q) {
            var dbtn = document.createElement('div');
            var answered = answeredQuestions.has(String(q.questionId));
            dbtn.className = 'badge rounded-pill ' + (answered ? 'bg-success' : 'bg-secondary');
            dbtn.style.cssText = 'font-size:0.85rem; padding:6px 10px; cursor:pointer; min-width:36px; width:36px; height:32px; display:flex; align-items:center; justify-content:center;';
            dbtn.innerText = q.displayNumber;
            dbtn.addEventListener('click', function() {
                var drawerEl = document.getElementById('questionNavDrawer');
                var drawer = bootstrap.Offcanvas.getInstance(drawerEl);
                if (drawer) drawer.hide();
                jumpToQuestion(q.questionId, q.pageNumber);
            });
            drawerNumbers.appendChild(dbtn);
        });
    }
```

**4. Hapus existing mobile CSS di bagian bawah file (line 1351-1357):**
Ganti block `@@media (max-width: 991px) { #questionPanelWrapper ... }` dengan:
```css
/* Mobile: sidebar hidden via d-none d-lg-block class — CSS override removed */
```

**5. Sembunyikan inline Prev/Next di mobile:**
Pada line 186, ubah `<div class="d-flex justify-content-between mt-3 mb-4">` menjadi:
```html
<div class="d-none d-lg-flex justify-content-between mt-3 mb-4">
```
Ini menyembunyikan inline navigation di mobile — digantikan sticky footer (Task 2).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\worktree 2" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StartExam.cshtml contains `d-none d-lg-block" id="questionPanelWrapper"`
    - StartExam.cshtml contains `id="questionNavDrawer"`
    - StartExam.cshtml contains `id="drawerNumbers"`
    - StartExam.cshtml contains `bootstrap.Offcanvas.getInstance`
    - StartExam.cshtml contains `d-none d-lg-flex justify-content-between mt-3 mb-4`
    - StartExam.cshtml contains `questionNavDrawerLabel`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Offcanvas drawer muncul di DOM, sidebar hidden di mobile portrait, inline nav hidden di mobile, drawer numbers sync dengan panel numbers</done>
</task>

<task type="auto">
  <name>Task 2: Sticky footer mobile + page size 5 soal di mobile</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - Views/CMP/StartExam.cshtml (baca ulang setelah Task 1 — lihat perubahan)
    - .planning/phases/300-mobile-optimization/300-CONTEXT.md (D-07, D-15, D-16)
    - .planning/phases/300-mobile-optimization/300-RESEARCH.md (Pattern 2, Pattern 6, Pitfall 1, Pitfall 3, Pitfall 5)
  </read_first>
  <action>
Modifikasi `Views/CMP/StartExam.cshtml`:

**1. Tambah sticky footer HTML (D-07):**
Tepat sebelum offcanvas drawer (yang ditambahkan Task 1), tambahkan:
```html
<!-- Mobile Sticky Footer (D-07) — hidden di desktop via d-lg-none -->
<div id="mobileFooter" class="d-lg-none">
    <div class="d-flex justify-content-between align-items-center gap-2">
        <button type="button" class="btn btn-outline-secondary flex-fill"
                id="mobilePrevBtn" onclick="changePage(currentPage - 1)">
            <i class="bi bi-chevron-left me-1"></i><span class="d-none d-sm-inline">Sebelumnya</span>
        </button>
        <button type="button" class="btn btn-outline-primary"
                data-bs-toggle="offcanvas" data-bs-target="#questionNavDrawer"
                aria-label="Buka navigasi soal">
            <i class="bi bi-list"></i>
        </button>
        <button type="button" class="btn btn-primary flex-fill"
                id="mobileNextBtn" onclick="changePage(currentPage + 1)">
            <span class="d-none d-sm-inline">Berikutnya</span><i class="bi bi-chevron-right ms-1"></i>
        </button>
        <button type="button" class="btn btn-success fw-bold flex-fill d-none"
                id="mobileSubmitBtn" onclick="document.getElementById('examForm').submit()">
            <i class="bi bi-clipboard-check me-1"></i>Submit
        </button>
    </div>
</div>
```

**2. Tambah CSS sticky footer di `<style>` block (sebelum closing `</style>`):**
```css
/* Mobile Sticky Footer (D-07) */
#mobileFooter {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: #fff;
    padding: 8px 16px;
    box-shadow: 0 -2px 8px rgba(0,0,0,0.12);
    z-index: 1019;
}
@@media (max-width: 991.98px) {
    #examContainer { padding-bottom: 72px; }
}
```

**3. Tambah JS function `updateMobileNavButtons()` setelah `updatePanel()`:**
```javascript
function updateMobileNavButtons() {
    var prevBtn = document.getElementById('mobilePrevBtn');
    var nextBtn = document.getElementById('mobileNextBtn');
    var submitBtn = document.getElementById('mobileSubmitBtn');
    if (!prevBtn) return;

    prevBtn.disabled = (currentPage === 0);

    var isLastPage = (currentPage === TOTAL_PAGES - 1);
    if (nextBtn) {
        nextBtn.classList.toggle('d-none', isLastPage);
    }
    if (submitBtn) {
        submitBtn.classList.toggle('d-none', !isLastPage);
    }
}
```

**4. Panggil `updateMobileNavButtons()` di `performPageSwitch()` (sekitar line 911-928):**
Setelah `updatePanel();` (line 915), tambahkan:
```javascript
        updateMobileNavButtons();
```

**5. Panggil `updateMobileNavButtons()` di init (setelah `prePopulateAnswers()` sekitar line 1031):**
Tambahkan setelah block `if (!IS_RESUME && window.innerWidth <= 991)`:
```javascript
    updateMobileNavButtons();
```

**6. Extend disable selector di `changePage()` untuk cover sticky footer buttons (Pitfall 1):**
Di 3 tempat dalam `changePage()` function (lines ~882, 889, 899), ubah selector dari:
```javascript
document.querySelectorAll('[onclick^="changePage"], #reviewSubmitBtn')
```
menjadi:
```javascript
document.querySelectorAll('[onclick^="changePage"], #reviewSubmitBtn, #mobilePrevBtn, #mobileNextBtn, #mobileSubmitBtn')
```

**7. Page size conditional — client-side JS (D-15, D-16):**
Ini TIDAK bisa murni client-side karena `questionsPerPage` adalah Razor constant yang menentukan HTML generation. Gunakan pendekatan server-side.

Pada line 5-6, ubah:
```razor
const int questionsPerPage = 10;
int totalPages = (int)Math.Ceiling((double)Model.TotalQuestions / questionsPerPage);
```
menjadi:
```razor
int questionsPerPage = 10;  // Default desktop; mobile override via ViewBag
if (ViewBag.QuestionsPerPage != null) { questionsPerPage = (int)ViewBag.QuestionsPerPage; }
int totalPages = (int)Math.Ceiling((double)Model.TotalQuestions / questionsPerPage);
```

Kemudian di `Controllers/CMPController.cs`, cari method `StartExam` (yang mengembalikan view StartExam). Sebelum `return View(...)`, tambahkan:
```csharp
// Mobile page size (D-15): 5 questions per page on mobile devices
var userAgent = Request.Headers["User-Agent"].ToString();
if (userAgent.Contains("Mobile") || userAgent.Contains("Android") || userAgent.Contains("iPhone"))
{
    ViewBag.QuestionsPerPage = 5;
}
```

**PENTING:** Tambahkan `Controllers/CMPController.cs` ke files_modified — ini adalah perubahan kecil (3 baris).

**8. Panggil init `updateMobileNavButtons()` juga saat resume (setelah resume modal confirm handler jika ada).**
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\worktree 2" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StartExam.cshtml contains `id="mobileFooter"`
    - StartExam.cshtml contains `id="mobilePrevBtn"`
    - StartExam.cshtml contains `id="mobileNextBtn"`
    - StartExam.cshtml contains `id="mobileSubmitBtn"`
    - StartExam.cshtml contains `position: fixed` and `bottom: 0` in #mobileFooter CSS
    - StartExam.cshtml contains `z-index: 1019`
    - StartExam.cshtml contains `padding-bottom: 72px`
    - StartExam.cshtml contains `updateMobileNavButtons()`
    - StartExam.cshtml contains `#mobilePrevBtn, #mobileNextBtn, #mobileSubmitBtn` in changePage disable selector
    - StartExam.cshtml contains `ViewBag.QuestionsPerPage`
    - CMPController.cs contains `ViewBag.QuestionsPerPage = 5`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Sticky footer dengan Prev/[≡]/Next/Submit terlihat di mobile, page size 5 soal di mobile, button states terupdate saat navigasi halaman</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser | Semua perubahan adalah UI/CSS/JS — tidak ada data flow baru |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-300-01 | Tampering | Anti-copy bypass via touch events | accept | D-11: anti-copy events (copy/cut/paste/contextmenu/selectstart/dragstart) tidak konflik dengan layout responsive. `user-select: none` tetap aktif pada `.exam-protected`. Tidak ada touch event listener baru yang ditambahkan. |
| T-300-02 | Tampering | User-Agent spoofing for page size | accept | Dampak minimal — hanya mengubah 10 vs 5 soal per halaman. Tidak ada security impact karena total soal sama. |
| T-300-03 | Denial of Service | Rapid button clicks during save | mitigate | Extend disable selector di `changePage()` untuk mencakup `#mobilePrevBtn, #mobileNextBtn, #mobileSubmitBtn` — tombol di-disable saat save sedang berjalan. |
</threat_model>

<verification>
1. `dotnet build` passes tanpa error
2. Buka StartExam di Chrome DevTools → toggle device toolbar ke iPhone SE (375px portrait)
3. Sidebar tersembunyi, sticky footer muncul di bawah
4. Klik [≡] → offcanvas drawer muncul dari kanan dengan grid nomor soal
5. Klik nomor soal di drawer → navigasi ke soal, drawer tertutup otomatis
6. Tombol Prev disabled di halaman pertama, Submit muncul di halaman terakhir
7. Desktop (>= 992px) tidak berubah — sidebar tetap ada, footer tidak muncul
</verification>

<success_criteria>
- Offcanvas drawer berisi grid nomor soal yang sync dengan desktop panel
- Sticky footer selalu terlihat di mobile dengan Prev + [≡] + Next/Submit
- Mobile menampilkan 5 soal per halaman
- Desktop layout tidak berubah sama sekali
- Inline Prev/Next buttons tersembunyi di mobile
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/300-mobile-optimization/300-01-SUMMARY.md`
</output>
