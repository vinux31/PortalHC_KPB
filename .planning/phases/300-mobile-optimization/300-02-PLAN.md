---
phase: 300-mobile-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 300-01
files_modified:
  - Views/CMP/StartExam.cshtml
autonomous: false
requirements:
  - MOB-01
  - MOB-04
  - MOB-05
  - MOB-06
must_haves:
  truths:
    - "Semua tombol dan opsi jawaban di mobile memiliki area sentuh minimal 48px height"
    - "Timer tetap terlihat di header mobile saat scroll — compact format MM:SS saja"
    - "Title assessment truncate dengan ellipsis di mobile"
    - "Landscape mode menampilkan sidebar kembali"
    - "Anti-copy Phase 280 tetap berfungsi tanpa modifikasi"
    - "Layout tidak terpotong di viewport 320px sampai 414px"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "CSS responsive media queries untuk touch targets, header compact, landscape mode"
      contains: "max-width: 767.98px"
  key_links:
    - from: "media query max-width 767.98px"
      to: "list-group-item, form-check-input, card-body"
      via: "CSS override"
      pattern: "min-height: 48px"
    - from: "media query orientation landscape"
      to: "#questionPanelWrapper"
      via: "CSS display override"
      pattern: "orientation: landscape"
---

<objective>
Tambahkan CSS responsive untuk touch targets 48dp, header compact mobile, title truncate, dan landscape mode sidebar restore.

Purpose: Elemen UI ujian optimal untuk sentuhan jari di layar kecil — tidak ada tombol yang sulit ditekan dan informasi penting (timer) tetap terlihat.
Output: StartExam.cshtml dengan media queries lengkap untuk semua breakpoint mobile.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/300-mobile-optimization/300-CONTEXT.md
@.planning/phases/300-mobile-optimization/300-RESEARCH.md
@.planning/phases/300-mobile-optimization/300-UI-SPEC.md
@.planning/phases/300-mobile-optimization/300-01-SUMMARY.md

<interfaces>
<!-- Key CSS/HTML structures yang sudah ada setelah Plan 01 -->

Header (line 10-30):
```html
<div class="sticky-top bg-white shadow-sm py-2 px-3 border-bottom" id="examHeader">
    <h6 class="mb-0 fw-bold text-primary">@Model.Title</h6>
    <span class="text-muted small" id="answeredProgress">0/@Model.TotalQuestions answered</span>
    <span class="fw-bold fs-5" id="examTimer">--:--</span>
    <span id="hubStatusBadge" class="badge bg-secondary ms-2 small">Connecting...</span>
    <span id="networkStatusBadge" class="badge bg-success ms-2 small">Tersimpan</span>
    <div class="small text-muted">Time Remaining</div>
</div>
```

Sidebar (setelah Plan 01 — hidden di mobile):
```html
<div class="col-lg-3 d-none d-lg-block" id="questionPanelWrapper">
```

Anti-copy CSS (line 1309-1316):
```css
.exam-protected {
    -webkit-user-select: none;
    user-select: none;
    -webkit-touch-callout: none;
}
```

Existing style elements:
- `#examHeader { z-index: 1020; }`
- `#mobileFooter { z-index: 1019; }` (dari Plan 01)
- `.option-selected { background-color: #cfe2ff !important; }`
- `form-check-input` sudah pakai `transform: scale(1.2)` inline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS responsive — touch targets 48dp + header compact + title truncate</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - Views/CMP/StartExam.cshtml (full file — lihat perubahan Plan 01, pahami existing CSS di bagian bawah)
    - .planning/phases/300-mobile-optimization/300-UI-SPEC.md (spacing scale, typography, interaction contract)
    - .planning/phases/300-mobile-optimization/300-CONTEXT.md (D-04, D-05, D-08, D-09)
  </read_first>
  <action>
Tambahkan CSS media queries di block `<style>` di bagian bawah `StartExam.cshtml` (sebelum closing `</style>`):

**1. Touch targets mobile (D-04, MOB-01):**
```css
/* Mobile touch targets (D-04, MOB-01) — hanya di < 768px per D-05 */
@@media (max-width: 767.98px) {
    .list-group-item {
        min-height: 48px;
        padding: 12px 16px;
    }
    .form-check-input {
        transform: scale(1.4) !important;
    }
    .card-body {
        padding: 16px;
    }
    #mobileFooter .btn {
        min-height: 48px;
    }
}
```

**2. Header compact mobile (D-08, D-09, MOB-04):**
```css
/* Header compact mobile (D-08, D-09) */
@@media (max-width: 767.98px) {
    #examHeader h6 {
        max-width: calc(100vw - 160px);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }
    #examHeader .small.text-muted {
        display: none !important;
    }
    #hubStatusBadge {
        display: none !important;
    }
    #answeredProgress {
        display: none !important;
    }
}
```
Catatan: `#examTimer` (timer angka MM:SS) TETAP terlihat — hanya label "Time Remaining", hub status badge, dan answered progress yang disembunyikan. Network status badge (`#networkStatusBadge`) tetap terlihat karena informatif per UI-SPEC.

**3. Juga tambah CSS untuk offcanvas drawer grid (dari Plan 01):**
```css
/* Offcanvas drawer grid layout */
#drawerNumbers {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 8px;
    align-items: start;
    justify-items: center;
}
```

**4. Pastikan `form-check-input` inline style `transform: scale(1.2)` di HTML TIDAK di-override salah:**
Cek semua `style="transform: scale(1.2);"` di checkbox/radio elements. CSS `!important` pada media query akan menang atas inline style. Ini benar — di mobile naik ke 1.4, di desktop inline 1.2 tetap berlaku.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\worktree 2" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StartExam.cshtml contains `min-height: 48px` inside a media query block
    - StartExam.cshtml contains `transform: scale(1.4) !important`
    - StartExam.cshtml contains `max-width: calc(100vw - 160px)` for header h6
    - StartExam.cshtml contains `text-overflow: ellipsis`
    - StartExam.cshtml contains `#hubStatusBadge` with `display: none !important`
    - StartExam.cshtml contains `#answeredProgress` with `display: none !important`
    - StartExam.cshtml contains `#drawerNumbers` grid CSS with `repeat(7, 1fr)`
    - Anti-copy CSS `.exam-protected` tetap utuh (user-select: none, -webkit-touch-callout: none)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Touch targets minimal 48px di mobile, timer compact tanpa label, title truncate, drawer grid layout, desktop tidak berubah</done>
</task>

<task type="auto">
  <name>Task 2: CSS landscape mode sidebar restore + anti-copy verification</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - Views/CMP/StartExam.cshtml (baca ulang setelah Task 1)
    - .planning/phases/300-mobile-optimization/300-CONTEXT.md (D-13, D-14, D-11)
    - .planning/phases/300-mobile-optimization/300-UI-SPEC.md (landscape mode section)
  </read_first>
  <action>
Tambahkan CSS media query untuk landscape mode di `<style>` block:

**1. Landscape sidebar restore (D-13, D-14):**
```css
/* Landscape mode: sidebar kembali tampil (D-13, D-14) */
@@media (orientation: landscape) and (max-width: 991.98px) {
    #questionPanelWrapper {
        display: block !important;
        flex: 0 0 200px;
        width: 200px;
        max-width: 200px;
    }
    #questionPanel {
        max-height: calc(100vh - 140px);
        overflow-y: auto;
    }
    /* Sembunyikan offcanvas trigger di landscape karena sidebar sudah tampil */
    #mobileFooter [data-bs-target="#questionNavDrawer"] {
        display: none !important;
    }
}
```

**2. Verifikasi anti-copy tetap utuh (D-11, MOB-05):**
Baca section anti-copy (sekitar line 1263-1282 dan 1309-1316). Pastikan:
- TIDAK ada event listener baru untuk `touchstart`, `touchend`, `touchmove`
- CSS `.exam-protected` masih memiliki `user-select: none` dan `-webkit-touch-callout: none`
- JS anti-copy IIFE (line 1264-1282) tidak dimodifikasi

Jika semua tetap utuh, tidak ada perubahan yang diperlukan — cukup verifikasi.

**3. Pastikan tidak ada CSS conflict:**
Verifikasi bahwa z-index hierarchy benar:
- `#examLoadingOverlay`: z-index 2000
- `#examHeader`: z-index 1020
- `#mobileFooter`: z-index 1019
- Bootstrap offcanvas default: ~1045 (di antara header dan loading overlay)
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\worktree 2" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StartExam.cshtml contains `orientation: landscape` in a media query
    - StartExam.cshtml contains `flex: 0 0 200px` for questionPanelWrapper in landscape
    - StartExam.cshtml contains `display: block !important` for questionPanelWrapper in landscape
    - StartExam.cshtml still contains `.exam-protected` with `user-select: none`
    - StartExam.cshtml still contains `-webkit-touch-callout: none`
    - StartExam.cshtml does NOT contain `touchstart` or `touchend` event listeners
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Landscape mode menampilkan sidebar 200px, anti-copy tidak termodifikasi, z-index hierarchy benar</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — mobile optimization end-to-end</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <action>Verifikasi visual dan fungsional mobile optimization di browser.</action>
  <what-built>Mobile optimization lengkap untuk StartExam — offcanvas drawer, sticky footer, touch targets 48dp, header compact, landscape sidebar, page size 5 soal</what-built>
  <how-to-verify>
    1. Buka aplikasi, masuk sebagai worker, mulai ujian yang memiliki lebih dari 10 soal
    2. Buka Chrome DevTools (F12) lalu toggle device toolbar lalu pilih iPhone SE (375px)
    3. Verifikasi: sidebar TIDAK muncul, sticky footer dengan 3 tombol muncul di bawah
    4. Klik tombol list di tengah footer lalu offcanvas drawer muncul dari kanan dengan grid nomor soal
    5. Klik salah satu nomor di drawer lalu navigasi ke soal tersebut, drawer tertutup
    6. Verifikasi: halaman hanya menampilkan 5 soal (bukan 10)
    7. Navigasi ke halaman terakhir lalu tombol Next berubah jadi Submit
    8. Verifikasi: tombol Prev disabled di halaman pertama
    9. Scroll ke bawah lalu timer (angka saja, tanpa label) tetap terlihat di header sticky
    10. Verifikasi: opsi jawaban dan tombol terasa mudah ditekan (min 48px height)
    11. Switch ke landscape lalu sidebar muncul kembali (~200px), tombol list hilang
    12. Switch ke desktop viewport (1200px) lalu layout normal, sidebar col-lg-3, tidak ada footer
    13. Coba select/copy teks soal lalu HARUS diblokir (anti-copy masih aktif)
  </how-to-verify>
  <verify>Human verification</verify>
  <done>Semua 13 langkah verifikasi lulus</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser | Pure CSS/HTML/JS changes — no new data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-300-04 | Tampering | Anti-copy circumvention via mobile gestures | accept | Anti-copy blocks clipboard events + user-select:none. Touch gestures (tap, scroll) are normal browser interactions, not copy vectors. No touch event listeners added. |
| T-300-05 | Information Disclosure | Timer visibility in compact mode | accept | Timer tetap ditampilkan (MM:SS format). Hanya label dekoratif yang disembunyikan. Informasi waktu tidak berkurang. |
</threat_model>

<verification>
1. `dotnet build` passes
2. Mobile viewport (375px): touch targets >= 48px, footer visible, drawer works
3. Landscape: sidebar 200px restored, offcanvas trigger hidden
4. Desktop (1200px): zero visual changes from before Phase 300
5. Anti-copy: text selection blocked di `.exam-protected` area
</verification>

<success_criteria>
- Semua opsi jawaban dan tombol minimal 48px height di mobile
- Timer compact (MM:SS saja) terlihat sticky di header mobile
- Title assessment truncate dengan ellipsis
- Landscape mode menampilkan sidebar 200px
- Anti-copy Phase 280 100% utuh
- Desktop layout tidak berubah
</success_criteria>

<output>
After completion, create `.planning/phases/300-mobile-optimization/300-02-SUMMARY.md`
</output>
