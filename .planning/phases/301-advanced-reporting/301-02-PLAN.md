---
phase: 301-advanced-reporting
plan: 02
type: execute
wave: 2
depends_on: [301-01]
files_modified:
  - Views/CMP/AnalyticsDashboard.cshtml
autonomous: false
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, RPT-07]

must_haves:
  truths:
    - "Tab Item Analysis tampil di Analytics Dashboard dan menampilkan tabel soal dengan p-value dan badge Mudah/Sedang/Sulit"
    - "Tab Gain Score Report tampil dan menampilkan tabel per pekerja dan per elemen kompetensi"
    - "Dropdown Pilih Assessment muncul di filter bar saat tab Item Analysis atau Gain Score aktif"
    - "Distractor analysis menampilkan tabel opsi per soal dengan highlight jawaban benar"
    - "Warning badge Data belum cukup (N<30) ditampilkan di samping discrimination index"
    - "Group comparison per Bagian tampil di tab Gain Score"
  artifacts:
    - path: "Views/CMP/AnalyticsDashboard.cshtml"
      provides: "Tab Item Analysis + tab Gain Score Report + dropdown assessment + distractor table"
      contains: "Item Analysis"
  key_links:
    - from: "Views/CMP/AnalyticsDashboard.cshtml"
      to: "/CMP/GetItemAnalysisData"
      via: "fetch AJAX"
      pattern: "GetItemAnalysisData"
    - from: "Views/CMP/AnalyticsDashboard.cshtml"
      to: "/CMP/GetGainScoreData"
      via: "fetch AJAX"
      pattern: "GetGainScoreData"
---

<objective>
Menambahkan tab Item Analysis dan Gain Score Report ke Analytics Dashboard view, termasuk konversi layout ke Bootstrap nav-tabs, dropdown pilih assessment, dan rendering data dari endpoint Plan 01.

Purpose: HC dapat melihat statistik kualitas soal dan gain score langsung dari dashboard.
Output: View AnalyticsDashboard.cshtml dengan 6 tab (4 existing + 2 baru)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/301-advanced-reporting/301-CONTEXT.md
@.planning/phases/301-advanced-reporting/301-RESEARCH.md
@.planning/phases/301-advanced-reporting/301-UI-SPEC.md
@.planning/phases/301-advanced-reporting/301-01-SUMMARY.md

<interfaces>
From Models/AnalyticsDashboardViewModel.cs (created in Plan 01):
```csharp
public class ItemAnalysisResult
{
    public int TotalResponden { get; set; }
    public bool IsLowN { get; set; }
    public List<ItemAnalysisRow> Items { get; set; } = new();
}

public class ItemAnalysisRow
{
    public int QuestionNumber { get; set; }
    public string QuestionText { get; set; } = "";
    public string QuestionType { get; set; } = "MultipleChoice";
    public double DifficultyIndex { get; set; }
    public double? DiscriminationIndex { get; set; }
    public int TotalResponden { get; set; }
    public bool IsLowN { get; set; }
    public List<DistractorRow> Distractors { get; set; } = new();
}

public class DistractorRow
{
    public string OptionText { get; set; } = "";
    public bool IsCorrect { get; set; }
    public int Count { get; set; }
    public double Percent { get; set; }
}

public class GainScoreResult
{
    public List<GainScorePerWorker> PerWorker { get; set; } = new();
    public List<GainScorePerElemen> PerElemen { get; set; } = new();
    public List<GroupComparisonItem> GroupComparison { get; set; } = new();
}

public class PrePostAssessmentListItem
{
    public int LinkedGroupId { get; set; }
    public string Title { get; set; } = "";
    public int TotalWorker { get; set; }
}
```

Endpoints (dari Plan 01):
- GET /CMP/GetItemAnalysisData?assessmentGroupId=123
- GET /CMP/GetGainScoreData?assessmentGroupId=123
- GET /CMP/GetPrePostAssessmentList?bagian=X&unit=Y
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Konversi layout ke nav-tabs + Tab Item Analysis dengan distractor analysis</name>
  <files>Views/CMP/AnalyticsDashboard.cshtml</files>
  <read_first>
    - Views/CMP/AnalyticsDashboard.cshtml
    - .planning/phases/301-advanced-reporting/301-UI-SPEC.md
    - .planning/phases/301-advanced-reporting/301-CONTEXT.md
  </read_first>
  <action>
**1. Konversi 4 panel grid menjadi Bootstrap nav-tabs** (per D-01, RESEARCH rekomendasi):

Ganti layout card grid dengan:
```html
<ul class="nav nav-tabs mb-3" id="analyticsTabs" role="tablist">
  <li class="nav-item"><a class="nav-link active" data-bs-toggle="tab" href="#tabFailRate">Fail Rate</a></li>
  <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#tabTrend">Trend</a></li>
  <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#tabEtBreakdown">Skor Elemen Teknis</a></li>
  <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#tabExpiring">Sertifikat Expired</a></li>
  <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#tabItemAnalysis">Item Analysis</a></li>
  <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#tabGainScore">Gain Score Report</a></li>
</ul>
<div class="tab-content">
  <div class="tab-pane fade show active" id="tabFailRate">...</div>
  <div class="tab-pane fade" id="tabTrend">...</div>
  <div class="tab-pane fade" id="tabEtBreakdown">...</div>
  <div class="tab-pane fade" id="tabExpiring">...</div>
  <div class="tab-pane fade" id="tabItemAnalysis">...</div>
  <div class="tab-pane fade" id="tabGainScore">...</div>
</div>
```

Pindahkan konten 4 panel existing ke dalam masing-masing tab-pane. Pastikan JS rendering (chart, table) tetap berfungsi — trigger render saat tab shown (event `shown.bs.tab`).

**2. Tambah dropdown "Pilih Assessment" ke filter bar** (per D-02):

Setelah dropdown SubKategori di filter bar, tambah:
```html
<div class="col-md-3" id="filterAssessmentGroup" style="display:none;">
  <label class="form-label form-label-sm">Pilih Assessment</label>
  <select class="form-select form-select-sm" id="filterAssessment">
    <option value="">— Pilih assessment —</option>
  </select>
</div>
```

JavaScript: show/hide `filterAssessmentGroup` berdasarkan tab aktif. Populate via AJAX ke `GetPrePostAssessmentList` saat filter Bagian/Unit berubah.

**3. Tab Item Analysis content** (per D-01, D-03, D-04, D-05):

```html
<div class="tab-pane fade" id="tabItemAnalysis">
  <div class="card border-0 shadow-sm">
    <div class="card-header bg-light d-flex justify-content-between align-items-center">
      <h6 class="fw-semibold mb-0">Item Analysis</h6>
      <button class="btn btn-outline-success btn-sm" onclick="exportItemAnalysis()">
        <i class="bi bi-file-earmark-excel me-1"></i>Export Item Analysis
      </button>
    </div>
    <div class="card-body" id="itemAnalysisContainer">
      <div class="text-center text-muted py-5" id="itemAnalysisEmpty">
        <h6>Pilih assessment terlebih dahulu</h6>
        <p>Gunakan dropdown di atas untuk memilih assessment, lalu klik Terapkan Filter.</p>
      </div>
      <div id="itemAnalysisContent" style="display:none;">
        <div class="mb-2" id="itemAnalysisWarning" style="display:none;">
          <span class="badge bg-warning text-dark">Data belum cukup (N&lt;30)</span>
          <small class="text-muted ms-2">Nilai discrimination index mungkin tidak reliabel.</small>
        </div>
        <div class="table-responsive">
          <table class="table table-sm table-hover" id="itemAnalysisTable">
            <thead class="table-light">
              <tr>
                <th>No</th>
                <th>Soal</th>
                <th>Tingkat Kesulitan (p-value)</th>
                <th>Kategori</th>
                <th>Indeks Diskriminasi</th>
                <th>N Responden</th>
              </tr>
            </thead>
            <tbody></tbody>
          </table>
        </div>
        <!-- Distractor detail per soal — rendered dynamically below table -->
        <div id="distractorDetails"></div>
      </div>
    </div>
  </div>
</div>
```

**4. JavaScript untuk render Item Analysis:**

```javascript
function renderItemAnalysis(data) {
    if (!data || !data.items || data.items.length === 0) {
        document.getElementById('itemAnalysisEmpty').style.display = '';
        document.getElementById('itemAnalysisEmpty').querySelector('h6').textContent = 'Belum ada data soal';
        document.getElementById('itemAnalysisEmpty').querySelector('p').textContent = 'Assessment yang dipilih tidak memiliki soal atau belum ada peserta yang mengerjakan.';
        document.getElementById('itemAnalysisContent').style.display = 'none';
        return;
    }
    document.getElementById('itemAnalysisEmpty').style.display = 'none';
    document.getElementById('itemAnalysisContent').style.display = '';

    // Warning N<30 (D-04)
    document.getElementById('itemAnalysisWarning').style.display = data.isLowN ? '' : 'none';

    var tbody = document.querySelector('#itemAnalysisTable tbody');
    tbody.innerHTML = '';
    var distractorHtml = '';

    data.items.forEach(function(item) {
        // P-value badge color (Claude's discretion: color coding)
        var pBadge = '';
        if (item.difficultyIndex > 0.70) pBadge = '<span class="badge bg-success">Mudah</span>';
        else if (item.difficultyIndex >= 0.30) pBadge = '<span class="badge bg-warning text-dark">Sedang</span>';
        else pBadge = '<span class="badge bg-danger">Sulit</span>';

        // Discrimination index with gray-out if N<30 (D-04)
        var dVal = item.discriminationIndex !== null ? item.discriminationIndex.toFixed(3) : '—';
        var dClass = item.isLowN ? 'text-muted' : '';
        var dWarning = item.isLowN ? ' <span class="badge bg-warning text-dark" style="font-size:0.7rem;">Data belum cukup (N&lt;30)</span>' : '';

        tbody.innerHTML += '<tr data-q="' + item.questionNumber + '" style="cursor:pointer;" onclick="toggleDistractor(' + item.questionNumber + ')">' +
            '<td>' + item.questionNumber + '</td>' +
            '<td>' + escapeHtml(item.questionText.substring(0, 80)) + (item.questionText.length > 80 ? '...' : '') + '</td>' +
            '<td>' + item.difficultyIndex.toFixed(3) + '</td>' +
            '<td>' + pBadge + '</td>' +
            '<td class="' + dClass + '">' + dVal + dWarning + '</td>' +
            '<td>' + item.totalResponden + '</td>' +
            '</tr>';

        // Distractor analysis (D-05, RPT-03) — collapsible per soal
        if (item.distractors && item.distractors.length > 0) {
            distractorHtml += '<div id="distractor-' + item.questionNumber + '" class="mb-3" style="display:none;">' +
                '<h6 class="fw-semibold">Soal ' + item.questionNumber + ' — Distractor Analysis</h6>' +
                '<table class="table table-sm table-bordered">' +
                '<thead><tr><th>Opsi</th><th>Jumlah Pemilih</th><th>Persentase</th><th>Keterangan</th></tr></thead><tbody>';
            item.distractors.forEach(function(d) {
                var rowClass = d.isCorrect ? 'table-success' : '';
                var keterangan = d.isCorrect ? '<span class="badge bg-success">Jawaban Benar</span>' : '';
                distractorHtml += '<tr class="' + rowClass + '"><td>' + escapeHtml(d.optionText) + '</td><td>' + d.count + '</td><td>' + d.percent + '%</td><td>' + keterangan + '</td></tr>';
            });
            distractorHtml += '</tbody></table></div>';
        }
    });

    document.getElementById('distractorDetails').innerHTML = distractorHtml;
}

function toggleDistractor(qNum) {
    var el = document.getElementById('distractor-' + qNum);
    if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}

function escapeHtml(str) {
    var div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
}
```

**5. AJAX fetch untuk Item Analysis saat "Terapkan Filter" diklik dan tab Item Analysis aktif:**

```javascript
// Dalam handler btnFilter click, setelah existing analytics fetch:
var activeTab = document.querySelector('#analyticsTabs .nav-link.active');
var assessmentGroupId = document.getElementById('filterAssessment').value;

if (activeTab && activeTab.getAttribute('href') === '#tabItemAnalysis' && assessmentGroupId) {
    fetch('/CMP/GetItemAnalysisData?assessmentGroupId=' + assessmentGroupId)
        .then(function(r) { return r.json(); })
        .then(renderItemAnalysis)
        .catch(function() {
            document.getElementById('itemAnalysisContent').style.display = 'none';
            document.getElementById('itemAnalysisEmpty').style.display = '';
            document.getElementById('itemAnalysisEmpty').querySelector('h6').textContent = 'Gagal memuat data';
            document.getElementById('itemAnalysisEmpty').querySelector('p').textContent = 'Gagal memuat data. Periksa koneksi dan coba lagi, atau hubungi administrator.';
        });
}
```

**6. Populate dropdown assessment saat filter Bagian/Unit berubah:**

```javascript
function loadAssessmentList() {
    var bagian = document.getElementById('filterBagian').value;
    var unit = document.getElementById('filterUnit') ? document.getElementById('filterUnit').value : '';
    fetch('/CMP/GetPrePostAssessmentList?bagian=' + encodeURIComponent(bagian) + '&unit=' + encodeURIComponent(unit))
        .then(function(r) { return r.json(); })
        .then(function(list) {
            var sel = document.getElementById('filterAssessment');
            sel.innerHTML = '<option value="">— Pilih assessment —</option>';
            list.forEach(function(item) {
                sel.innerHTML += '<option value="' + item.linkedGroupId + '">' + escapeHtml(item.title) + ' (' + item.totalWorker + ' pekerja)</option>';
            });
        });
}
```

Panggil `loadAssessmentList()` saat dropdown Bagian/Unit berubah.

**7. Show/hide dropdown assessment berdasarkan tab aktif:**
```javascript
document.getElementById('analyticsTabs').addEventListener('shown.bs.tab', function(e) {
    var isAnalysisTab = e.target.getAttribute('href') === '#tabItemAnalysis' || e.target.getAttribute('href') === '#tabGainScore';
    document.getElementById('filterAssessmentGroup').style.display = isAnalysisTab ? '' : 'none';
});
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/CMP/AnalyticsDashboard.cshtml contains `id="analyticsTabs"`
    - Views/CMP/AnalyticsDashboard.cshtml contains `tabItemAnalysis`
    - Views/CMP/AnalyticsDashboard.cshtml contains `filterAssessment`
    - Views/CMP/AnalyticsDashboard.cshtml contains `GetItemAnalysisData`
    - Views/CMP/AnalyticsDashboard.cshtml contains `GetPrePostAssessmentList`
    - Views/CMP/AnalyticsDashboard.cshtml contains `renderItemAnalysis`
    - Views/CMP/AnalyticsDashboard.cshtml contains `toggleDistractor`
    - Views/CMP/AnalyticsDashboard.cshtml contains `Data belum cukup`
    - Views/CMP/AnalyticsDashboard.cshtml contains `bg-success">Mudah`
    - Views/CMP/AnalyticsDashboard.cshtml contains `Jawaban Benar`
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>
    - Analytics Dashboard menggunakan Bootstrap nav-tabs dengan 6 tab
    - Tab Item Analysis menampilkan tabel soal dengan p-value, badge Mudah/Sedang/Sulit, discrimination index
    - Warning N<30 badge kuning ditampilkan di samping discrimination index yang di-gray-out (D-04)
    - Distractor analysis collapsible per soal menampilkan opsi, jumlah pemilih, persentase, highlight jawaban benar (D-05)
    - Dropdown "Pilih Assessment" muncul di filter bar saat tab Item Analysis atau Gain Score aktif (D-02)
    - Build berhasil
  </done>
</task>

<task type="auto">
  <name>Task 2: Tab Gain Score Report dengan per pekerja, per elemen, group comparison</name>
  <files>Views/CMP/AnalyticsDashboard.cshtml</files>
  <read_first>
    - Views/CMP/AnalyticsDashboard.cshtml
    - .planning/phases/301-advanced-reporting/301-UI-SPEC.md
  </read_first>
  <action>
**1. Tab Gain Score content** (per D-06, D-07, D-08):

```html
<div class="tab-pane fade" id="tabGainScore">
  <div class="card border-0 shadow-sm">
    <div class="card-header bg-light d-flex justify-content-between align-items-center">
      <div class="d-flex align-items-center gap-3">
        <h6 class="fw-semibold mb-0">Gain Score Report</h6>
        <div class="btn-group btn-group-sm" role="group">
          <input type="radio" class="btn-check" name="gainView" id="gainViewWorker" checked autocomplete="off">
          <label class="btn btn-outline-primary" for="gainViewWorker" onclick="toggleGainView('worker')">Per Pekerja</label>
          <input type="radio" class="btn-check" name="gainView" id="gainViewElemen" autocomplete="off">
          <label class="btn btn-outline-primary" for="gainViewElemen" onclick="toggleGainView('elemen')">Per Elemen Kompetensi</label>
        </div>
      </div>
      <button class="btn btn-outline-success btn-sm" onclick="exportGainScore()">
        <i class="bi bi-file-earmark-excel me-1"></i>Export Gain Score
      </button>
    </div>
    <div class="card-body" id="gainScoreContainer">
      <div class="text-center text-muted py-5" id="gainScoreEmpty">
        <h6>Pilih assessment terlebih dahulu</h6>
        <p>Gunakan dropdown di atas untuk memilih assessment, lalu klik Terapkan Filter.</p>
      </div>
      <div id="gainScoreContent" style="display:none;">
        <!-- Per Pekerja table (D-07 view 1) -->
        <div id="gainWorkerView">
          <div class="table-responsive">
            <table class="table table-sm table-hover">
              <thead class="table-light">
                <tr><th>Nama Pekerja</th><th>NIP</th><th>Pre Score</th><th>Post Score</th><th class="fw-semibold">Gain Score</th></tr>
              </thead>
              <tbody id="gainWorkerBody"></tbody>
            </table>
          </div>
        </div>
        <!-- Per Elemen Kompetensi table (D-07 view 2) -->
        <div id="gainElemenView" style="display:none;">
          <div class="table-responsive">
            <table class="table table-sm table-hover">
              <thead class="table-light">
                <tr><th>Elemen Kompetensi</th><th>Avg Pre</th><th>Avg Post</th><th class="fw-semibold">Avg Gain</th></tr>
              </thead>
              <tbody id="gainElemenBody"></tbody>
            </table>
          </div>
        </div>
        <!-- Group Comparison (RPT-07) -->
        <div class="mt-4">
          <h6 class="fw-semibold">Perbandingan Antar Kelompok (Bagian)</h6>
          <div class="table-responsive">
            <table class="table table-sm table-hover">
              <thead class="table-light">
                <tr><th>Bagian</th><th>Jumlah Pekerja</th><th>Avg Pre Score</th><th>Avg Post Score</th><th class="fw-semibold">Avg Gain Score</th></tr>
              </thead>
              <tbody id="groupComparisonBody"></tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

**2. JavaScript render Gain Score:**

```javascript
var _gainScoreData = null;

function renderGainScore(data) {
    _gainScoreData = data;
    if (!data || (!data.perWorker.length && !data.perElemen.length)) {
        document.getElementById('gainScoreEmpty').style.display = '';
        document.getElementById('gainScoreEmpty').querySelector('h6').textContent = 'Belum ada data gain score';
        document.getElementById('gainScoreEmpty').querySelector('p').textContent = 'Belum ada peserta yang menyelesaikan Pre-Test dan Post-Test untuk assessment ini.';
        document.getElementById('gainScoreContent').style.display = 'none';
        return;
    }
    document.getElementById('gainScoreEmpty').style.display = 'none';
    document.getElementById('gainScoreContent').style.display = '';

    // Per Worker
    var wBody = document.getElementById('gainWorkerBody');
    wBody.innerHTML = '';
    data.perWorker.forEach(function(w) {
        wBody.innerHTML += '<tr><td>' + escapeHtml(w.namaPekerja) + '</td><td>' + escapeHtml(w.nip) + '</td><td>' + w.preScore + '</td><td>' + w.postScore + '</td><td class="fw-semibold">' + w.gainScore + '</td></tr>';
    });

    // Per Elemen
    var eBody = document.getElementById('gainElemenBody');
    eBody.innerHTML = '';
    data.perElemen.forEach(function(e) {
        eBody.innerHTML += '<tr><td>' + escapeHtml(e.elemenTeknis) + '</td><td>' + e.avgPre + '</td><td>' + e.avgPost + '</td><td class="fw-semibold">' + e.avgGain + '</td></tr>';
    });

    // Group Comparison (RPT-07)
    var gBody = document.getElementById('groupComparisonBody');
    gBody.innerHTML = '';
    data.groupComparison.forEach(function(g) {
        gBody.innerHTML += '<tr><td>' + escapeHtml(g.groupName) + '</td><td>' + g.workerCount + '</td><td>' + g.avgPreScore + '</td><td>' + g.avgPostScore + '</td><td class="fw-semibold">' + g.avgGainScore + '</td></tr>';
    });
}

function toggleGainView(view) {
    document.getElementById('gainWorkerView').style.display = view === 'worker' ? '' : 'none';
    document.getElementById('gainElemenView').style.display = view === 'elemen' ? '' : 'none';
}
```

**3. AJAX fetch untuk Gain Score saat filter diterapkan dan tab Gain Score aktif:**

```javascript
// Dalam handler btnFilter, tambah:
if (activeTab && activeTab.getAttribute('href') === '#tabGainScore' && assessmentGroupId) {
    fetch('/CMP/GetGainScoreData?assessmentGroupId=' + assessmentGroupId)
        .then(function(r) { return r.json(); })
        .then(renderGainScore)
        .catch(function() {
            document.getElementById('gainScoreContent').style.display = 'none';
            document.getElementById('gainScoreEmpty').style.display = '';
            document.getElementById('gainScoreEmpty').querySelector('h6').textContent = 'Gagal memuat data';
            document.getElementById('gainScoreEmpty').querySelector('p').textContent = 'Gagal memuat data. Periksa koneksi dan coba lagi, atau hubungi administrator.';
        });
}
```

**4. Empty state untuk assessment non-PrePostTest (D-08):** Jika dropdown assessment kosong (tidak ada PrePostTest), tampilkan:
```
<h6>Gain Score tidak tersedia</h6>
<p>Laporan ini hanya tersedia untuk assessment bertipe Pre-Post Test.</p>
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/CMP/AnalyticsDashboard.cshtml contains `tabGainScore`
    - Views/CMP/AnalyticsDashboard.cshtml contains `gainWorkerBody`
    - Views/CMP/AnalyticsDashboard.cshtml contains `gainElemenBody`
    - Views/CMP/AnalyticsDashboard.cshtml contains `groupComparisonBody`
    - Views/CMP/AnalyticsDashboard.cshtml contains `toggleGainView`
    - Views/CMP/AnalyticsDashboard.cshtml contains `GetGainScoreData`
    - Views/CMP/AnalyticsDashboard.cshtml contains `Perbandingan Antar Kelompok`
    - Views/CMP/AnalyticsDashboard.cshtml contains `Gain Score tidak tersedia`
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>
    - Tab Gain Score Report menampilkan tabel per pekerja (Nama, NIP, Pre, Post, Gain) dan per elemen (ET, Avg Pre, Avg Post, Avg Gain)
    - Toggle Per Pekerja / Per Elemen Kompetensi berfungsi tanpa AJAX ulang (D-07)
    - Group comparison tabel per Bagian tampil di bawah (RPT-07)
    - Empty states sesuai UI-SPEC copywriting contract
    - Build berhasil
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — Item Analysis dan Gain Score tabs</name>
  <files>Views/CMP/AnalyticsDashboard.cshtml</files>
  <action>Human verification: buka browser dan verifikasi semua tab berfungsi sesuai langkah how-to-verify.</action>
  <verify>User confirms all tabs render correctly</verify>
  <done>User types approved — semua 6 tab berfungsi, data loads, empty states correct, no regression pada 4 tab existing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | AJAX calls dari browser ke endpoint analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-301-05 | Tampering | Client-side JS rendering | accept | Data read-only; no state mutation via client-side rendering |
| T-301-06 | Information Disclosure | assessmentGroupId in URL | accept | HC role sudah authorized melihat semua data analytics |
</threat_model>

<verification>
- 6 tab tampil di Analytics Dashboard
- Tab Item Analysis menampilkan data soal dengan p-value dan distractor
- Tab Gain Score menampilkan per pekerja dan per elemen
- 4 tab existing tidak regression
</verification>

<success_criteria>
- Tab Item Analysis dan Gain Score Report berfungsi penuh di Analytics Dashboard
- Dropdown assessment di filter bar berfungsi
- Empty states dan error states sesuai UI-SPEC
- Visual checkpoint diverifikasi oleh user
</success_criteria>

<output>
After completion, create `.planning/phases/301-advanced-reporting/301-02-SUMMARY.md`
</output>
