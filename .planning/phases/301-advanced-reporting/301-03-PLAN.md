---
phase: 301-advanced-reporting
plan: 03
type: execute
wave: 2
depends_on: [301-01]
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/AnalyticsDashboard.cshtml
autonomous: true
requirements: [RPT-05, RPT-06]

must_haves:
  truths:
    - "HC dapat klik Export Item Analysis dan mendapat file .xlsx dengan header berwarna, freeze row, auto-fit"
    - "HC dapat klik Export Gain Score dan mendapat file .xlsx terpisah"
    - "Line chart Gain Score Trend tampil di tab Trend di bawah chart lulus/gagal"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "ExportItemAnalysisExcel dan ExportGainScoreExcel endpoints"
      contains: "ExportItemAnalysisExcel"
    - path: "Views/CMP/AnalyticsDashboard.cshtml"
      provides: "Gain Score Trend line chart di tab Trend"
      contains: "gainScoreTrend"
  key_links:
    - from: "Views/CMP/AnalyticsDashboard.cshtml"
      to: "/CMP/ExportItemAnalysisExcel"
      via: "window.location.href"
      pattern: "ExportItemAnalysisExcel"
---

<objective>
Menambahkan export Excel untuk Item Analysis dan Gain Score Report, serta line chart Gain Score Trend di tab Trend.

Purpose: HC dapat mengekspor data analytics ke Excel dan melihat tren gain score dari waktu ke waktu.
Output: 2 export endpoints + gain score trend chart di tab Trend
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/301-advanced-reporting/301-CONTEXT.md
@.planning/phases/301-advanced-reporting/301-RESEARCH.md
@.planning/phases/301-advanced-reporting/301-UI-SPEC.md
@.planning/phases/301-advanced-reporting/301-01-SUMMARY.md

<interfaces>
From Helpers/ExcelExportHelper.cs:
```csharp
public static IXLWorksheet CreateSheet(XLWorkbook workbook, string sheetName, string[] headers);
public static FileContentResult ToFileResult(XLWorkbook workbook, string fileName, ControllerBase controller);
```

Endpoints dari Plan 01 (data re-queried di export endpoints):
- GetItemAnalysisData(int assessmentGroupId) → ItemAnalysisResult
- GetGainScoreData(int assessmentGroupId) → GainScoreResult

AnalyticsDataResult sekarang berisi:
```csharp
public List<GainScoreTrendItem> GainScoreTrend { get; set; } = new();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExportItemAnalysisExcel + ExportGainScoreExcel endpoints</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs
    - Helpers/ExcelExportHelper.cs
    - Models/AnalyticsDashboardViewModel.cs
    - .planning/phases/301-advanced-reporting/301-RESEARCH.md
  </read_first>
  <action>
**1. Tambah ExportItemAnalysisExcel di CMPController.cs** (per D-09, D-10, RPT-05):

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ExportItemAnalysisExcel(int assessmentGroupId)
{
    // Re-query data (sama dengan GetItemAnalysisData logic)
    var sessions = await _context.AssessmentSessions
        .Where(s => s.LinkedGroupId == assessmentGroupId
                    && s.AssessmentType == "PreTest"
                    && s.Status == "Completed"
                    && s.Score.HasValue)
        .Select(s => new { s.Id, s.Score })
        .ToListAsync();

    int totalResponden = sessions.Count;
    var sessionIds = sessions.Select(s => s.Id).ToList();
    var responses = await _context.PackageUserResponses
        .Include(r => r.PackageQuestion)
            .ThenInclude(q => q.Options)
        .Where(r => sessionIds.Contains(r.AssessmentSessionId))
        .ToListAsync();

    var sessionScores = sessions
        .Select(s => (SessionId: s.Id, TotalScore: (int)(s.Score ?? 0)))
        .ToList();

    using var wb = new XLWorkbook();

    // Sheet 1: Item Analysis
    var headers1 = new[] { "No", "Soal", "P-Value", "Interpretasi", "D-Index", "N Responden" };
    var ws1 = ExcelExportHelper.CreateSheet(wb, "Item Analysis", headers1);
    ws1.SheetView.FreezeRows(1);
    ws1.Range(1, 1, 1, headers1.Length).Style.Fill.SetBackgroundColor(XLColor.FromHtml("#0d6efd"));
    ws1.Range(1, 1, 1, headers1.Length).Style.Font.SetFontColor(XLColor.White);

    int row1 = 2;
    int qNum = 0;
    var questionGroups = responses.GroupBy(r => r.PackageQuestionId).ToList();
    foreach (var group in questionGroups)
    {
        qNum++;
        var question = group.First().PackageQuestion;
        if (question == null) continue;

        int correctCount = group.Count(r => r.IsCorrect);
        double pValue = totalResponden > 0 ? (double)correctCount / totalResponden : 0;
        var correctSessionIds = group.Where(r => r.IsCorrect).Select(r => r.AssessmentSessionId).ToList();
        double? dIndex = CalculateKelleyDiscrimination(sessionScores, correctSessionIds);

        string interpretasi = pValue > 0.70 ? "Mudah" : pValue >= 0.30 ? "Sedang" : "Sulit";

        ws1.Cell(row1, 1).Value = qNum;
        ws1.Cell(row1, 2).Value = question.QuestionText ?? "";
        ws1.Cell(row1, 3).Value = Math.Round(pValue, 3);
        ws1.Cell(row1, 4).Value = interpretasi;
        ws1.Cell(row1, 5).Value = dIndex.HasValue ? Math.Round(dIndex.Value, 3) : 0;
        ws1.Cell(row1, 6).Value = totalResponden;
        row1++;
    }

    // Sheet 2: Distractor Analysis
    var headers2 = new[] { "No Soal", "Opsi", "Jawaban Benar", "Jumlah Pemilih", "Persentase" };
    var ws2 = ExcelExportHelper.CreateSheet(wb, "Distractor Analysis", headers2);
    ws2.SheetView.FreezeRows(1);
    ws2.Range(1, 1, 1, headers2.Length).Style.Fill.SetBackgroundColor(XLColor.FromHtml("#0d6efd"));
    ws2.Range(1, 1, 1, headers2.Length).Style.Font.SetFontColor(XLColor.White);

    int row2 = 2;
    qNum = 0;
    foreach (var group in questionGroups)
    {
        qNum++;
        var question = group.First().PackageQuestion;
        if (question == null) continue;
        var questionType = question.QuestionType ?? "MultipleChoice";
        if (questionType != "MultipleChoice" && questionType != "TrueFalse") continue;

        var options = question.Options?.ToList() ?? new List<PackageOption>();
        foreach (var opt in options)
        {
            int optCount = group.Count(r => r.PackageOptionId == opt.Id);
            ws2.Cell(row2, 1).Value = qNum;
            ws2.Cell(row2, 2).Value = opt.OptionText ?? "";
            ws2.Cell(row2, 3).Value = opt.IsCorrect ? "Ya" : "Tidak";
            ws2.Cell(row2, 4).Value = optCount;
            ws2.Cell(row2, 5).Value = totalResponden > 0 ? Math.Round((double)optCount / totalResponden * 100, 1) : 0;
            if (opt.IsCorrect)
                ws2.Range(row2, 1, row2, 5).Style.Fill.SetBackgroundColor(XLColor.FromHtml("#d1e7dd"));
            row2++;
        }
    }

    return ExcelExportHelper.ToFileResult(wb,
        $"ItemAnalysis_{assessmentGroupId}_{DateTime.Now:yyyyMMdd}.xlsx", this);
}
```

**2. Tambah ExportGainScoreExcel di CMPController.cs** (per D-09, D-10, RPT-05):

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ExportGainScoreExcel(int assessmentGroupId)
{
    // Re-query gain score data (sama logic dengan GetGainScoreData)
    var preSessions = await _context.AssessmentSessions
        .Include(s => s.User)
        .Where(s => s.LinkedGroupId == assessmentGroupId
                    && s.AssessmentType == "PreTest"
                    && s.Status == "Completed"
                    && s.Score.HasValue)
        .ToListAsync();

    var postSessionDict = await _context.AssessmentSessions
        .Where(s => s.LinkedGroupId == assessmentGroupId
                    && s.AssessmentType == "PostTest"
                    && s.Status == "Completed"
                    && s.Score.HasValue)
        .ToDictionaryAsync(s => s.UserId, s => s);

    using var wb = new XLWorkbook();

    // Sheet 1: Per Pekerja
    var headers1 = new[] { "Nama Pekerja", "NIP", "Bagian", "Pre Score", "Post Score", "Gain Score" };
    var ws1 = ExcelExportHelper.CreateSheet(wb, "Per Pekerja", headers1);
    ws1.SheetView.FreezeRows(1);
    ws1.Range(1, 1, 1, headers1.Length).Style.Fill.SetBackgroundColor(XLColor.FromHtml("#0d6efd"));
    ws1.Range(1, 1, 1, headers1.Length).Style.Font.SetFontColor(XLColor.White);

    int row1 = 2;
    foreach (var pre in preSessions)
    {
        if (!postSessionDict.TryGetValue(pre.UserId, out var post)) continue;
        double preScore = pre.Score ?? 0;
        double postScore = post.Score ?? 0;
        double gainScore = preScore >= 100 ? 100 : (100 - preScore) == 0 ? 0 : Math.Round((postScore - preScore) / (100 - preScore) * 100, 1);

        ws1.Cell(row1, 1).Value = pre.User?.FullName ?? pre.User?.UserName ?? "";
        ws1.Cell(row1, 2).Value = pre.User?.NIP ?? "";
        ws1.Cell(row1, 3).Value = pre.User?.Section ?? "";
        ws1.Cell(row1, 4).Value = Math.Round(preScore, 1);
        ws1.Cell(row1, 5).Value = Math.Round(postScore, 1);
        ws1.Cell(row1, 6).Value = gainScore;
        row1++;
    }

    // Sheet 2: Per Elemen Kompetensi
    var preSessionIds = preSessions.Select(s => s.Id).ToList();
    var postSessionIds = postSessionDict.Values.Select(s => s.Id).ToList();

    var preEt = await _context.SessionElemenTeknisScores
        .Where(e => preSessionIds.Contains(e.AssessmentSessionId))
        .ToListAsync();
    var postEt = await _context.SessionElemenTeknisScores
        .Where(e => postSessionIds.Contains(e.AssessmentSessionId))
        .ToListAsync();

    var preEtAvg = preEt.GroupBy(e => e.ElemenTeknis)
        .ToDictionary(g => g.Key, g => g.Average(e => e.QuestionCount > 0 ? (double)e.CorrectCount * 100.0 / e.QuestionCount : 0));
    var postEtAvg = postEt.GroupBy(e => e.ElemenTeknis)
        .ToDictionary(g => g.Key, g => g.Average(e => e.QuestionCount > 0 ? (double)e.CorrectCount * 100.0 / e.QuestionCount : 0));

    var headers2 = new[] { "Elemen Kompetensi", "Avg Pre", "Avg Post", "Avg Gain" };
    var ws2 = ExcelExportHelper.CreateSheet(wb, "Per Elemen Kompetensi", headers2);
    ws2.SheetView.FreezeRows(1);
    ws2.Range(1, 1, 1, headers2.Length).Style.Fill.SetBackgroundColor(XLColor.FromHtml("#0d6efd"));
    ws2.Range(1, 1, 1, headers2.Length).Style.Font.SetFontColor(XLColor.White);

    int row2 = 2;
    foreach (var et in preEtAvg.Keys.Union(postEtAvg.Keys).OrderBy(k => k))
    {
        double avgPre = preEtAvg.GetValueOrDefault(et, 0);
        double avgPost = postEtAvg.GetValueOrDefault(et, 0);
        double avgGain = avgPre >= 100 ? 100 : (100 - avgPre) == 0 ? 0 : Math.Round((avgPost - avgPre) / (100 - avgPre) * 100, 1);

        ws2.Cell(row2, 1).Value = et;
        ws2.Cell(row2, 2).Value = Math.Round(avgPre, 1);
        ws2.Cell(row2, 3).Value = Math.Round(avgPost, 1);
        ws2.Cell(row2, 4).Value = avgGain;
        row2++;
    }

    return ExcelExportHelper.ToFileResult(wb,
        $"GainScore_{assessmentGroupId}_{DateTime.Now:yyyyMMdd}.xlsx", this);
}
```

**3. Pastikan using ClosedXML.Excel sudah ada** di atas file CMPController.cs. Jika belum, tambah `using ClosedXML.Excel;`.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/CMPController.cs contains `ExportItemAnalysisExcel(int assessmentGroupId)`
    - Controllers/CMPController.cs contains `ExportGainScoreExcel(int assessmentGroupId)`
    - Controllers/CMPController.cs contains `FreezeRows(1)`
    - Controllers/CMPController.cs contains `SetBackgroundColor(XLColor.FromHtml("#0d6efd"))`
    - Controllers/CMPController.cs contains `ExcelExportHelper.ToFileResult`
    - Controllers/CMPController.cs contains `Distractor Analysis`
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>
    - ExportItemAnalysisExcel mengembalikan .xlsx dengan 2 sheet (Item Analysis + Distractor Analysis)
    - ExportGainScoreExcel mengembalikan .xlsx dengan 2 sheet (Per Pekerja + Per Elemen Kompetensi)
    - Header berwarna biru (#0d6efd), font putih, freeze row, auto-fit (via ExcelExportHelper.ToFileResult)
    - Jawaban benar di-highlight hijau muda di sheet Distractor Analysis
    - Build berhasil
  </done>
</task>

<task type="auto">
  <name>Task 2: Gain Score Trend line chart di tab Trend + export JS functions</name>
  <files>Views/CMP/AnalyticsDashboard.cshtml</files>
  <read_first>
    - Views/CMP/AnalyticsDashboard.cshtml
    - .planning/phases/301-advanced-reporting/301-UI-SPEC.md
    - .planning/phases/301-advanced-reporting/301-RESEARCH.md
  </read_first>
  <action>
**1. Tambah Gain Score Trend chart di tab Trend** (per D-11, D-12, RPT-06):

Di dalam `#tabTrend` tab-pane, setelah canvas chart lulus/gagal yang sudah ada, tambahkan:

```html
<div class="mt-4">
  <h6 class="fw-semibold">Rata-rata Gain Score per Bulan</h6>
  <canvas id="gainScoreTrendChart" height="200"></canvas>
  <div class="text-center text-muted py-3" id="gainTrendEmpty" style="display:none;">
    <small>Belum ada data gain score untuk periode ini.</small>
  </div>
</div>
```

**2. Extend JavaScript renderTrend function** (atau the function that renders the trend chart):

Cari function yang di-call setelah GetAnalyticsData fetch berhasil (yang menggunakan `data.trend`). Tambahkan rendering gain score trend:

```javascript
// Setelah rendering trend chart yang sudah ada, tambahkan:
function renderGainScoreTrend(gainScoreTrend) {
    var canvas = document.getElementById('gainScoreTrendChart');
    var emptyMsg = document.getElementById('gainTrendEmpty');

    // Destroy previous chart instance if exists
    if (window._gainTrendChart) {
        window._gainTrendChart.destroy();
    }

    if (!gainScoreTrend || gainScoreTrend.length === 0) {
        canvas.style.display = 'none';
        emptyMsg.style.display = '';
        return;
    }
    canvas.style.display = '';
    emptyMsg.style.display = 'none';

    var labels = gainScoreTrend.map(function(d) { return d.label; });
    var values = gainScoreTrend.map(function(d) { return d.avgGainScore; });
    var counts = gainScoreTrend.map(function(d) { return d.sampleCount; });

    window._gainTrendChart = new Chart(canvas.getContext('2d'), {
        type: 'line',
        data: {
            labels: labels,
            datasets: [{
                label: 'Avg Gain Score (%)',
                data: values,
                borderColor: '#6f42c1',
                backgroundColor: 'rgba(111,66,193,0.1)',
                fill: true,
                tension: 0.3,
                pointRadius: 4,
                pointHoverRadius: 6
            }]
        },
        options: {
            responsive: true,
            plugins: {
                tooltip: {
                    callbacks: {
                        afterLabel: function(ctx) {
                            return 'Sample: ' + counts[ctx.dataIndex] + ' pasangan';
                        }
                    }
                }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    title: { display: true, text: 'Avg Gain Score (%)' }
                },
                x: {
                    title: { display: true, text: 'Bulan' }
                }
            }
        }
    });
}
```

**3. Panggil renderGainScoreTrend saat GetAnalyticsData berhasil:**

Dalam callback `.then()` setelah `fetch('/CMP/GetAnalyticsData?...')`, tambahkan:
```javascript
renderGainScoreTrend(data.gainScoreTrend);
```

**4. Tambah export JS functions** (dipanggil dari tombol Export di Plan 02):

```javascript
function exportItemAnalysis() {
    var assessmentGroupId = document.getElementById('filterAssessment').value;
    if (!assessmentGroupId) { alert('Pilih assessment terlebih dahulu'); return; }
    window.location.href = '/CMP/ExportItemAnalysisExcel?assessmentGroupId=' + assessmentGroupId;
}

function exportGainScore() {
    var assessmentGroupId = document.getElementById('filterAssessment').value;
    if (!assessmentGroupId) { alert('Pilih assessment terlebih dahulu'); return; }
    window.location.href = '/CMP/ExportGainScoreExcel?assessmentGroupId=' + assessmentGroupId;
}
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Views/CMP/AnalyticsDashboard.cshtml contains `gainScoreTrendChart`
    - Views/CMP/AnalyticsDashboard.cshtml contains `renderGainScoreTrend`
    - Views/CMP/AnalyticsDashboard.cshtml contains `Avg Gain Score (%)`
    - Views/CMP/AnalyticsDashboard.cshtml contains `#6f42c1`
    - Views/CMP/AnalyticsDashboard.cshtml contains `exportItemAnalysis`
    - Views/CMP/AnalyticsDashboard.cshtml contains `exportGainScore`
    - Views/CMP/AnalyticsDashboard.cshtml contains `ExportItemAnalysisExcel`
    - Views/CMP/AnalyticsDashboard.cshtml contains `ExportGainScoreExcel`
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>
    - Gain Score Trend line chart (ungu #6f42c1) tampil di tab Trend di bawah chart lulus/gagal
    - Chart menampilkan rata-rata gain score per bulan (D-11, D-12)
    - Tooltip menampilkan sample count per titik data
    - Empty state ditampilkan jika tidak ada data gain score
    - Export functions berfungsi — trigger download langsung via window.location.href
    - Build berhasil
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Export endpoint menerima assessmentGroupId sebagai int query param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-301-07 | Tampering | Excel formula injection | accept | ClosedXML escapes cell values by default; no user-controlled formulas |
| T-301-08 | Spoofing | Export endpoints | mitigate | `[Authorize(Roles = "Admin, HC")]` pada kedua export endpoints |
| T-301-09 | Denial of Service | Large export query | accept | Data bounded by assessment group size (typically < 1000 rows); no pagination needed |
</threat_model>

<verification>
- `dotnet build` berhasil
- grep "ExportItemAnalysisExcel" Controllers/CMPController.cs mengembalikan hasil
- grep "gainScoreTrendChart" Views/CMP/AnalyticsDashboard.cshtml mengembalikan hasil
</verification>

<success_criteria>
- 2 export endpoint menghasilkan file .xlsx yang valid dengan header berwarna dan freeze row
- Gain Score Trend line chart tampil di tab Trend
- Export button di view trigger download langsung
</success_criteria>

<output>
After completion, create `.planning/phases/301-advanced-reporting/301-03-SUMMARY.md`
</output>
