---
phase: 302-accessibility-wcag-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/StartExam.cshtml
  - wwwroot/css/site.css
autonomous: true
requirements: [A11Y-01, A11Y-02, A11Y-03, A11Y-04, A11Y-06]

must_haves:
  truths:
    - "Skip link 'Lewati ke konten utama' muncul saat user menekan Tab pertama kali di StartExam"
    - "Skip link memindahkan focus ke area soal saat diklik/Enter"
    - "Semua opsi jawaban dapat dinavigasi dengan keyboard (Tab antar soal, Arrow antar opsi)"
    - "Focus otomatis berpindah ke card soal pertama saat pindah halaman via Prev/Next"
  artifacts:
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Skip link HTML, auto-focus di performPageSwitch(), tabindex pada question cards"
      contains: "skip-link"
    - path: "wwwroot/css/site.css"
      provides: "Skip link CSS class dan focus outline styling"
      contains: ".skip-link"
  key_links:
    - from: "Views/CMP/StartExam.cshtml"
      to: "wwwroot/css/site.css"
      via: "CSS class skip-link"
      pattern: "skip-link"
---

<objective>
Implementasi skip link, keyboard navigation audit, dan auto-focus pada halaman ujian StartExam.

Purpose: Peserta ujian dapat menggunakan keyboard untuk navigasi seluruh halaman ujian tanpa mouse.
Output: StartExam.cshtml dengan skip link, focus management, dan CSS aksesibilitas.

NOTE: A11Y-03 (screen reader aria-live) DROPPED per D-18. A11Y-04 (font size control) DROPPED per D-19. Requirement IDs tercantum di frontmatter untuk traceability tetapi TIDAK diimplementasikan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/302-accessibility-wcag-quick-wins/302-CONTEXT.md
@.planning/phases/302-accessibility-wcag-quick-wins/302-RESEARCH.md
@.planning/phases/302-accessibility-wcag-quick-wins/302-UI-SPEC.md
@Views/CMP/StartExam.cshtml
@wwwroot/css/site.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skip link + focus outline CSS</name>
  <files>wwwroot/css/site.css</files>
  <read_first>
    - wwwroot/css/site.css
    - .planning/phases/302-accessibility-wcag-quick-wins/302-UI-SPEC.md
  </read_first>
  <action>
Tambahkan CSS class berikut di akhir file wwwroot/css/site.css (per UI-SPEC):

```css
/* Phase 302: Accessibility */
.skip-link {
    position: absolute;
    left: -9999px;
    top: 8px;
    z-index: 9999;
    padding: 8px 16px;
    background: #0d6efd;
    color: #ffffff;
    border-radius: 4px;
    font-size: 14px;
    font-weight: 600;
    text-decoration: none;
}
.skip-link:focus-visible {
    left: 8px;
    outline: 2px solid #ffffff;
    outline-offset: 2px;
}

/* Focus outline for exam elements */
.exam-protected :focus-visible {
    outline: 2px solid #0d6efd;
    outline-offset: 2px;
}
```
  </action>
  <verify>
    <automated>grep -c "skip-link" wwwroot/css/site.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "skip-link" wwwroot/css/site.css menampilkan minimal 3 baris (class definition + :focus-visible)
    - grep "focus-visible" wwwroot/css/site.css menampilkan minimal 2 baris
    - grep "exam-protected.*focus-visible" wwwroot/css/site.css menampilkan 1 baris
  </acceptance_criteria>
  <done>CSS class skip-link dan focus outline tersedia di site.css</done>
</task>

<task type="auto">
  <name>Task 2: Skip link HTML + auto-focus di performPageSwitch + keyboard nav audit</name>
  <files>Views/CMP/StartExam.cshtml</files>
  <read_first>
    - Views/CMP/StartExam.cshtml
    - .planning/phases/302-accessibility-wcag-quick-wins/302-RESEARCH.md
    - .planning/phases/302-accessibility-wcag-quick-wins/302-CONTEXT.md
  </read_first>
  <action>
Tiga perubahan di StartExam.cshtml:

**1. Skip link HTML (per D-01, D-02, D-03):**
Tambahkan sebagai elemen PERTAMA sebelum `#examHeader` (sticky-top div):
```html
<a href="#mainContent" class="skip-link">Lewati ke konten utama</a>
```
Dan tambahkan `id="mainContent"` pada `<div class="col-lg-9 exam-protected">`.

**2. Auto-focus di performPageSwitch() (per D-14, D-15):**
Di dalam function `performPageSwitch()`, setelah baris `window.scrollTo(0, 0);` (atau setelah `if (!skipScroll)` block), tambahkan:
```javascript
// A11Y-06: Auto-focus ke soal pertama di halaman baru
var firstCard = document.querySelector('#page_' + currentPage + ' .card');
if (firstCard) {
    firstCard.setAttribute('tabindex', '-1');
    firstCard.focus({ preventScroll: true });
}
```

**3. Keyboard navigation verification (per D-04, D-05, D-06, D-07):**
- Verifikasi bahwa radio buttons untuk MC/TF sudah menggunakan `<input type="radio">` dengan name grouping (native Arrow key support) — jika sudah native, TIDAK perlu tambah JS
- Verifikasi bahwa checkboxes untuk MA sudah menggunakan `<input type="checkbox">` — native Tab + Space behavior
- Verifikasi bahwa sticky footer buttons (Prev, Next, Submit) memiliki natural tab order (tidak ada tabindex positif yang merusak)
- Verifikasi anti-copy handler (cari `keydown` event listener di ~line 1349-1358) hanya block Ctrl+kombinasi dan TIDAK block Tab/Arrow/Enter/Space
- Jika ada elemen yang menggunakan `tabindex` positif, ubah ke `tabindex="0"` atau hapus
- Jika ada `outline: none` atau `outline: 0` pada elemen focusable, hapus agar focus visible

JANGAN implementasikan:
- aria-live timer (A11Y-03 dropped per D-18)
- Font size control (A11Y-04 dropped per D-19)
  </action>
  <verify>
    <automated>grep -c "mainContent" Views/CMP/StartExam.cshtml && grep -c "skip-link" Views/CMP/StartExam.cshtml && grep -c "firstCard" Views/CMP/StartExam.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - grep "skip-link" Views/CMP/StartExam.cshtml menampilkan 1 baris berisi `<a href="#mainContent" class="skip-link">`
    - grep 'id="mainContent"' Views/CMP/StartExam.cshtml menampilkan 1 baris pada div col-lg-9
    - grep "firstCard" Views/CMP/StartExam.cshtml menampilkan minimal 2 baris (deklarasi variabel + focus call)
    - grep "tabindex.*-1" Views/CMP/StartExam.cshtml menampilkan baris setAttribute tabindex -1
    - Anti-copy handler (keydown) TIDAK berisi block untuk Tab/Arrow/Enter/Space keys
  </acceptance_criteria>
  <done>Skip link muncul saat Tab, mengarah ke mainContent. Auto-focus aktif saat pindah halaman. Keyboard nav native berfungsi tanpa konflik anti-copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM | Skip link dan focus management sepenuhnya client-side, tidak ada trust boundary baru |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-302-F1 | Tampering | tabindex attributes | accept | tabindex="-1" hanya pada card soal, tidak mengubah form submission behavior |
</threat_model>

<verification>
1. `dotnet build` harus sukses tanpa error
2. `grep "skip-link" Views/CMP/StartExam.cshtml` menampilkan elemen anchor
3. `grep "mainContent" Views/CMP/StartExam.cshtml` menampilkan id dan href
4. `grep "firstCard" Views/CMP/StartExam.cshtml` menampilkan focus logic
</verification>

<success_criteria>
- Skip link "Lewati ke konten utama" tersedia di StartExam, hidden by default, muncul saat Tab
- Keyboard navigation (Tab antar soal, Arrow antar opsi) berfungsi native tanpa konflik anti-copy
- Auto-focus ke card soal pertama saat pindah halaman via performPageSwitch()
- Focus outline visible (2px solid #0d6efd) pada semua elemen focusable di area exam
</success_criteria>

<output>
After completion, create `.planning/phases/302-accessibility-wcag-quick-wins/302-01-SUMMARY.md`
</output>
