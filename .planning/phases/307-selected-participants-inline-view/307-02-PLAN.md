---
phase: 307-selected-participants-inline-view
plan: 02
type: execute
wave: 1
depends_on:
  - 307-01
files_modified:
  - Views/Admin/CreateAssessment.cshtml
autonomous: false
requirements:
  - WIZ-01
tags:
  - wizard
  - dom
  - vanilla-js
  - razor
  - refactor
user_setup: []

must_haves:
  truths:
    - "Step 2 wizard CreateAssessment.cshtml menampilkan panel `#selected-participants-panel-wrapper` setelah `#userCheckboxContainer` (line 309) dengan badge count + body render area, always-visible default empty state 'Belum ada peserta dipilih'"
    - "Centang/uncheck checkbox di `#userCheckboxContainer` ATAU `#protonUserCheckboxContainer` trigger update real-time: count badge `#selected-participants-count` immediate, panel body render dalam debounce 100ms window"
    - "Helper `renderSelectedParticipants(targetEl, checkboxes, opts)` exists sebagai top-level named function (di luar IIFE, antara line 1501 dan 1504), accessible dari main IIFE dan Proton IIFE"
    - "Helper `scheduleRenderSelectedPanel()` debounce wrapper exists di top-level scope dengan `setTimeout`/`clearTimeout` pattern 100ms"
    - "Function `updateSelectedCount()` di-HOIST ke top-level scope (deleted dari main IIFE line 1436-1447, re-declared di top-level antara line 1501 dan 1504), extended untuk juga update panel count badge (immediate) + schedule panel render (debounced)"
    - "Step 4 markup `summary-peserta-list-container` (refactor line 626-628 jadi single container), `populateSummary` line 1042-1103 di-refactor untuk delegate render ke helper baru — Step 4 output identik dengan Step 2 (no divergence)"
    - "Proton IIFE inline change listener line 1513-1520 di-replace dengan single line `protonCbContainer.addEventListener('change', updateSelectedCount)` — duplikasi count logic eliminated"
    - "Reset handler line 1726-1728 di-edit: hapus manual `badge.textContent = '0 terpilih'`, ganti dengan `updateSelectedCount();` call (panel auto-refresh ke empty state)"
    - "AJAX Proton success handler (line ~1574 setelah `protonCbContainer.classList.remove('d-none')`) explicit panggil `updateSelectedCount()` agar panel hydrate kalau ada pre-checked checkbox (Pitfall 3 RESEARCH)"
    - "All Phase 307 E2E tests (7.1-7.4) pass setelah implementasi merged"
    - "FLOW 1 test (1.2) tetap pass — no regression dari rot fix dan tidak ada perubahan ke `#selectedCountBadge` behavior"
  artifacts:
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "Panel 'Peserta Terpilih' Step 2 + helper extract + IIFE refactor"
      contains: "selected-participants-panel-wrapper"
  key_links:
    - from: "main IIFE selectAllBtn handler (line 1417-1424)"
      to: "top-level updateSelectedCount() (post-hoist)"
      via: "bareword call `updateSelectedCount();` (existing line 1424)"
      pattern: "updateSelectedCount\\(\\)"
    - from: "Proton IIFE change listener (line 1515 post-edit)"
      to: "top-level updateSelectedCount()"
      via: "addEventListener callback reference"
      pattern: "addEventListener\\('change', updateSelectedCount\\)"
    - from: "updateSelectedCount() (top-level post-hoist)"
      to: "scheduleRenderSelectedPanel() → renderSelectedParticipants()"
      via: "function call dengan panel element + checkbox array"
      pattern: "scheduleRenderSelectedPanel\\("
    - from: "populateSummary() (line ~1052 post-refactor)"
      to: "renderSelectedParticipants() helper"
      via: "single helper call dengan opts.countBadgeEl = #summary-peserta-count"
      pattern: "renderSelectedParticipants\\("
    - from: "reset handler (line 1726-1727 post-edit)"
      to: "top-level updateSelectedCount()"
      via: "single bareword call replace manual badge update"
      pattern: "updateSelectedCount\\(\\);"
---

<objective>
Wave 1: Implementasi inti REQ WIZ-01 — single-file edit ke `Views/Admin/CreateAssessment.cshtml` yang menambahkan panel "Peserta Terpilih" Step 2 dengan helper JS yang shared antara Step 2 (real-time view) dan Step 4 (populateSummary). Plan ini DEPEND on Wave 0 (307-01-PLAN) yang sudah membuat test infrastructure — setiap task di plan ini dapat di-smoke-verify via Playwright Phase 307 tests.

Purpose: Real-time visibility participant selection di Step 2 (audit Temuan 4) — saat ini user harus advance ke Step 4 untuk lihat list yang dipilih, friction tinggi untuk wizard 50+ peserta. Phase 307 menutup gap dengan helper extract yang juga konsolidasi duplikat code (Step 4 populateSummary, Proton IIFE inline count, reset handler manual badge update) → 1 source of truth untuk count + panel render lifecycle.

Output: 1 file di-edit (`Views/Admin/CreateAssessment.cshtml`) dengan 6 perubahan logis: (1) markup panel insert setelah line 309, (2) Step 4 markup refactor line 625-629 single container, (3) helper top-level + debounce + hoist `updateSelectedCount` antara line 1501-1504, (4) `populateSummary` refactor delegate ke helper, (5) Proton IIFE listener replace + AJAX hydrate call, (6) reset handler edit line 1726-1728.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/307-selected-participants-inline-view/307-CONTEXT.md
@.planning/phases/307-selected-participants-inline-view/307-RESEARCH.md
@.planning/phases/307-selected-participants-inline-view/307-PATTERNS.md
@.planning/phases/307-selected-participants-inline-view/307-VALIDATION.md
@.planning/phases/307-selected-participants-inline-view/307-UAT.md
@.planning/phases/307-selected-participants-inline-view/307-01-SUMMARY.md
@CLAUDE.md
@Views/Admin/CreateAssessment.cshtml

<interfaces>
<!-- Verbatim DOM IDs, function signatures, dan line ranges yang executor butuh — extracted dari verifikasi langsung file. -->
<!-- Executor TIDAK perlu eksplorasi codebase untuk dapat informasi konkret. -->

EXISTING DOM IDs (UNCHANGED — Phase 304 D-18 stability):
- `#selectedCountBadge` (line 289 — filter bar badge, format "N terpilih")
- `#userCheckboxContainer` (line 294 — normal user list container)
- `#protonUserCheckboxContainer` (line 316 — Proton coachee container, AJAX-loaded)
- `#protonEligibleSection` (line 312 — Proton wrapper)
- `#summary-peserta-count` (line 620 — Step 4 header badge, format "N peserta")
- `#selectAllBtn` (line ~286 — "Pilih Semua" button)
- `#deselectAllBtn` (line 286 — "Batalkan Semua" button)
- `#Category` (form select — used for isProton flag)
- `.user-checkbox` (class — checkbox element)
- `.user-check-item` (class — wrapper div with data-name etc)

NEW DOM IDs (Phase 307 — selected-participants-* prefix per D-13):
- `#selected-participants-panel-wrapper` (root container, mini-card style)
- `#selected-participants-panel` (body — helper render target, role="status" aria-live="polite")
- `#selected-participants-count` (header badge, format "N peserta")

REFACTORED DOM IDs (Phase 307 — Option A markup consolidation):
- REMOVE: `#summary-peserta-list`, `#summary-peserta-expand`, `#summary-peserta-extra` (line 626-628)
- ADD: `#summary-peserta-list-container` (line ~626 — single container, helper render target)
- KEEP: `#summary-peserta-count` (line 620 — header badge UNCHANGED)

EXISTING JS function signatures di main IIFE (line 831-1501):
```javascript
function populateSummary() { ... }                    // line 1042-1115+
function updateSelectedCount() { ... }                // line 1436-1447 (akan di-HOIST)
// Event handlers: selectAllBtn (line 1417-1424), deselectAllBtn (line 1426-1433)
// Existing call: userContainer.addEventListener('change', updateSelectedCount) (line 1450)
// Existing call: updateSelectedCount(); (line 1451 — initial)
```

NEW JS function signatures (top-level, antara line 1501 dan 1504):
```javascript
function renderSelectedParticipants(targetEl, checkboxes, opts) { ... }
function scheduleRenderSelectedPanel(targetEl, checkboxes, opts) { ... }
function updateSelectedCount() { ... }   // HOISTED dari line 1436
var __selectedParticipantsRenderTimer = null;   // module-scope debounce state
```

Bahasa Indonesia strings (CLAUDE.md C-01):
- Panel header text: "Peserta Terpilih"
- Empty state: "Belum ada peserta dipilih"
- Expand: `'...dan ' + N + ' lainnya'`
- Collapse: "Sembunyikan"
- Count Step 2 panel: `N + ' peserta'`
- Count filter bar: `N + ' terpilih'` (existing format, tidak diubah)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: INSERT panel markup Step 2 + REFACTOR markup Step 4 jadi single container</name>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml lines 280-320 (verify current state Step 2 markup, terutama close `</div>` line 309 dan `<div id="protonEligibleSection">` line 311)
    - Views/Admin/CreateAssessment.cshtml lines 615-635 (verify current state Step 4 summary peserta card, line 625-629 yang akan di-konsolidasi)
    - .planning/phases/307-selected-participants-inline-view/307-PATTERNS.md (item 1 NEW Panel Markup + item 5 EXTEND populateSummary Option A markup)
    - .planning/phases/307-selected-participants-inline-view/307-CONTEXT.md (D-01, D-02, D-13, CD-01, CD-05, CD-07)
    - .planning/phases/307-selected-participants-inline-view/307-RESEARCH.md (Code Examples — Phase 307 panel markup recommended)
  </read_first>
  <action>
DUA edit di file `Views/Admin/CreateAssessment.cshtml`:

**Edit 1: INSERT panel markup setelah `</div>` close `#userCheckboxContainer` (line 309)**

Lokasi insertion: tepat setelah line 309 (`</div>` close `#userCheckboxContainer`), sebelum line 310 blank line, sebelum `<!-- Proton eligible section -->` line 311.

INSERT block ini (10 lines, mengikuti VERBATIM markup dari PATTERNS.md item 1):

```html
                    <!-- Phase 307 — Panel "Peserta Terpilih" inline (D-01, D-02, D-13, CD-01, CD-05, CD-07) -->
                    <div class="border rounded p-3 mt-3" id="selected-participants-panel-wrapper">
                        <div class="d-flex align-items-center mb-2">
                            <strong><i class="bi bi-people-fill me-1 text-primary"></i>Peserta Terpilih</strong>
                            <span class="badge bg-primary ms-2" id="selected-participants-count">0 peserta</span>
                        </div>
                        <div id="selected-participants-panel" role="status" aria-live="polite" aria-atomic="false">
                            <span class="text-muted fst-italic">Belum ada peserta dipilih</span>
                        </div>
                    </div>

```

Pastikan indentation match indentation existing line 309 `</div>` (5 levels of 4-space indent atau apa pun yang persis match line 294 `<div id="userCheckboxContainer"`).

**Edit 2: REFACTOR markup Step 4 line 625-629 jadi single container (Option A per RESEARCH A1)**

Locate block ini (line 625-629, verify dengan grep `id="summary-peserta-list"`):

BEFORE (line 625-629 verbatim):
```html
                        <div class="card-body">
                            <span id="summary-peserta-list"></span>
                            <button type="button" id="summary-peserta-expand" class="btn btn-link btn-sm p-0 d-none ms-1"></button>
                            <span id="summary-peserta-extra" class="d-none"></span>
                        </div>
```

AFTER (Option A — single container, helper render target):
```html
                        <div class="card-body" id="summary-peserta-list-container">
                            <!-- Phase 307 — populateSummary helper render target (D-13, A1 Option A) -->
                        </div>
```

Catatan implementasi:
- Edit 1 ID prefix BARU `selected-participants-*` per D-13 (TIDAK conflict dengan existing).
- Edit 2 menghapus 3 ID lama (`summary-peserta-list`, `summary-peserta-expand`, `summary-peserta-extra`) — ID `summary-peserta-count` di header (line 620) DIPERTAHANKAN per Phase 304 D-18 stability (consumer JS reference).
- Setelah edit ini, `populateSummary` (Task 3) akan reference `#summary-peserta-list-container` instead of 3 separate ID — single helper call.
  </action>
  <verify>
    <automated>grep -n "id=\"selected-participants-panel-wrapper\"" "Views/Admin/CreateAssessment.cshtml" && grep -n "id=\"summary-peserta-list-container\"" "Views/Admin/CreateAssessment.cshtml"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "id=\"selected-participants-panel-wrapper\"" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 309-330
    - `grep -n "id=\"selected-participants-panel\"" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 309-330
    - `grep -n "id=\"selected-participants-count\"" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 309-330
    - `grep -n "Peserta Terpilih" Views/Admin/CreateAssessment.cshtml` returns minimum ONE line (header text — Bahasa Indonesia per CLAUDE.md C-01)
    - `grep -n "Belum ada peserta dipilih" Views/Admin/CreateAssessment.cshtml` returns minimum ONE line (server-rendered placeholder)
    - `grep -n "role=\"status\"" Views/Admin/CreateAssessment.cshtml` returns minimum ONE line (CD-07 a11y)
    - `grep -n "aria-live=\"polite\"" Views/Admin/CreateAssessment.cshtml` returns minimum ONE line (CD-07 a11y)
    - `grep -n "id=\"summary-peserta-list-container\"" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 620-635
    - `grep -c "id=\"summary-peserta-list\"\|id=\"summary-peserta-expand\"\|id=\"summary-peserta-extra\"" Views/Admin/CreateAssessment.cshtml` returns 0 (3 old IDs removed dari markup — populateSummary akan di-update di Task 3)
    - `grep -n "id=\"summary-peserta-count\"" Views/Admin/CreateAssessment.cshtml` returns ONE line (Step 4 header badge UNCHANGED — Phase 304 D-18)
    - `grep -n "id=\"selectedCountBadge\"" Views/Admin/CreateAssessment.cshtml` returns ONE line at original line 289 (filter bar badge UNCHANGED — Phase 304 D-18)
  </acceptance_criteria>
  <done>
Markup panel Step 2 inserted setelah line 309 dengan ID prefix `selected-participants-*` (3 IDs), Step 4 markup line 625-629 di-refactor jadi single container `#summary-peserta-list-container` (3 lama IDs removed). Existing IDs `#selectedCountBadge` (line 289) dan `#summary-peserta-count` (line 620) UNCHANGED. Indentation match konsisten dengan existing.
  </done>
</task>

<task type="auto">
  <name>Task 2: HOIST updateSelectedCount + INSERT helper renderSelectedParticipants + scheduleRenderSelectedPanel di top-level</name>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml lines 1430-1455 (verify existing `updateSelectedCount` body line 1436-1447 + initial call line 1451)
    - Views/Admin/CreateAssessment.cshtml lines 1495-1510 (verify IIFE close line 1501 + Proton IIFE open line 1504)
    - .planning/phases/307-selected-participants-inline-view/307-PATTERNS.md (items 2, 3, 4 — helper + debounce + hoist sections dengan VERBATIM target patterns)
    - .planning/phases/307-selected-participants-inline-view/307-RESEARCH.md (Pattern 1, 2, 3 + Pitfall 1 dan Common Pitfalls section)
    - .planning/phases/307-selected-participants-inline-view/307-CONTEXT.md (D-05, D-06, D-07, D-11, D-17, D-18, D-19, CD-04, CD-06, CD-07)
  </read_first>
  <action>
DUA sub-edit di file `Views/Admin/CreateAssessment.cshtml`:

**Edit 1: DELETE existing `updateSelectedCount` declaration di main IIFE (line 1435-1447)**

Locate block (line 1435-1447 verbatim, verify dengan grep `function updateSelectedCount`):

```javascript
    // ---- Selected count badge ----
    function updateSelectedCount() {
        var catEl = document.getElementById('Category');
        var isProton = catEl && catEl.value === 'Assessment Proton';
        var count;
        if (isProton) {
            count = document.querySelectorAll('#protonUserCheckboxContainer .user-checkbox:checked').length;
        } else {
            count = document.querySelectorAll('#userCheckboxContainer .user-checkbox:checked').length;
        }
        var badge = document.getElementById('selectedCountBadge');
        if (badge) badge.textContent = count + ' terpilih';
    }
```

DELETE seluruh block ini (line 1435 comment + line 1436-1447 function declaration). Existing call sites di line 1424 (selectAllBtn), 1432 (deselectAllBtn), 1450 (event listener), 1451 (initial call) TIDAK perlu di-update — bareword `updateSelectedCount` resolves ke top-level setelah hoist.

**Edit 2: INSERT 3 top-level functions + module-scope timer var di antara line 1501 dan 1504**

Lokasi insertion: tepat setelah line 1501 (`})();` close main IIFE), sebelum line 1503 (`// ===== Proton Track AJAX =====`).

INSERT block ini VERBATIM (top-level scope, masih di dalam `<script>` block existing):

```javascript

// ============================================================
// Phase 307 — Top-level helpers (cross-IIFE accessibility per CD-06)
// ============================================================
// renderSelectedParticipants: shared helper untuk Step 2 panel + Step 4 summary
// scheduleRenderSelectedPanel: debounce 100ms wrapper (D-19)
// updateSelectedCount: HOISTED dari main IIFE — accessible dari Proton IIFE (D-18)
// ============================================================

function renderSelectedParticipants(targetEl, checkboxes, opts) {
    opts = opts || {};
    var maxInline = opts.maxInline || 5;
    var emptyText = opts.emptyText || 'Belum ada peserta dipilih';

    // Extract names — mirror existing populateSummary line 1061-1068 logic 1:1 (D-15)
    var names = (checkboxes || []).map(function(cb) {
        var label = document.querySelector('label[for="' + cb.id + '"]');
        if (label) {
            var strong = label.querySelector('strong');
            return strong ? strong.textContent : label.textContent.trim();
        }
        return cb.value;
    });

    // DocumentFragment build (D-19 + Guide.cshtml:739 precedent)
    var frag = document.createDocumentFragment();

    if (names.length === 0) {
        var empty = document.createElement('span');
        empty.className = 'text-muted fst-italic';
        empty.textContent = emptyText;
        frag.appendChild(empty);
    } else {
        var first = names.slice(0, maxInline);
        var listSpan = document.createElement('span');
        listSpan.textContent = first.join(', ');
        frag.appendChild(listSpan);

        if (names.length > maxInline) {
            var remaining = names.slice(maxInline);
            var btn = document.createElement('button');
            btn.type = 'button';
            btn.className = 'btn btn-link btn-sm p-0 ms-1';
            btn.textContent = '...dan ' + remaining.length + ' lainnya';

            var extra = document.createElement('span');
            extra.className = 'd-none';

            // onclick property assignment (mirror line 1087, no listener leak — Pitfall 5)
            btn.onclick = function() {
                if (extra.classList.contains('d-none')) {
                    extra.textContent = ', ' + remaining.join(', ');
                    extra.classList.remove('d-none');
                    btn.textContent = 'Sembunyikan';
                } else {
                    extra.classList.add('d-none');
                    btn.textContent = '...dan ' + remaining.length + ' lainnya';
                }
            };

            frag.appendChild(btn);
            frag.appendChild(extra);
        }
    }

    // replaceChildren atomic (with defensive fallback per A3 — IE11/legacy)
    if (targetEl) {
        if (typeof targetEl.replaceChildren === 'function') {
            targetEl.replaceChildren(frag);
        } else {
            targetEl.innerHTML = '';
            targetEl.appendChild(frag);
        }
    }

    // Optional badge update (D-07) — Step 4 caller pakai opts.countBadgeEl
    if (opts.countBadgeEl) {
        opts.countBadgeEl.textContent = names.length + ' peserta';
    }
}

// Phase 307 — Module-scope debounce timer (D-11, D-19, CD-04 setTimeout)
var __selectedParticipantsRenderTimer = null;
function scheduleRenderSelectedPanel(targetEl, checkboxes, opts) {
    if (__selectedParticipantsRenderTimer) clearTimeout(__selectedParticipantsRenderTimer);
    __selectedParticipantsRenderTimer = setTimeout(function() {
        renderSelectedParticipants(targetEl, checkboxes, opts);
        __selectedParticipantsRenderTimer = null;
    }, 100);
}

// Phase 307 — HOISTED dari main IIFE (line 1436-1447 di-DELETE) — D-17 + D-18 + CD-06
// Single source of truth: count badge filter bar (immediate) + panel count (immediate) + panel body (debounced)
function updateSelectedCount() {
    var catEl = document.getElementById('Category');
    var isProton = catEl && catEl.value === 'Assessment Proton';

    // Query checkboxes dari container aktif (mirror existing isProton branching)
    var sel = isProton
        ? '#protonUserCheckboxContainer .user-checkbox:checked'
        : '#userCheckboxContainer .user-checkbox:checked';
    var checkboxes = Array.from(document.querySelectorAll(sel));

    // 1. Count badge filter bar — IMMEDIATE (cheap textContent assignment) (D-11)
    var badge = document.getElementById('selectedCountBadge');
    if (badge) badge.textContent = checkboxes.length + ' terpilih';

    // 2. Panel header count badge — IMMEDIATE (D-07: Step 2 update count terpisah dari panel render)
    var panelCount = document.getElementById('selected-participants-count');
    if (panelCount) panelCount.textContent = checkboxes.length + ' peserta';

    // 3. Panel body render — DEBOUNCED 100ms (D-11, D-19) — Phase 307 EXTEND
    var panel = document.getElementById('selected-participants-panel');
    if (panel) scheduleRenderSelectedPanel(panel, checkboxes, {
        emptyText: 'Belum ada peserta dipilih'
    });
}

```

Catatan implementasi:
- Insertion location: ANTARA `})();` line 1501 (close main IIFE) dan `// ===== Proton Track AJAX =====` line 1503. Pastikan ada blank line sebelum dan sesudah block insert.
- Penamaan timer var: `__selectedParticipantsRenderTimer` (lebih spesifik daripada generic `__renderTimer` di RESEARCH).
- `var` (bukan `let`/`const`) — match style ES5 dari file existing.
- Block insert SUDAH dalam scope `<script>` block existing (file pakai single inline script).
- TIDAK perlu update call sites existing line 1424 / 1432 / 1450 / 1451 — bareword `updateSelectedCount` resolves ke top-level setelah hoist (verified A2: tidak ada local rebinding).
  </action>
  <verify>
    <automated>grep -n "function renderSelectedParticipants" "Views/Admin/CreateAssessment.cshtml" && grep -n "function scheduleRenderSelectedPanel" "Views/Admin/CreateAssessment.cshtml" && [ "$(grep -c "function updateSelectedCount" "Views/Admin/CreateAssessment.cshtml")" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function updateSelectedCount" Views/Admin/CreateAssessment.cshtml` returns EXACTLY 1 (DELETE old + INSERT new at top-level — final count = 1)
    - `grep -n "function renderSelectedParticipants" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 1502-1605 (top-level position antara IIFE main close 1501 dan Proton IIFE open 1504-extended)
    - `grep -n "function scheduleRenderSelectedPanel" Views/Admin/CreateAssessment.cshtml` returns ONE line in same top-level range
    - `grep -n "function updateSelectedCount" Views/Admin/CreateAssessment.cshtml` returns ONE line in same top-level range (NO local declaration in main IIFE)
    - `grep -n "var __selectedParticipantsRenderTimer" Views/Admin/CreateAssessment.cshtml` returns ONE line in same top-level range
    - `grep -n "// ---- Selected count badge ----" Views/Admin/CreateAssessment.cshtml` returns ZERO lines (old comment + function block deleted from main IIFE)
    - Top-level `updateSelectedCount` body contains 3 update steps: filter badge, panel count, scheduleRenderSelectedPanel call. Verify: `grep -A 30 "function updateSelectedCount" Views/Admin/CreateAssessment.cshtml | grep -c "selectedCountBadge\|selected-participants-count\|scheduleRenderSelectedPanel"` returns minimum 3
    - `grep -n "scheduleRenderSelectedPanel" Views/Admin/CreateAssessment.cshtml` returns minimum 2 lines (declaration + call inside updateSelectedCount)
    - `grep -n "Belum ada peserta dipilih" Views/Admin/CreateAssessment.cshtml` returns minimum 3 lines (markup default + helper emptyText default + updateSelectedCount opts emptyText)
    - `grep -n "createDocumentFragment" Views/Admin/CreateAssessment.cshtml` returns ONE line in helper body (D-19 verified)
    - `grep -n "replaceChildren" Views/Admin/CreateAssessment.cshtml` returns minimum 2 lines (typeof check + atomic call) — defensive fallback A3 implemented
  </acceptance_criteria>
  <done>
3 top-level functions (`renderSelectedParticipants`, `scheduleRenderSelectedPanel`, `updateSelectedCount` HOISTED) + 1 module-scope timer var inserted antara line 1501-1504. Old `updateSelectedCount` declaration di main IIFE (line 1435-1447) DELETED. Helper body uses `textContent` (XSS-safe), `DocumentFragment`, `replaceChildren` dengan defensive fallback. Existing call sites tidak perlu update (bareword resolves to top-level).
  </done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR populateSummary delegate ke helper + REPLACE Proton IIFE listener + ADD AJAX hydrate call + EDIT reset handler</name>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml lines 1042-1120 (verify current populateSummary body — extract logic line 1052-1103)
    - Views/Admin/CreateAssessment.cshtml lines 1510-1525 (verify Proton IIFE inline change listener line 1513-1520)
    - Views/Admin/CreateAssessment.cshtml lines 1560-1580 (verify Proton AJAX success handler — line ~1574 `protonCbContainer.classList.remove('d-none')` adalah anchor untuk hydrate call insert)
    - Views/Admin/CreateAssessment.cshtml lines 1720-1735 (verify reset handler line 1726-1728)
    - .planning/phases/307-selected-participants-inline-view/307-PATTERNS.md (items 5, 6, 7 — populateSummary refactor + Proton IIFE replace + reset edit)
    - .planning/phases/307-selected-participants-inline-view/307-RESEARCH.md (Pitfall 2 reset gap + Pitfall 3 AJAX Proton race)
    - .planning/phases/307-selected-participants-inline-view/307-CONTEXT.md (D-15, D-17, D-18)
  </read_first>
  <action>
EMPAT sub-edit di file `Views/Admin/CreateAssessment.cshtml` (semua dependent pada Task 1 markup + Task 2 helpers existing):

**Edit 1: REFACTOR `populateSummary()` body line 1052-1103 — delegate ke helper**

Locate `populateSummary()` function (starts line 1042). REPLACE block dari line 1052 (`// Peserta`) sampai line 1103 (`if (extraEl) extraEl.classList.add('d-none');\n}`). Lines 1043-1051 (Category & Title — UNCHANGED). Lines 1105+ (Tipe badge, Settings, dll — UNCHANGED).

BEFORE (line 1052-1103 verbatim — extract & replace):

```javascript
        // Peserta
        var isProton = catEl && catEl.value === 'Assessment Proton';
        var checkboxes;
        if (isProton) {
            checkboxes = Array.from(document.querySelectorAll('#protonUserCheckboxContainer .user-checkbox:checked'));
        } else {
            checkboxes = Array.from(document.querySelectorAll('#userCheckboxContainer .user-checkbox:checked'));
        }

        var names = checkboxes.map(function(cb) {
            var label = document.querySelector('label[for="' + cb.id + '"]');
            if (label) {
                var strong = label.querySelector('strong');
                return strong ? strong.textContent : label.textContent.trim();
            }
            return cb.value;
        });

        var countBadge = document.getElementById('summary-peserta-count');
        if (countBadge) countBadge.textContent = names.length + ' peserta';

        var listEl = document.getElementById('summary-peserta-list');
        var expandBtn = document.getElementById('summary-peserta-expand');
        var extraEl = document.getElementById('summary-peserta-extra');

        if (listEl) {
            var first5 = names.slice(0, 5);
            listEl.textContent = first5.join(', ');
        }

        if (names.length > 5) {
            var remaining = names.slice(5);
            if (expandBtn) {
                expandBtn.classList.remove('d-none');
                expandBtn.textContent = '...dan ' + remaining.length + ' lainnya';
                expandBtn.onclick = function() {
                    if (extraEl) {
                        if (extraEl.classList.contains('d-none')) {
                            extraEl.textContent = ', ' + remaining.join(', ');
                            extraEl.classList.remove('d-none');
                            expandBtn.textContent = 'Sembunyikan';
                        } else {
                            extraEl.classList.add('d-none');
                            expandBtn.textContent = '...dan ' + remaining.length + ' lainnya';
                        }
                    }
                };
            }
        } else {
            if (expandBtn) expandBtn.classList.add('d-none');
            if (extraEl) extraEl.classList.add('d-none');
        }
```

AFTER (Phase 307 D-15 DRY — single helper call):

```javascript
        // Peserta — Phase 307: delegate ke renderSelectedParticipants helper (D-15 DRY)
        var isProton = catEl && catEl.value === 'Assessment Proton';
        var checkboxes = isProton
            ? Array.from(document.querySelectorAll('#protonUserCheckboxContainer .user-checkbox:checked'))
            : Array.from(document.querySelectorAll('#userCheckboxContainer .user-checkbox:checked'));

        renderSelectedParticipants(
            document.getElementById('summary-peserta-list-container'),
            checkboxes,
            {
                countBadgeEl: document.getElementById('summary-peserta-count'),
                emptyText: 'Belum ada peserta dipilih',
                maxInline: 5
            }
        );
```

**Edit 2: REPLACE Proton IIFE inline change listener line 1513-1520**

Locate block (verify dengan grep `protonCbContainer.addEventListener('change', function`):

BEFORE (line 1513-1520 verbatim):

```javascript
    // Event delegation: listen once on container, works for dynamically added checkboxes
    if (protonCbContainer) {
        protonCbContainer.addEventListener('change', function() {
            var count = protonCbContainer.querySelectorAll('.user-checkbox:checked').length;
            var badge = document.getElementById('selectedCountBadge');
            if (badge) badge.textContent = count + ' terpilih';
        });
    }
```

AFTER (Phase 307 D-18 — single source of truth via top-level updateSelectedCount):

```javascript
    // Event delegation: listen once on container, works for dynamically added checkboxes
    // Phase 307 D-18: delegate ke top-level updateSelectedCount (eliminates duplikasi count logic)
    if (protonCbContainer) {
        protonCbContainer.addEventListener('change', updateSelectedCount);
    }
```

**Edit 3: ADD explicit `updateSelectedCount()` call setelah AJAX inject Proton checkbox (Pitfall 3 RESEARCH)**

Locate AJAX success handler di Proton IIFE — anchor adalah `protonCbContainer.classList.remove('d-none');` (line ~1574 di branching `else` (kalau users array > 0), sebelum `}` close `.then` callback).

BEFORE (line ~1573-1575 verbatim):

```javascript
                    });
                    protonCbContainer.classList.remove('d-none');
                }
```

AFTER (Phase 307 — Pitfall 3 hydrate panel setelah AJAX inject):

```javascript
                    });
                    protonCbContainer.classList.remove('d-none');
                    // Phase 307 (Pitfall 3): hydrate panel setelah inject — checkbox baru tidak fire change event sendiri
                    updateSelectedCount();
                }
```

**Edit 4: EDIT reset handler line 1726-1728 (Pitfall 2 RESEARCH)**

Locate block (verify dengan grep `'.user-checkbox'.*forEach.*checked = false`):

BEFORE (line 1726-1728 verbatim):

```javascript
            document.querySelectorAll('.user-checkbox').forEach(function(cb) { cb.checked = false; });
            var badge = document.getElementById('selectedCountBadge');
            if (badge) badge.textContent = '0 terpilih';
```

AFTER (Phase 307 D-17 piggyback — single source of truth):

```javascript
            document.querySelectorAll('.user-checkbox').forEach(function(cb) { cb.checked = false; });
            // Phase 307 D-17 (Pitfall 2): single call refresh badge filter bar + panel count + panel body
            updateSelectedCount();
```

Catatan implementasi:
- Edit 1 (populateSummary): Lines 1043-1051 (Category & Title) UNCHANGED. Lines 1105+ (Tipe badge, Settings) UNCHANGED. Hanya body 1052-1103 di-replace.
- Edit 2 (Proton IIFE): Comment baru optional — kalau ingin terse, hanya ganti line 1515-1519 dengan satu line `protonCbContainer.addEventListener('change', updateSelectedCount);`.
- Edit 3 (AJAX hydrate): Pastikan insertion DI DALAM `.then(function(users) { ... if (!users || users.length === 0) { ... } else { ... users.forEach(...); protonCbContainer.classList.remove('d-none'); /* HYDRATE HERE */ } })` — bukan setelah `.catch()`. Verify dengan baca sekitar line 1565-1580 untuk lokasi exact.
- Edit 4 (reset): SETELAH reset semua `.user-checkbox`. Pastikan bareword `updateSelectedCount();` adalah top-level reference (kalau Task 2 hoist sukses, ini auto-resolve).

Setelah 4 edit ini selesai, RUN smoke test:
```bash
cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307"
```
Expect: 4 tests PASS (atau minimum 3 — test 7.4 dependent pada deselectAllBtn flow yang stabil). Kalau ada FAIL, periksa exact selector vs markup yang baru dibuat.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "renderSelectedParticipants(" Views/Admin/CreateAssessment.cshtml` returns minimum 3 lines (declaration + 2 calls — di updateSelectedCount Task 2 + di populateSummary Task 3)
    - `grep -n "summary-peserta-list-container" Views/Admin/CreateAssessment.cshtml` returns minimum 2 lines (markup Task 1 + populateSummary call Task 3)
    - `grep -c "summary-peserta-list\b" Views/Admin/CreateAssessment.cshtml` returns 0 (lama IDs `summary-peserta-list/expand/extra` tidak lagi di-reference di JS — Option A consolidation complete)
    - `grep -c "summary-peserta-expand\|summary-peserta-extra" Views/Admin/CreateAssessment.cshtml` returns 0 (3 lama IDs tidak ada lagi di JS)
    - `grep -n "summary-peserta-count" Views/Admin/CreateAssessment.cshtml` returns minimum 2 lines (markup line 620 UNCHANGED + populateSummary `countBadgeEl` reference Task 3)
    - `grep -n "addEventListener('change', updateSelectedCount)" Views/Admin/CreateAssessment.cshtml` returns minimum 2 lines (existing line 1450 main IIFE + Task 3 Edit 2 Proton IIFE)
    - `grep -A 2 "protonCbContainer.classList.remove('d-none');" Views/Admin/CreateAssessment.cshtml | grep -c "updateSelectedCount();"` returns 1 (Edit 3 hydrate call inserted setelah classList.remove)
    - `grep -A 1 "'.user-checkbox'..forEach.function.cb...cb.checked = false" Views/Admin/CreateAssessment.cshtml | grep -c "updateSelectedCount();"` returns 1 (Edit 4 reset replace badge update)
    - `grep -c "badge.textContent = '0 terpilih'" Views/Admin/CreateAssessment.cshtml` returns 0 (manual reset badge update line 1727-1728 di-replace dengan updateSelectedCount call)
    - `grep -c "var count = protonCbContainer.querySelectorAll" Views/Admin/CreateAssessment.cshtml` returns 0 (Proton IIFE inline duplicate count logic eliminated)
    - `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --reporter=list` exit code 0 (all 4 Phase 307 tests pass)
    - `cd tests && npx playwright test e2e/assessment.spec.ts --grep "1\\.2"` exit code 0 (no regression FLOW 1 test 1.2)
  </acceptance_criteria>
  <done>
4 sub-edit complete: populateSummary di-refactor delegate ke helper (Step 4 parity dengan Step 2), Proton IIFE listener replace single-line ke top-level updateSelectedCount, AJAX Proton success handler punya hydrate call (Pitfall 3 fixed), reset handler edit (Pitfall 2 fixed). 3 IDs lama (`summary-peserta-list/expand/extra`) zero references di file. Phase 307 E2E tests (4 tests) PASS, FLOW 1 test 1.2 PASS (no regression).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT 5-step Bahasa Indonesia (UAT.md)</name>
  <files>.planning/phases/307-selected-participants-inline-view/307-UAT.md</files>
  <action>
EXECUTE manual UAT script di .planning/phases/307-selected-participants-inline-view/307-UAT.md (created Wave 0 Task 3) — 5 step Bahasa Indonesia. Run di browser modern (Chrome/Edge ≥ 86) dengan DevTools terbuka. App harus running di development atau staging environment. Login sebagai HC. Sign-off section di akhir UAT.md harus diisi dengan tester name, timestamp, browser version, dan result PASS/FAIL. Capture screenshot evidence (Step 4 Console output, Step 5 side-by-side Step 2 vs Step 4 panel).
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL" .planning/phases/307-selected-participants-inline-view/307-UAT.md</automated>
  </verify>
  <done>
Sign-off section di UAT.md filled dengan tester name + timestamp + result PASS untuk semua 5 step. Performance delta Step 4 < 200ms (captured di Console screenshot). Step 5 side-by-side parity Step 2 vs Step 4 verified visual identik (captured screenshot).
  </done>
  <what-built>
Implementasi panel "Peserta Terpilih" Step 2 di `Views/Admin/CreateAssessment.cshtml`:
- Markup panel inserted setelah `#userCheckboxContainer` (line 309) — always-visible dengan badge count + body render area
- Helper `renderSelectedParticipants()` + `scheduleRenderSelectedPanel()` + HOISTED `updateSelectedCount()` di top-level scope
- Step 4 markup `summary-peserta-list-container` consolidated (Option A), `populateSummary()` delegate ke helper (DRY parity)
- Proton IIFE listener replace, AJAX success handler hydrate call (Pitfall 3 fixed)
- Reset handler "Buat lagi" di-edit (Pitfall 2 fixed)
- 4 Phase 307 E2E tests PASS automated, manual UAT 5-step required untuk performance + visual parity + Proton scenario
  </what-built>
  <how-to-verify>
EXECUTE manual UAT script di `.planning/phases/307-selected-participants-inline-view/307-UAT.md` — 5 step Bahasa Indonesia:

1. **Step 1 — Initial render & empty state:** Navigate `/Admin/CreateAssessment`, pilih Category "Assessment OJT", verify panel "Peserta Terpilih" tampil dengan placeholder "Belum ada peserta dipilih" + badge "0 peserta", filter bar badge "0 terpilih" UNCHANGED, no layout shift.

2. **Step 2 — Real-time toggle:** Centang 1/5/6 peserta, verify count badge immediate update, panel body update dalam ≤150ms, expand button "...dan N lainnya" muncul saat >5, klik expand reveal/collapse dengan tombol "Sembunyikan".

3. **Step 3 — Bulk operations:** Klik "Pilih Semua" (50+ user), verify single-batch render no flicker, klik "Batalkan Semua", verify revert ke empty state.

4. **Step 4 — Performance budget instrumentation:**
   ```javascript
   performance.mark('p307-start');
   document.getElementById('selectAllBtn').click();
   setTimeout(() => {
       performance.mark('p307-end');
       performance.measure('p307-render', 'p307-start', 'p307-end');
       console.log('[Phase 307] delta:', performance.getEntriesByName('p307-render')[0].duration.toFixed(2), 'ms');
   }, 200);
   ```
   Verify delta < 200ms (success criteria #4). Capture screenshot Console output.

5. **Step 5 — Reset modal "Buat lagi" + Proton mode + Step 2/Step 4 parity:** Submit assessment 3 peserta, klik "Buat lagi", verify reset. Switch Category ke "Assessment Proton", load track, centang 2 coachee, verify panel update dengan nama coachee. Advance ke Step 4, side-by-side compare panel Step 2 vs summary Step 4 — verify identik nama, urutan, format. Capture screenshot.

Sign-off di section bawah `307-UAT.md` dengan tester name + timestamp + browser version + result PASS/FAIL.

**Required evidence:**
- Screenshot Step 4 Console output dengan performance delta value
- Screenshot Step 5 side-by-side Step 2 vs Step 4 parity
- Filled sign-off section di UAT.md
  </how-to-verify>
  <resume-signal>Type "approved" jika 5 step PASS dan evidence captured, atau describe issues per step number</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server (Razor) → Client (browser) | `ApplicationUser.FullName` (sumber AD/LDAP) di-render via Razor `<strong>@user.FullName</strong>` line 304, lalu JS helper read dari DOM via `label.querySelector('strong').textContent`. Server tidak validate strict HTML chars di FullName. |
| Client → DOM | Helper Phase 307 build DOM dari user data — XSS risk kalau pakai `innerHTML` |
| Cross-IIFE function reference | Top-level helpers (`updateSelectedCount` etc) accessible dari main IIFE + Proton IIFE — exposure via global scope (avoiding `window.X` pollution per CD-06 hoist pattern) |

## STRIDE Threat Register

Phase 307 attack surface terbatas pada client-side DOM rendering dari data yang sudah server-rendered. ASVS L1 baseline.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-307-01 | Tampering / Information Disclosure (XSS) | `renderSelectedParticipants()` helper — name extraction dari `label.querySelector('strong').textContent` lalu render ke DOM | mitigate | Helper PAKAI `textContent` (NOT `innerHTML`) untuk semua user data assignment: `listSpan.textContent = first.join(', ')`, `extra.textContent = ...`, `btn.textContent = ...`, `empty.textContent = emptyText`. DOM API `textContent` is XSS-safe by construction — tidak parse HTML. Verify: `grep -n "innerHTML" Views/Admin/CreateAssessment.cshtml` di range helper top-level scope returns ZERO matches kecuali di defensive fallback `targetEl.innerHTML = ''` (string literal kosong, no user data). Pattern existing line 1079, 1086, 1090 sudah pakai textContent — Phase 307 helper preserve disiplin ini. |
| T-307-02 | DOM Clobbering | Panel ID baru `selected-participants-*` collision risk dengan existing IDs | mitigate | D-04 stability: panel pakai prefix UNIQUE `selected-participants-*` (3 IDs) yang TIDAK overlap dengan existing `selectedCountBadge` (line 289), `summary-peserta-*` (line 620+), atau `userCheckboxContainer` (line 294). Step 4 markup consolidation (Option A) replace 3 IDs (`summary-peserta-list/expand/extra`) dengan 1 ID (`summary-peserta-list-container`) — verify zero reference ke 3 IDs lama setelah Task 3. ID `summary-peserta-count` (line 620) UNCHANGED per Phase 304 D-18. Acceptance criterion `grep -c "summary-peserta-list\|summary-peserta-expand\|summary-peserta-extra"` = 0 enforces ini. |
| T-307-03 | Denial of Service (listener leak) | Repeated `renderSelectedParticipants()` call via `updateSelectedCount` → `scheduleRenderSelectedPanel` debounce | mitigate | Helper pakai `btn.onclick = function() {...}` (property assignment, single slot overwrite) bukan `addEventListener` (accumulating). `replaceChildren(frag)` atomic clear-and-fill — node lama disconnected dari DOM, listener pada node lama eligible untuk GC. Pattern existing line 1087 `expandBtn.onclick = ...` sudah konsisten. Debounce wrapper `clearTimeout(__selectedParticipantsRenderTimer)` sebelum `setTimeout` baru — single timer slot, tidak accumulate. Verify post-implementation via DevTools Memory profiling: instantiate panel berulang (50x toggle), cek detached DOM node count tidak grow linearly. |

**Catatan:** Tidak ada threat S/R/E (Spoofing / Repudiation / Elevation of Privilege) yang material untuk scope phase 307 — pure client-side render, no auth path, no privilege change, no audit log.
</threat_model>

<verification>
**Phase-level verification (Wave 1 complete):**

1. Markup audit: `grep -n "id=\"selected-participants-panel-wrapper\"" Views/Admin/CreateAssessment.cshtml` returns ONE line (Task 1 Edit 1)
2. Markup audit: `grep -n "id=\"summary-peserta-list-container\"" Views/Admin/CreateAssessment.cshtml` returns ONE line (Task 1 Edit 2)
3. Helper audit: `grep -c "function renderSelectedParticipants\|function scheduleRenderSelectedPanel\|function updateSelectedCount" Views/Admin/CreateAssessment.cshtml` returns 3 (Task 2 — 3 top-level functions)
4. Old IDs zero refs: `grep -c "summary-peserta-list\b\|summary-peserta-expand\|summary-peserta-extra" Views/Admin/CreateAssessment.cshtml` returns 0 (Task 1 + 3 — Option A consolidation complete)
5. Pitfall 2 fix: `grep -c "badge.textContent = '0 terpilih'" Views/Admin/CreateAssessment.cshtml` returns 0 (Task 3 Edit 4 — reset handler edit)
6. Pitfall 3 fix: `grep -A 2 "protonCbContainer.classList.remove('d-none');" Views/Admin/CreateAssessment.cshtml | grep -c "updateSelectedCount();"` returns 1 (Task 3 Edit 3 — AJAX hydrate)
7. E2E tests: `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --reporter=list` exit 0 (4 Phase 307 tests pass)
8. Regression: `cd tests && npx playwright test e2e/assessment.spec.ts --grep "1\\.2"` exit 0 (FLOW 1 not broken)
9. Full suite (optional): `cd tests && npx playwright test e2e/assessment.spec.ts` exit 0 (no regression in any assessment.spec test)
10. Manual UAT 5-step PASS dengan performance delta < 200ms captured + side-by-side parity screenshot
</verification>

<success_criteria>
Wave 1 complete saat:
- [ ] Task 1: Markup panel inserted (3 new IDs) + Step 4 consolidated (1 new ID, 3 lama removed)
- [ ] Task 2: 3 top-level helpers + module-scope timer var inserted di antara line 1501-1504; old `updateSelectedCount` di main IIFE deleted (count = 1)
- [ ] Task 3: populateSummary refactor delegate ke helper (Step 4 parity), Proton IIFE listener replace single-line, AJAX success handler hydrate call inserted, reset handler edit
- [ ] All `<acceptance_criteria>` per task lulus (grep verifications + Playwright test green)
- [ ] Phase 307 E2E tests (test 7.1, 7.2, 7.3, 7.4) PASS
- [ ] FLOW 1 test (1.2) PASS — no regression
- [ ] Task 4 (checkpoint): Manual UAT 5-step PASS dengan evidence (performance delta + side-by-side screenshot)
- [ ] All 5 ROADMAP success criteria Phase 307 verified:
  - [ ] #1: Panel "Peserta Terpilih" muncul setelah userCheckboxContainer dengan badge + 5 nama + tombol expand
  - [ ] #2: Real-time update saat checkbox toggle (event delegation di container)
  - [ ] #3: DRY helper `renderSelectedParticipants(targetEl, checkboxes, opts)` extract + reuse Step 2 + Step 4
  - [ ] #4: 50+ peserta render < 200ms (DocumentFragment + debounce 100ms) — UAT Step 4 evidence
  - [ ] #5: Step 2 list = Step 4 summary list (no divergence) — UAT Step 5 side-by-side evidence
</success_criteria>

<output>
After completion, create `.planning/phases/307-selected-participants-inline-view/307-02-SUMMARY.md` containing:
- Files modified (1 file: Views/Admin/CreateAssessment.cshtml)
- Lines added/removed (markup ~8 added, JS helpers ~85 added; markup 4 lines removed, IIFE updateSelectedCount 13 lines removed, populateSummary body ~50 lines reduced to ~12)
- Sub-edit count: Task 1 (2 markup), Task 2 (2 JS — delete + insert top-level), Task 3 (4 JS — populateSummary, Proton listener, AJAX hydrate, reset)
- Test results: Phase 307 (4 tests) + FLOW 1 (1.2) + full suite status
- UAT result: PASS/FAIL per step + performance delta value + browser version
- Verification commands run + exit codes
- Threat mitigation evidence: textContent usage count, DOM ID uniqueness audit, debounce timer single-slot verification
- Phase 307 success criteria checklist (5 items, all checked)
</output>
