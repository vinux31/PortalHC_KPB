---
phase: 308-prepost-wizard-validation-fix
plan: 02
type: execute
wave: 1
depends_on:
  - 308-01
files_modified:
  - Views/Admin/CreateAssessment.cshtml
  - Controllers/AssessmentAdminController.cs
autonomous: false
requirements:
  - WIZ-04
tags:
  - wizard
  - validation
  - prepost
  - razor
  - aspnet-mvc
  - vanilla-js
  - modelstate
user_setup: []

must_haves:
  truths:
    - "Saat user pilih `value === 'PrePostTest'` di Step 1 type selector di-Views/Admin/CreateAssessment.cshtml, JS handler di line 1876-1888 set `document.getElementById('Status').value = 'Upcoming'` (D-01) — verified via DevTools Console inspection"
    - "Saat user switch back PrePost → Standard di type selector, JS handler clear Status value `statusEl.value = ''` (D-02) sehingga dropdown kembali default `-- Pilih Status --`, force user re-pick"
    - "POST handler `Controllers/AssessmentAdminController.cs` line 779-782 punya conditional `if (isPrePostMode) ModelState.Remove(\"Status\")` (D-04) yang fire HANYA saat `AssessmentTypeInput == \"PrePostTest\"` — Standard mode validator tetap aktif (D-11 regression guard)"
    - "Submit assessment Pre-Post Test sukses tanpa error \"Status field is required\" yang me-reset wizard ke Step 1 (success criteria #4 main acceptance — bug REQ WIZ-04 fixed)"
    - "Standard mode tanpa pilih Status TETAP menampilkan validation error \"Status wajib dipilih\" (success criteria #5 regression guard — D-11)"
    - "Phase 307 helpers (`renderSelectedParticipants`, `scheduleRenderSelectedPanel`, `updateSelectedCount` di line 1469-1614) UNTOUCHED (D-17) — Phase 308 hanya touch handler block line 1860-1889 dan controller line 779-782"
    - "All 4 Phase 308 E2E tests (8.1, 8.2, 8.3, 8.4) PASS setelah implementasi merged (RED → GREEN transition dari Wave 0)"
    - "FLOW 1 test 1.2 + FLOW 7 (Phase 307) tests PASS — no regression dari Phase 308 edits"
    - ".NET build sanity: `dotnet build --no-restore` exit 0 (0 errors, max 92 pre-existing warnings — Phase 307 baseline)"
    - "Manual UAT 4-step Bahasa Indonesia PASS dengan sign-off filled di 308-UAT.md (test matrix 4 kombinasi + sub-step 1a regression check)"
  artifacts:
    - path: "Views/Admin/CreateAssessment.cshtml"
      provides: "JS value assignment (D-01, D-02) di typeSelect change handler — single edit di blok line 1872-1889"
      contains: "statusEl.value = 'Upcoming'"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Server-side conditional ModelState.Remove(\"Status\") (D-04, D-05) — 4-line insert antara line 779 dan 782"
      contains: "ModelState.Remove(\"Status\")"
  key_links:
    - from: "Views/Admin/CreateAssessment.cshtml line 1876 (`if (this.value === 'PrePostTest')`)"
      to: "DOM `#Status` element (line 481 `<select asp-for=\"Status\">`)"
      via: "`document.getElementById('Status').value = 'Upcoming'` (D-01 inside if-branch)"
      pattern: "statusEl\\.value = 'Upcoming'"
    - from: "Views/Admin/CreateAssessment.cshtml line 1883 (`else` branch — switch back to non-PrePost)"
      to: "DOM `#Status` element"
      via: "`statusEl.value = ''` (D-02 inside else-branch)"
      pattern: "statusEl\\.value = ''"
    - from: "Controllers/AssessmentAdminController.cs line 779 (`bool isPrePostMode = AssessmentTypeInput == \"PrePostTest\"`)"
      to: "ModelState collection (skip Status validation untuk PrePost path)"
      via: "`if (isPrePostMode) ModelState.Remove(\"Status\")` (D-04, between line 779 dan line 782)"
      pattern: "ModelState\\.Remove\\(\"Status\"\\)"
    - from: "Controllers/AssessmentAdminController.cs PrePost session creation paths (line 1078, 1112, 1170, 1644, 1663)"
      to: "AssessmentSession.Status persisted value"
      via: "Hardcoded `Status = \"Upcoming\"` (UNCHANGED — defense-in-depth, JS hint tidak authoritative)"
      pattern: "Status = \"Upcoming\""
---

<objective>
Wave 1: Implementasi inti REQ WIZ-04 — surgical 2-spot edit untuk fix bug "Status field is required" yang me-reset wizard ke Step 1 saat submit Pre-Post Test. Plan ini DEPEND on Wave 0 (308-01-PLAN) yang sudah membuat test scaffold (4 tests 8.1-8.4 + 4 selector baru + UAT.md) — setiap task dapat di-smoke-verify via Playwright Phase 308 tests.

Purpose: Audit Temuan 11 — Admin/HC submit assessment Pre-Post Test triggered server `[Required] Status` validation (Status field hidden untuk PrePost mode, value tidak di-set), causing `ModelState.IsValid == false` → controller `return View(model)` → init JS `goToStep(1)` → user lose progress + bingung error message untuk field yang tidak terlihat. Phase 308 menutup bug dengan two-layer fix: (1) JS handler set Status='Upcoming' / clear '' saat type-switch, (2) server `ModelState.Remove("Status")` conditional saat PrePost mode. Defense-in-depth: server line 1078/1112/1170 hardcode `Status = "Upcoming"` untuk PrePost session creation (TIDAK rely pada JS hint).

Output: 2 file di-edit dengan minimal-touch surgical edits — `Views/Admin/CreateAssessment.cshtml` (JS edit di handler block line 1872-1889 — tambah 1 lookup line + 2 value assignment lines), `Controllers/AssessmentAdminController.cs` (insert 5-line block antara line 779 dan 782). Total: ~8 lines added, 0 lines removed. Plus 1 checkpoint Task 4 manual UAT 4-step (blocking gate).

**RESEARCH-corrected items** (override CONTEXT.md):
- Form ID: `#createAssessmentForm` (BUKAN `#createForm` di CONTEXT D-07/CD-01) — verified line 102.
- Anchor JS handler: line **1876** (BUKAN ROADMAP refs 1790-1807, stale post-Phase 307 +47 lines).
- jQuery validate re-parse (D-07/D-08/D-09): **TIDAK APPLICABLE** — `_ValidationScriptsPartial` 0 matches di file (jquery.validate plugin tidak loaded). Existing `validateStep(n)` line 902 dengan visibility guard line 996-1004 sudah handle hidden Status case correctly. **D-07/D-08/D-09 di-SKIP total** per RESEARCH Pitfall 2 Pilihan A.
- ROADMAP success criteria #3 wording: N/A — superseded by existing custom validateStep visibility guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/308-prepost-wizard-validation-fix/308-CONTEXT.md
@.planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md
@.planning/phases/308-prepost-wizard-validation-fix/308-VALIDATION.md
@.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md
@.planning/phases/308-prepost-wizard-validation-fix/308-01-SUMMARY.md
@.planning/phases/307-selected-participants-inline-view/307-02-SUMMARY.md
@CLAUDE.md
@Views/Admin/CreateAssessment.cshtml
@Controllers/AssessmentAdminController.cs

<interfaces>
<!-- Verbatim DOM IDs, function signatures, line ranges, dan code snippets yang executor butuh — extracted langsung dari verifikasi RESEARCH.md (semua line numbers verified via grep 2026-04-29). -->
<!-- Executor TIDAK perlu eksplorasi codebase untuk dapat informasi konkret. -->

EXISTING DOM IDs di CreateAssessment.cshtml (RESEARCH-verified, post-Phase 307):
- `#createAssessmentForm` (line 102) — form root, attribute `novalidate`. **NOTE:** form ID correction per RESEARCH (CONTEXT D-07/CD-01 menulis `#createForm` — TIDAK ADA, 0 matches).
- `#assessmentTypeInput` (line 1862 — fetched di handler init) — Step 1 type selector dropdown
- `#statusFieldWrapper` (line 479 — markup; line 997 — validateStep guard; line 1873 — handler fetch) — Status field container div
- `#Status` (line 481) — Status field `<select>` element (asp-for binding to AssessmentSession.Status)
- `#prePostCertNote` (line 1874 — handler fetch) — info note tampil saat PrePost mode

EXISTING JS handler block di CreateAssessment.cshtml line 1860-1889 (VERIFIED post-Phase 307 +47 line shift, BUKAN ROADMAP ref 1790-1807):

```javascript
// Source: Views/Admin/CreateAssessment.cshtml line 1860-1889 (verified via Read 2026-04-29)
// Pre-Post Test mode toggle — Phase 297
document.addEventListener('DOMContentLoaded', function() {                  // line 1861
    var typeSelect = document.getElementById('assessmentTypeInput');         // line 1862
    var pptSection = document.getElementById('ppt-jadwal-section');
    var stdSection = document.getElementById('standard-jadwal-section');
    var preScheduleInput = document.getElementById('preSchedule');
    var postScheduleInput = document.getElementById('postSchedule');
    var samePackageCheck = document.getElementById('samePackageCheck');
    var samePackageBadge = document.getElementById('samePackageBadge');

    if (!typeSelect) return;                                                 // line 1870

    typeSelect.addEventListener('change', function() {                       // line 1872
        var statusWrapper = document.getElementById('statusFieldWrapper');   // line 1873
        var certNote = document.getElementById('prePostCertNote');           // line 1874

        if (this.value === 'PrePostTest') {                                  // line 1876 — D-01 ANCHOR
            pptSection.classList.add('show');
            if (stdSection) stdSection.classList.add('d-none');
            // Hide Status dropdown (Pre-Post always Upcoming)
            if (statusWrapper) statusWrapper.classList.add('d-none');
            // Show cert note
            if (certNote) certNote.classList.remove('d-none');
            // === PHASE 308 D-01 INSERT POINT ===
        } else {                                                             // line 1883 — D-02 ANCHOR
            pptSection.classList.remove('show');
            if (stdSection) stdSection.classList.remove('d-none');
            if (statusWrapper) statusWrapper.classList.remove('d-none');
            if (certNote) certNote.classList.add('d-none');
            // === PHASE 308 D-02 INSERT POINT ===
        }
    });                                                                      // line 1889

    // ...rest unchanged (preSchedule listener, samePackageCheck, etc — Phase 297 logic)
});
```

EXISTING controller block di AssessmentAdminController.cs line 778-782 (VERIFIED 2026-04-29):

```csharp
// Source: Controllers/AssessmentAdminController.cs line 778-782 (verified via Read 2026-04-29)

// Early Pre-Post mode determination (needed before standard field validation)
bool isPrePostMode = AssessmentTypeInput == "PrePostTest";                  // line 779
                                                                              // line 780 (blank)
                                                                              // line 781 (blank or comment — VERIFY pre-edit)
// Validate schedule date (skip for Pre-Post — uses PreSchedule/PostSchedule instead)  // line 782
if (!isPrePostMode)                                                          // line 783
{
    // ...rest unchanged
```

PATTERN existing `ModelState.Remove(...)` di file (5 instances — RESEARCH-verified anchor):
- Line 742: `ModelState.Remove("UserId");` — unconditional (model uses UserIds list)
- Line 756: `ModelState.Remove("AccessToken");` — conditional (`if (!model.IsTokenRequired)`)
- Line 821: `ModelState.Remove("ExamWindowCloseDate");` — unconditional + re-validate manually
- Line 835: `ModelState.Remove("ValidUntil");` — unconditional + re-validate manually
- Line 870: `ModelState.Remove("NomorSertifikat");` — unconditional (auto-generated)

PrePost session creation paths di controller — Status hardcoded "Upcoming" (5 instances, UNCHANGED — defense-in-depth):
- Line 1078, 1112, 1170, 1644, 1663: `Status = "Upcoming"` for PrePost AssessmentSession creation

EXISTING `validateStep(3)` Status visibility guard di CreateAssessment.cshtml line 996-1004 (UNCHANGED — supersede ROADMAP success criteria #3):

```javascript
// Source: Views/Admin/CreateAssessment.cshtml line 996-1004 (verified Read)
var statusWrapper = document.getElementById('statusFieldWrapper');
if (statusWrapper && !statusWrapper.classList.contains('d-none')) {
    var statusEl = document.getElementById('Status');
    if (!statusEl || !statusEl.value) {
        // ... show "Status wajib dipilih" error
    }
}
```

Bahasa Indonesia strings (CLAUDE.md C-01 — comments di code English per RESEARCH Open Question #2 resolution):
- Server-side comment: English (consistent dengan line 755 pattern)
- UAT.md script: Bahasa Indonesia (D-19)
- Test names: Bahasa Indonesia (CLAUDE.md C-01) — done di 308-01 Wave 0
- Error messages user-facing: Bahasa Indonesia ("Status wajib dipilih" — existing line 996-1004)

Form ID correction per RESEARCH (jQuery re-parse SKIPPED — D-07/D-08/D-09 N/A):
- jQuery validate plugin TIDAK loaded — `_ValidationScriptsPartial` 0 matches via grep
- D-07/D-08/D-09 dropped per RESEARCH Pitfall 2 Pilihan A
- Existing `validateStep(n)` visibility guard sudah handle hidden Status correctly (line 996-1004)
- ROADMAP success criteria #3 wording N/A → document di Task 1 action sebagai dropped scope dengan rationale
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: INSERT JS value assignment (D-01 + D-02) di typeSelect change handler line 1872-1889 — Views/Admin/CreateAssessment.cshtml</name>
  <files>Views/Admin/CreateAssessment.cshtml</files>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml lines 1860-1895 (verify current state handler block — typeSelect change handler dengan 2 branch if/else, line 1876 anchor `if (this.value === 'PrePostTest')`, line 1883 else branch)
    - Views/Admin/CreateAssessment.cshtml lines 478-487 (verify Status field markup unchanged — `<select asp-for="Status" id="Status">`)
    - Views/Admin/CreateAssessment.cshtml lines 996-1004 (verify existing validateStep visibility guard yang supersede jQuery validate re-parse — D-07/D-08/D-09 N/A)
    - .planning/phases/308-prepost-wizard-validation-fix/308-CONTEXT.md (D-01, D-02, D-03, D-12, D-17 — locked decisions; CD-01/CD-02/CD-03 — discretion areas)
    - .planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md (Code Examples Example 1 — verbatim D-01 + D-02 client value assignment; Pitfall 1, 2, 6 — form ID, jQuery N/A, no change event dispatch needed; Verified Line Numbers — anchor 1876 dan 1883)
    - .planning/phases/307-selected-participants-inline-view/307-02-SUMMARY.md (verify +47 net lines impact — handler block shifted dari ROADMAP ref 1790-1807 ke verified 1872-1889)
  </read_first>
  <action>
SATU edit di file `Views/Admin/CreateAssessment.cshtml` (handler block line 1872-1889 — extend dengan 1 lookup line + 2 value assignment lines).

**Lokasi insertion:** typeSelect.addEventListener('change', function() { ... }) di line 1872-1889. Add 1 line di awal handler body (after line 1874 `var certNote = ...;`), dan 2 lines (1 di if-branch line 1876-1882, 1 di else-branch line 1883-1888).

**BEFORE (line 1872-1889 verbatim — 18 lines):**

```javascript
    typeSelect.addEventListener('change', function() {
        var statusWrapper = document.getElementById('statusFieldWrapper');
        var certNote = document.getElementById('prePostCertNote');

        if (this.value === 'PrePostTest') {
            pptSection.classList.add('show');
            if (stdSection) stdSection.classList.add('d-none');
            // Hide Status dropdown (Pre-Post always Upcoming)
            if (statusWrapper) statusWrapper.classList.add('d-none');
            // Show cert note
            if (certNote) certNote.classList.remove('d-none');
        } else {
            pptSection.classList.remove('show');
            if (stdSection) stdSection.classList.remove('d-none');
            if (statusWrapper) statusWrapper.classList.remove('d-none');
            if (certNote) certNote.classList.add('d-none');
        }
    });
```

**AFTER (3 line additions — D-01 + D-02 + statusEl lookup):**

```javascript
    typeSelect.addEventListener('change', function() {
        var statusWrapper = document.getElementById('statusFieldWrapper');
        var certNote = document.getElementById('prePostCertNote');
        var statusEl = document.getElementById('Status');

        if (this.value === 'PrePostTest') {
            pptSection.classList.add('show');
            if (stdSection) stdSection.classList.add('d-none');
            // Hide Status dropdown (Pre-Post always Upcoming)
            if (statusWrapper) statusWrapper.classList.add('d-none');
            // Show cert note
            if (certNote) certNote.classList.remove('d-none');
            // Phase 308 D-01: auto-set Status='Upcoming' (matches server hardcode at line 1078/1112/1170)
            if (statusEl) statusEl.value = 'Upcoming';
        } else {
            pptSection.classList.remove('show');
            if (stdSection) stdSection.classList.remove('d-none');
            if (statusWrapper) statusWrapper.classList.remove('d-none');
            if (certNote) certNote.classList.add('d-none');
            // Phase 308 D-02: clear Status value — force user re-pick after switch back to Standard
            if (statusEl) statusEl.value = '';
        }
    });
```

**Three additions:**
1. `var statusEl = document.getElementById('Status');` — sebelum `if (this.value === 'PrePostTest')` (after line 1874 `var certNote = ...;`). Single lookup, DRY untuk both branches.
2. `if (statusEl) statusEl.value = 'Upcoming';` dengan comment `// Phase 308 D-01: ...` — di akhir if-branch (after `if (certNote) certNote.classList.remove('d-none');` line 1882).
3. `if (statusEl) statusEl.value = '';` dengan comment `// Phase 308 D-02: ...` — di akhir else-branch (after `if (certNote) certNote.classList.add('d-none');` line 1887).

Catatan implementasi:
- Defensive `if (statusEl)` check — paralel dengan existing `if (statusWrapper)` / `if (certNote)` style di handler. ES5 vanilla style match file existing.
- Single `statusEl` lookup di awal handler (DRY — satu `getElementById` call per change event, bukan dua). Penting karena change handler bisa fire multiple times (Pitfall 4 mode-switching scenarios).
- TIDAK panggil `statusEl.dispatchEvent(new Event('change'))` — per RESEARCH Pitfall 6 grep verified TIDAK ada listener `change` di `#Status` element. Programmatic update aman tanpa notifikasi.
- TIDAK INCLUDE D-07/D-08/D-09 jQuery validate re-parse code — RESEARCH Pitfall 2 Pilihan A: plugin tidak loaded (`_ValidationScriptsPartial` 0 matches di file), re-parse akan no-op atau throw TypeError tanpa guard. Existing `validateStep(n)` visibility guard line 996-1004 sudah handle hidden Status correctly. **ROADMAP success criteria #3 wording N/A — superseded.**
- Comment style English (consistent dengan existing line 755, 821 pattern di server code, dan line 1879/1881 inline JS comments).
- Indentation match indentation existing (4-space, 2 levels deep di dalam handler function body).
  </action>
  <verify>
    <automated>grep -n "statusEl.value = 'Upcoming'" Views/Admin/CreateAssessment.cshtml && grep -n "statusEl.value = ''" Views/Admin/CreateAssessment.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "var statusEl = document.getElementById('Status');" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 1872-1879 (single lookup di awal handler body)
    - `grep -n "statusEl.value = 'Upcoming'" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 1880-1888 (D-01 inside if-branch)
    - `grep -n "statusEl.value = ''" Views/Admin/CreateAssessment.cshtml` returns ONE line in range 1885-1895 (D-02 inside else-branch)
    - `grep -c "Phase 308 D-01\|Phase 308 D-02" Views/Admin/CreateAssessment.cshtml` returns minimum 2 (comments untuk traceability — D-01 dan D-02)
    - `grep -c "if (statusEl) statusEl.value" Views/Admin/CreateAssessment.cshtml` returns 2 (defensive guard pattern, 2 assignments)
    - `grep -c "if (this.value === 'PrePostTest')" Views/Admin/CreateAssessment.cshtml` returns minimum 1 (anchor preserved — handler block intact)
    - `grep -n "statusFieldWrapper" Views/Admin/CreateAssessment.cshtml` returns minimum 3 lines (line 479 markup + line 997 validateStep + line 1873 handler — semua UNCHANGED, D-12)
    - `grep -c "_ValidationScriptsPartial" Views/Admin/CreateAssessment.cshtml` returns 0 (D-07/D-08/D-09 SKIPPED per RESEARCH Pitfall 2 — jQuery validate plugin tidak loaded, re-parse code TIDAK ditambahkan)
    - `grep -c "\\$\\.validator\\.unobtrusive\\|removeData('validator')" Views/Admin/CreateAssessment.cshtml` returns 0 (D-07/D-08/D-09 explicitly NOT included — confirms Pilihan A)
    - `grep -n "Pre-Post Test mode toggle — Phase 297" Views/Admin/CreateAssessment.cshtml` returns 1 line (existing comment marker preserved — handler block context intact)
    - `dotnet build --no-restore` exit code 0 (Razor compile lulus tanpa error baru — verify edits tidak break .cshtml syntax)
  </acceptance_criteria>
  <done>
File `Views/Admin/CreateAssessment.cshtml` di-edit dengan 3 line additions di handler block line 1872-1889: (1) `var statusEl = ...` lookup di awal body, (2) `statusEl.value = 'Upcoming'` di if-branch (D-01), (3) `statusEl.value = ''` di else-branch (D-02). Comments include "Phase 308 D-01" dan "Phase 308 D-02" untuk traceability. D-07/D-08/D-09 jQuery validate re-parse SKIPPED per RESEARCH Pitfall 2 Pilihan A. Razor compile lulus.
  </done>
</task>

<task type="auto">
  <name>Task 2: INSERT server-side conditional ModelState.Remove("Status") (D-04 + D-05) di Controllers/AssessmentAdminController.cs antara line 779 dan 782</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs lines 740-785 (verify current state — pattern `ModelState.Remove("UserId")` line 742, `ModelState.Remove("AccessToken")` line 756, anchor `bool isPrePostMode` line 779, schedule validation conditional line 782)
    - Controllers/AssessmentAdminController.cs lines 970-985 (verify defensive default `if (string.IsNullOrEmpty(model.Status)) model.Status = "Open";` line 975-978 UNCHANGED — D-06)
    - Controllers/AssessmentAdminController.cs lines 1075-1180 (verify PrePost session creation paths line 1078, 1112, 1170 — Status="Upcoming" hardcoded UNCHANGED — defense-in-depth, JS hint tidak authoritative)
    - .planning/phases/308-prepost-wizard-validation-fix/308-CONTEXT.md (D-04, D-05, D-06, D-11 — locked decisions)
    - .planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md (Pattern 1 — conditional ModelState.Remove; Code Examples Example 3 — verbatim D-04 server insert; Verified Line Numbers — anchor 779)
  </read_first>
  <action>
SATU edit di file `Controllers/AssessmentAdminController.cs` (insert 5-line block antara line 779 dan 782 — sebelum block schedule validation).

**Lokasi insertion:** SETELAH line 779 (`bool isPrePostMode = AssessmentTypeInput == "PrePostTest";`) dan SEBELUM line 781-782 (blank line + comment `// Validate schedule date (skip for Pre-Post — uses PreSchedule/PostSchedule instead)`).

**BEFORE (line 778-789 verbatim — current state):**

```csharp
            // Early Pre-Post mode determination (needed before standard field validation)
            bool isPrePostMode = AssessmentTypeInput == "PrePostTest";

            // Validate schedule date (skip for Pre-Post — uses PreSchedule/PostSchedule instead)
            if (!isPrePostMode)
            {
                if (model.Schedule < DateTime.Today)
                {
                    ModelState.AddModelError("Schedule", "Schedule date cannot be in the past.");
                }

                if (model.Schedule > DateTime.Today.AddYears(2))
```

**AFTER (insert 5 lines — Phase 308 D-04 conditional ModelState.Remove):**

```csharp
            // Early Pre-Post mode determination (needed before standard field validation)
            bool isPrePostMode = AssessmentTypeInput == "PrePostTest";

            // Phase 308 D-04: Status field hidden in PrePost mode — JS sets default 'Upcoming', server skips [Required] validation
            if (isPrePostMode)
            {
                ModelState.Remove("Status");
            }

            // Validate schedule date (skip for Pre-Post — uses PreSchedule/PostSchedule instead)
            if (!isPrePostMode)
            {
                if (model.Schedule < DateTime.Today)
                {
                    ModelState.AddModelError("Schedule", "Schedule date cannot be in the past.");
                }

                if (model.Schedule > DateTime.Today.AddYears(2))
```

**Five-line insertion:**
1. `// Phase 308 D-04: Status field hidden in PrePost mode — JS sets default 'Upcoming', server skips [Required] validation` (comment line)
2. `if (isPrePostMode)` (conditional opening)
3. `{` (block opening)
4. `    ModelState.Remove("Status");` (action — exact field name, no glob)
5. `}` (block closing)

Plus 1 blank line setelah block close (line 6 — separator before existing `// Validate schedule date` comment).

Catatan implementasi:
- Comment style English — match existing pattern line 755 (`// Token is NOT required - remove from validation and clear value`) dan line 781 (`// Validate schedule date (skip for Pre-Post — uses PreSchedule/PostSchedule instead)`).
- Comment include "Phase 308 D-04" untuk traceability + rationale "JS sets default 'Upcoming', server skips [Required] validation" (per CONTEXT CD-02 dan RESEARCH suggestion).
- Conditional `if (isPrePostMode)` mirror pattern existing line 753-758 (`if (!model.IsTokenRequired) { ModelState.Remove("AccessToken"); model.AccessToken = ""; }`).
- HANYA `ModelState.Remove("Status")` — exact field name, BUKAN `ModelState.Clear()` (terlalu broad — akan remove semua field). Acceptance criterion enforces exact-string match.
- TIDAK set `model.Status = "Upcoming"` server-side karena: (a) JS sudah set client-side via D-01, (b) line 975-978 defensive fallback ke "Open" UNCHANGED untuk Standard fallback path, (c) line 1078/1112/1170 explicit set "Upcoming" di PrePost session creation. Triple defense — JS hint tidak authoritative, server hardcode menang.
- Indentation: 12 spaces (3 levels deep — di dalam method body, sama dengan line 742 `ModelState.Remove("UserId");`).
- TIDAK touch defensive default line 975-978 (D-06 UNCHANGED).
- TIDAK touch PrePost session creation line 1078, 1112, 1170, 1644, 1663 (Status hardcoded "Upcoming" UNCHANGED — defense-in-depth verified).
  </action>
  <verify>
    <automated>grep -A 3 "bool isPrePostMode" Controllers/AssessmentAdminController.cs | grep -c "ModelState.Remove(\"Status\")"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 5 "bool isPrePostMode = AssessmentTypeInput" Controllers/AssessmentAdminController.cs | grep -c "ModelState.Remove(\"Status\")"` returns 1 (exact insertion antara line 779 dan schedule validation block)
    - `grep -n "ModelState.Remove(\"Status\")" Controllers/AssessmentAdminController.cs` returns ONE line in range 780-786 (exactly 1 match — D-04 single insert, no duplicate)
    - `grep -n "// Phase 308 D-04" Controllers/AssessmentAdminController.cs` returns ONE line (traceability comment)
    - `grep -B 1 "ModelState.Remove(\"Status\")" Controllers/AssessmentAdminController.cs | grep -c "if (isPrePostMode)"` returns 1 (conditional wrapping verified — D-04 fire HANYA saat PrePost)
    - `grep -n "bool isPrePostMode = AssessmentTypeInput == \"PrePostTest\"" Controllers/AssessmentAdminController.cs` returns 1 line (anchor preserved — line 779 unchanged)
    - `grep -n "if (!isPrePostMode)" Controllers/AssessmentAdminController.cs` returns minimum 1 line in range 783-790 (existing schedule validation conditional preserved — D-04 insert sebelum block ini)
    - `grep -c "ModelState.Remove(\"UserId\")\|ModelState.Remove(\"AccessToken\")" Controllers/AssessmentAdminController.cs` returns minimum 2 (existing patterns line 742 dan 756 UNCHANGED)
    - `grep -n "if (string.IsNullOrEmpty(model.Status)) model.Status = \"Open\";" Controllers/AssessmentAdminController.cs` returns 1 line in range 970-980 (D-06 defensive default UNCHANGED)
    - `grep -c "Status = \"Upcoming\"" Controllers/AssessmentAdminController.cs` returns minimum 5 (5 PrePost session creation paths line 1078/1112/1170/1644/1663 UNCHANGED — defense-in-depth)
    - `dotnet build --no-restore` exit code 0 (build sanity — 0 errors, max 92 pre-existing warnings per Phase 307 baseline)
  </acceptance_criteria>
  <done>
File `Controllers/AssessmentAdminController.cs` di-edit dengan 5-line insert antara line 779 dan 782: comment + `if (isPrePostMode)` + block + `ModelState.Remove("Status")` + close. Defensive default line 975-978 UNCHANGED (D-06). PrePost session creation Status="Upcoming" hardcoded di 5 location UNCHANGED (defense-in-depth). .NET build lulus dengan 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run automated verification — Phase 308 E2E tests transition RED → GREEN + .NET build sanity + Phase 307 regression check</name>
  <files></files>
  <read_first>
    - .planning/phases/308-prepost-wizard-validation-fix/308-VALIDATION.md (Test Infrastructure + Sampling Rate sections — quick run + full suite commands; Per-Task Verification Map untuk task 308-02-03 dan 308-02-04)
    - tests/e2e/assessment.spec.ts (verify 4 Phase 308 tests 8.1-8.4 di FLOW 8 describe — added by Wave 0 Task 2)
    - .planning/phases/307-selected-participants-inline-view/307-02-SUMMARY.md (verify Phase 307 baseline — 4 tests 7.1-7.4 + FLOW 1 test 1.2 expected PASS)
    - .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md (review 4-step Bahasa Indonesia — created Wave 0 Task 3, akan executed di Task 4 checkpoint)
  </read_first>
  <action>
RUN sequential verification commands untuk confirm Wave 1 implementation berhasil sebelum Task 4 manual UAT checkpoint. Tidak ada file edit di task ini — pure verification.

**Step 1 — TypeScript compile sanity (no breakage from Wave 0 selector additions):**

```bash
cd tests && npx tsc --noEmit -p tsconfig.json
```

Expected: exit 0, no error output.

**Step 2 — Playwright Phase 308 tests RED → GREEN transition:**

```bash
cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --reporter=list
```

Expected: exit 0, 4 tests PASS (8.1, 8.2, 8.3, 8.4). Sebelum Wave 1 implementasi merge, tests ini adalah RED state (Status value tetap '' saat switch PrePost karena D-01 belum apply). Setelah Task 1 + Task 2 selesai, tests transition ke GREEN.

**Step 3 — Phase 307 regression check (no regression dari Phase 308 edits):**

```bash
cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --reporter=list
```

Expected: exit 0, 4 tests PASS (7.1, 7.2, 7.3, 7.4). Phase 308 edits di handler block line 1872-1889 TIDAK touch Phase 307 helpers di line 1469-1614 (D-17), so 0 regression expected.

**Step 4 — FLOW 1 test 1.2 regression check (Standard mode submit baseline tidak break):**

```bash
cd tests && npx playwright test e2e/assessment.spec.ts --grep "1\\.2" --reporter=list
```

Expected: exit 0, test 1.2 PASS. Test ini submit Standard assessment dengan Status='Open' — Phase 308 tidak break Standard submit path (D-11 regression guard).

**Step 5 — .NET build sanity:**

```bash
dotnet build --no-restore
```

Expected: exit 0, 0 errors, max 92 pre-existing warnings (CA1416 LdapAuthService — out of scope per Phase 307 baseline). Phase 308 server insert line 780-784 + Razor edit line 1872-1889 tidak introduce new warnings.

**Step 6 — Full suite optional (deeper regression check, requires dev server):**

```bash
cd tests && npx playwright test e2e/assessment.spec.ts --reporter=list
```

Expected: 0 failed tests. Run kalau dev server available; skip kalau tidak (Step 2-4 sudah cover critical regression).

Catatan implementasi:
- Tasks 1 dan 2 harus complete sebelum Task 3 (urutan sequential dalam plan).
- Kalau ada test FAIL di Step 2 (Phase 308 tests) — periksa exact match: D-01 set 'Upcoming' (Task 1)? D-04 ModelState.Remove (Task 2)? Re-grep verify acceptance criteria task 1 dan task 2.
- Kalau ada FAIL di Step 3 (Phase 307 regression) — periksa apakah Phase 308 edits tidak sengaja modify line 1469-1614 (D-17 violation). Use git diff untuk audit changes.
- Kalau ada FAIL di Step 4 (FLOW 1 test 1.2) — periksa server-side: Standard mode masih trigger Status validation (D-11)? Verify `if (isPrePostMode)` conditional dengan grep.
- Step 5 .NET build: 92 pre-existing warnings expected (Phase 307 baseline). Lebih dari 92 = Phase 308 introduce new warning, investigate.
- Step 6 full suite optional — nice-to-have untuk extra confidence sebelum Task 4 manual UAT.
- Setelah Task 3 selesai (Step 1-5 PASS), proceed ke Task 4 manual UAT checkpoint.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --reporter=list && cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307|1\\.2" --reporter=list && cd .. && dotnet build --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - Step 1: `cd tests && npx tsc --noEmit -p tsconfig.json` exit code 0 (no TypeScript error)
    - Step 2: `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --reporter=list` exit code 0 dan output mengandung "4 passed" atau equivalent (4 tests 8.1-8.4 PASS — RED → GREEN transition complete)
    - Step 3: `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --reporter=list` exit code 0 dan output mengandung "4 passed" (4 tests 7.1-7.4 PASS — Phase 307 baseline preserved, no regression)
    - Step 4: `cd tests && npx playwright test e2e/assessment.spec.ts --grep "1\\.2" --reporter=list` exit code 0 dan output mengandung "1 passed" (test 1.2 PASS — FLOW 1 Standard mode submit baseline)
    - Step 5: `dotnet build --no-restore` exit code 0 dan output mengandung "0 Error" atau "Build succeeded"
    - Pre-existing warning count ≤ 92 (Phase 307 baseline) — verify dengan grep output `dotnet build --no-restore 2>&1 | grep -c "warning"` returns ≤ 92
    - Step 6 (optional): `cd tests && npx playwright test e2e/assessment.spec.ts --reporter=list` exit code 0 (kalau dev server available)
  </acceptance_criteria>
  <done>
Automated verification complete: 4 Phase 308 tests PASS (RED → GREEN transition dari Wave 0), 4 Phase 307 tests PASS (no regression), FLOW 1 test 1.2 PASS (Standard mode baseline preserved), .NET build 0 errors. Ready untuk Task 4 manual UAT checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT 4-step Bahasa Indonesia (308-UAT.md) — checkpoint blocking gate</name>
  <files>.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md</files>
  <read_first>
    - .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md (file yang dieksekusi sebagai script UAT)
    - .planning/phases/308-prepost-wizard-validation-fix/308-RESEARCH.md (Pitfall 4 mode-switch UX, Pitfall 6 programmatic value, Validation Architecture)
    - Views/Admin/CreateAssessment.cshtml (lines 1872-1889 — verify visual: statusFieldWrapper toggle + Status value assignment Task 1)
    - Controllers/AssessmentAdminController.cs (lines 779-786 — verify ModelState.Remove inserted Task 2)
  </read_first>
  <action>
EXECUTE manual UAT script di `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` (created Wave 0 Task 3) — 4 step Bahasa Indonesia cover test matrix D-10. Run di browser modern (Chrome/Edge/Firefox versi terbaru) dengan DevTools terbuka (Console tab — capture JS errors). App harus running di development atau staging environment. Login sebagai HC. Sign-off section di akhir UAT.md harus diisi dengan tester name, timestamp, browser version, OS, dan result PASS/FAIL.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` exists
    - `grep -E "Tester name: \*\*[A-Z]" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns ≥ 1 line (tester name field filled with capitalized value, NOT empty placeholder `_____`)
    - `grep -c "_____" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns 0 (no remaining blank placeholder fields in sign-off section)
    - `grep -E "Tested at: \*\*[0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns ≥ 1 line (timestamp ISO date filled)
    - `grep -E "Browser/version: \*\*[A-Za-z]" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns ≥ 1 line (browser version filled with text)
    - `grep -E "Result:.*PASS" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns ≥ 1 line (explicit PASS keyword setelah dipilih, bukan template `PASS / FAIL`)
    - All 4 step checkboxes filled `[x]` — `grep -c "^- \[x\] Step [1-4]:" .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` returns 4
  </acceptance_criteria>
  <done>
Sign-off section di 308-UAT.md filled dengan tester name + timestamp + browser version + OS + result PASS untuk 4 step (Standard saja, Switch S→PP→S, PP saja, Switch PP→S→PP) + sub-step 1a regression. DevTools Console clean (no JS errors observed). Sub-step 1a regression Standard tanpa Status confirmed tetap require pick (D-11, success criteria #5).
  </done>
  <what-built>
Implementasi REQ WIZ-04 PrePost Wizard Validation Fix di 2 file:

**Views/Admin/CreateAssessment.cshtml (3 line additions di handler block line 1872-1889):**
- `var statusEl = document.getElementById('Status');` di awal handler body (single lookup, DRY)
- `if (statusEl) statusEl.value = 'Upcoming';` di if-branch saat `value === 'PrePostTest'` (D-01 — auto-set sebelum hidden)
- `if (statusEl) statusEl.value = '';` di else-branch saat switch back Standard (D-02 — clear, force user re-pick)

**Controllers/AssessmentAdminController.cs (5-line insert antara line 779 dan 782):**
- Conditional `if (isPrePostMode) { ModelState.Remove("Status"); }` (D-04 — skip [Required] validation untuk PrePost path)

**Defense-in-depth verified:**
- Server line 1078/1112/1170/1644/1663 hardcode `Status = "Upcoming"` UNCHANGED untuk PrePost session creation
- D-06 defensive default line 975-978 UNCHANGED (Standard fallback)
- D-12 Status field markup line 481-487 UNCHANGED ([Required] attribute preserved)
- D-17 Phase 307 helpers line 1469-1614 UNCHANGED (no overlap)

**Automated verification (Task 3 PASSED):**
- 4 Phase 308 E2E tests PASS (RED → GREEN dari Wave 0)
- 4 Phase 307 E2E tests PASS (no regression)
- FLOW 1 test 1.2 PASS (Standard baseline preserved)
- .NET build 0 errors

**SCOPE NOTE (RESEARCH-corrected):**
- Form ID `#createAssessmentForm` (BUKAN `#createForm` di CONTEXT D-07/CD-01)
- jQuery validate re-parse (D-07/D-08/D-09) **DROPPED** — `_ValidationScriptsPartial` 0 matches, plugin tidak loaded; existing `validateStep(n)` visibility guard line 996-1004 sudah handle hidden Status correctly. ROADMAP success criteria #3 N/A.

Manual UAT 4-step Bahasa Indonesia required untuk verify server-side submit success path (D-04) dengan full wizard navigation — E2E tests cover client-side state assertion (D-01/D-02) saja.
  </what-built>
  <how-to-verify>
EXECUTE manual UAT script di `.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md` — 4 step Bahasa Indonesia cover test matrix D-10:

**Step 1 — Standard saja (success criteria #5 regression guard):**
1. Navigate `/Admin/CreateAssessment`, default Standard mode → Status field visible (`#statusFieldWrapper` TIDAK d-none)
2. Pilih 1-2 peserta, fill Title + Category + Schedule + Duration + EWCD + **Status="Open"**
3. Submit → expect success (redirect ke ManageAssessment atau TempData success), TIDAK ada "Status field is required"
4. **Sub-step 1a regression:** refresh, ulangi tanpa pilih Status → expect error "Status wajib dipilih" muncul (D-11 enforced)

**Step 2 — Switch S→PP→S (D-02 mode-switch cleanup):**
1. Standard → pilih Status="Open" → switch ke PrePost → DevTools Console: `document.getElementById('Status').value` returns `'Upcoming'` (D-01)
2. Switch back Standard → wrapper visible, Console: `'Status' value` returns `''` (D-02 clear)
3. Re-pick Status="Open", fill semua field, submit → expect success

**Step 3 — PrePost saja (success criteria #1, #4 main acceptance — bug REQ WIZ-04 fixed):**
1. Switch ke PrePost → wrapper hidden, Console: Status value `'Upcoming'`
2. Pilih peserta, fill Title + Category + PreSchedule + PreDurationMinutes + PreExamWindowCloseDate + PostSchedule + PostDurationMinutes + PostExamWindowCloseDate + Pre/Post packages
3. Submit → **expect success: NO "Status field is required" error, NO reset ke Step 1, AssessmentSession created dengan Status="Upcoming"**
4. Verify AssessmentSession baru muncul di `/Admin/ManageAssessment` dengan status "Upcoming"

**Step 4 — Switch PP→S→PP (D-01 idempotency):**
1. PrePost → Standard → PrePost (3 cycles)
2. Setiap PP cycle: Console verify `Status` value `'Upcoming'` (D-01 idempotent re-fire)
3. Wrapper hidden konsisten setiap PP mode
4. Submit di akhir PP cycle → expect success

**Required evidence:**
- Sign-off section di 308-UAT.md filled dengan tester name + timestamp + browser version + OS + result
- DevTools Console screenshot kalau ada JS errors (expected: tidak ada)
- Screenshot atau text capture sub-step 1a regression error (Step 1a — Standard tanpa Status tetap require pick)
- AssessmentSession PrePost record visible di /Admin/ManageAssessment list (Step 3 success acceptance)

**Key acceptance untuk PASS verdict:**
- Step 3 main behavior verified: NO error "Status field is required", NO wizard reset ke Step 1 setelah submit PrePost — ini adalah **fix bug REQ WIZ-04**.
- Step 1 sub-step 1a regression confirmed: Standard tanpa Status tetap show "Status wajib dipilih" — D-11 enforced.
- Step 2 dan Step 4 mode-switching state cleanup berfungsi: Status auto-'Upcoming' saat PP, clear '' saat back Standard, idempotent.
- Tidak ada JS error baru di DevTools Console selama 4 step (Phase 307 helper warnings boleh, errors tidak).

Sign-off di section bawah `308-UAT.md` dengan tester name + timestamp + browser version + OS + result PASS/FAIL. Result harus PASS untuk close Wave 1.
  </how-to-verify>
  <resume-signal>Type "approved" jika 4 step + sub-step 1a regression PASS dan evidence captured (sign-off filled di 308-UAT.md). Atau describe issues per step number jika FAIL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → Server (controller POST) | User-submitted form data (`AssessmentTypeInput`, `Status`, `Schedule`, dll) crosses dari untrusted client ke trusted server. ASP.NET Core `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` (existing, UNCHANGED) sudah mitigate auth/CSRF. |
| JS programmatic value assignment → DOM `#Status` | Phase 308 D-01 set `statusEl.value = 'Upcoming'` programmatically — TIDAK fire `change` event (Pitfall 6). Risk: kalau future code attach `change` listener ke `#Status` akan miss this update. Mitigation: code comment di Task 1 action note ini. |
| ModelState collection → validation pipeline | `ModelState.Remove("Status")` skip validator untuk field `Status` saat PrePost — risk: kalau accidentally remove field LAIN, tampering possible. Mitigation: exact-string `"Status"` match (no glob, no list iteration), acceptance criterion enforces single match via grep. |
| User-submitted `model.Status` → AssessmentSession persistence | User dapat tamper submitted Status value via DevTools (set ke arbitrary string). Mitigation: server line 1078/1112/1170/1644/1663 hardcode `Status = "Upcoming"` di PrePost session creation paths — submitted value diabaikan untuk PrePost (defense-in-depth verified, D-04 hanya skip validation, tidak grant authority). |

## STRIDE Threat Register

Phase 308 attack surface terbatas pada server-side ModelState Remove (scoped ke field "Status") + client-side JS value assignment (read-only side-effect). ASVS Level 1 baseline. Mostly accept disposition — no new attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-308-01 | Tampering | Server-side `ModelState.Remove("Status")` (D-04, line 780-783) | mitigate | Exact-string `"Status"` match (BUKAN glob/list/dynamic) — hanya field bernama "Status" di-remove, semua field LAIN ([Required] Title, UserIds, Schedule, PassPercentage, dll) tetap validated. Acceptance criterion `grep -c "ModelState.Remove(\"Status\")"` = 1 (single insert, no duplicate). Conditional fire HANYA saat `isPrePostMode == true` — Standard mode ModelState integrity preserved (D-11 regression guard). Verify via Phase 308 test 8.1 + Step 1a UAT. |
| T-308-02 | Tampering | Client-side `statusEl.value = 'Upcoming'` (D-01) — user dapat tamper via DevTools | accept | Defense-in-depth: server line 1078/1112/1170/1644/1663 hardcode `Status = "Upcoming"` untuk PrePost session creation — submitted `model.Status` diabaikan di code path ini. JS hint via D-01 adalah UX optimization (auto-set value sebelum submit, agar tidak trigger `[Required]`), BUKAN authority. RESEARCH verified 5-instance hardcode pattern. Risk profile: minimal — tamper attempt menghasilkan AssessmentSession dengan `Status="Upcoming"` regardless of user-submitted value. |
| T-308-03 | Information Disclosure | Logging / TempData error messages (`return View(model)` after ModelState invalid) | accept | Phase 308 fix mencegah error path "Status field is required" untuk PrePost — TIDAK introduce new error message. Existing error untuk Standard mode (D-11 "Status wajib dipilih") UNCHANGED. No PII di error messages, no stack traces leaked di production (existing ASP.NET Core configuration). |
| T-308-04 | Denial of Service | JS handler programmatic `statusEl.value = ...` di typeSelect change event | accept | Single property assignment, O(1) operation. No loop, no recursion, no fetch. Handler fire HANYA saat user UI interaction (`change` event semantics, Pitfall 7). No accumulating state (single timer slot via existing handler structure). Risk profile: zero — DOS via type-switching scenario tidak feasible. |
| T-308-05 | Repudiation | AuditLog (existing pattern in controller) — Phase 308 tidak menambah log entry | accept | Phase 308 hanya skip Status validation, TIDAK touch AuditLog code. AuditLog entries untuk AssessmentSession creation (existing) capture user identity + timestamp + action — preserved UNCHANGED. No new audit gap introduced. |
| T-308-06 | Cross-IIFE scope collision | Phase 307 helpers (line 1469-1614) vs Phase 308 edits (line 1872-1889) | mitigate | D-17 enforced: Phase 308 edits di handler block line 1872-1889 tidak overlap dengan Phase 307 helpers area line 1469-1614. Acceptance criterion via Task 3 Step 3 (Phase 307 regression check via Playwright `--grep "Phase 307"` 4 tests PASS). Different IIFE scope — Phase 307 helpers top-level, Phase 308 edits di dalam DOMContentLoaded handler block. Zero variable name collision (statusEl baru, tidak conflict dengan top-level helpers). |

**Catatan:** Tidak ada threat S (Spoofing) atau E (Elevation of Privilege) yang material — Phase 308 tidak touch auth path, tidak grant new privilege, role guard `[Authorize(Roles="Admin, HC")]` UNCHANGED.

**ASVS L1 compliance:** V5 Input Validation tetap enforced untuk semua field LAIN. Phase 308 net-zero attack surface increase.
</threat_model>

<verification>
**Phase-level verification (Wave 1 complete):**

1. JS edit audit (Task 1):
   - `grep -n "var statusEl = document.getElementById('Status');" Views/Admin/CreateAssessment.cshtml` returns 1 line in range 1872-1879
   - `grep -c "statusEl.value = 'Upcoming'" Views/Admin/CreateAssessment.cshtml` returns 1 (D-01)
   - `grep -c "statusEl.value = ''" Views/Admin/CreateAssessment.cshtml` returns 1 (D-02)
   - `grep -c "Phase 308 D-01\|Phase 308 D-02" Views/Admin/CreateAssessment.cshtml` returns minimum 2 (traceability)

2. Server edit audit (Task 2):
   - `grep -A 3 "bool isPrePostMode" Controllers/AssessmentAdminController.cs | grep -c "ModelState.Remove(\"Status\")"` returns 1 (D-04 single insert)
   - `grep -n "// Phase 308 D-04" Controllers/AssessmentAdminController.cs` returns 1 line
   - `grep -B 1 "ModelState.Remove(\"Status\")" Controllers/AssessmentAdminController.cs | grep -c "if (isPrePostMode)"` returns 1 (conditional wrapping)

3. Defense-in-depth preservation:
   - `grep -c "Status = \"Upcoming\"" Controllers/AssessmentAdminController.cs` returns minimum 5 (line 1078/1112/1170/1644/1663 UNCHANGED)
   - `grep -n "if (string.IsNullOrEmpty(model.Status)) model.Status = \"Open\";" Controllers/AssessmentAdminController.cs` returns 1 line in range 970-980 (D-06 UNCHANGED)
   - `grep -n "asp-for=\"Status\"" Views/Admin/CreateAssessment.cshtml` returns 1 line in range 478-487 (D-12 markup UNCHANGED)

4. Phase 307 / FLOW 1 regression check (Task 3):
   - `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 307" --reporter=list` exit 0 (4 Phase 307 tests PASS — no regression dari D-17 boundary respect)
   - `cd tests && npx playwright test e2e/assessment.spec.ts --grep "1\\.2" --reporter=list` exit 0 (FLOW 1 baseline preserved)

5. Phase 308 RED → GREEN transition (Task 3):
   - `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 308" --reporter=list` exit 0 (4 tests 8.1-8.4 PASS)

6. Build sanity (Task 3):
   - `dotnet build --no-restore` exit 0, 0 errors, ≤ 92 pre-existing warnings

7. RESEARCH-corrected items enforced:
   - `grep -c "_ValidationScriptsPartial\|jquery.validate" Views/Admin/CreateAssessment.cshtml` returns 0 (D-07/D-08/D-09 SKIPPED — Pitfall 2 Pilihan A)
   - `grep -c "\\$\\.validator\\.unobtrusive\\|removeData('validator')" Views/Admin/CreateAssessment.cshtml` returns 0 (re-parse code TIDAK ditambahkan)
   - `grep -c "'#createForm'" Views/Admin/CreateAssessment.cshtml` returns 0 (form ID correction — TIDAK ada `#createForm` reference)

8. Manual UAT 4-step PASSED (Task 4 checkpoint):
   - Sign-off section di 308-UAT.md filled dengan PASS result
   - Sub-step 1a regression confirmed (Standard tanpa Status tetap require pick — D-11)
   - Step 3 main acceptance: PrePost submit success TANPA error/reset (REQ WIZ-04 fix verified)
</verification>

<success_criteria>
Wave 1 complete saat:
- [ ] Task 1: JS edit di handler block line 1872-1889 dengan 3 line additions (statusEl lookup + D-01 + D-02), comment "Phase 308 D-01" dan "Phase 308 D-02" untuk traceability, D-07/D-08/D-09 jQuery re-parse SKIPPED
- [ ] Task 2: Server-side conditional `ModelState.Remove("Status")` 5-line insert antara line 779 dan 782, comment "Phase 308 D-04" dengan rationale, defensive default line 975-978 UNCHANGED, PrePost session creation Status="Upcoming" line 1078/1112/1170/1644/1663 UNCHANGED
- [ ] Task 3: Automated verification PASS — 4 Phase 308 tests + 4 Phase 307 tests + FLOW 1 test 1.2 + .NET build 0 errors
- [ ] All `<acceptance_criteria>` per task lulus (grep verifications + Playwright tests + dotnet build)
- [ ] Phase 308 E2E tests (8.1, 8.2, 8.3, 8.4) PASS — RED → GREEN transition dari Wave 0 confirmed
- [ ] Phase 307 E2E tests (7.1-7.4) PASS — no regression (D-17 boundary respected)
- [ ] FLOW 1 test 1.2 PASS — Standard mode submit baseline preserved
- [ ] Task 4 (checkpoint): Manual UAT 4-step Bahasa Indonesia PASS dengan sign-off filled di 308-UAT.md (test matrix 4 kombinasi + sub-step 1a regression)
- [ ] All 5 ROADMAP success criteria Phase 308 verified atau accounted-for:
  - [ ] #1: JS handler set `Status='Upcoming'` saat `value === 'PrePostTest'` — verified Task 1 + UAT Step 3/4
  - [ ] #2: Server `if (isPrePostMode) ModelState.Remove("Status")` — verified Task 2 + UAT Step 3 (submit success)
  - [ ] #3: jQuery validate re-parse — **N/A per RESEARCH Pitfall 2** (plugin tidak loaded; existing `validateStep` line 996-1004 visibility guard supersede). DOCUMENTED di plan output sebagai dropped scope dengan rationale.
  - [ ] #4: Test matrix 4 kombinasi pass (Standard saja, S→PP→S, PP saja, PP→S→PP) — verified via Phase 308 E2E tests 8.1-8.4 + manual UAT 4-step
  - [ ] #5: Regresi Standard tanpa Status tetap "Status wajib dipilih" — verified UAT Step 1 sub-step 1a + Phase 307 test 1.2 baseline preserved
</success_criteria>

<output>
After completion, create `.planning/phases/308-prepost-wizard-validation-fix/308-02-SUMMARY.md` containing:
- Files modified (2 files: Views/Admin/CreateAssessment.cshtml + Controllers/AssessmentAdminController.cs)
- Lines added/removed (~3 client + ~5 server = ~8 lines added, 0 lines removed)
- Sub-edit count: Task 1 (1 client edit dengan 3 line additions), Task 2 (1 server insert 5 lines), Task 3 (0 file edit — verification only), Task 4 (checkpoint UAT)
- Test results: Phase 308 (4 tests 8.1-8.4 RED→GREEN), Phase 307 regression (4 tests 7.1-7.4 PASS), FLOW 1 (test 1.2 PASS)
- .NET build status: 0 errors, ≤ 92 warnings (Phase 307 baseline preserved)
- UAT result: PASS/FAIL per step + sub-step 1a regression result + browser version + OS
- Verification commands run + exit codes
- RESEARCH-corrected items applied: form ID `#createAssessmentForm` (BUKAN `#createForm`), jQuery validate re-parse SKIPPED (D-07/D-08/D-09 N/A — Pitfall 2 Pilihan A), ROADMAP success criteria #3 marked N/A
- Threat mitigation evidence: ModelState.Remove single-string match (T-308-01), defense-in-depth Status="Upcoming" hardcode count (T-308-02), Phase 307 boundary respected (T-308-06)
- Phase 308 success criteria checklist (5 items — #3 marked N/A dengan rationale, lainnya checked)
</output>
