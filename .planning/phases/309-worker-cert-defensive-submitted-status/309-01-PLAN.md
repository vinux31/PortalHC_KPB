---
phase: 309
slug: worker-cert-defensive-submitted-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/Certificate.cshtml
autonomous: false
requirements:
  - WCRT-01
threat_model_summary:
  asvs_level: 1
  primary_category: "V7 Error Handling & Logging"
  threat_count: 3
user_setup: []

must_haves:
  truths:
    - "Worker yang sudah lulus assessment dengan GenerateCertificate=true membuka /CMP/Certificate/{id} TIDAK dapat HTTP 500"
    - "Saat exception terjadi (DbException/FormatException/NRE/Exception generic) di Certificate action, worker di-redirect ke Results dengan TempData[\"Error\"] generic Bahasa Indonesia (BUKAN stack trace)"
    - "Saat User navigation property null di ViewModel, sertifikat tetap render dengan placeholder \"(Nama tidak tersedia)\" alih-alih blank"
    - "Saat Category null/empty/exotic, ResolveCategorySignatory tetap return PSignViewModel fallback (Position=\"HC Manager\", FullName=\"\") tanpa exception bubble"
    - "Production log mencatat structured exception via _logger.LogError(ex, \"Certificate view failed for session {Id}\", id) untuk forensic root cause analysis"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Certificate action try-catch wrap (DbException → FormatException → NRE → Exception) + ResolveCategorySignatory try-catch + System.Data.Common using directive"
      contains: "catch (System.Data.Common.DbException"
    - path: "Controllers/CMPController.cs"
      provides: "ResolveCategorySignatory wrapped try-catch returning fallback PSignViewModel"
      contains: "ResolveCategorySignatory failed for category"
    - path: "Views/CMP/Certificate.cshtml"
      provides: "Null-safe accessor pada line 227 untuk Model.User?.FullName"
      contains: "(Nama tidak tersedia)"
  key_links:
    - from: "Controllers/CMPController.cs Certificate(int id)"
      to: "_logger.LogError(ex, ...)"
      via: "4 catch blocks (DbException, FormatException, NullReferenceException, Exception) — semua memanggil _logger.LogError dengan format identik"
      pattern: "_logger\\.LogError\\(ex, \"Certificate view failed for session \\{Id\\}\", id\\)"
    - from: "Controllers/CMPController.cs Certificate(int id) catch handlers"
      to: "RedirectToAction(\"Results\", new { id })"
      via: "Mirror CertificatePdf pattern line 2082 — semua catch redirect ke Results dengan TempData Error"
      pattern: "RedirectToAction\\(\"Results\", new \\{ id \\}\\)"
    - from: "Controllers/CMPController.cs ResolveCategorySignatory"
      to: "new PSignViewModel { Position = \"HC Manager\", FullName = \"\" }"
      via: "single catch (Exception ex) → _logger.LogWarning + return fallback"
      pattern: "_logger\\.LogWarning\\(ex, \"ResolveCategorySignatory failed"
    - from: "Views/CMP/Certificate.cshtml line 227"
      to: "Model.User?.FullName ?? \"(Nama tidak tersedia)\""
      via: "Razor null-coalescing operator"
      pattern: "@\\(Model\\.User\\?\\.FullName \\?\\? \"\\(Nama tidak tersedia\\)\"\\)"
---

<objective>
Plan 309-01 mengeraskan halaman Worker Certificate (`/CMP/Certificate/{id}`) terhadap data exotic (User null, Category null, Signatory chain rusak) dengan defensive try-catch berlapis, structured logging, signatory fallback, dan null-safe view accessor — mirror pattern `CertificatePdf` (line 2078-2083) tapi dengan **specific exception order** (DbException → FormatException → NullReferenceException → Exception generic) per CONTEXT D-01.

**Purpose:** Eliminasi HTTP 500 di production saat worker membuka halaman sertifikat. Issue audit Temuan 10 (27 April 2026) — root cause exotic data belum teridentifikasi; structured `_logger.LogError(ex, "Certificate view failed for session {Id}", id)` akan menyediakan forensic data untuk milestone berikutnya.

**Output:** 3 file edit — `Controllers/CMPController.cs` (Certificate action wrap + ResolveCategorySignatory wrap + `using System.Data.Common;`), `Views/CMP/Certificate.cshtml` (line 227 null-safe accessor). Tidak ada signature/contract yang berubah — non-breaking.

**Decision yang ditutup planner (researcher Open Question):**
- Mempertahankan single user-facing copy `"Gagal memuat sertifikat. Silakan coba lagi."` di SEMUA 4 catch handler (CONTEXT D-01 deferred per-exception copy explicitly)
- `using System.Data.Common;` directive WAJIB ditambah di top file `CMPController.cs` (Assumption A1 di RESEARCH — researcher verified `using System.Data.Common;` belum ada di line 1-19)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md
@.planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md

<interfaces>
<!-- Key contracts diekstrak dari codebase. Executor pakai langsung — tidak perlu explore codebase. -->

From `Controllers/CMPController.cs` line 33 (DI container):
```csharp
private readonly ILogger<CMPController> _logger; // Microsoft.Extensions.Logging
```

From `Controllers/CMPController.cs` line 2078-2083 (CertificatePdf catch — REFERENCE PATTERN to mirror):
```csharp
catch (Exception ex)
{
    _logger.LogError(ex, "CertificatePdf generation failed for session {Id}", id);
    TempData["Error"] = "Gagal membuat PDF sertifikat. Silakan coba lagi.";
    return RedirectToAction("Results", new { id });
}
```

From `Models/PSignViewModel.cs` (fallback construct properties):
```csharp
public class PSignViewModel
{
    public string LogoUrl { get; set; } = "/images/psign-pertamina.png"; // default
    public string? Position { get; set; }
    public string? Unit { get; set; }
    public string FullName { get; set; } = ""; // default
}
```

From `Controllers/CMPController.cs` line 1813-1838 (ResolveCategorySignatory existing — wrap target):
```csharp
private async Task<PSignViewModel> ResolveCategorySignatory(string? categoryName)
{
    var fallback = new PSignViewModel { Position = "HC Manager", FullName = "" };
    if (string.IsNullOrWhiteSpace(categoryName)) return fallback;

    var category = await _context.AssessmentCategories
        .Include(c => c.Signatory)
        .Include(c => c.Parent).ThenInclude(p => p!.Signatory)
        .FirstOrDefaultAsync(c => c.Name == categoryName);

    if (category?.Signatory != null)
        return new PSignViewModel { /* ... */ };
    if (category?.Parent?.Signatory != null)
        return new PSignViewModel { /* ... */ };

    return fallback;
}
```

From `Controllers/CMPController.cs` line 1771-1811 (Certificate action existing — wrap target):
```csharp
[HttpGet]
public async Task<IActionResult> Certificate(int id)
{
    var assessment = await _context.AssessmentSessions
        .Include(a => a.User)
        .FirstOrDefaultAsync(a => a.Id == id);
    if (assessment == null) return NotFound();
    // ... 7 more checks (Auth, Status, GenerateCertificate, IsPassed) ...
    ViewBag.PSign = await ResolveCategorySignatory(assessment.Category);
    return View(assessment);
}
```

From `Views/CMP/Certificate.cshtml` line 227 (null-safe target):
```cshtml
<div class="recipient-name">@Model.User?.FullName</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wrap Certificate action try-catch + tambah using System.Data.Common</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs line 1-20 (verify `using System.Data.Common;` belum ada — Assumption A1 RESEARCH)
    - Controllers/CMPController.cs line 1771-1811 (Certificate action body — current state SEBELUM wrap)
    - Controllers/CMPController.cs line 2078-2083 (CertificatePdf reference pattern — single generic catch dengan _logger.LogError + TempData["Error"] + RedirectToAction("Results", new { id }))
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pattern 1: Try-Catch Per-Action" (lines 258-345 — full target code example untuk Certificate action wrap)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md §D-01 (specific exception order: DbException → FormatException → NRE → Exception)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pitfall 1: Try-catch terlalu broad menutupi NotFound 404" (lines 555-565 — `return NotFound()` dalam try-block adalah RETURN statement non-throwing, AMAN; verify executor TIDAK ubah `NotFound()` ke `throw new NotFoundException()`)
  </read_first>
  <action>
**Step 1 — Tambah using directive di top file `Controllers/CMPController.cs`:**

INSERT 1 baris baru SETELAH baris `using Microsoft.EntityFrameworkCore;` (line 6):

```csharp
using System.Data.Common;
```

Tujuan: import `DbException` base class (Assumption A1 RESEARCH — verified `using System.Data.Common;` belum ada di line 1-19).

**Step 2 — Wrap method body `Certificate(int id)` (line 1771-1811) dengan try-catch berlapis:**

REPLACE entire method body (line 1772-1811, bukan signature) dengan struktur berikut. Existing logic body (line 1774-1810) di-PINDAHKAN VERBATIM ke dalam `try { ... }` block — **TIDAK ada perubahan logic, hanya wrap**:

```csharp
[HttpGet]
public async Task<IActionResult> Certificate(int id)
{
    try
    {
        var assessment = await _context.AssessmentSessions
            .Include(a => a.User)
            .FirstOrDefaultAsync(a => a.Id == id);

        if (assessment == null) return NotFound();

        // Security: Owner, Admin, HC
        var user = await _userManager.GetUserAsync(User);
        if (user == null) return Challenge(); // Force login if session expired

        var userRoles = await _userManager.GetRolesAsync(user);
        bool isAuthorized = assessment.UserId == user.Id ||
                          userRoles.Contains("Admin") ||
                          userRoles.Contains("HC");

        if (!isAuthorized) return Forbid();

        // Only generate if Completed
        if (assessment.Status != "Completed")
        {
            TempData["Error"] = "Assessment not completed yet.";
            return RedirectToAction("Assessment");
        }

        // Guard: certificate generation disabled for this assessment
        if (!assessment.GenerateCertificate)
            return NotFound();

        // Guard: certificate only available for passed assessments
        if (assessment.IsPassed != true)
        {
            TempData["Error"] = "Certificate is only available for passed assessments.";
            return RedirectToAction("Results", new { id });
        }

        ViewBag.PSign = await ResolveCategorySignatory(assessment.Category);
        return View(assessment);
    }
    catch (DbException ex)
    {
        _logger.LogError(ex, "Certificate view failed for session {Id}", id);
        TempData["Error"] = "Gagal memuat sertifikat. Silakan coba lagi.";
        return RedirectToAction("Results", new { id });
    }
    catch (FormatException ex)
    {
        _logger.LogError(ex, "Certificate view failed for session {Id}", id);
        TempData["Error"] = "Gagal memuat sertifikat. Silakan coba lagi.";
        return RedirectToAction("Results", new { id });
    }
    catch (NullReferenceException ex)
    {
        _logger.LogError(ex, "Certificate view failed for session {Id}", id);
        TempData["Error"] = "Gagal memuat sertifikat. Silakan coba lagi.";
        return RedirectToAction("Results", new { id });
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "Certificate view failed for session {Id}", id);
        TempData["Error"] = "Gagal memuat sertifikat. Silakan coba lagi.";
        return RedirectToAction("Results", new { id });
    }
}
```

**Specific instruksi exec:**

1. **Status check di line 1792** (`if (assessment.Status != "Completed")`) — JANGAN UBAH di task ini. Plan 309-02 yang akan swap ke `!AssessmentConstants.IsAssessmentSubmitted(assessment.Status)`. Task ini fokus DEFENSIVE WRAP saja.
2. **`return NotFound()` / `return Challenge()` / `return Forbid()` / `return RedirectToAction(...)`** dalam try-block adalah RETURN statement non-throwing — **AMAN tetap di dalam try** (Pitfall 1 RESEARCH verified). JANGAN refactor jadi `throw`.
3. **Catch order WAJIB**: `DbException` → `FormatException` → `NullReferenceException` → `Exception` (CONTEXT D-01 lock). Jika order lain dipakai, compiler akan error karena `Exception` lebih specific dari `DbException` (mustahil) — TAPI JIKA `Exception` ditaruh sebelum specific catch, akan compile-error "A previous catch clause already catches all exceptions".
4. **User-facing copy IDENTIK 4x:** `"Gagal memuat sertifikat. Silakan coba lagi."` (Bahasa Indonesia, CLAUDE.md lock + CONTEXT D-01). JANGAN tambah differential per-exception copy (deferred per CONTEXT).
5. **Log message format IDENTIK 4x:** `"Certificate view failed for session {Id}"` dengan parameter `id` (structured logging — Microsoft.Extensions.Logging escape automatic).
6. **TIDAK pakai `using` BeginScope** — research RESEARCH §discretion: "Logger category/scope — planner pilih sesuai existing convention"; existing CMPController TIDAK pakai BeginScope, jadi konsisten omit.

**Reasoning belakang per-line copy:** CONTEXT D-01 explicit "Mirror CertificatePdf pattern" — 4 catch handler dengan log+message+redirect identik adalah verbatim copy structurally, hanya tambah specific catch types di depan. Hindari ekstrak helper "private IActionResult HandleCertException(...)" — over-engineering untuk 4 line block.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 (compile success); warning count ≤ 92 (Phase 308 baseline maintained)
    - `Controllers/CMPController.cs` line 1-20 contains `using System.Data.Common;` directive
    - `Controllers/CMPController.cs` body contains exactly 4 catch blocks dengan order specific-first: `catch (DbException ex)` → `catch (FormatException ex)` → `catch (NullReferenceException ex)` → `catch (Exception ex)` (verifiable via grep `catch (DbException|FormatException|NullReferenceException|Exception) ex` di method `Certificate(int id)`)
    - `grep -c '_logger.LogError(ex, "Certificate view failed for session {Id}", id);' Controllers/CMPController.cs` returns ≥ 4 (4 identical log calls dari 4 catch handler)
    - `grep -c 'TempData\["Error"\] = "Gagal memuat sertifikat. Silakan coba lagi.";' Controllers/CMPController.cs` returns ≥ 4
    - `grep -c 'return RedirectToAction("Results", new { id });' Controllers/CMPController.cs` returns ≥ 5 (4 catch handler + 1 existing CertificatePdf catch line 2082)
    - Existing `return NotFound()`, `return Challenge()`, `return Forbid()`, `return RedirectToAction("Assessment")` di method body Certificate TETAP ADA di dalam try-block (verifiable via line-context inspection — bahwa NotFound TIDAK di-replace ke throw)
  </acceptance_criteria>
  <done>Method `Certificate(int id)` di `Controllers/CMPController.cs` wrapped dengan try-catch berlapis (DbException → FormatException → NRE → Exception); semua 4 handler log via `_logger.LogError(ex, "Certificate view failed for session {Id}", id)` dan redirect ke Results dengan TempData Error generic Bahasa Indonesia; `using System.Data.Common;` ditambah di top file; dotnet build pass 0 error; warning count ≤ 92.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wrap ResolveCategorySignatory try-catch + Certificate.cshtml null-safe accessor</name>
  <files>Controllers/CMPController.cs, Views/CMP/Certificate.cshtml</files>
  <read_first>
    - Controllers/CMPController.cs line 1813-1838 (ResolveCategorySignatory current state — fallback object di line 1815, return paths line 1822-1837)
    - Views/CMP/Certificate.cshtml line 227 (current `@Model.User?.FullName` — sudah `?.` operator tapi TIDAK ada `??` fallback string)
    - Views/CMP/Certificate.cshtml line 268-269 (existing `pSign?.Position ?? "HC Manager"` — pattern referensi untuk null-coalesce)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pattern 2: Helper-Wrap dengan Fallback" (lines 351-396 — full target code untuk ResolveCategorySignatory wrap)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pattern 6: Razor Null-Safe Accessor" (lines 495-530 — audit table line 5/18/227/229/231/238/249/253/256/268-269; HANYA line 227 butuh edit)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md §D-02 + D-03
  </read_first>
  <action>
**Step 1 — Wrap method body `ResolveCategorySignatory(string? categoryName)` (line 1813-1838 di `Controllers/CMPController.cs`) dengan try-catch:**

REPLACE entire method (line 1813-1838) dengan struktur berikut. Existing body logic (line 1815-1837) DIPINDAH VERBATIM ke dalam `try { ... }` block — **TIDAK ada perubahan logic, hanya wrap**:

```csharp
private async Task<PSignViewModel> ResolveCategorySignatory(string? categoryName)
{
    var fallback = new PSignViewModel { Position = "HC Manager", FullName = "" };
    if (string.IsNullOrWhiteSpace(categoryName)) return fallback;

    try
    {
        var category = await _context.AssessmentCategories
            .Include(c => c.Signatory)
            .Include(c => c.Parent).ThenInclude(p => p!.Signatory)
            .FirstOrDefaultAsync(c => c.Name == categoryName);

        if (category?.Signatory != null)
            return new PSignViewModel
            {
                FullName = category.Signatory.FullName ?? "",
                Position = category.Signatory.Position ?? "HC Manager"
            };

        if (category?.Parent?.Signatory != null)
            return new PSignViewModel
            {
                FullName = category.Parent.Signatory.FullName ?? "",
                Position = category.Parent.Signatory.Position ?? "HC Manager"
            };

        return fallback;
    }
    catch (Exception ex)
    {
        _logger.LogWarning(ex, "ResolveCategorySignatory failed for category {Category}", categoryName);
        return fallback;
    }
}
```

**Specific instruksi exec:**

1. **`var fallback = ...` dan early-return null-check (`if (string.IsNullOrWhiteSpace(categoryName)) return fallback;`) TETAP DI LUAR try-block** — kedua statement ini guaranteed-non-throw (object init + null check), TIDAK perlu wrap. Wrap hanya bagian DB query + return paths.
2. **Single broad `catch (Exception ex)`** — CONTEXT D-02 explicit: tidak perlu specific exception order untuk helper karena fallback acceptable di semua kasus exception. Berbeda dari Certificate action (D-01 specific order).
3. **Log level WARNING bukan ERROR** — CONTEXT D-02 explicit: "warning karena fallback acceptable". Pesan: `"ResolveCategorySignatory failed for category {Category}"` dengan parameter `categoryName` (structured).
4. **Return path catch HARUS `return fallback`** (variable yang sudah declared di line awal) — JANGAN bikin new instance lagi. Konsistensi dengan happy-path fallback.
5. **TIDAK ubah signature method** — return type `Task<PSignViewModel>` non-nullable, parameter `string?` nullable. Caller (line 1809 `ViewBag.PSign = await ResolveCategorySignatory(...)`) tidak butuh adaptation.

**Step 2 — Edit `Views/CMP/Certificate.cshtml` line 227 untuk null-safe fallback string:**

REPLACE line 227 dari:

```cshtml
<div class="recipient-name">@Model.User?.FullName</div>
```

Menjadi:

```cshtml
<div class="recipient-name">@(Model.User?.FullName ?? "(Nama tidak tersedia)")</div>
```

**Specific instruksi exec:**

1. **Razor explicit `@(...)` syntax WAJIB** — Razor parser tidak bisa parse `@Model.User?.FullName ?? "..."` tanpa parentheses (`??` operator butuh expression context).
2. **Fallback string Bahasa Indonesia** `"(Nama tidak tersedia)"` — CLAUDE.md lock + CONTEXT D-03 verbatim string.
3. **HANYA line 227** yang diubah. RESEARCH §"Pattern 6" audit (lines 514-528) confirms 9 lokasi accessor lain di Certificate.cshtml SAFE (sudah `??` chain, sudah typed default, sudah dalam null-checked branch, atau wrapped `if (HasValue)`). JANGAN edit accessor lain — scope creep.
4. **Tidak boleh ubah CSS class atau struktur HTML** — hanya isi expression Razor saja. `<div class="recipient-name">` tetap (CSS rule existing reference class ini untuk styling sertifikat).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0; warning count ≤ 92 (Phase 308 baseline)
    - `Controllers/CMPController.cs` method `ResolveCategorySignatory` body contains `try` block dan `catch (Exception ex)` block
    - `grep -n '_logger.LogWarning(ex, "ResolveCategorySignatory failed for category {Category}", categoryName);' Controllers/CMPController.cs` returns ≥ 1 hit (di catch block ResolveCategorySignatory)
    - `Controllers/CMPController.cs` line ~1815 `var fallback = new PSignViewModel { Position = "HC Manager", FullName = "" };` TETAP ADA, di LUAR try-block (early-return path)
    - `grep -F '@(Model.User?.FullName ?? "(Nama tidak tersedia)")' Views/CMP/Certificate.cshtml` returns ≥ 1 hit
    - `grep -F '@Model.User?.FullName' Views/CMP/Certificate.cshtml` (NO trailing `??`) returns 0 hit (LINE 227 fully replaced — TIDAK ada literal lama yang tertinggal)
    - `grep -cE '<div class="recipient-name">' Views/CMP/Certificate.cshtml` returns 1 (struktur HTML wrapper TIDAK berubah)
    - File `Views/CMP/Certificate.cshtml` line 268-269 (`pSign?.Position ?? "HC Manager"` / `pSign?.FullName ?? ""`) TIDAK berubah (out-of-scope; verify via grep `pSign\?\.Position \?\? "HC Manager"` returns ≥ 1)
  </acceptance_criteria>
  <done>Method `ResolveCategorySignatory` di `CMPController.cs` wrapped try-catch dengan `_logger.LogWarning` + return fallback pada exception; line 227 di `Certificate.cshtml` null-safe dengan fallback string `"(Nama tidak tersedia)"`; dotnet build pass 0 error; existing accessor lain di Certificate.cshtml dan helper line 268-269 TIDAK berubah.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT 3-step Bahasa Indonesia (defensive smoke test)</name>
  <what-built>
Plan 309-01 telah meng-implementasi 3 perubahan defensive di flow Worker Certificate:
- `Controllers/CMPController.cs`: Certificate action wrapped try-catch berlapis (DbException → FormatException → NRE → Exception generic) dengan structured logging `_logger.LogError(ex, "Certificate view failed for session {Id}", id)` di setiap catch
- `Controllers/CMPController.cs`: ResolveCategorySignatory wrapped single try-catch dengan `_logger.LogWarning` + return fallback PSignViewModel (Position="HC Manager")
- `Views/CMP/Certificate.cshtml` line 227: null-safe accessor dengan fallback string `(Nama tidak tersedia)`
- `Controllers/CMPController.cs` top: `using System.Data.Common;` directive ditambah

Compile sukses (dotnet build 0 error, warning ≤ 92 baseline).

**Catatan:** Plan 309-01 adalah pure DEFENSIVE wrap — tidak mengubah happy-path behavior. Worker yang sudah bisa view sertifikat sebelum Phase 309 TETAP bisa view sertifikat dengan tampilan visual identik. Hanya path-error dan path-edge yang berubah.
  </what-built>
  <how-to-verify>
**SETUP — Dev environment setup (1x saja):**

1. Pastikan aplikasi running di local: `dotnet run` di root folder. Default URL biasanya `https://localhost:5001` atau `http://localhost:5000`.
2. Login sebagai worker yang sudah punya minimal 1 assessment dengan `Status=Completed`, `GenerateCertificate=true`, `IsPassed=true` (gunakan akun worker UAT existing dari milestone v14.0/v15.0 phase sebelumnya — referensi STATE.md "Phase 308 manual UAT").

**STEP 1 — Smoke test happy path (regression check):**

1. Navigate ke `/CMP/Assessment` (atau halaman Riwayat Assessment worker) — pilih session yang sudah Completed dan Passed.
2. Klik tombol "Hasil" / "Lihat Hasil" → landing di `/CMP/Results/{id}` → klik tombol "Lihat Sertifikat".
3. **Expected:** Halaman sertifikat (`/CMP/Certificate/{id}`) RENDER NORMAL dengan:
   - Nama recipient muncul (bukan "(Nama tidak tersedia)") karena User normal
   - Tanggal issue, course title, NIP semua tampil seperti sebelum Phase 309
   - Footer signatory section (line 264-269) tampil "HC Manager" atau signatory name yang ter-resolved
4. **Acceptance:** Visual sertifikat IDENTIK dengan sebelum Phase 309 — tidak ada layout shift, tidak ada text "(Nama tidak tersedia)" muncul untuk worker normal.

**STEP 2 — Smoke test exotic User=null (defensive check):**

OPSI A — manual DB edit (ada akses SQL Server / SSMS):

1. SQL: `SELECT Id, UserId FROM AssessmentSessions WHERE Status='Completed' AND IsPassed=1 LIMIT 1` — note SessionId X dan UserId Y.
2. SQL: temporarily NULL the UserId: `UPDATE AssessmentSessions SET UserId = NULL WHERE Id = X` (BACKUP dulu — `SELECT ... INTO bak_AS WHERE Id = X`)
3. Refresh halaman `/CMP/Certificate/X`.
4. **Expected:** Sertifikat tetap render (TIDAK 500), dengan recipient line tampil `"(Nama tidak tersedia)"`.
5. **Restore:** `UPDATE AssessmentSessions SET UserId = '<Y>' WHERE Id = X`.

OPSI B — code-side mock (jika tidak punya DB akses):

1. Di `Controllers/CMPController.cs` line 1810 (di dalam try, sebelum `return View(assessment)`) — TEMPORARILY tambah `assessment.User = null;` (HANYA untuk smoke test, REVERT setelah verify).
2. Refresh page `/CMP/Certificate/{id}` → expect "(Nama tidak tersedia)" muncul.
3. **REVERT** edit code segera setelah verify.

**Acceptance Step 2:** Sertifikat TIDAK error 500. Recipient name menampilkan `"(Nama tidak tersedia)"` (alert visual operator: data integrity issue di-flag).

**STEP 3 — Smoke test exotic Category=null (defensive signatory fallback):**

OPSI A — DB edit:

1. SQL: `UPDATE AssessmentSessions SET Category = NULL WHERE Id = X` (atau set ke string yang TIDAK ada di AssessmentCategories: `'__exotic_category__'`).
2. Refresh `/CMP/Certificate/X`.
3. **Expected:** Sertifikat tetap render. Footer signatory section menampilkan:
   - Position: "HC Manager"
   - FullName: "" (empty, karena fallback `FullName = ""`)
4. **Restore:** `UPDATE AssessmentSessions SET Category = '<original>' WHERE Id = X`.

**Verify production log (jika app log ke file/console):**

5. Cek console app atau log file untuk entry baru — TIDAK boleh ada `_logger.LogError "Certificate view failed..."` di Step 1 (happy path), boleh ada `_logger.LogWarning "ResolveCategorySignatory failed..."` di Step 3 jika DB query throw (kemungkinan kecil — biasanya category not-found return null silently, bukan throw).

**Acceptance Step 3:** Sertifikat tetap render dengan signatory "HC Manager"; tidak ada HTTP 500.

**Resume signal:** Type "approved" jika semua 3 step PASS. Jika ada yang fail/red, deskripsikan: step mana, apa yang muncul, console log apa yang relevan, screenshot opsional.
  </how-to-verify>
  <resume-signal>Type "approved" atau describe issues per Step (Step 1/2/3 PASS atau FAIL + detail)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CMPController.Certificate (HTTP GET) | Worker user input via route param `id` (typed `int`); class-level `[Authorize]` enforces authentication, body checks owner+role authorization |
| CMPController → DbContext (EF Core query) | `Include(a => a.User)` + `FirstOrDefaultAsync` — DB read can throw `DbException` (provider-specific subclass); orphan FK to AspNetUsers possible (User=null payload) |
| CMPController → ResolveCategorySignatory → DbContext | Helper does additional EF query (`AssessmentCategories` + `Signatory` + `Parent.Signatory` chain); chain can return null at any link OR throw on DB error |
| CMPController → ILogger sink | Production log destination (file/EventLog/Application Insights) — boundary: log MUST stay in ops-only access (V7 ASVS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-309-01 | Information Disclosure | `CMPController.Certificate` catch handlers | mitigate | All 4 catch handlers return generic user-facing copy "Gagal memuat sertifikat. Silakan coba lagi." (Bahasa Indonesia, NO `ex.Message` / `ex.StackTrace` / DB error detail in TempData["Error"]). Internal `_logger.LogError(ex, "Certificate view failed for session {Id}", id)` writes full exception to log sink; production log sink restricted to ops-team only (verify deploy config — out-of-scope edit, monitoring concern). ASVS V7-3.1 satisfied. |
| T-309-02 | Tampering | `RedirectToAction("Results", new { id })` in catch handlers | accept | Redirect target is internal action `Results` with route param `{id}` from typed `int` route binding (no user-controlled URL fragment). Open-redirect surface zero. |
| T-309-03 | Repudiation | Forensic root-cause analysis pasca-audit Temuan 10 | mitigate | Structured `_logger.LogError(ex, "Certificate view failed for session {Id}", id)` with non-sensitive `id` parameter (session ID is integer, non-PII) + full exception object in log — provides forensic data for milestone-next root-cause investigation (CONTEXT D-11 post-deploy monitor). Microsoft.Extensions.Logging escapes `{Id}` parameter automatically (no log-injection surface). |
</threat_model>

<verification>
**Build & Compile:**
- `dotnet build` exits 0; warning count ≤ 92 (Phase 308 baseline maintained)

**Code Pattern Verification:**
- `using System.Data.Common;` directive added at top of `Controllers/CMPController.cs`
- `Certificate(int id)` body wrapped in try-catch with 4 specific catches in order: DbException → FormatException → NullReferenceException → Exception
- All 4 catch handlers contain identical: `_logger.LogError(ex, "Certificate view failed for session {Id}", id);` + `TempData["Error"] = "Gagal memuat sertifikat. Silakan coba lagi.";` + `return RedirectToAction("Results", new { id });`
- `ResolveCategorySignatory` body has try-catch with `_logger.LogWarning(ex, "ResolveCategorySignatory failed for category {Category}", categoryName);` + `return fallback;`
- `Views/CMP/Certificate.cshtml` line 227 contains `@(Model.User?.FullName ?? "(Nama tidak tersedia)")`

**Manual UAT (Task 3 checkpoint):**
- Step 1 happy path: sertifikat render normal (regression PASS)
- Step 2 User=null: sertifikat render dengan "(Nama tidak tersedia)" (defensive view PASS)
- Step 3 Category=null: sertifikat render dengan signatory "HC Manager" (defensive helper PASS)
</verification>

<success_criteria>
**Mapped to ROADMAP §Phase 309 SC #1-#7 (WCRT-01 portion):**

- [x] SC #1: `CMPController.Certificate` line 1771-1811 dibungkus try-catch (Task 1)
- [x] SC #2: Specific exception catches (DbException, FormatException, NRE) sebelum generic (Task 1)
- [x] SC #3: Structured logging `_logger.LogError(ex, "Certificate view failed for session {Id}", id)` (Task 1)
- [x] SC #4: View `Certificate.cshtml` null-safe accessor `Model.User?.FullName ?? "(Nama tidak tersedia)"` (Task 2)
- [x] SC #5: Helper `ResolveCategorySignatory` (1813-1838) wrapped try-catch + fallback signatory (Task 2)
- [x] SC #6: Worker dengan exotic Category bisa view sertifikat dengan fallback "HC Manager" (Task 3 manual UAT Step 3)
- [x] SC #7: Post-deploy monitor `_logger.LogError` di production untuk pin-point root cause (deferred ke ops monitoring; structured log ready)
</success_criteria>

<output>
After completion, create `.planning/phases/309-worker-cert-defensive-submitted-status/309-01-SUMMARY.md` capturing:
- Decision yang ditutup planner: single user-facing copy 4x catch (CONTEXT D-01 deferred per-exception copy)
- Compile result + warning count
- Manual UAT result (Step 1/2/3 PASS atau FAIL detail)
- Files changed: 2 file (`Controllers/CMPController.cs`, `Views/CMP/Certificate.cshtml`)
- Commits: 2 (Task 1 commit, Task 2 commit) — Task 3 manual UAT no-commit
- Phase 309 progress: 1/2 plan complete; Plan 309-02 SUB-01 ready to execute
</output>
