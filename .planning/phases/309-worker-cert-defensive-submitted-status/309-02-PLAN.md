---
phase: 309
slug: worker-cert-defensive-submitted-status
plan: 02
type: execute
wave: 2
depends_on:
  - 309-01
files_modified:
  - Models/AssessmentConstants.cs
  - Models/AssessmentResultsViewModel.cs
  - Controllers/CMPController.cs
  - Views/CMP/Results.cshtml
  - Views/Shared/_Layout.cshtml
autonomous: false
requirements:
  - SUB-01
threat_model_summary:
  asvs_level: 1
  primary_category: "V7 Error Handling & Logging"
  threat_count: 4
user_setup: []

must_haves:
  truths:
    - "Worker submit assessment ber-essay (status berubah ke \"Menunggu Penilaian\" via GradingService) dapat membuka /CMP/Results/{id} TANPA popup error merah \"Error: Assessment belum selesai.\""
    - "Worker klik \"Lihat Sertifikat\" pada session pending (status=Menunggu Penilaian) di-redirect ke Results dengan banner BIRU (alert-info) \"Sertifikat akan tersedia setelah penilaian essay selesai.\" — BUKAN banner merah error"
    - "Worker klik \"Download PDF\" pada session pending tidak crash — di-redirect ke Results dengan banner Info yang sama"
    - "Halaman Results untuk session pending menampilkan: banner alert-info \"Hasil sementara\", score interim (MC+MA only), badge status \"MENUNGGU PENILAIAN\" (warna secondary, bukan success/danger), tombol \"Lihat Sertifikat\" HIDDEN"
    - "Essay items TETAP tampil di question review section dengan label \"Menunggu Penilaian\" tanpa skor/correct-incorrect; MC/MA items tetap show correct/incorrect (per CONTEXT D-08 lock)"
    - "Setelah HC menyelesaikan penilaian essay (status berubah ke Completed), worker view Certificate normal — regression-free"
    - "Helper IsAssessmentSubmitted(string?) returns true untuk \"Completed\" DAN \"Menunggu Penilaian\"; returns false untuk \"Open\", \"Upcoming\", null, atau string lain"
  artifacts:
    - path: "Models/AssessmentConstants.cs"
      provides: "Constant PendingGrading + static helper IsAssessmentSubmitted(string?)"
      contains: "public const string PendingGrading = \"Menunggu Penilaian\""
    - path: "Models/AssessmentConstants.cs"
      provides: "Top-level static helper bool IsAssessmentSubmitted"
      contains: "public static bool IsAssessmentSubmitted"
    - path: "Models/AssessmentResultsViewModel.cs"
      provides: "Field IsPendingGrading bool (default false) untuk pending mode flag + field IsEssayPending bool di QuestionReviewItem (default false) untuk Essay pending label"
      contains: "public bool IsPendingGrading"
    - path: "Models/AssessmentResultsViewModel.cs"
      provides: "Field IsEssayPending di QuestionReviewItem ViewModel"
      contains: "public bool IsEssayPending"
    - path: "Controllers/CMPController.cs"
      provides: "3 lokasi swap (line 1792, 1858, 2105) ke !AssessmentConstants.IsAssessmentSubmitted + 2 PendingGrading branch (Certificate + CertificatePdf) + Results ViewModel projection IsPendingGrading + Essay items TETAP tampil dengan IsEssayPending=true saat status PendingGrading + TempData[\"Error\"] copy Bahasa Indonesia"
      contains: "AssessmentConstants.IsAssessmentSubmitted"
    - path: "Views/CMP/Results.cshtml"
      provides: "Pending mode rendering: banner alert-info, badge MENUNGGU PENILAIAN, hide cert button, hide pass/fail badge + Essay items render label 'Menunggu Penilaian' alih-alih correct/incorrect saat IsEssayPending=true"
      contains: "Hasil sementara"
    - path: "Views/Shared/_Layout.cshtml"
      provides: "TempData[\"Info\"] alert block dengan icon bi-info-circle-fill (antara Error block dan Success block)"
      contains: "TempData[\"Info\"]"
  key_links:
    - from: "Controllers/CMPController.cs Certificate(int id) line ~1792"
      to: "AssessmentConstants.IsAssessmentSubmitted(assessment.Status)"
      via: "Status check swap dari literal \"Completed\" ke helper call (negated)"
      pattern: "!AssessmentConstants\\.IsAssessmentSubmitted\\(assessment\\.Status\\)"
    - from: "Controllers/CMPController.cs Certificate / CertificatePdf"
      to: "TempData[\"Info\"] = \"Sertifikat akan tersedia setelah penilaian essay selesai.\" + RedirectToAction(\"Results\", new { id })"
      via: "PendingGrading branch — di-eksekusi SETELAH IsAssessmentSubmitted check + SEBELUM GenerateCertificate/IsPassed check (Pitfall #3 RESEARCH lock)"
      pattern: "Sertifikat akan tersedia setelah penilaian essay selesai"
    - from: "Controllers/CMPController.cs TempData[\"Error\"] di 3 lokasi status check"
      to: "\"Assessment belum selesai.\" (Bahasa Indonesia per CLAUDE.md)"
      via: "User-facing copy WAJIB Bahasa Indonesia — swap dari English literal \"Assessment not completed yet.\""
      pattern: "TempData\\[\"Error\"\\] = \"Assessment belum selesai\\.\""
    - from: "Controllers/CMPController.cs Results(int id)"
      to: "viewModel.IsPendingGrading = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading)"
      via: "Controller projection ke ViewModel field baru (typesafe ViewModel field, BUKAN ViewBag)"
      pattern: "IsPendingGrading\\s*=\\s*\\(assessment\\.Status\\s*==\\s*AssessmentConstants\\.AssessmentStatus\\.PendingGrading\\)"
    - from: "Controllers/CMPController.cs questionReviews loop"
      to: "QuestionReviewItem.IsEssayPending = (assessment.Status == PendingGrading && question.QuestionType == \"Essay\")"
      via: "Controller projection: Essay items TETAP masuk list, hanya flag IsEssayPending di-set true saat pending — view Razor render label 'Menunggu Penilaian' alih-alih correct/incorrect (CONTEXT D-08 lock)"
      pattern: "IsEssayPending\\s*=\\s*"
    - from: "Views/CMP/Results.cshtml Question Review loop"
      to: "@if (q.IsEssayPending) { <span class=\"badge text-bg-secondary\">Menunggu Penilaian</span> } else { existing correct/incorrect badge }"
      via: "Razor conditional render label berdasarkan IsEssayPending flag — CONTEXT D-08 verbatim 'Essay questions tetap tampil di review section dengan label Menunggu Penilaian tanpa nilai'"
      pattern: "if \\(.*\\.IsEssayPending\\)"
    - from: "Views/CMP/Results.cshtml"
      to: "@if (Model.IsPendingGrading) { ... banner + badge + hide cert button ... }"
      via: "Razor conditional branching pada flag — hindari Pitfall #4 (Model.IsPassed.HasValue compile error karena IsPassed non-nullable)"
      pattern: "if \\(Model\\.IsPendingGrading\\)"
    - from: "Views/Shared/_Layout.cshtml line ~209"
      to: "TempData[\"Info\"] alert block (alert-info dengan icon bi-info-circle-fill)"
      via: "Insert blok BARU antara existing Error block (line 199-208) dan Success block (line 209)"
      pattern: "TempData\\[\"Info\"\\]"
---

<objective>
Plan 309-02 mengelaborasi semantic state machine assessment dengan menjadikan `"Menunggu Penilaian"` (status yang di-set GradingService line 199 untuk session ber-essay yang sudah submit tapi belum dinilai HC) sebagai **submitted status yang sah** di endpoint worker-facing (`Results`, `Certificate`, `CertificatePdf`). Implementasi: helper `IsAssessmentSubmitted(string?)` di `AssessmentConstants`, swap 3 lokasi check di `CMPController`, friendly Info-banner branch untuk Certificate/CertificatePdf, pending-mode rendering di Results view (termasuk **Essay items TETAP tampil dengan label "Menunggu Penilaian" per CONTEXT D-08 lock**), dan blok TempData["Info"] di `_Layout.cshtml`.

**Purpose:** Eliminasi popup merah `"Error: Assessment belum selesai."` saat worker yang submit essay-bearing assessment buka Results / klik Lihat Sertifikat / klik Download PDF (audit Temuan 29 April T3). UX correct: pesan ramah biru "Sertifikat akan tersedia setelah penilaian essay selesai." + interim score + badge "MENUNGGU PENILAIAN" + Essay items review TETAP terlihat dengan label pending (BUKAN di-skip).

**Output:** 5 file edit (post-revision iter-1 — Task 5 GradingService refactor di-split ke Plan 309-03 Wave 2 sequential):
- `Models/AssessmentConstants.cs` — tambah constant + helper
- `Models/AssessmentResultsViewModel.cs` — tambah field `bool IsPendingGrading` di main ViewModel + tambah field `bool IsEssayPending` di nested `QuestionReviewItem` (default false, non-breaking)
- `Controllers/CMPController.cs` — 3 status check swap (TempData["Error"] copy ke Bahasa Indonesia "Assessment belum selesai.") + 2 PendingGrading branch (Certificate + CertificatePdf) + Results projection IsPendingGrading + Essay items TETAP masuk `questionReviews` dengan flag `IsEssayPending = (status==PendingGrading && QuestionType==Essay)`
- `Views/CMP/Results.cshtml` — pending mode rendering (banner, badge, hide cert button, hide pass/fail) + Essay items render label "Menunggu Penilaian" saat IsEssayPending=true
- `Views/Shared/_Layout.cshtml` — tambah TempData["Info"] alert block (antara Error block dan Success block)

**Decision yang ditutup planner (researcher Open Questions — REVISED iter-1):**
- **OQ#1 — ViewBag vs ViewModel field untuk IsPendingGrading:** Pilih **ViewModel field** `bool IsPendingGrading` di `AssessmentResultsViewModel.cs` (default false). Reasoning: typesafe (compile-time check), eksplisit di ViewModel contract, non-breaking (default false maintain semantik existing).
- **OQ#2 — Refactor `"Menunggu Penilaian"` literal di GradingService line 196, 199 ke constant:** **SPLIT ke Plan 309-03** sebagai opportunistic Wave 2 sequential plan (post-iter-1 revision per scope_sanity warning — kurangi task count Plan 309-02 dari 6 ke 5).
- **OQ#3 — Essay items di question review saat pending:** **REVISED iter-1** — Pilih **option A: Essay items TETAP masuk `questionReviews` list dengan field `IsEssayPending` baru di `QuestionReviewItem`**. Controller set `IsEssayPending = (assessment.Status == PendingGrading && q.QuestionType == "Essay")`. View Razor render label `"Menunggu Penilaian"` (badge text-bg-secondary) alih-alih correct/incorrect untuk Essay items dengan flag true; MC/MA items tetap show correct/incorrect normal. **Reasoning:** CONTEXT D-08 LOCKED explicit "Essay questions TETAP tampil di review section dengan label Menunggu Penilaian tanpa nilai (MC/MA tetap show correct/incorrect)". Decision sebelumnya (option B skip) BERTENTANGAN dengan D-08 — diperbaiki di iter-1 revision.
- **OQ#4 — Optional `_logger.LogInformation` di branch PendingGrading:** **OMIT** per CONTEXT D-10 lock (log noise).

**CLAUDE.md compliance fix iter-1:** TempData["Error"] di 3 lokasi status check menggunakan Bahasa Indonesia `"Assessment belum selesai."` (BUKAN English `"Assessment not completed yet."`). Konsistensi dengan project directive `./CLAUDE.md` line 3 + RESEARCH.md §"Project Constraints (from CLAUDE.md)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md
@.planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md

<interfaces>
<!-- Key contracts diekstrak dari codebase. Executor pakai langsung — tidak perlu explore codebase. -->

From `Models/AssessmentConstants.cs` (current state — full file 41 lines):
```csharp
namespace HcPortal.Models
{
    public static class AssessmentConstants
    {
        public static class AssessmentType { /* Manual, Online, PreTest, PostTest */ }
        public static class AssessmentStatus
        {
            public const string Open = "Open";
            public const string Upcoming = "Upcoming";
            public const string Completed = "Completed";
            // ← INSERT PendingGrading di sini
        }
        public static class CertificateType { /* Permanent, Annual, ThreeYear */ }
        public static class FileValidation { /* MaxCertificateFileSizeBytes, AllowedCertificateExtensions */ }
        // ← INSERT IsAssessmentSubmitted helper di sini (top-level static method, BUKAN nested)
    }
}
```

From `Models/AssessmentResultsViewModel.cs` (current state — extend dengan 2 field baru):
```csharp
public class AssessmentResultsViewModel
{
    public int AssessmentId { get; set; }
    public string Title { get; set; } = "";
    // ... 11 properties existing ...
    public string? NomorSertifikat { get; set; }
    // ← INSERT public bool IsPendingGrading { get; set; } = false;
}

public class QuestionReviewItem
{
    public int QuestionNumber { get; set; }
    public string QuestionText { get; set; } = "";
    public string? UserAnswer { get; set; }
    public string CorrectAnswer { get; set; } = "";
    public bool IsCorrect { get; set; }
    public List<OptionReviewItem> Options { get; set; } = new List<OptionReviewItem>();
    // ← INSERT public bool IsEssayPending { get; set; } = false;
}
```

From `Controllers/CMPController.cs` line 1792, 1858, 2105 (3 swap targets — IDENTICAL pattern; user-facing copy WAJIB Bahasa Indonesia per CLAUDE.md):
```csharp
if (assessment.Status != "Completed")
{
    TempData["Error"] = "Assessment not completed yet.";  // ← swap ke Bahasa Indonesia "Assessment belum selesai."
    return RedirectToAction("Assessment");
}
```

From `Views/Shared/_Layout.cshtml` line 199-218 (existing Error + Success blocks — Info to be inserted between):
```cshtml
@if (TempData["Error"] != null) { /* alert-danger with bi-x-circle-fill */ }
@if (TempData["Success"] != null) { /* alert-success with bi-check-circle-fill */ }
```

From `Views/CMP/Results.cshtml` line 36 (card-header — current):
```cshtml
<div class="card-header @(Model.IsPassed ? "bg-success" : "bg-danger") text-white">
```

From `Views/CMP/Results.cshtml` line 58-69 (status badge — current):
```cshtml
@if (Model.IsPassed) { /* LULUS badge text-bg-success */ }
else { /* TIDAK LULUS badge text-bg-danger */ }
```

From `Views/CMP/Results.cshtml` line 374-384 (action buttons — current):
```cshtml
@if (Model.IsPassed && Model.GenerateCertificate) {
    <a asp-action="Certificate" asp-route-id="@Model.AssessmentId" class="btn btn-primary" target="_blank">
        <i class="bi bi-award me-1"></i>Lihat Sertifikat
    </a>
}
```

From `Views/CMP/Results.cshtml` line ~302-364 (Question Review loop — Essay item label target):
Question Review loop iterates `Model.QuestionReviews` (List<QuestionReviewItem>). Existing render correct/incorrect badge per item. Iter-1 revision: tambah conditional di dalam loop — JIKA `q.IsEssayPending == true` render badge "Menunggu Penilaian" (text-bg-secondary), ELSE render existing correct/incorrect badge.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AssessmentConstants — tambah PendingGrading constant + IsAssessmentSubmitted helper</name>
  <files>Models/AssessmentConstants.cs</files>
  <read_first>
    - Models/AssessmentConstants.cs (full 41 lines — current structure dengan nested static AssessmentType/AssessmentStatus/CertificateType/FileValidation)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md §D-04 + D-05 (constant string + helper signature lock)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pattern 3: Status Normalization Helper" lines 397-447 (target output verbatim)
  </read_first>
  <action>
EDIT `Models/AssessmentConstants.cs`. Buat 2 perubahan terpisah:

**Change A — Append `PendingGrading` constant ke nested `AssessmentStatus` class (current line 13-18):**

REPLACE block:
```csharp
public static class AssessmentStatus
{
    public const string Open = "Open";
    public const string Upcoming = "Upcoming";
    public const string Completed = "Completed";
}
```

DENGAN:
```csharp
public static class AssessmentStatus
{
    public const string Open = "Open";
    public const string Upcoming = "Upcoming";
    public const string Completed = "Completed";
    public const string PendingGrading = "Menunggu Penilaian"; // Phase 309 D-04 — set by GradingService L199 untuk session ber-essay
}
```

**Change B — Append top-level static helper `IsAssessmentSubmitted` ke class `AssessmentConstants` (BUKAN nested — di luar nested classes, di dalam class outer):**

INSERT setelah closing brace `FileValidation` class (current line 38) tapi SEBELUM closing brace `AssessmentConstants` class (current line 39):

```csharp

// Phase 309 D-05 — top-level helper untuk normalisasi status semantik "submitted"
// Returns true untuk Completed (selesai dinilai) ATAU PendingGrading (essay belum dinilai HC)
public static bool IsAssessmentSubmitted(string? status) =>
    status == AssessmentStatus.Completed || status == AssessmentStatus.PendingGrading;
```

**Specific instruksi exec:**

1. **Helper SIGNATURE WAJIB EXACT:**
   - Modifier `public static` (bukan internal/private)
   - Return type `bool` (non-nullable)
   - Parameter `string?` (nullable string — harus reject `null` argument tanpa NRE — `null == "Completed"` evaluasi `false` di C#, AMAN)
   - Body expression-bodied `=>` (single-line, sesuai existing convention CertificateType)
   - Return statement: `status == AssessmentStatus.Completed || status == AssessmentStatus.PendingGrading`
2. **Helper di TOP-LEVEL `AssessmentConstants`** (bukan di dalam `AssessmentStatus` nested) — CONTEXT D-05 explicit: "top-level, bukan nested". Call site akan pakai `AssessmentConstants.IsAssessmentSubmitted(...)`.
3. **JANGAN refactor nested static `AssessmentStatus`/`AssessmentType`/`CertificateType`/`FileValidation`** ke top-level. Existing nested structure per CONVENTIONS — preserve.
4. **JANGAN tambah XML doc summary tag (`/// <summary>`)** — codebase ini tidak konsisten pakai XML doc; comment satu-baris `// Phase 309 D-04` cukup untuk traceability.
5. **Namespace `HcPortal.Models` tetap.** No new `using` directive needed — `string?` dan `bool` adalah primitive.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0; warning count ≤ 92
    - `grep -F 'public const string PendingGrading = "Menunggu Penilaian";' Models/AssessmentConstants.cs` returns 1 hit
    - `grep -F 'public static bool IsAssessmentSubmitted(string? status) =>' Models/AssessmentConstants.cs` returns 1 hit
    - `grep -F 'status == AssessmentStatus.Completed || status == AssessmentStatus.PendingGrading;' Models/AssessmentConstants.cs` returns 1 hit
    - Helper `IsAssessmentSubmitted` defined OUTSIDE nested classes (verify via line context: helper appears AFTER `}` closing brace of `FileValidation` class AND BEFORE final `}` closing brace of `AssessmentConstants` class)
    - Existing constants `Open`, `Upcoming`, `Completed`, `Manual`, `Online`, `PreTest`, `PostTest`, `Permanent`, `Annual`, `ThreeYear`, `MaxCertificateFileSizeBytes`, `AllowedCertificateExtensions` TIDAK berubah (verify: `grep -c 'public const string' Models/AssessmentConstants.cs` returns ≥ 11 — 3 status + 4 type + 3 cert + at least 1 from PendingGrading)
  </acceptance_criteria>
  <done>`Models/AssessmentConstants.cs` punya constant `AssessmentStatus.PendingGrading = "Menunggu Penilaian"` + top-level static helper `IsAssessmentSubmitted(string?)` returning bool; dotnet build pass 0 error; existing constants preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ViewModel + 3-lokasi swap CMPController + 2 PendingGrading branch + Results IsPendingGrading projection + Essay items dengan IsEssayPending flag (CONTEXT D-08)</name>
  <files>Models/AssessmentResultsViewModel.cs, Controllers/CMPController.cs</files>
  <read_first>
    - Models/AssessmentResultsViewModel.cs (full 53 lines — current ViewModel definition; main class line 3-21, nested QuestionReviewItem line 23-31, OptionReviewItem line 33-38; field IsPassed di line 11 typed `bool` non-nullable)
    - Controllers/CMPController.cs line 1771-1811 (Certificate action — Plan 309-01 sudah wrap try-catch; line numbers di Task ini relatif ke POST-Plan-309-01, jadi line numbers shifted by ~+8 baris dari pre-309 baseline — gunakan grep `assessment.Status != "Completed"` untuk locate ulang)
    - Controllers/CMPController.cs line 1858 (CertificatePdf status check)
    - Controllers/CMPController.cs line 2105 (Results status check)
    - Controllers/CMPController.cs line 2228-2245 + 2250-2267 (Results ViewModel construction — 2 lokasi `viewModel = new AssessmentResultsViewModel { ... }` di package path dan legacy path)
    - Controllers/CMPController.cs line 2146-2177 (`questionReviews` building — Essay items projection target untuk OQ#3 REVISED iter-1: TETAP masuk list dengan IsEssayPending flag)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md §D-06 (3 lokasi swap) + §D-07 (PendingGrading branch placement) + §D-08 (Results pending mode — **Essay TETAP tampil dengan label "Menunggu Penilaian"**)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pattern 4: PendingGrading Branch" lines 449-467 + §"Pitfall 3: PendingGrading branch placement" lines 581-594 (CRITICAL: branch HARUS sebelum GenerateCertificate + IsPassed checks)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pitfall 4: Model.IsPassed.HasValue Razor guard" lines 595-617 (verify pakai bool IsPendingGrading field, BUKAN ubah IsPassed ke bool?)
    - ./CLAUDE.md (Bahasa Indonesia constraint untuk TempData["Error"] copy)
  </read_first>
  <action>
**Step 1 — Edit `Models/AssessmentResultsViewModel.cs` tambah 2 field baru:**

**Change 1A:** INSERT 1 baris BARU di class `AssessmentResultsViewModel` (line 3-21) SETELAH baris `public string? NomorSertifikat { get; set; }` (line 20) tapi SEBELUM closing brace `}` class (line 21):

```csharp
public bool IsPendingGrading { get; set; } = false; // Phase 309 SUB-01 — true saat assessment.Status == PendingGrading
```

**Change 1B (NEW iter-1 — CONTEXT D-08 fix):** INSERT 1 baris BARU di nested class `QuestionReviewItem` (line 23-31) SETELAH baris `public List<OptionReviewItem> Options { get; set; } = new List<OptionReviewItem>();` (line 30) tapi SEBELUM closing brace `}` class (line 31):

```csharp
public bool IsEssayPending { get; set; } = false; // Phase 309 OQ#3 D-08 — true saat status PendingGrading dan QuestionType Essay (label "Menunggu Penilaian" di view)
```

**JANGAN ubah field `IsPassed` (line 11)** — tetap typed `bool` non-nullable. Mengubah ke `bool?` adalah BREAKING CHANGE (Pitfall #4 RESEARCH).

**Step 2 — Edit `Controllers/CMPController.cs` swap 3 lokasi `assessment.Status != "Completed"` → `!AssessmentConstants.IsAssessmentSubmitted(assessment.Status)` + TempData["Error"] copy ke Bahasa Indonesia:**

3 LOKASI dengan PATTERN IDENTIK. Catatan: line numbers di-shift sekitar +8 baris pasca Plan 309-01 try-catch wrap. Locate via grep `assessment.Status != "Completed"` di file `CMPController.cs` — harus ada 3 hits target di Certificate, CertificatePdf, Results action.

**Lokasi A (Certificate — sekitar line 1792 pre-309 / ~1800 post-309-01):**

REPLACE block:
```csharp
if (assessment.Status != "Completed")
{
    TempData["Error"] = "Assessment not completed yet.";
    return RedirectToAction("Assessment");
}
```

DENGAN:
```csharp
// SUB-01 D-06: normalize submitted status (Completed OR PendingGrading)
if (!AssessmentConstants.IsAssessmentSubmitted(assessment.Status))
{
    TempData["Error"] = "Assessment belum selesai.";
    return RedirectToAction("Assessment");
}

// SUB-01 D-07: PendingGrading branch — HARUS sebelum check GenerateCertificate dan IsPassed (Pitfall 3)
if (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading)
{
    TempData["Info"] = "Sertifikat akan tersedia setelah penilaian essay selesai.";
    return RedirectToAction("Results", new { id });
}
```

**Lokasi B (CertificatePdf — sekitar line 1858):** PATTERN IDENTIK Lokasi A. Apply SAMA: ganti `if (assessment.Status != "Completed")` block dengan IsAssessmentSubmitted swap (TempData["Error"] = "Assessment belum selesai."), KEMUDIAN tambah PendingGrading branch SEBELUM existing `if (!assessment.GenerateCertificate) return NotFound();` line.

**Lokasi C (Results — sekitar line 2105):** ONLY swap, **TIDAK** tambah PendingGrading branch (CONTEXT D-07 explicit: PendingGrading branch hanya di Certificate + CertificatePdf, BUKAN Results — Results render pending mode di view, bukan redirect).

REPLACE di Lokasi C:
```csharp
if (assessment.Status != "Completed")
{
    TempData["Error"] = "Assessment not completed yet.";
    return RedirectToAction("Assessment");
}
```

DENGAN:
```csharp
// SUB-01 D-06: normalize submitted status (Completed OR PendingGrading)
if (!AssessmentConstants.IsAssessmentSubmitted(assessment.Status))
{
    TempData["Error"] = "Assessment belum selesai.";
    return RedirectToAction("Assessment");
}
```

(Tidak tambah PendingGrading branch di Lokasi C.)

**CLAUDE.md compliance (iter-1 fix Warning #1):** TempData["Error"] copy WAJIB Bahasa Indonesia `"Assessment belum selesai."` di SEMUA 3 lokasi swap. Existing literal English `"Assessment not completed yet."` MUST be replaced. CLAUDE.md line 3: "Always respond in Bahasa Indonesia" + RESEARCH.md §"Project Constraints (from CLAUDE.md)": "Semua user-facing copy di TempData ... WAJIB Bahasa Indonesia."

**Step 3 — Edit Results action (Controllers/CMPController.cs sekitar line 2228-2245 dan 2250-2267) untuk projection `IsPendingGrading` di kedua ViewModel construction:**

Cari **2 instance** `viewModel = new AssessmentResultsViewModel { ... }` di method Results (1 di package path ~line 2228, 1 di legacy path ~line 2250). Di setiap instance, SETELAH line `NomorSertifikat = assessment.NomorSertifikat` tambah:

```csharp
viewModel = new AssessmentResultsViewModel
{
    // ... existing fields ...
    NomorSertifikat = assessment.NomorSertifikat,
    IsPendingGrading = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading)
};
```

(Comma sebelum `IsPendingGrading`, dan TIDAK ada koma trailing setelah karena ini field terakhir.)

**Step 4 — Edit Results action questionReviews loop dengan IsEssayPending projection (REVISED iter-1 per CONTEXT D-08):**

Locate package path `questionReviews` building loop di line ~2148-2177 (di dalam `if (assessment.AllowAnswerReview)` block). PackageQuestion `q` punya field `QuestionType` (string, values: `"MultipleChoice"`, `"MultipleAnswer"`, `"Essay"`).

INSIDE foreach loop, locate `new QuestionReviewItem { ... }` instantiation (atau equivalent var assignment). Tambah field `IsEssayPending` ke object initializer:

```csharp
foreach (var qId in orderedQuestionIds)
{
    if (!questionLookup.TryGetValue(qId, out var question)) continue;

    questionNum++;

    // ... existing logic untuk compute userAnswer, correctAnswer, isCorrect, options ...

    questionReviews.Add(new QuestionReviewItem
    {
        QuestionNumber = questionNum,
        QuestionText = question.QuestionText ?? "",
        UserAnswer = userAnswer,
        CorrectAnswer = correctAnswer,
        IsCorrect = isCorrect,
        Options = options,
        // SUB-01 OQ#3 D-08: Essay items TETAP tampil di review — flag true saat status PendingGrading + QuestionType Essay
        // (View Razor render label "Menunggu Penilaian" alih-alih correct/incorrect; CONTEXT D-08 lock)
        IsEssayPending = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading
                         && (question.QuestionType ?? "MultipleChoice") == "Essay")
    });
}
```

**KEY DIFFERENCE iter-1 vs iter-0:** iter-0 SKIP Essay items dengan `continue` saat pending — BERTENTANGAN dengan CONTEXT D-08 lock. iter-1 REVISI: Essay items TETAP masuk list (tidak skip), hanya flag `IsEssayPending` di-set true. View Razor (Task 4) yang handle conditional rendering label.

**Specific instruksi exec:**

1. **Line numbers POST-Plan-309-01:** Plan 309-01 wrap Certificate dengan try-catch (~+8 lines indent + catch handlers). LINE NUMBERS di RESEARCH/CONTEXT adalah PRE-309 baseline. Executor WAJIB locate-by-content via grep, BUKAN hard-code line number.
2. **PendingGrading branch placement (Pitfall #3 CRITICAL):** SETELAH IsAssessmentSubmitted check, SEBELUM `if (!assessment.GenerateCertificate)`. Jangan tukar order.
3. **Lokasi C (Results) TIDAK punya `if (!assessment.GenerateCertificate) return NotFound();`** — verify by reading: Results action setelah status check langsung lanjut ke build ViewModel logic (line 2112+). Jadi insert PendingGrading branch tidak applicable di Results.
4. **CLAUDE.md BI compliance:** TempData["Error"] = "Assessment belum selesai." (BUKAN "Assessment not completed yet.") di 3 lokasi swap — iter-1 fix Warning #1.
5. **OQ#3 Essay flag (REVISED iter-1):** field `QuestionType` di `PackageQuestion` typed `string?` (nullable) — pakai null-coalescing default `"MultipleChoice"` saat null untuk safety. Flag true HANYA jika status PendingGrading AND tipe Essay. Saat Completed (final) flag false untuk semua items — Essay items tampil dengan correct/incorrect normal (existing behavior preserved — non-breaking).
6. **`questionNum++` placement** — TETAP SEBELUM `questionReviews.Add(...)` (existing position). Verify existing line ~2153 `questionNum++;`.
7. **Import:** TIDAK perlu tambah `using` directive baru — `AssessmentConstants` sudah accessible via `using HcPortal.Models;` (existing line 4).
8. **Comment trail Phase 309:** setiap blok edit prefix `// SUB-01 D-NN` (D-04, D-05, D-06, D-07, D-08) dan `// SUB-01 OQ#3` untuk traceability post-deploy review.

**JANGAN:**
- Ubah signature method Certificate / CertificatePdf / Results
- Refactor existing IsPassed/Score/PassPercentage projection (out of scope)
- Tambah AuditLog entry untuk pending branch (CONTEXT D-10 omit)
- Refactor `questionReviews` building ke helper method (over-engineering)
- **SKIP Essay items dari list** (iter-0 pattern — REVERTED iter-1 per CONTEXT D-08 lock)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0; warning count ≤ 92
    - `grep -F 'public bool IsPendingGrading { get; set; } = false;' Models/AssessmentResultsViewModel.cs` returns 1 hit
    - `grep -F 'public bool IsEssayPending { get; set; } = false;' Models/AssessmentResultsViewModel.cs` returns 1 hit (NEW iter-1 D-08 fix)
    - `Models/AssessmentResultsViewModel.cs` field `IsPassed` line ~11 TETAP `public bool IsPassed { get; set; }` (non-nullable, NO change)
    - `grep -c 'AssessmentConstants.IsAssessmentSubmitted(assessment.Status)' Controllers/CMPController.cs` returns ≥ 3 (3 lokasi swap)
    - `grep -c 'assessment.Status != "Completed"' Controllers/CMPController.cs` returns ≤ 1 (target 1146 exam abandon flow tetap; 3 target SUB-01 sudah swap; jika 0 hit OK juga)
    - `grep -c 'AssessmentConstants.AssessmentStatus.PendingGrading' Controllers/CMPController.cs` returns ≥ 5 (2 PendingGrading branch di Certificate + CertificatePdf, 2 ViewModel projection IsPendingGrading di Results package + legacy path, 1 di IsEssayPending projection di questionReviews loop)
    - `grep -F 'TempData["Info"] = "Sertifikat akan tersedia setelah penilaian essay selesai.";' Controllers/CMPController.cs` returns ≥ 2 hits (Certificate + CertificatePdf branch)
    - `grep -c 'TempData\["Error"\] = "Assessment belum selesai\.";' Controllers/CMPController.cs` returns ≥ 3 hits (CLAUDE.md BI compliance — 3 lokasi swap; iter-1 fix Warning #1)
    - `grep -c 'TempData\["Error"\] = "Assessment not completed yet\.";' Controllers/CMPController.cs` returns 0 hits (English literal SUDAH HILANG dari 3 lokasi target — iter-1 fix Warning #1)
    - `grep -c 'IsPendingGrading = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading)' Controllers/CMPController.cs` returns ≥ 2 (package path + legacy path ViewModel projection)
    - `grep -F 'IsEssayPending = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading' Controllers/CMPController.cs` returns ≥ 1 hit (NEW iter-1 D-08 — Essay items projection di questionReviews loop)
    - `grep -F '(question.QuestionType ?? "MultipleChoice") == "Essay"' Controllers/CMPController.cs` returns ≥ 1 hit (Essay type check)
    - PendingGrading branch di Certificate ditempatkan SEBELUM `if (!assessment.GenerateCertificate)` (verify via line context: TempData["Info"] line < `assessment.GenerateCertificate` line in Certificate method)
    - **NEGATIVE check (iter-1 D-08 lock):** `grep -B2 -A2 'continue;' Controllers/CMPController.cs | grep -i "essay"` returns 0 hits (Essay items TIDAK di-skip dengan continue dari questionReviews loop — iter-0 pattern ABSENT)
  </acceptance_criteria>
  <done>`Models/AssessmentResultsViewModel.cs` punya 2 field baru: `IsPendingGrading` di main class (default false) + `IsEssayPending` di nested QuestionReviewItem (default false); `Controllers/CMPController.cs` swap 3 lokasi status check ke `!AssessmentConstants.IsAssessmentSubmitted` dengan TempData["Error"] copy Bahasa Indonesia; PendingGrading branch ditambahkan di Certificate dan CertificatePdf SEBELUM GenerateCertificate/IsPassed checks dengan TempData["Info"] redirect Results; Results action projection IsPendingGrading di 2 ViewModel construction; **Essay items TETAP masuk questionReviews list (TIDAK di-skip) dengan flag IsEssayPending=true saat status PendingGrading + QuestionType Essay (CONTEXT D-08 lock)**; dotnet build pass 0 error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: _Layout.cshtml — tambah TempData["Info"] alert block</name>
  <files>Views/Shared/_Layout.cshtml</files>
  <read_first>
    - Views/Shared/_Layout.cshtml line 188-220 (existing TempData blocks: Warning line 189-198, Error line 199-208, Success line 209-218)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Pattern 5: TempData Info Alert Render" lines 469-493 (target output verbatim)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md §D-09
  </read_first>
  <action>
EDIT `Views/Shared/_Layout.cshtml`. INSERT blok BARU `TempData["Info"]` alert SETELAH existing Error block (line 199-208) tapi SEBELUM existing Success block (line 209).

Locate via grep: tepat setelah `</div>` line 208 (closing Error block — `}` line 208) dan SEBELUM `@if (TempData["Success"] != null)` line 209.

INSERT exact block ini:

```cshtml
    @if (TempData["Info"] != null)
    {
        <div class="container mt-3">
            <div class="alert alert-info alert-dismissible fade show" role="alert">
                <i class="bi bi-info-circle-fill me-2"></i>
                <strong>Info:</strong> @TempData["Info"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    }
```

**Specific instruksi exec:**

1. **Indentation WAJIB 4 spaces** (sesuai existing Warning/Error/Success blocks line 189, 199, 209 — pakai 4-space indent untuk root `@if`, 8-space untuk inner `<div class="container mt-3">`, 12-space untuk inner `<div class="alert">`). Match existing file convention.
2. **CSS class WAJIB `alert-info`** (BUKAN `alert-primary` atau `alert-secondary`) — CONTEXT D-09 + Bootstrap 5.3 convention.
3. **Icon WAJIB `bi bi-info-circle-fill`** — convention existing pattern: Warning pakai `bi-exclamation-triangle-fill`, Error pakai `bi-x-circle-fill`, Success pakai `bi-check-circle-fill`. Info pakai `bi-info-circle-fill`.
4. **Strong word `<strong>Info:</strong>`** (Bahasa Inggris seperti existing `<strong>Warning:</strong>`/`<strong>Error:</strong>`/`<strong>Success:</strong>` — preserve convention; CLAUDE.md Bahasa Indonesia rule applies untuk DYNAMIC user-facing copy di TempData value, BUKAN label prefix).
5. **`alert-dismissible fade show`** + dismiss button konsisten existing.
6. **JANGAN ubah** existing Warning/Error/Success blocks (line 189-218) — append-only, surgical insert.
7. **JANGAN tambah CSS file baru atau edit `<head>`** — `alert-info` style sudah loaded via Bootstrap 5.3 CDN (line 38).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 (Razor view compile success); warning count ≤ 92
    - `grep -F '@if (TempData["Info"] != null)' Views/Shared/_Layout.cshtml` returns 1 hit
    - `grep -F 'alert alert-info alert-dismissible fade show' Views/Shared/_Layout.cshtml` returns 1 hit
    - `grep -F 'bi bi-info-circle-fill me-2' Views/Shared/_Layout.cshtml` returns 1 hit
    - `grep -F '<strong>Info:</strong> @TempData["Info"]' Views/Shared/_Layout.cshtml` returns 1 hit
    - Existing TempData blocks (Warning, Error, Success) TIDAK berubah: `grep -c '@if (TempData\["Warning"\] != null)' Views/Shared/_Layout.cshtml` returns 1; same untuk Error dan Success
    - Info block placement BETWEEN Error and Success: verify via line context — line number of `@if (TempData["Info"]` falls between line of `@if (TempData["Error"]` and `@if (TempData["Success"]`
  </acceptance_criteria>
  <done>`Views/Shared/_Layout.cshtml` punya blok `TempData["Info"]` alert dengan class `alert-info` + icon `bi-info-circle-fill` + label `<strong>Info:</strong>` + dismiss button; ditempatkan antara existing Error block dan Success block; dotnet build pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Results.cshtml — pending mode rendering (banner, badge, hide cert button, hide pass/fail) + Essay items label "Menunggu Penilaian" (CONTEXT D-08)</name>
  <files>Views/CMP/Results.cshtml</files>
  <read_first>
    - Views/CMP/Results.cshtml line 1-12 (Razor model declaration + ViewData)
    - Views/CMP/Results.cshtml line 32-118 (Page Header, Results Summary Card, status badge area, action buttons, motivasi block — 4 lokasi conditional render target)
    - Views/CMP/Results.cshtml line 294-365 (Question Review section — Essay items label target NEW iter-1 D-08 fix)
    - Views/CMP/Results.cshtml line 374-385 (action buttons "Lihat Sertifikat" — hide-target)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-CONTEXT.md §D-08 (Results pending mode requirements: banner, score interim, badge hide, cert button hide, **Essay TETAP tampil dengan label "Menunggu Penilaian"**)
    - .planning/phases/309-worker-cert-defensive-submitted-status/309-RESEARCH.md §"Code Examples §5: Pending-mode render di Results.cshtml" lines 720-797 (full target Razor markup)
  </read_first>
  <action>
EDIT `Views/CMP/Results.cshtml`. Buat 5 perubahan terpisah pada locations berbeda. Setiap perubahan menggunakan flag `Model.IsPendingGrading` (sudah disediakan oleh Task 2). Change F (Question Review label) menggunakan flag `q.IsEssayPending` per item (NEW iter-1 D-08 fix).

**Change A — INSERT banner "Hasil sementara" SETELAH Page Header div (line 32 closing `</div>`), SEBELUM `<!-- Results Summary Card -->` comment (line 34):**

INSERT setelah line 32 `</div>` dan sebelum line 33 (kosong) / line 34:

```cshtml

    <!-- Phase 309 SUB-01: Pending grading banner -->
    @if (Model.IsPendingGrading)
    {
        <div class="alert alert-info mb-4">
            <i class="bi bi-info-circle-fill me-2"></i>
            <strong>Hasil sementara</strong> &mdash; Essay menunggu penilaian HC. Skor &amp; sertifikat akan diperbarui setelah penilaian selesai.
        </div>
    }
```

**Change B — UPDATE card-header line 36 dengan tri-state class:**

REPLACE:
```cshtml
<div class="card-header @(Model.IsPassed ? "bg-success" : "bg-danger") text-white">
```

DENGAN:
```cshtml
<div class="card-header @(Model.IsPendingGrading ? "bg-secondary" : (Model.IsPassed ? "bg-success" : "bg-danger")) text-white">
```

**Change C — UPDATE status badge block line 58-69 dengan tri-state branching:**

REPLACE entire block:
```cshtml
@if (Model.IsPassed)
{
    <span class="badge text-bg-success fs-5 py-2 px-3">
        <i class="bi bi-check-circle-fill me-1"></i>LULUS
    </span>
}
else
{
    <span class="badge text-bg-danger fs-5 py-2 px-3">
        <i class="bi bi-x-circle-fill me-1"></i>TIDAK LULUS
    </span>
}
```

DENGAN:
```cshtml
@if (Model.IsPendingGrading)
{
    <span class="badge text-bg-secondary fs-5 py-2 px-3">
        <i class="bi bi-hourglass-split me-1"></i>MENUNGGU PENILAIAN
    </span>
}
else if (Model.IsPassed)
{
    <span class="badge text-bg-success fs-5 py-2 px-3">
        <i class="bi bi-check-circle-fill me-1"></i>LULUS
    </span>
}
else
{
    <span class="badge text-bg-danger fs-5 py-2 px-3">
        <i class="bi bi-x-circle-fill me-1"></i>TIDAK LULUS
    </span>
}
```

**Change D — UPDATE motivasi block line 92-118 dengan tri-state branching:**

REPLACE entire block:
```cshtml
@if (Model.IsPassed)
{
    <div class="alert alert-success mb-4">
        <i class="bi bi-check-circle-fill me-2"></i><strong>Selamat!</strong> Anda lulus assessment ini.
    </div>
    @if (!string.IsNullOrEmpty(Model.NomorSertifikat))
    {
        <div class="alert alert-info mb-4 d-flex align-items-center">
            <i class="bi bi-award-fill me-2 fs-5"></i>
            <span>Nomor Sertifikat: <strong>@Model.NomorSertifikat</strong></span>
        </div>
    }
    else if (Model.GenerateCertificate)
    {
        <div class="alert alert-warning mb-4 d-flex align-items-center">
            <i class="bi bi-exclamation-triangle-fill me-2 fs-5"></i>
            <span>Sertifikat belum tergenerate. Silakan hubungi administrator.</span>
        </div>
    }
}
else
{
    <div class="alert alert-warning mb-4">
        <i class="bi bi-exclamation-triangle-fill me-2"></i><strong>Terus semangat.</strong> Pelajari kembali materi dan coba lagi.
    </div>
}
```

DENGAN:
```cshtml
@if (Model.IsPendingGrading)
{
    <div class="alert alert-info mb-4 d-flex align-items-center">
        <i class="bi bi-hourglass-split me-2 fs-5"></i>
        <span>Skor di atas adalah <strong>nilai sementara</strong> dari soal Pilihan Tunggal &amp; Pilihan Jamak. Skor final &amp; sertifikat tersedia setelah penilaian Essay selesai.</span>
    </div>
}
else if (Model.IsPassed)
{
    <div class="alert alert-success mb-4">
        <i class="bi bi-check-circle-fill me-2"></i><strong>Selamat!</strong> Anda lulus assessment ini.
    </div>
    @if (!string.IsNullOrEmpty(Model.NomorSertifikat))
    {
        <div class="alert alert-info mb-4 d-flex align-items-center">
            <i class="bi bi-award-fill me-2 fs-5"></i>
            <span>Nomor Sertifikat: <strong>@Model.NomorSertifikat</strong></span>
        </div>
    }
    else if (Model.GenerateCertificate)
    {
        <div class="alert alert-warning mb-4 d-flex align-items-center">
            <i class="bi bi-exclamation-triangle-fill me-2 fs-5"></i>
            <span>Sertifikat belum tergenerate. Silakan hubungi administrator.</span>
        </div>
    }
}
else
{
    <div class="alert alert-warning mb-4">
        <i class="bi bi-exclamation-triangle-fill me-2"></i><strong>Terus semangat.</strong> Pelajari kembali materi dan coba lagi.
    </div>
}
```

**Change E — UPDATE action buttons line 374-384 hide cert button saat pending:**

REPLACE block:
```cshtml
<div class="d-flex gap-2 mb-4">
    @if (Model.IsPassed && Model.GenerateCertificate)
    {
        <a asp-action="Certificate" asp-route-id="@Model.AssessmentId" class="btn btn-primary" target="_blank">
            <i class="bi bi-award me-1"></i>Lihat Sertifikat
        </a>
    }
    <a href="@backUrl" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>Kembali
    </a>
</div>
```

DENGAN:
```cshtml
<div class="d-flex gap-2 mb-4">
    @if (!Model.IsPendingGrading && Model.IsPassed && Model.GenerateCertificate)
    {
        <a asp-action="Certificate" asp-route-id="@Model.AssessmentId" class="btn btn-primary" target="_blank">
            <i class="bi bi-award me-1"></i>Lihat Sertifikat
        </a>
    }
    <a href="@backUrl" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>Kembali
    </a>
</div>
```

**Change F (NEW iter-1 D-08 fix) — UPDATE Question Review section line ~302-364 untuk Essay items label "Menunggu Penilaian":**

Locate Question Review loop di area `@foreach (var q in Model.QuestionReviews) { ... }` (atau equivalent). Inside loop existing render correct/incorrect badge per item. Tambah conditional di awal item rendering:

LOCATE existing badge render (kemungkinan struktur):
```cshtml
@if (q.IsCorrect)
{
    <span class="badge text-bg-success">Benar</span>
}
else
{
    <span class="badge text-bg-danger">Salah</span>
}
```

WRAP dengan IsEssayPending guard:
```cshtml
@if (q.IsEssayPending)
{
    <span class="badge text-bg-secondary">
        <i class="bi bi-hourglass-split me-1"></i>Menunggu Penilaian
    </span>
}
else if (q.IsCorrect)
{
    <span class="badge text-bg-success">
        <i class="bi bi-check-circle me-1"></i>Benar
    </span>
}
else
{
    <span class="badge text-bg-danger">
        <i class="bi bi-x-circle me-1"></i>Salah
    </span>
}
```

**Important iter-1 specifics for Change F:**

- Read existing line 302-364 dulu — exact markup struktur (apakah pakai `<span class="badge">` langsung atau wrapped di `<div>`) — preserve existing wrapper, hanya replace inner conditional.
- Jika existing pakai different text (e.g., "Correct" alih-alih "Benar"), MATCH existing convention — JANGAN ubah text correct/incorrect (out of scope; hanya tambah Essay pending branch).
- Jika existing punya MORE markup di dalam if-else (e.g., user answer comparison block, correctAnswer reveal, options breakdown), pertahankan SEMUA markup tersebut di branch `else if (q.IsCorrect)` dan `else` — Essay branch HANYA render badge "Menunggu Penilaian" tanpa user answer breakdown (essay belum dinilai, tidak ada correct answer untuk dibandingkan).
- Untuk Essay items: TIDAK render `q.UserAnswer` atau `q.CorrectAnswer` — ini essay text yang akan dinilai HC. Cukup tampilkan question text + label "Menunggu Penilaian". JIKA executor butuh menampilkan user answer essay (apa yang ditulis worker), TANYAKAN user via decision checkpoint — saat ini scope D-08 hanya "label Menunggu Penilaian tanpa nilai".

**Specific instruksi exec:**

1. **Bahasa Indonesia consistency (CLAUDE.md):** semua string user-facing baru — `"MENUNGGU PENILAIAN"`, `"Menunggu Penilaian"`, `"Hasil sementara"`, `"nilai sementara"`, `"Skor final & sertifikat tersedia setelah penilaian Essay selesai"` — pakai Bahasa Indonesia.
2. **Razor entity escapes:** `&mdash;` untuk em-dash, `&amp;` untuk ampersand, `&` literal di Razor sebenarnya valid tapi escape lebih safe.
3. **Bootstrap classes:**
   - `bg-secondary` (gray) untuk pending header — neutral, bukan success/danger
   - `text-bg-secondary` untuk badge
   - `alert alert-info` untuk banner pending
   - `bi-hourglass-split` untuk pending icon (sand timer dropping — visual untuk "menunggu")
4. **Skor display tetap tampil saat pending** — Score, PassPercentage, dan correctCount/total tetap render normal di line 41-71 (TIDAK perlu hide). Banner sudah jelaskan ini "nilai sementara".
5. **Question Review section iter-1 (Change F NEW):** Essay items TETAP render di loop (controller TIDAK skip — Task 2 iter-1 fix). View Razor handle conditional label berdasarkan `q.IsEssayPending` flag. Saat `IsEssayPending=false` (Completed flow atau MC/MA item), existing correct/incorrect rendering preserved (regression-safe).
6. **JANGAN ubah Pre-Post comparison section (line 120+)** — out of scope, Phase 309 fokus Certificate flow + status normalization.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0; warning count ≤ 92
    - `grep -F 'Hasil sementara' Views/CMP/Results.cshtml` returns ≥ 1 hit (banner heading)
    - `grep -F 'MENUNGGU PENILAIAN' Views/CMP/Results.cshtml` returns 1 hit (badge text uppercase di status badge area)
    - `grep -F 'Menunggu Penilaian' Views/CMP/Results.cshtml` returns ≥ 2 hits (badge "MENUNGGU PENILAIAN" di Change C + badge label "Menunggu Penilaian" di Change F Question Review)
    - `grep -F 'bi-hourglass-split' Views/CMP/Results.cshtml` returns ≥ 3 hits (badge status + motivasi block + Question Review Essay badge)
    - `grep -F 'Model.IsPendingGrading ? "bg-secondary"' Views/CMP/Results.cshtml` returns 1 hit (card-header tri-state)
    - `grep -F '@if (!Model.IsPendingGrading && Model.IsPassed && Model.GenerateCertificate)' Views/CMP/Results.cshtml` returns 1 hit (cert button hide guard)
    - `grep -c '@if (Model.IsPendingGrading)' Views/CMP/Results.cshtml` returns ≥ 3 (banner + badge + motivasi block)
    - `grep -F 'q.IsEssayPending' Views/CMP/Results.cshtml` returns ≥ 1 hit (Question Review Essay branch — NEW iter-1 D-08 fix; variable name boleh `q` atau `qr` atau apapun yang dipakai di foreach)
    - `grep -E '@if \(.*\.IsEssayPending\)' Views/CMP/Results.cshtml` returns ≥ 1 hit (conditional Razor di Question Review loop — flexible variable name)
    - Existing structure preserved: `grep -c 'Lihat Sertifikat' Views/CMP/Results.cshtml` returns 1 (cert button text unchanged); `grep -c 'TIDAK LULUS' Views/CMP/Results.cshtml` returns 1; `grep -c 'Kembali' Views/CMP/Results.cshtml` returns ≥ 2 (back button)
  </acceptance_criteria>
  <done>`Views/CMP/Results.cshtml` render mode pending complete: banner alert-info "Hasil sementara" muncul, card-header bg-secondary saat pending, badge "MENUNGGU PENILAIAN" dengan icon hourglass, motivasi block info-tone alert, cert button HIDDEN saat pending; **Question Review section: Essay items render badge "Menunggu Penilaian" saat q.IsEssayPending=true alih-alih correct/incorrect (CONTEXT D-08 lock — iter-1 fix Blocker #1); MC/MA items dan Completed flow Essay items tetap render correct/incorrect normal (regression-safe)**; existing happy-path render LULUS/TIDAK LULUS preserved; dotnet build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual UAT 6-step Bahasa Indonesia (full SUB-01 flow + regression Completed + Essay review label verify)</name>
  <files>(checkpoint task — no file modification; manual verification only)</files>
  <action>Manual UAT 6-step Bahasa Indonesia oleh user — see &lt;what-built&gt; dan &lt;how-to-verify&gt; sections below untuk steps detail. Executor TIDAK melakukan code edit di task ini; pause execution dan tunggu user resume signal "approved" atau issue description.</action>
  <verify>Manual visual + behavioral verification per &lt;how-to-verify&gt; 6-step protocol. No automated command. User describes pass/fail per step.</verify>
  <done>User signal "approved" setelah SEMUA 6 step PASS (atau document fail per Step + remediation plan). Resume signal yang valid: "approved" atau "Step N failed: [detail]".</done>
  <what-built>
Plan 309-02 (iter-1 revision) telah meng-implementasi state machine SUB-01 lengkap untuk worker Certificate flow:

**Code changes (5 file — Task 5 GradingService refactor SPLIT ke Plan 309-03 Wave 2 sequential per iter-1 scope_sanity fix):**
1. `Models/AssessmentConstants.cs`: constant `AssessmentStatus.PendingGrading = "Menunggu Penilaian"` + helper `IsAssessmentSubmitted(string?)`
2. `Models/AssessmentResultsViewModel.cs`: 2 field baru — `bool IsPendingGrading` di main class + `bool IsEssayPending` di nested QuestionReviewItem (default false, non-breaking)
3. `Controllers/CMPController.cs`:
   - 3 lokasi swap status check ke `!IsAssessmentSubmitted` dengan TempData["Error"] = "Assessment belum selesai." (Bahasa Indonesia per CLAUDE.md — iter-1 fix Warning #1)
   - 2 PendingGrading branch (Certificate + CertificatePdf) ke TempData["Info"] + redirect Results
   - Results action projection IsPendingGrading di 2 ViewModel construction
   - **Essay items TETAP masuk questionReviews list dengan flag IsEssayPending=true saat status PendingGrading + QuestionType Essay (CONTEXT D-08 lock — iter-1 fix Blocker #1; iter-0 SKIP pattern REVERTED)**
4. `Views/CMP/Results.cshtml`: pending mode rendering (banner "Hasil sementara", badge "MENUNGGU PENILAIAN", card-header bg-secondary, motivasi block info, cert button HIDDEN saat pending) + **Question Review section: Essay items label "Menunggu Penilaian" via `q.IsEssayPending` conditional (iter-1 D-08 fix)**
5. `Views/Shared/_Layout.cshtml`: TempData["Info"] alert block dengan icon `bi-info-circle-fill` (antara Error block dan Success block)

**Decision yang ditutup planner (REVISED iter-1):**
- ViewModel field `IsPendingGrading` dipilih (typesafe vs ViewBag)
- **Essay tampil via flag IsEssayPending (REVISED — option A; iter-0 option B SKIP REVERTED per CONTEXT D-08 lock)**
- LogInformation di branch pending omitted per CONTEXT D-10
- **GradingService refactor SPLIT ke Plan 309-03 Wave 2 sequential** (iter-1 scope_sanity fix — kurangi task count Plan 309-02 dari 6 ke 5)

Compile sukses (dotnet build 0 error, warning ≤ 92).
  </what-built>
  <how-to-verify>
**SETUP:**

1. App running di local: `dotnet run`. Login sebagai worker yang punya akses ke assessment ber-essay.
2. Punya akses query SQL Server (SSMS atau Visual Studio SQL editor) untuk verifikasi DB state.
3. Optional tapi useful: open browser DevTools (F12) → tab Network untuk inspeksi response HTTP.

**STEP 1 — Worker submit assessment ber-essay → status berubah ke "Menunggu Penilaian":**

1. Buat atau pilih assessment session ber-essay (mix MC + Essay) yang Open. Worker login → submit assessment (jawab semua soal termasuk essay).
2. Setelah submit, query DB:
   ```sql
   SELECT Id, Status, Score, IsPassed, HasManualGrading, Progress
   FROM AssessmentSessions
   WHERE Id = <SessionId>
   ```
3. **Expected:** `Status = 'Menunggu Penilaian'`, `IsPassed = NULL`, `HasManualGrading = 1`, `Progress = 100`, `Score = <interim_pct_dari_MC_MA>`.
4. **Acceptance Step 1:** GradingService set status correctly via existing literal (Plan 309-03 akan refactor literal ke constant — out-of-scope di Plan 309-02 setelah iter-1 split).

**STEP 2 — Worker klik "Lihat Sertifikat" pada session pending → expect banner BIRU info, BUKAN popup merah:**

1. Worker tetap login → navigate ke `/CMP/Results/<SessionId>` (atau via Riwayat Assessment).
2. Di halaman Results, klik tombol "Lihat Sertifikat" — atau (jika tombol HIDDEN per Step 4 expectation), navigate manual ke URL `/CMP/Certificate/<SessionId>`.
3. **Expected:**
   - Worker DI-REDIRECT ke `/CMP/Results/<SessionId>` (URL berubah)
   - Banner BIRU `alert-info` muncul di atas page: "**Info:** Sertifikat akan tersedia setelah penilaian essay selesai."
   - **TIDAK ADA** popup merah `alert-danger` "Error: Assessment belum selesai." atau "Error: Assessment not completed yet."
4. **Acceptance Step 2:** Critical SUB-01 SC #11 verified — friendly Info-banner alih-alih popup merah error.

**STEP 3 — Worker klik "Download PDF" pada session pending → expect redirect Info, bukan crash:**

1. Navigate manual ke URL `/CMP/CertificatePdf/<SessionId>` (worker biasanya tidak punya tombol explicit untuk PDF di pending mode, tapi URL handler exist).
2. **Expected:**
   - Worker DI-REDIRECT ke `/CMP/Results/<SessionId>` dengan banner BIRU "Info: Sertifikat akan tersedia setelah penilaian essay selesai."
   - **TIDAK ADA** download PDF, **TIDAK ADA** error 500, **TIDAK ADA** popup merah error.
3. **Acceptance Step 3:** PendingGrading branch di CertificatePdf method bekerja sama seperti Certificate method.

**STEP 4 — Worker view Results saat pending → render mode "hasil sementara" + Essay review label "Menunggu Penilaian" (iter-1 D-08 verify):**

1. Refresh page `/CMP/Results/<SessionId>`. Verify visual:
   - Banner alert-info di atas: "**Hasil sementara** — Essay menunggu penilaian HC. Skor & sertifikat akan diperbarui setelah penilaian selesai."
   - Card-header (banner panel atas card) berwarna ABU-ABU (`bg-secondary`), bukan hijau (`bg-success`) atau merah (`bg-danger`)
   - Score% tetap muncul (interim score MC+MA)
   - Status badge: warna ABU-ABU dengan icon hourglass (`bi-hourglass-split`) dan teks "**MENUNGGU PENILAIAN**"
   - Motivasi block (di bawah card): banner alert-info dengan teks "Skor di atas adalah **nilai sementara** dari soal Pilihan Tunggal & Pilihan Jamak. Skor final & sertifikat tersedia setelah penilaian Essay selesai."
   - Tombol "Lihat Sertifikat" **TIDAK MUNCUL** (HIDDEN). Hanya tombol "Kembali" yang muncul.
   - **Tinjauan Jawaban section (jika `AllowAnswerReview=true`) iter-1 D-08 verify:**
     - **Essay items TETAP MUNCUL** di list (TIDAK skipped seperti iter-0 — iter-1 fix Blocker #1)
     - Essay items render dengan badge ABU-ABU "Menunggu Penilaian" (text-bg-secondary + icon hourglass) — BUKAN badge correct/incorrect
     - MC/MA items tetap render badge HIJAU "Benar" atau MERAH "Salah" normal (regression-safe)
     - Question text Essay TETAP terlihat (worker bisa baca soal yang dijawab)
2. **Acceptance Step 4:** Full Results pending mode rendering verified per CONTEXT D-08; Essay items TETAP visible dengan label pending.

**STEP 5 — HC finalize essay grading → worker view Certificate normal (regression):**

1. Login sebagai HC user. Navigate ke admin panel essay grading (assumed Phase 310 belum aktif, jadi mungkin perlu manual SQL update untuk simulate finalize):
   ```sql
   -- Manual finalize (simulate Phase 310 FinalizeEssayGrading effect):
   UPDATE AssessmentSessions
   SET Status = 'Completed',
       Score = <final_score>,  -- e.g., 85
       IsPassed = 1,             -- true
       HasManualGrading = 0
   WHERE Id = <SessionId>;
   ```
2. Login balik sebagai worker → navigate `/CMP/Results/<SessionId>` → klik "Lihat Sertifikat".
3. **Expected:**
   - Halaman Certificate render normal (tidak redirect, tidak banner Info, tidak error)
   - Recipient name muncul, certificate styling normal
   - Card-header di Results berwarna HIJAU (`bg-success`) dengan badge "LULUS"
   - Banner "Hasil sementara" TIDAK MUNCUL
   - Tombol "Lihat Sertifikat" MUNCUL (visible)
   - **Tinjauan Jawaban: Essay items render correct/incorrect badge normal** (post-finalize, IsEssayPending=false untuk semua items)
4. **Acceptance Step 5:** Regression-free — Completed flow IDENTICAL dengan pre-Phase-309 behavior; Essay items review post-finalize render normal.

**STEP 6 — Verify TempData["Info"] di _Layout render correctly (visual smoke test):**

1. Trigger ulang Step 2 (Worker pending → klik Sertifikat).
2. **Expected visual:**
   - Banner muncul di area NAVBAR bagian bawah (sesuai existing Warning/Error/Success placement di `_Layout.cshtml` line 188-218)
   - Warna BIRU MUDA (`alert-info` Bootstrap 5.3 — biasanya `#cff4fc` background dengan border `#9eeaf9`)
   - Icon ⓘ (info-circle-fill, biru tua)
   - Bold prefix "**Info:**" diikuti pesan "Sertifikat akan tersedia..."
   - Tombol close (×) di kanan dapat di-klik untuk dismiss banner
3. Open browser DevTools (F12) → Elements → Inspect banner element → verify class = `alert alert-info alert-dismissible fade show`.
4. **Acceptance Step 6:** TempData["Info"] alert block di Layout fully functional.

**Resume signal:** Type "approved" jika SEMUA 6 step PASS. Jika ada step fail, deskripsikan: step mana, expected vs actual, screenshot opsional, console log relevan.
  </how-to-verify>
  <resume-signal>Type "approved" atau describe issues per Step (Step 1-6 PASS atau FAIL + detail)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CMPController.Certificate / CertificatePdf / Results (HTTP GET) | Worker route param `id` typed `int`, class-level `[Authorize]` + body owner+role check (preserved from Plan 309-01 — no security regression) |
| CMPController → AssessmentConstants.IsAssessmentSubmitted | Pure function call (compile-time constant compare) — no DB access, no side effect |
| CMPController → TempData["Info"] | New TempData key — boundary: layout MUST render the key OR worker silently miss the message (mitigation: Task 3 layout add) |
| Razor view → Model.IsPendingGrading + q.IsEssayPending | ViewModel fields — boundary: controller MUST project flag correctly (Task 2 ensures both package + legacy path + Essay items projection) OR view falls to `else` branch (default false → render existing LULUS/TIDAK LULUS as if not pending — incorrect UX) |
| Layout TempData rendering → User browser | Output rendering: text via `@TempData["Info"]` — boundary: Razor auto-encodes (no XSS surface from TempData value); BUT controller MUST not put HTML/script in TempData (verify: only static literal strings used in Plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-309-04 | Information Disclosure | TempData["Info"] / TempData["Error"] in `_Layout.cshtml` rendering | mitigate | All TempData values in Plan 309-02 are static literal Bahasa Indonesia copy ("Sertifikat akan tersedia setelah penilaian essay selesai.", "Assessment belum selesai." — iter-1 BI compliance fix). NO `ex.Message`, NO DB error detail, NO PII (e.g., user email, NIP). Razor auto-HTML-encodes via `@TempData["Info"]` syntax — XSS surface from TempData value zero. ASVS V7-3.1 satisfied. |
| T-309-05 | Tampering | Status state machine `Open/Upcoming/Completed/PendingGrading` — `IsAssessmentSubmitted` helper returns true for both Completed AND PendingGrading | accept | Helper expands access from `Completed`-only to `Completed|PendingGrading`. Worker dengan PendingGrading masih SUDAH submit (per GradingService L195-204 — assessment.UserId verified, response data persisted, Progress=100). Granting access to view interim Results is SAFE per CONTEXT D-08 (worker-owned data). NO escalation surface — IsPendingGrading flag ONLY controls UI rendering (not access decision; access still gated by `assessment.UserId == user.Id || isAdmin/HC`). |
| T-309-06 | Tampering | `IsPendingGrading` + `IsEssayPending` ViewModel fields default `false` — orphan path (e.g., legacy ViewModel construction missing assignment) | mitigate | Default `= false` in field declaration (both fields). Task 2 explicitly sets `IsPendingGrading` in BOTH package path AND legacy path ViewModel construction. `IsEssayPending` set di setiap `new QuestionReviewItem { ... }` di questionReviews loop — default false untuk MC/MA items dan untuk Essay items saat Status != PendingGrading. Future ViewModel construction (Phase X) yang omit fields defaults to false → render normal — graceful fallback (BUKAN crash). Acceptance criteria verifies field projection via grep. |
| T-309-07 | Repudiation | PendingGrading branch — no audit log entry per CONTEXT D-10 | accept | CONTEXT D-10 explicit decision: Certificate page existing TIDAK audit success path; consistency preserved. Forensics for pending flow optional via `_logger.LogInformation` (omitted per CONTEXT D-10 lock — log noise). Future monitoring: re-add via config flag if audit team requests. |
</threat_model>

<verification>
**Build & Compile:**
- `dotnet build` exits 0; warning count ≤ 92 (Phase 308 baseline maintained)

**Code Pattern Verification (grep checks across 5 files — Task 5 GradingService SPLIT ke Plan 309-03):**
- `AssessmentConstants.cs`: contains `PendingGrading = "Menunggu Penilaian"` constant + `IsAssessmentSubmitted(string? status) =>` helper
- `AssessmentResultsViewModel.cs`: contains `public bool IsPendingGrading { get; set; } = false;` field di main class + `public bool IsEssayPending { get; set; } = false;` field di nested QuestionReviewItem (NEW iter-1 D-08 fix)
- `CMPController.cs`:
  - 3 lokasi `!AssessmentConstants.IsAssessmentSubmitted(assessment.Status)` swap
  - 3 lokasi `TempData["Error"] = "Assessment belum selesai.";` (Bahasa Indonesia per CLAUDE.md — iter-1 fix Warning #1)
  - 0 lokasi `TempData["Error"] = "Assessment not completed yet.";` di 3 target lokasi (English literal eliminated)
  - 2 lokasi `TempData["Info"] = "Sertifikat akan tersedia setelah penilaian essay selesai.";`
  - 2 lokasi `IsPendingGrading = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading)` projection
  - 1 lokasi `IsEssayPending = (assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading && (question.QuestionType ?? "MultipleChoice") == "Essay")` projection (NEW iter-1 D-08 fix)
- `Results.cshtml`: contains `Hasil sementara`, `MENUNGGU PENILAIAN`, `Menunggu Penilaian` (≥ 2 hits — status badge + Question Review badge), `bi-hourglass-split` (≥ 3 hits), `bg-secondary` (tri-state header), `!Model.IsPendingGrading && Model.IsPassed && Model.GenerateCertificate` (cert button hide guard), `IsEssayPending` (Question Review conditional — NEW iter-1 D-08 fix)
- `_Layout.cshtml`: contains `@if (TempData["Info"] != null)` block dengan `alert-info` + `bi-info-circle-fill` + `<strong>Info:</strong>`

**Manual UAT (Task 5 checkpoint, 6-step):**
- Step 1: Worker submit essay → DB status `Menunggu Penilaian` confirmed (regression GradingService — refactor terjadi di Plan 309-03)
- Step 2: Worker klik Sertifikat → banner BIRU "Sertifikat akan tersedia..." (BUKAN popup merah) — SC #11 critical
- Step 3: Worker hit `/CMP/CertificatePdf/{id}` saat pending → redirect Info banner (no PDF, no 500)
- Step 4: Worker view Results saat pending → banner "Hasil sementara" + badge "MENUNGGU PENILAIAN" + cert button HIDDEN + **Essay items TETAP tampil dengan label "Menunggu Penilaian"** (iter-1 D-08 verify)
- Step 5: HC finalize → worker view Certificate normal (regression-free Completed flow + Essay items render correct/incorrect normal)
- Step 6: Visual smoke test TempData["Info"] alert in Layout (alert-info, info-circle-fill, dismiss button)
</verification>

<success_criteria>
**Mapped to ROADMAP §Phase 309 SC #8-#11 (SUB-01 portion):**

- [x] SC #8: Helper `IsAssessmentSubmitted(string? status)` di `AssessmentConstants.cs` returns true untuk Completed ATAU Menunggu Penilaian (Task 1)
- [x] SC #9: 3 lokasi cek di `CMPController.cs` (line 1792, 1858, 2105) ganti dari `assessment.Status != "Completed"` menjadi `!IsAssessmentSubmitted(assessment.Status)` dengan TempData["Error"] copy Bahasa Indonesia (Task 2 — iter-1 fix Warning #1)
- [x] SC #10: Branch khusus `Menunggu Penilaian` di `Certificate()` & `CertificatePdf()` → `TempData["Info"]` "Sertifikat akan tersedia setelah penilaian essay selesai." dan `Results()` render mode hasil sementara (Task 2 + Task 4); **Essay items TETAP tampil di review section dengan label "Menunggu Penilaian" per CONTEXT D-08 (iter-1 fix Blocker #1)**
- [x] SC #11: Worker submit assessment ber-essay tidak menerima popup merah `Error: Assessment belum selesai.` di alur manapun (Task 5 manual UAT Step 2 + Step 3)

**Iter-1 Revision Compliance:**
- [x] Blocker #1 fixed: Essay items TETAP di questionReviews list dengan field `IsEssayPending` baru (CONTEXT D-08 lock honored)
- [x] Warning #1 fixed: TempData["Error"] copy Bahasa Indonesia "Assessment belum selesai." (CLAUDE.md compliance)
- [x] Warning #2 fixed: Task 5 GradingService refactor SPLIT ke Plan 309-03 Wave 2 sequential (scope_sanity 5 task ≤ threshold; sebelumnya 6)

**Bonus (deferred ke Plan 309-03 per iter-1 split):**
- [ ] `Services/GradingService.cs` line 196, 199 menggunakan constant `AssessmentConstants.AssessmentStatus.PendingGrading` (Plan 309-03 — depends_on=[309-02])
</success_criteria>

<output>
After completion, create `.planning/phases/309-worker-cert-defensive-submitted-status/309-02-SUMMARY.md` capturing:
- 4 decisions ditutup planner (REVISED iter-1): ViewModel field (vs ViewBag), Essay tampil dengan IsEssayPending flag (option A — REVISED dari iter-0 option B SKIP), Plan 309-03 split untuk GradingService refactor, Omit LogInformation di branch pending
- Compile result + warning count
- Manual UAT 6-step result (Step 1-6 PASS atau FAIL detail)
- Files changed: 5 file (Task 5 GradingService SPLIT ke Plan 309-03)
- Commits: 4 (Task 1 + Task 2 + Task 3 + Task 4 — Task 5 manual UAT no-commit)
- Phase 309 progress: 2/3 plan complete; Plan 309-03 ready (Wave 2 sequential)
- Phase 310 dependency: `AssessmentConstants.AssessmentStatus.PendingGrading` constant available untuk reuse di Phase 310 FinalizeEssayGrading flow
</output>
