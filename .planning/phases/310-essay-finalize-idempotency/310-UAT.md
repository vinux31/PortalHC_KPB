# Manual UAT — Phase 310 Essay Finalize Idempotency

**REQ:** ESCG-01
**Phase:** 310-essay-finalize-idempotency
**Bahasa:** Bahasa Indonesia (mandatory user-facing)
**Created:** 2026-05-02
**Walkthrough:** 2026-05-05 (Path A — Hybrid Playwright MCP)
**Status:** Partial — 4 dari 7 criteria PASS via Playwright MCP walkthrough; 3 DEFERRED butuh seed PendingGrading lifecycle

## Walkthrough Path A — Coverage Matrix

Eksekusi UAT dilakukan pada Dev DB lokal (`localhost\SQLEXPRESS / HcPortalDB_Dev`) dengan app running di `http://localhost:5277` (`dotnet run`, ASPNETCORE_ENVIRONMENT=Development). Login: `admin@pertamina.com` / `123456`.

Path A dipilih karena seed DB pada saat walkthrough (2026-05-05) hanya punya 2 session ber-essay (`HasManualGrading=true`): Id 118 (UAT v14 Standard OJT) dan Id 65 (ojt v1.9 OJT). Keduanya `Status=Completed` dengan `NomorSertifikat=NULL`. Tidak ada session `Status=PendingGrading` siap finalize untuk full-flow test SC #3/4/5.

| Step | SC | Method | Result |
|------|----|--------|--------|
| 1 | SC #1 alreadyFinalized (D-03) | Klik tombol enabled session 118 → alert-info biru | **PASS** |
| 2 | SC #2 visual disabled + tooltip (D-02) | SQL temp inject `NomorSertifikat='TEST-CERT-310-UAT'` Id=118 → reload → snapshot → revert | **PASS** |
| 3 | SC #3 notif dedup (D-05) | Need real finalize lifecycle dengan PendingGrading session | **DEFERRED** |
| 4 | SC #4 audit dedup (D-07) | Same | **DEFERRED** |
| 5 | SC #5 concurrent parallel (D-06) | Need PendingGrading + dual-tab race window | **DEFERRED** |
| 6a | D-04 Open status BI literal | `browser_evaluate` fetch sessionId=97 (Status=Open) | **PASS** |
| 6b | D-04 Cancelled status BI literal | `browser_evaluate` fetch sessionId=22 (Status=Cancelled) | **PASS** |

## Step 1 — SC #1 (D-03 friendly no-op): alert-info biru ✅ PASS

**Setup:** Session Id 118, `Status=Completed`, `NomorSertifikat=NULL`. URL composite query string:

```
http://localhost:5277/Admin/AssessmentMonitoringDetail?title=UAT+v14+Standard&category=OJT&scheduleDate=2026-04-10
```

**Pre-condition rendered:** `#finalizeSection_118` berisi tombol ENABLED (`isFinalized=false` karena tidak ada cert), `display:block` (EssayPendingCount=0).

**Action:** Klik tombol "Selesaikan Penilaian" (confirm dialog auto-accepted via `window.confirm = () => true`).

**Observed result (DOM snapshot setelah click):**

```yaml
- alert [ref=e200]:
    - strong [ref=e202]: "Info:"
    - text: Penilaian sudah diselesaikan sebelumnya pada 08 Apr 2026 08:55 WIB
    - button "Close"
- button " Selesaikan Penilaian" [disabled]
```

**Verifikasi tepat:**
- ✓ Alert `role="alert"` rendered di top of `.container-fluid`
- ✓ `<strong>Info:</strong>` prefix per UI-SPEC contract
- ✓ Pesan literal Bahasa Indonesia: `Penilaian sudah diselesaikan sebelumnya pada 08 Apr 2026 08:55 WIB`
- ✓ Format tanggal `dd MMM yyyy HH:mm WIB` (Phase 304 convention)
- ✓ Button `disabled=true` setelah response (tidak reload — state sudah final)
- ✓ Auto-dismiss 5s (per UI-SPEC; visual confirmed alert hilang dari container saat polling 5s+)

**Backend trail:** controller branch `if (session.Status == AssessmentConstants.AssessmentStatus.Completed)` di `AssessmentAdminController.cs:2732` returns `{ success: true, alreadyFinalized: true, message, score, isPassed, nomorSertifikat }` — frontend JS branch `data.success && data.alreadyFinalized` invokes `showAlert('info', 'bi-info-circle-fill', '<strong>Info:</strong> ' + msg)` + `this.disabled = true`.

**Result:** ✅ **PASS** — verified `2026-05-05` via Playwright MCP

## Step 2 — SC #2 (D-02 UI gate): disabled span tooltip ✅ PASS

**Setup:** SQL inject sementara untuk simulate finalized state:

```sql
SET QUOTED_IDENTIFIER ON;
UPDATE AssessmentSessions SET NomorSertifikat='TEST-CERT-310-UAT' WHERE Id=118;
```

**Action:** Reload halaman session 118.

**Observed DOM (`#finalizeSection_118`):**

```html
<span data-bs-toggle="tooltip" style="display:inline-block;"
      data-bs-original-title="Sudah selesai pada 08 Apr 2026 08:55 WIB">
    <button class="btn btn-success btn-finalize-grading"
            data-session-id="118" disabled style="pointer-events: none;">
        <i class="bi bi-check-circle me-1"></i>Selesaikan Penilaian
    </button>
</span>
```

**Verifikasi tepat:**
- ✓ `<span data-bs-toggle="tooltip">` wrapper present (Pitfall #6 mitigation untuk disabled mouseenter)
- ✓ Bootstrap tooltip activated: `title` → `data-bs-original-title` (DOMContentLoaded snippet pada Edit D di-fire)
- ✓ Tooltip text Bahasa Indonesia: `Sudah selesai pada 08 Apr 2026 08:55 WIB`
- ✓ `style="display:inline-block;"` pada span (Bootstrap requirement untuk disabled button tooltip)
- ✓ Button `disabled=true`
- ✓ Computed `pointer-events: none`
- ✓ Computed `opacity: 0.65` (Bootstrap default disabled state, greyed out)
- ✓ Cursor `not-allowed` (browser default untuk disabled button)

**Razor branch:** `isFinalized = (Status==Completed && !string.IsNullOrEmpty(NomorSertifikat))` LOCKED dual-criterion D-02 evaluated true → renders `if` branch dengan span+tooltip+disabled.

**Cleanup:** SQL revert `UPDATE AssessmentSessions SET NomorSertifikat=NULL WHERE Id=118` — verified post-revert: `(Id=118, Status=Completed, NomorSertifikat=NULL)`.

**Result:** ✅ **PASS** — verified `2026-05-05` via Playwright MCP

## Step 3 — SC #3 (notif dedup) — DEFERRED

**Reason:** Butuh real `PendingGrading → Completed` lifecycle dengan klik finalize 2x sequential pada session yang punya AssessmentEssaySchemaUserId distinct recipients. Saat walkthrough seed DB tidak punya session `Status=PendingGrading`. Untuk seed manual butuh: pilih existing Completed session, un-grade essay (set `EssayScore=NULL`), set `Status=PendingGrading`, ulangi grading flow sampai `EssayPendingCount=0`. Skip pada walkthrough ini.

**Code-side coverage:** Plan 01 Task 1 implementasi `NotifyIfGroupCompleted` dedup via `UserNotifications.AnyAsync` lookup di `AssessmentAdminController.cs` — pattern equivalent ke Phase 309-03 `GradingService.cs` L195-212 (canonical). Build pass + grep verification ≥ 1 untuk D-05 acceptance.

**SQL verify query (saat resume seed):**
```sql
SELECT UserId, COUNT(*) AS NotifCount
FROM UserNotifications
WHERE Type = 'ASMT_ALL_COMPLETED' AND Title = 'Assessment Selesai'
  AND Message LIKE '%' + (SELECT Title FROM AssessmentSessions WHERE Id = {sessionId}) + '%'
  AND CreatedAt >= (SELECT CAST(Schedule AS DATE) FROM AssessmentSessions WHERE Id = {sessionId})
GROUP BY UserId;
-- Expected: setiap UserId punya NotifCount = 1
```

**Result:** 🟡 **DEFERRED** — phase 310 UAT debt; verify saat next real finalize lifecycle

## Step 4 — SC #4 (audit dedup) — DEFERRED

**Reason:** Same — butuh real finalize lifecycle untuk verify `AuditLog WHERE Action='FinalizeEssayGrading'` count = 1.

**Code-side coverage:** Plan 01 Task 1 audit gated by `rowsAffected > 0` (D-07 implementation) — race-lost thread tidak append audit. Pattern adopt dari Phase 309-03.

**SQL verify query:**
```sql
SELECT COUNT(*) AS AuditCount
FROM AuditLogs
WHERE Action = 'FinalizeEssayGrading' AND TargetId = {sessionId} AND TargetType = 'AssessmentSession';
-- Expected: 1
```

**Result:** 🟡 **DEFERRED** — phase 310 UAT debt

## Step 5 — SC #5 (concurrent parallel) — DEFERRED

**Reason:** Butuh PendingGrading session baru + dual-tab simultaneous click untuk race-window induction. Skip pada walkthrough ini.

**Code-side coverage:** Plan 01 `FinalizeEssayGrading` capture `rowsAffected` dari `ExecuteUpdateAsync` (D-06) — winner-thread punya rowsAffected=1, loser-thread rowsAffected=0 → loser falls through ke alreadyFinalized branch dengan friendly response. WHERE-clause guard pattern proven via Phase 309-03 GradingService.

**Result:** 🟡 **DEFERRED** — phase 310 UAT debt

## Step 6 — D-04 status branching: Per-status BI message ✅ PASS

### 6a — Status=Open

**Action:**
```js
fetch('/Admin/FinalizeEssayGrading', {
  method: 'POST',
  headers: {'Content-Type': 'application/x-www-form-urlencoded', 'X-Requested-With': 'XMLHttpRequest'},
  body: 'sessionId=97&__RequestVerificationToken=' + encodeURIComponent(token)
})
```

**Response (HTTP 200):**
```json
{ "success": false, "message": "Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian." }
```

**Verifikasi:**
- ✓ Status code 200 (controller returns Json, bukan exception)
- ✓ `success: false`
- ✓ Pesan literal Bahasa Indonesia exact match dengan switch arm `AssessmentStatus.Open => "Belum bisa di-finalize. Peserta belum mulai mengerjakan ujian."` di `AssessmentAdminController.cs:2748`

### 6b — Status=Cancelled

**Action:** same fetch, `sessionId=22`.

**Response (HTTP 200):**
```json
{ "success": false, "message": "Tidak bisa di-finalize. Session sudah dibatalkan." }
```

**Verifikasi:**
- ✓ Pesan literal exact match `"Cancelled" => "Tidak bisa di-finalize. Session sudah dibatalkan."`

**Frontend behavior implied (verified separately via Razor `else` branch + JS `else` branch):**
- `data.success === false` → JS branch `showAlert('danger', 'bi-x-circle-fill', '<strong>Error:</strong> ' + data.message)` + `this.disabled = false`

**Result:** ✅ **PASS** untuk Open + Cancelled — verified `2026-05-05`. InProgress + fallback DEFERRED ke real lifecycle (no live InProgress essay-having session di seed DB).

## Sign-Off

| Criterion | Verifier | Date | Result |
|-----------|----------|------|--------|
| SC #1 friendly no-op | Claude (Playwright MCP) | 2026-05-05 | ✅ PASS |
| SC #2 disabled + tooltip | Claude (Playwright MCP) | 2026-05-05 | ✅ PASS |
| SC #3 notif dedup | — | — | 🟡 DEFERRED (UAT debt) |
| SC #4 audit dedup | — | — | 🟡 DEFERRED (UAT debt) |
| SC #5 parallel finalize | — | — | 🟡 DEFERRED (UAT debt) |
| D-04 status branching (Open) | Claude (Playwright MCP) | 2026-05-05 | ✅ PASS |
| D-04 status branching (Cancelled) | Claude (Playwright MCP) | 2026-05-05 | ✅ PASS |

**Overall:** 🟡 **PARTIAL PASS** — 4 dari 7 testable criteria PASS via Path A walkthrough; 3 DEFERRED butuh seed `Status=PendingGrading` session untuk real finalize lifecycle. Code-side coverage SC #3/4/5 ter-validated secara struktural via grep acceptance (Plan 01 build + 22+ pattern matches) + pattern parity dengan canonical Phase 309-03 GradingService.

**Phase closure recommendation:** APPROVE dengan deferred items dicatat sebagai phase-310 UAT debt. Trigger verify saat next real `PendingGrading → Completed` finalize lifecycle terjadi pada production data.

---
*UAT script: 310-UAT.md — Phase 310 Essay Finalize Idempotency*
*Created: 2026-05-02 | Walkthrough: 2026-05-05 (Path A Hybrid Playwright MCP)*
