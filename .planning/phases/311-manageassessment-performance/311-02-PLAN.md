---
phase: 311-manageassessment-performance
plan: 02
type: execute
wave: 1
depends_on: [311-01]
files_modified:
  - Data/ApplicationDbContext.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - Controllers/AssessmentAdminController.cs
  - tests/e2e/assessment.spec.ts
  - .planning/phases/311-manageassessment-performance/311-UAT.md
autonomous: false
requirements: [PERF-01]
tags: [perf, ef-core, asnotracking, memorycache, migration, index]
must_haves:
  truths:
    - "DB migration AddManageAssessmentPerfIndexes adds 2 indexes: IX_AssessmentSessions_ExamWindowCloseDate + IX_AssessmentSessions_LinkedGroupId (D-05, D-06, D-07, ROADMAP SC #4)"
    - "Migration applied ke Dev DB tanpa error, indexes verified via SSMS sys.indexes query (ROADMAP SC #4)"
    - "managementQuery L66 punya .AsNoTracking() di chain start (D-08, ROADMAP SC #2)"
    - ".Include(a => a.User) L88 dihapus, projection L106-108 null-guard preserved (D-09, ROADMAP SC #3)"
    - "Distinct Categories query L172-176 di-wrap dengan _cache.GetOrCreateAsync(\"assessment_categories_distinct\", ...) TTL 5 menit (D-01, D-02, D-04, ROADMAP SC #5)"
    - "AddCategory L320, EditCategory L372, DeleteCategory L401 punya _cache.Remove(\"assessment_categories_distinct\") setelah SaveChangesAsync (D-03)"
    - "Build berhasil 0 errors, warnings <= 92 (Phase 309 baseline preserved)"
    - "Post-patch p95 <= baseline_p95 * 0.7 (>=30% improvement) — ROADMAP SC #6"
    - "Smoke test 3 tab (assessment, training, history) page load 200 OK + grouping count identik pre/post — ROADMAP SC #7"
    - "Cache invalidation working: AddCategory/EditCategory/DeleteCategory langsung refresh dropdown tanpa wait 5 menit (D-03)"
  artifacts:
    - path: "Data/ApplicationDbContext.cs"
      provides: "2 new HasIndex declarations untuk ExamWindowCloseDate + LinkedGroupId di AssessmentSession entity config"
      contains: "entity.HasIndex(a => a.ExamWindowCloseDate)"
    - path: "Migrations/{ts}_AddManageAssessmentPerfIndexes.cs"
      provides: "EF Core migration dengan 2 CreateIndex calls (Up) + 2 DropIndex calls (Down)"
      contains: "IX_AssessmentSessions_ExamWindowCloseDate"
    - path: "Migrations/ApplicationDbContextModelSnapshot.cs"
      provides: "Auto-updated snapshot dengan 2 baris baru b.HasIndex(...) untuk AssessmentSessions"
      contains: 'b.HasIndex("LinkedGroupId")'
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "AsNoTracking + Include removal + GetOrCreateAsync pattern + 3x cache.Remove di Category CRUD"
      contains: "assessment_categories_distinct"
    - path: "tests/e2e/assessment.spec.ts"
      provides: "Phase 311 smoke parity test 3 tab (test 1.0)"
      contains: "Phase 311"
    - path: ".planning/phases/311-manageassessment-performance/311-UAT.md"
      provides: "Manual UAT scaffold: post-patch baseline rerun + 3-tab smoke + cache invalidation behavior verification"
      contains: "Cache Behavior Verification"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (action ManageAssessment L172)"
      to: "IMemoryCache _cache (DI L22)"
      via: "_cache.GetOrCreateAsync(\"assessment_categories_distinct\", async entry => { entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5); ... })"
      pattern: 'GetOrCreateAsync\("assessment_categories_distinct"'
    - from: "Controllers/AssessmentAdminController.cs (AddCategory L320, EditCategory L372, DeleteCategory L401)"
      to: "IMemoryCache _cache invalidation"
      via: "_cache.Remove(\"assessment_categories_distinct\") setelah SaveChangesAsync, sebelum RedirectToAction"
      pattern: '_cache\.Remove\("assessment_categories_distinct"\)'
    - from: "Migrations/{ts}_AddManageAssessmentPerfIndexes.cs"
      to: "SQL Server AssessmentSessions table"
      via: "dotnet ef database update — emit CREATE INDEX IX_AssessmentSessions_ExamWindowCloseDate + IX_AssessmentSessions_LinkedGroupId"
      pattern: 'CreateIndex\(\s*name: "IX_AssessmentSessions_(ExamWindowCloseDate|LinkedGroupId)"'
    - from: "managementQuery L66 chain"
      to: "EF Core change tracker (disabled)"
      via: ".AsNoTracking() insertion sebelum .Where()"
      pattern: '_context\.AssessmentSessions\s*\n\s*\.AsNoTracking\(\)'
---

<objective>
Apply 4 patches optimasi (DB indexes, AsNoTracking, Include removal, Categories cache + invalidation) ke `Controllers/AssessmentAdminController.cs` action `ManageAssessment` untuk capai ROADMAP SC #6 >=30% p95 improvement, lalu re-measure post-patch dengan dataset SAMA seperti baseline (Pitfall 9 mitigation), validate parity 3 tab (SC #7), dan tutup phase dengan SUMMARY.md.

**Purpose:** Implementasi Skenario A scope per D-16 — assumes Plan 01 baseline confirmed T1 dominan >60% total. Setiap patch single-line atau few-line insertion ke file existing dengan pattern yang sudah established (PATTERNS.md "Implication untuk planner: Phase 311 sangat narrow scope. Tidak ada arsitektur baru, tidak ada helper class baru."). Wave 1 = code changes; Wave 2 = measurement + smoke + UAT + SUMMARY.

**Output:**
- 1 EF Core migration baru (AddManageAssessmentPerfIndexes) applied ke Dev DB
- Modified `Controllers/AssessmentAdminController.cs`: `.AsNoTracking()` di L66, `.Include(a => a.User)` removed di L88, `GetOrCreateAsync` di L172-176, `_cache.Remove(...)` di 3 Category CRUD actions
- Modified `Data/ApplicationDbContext.cs`: 2 HasIndex baru di blok `AssessmentSession` config
- Auto-updated `Migrations/ApplicationDbContextModelSnapshot.cs`
- Extended `tests/e2e/assessment.spec.ts` dengan smoke parity test 3 tab
- New `311-UAT.md` manual UAT scaffold + executed UAT pass/fail
- New `311-02-SUMMARY.md` dengan baseline vs post-patch numbers + improvement % computation + UAT result

**Skenario A precondition:** Plan 01 Task 4 commit `Skenario A approved` already landed di git history. Kalau Skenario B/C, orchestrator pause sebelum Plan 02 execute — re-discuss D-14 scope dulu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/311-manageassessment-performance/311-CONTEXT.md
@.planning/phases/311-manageassessment-performance/311-RESEARCH.md
@.planning/phases/311-manageassessment-performance/311-PATTERNS.md
@.planning/phases/311-manageassessment-performance/311-VALIDATION.md
@.planning/phases/311-manageassessment-performance/311-BASELINE.md
@Controllers/AssessmentAdminController.cs
@Data/ApplicationDbContext.cs
@Migrations/ApplicationDbContextModelSnapshot.cs
@tests/e2e/assessment.spec.ts

<interfaces>
<!-- Verbatim code excerpts from PATTERNS.md — copy directly, no codebase exploration needed. -->

**Pattern A — AsNoTracking insertion at L66 (D-08):**

BEFORE (current state per Plan 01 Task 1, with swT1 wrap):
```csharp
var swT1 = System.Diagnostics.Stopwatch.StartNew();
var managementQuery = _context.AssessmentSessions
    .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)
    .AsQueryable();
```

AFTER (D-08 patch):
```csharp
var swT1 = System.Diagnostics.Stopwatch.StartNew();
var managementQuery = _context.AssessmentSessions
    .AsNoTracking()                                              // NEW (D-08)
    .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)
    .AsQueryable();
```

**Pattern B — Remove `.Include(a => a.User)` at L88 (D-09):**

BEFORE:
```csharp
var allSessions = await managementQuery
    .Include(a => a.User)                                        // REMOVE
    .OrderByDescending(a => a.Schedule)
    .Select(a => new
    {
        // ... 13 fields ...
        UserFullName = a.User != null ? a.User.FullName : "Unknown",   // KEEP null guard
        UserEmail = a.User != null ? a.User.Email : "",
        UserId = a.User != null ? a.User.Id : ""
    })
    .ToListAsync();
```

AFTER (D-09 patch — Include removed, projection unchanged):
```csharp
var allSessions = await managementQuery
    .OrderByDescending(a => a.Schedule)
    .Select(a => new
    {
        // ... unchanged 16-field projection (KEEP null guards exactly as-is) ...
    })
    .ToListAsync();
```

**Pattern C — IMemoryCache.GetOrCreateAsync at L172-176 (D-01, D-02, D-04):**

BEFORE (current state with swT5 wrap from Plan 01):
```csharp
var swT5 = System.Diagnostics.Stopwatch.StartNew();
ViewBag.Categories = await _context.AssessmentSessions
    .Select(a => a.Category)
    .Distinct()
    .OrderBy(c => c)
    .ToListAsync();
swT5.Stop();
t5 = swT5.ElapsedMilliseconds;
```

AFTER (D-01 + D-02 + D-04 patch):
```csharp
var swT5 = System.Diagnostics.Stopwatch.StartNew();
ViewBag.Categories = await _cache.GetOrCreateAsync(CategoriesCacheKey, async entry =>
{
    entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5);  // D-02
    return await _context.AssessmentSessions
        .AsNoTracking()                                                // D-04 (cache miss path read-only)
        .Select(a => a.Category)
        .Distinct()
        .OrderBy(c => c)
        .ToListAsync();
});
swT5.Stop();
t5 = swT5.ElapsedMilliseconds;
```

**Pattern D — Cache invalidation at 3 Category CRUD actions (D-03):**

Existing analog dalam file yang sama: `Controllers/AssessmentAdminController.cs:3234` (`_cache.Remove($"exam-status-{id}");`).

Insertion point: SETELAH `await _context.SaveChangesAsync();`, SEBELUM blok audit log atau `RedirectToAction`. 3 lokasi:

**AddCategory L319-320 (verified via Read L293-332):**
```csharp
_context.AssessmentCategories.Add(category);
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);                               // NEW (D-03)

var currentUser = await _userManager.GetUserAsync(User);
// ... existing audit log + TempData["Success"] + RedirectToAction unchanged ...
```

**EditCategory POST L371-372 (verified via Read L347-384):**
```csharp
category.SignatoryUserId = string.IsNullOrWhiteSpace(signatoryUserId) ? null : signatoryUserId;
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);                               // NEW (D-03)

var currentUser = await _userManager.GetUserAsync(User);
// ... existing audit log + TempData["Success"] + RedirectToAction unchanged ...
```

**DeleteCategory L400-401 (verified via Read L386-413):**
```csharp
_context.AssessmentCategories.Remove(category);
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);                               // NEW (D-03)

var currentUser = await _userManager.GetUserAsync(User);
// ... existing audit log + TempData["Success"] + RedirectToAction unchanged ...
```

**Catatan implementasi cache key:** D-03 + Discretion mengizinkan `const string CategoriesCacheKey = "assessment_categories_distinct";` di top of class untuk hindari magic string × 4 occurrences (L172 cache.GetOrCreateAsync + L320 + L372 + L401 cache.Remove). Recommendation: pakai const supaya rename future = single line edit. Letakkan const setelah field declarations (~L25).

**Pattern E — HasIndex append at Data/ApplicationDbContext.cs L181 area (D-05, D-06):**

Verified existing code L177-181:
```csharp
// Indexes for performance
entity.HasIndex(a => a.UserId);
entity.HasIndex(a => new { a.UserId, a.Status });
entity.HasIndex(a => a.Schedule);
entity.HasIndex(a => a.AccessToken);
```

AFTER (D-05 + D-06 patch — append 2 baris):
```csharp
// Indexes for performance
entity.HasIndex(a => a.UserId);
entity.HasIndex(a => new { a.UserId, a.Status });
entity.HasIndex(a => a.Schedule);
entity.HasIndex(a => a.AccessToken);
// Phase 311: ManageAssessment perf indexes
entity.HasIndex(a => a.ExamWindowCloseDate);                     // NEW (D-05)
entity.HasIndex(a => a.LinkedGroupId);                           // NEW (D-06)
```

**Pattern F — EF Core migration generation (D-07):**

Run command:
```bash
dotnet ef migrations add AddManageAssessmentPerfIndexes
```

Auto-generates 3 files (timestamp resolved at execute time):
- `Migrations/{YYYYMMDDhhmmss}_AddManageAssessmentPerfIndexes.cs` — Up/Down body
- `Migrations/{YYYYMMDDhhmmss}_AddManageAssessmentPerfIndexes.Designer.cs`
- Auto-edit `Migrations/ApplicationDbContextModelSnapshot.cs` — appends 2 lines `b.HasIndex("ExamWindowCloseDate");` + `b.HasIndex("LinkedGroupId");` di blok AssessmentSessions (alphabetical order, expected near L440-460)

Expected migration body (matches PATTERNS.md verified template):
```csharp
public partial class AddManageAssessmentPerfIndexes : Migration
{
    protected override void Up(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.CreateIndex(
            name: "IX_AssessmentSessions_ExamWindowCloseDate",
            table: "AssessmentSessions",
            column: "ExamWindowCloseDate");

        migrationBuilder.CreateIndex(
            name: "IX_AssessmentSessions_LinkedGroupId",
            table: "AssessmentSessions",
            column: "LinkedGroupId");
    }

    protected override void Down(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.DropIndex(
            name: "IX_AssessmentSessions_LinkedGroupId",
            table: "AssessmentSessions");

        migrationBuilder.DropIndex(
            name: "IX_AssessmentSessions_ExamWindowCloseDate",
            table: "AssessmentSessions");
    }
}
```

Apply ke Dev DB:
```bash
dotnet ef database update
```

Verify via sqlcmd:
```bash
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E \
  -Q "SELECT name FROM sys.indexes WHERE object_id = OBJECT_ID('AssessmentSessions') AND name LIKE 'IX_AssessmentSessions_%';"
```
Expected output mengandung baris `IX_AssessmentSessions_ExamWindowCloseDate` + `IX_AssessmentSessions_LinkedGroupId`.

**Pattern G — Playwright smoke parity test (extends tests/e2e/assessment.spec.ts):**

Existing analog L1-31 di file: import header + `test.describe.configure({ mode: 'serial' })` + login pattern.

ADD test (insert sebagai test pertama dalam describe block, sebelum test 1.1):
```typescript
test('1.0 - Phase 311 ManageAssessment 3-tab smoke parity (200 OK)', async ({ page }) => {
  await login(page, 'hc');
  for (const tab of ['assessment', 'training', 'history']) {
    const resp = await page.goto(`/Admin/ManageAssessment?tab=${tab}`);
    expect(resp?.status()).toBe(200);
    await expect(page.locator('h2')).toContainText('Manage Assessment');
  }
});
```

Run:
```bash
cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 311" --reporter=list
```
Expected: 1 test PASS, 3 tab navigation succeed dengan 200 OK + h2 contains "Manage Assessment".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah HasIndex declarations + generate EF migration + apply ke Dev DB</name>
  <files>
    Data/ApplicationDbContext.cs
    Migrations/{timestamp}_AddManageAssessmentPerfIndexes.cs
    Migrations/{timestamp}_AddManageAssessmentPerfIndexes.Designer.cs
    Migrations/ApplicationDbContextModelSnapshot.cs
  </files>
  <read_first>
    - Data/ApplicationDbContext.cs L160-209 (existing AssessmentSession entity config + HasIndex chain L178-181)
    - Migrations/ApplicationDbContextModelSnapshot.cs L425-475 (current snapshot AssessmentSessions index list)
    - Migrations/20260324030227_ChangeUniqueIndexToComposite.cs L1-128 (analog migration template — namespace, partial class, CreateIndex/DropIndex primitive)
    - .planning/phases/311-manageassessment-performance/311-PATTERNS.md §"Migrations/{timestamp}_AddManageAssessmentPerfIndexes.cs" L271-356 (verbatim Up/Down body template)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-05 + D-06 + D-07 (decisions)
    - .planning/phases/311-manageassessment-performance/311-RESEARCH.md Pitfall 7 §"SQL Plan Verification" (post-apply verification via sys.indexes query)
  </read_first>
  <action>
**Step 1 — Edit `Data/ApplicationDbContext.cs` (PATTERNS.md Pattern E):**

Buka file, navigate ke L177-181 (di dalam blok `builder.Entity<AssessmentSession>(entity => { ... })`). Append 2 baris setelah `entity.HasIndex(a => a.AccessToken);`:

```csharp
entity.HasIndex(a => a.AccessToken);
// Phase 311: ManageAssessment perf indexes
entity.HasIndex(a => a.ExamWindowCloseDate);
entity.HasIndex(a => a.LinkedGroupId);
```

**Step 2 — Compile sanity check (sebelum migration generation):**
```bash
dotnet build --nologo -v q
```
Harus 0 errors. Kalau gagal, fix dulu sebelum generate migration.

**Step 3 — Generate migration:**
```bash
dotnet ef migrations add AddManageAssessmentPerfIndexes
```

Expected: 3 file baru di `Migrations/`. Capture timestamp prefix dari nama file.

**Step 4 — Review generated migration body:**

Buka `Migrations/{ts}_AddManageAssessmentPerfIndexes.cs`. Verify body MATCHES expected template di `<interfaces>` Pattern F. Kalau ada deviasi (extra column, beda naming), STOP dan investigasi.

**Step 5 — Verify snapshot auto-update:**
```bash
git diff Migrations/ApplicationDbContextModelSnapshot.cs | grep -E "^\+\s+b\.HasIndex"
```
Expected: 2 baris `+ b.HasIndex("ExamWindowCloseDate");` + `+ b.HasIndex("LinkedGroupId");` di blok AssessmentSessions.

**Step 6 — Apply migration ke Dev DB:**
```bash
dotnet ef database update
```

Expected output ending: `Done.` Kalau ada error, STOP + investigate.

**Step 7 — Verify indexes ada di Dev DB:**
```bash
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E \
  -Q "SELECT name FROM sys.indexes WHERE object_id = OBJECT_ID('AssessmentSessions') AND name LIKE 'IX_AssessmentSessions_%' ORDER BY name;"
```
Expected output mengandung 2 baris baru:
```
IX_AssessmentSessions_ExamWindowCloseDate
IX_AssessmentSessions_LinkedGroupId
```

**Step 8 — Verify migration tercatat di history:**
```bash
dotnet ef migrations list | grep AddManageAssessmentPerfIndexes
```
Expected: muncul tanpa `(Pending)` setelah `dotnet ef database update`.

**KRITIS:** TIDAK edit migration .cs file by hand. Snapshot fully owned by EF tooling. Kalau review menunjukkan deviasi, fix di DbContext config + regenerate (`dotnet ef migrations remove` lalu `add` lagi).
  </action>
  <verify>
    <automated>dotnet build --nologo -v q 2>&1 | grep -q "Build succeeded" && grep -E "IX_AssessmentSessions_(ExamWindowCloseDate|LinkedGroupId)" Migrations/*_AddManageAssessmentPerfIndexes.cs | wc -l</automated>
    Expected: "Build succeeded" + count = 4 (2 names × 2 occurrences each in Up + Down).
  </verify>
  <acceptance_criteria>
    - `grep -c "entity.HasIndex(a => a.ExamWindowCloseDate)" Data/ApplicationDbContext.cs` = 1
    - `grep -c "entity.HasIndex(a => a.LinkedGroupId)" Data/ApplicationDbContext.cs` = 1
    - `ls Migrations/*_AddManageAssessmentPerfIndexes.cs` returns 1 file (plus `.Designer.cs`)
    - `grep -E "IX_AssessmentSessions_(ExamWindowCloseDate|LinkedGroupId)" Migrations/*_AddManageAssessmentPerfIndexes.cs | wc -l` = 4
    - `grep -c 'b.HasIndex("ExamWindowCloseDate")' Migrations/ApplicationDbContextModelSnapshot.cs` = 1
    - `grep -c 'b.HasIndex("LinkedGroupId")' Migrations/ApplicationDbContextModelSnapshot.cs` = 1
    - `sqlcmd ... sys.indexes` query returns kedua nama index baru (manual run, dokumentasi di SUMMARY.md)
    - `dotnet build --nologo -v q` exit 0, 0 errors, ≤ 92 warnings
  </acceptance_criteria>
  <done>
    DbContext + migration + snapshot consistent. Migration applied ke Dev DB, sys.indexes confirms presence. Build hijau. Commit message: `feat(311): add ManageAssessment perf indexes — IX_ExamWindowCloseDate + IX_LinkedGroupId (D-05, D-06, D-07)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply 4 patches ke AssessmentAdminController (AsNoTracking + Include removal + cache + 3x invalidation)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs L1-50 (imports + constructor + field declarations area untuk const placement)
    - Controllers/AssessmentAdminController.cs L60-227 (post-Plan-01 state dengan Stopwatch instrumentation — verify swT1 + swT5 wrappers tetap ada)
    - Controllers/AssessmentAdminController.cs L293-413 (AddCategory + EditCategory + DeleteCategory action bodies — verify SaveChangesAsync line numbers)
    - Controllers/AssessmentAdminController.cs L3234 + L3314 (existing _cache.Remove analog untuk konvensi)
    - .planning/phases/311-manageassessment-performance/311-PATTERNS.md §"Core pattern A/B/C/D" L75-191 (verbatim BEFORE/AFTER snippets)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-01 sampai D-04 (cache strategy) + D-08 + D-09 (query optimization)
    - .planning/phases/311-manageassessment-performance/311-RESEARCH.md Pitfall 1 (AsNoTracking placement order) + Pitfall 2 (Include removal verification)
  </read_first>
  <action>
**Step 1 — Tambah const cache key (Discretion choice — recommended):**

Di field declaration area (~L25, dekat `private readonly IMemoryCache _cache;`), tambah:
```csharp
private const string CategoriesCacheKey = "assessment_categories_distinct";
```
Dipakai 4× (L172 GetOrCreateAsync + L320 + L372 + L401 invalidation), single-line rename future-proofing.

**Step 2 — Patch 1 (D-08 AsNoTracking di L66):**

Insertion: 1 baris `.AsNoTracking()` setelah `_context.AssessmentSessions` chain start, SEBELUM `.Where(...)`.

PRESERVE swT1 wrapper dari Plan 01. State akhir (di dalam swT1 wrap):
```csharp
var managementQuery = _context.AssessmentSessions
    .AsNoTracking()                                              // NEW (D-08)
    .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)
    .AsQueryable();
```

**Step 3 — Patch 2 (D-09 remove `.Include(a => a.User)` di L88):**

DELETE baris `.Include(a => a.User)` dari chain L87-90. KEEP semua field di Select projection L91-110 EXACTLY as-is (terutama null guards `a.User != null ? a.User.FullName : "Unknown"` dst).

State akhir L87-110:
```csharp
var allSessions = await managementQuery
    .OrderByDescending(a => a.Schedule)                          // (Include line GONE)
    .Select(a => new
    {
        a.Id,
        a.Title,
        a.Category,
        a.Schedule,
        a.ExamWindowCloseDate,
        a.DurationMinutes,
        a.Status,
        a.IsTokenRequired,
        a.AccessToken,
        a.PassPercentage,
        a.AllowAnswerReview,
        a.CreatedAt,
        a.AssessmentType,
        a.LinkedGroupId,
        UserFullName = a.User != null ? a.User.FullName : "Unknown",
        UserEmail = a.User != null ? a.User.Email : "",
        UserId = a.User != null ? a.User.Id : ""
    })
    .ToListAsync();
```

**Step 4 — Patch 3 (D-01 + D-02 + D-04 cache wrap di L172-176):**

Replace existing query dengan `_cache.GetOrCreateAsync` pattern. PRESERVE swT5 wrapper.

State akhir di blok swT5:
```csharp
var swT5 = System.Diagnostics.Stopwatch.StartNew();
ViewBag.Categories = await _cache.GetOrCreateAsync(CategoriesCacheKey, async entry =>
{
    entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5);  // D-02
    return await _context.AssessmentSessions
        .AsNoTracking()                                                // D-04
        .Select(a => a.Category)
        .Distinct()
        .OrderBy(c => c)
        .ToListAsync();
});
swT5.Stop();
t5 = swT5.ElapsedMilliseconds;
```

**KRITIS — type signature `GetOrCreateAsync` returns `Task<List<string>?>` (nullable):** ViewBag.Categories assignment ke nullable type acceptable karena ViewBag pakai dynamic. Kalau compiler emit nullability warning (CS8601), tolerable.

**Step 5 — Patch 4 (D-03 cache invalidation × 3 di Category CRUD):**

Insert 1 baris `_cache.Remove(CategoriesCacheKey);` SETELAH `await _context.SaveChangesAsync();` SEBELUM blok audit log di 3 lokasi:

**AddCategory (after L320):**
```csharp
_context.AssessmentCategories.Add(category);
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);                               // NEW (D-03)

var currentUser = await _userManager.GetUserAsync(User);
// ... unchanged ...
```

**EditCategory POST (after L372):**
```csharp
category.SignatoryUserId = string.IsNullOrWhiteSpace(signatoryUserId) ? null : signatoryUserId;
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);                               // NEW (D-03)

var currentUser = await _userManager.GetUserAsync(User);
// ... unchanged ...
```

**DeleteCategory (after L401):**
```csharp
_context.AssessmentCategories.Remove(category);
await _context.SaveChangesAsync();
_cache.Remove(CategoriesCacheKey);                               // NEW (D-03)

var currentUser = await _userManager.GetUserAsync(User);
// ... unchanged ...
```

**KRITIS — preserve as-is:**
- Semua signature method tidak berubah.
- Semua ViewBag assignments di action ManageAssessment tetap (D-15 smoke parity).
- Semua null guards di projection L106-108 KEEP exact (D-09 verification).
- Stopwatch wrapper dari Plan 01 PRESERVE (D-13 permanent).
- TIDAK menambah try/catch baru.

**Step 6 — Verify ViewBag shape preservation post-patch:**
```bash
git diff HEAD~1 HEAD Controllers/AssessmentAdminController.cs | grep -E "^\-\s+ViewBag\." | wc -l
```
Expected: 0 (no ViewBag assignment removed).

**Step 7 — Build:**
```bash
dotnet build --nologo -v q
```
Expected: 0 errors, ≤ 92 warnings.
  </action>
  <verify>
    <automated>dotnet build --nologo -v q 2>&1 | grep -q "Build succeeded" && grep -c "_cache.Remove(CategoriesCacheKey)" Controllers/AssessmentAdminController.cs && grep -c "GetOrCreateAsync(CategoriesCacheKey" Controllers/AssessmentAdminController.cs</automated>
    Expected: "Build succeeded" + count = 3 (Remove × 3) + count = 1 (GetOrCreateAsync).
  </verify>
  <acceptance_criteria>
    - `grep -c "private const string CategoriesCacheKey" Controllers/AssessmentAdminController.cs` = 1
    - `grep -c "AsNoTracking()" Controllers/AssessmentAdminController.cs` di action ManageAssessment area (L60-227) ≥ 2 (managementQuery + cache delegate)
    - `grep -c "Include(a => a.User)" Controllers/AssessmentAdminController.cs` di action ManageAssessment area = 0 (D-09 removed)
    - `grep -c "GetOrCreateAsync(CategoriesCacheKey" Controllers/AssessmentAdminController.cs` = 1
    - `grep -c "_cache.Remove(CategoriesCacheKey)" Controllers/AssessmentAdminController.cs` = 3
    - `grep -c "AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5)" Controllers/AssessmentAdminController.cs` = 1
    - `grep -c "UserFullName = a.User != null ? a.User.FullName" Controllers/AssessmentAdminController.cs` = 1 (null guard preserved)
    - `git diff HEAD~1 Controllers/AssessmentAdminController.cs | grep -E "^\-\s+ViewBag\." | wc -l` = 0
    - `dotnet build --nologo -v q` exit 0, 0 errors, warnings ≤ 92
  </acceptance_criteria>
  <done>
    Semua 4 patches applied: AsNoTracking di managementQuery, Include removed dengan projection unchanged, GetOrCreateAsync dengan TTL 5min, dan 3x cache.Remove di Category CRUD. Const `CategoriesCacheKey` declared. ViewBag shape preserved. Build hijau. Commit message: `feat(311): apply ManageAssessment perf patches — AsNoTracking + Include removal + Categories cache + invalidation (D-01..D-04, D-08, D-09)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/e2e/assessment.spec.ts dengan Phase 311 smoke parity 3-tab test</name>
  <files>tests/e2e/assessment.spec.ts</files>
  <read_first>
    - tests/e2e/assessment.spec.ts L1-60 (existing imports + login helper + test.describe.configure pattern)
    - .planning/phases/311-manageassessment-performance/311-PATTERNS.md §"tests/e2e/manageassessment.smoke.spec.ts" L457-501 (verbatim test template + recommendation untuk extend existing file)
    - .planning/phases/311-manageassessment-performance/311-VALIDATION.md row 9 task 311-02-09 (smoke E2E acceptance)
  </read_first>
  <action>
**Decision per PATTERNS.md recommendation:** EXTEND `tests/e2e/assessment.spec.ts` (test 1.0 baru) supaya tidak duplicate Playwright config, login helper, fixtures.

**Insertion location:** Setelah `test.describe.configure({ mode: 'serial' });` (L26 area), sebagai test PERTAMA dalam describe block `'Assessment - Admin Creates & Manages'` — supaya smoke parity check jalan duluan sebelum test functional lainnya.

**Code untuk insert (verbatim PATTERNS.md):**

```typescript
test('1.0 - Phase 311 ManageAssessment 3-tab smoke parity (200 OK)', async ({ page }) => {
  await login(page, 'hc');
  for (const tab of ['assessment', 'training', 'history']) {
    const resp = await page.goto(`/Admin/ManageAssessment?tab=${tab}`);
    expect(resp?.status()).toBe(200);
    await expect(page.locator('h2')).toContainText('Manage Assessment');
  }
});
```

**Verify TypeScript compile:**
```bash
cd tests && npx tsc --noEmit
```
Expected: 0 errors.

**Verify test discoverable:**
```bash
cd tests && npx playwright test --list --grep "Phase 311"
```
Expected: 1 test listed.

**JANGAN run test execute di sini** — test akan run di Task 5 + Task 6 Step 3.

**KRITIS:** TIDAK rename existing test names, TIDAK modify imports, TIDAK ubah `test.describe.configure({ mode: 'serial' })`. Hanya 1 test addition.
  </action>
  <verify>
    <automated>cd tests && npx playwright test --list --grep "Phase 311" 2>&1 | grep -q "Phase 311 ManageAssessment 3-tab" && echo "OK"</automated>
    Expected: "OK" — test discoverable.
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 311 ManageAssessment 3-tab" tests/e2e/assessment.spec.ts` = 1
    - `grep -c "/Admin/ManageAssessment?tab=" tests/e2e/assessment.spec.ts` ≥ 1
    - TypeScript compile: `cd tests && npx tsc --noEmit` exit 0
    - Playwright list mention test baru
    - Existing tests tidak terhapus
  </acceptance_criteria>
  <done>
    Smoke parity test ditambah ke `tests/e2e/assessment.spec.ts` sebagai test 1.0. TypeScript compile hijau. Test belum di-run (defer ke Task 5 post-patch). Commit message: `test(311): add Phase 311 ManageAssessment 3-tab smoke parity (SC #7)`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Tulis 311-UAT.md scaffold (post-patch baseline rerun + cache invalidation behavior verification)</name>
  <files>.planning/phases/311-manageassessment-performance/311-UAT.md</files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-15 (smoke methodology) + D-03 (cache invalidation behavior)
    - .planning/phases/311-manageassessment-performance/311-VALIDATION.md §"Manual-Only Verifications" (5 manual scenarios)
    - .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md kalau ada (recent UAT scaffold pattern)
    - .planning/phases/310-essay-finalize-idempotency/310-UAT.md kalau ada (most recent UAT pattern)
  </read_first>
  <action>
**Tulis `.planning/phases/311-manageassessment-performance/311-UAT.md` dengan 4-step manual UAT (Bahasa Indonesia narrative, code/commands English):**

```markdown
---
phase: 311
artifact: uat
created: <ISO date>
status: draft
related_baseline: 311-BASELINE.md
---

# Phase 311 — Manual UAT Checklist

> Manual verification untuk SC #1, SC #6, SC #7 + D-03 cache invalidation behavior.
> Pre-run: pastikan `dotnet ef database update` sudah applied (Task 1) + Plan 02 patches sudah committed (Task 2).
> Dataset: HARUS sama dengan baseline 311-BASELINE.md (restore dari `C:\temp\HcPortalDB_Dev_311_baseline.bak` kalau dataset Dev sudah berubah sejak Plan 01).

## Pre-UAT Setup

1. Restore Dev DB ke snapshot baseline (Pitfall 9 mitigation):
   ```bash
   sqlcmd -S "localhost\SQLEXPRESS" -E -Q "RESTORE DATABASE [HcPortalDB_Dev] FROM DISK = 'C:\temp\HcPortalDB_Dev_311_baseline.bak' WITH REPLACE;"
   ```
   *Skip kalau dataset belum berubah.*

2. Re-apply migration kalau restore wipe schema state:
   ```bash
   dotnet ef database update
   ```

3. Verify build clean:
   ```bash
   dotnet build --nologo -v q
   ```

4. Start app fresh (cold cache state):
   ```bash
   dotnet run --no-launch-profile --project HcPortal.csproj > postpatch-311.log 2>&1 &
   ```

5. Login admin.

---

## Step 1 — Post-Patch Measurement (SC #1, SC #6)

**Tujuan:** Verifikasi p95 ≥ 30% improvement vs baseline.

**Procedure:**

1. Hit `/Admin/ManageAssessment?tab=assessment&page=1&pageSize=20` 5x cold (3s sleep antar request).
2. Extract log entries:
   ```bash
   grep "ManageAssessment perf breakdown" postpatch-311.log
   ```
3. Drop run 1 (JIT warmup), compute median dari run 2-5 per segment.
4. Hitung improvement %:
   ```
   improvement = (baseline_median_total - postpatch_median_total) / baseline_median_total
   ```
   Catat baseline numbers dari `311-BASELINE.md` Bottleneck Analysis median row.

**Acceptance:** improvement ≥ 0.30 (30%) untuk Total.

**Result:**
- ☐ PASS — improvement = ___% (≥30%)
- ☐ FAIL — improvement = ___% (< 30%) → escalate per Pitfall 7

**Notes:** ___________

---

## Step 2 — Cache Behavior Verification (D-03)

**Tujuan:** Verifikasi cache invalidation working — admin yang add/edit/delete kategori langsung lihat fresh dropdown.

**Procedure:**

1. Buka `/Admin/ManageCategories`, tambah kategori `"PHASE-311-TEST-CATEGORY"`.
2. Navigate `/Admin/ManageAssessment?tab=assessment`. Buka dropdown Filter Kategori.
3. **Acceptance 2.1:** Kategori MUNCUL immediately (TIDAK perlu wait 5 menit).
4. Edit kategori jadi `"PHASE-311-TEST-CATEGORY-EDIT"`.
5. Navigate ke ManageAssessment, refresh dropdown.
6. **Acceptance 2.2:** Kategori dengan nama baru MUNCUL immediately.
7. Hapus kategori.
8. Refresh dropdown.
9. **Acceptance 2.3:** Kategori HILANG immediately.

**Result:**
- ☐ PASS
- ☐ FAIL

**Notes:** ___________

---

## Step 3 — 3-Tab Smoke Parity (SC #7, D-15)

**Tujuan:** Grouping & paging output identik pre/post patch untuk 3 tab.

**Procedure:**

1. Run Playwright smoke:
   ```bash
   cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 311" --reporter=list
   ```
   **Acceptance 3.1:** Test PASS.

2. Manual visual check:
   - `/Admin/ManageAssessment?tab=assessment` — record total count + first 5 group titles.
   - **Acceptance 3.2:** Total + titles IDENTIK dengan pre-patch.

3. Repeat untuk `tab=training` dan `tab=history`:
   - **Acceptance 3.3:** Tab training render dengan dropdown Bagian/Unit functional.
   - **Acceptance 3.4:** Tab history render dengan filter assessment dropdown functional.

**Result:**
- ☐ PASS
- ☐ FAIL

**Notes:** ___________

---

## Step 4 — EF Generated SQL Diff Verification (D-10)

**Tujuan:** Confirm `LEFT JOIN AspNetUsers` masih emit dari projection (tanpa Include redundancy).

**Procedure:**

1. `appsettings.Development.json` sudah punya `Microsoft.EntityFrameworkCore.Database.Command: Information` (verified L6).
2. Hit `/Admin/ManageAssessment?tab=assessment` 1x dengan app running.
3. Grep generated SQL:
   ```bash
   grep -A 30 "Executed DbCommand" postpatch-311.log | grep -B 2 -A 30 "FROM \[AssessmentSessions\]"
   ```
4. **Acceptance 4.1:** SQL output mengandung `LEFT JOIN [AspNetUsers]`.
5. **Acceptance 4.2:** Tidak ada extra INCLUDE syntax (no double-fetch User entity).

**Result:**
- ☐ PASS
- ☐ FAIL — paste SQL di Notes

**Notes:** ___________

---

## Sign-Off

**UAT executed by:** ___________________
**Date:** ___________
**Overall Result:** ☐ PASS | ☐ FAIL
**Carry-over items:** ___________
```

**Tulis file dengan placeholder cells `___` — diisi user saat UAT execution.**

**JANGAN execute UAT yet** — execute di Task 6 (orchestrator checkpoint).
  </action>
  <verify>
    <automated>test -f .planning/phases/311-manageassessment-performance/311-UAT.md && grep -q "Step 1 — Post-Patch Measurement" .planning/phases/311-manageassessment-performance/311-UAT.md && grep -q "Step 2 — Cache Behavior Verification" .planning/phases/311-manageassessment-performance/311-UAT.md && echo "OK"</automated>
    Expected: "OK" — file exists dengan 4-step struktur.
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/311-manageassessment-performance/311-UAT.md` exists
    - File mengandung 4 step header
    - File mengandung "Sign-Off" section dengan placeholder
    - Frontmatter `related_baseline: 311-BASELINE.md` (audit trail link)
    - Bahasa narrative Bahasa Indonesia, code blocks English
  </acceptance_criteria>
  <done>
    `311-UAT.md` scaffold tertulis dengan 4-step procedure. File belum diisi result (wait Task 6 user execution). Tidak commit yet — bundle dengan Task 7.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run post-patch measurement (5x cold) + populate 311-UAT.md Step 1</name>
  <files>.planning/phases/311-manageassessment-performance/311-UAT.md</files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-BASELINE.md (baseline median numbers)
    - .planning/phases/311-manageassessment-performance/311-UAT.md Step 1 procedure (Task 4)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md D-12 (5x cold protocol)
  </read_first>
  <action>
**Step 1 — Verify Dev DB state:**
```bash
dotnet ef migrations list | grep AddManageAssessmentPerfIndexes
```
Confirm migration applied. Kalau pending, `dotnet ef database update`.

**Step 2 — Restore baseline DB snapshot kalau dataset berubah:**
Cek `311-BASELINE.md` frontmatter untuk path snapshot. Kalau ada perubahan data sejak baseline:
```bash
sqlcmd -S "localhost\SQLEXPRESS" -E -Q "RESTORE DATABASE [HcPortalDB_Dev] FROM DISK = 'C:\temp\HcPortalDB_Dev_311_baseline.bak' WITH REPLACE;"
dotnet ef database update
```
Kalau dataset masih sama, skip restore.

**Step 3 — Start app fresh (cold IMemoryCache):**
```bash
dotnet run --no-launch-profile --project HcPortal.csproj > postpatch-311.log 2>&1 &
```

**Step 4 — Login + run 5x cold sama persis dengan baseline filter:**
```bash
for i in 1 2 3 4 5; do
  curl -b cookies.txt "https://localhost:5001/Admin/ManageAssessment?tab=assessment&page=1&pageSize=20" \
    -o /dev/null -k -s -w "Run $i: %{time_total}s HTTP %{http_code}\n"
  sleep 3
done
```

**Step 5 — Extract log entries:**
```bash
grep "ManageAssessment perf breakdown" postpatch-311.log | tee postpatch-runs.txt
```

**Step 6 — Compute median per segment (drop run 1).**

**Step 7 — Compute improvement %:**

Baca `311-BASELINE.md` Bottleneck Analysis median row. Hitung:
```
improvement_total = (baseline_median_total - postpatch_median_total) / baseline_median_total
improvement_t1    = (baseline_median_t1    - postpatch_median_t1)    / baseline_median_t1
improvement_t5    = (baseline_median_t5    - postpatch_median_t5)    / baseline_median_t5
```

Acceptance: improvement_total ≥ 0.30 per ROADMAP SC #6.

**Step 8 — Edit `311-UAT.md` Step 1 section:**
- Fill 5 raw log entries di Result section
- Fill Run 1-5 + Median table
- Tick checkbox PASS atau FAIL
- Tulis Notes: improvement % per segment

**Step 9 — Decision branch:**

- **Kalau improvement_total ≥ 30%:** Lanjut Task 6 (full UAT human checkpoint).
- **Kalau improvement_total < 30%:** STOP, surface ke user:
  - Run SQL plan check (Pitfall 7): verify execution plan pakai 2 indexes baru
  - Kalau indexes TIDAK dipakai → COALESCE issue (Pitfall 3) — defer composite index
  - Kalau indexes dipakai tapi gain kecil → Skenario A wrong assumption, atau dataset Dev terlalu kecil
  - User decide: (a) proceed dengan dokumentasi gain partial, (b) escalate untuk add composite/computed column, (c) defer Phase 311 close pending production-scale measurement.
  </action>
  <verify>
    <automated>grep -E "^\- \[x\] PASS" .planning/phases/311-manageassessment-performance/311-UAT.md && echo "OK" || (grep -E "^\- \[x\] FAIL" .planning/phases/311-manageassessment-performance/311-UAT.md && echo "FAIL_RECORDED")</automated>
    Expected: "OK" atau "FAIL_RECORDED" — bukan kosong.
  </verify>
  <acceptance_criteria>
    - 311-UAT.md Step 1 punya 5 raw log entries terisi
    - 311-UAT.md Step 1 punya Run 1-5 + Median table cells terisi numerik
    - 311-UAT.md Step 1 punya checkbox PASS atau FAIL ticked
    - 311-UAT.md Step 1 Notes mengandung improvement % per segment
    - Kalau FAIL: orchestrator pause + escalate ke user
  </acceptance_criteria>
  <done>
    Post-patch measurement complete dengan numbers di-fill di 311-UAT.md Step 1. Decision PASS atau FAIL_RECORDED.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Manual UAT execution Step 2-4 + sign-off</name>
  <what-built>
    - Plan 02 Tasks 1-5 selesai: migration applied, 4 patches di controller, smoke test extended, UAT scaffold ditulis, post-patch measurement Step 1 di-fill.
    - User sekarang execute manual UAT Step 2 (cache invalidation), Step 3 (3-tab smoke parity), Step 4 (EF SQL diff verification).
  </what-built>
  <how-to-verify>
**Procedure (~20-30 menit):**

1. **Open `311-UAT.md`** di editor. Pastikan Step 1 sudah di-fill oleh Task 5.

2. **Step 2 — Cache invalidation behavior:**
   - Browser: navigate `/Admin/ManageCategories`
   - Add kategori `"PHASE-311-TEST"` → simpan
   - Navigate `/Admin/ManageAssessment?tab=assessment` → check dropdown
   - **Expected:** kategori muncul SAAT ITU JUGA — D-03 invalidation working
   - Edit kategori → check dropdown
   - Delete kategori → check dropdown
   - Tick PASS/FAIL di Step 2

3. **Step 3 — 3-tab smoke parity:**
   - CLI: `cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 311" --reporter=list`
   - **Expected:** 1 test PASS
   - Browser manual: navigate ke 3 tab, verify rendering OK
   - Tick PASS/FAIL di Step 3

4. **Step 4 — EF SQL diff verification:**
   - App running dengan EF Information logging enabled
   - Hit `/Admin/ManageAssessment?tab=assessment` 1x sambil tail postpatch-311.log
   - Grep: `grep -A 30 "Executed DbCommand" postpatch-311.log | grep -B 2 -A 30 "FROM \[AssessmentSessions\]"`
   - **Expected:** SQL mengandung `LEFT JOIN [AspNetUsers]`
   - Tick PASS/FAIL di Step 4 + paste SQL snippet

5. **Sign-Off:**
   - Tulis nama + date + Overall Result
   - Save 311-UAT.md

**Resume signal:**
- Reply `"uat passed"` kalau semua step PASS
- Reply `"uat failed: step X"` kalau ada step FAIL — escalate

**KRITIS — UAT FAIL recovery paths:**
- **Step 2 FAIL** (cache stale): typo cache key. Check: `grep "_cache.Remove" Controllers/AssessmentAdminController.cs` — semua harus pakai `CategoriesCacheKey` const.
- **Step 3 FAIL** (Playwright fail atau visual mismatch): ViewBag shape regression. Check: `git diff HEAD~3 Controllers/AssessmentAdminController.cs | grep "ViewBag\."`
- **Step 4 FAIL** (no LEFT JOIN AspNetUsers): EF Core 8 behavior shift, atau projection berubah. Check: `grep "UserFullName = a.User" Controllers/AssessmentAdminController.cs` = 1 hit. Re-add `.Include(a => a.User)` sebagai compensation kalau EF Core gagal auto-emit.
  </how-to-verify>
  <resume-signal>Reply "uat passed" atau "uat failed: step X" + describe issue</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Tulis 311-02-SUMMARY.md + commit semua Plan 02 artifacts</name>
  <files>
    .planning/phases/311-manageassessment-performance/311-02-SUMMARY.md
    Data/ApplicationDbContext.cs
    Migrations/{ts}_AddManageAssessmentPerfIndexes.cs
    Migrations/{ts}_AddManageAssessmentPerfIndexes.Designer.cs
    Migrations/ApplicationDbContextModelSnapshot.cs
    Controllers/AssessmentAdminController.cs
    tests/e2e/assessment.spec.ts
    .planning/phases/311-manageassessment-performance/311-UAT.md
  </files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-BASELINE.md (baseline numbers)
    - .planning/phases/311-manageassessment-performance/311-UAT.md (post-patch numbers + UAT result)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY.md template)
    - .planning/phases/310-essay-finalize-idempotency/310-02-SUMMARY.md kalau ada (recent SUMMARY pattern)
  </read_first>
  <action>
**Step 1 — Tulis `.planning/phases/311-manageassessment-performance/311-02-SUMMARY.md`:**

Struktur (mirror SUMMARY template + Phase 310 precedent):

```markdown
---
phase: 311
plan: 02
status: complete
completed: <ISO date>
related_artifacts:
  - 311-01-PLAN.md (Wave 0 baseline)
  - 311-02-PLAN.md (this plan)
  - 311-BASELINE.md (pre-patch measurement)
  - 311-UAT.md (post-patch measurement + UAT)
requirements_satisfied: [PERF-01]
---

# Phase 311 Plan 02 — SUMMARY

## What Was Built

- **EF Core migration `AddManageAssessmentPerfIndexes`** — 2 single-column indexes (`IX_AssessmentSessions_ExamWindowCloseDate`, `IX_AssessmentSessions_LinkedGroupId`) added via `Data/ApplicationDbContext.cs` HasIndex declarations + `dotnet ef migrations add` + `dotnet ef database update` ke Dev DB. (D-05, D-06, D-07, ROADMAP SC #4)
- **`Controllers/AssessmentAdminController.cs` patches:**
  - `.AsNoTracking()` di managementQuery L66 chain start (D-08, ROADMAP SC #2)
  - `.Include(a => a.User)` L88 dihapus, projection L106-108 null-guard preserved (D-09, ROADMAP SC #3)
  - `_cache.GetOrCreateAsync("assessment_categories_distinct", ...)` TTL 5 menit di L172-176 (D-01, D-02, D-04, ROADMAP SC #5)
  - `_cache.Remove(CategoriesCacheKey)` di AddCategory L320 + EditCategory L372 + DeleteCategory L401 (D-03)
  - `private const string CategoriesCacheKey = "assessment_categories_distinct";` di field declaration area
- **`tests/e2e/assessment.spec.ts`** — extended dengan test `1.0 - Phase 311 ManageAssessment 3-tab smoke parity (200 OK)` (ROADMAP SC #7)
- **`311-UAT.md`** — manual UAT 4-step executed dengan PASS sign-off

## Performance Results (ROADMAP SC #6)

Baseline (dari 311-BASELINE.md, Skenario A):
| Segment | Baseline median (ms) | Post-patch median (ms) | Improvement |
|---------|---------------------|------------------------|-------------|
| T1 (Assessment query L66-169) | <fill> | <fill> | <fill>% |
| T2 (GetWorkersInSection L210) | <fill> | <fill> | <fill>% (no patch — sanity) |
| T3 (GetAllWorkersHistory L212) | <fill> | <fill> | <fill>% (no patch — sanity) |
| T4 (Sections+Units L220-223) | <fill> | <fill> | <fill>% (no patch — sanity) |
| T5 (Distinct Categories L172-176) | <fill> | <fill> | <fill>% (cache hit) |
| **Total** | **<fill>** | **<fill>** | **<fill>%** |

**Acceptance: Total improvement ≥ 30% per ROADMAP SC #6 → ☐ PASS / ☐ FAIL.**

## SQL Plan Verification (D-10)

Pre-patch (with `.Include(a => a.User)`):
```sql
<paste excerpt dari postpatch-311.log baseline run kalau diarchive — atau "not captured pre-Plan-01"; di Plan 02 capture POST saja>
```

Post-patch (Include removed, projection only):
```sql
<paste relevant SQL output dari grep "Executed DbCommand" — confirm LEFT JOIN AspNetUsers present>
```

**Acceptance:** post-patch SQL emit `LEFT JOIN AspNetUsers` (D-10) → ☐ PASS

## Files Modified

| File | Change Type | Lines Touched |
|------|-------------|---------------|
| `Data/ApplicationDbContext.cs` | modified | +2 lines (L181-area HasIndex append) |
| `Migrations/{ts}_AddManageAssessmentPerfIndexes.cs` | new | full file (auto-generated) |
| `Migrations/{ts}_AddManageAssessmentPerfIndexes.Designer.cs` | new | full file (auto-generated) |
| `Migrations/ApplicationDbContextModelSnapshot.cs` | modified | +2 lines (auto-edit) |
| `Controllers/AssessmentAdminController.cs` | modified | ~12 lines net (const + 4 patches) |
| `tests/e2e/assessment.spec.ts` | modified | +9 lines (new test 1.0) |
| `.planning/phases/311-manageassessment-performance/311-UAT.md` | new | full file |
| `.planning/phases/311-manageassessment-performance/311-02-SUMMARY.md` | new | this file |

## Decisions Honored

| Decision | Where Implemented |
|----------|-------------------|
| D-01 (cache key static) | `CategoriesCacheKey` const + 4 occurrences |
| D-02 (TTL 5 min absolute) | `entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5)` di GetOrCreateAsync delegate |
| D-03 (explicit invalidation) | 3× `_cache.Remove(CategoriesCacheKey)` di Category CRUD |
| D-04 (cache miss path AsNoTracking) | `.AsNoTracking()` di delegate body |
| D-05 (IX_ExamWindowCloseDate) | HasIndex + migration |
| D-06 (IX_LinkedGroupId) | HasIndex + migration |
| D-07 (migration name) | `AddManageAssessmentPerfIndexes` |
| D-08 (AsNoTracking di L66) | managementQuery chain |
| D-09 (Include removed) | L88 deleted, projection preserved |
| D-10 (SQL diff verification) | UAT Step 4 PASS |
| D-11 (Stopwatch logging) | Plan 01 Task 1 |
| D-12 (5x cold protocol) | Both BASELINE and UAT |
| D-13 (Stopwatch permanent) | Code retained — not removed |
| D-14 (scope = Assessment query only) | Skenario A confirmed Plan 01 |
| D-15 (smoke methodology) | UAT Step 3 + Playwright test 1.0 |
| D-16 (baseline breakdown gate) | Plan 01 Task 3 sign-off |

## Threats Mitigated (from threat_model)

- **T-311-04 (cache poisoning):** Existing role gate `[Authorize(Roles = "Admin, HC")]` di Category CRUD actions blocks unauthorized writes; cache only refreshed via authorized writes + 5-min TTL safety net.
- **T-311-05 (information disclosure via slow query log):** Stopwatch log fields = ms numbers + tab enum + bool only; no PII.
- **T-311-06 (index lock contention selama migration):** Dev DB applied first; production deploy gunakan ONLINE=ON kalau SQL Server Standard+ — documented sebagai deploy note.

## Operational Notes

- **Production deploy:** Migration adalah offline index creation (SQL Server default). Untuk PortalHC scale (low traffic, < 100k rows), expected duration < 1 detik. Apply during low-traffic window. Kalau SQL Server Enterprise edition, tambah `WITH (ONLINE = ON)` ke generated `CreateIndex` (manual edit before deploy).
- **Monitoring:** Stopwatch logging permanent (D-13). Log entries `"ManageAssessment perf breakdown: ..."` bisa di-aggregate oleh log analyzer untuk track perf trend.
- **Cleanup:** `C:\temp\HcPortalDB_Dev_311_baseline.bak` (Pitfall 9 backup) bisa dihapus setelah UAT pass + Phase 311 closed.

## Carry-over

- **Composite index `IX_AssessmentSessions_Schedule_ExamWindowCloseDate`** — D-05 defer kalau improvement < 30% post-patch. Kalau Skenario A approval + post-patch ≥ 30%, composite tidak diperlukan.
- **Persisted computed column `EffectiveDate = COALESCE(ExamWindowCloseDate, Schedule)`** — defer (schema overhead).
- **Lazy-load tab non-aktif** — defer ke phase masa depan kalau D-16 confirms T2/T3 dominant.
- **MiniProfiler / Application Insights** — defer ke future observability phase.

## Verification Sign-Off

- [x] Plan 01 Wave 0 baseline complete (Skenario A approved)
- [x] Plan 02 Wave 1 patches applied (commit `<hash>`)
- [x] Plan 02 Wave 2 measurement complete (improvement ≥ 30%)
- [x] Manual UAT 4-step PASS (sign-off di 311-UAT.md)
- [x] Build hijau (0 errors, ≤ 92 warnings)
- [x] Smoke E2E pass (Phase 311 test 1.0)
- [x] All 7 ROADMAP SC satisfied

**Phase 311 ready for `/gsd-verify-work` closure.**
```

**Step 2 — Fill placeholder cells dengan numerik dari 311-BASELINE.md + 311-UAT.md.**

**Step 3 — Commit semua artifacts dalam 3 commits (granular audit trail):**

```bash
# Commit 1: Migration + DbContext (Task 1 outputs)
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "feat(311): add ManageAssessment perf indexes — IX_ExamWindowCloseDate + IX_LinkedGroupId (D-05, D-06, D-07)" \
  --files Data/ApplicationDbContext.cs \
          Migrations/*_AddManageAssessmentPerfIndexes.cs \
          Migrations/*_AddManageAssessmentPerfIndexes.Designer.cs \
          Migrations/ApplicationDbContextModelSnapshot.cs

# Commit 2: Controller patches (Task 2 outputs)
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "feat(311): apply ManageAssessment perf patches — AsNoTracking + Include removal + Categories cache + invalidation (D-01..D-04, D-08, D-09)" \
  --files Controllers/AssessmentAdminController.cs

# Commit 3: Smoke test + UAT + SUMMARY (Tasks 3, 4, 5, 6, 7 outputs)
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "docs(311): post-patch measurement + UAT pass + Plan 02 SUMMARY" \
  --files tests/e2e/assessment.spec.ts \
          .planning/phases/311-manageassessment-performance/311-UAT.md \
          .planning/phases/311-manageassessment-performance/311-02-SUMMARY.md
```

Verify:
```bash
git log --oneline | head -5
```
Expected: 3 commits dengan messages above.
  </action>
  <verify>
    <automated>test -f .planning/phases/311-manageassessment-performance/311-02-SUMMARY.md && grep -q "Phase 311 ready for" .planning/phases/311-manageassessment-performance/311-02-SUMMARY.md && git log -3 --oneline | grep -c "311" | grep -q "3" && echo "OK"</automated>
    Expected: "OK" — SUMMARY ada + 3 commits dengan tag "311".
  </verify>
  <acceptance_criteria>
    - `311-02-SUMMARY.md` exists dengan semua 16 D-XX decisions ter-document di "Decisions Honored" table
    - Performance Results table mengandung baseline + post-patch numbers + improvement % (bukan placeholder `<fill>`)
    - SQL plan verification section mengandung paste actual SQL output post-patch
    - 3 git commits landed dengan message convention `feat(311):` + `docs(311):`
    - `git status` clean
    - All 7 ROADMAP SC checked off di "Verification Sign-Off" section
  </acceptance_criteria>
  <done>
    Phase 311 complete. SUMMARY documents semua hasil + decisions + carry-over. 3 commits ter-tag dengan PERF-01 traceability. Ready handoff ke `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Admin → ASP.NET Core route | Authenticated request enters `ManageAssessment` action; existing `[Authorize(Roles = "Admin, HC")]` gate aktif (no change) |
| Action body → DB (read) | EF Core query path optimized — `.AsNoTracking()` + index seek, no new injection surface |
| Action body → IMemoryCache | New write surface: `_cache.GetOrCreateAsync(CategoriesCacheKey, ...)` populates cache; `_cache.Remove(CategoriesCacheKey)` invalidates. Cache scope = process-wide (single global key D-01) |
| Category CRUD action body → DB (write) + Cache (invalidate) | Existing `[Authorize(Roles = "Admin, HC")]` gate aktif untuk semua 3 actions; cache invalidation co-located dengan write |
| EF migration → SQL Server schema | DDL operation: `CREATE INDEX` × 2 di `AssessmentSessions` table; reversible via `DropIndex` Down migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-311-04 | Tampering | Categories cache poisoning via unauthorized write | mitigate | Existing role gate `[Authorize(Roles = "Admin, HC")]` di AddCategory L294, EditCategory L348, DeleteCategory L387 prevents unauthorized invalidation/refresh. Cache reload only triggered via authorized writes ATAU 5-min TTL natural expiry. Grep verifiable: `grep -B 1 "_cache.Remove(CategoriesCacheKey)" Controllers/AssessmentAdminController.cs` shows each Remove preceded by SaveChangesAsync after Authorize-gated action. |
| T-311-05 | Information Disclosure | Stopwatch log fields visible di Serilog/Console output | accept | Log entries `"ManageAssessment perf breakdown: ..."` HANYA berisi numeric ms + tab enum + bool. No PII, no user identifier, no query parameter content. Same risk profile as existing logging convention. Acceptable per existing project logging policy. |
| T-311-06 | Denial of Service | Index creation lock contention on `AssessmentSessions` table during migration | mitigate | Dev DB applied first (Task 1 Step 6) — verify migration completes < 5 detik untuk dataset PortalHC. Production deploy strategy documented di SUMMARY.md "Operational Notes": apply during low-traffic window; for SQL Server Enterprise+, add `WITH (ONLINE = ON)` ke generated `CreateIndex`. For Standard Edition: offline build expected < 1 detik untuk PortalHC scale. Grep verifiable: SUMMARY.md mengandung "Operational Notes" section dengan deploy guidance. |
| T-311-07 | Tampering | Cache stampede saat cold cache + concurrent admin requests | accept | IMemoryCache.GetOrCreateAsync TIDAK punya native stampede protection (RESEARCH Pitfall 4 + Assumption A7). Risk = N concurrent admin requests bisa execute factory delegate N kali pada cold cache. Acceptable karena: (a) admin endpoint low concurrency (<10 users typical, A7), (b) Categories distinct query relatively cheap (~100ms), (c) factory delegate idempotent. Mitigation defer: kalau production traffic surface stampede issue, adopt Option B SemaphoreSlim wrapper sebagai future enhancement. |
| T-311-08 | Denial of Service | IMemoryCache memory exhaustion via large Categories list | accept | `Program.cs:17` `AddMemoryCache()` no SizeLimit configured (default unlimited per Assumption A6). Categories distinct list bounded oleh AssessmentSessions.Category column distinct values — typically < 50 entries × ~50 bytes/entry = ~2.5 KB total. Negligible memory footprint. Acceptable. |

**No HIGH/CRITICAL threats.** Phase 311 = backend perf optimization dengan attack surface NETO ZERO (existing role gates preserved, no new endpoints, no new external integration).
</threat_model>

<verification>
**Phase-level checks (Plan 02 scope):**

1. **Migration applied (Task 1):**
   ```bash
   dotnet ef migrations list | grep AddManageAssessmentPerfIndexes
   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E \
     -Q "SELECT name FROM sys.indexes WHERE object_id = OBJECT_ID('AssessmentSessions') AND name LIKE 'IX_AssessmentSessions_%';" \
     | grep -E "IX_AssessmentSessions_(ExamWindowCloseDate|LinkedGroupId)"
   ```
   Expected: migration listed (no `(Pending)`); both indexes present in sys.indexes.

2. **Code patches applied (Task 2):**
   ```bash
   grep -c "AsNoTracking()" Controllers/AssessmentAdminController.cs        # ≥ 2 (managementQuery + cache delegate)
   grep -c "Include(a => a.User)" Controllers/AssessmentAdminController.cs  # = 0 di action ManageAssessment
   grep -c "GetOrCreateAsync(CategoriesCacheKey" Controllers/AssessmentAdminController.cs   # = 1
   grep -c "_cache.Remove(CategoriesCacheKey)" Controllers/AssessmentAdminController.cs    # = 3
   ```

3. **Build clean (Task 1+2):**
   ```bash
   dotnet build --nologo -v q
   ```
   Expected: 0 errors, ≤ 92 warnings.

4. **Playwright smoke pass (Task 3 + Task 6 Step 3):**
   ```bash
   cd tests && npx playwright test e2e/assessment.spec.ts --grep "Phase 311" --reporter=list
   ```
   Expected: 1 test PASS.

5. **Performance gain ≥ 30% (Task 5):**
   ```bash
   grep -E "Total improvement" .planning/phases/311-manageassessment-performance/311-02-SUMMARY.md
   ```
   Expected: line shows ≥ 30%.

6. **UAT signed off (Task 6):**
   ```bash
   grep -E "Overall Result.*PASS" .planning/phases/311-manageassessment-performance/311-UAT.md
   ```

7. **Commits landed (Task 7):**
   ```bash
   git log --oneline -5 | grep -c "311"
   ```
   Expected: ≥ 3 (Plan 02 commits).
</verification>

<success_criteria>
**Plan 02 complete when:**

- [x] EF migration `AddManageAssessmentPerfIndexes` generated dan applied ke Dev DB; sys.indexes confirms 2 indexes baru (ROADMAP SC #4)
- [x] `Controllers/AssessmentAdminController.cs` mengandung `.AsNoTracking()` di managementQuery (ROADMAP SC #2)
- [x] `.Include(a => a.User)` L88 dihapus, projection null guards preserved (ROADMAP SC #3)
- [x] `IMemoryCache.GetOrCreateAsync(CategoriesCacheKey, ...)` dengan TTL 5 menit applied di L172 (ROADMAP SC #5)
- [x] 3× `_cache.Remove(CategoriesCacheKey)` di Category CRUD actions (D-03)
- [x] Build hijau: 0 errors, ≤ 92 warnings
- [x] Playwright smoke parity test 1.0 added + PASS
- [x] Post-patch p95 ≤ baseline_p95 × 0.7 (≥ 30% improvement) per ROADMAP SC #1 + SC #6 — verified via 311-UAT.md Step 1
- [x] Cache invalidation behavior verified manual via 311-UAT.md Step 2 (D-03)
- [x] 3-tab smoke parity verified manual via 311-UAT.md Step 3 (ROADMAP SC #7)
- [x] EF SQL diff verified — `LEFT JOIN AspNetUsers` present post-Include-removal (D-10) via 311-UAT.md Step 4
- [x] `311-02-SUMMARY.md` complete dengan numbers, decisions table, threats mitigated, operational notes, carry-over
- [x] 3 git commits landed dengan tag `feat(311):` + `docs(311):`
</success_criteria>

<output>
After completion:
- `.planning/phases/311-manageassessment-performance/311-02-SUMMARY.md` ditulis lengkap (Task 7)
- All Plan 02 artifacts committed (3 commits per Task 7 split)
- Phase 311 ready for `/gsd-verify-work` untuk:
  - Mark Phase 311 ✅ Complete di ROADMAP.md
  - Update STATE.md (Phase 311 done, milestone v15.0 progress)
  - Verify all 7 ROADMAP SC satisfied via VERIFICATION.md generation
- Cleanup todo: hapus `C:\temp\HcPortalDB_Dev_311_baseline.bak` setelah verify-work close
</output>
