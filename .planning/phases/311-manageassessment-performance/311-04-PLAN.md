---
phase: 311
plan: 04
type: execute
wave: 3
depends_on: [311-02]
files_modified:
  - Views/Admin/ManageAssessment.cshtml
  - wwwroot/css/site.css
autonomous: false
gap_closure: true
requirements: [PERF-01]
tags: [perf, htmx, gap-closure, frontend, bugfix]

must_haves:
  truths:
    - "Filter form satu instance per tab visible di shell level (legacy filter di partial views ter-hide via CSS)"
    - "Cross-tab cache invalidation HANYA fire saat filter form berubah, bukan setiap tab activation swap"
    - "Tab non-aktif refetch berhasil setelah filter change (no stuck skeleton — once modifier tidak menahan re-fire)"
    - "Tombol 'Coba Lagi' di error template fire HTTP request fresh saat di-klik, error template hilang, content swap"
    - "Build hijau: dotnet build exit 0, warnings ≤92 (Phase 309 baseline preserved)"
    - "D-10 backward compat preserved: 3 partial view files (_AssessmentGroupsTab, _TrainingRecordsTab, _HistoryTab) TIDAK dimodifikasi"
  artifacts:
    - path: "wwwroot/css/site.css"
      provides: "CSS rule hide filter row legacy di partial views (Phase 311.1 marker)"
      contains: "Phase 311.1 gap closure"
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Revised JS handler (filter-form-only invalidation, drop once on restore, htmx.ajax retry)"
      contains: "filter-form change"
  key_links:
    - from: "wwwroot/css/site.css"
      to: ".htmx-tab-wrapper > form, .htmx-tab-wrapper > .row.mb-4, .htmx-tab-wrapper > .row.mb-3, .htmx-tab-wrapper > .card.bg-light"
      via: "display:none CSS"
      pattern: "Phase 311\\.1"
    - from: "Views/Admin/ManageAssessment.cshtml (filter listener)"
      to: "#filter-form change/input event"
      via: "addEventListener filter-form scoped"
      pattern: "filter-form.*addEventListener|getElementById\\('filter-form'\\)\\.addEventListener"
    - from: "Views/Admin/ManageAssessment.cshtml (retry handler)"
      to: "htmx.ajax direct call (bypass trigger dispatcher)"
      via: "htmx.ajax('GET', url, {target, swap})"
      pattern: "htmx\\.ajax\\(\\s*'GET'"
---

<objective>
Tutup 4 sub-bug dari UAT Plan 02 Wave 1 (BUG-1 filter duplikasi, BUG-2A invalidation agresif, BUG-2B once sticky, BUG-5A retry no-op) di `Views/Admin/ManageAssessment.cshtml` + `wwwroot/css/site.css`. Scope concentrated di 2 file, no controller/partial view changes (D-10 preserve).

Purpose: Plan 02 paused-at-checkpoint karena 4 functional defects pada lazy-load architecture. Tanpa fix ini, UAT smoke parity Test 1/2/5 fail dan Test 4 (perf wifi kantor) tidak bisa di-run. Setelah Plan 04 close, UAT lanjut ke webdev deploy untuk validate acceptance ≤40 detik / <14 KB / ≤2s tab switch.

Output: 2 file revised + commit. Re-UAT manual (5 step) diharapkan PASS untuk Test 1, Test 2, Test 5 lokal; Test 4 dilanjutkan di webdev oleh IT setelah merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/311-manageassessment-performance/311-UAT.md
@.planning/phases/311-manageassessment-performance/311-CONTEXT.md
@.planning/phases/311-manageassessment-performance/311-UI-SPEC.md
@.planning/phases/311-manageassessment-performance/311-02-SUMMARY.md

<interfaces>
<!-- Existing structures relevant ke gap closure (extracted dari codebase 2026-05-07). -->

From Views/Admin/ManageAssessment.cshtml (current Plan 02 implementation):

Filter form (shell level, line 88):
```html
<form id="filter-form" method="get" action="@Url.Action(...)">
  <input type="hidden" name="tab" id="filter-tab" />
  <input type="hidden" name="page" id="filter-page" />
  <input type="text" name="search" id="filter-search"
         hx-get="@urlActive" hx-trigger="input changed delay:500ms, keyup[key=='Enter']"
         hx-target="#pane-{tab} > div.htmx-tab-wrapper" hx-include="#filter-form" hx-sync="this:replace" />
  <select id="filter-category" name="category" hx-trigger="change" ... />
  <select id="filter-status" name="statusFilter" hx-trigger="change" ... />
</form>
```

Tab wrappers (3 instances, line 203/237/281):
```html
<div class="htmx-tab-wrapper" data-tab-name="{assessment|training|history}"
     hx-get="@urlTab" hx-trigger="load|shown.bs.tab from:#tab-{name} once"
     hx-target="this" hx-swap="innerHTML" hx-include="#filter-form" hx-sync="this:replace">
  <!-- skeleton -->
</div>
```

Existing JS handlers (TO BE REPLACED in this plan):
- L368-386: `htmx:afterSwap` listener — fires for ALL swaps including normal lazy-load (BUG-2A)
- L381: setAttribute restoring `hx-trigger='shown.bs.tab from:#tab-X once'` — once sticky (BUG-2B)
- L415-422: retry click handler calling `htmx.trigger(wrapper, 'load')` — event mismatch (BUG-5A)

From Views/Admin/Shared/_AssessmentGroupsTab.cshtml (PARTIAL — must NOT be modified per D-10):
- L13: `<div class="row mb-4">` containing `<form method="get" asp-action="ManageAssessment">` (search input legacy)
- L37: `<div class="row mb-3">` containing category + status `<select>` legacy

From Views/Admin/Shared/_TrainingRecordsTab.cshtml (PARTIAL — must NOT be modified per D-10):
- L28: `<div class="card bg-light border-0 mb-4">` containing `<form id="filterFormTraining">` (legacy 4-column filter)

From Views/Admin/Shared/_HistoryTab.cshtml (PARTIAL — must NOT be modified per D-10):
- L40: `<div class="row g-2 mb-3 pt-3 px-0">` containing search input + title dropdown legacy (nested di sub-tab content)

CSS hook semantics:
- `.htmx-tab-wrapper` is the outer DIV at shell level. Setelah HTMX swap innerHTML, partial content (with its own filter rows/forms) becomes direct children of wrapper.
- Selector `.htmx-tab-wrapper > <legacy-filter>` matches direct child of the wrapper post-swap.
</interfaces>

<constraints>
- D-05 (CONTEXT.md): cross-tab invalidation HANYA saat filter berubah, bukan tab switch.
- D-10 (CONTEXT.md): backward compat partial views — JANGAN modify 3 partial view files.
- UI-SPEC §2: error template + retry button copy "Gagal memuat data" + "Coba Lagi" — preserved as-is.
- D-09: per-action Stopwatch instrumentation — preserved (no changes to controller).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide legacy partial filter rows via CSS (BUG-1)</name>
  <files>wwwroot/css/site.css</files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-UAT.md (Gaps section truth #1, fix_options Option 1 CSS-only)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md (D-10 backward compat boundary)
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml lines 13-60 (DOM struktur sebenarnya)
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml lines 28-35 (card.bg-light filter container)
    - Views/Admin/Shared/_HistoryTab.cshtml lines 38-55 (row.g-2.mb-3 nested di sub-tab)
    - wwwroot/css/site.css (existing Phase 311 marker rules — append after, no overwrite)
  </read_first>
  <action>
Append CSS block ke BAGIAN BAWAH `wwwroot/css/site.css` (jangan overwrite existing rules). Selector targets DIRECT CHILDREN dari `.htmx-tab-wrapper` post-swap untuk match struktur sebenarnya 3 partial views (gap_summary selector `.row.g-2.mb-2` salah — actual struktur berbeda per partial; verified by grep di partial view sources).

Append the following CSS verbatim (preserve indentation, comments):

```css

/* =====================================================================
   Phase 311.1 gap closure (2026-05-07): hide partial view filter rows.
   Reason: 3 partial views (_AssessmentGroupsTab, _TrainingRecordsTab,
   _HistoryTab) have own legacy filter rows yang TIDAK punya hx-attrs.
   Shell #filter-form HTMX-driven sudah benar (di-promote ke shell level
   per Plan 02). Per D-10 backward compat, partial files TIDAK dimodifikasi —
   gunakan CSS untuk hide legacy filter row yang muncul setelah HTMX swap.
   Selector: direct child .htmx-tab-wrapper untuk avoid affecting nested
   table rows yang juga pakai class `.row` di partial view internal.
   ===================================================================== */

/* _AssessmentGroupsTab.cshtml — search form (row.mb-4) + filter row (row.mb-3) */
.htmx-tab-wrapper > .row.mb-4,
.htmx-tab-wrapper > .row.mb-3 {
    display: none !important;
}

/* _TrainingRecordsTab.cshtml — filter card (card.bg-light) */
.htmx-tab-wrapper > .card.bg-light {
    display: none !important;
}

/* _HistoryTab.cshtml — nested filter row inside sub-tab pane.
   Filter di sini bersarang dalam .tab-content > .tab-pane (sub-tabs Riwayat
   Assessment / Riwayat Training), TIDAK direct child wrapper. Match via
   descendant selector dengan `.row.g-2.mb-3.pt-3` (4-class match unique
   ke filter row history, bukan content row generic). */
.htmx-tab-wrapper .row.g-2.mb-3.pt-3 {
    display: none !important;
}
```

JANGAN modify file partial views. JANGAN tambah ViewBag flag. JANGAN modify css rules existing (Phase 311 Plan 02 .htmx-request rules). HANYA append block di atas.
  </action>
  <verify>
    <automated>grep -q "Phase 311.1 gap closure" wwwroot/css/site.css && grep -q "htmx-tab-wrapper > .row.mb-4" wwwroot/css/site.css && grep -q "htmx-tab-wrapper > .card.bg-light" wwwroot/css/site.css && grep -q "htmx-tab-wrapper .row.g-2.mb-3.pt-3" wwwroot/css/site.css && echo PASS</automated>
  </verify>
  <done>
    CSS appended dengan 4 marker string verifiable: "Phase 311.1 gap closure", `htmx-tab-wrapper > .row.mb-4`, `htmx-tab-wrapper > .card.bg-light`, `htmx-tab-wrapper .row.g-2.mb-3.pt-3`. Existing Plan 02 .htmx-request rules preserved (no diff lines removed di section atasnya). 3 partial view files TIDAK ter-touch (D-10 preserved — verifiable: `git status` shows only site.css changed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Scope cross-tab invalidation to filter-form-only events (BUG-2A) + drop once on restore (BUG-2B)</name>
  <files>Views/Admin/ManageAssessment.cshtml</files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-UAT.md (Gaps truth #2 dan #3, fix_hint BUG-2A + BUG-2B Option 2 drop once)
    - .planning/phases/311-manageassessment-performance/311-CONTEXT.md (D-05 cross-tab invalidation strategy)
    - Views/Admin/ManageAssessment.cshtml lines 365-386 (existing afterSwap listener — TARGET REPLACE)
    - Views/Admin/ManageAssessment.cshtml lines 88-160 (filter form structure — element ID `filter-form`)
    - wwwroot/lib/htmx/htmx.min.js (peek minified API: confirm `htmx.process` signature accepts wrapper element; confirm event `htmx:configRequest` fires before request)
  </read_first>
  <action>
Replace existing `htmx:afterSwap` listener di `Views/Admin/ManageAssessment.cshtml` (saat ini di L365-386) dengan filter-form-scoped listener. Detection rule: invalidation HANYA fire saat swap berasal dari request yang di-trigger oleh element di dalam `#filter-form` (per D-05). Sekaligus drop `once` modifier dari restored hx-trigger string (per BUG-2B fix_options Option 2).

**Step A — Locate existing block:** Cari blok komentar `// Phase 311 Plan 02: Cross-tab cache invalidation (D-05).` (L365). Block-nya extends sampai closing `});` di L386 (sebelum block error template di L388).

**Step B — Replace seluruh block (L365-386) dengan implementasi berikut verbatim:**

```javascript
// Phase 311 Plan 04 gap closure (BUG-2A): Cross-tab cache invalidation (D-05).
// Listener fire HANYA saat swap berasal dari filter form change/input —
// BUKAN saat normal lazy-load tab activation swap. Dipakai elt provenance
// check via htmx:afterSwap event detail.requestConfig.elt: kalau elt
// closest ke #filter-form, swap = filter-driven; selain itu skip.
// BUG-2B fix: restored hx-trigger DROP `once` modifier — wrapper akan
// re-fire setiap shown.bs.tab event sampai content swap (saat user filter
// ulang, invalidation fire lagi). Sederhana, no htmx.cleanup needed.
document.body.addEventListener('htmx:afterSwap', function(evt) {
    var target = evt.detail.target;
    if (!target || !target.classList || !target.classList.contains('htmx-tab-wrapper')) return;

    // Provenance check: hanya invalidate saat trigger elt berada di dalam #filter-form.
    // evt.detail.requestConfig.elt = element yang trigger HTMX request.
    // Tab activation swap (hx-trigger="load" atau "shown.bs.tab") punya elt = wrapper itu sendiri,
    // BUKAN element di dalam #filter-form. Filter-driven swap punya elt = input/select di filter-form.
    var triggerElt = evt.detail.requestConfig && evt.detail.requestConfig.elt;
    if (!triggerElt || !triggerElt.closest || !triggerElt.closest('#filter-form')) {
        return; // Skip: bukan filter-driven swap (normal tab activation atau retry)
    }

    var activeTabName = target.getAttribute('data-tab-name');
    document.querySelectorAll('.htmx-tab-wrapper').forEach(function(wrapper){
        var tabName = wrapper.getAttribute('data-tab-name');
        if (tabName === activeTabName) return;
        // Reset ke skeleton placeholder + restore hx-trigger TANPA `once` (BUG-2B fix Option 2).
        // Tanpa `once`, wrapper akan re-fire setiap shown.bs.tab event — acceptable karena
        // selama wrapper sudah swap content sekali, fire kedua akan re-fetch dengan filter terbaru
        // (idempotent + intentional behavior post-invalidation).
        wrapper.innerHTML = '<div aria-busy="true" aria-label="Memuat data" class="placeholder-glow"><div class="row g-2 mb-2"><span class="placeholder col-12" style="height:48px"></span></div><div class="row g-2 mb-2"><span class="placeholder col-12" style="height:48px"></span></div></div>';
        wrapper.setAttribute('hx-trigger', 'shown.bs.tab from:#tab-' + tabName);
        if (window.htmx) htmx.process(wrapper);
    });
});
```

**Step C — Verifikasi inline:** Pastikan tidak ada lagi string `'shown.bs.tab from:#tab-' + tabName + ' once'` (with literal ` once`) di file. Search dan delete kalau ada residue dari implementasi lama.

JANGAN ubah block error template (L388-413) atau retry handler (L415-422 — itu Task 3). JANGAN ubah filter form HTML attributes (L88-160). JANGAN ubah tab activation listener (L340-363).
  </action>
  <verify>
    <automated>grep -q "Phase 311 Plan 04 gap closure (BUG-2A)" Views/Admin/ManageAssessment.cshtml && grep -q "triggerElt.closest('#filter-form')" Views/Admin/ManageAssessment.cshtml && ! grep -q "from:#tab-' + tabName + ' once'" Views/Admin/ManageAssessment.cshtml && echo PASS</automated>
  </verify>
  <done>
    `htmx:afterSwap` listener block di-replace dengan filter-form provenance check (`triggerElt.closest('#filter-form')` returns null → early return). String `'shown.bs.tab from:#tab-' + tabName + ' once'` (with trailing ` once`) sudah TIDAK ADA di file (verified by negative grep). String `'shown.bs.tab from:#tab-' + tabName` (without once) ada (positive grep). Build masih hijau (verified Task 4).
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace retry handler dengan htmx.ajax direct call (BUG-5A)</name>
  <files>Views/Admin/ManageAssessment.cshtml</files>
  <read_first>
    - .planning/phases/311-manageassessment-performance/311-UAT.md (Gaps truth #4, fix_options Option 1 htmx.ajax direct)
    - .planning/phases/311-manageassessment-performance/311-UI-SPEC.md §2 (Error Template — retry button copy preserved)
    - Views/Admin/ManageAssessment.cshtml lines 415-422 (existing retry click handler — TARGET REPLACE)
    - Views/Admin/ManageAssessment.cshtml line 388-413 (error template render — preserved, retry button class `[data-htmx-retry]` di-emit di sini)
  </read_first>
  <action>
Replace existing retry click handler (L415-422) dengan implementasi `htmx.ajax('GET', ...)` direct yang bypass trigger dispatcher. Reason: handler lama call `htmx.trigger(wrapper, 'load')` padahal wrapper hx-trigger = `shown.bs.tab from:#tab-X once` (BUG-2B sudah drop `once`, tapi event name mismatch tetap problematik) — direct ajax call bypass seluruh trigger machinery dan paling robust.

**Step A — Locate existing block:** Cari komentar `// Retry button delegation` (L414). Block-nya:
```javascript
// Retry button delegation
document.body.addEventListener('click', function(evt) {
    var btn = evt.target.closest('[data-htmx-retry]');
    if (!btn) return;
    var wrapper = btn.closest('.htmx-tab-wrapper');
    if (wrapper && window.htmx) {
        htmx.trigger(wrapper, 'load');
    }
});
```

**Step B — Replace seluruh block (L414-422) dengan:**

```javascript
// Phase 311 Plan 04 gap closure (BUG-5A): Retry button delegation.
// Pakai htmx.ajax('GET', url, {target, swap}) direct supaya bypass trigger
// dispatcher (handler lama trigger 'load' tidak match wrapper hx-trigger
// 'shown.bs.tab' → no-op). Direct ajax call paling robust dan preserve
// filter values via hx-include semantics (htmx.ajax otomatis include form
// data sesuai hx-include attribute pada wrapper).
document.body.addEventListener('click', function(evt) {
    var btn = evt.target.closest('[data-htmx-retry]');
    if (!btn) return;
    var wrapper = btn.closest('.htmx-tab-wrapper');
    if (!wrapper || !window.htmx) return;
    var url = wrapper.getAttribute('hx-get');
    if (!url) return;
    // Show skeleton selama refetch in-flight (UX feedback).
    wrapper.innerHTML = '<div aria-busy="true" aria-label="Memuat ulang data" class="placeholder-glow"><div class="row g-2 mb-2"><span class="placeholder col-12" style="height:48px"></span></div><div class="row g-2 mb-2"><span class="placeholder col-12" style="height:48px"></span></div></div>';
    // Direct AJAX call: bypass trigger dispatcher, target = wrapper, innerHTML swap.
    // hx-include="#filter-form" pada wrapper sudah declarative — htmx.ajax
    // honor hx-include attribute saat element argument disediakan (3rd arg).
    htmx.ajax('GET', url, { target: wrapper, swap: 'innerHTML', source: wrapper });
});
```

JANGAN ubah error template render function `renderHtmxError` (L390-401). JANGAN ubah event listeners `htmx:responseError` / `htmx:sendError` (L402-413). JANGAN ubah Regenerate Token handler (L424+ — separate concern).
  </action>
  <verify>
    <automated>grep -q "Phase 311 Plan 04 gap closure (BUG-5A)" Views/Admin/ManageAssessment.cshtml && grep -qE "htmx\.ajax\(\s*'GET'" Views/Admin/ManageAssessment.cshtml && ! grep -q "htmx.trigger(wrapper, 'load')" Views/Admin/ManageAssessment.cshtml && echo PASS</automated>
  </verify>
  <done>
    Retry click handler di-replace dengan `htmx.ajax('GET', url, {target, swap, source})`. String `htmx.trigger(wrapper, 'load')` sudah TIDAK ADA di file (negative grep). `data-htmx-retry` selector masih dipakai (delegation pattern preserved). Skeleton di-show sebelum ajax call (UX feedback consistency dengan UI-SPEC §1).
  </done>
</task>

<task type="auto">
  <name>Task 4: Build verification + smoke load gate</name>
  <files>(verification only, no file edits)</files>
  <read_first>
    - Views/Admin/ManageAssessment.cshtml (full file post Task 1-3 untuk syntax sanity, especially closing `<script>` tag dan brace balance)
    - wwwroot/css/site.css (full file post Task 1 untuk closing brace dan `}` count)
  </read_first>
  <action>
Run build dan basic syntax sanity gate sebelum hand off ke human UAT.

1. **Build gate:** Run `dotnet build -c Release` from project root. Expected: 0 errors, ≤92 warnings (Phase 309 baseline). Kalau warnings >92: list new warnings dan fail task.

2. **JS syntax sanity:** Run `node --check Views/Admin/ManageAssessment.cshtml` — TIDAK applicable (Razor syntax akan invalid for node). Sebagai gantinya:
   - Verify brace balance via grep count: `grep -c '^document\.body\.addEventListener' Views/Admin/ManageAssessment.cshtml` should return 4-5 (dynamic-tab-listener, filter-form afterSwap, responseError, sendError, retry click — minimum 4 documented + tab activation listener counted separately).
   - Verify no obvious syntax break: ekstrak section script lalu peek closing `</script>` tag balance.

3. **CSS syntax sanity:** Verify all braces balanced di rule baru:
   - `grep -c "^}" wwwroot/css/site.css` (closing braces count)
   - `grep -c "{$" wwwroot/css/site.css` (opening braces count, line-end)
   - Kedua count harus equal (basic sanity, not strict CSS parser).

4. **Smoke run lokal (read-only sanity, no UAT):** Start `dotnet run` background dan curl shell document:
   ```bash
   curl -k -s -o /dev/null -w "HTTP %{http_code} size=%{size_download}\n" https://localhost:5001/KPB-PortalHC/Admin/ManageAssessment
   ```
   Expected: HTTP 200 atau 302 redirect to login (kalau tidak auth). Kalau HTTP 500 → fail task, periksa server log untuk Razor render error (kemungkinan typo di Task 2/3).

5. **Stop dotnet run** (kalau di-start) sebelum hand off Task 5.

Acceptance: build green, brace balanced, smoke HTTP 200/302.

Catatan: Test 4 (perf wifi kantor) BUKAN scope task ini — di-defer ke webdev deploy oleh IT setelah Plan 04 merge. Localhost perf metrics meaningless per UAT environment matrix.
  </action>
  <verify>
    <automated>dotnet build -c Release 2>&1 | tee /tmp/311-04-build.log | tail -10 && grep -E "^\s*[0-9]+ Warning" /tmp/311-04-build.log | head -1 && grep -qE "0 Error" /tmp/311-04-build.log && echo BUILD_PASS</automated>
  </verify>
  <done>
    Build exit 0 (0 errors). Warnings count printed dan ≤92 (Phase 309 baseline preserved — kalau >92, list delta dan justify atau fix). CSS opening dan closing brace count equal. Smoke curl HTTP 200/302 (no 500 render error). Hand off ke human UAT (Task 5).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Re-UAT manual lokal — Test 1, Test 2, Test 5 retry</name>
  <files>(verification only, no file edits)</files>
  <action>Pause untuk human UAT manual. Lihat <how-to-verify> untuk 5 step verifikasi.</action>
  <verify>Human reply sesuai resume-signal format.</verify>
  <done>User reply "uat passed local" atau "uat failed [step] [observation]".</done>
  <what-built>
    BUG-1 fix: CSS rule hide legacy filter rows di 3 partial views (D-10 preserved — partial files unchanged).
    BUG-2A fix: htmx:afterSwap listener scoped ke filter-form provenance check.
    BUG-2B fix: restored hx-trigger drop `once` modifier (re-fire setiap shown.bs.tab post-invalidation).
    BUG-5A fix: retry handler pakai htmx.ajax('GET', url, {target, swap, source}) direct.
    Build: 0 errors, warnings ≤92 (Phase 309 baseline).
  </what-built>
  <how-to-verify>
**Pre-requisite:** `dotnet run` di lokal (port 5001 default https). Buka Chrome/Edge DevTools sebelum tiap test untuk inspect Network + Console.

**Step 1 — Test 1 BUG-1 verification (~2 menit):**
- Buka https://localhost:5001/KPB-PortalHC/Admin/ManageAssessment
- Tab Assessment auto-aktif → tunggu skeleton swap ke tabel real
- Inspect dengan DevTools Elements: pastikan HANYA 1 row filter visible (shell `#filter-form`). Filter row dari `_AssessmentGroupsTab.cshtml` (search input + category/status dropdowns) HARUS ter-hide via CSS — Computed style `display: none`.
- Klik tab "Input Records" (Training) → tunggu swap. Pastikan filter card `bg-light` dari partial Training TIDAK visible.
- Klik tab "History" → tunggu swap. Pastikan filter row di sub-tab Riwayat Assessment TIDAK visible.
- Verifikasi visual: TIDAK ADA filter form duplikat di 3 tab.

**Step 2 — Test 2 BUG-2A + BUG-2B verification (~3 menit):**
- Refresh halaman (clean state).
- DevTools Network tab → buka, biarkan recording.
- Klik tab "Input Records" pertama kali → catat: 1 request fired ke `/ManageAssessmentTab_Training`.
- Klik tab "History" pertama kali → catat: 1 request fired ke `/ManageAssessmentTab_History`.
- Klik balik tab "Input Records" → **EXPECTED: TIDAK ada request baru** (cached, no skeleton, content instant).
- Sekarang ubah search box di shell filter (ketik "test" di `#filter-search`). Tunggu 500ms debounce.
- Catat: request fired ke endpoint tab aktif (Training kalau lagi di Training).
- Klik tab "Assessment" → **EXPECTED: ada request fired ke `/ManageAssessmentTab_Assessment` dengan param search=test** (cross-tab invalidation berhasil — wrapper di-reset ke skeleton, shown.bs.tab event re-fire dengan filter terbaru).
- Klik tab "History" → **EXPECTED: ada request fired ke `/ManageAssessmentTab_History` dengan param search=test**.
- Pastikan TIDAK ada tab stuck di skeleton "Memuat data".

**Step 3 — Test 5 BUG-5A verification (~2 menit):**
- Refresh halaman. DevTools Network tab → Throttling dropdown → pilih "Offline".
- Klik tab "Input Records" (yang belum di-fetch) → **EXPECTED: error template muncul** dengan heading "Gagal memuat data" + tombol "Coba Lagi".
- DevTools Network → Throttling kembali ke "No throttling" (online).
- Klik tombol "Coba Lagi" → **EXPECTED: skeleton muncul → 1 request baru fired di Network → swap dengan tabel real**.
- Pastikan tombol "Coba Lagi" benar-benar fire request (BUG-5A pre-fix: 0 request fired).

**Step 4 — Race condition smoke (~1 menit):**
- Refresh halaman.
- Cepat klik antar tab (Assessment → Training → History → Assessment dalam <2 detik).
- DevTools Network: lihat request previous status "cancelled" (hx-sync auto-cancel) bukan "complete-then-replace".
- Visual: tidak ada flicker / duplicate render.

**Step 5 — Server log sanity (optional, ~30 detik):**
- Cek server console output: untuk tiap partial endpoint hit, ada log line `ManageAssessment perf [tab=...]: elapsed=...ms` (Stopwatch instrumentation D-09 preserved — pastikan TIDAK regress).

**Reply format:**
- "uat passed local" → Plan 04 close, hand off ke webdev deploy untuk Test 4 (perf wifi kantor)
- "uat failed [step] [observation]" → revisit task terkait + iterate
  </how-to-verify>
  <resume-signal>Reply "uat passed local" atau "uat failed [step N] [observation]"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → admin endpoint | Auth boundary: `[Authorize(Roles="Admin,HC")]` di partial actions (preserved dari Plan 02, no changes). |
| Static assets (CSS) | No trust boundary: `wwwroot/css/site.css` di-serve via static middleware, no user input. |
| Inline JS handler | No trust boundary: code di Razor view, no eval/innerHTML dari user input — innerHTML strings 100% literal/programmatic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-311.04-01 | I (Information Disclosure) | CSS hide rule pada partial filter | accept | Filter row hanya di-hide visual via `display:none`. DOM tetap render, content masih di HTML response. Tidak ada rahasia di filter row markup (hanya search box + dropdown values yang sama dengan shell filter). Risk: LOW. |
| T-311.04-02 | T (Tampering) | Retry handler `htmx.ajax('GET', url)` URL source | mitigate | URL diambil dari `wrapper.getAttribute('hx-get')` yang di-set Razor via `@Url.Action(...)` (server-controlled). User TIDAK bisa inject URL via DOM manipulation lebih dari yang sudah bisa via DevTools (DevTools attack out of scope ASVS L1 untuk admin tool). |
| T-311.04-03 | I | Filter-form provenance check (`closest('#filter-form')`) | accept | Provenance check via DOM closest() — attacker yang punya XSS bisa bypass, tapi XSS itu sendiri lebih severe issue. Mitigation chain: Razor encoding default + `[Authorize]` di endpoints + admin-only audience. ASVS L1 acceptable. |
| T-311.04-04 | D (Denial of Service) | Drop `once` modifier → wrapper re-fire setiap shown.bs.tab | accept | Tanpa `once`, click berkali-kali pada tab yang sama akan trigger fetch berulang. `hx-sync="this:replace"` (preserved dari Plan 02) auto-cancel previous request. Backend partial action cepat (<100ms warm DB). User-induced DoS terhadap diri sendiri only. ASVS L1 acceptable. |

**Disposition summary:** 0 mitigate-required (all LOW/INFO), 4 accept dengan justifikasi. ASVS L1 block-on-HIGH gate: PASS (no HIGH severity).
</threat_model>

<verification>
## Phase-level checks (post-Task 4)

```bash
# 1. CSS marker present
grep -q "Phase 311.1 gap closure" wwwroot/css/site.css

# 2. JS handler scoped to filter-form
grep -q "triggerElt.closest('#filter-form')" Views/Admin/ManageAssessment.cshtml

# 3. once modifier dropped from restored trigger
! grep -q "from:#tab-' + tabName + ' once'" Views/Admin/ManageAssessment.cshtml

# 4. retry uses htmx.ajax direct
grep -qE "htmx\.ajax\(\s*'GET'" Views/Admin/ManageAssessment.cshtml

# 5. retry handler does NOT call htmx.trigger(wrapper, 'load')
! grep -q "htmx.trigger(wrapper, 'load')" Views/Admin/ManageAssessment.cshtml

# 6. Build green
dotnet build -c Release | grep -qE "0 Error"

# 7. D-10 partial views unchanged
git diff --name-only HEAD~1 HEAD | ! grep -E "Views/Admin/Shared/_(AssessmentGroupsTab|TrainingRecordsTab|HistoryTab)\.cshtml"
```
</verification>

<success_criteria>
- [ ] BUG-1 fix: CSS marker `Phase 311.1 gap closure` ada di `wwwroot/css/site.css`; 4 selector rules present; D-10 preserved (3 partial files unchanged).
- [ ] BUG-2A fix: `triggerElt.closest('#filter-form')` provenance guard ada di handler; tab activation swap TIDAK trigger invalidation.
- [ ] BUG-2B fix: restored hx-trigger TIDAK punya ` once` suffix; tab non-aktif refire saat shown.bs.tab post-invalidation.
- [ ] BUG-5A fix: retry handler pakai `htmx.ajax('GET', url, {target, swap, source})`; klik "Coba Lagi" fire fresh request.
- [ ] Build hijau: 0 errors, warnings ≤92 (Phase 309 baseline).
- [ ] Manual UAT lokal Test 1 + Test 2 + Test 5 PASS (Step 5 Task 5 reply "uat passed local").
- [ ] D-10 backward compat: 3 partial views (_AssessmentGroupsTab, _TrainingRecordsTab, _HistoryTab) TIDAK ter-modify.
- [ ] D-09 Stopwatch instrumentation TIDAK regress (server log per partial endpoint masih emit `ManageAssessment perf [tab=...]: elapsed=...ms`).
</success_criteria>

<output>
After completion (Task 5 user reply "uat passed local"), create `.planning/phases/311-manageassessment-performance/311-04-SUMMARY.md` dengan:
- 4 BUG closure status (all closed)
- Files modified: 2 (Views/Admin/ManageAssessment.cshtml, wwwroot/css/site.css)
- Files unchanged (D-10 preserve): 3 partial views
- Build verdict: 0 errors, X warnings (note actual count)
- UAT result: Test 1 PASS, Test 2 PASS, Test 5 PASS
- Test 4 (perf wifi kantor): deferred ke webdev deploy by IT
- Handoff: Plan 02 paused-at-checkpoint dapat dilanjutkan setelah Test 4 wifi kantor PASS
</output>
