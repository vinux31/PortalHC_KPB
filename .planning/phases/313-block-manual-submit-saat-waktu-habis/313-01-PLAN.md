---
phase: 313-block-manual-submit-saat-waktu-habis
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/seeds/313-timer-fixtures.sql
  - tests/e2e/exam-taking.spec.ts
  - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md
autonomous: true
requirements: [TMR-01]
must_haves:
  truths:
    - "Tester punya 7 fixture AssessmentSessions dengan title pattern `Phase 313 Timer Fixture {Type} {Scenario}` di DB lokal"
    - "Suite Playwright `FLOW 313` berisi 7 test (313.1..313.7) dengan `test.skip` graceful kalau fixture absent"
    - "313-UAT.md berisi 7-step manual checklist Bahasa Indonesia + DB AuditLog SQL spot-check"
    - "Suite Playwright run tanpa fixture → 7 test SKIP (bukan FAIL) — sebelum Plan 02/03 implementasi"
  artifacts:
    - path: ".planning/seeds/313-timer-fixtures.sql"
      provides: "SQL script seed 7 fixture untuk Wave 0 (idempotent)"
      contains: "Phase 313 Timer Fixture"
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "FLOW 313 describe block (7 tests, masih RED/SKIP state)"
      contains: "FLOW 313"
    - path: ".planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md"
      provides: "Manual UAT checklist 7 langkah + AuditLog SQL spot-check"
      contains: "SubmitExamBlocked"
  key_links:
    - from: "tests/e2e/exam-taking.spec.ts FLOW 313"
      to: ".planning/seeds/313-timer-fixtures.sql"
      via: "fixture title `Phase 313 Timer Fixture {Type} {Scenario}` (D-08)"
      pattern: "Phase 313 Timer Fixture"
    - from: "313-UAT.md"
      to: ".planning/seeds/313-timer-fixtures.sql"
      via: "Pre-conditions section reference SQL script run dulu"
      pattern: "313-timer-fixtures\\.sql"
user_setup: []
---

<objective>
Wave 0 — siapkan test infrastructure (Playwright FLOW 313 RED state + SQL seed 7 fixture + 313-UAT.md manual checklist) **sebelum** kode produksi backend/frontend dimulai. Tujuan: setiap implementasi task di Plan 02/03 punya verifikasi `<automated>` yang konkret + manual UAT untuk closure.

Purpose: Per Phase 312 Path B precedent, .NET unit test project TIDAK ADA di repo (verified via 313-PATTERNS.md `## No Analog Found` line 862-864). Coverage closure phase 313 = Playwright FLOW 313 supplemental + manual UAT primary. Wave 0 menjamin RED state: 7 test SKIP graceful sampai fixture seed dijalankan.

Output:
- `.planning/seeds/313-timer-fixtures.sql` — SQL script seed 7 fixture (idempotent, schema-verified)
- `tests/e2e/exam-taking.spec.ts` — append FLOW 313 describe block (7 test 313.1..313.7) dengan `test.skip` graceful pattern
- `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md` — manual UAT 7-step Bahasa Indonesia + DB SQL AuditLog spot-check
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-RESEARCH.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-VALIDATION.md
@.planning/phases/312-admin-full-delete-assessment-room/312-UAT.md
@tests/e2e/assessment.spec.ts
@tests/helpers/accounts.ts
@Models/AssessmentSession.cs
@Models/AssessmentConstants.cs
@CLAUDE.md

<interfaces>
<!-- Critical references — executor pakai langsung, no codebase scavenger hunt -->

From Models/AssessmentConstants.cs:5-11 [VERIFIED]:
```csharp
public static class AssessmentType
{
    public const string Manual = "Manual";
    public const string Online = "Online";
    public const string PreTest = "PreTest";
    public const string PostTest = "PostTest";
}
```

From Models/AssessmentSession.cs (verified field names):
- `Title` (string)
- `Category` (string)
- `UserId` (string FK → AspNetUsers)
- `ScheduleId` (int FK → Schedules)
- `AssessmentType` (string nullable, line 154 — C-01 OVERRIDE: bukan `Type`)
- `DurationMinutes` (int, line 19)
- `ExtraTimeMinutes` (int? nullable, line 192)
- `StartedAt` (DateTime? nullable, line 40 — D-07 back-date target)
- `Status` (string)

From tests/helpers/accounts.ts:4 [VERIFIED]:
```typescript
coachee: { email: 'rino.prasetyo@pertamina.com', password: '123456' }
```

From tests/e2e/assessment.spec.ts:584-595 [VERIFIED Phase 312 FLOW 12 template]:
```typescript
test.describe('Assessment - Phase 312 Admin Full-Delete Role Guard', () => {
  // tidak pakai beforeEach login — tiap test login berbeda role
});

// Pattern test.skip graceful (line 728):
const targetRow = page.locator('tr', { hasText: 'Phase 312 HC Block Fixture' }).first();
if (await targetRow.count() === 0) test.skip(true, 'Seed "..." tidak ditemukan — Wave 1 manual seed required');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .planning/seeds/313-timer-fixtures.sql with 7 fixture inserts (idempotent)</name>

  <files>.planning/seeds/313-timer-fixtures.sql</files>

  <read_first>
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md (section 5 SQL seed skeleton line 609-676)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md (D-07/D-08 + Claude's Discretion fixture seed)
    - Models/AssessmentSession.cs (full file — verify required vs nullable fields, default values)
    - Models/AssessmentConstants.cs (AssessmentType + AssessmentStatus constants — anti-magic-string)
    - tests/helpers/accounts.ts (coachee email = rino.prasetyo@pertamina.com)
  </read_first>

  <behavior>
    - File harus idempotent — re-run tanpa duplicate (DELETE-by-prefix pattern atau IF NOT EXISTS guard)
    - 7 fixture rows dengan title pattern persis: `Phase 313 Timer Fixture {Type} {Scenario}`
    - StartedAt back-dated sesuai matrix below
    - UserId NOT NULL — pakai subquery `(SELECT TOP 1 Id FROM AspNetUsers WHERE Email='rino.prasetyo@pertamina.com')` (per anti-pattern Phase 309 UserId=NULL FK violation)
    - Final SELECT untuk verify hasil (tampilkan ElapsedMinutes computed)
  </behavior>

  <action>
    Tulis SQL script di `.planning/seeds/313-timer-fixtures.sql` dengan struktur:

    1. **Header comment block** (Bahasa Indonesia):
       - Phase ref + REQ TMR-01
       - Cara run: SSMS / sqlcmd terhadap DB lokal
       - Idempotent note
       - Pre-condition: user `rino.prasetyo@pertamina.com` ada + minimal 1 Schedule active

    2. **DECLARE block** (vars):
       ```sql
       DECLARE @UserId NVARCHAR(450) = (SELECT TOP 1 Id FROM AspNetUsers WHERE Email = 'rino.prasetyo@pertamina.com');
       DECLARE @ScheduleId INT = (SELECT TOP 1 Id FROM Schedules ORDER BY Date DESC);
       DECLARE @Now DATETIME2 = SYSUTCDATETIME();
       ```
       Add validation: `IF @UserId IS NULL THROW 50001, 'User rino.prasetyo@pertamina.com tidak ditemukan — abort.', 1;`
       Add validation: `IF @ScheduleId IS NULL THROW 50002, 'Tidak ada Schedule active — abort.', 1;`

    3. **Idempotent cleanup**: `DELETE FROM AssessmentSessions WHERE Title LIKE 'Phase 313 Timer Fixture%';`

    4. **7 INSERT statements** — title + AssessmentType + StartedAt offset matrix:

       | # | Title | AssessmentType | DurationMin | ExtraTimeMin | StartedAt offset (from @Now) | Expected |
       |---|-------|----------------|-------------|--------------|------------------------------|----------|
       | 1 | `Phase 313 Timer Fixture Online ManualBeforeTime` | `'Online'` | 60 | NULL | `DATEADD(MINUTE, -5, @Now)` | Submit OK |
       | 2 | `Phase 313 Timer Fixture Online ManualAfterGrace` | `'Online'` | 60 | NULL | `DATEADD(MINUTE, -61, @Now)` | Tier-1 BLOCK |
       | 3 | `Phase 313 Timer Fixture Online AutoInGrace` | `'Online'` | 60 | NULL | `DATEADD(MINUTE, -61, @Now)` | Submit OK (Tier-2 grace) |
       | 4 | `Phase 313 Timer Fixture Online AutoAfterGrace` | `'Online'` | 60 | NULL | `DATEADD(MINUTE, -67, @Now)` | Tier-2 BLOCK |
       | 5 | `Phase 313 Timer Fixture PreTest ManualAfterGrace` | `'PreTest'` | 60 | NULL | `DATEADD(MINUTE, -61, @Now)` | Tier-1 BLOCK |
       | 6 | `Phase 313 Timer Fixture PostTest ManualAfterGrace` | `'PostTest'` | 60 | NULL | `DATEADD(MINUTE, -61, @Now)` | Tier-1 BLOCK |
       | 7 | `Phase 313 Timer Fixture Manual ExcludeVerify` | `'Manual'` | 60 | NULL | `DATEADD(MINUTE, -161, @Now)` | Submit OK (D-15 exclude) |

       Use AssessmentType string literal `'Online'` / `'PreTest'` / `'PostTest'` / `'Manual'` (SQL tidak akses C# constants, magic-string OK di SQL — tapi harus match `AssessmentConstants.AssessmentType.*` values exactly).

       Status untuk semua fixture: `'InProgress'` (cek `Models/AssessmentConstants.cs` AssessmentStatus jika ada; kalau tidak yakin pakai literal `'InProgress'`).

       Category: `'Test Phase 313'` untuk semua.

       CreatedAt: `@Now`.

       Required fields lain di AssessmentSession (cek `Models/AssessmentSession.cs` full read): set sesuai default reasonable. Kalau `IsTokenRequired bit not null default 0`, set `0`. Kalau `Score`/`PassingScore` ada nullable, set NULL. Pattern: open file Models/AssessmentSession.cs full, list required (NOT NULL no default) fields, set explicit value setiap.

    5. **Final verification SELECT**:
       ```sql
       SELECT
         Id, Title, AssessmentType, StartedAt, DurationMinutes, ISNULL(ExtraTimeMinutes, 0) AS ExtraTimeMinutes,
         DATEDIFF(MINUTE, StartedAt, SYSUTCDATETIME()) AS ElapsedMinutes,
         Status
       FROM AssessmentSessions
       WHERE Title LIKE 'Phase 313 Timer Fixture%'
       ORDER BY Title;
       ```

    Bahasa: comment header dan THROW message Bahasa Indonesia (per CLAUDE.md). Field names + SQL keywords English (SQL Server convention).
  </action>

  <verify>
    <automated>test -f ".planning/seeds/313-timer-fixtures.sql" && grep -c "Phase 313 Timer Fixture" .planning/seeds/313-timer-fixtures.sql</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/seeds/313-timer-fixtures.sql` exists
    - File berisi 7 occurrence string `Phase 313 Timer Fixture` (1 di header comment + 7 INSERT title + 1 cleanup + 1 verify SELECT WHERE = boleh > 7, minimum 8)
    - File berisi exact title `Phase 313 Timer Fixture Online ManualBeforeTime`
    - File berisi exact title `Phase 313 Timer Fixture Online ManualAfterGrace`
    - File berisi exact title `Phase 313 Timer Fixture Online AutoInGrace`
    - File berisi exact title `Phase 313 Timer Fixture Online AutoAfterGrace`
    - File berisi exact title `Phase 313 Timer Fixture PreTest ManualAfterGrace`
    - File berisi exact title `Phase 313 Timer Fixture PostTest ManualAfterGrace`
    - File berisi exact title `Phase 313 Timer Fixture Manual ExcludeVerify`
    - File berisi `DELETE FROM AssessmentSessions WHERE Title LIKE 'Phase 313 Timer Fixture%'` (idempotent cleanup)
    - File TIDAK berisi `UserId = NULL` literal (FK violation prevention per Phase 309 anti-pattern)
    - File berisi subquery `SELECT TOP 1 Id FROM AspNetUsers WHERE Email = 'rino.prasetyo@pertamina.com'`
    - File berisi `THROW 50001` atau `THROW 50002` (validation guard kalau UserId/ScheduleId null)
    - File berisi 4 distinct AssessmentType values: `'Online'`, `'PreTest'`, `'PostTest'`, `'Manual'`
  </acceptance_criteria>

  <done>
    File `.planning/seeds/313-timer-fixtures.sql` ada di repo, 7 fixture title persis matching pattern, idempotent cleanup + validation guard tersedia. NB: file BELUM dijalankan di DB di task ini — eksekusi DB adalah pre-condition saat tester/UAT operator jalankan manual sebelum FLOW 313 run.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append FLOW 313 describe block (7 tests, RED/SKIP state) ke tests/e2e/exam-taking.spec.ts</name>

  <files>tests/e2e/exam-taking.spec.ts</files>

  <read_first>
    - tests/e2e/exam-taking.spec.ts (full file — confirm imports + existing test structure)
    - tests/e2e/assessment.spec.ts:584-757 (FLOW 12 Phase 312 template — describe block, test.skip graceful, fixture title selector, login per-test)
    - tests/helpers/auth.ts (login signature)
    - tests/helpers/accounts.ts (coachee credentials)
    - tests/helpers/utils.ts (helper functions available)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md (section 4 line 495-606 — FLOW 313 full skeleton)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-RESEARCH.md (Pitfall 5 — selector substring match mitigation)
  </read_first>

  <behavior>
    - Append (bukan replace) FLOW 313 describe block ke akhir file `tests/e2e/exam-taking.spec.ts`
    - 7 test functions (313.1 .. 313.7), masing-masing pakai `test.skip` graceful kalau fixture row tidak ditemukan di `/CMP/Assessment` page
    - Tidak ada `test.beforeEach` global — tiap test login coachee per-test (mirror FLOW 12 line 595)
    - Selector pakai exact-match regex untuk hindari substring-match bug (Pitfall 5 — Phase 312 WR-03)
    - Saat fixture absent → all 7 tests SKIP (Wave 0 RED state). After Plan 02/03 + DB seed run → tests transisi ke GREEN.
    - Imports preserved (jika file punya `import { test, expect } from '@playwright/test'` + helper imports, jangan duplikat).
  </behavior>

  <action>
    Buka `tests/e2e/exam-taking.spec.ts` full read terlebih dahulu untuk lihat:
    - Existing imports (top of file)
    - Existing describe blocks (jangan modify)
    - Akhir file (tempat append)

    Append di akhir file (sebelum EOF):

    ```typescript
    // ============================================================
    // FLOW 313: Phase 313 — Block Manual Submit Saat Waktu Habis
    // REQ: TMR-01 (7 success criteria)
    // 313.1 — Manual + before-time + Online → submit OK (regression)
    // 313.2 — Manual + after-time (in grace) + Online → BLOCKED + AuditLog SubmitExamBlocked + redirect
    // 313.3 — Auto + after-time (in grace) + Online → submit OK (Tier 2 grace covers)
    // 313.4 — Auto + after-grace + Online → BLOCKED Tier 2 (existing preserved)
    // 313.5 — Manual + after-time + PreTest → BLOCKED (3 timer types verify)
    // 313.6 — Manual + after-time + PostTest → BLOCKED
    // 313.7 — Manual + after-time + Manual type → submit OK (D-15 exclude verify)
    // Wave 0: semua test SKIP graceful jika fixture .planning/seeds/313-timer-fixtures.sql belum dijalankan.
    // ============================================================
    test.describe('Exam Taking - Phase 313 Block Manual Submit', () => {

      test('313.1 - Manual + before-time + Online → submit OK (regression)', async ({ page }) => {
        await login(page, 'coachee');
        const fixtureTitle = 'Phase 313 Timer Fixture Online ManualBeforeTime';
        await page.goto('/CMP/Assessment');
        const targetRow = page.locator('tr', { hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm') }).first();
        if (await targetRow.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — Wave 0 manual seed required (jalankan .planning/seeds/313-timer-fixtures.sql)`);
        }
        // Plan 03 implementasi finalisasi flow assertion. Wave 0 placeholder:
        await expect(targetRow).toBeVisible();
      });

      test('313.2 - Manual + after-time (in grace) + Online → BLOCKED + AuditLog SubmitExamBlocked', async ({ page }) => {
        await login(page, 'coachee');
        const fixtureTitle = 'Phase 313 Timer Fixture Online ManualAfterGrace';
        await page.goto('/CMP/Assessment');
        const targetRow = page.locator('tr', { hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm') }).first();
        if (await targetRow.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — Wave 0 manual seed required`);
        }
        // Expected: redirect StartExam dengan TempData D-01 banner.
        // AuditLog verify via DB SQL spot-check di Manual UAT (313-UAT.md).
        await expect(targetRow).toBeVisible();
      });

      test('313.3 - Auto + after-time (in grace) + Online → submit OK (Tier 2 grace covers)', async ({ page }) => {
        await login(page, 'coachee');
        const fixtureTitle = 'Phase 313 Timer Fixture Online AutoInGrace';
        await page.goto('/CMP/Assessment');
        const targetRow = page.locator('tr', { hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm') }).first();
        if (await targetRow.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — Wave 0 manual seed required`);
        }
        await expect(targetRow).toBeVisible();
      });

      test('313.4 - Auto + after-grace + Online → BLOCKED Tier 2 (existing preserved)', async ({ page }) => {
        await login(page, 'coachee');
        const fixtureTitle = 'Phase 313 Timer Fixture Online AutoAfterGrace';
        await page.goto('/CMP/Assessment');
        const targetRow = page.locator('tr', { hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm') }).first();
        if (await targetRow.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — Wave 0 manual seed required`);
        }
        await expect(targetRow).toBeVisible();
      });

      test('313.5 - Manual + after-time + PreTest → BLOCKED (Tier-1)', async ({ page }) => {
        await login(page, 'coachee');
        const fixtureTitle = 'Phase 313 Timer Fixture PreTest ManualAfterGrace';
        await page.goto('/CMP/Assessment');
        const targetRow = page.locator('tr', { hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm') }).first();
        if (await targetRow.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — Wave 0 manual seed required`);
        }
        await expect(targetRow).toBeVisible();
      });

      test('313.6 - Manual + after-time + PostTest → BLOCKED (Tier-1)', async ({ page }) => {
        await login(page, 'coachee');
        const fixtureTitle = 'Phase 313 Timer Fixture PostTest ManualAfterGrace';
        await page.goto('/CMP/Assessment');
        const targetRow = page.locator('tr', { hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm') }).first();
        if (await targetRow.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — Wave 0 manual seed required`);
        }
        await expect(targetRow).toBeVisible();
      });

      test('313.7 - Manual + after-time + Manual type → submit OK (D-15 exclude verify)', async ({ page }) => {
        await login(page, 'coachee');
        const fixtureTitle = 'Phase 313 Timer Fixture Manual ExcludeVerify';
        await page.goto('/CMP/Assessment');
        const targetRow = page.locator('tr', { hasText: new RegExp(`^\\s*${escapeRegex(fixtureTitle)}\\s*`, 'm') }).first();
        if (await targetRow.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — Wave 0 manual seed required`);
        }
        // Manual type harus PASS guard (D-15 exclude). AuditLog SubmitExamBlocked TIDAK boleh exist untuk fixture ini.
        await expect(targetRow).toBeVisible();
      });
    });

    // Helper untuk escape regex special chars (Pitfall 5 — Phase 312 WR-03 selector substring mitigation)
    function escapeRegex(s: string): string {
      return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    }
    ```

    NB: kalau file existing sudah punya `function escapeRegex` atau ada export dari `tests/helpers/utils.ts`, gunakan yang ada (jangan duplikat). Cek `tests/helpers/utils.ts` dulu — kalau tidak ada, append helper inline di akhir file spec (di luar `test.describe`).

    Cek imports di top file `tests/e2e/exam-taking.spec.ts`. Pastikan `login` (dari `../helpers/auth`) sudah di-import. Kalau belum, tambah import di top.

    Bahasa: comment header + skip messages Bahasa Indonesia (per CLAUDE.md).
  </action>

  <verify>
    <automated>cd tests && npx tsc --noEmit && npx playwright test --grep "Phase 313" --list</automated>
  </verify>

  <acceptance_criteria>
    - File `tests/e2e/exam-taking.spec.ts` berisi `test.describe('Exam Taking - Phase 313 Block Manual Submit'`
    - File berisi 7 occurrence pattern `test('313.[1-7] -` (313.1 sampai 313.7)
    - File berisi 7 distinct fixture title literals: `Phase 313 Timer Fixture Online ManualBeforeTime`, `... Online ManualAfterGrace`, `... Online AutoInGrace`, `... Online AutoAfterGrace`, `... PreTest ManualAfterGrace`, `... PostTest ManualAfterGrace`, `... Manual ExcludeVerify`
    - File berisi 7 occurrence `test.skip(true,` (graceful skip per test)
    - File berisi `escapeRegex` (helper inline atau import)
    - `cd tests && npx tsc --noEmit` exit 0 (TypeScript compile success)
    - `cd tests && npx playwright test --grep "Phase 313" --list` listing 7 tests
    - File TIDAK berisi `tr:has-text(\"Phase 313` substring-style (Pitfall 5 anti-pattern)
  </acceptance_criteria>

  <done>
    7 test 313.1..313.7 listed di `npx playwright test --grep "Phase 313" --list`. TypeScript compile sukses. Saat fixture seed BELUM dijalankan di DB lokal → 7 test SKIP graceful (RED state). Plan 03 akan finalisasi assertion bodies.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create 313-UAT.md manual checklist 7-step Bahasa Indonesia</name>

  <files>.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md</files>

  <read_first>
    - .planning/phases/312-admin-full-delete-assessment-room/312-UAT.md (template precedent — frontmatter, coverage matrix, step format, sign-off section)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-PATTERNS.md (section 6 line 680-787 — 313-UAT.md skeleton)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-CONTEXT.md (D-01 banner copy, D-05 AuditLog format, D-15 Manual exclude)
    - .planning/phases/313-block-manual-submit-saat-waktu-habis/313-RESEARCH.md (SC #1-7 acceptance criteria + AuditLog SQL spot-check pattern)
    - CLAUDE.md (Bahasa Indonesia mandate, Lokal verify SOP)
  </read_first>

  <behavior>
    - Markdown file Bahasa Indonesia, struktur mirror `312-UAT.md`
    - 7 step manual UAT (1:1 dengan FLOW 313 Playwright tests + 1 ekstra Manual exclude)
    - Setiap step: Pre-condition + Action + Expect + DB SQL spot-check (where applicable) + Sign-off checkbox
    - Coverage matrix di top + Pre-conditions section + footer Sign-Off Final
    - Step 2 (Tier-1 BLOCK) wajib include AuditLog SQL block lengkap
    - Step 7 (Manual exclude D-15) wajib NEGATIVE assertion (TIDAK ada AuditLog SubmitExamBlocked entry baru)
  </behavior>

  <action>
    Tulis file `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md` dengan struktur:

    ## 1. Header + Frontmatter (mirror 312-UAT.md line 1-9):
    ```markdown
    # Phase 313 — Manual UAT Script

    **Phase:** 313-block-manual-submit-saat-waktu-habis
    **REQ:** TMR-01 (Server menolak manual submit saat elapsed > Duration+ExtraTime; auto-submit grace 2min preserved; UI disable button + AuditLog blocked)
    **Created:** 2026-05-08
    **Mandatory before:** `/gsd-verify-work` Phase 313

    > Manual UAT cover 7 skenario smoke (6 timer matrix manual/auto × before/in-grace/after-grace + 1 Manual-type exclude verify D-15). Eksekusi di environment lokal (`http://localhost:5277/CMP/StartExam/{id}`). UAT di Dev/Prod adalah tanggung jawab Team IT (per CLAUDE.md DEV_WORKFLOW). Wave 0 fixtures via `.planning/seeds/313-timer-fixtures.sql`.
    ```

    ## 2. Coverage Matrix (table 7 row):
    ```markdown
    | Step | Skenario | AssessmentType | isAutoSubmit | StartedAt offset | Expected Result | Sign-off |
    |------|----------|----------------|--------------|-------------------|-----------------|----------|
    | 1 | Manual + before-time | Online | false | NOW − 5 min | Submit OK → grading + redirect Results | ⬜ |
    | 2 | Manual + after-time (in grace) | Online | false | NOW − 61 min (Duration=60) | **Tier-1 BLOCK** → TempData D-01 + redirect StartExam + AuditLog `SubmitExamBlocked` (Type=Online) | ⬜ |
    | 3 | Auto + after-time (in grace) | Online | true | NOW − 61 min | Submit OK (Tier-2 grace covers) → grading + redirect Results | ⬜ |
    | 4 | Auto + after-grace | Online | true | NOW − 67 min (Duration+Extra+5+) | **Tier-2 BLOCK** (existing preserved) → TempData "Pengiriman jawaban tidak dapat diproses" + redirect StartExam | ⬜ |
    | 5 | Manual + after-time + PreTest | PreTest | false | NOW − 61 min | **Tier-1 BLOCK** + AuditLog Type=PreTest | ⬜ |
    | 6 | Manual + after-time + PostTest | PostTest | false | NOW − 61 min | **Tier-1 BLOCK** + AuditLog Type=PostTest | ⬜ |
    | 7 | Manual + after-time + Manual type (D-15 exclude) | Manual | false | NOW − 161 min | Submit OK (no guard apply) → grading + redirect Results, **TIDAK** ada AuditLog SubmitExamBlocked baru | ⬜ |
    ```

    ## 3. Pre-conditions section:
    ```markdown
    ## Pre-conditions

    - App di-build & running lokal: `dotnet build` + `dotnet run` (cek `http://localhost:5277/`)
    - Login fixture: **Coachee:** `rino.prasetyo@pertamina.com / 123456` (per `tests/helpers/accounts.ts:4`)
    - Wave 0 fixtures: jalankan `.planning/seeds/313-timer-fixtures.sql` di SSMS / sqlcmd terhadap DB lokal — verify 7 row dengan title prefix `Phase 313 Timer Fixture` muncul
    - Browser modern + DevTools (Network tab — verify POST `/CMP/SubmitExam` redirect status)
    - DB akses (SSMS / DBeaver / sqlcmd) untuk verify AuditLog post-action via SQL spot-check
    - Pre-AuditLog snapshot baseline:
      ```sql
      SELECT COUNT(*) AS BaselineBlockedCount FROM AuditLogs WHERE ActionType = 'SubmitExamBlocked';
      ```
      Catat angka `BaselineBlockedCount` sebelum jalankan UAT — increment angka harus match jumlah Tier-1 BLOCK steps (Step 2, 5, 6 = 3 entries baru).
    ```

    ## 4. Step-by-step (7 step):

    Setiap step format identik:
    ```markdown
    ## Step N — {Skenario}

    **Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture {Type} {Scenario}` ada (StartedAt back-dated {N} min ago, Duration={X}).

    **Action:**
    1. ...
    2. ...

    **Expect:**
    - {Behavioral assertion}
    - {URL/banner assertion}
    - {Optional DB SQL block untuk AuditLog verify}

    **Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________
    ```

    Detail per step:

    **Step 1 — Manual + before-time + Online → Submit OK (regression):**
    - Action: navigasi `/CMP/Assessment`, klik fixture row "Phase 313 Timer Fixture Online ManualBeforeTime", klik "Mulai Ujian", isi minimal 1 jawaban, klik "Tinjau Jawaban" → ExamSummary → klik "Kumpulkan Ujian", confirm.
    - Expect: redirect ke `/CMP/Results/{id}`, status `Completed` di DB. Tidak ada AuditLog `SubmitExamBlocked` baru.

    **Step 2 — Manual + after-time (in grace) + Online → Tier-1 BLOCK** (CRITICAL):
    - Action: detail per `313-PATTERNS.md` section 6 step format line 758-783. Termasuk DevTools `removeAttribute('disabled')` workaround untuk simulasi user race click button yang seharusnya disabled.
    - Expect:
      - Redirect ke `/CMP/StartExam/{id}`
      - Banner danger berisi: `"Waktu ujian Anda sudah habis. Sistem akan otomatis mengirim jawaban Anda dalam beberapa detik. Mohon tunggu, jangan refresh halaman."` (D-01 verbatim)
      - DB SQL block:
        ```sql
        SELECT TOP 5 ActionType, Description, TargetId, TargetType, CreatedAt
        FROM AuditLogs
        WHERE ActionType = 'SubmitExamBlocked'
          AND TargetId = {fixtureSessionId}
        ORDER BY CreatedAt DESC;
        ```
      - Assert: 1+ row, `Description` LIKE `%Type=Online%` AND `%ElapsedMin=%` AND `%AllowedMin=60%` AND `%SessionId={fixtureSessionId}%`. `TargetType = 'AssessmentSession'`.

    **Step 3 — Auto + in-grace + Online → Submit OK:**
    - Action: navigasi StartExam fixture "AutoInGrace", tunggu countdown 00:00 (server compute D-12 → tampil 00:00 langsung), modal `timeUpWarningModal` muncul (info-only spinner per C-03), form auto-submit fire paralel ke ExamSummary → POST SubmitExam dengan `isAutoSubmit=true`.
    - Expect: Tier-2 grace 2min covers (elapsed 61min, allowed 60min, grace limit 62min) → submit success → redirect Results. AuditLog SubmitExamBlocked tidak bertambah.

    **Step 4 — Auto + after-grace + Online → Tier-2 BLOCK:**
    - Action: navigasi fixture "AutoAfterGrace", auto-submit fire, POST `/CMP/SubmitExam` dengan `isAutoSubmit=true`.
    - Expect: redirect StartExam, banner "Waktu ujian Anda telah habis. Pengiriman jawaban tidak dapat diproses." (existing tier-2 message preserved D-06).
    - NB: AuditLog SubmitExamBlocked TIDAK ditulis untuk Tier-2 (D-06 — hanya Tier-1 yang punya audit entry). Verify SQL: count BlockedCount tetap = baseline + 1 (dari Step 2 saja, tidak bertambah).

    **Step 5 — Manual + after-time + PreTest → Tier-1 BLOCK:**
    - Mirror Step 2 dengan AssessmentType=PreTest. Verify AuditLog Description contains `Type=PreTest`.

    **Step 6 — Manual + after-time + PostTest → Tier-1 BLOCK:**
    - Mirror Step 2 dengan AssessmentType=PostTest. Verify AuditLog Description contains `Type=PostTest`.

    **Step 7 — Manual + after-time + Manual type (D-15 exclude) → Submit OK:**
    - Action: navigasi fixture "Manual ExcludeVerify" (AssessmentType=Manual, StartedAt -161 min). Klik "Mulai Ujian" → ExamSummary → klik manual Submit "Kumpulkan Ujian".
    - Expect: submit success → redirect Results. **NEGATIVE assertion:**
      ```sql
      SELECT COUNT(*) AS BlockedForManualType
      FROM AuditLogs
      WHERE ActionType = 'SubmitExamBlocked'
        AND TargetId = {fixtureSessionId};
      ```
      `BlockedForManualType = 0` (D-15 verify — Manual type bypass guard).

    ## 5. Final Sign-Off section:
    ```markdown
    ## Final Sign-Off

    - [ ] Semua 7 step PASS (atau dokumentasi FAIL + remediation note)
    - [ ] Total AuditLog `SubmitExamBlocked` increment = 3 (Step 2, 5, 6) — verify via post-snapshot SQL count
    - [ ] Tester: _____________________ • Tanggal: _____________________ • Build commit: _____________________

    > Kalau ada FAIL → catat issue, diskusi dengan planner / executor sebelum `/gsd-verify-work`. Mirror Phase 312 Path B precedent.
    ```

    Bahasa: full Bahasa Indonesia. Field name + SQL keywords + AuditLog ActionType `SubmitExamBlocked` literal English (technical reference).
  </action>

  <verify>
    <automated>test -f ".planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md" && grep -c "Step [1-7]" .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md && grep -c "SubmitExamBlocked" .planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT.md` exists
    - File berisi minimum 7 occurrence pattern `Step [1-7]` (header per step)
    - File berisi minimum 4 occurrence string `SubmitExamBlocked` (Step 2 + 4 + 5 + 6 + 7 negative + final sign-off → ≥5; minimum 4)
    - File berisi exact D-01 message: `Waktu ujian Anda sudah habis. Sistem akan otomatis mengirim jawaban Anda dalam beberapa detik. Mohon tunggu, jangan refresh halaman.`
    - File berisi existing Tier-2 message: `Waktu ujian Anda telah habis. Pengiriman jawaban tidak dapat diproses.`
    - File berisi 7 distinct fixture title references (`Phase 313 Timer Fixture {Type} {Scenario}` 7 variations)
    - File berisi `## Pre-conditions` section dengan reference `.planning/seeds/313-timer-fixtures.sql`
    - File berisi `## Final Sign-Off` section dengan checkbox
    - File berisi minimum 3 SQL `SELECT` statement (baseline count + Step 2 spot-check + Step 7 negative assertion)
    - File berisi reference `D-15` (Manual exclude verify)
    - File berisi reference `D-01` atau D-01 message verbatim
    - File berisi reference `C-03` atau "spinner indicator" (modal info-only adjustment)
  </acceptance_criteria>

  <done>
    File `313-UAT.md` ada di phase directory, 7 step lengkap dengan AuditLog SQL spot-check, Bahasa Indonesia, mirror struktur 312-UAT.md. UAT siap dijalankan tester setelah Plan 02/03 selesai + DB seed run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture (DB) → Application | Seed SQL menulis langsung ke DB lokal (bypass app) — boundary test infrastructure → production code; risk hanya di env lokal/test |
| Wave 0 artifacts (test scripts) → Wave 1 implementation | Implementer Plan 02/03 baca FLOW 313 + UAT.md untuk understand expected behavior — knowledge transfer boundary |

## STRIDE Threat Register

Wave 0 plan (test infrastructure only) — minimal threat surface karena no production code modification. Threat berikut adalah informational / preventive untuk Plan 02/03 downstream.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313-W0-01 | Tampering | `.planning/seeds/313-timer-fixtures.sql` (DB direct INSERT) | accept | Test env only; idempotent cleanup `DELETE FROM AssessmentSessions WHERE Title LIKE 'Phase 313 Timer Fixture%'` mencegah orphan rows. Tester aware: jangan jalankan di Dev/Prod (CLAUDE.md DEV_WORKFLOW lock). |
| T-313-W0-02 | Information Disclosure | 313-UAT.md DB SQL block | accept | UAT.md committed ke repo — SQL examples generic (no real PII). Field references via `{fixtureSessionId}` placeholder, bukan hardcoded. Aman utk public repo per Phase 312 precedent. |
| T-313-W0-03 | Repudiation | Playwright test selectors substring match (Pitfall 5 — Phase 312 WR-03) | mitigate | Use `escapeRegex` + `^...$` exact regex match dalam `tr` selectors (per acceptance_criteria Task 2). Hindari naked `tr:has-text("Phase 313")` substring. |
</threat_model>

<verification>
- [ ] Task 1: `.planning/seeds/313-timer-fixtures.sql` ada, 7 fixture title benar, idempotent
- [ ] Task 2: `tests/e2e/exam-taking.spec.ts` berisi FLOW 313 describe block, TypeScript compile sukses, 7 test SKIP graceful
- [ ] Task 3: `313-UAT.md` ada, 7 step Bahasa Indonesia, SQL spot-check tersedia
- [ ] **Aggregate sanity:** `npx playwright test --grep "Phase 313" --list` returns 7 tests
- [ ] **Aggregate sanity:** Sebelum DB seed → `npx playwright test --grep "Phase 313"` = 7 SKIP, 0 FAIL (Wave 0 RED state correct)
</verification>

<success_criteria>
**Phase SC mapping (per ROADMAP):**
- SC #6 partial — fixture coverage 3 timer types (Online, PreTest, PostTest) + 1 Manual exclude (Wave 0 setup)
- SC #7 — E2E test 6 (+1 ekstra) skenario manual/auto × before/in-grace/after-grace listed di Playwright

**Plan-level SC (executor close criteria):**
1. SQL seed file 7 fixture, idempotent, schema-verified
2. FLOW 313 describe block 7 test, RED state graceful (no fail tanpa fixture)
3. 313-UAT.md 7-step Bahasa Indonesia + DB SQL spot-check
4. TypeScript compile sukses (`npx tsc --noEmit` exit 0)
5. NO production code modified (Plan 02/03 scope)
</success_criteria>

<output>
After completion, create `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-01-SUMMARY.md` mencakup:
- Tasks 1-3 outcomes (file paths created)
- Verification commands run + results
- Notes untuk Plan 02/03 executor: fixture seed perlu run sebelum FLOW 313 transisi GREEN
- Decisions implemented: D-07, D-08, fixture title pattern, anti-pattern Phase 309 mitigation (UserId valid subquery)
</output>
