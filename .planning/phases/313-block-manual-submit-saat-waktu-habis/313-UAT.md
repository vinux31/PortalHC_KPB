# Phase 313 — Manual UAT Script

**Phase:** 313-block-manual-submit-saat-waktu-habis
**REQ:** TMR-01 (Server menolak manual submit saat elapsed > Duration+ExtraTime; auto-submit grace 2min preserved; UI disable button + AuditLog blocked)
**Created:** 2026-05-08
**Mandatory before:** `/gsd-verify-work` Phase 313

> Manual UAT cover 7 skenario smoke (6 timer matrix manual/auto × before/in-grace/after-grace + 1 Manual-type exclude verify D-15). Eksekusi di environment lokal (`http://localhost:5277/CMP/StartExam/{id}`). UAT di Dev/Prod adalah tanggung jawab Team IT (per CLAUDE.md DEV_WORKFLOW). Wave 0 fixtures via `.planning/seeds/313-timer-fixtures.sql`.

## Coverage Matrix

| Step | Skenario | AssessmentType | isAutoSubmit | StartedAt offset | Expected Result | Sign-off |
|------|----------|----------------|--------------|-------------------|-----------------|----------|
| 1 | Manual + before-time | Online | false | NOW − 5 min | Submit OK → grading + redirect Results | ⬜ |
| 2 | Manual + after-time (in grace) | Online | false | NOW − 61 min (Duration=60) | **Tier-1 BLOCK** → TempData D-01 + redirect StartExam + AuditLog `SubmitExamBlocked` (Type=Online) | ⬜ |
| 3 | Auto + after-time (in grace) | Online | true | NOW − 61 min | Submit OK (Tier-2 grace covers) → grading + redirect Results | ⬜ |
| 4 | Auto + after-grace | Online | true | NOW − 67 min (Duration+Extra+5+) | **Tier-2 BLOCK** (existing preserved) → TempData "Pengiriman jawaban tidak dapat diproses" + redirect StartExam | ⬜ |
| 5 | Manual + after-time + PreTest | PreTest | false | NOW − 61 min | **Tier-1 BLOCK** + AuditLog Type=PreTest | ⬜ |
| 6 | Manual + after-time + PostTest | PostTest | false | NOW − 61 min | **Tier-1 BLOCK** + AuditLog Type=PostTest | ⬜ |
| 7 | Manual + after-time + Manual type (D-15 exclude) | Manual | false | NOW − 161 min | Submit OK (no guard apply) → grading + redirect Results, **TIDAK** ada AuditLog `SubmitExamBlocked` baru | ⬜ |

## Pre-conditions

- App di-build & running lokal: `dotnet build` + `dotnet run` (cek `http://localhost:5277/`)
- Login fixture: **Coachee:** `rino.prasetyo@pertamina.com / 123456` (per `tests/helpers/accounts.ts:4`)
- Wave 0 fixtures: jalankan `.planning/seeds/313-timer-fixtures.sql` di SSMS / sqlcmd terhadap DB lokal — verify 7 row dengan title prefix `Phase 313 Timer Fixture` muncul:
  ```sql
  SELECT s.Id, s.Title, s.AssessmentType, s.StartedAt,
         DATEDIFF(MINUTE, s.StartedAt, SYSUTCDATETIME()) AS ElapsedMinutes,
         (SELECT COUNT(*) FROM AssessmentPackages WHERE AssessmentSessionId = s.Id) AS PkgCount,
         (SELECT COUNT(*) FROM PackageQuestions pq INNER JOIN AssessmentPackages ap
              ON pq.AssessmentPackageId=ap.Id WHERE ap.AssessmentSessionId=s.Id) AS QCount
  FROM AssessmentSessions s
  WHERE s.Title LIKE 'Phase 313 Timer Fixture%'
  ORDER BY s.Title;
  ```
  Expected: 7 row, semua dengan `PkgCount=1` dan `QCount=3` (Phase 313.1 gap closure).

> **Phase 313.1 update (2026-05-08):** Seed sekarang self-contained (Package+Question+Option auto-INSERT). Sebelumnya UAT 7-step blocked karena fixture tidak punya AssessmentPackages → `/CMP/StartExam` redirect ke `/CMP/Assessment` dengan TempData "Sesi ujian ini tidak memiliki paket soal". Re-run seed setelah pull commit 313.1.
- Browser modern + DevTools (Network tab — verify POST `/CMP/SubmitExam` redirect status; Elements tab — untuk simulasi DevTools `removeAttribute('disabled')` saat tester perlu force-klik manual button yang seharusnya disabled C-03/D-03)
- DB akses (SSMS / DBeaver / sqlcmd) untuk verify AuditLog post-action via SQL spot-check
- Pre-AuditLog snapshot baseline:
  ```sql
  SELECT COUNT(*) AS BaselineBlockedCount
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked';
  ```
  Catat angka `BaselineBlockedCount` sebelum jalankan UAT — increment angka harus match jumlah Tier-1 BLOCK steps (Step 2, 5, 6 = 3 entries baru).

## Step 1 — Manual + before-time + Online → Submit OK (regression)

**Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture Online ManualBeforeTime` ada (StartedAt back-dated 5 min ago, Duration=60). Tester pastikan elapsed < 60 min.

**Action:**
1. Navigasi ke `/CMP/Assessment`, cari row "Phase 313 Timer Fixture Online ManualBeforeTime".
2. Klik "Mulai Ujian" → masuk StartExam, timer countdown menampilkan ~55 menit tersisa (server compute D-12).
3. Isi minimal 1 jawaban di soal pertama.
4. Klik "Tinjau Jawaban" → masuk ExamSummary review page.
5. Klik tombol "Kumpulkan Ujian" → confirm dialog → Yes.

**Expect:**
- Browser navigate ke `/CMP/Results/{id}`.
- Status di DB: `SELECT Status FROM AssessmentSessions WHERE Id={fixtureSessionId};` = `Completed` atau `Menunggu Penilaian`.
- TIDAK ada entri baru di AuditLog `SubmitExamBlocked` untuk SessionId ini:
  ```sql
  SELECT COUNT(*) AS BlockedForFixture
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked'
    AND TargetId = {fixtureSessionId};
  ```
  `BlockedForFixture = 0`.

**Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

## Step 2 — Manual + after-time (in grace) + Online → Tier-1 BLOCK (CRITICAL)

**Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture Online ManualAfterGrace` ada (StartedAt back-dated 61 min ago, Duration=60, ExtraTime=NULL → elapsed > allowed, dalam window grace 2min).

**Action:**
1. Navigasi `/CMP/Assessment`, cari row "Phase 313 Timer Fixture Online ManualAfterGrace".
2. Klik "Mulai Ujian" → masuk StartExam. Timer akan langsung tampil 00:00 (server compute D-12 — elapsed > Duration).
3. Modal `timeUpWarningModal` muncul (info-only spinner indicator, no "OK" button — per C-03).
4. Form auto-submit fire paralel ke ExamSummary (POST `/CMP/ExamSummary`). Tunggu redirect.
5. Di ExamSummary, render seharusnya `timerExpired=true` → tombol Submit utama menjadi **disabled** dengan label "Waktu Habis - Submit Otomatis Berjalan..." (D-03).
6. **Simulasi user race click manual:** buka DevTools (F12) → Elements → cari `#manualSubmitDisabledBtn` (atau button dengan label "Waktu Habis"), klik kanan → Edit attribute → hapus attribute `disabled`. Kemudian klik tombol manual.
7. Form submit fire dengan `isAutoSubmit=false` (hidden field). Karena ini simulasi user race, frontend retry handler tidak fire (this path sengaja bypass auto-submit).
8. POST `/CMP/SubmitExam` dengan `isAutoSubmit=false`. Server hit Tier-1 guard.

**Expect:**
- Redirect ke `/CMP/StartExam/{fixtureSessionId}`.
- Banner danger (alert-danger) muncul berisi pesan **VERBATIM** D-01: "Waktu ujian Anda sudah habis. Sistem akan otomatis mengirim jawaban Anda dalam beberapa detik. Mohon tunggu, jangan refresh halaman."
- DB SQL spot-check (PRIMARY VERIFY untuk SC #5):
  ```sql
  SELECT TOP 5 ActionType, Description, TargetId, TargetType, CreatedAt
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked'
    AND TargetId = {fixtureSessionId}
  ORDER BY CreatedAt DESC;
  ```
- Assertion:
  - 1+ row dengan `ActionType = 'SubmitExamBlocked'`
  - `Description` LIKE `%Type=Online%` AND LIKE `%ElapsedMin=%` AND LIKE `%AllowedMin=60%` AND LIKE `%SessionId={fixtureSessionId}%`
  - `TargetType = 'AssessmentSession'`
  - `TargetId = {fixtureSessionId}`
- Status fixture session di DB tetap `InProgress` (TIDAK auto-Completed):
  ```sql
  SELECT Status FROM AssessmentSessions WHERE Id={fixtureSessionId};
  ```

**Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

## Step 3 — Auto + after-time (in grace) + Online → Submit OK (Tier-2 grace covers)

**Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture Online AutoInGrace` ada (StartedAt back-dated 61 min ago, Duration=60, ExtraTime=NULL → elapsed 61min, allowed 60min, grace limit 62min — dalam window grace).

**Action:**
1. Navigasi `/CMP/Assessment`, cari row "Phase 313 Timer Fixture Online AutoInGrace".
2. Klik "Mulai Ujian" → timer countdown tampil 00:00 (server compute D-12).
3. Modal `timeUpWarningModal` muncul (info-only spinner per C-03).
4. Form auto-submit fire **paralel langsung** (no setTimeout delay per C-03) ke ExamSummary.
5. Di ExamSummary, hidden field `isAutoSubmit=true` (karena `timerExpired=true` di ViewBag). Auto-submit chain otomatis fire POST `/CMP/SubmitExam` dengan `isAutoSubmit=true` (tanpa user click manual).
6. Server Tier-1 skip (karena `isAutoSubmit=true`), Tier-2 check elapsed (61) > graceLimit (62)? → `false` → **lewat guard** → grading proceed.

**Expect:**
- Browser navigate ke `/CMP/Results/{fixtureSessionId}`.
- Status fixture: `Completed` atau `Menunggu Penilaian`.
- AuditLog `SubmitExamBlocked` count untuk SessionId ini tetap **0** (Tier-2 grace covers, no block):
  ```sql
  SELECT COUNT(*) AS BlockedForFixture
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked'
    AND TargetId = {fixtureSessionId};
  ```
  `BlockedForFixture = 0`.

**Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

## Step 4 — Auto + after-grace + Online → Tier-2 BLOCK (existing behavior preserved)

**Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture Online AutoAfterGrace` ada (StartedAt back-dated 67 min ago, Duration=60 → elapsed > grace limit 62, server harus reject).

**Action:**
1. Navigasi `/CMP/Assessment`, cari row "Phase 313 Timer Fixture Online AutoAfterGrace".
2. Klik "Mulai Ujian" → timer 00:00.
3. Modal info-only muncul, form auto-submit fire paralel ke ExamSummary.
4. ExamSummary auto-submit → POST `/CMP/SubmitExam` dengan `isAutoSubmit=true`.
5. Server Tier-1 skip, Tier-2 check elapsed (67) > graceLimit (62)? → `true` → **REJECT**.

**Expect:**
- Redirect ke `/CMP/StartExam/{fixtureSessionId}`.
- Banner danger berisi pesan **EXISTING preserved** (D-06): "Waktu ujian Anda telah habis. Pengiriman jawaban tidak dapat diproses."
- AuditLog `SubmitExamBlocked` untuk SessionId ini **TIDAK bertambah** (D-06: hanya Tier-1 yang punya audit entry, Tier-2 tidak):
  ```sql
  SELECT COUNT(*) AS BlockedForFixture
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked'
    AND TargetId = {fixtureSessionId};
  ```
  `BlockedForFixture = 0` (Tier-2 path tidak tulis Blocked entry).

**Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

## Step 5 — Manual + after-time + PreTest → Tier-1 BLOCK (3 timer types verify)

**Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture PreTest ManualAfterGrace` ada (AssessmentType=PreTest, StartedAt back-dated 61 min ago, Duration=60).

**Action:** Mirror Step 2 dengan AssessmentType=PreTest.
1. Navigasi `/CMP/Assessment`, klik fixture row, "Mulai Ujian" → ExamSummary auto-redirect.
2. DevTools `removeAttribute('disabled')` pada button manual Submit → klik manual.
3. POST `/CMP/SubmitExam` dengan `isAutoSubmit=false`.

**Expect:**
- Redirect StartExam dengan banner D-01.
- AuditLog `SubmitExamBlocked` entry baru:
  ```sql
  SELECT TOP 1 Description
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked'
    AND TargetId = {fixtureSessionId}
  ORDER BY CreatedAt DESC;
  ```
- `Description` LIKE `%Type=PreTest%` (verify D-14 — Type info embedded di Description metadata, ActionType uniform).

**Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

## Step 6 — Manual + after-time + PostTest → Tier-1 BLOCK

**Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture PostTest ManualAfterGrace` ada (AssessmentType=PostTest, StartedAt back-dated 61 min ago).

**Action:** Mirror Step 5 dengan AssessmentType=PostTest.

**Expect:**
- Redirect StartExam dengan banner D-01.
- AuditLog entry: `Description` LIKE `%Type=PostTest%`:
  ```sql
  SELECT TOP 1 Description
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked'
    AND TargetId = {fixtureSessionId}
  ORDER BY CreatedAt DESC;
  ```

**Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

## Step 7 — Manual + after-time + Manual type (D-15 exclude) → Submit OK

**Pre-condition:** Login coachee, fixture `Phase 313 Timer Fixture Manual ExcludeVerify` ada (AssessmentType=Manual, StartedAt back-dated 161 min ago — sangat lama lewat grace, tapi tipe Manual harus bypass guard per D-15).

**Action:**
1. Navigasi `/CMP/Assessment`, klik fixture row "Phase 313 Timer Fixture Manual ExcludeVerify".
2. Klik "Mulai Ujian" → masuk StartExam.
3. Isi minimal 1 jawaban → klik "Tinjau Jawaban" → ExamSummary.
4. Klik manual Submit "Kumpulkan Ujian" → confirm.
5. POST `/CMP/SubmitExam` dengan `isAutoSubmit=false`.
6. Server guard cek AssessmentType: `Manual` → **skip guard** (D-15) → grading proceed.

**Expect:**
- Browser navigate ke `/CMP/Results/{fixtureSessionId}`.
- Status fixture session: `Completed` atau `Menunggu Penilaian`.
- **NEGATIVE assertion** (D-15 verify — Manual type bypass guard, TIDAK ada Blocked entry):
  ```sql
  SELECT COUNT(*) AS BlockedForManualType
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked'
    AND TargetId = {fixtureSessionId};
  ```
  `BlockedForManualType = 0`.

**Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

## Final Sign-Off

- [ ] Semua 7 step PASS (atau dokumentasi FAIL + remediation note)
- [ ] Total AuditLog `SubmitExamBlocked` increment = **3** (Step 2 Online, Step 5 PreTest, Step 6 PostTest) — verify via post-snapshot SQL count:
  ```sql
  SELECT COUNT(*) AS PostBlockedCount
  FROM AuditLogs
  WHERE ActionType = 'SubmitExamBlocked';
  -- Expect: PostBlockedCount = BaselineBlockedCount + 3
  ```
- [ ] Step 7 NEGATIVE assertion verified — Manual type tidak punya Blocked entry baru (D-15 exclude works)
- [ ] Tier-2 message "Pengiriman jawaban tidak dapat diproses" tidak berubah (existing preserved per D-06)
- [ ] Modal `timeUpWarningModal` info-only (no "OK" button, spinner indicator) per C-03 adjustment verified visual
- [ ] Tester: _____________________ • Tanggal: _____________________ • Build commit: _____________________

> Kalau ada FAIL → catat issue, diskusi dengan planner / executor sebelum `/gsd-verify-work`. Mirror Phase 312 Path B precedent.

---

*Phase: 313-block-manual-submit-saat-waktu-habis*
*UAT script created: 2026-05-08*
*Honors: D-01 (banner copy verbatim), D-05 (AuditLog ActionType + Description format), D-06 (Tier-2 no audit), D-14 (Type metadata uniform ActionType), D-15 (Manual exclude), C-03 (modal info-only)*
