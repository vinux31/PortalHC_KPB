---
phase: 313.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/seeds/313-timer-fixtures.sql
  - docs/SEED_JOURNAL.md
autonomous: false
requirements:
  - F-313-UAT-01
  - TMR-01
gap_closure: true

must_haves:
  truths:
    - "Re-run seed `.planning/seeds/313-timer-fixtures.sql` aman (idempotent) tanpa FK error walaupun fixture lama punya PackageUserResponses dari UAT sebelumnya"
    - "Setelah seed, fixture sessions 7 baris muncul + 7 AssessmentPackages + 21 PackageQuestions + 84 PackageOptions linked ke 7 fixture"
    - "CMPController.StartExam line 905-921 query `packages.Any() == true` untuk fixture id 150-156 (atau ID baru hasil regenerasi seed)"
    - "Snapshot DB lokal dibuat sebelum apply seed extended (per CLAUDE.md Seed Workflow)"
    - "Entry baru di docs/SEED_JOURNAL.md untuk Phase 313.1 dengan klasifikasi temporary+local-only + status awal `active`"
  artifacts:
    - path: ".planning/seeds/313-timer-fixtures.sql"
      provides: "Hierarchical seed Sessions → Packages → Questions → Options dengan OUTPUT INSERTED.Id INTO @table-var pattern + cleanup chain 6-step FK-respecting"
      contains: "OUTPUT INSERTED.Id"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Journal entry Phase 313.1 seed extend (klasifikasi + dampak entitas + snapshot path + status)"
      contains: "313.1"
  key_links:
    - from: ".planning/seeds/313-timer-fixtures.sql cleanup section"
      to: "PackageUserResponses, UserPackageAssignments, PackageOptions, PackageQuestions, AssessmentPackages, AssessmentSessions"
      via: "DELETE chain 6-step ordered FK-descend (Pitfall 1 mitigation)"
      pattern: "DELETE.*PackageUserResponses.*Phase 313 Timer Fixture"
    - from: "INSERT AssessmentSessions"
      to: "INSERT AssessmentPackages SELECT FROM @SessionIds"
      via: "OUTPUT INSERTED.Id INTO @SessionIds(Id, Title) + cross-join"
      pattern: "OUTPUT INSERTED.Id, INSERTED.Title INTO @SessionIds"
    - from: "INSERT AssessmentPackages"
      to: "INSERT PackageQuestions CROSS JOIN @QuestionTemplate"
      via: "OUTPUT INSERTED.Id INTO @PackageIds + cross-join template 3 rows"
      pattern: "CROSS JOIN @QuestionTemplate"
    - from: "INSERT PackageQuestions"
      to: "INSERT PackageOptions CROSS JOIN @OptionTemplate"
      via: "OUTPUT INSERTED.Id INTO @QuestionIds + cross-join template 4 rows"
      pattern: "CROSS JOIN @OptionTemplate"

user_setup: []
---

<objective>
Extend `.planning/seeds/313-timer-fixtures.sql` supaya fixture 7 sesi (Online/PreTest/PostTest/Manual) jadi self-contained: tambah hierarchical INSERT untuk `AssessmentPackages` (1 per session) → `PackageQuestions` (3 per package, MultipleChoice synthetic) → `PackageOptions` (4 per question, IsCorrect=true HANYA index 0). Pakai T-SQL idiom `OUTPUT INSERTED.Id INTO @table-var` + cross-join template untuk identity preservation tanpa cursor loop. Tambah cleanup chain 6-step FK-respecting (PackageUserResponses → UserPackageAssignments → Options → Questions → Packages → Sessions) di-PREPEND ke existing DELETE.

Setelah landed + di-apply ke DB lokal, `CMPController.StartExam` query `packages.Any() == true` resolve untuk fixture id 150-156 → user masuk halaman exam (bukan redirect dengan TempData "Sesi ujian ini tidak memiliki paket soal").

Purpose: Resolve **F-313-UAT-01** root cause (per `.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT-FINDINGS.md` line 32-82) — seed Plan 01 Phase 313 hanya INSERT row di tabel `AssessmentSessions`, tidak ada Package/Question/Option, sehingga UAT 7-step blocked di Step 1. Phase 313.1 menutup gap ini supaya UAT 313 bisa di-re-run end-to-end via fixture.

Output:
- Modified `.planning/seeds/313-timer-fixtures.sql` rewrite full file (per CONTEXT.md Claude's Discretion — rewrite lebih maintainable dari append).
- Snapshot DB lokal `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` (per CLAUDE.md Seed Workflow item 2).
- Journal entry baru di `docs/SEED_JOURNAL.md` mengikuti format kolom existing (Phase 313 entry line 9 sebagai analog).
- Verifikasi SELECT counts post-INSERT: 7 sessions / 7 packages / 21 questions / 84 options.
</objective>

<execution_context>
@.claude/agents/gsd-execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-RESEARCH.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-PATTERNS.md
@.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-VALIDATION.md
@.planning/phases/313-block-manual-submit-saat-waktu-habis/313-UAT-FINDINGS.md
@.planning/seeds/313-timer-fixtures.sql
@docs/SEED_JOURNAL.md

<interfaces>
<!-- Schema NOT NULL summary (extracted dari RESEARCH.md "Schema Validation" line 398-437 + Migrations/ApplicationDbContextModelSnapshot.cs:258-284, 1198-1262 [VERIFIED]). Executor wajib populate semua kolom required di setiap INSERT. -->

AssessmentPackages (4 NOT NULL columns):
- AssessmentSessionId (int, FK → AssessmentSessions.Id, OnDelete Cascade)
- PackageName (nvarchar(max), IsRequired)
- PackageNumber (int)
- CreatedAt (datetime2)

PackageQuestions (5 NOT NULL columns):
- AssessmentPackageId (int, FK → AssessmentPackages.Id, OnDelete Cascade)
- QuestionText (nvarchar(max), IsRequired)
- [Order] (int — RESERVED keyword, MUST escape with square brackets per Pitfall 7)
- ScoreValue (int)
- MaxCharacters (int — model default 2000, tapi seed pakai 0 per D-02)
- QuestionType (nvarchar(max), nullable — set explicit "MultipleChoice" untuk clarity)
- Rubrik (nvarchar(max), nullable — set NULL per D-02)
- ElemenTeknis (nvarchar(max), nullable — set NULL per D-02)

PackageOptions (3 NOT NULL columns):
- PackageQuestionId (int, FK → PackageQuestions.Id, OnDelete Cascade)
- OptionText (nvarchar(max), IsRequired)
- IsCorrect (bit)

FK topology (RESEARCH.md line 446-460):
AssessmentSession → AssessmentPackage (Cascade)
                  → PackageUserResponse (Restrict — BLOCK Session DELETE)
                  → UserPackageAssignment (Cascade)
AssessmentPackage → PackageQuestion (Cascade)
PackageQuestion  → PackageOption (Cascade)
                 → PackageUserResponse (Restrict — BLOCK Question DELETE)
PackageOption    → PackageUserResponse (Restrict — BLOCK Option DELETE)

Implication: cleanup CHAIN harus DELETE PackageUserResponses + UserPackageAssignments DULU sebelum DELETE Options/Questions/Packages/Sessions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `313-timer-fixtures.sql` dengan cleanup 6-step + hierarchical INSERT (Sessions/Packages/Questions/Options) + final verification SELECT</name>
  <files>.planning/seeds/313-timer-fixtures.sql</files>
  <read_first>
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-CONTEXT.md (verifikasi locked decisions D-01 s/d D-09)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-RESEARCH.md (Section "Schema Validation" line 398-437, Section "FK Topology and Cleanup Order" line 441-505, Section "Code Examples — Seed Extension Skeleton" line 794+, Section "Common Pitfalls" line 711-790)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-PATTERNS.md (Section "1. `.planning/seeds/313-timer-fixtures.sql`" line 23-222 — verbatim SQL excerpts untuk cleanup chain + Package/Question/Option INSERT)
    - .planning/seeds/313-timer-fixtures.sql (existing file — preserve header comment, boilerplate guard, 7 fixture VALUES verbatim)
  </read_first>
  <action>
Rewrite full file `.planning/seeds/313-timer-fixtures.sql` (per CONTEXT.md Claude's Discretion: "rewrite full file lebih maintainable dari append"). Struktur final ada 6 section:

**Section 1 — Header comment** (preserve existing line 1-27 verbatim, **TAMBAH** paragraph baru di akhir comment block sebelum line 27 separator):
```
-- ============================================================
-- Phase 313.1 update (2026-05-08, gap closure F-313-UAT-01):
--   Seed sekarang hierarchical: 7 Sessions → 7 AssessmentPackages → 21 PackageQuestions
--   (3 per package, MultipleChoice synthetic) → 84 PackageOptions (4 per question,
--   IsCorrect=true index 0). Cleanup chain 6-step FK-respecting di-PREPEND.
--
-- Skema verified Migrations/ApplicationDbContextModelSnapshot.cs:258-284 (Package),
-- 1198-1262 (Option/Question), 286-459 (Session). FK topology:
--   AssessmentSession → AssessmentPackage (Cascade) → PackageQuestion (Cascade) → PackageOption (Cascade)
--   AssessmentSession ← PackageUserResponse (Restrict)
--   AssessmentSession ← UserPackageAssignment (Cascade Session, Restrict Package)
-- ============================================================
```

**Section 2 — Boilerplate guard** (preserve existing line 29-52 verbatim — `SET NOCOUNT ON;`, `SET XACT_ABORT ON;`, resolve `@UserId`, `@Now`, `THROW 50001` guard, PRINT statements). JANGAN ubah.

**Section 3 — Idempotent cleanup chain 6-step** (REPLACE existing line 54-60 yang cuma DELETE Sessions). Ikuti pattern verbatim dari `313.1-PATTERNS.md` line 76-107 (analog `313.1-RESEARCH.md` line 466-499). Tulis sebagai 6 DELETE statements dengan PRINT setelah masing-masing untuk debugging:

```sql
-- ============================================================
-- 2. Idempotent cleanup chain 6-step (FK-respecting; D-08 + D-09)
-- ============================================================

-- 2.1 PackageUserResponses pertama (Restrict on Session/Question/Option)
DELETE pur FROM PackageUserResponses pur
INNER JOIN AssessmentSessions s ON pur.AssessmentSessionId = s.Id
WHERE s.Title LIKE 'Phase 313 Timer Fixture%';
PRINT N'[Phase 313.1 seed] PackageUserResponses cleaned.';

-- 2.2 UserPackageAssignments (Cascade on Session, but Restrict on Package)
DELETE upa FROM UserPackageAssignments upa
INNER JOIN AssessmentSessions s ON upa.AssessmentSessionId = s.Id
WHERE s.Title LIKE 'Phase 313 Timer Fixture%';
PRINT N'[Phase 313.1 seed] UserPackageAssignments cleaned.';

-- 2.3 PackageOptions
DELETE po FROM PackageOptions po
INNER JOIN PackageQuestions pq ON po.PackageQuestionId = pq.Id
INNER JOIN AssessmentPackages ap ON pq.AssessmentPackageId = ap.Id
INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId = s.Id
WHERE s.Title LIKE 'Phase 313 Timer Fixture%';
PRINT N'[Phase 313.1 seed] PackageOptions cleaned.';

-- 2.4 PackageQuestions
DELETE pq FROM PackageQuestions pq
INNER JOIN AssessmentPackages ap ON pq.AssessmentPackageId = ap.Id
INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId = s.Id
WHERE s.Title LIKE 'Phase 313 Timer Fixture%';
PRINT N'[Phase 313.1 seed] PackageQuestions cleaned.';

-- 2.5 AssessmentPackages
DELETE ap FROM AssessmentPackages ap
INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId = s.Id
WHERE s.Title LIKE 'Phase 313 Timer Fixture%';
PRINT N'[Phase 313.1 seed] AssessmentPackages cleaned.';

-- 2.6 AssessmentSessions
DELETE FROM AssessmentSessions WHERE Title LIKE 'Phase 313 Timer Fixture%';
PRINT N'[Phase 313.1 seed] AssessmentSessions cleaned.';
```

**Section 4 — INSERT AssessmentSessions dengan OUTPUT clause** (modify existing line 81-152). Preserve VERBATIM 7 fixture VALUES tuple — JANGAN ubah back-dated `StartedAt` offset (D-21), title pattern (D-08), atau kolom values. Tambahkan `DECLARE @SessionIds TABLE (Id INT, Title NVARCHAR(450));` di atas INSERT, dan `OUTPUT INSERTED.Id, INSERTED.Title INTO @SessionIds(Id, Title)` setelah column list (sebelum `VALUES`):

```sql
-- ============================================================
-- 3. Insert 7 fixture (D-07 back-dated + D-08 title pattern) — capture identity
-- ============================================================
DECLARE @SessionIds TABLE (Id INT, Title NVARCHAR(450));

INSERT INTO AssessmentSessions (
    Title, Category, UserId, AssessmentType,
    DurationMinutes, ExtraTimeMinutes, StartedAt, Status,
    Schedule, AccessToken, BannerColor,
    AllowAnswerReview, GenerateCertificate, HasManualGrading,
    IsManualEntry, IsTokenRequired, SamePackage,
    PassPercentage, Progress, ElapsedSeconds,
    CreatedAt
)
OUTPUT INSERTED.Id, INSERTED.Title INTO @SessionIds(Id, Title)
VALUES
    -- 7 fixture rows VERBATIM dari existing line 91-152 (preserve)
    ;

PRINT N'[Phase 313.1 seed] 7 AssessmentSessions di-INSERT (identity captured).';
```

**Section 5 — INSERT AssessmentPackages cross-join + INSERT PackageQuestions cross-join + INSERT PackageOptions cross-join** (NEW). Ikuti pattern verbatim dari `313.1-PATTERNS.md` line 136-203 (analog `313.1-RESEARCH.md` Pattern 1+2 line 236-308):

```sql
-- ============================================================
-- 4. Insert AssessmentPackages (1 per session) — D-01
-- ============================================================
DECLARE @PackageIds TABLE (Id INT, AssessmentSessionId INT);

INSERT INTO AssessmentPackages (AssessmentSessionId, PackageName, PackageNumber, CreatedAt)
OUTPUT INSERTED.Id, INSERTED.AssessmentSessionId INTO @PackageIds(Id, AssessmentSessionId)
SELECT
    s.Id,
    N'Phase 313 Test Package',  -- D-01
    1,                           -- PackageNumber
    @Now
FROM @SessionIds s;

PRINT N'[Phase 313.1 seed] 7 AssessmentPackages di-INSERT.';

-- ============================================================
-- 5. Insert PackageQuestions (3 per package) — D-02 + D-04 (synthetic) + D-05 (identik 7 fixture)
-- ============================================================
DECLARE @QuestionTemplate TABLE ([Order] INT, QuestionText NVARCHAR(MAX), ScoreValue INT);
INSERT INTO @QuestionTemplate VALUES
    (1, N'Phase 313 Test Q1: Pilih jawaban yang benar.', 10),
    (2, N'Phase 313 Test Q2: Pilih jawaban yang benar.', 10),
    (3, N'Phase 313 Test Q3: Pilih jawaban yang benar.', 10);

DECLARE @QuestionIds TABLE (Id INT, AssessmentPackageId INT, [Order] INT);

INSERT INTO PackageQuestions (
    AssessmentPackageId, [Order], QuestionText, ScoreValue,
    QuestionType, MaxCharacters, Rubrik, ElemenTeknis
)
OUTPUT INSERTED.Id, INSERTED.AssessmentPackageId, INSERTED.[Order]
    INTO @QuestionIds(Id, AssessmentPackageId, [Order])
SELECT
    p.Id,
    t.[Order],
    t.QuestionText,
    t.ScoreValue,
    N'MultipleChoice',  -- D-02
    0,                  -- MaxCharacters (MC ignore)
    NULL,               -- Rubrik
    NULL                -- ElemenTeknis
FROM @PackageIds p
CROSS JOIN @QuestionTemplate t;

PRINT N'[Phase 313.1 seed] 21 PackageQuestions di-INSERT.';

-- ============================================================
-- 6. Insert PackageOptions (4 per question) — D-03 (IsCorrect=1 HANYA index 0)
-- ============================================================
DECLARE @OptionTemplate TABLE ([Index] INT, OptionText NVARCHAR(MAX), IsCorrect BIT);
INSERT INTO @OptionTemplate VALUES
    (0, N'Pilihan A', 1),
    (1, N'Pilihan B', 0),
    (2, N'Pilihan C', 0),
    (3, N'Pilihan D', 0);

INSERT INTO PackageOptions (PackageQuestionId, OptionText, IsCorrect)
SELECT
    q.Id,
    o.OptionText,
    o.IsCorrect
FROM @QuestionIds q
CROSS JOIN @OptionTemplate o;

PRINT N'[Phase 313.1 seed] 84 PackageOptions di-INSERT.';
```

**Section 6 — Final verification SELECT** (modify existing line 159-171 — extend untuk verify count Package + Question per session, mirror UAT annotation pattern di `313.1-PATTERNS.md` line 426-441):

```sql
-- ============================================================
-- 7. Final verification SELECT
-- ============================================================
SELECT
    s.Id, s.Title, s.AssessmentType, s.StartedAt, s.DurationMinutes,
    ISNULL(s.ExtraTimeMinutes, 0) AS ExtraTimeMinutes,
    DATEDIFF(MINUTE, s.StartedAt, SYSUTCDATETIME()) AS ElapsedMinutes,
    s.Status, s.UserId,
    (SELECT COUNT(*) FROM AssessmentPackages WHERE AssessmentSessionId = s.Id) AS PkgCount,
    (SELECT COUNT(*) FROM PackageQuestions pq INNER JOIN AssessmentPackages ap
         ON pq.AssessmentPackageId = ap.Id WHERE ap.AssessmentSessionId = s.Id) AS QCount,
    (SELECT COUNT(*) FROM PackageOptions po
         INNER JOIN PackageQuestions pq ON po.PackageQuestionId = pq.Id
         INNER JOIN AssessmentPackages ap ON pq.AssessmentPackageId = ap.Id
         WHERE ap.AssessmentSessionId = s.Id) AS OptCount
FROM AssessmentSessions s
WHERE s.Title LIKE 'Phase 313 Timer Fixture%'
ORDER BY s.Title;
-- Expected: 7 row, semua dengan PkgCount=1, QCount=3, OptCount=12
```

**Anti-pattern compliance (per RESEARCH.md "Anti-Patterns" + Common Pitfalls + CONTEXT.md "Anti-Patterns"):**
- TIDAK boleh `SCOPE_IDENTITY()` — pakai `OUTPUT INSERTED.Id INTO @table-var` (Pitfall: only returns last row).
- TIDAK boleh cursor `WHILE` loop — pakai cross-join set-based.
- TIDAK boleh hardcode integer ID untuk SessionId/PackageId/QuestionId (anti-pattern Phase 309 NULL FK violation).
- WAJIB escape `[Order]` dengan square brackets (Pitfall 7 — reserved keyword).
- TIDAK perlu `SET IDENTITY_INSERT ON` (Pitfall 6).
- WAJIB `OUTPUT INTO @TABLE-variable` (bukan real table) per Pitfall 2 (FK constraint restriction).

**Catatan implementasi:**
- Single transaction implicit via `SET XACT_ABORT ON` (existing line 30) — kalau ada error di tengah, rollback otomatis.
- Ekstensi belum panggil `BEGIN TRAN` eksplisit — preserve existing pattern (XACT_ABORT cukup).
- File diakhiri dengan PRINT total + final SELECT (jangan tambah `COMMIT` eksplisit kalau tidak ada `BEGIN TRAN`).
  </action>
  <verify>
    <automated>grep -c "OUTPUT INSERTED.Id" .planning/seeds/313-timer-fixtures.sql</automated>
    Expected: returns >= 3 (3 INSERT dengan OUTPUT clause: Sessions, Packages, Questions).

    Additional spot-checks (planner running checker):
    - `grep -c "DELETE FROM PackageUserResponses\|DELETE pur FROM PackageUserResponses" .planning/seeds/313-timer-fixtures.sql` returns >= 1
    - `grep -c "CROSS JOIN @QuestionTemplate" .planning/seeds/313-timer-fixtures.sql` returns 1
    - `grep -c "CROSS JOIN @OptionTemplate" .planning/seeds/313-timer-fixtures.sql` returns 1
    - `grep -c "Phase 313 Test Package" .planning/seeds/313-timer-fixtures.sql` returns >= 1
    - `grep -c "Pilihan A" .planning/seeds/313-timer-fixtures.sql` returns >= 1
    - `grep -c "\[Order\]" .planning/seeds/313-timer-fixtures.sql` returns >= 3 (template, INSERT column list, OUTPUT)
  </verify>
  <acceptance_criteria>
    - File `.planning/seeds/313-timer-fixtures.sql` exists.
    - Header comment block mention "Phase 313.1 update (2026-05-08, gap closure F-313-UAT-01)" + skema verified citation.
    - `SET NOCOUNT ON; SET XACT_ABORT ON;` preserved (line ~29-30).
    - Cleanup chain 6 DELETE statements (PackageUserResponses, UserPackageAssignments, PackageOptions, PackageQuestions, AssessmentPackages, AssessmentSessions) dengan PRINT per step.
    - INSERT AssessmentSessions dengan `OUTPUT INSERTED.Id, INSERTED.Title INTO @SessionIds(Id, Title)`.
    - 7 fixture VALUES tuple preserved verbatim (Title pattern `Phase 313 Timer Fixture …`, AssessmentType, StartedAt offsets, dst).
    - INSERT AssessmentPackages SELECT FROM @SessionIds dengan PackageName=`'Phase 313 Test Package'`, PackageNumber=1.
    - INSERT PackageQuestions CROSS JOIN @QuestionTemplate (3 rows × 7 packages = 21).
    - INSERT PackageOptions CROSS JOIN @OptionTemplate (4 rows × 21 questions = 84).
    - Final verification SELECT mengikutsertakan PkgCount, QCount, OptCount sub-queries.
    - `[Order]` selalu di-escape dengan square brackets (3 occurrences minimum: di template DECLARE, di INSERT column list PackageQuestions, di OUTPUT clause).
    - Tidak mengandung `SCOPE_IDENTITY()`, tidak mengandung `DECLARE.*CURSOR`, tidak mengandung `SET IDENTITY_INSERT`.
    - File berakhir dengan SELECT statement (bukan COMMIT atau dangling DECLARE).
  </acceptance_criteria>
  <done>
    File rewrite landed; grep verifications pass; ready untuk apply ke DB lokal di Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Snapshot DB lokal + apply seed extended + verify counts + journal entry update</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - CLAUDE.md (Seed Data Workflow item 2-4)
    - docs/SEED_JOURNAL.md (existing Phase 313 entry line 9 sebagai analog format)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-PATTERNS.md (Section 4 "docs/SEED_JOURNAL.md" line 388-402 — verbatim entry format)
    - .planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-VALIDATION.md (Section "Manual-Only Verifications" + Sampling Rate "After Wave 0 (seed extension)")
  </read_first>
  <what-built>
    Task 1 menulis ulang `.planning/seeds/313-timer-fixtures.sql` dengan cleanup chain 6-step + hierarchical INSERT (Sessions/Packages/Questions/Options) + final verification SELECT. SQL belum di-apply ke DB lokal.
  </what-built>
  <how-to-verify>
**Step 2.1 — Snapshot DB sebelum apply seed (CLAUDE.md Seed Workflow item 2):**

Jalankan command berikut di terminal lokal (PowerShell atau cmd):
```
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK = 'C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak' WITH FORMAT, INIT, NAME = 'HcPortalDB_Dev pre-313.1 seed extend';"
```

Expected output: "Processed N pages for database 'HcPortalDB_Dev', file 'HcPortalDB_Dev'..." + "BACKUP DATABASE successfully processed N pages...".

Verifikasi file `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` exists.

**Step 2.2 — Apply seed extended:**

```
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -i .planning\seeds\313-timer-fixtures.sql
```

Expected output (PRINT messages):
- `[Phase 313 seed] UserId resolved: <GUID>`
- `[Phase 313 seed] @Now = 2026-05-08T...`
- `[Phase 313.1 seed] PackageUserResponses cleaned.`
- `[Phase 313.1 seed] UserPackageAssignments cleaned.`
- `[Phase 313.1 seed] PackageOptions cleaned.`
- `[Phase 313.1 seed] PackageQuestions cleaned.`
- `[Phase 313.1 seed] AssessmentPackages cleaned.`
- `[Phase 313.1 seed] AssessmentSessions cleaned.`
- `[Phase 313.1 seed] 7 AssessmentSessions di-INSERT (identity captured).`
- `[Phase 313.1 seed] 7 AssessmentPackages di-INSERT.`
- `[Phase 313.1 seed] 21 PackageQuestions di-INSERT.`
- `[Phase 313.1 seed] 84 PackageOptions di-INSERT.`

Final SELECT result: 7 row dengan kolom `PkgCount=1`, `QCount=3`, `OptCount=12` untuk semua row.

**Step 2.3 — Verify counts via independent SQL query:**

```
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -Q "SELECT COUNT(*) AS Sessions FROM AssessmentSessions WHERE Title LIKE 'Phase 313 Timer Fixture%'; SELECT COUNT(*) AS Packages FROM AssessmentPackages ap INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId=s.Id WHERE s.Title LIKE 'Phase 313 Timer Fixture%'; SELECT COUNT(*) AS Questions FROM PackageQuestions pq INNER JOIN AssessmentPackages ap ON pq.AssessmentPackageId=ap.Id INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId=s.Id WHERE s.Title LIKE 'Phase 313 Timer Fixture%'; SELECT COUNT(*) AS Options FROM PackageOptions po INNER JOIN PackageQuestions pq ON po.PackageQuestionId=pq.Id INNER JOIN AssessmentPackages ap ON pq.AssessmentPackageId=ap.Id INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId=s.Id WHERE s.Title LIKE 'Phase 313 Timer Fixture%';"
```

Expected: Sessions=7, Packages=7, Questions=21, Options=84.

**Step 2.4 — Re-run seed (idempotency test):**

Jalankan ulang command Step 2.2. Expected: tidak ada error FK violation, output PRINT counts identik dengan run pertama. (Membuktikan cleanup chain 6-step bekerja saat Package/Question/Option lama sudah ada dari run sebelumnya.)

**Step 2.5 — Smoke test functional via UI (manual, atau lanjut Plan 02 yang otomatisasi):**

Login coachee `rino.prasetyo@pertamina.com / 123456` di `http://localhost:5277` (`dotnet run` running). Navigate `/CMP/Assessment`. Expected: 7 fixture muncul dengan title `Phase 313 Timer Fixture …`. Klik "Resume" pada `Phase 313 Timer Fixture Online ManualBeforeTime` → expected URL match `/CMP/StartExam/<id>` + halaman exam dengan 1 question pertama visible (BUKAN redirect ke `/CMP/Assessment` dengan TempData "Sesi ujian ini tidak memiliki paket soal").

**Step 2.6 — Append entry baru ke `docs/SEED_JOURNAL.md`:**

Append row baru (mengikuti format kolom existing — verbatim dari `313.1-PATTERNS.md` line 397-399):

```
| 2026-05-08 | 313.1 | temporary + local-only | Gap closure F-313-UAT-01: extend seed dengan AssessmentPackages(7)+PackageQuestions(21)+PackageOptions(84) supaya CMPController.StartExam packages.Any() resolve true → unblock UAT 7-step | AssessmentSessions(7), AssessmentPackages(+7), PackageQuestions(+21), PackageOptions(+84) prefix `Phase 313 Timer Fixture` (cleanup chain idempotent, FK-respecting) | HcPortalDB_Dev.20260508-pre313_1.bak | active |
```

Status awal `active` — akan di-update jadi `cleaned` setelah Plan 02 + manual UAT 7-step selesai dan snapshot di-restore (per CLAUDE.md item 4).

**Verify journal entry:**
```
grep -c "313.1.*Gap closure F-313-UAT-01" docs/SEED_JOURNAL.md
```
Expected: returns 1.

**Tunggu user konfirmasi:** balas "approved" kalau Step 2.1-2.6 semua PASS, atau jelaskan issue (mis. "Step 2.4 idempotency error: FK violation di DELETE PackageQuestions").
  </how-to-verify>
  <action>Lihat blok <how-to-verify> di bawah — eksekusi 6 langkah secara berurutan: (2.1) BACKUP DATABASE snapshot ke C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak; (2.2) sqlcmd jalankan seed file `.planning/seeds/313-timer-fixtures.sql`; (2.3) verify counts via independent COUNT(*) query (Sessions=7, Packages=7, Questions=21, Options=84); (2.4) re-run seed (idempotency test); (2.5) UI smoke test login coachee + Resume fixture ManualBeforeTime → masuk halaman exam (BUKAN redirect); (2.6) append journal entry baru ke `docs/SEED_JOURNAL.md` dengan format kolom existing (Phase 313 entry analog) + status `active`.</action>
  <verify>
    <automated>grep -c "313.1.*Gap closure F-313-UAT-01" docs/SEED_JOURNAL.md</automated>
    Expected: returns 1.
  </verify>
  <done>
    Snapshot file `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` exists; seed exec sukses dengan PRINT counts 7/7/21/84; independent COUNT query confirm; re-run idempotent; UI smoke test fixture Resume berhasil masuk exam page; journal entry `Phase 313.1` baris baru appended dengan klasifikasi temporary+local-only + status active.
  </done>
  <resume-signal>Type "approved" untuk lanjut Plan 02, atau jelaskan issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → DB lokal | T-SQL seed jalan dengan integrated security (Windows auth) terhadap `HcPortalDB_Dev` lokal. Tidak boleh menyentuh Dev (10.55.3.3) atau Prod. |
| Seed file → Git repo | Seed file di-commit ke `.planning/seeds/`; perlu hindari leak credential atau real production data ke history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313.1-01 | I (Information Disclosure) | Seed file leak ke environment Dev/Prod | mitigate | File path `.planning/seeds/` (non-deployed); CLAUDE.md SOP enforcement (tester JANGAN apply ke Dev/Prod, IT yang promote setelah verify). Header comment line 11-13 sudah explicitly state lokal-only. Snapshot+restore loop di Task 2 step 2.1 + post-UAT membatasi window data presence. |
| T-313.1-02 | T (Tampering) / D (Denial of Service) | FK violation saat re-run seed (DELETE Sessions block oleh PackageUserResponse Restrict) | mitigate | Cleanup chain 6-step FK-respecting (Section 3 Task 1) — DELETE PackageUserResponses + UserPackageAssignments DULU sebelum DELETE Options/Questions/Packages/Sessions. Pitfall 1 RESEARCH.md line 713-721. Idempotency test di Task 2 Step 2.4 verify re-run tidak gagal. |
| T-313.1-03 | I | Fragility test data drift (jika clone dari real production package) | mitigate (per D-04) | Synthetic hardcoded content (`Phase 313 Test Q1: Pilih jawaban yang benar.` + `Pilihan A/B/C/D`) — tidak depend pada production data. Self-contained, deterministic. |
| T-313.1-04 | E (Elevation of Privilege) | UserId NULL FK violation (anti-pattern Phase 309) | mitigate | Existing guard line 44-49 `THROW 50001` kalau `@UserId IS NULL` — abort sebelum DELETE/INSERT. UserId resolve via subquery (bukan hardcode). |
| T-313.1-05 | T | OUTPUT INTO real table FK constraint chain failure (Pitfall 2) | mitigate | Pakai `DECLARE @TABLE` variable EXCLUSIVELY untuk capture identity (Sessions/Packages/Questions). Tidak ada `OUTPUT INTO RealTable` di seed. |
</threat_model>

<verification>
**Per-task automated verifies sudah ada di setiap task. Plan-level final check:**

- Setelah Task 2 user approval, `git diff .planning/seeds/313-timer-fixtures.sql` menunjukkan rewrite valid (cleanup chain + 4 INSERT block + verification SELECT extended).
- `git diff docs/SEED_JOURNAL.md` shows 1 row baru appended (Phase 313.1 entry).
- DB lokal state: 7 sessions / 7 packages / 21 questions / 84 options dengan title prefix `Phase 313 Timer Fixture`.
- Snapshot file `C:\Temp\HcPortalDB_Dev.20260508-pre313_1.bak` exists (untuk restore akhir UAT).
</verification>

<success_criteria>
1. Seed file `.planning/seeds/313-timer-fixtures.sql` rewrite landed dengan grep checks pass (Task 1 verify).
2. Snapshot DB `HcPortalDB_Dev.20260508-pre313_1.bak` created sebelum apply seed (Task 2 step 2.1).
3. Seed exec sukses dengan PRINT counts: 7/7/21/84 (Task 2 step 2.2).
4. Independent COUNT(*) query confirm 7/7/21/84 (Task 2 step 2.3).
5. Re-run seed idempotent (Task 2 step 2.4).
6. UI smoke test: fixture `Online ManualBeforeTime` Resume → masuk halaman exam (BUKAN redirect dengan TempData) (Task 2 step 2.5).
7. Journal entry baru di `docs/SEED_JOURNAL.md` dengan klasifikasi temporary+local-only + status active (Task 2 step 2.6).
</success_criteria>

<output>
After completion, create `.planning/phases/313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages-/313.1-01-SUMMARY.md` dengan:
- Files modified (path + before/after summary).
- DB state post-apply (7/7/21/84).
- Snapshot file path untuk recovery.
- Anti-pattern compliance (no SCOPE_IDENTITY, no cursor, no IDENTITY_INSERT, [Order] escaped).
- Open issues (e.g., Plan 02 deps: helper file + Playwright finalize).
</output>
