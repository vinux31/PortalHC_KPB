# Phase 313.1: Gap Closure - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — log preserves alternatives considered.

**Date:** 2026-05-08
**Phase:** 313.1-gap-closure-phase-313-extend-seed-dengan-assessmentpackages
**Mode:** --auto (Claude auto-selected recommended defaults)
**Areas discussed:** Seed scope, Question content source, Identity preservation, Cleanup ordering, Playwright assertion depth, Test helper pattern, Test data isolation, Test stability

---

## A. Seed Scope per Fixture

| Option | Description | Selected |
|--------|-------------|----------|
| 1 package × 3 MC × 4 options (recommended) | Minimum trigger `packages.Any() == true` di line 921, ringan | ✓ |
| Multi-package (2-3) per fixture | Test cross-package shuffle algorithm | |
| 1 package × 1 MC × 2 options | Sangat minimal | |

**Auto-selected:** 1 package × 3 MC × 4 options
**Why recommended:** Cukup untuk masuk halaman exam, ringan secara DB footprint, predictable answer (index 0 correct).

---

## B. Question Content Source

| Option | Description | Selected |
|--------|-------------|----------|
| Synthetic hardcoded (recommended) | "Phase 313 Test Q1: Pilih jawaban yang benar" + "Pilihan A/B/C/D" | ✓ |
| Clone dari real production package | Realistic data | |
| Lorem ipsum random | Generic | |

**Auto-selected:** Synthetic hardcoded
**Why recommended:** Self-contained, deterministic, tidak fragile terhadap data drift, identity preserved.

---

## C. Identity Preservation Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| OUTPUT INSERTED.Id INTO @table-var (recommended) | Bulk INSERT capture IDs cross-join template | ✓ |
| @@SCOPE_IDENTITY per row INSERT | Loop one-by-one | |
| Hardcode IDs assuming sequence | Brittle | |

**Auto-selected:** OUTPUT INSERTED.Id INTO table-variable
**Why recommended:** Bulk-friendly, tidak break existing INSERT pattern, standard SQL Server idiom.

---

## D. Idempotent Cleanup Ordering

| Option | Description | Selected |
|--------|-------------|----------|
| FK-respecting cascade DELETE Options→Questions→Packages→Sessions (recommended) | 4-step ordered DELETE before INSERT | ✓ |
| ON DELETE CASCADE pakai schema constraint | Butuh migration | |
| TRUNCATE table | Hapus semua row, bukan filter title | |

**Auto-selected:** FK-respecting cascade DELETE
**Why recommended:** Match existing wipe-and-insert pattern, no migration required, scope = title prefix only.

---

## E. Playwright Assertion Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Click Resume + URL assert + banner text assert per scenario (recommended) | Concrete flow, manual SQL audit log spot-check | ✓ |
| Toleransi placeholder (toBeVisible saja) | Cepat tapi tidak verify behavior | |
| Full E2E + AuditLog query via DB connection | Maksimal coverage tapi infra heavy | |

**Auto-selected:** Click Resume + URL assert + banner text assert per scenario
**Why recommended:** Match D-10 retry handler verification + Tier-1/Tier-2 banner rendering, AuditLog tetap manual SQL.

---

## F. Test Helper Pattern

| Option | Description | Selected |
|--------|-------------|----------|
| New file `tests/e2e/helpers/exam313.ts` (recommended) | Phase 307 D-01 pattern, shared assertions | ✓ |
| Inline di test bodies | Duplicate code | |
| Helper di `tests/helpers/` | Wrong location per Phase 307 D-01 | |

**Auto-selected:** New file `tests/e2e/helpers/exam313.ts`
**Why recommended:** Konsisten dengan Phase 307 helper pattern (e2e-specific selectors).

---

## G. Test Data Isolation

| Option | Description | Selected |
|--------|-------------|----------|
| Rely on seed wipe-and-insert idempotency (recommended) | Standard workflow per seed file header | ✓ |
| Per-test afterEach DB cleanup | Heavy infra, butuh DB connection | |
| Snapshot/restore per test | Slow, brittle | |

**Auto-selected:** Rely on seed wipe-and-insert idempotency
**Why recommended:** Standard manual UAT workflow, no test infra change needed.

---

## H. Test Stability

| Option | Description | Selected |
|--------|-------------|----------|
| Existing back-dated StartedAt + bumped timeout for retry (recommended) | No real timer wait, deterministic | ✓ |
| Real timer wait setup | Slow, flaky | |
| Mock timer via DI | Touch production code | |

**Auto-selected:** Existing back-dated StartedAt + bumped Playwright timeout 30s untuk SubmitExam POST (cover 1+2+4s retry)
**Why recommended:** Existing strategy proven Phase 313 D-07, deterministic.

---

## Claude's Discretion

- Exact banner text phrasing (extract dari ExamSummary.cshtml + StartExam.cshtml saat plan)
- Helper function naming conventions di `exam313.ts`
- SQL formatting/indentation (ikuti style existing)
- Append vs rewrite full seed file (planner pilih, rewrite preferred)

## Deferred Ideas

- AuditLog assertion via Playwright DB query (saat ini manual SQL)
- Multi-package cross-shuffle test coverage
- Real production package clone untuk edge case test data
