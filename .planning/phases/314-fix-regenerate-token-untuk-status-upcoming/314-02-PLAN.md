---
phase: 314-fix-regenerate-token-untuk-status-upcoming
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/AssessmentMonitoring.cshtml
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/Admin/ManageAssessment.cshtml
  - tests/e2e/admin-assessment-token.spec.ts
  - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [TKN-01]
must_haves:
  truths:
    - "RegenerateToken endpoint sukses untuk Status=Upcoming + IsTokenRequired=true + 0 worker (TKN-01 acceptance)"
    - "Backend punya 8 layered defensive guards: D-25 status block, D-33 sibling 0-row, D-17 transaction wrap, D-06 audit try-catch swallow, D-12 specific exception catches, D-20/D-21 structured logging, D-37/D-38 conditional Schedule guard, GenerateSecureToken reuse"
    - "Frontend 3 view (Monitoring, MonitoringDetail, ManageAssessment) propagate server message ke alert (D-07/D-09/D-11)"
    - "Frontend warn dialog Open + active worker dengan wording REVISED (D-22/D-23 — token TIDAK invalidate sesi)"
    - "Existing per-view convention preserved (querySelectorAll forEach handler #1, named function spinner handler #2, document.body delegation handler #3)"
    - "Playwright spec admin-assessment-token.spec.ts verify 3 skenario PASS"
    - "Manual UAT 314-UAT.md sign-off oleh user"
    - "Seed Data Workflow compliance: snapshot DB sebelum seed, SEED_JOURNAL.md entry status active → cleaned setelah test"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "RegenerateToken method body refactored dengan 8 layered guards"
      contains: "BeginTransactionAsync"
    - path: "Views/Admin/AssessmentMonitoring.cshtml"
      provides: "Handler #1 (querySelectorAll forEach) refactored dengan response.ok + r.text fallback + propagate server message"
      contains: "if (!r.ok)"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Handler #2 (named function regenToken + spinner) refactored — preserve spinner UX"
      contains: "spinner-border"
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Handler #3 (document.body event delegation HTMX-friendly) refactored — preserve delegation"
      contains: "document.body.addEventListener"
    - path: "tests/e2e/admin-assessment-token.spec.ts"
      provides: "Playwright E2E 3 skenario (Upcoming0 / UpcomingPartial / OpenRunning) dengan dedicated fixture title"
      contains: "Phase 314 Token Fixture"
    - path: ".planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md"
      provides: "Manual UAT 6-step Bahasa Indonesia (Phase 312 format)"
      contains: "## Step 1"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Phase 314 entry classified temporary + local-only, status active → cleaned"
      contains: "Phase 314 Token Fixture"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs RegenerateToken catch"
      to: "JSON response {success:false, message:ex.Message}"
      via: "D-12 specific exception types"
      pattern: "catch \\(DbUpdateException|catch \\(NullReferenceException"
    - from: "3 view handlers .catch()"
      to: "alert('Gagal regenerate token: ' + serverMessage + '. Coba lagi atau hubungi IT.')"
      via: "D-07 throw data.message + D-11 r.text fallback"
      pattern: "Gagal regenerate token:"
    - from: "tests/e2e/admin-assessment-token.spec.ts test 13.3"
      to: "PERINGATAN dialog accept"
      via: "page.on('dialog') handler"
      pattern: "TIDAK akan invalidate sesi"
    - from: "docs/SEED_JOURNAL.md entry"
      to: "post-test cleanup status update (active → cleaned)"
      via: "Wave 3 cleanup task"
      pattern: "cleaned"
---

<objective>
Plan 02 = patch + test phase Phase 314. Apply defensive backend patch (8 layered guards per D-05/D-06/D-12/D-17/D-20/D-21/D-25/D-33 + conditional D-37/D-38) di `Controllers/AssessmentAdminController.cs:2427-2475`, propagate server message ke alert di 3 view (`AssessmentMonitoring.cshtml`, `AssessmentMonitoringDetail.cshtml`, `ManageAssessment.cshtml`) dengan preserve per-view convention (querySelectorAll forEach / named function + spinner / HTMX delegation), tambah warn dialog Open + active worker (D-22/D-23 REVISED wording), tulis Playwright E2E 3 skenario di file baru `tests/e2e/admin-assessment-token.spec.ts`, isi UAT 6-step Bahasa Indonesia, log seed di `docs/SEED_JOURNAL.md` sesuai CLAUDE.md NEW Seed Data Workflow.

Purpose: Plan 01 menentukan root cause + Schedule MinValue conditional. Plan 02 turn that intelligence into shipped code: backend dengan defensive cumulative coverage tahan regression, frontend yang menampilkan error detail (bukan generic), test coverage untuk 3 skenario success path + edge case.

Output: 7 file modified/created. dotnet build pass. Playwright spec PASS atau test.skip() graceful kalau seed missing. Manual UAT signed off. SEED_JOURNAL entry recorded + cleaned.

**CRITICAL:** Plan 02 INHERITS Plan 01 outputs. Sebelum mulai, verify:
- RESEARCH.md `## Root Cause (Finalized` ada
- RESEARCH.md `## Fix Proposal Mapping` ada (untuk drive D-37/D-38 conditional)
- CONTEXT.md D-23 wording = "TIDAK akan invalidate sesi mereka yang sudah berjalan"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-VALIDATION.md
@.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-01-SUMMARY.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md
@docs/SEED_WORKFLOW.md
@Controllers/AssessmentAdminController.cs
@Views/Admin/AssessmentMonitoring.cshtml
@Views/Admin/AssessmentMonitoringDetail.cshtml
@Views/Admin/ManageAssessment.cshtml
@Services/AuditLogService.cs
@Models/AssessmentSession.cs
@Models/AuditLog.cs
@tests/e2e/assessment.spec.ts
@tests/helpers/auth.ts
@tests/helpers/accounts.ts
@.planning/phases/308-prepost-wizard-validation-fix/308-UAT.md
@.planning/phases/312-admin-full-delete-assessment-room/312-UAT.md

<interfaces>
<!-- Backend signature (PRESERVED) -->
From Controllers/AssessmentAdminController.cs:2424-2427:
```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> RegenerateToken(int id)
```

<!-- Audit log API (REUSED — no change) -->
From Services/AuditLogService.cs:21-42:
```csharp
public async Task LogAsync(
    string actorUserId,
    string actorName,
    string actionType,
    string description,
    int? targetId = null,
    string? targetType = null)
```

<!-- AssessmentSession schema (REFERENCE only) -->
From Models/AssessmentSession.cs:
```csharp
public int Id { get; set; }
public string Title { get; set; }
public string Category { get; set; }
public DateTime Schedule { get; set; }       // line 18 — non-nullable, DateTime struct
public DateTime? StartedAt { get; set; }     // line 40 — used for hasStarted (D-19)
public bool IsTokenRequired { get; set; }
public string? AccessToken { get; set; }
public string Status { get; set; }            // values: Upcoming / Open / Completed / Cancelled
public DateTime UpdatedAt { get; set; }
```

<!-- JSON response shape (PRESERVED — server contract S3) -->
```typescript
{ success: boolean; message: string; token?: string }
```

<!-- Existing helper (REUSED, NOT modified) -->
From Controllers/AssessmentAdminController.cs:2478-2492:
```csharp
private string GenerateSecureToken(int length = 6)
// Returns 6-char string from "ABCDEFGHJKLMNPQRSTUVWXYZ23456789"
```

<!-- Playwright fixtures -->
From tests/helpers/accounts.ts:2:
```typescript
admin: { email: 'admin@pertamina.com', password: '123456' }
```

From tests/helpers/auth.ts:4-11:
```typescript
export async function login(page: Page, role: 'admin' | 'hc' | ...): Promise<void>
```
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!-- WAVE 1 — Backend defensive guards (sequential within wave) -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 1 (W1): Backend RegenerateToken — replace full method body dengan 8 layered defensive guards</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (line 2420-2495 — existing RegenerateToken + GenerateSecureToken helper)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#H composite full method line 270-373 — canonical template)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-05, D-06, D-12, D-17, D-20, D-21, D-25, D-28, D-33, D-37, D-38)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md (§ Root Cause Finalized + § Fix Proposal Mapping — drive D-37/D-38 conditional)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-01-SUMMARY.md (D-37/D-38 verdict)
    - Controllers/AssessmentAdminController.cs (line 2391-2410 — DeletePrePostGroup audit try-catch swallow analog)
    - Controllers/AssessmentAdminController.cs (line 1963-2002 — EditAssessment BeginTransactionAsync analog)
  </read_first>
  <action>
    Verify ROOT CAUSE pre-condition: `grep -q "## Root Cause (Finalized" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-RESEARCH.md` HARUS return 0 (Plan 01 complete). Kalau tidak, ABORT dan minta user run Plan 01 dulu.

    Edit `Controllers/AssessmentAdminController.cs`. Locate method `RegenerateToken(int id)` (line 2424-2475 — full block dari `[HttpPost]` attribute sampai closing `}` method).

    **REPLACE seluruh method** (line 2424-2475, jangan touch line 2477+ `GenerateSecureToken` helper) dengan composite template berikut. Template ini berasal dari PATTERNS.md #H + adjustment per Plan 01 D-37/D-38 verdict:

    ```csharp
    // --- REGENERATE TOKEN ---
    [HttpPost]
    [Authorize(Roles = "Admin, HC")]
    [ValidateAntiForgeryToken]
    public async Task<IActionResult> RegenerateToken(int id)
    {
        var assessment = await _context.AssessmentSessions.FindAsync(id);
        if (assessment == null)
            return Json(new { success = false, message = "Assessment not found." });

        if (!assessment.IsTokenRequired)
            return Json(new { success = false, message = "This assessment does not require a token." });

        // D-25 — Status block (Cancelled / Completed)
        if (assessment.Status == "Cancelled" || assessment.Status == "Completed")
            return Json(new { success = false, message = $"Tidak bisa regenerate token untuk assessment yang sudah {assessment.Status}." });

        // D-37/D-38 — Schedule MinValue guard
        // CONDITIONAL: include block ini HANYA kalau Plan 01 RESEARCH §Data Shape Baseline Query (a) MinValueCount > 0.
        // Kalau Plan 01 confirm count = 0, comment-out block (preserve sebagai dokumentasi).
        // Default (sampai Plan 01 SUMMARY confirm): keep ENABLED — defensive.
        if (assessment.Schedule == DateTime.MinValue)
            return Json(new { success = false, message = "Schedule assessment tidak valid. Hubungi IT." });

        var logger = HttpContext.RequestServices.GetRequiredService<ILogger<AssessmentAdminController>>();
        int siblingCount = 0;
        bool hasStarted = false;
        try
        {
            var newToken = GenerateSecureToken();

            var siblings = await _context.AssessmentSessions
                .Where(a => a.Title == assessment.Title
                         && a.Category == assessment.Category
                         && a.Schedule.Date == assessment.Schedule.Date)
                .ToListAsync();

            siblingCount = siblings.Count;
            hasStarted = siblings.Any(s => s.StartedAt != null);  // D-19

            // D-33 — Sibling 0-row guard + D-35 LogWarning anomaly
            if (siblings.Count == 0)
            {
                logger.LogWarning("RegenerateToken: empty sibling group for session {Id}, title={Title}", id, assessment.Title);
                return Json(new { success = false, message = "Data assessment tidak konsisten — sibling group tidak ditemukan. Hubungi IT." });
            }

            // D-17 — explicit transaction wrap loop UPDATE
            using var tx = await _context.Database.BeginTransactionAsync();
            foreach (var sibling in siblings)
            {
                sibling.AccessToken = newToken;
                sibling.UpdatedAt = DateTime.UtcNow;
            }
            await _context.SaveChangesAsync();
            await tx.CommitAsync();

            // D-06 — Audit log try-catch swallow (DI LUAR transaction)
            var regenUser = await _userManager.GetUserAsync(User);
            var regenActorName = string.IsNullOrWhiteSpace(regenUser?.NIP)
                ? (regenUser?.FullName ?? "Unknown")
                : $"{regenUser.NIP} - {regenUser.FullName}";
            try
            {
                await _auditLog.LogAsync(
                    regenUser?.Id ?? "",
                    regenActorName,
                    "RegenerateToken",
                    $"Regenerated access token for '{assessment.Title}' ({assessment.Category}, {assessment.Schedule:yyyy-MM-dd}) — {siblings.Count} sibling(s) updated",
                    id,
                    "AssessmentSession");
            }
            catch (Exception auditEx)
            {
                logger.LogWarning(auditEx, "Audit log write failed for RegenerateToken session {Id}", id);
            }

            // D-21 — success info log
            logger.LogInformation(
                "RegenerateToken success for session {Id}, {Count} siblings updated by {ActorName}",
                id, siblings.Count, regenActorName);

            return Json(new { success = true, token = newToken, message = "Token regenerated successfully." });
        }
        // D-12 — specific exception catches (order: specific → general)
        catch (DbUpdateException ex)
        {
            logger.LogError(ex,
                "RegenerateToken DB error for session {Id}, status={Status}, hasStarted={HasStarted}, siblingCount={SiblingCount}, isTokenRequired={IsTokenRequired}",
                id, assessment.Status, hasStarted, siblingCount, assessment.IsTokenRequired);
            return Json(new { success = false, message = "Database error: " + ex.Message });
        }
        catch (NullReferenceException ex)
        {
            logger.LogError(ex,
                "RegenerateToken NRE for session {Id}, status={Status}, hasStarted={HasStarted}, siblingCount={SiblingCount}, isTokenRequired={IsTokenRequired}",
                id, assessment.Status, hasStarted, siblingCount, assessment.IsTokenRequired);
            return Json(new { success = false, message = "Data assessment tidak lengkap (sibling/Schedule null). Hubungi IT." });
        }
        catch (Exception ex)
        {
            logger.LogError(ex,
                "RegenerateToken failed for session {Id}, status={Status}, hasStarted={HasStarted}, siblingCount={SiblingCount}, isTokenRequired={IsTokenRequired}",
                id, assessment.Status, hasStarted, siblingCount, assessment.IsTokenRequired);
            return Json(new { success = false, message = ex.Message });
        }
    }
    ```

    **CRITICAL: Decision pivot pada D-37/D-38 conditional inclusion.** Sebelum write file, READ Plan 01 SUMMARY:
    - `cat .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-01-SUMMARY.md | grep -i "MinValue\|D-37\|D-38"`
    - **KALAU Plan 01 SUMMARY says "INCLUDE" / Query (a) > 0:** keep block `if (assessment.Schedule == DateTime.MinValue)` aktif (uncommented)
    - **KALAU Plan 01 SUMMARY says "SKIP" / Query (a) = 0:** comment-out block (preserve text dengan `//` prefix sebagai dokumentasi):
      ```csharp
      // D-37/D-38 — Schedule MinValue guard SKIPPED per Plan 01 RESEARCH Query (a) = 0 (no legacy MinValue rows)
      // if (assessment.Schedule == DateTime.MinValue)
      //     return Json(new { success = false, message = "Schedule assessment tidak valid. Hubungi IT." });
      ```

    **Imports verify:** Pastikan `using Microsoft.EntityFrameworkCore;` ada di top of file (untuk `DbUpdateException`). Existing controller sudah pakai EF Core, kemungkinan import sudah ada — verify dengan grep.

    **dotnet build verify** setelah edit:
    ```bash
    dotnet build --nologo --verbosity quiet
    # Expected: 0 Error(s). Warnings boleh (Phase 311/312/313 baseline 92 warnings).
    ```

    Kalau build fail, fix typo / missing using directive sebelum proceed.
  </action>
  <acceptance_criteria>
    - `dotnet build --nologo --verbosity quiet` exit code = 0
    - `grep -c "BeginTransactionAsync" Controllers/AssessmentAdminController.cs` returns ≥ 4 (existing 3 + 1 baru di RegenerateToken)
    - `grep -c "catch (DbUpdateException" Controllers/AssessmentAdminController.cs` returns ≥ 1
    - `grep -c "catch (NullReferenceException" Controllers/AssessmentAdminController.cs` returns ≥ 1
    - `grep -q "Tidak bisa regenerate token untuk assessment yang sudah" Controllers/AssessmentAdminController.cs` returns 0 (D-25 wording)
    - `grep -q "Data assessment tidak konsisten — sibling group tidak ditemukan" Controllers/AssessmentAdminController.cs` returns 0 (D-33 wording)
    - `grep -q "RegenerateToken: empty sibling group" Controllers/AssessmentAdminController.cs` returns 0 (D-35 LogWarning)
    - `grep -q "RegenerateToken success for session" Controllers/AssessmentAdminController.cs` returns 0 (D-21 LogInformation)
    - `grep -q "Audit log write failed for RegenerateToken" Controllers/AssessmentAdminController.cs` returns 0 (D-06 LogWarning)
    - Existing `GenerateSecureToken` helper (line 2478+) UNTOUCHED — `grep -A 14 "private string GenerateSecureToken" Controllers/AssessmentAdminController.cs` masih show identical body
  </acceptance_criteria>
  <verify>
    <automated>dotnet build --nologo --verbosity quiet &amp;&amp; grep -q "Tidak bisa regenerate token untuk assessment yang sudah" Controllers/AssessmentAdminController.cs &amp;&amp; grep -q "catch (DbUpdateException" Controllers/AssessmentAdminController.cs &amp;&amp; grep -q "catch (NullReferenceException" Controllers/AssessmentAdminController.cs &amp;&amp; grep -q "Audit log write failed for RegenerateToken" Controllers/AssessmentAdminController.cs &amp;&amp; grep -q "RegenerateToken: empty sibling group" Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <done>RegenerateToken method body = 8 layered guards. dotnet build 0 error. GenerateSecureToken helper preserved.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 2 — Frontend handler patches (3 view, sequential per file) -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 2 (W2): Frontend handler #1 — Views/Admin/AssessmentMonitoring.cshtml line 396-419 (querySelectorAll forEach)</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - Views/Admin/AssessmentMonitoring.cshtml (line 380-430 — existing handler context + nearby Razor markup `.btn-regenerate-token` rendering site)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#2.A existing line 391-414 + #2.B refactored line 423-462)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-07, D-09, D-10, D-11, D-22, D-23 REVISED)
  </read_first>
  <action>
    Edit `Views/Admin/AssessmentMonitoring.cshtml`.

    **Step 1: Verify Razor markup `data-status` + `data-started-count` attribute presence.**
    Grep di file untuk locate rendering site `.btn-regenerate-token`:
    ```bash
    grep -n "btn-regenerate-token" Views/Admin/AssessmentMonitoring.cshtml
    ```
    Cari Razor block (kemungkinan dalam loop `@foreach (var session in Model.Sessions)` atau partial). Verify attribute `data-status="@session.Status"` dan `data-started-count` exist.

    **Kalau attribute belum ada:** add dengan format berikut (insert di tag `<button class="btn btn-regenerate-token" ...>`):
    ```html
    data-id="@session.Id"
    data-status="@session.Status"
    data-started-count="@(session.StartedCountSibling)"
    ```
    Note: `StartedCountSibling` = sibling count yang punya StartedAt != null. Kalau ViewModel belum punya field ini:
    - Opsi A (simple): Compute inline di Razor: `data-started-count="@(Model.Sessions.Where(s => s.Title == session.Title && s.Category == session.Category && s.Schedule.Date == session.Schedule.Date && s.StartedAt != null).Count())"` — N+1 query risk kalau Model.Sessions tidak preload. Acceptable kalau page already loaded full list.
    - Opsi B (planner discretion per CONTEXT D-23 Claude's Discretion): tambah field `int StartedCountSibling { get; set; }` ke ViewModel `AssessmentMonitoringRowVm` (atau equivalent), populate di controller GET dengan grouped query. Lebih clean tapi require ViewModel touch.
    - **Default rekomendasi: Opsi A** kalau Model.Sessions sudah present (verify dengan grep nearby). Kalau tidak, fall back ke `data-started-count="0"` static (planner discretion — D-22/D-23 warn dialog tetap fungsi tapi never trigger di Monitoring view; Detail view tetap show warning karena render per-group dengan accurate count).

    **Step 2: Replace handler script block** (line 396-419 atau equivalent setelah Step 1 markup change). Locate existing block:
    ```javascript
    document.querySelectorAll('.btn-regenerate-token').forEach(function (btn) {
        btn.addEventListener('click', function () {
            var id = this.getAttribute('data-id');
            if (!confirm('Regenerate token untuk assessment ini?')) return;
            fetch(appUrl('/Admin/RegenerateToken/' + id), {
                ...
            })
            .then(function (r) { return r.json(); })
            .then(function (data) {
                if (data.success) {
                    alert('Token baru: ' + data.token);
                    location.reload();
                } else {
                    alert('Error: ' + data.message);
                }
            })
            .catch(function (err) {
                alert('Gagal regenerate token. Periksa koneksi jaringan.');
            });
        });
    });
    ```

    Replace dengan refactored handler (PATTERNS.md #2.B verbatim, dengan D-23 REVISED wording per CONTEXT.md update):
    ```javascript
    document.querySelectorAll('.btn-regenerate-token').forEach(function (btn) {
        btn.addEventListener('click', function () {
            var id = this.getAttribute('data-id');
            var startedCount = parseInt(this.getAttribute('data-started-count') || '0', 10);
            var status = this.getAttribute('data-status') || '';
            var msg = 'Regenerate token untuk assessment ini?';
            if (status === 'Open' && startedCount > 0) {
                // D-23 REVISED — token TIDAK invalidate sesi worker yang sudah berjalan
                msg = 'PERINGATAN: ' + startedCount + ' worker sudah masuk ujian. Regenerate token TIDAK akan invalidate sesi mereka yang sudah berjalan — mereka tetap bisa lanjut. Tapi worker lain yang belum masuk dan punya token lama akan ditolak login. Lanjutkan?';
            }
            if (!confirm(msg)) return;

            fetch(appUrl('/Admin/RegenerateToken/' + id), {
                method: 'POST',
                headers: {
                    'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
                }
            })
            .then(function (r) {
                if (!r.ok) {
                    return r.text().then(function (t) {
                        return Promise.reject(t || ('HTTP ' + r.status));
                    });
                }
                return r.json();
            })
            .then(function (data) {
                if (data.success) {
                    alert('Token baru: ' + data.token);
                    location.reload();
                } else {
                    return Promise.reject(data.message || 'Unknown server error');
                }
            })
            .catch(function (err) {
                alert('Gagal regenerate token: ' + err + '. Coba lagi atau hubungi IT.');
            });
        });
    });
    ```

    **CRITICAL preserve (PATTERNS.md anti-pattern):** Pertahankan `querySelectorAll('.btn-regenerate-token').forEach` — jangan switch ke `document.body.addEventListener` (handler #3 pattern). Monitoring view RENDER FULL LIST di server-side, no HTMX swap → forEach OK.

    **dotnet build verify** setelah edit (cshtml compiles via Razor):
    ```bash
    dotnet build --nologo --verbosity quiet
    # Expected: 0 Error(s).
    ```
  </action>
  <acceptance_criteria>
    - `dotnet build --nologo --verbosity quiet` exit code = 0
    - `grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" Views/Admin/AssessmentMonitoring.cshtml` returns 0
    - `grep -q "if (!r.ok)" Views/Admin/AssessmentMonitoring.cshtml` returns 0 (D-11 fallback)
    - `grep -q "Gagal regenerate token: ' + err + '. Coba lagi atau hubungi IT." Views/Admin/AssessmentMonitoring.cshtml` returns 0
    - `grep -c "querySelectorAll('.btn-regenerate-token').forEach" Views/Admin/AssessmentMonitoring.cshtml` returns ≥ 1 (preserved convention)
    - `grep -q "Periksa koneksi jaringan" Views/Admin/AssessmentMonitoring.cshtml` returns 1 (wording lama TIDAK ada — `! grep ... && exit 0`)
    - `grep -q "data-status=\"@" Views/Admin/AssessmentMonitoring.cshtml` returns 0 (markup attribute added) ATAU planner explicitly noted fallback `data-status=\"\"` static
  </acceptance_criteria>
  <verify>
    <automated>dotnet build --nologo --verbosity quiet &amp;&amp; grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" Views/Admin/AssessmentMonitoring.cshtml &amp;&amp; grep -q "if (!r.ok)" Views/Admin/AssessmentMonitoring.cshtml &amp;&amp; ! grep -q "Periksa koneksi jaringan" Views/Admin/AssessmentMonitoring.cshtml &amp;&amp; grep -q "querySelectorAll('.btn-regenerate-token').forEach" Views/Admin/AssessmentMonitoring.cshtml</automated>
  </verify>
  <done>Handler #1 refactored. PERINGATAN wording REVISED + r.ok fallback + Promise.reject propagate. Wording lama removed. forEach convention preserved.</done>
</task>

<task type="auto">
  <name>Task 3 (W2): Frontend handler #2 — Views/Admin/AssessmentMonitoringDetail.cshtml line 1004-1033 (named function regenToken + spinner)</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (line 990-1040 — existing handler context + spinner pattern)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#3.A existing line 482-511 + #3.B refactored line 526-569)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-07, D-09, D-11, D-22, D-23 REVISED)
  </read_first>
  <action>
    Edit `Views/Admin/AssessmentMonitoringDetail.cshtml`.

    **Step 1: Verify caller markup** — function `regenToken(this)` di-call via `onclick=` attribute. Locate dengan grep:
    ```bash
    grep -n "regenToken(this)" Views/Admin/AssessmentMonitoringDetail.cshtml
    ```
    Verify caller button juga punya `data-id`, `data-status`, `data-started-count` attributes. Detail view per-group rendering, jadi accurate startedCount available di Razor model. Kalau belum ada, add dengan format:
    ```html
    data-id="@Model.Id"
    data-status="@Model.Status"
    data-started-count="@(Model.Sessions.Count(s => s.StartedAt != null))"
    ```

    **Step 2: Replace `regenToken(btn)` function** (line 1004-1033 atau equivalent). Locate existing:
    ```javascript
    function regenToken(btn) {
        if (!confirm('Regenerate token untuk grup ini? Token lama tidak akan bisa digunakan.')) return;
        var id = btn.getAttribute('data-id');
        var token = document.querySelector('#antiforgeryForm input[name="__RequestVerificationToken"]').value;
        btn.disabled = true;
        var originalHtml = btn.innerHTML;
        btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span>';

        fetch(appUrl('/Admin/RegenerateToken/' + id), {
            method: 'POST',
            headers: { 'RequestVerificationToken': token }
        })
        .then(function (r) { return r.json(); })
        .then(function (data) {
            if (data.success) {
                document.getElementById('token-display').textContent = data.token;
                var labelEl = document.getElementById('copy-label');
                if (labelEl) { labelEl.textContent = 'Copy'; }
            } else {
                alert('Error: ' + (data.message || 'Gagal regenerate token.'));
            }
        })
        .catch(function () {
            alert('Terjadi kesalahan saat regenerate token.');
        })
        .finally(function () {
            btn.disabled = false;
            btn.innerHTML = originalHtml;
        });
    }
    ```

    Replace dengan refactored composite (PATTERNS.md #3.B verbatim — preserve spinner + named function `regenToken(btn)` signature, ADD warning conditional D-22/D-23 + r.ok D-11 fallback + propagate D-07):
    ```javascript
    function regenToken(btn) {
        var id = btn.getAttribute('data-id');
        var startedCount = parseInt(btn.getAttribute('data-started-count') || '0', 10);
        var status = btn.getAttribute('data-status') || '';
        var msg = 'Regenerate token untuk grup ini? Token lama tidak akan bisa digunakan untuk login worker baru.';
        if (status === 'Open' && startedCount > 0) {
            // D-23 REVISED — token TIDAK invalidate sesi worker yang sudah berjalan
            msg = 'PERINGATAN: ' + startedCount + ' worker sudah masuk ujian. Regenerate token TIDAK akan invalidate sesi mereka yang sudah berjalan — mereka tetap bisa lanjut. Tapi worker lain yang belum masuk dan punya token lama akan ditolak login. Lanjutkan?';
        }
        if (!confirm(msg)) return;

        var token = document.querySelector('#antiforgeryForm input[name="__RequestVerificationToken"]').value;
        btn.disabled = true;
        var originalHtml = btn.innerHTML;
        btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span>';

        fetch(appUrl('/Admin/RegenerateToken/' + id), {
            method: 'POST',
            headers: { 'RequestVerificationToken': token }
        })
        .then(function (r) {
            if (!r.ok) {
                return r.text().then(function (t) {
                    return Promise.reject(t || ('HTTP ' + r.status));
                });
            }
            return r.json();
        })
        .then(function (data) {
            if (data.success) {
                document.getElementById('token-display').textContent = data.token;
                var labelEl = document.getElementById('copy-label');
                if (labelEl) { labelEl.textContent = 'Copy'; }
            } else {
                return Promise.reject(data.message || 'Unknown server error');
            }
        })
        .catch(function (err) {
            alert('Gagal regenerate token: ' + err + '. Coba lagi atau hubungi IT.');
        })
        .finally(function () {
            btn.disabled = false;
            btn.innerHTML = originalHtml;
        });
    }
    ```

    **CRITICAL preserve (PATTERNS.md):**
    1. Function name `regenToken(btn)` — JANGAN rename (caller `onclick="regenToken(this)"` di markup)
    2. Spinner UX — `btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span>'` + `.finally()` restore originalHtml
    3. Token-display swap: `document.getElementById('token-display').textContent = data.token` + skip `location.reload()` (Detail view inline display, bukan list reload)
    4. AntiForgery via header `RequestVerificationToken` (handler #2 convention)

    **dotnet build verify** setelah edit.
  </action>
  <acceptance_criteria>
    - `dotnet build --nologo --verbosity quiet` exit code = 0
    - `grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 0
    - `grep -q "if (!r.ok)" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 0
    - `grep -q "spinner-border spinner-border-sm" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 0 (preserved)
    - `grep -q "function regenToken(btn)" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 0 (function name preserved)
    - `grep -q "document.getElementById('token-display')" Views/Admin/AssessmentMonitoringDetail.cshtml` returns 0 (preserved)
    - `! grep -q "Terjadi kesalahan saat regenerate token" Views/Admin/AssessmentMonitoringDetail.cshtml` (wording lama removed)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build --nologo --verbosity quiet &amp;&amp; grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" Views/Admin/AssessmentMonitoringDetail.cshtml &amp;&amp; grep -q "if (!r.ok)" Views/Admin/AssessmentMonitoringDetail.cshtml &amp;&amp; grep -q "spinner-border spinner-border-sm" Views/Admin/AssessmentMonitoringDetail.cshtml &amp;&amp; grep -q "function regenToken(btn)" Views/Admin/AssessmentMonitoringDetail.cshtml &amp;&amp; ! grep -q "Terjadi kesalahan saat regenerate token" Views/Admin/AssessmentMonitoringDetail.cshtml</automated>
  </verify>
  <done>Handler #2 refactored. Spinner + named function preserved. PERINGATAN wording REVISED. r.ok fallback + propagate.</done>
</task>

<task type="auto">
  <name>Task 4 (W2): Frontend handler #3 — Views/Admin/ManageAssessment.cshtml line 447-471 (HTMX document.body delegation)</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - Views/Admin/ManageAssessment.cshtml (line 440-475 — existing handler + nearby HTMX swap markup)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#4.A existing line 587-611 + #4.B refactored line 624-660 + Pitfall 5 anti-pattern: JANGAN switch ke querySelectorAll)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-07, D-09, D-11, D-22, D-23 REVISED)
  </read_first>
  <action>
    Edit `Views/Admin/ManageAssessment.cshtml`.

    **Step 1: Verify markup** — buttons `.btn-regenerate-token` di-render dalam HTMX-swapped partial. Verify rendering site (kemungkinan partial `_AssessmentRow.cshtml` atau in-line dalam table). Add `data-status` + `data-started-count` attributes:
    ```html
    data-id="@assessment.Id"
    data-status="@assessment.Status"
    data-started-count="@(...computed...)"
    ```
    Note: ManageAssessment list paginated dengan HTMX, kalau startedCount expensive untuk compute per-row, fall back ke `data-started-count="0"` static (planner discretion — warn dialog tetap fungsi via Detail view yang accurate).

    **Step 2: Replace handler script** (line 447-471 atau equivalent). Locate existing:
    ```javascript
    // Regenerate Token handler — preserved dari versi lama
    document.body.addEventListener('click', function(evt) {
        var btn = evt.target.closest('.btn-regenerate-token');
        if (!btn) return;
        (async function() {
            var id = btn.dataset.id;
            var token = document.querySelector('input[name="__RequestVerificationToken"]')?.value
                || document.querySelector('[name="__RequestVerificationToken"]')?.value;
            try {
                var res = await fetch(appUrl('/Admin/RegenerateToken/' + id), {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                    body: '__RequestVerificationToken=' + encodeURIComponent(token)
                });
                var data = await res.json();
                if (data.success) {
                    alert('Token baru: ' + data.token);
                } else {
                    alert('Error: ' + data.message);
                }
            } catch (e) {
                alert('Gagal regenerate token.');
            }
        })();
    });
    ```

    Replace dengan refactored handler (PATTERNS.md #4.B verbatim — preserve `document.body.addEventListener` delegation + `closest()` + body-encoded CSRF + async/await pattern, ADD confirm + warning D-22/D-23 + r.ok D-11 fallback + propagate D-07):
    ```javascript
    // Regenerate Token handler — Phase 314 refactor (preserve HTMX-friendly delegation)
    document.body.addEventListener('click', function(evt) {
        var btn = evt.target.closest('.btn-regenerate-token');
        if (!btn) return;
        (async function() {
            var id = btn.dataset.id;
            var startedCount = parseInt(btn.dataset.startedCount || '0', 10);
            var status = btn.dataset.status || '';
            var msg = 'Regenerate token untuk assessment ini?';
            if (status === 'Open' && startedCount > 0) {
                // D-23 REVISED
                msg = 'PERINGATAN: ' + startedCount + ' worker sudah masuk ujian. Regenerate token TIDAK akan invalidate sesi mereka yang sudah berjalan — mereka tetap bisa lanjut. Tapi worker lain yang belum masuk dan punya token lama akan ditolak login. Lanjutkan?';
            }
            if (!confirm(msg)) return;

            var token = document.querySelector('input[name="__RequestVerificationToken"]')?.value
                || document.querySelector('[name="__RequestVerificationToken"]')?.value;
            try {
                var res = await fetch(appUrl('/Admin/RegenerateToken/' + id), {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                    body: '__RequestVerificationToken=' + encodeURIComponent(token)
                });
                if (!res.ok) {
                    var text = await res.text();
                    throw new Error(text || ('HTTP ' + res.status));
                }
                var data = await res.json();
                if (data.success) {
                    alert('Token baru: ' + data.token);
                } else {
                    throw new Error(data.message || 'Unknown server error');
                }
            } catch (e) {
                alert('Gagal regenerate token: ' + e.message + '. Coba lagi atau hubungi IT.');
            }
        })();
    });
    ```

    **CRITICAL preserve (PATTERNS.md Pitfall 5):**
    1. `document.body.addEventListener('click', ...)` — JANGAN switch ke querySelectorAll forEach. HTMX lazy load partial swap rows; non-delegated listeners tidak fire untuk dynamically-loaded rows.
    2. `evt.target.closest('.btn-regenerate-token')` — preserve delegation pattern
    3. `Content-Type: application/x-www-form-urlencoded` body-encoded CSRF — JANGAN ubah ke header (handler #1/#2 convention berbeda karena ManageAssessment markup tidak punya `<form>` wrapper individual)
    4. async/await — JANGAN convert ke `.then()` chain (preserve handler #3 idiom)

    **dotnet build verify**.
  </action>
  <acceptance_criteria>
    - `dotnet build --nologo --verbosity quiet` exit code = 0
    - `grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" Views/Admin/ManageAssessment.cshtml` returns 0
    - `grep -q "document.body.addEventListener" Views/Admin/ManageAssessment.cshtml` returns 0 (delegation preserved)
    - `grep -q "evt.target.closest('.btn-regenerate-token')" Views/Admin/ManageAssessment.cshtml` returns 0 (closest preserved)
    - `grep -q "application/x-www-form-urlencoded" Views/Admin/ManageAssessment.cshtml` returns 0 (body-encoded CSRF preserved)
    - `grep -q "if (!res.ok)" Views/Admin/ManageAssessment.cshtml` returns 0 (D-11 fallback)
    - `grep -c "Gagal regenerate token: ' + e.message" Views/Admin/ManageAssessment.cshtml` returns ≥ 1 (D-09 propagate)
    - `! grep -q "alert('Gagal regenerate token.');" Views/Admin/ManageAssessment.cshtml` (wording lama removed — exact match `'Gagal regenerate token.'` standalone)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build --nologo --verbosity quiet &amp;&amp; grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" Views/Admin/ManageAssessment.cshtml &amp;&amp; grep -q "document.body.addEventListener" Views/Admin/ManageAssessment.cshtml &amp;&amp; grep -q "evt.target.closest('.btn-regenerate-token')" Views/Admin/ManageAssessment.cshtml &amp;&amp; grep -q "application/x-www-form-urlencoded" Views/Admin/ManageAssessment.cshtml &amp;&amp; grep -q "if (!res.ok)" Views/Admin/ManageAssessment.cshtml</automated>
  </verify>
  <done>Handler #3 refactored. HTMX delegation + body-encoded CSRF + async/await preserved. PERINGATAN wording REVISED. r.ok fallback + throw + propagate.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 3 — E2E spec + UAT + SEED_JOURNAL + sign-off -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 5 (W3): NEW Playwright spec — tests/e2e/admin-assessment-token.spec.ts (3 skenario)</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - tests/e2e/assessment.spec.ts (line 1-22 imports + line 584-757 FLOW 12 Phase 312 admin-assessment-delete pattern dengan dedicated fixture title + status badge-scoped row selector)
    - tests/e2e/assessment.spec.ts (line 266-397 FLOW 9 Phase 310 page.on('dialog') multi-handler pattern)
    - tests/helpers/auth.ts (login function)
    - tests/helpers/accounts.ts (admin fixture)
    - tests/e2e/helpers/wizardSelectors.ts (optional extend with tokenRegen namespace)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#5 NEW spec template line 683-806)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-13, D-14, D-15, D-16, D-22, D-23 REVISED, D-24)
  </read_first>
  <action>
    Create file baru `tests/e2e/admin-assessment-token.spec.ts` dengan content (PATTERNS.md #5 verbatim, dengan adjustment per D-23 REVISED wording + Phase 312 anti-pattern lessons):

    ```typescript
    import { test, expect, Page } from '@playwright/test';
    import { login } from '../helpers/auth';

    test.describe.configure({ mode: 'serial' });

    // Helper: search on AssessmentMonitoring search input
    async function searchAssessment(page: Page, term: string) {
      const searchInput = page.getByPlaceholder('Cari berdasarkan judul,');
      await searchInput.fill(term);
      await searchInput.press('Enter');
      await page.waitForLoadState('networkidle');
    }

    // ============================================================
    // FLOW 13: Phase 314 — Regenerate Token Defensive Patch
    // REQ: TKN-01 (3 success criteria covered by E2E)
    // 13.1 — Upcoming + 0 worker started → regen success + token rotated + AuditLog row
    // 13.2 — Upcoming + sebagian worker started → regen success (no PERINGATAN dialog karena Status=Upcoming bukan Open)
    // 13.3 — Open + worker running → PERINGATAN dialog confirm → regen success
    // ============================================================
    test.describe('Assessment - Phase 314 Regenerate Token Defensive Patch', () => {

      test.beforeEach(async ({ page }) => {
        await login(page, 'admin');
      });

      test('13.1 - Upcoming + 0 worker started → regen success + alert "Token baru: XXXXXX"', async ({ page }) => {
        // PHASE 314 dedicated fixture (Phase 312 WR-04 lesson — exact title, no uniqueTitle)
        const fixtureTitle = 'Phase 314 Token Fixture Upcoming0';
        await page.goto('/Admin/AssessmentMonitoring');
        await searchAssessment(page, fixtureTitle);

        // Phase 312 WR-03 lesson — status badge-scoped row selector (NOT substring match)
        const row = page.locator('tr', { hasText: fixtureTitle })
          .filter({ has: page.locator('span.badge', { hasText: /^Upcoming$/ }) })
          .first();
        if (await row.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — manual seed required (Status=Upcoming + IsTokenRequired=true + 0 StartedAt). Lihat 314-UAT.md Pre-conditions.`);
        }

        const regenBtn = row.locator('.btn-regenerate-token').first();
        await expect(regenBtn).toBeVisible();

        // Multi-dialog handler (Phase 310 FLOW 9 pattern) — branch by message prefix
        let alertMessage = '';
        let warningSeen = false;
        page.on('dialog', async dialog => {
          const m = dialog.message();
          if (m.startsWith('PERINGATAN:')) {
            warningSeen = true;
            await dialog.accept();
          } else if (m.startsWith('Regenerate token')) {
            // plain confirm — Status=Upcoming, no warning expected
            await dialog.accept();
          } else if (m.startsWith('Token baru:')) {
            alertMessage = m;
            await dialog.accept();
          } else {
            await dialog.dismiss();
          }
        });

        await Promise.all([
          page.waitForResponse(res => res.url().includes('/Admin/RegenerateToken/') && res.status() === 200),
          regenBtn.click()
        ]);

        await page.waitForTimeout(1_000);
        // D-15d UI assertion + RESEARCH §GenerateSecureToken alphabet
        expect(alertMessage).toMatch(/^Token baru: [A-Z2-9]{6}$/);
        // Status=Upcoming → NO PERINGATAN dialog (D-22 only triggers for Open + activeWorker)
        expect(warningSeen).toBe(false);
      });

      test('13.2 - Upcoming + sebagian worker started → regen success (no PERINGATAN karena Status≠Open)', async ({ page }) => {
        const fixtureTitle = 'Phase 314 Token Fixture UpcomingPartial';
        await page.goto('/Admin/AssessmentMonitoring');
        await searchAssessment(page, fixtureTitle);

        const row = page.locator('tr', { hasText: fixtureTitle })
          .filter({ has: page.locator('span.badge', { hasText: /^Upcoming$/ }) })
          .first();
        if (await row.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — manual seed required (Status=Upcoming + StartedAt subset != null). Lihat 314-UAT.md Pre-conditions.`);
        }

        const regenBtn = row.locator('.btn-regenerate-token').first();
        let alertMessage = '';
        let warningSeen = false;
        page.on('dialog', async dialog => {
          const m = dialog.message();
          if (m.startsWith('PERINGATAN:')) {
            warningSeen = true;
            await dialog.accept();
          } else if (m.startsWith('Regenerate token')) {
            await dialog.accept();
          } else if (m.startsWith('Token baru:')) {
            alertMessage = m;
            await dialog.accept();
          } else {
            await dialog.dismiss();
          }
        });

        await Promise.all([
          page.waitForResponse(res => res.url().includes('/Admin/RegenerateToken/') && res.status() === 200),
          regenBtn.click()
        ]);

        await page.waitForTimeout(1_000);
        expect(alertMessage).toMatch(/^Token baru: [A-Z2-9]{6}$/);
        // Status=Upcoming → NO PERINGATAN, walaupun ada StartedAt subset
        expect(warningSeen).toBe(false);
      });

      test('13.3 - Open + worker running → PERINGATAN dialog (TIDAK invalidate sesi) → confirm → regen success', async ({ page }) => {
        const fixtureTitle = 'Phase 314 Token Fixture OpenRunning';
        await page.goto('/Admin/AssessmentMonitoring');
        await searchAssessment(page, fixtureTitle);

        const row = page.locator('tr', { hasText: fixtureTitle })
          .filter({ has: page.locator('span.badge', { hasText: /^Open$/ }) })
          .first();
        if (await row.count() === 0) {
          test.skip(true, `Seed "${fixtureTitle}" tidak ditemukan — manual seed required (Status=Open + StartedAt subset != null). Lihat 314-UAT.md Pre-conditions.`);
        }

        let alertMessage = '';
        let warningSeen = false;
        page.on('dialog', async dialog => {
          const m = dialog.message();
          if (m.startsWith('PERINGATAN:')) {
            warningSeen = true;
            // D-23 REVISED wording verify (RESEARCH §D-24 finding)
            expect(m).toContain('TIDAK akan invalidate sesi mereka yang sudah berjalan');
            await dialog.accept();
          } else if (m.startsWith('Token baru:')) {
            alertMessage = m;
            await dialog.accept();
          } else {
            await dialog.dismiss();
          }
        });

        const regenBtn = row.locator('.btn-regenerate-token').first();
        await Promise.all([
          page.waitForResponse(res => res.url().includes('/Admin/RegenerateToken/') && res.status() === 200),
          regenBtn.click()
        ]);

        await page.waitForTimeout(1_000);
        expect(warningSeen).toBe(true);  // D-22 PERINGATAN dialog triggered
        expect(alertMessage).toMatch(/^Token baru: [A-Z2-9]{6}$/);  // D-15d success
      });
    });
    ```

    **CRITICAL preserve patterns:**
    1. **Dedicated fixture title** EXACT (no `uniqueTitle()`) — Phase 312 WR-04 lesson: `Phase 314 Token Fixture Upcoming0`, `Phase 314 Token Fixture UpcomingPartial`, `Phase 314 Token Fixture OpenRunning`
    2. **Status badge-scoped row selector** — Phase 312 WR-03 lesson: `.filter({ has: page.locator('span.badge', { hasText: /^Upcoming$/ }) })` (regex anchor `^...$`, NOT substring `Upcoming`)
    3. **test.skip() graceful** kalau seed missing — Phase 312 pattern, JANGAN fail. Manual seed via UI Admin (D-14) per 314-UAT.md Pre-conditions
    4. **Multi-dialog handler** branch by message prefix — Phase 310 FLOW 9 pattern. Register BEFORE click
    5. **Token alphabet regex** `/^Token baru: [A-Z2-9]{6}$/` — `GenerateSecureToken` chars `"ABCDEFGHJKLMNPQRSTUVWXYZ23456789"` (exclude 0/O/1/I/L)

    **Run command verify:**
    ```bash
    cd tests && npx playwright test e2e/admin-assessment-token.spec.ts --reporter=list
    ```
    Expected: 3 tests RUN. Kalau seed belum ada (default state), 3 tests SKIP (graceful — bukan error). Kalau seed ada (manual setup di Wave 3 Task 7 UAT), 3 tests PASS atau ekspos bug yang harus di-fix di Wave 1 Task 1 backend.
  </action>
  <acceptance_criteria>
    - File `tests/e2e/admin-assessment-token.spec.ts` ada
    - `grep -c "Phase 314 Token Fixture" tests/e2e/admin-assessment-token.spec.ts` returns ≥ 3 (3 fixture title)
    - `grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" tests/e2e/admin-assessment-token.spec.ts` returns 0
    - `grep -q "span.badge" tests/e2e/admin-assessment-token.spec.ts` returns 0 (badge-scoped selector)
    - `grep -q "test.skip(true" tests/e2e/admin-assessment-token.spec.ts` returns 0 (graceful skip)
    - `grep -q "/^Token baru: \\[A-Z2-9\\]{6}$/" tests/e2e/admin-assessment-token.spec.ts` returns 0 (regex anchor + token alphabet)
    - `cd tests && npx playwright test e2e/admin-assessment-token.spec.ts --reporter=list --list 2>&1 | grep -c "13\\."` returns ≥ 3 (3 tests detected)
    - TypeScript compile: `cd tests && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/e2e/admin-assessment-token.spec.ts &amp;&amp; grep -c "Phase 314 Token Fixture" tests/e2e/admin-assessment-token.spec.ts | awk '$1 &gt;= 3 { exit 0 } { exit 1 }' &amp;&amp; grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" tests/e2e/admin-assessment-token.spec.ts &amp;&amp; grep -q "span.badge" tests/e2e/admin-assessment-token.spec.ts &amp;&amp; grep -q "test.skip(true" tests/e2e/admin-assessment-token.spec.ts</automated>
  </verify>
  <done>Spec file created. 3 tests dengan dedicated fixture title + badge-scoped selector + graceful skip + multi-dialog handler. TypeScript compile clean.</done>
</task>

<task type="auto">
  <name>Task 6 (W3): NEW UAT — .planning/phases/314-.../314-UAT.md (6-step Bahasa Indonesia)</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - .planning/phases/308-prepost-wizard-validation-fix/308-UAT.md (4-step format precedent)
    - .planning/phases/312-admin-full-delete-assessment-room/312-UAT.md (admin destructive endpoint precedent — closest domain match)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#6.A header + Coverage Matrix template line 822-866)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-CONTEXT.md (D-25, D-33, all guards)
  </read_first>
  <action>
    Create file baru `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` dengan content (Phase 312 format + Phase 314 6-step coverage):

    ```markdown
    # Phase 314 — Manual UAT Script

    **Phase:** 314-fix-regenerate-token-untuk-status-upcoming
    **REQ:** TKN-01 (Fix Regenerate Token untuk Status Upcoming)
    **Created:** 2026-05-08
    **Mandatory before:** `/gsd-verify-work` Phase 314

    > Manual UAT cover 6 skenario (3 dari ROADMAP SC #6 + 2 D-25 status block edge case + 1 D-07/D-09/D-11 frontend error propagation). Eksekusi di environment lokal (`http://localhost:5277/Admin/AssessmentMonitoring`). UAT di Dev/Prod = tanggung jawab Team IT (per CLAUDE.md).

    > **Catatan RESEARCH §D-24:** Token TIDAK invalidate sesi worker yang sudah berjalan. PERINGATAN dialog wording REVISED — "TIDAK akan invalidate sesi mereka yang sudah berjalan — mereka tetap bisa lanjut. Tapi worker lain yang belum masuk dan punya token lama akan ditolak login."

    ## Coverage Matrix

    | Step | Skenario | Status seed | StartedAt subset | Expected Result | Sign-off |
    |------|----------|-------------|------------------|-----------------|----------|
    | 1 | Upcoming + 0 worker started | Upcoming | none | Confirm dialog plain → regen OK + alert "Token baru: XXXXXX" + AuditLog row exists + 0 sibling updated kalau Online single, > 1 kalau PrePost | ⬜ |
    | 2 | Upcoming + sebagian worker started | Upcoming | subset (e.g. 1 dari 3) | Confirm dialog plain (no PERINGATAN — Status≠Open) → regen OK | ⬜ |
    | 3 | Open + worker running | Open | subset | PERINGATAN dialog (D-23 REVISED wording mengandung "TIDAK akan invalidate sesi") → confirm → regen OK + worker active session lanjut tanpa error | ⬜ |
    | 4 | Cancelled → block (D-25) | Cancelled | any | alert "Gagal regenerate token: Tidak bisa regenerate token untuk assessment yang sudah Cancelled. Coba lagi atau hubungi IT." | ⬜ |
    | 5 | Completed → block (D-25) | Completed | any | alert "Gagal regenerate token: Tidak bisa regenerate token untuk assessment yang sudah Completed. Coba lagi atau hubungi IT." | ⬜ |
    | 6 | Frontend error propagation (D-07/D-09/D-11) — trigger 500 manual via DevTools | n/a | n/a | alert "Gagal regenerate token: {server message detail}. Coba lagi atau hubungi IT." (BUKAN generic "Periksa koneksi jaringan") | ⬜ |

    ## Pre-conditions

    - App di-build & running lokal: `dotnet build` + `dotnet run` (cek `http://localhost:5277/`)
    - Login fixture: **Admin:** `admin@pertamina.com` / `123456` (lihat `tests/helpers/accounts.ts`)
    - Browser modern (Chrome/Edge/Firefox terbaru)
    - DevTools terbuka (Network tab — verify POST `/Admin/RegenerateToken/{id}` returns 200 dengan JSON shape valid `{success, message, token?}`)
    - Snapshot DB lokal sebelum seed (per CLAUDE.md NEW Seed Data Workflow):
      ```bash
      sqlcmd -S localhost -d HcPortalDB -E -Q "BACKUP DATABASE HcPortalDB TO DISK = 'C:\Temp\HcPortalDB_pre314.bak' WITH FORMAT, INIT, COMPRESSION"
      ```
    - DB seed minimal 5 fixture via UI Admin `/Admin/CreateAssessment` (login admin, buat assessment dengan title sebagai berikut):
      - **Phase 314 Token Fixture Upcoming0**: Status auto-Upcoming (Schedule = today+1), IsTokenRequired=true, peserta 1 (admin sendiri, jangan login as worker untuk start)
      - **Phase 314 Token Fixture UpcomingPartial**: Status=Upcoming, peserta 3, manual UPDATE 1 row UserAssessmentSessions.StartedAt = NOW (atau login as worker 1, masuk ujian, exit)
      - **Phase 314 Token Fixture OpenRunning**: Status=Open (Schedule = today, ExamWindowCloseDate = today+1), peserta 2, login worker, masuk ujian (StartedAt != null), STAY di ujian (jangan submit)
      - **Phase 314 Token Fixture Cancelled**: buat assessment normal, kemudian admin cancel
      - **Phase 314 Token Fixture Completed**: buat assessment dengan Schedule lewat + ExamWindowCloseDate lewat (auto Status=Completed)
    - DB akses (SSMS / DBeaver) untuk verifikasi AccessToken berubah + AuditLog post-action

    ## Step 1 — Upcoming + 0 worker started → regen OK (SC #6 skenario 1)

    **Pre-condition:** Login Admin, navigate `/Admin/AssessmentMonitoring`, cari fixture "Phase 314 Token Fixture Upcoming0".

    **Action:**
    1. Buka DevTools Network tab, filter `RegenerateToken`
    2. Klik tombol "Regenerate Token" pada row fixture
    3. Confirm dialog `'Regenerate token untuk assessment ini?'` muncul (plain — no PERINGATAN karena Status=Upcoming)
    4. Klik OK
    5. Tunggu response — alert `'Token baru: XXXXXX'` (6 char A-Z2-9 exclude 0/O/1/I/L)
    6. Klik OK pada alert, page reload (Monitoring view convention)

    **Expect:**
    - alert MATCH regex `/^Token baru: [A-Z2-9]{6}$/`
    - DevTools Network: POST `/Admin/RegenerateToken/{id}` HTTP 200, response `{success: true, token: "XXXXXX", message: "Token regenerated successfully."}`
    - DB query: `SELECT AccessToken FROM AssessmentSessions WHERE Id = {id}` returns new 6-char token ≠ old token
    - DB query: `SELECT TOP 1 * FROM AuditLogs WHERE ActionType = 'RegenerateToken' AND TargetId = {id} ORDER BY CreatedAt DESC` returns 1 row dengan Description mengandung `"Regenerated access token for 'Phase 314 Token Fixture Upcoming0'"`

    **Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

    ---

    ## Step 2 — Upcoming + sebagian worker started → regen OK no PERINGATAN (SC #6 skenario 2)

    **Pre-condition:** Login Admin, navigate `/Admin/AssessmentMonitoring`, cari fixture "Phase 314 Token Fixture UpcomingPartial".

    **Action:**
    1. Verify di DB: `SELECT COUNT(*) FROM UserAssessmentSessions WHERE AssessmentSessionId = {id} AND StartedAt IS NOT NULL` returns ≥ 1
    2. Klik "Regenerate Token"
    3. Confirm dialog `'Regenerate token untuk assessment ini?'` (plain — Status=Upcoming, walaupun ada StartedAt subset, D-22 tidak trigger karena status≠Open)
    4. Klik OK
    5. Alert `'Token baru: XXXXXX'`

    **Expect:**
    - **TIDAK** ada PERINGATAN dialog (D-22 condition: status === 'Open' AND startedCount > 0; di sini status='Upcoming' → branch tidak triggered)
    - alert MATCH regex `/^Token baru: [A-Z2-9]{6}$/`
    - DB token rotated

    **Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

    ---

    ## Step 3 — Open + worker running → PERINGATAN dialog → regen OK (SC #6 skenario 3)

    **Pre-condition:** Login Admin, navigate `/Admin/AssessmentMonitoring`, cari fixture "Phase 314 Token Fixture OpenRunning". Pastikan minimal 1 worker masih masuk ujian (di tab browser lain, login as worker, masuk ujian, STAY di /Cmp/StartExam atau /Cmp/Exam page).

    **Action:**
    1. Klik "Regenerate Token"
    2. PERINGATAN dialog muncul dengan wording:
       ```
       PERINGATAN: 1 worker sudah masuk ujian. Regenerate token TIDAK akan invalidate sesi mereka yang sudah berjalan — mereka tetap bisa lanjut. Tapi worker lain yang belum masuk dan punya token lama akan ditolak login. Lanjutkan?
       ```
    3. Klik OK (confirm)
    4. Alert `'Token baru: XXXXXX'`
    5. Switch ke browser tab worker → kerjakan 1 question → submit answer → verify TIDAK error (sesi lanjut)

    **Expect:**
    - PERINGATAN dialog mengandung **"TIDAK akan invalidate sesi mereka yang sudah berjalan"** (D-23 REVISED wording, RESEARCH §D-24 finding)
    - PERINGATAN dialog mengandung **angka {N}** sesuai sibling startedCount actual
    - Worker active session: TIDAK invalidated, TIDAK error 401/403, lanjut bisa submit answer
    - DB token rotated (semua sibling AccessToken berubah)
    - AuditLog row exists

    **Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

    ---

    ## Step 4 — Cancelled → block (D-25)

    **Pre-condition:** Cari fixture "Phase 314 Token Fixture Cancelled" di Monitoring view.

    **Action:**
    1. Klik "Regenerate Token"
    2. Confirm dialog → klik OK
    3. Observe alert

    **Expect:**
    - Alert: `'Gagal regenerate token: Tidak bisa regenerate token untuk assessment yang sudah Cancelled. Coba lagi atau hubungi IT.'`
    - DB: AccessToken TIDAK berubah (verify via SELECT)
    - DB: AuditLog NO row baru untuk action ini (D-06 audit hanya success path)

    **Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

    ---

    ## Step 5 — Completed → block (D-25)

    **Pre-condition:** Cari fixture "Phase 314 Token Fixture Completed".

    **Action & Expect:** Sama dengan Step 4, dengan wording `'... assessment yang sudah Completed.'`.

    **Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

    ---

    ## Step 6 — Frontend error propagation (D-07/D-09/D-11) — manual force 500

    **Pre-condition:** Login Admin, navigate `/Admin/AssessmentMonitoring`, pilih any fixture (Upcoming0 OK).

    **Action (force HTTP 500 via DevTools):**
    1. Buka DevTools → Network tab
    2. Right-click row `RegenerateToken` (kalau pernah triggered) atau pre-emptive: Network tab → "Block request URL" → masukkan pattern `*/Admin/RegenerateToken/*`
    3. Klik "Regenerate Token" lagi
    4. DevTools record request status (kalau Block: net::ERR_BLOCKED_BY_CLIENT; kalau real 500: pakai server-side simulate via temp `throw new Exception("Simulated server error");` di RegenerateToken — ROLLBACK setelah test)
    5. Observe alert wording

    **Expect (D-09 propagate format):**
    - Alert mengandung prefix `'Gagal regenerate token: '`
    - Alert mengandung server message detail (kalau real 500: HTML 500 page text via D-11 r.text fallback; kalau JSON error response: `data.message` content)
    - Alert mengandung suffix `'. Coba lagi atau hubungi IT.'`
    - Alert **TIDAK** mengandung `'Periksa koneksi jaringan'` (wording lama generic — pre-D-07/D-09 fix)

    **Result:** ⬜ PASS / ⬜ FAIL — _catatan:_ ___________

    ---

    ## Sign-off

    - [ ] Step 1 — Upcoming + 0 worker started: PASS
    - [ ] Step 2 — Upcoming + sebagian worker started: PASS
    - [ ] Step 3 — Open + worker running: PASS
    - [ ] Step 4 — Cancelled block: PASS
    - [ ] Step 5 — Completed block: PASS
    - [ ] Step 6 — Frontend error propagation: PASS
    - [ ] Tester: ___________ Tanggal: ___________ Tanda tangan: ___________

    ## Post-test cleanup (per CLAUDE.md NEW Seed Data Workflow)

    - [ ] Update `docs/SEED_JOURNAL.md` Phase 314 entry status `active` → `cleaned`
    - [ ] Restore DB lokal:
      ```bash
      sqlcmd -S localhost -d master -E -Q "USE master; ALTER DATABASE HcPortalDB SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE HcPortalDB FROM DISK = 'C:\Temp\HcPortalDB_pre314.bak' WITH REPLACE; ALTER DATABASE HcPortalDB SET MULTI_USER"
      ```
    - [ ] Verify DB clean: `SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE 'Phase 314 Token Fixture%'` returns 0
    ```

    Verify file dengan grep.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` ada
    - `grep -c "^## Step " .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` returns ≥ 6 (6 step section)
    - `grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` returns 0
    - `grep -q "Phase 314 Token Fixture" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` returns 0 (≥ 5 fixture title mention)
    - `grep -q "BACKUP DATABASE HcPortalDB" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` returns 0 (Seed Workflow snapshot)
    - `grep -q "RESTORE DATABASE HcPortalDB" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` returns 0 (Seed Workflow restore)
    - `grep -q "Sign-off" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md` returns 0
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md &amp;&amp; grep -c "^## Step " .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md | awk '$1 &gt;= 6 { exit 0 } { exit 1 }' &amp;&amp; grep -q "TIDAK akan invalidate sesi mereka yang sudah berjalan" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md &amp;&amp; grep -q "BACKUP DATABASE HcPortalDB" .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md</automated>
  </verify>
  <done>314-UAT.md created dengan 6-step format BI + Coverage Matrix + Pre-conditions seed + Sign-off + Post-test cleanup snapshot/restore.</done>
</task>

<task type="auto">
  <name>Task 7 (W3): SEED_JOURNAL.md entry — Phase 314 fixture classified temporary + local-only</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - docs/SEED_JOURNAL.md (existing template line 1-9)
    - docs/SEED_WORKFLOW.md (classification rules + BACKUP/RESTORE SQL Server)
    - CLAUDE.md (NEW Seed Data Workflow section)
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-PATTERNS.md (#7 SEED_JOURNAL entry template line 901-908)
  </read_first>
  <action>
    Edit `docs/SEED_JOURNAL.md`. Locate line dengan placeholder `_(belum ada entry)_`:
    ```markdown
    | _(belum ada entry)_ | | | | | | |
    ```

    **REPLACE** dengan Phase 314 entry (status=active saat insert, akan di-update jadi cleaned setelah UAT Task 6 post-test cleanup):
    ```markdown
    | 2026-05-08 | 314 | temporary + local-only | Phase 314 Playwright fixture E2E regen token (5 fixture: Upcoming0, UpcomingPartial, OpenRunning, Cancelled, Completed) untuk smoke test 6 skenario UAT + 3 skenario Playwright spec | AssessmentSessions(5+), UserAssessmentSessions(beberapa untuk StartedAt subset di UpcomingPartial + OpenRunning) | C:\Temp\HcPortalDB_pre314.bak | active |
    ```

    Save file. Note: status field = `active` saat ini. Akan di-update ke `cleaned` di Task 8 (final cleanup setelah UAT sign-off).
  </action>
  <acceptance_criteria>
    - `grep -q "Phase 314 Playwright fixture E2E regen token" docs/SEED_JOURNAL.md` returns 0
    - `grep -q "_(belum ada entry)_" docs/SEED_JOURNAL.md` returns 1 (placeholder removed) atau ≥ 1 row baru tanpa placeholder
    - `grep -q "C:\\\\Temp\\\\HcPortalDB_pre314.bak" docs/SEED_JOURNAL.md` returns 0 (snapshot path recorded — note Windows path escaped)
    - `grep -c "| 2026-05-08 | 314 |" docs/SEED_JOURNAL.md` returns ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Phase 314 Playwright fixture E2E regen token" docs/SEED_JOURNAL.md &amp;&amp; grep -q "| 2026-05-08 | 314 |" docs/SEED_JOURNAL.md</automated>
  </verify>
  <done>SEED_JOURNAL entry added — temporary + local-only classification, snapshot file recorded, status=active.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8 (W3): Manual UAT execution + Playwright PASS verify + SEED_JOURNAL cleanup</name>
  <files>see &lt;read_first&gt; below for exact paths to read and modify</files>
  <read_first>
    - .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-UAT.md (6-step manual UAT — created Task 6)
    - tests/e2e/admin-assessment-token.spec.ts (Playwright spec — created Task 5)
    - docs/SEED_JOURNAL.md (entry status=active — created Task 7)
    - CLAUDE.md (Develop Workflow + Seed Data Workflow restore)
  </read_first>
  <what-built>
    Final manual UAT 6-step execution + Playwright spec PASS verify + SEED_JOURNAL status update active → cleaned.
  </what-built>
  <how-to-verify>
    **Pre-step:** Snapshot DB lokal (kalau belum di Task 7):
    ```bash
    sqlcmd -S localhost -d HcPortalDB -E -Q "BACKUP DATABASE HcPortalDB TO DISK = 'C:\Temp\HcPortalDB_pre314.bak' WITH FORMAT, INIT, COMPRESSION"
    ```

    **Step 1: Seed 5 fixture via UI Admin** (per 314-UAT.md Pre-conditions):
    1. `dotnet run` lokal (`http://localhost:5277/`)
    2. Login admin@pertamina.com
    3. Buat 5 fixture sesuai pattern di 314-UAT.md Pre-conditions

    **Step 2: Run Playwright spec** :
    ```bash
    cd tests && npx playwright test e2e/admin-assessment-token.spec.ts --reporter=list
    ```
    Expected: 3 tests PASS (kalau seed lengkap) atau test.skip graceful (kalau partial). Capture output ke `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/07-playwright-output.txt`.

    **Step 3: Manual UAT 6-step** sesuai 314-UAT.md:
    1. Eksekusi Step 1-6 satu per satu, fill Result PASS / FAIL + catatan
    2. Sign-off section: tester nama + tanggal + tanda tangan

    **Step 4: Restore DB + update SEED_JOURNAL**:
    1. ```bash
       sqlcmd -S localhost -d master -E -Q "USE master; ALTER DATABASE HcPortalDB SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE HcPortalDB FROM DISK = 'C:\Temp\HcPortalDB_pre314.bak' WITH REPLACE; ALTER DATABASE HcPortalDB SET MULTI_USER"
       ```
    2. Verify clean: `sqlcmd -S localhost -d HcPortalDB -E -Q "SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE 'Phase 314 Token Fixture%'"` → expected 0
    3. Edit `docs/SEED_JOURNAL.md`, change status `active` → `cleaned` di Phase 314 row

    **Step 5: Final commit prep**:
    1. dotnet build verify lokal: `dotnet build` exit 0
    2. Files modified summary: 3 view, 1 controller, 1 spec, 2 docs (UAT + SEED_JOURNAL)
  </how-to-verify>
  <action>
    Manual checkpoint. JANGAN proceed sampai user sign-off UAT 6/6 PASS + Playwright spec PASS / SKIP graceful + DB restored + SEED_JOURNAL status=cleaned.

    Kalau Playwright spec FAIL untuk specific test (e.g. 13.3 PERINGATAN wording mismatch), ITERATE: fix backend/frontend code di Wave 1 atau Wave 2 task, re-run, sampai PASS.

    Kalau UAT Step gagal (e.g. Step 4 Cancelled tidak block), bug Wave 1 Task 1 belum cover D-25 → fix backend.

    Kalau Step 6 frontend error propagation tidak menampilkan server message detail (alert masih generic "Periksa koneksi jaringan"), bug Wave 2 (handler #1/#2/#3) → re-check refactored handler block, verify `if (!r.ok)` chain dan `Promise.reject(data.message)` propagate.
  </action>
  <acceptance_criteria>
    - `dotnet build --nologo --verbosity quiet` exit 0
    - File `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/07-playwright-output.txt` ada (Playwright run output)
    - 314-UAT.md Sign-off section: 6 step ALL marked PASS (tester nama + tanggal + tanda tangan terisi) — verify dengan `grep -c "PASS$" 314-UAT.md` ≥ 6 atau visual review
    - `grep -q "| 2026-05-08 | 314 |.*| cleaned |" docs/SEED_JOURNAL.md` returns 0 (status active → cleaned)
    - DB clean: `sqlcmd -Q "SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE 'Phase 314 Token Fixture%'"` returns 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build --nologo --verbosity quiet &amp;&amp; test -f .planning/phases/314-fix-regenerate-token-untuk-status-upcoming/repro-evidence/07-playwright-output.txt &amp;&amp; grep -q "| cleaned |" docs/SEED_JOURNAL.md</automated>
  </verify>
  <done>UAT 6/6 PASS, Playwright spec PASS / SKIP graceful, DB restored, SEED_JOURNAL status=cleaned. Phase 314 ready untuk `/gsd-verify-work`.</done>
  <resume-signal>Type "approved" setelah UAT 6/6 PASS, Playwright PASS, DB restored. Atau describe issues yang perlu fix di Wave 1/2 sebelum sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → ASP.NET Core MVC pipeline | Authentication via cookie + AntiForgery token (CSRF). Untrusted input: `id` parameter (route), `__RequestVerificationToken` (header/body). |
| Controller → Database (EF Core) | Trusted server-side. SaveChangesAsync wrapped in transaction (D-17). |
| Controller → AuditLogService | Trusted internal call. Audit failure swallow + LogWarning (D-06). |
| Server JSON response → Frontend handler | Untrusted output kalau attacker control input — but `id` validated via FindAsync (returns null if invalid), token random per request. Server message di JSON `{success, message}` BISA berisi `ex.Message` (D-12 generic catch) — verified safe per RESEARCH §Security: EF Core sanitize connection string. |
| Test fixture seed → DB lokal | Local-only, snapshot/restore protocol (CLAUDE.md NEW Seed Data Workflow). |

## STRIDE Threat Register (Plan 02 — patch + test)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-314-02-D25 | Tampering | Status field bypass (admin re-set Cancelled → click regen) | mitigate | D-25 server-side guard `if (Status == "Cancelled" \|\| "Completed") return Json error`. UI hide button = UX, server adalah authority. |
| T-314-02-D17 | Atomicity violation | Multi-row UPDATE partial commit + AuditLog write fail mid-loop | mitigate | D-17 `BeginTransactionAsync` wrap loop UPDATE + SaveChangesAsync. Audit DI LUAR transaction (D-06) — audit fail tidak rollback token. |
| T-314-02-D06 | Repudiation | Audit log fail → action lost | mitigate | D-06 try-catch swallow + LogWarning fallback. Action tidak rollback (audit best-effort, business action atomic). Plus existing LogInformation success (D-21) provide forensic trace di absence of audit row. |
| T-314-02-D33 | Information Disclosure | Sibling 0-row anomaly silent fail (token rotated tapi siblings.Count=0) | mitigate | D-33 explicit guard return error message + LogWarning. Detect data corruption early. |
| T-314-02-D12 | Information Disclosure (controlled) | Generic `ex.Message` exposed to client | accept | EF Core sanitize connection string per RESEARCH §Security. `DbUpdateException` message umumnya `"An error occurred while saving the entity changes..."` — no PII. `NullReferenceException` mapped ke BI custom message (no ex.Message exposed). |
| T-314-02-D20 | Repudiation | Failed regen tidak terlacak | mitigate | D-20 extended structured logging dengan {Id, Status, HasStarted, SiblingCount, IsTokenRequired} di setiap catch block. Searchable di Application Insights / log aggregator. |
| T-314-02-FE07 | Information Disclosure (controlled) | Frontend alert propagate server message — possible info leak ke admin | accept | Admin/HC role only ([Authorize]). Server messages BI + sanitized — no PII. RESEARCH §Security V7 ASVS L1 acceptable. |
| T-314-02-Seed | Tampering | Test fixture pollute Dev DB | mitigate | Lokal-only seed (CLAUDE.md), BACKUP/RESTORE protocol di UAT Pre-conditions + Post-test cleanup, SEED_JOURNAL audit trail status=active → cleaned. |
| T-314-02-CSRF | Spoofing | Cross-site request to RegenerateToken endpoint | mitigate | Existing `[ValidateAntiForgeryToken]` attribute — preserved. 3 view handler send `__RequestVerificationToken` (header variant A or body-encoded variant B per S1 PATTERNS.md). |
| T-314-02-Authz | Elevation of Privilege | Non-admin trigger regen | mitigate | Existing `[Authorize(Roles = "Admin, HC")]` attribute — preserved per D-26 (no granular ownership, admin role cross-cutting). |
</threat_model>

<verification>
**Per-wave verification gates:**

**Wave 1 (Backend Task 1):**
- `dotnet build --nologo --verbosity quiet` exit 0 (mandatory after Task 1)
- 6 grep verifications per acceptance_criteria

**Wave 2 (Frontend Tasks 2-4):**
- `dotnet build --nologo --verbosity quiet` exit 0 (each task)
- 6+ grep verifications per acceptance_criteria per task
- 3 view convention preserved (querySelectorAll forEach / named function spinner / document.body delegation)

**Wave 3 (Tasks 5-8):**
- TypeScript compile: `cd tests && npx tsc --noEmit` exit 0
- Playwright spec detected: `cd tests && npx playwright test --list` shows 3 tests
- 314-UAT.md 6 step + Coverage Matrix + Sign-off
- SEED_JOURNAL entry recorded
- Final: dotnet build pass + UAT 6/6 PASS + Playwright PASS/SKIP + DB restored + SEED_JOURNAL status=cleaned
</verification>

<success_criteria>
- [ ] **TKN-01 acceptance:** RegenerateToken sukses untuk Status=Upcoming + IsTokenRequired=true + 0 worker (UAT Step 1 + Playwright 13.1 PASS)
- [ ] **ROADMAP SC #1-3:** Plan 01 SUMMARY references repro + root cause documented (Plan 01 dependency)
- [ ] **ROADMAP SC #4:** Logging granular `_logger.LogError(ex, "RegenerateToken failed for session {Id}, status={Status}, hasStarted={HasStarted}", ...)` — extended to siblingCount + isTokenRequired
- [ ] **ROADMAP SC #5:** error message dari server JSON dipropagasi ke alert() di 3 view (Step 6 UAT verifies)
- [ ] **ROADMAP SC #6:** 3 skenario verified PASS (Upcoming+0, Upcoming+sebagian, Open running) — UAT Steps 1-3 + Playwright 13.1-13.3
- [ ] **D-23 REVISED wording** propagated ke 3 view + spec + UAT (RESEARCH §D-24 finding)
- [ ] **D-25 status block** guard Cancelled+Completed (UAT Steps 4-5)
- [ ] **D-33 sibling 0-row** guard explanatory error (defensive — covered di Wave 1)
- [ ] **D-17 transaction wrap** loop UPDATE + commit (verified via grep)
- [ ] **D-06 audit try-catch swallow** + LogWarning (verified via grep)
- [ ] **D-12 specific exception types** DbUpdateException + NullReferenceException + Exception (verified via grep)
- [ ] **D-37/D-38 conditional** Schedule MinValue guard (per Plan 01 Query (a) verdict)
- [ ] **3 view convention preserved** (no anti-pattern regress)
- [ ] **CLAUDE.md compliance:** Lokal verify (dotnet build + manual smoke) sebelum push, NEW Seed Data Workflow (snapshot + SEED_JOURNAL active → cleaned)
- [ ] dotnet build 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/314-fix-regenerate-token-untuk-status-upcoming/314-02-SUMMARY.md` dengan:
- Files modified list (7)
- 8 backend guards applied (with grep evidence per guard)
- 3 view handler refactor diff summary
- Playwright spec result (PASS/SKIP/FAIL counts)
- UAT 6 step result matrix (6/6 expected)
- D-37/D-38 conditional decision applied
- SEED_JOURNAL entry status final (cleaned)
- dotnet build exit code
- Note: Phase 314 closure ready untuk `/gsd-verify-work` setelah Plan 01 + Plan 02 SUMMARY both written
</output>
