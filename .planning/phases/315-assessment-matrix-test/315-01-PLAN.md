---
phase: 315
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md
autonomous: true
requirements: [QA-01]
requirements_addressed: [QA-01]
must_haves:
  truths:
    - "5 open questions di spec (MA save flow, Essay save flow, Notes field, ID collision check, URL encoding) terjawab di Wave 0 investigation"
    - "AssessmentPackage cardinality (A1) terkonfirmasi: 1-per-session atau shared, dengan source line cite"
    - "SubmitExam Essay branch (A2) terkonfirmasi: form value vs DB-persisted (PackageUserResponse.TextAnswer) authoritative"
    - "UserPackageAssignment auto-create vs pre-seed (A6) terkonfirmasi: spec asumsi 9 vs revised 18 sibling sessions resolved"
    - "Final seed cardinality keputusan ditulis: jumlah sessions, packages, questions, options"
  artifacts:
    - path: ".planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md"
      provides: "Wave 0 source-code investigation output"
      contains: "## A1 AssessmentPackage Cardinality, ## A2 SubmitExam Essay Branch, ## A6 UserPackageAssignment, ## Final Seed Dimensions"
  key_links:
    - from: ".planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md"
      to: "Wave 1 seed SQL drafting"
      via: "Final seed dimension numbers (N sessions, N packages, N questions, N options)"
      pattern: "## Final Seed Dimensions"
---

<objective>
Wave 0 source-code investigation: resolve 3 HIGH RISK assumptions (A1, A2, A6) dari RESEARCH.md sebelum drafting seed SQL dimensions di Wave 1.

Purpose: Spec asumsi 9 sessions kemungkinan undercount (Pitfall 3 — RESEARCH.md line 723-737 mengindikasikan 18 sibling sessions diperlukan karena AssessmentSession.UserId adalah single FK). Tanpa resolution definitif, seed SQL akan salah dimensi → Layer 1 validation gagal → spec run kosong.

Output: 1 dokumen `315-INVESTIGATION.md` yang mencatat jawaban A1/A2/A6 dengan source code citation (file path + line range), plus keputusan final seed dimensions (sessions × packages × questions × options count) yang akan dipakai Plan 02 + 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/315-assessment-matrix-test/315-CONTEXT.md
@.planning/phases/315-assessment-matrix-test/315-RESEARCH.md
@.planning/phases/315-assessment-matrix-test/315-PATTERNS.md
@docs/superpowers/specs/2026-05-11-assessment-matrix-test-design.md

<interfaces>
<!-- Key types and source paths the executor needs.
     Executor should use these directly — no codebase exploration needed beyond Wave 0 file reads. -->

Source files to investigate (per CONTEXT D-04):
- `Models/AssessmentPackage.cs` — verify FK relationship to AssessmentSession (1-to-1 vs shared)
- `Models/UserPackageAssignment.cs` — verify auto-create on StartExam vs pre-seed requirement
- `Controllers/CMPController.cs:880-1000` — StartExam package mode + UserPackageAssignment lazy create
- `Controllers/CMPController.cs:1569-1800` — SubmitExam Essay branch (form value vs DB read)
- `Services/GradingService.cs` (or equivalent) — Essay grading: where TextAnswer pulled from?
- `Hubs/AssessmentHub.cs:134-252` — SaveTextAnswer + SaveMultipleAnswer (already partial-read; confirm)
- `Models/AssessmentSession.cs` — Notes field absence (already verified Q3)
- `Data/SeedData.cs` — ID range collision (already verified Q4)

RESEARCH.md sections to consult:
- `## Wave 0 Investigation Answers` — Q1, Q2, Q3, Q4, Q5 baseline answers
- `## Assumptions Log` — A1, A2, A3, A4, A5, A6, A7 risk levels
- `## Open Questions` — 3 items (AssessmentPackage cardinality, SubmitExam Essay, console error whitelist)
- `## Common Pitfalls` Pitfall 3 — full reasoning for 18 sibling sessions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve A1 + A6 — AssessmentPackage cardinality + UserPackageAssignment lifecycle</name>
  <files>.planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md (NEW)</files>
  <read_first>
    - .planning/phases/315-assessment-matrix-test/315-RESEARCH.md (full — fokus § Wave 0 Investigation Answers, § Assumptions Log, § Common Pitfalls Pitfall 3, § Open Questions)
    - .planning/phases/315-assessment-matrix-test/315-CONTEXT.md (D-04 investigation approach)
    - docs/superpowers/specs/2026-05-11-assessment-matrix-test-design.md (commit 94bacecf — fokus § Schema seed + § Endpoint contract)
    - Models/AssessmentPackage.cs (full file — verify properties: AssessmentSessionId FK direction, navigation properties, unique constraint if any)
    - Models/UserPackageAssignment.cs (full file — verify FK to Session + Package, unique key combination, navigation)
    - Controllers/CMPController.cs lines 880-1000 (StartExam method — perhatikan UserPackageAssignment create pattern: ada `dbContext.UserPackageAssignments.Add(new ...)` di mana?)
  </read_first>
  <action>
Buat file BARU `.planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` dengan struktur Bahasa Indonesia berikut:

```markdown
# Phase 315 Wave 0 Investigation Output

**Investigation date:** 2026-05-11
**Investigator:** Wave 0 executor
**Resolves:** A1 (AssessmentPackage cardinality), A2 (SubmitExam Essay branch), A6 (UserPackageAssignment lifecycle)

## A1: AssessmentPackage Cardinality with Sibling Sessions

**Question:** Apakah 1 AssessmentSession punya 1 AssessmentPackage (1-to-1) atau bisa share AssessmentPackage di multiple sibling sessions?

**Source citations:**
- `Models/AssessmentPackage.cs:<line>` — definisi FK `AssessmentSessionId`. Type: <int / int? — actual>. Navigation property: <one-to-one / one-to-many>
- `Controllers/CMPController.cs:<line>` — query pattern: `dbContext.AssessmentPackages.Where(p => p.AssessmentSessionId == ...)` (1-to-1 per session) ATAU `dbContext.AssessmentPackages.Where(p => siblingSessionIds.Contains(p.AssessmentSessionId))` (shared antar siblings).
- (tambah citation tambahan jika perlu)

**Verdict:** [1-PER-SESSION | SHARED-ACROSS-SIBLINGS]

**Rationale:** [1 paragraf, cite line number]

**Implication for seed:**
- Jika 1-PER-SESSION: 18 sibling sessions × 1 package = **18 AssessmentPackage rows**
- Jika SHARED: 9 distinct scenarios × 1 package = **9 AssessmentPackage rows** (shared antar 2 peserta per scenario via composite key Title+Category+Schedule.Date)

## A6: UserPackageAssignment Auto-create vs Pre-seed

**Question:** Apakah seed SQL harus INSERT UserPackageAssignment row eksplisit, atau auto-created saat user pertama kali hit StartExam endpoint?

**Source citations:**
- `Controllers/CMPController.cs:<line>` — pattern `var upa = dbContext.UserPackageAssignments.FirstOrDefault(...) ?? new UserPackageAssignment { ... }` (lazy create) ATAU explicit fail jika tidak ada.
- (tambah citation)

**Verdict:** [AUTO-CREATE-LAZY | MUST-PRESEED]

**Rationale:** [1 paragraf, cite line number]

**Implication for seed:**
- Jika AUTO-CREATE-LAZY: seed SQL skip UserPackageAssignments rows; app akan auto-create saat peserta1/peserta2 pertama buka StartExam.
- Jika MUST-PRESEED: seed SQL WAJIB tambah UserPackageAssignments rows untuk setiap (Session × User) pair = 18 rows.

## A2: SubmitExam Essay Branch — Form Value vs DB-Persisted

**Question:** Untuk question type Essay, saat SubmitExam dipanggil, apakah controller membaca jawaban dari form payload `answers[questionId]=text` atau dari DB record `PackageUserResponse.TextAnswer` yang sudah disimpan via SignalR hub?

**Source citations:**
- `Controllers/CMPController.cs:<line>` — SubmitExam signature: `public IActionResult SubmitExam(int sessionId, Dictionary<int, ...> answers)`. Untuk Essay branch: <line>: `if (question.QuestionType == "Essay") { var text = answers[q.Id].ToString(); ... }` ATAU `var saved = dbContext.PackageUserResponses.FirstOrDefault(...); var text = saved?.TextAnswer ?? "";`
- `Services/GradingService.cs:<line>` (atau wherever Essay grade flow): essay record yang diambil untuk grading.

**Verdict:** [FORM-VALUE-AUTHORITATIVE | DB-PERSISTED-AUTHORITATIVE | HYBRID]

**Rationale:** [1 paragraf, cite line number]

**Implication for test:**
- Jika FORM-VALUE-AUTHORITATIVE: examMatrix.takeExam helper WAJIB jaga `<textarea>` value sampai submit (jangan clear), dan SignalR debounce 2s tidak cukup — perlu blur trigger.
- Jika DB-PERSISTED-AUTHORITATIVE: helper cukup tunggu `#saveIndicatorText=saved` sebelum next question, lalu submit. Server akan tarik dari DB.
- Jika HYBRID: dokumentasi specific branching.

## Final Seed Dimensions

Berdasarkan A1, A6 (revised), tabel final:

| Tabel | Count | ID Range | Rationale |
|-------|-------|----------|-----------|
| AssessmentSessions | [9 atau 18] | [9001-9009 atau 9001-9018] | [cite A1+A6 verdict] |
| AssessmentPackages | [9 atau 18] | [9001-9009 atau 9001-9018] | [per A1 verdict] |
| PackageQuestions | [37, 68, 74, atau lainnya] | [50001-50037 atau 50001-50068 atau 50001-50074] | [per scenario × questions per session] |
| PackageOptions | [124, 248, atau lainnya] | [80001-80124 atau 80001-80248] | [per question × ~4 options] |
| UserPackageAssignments | [0 atau 18] | (auto-generated atau eksplisit) | [per A6 verdict] |

**Layer 1 expected counts (Plan 03 globalSetup):**
- `SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[MATRIX_TEST_2026_05_11]%'` = [9 atau 18]
- `SELECT COUNT(*) FROM AssessmentPackages WHERE AssessmentSessionId IN (SELECT Id FROM AssessmentSessions WHERE Title LIKE '[MATRIX_TEST_2026_05_11]%')` = [9 atau 18]
- (tambah per tabel)

**Layer 4 expected counts (Plan 03 globalTeardown):**
- Semua query di atas = 0 post-RESTORE.

## Investigation Method

Pendekatan: source-code read (per CONTEXT D-04), bukan runtime probe. File yang dibaca:
- Models/AssessmentPackage.cs
- Models/UserPackageAssignment.cs
- Controllers/CMPController.cs (StartExam + SubmitExam sections)
- Services/GradingService.cs (jika ada, atau AssessmentAdminController grading flow)

**Tidak dilakukan:** runtime DB probe (tidak ada Kestrel running, tidak ada test execution).
```

WAJIB:
1. Setiap section A1/A2/A6 punya **minimal 2 source citation dengan format `file:line`**.
2. Verdict harus **DEFINITIF** (bukan "mungkin" atau "kemungkinan") — jika source code ambiguous, baca lebih banyak file sampai jelas.
3. Final Seed Dimensions tabel WAJIB diisi numeric (bukan placeholder `[N]`) — Plan 02 + 03 akan reference angka ini.
4. Bahasa Indonesia untuk seluruh prosa (per CLAUDE.md). Code citation boleh English (sesuai source).
5. Jika selama investigation menemukan info baru yang bertentangan dengan RESEARCH.md, dokumentasikan sebagai `## Deviation from RESEARCH.md` section di akhir file.
  </action>
  <verify>
    <automated>test -f .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md &amp;&amp; grep -c "Verdict:" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md | grep -q "^3$"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` exists (`test -f` returns 0)
    - `grep -c "^## A1" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns 1
    - `grep -c "^## A2" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns 1
    - `grep -c "^## A6" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns 1
    - `grep -c "^## Final Seed Dimensions" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns 1
    - `grep -cE "^\\*\\*Verdict:\\*\\*" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns 3 (one per A1/A2/A6)
    - `grep -cE "Models/AssessmentPackage\\.cs:[0-9]+" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns ≥ 1 (source citation with line number)
    - `grep -cE "Controllers/CMPController\\.cs:[0-9]+" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns ≥ 2 (StartExam + SubmitExam citations)
    - Final Seed Dimensions tabel berisi numeric values (bukan placeholder `[N]` atau `TBD`): `grep -cE "\\| (9|18) \\|" .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` returns ≥ 2
  </acceptance_criteria>
  <done>
    315-INVESTIGATION.md exists dengan 3 verdict definitif (A1, A2, A6), masing-masing minimal 2 source citation (file:line), dan Final Seed Dimensions tabel berisi numeric counts untuk semua tabel. Plan 02 + 03 dapat reference angka ini langsung.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Investigation output → Plan 02/03 consumer | Numeric seed dimensions consumed verbatim by SQL seed authoring; misread → wrong dimensions seeded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-315-INVEST-01 | Information Disclosure | 315-INVESTIGATION.md | accept | Read-only investigation, no secret/PII content; document hanya berisi source code references + dimension numbers |
| T-315-INVEST-02 | Tampering | Final dimension misread by downstream plan | mitigate | Acceptance criteria require numeric (not placeholder) values; Plan 02 + 03 will fail validation if dimensions absent |

(T-315-01..T-315-05 dari security_gate tidak applicable di Wave 0 — Plan ini tidak sentuh DB, tidak run sqlcmd, tidak create seed file.)
</threat_model>

<verification>
- File `.planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md` exists
- 3 sections (A1, A2, A6) plus Final Seed Dimensions
- Minimum 2 source citations per assumption (file:line format)
- Verdicts definitif (bukan tentatif)
- Final dimension table numeric, ready consume by Plan 02 + 03
</verification>

<success_criteria>
- 5 open questions (Q1 MA, Q2 Essay, Q3 Notes, Q4 ID collision, Q5 URL encoding) sudah terjawab di RESEARCH.md; Wave 0 ini fokus 3 HIGH RISK assumption (A1, A2, A6) yang belum tuntas
- Plan 02 + 03 dapat baca `## Final Seed Dimensions` untuk angka eksak
- Tidak ada DB write, tidak ada code change di production source — Wave 0 read-only
</success_criteria>

<output>
After completion, create `.planning/phases/315-assessment-matrix-test/315-01-SUMMARY.md` dengan:
- A1 verdict + 1-line rationale
- A2 verdict + 1-line rationale
- A6 verdict + 1-line rationale
- Final dimension table (compact)
- Files read (path + line range)
- Deviation from RESEARCH.md (jika ada)
</output>
