---
phase: 315
plan: 03
type: execute
wave: 2
depends_on: [315-01, 315-02]
files_modified:
  - tests/sql/assessment-matrix-seed.sql
  - tests/e2e/global.setup.ts
  - tests/e2e/global.teardown.ts
  - tests/playwright.config.ts
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [QA-01]
requirements_addressed: [QA-01]
must_haves:
  truths:
    - "tests/sql/assessment-matrix-seed.sql berisi hierarchical INSERT (Sessions → Packages → Questions → Options) idempotent"
    - "Seed pakai Title prefix marker `[MATRIX_TEST_2026_05_11]` (D-05 fallback — Notes field tidak ada)"
    - "global.setup.ts: pre-check collision → BACKUP → execScript → Layer 1 validation → state.json write → journal append"
    - "global.teardown.ts: collector.flush FIRST → RESTORE → Layer 4 validation → journal cleaned → cleanup state.json + .bak"
    - "playwright.config.ts register globalTeardown via require.resolve"
    - "Layer 4 post-RESTORE row count = 0 (T-315-03 cleanup leak prevention)"
  artifacts:
    - path: "tests/sql/assessment-matrix-seed.sql"
      provides: "Hierarchical seed Phase 315 dengan IDENTITY_INSERT deterministic"
      contains: "SET IDENTITY_INSERT AssessmentSessions ON"
    - path: "tests/e2e/global.setup.ts"
      provides: "Setup pipeline: app check → BACKUP → execScript → Layer 1 → state.json + journal"
      contains: "db.backup"
    - path: "tests/e2e/global.teardown.ts"
      provides: "Teardown pipeline: flush → RESTORE → Layer 4 → journal close + cleanup"
      contains: "collector.flush"
    - path: "tests/playwright.config.ts"
      provides: "globalTeardown registered"
      contains: "globalTeardown: require.resolve"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Journal entry Phase 315 (initially active, cleaned post-teardown via regex replace)"
      contains: "MATRIX_TEST_2026_05_11"
  key_links:
    - from: "tests/e2e/global.setup.ts"
      to: "tests/.matrix-state.json (runtime artifact)"
      via: "writeFile('tests/.matrix-state.json', JSON.stringify({snapshotPath, scenarios}))"
      pattern: "writeFile.*matrix-state\\.json"
    - from: "tests/e2e/global.teardown.ts"
      to: "tests/.matrix-state.json"
      via: "readFile + parse snapshotPath untuk RESTORE"
      pattern: "readFile.*matrix-state\\.json"
    - from: "tests/e2e/global.teardown.ts"
      to: "tests/helpers/dbSnapshot.ts + tests/e2e/helpers/matrixReport.ts"
      via: "import * as db from '../helpers/dbSnapshot' + import {collector} from './helpers/matrixReport'"
      pattern: "import.*dbSnapshot|matrixReport"
---

<objective>
Bangun seed SQL + lifecycle integration (globalSetup extend + globalTeardown new + playwright.config register) supaya DB lokal di-snapshot/seeded/cleaned otomatis saat full run.

Purpose: Plan 04 (spec utama) butuh state.json sudah ditulis + DB ter-seeded sebelum test() blocks jalan. Plan 04 juga butuh teardown lengkap supaya Layer 4 cleanup terjamin (no leakage across runs).

Output: 4 file core lifecycle + 1 journal append + 1 config edit, semua compile + idempotent + Bahasa Indonesia (Pattern A).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/315-assessment-matrix-test/315-CONTEXT.md
@.planning/phases/315-assessment-matrix-test/315-RESEARCH.md
@.planning/phases/315-assessment-matrix-test/315-PATTERNS.md
@.planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md
@docs/superpowers/specs/2026-05-11-assessment-matrix-test-design.md
@docs/SEED_WORKFLOW.md
@docs/SEED_JOURNAL.md
@CLAUDE.md
@tests/e2e/global.setup.ts
@tests/playwright.config.ts
@tests/helpers/dbSnapshot.ts
@tests/e2e/helpers/matrixReport.ts
@tests/e2e/helpers/matrixTypes.ts

<interfaces>
<!-- Plan 02 outputs to consume -->

From tests/helpers/dbSnapshot.ts:
```typescript
export async function backup(snapshotPath: string): Promise<void>;
export async function restore(snapshotPath: string): Promise<void>;
export async function execScript(sqlPath: string): Promise<void>;
export async function queryScalar(sql: string): Promise<number>;
```

From tests/e2e/helpers/matrixReport.ts:
```typescript
export const collector: { flush(outPath: string): Promise<void>; count(): number };
```

From tests/e2e/helpers/matrixTypes.ts:
```typescript
export type ScenarioConfig = { /* sessionIdPeserta1, sessionIdPeserta2, hasEssay, questions[] */ }
```

<!-- Plan 01 outputs to consume -->

From .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md:
- ## Final Seed Dimensions tabel — numeric counts untuk AssessmentSessions, AssessmentPackages, PackageQuestions, PackageOptions, UserPackageAssignments (per scenario)
- ## A6 verdict — auto-create-lazy vs must-preseed untuk UserPackageAssignment

<!-- Existing files to extend -->

tests/e2e/global.setup.ts (PRESERVE existing 7 lines):
```typescript
import { test as setup, expect } from '@playwright/test';

setup('verify app is running', async ({ page }) => {
  const response = await page.goto('/Account/Login');
  expect(response?.ok()).toBeTruthy();
  await expect(page.locator('button[type="submit"]')).toBeVisible();
});
```

tests/playwright.config.ts (existing config — only add globalTeardown):
```typescript
export default defineConfig({
  testDir: './e2e',
  // ADD: globalTeardown: require.resolve('./e2e/global.teardown.ts'),
  timeout: 60_000,
  // ... rest unchanged
});
```

docs/SEED_JOURNAL.md format (existing table schema):
```
| Tanggal | Phase | Klasifikasi | Tujuan | Dampak (entitas tersentuh) | Snapshot file | Status |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/sql/assessment-matrix-seed.sql (hierarchical seed with IDENTITY_INSERT + Title prefix marker)</name>
  <files>
    tests/sql/assessment-matrix-seed.sql (NEW)
  </files>
  <read_first>
    - .planning/phases/315-assessment-matrix-test/315-INVESTIGATION.md (Final Seed Dimensions — pakai numeric counts dari sini, JANGAN improvise)
    - .planning/seeds/313-timer-fixtures.sql (full file — analog seed pattern; copy header, SET NOCOUNT/XACT_ABORT, user resolution + THROW guard, 6-step FK cleanup, BEGIN TRAN + COMMIT, final SELECT verification)
    - .planning/phases/315-assessment-matrix-test/315-PATTERNS.md § Pattern C (Anti-Pattern Phase 309 UserId NOT NULL FK guard), § Pattern D (6-step FK cleanup), § Pattern E (BEGIN TRAN + COMMIT envelope), § Pattern F (Title prefix marker)
    - .planning/phases/315-assessment-matrix-test/315-RESEARCH.md § Pattern 4 lines 575-647 (IDENTITY_INSERT pattern + 18 sibling sessions revision), § Common Pitfalls Pitfall 3 + Pitfall 4
    - docs/superpowers/specs/2026-05-11-assessment-matrix-test-design.md (commit 94bacecf — § Schema seed / § Scenario config: 7 discovery + 3 sentinel, tipe questions per scenario)
    - Models/AssessmentSession.cs (verify required NOT NULL fields: Title, Category, UserId, AccessToken, BannerColor, Status, Schedule, DurationMinutes, ElapsedSeconds, GenerateCertificate, HasManualGrading, IsManualEntry, IsTokenRequired, PassPercentage, Progress, SamePackage, AllowAnswerReview, CreatedAt)
  </read_first>
  <action>
Buat `tests/sql/assessment-matrix-seed.sql` (NEW) yang mengikuti pola Phase 313.1 dengan modifikasi Phase 315.

**Struktur file (urutan WAJIB sama dengan 313-timer-fixtures.sql lines 1-311):**

1. **Header docblock** (Pattern: copy lines 1-42 dari 313-timer-fixtures.sql, sesuaikan):

```sql
-- ============================================================
-- Phase 315 Assessment Matrix Test Seed
-- REQ: QA-01
-- ============================================================
--
-- Tujuan: Seed N AssessmentSession (sibling pairs per scenario) + packages + questions + options
--   untuk matrix discovery test (7 skenario + 3 sentinel meta-validation).
--   2 peserta (coachee + coachee2) → 2 sibling sessions per scenario.
--
-- Cara run (DB lokal saja - JANGAN jalankan di Dev/Prod per CLAUDE.md § Develop Workflow):
--   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -b -i tests\sql\assessment-matrix-seed.sql
--
-- Idempotent: WIPE-AND-INSERT - re-run aman, akan delete fixture lama dengan Title prefix [MATRIX_TEST_2026_05_11].
-- Pre-condition: Users dengan Email = 'rino.prasetyo@pertamina.com' DAN 'iwan3@pertamina.com' ada di Users
-- Marker strategy: Title prefix `[MATRIX_TEST_2026_05_11]` (D-05 fallback — Notes field tidak ada per Wave 0 Q3)
-- Anti-pattern Phase 309 (UserId=NULL FK violation): subquery + THROW guard
-- IDENTITY_INSERT pattern: deterministic PK 9001+ supaya .matrix-state.json config match DB (Pitfall 4 mitigation)
-- ============================================================

SET NOCOUNT ON;
SET XACT_ABORT ON;
```

2. **User resolution + THROW guard (2 users — coachee + coachee2):**

```sql
DECLARE @CoacheeId NVARCHAR(450) = (SELECT TOP 1 Id FROM Users WHERE Email = 'rino.prasetyo@pertamina.com');
DECLARE @Coachee2Id NVARCHAR(450) = (SELECT TOP 1 Id FROM Users WHERE Email = 'iwan3@pertamina.com');

IF @CoacheeId IS NULL OR @Coachee2Id IS NULL
BEGIN
    THROW 50001, 'Fixture user (rino.prasetyo@pertamina.com atau iwan3@pertamina.com) tidak ditemukan di Users — abort. Seed UserManager dulu via dotnet ef seed.', 1;
END;

PRINT N'[Phase 315 seed] CoacheeId resolved: ' + @CoacheeId;
PRINT N'[Phase 315 seed] Coachee2Id resolved: ' + @Coachee2Id;
```

3. **Pre-check ID collision (defensive — RESEARCH Q4 mitigation):**

```sql
DECLARE @CollisionCount INT = (
    SELECT COUNT(*) FROM AssessmentSessions
    WHERE Id BETWEEN 9001 AND 9018
);
IF @CollisionCount > 0
BEGIN
    THROW 50002, 'ID range 9001-9018 sudah dipakai di AssessmentSessions — abort. Inspect: SELECT Id, Title FROM AssessmentSessions WHERE Id BETWEEN 9001 AND 9018', 1;
END;
```

4. **6-step FK-respecting cleanup chain (idempotent — copy Pattern D dari 313 dengan filter Title prefix [MATRIX_TEST_2026_05_11]):**

```sql
-- 4.1 PackageUserResponses
DELETE pur FROM PackageUserResponses pur
INNER JOIN AssessmentSessions s ON pur.AssessmentSessionId = s.Id
WHERE s.Title LIKE '[MATRIX_TEST_2026_05_11]%';

-- 4.2 UserPackageAssignments
DELETE upa FROM UserPackageAssignments upa
INNER JOIN AssessmentSessions s ON upa.AssessmentSessionId = s.Id
WHERE s.Title LIKE '[MATRIX_TEST_2026_05_11]%';

-- 4.3 PackageOptions (JOIN via PackageQuestions → AssessmentPackages → AssessmentSessions)
DELETE po FROM PackageOptions po
INNER JOIN PackageQuestions pq ON po.PackageQuestionId = pq.Id
INNER JOIN AssessmentPackages ap ON pq.AssessmentPackageId = ap.Id
INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId = s.Id
WHERE s.Title LIKE '[MATRIX_TEST_2026_05_11]%';

-- 4.4 PackageQuestions
DELETE pq FROM PackageQuestions pq
INNER JOIN AssessmentPackages ap ON pq.AssessmentPackageId = ap.Id
INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId = s.Id
WHERE s.Title LIKE '[MATRIX_TEST_2026_05_11]%';

-- 4.5 AssessmentPackages
DELETE ap FROM AssessmentPackages ap
INNER JOIN AssessmentSessions s ON ap.AssessmentSessionId = s.Id
WHERE s.Title LIKE '[MATRIX_TEST_2026_05_11]%';

-- 4.6 AssessmentSessions
DELETE FROM AssessmentSessions WHERE Title LIKE '[MATRIX_TEST_2026_05_11]%';
```

5. **BEGIN TRAN + IDENTITY_INSERT block per table (per Pitfall 4) — INSERT 18 sibling sessions (9 scenarios × 2 peserta) atau N sesuai INVESTIGATION verdict:**

```sql
BEGIN TRAN;

DECLARE @Now DATETIME2 = SYSUTCDATETIME();
DECLARE @ScheduleDate DATETIME2 = '2026-05-11T00:00:00';

-- 5.1 AssessmentSessions — sibling pairs (coachee + coachee2 per scenario)
SET IDENTITY_INSERT AssessmentSessions ON;
INSERT INTO AssessmentSessions
  (Id, Title, Category, UserId, AccessToken, BannerColor, Status, Schedule, DurationMinutes,
   ElapsedSeconds, GenerateCertificate, HasManualGrading, IsManualEntry, IsTokenRequired,
   PassPercentage, Progress, SamePackage, AllowAnswerReview, CreatedAt, AssessmentType)
VALUES
  -- Scenario 1: Matrix Manual Mixed (MC + MA + Essay)
  (9001, '[MATRIX_TEST_2026_05_11] S1 Manual Mixed', 'Matrix Test Category', @CoacheeId,
   'MTX-S1-P1', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 1, 0, 75, 0, 1, 1, @Now, 'Manual'),
  (9002, '[MATRIX_TEST_2026_05_11] S1 Manual Mixed', 'Matrix Test Category', @Coachee2Id,
   'MTX-S1-P2', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 1, 0, 75, 0, 1, 1, @Now, 'Manual'),
  -- Scenario 2: Matrix Online Mixed
  (9003, '[MATRIX_TEST_2026_05_11] S2 Online Mixed', 'Matrix Test Category', @CoacheeId,
   'MTX-S2-P1', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  (9004, '[MATRIX_TEST_2026_05_11] S2 Online Mixed', 'Matrix Test Category', @Coachee2Id,
   'MTX-S2-P2', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  -- Scenario 3: Matrix PreTest Mixed
  (9005, '[MATRIX_TEST_2026_05_11] S3 PreTest Mixed', 'Matrix Test Category', @CoacheeId,
   'MTX-S3-P1', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'PreTest'),
  (9006, '[MATRIX_TEST_2026_05_11] S3 PreTest Mixed', 'Matrix Test Category', @Coachee2Id,
   'MTX-S3-P2', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'PreTest'),
  -- Scenario 4: Matrix PostTest Mixed
  (9007, '[MATRIX_TEST_2026_05_11] S4 PostTest Mixed', 'Matrix Test Category', @CoacheeId,
   'MTX-S4-P1', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'PostTest'),
  (9008, '[MATRIX_TEST_2026_05_11] S4 PostTest Mixed', 'Matrix Test Category', @Coachee2Id,
   'MTX-S4-P2', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'PostTest'),
  -- Scenario 5: Matrix Online MC only
  (9009, '[MATRIX_TEST_2026_05_11] S5 Online MC only', 'Matrix Test Category', @CoacheeId,
   'MTX-S5-P1', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  (9010, '[MATRIX_TEST_2026_05_11] S5 Online MC only', 'Matrix Test Category', @Coachee2Id,
   'MTX-S5-P2', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  -- Scenario 6: Matrix Online MA only
  (9011, '[MATRIX_TEST_2026_05_11] S6 Online MA only', 'Matrix Test Category', @CoacheeId,
   'MTX-S6-P1', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  (9012, '[MATRIX_TEST_2026_05_11] S6 Online MA only', 'Matrix Test Category', @Coachee2Id,
   'MTX-S6-P2', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  -- Scenario 7: Matrix Online Essay only
  (9013, '[MATRIX_TEST_2026_05_11] S7 Online Essay only', 'Matrix Test Category', @CoacheeId,
   'MTX-S7-P1', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 1, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  (9014, '[MATRIX_TEST_2026_05_11] S7 Online Essay only', 'Matrix Test Category', @Coachee2Id,
   'MTX-S7-P2', '#1e88e5', 'NotStarted', @ScheduleDate, 60, 0, 1, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  -- Sentinel 8: [META-AllCorrect]
  (9015, '[MATRIX_TEST_2026_05_11] [META-AllCorrect] Sentinel', 'Matrix Test Category', @CoacheeId,
   'MTX-S8-P1', '#9e9e9e', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  (9016, '[MATRIX_TEST_2026_05_11] [META-AllCorrect] Sentinel', 'Matrix Test Category', @Coachee2Id,
   'MTX-S8-P2', '#9e9e9e', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  -- Sentinel 9: [META-AllWrong] (1 user cukup untuk sentinel — tetap 2 demi konsistensi sibling pattern)
  (9017, '[MATRIX_TEST_2026_05_11] [META-AllWrong] Sentinel', 'Matrix Test Category', @CoacheeId,
   'MTX-S9-P1', '#9e9e9e', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online'),
  -- Sentinel 10: [META-CollectorCheck] (1 user — test.fail() block)
  (9018, '[MATRIX_TEST_2026_05_11] [META-CollectorCheck] Sentinel', 'Matrix Test Category', @CoacheeId,
   'MTX-S10-P1', '#9e9e9e', 'NotStarted', @ScheduleDate, 60, 0, 0, 1, 0, 0, 75, 0, 1, 1, @Now, 'Online');
SET IDENTITY_INSERT AssessmentSessions OFF;

-- 5.2 AssessmentPackages — 1 package per session (per A1 verdict; adjust jika shared)
SET IDENTITY_INSERT AssessmentPackages ON;
INSERT INTO AssessmentPackages (Id, AssessmentSessionId, PackageName, PackageNumber, CreatedAt)
VALUES
  (9001, 9001, N'Phase 315 Matrix Package', 1, @Now),
  (9002, 9002, N'Phase 315 Matrix Package', 1, @Now),
  (9003, 9003, N'Phase 315 Matrix Package', 1, @Now),
  (9004, 9004, N'Phase 315 Matrix Package', 1, @Now),
  (9005, 9005, N'Phase 315 Matrix Package', 1, @Now),
  (9006, 9006, N'Phase 315 Matrix Package', 1, @Now),
  (9007, 9007, N'Phase 315 Matrix Package', 1, @Now),
  (9008, 9008, N'Phase 315 Matrix Package', 1, @Now),
  (9009, 9009, N'Phase 315 Matrix Package', 1, @Now),
  (9010, 9010, N'Phase 315 Matrix Package', 1, @Now),
  (9011, 9011, N'Phase 315 Matrix Package', 1, @Now),
  (9012, 9012, N'Phase 315 Matrix Package', 1, @Now),
  (9013, 9013, N'Phase 315 Matrix Package', 1, @Now),
  (9014, 9014, N'Phase 315 Matrix Package', 1, @Now),
  (9015, 9015, N'Phase 315 Matrix Package', 1, @Now),
  (9016, 9016, N'Phase 315 Matrix Package', 1, @Now),
  (9017, 9017, N'Phase 315 Matrix Package', 1, @Now),
  (9018, 9018, N'Phase 315 Matrix Package', 1, @Now);
SET IDENTITY_INSERT AssessmentPackages OFF;

-- 5.3 PackageQuestions — per scenario tipe questions berbeda
-- Scenario mixed (S1-S4): MC + MA + Essay (3 questions per session, 1 type each)
-- Scenario single (S5: MC, S6: MA, S7: Essay): 3 questions of same type
-- Sentinel S8-S10: 3 questions mixed (untuk variasi sentinel scenario)
-- Total: 18 packages × 3 questions = 54 PackageQuestions (range 50001-50054)
SET IDENTITY_INSERT PackageQuestions ON;
INSERT INTO PackageQuestions (Id, AssessmentPackageId, QuestionText, QuestionType, ScoreValue, OrderNumber, CreatedAt)
VALUES
  -- S1 Manual Mixed package 9001 (coachee) + 9002 (coachee2): MC + MA + Essay
  (50001, 9001, N'S1 MC: Pilih jawaban benar', 'MultipleChoice', 30, 1, @Now),
  (50002, 9001, N'S1 MA: Pilih ≥2 jawaban benar', 'MultipleAnswer', 35, 2, @Now),
  (50003, 9001, N'S1 Essay: Jelaskan konsep singkat', 'Essay', 35, 3, @Now),
  (50004, 9002, N'S1 MC: Pilih jawaban benar', 'MultipleChoice', 30, 1, @Now),
  (50005, 9002, N'S1 MA: Pilih ≥2 jawaban benar', 'MultipleAnswer', 35, 2, @Now),
  (50006, 9002, N'S1 Essay: Jelaskan konsep singkat', 'Essay', 35, 3, @Now),
  -- S2 Online Mixed: MC + MA + Essay (sibling 9003, 9004)
  (50007, 9003, N'S2 MC', 'MultipleChoice', 30, 1, @Now),
  (50008, 9003, N'S2 MA', 'MultipleAnswer', 35, 2, @Now),
  (50009, 9003, N'S2 Essay', 'Essay', 35, 3, @Now),
  (50010, 9004, N'S2 MC', 'MultipleChoice', 30, 1, @Now),
  (50011, 9004, N'S2 MA', 'MultipleAnswer', 35, 2, @Now),
  (50012, 9004, N'S2 Essay', 'Essay', 35, 3, @Now),
  -- S3 PreTest Mixed (9005, 9006)
  (50013, 9005, N'S3 MC', 'MultipleChoice', 30, 1, @Now),
  (50014, 9005, N'S3 MA', 'MultipleAnswer', 35, 2, @Now),
  (50015, 9005, N'S3 Essay', 'Essay', 35, 3, @Now),
  (50016, 9006, N'S3 MC', 'MultipleChoice', 30, 1, @Now),
  (50017, 9006, N'S3 MA', 'MultipleAnswer', 35, 2, @Now),
  (50018, 9006, N'S3 Essay', 'Essay', 35, 3, @Now),
  -- S4 PostTest Mixed (9007, 9008)
  (50019, 9007, N'S4 MC', 'MultipleChoice', 30, 1, @Now),
  (50020, 9007, N'S4 MA', 'MultipleAnswer', 35, 2, @Now),
  (50021, 9007, N'S4 Essay', 'Essay', 35, 3, @Now),
  (50022, 9008, N'S4 MC', 'MultipleChoice', 30, 1, @Now),
  (50023, 9008, N'S4 MA', 'MultipleAnswer', 35, 2, @Now),
  (50024, 9008, N'S4 Essay', 'Essay', 35, 3, @Now),
  -- S5 Online MC only (9009, 9010): 3 MC questions
  (50025, 9009, N'S5 MC #1', 'MultipleChoice', 34, 1, @Now),
  (50026, 9009, N'S5 MC #2', 'MultipleChoice', 33, 2, @Now),
  (50027, 9009, N'S5 MC #3', 'MultipleChoice', 33, 3, @Now),
  (50028, 9010, N'S5 MC #1', 'MultipleChoice', 34, 1, @Now),
  (50029, 9010, N'S5 MC #2', 'MultipleChoice', 33, 2, @Now),
  (50030, 9010, N'S5 MC #3', 'MultipleChoice', 33, 3, @Now),
  -- S6 Online MA only (9011, 9012)
  (50031, 9011, N'S6 MA #1', 'MultipleAnswer', 34, 1, @Now),
  (50032, 9011, N'S6 MA #2', 'MultipleAnswer', 33, 2, @Now),
  (50033, 9011, N'S6 MA #3', 'MultipleAnswer', 33, 3, @Now),
  (50034, 9012, N'S6 MA #1', 'MultipleAnswer', 34, 1, @Now),
  (50035, 9012, N'S6 MA #2', 'MultipleAnswer', 33, 2, @Now),
  (50036, 9012, N'S6 MA #3', 'MultipleAnswer', 33, 3, @Now),
  -- S7 Online Essay only (9013, 9014)
  (50037, 9013, N'S7 Essay #1', 'Essay', 34, 1, @Now),
  (50038, 9013, N'S7 Essay #2', 'Essay', 33, 2, @Now),
  (50039, 9013, N'S7 Essay #3', 'Essay', 33, 3, @Now),
  (50040, 9014, N'S7 Essay #1', 'Essay', 34, 1, @Now),
  (50041, 9014, N'S7 Essay #2', 'Essay', 33, 2, @Now),
  (50042, 9014, N'S7 Essay #3', 'Essay', 33, 3, @Now),
  -- Sentinel S8 META-AllCorrect (9015, 9016): MC + MA + Essay
  (50043, 9015, N'S8 MC sentinel', 'MultipleChoice', 30, 1, @Now),
  (50044, 9015, N'S8 MA sentinel', 'MultipleAnswer', 35, 2, @Now),
  (50045, 9015, N'S8 Essay sentinel', 'Essay', 35, 3, @Now),
  (50046, 9016, N'S8 MC sentinel', 'MultipleChoice', 30, 1, @Now),
  (50047, 9016, N'S8 MA sentinel', 'MultipleAnswer', 35, 2, @Now),
  (50048, 9016, N'S8 Essay sentinel', 'Essay', 35, 3, @Now),
  -- Sentinel S9 META-AllWrong (9017)
  (50049, 9017, N'S9 MC sentinel', 'MultipleChoice', 30, 1, @Now),
  (50050, 9017, N'S9 MA sentinel', 'MultipleAnswer', 35, 2, @Now),
  (50051, 9017, N'S9 Essay sentinel', 'Essay', 35, 3, @Now),
  -- Sentinel S10 META-CollectorCheck (9018)
  (50052, 9018, N'S10 MC sentinel', 'MultipleChoice', 30, 1, @Now),
  (50053, 9018, N'S10 MA sentinel', 'MultipleAnswer', 35, 2, @Now),
  (50054, 9018, N'S10 Essay sentinel', 'Essay', 35, 3, @Now);
SET IDENTITY_INSERT PackageQuestions OFF;

-- 5.4 PackageOptions — 4 options per question (3 wrong + 1 correct for MC, 4 with 2 correct for MA, none for Essay typically)
-- Untuk simplicity: 4 options per MC question, 4 options per MA question (2 correct + 2 wrong), 0 for Essay
-- MC: 36 questions (S1 MC + S2 MC + S3 MC + S4 MC × 2 peserta = 8, S5 MC × 3 × 2 = 6, S8-S10 MC × ~3 = 5 → recount)
-- Actual MC count: S1-S4 mixed (1 per scenario × 2 = 8) + S5 (3 × 2 = 6) + Sentinel S8-S10 (3) = 17 MC questions
-- Actual MA count: S1-S4 mixed (1 × 2 = 8) + S6 (3 × 2 = 6) + Sentinel S8-S10 (3) = 17 MA questions
-- Actual Essay count: S1-S4 mixed (1 × 2 = 8) + S7 (3 × 2 = 6) + Sentinel S8-S10 (3) = 17 Essay questions
-- Total questions: 17 + 17 + 17 = 51 (BUT NOTE PackageQuestion seed di atas adalah 54 — recount lagi atau accept extra essay)
-- Option range: 17 MC × 4 + 17 MA × 4 = 136 options
-- Adjust range PackageOptions Id: 80001-80136

SET IDENTITY_INSERT PackageOptions ON;
-- Helper: untuk MC questions, 4 options (option 1 = correct), e.g. q=50001 (MC) → optIds 80001-80004 (80001 correct)
-- Untuk MA questions, 4 options (options 1+2 = correct), e.g. q=50002 (MA) → optIds 80005-80008 (80005+80006 correct)
-- Untuk Essay questions, 0 options (akan di-skip — Essay tidak butuh options)

-- Pattern: per MC question 4 options dengan IsCorrect flag; per MA 2 correct + 2 wrong
-- Compose via INSERT VALUES manually (verbose but predictable + reviewable):

INSERT INTO PackageOptions (Id, PackageQuestionId, OptionText, IsCorrect, OrderNumber, CreatedAt)
VALUES
  -- 50001 MC (correct = first option)
  (80001, 50001, N'Jawaban A (benar)', 1, 1, @Now),
  (80002, 50001, N'Jawaban B', 0, 2, @Now),
  (80003, 50001, N'Jawaban C', 0, 3, @Now),
  (80004, 50001, N'Jawaban D', 0, 4, @Now),
  -- 50002 MA (correct = first 2 options)
  (80005, 50002, N'Pilihan 1 (benar)', 1, 1, @Now),
  (80006, 50002, N'Pilihan 2 (benar)', 1, 2, @Now),
  (80007, 50002, N'Pilihan 3', 0, 3, @Now),
  (80008, 50002, N'Pilihan 4', 0, 4, @Now);
  -- Repeat pattern untuk semua MC dan MA questions (Essay tidak butuh options)
  -- Total INSERT akan sekitar 136 rows untuk MC+MA, atau lebih sesuai cardinality akhir.
SET IDENTITY_INSERT PackageOptions OFF;

-- ============================================================
-- DETERMINISTIC OPTION ID FORMULA (single source of truth):
--   optId = 80001 + (qId - 50001) * 4 + optIndex
--   where qId ∈ [50001..50054], optIndex ∈ [0..3]
-- Examples:
--   qId=50001 (MC) → opts [80001, 80002, 80003, 80004]; correct = 80001 (optIndex=0)
--   qId=50002 (MA) → opts [80005, 80006, 80007, 80008]; correct = 80005+80006 (optIndex=0,1)
--   qId=50003 (Essay) → NO options (Essay tidak butuh PackageOptions)
--   qId=50054 (last MC sentinel) → opts [80213, 80214, 80215, 80216]
-- This formula MUST be replicated in tests/e2e/global.setup.ts buildScenarios()
-- (per Plan 02 Task 1 + Plan 03 Task 2) supaya correctOptionIds[] derivation
-- deterministic, NOT hand-typed. Single source of truth → seed SQL dan helper consume
-- formula yang sama, mismatch jadi mustahil.
-- ============================================================
-- Plan 03 Task 1 executor: Generate sisa PackageOptions INSERT secara mekanis menggunakan
-- formula di atas. Untuk SETIAP PackageQuestion dengan QuestionType IN ('MultipleChoice',
-- 'MultipleAnswer'), insert 4 PackageOptions dengan Id sesuai formula. MC: IsCorrect=1
-- pada optIndex=0. MA: IsCorrect=1 pada optIndex=0+1. Essay: skip (tidak insert apa-apa).
-- Mekanis = no improvisasi; jika ragu, hitung manual dengan formula dan verifikasi range.

COMMIT;

-- 6. Final verification SELECT (Layer 1 source — dipakai global.setup.ts queryScalar)
SELECT s.Id, s.Title, s.UserId, s.AssessmentType,
    (SELECT COUNT(*) FROM AssessmentPackages WHERE AssessmentSessionId = s.Id) AS PkgCount,
    (SELECT COUNT(*) FROM PackageQuestions pq INNER JOIN AssessmentPackages ap
        ON pq.AssessmentPackageId = ap.Id WHERE ap.AssessmentSessionId = s.Id) AS QCount,
    (SELECT COUNT(*) FROM PackageOptions po INNER JOIN PackageQuestions pq
        ON po.PackageQuestionId = pq.Id INNER JOIN AssessmentPackages ap
        ON pq.AssessmentPackageId = ap.Id WHERE ap.AssessmentSessionId = s.Id) AS OptCount
FROM AssessmentSessions s
WHERE s.Title LIKE '[MATRIX_TEST_2026_05_11]%'
ORDER BY s.Id;
-- Expected: 18 rows; PkgCount=1 per session; QCount=3 per session; OptCount per session varies by question type mix
```

**Executor WAJIB:**
1. Adjust seed cardinality berdasarkan `315-INVESTIGATION.md` § Final Seed Dimensions — kalau A1 verdict = SHARED-ACROSS-SIBLINGS, packages bisa 9 (bukan 18) dan AssessmentSessions tetap 18, tapi pivot ke link via UserPackageAssignments dengan A6 = MUST-PRESEED.
2. Generate sisa `PackageOptions` INSERT secara mekanis untuk SEMUA MC + MA questions (skip Essay) — comment "NOTE Plan 03 Task 1 executor" di atas WAJIB di-resolve menjadi full INSERT block sebelum file dianggap selesai.
3. Snapshot DB lokal sebelum testing seed: `sqlcmd -S "localhost\SQLEXPRESS" -E -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev.pre-315.bak' WITH INIT"` (per CLAUDE.md + SEED_WORKFLOW Step 2).
4. Test seed: `sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -b -i tests\sql\assessment-matrix-seed.sql` → expect final SELECT shows 18 rows.
5. Restore DB lokal setelah test seed: pakai BACKUP file di Step 3.
6. Update INVESTIGATION.md final seed dimension counts jika berbeda dari plan ini.
  </action>
  <verify>
    <automated>test -f tests/sql/assessment-matrix-seed.sql &amp;&amp; grep -c "SET IDENTITY_INSERT" tests/sql/assessment-matrix-seed.sql | awk '{exit ($1 &gt;= 6) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/sql/assessment-matrix-seed.sql` exists
    - `grep -c "SET NOCOUNT ON" tests/sql/assessment-matrix-seed.sql` returns 1
    - `grep -c "SET XACT_ABORT ON" tests/sql/assessment-matrix-seed.sql` returns 1
    - `grep -c "rino.prasetyo@pertamina.com" tests/sql/assessment-matrix-seed.sql` returns 1
    - `grep -c "iwan3@pertamina.com" tests/sql/assessment-matrix-seed.sql` returns 1
    - `grep -cE "THROW 5000[12]" tests/sql/assessment-matrix-seed.sql` returns ≥ 2 (user resolution guard + collision guard)
    - `grep -c "\\[MATRIX_TEST_2026_05_11\\]" tests/sql/assessment-matrix-seed.sql` returns ≥ 10 (Title prefix marker per scenario)
    - `grep -c "SET IDENTITY_INSERT" tests/sql/assessment-matrix-seed.sql` returns ≥ 8 (4 ON + 4 OFF for 4 tables)
    - `grep -c "BEGIN TRAN" tests/sql/assessment-matrix-seed.sql` returns 1
    - `grep -c "^COMMIT;$" tests/sql/assessment-matrix-seed.sql` returns 1
    - `grep -cE "DELETE (pur|upa|po|pq|ap) FROM" tests/sql/assessment-matrix-seed.sql` returns ≥ 5 (6-step cleanup chain)
    - Final SELECT block present: `grep -c "SELECT s.Id, s.Title" tests/sql/assessment-matrix-seed.sql` returns ≥ 1
    - No `NOTE Plan 03 Task 1 executor` placeholder remains: `grep -c "NOTE Plan 03 Task 1 executor" tests/sql/assessment-matrix-seed.sql` returns 0
    - Deterministic ID formula documented in seed SQL header (single source of truth — must match buildScenarios() di Plan 03 Task 2): `grep -E "optId = 80001 \+ \(qId - 50001\) \* 4 \+ optIndex" tests/sql/assessment-matrix-seed.sql` returns ≥ 1
    - Manual smoke test runtime (executor verification): `sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -b -i tests/sql/assessment-matrix-seed.sql` exit 0 + final SELECT returns 18 rows (after pre-test BACKUP snapshot, then RESTORE post-verify)
  </acceptance_criteria>
  <done>
    Seed SQL file complete (header, user resolution, collision check, 6-step cleanup, BEGIN TRAN, 4 IDENTITY_INSERT blocks, final SELECT). Manual smoke run exit 0 + 18 sessions seeded. DB lokal di-RESTORE setelah verification (no dirty state).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend global.setup.ts + Create global.teardown.ts + Register di playwright.config + Append SEED_JOURNAL</name>
  <files>
    tests/e2e/global.setup.ts (EDIT — extend existing)
    tests/e2e/global.teardown.ts (NEW)
    tests/playwright.config.ts (EDIT — register globalTeardown)
    docs/SEED_JOURNAL.md (EDIT — append entry — runtime, tapi initial template line di-prep di setup)
  </files>
  <read_first>
    - tests/e2e/global.setup.ts (existing 7 lines — PRESERVE assertion)
    - tests/playwright.config.ts (existing — verify fullyParallel:false, screenshot:'on')
    - .planning/phases/315-assessment-matrix-test/315-RESEARCH.md § Code Example 2 (lines 858-919 — full extended global.setup.ts) + § Code Example 3 (lines 922-980 — full global.teardown.ts)
    - .planning/phases/315-assessment-matrix-test/315-PATTERNS.md § Pattern G (Seed Journal Append + Cleanup Update — regex pattern)
    - .planning/phases/315-assessment-matrix-test/315-RESEARCH.md § Pattern 1 (lines 367-405 — globalTeardown registration)
    - docs/SEED_JOURNAL.md (existing — verify format: 7-column markdown table)
    - tests/helpers/dbSnapshot.ts (Plan 02 output — confirm `backup, restore, execScript, queryScalar` exports)
    - tests/e2e/helpers/matrixReport.ts (Plan 02 output — confirm `collector.flush` signature)
  </read_first>
  <action>
**FILE 1 — `tests/e2e/global.setup.ts` (EDIT):**

PRESERVE existing 7 lines (sampai `await expect(page.locator('button[type="submit"]')).toBeVisible();`). Tambah extension setelah baris terakhir.

```typescript
import { test as setup, expect } from '@playwright/test';
import * as db from '../helpers/dbSnapshot';
import { writeFile, appendFile, mkdir } from 'fs/promises';

setup('verify app is running + seed matrix', async ({ page }) => {
  // ===== EXISTING (PRESERVE) =====
  const response = await page.goto('/Account/Login');
  expect(response?.ok()).toBeTruthy();
  await expect(page.locator('button[type="submit"]')).toBeVisible();

  // ===== NEW Phase 315 =====
  // Step 1: snapshot path
  const ts = new Date().toISOString().replace(/[:.]/g, '-');
  // Coba C:/Temp/ dulu (default), fallback ke tests/sql/ kalau tidak writable
  const snapshotPath = `C:/Temp/HcPortalDB_Dev-matrix-${ts}.bak`;

  // Step 2: pre-check ID collision (defense — RESEARCH Q4)
  const collisionCount = await db.queryScalar(
    `SELECT COUNT(*) FROM AssessmentSessions WHERE Id BETWEEN 9001 AND 9018`
  );
  expect(collisionCount, `Pre-check: ID range 9001-9018 sudah ada ${collisionCount} row — abort`).toBe(0);

  // Step 3: BACKUP DB lokal (SEED_WORKFLOW.md §5.1 SOP)
  await db.backup(snapshotPath);
  console.log(`[setup] BACKUP OK: ${snapshotPath}`);

  // Step 4: Execute seed SQL
  await db.execScript('tests/sql/assessment-matrix-seed.sql');
  console.log(`[setup] Seed executed`);

  // Step 5: Layer 1 validation — expect 18 sessions tagged
  const sessionCount = await db.queryScalar(
    `SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[MATRIX_TEST_2026_05_11]%'`
  );
  expect(sessionCount, `Layer 1: expected 18 matrix sessions seeded`).toBe(18);

  // Step 6: Pre-create docs/test-reports/ directory (avoid teardown mkdir race)
  await mkdir('docs/test-reports', { recursive: true });

  // Step 7: Write tests/.matrix-state.json (consumed by spec + teardown)
  // scenarios array: Plan 04 spec utama akan read this; struktur per ScenarioConfig type
  const scenarios = buildScenarios();  // see helper below
  await writeFile('tests/.matrix-state.json', JSON.stringify({
    snapshotPath,
    seededAt: new Date().toISOString(),
    scenarios,
  }, null, 2));
  console.log(`[setup] State file written with ${scenarios.length} scenarios`);

  // Step 8: Append SEED_JOURNAL.md entry (status=active)
  const today = new Date().toISOString().slice(0, 10);
  const journalEntry = `| ${today} | 315 | temporary + local-only | Assessment matrix discovery test sweep — 7 discovery + 3 sentinel | AssessmentSessions(18), Packages(18), Questions(54), Options(~136) | ${snapshotPath} | active |\n`;
  await appendFile('docs/SEED_JOURNAL.md', journalEntry);
  console.log(`[setup] Journal entry appended (status=active)`);
});

// Helper: build 10 ScenarioConfig (7 discovery + 3 sentinel) per Final Seed Dimensions
// DETERMINISTIC FORMULA (single source of truth — mirror dari tests/sql/assessment-matrix-seed.sql header):
//   optId = 80001 + (qId - 50001) * 4 + optIndex
//   where qId ∈ [50001..50054], optIndex ∈ [0..3]
//   MC: correctOptionIds = [opt(0)]      → [80001 + (qId-50001)*4 + 0]
//   MA: correctOptionIds = [opt(0), opt(1)]→ [80001 + (qId-50001)*4 + 0, +1]
//   Essay: correctOptionIds = [], allOptionIds = []  (Essay tidak punya options)
// JANGAN hand-type literal arrays — pakai formula di bawah. Mismatch dengan seed = bug.
function buildScenarios() {
  // Helper formula closures — pakai instead of typing literal arrays
  const optBase = (qId) => 80001 + (qId - 50001) * 4;
  const optAll = (qId) => [optBase(qId), optBase(qId) + 1, optBase(qId) + 2, optBase(qId) + 3];
  const mcQ = (qId, scoreValue) => ({
    id: qId, type: 'MultipleChoice', scoreValue,
    correctOptionIds: [optBase(qId)],            // optIndex 0
    allOptionIds: optAll(qId),
  });
  const maQ = (qId, scoreValue) => ({
    id: qId, type: 'MultipleAnswer', scoreValue,
    correctOptionIds: [optBase(qId), optBase(qId) + 1],  // optIndex 0+1
    allOptionIds: optAll(qId),
  });
  const essayQ = (qId, scoreValue) => ({
    id: qId, type: 'Essay', scoreValue,
    correctOptionIds: [], allOptionIds: [],      // Essay: no options
  });

  return [
    {
      id: 1, title: '[MATRIX_TEST_2026_05_11] S1 Manual Mixed',
      sessionIdPeserta1: 9001, sessionIdPeserta2: 9002,
      type: 'Manual', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [mcQ(50001, 30), maQ(50002, 35), essayQ(50003, 35)],
    },
    // S2 Online Mixed (qIds 50007..50012 sesuai seed; peserta1 questions 50007-50009, peserta2 50010-50012;
    //   spec: scenario config referensi peserta1 questions — peserta2 mirror)
    {
      id: 2, title: '[MATRIX_TEST_2026_05_11] S2 Online Mixed',
      sessionIdPeserta1: 9003, sessionIdPeserta2: 9004,
      type: 'Online', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [mcQ(50007, 30), maQ(50008, 35), essayQ(50009, 35)],
    },
    // S3 PreTest Mixed (qIds 50013..50018)
    {
      id: 3, title: '[MATRIX_TEST_2026_05_11] S3 PreTest Mixed',
      sessionIdPeserta1: 9005, sessionIdPeserta2: 9006,
      type: 'PreTest', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [mcQ(50013, 30), maQ(50014, 35), essayQ(50015, 35)],
    },
    // S4 PostTest Mixed (qIds 50019..50024)
    {
      id: 4, title: '[MATRIX_TEST_2026_05_11] S4 PostTest Mixed',
      sessionIdPeserta1: 9007, sessionIdPeserta2: 9008,
      type: 'PostTest', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [mcQ(50019, 30), maQ(50020, 35), essayQ(50021, 35)],
    },
    // S5 Online MC only (qIds 50025..50030, 3 MC questions per peserta)
    {
      id: 5, title: '[MATRIX_TEST_2026_05_11] S5 Online MC only',
      sessionIdPeserta1: 9009, sessionIdPeserta2: 9010,
      type: 'Online', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: false,
      questions: [mcQ(50025, 34), mcQ(50026, 33), mcQ(50027, 33)],
    },
    // S6 Online MA only (qIds 50031..50036)
    {
      id: 6, title: '[MATRIX_TEST_2026_05_11] S6 Online MA only',
      sessionIdPeserta1: 9011, sessionIdPeserta2: 9012,
      type: 'Online', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: false,
      questions: [maQ(50031, 34), maQ(50032, 33), maQ(50033, 33)],
    },
    // S7 Online Essay only (qIds 50037..50042) — no options seeded
    {
      id: 7, title: '[MATRIX_TEST_2026_05_11] S7 Online Essay only',
      sessionIdPeserta1: 9013, sessionIdPeserta2: 9014,
      type: 'Online', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [essayQ(50037, 34), essayQ(50038, 33), essayQ(50039, 33)],
    },
    // S8 [META-AllCorrect] Sentinel (qIds 50043..50048)
    {
      id: 8, title: '[MATRIX_TEST_2026_05_11] [META-AllCorrect] Sentinel',
      sessionIdPeserta1: 9015, sessionIdPeserta2: 9016,
      type: 'Online', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [mcQ(50043, 30), maQ(50044, 35), essayQ(50045, 35)],
    },
    // S9 [META-AllWrong] Sentinel (qIds 50049..50051)
    {
      id: 9, title: '[MATRIX_TEST_2026_05_11] [META-AllWrong] Sentinel',
      sessionIdPeserta1: 9017, sessionIdPeserta2: 9017,
      type: 'Online', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [mcQ(50049, 30), maQ(50050, 35), essayQ(50051, 35)],
    },
    // S10 [META-CollectorCheck] Sentinel (qIds 50052..50054)
    {
      id: 10, title: '[MATRIX_TEST_2026_05_11] [META-CollectorCheck] Sentinel',
      sessionIdPeserta1: 9018, sessionIdPeserta2: 9018,
      type: 'Online', category: 'Matrix Test Category', scheduleDate: '2026-05-11', hasEssay: true,
      questions: [mcQ(50052, 30), maQ(50053, 35), essayQ(50054, 35)],
    },
  ];
}
```

**FILE 2 — `tests/e2e/global.teardown.ts` (NEW):**

Per RESEARCH § Code Example 3 lines 922-980:

```typescript
import type { FullConfig } from '@playwright/test';
import * as db from '../helpers/dbSnapshot';
import { collector } from './helpers/matrixReport';
import { readFile, unlink, writeFile } from 'fs/promises';

async function globalTeardown(_config: FullConfig): Promise<void> {
  const today = new Date().toISOString().slice(0, 10);
  const reportPath = `docs/test-reports/${today}-assessment-matrix.md`;

  // Step 1: FLUSH report FIRST (preserve findings even if RESTORE fails — Pitfall: flush before restore)
  try {
    await collector.flush(reportPath);
    console.log(`[teardown] Report ditulis: ${reportPath} (${collector.count()} findings)`);
  } catch (e) {
    console.error('[teardown] flush() gagal:', e);
  }

  // Step 2: Read state file untuk dapat snapshotPath
  let state: { snapshotPath: string } | null = null;
  try {
    state = JSON.parse(await readFile('tests/.matrix-state.json', 'utf-8'));
  } catch (e) {
    console.error('[teardown] tests/.matrix-state.json missing — skipping RESTORE');
    throw new Error('Teardown abort: state file missing');
  }

  // Step 3: RESTORE DB
  try {
    await db.restore(state!.snapshotPath);
    console.log(`[teardown] RESTORE OK dari ${state!.snapshotPath}`);
  } catch (e) {
    console.error('[teardown] RESTORE GAGAL — manual restore:');
    console.error(`  sqlcmd -S "localhost\\SQLEXPRESS" -E -Q "USE master; ALTER DATABASE HcPortalDB_Dev SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE HcPortalDB_Dev FROM DISK='${state!.snapshotPath}' WITH REPLACE; ALTER DATABASE HcPortalDB_Dev SET MULTI_USER;"`);
    throw e;
  }

  // Step 4: Layer 4 validation — expect 0 matrix rows post-RESTORE
  const remainingRows = await db.queryScalar(
    `SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[MATRIX_TEST_2026_05_11]%'`
  );
  if (remainingRows !== 0) {
    console.error(`[teardown] Layer 4 FAIL: ${remainingRows} matrix rows remain post-RESTORE`);
    throw new Error(`Layer 4 cleanup validation failed (${remainingRows} rows remain)`);
  }
  console.log(`[teardown] Layer 4 OK: 0 matrix rows post-RESTORE`);

  // Step 5: Update SEED_JOURNAL.md entry dari 'active' → 'cleaned' (Pattern G regex replace)
  const journalText = await readFile('docs/SEED_JOURNAL.md', 'utf-8');
  const updatedJournal = journalText.replace(
    /(\|\s*\S*matrix[^|]*\.bak\s*\|\s*)active(\s*\|)/,
    '$1cleaned$2'
  );
  await writeFile('docs/SEED_JOURNAL.md', updatedJournal);
  console.log(`[teardown] Journal entry updated → cleaned`);

  // Step 6: Cleanup state file + .bak snapshot (best-effort)
  await unlink('tests/.matrix-state.json').catch(() => {});
  await unlink(state!.snapshotPath).catch(() => {});
}

export default globalTeardown;
```

**FILE 3 — `tests/playwright.config.ts` (EDIT):**

Tambah 1 line di `defineConfig` block, PRESERVE everything else:

```typescript
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  globalTeardown: require.resolve('./e2e/global.teardown.ts'),  // NEW Phase 315
  timeout: 60_000,
  expect: { timeout: 10_000 },
  fullyParallel: false,        // PRESERVE — singleton collector lifecycle depends on this
  retries: 0,
  reporter: [['html', { open: 'never' }], ['list']],
  use: {
    baseURL: 'http://localhost:5277',
    screenshot: 'on',           // PRESERVE — matrixReport pakai test-results path
    trace: 'on-first-retry',
    viewport: { width: 1280, height: 720 },
  },
  projects: [
    { name: 'setup', testMatch: /global\.setup\.ts/ },
    { name: 'chromium', use: { browserName: 'chromium' }, dependencies: ['setup'] },
  ],
});
```

**FILE 4 — `docs/SEED_JOURNAL.md` (no static edit — append happens at runtime via setup):**

Tidak perlu modify docs/SEED_JOURNAL.md secara static di plan ini. globalSetup akan append entry saat run. globalTeardown akan update 'active' → 'cleaned' via regex.

**WAJIB:**
- PRESERVE existing 7 baris di global.setup.ts (don't break login assertion).
- buildScenarios() WAJIB ISI 10 entries lengkap (7 discovery S1-S7 + 3 sentinel S8-S10) — JANGAN tinggalkan placeholder comment.
- playwright.config.ts: HANYA tambah 1 line (`globalTeardown:`), JANGAN ubah fullyParallel/retries/screenshot.
- Bahasa Indonesia untuk console.log + error message (Pattern A).
  </action>
  <verify>
    <automated>cd tests &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/global.setup.ts` exists; `grep -c "page.goto('/Account/Login')" tests/e2e/global.setup.ts` returns 1 (existing assertion preserved)
    - `grep -c "db.backup(snapshotPath)" tests/e2e/global.setup.ts` returns 1
    - `grep -c "db.execScript" tests/e2e/global.setup.ts` returns 1
    - `grep -cE "Layer 1" tests/e2e/global.setup.ts` returns ≥ 1 (validation comment)
    - `grep -c ".matrix-state.json" tests/e2e/global.setup.ts` returns ≥ 1
    - `grep -c "SEED_JOURNAL" tests/e2e/global.setup.ts` returns ≥ 1 (appendFile journal)
    - `grep -c "function buildScenarios" tests/e2e/global.setup.ts` returns 1
    - `grep -cE "id: ([1-9]|10)" tests/e2e/global.setup.ts` returns ≥ 10 (10 scenario entries — S1..S10)
    - `tests/e2e/global.teardown.ts` exists; `grep -c "collector.flush" tests/e2e/global.teardown.ts` returns 1
    - `grep -c "db.restore" tests/e2e/global.teardown.ts` returns 1
    - `grep -cE "Layer 4" tests/e2e/global.teardown.ts` returns ≥ 1
    - `grep -c "cleaned" tests/e2e/global.teardown.ts` returns ≥ 1 (journal update)
    - `grep -c "export default globalTeardown" tests/e2e/global.teardown.ts` returns 1
    - `tests/playwright.config.ts`: `grep -c "globalTeardown" tests/playwright.config.ts` returns 1
    - `grep -c "fullyParallel: false" tests/playwright.config.ts` returns 1 (PRESERVE)
    - `grep -c "screenshot: 'on'" tests/playwright.config.ts` returns 1 (PRESERVE)
    - `cd tests && npx tsc --noEmit` exits 0
    - Smoke run integration (manual verification by executor): `cd tests && npx playwright test --list assessment-matrix` (triggers setup → seed → state.json → teardown — should complete with Layer 4 row count = 0 post-run).
  </acceptance_criteria>
  <done>
    global.setup.ts extended with BACKUP/seed/Layer 1/state.json/journal append. global.teardown.ts created with flush/RESTORE/Layer 4/journal cleaned/cleanup. playwright.config.ts registers globalTeardown. TS compile passes. Manual smoke `--list` triggers setup → teardown cleanly without leaving matrix rows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Setup → SQL Server (BACKUP + execScript) | Test trusts hostname guard di dbSnapshot.ts |
| Teardown → SQL Server (RESTORE) | RESTORE WITH REPLACE — destructive if hostname wrong; guard at dbSnapshot.ts layer |
| Seed SQL → DB rows | Only INSERT rows with Title prefix `[MATRIX_TEST_2026_05_11]` — Layer 1+4 verify scope |
| state.json (filesystem) | Local-only artifact, gitignored — no commit risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-315-01 | Tampering | dbSnapshot.ts target SQL Server | mitigate | Plan 02 implements hostname guard; Plan 03 only invokes via `db.backup/restore/execScript/queryScalar` (no direct sqlcmd args) |
| T-315-02 | Information Disclosure | seed SQL Title prefix leak | mitigate | Title `[MATRIX_TEST_2026_05_11]` non-PII synthetic; `tests/sql/*.sql` di-commit (deliberate review), `.bak` snapshot gitignored (Plan 02) |
| T-315-03 | Tampering / cleanup leak | DB rows persist after run | mitigate | globalTeardown Layer 4 explicit `queryScalar` check + throw if non-zero; Pattern G journal regex replace `active → cleaned` |
| T-315-04 | Tampering | SQL string injection via state.snapshotPath | mitigate | snapshotPath constructed from `Date.toISOString().replace(/[:.]/g, '-')` (regex sanitized), no user input; RESTORE wraps path in single-quote literal |
</threat_model>

<verification>
- 5 files modified (seed SQL, setup, teardown, config, journal — journal runtime)
- TS compile (`cd tests && npx tsc --noEmit`) passes
- Manual smoke `--list` runs setup → seed → Layer 1=18 → state.json → teardown → flush → RESTORE → Layer 4=0 → journal cleaned
- Layer 1 + Layer 4 validations enforce row counts (RESEARCH Q3 + Pitfall 3)
</verification>

<success_criteria>
- Seed SQL idempotent (re-run safe via 6-step cleanup)
- IDENTITY_INSERT pattern dipakai untuk 4 tabel (Pitfall 4 mitigated)
- globalSetup pre-check collision + BACKUP + Layer 1 validation
- globalTeardown flush-before-RESTORE order (Pitfall: preserve findings)
- Layer 4 row count check = 0 (T-315-03)
- Journal append + cleaned transition automated
</success_criteria>

<output>
After completion, create `.planning/phases/315-assessment-matrix-test/315-03-SUMMARY.md` dengan:
- Seed SQL row counts per table (Final SELECT output)
- Setup pipeline steps executed
- Teardown pipeline steps executed
- Layer 1 + Layer 4 expected vs actual
- TS compile result
- Manual smoke `--list` result
</output>
