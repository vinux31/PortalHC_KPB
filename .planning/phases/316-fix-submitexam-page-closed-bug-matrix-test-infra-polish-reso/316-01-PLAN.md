---
phase: 316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/helpers/matrixReport.ts
  - tests/e2e/helpers/examMatrix.ts
autonomous: true
requirements: [GAP-315-1, GAP-315-2, GAP-315-3]
tags: [playwright, e2e, test-infra, race-condition, screenshot]

must_haves:
  truths:
    - "softAssert catch handler re-throws SkipScenarioError tanpa record finding (Pitfall 5 fix)"
    - "Submit-exam helper memakai Promise.all([waitForURL, click]) — listener arm sebelum click fire navigate (D-01)"
    - "Setiap softAssert callback MC/MA/Essay punya page.isClosed() gate yang throw SkipScenarioError saat closed (D-06)"
    - "Screenshot capture di softAssert defensive (try/catch + isClosed pre-check) — page closed → screenshotPath undefined (D-09)"
    - "renderFinding fallback ke Playwright auto-capture path saat custom path missing (D-11)"
    - "Forward-slash path emission di markdown report — Windows backslash dihindari (Pitfall 4)"
  artifacts:
    - path: "tests/e2e/helpers/matrixReport.ts"
      provides: "softAssert with SkipScenarioError re-throw branch + defensive screenshot + resolveScreenshotPath fallback"
      contains: "instanceof SkipScenarioError"
    - path: "tests/e2e/helpers/matrixReport.ts"
      provides: "resolveScreenshotPath() helper for renderer fallback"
      contains: "function resolveScreenshotPath"
    - path: "tests/e2e/helpers/examMatrix.ts"
      provides: "submit step uses Promise.all race-tolerant + isClosed gate di MC/MA/Essay"
      contains: "Promise.all"
    - path: "tests/e2e/helpers/examMatrix.ts"
      provides: "isClosed gate pattern di MC/MA/Essay softAssert callback"
      contains: "page.isClosed()"
  key_links:
    - from: "tests/e2e/helpers/examMatrix.ts (submit step)"
      to: "tests/e2e/helpers/matrixReport.ts (softAssert catch handler)"
      via: "throw SkipScenarioError → softAssert re-throw branch → bubble ke runDiscoveryScenario"
      pattern: "if \\(e instanceof SkipScenarioError\\) \\{\\s*throw e;"
    - from: "tests/e2e/helpers/examMatrix.ts (isClosed gate)"
      to: "matrixReport.SkipScenarioError"
      via: "throw new SkipScenarioError(`page closed before...`)"
      pattern: "if \\(page\\.isClosed\\(\\)\\) \\{[^}]*throw new SkipScenarioError"
    - from: "tests/e2e/helpers/matrixReport.ts (renderFinding)"
      to: "test-results/assessment-matrix-Scenario-*/test-failed-*.png"
      via: "resolveScreenshotPath fallback glob readdirSync"
      pattern: "assessment-matrix-Scenario-"
---

<objective>
Surgical hardening Playwright matrix test helper untuk resolve 3 acknowledged gaps Phase 315 UAT (GAP-315-1 sentinel S8/S9/S10, GAP-315-2 screenshot path, GAP-315-3 inter-scenario continue).

**Purpose:** Eliminate submit-exam page-closed race + cascade noise + dead screenshot link di report. Setelah plan ini selesai, full 10-scenario run di-expect bisa execute tanpa halt → meta-validation sentinel verifiable di Plan 02.

**Output:** 2 file ter-edit (`matrixReport.ts`, `examMatrix.ts`). Tidak ada file baru, tidak ada schema change, tidak ada server code touch. Blast radius minimal per D-08 + D-12.

**Critical sequencing dependency:** Task 1 (softAssert re-throw amendment) WAJIB SEBELUM Task 3 (isClosed gate) — tanpa re-throw branch, gate throw akan di-swallow softAssert generic catch dan record finding severity='major' → cascade noise tetap muncul (Pitfall 5 di RESEARCH.md:400-407 + Open Question 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-PATTERNS.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md
@.planning/phases/315-assessment-matrix-test/315-UAT.md
@CLAUDE.md
@tests/e2e/helpers/examMatrix.ts
@tests/e2e/helpers/matrixReport.ts
@tests/e2e/helpers/matrixTypes.ts
@tests/e2e/helpers/exam313.ts

<interfaces>
<!-- Type contracts & exports yang executor butuh — JANGAN exploration ulang. -->

From tests/e2e/helpers/matrixTypes.ts:
```typescript
export type Severity = 'critical' | 'major' | 'minor';
export type Finding = {
  scenarioId: number;
  scenarioTitle: string;
  step: string;
  expected: string;
  actual: string;
  screenshotPath?: string;   // SUDAH optional — tidak perlu edit type
  severity: Severity;
  isMeta?: boolean;
};
```

From tests/e2e/helpers/matrixReport.ts (current — pre-fix):
```typescript
export class SkipScenarioError extends Error { /* line 30-35 */ }
export const collector: Collector;
export async function softAssert<T>(
  ctx: { scenario: ScenarioConfig; step: string; severity: Severity; page: Page; isMeta?: boolean },
  fn: () => Promise<T>,
  expected: string
): Promise<T | null>;  // line 198-227 — TARGET amendment
// renderFinding (line 274-287, internal) — TARGET amendment
// FINDINGS_DIR = resolve(__dirname, '..', '..', 'test-results')   // line 42
```

From tests/e2e/helpers/examMatrix.ts (current — pre-fix):
```typescript
import { softAssert, SkipScenarioError } from './matrixReport';   // line 26 — sudah import, siap pakai
// takeExam loop body (line 96-148) — TARGET isClosed gate (MC line 104, MA line 118, Essay line 135)
// submit softAssert (line 150-159) — TARGET Promise.all wrap
```

From tests/e2e/helpers/exam313.ts (REFERENCE PRECEDENT — Phase 313.1):
```typescript
// exam313.ts:37-40 — waitForURL setelah click (navigation race-tolerant style)
// exam313.ts:60-66 — Promise.race fallback pattern
// exam313.ts:106-108 — assertSubmitSuccess waitForURL Results pattern
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Amend softAssert dengan SkipScenarioError re-throw branch (Pitfall 5 prerequisite)</name>
  <files>tests/e2e/helpers/matrixReport.ts</files>
  <read_first>
    - tests/e2e/helpers/matrixReport.ts:198-227 (softAssert current implementation)
    - tests/e2e/helpers/matrixReport.ts:30-35 (SkipScenarioError class)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH.md (§ Pitfall 5 line 400-407 + Open Question 2 line 486-489)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-PATTERNS.md (§ Core pattern A softAssert re-throw line 186-264)
  </read_first>
  <action>
    Edit `tests/e2e/helpers/matrixReport.ts` softAssert function (line 198-227) untuk insert SkipScenarioError re-throw branch di catch handler PALING ATAS (sebelum screenshot capture + collector.record).

    Implementasi mengikuti pattern di 316-PATTERNS.md § "Target pattern after fix" (line 214-264). Per D-09 + Pitfall 5:

    1. **Re-throw branch di awal catch** — block paling pertama setelah `catch (e: unknown) {`:
       ```typescript
       // Phase 316: re-throw SkipScenarioError tanpa record finding — helper sudah signal
       // explicit skip (page-closed cascade abort). Tanpa branch ini, isClosed gate akan
       // di-swallow oleh severity='major' continue-logic → cascade noise tetap muncul.
       if (e instanceof SkipScenarioError) {
         throw e;
       }
       ```
       Branch ini WAJIB sebelum `const err = e as ...`. Per D-12 — `matrixReport.ts` only.

    2. **Defensive screenshot capture (D-09)** — ganti baris 207-209 (current):
       ```typescript
       const stepSlug = ctx.step.replace(/\s+/g, '-').replace(/[^a-zA-Z0-9-]/g, '');
       const screenshotPath = `test-results/matrix-s${ctx.scenario.id}-${stepSlug}.png`;
       await ctx.page.screenshot({ path: screenshotPath, fullPage: true }).catch(() => {});
       ```
       Dengan defensive variant:
       ```typescript
       const err = e as { message?: string };
       const stepSlug = ctx.step.replace(/\s+/g, '-').replace(/[^a-zA-Z0-9-]/g, '');
       const candidatePath = `test-results/matrix-s${ctx.scenario.id}-${stepSlug}.png`;

       // Phase 316: defensive screenshot — page-closed pre-check + try/catch.
       // Closed → skip custom path (jangan throw, jangan retry); renderer fallback
       // ke Playwright auto-capture handle missing custom path di renderFinding.
       let screenshotPath: string | undefined;
       if (!ctx.page.isClosed()) {
         try {
           await ctx.page.screenshot({ path: candidatePath, fullPage: true });
           screenshotPath = candidatePath;  // only set kalau write sukses
         } catch {
           // Page may have closed antara isClosed() check dan screenshot fire (microsec race).
           // Skip silent — renderer fallback handles missing custom path.
         }
       }
       ```

    3. **collector.record pass screenshotPath optional** — collector.record sudah accept `screenshotPath?: string` (matrixTypes.ts:27). Tidak perlu ubah type. Pass `screenshotPath` (mungkin undefined) ke field.

    4. **Preserve critical → throw SkipScenarioError di akhir** — block existing line 222-224 tetap. Tidak diubah.

    PENTING: Re-throw branch HARUS instance check `e instanceof SkipScenarioError`, bukan string match. SkipScenarioError class sudah ter-export di line 30-35 — accessible di scope softAssert.

    Tidak edit file lain. Tidak ubah `matrixTypes.ts`. Tidak ubah `examMatrix.ts` di task ini (terpisah di Task 2 & 3).
  </action>
  <verify>
    <automated>cd tests &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep `instanceof SkipScenarioError` di `tests/e2e/helpers/matrixReport.ts` returns ≥1 match (di softAssert function body)
    - grep `if (!ctx.page.isClosed())` di `tests/e2e/helpers/matrixReport.ts` returns 1 match (defensive screenshot block)
    - grep `screenshotPath = candidatePath` di `tests/e2e/helpers/matrixReport.ts` returns 1 match (assign only on success)
    - TypeScript compile (`npx tsc --noEmit`) zero error di tests/ scope
    - softAssert function body order: (a) try fn (b) catch — re-throw SkipScenarioError FIRST (c) defensive screenshot dengan isClosed gate (d) collector.record (e) critical → throw SkipScenarioError
    - File header docblock (line 1-19) tidak diubah; comment baru hanya inline di body
  </acceptance_criteria>
  <done>softAssert catch handler re-throws SkipScenarioError tanpa record finding. Defensive screenshot dengan isClosed pre-check + try/catch implemented. screenshotPath jadi optional `string | undefined`, pass ke collector.record. TypeScript compile clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply Promise.all submit race fix + JSDoc update di examMatrix.ts (D-01, D-03)</name>
  <files>tests/e2e/helpers/examMatrix.ts</files>
  <read_first>
    - tests/e2e/helpers/examMatrix.ts:150-159 (current submit step)
    - tests/e2e/helpers/examMatrix.ts:42-61 (current takeExam JSDoc header)
    - tests/e2e/helpers/exam313.ts:37-40, 60-66, 106-108 (Phase 313.1 race-tolerant precedent)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH.md (§ Promise.all submit race fix line 108-158 + Pitfall 1 line 368-375)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-PATTERNS.md (§ Core pattern A line 33-84)
  </read_first>
  <action>
    Edit `tests/e2e/helpers/examMatrix.ts` submit step (line 150-159) — wrap `page.click()` + `page.waitForURL()` di single `Promise.all` per D-01.

    **CRITICAL ordering constraint (Pitfall 1):** `waitForURL` HARUS di Promise.all array index 0 (listener arm synchronously sebelum click). Reverse order = bug-equivalent.

    1. **Replace submit softAssert body (line 152-156 current):**
       ```typescript
       // current — BUG
       await page.click('#reviewSubmitBtn, [type="submit"]:not(.btn-cancel)');
       await page.waitForURL(/\/CMP\/Results\/\d+/, { timeout: 15_000 });
       ```
       Dengan (per D-01 + D-03 — selector OR-clause dipertahankan utuh):
       ```typescript
       // Phase 316 fix: arm waitForURL BEFORE click fires navigate.
       // Race-tolerant per Phase 313.1 precedent (exam313.ts:39, 107).
       // Order matters: waitForURL index 0 (listener arm sync), click index 1 (action fire).
       // Reverse order = bug-equivalent (Pitfall 1 RESEARCH.md:368-375).
       await Promise.all([
         page.waitForURL(/\/CMP\/Results\/\d+/, { timeout: 15_000 }),
         page.click('#reviewSubmitBtn, [type="submit"]:not(.btn-cancel)'),
       ]);
       ```

    2. **Update takeExam JSDoc header (line 42-61)** — tambahkan Phase 316 fix note di awal JSDoc, sebelum existing "Hub readiness gate" paragraph. Format:
       ```typescript
       /**
        * Peserta login + buka StartExam + jawab semua questions + Submit.
        *
        * Phase 316 fix:
        * - Submit click memakai `Promise.all([waitForURL, click])` race-tolerant
        *   (precedent exam313.ts:107). Eliminate "Target page, context or browser has been closed"
        *   regression dari Phase 315 smoke run 2026-05-11T06:14:36Z.
        * - `page.isClosed()` gate di awal setiap softAssert callback (MC/MA/Essay) — throw
        *   SkipScenarioError langsung saat page closed mid-loop. Cegah cascade-fail noise di report.
        *
        * Hub readiness gate via `window.assessmentHub.state === 'Connected'` mencegah Pitfall 1
        * [...existing JSDoc content preserved...]
        */
       ```

    3. **Tidak edit OR-selector** — pertahankan `'#reviewSubmitBtn, [type="submit"]:not(.btn-cancel)'` per D-03 (cover desktop review modal + mobile submit).

    4. **Tidak ubah severity** — submit step tetap `severity: 'critical'` (existing). Critical fail tetap escalate via softAssert standard logic.

    5. **Tidak ubah other softAssert calls di file ini** — isClosed gate ditangani Task 3.

    Constraint per D-08: minimal blast radius. Hanya 2 perubahan: (a) Promise.all wrap, (b) JSDoc update. Tidak refactor function, tidak extract submitExam standalone.
  </action>
  <verify>
    <automated>cd tests &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -n `Promise.all` di `tests/e2e/helpers/examMatrix.ts` returns ≥1 match di submit step (sekitar line 150-159 region)
    - grep -n `page.waitForURL` di `tests/e2e/helpers/examMatrix.ts` submit step muncul di array index 0 (LINE pertama dalam Promise.all array, sebelum page.click)
    - grep -n `page.click('#reviewSubmitBtn` di `tests/e2e/helpers/examMatrix.ts` muncul HANYA 1× (di Promise.all array index 1), bukan standalone statement di luar Promise.all
    - grep `Phase 316 fix` di JSDoc takeExam returns ≥1 match
    - TypeScript compile (`npx tsc --noEmit`) zero error di tests/ scope
    - Submit step softAssert `severity: 'critical'` preserved (tidak diubah ke major)
    - OR-selector string `'#reviewSubmitBtn, [type="submit"]:not(.btn-cancel)'` preserved verbatim
  </acceptance_criteria>
  <done>Submit step memakai Promise.all dengan waitForURL di index 0 + click di index 1. JSDoc takeExam header mencatat Phase 316 fix. OR-selector + critical severity tidak berubah. TypeScript compile clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Insert page.isClosed() gate di MC/MA/Essay softAssert callback + renderFinding fallback (D-06, D-09, D-11)</name>
  <files>tests/e2e/helpers/examMatrix.ts, tests/e2e/helpers/matrixReport.ts</files>
  <read_first>
    - tests/e2e/helpers/examMatrix.ts:96-148 (MC/MA/Essay softAssert callbacks)
    - tests/e2e/helpers/matrixReport.ts:21-24 (current imports)
    - tests/e2e/helpers/matrixReport.ts:42 (FINDINGS_DIR resolve)
    - tests/e2e/helpers/matrixReport.ts:274-287 (renderFinding current)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH.md (§ page.isClosed line 160-196 + § Screenshot defensive line 198-256 + Pitfall 3 line 384-391 + Pitfall 4 line 393-398)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-PATTERNS.md (§ Core pattern B isClosed line 87-133 + § renderFinding fallback line 269-346)
  </read_first>
  <action>
    Dua edit di dua file. Task ini boleh dipisah commit per file kalau executor mau, tapi pattern + scope satu task tunggal karena saling tergantung secara konseptual (gate butuh fallback supaya report tidak dead-link).

    **Bagian A — `tests/e2e/helpers/examMatrix.ts` (D-06):**

    Insert `page.isClosed()` gate di **awal setiap softAssert callback body** untuk 3 step type (MC line 102-113, MA line 116-128, Essay line 133-145).

    Pattern WAJIB per step (per D-06 + 316-PATTERNS.md line 110-126):
    ```typescript
    // Phase 316: page-closed gate — abort cascade saat submit-exam (langkah sebelumnya) sudah
    // close context. SkipScenarioError di-rethrow oleh softAssert catch handler (matrixReport.ts).
    if (page.isClosed()) {
      throw new SkipScenarioError(`page closed before mc-q${q.id} step — cascade abort`);
    }
    ```

    Pesan throw per step type:
    - MC (line 104): `\`page closed before mc-q${q.id} step — cascade abort\``
    - MA (line 118): `\`page closed before ma-q${q.id} step — cascade abort\``
    - Essay (line 135): `\`page closed before essay-q${q.id} step — cascade abort\``

    **Placement constraint (Pitfall 2):** Gate WAJIB di dalam softAssert callback body (line PERTAMA setelah `async () => {`), BUKAN di luar softAssert atau di top-level helper. Per loop iteration page state shifts; satu-kali-check-di-luar tidak cukup.

    **Coupling note:** Pattern ini hanya bekerja kalau Task 1 sudah commit (softAssert re-throw branch). Eksekusi Task 1 → Task 2 → Task 3 sequential.

    **Bagian B — `tests/e2e/helpers/matrixReport.ts` renderer fallback (D-11):**

    1. **Extend imports (line 21-24)** dari `fs/promises` saja menjadi juga import sync `existsSync` + `readdirSync` dari `fs`:
       ```typescript
       import { Page } from '@playwright/test';
       import { writeFile, mkdir, readdir, readFile, unlink, appendFile } from 'fs/promises';
       import { existsSync, readdirSync } from 'fs';
       import { dirname, resolve, join } from 'path';
       import type { Finding, Severity, ScenarioConfig } from './matrixTypes';
       ```
       Rationale: `renderFinding` sinkron — async fs API tidak applicable tanpa convert whole flush chain ke async. Sync fs API minimal blast radius per D-12.

    2. **Tambah `resolveScreenshotPath()` helper SEBELUM `renderFinding()`** (sebelum line 274) — implementasi per 316-PATTERNS.md line 302-330:
       ```typescript
       /**
        * Phase 316 fallback strategy (D-11):
        * 1. Kalau `f.screenshotPath` di-set DAN file exists di disk → emit custom path.
        * 2. Kalau missing OR undefined → scan Playwright auto-capture dir
        *    (`test-results/assessment-matrix-Scenario-*/test-failed-*.png`), emit first match.
        *    Best-effort — slug→scenarioId correlation tidak guaranteed (Pitfall 3 RESEARCH.md:384-391).
        * 3. Kalau juga tidak ada → return undefined → renderFinding omit Screenshot line.
        *
        * Forward-slash path emission (Pitfall 4 RESEARCH.md:393-398) — string concat,
        * JANGAN path.join (Windows emit backslash, broken di markdown viewer).
        */
       function resolveScreenshotPath(f: Finding): string | undefined {
         // Layer 1: custom path exists di disk.
         if (f.screenshotPath) {
           const abs = resolve(f.screenshotPath);
           if (existsSync(abs)) {
             return f.screenshotPath;
           }
         }
         // Layer 2: Playwright auto-capture fallback. Scan FINDINGS_DIR untuk
         // dir berawalan 'assessment-matrix-Scenario-' lalu cari 'test-failed-*.png' di dalamnya.
         // [ASSUMED A1] naming pattern observed di smoke run 315-UAT.md:24 — Wave 0 verify.
         try {
           if (!existsSync(FINDINGS_DIR)) return undefined;
           const subdirs = readdirSync(FINDINGS_DIR, { withFileTypes: true })
             .filter((d) => d.isDirectory() && d.name.startsWith('assessment-matrix-Scenario-'));
           for (const d of subdirs) {
             const inner = readdirSync(join(FINDINGS_DIR, d.name))
               .filter((n) => n.startsWith('test-failed-') && n.endsWith('.png'));
             if (inner.length > 0) {
               // Forward-slash output untuk markdown viewer compat (Windows safety).
               return `test-results/${d.name}/${inner[0]}`;
             }
           }
         } catch {
           // fs probe error — fall through to undefined (no Screenshot line).
         }
         return undefined;
       }
       ```

    3. **Modify `renderFinding()` (line 274-287)** — replace direct `f.screenshotPath` reference dengan `resolveScreenshotPath(f)`:
       ```typescript
       function renderFinding(f: Finding): string {
         const lines = [
           `### Scenario ${f.scenarioId}: ${f.scenarioTitle} — ${f.step}`,
           ``,
           `- **Severity:** ${f.severity}`,
           `- **Expected:** ${f.expected}`,
           `- **Actual:** ${f.actual}`,
         ];
         const resolvedPath = resolveScreenshotPath(f);
         if (resolvedPath) {
           lines.push(`- **Screenshot:** \`${resolvedPath}\``);
         }
         lines.push(`- **Hypothesis:** ${deriveHypothesis(f)}`);
         return lines.join('\n');
       }
       ```

    **JANGAN:**
    - Convert renderFinding ke async (blast radius besar — touches flush + callers)
    - Pakai `path.join` saat emit markdown path string (Windows backslash bug — Pitfall 4)
    - Ubah `matrixTypes.ts` (Finding.screenshotPath sudah optional)
    - Edit `assessment-matrix.spec.ts` (consumer-only per D-12 + 316-PATTERNS.md line 374-398)
  </action>
  <verify>
    <automated>cd tests &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx playwright test assessment-matrix --grep "Scenario 5"</automated>
  </verify>
  <acceptance_criteria>
    - grep `page.isClosed()` di `tests/e2e/helpers/examMatrix.ts` returns ≥3 matches (MC, MA, Essay callback bodies)
    - grep `throw new SkipScenarioError` di `tests/e2e/helpers/examMatrix.ts` returns ≥3 matches (MC, MA, Essay)
    - Setiap match `page.isClosed()` ada di line PERTAMA dalam softAssert callback `async () => {` body (sebelum `await page.check/fill/locator`)
    - grep `function resolveScreenshotPath` di `tests/e2e/helpers/matrixReport.ts` returns 1 match
    - grep `import { existsSync, readdirSync } from 'fs'` di `tests/e2e/helpers/matrixReport.ts` returns 1 match
    - grep `assessment-matrix-Scenario-` di `tests/e2e/helpers/matrixReport.ts` returns ≥1 match (fallback glob filter)
    - grep `path.join` di `resolveScreenshotPath` body returns 0 match (Pitfall 4 — pakai string concat)
    - grep `test-results/${d.name}/${inner[0]}` (atau equivalent forward-slash template) returns 1 match
    - renderFinding pakai `resolveScreenshotPath(f)` — grep returns 1 match
    - TypeScript compile (`npx tsc --noEmit`) zero error
    - Quick run `cd tests && npx playwright test assessment-matrix --grep "Scenario 5"` complete: report mencatat S5 reach `/CMP/Results/{id}` URL OR S5 finding ≤1 (no cascade major findings post-submit). Manual verifikasi di Plan 02.
  </acceptance_criteria>
  <done>isClosed gate diterapkan di 3 softAssert callback MC/MA/Essay; SkipScenarioError di-throw eksplisit per step. resolveScreenshotPath() helper baru di-implement; renderFinding pakai fallback. Forward-slash path emission. TypeScript compile clean. S5 isolated run hasilnya akan di-verify formal di Plan 02 task 1.</done>
</task>

</tasks>

<threat_model>
**Trust boundaries:** Phase 316 = test-helper code only. Tidak melewati trust boundary baru (no user input, no auth flow, no DB query, no external API). softAssert wrapper sudah ada di Phase 315 — tidak introduce new attack surface.

**STRIDE assessment:** N/A — test infrastructure change, no production code path affected. CLAUDE.md security_enforcement: phase scope explicit "test-helper hardening only, no app code/DB schema impact" (per 316-RESEARCH.md line 76). Server-side SubmitExam scope di-tolak (D-02 = manual smoke verify only).
</threat_model>

<verification>
**Plan-level checks (executor responsibility post all tasks):**
1. `cd tests && npx tsc --noEmit -p tsconfig.json` → zero error di tests/ scope
2. `cd tests && npx playwright test assessment-matrix --grep "Scenario 5"` → execute (verify quality di Plan 02)
3. Inspect generated `docs/test-reports/{YYYY-MM-DD}-assessment-matrix.md` post quick run:
   - S5 zero critical OR ≤1 critical finding (submit) tanpa cascade major findings di step MC/MA/Essay setelah submit
   - Screenshot links di report mencapai file real (custom path atau fallback path)
4. Run `git diff tests/e2e/helpers/` → konfirmasi scope minimal (≤80 lines added net across kedua file)

**Pitfall checks (PENTING — verify each):**
- Pitfall 1 — Promise.all order: grep submit step di examMatrix.ts, verifikasi `waitForURL(...)` muncul **sebelum** `page.click(...)` di array literal
- Pitfall 2 — isClosed placement: setiap gate muncul di line pertama callback body (sebelum await page action)
- Pitfall 3 — fallback best-effort: documented di JSDoc resolveScreenshotPath (assumed naming pattern A1)
- Pitfall 4 — forward-slash: tidak ada `path.join` di markdown path emit; pakai template literal `test-results/...`
- Pitfall 5 — re-throw branch: muncul di awal softAssert catch handler, sebelum stepSlug/screenshot logic
</verification>

<success_criteria>
- [ ] `tests/e2e/helpers/matrixReport.ts` modified — softAssert dengan SkipScenarioError re-throw branch + defensive screenshot + resolveScreenshotPath() helper + renderFinding fallback
- [ ] `tests/e2e/helpers/examMatrix.ts` modified — Promise.all submit fix + isClosed gate 3× + JSDoc Phase 316 note
- [ ] TypeScript compile clean (`npx tsc --noEmit`)
- [ ] S5 isolated run executes (full quality verdict di Plan 02 task 1)
- [ ] Tidak ada edit di `matrixTypes.ts`, `assessment-matrix.spec.ts`, atau file lain
- [ ] All grep-based acceptance criteria di setiap task pass

**Deferred ke Plan 02:** Full 10-scenario run, sentinel S8/S9/S10 meta validation, UAT 5 items, server smoke 302 verify, screenshot path consistency human-verify.
</success_criteria>

<output>
After completion, create `.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-01-SUMMARY.md` dengan:
- Diff stats (file modified, lines added/removed)
- TypeScript compile result
- Quick S5 run result (raw output dipersembahkan, formal verdict di Plan 02)
- Catatan kalau Wave 0 assumption A1 (auto-capture dir naming) butuh tune di Plan 02
</output>
