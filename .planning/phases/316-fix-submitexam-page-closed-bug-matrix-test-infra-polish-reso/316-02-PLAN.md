---
phase: 316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso
plan: 02
type: execute
wave: 2
depends_on: ["316-01"]
files_modified:
  - docs/test-reports/2026-05-11-assessment-matrix.md
  - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md
autonomous: false
requirements: [GAP-315-1, GAP-315-2, GAP-315-3]
tags: [playwright, e2e, verification, uat, sentinel]

must_haves:
  truths:
    - "S5 isolated run reach `/CMP/Results/{id}` redirect post Promise.all fix (D-15 acceptance #1)"
    - "S8 [META-AllCorrect] score 100/100 — meta finding absent OR documents match (D-15 #2)"
    - "S9 [META-AllWrong] score 0/0 — meta finding absent OR documents match (D-15 #3)"
    - "S10 [META-CollectorCheck] meta finding tercatat di `## Meta-validation results` + exit 0 (D-15 #4)"
    - "Inter-scenario continue: S1 fail tidak halt S2-S10 (D-15 #5)"
    - "Server SubmitExam returns 302 + Location `/CMP/Results/{id}` (D-02 manual smoke)"
    - "Report markdown screenshot links resolve to real files (custom path OR fallback)"
  artifacts:
    - path: "docs/test-reports/2026-05-11-assessment-matrix.md"
      provides: "Full 10-scenario run report dengan meta-validation section non-empty"
      contains: "## Meta-validation results"
    - path: ".planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md"
      provides: "5 UAT items per D-16 dengan verdict pass/fail per item"
      contains: "fix-validate-S5"
  key_links:
    - from: "316-UAT.md fix-validate-S5"
      to: "S5 scenario reach /CMP/Results/{id}"
      via: "playwright test --grep Scenario 5 → quick run report"
      pattern: "Scenario 5"
    - from: "316-UAT.md full-run-S1-S10"
      to: "10 scenario test() blocks executed"
      via: "playwright test assessment-matrix full run"
      pattern: "## Meta-validation results"
    - from: "316-UAT.md sentinel-meta-verify"
      to: "S10 META-CollectorCheck finding"
      via: "report grep '## Meta-validation results' non-empty"
      pattern: "META-CollectorCheck"
---

<objective>
Verifikasi E2E hardening Plan 01 via staged validation (D-13): (a) quick S5 isolated run konfirmasi submit race fix, (b) full 10-scenario run konfirmasi sentinel + continue-on-fail, (c) manual server smoke 302 redirect, (d) UAT 5 items terdokumentasi.

**Purpose:** Close 3 acknowledged gaps Phase 315 UAT via observable evidence (report file + UAT verdict). Plan ini tidak edit code — semua verifikasi runtime + manual.

**Output:** 1 report file regenerated (`docs/test-reports/2026-05-11-assessment-matrix.md` — overwrite by Playwright globalTeardown) + 1 new UAT file (`316-UAT.md` dengan 5 D-16 items verdict + screenshot evidence). Plus manual D-02 server smoke result documented.

**Critical dependency:** Plan 01 WAJIB committed sebelum plan ini run. Quick run S5 di Task 3 Plan 01 hanya smoke — formal verdict pass/fail dicatat di Plan 02 Task 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-01-PLAN.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-01-SUMMARY.md
@.planning/phases/315-assessment-matrix-test/315-UAT.md
@docs/test-reports/2026-05-11-assessment-matrix.md
@CLAUDE.md

<interfaces>
<!-- Commands & file paths yang executor butuh — JANGAN exploration ulang. -->

**Quick run command (per D-13.a):**
```bash
cd tests && npx playwright test assessment-matrix --grep "Scenario 5"
```
Expected runtime: ~120-180s. Output: `docs/test-reports/{YYYY-MM-DD}-assessment-matrix.md` (date stamp = today's date).

**Full run command (per D-13.b + D-14):**
```bash
cd tests && npx playwright test assessment-matrix
echo "Exit code: $?"
```
Expected runtime: ~20-30 min. Exit 0 contract: S10 test.fail() satisfied + no real bugs.

**Manual server smoke procedure (D-02 — pilih salah satu):**

Option A — PowerShell (Windows native, recommended):
```powershell
# 1. Login lokal via browser http://localhost:5277 sebagai coachee (rino.prasetyo@pertamina.com)
# 2. DevTools → Application → Cookies → copy '.AspNetCore.Identity.Application' value
$cookie = ".AspNetCore.Identity.Application=<paste-cookie-value>"
$sessionId = <pakai sessionId in-flight dari .matrix-state.json atau seed manual via UI>
$resp = Invoke-WebRequest -Uri "http://localhost:5277/CMP/SubmitExam/$sessionId" `
  -Method Post -Headers @{ "Cookie" = $cookie } -MaximumRedirection 0 -SkipHttpErrorCheck
Write-Host "Status: $($resp.StatusCode)"           # expect 302
Write-Host "Location: $($resp.Headers.Location)"   # expect /CMP/Results/$sessionId
```

Option B — Browser DevTools:
1. Login lokal sebagai coachee
2. Network tab, filter `SubmitExam`
3. Submit exam manually via UI
4. Verify request return 302 + Location header `/CMP/Results/{id}`

**Report file location:** `docs/test-reports/{YYYY-MM-DD}-assessment-matrix.md` — overwritten setiap full run via `tests/e2e/global.teardown.ts` flush().

**UAT items per D-16 (5 items):**
1. `fix-validate-S5` — S5 quick run reach Results URL
2. `full-run-S1-S10` — Full 10 scenario execute, exit code documented
3. `sentinel-meta-verify` — S8/S9/S10 meta findings di `## Meta-validation results`
4. `continue-on-fail-E2E` — S1 fail (kalau ada) tidak halt S2-S10
5. `screenshot-path-consistency` — Report screenshot link tidak dead (custom path OR fallback path)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run staged validation (S5 isolated + full S1-S10) + capture evidence</name>
  <files>docs/test-reports/2026-05-11-assessment-matrix.md (Playwright overwrites — read-after-write only)</files>
  <read_first>
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-01-SUMMARY.md (verify Plan 01 commit + TS clean)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH.md (§ Sentinel + test.fail line 258-283 + § Continue-on-fail line 290-321)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md (D-13, D-14, D-15 acceptance criteria)
    - tests/e2e/assessment-matrix.spec.ts:64, 128-167, 223-275, 277-360, 362-404 (catch boundary + S8/S9/S10 spec body)
    - tests/playwright.config.ts (screenshot config + fullyParallel setting)
  </read_first>
  <action>
    Eksekusi staged validation per D-13. SETIAP run capture: (a) exit code, (b) generated report file path, (c) findings count (critical/major/meta).

    **Pre-flight (sebelum run):**
    1. Konfirmasi `dotnet run` app server aktif di `http://localhost:5277` (per CLAUDE.md develop workflow — lokal verify wajib)
    2. Konfirmasi DB lokal punya seed matrix Phase 315 (`.matrix-state.json` exists di `tests/` OR seed via `tests/e2e/global.setup.ts` first run)
    3. Konfirmasi Plan 01 committed: `git log --oneline -3 | grep "316"` ada commit Plan 01

    **Step 1 — Quick S5 isolated run (D-13.a):**
    ```bash
    cd tests && npx playwright test assessment-matrix --grep "Scenario 5" 2>&1 | tee /tmp/316-s5-run.log
    echo "Exit code: $?" | tee -a /tmp/316-s5-run.log
    ```
    Expect: exit 0 (atau exit 1 kalau S5 ada finding lain — record but acceptable per D-15 #1 yang fokus reach Results URL).

    Cek report `docs/test-reports/{YYYY-MM-DD}-assessment-matrix.md` (date = hari run):
    - `## Discovery findings` section S5 entry: zero critical finding OR ≤1 critical yang BUKAN `submit-exam` + tidak ada cascade major mc-q/ma-q/essay-q
    - Atau S5 zero finding total (clean run)

    Catat hasil sebagai S5 verdict: PASS (reach Results) atau FAIL (still cascade page-closed).

    **Step 2 — Full 10-scenario run (D-13.b + D-14):**
    ```bash
    cd tests && npx playwright test assessment-matrix 2>&1 | tee /tmp/316-full-run.log
    EXIT_FULL=$?
    echo "Exit code: $EXIT_FULL" | tee -a /tmp/316-full-run.log
    ```
    Expected runtime ~20-30 min. Bisa run di background (`run_in_background: true` flag).

    **Step 3 — Parse report file (post run):**
    ```bash
    REPORT=$(ls -t docs/test-reports/*-assessment-matrix.md | head -1)
    echo "Report: $REPORT"
    grep -c "^### Scenario" "$REPORT"           # count finding entries
    grep -A 2 "## Meta-validation results" "$REPORT"   # konfirmasi section non-empty
    grep "META-CollectorCheck" "$REPORT"        # S10 sentinel evidence
    grep "META-AllCorrect" "$REPORT"            # S8 (kalau ada meta finding = score mismatch detected; absent = score match)
    grep "META-AllWrong" "$REPORT"              # S9 same
    ```

    **Step 4 — Document Wave 0 verifications (per 316-VALIDATION.md):**
    ```bash
    # Confirm auto-capture dir naming (A1 assumption verify)
    ls tests/test-results/ | grep "^assessment-matrix-Scenario" | head -5
    # Confirm test-failed-*.png ada di dir tersebut
    find tests/test-results -name "test-failed-*.png" 2>/dev/null | head -5
    ```
    Catat: kalau naming pattern `assessment-matrix-Scenario-{slug}-{hash}` MATCH expectation (A1 confirmed) atau berbeda (fallback glob butuh tune di Plan 03 future).

    **Step 5 — Validate Plan 01 acceptance criteria realtime:**
    - Verify report S5 entry tidak ada `page closed` cascade text di Actual field (fix Plan 01 efektif)
    - Verify screenshot links di report point ke real file: `for path in $(grep -oP "Screenshot:\` ?\`?\K[^\`]+" "$REPORT"); do test -f "$path" && echo "OK: $path" || echo "MISSING: $path"; done`
    - Verify `## Meta-validation results` section ada di report (bukan markdown placeholder kosong)

    **Output catatan untuk Task 2 UAT:**
    - S5 verdict (pass/fail + reasoning)
    - Full run exit code
    - Total findings (critical / major / meta) per category
    - Meta section content untuk S10 (eksis = sentinel collector OK)
    - Screenshot link resolve rate (% real / % missing)
    - Auto-capture dir naming pattern (A1 verify result)

    Tidak edit code di task ini. Read-only verification + log capture.
  </action>
  <verify>
    <automated>cd tests && npx playwright test assessment-matrix --grep "Scenario 5" 2>&amp;1 | tee /tmp/316-s5-run.log; echo "S5 exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - File `/tmp/316-s5-run.log` exists dengan output Playwright + exit code line
    - File `/tmp/316-full-run.log` exists dengan output full run + exit code line
    - Report file `docs/test-reports/{today}-assessment-matrix.md` regenerated (mtime today)
    - S5 quick run report tidak menampilkan `page closed` text di S5 finding Actual field (atau S5 zero finding sama sekali)
    - Full run: `grep -c "^### Scenario" docs/test-reports/*-assessment-matrix.md` returns total findings count (catat angka — bisa 0 atau >0)
    - `## Meta-validation results` section EXISTS di report (bukan placeholder `_Tidak ada finding sentinel di run ini._` saat sentinel S10 di-spec emit finding)
    - Untuk setiap `Screenshot:` link di report → file referenced exists di disk OR fallback path resolved (zero dead link tolerable)
    - Auto-capture dir naming verified — `tests/test-results/assessment-matrix-Scenario-*` directories exist
    - Output ringkasan ditulis ke `/tmp/316-validation-summary.txt` dengan format:
      ```
      S5 verdict: PASS/FAIL
      Full run exit: <N>
      Findings: critical=<n> major=<n> meta=<n>
      Meta S10 evidence: <yes/no>
      Screenshot resolve rate: <n/m>
      Auto-capture dir naming: confirmed/different
      ```
  </acceptance_criteria>
  <done>Quick run + full run executed; exit codes + finding counts captured; report file parsed; auto-capture dir pattern confirmed; summary ditulis ke `/tmp/316-validation-summary.txt` siap untuk Task 2 UAT documentation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — D-02 server smoke + D-16 5 UAT items verdict + 316-UAT.md write</name>
  <files>.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md</files>
  <read_first>
    - /tmp/316-validation-summary.txt (Task 1 output)
    - /tmp/316-s5-run.log + /tmp/316-full-run.log (Task 1 raw logs)
    - docs/test-reports/{today}-assessment-matrix.md (regenerated report)
    - .planning/phases/315-assessment-matrix-test/315-UAT.md (precedent format)
    - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md (D-15 acceptance + D-16 UAT item list)
  </read_first>
  <action>
    Pause untuk human verification + dokumentasi `316-UAT.md`. Lihat `<what-built>` dan `<how-to-verify>` di bawah untuk panduan langkah demi langkah.

    Ringkas: (1) eksekusi D-02 server smoke manual (PowerShell atau DevTools), (2) judge 5 UAT items per D-16 berdasarkan output log/report Task 1, (3) tulis `316-UAT.md` pakai format precedent `315-UAT.md`, (4) commit `316-UAT.md`, (5) reply approved/issues/blocked.

    JANGAN edit code di task ini — verification + documentation only.
  </action>
  <verify>
    <automated>test -f .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md &amp;&amp; grep -c "^### " .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `316-UAT.md` exists di phase dir
    - Frontmatter punya field `total: 5` + breakdown passed/issues/blocked
    - 5 D-16 UAT item ID muncul sebagai heading: `fix-validate-S5`, `full-run-S1-S10`, `sentinel-meta-verify`, `continue-on-fail-E2E`, `screenshot-path-consistency`
    - Section `### D-02 Server smoke` ada + dokumentasi PowerShell/DevTools output (Status 302 + Location)
    - Section `## Gaps` ada (kosong kalau semua pass)
    - Human reply: `approved` ATAU `issues: ...` ATAU `blocked: ...`
  </acceptance_criteria>
  <done>5 UAT items punya verdict tertulis + D-02 smoke section terisi + 316-UAT.md committed. Human approval received.</done>
  <what-built>
    Plan 01 hardening Playwright matrix test helper sudah commit + Plan 02 Task 1 sudah run staged validation (S5 isolated + full 10-scenario). Hasil log + report file siap di-verifikasi human untuk:

    1. **D-02 server smoke** — manual PowerShell/DevTools 302 redirect check
    2. **5 UAT items per D-16** — manual verdict per item dengan evidence dari log + report
    3. **316-UAT.md write** — dokumentasi formal verdict (precedent: 315-UAT.md)
  </what-built>
  <how-to-verify>
    **Step 1 — D-02 server smoke (manual, ~5 menit):**

    Pilih PowerShell OR Browser DevTools (per 316-RESEARCH.md § Server smoke 302 verify line 327-364):

    PowerShell:
    1. Login lokal `http://localhost:5277` sebagai coachee (`rino.prasetyo@pertamina.com`)
    2. DevTools → Application → Cookies → copy `.AspNetCore.Identity.Application` value
    3. Jalankan PowerShell script di `<interfaces>` Option A dengan sessionId in-flight
    4. Verify output: `Status: 302` + `Location: /CMP/Results/{sessionId}`

    DevTools alternative:
    1. Login lokal sebagai coachee
    2. Network tab filter `SubmitExam`
    3. Submit exam manually via UI
    4. Verify request return 302 + Location header `/CMP/Results/{id}`

    Catat hasil → masuk ke 316-UAT.md `### D-02 Server smoke` section.

    **Step 2 — 5 UAT items per D-16 verdict (manual, ~10 menit):**

    Buka `/tmp/316-validation-summary.txt` + report file → judge per item:

    | Item ID | How to Verify | Pass Criteria |
    |---------|---------------|---------------|
    | `fix-validate-S5` | Inspect S5 finding di report | Zero critical finding S5 ATAU critical finding bukan `submit-exam page closed` |
    | `full-run-S1-S10` | grep "Scenario" di report + Playwright stdout summary | 10 test() blocks executed (count via stdout `10 passed/failed/skipped`) |
    | `sentinel-meta-verify` | Inspect `## Meta-validation results` section di report | Section non-empty + S10 META-CollectorCheck finding tercatat |
    | `continue-on-fail-E2E` | Inspect stdout per-scenario timestamp + report findings | Kalau S1 ada finding, S2-S10 tetap ada di output (tidak halt) |
    | `screenshot-path-consistency` | Open report markdown preview, click setiap Screenshot link | Setiap link buka image (custom path OR fallback). Zero dead link. |

    **Step 3 — Tulis 316-UAT.md:**

    Pakai format mirip `315-UAT.md` (precedent). Frontmatter:
    ```yaml
    ---
    phase: 316
    uat_date: <today YYYY-MM-DD>
    total: 5
    passed: <N>
    issues: <N>
    blocked: <N>
    ---
    ```

    Per item, format:
    ```markdown
    ### {item-id}
    - **Status:** PASS / FAIL / BLOCKED
    - **Evidence:** {path ke log/report + line range OR screenshot reference}
    - **Notes:** {ringkas observation}
    ```

    Plus section `### D-02 Server smoke` dengan PowerShell/DevTools output.

    Plus `## Gaps` section (kalau ada finding belum tertangani) — kosong kalau semua pass.

    **Step 4 — Decision:**

    Reply dengan salah satu:
    - `approved` — semua 5 items pass + D-02 smoke OK + 316-UAT.md committed
    - `issues: <description>` — jelaskan item yang fail + screenshot path reference + apa yang butuh follow-up (kemungkinan Plan 03 atau gap closure)
    - `blocked: <reason>` — kalau server lokal mati, DB seed corrupt, atau dependency lain
  </how-to-verify>
  <resume-signal>Type "approved" kalau D-02 smoke 302 OK + 5 UAT items pass + 316-UAT.md committed. Atau "issues: ..." dengan detail per item gagal.</resume-signal>
</task>

</tasks>

<verification>
**Plan-level checks:**

1. `docs/test-reports/{today}-assessment-matrix.md` exists dan mtime = today
2. `.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md` exists dengan 5 items verdict + D-02 smoke section
3. Total 5 D-16 UAT items mendapat verdict eksplisit (passed/issues/blocked di frontmatter total = 5)
4. D-02 server smoke documented dengan PowerShell/DevTools output (302 status + Location header captured)

**Acceptance per D-15 (final gate):**
- [ ] S5 reach `/CMP/Results/{id}` redirect (atau finding S5 bukan page-closed cascade)
- [ ] S8 [META-AllCorrect] verdict tercatat
- [ ] S9 [META-AllWrong] verdict tercatat
- [ ] S10 [META-CollectorCheck] meta finding di `## Meta-validation results` section
- [ ] Inter-scenario continue: 10 scenario execute (zero halt-on-first-fail)
</verification>

<success_criteria>
- [ ] Task 1 selesai: quick S5 + full run executed, logs captured, summary file generated
- [ ] Task 2 selesai: D-02 server smoke documented + 316-UAT.md committed dengan 5 items + verdict
- [ ] All 5 D-15 acceptance items have verdict
- [ ] Auto-capture dir naming A1 assumption verified (catat hasilnya — confirmed atau need-tune)
- [ ] Tidak ada code edit di plan ini (verification + UAT documentation only)
- [ ] Human approval received di Task 2 checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-02-SUMMARY.md` dengan:
- Staged validation results (S5 verdict, full run exit code, finding counts)
- D-02 server smoke result
- Link ke 316-UAT.md
- Wave 0 verifications (auto-capture dir naming A1 outcome)
- Recommendation: phase 316 close OR butuh Plan 03 (gap closure)
</output>
