---
phase: 316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso
plan: 04
type: execute
wave: 1
depends_on: ["316-01", "316-02", "316-03"]
files_modified:
  - tests/e2e/helpers/examMatrix.ts
  - tests/e2e/helpers/matrixReport.ts
autonomous: true
gap_closure: true
requirements: [GAP-316-1, GAP-316-2]
tags: [playwright, e2e, gap-closure, regex, soft-assert, page-closed]

must_haves:
  truths:
    - "Helper waitForURL regex tolerant terhadap `/CMP/Results/{id}` DAN `/CMP/ExamSummary/{id}` (D-02 server smoke evidence — incomplete-answers branch valid)"
    - "softAssert catch handler detect page-closed cascade — promote severity='major' yang error contain 'closed'/'Test ended' ke SkipScenarioError (skip sisa step scenario, bypass accumulator timing)"
    - "Defensive screenshot SKIP saat page-closed cascade detected (analog dengan existing isClosed check, tapi sebelum await screenshot fire)"
    - "Existing softAssert behavior preserved untuk error yang BUKAN page-closed (severity='major' tetap continue, record finding normal)"
    - "Existing SkipScenarioError re-throw branch preserved (line 211-213 matrixReport.ts) — no regression"
    - "TypeScript compile clean (`npx tsc --noEmit` exit 0)"
    - "Helper signature unchanged (takeExam, runDiscoveryScenario, softAssert) — surgical preserve"
  artifacts:
    - path: "tests/e2e/helpers/examMatrix.ts"
      provides: "Submit-exam helper dengan regex widened ke (Results|ExamSummary)"
      contains: "/\\/CMP\\/(Results|ExamSummary)\\/\\d+/"
    - path: "tests/e2e/helpers/matrixReport.ts"
      provides: "softAssert catch handler dengan page-closed cascade promotion logic"
      contains: "page-closed cascade"
  key_links:
    - from: "examMatrix.ts:182 submit-exam softAssert callback"
      to: "Promise.all waitForURL regex"
      via: "regex pattern widen literal"
      pattern: "Results\\|ExamSummary"
    - from: "matrixReport.ts:206 softAssert catch entry"
      to: "SkipScenarioError throw (cascade promotion)"
      via: "page-closed error detection branch (isPageClosed check)"
      pattern: "isPageClosed"
    - from: "matrixReport.ts cascade branch"
      to: "collector.record severity='critical' (promoted from major)"
      via: "screenshotPath=undefined (skip page.screenshot defensive)"
      pattern: "severity: 'critical'"
---

<objective>
Wave 1 surgical helper edits untuk close GAP-316-1 (submit URL regex too narrow) + GAP-316-2 root cause (page-closed cascade accumulator timing). 2 file edits parallel-safe (no overlap dengan Plan 05 file scope). Total LOC delta: ~11-16 LOC (1 regex + 10-15 cascade catch branch).

**Purpose:** Selesaikan masalah arsitektural Plan 02 — saat MA/Essay step page closed mid-action, `page.check()` retry sampai 240s (test timeout). Plan 04 promote ke critical+SkipScenarioError supaya scenario abort fast (<10s). Plus regex widen supaya S5 submit tolerant ke incomplete-answers redirect.

**Output:** 2 helper file modified, TypeScript clean, existing tests behavior preserved untuk non-cascade path. Plan 05+06 verify E2E.

**Critical sequencing:** Plan 04 MUST land BEFORE Plan 05 (T2 cascade catch hilangkan accumulator timing — tanpa T2, describe restructure di Plan 05 tetap akan hit 240s per scenario karena page.check retry; even isolated describe akan slow). Per 316-RESEARCH-GAP-316-2.md line 188-198 — defense in depth ordering.

**Wave 0 dependency:** Plan 03 A2 outcome — Plan 04 lanjut TERLEPAS dari A2 valid/invalid (T1 + T2 surgical fix tidak depend describe restructure). Jika A2 INVALID, Plan 05 skip tapi Plan 04 tetap exec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-PATTERNS.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH-GAP-316-2.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-02-SUMMARY.md
@tests/e2e/helpers/examMatrix.ts
@tests/e2e/helpers/matrixReport.ts
@CLAUDE.md

<interfaces>
<!-- Key types + exports yang diperlukan executor. Sumber: grep tests/e2e/helpers/ -->

From `tests/e2e/helpers/matrixTypes.ts`:
```typescript
export type Severity = 'critical' | 'major' | 'minor';
export interface ScenarioConfig {
  id: number;
  title: string;
  category: string;
  scheduleDate: string;
  questions: QuestionConfig[];
  hasEssay: boolean;
  sessionIdPeserta1: number;
  sessionIdPeserta2: number;
}
export interface Finding {
  scenarioId: number;
  scenarioTitle: string;
  step: string;
  expected: string;
  actual: string;
  screenshotPath?: string;
  severity: Severity;
  isMeta?: boolean;
}
```

From `tests/e2e/helpers/matrixReport.ts` (existing exports — DO NOT change signatures):
```typescript
export class SkipScenarioError extends Error { /* tag class */ }
export const collector: Collector;
export async function softAssert<T>(
  ctx: { scenario: ScenarioConfig; step: string; severity: Severity; page: Page; isMeta?: boolean },
  fn: () => Promise<T>,
  expected: string
): Promise<T | null>;
```

From `tests/e2e/helpers/examMatrix.ts` (existing exports):
```typescript
export async function takeExam(page: Page, cfg: ScenarioConfig, peserta: AccountKey, sessionId: number, options?: { sabotageOneAnswer?: boolean }): Promise<void>;
export async function gradeEssaysAsHc(pageHc: Page, cfg: ScenarioConfig): Promise<void>;
export async function verifyResultPage(page: Page, cfg: ScenarioConfig, peserta: AccountKey): Promise<void>;
export { SkipScenarioError };
```

**Critical existing pattern (matrixReport.ts:206-213) — Phase 316 Plan 01 amendment:**
```typescript
} catch (e: unknown) {
  // Phase 316: re-throw SkipScenarioError tanpa record finding — helper sudah signal
  // explicit skip (page-closed cascade abort).
  if (e instanceof SkipScenarioError) {
    throw e;
  }
  // ... rest of catch
}
```

**Cascade promotion branch (NEW Plan 04 T2) — placement AFTER existing re-throw, BEFORE existing severity logic.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen submit-exam waitForURL regex (GAP-316-1)</name>
  <files>tests/e2e/helpers/examMatrix.ts</files>
  <action>
**Single-line edit di `tests/e2e/helpers/examMatrix.ts:182`.**

**Sebelum (current line 182):**
```typescript
      await Promise.all([
        page.waitForURL(/\/CMP\/Results\/\d+/, { timeout: 15_000 }),
        page.click('#reviewSubmitBtn, [type="submit"]:not(.btn-cancel)'),
      ]);
```

**Sesudah:**
```typescript
      await Promise.all([
        // Phase 316 Plan 04 (GAP-316-1): widen regex accept `/CMP/ExamSummary/{id}` —
        // server-side incomplete-answers branch (Controllers/CMPController.cs:1630)
        // redirect ke ExamSummary saat answeredCount < totalQuestions. D-02 server smoke
        // (316-UAT.md) confirm BOTH paths valid 302 dari SubmitExam endpoint.
        page.waitForURL(/\/CMP\/(Results|ExamSummary)\/\d+/, { timeout: 15_000 }),
        page.click('#reviewSubmitBtn, [type="submit"]:not(.btn-cancel)'),
      ]);
```

**Update juga JSDoc string di softAssert call (line 186) untuk reflect both paths:**

**Sebelum (line 186):**
```typescript
    'SubmitExam redirects to /CMP/Results/{id}'
```

**Sesudah:**
```typescript
    'SubmitExam redirects to /CMP/Results/{id} OR /CMP/ExamSummary/{id} (incomplete-answers branch)'
```

**Update JSDoc header takeExam (line 44-49) — append note Phase 316 Plan 04 fix:**

```typescript
 * Phase 316 fix:
 * - Submit click memakai `Promise.all([waitForURL, click])` race-tolerant
 *   (precedent exam313.ts:107). Eliminate "Target page, context or browser has been closed"
 *   regression dari Phase 315 smoke run 2026-05-11T06:14:36Z.
 * - `page.isClosed()` gate di awal setiap softAssert callback (MC/MA/Essay) — throw
 *   SkipScenarioError langsung saat page closed mid-loop. Cegah cascade-fail noise di report.
 * - **Plan 04 (gap closure):** waitForURL regex widen ke `(Results|ExamSummary)` — tolerant
 *   terhadap server-side incomplete-answers branch (Controllers/CMPController.cs:1630)
 *   yang redirect ke ExamSummary saat helper takeExam tidak sukses answer semua soal
 *   sebelum submit (page-closed cascade, mc selector timeout, dll). Server BERPERILAKU
 *   BENAR (D-02 smoke verified) — bug ada di helper regex yang terlalu sempit.
 *
```

**Constraint:**
- ZERO edit di file lain (matrixReport.ts edit di Task 2 terpisah)
- Helper signature unchanged (`takeExam` argument list IDENTIK)
- TS clean: `cd tests && npx tsc --noEmit` exit 0
- LOC delta: ~1 LOC regex + ~6 LOC JSDoc comment = ~7 LOC net change

**Verification step inline:**
```bash
cd tests && npx tsc --noEmit
grep -c "Results\\|ExamSummary" e2e/helpers/examMatrix.ts  # expect ≥2 (regex + JSDoc)
grep -c "Promise.all" e2e/helpers/examMatrix.ts  # expect ≥1 (preserved)
```
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit && grep -qE "Results\\|ExamSummary" e2e/helpers/examMatrix.ts && grep -qE "Promise\\.all" e2e/helpers/examMatrix.ts && echo "T1_OK"</automated>
  </verify>
  <done>`examMatrix.ts:182` regex `/\/CMP\/(Results|ExamSummary)\/\d+/`. JSDoc header + softAssert expected string mention both paths. TS compile clean. Helper signatures unchanged. Git diff hanya touch examMatrix.ts (no other files).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: softAssert page-closed cascade promotion (GAP-316-2 a-revised)</name>
  <files>tests/e2e/helpers/matrixReport.ts</files>
  <behavior>
    - Test 1: softAssert dengan severity='major' + error message contain "Target page closed" → throws SkipScenarioError (cascade promotion). Collector receives 1 finding dengan severity='critical' + screenshotPath=undefined.
    - Test 2: softAssert dengan severity='major' + error message contain "Test ended" → throws SkipScenarioError. Same behavior.
    - Test 3: softAssert dengan severity='major' + page.isClosed() === true (TIDAK perlu error message match) → throws SkipScenarioError.
    - Test 4: softAssert dengan severity='major' + error generic ("validation failed") → existing behavior preserved (record finding severity='major' + return null, NO throw).
    - Test 5: softAssert dengan severity='critical' + error generic → existing behavior (record + throw SkipScenarioError). Cascade branch tidak trigger karena severity already critical.
    - Test 6: softAssert dengan e instanceof SkipScenarioError → existing re-throw preserved (no record, bubble).

    **Test scaffold STATUS:** MISSING — phase 315/316 tidak punya unit test untuk softAssert standalone. **Decision:** Skip unit test untuk Plan 04 (consistent dengan Plan 01 pattern — TIDAK ada softAssert unit test ditulis). Verify via E2E full run di Plan 06 sebagai integration test. Behavior table di atas adalah informal spec untuk reviewer.
  </behavior>
  <action>
**Edit `tests/e2e/helpers/matrixReport.ts` catch block softAssert (line 206-248).**

**Sebelum (current line 206-248 — preserved structure):**

```typescript
  } catch (e: unknown) {
    // Phase 316: re-throw SkipScenarioError tanpa record finding — helper sudah signal
    // explicit skip (page-closed cascade abort).
    if (e instanceof SkipScenarioError) {
      throw e;
    }

    const err = e as { message?: string };
    const stepSlug = ctx.step.replace(/\s+/g, '-').replace(/[^a-zA-Z0-9-]/g, '');
    const candidatePath = `test-results/matrix-s${ctx.scenario.id}-${stepSlug}.png`;

    // Phase 316: defensive screenshot — page-closed pre-check + try/catch.
    let screenshotPath: string | undefined;
    if (!ctx.page.isClosed()) {
      try {
        await ctx.page.screenshot({ path: candidatePath, fullPage: true });
        screenshotPath = candidatePath;
      } catch {
        // ...
      }
    }

    await collector.record({
      scenarioId: ctx.scenario.id,
      // ... full record
    });

    if (ctx.severity === 'critical') {
      throw new SkipScenarioError(`Critical at ${ctx.step}: ${err?.message ?? String(e)}`);
    }
    return null;
  }
```

**Sesudah — INSERT cascade promotion branch SETELAH SkipScenarioError re-throw, SEBELUM existing screenshot/record logic:**

```typescript
  } catch (e: unknown) {
    // Phase 316: re-throw SkipScenarioError tanpa record finding — helper sudah signal
    // explicit skip (page-closed cascade abort). Tanpa branch ini, isClosed gate akan
    // di-swallow oleh severity='major' continue-logic → cascade noise tetap muncul.
    // Ref: 316-RESEARCH.md Pitfall 5 (line 400-407) + Open Question 2 (line 486-489).
    if (e instanceof SkipScenarioError) {
      throw e;
    }

    const err = e as { message?: string };
    const errMsg = err?.message ?? String(e);

    // === Phase 316 Plan 04 (GAP-316-2 a-revised): page-closed cascade promotion ===
    // Saat severity='major' tapi error message contain "closed"/"Test ended" ATAU
    // ctx.page.isClosed() === true, page sudah dead — sisa step scenario akan retry-
    // then-timeout (page.check accumulator → 240s test timeout hit). Promote langsung
    // ke SkipScenarioError (analog severity='critical' branch bawah, tapi):
    //   1. Skip page.screenshot defensive capture (page closed = screenshot akan timeout)
    //   2. Record finding dengan severity='critical' (promoted) + screenshotPath=undefined
    //   3. Throw SkipScenarioError → bubble ke runDiscoveryScenario catch → scenario abort
    //
    // Edge case: kalau MA step REAL bug page-closed (BUKAN cascade dari step sebelumnya)
    // → still skip → masuk report sebagai critical finding di MA step. Acceptable — report
    // tetap punya signal. Ref: 316-RESEARCH-GAP-316-2.md line 287-292.
    const isPageClosedError = /closed|Test ended/i.test(errMsg) || ctx.page.isClosed();
    if (isPageClosedError && ctx.severity === 'major') {
      await collector.record({
        scenarioId: ctx.scenario.id,
        scenarioTitle: ctx.scenario.title,
        step: ctx.step,
        expected,
        actual: errMsg,
        screenshotPath: undefined,  // page closed — skip screenshot capture
        severity: 'critical',  // promoted from 'major' (page-closed cascade)
        isMeta: ctx.isMeta,
      });
      throw new SkipScenarioError(
        `page-closed cascade at ${ctx.step}: ${errMsg}`
      );
    }
    // === End Phase 316 Plan 04 cascade promotion ===

    const stepSlug = ctx.step.replace(/\s+/g, '-').replace(/[^a-zA-Z0-9-]/g, '');
    const candidatePath = `test-results/matrix-s${ctx.scenario.id}-${stepSlug}.png`;

    // Phase 316: defensive screenshot — page-closed pre-check + try/catch.
    // Closed → skip custom path (jangan throw, jangan retry); renderer fallback
    // ke Playwright auto-capture handle missing custom path di renderFinding.
    let screenshotPath: string | undefined;
    if (!ctx.page.isClosed()) {
      try {
        await ctx.page.screenshot({ path: candidatePath, fullPage: true });
        screenshotPath = candidatePath;
      } catch {
        // Page may have closed antara isClosed() check dan screenshot fire (microsec race).
        // Skip silent — renderer fallback handles missing custom path.
      }
    }

    await collector.record({
      scenarioId: ctx.scenario.id,
      scenarioTitle: ctx.scenario.title,
      step: ctx.step,
      expected,
      actual: errMsg,
      screenshotPath,
      severity: ctx.severity,
      isMeta: ctx.isMeta,
    });

    if (ctx.severity === 'critical') {
      throw new SkipScenarioError(`Critical at ${ctx.step}: ${errMsg}`);
    }
    return null;
  }
```

**Catatan implementasi:**
1. **Extract `errMsg` ke top of catch** — pakai 2x (cascade branch + existing logic). Cuma 1 line refactor (`const errMsg = err?.message ?? String(e);` moved up).
2. **Cascade branch placement:** SETELAH `SkipScenarioError` re-throw, SEBELUM screenshot capture. Urutan penting — screenshot capture juga butuh `ctx.page.isClosed()` check yang sudah ada di branch sendiri.
3. **`isPageClosedError` regex:** match `closed` OR `Test ended` case-insensitive. Plus `ctx.page.isClosed()` boolean fallback untuk kasus dimana error tidak punya page-closed message tapi page secara state sudah closed (e.g., context closed via test cleanup).
4. **Promote severity ke 'critical' di record** — supaya report distinguish "real major" vs "promoted-from-cascade". Visible di report `## Discovery findings` section.
5. **screenshotPath undefined** — bukan empty string. Renderer fallback (Layer 2 auto-capture) akan handle: tanpa custom path, fallback ke Playwright auto-capture dir kalau ada. Plan 01 Plan resolveScreenshotPath() sudah implement Layer 2.

**Constraint:**
- ZERO edit di file lain (Task 1 sudah commit examMatrix.ts terpisah)
- softAssert signature unchanged
- Existing branches (SkipScenarioError re-throw, severity='critical' throw) PRESERVED — no regression
- TS clean: `cd tests && npx tsc --noEmit` exit 0
- LOC delta: ~15 LOC net insert (cascade branch) + ~1 LOC refactor (errMsg extract)

**Test inline (manual):**
```bash
cd tests && npx tsc --noEmit
grep -c "page-closed cascade" e2e/helpers/matrixReport.ts  # expect ≥2 (comment + throw message)
grep -c "isPageClosedError" e2e/helpers/matrixReport.ts  # expect ≥1
grep -c "instanceof SkipScenarioError" e2e/helpers/matrixReport.ts  # expect ≥1 (preserved)
```
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit && grep -qE "isPageClosedError" e2e/helpers/matrixReport.ts && grep -qE "page-closed cascade" e2e/helpers/matrixReport.ts && grep -qE "instanceof SkipScenarioError" e2e/helpers/matrixReport.ts && echo "T2_OK"</automated>
  </verify>
  <done>`matrixReport.ts` softAssert catch handler punya cascade promotion branch (line ~210-228 estimate post-edit). Existing SkipScenarioError re-throw preserved. Existing severity='critical' throw preserved. TS compile clean. Helper signature unchanged.</done>
</task>

</tasks>

<verification>
**Plan 04 Wave 1 sukses jika:**
1. `examMatrix.ts:182` regex widened ke `(Results|ExamSummary)`
2. `matrixReport.ts` softAssert punya cascade promotion branch
3. TS compile clean (`cd tests && npx tsc --noEmit` exit 0)
4. Helper signatures unchanged (grep `takeExam(`, `softAssert<T>` match existing)
5. 2 atomic commits (1 per task) untuk audit trail
6. Quick sanity run S5: `cd tests && npx playwright test assessment-matrix --grep "Scenario 5"` — eksitcode (per behavior baru), tidak ada finding "page.waitForURL: Test ended" untuk `Results/` (regex sekarang accept ExamSummary)

**Note:** Full validation E2E di Plan 06. Plan 04 cuma surgical edits.
</verification>

<success_criteria>
- 2 file modified (examMatrix.ts + matrixReport.ts), zero other files touched
- TS compile clean throughout
- Existing behavior preserved untuk non-cascade error path (regression-free)
- Helper signatures unchanged (Pattern 316 surgical principle)
- 2 atomic commits per task
</success_criteria>

<output>
After completion, create `.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-04-SUMMARY.md`
</output>
