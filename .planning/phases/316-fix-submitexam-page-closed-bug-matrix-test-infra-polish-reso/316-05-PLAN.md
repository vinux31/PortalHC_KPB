---
phase: 316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso
plan: 05
type: execute
wave: 2
depends_on: ["316-03", "316-04"]
files_modified:
  - tests/e2e/assessment-matrix.spec.ts
autonomous: true
gap_closure: true
requirements: [GAP-316-2, GAP-316-3]
tags: [playwright, e2e, gap-closure, describe-restructure, serial-halt]

must_haves:
  truths:
    - "10 test() blocks wrap dengan 10 test.describe() block terpisah (S1-S10)"
    - "Spec-level `test.describe.configure({ mode: 'serial' })` REMOVED (line 64) — `fullyParallel: false` di config preserved untuk sequential execution"
    - "Failure di describe-1 (S1) TIDAK halt describe-2..10 (S2-S10) — verified via 316-RESEARCH-GAP-316-2.md A2 + Plan 03 Wave 0 probe outcome"
    - "Sentinel S10 `test.fail()` annotation TETAP valid — annotation scope=test (bukan describe boundary). Per Playwright docs annotation."
    - "matrixReport collector singleton TIDAK race — `workers: 1` + `fullyParallel: false` jaga 1 process. Multiple describe TIDAK trigger multiple workers (Playwright spawn worker per file)"
    - "TypeScript compile clean (`npx tsc --noEmit` exit 0)"
    - "runDiscoveryScenario function signature unchanged — describe wrap pure organizational"
  artifacts:
    - path: "tests/e2e/assessment-matrix.spec.ts"
      provides: "10 test.describe() blocks tiap punya 1 test('execute'), serial spec-level config DROPPED"
      contains: "test.describe('Scenario 1"
  key_links:
    - from: "assessment-matrix.spec.ts test.describe boundary"
      to: "Playwright runner failure isolation (no halt-on-first-fail)"
      via: "fullyParallel:false di config + NO spec-level mode:'serial'"
      pattern: "test\\.describe\\('Scenario"
    - from: "Wave 0 probe outcome (316-VALIDATION.md)"
      to: "Plan 05 execution decision"
      via: "A2 VALID → execute Plan 05; A2 INVALID → SKIP Plan 05 (fallback ke Plan 04 alone)"
      pattern: "A2 VALID"
---

<objective>
Wave 2 spec restructure untuk close GAP-316-2 di Playwright runner level. Wrap 10 `test()` ke 10 `test.describe()` blocks terpisah supaya failure di S1 TIDAK halt S2-S10. Drop `test.describe.configure({ mode: 'serial' })` (spec line 64) — failure isolation berasal dari describe boundary di mode `fullyParallel: false`, BUKAN serial mode.

**Purpose:** Plan 04 sudah fix cascade timing (catch fast <60s), tapi kalau ada scenario yang masih surface novel page-closed pattern + hit 240s, current serial mode masih halt subsequent. Plan 05 = defense in depth — failure boundary per describe → S2-S10 tetap run terlepas dari S1 outcome.

**Output:** 1 file modified (`tests/e2e/assessment-matrix.spec.ts`) dengan ~30 LOC restructure (mechanical wrap). Existing logic semua preserved — pure organizational change.

**Critical dependency:** Plan 03 Wave 0 A2 VALID. Jika A2 INVALID (probe shows "1 failed, 1 did not run"), SKIP Plan 05 dan langsung ke Plan 06 dengan fallback path (Plan 04 alone). Decision di-read dari `316-VALIDATION.md` Plan 03 Wave 0 section.

**Wave 0 dependency gate:**
```bash
grep -A3 "Plan 03 Wave 0" .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md | grep -E "A2 VALID|A2 INVALID"
```
- Jika "A2 VALID" → proceed
- Jika "A2 INVALID" → halt Plan 05, lapor ke user, lanjut Plan 06 dengan note "Plan 05 skipped per A2 fallback"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH-GAP-316-2.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-04-SUMMARY.md
@tests/e2e/assessment-matrix.spec.ts
@tests/playwright.config.ts
@CLAUDE.md

<interfaces>
<!-- Spec structure invariants — DO NOT break -->

**Existing helper imports (preserve):**
```typescript
import { test, type Browser } from '@playwright/test';
import { readFileSync } from 'fs';
import { resolve } from 'path';
import type { ScenarioConfig } from './helpers/matrixTypes';
import { takeExam, gradeEssaysAsHc, verifyResultPage } from './helpers/examMatrix';
import { softAssert, SkipScenarioError } from './helpers/matrixReport';
```

**Existing module-scope state (preserve):**
```typescript
const SCENARIO_TIMEOUT_MS = 240_000;
const STATE_FILE = resolve(__dirname, '..', '.matrix-state.json');
let state: MatrixState;
// ... try/catch read state file
function getScenario(id: number): ScenarioConfig;
async function runDiscoveryScenario(browser: Browser, cfg: ScenarioConfig): Promise<void>;
```

**Existing test() block structure (CURRENT — 10 blocks flat):**
```typescript
test('Scenario 1: S1 Manual Mixed (MC + MA + Essay)', async ({ browser }) => {
  test.setTimeout(SCENARIO_TIMEOUT_MS);
  await runDiscoveryScenario(browser, getScenario(1));
});
// ... 9 more test() blocks
```

**TARGET structure (POST Plan 05):**
```typescript
test.describe('Scenario 1: S1 Manual Mixed', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(1));
  });
});
```

**Special sentinels (S8, S9, S10) — preserve inline body:**
- S8: inline takeExam + gradeEssaysAsHc + sentinel softAssert (NOT runDiscoveryScenario)
- S9: cfgAllWrong clone + inline body
- S10: `test.fail()` annotation + inline force-fail softAssert

**Annotation scope confirmation:** `test.fail()` annotation berada di BODY function (line 376), bukan outer form. Scope = test (NOT describe). Per Playwright docs https://playwright.dev/docs/test-annotations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 gate check — confirm A2 outcome sebelum restructure</name>
  <files>(read-only — 316-VALIDATION.md)</files>
  <action>
**Pre-execution gate:** Verifikasi Plan 03 Wave 0 A2 outcome valid.

```bash
# Step 1: grep VALIDATION.md untuk Wave 0 A2 result
A2_OUTCOME=$(grep -A5 "Plan 03 Wave 0" .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md | grep -oE "A2 (VALID|INVALID)" | head -1)
echo "A2 outcome: $A2_OUTCOME"

# Step 2: branch logic
if [ "$A2_OUTCOME" = "A2 VALID" ]; then
  echo "GATE PASS — proceeding Task 2 (describe restructure)"
elif [ "$A2_OUTCOME" = "A2 INVALID" ]; then
  echo "GATE FAIL — A2 INVALID, Plan 05 SKIPPED per fallback path"
  echo "Append decision note to VALIDATION.md and exit"
  exit 1
else
  echo "GATE ERROR — A2 outcome tidak ditemukan di VALIDATION.md. Jalankan Plan 03 dulu."
  exit 1
fi
```

**Jika gate FAIL (A2 INVALID):**
- Append catatan ke `316-VALIDATION.md`:
  ```markdown
  ### Plan 05 Skipped (2026-05-11)
  - **Reason:** Wave 0 A2 probe outcome = INVALID
  - **Decision:** Fallback path — Plan 04 (regex widen + cascade catch) saja
  - **Implication:** Full S1-S10 run mungkin masih halt di first-fail jika ada scenario yang surface novel page-closed pattern yang Plan 04 cascade catch tidak detect
  ```
- Commit VALIDATION.md update
- **EXIT Plan 05 dengan status "skipped — fallback path"** — Plan 06 baca status ini dan adjust validation expectations.

**Jika gate PASS (A2 VALID):** Lanjut Task 2.
  </action>
  <verify>
    <automated>grep -qE "Plan 03 Wave 0|A2 (VALID|INVALID)" .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md && echo "GATE_CHECK_OK"</automated>
  </verify>
  <done>Wave 0 A2 outcome di-confirm dari VALIDATION.md. Jika VALID: Task 2 lanjut. Jika INVALID: Plan 05 marked skipped, commit VALIDATION.md note, exit.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap 10 test() ke 10 test.describe() + drop spec-level serial config</name>
  <files>tests/e2e/assessment-matrix.spec.ts</files>
  <action>
**Edit `tests/e2e/assessment-matrix.spec.ts`. Hanya touch 3 area: line 60-71 (spec config), line 180-218 (S1-S7 discovery tests), line 228-409 (S8-S10 sentinel tests).**

**Step 1 — Hapus spec-level serial config (line 60-65):**

**Sebelum:**
```typescript
// Serial mode — singleton `collector` di matrixReport.ts + sequential scenario execution
// (RESEARCH.md § Pattern 3 line 573). fullyParallel=false sudah di playwright.config.ts:7
// tapi konfirmasi di sini agar describe behavior eksplisit.
test.describe.configure({ mode: 'serial' });
```

**Sesudah:**
```typescript
// Phase 316 Plan 05 (GAP-316-2): spec-level `test.describe.configure({mode:'serial'})`
// DROPPED. Sequential execution preserved via `fullyParallel: false` + `workers: 1`
// di playwright.config.ts. Failure isolation per describe boundary — failure di S1
// TIDAK halt S2-S10 (Plan 03 Wave 0 A2 verified).
//
// Singleton collector matrixReport.ts:189 TETAP safe — Playwright spawn worker per FILE,
// bukan per describe. 1 worker = 1 process = 1 collector instance (file-system NDJSON
// per worker via matrixReport.ts:42-185, multi-worker safe by design walaupun di sini
// kita pakai 1 worker).
```

**Step 2 — S1-S4 (Group 1 — line 180-198) wrap:**

**Sebelum:**
```typescript
test('Scenario 1: S1 Manual Mixed (MC + MA + Essay)', async ({ browser }) => {
  test.setTimeout(SCENARIO_TIMEOUT_MS);
  await runDiscoveryScenario(browser, getScenario(1));
});

test('Scenario 2: S2 Online Mixed (MC + MA + Essay)', async ({ browser }) => {
  test.setTimeout(SCENARIO_TIMEOUT_MS);
  await runDiscoveryScenario(browser, getScenario(2));
});

test('Scenario 3: S3 PreTest Mixed (MC + MA + Essay)', async ({ browser }) => {
  test.setTimeout(SCENARIO_TIMEOUT_MS);
  await runDiscoveryScenario(browser, getScenario(3));
});

test('Scenario 4: S4 PostTest Mixed (MC + MA + Essay)', async ({ browser }) => {
  test.setTimeout(SCENARIO_TIMEOUT_MS);
  await runDiscoveryScenario(browser, getScenario(4));
});
```

**Sesudah:**
```typescript
test.describe('Scenario 1: S1 Manual Mixed (MC + MA + Essay)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(1));
  });
});

test.describe('Scenario 2: S2 Online Mixed (MC + MA + Essay)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(2));
  });
});

test.describe('Scenario 3: S3 PreTest Mixed (MC + MA + Essay)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(3));
  });
});

test.describe('Scenario 4: S4 PostTest Mixed (MC + MA + Essay)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(4));
  });
});
```

**Step 3 — S5-S7 (Group 2 — line 206-218) wrap identical pattern:**

```typescript
test.describe('Scenario 5: S5 Online MC only (3 MultipleChoice questions)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(5));
  });
});

test.describe('Scenario 6: S6 Online MA only (3 MultipleAnswer questions)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(6));
  });
});

test.describe('Scenario 7: S7 Online Essay only (3 Essay questions)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    await runDiscoveryScenario(browser, getScenario(7));
  });
});
```

**Step 4 — S8 (line 228-280) wrap. Inner body PRESERVE:**

**Sebelum:**
```typescript
test('Scenario 8: META-AllCorrect Sentinel (expect 100% score)', async ({ browser }) => {
  test.setTimeout(SCENARIO_TIMEOUT_MS);
  // [META-AllCorrect]: peserta1 jawab semua benar (no sabotage), HC grading penuh.
  // ... (entire body)
  const cfg = getScenario(8);
  // ... newContext, takeExam, gradeEssaysAsHc, softAssert sentinel, try/catch/finally
});
```

**Sesudah:**
```typescript
test.describe('Scenario 8: META-AllCorrect Sentinel (expect 100% score)', () => {
  test('execute', async ({ browser }) => {
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    // [META-AllCorrect]: peserta1 jawab semua benar (no sabotage), HC grading penuh.
    // ... (entire body PRESERVED — semua softAssert + try/catch/finally tetap di dalam test('execute'))
    const cfg = getScenario(8);
    // ... rest identical to existing
  });
});
```

**Step 5 — S9 (line 282-365) wrap dengan pattern sama. PRESERVE inner cfgAllWrong + try/catch body.**

**Step 6 — S10 (line 375-409) wrap. `test.fail()` annotation TETAP di dalam test body (inner form), TIDAK di-move ke describe:**

**Sebelum:**
```typescript
test('Scenario 10: META-CollectorCheck Sentinel (sengaja gagal — verifikasi collector working)', async ({ browser }) => {
  test.fail(); // expected-failure annotation
  test.setTimeout(SCENARIO_TIMEOUT_MS);
  const cfg = getScenario(10);
  // ... force-fail body
});
```

**Sesudah:**
```typescript
test.describe('Scenario 10: META-CollectorCheck Sentinel (sengaja gagal — verifikasi collector working)', () => {
  test('execute', async ({ browser }) => {
    test.fail(); // expected-failure annotation — scope=test (bukan describe). Per Playwright docs https://playwright.dev/docs/test-annotations annotation valid di any describe context.
    test.setTimeout(SCENARIO_TIMEOUT_MS);
    const cfg = getScenario(10);
    // ... force-fail body PRESERVED
  });
});
```

**Implementation constraints:**

1. **NO change ke runDiscoveryScenario function signature.** Pure organizational wrap.
2. **NO change ke spec imports, module-scope state (SCENARIO_TIMEOUT_MS, STATE_FILE, getScenario).**
3. **NO change ke S8/S9/S10 inner body** — semua softAssert call, try/catch/finally, ctx.close logic PRESERVE 100% literal.
4. **Test name di within-describe = 'execute'** untuk semua 10 blocks (sederhana, konsisten). Full scenario name di describe.
5. **`test.fail()` placement S10** — INSIDE test body (line ~377 estimate post-restructure), bukan di describe block.

**File output structure (estimate):**
- Line 1-99: unchanged (imports + JSDoc + state file read)
- Line ~60-67: drop describe.configure({mode:'serial'}) + comment update (~6 line)
- Line ~180-219: S1-S7 7 describe wraps (each: open describe → test → setTimeout → runDiscovery → close) = ~7 × 5 = 35 LOC restructure
- Line ~228-280: S8 describe wrap (~3 LOC add — outer describe block, indent body)
- Line ~282-365: S9 describe wrap (~3 LOC add)
- Line ~375-409: S10 describe wrap (~3 LOC add)
- **Net LOC delta:** ~30-35 LOC structural change (mostly indent + open/close braces)

**Verification:**
```bash
cd tests && npx tsc --noEmit
grep -c "test.describe('Scenario" e2e/assessment-matrix.spec.ts  # expect 10
grep -c "test.describe.configure" e2e/assessment-matrix.spec.ts  # expect 0 (dropped)
grep -c "test.setTimeout(SCENARIO_TIMEOUT_MS)" e2e/assessment-matrix.spec.ts  # expect 10 (preserved per-test)
grep -c "test.fail()" e2e/assessment-matrix.spec.ts  # expect 1 (S10 preserved inside test body)
```

**Quick S5 sanity run:**
```bash
cd tests && npx playwright test assessment-matrix --grep "Scenario 5"
# Expect: 1 test executed (di dalam describe "Scenario 5"), test name = "execute"
# Behavior: same as before — quick run S5 isolated.
# Log output format akan beda — Playwright list reporter akan show "Scenario 5 > execute" path
```
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit && grep -c "test\\.describe('Scenario" e2e/assessment-matrix.spec.ts | grep -q "^10$" && ! grep -q "test\\.describe\\.configure" e2e/assessment-matrix.spec.ts && grep -c "test\\.setTimeout(SCENARIO_TIMEOUT_MS)" e2e/assessment-matrix.spec.ts | grep -q "^10$" && echo "T2_RESTRUCTURE_OK"</automated>
  </verify>
  <done>10 test.describe() blocks (S1-S10), tiap punya 1 test('execute'). `test.describe.configure({mode:'serial'})` DROPPED. S10 `test.fail()` annotation preserved INSIDE test body. TS clean. Helper signatures dan inner test bodies (S8/S9/S10 sentinel logic) preserve literal.</done>
</task>

</tasks>

<verification>
**Plan 05 Wave 2 sukses jika (A2 VALID path):**
1. `assessment-matrix.spec.ts` punya 10 `test.describe('Scenario N: ...')` blocks
2. Spec-level `test.describe.configure({mode:'serial'})` dropped
3. S10 `test.fail()` annotation tetap di dalam test body (preserve sentinel semantic)
4. TS compile clean (`cd tests && npx tsc --noEmit` exit 0)
5. Quick S5 run: `cd tests && npx playwright test assessment-matrix --grep "Scenario 5"` — exit 0 atau exit 1 (behavior same as Plan 04 — boundary test bahwa runner pick up describe)
6. Inner test body S8/S9/S10 preserve LITERAL (cfg, softAssert, ctx.close all unchanged)

**Plan 05 skipped (A2 INVALID path):**
1. VALIDATION.md punya "Plan 05 Skipped" decision note
2. assessment-matrix.spec.ts unchanged dari Plan 04 state
3. Plan 06 lanjut dengan fallback expectation

**Note:** Full S1-S10 validation di Plan 06 — D-15 acceptance criteria final eligible setelah Plan 05+06 sukses.
</verification>

<success_criteria>
- 1 file modified (assessment-matrix.spec.ts), zero other files touched
- 10 describe blocks symmetric structure
- No regression di S8/S9/S10 sentinel semantic
- TS compile clean
- Atomic commit untuk audit trail
</success_criteria>

<output>
After completion, create `.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-05-SUMMARY.md`
</output>
