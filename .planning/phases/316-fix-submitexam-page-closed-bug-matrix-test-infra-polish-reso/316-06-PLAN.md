---
phase: 316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso
plan: 06
type: execute
wave: 3
depends_on: ["316-04", "316-05"]
files_modified:
  - tests/playwright.config.ts
  - tests/e2e/_throwaway-probe.spec.ts
  - .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md
  - docs/test-reports/2026-05-11-assessment-matrix.md
autonomous: true
gap_closure: true
requirements: [GAP-316-1, GAP-316-2, GAP-316-3]
tags: [playwright, e2e, gap-closure, action-timeout, validation, uat-update]

must_haves:
  truths:
    - "Optional T4 — `actionTimeout: 10_000` di playwright.config.ts:11 jadi 3rd-line defense (bound retry untuk page.{check,click,fill})"
    - "Full S1-S10 run executed — 10 tests run (NOT 9 did not run). Per-scenario wallclock <60s untuk fail-fast scenarios (cascade catch bekerja)"
    - "316-UAT.md re-verdict: 5 D-16 items + D-02 = 5/5 PASS atau eligible-after-Plan-04+05"
    - "Plan 03 throwaway probe spec DELETED — tidak ada residual test infrastructure"
    - "Report `docs/test-reports/2026-05-11-assessment-matrix.md` regenerated post full run dengan `## Meta-validation results` section non-empty (minimal S10 sentinel finding)"
    - "Exit code rationality — S10 test.fail() satisfied (expected fail), exit 0 jika no unexpected pass dari sentinel collector silent break"
  artifacts:
    - path: "tests/playwright.config.ts"
      provides: "actionTimeout: 10_000 inserted (T4 optional defense)"
      contains: "actionTimeout"
    - path: ".planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md"
      provides: "Re-verdict 5 D-16 items pasca Plan 04+05 + D-02 closure"
      contains: "fix-validate-S5"
    - path: "docs/test-reports/2026-05-11-assessment-matrix.md"
      provides: "Full S1-S10 run report — `## Meta-validation results` non-empty"
      contains: "Meta-validation results"
  key_links:
    - from: "tests/playwright.config.ts actionTimeout"
      to: "page.check retry bound (Phase 316 GAP-316-2 defense)"
      via: "playwright.config.ts use.actionTimeout key"
      pattern: "actionTimeout: 10_000"
    - from: "Full S1-S10 run output"
      to: "316-UAT.md re-verdict per D-16"
      via: "playwright stdout 'X passed, Y failed, Z did not run' parse + report file regenerate"
      pattern: "(passed|failed|did not run)"
    - from: "Report `## Meta-validation results`"
      to: "316-UAT.md sentinel-meta-verify verdict"
      via: "grep section non-empty + S10 finding present"
      pattern: "META-CollectorCheck"
---

<objective>
Wave 3 final close untuk Phase 316 — (1) optional T4 add `actionTimeout: 10_000` sebagai 3rd-line defense, (2) cleanup Plan 03 throwaway probe spec, (3) execute full S1-S10 validation run, (4) update 316-UAT.md re-verdict, (5) commit final artifacts. D-15 acceptance criteria evaluated final.

**Purpose:** Verify Plan 04+05 (atau Plan 04 alone jika A2 INVALID fallback) deliver D-15 acceptance criteria. Plus close phase 316 dengan clean state — no residual throwaway files, UAT verdict honest, report file fresh.

**Output:**
- 1 config file modified (actionTimeout add — optional)
- 1 file deleted (throwaway probe spec cleanup)
- 1 UAT file updated (5 D-16 items re-verdict)
- 1 report file regenerated (full run output)
- 1 SUMMARY file created (316-06-SUMMARY.md)

**Critical sequencing:** Plan 06 lanjut TERLEPAS dari Plan 05 outcome (A2 valid path execute restructure, A2 invalid path skip). Validation expectations adjust based on actual deliverables.

**Run wallclock estimate:** 20-30 min validation runs (1 quick S5 + 1 full S1-S10) + 10-15 min UAT write + 5 min cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-CONTEXT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-RESEARCH-GAP-316-2.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-VALIDATION.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-04-SUMMARY.md
@.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-05-SUMMARY.md
@tests/playwright.config.ts
@CLAUDE.md

<interfaces>
**Existing playwright.config.ts (no breaking change):**
```typescript
export default defineConfig({
  testDir: './e2e',
  globalTeardown: require.resolve('./e2e/global.teardown.ts'),
  timeout: 60_000,
  expect: { timeout: 10_000 },
  fullyParallel: false,
  retries: 0,
  reporter: [['html', { open: 'never' }], ['list']],
  use: {
    baseURL: 'http://localhost:5277',
    screenshot: 'on',
    trace: 'on-first-retry',
    viewport: { width: 1280, height: 720 },
  },
  // ...
});
```

**TARGET add `actionTimeout: 10_000` di `use` section (immediately after baseURL):**
```typescript
  use: {
    baseURL: 'http://localhost:5277',
    actionTimeout: 10_000,  // Phase 316 Plan 06 (GAP-316-2 defense) — bound retry untuk page.{check,click,fill}
    screenshot: 'on',
    // ...
  },
```

**D-15 acceptance criteria mapping (per 316-CONTEXT.md line 43-48):**
1. S5 critical reach `/CMP/Results/{id}` redirect → eligible after Plan 04 T1 (regex widen)
2. S8 [META-AllCorrect] score 100/100 → eligible after Plan 04+05 (S2-S8 must run, requires cascade catch + describe boundary)
3. S9 [META-AllWrong] score 0/0 → eligible after Plan 04+05 (S9 must run)
4. S10 [META-CollectorCheck] meta finding tercatat → eligible after Plan 04+05 (S10 must run)
5. Inter-scenario continue: S1 fail tidak halt S2-S10 → eligible after Plan 05 (describe boundary isolation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optional T4 — add actionTimeout: 10_000 di playwright.config.ts</name>
  <files>tests/playwright.config.ts</files>
  <action>
**Single-line insert di `tests/playwright.config.ts`.**

Insert `actionTimeout: 10_000` di `use` section, immediately after `baseURL`:

**Sebelum (current line 11-16):**
```typescript
  use: {
    baseURL: 'http://localhost:5277',
    screenshot: 'on',
    trace: 'on-first-retry',
    viewport: { width: 1280, height: 720 },
  },
```

**Sesudah:**
```typescript
  use: {
    baseURL: 'http://localhost:5277',
    // Phase 316 Plan 06 (GAP-316-2 defense-in-depth): bound retry untuk page.{check,click,fill}
    // actions ke 10s. Tanpa setting ini, default = test-level timeout (240s) → 1 hung action
    // bisa akumulate seluruh budget. Plus Plan 04 cascade catch sebagai 1st defense.
    // Tradeoff: 10s per action vs 240s per test. SignalR negotiate typically <2s acceptable.
    actionTimeout: 10_000,
    screenshot: 'on',
    trace: 'on-first-retry',
    viewport: { width: 1280, height: 720 },
  },
```

**Constraint:**
- 1 LOC insert + 4 LOC comment = 5 LOC net add
- TIDAK ubah field lain (timeout, expect.timeout, fullyParallel, retries, projects)
- TS clean (Playwright type accept `actionTimeout: number` di `use`)

**Edge case awareness:** Kalau actionTimeout terlalu agresif (10s) menyebabkan false-positive failures di legitimate slow operations (SignalR negotiate, saveIndicator update under load), bump ke 15_000 atau 20_000 di iteration nanti. Tradeoff documented di research line 374-375.

**Verification:**
```bash
cd tests && npx tsc --noEmit
grep -c "actionTimeout" playwright.config.ts  # expect ≥1
```
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit && grep -qE "actionTimeout: 10_000" playwright.config.ts && echo "T1_CONFIG_OK"</automated>
  </verify>
  <done>`actionTimeout: 10_000` ditambahkan di `use` section. TS clean. Comment header eksplisit. No other config field changed.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup throwaway probe spec + full S1-S10 validation run</name>
  <files>tests/e2e/_throwaway-probe.spec.ts, docs/test-reports/2026-05-11-assessment-matrix.md</files>
  <action>
**Step 1 — Delete throwaway probe spec (Plan 03 cleanup):**

```bash
# Delete throwaway probe — sudah verified A2 di Wave 0, no longer needed.
rm tests/e2e/_throwaway-probe.spec.ts
test ! -f tests/e2e/_throwaway-probe.spec.ts && echo "PROBE_DELETED"
```

**Step 2 — Quick S5 sanity run (post Plan 04 + Plan 05 + Plan 06 T1):**

```bash
cd tests && npx playwright test assessment-matrix --grep "Scenario 5" --reporter=list 2>&1 | tee /tmp/316-final-s5.log
echo "S5_EXIT: $?" | tee -a /tmp/316-final-s5.log
```

**Expected:**
- S5 reach `/CMP/Results/{id}` OR `/CMP/ExamSummary/{id}` (regex widened di Plan 04 T1)
- No finding "page.waitForURL: Test ended" untuk submit-exam step
- Cascade catch (Plan 04 T2) bekerja kalau ada MA/Essay fail mid-step
- Exit code: 0 atau 1 (acceptable kalau finding tercatat — verify via report file, bukan exit)

**Step 3 — Full S1-S10 validation run:**

```bash
cd tests && npx playwright test assessment-matrix --reporter=list 2>&1 | tee /tmp/316-final-full.log
echo "FULL_EXIT: $?" | tee -a /tmp/316-final-full.log
```

**Expected (A2 VALID + Plan 05 executed):**
- 10 tests run — TIDAK ada "did not run"
- Per-scenario wallclock <60s untuk fail-fast scenarios (Plan 04 cascade catch effective)
- S10 test.fail() satisfied → exit 0 jika collector working
- S8/S9 meta finding eligible — score 100/0 atau finding documents mismatch
- Report `docs/test-reports/2026-05-11-assessment-matrix.md` regenerated dengan `## Meta-validation results` section non-empty

**Expected (A2 INVALID + Plan 05 skipped fallback):**
- Mungkin masih halt jika scenario surface novel page-closed pattern Plan 04 cascade catch tidak detect
- BUT Plan 04 T1 regex widen masih effective untuk S5 submit
- Plan 04 T2 cascade catch masih membantu kalau page-closed message recognizable

**Step 4 — Parse run outcome:**

```bash
# Count execution
PASSED=$(grep -oP "\d+ passed" /tmp/316-final-full.log | head -1 | grep -oP "\d+")
FAILED=$(grep -oP "\d+ failed" /tmp/316-final-full.log | head -1 | grep -oP "\d+")
DID_NOT_RUN=$(grep -oP "\d+ did not run" /tmp/316-final-full.log | head -1 | grep -oP "\d+")
TOTAL=$((${PASSED:-0} + ${FAILED:-0} + ${DID_NOT_RUN:-0}))

echo "PASSED=$PASSED FAILED=$FAILED DID_NOT_RUN=$DID_NOT_RUN TOTAL=$TOTAL"

# Verify report meta section
grep -q "## Meta-validation results" docs/test-reports/2026-05-11-assessment-matrix.md
META_EXISTS=$?
grep -A50 "## Meta-validation results" docs/test-reports/2026-05-11-assessment-matrix.md | grep -E "CollectorCheck|AllCorrect|AllWrong" | head -5
```

**Cataat di `/tmp/316-final-summary.txt`:**

```
Phase 316 Plan 06 Final Validation Summary
==========================================
Date: 2026-05-11
S5 quick run log: /tmp/316-final-s5.log
Full run log: /tmp/316-final-full.log
Report regenerated: docs/test-reports/2026-05-11-assessment-matrix.md

Full run outcome:
  PASSED: $PASSED
  FAILED: $FAILED
  DID NOT RUN: $DID_NOT_RUN
  TOTAL: $TOTAL (expect 10)

Meta-validation section: [PRESENT | EMPTY]
S10 META-CollectorCheck finding: [PRESENT | ABSENT]
S8 META-AllCorrect finding: [PRESENT | ABSENT]
S9 META-AllWrong finding: [PRESENT | ABSENT]

D-15 acceptance evaluation:
  #1 S5 reach Results/ExamSummary: [PASS | FAIL]
  #2 S8 meta-correct: [PASS | FAIL | BLOCKED]
  #3 S9 meta-wrong: [PASS | FAIL | BLOCKED]
  #4 S10 meta-collector: [PASS | FAIL | BLOCKED]
  #5 Inter-scenario continue: [PASS | FAIL]
```

**Step 5 — Commit deletion + log capture:**

```bash
git add tests/e2e/_throwaway-probe.spec.ts  # actually `git rm`
git rm tests/e2e/_throwaway-probe.spec.ts
git add docs/test-reports/2026-05-11-assessment-matrix.md
git commit -m "test(316-06): cleanup probe + regenerate full run report

Phase 316 Plan 06 final validation:
- Throwaway probe spec deleted (Plan 03 Wave 0 cleanup)
- Full S1-S10 run executed — $TOTAL tests, $PASSED passed, $FAILED failed, $DID_NOT_RUN did not run
- Report regenerated dengan Meta-validation section [PRESENT/EMPTY]
- Logs: /tmp/316-final-s5.log + /tmp/316-final-full.log

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>test ! -f tests/e2e/_throwaway-probe.spec.ts && test -f /tmp/316-final-full.log && test -f docs/test-reports/2026-05-11-assessment-matrix.md && grep -qE "## Meta-validation results" docs/test-reports/2026-05-11-assessment-matrix.md && echo "T2_VALIDATION_OK"</automated>
  </verify>
  <done>Probe spec deleted. Quick S5 + full S1-S10 runs executed dengan logs di /tmp. Report file regenerated. Validation summary di /tmp/316-final-summary.txt dengan D-15 evaluation per item. Commit done.</done>
</task>

<task type="auto">
  <name>Task 3: Update 316-UAT.md re-verdict 5 D-16 items + close phase</name>
  <files>.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md</files>
  <action>
**Update `316-UAT.md` dengan re-verdict pasca Plan 04+05+06.**

**Step 1 — Read current state + validation summary:**

```bash
cat .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md
cat /tmp/316-final-summary.txt
```

**Step 2 — Update frontmatter:**

**Sebelum (line 1-9):**
```yaml
---
phase: 316
uat_date: 2026-05-11
total: 6
passed: 2
issues: 2
blocked: 2
notes: "Plan 01 race-tolerant submit fix + ..."
---
```

**Sesudah (post Plan 04+05+06 — example A2 VALID happy path):**
```yaml
---
phase: 316
uat_date: 2026-05-11
total: 6
passed: [actual count]
issues: [actual count]
blocked: 0  # Plan 04+05+06 close 3 carry-forward gaps (GAP-316-1/2/3)
notes: "Plan 04 (regex widen + cascade catch) + Plan 05 (describe restructure A2 VALID) + Plan 06 (actionTimeout + full validation) deliver gap closure. Full S1-S10 run executed [N passed M failed K did not run]. Meta-validation section non-empty. Sentinel S8/S9/S10 [outcomes]."
---
```

**Step 3 — Update per-item verdict** (5 D-16 items + D-02):

Update tiap section dengan post-validation evidence:

```markdown
### fix-validate-S5

- **Status:** [PASS atau ISSUES based on actual run]
- **Evidence:** /tmp/316-final-s5.log lines [N-M]; docs/test-reports/2026-05-11-assessment-matrix.md S5 section
- **Notes:** Plan 04 T1 regex widen ke `(Results|ExamSummary)` APPLIED. Submit reach [Results | ExamSummary | masih timeout]. [Detail aktual run].

### full-run-S1-S10

- **Status:** [PASS atau ISSUES based on actual run]
- **Evidence:** /tmp/316-final-full.log; report regenerated.
- **Notes:** Plan 05 describe restructure APPLIED. Output: [N passed, M failed, K did not run] dari 10 expected. [Detail per scenario].

### sentinel-meta-verify

- **Status:** [PASS atau ISSUES based on actual run]
- **Evidence:** docs/test-reports/2026-05-11-assessment-matrix.md `## Meta-validation results` section.
- **Notes:** S8/S9 [findings outcome]. S10 META-CollectorCheck [present/absent]. Collector verified [working/not working].

### continue-on-fail-E2E

- **Status:** [PASS atau ISSUES based on actual run]
- **Evidence:** /tmp/316-final-full.log "[N passed, M failed, K did not run]" summary line.
- **Notes:** Plan 05 describe restructure + Plan 04 cascade catch. Inter-scenario continue [verified/not verified].

### screenshot-path-consistency

- **Status:** PASS (preserved — Plan 01 effective + Plan 04 T2 promote screenshotPath=undefined untuk cascade)
- **Evidence:** docs/test-reports links resolve.
- **Notes:** Cascade-promoted findings (Plan 04 T2) emit screenshotPath=undefined → renderer fallback Layer 2 handle (auto-capture or omit Screenshot line).

## D-02 Server Smoke

- **Status:** PASS (preserved dari Plan 02 code inspection — server 302 + conditional Location verified)
```

**Step 4 — Update Gaps section** (clear closed gaps):

**Sebelum (line 98-111):**
```markdown
## Gaps (carry forward — perlu Plan 03 atau phase baru)

1. **GAP-316-1: Submit-exam helper waitForURL regex terlalu sempit** — ...
2. **GAP-316-2: Serial mode + per-test timeout 240s = halt-on-first-fail** — ...
3. **GAP-316-3: S8/S9/S10 sentinel belum ter-exercise** — depends on GAP-316-2.
4. **D-02 server smoke pending human action** — automation infeasible.
```

**Sesudah (post Plan 04+05+06):**
```markdown
## Gaps (closed di Plan 04+05+06)

1. **GAP-316-1: CLOSED** — Plan 04 T1 (commit [hash]). Helper waitForURL regex widened ke `(Results|ExamSummary)`. Tolerant terhadap incomplete-answers branch.

2. **GAP-316-2: [CLOSED | PARTIAL]** — Plan 04 T2 (cascade catch) + Plan 05 (describe restructure). [Detail outcome].

3. **GAP-316-3: [CLOSED | PARTIAL]** — Sentinel S8/S9/S10 ter-exercise [verified via full run | masih blocked]. Report Meta-validation section [non-empty | empty].

4. **D-02 server smoke: PASS** — verified via Plan 02 code inspection. CMPController.cs SubmitExam returns 302 + Location conditional Results/ExamSummary. Tidak perlu live POST smoke.

## Plan 06 Closing Notes

- Plan 04 surgical regex + cascade catch — LOC delta 11 LOC, helper signature preserved
- Plan 05 describe restructure — LOC delta ~30, organizational change only
- Plan 06 actionTimeout add — LOC delta 5
- Throwaway probe Plan 03 — cleaned up di Plan 06 T2

**Total Phase 316 LOC delta:** [Plan 01 + Plan 02 + Plan 04 + Plan 05 + Plan 06 sum]
**Total Phase 316 helper edits:** examMatrix.ts + matrixReport.ts + assessment-matrix.spec.ts + playwright.config.ts

Phase 316 READY-TO-CLOSE pending user approval kalau D-15 acceptance criteria final pass.
```

**Step 5 — Commit UAT update:**

```bash
git add .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md
git commit -m "docs(316-06): update UAT re-verdict — Plan 04+05+06 gap closure

Phase 316 Plan 06 final UAT:
- fix-validate-S5: [status]
- full-run-S1-S10: [status]
- sentinel-meta-verify: [status]
- continue-on-fail-E2E: [status]
- screenshot-path-consistency: PASS (preserved)
- D-02 Server Smoke: PASS (code inspection Plan 02)

Total passed: [N], issues: [M], blocked: 0.
Phase 316 ready-to-close pending user approval.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>grep -qE "Plan 04\\+05\\+06|gap closure|CLOSED" .planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-UAT.md && echo "T3_UAT_OK"</automated>
  </verify>
  <done>316-UAT.md updated dengan 5 D-16 items re-verdict + D-02 PASS + Gaps section reflect closure status (1/2/3 CLOSED atau PARTIAL berdasarkan actual run). Frontmatter total/passed/issues/blocked accurate. Commit done.</done>
</task>

</tasks>

<verification>
**Plan 06 Wave 3 sukses jika:**
1. `playwright.config.ts` punya `actionTimeout: 10_000` di `use` section
2. Throwaway probe spec deleted (`tests/e2e/_throwaway-probe.spec.ts` absent)
3. Quick S5 + full S1-S10 runs executed dengan logs di /tmp
4. Report file regenerated post full run
5. 316-UAT.md updated dengan 5 D-16 re-verdict + frontmatter accurate
6. D-15 acceptance criteria evaluation documented di /tmp/316-final-summary.txt
7. 3 atomic commits (config edit, validation+cleanup, UAT update)

**Phase 316 READY-TO-CLOSE jika:**
- D-15 #1 (S5 Results/ExamSummary): PASS
- D-15 #5 (Inter-scenario continue): PASS atau eligible-evidence-documented
- D-15 #2/3/4 (Sentinel S8/9/10): PASS atau finding-documents-mismatch (sentinel infra working)

**Phase 316 NEEDS NEXT PLAN jika:**
- D-15 acceptance criteria majority FAIL → escalate ke checkpoint:decision untuk user
</verification>

<success_criteria>
- 1 config file modified (5 LOC) + 1 file deleted + 1 UAT updated + 1 report regenerated
- 3 atomic commits untuk audit trail
- Validation summary file /tmp/316-final-summary.txt dengan D-15 evaluation per item
- Phase 316 close decision dokumentasi (READY-TO-CLOSE vs NEEDS-NEXT-PLAN)
</success_criteria>

<output>
After completion, create `.planning/phases/316-fix-submitexam-page-closed-bug-matrix-test-infra-polish-reso/316-06-SUMMARY.md`
</output>
