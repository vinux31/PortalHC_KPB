---
phase: 317
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/e2e/helpers/wizardSelectors.ts
  - tests/e2e/helpers/examTypes.ts
  - tests/e2e/exam-types.spec.ts
  - docs/test-reports/2026-05-11-flow-a-j-regression.md
autonomous: true
requirements: [QA-02]
tags: [e2e, playwright, exam-types, mixed, allow-answer-review, extra-time, signalr, regression]

must_haves:
  truths:
    - "FLOW M (Mixed MC+MA+Essay full cycle 5 sub-tests M1-M5) HIJAU: 3 question types combined → submit → HC grade essay → total score 100 (MC40+MA30+Essay30)"
    - "FLOW N (AllowAnswerReview=false 3 sub-tests N1-N3) HIJAU: Results page menampilkan .alert-info \"Tinjauan jawaban tidak tersedia\" (BUKAN .card review)"
    - "FLOW O (AddExtraTime 5 sub-tests O1-O5) HIJAU: 2-context (HC + worker) — HC fire modal → SignalR ExtraTimeAdded broadcast → worker window.timerStartRemaining bertambah ≥ (extraMinutes*60 - 30s margin)"
    - "Regression smoke FLOW A-J di exam-taking.spec.ts ter-execute + baseline pass rate ter-catat di docs/test-reports/2026-05-11-flow-a-j-regression.md (diagnose-only, BUKAN fix)"
    - "wizardSelectors.ts extend dengan extraTimeSelectors const map"
    - "Full Phase 317 suite HIJAU: smoke + FLOW K-O (Plan 01 + Plan 02) via npx playwright test exam-types"
  artifacts:
    - path: "tests/e2e/helpers/wizardSelectors.ts"
      provides: "Extended with extraTimeSelectors const map"
      contains: "export const extraTimeSelectors"
    - path: "tests/e2e/helpers/examTypes.ts"
      provides: "Extended with addExtraTimeViaModal helper"
      exports: ["addExtraTimeViaModal"]
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "Complete spec dengan FLOW K-O all 5 flows + smoke"
      contains: "FLOW O"
    - path: "docs/test-reports/2026-05-11-flow-a-j-regression.md"
      provides: "Regression smoke FLOW A-J baseline report"
      contains: "Pass rate"
  key_links:
    - from: "tests/e2e/exam-types.spec.ts"
      to: "tests/e2e/helpers/examTypes.ts"
      via: "import addExtraTimeViaModal from examTypes"
      pattern: "addExtraTimeViaModal"
    - from: "tests/e2e/helpers/examTypes.ts"
      to: "Controllers/AssessmentAdminController.cs"
      via: "POST /Admin/AddExtraTime → SignalR group batch-{Title}|{Category}|{Date} broadcast ExtraTimeAdded"
      pattern: "AddExtraTime"
    - from: "tests/e2e/helpers/examTypes.ts"
      to: "Views/CMP/StartExam.cshtml"
      via: "SignalR listener line 1251 update window.timerStartRemaining anchor"
      pattern: "timerStartRemaining"
---

<objective>
Tutup 3/5 gap exam-type coverage QA-02 yang tersisa setelah Plan 01: FLOW M (Mixed MC+MA+Essay), FLOW N (AllowAnswerReview=false negative assertion), FLOW O (AddExtraTime SignalR real-time broadcast). Tambah regression smoke FLOW A-J `exam-taking.spec.ts` untuk catat baseline pass rate (diagnose-only, BUKAN fix scope).

Purpose: Mencapai 100% coverage QA-02 (5/5 FLOW K-O hijau) + dokumentasi baseline FLOW A-J untuk dipakai phase berikutnya identifikasi regression. Plan 02 ini menutup phase 317.

Output:
- `tests/e2e/helpers/wizardSelectors.ts` extended dengan `extraTimeSelectors`
- `tests/e2e/helpers/examTypes.ts` extended dengan `addExtraTimeViaModal` helper (2-context SignalR pattern)
- `tests/e2e/exam-types.spec.ts` extended dengan 3 describe blocks: FLOW M (5 sub-tests), FLOW N (3 sub-tests), FLOW O (5 sub-tests, multi-context)
- `docs/test-reports/2026-05-11-flow-a-j-regression.md` baseline report
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-CONTEXT.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-RESEARCH.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-PATTERNS.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-VALIDATION.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-01-PLAN.md
@tests/e2e/exam-types.spec.ts
@tests/e2e/helpers/examTypes.ts
@tests/e2e/helpers/wizardSelectors.ts
@tests/e2e/helpers/examMatrix.ts
@tests/e2e/assessment-matrix.spec.ts
@tests/e2e/exam-taking.spec.ts
@Views/CMP/StartExam.cshtml
@Views/CMP/Results.cshtml
@Views/Admin/AssessmentMonitoringDetail.cshtml
@Controllers/AssessmentAdminController.cs
@Hubs/AssessmentHub.cs

<interfaces>
<!-- Phase 317 Plan 01 deliverables yang Plan 02 consume directly. -->

From `tests/e2e/helpers/examTypes.ts` (created in Plan 01):
```typescript
export type QuestionInput =
  | { type: 'MultipleChoice'; text: string; options: [string, string, string, string]; correctIndex: 0|1|2|3; score: number }
  | { type: 'MultipleAnswer'; text: string; options: [string, string, string, string]; correctIndices: (0|1|2|3)[]; score: number }
  | { type: 'Essay'; text: string; rubrik: string; maxCharacters?: number; score: number };

export async function createAssessmentViaWizard(page: Page, opts: { /* ... */ }): Promise<void>;
export async function addQuestionViaForm(page: Page, packageId: number, q: QuestionInput): Promise<void>;
export async function submitExamTwoStep(page: Page): Promise<void>;
export async function fillEssayAnswer(page: Page, qIndex: number, answer: string): Promise<void>;
export async function gradeSingleEssaySession(pageHc: Page, opts: { title; category; scheduleDate; sessionId; score }): Promise<void>;
```

From `tests/e2e/helpers/wizardSelectors.ts` (extended in Plan 01):
```typescript
export const selectors = { /* Phase 307+308 — preserve */ } as const;
export const wizardSelectors = { /* 4-step wizard — Phase 317 Plan 01 */ } as const;
export const questionFormSelectors = { /* ManagePackageQuestions form */ } as const;
// Plan 02 will ADD extraTimeSelectors here.
```

From `Views/Admin/AssessmentMonitoringDetail.cshtml` (lines 132-143 + 1462-1495 — verified 2026-05-11):
- ExtraTime trigger: `button[data-bs-target="#extraTimeModal"]`
- Modal: `#extraTimeModal`
- Select dropdown: `#extraTimeSelect` (option values: 5, 10, 15, 20, 25, 30, 45, 60, 90, 120)
- Confirm button: `#btnConfirmExtraTime` (onclick=`addExtraTime()`)
- AJAX endpoint: POST `/Admin/AddExtraTime` body `{ title, category, scheduleDate, extraMinutes }`
- HC feedback: `.alert-success` text matches `/berhasil ditambahkan/i`

From `Controllers/AssessmentAdminController.cs` (lines 5483-5527 — verified 2026-05-11):
- SignalR group naming (composite): `batch-{Title}|{Category}|{Date:yyyy-MM-dd}`
- Broadcast event: `ExtraTimeAdded(additionalSeconds)`
- Server filter: only sessions where `Status == "InProgress"` receive update

From `Views/CMP/StartExam.cshtml` (lines 1250-1256 — verified 2026-05-11):
```javascript
window.assessmentHub.on('ExtraTimeAdded', additionalSeconds => {
  timerStartRemaining = timerStartRemaining - elapsed + additionalSeconds;
  timerStartWallClock = Date.now();
});
```
- Worker JS var: `window.timerStartRemaining` (confirmed accessible via `page.evaluate` per Plan 01 Wave 0 A5)

From `Views/CMP/Results.cshtml` (lines 316-399 — verified 2026-05-11):
- Razor branch when `AllowAnswerReview=true`: `<div class="card">` containing text "Tinjauan Jawaban"
- Razor branch when `AllowAnswerReview=false`: `<div class="alert alert-info">` containing text "Tinjauan jawaban tidak tersedia"

From `tests/e2e/exam-taking.spec.ts` (read-only, regression target):
- 10 describe blocks FLOW A through FLOW J — legacy patterns dengan banyak selector obsolete per RESEARCH State-of-the-Art (single-page CreateAssessment, `name="question_text"`, `correct_option_index`)
- Expected fail rate ≥ baseline; diagnose-only — JANGAN fix di Phase 317
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FLOW M — Mixed (MC+MA+Essay) Full Cycle + FLOW N — AllowAnswerReview=false negative assertion</name>
  <files>
    tests/e2e/exam-types.spec.ts,
    tests/e2e/helpers/examTypes.ts (read-only consume — delivered from Plan 01: gradeSingleEssaySession, fillEssayAnswer, createAssessmentViaWizard, addQuestionViaForm, submitExamTwoStep, QuestionInput)
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (current — Plan 01 commits: smoke + FLOW K + FLOW L)
    - tests/e2e/helpers/examTypes.ts (current — 5 exports + QuestionInput)
    - tests/e2e/helpers/examMatrix.ts (lines 108-184 type-branch answer fill — MC + MA + Essay mixed iteration)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-RESEARCH.md (FLOW K Skeleton lines 542-648 sebagai template; Pattern 4 AllowAnswerReview lines 304-319; Open Question 2 default state)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-PATTERNS.md (lines 124-130 FLOW N negative assertion pattern)
    - Views/CMP/StartExam.cshtml (MC radio + MA checkbox + Essay textarea markup verifikasi)
    - Views/CMP/Results.cshtml (lines 316-399 — Razor @if AllowAnswerReview branch markup verifikasi)
  </read_first>
  <behavior>
    - FLOW M (5 sub-tests M1-M5): HC create wizard → 3 questions (MC score=40 correctIndex=0, MA score=30 correctIndices=[0,1], Essay score=30) → coachee submit mixed answers → HC grade essay 30 → finalize → Results score 100 (40+30+30) badge success
    - FLOW N (3 sub-tests N1-N3): HC create wizard WITH `allowAnswerReview: false` → 1 MC question (score=100 correctIndex=0) → coachee submit → Results page menampilkan `.alert-info` containing "Tinjauan jawaban tidak tersedia" AND TIDAK ada `.card` containing "Tinjauan Jawaban"
  </behavior>
  <action>
    Replace `FLOW M` + tambah `FLOW N` describe blocks di `tests/e2e/exam-types.spec.ts`. Pattern shared-state per 317-PATTERNS.md lines 47-67.

    **FLOW M Implementation:**

    ```typescript
    test.describe('FLOW M — Mixed (MC+MA+Essay) Full Cycle', () => {
      let title: string;
      let category: string;
      let scheduleDate: string;
      let assessmentId: number;
      let packageId: number;
      let sessionId: number;

      test('M1 — HC creates Mixed assessment via wizard', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        title = uniqueTitle('[317-M] Mixed Exam');
        category = 'OJT';
        scheduleDate = today();
        await login(page, 'hc');
        await createAssessmentViaWizard(page, {
          title, category,
          scheduleDate, scheduleTime: '00:01',
          durationMinutes: 60, passPercentage: 60,
          allowAnswerReview: true, generateCertificate: false,
          participantEmails: ['rino.prasetyo@pertamina.com'],
        });
        const href = await page.locator('#modal-manage-btn').getAttribute('href');
        assessmentId = parseInt(href!.match(/ManagePackages\/(\d+)/)![1], 10);
      });

      test('M2 — HC navigates ManagePackages → ManageQuestions', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto(`/Admin/ManagePackages/${assessmentId}`);
        await page.locator('a[href*="ManageQuestions"]').first().click();
        await page.waitForLoadState('networkidle');
        packageId = parseInt(page.url().match(/packageId=(\d+)/)![1], 10);
      });

      test('M3 — HC adds 1 MC + 1 MA + 1 Essay (3 question types)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        // Q1: MC score 40 (correctIndex=0)
        await addQuestionViaForm(page, packageId, {
          type: 'MultipleChoice',
          text: '[317-M] Q1 (MC) — Apa kepanjangan OJT?',
          options: ['On-the-Job Training', 'Online Job Test', 'Office Job Theory', 'Operational Job Task'],
          correctIndex: 0,
          score: 40,
        });
        // Q2: MA score 30 (correctIndices=[0,1])
        await addQuestionViaForm(page, packageId, {
          type: 'MultipleAnswer',
          text: '[317-M] Q2 (MA) — Pilih komponen OJT yang wajib (pilih ≥2)',
          options: ['Coach senior', 'Coachee junior', 'Theoretical exam', 'External certification'],
          correctIndices: [0, 1],
          score: 30,
        });
        // Q3: Essay score 30
        await addQuestionViaForm(page, packageId, {
          type: 'Essay',
          text: '[317-M] Q3 (Essay) — Jelaskan tujuan utama OJT (minimum 50 kata).',
          rubrik: 'Penilaian: clarity + kelengkapan + relevansi.',
          maxCharacters: 1000,
          score: 30,
        });
      });

      test('M4 — Worker answers all 3 question types + submits', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        const card = page.locator('.assessment-card, .card', { hasText: title });
        await expect(card).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await card.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard, a:has-text("Resume")').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });

        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );

        sessionId = parseInt(page.url().match(/StartExam\/(\d+)/)![1], 10);

        const qCards = page.locator('[id^="qcard_"]');
        await expect(qCards).toHaveCount(3);

        // Q1 MC — radio nth(0) correct
        await qCards.nth(0).locator('input.exam-radio').nth(0).check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i }).first().waitFor({ timeout: 5_000 });

        // Q2 MA — checkbox nth(0) + nth(1) correct
        const ma = qCards.nth(1).locator('input.exam-checkbox');
        await ma.nth(0).check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i }).first().waitFor({ timeout: 5_000 });
        await ma.nth(1).check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i }).first().waitFor({ timeout: 5_000 });

        // Q3 Essay
        await fillEssayAnswer(page, 2, 'Tujuan utama OJT adalah pengembangan kompetensi pekerja melalui pembelajaran berbasis praktek langsung di tempat kerja, dengan supervisi coach senior, evaluasi berdasarkan kinerja aktual, dan transfer pengetahuan terstruktur dari pengalaman lapangan ke peserta latih dalam konteks operasional nyata.');

        // Submit 2-step
        await submitExamTwoStep(page);
      });

      test('M5 — HC grades essay 30 + Worker sees total score 100', async ({ browser }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        // HC: grade essay 30 (max=30)
        const ctxHc = await browser.newContext();
        const pageHc = await ctxHc.newPage();
        try {
          await login(pageHc, 'hc');
          await gradeSingleEssaySession(pageHc, { title, category, scheduleDate, sessionId, score: 30 });
        } finally {
          await ctxHc.close().catch(() => { /* non-fatal */ });
        }

        // Worker: verify Results
        const ctxWorker = await browser.newContext();
        const pageWorker = await ctxWorker.newPage();
        try {
          await login(pageWorker, 'coachee');
          await pageWorker.goto(`/CMP/Results/${sessionId}`);
          // Total = 40 (MC) + 30 (MA) + 30 (Essay) = 100
          await expect(pageWorker.locator('body')).toContainText(/100/);
          await expect(pageWorker.locator('.badge.text-bg-success').first()).toBeVisible({ timeout: 10_000 });
        } finally {
          await ctxWorker.close().catch(() => { /* non-fatal */ });
        }
      });
    });
    ```

    **FLOW N Implementation:**

    ```typescript
    test.describe('FLOW N — AllowAnswerReview=false negative assertion', () => {
      let title: string;
      let assessmentId: number;
      let packageId: number;
      let sessionId: number;

      test('N1 — HC creates assessment with AllowAnswerReview=false + 1 MC', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        title = uniqueTitle('[317-N] NoReview Exam');
        await login(page, 'hc');
        // CRITICAL: allowAnswerReview: false — UNCHECK di wizard step 3
        await createAssessmentViaWizard(page, {
          title, category: 'OJT',
          scheduleDate: today(), scheduleTime: '00:01',
          durationMinutes: 60, passPercentage: 70,
          allowAnswerReview: false,  // ← FLOW N key parameter
          generateCertificate: false,
          participantEmails: ['rino.prasetyo@pertamina.com'],
        });
        const href = await page.locator('#modal-manage-btn').getAttribute('href');
        assessmentId = parseInt(href!.match(/ManagePackages\/(\d+)/)![1], 10);

        // Add 1 MC question
        await page.locator('#modal-manage-btn').click();
        await page.waitForLoadState('networkidle');
        await page.locator('a[href*="ManageQuestions"]').first().click();
        await page.waitForLoadState('networkidle');
        packageId = parseInt(page.url().match(/packageId=(\d+)/)![1], 10);

        await addQuestionViaForm(page, packageId, {
          type: 'MultipleChoice',
          text: '[317-N] Q1 — Pilih jawaban benar',
          options: ['Correct', 'Wrong-1', 'Wrong-2', 'Wrong-3'],
          correctIndex: 0,
          score: 100,
        });
      });

      test('N2 — Worker submits exam', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        const card = page.locator('.assessment-card, .card', { hasText: title });
        await expect(card).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await card.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard, a:has-text("Resume")').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });

        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );

        sessionId = parseInt(page.url().match(/StartExam\/(\d+)/)![1], 10);

        // Answer MC correct
        await page.locator('[id^="qcard_"]').first().locator('input.exam-radio').nth(0).check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i }).first().waitFor({ timeout: 5_000 });

        await submitExamTwoStep(page);
      });

      test('N3 — Results page hides review (alert-info shown, review card absent)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto(`/CMP/Results/${sessionId}`);
        // Score visible (100)
        await expect(page.locator('body')).toContainText(/100/);

        // POSITIVE assertion: alert-info "Tinjauan jawaban tidak tersedia" visible
        await expect(
          page.locator('.alert-info, .alert.alert-info', { hasText: /Tinjauan jawaban tidak tersedia/i })
        ).toBeVisible({ timeout: 10_000 });

        // NEGATIVE assertion: review card "Tinjauan Jawaban" TIDAK ada
        await expect(
          page.locator('.card', { hasText: /^Tinjauan Jawaban/i })
        ).toHaveCount(0);
      });
    });
    ```

    **Run:**
    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW M|FLOW N"
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "FLOW M|FLOW N"</automated>
  </verify>
  <acceptance_criteria>
    - 5 sub-tests M1-M5 PASSED dalam describe `FLOW M — Mixed (MC+MA+Essay) Full Cycle`
    - 3 sub-tests N1-N3 PASSED dalam describe `FLOW N — AllowAnswerReview=false negative assertion`
    - Grep `tests/e2e/exam-types.spec.ts` for `'FLOW M — Mixed`, `'FLOW N — AllowAnswerReview` returns 2 lines
    - Grep `tests/e2e/exam-types.spec.ts` for `M1 —`, `M2 —`, `M3 —`, `M4 —`, `M5 —` returns 5 lines
    - Grep `tests/e2e/exam-types.spec.ts` for `N1 —`, `N2 —`, `N3 —` returns 3 lines
    - M5 assertion `expect(body).toContainText(/100/)` PASS — total 100 (40+30+30) verified
    - N3 assertion `.alert-info` containing "Tinjauan jawaban tidak tersedia" visible PASS
    - N3 assertion `.card` containing "Tinjauan Jawaban" toHaveCount(0) PASS
    - Per-sub-test runtime ≤ 120s
  </acceptance_criteria>
  <done>
    FLOW M (5 sub-tests Mixed) + FLOW N (3 sub-tests AllowAnswerReview=false negative) HIJAU end-to-end. 3 question types combined cycle proven; Razor branch AllowAnswerReview=false negative assertion verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FLOW O — AddExtraTime SignalR (multi-context HC + worker simultan)</name>
  <files>
    tests/e2e/helpers/wizardSelectors.ts,
    tests/e2e/helpers/examTypes.ts,
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/helpers/wizardSelectors.ts (Plan 01 state — extend dengan extraTimeSelectors)
    - tests/e2e/helpers/examTypes.ts (Plan 01 state — extend dengan addExtraTimeViaModal)
    - tests/e2e/exam-types.spec.ts (current — Plan 01 + Plan 02 Task 1)
    - tests/e2e/assessment-matrix.spec.ts (lines 135-180 — multi-context pattern + defensive close)
    - tests/e2e/helpers/examMatrix.ts (lines 94-103 SignalR readiness gate, lines 225-290 gradeEssaysAsHc URLSearchParams MonitoringDetail navigation pattern)
    - Views/Admin/AssessmentMonitoringDetail.cshtml (lines 132-143 — modal trigger + select + confirm markup; lines 1462-1495 — addExtraTime JS handler)
    - Views/CMP/StartExam.cshtml (lines 1240-1268 — ExtraTimeAdded SignalR listener verifikasi)
    - Controllers/AssessmentAdminController.cs (lines 5483-5527 — AddExtraTime endpoint signature, group naming verifikasi)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-RESEARCH.md (Pattern 3 lines 280-301, Pitfall 4 lines 399-410, Pitfall 5 lines 411-428, Open Q5 line 703-707)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-PATTERNS.md (lines 84-101 multi-context pattern, lines 237-289 addExtraTimeViaModal example)
  </read_first>
  <behavior>
    - 5 sub-tests O1-O5 dalam describe `FLOW O — AddExtraTime SignalR real-time`
    - O1: HC create wizard (duration=30 menit kecil untuk easy verify, allowAnswerReview=true)
    - O2: HC add 1 MC question (score=100)
    - O3: 2-context (browser.newContext × 2) — Worker context starts exam → SignalR connected → captures initial `window.timerStartRemaining`; HC context navigates AssessmentMonitoringDetail → fires `#extraTimeModal` → selects 10 menit → clicks `#btnConfirmExtraTime` → `.alert-success` "berhasil ditambahkan" visible; Worker context waits `timerStartRemaining > initial + (10*60 - 30)` via `page.waitForFunction`
    - O4: Worker submits exam (validates exam masih dapat di-submit setelah extra time)
    - O5: Worker sees Results normal (score 100, badge success)
    - All multi-context tests pakai try/finally defensive close per 317-PATTERNS.md lines 96-101
  </behavior>
  <action>
    **STEP 1 — Extend `tests/e2e/helpers/wizardSelectors.ts`:**

    Append at end of file (preserve existing):
    ```typescript
    // Phase 317 Plan 02 — AddExtraTime modal
    // Source: Views/Admin/AssessmentMonitoringDetail.cshtml lines 132-143 + 1462-1495 (verified 2026-05-11)
    export const extraTimeSelectors = {
      triggerBtn: 'button[data-bs-target="#extraTimeModal"]',
      modal: '#extraTimeModal',
      select: '#extraTimeSelect',           // option values: 5, 10, 15, 20, 25, 30, 45, 60, 90, 120
      confirmBtn: '#btnConfirmExtraTime',
      // Effect on HC page: .alert-success/.alert-danger di .container-fluid
    } as const;
    ```

    **STEP 2 — Extend `tests/e2e/helpers/examTypes.ts`:**

    Add import at top:
    ```typescript
    import { wizardSelectors, questionFormSelectors, extraTimeSelectors } from './wizardSelectors';
    ```

    Add new function per 317-PATTERNS.md lines 237-289:
    ```typescript
    /**
     * HC fire AddExtraTime modal → AJAX POST → SignalR broadcast → worker timer JS update.
     *
     * Group naming (Controllers/AssessmentAdminController.cs:5485):
     *   batch-{Title}|{Category}|{Date:yyyy-MM-dd} — composite key, BUKAN per-token group.
     *
     * Verify strategy (RESEARCH Pitfall 4 mitigation):
     *  1. pageHc: alert-success text /berhasil ditambahkan/i — server-side success confirmation
     *  2. pageWorker: window.timerStartRemaining increased ≥ (extraMinutes*60 - 30s margin) via waitForFunction
     *
     * Pre-condition: pageWorker MUST be at /CMP/StartExam/{id} with SignalR Connected state.
     *
     * Open Q5: server filter status="InProgress" — pageWorker MUST be at StartExam (status auto-flipped to InProgress at StartExam controller action).
     */
    export async function addExtraTimeViaModal(
      pageHc: Page,
      pageWorker: Page,
      opts: { title: string; category: string; scheduleDate: string; extraMinutes: 5|10|15|20|25|30|45|60|90|120 }
    ): Promise<void> {
      // 1. Capture initial timerStartRemaining (verified A5 Plan 01 Wave 0 — accessible JS var)
      const initialRemaining = await pageWorker.evaluate(() => {
        return (window as unknown as { timerStartRemaining?: number }).timerStartRemaining ?? 0;
      });
      expect(initialRemaining).toBeGreaterThan(0);

      // 2. HC navigate to MonitoringDetail (URLSearchParams pattern dari examMatrix.ts:230-235)
      const baseUrl = pageHc.url() || 'http://localhost:5277';
      const url = new URL('/Admin/AssessmentMonitoringDetail', baseUrl);
      url.searchParams.set('title', opts.title);
      url.searchParams.set('category', opts.category);
      url.searchParams.set('scheduleDate', opts.scheduleDate);
      await pageHc.goto(url.toString());
      await pageHc.waitForLoadState('networkidle');

      // 3. Fire modal + confirm
      await pageHc.locator(extraTimeSelectors.triggerBtn).first().click();
      await pageHc.locator(extraTimeSelectors.modal).waitFor({ state: 'visible', timeout: 10_000 });
      await pageHc.selectOption(extraTimeSelectors.select, String(opts.extraMinutes));
      await pageHc.locator(extraTimeSelectors.confirmBtn).click();

      // 4. Verify HC alert-success (server-side success)
      await expect(
        pageHc.locator('.alert-success, .alert.alert-success', { hasText: /berhasil ditambahkan/i })
      ).toBeVisible({ timeout: 10_000 });

      // 5. Verify worker timer increased (SignalR broadcast received)
      const expectedDelta = opts.extraMinutes * 60 - 30; // -30s margin for elapsed time during HC action
      await pageWorker.waitForFunction(
        (args) => {
          const remaining = (window as unknown as { timerStartRemaining?: number }).timerStartRemaining ?? 0;
          return remaining > args.initial + args.expectedDelta;
        },
        { initial: initialRemaining, expectedDelta },
        { timeout: 20_000 }
      );
    }
    ```

    **STEP 3 — Add `FLOW O` describe to `tests/e2e/exam-types.spec.ts`:**

    Update import:
    ```typescript
    import { createAssessmentViaWizard, addQuestionViaForm, submitExamTwoStep, fillEssayAnswer, gradeSingleEssaySession, addExtraTimeViaModal, type QuestionInput } from './helpers/examTypes';
    ```

    Add constant:
    ```typescript
    const FLOW_O_TIMEOUT_MS = 180_000; // 2-context simul + SignalR wait extends runtime
    ```

    Add describe block:
    ```typescript
    test.describe('FLOW O — AddExtraTime SignalR real-time', () => {
      let title: string;
      let category: string;
      let scheduleDate: string;
      let assessmentId: number;
      let packageId: number;
      let sessionId: number;

      test('O1 — HC creates assessment (duration 30 min)', async ({ page }) => {
        test.setTimeout(FLOW_O_TIMEOUT_MS);
        title = uniqueTitle('[317-O] ExtraTime Exam');
        category = 'OJT';
        scheduleDate = today();
        await login(page, 'hc');
        await createAssessmentViaWizard(page, {
          title, category,
          scheduleDate, scheduleTime: '00:01',
          durationMinutes: 30, passPercentage: 70,
          allowAnswerReview: true, generateCertificate: false,
          participantEmails: ['rino.prasetyo@pertamina.com'],
        });
        const href = await page.locator('#modal-manage-btn').getAttribute('href');
        assessmentId = parseInt(href!.match(/ManagePackages\/(\d+)/)![1], 10);
      });

      test('O2 — HC adds 1 MC question', async ({ page }) => {
        test.setTimeout(FLOW_O_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto(`/Admin/ManagePackages/${assessmentId}`);
        await page.locator('a[href*="ManageQuestions"]').first().click();
        await page.waitForLoadState('networkidle');
        packageId = parseInt(page.url().match(/packageId=(\d+)/)![1], 10);

        await addQuestionViaForm(page, packageId, {
          type: 'MultipleChoice',
          text: '[317-O] Q1 — Pilih jawaban benar',
          options: ['Correct', 'Wrong-1', 'Wrong-2', 'Wrong-3'],
          correctIndex: 0,
          score: 100,
        });
      });

      test('O3 — HC adds 10 min extra time → worker timer updates real-time via SignalR (multi-context)', async ({ browser }) => {
        test.setTimeout(FLOW_O_TIMEOUT_MS);
        // Multi-context: HC + Worker simultan (Pitfall 5 mitigation — cookie isolation)
        const ctxWorker = await browser.newContext();
        const ctxHc = await browser.newContext();
        const pageWorker = await ctxWorker.newPage();
        const pageHc = await ctxHc.newPage();

        try {
          // Worker: login + start exam
          await login(pageWorker, 'coachee');
          await pageWorker.goto('/CMP/Assessment');
          const card = pageWorker.locator('.assessment-card, .card', { hasText: title });
          await expect(card).toBeVisible({ timeout: 10_000 });
          pageWorker.once('dialog', d => d.accept());
          await card.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard, a:has-text("Resume")').first().click();
          await pageWorker.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });

          // SignalR readiness (worker)
          await pageWorker.waitForFunction(
            () => {
              const w = window as unknown as { assessmentHub?: { state?: string } };
              return w.assessmentHub?.state === 'Connected';
            },
            undefined,
            { timeout: 10_000 }
          );

          sessionId = parseInt(pageWorker.url().match(/StartExam\/(\d+)/)![1], 10);

          // Open Q5 buffer — 2s wait setelah StartExam supaya server set status=InProgress
          await pageWorker.waitForTimeout(2_000);

          // HC: login (separate context, no cookie collision)
          await login(pageHc, 'hc');

          // Fire AddExtraTime modal → verify HC alert + worker timer increment
          await addExtraTimeViaModal(pageHc, pageWorker, {
            title, category, scheduleDate, extraMinutes: 10,
          });
        } finally {
          await ctxWorker.close().catch(() => { /* already closed — non-fatal */ });
          await ctxHc.close().catch(() => { /* already closed — non-fatal */ });
        }
      });

      test('O4 — Worker submits exam after extra time added', async ({ page }) => {
        test.setTimeout(FLOW_O_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        const card = page.locator('.assessment-card, .card', { hasText: title });
        await expect(card).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await card.locator('a:has-text("Resume"), a:has-text("Lanjutkan"), a:has-text("Mulai"), .btn-start-standard').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });

        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );

        // Confirm sessionId still valid
        const resumeSessionId = parseInt(page.url().match(/StartExam\/(\d+)/)![1], 10);
        expect(resumeSessionId).toBe(sessionId);

        // Answer + submit
        await page.locator('[id^="qcard_"]').first().locator('input.exam-radio').nth(0).check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i }).first().waitFor({ timeout: 5_000 });

        await submitExamTwoStep(page);
      });

      test('O5 — Worker sees Results score 100 normal', async ({ page }) => {
        test.setTimeout(FLOW_O_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto(`/CMP/Results/${sessionId}`);
        await expect(page.locator('body')).toContainText(/100/);
        await expect(page.locator('.badge.text-bg-success').first()).toBeVisible({ timeout: 10_000 });
      });
    });
    ```

    **Run:**
    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW O"
    ```

    Kalau O3 RED (worker timer tidak update):
    - Verify HC alert-success VISIBLE — kalau YES = server OK, SignalR group naming issue
    - Read `Hubs/AssessmentHub.cs` OnConnectedAsync — confirm worker auto-join group `batch-{Title}|{Category}|{Date}`
    - Alternative pivot: hapus assertion timer increment, hanya verify HC alert-success (server-side success only) — note SURF-317 untuk follow-up
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "FLOW O"</automated>
  </verify>
  <acceptance_criteria>
    - 5 sub-tests O1-O5 PASSED dalam describe `FLOW O — AddExtraTime SignalR real-time`
    - Grep `tests/e2e/helpers/wizardSelectors.ts` for `export const extraTimeSelectors` returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `export async function addExtraTimeViaModal` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `'FLOW O — AddExtraTime` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `O1 —`, `O2 —`, `O3 —`, `O4 —`, `O5 —` returns 5 lines
    - O3 (multi-context): worker `timerStartRemaining` increased ≥ (600 - 30 = 570 detik) verified
    - O3 (multi-context): HC `.alert-success` text "berhasil ditambahkan" visible verified
    - Try/finally defensive close pattern present (grep for `.catch(() =>`)
    - O3 runtime ≤ 180s
  </acceptance_criteria>
  <done>
    FLOW O (AddExtraTime SignalR real-time, 5 sub-tests) HIJAU end-to-end. Multi-context pattern proven untuk HC + worker concurrent. SignalR broadcast → worker timer update verified via page.waitForFunction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression smoke FLOW A-J + Phase 317 finalize gate</name>
  <files>
    docs/test-reports/2026-05-11-flow-a-j-regression.md,
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts (read-only — FLOW A-J describe blocks + selectors yang dipakai)
    - tests/e2e/exam-types.spec.ts (final state setelah Task 1+2 Plan 02)
    - tests/playwright.config.ts (config baseURL + testDir)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-CONTEXT.md (Task 8 scope — diagnose-only, BUKAN fix)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-RESEARCH.md (State of the Art lines 651-666 — known FLOW A obsolete selectors)
  </read_first>
  <behavior>
    - Run `npx playwright test exam-taking.spec.ts --reporter=list` capture exit code + pass rate
    - Generate `docs/test-reports/2026-05-11-flow-a-j-regression.md` dengan:
      - Header: Phase 317 baseline date 2026-05-11
      - Total tests count
      - Passed / Failed / Skipped breakdown per FLOW (A-J)
      - List failed test names dengan brief failure reason (1-line)
      - Known obsolete selectors dari RESEARCH State-of-the-Art (single-page CreateAssessment, `name="question_text"`, `correct_option_index`, `#ScheduleDate`)
      - Recommendation: "Phase 317 SCOPE = diagnose-only. Fix regressions di Phase 318+ as SURF-317-x."
    - Verify full Phase 317 suite (Plan 01 + Plan 02 = smoke + FLOW K-O) HIJAU end-to-end via single command
  </behavior>
  <action>
    **STEP 1 — Run regression smoke FLOW A-J:**

    ```bash
    cd tests && npx playwright test exam-taking.spec.ts --reporter=list 2>&1 | tee /tmp/flow-a-j-output.txt
    ```

    Capture: exit code, total count, passed, failed, skipped, list of failed test names.

    **STEP 2 — Create `docs/test-reports/2026-05-11-flow-a-j-regression.md`:**

    Generate report dengan format:

    ```markdown
    # Phase 317 — Regression Smoke FLOW A-J Baseline

    **Date:** 2026-05-11
    **Spec:** `tests/e2e/exam-taking.spec.ts`
    **Command:** `cd tests && npx playwright test exam-taking.spec.ts --reporter=list`
    **Phase scope:** Diagnose-only (BUKAN fix — fix regressions ditunda ke Phase 318+ as SURF-317-x)
    **Baseline purpose:** Catat pass rate FLOW A-J SETELAH v15.0 changes (Phase 304-314) untuk dibandingkan dengan re-run di phase berikutnya.

    ## Summary

    | Metric | Value |
    |--------|-------|
    | Total tests | {N} |
    | Passed | {P} ({P/N}%) |
    | Failed | {F} ({F/N}%) |
    | Skipped | {S} ({S/N}%) |
    | Runtime | {duration} |

    ## Per-FLOW Breakdown

    | FLOW | Total | Passed | Failed | Skipped | Notes |
    |------|-------|--------|--------|---------|-------|
    | A | ? | ? | ? | ? | (description) |
    | B | ? | ? | ? | ? | |
    | ... | | | | | |
    | J | ? | ? | ? | ? | |

    ## Failed Tests Detail

    {Untuk setiap failed test:}

    ### `{test name}`

    - **FLOW:** {A/B/.../J}
    - **Failure:** {1-line reason from output, e.g., "Element not found #ScheduleDate"}
    - **Suspected cause:** {obsolete selector / wizard 4-step / SignalR race / etc — cross-reference RESEARCH State-of-the-Art}
    - **Anchor ID:** SURF-317-{N} (untuk follow-up phase tracking)

    ## Known Obsolete Selectors (from 317-RESEARCH.md State-of-the-Art)

    | Old Approach (FLOW A line ref) | Current Markup | When Changed |
    |--------------------------------|----------------|--------------|
    | Single-page `#submitBtn` click | 4-step wizard `#btnNext1/2/3` + `#btnSubmit` | Phase 304 (2026-04-28) |
    | `#ScheduleDate` direct fill | `#schedDateInput` + `#schedTimeInput` (hidden combiner) | Phase 304 |
    | `input[name="correct_option_index"][value="N"]` | `#correct_A/B/C/D` radio/checkbox | Phase 298 (2026-04-07) |
    | `name="question_text"` + `name="options"` array | `name="questionText"` + `name="optionA..D"` | Phase 298 |

    ## Recommendation

    Phase 317 SCOPE = diagnose-only — catat baseline, BUKAN fix. Setiap failure di-anchor sebagai `SURF-317-{N}` untuk follow-up phase:

    - Phase 318 (PreTest/PostTest + ExamWindowCloseDate + Certificate PDF) — opportunistic update FLOW A wizard creation if needed
    - Phase 319 (ManualAssessment + Export + Analytics + CertificationManagement) — fixed scope, regression fix optional

    Atau buat dedicated phase `Phase 320 — FLOW A-J refresh` kalau pass rate jatuh > 50%.
    ```

    Replace `{N}`, `{P}`, `{F}`, `{S}`, `{duration}`, per-FLOW breakdown, failed tests detail dengan actual output dari step 1.

    **STEP 3 — Verify full Phase 317 suite (Plan 01 + Plan 02):**

    ```bash
    cd tests && npx playwright test exam-types.spec.ts --reporter=list
    ```

    Expected:
    - 2 (smoke wave-0) + 5 (K) + 6 (L) + 5 (M) + 3 (N) + 5 (O) = 26 sub-tests
    - Semua HIJAU
    - Exit code 0

    Kalau ada sub-test RED:
    - Identify which (smoke wave-0 / K / L / M / N / O)
    - Cross-reference RESEARCH Pitfall + Open Question
    - Fix root cause SEBELUM commit final

    **STEP 4 — Final TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```

    Must exit 0.
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types.spec.ts --reporter=list && test -f docs/test-reports/2026-05-11-flow-a-j-regression.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/test-reports/2026-05-11-flow-a-j-regression.md` exists
    - Report contains "Phase 317", "Pass rate", "FLOW A", "FLOW J", "Recommendation" (grep verifiable)
    - Report contains per-FLOW breakdown table dengan ≥ 10 rows (A-J)
    - Report contains "SURF-317-" anchor (grep returns ≥1 line jika ada failures, atau 0 lines jika 100% pass)
    - Full Phase 317 suite `npx playwright test exam-types.spec.ts` exit 0 dengan 26 sub-tests HIJAU
    - `cd tests && npx tsc --noEmit` exit 0
    - exam-taking.spec.ts NOT modified (regression scope = diagnose only) — `git diff tests/e2e/exam-taking.spec.ts` empty
  </acceptance_criteria>
  <done>
    Regression smoke FLOW A-J ter-execute, baseline pass rate ter-catat di `docs/test-reports/2026-05-11-flow-a-j-regression.md` dengan per-FLOW breakdown + failed test list + SURF-317-x anchors. Full Phase 317 exam-types suite (26 sub-tests) HIJAU end-to-end. TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → dev DB | Multi-context test (FLOW O) writes assessment + grading via UI |
| Worker context → SignalR hub | SignalR `ExtraTimeAdded` broadcast cross-context |
| HC context → AJAX `/Admin/AddExtraTime` | Authenticated API call (different cookie context) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-04 | Tampering | Multi-context cookie collision FLOW O | mitigate | `browser.newContext()` × 2 dengan defensive close di finally (assessment-matrix.spec.ts:168-177 pattern) |
| T-317-05 | Denial of Service | FLOW O 2-context test runtime overflow | mitigate | `test.setTimeout(FLOW_O_TIMEOUT_MS = 180_000)` untuk O3 sub-test (2-context simul + SignalR wait) |
| T-317-06 | Information Disclosure | Regression report exposes test failure detail | accept | Report committed ke `docs/test-reports/` — internal scope only, no PII/credentials |
| T-317-07 | Spoofing | SignalR group name composite `batch-{Title}|{Category}|{Date}` — attacker bisa rejoin group dengan crafted title | accept | Out of scope Phase 317 — test pakai dev creds local. Production should validate at hub OnConnectedAsync. |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# FLOW M + FLOW N (Task 1)
cd tests && npx playwright test exam-types --grep "FLOW M|FLOW N"

# FLOW O (Task 2)
cd tests && npx playwright test exam-types --grep "FLOW O"

# Regression smoke FLOW A-J + Phase 317 finalize (Task 3)
cd tests && npx playwright test exam-taking.spec.ts --reporter=list
cd tests && npx playwright test exam-types.spec.ts --reporter=list

# TypeScript compile gate
cd tests && npx tsc --noEmit
```

## Manual UAT (post-automation)

- HC manual fire AddExtraTime di local UI → confirm modal UX natural (Indonesian copy)
- Worker mid-exam in-progress → verify timer visual increment within 2s post-AddExtraTime (matches automated assertion)
- Setelah commit, cleanup dev DB: hapus row prefix `[317-M]`, `[317-N]`, `[317-O]` per CLAUDE.md SEED_WORKFLOW
</verification>

<success_criteria>
## Plan 02 Complete When

1. `tests/e2e/helpers/wizardSelectors.ts` extended dengan `extraTimeSelectors` const map
2. `tests/e2e/helpers/examTypes.ts` extended dengan `addExtraTimeViaModal` function (multi-context SignalR helper)
3. `tests/e2e/exam-types.spec.ts` extended dengan 3 describe blocks: `FLOW M`, `FLOW N`, `FLOW O` (5+3+5 = 13 sub-tests)
4. FLOW M HIJAU: Mixed 3 question types → submit → grade essay → score 100 verified
5. FLOW N HIJAU: AllowAnswerReview=false → Results menampilkan alert-info, no review card
6. FLOW O HIJAU: Multi-context HC + worker → SignalR broadcast → timer increment verified
7. Regression smoke FLOW A-J ter-execute (BUKAN fix), baseline `docs/test-reports/2026-05-11-flow-a-j-regression.md` created
8. Full Phase 317 suite (Plan 01 + Plan 02 = 26 sub-tests) HIJAU via `npx playwright test exam-types.spec.ts`
9. TypeScript clean (`npx tsc --noEmit` exit 0)
10. Commit: `feat(317): plan 02 — FLOW M Mixed + FLOW N AllowAnswerReview + FLOW O AddExtraTime + regression baseline (QA-02)`
</success_criteria>

<output>
After completion, create `.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md` mencakup:
- Files created/modified: wizardSelectors.ts (extraTimeSelectors), examTypes.ts (addExtraTimeViaModal), exam-types.spec.ts (FLOW M/N/O), `docs/test-reports/2026-05-11-flow-a-j-regression.md`
- FLOW M / FLOW N / FLOW O runtime measurements per sub-test
- Multi-context FLOW O findings: any cookie collision, timing issue, SignalR race
- Regression baseline FLOW A-J: total tests, pass rate, list of failed tests with SURF-317-x anchors
- Phase 317 closure: QA-02 coverage 5/5 FLOW HIJAU; recommendation untuk FLOW A-J fix scope (Phase 318+ atau dedicated Phase 320)
- Any deviation dari RESEARCH (e.g., HC alert-success text variant, timer increment margin adjustment, SignalR group naming edge case)
</output>
