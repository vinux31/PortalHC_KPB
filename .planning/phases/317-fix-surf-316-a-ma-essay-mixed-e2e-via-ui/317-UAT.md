---
status: complete
phase: 317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui
source:
  - 317-01-SUMMARY.md
  - 317-02-SUMMARY.md
started: 2026-05-12T00:44:13Z
updated: 2026-05-12T00:50:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Full Phase 317 suite hijau end-to-end
expected: |
  Run `cd tests && npx playwright test exam-types.spec.ts --reporter=list`.
  28/28 PASS in ~1.9 min. Per-FLOW breakdown:
  - smoke wave-0: 2/2 (W0.1 wizard+3MC, W0.2 A4 order + A5 timer)
  - FLOW K (MA): 5/5 (K1-K5 + DB verify Score=100)
  - FLOW L (Essay + HC grade): 6/6 (L1-L6 + DB verify Score=80)
  - FLOW M (Mixed MC+MA+Essay): 5/5 (M1-M5 + DB verify Score=100)
  - FLOW N (AllowAnswerReview=false): 4/4 (N1-N4 + alert-info shown)
  - FLOW O (AddExtraTime SignalR): 5/5 (O1-O5 + worker timer increment)
result: pass
evidence: |
  Verified via automated playwright run during Plan 02 Task 3 finalize gate.
  Output: "28 passed (1.9m)" — 1 setup + 27 sub-tests hijau.
  Browser smoke: CreateAssessment wizard 4-step renders (Step 1 active, 2/3/4 disabled),
  AssessmentMonitoringDetail page renders with Tambah Waktu button visible.

### 2. SURF-317-A workaround documented + applied
expected: |
  Production bug surfaced — `GET /CMP/Results/{sessionId}` returns 500 untuk MA assessments
  karena `ToDictionary` collision di CMPController.cs:2190.
  Workaround applied di K5 (MA), L6 (Essay), M5 (Mixed) — verifikasi via `db.queryScalar`
  `SELECT ISNULL(Score, -1) FROM AssessmentSessions WHERE Id = {sessionId}`.
  Grep `SURF-317-A` di repo returns ≥3 occurrences (K5, L6, M5 inline + summaries).
result: pass
evidence: |
  Grep `SURF-317-A` di tests/e2e/exam-types.spec.ts returns 8 occurrences
  (K5, L6, M5 inline comments + assertions). Workaround pattern reusable di Plan 02 FLOW M.

### 3. FLOW O AddExtraTime SignalR multi-context broadcast verified
expected: |
  FLOW O O3 sub-test (multi-context):
  - Worker context: startExam → SignalR `Connected` state → captures initial `window.timerStartRemaining`
  - HC context (separate browser.newContext): login → navigate AssessmentMonitoringDetail
    → click `#extraTimeModal` trigger → select 10 menit → confirm
  - HC `.alert-success` text /berhasil ditambahkan/i visible
  - Worker `timerStartRemaining > initial + 570` (10 menit - 30s margin) via `page.waitForFunction`
  - Both contexts close cleanly di try/finally (no cookie collision)
result: pass
evidence: |
  Automated O3 PASS dalam 12.9s (28/28 suite run).
  Browser manual: Modal "Tambah Waktu Ujian" opens, dropdown values 5/10/15(default)/20/25/30/45/60/90/120 menit
  match extraTimeSelectors.select option list byte-for-byte. Buttons Batal + Tambah Waktu present.

### 4. Regression baseline report exists + SURF-317-A1 anchored
expected: |
  File `docs/test-reports/2026-05-11-flow-a-j-regression.md` exists.
  Contains: summary table (76 tests total, 1 setup + 1 failed + 74 cascade-skipped),
  per-FLOW breakdown (A-J + Phase 313 block = 11 describes),
  known obsolete selectors table (9 entries),
  SURF-317-A1 anchor documented (legacy `.user-check-item input` selector tidak match Phase 304+ markup),
  recommendation tier (Phase 318+ opportunistic / Phase 320 dedicated refresh).
  `git diff tests/e2e/exam-taking.spec.ts` empty (regression diagnose-only honored).
result: pass
evidence: |
  File exists 8028 bytes. Contains all required sections.
  git diff tests/e2e/exam-taking.spec.ts: empty (verified pre-commit Task 3).

### 5. Helper artifacts complete + reusable
expected: |
  `tests/e2e/helpers/examTypes.ts` exports 8 functions:
  createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm, submitExamTwoStep,
  checkMAOptionsForQuestion, fillEssayAnswer, gradeSingleEssaySession, addExtraTimeViaModal
  plus QuestionInput discriminated union type.
  `tests/e2e/helpers/wizardSelectors.ts` exports: selectors (Phase 307/308 preserved),
  wizardSelectors (4-step wizard), questionFormSelectors (ManagePackageQuestions form),
  extraTimeSelectors (AddExtraTime modal — Phase 317 Plan 02).
  `npx tsc --noEmit` exit 0 (TypeScript clean).
result: pass
evidence: |
  Grep `^export` examTypes.ts: 10 lines (8 functions + QuestionInput type + CreateAssessmentOpts interface).
  wizardSelectors.ts: 4 exports (selectors, wizardSelectors, extraTimeSelectors, questionFormSelectors).
  npx tsc --noEmit: exit 0 verified Task 3 finalize gate.

### 6. QA-02 coverage 5/5 FLOW K-O closed
expected: |
  Milestone v16.0 requirement QA-02 (exam-types E2E coverage) achieves 5/5 FLOW K-O hijau:
  - FLOW K (MultipleAnswer full cycle)
  - FLOW L (Essay full cycle + HC grading)
  - FLOW M (Mixed MC+MA+Essay)
  - FLOW N (AllowAnswerReview=false negative)
  - FLOW O (AddExtraTime SignalR real-time broadcast)
  Phase 317 ready close. SURF-317-A + SURF-317-A1 carried forward ke Phase 318+ scope.
result: pass
evidence: |
  All 5 FLOW K-O describes hijau in full suite run (28/28).
  Plan 01 + Plan 02 SUMMARYs document QA-02 closure.
  SURF-317-A workaround applied (3 occurrences spec). SURF-317-A1 anchored di regression report.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none — all UAT tests pass]

## Browser Verification Findings (manual UAT 2026-05-12)

**Endpoint:** http://localhost:5277 (dotnet bg, PID 24200)
**HC akun:** meylisa.tjiang@pertamina.com / 123456

| # | Checkpoint | Hasil |
|---|------------|-------|
| 1 | Login HC → Home `/Home/Index` | ✅ PASS |
| 2 | CreateAssessment wizard 4-step renders | ✅ PASS — Step 1 active, 2/3/4 disabled |
| 3 | AssessmentMonitoringDetail URL composite key `?title=&category=&scheduleDate=` | ✅ PASS — matches helper navigation pattern |
| 4 | "Tambah Waktu" button visible | ✅ PASS — header section "Waktu Tambahan (Aksesibilitas)" |
| 5 | ExtraTime modal opens + dropdown 5-120 menit | ✅ PASS — values match extraTimeSelectors byte-for-byte |
| 6 | Modal action buttons (Batal + Tambah Waktu confirm) | ✅ PASS |

**Tidak ada temuan negatif.** Phase 317 ready close.
