---
phase: 318
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/Results.cshtml
  - tests/e2e/exam-types.spec.ts
  - docs/test-reports/2026-05-12-surf-317-a-fix.md
autonomous: true
requirements: [QA-08]
tags: [surf-317-a, production-fix, mvc-controller, razor, ma-results, tolookup, regression]

must_haves:
  truths:
    - "Controllers/CMPController.cs:2190 dipatch dari `packageResponses.ToDictionary(r => r.PackageQuestionId)` ke `packageResponses.ToLookup(r => r.PackageQuestionId)` — MA-aware multi-row-per-question semantik"
    - "Controllers/CMPController.cs lines 2209-2249 + 2244 + 2267-2273 + 2228 consumer code di-update ke MA-aware semantic (HashSet selectedOptionIds, SetEquals untuk MA correct, FirstOrDefault untuk MC)"
    - "Views/CMP/Results.cshtml lines 355-386 (option-level review loop) consumed via correctly-populated `OptionReviewItem.IsSelected` — controller line 2228 ubah dari `userResponse?.PackageOptionId == o.Id` ke `selectedOptionIds.Contains(o.Id)`"
    - "Results page GET /CMP/Results/{id} return 200 untuk MA assessment (tidak lagi 500 ArgumentException) — verified via existing Phase 317 FLOW K + FLOW M assessment Id"
    - "Phase 317 full regression suite HIJAU end-to-end: K (5/5 MA full cycle), L (6/6 Essay), M (5/5 Mixed), N (4/4 MC AllowAnswerReview=false), O (5/5 ExtraTime) — 27 sub-tests + setup = 28/28"
    - "Optional UI assertion upgrade — K5/M5 (FLOW K + FLOW M Results page) opsional convert DB-based queryScalar workaround ke UI .badge.text-bg-success assertion (kalau test code lebih bersih, BOLEH defer ke Phase 318 Plan 05 cleanup)"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Results action MA-aware aggregation via ToLookup"
      contains: "ToLookup"
    - path: "Views/CMP/Results.cshtml"
      provides: "OptionReviewItem loop unchanged jika controller correctly populates IsSelected; minor Razor adjust kalau perlu"
      contains: "IsSelected"
    - path: "docs/test-reports/2026-05-12-surf-317-a-fix.md"
      provides: "Production fix report — root cause, refactor diff, regression rerun stats"
      contains: "SURF-317-A"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "Hubs/AssessmentHub.cs"
      via: "PackageUserResponse multi-row insert per MA question matched via ToLookup grouping di Results action"
      pattern: "ToLookup"
    - from: "Controllers/CMPController.cs"
      to: "Views/CMP/Results.cshtml"
      via: "view-model populate OptionReviewItem.IsSelected via selectedOptionIds.Contains(o.Id) — HashSet match MA-aware"
      pattern: "selectedOptionIds\\.Contains"
    - from: "Views/CMP/Results.cshtml"
      to: "Controllers/CMPController.cs"
      via: "Results MA → 200 OK render — eliminates Phase 317 K5/M5 DB-based workaround"
      pattern: "ArgumentException"
---

<objective>
Tutup SURF-317-A (production code bug) carryover dari Phase 317 Plan 01: refactor `Controllers/CMPController.cs:2190` `packageResponses.ToDictionary(r => r.PackageQuestionId)` ke pola `ToLookup` + downstream consumer di lines 2209-2249/2244/2267-2273 di-update ke MA-aware semantic (HashSet `selectedOptionIds`, `SetEquals` untuk MA correct check, `FirstOrDefault` untuk MC). Update view-model populate `OptionReviewItem.IsSelected = selectedOptionIds.Contains(o.Id)` (line 2228) — supersede single-row equality semantik yang break untuk MA multi-row.

Purpose: Eliminate `ArgumentException: An item with the same key has already been added` untuk MA assessments. Phase 317 Plan 01 surfaced root cause (`Hubs/AssessmentHub.cs:240-249 SaveMultipleAnswer` insert N rows per MA question). Phase 317 pakai DB-based score verify (K5/M5) sebagai workaround — Plan 02 ini hapus kebutuhan workaround untuk depan Plan 03+ (FLOW S MC-only tidak perlu workaround, FLOW P PreTest/PostTest MC default).

Regression risk mitigation: rerun Phase 317 FLOW K (MA full cycle) + FLOW M (Mixed includes MA) + FLOW N (MC AllowAnswerReview=false) + FLOW L (Essay) + FLOW O (MC + ExtraTime SignalR). Semua 5 FLOW + Wave 0 smoke harus tetap HIJAU (28/28 sebelum + sesudah). MC semantic tidak boleh regress (HashSet single-item equivalent ke direct `==` untuk MC + Essay path).

Output:
- `Controllers/CMPController.cs:2190` patched (ToDictionary → ToLookup)
- `Controllers/CMPController.cs` consumer lines 2209-2249, 2228, 2244, 2267-2273 di-update MA-aware
- `Views/CMP/Results.cshtml` Razor markup verified (lines 355-386 — no change expected jika controller correctly populates IsSelected; minor adjust kalau Razor butuh foreach option pattern adjustment)
- Phase 317 regression suite 28/28 HIJAU post-fix
- `docs/test-reports/2026-05-12-surf-317-a-fix.md` fix report dengan root cause + diff + regression rerun stats
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-CONTEXT.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-01-SUMMARY.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md
@Controllers/CMPController.cs
@Views/CMP/Results.cshtml
@Hubs/AssessmentHub.cs
@tests/e2e/exam-types.spec.ts
@tests/e2e/helpers/examTypes.ts
@docs/DEV_WORKFLOW.md

<interfaces>
<!-- Current state — SURF-317-A bug site verified 2026-05-12. -->

From `Controllers/CMPController.cs:2185-2249` (BUG site — to be patched):
```csharp
// LINE 2190 — BUG: ToDictionary throws ArgumentException untuk MA multi-row-per-question
var responseDict = packageResponses.ToDictionary(r => r.PackageQuestionId);

// LINE 2209-2228 — consumer pattern (single-row assumption):
foreach (var question in questions)
{
    int qId = question.Id;
    responseDict.TryGetValue(qId, out var userResponse);
    var correctOption = question.Options.FirstOrDefault(o => o.IsCorrect);
    var selectedOption = userResponse?.PackageOptionId != null
        ? question.Options.FirstOrDefault(o => o.Id == userResponse.PackageOptionId)
        : null;
    bool isCorrect = selectedOption != null && selectedOption.IsCorrect;
    if (isCorrect) correctCount++;

    // LINE 2228 — IsSelected populate single-row equality:
    optionReviews.Add(new OptionReviewItem {
        Id = o.Id,
        Text = o.Text,
        IsCorrect = o.IsCorrect,
        IsSelected = userResponse?.PackageOptionId == o.Id   // ← MA breaks here
    });
}
```

From `Controllers/CMPController.cs:2267-2273` (ElemenTeknis scoring branch — additional MA-aware update needed):
```csharp
var correct = g.Count(q =>
{
    if (!responseDict.TryGetValue(q.Id, out var resp)) return false;
    var sel = q.Options.FirstOrDefault(o => o.Id == resp.PackageOptionId);
    return sel != null && sel.IsCorrect;
});
```

From `Hubs/AssessmentHub.cs:240-249` (ROOT CAUSE — multi-row insert per MA question):
```csharp
// SaveMultipleAnswer creates ONE PackageUserResponse PER selected option
// (e.g., MA Q with 2 correct answers → 2 rows sama PackageQuestionId)
foreach (var optionId in selectedOptionIds)
{
    db.PackageUserResponses.Add(new PackageUserResponse {
        AssessmentSessionId = sessionId,
        PackageQuestionId = questionId,
        PackageOptionId = optionId,
        // ...
    });
}
```

From `Views/CMP/Results.cshtml:316-399` (Razor — depends on OptionReviewItem.IsSelected):
- Line 355-365 loop: `@foreach (var option in question.Options)` — render badge per option based on `option.IsSelected` + `option.IsCorrect`
- Pattern: `@if (option.IsCorrect) { ... } else if (option.IsSelected && !option.IsCorrect) { ... }` — assumes IsSelected reflects user's choice(s)
- MA semantic: multiple options can have IsSelected=true (HashSet match). Markup loop sudah benar — yang penting controller populate correctly.

From Phase 317 Plan 01 SURF-317-A documentation (`317-01-SUMMARY.md:136-156`):
- Stack trace: `System.ArgumentException "An item with the same key has already been added"` at `System.Linq.Enumerable.ToDictionary` from `CMPController.cs:2190`
- Workaround used in K5/M5: `SELECT ISNULL(Score, -1) FROM AssessmentSessions WHERE Id = {sessionId}` (DB-based bypass)
- Fix recommendation: `var responseLookup = packageResponses.ToLookup(r => r.PackageQuestionId);`

From `docs/DEV_WORKFLOW.md`:
- Local-first verify (port 5277) sebelum commit
- Promosi ke Dev/Prod = Team IT (notif: commit hash + flag "no migration")
- Plan 02 = production code change (controller + Razor) — flag "no migration" required di commit body
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor `CMPController.cs:2190` ToDictionary → ToLookup + MA-aware consumer update (lines 2209-2273)</name>
  <files>
    Controllers/CMPController.cs
  </files>
  <read_first>
    - Controllers/CMPController.cs (lines 2140-2330 — Results action full context: signature, gating, view-model populate, ElemenTeknis branch)
    - Hubs/AssessmentHub.cs (lines 180-290 — SaveMultipleAnswer logic untuk root cause verification)
    - Views/CMP/Results.cshtml (lines 300-410 — depends on OptionReviewItem.IsSelected populate behavior)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 424-543 — SURF-317-A Production Fix Approach + Refactor Strategy + Regression Risk table)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-01-SUMMARY.md (lines 136-160 — SURF-317-A discovery + stack trace + workaround)
  </read_first>
  <behavior>
    - Sebelum patch: Identify exact line numbers di CMPController.cs Results action — line 2190 (ToDictionary), 2209-2249 (per-question consumer), 2228 (IsSelected populate), 2244 (AllowAnswerReview=false branch correct counting), 2267-2273 (ElemenTeknis aggregation)
    - Setelah patch: Semua consumer call site di-update ke MA-aware pattern:
      - `var userResponses = responseLookup[qId].ToList();` instead of `responseDict.TryGetValue(qId, out userResponse)`
      - `var selectedOptionIds = userResponses.Where(r => r.PackageOptionId != null).Select(r => r.PackageOptionId!.Value).ToHashSet();`
      - MA branch: `bool isCorrect = correctIds.SetEquals(selectedOptionIds);` (where `correctIds = correctOptions.Select(o => o.Id).ToHashSet()`)
      - MC + Essay branch: `bool isCorrect = single != null && single.IsCorrect;` (unchanged semantic untuk single-row case)
      - `IsSelected = selectedOptionIds.Contains(o.Id)` di OptionReviewItem populate (line 2228)
    - Local build gate: `dotnet build` exit 0 (zero warning baru — preserve existing baseline)
    - Local smoke gate: Manual test atau automated test (Phase 317 K5 query) Results MA assessment return 200 (bukan 500)
  </behavior>
  <action>
    **STEP 1 — Read full Results action context:**

    ```bash
    # Use Read tool dengan offset+limit untuk fokus:
    # Controllers/CMPController.cs lines 2140-2330 (Results action)
    ```

    Identify exact line numbers untuk:
    - ToDictionary call site (current line 2190)
    - per-question foreach loop start (~line 2200)
    - OptionReviewItem populate site (~line 2228)
    - AllowAnswerReview=false branch (~line 2244)
    - ElemenTeknis aggregation (~line 2267-2273)

    Line numbers di plan adalah baseline 2026-05-12 — actual line number bisa drift sedikit kalau ada file edit sejak research. Cross-reference dengan grep `\.ToDictionary\(r => r\.PackageQuestionId\)` untuk pinpoint.

    **STEP 2 — Patch line 2190 (primary fix):**

    BEFORE:
    ```csharp
    var responseDict = packageResponses.ToDictionary(r => r.PackageQuestionId);
    ```

    AFTER:
    ```csharp
    // SURF-317-A fix (Phase 318 Plan 02): ToDictionary throws ArgumentException
    // untuk MA multi-row-per-question (Hubs/AssessmentHub.cs:240-249 SaveMultipleAnswer
    // insert 1 row per selected option). ToLookup safe grouping.
    var responseLookup = packageResponses.ToLookup(r => r.PackageQuestionId);
    ```

    **STEP 3 — Refactor per-question consumer loop (line 2209-2249):**

    BEFORE (assume single-row semantic):
    ```csharp
    foreach (var question in questions)
    {
        int qId = question.Id;
        responseDict.TryGetValue(qId, out var userResponse);
        var correctOption = question.Options.FirstOrDefault(o => o.IsCorrect);
        var selectedOption = userResponse?.PackageOptionId != null
            ? question.Options.FirstOrDefault(o => o.Id == userResponse.PackageOptionId)
            : null;
        bool isCorrect = selectedOption != null && selectedOption.IsCorrect;
        if (isCorrect) correctCount++;

        // ... build OptionReviewItem ...
        optionReviews.Add(new OptionReviewItem {
            Id = o.Id,
            Text = o.Text,
            IsCorrect = o.IsCorrect,
            IsSelected = userResponse?.PackageOptionId == o.Id
        });
    }
    ```

    AFTER (MA-aware via HashSet):
    ```csharp
    foreach (var question in questions)
    {
        int qId = question.Id;
        var userResponses = responseLookup[qId].ToList(); // empty list if no answer
        var correctOptions = question.Options.Where(o => o.IsCorrect).ToList();
        var selectedOptionIds = userResponses
            .Where(r => r.PackageOptionId != null)
            .Select(r => r.PackageOptionId!.Value)
            .ToHashSet();

        // MA-correct semantics: user selected EXACTLY all-correct, none-incorrect
        bool isCorrect;
        if ((question.QuestionType ?? "MultipleChoice") == "MultipleAnswer")
        {
            var correctIds = correctOptions.Select(o => o.Id).ToHashSet();
            isCorrect = correctIds.SetEquals(selectedOptionIds);
        }
        else
        {
            // MC + Essay path: single PackageOptionId or null
            var single = question.Options.FirstOrDefault(o => selectedOptionIds.Contains(o.Id));
            isCorrect = single != null && single.IsCorrect;
        }
        if (isCorrect) correctCount++;

        // OptionReviewItem populate — MA-aware via HashSet match
        foreach (var o in question.Options)
        {
            optionReviews.Add(new OptionReviewItem {
                Id = o.Id,
                Text = o.Text,
                IsCorrect = o.IsCorrect,
                IsSelected = selectedOptionIds.Contains(o.Id)
            });
        }
    }
    ```

    **STEP 4 — Update line 2244 (AllowAnswerReview=false branch correct counting):**

    Same MA-aware logic — extract helper untuk DRY (optional):
    ```csharp
    private static bool IsResponseCorrect(PackageQuestion question, HashSet<int> selectedOptionIds)
    {
        if ((question.QuestionType ?? "MultipleChoice") == "MultipleAnswer")
        {
            var correctIds = question.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
            return correctIds.SetEquals(selectedOptionIds);
        }
        else
        {
            var sel = question.Options.FirstOrDefault(o => selectedOptionIds.Contains(o.Id));
            return sel != null && sel.IsCorrect;
        }
    }
    ```

    Replace inline duplication dengan helper call SI duplikasi terlalu painful. Optional refactor — boleh keep inline jika hanya 2-3 call sites.

    **STEP 5 — Update ElemenTeknis aggregation (line 2267-2273):**

    BEFORE:
    ```csharp
    var correct = g.Count(q =>
    {
        if (!responseDict.TryGetValue(q.Id, out var resp)) return false;
        var sel = q.Options.FirstOrDefault(o => o.Id == resp.PackageOptionId);
        return sel != null && sel.IsCorrect;
    });
    ```

    AFTER:
    ```csharp
    var correct = g.Count(q =>
    {
        var resp = responseLookup[q.Id].ToList();
        if (!resp.Any()) return false;
        var selectedIds = resp.Where(r => r.PackageOptionId != null)
                              .Select(r => r.PackageOptionId!.Value).ToHashSet();
        if ((q.QuestionType ?? "MultipleChoice") == "MultipleAnswer")
        {
            var correctIds = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
            return correctIds.SetEquals(selectedIds);
        }
        else
        {
            var sel = q.Options.FirstOrDefault(o => selectedIds.Contains(o.Id));
            return sel != null && sel.IsCorrect;
        }
    });
    ```

    **STEP 6 — Build gate:**

    ```bash
    dotnet build
    ```

    Expected: exit 0, no new warnings. JIKA error: review null-forgiving operator (`PackageOptionId!.Value`), nullable annotation, atau `ToHashSet()` import (System.Linq sudah default). Fix sebelum lanjut.

    **STEP 7 — Local smoke run (manual):**

    Optional — start `dotnet run` (`http://localhost:5277`), navigate ke existing MA assessment Results page (kalau ada test data dari Phase 317 belum di-cleanup), verify HTTP 200 + page render OK. Kalau tidak ada test data, skip manual smoke — automated regression (Task 2) akan cover.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Grep `Controllers/CMPController.cs` for `responseLookup` returns ≥3 lines (primary + consumer site + ElemenTeknis)
    - Grep `Controllers/CMPController.cs` for `\.ToDictionary\(r => r\.PackageQuestionId\)` returns 0 lines (old pattern fully replaced)
    - Grep `Controllers/CMPController.cs` for `selectedOptionIds\.Contains` returns ≥1 line (HashSet match populate IsSelected)
    - Grep `Controllers/CMPController.cs` for `SetEquals` returns ≥2 lines (MA correct check di Results action + ElemenTeknis)
    - `dotnet build` exit 0 (no new warnings vs baseline)
    - `Views/CMP/Results.cshtml` NOT modified at this step (Task 2 verifies Razor markup compat)
  </acceptance_criteria>
  <done>
    `Controllers/CMPController.cs:2190` + downstream consumer sites di-refactor ke ToLookup + MA-aware HashSet semantic. `dotnet build` HIJAU. Razor view tidak di-touch sampai Task 2 verification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Razor view audit `Views/CMP/Results.cshtml` + minor tweak kalau perlu</name>
  <files>
    Views/CMP/Results.cshtml
  </files>
  <read_first>
    - Views/CMP/Results.cshtml (lines 300-410 — full AllowAnswerReview branch + option-level review loop)
    - Controllers/CMPController.cs (lines 2200-2260 post-Task-1 state — verify OptionReviewItem populate consumer)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 516-533 — Razor view step 5 refactor recommendation)
  </read_first>
  <behavior>
    - Sebelum audit: Read full lines 300-410 untuk identify exact option-level loop semantik
    - Setelah audit: KALAU loop sudah benar (foreach option dengan IsSelected + IsCorrect branch), tidak ada change needed — controller populate-correctly is sufficient
    - KALAU loop ada branch yang assume single-row semantic (e.g., `@if (Model.UserAnswer == option.Id) { ... }`), refactor ke `@if (option.IsSelected) { ... }`
    - JANGAN add new ViewBag / new model field — view-model contract sudah cukup (OptionReviewItem.IsSelected populated by controller)
    - Manual visual verification dev DB: navigate `/CMP/Results/{maSessionId}` dari Phase 317 K assessment (kalau test data masih ada) → confirm semua option correct/wrong badge render benar untuk MA
  </behavior>
  <action>
    **STEP 1 — Read full `Views/CMP/Results.cshtml` lines 300-410:**

    Identify section:
    - Outer `@if (Model.AllowAnswerReview && Model.QuestionReviews != null) { ... <div class="card">"Tinjauan Jawaban"</div> }` (line ~316)
    - `else if (!Model.AllowAnswerReview) { ... <div class="alert alert-info">"Tinjauan jawaban tidak tersedia"</div> }` (line ~389)
    - Per-question section dengan option-level loop (line ~355-386)

    **STEP 2 — Evaluate option loop pattern:**

    Expected current pattern (Phase 317 working state — for MC only):
    ```cshtml
    @foreach (var option in question.Options)
    {
        @if (option.IsCorrect)
        {
            <span class="badge text-bg-success">@option.Text (Benar)</span>
        }
        else if (option.IsSelected && !option.IsCorrect)
        {
            <span class="badge text-bg-danger">@option.Text (Pilihan Anda — Salah)</span>
        }
        else
        {
            <span>@option.Text</span>
        }
    }
    ```

    **STEP 3 — Decision tree:**

    **Path A: Loop sudah benar (option.IsSelected branch tidak assume single-row):**
    - Tidak ada perubahan Razor needed. Skip ke Step 4 verify.

    **Path B: Loop ada branch yang assume single-row (e.g. comparing `Model.UserAnswerId == option.Id`):**
    - Refactor ke `option.IsSelected` based check.
    - Add comment header:
      ```cshtml
      @* SURF-317-A fix (Phase 318 Plan 02): option-level loop konsisten dengan
         controller MA-aware populate. IsSelected = HashSet match (MC/MA/Essay-safe). *@
      ```

    **STEP 4 — Manual visual verification (optional if dev test data exists):**

    Start `dotnet run`. Navigate ke `/CMP/Results/{phase317MASessionId}` (boleh pakai sessionId dari Phase 317 K test data kalau belum di-cleanup; alternatif: re-run Phase 317 K1-K4 manual sampai sessionId tersedia).

    Verifikasi:
    - Page status 200 (NOT 500)
    - MA question render dengan 2 option marked IsSelected (kalau user pilih 2 dari 4 correct options)
    - Badge `text-bg-success` untuk IsCorrect option
    - Badge `text-bg-danger` untuk IsSelected && !IsCorrect option (wrong choice)

    **STEP 5 — Save file (if changed):**

    Kalau Path A (no change): commit hanya CMPController.cs. Kalau Path B (Razor modified): commit kedua file bersama.

    **STEP 6 — Build re-verify:**

    ```bash
    dotnet build
    ```

    Expected: exit 0 (Razor compile clean).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `Views/CMP/Results.cshtml` audited — decision Path A (no change) atau Path B (refactor) documented di Task 3 report
    - Kalau Path B: grep `Views/CMP/Results.cshtml` for `option.IsSelected` returns ≥1 line (HashSet-aware branch)
    - Kalau Path B: grep `Views/CMP/Results.cshtml` for `Model.UserAnswerId` returns 0 lines (single-row assumption hilang)
    - `dotnet build` exit 0 post-Task-2
    - Razor view markup `<div class="card">"Tinjauan Jawaban"</div>` + `<div class="alert alert-info">"Tinjauan jawaban tidak tersedia"</div>` PRESERVED — Phase 317 FLOW N + future Phase 318 FLOW S relies on these markers (grep returns ≥1 each)
  </acceptance_criteria>
  <done>
    Razor view `Views/CMP/Results.cshtml` audit selesai. Path A (no change needed) atau Path B (refactor applied) documented. Build clean. Existing Phase 317 markers preserved untuk Plan 04 FLOW S consumption.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase 317 regression rerun gate + (optional) UI assertion upgrade K5/M5 + delta report</name>
  <files>
    tests/e2e/exam-types.spec.ts (optional — UI assertion upgrade kalau scope time-budget OK),
    docs/test-reports/2026-05-12-surf-317-a-fix.md
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (full file — current state setelah Phase 317 Plan 02 commit f23e77e6: smoke + K + L + M + N + O = 27 sub-tests)
    - tests/e2e/exam-types.spec.ts (focus K5 test — DB-based score verify pattern, untuk optional upgrade)
    - tests/e2e/exam-types.spec.ts (focus M5 test — sama dengan K5)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 534-545 — Regression Risk + Mitigation table)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md (lines 60-90 — Phase 317 verification stats: 28/28 in 1.9m)
  </read_first>
  <behavior>
    - Run `cd tests && npx playwright test exam-types.spec.ts --reporter=list` — expected 28/28 PASS post-fix (regression gate)
    - SI ada RED: identify which FLOW (K/L/M/N/O), investigate apakah refactor break MC semantic / MA semantic / ElemenTeknis aggregation. Rollback CMPController.cs change + re-attempt SI butuh
    - (Optional) Upgrade K5 + M5 DB-based score verify ke UI assertion `.badge.text-bg-success` — eliminates SURF-317-A workaround completely. Hanya lakukan kalau Plan 03+ time budget masih cukup; kalau tidak, defer ke Plan 05 cleanup
    - Generate `docs/test-reports/2026-05-12-surf-317-a-fix.md` fix report
    - TypeScript compile gate: `cd tests && npx tsc --noEmit` exit 0
  </behavior>
  <action>
    **STEP 1 — Regression rerun gate (CRITICAL):**

    ```bash
    cd tests && npx playwright test exam-types.spec.ts --reporter=list
    ```

    Expected: 28/28 PASS (1 setup + 27 sub-tests). Runtime ~1.9-2.5m (sedikit lebih lama karena Results page sekarang render 200 untuk MA, bukan instant 500 redirect).

    **JIKA RED:**
    - Identify which sub-test fail (K1-K5, L1-L6, M1-M5, N1-N4, O1-O5)
    - Likely break sites:
      - FLOW K K5 — DB-based assertion: kalau Score=100 tidak tercapai = MA correct logic regressed (SetEquals issue)
      - FLOW M M5 — DB-based assertion: kalau Score=100 tidak tercapai = Mixed (MC+MA+Essay) total aggregation regressed
      - FLOW N N3 — `.alert-info` visible: kalau redirect Results 500 = controller crash (build error masked, atau ElemenTeknis path break)
      - FLOW O O5 — `.badge text-bg-success`: kalau missing = Results render path issue
    - Investigate via `dotnet build` output (kalau warning Razor) + browser direct nav `/CMP/Results/{failedSessionId}` (kalau dev DB masih punya test data) untuk inspect 500 vs 200
    - Rollback option: revert `Controllers/CMPController.cs` ke pre-Task-1 state via git checkout HEAD~1 — re-attempt dengan smaller refactor (e.g., bila Razor view path A → ternyata butuh tweak, switch ke Path B)
    - Document fail + fix di SUMMARY.md deviation section

    **STEP 2 — (OPTIONAL — only if time budget allows) Upgrade K5 + M5 ke UI assertion:**

    Current K5 (DB-based, workaround SURF-317-A):
    ```typescript
    test('K5 - DB verify score 100', async () => {
        const score = await db.queryScalar(
            `SELECT ISNULL(Score, -1) FROM AssessmentSessions WHERE Id = ${sessionId}`
        );
        expect(score).toBe(100);
    });
    ```

    Optional upgrade (UI assertion):
    ```typescript
    test('K5 - Results page shows score 100 (UI assertion post SURF-317-A fix)', async ({ page }) => {
        await login(page, 'coachee');
        await page.goto(`/CMP/Results/${sessionId}`);
        await expect(page.locator('body')).toContainText(/100/);
        await expect(page.locator('.badge.text-bg-success').first()).toBeVisible({ timeout: 10_000 });

        // Verify SURF-317-A regression: page returns 200 (bukan 500)
        await expect(page.locator('h1, h2, h3').first()).toBeVisible(); // page rendered, bukan error page
    });
    ```

    **CRITICAL — kalau melakukan upgrade ini, tetap preserve DB-based query sebagai SECONDARY assertion (defense in depth):**
    ```typescript
    // Bonus DB cross-check (post-UI assertion)
    const dbScore = await db.queryScalar(
        `SELECT ISNULL(Score, -1) FROM AssessmentSessions WHERE Id = ${sessionId}`
    );
    expect(dbScore).toBe(100);
    ```

    Same upgrade pattern untuk M5. JANGAN upgrade L6 (Essay) atau N3 (negative) atau O5 — semua sudah pakai UI assertion atau tidak butuh.

    **STEP 3 — (Decision gate) Skip upgrade kalau:**
    - Regression rerun (Step 1) sudah 28/28 → workaround tidak harmful, defer upgrade
    - Plan 03+ time budget tinggi (FLOW P/Q/R/S = 13+ sub-tests baru)
    - Phase 318 Plan 05 sudah scope final regression gate — bisa tackle upgrade di sana

    Recommendation: SKIP upgrade kecuali eksekutor confident punya time. Defer ke Phase 318 Plan 05 cleanup atau dedicated Phase 320 refresh.

    **STEP 4 — Create `docs/test-reports/2026-05-12-surf-317-a-fix.md`:**

    Template:
    ```markdown
    # Phase 318 Plan 02 — SURF-317-A Production Fix Report

    **Date:** 2026-05-12
    **Phase:** 318
    **Plan:** 02
    **Scope:** SURF-317-A production code fix — `Controllers/CMPController.cs:2190` ToDictionary→ToLookup + MA-aware consumer

    ## Root Cause Recap

    `Hubs/AssessmentHub.cs:240-249 SaveMultipleAnswer` insert 1 `PackageUserResponse` row PER selected option. MA question dengan 2 correct answers = 2 rows sama `PackageQuestionId`. `Controllers/CMPController.cs:2190` `packageResponses.ToDictionary(r => r.PackageQuestionId)` throws `ArgumentException: An item with the same key has already been added`. Results page MA = 500.

    **Discovered:** Phase 317 Plan 01 FLOW K (workaround via DB-based score verify K5+M5).

    ## Refactor Diff

    ### `Controllers/CMPController.cs:2190` (primary)
    BEFORE:
    ```csharp
    var responseDict = packageResponses.ToDictionary(r => r.PackageQuestionId);
    ```
    AFTER:
    ```csharp
    var responseLookup = packageResponses.ToLookup(r => r.PackageQuestionId);
    ```

    ### Consumer pattern (`lines 2209-2249`)
    Pattern change: single-row `userResponse?.PackageOptionId` → multi-row `selectedOptionIds.HashSet` dengan MA-aware `SetEquals` branch + MC fallback `FirstOrDefault`.

    ### IsSelected populate (`line ~2228`)
    BEFORE: `IsSelected = userResponse?.PackageOptionId == o.Id`
    AFTER: `IsSelected = selectedOptionIds.Contains(o.Id)`

    ### ElemenTeknis aggregation (`lines ~2267-2273`)
    Same MA-aware refactor pattern.

    ### `Views/CMP/Results.cshtml` (audit result)
    Decision: **{Path A — no change / Path B — minor refactor applied}**. Markup `Tinjauan Jawaban` + `Tinjauan jawaban tidak tersedia` preserved untuk Phase 317 FLOW N + Phase 318 FLOW S downstream consumption.

    ## Regression Rerun Stats

    | Suite | Pre-fix | Post-fix |
    |-------|---------|----------|
    | exam-types.spec.ts | 28/28 (DB-based K5/M5) | 28/28 ({duration}) |
    | FLOW K (MA full cycle) | ✓ K5 DB-based | ✓ K5 {DB-based / UI assertion upgrade} |
    | FLOW M (Mixed) | ✓ M5 DB-based | ✓ M5 {DB-based / UI assertion upgrade} |
    | FLOW L (Essay) | ✓ | ✓ |
    | FLOW N (MC AllowAnswerReview=false) | ✓ N3 .alert-info | ✓ N3 .alert-info preserved |
    | FLOW O (MC ExtraTime SignalR) | ✓ O5 .badge | ✓ O5 .badge preserved |

    ## SURF-317-A Workaround Elimination Status

    - **K5 + M5 DB-based verify:** {DEFERRED to Plan 05 / UPGRADED in Task 3 Step 2}
    - Rationale: {time budget / defense-in-depth / etc}

    ## Build + Type Gate

    - `dotnet build`: exit 0
    - `cd tests && npx tsc --noEmit`: exit 0

    ## Production Promotion Notice

    Plan 02 = production code change (`Controllers/CMPController.cs` + optional `Views/CMP/Results.cshtml`). Per CLAUDE.md DEV_WORKFLOW:
    - Promosi ke Dev (10.55.3.3) = Team IT tanggung jawab
    - Notif: commit hash `{commit_hash}` + **flag "no migration"** (zero schema change, zero seed data change)

    ## Next Steps

    - Phase 318 Plan 03 (Wave 2 sequential) — FLOW P PrePostTest + FLOW Q EWCD reject
    - Phase 318 Plan 05 (final regression gate) — kalau K5/M5 DB-based workaround TIDAK di-upgrade di Plan 02, lakukan optional cleanup di Plan 05
    ```

    Replace placeholders dengan actual values.

    **STEP 5 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```

    Must exit 0.

    **STEP 6 — Commit:**
    ```bash
    git add Controllers/CMPController.cs Views/CMP/Results.cshtml docs/test-reports/2026-05-12-surf-317-a-fix.md
    # if K5/M5 upgrade: also stage tests/e2e/exam-types.spec.ts
    git commit -m "fix(318-02): SURF-317-A — CMPController Results ToLookup MA-aware (no migration)" -m "Phase 317 regression 28/28 HIJAU post-fix. Razor view {Path A no change / Path B minor adjust}. Production promotion: notify Team IT untuk Dev rollout."
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types.spec.ts --reporter=list && cd tests && npx tsc --noEmit && test -f docs/test-reports/2026-05-12-surf-317-a-fix.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd tests && npx playwright test exam-types.spec.ts` exit 0 dengan 28/28 sub-tests HIJAU (regression gate)
    - `dotnet build` exit 0 (production build clean)
    - `cd tests && npx tsc --noEmit` exit 0
    - File `docs/test-reports/2026-05-12-surf-317-a-fix.md` exists
    - Report contains "SURF-317-A", "Root Cause Recap", "Refactor Diff", "Regression Rerun Stats", "Production Promotion Notice" (grep verifiable)
    - Report contains BEFORE/AFTER code blocks dengan `ToDictionary` → `ToLookup`
    - Commit message contains "no migration" flag
    - (Optional) Kalau K5/M5 upgrade dilakukan: grep `tests/e2e/exam-types.spec.ts` for `.badge.text-bg-success` di K5 + M5 returns ≥2 lines
  </acceptance_criteria>
  <done>
    Phase 317 regression suite HIJAU 28/28 post-fix. SURF-317-A production code refactor verified via regression gate. Razor view audit decision documented. Fix report + commit message dengan "no migration" flag. (Optional) K5/M5 DB-based workaround di-upgrade ke UI assertion kalau time budget cukup.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → MVC controller | `/CMP/Results/{id}` GET masuk Results action — auth via cookies, owner/Admin/HC check |
| Controller → DB query | EF Core query `PackageUserResponses.Where(r.AssessmentSessionId = sessionId)` — projection ke ToLookup grouping di server-side |
| Controller → Razor view | OptionReviewItem.IsSelected populate via HashSet match — view-model contract preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-04 | Information Disclosure | SURF-317-A bug leaks 500 stack trace di dev mode (production mode strips) | mitigate | Plan 02 fix elimimnates 500. Defense in depth: confirm `ASPNETCORE_ENVIRONMENT=Production` di prod (per existing `appsettings.json`) |
| T-318-05 | Tampering | MA-aware refactor breaks MC semantic (regression) | mitigate | Task 3 regression rerun gate 28/28 — covers MC (FLOW N, O), MA (FLOW K), Mixed (FLOW M), Essay (FLOW L). MC HashSet single-item equivalent ke direct `==` (low risk per RESEARCH risk table) |
| T-318-06 | Denial of Service | Refactored consumer slower untuk MA dengan 10+ options (HashSet overhead) | accept | MA option count practically ≤4 per UI constraint. HashSet O(N) build + O(1) lookup tetap fast. Performance regression unlikely di production load |
| T-318-07 | Repudiation | Production fix tanpa migration commit metadata | mitigate | Commit message flag "no migration" + notify Team IT — preserves SOP per CLAUDE.md DEV_WORKFLOW |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# Task 1: Build gate
dotnet build

# Task 2: Razor build gate
dotnet build  # Razor compile included

# Task 3: Phase 317 regression gate (critical)
cd tests && npx playwright test exam-types.spec.ts --reporter=list

# TypeScript clean
cd tests && npx tsc --noEmit

# Grep verification
grep -n "ToLookup" Controllers/CMPController.cs
grep -n "selectedOptionIds.Contains" Controllers/CMPController.cs
grep -n "SetEquals" Controllers/CMPController.cs
grep -c "ToDictionary(r => r.PackageQuestionId)" Controllers/CMPController.cs  # expect 0
```

## Manual UAT (post-automation)

- Manual smoke: `dotnet run` lokal → login `hc` → navigate ManageAssessment → find Phase 317 MA assessment (kalau dev DB masih simpan) → click Results → verify page render 200, badge correct/wrong per option, total score visible
- (Recommended) Run 1x manual MA test cycle end-to-end: wizard → 2 MA questions → coachee submit → Results → verify visual rendering MA option-level review correct
- Setelah Plan 02 commit final: notify Team IT untuk rollout ke Dev (10.55.3.3) — flag "no migration" karena zero schema/seed change
</verification>

<success_criteria>
## Plan 02 Complete When

1. `Controllers/CMPController.cs:2190` patched `ToDictionary` → `ToLookup`
2. `Controllers/CMPController.cs` consumer sites (lines ~2209-2249, ~2228, ~2244, ~2267-2273) refactored ke MA-aware HashSet semantic
3. `Views/CMP/Results.cshtml` audited (Path A no-change atau Path B minor refactor) — markers `Tinjauan Jawaban` + `Tinjauan jawaban tidak tersedia` preserved
4. `dotnet build` exit 0
5. Phase 317 regression suite HIJAU 28/28: `npx playwright test exam-types.spec.ts`
6. `npx tsc --noEmit` exit 0
7. `docs/test-reports/2026-05-12-surf-317-a-fix.md` created dengan root cause + diff + regression stats + production promotion notice
8. (Optional, time-budget dependent) K5 + M5 DB-based workaround di-upgrade ke UI assertion `.badge.text-bg-success` — kalau skip, defer ke Plan 05
9. Commit: `fix(318-02): SURF-317-A — CMPController Results ToLookup MA-aware (no migration)` dengan body explaining regression rerun stats
</success_criteria>

<output>
After completion, create `.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-02-SUMMARY.md` mencakup:
- Files modified: `Controllers/CMPController.cs` (line numbers actual), `Views/CMP/Results.cshtml` (Path A/B + line range kalau touched), `docs/test-reports/2026-05-12-surf-317-a-fix.md` (new), (optional) `tests/e2e/exam-types.spec.ts` K5/M5 upgrade
- Refactor diff summary (ToDictionary → ToLookup + MA-aware HashSet pattern)
- Razor view audit decision (Path A no-change / Path B refactor) + rationale
- Phase 317 regression rerun stats — pre vs post fix, total + per-FLOW + runtime
- (Optional) K5/M5 UI assertion upgrade status (done / deferred to Plan 05)
- Any deviation dari RESEARCH refactor strategy (e.g., helper extraction skipped, ElemenTeknis inline preserved)
- Production promotion notice — commit hash + "no migration" flag untuk Team IT
- Next: Plan 03 (Wave 2 sequential — FLOW P + FLOW Q)
</output>
</content>
</invoke>