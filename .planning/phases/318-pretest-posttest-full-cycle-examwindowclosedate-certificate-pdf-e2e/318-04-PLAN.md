---
phase: 318
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - tests/e2e/helpers/examTypes.ts
  - tests/e2e/exam-types.spec.ts
autonomous: true
requirements: [QA-08]
tags: [e2e, playwright, exam-types, certificate-pdf, allow-answer-review, nomor-sertifikat, comparison]

must_haves:
  truths:
    - "Helper `verifyCertificatePdfDownload(page, sessionId)` ada di examTypes.ts — pakai `page.request.get('/CMP/CertificatePdf/{id}')` (cookies inherited), assert status=200 + content-type application/pdf + body bytes>1024, return `{ bytes, filename }`"
    - "FLOW R (Certificate PDF Download + NomorSertifikat, 5 sub-tests R1-R5) HIJAU: wizard generateCertificate=true + passPercentage=70 + 1 MC → worker correct answer → IsPassed=true → PDF download 200 + bytes>1024 + DB NomorSertifikat non-null"
    - "FLOW S (AllowAnswerReview True vs False Paired Comparison, 6 sub-tests S1-S6) HIJAU: 2 paired assessments — A (allowAnswerReview=true) → Results `.card 'Tinjauan Jawaban'` visible; B (allowAnswerReview=false) → Results `.alert-info 'Tinjauan jawaban tidak tersedia'` visible + `.card 'Tinjauan Jawaban'` count=0"
    - "Wave 0 verify YELLOW assumption A4 (CertificatePdf endpoint accessible via APIRequest dengan cookies-inherited dari page context) ter-confirm di R4"
    - "Post Plan 02 SURF-317-A fix: FLOW S menggunakan UI assertion langsung (no DB workaround) — MC-only tidak trigger SURF-317-A. FLOW R menggunakan UI assertion + DB cross-check"
  artifacts:
    - path: "tests/e2e/helpers/examTypes.ts"
      provides: "Extended with verifyCertificatePdfDownload helper"
      exports: ["verifyCertificatePdfDownload"]
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "Extended with FLOW R (5 sub-tests) + FLOW S (6 sub-tests)"
      contains: "FLOW R"
  key_links:
    - from: "tests/e2e/exam-types.spec.ts"
      to: "tests/e2e/helpers/examTypes.ts"
      via: "import verifyCertificatePdfDownload from examTypes"
      pattern: "verifyCertificatePdfDownload"
    - from: "tests/e2e/helpers/examTypes.ts"
      to: "Controllers/CMPController.cs"
      via: "GET /CMP/CertificatePdf/{id} → owner/Admin/HC auth → gating chain → File(pdf, 'application/pdf', filename)"
      pattern: "CertificatePdf"
    - from: "tests/e2e/exam-types.spec.ts"
      to: "Services/GradingService.cs"
      via: "SubmitExam path → NomorSertifikat generated sync via ExecuteUpdateAsync retry-3x"
      pattern: "NomorSertifikat"
    - from: "tests/e2e/exam-types.spec.ts"
      to: "Views/CMP/Results.cshtml"
      via: "Razor @if AllowAnswerReview branch — .card 'Tinjauan Jawaban' (true) vs .alert-info 'Tinjauan jawaban tidak tersedia' (false)"
      pattern: "Tinjauan"
---

<objective>
Implementasi FLOW R (Certificate PDF Download + NomorSertifikat Verify, 5 sub-tests R1-R5) + FLOW S (AllowAnswerReview True vs False Paired Comparison, 6 sub-tests S1-S6) sebagai append ke `tests/e2e/exam-types.spec.ts`. Tambah helper baru `verifyCertificatePdfDownload(page, sessionId)` yang pakai `page.request.get()` APIRequest pattern (cookies inherited dari page context — no multi-context needed). Wave 2 sequential setelah Plan 03 (FLOW P + Q) selesai.

Purpose: Tutup 2/4 FLOW QA-08 coverage gap terakhir (D-318-03 Cert PDF UI download + DB NomorSertifikat verify; D-318-04 AllowAnswerReview paired comparison). FLOW R + S both MC-only. FLOW S formally pairs positive (Phase 317 FLOW N sudah cover negative case) untuk explicit comparison documentation.

Output:
- `tests/e2e/helpers/examTypes.ts` extended dengan `verifyCertificatePdfDownload` helper
- `tests/e2e/exam-types.spec.ts` extended dengan FLOW R (5 sub-tests) + FLOW S (6 sub-tests) — total spec sekarang 38 + 11 = 49 sub-tests (target ≥40 per D-318-05 + RESEARCH `## Sampling Rate`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-CONTEXT.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-01-SUMMARY.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-02-SUMMARY.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-03-SUMMARY.md
@.planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md
@tests/e2e/exam-types.spec.ts
@tests/e2e/helpers/examTypes.ts
@tests/e2e/helpers/wizardSelectors.ts
@tests/helpers/utils.ts
@tests/helpers/dbSnapshot.ts
@Controllers/CMPController.cs
@Services/GradingService.cs
@Views/CMP/Results.cshtml

<interfaces>
<!-- Post Plan 03 state — helpers + selectors siap untuk Plan 04 consume. -->

From `tests/e2e/helpers/examTypes.ts` (post-Plan-03 state):
```typescript
// All Phase 317 exports (8) + Plan 03 export (1):
export async function createPrePostAssessmentViaWizard(page: Page, opts: CreatePrePostOpts): Promise<{ preIds; postIds }>;
// Plan 04 APPEND: verifyCertificatePdfDownload
```

From `Controllers/CMPController.cs:1898-2138` (CertificatePdf endpoint — verified 2026-05-12):
- Route: GET `/CMP/CertificatePdf/{id}` (sessionId path param)
- Auth check: owner OR Admin OR HC role (line 1906-1913 via `User.IsInRole` / `session.UserId == currentUser.Id`)
- Gating chain (line 1916-1933):
  1. `IsAssessmentSubmitted(Status)` → reject ke Assessment kalau tidak submitted
  2. `Status == "PendingGrading"` → redirect Results + TempData Info (kalau ada Essay un-graded)
  3. `!GenerateCertificate` → return `NotFound()` (line 1929)
  4. `IsPassed != true` → redirect Results + TempData Error (line 1931)
- Success (line 1935-1962): QuestPDF `Document.Create` → `pdf.GeneratePdf()` → `File(pdfStream.ToArray(), "application/pdf", filename)`
- Filename: `Sertifikat_{NIP}_{safeTitle}_{year}.pdf` (regex-sanitized title)
- Zero-byte guard (line 2118-2122): kalau pdf.GeneratePdf fail silently, log error + redirect Error

From `Services/GradingService.cs:288-322` (NomorSertifikat sync generation — verified 2026-05-12):
- Synchronous di SubmitExam path (await ExecuteUpdateAsync)
- Retry 3x WHERE NomorSertifikat IS NULL dengan sequence increment
- Test FLOW R can DB-query NomorSertifikat IMMEDIATELY post-submit

From `Views/CMP/Results.cshtml:316-399` (AllowAnswerReview Razor branch):
- `@if (Model.AllowAnswerReview && Model.QuestionReviews != null) { <div class="card">"Tinjauan Jawaban"...</div> }`
- `else if (!Model.AllowAnswerReview) { <div class="alert alert-info">"Tinjauan jawaban tidak tersedia"</div> }`
- POST-Plan-02 fix: OptionReviewItem.IsSelected populated via HashSet match (MC + MA + Essay safe)

From Phase 317 FLOW N (`exam-types.spec.ts` N1-N4 — Phase 317 Plan 02 final state):
- Negative assertion pattern: `.alert-info` containing "Tinjauan jawaban tidak tersedia" visible + `.card` containing "Tinjauan Jawaban" toHaveCount(0)
- Plan 04 FLOW S reuse pattern verbatim untuk negative branch (S5-S6), invert untuk positive branch (S2-S3)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append `verifyCertificatePdfDownload` helper ke examTypes.ts</name>
  <files>
    tests/e2e/helpers/examTypes.ts
  </files>
  <read_first>
    - tests/e2e/helpers/examTypes.ts (post-Plan-03 state — all 9 exports + Plan 03 createPrePostAssessmentViaWizard)
    - Controllers/CMPController.cs (lines 1898-1962 — CertificatePdf signature + response + filename)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 156-178 — verifyCertificatePdfDownload spec; lines 334-376 — FLOW R Critical Pitfalls)
  </read_first>
  <behavior>
    - Helper signature: `verifyCertificatePdfDownload(page: Page, sessionId: number): Promise<{ bytes: number; filename: string }>`
    - Strategy: `page.request.get('/CMP/CertificatePdf/{sessionId}')` — APIRequest inherits page context cookies (auth preserved)
    - Assertions: status===200, content-type startsWith 'application/pdf', content-disposition match /attachment.*Sertifikat_/i, body bytes>1024
    - Pre-condition: caller MUST be logged in (worker atau HC) — cookies sudah set di context
  </behavior>
  <action>
    **STEP 1 — Append helper di `tests/e2e/helpers/examTypes.ts`:**

    Append at end of file:
    ```typescript
    /**
     * Phase 318 Plan 04 — verify Certificate PDF download dari CertificatePdf endpoint.
     *
     * Pattern source: Controllers/CMPController.cs:1898-1962 (verified 2026-05-12).
     * Strategy: page.request.get() pakai APIRequest context — cookies inherited dari page
     * context, no multi-context needed. Bypass DOM/navigation untuk binary stream verification.
     *
     * Assertions:
     *  1. response.status() === 200 (kalau 4xx/5xx = auth fail atau gating reject)
     *  2. content-type header startsWith 'application/pdf'
     *  3. content-disposition match /attachment; filename=Sertifikat_/i
     *  4. body bytes > 1024 (minimum 1KB — guard against 0-byte regression per CMPController.cs:2118-2122)
     *
     * Pre-condition: caller MUST be logged in (cookies di page context).
     * Pre-condition: session.Status='Completed', IsPassed=true, GenerateCertificate=true.
     *
     * @param page worker atau HC page (cookies cookied)
     * @param sessionId AssessmentSession.Id
     * @returns { bytes: response body length, filename: parsed dari Content-Disposition }
     */
    export async function verifyCertificatePdfDownload(
      page: Page,
      sessionId: number
    ): Promise<{ bytes: number; filename: string }> {
      const response = await page.request.get(`/CMP/CertificatePdf/${sessionId}`);

      // Status assertion
      expect(response.status(), `CertificatePdf status (sessionId=${sessionId})`).toBe(200);

      // Content-Type assertion
      const contentType = response.headers()['content-type'] ?? '';
      expect(contentType, `Content-Type header`).toMatch(/^application\/pdf/i);

      // Content-Disposition + filename extraction
      const contentDisp = response.headers()['content-disposition'] ?? '';
      expect(contentDisp, `Content-Disposition header`).toMatch(/attachment.*Sertifikat_/i);
      const filenameMatch = contentDisp.match(/filename=(?:"([^"]+)"|([^;\s]+))/i);
      const filename = (filenameMatch?.[1] ?? filenameMatch?.[2] ?? '').trim();
      expect(filename, `Filename extracted from Content-Disposition`).toMatch(/^Sertifikat_/);

      // Body bytes assertion (minimum 1KB)
      const body = await response.body();
      expect(body.length, `PDF body bytes`).toBeGreaterThan(1024);

      return { bytes: body.length, filename };
    }
    ```

    **STEP 2 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```

    Expected: exit 0.

    **STEP 3 — Playwright dry-discovery:**

    ```bash
    cd tests && npx playwright test exam-types --list
    ```
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Grep `tests/e2e/helpers/examTypes.ts` for `export async function verifyCertificatePdfDownload` returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `page.request.get` di context CertificatePdf returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `toBeGreaterThan\(1024\)` returns 1 line (bytes assertion)
    - `cd tests && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Helper `verifyCertificatePdfDownload` ada di examTypes.ts dengan APIRequest pattern + 4 assertions (status/content-type/content-disposition/bytes). TypeScript clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FLOW R — Certificate PDF Download + NomorSertifikat Verify (5 sub-tests R1-R5)</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Plan-03 state — FLOW P + Q final structure)
    - tests/e2e/helpers/examTypes.ts (post-Task-1 — verifyCertificatePdfDownload signature)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 334-376 — FLOW R Skeleton + Critical Pitfalls; lines 911-1001 — FLOW R Skeleton verbatim code)
    - Controllers/CMPController.cs (lines 1898-1962 — CertificatePdf full action)
    - Services/GradingService.cs (lines 288-322 — NomorSertifikat sync generation)
  </read_first>
  <behavior>
    - R1: HC wizard create dengan `generateCertificate=true`, `passPercentage=70`, `allowAnswerReview=true`
    - R2: HC createDefaultPackage + 1 MC question (correct=A) score 100
    - R3: Worker take exam → correct answer → submit → DB verify Status='Completed' + IsPassed=true + Score=100
    - R4: Worker `verifyCertificatePdfDownload(page, sessionId)` — APIRequest cookies inherited → 200 + content-type + filename + bytes>1024
    - R5: DB verify NomorSertifikat populated (non-null + non-empty)
    - Title marker `[318-R]` + Q marker `[318-R] Q1` + correct option text `Human Capital`
  </behavior>
  <action>
    **STEP 1 — Update import line di `tests/e2e/exam-types.spec.ts`:**

    Tambah `verifyCertificatePdfDownload` ke existing import:
    ```typescript
    import {
      createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm, submitExamTwoStep,
      fillEssayAnswer, gradeSingleEssaySession, addExtraTimeViaModal,
      createPrePostAssessmentViaWizard, verifyCertificatePdfDownload,
      type QuestionInput, type CreatePrePostOpts,
    } from './helpers/examTypes';
    ```

    **STEP 2 — Append FLOW R describe block (setelah FLOW Q):**

    ```typescript
    test.describe('FLOW R — Certificate PDF Download + NomorSertifikat', () => {
      let title: string;
      let assessmentId: number;
      let packageId: number;
      let sessionId: number;
      const Q_MARKER = '[318-R] Q1 — Apa singkatan HC?';
      const Q_CORRECT = 'Human Capital';

      test('R1 — HC creates assessment with GenerateCertificate=true', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        title = uniqueTitle('[318-R] Cert Exam');
        await login(page, 'hc');
        await createAssessmentViaWizard(page, {
          title,
          category: 'OJT',
          scheduleDate: today(),
          scheduleTime: '00:01',
          durationMinutes: 60,
          passPercentage: 70,
          allowAnswerReview: true,
          generateCertificate: true,  // CRITICAL untuk Cert PDF gating chain pass
          participantEmails: ['rino.prasetyo@pertamina.com'],
        });
        const href = await page.locator('#modal-manage-btn').getAttribute('href');
        assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
        expect(assessmentId).toBeGreaterThan(0);
      });

      test('R2 — HC creates package + 1 MC question (correct=A) score 100', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto(`/Admin/ManagePackages?assessmentId=${assessmentId}`);
        await page.waitForLoadState('networkidle');
        packageId = await createDefaultPackage(page);
        await addQuestionViaForm(page, packageId, {
          type: 'MultipleChoice',
          text: Q_MARKER,
          options: [Q_CORRECT, 'Health Care', 'Hard Copy', 'Help Center'],
          correctIndex: 0,
          score: 100,
        });
      });

      test('R3 — Worker submits correct answer + DB Passed=true', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        const card = page.locator('.assessment-card, .card', { hasText: title }).first();
        await expect(card).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await card.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });
        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );

        sessionId = parseInt(page.url().match(/StartExam\/(\d+)/)![1], 10);
        expect(sessionId).toBeGreaterThan(0);

        // DOM-text match (anti-cheat shuffle) — Phase 317 pattern
        const qCard = page.locator('[id^="qcard_"]').filter({ hasText: Q_MARKER });
        await qCard.locator('label.list-group-item', { hasText: Q_CORRECT })
          .locator('input.exam-radio').check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i })
          .first().waitFor({ timeout: 7_500 });
        await submitExamTwoStep(page);

        // DB verify gating chain pre-conditions met
        const passed = await db.queryScalar(
          `SELECT CAST(IsPassed AS int) FROM AssessmentSessions WHERE Id = ${sessionId}`
        );
        expect(passed).toBe(1);

        const status = await db.queryScalar(
          `SELECT Status FROM AssessmentSessions WHERE Id = ${sessionId}`
        );
        expect(status).toBe('Completed');

        const score = await db.queryScalar(
          `SELECT ISNULL(Score, -1) FROM AssessmentSessions WHERE Id = ${sessionId}`
        );
        expect(score).toBe(100);
      });

      test('R4 — Worker downloads Certificate PDF via APIRequest (Wave 0 verify A4)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');  // ensure fresh cookies di page context
        const result = await verifyCertificatePdfDownload(page, sessionId);
        expect(result.bytes).toBeGreaterThan(1024);
        expect(result.filename).toMatch(/^Sertifikat_/);
        // eslint-disable-next-line no-console
        console.log(`[FLOW R4] PDF download OK — bytes=${result.bytes}, filename=${result.filename}`);
      });

      test('R5 — DB verify NomorSertifikat populated (sync generation)', async () => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        const nomor = await db.queryScalar(
          `SELECT ISNULL(NomorSertifikat, '') FROM AssessmentSessions WHERE Id = ${sessionId}`
        );
        expect(typeof nomor).toBe('string');
        expect((nomor as string).length).toBeGreaterThan(0);
        // eslint-disable-next-line no-console
        console.log(`[FLOW R5] NomorSertifikat=${nomor}`);
      });
    });
    ```

    **STEP 3 — Run FLOW R isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW R"
    ```

    Expected: 5/5 PASS.

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | R3 IsPassed=0 | Pass percentage 70 not reached — score < 70 | Check Q score=100, correctIndex=0, worker did pick `Human Capital`. Re-verify DOM-text match locator |
    | R4 status 302 redirect (not 200) | Gating chain reject — likely IsPassed=false OR GenerateCertificate=false | Verify R1 generateCertificate=true checked di wizard. Verify R3 passed assertion passed |
    | R4 status 404 NotFound | GenerateCertificate flag tidak persisted di DB | DB query `SELECT GenerateCertificate FROM Assessments WHERE Id={assessmentId}` — expect 1; kalau 0 = wizard checkbox tidak ter-fire |
    | R4 bytes ≤ 1024 | PDF generation gagal silent (0-byte regression) | Check `dotnet run` console logs untuk QuestPDF error. Likely missing font (graceful fallback didn't fire). Investigate `wwwroot/fonts/*.ttf` |
    | R5 NomorSertifikat empty | Race — query immediately post-submit BEFORE GradingService finalize | Per RESEARCH A6 GREEN, sync via `await ExecuteUpdateAsync` — populated atomic. Kalau empty, investigate retry-3x branch failure |

    **STEP 4 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "FLOW R"</automated>
  </verify>
  <acceptance_criteria>
    - 5 sub-tests R1-R5 PASSED dalam describe `FLOW R — Certificate PDF Download + NomorSertifikat`
    - Grep `tests/e2e/exam-types.spec.ts` for `'FLOW R — Certificate` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `R1 —`, `R2 —`, `R3 —`, `R4 —`, `R5 —` returns 5 lines
    - R3: DB IsPassed=1, Status='Completed', Score=100 verified
    - R4: PDF response 200 + content-type application/pdf + filename starts `Sertifikat_` + bytes>1024 verified
    - R5: NomorSertifikat non-null + non-empty verified
    - `[318-R]` title marker ter-present
    - Per-sub-test runtime ≤ 60s
  </acceptance_criteria>
  <done>
    FLOW R (5 sub-tests) HIJAU end-to-end. Cert PDF download via APIRequest cookies-inherited verified. NomorSertifikat sync generation di SubmitExam path confirmed via DB query. Gating chain (Submitted + !PendingGrading + GenerateCertificate + IsPassed) traversed berhasil.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FLOW S — AllowAnswerReview True vs False Paired Comparison (6 sub-tests S1-S6)</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Task-2 state — focus existing FLOW N N1-N4 untuk negative pattern reuse)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-RESEARCH.md (lines 378-421 — FLOW S Skeleton + Critical Pitfalls; lines 1005-1043 — FLOW S Skeleton verbatim)
    - Views/CMP/Results.cshtml (lines 316-399 — Razor branch toggle)
    - .planning/phases/317-fix-surf-316-a-ma-essay-mixed-e2e-via-ui/317-02-SUMMARY.md (FLOW N positive assertion idiom)
  </read_first>
  <behavior>
    - 6 sub-tests total — 3 untuk assessment A (allowAnswerReview=true) + 3 untuk assessment B (allowAnswerReview=false)
    - S1 (A wizard+package+question, allowAnswerReview=true), S2 (worker submit + sessTrue), S3 (Results positive — `.card "Tinjauan Jawaban"` visible)
    - S4 (B wizard+package+question, allowAnswerReview=false), S5 (worker submit + sessFalse), S6 (Results negative — `.alert-info "Tinjauan jawaban tidak tersedia"` visible + `.card` count=0)
    - Both MC-only (no SURF-317-A trigger) — boleh pakai UI assertion langsung
    - Title markers `[318-S-TRUE]` + `[318-S-FALSE]` untuk visual separation
  </behavior>
  <action>
    **STEP 1 — Append FLOW S describe block (setelah FLOW R):**

    ```typescript
    test.describe('FLOW S — AllowAnswerReview True vs False Paired Comparison', () => {
      let titleTrue: string;
      let titleFalse: string;
      let aIdTrue: number;
      let aIdFalse: number;
      let pkgTrue: number;
      let pkgFalse: number;
      let sessTrue: number;
      let sessFalse: number;
      const Q_TRUE_MARKER = '[318-S-TRUE] Q1 — Pilih jawaban benar';
      const Q_FALSE_MARKER = '[318-S-FALSE] Q1 — Pilih jawaban benar';

      test('S1 — HC creates assessment A (allowAnswerReview=true) + package + MC question', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        titleTrue = uniqueTitle('[318-S-TRUE] Review Exam');
        await login(page, 'hc');
        await createAssessmentViaWizard(page, {
          title: titleTrue,
          category: 'OJT',
          scheduleDate: today(),
          scheduleTime: '00:01',
          durationMinutes: 60,
          passPercentage: 70,
          allowAnswerReview: true,  // POSITIVE branch
          generateCertificate: false,
          participantEmails: ['rino.prasetyo@pertamina.com'],
        });
        const href = await page.locator('#modal-manage-btn').getAttribute('href');
        aIdTrue = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);

        await page.goto(`/Admin/ManagePackages?assessmentId=${aIdTrue}`);
        await page.waitForLoadState('networkidle');
        pkgTrue = await createDefaultPackage(page);
        await addQuestionViaForm(page, pkgTrue, {
          type: 'MultipleChoice',
          text: Q_TRUE_MARKER,
          options: ['Correct', 'Wrong-1', 'Wrong-2', 'Wrong-3'],
          correctIndex: 0,
          score: 100,
        });
      });

      test('S2 — Worker submits A → captures sessTrue', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        const card = page.locator('.assessment-card, .card', { hasText: titleTrue }).first();
        await expect(card).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await card.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });
        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );
        sessTrue = parseInt(page.url().match(/StartExam\/(\d+)/)![1], 10);

        const qCard = page.locator('[id^="qcard_"]').filter({ hasText: Q_TRUE_MARKER });
        await qCard.locator('label.list-group-item', { hasText: 'Correct' })
          .locator('input.exam-radio').check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i })
          .first().waitFor({ timeout: 7_500 });
        await submitExamTwoStep(page);
      });

      test('S3 — Results A: .card "Tinjauan Jawaban" VISIBLE (positive)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto(`/CMP/Results/${sessTrue}`);

        // Score visible (100)
        await expect(page.locator('body')).toContainText(/100/);

        // POSITIVE: .card containing "Tinjauan Jawaban" visible
        await expect(
          page.locator('.card', { hasText: /Tinjauan Jawaban/i }).first()
        ).toBeVisible({ timeout: 10_000 });

        // Optional: list-group-item count = 1 (1 question)
        await expect(
          page.locator('.card', { hasText: /Tinjauan Jawaban/i }).first()
            .locator('.list-group-item')
        ).toHaveCount(1, { timeout: 5_000 });

        // NEGATIVE check: alert-info "tidak tersedia" TIDAK ada
        await expect(
          page.locator('.alert-info', { hasText: /Tinjauan jawaban tidak tersedia/i })
        ).toHaveCount(0);
      });

      test('S4 — HC creates assessment B (allowAnswerReview=false) + package + MC question', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        titleFalse = uniqueTitle('[318-S-FALSE] NoReview Exam');
        await login(page, 'hc');
        await createAssessmentViaWizard(page, {
          title: titleFalse,
          category: 'OJT',
          scheduleDate: today(),
          scheduleTime: '00:01',
          durationMinutes: 60,
          passPercentage: 70,
          allowAnswerReview: false,  // NEGATIVE branch — key parameter
          generateCertificate: false,
          participantEmails: ['rino.prasetyo@pertamina.com'],
        });
        const href = await page.locator('#modal-manage-btn').getAttribute('href');
        aIdFalse = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);

        await page.goto(`/Admin/ManagePackages?assessmentId=${aIdFalse}`);
        await page.waitForLoadState('networkidle');
        pkgFalse = await createDefaultPackage(page);
        await addQuestionViaForm(page, pkgFalse, {
          type: 'MultipleChoice',
          text: Q_FALSE_MARKER,
          options: ['Correct', 'Wrong-1', 'Wrong-2', 'Wrong-3'],
          correctIndex: 0,
          score: 100,
        });
      });

      test('S5 — Worker submits B → captures sessFalse', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        const card = page.locator('.assessment-card, .card', { hasText: titleFalse }).first();
        await expect(card).toBeVisible({ timeout: 10_000 });
        page.once('dialog', d => d.accept());
        await card.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard').first().click();
        await page.waitForURL(/\/CMP\/StartExam\/\d+/, { timeout: 15_000 });
        await page.waitForFunction(
          () => {
            const w = window as unknown as { assessmentHub?: { state?: string } };
            return w.assessmentHub?.state === 'Connected';
          },
          undefined,
          { timeout: 10_000 }
        );
        sessFalse = parseInt(page.url().match(/StartExam\/(\d+)/)![1], 10);

        const qCard = page.locator('[id^="qcard_"]').filter({ hasText: Q_FALSE_MARKER });
        await qCard.locator('label.list-group-item', { hasText: 'Correct' })
          .locator('input.exam-radio').check();
        await page.locator('#saveIndicatorText').filter({ hasText: /saved|tersimpan/i })
          .first().waitFor({ timeout: 7_500 });
        await submitExamTwoStep(page);
      });

      test('S6 — Results B: .alert-info "Tinjauan jawaban tidak tersedia" VISIBLE + .card count=0 (negative)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto(`/CMP/Results/${sessFalse}`);

        // Score visible (100)
        await expect(page.locator('body')).toContainText(/100/);

        // NEGATIVE: .alert-info containing "Tinjauan jawaban tidak tersedia" visible (Phase 317 FLOW N pattern)
        await expect(
          page.locator('.alert-info, .alert.alert-info', { hasText: /Tinjauan jawaban tidak tersedia/i })
        ).toBeVisible({ timeout: 10_000 });

        // NEGATIVE check: .card "Tinjauan Jawaban" TIDAK ada
        await expect(
          page.locator('.card', { hasText: /^Tinjauan Jawaban/i })
        ).toHaveCount(0);
      });
    });
    ```

    **STEP 2 — Run FLOW S isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW S"
    ```

    Expected: 6/6 PASS.

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | S3 `.card "Tinjauan Jawaban"` not visible | AllowAnswerReview flag tidak persisted, OR Razor branch logic edge case | DB query `SELECT AllowAnswerReview FROM AssessmentSessions WHERE Id=${sessTrue}` — expect 1. Verify Plan 02 SURF-317-A fix tidak break view-model populate |
    | S3 list-group-item count != 1 | Multiple questions di package | Verify hanya 1 question added di S1 |
    | S6 `.alert-info` not visible | AllowAnswerReview flag mis-persisted, OR Plan 02 Razor view edit broke marker | Cross-reference Plan 02 SUMMARY untuk Path A/B audit decision. Kalau Path B applied, verify markers preserved |
    | S6 `.card` count > 0 | Razor branch logic show both — unexpected | Verify Views/CMP/Results.cshtml @if @else if structure tidak rusak |

    **STEP 3 — Run full Plan 04 suite (FLOW R + S):**

    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW R|FLOW S"
    ```

    Expected: 11/11 PASS (5 R + 6 S).

    **STEP 4 — Cumulative regression (full Phase 318 suite up to Plan 04):**

    ```bash
    cd tests && npx playwright test exam-types.spec.ts --reporter=list
    ```

    Expected: 49/49 (1 setup + 27 Phase 317 + 10 Plan 03 + 11 Plan 04).

    **STEP 5 — TypeScript compile gate + commit:**

    ```bash
    cd tests && npx tsc --noEmit
    git add tests/e2e/exam-types.spec.ts tests/e2e/helpers/examTypes.ts
    git commit -m "feat(318-04): FLOW R Cert PDF + FLOW S AllowAnswerReview compare hijau (11/11 — QA-08)"
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "FLOW R|FLOW S"</automated>
  </verify>
  <acceptance_criteria>
    - 6 sub-tests S1-S6 PASSED dalam describe `FLOW S — AllowAnswerReview True vs False Paired Comparison`
    - Grep `tests/e2e/exam-types.spec.ts` for `'FLOW S — AllowAnswerReview` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `S1 —`, `S2 —`, `S3 —`, `S4 —`, `S5 —`, `S6 —` returns 6 lines
    - S3: `.card` containing "Tinjauan Jawaban" visible PASS + `.alert-info "tidak tersedia"` count=0
    - S6: `.alert-info "tidak tersedia"` visible PASS + `.card "Tinjauan Jawaban"` count=0
    - Full Plan 04 suite (FLOW R + FLOW S) = 11 sub-tests PASS
    - Cumulative regression `npx playwright test exam-types.spec.ts` = 49/49 PASS
    - `cd tests && npx tsc --noEmit` exit 0
    - `[318-S-TRUE]` + `[318-S-FALSE]` title markers ter-present
  </acceptance_criteria>
  <done>
    FLOW S (6 sub-tests) HIJAU end-to-end. AllowAnswerReview true vs false paired comparison verified. Razor branch toggle `Views/CMP/Results.cshtml:316-399` confirmed working post Plan 02 SURF-317-A fix. Full Plan 04 (FLOW R + S = 11 sub-tests) HIJAU. Cumulative Phase 318 regression = 49/49 HIJAU.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker page context → CertificatePdf endpoint | APIRequest dengan cookies-inherited untuk binary stream download |
| Test runner → dev DB | FLOW R creates 1 cert assessment + session; FLOW S creates 2 assessments + 2 sessions (paired comparison) |
| Worker → Results page | Razor branch render based on AllowAnswerReview persisted flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-12 | Tampering | A4 YELLOW assumption fail — APIRequest cookies tidak inherited | mitigate | R4 explicit verify status=200. JIKA RED, fallback: instantiate `await context.request.newContext({ storageState: ... })` dari `page.context().storageState()` |
| T-318-13 | Information Disclosure | CertificatePdf download exposes PII (NIP, peserta name) di filename + body | accept | Test pakai dev creds — known dev identifier. Production endpoint sudah authorized via owner/Admin/HC check (line 1906-1913) |
| T-318-14 | Tampering | Plan 02 SURF-317-A fix break Razor markers untuk FLOW S | mitigate | Cross-reference Plan 02 Path A/B audit decision. Plan 04 verify markers preserved via assertion. JIKA marker hilang, rollback Plan 02 + reattempt dengan smaller refactor |
| T-318-15 | Denial of Service | PDF generation slow untuk QuestPDF complex layout | accept | Test timeout 60s per sub-test sufficient. PDF size ~5-20KB typical (QuestPDF 1-page cert) |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# Task 1: Helper foundation
cd tests && npx tsc --noEmit

# Task 2: FLOW R isolated
cd tests && npx playwright test exam-types --grep "FLOW R"

# Task 3: FLOW S isolated
cd tests && npx playwright test exam-types --grep "FLOW S"

# Combined Plan 04 suite
cd tests && npx playwright test exam-types --grep "FLOW R|FLOW S"

# Cumulative regression (Phase 317 + Plan 03 + Plan 04 = 49/49)
cd tests && npx playwright test exam-types.spec.ts --reporter=list
```

## Manual UAT (post-automation)

- Manual: HC wizard generateCertificate=true → coachee submit correct → click "Download Sertifikat" button di Results page → verify PDF downloads + opens normally
- Manual: 2 assessments paired (allowAnswerReview true/false) → coachee submit both → visual confirmation Results page render different (review card vs alert-info)
- Setelah commit: cleanup dev DB hapus rows prefix `[318-R]` + `[318-S-TRUE]` + `[318-S-FALSE]` per CLAUDE.md SEED_WORKFLOW
</verification>

<success_criteria>
## Plan 04 Complete When

1. `tests/e2e/helpers/examTypes.ts` extended dengan `verifyCertificatePdfDownload` helper (APIRequest pattern)
2. FLOW R HIJAU: 5 sub-tests R1-R5 — wizard generateCertificate=true → correct answer → PDF download 200/bytes>1024 → DB NomorSertifikat non-null
3. FLOW S HIJAU: 6 sub-tests S1-S6 — paired comparison true (positive review card) vs false (negative alert-info)
4. Full Plan 04 suite (FLOW R + S) = 11 sub-tests PASS
5. Cumulative regression `npx playwright test exam-types.spec.ts` 49/49 HIJAU
6. `npx tsc --noEmit` exit 0
7. Wave 0 YELLOW assumption A4 (APIRequest cookies inherited) ter-confirm di R4
8. Commit: `feat(318-04): FLOW R Cert PDF + FLOW S AllowAnswerReview compare hijau (11/11 — QA-08)`
</success_criteria>

<output>
After completion, create `.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-04-SUMMARY.md` mencakup:
- Files modified: `examTypes.ts` (verifyCertificatePdfDownload), `exam-types.spec.ts` (FLOW R + S)
- FLOW R runtime per sub-test + total
- FLOW S runtime per sub-test + total
- R4 PDF download stats: bytes range observed, filename pattern matched (e.g., `Sertifikat_{NIP}_{safe_title}_2026.pdf`)
- R5 NomorSertifikat format observed (untuk reference, BUKAN brittle assertion)
- S3/S6 Razor branch render verification — markers preserved post Plan 02 SURF-317-A fix
- Any deviation dari RESEARCH (e.g., APIRequest cookies inheritance edge case, Plan 02 Path B Razor adjustment impact ke FLOW S)
- Cumulative regression stats: 49/49 vs baseline 38/38 vs Phase 317 28/28
- Next: Plan 05 (final regression gate + QA-08 docs)
</output>
</content>
</invoke>