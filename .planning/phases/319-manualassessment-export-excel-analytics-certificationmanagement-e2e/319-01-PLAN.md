---
phase: 319
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/helpers/examTypes.ts
  - tests/e2e/exam-types.spec.ts
autonomous: true
requirements: [QA-09]
tags: [e2e, playwright, exam-types, manual-assessment, tomselect, helpers, wave-0]

must_haves:
  truths:
    - "Helper `verifyExcelDownload(page, endpointPath, opts)` ada di examTypes.ts — APIRequest pattern adaptasi dari `verifyCertificatePdfDownload`: status 200 + Excel MIME (`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` ATAU `application/vnd.ms-excel`) + content-disposition `attachment.*\\.xlsx` + bytes > minBytes (default 2048; pre-eksekusi user dapat lowering ke 256 untuk header-only DB)"
    - "Helper `interceptAnalyticsResponse(page, action, endpointMatcher)` ada di examTypes.ts — wrap `page.waitForResponse` + JSON parse + return generic `T extends Record<string, unknown>` shape; `endpointMatcher` accept string substring atau RegExp; timeout 15s"
    - "Interface `AnalyticsResponseShape` exported (totalSessions, passRate, expiringCount, avgGainScore — verified dari CMPController.cs:2731-2813)"
    - "FLOW T (ManualAssessment Full CRUD, 6 sub-tests T1-T6 + Wave 0 W0.T0 smoke) HIJAU: navigate AddManualAssessment → TomSelect pick worker → submit form → DB INSERT verify (IsManualEntry=1, Status='Completed') → Edit (Score update) → Worker view di /CMP/Assessment → Delete → DB row removed"
    - "Wave 0 W0.T0 smoke ter-confirm RESEARCH A3 (TomSelect `.ts-control` interaction pattern works untuk `#WorkerSelect` di AddManualAssessment form)"
    - "Title marker `[319-T]` ter-present di semua 6 sub-tests untuk dev DB grep-able cleanup"
    - "Cumulative regression Plan 01: 49 baseline + 7 (W0.T0 + T1-T6) = 56 sub-tests HIJAU end-to-end"
  artifacts:
    - path: "tests/e2e/helpers/examTypes.ts"
      provides: "Extended dengan verifyExcelDownload + interceptAnalyticsResponse + AnalyticsResponseShape"
      exports: ["verifyExcelDownload", "interceptAnalyticsResponse", "AnalyticsResponseShape"]
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "Extended dengan Wave 0 W0.T0 smoke + FLOW T (6 sub-tests T1-T6)"
      contains: "FLOW T"
  key_links:
    - from: "tests/e2e/exam-types.spec.ts"
      to: "tests/e2e/helpers/examTypes.ts"
      via: "import verifyExcelDownload, interceptAnalyticsResponse — staged di Plan 01 untuk dipakai Plan 03"
      pattern: "verifyExcelDownload|interceptAnalyticsResponse"
    - from: "tests/e2e/exam-types.spec.ts (FLOW T)"
      to: "Controllers/TrainingAdminController.cs:555-740"
      via: "GET /TrainingAdmin/AddManualAssessment (Razor) → POST IsManualEntry=true → INSERT AssessmentSessions → redirect /AssessmentAdmin/ManageAssessment?tab=training"
      pattern: "AddManualAssessment|EditManualAssessment|DeleteManualAssessment"
    - from: "tests/e2e/exam-types.spec.ts (FLOW T2)"
      to: "Views/Admin/AddManualAssessment.cshtml:283-388"
      via: "TomSelect `.ts-control` interaction → addWorkerRow JS → `.worker-cert-card` rendered → `#btnSimpanAssessment` enabled"
      pattern: "WorkerSelect|btnSimpanAssessment"
    - from: "tests/e2e/exam-types.spec.ts (FLOW T5)"
      to: "Controllers/CMPController.cs (Assessment list)"
      via: "Worker context navigate /CMP/Assessment → ManualAssessment visible di Completed tab, no Start button"
      pattern: "CMP/Assessment"
---

<objective>
Implementasi Wave 0 W0.T0 smoke + FLOW T (ManualAssessment Full CRUD, 6 sub-tests T1-T6) sebagai append ke `tests/e2e/exam-types.spec.ts`. Pre-append 2-3 helpers baru di `tests/e2e/helpers/examTypes.ts` (`verifyExcelDownload` + `interceptAnalyticsResponse` + `AnalyticsResponseShape`) untuk staging — helpers dipakai Plan 03 (Wave 2) tetapi di-build sekarang sebagai Wave 1 work supaya Plan 03 cukup consume tanpa helper-ext task duplikat.

Purpose: Tutup 1/5 FLOW QA-09 coverage (D-319-07 ManualAssessment full CRUD via UI). Wave 0 W0.T0 resolve RESEARCH A3 YELLOW assumption (TomSelect interaction pattern). Stage helpers `verifyExcelDownload` + `interceptAnalyticsResponse` ready untuk Plan 03 consume (depends_on chain: 03 depends on 01 untuk helpers).

Output:
- `tests/e2e/helpers/examTypes.ts` extended dengan `verifyExcelDownload` + `interceptAnalyticsResponse` + `AnalyticsResponseShape` interface (~70-80 LOC append)
- `tests/e2e/exam-types.spec.ts` extended dengan describe Wave 0 W0 smoke (1 sub-test W0.T0) + describe FLOW T (6 sub-tests T1-T6) — total spec sekarang 49 + 7 = 56 sub-tests
- Per-sub-test runtime ≤ 120s (FLOW_TIMEOUT_MS shared)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-CONTEXT.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-04-SUMMARY.md
@tests/e2e/exam-types.spec.ts
@tests/e2e/helpers/examTypes.ts
@tests/e2e/helpers/wizardSelectors.ts
@tests/helpers/utils.ts
@tests/helpers/dbSnapshot.ts
@Controllers/TrainingAdminController.cs
@Views/Admin/AddManualAssessment.cshtml
@Views/Admin/EditManualAssessment.cshtml
@Views/Admin/Shared/_TrainingRecordsTab.cshtml

<interfaces>
<!-- Post-Phase-318 state — examTypes.ts has 10 exports termasuk verifyCertificatePdfDownload. -->
<!-- Plan 01 APPEND tambahan: verifyExcelDownload + interceptAnalyticsResponse + AnalyticsResponseShape -->

From `tests/e2e/helpers/examTypes.ts:600-621` (existing — pattern reference untuk Excel adaptation):
```typescript
export async function verifyCertificatePdfDownload(
  page: Page,
  sessionId: number
): Promise<{ bytes: number; filename: string }> {
  const response = await page.request.get(`/CMP/CertificatePdf/${sessionId}`);
  expect(response.status(), ...).toBe(200);
  // ... content-type, content-disposition, body bytes assertions
}
```

From `Controllers/TrainingAdminController.cs:555-740` (verified 2026-05-12):
- GET `/TrainingAdmin/AddManualAssessment` → Razor form
- POST `/TrainingAdmin/AddManualAssessment` (line 566) → INSERT AssessmentSessions dengan `IsManualEntry=true`, `Status='Completed'` (line 603-628)
- Redirect: `RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" })` (line 637)
- TempData["Success"]: "Berhasil membuat {N} assessment manual."
- GET `/TrainingAdmin/EditManualAssessment/{id}` (line 647) — DB filter `s.Id == id && s.IsManualEntry`
- POST `/TrainingAdmin/EditManualAssessment` (line 690) → UPDATE Score + redirect ManageAssessment?tab=training
- POST `/TrainingAdmin/DeleteManualAssessment/{id}` → DELETE row + redirect

From `Views/Admin/AddManualAssessment.cshtml` (verified):
- `#WorkerSelect` (line 60-66) — TomSelect-wrapped `<select multiple>`
- `#btnSimpanAssessment` starts `disabled=true` (line 311), enabled hanya jika ≥1 `.worker-cert-card` rendered (line 386-387)
- Fields: `#Title`, `#Category` (dropdown), `#Score`, `#CompletedAt`, `#PassPercentage` (default 70)
- TomSelect markup: `.ts-control` (open dropdown trigger), `.ts-dropdown` (open dropdown panel), `.ts-dropdown .option`
- Per-worker section: `<input name="WorkerCerts[0].UserId">`, `<input name="WorkerCerts[0].NomorSertifikat">`

From `Views/Admin/Shared/_TrainingRecordsTab.cshtml:200-337` (Pitfall 3):
- Worker rows nested di `<tr class="collapse" id="@workerCollapseId">` — chevron button `[data-bs-target="#..."]` toggles `.collapse.show`
- Recommended FLOW T4/T6 bypass UI: direct nav `/TrainingAdmin/EditManualAssessment/{id}` via captured `manualSessionId`

From `tests/helpers/dbSnapshot.ts` (verified post-Phase-318):
- `queryScalar(sql)` → number (regex parse first numeric line) — use untuk COUNT, IsPassed cast, Score
- `queryString(sql)` → string — use untuk Status text, NomorSertifikat, Title lookup

From `tests/helpers/accounts.ts`:
- `hc` = `meylisa.tjiang@pertamina.com` (Admin+HC role)
- `coachee` = `rino.prasetyo@pertamina.com` (test worker — DB Email lookup)

From `tests/helpers/utils.ts`:
- `today()`, `yesterday()`, `tomorrow()` → `YYYY-MM-DD`
- `uniqueTitle(prefix)` → suffix `Date.now()` untuk title isolation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append `verifyExcelDownload` + `interceptAnalyticsResponse` helpers ke examTypes.ts</name>
  <files>
    tests/e2e/helpers/examTypes.ts
  </files>
  <read_first>
    - tests/e2e/helpers/examTypes.ts (post-Phase-318 state — focus lines 580-622 untuk verifyCertificatePdfDownload pattern reference)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md (lines 240-310 — Pattern 2 + Pattern 3 skeleton verbatim)
    - Helpers/ExcelExportHelper.cs (lines 1-40 — MIME `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` + filename format)
    - Controllers/CMPController.cs (lines 2731-2813 — GetAnalyticsSummary return shape camelCase verification)
  </read_first>
  <behavior>
    - `verifyExcelDownload(page, endpointPath, opts?)`: APIRequest pattern adaptasi `verifyCertificatePdfDownload`. Assertions: status 200 + content-type Excel MIME regex + content-disposition `attachment.*\.xlsx` + body bytes > `opts.minBytes ?? 2048`. Return `{ bytes; filename; contentType }`.
    - `interceptAnalyticsResponse<T>(page, action, endpointMatcher)`: generic JSON intercept. Wrap `page.waitForResponse(matcher, { timeout: 15_000 })` → `await action()` → `await response.json()` → cast `T`. `endpointMatcher` accept `string` (substring includes) atau `RegExp`.
    - Export interface `AnalyticsResponseShape` (totalSessions, passRate, expiringCount, avgGainScore — from CMPController.cs:2731-2813 verified anonymous object).
    - **PENTING:** Helpers staged di Plan 01 supaya Plan 03 consume tanpa helper-ext task; FLOW T sendiri TIDAK consume helpers ini (FLOW T pakai TomSelect + DB queries inline).
  </behavior>
  <action>
    **STEP 1 — Append helpers di akhir `tests/e2e/helpers/examTypes.ts` (setelah line 621 `verifyCertificatePdfDownload`):**

    ```typescript
    // ============================================================
    // Phase 319 Plan 01 — Excel download + Analytics JSON intercept helpers
    // Staged in Plan 01 untuk consume di Plan 03 (FLOW V + W).
    // Source: Helpers/ExcelExportHelper.cs:1-40, Controllers/CMPController.cs:2731-2813
    // ============================================================

    /**
     * Phase 319 — verify Excel download dari endpoint binary.
     * Pattern: adaptasi `verifyCertificatePdfDownload` (Phase 318 R4 PROVEN).
     * APIRequest cookies inherit dari page context — caller MUST be logged in.
     *
     * Assertions:
     *  1. response.status() === 200
     *  2. content-type matches Excel MIME (xlsx OR xls)
     *  3. content-disposition match /attachment.*\.xlsx?/i (filename optional pattern)
     *  4. body bytes > opts.minBytes (default 2048; lowering OK untuk empty-DB header-only)
     *
     * @param page logged-in page context
     * @param endpointPath e.g. '/AssessmentAdmin/ExportCategoriesExcel'
     * @param opts.minBytes minimum byte threshold (default 2048; can lower to 256 utk header-only)
     * @param opts.filenamePattern optional regex utk filename verify
     * @returns { bytes, filename, contentType }
     */
    export async function verifyExcelDownload(
      page: Page,
      endpointPath: string,
      opts: { minBytes?: number; filenamePattern?: RegExp } = {}
    ): Promise<{ bytes: number; filename: string; contentType: string }> {
      const response = await page.request.get(endpointPath);

      expect(response.status(), `Excel download status (${endpointPath})`).toBe(200);

      const contentType = response.headers()['content-type'] ?? '';
      expect(contentType, `Content-Type (${endpointPath})`).toMatch(
        /application\/vnd\.(openxmlformats-officedocument\.spreadsheetml\.sheet|ms-excel)/i
      );

      const contentDisp = response.headers()['content-disposition'] ?? '';
      expect(contentDisp, `Content-Disposition (${endpointPath})`).toMatch(/attachment.*\.xlsx?/i);

      const filenameMatch = contentDisp.match(/filename=(?:"([^"]+)"|([^;\s]+))/i);
      const filename = (filenameMatch?.[1] ?? filenameMatch?.[2] ?? '').trim();
      if (opts.filenamePattern) {
        expect(filename, `Filename pattern (${endpointPath})`).toMatch(opts.filenamePattern);
      }

      const body = await response.body();
      const minBytes = opts.minBytes ?? 2048;
      expect(body.length, `Excel body bytes (min ${minBytes}, ${endpointPath})`).toBeGreaterThan(minBytes);

      return { bytes: body.length, filename, contentType };
    }

    /**
     * Phase 319 — Analytics endpoint JSON response shape.
     * Verified dari Controllers/CMPController.cs:2731-2813 (`Json(new { totalSessions, passRate, ... })`).
     * ASP.NET Core default JsonSerializerOptions = camelCase (anonymous object → camelCase keys).
     *
     * NOTE: Wave 0 W0.W0 di Plan 03 verify camelCase serialization (RESEARCH A4 YELLOW).
     */
    export interface AnalyticsResponseShape {
      totalSessions: number;
      passRate: number;
      expiringCount: number;
      avgGainScore: number;
    }

    /**
     * Phase 319 — intercept JSON response dari analytics endpoint.
     * Pattern: page.waitForResponse() → action() → parse JSON → cast T.
     *
     * @param page logged-in page
     * @param action navigation/click yang trigger endpoint fire
     * @param endpointMatcher string substring (URL.includes) atau RegExp utk URL match
     * @returns parsed JSON cast ke generic T
     */
    export async function interceptAnalyticsResponse<T = AnalyticsResponseShape>(
      page: Page,
      action: () => Promise<void>,
      endpointMatcher: string | RegExp
    ): Promise<T> {
      const responsePromise = page.waitForResponse(
        (r) => {
          if (typeof endpointMatcher === 'string') {
            return r.url().includes(endpointMatcher);
          }
          return endpointMatcher.test(r.url());
        },
        { timeout: 15_000 }
      );
      await action();
      const response = await responsePromise;
      expect(response.status(), `Analytics response status (${endpointMatcher})`).toBe(200);
      return (await response.json()) as T;
    }
    ```

    **STEP 2 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```

    Expected: exit 0.

    **STEP 3 — Playwright dry-discovery:**

    ```bash
    cd tests && npx playwright test exam-types --list | head -30
    ```

    Expected: helpers loadable, no syntax error.
  </action>
  <verify>
    <automated>cd tests && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Grep `tests/e2e/helpers/examTypes.ts` for `export async function verifyExcelDownload` returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `export async function interceptAnalyticsResponse` returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `export interface AnalyticsResponseShape` returns 1 line
    - Grep `tests/e2e/helpers/examTypes.ts` for `application\/vnd\.\(openxmlformats` returns 1 line (MIME regex)
    - Grep `tests/e2e/helpers/examTypes.ts` for `page.waitForResponse` returns 1 line (intercept pattern)
    - `cd tests && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Helpers `verifyExcelDownload` + `interceptAnalyticsResponse` + interface `AnalyticsResponseShape` ada di examTypes.ts dengan APIRequest pattern + JSON intercept pattern. TypeScript clean. Staged ready untuk Plan 03 consume.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 W0.T0 — TomSelect smoke verify A3 assumption</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Phase-318 — lines 1-100 untuk import + Wave-0 describe pattern reference)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md (lines 430-470 — Pitfall 4 TomSelect Pattern 1 + Pattern 2 fallback; lines 815-823 — A3 risk assessment)
    - Views/Admin/AddManualAssessment.cshtml (lines 60-66, 283-388 — TomSelect markup + JS event handlers)
  </read_first>
  <behavior>
    - W0.T0 single smoke sub-test verify TomSelect interaction reliable di `#WorkerSelect`.
    - Navigate AddManualAssessment → click `.ts-control` → type `Rino` → click matching `.ts-dropdown .option` → assert `.worker-cert-card` count==1 → assert `#btnSimpanAssessment` enabled.
    - Tidak submit form (cleanup avoidance). Pure interaction-pattern verify.
    - Marker title `[319-W0-T0]` minimal (sub-test tidak create DB row).
    - Fallback path documented: kalau Pattern 1 RED, switch ke Pattern 2 (`page.evaluate()` inject hidden inputs) di Task 3.
  </behavior>
  <action>
    **STEP 1 — Append describe block W0.T0 di `tests/e2e/exam-types.spec.ts` (setelah existing smoke wave-0 di line ~120, sebelum FLOW K describe):**

    Locate insertion point: setelah closing `});` dari existing `test.describe('smoke wave-0 ...`. Append new describe:

    ```typescript
    test.describe('smoke wave-0 phase-319 (verify RESEARCH A3 TomSelect)', () => {
      test('W0.T0 — TomSelect #WorkerSelect interaction pattern reliable', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto('/TrainingAdmin/AddManualAssessment');

        // Form loaded
        await expect(page.locator('form[action*="AddManualAssessment"]')).toBeVisible({ timeout: 10_000 });
        await expect(page.locator('#WorkerSelect')).toBeAttached();

        // Btn starts disabled (verified Views/Admin/AddManualAssessment.cshtml:311)
        const btnSubmit = page.locator('#btnSimpanAssessment');
        await expect(btnSubmit).toBeDisabled();

        // TomSelect interaction Pattern 1 (RESEARCH lines 442-450)
        const tsControl = page.locator('.ts-control').first();
        await expect(tsControl).toBeVisible({ timeout: 5_000 });
        await tsControl.click();

        // Dropdown panel opens
        const tsDropdown = page.locator('.ts-dropdown').first();
        await tsDropdown.waitFor({ state: 'visible', timeout: 5_000 });

        // Type search
        const tsInput = page.locator('.ts-control input').first();
        await tsInput.fill('Rino');

        // Click matching option
        const option = page.locator('.ts-dropdown .option', { hasText: /Rino/i }).first();
        await option.waitFor({ state: 'visible', timeout: 5_000 });
        await option.click();

        // .worker-cert-card rendered (JS addWorkerRow fired)
        await expect(page.locator('.worker-cert-card')).toHaveCount(1, { timeout: 5_000 });

        // Submit button now enabled
        await expect(btnSubmit).toBeEnabled({ timeout: 5_000 });

        // eslint-disable-next-line no-console
        console.log('[W0.T0] TomSelect Pattern 1 OK — proceed dengan Pattern 1 di FLOW T2');
      });
    });
    ```

    **STEP 2 — Run W0.T0 isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "W0.T0"
    ```

    Expected: 1/1 PASS.

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | `.ts-control` not visible | TomSelect JS not loaded (CDN down OR initialization race) | Add `page.waitForLoadState('networkidle')` setelah `goto`. Verify Network tab di local Chrome dev untuk TomSelect CDN URL resolve |
    | `.ts-dropdown .option` empty | Workers list empty di DB OR Filter logic mismatch | DB query `SELECT COUNT(*) FROM Users WHERE Email LIKE '%pertamina%' AND IsActive=1` — expect ≥1. Verify worker `rino.prasetyo@pertamina.com` ada di DB |
    | `.worker-cert-card` count!=1 | JS addWorkerRow tidak fire — TomSelect onItemAdd hook missed | Inspect Console errors. Switch ke Pattern 2 fallback di FLOW T2 (page.evaluate inject hidden input) |
    | `#btnSimpanAssessment` still disabled | JS observer tidak detect card added | Same root cause — Pattern 1 failure → Pattern 2 fallback |

    **JIKA Pattern 1 RED:** Update Task 3 FLOW T2 untuk pakai Pattern 2 (page.evaluate). Document deviation di SUMMARY.

    **STEP 3 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "W0.T0"</automated>
  </verify>
  <acceptance_criteria>
    - W0.T0 PASSED dalam describe `smoke wave-0 phase-319`
    - Grep `tests/e2e/exam-types.spec.ts` for `W0.T0` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `TomSelect` returns ≥1 line (smoke context)
    - `.worker-cert-card` count==1 assertion PASSED
    - `#btnSimpanAssessment` enabled assertion PASSED
    - Runtime W0.T0 ≤ 30s
  </acceptance_criteria>
  <done>
    W0.T0 HIJAU. RESEARCH A3 (TomSelect Pattern 1 interaction) confirmed reliable untuk `#WorkerSelect`. Task 3 FLOW T2 safe pakai Pattern 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FLOW T — ManualAssessment Full CRUD (6 sub-tests T1-T6)</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Task-2 state — focus existing FLOW R/S describe structure untuk shared-state pattern)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md (lines 545-640 — Example 1 + Example 2 verbatim skeleton; lines 399-470 — Pitfall 3 + 4 + 7)
    - Controllers/TrainingAdminController.cs (lines 555-740 — full Add/Edit/Delete flow)
    - Views/Admin/AddManualAssessment.cshtml (lines 46, 60-66, 283-388 — form structure)
    - Views/Admin/EditManualAssessment.cshtml (lines 1-100 — edit form fields)
  </read_first>
  <behavior>
    - 6 sub-tests T1-T6 dengan shared state `manualTitle`, `workerId`, `manualSessionId`.
    - T1: navigate AddManualAssessment + verify form fields visible (`#Title`, `#Category`, `#WorkerSelect`, `#Score`, `#CompletedAt`).
    - T2: TomSelect pick `rino.prasetyo@pertamina.com` → fill form (title `[319-T]`, category `OJT`, score 85, completed date today) → submit → wait redirect `/AssessmentAdmin/ManageAssessment` → assert `.alert-success` "berhasil membuat".
    - T3: DB verify — `SELECT COUNT(*) FROM AssessmentSessions WHERE Title=N'{manualTitle}' AND IsManualEntry=1` returns 1. Capture `manualSessionId` via `queryString CAST(Id AS NVARCHAR(20))`. Verify Status='Completed', Score=85.
    - T4: HC edit — direct nav `/TrainingAdmin/EditManualAssessment/{manualSessionId}` (bypass collapse UI per Pitfall 3) → update Score to 92 → submit → DB verify Score=92.
    - T5: Worker view — login coachee → navigate `/CMP/Assessment` → assert `[319-T]` title row visible (di Completed atau Riwayat tab) tanpa Start button.
    - T6: HC delete — `POST /TrainingAdmin/DeleteManualAssessment/{manualSessionId}` via form (atau direct AJAX if available) → DB verify row count 0.
  </behavior>
  <action>
    **STEP 1 — Append FLOW T describe block di `tests/e2e/exam-types.spec.ts` (setelah FLOW S describe, sebelum `void verifyResultPage`):**

    ```typescript
    test.describe('FLOW T — ManualAssessment Full CRUD', () => {
      let manualTitle: string;
      let manualSessionId: number;
      const WORKER_EMAIL = 'rino.prasetyo@pertamina.com';
      const escapeSql = (s: string) => s.replace(/'/g, "''");

      test('T1 — HC navigate AddManualAssessment + verify form fields', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        manualTitle = uniqueTitle('[319-T] Manual CRUD');
        await login(page, 'hc');
        await page.goto('/TrainingAdmin/AddManualAssessment');

        await expect(page.locator('form[action*="AddManualAssessment"]')).toBeVisible({ timeout: 10_000 });

        // Verified Views/Admin/AddManualAssessment.cshtml field IDs
        await expect(page.locator('#Title')).toBeAttached();
        await expect(page.locator('#Category')).toBeAttached();
        await expect(page.locator('#WorkerSelect')).toBeAttached();
        await expect(page.locator('#Score')).toBeAttached();
        await expect(page.locator('#CompletedAt')).toBeAttached();

        // Card headers visible (verified Views lines 51, 71, 148, 248)
        await expect(page.locator('.card-header', { hasText: /Peserta|Worker/i }).first()).toBeVisible();
      });

      test('T2 — HC submit form (TomSelect pick + score 85) → DB INSERT', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto('/TrainingAdmin/AddManualAssessment');

        await page.fill('#Title', manualTitle);
        await page.selectOption('#Category', 'OJT');
        await page.fill('#Score', '85');
        await page.fill('#CompletedAt', today());

        // TomSelect Pattern 1 (verified W0.T0)
        await page.locator('.ts-control').first().click();
        await page.locator('.ts-control input').first().fill('Rino');
        await page.locator('.ts-dropdown .option', { hasText: /Rino/i }).first().click();
        await expect(page.locator('.worker-cert-card')).toHaveCount(1, { timeout: 5_000 });

        // Optional NomorSertifikat (verified WorkerCerts[0].NomorSertifikat field name)
        const certInput = page.locator('input[name="WorkerCerts[0].NomorSertifikat"]');
        if (await certInput.count() > 0) {
          await certInput.fill(`CERT-319-T-${Date.now()}`);
        }

        const btnSubmit = page.locator('#btnSimpanAssessment');
        await expect(btnSubmit).toBeEnabled({ timeout: 5_000 });

        // Submit + wait redirect (Pitfall 7)
        await Promise.all([
          page.waitForURL(/\/AssessmentAdmin\/ManageAssessment/, { timeout: 15_000 }),
          btnSubmit.click(),
        ]);

        // TempData alert-success rendered post-redirect
        await expect(
          page.locator('.alert-success', { hasText: /berhasil/i })
        ).toBeVisible({ timeout: 10_000 });
      });

      test('T3 — DB verify AssessmentSession IsManualEntry=1, Status=Completed, Score=85', async () => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        const count = await db.queryScalar(
          `SELECT COUNT(*) FROM AssessmentSessions WHERE Title = N'${escapeSql(manualTitle)}' AND IsManualEntry = 1`
        );
        expect(count).toBe(1);

        const status = await db.queryString(
          `SELECT TOP 1 Status FROM AssessmentSessions WHERE Title = N'${escapeSql(manualTitle)}'`
        );
        expect(status).toBe('Completed');

        const score = await db.queryScalar(
          `SELECT TOP 1 ISNULL(Score, -1) FROM AssessmentSessions WHERE Title = N'${escapeSql(manualTitle)}'`
        );
        expect(score).toBe(85);

        // Capture sessionId untuk T4-T6
        const idStr = await db.queryString(
          `SELECT TOP 1 CAST(Id AS NVARCHAR(20)) FROM AssessmentSessions WHERE Title = N'${escapeSql(manualTitle)}' ORDER BY Id DESC`
        );
        manualSessionId = parseInt(idStr, 10);
        expect(manualSessionId).toBeGreaterThan(0);
        // eslint-disable-next-line no-console
        console.log(`[FLOW T3] manualSessionId=${manualSessionId}, Title=${manualTitle}`);
      });

      test('T4 — HC edit ManualAssessment + Score update to 92', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        // Direct nav bypass collapse UI (Pitfall 3)
        await page.goto(`/TrainingAdmin/EditManualAssessment/${manualSessionId}`);
        await page.waitForLoadState('networkidle');

        await expect(page.locator('form[action*="EditManualAssessment"]')).toBeVisible({ timeout: 10_000 });
        await expect(page.locator('#Score')).toBeAttached();

        await page.fill('#Score', '92');

        const btnSubmit = page.locator('button[type="submit"]').filter({ hasText: /simpan|update|save/i }).first();
        await Promise.all([
          page.waitForURL(/\/AssessmentAdmin\/ManageAssessment|\/EditManualAssessment/, { timeout: 15_000 }),
          btnSubmit.click(),
        ]);

        // DB verify Score updated
        const newScore = await db.queryScalar(
          `SELECT TOP 1 ISNULL(Score, -1) FROM AssessmentSessions WHERE Id = ${manualSessionId}`
        );
        expect(newScore).toBe(92);
      });

      test('T5 — Worker view: ManualAssessment visible di /CMP/Assessment (Completed, no Start)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'coachee');
        await page.goto('/CMP/Assessment');
        await page.waitForLoadState('networkidle');

        // Title visible somewhere di page (could be Completed tab atau Riwayat — both accept)
        const titleLocator = page.locator(`text=${manualTitle}`).first();
        await expect(titleLocator).toBeVisible({ timeout: 10_000 });

        // Manual assessment di worker side TIDAK punya Start button — worker just views completion
        // Assertion: card containing manualTitle TIDAK contain "Mulai" / "Start" button
        const containingCard = page.locator('.assessment-card, .card', { hasText: manualTitle }).first();
        if (await containingCard.count() > 0) {
          await expect(
            containingCard.locator('a:has-text("Mulai"), a:has-text("Start"), .btn-start-standard')
          ).toHaveCount(0);
        }
      });

      test('T6 — HC delete + DB row removed', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');

        // Strategy 1: POST direct via page.request (cookies inherited)
        const csrfPage = await page.goto('/AssessmentAdmin/ManageAssessment?tab=training');
        // Get antiforgery token dari page meta atau hidden input
        const tokenInput = await page.locator('input[name="__RequestVerificationToken"]').first().getAttribute('value').catch(() => null);

        if (tokenInput) {
          const formData = new URLSearchParams();
          formData.set('id', String(manualSessionId));
          formData.set('__RequestVerificationToken', tokenInput);
          const deleteResp = await page.request.post(`/TrainingAdmin/DeleteManualAssessment/${manualSessionId}`, {
            form: { __RequestVerificationToken: tokenInput },
          });
          // Accept 200 OR 302 redirect — both = delete success
          expect([200, 302]).toContain(deleteResp.status());
        } else {
          // Strategy 2 fallback: navigate ke ManageAssessment table → expand worker row → click delete
          await page.goto('/AssessmentAdmin/ManageAssessment?tab=training');
          await page.waitForLoadState('networkidle');
          // Per Pitfall 3 collapse expand pattern
          const workerRow = page.locator('tr', { hasText: /Rino/i }).first();
          const chevronBtn = workerRow.locator('button[data-bs-target^="#"]').first();
          await chevronBtn.click();
          const expandedRow = page.locator('tr.collapse.show').first();
          await expandedRow.waitFor({ state: 'visible', timeout: 5_000 });
          page.once('dialog', d => d.accept());
          await expandedRow.locator(`button[data-session-id="${manualSessionId}"], a[href*="DeleteManualAssessment/${manualSessionId}"]`).first().click();
          await page.waitForTimeout(2_000);
        }

        // DB verify row deleted
        const count = await db.queryScalar(
          `SELECT COUNT(*) FROM AssessmentSessions WHERE Id = ${manualSessionId}`
        );
        expect(count).toBe(0);
      });
    });
    ```

    **STEP 2 — Run FLOW T isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "FLOW T"
    ```

    Expected: 6/6 PASS.

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | T2 submit hangs | btnSubmit not enabled — TomSelect onItemAdd not fired | Fallback Pattern 2 (page.evaluate inject hidden input) per RESEARCH Pitfall 4 |
    | T3 count=0 | INSERT not committed OR Title escape issue | Verify single-quote replaced. Check `dotnet run` console untuk POST log |
    | T4 Score not updated | EditManualAssessment endpoint reject OR ModelState invalid | DB query directly `SELECT Score FROM AssessmentSessions WHERE Id={id}` post-submit. Verify form submit success message |
    | T5 title not visible | Worker view filter excludes ManualAssessment rows | Verify CMPController.cs Assessment list query include `IsManualEntry=true` rows |
    | T6 delete fails 403 | Antiforgery token missing/wrong | Use Strategy 2 UI-click fallback |
    | T6 DB row still exists | DeleteManualAssessment endpoint signature mismatch | Verify endpoint method (POST vs DELETE). Check Controllers/TrainingAdminController.cs:~720 for actual delete pattern |

    **STEP 3 — TypeScript compile gate + commit:**

    ```bash
    cd tests && npx tsc --noEmit
    git add tests/e2e/helpers/examTypes.ts tests/e2e/exam-types.spec.ts
    git commit -m "feat(319-01): W0.T0 TomSelect smoke + FLOW T ManualAssessment CRUD hijau (7/7 — QA-09)

    Append verifyExcelDownload + interceptAnalyticsResponse helpers (staged untuk Plan 03).
    FLOW T 6 sub-tests T1-T6: AddManualAssessment via TomSelect + DB verify + Edit + Worker view + Delete.
    W0.T0 confirm RESEARCH A3 TomSelect Pattern 1 reliable.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "FLOW T"</automated>
  </verify>
  <acceptance_criteria>
    - 6 sub-tests T1-T6 PASSED dalam describe `FLOW T — ManualAssessment Full CRUD`
    - Grep `tests/e2e/exam-types.spec.ts` for `'FLOW T — ManualAssessment` returns 1 line
    - Grep `tests/e2e/exam-types.spec.ts` for `T1 —`, `T2 —`, `T3 —`, `T4 —`, `T5 —`, `T6 —` returns 6 lines
    - T3: DB IsManualEntry=1, Status='Completed', Score=85 verified
    - T4: DB Score updated ke 92
    - T5: ManualAssessment title visible di /CMP/Assessment (worker side, no Start button)
    - T6: DB COUNT=0 post-delete
    - `[319-T]` title marker ter-present
    - Per-sub-test runtime ≤ 60s typical
    - Full Plan 01 suite (W0.T0 + T1-T6 = 7 sub-tests) PASS
    - `cd tests && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    FLOW T (6 sub-tests) HIJAU end-to-end. ManualAssessment full CRUD via UI verified — TomSelect pattern + form submit + DB INSERT + Edit + Worker visibility + Delete. Wave 0 W0.T0 ter-confirm A3 assumption. Helpers `verifyExcelDownload` + `interceptAnalyticsResponse` staged ready untuk Plan 03 consume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC page context → TrainingAdmin POST endpoints | Antiforgery token + cookie auth required (verified `[ValidateAntiForgeryToken]` on POST actions) |
| Worker page context → /CMP/Assessment | Role-based view filter (worker hanya lihat assigned + ManualAssessment yang ada UserId match) |
| Test runner → dev DB (sqlcmd Integrated Auth) | FLOW T creates 1 row + edits + deletes — net-zero side-effect post T6 |
| TomSelect JS layer → form serialization | onItemAdd callback inject hidden `WorkerCerts[i].UserId` input — critical untuk POST body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-01 | Tampering | A3 YELLOW assumption fail — TomSelect Pattern 1 unreliable | mitigate | W0.T0 explicit verify. JIKA RED, fallback Pattern 2 (`page.evaluate()` inject hidden input) per RESEARCH Pitfall 4 Pattern 2 |
| T-319-02 | Information Disclosure | DB query exposes Title + Score di test logs | accept | Test pakai dev creds + sanitized titles `[319-T] Manual CRUD-${Date.now()}` — no PII. Production endpoints already `[Authorize(Roles="Admin, HC")]` |
| T-319-03 | Tampering | T6 delete via raw POST without antiforgery token → 403 | mitigate | Strategy 1 capture token dari page meta sebelum POST. Strategy 2 UI-click fallback (full form submit dengan token) |
| T-319-04 | Denial of Service | TomSelect dropdown lookup slow untuk large worker list | accept | Dev DB worker count <100 typical — sub-second response. Production scale tetap functional (FLOW T1 timeout 10s sufficient) |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# Task 1: Helpers staging compile gate
cd tests && npx tsc --noEmit

# Task 2: W0.T0 TomSelect smoke
cd tests && npx playwright test exam-types --grep "W0.T0"

# Task 3: FLOW T isolated
cd tests && npx playwright test exam-types --grep "FLOW T"

# Plan 01 combined suite (W0.T0 + FLOW T)
cd tests && npx playwright test exam-types --grep "W0.T0|FLOW T"

# Cumulative regression Phase 318 + Plan 01 = 49 + 7 = 56 sub-tests
cd tests && npx playwright test exam-types.spec.ts --reporter=list
```

## Manual UAT (post-automation)

- Manual: HC login → /TrainingAdmin/AddManualAssessment → pick worker via TomSelect → submit dengan title `UAT-MANUAL-319` → verify alert-success + redirect ke ManageAssessment training tab
- Manual: HC navigate /AssessmentAdmin/ManageAssessment?tab=training → expand worker row chevron → verify nested table row dengan title `UAT-MANUAL-319` visible
- Manual: HC click Edit → ubah Score → submit → verify update success
- Manual: Worker login coachee → /CMP/Assessment → verify ManualAssessment visible di list tanpa Start button
- Manual: HC delete → verify row hilang dari ManageAssessment training tab
- Cleanup dev DB post-test: `DELETE FROM AssessmentSessions WHERE Title LIKE N'[319-T]%' OR Title LIKE N'UAT-MANUAL-319%'` (per CLAUDE.md SEED_WORKFLOW)
</verification>

<success_criteria>
## Plan 01 Complete When

1. `tests/e2e/helpers/examTypes.ts` extended dengan 3 staged exports: `verifyExcelDownload` (APIRequest Excel) + `interceptAnalyticsResponse<T>` (JSON intercept generic) + `AnalyticsResponseShape` interface
2. Wave 0 W0.T0 HIJAU — TomSelect Pattern 1 verified reliable (RESEARCH A3 resolved)
3. FLOW T HIJAU: 6 sub-tests T1-T6 — navigate → submit (TomSelect) → DB INSERT verify → Edit Score → Worker view → Delete + DB removed
4. Full Plan 01 suite (W0.T0 + FLOW T = 7 sub-tests) PASS
5. Cumulative regression `npx playwright test exam-types.spec.ts` = 56/56 HIJAU (49 baseline + 7 Plan 01)
6. `npx tsc --noEmit` exit 0
7. Title marker `[319-T]` ter-present di semua FLOW T sub-tests
8. Commit message format: `feat(319-01): W0.T0 TomSelect smoke + FLOW T ManualAssessment CRUD hijau (7/7 — QA-09)`
9. SUMMARY.md created at `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-01-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-01-SUMMARY.md` mencakup:
- Files modified: `examTypes.ts` (+~80 LOC: verifyExcelDownload + interceptAnalyticsResponse + AnalyticsResponseShape), `exam-types.spec.ts` (+~200 LOC: W0.T0 + FLOW T)
- W0.T0 outcome — TomSelect Pattern 1 reliable? (Pattern 2 fallback applied jika RED)
- FLOW T runtime per sub-test + total
- T2 form submit pattern actually used (Pattern 1 TomSelect vs Pattern 2 evaluate)
- T6 delete strategy actually used (Strategy 1 raw POST + token, atau Strategy 2 UI click expand)
- Any deviation dari RESEARCH (e.g., NomorSertifikat field optional, Score 100 atau 92, Worker view tab location)
- Cumulative regression stats: 56/56 vs baseline 49/49
- Next: Plan 02 FLOW U ManageCategories (sequential, depends_on [01])
</output>
