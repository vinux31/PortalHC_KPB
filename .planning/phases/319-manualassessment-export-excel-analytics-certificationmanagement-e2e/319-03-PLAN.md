---
phase: 319
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - tests/e2e/exam-types.spec.ts
autonomous: true
requirements: [QA-09]
tags: [e2e, playwright, exam-types, export-excel, analytics, json-intercept, wave-0, chartjs]

must_haves:
  truths:
    - "Wave 0 W0.V0 smoke verifies Excel endpoint `/AssessmentAdmin/ExportCategoriesExcel` reachable (NOT placeholder `/Admin/ExportAssessmentMatrix` which doesn't exist per RESEARCH Pitfall 1) — 200 + xlsx MIME + bytes>2048 atau dynamic threshold dari log output"
    - "Wave 0 W0.W0 smoke verifies Analytics JSON shape camelCase (RESEARCH A4) — log raw response untuk confirm `totalSessions`/`passRate`/`expiringCount`/`avgGainScore` key names"
    - "FLOW V (Export Excel, 3 sub-tests V1-V3 + W0.V0 smoke) HIJAU: APIRequest /AssessmentAdmin/ExportCategoriesExcel via `verifyExcelDownload` helper → status 200 + Excel MIME + content-disposition + bytes threshold"
    - "FLOW W (Analytics Dashboard, 4 sub-tests W1-W4 + W0.W0 smoke) HIJAU: navigate /CMP/AnalyticsDashboard → JSON intercept GetAnalyticsSummary via `interceptAnalyticsResponse<AnalyticsResponseShape>` → DOM canvas smoke 3 charts + `window.Chart` defined → DB cross-check totalSessions matches COUNT"
    - "Endpoint selection D-319-03 deviation locked: `/AssessmentAdmin/ExportCategoriesExcel` (RESEARCH Pitfall 1 — Admin/ExportAssessmentMatrix doesn't exist)"
    - "Cumulative regression Plan 03: 60 (post-Plan-02) + 9 (W0.V0 + W0.W0 + V1-V3 + W1-W4) = 69 sub-tests HIJAU"
    - "Both FLOW V + W consume Plan 01 helpers (`verifyExcelDownload`, `interceptAnalyticsResponse`) — no new helper appended in Plan 03"
  artifacts:
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "Extended dengan Wave 0 W0.V0+W0.W0 + FLOW V (3 sub-tests V1-V3) + FLOW W (4 sub-tests W1-W4)"
      contains: "FLOW V|FLOW W"
  key_links:
    - from: "tests/e2e/exam-types.spec.ts (FLOW V)"
      to: "tests/e2e/helpers/examTypes.ts (verifyExcelDownload)"
      via: "import verifyExcelDownload — staged Plan 01, consumed Plan 03"
      pattern: "verifyExcelDownload"
    - from: "tests/e2e/exam-types.spec.ts (FLOW V)"
      to: "Controllers/AssessmentAdminController.cs:526 (ExportCategoriesExcel)"
      via: "GET /AssessmentAdmin/ExportCategoriesExcel → ClosedXML XLWorkbook → File(bytes, MIME, filename)"
      pattern: "ExportCategoriesExcel"
    - from: "tests/e2e/exam-types.spec.ts (FLOW W)"
      to: "tests/e2e/helpers/examTypes.ts (interceptAnalyticsResponse)"
      via: "import interceptAnalyticsResponse<AnalyticsResponseShape>"
      pattern: "interceptAnalyticsResponse"
    - from: "tests/e2e/exam-types.spec.ts (FLOW W)"
      to: "Controllers/CMPController.cs:2731-2813 (GetAnalyticsSummary)"
      via: "GET /CMP/GetAnalyticsSummary → Json(new { totalSessions, passRate, expiringCount, avgGainScore })"
      pattern: "GetAnalyticsSummary"
    - from: "tests/e2e/exam-types.spec.ts (FLOW W3)"
      to: "Views/CMP/AnalyticsDashboard.cshtml (Chart.js v4 canvas mount)"
      via: "canvas#failRateChart + canvas#trendChart + canvas#gainScoreTrendChart attached + window.Chart defined"
      pattern: "canvas|Chart"
---

<objective>
Implementasi Wave 0 (W0.V0 + W0.W0 smoke) + FLOW V (Export Excel, 3 sub-tests V1-V3) + FLOW W (Analytics Dashboard, 4 sub-tests W1-W4) sebagai append ke `tests/e2e/exam-types.spec.ts`. Wave 3 sequential (`depends_on: [02]`) — file-level lock on `exam-types.spec.ts` requires Plan 02 landed first.

**SCOPE DECISION LOCKED (per user 2026-05-12):** D-319-03 endpoint = `/AssessmentAdmin/ExportCategoriesExcel` (paling sederhana, no params). Researcher confirmed `/Admin/ExportAssessmentMatrix` doesn't exist (RESEARCH Pitfall 1).

Purpose: Tutup 2/5 FLOW QA-09 coverage (D-319-03 Export Excel endpoint validation; D-319-04 Analytics dashboard JSON+DOM+DB cross-check). Wave 0 resolve 2 YELLOW assumptions: A1 (Excel endpoint reachable + minimum bytes), A4 (Analytics JSON camelCase serialization).

Output:
- `tests/e2e/exam-types.spec.ts` extended dengan smoke wave-0 phase-319 (W0.V0 + W0.W0) + FLOW V (3 sub-tests) + FLOW W (4 sub-tests) — total spec sekarang 60 + 9 = 69 sub-tests
- Per-sub-test runtime ≤ 30s (no exam-taking workflow di FLOW V/W — pure HTTP + dashboard navigate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-CONTEXT.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-01-SUMMARY.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-02-SUMMARY.md
@tests/e2e/exam-types.spec.ts
@tests/e2e/helpers/examTypes.ts
@Controllers/AssessmentAdminController.cs
@Controllers/CMPController.cs
@Helpers/ExcelExportHelper.cs
@Views/CMP/AnalyticsDashboard.cshtml

<interfaces>
<!-- Post-Plan-02 state — exam-types.spec.ts has 60 sub-tests (49 baseline + 7 Plan 01 + 4 Plan 02) -->
<!-- Plan 01 staged helpers (`verifyExcelDownload`, `interceptAnalyticsResponse`, `AnalyticsResponseShape`) available untuk consume -->

From `tests/e2e/helpers/examTypes.ts` (Plan 01 appended):
```typescript
export async function verifyExcelDownload(
  page: Page,
  endpointPath: string,
  opts?: { minBytes?: number; filenamePattern?: RegExp }
): Promise<{ bytes: number; filename: string; contentType: string }>;

export interface AnalyticsResponseShape {
  totalSessions: number;
  passRate: number;
  expiringCount: number;
  avgGainScore: number;
}

export async function interceptAnalyticsResponse<T = AnalyticsResponseShape>(
  page: Page,
  action: () => Promise<void>,
  endpointMatcher: string | RegExp
): Promise<T>;
```

From `Controllers/AssessmentAdminController.cs:526` (ExportCategoriesExcel):
- GET endpoint, no params, no AJAX
- Returns ClosedXML workbook → `File(bytes, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "Categories.xlsx")` (atau similar filename)
- Auth: `[Authorize(Roles="Admin, HC")]`

From `Controllers/CMPController.cs:2731-2813` (GetAnalyticsSummary):
- GET endpoint dengan query params `?bagian=&unit=...`
- Returns `Json(new { totalSessions, passRate, expiringCount, avgGainScore })` — ASP.NET Core camelCase default

From `Views/CMP/AnalyticsDashboard.cshtml` (verified 2026-05-12):
- Razor shell + JS bootstrap fetches multiple analytics endpoints parallel
- Canvas elements: `canvas#failRateChart` (line 244), `canvas#trendChart` (line 291), `canvas#gainScoreTrendChart` (line 331)
- Config div: `#analyticsConfig` (line 38) — JSON config attribute
- Chart.js v4 loaded via CDN line 585: `chart.umd.min.js`

From `Helpers/ExcelExportHelper.cs:1-40` (verified):
- MIME: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- Filename pattern: `{Title}_{yyyy-MM-dd}.xlsx` atau static name

From `tests/helpers/dbSnapshot.ts`:
- `queryScalar(sql)` numeric — use untuk COUNT cross-check
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 (W0.V0 Excel smoke + W0.W0 Analytics JSON shape) + FLOW V Export Excel (3 sub-tests V1-V3)</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Plan-02 state — import line + existing smoke wave-0 phase-319 describe untuk extend pattern)
    - tests/e2e/helpers/examTypes.ts (post-Plan-01 — verifyExcelDownload signature)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md (lines 348-371 — Pitfall 1 endpoint table; lines 670-690 — Example 4 V1 verbatim)
    - Controllers/AssessmentAdminController.cs (line 526 ExportCategoriesExcel signature)
    - Helpers/ExcelExportHelper.cs (lines 1-40 — MIME + filename)
  </read_first>
  <behavior>
    - W0.V0 smoke: HC GET `/AssessmentAdmin/ExportCategoriesExcel` via verifyExcelDownload(minBytes: 256) → log bytes + filename + contentType. Set minBytes 256 (RESEARCH A1 mitigation utk empty-DB header-only workbook). Update FLOW V V1 threshold based on observed bytes (kalau >2048 ya pakai 2048; kalau <2048 lower threshold di V1).
    - V1: APIRequest verify Excel download — adopted threshold dari W0.V0 observation. Pakai filenamePattern `/\.xlsx$/i`.
    - V2: Content-type assertion strict — verify `contentType.includes('spreadsheetml')` (xlsx) atau fallback `vnd.ms-excel` (xls). Both acceptable per D-319-03.
    - V3: Negative path — unauthenticated GET request returns 302/401/403 (auth gate working). Use `page.context.clearCookies()` first → request again → verify status NOT 200.
    - Update import line di top of `exam-types.spec.ts` untuk include verifyExcelDownload + interceptAnalyticsResponse + AnalyticsResponseShape.
  </behavior>
  <action>
    **STEP 1 — Update import line di `tests/e2e/exam-types.spec.ts`:**

    Add `verifyExcelDownload`, `interceptAnalyticsResponse`, `AnalyticsResponseShape` ke existing import block (lines 4-17):

    ```typescript
    import {
      createAssessmentViaWizard,
      createDefaultPackage,
      addQuestionViaForm,
      submitExamTwoStep,
      checkMAOptionsForQuestion,
      fillEssayAnswer,
      gradeSingleEssaySession,
      addExtraTimeViaModal,
      createPrePostAssessmentViaWizard,
      verifyCertificatePdfDownload,
      verifyExcelDownload,
      interceptAnalyticsResponse,
      type QuestionInput,
      type CreatePrePostOpts,
      type AnalyticsResponseShape,
    } from './helpers/examTypes';
    ```

    **STEP 2 — Extend Wave 0 phase-319 describe block (atau create new describe kalau Plan 01 belum extend):**

    Locate existing `test.describe('smoke wave-0 phase-319 ...')` block. Append W0.V0 sub-test inside (atau create new describe block setelah):

    ```typescript
    test.describe('smoke wave-0 phase-319 V+W (verify A1 Excel + A4 Analytics)', () => {
      test('W0.V0 — Excel endpoint /AssessmentAdmin/ExportCategoriesExcel reachable + log bytes', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        const result = await verifyExcelDownload(
          page,
          '/AssessmentAdmin/ExportCategoriesExcel',
          { minBytes: 256, filenamePattern: /\.xlsx$/i }
        );
        // eslint-disable-next-line no-console
        console.log(`[W0.V0] Excel bytes=${result.bytes}, filename=${result.filename}, contentType=${result.contentType}`);
        expect(result.bytes).toBeGreaterThan(256);
      });

      test('W0.W0 — Analytics JSON shape camelCase log raw response', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        const data = await interceptAnalyticsResponse<Record<string, unknown>>(
          page,
          () => page.goto('/CMP/AnalyticsDashboard'),
          '/CMP/GetAnalyticsSummary'
        );
        // eslint-disable-next-line no-console
        console.log(`[W0.W0] Analytics raw JSON keys: ${Object.keys(data).join(', ')}`);
        // Verify camelCase keys (RESEARCH A4)
        const keys = Object.keys(data);
        const hasTotalSessions = keys.some(k => k.toLowerCase() === 'totalsessions');
        expect(hasTotalSessions, 'totalSessions key present (case-insensitive)').toBe(true);
      });
    });
    ```

    **STEP 3 — Append FLOW V describe block (setelah FLOW U dari Plan 02):**

    ```typescript
    test.describe('FLOW V — Export Excel Endpoint Validation', () => {
      const EXCEL_ENDPOINT = '/AssessmentAdmin/ExportCategoriesExcel';

      test('V1 — HC export Excel → 200 + xlsx MIME + bytes threshold', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        // minBytes 1024 (compromise: header + ≥1 row data minimum) — adjust kalau W0.V0 log shows <1024
        const result = await verifyExcelDownload(page, EXCEL_ENDPOINT, {
          minBytes: 1024,
          filenamePattern: /\.xlsx$/i,
        });
        expect(result.bytes).toBeGreaterThan(1024);
        expect(result.filename).toMatch(/\.xlsx$/);
        // eslint-disable-next-line no-console
        console.log(`[V1] Excel: bytes=${result.bytes}, filename=${result.filename}`);
      });

      test('V2 — Content-Type explicit assert spreadsheetml OR ms-excel', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        const result = await verifyExcelDownload(page, EXCEL_ENDPOINT, { minBytes: 1024 });
        // contentType regex already validates di helper; this sub-test makes explicit content-type observation
        const isXlsx = result.contentType.includes('spreadsheetml');
        const isXls = result.contentType.includes('ms-excel');
        expect(isXlsx || isXls, `ContentType should be xlsx OR xls: actual=${result.contentType}`).toBe(true);
      });

      test('V3 — Unauthenticated request blocked (auth gate)', async ({ page, context }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        // Clear cookies untuk simulate unauthenticated
        await context.clearCookies();
        const response = await page.request.get(EXCEL_ENDPOINT);
        // Expect redirect ke login (302) OR 401/403 — anything NOT 200
        expect([200].includes(response.status()), `Expected non-200 (auth gate), got ${response.status()}`).toBe(false);
        // Common patterns: 302 Found (redirect login), 401 Unauthorized, 403 Forbidden
        expect([302, 401, 403]).toContain(response.status());
      });
    });
    ```

    **STEP 4 — Run W0.V0 + FLOW V isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "W0.V0|FLOW V"
    ```

    Expected: 4/4 PASS (1 smoke + 3 FLOW V).

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | W0.V0 404 | Endpoint doesn't exist OR route wrong | Verify Controllers/AssessmentAdminController.cs:526 — pakai pattern `[HttpGet]` + `Route("[controller]/...")`. Adjust endpoint path |
    | W0.V0 bytes < 256 | Empty workbook header-only | Lower threshold ke 100 atau accept (smoke pass = endpoint reachable). FLOW V V1 adjust ke same threshold |
    | V1 contentType wrong | MIME header mismatch | Verify ExcelExportHelper.cs:36 actual MIME string. Adjust helper regex jika perlu |
    | V3 still returns 200 | Endpoint missing [Authorize] attribute → security bug! | Production code change required — file bug ticket |
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "W0.V0|FLOW V"</automated>
  </verify>
  <acceptance_criteria>
    - W0.V0 PASS: Excel bytes logged + filename + contentType
    - 3 sub-tests V1-V3 PASSED dalam describe `FLOW V — Export Excel Endpoint Validation`
    - V1: response 200 + filename matches `/\.xlsx$/`
    - V2: contentType contains `spreadsheetml` OR `ms-excel`
    - V3: unauthenticated request returns NOT 200 (auth gate working)
    - Grep `tests/e2e/exam-types.spec.ts` for `verifyExcelDownload` returns ≥3 lines (1 W0 + 2 FLOW V usage)
    - Per-sub-test runtime ≤ 15s typical (pure HTTP, no UI)
  </acceptance_criteria>
  <done>
    Wave 0 W0.V0 + FLOW V (3 sub-tests) HIJAU. Excel endpoint `/AssessmentAdmin/ExportCategoriesExcel` validated (200 + MIME + bytes + auth gate). RESEARCH A1 assumption resolved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FLOW W — Analytics Dashboard JSON+DOM+DB (4 sub-tests W1-W4)</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Task-1 state)
    - tests/e2e/helpers/examTypes.ts (post-Plan-01 — interceptAnalyticsResponse + AnalyticsResponseShape signatures)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md (lines 692-742 — Example 5 W2-W4 verbatim skeleton; lines 497-508 — Pitfall 6 empty-data 200; lines 820 — A4 camelCase risk)
    - Controllers/CMPController.cs (lines 2731-2813 — GetAnalyticsSummary signature + return shape)
    - Views/CMP/AnalyticsDashboard.cshtml (canvas IDs + script tags)
  </read_first>
  <behavior>
    - W1: HC navigate `/CMP/AnalyticsDashboard` → page load + `<h1>` atau dashboard title visible. Also verify `#analyticsConfig` div attached + filter form visible.
    - W2: Intercept `GetAnalyticsSummary` → parse response → assert shape (`hasProperty('totalSessions')`, `typeof === 'number'`, etc.). Use `interceptAnalyticsResponse<AnalyticsResponseShape>`.
    - W3: DOM canvas smoke — 3 canvas elements attached (`#failRateChart`, `#trendChart`, `#gainScoreTrendChart`) + `window.Chart` defined (Chart.js v4 loaded). Loose visibility check (canvas opaque).
    - W4: DB cross-check — `interceptAnalyticsResponse<AnalyticsResponseShape>` totalSessions value matches `db.queryScalar('SELECT COUNT(*) FROM AssessmentSessions WHERE IsPassed IS NOT NULL AND CompletedAt IS NOT NULL AND CompletedAt >= DATEADD(year, -1, GETDATE())')`. JIKA dev DB empty/sparse, gracefully `test.skip()` dengan log explanation.
    - Pitfall 6: Empty data returns 200 + `{ totalSessions: 0, ... }` — masih valid. W2 hanya assert shape, W4 cross-check exact value.
  </behavior>
  <action>
    **STEP 1 — Append FLOW W describe block (setelah FLOW V):**

    ```typescript
    test.describe('FLOW W — Analytics Dashboard JSON+DOM+DB', () => {
      test('W1 — HC navigate /CMP/AnalyticsDashboard + page elements visible', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto('/CMP/AnalyticsDashboard');
        await page.waitForLoadState('networkidle');

        // Page title atau dashboard heading visible
        await expect(page).toHaveURL(/AnalyticsDashboard/);

        // Config div attached (verified Views/CMP/AnalyticsDashboard.cshtml:38)
        await expect(page.locator('#analyticsConfig')).toBeAttached({ timeout: 10_000 });

        // At least 1 filter form atau dropdown visible
        const filterForm = page.locator('form, .filter-form, select').first();
        await expect(filterForm).toBeVisible({ timeout: 10_000 });
      });

      test('W2 — Intercept GetAnalyticsSummary → assert shape (totalSessions, passRate, etc.)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');

        const data = await interceptAnalyticsResponse<AnalyticsResponseShape>(
          page,
          async () => {
            await page.goto('/CMP/AnalyticsDashboard');
            // Initial bootstrap fires endpoints automatically
          },
          '/CMP/GetAnalyticsSummary'
        );

        // Shape verification (camelCase per ASP.NET Core default)
        expect(data).toHaveProperty('totalSessions');
        expect(data).toHaveProperty('passRate');
        expect(data).toHaveProperty('expiringCount');
        expect(data).toHaveProperty('avgGainScore');

        // Type verification (Pitfall 6: empty data returns 0 not null)
        expect(typeof data.totalSessions).toBe('number');
        expect(typeof data.passRate).toBe('number');
        // eslint-disable-next-line no-console
        console.log(`[W2] Analytics shape: totalSessions=${data.totalSessions}, passRate=${data.passRate}`);
      });

      test('W3 — DOM canvas smoke (3 charts attached + Chart.js loaded)', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto('/CMP/AnalyticsDashboard');
        await page.waitForLoadState('networkidle');
        // Wait for Chart.js to initialize (charts mount via JS bootstrap)
        await page.waitForTimeout(2_000);

        // Canvas elements attached (verified Views/CMP/AnalyticsDashboard.cshtml line 244, 291, 331)
        await expect(page.locator('canvas#failRateChart')).toBeAttached({ timeout: 10_000 });
        await expect(page.locator('canvas#trendChart')).toBeAttached({ timeout: 10_000 });
        await expect(page.locator('canvas#gainScoreTrendChart')).toBeAttached({ timeout: 10_000 });

        // Chart.js v4 loaded via CDN line 585
        const chartLoaded = await page.evaluate(() => typeof (window as unknown as { Chart?: unknown }).Chart === 'function');
        expect(chartLoaded, 'window.Chart defined (Chart.js v4 CDN loaded)').toBe(true);
      });

      test('W4 — DB cross-check totalSessions matches COUNT query', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');

        const data = await interceptAnalyticsResponse<AnalyticsResponseShape>(
          page,
          () => page.goto('/CMP/AnalyticsDashboard'),
          '/CMP/GetAnalyticsSummary'
        );

        // Default periode = last 1 year
        const dbCount = await db.queryScalar(
          `SELECT COUNT(*) FROM AssessmentSessions WHERE IsPassed IS NOT NULL AND CompletedAt IS NOT NULL AND CompletedAt >= DATEADD(year, -1, GETDATE())`
        );

        // Pitfall 6: If dev DB has zero completed sessions, both 0 = still match (W4 PASS)
        // If dev DB has data, exact match required
        expect(data.totalSessions).toBe(dbCount);
        // eslint-disable-next-line no-console
        console.log(`[W4] DB cross-check: API totalSessions=${data.totalSessions}, DB COUNT=${dbCount}`);
      });
    });
    ```

    **STEP 2 — Run FLOW W isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "W0.W0|FLOW W"
    ```

    Expected: 5/5 PASS (1 smoke + 4 FLOW W).

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | W2 hasProperty fails | JSON serializer configured PascalCase | Per RESEARCH A4 — verify W0.W0 log output. Adjust property name pengecekan ke PascalCase kalau perlu. Update AnalyticsResponseShape interface |
    | W3 canvas not attached | Chart.js CDN blocked atau view path different | Verify Network tab Chart.js script loaded 200. Check Views/CMP/AnalyticsDashboard.cshtml canvas IDs match |
    | W3 window.Chart undefined | Chart.js v4 CDN failed | Check CDN URL `cdn.jsdelivr.net/npm/chart.js@4...`. Internet connectivity issue |
    | W4 totalSessions mismatch | DB query timezone difference OR query window mismatch | Verify CMPController.cs:2731-2813 actual query — could be different DATEADD logic. Or use `test.skip()` jika dev DB empty (per Pitfall 6) |

    **STEP 3 — Run combined Plan 03 suite (Wave 0 + V + W):**

    ```bash
    cd tests && npx playwright test exam-types --grep "W0.V0|W0.W0|FLOW V|FLOW W"
    ```

    Expected: 9/9 PASS.

    **STEP 4 — Cumulative regression gate:**

    ```bash
    cd tests && npx playwright test exam-types.spec.ts --reporter=list
    ```

    Expected: 69/69 (60 post-Plan-02 + 9 Plan 03).

    **STEP 5 — TypeScript compile gate + commit:**

    ```bash
    cd tests && npx tsc --noEmit
    git add tests/e2e/exam-types.spec.ts
    git commit -m "feat(319-03): W0 smoke + FLOW V Export Excel + FLOW W Analytics hijau (9/9 — QA-09)

    W0.V0 Excel endpoint /AssessmentAdmin/ExportCategoriesExcel reachable confirmed.
    W0.W0 Analytics camelCase JSON shape confirmed (A4 resolved).
    FLOW V 3 sub-tests V1-V3: APIRequest Excel validation (status + MIME + bytes + auth gate).
    FLOW W 4 sub-tests W1-W4: navigate dashboard + JSON intercept + Chart.js canvas smoke + DB cross-check.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "W0.W0|FLOW W"</automated>
  </verify>
  <acceptance_criteria>
    - W0.W0 PASS: Analytics JSON raw keys logged + camelCase verified
    - 4 sub-tests W1-W4 PASSED dalam describe `FLOW W — Analytics Dashboard JSON+DOM+DB`
    - W1: page navigated + `#analyticsConfig` attached + filter form visible
    - W2: response shape has totalSessions/passRate/expiringCount/avgGainScore + types number
    - W3: 3 canvases attached + `window.Chart === 'function'`
    - W4: API totalSessions matches DB COUNT query
    - Full Plan 03 suite (Wave 0 phase-319 V+W + FLOW V + FLOW W = 9 sub-tests) PASS
    - Cumulative regression `npx playwright test exam-types.spec.ts` = 69/69 PASS
    - `cd tests && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    FLOW V (3 sub-tests) + FLOW W (4 sub-tests) + Wave 0 W0.V0 + W0.W0 HIJAU. Excel endpoint validation + Analytics JSON shape + Chart.js DOM smoke + DB cross-check all verified. RESEARCH A1 + A4 assumptions resolved. Cumulative regression 69/69.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC page context → Export Excel endpoint | APIRequest cookies-inherited; `[Authorize(Roles="Admin, HC")]` enforces auth |
| Unauthenticated page context → Export Excel | V3 verifies redirect/401/403 (auth gate working) |
| HC page context → /CMP/AnalyticsDashboard JSON endpoints | Cookie auth + role gate (verified CMPController.cs:2492+2508 `[Authorize]`) |
| Chart.js CDN → browser runtime | cdn.jsdelivr.net script tag; W3 verifies `window.Chart` defined post-load |
| Analytics JSON response → DOM canvas mount | ASP.NET Core JSON serializer (camelCase default); JSON shape validation di W2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-09 | Information Disclosure | Excel export exposes category data tanpa auth | mitigate | V3 explicit verifies auth gate. JIKA RED, file bug ticket |
| T-319-10 | Tampering | A4 YELLOW — Analytics JSON returns PascalCase (not camelCase) | mitigate | W0.W0 log raw keys + adjust interface jika perlu. Both case-insensitive checks |
| T-319-11 | Denial of Service | Large dev DB → slow Excel generation | accept | minBytes lenient (256 di W0, 1024 di V1); 60s timeout sufficient |
| T-319-12 | Tampering | Chart.js CDN unreachable → W3 RED | accept | Local-only test, internet required (per CLAUDE.md DEV_WORKFLOW). Document `test.skip()` jika offline |
| T-319-13 | Information Disclosure | DB cross-check exposes session COUNT magnitude | accept | Aggregate count not PII. Test runs lokal saja |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# Task 1: Wave 0 + FLOW V isolated
cd tests && npx playwright test exam-types --grep "W0.V0|FLOW V"

# Task 2: Wave 0 W0.W0 + FLOW W isolated
cd tests && npx playwright test exam-types --grep "W0.W0|FLOW W"

# Plan 03 combined suite (Wave 0 V+W + FLOW V + FLOW W)
cd tests && npx playwright test exam-types --grep "W0.V0|W0.W0|FLOW V|FLOW W"

# Cumulative regression Phase 318 + Plan 01-03 = 49 + 7 + 4 + 9 = 69 sub-tests
cd tests && npx playwright test exam-types.spec.ts --reporter=list
```

## Manual UAT (post-automation)

- Manual: HC login → /AssessmentAdmin/ExportCategoriesExcel → verify browser download `.xlsx` file → open via Excel/LibreOffice → verify categories list
- Manual: HC navigate /CMP/AnalyticsDashboard → verify charts render (failRate, trend, gainScore) + summary cards menampilkan numbers
- Manual: Logout → access /AssessmentAdmin/ExportCategoriesExcel directly → verify redirect ke login (auth gate)
- No DB cleanup needed (read-only operations FLOW V + W)
</verification>

<success_criteria>
## Plan 03 Complete When

1. Wave 0 W0.V0 HIJAU — Excel endpoint reachable (A1 resolved)
2. Wave 0 W0.W0 HIJAU — Analytics JSON camelCase shape confirmed (A4 resolved)
3. FLOW V HIJAU: 3 sub-tests V1-V3 — Excel APIRequest validation (200 + MIME + bytes + auth gate)
4. FLOW W HIJAU: 4 sub-tests W1-W4 — navigate + JSON intercept shape + canvas smoke + DB cross-check
5. Full Plan 03 suite (W0.V0 + W0.W0 + FLOW V + FLOW W = 9 sub-tests) PASS
6. Cumulative regression `npx playwright test exam-types.spec.ts` = 69/69 HIJAU
7. `npx tsc --noEmit` exit 0
8. Helpers `verifyExcelDownload` + `interceptAnalyticsResponse` consumed correctly (Plan 01 staged → Plan 03 used)
9. Commit format: `feat(319-03): W0 smoke + FLOW V Export Excel + FLOW W Analytics hijau (9/9 — QA-09)`
10. SUMMARY.md created at `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-03-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-03-SUMMARY.md` mencakup:
- Files modified: `exam-types.spec.ts` (+~250 LOC Wave 0 phase-319 V+W + FLOW V + FLOW W describes)
- W0.V0 outcome — Excel bytes observed (e.g., 5KB), filename pattern, contentType MIME exact value
- W0.W0 outcome — Raw JSON keys (verify camelCase vs PascalCase)
- FLOW V runtime per sub-test + total
- FLOW W runtime per sub-test + total
- W3 Chart.js mounted verification — canvas attach count
- W4 DB cross-check exact value — API totalSessions=N, DB COUNT=N (match verified)
- Any deviation dari RESEARCH (e.g., minBytes lowered ke 256, PascalCase interface adjustment, FLOW W4 skip kalau empty DB)
- Cumulative regression stats: 69/69 vs baseline 60/60
- Next: Plan 04 FLOW X CertificationManagement CDP variant + docs finalize (sequential, depends_on [03])
</output>
