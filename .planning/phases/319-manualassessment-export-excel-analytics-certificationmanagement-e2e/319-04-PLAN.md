---
phase: 319
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - tests/e2e/exam-types.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/test-reports/2026-05-12-phase-319-summary.md
autonomous: true
requirements: [QA-09]
tags: [e2e, playwright, exam-types, certification-management, cdp, docs-finalize, requirements, roadmap, phase-closure, qa-09]

must_haves:
  truths:
    - "Wave 0 W0.X0 smoke verifies `/CDP/CertificationManagement` reachable (NOT `/CMP/CertificationManagement` which returns 500 — view file missing per RESEARCH Pitfall 2). Scope locked CDP variant per user 2026-05-12 (deviation from D-319-05 documented)."
    - "FLOW X (CertificationManagement CDP variant, 3 sub-tests X1-X3 + W0.X0 smoke) HIJAU: navigate /CDP/CertificationManagement → filter by category via AJAX `/CDP/FilterCertificationManagement` → detail page navigation `/CDP/CertificationManagementDetail`"
    - "REQUIREMENTS.md Future Requirements section di-insert QA-09 entry verbatim from CONTEXT.md D-319-09 (`Admin features E2E coverage — ManualAssessment CRUD, ManageCategories CRUD, Export Excel endpoint validation, Analytics dashboard JSON+DOM verification, CertificationManagement listing+filter.`)"
    - "REQUIREMENTS.md Traceability table di-append row: `| QA-09 | 319 | Complete |`"
    - "ROADMAP.md Phase 319 `Requirements` line updated dari placeholder `QA-04` ke `QA-09` per D-319-09 mapping fix"
    - "ROADMAP.md Phase 319 `Plans` section finalized — 4 plan files listed dengan brief objective"
    - "docs/test-reports/2026-05-12-phase-319-summary.md Phase 319 closure report — 4-plan summary, regression stats 69/69, scope decisions (D-319-03 endpoint + D-319-05 CDP variant), QA-09 deliverables checkboxes"
    - "Final regression gate HIJAU: `npx playwright test exam-types.spec.ts --reporter=list` ≥69/69 PASS (target met)"
  artifacts:
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "Extended dengan Wave 0 W0.X0 + FLOW X (3 sub-tests X1-X3)"
      contains: "FLOW X"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated dengan QA-09 entry + Traceability row"
      contains: "QA-09"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 319 Requirements + Plans finalized"
      contains: "QA-09 (admin features E2E coverage)"
    - path: "docs/test-reports/2026-05-12-phase-319-summary.md"
      provides: "Phase 319 closure report — 4-plan summary, regression stats, scope decisions"
      contains: "Phase 319"
  key_links:
    - from: "tests/e2e/exam-types.spec.ts (FLOW X)"
      to: "Controllers/CDPController.cs:3539+ (CertificationManagement CDP variant)"
      via: "GET /CDP/CertificationManagement → Razor render Views/CDP/CertificationManagement.cshtml → AJAX filter /CDP/FilterCertificationManagement → detail nav /CDP/CertificationManagementDetail"
      pattern: "CertificationManagement"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "QA-09 ↔ Phase 319 cross-reference (Traceability table + Phase entry Requirements line)"
      pattern: "QA-09"
    - from: "docs/test-reports/2026-05-12-phase-319-summary.md"
      to: ".planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-*-SUMMARY.md"
      via: "consolidated phase closure summary references per-plan SUMMARYs"
      pattern: "319-0[1-4]-SUMMARY"
---

<objective>
Plan terakhir Phase 319 — dual concern:

**Concern A: FLOW X test coverage** (3 sub-tests X1-X3 + Wave 0 W0.X0) — append ke `tests/e2e/exam-types.spec.ts`. **SCOPE DECISION LOCKED:** D-319-05 deviation dari "CMP variant" ke "CDP variant" per user 2026-05-12 (RESEARCH Pitfall 2 — `/CMP/CertificationManagement` view file missing → returns 500). CDP variant adalah real production page yang user-facing.

**Concern B: Docs finalize** (REQUIREMENTS.md + ROADMAP.md + closure report) — Phase 319 wrap-up. Per D-319-09: insert QA-09 entry di Future Requirements + Traceability row + ROADMAP Phase 319 Requirements line update `QA-04 → QA-09` + Plans finalize.

Wave 4 sequential (`depends_on: [03]`) — file-level lock on `exam-types.spec.ts` + REQUIREMENTS.md/ROADMAP.md require Plan 03 closed first.

Purpose: Tutup 1/5 FLOW QA-09 final coverage gap (D-319-05 CertificationManagement) + tutup Phase 319 dengan REQUIREMENTS+ROADMAP sync + final regression gate ≥69/69.

Output:
- `tests/e2e/exam-types.spec.ts` extended dengan W0.X0 + FLOW X (3 sub-tests) — total spec ≥69 sub-tests (60 post-Plan-02 + 9 Plan 03 + 4 Plan 04)
- `.planning/REQUIREMENTS.md` updated dengan QA-09 entry + Traceability row
- `.planning/ROADMAP.md` Phase 319 entry finalized (QA-09 Requirements + 4 plans listed)
- `docs/test-reports/2026-05-12-phase-319-summary.md` Phase 319 closure report
- Final regression gate HIJAU ≥69/69 (target met per RESEARCH Sampling Rate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-CONTEXT.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-01-SUMMARY.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-02-SUMMARY.md
@.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-03-SUMMARY.md
@.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-05-PLAN.md
@docs/test-reports/2026-05-12-phase-318-summary.md
@tests/e2e/exam-types.spec.ts
@tests/e2e/helpers/examTypes.ts
@Controllers/CDPController.cs
@Controllers/CMPController.cs
@Views/CDP/CertificationManagement.cshtml

<interfaces>
<!-- Post-Plan-03 state — exam-types.spec.ts has 69 sub-tests (49 baseline + 7 Plan 01 + 4 Plan 02 + 9 Plan 03) -->

From `Controllers/CDPController.cs:3539+` (CDP CertificationManagement — verified 2026-05-12):
- GET `/CDP/CertificationManagement` → Razor view `Views/CDP/CertificationManagement.cshtml` (verified exists, 1-589 lines)
- Action body builds ViewModel with `BuildSertifikatRowsAsync` — initial render contains `#cert-table-container` partial
- GET `/CDP/FilterCertificationManagement` → AJAX partial HTML response (refresh tabel) — verified `CDP/CertificationManagement.cshtml:280-310` `refreshTable` fetch
- GET `/CDP/CertificationManagementDetail?judul=...` (atau equivalent) → detail page

From `Views/CDP/CertificationManagement.cshtml` (verified 2026-05-12):
- `<h2>` heading "Certification Management" atau similar
- `#filter-category` dropdown (category filter)
- `#cert-table-container` div (partial HTML target)
- Detail link pattern `a[href*="CertificationManagementDetail"]` di table rows

From `.planning/REQUIREMENTS.md` (current state — verified 2026-05-12):
- Section "## Future Requirements (deferred)" lines 17-25 — lists QA-02..QA-08 (Phase 318 already added QA-08)
- Section "## Traceability" lines 36-43 — table dengan 3 rows (QA-01 Phase 315, QA-02 Phase 317, QA-08 Phase 318)

From `.planning/ROADMAP.md:406-411` (Phase 319 current entry):
```
### Phase 319: ManualAssessment + Export Excel + Analytics + CertificationManagement E2E

**Goal:** Test coverage untuk ManualAssessment workflow...
**Requirements:** QA-04 (admin features coverage)   ← placeholder, perlu sync ke QA-09
**Depends on:** Phase 318
**Plans:** TBD (preview only)                       ← placeholder, perlu finalize 4 plans
```

From `.planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-05-PLAN.md` (pattern reference untuk docs-finalize plan):
- Task 1: Update REQUIREMENTS.md + ROADMAP.md (D-318-06 verbatim insertion)
- Task 2: Final regression gate
- Task 3: Closure report `docs/test-reports/2026-05-12-phase-318-summary.md`
- Pattern: 3 tasks dengan dependency chain (docs → gate → report)

From CONTEXT.md D-319-09 (verbatim text):
> "Admin features E2E coverage — ManualAssessment CRUD, ManageCategories CRUD, Export Excel endpoint validation, Analytics dashboard JSON+DOM verification, CertificationManagement listing+filter."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 W0.X0 + FLOW X CertificationManagement CDP variant (3 sub-tests X1-X3)</name>
  <files>
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Plan-03 state — import + existing FLOW V/W describe structure)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-RESEARCH.md (lines 372-397 — Pitfall 2 CDP scope decision; lines 744-791 — Example 6 X1-X3 verbatim CDP variant)
    - Controllers/CDPController.cs (lines 3539+ — CertificationManagement + Filter + Detail actions)
    - Views/CDP/CertificationManagement.cshtml (verified — `<h2>`, `#filter-category`, `#cert-table-container`)
  </read_first>
  <behavior>
    - W0.X0 smoke: HC navigate `/CDP/CertificationManagement` → assert page reachable (200) + `<h2>` heading visible. JIKA RED, document scope failure + escalate.
    - X1: HC navigate full page load — verify heading + filter dropdown + table container all visible.
    - X2: Filter by category → wait response `/CDP/FilterCertificationManagement` → assert 200 + partial HTML returned. Graceful `test.skip()` jika dropdown kosong (dev DB no categories).
    - X3: Detail navigation — click first detail link → wait URL match `CertificationManagementDetail` → assert detail heading visible. Graceful `test.skip()` jika tabel kosong.
    - Sub-tests pakai inline locators (no new helper). Read-only operations — no DB write needed.
  </behavior>
  <action>
    **STEP 1 — Append Wave 0 W0.X0 ke existing smoke wave-0 phase-319 describe (atau new describe):**

    ```typescript
    test.describe('smoke wave-0 phase-319 X (verify A2 CDP CertMgmt reachable)', () => {
      test('W0.X0 — /CDP/CertificationManagement reachable + heading visible', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        const response = await page.goto('/CDP/CertificationManagement');
        expect(response?.status(), 'GET /CDP/CertificationManagement status').toBe(200);

        // Heading present (verified Views/CDP/CertificationManagement.cshtml)
        const heading = page.locator('h1, h2, h3').filter({ hasText: /Certif/i }).first();
        await expect(heading).toBeVisible({ timeout: 10_000 });
        // eslint-disable-next-line no-console
        console.log('[W0.X0] /CDP/CertificationManagement page reachable + heading verified');
      });
    });
    ```

    **STEP 2 — Append FLOW X describe block (setelah FLOW W dari Plan 03):**

    ```typescript
    test.describe('FLOW X — CertificationManagement CDP Variant', () => {
      test('X1 — HC navigate /CDP/CertificationManagement + page elements visible', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto('/CDP/CertificationManagement');
        await page.waitForLoadState('networkidle');

        await expect(page).toHaveURL(/CertificationManagement/);

        // Heading visible
        const heading = page.locator('h1, h2, h3').filter({ hasText: /Certif/i }).first();
        await expect(heading).toBeVisible({ timeout: 10_000 });

        // Filter form atau category dropdown visible
        const filterDropdown = page.locator('#filter-category, select[name*="category" i], select[name*="filter" i]').first();
        await expect(filterDropdown).toBeVisible({ timeout: 10_000 });

        // Table container visible
        const tableContainer = page.locator('#cert-table-container, table, .table-responsive').first();
        await expect(tableContainer).toBeVisible({ timeout: 10_000 });
      });

      test('X2 — Filter by category → AJAX partial refresh /CDP/FilterCertificationManagement', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto('/CDP/CertificationManagement');
        await page.waitForLoadState('networkidle');

        // Pick first non-empty option dari category dropdown
        const dropdown = page.locator('#filter-category, select').first();
        const optionCount = await dropdown.locator('option').count();
        if (optionCount < 2) {
          test.skip(true, 'No categories available di dropdown — empty dev DB');
          return;
        }
        const firstValue = await dropdown.locator('option').nth(1).getAttribute('value');
        if (!firstValue) {
          test.skip(true, 'Category option value null');
          return;
        }

        // Watch for filter AJAX response
        const responsePromise = page.waitForResponse(
          (r) => r.url().includes('/FilterCertificationManagement') || r.url().includes('/Filter'),
          { timeout: 10_000 }
        ).catch(() => null);

        await dropdown.selectOption(firstValue);
        const response = await responsePromise;

        if (response) {
          expect(response.status()).toBe(200);
          // Partial HTML response (atau JSON depending on implementation)
          const text = await response.text();
          expect(text.length, 'Filter response body length').toBeGreaterThan(0);
        } else {
          // Mungkin form-submit pattern (POST-redirect-GET) — verify URL changed atau page reloaded
          await page.waitForLoadState('networkidle');
          await expect(page.locator('#cert-table-container, table').first()).toBeVisible();
        }
      });

      test('X3 — Detail page navigation', async ({ page }) => {
        test.setTimeout(FLOW_TIMEOUT_MS);
        await login(page, 'hc');
        await page.goto('/CDP/CertificationManagement');
        await page.waitForLoadState('networkidle');

        // Find first detail link
        const detailLink = page.locator('a[href*="CertificationManagementDetail"], a[href*="Detail"]', { hasText: /detail|lihat|view/i }).first();
        const linkCount = await detailLink.count();
        const anyDetailLink = await page.locator('#cert-table-container a, table a').first().count();

        if (linkCount === 0 && anyDetailLink === 0) {
          test.skip(true, 'No detail links available — empty data');
          return;
        }

        const targetLink = linkCount > 0 ? detailLink : page.locator('#cert-table-container a, table a').first();
        await Promise.all([
          page.waitForURL(/CertificationManagementDetail|Detail/i, { timeout: 10_000 }),
          targetLink.click(),
        ]);

        // Detail heading visible
        const detailHeading = page.locator('h1, h2, h3, h4').filter({ hasText: /detail|certif/i }).first();
        await expect(detailHeading).toBeVisible({ timeout: 10_000 });
      });
    });
    ```

    **STEP 3 — Run W0.X0 + FLOW X isolated:**

    ```bash
    cd tests && npx playwright test exam-types --grep "W0.X0|FLOW X"
    ```

    Expected: 4/4 PASS (1 smoke + 3 FLOW X). X2/X3 boleh `test.skip()` jika data kosong (gracefully).

    **Failure modes + recovery:**

    | Failure | Likely cause | Mitigation |
    |---------|--------------|------------|
    | W0.X0 status 500 | CDP route mismatch atau view file missing | Verify Controllers/CDPController.cs:3539+ exists. Cross-reference Views/CDP/CertificationManagement.cshtml exists |
    | W0.X0 status 200 tapi heading not found | Markup uses different heading tag/class | Inspect actual `Views/CDP/CertificationManagement.cshtml` line 1-30 for heading element |
    | X2 filter dropdown empty | Dev DB no AssessmentCategories with sertifikat data | test.skip() acceptable; document |
    | X2 no AJAX response | Filter uses form-submit pattern (no AJAX) | Fallback URL+table reload verification (already coded) |
    | X3 no detail link | Empty table | test.skip() acceptable |

    **STEP 4 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types --grep "W0.X0|FLOW X"</automated>
  </verify>
  <acceptance_criteria>
    - W0.X0 PASS — `/CDP/CertificationManagement` 200 + heading visible
    - 3 sub-tests X1-X3 PASSED dalam describe `FLOW X — CertificationManagement CDP Variant` (X2/X3 boleh test.skip() jika data kosong, NOT fail)
    - X1: page navigated + heading + filter dropdown + table container all visible
    - X2: filter AJAX 200 (atau form-submit table reload sukses)
    - X3: detail page navigated + heading visible (atau skip)
    - Per-sub-test runtime ≤ 30s
    - `cd tests && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Wave 0 W0.X0 + FLOW X (3 sub-tests) HIJAU. CertificationManagement CDP variant verified (deviation from D-319-05 CMP→CDP locked per user 2026-05-12). RESEARCH A2 assumption resolved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update REQUIREMENTS.md (QA-09 entry + Traceability) + ROADMAP.md (Phase 319 finalize)</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — current QA-01..QA-08 structure post-Phase-318)
    - .planning/ROADMAP.md (lines 380-415 — Phase 317/318/319 entries)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-CONTEXT.md (lines 105-110 — D-319-09 QA-09 entry verbatim)
    - .planning/phases/318-pretest-posttest-full-cycle-examwindowclosedate-certificate-pdf-e2e/318-05-PLAN.md (Task 1 — pattern reference)
  </read_first>
  <behavior>
    - REQUIREMENTS.md updates:
      - Insert QA-09 entry di "Future Requirements (deferred)" section setelah QA-08 (preserving QA-02..QA-08 list)
      - Append Traceability row: `| QA-09 | 319 | Complete |`
      - Update header line "Active mapped: 4/4 ✓" (currently 3/3 post-Phase-318)
    - ROADMAP.md updates Phase 319 entry:
      - Line `**Requirements:**` ubah dari `QA-04 (admin features coverage)` jadi `QA-09 (admin features E2E coverage)`
      - Line `**Plans:**` finalize — replace `TBD (preview only)` dengan 4 plan files listed
      - Update bottom footer comment (jika ada) dengan tanggal sync 2026-05-12 dan note Phase 319 closure
  </behavior>
  <action>
    **STEP 1 — Update `.planning/REQUIREMENTS.md`:**

    Cari section `## Future Requirements (deferred)` dan **append QA-09 entry** setelah baris terakhir (QA-08):

    ```markdown
    - **QA-09** — Admin features E2E coverage — ManualAssessment CRUD, ManageCategories CRUD, Export Excel endpoint validation, Analytics dashboard JSON+DOM verification, CertificationManagement listing+filter.
    ```

    Cari section `## Traceability` dan **append row** setelah baris terakhir (QA-08):

    ```markdown
    | QA-09 | 319 | Complete |
    ```

    Update header **"Active mapped: 3/3"** jadi **"Active mapped: 4/4 ✓ — Orphans: 0 — Duplicates: 0"**.

    **STEP 2 — Update `.planning/ROADMAP.md` Phase 319 entry:**

    Locate section `### Phase 319: ManualAssessment + Export Excel + Analytics + CertificationManagement E2E` (line ~406-411).

    Update:
    - **Goal:** preserve existing goal text (sudah deskriptif)
    - **Requirements:** dari `QA-04 (admin features coverage)` jadi `QA-09 (admin features E2E coverage)`
    - **Depends on:** preserve `Phase 318`
    - **Plans:** dari `TBD (preview only)` jadi:
      ```
      Plans:
      - [x] 319-01-PLAN.md — Helpers extension (verifyExcelDownload + interceptAnalyticsResponse) + W0.T0 TomSelect smoke + FLOW T ManualAssessment CRUD (QA-09)
      - [x] 319-02-PLAN.md — FLOW U ManageCategories CRUD + duplicate-reject negative (QA-09)
      - [x] 319-03-PLAN.md — W0.V0+W0.W0 smoke + FLOW V Export Excel + FLOW W Analytics dashboard (QA-09)
      - [x] 319-04-PLAN.md — W0.X0 smoke + FLOW X CertificationManagement CDP variant + REQUIREMENTS QA-09 + ROADMAP Phase 319 closure + final regression gate ≥69/69
      ```

    Update footer (jika ada): tambah note `Phase 319 closed 2026-05-12 (QA-09 covered, regression 69/69 baseline)`.

    **STEP 3 — Verify via grep:**

    ```bash
    grep -n "QA-09" .planning/REQUIREMENTS.md
    grep -n "QA-09" .planning/ROADMAP.md
    grep -n "319-04-PLAN" .planning/ROADMAP.md
    ```

    Expected: each grep returns ≥1 line.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const road=fs.readFileSync('.planning/ROADMAP.md','utf8');if(!req.includes('QA-09'))process.exit(1);if(!req.includes('| QA-09 | 319 | Complete |'))process.exit(2);if(!road.includes('QA-09 (admin features E2E coverage)'))process.exit(3);if(!road.includes('319-04-PLAN'))process.exit(4);console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `QA-09` di Future Requirements section dengan text dari D-319-09 verbatim
    - REQUIREMENTS.md Traceability table contains row `| QA-09 | 319 | Complete |`
    - REQUIREMENTS.md header updated `Active mapped: 4/4 ✓`
    - ROADMAP.md Phase 319 `Requirements` line shows `QA-09 (admin features E2E coverage)`
    - ROADMAP.md Phase 319 Plans section lists 4 plan files dengan [x] check
    - ROADMAP.md Phase 319 footer note updated (optional)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md + ROADMAP.md disinkronkan. QA-09 ter-insert ke Future Requirements + Traceability. ROADMAP Phase 319 Requirements + Plans finalized.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final regression gate + Phase 319 closure report</name>
  <files>
    docs/test-reports/2026-05-12-phase-319-summary.md,
    tests/e2e/exam-types.spec.ts
  </files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (post-Task-1 final state — verify total sub-tests)
    - docs/test-reports/2026-05-12-phase-318-summary.md (pattern reference — closure report structure)
    - .planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-01-SUMMARY.md ... 319-03-SUMMARY.md
  </read_first>
  <behavior>
    - Run final regression gate `npx playwright test exam-types.spec.ts --reporter=list` → expected ≥69/69 PASS
    - Run TypeScript compile gate `cd tests && npx tsc --noEmit` → exit 0
    - Generate consolidated `docs/test-reports/2026-05-12-phase-319-summary.md` dengan structure mengikuti 2026-05-12-phase-318-summary.md
    - Final commit: gabungkan Task 1+2+3 atau 2 commit terpisah (feat + docs)
  </behavior>
  <action>
    **STEP 1 — Run final regression gate:**

    ```bash
    cd tests && npx playwright test exam-types.spec.ts --reporter=list
    ```

    Expected: ≥69/69 PASS. Log total + per-FLOW count.

    Jika RED → fix individual FLOW yang fail SEBELUM proceed ke closure report.

    **STEP 2 — TypeScript compile gate:**

    ```bash
    cd tests && npx tsc --noEmit
    ```

    Expected: exit 0.

    **STEP 3 — Generate `docs/test-reports/2026-05-12-phase-319-summary.md`:**

    Use Write tool dengan content (template based on Phase 318 summary):

    ```markdown
    # Phase 319 Summary — Admin Features E2E Coverage (QA-09)

    **Date:** 2026-05-12
    **Status:** COMPLETED
    **Milestone:** v16.0 QA Test Coverage
    **Requirement:** QA-09

    ## Outcome

    **5 FLOWs T/U/V/W/X (20 sub-tests) + 4 Wave 0 smokes HIJAU. Cumulative regression 69/69 baseline.**

    ## Scope Decisions Locked

    | Decision | Original (CONTEXT.md) | Final | Source |
    |----------|----------------------|-------|--------|
    | D-319-03 Excel endpoint | `/Admin/ExportAssessmentMatrix` (placeholder) | `/AssessmentAdmin/ExportCategoriesExcel` | RESEARCH Pitfall 1 — placeholder doesn't exist |
    | D-319-05 CertMgmt variant | CMP variant | **CDP variant** | RESEARCH Pitfall 2 — CMP view file missing (500 error); CDP is real user-facing page |
    | All other D-319-01/02/04/06/07/08/09 | locked | unchanged | CONTEXT.md verbatim |

    ## Plans Summary

    | Plan | Wave | Deliverable | Sub-tests | Status |
    |------|------|-------------|-----------|--------|
    | 319-01 | 1 | Helpers (verifyExcelDownload + interceptAnalyticsResponse) + W0.T0 TomSelect + FLOW T ManualAssessment CRUD | 7 (1 smoke + 6 FLOW T) | ✓ |
    | 319-02 | 2 | FLOW U ManageCategories CRUD + duplicate-reject | 4 | ✓ |
    | 319-03 | 3 | W0.V0+W0.W0 smokes + FLOW V Export Excel + FLOW W Analytics | 9 (2 smoke + 3 V + 4 W) | ✓ |
    | 319-04 | 4 | W0.X0 smoke + FLOW X CertMgmt CDP + docs finalize | 4 (1 smoke + 3 X) | ✓ |

    **Total:** 24 new sub-tests Phase 319 (20 FLOW + 4 Wave 0).

    ## FLOW-by-FLOW Breakdown

    | FLOW | Sub-tests | Coverage | Notes |
    |------|-----------|----------|-------|
    | T (Plan 01) | T1-T6 | ManualAssessment CRUD via UI + DB cross-check | TomSelect Pattern 1 |
    | U (Plan 02) | U1-U4 | ManageCategories CRUD + duplicate negative | TempData alert-danger reject |
    | V (Plan 03) | V1-V3 | Export Excel endpoint validation | `/AssessmentAdmin/ExportCategoriesExcel` |
    | W (Plan 03) | W1-W4 | Analytics dashboard JSON+DOM+DB | Chart.js v4 canvas smoke |
    | X (Plan 04) | X1-X3 | CertificationManagement CDP listing+filter+detail | CDP variant (not CMP) |

    ## Wave 0 Assumption Resolutions

    | Assumption | Plan | Status |
    |------------|------|--------|
    | A1 — Excel endpoint reachable + bytes threshold | 03 W0.V0 | RESOLVED |
    | A2 — CDP CertMgmt reachable | 04 W0.X0 | RESOLVED |
    | A3 — TomSelect `.ts-control` interaction reliable | 01 W0.T0 | RESOLVED |
    | A4 — Analytics JSON camelCase serialization | 03 W0.W0 | RESOLVED |

    ## Cumulative Regression

    ```
    exam-types.spec.ts: 69/69 PASS (≈5 min)
      Phase 317 (28 sub-tests) — FLOW K/L/M/N/O + W0.1/W0.2
      Phase 318 (21 sub-tests) — FLOW P/Q/R/S
      Phase 319 (20 sub-tests) — FLOW T/U/V/W/X + 4 Wave 0 phase-319 smoke
    ```

    ## Files Modified Aggregate

    | File | LOC delta | Plans |
    |------|-----------|-------|
    | `tests/e2e/helpers/examTypes.ts` | +~80 LOC | 01 |
    | `tests/e2e/exam-types.spec.ts` | +~750 LOC | 01, 02, 03, 04 |
    | `.planning/REQUIREMENTS.md` | +2 entries (QA-09 + Traceability) | 04 |
    | `.planning/ROADMAP.md` | Phase 319 finalize | 04 |
    | `docs/test-reports/2026-05-12-phase-319-summary.md` | new file | 04 |

    ## QA-09 Coverage Status

    All 5 admin feature areas covered (ManualAssessment + ManageCategories + Export Excel + Analytics + CertificationManagement). Negative test (U4 duplicate reject) + auth gate test (V3 unauthenticated) included.

    ## Deferred (Phase 320+)

    - CDP/CMP CertificationManagement reissue workflow detail
    - Search-by-NomorSertifikat scenarios
    - Multi-page pagination edge cases
    - ManualAssessment bulk import (if feature exists)
    - Analytics drill-down per-employee
    - CMP view file missing fix (production code change — separate DEV_WORKFLOW)

    ## Next Action

    - Team IT promosi Plan commits ke server Dev (10.55.3.3) — flag "no migration" (test files saja)
    - v16.0 milestone progress: Phase 319 complete. Next phase TBD (potentially Phase 320 wholesale FLOW A-J refresh atau new milestone)

    ## Related Documents

    - `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-01-SUMMARY.md`
    - `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-02-SUMMARY.md`
    - `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-03-SUMMARY.md`
    - `docs/test-reports/2026-05-12-phase-318-summary.md` (preceding phase)
    ```

    **STEP 4 — Commit final work:**

    ```bash
    git add tests/e2e/exam-types.spec.ts .planning/REQUIREMENTS.md .planning/ROADMAP.md docs/test-reports/2026-05-12-phase-319-summary.md
    git commit -m "feat(319-04): W0.X0 + FLOW X CertMgmt CDP + docs finalize + 69/69 regression (QA-09)

    FLOW X CertMgmt CDP variant (3 sub-tests) — deviation D-319-05 CMP→CDP per RESEARCH Pitfall 2.
    REQUIREMENTS QA-09 entry + Traceability row added.
    ROADMAP Phase 319 finalized (QA-09 Requirements + 4 plans listed).
    Phase 319 closure report docs/test-reports/2026-05-12-phase-319-summary.md.
    Cumulative regression 69/69 baseline target met.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-types.spec.ts --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Final regression gate `npx playwright test exam-types.spec.ts` ≥69/69 PASS
    - `npx tsc --noEmit` exit 0
    - File `docs/test-reports/2026-05-12-phase-319-summary.md` exists dengan scope decisions table + plans summary + FLOW breakdown + Wave 0 assumption resolutions + cumulative regression stats + QA-09 coverage status
    - Commit message includes Co-Authored-By line
  </acceptance_criteria>
  <done>
    Phase 319 closed. Final regression 69/69 HIJAU. Closure report generated. QA-09 covered. Ready untuk Team IT promosi commits + milestone v16.0 next phase planning.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC page context → CDP/CertificationManagement endpoints | `[Authorize(Roles="Admin, HC")]` enforces auth (verified CDPController.cs) |
| HC page context → /CDP/FilterCertificationManagement AJAX | AJAX response inherits page cookies + role gate |
| Test runner → REQUIREMENTS.md + ROADMAP.md edits | Local-only file write; git commit captures audit trail |
| Closure report → external readers | Local file under `docs/test-reports/`; not auto-published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-14 | Tampering | A2 YELLOW — /CDP/CertificationManagement returns 500 di dev | mitigate | W0.X0 explicit verify status==200. JIKA RED, escalate sebelum FLOW X (BLOCKER) |
| T-319-15 | Information Disclosure | Closure report exposes test design + endpoint paths | accept | Local docs untuk team developer reference. No PII or secrets included |
| T-319-16 | Tampering | REQUIREMENTS.md merge conflict dengan parallel work | accept | Single-developer workflow per CLAUDE.md. Phase 319 dianggap atomic operation |
| T-319-17 | Repudiation | Commit message tidak record deviation D-319-05 CMP→CDP | mitigate | Closure report Scope Decisions table dokumentasi deviation eksplisit |
</threat_model>

<verification>
## Plan-Level Verification

```bash
# Task 1: W0.X0 + FLOW X isolated
cd tests && npx playwright test exam-types --grep "W0.X0|FLOW X"

# Task 2: Docs verification
grep -n "QA-09" .planning/REQUIREMENTS.md
grep -n "QA-09" .planning/ROADMAP.md
grep -n "319-04-PLAN" .planning/ROADMAP.md

# Task 3: Final regression gate (Phase 319 closure target)
cd tests && npx playwright test exam-types.spec.ts --reporter=list

# Cumulative all phases
cd tests && npx tsc --noEmit
```

## Manual UAT (post-automation)

- Manual: HC login → /CDP/CertificationManagement → verify halaman tampil tabel sertifikat + filter dropdowns
- Manual: HC filter by category → verify tabel ter-refresh (AJAX atau full reload, both acceptable)
- Manual: HC click first row detail link → verify detail page loads + heading visible
- Manual: Read `docs/test-reports/2026-05-12-phase-319-summary.md` → verify completeness (Scope Decisions table + Plans summary + FLOW breakdown + Wave 0 resolutions)
- Manual: Notify Team IT untuk Dev promosi (commits Plan 01-04) — flag "no migration"
</verification>

<success_criteria>
## Plan 04 Complete When

1. Wave 0 W0.X0 HIJAU — /CDP/CertificationManagement reachable (A2 resolved)
2. FLOW X HIJAU: 3 sub-tests X1-X3 (X2/X3 may test.skip() jika empty data) — navigate + filter + detail
3. CDP variant scope deviation documented (per user 2026-05-12)
4. REQUIREMENTS.md updated dengan QA-09 entry + Traceability row + header `4/4`
5. ROADMAP.md Phase 319 entry finalized — Requirements `QA-09` + 4 plans listed [x]
6. `docs/test-reports/2026-05-12-phase-319-summary.md` created dengan structure mengikuti Phase 318 template
7. Final regression gate `npx playwright test exam-types.spec.ts` ≥69/69 HIJAU
8. `npx tsc --noEmit` exit 0
9. Commit format: `feat(319-04): W0.X0 + FLOW X CertMgmt CDP + docs finalize + 69/69 regression (QA-09)`
10. SUMMARY.md created at `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-04-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/319-manualassessment-export-excel-analytics-certificationmanagement-e2e/319-04-SUMMARY.md` mencakup:
- Files modified: `exam-types.spec.ts` (+~120 LOC W0.X0 + FLOW X), `REQUIREMENTS.md` (+2 entries), `ROADMAP.md` (Phase 319 finalize), `docs/test-reports/2026-05-12-phase-319-summary.md` (new closure report)
- W0.X0 outcome — /CDP/CertificationManagement reachability
- FLOW X runtime per sub-test + total
- X2 actual filter pattern observed (AJAX vs form-submit)
- X3 detail link availability — pass atau skip
- Scope deviation logged: D-319-05 CMP→CDP per user 2026-05-12 + RESEARCH Pitfall 2
- Cumulative regression stats: 69/69 (target met)
- Closure report path + Scope Decisions table reference
- Next milestone: v16.0 closure consideration atau Phase 320 backlog (FLOW A-J wholesale refresh)
</output>
