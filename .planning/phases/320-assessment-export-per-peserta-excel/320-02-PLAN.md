---
phase: 320-assessment-export-per-peserta-excel
plan: 02
type: execute
wave: 2
depends_on:
  - 320-01
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - EXP-05
  - EXP-06
  - EXP-07
requirements_addressed:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - EXP-05
  - EXP-06
  - EXP-07

must_haves:
  truths:
    - "Tab pertama Excel export bernama 'Summary' (rename dari 'Results') dan filename berakhiran `_Summary.xlsx`"
    - "Excel workbook punya N tab tambahan {NIP}_{FullName} untuk session Status=Completed OR Status=Abandoned"
    - "Session InProgress/Not Started/Cancelled TIDAK mendapat tab"
    - "Sheet peserta Online (IsManualEntry=false) berisi header info + section Analisis Elemen Teknis + Spider Chart PNG (skip kalau <3 ET) + section Detail Jawaban MC/MA"
    - "Sheet peserta Manual Entry (IsManualEntry=true) berisi header info + section Info Sertifikasi Manual + hyperlink ManualSertifikatUrl, NO ElemenTeknis/Chart/Detail Jawaban"
    - "Sheet name format {NIP}_{FullName} truncated 31-char tanpa karakter invalid Excel"
    - "Soal tanpa response (Abandoned skip) tampil 'Tidak dijawab' di kolom Jawaban Peserta dengan Status ✗"
    - "Soal Essay di-skip dengan note 'Essay – manual grading (lihat Penilaian Essay)'"
    - "Pre-load batch query EF Core (no N+1) untuk PackageUserResponses + SessionElemenTeknisScores + UserPackageAssignments + PackageQuestions.Include(Options)"
    - "Sheet peserta urut OrderBy(s => s.User?.FullName ?? '') ascending (D-01)"
    - "Authorize attribute existing [Authorize(Roles = 'Admin, HC')] TIDAK diubah (REQ EXP-07 inherent)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Method ExportAssessmentResults refactored — Summary sheet + per-peserta loop"
      contains: 'Worksheets.Add("Summary")'
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Method ExportAssessmentResults — filename suffix _Summary.xlsx"
      contains: "_Summary.xlsx"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Private helper WriteManualEntrySection"
      contains: "private int WriteManualEntrySection"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs ExportAssessmentResults"
      to: "Helpers/SheetNameSanitizer.Sanitize"
      via: "Static call HcPortal.Helpers.SheetNameSanitizer.Sanitize(nip, fullName, usedNames)"
      pattern: "SheetNameSanitizer.Sanitize"
    - from: "Controllers/AssessmentAdminController.cs ExportAssessmentResults"
      to: "Helpers/SpiderChartRenderer.RenderRadarPng"
      via: "Static call HcPortal.Helpers.SpiderChartRenderer.RenderRadarPng(chartData)"
      pattern: "SpiderChartRenderer.RenderRadarPng"
    - from: "Controllers/AssessmentAdminController.cs ExportAssessmentResults"
      to: "_context.PackageUserResponses + SessionElemenTeknisScores + PackageQuestions + UserPackageAssignments"
      via: "EF Core pre-load batch (WHERE sessionId IN list)"
      pattern: "eligibleSessionIds.Contains"
    - from: "Controllers/AssessmentAdminController.cs ExportAssessmentResults"
      to: "Helpers/ExcelExportHelper.ToFileResult"
      via: "Existing return statement (NO changes — auto AdjustToContents)"
      pattern: "ExcelExportHelper.ToFileResult"
---

<objective>
Refactor method `AssessmentAdminController.ExportAssessmentResults` (line 3651-3795) untuk menambahkan per-peserta sheet generation di atas sheet Summary existing. Konsumsi 2 helper dari Plan 01 (`SpiderChartRenderer` + `SheetNameSanitizer`). Tidak ada perubahan signature, attribute Authorize, atau filename pattern selain suffix `_Results.xlsx` → `_Summary.xlsx` (breaking change tag).

Purpose: Layer utama Phase 320 — refactor controller yang merangkai semua sub-section (Summary, header info, ElemenTeknis tabel, Spider Chart embed, Detail Jawaban MC/MA, Variant B Manual Entry). REQ EXP-01..07 ter-covered di plan ini (EXP-08 performance + UAT diserahkan ke Plan 03).

Output:
- Method `ExportAssessmentResults` extended dengan eligible filter + per-peserta foreach loop + 1 private helper baru `WriteManualEntrySection`
- 7 commit atomic per task (Task 4-10 dari RESEARCH.md):
  - Task 4: `refactor(v17.0-p320)!: rename export sheet Results->Summary` (breaking change)
  - Task 5: `feat(v17.0-p320): filter eligible sessions (Completed+Abandoned) for per-peserta sheets`
  - Task 6: `feat(v17.0-p320): per-peserta sheet with session header + info`
  - Task 7: `feat(v17.0-p320): per-peserta ElemenTeknis section (Variant A)`
  - Task 8: `feat(v17.0-p320): embed spider chart PNG (>=3 ET, SkiaSharp render)`
  - Task 9: `feat(v17.0-p320): per-peserta Detail Jawaban table (MC+MA, Tidak dijawab handling)`
  - Task 10: `feat(v17.0-p320): per-peserta Variant B (Manual Entry section)`

**PATTERNS.md insight critical:** JANGAN tambah `ws.Columns().AdjustToContents();` di Task 9 Step 2 — `Helpers/ExcelExportHelper.ToFileResult` (existing return statement) sudah handle AdjustToContents untuk semua worksheet. Skip step 2 Task 9 yang redundant tersebut.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/320-assessment-export-per-peserta-excel/320-CONTEXT.md
@.planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md
@.planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md
@.planning/phases/320-assessment-export-per-peserta-excel/320-01-SUMMARY.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md
@Controllers/AssessmentAdminController.cs
@Models/AssessmentSession.cs
@Helpers/ExcelExportHelper.cs
@Helpers/SpiderChartRenderer.cs
@Helpers/SheetNameSanitizer.cs

<interfaces>
<!-- Contracts dari Plan 01 yang akan dikonsumsi di plan ini -->

From Helpers/SpiderChartRenderer.cs:
```csharp
public static byte[] RenderRadarPng(IList<(string label, double percentage)> data, int size = 500);
// Returns byte[0] kalau data.Count < 3
```

From Helpers/SheetNameSanitizer.cs:
```csharp
public static string Sanitize(string nip, string fullName, ISet<string> usedNames);
// Mutates usedNames; returns 31-char Excel-safe name
```

From Helpers/ExcelExportHelper.cs (existing — DO NOT REIMPLEMENT):
```csharp
public static FileContentResult ToFileResult(XLWorkbook workbook, string fileName, ControllerBase controller);
// Auto-calls AdjustToContents() per worksheet; sets MIME type; returns FileContentResult
```

From Models/AssessmentSession.cs (verified 130-147 — semua string? nullable):
```csharp
public bool IsManualEntry { get; set; }      // line 130, default false
public string? ManualSertifikatUrl { get; set; }   // line 135
public string? Penyelenggara { get; set; }   // line 138
public string? Kota { get; set; }            // line 141
public string? SubKategori { get; set; }     // line 144
public string? CertificateType { get; set; } // line 147
// Existing fields (used di header):
public string? AssessmentType { get; set; }
public DateTime? StartedAt { get; set; }
public DateTime? CompletedAt { get; set; }
public int? ElapsedSeconds { get; set; }
public string Status { get; set; }           // "Completed", "Abandoned", "InProgress", "Not Started", "Cancelled"
public User? User { get; set; }              // navigation -> User.FullName, User.NIP
```

Existing method signature (line 3649-3651 — DO NOT CHANGE):
```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> ExportAssessmentResults(string title, string category, DateTime scheduleDate)
```

Existing return (line 3791-3794 — DO NOT CHANGE except filename suffix at Task 4):
```csharp
var safeTitle = System.Text.RegularExpressions.Regex.Replace(title, @"[^\w]", "_");
var fileName = $"{safeTitle}_{scheduleDate:yyyyMMdd}_Summary.xlsx";   // post-Task 4
return ExcelExportHelper.ToFileResult(workbook, fileName, this);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 4: Rename sheet "Results" → "Summary" + filename suffix (breaking change)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (read baris 3640-3795 — method `ExportAssessmentResults` full body, terutama line 3727 sheet creation + line 3793 filename)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 4 Step 1-4)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-CONTEXT.md (D-06 breaking change commit format `refactor(v17.0-p320)!:`)
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs`. 2 perubahan minimal:

    **Edit 1 — Baris ~3727 (sheet creation):**
    ```csharp
    // BEFORE
    var worksheet = workbook.Worksheets.Add("Results");

    // AFTER
    var worksheet = workbook.Worksheets.Add("Summary");
    ```

    **Edit 2 — Baris ~3793 (filename):**
    ```csharp
    // BEFORE
    var fileName = $"{safeTitle}_{scheduleDate:yyyyMMdd}_Results.xlsx";

    // AFTER
    var fileName = $"{safeTitle}_{scheduleDate:yyyyMMdd}_Summary.xlsx";
    ```

    Verifikasi line number aktual via Grep `Worksheets.Add\("Results"\)` + Grep `_Results\.xlsx` sebelum edit (drift mitigation kalau ada commit lain yang shift line number).

    Build + UAT (DEV_WORKFLOW §5):
    ```bash
    dotnet build
    dotnet run
    ```
    Buka `http://localhost:5277/AssessmentAdmin/ManageAssessment` (login admin per MEMORY.md `admin@pertamina.com`). Trigger Export pada satu grup assessment. Download file. Buka di Excel → tab pertama bernama "Summary" (bukan "Results"). Filename berakhiran `_Summary.xlsx`.

    Commit dengan breaking change tag (per D-06):
    ```bash
    git add Controllers/AssessmentAdminController.cs
    git commit -m "refactor(v17.0-p320)!: rename export sheet Results->Summary

    BREAKING CHANGE: Tab pertama export Excel sekarang bernama 'Summary' (sebelumnya 'Results'). File suffix berubah _Results.xlsx -> _Summary.xlsx. Konsumer downstream (script integrasi luar yang baca tab by name) WAJIB update."
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `Controllers/AssessmentAdminController.cs` contains `Worksheets.Add("Summary")` (exactly once di method ExportAssessmentResults)
    - File `Controllers/AssessmentAdminController.cs` does NOT contain `Worksheets.Add("Results")` di method ExportAssessmentResults
    - File contains literal string `_Summary.xlsx`
    - File does NOT contain literal string `_Results.xlsx`
    - `dotnet build` exits 0
    - `git log --oneline -1` shows subject starting with `refactor(v17.0-p320)!: rename export sheet Results->Summary`
    - Commit body contains paragraph `BREAKING CHANGE:`
  </acceptance_criteria>
  <done>
    Rename ter-commit dengan breaking change tag. UAT manual verify tab pertama "Summary" + filename `_Summary.xlsx`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Filter eligible sessions (Completed+Abandoned) + sheet name registry</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (read baris 3780-3795 — block "Data rows" akhir loop summary, sebelum `var safeTitle`)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 5 Step 1)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-CONTEXT.md (D-01 OrderBy FullName asc)
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs`. Setelah block "Data rows" (akhir loop yang isi Summary sheet, sekitar line 3789), **sebelum** `var safeTitle = ...` (line 3791), sisipkan filter + sheet name registry:

    ```csharp
    // === Per-Peserta Sheets (v17.0 Phase 320) ===
    // REQ EXP-05 — Filter peserta eligible: Completed + Abandoned only.
    // Skip InProgress, Not Started, Cancelled.
    // D-01 — OrderBy(FullName) ascending konsisten UI ManageAssessment.
    var eligibleSessions = sessions
        .Where(s => s.Status == "Completed" || s.Status == "Abandoned")
        .OrderBy(s => s.User?.FullName ?? "")
        .ToList();

    // Sheet name registry untuk collision guard (case-insensitive).
    // Pre-populate dengan "Summary" supaya peserta tidak collide dengan tab utama.
    var usedSheetNames = new HashSet<string>(StringComparer.OrdinalIgnoreCase) { "Summary" };
    ```

    Catatan implementasi: RESEARCH.md Task 5 Step 1 punya draft awal yang salah (`StringComparer.OrdinalIgnoreCase ? "Summary" : "Summary"`) lalu di-simplify di step note. Pakai yang **simplified version** (langsung `new HashSet<string>(StringComparer.OrdinalIgnoreCase) { "Summary" }`).

    Belum ada loop sheet creation di task ini — itu di Task 6. Task 5 hanya setup variabel.

    Build:
    ```bash
    dotnet build
    ```

    Build pass. UAT verify TIDAK ada perubahan visible (variabel belum dipakai sampai Task 6) — hanya konfirmasi build clean.

    Commit:
    ```bash
    git add Controllers/AssessmentAdminController.cs
    git commit -m "feat(v17.0-p320): filter eligible sessions (Completed+Abandoned) for per-peserta sheets"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS|warning CS8\d+)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `eligibleSessions = sessions` followed by chain `.Where(s => s.Status == "Completed" || s.Status == "Abandoned")`
    - File contains literal `.OrderBy(s => s.User?.FullName ?? "")`
    - File contains literal `new HashSet<string>(StringComparer.OrdinalIgnoreCase) { "Summary" }`
    - File does NOT contain the broken draft `StringComparer.OrdinalIgnoreCase ? "Summary"`
    - `dotnet build` exits 0 with 0 errors (warning CS0219 "variable declared but never used" allowed temporarily — diselesaikan di Task 6 saat variabel dipakai)
    - Commit subject `feat(v17.0-p320): filter eligible sessions (Completed+Abandoned) for per-peserta sheets`
  </acceptance_criteria>
  <done>
    Filter + registry ter-commit. Variabel `eligibleSessions` + `usedSheetNames` siap dikonsumsi Task 6 loop.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Per-peserta sheet loop dengan header info + pre-load batch query</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (read baris 3654-3686 — EF Core query existing pattern untuk mirror; baris 3733-3759 — cell+styling pattern existing)
    - Models/AssessmentSession.cs:130-147 (verify field IsManualEntry, AssessmentType, StartedAt, CompletedAt, ElapsedSeconds, User navigation)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 6 Step 1)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md (section "Pre-load batch query pattern" + "Per-peserta sheet loop" + "Em-dash Placeholder")
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs`. Setelah block Task 5 (sebelum `var safeTitle`), sisipkan pre-load batch + per-peserta foreach loop dengan header:

    ```csharp
    // Pre-load all per-session data in single query (avoid N+1)
    var eligibleSessionIds = eligibleSessions.Select(s => s.Id).ToList();
    var allResponses = await _context.PackageUserResponses
        .Where(r => eligibleSessionIds.Contains(r.AssessmentSessionId))
        .ToListAsync();
    var allEtScores = await _context.SessionElemenTeknisScores
        .Where(et => eligibleSessionIds.Contains(et.AssessmentSessionId))
        .ToListAsync();

    // Load all questions+options for involved packages
    var sessionPackageMap = await _context.UserPackageAssignments
        .Where(a => eligibleSessionIds.Contains(a.AssessmentSessionId))
        .Select(a => new { a.AssessmentSessionId, a.AssessmentPackageId })
        .ToListAsync();
    var packageIds = sessionPackageMap.Select(x => x.AssessmentPackageId).Distinct().ToList();
    var allQuestions = await _context.PackageQuestions
        .Include(q => q.Options)
        .Where(q => packageIds.Contains(q.AssessmentPackageId))
        .ToListAsync();

    foreach (var session in eligibleSessions)
    {
        string sheetName = HcPortal.Helpers.SheetNameSanitizer.Sanitize(
            session.User?.NIP ?? "NA",
            session.User?.FullName ?? "Unknown",
            usedSheetNames);
        var ws = workbook.Worksheets.Add(sheetName);

        // === Header (REQ EXP-03 Variant A header + REQ EXP-04 Variant B header sama) ===
        ws.Cell(1, 1).Value = $"{session.User?.FullName ?? "Unknown"} (NIP {session.User?.NIP ?? "—"})";
        ws.Range(1, 1, 1, 4).Merge();
        ws.Cell(1, 1).Style.Font.Bold = true;
        ws.Cell(1, 1).Style.Font.FontSize = 13;

        ws.Cell(2, 1).Value = "Started At";
        ws.Cell(2, 2).Value = session.StartedAt?.ToString("dd MMM yyyy HH:mm") ?? "—";
        ws.Cell(3, 1).Value = "Completed At";
        ws.Cell(3, 2).Value = session.CompletedAt?.ToString("dd MMM yyyy HH:mm") ?? "—";
        ws.Cell(4, 1).Value = "Durasi Aktual";
        int? durasi = session.ElapsedSeconds.HasValue ? session.ElapsedSeconds.Value / 60 : (int?)null;
        ws.Cell(4, 2).Value = durasi.HasValue ? $"{durasi.Value} menit" : "—";
        ws.Cell(5, 1).Value = "Tipe Assessment";
        ws.Cell(5, 2).Value = session.AssessmentType ?? "—";

        ws.Range(2, 1, 5, 1).Style.Font.Bold = true;
        // Section ElemenTeknis + Chart + Detail Jawaban DI Task 7-9 (Variant A).
        // Section Info Sertifikasi Manual DI Task 10 (Variant B).
    }
    ```

    Em-dash convention (per PATTERNS.md "Em-dash Placeholder" + RESEARCH.md): pakai literal `"—"` (em-dash U+2014), BUKAN `"-"` (hyphen) atau `"N/A"` — konsisten dengan existing line 3703-3706.

    Build + UAT:
    ```bash
    dotnet build
    dotnet run
    ```

    Login admin. Trigger Export grup assessment yang punya minimal 1 Completed + 1 Abandoned. Download file. Buka di Excel:
    - Tab pertama "Summary" (dari Task 4)
    - Tab tambahan N tab dengan format `{NIP}_{FullName}` untuk peserta Completed + Abandoned
    - Tab peserta berisi header `{FullName} (NIP {NIP})` di row 1, lalu Started At / Completed At / Durasi Aktual / Tipe Assessment di row 2-5
    - Peserta InProgress / Not Started / Cancelled TIDAK muncul sebagai tab

    Commit:
    ```bash
    git add Controllers/AssessmentAdminController.cs
    git commit -m "feat(v17.0-p320): per-peserta sheet with session header + info"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains `_context.PackageUserResponses` query with `eligibleSessionIds.Contains`
    - File contains `_context.SessionElemenTeknisScores` query with `eligibleSessionIds.Contains`
    - File contains `_context.UserPackageAssignments` query with `eligibleSessionIds.Contains`
    - File contains `_context.PackageQuestions.Include(q => q.Options)` query
    - File contains `foreach (var session in eligibleSessions)` block
    - File contains call `HcPortal.Helpers.SheetNameSanitizer.Sanitize`
    - File contains literal `ws.Cell(1, 1).Value = $"{session.User?.FullName ?? "Unknown"} (NIP`
    - File contains literal `"Started At"`, `"Completed At"`, `"Durasi Aktual"`, `"Tipe Assessment"` (4 header labels)
    - File contains em-dash placeholder `?? "—"` (NOT `?? "-"` atau `?? "N/A"`)
    - `dotnet build` exits 0
    - Commit subject `feat(v17.0-p320): per-peserta sheet with session header + info`
  </acceptance_criteria>
  <done>
    Per-peserta loop + pre-load batch + header section ter-commit. Excel export menghasilkan N+1 tab dengan header info per peserta.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Section "Analisis Elemen Teknis" Variant A + stub WriteManualEntrySection</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (line 3774 — XLColor.LightBlue header pattern existing)
    - Models/SessionElemenTeknisScore.cs (verify field name: ElemenTeknis, CorrectCount, QuestionCount, AssessmentSessionId)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 7 Step 1-2)
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs`. Di dalam `foreach (var session in eligibleSessions)` (block Task 6), tambahkan branching Variant A vs Variant B + tabel ElemenTeknis setelah header (akhir line `ws.Range(2, 1, 5, 1).Style.Font.Bold = true;`):

    ```csharp
        int currentRow = 7;

        if (session.IsManualEntry)
        {
            // Variant B: Manual Entry — body diisi di Task 10
            currentRow = WriteManualEntrySection(ws, session, currentRow);
            continue;
        }

        // === Variant A: Online — Section Analisis Elemen Teknis (REQ EXP-03) ===
        var sessionEt = allEtScores.Where(et => et.AssessmentSessionId == session.Id).ToList();
        if (sessionEt.Any())
        {
            ws.Cell(currentRow, 1).Value = "Analisis Elemen Teknis";
            ws.Cell(currentRow, 1).Style.Font.Bold = true;
            ws.Range(currentRow, 1, currentRow, 4).Merge();
            currentRow++;

            // Table header
            ws.Cell(currentRow, 1).Value = "Elemen Teknis";
            ws.Cell(currentRow, 2).Value = "Benar";
            ws.Cell(currentRow, 3).Value = "Total";
            ws.Cell(currentRow, 4).Value = "Persentase";
            ws.Range(currentRow, 1, currentRow, 4).Style.Font.Bold = true;
            ws.Range(currentRow, 1, currentRow, 4).Style.Fill.BackgroundColor = XLColor.LightBlue;
            currentRow++;

            foreach (var et in sessionEt.OrderBy(e => e.ElemenTeknis))
            {
                ws.Cell(currentRow, 1).Value = et.ElemenTeknis;
                ws.Cell(currentRow, 2).Value = et.CorrectCount;
                ws.Cell(currentRow, 3).Value = et.QuestionCount;
                double pct = et.QuestionCount > 0 ? (double)et.CorrectCount / et.QuestionCount * 100 : 0;
                ws.Cell(currentRow, 4).Value = $"{pct:F1}%";
                currentRow++;
            }
            currentRow++; // blank separator
        }
        // Skip section kalau sessionEt kosong (Abandoned tanpa ET, atau Essay-only)
    ```

    Tambah stub method `WriteManualEntrySection` di akhir class `AssessmentAdminController` (body lengkap diisi di Task 10):

    ```csharp
    private int WriteManualEntrySection(ClosedXML.Excel.IXLWorksheet ws, AssessmentSession session, int startRow)
    {
        // Implemented in Task 10
        return startRow;
    }
    ```

    Build + UAT:
    ```bash
    dotnet build
    dotnet run
    ```

    Login admin. Trigger Export. Buka tab peserta Online (non-Manual) → verify section "Analisis Elemen Teknis" muncul dengan tabel 4 kolom (Elemen / Benar / Total / Persentase), header row warna LightBlue. Buka tab peserta Abandoned dengan ET kosong → verify section auto-skip (langsung blank, tidak ada header tabel kosong).

    Commit:
    ```bash
    git add Controllers/AssessmentAdminController.cs
    git commit -m "feat(v17.0-p320): per-peserta ElemenTeknis section (Variant A)"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `int currentRow = 7;`
    - File contains literal `if (session.IsManualEntry)` branching
    - File contains call `currentRow = WriteManualEntrySection(ws, session, currentRow);` followed by `continue;`
    - File contains literal string `"Analisis Elemen Teknis"`
    - File contains literal `XLColor.LightBlue` for ET table header style
    - File contains 4 table header labels: `"Elemen Teknis"`, `"Benar"`, `"Total"`, `"Persentase"`
    - File contains literal `sessionEt.OrderBy(e => e.ElemenTeknis)`
    - File contains private stub method `private int WriteManualEntrySection(ClosedXML.Excel.IXLWorksheet ws, AssessmentSession session, int startRow)`
    - `dotnet build` exits 0
    - Commit subject `feat(v17.0-p320): per-peserta ElemenTeknis section (Variant A)`
  </acceptance_criteria>
  <done>
    Section ET ter-commit. Tab peserta Online menampilkan tabel ElemenTeknis. Branching Variant B siap untuk Task 10 (stub return startRow).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Embed Spider Chart PNG (>=3 ET, SkiaSharp inline render)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Helpers/SpiderChartRenderer.cs (signature `RenderRadarPng(IList<(string label, double percentage)> data, int size = 500)`)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 8 Step 1)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md (section "PNG embed pattern")
    - .planning/phases/320-assessment-export-per-peserta-excel/320-CONTEXT.md (D-02 chart warna, D-03 size 500 render embed 400×400)
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs`. Di dalam Variant A foreach (setelah block ET tabel Task 7, sebelum block Detail Jawaban di Task 9), tambah PNG render + embed:

    ```csharp
        // === Spider Chart PNG (REQ EXP-03, D-02 warna biru, D-03 render 500 embed 400) ===
        if (sessionEt.Count >= 3)
        {
            var chartData = sessionEt
                .OrderBy(e => e.ElemenTeknis)
                .Select(e => (e.ElemenTeknis, e.QuestionCount > 0 ? (double)e.CorrectCount / e.QuestionCount * 100 : 0d))
                .ToList();
            var png = HcPortal.Helpers.SpiderChartRenderer.RenderRadarPng(chartData);
            if (png.Length > 0)
            {
                using var ms = new MemoryStream(png);
                var pic = ws.AddPicture(ms, $"spider-{session.Id}")
                    .MoveTo(ws.Cell(currentRow, 1))
                    .WithSize(400, 400);
                currentRow += 22; // approx rows occupied by 400px image
            }
        }
    ```

    PENTING: render inline di Task 8 ini akan **direfactor jadi cache lookup di Plan 03 Task 11** (parallel pre-compute). Untuk Plan 02 Task 8 ini, inline render sudah sufficient untuk verify visual.

    Disposal: `using var ms = new MemoryStream(png)` memastikan stream di-release setelah `AddPicture` selesai consume bytes. Threat model T-320-02-04 (resource exhaustion) mitigated.

    Build + UAT:
    ```bash
    dotnet build
    dotnet run
    ```

    Login admin. Trigger Export grup dengan minimal 1 peserta Completed yang punya ≥3 ElemenTeknis. Download → buka di Excel → verify gambar radar/spider tampil 400×400 (warna biru per D-02) di bawah tabel ET. Verify chart skip untuk peserta dengan <3 ET.

    Commit:
    ```bash
    git add Controllers/AssessmentAdminController.cs
    git commit -m "feat(v17.0-p320): embed spider chart PNG (>=3 ET, SkiaSharp render)"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `if (sessionEt.Count >= 3)` guard
    - File contains call `HcPortal.Helpers.SpiderChartRenderer.RenderRadarPng(chartData)`
    - File contains `using var ms = new MemoryStream(png)` (disposal)
    - File contains `ws.AddPicture(ms, $"spider-{session.Id}")`
    - File contains `.WithSize(400, 400)` (D-03 embed size)
    - File contains `currentRow += 22;` (approx row offset)
    - `dotnet build` exits 0
    - Commit subject `feat(v17.0-p320): embed spider chart PNG (>=3 ET, SkiaSharp render)`
  </acceptance_criteria>
  <done>
    Spider chart embed ter-commit. UAT visual chart muncul 400×400 biru untuk peserta ≥3 ET, skip untuk <3 ET. Akan direfactor jadi cache lookup di Plan 03.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 9: Section "Detail Jawaban" MC + MA dengan "Tidak dijawab" handling</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Models/PackageQuestion.cs + Models/PackageOption.cs + Models/PackageUserResponse.cs (verify field name: QuestionType, QuestionText, Options navigation, OptionText, IsCorrect, PackageOptionId, PackageQuestionId, AssessmentSessionId)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 9 Step 1 — full code block)
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs`. Di dalam Variant A foreach (setelah block chart Task 8), tambah section Detail Jawaban:

    ```csharp
        // === Detail Jawaban (REQ EXP-03 — MC/MA per soal, Essay skip, "Tidak dijawab" untuk Abandoned) ===
        var sessionPackage = sessionPackageMap.FirstOrDefault(x => x.AssessmentSessionId == session.Id);
        if (sessionPackage != null)
        {
            var sessionQuestions = allQuestions
                .Where(q => q.AssessmentPackageId == sessionPackage.AssessmentPackageId)
                .OrderBy(q => q.Id)
                .ToList();
            var sessionResp = allResponses.Where(r => r.AssessmentSessionId == session.Id).ToList();

            ws.Cell(currentRow, 1).Value = "Detail Jawaban";
            ws.Cell(currentRow, 1).Style.Font.Bold = true;
            ws.Range(currentRow, 1, currentRow, 6).Merge();
            currentRow++;

            // Table header
            ws.Cell(currentRow, 1).Value = "No";
            ws.Cell(currentRow, 2).Value = "Soal";
            ws.Cell(currentRow, 3).Value = "Tipe";
            ws.Cell(currentRow, 4).Value = "Jawaban Peserta";
            ws.Cell(currentRow, 5).Value = "Jawaban Benar";
            ws.Cell(currentRow, 6).Value = "Status";
            ws.Range(currentRow, 1, currentRow, 6).Style.Font.Bold = true;
            ws.Range(currentRow, 1, currentRow, 6).Style.Fill.BackgroundColor = XLColor.LightBlue;
            currentRow++;

            int no = 1;
            foreach (var q in sessionQuestions)
            {
                string tipe = q.QuestionType ?? "MultipleChoice";

                if (tipe == "Essay")
                {
                    ws.Cell(currentRow, 1).Value = no++;
                    ws.Cell(currentRow, 2).Value = q.QuestionText;
                    ws.Cell(currentRow, 3).Value = "Essay";
                    ws.Cell(currentRow, 4).Value = "Essay – manual grading (lihat Penilaian Essay)";
                    ws.Cell(currentRow, 5).Value = "—";
                    ws.Cell(currentRow, 6).Value = "—";
                    currentRow++;
                    continue;
                }

                var responses = sessionResp.Where(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue).ToList();
                string jawabanText;
                bool correct;

                if (!responses.Any())
                {
                    // Soal tanpa response (Abandoned skip soal) — REQ EXP-03
                    jawabanText = "Tidak dijawab";
                    correct = false;
                }
                else if (tipe == "MultipleChoice")
                {
                    var optId = responses.First().PackageOptionId!.Value;
                    var opt = q.Options.FirstOrDefault(o => o.Id == optId);
                    jawabanText = opt?.OptionText ?? "—";
                    correct = opt?.IsCorrect == true;
                }
                else // MultipleAnswer
                {
                    var selectedIds = responses.Select(r => r.PackageOptionId!.Value).ToHashSet();
                    jawabanText = string.Join(", ",
                        q.Options.Where(o => selectedIds.Contains(o.Id)).Select(o => o.OptionText));
                    var correctIds = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
                    correct = selectedIds.SetEquals(correctIds);
                }

                string correctText = string.Join(", ", q.Options.Where(o => o.IsCorrect).Select(o => o.OptionText));

                ws.Cell(currentRow, 1).Value = no++;
                ws.Cell(currentRow, 2).Value = q.QuestionText;
                ws.Cell(currentRow, 3).Value = tipe == "MultipleChoice" ? "MC" : "MA";
                ws.Cell(currentRow, 4).Value = jawabanText;
                ws.Cell(currentRow, 5).Value = correctText;
                ws.Cell(currentRow, 6).Value = correct ? "✓" : "✗";
                currentRow++;
            }
        }
    ```

    **CRITICAL — SKIP Step 2 dari RESEARCH.md Task 9** ("Auto-fit columns" `ws.Columns().AdjustToContents();`). Per PATTERNS.md insight (CRITICAL), `Helpers/ExcelExportHelper.ToFileResult` di return existing sudah auto-call AdjustToContents() untuk SEMUA worksheet (baris 28-29 helper). JANGAN duplicate logic — itu redundant. Jika RESEARCH.md step 2 diterapkan, hasil tetap idempotent tapi adds noise → skip.

    Build + UAT:
    ```bash
    dotnet build
    dotnet run
    ```

    Login admin. Trigger Export. Buka tab peserta Completed dengan soal MC + MA + Essay → verify:
    - Tabel "Detail Jawaban" muncul dengan 6 kolom (No / Soal / Tipe / Jawaban Peserta / Jawaban Benar / Status)
    - Soal MC → Tipe="MC", jawaban tunggal, status `✓` atau `✗`
    - Soal MA → Tipe="MA", jawaban multi comma-joined, status `✓` hanya kalau SET EXACT match dengan correct
    - Soal Essay → "Essay – manual grading (lihat Penilaian Essay)" di kolom Jawaban Peserta
    - Soal yang Abandoned skip → "Tidak dijawab" di kolom Jawaban Peserta, status `✗`

    Commit:
    ```bash
    git add Controllers/AssessmentAdminController.cs
    git commit -m "feat(v17.0-p320): per-peserta Detail Jawaban table (MC+MA, Tidak dijawab handling)"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal string `"Detail Jawaban"`
    - File contains 6 table header labels: `"No"`, `"Soal"`, `"Tipe"`, `"Jawaban Peserta"`, `"Jawaban Benar"`, `"Status"`
    - File contains literal `"Essay – manual grading (lihat Penilaian Essay)"` (em-dash variant)
    - File contains literal `"Tidak dijawab"` (Abandoned skip handling)
    - File contains literal `selectedIds.SetEquals(correctIds)` (MA exact match logic)
    - File contains ternary `correct ? "✓" : "✗"`
    - File does NOT contain redundant `ws.Columns().AdjustToContents()` inside foreach (PATTERNS.md skip directive)
    - `dotnet build` exits 0
    - Commit subject `feat(v17.0-p320): per-peserta Detail Jawaban table (MC+MA, Tidak dijawab handling)`
  </acceptance_criteria>
  <done>
    Detail Jawaban section ter-commit dengan MC/MA/Essay handling + "Tidak dijawab" untuk soal yang Abandoned skip. AdjustToContents() TIDAK di-duplicate (delegated ke ExcelExportHelper.ToFileResult existing return).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 10: Variant B — Implementasi WriteManualEntrySection (Info Sertifikasi Manual + hyperlink)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Models/AssessmentSession.cs:130-147 (verify field IsManualEntry, ManualSertifikatUrl, Penyelenggara, Kota, SubKategori, CertificateType — semua string? nullable)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-RESEARCH.md (Task 10 Step 1)
    - .planning/phases/320-assessment-export-per-peserta-excel/320-PATTERNS.md (section "Hyperlink pattern" + "Field access AssessmentSession Manual Entry" table)
  </read_first>
  <action>
    Edit `Controllers/AssessmentAdminController.cs`. Replace stub method `WriteManualEntrySection` (dari Task 7) dengan body lengkap:

    ```csharp
    private int WriteManualEntrySection(ClosedXML.Excel.IXLWorksheet ws, AssessmentSession session, int startRow)
    {
        int row = startRow;
        ws.Cell(row, 1).Value = "Info Sertifikasi Manual";
        ws.Cell(row, 1).Style.Font.Bold = true;
        ws.Range(row, 1, row, 2).Merge();
        row++;

        var fields = new (string Label, string? Value)[]
        {
            ("Penyelenggara",    session.Penyelenggara),
            ("Kota",             session.Kota),
            ("Sub Kategori",     session.SubKategori),
            ("Tipe Sertifikat",  session.CertificateType),
        };
        foreach (var (label, value) in fields)
        {
            ws.Cell(row, 1).Value = label;
            ws.Cell(row, 1).Style.Font.Bold = true;
            ws.Cell(row, 2).Value = value ?? "—";
            row++;
        }

        // Hyperlink ManualSertifikatUrl (REQ EXP-04 clickable link)
        ws.Cell(row, 1).Value = "Link Sertifikat";
        ws.Cell(row, 1).Style.Font.Bold = true;
        if (!string.IsNullOrWhiteSpace(session.ManualSertifikatUrl))
        {
            ws.Cell(row, 2).Value = session.ManualSertifikatUrl;
            ws.Cell(row, 2).SetHyperlink(new ClosedXML.Excel.XLHyperlink(session.ManualSertifikatUrl));
        }
        else
        {
            ws.Cell(row, 2).Value = "—";
        }
        row++;

        return row;
    }
    ```

    Field nullable handling (per PATTERNS.md table): semua field Manual Entry adalah `string?` → guard dengan `?? "—"` (em-dash, konsisten dengan existing controller line 3703-3706). JANGAN ganti dengan `?? "-"` atau `?? "N/A"`.

    Field name verify (per CONTEXT.md "Field name verification ... sudah verified codebase Models/AssessmentSession.cs:130-147 — no further check needed"). Kalau build error muncul "AssessmentSession does not contain ...", grep ulang pakai:
    ```bash
    grep -n "Penyelenggara\|Kota\|SubKategori\|CertificateType\|ManualSertifikatUrl\|IsManualEntry" Models/AssessmentSession.cs
    ```

    Build + UAT:
    ```bash
    dotnet build
    dotnet run
    ```

    Login admin. Cari satu group assessment yang punya minimal 1 session `IsManualEntry = true` (kalau tidak ada di DB lokal, lakukan klasifikasi seed temporary per CLAUDE.md `Seed Data Workflow` — UPDATE 1 session existing jadi IsManualEntry=1 + isi 5 field manual entry, snapshot DB dulu, restore setelah UAT). Trigger Export. Verify:
    - Tab peserta manual entry: section "Info Sertifikasi Manual" dengan 4 row label-value (Penyelenggara/Kota/Sub Kategori/Tipe Sertifikat) + row "Link Sertifikat" dengan hyperlink clickable
    - Click hyperlink → browser/Excel open URL ManualSertifikatUrl
    - Section ElemenTeknis / Chart / Detail Jawaban TIDAK muncul di tab Variant B (sudah di-skip via `continue;` di Task 7)

    Commit:
    ```bash
    git add Controllers/AssessmentAdminController.cs
    git commit -m "feat(v17.0-p320): per-peserta Variant B (Manual Entry section)"
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "(Build succeeded|error CS)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains private method body `private int WriteManualEntrySection(ClosedXML.Excel.IXLWorksheet ws, AssessmentSession session, int startRow)` with NON-empty body (>= 20 lines)
    - File contains literal string `"Info Sertifikasi Manual"`
    - File contains 4 field labels: `"Penyelenggara"`, `"Kota"`, `"Sub Kategori"`, `"Tipe Sertifikat"`
    - File contains literal `"Link Sertifikat"`
    - File contains `new ClosedXML.Excel.XLHyperlink(session.ManualSertifikatUrl)`
    - File contains `string.IsNullOrWhiteSpace(session.ManualSertifikatUrl)` guard
    - File contains `?? "—"` em-dash fallback (NOT `?? "-"` atau `?? "N/A"`)
    - Method body references all 5 verified fields: `session.Penyelenggara`, `session.Kota`, `session.SubKategori`, `session.CertificateType`, `session.ManualSertifikatUrl`
    - `dotnet build` exits 0
    - Commit subject `feat(v17.0-p320): per-peserta Variant B (Manual Entry section)`
  </acceptance_criteria>
  <done>
    WriteManualEntrySection body lengkap ter-commit. UAT tab Variant B menampilkan 5 row info + hyperlink ManualSertifikatUrl clickable. Plan 02 selesai — controller full functional, siap untuk Plan 03 performance refactor + Playwright + UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → controller action | `[Authorize(Roles = "Admin, HC")]` attribute existing — Worker dan role lain blocked di routing layer |
| User.FullName + User.NIP → Excel worksheet name | User-provided text yang masuk Excel API — Excel-invalid char scrub via SheetNameSanitizer (Plan 01) |
| EF Core query result → Excel cell value | Trusted internal DB; nullable string fields guarded dengan `?? "—"` |
| ManualSertifikatUrl → XLHyperlink | User-provided URL (input dari form admin/HC saat create manual entry session) — Excel akan render sebagai clickable link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-320-02-01 | Spoofing/EoP | Authorization bypass (Worker akses endpoint Export) | mitigate | `[Authorize(Roles = "Admin, HC")]` attribute existing di line 3650 — TIDAK diubah Plan 02. Acceptance: grep file shows attribute persists. REQ EXP-07 inherent. Verifikasi otomatis di Plan 03 Playwright (Worker 403). |
| T-320-02-02 | Tampering | Sheet name injection via User.FullName user-provided | mitigate | `SheetNameSanitizer.Sanitize` (Plan 01) scrub `\ / ? * [ ] :` sebelum AddWorksheet. Acceptance: call `HcPortal.Helpers.SheetNameSanitizer.Sanitize` ada di loop foreach. |
| T-320-02-03 | Information Disclosure | Filename `{title}_{date}_Summary.xlsx` + sheet name `{NIP}_{FullName}` ekspos NIP + FullName ke siapa pun yang punya file | accept | Sudah locked oleh REQ EXP-06 + EXP-07 — Admin/HC saja yang dapat trigger download. NIP unique guarantee + email-only distribution policy (out-of-scope phase). |
| T-320-02-04 | Denial of Service | Resource exhaustion (50+ peserta + render PNG inline per peserta → memory pressure thread pool) | accept (Plan 02) / mitigate (Plan 03) | Plan 02 inline render — akan di-refactor jadi `Parallel.ForEachAsync` cap MaxDegreeOfParallelism=Cores di Plan 03 Task 11. Plan 03 benchmark 30s gate ensure SLA. |
| T-320-02-05 | Tampering/XSS | Malicious URL di `ManualSertifikatUrl` masuk hyperlink (e.g. `javascript:` scheme) | accept | Excel/LibreOffice tidak execute `javascript:` di hyperlink (Excel sandboxes hyperlink ke browser external launcher). Input source dari form admin/HC saja (trusted). Tidak ada user-uploaded URL. |
| T-320-02-06 | Information Disclosure | Detail Jawaban berisi correct answer texts → kalau file leak, peserta lain bisa lihat kunci jawaban | accept | Sudah locked REQ EXP-07 Admin/HC only. Distribusi file = tanggung jawab user, out-of-scope teknis. |
| T-320-02-07 | Denial of Service | N+1 query (50 peserta × multiple table query in loop) → DB connection pool exhaustion | mitigate | Pre-load batch 4 query (PackageUserResponses + SessionElemenTeknisScores + UserPackageAssignments + PackageQuestions.Include(Options)) DI LUAR foreach. Acceptance: 4 `_context.` query ada SEBELUM `foreach (var session in eligibleSessions)`. |
</threat_model>

<verification>
**Per-task verification** (lihat `<verify>` setiap task — `dotnet build` exit 0 + grep acceptance criteria).

**Plan-level integration (sebelum claim Plan 02 selesai):**

1. `dotnet build` overall pass tanpa warning baru
2. `git log --oneline -7` menampilkan 7 commit dengan subject sesuai Task 4-10 (1 breaking change `refactor!:`, 6 `feat:`)
3. `grep -c "Worksheets.Add" Controllers/AssessmentAdminController.cs` ≥ 2 (Summary tab + per-peserta tab loop)
4. `grep "_context\." Controllers/AssessmentAdminController.cs | head -20` shows 4 baru query (PackageUserResponses, SessionElemenTeknisScores, UserPackageAssignments, PackageQuestions) DI LUAR foreach
5. End-to-end UAT smoke: `dotnet run` → login admin → Export grup test → verify:
   - Tab 1 "Summary" (renamed)
   - Tab 2..N+1 per-peserta dengan nama `{NIP}_{FullName}` 31-char
   - Tab Online: header + ET tabel + chart PNG + Detail Jawaban
   - Tab Manual Entry: header + Info Sertifikasi Manual + hyperlink
6. `grep "AdjustToContents" Controllers/AssessmentAdminController.cs` di dalam method ExportAssessmentResults TIDAK MUNCUL (PATTERNS.md skip directive verified — delegated ke ExcelExportHelper.ToFileResult existing return statement)

**Manual UAT singkat di akhir Plan 02 (full UAT checklist 8 step di Plan 03 Task 12):**
- Export 1 grup → buka di Excel → konfirmasi struktur Summary + per-peserta sheets benar
- Filename berakhiran `_Summary.xlsx` (bukan `_Results.xlsx`)
- Chart muncul untuk peserta ≥3 ET, skip untuk <3 ET
- Hyperlink Manual Entry clickable
</verification>

<success_criteria>
- 7 commit atomic merged ke local branch (1 breaking `refactor!`, 6 `feat`)
- `dotnet build` pass 0 error 0 warning baru
- Excel export functional end-to-end:
  - Tab "Summary" + N tab peserta
  - Variant A: header + ET tabel + Spider Chart PNG + Detail Jawaban
  - Variant B: header + Info Sertifikasi Manual + hyperlink
  - Filter Completed + Abandoned only
  - Sheet name 31-char + Excel-safe + collision-free
- Pre-load batch query (no N+1)
- Authorize attribute existing PRESERVED (no regression REQ EXP-07)
- AdjustToContents() TIDAK di-duplicate (PATTERNS.md insight applied)

**DEV_WORKFLOW §5 pre-commit checklist per commit:**
- [ ] dotnet build pass (tanpa warning baru)
- [ ] dotnet run + browser UAT verify
- [ ] Golden path (1 grup Online + 1 grup mixed Manual) + edge case (peserta <3 ET) dicek manual
- [ ] No DB migration (Plan 02 tidak touch schema)
- [ ] (Optional) Playwright test — defer ke Plan 03
- [ ] Notify IT — defer ke Plan 03 final commit (notif gabungan setelah benchmark + UAT selesai)
</success_criteria>

<output>
After completion, create `.planning/phases/320-assessment-export-per-peserta-excel/320-02-SUMMARY.md` dengan:
- Daftar 7 commit hash + subject
- Baseline file size hasil export 10 peserta (untuk Plan 03 baseline benchmark)
- Path file modified (1 file: AssessmentAdminController.cs)
- UAT manual notes (apakah ada peserta IsManualEntry di DB lokal; kalau tidak ada, sebut sudah seed temporary + restore)
- Konfirmasi AdjustToContents() di-skip per PATTERNS.md
- Sinyal Plan 03 ready: "Controller refactor selesai, siap parallel PNG pre-compute + Playwright + UAT full checklist"
</output>
</content>
</invoke>