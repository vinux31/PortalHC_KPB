---
phase: 321-assessment-edit-jawaban-peserta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentEditLog.cs
  - Models/EditPesertaAnswersViewModel.cs
  - Models/EditAnswersSubmission.cs
  - Data/ApplicationDbContext.cs
  - Migrations/{timestamp}_AddAssessmentEditLogs.cs
  - Migrations/{timestamp}_AddAssessmentEditLogs.Designer.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - Helpers/AssessmentEditEligibility.cs
autonomous: false
requirements: [EDIT-02, EDIT-06, EDIT-13]
user_setup: []
must_haves:
  truths:
    - "Tabel AssessmentEditLogs ada di DB lokal SQL Server HcPortalDB_Dev dengan index IX_AssessmentEditLogs_SessionId_EditedAt"
    - "Migration AddAssessmentEditLogs apply + rollback test lokal lulus (DEV_WORKFLOW §4)"
    - "Helper AssessmentEditEligibility.IsEditableAsync / IsEditableShallow tersedia untuk gating GET, POST, dan UI dropdown"
    - "View Model DTOs (EditPesertaAnswersViewModel + EditAnswersSubmission + EditDraftSubmission) tersedia untuk PLAN 03"
    - "Branch feature/phase-321-edit-jawaban dibuat dan checkout aktif"
  artifacts:
    - path: "Models/AssessmentEditLog.cs"
      provides: "EF entity audit granular per question edit (schema spec 4.7)"
      contains: "public class AssessmentEditLog"
    - path: "Models/EditPesertaAnswersViewModel.cs"
      provides: "View model GET edit page + EditQuestionRow + EditOptionRow"
      contains: "public class EditPesertaAnswersViewModel"
    - path: "Models/EditAnswersSubmission.cs"
      provides: "DTO POST SubmitEditAnswers + EditReason + EditDraft + EditDraftSubmission"
      contains: "public class EditAnswersSubmission"
    - path: "Helpers/AssessmentEditEligibility.cs"
      provides: "Static IsEditableAsync + IsEditableShallow gating helper"
      contains: "public static class AssessmentEditEligibility"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet<AssessmentEditLog> + fluent index config"
      contains: "DbSet<AssessmentEditLog> AssessmentEditLogs"
    - path: "Migrations/{timestamp}_AddAssessmentEditLogs.cs"
      provides: "EF migration creates AssessmentEditLogs table + index"
      contains: "AssessmentEditLogs"
  key_links:
    - from: "Models/AssessmentEditLog.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "DbSet registration + fluent config"
      pattern: "DbSet<AssessmentEditLog>"
    - from: "Helpers/AssessmentEditEligibility.cs"
      to: "Models/AssessmentSession.cs (Status, IsManualEntry, Category, TahunKe fields)"
      via: "field-based eligibility check"
      pattern: "s.Status == \"Completed\""
---

<objective>
PLAN 01 (Wave 1) — Model + Migration + Helper Foundation untuk Phase 321 Edit Jawaban Peserta.

Purpose: Bangun foundation layer (data + helper + ViewModels) yang dipakai PLAN 02 (service), PLAN 03 (controller/view/frontend), dan PLAN 04 (activity log + UAT). Sequential strict per CONTEXT D-02 — PLAN 02 dst. block sampai PLAN 01 selesai.

Output:
- Entity `AssessmentEditLog` + EF migration `AddAssessmentEditLogs` (apply + rollback verified lokal)
- Helper `AssessmentEditEligibility` (IsEditableAsync + IsEditableShallow)
- View Model DTOs (`EditPesertaAnswersViewModel`, `EditAnswersSubmission`, `EditDraft`, `EditDraftSubmission`)
- Branch `feature/phase-321-edit-jawaban` checkout aktif untuk semua commit PLAN 01-04 (per CONTEXT D-03)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md
@Models/AssessmentSession.cs
@Data/ApplicationDbContext.cs

<interfaces>
<!-- Key types the executor needs. Extracted from codebase 2026-05-21. -->

From Models/AssessmentSession.cs (verified line numbers):
- `string Category` (line 16) — "Assessment Proton" untuk Proton T3 gating
- `string Status` (line 20) — "Completed" / "InProgress" / "Abandoned" / "Cancelled" / "Not started"
- `int? Score` (existing) + `bool? IsPassed` (existing)
- `bool IsManualEntry` (line 130) — gating eligibility
- `string TahunKe` (line 101) — "Tahun 3" untuk Proton gating
- `DateTime? UpdatedAt` (existing concurrency token)
- `DateTime CreatedAt` (fallback kalau UpdatedAt null)

From Data/ApplicationDbContext.cs:
- DbSet `SessionElemenTeknisScores` (line ~77) — adjacent insertion point untuk `AssessmentEditLogs` DbSet
- `OnModelCreating` entry point line ~128
- `AssessmentSession` fluent config block ~line 170-228 (`CK_AssessmentSession_RenewalChain` constraint nearby)

ViewModel namespace convention (verified codebase scout):
- File location: `Models/*ViewModel.cs` (FLAT, not Models/ViewModels/) — sample existing: `Models/AssessmentMonitoringViewModel.cs`, `Models/AssessmentResultsViewModel.cs`
- Namespace: `namespace HcPortal.Models` (NOT `HcPortal.Models.ViewModels`)
- **RESEARCH override**: RESEARCH.md Task 5 + Task 8 menulis `Models/ViewModels/*.cs` dengan namespace `HcPortal.Models.ViewModels` — Executor WAJIB ikut konvensi codebase existing: `Models/EditPesertaAnswersViewModel.cs` + `Models/EditAnswersSubmission.cs` dengan namespace `HcPortal.Models`.
- Consumer di controller cukup `using HcPortal.Models;` (sudah ada di `AssessmentAdminController.cs`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feature branch + Model AssessmentEditLog + DbSet + Fluent Config</name>
  <files>Models/AssessmentEditLog.cs, Data/ApplicationDbContext.cs</files>
  <read_first>
    - Models/AssessmentSession.cs (verify field paths: Category line 16, Status line 20, IsManualEntry line 130, TahunKe line 101, UpdatedAt, CreatedAt)
    - Data/ApplicationDbContext.cs (verify DbSet insertion point near SessionElemenTeknisScores ~line 77, OnModelCreating ~line 128, AssessmentSession fluent config ~line 170-228)
    - Models/AssessmentMonitoringViewModel.cs (verify namespace convention: HcPortal.Models flat — NOT HcPortal.Models.ViewModels)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 1 (full code block lines 122-228)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-03 (branch strategy)
    - docs/DEV_WORKFLOW.md §5 (pre-commit checklist)
  </read_first>
  <action>
**Step 1 — Create feature branch (D-03 LOCKED):**
```bash
git checkout -b feature/phase-321-edit-jawaban
git branch --show-current   # verify output = "feature/phase-321-edit-jawaban"
```

**Step 2 — Create `Models/AssessmentEditLog.cs`** with exact namespace `HcPortal.Models` (codebase convention — NOT `HcPortal.Models.ViewModels`):

```csharp
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;

namespace HcPortal.Models
{
    /// <summary>
    /// Granular audit log per question edit oleh Admin/HC.
    /// Snapshot text disimpan supaya audit tetap readable kalau soal/option dihapus.
    /// </summary>
    public class AssessmentEditLog
    {
        [Key]
        public int Id { get; set; }

        public int AssessmentSessionId { get; set; }
        [ForeignKey("AssessmentSessionId")]
        public virtual AssessmentSession? AssessmentSession { get; set; }

        public int PackageQuestionId { get; set; }

        public string QuestionTextSnapshot { get; set; } = "";
        public string OldAnswerJson { get; set; } = "[]";
        public string OldAnswerTextSnapshot { get; set; } = "";
        public string NewAnswerJson { get; set; } = "[]";
        public string NewAnswerTextSnapshot { get; set; } = "";

        public int? OldScore { get; set; }
        public int? NewScore { get; set; }
        public bool? OldIsPassed { get; set; }
        public bool? NewIsPassed { get; set; }

        public string ActorUserId { get; set; } = "";
        public string ActorName { get; set; } = "";
        public string ActorRole { get; set; } = "";

        public DateTime EditedAt { get; set; } = DateTime.UtcNow;

        public string ReasonCode { get; set; } = "";
        public string? ReasonText { get; set; }
    }
}
```

**Step 3 — Modify `Data/ApplicationDbContext.cs`:**

(a) Add DbSet near existing `SessionElemenTeknisScores` DbSet (~line 77):
```csharp
public DbSet<AssessmentEditLog> AssessmentEditLogs { get; set; }
```

(b) In `OnModelCreating` add fluent config AFTER existing `AssessmentSession` block (insert AFTER `CK_AssessmentSession_RenewalChain` constraint, near line 228):
```csharp
builder.Entity<AssessmentEditLog>(entity =>
{
    entity.HasOne(e => e.AssessmentSession)
        .WithMany()
        .HasForeignKey(e => e.AssessmentSessionId)
        .OnDelete(DeleteBehavior.Restrict);

    entity.HasIndex(e => new { e.AssessmentSessionId, e.EditedAt })
        .HasDatabaseName("IX_AssessmentEditLogs_SessionId_EditedAt")
        .IsDescending(false, true);
});
```

**Step 4 — Build:**
```bash
dotnet build
```
Verify 0 error.

**Step 5 — Commit (1-task-1-commit per D-10):**
```bash
git add Models/AssessmentEditLog.cs Data/ApplicationDbContext.cs
git commit -m "feat(v17.0-p321): add AssessmentEditLog model + DbSet + fluent index"
```

NOTE: Migration generate + apply/rollback dipindah ke Task 2 (atomic test wajib `autonomous: false`).
  </action>
  <verify>
    <automated>git branch --show-current | grep -q "feature/phase-321-edit-jawaban" && dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "public class AssessmentEditLog" Models/AssessmentEditLog.cs && grep -q "DbSet<AssessmentEditLog>" Data/ApplicationDbContext.cs && grep -q "IX_AssessmentEditLogs_SessionId_EditedAt" Data/ApplicationDbContext.cs</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` returns exactly `feature/phase-321-edit-jawaban`
    - `dotnet build` exits 0 with "Build succeeded" and 0 Error
    - `grep "public class AssessmentEditLog" Models/AssessmentEditLog.cs` exits 0
    - `grep "namespace HcPortal.Models" Models/AssessmentEditLog.cs` exits 0 (NOT `HcPortal.Models.ViewModels`)
    - `grep "DbSet<AssessmentEditLog> AssessmentEditLogs" Data/ApplicationDbContext.cs` exits 0
    - `grep "IX_AssessmentEditLogs_SessionId_EditedAt" Data/ApplicationDbContext.cs` exits 0
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): add AssessmentEditLog model`
  </acceptance_criteria>
  <done>Branch feature/phase-321-edit-jawaban aktif, model + DbSet + fluent config compile clean, committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: Generate Migration AddAssessmentEditLogs + Apply + Rollback Test (DEV_WORKFLOW §4)</name>
  <files>Migrations/{timestamp}_AddAssessmentEditLogs.cs, Migrations/{timestamp}_AddAssessmentEditLogs.Designer.cs, Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <read_first>
    - docs/DEV_WORKFLOW.md §4 (Migration SOP — apply + rollback lokal WAJIB sebelum commit)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 1 Step 3-4 (lines 197-217)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-08 (IT notify preemptif setelah PLAN 01 selesai)
  </read_first>
  <what-built>
**Automated steps (Claude jalankan dulu):**

**Step 1 — Generate migration:**
```bash
dotnet ef migrations add AddAssessmentEditLogs --context ApplicationDbContext
```
Expected output: 3 file dibuat — `Migrations/{timestamp}_AddAssessmentEditLogs.cs` + `.Designer.cs` + update `ApplicationDbContextModelSnapshot.cs`.

**Step 2 — Inspect migration up() method:**
```bash
grep -A 30 "protected override void Up" Migrations/*_AddAssessmentEditLogs.cs
```
Verify CreateTable `AssessmentEditLogs` + CreateIndex `IX_AssessmentEditLogs_SessionId_EditedAt` muncul.

**Step 3 — Identify PreviousMigrationName:**
```bash
ls Migrations/*.cs | grep -v Designer | grep -v ModelSnapshot | sort | tail -2 | head -1
# Catat nama tanpa .cs untuk rollback test
```

**Step 4 — Apply migration ke DB lokal:**
```bash
dotnet ef database update --context ApplicationDbContext
```
Expected: "Done." Verify table exist:
```bash
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT name FROM sys.tables WHERE name='AssessmentEditLogs'; SELECT name FROM sys.indexes WHERE name='IX_AssessmentEditLogs_SessionId_EditedAt'"
```
Expected: 2 rows returned (table + index).

**Step 5 — Rollback test:**
```bash
dotnet ef database update {PreviousMigrationName} --context ApplicationDbContext
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT OBJECT_ID('AssessmentEditLogs')"
# Expected: NULL (table dropped)
```

**Step 6 — Re-apply:**
```bash
dotnet ef database update --context ApplicationDbContext
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT OBJECT_ID('AssessmentEditLogs')"
# Expected: integer (table re-created)
```

**Step 7 — Build:**
```bash
dotnet build
```

**Step 8 — Commit:**
```bash
git add Migrations/
git commit -m "feat(v17.0-p321): migration AddAssessmentEditLogs (apply + rollback verified lokal)"
```
  </what-built>
  <how-to-verify>
1. **User confirm DB state lokal:**
   ```bash
   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT TOP 1 * FROM AssessmentEditLogs"
   ```
   Expected: "(0 rows affected)" — table exist tapi kosong.

2. **User verify index exists:**
   ```bash
   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT i.name, c.name AS col, ic.is_descending_key FROM sys.indexes i JOIN sys.index_columns ic ON i.object_id=ic.object_id AND i.index_id=ic.index_id JOIN sys.columns c ON ic.object_id=c.object_id AND ic.column_id=c.column_id WHERE i.name='IX_AssessmentEditLogs_SessionId_EditedAt' ORDER BY ic.key_ordinal"
   ```
   Expected: 2 rows — `AssessmentSessionId` (is_descending_key=0) + `EditedAt` (is_descending_key=1).

3. **User verify git log:**
   ```bash
   git log --oneline -2
   ```
   Expected: 2 commits — Model commit + Migration commit.

4. **User notify IT preemptif (D-08 LOCKED):**
   Drop pesan ke channel IT:
   ```
   Heads-up: Phase 321 (Edit Jawaban Peserta) lagi development. Ada migration baru: AddAssessmentEditLogs.
   Target deploy: ~beberapa hari. Tolong siapin slot maintenance DB Dev untuk migration apply nanti.
   Branch: feature/phase-321-edit-jawaban.
   ```
  </how-to-verify>
  <resume-signal>Type "approved" kalau migration apply + rollback OK + IT preemptif sudah di-notify, atau describe issue (mis. table tidak terbuat, index missing, sqlcmd connection error).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Helper AssessmentEditEligibility (IsEditableAsync + IsEditableShallow)</name>
  <files>Helpers/AssessmentEditEligibility.cs</files>
  <read_first>
    - Models/AssessmentSession.cs (re-verify field paths: Status:20, IsManualEntry:130, Category:16, TahunKe:101)
    - Helpers/CertNumberHelper.cs (verify namespace pattern `HcPortal.Helpers` + static class convention)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 2 (lines 232-286)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md CD-01 (Proton T3 detection)
  </read_first>
  <action>
**Step 1 — Create `Helpers/AssessmentEditEligibility.cs`:**

```csharp
using HcPortal.Data;
using HcPortal.Models;
using Microsoft.EntityFrameworkCore;

namespace HcPortal.Helpers
{
    /// <summary>
    /// Cek apakah AssessmentSession boleh di-edit oleh Admin/HC (spec 4.2, REQ EDIT-02).
    /// Aturan gating:
    ///   - Status = "Completed"
    ///   - bukan IsManualEntry
    ///   - bukan Assessment Proton Tahun 3 (per CONTEXT CD-01: Category=="Assessment Proton" && TahunKe=="Tahun 3")
    ///   - punya UserPackageAssignment (async variant only — sync skip DB check)
    /// </summary>
    public static class AssessmentEditEligibility
    {
        public static async Task<bool> IsEditableAsync(ApplicationDbContext db, AssessmentSession s)
        {
            if (s == null) return false;
            if (s.Status != "Completed") return false;
            if (s.IsManualEntry) return false;
            if (s.Category == "Assessment Proton" && s.TahunKe == "Tahun 3") return false;

            bool hasAssignment = await db.UserPackageAssignments
                .AnyAsync(a => a.AssessmentSessionId == s.Id);
            return hasAssignment;
        }

        /// <summary>Sync version for view-side rendering (skip assignment DB check; render-time only).</summary>
        public static bool IsEditableShallow(AssessmentSession s)
        {
            if (s == null) return false;
            if (s.Status != "Completed") return false;
            if (s.IsManualEntry) return false;
            if (s.Category == "Assessment Proton" && s.TahunKe == "Tahun 3") return false;
            return true;
        }
    }
}
```

**Step 2 — Build:**
```bash
dotnet build
```
Verify 0 error. Field paths verified: `AssessmentSession.Category:16`, `Status:20`, `TahunKe:101`, `IsManualEntry:130`.

**Step 3 — Commit:**
```bash
git add Helpers/AssessmentEditEligibility.cs
git commit -m "feat(v17.0-p321): add AssessmentEditEligibility helper (IsEditable gating per CONTEXT CD-01)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "public static class AssessmentEditEligibility" Helpers/AssessmentEditEligibility.cs && grep -q "IsEditableAsync" Helpers/AssessmentEditEligibility.cs && grep -q "IsEditableShallow" Helpers/AssessmentEditEligibility.cs && grep -q "Assessment Proton" Helpers/AssessmentEditEligibility.cs && grep -q "Tahun 3" Helpers/AssessmentEditEligibility.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - `grep "public static class AssessmentEditEligibility" Helpers/AssessmentEditEligibility.cs` exits 0
    - Both `IsEditableAsync` (async) + `IsEditableShallow` (sync) defined
    - Proton T3 guard present: `grep -E "Assessment Proton.*Tahun 3" Helpers/AssessmentEditEligibility.cs` matches
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): add AssessmentEditEligibility helper`
  </acceptance_criteria>
  <done>Helper compile clean, sync + async variants tersedia, gating Proton T3 + IsManualEntry + Status!=Completed enforced, committed.</done>
</task>

<task type="auto">
  <name>Task 4: View Model DTOs (EditPesertaAnswersViewModel + EditAnswersSubmission)</name>
  <files>Models/EditPesertaAnswersViewModel.cs, Models/EditAnswersSubmission.cs</files>
  <read_first>
    - Models/AssessmentMonitoringViewModel.cs (verify namespace convention: `namespace HcPortal.Models` FLAT — NOT subdirectory ViewModels)
    - Models/AssessmentResultsViewModel.cs (verify sample existing structure)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 5 Step 1 (lines 619-657) — EditPesertaAnswersViewModel
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 8 Step 1 (lines 1163-1189) — EditAnswersSubmission + EditDraft DTOs
  </read_first>
  <action>
**CODEBASE CONVENTION OVERRIDE:** RESEARCH.md spec menulis path `Models/ViewModels/*.cs` namespace `HcPortal.Models.ViewModels`. **WAJIB IKUT codebase convention**: file di `Models/*.cs` namespace `HcPortal.Models` (verified `Models/AssessmentMonitoringViewModel.cs` + `Models/AssessmentResultsViewModel.cs`). Consumer di controller cukup `using HcPortal.Models;` (sudah ada).

**Step 1 — Create `Models/EditPesertaAnswersViewModel.cs`:**

```csharp
namespace HcPortal.Models
{
    public class EditPesertaAnswersViewModel
    {
        public AssessmentSession Session { get; set; } = null!;
        public string FullName { get; set; } = "";
        public string NIP { get; set; } = "";
        public DateTime UpdatedAt { get; set; }
        public List<EditQuestionRow> Questions { get; set; } = new();
    }

    public class EditQuestionRow
    {
        public int PackageQuestionId { get; set; }
        public string QuestionText { get; set; } = "";
        public string QuestionType { get; set; } = ""; // MultipleChoice / MultipleAnswer / Essay
        public List<EditOptionRow> Options { get; set; } = new();
        public List<int> SelectedOptionIds { get; set; } = new();
        public List<int> CorrectOptionIds { get; set; } = new();
        public bool IsCurrentCorrect { get; set; }
    }

    public class EditOptionRow
    {
        public int Id { get; set; }
        public string OptionText { get; set; } = "";
        public bool IsCorrect { get; set; }
    }
}
```

**Step 2 — Create `Models/EditAnswersSubmission.cs`:**

```csharp
namespace HcPortal.Models
{
    public class EditAnswersSubmission
    {
        public int SessionId { get; set; }
        public DateTime UpdatedAt { get; set; }
        public Dictionary<int, List<int>> Answers { get; set; } = new();   // qId -> optionIds
        public Dictionary<int, EditReason> Reasons { get; set; } = new();  // qId -> reason
    }

    public class EditReason
    {
        public string Code { get; set; } = "";
        public string? Text { get; set; }
    }

    // Dry-run DTOs (untuk PreviewEditScore endpoint Task 9 PLAN 03)
    public class EditDraft
    {
        public int QuestionId { get; set; }
        public List<int> Options { get; set; } = new();
    }

    public class EditDraftSubmission
    {
        public List<EditDraft> Drafts { get; set; } = new();
    }
}
```

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — Commit:**
```bash
git add Models/EditPesertaAnswersViewModel.cs Models/EditAnswersSubmission.cs
git commit -m "feat(v17.0-p321): add Edit ViewModels + DTOs (EditPesertaAnswersViewModel + EditAnswersSubmission)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "public class EditPesertaAnswersViewModel" Models/EditPesertaAnswersViewModel.cs && grep -q "public class EditAnswersSubmission" Models/EditAnswersSubmission.cs && grep -q "public class EditDraftSubmission" Models/EditAnswersSubmission.cs && grep -q "^namespace HcPortal.Models$" Models/EditPesertaAnswersViewModel.cs && grep -q "^namespace HcPortal.Models$" Models/EditAnswersSubmission.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - All 6 classes defined: EditPesertaAnswersViewModel, EditQuestionRow, EditOptionRow, EditAnswersSubmission, EditReason, EditDraft, EditDraftSubmission
    - Namespace = `HcPortal.Models` FLAT (verify via grep — NOT `HcPortal.Models.ViewModels`)
    - Files in `Models/` root (NOT `Models/ViewModels/` subdir)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): add Edit ViewModels + DTOs`
  </acceptance_criteria>
  <done>4 DTO files compile clean dengan namespace HcPortal.Models, siap di-consume PLAN 02 + PLAN 03, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → controller GET/POST | Admin/HC authenticated user → server endpoint (gated nanti di PLAN 03; foundation di sini) |
| controller → DB write (AssessmentEditLogs) | Server inserts audit row (append-only, never UPDATE/DELETE existing rows) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-321-01 | Tampering (audit integrity) | `Models/AssessmentEditLog.cs` + DbSet config | mitigate | Append-only design: tidak expose `DELETE` atau `UPDATE` operation via service/controller. Helper PLAN 03 hanya `Add()` baru, tidak pernah modify existing. `OnDelete(DeleteBehavior.Restrict)` di FK Session — block cascade delete dari session removal. Index `(AssessmentSessionId, EditedAt DESC)` immutable. |
| T-321-02 | Elevation of Privilege (eligibility bypass) | `Helpers/AssessmentEditEligibility.cs` | mitigate | Dual variant (async strict + sync shallow) — semua entry point (GET, POST, UI render) WAJIB invoke. Sync variant skip DB check tapi tetap enforce Status/IsManualEntry/ProtonT3. Consumer PLAN 03 controller MUST call before any state change. Threat verification: PLAN 03 verify grep call site di GET + POST action + view render. |

</threat_model>

<verification>
- `dotnet build` exits 0 (compile gate per task)
- Migration: `sqlcmd -Q "SELECT OBJECT_ID('AssessmentEditLogs')"` returns non-null (table created)
- Migration rollback test: drop + re-apply via `dotnet ef database update {prev} && dotnet ef database update` succeed
- Helper: dual variant exposed (async + sync)
- ViewModels: namespace `HcPortal.Models` flat (codebase convention)
- Branch: `git branch --show-current` returns `feature/phase-321-edit-jawaban`
- IT preemptif notify confirmed di Task 2 checkpoint
</verification>

<success_criteria>
- 4 commit di branch `feature/phase-321-edit-jawaban`:
  1. `feat(v17.0-p321): add AssessmentEditLog model + DbSet + fluent index`
  2. `feat(v17.0-p321): migration AddAssessmentEditLogs (apply + rollback verified lokal)`
  3. `feat(v17.0-p321): add AssessmentEditEligibility helper (IsEditable gating per CONTEXT CD-01)`
  4. `feat(v17.0-p321): add Edit ViewModels + DTOs (EditPesertaAnswersViewModel + EditAnswersSubmission)`
- Table `AssessmentEditLogs` + index `IX_AssessmentEditLogs_SessionId_EditedAt` exist di DB lokal
- Migration apply + rollback test lulus (DEV_WORKFLOW §4)
- `dotnet build` clean (0 error, 0 warning baru)
- IT preemptif sudah di-notify (D-08)
- Branch `feature/phase-321-edit-jawaban` aktif untuk PLAN 02-04
</success_criteria>

<output>
After completion, create `.planning/phases/321-assessment-edit-jawaban-peserta/321-01-SUMMARY.md` dengan:
- Daftar 4 commit hash + message
- Migration filename `{timestamp}_AddAssessmentEditLogs.cs`
- Verified field paths AssessmentSession (Status/IsManualEntry/Category/TahunKe)
- IT preemptif notify confirmation
- Handoff note ke PLAN 02: Helper `AssessmentEditEligibility` siap dipakai, ViewModels siap di-import `using HcPortal.Models;`, table AssessmentEditLogs siap di-insert dari PLAN 03 Task 8.
</output>
