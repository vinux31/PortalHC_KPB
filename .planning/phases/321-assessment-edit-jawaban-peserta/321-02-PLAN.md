---
phase: 321-assessment-edit-jawaban-peserta
plan: 02
type: execute
wave: 2
depends_on: [321-01]
files_modified:
  - Services/GradingService.cs
autonomous: true
requirements: [EDIT-03, EDIT-04]
user_setup: []
must_haves:
  truths:
    - "GradingService.ComputeScoreAndETInternalAsync extracted private method, no side effect, support overrideAnswers optional"
    - "GradingService.RegradeAfterEditAsync public method DELETE existing ET + recompute + ExecuteUpdateAsync status guard `WHERE Status == \"Completed\"`"
    - "GradingService.PreviewScoreAsync public wrapper untuk dry-run (PLAN 03 PreviewEditScore consumer)"
    - "Pass→Fail cascade: cabut NomorSertifikat + ValidUntil + TrainingRecord.Status = \"Failed\""
    - "Fail→Pass cascade: generate NomorSertifikat baru via CertNumberHelper.Build(seq, date) retry 3x via IsDuplicateKeyException + TrainingRecord upsert Status = \"Passed\" (hanya kalau GenerateCertificate && AssessmentType != \"PreTest\")"
    - "Initial grading method GradeAndCompleteAsync UNTOUCHED (zero regression risk pada flow existing)"
  artifacts:
    - path: "Services/GradingService.cs"
      provides: "ComputeScoreAndETInternalAsync (private) + RegradeAfterEditAsync (public) + PreviewScoreAsync (public)"
      contains: "RegradeAfterEditAsync"
  key_links:
    - from: "Services/GradingService.RegradeAfterEditAsync"
      to: "Helpers/CertNumberHelper (Build + GetNextSeqAsync + IsDuplicateKeyException)"
      via: "static helper call dengan correct signature Build(int seq, DateTime date)"
      pattern: "CertNumberHelper\\.Build"
    - from: "Services/GradingService.RegradeAfterEditAsync"
      to: "AssessmentSessions table (ExecuteUpdateAsync status guard)"
      via: "EF Core 8 ExecuteUpdateAsync WHERE Status == \"Completed\""
      pattern: "ExecuteUpdateAsync"
    - from: "Services/GradingService.PreviewScoreAsync"
      to: "PLAN 03 Task 9 PreviewEditScore controller action"
      via: "public method consumed via _gradingService field"
      pattern: "_gradingService.PreviewScoreAsync"
---

<objective>
PLAN 02 (Wave 2) — Service layer refactor: extract pure compute + add re-grade + cascade.

Purpose: Bangun service method `RegradeAfterEditAsync` yang di-call PLAN 03 controller POST `SubmitEditAnswers`. Pisah pure compute (no-side-effect) supaya bisa dipakai dry-run PreviewEditScore tanpa mutate DB. Cascade Pass↔Fail (sertifikat + TrainingRecord) atomic dalam method.

Output:
- `ComputeScoreAndETInternalAsync(session, overrideAnswers?)` — pure compute (private)
- `RegradeAfterEditAsync(session)` — recompute + persist + cascade (public)
- `PreviewScoreAsync(session, overrideAnswers)` — public dry-run wrapper

Dependency: PLAN 01 wajib selesai (DbSet AssessmentEditLogs + helper IsEditable + ViewModels — tapi PLAN 02 hanya konsume `_context` existing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-01-SUMMARY.md
@Services/GradingService.cs
@Helpers/CertNumberHelper.cs
@Models/AssessmentSession.cs
@Models/TrainingRecord.cs
@CLAUDE.md

<interfaces>
<!-- Verified codebase signatures 2026-05-21 -->

From Helpers/CertNumberHelper.cs (verified by RESEARCH refresh):
- `static string Build(int seq, DateTime date)` — line ~20 — returns `"KPB/{seq:D3}/{RomanMonth}/{year}"`
- `static Task<int> GetNextSeqAsync(ApplicationDbContext db, int year)` — line ~23
- `static bool IsDuplicateKeyException(DbUpdateException ex)` — line ~37 (true kalau IX_AssessmentSessions_NomorSertifikat hit OR SQL Server 2601/2627)

**KOREKSI** — RESEARCH lama menulis `Format(year, seq)` — NOT EXIST. Pakai `Build(seq, date)`.

From Services/GradingService.cs (existing structure):
- Line ~326 — end of `GradeAndCompleteAsync` (initial grading) — INSERT new methods AFTER this
- 329-line file pre-refactor — total file size after add ~530 lines
- DI fields existing: `_context` (ApplicationDbContext), `_logger` (ILogger<GradingService>)
- Cascade pattern reference: `GradeAndCompleteAsync` line 268-285 (TrainingRecord upsert pattern, CD-02)
- Cert retry pattern reference: `GradeAndCompleteAsync` line 287-321 (retry 3x)

From Models/AssessmentSession.cs:
- `int? Score`, `bool? IsPassed`, `string? NomorSertifikat`, `DateTime? ValidUntil`
- `bool GenerateCertificate`, `string AssessmentType` (untuk eligibility cert generate)
- `string Title`, `string Category`, `DateTime Schedule`, `string UserId` (untuk TrainingRecord lookup)
- `int PassPercentage`, `DateTime? UpdatedAt`

From Models/TrainingRecord.cs:
- Fields: `UserId`, `Judul`, `Kategori`, `Tanggal`, `TanggalSelesai`, `Penyelenggara`, `Status`
- Status values: "Passed" / "Failed" (string enum convention)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor GradingService — Extract ComputeScoreAndETInternalAsync (pure compute, no side effect)</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs full (focus line 1-329, especially `GradeAndCompleteAsync` body)
    - Models/AssessmentSession.cs (PassPercentage, Schedule, Title, Category, UserId fields)
    - Models/SessionElemenTeknisScore.cs (ElemenTeknis, CorrectCount, QuestionCount, AssessmentSessionId fields)
    - Models/PackageQuestion.cs + PackageOption.cs (QuestionType, ScoreValue, ElemenTeknis, Options.IsCorrect)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 3 (lines 290-426)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md (compute pattern analog)
  </read_first>
  <action>
**Step 1 — Locate insertion point:**
```bash
grep -n "GradeAndCompleteAsync" Services/GradingService.cs
```
Find closing brace of `GradeAndCompleteAsync` method (~line 326). New methods INSERT AFTER this brace, BEFORE class closing brace.

**KEEP `GradeAndCompleteAsync` UNTOUCHED** — zero regression risk pada initial grading flow. Hanya TAMBAH method baru.

**Step 2 — Insert private method `ComputeScoreAndETInternalAsync`** setelah `GradeAndCompleteAsync`:

```csharp
/// <summary>
/// Pure compute: hitung total/max score + IsPassed + ElemenTeknis breakdown TANPA side effect (tidak insert DB).
/// Dipakai oleh RegradeAfterEditAsync (re-grade post-edit) + PreviewScoreAsync (dry-run).
/// `GradeAndCompleteAsync` initial grading KEEP inline logic existing (TIDAK refactor) supaya regression risk = 0.
/// </summary>
/// <param name="session">Session target.</param>
/// <param name="overrideAnswers">
/// Optional. Dict (PackageQuestionId -> List of selected PackageOption.Id).
/// Null -> baca semua dari PackageUserResponses normal.
/// Non-null -> pakai override untuk question yang ada di dict, fallback DB untuk sisanya. Path PreviewEditScore.
/// </param>
private async Task<(int totalScore, int maxScore, bool isPassed, List<SessionElemenTeknisScore> etScores)>
    ComputeScoreAndETInternalAsync(AssessmentSession session, IDictionary<int, List<int>>? overrideAnswers = null)
{
    var packageAssignment = await _context.UserPackageAssignments
        .FirstOrDefaultAsync(a => a.AssessmentSessionId == session.Id);
    if (packageAssignment == null)
        return (0, 0, false, new List<SessionElemenTeknisScore>());

    var shuffledIds = packageAssignment.GetShuffledQuestionIds();
    var packageQuestions = await _context.PackageQuestions
        .Include(q => q.Options)
        .Where(q => shuffledIds.Contains(q.Id))
        .ToListAsync();
    var questionLookup = packageQuestions.ToDictionary(q => q.Id);

    var dbResponses = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == session.Id)
        .ToListAsync();

    // Merge: build effective answers per question
    HashSet<int> SelectedOptions(int qId)
    {
        if (overrideAnswers != null && overrideAnswers.TryGetValue(qId, out var ov))
            return ov.ToHashSet();
        return dbResponses
            .Where(r => r.PackageQuestionId == qId && r.PackageOptionId.HasValue)
            .Select(r => r.PackageOptionId!.Value)
            .ToHashSet();
    }

    int totalScore = 0, maxScore = 0;
    foreach (var qId in shuffledIds)
    {
        if (!questionLookup.TryGetValue(qId, out var q)) continue;
        maxScore += q.ScoreValue;

        switch (q.QuestionType ?? "MultipleChoice")
        {
            case "MultipleChoice":
                var mcSel = SelectedOptions(qId);
                if (mcSel.Count > 0)
                {
                    var optId = mcSel.First();
                    var opt = q.Options.FirstOrDefault(o => o.Id == optId);
                    if (opt?.IsCorrect == true) totalScore += q.ScoreValue;
                }
                break;
            case "MultipleAnswer":
                var maSel = SelectedOptions(qId);
                var maCorrect = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
                if (maSel.SetEquals(maCorrect)) totalScore += q.ScoreValue;
                break;
            case "Essay":
                break;
        }
    }

    int pct = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;
    bool isPassed = pct >= session.PassPercentage;

    var etScores = new List<SessionElemenTeknisScore>();
    var etGroups = packageQuestions
        .GroupBy(q => string.IsNullOrWhiteSpace(q.ElemenTeknis) ? "Lainnya" : q.ElemenTeknis);

    foreach (var etGroup in etGroups)
    {
        int etCorrect = 0;
        int etTotal = etGroup.Count();
        foreach (var q in etGroup)
        {
            switch (q.QuestionType ?? "MultipleChoice")
            {
                case "MultipleChoice":
                    var mcSel = SelectedOptions(q.Id);
                    if (mcSel.Count > 0)
                    {
                        var opt = q.Options.FirstOrDefault(o => o.Id == mcSel.First());
                        if (opt?.IsCorrect == true) etCorrect++;
                    }
                    break;
                case "MultipleAnswer":
                    var maSel = SelectedOptions(q.Id);
                    var maCorrect = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
                    if (maSel.SetEquals(maCorrect)) etCorrect++;
                    break;
                case "Essay":
                    break;
            }
        }
        etScores.Add(new SessionElemenTeknisScore
        {
            AssessmentSessionId = session.Id,
            ElemenTeknis = etGroup.Key,
            CorrectCount = etCorrect,
            QuestionCount = etTotal
        });
    }

    return (totalScore, maxScore, isPassed, etScores);
}
```

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — Smoke test no regression initial grading:**
```bash
dotnet run
```
Worker complete MC assessment normal. Verify session Completed dengan score + ET breakdown sama seperti sebelum refactor (karena `GradeAndCompleteAsync` BELUM diubah).

**Step 5 — Commit:**
```bash
git add Services/GradingService.cs
git commit -m "feat(v17.0-p321): add ComputeScoreAndETInternalAsync (pure compute, overrideAnswers? param)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "private async Task.*ComputeScoreAndETInternalAsync" Services/GradingService.cs && grep -q "IDictionary<int, List<int>>? overrideAnswers" Services/GradingService.cs && grep -q "SelectedOptions" Services/GradingService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - `grep "private async Task.*ComputeScoreAndETInternalAsync" Services/GradingService.cs` exits 0 (private method, async, return tuple)
    - `grep "IDictionary<int, List<int>>? overrideAnswers = null" Services/GradingService.cs` exits 0 (optional param)
    - Method NOT mutating DB — no `SaveChangesAsync` inside compute path
    - `GradeAndCompleteAsync` body UNCHANGED — diff hanya menambah method baru, tidak ubah existing (verify via `git diff HEAD~1 Services/GradingService.cs` — only additions, no deletions in old block)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): add ComputeScoreAndETInternalAsync`
  </acceptance_criteria>
  <done>Pure compute method exists, no side effect, optional override answers support, no regression initial grading, committed.</done>
</task>

<task type="auto">
  <name>Task 2: GradingService.RegradeAfterEditAsync + PreviewScoreAsync (recompute + cascade cert/TR)</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs (Task 1 just added ComputeScoreAndETInternalAsync — insertion point setelah method baru)
    - Helpers/CertNumberHelper.cs (VERIFY signature Build(int seq, DateTime date) + GetNextSeqAsync + IsDuplicateKeyException — NOT Format(year, seq))
    - Models/AssessmentSession.cs (NomorSertifikat, ValidUntil, GenerateCertificate, AssessmentType, UserId, Title, Schedule, Category)
    - Models/TrainingRecord.cs (UserId, Judul, Kategori, Tanggal, TanggalSelesai, Penyelenggara, Status fields)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 4 (lines 430-607) — full method body
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md CD-02 (TrainingRecord Fail→Pass upsert behavior)
  </read_first>
  <action>
**Step 1 — Verify CertNumberHelper signature (CRITICAL — RESEARCH refresh correction):**
```bash
grep -nE "public static (string|Task|bool).*Build|GetNextSeqAsync|IsDuplicateKeyException" Helpers/CertNumberHelper.cs
```
Expected output lines confirm:
- `public static string Build(int seq, DateTime date)`
- `public static async Task<int> GetNextSeqAsync(ApplicationDbContext db, int year)`
- `public static bool IsDuplicateKeyException(DbUpdateException ex)`

**Step 2 — Insert `RegradeAfterEditAsync` + `PreviewScoreAsync`** SETELAH `ComputeScoreAndETInternalAsync` (yang barusan ditambah Task 1):

```csharp
/// <summary>
/// Re-grade session yang sudah Completed setelah edit jawaban oleh Admin/HC.
/// DELETE existing SessionElemenTeknisScores -> recompute -> update session + cascade sertifikat/TR.
/// CALLER bertanggung jawab: open transaction sebelum invoke, commit setelahnya.
/// </summary>
/// <returns>(newScore, newIsPassed, oldScore, oldIsPassed)</returns>
public async Task<(int newScore, bool newIsPassed, int? oldScore, bool? oldIsPassed)>
    RegradeAfterEditAsync(AssessmentSession session)
{
    int? oldScore = session.Score;
    bool? oldIsPassed = session.IsPassed;

    // 1. DELETE existing ET scores
    await _context.SessionElemenTeknisScores
        .Where(et => et.AssessmentSessionId == session.Id)
        .ExecuteDeleteAsync();

    // 2. Recompute (overrideAnswers = null -> baca DB which is already updated)
    var (totalScore, maxScore, isPassed, etScores) = await ComputeScoreAndETInternalAsync(session);
    int newPct = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;

    // 3. Insert new ET scores
    _context.SessionElemenTeknisScores.AddRange(etScores);

    // 4. Update session — status guard WHERE Status == "Completed"
    var rowsAffected = await _context.AssessmentSessions
        .Where(s => s.Id == session.Id && s.Status == "Completed")
        .ExecuteUpdateAsync(s => s
            .SetProperty(r => r.Score, newPct)
            .SetProperty(r => r.IsPassed, isPassed)
            .SetProperty(r => r.UpdatedAt, DateTime.UtcNow)
        );

    if (rowsAffected == 0)
    {
        _logger.LogWarning(
            "GradingService.RegradeAfterEditAsync: session {SessionId} bukan Completed (race).",
            session.Id);
        throw new InvalidOperationException("Session bukan dalam status Completed saat re-grade.");
    }

    await _context.SaveChangesAsync();

    // 5. Cascade sertifikat + TrainingRecord (only when flip)
    bool wasPassed = oldIsPassed ?? false;
    if (wasPassed && !isPassed)
    {
        // Pass -> Fail
        await _context.AssessmentSessions
            .Where(s => s.Id == session.Id)
            .ExecuteUpdateAsync(s => s
                .SetProperty(r => r.NomorSertifikat, (string?)null)
                .SetProperty(r => r.ValidUntil, (DateTime?)null));

        var judul = $"Assessment: {session.Title}";
        await _context.TrainingRecords
            .Where(t => t.UserId == session.UserId && t.Judul == judul && t.Tanggal == session.Schedule)
            .ExecuteUpdateAsync(t => t.SetProperty(r => r.Status, "Failed"));

        _logger.LogInformation(
            "RegradeAfterEditAsync: session {SessionId} flip Pass->Fail — cert dicabut, TR=Failed.",
            session.Id);
    }
    else if (!wasPassed && isPassed)
    {
        // Fail -> Pass
        if (session.GenerateCertificate && session.AssessmentType != "PreTest")
        {
            // Generate NomorSertifikat (retry 3x — reuse pattern GradeAndCompleteAsync line 287-321)
            // CORRECTED SIGNATURE: CertNumberHelper.Build(int seq, DateTime date) — NOT Format(year, seq)
            var certNow = DateTime.Now;
            int certYear = certNow.Year;
            int certAttempts = 0;
            const int maxCertAttempts = 3;
            bool certSaved = false;
            while (!certSaved && certAttempts < maxCertAttempts)
            {
                certAttempts++;
                try
                {
                    var nextSeq = await HcPortal.Helpers.CertNumberHelper.GetNextSeqAsync(_context, certYear);
                    var nomor = HcPortal.Helpers.CertNumberHelper.Build(nextSeq, certNow);
                    var validUntil = certNow.AddYears(3);
                    var updated = await _context.AssessmentSessions
                        .Where(s => s.Id == session.Id && s.NomorSertifikat == null)
                        .ExecuteUpdateAsync(s => s
                            .SetProperty(r => r.NomorSertifikat, nomor)
                            .SetProperty(r => r.ValidUntil, validUntil));
                    if (updated > 0) certSaved = true;
                }
                catch (DbUpdateException ex) when (certAttempts < maxCertAttempts && HcPortal.Helpers.CertNumberHelper.IsDuplicateKeyException(ex))
                {
                    // Retry dengan sequence baru
                }
            }

            if (!certSaved)
            {
                _logger.LogError("RegradeAfterEditAsync: failed generate cert for session {SessionId} after {N} attempts",
                    session.Id, maxCertAttempts);
            }

            // Upsert TrainingRecord (CD-02: insert kalau missing, update kalau exist)
            var judul = $"Assessment: {session.Title}";
            var existingTr = await _context.TrainingRecords
                .FirstOrDefaultAsync(t => t.UserId == session.UserId && t.Judul == judul && t.Tanggal == session.Schedule);
            if (existingTr == null)
            {
                _context.TrainingRecords.Add(new TrainingRecord
                {
                    UserId = session.UserId,
                    Judul = judul,
                    Kategori = session.Category ?? "Assessment",
                    Tanggal = session.Schedule,
                    TanggalSelesai = DateTime.UtcNow,
                    Penyelenggara = "Internal",
                    Status = "Passed"
                });
                await _context.SaveChangesAsync();
            }
            else
            {
                existingTr.Status = "Passed";
                await _context.SaveChangesAsync();
            }
        }

        _logger.LogInformation(
            "RegradeAfterEditAsync: session {SessionId} flip Fail->Pass — cert generated (if applicable), TR=Passed.",
            session.Id);
    }
    // Pass->Pass, Fail->Fail: no cascade

    return (newPct, isPassed, oldScore, oldIsPassed);
}

/// <summary>
/// Public wrapper for PreviewEditScore (dry-run) — exposes ComputeScoreAndETInternalAsync without ET breakdown.
/// Consumed by PLAN 03 Task 9 PreviewEditScore controller action.
/// </summary>
public async Task<(int newScore, bool newIsPassed)> PreviewScoreAsync(
    AssessmentSession session,
    IDictionary<int, List<int>> overrideAnswers)
{
    var (totalScore, maxScore, isPassed, _) = await ComputeScoreAndETInternalAsync(session, overrideAnswers);
    int pct = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;
    return (pct, isPassed);
}
```

**Step 3 — Build:**
```bash
dotnet build
```
Verify 0 error. **JIKA** error pada `CertNumberHelper.Build` signature, kembali ke Step 1 — signature mismatch perlu re-verify file source.

**Step 4 — Commit:**
```bash
git add Services/GradingService.cs
git commit -m "feat(v17.0-p321): add GradingService.RegradeAfterEditAsync + PreviewScoreAsync (recompute + cascade cert/TR on flip)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "public async Task.*RegradeAfterEditAsync" Services/GradingService.cs && grep -q "public async Task.*PreviewScoreAsync" Services/GradingService.cs && grep -q "ExecuteUpdateAsync" Services/GradingService.cs && grep -q "CertNumberHelper.Build" Services/GradingService.cs && grep -q "IsDuplicateKeyException" Services/GradingService.cs && grep -q "Status == \"Completed\"" Services/GradingService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - `grep "public async Task.*RegradeAfterEditAsync" Services/GradingService.cs` exits 0
    - `grep "public async Task.*PreviewScoreAsync" Services/GradingService.cs` exits 0
    - ExecuteUpdateAsync status guard present: `grep "Status == \"Completed\"" Services/GradingService.cs` exits 0 (T-321-03 mitigation)
    - Cert helper correct signature: `grep "CertNumberHelper.Build" Services/GradingService.cs` exits 0 (NOT `Format(year, seq)`)
    - Retry guard via IsDuplicateKeyException: `grep "IsDuplicateKeyException" Services/GradingService.cs` exits 0
    - Pass→Fail cascade: cabut NomorSertifikat (string?)null + ValidUntil + TR.Status="Failed"
    - Fail→Pass cascade: cert generate via Build + TR upsert "Passed" (eligible by GenerateCertificate && AssessmentType != "PreTest")
    - PreviewScoreAsync TIDAK mutate DB (no `SaveChangesAsync` di body) — pure dry-run
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): add GradingService.RegradeAfterEditAsync + PreviewScoreAsync`
  </acceptance_criteria>
  <done>Service method recompute + cascade Pass↔Fail tersedia, status guard race-safe, cert retry 3x correct signature, no DB mutation di PreviewScoreAsync, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller PLAN 03 → service `RegradeAfterEditAsync` | Server-internal call; caller responsible untuk open transaction + commit |
| service → AssessmentSessions table (cascade UPDATE) | Server modifies score, IsPassed, NomorSertifikat, ValidUntil — must be race-safe |
| service → TrainingRecords table (cascade upsert) | Server inserts/updates TR status — must match initial grading pattern (CD-02) |
| service → external CertNumberHelper retry loop | Generates unique cert number — must guard duplicate key via IsDuplicateKeyException |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-321-03 | Tampering (recompute integrity) — race with concurrent grading | `RegradeAfterEditAsync` ExecuteUpdateAsync on AssessmentSessions | mitigate | ExecuteUpdateAsync `WHERE Id == sessionId && Status == "Completed"` — kalau status berubah (mis. worker re-submit while admin edit), `rowsAffected == 0` → throw `InvalidOperationException`. Controller PLAN 03 wrap transaction → rollback semua edit. Verifikasi grep `grep "Status == \"Completed\"" Services/GradingService.cs` exits 0. |
| T-321-03b | Tampering (cert duplicate key race) | Cert generate retry loop dalam RegradeAfterEditAsync | mitigate | Retry 3x via `CertNumberHelper.IsDuplicateKeyException(ex)` guard — kalau IX_AssessmentSessions_NomorSertifikat hit, generate seq baru. Maks 3 attempt, kalau gagal log error tanpa throw (cert kosong = degraded mode, bukan failure path). |
| T-321-03c | Information Disclosure (PreviewScoreAsync data leak) | `PreviewScoreAsync` public wrapper | mitigate | Method TIDAK insert/update DB (verified — no `SaveChangesAsync` call dalam body). Caller PLAN 03 PreviewEditScore endpoint enforce `[Authorize(Roles="Admin, HC")]` + IsEditable check. Tidak expose internal ET breakdown ke caller (return shape: `(int newScore, bool newIsPassed)` only). |

</threat_model>

<verification>
- `dotnet build` exits 0
- 3 method baru exist:
  - `private async Task ComputeScoreAndETInternalAsync(...)`
  - `public async Task RegradeAfterEditAsync(...)`
  - `public async Task PreviewScoreAsync(...)`
- CertNumberHelper signature correct (Build NOT Format)
- ExecuteUpdateAsync status guard `WHERE Status == "Completed"` present
- `GradeAndCompleteAsync` body unchanged (zero regression)
- Smoke test: worker complete MC assessment normal → score + ET sama seperti sebelum refactor
</verification>

<success_criteria>
- 2 commit di branch `feature/phase-321-edit-jawaban`:
  1. `feat(v17.0-p321): add ComputeScoreAndETInternalAsync (pure compute, overrideAnswers? param)`
  2. `feat(v17.0-p321): add GradingService.RegradeAfterEditAsync + PreviewScoreAsync (recompute + cascade cert/TR on flip)`
- `dotnet build` clean
- Method signature siap di-consume PLAN 03:
  - Controller POST SubmitEditAnswers → `_gradingService.RegradeAfterEditAsync(session)`
  - Controller POST PreviewEditScore → `_gradingService.PreviewScoreAsync(session, overrideAnswers)`
- Initial grading flow tidak regression (smoke test verified)
</success_criteria>

<output>
After completion, create `.planning/phases/321-assessment-edit-jawaban-peserta/321-02-SUMMARY.md` dengan:
- 2 commit hash + message
- Method signatures finalized (RegradeAfterEditAsync return tuple shape, PreviewScoreAsync return shape)
- CertNumberHelper signature confirmation (Build NOT Format)
- Smoke test no-regression confirmation
- Handoff note ke PLAN 03: `_gradingService` field controller existing (line ~29) — langsung pakai, tidak perlu [FromServices] injection.
</output>
