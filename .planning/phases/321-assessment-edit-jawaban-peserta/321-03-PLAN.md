---
phase: 321-assessment-edit-jawaban-peserta
plan: 03
type: execute
wave: 3
depends_on: [321-01, 321-02]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/EditPesertaAnswers.cshtml
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - wwwroot/js/edit-peserta-answers.js
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-05, EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-10, EDIT-12]
user_setup: []
must_haves:
  truths:
    - "Admin/HC bisa akses GET /Admin/EditPesertaAnswers/{id} untuk session Completed eligible — Worker dapat 403/redirect login"
    - "View edit page render form per soal MC/MA dengan jawaban current pre-selected + kunci ditandai + reason dropdown verbose (D-05) per question card"
    - "JS dirty state detect ubah jawaban → reveal reason block + asterisk; Lainnya → reveal textarea; client validate reason required; POST PreviewEditScore AJAX → kalau flip Pass↔Fail tampil modal D-06 eksplisit copy → user confirm → form.submit() real"
    - "POST SubmitEditAnswers wrap transaction: validate eligibility + UpdatedAt concurrency check (±1 detik tolerance) + reason server validation + delete-insert PackageUserResponses + insert AssessmentEditLog per question + call _gradingService.RegradeAfterEditAsync + backfill NewScore/NewIsPassed di log + insert AuditLog + commit + cache invalidate + SignalR broadcast workerAnswerEdited ke monitor-{batchKey} → rollback total kalau exception"
    - "POST PreviewEditScore return JSON {oldScore, oldIsPassed, newScore, newIsPassed, hasCert, nomorSertifikat, willGenerateCert} TANPA mutate DB"
    - "Dropdown ⋮ hybrid di AssessmentMonitoringDetail per-row: View Results + 🕐 inline; Edit Jawaban (conditional IsEditableShallow) + Reset + Akhiri Ujian + Reshuffle di dropdown dengan bi bi-* icons + ARIA + dropdown-menu-end + 40px touch target"
    - "SignalR frontend handler workerAnswerEdited update row score + result cell tanpa refresh + reuse window.showAssessmentToast dengan template D-07 verbose (default 5s timeout per existing assessment-hub.js — bukan override 8s)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "GET EditPesertaAnswers + POST SubmitEditAnswers + POST PreviewEditScore actions"
      contains: "EditPesertaAnswers"
    - path: "Views/Admin/EditPesertaAnswers.cshtml"
      provides: "Edit form view dengan question cards + reason dropdown verbose + flip modal markup"
      contains: "@model HcPortal.Models.EditPesertaAnswersViewModel"
    - path: "wwwroot/js/edit-peserta-answers.js"
      provides: "Frontend dirty state + reason validation + flip modal AJAX preview"
      contains: "editAnswersForm"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Dropdown ⋮ hybrid per-row action column + SignalR workerAnswerEdited handler"
      contains: "workerAnswerEdited"
  key_links:
    - from: "Views/Admin/EditPesertaAnswers.cshtml form submit"
      to: "wwwroot/js/edit-peserta-answers.js"
      via: "form id=editAnswersForm + data-preview-url attr"
      pattern: "editAnswersForm"
    - from: "wwwroot/js/edit-peserta-answers.js preview AJAX"
      to: "POST /Admin/PreviewEditScore"
      via: "fetch with FormData + RequestVerificationToken header"
      pattern: "PreviewEditScore"
    - from: "Controllers/AssessmentAdminController POST SubmitEditAnswers"
      to: "_gradingService.RegradeAfterEditAsync (PLAN 02)"
      via: "injected field _gradingService (ctor line ~29)"
      pattern: "_gradingService.RegradeAfterEditAsync"
    - from: "Controllers/AssessmentAdminController POST SubmitEditAnswers"
      to: "SignalR _hubContext.Clients.Group(monitor-{batchKey})"
      via: "injected field _hubContext (ctor line ~27)"
      pattern: "workerAnswerEdited"
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml dropdown item Edit Jawaban"
      to: "/Admin/EditPesertaAnswers/{id}"
      via: "asp-action + IsEditableShallow conditional render"
      pattern: "asp-action=\"EditPesertaAnswers\""
---

<objective>
PLAN 03 (Wave 3) — Controller + View + Frontend + Dropdown hybrid + SignalR handler.

Purpose: Bangun layer presentasi + endpoint utama Phase 321: halaman edit jawaban dedicated, dropdown ⋮ di monitoring, dan live update via SignalR. Sequential strict — depends_on PLAN 01 (helper + ViewModels) + PLAN 02 (RegradeAfterEditAsync + PreviewScoreAsync).

Output:
- 3 controller actions baru: GET EditPesertaAnswers, POST SubmitEditAnswers (transaction), POST PreviewEditScore (dry-run)
- 1 view baru: Views/Admin/EditPesertaAnswers.cshtml + flip modal markup
- 1 JS file baru: wwwroot/js/edit-peserta-answers.js (dirty state + flip modal AJAX)
- AssessmentMonitoringDetail.cshtml modify: dropdown ⋮ hybrid per-row + SignalR workerAnswerEdited handler

Open question RESOLVED: Toast timeout default 5s (consistency dengan existing `assessment-hub.js`) — UI-SPEC D-07 spec 8s di-override demi konsistensi cross-event. Rationale documented di Task 7 below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-01-SUMMARY.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-02-SUMMARY.md
@Controllers/AssessmentAdminController.cs
@Views/Admin/AssessmentMonitoringDetail.cshtml
@wwwroot/js/assessment-hub.js
@CLAUDE.md

<interfaces>
<!-- Verified codebase signatures 2026-05-21 (RESEARCH refresh corrections) -->

From Controllers/AssessmentAdminController.cs (verified):
- `[Route("Admin/[action]")]` line 19 → URL = `/Admin/{ActionName}/...`
- `protected new ViewResult View(...)` override line 53-57 → resolve ke `~/Views/Admin/{Action}.cshtml`
- DI fields (ctor line 22-49 — TIDAK perlu [FromServices]):
  - `_context` (ApplicationDbContext) line 21
  - `_cache` (IMemoryCache) line 22
  - `_logger` (ILogger<AssessmentAdminController>) line 25
  - `_hubContext` (IHubContext<AssessmentHub>) line 27 ← SignalR broadcast
  - `_gradingService` (GradingService) line 29 ← PLAN 02 service
- `ExportAssessmentResults` action line 3651 (Phase 320, unchanged) — context untuk locate insertion
- `AssessmentMonitoringDetail` action line 2684 — group new edit actions near related logic

From Views/Admin/AssessmentMonitoringDetail.cshtml (verified):
- Per-user table action column **line 286-338** (Reshuffle 290-296, AkhiriUjian 301-308, Reset 313-320, ViewResults 325-328, ActivityLog 331-337)
- Row field: `session.UserStatus` (NOT `session.Status`) — line 288, 299, 311, 323
- Status string: `"Not started"` (lowercase 's', single space) — line 288
- Modal Activity Log INLINE line 540-559 (PLAN 04 modifies this for Edit History tab)
- SignalR handler block: `window.assessmentHub.on('workerSubmitted', ...)` line ~1244 ← INSERT workerAnswerEdited setelah ini
- Class names existing: `.result-cell` line ~282, `.completedat-cell` line ~284 — `.score-cell` PERLU verify atau tambah

From wwwroot/js/assessment-hub.js (verified):
- `window.assessmentHub` set line 95 (SignalR connection variable)
- `window.showAssessmentToast(message, linkUrl?, linkText?)` line 96 — REUSE (D-07 + UI-SPEC §Toast)
- Toast auto-dismiss timeout: 5 detik fade existing pattern line ~32 (per RESEARCH Task 11 open question — DEFAULT 5s untuk konsistensi cross-event)
- **KOREKSI**: `showToast(msg, "info")` / `window.toastr` TIDAK EXIST — RESEARCH lama salah.

From AssessmentHub (verified):
- Group naming: `monitor-{batchKey}` line 57 — `batchKey = "{Title}|{Category}|{Schedule.Date:yyyy-MM-dd}"`

From PLAN 01 + 02 outputs:
- ViewModels: `HcPortal.Models.EditPesertaAnswersViewModel`, `EditQuestionRow`, `EditOptionRow`
- DTOs: `HcPortal.Models.EditAnswersSubmission`, `EditReason`, `EditDraft`, `EditDraftSubmission` (namespace HcPortal.Models flat)
- Helper: `HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session)` + `IsEditableShallow(session)`
- Service: `_gradingService.RegradeAfterEditAsync(session)` returns `(int newScore, bool newIsPassed, int? oldScore, bool? oldIsPassed)`
- Service: `_gradingService.PreviewScoreAsync(session, overrideAnswers)` returns `(int newScore, bool newIsPassed)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller GET EditPesertaAnswers (Admin/HC gate + IsEditable check + page render)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (ctor line 21-49, View override line 53-57, ExportAssessmentResults insertion neighborhood line 3651, AssessmentMonitoringDetail line 2684)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 5 (lines 611-753)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md (controller AJAX pattern + dual-response if applicable)
    - Models/UserPackageAssignment.cs (verify GetShuffledQuestionIds method exists)
  </read_first>
  <action>
**Step 1 — Locate insertion point in AssessmentAdminController.cs:**
```bash
grep -n "public.*ExportAssessmentResults\|public.*AssessmentMonitoringDetail" Controllers/AssessmentAdminController.cs
```
Insert new 3 actions (Task 1 GET + Task 4 POST + Task 5 PreviewEditScore) sebagai 1 contiguous block dekat `AssessmentMonitoringDetail` (~line 2684) — logical grouping monitoring + edit.

**Step 2 — Add GET action:**

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> EditPesertaAnswers(int id)
{
    var session = await _context.AssessmentSessions
        .Include(s => s.User)
        .FirstOrDefaultAsync(s => s.Id == id);
    if (session == null)
    {
        TempData["Error"] = "Sesi tidak ditemukan.";
        return RedirectToAction("ManageAssessment");
    }

    if (!await HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session))
    {
        TempData["Error"] = "Sesi ini tidak dapat diedit (status bukan Completed, atau IsManualEntry, atau Assessment Proton Tahun 3).";
        return RedirectToAction("AssessmentMonitoringDetail", new {
            title = session.Title, category = session.Category, scheduleDate = session.Schedule
        });
    }

    var assignment = await _context.UserPackageAssignments
        .FirstOrDefaultAsync(a => a.AssessmentSessionId == id);
    if (assignment == null) return NotFound();

    var shuffledIds = assignment.GetShuffledQuestionIds();
    var questions = await _context.PackageQuestions
        .Include(q => q.Options)
        .Where(q => shuffledIds.Contains(q.Id))
        .ToListAsync();
    var responses = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == id)
        .ToListAsync();

    var rows = new List<HcPortal.Models.EditQuestionRow>();
    foreach (var qId in shuffledIds)
    {
        var q = questions.FirstOrDefault(x => x.Id == qId);
        if (q == null) continue;
        var selectedIds = responses
            .Where(r => r.PackageQuestionId == qId && r.PackageOptionId.HasValue)
            .Select(r => r.PackageOptionId!.Value)
            .ToList();
        var correctIds = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToList();
        bool isCorrect = (q.QuestionType ?? "MultipleChoice") switch
        {
            "MultipleChoice" => selectedIds.Count == 1 && correctIds.Contains(selectedIds[0]),
            "MultipleAnswer" => selectedIds.ToHashSet().SetEquals(correctIds.ToHashSet()),
            _ => false
        };
        rows.Add(new HcPortal.Models.EditQuestionRow
        {
            PackageQuestionId = q.Id,
            QuestionText = q.QuestionText,
            QuestionType = q.QuestionType ?? "MultipleChoice",
            Options = q.Options.Select(o => new HcPortal.Models.EditOptionRow
            {
                Id = o.Id, OptionText = o.OptionText, IsCorrect = o.IsCorrect
            }).ToList(),
            SelectedOptionIds = selectedIds,
            CorrectOptionIds = correctIds,
            IsCurrentCorrect = isCorrect
        });
    }

    var vm = new HcPortal.Models.EditPesertaAnswersViewModel
    {
        Session = session,
        FullName = session.User?.FullName ?? "Unknown",
        NIP = session.User?.NIP ?? "—",
        UpdatedAt = session.UpdatedAt ?? session.CreatedAt,
        Questions = rows
    };
    return View(vm);   // Controller override line 54 resolve ke ~/Views/Admin/EditPesertaAnswers.cshtml
}
```

**Resulting URL** via `[Route("Admin/[action]")]` line 19: `/Admin/EditPesertaAnswers/{id}` (NOT `/AssessmentAdmin/...`).

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — Commit:**
```bash
git add Controllers/AssessmentAdminController.cs
git commit -m "feat(v17.0-p321): GET EditPesertaAnswers controller (Admin/HC gate + IsEditable check)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "EditPesertaAnswers(int id)" Controllers/AssessmentAdminController.cs && grep -q "AssessmentEditEligibility.IsEditableAsync" Controllers/AssessmentAdminController.cs && grep -q "Roles = \"Admin, HC\"" Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - GET action exists with `[Authorize(Roles = "Admin, HC")]` (T-321-04 mitigation)
    - IsEditableAsync called pre-render (T-321-02 mitigation extends to controller layer)
    - Returns View(vm) resolves to Views/Admin/EditPesertaAnswers.cshtml (via override line 54)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): GET EditPesertaAnswers controller`
  </acceptance_criteria>
  <done>GET endpoint accessible at /Admin/EditPesertaAnswers/{id}, gated to Admin/HC, IsEditable enforced, builds clean, committed.</done>
</task>

<task type="auto">
  <name>Task 2: View Views/Admin/EditPesertaAnswers.cshtml + JS stub + flip modal markup</name>
  <files>Views/Admin/EditPesertaAnswers.cshtml, wwwroot/js/edit-peserta-answers.js</files>
  <read_first>
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 6 (lines 757-949)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Copywriting Contract + §Component Inventory Surface 1 + §A11y Contract
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-05 (reason labels verbose) + D-06 (flip modal copy) + D-07 (toast template)
    - Views/Admin/AssessmentMonitoringDetail.cshtml (verify route params untuk Back link asp-route-* binding)
    - Views/_ViewImports.cshtml (verify @using HcPortal.Models declaration)
  </read_first>
  <action>
**CRITICAL — File location: `Views/Admin/EditPesertaAnswers.cshtml`** (NOT `Views/AssessmentAdmin/`). Controller View override line 54 resolves to `~/Views/Admin/`.

**Step 1 — Verify Views/Admin/ folder exist:**
```bash
ls Views/Admin/ | head -5
```
Expected: AssessmentMonitoringDetail.cshtml + others. Kalau folder belum ada (improbable), create dulu.

**Step 2 — Create `Views/Admin/EditPesertaAnswers.cshtml`** (UI-SPEC reconciled — D-05 verbose labels + D-06 flip modal copy + A11y full):

```html
@model HcPortal.Models.EditPesertaAnswersViewModel
@{
    ViewData["Title"] = $"Edit Jawaban — {Model.FullName}";
}

<div class="container-fluid py-3">
  <a asp-action="AssessmentMonitoringDetail"
     asp-route-title="@Model.Session.Title"
     asp-route-category="@Model.Session.Category"
     asp-route-scheduleDate="@Model.Session.Schedule.ToString("yyyy-MM-dd")"
     class="btn btn-sm btn-outline-secondary">← Back to Monitoring</a>

  <h3 class="mt-3">Edit Jawaban — @Model.FullName (NIP @Model.NIP)</h3>

  <div class="card mb-3">
    <div class="card-body small">
      <strong>@Model.Session.Title</strong> · Kategori: @Model.Session.Category
      · Schedule: @Model.Session.Schedule.ToString("dd MMM yyyy HH:mm")<br/>
      Skor saat ini: <strong>@(Model.Session.Score?.ToString() ?? "—")%</strong>
      · Status: <strong>@(Model.Session.IsPassed == true ? "Pass" : Model.Session.IsPassed == false ? "Fail" : "—")</strong>
    </div>
  </div>

  @if (TempData["Error"] != null)
  {
    <div class="alert alert-danger" role="alert">@TempData["Error"]</div>
  }

  <div class="alert alert-warning" role="alert">
    <strong>⚠️ Catatan:</strong> Edit jawaban akan recompute skor + spider chart otomatis.
    Aksi ini di-log audit. Hasil tidak ditampilkan ke peserta.
  </div>

  <form id="editAnswersForm" method="post"
        asp-action="SubmitEditAnswers"
        data-preview-url="@Url.Action("PreviewEditScore", new { sessionId = Model.Session.Id })">
    @Html.AntiForgeryToken()
    <input type="hidden" name="SessionId" value="@Model.Session.Id" />
    <input type="hidden" name="UpdatedAt" value="@Model.UpdatedAt.ToString("O")" />

    @foreach (var (q, idx) in Model.Questions.Select((q, i) => (q, i + 1)))
    {
      <div class="card mb-3 question-card"
           data-question-id="@q.PackageQuestionId"
           data-question-type="@q.QuestionType">
        <div class="card-body">
          <div class="mb-2">
            <strong>Soal @idx</strong>
            <span class="badge bg-secondary">@(q.QuestionType == "MultipleChoice" ? "MC" : q.QuestionType == "MultipleAnswer" ? "MA" : "Essay")</span>
            @if (q.IsCurrentCorrect) { <span class="badge bg-success">✓ Benar</span> }
            else { <span class="badge bg-danger">✗ Salah</span> }
          </div>
          <div class="mb-2">@q.QuestionText</div>

          @if (q.QuestionType == "Essay")
          {
            <div class="text-muted fst-italic">
              Essay – manual grading via halaman Penilaian Essay
            </div>
          }
          else if (q.QuestionType == "MultipleChoice")
          {
            <div class="ms-2">
              @foreach (var opt in q.Options)
              {
                <div class="form-check">
                  <input class="form-check-input answer-input" type="radio"
                         name="Answers[@q.PackageQuestionId]"
                         value="@opt.Id"
                         id="opt-@q.PackageQuestionId-@opt.Id"
                         @(q.SelectedOptionIds.Contains(opt.Id) ? "checked" : "") />
                  <label class="form-check-label" for="opt-@q.PackageQuestionId-@opt.Id">
                    @opt.OptionText
                    @if (opt.IsCorrect) { <span class="badge bg-success ms-2">Kunci</span> }
                  </label>
                </div>
              }
            </div>
          }
          else // MultipleAnswer
          {
            <div class="ms-2">
              @foreach (var opt in q.Options)
              {
                <div class="form-check">
                  <input class="form-check-input answer-input" type="checkbox"
                         name="Answers[@q.PackageQuestionId]"
                         value="@opt.Id"
                         id="opt-@q.PackageQuestionId-@opt.Id"
                         @(q.SelectedOptionIds.Contains(opt.Id) ? "checked" : "") />
                  <label class="form-check-label" for="opt-@q.PackageQuestionId-@opt.Id">
                    @opt.OptionText
                    @if (opt.IsCorrect) { <span class="badge bg-success ms-2">Kunci</span> }
                  </label>
                </div>
              }
            </div>
          }

          @if (q.QuestionType != "Essay")
          {
            <div class="reason-block mt-2 d-none">
              <label class="form-label small" for="reason-code-@q.PackageQuestionId">
                Alasan edit <span class="text-danger" aria-hidden="true">*</span>
              </label>
              <select class="form-select form-select-sm reason-code"
                      id="reason-code-@q.PackageQuestionId"
                      name="Reasons[@q.PackageQuestionId].Code"
                      aria-required="true">
                <option value="">— Pilih —</option>
                @* D-05 verbose labels (UI-SPEC override) *@
                <option value="SoalSalah">Soal salah / typo</option>
                <option value="KunciSalah">Kunci jawaban salah</option>
                <option value="BugSistem">Bug sistem / glitch</option>
                <option value="PermintaanPeserta">Permintaan koreksi peserta</option>
                <option value="Lainnya">Lainnya (jelaskan)</option>
              </select>
              <textarea class="form-control form-control-sm mt-2 reason-text d-none" rows="2"
                        id="reason-text-@q.PackageQuestionId"
                        name="Reasons[@q.PackageQuestionId].Text"
                        placeholder="Detail alasan (wajib kalau pilih Lainnya)"></textarea>
            </div>
          }
        </div>
      </div>
    }

    <div class="d-flex gap-2 mt-3">
      <a asp-action="AssessmentMonitoringDetail"
         asp-route-title="@Model.Session.Title"
         asp-route-category="@Model.Session.Category"
         asp-route-scheduleDate="@Model.Session.Schedule.ToString("yyyy-MM-dd")"
         class="btn btn-outline-secondary">Cancel</a>
      <button type="submit" class="btn btn-primary" id="submitEditBtn">Save & Recompute</button>
    </div>
  </form>
</div>

<!-- Flip confirmation modal (D-06 eksplisit copy) -->
<div class="modal fade" id="flipConfirmModal" tabindex="-1"
     aria-labelledby="flipConfirmModalLabel" aria-describedby="flipModalBody" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="flipConfirmModalLabel">⚠️ Perubahan Hasil</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Tutup"></button>
      </div>
      <div class="modal-body" id="flipModalBody"></div>
      <div class="modal-footer">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Batal</button>
        <button type="button" class="btn btn-primary" id="flipConfirmBtn">Lanjutkan, simpan perubahan</button>
      </div>
    </div>
  </div>
</div>

@section Scripts {
  <script src="~/js/edit-peserta-answers.js" asp-append-version="true"></script>
}
```

**Step 3 — Create JS stub `wwwroot/js/edit-peserta-answers.js`** (full implementation di Task 3):

```javascript
console.log("edit-peserta-answers.js loaded");
```

**Step 4 — Build + smoke UAT initial render:**
```bash
dotnet build
dotnet run
```
Buka `http://localhost:5277/Admin/EditPesertaAnswers/{sessionId}` untuk session Completed valid. Verify:
- Page render tanpa error 500
- Semua soal MC/MA tampil dengan radio/checkbox terisi
- Kunci ditandai badge hijau
- Reason block hidden (d-none) initial
- Modal flipConfirmModal exist di DOM

**Step 5 — Commit:**
```bash
git add Views/Admin/EditPesertaAnswers.cshtml wwwroot/js/edit-peserta-answers.js
git commit -m "feat(v17.0-p321): EditPesertaAnswers.cshtml + JS stub (UI-SPEC D-05/D-06 reconciled + A11y)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && test -f Views/Admin/EditPesertaAnswers.cshtml && grep -q "HcPortal.Models.EditPesertaAnswersViewModel" Views/Admin/EditPesertaAnswers.cshtml && grep -q "Soal salah / typo" Views/Admin/EditPesertaAnswers.cshtml && grep -q "Kunci jawaban salah" Views/Admin/EditPesertaAnswers.cshtml && grep -q "flipConfirmModal" Views/Admin/EditPesertaAnswers.cshtml && grep -q "@Html.AntiForgeryToken" Views/Admin/EditPesertaAnswers.cshtml && test -f wwwroot/js/edit-peserta-answers.js</automated>
  </verify>
  <acceptance_criteria>
    - View file di `Views/Admin/` (NOT `Views/AssessmentAdmin/`)
    - `@model HcPortal.Models.EditPesertaAnswersViewModel` (namespace flat per PLAN 01)
    - D-05 verbose labels present: "Soal salah / typo", "Kunci jawaban salah", "Bug sistem / glitch", "Permintaan koreksi peserta", "Lainnya (jelaskan)"
    - Flip modal markup present dengan id flipConfirmModal + aria-labelledby + aria-describedby
    - `@Html.AntiForgeryToken()` present (T-321-05 mitigation)
    - Hidden inputs SessionId + UpdatedAt present (UpdatedAt format ISO 8601 `"O"`)
    - JS stub created (Task 3 akan replace content)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): EditPesertaAnswers.cshtml`
  </acceptance_criteria>
  <done>Edit page render successfully, D-05/D-06 copy verbose, A11y full, modal markup ready, JS stub linked, committed.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend JS — Dirty state + Reason validation + Flip modal AJAX</name>
  <files>wwwroot/js/edit-peserta-answers.js</files>
  <read_first>
    - wwwroot/js/edit-peserta-answers.js (Task 2 stub)
    - Views/Admin/EditPesertaAnswers.cshtml (Task 2 markup — verify selector class names: .question-card, .answer-input, .reason-block, .reason-code, .reason-text, #editAnswersForm, #submitEditBtn, #flipConfirmModal, #flipModalBody, #flipConfirmBtn)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 7 (lines 953-1149)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md CD-03 (vanilla addEventListener + snapshot compare)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Interaction Contract → Edit Page Flow + §Copywriting Contract (modal copy D-06)
  </read_first>
  <action>
**Step 1 — Replace `wwwroot/js/edit-peserta-answers.js` content** dengan implementation full (D-06 modal copy + a11y focus restore):

```javascript
document.addEventListener("DOMContentLoaded", () => {
  const form = document.getElementById("editAnswersForm");
  if (!form) return;
  const previewUrl = form.dataset.previewUrl;
  const submitBtn = document.getElementById("submitEditBtn");
  const flipModal = new bootstrap.Modal(document.getElementById("flipConfirmModal"));
  const flipBody = document.getElementById("flipModalBody");
  const flipConfirmBtn = document.getElementById("flipConfirmBtn");

  // Snapshot initial answers per question
  const initialState = {};
  document.querySelectorAll(".question-card").forEach(card => {
    const qid = card.dataset.questionId;
    const type = card.dataset.questionType;
    if (type === "Essay") return;
    const checked = Array.from(card.querySelectorAll(".answer-input:checked"))
      .map(i => i.value).sort();
    initialState[qid] = { type, options: checked };
  });

  function getCurrentAnswers(card) {
    const qid = card.dataset.questionId;
    const type = card.dataset.questionType;
    if (type === "Essay") return null;
    const checked = Array.from(card.querySelectorAll(".answer-input:checked"))
      .map(i => i.value).sort();
    return { qid, type, options: checked };
  }

  function isDirty(card) {
    const cur = getCurrentAnswers(card);
    if (!cur) return false;
    const init = initialState[cur.qid];
    if (!init) return false;
    if (cur.options.length !== init.options.length) return true;
    return cur.options.some((v, i) => v !== init.options[i]);
  }

  function updateDirtyUI(card) {
    const reasonBlock = card.querySelector(".reason-block");
    if (!reasonBlock) return;
    if (isDirty(card)) {
      card.classList.add("border-warning");
      reasonBlock.classList.remove("d-none");
    } else {
      card.classList.remove("border-warning");
      reasonBlock.classList.add("d-none");
      const select = reasonBlock.querySelector(".reason-code");
      const textarea = reasonBlock.querySelector(".reason-text");
      if (select) select.value = "";
      if (textarea) { textarea.value = ""; textarea.classList.add("d-none"); }
    }
  }

  document.querySelectorAll(".answer-input").forEach(input => {
    input.addEventListener("change", () => {
      const card = input.closest(".question-card");
      updateDirtyUI(card);
    });
  });

  document.querySelectorAll(".reason-code").forEach(sel => {
    sel.addEventListener("change", () => {
      const block = sel.closest(".reason-block");
      const ta = block.querySelector(".reason-text");
      if (sel.value === "Lainnya") {
        ta.classList.remove("d-none");
      } else {
        ta.classList.add("d-none");
        ta.value = "";
      }
    });
  });

  function collectDiff() {
    const diff = [];
    document.querySelectorAll(".question-card").forEach(card => {
      if (!isDirty(card)) return;
      const qid = card.dataset.questionId;
      const cur = getCurrentAnswers(card);
      const reasonBlock = card.querySelector(".reason-block");
      const code = reasonBlock.querySelector(".reason-code").value;
      const text = reasonBlock.querySelector(".reason-text").value;
      diff.push({ questionId: qid, options: cur.options.map(Number), reasonCode: code, reasonText: text });
    });
    return diff;
  }

  function validateClient(diff) {
    if (diff.length === 0) {
      alert("Tidak ada perubahan untuk disimpan.");
      return false;
    }
    for (const d of diff) {
      if (!d.reasonCode) {
        alert(`Pilih alasan edit terlebih dahulu (Soal #${d.questionId}).`);
        return false;
      }
      if (d.reasonCode === "Lainnya" && !d.reasonText.trim()) {
        alert(`Isi detail alasan untuk opsi Lainnya (Soal #${d.questionId}).`);
        return false;
      }
    }
    return true;
  }

  form.addEventListener("submit", async (e) => {
    e.preventDefault();
    const diff = collectDiff();
    if (!validateClient(diff)) return;

    submitBtn.disabled = true;
    submitBtn.textContent = "Memeriksa...";

    try {
      // Dry-run preview
      const fd = new FormData();
      diff.forEach((d, i) => {
        fd.append(`Drafts[${i}].QuestionId`, d.questionId);
        d.options.forEach(o => fd.append(`Drafts[${i}].Options`, o));
      });
      const resp = await fetch(previewUrl, {
        method: "POST",
        body: fd,
        headers: {
          "RequestVerificationToken": form.querySelector('input[name=__RequestVerificationToken]').value
        }
      });
      if (!resp.ok) throw new Error("Preview gagal");
      const preview = await resp.json();
      const oldPassed = preview.oldIsPassed;
      const newPassed = preview.newIsPassed;
      const flip = (oldPassed === true && newPassed === false) || (oldPassed === false && newPassed === true);

      if (flip) {
        // D-06 eksplisit copy
        let msg;
        if (oldPassed === true && newPassed === false) {
          msg = `Perubahan ini akan <strong>menggagalkan peserta</strong>. ` +
                `NomorSertifikat akan dicabut${preview.nomorSertifikat ? ` (No: ${preview.nomorSertifikat})` : ""} ` +
                `dan TrainingRecord di-set Failed. Lanjutkan?`;
        } else {
          msg = `Perubahan ini akan <strong>meluluskan peserta</strong>. ` +
                (preview.willGenerateCert
                  ? `NomorSertifikat baru akan di-generate (GenerateCertificate && bukan PreTest).`
                  : `Sertifikat TIDAK akan di-generate (session bukan eligible).`) +
                ` Lanjutkan?`;
        }
        flipBody.innerHTML = msg;
        flipConfirmBtn.onclick = () => { flipModal.hide(); form.submit(); };
        flipModal.show();
        submitBtn.disabled = false;
        submitBtn.textContent = "Save & Recompute";
        // A11y: restore focus to submit button after modal close
        document.getElementById('flipConfirmModal').addEventListener('hidden.bs.modal', function once() {
          submitBtn.focus();
          document.getElementById('flipConfirmModal').removeEventListener('hidden.bs.modal', once);
        });
        return;
      }

      // No flip -> submit langsung
      form.submit();
    } catch (err) {
      console.error(err);
      alert("Gagal memeriksa preview: " + err.message);
      submitBtn.disabled = false;
      submitBtn.textContent = "Save & Recompute";
    }
  });
});
```

**Step 2 — UAT manual (browser test):**
```bash
dotnet run
```
Buka edit page:
1. Ubah 1 jawaban MC → card highlight border-warning, reason dropdown muncul.
2. Pilih "Lainnya" → textarea muncul.
3. Switch back ke jawaban asli → highlight + reason hilang.
4. Submit dengan jawaban yang flip Pass→Fail → modal muncul dengan body copy "menggagalkan peserta + NomorSertifikat dicabut".
5. Klik "Batal" → modal close, focus kembali ke submit button.
6. Klik "Lanjutkan, simpan perubahan" → form submit (POST endpoint belum ada Task 4 — expect 404 atau redirect; UAT step 4-6 lengkap setelah Task 4).

**Step 3 — Commit:**
```bash
git add wwwroot/js/edit-peserta-answers.js
git commit -m "feat(v17.0-p321): edit-peserta-answers.js (dirty state + reason validation + D-06 flip modal + a11y focus restore)"
```
  </action>
  <verify>
    <automated>grep -q "editAnswersForm" wwwroot/js/edit-peserta-answers.js && grep -q "previewUrl" wwwroot/js/edit-peserta-answers.js && grep -q "flipConfirmModal" wwwroot/js/edit-peserta-answers.js && grep -q "menggagalkan peserta" wwwroot/js/edit-peserta-answers.js && grep -q "meluluskan peserta" wwwroot/js/edit-peserta-answers.js && grep -q "RequestVerificationToken" wwwroot/js/edit-peserta-answers.js && grep -q "hidden.bs.modal" wwwroot/js/edit-peserta-answers.js</automated>
  </verify>
  <acceptance_criteria>
    - Dirty state detection: snapshot initialState + isDirty() per card via answer-input change listener
    - Reason validation: client-side require code + require text for Lainnya
    - AJAX POST to data-preview-url with RequestVerificationToken header
    - Flip modal body uses D-06 copy: "menggagalkan peserta" / "meluluskan peserta"
    - A11y: focus restore via `hidden.bs.modal` once-listener
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): edit-peserta-answers.js`
  </acceptance_criteria>
  <done>JS implements dirty state + reason validation + AJAX preview + D-06 flip modal copy + a11y focus restore, manual UAT initial flow OK, committed.</done>
</task>

<task type="auto">
  <name>Task 4: Controller POST SubmitEditAnswers (Transaction + Audit + Regrade + SignalR broadcast)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (Task 1 GET action inserted; insert POST adjacent)
    - Models/EditAnswersSubmission.cs (PLAN 01 DTO: SessionId, UpdatedAt, Answers, Reasons)
    - Models/AuditLog.cs (ActionType, EntityType, EntityId, UserId, Description, CreatedAt fields)
    - Models/AssessmentEditLog.cs (PLAN 01 — all snapshot fields)
    - Hubs/AssessmentHub.cs (verify group naming `monitor-{batchKey}` line ~57)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 8 (lines 1153-1398)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md (T-321-05 CSRF, T-321-06 tx, T-321-07 concurrency)
  </read_first>
  <action>
**Step 1 — Insert POST action SETELAH GET EditPesertaAnswers** (Task 1) di `Controllers/AssessmentAdminController.cs`:

```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> SubmitEditAnswers(HcPortal.Models.EditAnswersSubmission form)
{
    var session = await _context.AssessmentSessions
        .Include(s => s.User)
        .FirstOrDefaultAsync(s => s.Id == form.SessionId);
    if (session == null) { TempData["Error"] = "Sesi tidak ditemukan."; return RedirectToAction("ManageAssessment"); }

    var redirectBack = RedirectToAction("AssessmentMonitoringDetail", new {
        title = session.Title, category = session.Category, scheduleDate = session.Schedule
    });

    // Eligibility (T-321-02)
    if (!await HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session))
    {
        TempData["Error"] = "Sesi tidak dapat di-edit.";
        return redirectBack;
    }

    // Concurrency check (T-321-07) — tolerance 1 detik karena DateTime round-trip ISO 8601 "O"
    var currentUpdatedAt = session.UpdatedAt ?? session.CreatedAt;
    if (Math.Abs((currentUpdatedAt - form.UpdatedAt).TotalSeconds) > 1)
    {
        TempData["Error"] = "Sesi sudah diubah admin lain. Refresh halaman.";
        return redirectBack;
    }

    // Validate reasons (server-side, T-321-05 server validation in addition to client)
    foreach (var (qid, _) in form.Answers)
    {
        if (!form.Reasons.TryGetValue(qid, out var reason) || string.IsNullOrWhiteSpace(reason.Code))
        {
            TempData["Error"] = $"Alasan wajib untuk soal {qid}.";
            return redirectBack;
        }
        if (!new[] { "SoalSalah", "KunciSalah", "BugSistem", "PermintaanPeserta", "Lainnya" }.Contains(reason.Code))
        {
            TempData["Error"] = "ReasonCode tidak valid.";
            return redirectBack;
        }
        if (reason.Code == "Lainnya" && string.IsNullOrWhiteSpace(reason.Text))
        {
            TempData["Error"] = "ReasonText wajib kalau ReasonCode == Lainnya.";
            return redirectBack;
        }
    }

    var actorId = User.FindFirstValue(System.Security.Claims.ClaimTypes.NameIdentifier) ?? "";
    var actor = await _context.Users.FirstOrDefaultAsync(u => u.Id == actorId);
    var actorRole = User.IsInRole("Admin") ? "Admin" : "HC";
    var actorName = $"{actor?.NIP} - {actor?.FullName}";

    // Load questions+options + existing responses
    var qIds = form.Answers.Keys.ToList();
    var questions = await _context.PackageQuestions
        .Include(q => q.Options)
        .Where(q => qIds.Contains(q.Id))
        .ToListAsync();
    var existingResponses = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == session.Id && qIds.Contains(r.PackageQuestionId))
        .ToListAsync();

    using var tx = await _context.Database.BeginTransactionAsync();
    try
    {
        int editCount = 0;
        foreach (var (qId, newOptionIds) in form.Answers)
        {
            var q = questions.FirstOrDefault(x => x.Id == qId);
            if (q == null) continue;
            if ((q.QuestionType ?? "MultipleChoice") == "Essay") continue;

            var validOptionIds = q.Options.Select(o => o.Id).ToHashSet();
            var sanitizedNew = newOptionIds.Where(id => validOptionIds.Contains(id)).ToList();

            // Snapshot OLD
            var oldResponses = existingResponses
                .Where(r => r.PackageQuestionId == qId && r.PackageOptionId.HasValue).ToList();
            var oldOptionIds = oldResponses.Select(r => r.PackageOptionId!.Value).ToList();
            var oldTextSnapshot = string.Join(", ",
                q.Options.Where(o => oldOptionIds.Contains(o.Id)).Select(o => o.OptionText));

            // Apply edit: MA pattern = delete-all + insert-new
            _context.PackageUserResponses.RemoveRange(oldResponses);
            foreach (var newOid in sanitizedNew)
            {
                _context.PackageUserResponses.Add(new PackageUserResponse
                {
                    AssessmentSessionId = session.Id,
                    PackageQuestionId = qId,
                    PackageOptionId = newOid,
                    TextAnswer = null,
                    SubmittedAt = DateTime.UtcNow
                });
            }

            // Snapshot NEW
            var newTextSnapshot = string.Join(", ",
                q.Options.Where(o => sanitizedNew.Contains(o.Id)).Select(o => o.OptionText));

            // Insert AssessmentEditLog (NewScore/NewIsPassed diisi setelah regrade)
            _context.AssessmentEditLogs.Add(new AssessmentEditLog
            {
                AssessmentSessionId = session.Id,
                PackageQuestionId = qId,
                QuestionTextSnapshot = q.QuestionText ?? "",
                OldAnswerJson = System.Text.Json.JsonSerializer.Serialize(oldOptionIds),
                OldAnswerTextSnapshot = oldTextSnapshot,
                NewAnswerJson = System.Text.Json.JsonSerializer.Serialize(sanitizedNew),
                NewAnswerTextSnapshot = newTextSnapshot,
                OldScore = session.Score,
                OldIsPassed = session.IsPassed,
                ActorUserId = actorId,
                ActorName = actorName,
                ActorRole = actorRole,
                ReasonCode = form.Reasons[qId].Code,
                ReasonText = form.Reasons[qId].Text,
                EditedAt = DateTime.UtcNow
            });
            editCount++;
        }
        await _context.SaveChangesAsync();

        // Recompute via PLAN 02 service
        var (newScore, newIsPassed, oldScore, oldIsPassed) = await _gradingService.RegradeAfterEditAsync(session);

        // Backfill NewScore/NewIsPassed di AssessmentEditLog entries baru
        await _context.AssessmentEditLogs
            .Where(l => l.AssessmentSessionId == session.Id && l.NewScore == null && l.ActorUserId == actorId)
            .ExecuteUpdateAsync(l => l
                .SetProperty(x => x.NewScore, newScore)
                .SetProperty(x => x.NewIsPassed, newIsPassed));

        // AuditLog generic dual-write
        _context.AuditLogs.Add(new AuditLog
        {
            ActionType = "EditAssessmentAnswer",
            EntityType = "AssessmentSession",
            EntityId = session.Id.ToString(),
            UserId = actorId,
            Description = $"Edit {editCount} jawaban session #{session.Id} ({session.User?.FullName}), score {oldScore?.ToString() ?? "—"} → {newScore}",
            CreatedAt = DateTime.UtcNow
        });
        await _context.SaveChangesAsync();

        await tx.CommitAsync();

        // Cache invalidate (kalau ada key exam-status)
        _cache.Remove($"exam-status-{session.Id}");

        // SignalR broadcast via injected _hubContext (field, NOT [FromServices])
        var batchKey = $"{session.Title}|{session.Category}|{session.Schedule.Date:yyyy-MM-dd}";
        await _hubContext.Clients.Group($"monitor-{batchKey}").SendAsync("workerAnswerEdited", new
        {
            sessionId = session.Id,
            workerName = session.User?.FullName ?? "Unknown",
            oldScore, newScore, oldIsPassed, newIsPassed,
            actorName, actorRole
        });

        string flip = (oldIsPassed == true, newIsPassed) switch
        {
            (true, false) => "Pass→Fail",
            (false, true) => "Fail→Pass",
            (true, true) => "Pass→Pass",
            _ => "Fail→Fail"
        };
        TempData["Success"] = $"Edit {editCount} jawaban berhasil. Score: {oldScore?.ToString() ?? "—"} → {newScore}, {flip}";
        return redirectBack;
    }
    catch (Exception ex)
    {
        await tx.RollbackAsync();
        _logger.LogError(ex, "Edit jawaban gagal untuk session {SessionId}", session.Id);
        TempData["Error"] = "Terjadi kesalahan saat menyimpan. Coba lagi atau hubungi administrator.";
        return redirectBack;
    }
}
```

**Step 2 — Verify ctor injection ada (sanity check):**
```bash
grep -nE "_context|_cache|_logger|_hubContext|_gradingService" Controllers/AssessmentAdminController.cs | head -10
```
Confirm all 5 fields present di ctor.

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — Smoke UAT happy-path:**
```bash
dotnet run
```
Browser:
- Buka `/Admin/EditPesertaAnswers/{completedSessionId}`
- Ubah 1 jawaban MC → pilih reason SoalSalah
- Klik Save → preview AJAX → kalau flip → modal → Lanjutkan → form submit
- Verify redirect ke AssessmentMonitoringDetail dengan TempData["Success"]
- Verify DB: `SELECT COUNT(*) FROM AssessmentEditLogs WHERE AssessmentSessionId={id}` ++, `SELECT Score FROM AssessmentSessions WHERE Id={id}` berubah

**Step 5 — Commit:**
```bash
git add Controllers/AssessmentAdminController.cs
git commit -m "feat(v17.0-p321): POST SubmitEditAnswers (tx + audit + regrade + SignalR workerAnswerEdited)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "SubmitEditAnswers" Controllers/AssessmentAdminController.cs && grep -q "ValidateAntiForgeryToken" Controllers/AssessmentAdminController.cs && grep -q "BeginTransactionAsync" Controllers/AssessmentAdminController.cs && grep -q "_gradingService.RegradeAfterEditAsync" Controllers/AssessmentAdminController.cs && grep -q "workerAnswerEdited" Controllers/AssessmentAdminController.cs && grep -q "Sesi sudah diubah admin lain" Controllers/AssessmentAdminController.cs && grep -q "monitor-" Controllers/AssessmentAdminController.cs    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "SubmitEditAnswers" Controllers/AssessmentAdminController.cs && grep -q "ValidateAntiForgeryToken" Controllers/AssessmentAdminController.cs && grep -q "BeginTransactionAsync" Controllers/AssessmentAdminController.cs && grep -q "_gradingService.RegradeAfterEditAsync" Controllers/AssessmentAdminController.cs && grep -q "workerAnswerEdited" Controllers/AssessmentAdminController.cs && grep -q "Sesi sudah diubah admin lain" Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - POST action exists with `[ValidateAntiForgeryToken]` (T-321-05) + `[Authorize(Roles = "Admin, HC")]` (T-321-04)
    - `BeginTransactionAsync` + try/catch + RollbackAsync present (T-321-06 transaction integrity)
    - Concurrency token compare with ±1s tolerance (T-321-07)
    - Server-side reason validation: enum whitelist + Lainnya requires text
    - Dual audit: AssessmentEditLog per question + AuditLog generic ActionType="EditAssessmentAnswer"
    - SignalR broadcast `Clients.Group($"monitor-{batchKey}").SendAsync("workerAnswerEdited", ...)`
    - Backfill NewScore/NewIsPassed via ExecuteUpdateAsync after RegradeAfterEditAsync
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): POST SubmitEditAnswers`
  </acceptance_criteria>
  <done>POST endpoint complete dengan tx wrap + dual audit + regrade + SignalR broadcast, builds clean, smoke UAT happy-path OK, committed.</done>
</task>

<task type="auto">
  <name>Task 5: Controller POST PreviewEditScore (Dry-run flip detection — JSON contract)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (Tasks 1+4 inserted, append POST PreviewEditScore adjacent)
    - Models/EditAnswersSubmission.cs (EditDraft + EditDraftSubmission DTOs from PLAN 01)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 9 (lines 1402-1451)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md (T-321-08 preview contract — no DB write)
  </read_first>
  <action>
**Step 1 — Insert POST PreviewEditScore SETELAH SubmitEditAnswers** (Task 4):

```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> PreviewEditScore(int sessionId,
    HcPortal.Models.EditDraftSubmission form)
{
    var session = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == sessionId);
    if (session == null) return NotFound();
    if (!await HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session))
        return Forbid();

    var overrideAnswers = form.Drafts.ToDictionary(d => d.QuestionId, d => d.Options);

    // PreviewScoreAsync = PLAN 02 public wrapper (TIDAK mutate DB)
    var (newScore, newIsPassed) = await _gradingService.PreviewScoreAsync(session, overrideAnswers);

    return Json(new
    {
        oldScore = session.Score,
        oldIsPassed = session.IsPassed,
        newScore,
        newIsPassed,
        hasCert = !string.IsNullOrEmpty(session.NomorSertifikat),
        nomorSertifikat = session.NomorSertifikat,
        willGenerateCert = session.GenerateCertificate && session.AssessmentType != "PreTest"
    });
}
```

**Step 2 — Verify TIDAK ada SaveChanges di action body (T-321-08):**
```bash
grep -A 30 "PreviewEditScore" Controllers/AssessmentAdminController.cs | grep -E "SaveChanges|Add\(|Update\(|Remove" | head -5
```
Expected output: kosong (no DB mutation di preview).

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — Smoke test endpoint via DevTools:**
```bash
dotnet run
```
Buka edit page, ubah jawaban, klik Save. Browser DevTools Network tab → verify POST `/Admin/PreviewEditScore?sessionId=...` return Status 200 + JSON shape:
```json
{"oldScore": 65, "oldIsPassed": false, "newScore": 75, "newIsPassed": true, "hasCert": false, "nomorSertifikat": null, "willGenerateCert": true}
```

**Step 5 — Commit:**
```bash
git add Controllers/AssessmentAdminController.cs
git commit -m "feat(v17.0-p321): POST PreviewEditScore endpoint (dry-run flip detection, no DB mutation)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "PreviewEditScore" Controllers/AssessmentAdminController.cs && grep -q "_gradingService.PreviewScoreAsync" Controllers/AssessmentAdminController.cs && grep -q "willGenerateCert" Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - PreviewEditScore action exists with `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`
    - Calls `_gradingService.PreviewScoreAsync` (PLAN 02 public wrapper)
    - JSON response shape exact: `{oldScore, oldIsPassed, newScore, newIsPassed, hasCert, nomorSertifikat, willGenerateCert}`
    - NO `SaveChangesAsync` / `Add()` / `Update()` / `Remove()` di action body (T-321-08 mitigation — dry-run only)
    - IsEditableAsync gate present (Forbid kalau ineligible)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): POST PreviewEditScore`
  </acceptance_criteria>
  <done>Dry-run preview endpoint complete, JSON contract correct, no DB mutation verified, committed.</done>
</task>

<task type="auto">
  <name>Task 6: View AssessmentMonitoringDetail.cshtml — Dropdown ⋮ hybrid refactor (UI-SPEC reconciled)</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (VERIFY action column block line 286-338)
    - Verify field `session.UserStatus` di line 288, 299, 311, 323
    - Verify status string `"Not started"` (lowercase s) di line 288
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 10 (lines 1455-1579)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Component Inventory Surface 2 + §Dropdown ⋮ items + §A11y Contract Dropdown
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md (T-321-09 UI gating server-side)
  </read_first>
  <action>
**Step 1 — Re-verify action column location:**
```bash
grep -nE "Reshuffle|AkhiriUjian|btn-activity-log|reshuffleWorker" Views/Admin/AssessmentMonitoringDetail.cshtml | head -10
```

**Step 2 — Replace action column block (line ~286-338)** dengan hybrid layout (preserve `Model.IsPackageMode`, `session.UserStatus`, `reshuffleWorker(@session.Id)`):

```html
@{ var canEdit = HcPortal.Helpers.AssessmentEditEligibility.IsEditableShallow(session); }

<td>
  <div class="d-flex gap-1 align-items-center">
    @* Inline: View Results (Completed only) *@
    @if (session.UserStatus == "Completed")
    {
      <a href="@Url.Action("Results", "CMP", new { id = session.Id })" target="_blank"
         class="btn btn-success btn-sm">View Results</a>
    }
    @* Inline: Activity Log (always) *@
    <button type="button"
            class="btn btn-outline-secondary btn-sm btn-activity-log"
            data-session-id="@session.Id"
            data-worker-name="@session.UserFullName"
            title="Lihat activity log">
      <i class="bi bi-clock-history"></i>
    </button>

    @* Dropdown ⋮ *@
    <div class="dropdown">
      <button class="btn btn-sm btn-outline-secondary"
              type="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
              aria-haspopup="true"
              aria-label="Aksi lain untuk @session.UserFullName"
              style="min-height:40px;">⋮</button>
      <ul class="dropdown-menu dropdown-menu-end">
        @if (canEdit)
        {
          <li>
            <a class="dropdown-item" asp-action="EditPesertaAnswers" asp-controller="AssessmentAdmin" asp-route-id="@session.Id">
              <i class="bi bi-pencil-square me-1"></i>Edit Jawaban
            </a>
          </li>
        }
        @if (session.UserStatus != "Cancelled")
        {
          <li>
            <form asp-action="ResetAssessment" asp-controller="AssessmentAdmin" method="post" class="m-0"
                  onsubmit="return confirm('Reset sesi ini? Semua jawaban akan dihapus dan peserta dapat mengulang ujian.')">
              @Html.AntiForgeryToken()
              <input type="hidden" name="id" value="@session.Id" />
              <button type="submit" class="dropdown-item">
                <i class="bi bi-arrow-counterclockwise me-1"></i>Reset
              </button>
            </form>
          </li>
        }
        @if (session.UserStatus == "InProgress")
        {
          <li>
            <form asp-action="AkhiriUjian" asp-controller="AssessmentAdmin" method="post" class="m-0"
                  onsubmit="return confirm('Akhiri ujian untuk @(session.UserFullName)? Jawaban tersimpan akan dinilai otomatis.')">
              @Html.AntiForgeryToken()
              <input type="hidden" name="id" value="@session.Id" />
              <button type="submit" class="dropdown-item text-danger">
                <i class="bi bi-x-octagon me-1"></i>Akhiri Ujian
              </button>
            </form>
          </li>
        }
        @if (Model.IsPackageMode && (session.UserStatus == "Not started" || session.UserStatus == "Abandoned"))
        {
          <li>
            <button type="button"
                    class="dropdown-item"
                    data-session-id="@session.Id"
                    onclick="reshuffleWorker(@session.Id)">
              <i class="bi bi-shuffle me-1"></i>Reshuffle
            </button>
          </li>
        }
      </ul>
    </div>
  </div>
</td>
```

> **UI-SPEC reconciled** (override RESEARCH lama emoji):
> - `bi bi-*` icons — UI-SPEC line 159
> - `aria-label="Aksi lain untuk {workerName}"` + `aria-haspopup="true"` + `dropdown-menu-end`
> - Touch target 40px via `style="min-height:40px"`
> - Status string `"Not started"` (lowercase 's') — verified line 288
> - Existing handlers PRESERVED: form POST + onsubmit confirm dialog + reshuffleWorker JS

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — UAT manual (browser):**
Buka monitoring detail page. Per row verify View Results + 🕐 inline; ⋮ menu dropdown-menu-end + ARIA; Edit Jawaban conditional render; ESC close; mobile auto-flip.

**Step 5 — Commit:**
```bash
git add Views/Admin/AssessmentMonitoringDetail.cshtml
git commit -m "feat(v17.0-p321): per-user action dropdown ⋮ hybrid (bi-icons + ARIA + IsEditableShallow gating)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "AssessmentEditEligibility.IsEditableShallow" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "dropdown-menu-end" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "bi-pencil-square" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "Aksi lain untuk" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "asp-action=\"EditPesertaAnswers\"" Views/Admin/AssessmentMonitoringDetail.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - `IsEditableShallow(session)` called for `canEdit` conditional render (T-321-09 mitigation — server-side gating)
    - Dropdown uses `bi bi-*` icons — NOT emoji
    - ARIA: `aria-label="Aksi lain untuk ..."` + `aria-haspopup="true"` + `dropdown-menu-end`
    - Touch target: `style="min-height:40px"` on toggle
    - Conditional Edit Jawaban hanya saat canEdit==true
    - Existing handlers preserved (form POST + onsubmit confirm + reshuffleWorker)
    - Status string verified `"Not started"` (lowercase 's')
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): per-user action dropdown ⋮ hybrid`
  </acceptance_criteria>
  <done>Dropdown ⋮ hybrid render correct, IsEditable conditional UI gating, ARIA + touch target compliant, committed.</done>
</task>

<task type="auto">
  <name>Task 7: SignalR Frontend Handler workerAnswerEdited (reuse window.showAssessmentToast, 5s default)</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (scripts section — search `window.assessmentHub.on('workerSubmitted'` ~line 1244)
    - wwwroot/js/assessment-hub.js (verify line 95 hub connection variable, line 96 `window.showAssessmentToast(message)`)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 11 (lines 1583-1659)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-07 (toast template verbose)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Toast SignalR
  </read_first>
  <action>
**TOAST TIMEOUT DECISION (RESOLVED PLANNING OPEN QUESTION):**
Per planning_context guidance — DEFAULT 5 detik (existing `assessment-hub.js:32` pattern). UI-SPEC §Toast SignalR specs 8 detik tapi:
- Konsistensi cross-event (workerSubmitted, workerStatusChanged, dst. semua 5s existing)
- Easy revision post-UAT kalau user objects
- Reuse `window.showAssessmentToast(message)` tanpa override timeout param

Rationale recorded di commit message + SUMMARY.

**Step 1 — Verify SignalR handler insertion point:**
```bash
grep -n "window.assessmentHub.on" Views/Admin/AssessmentMonitoringDetail.cshtml | head -5
```

**Step 2 — Verify selector class names existing:**
```bash
grep -nE 'class="(score|result|completedat)-cell|data-session-id' Views/Admin/AssessmentMonitoringDetail.cshtml | head -10
```
Adjust handler kalau class actual berbeda. RESEARCH verified `.result-cell` line ~282, `.completedat-cell` line ~284. **`.score-cell` perlu confirm** — kalau tidak ada class, tambah `class="score-cell"` di markup row template.

**Step 3 — Insert handler block** setelah block `window.assessmentHub.on('workerSubmitted', ...)`:

```javascript
// workerAnswerEdited: admin/HC edited a session's answers (Phase 321 EDIT-09)
window.assessmentHub.on('workerAnswerEdited', function (data) {
    // Update row score + result cell tanpa full reload
    var row = document.querySelector('tr[data-session-id="' + data.sessionId + '"]');
    if (row) {
        var scoreCell = row.querySelector('.score-cell');
        var resultCell = row.querySelector('.result-cell');
        if (scoreCell) scoreCell.textContent = data.newScore + '%';
        if (resultCell) {
            resultCell.textContent = data.newIsPassed ? 'Pass' : 'Fail';
            resultCell.className = 'result-cell ' + (data.newIsPassed ? 'text-success' : 'text-danger');
        }
    }

    // D-07 toast verbose template
    var oldResult = (data.oldIsPassed === true) ? 'Pass' : (data.oldIsPassed === false) ? 'Fail' : '—';
    var newResult = data.newIsPassed ? 'Pass' : 'Fail';
    var flipSegment = (data.oldIsPassed !== data.newIsPassed)
        ? ', ' + oldResult + '→' + newResult
        : '';
    var msg = data.actorRole + ' ' + data.actorName +
              ' edit jawaban ' + data.workerName +
              ': Score ' + (data.oldScore !== null && data.oldScore !== undefined ? data.oldScore : '—') + '→' + data.newScore +
              flipSegment;
    // REUSE existing toast (verified wwwroot/js/assessment-hub.js:96)
    // TIMEOUT DECISION: default 5s consistency (no timeout override param)
    if (typeof window.showAssessmentToast === 'function') {
        window.showAssessmentToast(msg);
    } else {
        console.log('[toast]', msg);
    }
});
```

**Step 4 — Kalau `.score-cell` tidak ada, tambah `class="score-cell"` ke `<td>` row template area.**

**Step 5 — Build:**
```bash
dotnet build
```

**Step 6 — UAT 2-tab cross-tab live update:**
Buka monitoring detail di 2 tab browser (sama session group). Di tab 1 (Admin), edit jawaban → save sukses → verify di tab 2: row score+result cell update otomatis tanpa reload + toast verbose D-07 muncul "Admin {Name} edit jawaban {Worker}: Score 65→75, Fail→Pass".

**Step 7 — Commit:**
```bash
git add Views/Admin/AssessmentMonitoringDetail.cshtml
git commit -m "feat(v17.0-p321): SignalR workerAnswerEdited handler + showAssessmentToast reuse (5s default per consistency)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "workerAnswerEdited" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "window.showAssessmentToast" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "edit jawaban" Views/Admin/AssessmentMonitoringDetail.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - Handler `window.assessmentHub.on('workerAnswerEdited', ...)` present
    - Reuse `window.showAssessmentToast(msg)` — NO timeout override (5s default per planning_context decision)
    - Toast template D-07 verbose: `{actorRole} {actorName} edit jawaban {workerName}: Score {old}→{new}{, flip?}`
    - Row update via selectors `tr[data-session-id]`, `.score-cell`, `.result-cell` (score-cell added kalau missing)
    - Result cell class swap text-success/text-danger
    - Manual UAT cross-tab live update verified (2-tab observe)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): SignalR workerAnswerEdited handler`
  </acceptance_criteria>
  <done>SignalR handler integrated, reuse existing toast helper, D-07 template + 5s default consistency, cross-tab UAT pass, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → controller GET/POST | Untrusted user input crosses here; auth gate + CSRF token + IsEditable required |
| client form (JS) → POST endpoints | Anti-forgery token + server-side validation reinforces client-side |
| controller POST → DB write (PackageUserResponses + AssessmentEditLogs + AuditLogs + AssessmentSessions + TrainingRecords) | Multi-table write must be atomic via transaction |
| controller POST → SignalR broadcast `monitor-{batchKey}` | Broadcast payload visible to all admins in group; no PII risk (worker name + score already visible) |
| controller PreviewEditScore → service PreviewScoreAsync | Dry-run path; NO DB mutation allowed |
| view dropdown ⋮ render → IsEditableShallow | Server-side conditional render prevents client-only bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-321-04 | Elevation of Privilege (Auth gating) | GET + POST + PreviewEditScore actions | mitigate | `[Authorize(Roles = "Admin, HC")]` di semua 3 actions. Worker → 403/redirect login. Verify via Playwright auth-gate test (PLAN 04 Task 2). |
| T-321-05 | Tampering (CSRF) + Information Disclosure (XSS in Lainnya free-text) | POST SubmitEditAnswers + view form | mitigate | `[ValidateAntiForgeryToken]` di POST + `@Html.AntiForgeryToken()` di form (Task 2). XSS: free-text `ReasonText` ditampilkan di Activity Log Edit History (PLAN 04) via Razor `@` auto-encode — JANGAN pakai `@Html.Raw`. Verify PLAN 04 partial gunakan `@log.ReasonText` (encoded), bukan `@Html.Raw(log.ReasonText)`. |
| T-321-06 | Tampering (Transaction integrity) | POST SubmitEditAnswers | mitigate | `using var tx = await _context.Database.BeginTransactionAsync()` + try/catch + `await tx.RollbackAsync()` di catch block + `await tx.CommitAsync()` di happy path. Multi-table write atomic. Exception apa pun → rollback total. |
| T-321-07 | Concurrency (Lost-update race) | POST SubmitEditAnswers | mitigate | `UpdatedAt` hidden field ISO 8601 `"O"` round-trip + server compare `Math.Abs((current - form.UpdatedAt).TotalSeconds) > 1` (1s tolerance). Stale → TempData error + redirect. Service PLAN 02 ExecuteUpdateAsync status guard tambahan layer. |
| T-321-08 | Information Disclosure + Tampering (Preview contract violation) | POST PreviewEditScore | mitigate | Action TIDAK pernah call `SaveChangesAsync` / `Add()` / `Update()` / `Remove()`. PreviewScoreAsync PLAN 02 verified pure. Verify grep returns empty. |
| T-321-09 | Elevation of Privilege (UI dropdown bypass) | AssessmentMonitoringDetail.cshtml dropdown item Edit Jawaban | mitigate | Conditional render server-side via `IsEditableShallow(session)` Razor `@if (canEdit)` — bukan JS hide. Klien yang tweak HTML via DevTools tetap kena Server-side IsEditableAsync di GET endpoint (T-321-04 + T-321-02). Defense in depth. |

</threat_model>

<verification>
- `dotnet build` exits 0 after each task commit
- 7 commit di branch `feature/phase-321-edit-jawaban`
- Smoke UAT: edit page render OK, dirty state + reason validation work, preview AJAX + flip modal show, submit POST persist + SignalR cross-tab update verified manual
- Threat mitigations grep-verified per task
</verification>

<success_criteria>
- 7 task commit di branch
- 3 controller actions baru exist (/Admin/EditPesertaAnswers/{id} GET + POST SubmitEditAnswers + POST PreviewEditScore)
- 1 view baru exist (Views/Admin/EditPesertaAnswers.cshtml)
- 1 JS file baru exist (wwwroot/js/edit-peserta-answers.js)
- Views/Admin/AssessmentMonitoringDetail.cshtml modified: dropdown ⋮ hybrid + SignalR handler
- All threat mitigations enforced (T-321-04..T-321-09)
- Manual UAT happy path verified
- D-07 toast timeout decision = 5s default (consistency) — rationale committed
</success_criteria>

<output>
After completion, create `.planning/phases/321-assessment-edit-jawaban-peserta/321-03-SUMMARY.md` dengan:
- 7 commit hash + message
- Threat mitigations status (T-321-04..T-321-09) grep-verified
- Manual UAT happy-path observations
- Toast timeout decision (5s default, rationale: cross-event consistency)
- Handoff note ke PLAN 04: Endpoint GET/POST/Preview siap. SignalR handler workerAnswerEdited siap. Modal Activity Log INLINE di AssessmentMonitoringDetail.cshtml:540-559 belum di-modify (PLAN 04 Task 1 refactor jadi tabs Edit History).
</output>
