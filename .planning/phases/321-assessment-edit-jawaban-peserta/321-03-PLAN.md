---
phase: 321-assessment-edit-jawaban-peserta
plan: 03
type: execute
wave: 3
depends_on: [321-01, 321-02]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/EditPesertaAnswers.cshtml
  - wwwroot/js/edit-peserta-answers.js
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-05, EDIT-10]
user_setup: []
must_haves:
  truths:
    - "Admin/HC bisa akses GET /Admin/EditPesertaAnswers/{id} untuk session Completed eligible — Worker dapat 403/redirect login"
    - "View edit page render form per soal MC/MA dengan jawaban current pre-selected + kunci ditandai + reason dropdown verbose (D-05) per question card"
    - "JS dirty state detect ubah jawaban → reveal reason block + asterisk; Lainnya → reveal textarea; client validate reason required; POST PreviewEditScore AJAX → kalau flip Pass↔Fail tampil modal D-06 eksplisit copy → user confirm → form.submit() real"
    - "POST PreviewEditScore return JSON {oldScore, oldIsPassed, newScore, newIsPassed, hasCert, nomorSertifikat, willGenerateCert} TANPA mutate DB"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "GET EditPesertaAnswers + POST PreviewEditScore actions"
      contains: "EditPesertaAnswers"
    - path: "Views/Admin/EditPesertaAnswers.cshtml"
      provides: "Edit form view dengan question cards + reason dropdown verbose + flip modal markup"
      contains: "@model HcPortal.Models.EditPesertaAnswersViewModel"
    - path: "wwwroot/js/edit-peserta-answers.js"
      provides: "Frontend dirty state + reason validation + flip modal AJAX preview"
      contains: "editAnswersForm"
  key_links:
    - from: "Views/Admin/EditPesertaAnswers.cshtml form submit"
      to: "wwwroot/js/edit-peserta-answers.js"
      via: "form id=editAnswersForm + data-preview-url attr"
      pattern: "editAnswersForm"
    - from: "wwwroot/js/edit-peserta-answers.js preview AJAX"
      to: "POST /Admin/PreviewEditScore"
      via: "fetch with FormData + RequestVerificationToken header"
      pattern: "PreviewEditScore"
---

<objective>
PLAN 03 (Wave 3) — Controller GET + View + Frontend JS + PreviewEditScore endpoint.

**Plan split note (user-amendment iteration 1):** PLAN 03 awal punya 7 tasks (GET, View, JS, POST, Preview, Dropdown, SignalR) melebihi context budget 50%. Per user decision di iterasi checker, PLAN 03 di-split jadi:
- **PLAN 03 (sekarang)** — Tasks 1+2+3+4: GET + View + JS + Preview (4 tasks)
- **PLAN 04 (baru)** — POST + Dropdown + SignalR (3 tasks)
- **PLAN 05** (rename dari 04 lama) — Activity Log + UAT + Tag + Merge (3 tasks)

Purpose: Bangun layer presentasi read-only Phase 321 — halaman edit jawaban + endpoint dry-run flip detection. POST mutation dipindah ke PLAN 04 supaya PLAN 03 fokus render + dry-run preview (no DB write). Sequential strict — depends_on PLAN 01 (helper + ViewModels) + PLAN 02 (PreviewScoreAsync).

Output:
- 2 controller actions baru: GET EditPesertaAnswers, POST PreviewEditScore (dry-run)
- 1 view baru: Views/Admin/EditPesertaAnswers.cshtml + flip modal markup
- 1 JS file baru: wwwroot/js/edit-peserta-answers.js (dirty state + flip modal AJAX)

Hand-off ke PLAN 04: POST SubmitEditAnswers (write path), Dropdown ⋮ hybrid di AssessmentMonitoringDetail, SignalR workerAnswerEdited handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-01-SUMMARY.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-02-SUMMARY.md
@Controllers/AssessmentAdminController.cs
@CLAUDE.md

<interfaces>
<!-- Verified codebase signatures 2026-05-21 (RESEARCH refresh corrections) -->

From Controllers/AssessmentAdminController.cs (verified):
- `[Route("Admin/[action]")]` line 19 → URL = `/Admin/{ActionName}/...`
- `protected new ViewResult View(...)` override line 53-57 → resolve ke `~/Views/Admin/{Action}.cshtml`
- DI fields (ctor — TIDAK perlu [FromServices]):
  - `_context` (ApplicationDbContext)
  - `_cache` (IMemoryCache)
  - `_logger` (ILogger<AssessmentAdminController>)
  - `_hubContext` (IHubContext<AssessmentHub>) ← SignalR broadcast (dipakai PLAN 04)
  - `_gradingService` (GradingService) ← PLAN 02 service
- `ExportAssessmentResults` action line 3651 (Phase 320, unchanged) — context untuk locate insertion
- `AssessmentMonitoringDetail` action line 2684 — group new edit actions near related logic

From PLAN 01 + 02 outputs:
- ViewModels: `HcPortal.Models.EditPesertaAnswersViewModel`, `EditQuestionRow`, `EditOptionRow`
- DTOs: `HcPortal.Models.EditAnswersSubmission`, `EditReason`, `EditDraft`, `EditDraftSubmission` (namespace HcPortal.Models flat)
- Helper: `HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session)` + `IsEditableShallow(session)`
- Service: `_gradingService.PreviewScoreAsync(session, overrideAnswers)` returns `(int newScore, bool newIsPassed)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller GET EditPesertaAnswers (Admin/HC gate + IsEditable check + page render)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (ctor, View override line 53-57, ExportAssessmentResults insertion neighborhood line 3651, AssessmentMonitoringDetail line 2684)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 5 (lines 611-753)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md (controller AJAX pattern + dual-response if applicable)
    - Models/UserPackageAssignment.cs (verify GetShuffledQuestionIds method exists)
  </read_first>
  <action>
**Step 1 — Locate insertion point in AssessmentAdminController.cs:**
```bash
grep -n "public.*ExportAssessmentResults\|public.*AssessmentMonitoringDetail" Controllers/AssessmentAdminController.cs
```
Insert new 2 actions (Task 1 GET + Task 4 PreviewEditScore) sebagai 1 contiguous block dekat `AssessmentMonitoringDetail` (~line 2684) — logical grouping monitoring + edit. POST SubmitEditAnswers dari PLAN 04 nanti akan menempati posisi di antara kedua action ini (planner inisialisasi gap).

**Step 2 — Add GET action:**

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> EditPesertaAnswers(int id)
{
    var session = await _context.AssessmentSessions
        .Include(s => s.User)
        .FirstOrDefaultAsync(s => s.Id == id);
    if (session == null)
    {
        TempData["Error"] = "Sesi tidak ditemukan.";
        return RedirectToAction("ManageAssessment");
    }

    if (!await HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session))
    {
        TempData["Error"] = "Sesi ini tidak dapat diedit (status bukan Completed, atau IsManualEntry, atau Assessment Proton Tahun 3).";
        return RedirectToAction("AssessmentMonitoringDetail", new {
            title = session.Title, category = session.Category, scheduleDate = session.Schedule
        });
    }

    var assignment = await _context.UserPackageAssignments
        .FirstOrDefaultAsync(a => a.AssessmentSessionId == id);
    if (assignment == null) return NotFound();

    var shuffledIds = assignment.GetShuffledQuestionIds();
    var questions = await _context.PackageQuestions
        .Include(q => q.Options)
        .Where(q => shuffledIds.Contains(q.Id))
        .ToListAsync();
    var responses = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == id)
        .ToListAsync();

    var rows = new List<HcPortal.Models.EditQuestionRow>();
    foreach (var qId in shuffledIds)
    {
        var q = questions.FirstOrDefault(x => x.Id == qId);
        if (q == null) continue;
        var selectedIds = responses
            .Where(r => r.PackageQuestionId == qId && r.PackageOptionId.HasValue)
            .Select(r => r.PackageOptionId!.Value)
            .ToList();
        var correctIds = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToList();
        bool isCorrect = (q.QuestionType ?? "MultipleChoice") switch
        {
            "MultipleChoice" => selectedIds.Count == 1 && correctIds.Contains(selectedIds[0]),
            "MultipleAnswer" => selectedIds.ToHashSet().SetEquals(correctIds.ToHashSet()),
            _ => false
        };
        rows.Add(new HcPortal.Models.EditQuestionRow
        {
            PackageQuestionId = q.Id,
            QuestionText = q.QuestionText,
            QuestionType = q.QuestionType ?? "MultipleChoice",
            Options = q.Options.Select(o => new HcPortal.Models.EditOptionRow
            {
                Id = o.Id, OptionText = o.OptionText, IsCorrect = o.IsCorrect
            }).ToList(),
            SelectedOptionIds = selectedIds,
            CorrectOptionIds = correctIds,
            IsCurrentCorrect = isCorrect
        });
    }

    var vm = new HcPortal.Models.EditPesertaAnswersViewModel
    {
        Session = session,
        FullName = session.User?.FullName ?? "Unknown",
        NIP = session.User?.NIP ?? "—",
        UpdatedAt = session.UpdatedAt ?? session.CreatedAt,
        Questions = rows
    };
    return View(vm);   // Controller override line 54 resolve ke ~/Views/Admin/EditPesertaAnswers.cshtml
}
```

**Resulting URL** via `[Route("Admin/[action]")]` line 19: `/Admin/EditPesertaAnswers/{id}` (NOT `/AssessmentAdmin/...`).

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — Commit:**
```bash
git add Controllers/AssessmentAdminController.cs
git commit -m "feat(v17.0-p321): GET EditPesertaAnswers controller (Admin/HC gate + IsEditable check)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "EditPesertaAnswers(int id)" Controllers/AssessmentAdminController.cs && grep -q "AssessmentEditEligibility.IsEditableAsync" Controllers/AssessmentAdminController.cs && grep -q "Roles = \"Admin, HC\"" Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - GET action exists with `[Authorize(Roles = "Admin, HC")]` (T-321-04 mitigation)
    - IsEditableAsync called pre-render (T-321-02 mitigation extends to controller layer)
    - Returns View(vm) resolves to Views/Admin/EditPesertaAnswers.cshtml (via override line 54)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): GET EditPesertaAnswers controller`
  </acceptance_criteria>
  <done>GET endpoint accessible at /Admin/EditPesertaAnswers/{id}, gated to Admin/HC, IsEditable enforced, builds clean, committed.</done>
</task>

<task type="auto">
  <name>Task 2: View Views/Admin/EditPesertaAnswers.cshtml + JS stub + flip modal markup</name>
  <files>Views/Admin/EditPesertaAnswers.cshtml, wwwroot/js/edit-peserta-answers.js</files>
  <read_first>
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 6 (lines 757-949)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Copywriting Contract + §Component Inventory Surface 1 + §A11y Contract
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-05 (reason labels verbose) + D-06 (flip modal copy) + D-07 (toast template)
    - Views/Admin/AssessmentMonitoringDetail.cshtml (verify route params untuk Back link asp-route-* binding)
    - Views/_ViewImports.cshtml (verify @using HcPortal.Models declaration)
  </read_first>
  <action>
**CRITICAL — File location: `Views/Admin/EditPesertaAnswers.cshtml`** (NOT `Views/AssessmentAdmin/`). Controller View override line 54 resolves to `~/Views/Admin/`.

**Step 1 — Verify Views/Admin/ folder exist:**
```bash
ls Views/Admin/ | head -5
```
Expected: AssessmentMonitoringDetail.cshtml + others. Kalau folder belum ada (improbable), create dulu.

**Step 2 — Create `Views/Admin/EditPesertaAnswers.cshtml`** (UI-SPEC reconciled — D-05 verbose labels + D-06 flip modal copy + A11y full):

```html
@model HcPortal.Models.EditPesertaAnswersViewModel
@{
    ViewData["Title"] = $"Edit Jawaban — {Model.FullName}";
}

<div class="container-fluid py-3">
  <a asp-action="AssessmentMonitoringDetail"
     asp-route-title="@Model.Session.Title"
     asp-route-category="@Model.Session.Category"
     asp-route-scheduleDate="@Model.Session.Schedule.ToString("yyyy-MM-dd")"
     class="btn btn-sm btn-outline-secondary">← Back to Monitoring</a>

  <h3 class="mt-3">Edit Jawaban — @Model.FullName (NIP @Model.NIP)</h3>

  <div class="card mb-3">
    <div class="card-body small">
      <strong>@Model.Session.Title</strong> · Kategori: @Model.Session.Category
      · Schedule: @Model.Session.Schedule.ToString("dd MMM yyyy HH:mm")<br/>
      Skor saat ini: <strong>@(Model.Session.Score?.ToString() ?? "—")%</strong>
      · Status: <strong>@(Model.Session.IsPassed == true ? "Pass" : Model.Session.IsPassed == false ? "Fail" : "—")</strong>
    </div>
  </div>

  @if (TempData["Error"] != null)
  {
    <div class="alert alert-danger" role="alert">@TempData["Error"]</div>
  }

  <div class="alert alert-warning" role="alert">
    <strong>⚠️ Catatan:</strong> Edit jawaban akan recompute skor + spider chart otomatis.
    Aksi ini di-log audit. Hasil tidak ditampilkan ke peserta.
  </div>

  <form id="editAnswersForm" method="post"
        asp-action="SubmitEditAnswers"
        data-preview-url="@Url.Action("PreviewEditScore", new { sessionId = Model.Session.Id })">
    @Html.AntiForgeryToken()
    <input type="hidden" name="SessionId" value="@Model.Session.Id" />
    <input type="hidden" name="UpdatedAt" value="@Model.UpdatedAt.ToString("O")" />

    @foreach (var (q, idx) in Model.Questions.Select((q, i) => (q, i + 1)))
    {
      <div class="card mb-3 question-card"
           data-question-id="@q.PackageQuestionId"
           data-question-type="@q.QuestionType">
        <div class="card-body">
          <div class="mb-2">
            <strong>Soal @idx</strong>
            <span class="badge bg-secondary">@(q.QuestionType == "MultipleChoice" ? "MC" : q.QuestionType == "MultipleAnswer" ? "MA" : "Essay")</span>
            @if (q.IsCurrentCorrect) { <span class="badge bg-success">✓ Benar</span> }
            else { <span class="badge bg-danger">✗ Salah</span> }
          </div>
          <div class="mb-2">@q.QuestionText</div>

          @if (q.QuestionType == "Essay")
          {
            <div class="text-muted fst-italic">
              Essay – manual grading via halaman Penilaian Essay
            </div>
          }
          else if (q.QuestionType == "MultipleChoice")
          {
            <div class="ms-2">
              @foreach (var opt in q.Options)
              {
                <div class="form-check">
                  <input class="form-check-input answer-input" type="radio"
                         name="Answers[@q.PackageQuestionId]"
                         value="@opt.Id"
                         id="opt-@q.PackageQuestionId-@opt.Id"
                         @(q.SelectedOptionIds.Contains(opt.Id) ? "checked" : "") />
                  <label class="form-check-label" for="opt-@q.PackageQuestionId-@opt.Id">
                    @opt.OptionText
                    @if (opt.IsCorrect) { <span class="badge bg-success ms-2">Kunci</span> }
                  </label>
                </div>
              }
            </div>
          }
          else // MultipleAnswer
          {
            <div class="ms-2">
              @foreach (var opt in q.Options)
              {
                <div class="form-check">
                  <input class="form-check-input answer-input" type="checkbox"
                         name="Answers[@q.PackageQuestionId]"
                         value="@opt.Id"
                         id="opt-@q.PackageQuestionId-@opt.Id"
                         @(q.SelectedOptionIds.Contains(opt.Id) ? "checked" : "") />
                  <label class="form-check-label" for="opt-@q.PackageQuestionId-@opt.Id">
                    @opt.OptionText
                    @if (opt.IsCorrect) { <span class="badge bg-success ms-2">Kunci</span> }
                  </label>
                </div>
              }
            </div>
          }

          @if (q.QuestionType != "Essay")
          {
            <div class="reason-block mt-2 d-none">
              <label class="form-label small" for="reason-code-@q.PackageQuestionId">
                Alasan edit <span class="text-danger" aria-hidden="true">*</span>
              </label>
              <select class="form-select form-select-sm reason-code"
                      id="reason-code-@q.PackageQuestionId"
                      name="Reasons[@q.PackageQuestionId].Code"
                      aria-required="true">
                <option value="">— Pilih —</option>
                @* D-05 verbose labels (UI-SPEC override) *@
                <option value="SoalSalah">Soal salah / typo</option>
                <option value="KunciSalah">Kunci jawaban salah</option>
                <option value="BugSistem">Bug sistem / glitch</option>
                <option value="PermintaanPeserta">Permintaan koreksi peserta</option>
                <option value="Lainnya">Lainnya (jelaskan)</option>
              </select>
              <textarea class="form-control form-control-sm mt-2 reason-text d-none" rows="2"
                        id="reason-text-@q.PackageQuestionId"
                        name="Reasons[@q.PackageQuestionId].Text"
                        placeholder="Detail alasan (wajib kalau pilih Lainnya)"></textarea>
            </div>
          }
        </div>
      </div>
    }

    <div class="d-flex gap-2 mt-3">
      <a asp-action="AssessmentMonitoringDetail"
         asp-route-title="@Model.Session.Title"
         asp-route-category="@Model.Session.Category"
         asp-route-scheduleDate="@Model.Session.Schedule.ToString("yyyy-MM-dd")"
         class="btn btn-outline-secondary">Cancel</a>
      <button type="submit" class="btn btn-primary" id="submitEditBtn">Save & Recompute</button>
    </div>
  </form>
</div>

<!-- Flip confirmation modal (D-06 eksplisit copy) -->
<div class="modal fade" id="flipConfirmModal" tabindex="-1"
     aria-labelledby="flipConfirmModalLabel" aria-describedby="flipModalBody" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="flipConfirmModalLabel">⚠️ Perubahan Hasil</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Tutup"></button>
      </div>
      <div class="modal-body" id="flipModalBody"></div>
      <div class="modal-footer">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Batal</button>
        <button type="button" class="btn btn-primary" id="flipConfirmBtn">Lanjutkan, simpan perubahan</button>
      </div>
    </div>
  </div>
</div>

@section Scripts {
  <script src="~/js/edit-peserta-answers.js" asp-append-version="true"></script>
}
```

**Step 3 — Create JS stub `wwwroot/js/edit-peserta-answers.js`** (full implementation di Task 3):

```javascript
console.log("edit-peserta-answers.js loaded");
```

**Step 4 — Build + smoke UAT initial render:**
```bash
dotnet build
dotnet run
```
Buka `http://localhost:5277/Admin/EditPesertaAnswers/{sessionId}` untuk session Completed valid. Verify:
- Page render tanpa error 500
- Semua soal MC/MA tampil dengan radio/checkbox terisi
- Kunci ditandai badge hijau
- Reason block hidden (d-none) initial
- Modal flipConfirmModal exist di DOM

**Step 5 — Commit:**
```bash
git add Views/Admin/EditPesertaAnswers.cshtml wwwroot/js/edit-peserta-answers.js
git commit -m "feat(v17.0-p321): EditPesertaAnswers.cshtml + JS stub (UI-SPEC D-05/D-06 reconciled + A11y)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && test -f Views/Admin/EditPesertaAnswers.cshtml && grep -q "HcPortal.Models.EditPesertaAnswersViewModel" Views/Admin/EditPesertaAnswers.cshtml && grep -q "Soal salah / typo" Views/Admin/EditPesertaAnswers.cshtml && grep -q "Kunci jawaban salah" Views/Admin/EditPesertaAnswers.cshtml && grep -q "flipConfirmModal" Views/Admin/EditPesertaAnswers.cshtml && grep -q "@Html.AntiForgeryToken" Views/Admin/EditPesertaAnswers.cshtml && test -f wwwroot/js/edit-peserta-answers.js</automated>
  </verify>
  <acceptance_criteria>
    - View file di `Views/Admin/` (NOT `Views/AssessmentAdmin/`)
    - `@model HcPortal.Models.EditPesertaAnswersViewModel` (namespace flat per PLAN 01)
    - D-05 verbose labels present: "Soal salah / typo", "Kunci jawaban salah", "Bug sistem / glitch", "Permintaan koreksi peserta", "Lainnya (jelaskan)"
    - Flip modal markup present dengan id flipConfirmModal + aria-labelledby + aria-describedby
    - `@Html.AntiForgeryToken()` present (T-321-05 mitigation)
    - Hidden inputs SessionId + UpdatedAt present (UpdatedAt format ISO 8601 `"O"`)
    - JS stub created (Task 3 akan replace content)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): EditPesertaAnswers.cshtml`
  </acceptance_criteria>
  <done>Edit page render successfully, D-05/D-06 copy verbose, A11y full, modal markup ready, JS stub linked, committed.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend JS — Dirty state + Reason validation + Flip modal AJAX</name>
  <files>wwwroot/js/edit-peserta-answers.js</files>
  <read_first>
    - wwwroot/js/edit-peserta-answers.js (Task 2 stub)
    - Views/Admin/EditPesertaAnswers.cshtml (Task 2 markup — verify selector class names: .question-card, .answer-input, .reason-block, .reason-code, .reason-text, #editAnswersForm, #submitEditBtn, #flipConfirmModal, #flipModalBody, #flipConfirmBtn)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 7 (lines 953-1149)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md CD-03 (vanilla addEventListener + snapshot compare)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Interaction Contract → Edit Page Flow + §Copywriting Contract (modal copy D-06)
  </read_first>
  <action>
**Step 1 — Replace `wwwroot/js/edit-peserta-answers.js` content** dengan implementation full (D-06 modal copy + a11y focus restore):

```javascript
document.addEventListener("DOMContentLoaded", () => {
  const form = document.getElementById("editAnswersForm");
  if (!form) return;
  const previewUrl = form.dataset.previewUrl;
  const submitBtn = document.getElementById("submitEditBtn");
  const flipModal = new bootstrap.Modal(document.getElementById("flipConfirmModal"));
  const flipBody = document.getElementById("flipModalBody");
  const flipConfirmBtn = document.getElementById("flipConfirmBtn");

  // Snapshot initial answers per question
  const initialState = {};
  document.querySelectorAll(".question-card").forEach(card => {
    const qid = card.dataset.questionId;
    const type = card.dataset.questionType;
    if (type === "Essay") return;
    const checked = Array.from(card.querySelectorAll(".answer-input:checked"))
      .map(i => i.value).sort();
    initialState[qid] = { type, options: checked };
  });

  function getCurrentAnswers(card) {
    const qid = card.dataset.questionId;
    const type = card.dataset.questionType;
    if (type === "Essay") return null;
    const checked = Array.from(card.querySelectorAll(".answer-input:checked"))
      .map(i => i.value).sort();
    return { qid, type, options: checked };
  }

  function isDirty(card) {
    const cur = getCurrentAnswers(card);
    if (!cur) return false;
    const init = initialState[cur.qid];
    if (!init) return false;
    if (cur.options.length !== init.options.length) return true;
    return cur.options.some((v, i) => v !== init.options[i]);
  }

  function updateDirtyUI(card) {
    const reasonBlock = card.querySelector(".reason-block");
    if (!reasonBlock) return;
    if (isDirty(card)) {
      card.classList.add("border-warning");
      reasonBlock.classList.remove("d-none");
    } else {
      card.classList.remove("border-warning");
      reasonBlock.classList.add("d-none");
      const select = reasonBlock.querySelector(".reason-code");
      const textarea = reasonBlock.querySelector(".reason-text");
      if (select) select.value = "";
      if (textarea) { textarea.value = ""; textarea.classList.add("d-none"); }
    }
  }

  document.querySelectorAll(".answer-input").forEach(input => {
    input.addEventListener("change", () => {
      const card = input.closest(".question-card");
      updateDirtyUI(card);
    });
  });

  document.querySelectorAll(".reason-code").forEach(sel => {
    sel.addEventListener("change", () => {
      const block = sel.closest(".reason-block");
      const ta = block.querySelector(".reason-text");
      if (sel.value === "Lainnya") {
        ta.classList.remove("d-none");
      } else {
        ta.classList.add("d-none");
        ta.value = "";
      }
    });
  });

  function collectDiff() {
    const diff = [];
    document.querySelectorAll(".question-card").forEach(card => {
      if (!isDirty(card)) return;
      const qid = card.dataset.questionId;
      const cur = getCurrentAnswers(card);
      const reasonBlock = card.querySelector(".reason-block");
      const code = reasonBlock.querySelector(".reason-code").value;
      const text = reasonBlock.querySelector(".reason-text").value;
      diff.push({ questionId: qid, options: cur.options.map(Number), reasonCode: code, reasonText: text });
    });
    return diff;
  }

  function validateClient(diff) {
    if (diff.length === 0) {
      alert("Tidak ada perubahan untuk disimpan.");
      return false;
    }
    for (const d of diff) {
      if (!d.reasonCode) {
        alert(`Pilih alasan edit terlebih dahulu (Soal #${d.questionId}).`);
        return false;
      }
      if (d.reasonCode === "Lainnya" && !d.reasonText.trim()) {
        alert(`Isi detail alasan untuk opsi Lainnya (Soal #${d.questionId}).`);
        return false;
      }
    }
    return true;
  }

  form.addEventListener("submit", async (e) => {
    e.preventDefault();
    const diff = collectDiff();
    if (!validateClient(diff)) return;

    submitBtn.disabled = true;
    submitBtn.textContent = "Memeriksa...";

    try {
      // Dry-run preview
      const fd = new FormData();
      diff.forEach((d, i) => {
        fd.append(`Drafts[${i}].QuestionId`, d.questionId);
        d.options.forEach(o => fd.append(`Drafts[${i}].Options`, o));
      });
      const resp = await fetch(previewUrl, {
        method: "POST",
        body: fd,
        headers: {
          "RequestVerificationToken": form.querySelector('input[name=__RequestVerificationToken]').value
        }
      });
      if (!resp.ok) throw new Error("Preview gagal");
      const preview = await resp.json();
      const oldPassed = preview.oldIsPassed;
      const newPassed = preview.newIsPassed;
      const flip = (oldPassed === true && newPassed === false) || (oldPassed === false && newPassed === true);

      if (flip) {
        // D-06 eksplisit copy
        let msg;
        if (oldPassed === true && newPassed === false) {
          msg = `Perubahan ini akan <strong>menggagalkan peserta</strong>. ` +
                `NomorSertifikat akan dicabut${preview.nomorSertifikat ? ` (No: ${preview.nomorSertifikat})` : ""} ` +
                `dan TrainingRecord di-set Failed. Lanjutkan?`;
        } else {
          msg = `Perubahan ini akan <strong>meluluskan peserta</strong>. ` +
                (preview.willGenerateCert
                  ? `NomorSertifikat baru akan di-generate (GenerateCertificate && bukan PreTest).`
                  : `Sertifikat TIDAK akan di-generate (session bukan eligible).`) +
                ` Lanjutkan?`;
        }
        flipBody.innerHTML = msg;
        flipConfirmBtn.onclick = () => { flipModal.hide(); form.submit(); };
        flipModal.show();
        submitBtn.disabled = false;
        submitBtn.textContent = "Save & Recompute";
        // A11y: restore focus to submit button after modal close
        document.getElementById('flipConfirmModal').addEventListener('hidden.bs.modal', function once() {
          submitBtn.focus();
          document.getElementById('flipConfirmModal').removeEventListener('hidden.bs.modal', once);
        });
        return;
      }

      // No flip -> submit langsung
      form.submit();
    } catch (err) {
      console.error(err);
      alert("Gagal memeriksa preview: " + err.message);
      submitBtn.disabled = false;
      submitBtn.textContent = "Save & Recompute";
    }
  });
});
```

**Step 2 — UAT manual (browser test):**
```bash
dotnet run
```
Buka edit page:
1. Ubah 1 jawaban MC → card highlight border-warning, reason dropdown muncul.
2. Pilih "Lainnya" → textarea muncul.
3. Switch back ke jawaban asli → highlight + reason hilang.
4. Submit dengan jawaban yang flip Pass→Fail → modal muncul dengan body copy "menggagalkan peserta + NomorSertifikat dicabut".
5. Klik "Batal" → modal close, focus kembali ke submit button.
6. Klik "Lanjutkan, simpan perubahan" → form submit (POST endpoint belum ada di PLAN 03 — expect 404 / "no action found" SAMPAI PLAN 04 Task 1 selesai; UAT step 4-6 lengkap setelah PLAN 04).

**Step 3 — Commit:**
```bash
git add wwwroot/js/edit-peserta-answers.js
git commit -m "feat(v17.0-p321): edit-peserta-answers.js (dirty state + reason validation + D-06 flip modal + a11y focus restore)"
```
  </action>
  <verify>
    <automated>grep -q "editAnswersForm" wwwroot/js/edit-peserta-answers.js && grep -q "previewUrl" wwwroot/js/edit-peserta-answers.js && grep -q "flipConfirmModal" wwwroot/js/edit-peserta-answers.js && grep -q "menggagalkan peserta" wwwroot/js/edit-peserta-answers.js && grep -q "meluluskan peserta" wwwroot/js/edit-peserta-answers.js && grep -q "RequestVerificationToken" wwwroot/js/edit-peserta-answers.js && grep -q "hidden.bs.modal" wwwroot/js/edit-peserta-answers.js</automated>
  </verify>
  <acceptance_criteria>
    - Dirty state detection: snapshot initialState + isDirty() per card via answer-input change listener
    - Reason validation: client-side require code + require text for Lainnya
    - AJAX POST to data-preview-url with RequestVerificationToken header
    - Flip modal body uses D-06 copy: "menggagalkan peserta" / "meluluskan peserta"
    - A11y: focus restore via `hidden.bs.modal` once-listener
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): edit-peserta-answers.js`
  </acceptance_criteria>
  <done>JS implements dirty state + reason validation + AJAX preview + D-06 flip modal copy + a11y focus restore, manual UAT initial flow OK (full submit flow setelah PLAN 04), committed.</done>
</task>

<task type="auto">
  <name>Task 4: Controller POST PreviewEditScore (Dry-run flip detection — JSON contract)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (Task 1 GET action inserted, append POST PreviewEditScore adjacent)
    - Models/EditAnswersSubmission.cs (EditDraft + EditDraftSubmission DTOs from PLAN 01)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 9 (lines 1402-1451)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md (T-321-08 preview contract — no DB write)
  </read_first>
  <action>
**Step 1 — Insert POST PreviewEditScore SETELAH GET EditPesertaAnswers** (Task 1). PLAN 04 Task 1 nanti akan menyisipkan POST SubmitEditAnswers di antara GET + PreviewEditScore (atau adjacent — executor decision):

```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> PreviewEditScore(int sessionId,
    HcPortal.Models.EditDraftSubmission form)
{
    var session = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == sessionId);
    if (session == null) return NotFound();
    if (!await HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session))
        return Forbid();

    var overrideAnswers = form.Drafts.ToDictionary(d => d.QuestionId, d => d.Options);

    // PreviewScoreAsync = PLAN 02 public wrapper (TIDAK mutate DB)
    var (newScore, newIsPassed) = await _gradingService.PreviewScoreAsync(session, overrideAnswers);

    return Json(new
    {
        oldScore = session.Score,
        oldIsPassed = session.IsPassed,
        newScore,
        newIsPassed,
        hasCert = !string.IsNullOrEmpty(session.NomorSertifikat),
        nomorSertifikat = session.NomorSertifikat,
        willGenerateCert = session.GenerateCertificate && session.AssessmentType != "PreTest"
    });
}
```

**Step 2 — Verify TIDAK ada SaveChanges di action body (T-321-08):**
```bash
grep -A 30 "PreviewEditScore" Controllers/AssessmentAdminController.cs | grep -E "SaveChanges|Add\(|Update\(|Remove" | head -5
```
Expected output: kosong (no DB mutation di preview).

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — Smoke test endpoint via DevTools:**
```bash
dotnet run
```
Buka edit page, ubah jawaban, klik Save. Browser DevTools Network tab → verify POST `/Admin/PreviewEditScore?sessionId=...` return Status 200 + JSON shape:
```json
{"oldScore": 65, "oldIsPassed": false, "newScore": 75, "newIsPassed": true, "hasCert": false, "nomorSertifikat": null, "willGenerateCert": true}
```

**Step 5 — Commit:**
```bash
git add Controllers/AssessmentAdminController.cs
git commit -m "feat(v17.0-p321): POST PreviewEditScore endpoint (dry-run flip detection, no DB mutation)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "PreviewEditScore" Controllers/AssessmentAdminController.cs && grep -q "_gradingService.PreviewScoreAsync" Controllers/AssessmentAdminController.cs && grep -q "willGenerateCert" Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - PreviewEditScore action exists with `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`
    - Calls `_gradingService.PreviewScoreAsync` (PLAN 02 public wrapper)
    - JSON response shape exact: `{oldScore, oldIsPassed, newScore, newIsPassed, hasCert, nomorSertifikat, willGenerateCert}`
    - NO `SaveChangesAsync` / `Add()` / `Update()` / `Remove()` di action body (T-321-08 mitigation — dry-run only)
    - IsEditableAsync gate present (Forbid kalau ineligible)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): POST PreviewEditScore`
  </acceptance_criteria>
  <done>Dry-run preview endpoint complete, JSON contract correct, no DB mutation verified, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → controller GET/POST | Untrusted user input crosses here; auth gate + CSRF token + IsEditable required |
| client form (JS) → POST PreviewEditScore | Anti-forgery token + server-side validation reinforces client-side |
| controller PreviewEditScore → service PreviewScoreAsync | Dry-run path; NO DB mutation allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-321-04 | Elevation of Privilege (Auth gating) | GET + PreviewEditScore actions (POST SubmitEditAnswers di PLAN 04) | mitigate | `[Authorize(Roles = "Admin, HC")]` di 2 actions. Worker → 403/redirect login. Verify via Playwright auth-gate test (PLAN 05 Task 2). |
| T-321-05 | Tampering (CSRF) + Information Disclosure (XSS in Lainnya free-text) | POST PreviewEditScore + view form | mitigate | `[ValidateAntiForgeryToken]` di POST PreviewEditScore + `@Html.AntiForgeryToken()` di form (Task 2). XSS: free-text `ReasonText` ditampilkan di Activity Log Edit History (PLAN 05) via Razor `@` auto-encode — JANGAN pakai `@Html.Raw`. Verify PLAN 05 partial gunakan `@log.ReasonText` (encoded), bukan `@Html.Raw(log.ReasonText)`. |
| T-321-08 | Information Disclosure + Tampering (Preview contract violation) | POST PreviewEditScore | mitigate | Action TIDAK pernah call `SaveChangesAsync` / `Add()` / `Update()` / `Remove()`. PreviewScoreAsync PLAN 02 verified pure. Verify grep returns empty. |

</threat_model>

<verification>
- `dotnet build` exits 0 after each task commit
- 4 commit di branch `feature/phase-321-edit-jawaban`
- Smoke UAT: edit page render OK, dirty state + reason validation work, preview AJAX + flip modal show (submit POST mutation diverify di PLAN 04)
- Threat mitigations grep-verified per task
</verification>

<success_criteria>
- 4 task commit di branch
- 2 controller actions baru exist (/Admin/EditPesertaAnswers/{id} GET + POST PreviewEditScore)
- 1 view baru exist (Views/Admin/EditPesertaAnswers.cshtml)
- 1 JS file baru exist (wwwroot/js/edit-peserta-answers.js)
- All threat mitigations enforced (T-321-04, T-321-05, T-321-08)
- Manual UAT initial flow verified (render + dirty state + preview)
- POST SubmitEditAnswers + Dropdown + SignalR dipindah ke PLAN 04
</success_criteria>

<output>
After completion, create `.planning/phases/321-assessment-edit-jawaban-peserta/321-03-SUMMARY.md` dengan:
- 4 commit hash + message
- Threat mitigations status (T-321-04, T-321-05, T-321-08) grep-verified
- Manual UAT happy-path observations (render + dirty + preview)
- Handoff note ke PLAN 04: GET + PreviewEditScore siap. Endpoint POST SubmitEditAnswers BELUM ada (UAT submit Pass↔Fail full di PLAN 04). Dropdown ⋮ + SignalR handler workerAnswerEdited belum diimplementasi.
</output>
</content>
</invoke>