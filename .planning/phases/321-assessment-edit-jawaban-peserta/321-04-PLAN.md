---
phase: 321-assessment-edit-jawaban-peserta
plan: 04
type: execute
wave: 4
depends_on: [321-01, 321-02, 321-03]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - wwwroot/js/assessment-hub.js
autonomous: true
requirements: [EDIT-02, EDIT-03, EDIT-04, EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-12]
user_setup: []
must_haves:
  truths:
    - "POST SubmitEditAnswers wrap transaction: validate eligibility + UpdatedAt concurrency check (±1 detik tolerance) + reason server validation + delete-insert PackageUserResponses + insert AssessmentEditLog per question + call _gradingService.RegradeAfterEditAsync + backfill NewScore/NewIsPassed di log + insert AuditLog + commit + cache invalidate + SignalR broadcast workerAnswerEdited ke monitor-{batchKey} → rollback total kalau exception"
    - "Dropdown ⋮ hybrid di AssessmentMonitoringDetail per-row: View Results + 🕐 inline; Edit Jawaban (conditional IsEditableShallow) + Reset + Akhiri Ujian + Reshuffle di dropdown dengan bi bi-* icons + ARIA + dropdown-menu-end + 40px touch target"
    - "SignalR frontend handler workerAnswerEdited update row score + result cell tanpa refresh + reuse window.showAssessmentToast dengan template D-07 verbose timeout 8 detik (D-07 LOCKED) via optional delayMs param di showAssessmentToast"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "POST SubmitEditAnswers action (transaction + audit + regrade + SignalR broadcast)"
      contains: "SubmitEditAnswers"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Dropdown ⋮ hybrid per-row action column + SignalR workerAnswerEdited handler"
      contains: "workerAnswerEdited"
    - path: "wwwroot/js/assessment-hub.js"
      provides: "showAssessmentToast extended dengan optional delayMs param (default 5000, edit handler pakai 8000 per D-07)"
      contains: "delayMs"
  key_links:
    - from: "Controllers/AssessmentAdminController POST SubmitEditAnswers"
      to: "_gradingService.RegradeAfterEditAsync (PLAN 02)"
      via: "injected field _gradingService"
      pattern: "_gradingService.RegradeAfterEditAsync"
    - from: "Controllers/AssessmentAdminController POST SubmitEditAnswers"
      to: "SignalR _hubContext.Clients.Group(monitor-{batchKey})"
      via: "injected field _hubContext"
      pattern: "workerAnswerEdited"
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml dropdown item Edit Jawaban"
      to: "/Admin/EditPesertaAnswers/{id}"
      via: "asp-action + IsEditableShallow conditional render"
      pattern: "asp-action=\"EditPesertaAnswers\""
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml inline script workerAnswerEdited handler"
      to: "wwwroot/js/assessment-hub.js showAssessmentToast(message, link, linkText, delayMs)"
      via: "window.showAssessmentToast(msg, null, null, 8000)"
      pattern: "showAssessmentToast.*8000"
---

<objective>
PLAN 04 (Wave 4) — POST SubmitEditAnswers (write path) + Dropdown ⋮ hybrid refactor + SignalR workerAnswerEdited handler.

**Plan split note (user-amendment iteration 1):** PLAN ini baru di-create saat checker iteration 1: PLAN 03 awal 7 tasks (GET, View, JS, POST, Preview, Dropdown, SignalR) di-split. Tugas POST + Dropdown + SignalR dipindah ke PLAN 04 supaya tiap plan tetap ≤4 tasks dan ≤50% context budget.

Purpose: Tutup loop write-path Phase 321 — POST mutation dengan transaction wrap + dual audit + cascade regrade + SignalR live broadcast. Refactor action column AssessmentMonitoringDetail menjadi dropdown ⋮ hybrid (UI-SPEC compliant) + tambah SignalR frontend listener workerAnswerEdited.

Sequential strict — depends_on PLAN 01 (helper + ViewModels + AssessmentEditLog) + PLAN 02 (RegradeAfterEditAsync) + PLAN 03 (GET + View + JS + PreviewEditScore).

Output:
- 1 controller action baru: POST SubmitEditAnswers (transaction + audit + regrade + SignalR broadcast)
- AssessmentMonitoringDetail.cshtml modify: dropdown ⋮ hybrid per-row + SignalR workerAnswerEdited inline handler
- wwwroot/js/assessment-hub.js modify: extend showAssessmentToast dengan optional `delayMs` param (default 5000ms backward compatible, edit handler pakai 8000ms per D-07)

**D-07 Toast Timeout (LOCKED per user decision iteration 1):** 8 detik (override planning open question awal yang menyimpulkan 5s consistency). Implementation pakai Option B (minimal-edit, safer): modify `wwwroot/js/assessment-hub.js showAssessmentToast` accept optional `delayMs` param defaulting ke existing 5000ms. Edit handler invoke dengan 8000. Single source of truth + backward compatible untuk semua call site existing yang TIDAK pass param baru.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-PATTERNS.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-01-SUMMARY.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-02-SUMMARY.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-03-SUMMARY.md
@Controllers/AssessmentAdminController.cs
@Views/Admin/AssessmentMonitoringDetail.cshtml
@wwwroot/js/assessment-hub.js
@CLAUDE.md

<interfaces>
<!-- Verified codebase signatures 2026-05-21 (RESEARCH refresh corrections) -->

From Controllers/AssessmentAdminController.cs (verified):
- `[Route("Admin/[action]")]` line 19 → URL = `/Admin/{ActionName}/...`
- `protected new ViewResult View(...)` override line 53-57 → resolve ke `~/Views/Admin/{Action}.cshtml`
- DI fields (ctor — TIDAK perlu [FromServices]):
  - `_context` (ApplicationDbContext)
  - `_cache` (IMemoryCache)
  - `_logger` (ILogger<AssessmentAdminController>)
  - `_hubContext` (IHubContext<AssessmentHub>) ← SignalR broadcast
  - `_gradingService` (GradingService) ← PLAN 02 service
- Verify DI fields exist via grep (line numbers may drift):
  ```bash
  grep -n "_hubContext\|_gradingService\|_cache\|_logger" Controllers/AssessmentAdminController.cs | head -5
  ```

From Views/Admin/AssessmentMonitoringDetail.cshtml (verified):
- Per-user table action column **line 286-338** (Reshuffle 290-296, AkhiriUjian 301-308, Reset 313-320, ViewResults 325-328, ActivityLog 331-337)
- Row field: `session.UserStatus` (NOT `session.Status`) — line 288, 299, 311, 323
- Status string: `"Not started"` (lowercase 's', single space) — line 288
- Modal Activity Log INLINE line 540-559 (PLAN 05 modifies ini untuk Edit History tab)
- SignalR handler block: `window.assessmentHub.on('workerSubmitted', ...)` line ~1244 ← INSERT workerAnswerEdited setelah ini
- Class names existing: `.result-cell` line ~282, `.completedat-cell` line ~284 — `.score-cell` PERLU verify atau tambah

From wwwroot/js/assessment-hub.js (verified 2026-05-21):
- `window.assessmentHub` set line 95 (SignalR connection variable)
- `window.showAssessmentToast = showToast` line 96 — REUSE (D-07 LOCKED 8s)
- `function showToast(message, linkUrl, linkText)` line 9-33 — current 3-arg signature, hardcoded `setTimeout(..., 5000)` line 32
- Edit Task 3: extend signature jadi `function showToast(message, linkUrl, linkText, delayMs)` dengan default `delayMs = 5000` (backward compatible)

From AssessmentHub (verified):
- Group naming: `monitor-{batchKey}` line 57 — `batchKey = "{Title}|{Category}|{Schedule.Date:yyyy-MM-dd}"`

From PLAN 01 + 02 outputs:
- ViewModels: `HcPortal.Models.EditPesertaAnswersViewModel`, `EditQuestionRow`, `EditOptionRow`
- DTOs: `HcPortal.Models.EditAnswersSubmission`, `EditReason`, `EditDraft`, `EditDraftSubmission` (namespace HcPortal.Models flat)
- Helper: `HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session)` + `IsEditableShallow(session)`
- Service: `_gradingService.RegradeAfterEditAsync(session)` returns `(int newScore, bool newIsPassed, int? oldScore, bool? oldIsPassed)`
- Entity: `Models.AssessmentEditLog` dengan QuestionTextSnapshot, OldAnswerJson, OldAnswerTextSnapshot, NewAnswerJson, NewAnswerTextSnapshot, OldScore, NewScore, OldIsPassed, NewIsPassed, ActorUserId, ActorName, ActorRole, ReasonCode, ReasonText, EditedAt

From PLAN 03 outputs:
- Controller actions: GET EditPesertaAnswers (line ~2700+ baru) + POST PreviewEditScore (adjacent)
- View: Views/Admin/EditPesertaAnswers.cshtml dengan form id=editAnswersForm + hidden SessionId + hidden UpdatedAt (ISO 8601 "O")
- JS: wwwroot/js/edit-peserta-answers.js submit handler → form.submit() setelah preview/flip modal confirm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller POST SubmitEditAnswers (Transaction + Audit + Regrade + SignalR broadcast)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (PLAN 03 Task 1 GET + Task 4 PreviewEditScore inserted; insert POST SubmitEditAnswers adjacent — antara GET dan PreviewEditScore)
    - Models/EditAnswersSubmission.cs (PLAN 01 DTO: SessionId, UpdatedAt, Answers, Reasons)
    - Models/AuditLog.cs (ActionType, EntityType, EntityId, UserId, Description, CreatedAt fields)
    - Models/AssessmentEditLog.cs (PLAN 01 — all snapshot fields)
    - Hubs/AssessmentHub.cs (verify group naming `monitor-{batchKey}` line ~57)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 8 (lines 1153-1398)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md (T-321-05 CSRF, T-321-06 tx, T-321-07 concurrency)
  </read_first>
  <action>
**Pre-step — Verify DI fields ada di ctor (line numbers drift after PLAN 03 inserts):**
```bash
grep -n "_hubContext\|_gradingService\|_cache\|_logger" Controllers/AssessmentAdminController.cs | head -5
```
Confirm semua 4 field (`_context`, `_cache`, `_logger`, `_hubContext`, `_gradingService`) terdeklarasi di ctor. Tidak perlu line number — grep nama field cukup.

**Step 1 — Insert POST action ADJACENT ke GET EditPesertaAnswers** (PLAN 03 Task 1). Lokasi: setelah GET, sebelum PreviewEditScore (atau adjacent — executor judgment) di `Controllers/AssessmentAdminController.cs`:

```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> SubmitEditAnswers(HcPortal.Models.EditAnswersSubmission form)
{
    var session = await _context.AssessmentSessions
        .Include(s => s.User)
        .FirstOrDefaultAsync(s => s.Id == form.SessionId);
    if (session == null) { TempData["Error"] = "Sesi tidak ditemukan."; return RedirectToAction("ManageAssessment"); }

    var redirectBack = RedirectToAction("AssessmentMonitoringDetail", new {
        title = session.Title, category = session.Category, scheduleDate = session.Schedule
    });

    // Eligibility (T-321-02)
    if (!await HcPortal.Helpers.AssessmentEditEligibility.IsEditableAsync(_context, session))
    {
        TempData["Error"] = "Sesi tidak dapat di-edit.";
        return redirectBack;
    }

    // Concurrency check (T-321-07) — tolerance 1 detik karena DateTime round-trip ISO 8601 "O"
    var currentUpdatedAt = session.UpdatedAt ?? session.CreatedAt;
    if (Math.Abs((currentUpdatedAt - form.UpdatedAt).TotalSeconds) > 1)
    {
        TempData["Error"] = "Sesi sudah diubah admin lain. Refresh halaman.";
        return redirectBack;
    }

    // Validate reasons (server-side, T-321-05 server validation in addition to client)
    foreach (var (qid, _) in form.Answers)
    {
        if (!form.Reasons.TryGetValue(qid, out var reason) || string.IsNullOrWhiteSpace(reason.Code))
        {
            TempData["Error"] = $"Alasan wajib untuk soal {qid}.";
            return redirectBack;
        }
        if (!new[] { "SoalSalah", "KunciSalah", "BugSistem", "PermintaanPeserta", "Lainnya" }.Contains(reason.Code))
        {
            TempData["Error"] = "ReasonCode tidak valid.";
            return redirectBack;
        }
        if (reason.Code == "Lainnya" && string.IsNullOrWhiteSpace(reason.Text))
        {
            TempData["Error"] = "ReasonText wajib kalau ReasonCode == Lainnya.";
            return redirectBack;
        }
    }

    var actorId = User.FindFirstValue(System.Security.Claims.ClaimTypes.NameIdentifier) ?? "";
    var actor = await _context.Users.FirstOrDefaultAsync(u => u.Id == actorId);
    var actorRole = User.IsInRole("Admin") ? "Admin" : "HC";
    var actorName = $"{actor?.NIP} - {actor?.FullName}";

    // Load questions+options + existing responses
    var qIds = form.Answers.Keys.ToList();
    var questions = await _context.PackageQuestions
        .Include(q => q.Options)
        .Where(q => qIds.Contains(q.Id))
        .ToListAsync();
    var existingResponses = await _context.PackageUserResponses
        .Where(r => r.AssessmentSessionId == session.Id && qIds.Contains(r.PackageQuestionId))
        .ToListAsync();

    using var tx = await _context.Database.BeginTransactionAsync();
    try
    {
        int editCount = 0;
        foreach (var (qId, newOptionIds) in form.Answers)
        {
            var q = questions.FirstOrDefault(x => x.Id == qId);
            if (q == null) continue;
            if ((q.QuestionType ?? "MultipleChoice") == "Essay") continue;

            var validOptionIds = q.Options.Select(o => o.Id).ToHashSet();
            var sanitizedNew = newOptionIds.Where(id => validOptionIds.Contains(id)).ToList();

            // Snapshot OLD
            var oldResponses = existingResponses
                .Where(r => r.PackageQuestionId == qId && r.PackageOptionId.HasValue).ToList();
            var oldOptionIds = oldResponses.Select(r => r.PackageOptionId!.Value).ToList();
            var oldTextSnapshot = string.Join(", ",
                q.Options.Where(o => oldOptionIds.Contains(o.Id)).Select(o => o.OptionText));

            // Apply edit: MA pattern = delete-all + insert-new
            _context.PackageUserResponses.RemoveRange(oldResponses);
            foreach (var newOid in sanitizedNew)
            {
                _context.PackageUserResponses.Add(new PackageUserResponse
                {
                    AssessmentSessionId = session.Id,
                    PackageQuestionId = qId,
                    PackageOptionId = newOid,
                    TextAnswer = null,
                    SubmittedAt = DateTime.UtcNow
                });
            }

            // Snapshot NEW
            var newTextSnapshot = string.Join(", ",
                q.Options.Where(o => sanitizedNew.Contains(o.Id)).Select(o => o.OptionText));

            // Insert AssessmentEditLog (NewScore/NewIsPassed diisi setelah regrade)
            _context.AssessmentEditLogs.Add(new AssessmentEditLog
            {
                AssessmentSessionId = session.Id,
                PackageQuestionId = qId,
                QuestionTextSnapshot = q.QuestionText ?? "",
                OldAnswerJson = System.Text.Json.JsonSerializer.Serialize(oldOptionIds),
                OldAnswerTextSnapshot = oldTextSnapshot,
                NewAnswerJson = System.Text.Json.JsonSerializer.Serialize(sanitizedNew),
                NewAnswerTextSnapshot = newTextSnapshot,
                OldScore = session.Score,
                OldIsPassed = session.IsPassed,
                ActorUserId = actorId,
                ActorName = actorName,
                ActorRole = actorRole,
                ReasonCode = form.Reasons[qId].Code,
                ReasonText = form.Reasons[qId].Text,
                EditedAt = DateTime.UtcNow
            });
            editCount++;
        }
        await _context.SaveChangesAsync();

        // Recompute via PLAN 02 service
        var (newScore, newIsPassed, oldScore, oldIsPassed) = await _gradingService.RegradeAfterEditAsync(session);

        // Backfill NewScore/NewIsPassed di AssessmentEditLog entries baru
        await _context.AssessmentEditLogs
            .Where(l => l.AssessmentSessionId == session.Id && l.NewScore == null && l.ActorUserId == actorId)
            .ExecuteUpdateAsync(l => l
                .SetProperty(x => x.NewScore, newScore)
                .SetProperty(x => x.NewIsPassed, newIsPassed));

        // AuditLog generic dual-write
        _context.AuditLogs.Add(new AuditLog
        {
            ActionType = "EditAssessmentAnswer",
            EntityType = "AssessmentSession",
            EntityId = session.Id.ToString(),
            UserId = actorId,
            Description = $"Edit {editCount} jawaban session #{session.Id} ({session.User?.FullName}), score {oldScore?.ToString() ?? "—"} → {newScore}",
            CreatedAt = DateTime.UtcNow
        });
        await _context.SaveChangesAsync();

        await tx.CommitAsync();

        // Cache invalidate (kalau ada key exam-status)
        _cache.Remove($"exam-status-{session.Id}");

        // SignalR broadcast via injected _hubContext (field, NOT [FromServices])
        var batchKey = $"{session.Title}|{session.Category}|{session.Schedule.Date:yyyy-MM-dd}";
        await _hubContext.Clients.Group($"monitor-{batchKey}").SendAsync("workerAnswerEdited", new
        {
            sessionId = session.Id,
            workerName = session.User?.FullName ?? "Unknown",
            oldScore, newScore, oldIsPassed, newIsPassed,
            actorName, actorRole
        });

        string flip = (oldIsPassed == true, newIsPassed) switch
        {
            (true, false) => "Pass→Fail",
            (false, true) => "Fail→Pass",
            (true, true) => "Pass→Pass",
            _ => "Fail→Fail"
        };
        TempData["Success"] = $"Edit {editCount} jawaban berhasil. Score: {oldScore?.ToString() ?? "—"} → {newScore}, {flip}";
        return redirectBack;
    }
    catch (Exception ex)
    {
        await tx.RollbackAsync();
        _logger.LogError(ex, "Edit jawaban gagal untuk session {SessionId}", session.Id);
        TempData["Error"] = "Terjadi kesalahan saat menyimpan. Coba lagi atau hubungi administrator.";
        return redirectBack;
    }
}
```

**Step 2 — Build:**
```bash
dotnet build
```

**Step 3 — Smoke UAT happy-path:**
```bash
dotnet run
```
Browser:
- Buka `/Admin/EditPesertaAnswers/{completedSessionId}`
- Ubah 1 jawaban MC → pilih reason SoalSalah
- Klik Save → preview AJAX (PLAN 03 Task 4) → kalau flip → modal → Lanjutkan → form submit
- Verify redirect ke AssessmentMonitoringDetail dengan TempData["Success"]
- Verify DB: `SELECT COUNT(*) FROM AssessmentEditLogs WHERE AssessmentSessionId={id}` ++, `SELECT Score FROM AssessmentSessions WHERE Id={id}` berubah

**Step 4 — Commit:**
```bash
git add Controllers/AssessmentAdminController.cs
git commit -m "feat(v17.0-p321): POST SubmitEditAnswers (tx + audit + regrade + SignalR workerAnswerEdited)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "SubmitEditAnswers" Controllers/AssessmentAdminController.cs && grep -q "ValidateAntiForgeryToken" Controllers/AssessmentAdminController.cs && grep -q "BeginTransactionAsync" Controllers/AssessmentAdminController.cs && grep -q "_gradingService.RegradeAfterEditAsync" Controllers/AssessmentAdminController.cs && grep -q "workerAnswerEdited" Controllers/AssessmentAdminController.cs && grep -q "Sesi sudah diubah admin lain" Controllers/AssessmentAdminController.cs && grep -q "monitor-" Controllers/AssessmentAdminController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - POST action exists with `[ValidateAntiForgeryToken]` (T-321-05) + `[Authorize(Roles = "Admin, HC")]` (T-321-04)
    - `BeginTransactionAsync` + try/catch + RollbackAsync present (T-321-06 transaction integrity)
    - Concurrency token compare with ±1s tolerance (T-321-07)
    - Server-side reason validation: enum whitelist + Lainnya requires text
    - Dual audit: AssessmentEditLog per question + AuditLog generic ActionType="EditAssessmentAnswer"
    - SignalR broadcast `Clients.Group($"monitor-{batchKey}").SendAsync("workerAnswerEdited", ...)`
    - Backfill NewScore/NewIsPassed via ExecuteUpdateAsync after RegradeAfterEditAsync
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): POST SubmitEditAnswers`
  </acceptance_criteria>
  <done>POST endpoint complete dengan tx wrap + dual audit + regrade + SignalR broadcast, builds clean, smoke UAT happy-path OK, committed.</done>
</task>

<task type="auto">
  <name>Task 2: View AssessmentMonitoringDetail.cshtml — Dropdown ⋮ hybrid refactor (UI-SPEC reconciled)</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (VERIFY action column block line 286-338)
    - Verify field `session.UserStatus` di line 288, 299, 311, 323
    - Verify status string `"Not started"` (lowercase s) di line 288
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 10 (lines 1455-1579)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Component Inventory Surface 2 + §Dropdown ⋮ items + §A11y Contract Dropdown
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md (T-321-09 UI gating server-side)
  </read_first>
  <action>
**Step 1 — Re-verify action column location:**
```bash
grep -nE "Reshuffle|AkhiriUjian|btn-activity-log|reshuffleWorker" Views/Admin/AssessmentMonitoringDetail.cshtml | head -10
```

**Step 2 — Replace action column block (line ~286-338)** dengan hybrid layout (preserve `Model.IsPackageMode`, `session.UserStatus`, `reshuffleWorker(@session.Id)`):

```html
@{ var canEdit = HcPortal.Helpers.AssessmentEditEligibility.IsEditableShallow(session); }

<td>
  <div class="d-flex gap-1 align-items-center">
    @* Inline: View Results (Completed only) *@
    @if (session.UserStatus == "Completed")
    {
      <a href="@Url.Action("Results", "CMP", new { id = session.Id })" target="_blank"
         class="btn btn-success btn-sm">View Results</a>
    }
    @* Inline: Activity Log (always) *@
    <button type="button"
            class="btn btn-outline-secondary btn-sm btn-activity-log"
            data-session-id="@session.Id"
            data-worker-name="@session.UserFullName"
            title="Lihat activity log">
      <i class="bi bi-clock-history"></i>
    </button>

    @* Dropdown ⋮ *@
    <div class="dropdown">
      <button class="btn btn-sm btn-outline-secondary"
              type="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
              aria-haspopup="true"
              aria-label="Aksi lain untuk @session.UserFullName"
              style="min-height:40px;">⋮</button>
      <ul class="dropdown-menu dropdown-menu-end">
        @if (canEdit)
        {
          <li>
            <a class="dropdown-item" asp-action="EditPesertaAnswers" asp-controller="AssessmentAdmin" asp-route-id="@session.Id">
              <i class="bi bi-pencil-square me-1"></i>Edit Jawaban
            </a>
          </li>
        }
        @if (session.UserStatus != "Cancelled")
        {
          <li>
            <form asp-action="ResetAssessment" asp-controller="AssessmentAdmin" method="post" class="m-0"
                  onsubmit="return confirm('Reset sesi ini? Semua jawaban akan dihapus dan peserta dapat mengulang ujian.')">
              @Html.AntiForgeryToken()
              <input type="hidden" name="id" value="@session.Id" />
              <button type="submit" class="dropdown-item">
                <i class="bi bi-arrow-counterclockwise me-1"></i>Reset
              </button>
            </form>
          </li>
        }
        @if (session.UserStatus == "InProgress")
        {
          <li>
            <form asp-action="AkhiriUjian" asp-controller="AssessmentAdmin" method="post" class="m-0"
                  onsubmit="return confirm('Akhiri ujian untuk @(session.UserFullName)? Jawaban tersimpan akan dinilai otomatis.')">
              @Html.AntiForgeryToken()
              <input type="hidden" name="id" value="@session.Id" />
              <button type="submit" class="dropdown-item text-danger">
                <i class="bi bi-x-octagon me-1"></i>Akhiri Ujian
              </button>
            </form>
          </li>
        }
        @if (Model.IsPackageMode && (session.UserStatus == "Not started" || session.UserStatus == "Abandoned"))
        {
          <li>
            <button type="button"
                    class="dropdown-item"
                    data-session-id="@session.Id"
                    onclick="reshuffleWorker(@session.Id)">
              <i class="bi bi-shuffle me-1"></i>Reshuffle
            </button>
          </li>
        }
      </ul>
    </div>
  </div>
</td>
```

> **UI-SPEC reconciled** (override RESEARCH lama emoji):
> - `bi bi-*` icons — UI-SPEC line 159
> - `aria-label="Aksi lain untuk {workerName}"` + `aria-haspopup="true"` + `dropdown-menu-end`
> - Touch target 40px via `style="min-height:40px"`
> - Status string `"Not started"` (lowercase 's') — verified line 288
> - Existing handlers PRESERVED: form POST + onsubmit confirm dialog + reshuffleWorker JS

**Step 3 — Build:**
```bash
dotnet build
```

**Step 4 — UAT manual (browser):**
Buka monitoring detail page. Per row verify View Results + 🕐 inline; ⋮ menu dropdown-menu-end + ARIA; Edit Jawaban conditional render; ESC close; mobile auto-flip.

**Step 5 — Commit:**
```bash
git add Views/Admin/AssessmentMonitoringDetail.cshtml
git commit -m "feat(v17.0-p321): per-user action dropdown ⋮ hybrid (bi-icons + ARIA + IsEditableShallow gating)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "AssessmentEditEligibility.IsEditableShallow" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "dropdown-menu-end" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "bi-pencil-square" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "Aksi lain untuk" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "asp-action=\"EditPesertaAnswers\"" Views/Admin/AssessmentMonitoringDetail.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - `IsEditableShallow(session)` called for `canEdit` conditional render (T-321-09 mitigation — server-side gating)
    - Dropdown uses `bi bi-*` icons — NOT emoji
    - ARIA: `aria-label="Aksi lain untuk ..."` + `aria-haspopup="true"` + `dropdown-menu-end`
    - Touch target: `style="min-height:40px"` on toggle
    - Conditional Edit Jawaban hanya saat canEdit==true
    - Existing handlers preserved (form POST + onsubmit confirm + reshuffleWorker)
    - Status string verified `"Not started"` (lowercase 's')
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): per-user action dropdown ⋮ hybrid`
  </acceptance_criteria>
  <done>Dropdown ⋮ hybrid render correct, IsEditable conditional UI gating, ARIA + touch target compliant, committed.</done>
</task>

<task type="auto">
  <name>Task 3: SignalR Frontend Handler workerAnswerEdited + Toast delayMs Extension (D-07 LOCKED 8s)</name>
  <files>wwwroot/js/assessment-hub.js, Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - wwwroot/js/assessment-hub.js (verify line 9-33 showToast 3-arg signature dengan hardcoded 5000ms timeout line 32, line 95 hub connection variable, line 96 window.showAssessmentToast = showToast export)
    - Views/Admin/AssessmentMonitoringDetail.cshtml (scripts section — search `window.assessmentHub.on('workerSubmitted'` ~line 1244)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 11 (lines 1583-1659)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-07 (toast template verbose + 8 detik LOCKED)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Toast SignalR
  </read_first>
  <action>
**D-07 LOCKED PRESERVATION (user-amendment iteration 1):** UI-SPEC + CONTEXT D-07 spec 8 detik. Implementation pakai **Option B (minimal-edit, safer)** — modify `wwwroot/js/assessment-hub.js showAssessmentToast` accept optional `delayMs` param defaulting ke existing 5000ms. Edit handler invoke dengan 8000. Single source of truth + backward compatible untuk SEMUA call site existing yang TIDAK pass param baru.

**Note:** "D-07 8s preserved per CONTEXT, wrapper minimal-invasive backward compatible — semua existing call site (`showAssessmentToast('Koneksi terputus...')`, `showAssessmentToast('Koneksi pulih')`, `showAssessmentToast('Sesi login habis...', url, 'Login')`) tetap pakai default 5000ms tanpa perubahan."

---

**Step 1 — Modify `wwwroot/js/assessment-hub.js` showToast signature** (line ~9-33). Replace existing `function showToast(message, linkUrl, linkText)` block dengan signature 4-arg + default:

```javascript
function showToast(message, linkUrl, linkText, delayMs) {
    var el = document.createElement('div');
    el.className = 'assessment-toast';
    el.textContent = message;

    if (linkUrl) {
        var a = document.createElement('a');
        a.href = linkUrl;
        a.textContent = linkText || 'Klik di sini';
        el.appendChild(a);
    }

    document.body.appendChild(el);

    requestAnimationFrame(function () {
        el.classList.add('visible');
    });

    // delayMs default 5000ms (D-07 LOCKED 8s untuk edit event — caller invoke dgn 8000)
    var timeout = (typeof delayMs === 'number' && delayMs > 0) ? delayMs : 5000;
    setTimeout(function () {
        el.classList.remove('visible');
        setTimeout(function () {
            if (el.parentNode) el.parentNode.removeChild(el);
        }, 350);
    }, timeout);
}
```

**Step 2 — Verify SignalR handler insertion point di MonitoringDetail.cshtml:**
```bash
grep -n "window.assessmentHub.on" Views/Admin/AssessmentMonitoringDetail.cshtml | head -5
```

**Step 3 — Verify selector class names existing:**
```bash
grep -nE 'class="(score|result|completedat)-cell|data-session-id' Views/Admin/AssessmentMonitoringDetail.cshtml | head -10
```
Adjust handler kalau class actual berbeda. RESEARCH verified `.result-cell` line ~282, `.completedat-cell` line ~284. **`.score-cell` perlu confirm** — kalau tidak ada class, tambah `class="score-cell"` di markup row template.

**Step 4 — Insert handler block** setelah block `window.assessmentHub.on('workerSubmitted', ...)`:

```javascript
// workerAnswerEdited: admin/HC edited a session's answers (Phase 321 EDIT-09)
window.assessmentHub.on('workerAnswerEdited', function (data) {
    // Update row score + result cell tanpa full reload
    var row = document.querySelector('tr[data-session-id="' + data.sessionId + '"]');
    if (row) {
        var scoreCell = row.querySelector('.score-cell');
        var resultCell = row.querySelector('.result-cell');
        if (scoreCell) scoreCell.textContent = data.newScore + '%';
        if (resultCell) {
            resultCell.textContent = data.newIsPassed ? 'Pass' : 'Fail';
            resultCell.className = 'result-cell ' + (data.newIsPassed ? 'text-success' : 'text-danger');
        }
    }

    // D-07 toast verbose template (8 detik per D-07 LOCKED)
    var oldResult = (data.oldIsPassed === true) ? 'Pass' : (data.oldIsPassed === false) ? 'Fail' : '—';
    var newResult = data.newIsPassed ? 'Pass' : 'Fail';
    var flipSegment = (data.oldIsPassed !== data.newIsPassed)
        ? ', ' + oldResult + '→' + newResult
        : '';
    var msg = data.actorRole + ' ' + data.actorName +
              ' edit jawaban ' + data.workerName +
              ': Score ' + (data.oldScore !== null && data.oldScore !== undefined ? data.oldScore : '—') + '→' + data.newScore +
              flipSegment;
    // D-07 LOCKED 8000ms (override default 5000ms via delayMs param)
    if (typeof window.showAssessmentToast === 'function') {
        window.showAssessmentToast(msg, null, null, 8000);
    } else {
        console.log('[toast]', msg);
    }
});
```

**Step 5 — Kalau `.score-cell` tidak ada, tambah `class="score-cell"` ke `<td>` row template area.**

**Step 6 — Build:**
```bash
dotnet build
```

**Step 7 — UAT 2-tab cross-tab live update:**
Buka monitoring detail di 2 tab browser (sama session group). Di tab 1 (Admin), edit jawaban → save sukses → verify di tab 2: row score+result cell update otomatis tanpa reload + toast verbose D-07 muncul "Admin {Name} edit jawaban {Worker}: Score 65→75, Fail→Pass". **Verify timeout 8 detik** (toast tidak hilang sebelum 8s).

**Step 8 — Commit:**
```bash
git add wwwroot/js/assessment-hub.js Views/Admin/AssessmentMonitoringDetail.cshtml
git commit -m "feat(v17.0-p321): SignalR workerAnswerEdited handler + showAssessmentToast delayMs (D-07 8s LOCKED)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && grep -q "delayMs" wwwroot/js/assessment-hub.js && grep -q "workerAnswerEdited" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "showAssessmentToast.*8000" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "edit jawaban" Views/Admin/AssessmentMonitoringDetail.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - `wwwroot/js/assessment-hub.js showToast` accept 4-arg `(message, linkUrl, linkText, delayMs)` dengan default 5000ms (backward compatible)
    - Handler `window.assessmentHub.on('workerAnswerEdited', ...)` present di AssessmentMonitoringDetail.cshtml
    - Edit handler invoke `window.showAssessmentToast(msg, null, null, 8000)` — D-07 LOCKED 8s
    - Toast template D-07 verbose: `{actorRole} {actorName} edit jawaban {workerName}: Score {old}→{new}{, flip?}`
    - Row update via selectors `tr[data-session-id]`, `.score-cell`, `.result-cell` (score-cell added kalau missing)
    - Result cell class swap text-success/text-danger
    - Manual UAT cross-tab live update verified (2-tab observe + 8s timeout)
    - Backward compatibility: existing `showAssessmentToast(...)` 3-arg call sites di assessment-hub.js (`onreconnecting`, `onreconnected`, `onclose`) tidak break — pakai default 5000ms
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): SignalR workerAnswerEdited handler`
  </acceptance_criteria>
  <done>SignalR handler integrated, showAssessmentToast extended dengan delayMs backward-compatible, D-07 8s LOCKED preserved, cross-tab UAT pass, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → controller POST SubmitEditAnswers | Untrusted user input crosses here; auth gate + CSRF token + IsEditable + UpdatedAt token required |
| controller POST → DB write (PackageUserResponses + AssessmentEditLogs + AuditLogs + AssessmentSessions + TrainingRecords) | Multi-table write must be atomic via transaction |
| controller POST → SignalR broadcast `monitor-{batchKey}` | Broadcast payload visible to all admins in group; no PII risk (worker name + score already visible) |
| view dropdown ⋮ render → IsEditableShallow | Server-side conditional render prevents client-only bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-321-06 | Tampering (Transaction integrity) | POST SubmitEditAnswers | mitigate | `using var tx = await _context.Database.BeginTransactionAsync()` + try/catch + `await tx.RollbackAsync()` di catch block + `await tx.CommitAsync()` di happy path. Multi-table write atomic. Exception apa pun → rollback total. |
| T-321-07 | Concurrency (Lost-update race) | POST SubmitEditAnswers | mitigate | `UpdatedAt` hidden field ISO 8601 `"O"` round-trip + server compare `Math.Abs((current - form.UpdatedAt).TotalSeconds) > 1` (1s tolerance). Stale → TempData error + redirect. Service PLAN 02 ExecuteUpdateAsync status guard tambahan layer. Playwright concurrency test di PLAN 05 Task 2. |
| T-321-09 | Elevation of Privilege (UI dropdown bypass) | AssessmentMonitoringDetail.cshtml dropdown item Edit Jawaban | mitigate | Conditional render server-side via `IsEditableShallow(session)` Razor `@if (canEdit)` — bukan JS hide. Klien yang tweak HTML via DevTools tetap kena Server-side IsEditableAsync di GET endpoint (T-321-04 + T-321-02). Defense in depth. |

</threat_model>

<verification>
- `dotnet build` exits 0 after each task commit
- 3 commit di branch `feature/phase-321-edit-jawaban`
- Smoke UAT: full submit flow Pass↔Fail flip verified via browser + sqlcmd
- SignalR cross-tab live update verified manual (2-tab observe + 8s toast timeout)
- Threat mitigations grep-verified per task (T-321-06, T-321-07, T-321-09)
- Backward compatibility assessment-hub.js call sites verified (default 5000ms preserved)
</verification>

<success_criteria>
- 3 task commit di branch
- 1 controller action baru exist (POST SubmitEditAnswers)
- Views/Admin/AssessmentMonitoringDetail.cshtml modified: dropdown ⋮ hybrid + SignalR handler
- wwwroot/js/assessment-hub.js modified: showAssessmentToast accept optional delayMs (backward compatible)
- All threat mitigations enforced (T-321-06, T-321-07, T-321-09)
- D-07 8s LOCKED preserved (edit handler invoke 8000ms, default 5000ms backward compat)
- Manual UAT happy path + cross-tab live update verified
</success_criteria>

<output>
After completion, create `.planning/phases/321-assessment-edit-jawaban-peserta/321-04-SUMMARY.md` dengan:
- 3 commit hash + message
- Threat mitigations status (T-321-06, T-321-07, T-321-09) grep-verified
- Manual UAT happy-path observations (full Pass↔Fail flip flow + cross-tab)
- D-07 8s LOCKED preservation confirmation (delayMs param implementation + backward compat)
- Handoff note ke PLAN 05: Endpoint POST + Dropdown + SignalR handler siap. Activity Log Edit History tab + Playwright spec + Manual UAT + Tag + Merge belum.
</output>
</content>
