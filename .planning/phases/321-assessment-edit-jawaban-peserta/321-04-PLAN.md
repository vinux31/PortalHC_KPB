---
phase: 321-assessment-edit-jawaban-peserta
plan: 04
type: execute
wave: 4
depends_on: [321-01, 321-02, 321-03]
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/Admin/_EditHistoryPartial.cshtml
  - Controllers/AssessmentAdminController.cs
  - tests/e2e/edit-peserta-answers.spec.ts
autonomous: false
requirements: [EDIT-04, EDIT-07, EDIT-09, EDIT-11, EDIT-13]
user_setup: []
must_haves:
  truths:
    - "Modal Activity Log di AssessmentMonitoringDetail.cshtml di-refactor jadi 2 nav-tabs: Activity Timeline (existing content preserved) + Edit History (lazy-load partial via shown.bs.tab)"
    - "GET /Admin/EditHistoryPartial?sessionId={id} return PartialView _EditHistoryPartial dengan AssessmentEditLog filter SessionId sort EditedAt DESC + reason label verbose D-05 mapper"
    - "Lazy-load JS cache via dataset.loaded='1' attr, reset saat user switch ke session lain (btn-activity-log click handler)"
    - "Playwright spec tests/e2e/edit-peserta-answers.spec.ts cover 4 test: auth-gate, happy-path, concurrency-stale, flip-preview-AJAX"
    - "Manual UAT 4 item lulus: DB cascade flip (sqlcmd), SignalR 2-tab live update, Activity Log Edit History tab content, Migration rollback re-verify"
    - "Branch feature/phase-321-edit-jawaban di-merge ke main via git merge --no-ff (CD-07 default preserve task granularity)"
    - "Tag v17.0-p321-complete di-buat + push remote + IT notify final dengan commit hash + tag + flag MIGRATION ADA"
  artifacts:
    - path: "Views/Admin/_EditHistoryPartial.cshtml"
      provides: "Partial view tab content Activity Log Edit History dengan reason label mapper D-05"
      contains: "@model List<HcPortal.Models.AssessmentEditLog>"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Modal body refactor 2-tab + lazy-load JS handler"
      contains: "tab-edit-history"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "GET EditHistoryPartial action returning PartialView"
      contains: "EditHistoryPartial"
    - path: "tests/e2e/edit-peserta-answers.spec.ts"
      provides: "Playwright spec 4 test (auth-gate, happy-path, concurrency-stale, flip-preview-AJAX)"
      contains: "edit-peserta-answers"
  key_links:
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml tab Edit History button"
      to: "GET /Admin/EditHistoryPartial?sessionId={id}"
      via: "shown.bs.tab event + fetch + dataset.loaded cache"
      pattern: "EditHistoryPartial"
    - from: "Controllers EditHistoryPartial action"
      to: "Views/Admin/_EditHistoryPartial.cshtml"
      via: "PartialView (controller View override line 57 resolve)"
      pattern: "PartialView\\(\"_EditHistoryPartial\""
    - from: "tests/e2e/edit-peserta-answers.spec.ts"
      to: "tests/e2e/fixtures/auth.ts + accounts.ts (reuse existing)"
      via: "Playwright fixture import"
      pattern: "import.*from.*fixtures"
---

<objective>
PLAN 04 (Wave 4) — Activity Log Edit History tab + Playwright spec + manual UAT + tag + IT handoff.

Purpose: Closing phase 321 — UI tab Edit History untuk audit visibility, 4 Playwright test coverage, 4 manual UAT (DB cascade + SignalR cross-tab + history tab content + migration rollback re-verify), tag milestone, merge feature branch ke main, push remote, IT notify final.

Output:
- Activity Log modal refactor jadi 2-tab + partial `_EditHistoryPartial.cshtml` + lazy-load JS handler
- GET `EditHistoryPartial` controller action
- Playwright spec `tests/e2e/edit-peserta-answers.spec.ts` (4 test)
- Manual UAT 4/4 ticked
- Tag `v17.0-p321-complete` + merge main + push remote + IT notify final

Dependency: PLAN 03 selesai (controller actions + SignalR handler + dropdown ⋮).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-01-SUMMARY.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-02-SUMMARY.md
@.planning/phases/321-assessment-edit-jawaban-peserta/321-03-SUMMARY.md
@Views/Admin/AssessmentMonitoringDetail.cshtml
@tests/e2e/export-per-peserta.spec.ts
@tests/e2e/fixtures/accounts.ts
@docs/DEV_WORKFLOW.md

<interfaces>
<!-- PLAN 04 consumer references -->

From Views/Admin/AssessmentMonitoringDetail.cshtml (verified):
- Modal Activity Log INLINE line 540-559 (NOT separate partial file)
- Existing modal body structure:
  ```html
  <div class="modal-body">
      <div id="logSummary" class="mb-3 p-2 bg-light rounded small text-muted"></div>
      <div id="logTimeline"></div>
      <div id="logLoading" class="text-center d-none">
          <div class="spinner-border spinner-border-sm me-2"></div>Memuat...
      </div>
  </div>
  ```
- Existing JS populate `#logSummary` + `#logTimeline` saat modal open (line ~1052 `bootstrap.Modal`)
- `btn-activity-log` click handler ~line 928 (set modal data attrs)

From PLAN 01:
- `Models/AssessmentEditLog.cs` — entity dengan QuestionTextSnapshot, OldAnswerTextSnapshot, NewAnswerTextSnapshot, ActorRole, ActorName, ReasonCode, ReasonText, EditedAt

From PLAN 03:
- POST SubmitEditAnswers menulis AssessmentEditLog row + AuditLog row per edit
- SignalR broadcast workerAnswerEdited handled di MonitoringDetail scripts

From Playwright existing infra (verified `tests/e2e/`):
- `tests/e2e/fixtures/accounts.ts` — 10 role credentials all password `123456`, key `coachee` = Worker negative
- `tests/e2e/export-per-peserta.spec.ts` — Phase 320 pattern reference untuk Admin/HC/Worker auth gate test
- `tests/e2e/global.setup.ts` + `global.teardown.ts` — fixture orchestration
- `playwright.config.ts` di root — base URL `http://localhost:5277` + storageState pattern
- NO new framework install (CONTEXT D-11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activity Log Edit History Tab (refactor modal + partial + controller action + lazy-load JS)</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml, Views/Admin/_EditHistoryPartial.cshtml, Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml (modal INLINE line 540-559 + existing scripts area line ~928 btn-activity-log click handler + line ~1052 bootstrap.Modal populate)
    - Controllers/AssessmentAdminController.cs (PLAN 03 actions inserted, insert EditHistoryPartial adjacent)
    - Models/AssessmentEditLog.cs (PLAN 01 fields)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 12 (lines 1663-1853)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-UI-SPEC.md §Surface 3 Activity Log Edit History Tab + §Copywriting Contract Edit History Tab
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-05 (reason label verbose) + CD-04 (lazy-load AJAX cache)
  </read_first>
  <action>
**Step 1 — Refactor modal body (line 550-556 `Views/Admin/AssessmentMonitoringDetail.cshtml`) jadi 2-tab:**

Replace existing modal-body block dengan:

```html
<div class="modal-body">
  <ul class="nav nav-tabs" role="tablist" id="activityLogTabs">
    <li class="nav-item" role="presentation">
      <button class="nav-link active" id="tab-timeline-btn"
              data-bs-toggle="tab" data-bs-target="#tab-timeline"
              role="tab" aria-controls="tab-timeline" aria-selected="true">
        Activity Timeline
      </button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link" id="tab-edit-history-btn"
              data-bs-toggle="tab" data-bs-target="#tab-edit-history"
              role="tab" aria-controls="tab-edit-history" aria-selected="false"
              data-load-url-template="@Url.Action("EditHistoryPartial")">
        Edit History
      </button>
    </li>
  </ul>
  <div class="tab-content pt-3">
    <div class="tab-pane fade show active" id="tab-timeline" role="tabpanel" aria-labelledby="tab-timeline-btn">
      <div id="logSummary" class="mb-3 p-2 bg-light rounded small text-muted"></div>
      <div id="logTimeline"></div>
      <div id="logLoading" class="text-center d-none">
        <div class="spinner-border spinner-border-sm me-2"></div>Memuat...
      </div>
    </div>
    <div class="tab-pane fade" id="tab-edit-history" role="tabpanel" aria-labelledby="tab-edit-history-btn">
      <div class="edit-history-placeholder text-muted small">Memuat...</div>
    </div>
  </div>
</div>
```

> Note: existing JS yang populate `#logSummary` + `#logTimeline` saat modal open tetap berfungsi karena wrapper di-preserve inside `#tab-timeline`.

**Step 2 — Tambah controller action `EditHistoryPartial`** di `Controllers/AssessmentAdminController.cs` (dekat 3 actions PLAN 03):

```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> EditHistoryPartial(int sessionId)
{
    var logs = await _context.AssessmentEditLogs
        .Where(l => l.AssessmentSessionId == sessionId)
        .OrderByDescending(l => l.EditedAt)
        .ToListAsync();
    return PartialView("_EditHistoryPartial", logs);
    // Controller override line 57 resolve ke ~/Views/Admin/_EditHistoryPartial.cshtml
}
```

> Threat mitigation T-321-10 (audit visibility cross-session leak): filter `Where(l => l.AssessmentSessionId == sessionId)` strict.

**Step 3 — Create partial `Views/Admin/_EditHistoryPartial.cshtml`** (UI-SPEC §Surface 3 + D-05 reason label):

```html
@model List<HcPortal.Models.AssessmentEditLog>

@{
    // D-05 verbose reason label mapper
    string ReasonLabel(string code) => code switch
    {
        "SoalSalah" => "Soal salah / typo",
        "KunciSalah" => "Kunci jawaban salah",
        "BugSistem" => "Bug sistem / glitch",
        "PermintaanPeserta" => "Permintaan koreksi peserta",
        "Lainnya" => "Lainnya (jelaskan)",
        _ => code
    };
}

@if (!Model.Any())
{
  <p class="text-muted">Belum ada edit untuk sesi ini.</p>
}
else
{
  <ul class="list-unstyled">
    @foreach (var log in Model)
    {
      <li class="border-bottom pb-2 mb-2">
        <div class="small text-muted">[@log.EditedAt.ToString("yyyy-MM-dd HH:mm")] Soal #@log.PackageQuestionId</div>
        <div class="fw-bold">@log.QuestionTextSnapshot</div>
        <div>
          <span class="text-muted">@(string.IsNullOrEmpty(log.OldAnswerTextSnapshot) ? "—" : log.OldAnswerTextSnapshot)</span>
          → <span class="text-success">@log.NewAnswerTextSnapshot</span>
        </div>
        <div class="small">oleh @log.ActorRole (@log.ActorName)</div>
        <div class="small text-muted">
          Alasan: @ReasonLabel(log.ReasonCode)
          @if (!string.IsNullOrEmpty(log.ReasonText)) { @($" — {log.ReasonText}") }
        </div>
      </li>
    }
  </ul>
}
```

> Threat mitigation T-321-05 XSS: SEMUA field pakai Razor `@` auto-encode (`@log.ReasonText`, `@log.NewAnswerTextSnapshot`, `@log.ActorName`). JANGAN PAKAI `@Html.Raw` untuk field user-controlled. Verify grep `grep "@Html.Raw" Views/Admin/_EditHistoryPartial.cshtml` exits 1 (no match).

**Step 4 — Tambah inline JS lazy-load + reset cache** di scripts section MonitoringDetail (CD-04 + UI-SPEC §Activity Log Tab Flow):

```javascript
// Lazy-load Edit History tab on first show per session
document.addEventListener('shown.bs.tab', function (event) {
    var btn = event.target;
    if (btn.id !== 'tab-edit-history-btn') return;
    if (btn.dataset.loaded === '1') return;

    var modal = document.getElementById('activityLogModal');
    var sessionId = modal ? modal.dataset.currentSessionId : null;
    if (!sessionId) {
        document.querySelector('#tab-edit-history .edit-history-placeholder').textContent = 'Sesi tidak diketahui.';
        return;
    }

    var urlTemplate = btn.dataset.loadUrlTemplate;
    var url = urlTemplate + '?sessionId=' + sessionId;
    var pane = document.getElementById('tab-edit-history');

    fetch(url)
        .then(function (resp) {
            if (!resp.ok) throw new Error('HTTP ' + resp.status);
            return resp.text();
        })
        .then(function (html) {
            pane.innerHTML = html;
            btn.dataset.loaded = '1';
        })
        .catch(function (err) {
            pane.innerHTML = '<p class="text-danger small">Gagal memuat edit history.</p>';
            console.error('EditHistory load failed:', err);
        });
});

// Set modal.dataset.currentSessionId + reset loaded flag when btn-activity-log clicked
document.addEventListener('click', function (e) {
    var btn = e.target.closest('.btn-activity-log');
    if (!btn) return;
    var modal = document.getElementById('activityLogModal');
    if (!modal) return;
    modal.dataset.currentSessionId = btn.dataset.sessionId;
    var ehBtn = document.getElementById('tab-edit-history-btn');
    if (ehBtn) ehBtn.dataset.loaded = '';
    var ehPane = document.getElementById('tab-edit-history');
    if (ehPane) ehPane.innerHTML = '<div class="edit-history-placeholder text-muted small">Memuat...</div>';
});
```

**Step 5 — Build:**
```bash
dotnet build
```

**Step 6 — UAT manual:**
```bash
dotnet run
```
Buka monitoring detail → klik 🕐 tombol di row → modal Activity Log muncul dengan 2 tab. Default tab "Activity Timeline" (existing content preserved). Klik tab "Edit History" → trigger AJAX → render entries. Kalau belum ada edit → "Belum ada edit untuk sesi ini.". Lakukan 1 edit dulu via Task PLAN 03 → reopen modal → tab Edit History show entry baru dengan label verbose.

**Step 7 — Commit:**
```bash
git add Views/Admin/AssessmentMonitoringDetail.cshtml Views/Admin/_EditHistoryPartial.cshtml Controllers/AssessmentAdminController.cs
git commit -m "feat(v17.0-p321): Activity Log Edit History tab (lazy-load partial, D-05 reason label mapper)"
```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|0 Error" && test -f Views/Admin/_EditHistoryPartial.cshtml && grep -q "EditHistoryPartial" Controllers/AssessmentAdminController.cs && grep -q "ReasonLabel" Views/Admin/_EditHistoryPartial.cshtml && grep -q "Soal salah / typo" Views/Admin/_EditHistoryPartial.cshtml && grep -q "tab-edit-history" Views/Admin/AssessmentMonitoringDetail.cshtml && grep -q "EditHistoryPartial" Views/Admin/AssessmentMonitoringDetail.cshtml && ! grep -q "@Html.Raw" Views/Admin/_EditHistoryPartial.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - Modal body refactor 2-tab: tab-timeline (existing preserved) + tab-edit-history (lazy-load placeholder)
    - Controller action `EditHistoryPartial` exist with `[Authorize(Roles="Admin, HC")]`
    - Strict filter `Where(l => l.AssessmentSessionId == sessionId)` (T-321-10 audit visibility)
    - Partial `Views/Admin/_EditHistoryPartial.cshtml` exists with ReasonLabel switch mapper (D-05 verbose)
    - NO `@Html.Raw` in partial (T-321-05 XSS prevention)
    - Lazy-load JS: shown.bs.tab event + fetch + dataset.loaded cache
    - btn-activity-log click handler reset dataset.loaded='' + placeholder reset (per-session cache invalidation)
    - `git log -1 --pretty=%B` contains `feat(v17.0-p321): Activity Log Edit History tab`
  </acceptance_criteria>
  <done>Edit History tab functional, lazy-load + cache, partial render verbose label, no XSS leak, committed.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright Spec tests/e2e/edit-peserta-answers.spec.ts (4 test: auth-gate + happy-path + concurrency-stale + flip-preview-AJAX)</name>
  <files>tests/e2e/edit-peserta-answers.spec.ts</files>
  <read_first>
    - tests/e2e/export-per-peserta.spec.ts (Phase 320 pattern reference — Admin/HC/Worker auth gate test)
    - tests/e2e/fixtures/accounts.ts (role credentials structure)
    - tests/e2e/global.setup.ts (storageState pattern)
    - playwright.config.ts (root — base URL + project config)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 13 Step 1 (4 Playwright test descriptions, lines 1863-1868) + Validation Architecture (lines 1934-1948)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md (test mapping)
  </read_first>
  <action>
**Pre-step — Verify Playwright infra exists:**
```bash
test -f playwright.config.ts && echo "config exists" && ls tests/e2e/fixtures/ | head -5
```

**Step 1 — Create `tests/e2e/edit-peserta-answers.spec.ts`** mirroring `export-per-peserta.spec.ts` pattern:

```typescript
import { test, expect, type Page } from '@playwright/test';
import { ACCOUNTS } from './fixtures/accounts';

const BASE = process.env.BASE_URL ?? 'http://localhost:5277';

// Helper login (reuse pattern from export-per-peserta.spec.ts)
async function login(page: Page, role: 'admin' | 'hc' | 'coachee') {
  const account = ACCOUNTS[role];
  if (!account) throw new Error(`Account fixture missing: ${role}`);
  await page.goto(`${BASE}/Account/Login`);
  await page.fill('input[name="Email"]', account.email);
  await page.fill('input[name="Password"]', account.password);
  await page.click('button[type="submit"]');
  await page.waitForLoadState('networkidle');
}

// SET via env or hardcode known Completed session ID for local DB (seed via SEED_WORKFLOW if needed)
const COMPLETED_SESSION_ID = parseInt(process.env.EDIT_TEST_SESSION_ID ?? '1', 10);

test.describe('Phase 321 — Edit Peserta Answers', () => {

  test('auth-gate: Admin/HC accessible, Worker blocked (REQ EDIT-01)', async ({ browser }) => {
    // Admin → 200 OK + form render
    const adminCtx = await browser.newContext();
    const adminPage = await adminCtx.newPage();
    await login(adminPage, 'admin');
    const adminResp = await adminPage.goto(`${BASE}/Admin/EditPesertaAnswers/${COMPLETED_SESSION_ID}`);
    expect(adminResp?.status()).toBe(200);
    await expect(adminPage.locator('#editAnswersForm')).toBeVisible();
    await adminCtx.close();

    // HC → 200 OK + form render
    const hcCtx = await browser.newContext();
    const hcPage = await hcCtx.newPage();
    await login(hcPage, 'hc');
    const hcResp = await hcPage.goto(`${BASE}/Admin/EditPesertaAnswers/${COMPLETED_SESSION_ID}`);
    expect(hcResp?.status()).toBe(200);
    await expect(hcPage.locator('#editAnswersForm')).toBeVisible();
    await hcCtx.close();

    // Worker → 403 OR redirect ke login
    const workerCtx = await browser.newContext();
    const workerPage = await workerCtx.newPage();
    await login(workerPage, 'coachee');
    const workerResp = await workerPage.goto(`${BASE}/Admin/EditPesertaAnswers/${COMPLETED_SESSION_ID}`);
    const status = workerResp?.status() ?? 0;
    const url = workerPage.url();
    // Accept either 403 OR redirect to Account/Login OR access denied page
    expect(status === 403 || url.includes('/Account/Login') || url.includes('/AccessDenied')).toBe(true);
    await workerCtx.close();
  });

  test('happy-path edit save: 1 MC edit + reason SoalSalah → submit → AssessmentEditLog++ + score recompute (REQ EDIT-01/03/06)', async ({ page }) => {
    await login(page, 'admin');
    await page.goto(`${BASE}/Admin/EditPesertaAnswers/${COMPLETED_SESSION_ID}`);
    await expect(page.locator('#editAnswersForm')).toBeVisible();

    // Find first MC question card + first radio option that is NOT checked
    const firstMcCard = page.locator('.question-card[data-question-type="MultipleChoice"]').first();
    await expect(firstMcCard).toBeVisible();
    const uncheckedRadio = firstMcCard.locator('input[type="radio"]:not(:checked)').first();
    await uncheckedRadio.check();

    // Reason block should be revealed (d-none removed)
    const reasonBlock = firstMcCard.locator('.reason-block');
    await expect(reasonBlock).toBeVisible();

    // Select SoalSalah preset
    await firstMcCard.locator('.reason-code').selectOption('SoalSalah');

    // Submit form — flip modal may appear, if so click confirm
    await page.click('#submitEditBtn');

    // If flip modal shown, confirm
    const flipModal = page.locator('#flipConfirmModal');
    if (await flipModal.isVisible({ timeout: 2000 }).catch(() => false)) {
      await page.click('#flipConfirmBtn');
    }

    // Wait redirect to monitoring detail
    await page.waitForLoadState('networkidle');
    expect(page.url()).toContain('AssessmentMonitoringDetail');

    // Success TempData visible (banner kelas alert-success or similar)
    // Optional: verify via DB sqlcmd separate manual step
  });

  test('concurrency stale: 2 admin contexts, A submit → B stale error (REQ EDIT-07)', async ({ browser }) => {
    const ctxA = await browser.newContext();
    const ctxB = await browser.newContext();
    const pageA = await ctxA.newPage();
    const pageB = await ctxB.newPage();

    await login(pageA, 'admin');
    await login(pageB, 'admin');

    // Both open same edit page
    await pageA.goto(`${BASE}/Admin/EditPesertaAnswers/${COMPLETED_SESSION_ID}`);
    await pageB.goto(`${BASE}/Admin/EditPesertaAnswers/${COMPLETED_SESSION_ID}`);

    // A makes an edit and submits
    const firstMcCardA = pageA.locator('.question-card[data-question-type="MultipleChoice"]').first();
    await firstMcCardA.locator('input[type="radio"]:not(:checked)').first().check();
    await firstMcCardA.locator('.reason-code').selectOption('BugSistem');
    await pageA.click('#submitEditBtn');
    const flipModalA = pageA.locator('#flipConfirmModal');
    if (await flipModalA.isVisible({ timeout: 2000 }).catch(() => false)) {
      await pageA.click('#flipConfirmBtn');
    }
    await pageA.waitForLoadState('networkidle');

    // B tries to submit dengan stale UpdatedAt
    const firstMcCardB = pageB.locator('.question-card[data-question-type="MultipleChoice"]').first();
    await firstMcCardB.locator('input[type="radio"]:not(:checked)').first().check();
    await firstMcCardB.locator('.reason-code').selectOption('PermintaanPeserta');
    await pageB.click('#submitEditBtn');
    const flipModalB = pageB.locator('#flipConfirmModal');
    if (await flipModalB.isVisible({ timeout: 2000 }).catch(() => false)) {
      await pageB.click('#flipConfirmBtn');
    }
    await pageB.waitForLoadState('networkidle');

    // B should see stale error (TempData banner)
    const staleText = await pageB.textContent('body');
    expect(staleText).toContain('Sesi sudah diubah admin lain');

    await ctxA.close();
    await ctxB.close();
  });

  test('flip-preview AJAX: POST PreviewEditScore returns JSON contract (REQ EDIT-10)', async ({ page }) => {
    await login(page, 'admin');
    await page.goto(`${BASE}/Admin/EditPesertaAnswers/${COMPLETED_SESSION_ID}`);

    // Grab anti-forgery token
    const token = await page.locator('input[name=__RequestVerificationToken]').first().inputValue();

    // POST PreviewEditScore with single draft
    const firstQuestionId = await page.locator('.question-card').first().getAttribute('data-question-id');
    expect(firstQuestionId).toBeTruthy();
    const firstOptionId = await page.locator('.question-card').first().locator('input[type="radio"], input[type="checkbox"]').first().getAttribute('value');
    expect(firstOptionId).toBeTruthy();

    const fd = new FormData();
    fd.append('Drafts[0].QuestionId', firstQuestionId!);
    fd.append('Drafts[0].Options', firstOptionId!);

    const previewResp = await page.evaluate(async ({ token, url, sessionId, qId, oId }) => {
      const fd = new FormData();
      fd.append('Drafts[0].QuestionId', qId);
      fd.append('Drafts[0].Options', oId);
      const r = await fetch(`${url}/Admin/PreviewEditScore?sessionId=${sessionId}`, {
        method: 'POST',
        body: fd,
        headers: { 'RequestVerificationToken': token }
      });
      return { status: r.status, json: await r.json() };
    }, { token, url: BASE, sessionId: COMPLETED_SESSION_ID, qId: firstQuestionId!, oId: firstOptionId! });

    expect(previewResp.status).toBe(200);
    const json = previewResp.json;
    expect(json).toHaveProperty('oldScore');
    expect(json).toHaveProperty('newScore');
    expect(json).toHaveProperty('oldIsPassed');
    expect(json).toHaveProperty('newIsPassed');
    expect(json).toHaveProperty('hasCert');
    expect(json).toHaveProperty('nomorSertifikat');
    expect(json).toHaveProperty('willGenerateCert');
  });
});
```

**Step 2 — Run Playwright (lokal dotnet run dulu):**
```bash
# Terminal 1
dotnet run

# Terminal 2 (set EDIT_TEST_SESSION_ID kalau session ID di-DB lokal bukan 1)
EDIT_TEST_SESSION_ID=42 npx playwright test tests/e2e/edit-peserta-answers.spec.ts
```

Expected: 4 test pass. Kalau fail karena fixture seed missing, document di SUMMARY + bypass dengan manual UAT note (CONTEXT D-04 hybrid pattern allow opportunistic).

**Step 3 — Commit:**
```bash
git add tests/e2e/edit-peserta-answers.spec.ts
git commit -m "test(v17.0-p321): Playwright spec 4 test (auth-gate + happy-path + concurrency-stale + flip-preview-AJAX)"
```
  </action>
  <verify>
    <automated>test -f tests/e2e/edit-peserta-answers.spec.ts && grep -q "auth-gate" tests/e2e/edit-peserta-answers.spec.ts && grep -q "happy-path edit save" tests/e2e/edit-peserta-answers.spec.ts && grep -q "concurrency stale" tests/e2e/edit-peserta-answers.spec.ts && grep -q "flip-preview AJAX" tests/e2e/edit-peserta-answers.spec.ts && grep -q "EditPesertaAnswers" tests/e2e/edit-peserta-answers.spec.ts && grep -q "PreviewEditScore" tests/e2e/edit-peserta-answers.spec.ts && grep -q "Sesi sudah diubah admin lain" tests/e2e/edit-peserta-answers.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec file exists: `tests/e2e/edit-peserta-answers.spec.ts`
    - 4 test functions present: auth-gate, happy-path edit save, concurrency stale, flip-preview AJAX
    - Reuse ACCOUNTS fixture (`tests/e2e/fixtures/accounts.ts`) — NO new credentials
    - Auth-gate test cover 3 role: admin, hc, coachee (worker)
    - Concurrency test asserts text "Sesi sudah diubah admin lain"
    - Preview test asserts JSON shape (oldScore, newScore, oldIsPassed, newIsPassed, hasCert, nomorSertifikat, willGenerateCert)
    - `git log -1 --pretty=%B` contains `test(v17.0-p321): Playwright spec`
    - Run: `npx playwright test tests/e2e/edit-peserta-answers.spec.ts` 4 pass (or documented bypass kalau seed missing — hybrid D-04)
  </acceptance_criteria>
  <done>Playwright spec 4 test ready, mirrors export-per-peserta pattern, reuse fixtures, committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: Manual UAT 4-Item Checklist + Tag + Merge Main + Push + IT Notify Final</name>
  <files>none (UAT + tag + merge + push + notify only)</files>
  <read_first>
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-RESEARCH.md Task 13 (lines 1857-1913)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-VALIDATION.md (Manual-Only Verifications + Validation Sign-Off)
    - .planning/phases/321-assessment-edit-jawaban-peserta/321-CONTEXT.md D-03 (branch merge --no-ff CD-07) + D-08 (IT notify final) + D-10 (commit format)
    - docs/DEV_WORKFLOW.md §5 (pre-commit checklist)
  </read_first>
  <what-built>
**Automated steps (Claude jalankan dulu sebagai prep):**

**Step A — Verify all PLAN 01-04 task commits di branch:**
```bash
git log --oneline feature/phase-321-edit-jawaban ^main | wc -l
# Expected: 14 commit (4 PLAN 01 + 2 PLAN 02 + 7 PLAN 03 + 1 PLAN 04 Task 1 + 1 PLAN 04 Task 2 = 15. Adjust kalau Task 2 commit fail karena seed missing skip — minimum 13.)
git log --oneline feature/phase-321-edit-jawaban ^main
```

**Step B — Final build + smoke run:**
```bash
dotnet build
dotnet run &  # background
sleep 5
curl -sf http://localhost:5277/ | head -5  # smoke
# kill dotnet run after test
```

**Step C — Pre-commit checklist DEV_WORKFLOW §5 quick recap:**
- [x] dotnet build pass tanpa warning baru
- [x] dotnet run + manual smoke verified
- [x] Migration file di-commit (PLAN 01 Task 2)
- [x] CLAUDE.md compliance: semua copy Bahasa Indonesia
  </what-built>
  <how-to-verify>

### 4 MANUAL UAT (User WAJIB lakukan sendiri di browser + sqlcmd lokal — Playwright tidak praktis)

**UAT 1 — DB cascade flip Pass↔Fail (REQ EDIT-04, T-321-11):**
1. Pilih 1 session Completed yang Pass (NomorSertifikat != null, IsPassed = true). Catat session.Id + UserId + PackageId.
2. Login Admin → buka `/Admin/EditPesertaAnswers/{sessionId}`.
3. Ubah cukup jawaban supaya skor turun di bawah PassPercentage → modal "menggagalkan peserta" muncul → Lanjutkan.
4. Verify DB:
   ```bash
   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT NomorSertifikat, ValidUntil, IsPassed, Score FROM AssessmentSessions WHERE Id={sessionId}; SELECT Status FROM TrainingRecords WHERE UserId='{userId}' AND Judul LIKE 'Assessment: %'"
   ```
   Expected: NomorSertifikat = NULL, ValidUntil = NULL, IsPassed = 0, TR.Status = "Failed".
5. Test Fail→Pass: ubah balik jawaban supaya skor naik di atas PassPercentage → modal "meluluskan peserta" → Lanjutkan.
6. Verify DB:
   ```bash
   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT NomorSertifikat, ValidUntil, IsPassed FROM AssessmentSessions WHERE Id={sessionId}; SELECT Status FROM TrainingRecords WHERE UserId='{userId}' AND Judul LIKE 'Assessment: %'"
   ```
   Expected: NomorSertifikat NOT NULL (format `KPB/NNN/RomanMonth/YYYY`), ValidUntil = sekarang+3y, IsPassed=1, TR.Status="Passed".

**UAT 2 — SignalR cross-tab live update (REQ EDIT-09):**
1. Buka 2 tab browser: tab A + tab B → keduanya navigate ke `/Admin/AssessmentMonitoringDetail?title=...&category=...&scheduleDate=...` (sama session group, sama Admin login).
2. Di tab A: klik dropdown ⋮ row peserta X → Edit Jawaban → ubah 1 jawaban → reason → save.
3. Di tab B (tanpa refresh manual): verify dalam <2 detik row peserta X score + result cell update + toast verbose D-07 muncul "Admin {Name} edit jawaban {Worker}: Score 65→75, Fail→Pass".

**UAT 3 — Activity Log Edit History tab content (REQ EDIT-11):**
1. Buka monitoring detail untuk session yang sudah ada edit (post-UAT 1 atau 2).
2. Klik 🕐 button row peserta tersebut → modal Activity Log open.
3. Verify default tab "Activity Timeline" (existing content load via existing JS).
4. Klik tab "Edit History" → AJAX trigger (Network tab DevTools) → render list entries:
   - Urut EditedAt DESC (terbaru di atas)
   - Format `[yyyy-MM-dd HH:mm] Soal #N` timestamp prefix
   - QuestionTextSnapshot bold
   - Old → New arrow (Old text-muted, New text-success)
   - "oleh {ActorRole} ({ActorName})"
   - "Alasan: {Verbose label D-05}" (e.g., "Soal salah / typo" — NOT "SoalSalah")
   - Kalau Lainnya → " — {ReasonText}" suffix

**UAT 4 — Migration rollback re-verify (REQ EDIT-13):**
1. Stop `dotnet run` kalau jalan.
2. ```bash
   # Identify previous migration name
   ls Migrations/*.cs | grep -v Designer | grep -v ModelSnapshot | sort | tail -2 | head -1
   ```
3. ```bash
   dotnet ef database update {PreviousMigrationName} --context ApplicationDbContext
   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT OBJECT_ID('AssessmentEditLogs')"
   # Expected: NULL (table dropped)
   ```
4. ```bash
   dotnet ef database update --context ApplicationDbContext
   sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -Q "SELECT OBJECT_ID('AssessmentEditLogs')"
   # Expected: integer (re-created)
   ```

### TAG + MERGE + PUSH (CONTEXT D-03 + CD-07)

**Step T1 — Tag milestone:**
```bash
git tag -a v17.0-p321-complete -m "Milestone v17.0 Phase 321: Edit Jawaban Peserta complete"
```

**Step T2 — Merge ke main (CD-07 default merge --no-ff):**
```bash
git checkout main
git pull --ff-only origin main   # ensure main up-to-date
git merge --no-ff feature/phase-321-edit-jawaban -m "Merge phase 321: edit jawaban peserta (v17.0)"
```

**Step T3 — Push remote (main + tag):**
```bash
git push origin main
git push origin v17.0-p321-complete
```

### IT NOTIFY FINAL (CONTEXT D-08)

Drop message ke channel IT (template):

```
Milestone v17.0 Phase 321 deploy-ready. 

- Commit hash (merge to main): {output of `git rev-parse HEAD` after merge}
- Tag: v17.0-p321-complete

⚠️ ADA MIGRATION BARU: AddAssessmentEditLogs
   Command: dotnet ef database update --context ApplicationDbContext
   
Post-deploy verify checklist:
1. /Admin/EditPesertaAnswers/{sessionId} accessible untuk Admin/HC role di DB Dev
2. SELECT OBJECT_ID('AssessmentEditLogs') returns non-null

Branch source: feature/phase-321-edit-jawaban (sudah di-merge ke main)
```

### USER MUST CONFIRM (block sebelum complete):

- [ ] UAT 1 DB cascade flip Pass→Fail + Fail→Pass verified via sqlcmd
- [ ] UAT 2 SignalR cross-tab live update verified (toast muncul + row update tanpa refresh)
- [ ] UAT 3 Activity Log Edit History tab verified (verbose label, sort DESC, format correct)
- [ ] UAT 4 Migration rollback re-verify lulus
- [ ] Tag v17.0-p321-complete dibuat
- [ ] Branch feature/phase-321-edit-jawaban di-merge ke main (merge --no-ff)
- [ ] Push main + tag ke origin sukses
- [ ] IT notify final terkirim dengan commit hash + tag + flag MIGRATION
  </how-to-verify>
  <resume-signal>Type "approved" kalau semua 4 UAT pass + tag + merge + push + IT notify selesai. Atau describe issue spesifik (mis. UAT 2 toast tidak muncul, UAT 4 rollback gagal di Down() method, dst.) — Claude jalankan gap fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller `EditHistoryPartial` → DB query | Server queries AssessmentEditLogs filtered by sessionId — must NOT leak cross-session rows |
| partial render → browser HTML | Snapshot text fields (QuestionText, OldAnswerText, NewAnswerText, ActorName, ReasonText) user-controlled — must auto-encode via Razor `@` |
| Playwright spec → live app | E2E tests authenticate real user accounts + perform real DB writes — must run against lokal DB only, never prod |
| Merge branch → main | Atomic merge --no-ff preserves task granularity, no rebase that loses commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-321-10 | Information Disclosure (audit visibility cross-session leak) | `EditHistoryPartial` action + `_EditHistoryPartial.cshtml` | mitigate | Query filter strict: `Where(l => l.AssessmentSessionId == sessionId)` — TIDAK pernah pakai user-input untuk select session lain. Authorize Admin/HC role only. JS client kirim `?sessionId=` via dataset attr yang set dari btn-activity-log click (server-controlled di markup). Defense in depth: GET parameter int sessionId — kalau user manipulate via URL, controller cuma return AssessmentEditLogs untuk session ID itu (mereka memang admin/HC dengan akses kross-session legitimate). |
| T-321-11 | Tampering (Cascade integrity orphan cert) | UAT 1 verify Pass↔Fail full transaction atomicity | mitigate | Layer 1: PLAN 02 RegradeAfterEditAsync invoke dalam scope transaction PLAN 03 SubmitEditAnswers — exception apa pun rollback total. Layer 2: ExecuteUpdateAsync status guard `WHERE Status == "Completed"` (T-321-03). Layer 3: Cert generate retry 3x via IsDuplicateKeyException (T-321-03b). UAT 1 verify end-to-end (no orphan cert / TR mismatch). Acceptance: sqlcmd query confirms either both updated (commit) OR both unchanged (rollback). |
| T-321-05b | Information Disclosure (XSS in Edit History partial) | `_EditHistoryPartial.cshtml` rendering user-controlled snapshot fields | mitigate | Razor `@` auto-encode SEMUA field user-controlled: `@log.QuestionTextSnapshot`, `@log.OldAnswerTextSnapshot`, `@log.NewAnswerTextSnapshot`, `@log.ActorName`, `@log.ReasonText`. NO `@Html.Raw` di partial — verified grep exits 1. |

</threat_model>

<verification>
- `dotnet build` exits 0
- Task 1: Activity Log Edit History tab functional, partial render verbose label D-05, no Html.Raw
- Task 2: Playwright spec 4 test exist (auth-gate + happy-path + concurrency + preview-AJAX)
- Task 3 [BLOCKING]: 4 manual UAT user-confirmed PASS + tag + merge + push + IT notify
- Branch feature/phase-321-edit-jawaban di-merge ke main via `git merge --no-ff`
- Remote main + tag v17.0-p321-complete di-push ke origin
- IT notify final terkirim dengan MIGRATION flag
</verification>

<success_criteria>
- 2 task auto + 1 task blocking checkpoint
- 3 commit di branch (Task 1 partial+modal+controller, Task 2 Playwright spec) ATAU 2 commit (kalau Task 2 spec skip dengan documented bypass)
- Activity Log Edit History tab live, lazy-load OK, partial render verbose label D-05
- Manual UAT 4/4 ticked oleh user
- Tag `v17.0-p321-complete` exist di local + origin
- Branch merged ke main (merge --no-ff preserve task granularity)
- IT notify final terkirim
- Phase 321 SHIPPED LOKAL (pending IT promo Dev/Prod)
</success_criteria>

<output>
After completion, create `.planning/phases/321-assessment-edit-jawaban-peserta/321-04-SUMMARY.md` dengan:
- Daftar commit Task 1 + Task 2
- Playwright spec run result (4/4 pass atau bypass reason)
- Manual UAT 4/4 user-confirmed status
- Tag + merge + push confirmation (commit hash post-merge + tag SHA)
- IT notify final message + timestamp
- Phase 321 status: SHIPPED LOKAL — pending user push remote + IT promo Dev/Prod
- Total commit Phase 321: {N} (sum dari PLAN 01-04)
- Carry-forward / deferred items kalau ada
</output>
