---
phase: 322-filter-scope-per-tab-manage-assessment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
  - Views/Admin/Shared/_TrainingRecordsTab.cshtml
  - Views/Admin/Shared/_HistoryTab.cshtml
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "Tab Assessment Groups punya filter HTMX inline form id=filterFormAssessment (search + kategori + status, 3-field)"
    - "Tab Assessment Groups pagination convert ke HTMX button dengan hx-include=#filterFormAssessment (preserve filter state saat klik page)"
    - "Tab Input Records punya filter HTMX inline form id=filterFormTraining (Bagian + Kategori Training + Unit + Status + Cari Nama/Nopeg, 5-field)"
    - "Cascade Bagian → Unit preserved via onchange inline pre-HTMX trigger"
    - "Sub-tab Riwayat Training punya input filterTrainingRows client-side + table id=trainingHistoryTable + tr class=training-history-row + data-worker attribute per row"
    - "dotnet build sukses tanpa Razor compile error setelah 4 task edit"
  artifacts:
    - path: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml"
      provides: "Tab 1 filter HTMX inline + pagination HTMX preserve filter state"
      contains: "id=\"filterFormAssessment\""
    - path: "Views/Admin/Shared/_TrainingRecordsTab.cshtml"
      provides: "Tab 2 filter HTMX inline 5-field + cascade Bagian→Unit preserved"
      contains: "id=\"filterFormTraining\""
    - path: "Views/Admin/Shared/_HistoryTab.cshtml"
      provides: "Sub-tab Riwayat Training filter client-side + DOM attributes untuk filterTrainingRows JS (function ditambah di PLAN 02)"
      contains: "id=\"trainingHistoryTable\""
  key_links:
    - from: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml (pagination button hx-include)"
      to: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml (form id=filterFormAssessment)"
      via: "hx-include='#filterFormAssessment'"
      pattern: "HTMX form state preservation"
    - from: "Views/Admin/Shared/_HistoryTab.cshtml (sub-tab Riwayat Training input + table + tr)"
      to: "Views/Admin/ManageAssessment.cshtml (filterTrainingRows JS function ditambah di PLAN 02)"
      via: "DOM ID #trainingHistoryTable + class .training-history-row + data-worker attribute"
      pattern: "Client-side row filter parity sama filterAssessmentRows existing"
---

<objective>
PLAN 01 (Wave 1) — Partial Views Filter HTMX Refactor untuk Phase 322 Filter Scope Per Tab.

Purpose: Convert filter form di 3 partial view (Tab 1 Assessment Groups, Tab 2 Input Records, Tab 3 sub-tab Riwayat Training) dari pattern lama (form GET submit ke shell `ManageAssessment` action / no filter) ke HTMX inline trigger pattern direct ke partial endpoint (`ManageAssessmentTab_*`). Preserve cascade Bagian → Unit. Bonus fix pagination Tab 1 filter state preservation.

PLAN ini foundation untuk PLAN 02 (shell cleanup + JS function `filterTrainingRows`). Sequential strict — PLAN 02 reference DOM ID yang ditambah di PLAN 01 Task 4 (`#trainingHistoryTable`, `.training-history-row`).

Output:
- `_AssessmentGroupsTab.cshtml` — form id=`filterFormAssessment` HTMX + pagination HTMX `hx-include`
- `_TrainingRecordsTab.cshtml` — form id=`filterFormTraining` HTMX 5-field + cascade preserved
- `_HistoryTab.cshtml` — sub-tab Riwayat Training tambah filter + data-worker + id=`trainingHistoryTable`
- 4 commit atomic (1 per task)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/322-filter-scope-per-tab-manage-assessment/322-CONTEXT.md
@.planning/phases/322-filter-scope-per-tab-manage-assessment/322-RESEARCH.md
@.planning/phases/322-filter-scope-per-tab-manage-assessment/322-PATTERNS.md
@docs/superpowers/specs/2026-05-22-filter-scope-per-tab-manage-assessment-design.md
@docs/superpowers/plans/2026-05-22-filter-scope-per-tab-manage-assessment.md
</context>

## Task 1: Convert `_AssessmentGroupsTab.cshtml` filter form to HTMX inline

**File:** `Views/Admin/Shared/_AssessmentGroupsTab.cshtml:12-83`

**Action:** Replace existing `<form method="get" asp-action="ManageAssessment">` block (search) + `<div class="row mb-3">` block (kategori + status) dengan single `<form id="filterFormAssessment" onsubmit="return false;">` HTMX inline trigger pattern. Reset filter convert dari `<a href>` ke `<button type="button">` HTMX trigger.

**Code spec (full block ganti baris 12-83):** lihat plan superpowers `docs/superpowers/plans/2026-05-22-filter-scope-per-tab-manage-assessment.md` Task 1 Step 2.

**Verify:** `dotnet build` sukses. Browser: load Tab Assessment Groups, filter `Kategori=OJT` via dropdown trigger XHR ke `/Admin/ManageAssessmentTab_Assessment?category=OJT...`, table refresh. Console no JS error.

**Commit:** `feat(322-01): HTMX inline filter Tab Assessment Groups (no shared shell form)`

---

## Task 2: Convert `_AssessmentGroupsTab.cshtml` pagination to HTMX

**File:** `Views/Admin/Shared/_AssessmentGroupsTab.cshtml:294-341`

**Action:** Replace pagination block (`@if (totalPages > 1) { <nav>...</nav> }`) — convert `<a class="page-link" href="?tab=assessment&page=N...">` ke `<button class="page-link" type="button" hx-get="..." hx-vals='{"page":"N"}' hx-include="#filterFormAssessment" hx-target="closest .htmx-tab-wrapper" hx-swap="innerHTML">`.

**Bonus fix:** pre-existing bug pagination cuma include `search` param (miss `category` + `statusFilter`). Pattern `hx-include="#filterFormAssessment"` solve sekalian — semua filter field ikut serta saat klik page.

**Code spec:** lihat plan superpowers Task 2 Step 2.

**Verify:** `dotnet build` sukses. Browser: filter `Kategori=OJT`, ada multi-page → klik page 2 → XHR `?page=2&category=OJT&...` (kategori ikut). Table show OJT page 2.

**Commit:** `feat(322-02): HTMX pagination Tab Assessment Groups + preserve filter state`

---

## Task 3: Convert `_TrainingRecordsTab.cshtml` filter form to HTMX inline

**File:** `Views/Admin/Shared/_TrainingRecordsTab.cshtml:28-130`

**Action:** Replace entire `<div class="card bg-light border-0 mb-4">` block dengan `<form id="filterFormTraining" onsubmit="return false;">` HTMX 5-field pattern:
- Dropdown Bagian (`name="section"` + cascade `onchange="document.getElementById('filterUnit').value='';"` PRE HTMX `hx-trigger="change"`)
- Dropdown Kategori Training (`name="category"` + `hx-trigger="change"`)
- Dropdown Unit (`name="unit"` + `hx-trigger="change"`)
- Dropdown Status (`name="statusFilter"` + `hx-trigger="change"`)
- Input Search Nama/Nopeg (`name="search"` + `hx-trigger="input changed delay:500ms, keyup[key=='Enter']"`) + button Cari + button Reset

Reset filter button convert dari `<a href>` ke `<button type="button">` + `onclick` clear field values + HTMX trigger.

**Code spec:** lihat plan superpowers Task 3 Step 2.

**Verify:** `dotnet build` sukses. Browser: load Tab Input Records, filter `Bagian=Operation` → Unit dropdown populate cascade (filter Unit dikosongkan dulu). Filter `Status=Sudah` → XHR + table refresh. Console no JS error.

**Commit:** `feat(322-03): HTMX inline filter Tab Input Records (5-field native)`

---

## Task 4: Add Riwayat Training filter + data-worker di `_HistoryTab.cshtml`

**File:** `Views/Admin/Shared/_HistoryTab.cshtml:108-146`

**Action:** Replace sub-tab Training pane (`<div class="tab-pane fade" id="riwayat-training-pane">`) — tambah:
1. **Filter input** di top sub-tab Training (mirror sub-tab Assessment pattern `_HistoryTab.cshtml:39-55`):
   ```cshtml
   <div class="row g-2 mb-3 pt-3 px-0">
       <div class="col-md-4">
           <input type="text" class="form-control form-control-sm"
                  placeholder="Cari nama pekerja / Nopeg..."
                  id="trainingWorkerFilter" oninput="filterTrainingRows()">
       </div>
   </div>
   ```
2. **Table** tambah `id="trainingHistoryTable"`.
3. **Row** tambah `class="training-history-row" data-worker="@row.WorkerName.ToLower() @(row.WorkerNIP?.ToLower() ?? "")"`.

JS function `filterTrainingRows()` BELUM ditambah di PLAN ini — ditambah di PLAN 02 Task 5 Step 5 (di shell `ManageAssessment.cshtml`). Sub-tab Training input akan tidak ada effect sampai PLAN 02 selesai — acceptable, sequential strict.

**Code spec:** lihat plan superpowers Task 4 Step 2.

**Verify:** `dotnet build` sukses. Browser: load Tab History → klik sub-tab Riwayat Training → filter input visible. Inspect DOM → `<tr class="training-history-row" data-worker="...">` exists.

**Commit:** `feat(322-04): client-side filter Riwayat Training sub-tab parity`

---

<verify>
<automated>
- `dotnet build` sukses (no Razor compile error) setelah tiap Task 1-4 commit.
- Browser load `/Admin/ManageAssessment` Tab Assessment Groups → 1 filter row visible (TIDAK double — lapisan partial converted ke HTMX inline trigger, lapisan shell akan dihapus di PLAN 02; sementara PLAN 01 selesai, shell shared filter MASIH ada di shell view = masih double sampai PLAN 02 selesai).
- Network tab: filter Tab 1/2 → XHR ke `ManageAssessmentTab_*` endpoint (BUKAN shell `ManageAssessment`).
</automated>
<manual>
- Visual: 4 commit total (322-01 + 322-02 + 322-03 + 322-04) di `git log`.
- DEV_WORKFLOW §5 checklist per commit (build + manual smoke).
</manual>
</verify>

<blocking_checkpoint>
PLAN 01 selesai = transition ke PLAN 02. PLAN 02 (shell cleanup) WAJIB dimulai segera setelah PLAN 01 commit terakhir — sementara PLAN 01 selesai sendiri, shell `<form id="filter-form">` masih ada → double filter masih terlihat user. PLAN 02 hapus shell form sekalian tambah `filterTrainingRows()` JS function yang dibutuhkan PLAN 01 Task 4 effective.
</blocking_checkpoint>
