---
phase: 322-filter-scope-per-tab-manage-assessment
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Views/Admin/ManageAssessment.cshtml
  - Controllers/AssessmentAdminController.cs
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "Shell ManageAssessment.cshtml tidak punya <form id=filter-form> lagi (shared filter form dihapus)"
    - "Shell 3 wrapper htmx-tab-wrapper tidak punya hx-include=#filter-form lagi (selector dangling fix); diganti dengan hx-vals per-tab dengan ViewBag.* values relevant ke tab (D-21)"
    - "URL bookmark backward compat preserved: GET /Admin/ManageAssessment?category=OJT → wrapper Tab 1 inject category via hx-vals → partial dapet category=OJT param"
    - "Shell tidak punya htmx:afterSwap cross-tab invalidation listener lagi (Phase 311 Plan 04 BUG-2A/2B logic dihapus)"
    - "Shell shown.bs.tab handler bagian update hx-get endpoint dihapus, bagian toggle header buttons visibility preserved"
    - "Shell punya JS function filterTrainingRows() (parity sama filterAssessmentRows existing)"
    - "Controller AssessmentAdminController.ManageAssessment action body tidak punya block ViewBag.Categories = await _cache.GetOrCreateAsync(...) lagi"
    - "Controller _cache injection + CategoriesCacheKey field + invalidation di Add/Edit/DeleteCategory preserved (partial action masih pakai)"
    - "dotnet build sukses tanpa C# atau Razor compile error setelah 2 task edit"
    - "Tab Assessment Groups TIDAK double filter lagi (visual verify di PLAN 03 UAT)"
  artifacts:
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Shell view bersih dari shared filter + cross-tab listener + endpoint updater script; tambah filterTrainingRows JS function"
      contains: "filterTrainingRows"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Shell action ManageAssessment drop ViewBag.Categories redundant cache fetch"
      contains: "Phase 322"
  key_links:
    - from: "Views/Admin/ManageAssessment.cshtml (filterTrainingRows JS function)"
      to: "Views/Admin/Shared/_HistoryTab.cshtml (sub-tab Training input + table + tr, ditambah di PLAN 01 Task 4)"
      via: "DOM ID #trainingHistoryTable + class .training-history-row + data-worker attribute"
      pattern: "Client-side row filter parity sama filterAssessmentRows existing"
    - from: "Controllers/AssessmentAdminController.cs (shell action drop ViewBag.Categories)"
      to: "Controllers/AssessmentAdminController.cs:227 partial action ManageAssessmentTab_Assessment (cache fetch preserved)"
      via: "CategoriesCacheKey shared invalidation preserved di Add/Edit/DeleteCategory actions"
      pattern: "Single cache key, multi-consumer, single-writer invalidation"
---

<objective>
PLAN 02 (Wave 2) — Shell View Cleanup + Controller Cleanup untuk Phase 322 Filter Scope Per Tab.

Purpose: Hapus residual Phase 311 Plan 02 shared filter design dari shell view + drop redundant `ViewBag.Categories` cache di controller shell action. Tambah JS function `filterTrainingRows()` yang dibutuhkan PLAN 01 Task 4 (sub-tab Riwayat Training filter input).

Sequential strict — PLAN 01 wajib selesai dulu (DOM ID `#trainingHistoryTable` + `.training-history-row` + `data-worker` attribute exists di partial sub-tab Training, target JS function `filterTrainingRows()`).

Output:
- `ManageAssessment.cshtml` shell: hapus `<form id="filter-form">` + cross-tab listener + endpoint updater script; tambah `filterTrainingRows()` JS function; cleanup unused ViewBag references
- `AssessmentAdminController.cs` shell action `ManageAssessment`: drop `ViewBag.Categories` cache fetch block (redundant pasca shell filter delete)
- 2 commit atomic (1 per task)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/322-filter-scope-per-tab-manage-assessment/322-CONTEXT.md
@.planning/phases/322-filter-scope-per-tab-manage-assessment/322-RESEARCH.md
@.planning/phases/322-filter-scope-per-tab-manage-assessment/322-PATTERNS.md
@.planning/phases/322-filter-scope-per-tab-manage-assessment/322-01-PLAN.md
@docs/superpowers/specs/2026-05-22-filter-scope-per-tab-manage-assessment-design.md
@docs/superpowers/plans/2026-05-22-filter-scope-per-tab-manage-assessment.md
</context>

## Task 5: Cleanup `ManageAssessment.cshtml` shell view

**File:** `Views/Admin/ManageAssessment.cshtml`

**Actions (5 sub-edit dalam 1 task, 1 commit):**

### Sub-edit 5a: Delete shared filter form block (baris 88-190)

Hapus entire `<form id="filter-form">` block — dari comment `<!-- ============================================================ -->` Phase 311 Plan 02 (baris 84-87) sampai `</form>` (baris 190).

Setelah edit: structure langsung lanjut dari `</ul>` (close tab nav, baris 82) ke `<!-- Tab content -->` comment (baris 192-193).

### Sub-edit 5b: Delete cross-tab `htmx:afterSwap` invalidation listener (baris 365-398)

Hapus entire comment block + listener function `document.body.addEventListener('htmx:afterSwap', function(evt) { ... })` — Phase 311 Plan 04 BUG-2A/2B.

**Catatan:** TIDAK menghapus `htmx:afterSwap` listener Phase 312 (di partial `_AssessmentGroupsTab.cshtml:578-583` defensive check modal exists) — listener tersebut di file partial yang berbeda, scope berbeda, dipertahankan.

### Sub-edit 5c: Delete `hx-get` endpoint updater script (di dalam `shown.bs.tab` handler)

Di dalam `shown.bs.tab` event listener handler (yang juga handle header button toggle), hapus block (baris 338-358) yang update `hidden #filter-tab` value + loop `document.querySelectorAll('#filter-form [hx-get]').forEach(...)`.

**WAJIB preserve** bagian header button toggle (baris 329-337) — kondisi `if (target === '#pane-assessment')`.

### Sub-edit 5d: Add `filterTrainingRows()` JS function

Di dalam `@section Scripts { ... }`, setelah existing `filterAssessmentRows()` function (baris 473-485), tambah:

```js
// Phase 322: History tab Riwayat Training client-side filter (parity sama Riwayat Assessment)
function filterTrainingRows() {
    var workerFilter = document.getElementById('trainingWorkerFilter')?.value.toLowerCase() || '';
    var rows = document.querySelectorAll('#trainingHistoryTable .training-history-row');
    rows.forEach(function(row) {
        var worker = row.getAttribute('data-worker') || '';
        row.style.display = (!workerFilter || worker.includes(workerFilter)) ? '' : 'none';
    });
}
```

### Sub-edit 5e: Cleanup unused ViewBag references di top `@{ ... }` block

Di baris 1-15 `@{ ... }` block, hapus var yang tidak dipakai pasca shell filter delete:
- `var searchTerm = ViewBag.SearchTerm as string;`
- `var assessmentCategories = ViewBag.Categories as List<string> ?? new List<string>();`
- `var selectedCategory = ViewBag.SelectedCategory as string ?? "";`
- `var selectedStatus = ViewBag.SelectedStatus as string ?? "";`

Preserve: `activeTab`, `urlAssessment`, `urlTraining`, `urlHistory`.

Note: ViewBag.SearchTerm/SelectedCategory/SelectedStatus/SelectedSection/SelectedUnit/CurrentPage tetap di-set controller (PLAN 02 T6) — diakses langsung via `ViewBag.*` di sub-edit 5f wrapper `hx-vals`, tidak butuh local var di shell view.

### Sub-edit 5f: Update 3 wrapper `htmx-tab-wrapper` attributes (D-21 fix dangling hx-include)

Di 3 wrapper (Tab 1 baris 198-205, Tab 2 baris 232-239, Tab 3 baris 276-283), hapus `hx-include="#filter-form"` dan ganti dengan `hx-vals` per-tab.

**Wrapper Tab 1 (baris 198-205) — hapus baris 203 `hx-include="#filter-form"`, ganti dengan:**

```cshtml
hx-vals='@Json.Serialize(new {
    search = ViewBag.SearchTerm,
    category = ViewBag.SelectedCategory,
    statusFilter = ViewBag.SelectedStatus,
    page = ViewBag.CurrentPage
})'
```

**Wrapper Tab 2 (baris 232-239) — hapus baris 237 `hx-include="#filter-form"`, ganti dengan:**

```cshtml
hx-vals='@Json.Serialize(new {
    section = ViewBag.SelectedSection,
    unit = ViewBag.SelectedUnit,
    category = ViewBag.SelectedCategory,
    statusFilter = ViewBag.SelectedStatus,
    search = ViewBag.SearchTerm,
    page = ViewBag.CurrentPage
})'
```

**Wrapper Tab 3 (baris 276-283) — hapus baris 281 `hx-include="#filter-form"`, ganti dengan:**

```cshtml
hx-vals='@Json.Serialize(new {
    search = ViewBag.SearchTerm,
    page = ViewBag.CurrentPage
})'
```

**Rationale (CONTEXT D-21):** Wrapper rely `hx-include="#filter-form"` untuk pass query string URL bookmark → partial action saat initial load. PLAN 02 5a hapus form → selector dangling → URL bookmark BROKEN. Fix: explicit `hx-vals` per-tab dengan ViewBag values yang RELEVANT ke tab itu only — preserve D-10 backward compat + structurally prevent Bug 2 cross-tab leak.

**Verify:** `dotnet build` sukses. Browser: akses `/Admin/ManageAssessment?category=OJT&statusFilter=Active` → Tab 1 initial load filter OJT+Active applied (DevTools Network: XHR `ManageAssessmentTab_Assessment?category=OJT&statusFilter=Active&...`). Switch Tab 2 → XHR `ManageAssessmentTab_Training` TIDAK include `category=OJT` (Tab 2 wrapper hx-vals tidak inject category Tab 1 — hanya inject ViewBag.SelectedCategory yang controller TIDAK set saat URL bookmark Tab 1; bug 2 prevented).

**Verify Razor `@Json.Serialize`:** namespace `System.Text.Json` atau Microsoft alternative. Cek `_ViewImports.cshtml` apakah `@using System.Text.Json` exists. Kalau tidak, tambah atau pakai `Newtonsoft.Json.JsonConvert.SerializeObject(...)`. Razor convention proyek harus dicek per file edit time.

**Verify:** `dotnet build` sukses. Browser: load `/Admin/ManageAssessment` Tab 1 — **TIDAK double filter lagi** (shell form dihapus, partial form HTMX inline trigger sole filter). Tab switch ke Tab 2 → header buttons hide (preserved). Console no JS error. Sub-tab Riwayat Training filter input bekerja → ketik nama → rows hide/show.

**Commit:** `feat(322-05): rollback shared filter shell, tambah filterTrainingRows JS, fix wrapper hx-vals (D-21)`

---

## Task 6: Cleanup `AssessmentAdminController.cs` shell action

**File:** `Controllers/AssessmentAdminController.cs:62-105` (method `ManageAssessment` shell action)

**Action:** Hapus block `ViewBag.Categories = await _cache.GetOrCreateAsync(...)` (baris 88-97 + comment baris 85-87) — total 13 baris.

**Action body setelah edit (target final state):**

```csharp
public async Task<IActionResult> ManageAssessment(string? search, int page = 1, int pageSize = 20,
    string? tab = null, string? section = null, string? unit = null,
    string? category = null, string? statusFilter = null, string? isFiltered = null)
{
    // Phase 322: shared filter shell removed (rollback Phase 311 Plan 02).
    // Per-tab native filter di partial views — shell hanya routing + lazy-load HTMX trigger.

    var swShell = System.Diagnostics.Stopwatch.StartNew();

    // Tab routing — default to "assessment" (preserved dari logika lama)
    var activeTab = tab switch { "training" => "training", "history" => "history", _ => "assessment" };
    ViewBag.ActiveTab = activeTab;

    // Filter values preserve untuk backward compat URL bookmark (partial action read via param)
    ViewBag.SearchTerm = search;
    ViewBag.SelectedCategory = category ?? "";
    ViewBag.SelectedStatus = statusFilter ?? "";
    ViewBag.SelectedSection = section;
    ViewBag.SelectedUnit = unit;
    ViewBag.CurrentPage = page;
    ViewBag.PageSize = pageSize;

    swShell.Stop();
    _logger.LogInformation(
        "ManageAssessment perf [tab=shell]: elapsed={Ms}ms search_present={SearchPresent} page={Page}",
        swShell.ElapsedMilliseconds, !string.IsNullOrEmpty(search), page);

    return View();
}
```

**WAJIB preserve:**
- `_cache` field injection (constructor) — partial action `ManageAssessmentTab_Assessment` line 227 still uses.
- `CategoriesCacheKey` constant — partial action + invalidation di Add/Edit/DeleteCategory pakai.
- Cache invalidation `_cache.Remove(CategoriesCacheKey)` di Add/Edit/DeleteCategory actions — preserved.

**Verify:** `dotnet build` sukses. Browser: load `/Admin/ManageAssessment` Tab 1 → kategori dropdown populated correctly (cache populated oleh partial action). Add Category baru di `/Admin/ManageCategories` → cache invalidate → reload Tab 1 → kategori baru muncul.

**Commit:** `feat(322-06): drop ViewBag.Categories cache di shell action (redundant)`

---

<verify>
<automated>
- `dotnet build` sukses (no C# atau Razor compile error) setelah Task 5 + Task 6 commit.
- Browser load `/Admin/ManageAssessment` Tab Assessment Groups → **1 filter row visible** (no double — shell shared form dihapus, partial form HTMX inline trigger sole filter).
- Tab switch Tab 1 → Tab 2: header buttons (Buat Assessment | Monitoring | Audit Log) **hide** saat Tab 2 active (visibility toggle preserved).
- Tab switch ke Tab 2 → Tab 1: header buttons **show** lagi.
- Sub-tab Riwayat Training filter input → ketik nama → rows hide/show (JS function `filterTrainingRows()` works).
- Sub-tab Riwayat Assessment filter input → existing `filterAssessmentRows()` works (preserved, no regression).
- Network tab: switch Tab 1 → Tab 2 → Tab 1: TIDAK ada XHR re-fetch (HTMX `once` modifier preserved, partial cached di DOM).
- Console: no JS error.
</automated>
<manual>
- Visual: 2 commit total (322-05 + 322-06) di `git log`.
- DEV_WORKFLOW §5 checklist per commit (build + manual smoke).
- Verify rollback ergonomic: single `git revert HEAD~2..HEAD` mengembalikan state PLAN 02 selesai (shell shared filter back, controller ViewBag.Categories back).
</manual>
</verify>

<blocking_checkpoint>
PLAN 02 selesai = filter scope per tab fully working. **Pre-condition PLAN 03 UAT:** semua 6 task (PLAN 01 + 02) commit, `dotnet watch run` running, login `admin@pertamina.com` lokal ready, browser DevTools open Network + Console tab.

PLAN 03 verify hasil PLAN 01 + 02 combined via 11-step manual UAT golden path + edge. Kalau ada bug discovered di PLAN 03, fix loop kembali ke PLAN 01/02 task affected, kemudian re-run UAT.
</blocking_checkpoint>
