---
phase: 323
plan: 02
type: execute
wave: 2
depends_on:
  - "323-01"
files_modified:
  - tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: false
requirements:
  - CASCADE-01

must_haves:
  truths:
    - "Playwright E2E spec Phase323 exist dengan 3 test (a no-edits / b with-edits / c group-mixed)"
    - "Spec login admin@pertamina.com via inline loginAny() helper + accounts fixture"
    - "Seed temporary AssessmentEditLog ikut SEED_WORKFLOW (snapshot BAK + journal entry active → cleaned)"
    - "3 test green sebelum cleanup; full regression npx playwright test no new failure"
    - "Audit log row verified manual mengandung token EditLogsCount={N}"
    - "Manual UAT 3 skenario via browser localhost:5277 PASS"
  artifacts:
    - path: "tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts"
      provides: "3 Playwright test cases dengan env var session ID + describe block Phase 323"
      exports: ["test.describe('Phase 323 — Cascade AssessmentEditLogs Delete', ...)"]
      min_lines: 80
    - path: "docs/SEED_JOURNAL.md"
      provides: "Phase 323 journal entry (status active → cleaned)"
      contains: "323 | temporary + local-only"
  key_links:
    - from: "tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts"
      to: "tests/helpers/accounts.ts (accounts.admin)"
      via: "import { accounts, AccountKey } from '../helpers/accounts'"
      pattern: "import.*accounts.*from.*'\\.\\./helpers/accounts'"
    - from: "tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts"
      to: "Controllers/AssessmentAdminController.cs (cascade endpoints patched in Plan 01)"
      via: "HTTP POST trigger via UI form submit"
      pattern: "/Admin/ManageAssessment"
  - from: "docs/SEED_JOURNAL.md Phase 323 entry"
      to: "BACKUP/RESTORE sqlcmd lifecycle"
      via: "SEED_WORKFLOW §5 snapshot+restore"
      pattern: "HcPortalDB_Dev-pre323.*\\.bak"
---

<objective>
Buat Playwright TypeScript E2E spec `tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts` dengan 3 test (no-edits / with-edits / group-mixed per D-03), setup seed temporary AssessmentEditLog mengikuti SEED_WORKFLOW (snapshot DB lokal + journal entry active + restore + journal entry cleaned), execute spec dan verify hijau, lalu manual UAT 3 skenario via browser localhost:5277 untuk gate Wave 2 selesai sebelum commit + push.

Purpose: Validate Plan 01 controller patch — confirm 3 endpoint cascade EditLogs benar-benar resolve repro Dev (Session Id 2+5 "Gagal menghapus assessment"). Tanpa Wave 2, Plan 01 hanya verified static (grep/build); E2E + manual UAT memberikan runtime confidence di DB lokal + browser sebelum IT promo ke Dev.

Output: Spec file 3 test green, audit log row verified mengandung `EditLogsCount={N}` token, journal entry cleaned, manual UAT checklist tick 3/3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-CONTEXT.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-RESEARCH.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-PATTERNS.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-VALIDATION.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-01-SUMMARY.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md
@docs/SEED_JOURNAL.md
@tests/playwright.config.ts
@tests/package.json
@tests/helpers/accounts.ts
@tests/e2e/edit-peserta-answers.spec.ts

<interfaces>
<!-- Existing test infrastructure (Phase 311+ Playwright TS). REUSE — jangan extract helper baru. -->

From `tests/helpers/accounts.ts`:
```typescript
export type AccountKey = 'admin' | 'hc' | 'coach' | 'coachee' | ...;
export const accounts: Record<AccountKey, { email: string; password: string; role: string }> = {
  admin: { email: 'admin@pertamina.com', password: '123456', role: 'Admin' },
  // ...
};
```

From `tests/playwright.config.ts:13`:
```typescript
baseURL: 'http://localhost:5277',  // MUST stay — Pitfall 5 protection
testDir: './e2e',
projects: [{ name: 'chromium', use: { browserName: 'chromium', ... } }],
```

From `tests/e2e/edit-peserta-answers.spec.ts:17-42` (THE TEMPLATE — Phase 321 pattern):
```typescript
import { test, expect, type Page } from '@playwright/test';
import { accounts, AccountKey } from '../helpers/accounts';

const SESSION_COMPLETED_PASS = parseInt(process.env.SESSION_COMPLETED_PASS ?? '0', 10);

async function loginAny(page: Page, accountKey: AccountKey) {
  const { email, password } = accounts[accountKey];
  await page.goto('/Account/Login');
  await page.fill('input[name="email"]', email);
  await page.fill('input[name="password"]', password);
  await Promise.all([
    page.waitForURL(url => !url.toString().includes('/Account/Login'), { timeout: 15_000 }),
    page.click('button[type="submit"]'),
  ]);
}

test.describe('Phase 321 — Edit Peserta Answers', () => {
  test.beforeAll(() => {
    if (!SESSION_COMPLETED_PASS) {
      throw new Error('Set SESSION_COMPLETED_PASS env var ...');
    }
  });
  // tests ...
});
```

From `docs/SEED_WORKFLOW.md §5` (sqlcmd lifecycle):
```bash
# BACKUP (Step 2 — sebelum INSERT seed)
sqlcmd -S "localhost\SQLEXPRESS" -E -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Program Files\Microsoft SQL Server\MSSQL17.SQLEXPRESS\MSSQL\Backup\HcPortalDB_Dev-pre323-{ts}.bak' WITH INIT"

# RESTORE (Step 6 — setelah test cleanup)
sqlcmd -S "localhost\SQLEXPRESS" -E -Q "USE master; ALTER DATABASE HcPortalDB_Dev SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE HcPortalDB_Dev FROM DISK='<path>.bak' WITH REPLACE; ALTER DATABASE HcPortalDB_Dev SET MULTI_USER;"

# Audit log inspection
sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "SELECT TOP 5 ActionType, Description, CreatedAt FROM AuditLogs WHERE ActionType LIKE 'Delete%' ORDER BY CreatedAt DESC"
```

From `docs/SEED_JOURNAL.md` table header:
```markdown
| Tanggal | Phase | Klasifikasi | Tujuan | Dampak (entitas tersentuh) | Snapshot file | Status |
```

Seed SQL template (RESEARCH.md §Code Examples):
```sql
-- Query untuk pick session target (no-edits, with-edits, group-rep)
SELECT TOP 5 s.Id, s.Title, s.Status, s.LinkedGroupId,
       (SELECT COUNT(*) FROM AssessmentEditLogs e WHERE e.AssessmentSessionId = s.Id) AS EditLogCount
FROM AssessmentSessions s
WHERE s.Status IN ('Completed', 'Open')
ORDER BY s.Id DESC;

-- INSERT temporary AssessmentEditLog untuk SESSION_WITH_EDITS
DECLARE @SessionId INT = <set manually>;
DECLARE @QuestionId INT = (
  SELECT TOP 1 pq.Id
  FROM AssessmentPackages p
  JOIN PackageQuestions pq ON pq.AssessmentPackageId = p.Id
  WHERE p.AssessmentSessionId = @SessionId
);
INSERT INTO AssessmentEditLogs (
  AssessmentSessionId, PackageQuestionId,
  QuestionTextSnapshot, OldAnswerJson, OldAnswerTextSnapshot,
  NewAnswerJson, NewAnswerTextSnapshot,
  OldScore, NewScore, OldIsPassed, NewIsPassed,
  ActorUserId, ActorName, ActorRole,
  EditedAt, ReasonCode, ReasonText
) VALUES (
  @SessionId, @QuestionId,
  '[P323 SEED] Question snapshot temporary', '[]', '[P323 SEED] Old',
  '[]', '[P323 SEED] New',
  NULL, NULL, NULL, NULL,
  'p323-seed-user', '[P323 SEED] Seed User', 'Admin',
  GETUTCDATE(), 'BugSistem', '[P323 SEED] Temporary seed untuk smoke test'
);
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1a (Wave 0 probe): Probe selector partial views + pick 3 session ID + lock env vars</name>
  <files>
    (no file write — manual investigation + env var locking + scratch notes)
  </files>
  <read_first>
    - Views/Admin/ManageAssessment.cshtml + Views/Admin/Partials/ManageAssessmentTab_Assessment.cshtml (probe button selector — "Hapus" / "Hapus Grup" / "Hapus Pre-Post" — Open Question #1 RESEARCH.md RESOLVED)
    - tests/e2e/manage-assessment-filter.spec.ts (existing spec navigating same page — possible selector reference)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-CONTEXT.md (D-03 spec scope)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-RESEARCH.md §"Validation Architecture" + §"Common Pitfalls" 4/5/6 (env var, baseURL, FK valid)
  </read_first>
  <what-built>
    Plan 01 (3 endpoint controller patch) sudah PASS dotnet build + grep verification.

    Task 1a ini = INVESTIGASI manual yang butuh **human decision** untuk pick 3 session ID dari output `SELECT TOP 10` (karena tidak ada heuristik deterministic — ID drift antar lokal DB; pick session yang exists + sehat). Claude prepare query + tampilkan output, human pilih + set env var via shell export sebelum Task 1b lanjut.

    Output gate: 3 env var `P323_SESSION_NO_EDITS` / `P323_SESSION_WITH_EDITS` / `P323_GROUP_REP_ID` locked di shell session + 3 selector string documented di scratch notes.
  </what-built>
  <how-to-verify>
    **Pre-condition:** `dotnet run` aktif di `http://localhost:5277` (atau setidaknya DB lokal `HcPortalDB_Dev` accessible via sqlcmd).

    **Sub-step A1 — Probe selector partial views (manual investigation — 10 menit):**

    Claude akan baca `Views/Admin/ManageAssessment.cshtml` + partial `ManageAssessmentTab_*.cshtml` (post Phase 322 split) dan output:
    - Selector tombol delete single: `<actual selector found>` (e.g., `button[data-action="delete-assessment"]` atau `form[action*="DeleteAssessment"] button[type="submit"]`)
    - Selector tombol "Hapus Grup": `<actual selector found>`
    - Selector tombol "Hapus Pre-Post": `<actual selector found>` (kalau ada UI surface; kalau tidak, skip — coverage code-review-only per Open Question #2 RESOLVED)
    - Konfirmasi modal pattern: Bootstrap modal / SweetAlert / native confirm? (affect Promise.all wait pattern di spec)

    Catat 3 selector string di scratch notes — akan jadi input Task 1b Sub-step D.

    **Sub-step A2 — Run SELECT query identify 3 session ID kandidat:**

    Claude jalankan:
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "SET NOCOUNT ON; SELECT TOP 10 s.Id, s.Title, s.Status, s.LinkedGroupId, (SELECT COUNT(*) FROM AssessmentEditLogs e WHERE e.AssessmentSessionId = s.Id) AS EditLogCount FROM AssessmentSessions s WHERE s.Status IN ('Completed','Open') ORDER BY s.Id DESC"
    ```

    Output akan menampilkan 10 row `Id | Title | Status | LinkedGroupId | EditLogCount`.

    **Sub-step A3 — User pick 3 session ID + set env var:**

    User (human) review output dan pilih:
    - `P323_SESSION_NO_EDITS` — session existing dengan `EditLogCount=0` (untuk skenario a no-regression)
    - `P323_SESSION_WITH_EDITS` — session existing dengan `EditLogCount=0` (akan di-seed jadi ≥1 di Task 1b)
    - `P323_GROUP_REP_ID` — session representative grup (`LinkedGroupId IS NOT NULL` ATAU Title+Schedule grouping ≥1 sibling)

    User set env var via shell export atau write ke `.env.phase323` temp file. Pilihan teknis (user pilih salah satu):

    **Opsi 1 — Shell export (persistent dalam shell session):**
    ```bash
    export P323_SESSION_NO_EDITS=<pilihan_user>
    export P323_SESSION_WITH_EDITS=<pilihan_user>
    export P323_GROUP_REP_ID=<pilihan_user>
    ```

    **Opsi 2 — Temp env file (persistent across shell — recommended jika resume di session berbeda):**
    ```bash
    cat > .env.phase323 <<EOF
    P323_SESSION_NO_EDITS=<pilihan_user>
    P323_SESSION_WITH_EDITS=<pilihan_user>
    P323_GROUP_REP_ID=<pilihan_user>
    EOF
    ```
    (Task 1b + Task 3 nanti `source .env.phase323` sebelum run command.)

    **User wajib confirm 3 env var sudah ter-set sebelum lanjut Task 1b.**

    Verify via:
    ```bash
    echo "NO_EDITS=$P323_SESSION_NO_EDITS WITH_EDITS=$P323_SESSION_WITH_EDITS GROUP_REP=$P323_GROUP_REP_ID"
    # Expect: NO_EDITS=<n> WITH_EDITS=<m> GROUP_REP=<k> (semua > 0)
    ```

    Atau dari file:
    ```bash
    cat .env.phase323
    # Expect: 3 baris dengan value integer > 0
    ```
  </how-to-verify>
  <action>
See <how-to-verify> block above. This is a checkpoint task — Claude prepare query output + partial view excerpts; human pick 3 session ID + lock env var via shell export atau `.env.phase323` file + dokumentasi 3 selector string sebagai scratch notes untuk Task 1b consumption.
  </action>
  <verify>
    <automated>echo "Env var probe:"; [ -n "$P323_SESSION_NO_EDITS" ] && [ -n "$P323_SESSION_WITH_EDITS" ] && [ -n "$P323_GROUP_REP_ID" ] && echo "ENV_LOCKED_SHELL=OK NO_EDITS=$P323_SESSION_NO_EDITS WITH_EDITS=$P323_SESSION_WITH_EDITS GROUP_REP=$P323_GROUP_REP_ID" || ([ -f .env.phase323 ] && grep -q "P323_SESSION_NO_EDITS=[1-9]" .env.phase323 && grep -q "P323_SESSION_WITH_EDITS=[1-9]" .env.phase323 && grep -q "P323_GROUP_REP_ID=[1-9]" .env.phase323 && echo "ENV_LOCKED_FILE=OK" || echo "ENV_NOT_LOCKED — Task 1a belum selesai")</automated>
  </verify>
  <acceptance_criteria>
    - 3 env var `P323_SESSION_NO_EDITS`, `P323_SESSION_WITH_EDITS`, `P323_GROUP_REP_ID` ter-set dengan nilai integer > 0 (via shell `export` ATAU `.env.phase323` file)
    - 3 selector string documented di scratch notes (delete single + "Hapus Grup" + confirmation modal pattern) — siap di-paste ke spec body Task 1b
    - User explicit confirm 3 session ID hasil query exists di DB lokal (bukan asumsi)
    - (Optional) Output sqlcmd `SELECT TOP 10` ditangkap sebagai snapshot di scratch notes untuk audit traceability
  </acceptance_criteria>
  <done>
    Wave 0 probe complete: 3 env var locked + 3 selector string documented. Siap untuk Task 1b (BACKUP DB + INSERT seed + write spec file).
  </done>
  <resume-signal>
    Type "approved" + confirm 3 env var ter-set (paste output `echo "NO_EDITS=$P323_SESSION_NO_EDITS ..."` atau `cat .env.phase323`) sebelum lanjut Task 1b.

    Type "failed-probe" + describe issue (e.g., partial view tidak ada button delete, atau DB query tidak return session yang sesuai kriteria) — replan selector strategy atau pick alternative session source.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1b (Wave 0 setup): Snapshot DB + INSERT seed + write spec file body</name>
  <files>
    tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts
    docs/SEED_JOURNAL.md
  </files>
  <read_first>
    - tests/e2e/edit-peserta-answers.spec.ts (lines 1-100 — Phase 321 spec template, exact pattern reuse)
    - tests/helpers/accounts.ts (verify accounts.admin shape)
    - tests/playwright.config.ts (verify baseURL stays http://localhost:5277 — Pitfall 5 defensive)
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-PATTERNS.md §2 (Playwright spec pattern) + §3 (SEED_JOURNAL format)
    - docs/SEED_WORKFLOW.md (klasifikasi temporary + sqlcmd template + journal flow)
    - Scratch notes Task 1a (3 selector string + 3 session ID locked di env var)
  </read_first>
  <action>
3 sub-step urut. Input dari Task 1a: env var `P323_SESSION_NO_EDITS` / `P323_SESSION_WITH_EDITS` / `P323_GROUP_REP_ID` sudah locked + 3 selector string sudah dicatat di scratch notes.

Kalau env var via `.env.phase323` file: `source .env.phase323` di awal command.

---

**Sub-step B — DB snapshot (WAJIB sebelum INSERT seed per SEED_WORKFLOW):**

```bash
TS=$(date +%Y%m%d-%H%M%S)
sqlcmd -S "localhost\SQLEXPRESS" -E -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Program Files\Microsoft SQL Server\MSSQL17.SQLEXPRESS\MSSQL\Backup\HcPortalDB_Dev-pre323-${TS}.bak' WITH INIT"
```

Verify backup file exists:
```bash
ls -la "C:\Program Files\Microsoft SQL Server\MSSQL17.SQLEXPRESS\MSSQL\Backup\HcPortalDB_Dev-pre323-"*.bak
```

Catat path file backup (full path) — akan dipakai di SEED_JOURNAL entry Sub-step C + restore command Task 4.

**Sub-step C — Append SEED_JOURNAL.md entry (status `active`) + INSERT seed pakai session ID dari Task 1a:**

Append baris baru ke `docs/SEED_JOURNAL.md` mengikuti format 7-kolom existing (lihat 323-PATTERNS.md §3.3):

```markdown
| 2026-05-26 | 323 | temporary + local-only | Smoke test cascade AssessmentEditLogs (3 skenario: no-edits / with-edits / group-mixed) — verify FK Restrict cleanup di DeleteAssessment + DeleteAssessmentGroup + DeletePrePostGroup | AssessmentEditLogs(+1-3 row) prefix `[P323 SEED]` FK ke 2-3 existing AssessmentSession id; tidak insert Session/Package/Question baru (pakai existing) | C:/Program Files/Microsoft SQL Server/MSSQL17.SQLEXPRESS/MSSQL/Backup/HcPortalDB_Dev-pre323-${TS}.bak | active |
```

INSERT seed pakai session ID dari env var Task 1a (`$P323_SESSION_WITH_EDITS` + 1-2 sibling di grup `$P323_GROUP_REP_ID`):

```bash
# Source env var jika via file
[ -f .env.phase323 ] && source .env.phase323

# INSERT seed untuk SESSION_WITH_EDITS
sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "
DECLARE @SessionId INT = $P323_SESSION_WITH_EDITS;
DECLARE @QuestionId INT = (
  SELECT TOP 1 pq.Id
  FROM AssessmentPackages p
  JOIN PackageQuestions pq ON pq.AssessmentPackageId = p.Id
  WHERE p.AssessmentSessionId = @SessionId
);
INSERT INTO AssessmentEditLogs (
  AssessmentSessionId, PackageQuestionId,
  QuestionTextSnapshot, OldAnswerJson, OldAnswerTextSnapshot,
  NewAnswerJson, NewAnswerTextSnapshot,
  OldScore, NewScore, OldIsPassed, NewIsPassed,
  ActorUserId, ActorName, ActorRole,
  EditedAt, ReasonCode, ReasonText
) VALUES (
  @SessionId, @QuestionId,
  '[P323 SEED] Question snapshot temporary', '[]', '[P323 SEED] Old',
  '[]', '[P323 SEED] New',
  NULL, NULL, NULL, NULL,
  'p323-seed-user', '[P323 SEED] Seed User', 'Admin',
  GETUTCDATE(), 'BugSistem', '[P323 SEED] Temporary seed untuk smoke test'
);
SELECT @@ROWCOUNT AS RowsInserted;
"
```

Ulangi untuk 1-2 sibling session di grup `$P323_GROUP_REP_ID` (variasi skenario c — campuran no-edits + edits). Pick sibling via:
```bash
sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "
SELECT Id FROM AssessmentSessions WHERE LinkedGroupId = (SELECT LinkedGroupId FROM AssessmentSessions WHERE Id = $P323_GROUP_REP_ID) AND Id != $P323_GROUP_REP_ID;
"
```
Lalu re-run INSERT template di atas dengan `@SessionId = <sibling_id>`.

Verify INSERT sukses (FK valid — Pitfall 6 protection):
```bash
sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "SELECT COUNT(*) FROM AssessmentEditLogs WHERE ActorUserId='p323-seed-user'"
# Expect: ≥2 (1 untuk WITH_EDITS + 1+ untuk grup mixed)
```

Jika INSERT gagal (FK violation) → fix `@QuestionId` (Pitfall 6) atau pick session lain (revert ke Task 1a untuk re-pick). JANGAN proceed ke spec write sebelum seed sukses.

**Sub-step D — Write spec file `tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts`:**

Copy struktur dari `tests/e2e/edit-peserta-answers.spec.ts` (Phase 321 analog 1:1). Selector di-paste dari scratch notes Task 1a. Spec body minimum:

```typescript
// Phase 323 — Cascade AssessmentEditLogs Delete Smoke Test
// Run: cd tests && source ../.env.phase323 && npx playwright test Phase323 --headed
//   ATAU: cd tests && P323_SESSION_NO_EDITS=<x> P323_SESSION_WITH_EDITS=<y> P323_GROUP_REP_ID=<z> npx playwright test Phase323 --headed
// See: docs/SEED_JOURNAL.md Phase 323 entry untuk seed setup details
//
// Phase 323 selector probe (2026-05-26, Task 1a):
//   - Delete button: <actual selector from Task 1a scratch notes>
//   - Confirm modal: <actual pattern from Task 1a>
//   - Group delete: <actual selector from Task 1a>

import { test, expect, type Page } from '@playwright/test';
import { accounts, AccountKey } from '../helpers/accounts';

const SESSION_NO_EDITS = parseInt(process.env.P323_SESSION_NO_EDITS ?? '0', 10);
const SESSION_WITH_EDITS = parseInt(process.env.P323_SESSION_WITH_EDITS ?? '0', 10);
const SESSION_GROUP_REP = parseInt(process.env.P323_GROUP_REP_ID ?? '0', 10);

async function loginAny(page: Page, accountKey: AccountKey) {
  const { email, password } = accounts[accountKey];
  await page.goto('/Account/Login');
  await page.fill('input[name="email"]', email);
  await page.fill('input[name="password"]', password);
  await Promise.all([
    page.waitForURL(url => !url.toString().includes('/Account/Login'), { timeout: 15_000 }),
    page.click('button[type="submit"]'),
  ]);
}

test.describe('Phase 323 — Cascade AssessmentEditLogs Delete', () => {

  test.beforeAll(() => {
    // Pitfall 4 + 5 defensive
    if (!SESSION_NO_EDITS || !SESSION_WITH_EDITS || !SESSION_GROUP_REP) {
      throw new Error(
        'Set env vars P323_SESSION_NO_EDITS, P323_SESSION_WITH_EDITS, P323_GROUP_REP_ID. ' +
        'See docs/SEED_JOURNAL.md Phase 323 entry for setup (Task 1a probe step).'
      );
    }
  });

  test('a) Session no-edits → delete OK (no regression)', async ({ page }) => {
    await loginAny(page, 'admin');
    await page.goto(`/Admin/ManageAssessment`);
    // Trigger delete untuk SESSION_NO_EDITS
    // (selector dari Task 1a probe — adapt sesuai temuan markup)
    // Expected: TempData success banner (NOT "Gagal menghapus assessment")
    // Expected: row hilang dari listing setelah redirect

    // Defensive: pastikan TIDAK ada flash error "Gagal menghapus"
    const errorBanner = page.locator('text=Gagal menghapus assessment');
    await expect(errorBanner).toHaveCount(0, { timeout: 5000 });
  });

  test('b) Session 1+ edits → delete OK, EditLogs ikut terhapus', async ({ page }) => {
    await loginAny(page, 'admin');
    await page.goto(`/Admin/ManageAssessment`);
    // Trigger delete untuk SESSION_WITH_EDITS
    // CRITICAL: ini repro Dev — sebelum Plan 01 ini akan FAIL dengan "Gagal menghapus assessment"
    // Setelah Plan 01 cascade patch → success banner

    const errorBanner = page.locator('text=Gagal menghapus assessment');
    await expect(errorBanner).toHaveCount(0, { timeout: 5000 });
    // Optional: navigate verification page untuk confirm session ID gone
  });

  test('c) Group campuran sibling no-edits + edits → delete OK', async ({ page }) => {
    await loginAny(page, 'admin');
    await page.goto(`/Admin/ManageAssessment`);
    // Trigger "Hapus Grup" untuk SESSION_GROUP_REP
    // Expected: semua sibling hilang (campuran no-edits + edits dari seed Sub-step C)

    const errorBanner = page.locator('text=Gagal menghapus assessment');
    await expect(errorBanner).toHaveCount(0, { timeout: 5000 });
  });
});
```

**Constraint:**
- HARUS pakai `parseInt(process.env.XXX ?? '0', 10)` pattern — JANGAN hardcode session ID (Pitfall 4).
- HARUS pakai `input[name="email"]` lowercase (verified accounts.ts).
- JANGAN ubah `tests/playwright.config.ts` `baseURL` (Pitfall 5 defensive).
- JANGAN extract helper baru — `loginAny` inline per pattern Phase 321.
- Selector tombol Delete WAJIB dari Task 1a probe scratch notes — JANGAN copy random selector.
- Test body MINIMUM: trigger delete + assert error banner absent (most defensive — flash success exact text bervariasi).
- Env var name di spec: `P323_GROUP_REP_ID` (bukan `P323_SESSION_GROUP_REP`) — konsisten dengan locking Task 1a.
  </action>
  <verify>
    <automated>test -f tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts && echo "SPEC_EXIST=OK"; grep -c "test('" tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts; grep -c "P323_SESSION_\|P323_GROUP_REP" tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts; grep -c "Phase 323" docs/SEED_JOURNAL.md; sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SET NOCOUNT ON; SELECT COUNT(*) FROM AssessmentEditLogs WHERE ActorUserId='p323-seed-user'" 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts` exists (`test -f` exit 0)
    - Spec mengandung minimal 3 `test('` blocks (untuk skenario a/b/c)
    - Spec mengandung minimal 3 reference ke env var `P323_SESSION_` / `P323_GROUP_REP` (untuk NO_EDITS, WITH_EDITS, GROUP_REP)
    - `docs/SEED_JOURNAL.md` mengandung baris baru dengan `Phase 323` token, status `active`, path backup file `HcPortalDB_Dev-pre323-*.bak`
    - DB lokal mengandung minimal 1 row `AssessmentEditLogs WHERE ActorUserId='p323-seed-user'` (seed temporary inserted sukses — FK valid)
    - Backup file `HcPortalDB_Dev-pre323-*.bak` exists di backup directory SQL Server (snapshot lifecycle dimulai)
    - Spec NO hardcoded session ID (grep `const SESSION.*= [0-9]+` zero matches in spec file)
    - Spec USES `input[name="email"]` lowercase (1+ occurrence)
    - Spec contains `loginAny` function definition (inline, bukan import — Phase 321 pattern)
    - `tests/playwright.config.ts` baseURL UNCHANGED (`baseURL: 'http://localhost:5277'` masih ada — Pitfall 5)
    - Selector di spec body match dengan probe Task 1a scratch notes (visual review)
  </acceptance_criteria>
  <done>
    Wave 0 setup complete: spec file dengan 3 test draft created pakai selector + env var dari Task 1a, seed 1-3 row AssessmentEditLog inserted dengan FK valid, journal entry active, backup file present. Siap untuk Task 3 execution (run spec + verify hijau).
  </done>
</task>

<task type="auto">
  <name>Task 3: Run Playwright spec + verify 3 test green + verify audit log token</name>
  <files>tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts</files>
  <read_first>
    - tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts (hasil Task 1b)
    - Env var dari Task 1a (`$P323_SESSION_NO_EDITS` / `$P323_SESSION_WITH_EDITS` / `$P323_GROUP_REP_ID`) — via shell export ATAU `source .env.phase323`
    - .planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-VALIDATION.md (sampling rate + per-task verification map row 323-02-01..05)
  </read_first>
  <action>
NOTE: Env var `P323_SESSION_NO_EDITS` / `P323_SESSION_WITH_EDITS` / `P323_GROUP_REP_ID` diturunkan dari Task 1a (session-persistent shell variable atau `source .env.phase323` temp file). Kalau env var belum di-set di shell session ini, jalankan `source .env.phase323` dulu sebelum command Sub-step B di bawah.

3 sub-step:

**Sub-step A — Pre-flight: pastikan app running di lokal + env var locked:**

```bash
# Pastikan dotnet run aktif di port 5277 (separate terminal — atau cek)
curl -sf http://localhost:5277/ -o /dev/null && echo "APP_UP" || echo "APP_DOWN — start dotnet run dulu"

# Source env var dari Task 1a (kalau via file)
[ -f .env.phase323 ] && source .env.phase323

# Verify env var locked (PRE-FLIGHT GUARD per checker warning #2)
[ "$P323_SESSION_NO_EDITS" != "0" ] && [ "$P323_SESSION_NO_EDITS" != "" ] \
  && [ "$P323_SESSION_WITH_EDITS" != "0" ] && [ "$P323_SESSION_WITH_EDITS" != "" ] \
  && [ "$P323_GROUP_REP_ID" != "0" ] && [ "$P323_GROUP_REP_ID" != "" ] \
  || { echo "ERROR: env vars belum di-set. Run Task 1a dulu untuk pick session ID, atau source .env.phase323 di shell ini."; exit 1; }
```

Jika APP DOWN: start `dotnet run` di background atau separate shell sebelum lanjut.

Jika ENV VARS belum locked: STOP — kembali ke Task 1a, pick session ID, set env var, baru re-run Task 3.

**Sub-step B — Run spec dengan env var (post pre-flight guard PASS):**

```bash
cd tests && \
P323_SESSION_NO_EDITS=$P323_SESSION_NO_EDITS \
P323_SESSION_WITH_EDITS=$P323_SESSION_WITH_EDITS \
P323_GROUP_REP_ID=$P323_GROUP_REP_ID \
npx playwright test Phase323 --reporter=list 2>&1 | tee /tmp/p323-run.log
cd ..
```

Expected output:
- 3 test pass (no skipped, no failed)
- Total runtime < 60s (per VALIDATION.md sampling rate)

Jika ada test FAIL:
- Inspect screenshot di `tests/playwright-report/` (auto-generated)
- Cek apakah selector dari Task 1a probe salah — adjust spec → re-run
- Cek apakah session ID drift (DB state changed) — re-query Task 1a sub-step A2
- Cek apakah Plan 01 controller patch sudah commit — `git log --oneline | head -5` confirm 323-01 commit present
- JANGAN PROCEED ke Task 4 sebelum 3/3 test green

**Sub-step C — Verify audit log token (manual DB query — CASCADE-01 #4):**

```bash
sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "
SELECT TOP 5 ActionType, LEFT(Description, 300) AS Description, CreatedAt
FROM AuditLogs
WHERE ActionType LIKE 'Delete%'
  AND CreatedAt >= DATEADD(MINUTE, -10, GETUTCDATE())
ORDER BY CreatedAt DESC;
"
```

Expected: minimal 3 row dengan `Description` mengandung token `EditLogsCount=N` (N=0 untuk skenario a, N≥1 untuk skenario b, N≥1 untuk skenario c). Snapshot output sebagai bukti CASCADE-01 #4.

Jika token TIDAK MUNCUL di Description: Plan 01 Task tertentu kemungkinan miss audit description extension — re-check `grep "EditLogsCount=" Controllers/AssessmentAdminController.cs` ≥3.

**Sub-step D — Full regression smoke (no new failure — VALIDATION.md sign-off):**

```bash
cd tests && npx playwright test --reporter=list 2>&1 | tee /tmp/p323-regression.log
cd ..
grep -E "passed|failed|skipped" /tmp/p323-regression.log | tail -3
```

Expected: total pass count tidak menurun dibanding baseline pre-Phase-323 (Phase 322 run baseline). Tidak boleh ada failure baru di spec lain karena Plan 01 hanya tambah cascade block (zero risk regression di luar 3 endpoint delete).

Jika ada NEW failure (bukan flaky) → investigate; kemungkinan side-effect tidak terduga.
  </action>
  <verify>
    <automated>[ -f .env.phase323 ] && source .env.phase323; [ "$P323_SESSION_NO_EDITS" != "0" ] && [ "$P323_SESSION_NO_EDITS" != "" ] && [ "$P323_SESSION_WITH_EDITS" != "0" ] && [ "$P323_SESSION_WITH_EDITS" != "" ] && [ "$P323_GROUP_REP_ID" != "0" ] && [ "$P323_GROUP_REP_ID" != "" ] || { echo "ERROR: env vars belum di-set. Run Task 1a dulu untuk pick session ID."; exit 1; }; cd tests && P323_SESSION_NO_EDITS=$P323_SESSION_NO_EDITS P323_SESSION_WITH_EDITS=$P323_SESSION_WITH_EDITS P323_GROUP_REP_ID=$P323_GROUP_REP_ID npx playwright test Phase323 --reporter=list 2>&1 | tee /tmp/p323-final.log; cd ..; grep -c "passed" /tmp/p323-final.log; ! grep -q "failed" /tmp/p323-final.log && echo "NO_FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight guard PASS: env var `P323_SESSION_NO_EDITS` / `P323_SESSION_WITH_EDITS` / `P323_GROUP_REP_ID` semua ter-set integer > 0 (bukan fallback `0`)
    - `cd tests && npx playwright test Phase323 --reporter=list` exit code 0
    - Output mengandung "3 passed" (atau equivalent — 3 test all green)
    - Output TIDAK mengandung "failed" atau "FAIL" untuk Phase323 specs
    - SQL query `SELECT TOP 5 ... FROM AuditLogs WHERE ActionType LIKE 'Delete%' ORDER BY CreatedAt DESC` returns ≥3 row recent (last 10 min)
    - Sample Description di ≥3 row mengandung substring `EditLogsCount=` (regex `EditLogsCount=\d+`)
    - Full regression `npx playwright test` total pass count ≥ baseline pre-Phase-323 (no new failure)
    - Manual review: kalau scenario b lulus → Phase 323 ENDPOINT WORKS as designed (repro Dev resolved)
  </acceptance_criteria>
  <done>
    3/3 Playwright test Phase323 green, audit log row verified mengandung token `EditLogsCount=N` (CASCADE-01 #4 satisfied via DB query manual), full regression no new failure. Spec validated — Plan 01 controller patch behaves correctly E2E. Siap untuk Task 4 (manual UAT + cleanup + commit).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT 3 skenario via browser localhost + cleanup seed + commit + handoff IT</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - Hasil Task 3 (Playwright green + audit log verified)
    - docs/DEV_WORKFLOW.md (push checklist + IT notify protocol)
    - docs/SEED_WORKFLOW.md §6 (RESTORE template)
    - Path backup file dari SEED_JOURNAL.md Phase 323 entry (kolom Snapshot file — di-tulis Task 1b Sub-step C)
  </read_first>
  <what-built>
    Plan 01 (3 endpoint controller patch) sudah PASS dotnet build + grep verification.
    Plan 02 Task 1a (probe + env lock) + Task 1b (seed + spec) + Task 3 (run spec + audit verify) sudah PASS.

    Ini gate FINAL — manual browser UAT 3 skenario di localhost:5277 (cross-validation E2E Playwright) + cleanup seed lifecycle (RESTORE DB + journal status cleaned) + commit + notify IT.
  </what-built>
  <how-to-verify>
    **Pre-condition:** `dotnet run` aktif di `http://localhost:5277`. Login `admin@pertamina.com` / `123456`. Env var `$P323_SESSION_NO_EDITS` / `$P323_SESSION_WITH_EDITS` / `$P323_GROUP_REP_ID` masih locked (atau `source .env.phase323` ulang).

    **UAT Step 1 — Skenario (a) Session no-edits:**
    1. Navigate `http://localhost:5277/Admin/ManageAssessment`
    2. Cari row dengan SESSION ID = `$P323_SESSION_NO_EDITS` value (atau pilih session lain yang 0 edit logs)
    3. Klik tombol "Hapus" / Delete
    4. Confirm modal → OK
    5. **Expected:** Flash banner success (hijau) — TIDAK boleh "Gagal menghapus assessment"
    6. Refresh page — row sudah hilang dari listing

    **UAT Step 2 — Skenario (b) Session with edits (REPRO DEV — KRITIS):**
    1. Re-seed kalau perlu (kalau spec sudah hapus): re-run sqlcmd INSERT seed dari Task 1b Sub-step C
    2. Navigate `http://localhost:5277/Admin/ManageAssessment`
    3. Cari row dengan SESSION ID = `$P323_SESSION_WITH_EDITS` (mengandung edit log seed)
    4. Klik tombol "Hapus" / Delete
    5. Confirm modal → OK
    6. **Expected:** Flash banner SUCCESS — kalau "Gagal menghapus assessment" muncul = REGRESSION (Plan 01 belum benar), STOP commit
    7. Refresh page — row hilang

    **UAT Step 3 — Skenario (c) Group campuran:**
    1. Re-seed sibling edit logs kalau perlu
    2. Navigate `/Admin/ManageAssessment`
    3. Cari grup yang mengandung `$P323_GROUP_REP_ID`
    4. Klik tombol "Hapus Grup" (atau equivalent)
    5. Confirm modal → OK
    6. **Expected:** Flash banner SUCCESS, semua sibling grup hilang

    **UAT Step 4 — Cleanup seed (SEED_WORKFLOW §6):**
    ```bash
    # Pakai path backup file dari SEED_JOURNAL Phase 323 entry kolom Snapshot file
    sqlcmd -S "localhost\SQLEXPRESS" -E -Q "USE master; ALTER DATABASE HcPortalDB_Dev SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE HcPortalDB_Dev FROM DISK='C:\Program Files\Microsoft SQL Server\MSSQL17.SQLEXPRESS\MSSQL\Backup\HcPortalDB_Dev-pre323-<TS>.bak' WITH REPLACE; ALTER DATABASE HcPortalDB_Dev SET MULTI_USER;"

    # Verify restore
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "SELECT COUNT(*) FROM AssessmentEditLogs WHERE ActorUserId='p323-seed-user'"
    # Expect: 0 (seed wiped via restore)
    ```

    **UAT Step 5 — Update journal status `active` → `cleaned`:**

    Edit `docs/SEED_JOURNAL.md` row Phase 323 — ganti kolom Status terakhir dari `active` ke `cleaned`. Atau append cleanup timestamp.

    **UAT Step 6 — Cleanup env var file (jika dipakai):**
    ```bash
    # Remove .env.phase323 (temp file, jangan commit)
    [ -f .env.phase323 ] && rm .env.phase323 && echo "ENV_FILE_REMOVED"
    ```
    (Pastikan `.env.phase323` masuk `.gitignore` — kalau belum, tambahkan dulu sebelum commit.)

    **UAT Step 7 — Commit + notify IT:**

    Per CLAUDE.md Develop Workflow:
    1. `git add Controllers/AssessmentAdminController.cs tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts docs/SEED_JOURNAL.md .planning/phases/323-*/323-*-SUMMARY.md`
    2. Commit message format mirror Phase 312/322 style: `fix(323): cascade AssessmentEditLogs in 3 delete endpoints + smoke spec`
    3. `git push origin main`
    4. Notify Team IT: commit hash + flag `NO MIGRATION` (CASCADE-01 #7 explicit) + retry hapus AssessmentSession Id 2 + Id 5 di Dev via UI Admin
  </how-to-verify>
  <acceptance_criteria>
    - UAT Step 1 PASS (skenario a — flash success, row gone)
    - UAT Step 2 PASS (skenario b — flash success, ini gate KRITIS — kalau gagal STOP commit, revisit Plan 01)
    - UAT Step 3 PASS (skenario c — flash success, sibling gone)
    - UAT Step 4 PASS (DB restored — `SELECT COUNT(*) FROM AssessmentEditLogs WHERE ActorUserId='p323-seed-user'` = 0)
    - UAT Step 5 PASS (journal row Phase 323 status = `cleaned`)
    - UAT Step 6 PASS (`.env.phase323` removed dari working dir, tidak ter-commit)
    - UAT Step 7 PASS (commit pushed, IT notified dengan commit hash + flag no-migration)
  </acceptance_criteria>
  <action>
See <how-to-verify> block above. This is a checkpoint task — execution is human-driven via 7 UAT steps (skenario a + b + c browser UAT + DB restore + journal cleaned + env file cleanup + commit/push/notify IT). Claude prepares pre-conditions (re-seed if needed, restore command ready) but human executes browser clicks + verifies flash banners + runs sqlcmd restore.
  </action>
  <verify>
    <automated>test -f docs/SEED_JOURNAL.md && grep -q "323.*cleaned" docs/SEED_JOURNAL.md && echo JOURNAL_CLEANED=OK; ! test -f .env.phase323 && echo ENV_FILE_CLEANED=OK; git log --oneline -5 | grep -q 323 && echo COMMIT_PRESENT=OK</automated>
  </verify>
  <done>
    3/3 UAT skenario PASS via browser localhost:5277, DB lokal restored (seed wiped), journal entry status cleaned, env file removed, commit pushed origin/main dengan format `fix(323): ...`, IT notified dengan commit hash + flag NO MIGRATION + instruksi retry hapus Session Id 2+5 di Dev. Phase 323 SHIPPED.
  </done>
  <resume-signal>
    Type "approved" jika 3/3 UAT skenario PASS + DB restored + journal cleaned + env file cleaned + commit pushed + IT notified.

    Type "failed-uat-stepX" + describe issue jika ada step yang gagal — STOP commit, revert kalau perlu, return ke Plan 01 untuk fix.

    Type "cleanup-only" jika butuh skip UAT (e.g., spec already cross-validated cukup) tapi tetap eksekusi DB RESTORE + journal cleaned + commit (rare; defer ke user judgment).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright Node.js process → Browser (Chromium) → ASP.NET Core (localhost:5277) | Local-only — `baseURL` hardcoded `http://localhost:5277` per playwright.config.ts (Pitfall 5 protection) |
| sqlcmd CLI → SQL Server Express localhost\SQLEXPRESS | Windows Integrated Security `-E` — no credential di-pass via CLI args |
| Seed temporary AssessmentEditLog → AssessmentSession FK | Restrict FK juga di-cek di Pitfall 6 (FK valid wajib sebelum INSERT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-323-07 | Information Disclosure | Seed temporary data lupa di-cleanup → carry-over ke development session berikutnya | mitigate | Task 4 UAT Step 4 (RESTORE DB) + Step 5 (journal `cleaned`) — gate WAJIB. SEED_WORKFLOW Step 4 explicit. |
| T-323-08 | Tampering | Spec accidentally hit DB Dev (10.55.3.3) via salah env var | mitigate | (a) `tests/playwright.config.ts` baseURL hardcoded `localhost:5277` UNCHANGED per acceptance criteria Task 1b; (b) Pitfall 5 prevention noted di RESEARCH.md; (c) Defensive: spec tidak punya code yang switch baseURL berdasarkan env var. |
| T-323-09 | Tampering | sqlcmd query salah pakai parameter substitution (SQL injection via shell var) | accept | Session ID adalah integer; sqlcmd command pakai server-side `DECLARE @X INT = <value>` — bukan dynamic SQL. Shell substitution risk minimal karena value berasal dari `SELECT` query controlled, bukan user input external. |
| T-323-10 | Repudiation | Commit log tanpa SEED_JOURNAL update → audit trail terputus | mitigate | Task 4 UAT Step 5 + Step 7 — `git add docs/SEED_JOURNAL.md` masuk commit (acceptance criteria). |
| T-323-11 | Elevation of Privilege | Test login admin@pertamina.com credentials accidentally committed ke spec hardcoded | accept | Credentials sudah di `tests/helpers/accounts.ts` (existing — bukan secret production, kredensial UAT lokal per memory note). Spec import dari accounts.ts (Phase 321 pattern), bukan hardcode di spec. |
| T-323-12 | Information Disclosure | `.env.phase323` temp file ter-commit ke git (mengandung session ID — bukan secret tapi sampah) | mitigate | Task 4 UAT Step 6 — wajib `rm .env.phase323` sebelum `git add`. Plus ensure `.env.phase323` ada di `.gitignore` (atau pattern `.env.*`). |

**Risk summary:** Plan 02 menambah 1 spec file + 1 journal entry — zero production code change. Threat surface = test infrastructure + DB lokal lifecycle. Semua mitigasi via disiplin SEED_WORKFLOW + Playwright config preserve + env file cleanup. Inherited dari Phase 313/315/321 pattern (production-tested).
</threat_model>

<verification>
## Plan-level verification

```bash
# 1. Spec file exists + structure
test -f tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts && echo "OK: spec file exists"
grep -c "test('" tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts
# Expect: ≥3

# 2. Spec uses env var (no hardcode)
grep -E "const SESSION_[A-Z_]+ = parseInt\(process\.env\." tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts | wc -l
# Expect: ≥3

# 3. Spec login pattern follows Phase 321
grep -c "loginAny" tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts
# Expect: ≥4 (1 declaration + 3 call di 3 test)

# 4. baseURL UNCHANGED (Pitfall 5)
grep "baseURL: 'http://localhost:5277'" tests/playwright.config.ts | wc -l
# Expect: 1

# 5. Journal entry
grep -c "323 | temporary + local-only" docs/SEED_JOURNAL.md
# Expect: ≥1

# 6. Journal status cleaned (after Task 4 cleanup)
grep "323.*cleaned" docs/SEED_JOURNAL.md
# Expect: 1+ row (after UAT Step 5)

# 7. Playwright Phase323 green
cd tests && npx playwright test Phase323 --reporter=list 2>&1 | grep -E "passed|failed"
# Expect: "3 passed" (or "X passed" where X ≥ 3) AND no "failed"

# 8. DB clean (seed wiped)
sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "SELECT COUNT(*) FROM AssessmentEditLogs WHERE ActorUserId='p323-seed-user'"
# Expect: 0 (post RESTORE)

# 9. Audit log token verified (Task 3 Sub-step C)
sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "SELECT TOP 5 Description FROM AuditLogs WHERE Description LIKE '%EditLogsCount=%' ORDER BY CreatedAt DESC"
# Expect: ≥3 row dengan token present

# 10. Env file cleaned
! test -f .env.phase323 && echo "OK: env file cleaned"
```
</verification>

<success_criteria>

1. ✅ `tests/e2e/Phase323_CascadeAssessmentEditLogs.spec.ts` exist dengan 3 test (no-edits / with-edits / group-mixed) — D-03 LOCKED
2. ✅ Spec pakai env var pattern `P323_SESSION_*` / `P323_GROUP_REP_ID` (no hardcode session ID — Pitfall 4)
3. ✅ Spec REUSE `accounts.admin` + inline `loginAny` (Phase 321 pattern — no helper extraction)
4. ✅ `tests/playwright.config.ts` baseURL UNCHANGED (`http://localhost:5277` — Pitfall 5)
5. ✅ Seed temporary AssessmentEditLog di-INSERT dengan FK valid (Pitfall 6 avoided) — `docs/SEED_JOURNAL.md` entry status `active`
6. ✅ Playwright `npx playwright test Phase323` exit 0 dengan 3/3 test green (post pre-flight env var guard)
7. ✅ Audit log row verified manual: Description mengandung `EditLogsCount=N` token (CASCADE-01 #4)
8. ✅ Full regression `npx playwright test` no new failure
9. ✅ Manual UAT 3 skenario via browser localhost PASS (gate CRITICAL Step 2 — skenario b repro Dev resolved)
10. ✅ DB lokal RESTORED via sqlcmd (`SELECT COUNT(*) WHERE ActorUserId='p323-seed-user'` = 0) — SEED_WORKFLOW §6
11. ✅ Journal entry status `active` → `cleaned` (SEED_WORKFLOW lifecycle complete)
12. ✅ `.env.phase323` temp file cleaned dari working dir (tidak ter-commit)
13. ✅ Commit pushed dengan message format `fix(323): cascade AssessmentEditLogs ...` + IT notified (commit hash + flag NO MIGRATION) — CLAUDE.md Develop Workflow Step 4-5

Phase 323 SHIPPED — repro evidence Dev Session Id 2+5 resolvable post IT promo.
</success_criteria>

<output>
After completion, create `.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-02-SUMMARY.md` dengan:
- Spec file path + 3 test name + runtime
- Seed lifecycle (snapshot file + INSERT row count + RESTORE verify count)
- Audit log token sample (sample Description text)
- UAT 3/3 result + browser screenshot reference (kalau ada)
- Commit hash + push status + IT notify status
- Phase 323 SHIPPED announcement + pointer ke STATE.md update
</output>
