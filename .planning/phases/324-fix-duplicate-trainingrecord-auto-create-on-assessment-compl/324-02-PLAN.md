---
phase: 324
plan: 02
type: execute
wave: 2
depends_on: ["324-01"]
files_modified:
  - tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts
  - tests/e2e/helpers/phase324.ts
autonomous: false
requirements: [DUPL-02a]
tags: [playwright, e2e, uat, regression-guard]
must_haves:
  truths:
    - "Spec file Phase324_NoDuplicateTrainingRecord.spec.ts exists dengan 7 test block (S1-S7) — file structure complete"
    - "Helper module phase324.ts exists dengan 3 helper fully implemented (NO placeholder bodies, NO `break; // placeholder` patterns)"
    - "Spec dapat dijalankan via `cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts`"
    - "S1 worker submit non-essay → /CMP/Records 1 row 'Assessment Online' (DUPL-02a criteria 2) IMPLEMENTED + GREEN"
    - "S2 PreTest skip regression guard (DUPL-02a criteria 3) IMPLEMENTED + GREEN"
    - "S3-S7 explicit deferred-to-Phase-325 via `test.skip(true, '...Phase 325...')` annotation + TODO comment referencing CONTEXT.md D-07b (DUPL-02a criteria 4)"
    - "Full spec run <=3 menit"
  artifacts:
    - path: "tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts"
      provides: "7-scenario regression guard E2E UAT untuk Phase 324 (S1+S2 implemented, S3-S7 skeleton deferred-to-Phase-325)"
      min_lines: 200
    - path: "tests/e2e/helpers/phase324.ts"
      provides: "submitNonEssayAssessment + assertRecordsRowCount + sqlcmdQueryCount helpers (login via existing tests/helpers/auth.ts)"
      exports: ["submitNonEssayAssessment", "assertRecordsRowCount", "sqlcmdQueryCount"]
  key_links:
    - from: "tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts"
      to: "tests/e2e/helpers/phase324.ts"
      via: "import"
    - from: "tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts"
      to: "tests/helpers/auth.ts"
      via: "import { login } from '../../helpers/auth' (existing helper, INFO 6 pattern compliance)"
    - from: "tests/e2e/helpers/phase324.ts"
      to: "tests/helpers/accounts.ts"
      via: "import accounts + AccountKey type (for sqlcmdQueryCount only)"
    - from: "Phase 324 code edit Plan 01"
      to: "Spec assertion (S1+S2 regression validates Plan 01 Task 1 D-01 edit)"
      via: "E2E test exercises GradingService.GradeAndCompleteAsync path post-edit"
---

<objective>
Buat Playwright E2E UAT spec untuk verifikasi 2 skenario primary regression guard (S1+S2) Phase 324 (D-07a dari CONTEXT.md) + skeleton file untuk 5 scenario lanjutan (S3-S7) yang akan di-implement di Phase 325 (D-07b deferred).

Purpose: Automated regression guard supaya code edit Plan 01 Task 1 (subtract block insert TR di GradingService.GradeAndCompleteAsync) sukses menghilangkan duplicate TR di `/CMP/Records` (S1) tanpa break PreTest existing behavior (S2). File skeleton 7-scenario menjadi handoff yang clean untuk Phase 325 fixture engineering (S3 essay, S4 AkhiriUjian, S5 AkhiriSemuaUjian, S6 Regrade Pass→Fail, S7 Regrade Fail→Pass).

Output: 1 spec file dengan 7 test block (S1+S2 GREEN, S3-S7 explicit `test.skip(true, '...Phase 325...')`) + 1 helper module dengan 3 helper fully implemented + full spec run pass green dalam <=3 menit.

**PHASE SPLIT context (decided 2026-05-26 via checker iter 3 BLOCKER 1):**
- Phase 324 ships dengan S1 + S2 + 7-scenario file structure + Plan 01 code edit + Plan 03 cleanup + Plan 04 IT handoff
- Phase 325 (slug draft: `complete-uat-phase324-s3-to-s7`, di-spawn user via `/gsd-add-phase`) akan implement S3-S7 karena setiap-nya butuh fixture seed assessment yang berbeda (essay vs running session vs Pass-state vs Fail-state) + multi-actor orchestration (HC + worker) → context cost > 50% single agent kalau dipaksa di Phase 324.

**TIDAK ADA bahasa "boleh skipped", "bandwidth-permitting", "v1", "minimum 2 dari 7" di plan ini.** S3-S7 di-skip karena explicit deferred-to-Phase-325 (bukan reduced scope dari D-07).

CHECKPOINT: Plan ini berisi `checkpoint:human-verify` di Task 3 untuk konfirmasi user setelah full spec run hijau di lokal (sebelum lanjut Plan 03 cleanup).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-CONTEXT.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-VALIDATION.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Reusable infrastructure existing (verified via Read):
- `tests/helpers/accounts.ts` exports `accounts` object + `AccountKey` type
- `tests/helpers/auth.ts` exports `login(page, AccountKey)` — **INFO 6 fix: pakai existing `login` (BUKAN buat new `loginAs`)**
- `tests/helpers/dbSnapshot.ts` BACKUP/RESTORE helper (referensi pattern sqlcmd, tidak di-invoke langsung)
- `tests/playwright.config.ts` testDir `./e2e`, baseURL `http://localhost:5277`, globalTeardown wired
- Pattern Phase 322 `tests/e2e/manage-assessment-filter.spec.ts` + Phase 321 `tests/e2e/edit-peserta-answers.spec.ts` (role-model `login` + `accounts` fixture usage)
- Pattern submit exam: `tests/e2e/exam-taking.spec.ts` FLOW A test 'A6 - Worker answers all questions correctly' + 'A7 - Worker submits exam via ExamSummary' (lines 136-193) — role-model untuk full helper body `submitNonEssayAssessment`

Helper signatures yang akan dibuat di Task 1 (3 helper, BUKAN 4 — `login` reuse existing):
```typescript
// tests/e2e/helpers/phase324.ts
export async function submitNonEssayAssessment(page: Page, assessmentTitle: string): Promise<void>;
export async function assertRecordsRowCount(page: Page, sessionTitle: string, expected: { assessmentOnline: number; trainingManual: number }): Promise<void>;
export async function sqlcmdQueryCount(query: string): Promise<number>;
```

Submit pattern reference (FLOW A6+A7 lines 136-193):
```typescript
// A6: locate questionCards, loop, click .exam-radio nth(correctIdx)
const questionCards = page.locator('[id^="qcard_"]');
const qCount = await questionCards.count();
for (let i = 0; i < qCount; i++) {
  await questionCards.nth(i).locator('.exam-radio').first().click({ force: true });
  await page.waitForTimeout(500);
}
// A7: navigate ExamSummary, accept dialog, submit
await page.locator('#reviewSubmitBtn').click();
await page.waitForURL('**/CMP/ExamSummary**', { timeout: 15_000 });
page.once('dialog', d => d.accept());
await page.click('button:has-text("Submit Exam")');
await page.waitForURL('**/CMP/Results/**', { timeout: 15_000 });
```

Navigate-to-assessment pattern (untuk submitNonEssayAssessment by title):
```typescript
await page.goto('/CMP/Assessment');
const card = page.locator('.assessment-card', { hasText: assessmentTitle });
await card.locator('a:has-text("Start"), a:has-text("Resume")').click();
await page.waitForURL('**/CMP/StartExam/**', { timeout: 15_000 });
// Optional resume modal
const resumeModal = page.locator('#resumeConfirmModal');
if (await resumeModal.isVisible({ timeout: 2_000 }).catch(() => false)) {
  await page.locator('#resumeConfirmBtn').click();
  await page.waitForTimeout(500);
}
```

Accounts yang dipakai per test S1+S2:
- S1 (worker submit non-essay): `accounts.coachee` (`rino.prasetyo@pertamina.com`)
- S2 (PreTest skip): `accounts.coachee2` (`iwan3@pertamina.com`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Buat helper module phase324.ts (3 helper FULLY IMPLEMENTED — no placeholder bodies)</name>
  <read_first>
    - tests/helpers/auth.ts (existing `login` helper — INFO 6 reuse pattern)
    - tests/helpers/accounts.ts (struktur accounts + AccountKey type)
    - tests/e2e/exam-taking.spec.ts lines 136-193 (FLOW A A6+A7 submit pattern — role-model body submitNonEssayAssessment)
    - tests/e2e/manage-assessment-filter.spec.ts (Phase 322 role-model selector + locator patterns)
    - tests/helpers/dbSnapshot.ts (sqlcmd shell-out pattern existing)
  </read_first>
  <files>tests/e2e/helpers/phase324.ts</files>
  <behavior>
    - File exists dan TypeScript compile OK
    - 3 function exported: `submitNonEssayAssessment`, `assertRecordsRowCount`, `sqlcmdQueryCount` (`login` reuse via existing tests/helpers/auth.ts)
    - **submitNonEssayAssessment FULLY IMPLEMENTED** (NO `break; // placeholder`, NO TODO): navigate /CMP/Assessment → locate card by title → click Start/Resume → handle resume modal → loop questionCards click first radio per question → navigate ExamSummary → accept dialog → submit → wait Results
    - assertRecordsRowCount: navigate /CMP/Records, filter by session title, count "Assessment Online" + "Training Manual" rows, assert exact count
    - sqlcmdQueryCount: shell-out sqlcmd, parse integer dari output
  </behavior>
  <action>
    Buat file `tests/e2e/helpers/phase324.ts` dengan 3 helper FULLY IMPLEMENTED. Body submitNonEssayAssessment HARUS lengkap — TIDAK BOLEH ada `break; // placeholder`, TODO, atau partial loop.

    Pattern body submitNonEssayAssessment di-derive verbatim dari `tests/e2e/exam-taking.spec.ts` FLOW A test A6 (line 152-167) + A7 (line 184-192). Adapt: cari card by title (bukan by `title` variable test-local), accept first option per question (deterministic — tidak perlu correctIdx mapping karena bukan testing correctness, hanya testing TR insert behavior).

    Struktur lengkap file:

    ```typescript
    import { Page, expect } from '@playwright/test';
    import { exec } from 'node:child_process';
    import { promisify } from 'node:util';

    const execP = promisify(exec);

    /**
     * Worker submit non-essay assessment by title (FULL flow: locate card → start/resume → answer all
     * questions with first radio option → ExamSummary → Submit → Results page).
     *
     * Pattern derived from tests/e2e/exam-taking.spec.ts FLOW A tests A6+A7 (lines 136-193).
     *
     * Pre-req: caller HARUS sudah login via existing `login(page, accountKey)` helper
     * (tests/helpers/auth.ts). Assessment dengan judul `assessmentTitle` HARUS visible di
     * /CMP/Assessment untuk akun login (status Open atau In-Progress).
     */
    export async function submitNonEssayAssessment(page: Page, assessmentTitle: string): Promise<void> {
      // Navigate ke daftar assessment worker
      await page.goto('/CMP/Assessment');

      // Locate card by title, click Start atau Resume
      const card = page.locator('.assessment-card', { hasText: assessmentTitle });
      await expect(card).toBeVisible({ timeout: 10_000 });
      const startOrResume = card.locator('a:has-text("Start"), a:has-text("Resume")').first();
      await startOrResume.click();
      await page.waitForURL('**/CMP/StartExam/**', { timeout: 15_000 });

      // Handle optional resume confirmation modal (kalau Resume)
      const resumeModal = page.locator('#resumeConfirmModal');
      if (await resumeModal.isVisible({ timeout: 2_000 }).catch(() => false)) {
        await page.locator('#resumeConfirmBtn').click();
        await page.waitForTimeout(500);
      }

      // Wait untuk exam header + timer (FLOW A5 verify)
      await expect(page.locator('#examHeader')).toBeVisible({ timeout: 10_000 });
      await expect(page.locator('#examTimer')).toBeVisible({ timeout: 10_000 });

      // Loop semua question card, klik first radio option per question
      const questionCards = page.locator('[id^="qcard_"]');
      const qCount = await questionCards.count();
      if (qCount === 0) {
        throw new Error(`submitNonEssayAssessment: no question card ditemukan untuk assessment "${assessmentTitle}". Pastikan assessment punya soal MC.`);
      }
      for (let i = 0; i < qCount; i++) {
        const qCard = questionCards.nth(i);
        // Pilih first radio (deterministic — bukan testing correctness, testing TR insert behavior)
        await qCard.locator('.exam-radio').first().click({ force: true });
        await page.waitForTimeout(300); // micro-debounce
      }

      // Confirm semua terjawab via progress indicator (FLOW A6 line 168 pattern)
      await expect(page.locator('#answeredProgress')).toContainText(`${qCount}/${qCount}`, { timeout: 5_000 });

      // Navigate ke ExamSummary via reviewSubmit button (FLOW A7 line 185)
      await page.locator('#reviewSubmitBtn').click();
      await page.waitForURL('**/CMP/ExamSummary**', { timeout: 15_000 });
      await expect(page.locator('body')).toContainText('Submit Exam', { timeout: 5_000 });

      // Submit + handle confirm dialog (FLOW A7 line 190-192)
      page.once('dialog', d => d.accept());
      await page.click('button:has-text("Submit Exam")');
      await page.waitForURL('**/CMP/Results/**', { timeout: 30_000 });
    }

    /**
     * Navigate ke /CMP/Records, filter by session title, assert exact count untuk
     * "Assessment Online" + "Training Manual" row.
     *
     * Phase 324 primary assertion: post-fix `assessmentOnline: 1, trainingManual: 0`
     * (vs pre-fix `assessmentOnline: 1, trainingManual: 1` yang menyebabkan visual duplicate).
     */
    export async function assertRecordsRowCount(
      page: Page,
      sessionTitle: string,
      expected: { assessmentOnline: number; trainingManual: number }
    ): Promise<void> {
      await page.goto('/CMP/Records');
      await page.waitForLoadState('networkidle');

      // Optional search filter kalau ada banyak row di /CMP/Records
      const searchInput = page.locator('input[type="search"], input[name="search"]').first();
      if (await searchInput.count() > 0) {
        await searchInput.fill(sessionTitle);
        await page.waitForTimeout(500); // debounce filter
      }

      // Count row "Assessment Online" (dari AssessmentSession branch GetUnifiedRecords)
      const assessmentRows = page.locator('tr', { hasText: 'Assessment Online' }).filter({ hasText: sessionTitle });
      await expect(assessmentRows).toHaveCount(expected.assessmentOnline, { timeout: 5_000 });

      // Count row "Training Manual" (dari TrainingRecord branch — post-fix expect 0)
      const trainingRows = page.locator('tr', { hasText: 'Training Manual' }).filter({ hasText: sessionTitle });
      await expect(trainingRows).toHaveCount(expected.trainingManual, { timeout: 5_000 });
    }

    /**
     * Shell-out ke sqlcmd untuk eksekusi SCALAR count query, parse integer dari output.
     *
     * SECURITY NOTE: query param TIDAK boleh berisi user input — caller responsibility.
     * Phase 324 spec hanya pakai literal const FIXTURE_SESSION_TITLE (controlled string).
     *
     * Helper ini juga di-reuse di Plan 03 Task 1+2+4 (DB schema verify + orphan check + post-cleanup verify).
     */
    export async function sqlcmdQueryCount(query: string): Promise<number> {
      const cmd = `sqlcmd -S "localhost\\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -W -Q "SET NOCOUNT ON; ${query.replace(/"/g, '\\"')}"`;
      const { stdout } = await execP(cmd);
      const match = stdout.match(/\d+/);
      if (!match) throw new Error(`sqlcmdQueryCount: sqlcmd returned no integer: ${stdout}`);
      return parseInt(match[0], 10);
    }
    ```

    CATATAN PENTING untuk executor:
    - **TIDAK boleh ada `break; // placeholder` atau TODO comment di body submitNonEssayAssessment** — full loop implementation WAJIB. Per checker iter 3 BLOCKER 1 fix.
    - Selectors di-verify against actual rendered HTML lokal (StartExam.cshtml + ExamSummary.cshtml + Records.cshtml) sebelum spec dianggap green. Pattern di atas verified via FLOW A pass di Phase 313/322.
    - **JANGAN buat `loginAs` baru** — INFO 6 fix: gunakan existing `login` dari `tests/helpers/auth.ts` (sudah cover seluruh flow login).
    - `assertRecordsRowCount` selector `tr` + filter `hasText: sessionTitle` HARUS exact-match. Kalau title contains regex meta chars, escape literal di caller.

    Verify post-create: `cd tests && npx playwright test --list e2e/Phase324_NoDuplicateTrainingRecord.spec.ts 2>&1 | head -20` (akan surface parse error kalau ada).
  </action>
  <verify>
    <automated>grep -c "^export async function" tests/e2e/helpers/phase324.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/helpers/phase324.ts` exists
    - File exports 3 function: `submitNonEssayAssessment`, `assertRecordsRowCount`, `sqlcmdQueryCount` (`grep -c "^export async function" tests/e2e/helpers/phase324.ts` returns 3)
    - **NO placeholder body**: `grep -c "break; // placeholder\|TODO\|FIXME\|XXX" tests/e2e/helpers/phase324.ts` returns 0 (zero hit — full implementation per checker iter 3 BLOCKER 1)
    - File TIDAK import `login` atau `loginAs` (helper ini fokus submit + assert + sql; login handled by spec via existing `tests/helpers/auth.ts`)
    - File import `Page, expect` dari `@playwright/test` + `exec, promisify` untuk sqlcmd
    - submitNonEssayAssessment body contains loop `for (let i = 0; i < qCount; i++)` + click `.exam-radio` + click `Submit Exam` button (verify via grep)
  </acceptance_criteria>
  <done>Helper module phase324.ts exists dengan 3 function FULLY IMPLEMENTED (no placeholder, no TODO), TypeScript syntactically valid, ready dipakai di Task 2 spec.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Buat spec file Phase324_NoDuplicateTrainingRecord.spec.ts (S1+S2 IMPLEMENTED GREEN, S3-S7 deferred-to-Phase-325)</name>
  <read_first>
    - tests/e2e/helpers/phase324.ts (helper signatures dari Task 1)
    - tests/helpers/auth.ts (existing `login` helper — import via `'../../helpers/auth'`)
    - tests/e2e/exam-taking.spec.ts FLOW A (role-model untuk submit flow)
    - tests/e2e/edit-peserta-answers.spec.ts (Phase 321 role-model — reference untuk S6+S7 Phase 325 implementation kelak)
    - tests/helpers/accounts.ts (accounts object)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-CONTEXT.md (D-07a SHIPS + D-07b DEFERRED)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (section "Playwright Test Pattern D-07")
  </read_first>
  <files>tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts</files>
  <behavior>
    - Spec file exists dengan 1 test.describe.serial block + 7 test() block (S1..S7)
    - test.describe.serial mode (sequential — supaya state S1 tidak race ke S2..)
    - **S1 + S2 fully implemented + GREEN** (DUPL-02a criteria 2 + 3)
    - **S3-S7 explicit `test.skip(true, "Implementasi di Phase 325 — butuh fixture seed ... — lihat CONTEXT.md D-07b")` annotations + TODO comments** (DUPL-02a criteria 4) — NOT "boleh skipped" / "bandwidth-permitting"
    - `cd tests && npx playwright test --list e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` returns 7 test
    - `cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` → 2 passed + 5 skipped (with reason) dalam <=3 menit
  </behavior>
  <action>
    Buat file `tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts`. Setiap test block follow pattern.

    Fixture pre-req: SEBELUM run spec, executor (atau user manual) HARUS:
    1. Login HC di lokal, buat 1 assessment Non-Essay biasa untuk Rino (akun coachee) — judul `[Phase 324] Test Non-Essay`, tambah 3 soal MC, set `AllowAnswerReview=true`. Catat assessmentTitle.
    2. Buat 1 PreTest assessment untuk Iwan3 (akun coachee2) — judul `[Phase 324] Test PreTest`, tambah 3 soal MC, set `AssessmentType=PreTest`. Catat assessmentTitle.
    3. Plug judul ke const `FIXTURE_TITLE_S1` + `FIXTURE_TITLE_S2` di spec.

    Documented di SUMMARY.md: pre-req fixture, cara setup, status pre-run.

    Struktur file:

    ```typescript
    import { test, expect } from '@playwright/test';
    import { login } from '../helpers/auth';
    import {
      submitNonEssayAssessment,
      assertRecordsRowCount,
      sqlcmdQueryCount,
    } from './helpers/phase324';

    // ============================================================
    // FIXTURE TITLES — pre-req: HC sudah buat 2 assessment lokal via UI
    // Phase 324 D-07a (CONTEXT.md): WAJIB IMPLEMENT S1 + S2
    // Phase 324 D-07b (CONTEXT.md): S3-S7 DEFERRED ke Phase 325
    //   (slug draft: complete-uat-phase324-s3-to-s7, akan di-spawn user via /gsd-add-phase)
    // ============================================================
    const FIXTURE_TITLE_S1 = '[Phase 324] Test Non-Essay';   // assessment non-essay biasa untuk Rino
    const FIXTURE_TITLE_S2 = '[Phase 324] Test PreTest';      // PreTest untuk Iwan3

    test.describe.serial('Phase 324 - No Duplicate TrainingRecord on Assessment Completion', () => {

      // ============================================================
      // S1: Worker submit non-essay → /CMP/Records 1 row "Assessment Online"
      // DUPL-02a criteria 2 — PRIMARY regression guard untuk Plan 01 Task 1 (D-01)
      // ============================================================
      test('S1: Worker submit non-essay -> /CMP/Records 1 row "Assessment Online"', async ({ page }) => {
        // Baseline TR count pre-test (untuk verify post-test = baseline, bukan baseline+1)
        const preCount = await sqlcmdQueryCount(
          `SELECT COUNT(*) FROM TrainingRecords WHERE Judul = 'Assessment: ${FIXTURE_TITLE_S1}'`
        );

        // Worker login + submit
        await login(page, 'coachee');
        await submitNonEssayAssessment(page, FIXTURE_TITLE_S1);

        // PRIMARY ASSERTION: /CMP/Records show 1 row Assessment Online, 0 row Training Manual
        // Pre-fix: 1 + 1 (duplicate). Post-fix: 1 + 0 (sole source AssessmentSession).
        await assertRecordsRowCount(page, FIXTURE_TITLE_S1, {
          assessmentOnline: 1,
          trainingManual: 0,
        });

        // DB-level verify: post-submit TR count HARUS SAMA dengan preCount (bukan preCount+1)
        const postCount = await sqlcmdQueryCount(
          `SELECT COUNT(*) FROM TrainingRecords WHERE Judul = 'Assessment: ${FIXTURE_TITLE_S1}'`
        );
        expect(postCount).toBe(preCount); // POST-FIX: tidak ada TR baru ter-insert
      });

      // ============================================================
      // S2: PreTest tetap skip TR (regression guard existing behavior)
      // DUPL-02a criteria 3 — confirms PreTest branch unaffected oleh Phase 324 edit
      // ============================================================
      test('S2: PreTest tetap skip TR (regression guard)', async ({ page }) => {
        const preCount = await sqlcmdQueryCount(
          `SELECT COUNT(*) FROM TrainingRecords WHERE Judul = 'Assessment: ${FIXTURE_TITLE_S2}'`
        );

        await login(page, 'coachee2');
        await submitNonEssayAssessment(page, FIXTURE_TITLE_S2);

        // Records: PreTest tetap appear di Assessment Online branch (via AssessmentSession),
        // TIDAK ada Training Manual row (pre-existing PreTest skip behavior — line 264 pre-fix).
        await assertRecordsRowCount(page, FIXTURE_TITLE_S2, {
          assessmentOnline: 1,
          trainingManual: 0,
        });

        // DB verify: PreTest TIDAK pernah insert TR (pre-fix dan post-fix sama)
        const postCount = await sqlcmdQueryCount(
          `SELECT COUNT(*) FROM TrainingRecords WHERE Judul = 'Assessment: ${FIXTURE_TITLE_S2}'`
        );
        expect(postCount).toBe(preCount);
      });

      // ============================================================
      // S3-S7: DEFERRED ke Phase 325 (slug: complete-uat-phase324-s3-to-s7)
      // Rationale: setiap scenario butuh fixture seed assessment berbeda (essay vs running session
      // vs Pass-state vs Fail-state) + multi-actor orchestration (HC + worker). Context cost > 50%
      // single agent kalau dipaksa di Phase 324.
      // Decided 2026-05-26 via checker iter 3 BLOCKER 1 PHASE SPLIT.
      // Reference: CONTEXT.md D-07b, REQUIREMENTS.md DUPL-02b.
      // ============================================================

      test('S3: Essay flow finalize -> tidak insert TR', async ({ page }) => {
        // TODO Phase 325: butuh fixture seed essay assessment + HC FinalizeEssayGrading action.
        // Pattern reference: tests/e2e/essay-grading.spec.ts (kalau ada) atau buat baru.
        // Assertions: worker submit essay (status PendingGrading) → HC finalize via
        //   /Admin/FinalizeEssayGrading → /CMP/Records 1 row Assessment Online, 0 row Training Manual.
        test.skip(true, 'Implementasi di Phase 325 - butuh fixture seed essay assessment + sertifikat existing state. Lihat CONTEXT.md D-07b.');
      });

      test('S4: HC AkhiriUjian (force-end single) -> grading jalan, tidak insert TR', async ({ page }) => {
        // TODO Phase 325: butuh fixture seed running session + 1 worker yang belum submit.
        // Pattern reference: AssessmentMonitoringDetail HC view + AkhiriUjian action.
        // Assertions: HC click AkhiriUjian → AssessmentSession.Status=Completed + IsPassed evaluated
        //   → /CMP/Records 1 row Assessment Online, 0 row Training Manual.
        test.skip(true, 'Implementasi di Phase 325 - butuh fixture seed running session + 1 worker yang belum submit. Lihat CONTEXT.md D-07b.');
      });

      test('S5: HC AkhiriSemuaUjian (bulk) -> grading jalan untuk semua, tidak insert TR', async ({ page }) => {
        // TODO Phase 325: butuh fixture seed running session + multi-worker pending.
        // Pattern reference: AssessmentMonitoringDetail HC view + AkhiriSemuaUjian bulk action.
        // Assertions: HC click AkhiriSemuaUjian → semua worker AssessmentSession.Status=Completed
        //   → /CMP/Records per-worker 1 row Assessment Online, 0 row Training Manual.
        test.skip(true, 'Implementasi di Phase 325 - butuh fixture seed running session + multi-worker pending. Lihat CONTEXT.md D-07b.');
      });

      test('S6: Regrade Pass->Fail -> AssessmentSession.IsPassed update, no TR cascade', async ({ page }) => {
        // TODO Phase 325: butuh fixture seed Pass session + sertifikat existing untuk revoke verify.
        // Pattern reference: tests/e2e/edit-peserta-answers.spec.ts (Phase 321 EditPesertaAnswers spec).
        // Assertions: HC EditPesertaAnswers ganti jawaban supaya Pass→Fail, submit → AssessmentSession.IsPassed=false
        //   + NomorSertifikat=null + TR Status TIDAK berubah jadi Failed (cascade dihapus Plan 01 D-03).
        test.skip(true, 'Implementasi di Phase 325 - butuh fixture seed Pass session + sertifikat existing state untuk revoke verify. Lihat CONTEXT.md D-07b.');
      });

      test('S7: Regrade Fail->Pass -> NomorSertifikat generate, no TR cascade', async ({ page }) => {
        // TODO Phase 325: butuh fixture seed Fail session (GenerateCertificate=true, !PreTest).
        // Pattern reference: tests/e2e/edit-peserta-answers.spec.ts (Phase 321).
        // Assertions: HC EditPesertaAnswers ganti jawaban supaya Fail→Pass, submit → AssessmentSession.NomorSertifikat != null
        //   + TR row TIDAK ter-insert/update (cascade dihapus Plan 01 D-03).
        test.skip(true, 'Implementasi di Phase 325 - butuh fixture seed Fail session + GenerateCertificate=true assessment. Lihat CONTEXT.md D-07b.');
      });
    });
    ```

    PROTOKOL untuk executor:
    1. Buat file dengan 7 test block sesuai skeleton di atas.
    2. **WAJIB**: S1 + S2 fully implemented + green. S3-S7 explicit `test.skip(true, '...Phase 325...')` per CONTEXT.md D-07b.
    3. Setup fixture pre-req via HC UI lokal (2 assessment baru), catat title, plug ke const.
    4. Run: `cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts --reporter=line`.
    5. Expected output: `2 passed, 5 skipped` dalam <=3 menit.
    6. **TIDAK ADA bahasa "boleh skipped"/"bandwidth-permitting"** di SUMMARY.md — S3-S7 di-skip karena explicit deferred-to-Phase-325, BUKAN reduced scope.

    Dokumentasikan di SUMMARY.md:
    - 2 test green (S1, S2) + 5 test skipped dengan reason "Phase 325"
    - Reference: CONTEXT.md D-07a (SHIPS) + D-07b (DEFERRED) + REQUIREMENTS.md DUPL-02a + DUPL-02b
    - Fixture pre-req documented (judul assessment, akun, jumlah soal)
  </action>
  <verify>
    <automated>cd tests && npx playwright test --list e2e/Phase324_NoDuplicateTrainingRecord.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` exists
    - Spec list returns 7 test (`cd tests && npx playwright test --list e2e/Phase324_NoDuplicateTrainingRecord.spec.ts | grep -c "S[1-7]:"` returns 7)
    - **S1 + S2 IMPLEMENTED + GREEN** (`cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts -g "S1|S2" --reporter=line` returns 2 passed)
    - **S3-S7 explicit `test.skip(true, "...Phase 325...")` annotations** (`grep -c "test.skip(true.*Phase 325" tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` returns 5)
    - File import `login` dari `'../helpers/auth'` (INFO 6 fix — verify via grep)
    - File import 3 helper dari `'./helpers/phase324'` (verify via grep)
    - Full spec run `cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` returns `2 passed, 5 skipped` <=3 menit
    - Re-run S1+S2 2x consistent pass (no flakiness)
  </acceptance_criteria>
  <done>Spec file dengan 7 test block exists; S1 + S2 fully implemented + green; S3-S7 explicit skipped dengan Phase 325 reference; helper module dipakai correctly; documented di SUMMARY tanpa bahasa "boleh skipped"/"bandwidth-permitting".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint user verify Playwright UAT S1+S2 green + S3-S7 skipped-to-Phase-325 di lokal</name>
  <files>(checkpoint only - no file modified)</files>
  <action>Tunggu user manual verify Playwright UAT S1+S2 green + S3-S7 properly skipped dengan Phase 325 reference + visual sanity /CMP/Records 1-row state confirmed sebelum lanjut Plan 03 cleanup. Lihat how-to-verify section di bawah.</action>
  <what-built>Playwright UAT spec Phase 324 dengan 7 test scenario (S1+S2 IMPLEMENTED GREEN, S3-S7 deferred-to-Phase-325 dengan annotated skip) + helper module phase324.ts (3 helper fully implemented, no placeholder). Spec exercises 2 grading paths primary: SubmitExam non-essay (S1) + PreTest skip (S2). S3-S7 reserved untuk Phase 325 (slug: complete-uat-phase324-s3-to-s7).</what-built>
  <how-to-verify>
    1. Stop Kestrel kalau jalan; `dotnet run` fresh
    2. Pastikan fixture assessment lokal sudah ada (kalau belum: login HC, buat `[Phase 324] Test Non-Essay` untuk Rino + `[Phase 324] Test PreTest` untuk Iwan3, masing-masing 3 soal MC)
    3. Open new terminal: `cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts --reporter=line`
    4. EXPECTED output: `2 passed, 5 skipped` (S1+S2 pass; S3-S7 skipped dengan reason "Implementasi di Phase 325 - butuh fixture seed ...")
    5. Open Playwright HTML report kalau ada failure: `npx playwright show-report`
    6. Manual sanity (browser): login worker (Rino), submit assessment biasa lokal `[Phase 324] Test Non-Essay`, navigate `/CMP/Records`, capture screenshot — hanya 1 row "Assessment Online" untuk event terbaru (bukan 2). Simpan screenshot ke `docs/screenshots/phase324/after-fix.png` (untuk DUPL-05 acceptance).
    7. Konfirmasi total runtime spec <=3 menit (`time npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts`).
    8. Verify S3-S7 skip annotations explicit Phase 325 reference: `grep -c "Phase 325" tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` returns >=5.
  </how-to-verify>
  <resume-signal>Type "approved" kalau S1+S2 green, S3-S7 skipped dengan Phase 325 reason, visual check 1-row state confirmed. Type "issues: [deskripsi]" kalau ada bug atau S1/S2 gagal supaya planner adjust.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner -> ASP.NET Kestrel lokal | HTTP loopback, no untrusted input crosses |
| Playwright spec -> sqlcmd shell exec | sqlcmd query parameterized via const string literal (NO user input concat) |
| accounts.ts seed credentials -> spec runtime | credentials hardcoded lokal, NOT committed dalam ciphertext - already in repo (low risk, dev-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-324-07 | Tampering | sqlcmd query injection via session title containing SQL meta | mitigate | Helper signature `sqlcmdQueryCount(query: string)` — caller responsibility. Spec pakai literal const FIXTURE_TITLE_* (controlled string, no user input). Helper docstring explicit "query param TIDAK boleh berisi user input — caller responsibility." |
| T-324-08 | Repudiation | Spec spuriously pass karena selector wrong | mitigate | Helper assertion via `toHaveCount(expected)` — explicit count check (bukan `toBeVisible`). Failure mode = test FAIL bukan FALSE PASS. |
| T-324-09 | Info Disclosure | Console log credentials dari accounts | accept | accounts.ts sudah committed dengan dev credentials (existing pattern Phase 322). NOT new risk Phase 324. |
| T-324-10 | DoS-self | Spec runtime > 3 menit -> blok dev workflow | mitigate | `--reporter=line` (lightweight) + S3-S7 explicit `test.skip` (zero runtime cost) + parallel disabled `test.describe.serial` (avoid race) — total runtime untuk S1+S2 + 5 skip ~30-60s. |
| T-324-23 | Repudiation | S3-S7 silently "forgotten" karena skipped tanpa traceability | mitigate | Skip annotations explicit `"Implementasi di Phase 325 - ... Lihat CONTEXT.md D-07b"` + REQUIREMENTS.md DUPL-02b tracking + CONTEXT.md D-07b documented + (eventual) Phase 325 spawn via /gsd-add-phase |
</threat_model>

<verification>
End-of-plan verification:

1. File exists:
   - `tests/e2e/helpers/phase324.ts` (verify via `test -f`)
   - `tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` (verify via `test -f`)
2. Helper NO placeholder: `grep -c "break; // placeholder\|TODO\|FIXME\|XXX" tests/e2e/helpers/phase324.ts` returns 0
3. Helper 3 exports: `grep -c "^export async function" tests/e2e/helpers/phase324.ts` returns 3
4. Spec list: `cd tests && npx playwright test --list e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` returns 7 test
5. S1 + S2 green: `cd tests && npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts -g "S1|S2" --reporter=line` returns 2 passed
6. S3-S7 explicit Phase 325 skip: `grep -c "test.skip(true.*Phase 325" tests/e2e/Phase324_NoDuplicateTrainingRecord.spec.ts` returns 5
7. Full spec runtime <=3 menit (verify via `time npx playwright test e2e/Phase324_NoDuplicateTrainingRecord.spec.ts`)
8. Manual visual verify done (screenshot `docs/screenshots/phase324/after-fix.png` exists or user confirms 1-row state)
9. Checkpoint user approved
</verification>

<success_criteria>
- 7 test block defined (S1-S7)
- **S1 + S2 GREEN** (DUPL-02a criteria 2 + 3 fully satisfied) — primary regression guard untuk Plan 01 Task 1 D-01 edit
- **S3-S7 explicit `test.skip(true, '...Phase 325...')`** (DUPL-02a criteria 4 — deferred-to-Phase-325 explicit, BUKAN reduced scope)
- **DUPL-02a fully satisfied** (Phase 324 scope) — DUPL-02b deferred ke Phase 325 dedicated phase per user decision PHASE SPLIT (checker iter 3 BLOCKER 1)
- Helper module reusable untuk Plan 03 (sqlcmdQueryCount dipakai di Plan 03 Task 1 + Task 2 + Task 4 post-cleanup verify)
- User approve via checkpoint (visual + spec hijau confirmed)
- Atomic commit message: `test(324-02): playwright UAT phase 324 S1+S2 green + S3-S7 deferred-to-phase-325`
</success_criteria>

<output>
After completion, create `.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-02-SUMMARY.md` containing:
- Commit hash (test commit)
- Spec test status: S1 pass, S2 pass, S3-S7 skipped dengan reason "Implementasi di Phase 325"
- Full runtime measurement (target <=3 menit)
- Fixture pre-req documented (judul assessment, akun, jumlah soal)
- Manual UAT screenshot path (after-fix.png)
- Phase 325 reference: slug draft `complete-uat-phase324-s3-to-s7` (user spawn via `/gsd-add-phase` setelah Phase 324 ship)
- Notes untuk Plan 03 executor: helper sqlcmdQueryCount siap dipakai untuk DB schema verify + orphan check + post-cleanup verify (D-10 SQL count pre/post)
- Notes untuk Plan 04 executor: commit hash final Wave 2 untuk IT handoff pin
- **EXPLICIT statement**: "S3-S7 di-skip BUKAN karena reduced scope atau bandwidth-permitting — explicit deferred-to-Phase-325 per CONTEXT.md D-07b + REQUIREMENTS.md DUPL-02b decided 2026-05-26 via checker iter 3 BLOCKER 1 PHASE SPLIT."
</output>
</content>
</invoke>