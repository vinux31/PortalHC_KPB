---
phase: 324
plan: 03
type: execute
wave: 3
depends_on: ["324-01"]
files_modified:
  - docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [DUPL-03, DUPL-05]
tags: [data-cleanup, seed-workflow, local-only, sql, schema-verify]
must_haves:
  truths:
    - "TrainingRecords.CreatedAt column existence VERIFIED (RESEARCH A3 resolved)"
    - "Cleanup script filter terbukti hanya match TR auto-generated (verified via sample 20 row inspection pre-execute — Task 2 Pitfall 6 sanity)"
    - "RenewsTrainingId orphan risk MEASURED (RESEARCH OQ #3 resolved)"
    - "DB lokal di-BACKUP via sqlcmd sebelum cleanup"
    - "SQL cleanup script idempotent (re-run aman, post-count tetap 0)"
    - "Cleanup script transactional dengan safety cap 5000"
    - "Pre-count > 0 dan post-count = 0 setelah eksekusi"
    - "SEED_JOURNAL.md ada entry baru classification temporary + local-only, status cleaned"
    - "AssessmentSessions table TIDAK ter-touch (sole source-of-truth utuh)"
    - "Screenshot pre-fix + post-fix tersimpan di docs/screenshots/phase324/"
  artifacts:
    - path: "docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql"
      provides: "Idempotent transactional cleanup script dengan safety cap + pre/post count + rollback on cap exceeded"
      contains: "SET XACT_ABORT ON"
      min_lines: 60
    - path: "docs/SEED_JOURNAL.md"
      provides: "Audit trail entry Phase 324 cleanup activity"
      contains: "phase 324"
    - path: "docs/screenshots/phase324/before-fix.png"
      provides: "Visual proof 2-row state pre-fix"
    - path: "docs/screenshots/phase324/after-fix.png"
      provides: "Visual proof 1-row state post-fix"
  key_links:
    - from: "docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql"
      to: "DB HcPortalDB_Dev lokal (TrainingRecords table)"
      via: "sqlcmd execution"
    - from: "Plan 04 IT handoff doc"
      to: "docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql"
      via: "embedded verbatim copy + commit hash reference"
---

<objective>
Cleanup data legacy `TrainingRecord` auto-generated di DB lokal (D-04, D-05, D-10 dari CONTEXT.md) via SEED_WORKFLOW BACKUP/RESTORE lifecycle. Resolve 3 RESEARCH Open Questions (A3 CreatedAt column existence + OQ #2 cutoff atas + OQ #3 RenewsTrainingId orphan) sebelum finalize cleanup script.

Purpose: Hapus row TR yang ter-insert auto antara 2026-04-10 (commit `766011b6` re-introduce auto-create) sampai code edit Plan 01 merged. Setelah cleanup, `/CMP/Records` worker tampil 1 row per assessment event (bukan 2). Script jadi master copy untuk dipakai IT di Plan 04 handoff (Dev/Prod).

Output: Schema verified + filter column decided + orphan risk addressed + SQL script idempotent + DB lokal cleaned + journal entry + screenshot pre/post.

CHECKPOINT: Plan ini berisi `checkpoint:human-verify` di Task 5 untuk konfirmasi user setelah cleanup lokal selesai dan visual verify 1-row state pada `/CMP/Records` confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-CONTEXT.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-VALIDATION.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md
@docs/SEED_JOURNAL.md

<interfaces>
Existing infra yang dipakai:
- `sqlcmd` CLI (system-installed, dipakai Phase 313/315/322)
- DB lokal `HcPortalDB_Dev` di `localhost\SQLEXPRESS`
- `tests/helpers/dbSnapshot.ts` BACKUP/RESTORE pattern (referensi - tidak invoke langsung dari plan ini)
- `docs/SEED_JOURNAL.md` append-only audit log (existing pattern Phase 313/315)

Helper dari Plan 02 yang available:
- `tests/e2e/helpers/phase324.ts::sqlcmdQueryCount(query)` - bisa dipakai post-cleanup verify dari spec, atau langsung dari shell command di task ini.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Resolve RESEARCH Open Question A3 - verify TrainingRecords.CreatedAt column existence</name>
  <read_first>
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (section "Open Questions" #1, "Assumptions Log" A3)
    - Models/TrainingRecord.cs (model TIDAK ada property CreatedAt - VERIFIED via Read sebelumnya)
  </read_first>
  <files>(no file modified - investigation only, hasil di-document via task done message untuk Task 2-3 baseline)</files>
  <behavior>
    - Schema verify via sqlcmd: query INFORMATION_SCHEMA.COLUMNS untuk TrainingRecords table
    - Hasil determine filter column untuk Task 3 cleanup script:
      - Kalau `CreatedAt` exists: pakai `WHERE Judul LIKE 'Assessment:%' AND CreatedAt >= '2026-04-10'`
      - Kalau `CreatedAt` TIDAK exists: pakai `WHERE Judul LIKE 'Assessment:%' AND TanggalSelesai >= '2026-04-10'` (TanggalSelesai DI-SET di code line 273 GradingService pre-fix dengan `DateTime.UtcNow`)
      - Fallback Id-based (kalau TanggalSelesai juga nullable): identify max Id pre-regression via query `SELECT MAX(Id) FROM TrainingRecords WHERE TanggalSelesai < '2026-04-10' OR (TanggalSelesai IS NULL AND Id < <approx>)` - kompleks, hindari kalau bisa
  </behavior>
  <action>
    Eksekusi schema verify query via sqlcmd dan document hasil sebagai baseline untuk Task 3.

    QUERY 1 (verify CreatedAt column):
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COLUMN_NAME, DATA_TYPE, IS_NULLABLE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'TrainingRecords' ORDER BY ORDINAL_POSITION;"
    ```

    Expected output: list semua column TrainingRecords. Cek presence `CreatedAt`.

    QUERY 2 (sample data untuk verify TanggalSelesai populated):
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -W -Q "SELECT TOP 5 Id, Judul, Tanggal, TanggalSelesai FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' ORDER BY Id DESC;"
    ```

    Expected output: kalau TanggalSelesai populated (bukan NULL) untuk row Assessment:%, OK pakai sebagai filter fallback.

    QUERY 3 (count baseline - berapa row yang akan di-cleanup):
    ```bash
    # Jalankan dengan filter sesuai hasil Query 1:
    # Variant A (kalau CreatedAt ada):
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND CreatedAt >= '2026-04-10';"
    # Variant B (kalau hanya TanggalSelesai ada):
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND TanggalSelesai >= '2026-04-10';"
    ```

    Document hasil di task done message:
    - Has CreatedAt: yes/no
    - Filter column decided: CreatedAt | TanggalSelesai | Id-based
    - Baseline count: <N>
    - Sample row inspection: TanggalSelesai populated yes/no untuk Assessment:% rows
  </action>
  <verify>
    <automated>sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'TrainingRecords' AND COLUMN_NAME IN ('CreatedAt', 'TanggalSelesai');"</automated>
  </verify>
  <acceptance_criteria>
    - Query 1 executed, output captured (column list TrainingRecords)
    - Query 2 executed (sample row 5)
    - Query 3 executed (baseline count >= 0)
    - Filter column decision documented di task done message: salah satu dari [CreatedAt | TanggalSelesai | Id-based]
    - Decision feed ke Task 3 (cleanup script filter clause)
  </acceptance_criteria>
  <done>Schema verify selesai, filter column decision documented, baseline count tercatat. Hasil dipakai sebagai input Task 3 cleanup script construction.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Resolve RESEARCH Open Question #3 - measure RenewsTrainingId orphan risk</name>
  <read_first>
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (section "Open Questions" #3, "Don't Hand-Roll" RenewsTrainingId FK NoAction)
    - Data/ApplicationDbContext.cs lines 144-170 (TrainingRecord FK configuration, RenewsTrainingId NoAction)
  </read_first>
  <files>(no file modified - investigation only)</files>
  <behavior>
    - Query count child TR yang me-renew parent (Phase 200 renewal chain) yang akan di-delete
    - Kalau result = 0: aman, lanjut Task 3 tanpa modifikasi
    - Kalau result > 0: tambah pre-cleanup statement di Task 3 script untuk null-clear `RenewsTrainingId` di child rows sebelum DELETE parent (avoid FK violation karena `OnDelete(NoAction)`)
  </behavior>
  <action>
    Eksekusi orphan-check query via sqlcmd.

    QUERY (orphan risk check):
    ```bash
    # Gunakan filter column dari Task 1 decision (CreatedAt atau TanggalSelesai)
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COUNT(*) FROM TrainingRecords WHERE RenewsTrainingId IN (SELECT Id FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND {date_filter});"
    ```

    Replace `{date_filter}` sesuai Task 1 decision.

    Document hasil:
    - Orphan count: <N>
    - Decision:
      - N = 0: skip null-clear, Task 3 cleanup script standard
      - N > 0: Task 3 cleanup script tambah pre-DELETE statement:
        `UPDATE TrainingRecords SET RenewsTrainingId = NULL WHERE RenewsTrainingId IN (SELECT Id FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND {date_filter});`

    Optional QUERY 2 (visual sample 20 row untuk Pitfall 6 sanity - capture admin manual entry vs auto-generated):
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -W -Q "SELECT TOP 20 Id, UserId, Judul, Kategori, Tanggal, Penyelenggara, Status FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND {date_filter} ORDER BY Id;"
    ```

    Inspect: kalau ada row dengan `Penyelenggara` selain "Internal" atau `Kategori` aneh, FLAG sebagai anomali (mungkin admin manual entry). STOP dan diskusikan dengan user sebelum lanjut Task 3.
  </action>
  <verify>
    <automated>echo "Orphan check executed - hasil documented di task done message"</automated>
  </verify>
  <acceptance_criteria>
    - Orphan count query executed, result documented (N)
    - Sample 20 row inspection done (Pitfall 6 sanity)
    - Decision documented:
      - Standard cleanup (no null-clear pre-step), atau
      - Cleanup + null-clear pre-step, atau
      - STOP dan eskalasi ke user (anomali sample 20 row)
    - Hasil feed ke Task 3 (script structure modification)
  </acceptance_criteria>
  <done>Orphan risk measured, sample inspection done, decision documented untuk Task 3. Kalau ada anomali, FLAG ke user via task done message (checkpoint informal).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Buat SQL cleanup script docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql</name>
  <read_first>
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (section "Pattern 3: SQL Cleanup Script - Transaction + Count Guard" - full template)
    - Hasil Task 1 (filter column decision)
    - Hasil Task 2 (orphan risk decision)
  </read_first>
  <files>docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql</files>
  <behavior>
    - File exists di path `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql`
    - Script structure: SET XACT_ABORT ON + BEGIN TRY/BEGIN TRANSACTION + pre-count + safety cap (5000) + optional null-clear RenewsTrainingId pre-step (kalau Task 2 N>0) + DELETE + post-count verify + COMMIT (atau ROLLBACK kalau cap exceeded / post-count > 0) + BEGIN CATCH ROLLBACK + THROW
    - Filter column sesuai Task 1 decision
    - Script idempotent (re-run aman, kalau pre-count = 0 langsung COMMIT silent)
    - Database name `HcPortalDB_Dev` (lokal); IT akan adjust ke `HcPortalDB` saat eksekusi di Prod (documented di Plan 04 handoff)
  </behavior>
  <action>
    Buat file `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` dengan struktur transaction-wrapped.

    Pastikan `docs/sql/` directory exists (kalau belum, buat dulu via Bash `mkdir -p docs/sql`).

    Template (adjust `{date_filter}` dan optional null-clear sesuai Task 1+2 decision):

    ```sql
    -- Phase 324 D-04..D-06 cleanup: hapus TrainingRecord auto-generated antara 2026-04-10 sampai hari ini.
    -- Sumber bug: commit 766011b6 (2026-04-10) re-introduce auto-create TR yang menyebabkan visual duplicate di /CMP/Records.
    -- Fix di code: Phase 324 Plan 01 hapus block insert TR di 3 lokasi production (GradingService + AssessmentAdminController).
    -- Script ini idempotent: re-run aman, post-count = 0 = nothing to do.
    -- Filter column: {CreatedAt | TanggalSelesai} (decided berdasarkan Task 1 schema verify).
    -- Safety cap: 5000 row.
    -- Database name: HcPortalDB_Dev (lokal). IT adjust ke HcPortalDB saat run di Prod.

    USE HcPortalDB_Dev;
    GO

    SET XACT_ABORT ON;
    SET NOCOUNT ON;

    BEGIN TRY
        BEGIN TRANSACTION cleanup324;

        -- 1. Pre-count
        DECLARE @preCount INT;
        SELECT @preCount = COUNT(*)
        FROM TrainingRecords
        WHERE Judul LIKE 'Assessment:%' AND {date_filter};
        PRINT CONCAT('Phase 324 cleanup pre-count: ', @preCount);

        -- 2. Safety cap (defensive guard)
        IF @preCount > 5000
        BEGIN
            ROLLBACK TRANSACTION cleanup324;
            RAISERROR('Safety cap exceeded (%d > 5000). Investigate before bumping cap.', 16, 1, @preCount);
            RETURN;
        END

        -- 3. (Optional - hanya kalau Task 2 N>0) Null-clear RenewsTrainingId di child rows
        --    sebelum DELETE parent (avoid FK violation karena OnDelete(NoAction)).
        -- UPDATE TrainingRecords
        -- SET RenewsTrainingId = NULL
        -- WHERE RenewsTrainingId IN (
        --     SELECT Id FROM TrainingRecords
        --     WHERE Judul LIKE 'Assessment:%' AND {date_filter}
        -- );
        -- DECLARE @nullClearedCount INT = @@ROWCOUNT;
        -- PRINT CONCAT('Phase 324 null-cleared RenewsTrainingId in child rows: ', @nullClearedCount);

        -- 4. DELETE
        DELETE FROM TrainingRecords
        WHERE Judul LIKE 'Assessment:%' AND {date_filter};
        DECLARE @deletedCount INT = @@ROWCOUNT;
        PRINT CONCAT('Phase 324 cleanup deleted: ', @deletedCount);

        -- 5. Post-count verify
        DECLARE @postCount INT;
        SELECT @postCount = COUNT(*)
        FROM TrainingRecords
        WHERE Judul LIKE 'Assessment:%' AND {date_filter};
        PRINT CONCAT('Phase 324 cleanup post-count: ', @postCount);

        IF @postCount > 0
        BEGIN
            ROLLBACK TRANSACTION cleanup324;
            RAISERROR('Phase 324 post-count not zero (%d). Rolled back.', 16, 1, @postCount);
            RETURN;
        END

        COMMIT TRANSACTION cleanup324;
        PRINT 'Phase 324 cleanup committed successfully.';
    END TRY
    BEGIN CATCH
        IF XACT_STATE() <> 0 ROLLBACK TRANSACTION cleanup324;
        DECLARE @errMsg NVARCHAR(4000) = ERROR_MESSAGE();
        PRINT CONCAT('Phase 324 cleanup failed: ', @errMsg);
        THROW;
    END CATCH;
    GO

    -- Sanity check sample (IT bisa uncomment untuk visual inspection pre-run):
    -- SELECT TOP 20 Id, UserId, Judul, Kategori, Tanggal, TanggalSelesai, Penyelenggara, Status
    -- FROM TrainingRecords
    -- WHERE Judul LIKE 'Assessment:%' AND {date_filter}
    -- ORDER BY Id DESC;
    ```

    REPLACEMENT instructions:
    - `{date_filter}` -> EXACT clause dari Task 1 decision, contoh: `CreatedAt >= '2026-04-10'` atau `TanggalSelesai >= '2026-04-10'`.
    - Kalau Task 2 N=0: HAPUS section 3 (null-clear pre-step seluruhnya, jangan biarkan commented out - tidak relevan).
    - Kalau Task 2 N>0: UNCOMMENT section 3, sesuaikan filter sama dengan section 4.

    Setelah file dibuat, verify struktur via Read.
  </action>
  <verify>
    <automated>test -f docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql && grep -c "SET XACT_ABORT ON" docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` exists
    - File contains: `SET XACT_ABORT ON`, `BEGIN TRANSACTION cleanup324`, `IF @preCount > 5000`, `DELETE FROM TrainingRecords`, `COMMIT TRANSACTION cleanup324`, `BEGIN CATCH`, `THROW`
    - `{date_filter}` placeholder REPLACED dengan actual clause (no template literal tersisa)
    - Filter consistency: pre-count + delete + post-count semua pakai filter clause yang sama (verify via grep)
    - File size > 50 line (excluding blank lines)
  </acceptance_criteria>
  <done>SQL cleanup script ready untuk eksekusi di Task 4 (lokal) dan untuk embed di Plan 04 (IT handoff). Filter column sesuai Task 1, null-clear pre-step sesuai Task 2 decision.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Eksekusi cleanup lokal via SEED_WORKFLOW (BACKUP -> apply -> verify -> journal)</name>
  <read_first>
    - docs/SEED_WORKFLOW.md (full Section 5 - BACKUP + RESTORE commands)
    - docs/SEED_JOURNAL.md (existing format pattern entry)
    - docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql (script dari Task 3)
  </read_first>
  <files>docs/SEED_JOURNAL.md</files>
  <behavior>
    - DB lokal di-BACKUP ke `.bak` file SEBELUM eksekusi cleanup
    - Cleanup script di-eksekusi via `sqlcmd -i`
    - Post-count verify = 0
    - Re-run cleanup (idempotency check) = no-op (pre-count = 0)
    - Journal entry baru ditambahkan dengan classification `temporary + local-only`, awal status `active`, akhir `cleaned`
  </behavior>
  <action>
    Eksekusi 6-step sesuai SEED_WORKFLOW:

    STEP 1 - Stop Kestrel kalau jalan (Ctrl+C di terminal dotnet run).

    STEP 2 - BACKUP DB lokal:
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev.20260526-phase324-pre-cleanup.bak' WITH INIT"
    ```
    Kalau permission denied di C:\Temp, fallback ke folder default instance: `C:\Program Files\Microsoft SQL Server\MSSQL17.SQLEXPRESS\MSSQL\Backup\HcPortalDB_Dev.20260526-phase324-pre-cleanup.bak`.

    STEP 3 - APPEND entry di docs/SEED_JOURNAL.md sebagai `active`:

    Format mengikuti pattern existing (lihat docs/SEED_JOURNAL.md row terakhir untuk format yang konsisten). Contoh entry:
    ```
    | 2026-05-26 | 324 | temporary + local-only | Cleanup TR auto-generated (commit 766011b6 regression) di /CMP/Records | TrainingRecords(<preCount>) | HcPortalDB_Dev.20260526-phase324-pre-cleanup.bak | active |
    ```
    Replace `<preCount>` dengan baseline count dari Task 1 Query 3.

    Pakai Read tool untuk baca docs/SEED_JOURNAL.md dulu sebelum append, lalu Edit/Write append entry baru di bagian akhir table.

    STEP 4 - APPLY cleanup script:
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -i "docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql"
    ```
    Expected output: "Phase 324 cleanup pre-count: <N>", "Phase 324 cleanup deleted: <N>", "Phase 324 cleanup post-count: 0", "Phase 324 cleanup committed successfully."

    STEP 5 - VERIFY post-count:
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND {date_filter};"
    ```
    Expected: 0.

    STEP 6 - IDEMPOTENCY check - re-run script:
    ```bash
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -i "docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql"
    ```
    Expected output: "Phase 324 cleanup pre-count: 0", "Phase 324 cleanup deleted: 0", "Phase 324 cleanup post-count: 0", "Phase 324 cleanup committed successfully."

    STEP 7 - UPDATE journal entry status `active` -> `cleaned`. Edit docs/SEED_JOURNAL.md, ganti `active` jadi `cleaned` pada entry Phase 324.

    STEP 8 - Optional: hapus `.bak` file kalau sudah pasti tidak butuh rollback (`rm "C:\Temp\HcPortalDB_Dev.20260526-phase324-pre-cleanup.bak"`). Atau biarkan (gitignored).

    JANGAN sentuh AssessmentSessions table.
  </action>
  <verify>
    <automated>sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'Assessment:%';"</automated>
  </verify>
  <acceptance_criteria>
    - DB backup file `.bak` exists di lokasi yang valid (test -f check)
    - Cleanup script executed sukses (`Phase 324 cleanup committed successfully.` di output)
    - Post-count verify = 0 untuk filter clause
    - Re-run idempotent: pre-count = 0, deleted = 0 (no-op COMMIT)
    - docs/SEED_JOURNAL.md ada entry baru dengan klasifikasi temporary + local-only, status `cleaned`
    - AssessmentSessions row count TIDAK berubah pre vs post (verify via `SELECT COUNT(*) FROM AssessmentSessions WHERE Status='Completed'` - same number)
  </acceptance_criteria>
  <done>DB lokal cleaned, journal entry tertulis dengan status cleaned, idempotency verified, AssessmentSessions utuh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Checkpoint user verify cleanup lokal + /CMP/Records 1-row state</name>
  <files>(checkpoint only - no file modified)</files>
  <action>Tunggu user manual verify cleanup lokal selesai, screenshot after-fix.png saved, /CMP/Records 1-row state confirmed visually. Lihat how-to-verify section di bawah.</action>
  <what-built>Cleanup data legacy TR auto-generated di DB lokal via SEED_WORKFLOW BACKUP/RESTORE lifecycle. Script transactional dengan safety cap + idempotent. 3 RESEARCH Open Questions resolved (A3 column existence + cutoff atas decision + OQ#3 orphan risk). SEED_JOURNAL.md updated. **Pre-fix + post-fix screenshots MANDATORY** untuk DUPL-05 acceptance — Plan 03 punya .bak dari Task 4 sehingga before-fix capture feasible via RESTORE→capture→re-apply sequence.</what-built>
  <how-to-verify>
    1. Start Kestrel fresh: `dotnet run` (kalau stop sebelumnya untuk BACKUP)
    2. Login worker (akun coachee `rino.prasetyo@pertamina.com` / 123456) via browser di `http://localhost:5277/Account/Login`
    3. Navigate `/CMP/Records`
    4. EXPECTED VISUAL STATE: untuk setiap event assessment yang sudah Completed, tampil 1 row "Assessment Online" (TIDAK ada paired "Training Manual" row dengan judul "Assessment: ..."). Bandingkan dengan screenshot before-fix kalau sudah ada.
    5. Capture screenshot post-fix state -> simpan ke `docs/screenshots/phase324/after-fix.png` (buat folder kalau belum ada: `mkdir -p docs/screenshots/phase324`)
    6. **MANDATORY pre-fix screenshot (D-08)**: Plan 03 punya `.bak` dari Task 4 Step 2 — RESTORE DB sementara dari backup file, capture 2-row state di `/CMP/Records`, simpan ke `docs/screenshots/phase324/before-fix.png`, lalu re-apply cleanup script (Task 4 Step 4 sequence). Sequence: (a) stop Kestrel, (b) `sqlcmd -S "localhost\SQLEXPRESS" -E -Q "ALTER DATABASE HcPortalDB_Dev SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE HcPortalDB_Dev FROM DISK='<bak-path>' WITH REPLACE; ALTER DATABASE HcPortalDB_Dev SET MULTI_USER"`, (c) start Kestrel, (d) login worker + capture before-fix.png, (e) stop Kestrel, (f) re-execute cleanup script via sqlcmd -i, (g) start Kestrel + capture after-fix.png. Both screenshots WAJIB untuk DUPL-05 acceptance criteria 1+2.
    7. Sanity DB query (post-cleanup verifikasi final):
       ```
       sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'Assessment:%';"
       ```
       Expected: 0 (atau angka legitimate kalau ada filter cutoff atas).
    8. Verify AssessmentSessions utuh:
       ```
       sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -h -1 -Q "SELECT COUNT(*) FROM AssessmentSessions WHERE Status='Completed';"
       ```
       Expected: same count as pre-cleanup (write down before vs after).
  </how-to-verify>
  <resume-signal>Type "approved" kalau /CMP/Records show 1-row state confirmed, **before-fix.png + after-fix.png BOTH saved** di docs/screenshots/phase324/, sanity query OK. Type "issues: [deskripsi]" kalau ada bug.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sqlcmd CLI -> SQL Server local instance | Windows Integrated Security (`-E` flag), no credential transit |
| SQL script -> DB rows | Hard-coded literal `'Assessment:%'` + date literal - no user input concatenation |
| Backup file `.bak` -> filesystem | Local Windows path, no remote storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-324-11 | Tampering | Mass-delete runaway (filter terlalu lebar) | mitigate | Safety cap 5000 di script + IF @preCount > 5000 ROLLBACK RAISERROR (Pitfall 6 sanity sample 20 row inspection di Task 2) |
| T-324-12 | Tampering | Delete row legitimate admin manual entry (Judul pattern collision) | mitigate | Task 2 sample 20 row inspection + STOP-and-eskalasi rule kalau Penyelenggara/Kategori anomali |
| T-324-13 | Repudiation | Cleanup tanpa audit trail | mitigate | docs/SEED_JOURNAL.md append-only entry + .bak file preserved untuk rollback option |
| T-324-14 | DoS-self | Restore DB di tengah session kerja lain (Kestrel running) | mitigate | SEED_WORKFLOW Step 1 mandatory stop Kestrel dulu + sqlcmd SINGLE_USER WITH ROLLBACK IMMEDIATE evict koneksi |
| T-324-15 | Tampering | RenewsTrainingId FK orphan saat parent delete (OnDelete=NoAction) | mitigate | Task 2 measure + optional null-clear pre-DELETE statement di Task 3 script |
| T-324-16 | DoS-self | Script gagal di tengah transaction -> DB inconsistent | mitigate | BEGIN TRY/CATCH + ROLLBACK di catch handler + SET XACT_ABORT ON (auto-abort on runtime error) |
</threat_model>

<verification>
End-of-plan verification:

1. Task 1 evidence: filter column decision documented (CreatedAt | TanggalSelesai | Id-based)
2. Task 2 evidence: orphan count + sample 20 inspection result documented
3. Task 3: `test -f docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` && `grep -c "SET XACT_ABORT ON"` returns 1
4. Task 4: 
   - Backup file `.bak` exists (test -f)
   - Post-cleanup count: `sqlcmd ... SELECT COUNT(*) WHERE Judul LIKE 'Assessment:%' AND {filter}` returns 0
   - Re-run cleanup script -> "deleted: 0" output
   - SEED_JOURNAL.md entry exists dengan status cleaned (grep)
5. Task 5 checkpoint: user approved + screenshot after-fix.png exists
6. AssessmentSessions integrity: pre vs post row count for Status='Completed' SAMA
</verification>

<success_criteria>
- 3 RESEARCH Open Questions resolved (A3, OQ#2, OQ#3)
- SQL cleanup script ready (master copy untuk dipakai Plan 04 IT handoff verbatim)
- DB lokal cleaned (post-count = 0, idempotent verified)
- SEED_JOURNAL.md append + status cleaned
- Visual proof (after-fix screenshot) captured
- AssessmentSessions UTUH (sole source-of-truth preserved)
- User approve via checkpoint
- Atomic commits: `feat(324-03-NN): <description>` per task. Task 4 commit message: `chore(324-03): cleanup lokal TrainingRecord auto-generated + SEED_JOURNAL`. Task 3 commit: `feat(324-03): SQL cleanup script + 3 RESEARCH OQ resolved`.
</success_criteria>

<output>
After completion, create `.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-03-SUMMARY.md` containing:
- Commit hash (semua task)
- RESEARCH OQ resolution: A3 (filter column = ?), OQ#2 (cutoff atas needed?), OQ#3 (orphan count = ?)
- Baseline count pre-cleanup
- Post-count = 0 verified
- Backup file path + timestamp
- SEED_JOURNAL.md entry reference (date + status)
- Screenshot after-fix.png path
- AssessmentSessions integrity check (pre vs post count)
- Notes untuk Plan 04 executor:
  - Filter column decision (Plan 04 handoff harus pakai filter yang sama)
  - Orphan null-clear pre-step needed yes/no (Plan 04 script harus include atau skip)
  - Sample 20 row inspection result (Plan 04 handoff harus include pre-check sample query)
</output>
