---
phase: 324
plan: 04
type: execute
wave: 4
depends_on: ["324-01", "324-03"]
files_modified:
  - docs/DB_HANDOFF_IT_2026-05-26.html
autonomous: true
requirements: [DUPL-04]
tags: [it-handoff, html, pertamina-branding, sql-handoff]
must_haves:
  truths:
    - "docs/DB_HANDOFF_IT_2026-05-26.html exists"
    - "HTML structure mengikuti template 2026-05-13.html (Pertamina branding: --brand #e30613, --navy #1e3a8a)"
    - "TL;DR ada (gradient brand color) dengan flag NO schema change"
    - "Section context + commit hash + pre-check sample + pre-check orphan + backup + SQL script embedded + verification + rollback"
    - "EXPLICIT ordering callout (Step 1 deploy code DULU, Step 2 SQL cleanup) untuk prevent Pitfall 5 race"
    - "Section 'Yang TIDAK perlu IT lakukan' (no migration, no schema change) - sejajar template"
    - "SQL script embedded verbatim copy dari docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql"
    - "Commit hash final Wave 1+2 di-pin di handoff (commit hash decision deferred ke task creator pakai HEAD main saat author)"
  artifacts:
    - path: "docs/DB_HANDOFF_IT_2026-05-26.html"
      provides: "Pertamina-branded HTML handoff IT untuk eksekusi cleanup di Dev/Prod"
      contains: "var(--brand)"
      min_lines: 300
  key_links:
    - from: "docs/DB_HANDOFF_IT_2026-05-26.html"
      to: "docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql"
      via: "verbatim embed inside <pre class='sql'> block"
    - from: "docs/DB_HANDOFF_IT_2026-05-26.html"
      to: "Plan 01 commit hash"
      via: "displayed di meta-row + checklist Step 3 git pull target"
    - from: "docs/DB_HANDOFF_IT_2026-05-26.html"
      to: "docs/DB_HANDOFF_IT_2026-05-13.html (template precedent)"
      via: "verbatim CSS + structural fork"
---

<objective>
Buat HTML handoff document `docs/DB_HANDOFF_IT_2026-05-26.html` untuk IT team supaya bisa eksekusi cleanup data legacy TR di server Dev (10.55.3.3) dan Prod (D-06 dari CONTEXT.md). Document follows template `docs/DB_HANDOFF_IT_2026-05-13.html` (Pertamina branding) dengan content fork: scope cleanup data (bukan migration apply seperti template asli).

Purpose: Developer (Rino) tidak boleh edit DB Dev/Prod langsung per CLAUDE.md Golden Rules. IT team butuh standalone doc dengan context + commit hash + SQL script + verification + rollback plan yang self-contained dan auditable.

Output: 1 file HTML siap kirim ke IT (PDF-friendly via Ctrl+P), embedded SQL script verbatim, explicit ordering callout untuk prevent race condition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-CONTEXT.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-01-SUMMARY.md
@.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-03-SUMMARY.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md
@docs/DB_HANDOFF_IT_2026-05-13.html
@docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql

<interfaces>
Template `docs/DB_HANDOFF_IT_2026-05-13.html` (verified via Read):
- CSS variables: `--brand: #e30613`, `--brand-dark: #b8050f`, `--navy: #1e3a8a`, `--text: #1f2937`, etc.
- Structure: header.hero (badge BRAND + h1 + subtitle), meta-row (Tanggal + Baseline + HEAD + Migration + Risk), tldr (gradient brand), toc (Daftar Isi), section h2 numbered (1..8)
- Component classes: `.callout.info/.warn/.success/.danger`, `.tag.ok/.warn/.danger`, `pre.sql` untuk SQL block
- @media print rules supaya Ctrl+P -> PDF clean

Inputs untuk handoff content:
- Commit hash final Wave 1 dari Plan 01 SUMMARY (pin di meta-row + Step 3 git pull target)
- SQL script content dari docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql (embed verbatim di Section "SQL Cleanup Script")
- 3 RESEARCH OQ resolution dari Plan 03 SUMMARY (filter column + orphan decision + sample inspection)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/DB_HANDOFF_IT_2026-05-26.html dari template 2026-05-13.html</name>
  <read_first>
    - docs/DB_HANDOFF_IT_2026-05-13.html (full file - struktur + CSS + style precedent)
    - docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql (untuk embed verbatim)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-01-SUMMARY.md (commit hash final)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-03-SUMMARY.md (filter column + orphan decision + sample 20 inspection)
    - .planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-RESEARCH.md (Pitfall 5 ordering rationale + Pitfall 6 sample query)
  </read_first>
  <files>docs/DB_HANDOFF_IT_2026-05-26.html</files>
  <behavior>
    - File exists di path target
    - File parse valid HTML5 (Doctype + html lang + head + body + closing)
    - CSS verbatim copy dari template 2026-05-13 (`--brand`, `--navy`, etc.)
    - Header.hero badge "HANDOFF TEAM IT", h1 "Cleanup Data Duplikat TrainingRecord", subtitle relevan Phase 324
    - meta-row: Tanggal "26 Mei 2026", Commit Hash (final Wave 1), Phase "324", Migration "NONE", Risk "Low (data cleanup only)"
    - tldr (gradient brand): satu kalimat ringkas - cleanup TR auto-generated, NO migration, NO schema change
    - toc Daftar Isi 8 section
    - Section 1: Context (kenapa cleanup needed - regression dari commit 766011b6 di 2026-04-10)
    - Section 2: Pre-Check (sample 20 row query dari Pitfall 6 supaya IT visual sanity sebelum execute)
    - Section 3: SQL Script (embedded verbatim from docs/sql/cleanup-...sql)
    - Section 4: Checklist IT (Step 1 deploy code FIRST, Step 2 BACKUP, Step 3 Pre-check sample, Step 4 Execute cleanup, Step 5 Post-verify, Step 6 Rollback plan)
    - Section 5: Verification (post-cleanup query)
    - Section 6: Yang TIDAK perlu IT lakukan (no migration, no schema change, no data restore)
    - Section 7: Rollback Plan (RESTORE FROM DISK kalau perlu)
    - Section 8: File Referensi
  </behavior>
  <action>
    Buat file `docs/DB_HANDOFF_IT_2026-05-26.html`. Strategi: copy struktur + CSS dari `docs/DB_HANDOFF_IT_2026-05-13.html` verbatim (CSS block lines 7-250 dari template), lalu adapt body content untuk Phase 324 scope.

    LANGKAH:

    Step 1 - Read full content `docs/DB_HANDOFF_IT_2026-05-13.html` (lines 1-527 - dari head sampai closing body+html).

    Step 2 - Read `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` untuk dapat content embed (verbatim).

    Step 3 - Read 324-01-SUMMARY.md untuk commit hash final Wave 1.

    Step 4 - Compose new HTML. Verbatim copy:
    - HTML5 Doctype + `<html lang="id">` + `<head>` block (title adjust ke "Cleanup Data Duplikat TrainingRecord (2026-05-26)")
    - Full `<style>` block (lines 7-250 dari template, verbatim - tidak edit CSS)
    - Closing `</style></head><body><div class="container">` opening

    Adapt body:
    - `<header class="hero">`:
      - badge "HANDOFF TEAM IT"
      - h1 "Cleanup Data Duplikat TrainingRecord - HcPortal KPB"
      - subtitle "Apa yang IT perlu lakukan untuk eksekusi cleanup row TrainingRecord auto-generated di DB Dev (10.55.3.3) supaya halaman /CMP/Records hanya tampil 1 row per assessment event (bukan 2)."
    - `<div class="meta-row">`:
      - Tanggal: 26 Mei 2026
      - Phase: 324
      - Commit Hash Target: <COMMIT_HASH_FINAL_WAVE1> (replace dengan actual hash dari 324-01-SUMMARY.md)
      - Migration Pending: NONE
      - Risk: Low (data cleanup only - non-destructive ke AssessmentSessions)
    - `<div class="tldr">`:
      - label "TL;DR"
      - "IT cukup `git pull` (deploy code commit `<HASH>`) + apply `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` di DB Dev. **NO schema change, NO migration.** Script idempotent, transactional, safety cap 5000. Data AssessmentSessions 100% utuh - hanya hapus row TrainingRecord copy yang ter-insert auto antara 2026-04-10 sampai commit fix merge."
    - `<div class="toc">` 8 section sesuai behavior list

    Section content:

    H2 #1 - Context:
    Jelaskan bug + regression history: commit 766011b6 (2026-04-10) re-introduce auto-create TR setelah commit 79284609 (2026-03-18) menghapusnya karena "caused duplicate entries in RecordWorkerDetail unified view". Worker submit 1 assessment biasa -> DB store 1 AssessmentSession + 1 TR. WorkerDataService.GetUnifiedRecords UNION keduanya -> /CMP/Records tampil 2 row untuk 1 event. Fix di Plan 01 = hapus block insert TR di 3 lokasi production. Cleanup di Plan 04 (ini) = hapus row legacy yang sudah terlanjur ter-insert di DB Dev/Prod.

    H2 #2 - Pre-Check Sample (Pitfall 6 mandatory):
    Embed query sample 20 row untuk visual sanity sebelum cleanup. IT JANGAN execute DELETE sebelum confirm sample row semua auto-generated (Penyelenggara="Internal", Kategori match Assessment pattern). Kalau ada anomali (Penyelenggara berbeda, Kategori asing, Judul typo) -> STOP dan eskalasi ke developer.

    Query embed:
    ```sql
    SELECT TOP 20 Id, UserId, Judul, Kategori, Tanggal, TanggalSelesai, Penyelenggara, Status
    FROM TrainingRecords
    WHERE Judul LIKE 'Assessment:%' AND <FILTER_FROM_PLAN_03>
    ORDER BY Id DESC;
    ```
    Replace `<FILTER_FROM_PLAN_03>` dengan actual filter clause dari 324-03-SUMMARY.md (filter column decision: CreatedAt atau TanggalSelesai).

    H2 #3 - SQL Cleanup Script (embedded verbatim):
    Embed `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` content verbatim dalam `<pre class="sql">` block. CATATAN: IT execute di Prod harus ganti `USE HcPortalDB_Dev` -> `USE HcPortalDB` (Prod DB name). Document di handoff dengan callout warn.

    H2 #4 - Checklist IT (STEP-BY-STEP):
    Ordering MANDATORY (Pitfall 5 race prevention):

    `<div class="callout warn">` dengan title "URUTAN WAJIB":
    "Step 1 deploy code DULU (commit `<HASH>`) supaya app berhenti generate TR baru. STEP 2 baru SQL cleanup. Jangan reverse order - race condition bisa bikin row baru ter-insert selama cleanup running."

    Step 1: `git pull origin main` + restart IIS/Kestrel pool (deploy code commit `<HASH>`)
    Step 2: BACKUP DB Dev (`BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Backup\HcPortalDB_Dev.pre-phase324.bak' WITH FORMAT, INIT`)
    Step 3: Pre-check sample query (lihat Section #2) - IT visual sanity inspect
    Step 4: Execute cleanup script: `sqlcmd ... -i docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql`
    Step 5: Post-verify count = 0 (lihat Section #5)
    Step 6: Re-run script untuk confirm idempotent (post-count tetap 0)

    H2 #5 - Verification:
    Embed post-cleanup queries:
    ```sql
    -- Post-count verify cleanup berhasil
    SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'Assessment:%' AND <FILTER>;
    -- Expected: 0

    -- Sanity AssessmentSessions UTUH (sole source-of-truth)
    SELECT COUNT(*) FROM AssessmentSessions WHERE Status='Completed';
    -- Expected: SAMA dengan pre-cleanup count (IT catat pre vs post)

    -- Visual verify via app: login worker di server Dev, navigate /CMP/Records,
    -- assert hanya 1 row "Assessment Online" per assessment event (bukan 2).
    ```

    H2 #6 - Yang TIDAK Perlu IT Lakukan:
    - `<span class="tag danger">JANGAN</span>` Apply migration (NO migration di Phase 324)
    - JANGAN restore DB Dev dari lokal (data HC harus dipertahankan)
    - JANGAN ALTER table / drop column
    - JANGAN edit kode di server Dev langsung
    - JANGAN execute cleanup tanpa BACKUP dulu

    H2 #7 - Rollback Plan:
    Kalau ada masalah post-cleanup (misalnya delete row legitimate):
    ```sql
    ALTER DATABASE HcPortalDB_Dev SET SINGLE_USER WITH ROLLBACK IMMEDIATE;
    RESTORE DATABASE HcPortalDB_Dev FROM DISK='C:\Backup\HcPortalDB_Dev.pre-phase324.bak' WITH REPLACE;
    ALTER DATABASE HcPortalDB_Dev SET MULTI_USER;
    ```

    H2 #8 - File Referensi:
    Table dengan kolom Path + Purpose:
    - `Services/GradingService.cs` - Code edit Plan 01 D-01 + D-03
    - `Controllers/AssessmentAdminController.cs` - Code edit Plan 01 D-02
    - `docs/sql/cleanup-2026-05-26-trainingrecord-duplicates.sql` - SQL script
    - `docs/DEV_WORKFLOW.md` - SOP umum
    - `docs/SEED_WORKFLOW.md` - SOP BACKUP/RESTORE pattern
    - `.planning/phases/324-*/` - Full planning context

    Footer (verbatim mirror template):
    ```html
    <footer>
      Disusun oleh Rino - 2026-05-26 - HcPortal KPB Project<br>
      Tekan <code>Ctrl+P</code> -> "Save as PDF" untuk export ke PDF.
    </footer>
    ```

    Close `</div></body></html>`.

    PLACEHOLDER NOTES untuk executor:
    - `<COMMIT_HASH_FINAL_WAVE1>` -> ambil dari 324-01-SUMMARY.md (kalau belum ada, gunakan placeholder `<TBD-after-plan01-complete>` dan flag di SUMMARY untuk update sebelum kirim ke IT)
    - `<FILTER_FROM_PLAN_03>` -> ambil dari 324-03-SUMMARY.md (filter column decision)
    - Kalau dependencies (Plan 01, Plan 03 SUMMARY) belum ada saat task ini di-execute, gunakan placeholder dan dokumentasikan di SUMMARY untuk final update.

    Verify post-create: `test -f docs/DB_HANDOFF_IT_2026-05-26.html` + `grep -c "var(--brand)" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=1 (CSS preserved).
  </action>
  <verify>
    <automated>test -f docs/DB_HANDOFF_IT_2026-05-26.html && grep -c "var(--brand)" docs/DB_HANDOFF_IT_2026-05-26.html && grep -c "Phase 324" docs/DB_HANDOFF_IT_2026-05-26.html</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/DB_HANDOFF_IT_2026-05-26.html` exists
    - `grep -c "var(--brand)" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=1 (CSS Pertamina branding preserved)
    - `grep -c "Phase 324" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=1
    - `grep -c "SET XACT_ABORT ON" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=1 (SQL script embedded)
    - `grep -c "git pull" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=1 (Step 1 deploy code instruction)
    - `grep -c "BACKUP DATABASE" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=1 (Step 2 backup instruction)
    - `grep -c "URUTAN WAJIB" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=1 (ordering callout Pitfall 5)
    - `grep -c "JANGAN" docs/DB_HANDOFF_IT_2026-05-26.html` returns >=3 (section "yang TIDAK perlu IT lakukan")
    - File size > 300 line (rich HTML content)
    - Closing tags valid: `grep -c "</html>" docs/DB_HANDOFF_IT_2026-05-26.html` returns 1
    - Browser-renderable: open file di Chrome/Edge, layout intact (manual smoke - bukan automated)
  </acceptance_criteria>
  <done>HTML handoff doc lengkap dengan Pertamina branding, embedded SQL script verbatim, explicit ordering callout, semua 8 section ter-isi. Siap dikirim ke IT (PDF-friendly via Ctrl+P).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer (Rino) -> IT team (HTML handoff doc) | Asynchronous communication via file delivery - IT team execute di server tanpa direct developer access |
| HTML handoff -> IT browser/PDF reader | Inert document (HTML+CSS), no JS, no remote resources |
| Embedded SQL script -> IT sqlcmd execution | Verbatim copy of audited script (Plan 03 Task 3) - no new SQL logic introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-324-17 | Tampering | IT execute cleanup di Prod tanpa adjust DB name | mitigate | Explicit callout `<div class="callout warn">` "IT adjust `USE HcPortalDB_Dev` -> `USE HcPortalDB` di Prod" + verbatim USE statement comment di SQL embed |
| T-324-18 | Tampering | IT execute cleanup BEFORE deploy code (race condition Pitfall 5) | mitigate | `<div class="callout warn">` URUTAN WAJIB di Section #4 (Step 1 git pull DULU, Step 2 BACKUP, Step 3 sample, Step 4 cleanup) |
| T-324-19 | Info Disclosure | Handoff bocor ke pihak luar (file disclosure) | accept | SQL script idempotent + only deletes auto-generated TR rows + no credentials in doc + no sensitive PII. Risk LOW. |
| T-324-20 | Repudiation | IT lupa BACKUP sebelum execute -> rollback impossible | mitigate | Step 2 BACKUP explicit di Checklist + Rollback Section #7 mention prerequisite `.bak` file dari Step 2 |
| T-324-21 | Tampering | IT salah pakai filter clause (typo CreatedAt vs TanggalSelesai) | mitigate | Embedded SQL script verbatim (no re-typing) + pre-check sample query Section #2 untuk visual sanity sebelum DELETE |
| T-324-22 | Info Disclosure | Commit hash di-leak ke pihak luar | accept | Commit hash informational only - public repo (kalau ada) atau internal Git server. NOT secret. |
</threat_model>

<verification>
End-of-plan verification:

1. File exists: `test -f docs/DB_HANDOFF_IT_2026-05-26.html`
2. CSS branding preserved: `grep -c "var(--brand)" docs/DB_HANDOFF_IT_2026-05-26.html` >=1
3. SQL embedded: `grep -c "SET XACT_ABORT ON" docs/DB_HANDOFF_IT_2026-05-26.html` >=1
4. Ordering callout: `grep -c "URUTAN WAJIB" docs/DB_HANDOFF_IT_2026-05-26.html` >=1
5. NO schema change flag: `grep -c "NO migration\|NONE" docs/DB_HANDOFF_IT_2026-05-26.html` >=1
6. Browser smoke (manual - bukan blocker): open file di Chrome/Edge, layout intact dengan branding Pertamina visible
7. Commit hash placeholder updated (kalau dependencies SUMMARY tersedia) atau di-flag di SUMMARY untuk final update sebelum kirim ke IT
</verification>

<success_criteria>
- HTML handoff doc lengkap dengan 8 section
- Verbatim CSS dari template 2026-05-13 (Pertamina branding)
- SQL script embedded verbatim
- Explicit ordering callout (Step 1 deploy code DULU)
- IT bisa Ctrl+P -> Save as PDF tanpa layout broken
- Atomic commit message: `docs(324-04): IT handoff cleanup TrainingRecord 2026-05-26`
- File siap dikirim ke IT team setelah Plan 01 + Plan 03 SUMMARY tersedia (commit hash + filter clause final)
</success_criteria>

<output>
After completion, create `.planning/phases/324-fix-duplicate-trainingrecord-auto-create-on-assessment-compl/324-04-SUMMARY.md` containing:
- Commit hash (handoff doc commit)
- File path: docs/DB_HANDOFF_IT_2026-05-26.html
- Status placeholder commit hash + filter clause (final atau pending update kalau dependencies belum siap saat task execute)
- Instruction untuk Rino: "Kirim file `docs/DB_HANDOFF_IT_2026-05-26.html` ke IT team via [channel - email/chat] dengan commit hash terkonfirmasi (lihat 324-01-SUMMARY) supaya IT bisa pull + execute cleanup."
- Phase 324 END-OF-MILESTONE checklist: semua 5 DUPL-XX requirement ter-cover, ready untuk `/gsd-verify-work 324` atau `/gsd-complete-phase 324`
</output>
