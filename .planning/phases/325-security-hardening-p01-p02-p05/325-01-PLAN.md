---
phase: 325
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - HcPortal.Tests/HcPortal.Tests.csproj
  - HcPortal.Tests/FileUploadHelperTests.cs
  - HcPortal.sln
  - .gitignore
autonomous: true
requirements: [SC-6]
security_threats: []

must_haves:
  truths:
    - "xUnit project HcPortal.Tests exists sibling ke HcPortal.csproj"
    - "Test project terdaftar di HcPortal.sln"
    - "Test class FileUploadHelperTests.cs ada dengan 6 stub test (semua RED awal — belum compile karena helper belum diubah)"
    - "dotnet build sln-wide tetap green (test project compile walau test assertion RED)"
  artifacts:
    - path: "HcPortal.Tests/HcPortal.Tests.csproj"
      provides: "xUnit v2 project net8.0 + project reference HcPortal.csproj"
      contains: "Microsoft.NET.Test.Sdk"
    - path: "HcPortal.Tests/FileUploadHelperTests.cs"
      provides: "6 test case skeleton (NullFile, ValidPdf, ValidJpg, ValidPng, ExeRenamedPdf, UnsupportedExtension, JpegAliasMatchesJpg)"
      min_lines: 60
    - path: "HcPortal.sln"
      provides: "Solution entry untuk test project"
      contains: "HcPortal.Tests.csproj"
  key_links:
    - from: "HcPortal.Tests/HcPortal.Tests.csproj"
      to: "HcPortal.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"..\\\\HcPortal.csproj\""
    - from: "HcPortal.sln"
      to: "HcPortal.Tests/HcPortal.Tests.csproj"
      via: "Project entry"
      pattern: "HcPortal\\.Tests"
---

<objective>
Bootstrap xUnit v2 test project (`HcPortal.Tests/`) sibling ke `HcPortal.csproj`, daftarkan ke solution existing (`HcPortal.sln`), dan tulis 6 stub test case `FileUploadHelperTests.cs` (RED dulu — assertion akan PASS setelah Plan 02 implementasi magic byte).

Purpose: Wave 0 foundation per D-08 + Nyquist. Plan 02 depends pada test project sudah ada agar `dotnet test --filter FileUploadHelperTests` punya target valid. Stub test RED dulu (TDD discipline) — Plan 02 akan flip ke GREEN setelah magic byte logic landed.

Output: `HcPortal.Tests/HcPortal.Tests.csproj` (xUnit v2 + Microsoft.NET.Test.Sdk + xunit.runner.visualstudio + ProjectReference ke HcPortal.csproj), `HcPortal.Tests/FileUploadHelperTests.cs` (6 test stub), `HcPortal.sln` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-VALIDATION.md
@docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md
@HcPortal.sln
@HcPortal.csproj
@.gitignore

<interfaces>
Existing solution structure (verified RESEARCH.md):
```
HcPortal.sln (25 baris, 1 project entry HcPortal.csproj GUID {B16CC238-1A00-06DF-1F9A-DEF516C8CD8D})
HcPortal.csproj (Microsoft.NET.Sdk.Web, net8.0)
Helpers/FileUploadHelper.cs (static class, ValidateCertificateFile + SaveFileAsync + DeleteFile)
Models/AssessmentConstants.cs (FileValidation.AllowedCertificateExtensions + MaxCertificateFileSizeBytes)
tests/ (Playwright TypeScript — JANGAN sentuh, beda role)
```

Target NEW project structure:
```
HcPortal.Tests/
  HcPortal.Tests.csproj  (Microsoft.NET.Sdk, net8.0, xUnit v2)
  FileUploadHelperTests.cs  (6 test, stub RED awal)
```

xUnit v2 versions (verified RESEARCH.md §Code Examples):
- Microsoft.NET.Test.Sdk 17.13.0
- xunit 2.9.3
- xunit.runner.visualstudio 3.0.1
- coverlet.collector 6.0.4 (opsional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap xUnit project + register ke solution</name>
  <files>HcPortal.Tests/HcPortal.Tests.csproj, HcPortal.sln, .gitignore</files>

  <read_first>
    - `HcPortal.sln` — confirm existing 1 project entry HcPortal.csproj sebelum tambah
    - `HcPortal.csproj` — verify TargetFramework net8.0 (test project harus match)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md` §D-08 — anti-scope: minimum viable, NO CI integration
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Code Examples §xUnit Project Bootstrap (line 474-505) — verbatim csproj content + version pinning
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Risk 4 (line 567-572) — pakai `Microsoft.NET.Sdk` BUKAN `Microsoft.NET.Sdk.Web`
    - `.gitignore` existing — verify cover `**/bin/` + `**/obj/` (per RESEARCH §Runtime State Inventory A4)
  </read_first>

  <action>
    Eksekusi 3 langkah berikut dari cwd repo root:

    **Langkah 1: Generate xUnit project via dotnet CLI (skip manual file authoring).**
    ```bash
    dotnet new xunit -n HcPortal.Tests -o HcPortal.Tests --framework net8.0
    ```
    Template akan create `HcPortal.Tests/HcPortal.Tests.csproj` + `HcPortal.Tests/UnitTest1.cs` boilerplate.

    **Langkah 2: Edit csproj agar match RESEARCH §Code Examples verbatim (sinkronisasi versi paket + tambah ProjectReference ke HcPortal.csproj).**
    Overwrite `HcPortal.Tests/HcPortal.Tests.csproj` jadi VERBATIM (per D-08 + RESEARCH.md §Code Examples line 477-504):
    ```xml
    <Project Sdk="Microsoft.NET.Sdk">

      <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <IsPackable>false</IsPackable>
        <IsTestProject>true</IsTestProject>
      </PropertyGroup>

      <ItemGroup>
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.13.0" />
        <PackageReference Include="xunit" Version="2.9.3" />
        <PackageReference Include="xunit.runner.visualstudio" Version="3.0.1">
          <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
          <PrivateAssets>all</PrivateAssets>
        </PackageReference>
        <PackageReference Include="coverlet.collector" Version="6.0.4">
          <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
          <PrivateAssets>all</PrivateAssets>
        </PackageReference>
      </ItemGroup>

      <ItemGroup>
        <ProjectReference Include="..\HcPortal.csproj" />
      </ItemGroup>

    </Project>
    ```

    **Langkah 3: Hapus boilerplate template, register ke sln, restore.**
    ```bash
    rm HcPortal.Tests/UnitTest1.cs  # boilerplate generated dotnet new
    dotnet sln HcPortal.sln add HcPortal.Tests/HcPortal.Tests.csproj
    dotnet restore HcPortal.Tests/HcPortal.Tests.csproj
    ```

    **Langkah 4: Verify `.gitignore` cover `HcPortal.Tests/bin/` + `HcPortal.Tests/obj/`.**
    Cek apakah `.gitignore` punya pattern `**/bin/` + `**/obj/` (standard .NET ignore). Kalau hanya literal `HcPortal/bin/` → append 2 baris baru:
    ```
    HcPortal.Tests/bin/
    HcPortal.Tests/obj/
    ```
    Kalau sudah pakai wildcard, skip.

    **Catatan:** Pakai Bahasa Indonesia di komentar code kalau ada (per CLAUDE.md line 3).
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      test -f HcPortal.Tests/HcPortal.Tests.csproj &amp;&amp; \
      grep -q "Microsoft.NET.Test.Sdk" HcPortal.Tests/HcPortal.Tests.csproj &amp;&amp; \
      grep -q "xunit" HcPortal.Tests/HcPortal.Tests.csproj &amp;&amp; \
      grep -q "ProjectReference Include=\"..\\\\HcPortal.csproj\"" HcPortal.Tests/HcPortal.Tests.csproj &amp;&amp; \
      grep -q "HcPortal.Tests" HcPortal.sln &amp;&amp; \
      dotnet build HcPortal.sln --nologo -v quiet
    </automated>
  </verify>

  <acceptance_criteria>
    - File `HcPortal.Tests/HcPortal.Tests.csproj` exists
    - csproj memiliki PackageReference `Microsoft.NET.Test.Sdk` Version "17.13.0"
    - csproj memiliki PackageReference `xunit` Version "2.9.3"
    - csproj memiliki PackageReference `xunit.runner.visualstudio` Version "3.0.1"
    - csproj memiliki `<ProjectReference Include="..\HcPortal.csproj" />`
    - csproj SDK = `Microsoft.NET.Sdk` (BUKAN `Microsoft.NET.Sdk.Web`)
    - `HcPortal.sln` punya entry baris baru mengandung string `HcPortal.Tests`
    - `dotnet build HcPortal.sln` exit code 0
    - File `HcPortal.Tests/UnitTest1.cs` (template boilerplate) sudah dihapus
    - `.gitignore` mencakup `HcPortal.Tests/bin/` + `HcPortal.Tests/obj/` (literal atau via wildcard `**/bin/`)
  </acceptance_criteria>

  <done>
    `dotnet build HcPortal.sln` green dengan 2 project (HcPortal + HcPortal.Tests). `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` runs (0 test sebab UnitTest1.cs dihapus, exit code 0 — "no tests in assembly" warning OK karena Task 2 yang isi).
  </done>
</task>

<task type="auto">
  <name>Task 2: Tulis 6 stub test FileUploadHelperTests.cs (RED awal)</name>
  <files>HcPortal.Tests/FileUploadHelperTests.cs</files>

  <read_first>
    - `Helpers/FileUploadHelper.cs` — current API signature `ValidateCertificateFile(IFormFile?)` returns `(bool IsValid, string? Error)`
    - `Models/AssessmentConstants.cs` — confirm namespace `HcPortal.Models` + class `AssessmentConstants.FileValidation`
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Code Examples §Unit Test Skeleton (line 392-472) — VERBATIM 6 test stub source of truth
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md` §Claude's Discretion — test naming convention `MethodName_Scenario_ExpectedResult` (xUnit industry standard)
  </read_first>

  <action>
    Create `HcPortal.Tests/FileUploadHelperTests.cs` dengan content VERBATIM dari RESEARCH.md §Code Examples §Unit Test Skeleton (line 392-472):

    ```csharp
    // HcPortal.Tests/FileUploadHelperTests.cs
    using HcPortal.Helpers;
    using HcPortal.Models;
    using Microsoft.AspNetCore.Http;
    using Xunit;
    using System.IO;
    using System.Text;

    namespace HcPortal.Tests;

    public class FileUploadHelperTests
    {
        private static IFormFile MakeFile(string fileName, byte[] content)
        {
            var stream = new MemoryStream(content);
            return new FormFile(stream, 0, content.Length, "file", fileName)
            {
                Headers = new HeaderDictionary(),
                ContentType = "application/octet-stream"
            };
        }

        [Fact]
        public void ValidateCertificateFile_NullFile_ReturnsValid()
        {
            var (ok, err) = FileUploadHelper.ValidateCertificateFile(null);
            Assert.True(ok);
            Assert.Null(err);
        }

        [Fact]
        public void ValidateCertificateFile_ValidPdf_ReturnsValid()
        {
            var pdf = new byte[] { 0x25, 0x50, 0x44, 0x46, 0x2D, 0x31, 0x2E, 0x34 }; // %PDF-1.4
            var (ok, _) = FileUploadHelper.ValidateCertificateFile(MakeFile("test.pdf", pdf));
            Assert.True(ok);
        }

        [Fact]
        public void ValidateCertificateFile_ValidJpg_ReturnsValid()
        {
            var jpg = new byte[] { 0xFF, 0xD8, 0xFF, 0xE0, 0x00, 0x10, 0x4A, 0x46 };
            var (ok, _) = FileUploadHelper.ValidateCertificateFile(MakeFile("test.jpg", jpg));
            Assert.True(ok);
        }

        [Fact]
        public void ValidateCertificateFile_ValidPng_ReturnsValid()
        {
            var png = new byte[] { 0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A };
            var (ok, _) = FileUploadHelper.ValidateCertificateFile(MakeFile("test.png", png));
            Assert.True(ok);
        }

        [Fact]
        public void ValidateCertificateFile_ExeRenamedPdf_ReturnsInvalidMagicByte()
        {
            var exe = new byte[] { 0x4D, 0x5A, 0x90, 0x00, 0x03, 0x00, 0x00, 0x00 }; // MZ exe
            var (ok, err) = FileUploadHelper.ValidateCertificateFile(MakeFile("malware.pdf", exe));
            Assert.False(ok);
            Assert.Contains("magic byte", err!);
        }

        [Fact]
        public void ValidateCertificateFile_UnsupportedExtension_ReturnsInvalid()
        {
            var bytes = new byte[] { 0x00, 0x01, 0x02 };
            var (ok, err) = FileUploadHelper.ValidateCertificateFile(MakeFile("test.docx", bytes));
            Assert.False(ok);
            Assert.Contains("PDF, JPG", err!);
        }

        [Fact]
        public void MatchesMagicByte_JpegAliasMatchesJpg()
        {
            var jpg = new byte[] { 0xFF, 0xD8, 0xFF, 0xE1, 0x00, 0x00, 0x00, 0x00 }; // EXIF variant
            Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpeg", jpg));
            Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpg", jpg));
        }
    }
    ```

    **STATUS YANG DIHARAPKAN setelah Task 2 selesai (sebelum Plan 02 jalan):**
    - 7 test method (1 setup helper `MakeFile` + 7 [Fact]).
    - **3 test EXPECTED PASS (GREEN sekarang):** `NullFile_ReturnsValid`, `UnsupportedExtension_ReturnsInvalid` (extension check existing sudah jalan), dan `ValidPdf_ReturnsValid` / `ValidJpg_ReturnsValid` / `ValidPng_ReturnsValid` (existing ValidateCertificateFile tidak reject valid file).
    - **4 test EXPECTED COMPILE-ERROR / RED:**
      1. `ExeRenamedPdf_ReturnsInvalidMagicByte` → existing helper PASS-kan exe rename (belum ada magic byte check), test gagal assert False.
      2. `MatchesMagicByte_JpegAliasMatchesJpg` → COMPILE ERROR karena `AssessmentConstants.FileValidation.MatchesMagicByte` belum exist (Plan 02 yang add per D-09).

    **Itu OK** — Plan 02 yang flip RED → GREEN. Untuk Task 2 ini, kita expect **build berhasil tapi 2 test fail** (compile-error 1 test = build fail keseluruhan).

    **HOWEVER:** xUnit build akan fail kalau ada compile error di test file. Untuk amankan build green Task 2, **commit out** baris yang reference `MatchesMagicByte` (belum exist) pakai `#if false ... #endif` blok, kasih komentar bahasa Indonesia:

    Tambah di test `MatchesMagicByte_JpegAliasMatchesJpg`:
    ```csharp
    [Fact]
    public void MatchesMagicByte_JpegAliasMatchesJpg()
    {
        var jpg = new byte[] { 0xFF, 0xD8, 0xFF, 0xE1, 0x00, 0x00, 0x00, 0x00 }; // EXIF variant
        // TODO Plan 02 D-09: aktifkan setelah AssessmentConstants.FileValidation.MatchesMagicByte ditambahkan.
        // Sementara test ini diskip supaya build Wave 0 tetap green.
        Assert.Skip("MatchesMagicByte belum diimplementasikan — Plan 02 akan aktifkan.");
        // Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpeg", jpg));
        // Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpg", jpg));
    }
    ```

    Wait — `Assert.Skip` adalah xUnit v3 API. Untuk xUnit v2.9.3 tidak ada `Assert.Skip`. Pakai `[Fact(Skip = "...")]` attribute instead:

    ```csharp
    [Fact(Skip = "TODO Plan 02 D-09: MatchesMagicByte belum diimplementasikan — Plan 02 akan aktifkan + remove Skip.")]
    public void MatchesMagicByte_JpegAliasMatchesJpg()
    {
        var jpg = new byte[] { 0xFF, 0xD8, 0xFF, 0xE1, 0x00, 0x00, 0x00, 0x00 }; // EXIF variant
        Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpeg", jpg));
        Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpg", jpg));
    }
    ```

    Tapi ini masih COMPILE error karena `MatchesMagicByte` belum exist. Solusi clean: **comment out body test method**, simpan stub Fact dengan Skip attribute supaya method exists, body comment-out:

    ```csharp
    [Fact(Skip = "TODO Plan 02 D-09: MatchesMagicByte belum diimplementasikan.")]
    public void MatchesMagicByte_JpegAliasMatchesJpg()
    {
        // Body asli akan diaktifkan Plan 02 Task 2 (D-09 magic byte dict + helper)
        // var jpg = new byte[] { 0xFF, 0xD8, 0xFF, 0xE1, 0x00, 0x00, 0x00, 0x00 };
        // Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpeg", jpg));
        // Assert.True(AssessmentConstants.FileValidation.MatchesMagicByte(".jpg", jpg));
    }
    ```

    Untuk test `ExeRenamedPdf_ReturnsInvalidMagicByte` — body sudah compile (panggil helper existing), hanya assertion yang gagal sekarang. Biarkan as-is — Plan 02 yang flip RED → GREEN. Tidak perlu skip.

    Apply approach yang sama untuk test lain yang assertion-nya akan RED dulu sebelum Plan 02:
    - `ValidPdf_ReturnsValid`, `ValidJpg_ReturnsValid`, `ValidPng_ReturnsValid` → tetap PASS sekarang (existing helper tidak reject magic byte → return valid).
    - `ExeRenamedPdf_ReturnsInvalidMagicByte` → akan FAIL sekarang (existing helper accept .pdf extension tanpa cek magic). **Apply `[Fact(Skip = "TODO Plan 02 D-02/D-03: magic byte check belum diimplementasikan — Plan 02 akan aktifkan + remove Skip.")]`** supaya Wave 0 dotnet test green.

    **Acceptance kondisi final Task 2:** `dotnet test HcPortal.Tests/` exit 0 dengan 4 test pass + 2 test skipped (`ExeRenamedPdf` + `MatchesMagicByte_JpegAliasMatchesJpg`) + 0 fail. Plan 02 nanti remove dua `Skip` attribute setelah implementasi.
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      test -f HcPortal.Tests/FileUploadHelperTests.cs &amp;&amp; \
      grep -c "\[Fact" HcPortal.Tests/FileUploadHelperTests.cs &amp;&amp; \
      grep -q "ValidateCertificateFile_NullFile_ReturnsValid" HcPortal.Tests/FileUploadHelperTests.cs &amp;&amp; \
      grep -q "ValidateCertificateFile_ExeRenamedPdf_ReturnsInvalidMagicByte" HcPortal.Tests/FileUploadHelperTests.cs &amp;&amp; \
      grep -q "MatchesMagicByte_JpegAliasMatchesJpg" HcPortal.Tests/FileUploadHelperTests.cs &amp;&amp; \
      dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo
    </automated>
  </verify>

  <acceptance_criteria>
    - File `HcPortal.Tests/FileUploadHelperTests.cs` exists dengan ≥7 line `[Fact` (6 test + helper)
    - Test names match RESEARCH.md skeleton verbatim (NullFile, ValidPdf, ValidJpg, ValidPng, ExeRenamedPdf, UnsupportedExtension, JpegAliasMatchesJpg)
    - `[Fact(Skip = "...Plan 02..."` attribute ada di 2 test stub: `ExeRenamedPdf_ReturnsInvalidMagicByte` + `MatchesMagicByte_JpegAliasMatchesJpg` (TODO marker untuk Plan 02 remove)
    - `dotnet test HcPortal.Tests/` exit code 0 dengan output mengandung "Passed: 4" + "Skipped: 2" + "Failed: 0"
    - `dotnet build HcPortal.sln` exit code 0
    - Komentar code dalam Bahasa Indonesia (per CLAUDE.md line 3)
  </acceptance_criteria>

  <done>
    Wave 0 foundation siap. Plan 02 punya target test target valid, plan 02 cukup: (a) implementasi D-01/D-02/D-03/D-09/D-10 di helper + constants, (b) hapus 2 `Skip` attribute dari test, (c) `dotnet test` jadi "Passed: 6 Skipped: 0".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 325 = security phase. Plan 01 = test infrastructure bootstrap (no production code touched). Trust boundaries di Plan 02-04.

| Boundary | Description |
|----------|-------------|
| Test project → production assembly | Test project consume `HcPortal.dll` via ProjectReference — no inverse direction allowed |
| Build output isolation | `HcPortal.Tests/bin` + `obj/` must be gitignored (per RESEARCH §Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-04 | Tampering | xUnit project SDK choice (Web vs Standard) | mitigate | RESEARCH §Risk 4: pakai `dotnet new xunit` template (auto `Microsoft.NET.Sdk` standard) — TIDAK manual copy from HcPortal.csproj Web SDK |
| — | — | xUnit project auto-load pollute production build | mitigate | RESEARCH §Pitfall 4: sln explicit reference, `dotnet build HcPortal.csproj` (single project) tetap exclude test |
</threat_model>

<verification>
- `dotnet build HcPortal.sln` exit code 0 (2 project compile)
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit code 0 dengan 4 pass + 2 skip + 0 fail
- `HcPortal.sln` baris bertambah 1 project entry baru
- `.gitignore` cover test project bin/obj
- Komentar dalam Bahasa Indonesia
</verification>

<success_criteria>
SC-6 Foundation: Unit test helper `ValidateCertificateFile` 6 case skeleton ada (4 GREEN sekarang, 2 SKIP TODO Plan 02). Plan 02 akan flip ke 6/6 pass.
</success_criteria>

<output>
After completion, create `.planning/phases/325-security-hardening-p01-p02-p05/325-01-SUMMARY.md` summarizing:
- Files created (csproj + test cs + sln update)
- xUnit package versions pinned
- Test count (4 passing + 2 skipped TODO Plan 02)
- Verification commands executed
- Handoff note untuk Plan 02 (remove 2 Skip attribute setelah magic byte implementation)
</output>
