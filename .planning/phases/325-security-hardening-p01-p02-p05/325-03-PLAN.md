---
phase: 325
plan: 03
type: execute
wave: 2
depends_on: [325-02]
files_modified:
  - Controllers/TrainingAdminController.cs
autonomous: true
requirements: [SC-2, SC-3]
security_threats: [T-325-04]

must_haves:
  truths:
    - "3 inline duplicate validation sites di TrainingAdminController dihapus + diganti panggil FileUploadHelper.ValidateCertificateFile"
    - "Magic byte check Plan 02 sekarang ACTIVE di endpoint AddTraining + EditTraining (sebelumnya bypass-able karena inline duplicate)"
    - "Site #1 line 206-215 (AddTraining single file) — refactor"
    - "Site #2 line 221-233 (AddTraining per-worker loop) — refactor"
    - "Site #3 line 459-471 (EditTraining single file) — refactor"
    - "ModelState error message + TempData error message tetap konsisten dengan existing (Hanya file PDF JPG PNG + Ukuran 10MB + Isi file tidak cocok magic byte)"
    - "Caller SaveFileAsync existing tidak break (default ILogger? null)"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "3 endpoint dengan validation via helper (bukan inline)"
      contains: "FileUploadHelper.ValidateCertificateFile"
  key_links:
    - from: "TrainingAdminController.AddTraining POST"
      to: "FileUploadHelper.ValidateCertificateFile"
      via: "method call"
      pattern: "FileUploadHelper\\.ValidateCertificateFile"
    - from: "TrainingAdminController.EditTraining POST"
      to: "FileUploadHelper.ValidateCertificateFile"
      via: "method call"
      pattern: "FileUploadHelper\\.ValidateCertificateFile"
---

<objective>
Refactor 3 inline duplicate file validation sites di `Controllers/TrainingAdminController.cs` (line 206-215, 221-233, 459-471) agar memanggil `FileUploadHelper.ValidateCertificateFile` (existing pattern di line 581 `AddManualAssessment` + 681 `EditManualAssessment`). Tanpa refactor ini, magic byte fix Plan 02 BYPASS-able di 3 endpoint Add/Edit Training — Postman upload `.exe→.pdf` lewat `/Admin/AddTraining` lolos (RESEARCH §Pitfall 1 line 348-352).

Purpose: Address T-325-04 (P02 bypass via inline duplicate). RESEARCH CORRECTION 3 critical — spec §3 hanya mention line 206-215, tapi codebase verification 2026-05-27 confirm 3 site. Plan 02 implementasi helper TIDAK efektif tanpa Plan 03 refactor.

Output: 3 endpoint pakai helper, 0 inline duplicate, magic byte check ACTIVE di Add/Edit Training.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-02-SUMMARY.md
@docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md
@Controllers/TrainingAdminController.cs
@Helpers/FileUploadHelper.cs

<interfaces>
**Helper signature (post-Plan 02):**
```csharp
public static (bool IsValid, string? Error) ValidateCertificateFile(IFormFile? file);
```
Returns tuple — `IsValid=true` kalau valid (atau null/empty file = early return), `IsValid=false` kalau invalid dengan error message Bahasa Indonesia.

**Existing model usage di TrainingAdminController.cs:**
- `AddManualAssessment` line 581 (model dengan `WorkerCerts` loop) — VERIFIED uses helper, ini reference pattern.
- `EditManualAssessment` line 681 — VERIFIED uses helper.

**3 inline duplicate sites TARGET refactor (verbatim per RESEARCH §Codebase Findings line 87-95 + this plan's Read):**

**Site #1 — `AddTraining` POST single CertificateFile (line 202-216):**
```csharp
// File validation
string? sertifikatUrl = null;
if (model.CertificateFile != null && model.CertificateFile.Length > 0)
{
    var allowedExtensions = new[] { ".pdf", ".jpg", ".jpeg", ".png" };
    var ext = Path.GetExtension(model.CertificateFile.FileName).ToLowerInvariant();
    if (!allowedExtensions.Contains(ext))
    {
        ModelState.AddModelError("CertificateFile", "Hanya file PDF, JPG, dan PNG yang diperbolehkan.");
    }
    if (model.CertificateFile.Length > 10 * 1024 * 1024)
    {
        ModelState.AddModelError("CertificateFile", "Ukuran file maksimal 10MB.");
    }
}
```

**Site #2 — `AddTraining` POST per-worker loop WorkerCerts (line 218-233):**
```csharp
// Per-worker file validation (all-or-nothing)
if (hasWorkerCerts)
{
    var allowedExts = new[] { ".pdf", ".jpg", ".jpeg", ".png" };
    foreach (var wc in model.WorkerCerts!)
    {
        if (wc.CertificateFile != null && wc.CertificateFile.Length > 0)
        {
            var wcExt = Path.GetExtension(wc.CertificateFile.FileName).ToLowerInvariant();
            if (!allowedExts.Contains(wcExt))
                ModelState.AddModelError("", $"File untuk pekerja {wc.UserId} harus berformat PDF, JPG, atau PNG.");
            if (wc.CertificateFile.Length > 10 * 1024 * 1024)
                ModelState.AddModelError("", $"File untuk pekerja {wc.UserId} melebihi batas 10MB.");
        }
    }
}
```

**Site #3 — `EditTraining` POST single CertificateFile (line 456-471):**
```csharp
// File validation
if (model.CertificateFile != null && model.CertificateFile.Length > 0)
{
    var allowedExtensions = new[] { ".pdf", ".jpg", ".jpeg", ".png" };
    var ext = Path.GetExtension(model.CertificateFile.FileName).ToLowerInvariant();
    if (!allowedExtensions.Contains(ext))
    {
        TempData["Error"] = "Hanya file PDF, JPG, dan PNG yang diperbolehkan.";
        return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    }
    if (model.CertificateFile.Length > 10 * 1024 * 1024)
    {
        TempData["Error"] = "Ukuran file maksimal 10MB.";
        return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    }
}
```

**Pattern target (existing reference di AddManualAssessment line 581):**
```csharp
var (isValid, errorMsg) = FileUploadHelper.ValidateCertificateFile(model.CertificateFile);
if (!isValid)
    ModelState.AddModelError("CertificateFile", errorMsg!);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor 3 inline duplicate sites di TrainingAdminController → panggil FileUploadHelper.ValidateCertificateFile</name>
  <files>Controllers/TrainingAdminController.cs</files>

  <read_first>
    - `Controllers/TrainingAdminController.cs` line 195-275 — AddTraining POST (Site #1 dan Site #2)
    - `Controllers/TrainingAdminController.cs` line 450-475 — EditTraining POST (Site #3)
    - `Controllers/TrainingAdminController.cs` line 575-600 — AddManualAssessment POST (REFERENCE PATTERN existing helper usage)
    - `Helpers/FileUploadHelper.cs` — post-Plan 02 signature
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Common Pitfalls §Pitfall 1 (line 348-352) — verbatim warning
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Codebase Findings §TrainingAdminController (line 84-95) — 3 site line range exact
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md` (RESEARCH CORRECTION 3)
  </read_first>

  <action>
    **Edit `Controllers/TrainingAdminController.cs` di 3 lokasi.**

    **REFACTOR Site #1 — `AddTraining` POST single CertificateFile (line ~202-216):**
    Ganti block existing (lihat `<interfaces>` di atas) dengan:
    ```csharp
    // Phase 325: pakai FileUploadHelper.ValidateCertificateFile (extension + size + magic byte D-02/D-09).
    // Sebelumnya inline duplicate — magic byte fix Plan 02 bypass-able tanpa refactor ini (RESEARCH §Pitfall 1).
    string? sertifikatUrl = null;
    var (certValid, certErr) = FileUploadHelper.ValidateCertificateFile(model.CertificateFile);
    if (!certValid)
        ModelState.AddModelError("CertificateFile", certErr!);
    ```

    **REFACTOR Site #2 — `AddTraining` POST per-worker loop (line ~218-233):**
    Ganti block existing dengan:
    ```csharp
    // Phase 325: per-worker file validation pakai helper (extension + size + magic byte).
    if (hasWorkerCerts)
    {
        foreach (var wc in model.WorkerCerts!)
        {
            var (wcValid, wcErr) = FileUploadHelper.ValidateCertificateFile(wc.CertificateFile);
            if (!wcValid)
            {
                // Preserve message context "untuk pekerja {UserId}" agar UX tetap informatif siapa yang error.
                ModelState.AddModelError("", $"File untuk pekerja {wc.UserId}: {wcErr}");
            }
        }
    }
    ```

    **REFACTOR Site #3 — `EditTraining` POST single CertificateFile (line ~456-471):**
    Endpoint ini pakai pattern `TempData["Error"] + RedirectToAction` (BUKAN `ModelState.AddModelError`) karena Edit dipanggil dari halaman ManageAssessment redirect-back. Preserve pattern:
    ```csharp
    // Phase 325: pakai FileUploadHelper.ValidateCertificateFile (extension + size + magic byte D-02/D-09).
    // Pattern endpoint ini: TempData + RedirectToAction (bukan ModelState) karena dipanggil dari ManageAssessment redirect-back.
    var (editValid, editErr) = FileUploadHelper.ValidateCertificateFile(model.CertificateFile);
    if (!editValid)
    {
        TempData["Error"] = editErr;
        return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    }
    ```

    **CRITICAL preservation rules:**
    - Variable `sertifikatUrl` declaration di Site #1 tetap (digunakan di bawah handler line 290+ untuk save uploaded URL ke record).
    - Variable `hasWorkerCerts` di Site #2 tetap (declared sebelum di handler).
    - Existing pattern `RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" })` di Site #3 preserved.
    - Komentar dalam Bahasa Indonesia (CLAUDE.md line 3).

    **Verifikasi build setelah edit 3 site:**
    ```bash
    dotnet build HcPortal.sln --nologo -v quiet
    ```
    Build green = signature `ValidateCertificateFile(IFormFile?)` match dengan 4 call site total (3 refactored + 2 existing AddManualAssessment/EditManualAssessment yang sudah pakai).
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      ! grep -q "allowedExtensions = new\[\] { \".pdf\"" Controllers/TrainingAdminController.cs &amp;&amp; \
      ! grep -q "allowedExts = new\[\] { \".pdf\"" Controllers/TrainingAdminController.cs &amp;&amp; \
      ! grep -q "10 \* 1024 \* 1024" Controllers/TrainingAdminController.cs &amp;&amp; \
      grep -c "FileUploadHelper.ValidateCertificateFile" Controllers/TrainingAdminController.cs &amp;&amp; \
      dotnet build HcPortal.sln --nologo -v quiet &amp;&amp; \
      dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "allowedExtensions.Contains" Controllers/TrainingAdminController.cs` returns 0 (inline duplicate hilang)
    - `grep -c "allowedExtensions = new\\[\\]" Controllers/TrainingAdminController.cs` returns 0
    - `grep -c "allowedExts = new\\[\\]" Controllers/TrainingAdminController.cs` returns 0
    - `grep -c "10 \\* 1024 \\* 1024" Controllers/TrainingAdminController.cs` returns 0 (size literal moved ke constants)
    - `grep -c "FileUploadHelper.ValidateCertificateFile" Controllers/TrainingAdminController.cs` returns ≥5 (3 refactored sites + 2 existing AddManualAssessment + EditManualAssessment)
    - `dotnet build HcPortal.sln` exit code 0
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit code 0 dengan 6/6 pass (no regression dari Plan 02)
    - `Controllers/TrainingAdminController.cs` tetap valid C# (no orphan brace, variable `sertifikatUrl` + `hasWorkerCerts` preserved)
    - Komentar dalam Bahasa Indonesia
  </acceptance_criteria>

  <done>
    P02 magic byte check ACTIVE di endpoint Add/Edit Training. Postman test `.exe→.pdf` lewat `/Admin/AddTraining` SEKARANG ditolak (Plan 05 manual UAT akan verify). T-325-04 ditutup.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AddTraining form POST → server validation | Inline duplicate validation logic bypass helper magic byte check |
| EditTraining form POST → server validation | Same pattern, beda response (TempData vs ModelState) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-04 | Tampering (P02 bypass) | `TrainingAdminController.cs` 3 inline duplicate sites (line 206-215, 221-233, 459-471) | mitigate | RESEARCH CORRECTION 3: Refactor 3 sites pakai `FileUploadHelper.ValidateCertificateFile` (existing helper). Magic byte check P02 Plan 02 sekarang reachable dari Add/Edit Training endpoint. |
| T-325-02 | Spoofing (root cause P02) | All endpoint accepting cert file upload | mitigate (transitive via T-325-04) | Plan 02 + Plan 03 combined: helper magic byte + 3 endpoint refactor. Tanpa kedua plan, P02 incomplete. |
</threat_model>

<verification>
- 3 endpoint refactor + 2 endpoint existing = 5 call site `FileUploadHelper.ValidateCertificateFile` di TrainingAdminController
- Build green
- Unit test regression: 6/6 pass
- No literal `allowedExtensions` array atau `10 * 1024 * 1024` literal di controller (centralized ke constants via helper)
- Existing endpoint behavior preserved (variable scope, redirect pattern)
</verification>

<success_criteria>
- SC-2 reachable dari endpoint AddTraining + EditTraining (Plan 05 manual UAT verify)
- SC-3 reachable (normal PDF/JPG/PNG tetap lolos via helper)
- T-325-04 mitigated
</success_criteria>

<output>
After completion, create `.planning/phases/325-security-hardening-p01-p02-p05/325-03-SUMMARY.md` covering:
- 3 site refactored (line range pre-refactor + post-refactor)
- Build + test green
- Postman attack scenario ready (Plan 05 manual UAT akan verify)
- Handoff: Plan 04 P05 hard delete FK quick patch
</output>
