---
phase: 325
plan: 04
type: execute
wave: 2
depends_on: [325-01]
files_modified:
  - Controllers/TrainingAdminController.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [SC-4, SC-5]
security_threats: [T-325-03, T-325-05]

must_haves:
  truths:
    - "DeleteTraining (TrainingAdminController line ~527-548) pre-check referencing dengan RenewsTrainingId == id di 2 tabel TR + AS"
    - "DeleteManualAssessment (TrainingAdminController line ~732-753) pre-check referencing dengan RenewsSessionId == id di 2 tabel TR + AS"
    - "DeleteAssessment (AssessmentAdminController line ~2011-2169) pre-check referencing dengan RenewsSessionId == id sebelum BeginTransactionAsync (line 2040) per D-11"
    - "3 endpoint punya catch DbUpdateException SEBELUM existing catch generic (specific-before-general)"
    - "TempData[\"Error\"] pesan VERBATIM D-06 (Tidak bisa hapus + Gagal hapus constraint database dilanggar)"
    - "logger.LogWarning(ex, ...) untuk DbUpdateException catch per D-05"
    - "Phase 323 cascade tx scope di DeleteAssessment line 2040 INTACT (pre-check di luar tx)"
    - "AssessmentAdminController existing catch (Exception ex) line 2163 PRESERVED — bukan replace, di-prepend dengan catch DbUpdateException"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "DeleteTraining + DeleteManualAssessment dengan pre-check + try/catch DbUpdateException"
      contains: "RenewsTrainingId == id"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "DeleteAssessment dengan pre-check sebelum tx + catch DbUpdateException prepend"
      contains: "RenewsSessionId == id"
  key_links:
    - from: "DeleteTraining"
      to: "TrainingRecords.CountAsync(t => t.RenewsTrainingId == id)"
      via: "pre-check query"
      pattern: "RenewsTrainingId == id|RenewsTrainingId == record.Id"
    - from: "DeleteManualAssessment"
      to: "TrainingRecords.CountAsync(t => t.RenewsSessionId == id)"
      via: "pre-check query"
      pattern: "RenewsSessionId == id|RenewsSessionId == session.Id"
    - from: "DeleteAssessment"
      to: "TrainingRecords.CountAsync(t => t.RenewsSessionId == id)"
      via: "pre-check query awal endpoint"
      pattern: "RenewsSessionId == id"
---

<objective>
Implementasi P05 hard delete FK quick patch di 3 endpoint delete dengan FK column CORRECT per entity (RESEARCH CORRECTION 2 — line :744 = AssessmentSession, BUKAN TrainingRecord):

1. `TrainingAdminController.DeleteTraining(int id)` (entity `TrainingRecord`) — pre-check `RenewsTrainingId == id` di 2 tabel TR + AS
2. `TrainingAdminController.DeleteManualAssessment(int id)` (entity `AssessmentSession`) — pre-check `RenewsSessionId == id` di 2 tabel TR + AS
3. `AssessmentAdminController.DeleteAssessment(int id)` (entity `AssessmentSession`) — pre-check `RenewsSessionId == id` di 2 tabel TR + AS, INSERTED sebelum `BeginTransactionAsync` line 2040 per D-11

Setiap endpoint: pre-check + try/catch `DbUpdateException` + `TempData["Error"]` + `_logger.LogWarning` per D-04/D-05/D-06. Soft delete proper defer v20.0.

Purpose: Address T-325-03 (unhandled DB 500 leak) + T-325-05 (TOCTOU race acceptable). Phase 323 cascade tx di DeleteAssessment INTACT — pre-check di luar tx (D-11). Existing generic catch di AssessmentAdminController:2163 PRESERVED — specific catch DbUpdateException prepend ke atas (specific-before-general C# idiom).

Output: 3 endpoint delete dengan UX friendly error message saat FK violation, no regression saat referencing absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-01-SUMMARY.md
@docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md
@Controllers/TrainingAdminController.cs
@Controllers/AssessmentAdminController.cs
@Data/ApplicationDbContext.cs

<interfaces>
**FK schema verified (RESEARCH §Data/ApplicationDbContext.cs Renewal FK Behavior line 113-122):**
- `TR.RenewsTrainingId → TR` OnDelete=NoAction → SQL throws 547 saat parent dihapus.
- `TR.RenewsSessionId → AS` OnDelete=NoAction → same.
- `AS.RenewsSessionId → AS` OnDelete=NoAction → same.
- `AS.RenewsTrainingId → TR` OnDelete=NoAction → same.

**Pre-check query matrix (CRITICAL — RESEARCH CORRECTION 2):**

| Endpoint | Entity dihapus | Pre-check filter | Tabel scan |
|----------|---------------|------------------|------------|
| `DeleteTraining(id)` | TrainingRecord | `RenewsTrainingId == id` | TR + AS |
| `DeleteManualAssessment(id)` | AssessmentSession | `RenewsSessionId == id` | TR + AS |
| `DeleteAssessment(id)` | AssessmentSession | `RenewsSessionId == id` | TR + AS |

**Logger handles:**
- `TrainingAdminController._logger` (ILogger field, ctor injected line 17/24/27)
- `AssessmentAdminController.DeleteAssessment` — `var logger = HttpContext.RequestServices.GetRequiredService<ILogger<AssessmentAdminController>>();` (existing line 2013, not field — reuse local variable)

**Existing endpoint structures verified:**

**`TrainingAdminController.DeleteTraining` (line 523-548, 26 lines current):**
```csharp
public async Task<IActionResult> DeleteTraining(int id)
{
    var record = await _context.TrainingRecords.FindAsync(id);
    if (record == null) return NotFound();

    // existing: physical file delete
    if (!string.IsNullOrEmpty(record.SertifikatUrl)) { ... }

    var actor = await _userManager.GetUserAsync(User);
    _context.TrainingRecords.Remove(record);          // line 539
    await _context.SaveChangesAsync();                // line 540

    if (actor != null)
        await _auditLog.LogAsync(...);

    TempData["Success"] = "Training record berhasil dihapus.";
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
```

**`TrainingAdminController.DeleteManualAssessment` (line 732-753, 22 lines current):**
```csharp
public async Task<IActionResult> DeleteManualAssessment(int id)
{
    var session = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == id && s.IsManualEntry);
    if (session == null) return NotFound();

    FileUploadHelper.DeleteFile(_env.WebRootPath, session.ManualSertifikatUrl);

    var actor = await _userManager.GetUserAsync(User);
    _context.AssessmentSessions.Remove(session);     // line 744
    await _context.SaveChangesAsync();

    if (actor != null)
        await _auditLog.LogAsync(...);

    TempData["Success"] = "Assessment manual berhasil dihapus.";
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
```

**`AssessmentAdminController.DeleteAssessment` (line 2011-2169, 159 lines current — Phase 323 cascade):**
- Entry line 2011, fetch line 2017, PreTest guard line 2030-2035, `BeginTransactionAsync` line 2040, cascade body line 2042-2133, `Remove(assessment)` line 2136, commit 2139, audit 2142-2157, return 2160-2161, **outer `catch (Exception ex)` line 2163-2168** (PRESERVE).
- D-11: INSERT pre-check antara line 2035 (end PreTest guard) dan line 2037-2039 (comment) sebelum `using var tx` line 2040.

**Error message strings LOCKED VERBATIM (D-06):**
- Pre-check fail: `"Tidak bisa hapus: {N} sertifikat lain menggunakan record ini sebagai sumber renewal. Hapus atau update sertifikat pemakai terlebih dulu."`
- DbUpdateException catch: `"Gagal hapus: ada constraint database yang dilanggar."`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch TrainingAdminController.DeleteTraining + DeleteManualAssessment dengan pre-check + try/catch</name>
  <files>Controllers/TrainingAdminController.cs</files>

  <read_first>
    - `Controllers/TrainingAdminController.cs` line 520-560 — DeleteTraining current state (VERIFIED)
    - `Controllers/TrainingAdminController.cs` line 730-755 — DeleteManualAssessment current state (VERIFIED RESEARCH §Codebase Findings — entity = AssessmentSession line :744)
    - `Controllers/TrainingAdminController.cs` line 1-30 — confirm `_logger` field injected (ILogger ctor line 17/24/27 per RESEARCH)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Codebase Findings §TrainingAdminController (line 84-95) — line :744 RESOLUTION = DeleteManualAssessment
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Common Pitfalls §Pitfall 2 (line 354-361) — RenewsSessionId vs RenewsTrainingId distinction
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md` §D-04/D-05/D-06
    - `docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md` §5.3 line 139-166 — VERBATIM snippet template
    - `Data/ApplicationDbContext.cs` line 155-235 — FK schema confirm RenewsTrainingId + RenewsSessionId
  </read_first>

  <action>
    **EDIT 1: `TrainingAdminController.DeleteTraining` (line ~523-548).**

    Wrap body dari fetch + file delete + Remove + SaveChanges di try block, tambah pre-check di awal try (sebelum `_context.TrainingRecords.Remove`), tambah catch `DbUpdateException`. Pattern verbatim spec §5.3 line 139-165 dengan entity = TrainingRecord, FK column = `RenewsTrainingId == id`:

    ```csharp
    // POST /Admin/DeleteTraining
    [HttpPost]
    [ValidateAntiForgeryToken]
    [Authorize(Roles = "Admin, HC")]
    public async Task<IActionResult> DeleteTraining(int id)
    {
        var record = await _context.TrainingRecords.FindAsync(id);
        if (record == null) return NotFound();

        try
        {
            // Phase 325 P05 D-04/D-11: pre-check referencing rows SEBELUM hapus (UX friendly).
            // TrainingRecord punya 2 jenis renewal child: TR lain + AS lain. FK column = RenewsTrainingId.
            var referencingTr = await _context.TrainingRecords
                .CountAsync(t => t.RenewsTrainingId == record.Id);
            var referencingAs = await _context.AssessmentSessions
                .CountAsync(a => a.RenewsTrainingId == record.Id);

            if (referencingTr + referencingAs > 0)
            {
                var total = referencingTr + referencingAs;
                TempData["Error"] = $"Tidak bisa hapus: {total} sertifikat lain "
                                  + "menggunakan record ini sebagai sumber renewal. "
                                  + "Hapus atau update sertifikat pemakai terlebih dulu.";
                return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
            }

            // existing: physical file delete preserved
            if (!string.IsNullOrEmpty(record.SertifikatUrl))
            {
                var path = Path.Combine(_env.WebRootPath, record.SertifikatUrl.TrimStart('/'));
                if (System.IO.File.Exists(path)) System.IO.File.Delete(path);
            }

            var actor = await _userManager.GetUserAsync(User);
            _context.TrainingRecords.Remove(record);
            await _context.SaveChangesAsync();

            if (actor != null)
                await _auditLog.LogAsync(actor.Id, actor.FullName, "Delete",
                    $"Training record dihapus: {record.Judul}", record.Id, "TrainingRecord");

            TempData["Success"] = "Training record berhasil dihapus.";
        }
        catch (DbUpdateException ex)
        {
            // Phase 325 P05 D-05: catch specific DbUpdateException (TOCTOU safety net jika race antara pre-check dan delete).
            _logger.LogWarning(ex, "Delete failed for TrainingRecord {Id}", record.Id);
            TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";
        }
        return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    }
    ```

    **CRITICAL preservation:**
    - Physical file delete logic (line 532-536) DI DALAM try block — kalau FK pre-check pass tapi later violation race, file tetap di-clean sebelum DB Remove fire.
    - Wait, sebenarnya kalau race terjadi & DbUpdateException throw, file sudah keburu dihapus → DB rollback tapi file hilang. Apakah ini acceptable? Per RESEARCH §Risk 1 line 541-547 TOCTOU acceptable, plus race window sangat sempit, plus file restorable dari backup. **Acceptable** untuk D-04 quick patch positioning. Tidak refactor untuk transactional file ops sekarang.

    **EDIT 2: `TrainingAdminController.DeleteManualAssessment` (line ~732-753).**

    Sama pattern, entity = AssessmentSession (RESEARCH CORRECTION 2), FK column = `RenewsSessionId == id` (BUKAN RenewsTrainingId — pitfall RESEARCH §Pitfall 2):

    ```csharp
    // POST /Admin/DeleteManualAssessment
    [HttpPost]
    [ValidateAntiForgeryToken]
    [Authorize(Roles = "Admin, HC")]
    public async Task<IActionResult> DeleteManualAssessment(int id)
    {
        var session = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == id && s.IsManualEntry);
        if (session == null) return NotFound();

        try
        {
            // Phase 325 P05 D-04/D-11: pre-check referencing rows SEBELUM hapus.
            // AssessmentSession punya 2 jenis renewal child: TR + AS. FK column = RenewsSessionId (BUKAN RenewsTrainingId — pitfall RESEARCH §Pitfall 2).
            var referencingTr = await _context.TrainingRecords
                .CountAsync(t => t.RenewsSessionId == session.Id);
            var referencingAs = await _context.AssessmentSessions
                .CountAsync(a => a.RenewsSessionId == session.Id);

            if (referencingTr + referencingAs > 0)
            {
                var total = referencingTr + referencingAs;
                TempData["Error"] = $"Tidak bisa hapus: {total} sertifikat lain "
                                  + "menggunakan record ini sebagai sumber renewal. "
                                  + "Hapus atau update sertifikat pemakai terlebih dulu.";
                return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
            }

            FileUploadHelper.DeleteFile(_env.WebRootPath, session.ManualSertifikatUrl);

            var actor = await _userManager.GetUserAsync(User);
            _context.AssessmentSessions.Remove(session);
            await _context.SaveChangesAsync();

            if (actor != null)
                await _auditLog.LogAsync(actor.Id, actor.FullName, "Delete",
                    $"Assessment manual dihapus: {session.Title}", session.Id, "AssessmentSession");

            TempData["Success"] = "Assessment manual berhasil dihapus.";
        }
        catch (DbUpdateException ex)
        {
            _logger.LogWarning(ex, "Delete failed for AssessmentSession (manual) {Id}", session.Id);
            TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";
        }
        return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
    }
    ```

    **Komentar Bahasa Indonesia, error message string VERBATIM D-06.**

    Verify after edit:
    ```bash
    dotnet build HcPortal.sln --nologo -v quiet
    ```
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      grep -nE "RenewsTrainingId == record\.Id|RenewsTrainingId == id" Controllers/TrainingAdminController.cs &amp;&amp; \
      grep -nE "RenewsSessionId == session\.Id|RenewsSessionId == id" Controllers/TrainingAdminController.cs &amp;&amp; \
      grep -c "catch (DbUpdateException" Controllers/TrainingAdminController.cs &amp;&amp; \
      grep -n "Tidak bisa hapus:" Controllers/TrainingAdminController.cs &amp;&amp; \
      grep -n "Gagal hapus: ada constraint database yang dilanggar." Controllers/TrainingAdminController.cs &amp;&amp; \
      grep -n "_logger.LogWarning" Controllers/TrainingAdminController.cs &amp;&amp; \
      dotnet build HcPortal.sln --nologo -v quiet
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "RenewsTrainingId == record.Id|RenewsTrainingId == id" Controllers/TrainingAdminController.cs` returns ≥2 matches (2 tabel pre-check di DeleteTraining)
    - `grep -nE "RenewsSessionId == session.Id|RenewsSessionId == id" Controllers/TrainingAdminController.cs` returns ≥2 matches (2 tabel pre-check di DeleteManualAssessment) — CRITICAL pitfall check
    - `grep -c "catch (DbUpdateException" Controllers/TrainingAdminController.cs` returns 2 (1 per endpoint)
    - `grep -n "Tidak bisa hapus:" Controllers/TrainingAdminController.cs` returns 2 matches (D-06 verbatim)
    - `grep -n "Gagal hapus: ada constraint database yang dilanggar." Controllers/TrainingAdminController.cs` returns 2 matches (D-06 verbatim)
    - `grep -n "_logger.LogWarning" Controllers/TrainingAdminController.cs` returns ≥2 matches (D-05)
    - `dotnet build HcPortal.sln` exit code 0
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit code 0 (no regression Plan 02)
    - Komentar dalam Bahasa Indonesia
  </acceptance_criteria>

  <done>
    DeleteTraining + DeleteManualAssessment punya pre-check + catch FK-specific. RESEARCH CORRECTION 2 honored (RenewsSessionId untuk AS, RenewsTrainingId untuk TR).
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch AssessmentAdminController.DeleteAssessment dengan pre-check pre-tx + catch DbUpdateException prepend</name>
  <files>Controllers/AssessmentAdminController.cs</files>

  <read_first>
    - `Controllers/AssessmentAdminController.cs` line 2010-2170 — DeleteAssessment full body (VERIFIED Phase 323 cascade tx scope)
    - `Controllers/AssessmentAdminController.cs` line 2030-2042 — area D-11 insertion point (sesudah PreTest guard, sebelum `using var tx`)
    - `Controllers/AssessmentAdminController.cs` line 2160-2169 — outer catch `Exception ex` (PRESERVE — RESEARCH CORRECTION 5: specific-before-general prepend)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md` §D-11 — pre-check di awal endpoint, di luar tx scope
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Codebase Findings §AssessmentAdminController line 99-111 — DRIFT WARNING + insertion area
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Architecture Patterns §Pattern 3 line 287-326 — VERBATIM pre-check + catch snippet
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md` §Open Questions Q2 line 660-664 — preserve existing catch
    - `docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md` §5.3 line 168 — "ganti query referencing sesuai entity"
  </read_first>

  <action>
    **EDIT `Controllers/AssessmentAdminController.cs`.**

    **Step 1: Insert pre-check antara line 2035 (end PreTest guard `}`) dan line 2037 (comment "PHASE 312 WR-01") + line 2040 (`using var tx`).**

    Cari block existing (line 2030-2042 verbatim):
    ```csharp
                    // D-19: Block delete individual jika bagian Pre-Post group
                    if (assessment.AssessmentType == "PreTest" || assessment.AssessmentType == "PostTest")
                    {
                        TempData["Error"] = "Sesi ini bagian dari grup Pre-Post Test. Gunakan 'Hapus Grup' untuk menghapus keduanya.";
                        return RedirectToAction("ManageAssessment");
                    }

                    // PHASE 312 WR-01: bungkus guard + cascade dalam transaction agar TOCTOU race
                    // (peserta concurrent POST jawaban antara guard-check dan cascade) dapat ditangani
                    // dengan re-check responseCount dalam transaction, lalu rollback bersih jika race terjadi.
                    using var tx = await _context.Database.BeginTransactionAsync();
    ```

    INSERT pre-check antara baris "}" (akhir PreTest guard) dan "// PHASE 312 WR-01" comment:

    ```csharp
                    // D-19: Block delete individual jika bagian Pre-Post group
                    if (assessment.AssessmentType == "PreTest" || assessment.AssessmentType == "PostTest")
                    {
                        TempData["Error"] = "Sesi ini bagian dari grup Pre-Post Test. Gunakan 'Hapus Grup' untuk menghapus keduanya.";
                        return RedirectToAction("ManageAssessment");
                    }

                    // Phase 325 P05 D-11: pre-check referencing rows SEBELUM buka tx scope (fail-fast UX friendly).
                    // AssessmentSession punya 2 jenis renewal child: TR + AS. FK column = RenewsSessionId.
                    // Pre-check di luar tx supaya tidak konflik dengan Phase 323 cascade tx (line 2040 di bawah).
                    var refTr = await _context.TrainingRecords
                        .CountAsync(t => t.RenewsSessionId == id);
                    var refAs = await _context.AssessmentSessions
                        .CountAsync(a => a.RenewsSessionId == id);

                    if (refTr + refAs > 0)
                    {
                        var total = refTr + refAs;
                        TempData["Error"] = $"Tidak bisa hapus: {total} sertifikat lain "
                                          + "menggunakan record ini sebagai sumber renewal. "
                                          + "Hapus atau update sertifikat pemakai terlebih dulu.";
                        return RedirectToAction("ManageAssessment");
                    }

                    // PHASE 312 WR-01: bungkus guard + cascade dalam transaction agar TOCTOU race
                    // (peserta concurrent POST jawaban antara guard-check dan cascade) dapat ditangani
                    // dengan re-check responseCount dalam transaction, lalu rollback bersih jika race terjadi.
                    using var tx = await _context.Database.BeginTransactionAsync();
    ```

    **Step 2: Tambah `catch (DbUpdateException ex)` SEBELUM existing `catch (Exception ex)` di line ~2163 (specific-before-general C# idiom, RESEARCH CORRECTION 5).**

    Cari block existing line 2161-2169:
    ```csharp
                    logger.LogInformation($"Successfully deleted assessment {id}: {assessmentTitle}");
                    TempData["Success"] = $"Assessment '{assessmentTitle}' has been deleted successfully.";
                    return RedirectToAction("ManageAssessment");
                }
                catch (Exception ex)
                {
                    logger.LogError(ex, "Error deleting assessment {Id}", id);
                    TempData["Error"] = "Gagal menghapus assessment. Silakan coba lagi.";
                    return RedirectToAction("ManageAssessment");
                }
    ```

    INSERT specific catch ABOVE generic catch:
    ```csharp
                    logger.LogInformation($"Successfully deleted assessment {id}: {assessmentTitle}");
                    TempData["Success"] = $"Assessment '{assessmentTitle}' has been deleted successfully.";
                    return RedirectToAction("ManageAssessment");
                }
                catch (DbUpdateException ex)
                {
                    // Phase 325 P05 D-05: safety net jika race TOCTOU antara pre-check dan tx commit (concurrent insert renewal child).
                    logger.LogWarning(ex, "Delete failed for AssessmentSession {Id}: FK constraint", id);
                    TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";
                    return RedirectToAction("ManageAssessment");
                }
                catch (Exception ex)
                {
                    logger.LogError(ex, "Error deleting assessment {Id}", id);
                    TempData["Error"] = "Gagal menghapus assessment. Silakan coba lagi.";
                    return RedirectToAction("ManageAssessment");
                }
    ```

    **CRITICAL preservation:**
    - Existing `catch (Exception ex)` line 2163 NOT replaced — tetap ada di bawah specific catch.
    - Existing `logger.LogError(ex, "Error deleting assessment {Id}", id);` line 2165 PRESERVED di catch generic.
    - Phase 312 WR-01 tx scope INTACT (line 2040 `BeginTransactionAsync` tidak disentuh, pre-check ditambahkan SEBELUM).
    - Phase 323 cascade body line 2076-2133 INTACT.
    - `logger` variable di scope DeleteAssessment = local var line 2013 (`var logger = HttpContext.RequestServices.GetRequiredService<...>`), NOT field — pakai variable lokal ini saat call `logger.LogWarning` di DbUpdateException catch.

    Verify after edit:
    ```bash
    dotnet build HcPortal.sln --nologo -v quiet
    ```
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      grep -n "RenewsSessionId == id" Controllers/AssessmentAdminController.cs &amp;&amp; \
      grep -c "catch (DbUpdateException" Controllers/AssessmentAdminController.cs &amp;&amp; \
      grep -c "catch (Exception ex)" Controllers/AssessmentAdminController.cs &amp;&amp; \
      grep -n "Tidak bisa hapus:" Controllers/AssessmentAdminController.cs &amp;&amp; \
      grep -n "Gagal hapus: ada constraint database yang dilanggar." Controllers/AssessmentAdminController.cs &amp;&amp; \
      grep -n "Gagal menghapus assessment. Silakan coba lagi." Controllers/AssessmentAdminController.cs &amp;&amp; \
      grep -n "Phase 312 WR-01" Controllers/AssessmentAdminController.cs &amp;&amp; \
      grep -n "BeginTransactionAsync" Controllers/AssessmentAdminController.cs &amp;&amp; \
      dotnet build HcPortal.sln --nologo -v quiet
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "RenewsSessionId == id" Controllers/AssessmentAdminController.cs` returns ≥2 matches (2 tabel pre-check) — CRITICAL: BUKAN `RenewsTrainingId` (RESEARCH §Pitfall 2)
    - `grep -c "catch (DbUpdateException" Controllers/AssessmentAdminController.cs` returns ≥1 (new specific catch di DeleteAssessment)
    - `grep -c "catch (Exception ex)" Controllers/AssessmentAdminController.cs` returns ≥1 (existing generic catch PRESERVED — RESEARCH CORRECTION 5)
    - `grep -n "Tidak bisa hapus:" Controllers/AssessmentAdminController.cs` returns 1 (D-06 pre-check verbatim)
    - `grep -n "Gagal hapus: ada constraint database yang dilanggar." Controllers/AssessmentAdminController.cs` returns 1 (D-06 DbUpdateException catch verbatim)
    - `grep -n "Gagal menghapus assessment. Silakan coba lagi." Controllers/AssessmentAdminController.cs` returns 1 (existing generic catch message PRESERVED)
    - `grep -n "Phase 312 WR-01" Controllers/AssessmentAdminController.cs` returns ≥1 (Phase 312 cascade tx comment PRESERVED — verify tx scope intact)
    - `grep -n "BeginTransactionAsync" Controllers/AssessmentAdminController.cs` returns ≥1 (tx scope INTACT)
    - Specific catch DbUpdateException is positioned BEFORE generic catch (line number of DbUpdateException < line number of generic) — verify dengan `grep -n "catch (DbUpdateException\\|catch (Exception ex)" Controllers/AssessmentAdminController.cs | head -2` (within DeleteAssessment scope — easier: just compile + manual smoke)
    - `dotnet build HcPortal.sln` exit code 0
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit code 0
    - Komentar dalam Bahasa Indonesia
  </acceptance_criteria>

  <done>
    DeleteAssessment punya pre-check + dual catch (specific FK + generic preserve). Phase 323 cascade tx tidak terganggu. T-325-03 + T-325-05 mitigated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form delete POST → controller → EF SaveChanges → DB FK NoAction enforce | Tanpa pre-check, DB error 547 surface ke user sebagai 500/generic message |
| Concurrent operation race | Pre-check pass → concurrent INSERT renewal child → delete fire → DB throw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-03 | Information Disclosure / Denial of Service | 3 endpoint delete (TR + AS manual + AS regular) leak DB error 547 sebagai unhandled exception | mitigate | D-04 quick patch: pre-check `RenewsTrainingId`/`RenewsSessionId` count + `catch (DbUpdateException)` + `TempData["Error"]` friendly message (D-06 verbatim). Soft delete proper defer v20.0. |
| T-325-05 | TOCTOU race | Pre-check pass → concurrent INSERT renewal child between pre-check dan SaveChanges → DB throw 547 | accept | RESEARCH §Risk 1 line 541-547: race window sempit (~ms), Portal HC traffic profile rendah (admin endpoint), `catch (DbUpdateException)` sebagai safety net friendly message. Strict ACID isolation defer v20.0 kalau ada bukti race aktif. |
| T-325-03-aux | Information Disclosure (log error message leak) | Existing `catch (Exception ex)` di AssessmentAdminController:2163 — generic message OK, tetapi log Error level reveal stack di file log | accept | Existing pattern preserved (not Phase 325 scope). Log access restricted ke IT operations. |
</threat_model>

<verification>
- `dotnet build HcPortal.sln` exit code 0
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit code 0 (Plan 02 regression check)
- 3 endpoint dengan pre-check + catch DbUpdateException
- FK column distinction honored (TR uses RenewsTrainingId, AS uses RenewsSessionId)
- Phase 323 cascade tx INTACT di DeleteAssessment
- Existing generic catch di AssessmentAdminController PRESERVED (specific-before-general)
- Error message strings D-06 VERBATIM di semua 3 endpoint
- Komentar dalam Bahasa Indonesia
- Manual UAT (Plan 05) akan verify scenario SC-4 + SC-5
</verification>

<success_criteria>
- SC-4 covered: Delete TR dengan referencing → TempData error + tidak terhapus (Plan 05 UAT verify)
- SC-5 covered: Delete TR tanpa referencing → sukses normal (Plan 05 UAT verify)
- T-325-03 mitigated (MED)
- T-325-05 documented as acceptable (LOW)
</success_criteria>

<output>
After completion, create `.planning/phases/325-security-hardening-p01-p02-p05/325-04-SUMMARY.md` covering:
- 3 endpoint patched (line range pre/post)
- FK column matrix (RenewsTrainingId vs RenewsSessionId per entity)
- Phase 323 cascade tx integrity preserved
- Specific-before-general catch ordering in AssessmentAdminController
- Handoff: Plan 05 manual UAT batch (Postman + browser + SEED_WORKFLOW snapshot/restore)
</output>
