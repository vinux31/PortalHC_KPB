---
phase: 325
plan: 05
type: execute
wave: 3
depends_on: [325-02, 325-03, 325-04]
files_modified:
  - docs/SEED_JOURNAL.md
  - .planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]
security_threats: [T-325-01, T-325-02, T-325-03, T-325-04, T-325-05]

must_haves:
  truths:
    - "SC-1 manual UAT: Postman POST /Admin/AddTraining multipart filename=../../test.pdf → file landed flat di uploads/certificates/ (BUKAN parent folder) + log warning capture filename original di app log"
    - "SC-2 manual UAT: Browser upload notepad.exe rename .pdf → form error display 'Isi file tidak cocok dengan ekstensi (magic byte mismatch)'"
    - "SC-3 manual UAT: Browser upload 3 real PDF/JPG/PNG asli (mix variant: Word PDF + scan PDF + linearized PDF + EXIF JPG + JFIF JPG + PNG) → semua lolos validation"
    - "SC-4 manual UAT: DB snapshot → seed TR pair (A + B yang RenewsTrainingId=A.Id) → UI delete A → TempData error 'Tidak bisa hapus: 1 sertifikat lain...' + A masih ada → restore DB"
    - "SC-5 manual UAT: Seed TR standalone (no referencing) → UI delete → TempData success + record terhapus → restore DB"
    - "SEED_JOURNAL.md punya entry untuk Phase 325 UAT (temporary + local-only, snapshot before + restore after)"
    - "325-UAT.md document hasil 5 scenario (PASS/FAIL + screenshot/log path kalau ada)"
  artifacts:
    - path: ".planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md"
      provides: "5 UAT scenario result log dengan PASS/FAIL + observation"
      min_lines: 50
    - path: "docs/SEED_JOURNAL.md"
      provides: "Audit trail seed temp untuk SC4/SC5 dengan cleaned flag"
      contains: "Phase 325"
  key_links:
    - from: "Plan 05 SC-1 Postman test"
      to: "Plan 02 FileUploadHelper.SaveFileAsync (D-01 + D-10)"
      via: "POST /Admin/AddTraining multipart"
      pattern: "Path traversal attempt"
    - from: "Plan 05 SC-2/SC-3 browser test"
      to: "Plan 03 refactored 3 inline sites + Plan 02 helper"
      via: "browser form upload"
      pattern: "magic byte mismatch"
    - from: "Plan 05 SC-4/SC-5 DB seed test"
      to: "Plan 04 3 endpoint pre-check + try/catch"
      via: "UI delete + SEED_WORKFLOW"
      pattern: "Tidak bisa hapus|berhasil dihapus"
---

<objective>
Manual UAT batch akhir Phase 325 — verify 5 success criteria SC-1 sampai SC-5 via Postman (SC-1), browser form (SC-2/SC-3), dan DB snapshot+seed+UI delete+restore (SC-4/SC-5). Plan ini contain checkpoint:human-verify task karena visual/functional verification + manual file system check tidak fit automation.

Purpose: Close-out Phase 325 dengan empirical evidence semua 5 SC observed working pada local dev environment sebelum commit + push. Per CLAUDE.md DEV_WORKFLOW: lokal verify wajib sebelum commit. SEED_WORKFLOW snapshot/restore mandatory untuk seed temporary.

Output: `325-UAT.md` dengan 5 scenario result (PASS/FAIL + observation + log/screenshot path). `SEED_JOURNAL.md` updated. Kalau semua PASS → ready commit + push. IT promo Dev defer ke batch akhir milestone v19.0 (setelah Phase 327 ship).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-CONTEXT.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-RESEARCH.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-VALIDATION.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-02-SUMMARY.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-03-SUMMARY.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-04-SUMMARY.md
@docs/DEV_WORKFLOW.md
@docs/SEED_WORKFLOW.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start app lokal + automated SC-1 Postman path traversal test</name>
  <files>.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md</files>

  <read_first>
    - `CLAUDE.md` §Develop Workflow item 3 — verifikasi lokal wajib `dotnet build` + `dotnet run` di `http://localhost:5277`
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-VALIDATION.md` §Manual-Only Verifications row SC1
    - `docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md` §5.1 line 86-88 — SC1 verifikasi exact step
    - `docs/sertifikat-ecosystem/bug-findings.html` (kalau perlu reference repro original)
  </read_first>

  <action>
    **Step 1: Build + start app lokal.**
    ```bash
    cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && \
    dotnet build HcPortal.sln && \
    dotnet test HcPortal.Tests/HcPortal.Tests.csproj
    ```
    Confirm exit 0 + 6/6 test pass. Lalu jalankan app di background:
    ```bash
    dotnet run --project HcPortal.csproj &
    sleep 5  # tunggu kestrel listen
    curl -s -o /dev/null -w "%{http_code}" http://localhost:5277/  # confirm app up (302 redirect ke /Identity/Account/Login OK)
    ```

    **Step 2: Login admin via curl untuk dapat antiforgery + session cookie.**
    ```bash
    # Step 2a: GET login page → ambil cookie jar + antiforgery token
    curl -s -c /tmp/cookies-325.txt http://localhost:5277/Identity/Account/Login -o /tmp/login-page.html
    AF_TOKEN=$(grep -oP 'name="__RequestVerificationToken"[^>]*value="\K[^"]+' /tmp/login-page.html | head -1)
    echo "AF_TOKEN length: ${#AF_TOKEN}"

    # Step 2b: POST credentials (Email = admin@pertamina.com per memory reference_dev_credentials)
    curl -s -b /tmp/cookies-325.txt -c /tmp/cookies-325.txt -X POST \
      "http://localhost:5277/Identity/Account/Login" \
      -H "Content-Type: application/x-www-form-urlencoded" \
      --data-urlencode "Input.Email=admin@pertamina.com" \
      --data-urlencode "Input.Password={PASSWORD_FROM_MEMORY_OR_USER}" \
      --data-urlencode "__RequestVerificationToken=$AF_TOKEN" \
      -L -o /tmp/login-result.html

    # Confirm login success: cookie .AspNetCore.Identity.Application ada
    grep -q "AspNetCore.Identity.Application" /tmp/cookies-325.txt && echo "Login OK" || echo "Login FAIL"
    ```

    **Catatan:** Kalau password admin tidak ada di memory, **checkpoint** ke user — minta password Admin Portal HC lokal. Memory reference `project_v18.0_assessment_power_tools.md` mention `admin@pertamina.com` tapi tidak password.

    **Step 3: POST `/Admin/AddTraining` dengan multipart filename path traversal (SC-1).**

    Siapkan dummy PDF byte (`%PDF-1.4`) di file `/tmp/test-traversal.pdf`:
    ```bash
    printf '\x25\x50\x44\x46\x2D\x31\x2E\x34\x0A%%EOF\n' > /tmp/test-traversal.pdf
    ```

    Ambil antiforgery token baru dari halaman AddTraining (Admin role required):
    ```bash
    curl -s -b /tmp/cookies-325.txt -c /tmp/cookies-325.txt \
      http://localhost:5277/Admin/AddTraining -o /tmp/addtraining-page.html
    AF2=$(grep -oP 'name="__RequestVerificationToken"[^>]*value="\K[^"]+' /tmp/addtraining-page.html | head -1)
    ```

    Submit dengan filename path traversal (gunakan `-F` curl multipart, set filename header eksplisit):
    ```bash
    curl -s -b /tmp/cookies-325.txt -X POST \
      "http://localhost:5277/Admin/AddTraining" \
      -F "Judul=UAT SC-1 Path Traversal" \
      -F "Penyelenggara=UAT" \
      -F "Kategori=Self-Development" \
      -F "Tanggal=2026-01-01" \
      -F "Status=Lulus" \
      -F "UserId={USER_ID_PEKERJA_VALID}" \
      -F "CertificateType=Permanent" \
      -F "CertificateFile=@/tmp/test-traversal.pdf;filename=../../test.pdf;type=application/pdf" \
      -F "__RequestVerificationToken=$AF2" \
      -L -o /tmp/sc1-result.html -w "HTTP=%{http_code}\n"
    ```

    **Step 4: Verify file landed flat di `wwwroot/uploads/certificates/`.**
    ```bash
    ls -la wwwroot/uploads/certificates/ | tail -5
    # Expected: file dengan name {ts}_{guid}_test.pdf (BUKAN ../../test.pdf di parent folder)
    # Verify TIDAK ada file di parent folder atau /tmp dengan nama 'test.pdf' yang tidak diharapkan
    ls -la wwwroot/uploads/ 2>/dev/null | grep -i "test.pdf" && echo "FAIL: file di parent folder" || echo "OK flat di certificates/"
    ```

    **Step 5: Verify log warning capture (D-10).**
    Cek output `dotnet run` console (atau file log kalau dikonfigurasi). Cari line:
    ```
    warn: HcPortal.Controllers.TrainingAdminController[0]
          Path traversal attempt: filename=../../test.pdf stripped to test.pdf
    ```

    Capture stdout dari `dotnet run` ke `/tmp/app-stdout.log` saat jalankan command Step 1, lalu:
    ```bash
    grep "Path traversal attempt" /tmp/app-stdout.log && echo "SC-1 log OK" || echo "SC-1 log NOT CAPTURED (mungkin Console logger filter level)"
    ```

    **Step 6: Document hasil di 325-UAT.md.**

    Create `.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md` dengan header dan section SC-1:
    ```markdown
    # Phase 325 — UAT Result Log

    **Tanggal UAT:** {tanggal sekarang}
    **Environment:** Lokal Windows + dotnet 8.0.418 + http://localhost:5277
    **Tester:** Claude (automated via curl) + User (manual browser confirm)

    ## SC-1 (P01 Path Traversal) — {PASS/FAIL}

    **Step executed:**
    1. POST /Admin/AddTraining multipart filename=../../test.pdf
    2. ls wwwroot/uploads/certificates/ + grep parent folder
    3. grep "Path traversal attempt" stdout log

    **Observation:**
    - HTTP response: {code}
    - File landed: wwwroot/uploads/certificates/{ts}_{guid}_test.pdf
    - File di parent folder: {none/found}
    - Log warning captured: {yes/no} → "{log line if captured}"

    **Verdict:** {PASS/FAIL}

    ---
    ```
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      test -f .planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md &amp;&amp; \
      grep -q "SC-1" .planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md
    </automated>
  </verify>

  <acceptance_criteria>
    - App jalan di `http://localhost:5277` (curl returns valid HTTP code, bukan connection refused)
    - SC-1 POST executed (HTTP 200/302 redirect — error code 400/500 = need investigation)
    - File landed di `wwwroot/uploads/certificates/` dengan name match pattern `{timestamp}_{guid}_test.pdf` (NOT `../../test.pdf`)
    - No file leaked ke parent folder (`wwwroot/uploads/test.pdf` atau `wwwroot/test.pdf` TIDAK ada)
    - 325-UAT.md exists dengan section SC-1 + verdict
    - Kalau Step 2 login butuh password yang tidak ada di memory → executor checkpoint ke user
  </acceptance_criteria>

  <done>
    SC-1 documented. App siap untuk SC-2/SC-3/SC-4/SC-5 (next tasks). Kalau SC-1 FAIL → executor STOP + report, jangan lanjut sebelum Plan 02/03 di-review.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual browser UAT SC-2 + SC-3 (magic byte mismatch reject + normal upload regression)</name>
  <files>.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md</files>
  <action>
    Lihat <how-to-verify> di bawah — manual browser UAT 2 scenario (SC-2 .exe rename .pdf reject + SC-3 PDF/JPG/PNG asli regression). Append hasil ke 325-UAT.md.
  </action>

  <what-built>
    - Plan 02: `FileUploadHelper.ValidateCertificateFile` magic byte check (D-02/D-03/D-09)
    - Plan 03: 3 inline duplicate refactored di TrainingAdminController (line 206-215, 221-233, 459-471) → panggil helper
    - Magic byte hex: PDF `25 50 44 46`, JPG `FF D8 FF`, PNG `89 50 4E 47`
    - Error message: "Isi file tidak cocok dengan ekstensi (magic byte mismatch)." (D-06 verbatim)
  </what-built>

  <how-to-verify>
    **Pre-req:** App jalan di `http://localhost:5277` (lanjutan Task 1).

    **SC-2 Scenario — `.exe` rename `.pdf` ditolak:**

    1. Siapkan file fake: copy `C:\Windows\System32\notepad.exe` → rename jadi `fake-cert.pdf`. Atau create via PowerShell:
       ```powershell
       Copy-Item "C:\Windows\System32\notepad.exe" "$env:USERPROFILE\Downloads\fake-cert.pdf"
       ```

    2. Buka browser: `http://localhost:5277/Identity/Account/Login` → login `admin@pertamina.com` + password.

    3. Navigate ke `http://localhost:5277/Admin/AddTraining` (atau dari menu Admin → Manage Assessment → tab Training → tombol "Tambah Training").

    4. Fill form minimal: Judul "UAT SC-2 exe rename pdf", Penyelenggara "UAT", Kategori "Self-Development", Tanggal hari ini, Status "Lulus", pilih 1 pekerja, CertificateType "Permanent".

    5. Pada field upload "Sertifikat", select file `fake-cert.pdf` (yang sebenarnya notepad.exe rename).

    6. Submit form.

    **Expected result SC-2:**
    - Form NOT submit successfully — redirect kembali ke AddTraining page atau tampil ModelState error.
    - Error message visible di UI: `"Isi file tidak cocok dengan ekstensi (magic byte mismatch)."` (mungkin terkait `asp-validation-for="CertificateFile"` atau ModelState summary).
    - Tidak ada file baru di `wwwroot/uploads/certificates/` dengan nama `fake-cert.pdf`.

    **SC-3 Scenario — 3 format asli (PDF + JPG + PNG) lolos:**

    Repeat AddTraining form 3 kali dengan file asli berbeda format. Sumber file:
    - PDF asli: download sample dari `docs/sertifikat-ecosystem/` atau printer output PDF (any Word doc Save-As PDF).
    - JPG asli: foto camera atau download dari `wwwroot/img/avatar/` (jika ada existing JPG).
    - PNG asli: screenshot Windows (default save PNG).

    Untuk setiap upload:
    1. Fill form minimal (judul UAT SC-3 PDF/JPG/PNG, pekerja sample).
    2. Upload file asli sesuai format.
    3. Submit.

    **Expected result SC-3:**
    - Form submit successful (3×) — redirect ke ManageAssessment dengan TempData success "Training record berhasil ditambahkan."
    - 3 file baru muncul di `wwwroot/uploads/certificates/` dengan name pattern `{ts}_{guid}_{originalName}`.
    - Tidak ada error message di UI.

    **Catat hasil ke `.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md`:**

    Append section:
    ```markdown
    ## SC-2 (P02 .exe rename .pdf reject) — {PASS/FAIL}

    **Pre-condition:** notepad.exe rename fake-cert.pdf

    **Observation:**
    - Form submit: {rejected/accepted}
    - Error message visible: {yes/no} → "{exact message}"
    - File in uploads/: {none/found}

    **Verdict:** {PASS/FAIL}

    ## SC-3 (P02 normal PDF/JPG/PNG regression) — {PASS/FAIL}

    | Format | File source | Submit result | TempData |
    |--------|-------------|---------------|----------|
    | PDF    | {source}    | {success/fail}| {message}|
    | JPG    | {source}    | {success/fail}| {message}|
    | PNG    | {source}    | {success/fail}| {message}|

    **Verdict:** {PASS/FAIL}

    ---
    ```
  </how-to-verify>

  <resume-signal>
    Setelah selesai SC-2 + SC-3 manual UAT, balas:
    - **"approved"** kalau kedua SC PASS — executor lanjut ke Task 3 (SC-4/SC-5).
    - **"fail SC-2: {detail}"** atau **"fail SC-3: {detail}"** — executor STOP, report ke planner untuk revisi Plan 02/03.
    - **"butuh credential"** kalau password admin lokal tidak tersedia.
  </resume-signal>
  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp;       grep -q "SC-2" .planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md &amp;&amp;       grep -q "SC-3" .planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md
    </automated>
  </verify>
  <done>
    SC-2 + SC-3 documented dengan verdict di 325-UAT.md. User resume-signal = "approved" → executor lanjut Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT SC-4 + SC-5 dengan DB snapshot + seed temp + restore (SEED_WORKFLOW)</name>
  <files>.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md, docs/SEED_JOURNAL.md</files>
  <action>
    Lihat <how-to-verify> di bawah — SEED_WORKFLOW snapshot DB + seed 3 TR (parent A + child B referencing A + standalone C) + manual UI delete SC-4 (referenced blocked) + SC-5 (standalone success) + restore DB + update SEED_JOURNAL.md cleaned flag + append 325-UAT.md.
  </action>

  <what-built>
    - Plan 04: 3 endpoint delete dengan pre-check + try/catch DbUpdateException
    - DeleteTraining: pre-check `RenewsTrainingId == id` di TR + AS
    - DeleteManualAssessment: pre-check `RenewsSessionId == id` di TR + AS
    - DeleteAssessment: pre-check `RenewsSessionId == id` di TR + AS (sebelum tx scope Phase 323)
    - Error message: "Tidak bisa hapus: {N} sertifikat lain..." (D-06 verbatim)
  </what-built>

  <how-to-verify>
    **Pre-req:** App jalan + login admin (sama Task 1/2).

    **SEED_WORKFLOW step (per CLAUDE.md + `docs/SEED_WORKFLOW.md`):**

    **A. Snapshot DB lokal SEBELUM seed:**
    ```bash
    # Cari connection string dari appsettings.json untuk dapat DB name + server
    grep -A 3 "ConnectionStrings" appsettings.json

    # Snapshot pakai sqlcmd (default LocalDb atau SQL Server Express)
    sqlcmd -S "{SERVER_FROM_APPSETTINGS}" -E -Q "BACKUP DATABASE [{DB_NAME}] TO DISK = N'C:\Temp\hcportal-pre-phase325-uat.bak' WITH FORMAT, COMPRESSION;"
    ```

    Verify backup file `C:\Temp\hcportal-pre-phase325-uat.bak` ada + size > 0.

    **B. Update `docs/SEED_JOURNAL.md` SEBELUM insert seed:**

    Append entry:
    ```markdown
    ## 2026-05-27 — Phase 325 UAT SC-4/SC-5 seed

    **Tujuan:** Verify P05 hard delete FK quick patch — pre-check + try/catch.
    **Klasifikasi:** Temporary + local-only.
    **Snapshot:** C:\Temp\hcportal-pre-phase325-uat.bak (taken {timestamp})
    **Entitas tersentuh:**
    - TrainingRecord rows (insert 3 dummy: parent A + child B yang RenewsTrainingId=A.Id + standalone C)
    **Status:** active (akan di-cleaned setelah UAT selesai)
    ```

    **C. Seed via SQL atau UI (pilih yang lebih cepat).**

    Pilihan 1 (UI — preferred kalau gampang):
    1. Login admin → AddTraining 3 record:
       - Record A: judul "UAT SC-4 Parent", pekerja sample, ValidUntil today+30
       - Record B: judul "UAT SC-4 Child", pekerja sama, RenewsTrainingId=A.Id (via form dropdown atau radio "renewal dari")
       - Record C: judul "UAT SC-5 Standalone", pekerja sample, no renewal source

    Pilihan 2 (SQL direct — kalau form renewal complex):
    ```sql
    -- Get user Id valid
    SELECT TOP 1 Id, FullName FROM AspNetUsers WHERE FullName IS NOT NULL ORDER BY Id;
    -- Seed
    INSERT INTO TrainingRecords (UserId, Judul, Penyelenggara, Kategori, Tanggal, Status, CertificateType, CreatedAt, UpdatedAt)
    VALUES ('{user-id}', 'UAT SC-4 Parent', 'UAT', 'Self-Development', '2026-05-01', 'Lulus', 'Annual', GETUTCDATE(), GETUTCDATE());
    DECLARE @parentId INT = SCOPE_IDENTITY();

    INSERT INTO TrainingRecords (UserId, Judul, Penyelenggara, Kategori, Tanggal, Status, CertificateType, RenewsTrainingId, CreatedAt, UpdatedAt)
    VALUES ('{user-id}', 'UAT SC-4 Child', 'UAT', 'Self-Development', '2026-05-15', 'Lulus', 'Annual', @parentId, GETUTCDATE(), GETUTCDATE());

    INSERT INTO TrainingRecords (UserId, Judul, Penyelenggara, Kategori, Tanggal, Status, CertificateType, CreatedAt, UpdatedAt)
    VALUES ('{user-id}', 'UAT SC-5 Standalone', 'UAT', 'Self-Development', '2026-05-20', 'Lulus', 'Permanent', GETUTCDATE(), GETUTCDATE());
    ```

    **D. Test SC-4 (referenced delete blocked):**
    1. Navigate ke ManageAssessment → tab Training → cari "UAT SC-4 Parent" → klik tombol Delete.
    2. Confirm dialog kalau ada.
    3. Expected: TempData["Error"] display "Tidak bisa hapus: 1 sertifikat lain menggunakan record ini sebagai sumber renewal. Hapus atau update sertifikat pemakai terlebih dulu."
    4. Verify Parent A masih ada di list (TIDAK terhapus).

    **E. Test SC-5 (standalone delete success):**
    1. Cari "UAT SC-5 Standalone" → klik tombol Delete.
    2. Confirm dialog.
    3. Expected: TempData["Success"] display "Training record berhasil dihapus."
    4. Verify C tidak ada lagi di list.

    **F. Cleanup — restore DB:**
    ```bash
    # Stop app dulu (Ctrl+C di terminal yang dotnet run)
    sqlcmd -S "{SERVER}" -E -Q "USE master; ALTER DATABASE [{DB_NAME}] SET SINGLE_USER WITH ROLLBACK IMMEDIATE; RESTORE DATABASE [{DB_NAME}] FROM DISK = N'C:\Temp\hcportal-pre-phase325-uat.bak' WITH REPLACE; ALTER DATABASE [{DB_NAME}] SET MULTI_USER;"
    ```

    Verify restore success: query DB confirm 3 UAT record tidak ada.

    **G. Update `docs/SEED_JOURNAL.md` flag `cleaned`:**
    ```markdown
    **Status:** cleaned ({timestamp restore})
    ```

    **H. Catat hasil ke `325-UAT.md`:**
    ```markdown
    ## SC-4 (P05 referenced delete blocked) — {PASS/FAIL}

    **Pre-condition:** TR A + TR B (RenewsTrainingId=A.Id) seeded
    **Snapshot:** C:\Temp\hcportal-pre-phase325-uat.bak

    **Observation:**
    - Delete A attempted: {result}
    - TempData error: "{exact message displayed}"
    - A still exists: {yes/no}

    **Verdict:** {PASS/FAIL}

    ## SC-5 (P05 standalone delete success) — {PASS/FAIL}

    **Pre-condition:** TR C standalone seeded

    **Observation:**
    - Delete C attempted: {result}
    - TempData success: "{exact message}"
    - C still exists: {yes/no — expected no}

    **Verdict:** {PASS/FAIL}

    **DB restore:** {timestamp + verify zero UAT row left}

    ---

    ## Phase 325 UAT Summary

    | SC | Description | Verdict |
    |----|-------------|---------|
    | SC-1 | P01 path traversal | {PASS/FAIL} |
    | SC-2 | P02 .exe→.pdf reject | {PASS/FAIL} |
    | SC-3 | P02 normal upload regression | {PASS/FAIL} |
    | SC-4 | P05 referenced delete blocked | {PASS/FAIL} |
    | SC-5 | P05 standalone delete success | {PASS/FAIL} |
    | SC-6 | Unit test 6 case (covered Plan 02 automated) | PASS |

    **Overall:** {READY_TO_COMMIT / NEEDS_REVISION}
    ```
  </how-to-verify>

  <resume-signal>
    Setelah SC-4 + SC-5 manual UAT selesai + DB restored + SEED_JOURNAL.md flagged cleaned, balas:
    - **"approved — ready commit"** kalau semua 5 SC PASS — executor lanjut ke Task 4 (commit + push).
    - **"fail SC-4: {detail}"** atau **"fail SC-5: {detail}"** — executor STOP, report ke planner untuk revisi Plan 04.
    - **"snapshot fail"** atau **"restore fail"** — executor STOP, butuh manual DBA action.
  </resume-signal>
  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp;       grep -q "SC-4" .planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md &amp;&amp;       grep -q "SC-5" .planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md &amp;&amp;       grep -q "Phase 325" docs/SEED_JOURNAL.md &amp;&amp;       grep -q "cleaned" docs/SEED_JOURNAL.md
    </automated>
  </verify>
  <done>
    SC-4 + SC-5 documented + DB restored + SEED_JOURNAL.md flagged cleaned. User resume-signal = "approved — ready commit" → executor lanjut Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit + push Phase 325 changes ke origin/main (lokal verify done)</name>
  <files>(git commit only — no file edit)</files>

  <read_first>
    - `docs/DEV_WORKFLOW.md` — confirm Lokal → push flow + tidak deploy ke Dev (IT promo batch akhir milestone v19.0)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md` — confirm semua 5 SC verdict PASS
    - `docs/SEED_JOURNAL.md` — confirm seed temp di-flag `cleaned`
  </read_first>

  <action>
    **Pre-condition:** Task 3 resume-signal = "approved — ready commit" (5/5 SC PASS).

    **Step 1: Stop app lokal (Ctrl+C `dotnet run`).**

    **Step 2: Git status review.**
    ```bash
    cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"
    git status
    git diff --stat
    ```

    Expected changed files:
    - `Helpers/FileUploadHelper.cs` (Plan 02)
    - `Models/AssessmentConstants.cs` (Plan 02)
    - `Controllers/TrainingAdminController.cs` (Plan 03 + Plan 04)
    - `Controllers/AssessmentAdminController.cs` (Plan 04)
    - `HcPortal.Tests/HcPortal.Tests.csproj` (Plan 01)
    - `HcPortal.Tests/FileUploadHelperTests.cs` (Plan 01 + Plan 02)
    - `HcPortal.sln` (Plan 01)
    - `.gitignore` (Plan 01 — kalau perlu)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md` (Plan 05)
    - `.planning/phases/325-security-hardening-p01-p02-p05/325-01-SUMMARY.md` sampai `325-05-SUMMARY.md`
    - `docs/SEED_JOURNAL.md` (Plan 05)
    - `appsettings.json` mungkin (pre-existing M flag di git status start — verify tidak revert)

    **Step 3: Final build + test sanity check.**
    ```bash
    dotnet build HcPortal.sln && dotnet test HcPortal.Tests/HcPortal.Tests.csproj
    ```

    **Step 4: Commit (use HEREDOC).**

    Stage spesifik (jangan `git add -A` per CLAUDE.md anti-pattern):
    ```bash
    git add Helpers/FileUploadHelper.cs \
            Models/AssessmentConstants.cs \
            Controllers/TrainingAdminController.cs \
            Controllers/AssessmentAdminController.cs \
            HcPortal.Tests/ \
            HcPortal.sln \
            .gitignore \
            .planning/phases/325-security-hardening-p01-p02-p05/ \
            docs/SEED_JOURNAL.md
    ```

    ```bash
    git commit -m "$(cat <<'EOF'
    fix(325): security hardening P01 path traversal + P02 magic byte + P05 hard delete FK quick patch

    Phase 325 (milestone v19.0 portal HC bug fixes):
    - P01 HIGH path traversal: Path.GetFileName() strip di SaveFileAsync + audit log warning attempts (D-01 + D-10)
    - P02 MED MIME spoof: magic byte signature validation untuk PDF/JPG/PNG di ValidateCertificateFile (D-02/D-03 + D-09 dict di constants)
    - P05 MED hard delete FK: pre-check referencing + catch DbUpdateException + TempData friendly message (D-04/D-05/D-06)
      - DeleteTraining (TrainingRecord, RenewsTrainingId)
      - DeleteManualAssessment (AssessmentSession, RenewsSessionId per RESEARCH CORRECTION 2)
      - DeleteAssessment (AssessmentSession, RenewsSessionId, pre-check di luar tx Phase 323 per D-11)
    - Refactor 3 inline duplicate validation sites di TrainingAdminController (line 206-215/221-233/459-471) pakai FileUploadHelper.ValidateCertificateFile — magic byte fix sekarang reachable dari Add/Edit Training endpoint (RESEARCH CORRECTION 3)
    - xUnit project HcPortal.Tests/ bootstrap + 6 test FileUploadHelperTests pass

    UAT (lokal http://localhost:5277):
    - SC-1 Postman ../../test.pdf landed flat di uploads/certificates/ ✓
    - SC-2 .exe rename .pdf reject magic byte mismatch ✓
    - SC-3 PDF/JPG/PNG asli lolos no false positive ✓
    - SC-4 referenced delete blocked TempData error ✓
    - SC-5 standalone delete success ✓
    - SC-6 dotnet test 6/6 pass ✓

    No EF migration (D-07). Soft delete proper defer v20.0.
    IT promo Dev defer batch akhir milestone v19.0 (setelah Phase 327 ship).

    Refs:
    - docs/superpowers/specs/2026-05-26-v19.0-portal-hc-bug-fixes-design.md §5
    - .planning/phases/325-security-hardening-p01-p02-p05/

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    **Step 5: Push ke origin/main.**
    ```bash
    git push origin main
    ```

    **Step 6: Update STATE.md + ROADMAP.md ditandai Phase 325 selesai.**

    Update `.planning/ROADMAP.md` line 614: ganti `- [ ] **Phase 325...** ` → `- [x] **Phase 325...** ` (checkmark).

    Update `.planning/STATE.md` `Current Position` ke Phase 326 (next phase milestone v19.0).

    Commit STATE/ROADMAP update:
    ```bash
    git add .planning/STATE.md .planning/ROADMAP.md
    git commit -m "docs(325): mark Phase 325 complete in STATE + ROADMAP

    Phase 325 SHIPPED lokal. IT promo defer batch akhir milestone v19.0.
    Next: Phase 326 Validator Hardening (P03 + P06).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"

    git push origin main
    ```
  </action>

  <verify>
    <automated>
      cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" &amp;&amp; \
      git log -2 --oneline &amp;&amp; \
      git status --porcelain &amp;&amp; \
      grep -q "\[x\] \*\*Phase 325" .planning/ROADMAP.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --format=%s` mengandung "fix(325): security hardening"
    - `git status --porcelain` clean (no uncommitted changes)
    - `git log -1 origin/main --format=%H` == `git log -1 main --format=%H` (push success)
    - `.planning/ROADMAP.md` line 614 checked `[x]`
    - `.planning/STATE.md` Current Position updated ke Phase 326 atau "Phase 325 SHIPPED"
    - `dotnet build HcPortal.sln` exit 0 + `dotnet test` 6/6 pass (final sanity check)
  </acceptance_criteria>

  <done>
    Phase 325 SHIPPED lokal + pushed origin/main. Milestone v19.0 progress: 1/3 phase shipped (Phase 326 next). Pending IT promo batch akhir Phase 327.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Plan 05 = manual UAT verification. Trust boundaries di Plan 02-04 yang fix dilakukan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-01 | Tampering | Verified via SC-1 Postman test (file landed flat) + log warning capture | mitigate (verified) | Plan 02 D-01 + D-10 implementation |
| T-325-02 | Spoofing | Verified via SC-2 browser .exe rename .pdf reject | mitigate (verified) | Plan 02 D-02/D-03/D-09 + Plan 03 refactor |
| T-325-03 | Information Disclosure / DoS | Verified via SC-4 referenced delete blocked friendly message | mitigate (verified) | Plan 04 pre-check + DbUpdateException catch |
| T-325-04 | Tampering (P02 bypass) | Verified via SC-2 lewat AddTraining endpoint (Plan 03 refactored site) | mitigate (verified) | Plan 03 refactor 3 inline sites |
| T-325-05 | TOCTOU race | Documented as acceptable risk (Portal HC traffic profile rendah, DbUpdateException safety net verified) | accept | Plan 04 catch as safety net; soft delete v20.0 |
</threat_model>

<verification>
- 5 manual UAT scenario executed dengan verdict PASS
- DB snapshot taken sebelum seed + restore setelah test (SEED_WORKFLOW compliance)
- SEED_JOURNAL.md updated dengan cleaned flag
- 325-UAT.md document hasil lengkap
- `dotnet build` + `dotnet test` final green
- Commit + push success
- STATE + ROADMAP updated

Manual UAT IS the verification — no automated wrapper possible untuk file system check + browser form + DB state.
</verification>

<success_criteria>
**Phase 325 SHIPPED criteria:**
- SC-1 sampai SC-6 semua verdict PASS di `325-UAT.md`
- T-325-01 sampai T-325-04 verified mitigated (T-325-05 documented accept)
- Git push origin/main success
- IT promo Dev DEFER batch akhir milestone v19.0 (per spec strategy)
</success_criteria>

<output>
After completion, create `.planning/phases/325-security-hardening-p01-p02-p05/325-05-SUMMARY.md` covering:
- UAT 5 scenario verdict
- SEED_JOURNAL.md audit trail (snapshot + restore + cleaned)
- Commit hash + push confirmation
- STATE.md + ROADMAP.md update
- Milestone v19.0 progress (1/3 phase shipped)
- Pending: Phase 326 (next), Phase 327, IT promo batch akhir
</output>
