# Phase 325 — UAT Result Log

**Tanggal UAT:** 2026-05-27
**Environment:** Lokal Windows + dotnet 8.0.418 + http://localhost:5277
**Tester:** Claude (automated xUnit) + User (manual browser + DB SC-2..SC-5)

---

## SC-1 (P01 Path Traversal) — ✅ PASS (automated xUnit)

**Method:** Extended `HcPortal.Tests/FileUploadHelperTests.cs` dengan 3 test SaveFileAsync (commit `f47ff95a`).

**Coverage:**
- `SaveFileAsync_PathTraversalFilename_StripsToFlatNameNoEscape`:
  - Input filename: `"../../malicious.pdf"`
  - tempRoot: `Path.GetTempPath() + "hcportal-test-{guid}"`
  - **Assertion**: saved file end `_malicious.pdf`, tidak mengandung `..`/`/`/`\\`, tempRoot (parent) tidak punya file leaked
- `SaveFileAsync_PathTraversalFilename_LogsWarningD10`:
  - Input filename: `"../../etc/passwd.pdf"` + TestLogger
  - **Assertion**: `LogLevel.Warning` captured dengan template `"Path traversal attempt: filename={Original} stripped to {Safe}"`, mengandung original `../../etc/passwd.pdf` + stripped `passwd.pdf`
- `SaveFileAsync_NormalFilename_NoWarningLogged`:
  - Input filename: `"normal.pdf"` + TestLogger
  - **Assertion**: TIDAK ada `LogLevel.Warning "Path traversal attempt"` (avoid log noise false positive)

**Verdict:** ✅ PASS — `dotnet test → Passed: 10, Skipped: 0, Failed: 0, Total: 10`.

**Rationale untuk replace manual Postman:** Form `/Admin/AddTraining` pakai dynamic JS `WorkerCerts[i].*` index → curl POST butuh valid UserId GUID + index manual (kompleks scripting). xUnit covering helper directly = deterministic, automated, no DB seed, no auth required.

---

## SC-2 (P02 .exe rename .pdf reject) — ⏳ PENDING MANUAL USER

**Pre-condition:** App jalan lokal `dotnet run --project HcPortal.csproj` → `http://localhost:5277`. Login admin@pertamina.com / 123456.

**Step:**
1. Siapkan fake file:
   ```powershell
   Copy-Item "C:\Windows\System32\notepad.exe" "$env:USERPROFILE\Downloads\fake-cert.pdf"
   ```
2. Browser → `http://localhost:5277/Admin/AddTraining`.
3. Fill form minimal: Judul "UAT SC-2", Penyelenggara "UAT", Kategori "Self-Development", Tanggal hari ini, Status "Lulus", pilih 1 pekerja, CertificateType "Permanent".
4. Upload `fake-cert.pdf` (notepad.exe rename).
5. Submit.

**Expected:**
- Form rejected — ModelState error display `"Isi file tidak cocok dengan ekstensi (magic byte mismatch)."`
- No file added di `wwwroot/uploads/certificates/`.

**Observation (user fill):**
- Form submit result: ____ (rejected/accepted)
- Error message displayed: ____
- File added di uploads/certificates/: ____ (yes/no)

**Verdict:** ____ (PASS/FAIL)

---

## SC-3 (P02 normal PDF/JPG/PNG regression) — ⏳ PENDING MANUAL USER

**Step:** Repeat AddTraining 3 kali dengan 3 file asli:
- PDF asli (Word Save-As atau sample lain)
- JPG asli (foto camera atau `wwwroot/img/`)
- PNG asli (Windows screenshot)

**Expected:** 3 sukses submit, TempData success "Training record berhasil ditambahkan.", 3 file baru di `wwwroot/uploads/certificates/` dengan name pattern `{ts}_{guid}_{originalName}`.

**Observation:**

| Format | File source | Submit | TempData message |
|--------|-------------|--------|------------------|
| PDF    | ____        | ____   | ____             |
| JPG    | ____        | ____   | ____             |
| PNG    | ____        | ____   | ____             |

**Verdict:** ____ (PASS/FAIL)

---

## SC-4 (P05 referenced delete blocked) — ⏳ PENDING MANUAL USER

**Pre-condition:** SEED_WORKFLOW snapshot + seed 2 TR (parent A + child B yang RenewsTrainingId=A.Id).

**Step:**
1. `sqlcmd -S {SERVER} -E -Q "BACKUP DATABASE [{DB}] TO DISK = N'C:\Temp\hcportal-pre-phase325-uat.bak' WITH FORMAT, COMPRESSION;"` (lihat `appsettings.json` ConnectionStrings).
2. Update `docs/SEED_JOURNAL.md` entry "2026-05-27 Phase 325 UAT SC-4/SC-5 seed" (temporary + local-only).
3. Seed via UI: AddTraining 2 record (Parent A + Child B dengan renewal source = A).
4. Navigate ke ManageAssessment → tab Training → cari "UAT SC-4 Parent" → klik Delete.

**Expected:** TempData error `"Tidak bisa hapus: 1 sertifikat lain menggunakan record ini sebagai sumber renewal. Hapus atau update sertifikat pemakai terlebih dulu."` + A masih ada.

**Observation:**
- Delete A attempted: ____
- TempData error: ____
- A still exists: ____ (yes/no — expected yes)

**Verdict:** ____ (PASS/FAIL)

---

## SC-5 (P05 standalone delete success) — ⏳ PENDING MANUAL USER

**Pre-condition:** Continued dari SC-4 seed + 1 standalone record C.

**Step:**
1. Seed via UI: AddTraining 1 record "UAT SC-5 Standalone" (no renewal source).
2. Navigate ke list Training → cari → Delete.

**Expected:** TempData success `"Training record berhasil dihapus."` + C tidak ada.

**Observation:**
- Delete C attempted: ____
- TempData success: ____
- C still exists: ____ (yes/no — expected no)

**Verdict:** ____ (PASS/FAIL)

**Cleanup:**
- `sqlcmd RESTORE DATABASE` dari `C:\Temp\hcportal-pre-phase325-uat.bak`.
- Update `docs/SEED_JOURNAL.md` flag `cleaned`.

---

## Phase 325 UAT Summary

| SC | Description | Method | Verdict |
|----|-------------|--------|---------|
| SC-1 | P01 path traversal + D-10 audit log | xUnit (3 test) | ✅ PASS |
| SC-2 | P02 .exe rename .pdf reject | Manual browser | ⏳ pending |
| SC-3 | P02 normal PDF/JPG/PNG | Manual browser | ⏳ pending |
| SC-4 | P05 referenced delete blocked | Manual + DB seed | ⏳ pending |
| SC-5 | P05 standalone delete success | Manual + DB seed | ⏳ pending |
| SC-6 | Unit test foundation (Plan 02 automated) | xUnit (7 test) | ✅ PASS |

**Automated coverage:** 10/10 xUnit pass (commit `f47ff95a`).
**Pending:** SC-2..SC-5 manual UAT user — fill section di atas, reply approval, executor commit final + push gating.

**Overall status:** PARTIAL — SC-1 + SC-6 verified automated. SC-2..SC-5 pending user manual UAT sebelum push origin/main.
