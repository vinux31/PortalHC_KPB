# Phase 325 — UAT Result Log

**Tanggal UAT:** 2026-05-27
**Environment:** Lokal Windows + dotnet 8.0.418 + http://localhost:5277 + SQL Server Express LocalDB `HcPortalDB_Dev`
**Tester:** Claude (xUnit automated + Playwright MCP browser-driven)

---

## SC-1 (P01 Path Traversal) — ✅ PASS (automated xUnit)

**Method:** Extended `HcPortal.Tests/FileUploadHelperTests.cs` 3 test SaveFileAsync (commit `f47ff95a`).

**Coverage:**
- `SaveFileAsync_PathTraversalFilename_StripsToFlatNameNoEscape` — filename `"../../malicious.pdf"` → saved flat `{ts}_{guid}_malicious.pdf` di uploads/ sub-dir, parent tempRoot zero files
- `SaveFileAsync_PathTraversalFilename_LogsWarningD10` — TestLogger capture `LogLevel.Warning "Path traversal attempt: filename=../../etc/passwd.pdf stripped to passwd.pdf"`
- `SaveFileAsync_NormalFilename_NoWarningLogged` — `"normal.pdf"` TIDAK trigger Warning (no false-positive log noise)

**Verdict:** ✅ PASS — `dotnet test → Passed: 10, Skipped: 0, Failed: 0, Total: 10`.

---

## SC-2 (P02 .exe rename .pdf reject) — ✅ PASS (browser)

**Pre-condition:** `Copy-Item C:\Windows\System32\notepad.exe .tmp-uat-325\fake-cert.pdf` (file PE32+ executable extension rename .pdf).

**Step (Playwright MCP):**
1. Login admin@pertamina.com / 123456 di /Account/Login.
2. `/Admin/AddTraining` → fill form (Judul "UAT SC-2 exe rename pdf", Penyelenggara UAT, Kategori OJT, Pekerja Iwan, Tipe Permanent).
3. Upload `fake-cert.pdf` (real notepad.exe binary).
4. Submit "Simpan Training".

**Observation:**
- Form **NOT submit successful** — tetap di /Admin/AddTraining page.
- Error message visible (list item ref=e49):
  > `"File untuk pekerja 66227777-1974-43ca-8bdd-e5586fa4a5b8: Isi file tidak cocok dengan ekstensi (magic byte mismatch)."`
- `wwwroot/uploads/certificates/`: NO `fake-cert.pdf` leaked.

**Temuan minor (defer v20.0 UX polish):** Error message mention `UserId` GUID (`66227777-...`) bukan FullName "Iwan". Plan 03 refactor pattern `$"File untuk pekerja {wc.UserId}: {wcErr}"` preserve Plan 02 behavior — UserId field di ViewModel = GUID string. Bisa improved future: JOIN ke FullName atau lookup ViewBag.Workers.

**Verdict:** ✅ PASS — magic byte gate aktif di endpoint `/Admin/AddTraining` (Plan 03 refactor effective, T-325-04 mitigated).

---

## SC-3 (P02 PDF/JPG/PNG asli regression) — ✅ PASS (browser, 3/3)

**Files generated:**
- `.tmp-uat-325/sample-cert.pdf` — `%PDF-1.4\n%Test PDF UAT SC-3\n%%EOF` (33 byte)
- `.tmp-uat-325/sample-cert.jpg` — JFIF `FF D8 FF E0 00 10 JFIF...` (91 byte minimal valid)
- `.tmp-uat-325/sample-cert.png` — copy `wwwroot/documents/guides/screenshots/01-login.png` (317682 byte)

**Step (Playwright MCP):** AddTraining 3× dengan upload masing-masing file.

**Observation:**

| Format | File source | Submit | TempData | File landed |
|--------|-------------|--------|----------|-------------|
| PDF    | minimal `%PDF-1.4` byte stream | ✅ Success | "Berhasil membuat 1 training record." | `20260527072048567_e67b6449_sample-cert.pdf` |
| JPG    | minimal JFIF byte stream | ✅ Success | "Berhasil membuat 1 training record." | `20260527072133162_77b1965d_sample-cert.jpg` |
| PNG    | login screenshot real PNG | ✅ Success | "Berhasil membuat 1 training record." | `20260527072210811_7e35b524_sample-cert.png` |

3 file landed pattern `{ts:yyyyMMddHHmmssfff}_{guid8}_{originalName}` confirm `SaveFileAsync` D-01 + Plan 03 helper integration working.

Iwan "Status Training" count berubah: 3+2 → 3+3 (PDF) → 3+4 (JPG) → 3+5 (PNG).

**Verdict:** ✅ PASS — magic byte gate tidak reject format asli, regression-free.

---

## SC-4 (P05 referenced delete blocked) — ✅ PASS (browser + sqlcmd)

**Pre-condition (SEED_WORKFLOW per CLAUDE.md):**
1. `sqlcmd BACKUP DATABASE` → `C:\Temp\hcportal-pre-phase325-uat.bak` (1856 pages, 0.100s).
2. Update `docs/SEED_JOURNAL.md` entry temporary + local-only.
3. `sqlcmd INSERT INTO TrainingRecords (UserId, Judul, ..., RenewsTrainingId) VALUES (..., 'UAT SC-4 Child renews 28', ..., 28)` → seed Id=31 yang referencing Id=28 (Parent SC-3 PDF).

**Step (Playwright MCP):**
- Navigate `/Admin/ManageAssessment?tab=training` → expand Iwan accordion → form `POST /Admin/DeleteTraining` Id=28 dengan `confirm() = true` override.

**Observation:**
- Form submit, redirect back ke ManageAssessment.
- TempData error visible (alert .alert):
  > `"Tidak bisa hapus: 1 sertifikat lain menggunakan record ini sebagai sumber renewal. Hapus atau update sertifikat pemakai terlebih dulu."`
- Iwan "Status Training" tetap `8 completed (3 assessments + 5 trainings)` — Id=28 NOT deleted.

**Verdict:** ✅ PASS — D-04 pre-check `RenewsTrainingId == record.Id` di TR + AS aktif, message verbatim D-06.

---

## SC-5 (P05 standalone delete success) — ✅ PASS (browser)

**Pre-condition:** Id=29 (UAT SC-3 JPG asli) standalone — no child reference.

**Step (Playwright MCP):**
- Same accordion → form `POST /Admin/DeleteTraining` Id=29 + confirm override.

**Observation:**
- Form submit successful.
- Iwan "Status Training" turun ke `7 completed (3 assessments + 4 trainings)` — Id=29 deleted.
- File `wwwroot/uploads/certificates/20260527072133162_77b1965d_sample-cert.jpg` hilang (FileUploadHelper.DeleteFile aktif).

**Verdict:** ✅ PASS — endpoint normal flow tidak terganggu pre-check (returns 0 referencing → proceed delete).

---

## Cleanup

```sql
RESTORE DATABASE [HcPortalDB_Dev] FROM DISK = N'C:\Temp\hcportal-pre-phase325-uat.bak' WITH REPLACE;
-- Restore revert Id=29 delete, tapi Id=31 (seeded post-snapshot) tidak ter-cover.
DELETE FROM TrainingRecords WHERE Judul LIKE 'UAT SC-%';
-- DELETE pattern-match: 3 row (Id 28/29/30) cleared.
```

```bash
rm wwwroot/uploads/certificates/*sample-cert*  # 3 file dihapus
rm -rf .tmp-uat-325/  # temp UAT dir dihapus
```

**Verify post-cleanup:**
- `SELECT COUNT(*) FROM TrainingRecords WHERE Judul LIKE 'UAT SC-%'` → 0
- `ls wwwroot/uploads/certificates/*sample-cert*` → not found

`docs/SEED_JOURNAL.md` updated: status `cleaned`.

---

## Phase 325 UAT Summary

| SC | Description | Method | Verdict | Evidence |
|----|-------------|--------|---------|----------|
| SC-1 | P01 path traversal + D-10 audit | xUnit (3 test) | ✅ PASS | 10/10 dotnet test |
| SC-2 | P02 .exe rename .pdf reject | Browser (Playwright) | ✅ PASS | ModelState D-06 verbatim + no file leaked |
| SC-3 | P02 PDF/JPG/PNG asli regression | Browser (Playwright, 3 upload) | ✅ PASS | 3/3 success + file pattern correct |
| SC-4 | P05 referenced delete blocked | Browser + seed Id=31 child | ✅ PASS | TempData D-06 verbatim + parent intact |
| SC-5 | P05 standalone delete success | Browser delete Id=29 | ✅ PASS | TempData success + Iwan count -1 + file removed |
| SC-6 | Unit test foundation | xUnit (7 test ValidateCertificateFile) | ✅ PASS | 10/10 pass total |

**Overall:** ✅ READY_TO_PUSH origin/main.

**Threat mitigation verified:**
- T-325-01 path traversal HIGH ✅ (SC-1 xUnit)
- T-325-02 MIME spoof MED ✅ (SC-2 browser)
- T-325-03 DB 500 leak MED ✅ (SC-4 browser)
- T-325-04 P02 bypass HIGH ✅ (SC-2 reaches via Plan 03 refactor)
- T-325-05 TOCTOU LOW ✅ (accept + safety net catch verified pre-check returns clean)

**Temuan untuk follow-up:**
1. **SC-2 error message mention UserId GUID** (`{wc.UserId}` raw) — UX polish, defer v20.0. Bisa JOIN ke FullName via ViewBag.Workers lookup atau preserve placeholder ke ViewModel.
2. Tidak ada blocker untuk push origin/main.
