---
phase: 326
plan: 02
type: execute
wave: 2
depends_on: [326-01]
files_modified:
  - Views/Admin/EditTraining.cshtml
  - Views/Admin/AddTraining.cshtml
autonomous: true
requirements:
  - SC-3
  - SC-6
must_haves:
  truths:
    - "EditTraining.cshtml render section 'Renewal Source' card conditional kalau Model.RenewsTrainingId.HasValue OR Model.RenewsSessionId.HasValue"
    - "Section card punya button 'Hapus link renewal' yang clear hidden inputs RenewsTrainingId + RenewsSessionId via inline onclick"
    - "EditTraining.cshtml + AddTraining.cshtml punya <span asp-validation-for=\"ValidUntil\"> setelah input ValidUntil untuk display P06 field-level error"
    - "Section card insertion point antara card 'Data Training' (L125) dan card 'Data Sertifikat' (L127) EditTraining.cshtml"
    - "Copywriting 4 string locked verbatim per UI-SPEC L177-185"
  artifacts:
    - path: "Views/Admin/EditTraining.cshtml"
      provides: "Read-only renewal source section + clear button + ValidUntil error span"
      contains: "Hapus link renewal"
    - path: "Views/Admin/AddTraining.cshtml"
      provides: "ValidUntil field-level error span untuk P06 display"
      contains: "asp-validation-for=\"ValidUntil\""
  key_links:
    - from: "EditTraining.cshtml renewalSourceSection button"
      to: "hidden inputs RenewsTrainingId + RenewsSessionId"
      via: "inline onclick handler document.getElementById(...).value=''"
      pattern: "onclick=.*RenewsTrainingId.*value=''"
    - from: "EditTraining.cshtml hidden inputs"
      to: "EditTraining POST handler (Plan 01 Task 3)"
      via: "form POST bind via name attribute"
      pattern: "name=\"RenewsTrainingId\""
    - from: "AddTraining.cshtml ValidUntil input"
      to: "ModelState.AddModelError(\"ValidUntil\", ...) di handler"
      via: "asp-validation-for span"
      pattern: "asp-validation-for=\"ValidUntil\""
---

<objective>
Tambah section card read-only "Renewal Source" + clear button di Views/Admin/EditTraining.cshtml (conditional render kalau renewal FK present) + 2 span asp-validation-for="ValidUntil" di kedua file Views/Admin/{Edit,Add}Training.cshtml untuk display P06 error.

Purpose: UI surface untuk:
1. SC-6 — User bisa lihat link renewal saat ini di Edit form + clear via button "Hapus link renewal" (D-07).
2. SC-3 — P06 ModelState field-level error tampil dekat input ValidUntil di Add (Edit tampil via TempData toast — acceptable per CONTEXT L177-178).

Output: 2 file Razor modified. EditTraining.cshtml dapat ~28 baris section card (insertion antara L125-L127) + 1 baris span (setelah L153). AddTraining.cshtml dapat 1 baris span (setelah L198). Total UI delta ≤ 35 baris.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md
@.planning/phases/326-validator-hardening-p03-p06/326-UI-SPEC.md
@.planning/phases/326-validator-hardening-p03-p06/326-PATTERNS.md
@.planning/phases/326-validator-hardening-p03-p06/326-01-PLAN.md
@CLAUDE.md
@Views/Admin/EditTraining.cshtml
@Views/Admin/AddTraining.cshtml

<interfaces>
EditTraining.cshtml current structure (key landmark lines):
- L28-31: `<div asp-validation-summary="All" class="alert alert-danger">` (P03 summary tampil di sini)
- L37-40: hidden inputs pattern (Id, WorkerId, WorkerName, ExistingSertifikatUrl)
- L42-125: card "Data Training" (closing `</div>` L125)
- **INSERTION POINT: between L125 and L127** untuk section "Renewal Source"
- L127-179: card "Data Sertifikat"
- L150-154: ValidUntil field area (input L153, **INSERT span after L153 before L154 closing div**)

AddTraining.cshtml landmark:
- L195-199: Berlaku Sampai (shared) field, **INSERT span after L198 before L199 closing div**

Bootstrap utility classes (existing project standard):
- Card: `card border-0 shadow-sm mb-4`
- Card-header: `card-header bg-white py-3`
- Heading: `h5.mb-0.fw-bold`
- Row: `row g-3 align-items-end`
- Read-only display: `form-control-plaintext mb-0`
- Destructive button: `btn btn-outline-danger btn-sm`
- Error span: `text-danger small`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah section card "Renewal Source" + clear button di EditTraining.cshtml (UI-SPEC L115-146 verbatim)</name>
  <files>Views/Admin/EditTraining.cshtml</files>
  <read_first>
    - Views/Admin/EditTraining.cshtml:42-130 (card "Data Training" L42-125 + opening "Data Sertifikat" L127)
    - .planning/phases/326-validator-hardening-p03-p06/326-UI-SPEC.md Component Markup Contract L111-146 (markup verbatim) + L173-188 Copywriting Contract
    - .planning/phases/326-validator-hardening-p03-p06/326-PATTERNS.md section "3. Views/Admin/EditTraining.cshtml Pattern A"
    - .planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md D-07
  </read_first>
  <action>
Buka Views/Admin/EditTraining.cshtml. Cari closing `</div>` di L125 (akhir card "Data Training") dan opening `<div class="card border-0 shadow-sm mb-4">` di L127 (awal card "Data Sertifikat").

INSERT block markup VERBATIM antara L125 dan L127 (markup per UI-SPEC L115-146 + PATTERNS Pattern A):

```razor

    @if (Model.RenewsTrainingId.HasValue || Model.RenewsSessionId.HasValue)
    {
        <div id="renewalSourceSection" class="card border-0 shadow-sm mb-4">
            <div class="card-header bg-white py-3">
                <h5 class="mb-0 fw-bold">
                    <i class="bi bi-arrow-repeat me-2 text-primary"></i>Renewal Source
                </h5>
            </div>
            <div class="card-body">
                <div class="row g-3 align-items-end">
                    <div class="col-md-9">
                        <label class="form-label fw-bold">Renewal dari</label>
                        <p class="form-control-plaintext mb-0">
                            @(Model.RenewalSourceTitle ?? "(sertifikat sumber tidak ditemukan)")
                        </p>
                    </div>
                    <div class="col-md-3 text-md-end">
                        <button type="button"
                                class="btn btn-outline-danger btn-sm"
                                onclick="document.getElementById('RenewsTrainingId').value='';document.getElementById('RenewsSessionId').value='';document.getElementById('renewalSourceSection').style.display='none';return false;">
                            <i class="bi bi-x-circle me-1"></i>Hapus link renewal
                        </button>
                    </div>
                </div>
                <input type="hidden" id="RenewsTrainingId" name="RenewsTrainingId" value="@Model.RenewsTrainingId" />
                <input type="hidden" id="RenewsSessionId" name="RenewsSessionId" value="@Model.RenewsSessionId" />
            </div>
        </div>
    }

```

Rules verbatim (locked per UI-SPEC L177-188 — JANGAN ubah byte):
- Card header heading: `Renewal Source` (English term match domain FK name)
- Label: `Renewal dari`
- Fallback string: `(sertifikat sumber tidak ditemukan)`
- Clear button text: `Hapus link renewal`
- Icon header: `bi bi-arrow-repeat` + `me-2 text-primary`
- Icon button: `bi bi-x-circle` + `me-1`

JavaScript inline rules (UI-SPEC L283):
- SINGLE LINE onclick — JANGAN multi-line
- Clear urutan: `RenewsTrainingId` → `RenewsSessionId` → `renewalSourceSection.style.display='none'` → `return false`

Hidden input rules:
- Pakai raw `<input type="hidden">` (BUKAN `asp-for`) karena perlu eksplisit `id+name` matching JS `document.getElementById('RenewsTrainingId')`
- `value="@Model.RenewsTrainingId"` — Razor null int? render sebagai empty string (acceptable, backend bind null OK)

JANGAN:
- Tambah `alert-warning` (UI-SPEC L92-94 + L106-107 — Edit display read-only = persistent state, BUKAN warning context seperti AddTraining renewal-mode)
- Tambah confirm modal sebelum clear (UI-SPEC L191-192 — soft action reversible via Batal button)
- Tambah AJAX call (UI-SPEC L168 — no optimistic UI, server-side state berubah hanya saat Simpan Perubahan submit)
- Modify L125 atau L127 existing markup
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | grep -E "(error|Error)" | grep -v "warning"</automated>
  </verify>
  <acceptance_criteria>
    - Views/Admin/EditTraining.cshtml grep `id="renewalSourceSection"` returns 1 hit
    - File grep `Renewal Source` returns 1 hit (di h5 heading)
    - File grep `Renewal dari` returns 1 hit (di label)
    - File grep `Hapus link renewal` returns 1 hit (di button)
    - File grep `(sertifikat sumber tidak ditemukan)` returns 1 hit (di fallback)
    - File grep `bi-arrow-repeat me-2 text-primary` returns 1 hit (icon header)
    - File grep `bi-x-circle me-1` returns 1 hit (icon button)
    - File grep `@if (Model.RenewsTrainingId.HasValue || Model.RenewsSessionId.HasValue)` returns 1 hit (conditional guard)
    - File grep `name="RenewsTrainingId"` returns 1 hit (hidden input)
    - File grep `name="RenewsSessionId"` returns 1 hit
    - File grep `btn-outline-danger btn-sm` returns 1 hit
    - File grep `form-control-plaintext` returns 1 hit
    - `dotnet build` 0 Error (Razor syntax valid)
    - Existing markup L42-125 (Data Training card) + L127 onwards (Data Sertifikat card) NOT modified — grep `<i class="bi bi-journal-text me-2"></i>Data Training` masih 1 hit + `<i class="bi bi-award me-2"></i>Data Sertifikat` masih 1 hit
  </acceptance_criteria>
  <done>EditTraining.cshtml punya section card "Renewal Source" conditional render dengan clear button + 2 hidden input pass through. Build sukses.</done>
</task>

<task type="auto">
  <name>Task 2: Tambah <span asp-validation-for="ValidUntil"> di EditTraining.cshtml + AddTraining.cshtml (P06 field-level error display)</name>
  <files>Views/Admin/EditTraining.cshtml, Views/Admin/AddTraining.cshtml</files>
  <read_first>
    - Views/Admin/EditTraining.cshtml:150-155 (ValidUntil field area — verify NO span exists)
    - Views/Admin/AddTraining.cshtml:195-200 (ValidUntil shared field area — verify NO span exists)
    - .planning/phases/326-validator-hardening-p03-p06/326-UI-SPEC.md "Error Display Pattern" L196-224
    - .planning/phases/326-validator-hardening-p03-p06/326-PATTERNS.md section "3. Pattern B + 4. AddTraining.cshtml"
  </read_first>
  <action>
**File 1 — Views/Admin/EditTraining.cshtml:**

Cari blok ValidUntil di L150-154 (CATATAN: nomor baris bisa shift +28 setelah Task 1 insertion — gunakan grep `<input asp-for="ValidUntil" type="date"` untuk locate; bila ada multiple match, pilih yang dalam card "Data Sertifikat" — bukan yang muncul di section "Renewal Source" Task 1):

```razor
<div class="col-md-6">
    <label asp-for="ValidUntil" class="form-label fw-bold">Berlaku Sampai</label>
    <input asp-for="ValidUntil" type="date" class="form-control" />
</div>
```

SETELAH `<input asp-for="ValidUntil" type="date" class="form-control" />` dan SEBELUM closing `</div>`, INSERT:

```razor
                    <span asp-validation-for="ValidUntil" class="text-danger small"></span>
```

**File 2 — Views/Admin/AddTraining.cshtml:**

Cari blok ValidUntil di L195-199 (gunakan grep `<!-- Berlaku Sampai (shared) -->` untuk locate):

```razor
<!-- Berlaku Sampai (shared) -->
<div class="col-md-6">
    <label asp-for="ValidUntil" class="form-label fw-bold">Berlaku Sampai <i class="bi bi-question-circle text-muted" data-bs-toggle="tooltip" data-bs-placement="top" title="Tanggal kedaluwarsa sertifikat. Wajib diisi jika tipe bukan Permanent"></i></label>
    <input asp-for="ValidUntil" type="date" class="form-control" />
</div>
```

SETELAH `<input asp-for="ValidUntil" type="date" class="form-control" />` dan SEBELUM closing `</div>`, INSERT:

```razor
                    <span asp-validation-for="ValidUntil" class="text-danger small"></span>
```

Rules:
- Indentation match file existing (4-space atau tab — observe file style; UI-SPEC L213-217 example pakai 0-space tapi adapt ke existing indentation kolom)
- Class `text-danger small` parity dengan span existing L52/L59/L78 EditTraining + sibling pattern AddTraining
- 1 line span saja — TIDAK tambah custom message content (ASP.NET Core tag helper auto-render error message)
- TIDAK touch label, input, atau closing `</div>` — hanya 1 baris INSERT
- TIDAK tambah label-side `<span class="text-danger">*</span>` di AddTraining karena ValidUntil bukan required (PATTERNS L382)

UX context per UI-SPEC L222-224:
- AddTraining: `return View(model)` flow → ModelState preserved → P06 tampil **field-level native** dekat input ✓
- EditTraining: `TempData["Error"]` flow → P06 tampil **toast compressed**, span tetap tambah untuk consistency + future-proof
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | grep -E "(error|Error)" | grep -v "warning"</automated>
  </verify>
  <acceptance_criteria>
    - Views/Admin/EditTraining.cshtml grep `asp-validation-for="ValidUntil"` returns exactly 1 hit
    - Views/Admin/AddTraining.cshtml grep `asp-validation-for="ValidUntil"` returns exactly 1 hit
    - Cross-Views grep `asp-validation-for="ValidUntil"` global di Views/Admin/ returns exactly 2 hits (sebelum Phase 326 = 0 hit per PATTERNS L353)
    - Edit span dalam card "Data Sertifikat" (BUKAN section "Renewal Source" Task 1) — verify via Read + visual inspect
    - Add span dalam blok "Berlaku Sampai (shared)" L195+
    - File span pakai class `text-danger small` (parity sibling span)
    - `dotnet build` 0 Error
    - Existing markup unchanged: grep `<input asp-for="ValidUntil" type="date" class="form-control" />` returns ≥ 2 hits (1 Edit + 1 Add — Add file possible >1 instance bila ada non-renewal mode duplicate, count tetap ≥ 2 OK)
  </acceptance_criteria>
  <done>Kedua file Razor punya 1 span asp-validation-for="ValidUntil" baru. Build sukses. Cross-Views grep returns exactly 2 hits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → form POST | User input via hidden input (RenewsTrainingId/RenewsSessionId) + button click trigger inline JS |
| Client-side JS state → server-side bind | JS mutate hidden input value, submit form sends to EditTraining POST handler (Plan 01 Task 3 persist entity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326V-01 | Tampering | Hidden input RenewsTrainingId/RenewsSessionId value | mitigate | Server-side handler (Plan 01 Task 3) validate DAG + self-renewal — view tampering tidak bisa bypass backend ModelState. Hidden inputs trusted boundary client-side hanya, NOT trusted server-side. |
| T-326V-02 | XSS | RenewalSourceTitle text rendered via `@(Model.RenewalSourceTitle ?? ...)` | accept | Razor `@` HTML-encode default — safe. Source data dari TR.Judul / AS.Title yang di-input via form admin (Authorize Admin/HC) — low surface area; existing pattern reuse. |
| T-326V-03 | Information Disclosure | Section render expose source title to admin | accept | Admin user only (Authorize Admin/HC). RenewalSourceTitle = `Judul` (TR) atau `Title` (AS) — low sensitivity field already visible di Admin views. |
| T-326V-04 | Repudiation | Button "Hapus link renewal" click tidak audit-logged | accept | AuditLog "Update" existing L494-497 EditTraining POST capture event; granular field-level diff (RenewsTrainingId null→clear) tidak di-record — acceptable per Phase 321 EditLog scope precedent. |
| T-326V-05 | Denial of Service | Inline onclick JS performance | accept | Single-line synchronous DOM mutation — O(1) operation, no loop/async. Footprint negligible. |
</threat_model>

<verification>
Manual UAT deferred ke Plan 03. Plan 02 sufficient kalau:
1. `dotnet build` 0 Error setelah kedua task selesai
2. Grep acceptance criteria pass per task
3. Cross-Views grep `asp-validation-for="ValidUntil"` returns exactly 2 hits
4. Section "Renewal Source" markup match UI-SPEC L115-146 byte-for-byte (locked copywriting)
5. Indentation Razor file existing tidak rusak (style consistency)
</verification>

<success_criteria>
- Views/Admin/EditTraining.cshtml punya section card "Renewal Source" conditional (~28 baris) antara card "Data Training" + "Data Sertifikat"
- Views/Admin/EditTraining.cshtml + Views/Admin/AddTraining.cshtml masing-masing punya 1 span `asp-validation-for="ValidUntil"` baru
- Total UI delta ≤ 35 baris di 2 file (per UI-SPEC L287)
- Copywriting 4 string locked verbatim per UI-SPEC L177-188
- `dotnet build` 0 Error
- No new CSS, no new JS file, no new dependency (UI-SPEC L286)
</success_criteria>

<output>
After completion, create `.planning/phases/326-validator-hardening-p03-p06/326-02-SUMMARY.md` dengan:
- Files modified (2)
- Markup added (section card + 2 span)
- Build status (0 Error)
- Cross-Views ValidUntil span count (2)
- Next: Plan 03 UAT manual 6 SC + commit + push approval gate
</output>
