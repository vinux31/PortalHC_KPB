---
phase: 326
plan: 03
type: execute
wave: 3
depends_on: [326-01, 326-02]
files_modified:
  - .planning/phases/326-validator-hardening-p03-p06/326-UAT.md
autonomous: false
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4
  - SC-5
  - SC-6
must_haves:
  truths:
    - "SC-1 PASS: Add TR renewal tanggal < source → form error display (P03 summary)"
    - "SC-2 PASS: Add TR renewal tanggal > source → lolos no regression"
    - "SC-3 PASS: Add TR Permanent + ValidUntil isi → field-level error ValidUntil"
    - "SC-4 PASS: Add TR Permanent + ValidUntil null → lolos"
    - "SC-5 PASS: Add TR Annual + ValidUntil valid → lolos"
    - "SC-6 PASS: Edit TR yang IS renewal → section card visible → button click clear → submit persist null FK"
    - "326-UAT.md created dengan PASS/FAIL evidence per SC + screenshots/Playwright trace bila ada"
    - "Commit + push approval gate user-confirmed (CLAUDE.md DEV_WORKFLOW step 4-5)"
  artifacts:
    - path: ".planning/phases/326-validator-hardening-p03-p06/326-UAT.md"
      provides: "UAT evidence 6 SC + checkpoint verification"
      contains: "SC-1: PASS"
  key_links:
    - from: "User browser http://localhost:5277/Admin/AddTraining"
      to: "TrainingAdminController.AddTraining POST → ModelState"
      via: "form POST submit + error display di view"
      pattern: "Tanggal renewal harus lebih besar"
    - from: "User browser http://localhost:5277/Admin/EditTraining/{id}"
      to: "EditTraining GET render section + POST persist clear"
      via: "button click + form submit"
      pattern: "Hapus link renewal"
---

<objective>
Manual UAT 6 SC via browser lokal `http://localhost:5277` + Playwright MCP bila proton-helpful + commit + push approval gate user.

Purpose: Verifikasi end-to-end Plan 01 + Plan 02 deliverables match ROADMAP 6 SC. Hindari ship bug ke Dev/Prod kalau ada regression.

Output: 326-UAT.md dengan PASS/FAIL evidence per SC + screenshots/Playwright trace links. User explicit approval untuk commit + push ke origin/main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/326-validator-hardening-p03-p06/326-CONTEXT.md
@.planning/phases/326-validator-hardening-p03-p06/326-UI-SPEC.md
@.planning/phases/326-validator-hardening-p03-p06/326-01-PLAN.md
@.planning/phases/326-validator-hardening-p03-p06/326-02-PLAN.md
@.planning/phases/325-security-hardening-p01-p02-p05/325-UAT.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md

<reference_credentials>
Dev admin lokal (per MEMORY.md reference_dev_credentials.md):
- URL: http://localhost:5277
- Login: admin@pertamina.com / [password user-known]
- Routes: /Admin/AddTraining, /Admin/EditTraining/{id}, /Admin/ManageAssessment?tab=training
</reference_credentials>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + run lokal + smoke baseline check</name>
  <files>(no file modify — verifikasi runtime)</files>
  <read_first>
    - CLAUDE.md (Develop Workflow step 3 — verifikasi lokal dotnet build + dotnet run)
    - .planning/phases/326-validator-hardening-p03-p06/326-01-PLAN.md (acceptance criteria reference)
  </read_first>
  <action>
1. Run `dotnet build HcPortal.csproj` di working directory. Pastikan 0 Error + 0 baru warning (warning existing OK).

2. Run `dotnet run --project HcPortal.csproj` (atau equivalent kalau `appsettings.Development.json` punya custom binding). App listen di `http://localhost:5277`.

3. Smoke open browser `http://localhost:5277/Admin/AddTraining`:
   - Konfirmasi form render OK (no Razor exception, no 500 error)
   - Konfirmasi field "Berlaku Sampai" visible
   - Konfirmasi tidak ada section "Renewal Source" (Add tidak punya VM dengan renewal FK pre-populated)

4. Smoke open `http://localhost:5277/Admin/EditTraining/{any-id-non-renewal}`:
   - Konfirmasi form render OK
   - Konfirmasi section "Renewal Source" TIDAK render (guard `@if` evaluate false karena VM.RenewsTrainingId/SessionId = null)

5. Smoke open `http://localhost:5277/Admin/EditTraining/{any-id-IS-renewal}`:
   - **PRE-REQ:** Cari TR di DB lokal dengan `RenewsTrainingId IS NOT NULL OR RenewsSessionId IS NOT NULL`. Query: `sqlcmd -S localhost -d HcPortalDB_Dev -Q "SELECT TOP 5 Id, Judul, RenewsTrainingId, RenewsSessionId FROM TrainingRecords WHERE RenewsTrainingId IS NOT NULL OR RenewsSessionId IS NOT NULL"`
   - Bila tidak ada renewal record di DB lokal → skip step ini, defer ke Task 2 SC-6 (akan create renewal TR sebagai bagian SC-2)
   - Bila ada → konfirmasi section "Renewal Source" RENDER + button "Hapus link renewal" visible + heading "Renewal dari: {Judul}" tampil

Catat di 326-UAT.md draft awal: build OK + run OK + smoke render OK. Bila ada exception di console → STOP, debug, kembali ke Plan 01/02 untuk fix.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 Error
    - `dotnet run` start sukses, listen :5277 (cek via `curl -I http://localhost:5277` returns 200/302)
    - /Admin/AddTraining render no exception (cek browser DevTools console + server log)
    - /Admin/EditTraining/{id} render no exception untuk record non-renewal + IS-renewal (kalau ada)
    - 326-UAT.md draft pre-section "## Pre-UAT Smoke" PASS
  </acceptance_criteria>
  <done>Build + run sukses, smoke render OK kedua halaman. Ready untuk SC verification Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT 6 SC via browser + Playwright MCP (D-09)</name>
  <files>.planning/phases/326-validator-hardening-p03-p06/326-UAT.md</files>
  <what-built>
    Plan 01: P03 DAG validator (TR + AS branch) + self-renewal guard + P06 Permanent/ValidUntil validator di AddTraining POST + EditTraining GET (populate RenewalSourceTitle) + EditTraining POST (4 validator + 2 entity mapping). VM extended 3 field.
    Plan 02: Section card "Renewal Source" + clear button di EditTraining.cshtml + 2 span ValidUntil error di kedua Razor form.
  </what-built>
  <how-to-verify>
Login admin@pertamina.com di `http://localhost:5277`. Setup data test bila perlu (TR A tanggal 2026-01-01 sebagai source untuk SC-1/SC-2/SC-6 — bisa pakai existing TR di DB lokal atau create baru via /Admin/AddTraining standard non-renewal).

**SC-1 (P03 monotonic reject) — di /Admin/AddTraining:**
1. Pilih worker → toggle ke renewal mode → pilih TR A (tanggal 2026-01-01) sebagai source
2. Set Tanggal sertifikat baru = 2025-12-01 (lebih awal dari source)
3. Set fields lain valid (Judul, Penyelenggara, Kategori, Status=Passed)
4. Submit form
5. **Expected:** Form re-render dengan summary alert `"Tanggal renewal harus lebih besar dari tanggal sertifikat yang di-renew."` di `<div asp-validation-summary="All">`
6. Konfirmasi DB tidak insert record baru (`sqlcmd ... SELECT COUNT(*) FROM TrainingRecords WHERE Judul = '{judul-test}'` returns 0)

**SC-2 (P03 no-regression) — di /Admin/AddTraining:**
1. Sama setup SC-1, tapi Tanggal = 2026-06-01 (valid > source)
2. Submit form
3. **Expected:** Sukses redirect ke ManageAssessment?tab=training dengan TempData["Success"]
4. Konfirmasi DB insert 1 record baru dengan `RenewsTrainingId = A.Id`

**SC-3 (P06 reject Permanent+ValidUntil isi) — di /Admin/AddTraining:**
1. Mode non-renewal (atau renewal — irrelevant)
2. Set CertificateType = "Permanent" + ValidUntil = 2030-01-01
3. Set fields lain valid + Tanggal = today
4. Submit form
5. **Expected:** Form re-render dengan span `<span asp-validation-for="ValidUntil">` tampil dekat input ValidUntil dengan message `"Sertifikat Permanent tidak boleh punya tanggal expired."` (field-level error native — Add flow `return View(model)`)
6. Konfirmasi DB tidak insert record

**SC-4 (P06 no-regression Permanent+null) — di /Admin/AddTraining:**
1. Set CertificateType = "Permanent" + ValidUntil = (empty)
2. Set fields lain valid
3. Submit form
4. **Expected:** Sukses redirect, DB insert 1 record dengan CertificateType="Permanent" + ValidUntil=NULL

**SC-5 (P06 no-regression Annual+valid) — di /Admin/AddTraining:**
1. Set CertificateType = "Annual" + ValidUntil = today + 365
2. Set fields lain valid
3. Submit form
4. **Expected:** Sukses redirect, DB insert 1 record CertificateType="Annual" + ValidUntil populated

**SC-6 (P03 Edit self-renewal + clear button) — di /Admin/EditTraining/{renewal-record-id}:**
1. Pre-req: pakai record dari SC-2 (TR B yang renew TR A) atau seed manual renewal record
2. Open `/Admin/EditTraining/{B.Id}`
3. **Expected step A:** Section "Renewal Source" card visible dengan text `"Renewal dari: {A.Judul}"` + button "Hapus link renewal"
4. Klik button "Hapus link renewal"
5. **Expected step B:** Section hide (style.display='none' applied), hidden inputs cleared (cek DevTools — `document.getElementById('RenewsTrainingId').value === ''`)
6. Submit form (no other change)
7. **Expected step C:** Redirect sukses, DB `SELECT RenewsTrainingId FROM TrainingRecords WHERE Id = {B.Id}` returns NULL
8. **Form tampering test (defense step):** Re-open `/Admin/EditTraining/{B.Id}` (now non-renewal). Via DevTools, inject hidden input `<input name="RenewsTrainingId" value="{B.Id}">` (set ke diri sendiri). Submit form.
9. **Expected step D:** Form redirect dengan `TempData["Error"] = "Sertifikat tidak boleh renewal dirinya sendiri."` toast display

**Playwright MCP optional:** Bila ada Playwright MCP browser available, gunakan untuk screenshot evidence + automated form fill (parity Phase 325 UAT). Save trace ke `tests/playwright-report/`.

Document setiap SC di `.planning/phases/326-validator-hardening-p03-p06/326-UAT.md` dengan format:

```markdown
## SC-X: {Description}
**Status:** PASS / FAIL
**Steps:**
1. ...
**Expected:** ...
**Actual:** ...
**Evidence:** [screenshot path | Playwright trace link | sqlcmd output]
```

Resume signal: User reply "approved" atau "all PASS, lanjut commit" atau list FAIL detail kalau ada blocker.
  </how-to-verify>
  <resume-signal>Type "approved" (ALL 6 SC PASS, lanjut commit) atau "SC-X FAIL: {detail}" untuk back-track ke Plan 01/02 fix</resume-signal>
  <action>See &lt;how-to-verify&gt; above — manual UAT 6 SC via browser localhost:5277 + Playwright MCP optional + 326-UAT.md write evidence per SC.</action>
  <verify>Manual browser verification — UAT.md documents PASS/FAIL per SC dengan screenshot/Playwright trace/sqlcmd output evidence.</verify>
  <done>326-UAT.md exists dengan 6 SC PASS evidence + user explicit "approved" signal received.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Commit + push approval gate (CLAUDE.md DEV_WORKFLOW step 4-5)</name>
  <files>(no file modify — git commit + push)</files>
  <what-built>Plan 01 + Plan 02 + Plan 03 UAT evidence. Ready batch commit ke main lokal + push ke origin/main untuk IT promo prep.</what-built>
  <how-to-verify>
1. Run `git status` — konfirmasi modified files = exact match files_modified Plan 01 (2 file) + Plan 02 (2 file) + Plan 03 UAT.md (1 file). Total 5 file:
   - Controllers/TrainingAdminController.cs
   - Models/EditTrainingRecordViewModel.cs
   - Views/Admin/EditTraining.cshtml
   - Views/Admin/AddTraining.cshtml
   - .planning/phases/326-validator-hardening-p03-p06/326-UAT.md
   - + 326-01-SUMMARY.md + 326-02-SUMMARY.md (dari execute-plan output)

2. Run `git diff --stat` — review LOC delta. Expected: ~50-80 LOC total (validator + section card + 2 span + VM 3 field).

3. **User explicit decision:**
   - **Option A (recommended):** Commit + push batch sekarang ke origin/main (parity Phase 325 strategy).
   - **Option B:** Commit lokal saja, defer push sampai Phase 327 ready (v19.0 IT promo batch akhir per spec §11).
   - **Option C:** Hold (re-review code/UAT dulu).

4. Kalau Option A/B disetujui, eksekusi commit batch:
   ```
   git add Controllers/TrainingAdminController.cs Models/EditTrainingRecordViewModel.cs Views/Admin/EditTraining.cshtml Views/Admin/AddTraining.cshtml .planning/phases/326-validator-hardening-p03-p06/
   git commit -m "feat(326): validator hardening P03 DAG + P06 Permanent+ValidUntil reject + Edit renewal clear UX"
   ```
   Atau split per concern (validator commit + view commit) kalau user preference.

5. Kalau Option A, run `git push origin main`.

6. Update STATE.md `Last activity:` + `Current Position:` line + bump `completed_phases` counter. Update ROADMAP.md Phase 326 checkbox to `[x]` + tanggal SHIPPED.

7. (Defer post-Phase-327) IT_NOTIFY.md prep — Phase 327 selesai sebagai batch trigger.

Resume signal: User reply explicit "commit + push" atau "commit only, no push" atau "hold".
  </how-to-verify>
  <resume-signal>"commit + push" / "commit only" / "hold for review"</resume-signal>
  <action>See &lt;how-to-verify&gt; above — git status review + user decision Option A/B/C + execute git add/commit/push + update STATE.md + ROADMAP.md.</action>
  <verify>git log --oneline -1 shows feat(326) commit + git status clean + (Option A) git push origin main returns OK.</verify>
  <done>Phase 326 commit di main lokal (atau pushed origin/main per Option A) + STATE.md + ROADMAP.md updated reflect SHIPPED status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → AddTraining/EditTraining POST | Replay Plan 01 threat model — UAT verifikasi mitigasi T-326-01..04 actually trigger |
| Developer local → origin/main | Push approval gate prevent premature ship to remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326U-01 | Verification gap | Manual UAT 6 SC | mitigate | D-09 manual repro per spec §6.3 — coverage cukup untuk Phase 326 narrow scope (no async client validation, no race condition, no migration). xUnit defer to Phase 327 (DeriveCertificateStatus pure function lebih cocok). |
| T-326U-02 | Premature ship | git push tanpa user approval | mitigate | Task 3 explicit checkpoint:human-action gate — user confirm push / no-push / hold. CLAUDE.md DEV_WORKFLOW step 4-5 honored. |
| T-326U-03 | Tampering test inadequacy | Self-renewal defense step | mitigate | SC-6 step 8-9 explicit DevTools injection test — verify backend reject form tampering even though normal UI flow tidak expose self-renewal path. |
</threat_model>

<verification>
Phase 326 SHIPPED criteria (CLAUDE.md DEV_WORKFLOW Honored):
1. `dotnet build` 0 Error
2. `dotnet run` listen :5277 OK
3. 6 SC ALL PASS browser-verified (atau Playwright MCP automated where applicable)
4. 326-UAT.md exists dengan evidence per SC
5. Git commit + push approval user-explicit
6. STATE.md + ROADMAP.md updated post-ship
7. IT promo notify deferred ke Phase 327 batch akhir per spec §11
</verification>

<success_criteria>
- 326-UAT.md created dengan 6 SC PASS evidence (screenshots/Playwright trace/sqlcmd output)
- `dotnet build` 0 Error (final verify post-commit)
- User explicit approval untuk commit + push (Option A/B/C decision recorded)
- STATE.md updated: completed_phases bumps + last_activity reflect Phase 326 SHIPPED
- ROADMAP.md Phase 326 checkbox `[x]` + tanggal SHIPPED
- Git tag (optional) — defer ke Phase 327 batch tag v19.0-complete
</success_criteria>

<output>
After completion, create `.planning/phases/326-validator-hardening-p03-p06/326-03-SUMMARY.md` + `326-SUMMARY.md` (phase-level rollup) dengan:
- UAT 6 SC PASS/FAIL counts
- Commit hash(es) Phase 326
- Push status (pushed origin/main OR pending Phase 327 batch)
- STATE.md + ROADMAP.md updates applied
- Next phase trigger: `/gsd-plan-phase 327` (Timezone DateOnly Refactor — Migration required per ROADMAP L668-669)
</output>
