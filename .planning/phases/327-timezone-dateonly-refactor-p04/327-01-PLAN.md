---
phase: 327-timezone-dateonly-refactor-p04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/CertificateStatusTests.cs
autonomous: true
requirements: [SC-3]
threats: [T-327-04, T-327-05]
must_haves:
  truths:
    - "xUnit project HcPortal.Tests jalan dotnet test green dengan tambahan 8 test baseline"
    - "8 test case CertificateStatusTests pass pakai SIGNATURE EXISTING DateTime? (baseline GREEN pre-refactor)"
  artifacts:
    - path: "HcPortal.Tests/CertificateStatusTests.cs"
      provides: "8 test method (6 Theory InlineData + 2 Fact null) DeriveCertificateStatus boundary coverage"
      contains: "public class CertificateStatusTests"
  key_links:
    - from: "HcPortal.Tests/CertificateStatusTests.cs"
      to: "Models/CertificationManagementViewModel.cs SertifikatRow.DeriveCertificateStatus"
      via: "using HcPortal.Models; static method call"
      pattern: "SertifikatRow\\.DeriveCertificateStatus"
---

<objective>
Bootstrap xUnit baseline test untuk `DeriveCertificateStatus` SEBELUM refactor — 8 test case GREEN pakai signature existing `DateTime? validUntil` (T-327-04 cascade preventer). Plan 04 nanti refactor signature → DateOnly? + test data sesuaikan tanpa kehilangan baseline coverage.

Purpose: Wave 0 foundation untuk Nyquist Dim 8 — semua plan downstream punya safety net regression. Plan 04 cuma boleh GREEN kembali setelah signature flip.
Output: 1 file baru `HcPortal.Tests/CertificateStatusTests.cs` dengan komentar Bahasa Indonesia + vanilla Assert (D-14).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-VALIDATION.md
@HcPortal.Tests/FileUploadHelperTests.cs
@Models/CertificationManagementViewModel.cs

<interfaces>
<!-- Signature EXISTING (pre-Phase 327 refactor) — Plan 01 test PAKAI ini. Plan 04 akan flip ke DateOnly?. -->

From Models/CertificationManagementViewModel.cs:48-63 (EXISTING, pre-refactor):
```csharp
public static CertificateStatus DeriveCertificateStatus(DateTime? validUntil, string? certificateType)
{
    if (certificateType == "Permanent") return CertificateStatus.Permanent;
    if (validUntil == null) return CertificateStatus.Expired;
    var days = (validUntil.Value - DateTime.UtcNow).Days;
    if (days < 0) return CertificateStatus.Expired;
    if (days <= 30) return CertificateStatus.AkanExpired;
    return CertificateStatus.Aktif;
}

public enum CertificateStatus { Aktif, AkanExpired, Expired, Permanent }
```

From HcPortal.Tests/FileUploadHelperTests.cs (analog file Phase 325):
- File-scoped namespace `namespace HcPortal.Tests;`
- Top comment Bahasa Indonesia menyebut "Phase {N}"
- `using Xunit;` + `using HcPortal.Models;`
- Vanilla `Assert.Equal/True/Null` (NO FluentAssertions)
- Naming `Method_Scenario_ExpectedOutcome`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat HcPortal.Tests/CertificateStatusTests.cs (8 test baseline GREEN)</name>
  <files>HcPortal.Tests/CertificateStatusTests.cs</files>
  <read_first>
    - CLAUDE.md (Bahasa Indonesia, dev workflow)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md §decisions (D-14 vanilla xUnit + 8 case spec)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 7 (xUnit project structure) + Audit 11 (Validation) + Code Examples
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §1 (analog FileUploadHelperTests.cs) + Shared Pattern G
    - HcPortal.Tests/FileUploadHelperTests.cs (analog exact — komentar BI + vanilla Assert)
    - Models/CertificationManagementViewModel.cs (signature existing DeriveCertificateStatus + enum CertificateStatus)
  </read_first>
  <behavior>
    - Test 1 (Theory): validUntil=today+100, type="Annual" → CertificateStatus.Aktif
    - Test 2 (Theory): validUntil=today+30, type="Annual" → CertificateStatus.AkanExpired (boundary inclusive)
    - Test 3 (Theory): validUntil=today+1, type="Annual" → CertificateStatus.AkanExpired
    - Test 4 (Theory): validUntil=today (offset 0), type="Annual" → CertificateStatus.AkanExpired (days=0)
    - Test 5 (Theory): validUntil=today-1, type="Annual" → CertificateStatus.Expired
    - Test 6 (Theory): validUntil=today+100, type="Permanent" → CertificateStatus.Permanent (validUntil ignored)
    - Test 7 (Fact): validUntil=null, type=null → CertificateStatus.Expired
    - Test 8 (Fact): validUntil=null, type="Permanent" → CertificateStatus.Permanent
  </behavior>
  <action>
    Buat file baru `HcPortal.Tests/CertificateStatusTests.cs` PERSIS dengan body berikut (signature EXISTING `DateTime? validUntil` — Plan 04 nanti flip ke DateOnly?):

    ```csharp
    // Unit test SertifikatRow.DeriveCertificateStatus (Phase 327 P04 — Wave 0 baseline, signature DateTime? existing).
    // 8 test case boundary coverage (6 Theory + 2 Fact) per CONTEXT.md D-14 + spec §7.5.
    // Plan 04 akan refactor signature → DateOnly? + helper Today(int offset) sesuaikan.

    using System;
    using HcPortal.Models;
    using Xunit;

    namespace HcPortal.Tests;

    public class CertificateStatusTests
    {
        // Helper: today UTC + offset hari, return DateTime midnight.
        // Plan 04 akan ubah return DateOnly.
        private static DateTime Today(int offset) =>
            DateTime.UtcNow.Date.AddDays(offset);

        [Theory]
        [InlineData(100, "Annual", CertificateStatus.Aktif)]           // > 30 hari = Aktif
        [InlineData(30, "Annual", CertificateStatus.AkanExpired)]      // boundary inclusive (days <= 30)
        [InlineData(1, "Annual", CertificateStatus.AkanExpired)]       // 1 hari lagi
        [InlineData(0, "Annual", CertificateStatus.AkanExpired)]       // hari ini (days = 0)
        [InlineData(-1, "Annual", CertificateStatus.Expired)]          // sudah lewat
        [InlineData(100, "Permanent", CertificateStatus.Permanent)]    // Permanent override
        public void DeriveCertificateStatus_VariousScenarios_ReturnsExpected(
            int offset, string certificateType, CertificateStatus expected)
        {
            var result = SertifikatRow.DeriveCertificateStatus(Today(offset), certificateType);
            Assert.Equal(expected, result);
        }

        [Fact]
        public void DeriveCertificateStatus_NullValidUntil_NonPermanent_ReturnsExpired()
        {
            var result = SertifikatRow.DeriveCertificateStatus(null, null);
            Assert.Equal(CertificateStatus.Expired, result);
        }

        [Fact]
        public void DeriveCertificateStatus_NullValidUntil_Permanent_ReturnsPermanent()
        {
            var result = SertifikatRow.DeriveCertificateStatus(null, "Permanent");
            Assert.Equal(CertificateStatus.Permanent, result);
        }
    }
    ```

    Komit setelah `dotnet test` GREEN. Conventional commit: `test(327-01): add CertificateStatusTests 8 baseline cases pre-refactor`.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo --filter "FullyQualifiedName~CertificateStatusTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/CertificateStatusTests.cs` exists
    - `grep -c "public class CertificateStatusTests" HcPortal.Tests/CertificateStatusTests.cs` returns 1
    - `grep -c "\[Theory\]" HcPortal.Tests/CertificateStatusTests.cs` returns 1 (1 Theory method 6 InlineData)
    - `grep -c "\[InlineData(" HcPortal.Tests/CertificateStatusTests.cs` returns 6
    - `grep -c "\[Fact\]" HcPortal.Tests/CertificateStatusTests.cs` returns 2
    - `grep -c "FluentAssertions" HcPortal.Tests/CertificateStatusTests.cs` returns 0 (D-14 enforcement)
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo --filter "FullyQualifiedName~CertificateStatusTests"` exit 0, "Passed: 8" di output
    - `dotnet test HcPortal.sln --nologo` exit 0, total passed = 9 Phase 325 FileUploadHelper + 8 Phase 327 = 17 minimum
  </acceptance_criteria>
  <done>
    8 test method (6 Theory InlineData + 2 Fact) GREEN pakai signature existing DateTime?, vanilla Assert, komentar BI, file di-commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness ↔ Production code | xUnit test consume static method `SertifikatRow.DeriveCertificateStatus` (read-only call) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-04 | Tampering (compile-error cascade) | Plan ordering | mitigate | Plan 01 Wave 1 SEBELUM entity flip — test pakai signature DateTime? existing, GREEN baseline established sebelum Plan 02 break anything |
| T-327-05 | Tampering (Phase 326 regression) | TrainingAdminController POST handler | accept | Plan 01 zero touch ke Controllers/ — Phase 326 validator regression risk = 0 di plan ini. Smoke 6 SC Phase 326 di Plan 08 pre-final commit |
</threat_model>

<verification>
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` filter CertificateStatusTests → 8 passed
- `dotnet test HcPortal.sln` full suite → ≥17 total passed (Phase 325 9 + Phase 327 8)
- Zero modifikasi file production code (grep `git diff --name-only HEAD~1` returns only `HcPortal.Tests/CertificateStatusTests.cs`)
</verification>

<success_criteria>
SC-3 baseline established: 8 test GREEN pre-refactor. Plan 04 sukses kalau test GREEN kembali setelah signature flip ke DateOnly?.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-01-SUMMARY.md` dengan: file created, test count, sampling baseline (`dotnet test` runtime), commit hash.
</output>
