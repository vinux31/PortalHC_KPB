---
phase: 327-timezone-dateonly-refactor-p04
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Models/TrainingRecord.cs
  - Models/AssessmentSession.cs
  - Models/UnifiedTrainingRecord.cs
autonomous: true
requirements: []
threats: [T-327-04]
must_haves:
  truths:
    - "TrainingRecord.ValidUntil + AssessmentSession.ValidUntil + UnifiedTrainingRecord.ValidUntil bertype DateOnly?"
    - "TrainingRecord.IsExpiringSoon + DaysUntilExpiry pakai DayNumber arithmetic + UtcNow"
    - "UnifiedTrainingRecord.IsExpired pakai DateOnly.FromDateTime(DateTime.UtcNow) (kill 2nd tz bug DateTime.Now)"
    - "dotnet build di-EXPECT GAGAL di tahap ini (cascade error di Controllers/ + VM downstream) — itu signal benar untuk Plan 03"
  artifacts:
    - path: "Models/TrainingRecord.cs"
      provides: "ValidUntil DateOnly? + computed props DayNumber arithmetic"
      contains: "public DateOnly? ValidUntil"
    - path: "Models/AssessmentSession.cs"
      provides: "ValidUntil DateOnly?"
      contains: "public DateOnly? ValidUntil"
    - path: "Models/UnifiedTrainingRecord.cs"
      provides: "ValidUntil DateOnly? + IsExpired UtcNow alignment"
      contains: "DateOnly.FromDateTime(DateTime.UtcNow)"
  key_links:
    - from: "Models/TrainingRecord.cs IsExpiringSoon"
      to: "Services/WorkerDataService.cs:264"
      via: "computed prop bool consume"
      pattern: "tr\\.IsExpiringSoon"
---

<objective>
Flip 3 entity/rollup property `ValidUntil` dari `DateTime?` → `DateOnly?` + rewrite computed properties pakai DayNumber arithmetic + `DateOnly.FromDateTime(DateTime.UtcNow)` (D-09 + D-10).

Purpose: Step 2-3 dari 11-step Implementation Order. Build SENGAJA gagal di tahap ini karena Controllers/VM downstream belum di-flip — itu signal benar (RESEARCH Pitfall 1). Plan 03 + 05 nanti cascade fix.
Output: 3 file Model touched, computed props DayNumber-based.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@Models/TrainingRecord.cs
@Models/AssessmentSession.cs
@Models/UnifiedTrainingRecord.cs

<interfaces>
<!-- Pattern A (Shared) — DateOnly DayNumber arithmetic canonical formula -->
```csharp
var today = DateOnly.FromDateTime(DateTime.UtcNow);
var days = validUntil.Value.DayNumber - today.DayNumber;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Models/TrainingRecord.cs + rewrite IsExpiringSoon + DaysUntilExpiry</name>
  <files>Models/TrainingRecord.cs</files>
  <read_first>
    - Models/TrainingRecord.cs (current state L43, L71-82, L85-95)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-07, D-09, D-10)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §2 (analog AssessmentSession.cs + rewrite snippet) + Shared Pattern A
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 5 (computed props call sites)
  </read_first>
  <action>
    Di `Models/TrainingRecord.cs`:

    **Edit L43** — flip type:
    ```csharp
    // SEBELUM:
    public DateTime? ValidUntil { get; set; }

    // SESUDAH:
    public DateOnly? ValidUntil { get; set; }
    ```

    **Edit L71-82** — rewrite `IsExpiringSoon` getter (per D-10 + Shared Pattern A):
    ```csharp
    public bool IsExpiringSoon
    {
        get
        {
            if (ValidUntil.HasValue && Status == "Valid")
            {
                var today = DateOnly.FromDateTime(DateTime.UtcNow);
                var daysUntilExpiry = ValidUntil.Value.DayNumber - today.DayNumber;
                return daysUntilExpiry <= 30 && daysUntilExpiry >= 0;
            }
            return false;
        }
    }
    ```

    **Edit L85-95** — rewrite `DaysUntilExpiry` getter:
    ```csharp
    public int? DaysUntilExpiry
    {
        get
        {
            if (ValidUntil.HasValue)
            {
                var today = DateOnly.FromDateTime(DateTime.UtcNow);
                return ValidUntil.Value.DayNumber - today.DayNumber;
            }
            return null;
        }
    }
    ```

    Conventional commit: `refactor(327-02): TrainingRecord ValidUntil DateOnly + DayNumber arithmetic computed props`.
  </action>
  <verify>
    <automated>grep -c "public DateOnly? ValidUntil" Models/TrainingRecord.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public DateOnly? ValidUntil" Models/TrainingRecord.cs` returns 1
    - `grep -c "DateOnly.FromDateTime(DateTime.UtcNow)" Models/TrainingRecord.cs` returns 2 (IsExpiringSoon + DaysUntilExpiry)
    - `grep -c "DayNumber" Models/TrainingRecord.cs` returns ≥2
    - `grep -c "(ValidUntil.Value - DateTime" Models/TrainingRecord.cs` returns 0 (old pattern gone)
    - `grep -c "public DateTime? ValidUntil" Models/TrainingRecord.cs` returns 0
  </acceptance_criteria>
  <done>
    TrainingRecord.cs ValidUntil DateOnly? + 2 computed props DayNumber arithmetic + UtcNow. File-level grep clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip Models/AssessmentSession.cs ValidUntil DateOnly?</name>
  <files>Models/AssessmentSession.cs</files>
  <read_first>
    - Models/AssessmentSession.cs (current state L65)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §3
  </read_first>
  <action>
    Di `Models/AssessmentSession.cs` L65 (single edit, zero computed prop):

    ```csharp
    // SEBELUM:
    public DateTime? ValidUntil { get; set; }

    // SESUDAH:
    public DateOnly? ValidUntil { get; set; }
    ```

    Conventional commit: `refactor(327-02): AssessmentSession ValidUntil DateOnly?`.
  </action>
  <verify>
    <automated>grep -c "public DateOnly? ValidUntil" Models/AssessmentSession.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public DateOnly? ValidUntil" Models/AssessmentSession.cs` returns 1
    - `grep -c "public DateTime? ValidUntil" Models/AssessmentSession.cs` returns 0
  </acceptance_criteria>
  <done>
    AssessmentSession.cs ValidUntil DateOnly?. Single-line flip.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip Models/UnifiedTrainingRecord.cs ValidUntil + IsExpired (UtcNow alignment kill 2nd tz bug)</name>
  <files>Models/UnifiedTrainingRecord.cs</files>
  <read_first>
    - Models/UnifiedTrainingRecord.cs (current state L26, L40)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-08 cascade UtcNow)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §5
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md user_constraints D-08 "DateTime.Now → UtcNow alignment Q2"
  </read_first>
  <action>
    Di `Models/UnifiedTrainingRecord.cs`:

    **Edit L26** — flip type:
    ```csharp
    // SEBELUM:
    public DateTime? ValidUntil { get; set; }

    // SESUDAH:
    public DateOnly? ValidUntil { get; set; }
    ```

    **Edit L40** — rewrite IsExpired full (DateTime.Now → UtcNow alignment per D-09 + DayNumber comparison via `<` operator native DateOnly):
    ```csharp
    // SEBELUM:
    public bool IsExpired => ValidUntil.HasValue && ValidUntil.Value < DateTime.Now;

    // SESUDAH:
    public bool IsExpired => ValidUntil.HasValue && ValidUntil.Value < DateOnly.FromDateTime(DateTime.UtcNow);
    ```

    Conventional commit: `refactor(327-02): UnifiedTrainingRecord ValidUntil DateOnly + IsExpired UtcNow alignment`.
  </action>
  <verify>
    <automated>grep -c "DateOnly.FromDateTime(DateTime.UtcNow)" Models/UnifiedTrainingRecord.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public DateOnly? ValidUntil" Models/UnifiedTrainingRecord.cs` returns 1
    - `grep -c "DateOnly.FromDateTime(DateTime.UtcNow)" Models/UnifiedTrainingRecord.cs` returns 1
    - `grep -c "DateTime.Now" Models/UnifiedTrainingRecord.cs` returns 0 (kill 2nd tz bug)
    - `grep -c "public DateTime? ValidUntil" Models/UnifiedTrainingRecord.cs` returns 0
  </acceptance_criteria>
  <done>
    UnifiedTrainingRecord.cs flip + IsExpired pakai DateOnly + UtcNow. Bug DateTime.Now eliminasi.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entity layer ↔ Controller/Service | Entity property type contract berubah; consumer harus follow (Plan 03/05 cascade fix) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-04 | Tampering (compile cascade) | Entity flip pre VM/Controller fix | accept | Build EXPECTED to fail post-Plan 02 — itu signal benar (RESEARCH Pitfall 1). Cascade fix di Plan 03 + 05. Test xUnit Plan 01 baseline tetap GREEN (signature CertificationManagementViewModel belum di-flip — Plan 04 yang flip). |
</threat_model>

<verification>
- 3 file Model di-edit per acceptance criteria di atas.
- `dotnet build` EXPECTED gagal — TIDAK menjalankan build di plan ini (cascade error normal, di-fix Plan 03 + 05). Plan 03 verifikasi build sukses setelah VM cascade.
- xUnit baseline Plan 01: TEST TIDAK terpengaruh karena `CertificationManagementViewModel.cs SertifikatRow.DeriveCertificateStatus` signature belum di-flip (Plan 04 yang flip). Test masih reference `DateTime?` signature.
</verification>

<success_criteria>
3 entity/rollup ValidUntil DateOnly? + computed props DayNumber arithmetic + UtcNow alignment. 2nd tz bug (UnifiedTrainingRecord.cs `DateTime.Now`) eliminated.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-02-SUMMARY.md`: 3 file flipped, grep counts confirmed, note "build expected fail — Plan 03 cascade fix VM + L189 etc., Plan 05 controller var today sweep".
</output>
