---
phase: 327-timezone-dateonly-refactor-p04
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Models/CreateTrainingRecordViewModel.cs
  - Models/EditTrainingRecordViewModel.cs
  - Models/CreateManualAssessmentViewModel.cs
  - Models/CertificationManagementViewModel.cs
  - Models/AnalyticsDashboardViewModel.cs
autonomous: true
requirements: []
threats: [T-327-02, T-327-04]
must_haves:
  truths:
    - "4 input VM ValidUntil DateOnly? (CreateTraining + EditTraining + CreateManualAssessment Create + Edit)"
    - "SertifikatRow.ValidUntil + CertificateChainGroup.LatestValidUntil + RenewalGroup.MinValidUntil DateOnly?"
    - "ExpiringSoonItem.TanggalExpired DateOnly (non-nullable)"
    - "[DataType(DataType.Date)] annotation preserve di 4 input VM (D-04)"
    - "NO pre-add [DisplayFormat] — smoke first di Plan 08 (Pitfall 5)"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "SertifikatRow.ValidUntil + 2 rollup + DeriveCertificateStatus signature (refactored Plan 04)"
      contains: "public DateOnly? ValidUntil"
    - path: "Models/AnalyticsDashboardViewModel.cs"
      provides: "ExpiringSoonItem.TanggalExpired DateOnly"
      contains: "public DateOnly TanggalExpired"
  key_links:
    - from: "4 input VM"
      to: "Controllers/TrainingAdminController.cs + AssessmentAdminController.cs POST handlers"
      via: "ModelBinder DateOnly bind dari [DataType(Date)] + <input type=date>"
      pattern: "model\\.ValidUntil"
---

<objective>
Flip 4 input VM + 5 rollup property `ValidUntil`/`TanggalExpired` ke `DateOnly?` (atau `DateOnly` non-null) per D-07 + D-08. **TIDAK** refactor DeriveCertificateStatus signature di plan ini — Plan 04 yang refactor + xUnit update bersama.

Purpose: Step 4 dari 11-step Implementation Order. Cascade flip downstream entity Plan 02. Fix sub-set call sites yang masih hard-coded DateTime (e.g., L189 RenewalController via Plan 05).
Output: 5 file Models touched (4 input VM + 1 dashboard rollup).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@Models/CreateTrainingRecordViewModel.cs
@Models/EditTrainingRecordViewModel.cs
@Models/CreateManualAssessmentViewModel.cs
@Models/CertificationManagementViewModel.cs
@Models/AnalyticsDashboardViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip 4 input VM ValidUntil DateOnly?</name>
  <files>Models/CreateTrainingRecordViewModel.cs, Models/EditTrainingRecordViewModel.cs, Models/CreateManualAssessmentViewModel.cs</files>
  <read_first>
    - Models/CreateTrainingRecordViewModel.cs (L51)
    - Models/EditTrainingRecordViewModel.cs (L53 — post Phase 326 baseline 3 field renewal)
    - Models/CreateManualAssessmentViewModel.cs (L34 + L97 — Create + Edit kelas dalam 1 file)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-07 + D-04 annotation preserve)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §7-9
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 9 (TagHelper bug warning — NO pre-add DisplayFormat)
  </read_first>
  <action>
    Untuk SETIAP file di bawah, flip ValidUntil property dari `DateTime?` ke `DateOnly?`. PRESERVE `[DataType(DataType.Date)]` annotation. JANGAN tambah `[DisplayFormat]` (smoke first di Plan 08 per Pitfall 5 Shared Pattern F).

    **`Models/CreateTrainingRecordViewModel.cs:51`:**
    ```csharp
    // SEBELUM:
    [DataType(DataType.Date)]
    [Display(Name = "Valid Until")]
    public DateTime? ValidUntil { get; set; }

    // SESUDAH:
    [DataType(DataType.Date)]
    [Display(Name = "Valid Until")]
    public DateOnly? ValidUntil { get; set; }
    ```

    **`Models/EditTrainingRecordViewModel.cs:53`:** sama pattern (flip type, preserve annotation).

    **`Models/CreateManualAssessmentViewModel.cs`:** **2 occurrence** — L34 (kelas Create) dan L97 (kelas Edit dalam file sama). Flip keduanya.

    Conventional commit: `refactor(327-03): flip 4 input VM ValidUntil DateOnly?`.
  </action>
  <verify>
    <automated>grep -rc "public DateOnly? ValidUntil" Models/CreateTrainingRecordViewModel.cs Models/EditTrainingRecordViewModel.cs Models/CreateManualAssessmentViewModel.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public DateOnly? ValidUntil" Models/CreateTrainingRecordViewModel.cs` returns 1
    - `grep -c "public DateOnly? ValidUntil" Models/EditTrainingRecordViewModel.cs` returns 1
    - `grep -c "public DateOnly? ValidUntil" Models/CreateManualAssessmentViewModel.cs` returns 2
    - `grep -c "public DateTime? ValidUntil" Models/CreateTrainingRecordViewModel.cs` returns 0
    - `grep -c "public DateTime? ValidUntil" Models/EditTrainingRecordViewModel.cs` returns 0
    - `grep -c "public DateTime? ValidUntil" Models/CreateManualAssessmentViewModel.cs` returns 0
    - `grep -c "\[DataType(DataType.Date)\]" Models/CreateTrainingRecordViewModel.cs` returns ≥1 (preserve)
    - `grep -c "DisplayFormat" Models/CreateTrainingRecordViewModel.cs` returns 0 (no pre-add — smoke first)
    - `grep -c "DisplayFormat" Models/CreateManualAssessmentViewModel.cs` returns 0
  </acceptance_criteria>
  <done>
    4 input VM ValidUntil DateOnly? (3 file, 4 properties). Annotation preserved. NO DisplayFormat.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip CertificationManagementViewModel 3 property + AnalyticsDashboard ExpiringSoonItem.TanggalExpired</name>
  <files>Models/CertificationManagementViewModel.cs, Models/AnalyticsDashboardViewModel.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs (L38, L74, L108 — 3 property flip; L48-63 = DeriveCertificateStatus method JANGAN sentuh di plan ini, Plan 04 yang refactor)
    - Models/AnalyticsDashboardViewModel.cs (L64-70 — ExpiringSoonItem.TanggalExpired NON-nullable DateTime)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-07 + D-08)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §4 + §6
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 2 (CRITICAL: nama exact `ExpiringSoonItem` BUKAN `RenewalCertificateRow`)
  </read_first>
  <action>
    **`Models/CertificationManagementViewModel.cs`** — flip 3 property (DeriveCertificateStatus method body SKIP, Plan 04 punya):

    **L38** (SertifikatRow.ValidUntil):
    ```csharp
    // SEBELUM:
    public DateTime? ValidUntil { get; set; }
    // SESUDAH:
    public DateOnly? ValidUntil { get; set; }
    ```

    **L74** (CertificateChainGroup.LatestValidUntil):
    ```csharp
    public DateOnly? LatestValidUntil { get; set; }
    ```

    **L108** (RenewalGroup.MinValidUntil — per RESEARCH bukan `CertificateGroup`):
    ```csharp
    public DateOnly? MinValidUntil { get; set; }
    ```

    **`Models/AnalyticsDashboardViewModel.cs:68`** (ExpiringSoonItem.TanggalExpired — NON-nullable):
    ```csharp
    // SEBELUM:
    public DateTime TanggalExpired { get; set; }
    // SESUDAH:
    public DateOnly TanggalExpired { get; set; }
    ```

    Conventional commit: `refactor(327-03): flip 5 rollup VM (SertifikatRow + 2 CertGroup + ExpiringSoonItem) DateOnly`.
  </action>
  <verify>
    <automated>grep -c "public DateOnly" Models/CertificationManagementViewModel.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public DateOnly? ValidUntil" Models/CertificationManagementViewModel.cs` returns 1
    - `grep -c "public DateOnly? LatestValidUntil" Models/CertificationManagementViewModel.cs` returns 1
    - `grep -c "public DateOnly? MinValidUntil" Models/CertificationManagementViewModel.cs` returns 1
    - `grep -c "public DateTime? ValidUntil" Models/CertificationManagementViewModel.cs` returns 0
    - `grep -c "public DateTime? LatestValidUntil" Models/CertificationManagementViewModel.cs` returns 0
    - `grep -c "public DateTime? MinValidUntil" Models/CertificationManagementViewModel.cs` returns 0
    - `grep -c "public DateOnly TanggalExpired" Models/AnalyticsDashboardViewModel.cs` returns 1
    - `grep -c "public DateTime TanggalExpired" Models/AnalyticsDashboardViewModel.cs` returns 0
    - DeriveCertificateStatus signature UNCHANGED di plan ini: `grep -c "DateTime? validUntil" Models/CertificationManagementViewModel.cs` returns 1 (Plan 04 yang flip ke DateOnly?)
  </acceptance_criteria>
  <done>
    5 rollup VM property DateOnly. DeriveCertificateStatus signature TIDAK disentuh (Plan 04 punya).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VM ↔ Razor TagHelper `asp-for` binding | DateOnly? ↔ HTML5 `<input type=date>` value attribute via ASP.NET Core 8 binder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-02 | Tampering (Razor TagHelper bug #47628 d-M-yyyy) | 4 input VM Razor render | accept (smoke first) | NO pre-add `[DisplayFormat]` di Plan 03. Plan 08 manual UAT browser inspect input value. Kalau confirmed bug → Shared Pattern F retrofit `[DisplayFormat(DataFormatString = "{0:yyyy-MM-dd}", ApplyFormatInEditMode = true)]` ke 4 VM. |
| T-327-04 | Tampering (compile cascade) | DeriveCertificateStatus call sites (6+) di Controllers | accept | Signature DeriveCertificateStatus belum di-flip di plan ini — call sites di Controllers TETAP kompile (signature `DateTime?` accept null + accept DateOnly?.Value via implicit? **NO** — call site pass `t.ValidUntil` yang sekarang DateOnly? → type mismatch). Build EXPECTED gagal post-Plan 03 di Controllers. Plan 04 refactor signature + Plan 05 cascade fix. |
</threat_model>

<verification>
- 5 file Models di-edit per acceptance criteria.
- `dotnet build` EXPECTED tetap gagal — DeriveCertificateStatus signature mismatch + Controllers/ + RenewalController L189 etc. (Plan 04 + 05).
- xUnit Plan 01 baseline: TEST TETAP GREEN (DeriveCertificateStatus signature `DateTime?` belum di-flip).
</verification>

<success_criteria>
4 input VM + 5 rollup VM ValidUntil/TanggalExpired DateOnly. ExpiringSoonItem nama exact verified. Annotations preserve. Plan 04 next: refactor DeriveCertificateStatus signature + Plan 01 test update.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-03-SUMMARY.md`: 5 file touched, 9 property flipped (4 input + 5 rollup), build status (expected fail di Controllers/), test status (Plan 01 GREEN tetap), note Plan 04 next.
</output>
