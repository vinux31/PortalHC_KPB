---
phase: 327-timezone-dateonly-refactor-p04
plan: 04
type: tdd
wave: 4
depends_on: [03]
files_modified:
  - Models/CertificationManagementViewModel.cs
  - HcPortal.Tests/CertificateStatusTests.cs
autonomous: true
requirements: [SC-3]
threats: [T-327-04]
must_haves:
  truths:
    - "DeriveCertificateStatus signature DateOnly? validUntil, string? certificateType + body DayNumber arithmetic + UtcNow"
    - "8/8 CertificateStatusTests GREEN dengan signature baru + helper Today(int offset) return DateOnly"
    - "Semantik identik existing (5 case + boundary + null + Permanent override)"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "DeriveCertificateStatus DateOnly? signature + DayNumber body"
      contains: "DateOnly? validUntil"
    - path: "HcPortal.Tests/CertificateStatusTests.cs"
      provides: "8 test method GREEN signature baru"
      contains: "private static DateOnly Today(int offset)"
  key_links:
    - from: "HcPortal.Tests/CertificateStatusTests.cs Today helper"
      to: "DeriveCertificateStatus signature DateOnly?"
      via: "static method call vanilla Assert.Equal"
      pattern: "SertifikatRow\\.DeriveCertificateStatus\\(Today\\("
---

<objective>
Refactor `DeriveCertificateStatus` signature dari `DateTime? validUntil` → `DateOnly? validUntil` + body pakai DayNumber arithmetic + `DateOnly.FromDateTime(DateTime.UtcNow)` (D-06). Update Plan 01 test file `CertificateStatusTests.cs` agar 8/8 GREEN kembali — helper `Today(int offset)` return DateOnly.

Purpose: Step 5 dari 11-step Implementation Order. TDD RED→GREEN cycle: signature flip BREAK test (compile/runtime mismatch), update test helper + signature → GREEN.
Output: 2 file touched, 8/8 test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@Models/CertificationManagementViewModel.cs
@HcPortal.Tests/CertificateStatusTests.cs

<interfaces>
<!-- Target signature post-Plan 04 (D-06 spec verbatim) -->
```csharp
public static CertificateStatus DeriveCertificateStatus(DateOnly? validUntil, string? certificateType)
```
</interfaces>
</context>

<feature>
  <name>DeriveCertificateStatus DateOnly? signature refactor</name>
  <files>Models/CertificationManagementViewModel.cs, HcPortal.Tests/CertificateStatusTests.cs</files>
  <behavior>
    Semantik identik baseline (Plan 01):
    - Permanent + apapun → Permanent
    - null + non-Permanent → Expired
    - validUntil < today → Expired
    - 0 ≤ days ≤ 30 → AkanExpired (boundary inclusive)
    - days > 30 → Aktif

    8 test case sama persis (offset+type→expected). Hanya helper `Today` return type berubah dari DateTime → DateOnly.
  </behavior>
  <implementation>
    Two-step TDD cycle:
    1. RED: Edit signature + body Models/CertificationManagementViewModel.cs → test compile fail (helper Today return DateTime mismatch). Confirm fail via `dotnet test`.
    2. GREEN: Edit helper `Today(int offset)` di test file → return DateOnly. Run test → 8/8 pass.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor DeriveCertificateStatus signature + body (RED step)</name>
  <files>Models/CertificationManagementViewModel.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs L48-63 (existing signature + body)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md (D-06 spec verbatim)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §4 (refactor snippet ready-to-paste)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md "Code Examples" (DeriveCertificateStatus Refactor)
  </read_first>
  <behavior>
    - Signature: `DateOnly? validUntil, string? certificateType` (sebelumnya `DateTime?`)
    - Return type CertificateStatus enum (unchanged)
    - Body: today = `DateOnly.FromDateTime(DateTime.UtcNow)`, days = `validUntil.Value.DayNumber - today.DayNumber`
    - Semua semantik branch identik existing
  </behavior>
  <action>
    Di `Models/CertificationManagementViewModel.cs` ganti method DeriveCertificateStatus L48-63 PERSIS dengan:

    ```csharp
    public static CertificateStatus DeriveCertificateStatus(DateOnly? validUntil, string? certificateType)
    {
        if (certificateType == "Permanent") return CertificateStatus.Permanent;
        if (validUntil == null) return CertificateStatus.Expired;
        var today = DateOnly.FromDateTime(DateTime.UtcNow);
        var days = validUntil.Value.DayNumber - today.DayNumber;
        if (days < 0) return CertificateStatus.Expired;
        if (days <= 30) return CertificateStatus.AkanExpired;
        return CertificateStatus.Aktif;
    }
    ```

    Conventional commit: `refactor(327-04): DeriveCertificateStatus signature DateOnly? + DayNumber body`.

    **NOTE:** Setelah commit, `dotnet test` di-EXPECT FAIL — test helper Today() masih return DateTime. Task 2 fix helper.
  </action>
  <verify>
    <automated>grep -c "DateOnly? validUntil" Models/CertificationManagementViewModel.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DateOnly? validUntil" Models/CertificationManagementViewModel.cs` returns 1
    - `grep -c "DateTime? validUntil" Models/CertificationManagementViewModel.cs` returns 0
    - `grep -c "DateOnly.FromDateTime(DateTime.UtcNow)" Models/CertificationManagementViewModel.cs` returns 1
    - `grep -c "DayNumber" Models/CertificationManagementViewModel.cs` returns ≥2
    - `grep -c "(validUntil.Value - DateTime" Models/CertificationManagementViewModel.cs` returns 0 (old pattern gone)
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~CertificateStatusTests"` EXPECTED FAIL (compile error helper mismatch) — captured di summary
  </acceptance_criteria>
  <done>
    Signature + body refactored. Test fail signal untuk Task 2 (helper update).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update HcPortal.Tests/CertificateStatusTests.cs helper Today return DateOnly (GREEN step)</name>
  <files>HcPortal.Tests/CertificateStatusTests.cs</files>
  <read_first>
    - HcPortal.Tests/CertificateStatusTests.cs (current state — helper L18-19 `Today` return DateTime)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md §1 (Pattern 4 ready-to-paste — helper return DateOnly)
  </read_first>
  <behavior>
    - Helper `Today(int offset)` return DateOnly (sebelumnya DateTime)
    - Formula: `DateOnly.FromDateTime(DateTime.UtcNow).AddDays(offset)`
    - 8 test method body UNCHANGED (Theory + Fact pass Today(offset) ke DeriveCertificateStatus DateOnly? — compile compat after helper flip)
    - Komentar top file update: "Plan 04 refactor signature DateOnly?"
  </behavior>
  <action>
    Edit `HcPortal.Tests/CertificateStatusTests.cs` — replace helper `Today` method (sekitar L18-19):

    ```csharp
    // SEBELUM:
    private static DateTime Today(int offset) =>
        DateTime.UtcNow.Date.AddDays(offset);

    // SESUDAH:
    private static DateOnly Today(int offset) =>
        DateOnly.FromDateTime(DateTime.UtcNow).AddDays(offset);
    ```

    Update top-of-file komentar:
    ```csharp
    // Unit test SertifikatRow.DeriveCertificateStatus (Phase 327 P04 — Plan 04 GREEN post signature flip DateOnly?).
    // 8 test case boundary coverage (6 Theory + 2 Fact) per CONTEXT.md D-14 + spec §7.5.
    ```

    Conventional commit: `test(327-04): update CertificateStatusTests helper Today return DateOnly (GREEN)`.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo --filter "FullyQualifiedName~CertificateStatusTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private static DateOnly Today" HcPortal.Tests/CertificateStatusTests.cs` returns 1
    - `grep -c "DateOnly.FromDateTime(DateTime.UtcNow).AddDays" HcPortal.Tests/CertificateStatusTests.cs` returns 1
    - `grep -c "private static DateTime Today" HcPortal.Tests/CertificateStatusTests.cs` returns 0
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo --filter "FullyQualifiedName~CertificateStatusTests"` exit 0, "Passed: 8" output
    - Full suite `dotnet test HcPortal.sln --nologo` exit 0, total passed ≥17 (Phase 325 9 + Phase 327 8)
    - Build TIDAK harus full green — Controllers/ + RenewalController L189 etc. EXPECTED tetap gagal (Plan 05 cascade). Test project bisa kompile karena cuma reference Models/.

    **NOTE penting:** `dotnet test HcPortal.sln` mungkin gagal kalau main project (HcPortal.csproj) gagal build. Pakai filter project: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo` only (skip main project). Kalau test project gagal build sendiri karena reference type clash, fallback ke `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` cek compile.
  </acceptance_criteria>
  <done>
    8/8 test GREEN dengan helper Today() return DateOnly. SertifikatRow.DeriveCertificateStatus signature DateOnly? confirmed. Plan 05 next: controller var today sweep + RenewalController DateMaxValue.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure function DeriveCertificateStatus ↔ Controllers/Services consumer | Signature contract berubah — Plan 05 cascade fix call sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-04 | Tampering (compile cascade) | DeriveCertificateStatus call sites Controllers (6+) | accept | Test isolated (cuma reference Models/, bukan Controllers/). Plan 05 cascade fix. Test xUnit jadi safety net regression — kalau Plan 05 sengaja ubah semantik, test akan fail. |
</threat_model>

<verification>
- Models/CertificationManagementViewModel.cs DeriveCertificateStatus signature DateOnly? confirmed via grep.
- HcPortal.Tests/CertificateStatusTests.cs helper Today() return DateOnly confirmed.
- 8/8 test GREEN: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~CertificateStatusTests"`.
- Main project HcPortal.csproj `dotnet build` EXPECTED tetap gagal (Plan 05 cascade — RenewalController, AdminBaseController, GradingService, CMP/CDP var today, etc.).
</verification>

<success_criteria>
SC-3 CONFIRMED: DeriveCertificateStatus 8/8 test pass dengan DateOnly? signature + DayNumber body. Semantik identik baseline Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-04-SUMMARY.md`: signature flipped + test helper updated, 8/8 test GREEN runtime captured (~5s), build main project still failing (expected, Plan 05 next).
</output>
