---
phase: 327-timezone-dateonly-refactor-p04
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - Controllers/CMPController.cs
  - Controllers/HomeController.cs
  - Controllers/RenewalController.cs
  - Controllers/AdminBaseController.cs
  - Services/GradingService.cs
autonomous: true
requirements: []
threats: [T-327-04]
must_haves:
  truths:
    - "13 sites `var today = DateTime.UtcNow.AddHours(7).Date;` rewrite ke `var today = DateOnly.FromDateTime(DateTime.UtcNow);` (Shared Pattern B)"
    - "9 sites `?? DateTime.MaxValue` di RenewalController + AdminBaseController rewrite ke `?? DateOnly.MaxValue` (Shared Pattern C)"
    - "GradingService.cs:465 `(DateTime?)null` cast → `(DateOnly?)null` + L488 `certNow.AddYears(3)` → `DateOnly.FromDateTime(certNow).AddYears(3)` (Shared Pattern E)"
    - "dotnet build SUKSES 0 Error (cascade compile fix complete)"
    - "dotnet test HcPortal.sln full suite GREEN (17+ test, includes Phase 325 + 327 CertificateStatusTests)"
    - "CDPController.cs:2187 `var today = DateTime.Today;` TIDAK disentuh (out-of-scope, coaching deliverable bukan ValidUntil)"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "10 var today DateOnly + query expr compat"
      contains: "var today = DateOnly.FromDateTime(DateTime.UtcNow);"
    - path: "Controllers/HomeController.cs"
      provides: "3 var today DateOnly"
      contains: "var today = DateOnly.FromDateTime(DateTime.UtcNow);"
    - path: "Controllers/RenewalController.cs"
      provides: "7 sites DateOnly.MaxValue fallback + SertifikatRow build compat"
      contains: "?? DateOnly.MaxValue"
    - path: "Controllers/AdminBaseController.cs"
      provides: "2 sites DateOnly.MaxValue + BuildRenewalRowsAsync compat"
      contains: "?? DateOnly.MaxValue"
    - path: "Services/GradingService.cs"
      provides: "ExecuteUpdateAsync cast DateOnly + AddYears(3) cast"
      contains: "(DateOnly?)null"
  key_links:
    - from: "DeriveCertificateStatus(t.ValidUntil, ...)"
      to: "Models/CertificationManagementViewModel.cs (DateOnly? signature post-Plan 04)"
      via: "Plan 04 refactor signature compatible call sites auto"
      pattern: "DeriveCertificateStatus\\(.*ValidUntil"
---

<objective>
Cascade fix 5 file Controllers + Services agar `dotnet build` SUKSES setelah entity+VM flip Plan 02-04. Sweep grep Shared Pattern B (13 var today) + Pattern C (9 ?? DateTime.MaxValue) + Pattern E (GradingService 2 cast).

Purpose: Step 6 dari 11-step Implementation Order. Make project compile-clean SEBELUM ImportTraining (Plan 06) + EF migration (Plan 07).
Output: 5 file touched, build sukses, full suite test GREEN (≥17 test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-CONTEXT.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md
@.planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md
@Controllers/CMPController.cs
@Controllers/HomeController.cs
@Controllers/RenewalController.cs
@Controllers/AdminBaseController.cs
@Services/GradingService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep var today (Shared Pattern B) — CMPController 10 sites + HomeController 3 sites</name>
  <files>Controllers/CMPController.cs, Controllers/HomeController.cs</files>
  <read_first>
    - Controllers/CMPController.cs L2517, L2737, L2821, L2856, L2935, L2975, L3029, L3057, L3105, L3152 (10 sites enumerated RESEARCH Audit 4)
    - Controllers/HomeController.cs L78, L164, L277 (3 sites)
    - Controllers/CDPController.cs L2187 (CONFIRM: NO TOUCH — context MED-03 coaching deliverable bukan ValidUntil)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md Shared Pattern B + §12
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 4 + Pitfall 1
  </read_first>
  <action>
    Sweep grep + replace di 2 file Controllers. Untuk SETIAP occurrence pattern:

    ```csharp
    // SEBELUM:
    var today = DateTime.UtcNow.AddHours(7).Date;

    // SESUDAH:
    var today = DateOnly.FromDateTime(DateTime.UtcNow);
    ```

    **CRITICAL:** Kalau line setelahnya ada `var thirtyDaysFromNow = today.AddDays(30);` atau `var futureDate = today.AddDays(30);` — BIARKAN apa adanya. `DateOnly.AddDays(int)` return DateOnly native, compat.

    **CMPController.cs**: 10 occurrence di L2517, L2737, L2821, L2856, L2935, L2975, L3029, L3057, L3105, L3152.

    **HomeController.cs**: 3 occurrence di L78, L164, L277.

    **JANGAN sentuh `CDPController.cs:2187`** — `var today = DateTime.Today;` context MED-03 coaching deliverable, bukan ValidUntil (RESEARCH Audit 4 confirm).

    Conventional commit: `refactor(327-05): sweep 13 var today DateOnly (CMP 10 + Home 3)`.
  </action>
  <verify>
    <automated>grep -c "var today = DateOnly.FromDateTime(DateTime.UtcNow);" Controllers/CMPController.cs Controllers/HomeController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var today = DateOnly.FromDateTime(DateTime.UtcNow);" Controllers/CMPController.cs` returns 10
    - `grep -c "var today = DateOnly.FromDateTime(DateTime.UtcNow);" Controllers/HomeController.cs` returns 3
    - `grep -c "var today = DateTime.UtcNow.AddHours(7).Date;" Controllers/CMPController.cs` returns 0
    - `grep -c "var today = DateTime.UtcNow.AddHours(7).Date;" Controllers/HomeController.cs` returns 0
    - `grep -c "var today = DateTime.Today;" Controllers/CDPController.cs` returns 1 (out-of-scope preserve)
  </acceptance_criteria>
  <done>
    13 var today sites rewritten DateOnly. CDPController L2187 preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep ?? DateOnly.MaxValue (Shared Pattern C) + RenewalController + AdminBaseController</name>
  <files>Controllers/RenewalController.cs, Controllers/AdminBaseController.cs</files>
  <read_first>
    - Controllers/RenewalController.cs L189, L198, L201, L263, L281, L289, L339 (7 sites — RESEARCH Audit 4 Pitfall 2)
    - Controllers/AdminBaseController.cs L131, L201 (2 sites)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md Shared Pattern C + §13-14 + §16
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md Audit 4 Pitfall 2 + Pitfall 6
  </read_first>
  <action>
    Sweep replace di 2 file:

    ```csharp
    // SEBELUM:
    g.OrderByDescending(c => c.ValidUntil ?? DateTime.MaxValue).ToList()

    // SESUDAH:
    g.OrderByDescending(c => c.ValidUntil ?? DateOnly.MaxValue).ToList()
    ```

    **CRITICAL untuk presence DateTime.MaxValue di luar konteks ValidUntil:** JANGAN auto-replace blind. Verify context manual setiap site. Per RESEARCH Pitfall 2:
    - RenewalController.cs L189, L198, L201, L263, L281, L289, L339 — ALL ValidUntil context, replace
    - AdminBaseController.cs L131, L201 — ValidUntil context BuildRenewalRowsAsync, replace

    Conventional commit: `refactor(327-05): sweep 9 ?? DateOnly.MaxValue (Renewal 7 + AdminBase 2)`.
  </action>
  <verify>
    <automated>grep -c "?? DateOnly.MaxValue" Controllers/RenewalController.cs Controllers/AdminBaseController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "?? DateOnly.MaxValue" Controllers/RenewalController.cs` returns 7
    - `grep -c "?? DateOnly.MaxValue" Controllers/AdminBaseController.cs` returns 2
    - `grep -c "?? DateTime.MaxValue" Controllers/RenewalController.cs` returns 0 (semua ValidUntil context flipped)
    - `grep -c "?? DateTime.MaxValue" Controllers/AdminBaseController.cs` returns 0
  </acceptance_criteria>
  <done>
    9 sites Shared Pattern C applied. RenewalController + AdminBaseController OrderByDescending fallback compat.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix GradingService.cs SetProperty cast (Shared Pattern E) + Build full suite test</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs L465, L476-493 (2 site fix per RESEARCH Audit 6 + Pitfall 6)
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-PATTERNS.md Shared Pattern E + §17
    - .planning/phases/327-timezone-dateonly-refactor-p04/327-RESEARCH.md "Code Examples" GradingService SetProperty Fix
  </read_first>
  <action>
    Di `Services/GradingService.cs`:

    **Edit L465** (RegradeAfterEditAsync Pass→Fail revoke):
    ```csharp
    // SEBELUM:
    .SetProperty(r => r.ValidUntil, (DateTime?)null)
    // SESUDAH:
    .SetProperty(r => r.ValidUntil, (DateOnly?)null)
    ```

    **Edit L488** (Fail→Pass cert generate):
    ```csharp
    // SEBELUM:
    var validUntil = certNow.AddYears(3);  // certNow = DateTime.Now (L476)
    // SESUDAH:
    var validUntil = DateOnly.FromDateTime(certNow).AddYears(3);
    ```

    **L476 `certNow = DateTime.Now`** + **L493 `.SetProperty(r => r.ValidUntil, validUntil)`** TIDAK perlu sentuh — auto-compat (validUntil sekarang DateOnly via L488 fix).

    Conventional commit: `refactor(327-05): GradingService SetProperty cast DateOnly + AddYears wrap`.

    **POST-TASK:** Jalan FULL BUILD + TEST suite:
    ```bash
    dotnet build HcPortal.sln --nologo
    dotnet test HcPortal.sln --nologo
    ```

    Build harus 0 Error. Test ≥17 passed (Phase 325 9 + Phase 327 8 CertificateStatusTests).

    **Kalau ada compile error sisa** (e.g., view yang reference DateTime.Now.Date dengan ValidUntil compare): grep + fix per case sesuai pattern. RESEARCH Audit 4 sudah enumerate, kemungkinan zero sisa. Tapi kalau ada, fix dengan idiom DayNumber + DateOnly sebelum lanjut commit.
  </action>
  <verify>
    <automated>dotnet build HcPortal.sln --nologo && dotnet test HcPortal.sln --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "(DateOnly?)null" Services/GradingService.cs` returns ≥1
    - `grep -c "DateOnly.FromDateTime(certNow).AddYears(3)" Services/GradingService.cs` returns 1
    - `grep -c "(DateTime?)null" Services/GradingService.cs` returns 0 (di context ValidUntil; context lain mungkin ada — manual review)
    - `dotnet build HcPortal.sln --nologo` exit 0, "0 Error" output
    - `dotnet test HcPortal.sln --nologo` exit 0, total passed ≥17 ("Passed: 17" atau lebih)
    - `dotnet test --filter "FullyQualifiedName~CertificateStatusTests"` exit 0, "Passed: 8"
  </acceptance_criteria>
  <done>
    GradingService cast Pattern E applied. Project BUILD SUKSES 0 Error. Full suite GREEN ≥17 test. Plan 06 (ImportTraining + smoke) + Plan 07 (EF migration) unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller var today ↔ EF query expr ValidUntil | EF Core 8 native DateOnly LINQ translation SQL date — type compat post-flip |
| GradingService ExecuteUpdateAsync SetProperty ↔ Entity DateOnly? | Explicit cast `(DateOnly?)null` agar EF reasoning correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-04 | Tampering (compile cascade complete) | All Controllers/Services consumer DeriveCertificateStatus + ValidUntil | mitigate | Sweep grep Shared Pattern B + C + E. Build + full test suite GREEN gate sebelum Plan 06. Kalau false-positive (DateTime.MaxValue context lain), per-site verify manual. |
</threat_model>

<verification>
- `dotnet build HcPortal.sln --nologo` 0 Error.
- `dotnet test HcPortal.sln --nologo` total passed ≥17.
- `dotnet test --filter "FullyQualifiedName~CertificateStatusTests"` 8/8 GREEN (regression sanity check Plan 04).
- Grep counts confirm Shared Pattern B (13) + C (9) + E (2 GradingService) applied.
</verification>

<success_criteria>
Cascade compile fix complete. Build SUKSES, test GREEN. ImportTraining (Plan 06) + EF migration (Plan 07) ready.
</success_criteria>

<output>
After completion, create `.planning/phases/327-timezone-dateonly-refactor-p04/327-05-SUMMARY.md`: 5 file touched (CMP 10 var today, Home 3 var today, Renewal 7 MaxValue, AdminBase 2 MaxValue, GradingService 2 cast = 24 site total Shared Pattern B+C+E), build + test result captured.
</output>
