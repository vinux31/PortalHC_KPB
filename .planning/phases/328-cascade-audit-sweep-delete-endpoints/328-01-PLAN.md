---
phase: 328-cascade-audit-sweep-delete-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "RESEARCH.md row count di section 3 EQUALS `grep -E \"public async.*Delete\\w+\\(\" Controllers/*.cs Services/*.cs | wc -l` output"
    - "Setiap row di section 3 punya 7-dim cells (D1..D7) terisi ✅/❌/⚠️ + 1-line evidence `file:line` ref"
    - "Setiap row di section 3 punya severity tag (HIGH / MED / LOW / NONE)"
    - "Section 4 (HIGH Findings) berisi ≥1 finding dan WAJIB include DeleteTraining (TrainingAdminController.cs:527-548) renewal chain bug"
    - "Section 7 (Out-of-Scope) eksplisit list 5 item: Identity UserManager.DeleteAsync, soft-delete IsDeleted, concurrency/optimistic locking, idempotency, stored procedure cascade"
    - "Section 8 (Remediation Pattern Template) berisi canonical Phase 323 fix snippet (RemoveRange pattern) dari AssessmentAdminController.cs post-commit f1849367"
    - "Final commit hash RESEARCH.md di-append ke v19.0 ROADMAP entry Phase 328"
  artifacts:
    - path: ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"
      provides: "Audit deliverable — 9 section markdown report dengan inventory + audit table + HIGH/MED/LOW findings + out-of-scope + remediation template + recommended next phases"
      contains: "## 1. Methodology, ## 2. Endpoint Inventory, ## 3. Audit Table, ## 4. HIGH Findings, ## 5. MED Findings, ## 6. LOW Findings, ## 7. Out-of-Scope, ## 8. Remediation Pattern Template, ## 9. Recommended Next Phases"
  key_links:
    - from: ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md section 3"
      to: "Controllers/*.cs + Services/*.cs Delete* methods"
      via: "file:line evidence ref per dimension cell"
      pattern: "(Controllers|Services)/[A-Za-z]+\\.cs:\\d+"
    - from: ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md section 8"
      to: "Controllers/AssessmentAdminController.cs (post-commit f1849367)"
      via: "verbatim code snippet copy + reference link"
      pattern: "RemoveRange.*AssessmentEditLogs"
    - from: ".planning/ROADMAP.md Phase 328 entry"
      to: ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"
      via: "commit hash appended after audit ship"
      pattern: "RESEARCH commit:"
---

<objective>
Lakukan audit cascade-safety read-only terhadap SEMUA endpoint `Delete*` di `Controllers/*.cs` + `Services/*.cs`, grade tiap endpoint via 7-dimension checklist (D-03), severity-tag tiap baris (HIGH/MED/LOW/NONE per D-04), dan tulis hasilnya ke single deliverable markdown: `.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md`.

**Purpose:** Tutup deferred follow-up dari Phase 323 (`323-CONTEXT.md:122`). Audit ini jadi input untuk keputusan user post-audit terkait phase fix berikutnya — TIDAK auto-spawn fix phase (per D-10).

**Output:** Satu file markdown 9-section. NO code change. NO migration. NO test. NO Playwright. NO IT_NOTIFY.

**Critical constraint (D-01):** Phase ini AUDIT-ONLY. `files_modified` hanya boleh berisi `328-RESEARCH.md` dan (closing task) `ROADMAP.md`. Setiap task yang memodifikasi `.cs` / `.cshtml` / `.json` / `.sql` / `Migrations/*` adalah PELANGGARAN scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-CONTEXT.md
@docs/superpowers/specs/2026-05-27-v19.0-cascade-audit-sweep-design.md
@.planning/phases/323-fix-cascade-bug-assessmenteditlogs-di-3-endpoint-delete-asse/323-CONTEXT.md

<interfaces>
<!-- Pre-audit enumeration sudah dijalankan saat planning. Executor TIDAK perlu re-discover. -->
<!-- Output `grep -rEn "public async.*Delete\w+\(" Controllers/*.cs Services/*.cs` per 2026-05-27: -->

Controllers/AssessmentAdminController.cs:462    DeleteCategory(int id)
Controllers/AssessmentAdminController.cs:2011   DeleteAssessment(int id)                       [gold standard — Phase 323]
Controllers/AssessmentAdminController.cs:2175   DeleteAssessmentGroup(int id)                  [gold standard — Phase 323]
Controllers/AssessmentAdminController.cs:2335   DeletePrePostGroup(int linkedGroupId)          [gold standard — Phase 323]
Controllers/AssessmentAdminController.cs:3887   GetDeleteImpact(int id, string type)           [GET-only preview, BUKAN actual delete — periksa apakah masuk scope]
Controllers/AssessmentAdminController.cs:5014   DeletePackage(int packageId)
Controllers/AssessmentAdminController.cs:5926   DeleteQuestion(int questionId, int packageId)
Controllers/CDPController.cs:2433               DeleteCoachingSession(int id)
Controllers/CoachMappingController.cs:1114      CoachCoacheeMappingDeletePreview(int id)       [preview-only, BUKAN actual delete — periksa]
Controllers/DocumentAdminController.cs:283      DeleteBagian(int id, bool confirmed = false)
Controllers/OrganizationController.cs:357       DeleteOrganizationUnit(int id)
Controllers/ProtonDataController.cs:559         SilabusDeletePreview(int deliverableId)        [preview-only]
Controllers/ProtonDataController.cs:571         SubKompetensiDeletePreview(int subKompetensiId)[preview-only]
Controllers/ProtonDataController.cs:586         KompetensiDeletePreview(int kompetensiId)      [preview-only]
Controllers/ProtonDataController.cs:1516        DeleteKompetensi([FromBody] KompetensiDeleteRequest req)
Controllers/TrainingAdminController.cs:527      DeleteTraining(int id)                         [PRE-CONFIRMED HIGH per D-06]
Controllers/TrainingAdminController.cs:736      DeleteManualAssessment(int id)                 [PRE-CONFIRMED HIGH per D-06]
Controllers/WorkerController.cs:487             DeleteWorker(string id)
Services/NotificationService.cs:270             DeleteAsync(int notificationId, string userId)

Raw total: 19 match. Final inventory count ditentukan saat Task 1 (executor re-run untuk freshness + classify Preview-only vs actual mutator).
</interfaces>

<dimension_checklist>
<!-- D-03 locked. Setiap row di Audit Table section 3 evaluasi 7 dimensi: -->

D1 — Cascade FK risk:    All OnDelete(Restrict|NoAction) FK pointing ke target entity handled app-level (null-clear / pre-delete RemoveRange)
D2 — File-DB atomicity:  DB SaveChangesAsync SEBELUM file System.IO.File.Delete, OR transaction wrap dengan rollback file
D3 — AuditLog presence:  `_auditLog.LogAsync(...)` dengan action "Delete" + entity name + ID
D4 — Role/auth check:    `[Authorize(Roles = "...")]` attribute present dengan role tepat (Admin / HC / etc)
D5 — Renewal chain:      Untuk TrainingRecord/AssessmentSession — child rows me-reference target via RenewsTrainingId/RenewsSessionId di-null-clear SEBELUM Remove
D6 — Error handling:     `try/catch (DbUpdateException)` returning friendly `TempData["Error"]`
D7 — Transaction wrap:   `BeginTransactionAsync()` untuk multi-step deletes (file + DB + cascade pre-clear)

Per cell: ✅ (pass) / ❌ (fail) / ⚠️ (partial) / N/A (tidak applicable, eksplisit alasan) — dengan 1-line evidence `file.cs:LINE` ref.
</dimension_checklist>

<severity_rubric>
<!-- D-04 locked. -->
HIGH — Any of D1, D2, D5 fail.    Justification: data loss possible (FK violation 500, orphan file/row).
MED  — Any of D3, D4, D6 fail.    Justification: UX degradation / audit gap / authorization gap.
LOW  — Only D7 fail.              Justification: hygiene only, rarely user-visible.
NONE — All 7 dim ✅ atau N/A.       Used for gold-standard endpoint (Phase 323 post-fix).
</severity_rubric>

<gold_standard>
<!-- D-05 reference. Phase 323 final di-ship via commit f1849367 (PUSHED origin/main). -->
<!-- Pattern AssessmentAdminController.DeleteAssessment (line 2011-2173 approximate post-fix): -->

1. [Authorize(Roles = "Admin,HC")]                     → D4 ✅
2. Try { ... } catch (DbUpdateException) { ... }       → D6 ✅
3. using var tx = await _context.Database.BeginTransactionAsync()  → D7 ✅
4. Pre-load deps: AssessmentSession + AssessmentAnswers + AssessmentEditLogs + child sessions where RenewsSessionId == id  → D1+D5 prep
5. Null-clear child.RenewsSessionId  → D5 ✅
6. _context.AssessmentEditLogs.RemoveRange(...)         → D1 ✅ (Phase 323 fix for AssessmentEditLog Restrict FK)
7. _context.AssessmentAnswers.RemoveRange(...)          → D1 ✅
8. _context.AssessmentSessions.Remove(...)              → D1 ✅
9. await _context.SaveChangesAsync()                    → D2 ✅ (DB first)
10. await tx.CommitAsync()                              → D7 ✅
11. await _auditLog.LogAsync("Delete", "AssessmentSession", id, ...)  → D3 ✅

Section 8 dari RESEARCH.md WAJIB copy snippet ini verbatim sebagai template remediation.
</gold_standard>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap RESEARCH.md skeleton + run inventory grep</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-CONTEXT.md (D-01..D-10 locked decisions)
    - docs/superpowers/specs/2026-05-27-v19.0-cascade-audit-sweep-design.md (full design spec)
  </read_first>
  <action>
    Create `.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md` dengan struktur 9-section skeleton EXACT per D-07:

    ```markdown
    # Phase 328 — Cascade Audit Sweep RESEARCH

    **Phase:** 328-cascade-audit-sweep-delete-endpoints
    **Date:** 2026-05-27
    **Type:** Audit-only (no code change)
    **Spec:** docs/superpowers/specs/2026-05-27-v19.0-cascade-audit-sweep-design.md (commit `02f620be`)

    ## 1. Methodology
    [Filled di task ini]

    ## 2. Endpoint Inventory
    [Filled di task ini]

    ## 3. Audit Table
    [Skeleton table header only di task ini; rows filled task 3-6]

    ## 4. HIGH Findings
    [Filled task 7]

    ## 5. MED Findings
    [Filled task 8]

    ## 6. LOW Findings
    [Filled task 8]

    ## 7. Out-of-Scope Statement
    [Filled task 9]

    ## 8. Remediation Pattern Template (Phase 323 Gold Standard)
    [Filled task 9]

    ## 9. Recommended Next Phases (Proposal Only)
    [Filled task 9]
    ```

    Lalu isi:

    **Section 1 (Methodology):** Tulis 1 paragraf Bahasa Indonesia + bullet list teknis: (a) enumeration pattern `public async.*Delete\w+\(` against Controllers/*.cs + Services/*.cs, (b) per-endpoint Read method body + cross-ref `Data/ApplicationDbContext.cs` untuk Dim 1 FK risk, (c) compare against Phase 323 gold standard `AssessmentAdminController.DeleteAssessment` post-commit `f1849367`, (d) 7-dim grading per D-03, (e) severity rubric per D-04.

    **Section 2 (Endpoint Inventory):**
    Jalankan: `grep -rEn "public async.*Delete\w+\(" Controllers/*.cs Services/*.cs > /tmp/328-inventory.txt && wc -l /tmp/328-inventory.txt`

    Embed raw output di code block. Catat total count. Klasifikasi PREVIEW-only methods (`*DeletePreview`, `GetDeleteImpact`) — methods ini secara nama match pattern tapi TIDAK melakukan DB mutation (read-only impact preview). Bedakan dengan tabel:
    - Tabel 2A: Actual delete mutators (target audit utama)
    - Tabel 2B: Preview-only methods (out-of-audit untuk 7-dim grading, tapi dicatat untuk completeness — beri row di section 3 dengan severity NONE + note "preview-only, no mutation")

    Kalau executor menemukan endpoint yang TIDAK ada di `<interfaces>` block di plan (mis. ada controller baru post-2026-05-27), TAMBAHKAN ke inventory — jangan diam-diam skip.
  </action>
  <verify>
    <automated>test -f ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md" && grep -c "^## " ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md" | awk '$1 >= 9 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md` exists.
    - File berisi minimal 9 H2 heading (`## 1.` .. `## 9.`).
    - Section 2 berisi raw grep output (lihat code fence) + total count integer.
    - Section 1 berisi paragraf methodology Bahasa Indonesia + bullet teknis.
    - Verify: `grep -c "^## " 328-RESEARCH.md` returns ≥ 9.
    - Verify: `grep -E "public async.*Delete" 328-RESEARCH.md` returns ≥ 15 lines (raw grep output embedded).
  </acceptance_criteria>
  <done>RESEARCH.md skeleton ada dengan 9 H2 section, Section 1 + 2 fully populated, total inventory count tercatat.</done>
</task>

<task type="auto">
  <name>Task 2: Build Audit Table skeleton (section 3) — header + empty row per endpoint</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md (output Task 1, Section 2 inventory)
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-CONTEXT.md (D-03 7-dim checklist)
  </read_first>
  <action>
    Update section 3 (Audit Table) dengan markdown table struktur fixed-column:

    ```
    | # | Endpoint | File:Line | D1 FK | D2 Atomicity | D3 Audit | D4 Auth | D5 Renewal | D6 Error | D7 Tx | Severity | Remediation Outline |
    |---|----------|-----------|-------|--------------|----------|---------|------------|----------|-------|----------|---------------------|
    | 1 | DeleteCategory | AssessmentAdminController.cs:462 | TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD |
    | 2 | DeleteAssessment | AssessmentAdminController.cs:2011 | TBD | ... | ... | ... | ... | ... | ... | TBD | TBD |
    ...
    ```

    **Setiap baris pada tahap ini boleh punya cell value `TBD`** — task 3-6 akan fill in cell values dengan ✅/❌/⚠️ + evidence ref `file.cs:LINE`.

    **Grouping order (Claude's discretion per D-decisions):** GROUP BY controller file, ordering follows inventory raw order. Preview-only methods (Tabel 2B dari Task 1) DIPISAH ke section 3B di bawah tabel utama dengan note "Preview-only methods — listed for completeness, no 7-dim grading needed (no mutation)".

    Cell convention legend (tulis di atas tabel):
    - ✅ pass
    - ❌ fail (kontribusi severity)
    - ⚠️ partial (kontribusi severity, dengan note)
    - N/A not applicable (dengan 1-line alasan, BUKAN penalty)
    - Setiap non-N/A cell HARUS akhirnya berisi evidence ref `file.cs:LINE` (bisa pakai `(L527)` untuk inline kompaksi).
  </action>
  <verify>
    <automated>grep -cE "^\| [0-9]+ \|" ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md" | awk '$1 >= 10 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Section 3 berisi markdown table dengan header row 13-column (sesuai action).
    - Jumlah data row (numeric `| 1 |`, `| 2 |`, ...) EQUALS inventory count Section 2 (actual mutators; preview-only di sub-section 3B).
    - Setiap row punya kolom Endpoint + File:Line terisi (BUKAN TBD).
    - Cell D1..D7 + Severity + Remediation BOLEH `TBD` di task ini.
    - Legend ✅/❌/⚠️/N/A dijelaskan di atas tabel.
    - Verify: `grep -c "^| [0-9]" 328-RESEARCH.md` returns count matching inventory total (mutators).
  </acceptance_criteria>
  <done>Section 3 audit table skeleton ada dengan 1 row per endpoint mutator, header + legend lengkap, cell D1-D7+severity boleh TBD.</done>
</task>

<task type="auto">
  <name>Task 3: Audit TrainingAdminController — fill row DeleteTraining + DeleteManualAssessment (PRE-CONFIRMED HIGH)</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - Controllers/TrainingAdminController.cs (lines 520-760 — covers both methods + helpers)
    - Data/ApplicationDbContext.cs (lines 140-246 — TrainingRecord FK + renewal chain definitions; lines 157-165 + 220-228 = RenewsTrainingId NoAction per CONTEXT canonical refs)
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-CONTEXT.md (D-06 pre-confirmed evidence)
  </read_first>
  <action>
    **Step 3.1** — Read `Controllers/TrainingAdminController.cs` lines 520-760. Identify method body bounds:
    - `DeleteTraining(int id)` starts ~L527. Find closing brace.
    - `DeleteManualAssessment(int id)` starts ~L736. Find closing brace.

    **Step 3.2** — Read `Data/ApplicationDbContext.cs` lines 140-246. Catat:
    - FK pointing ke `TrainingRecord` (entity yang di-delete): RenewsTrainingId NoAction (self-ref renewal chain), dan lainnya (e.g., dari Sertifikat, dll).
    - Untuk DeleteManualAssessment: target entity = ManualAssessment? AssessmentSession? Confirm dari method body.

    **Step 3.3** — Fill row DeleteTraining di section 3 dengan grading:
    - D1 (FK): ❌ jika method TIDAK null-clear child TR yang RenewsTrainingId == id sebelum Remove (RESTRICT FK akan throw). Evidence: `TrainingAdminController.cs:LXXX` (line `_context.Remove(training)` tanpa pre-clear).
    - D2 (Atomicity): ❌ jika `System.IO.File.Delete(filePath)` dipanggil SEBELUM `SaveChangesAsync()` (orphan file risk on DB failure), atau ⚠️ jika tidak ada try/catch wrap.
    - D3 (Audit): ✅/❌ tergantung apakah `_auditLog.LogAsync` ada — catat line.
    - D4 (Auth): ✅/❌ — cek attribute `[Authorize(Roles=...)]` di atas method (atau class-level).
    - D5 (Renewal chain): ❌ confirmed (D-06 pre-confirmed). Evidence: `TrainingAdminController.cs:527-548` no null-clear of `RenewsTrainingId`. Cross-ref `ApplicationDbContext.cs:157-165`.
    - D6 (Error): ✅/❌ tergantung try/catch DbUpdateException.
    - D7 (Tx): ❌ kalau tidak ada `BeginTransactionAsync`.
    - Severity: **HIGH** (D1/D2/D5 fail per D-06).
    - Remediation Outline: 1 kalimat "Pre-clear `RenewsTrainingId` di child TR, wrap `BeginTransactionAsync`, DB-save sebelum file delete — pattern Phase 323."

    **Step 3.4** — Fill row DeleteManualAssessment dengan grading paralel. Severity: HIGH per D-06.

    **PENTING:** Setiap cell ❌/⚠️ HARUS punya 1-line evidence ref `(TrainingAdminController.cs:LXXX)` inline di cell value. Contoh:
    `❌ (L545 — System.IO.File.Delete dipanggil sebelum SaveChangesAsync)`

    Jika method ternyata sudah punya pattern lebih baik dari D-06 expectation (e.g., sudah ada tx wrap), CATAT akurat — TIDAK pakai severity HIGH "demi memenuhi acceptance #4". Acceptance #4 hanya require ≥1 HIGH total; kalau DeleteTraining ternyata NOT HIGH, ada methodology gap yang harus diraise sebagai ⚠️ note untuk reviewer.
  </action>
  <verify>
    <automated>grep -cE "(DeleteTraining|DeleteManualAssessment).*HIGH" ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md" | awk '$1 >= 2 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Row DeleteTraining di section 3 TIDAK lagi punya cell TBD (semua D1-D7 + severity + remediation terisi).
    - Row DeleteManualAssessment di section 3 TIDAK lagi punya cell TBD.
    - Severity column kedua row = "HIGH".
    - Setiap cell ❌/⚠️ punya inline evidence ref `(file.cs:LINE)` atau `(LXXX)`.
    - Remediation Outline kedua row reference Phase 323 pattern.
    - Verify: `grep -c "TrainingAdminController.cs:" 328-RESEARCH.md` returns ≥ 4 (multiple evidence refs).
  </acceptance_criteria>
  <done>Section 3 dua row TrainingAdmin terisi penuh dengan grading HIGH + evidence ref + remediation outline.</done>
</task>

<task type="auto">
  <name>Task 4: Audit AssessmentAdminController — fill rows (gold standard + lainnya)</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (focus ranges: 460-490 DeleteCategory, 2000-2400 Delete* trio gold standard, 5010-5060 DeletePackage, 5920-5980 DeleteQuestion)
    - Data/ApplicationDbContext.cs (FK definitions for Category / AssessmentSession / Package / Question)
  </read_first>
  <action>
    Audit dan fill row untuk 6 endpoint di file ini:

    1. **DeleteCategory (L462)** — periksa: apakah Category punya child Assessment/Package? FK definition di ApplicationDbContext.cs. D1 grading: jika cascade tidak handled → ❌. D3 audit log: cek `_auditLog.LogAsync`. D4 auth: cek attribute. D7 tx wrap.

    2. **DeleteAssessment (L2011)** — POST-PHASE-323 gold standard. Confirm pattern via Read:
       - `[Authorize(Roles = "Admin,HC")]` → D4 ✅
       - `using var tx = _context.Database.BeginTransactionAsync()` → D7 ✅
       - `RemoveRange(AssessmentEditLogs)` block (Phase 323 fix) → D1 ✅
       - `RemoveRange(AssessmentAnswers)` → D1 ✅
       - `SaveChangesAsync` sebelum `tx.CommitAsync` → D2 ✅
       - `_auditLog.LogAsync("Delete", ...)` → D3 ✅
       - try/catch DbUpdateException → D6 ✅
       - Null-clear `RenewsSessionId` di child sessions → D5 ✅ (kalau ada; kalau tidak ada cek apakah AssessmentSession bisa di-renew dari D-03 dim 5 perspective)
       - Severity: **NONE** (gold standard reference).
       - Remediation Outline: "N/A — reference pattern untuk section 8."

    3. **DeleteAssessmentGroup (L2175)** — paralel dengan DeleteAssessment, gold standard. Severity NONE.

    4. **DeletePrePostGroup (L2335)** — paralel. Severity NONE.

    5. **DeletePackage (L5014)** — audit fresh. Periksa FK Package → Question/AssessmentSession. Cascade handling?

    6. **DeleteQuestion (L5926)** — audit fresh. Periksa FK Question → AssessmentAnswer. Cascade?

    Untuk endpoint preview (`GetDeleteImpact` L3887) — TIDAK perlu 7-dim grading karena read-only. Tambah row di section 3B "Preview-only methods" dengan severity NONE + 1-line note "Read-only impact preview, no DB mutation".

    Setiap cell ❌/⚠️ inline evidence ref `(AssessmentAdminController.cs:LXXX)`.
  </action>
  <verify>
    <automated>grep -cE "AssessmentAdminController" ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md" | awk '$1 >= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - 6 row AssessmentAdminController di section 3 (DeleteCategory, DeleteAssessment, DeleteAssessmentGroup, DeletePrePostGroup, DeletePackage, DeleteQuestion) tidak ada cell TBD.
    - 3 row gold standard (DeleteAssessment / DeleteAssessmentGroup / DeletePrePostGroup) severity = NONE atau dokumentasi eksplisit kenapa NOT none.
    - `GetDeleteImpact` masuk section 3B (preview-only).
    - Setiap cell ❌/⚠️ punya evidence ref `(file.cs:LINE)`.
    - Verify: `grep -c "AssessmentAdminController.cs:" 328-RESEARCH.md` returns ≥ 8 (multiple evidence refs across 6 rows).
  </acceptance_criteria>
  <done>6 row AssessmentAdminController terisi penuh dengan grading + evidence + remediation outline.</done>
</task>

<task type="auto">
  <name>Task 5: Audit remaining Controllers — CDP, CoachMapping, DocumentAdmin, Organization, ProtonData, Worker</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - Controllers/CDPController.cs (DeleteCoachingSession ~L2433, baca L2400-2500)
    - Controllers/CoachMappingController.cs (CoachCoacheeMappingDeletePreview ~L1114 — preview-only, classify)
    - Controllers/DocumentAdminController.cs (DeleteBagian ~L283, baca L270-340)
    - Controllers/OrganizationController.cs (DeleteOrganizationUnit ~L357, baca L340-420)
    - Controllers/ProtonDataController.cs (DeleteKompetensi ~L1516, baca L1500-1600; SilabusDeletePreview/SubKompetensiDeletePreview/KompetensiDeletePreview L559-600 preview-only)
    - Controllers/WorkerController.cs (DeleteWorker ~L487, baca L470-560)
    - Data/ApplicationDbContext.cs (FK definitions per entity audit target)
  </read_first>
  <action>
    Untuk SETIAP endpoint actual mutator (skip preview-only — masuk section 3B):

    **DeleteCoachingSession (CDPController.cs:2433):**
    - Read method body. Identify entity target (CoachingSession) + FK in/out.
    - 7-dim grading. Severity per D-04 rubric.

    **DeleteBagian (DocumentAdminController.cs:283):**
    - Bagian = Document/Bagian entity? FK ke child Document?
    - Note signature `(int id, bool confirmed = false)` — apakah ada 2-step confirmation pattern? Boleh masuk evidence cell.
    - 7-dim grading.

    **DeleteOrganizationUnit (OrganizationController.cs:357):**
    - Cross-ref ApplicationDbContext.cs untuk OrganizationUnit FK (Worker.UnitId? Bagian.UnitId?).
    - Common pitfall: self-referencing Parent/Child unit (tree structure) — D1 critical.
    - 7-dim grading.

    **DeleteKompetensi (ProtonDataController.cs:1516):**
    - Body signature `[FromBody] KompetensiDeleteRequest req` — JSON API endpoint.
    - FK SubKompetensi/Silabus → Kompetensi cascade?
    - 7-dim grading.

    **DeleteWorker (WorkerController.cs:487):**
    - Worker = AspNetUsers? Atau ada Worker table sendiri?
    - FK massive (AssessmentSession.UserId, TrainingRecord.UserId, dll). D1 critical.
    - Apakah pakai `UserManager.DeleteAsync` (out-of-scope D-02) atau direct `_context.Remove`?
    - 7-dim grading.

    **Preview-only methods** (CoachCoacheeMappingDeletePreview, 3× Proton*DeletePreview): masuk section 3B dengan note "Read-only preview".

    Setiap cell evidence ref `(file.cs:LINE)`.
  </action>
  <verify>
    <automated>bash -c 'F=".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"; for ctrl in CDPController DocumentAdminController OrganizationController ProtonDataController WorkerController; do grep -q "$ctrl" "$F" || { echo "MISSING $ctrl"; exit 1; }; done; exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - Row untuk DeleteCoachingSession, DeleteBagian, DeleteOrganizationUnit, DeleteKompetensi, DeleteWorker semua terisi penuh di section 3 (no TBD).
    - Severity column terisi (HIGH/MED/LOW/NONE) per D-04.
    - Preview-only methods (4 buah) ada di section 3B.
    - Setiap row punya minimal 1 evidence ref `(file.cs:LINE)` di cell ❌/⚠️.
    - Verify: 5 controller file name dari list muncul di RESEARCH.md (grep loop di verify block).
  </acceptance_criteria>
  <done>5 controller mutator endpoint + 4 preview-only terisi di section 3/3B.</done>
</task>

<task type="auto">
  <name>Task 6: Audit Services — NotificationService + scan for _context.Remove call sites</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - Services/NotificationService.cs (DeleteAsync ~L270, baca L250-320)
    - Services/* (Glob check apakah ada service lain dengan `_context.Remove` / `RemoveRange` call site di luar pattern `public async.*Delete\w+\(`)
    - Data/ApplicationDbContext.cs (FK Notification entity)
  </read_first>
  <action>
    **Step 6.1** — Audit `NotificationService.DeleteAsync(int notificationId, string userId)`:
    - Signature: returns `Task<bool>`. Note: ini SERVICE layer (bukan controller), jadi D4 (Authorize attribute) N/A — auth dilakukan caller. Catat sebagai N/A dengan alasan.
    - Verify ownership check via `userId` param (anti-IDOR) — kalau ada ✅ note di Remediation outline.
    - 7-dim grading dengan D4=N/A.

    **Step 6.2** — Cross-scan untuk `_context.Remove` / `RemoveRange` di Services/* yang TIDAK match `public async.*Delete\w+\(`:

    ```bash
    grep -rEn "_context\.(Remove|RemoveRange)" Services/*.cs
    ```

    Untuk setiap hit, periksa apakah ada di:
    - (a) Method `Delete*` yang sudah audited → skip (sudah masuk inventory).
    - (b) Method non-`Delete*` (e.g., `UpdateXxx` yang melakukan delete sebagai bagian update flow) → CATAT di section 2C "Indirect Delete Call Sites" (sub-section baru di akhir section 2). Format:

      ```
      ## 2C. Indirect Delete Call Sites (RemoveRange di non-Delete methods)
      | File:Line | Calling Method | Target Entity | 7-dim Audit Status |
      |-----------|----------------|---------------|---------------------|
      | GradingService.cs:LXX | RegradeAfterEditAsync | TrainingRecord | Out-of-audit-scope (delete sebagai side-effect, bukan endpoint primary) |
      ```

      Severity tidak di-grade penuh — note di Remediation Outline "Audit follow-up bila endpoint primary di-promote".

    **Step 6.3** — Fill row NotificationService.DeleteAsync di section 3 dengan grading + evidence ref `(NotificationService.cs:LXXX)`.
  </action>
  <verify>
    <automated>grep -q "NotificationService" ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md" && grep -c "^| " ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md" | awk '$1 >= 15 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Row NotificationService.DeleteAsync di section 3 tidak ada TBD.
    - Section 2C "Indirect Delete Call Sites" exists kalau ada hit; kalau tidak ada hit, eksplisit tulis "No indirect call sites found di Services/* outside audited methods."
    - D4 cell untuk NotificationService = N/A dengan alasan "Service layer, auth di caller controller".
    - Total table row di section 3 (mutators only, excluding 3B preview) MATCH actual mutator count Section 2 — final integrity check.
    - Verify: NotificationService string muncul di RESEARCH.md.
  </acceptance_criteria>
  <done>NotificationService row di section 3, section 2C indirect call sites populated (atau eksplisit "none"), inventory total integrity verified.</done>
</task>

<task type="auto">
  <name>Task 7: Write Section 4 — HIGH Findings Detail (min 1 finding, DeleteTraining pre-confirmed)</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md (output Task 3-6, semua row HIGH di section 3)
    - Controllers/TrainingAdminController.cs (lines 520-760 untuk verbatim snippet HIGH finding)
    - Data/ApplicationDbContext.cs (lines 157-165 + 220-228 untuk RenewsTrainingId NoAction FK definition)
  </read_first>
  <action>
    Untuk setiap row dengan severity = HIGH di section 3, tulis sub-section detail di section 4 dengan structure:

    ```markdown
    ## 4. HIGH Findings

    ### 4.1 DeleteTraining (TrainingAdminController.cs:527-548)

    **Severity:** HIGH
    **Dimensions failing:** D1 (Cascade FK), D2 (File-DB atomicity), D5 (Renewal chain)

    **Evidence:**
    - L527-548 method body verbatim:
      ```csharp
      // [Paste verbatim snippet dari source]
      ```
    - L545 `System.IO.File.Delete(filePath)` dipanggil SEBELUM `SaveChangesAsync()` (D2 violation).
    - Tidak ada null-clear `RenewsTrainingId` di child TR (D5 violation).
    - FK definition `ApplicationDbContext.cs:157-165`: `RenewsTrainingId` self-ref dengan `OnDelete(NoAction)` → delete parent akan throw FK constraint exception bila ada child renewing.

    **Reproduction path:**
    1. Buat TR parent (training_001).
    2. Buat TR child (training_002) dengan `RenewsTrainingId = training_001`.
    3. Admin invoke `DELETE /TrainingAdmin/DeleteTraining/{training_001_id}`.
    4. Expected: 500 FK constraint exception "FK_TrainingRecords_TrainingRecords_RenewsTrainingId".
    5. Bonus orphan risk: kalau ada file upload `training_001.AttachmentPath` ter-File.Delete sebelum exception → orphan file di disk.

    **Remediation snippet (apply Phase 323 pattern):**
    ```csharp
    // [Pseudo-code per Phase 323 template, NOT applied di phase ini — proposal only]
    using var tx = await _context.Database.BeginTransactionAsync();
    try {
        var children = await _context.TrainingRecords
            .Where(t => t.RenewsTrainingId == id).ToListAsync();
        foreach (var c in children) c.RenewsTrainingId = null;
        var target = await _context.TrainingRecords.FindAsync(id);
        var filePath = target.AttachmentPath;
        _context.TrainingRecords.Remove(target);
        await _context.SaveChangesAsync();
        await tx.CommitAsync();
        if (!string.IsNullOrEmpty(filePath) && System.IO.File.Exists(filePath))
            System.IO.File.Delete(filePath);
        await _auditLog.LogAsync("Delete", "TrainingRecord", id, ...);
    } catch (DbUpdateException ex) {
        await tx.RollbackAsync();
        TempData["Error"] = "Gagal menghapus training — data terkait masih ada.";
        return RedirectToAction(...);
    }
    ```

    **Recommended fix phase:** [Proposal — included di section 9 list]

    ### 4.2 DeleteManualAssessment (TrainingAdminController.cs:736-756)
    [Same structure]

    ### 4.3 [Other HIGH findings dari section 3, kalau ada]
    [Same structure]
    ```

    **MINIMUM:** 2 sub-section (4.1 DeleteTraining + 4.2 DeleteManualAssessment per D-06). Tambahan kalau ada HIGH lain dari Task 4-6.

    **Verbatim snippet:** Copy 5-15 baris source code di code fence — beri line range comment di atas snippet.
  </action>
  <verify>
    <automated>bash -c 'F=".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"; grep -q "## 4. HIGH" "$F" && grep -c "^### 4\." "$F" | awk "\$1 >= 2 {exit 0} {exit 1}"'</automated>
  </verify>
  <acceptance_criteria>
    - Section 4 berisi minimal 2 sub-section (`### 4.1`, `### 4.2`).
    - Sub-section 4.1 = DeleteTraining (per D-06 pre-confirmed).
    - Sub-section 4.2 = DeleteManualAssessment (per D-06 pre-confirmed).
    - Setiap sub-section punya: Severity, Dimensions failing, Evidence (dengan verbatim snippet), Reproduction path (numbered ≥3 step), Remediation snippet (pseudo-code).
    - Verify: `grep -c "^### 4\." 328-RESEARCH.md` returns ≥ 2.
    - Verify: `grep -E "(File.Delete|RenewsTrainingId|RemoveRange)" 328-RESEARCH.md` returns ≥ 3 hit (evidence keywords present).
  </acceptance_criteria>
  <done>Section 4 lengkap dengan minimal 2 HIGH finding detail termasuk DeleteTraining + DeleteManualAssessment, full evidence + repro + remediation snippet.</done>
</task>

<task type="auto">
  <name>Task 8: Write Section 5 (MED Findings) + Section 6 (LOW Findings) — summary tables</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md (output Task 3-6, semua row dengan severity MED + LOW di section 3)
  </read_first>
  <action>
    **Section 5 — MED Findings (summary, NOT full detail):**

    Markdown table:
    ```
    | # | Endpoint | File:Line | Dimensions Failing (D3/D4/D6) | 1-line Issue Summary | Recommended Fix |
    |---|----------|-----------|-------------------------------|----------------------|-----------------|
    | 1 | [endpoint] | [file:line] | D3 (no audit), D4 (no role check) | [issue] | [fix outline] |
    ```

    Isi 1 row per endpoint MED dari section 3. Kalau tidak ada MED, eksplisit tulis "No MED findings — semua audited endpoints either HIGH (severity escalation) atau NONE (gold standard / preview-only)."

    **Section 6 — LOW Findings (summary):**

    Markdown table sama struktur, D7 (Tx wrap) hanya. Kalau tidak ada, eksplisit tulis "No LOW findings."

    **TIDAK perlu** verbatim snippet di section 5/6 — full detail hanya di section 4 (HIGH).
  </action>
  <verify>
    <automated>bash -c 'F=".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"; grep -q "## 5. MED" "$F" && grep -q "## 6. LOW" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - Section 5 ada dengan markdown table OR explicit "No MED findings" statement.
    - Section 6 ada dengan markdown table OR explicit "No LOW findings" statement.
    - Setiap MED row punya kolom Dimensions Failing yang valid (subset {D3, D4, D6}).
    - Setiap LOW row punya kolom Dimensions Failing = "D7".
    - Verify: section header `## 5.` dan `## 6.` keduanya muncul di file.
  </acceptance_criteria>
  <done>Section 5 + 6 lengkap (tabel atau eksplisit "none").</done>
</task>

<task type="auto">
  <name>Task 9: Write Section 7 (Out-of-Scope) + Section 8 (Remediation Template) + Section 9 (Recommended Next Phases)</name>
  <files>.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md</files>
  <read_first>
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-CONTEXT.md (D-02 out-of-scope items, D-05 gold standard ref, D-10 no auto-spawn)
    - Controllers/AssessmentAdminController.cs (lines ~2011-2173 post-Phase-323 — extract verbatim snippet untuk Section 8)
  </read_first>
  <action>
    **Section 7 — Out-of-Scope Statement (per D-02):**

    Bullet list eksplisit 5 item (wajib SEMUA muncul per acceptance #5):
    1. **Identity framework** — `UserManager.DeleteAsync` (ASP.NET Identity, separate concern, framework-managed cascade).
    2. **Soft-delete / `IsDeleted` flag** — product decision (kalau pindah ke soft-delete model = scope v20.0+, bukan audit ini).
    3. **Concurrency / optimistic locking** — `[ConcurrencyCheck]` / `RowVersion` audit = v20.0+ concern.
    4. **Idempotency / re-delete behavior** — what happens jika user double-click Delete? Out-of-audit, separate UX concern.
    5. **Cascade in stored procedures** — none confirmed di codebase (project uses EF Core, no T-SQL stored procs untuk delete logic).

    Tutup dengan 1 paragraf: "Lima area di atas TIDAK dievaluasi dalam audit ini sesuai D-02. Bila stakeholder ingin audit salah satu, buka phase baru via `/gsd-add-phase`."

    **Section 8 — Remediation Pattern Template (per D-05, D-08 acceptance #6):**

    Heading: "Phase 323 Gold Standard Pattern — `AssessmentAdminController.DeleteAssessment` post-commit `f1849367`"

    Copy snippet verbatim dari `Controllers/AssessmentAdminController.cs` lines ~2011-2173 (atau range exact post-Phase-323). Format:

    ```markdown
    ## 8. Remediation Pattern Template (Phase 323 Gold Standard)

    **Reference:** `Controllers/AssessmentAdminController.cs` lines 2011-2173 (post-commit `f1849367`)

    **Pattern checklist (apply ke endpoint HIGH/MED):**
    1. `[Authorize(Roles = "Admin,HC")]` attribute → D4
    2. `using var tx = await _context.Database.BeginTransactionAsync();` → D7
    3. Pre-load child entities yang FK Restrict ke target → D1 prep
    4. Null-clear renewal chain field (`RenewsSessionId` / `RenewsTrainingId`) → D5
    5. `_context.<ChildTable>.RemoveRange(...)` per FK Restrict child → D1
    6. `_context.<TargetTable>.Remove(target);` → D1
    7. `await _context.SaveChangesAsync();` SEBELUM file delete → D2
    8. `await tx.CommitAsync();` → D7
    9. `if (System.IO.File.Exists(...)) System.IO.File.Delete(...);` → D2 (post-DB-commit)
    10. `await _auditLog.LogAsync("Delete", "<Entity>", id, ...);` → D3
    11. `catch (DbUpdateException ex) { await tx.RollbackAsync(); TempData["Error"] = "..."; }` → D6

    **Verbatim snippet:**
    ```csharp
    [paste verbatim ~30-60 baris dari AssessmentAdminController.cs:2011..]
    ```
    ```

    **Section 9 — Recommended Next Phases (Proposal Only per D-10):**

    Heading note: "**PROPOSAL ONLY — user decides priority post-audit. Setiap phase fix berikutnya = separate `/gsd-add-phase` + `/gsd-plan-phase` cycle. TIDAK auto-spawned.**"

    Markdown table:
    ```
    | # | Proposed Phase Slug | Scope | Severity Trigger | Estimated Effort |
    |---|---------------------|-------|------------------|-------------------|
    | 1 | fix-cascade-deletetraining-renewal-chain | DeleteTraining + DeleteManualAssessment renewal null-clear + atomicity | HIGH | M (renewal chain pattern, 2 endpoint, mirror Phase 323) |
    | 2 | [optional dari MED] | ... | MED | S |
    ```

    Sortir by severity (HIGH first, LOW last). Tutup dengan: "User akan jalankan `/gsd-add-phase` per phase ini secara terpisah setelah review audit ini."
  </action>
  <verify>
    <automated>bash -c 'F=".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"; for kw in "UserManager" "soft-delete" "concurrency" "idempotency" "stored proc" "BeginTransactionAsync" "RemoveRange" "PROPOSAL ONLY"; do grep -qi "$kw" "$F" || { echo "MISSING $kw"; exit 1; }; done; exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - Section 7 berisi 5 bullet eksplisit: UserManager/Identity, soft-delete, concurrency, idempotency, stored proc.
    - Section 8 berisi pattern checklist (≥ 8 item) + verbatim code snippet dari `AssessmentAdminController.cs` post-f1849367.
    - Section 8 mengandung keyword `BeginTransactionAsync` + `RemoveRange` (D-08 acceptance #6).
    - Section 9 berisi proposal table dengan minimal 1 row (DeleteTraining renewal fix), dengan banner "PROPOSAL ONLY".
    - Verify: grep loop di automated test pass untuk 8 keyword.
  </acceptance_criteria>
  <done>Section 7, 8, 9 fully populated; semua D-02 out-of-scope items eksplisit; gold standard pattern di section 8; recommendation proposal list di section 9.</done>
</task>

<task type="auto">
  <name>Task 10: Integrity check + final RESEARCH.md commit + append commit hash ke ROADMAP Phase 328</name>
  <files>
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
    - .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md (final output Task 1-9)
    - .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-CONTEXT.md (D-08 acceptance criteria checklist)
    - .planning/ROADMAP.md lines 708-724 (Phase 328 entry untuk update target)
  </read_first>
  <action>
    **Step 10.1 — Integrity check (re-verify D-08):**

    Run:
    ```bash
    F=".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"

    # Acceptance #1: grep count == row count
    GREP_COUNT=$(grep -rEn "public async.*Delete\w+\(" Controllers/*.cs Services/*.cs | wc -l)
    # Tally row count di section 3 (mutators only, excluding 3B preview)
    # Manual visual check: jumlah `| N |` row di section 3 (NOT 3B) = GREP_COUNT minus preview-only methods
    echo "Inventory grep count: $GREP_COUNT"
    grep -cE "^\| [0-9]+ \|" "$F"

    # Acceptance #2: 7-dim cells filled (no TBD di section 3 mutators)
    grep -c "TBD" "$F"   # ideally low or zero in section 3

    # Acceptance #3: severity tag per row
    grep -cE "(HIGH|MED|LOW|NONE)" "$F"

    # Acceptance #4: ≥1 HIGH in section 4
    awk '/^## 4\./,/^## 5\./' "$F" | grep -c "^### 4\."

    # Acceptance #5: out-of-scope items
    for kw in UserManager soft-delete concurrency idempotency "stored proc"; do
      grep -qi "$kw" "$F" && echo "OK: $kw" || echo "MISSING: $kw"
    done

    # Acceptance #6: Phase 323 pattern di section 8
    grep -q "f1849367\|BeginTransactionAsync.*RemoveRange\|RemoveRange.*AssessmentEditLogs" "$F" && echo "OK: gold standard" || echo "MISSING gold standard"
    ```

    Kalau ANY check fail, FIX di RESEARCH.md sebelum lanjut.

    **Step 10.2 — Commit RESEARCH.md:**

    Hitung total counts dari section 3:
    - N = total mutator endpoint
    - M = HIGH count
    - K = MED count
    - L = LOW count

    Commit message format (per CONTEXT.md `<specifics>`):
    ```
    docs(328): cascade audit sweep RESEARCH — N endpoint, M HIGH, K MED, L LOW
    ```

    Eksekusi:
    ```bash
    cd "/c/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"
    git add .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
    git commit -m "docs(328): cascade audit sweep RESEARCH — N endpoint, M HIGH, K MED, L LOW"
    ```

    Capture commit hash output. Simpan untuk Step 10.3.

    **Step 10.3 — Append commit hash ke ROADMAP entry Phase 328 (D-08 acceptance #7):**

    Edit `.planning/ROADMAP.md` line 708-724 region. Tambahkan satu baris setelah `**Plans:** 1 plan (planned)` (atau update jadi "shipped"):

    ```
    **Plans:** 1 plan (shipped)
    **RESEARCH commit:** `<short-hash>` (2026-05-27 — N endpoint, M HIGH, K MED, L LOW)
    ```

    Update juga checklist item:
    ```
    Plans:
    - [x] 328-01: Enumerate + audit 7-dim per endpoint + write RESEARCH.md (shipped <short-hash>)
    ```

    Update timestamp footer ROADMAP:
    ```
    *Roadmap updated: 2026-05-27 (Phase 328 RESEARCH.md shipped, commit <hash>).*
    ```

    **Step 10.4 — Commit ROADMAP update:**

    ```bash
    git add .planning/ROADMAP.md
    git commit -m "docs(328): mark Phase 328 shipped — RESEARCH commit <hash>"
    ```

    **Step 10.5 — Write SUMMARY:**

    Create `.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-01-SUMMARY.md` dengan format minimal: phase ID, plan ID, status SHIPPED, total inventory N, breakdown HIGH/MED/LOW counts, commit hashes (RESEARCH + ROADMAP), next-step pointer ("user review audit, decide fix phase priority via `/gsd-add-phase`").

    Commit:
    ```bash
    git add .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-01-SUMMARY.md
    git commit -m "docs(328): plan 01 summary — audit sweep complete"
    ```
  </action>
  <verify>
    <automated>bash -c 'F=".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md"; R=".planning/ROADMAP.md"; test -f "$F" && grep -q "RESEARCH commit:" "$R" && test -f ".planning/phases/328-cascade-audit-sweep-delete-endpoints/328-01-SUMMARY.md"'</automated>
  </verify>
  <acceptance_criteria>
    - RESEARCH.md committed (git log shows `docs(328): cascade audit sweep RESEARCH — N endpoint, M HIGH, K MED, L LOW`).
    - ROADMAP.md updated dengan `RESEARCH commit: <hash>` di Phase 328 entry.
    - ROADMAP.md timestamp footer updated.
    - SUMMARY.md exists dengan counts + commit hashes.
    - All D-08 acceptance #1-#7 pass per integrity check Step 10.1.
    - Verify: 3 file present (RESEARCH.md, ROADMAP.md modified, SUMMARY.md present); ROADMAP grep "RESEARCH commit:" returns hit.
  </acceptance_criteria>
  <done>RESEARCH.md committed; ROADMAP Phase 328 entry diupdate dengan commit hash; SUMMARY.md ada; all D-08 acceptance criteria verifiable green.</done>
</task>

</tasks>

<verification>
**Phase-level checks (must pass before mark Phase 328 shipped):**

1. `test -f .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md`
2. `grep -c "^## " 328-RESEARCH.md` ≥ 9 (9 section header)
3. `grep -cE "^\| [0-9]+ \|" 328-RESEARCH.md` matches inventory mutator count
4. `grep -c "^### 4\." 328-RESEARCH.md` ≥ 2 (≥2 HIGH detail sub-section, DeleteTraining + DeleteManualAssessment per D-06)
5. Out-of-scope keywords present: `grep -qi "UserManager" && grep -qi "soft-delete" && grep -qi "concurrency" && grep -qi "idempotency" && grep -qi "stored proc"`
6. Gold standard reference present: `grep -q "BeginTransactionAsync" && grep -q "RemoveRange"` di section 8
7. `grep -q "RESEARCH commit:" .planning/ROADMAP.md` di Phase 328 entry
8. NO source code file (`Controllers/*.cs`, `Services/*.cs`, `Data/*.cs`, `Migrations/*`, `*.cshtml`, `appsettings.json`, `*.sql`) modified via `git diff --name-only HEAD~3 HEAD` (audit-only enforcement)

**Scope violation check:**
```bash
git diff --name-only HEAD~3 HEAD | grep -E "\.(cs|cshtml|json|sql)$|Migrations/" | grep -v "^\.planning/"
```
Expected output: EMPTY (no source file modified).
</verification>

<success_criteria>
- [ ] `328-RESEARCH.md` exists dengan 9 H2 section per D-07
- [ ] Section 3 audit table 1 row per actual mutator endpoint (count match `grep -E "public async.*Delete\w+\(" Controllers/*.cs Services/*.cs | wc -l` minus preview-only)
- [ ] Setiap row di section 3 punya D1..D7 cells ✅/❌/⚠️/N/A + evidence ref `file.cs:LINE`
- [ ] Setiap row di section 3 punya severity tag HIGH/MED/LOW/NONE
- [ ] Section 4 ≥ 2 HIGH detail (DeleteTraining + DeleteManualAssessment per D-06)
- [ ] Section 7 list 5 out-of-scope item per D-02 (Identity, soft-delete, concurrency, idempotency, stored proc)
- [ ] Section 8 berisi Phase 323 verbatim snippet + 11-step checklist
- [ ] Section 9 proposal table dengan banner "PROPOSAL ONLY" per D-10
- [ ] ROADMAP Phase 328 entry diupdate dengan RESEARCH commit hash
- [ ] SUMMARY.md exists
- [ ] NO `.cs` / `.cshtml` / `.json` / `.sql` / `Migrations/*` file modified (audit-only enforcement)
- [ ] Commit message format: `docs(328): cascade audit sweep RESEARCH — N endpoint, M HIGH, K MED, L LOW`
</success_criteria>

<output>
After completion, create `.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-01-SUMMARY.md` dengan:
- Phase ID, plan ID, status SHIPPED
- Inventory total N + breakdown HIGH/MED/LOW counts
- Commit hashes (RESEARCH + ROADMAP)
- Next-step pointer: "User review audit, decide fix phase priority via `/gsd-add-phase` per recommendation di section 9. NO auto-spawn (D-10)."
</output>
