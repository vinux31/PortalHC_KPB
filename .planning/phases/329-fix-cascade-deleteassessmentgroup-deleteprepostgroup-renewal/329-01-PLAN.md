---
phase: 329-fix-cascade-deleteassessmentgroup-deleteprepostgroup-renewal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: false
requirements:
  - PHASE-329-D5-FIX-GROUP
  - PHASE-329-D5-FIX-PREPOST
tags: [cascade, renewal, pre-check, audit-derived, v19.0]

must_haves:
  truths:
    - "Admin yang trigger DeleteAssessmentGroup untuk grup yang sibling-nya menjadi RenewsSessionId source untuk TR/AS lain menerima TempData[\"Error\"] friendly + redirect ke ManageAssessment (BUKAN FK 500)."
    - "Admin yang trigger DeletePrePostGroup untuk grup yang member-nya menjadi RenewsSessionId source untuk TR/AS lain menerima TempData[\"Error\"] friendly + redirect ke ManageAssessment (BUKAN FK 500)."
    - "DeleteAssessment (L2011, gold standard Phase 325 P05) tidak berubah perilakunya — gold standard preserved."
    - "Audit log untuk DeleteAssessmentGroup + DeletePrePostGroup tetap fire saat happy-path delete (tidak ada renewal child)."
    - "dotnet test 10/10 pass (no regression Phase 325/326/327 baseline)."
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Pre-check renewal chain di DeleteAssessmentGroup (~L2229) + DeletePrePostGroup (~L2378) + catch (DbUpdateException) safety net di kedua method."
      contains: "siblingIds.Contains(t.RenewsSessionId.Value)"
    - path: ".planning/phases/329-fix-cascade-deleteassessmentgroup-deleteprepostgroup-renewal/329-01-SUMMARY.md"
      provides: "Execution summary + UAT screenshots + commit hash + IT_NOTIFY append note."
  key_links:
    - from: "Controllers/AssessmentAdminController.cs DeleteAssessmentGroup pre-check block"
      to: "_context.TrainingRecords + _context.AssessmentSessions (CountAsync siblingIds.Contains)"
      via: "EF Core LINQ query SEBELUM BeginTransactionAsync"
      pattern: "siblingIds\\.Contains\\(t\\.RenewsSessionId\\.Value\\)"
    - from: "Controllers/AssessmentAdminController.cs DeletePrePostGroup pre-check block"
      to: "_context.TrainingRecords + _context.AssessmentSessions (CountAsync groupIds.Contains)"
      via: "EF Core LINQ query SEBELUM BeginTransactionAsync"
      pattern: "groupIds\\.Contains\\(t\\.RenewsSessionId\\.Value\\)"
    - from: "Pre-check fail path"
      to: "TempData[\"Error\"] + RedirectToAction(\"ManageAssessment\")"
      via: "Early return SEBELUM tx scope dibuka"
      pattern: "TempData\\[\"Error\"\\].*Tidak bisa hapus grup"
---

<objective>
Pasang **pre-check renewal chain (`RenewsSessionId` di `TrainingRecords` + `AssessmentSessions`)** di 2 endpoint group delete Phase 325 P05 D-11 yang terlewat: `DeleteAssessmentGroup` (Controllers/AssessmentAdminController.cs L2199-2353) + `DeletePrePostGroup` (L2359-2503). Pattern **verbatim** dari `DeleteAssessment` L2040-2052 (gold standard Phase 325 P05 commit `77a9c375`), substitusi `id` → `siblingIds`/`groupIds` via `.Contains(...)` LINQ.

Bonus same-LoC-budget: tambah `catch (DbUpdateException ex)` block sebelum existing `catch (Exception ex)` di kedua method (D-04 bundle), paralel L2180.

**Purpose:** Fix HIGH severity D5 fail dari Phase 328 RESEARCH §4.4 + §4.5. Bila salah satu sibling/group session jadi renewal source untuk worker lain's TR/AS, current code throws raw FK NoAction violation 500 di tengah cascade — UX broken + transaction half-committed risk. Fix mengubah jadi friendly TempData["Error"] + redirect SEBELUM tx scope dibuka (fail-fast).

**Output:**
- 2 method controller modified inline (~40 LoC pre-check delta + ~20 LoC catch refactor delta, total ~60 LoC)
- 1 commit `feat(329): cascade renewal pre-check DeleteAssessmentGroup + DeletePrePostGroup`
- 1 SUMMARY.md
- Bundled batch v19.0 push (D-07 — NO standalone push)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/329-fix-cascade-deleteassessmentgroup-deleteprepostgroup-renewal/329-CONTEXT.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md
@docs/DEV_WORKFLOW.md
@docs/IT_NOTIFY.md

<interfaces>
<!-- Key contracts extracted from codebase. Executor MUST use these directly. -->

From `Controllers/AssessmentAdminController.cs` L2040-2052 (gold standard pre-check, Phase 325 P05):
```csharp
// Phase 325 P05 D-11: pre-check referencing rows SEBELUM buka tx scope (fail-fast UX friendly).
var refTr = await _context.TrainingRecords
    .CountAsync(t => t.RenewsSessionId == id);
var refAs = await _context.AssessmentSessions
    .CountAsync(a => a.RenewsSessionId == id);

if (refTr + refAs > 0)
{
    var total = refTr + refAs;
    TempData["Error"] = $"Tidak bisa hapus: {total} sertifikat lain "
                      + "menggunakan record ini sebagai sumber renewal. "
                      + "Hapus atau update sertifikat pemakai terlebih dulu.";
    return RedirectToAction("ManageAssessment");
}
```

From `Controllers/AssessmentAdminController.cs` L2180-2186 (gold standard DbUpdateException catch):
```csharp
catch (DbUpdateException ex)
{
    // Phase 325 P05 D-05: safety net jika race TOCTOU antara pre-check dan tx commit.
    logger.LogWarning(ex, "Delete failed for AssessmentSession {Id}: FK constraint", id);
    TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";
    return RedirectToAction("ManageAssessment");
}
```

From `Controllers/AssessmentAdminController.cs` L2228 (variable already available in DeleteAssessmentGroup):
```csharp
var siblingIds = siblings.Select(s => s.Id).ToList();
```

From `Controllers/AssessmentAdminController.cs` L2377 (variable already available in DeletePrePostGroup):
```csharp
var groupIds = groupSessions.Select(s => s.Id).ToList();
```

From `Data/ApplicationDbContext.cs` L220-228 (FK definition justifying pre-check):
```csharp
// AssessmentSession.RenewsSessionId → AssessmentSession.Id (NoAction)
// TrainingRecord.RenewsSessionId → AssessmentSession.Id (NoAction)
// NoAction = RDBMS will throw FK violation; controller MUST pre-check.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pasang pre-check renewal chain di DeleteAssessmentGroup</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <behavior>
    - Sebelum modifikasi: L2229-2230 langsung lanjut ke L2231 `using var tx = await _context.Database.BeginTransactionAsync()` setelah `siblingIds` setup (L2228).
    - Setelah modifikasi: di antara L2228 (siblingIds assignment) dan L2231 (tx scope), terdapat pre-check block ~13 LoC yang melakukan `CountAsync` di `TrainingRecords` + `AssessmentSessions` dengan filter `RenewsSessionId.HasValue && siblingIds.Contains(...)`.
    - Jika count > 0: return early dengan `TempData["Error"]` friendly + `RedirectToAction("ManageAssessment")` SEBELUM tx dibuka.
    - Jika count == 0: lanjut ke happy path tanpa perubahan (tx scope existing tetap fire).
    - Audit log tetap fire saat happy path (tidak terpengaruh).
  </behavior>
  <action>
Buka `Controllers/AssessmentAdminController.cs` di method `DeleteAssessmentGroup` (signature `public async Task<IActionResult> DeleteAssessmentGroup(int id)` di L2199).

Cari blok `var siblingIds = siblings.Select(s => s.Id).ToList();` (saat ini L2228). **Sisipkan SETELAH baris ini, dan SEBELUM komentar `// PHASE 312 WR-01: bungkus guard + cascade dalam transaction`** (saat ini L2230), block berikut **verbatim** (sesuai D-02, substitusi `id` → `siblingIds.Contains(...)`):

```csharp
                // Phase 329 D-02: pre-check renewal chain SEBELUM buka tx scope (fail-fast UX friendly).
                // Paralel pola Phase 325 P05 DeleteAssessment L2040-2052, substitusi id → siblingIds via Contains.
                // RenewsSessionId NoAction FK (Data/ApplicationDbContext.cs:220-228) akan throw raw FK 500
                // di tengah cascade kalau pre-check ini absent.
                var refTr = await _context.TrainingRecords
                    .CountAsync(t => t.RenewsSessionId.HasValue && siblingIds.Contains(t.RenewsSessionId.Value));
                var refAs = await _context.AssessmentSessions
                    .CountAsync(a => a.RenewsSessionId.HasValue && siblingIds.Contains(a.RenewsSessionId.Value));

                if (refTr + refAs > 0)
                {
                    var total = refTr + refAs;
                    TempData["Error"] = $"Tidak bisa hapus grup: {total} sertifikat lain "
                                      + "menggunakan salah satu sesi di grup ini sebagai sumber renewal. "
                                      + "Hapus atau update sertifikat pemakai terlebih dulu.";
                    return RedirectToAction("ManageAssessment");
                }

```

**WAJIB:**
- Indentasi `                ` (16 spasi) konsisten dengan baris sekitar (di dalam `try { }` block).
- Variable `siblingIds` SUDAH ada di scope (L2228) — JANGAN deklarasi ulang.
- Naming `refTr` + `refAs` **verbatim** match L2040 L2042 (D-specifics).
- Komentar referensi `Phase 329 D-02` + `Paralel pola Phase 325 P05` (audit trail).
- Pesan error: text "Tidak bisa hapus grup: ... salah satu sesi di grup ini" (plural form, beda dari singular L2048).
- Pastikan `Contains` clause pakai `.RenewsSessionId.HasValue && siblingIds.Contains(t.RenewsSessionId.Value)` — nullable Int32 handling.

**JANGAN:**
- Mengubah baris existing L2228 atau L2231+ (tx scope dst).
- Mengubah method `DeleteAssessment` (L2011, gold standard).
- Refactor cascade RemoveRange block (out-of-scope, D-09).
  </action>
  <verify>
    <automated>dotnet build PortalHC_KPB.csproj --nologo -v minimal 2>&amp;1 | grep -E "(error|warning|Build succeeded|Build FAILED)"</automated>
  </verify>
  <done>
    1. `dotnet build` clean (0 error, warning unchanged dari baseline).
    2. `grep -n "Phase 329 D-02" Controllers/AssessmentAdminController.cs` menampilkan TEPAT 1 occurrence (di DeleteAssessmentGroup).
    3. `grep -n "siblingIds.Contains(t.RenewsSessionId.Value)" Controllers/AssessmentAdminController.cs` menampilkan 1 occurrence di DeleteAssessmentGroup region (L2199-2353).
    4. Existing logic `using var tx = await _context.Database.BeginTransactionAsync()` di DeleteAssessmentGroup tetap di tempat (line bergeser +13-14, tapi semantically unchanged).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pasang pre-check renewal chain di DeletePrePostGroup</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <behavior>
    - Sebelum modifikasi: L2378-2380 langsung lanjut ke L2382 `using var tx` setelah `groupIds` setup (L2377).
    - Setelah modifikasi: di antara L2377 (groupIds assignment) dan L2382 (tx scope), terdapat pre-check block ~13 LoC `CountAsync` dengan filter `RenewsSessionId.HasValue && groupIds.Contains(...)`.
    - Jika count > 0: return early dengan `TempData["Error"]` friendly + `RedirectToAction("ManageAssessment")` SEBELUM tx dibuka.
    - Jika count == 0: lanjut ke happy path tanpa perubahan.
  </behavior>
  <action>
Buka `Controllers/AssessmentAdminController.cs` di method `DeletePrePostGroup` (signature `public async Task<IActionResult> DeletePrePostGroup(int linkedGroupId)` di L2359).

Cari blok `var groupIds = groupSessions.Select(s => s.Id).ToList();` (saat ini L2377). **Sisipkan SETELAH baris `logger.LogInformation(...)` (L2379), dan SEBELUM komentar `// PHASE 312 WR-01: bungkus guard + cascade dalam transaction`** (saat ini L2381), block berikut **verbatim** (substitusi `siblingIds` → `groupIds`):

```csharp
                // Phase 329 D-02: pre-check renewal chain SEBELUM buka tx scope (fail-fast UX friendly).
                // Paralel pola Phase 325 P05 DeleteAssessment L2040-2052, substitusi id → groupIds via Contains.
                // RenewsSessionId NoAction FK (Data/ApplicationDbContext.cs:220-228) akan throw raw FK 500
                // di tengah cascade kalau pre-check ini absent.
                var refTr = await _context.TrainingRecords
                    .CountAsync(t => t.RenewsSessionId.HasValue && groupIds.Contains(t.RenewsSessionId.Value));
                var refAs = await _context.AssessmentSessions
                    .CountAsync(a => a.RenewsSessionId.HasValue && groupIds.Contains(a.RenewsSessionId.Value));

                if (refTr + refAs > 0)
                {
                    var total = refTr + refAs;
                    TempData["Error"] = $"Tidak bisa hapus grup Pre-Post: {total} sertifikat lain "
                                      + "menggunakan salah satu sesi di grup ini sebagai sumber renewal. "
                                      + "Hapus atau update sertifikat pemakai terlebih dulu.";
                    return RedirectToAction("ManageAssessment");
                }

```

**WAJIB:**
- Indentasi `                ` (16 spasi) konsisten dengan blok try DeletePrePostGroup.
- Variable `groupIds` SUDAH ada di scope (L2377) — JANGAN deklarasi ulang.
- Naming `refTr` + `refAs` **verbatim** (akan shadow Task 1's same-name local — itu OK karena beda method scope).
- Pesan error: "Tidak bisa hapus grup Pre-Post" (sedikit beda dari Task 1 untuk UX disambiguation).
- Comment text "groupIds" (bukan "siblingIds" copy-paste leftover).

**JANGAN:**
- Refactor cascade block di L2419-2471 (out-of-scope, D-09).
- Tambah pre-check di endpoint lain (Worker/Bagian/Kompetensi — out-of-scope D-09, defer Phase 330+).
  </action>
  <verify>
    <automated>dotnet build PortalHC_KPB.csproj --nologo -v minimal 2>&amp;1 | grep -E "(error|Build succeeded|Build FAILED)"</automated>
  </verify>
  <done>
    1. `dotnet build` clean (0 error).
    2. `grep -cn "Phase 329 D-02" Controllers/AssessmentAdminController.cs` = 2 (Task 1 + Task 2 occurrence).
    3. `grep -n "groupIds.Contains(t.RenewsSessionId.Value)" Controllers/AssessmentAdminController.cs` menampilkan 1 occurrence di DeletePrePostGroup region.
    4. `grep -n "Tidak bisa hapus grup Pre-Post" Controllers/AssessmentAdminController.cs` menampilkan 1 occurrence (disambiguation marker).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor catch — tambah DbUpdateException specific di kedua method (D-04 bonus)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <behavior>
    - Sebelum modifikasi: DeleteAssessmentGroup hanya punya `catch (Exception ex)` generic di akhir try block (~L2347). DeletePrePostGroup sama (~L2497).
    - Setelah modifikasi: kedua method punya `catch (DbUpdateException dbEx)` SEBELUM existing `catch (Exception ex)`, paralel pola DeleteAssessment L2180-2186.
    - DbUpdateException catch: log warning + TempData["Error"] friendly + redirect. Tidak rollback eksplisit (tx scope `using` auto-dispose handle that).
  </behavior>
  <action>
Buka `Controllers/AssessmentAdminController.cs`. Apply 2 edit:

**Edit 3a — DeleteAssessmentGroup catch refactor (~L2347):**

Cari blok existing:
```csharp
            catch (Exception ex)
            {
                logger.LogError(ex, "DeleteAssessmentGroup error for representative {Id}", id);
                TempData["Error"] = "Gagal menghapus grup assessment. Silakan coba lagi.";
                return RedirectToAction("ManageAssessment");
            }
```

Sisipkan **SEBELUM** blok `catch (Exception ex)` di atas, block baru berikut:
```csharp
            catch (DbUpdateException dbEx)
            {
                // Phase 329 D-04: safety net TOCTOU race antara pre-check Task 1 dan tx commit
                // (concurrent insert renewal child setelah pre-check passed). Paralel L2180.
                logger.LogWarning(dbEx, "DeleteAssessmentGroup FK violation for representative {Id}", id);
                TempData["Error"] = "Gagal hapus grup: ada constraint database yang dilanggar.";
                return RedirectToAction("ManageAssessment");
            }
```

**Edit 3b — DeletePrePostGroup catch refactor (~L2497):**

Cari blok existing:
```csharp
            catch (Exception ex)
            {
                logger.LogError(ex, "DeletePrePostGroup error for LinkedGroupId {LinkedGroupId}", linkedGroupId);
                TempData["Error"] = "Gagal menghapus grup Pre-Post Test. Silakan coba lagi.";
                return RedirectToAction("ManageAssessment");
            }
```

Sisipkan **SEBELUM** blok `catch (Exception ex)` di atas, block baru berikut:
```csharp
            catch (DbUpdateException dbEx)
            {
                // Phase 329 D-04: safety net TOCTOU race antara pre-check Task 2 dan tx commit
                // (concurrent insert renewal child setelah pre-check passed). Paralel L2180.
                logger.LogWarning(dbEx, "DeletePrePostGroup FK violation for LinkedGroupId {LinkedGroupId}", linkedGroupId);
                TempData["Error"] = "Gagal hapus grup Pre-Post: ada constraint database yang dilanggar.";
                return RedirectToAction("ManageAssessment");
            }
```

**WAJIB:**
- DbUpdateException catch sisip **SEBELUM** generic Exception catch (catch order matters — specific first per C# spec).
- Indentasi `            ` (12 spasi) konsisten dengan existing catch.
- Variable name `dbEx` (bukan `ex`) untuk avoid shadow dengan generic catch `ex`.
- Comment referensi `Phase 329 D-04` + `Paralel L2180`.
- Existing `catch (Exception ex)` tetap dipertahankan utuh sebagai fallback.

**JANGAN:**
- Hapus existing `catch (Exception ex)` block — itu fallback untuk non-DB exception (logic bug, NullRef, dll).
- Refactor catch di method LAIN (DeleteAssessment L2180 sudah gold standard, jangan touch).
  </action>
  <verify>
    <automated>dotnet build PortalHC_KPB.csproj --nologo -v minimal 2>&amp;1 | grep -E "(error|Build succeeded|Build FAILED)"</automated>
  </verify>
  <done>
    1. `dotnet build` clean (0 error).
    2. `grep -cn "Phase 329 D-04" Controllers/AssessmentAdminController.cs` = 2 (DeleteAssessmentGroup + DeletePrePostGroup).
    3. `grep -cn "catch (DbUpdateException" Controllers/AssessmentAdminController.cs` >= 3 (DeleteAssessment L2180 existing + 2 new di Task 3).
    4. Order verification: di setiap method (Group + PrePostGroup), `DbUpdateException` muncul SEBELUM `Exception` catch (C# specific-first rule).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verifikasi end-to-end — dotnet test + manual UAT seed-data + commit + SUMMARY</name>
  <files>Controllers/AssessmentAdminController.cs, .planning/phases/329-fix-cascade-deleteassessmentgroup-deleteprepostgroup-renewal/329-01-SUMMARY.md, docs/SEED_JOURNAL.md</files>
  <what-built>
    Task 1-3 sudah apply pre-check + catch refactor. Sekarang verify ujung-ke-ujung lewat (a) automated test suite, (b) 2 skenario manual UAT seed-data, (c) commit single + SUMMARY.md generation.
  </what-built>
  <action>
Step A + Step C wajib dieksekusi Claude (automated). Step B (manual UAT) wajib operator manusia karena perlu seed DB + browser interaction. Detail di `<how-to-verify>` di bawah.
  </action>
  <how-to-verify>

**Step A — Automated verification (Claude execute first, no human needed):**

```bash
# 1. Build clean
dotnet build PortalHC_KPB.csproj --nologo -v minimal

# 2. Test suite — MUST 10/10 pass (Phase 325/326/327 baseline)
dotnet test --nologo --logger "console;verbosity=normal"

# 3. Lint check — grep all 4 markers
grep -cn "Phase 329 D-02" Controllers/AssessmentAdminController.cs   # expect 2
grep -cn "Phase 329 D-04" Controllers/AssessmentAdminController.cs   # expect 2
grep -cn "siblingIds.Contains(t.RenewsSessionId.Value)" Controllers/AssessmentAdminController.cs   # expect 1
grep -cn "groupIds.Contains(t.RenewsSessionId.Value)" Controllers/AssessmentAdminController.cs     # expect 1
```

Jika test fail atau grep count tidak match, ROLLBACK dan re-apply Task 1-3 fix sebelum lanjut Step B.

**Step B — Manual UAT seed-data (sesuai docs/SEED_WORKFLOW.md):**

1. **Snapshot DB lokal** sebelum seed:
   ```bash
   sqlcmd -S "(localdb)\MSSQLLocalDB" -d PortalHC -Q "BACKUP DATABASE [PortalHC] TO DISK='C:\Temp\portalhc-pre-329-uat.bak' WITH INIT"
   ```

2. **Update `docs/SEED_JOURNAL.md`** dengan entry Phase 329 UAT (klasifikasi: `temporary + local-only`, tujuan: UAT-329-01 + UAT-329-02, entitas: AssessmentSessions, TrainingRecords).

3. **Seed UAT-329-01 (DeleteAssessmentGroup block scenario):**
   - Buat 2 AssessmentSession sibling (sama Title + Category + Schedule.Date) — sebut SessionA + SessionB.
   - Buat 1 TrainingRecord dengan `RenewsSessionId = SessionA.Id` (atau alternatif, AssessmentSession lain dengan `RenewsSessionId = SessionB.Id`).
   - Login Admin: `admin@pertamina.com` (reference memory `Dev Admin Credentials`).
   - Akses ManageAssessment → klik Delete Group untuk SessionA group.
   - **Expected:** Redirect ke ManageAssessment dengan TempData["Error"] = "Tidak bisa hapus grup: 1 sertifikat lain menggunakan salah satu sesi di grup ini sebagai sumber renewal. Hapus atau update sertifikat pemakai terlebih dulu."
   - **Forbidden:** FK 500 raw exception page atau partial cascade commit.

4. **Seed UAT-329-02 (DeletePrePostGroup block scenario):**
   - Buat 2 AssessmentSession dengan sama `LinkedGroupId` (1 PreTest + 1 PostTest) — sebut PrePost grup.
   - Buat 1 TrainingRecord dengan `RenewsSessionId = PreTestSession.Id`.
   - Akses ManageAssessment → klik Delete Pre-Post Group.
   - **Expected:** Redirect ke ManageAssessment dengan TempData["Error"] = "Tidak bisa hapus grup Pre-Post: 1 sertifikat lain menggunakan salah satu sesi di grup ini sebagai sumber renewal..."
   - **Forbidden:** FK 500.

5. **Happy-path regression smoke (optional but recommended):**
   - Hapus child TR yang block UAT-329-01.
   - Re-trigger Delete Group untuk SessionA group → harus success (TempData["Success"]) + audit log fire.

6. **RESTORE DB** post-UAT (sukses atau gagal):
   ```bash
   sqlcmd -S "(localdb)\MSSQLLocalDB" -Q "RESTORE DATABASE [PortalHC] FROM DISK='C:\Temp\portalhc-pre-329-uat.bak' WITH REPLACE"
   ```
   Lalu tandai `docs/SEED_JOURNAL.md` entry sebagai `cleaned`.

**Step C — Commit + SUMMARY (Claude execute setelah human approve Step B):**

```bash
git add Controllers/AssessmentAdminController.cs docs/SEED_JOURNAL.md
git commit -m "feat(329): cascade renewal pre-check DeleteAssessmentGroup + DeletePrePostGroup

- Pre-check RenewsSessionId di TR + AS via siblingIds.Contains / groupIds.Contains
- Verbatim pattern Phase 325 P05 DeleteAssessment L2040-2052
- Bonus D-04: catch (DbUpdateException) safety net di kedua method
- Audit-derived fix HIGH severity D5 fail Phase 328 RESEARCH §4.4 + §4.5
- NO push standalone (D-07 bundle batch v19.0 with 325-328)"
```

Tulis `.planning/phases/329-fix-cascade-deleteassessmentgroup-deleteprepostgroup-renewal/329-01-SUMMARY.md` dengan template:
- Status: SHIPPED LOCAL (NO push per D-07)
- Commit hash
- Acceptance criteria 8/8 checklist (D-08)
- UAT result UAT-329-01 + UAT-329-02 (pass/fail per scenario)
- IT_NOTIFY append note: bundle dengan batch v19.0 (Phase 325+326+327+329 — Phase 328 audit-only no kode change)
- Threat model recap: D5 HIGH → MITIGATED, D6 ⚠️ → MITIGATED via DbUpdateException specific catch.

  </how-to-verify>
  <verify>
    <automated>dotnet test --nologo --logger "console;verbosity=minimal" 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    1. `dotnet test` 10/10 pass (Phase 325/326/327 baseline preserved).
    2. UAT-329-01 PASS — DeleteAssessmentGroup block dengan friendly TempData["Error"] (BUKAN FK 500).
    3. UAT-329-02 PASS — DeletePrePostGroup block dengan friendly TempData["Error"] (BUKAN FK 500).
    4. Happy-path regression smoke PASS (delete tanpa renewal child → success + audit log fire).
    5. DB lokal restored post-UAT, `docs/SEED_JOURNAL.md` Phase 329 entry marked `cleaned`.
    6. Single commit `feat(329): cascade renewal pre-check DeleteAssessmentGroup + DeletePrePostGroup` exists di git log.
    7. SUMMARY.md ditulis dengan acceptance criteria 8/8 checklist (D-08) + threat model recap.
    8. NO push origin/main (D-07 — bundle batch v19.0 hold per Phase 327 option-b).
  </done>
  <resume-signal>Type "approved" jika 2/2 UAT pass + dotnet test 10/10 + commit done + SUMMARY ditulis. Type "rollback" jika UAT fail (jelaskan scenario fail mana — Claude akan revisit Task 1-3 fix).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC UI → Controller action | POST request dari ManageAssessment view membawa AssessmentSession.Id atau LinkedGroupId. Anti-forgery token validated `[ValidateAntiForgeryToken]`. Role-tier guard `[Authorize(Roles = "Admin, HC")]`. |
| Controller → EF Core | LINQ query `Contains` dengan parameter `siblingIds`/`groupIds` (List<int> dari DB, no user-controlled string). EF parameterizes IN clause — no SQL injection vector. |
| Controller → DB FK | `RenewsSessionId NoAction` FK enforces referential integrity di RDBMS layer kalau pre-check race. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-329-01 | D (Denial of Service) | `DeleteAssessmentGroup` cascade tx half-commit | mitigate | Pre-check SEBELUM `BeginTransactionAsync` mencegah cascade mid-flight FK violation. Tx scope tidak dibuka kalau renewal child detected. |
| T-329-02 | D (Denial of Service) | `DeletePrePostGroup` cascade tx half-commit | mitigate | Sama T-329-01, applied ke method PrePost variant. |
| T-329-03 | T (Tampering) | TOCTOU race: concurrent insert TR.RenewsSessionId antara pre-check dan tx commit | mitigate | `catch (DbUpdateException)` block di kedua method (Task 3, D-04). FK NoAction RDBMS-layer fire kalau race terjadi → friendly TempData["Error"] alih-alih 500 stack trace. |
| T-329-04 | I (Information Disclosure) | Error message bocor jumlah sertifikat referrer ke admin | accept | Admin role sudah trusted tier. Disclosure `{total}` count tidak sensitif (admin sudah punya akses Read full TR/AS table via ManageAssessment list). |
| T-329-05 | R (Repudiation) | Audit log untuk happy-path delete unchanged | accept | Pre-check fail path early-return SEBELUM audit log block — by design (no successful delete to audit). Audit log tetap fire saat cascade success. Risk: admin tidak punya trail untuk "attempted delete blocked by renewal" — accept karena TempData["Error"] sudah visible di UI + low compliance value. |
| T-329-06 | E (Elevation of Privilege) | Bypass `[Authorize(Roles = "Admin, HC")]` via direct POST | accept | Phase 312 role-tier guard `EnsureCanDeleteAsync` tetap fire **setelah** pre-check pass (existing behavior unchanged). Pre-check tidak melemahkan auth — hanya tambah short-circuit untuk renewal case. |
</threat_model>

<verification>

## Phase-Level Checks (Cross-Task)

1. **Build:** `dotnet build PortalHC_KPB.csproj` returns 0 error (warning = baseline unchanged).
2. **Test suite:** `dotnet test` returns 10/10 pass (Phase 325/326/327 baseline preserved — D-08 #3).
3. **File scope discipline (D-09):** `git diff --name-only HEAD~1 HEAD` (post-commit) MUST list ONLY:
   - `Controllers/AssessmentAdminController.cs`
   - `docs/SEED_JOURNAL.md`
   - `.planning/phases/329-*/329-01-SUMMARY.md`
   NO view file, NO migration, NO model, NO `Data/ApplicationDbContext.cs`, NO `DeleteAssessment` (L2011) modification.
4. **Marker validation:**
   - `grep -cn "Phase 329 D-02"` = 2
   - `grep -cn "Phase 329 D-04"` = 2
   - `grep -cn "Tidak bisa hapus grup"` >= 2 (Group + Pre-Post variant)
5. **UAT 2/2 pass** — UAT-329-01 (Group) + UAT-329-02 (PrePost) both BLOCK with friendly TempData["Error"] (not FK 500).
6. **Audit log smoke** — happy-path delete (no renewal child) untuk Group + PrePost still fire AuditLog.LogAsync entry (D-08 #5).
7. **DB clean post-UAT** — `docs/SEED_JOURNAL.md` Phase 329 entry marked `cleaned`, DB restored to pre-UAT snapshot.

</verification>

<success_criteria>

Phase 329 SHIPPED LOCAL when:
- [ ] Task 1: DeleteAssessmentGroup pre-check block apply + `dotnet build` pass.
- [ ] Task 2: DeletePrePostGroup pre-check block apply + `dotnet build` pass.
- [ ] Task 3: catch (DbUpdateException) refactor apply di kedua method + `dotnet build` pass.
- [ ] Task 4 Step A: `dotnet test` 10/10 pass + 4 grep markers match expected count.
- [ ] Task 4 Step B: UAT-329-01 + UAT-329-02 manual seed-data PASS (block dengan friendly error, BUKAN FK 500).
- [ ] Task 4 Step C: Single commit `feat(329): cascade renewal pre-check DeleteAssessmentGroup + DeletePrePostGroup` dengan scope file 1 (controller) + 1 (SEED_JOURNAL) + 1 (SUMMARY).
- [ ] SUMMARY.md ditulis dengan acceptance criteria 8/8 checklist (D-08).
- [ ] DB lokal restored post-UAT, `docs/SEED_JOURNAL.md` marked `cleaned`.
- [ ] NO push origin/main (D-07 bundle batch v19.0 hold per Phase 327 option-b).
- [ ] IT_NOTIFY.md entry appended (bundle dengan 325+326+327 batch — Phase 328 audit-only, no kode delta).

</success_criteria>

<output>

After completion, create:
- `.planning/phases/329-fix-cascade-deleteassessmentgroup-deleteprepostgroup-renewal/329-01-SUMMARY.md`

SUMMARY.md template MUST include:
- **Status:** SHIPPED LOCAL (NOT pushed, bundle v19.0 batch per D-07)
- **Commit:** {hash} `feat(329): cascade renewal pre-check DeleteAssessmentGroup + DeletePrePostGroup`
- **Files modified:** Controllers/AssessmentAdminController.cs (~60 LoC delta), docs/SEED_JOURNAL.md
- **Acceptance criteria 8/8:** checklist tickmark per D-08 item
- **UAT result:** UAT-329-01 PASS/FAIL + UAT-329-02 PASS/FAIL + happy-path regression smoke
- **Threat model:** T-329-01..06 disposition status
- **Next steps:** Append IT_NOTIFY.md entry → wait user explicit push approval → bundle batch v19.0 push (325+326+327+329 — 328 audit-only)
- **Open questions / risks:** (kosong jika 2/2 UAT clean)

</output>
