---
phase: 331-fix-cascade-deletetraining-deletemanualassessment-atomicity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/TrainingAdminController.cs
  - docs/IT_NOTIFY.md
autonomous: true
requirements:
  - D-01-DeleteTraining-tx-wrap
  - D-02-DeleteTraining-file-post-commit
  - D-03-DeleteManualAssessment-tx-wrap
  - D-04-DeleteManualAssessment-file-post-commit
must_haves:
  truths:
    - "DeleteTraining: Remove + SaveChangesAsync + AuditLog ter-wrap dalam BeginTransactionAsync tx, CommitAsync mendahului File.Delete"
    - "DeleteTraining: System.IO.File.Delete dieksekusi POST CommitAsync dengan inner try/catch warn-only (log + lanjut, tidak rollback)"
    - "DeleteManualAssessment: Remove + SaveChangesAsync + AuditLog ter-wrap dalam BeginTransactionAsync tx, CommitAsync mendahului FileUploadHelper.DeleteFile"
    - "DeleteManualAssessment: FileUploadHelper.DeleteFile dieksekusi POST CommitAsync dengan inner try/catch warn-only"
    - "Pre-check renewal (CountAsync RenewsTrainingId/RenewsSessionId) tetap di POSISI semula SEBELUM tx scope, tidak dipindah masuk tx"
    - "Catch DbUpdateException Phase 325 P05 (L598 + L828) preserved verbatim: _logger.LogWarning + TempData[Error] friendly"
    - "Tidak ada record DB yang ter-Remove kalau Save fail (atomicity dijamin via tx auto-rollback by using disposal)"
    - "Tidak ada file di disk yang ter-Delete kalau Save fail (reorder mencegah orphan-delete)"
    - "dotnet build clean, dotnet test 18/18 pass — tidak ada regresi dari Phase 330 baseline"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "DeleteTraining + DeleteManualAssessment refactored: tx wrap + File.Delete POST commit"
      contains: "BeginTransactionAsync"
    - path: "docs/IT_NOTIFY.md"
      provides: "Phase 331 IT notification entry (bundle v19.0 append)"
      contains: "Phase 331"
  key_links:
    - from: "Controllers/TrainingAdminController.cs"
      to: "DeleteTraining L559-605"
      via: "using var tx = await _context.Database.BeginTransactionAsync(); ... await tx.CommitAsync(); ... System.IO.File.Delete"
      pattern: "File\\.Delete post-commit failed"
    - from: "Controllers/TrainingAdminController.cs"
      to: "DeleteManualAssessment L793-834"
      via: "using var tx = await _context.Database.BeginTransactionAsync(); ... await tx.CommitAsync(); ... FileUploadHelper.DeleteFile"
      pattern: "FileUploadHelper\\.DeleteFile post-commit failed"
    - from: "Controllers/TrainingAdminController.cs"
      to: "Pre-check renewal L568-580 + L802-805"
      via: "CountAsync RenewsTrainingId / RenewsSessionId OUTSIDE tx scope, early return RedirectToAction"
      pattern: "referencingTr \\+ referencingAs > 0"
---

<objective>
Tutup 2 HIGH finding Phase 328 Cascade Audit Sweep (§4.1 + §4.2 + §9 proposal #1): file-DB atomicity di `DeleteTraining` + `DeleteManualAssessment` via `BeginTransactionAsync` wrap + reorder file deletion POST `CommitAsync`. Perubahan ini bersifat mechanical controller-only — tidak ada migration, tidak ada schema change, tidak ada service injection, tidak ada view change.

Purpose: Menghilangkan window orphan-delete (file hilang dari disk tapi row DB masih ada saat Save fail) dan menjamin atomicity Remove+SaveChanges+AuditLog dalam satu tx commit. Pre-check renewal Phase 325 P05 (L568-580 + L802-805) dan catch DbUpdateException Phase 325 P05 (L598 + L828) tetap dipertahankan verbatim.

Output: 1 file controller dimodifikasi (~80 LoC delta total, 2 endpoint), docs/IT_NOTIFY.md diupdate dengan Phase 331 entry, 1 commit bertag pesan standar D-09 item 8, SUMMARY.md digenerate.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-CONTEXT.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
@.planning/phases/330-fix-cascade-med-bundle-delete-category-package-question-orgu/330-01-SUMMARY.md

<interfaces>
<!-- Kontrak yang digunakan executor — tidak perlu eksplorasi codebase lebih jauh. -->

Dari Controllers/AdminBaseController.cs (base class TrainingAdminController):
```csharp
protected readonly ApplicationDbContext _context;
protected readonly UserManager<ApplicationUser> _userManager;
protected readonly AuditLogService _auditLog;
protected readonly IWebHostEnvironment _env;
protected readonly ILogger _logger;   // tersedia (Phase 325 P05 sudah pakai L601 + L830)
```

Dari Helpers/FileUploadHelper.cs:
```csharp
public static void DeleteFile(string webRootPath, string? relativeUrl);
// Idempotent: handle null/empty + File.Exists check internal
```

Dari Microsoft.EntityFrameworkCore (sudah imported di TrainingAdminController.cs):
```csharp
DbContext.Database.BeginTransactionAsync();   // returns IDbContextTransaction
IDbContextTransaction.CommitAsync();
// using var tx = ...; → disposal otomatis rollback jika exception escape sebelum CommitAsync
```

State kode aktual sebelum fix (verbatim dari TrainingAdminController.cs):

**DeleteTraining L559-605** (target Task 1):
- L561-562: `var record = await _context.TrainingRecords.FindAsync(id);` + null check NotFound
- L564 `try {`
- L568-580: pre-check `referencingTr + referencingAs` (Phase 325 P05) → keep di POSISI semula
- L582-586: `if (!string.IsNullOrEmpty(record.SertifikatUrl)) { var path = ...; if (File.Exists(path)) File.Delete(path); }` ← SEBELUM Save (D2 broken)
- L588: `var actor = await _userManager.GetUserAsync(User);`
- L589: `_context.TrainingRecords.Remove(record);`
- L590: `await _context.SaveChangesAsync();`
- L592-594: audit log `_auditLog.LogAsync(...)` jika actor != null
- L596: `TempData["Success"] = "Training record berhasil dihapus.";`
- L598-603: `catch (DbUpdateException ex)` → `_logger.LogWarning` + `TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";`
- L604: `return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });`

**DeleteManualAssessment L793-834** (target Task 2):
- L795-796: `var session = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == id && s.IsManualEntry);` + null check
- L798 `try {`
- L802-805: pre-check `referencingTr + referencingAs` (Phase 325 P05) → keep di POSISI semula
- L816: `FileUploadHelper.DeleteFile(_env.WebRootPath, session.ManualSertifikatUrl);` ← SEBELUM Save (D2 broken)
- L818: `var actor = await _userManager.GetUserAsync(User);`
- L819: `_context.AssessmentSessions.Remove(session);`
- L820: `await _context.SaveChangesAsync();`
- L822-824: audit log `_auditLog.LogAsync(...)` jika actor != null
- L826: `TempData["Success"] = "Assessment manual berhasil dihapus.";`
- L828-831: `catch (DbUpdateException ex)` → `_logger.LogWarning` + `TempData["Error"]`
- L833: `return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });`

Audit log signature yang dipakai (verbatim L592-594 + L822-824):
```csharp
await _auditLog.LogAsync(actor.Id, actor.FullName, "Delete",
    $"{detail}", entityId, entityType);
// entityType: "TrainingRecord" (DeleteTraining) / "AssessmentSession" (DeleteManualAssessment)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor DeleteTraining — capture path, tx wrap, reorder System.IO.File.Delete POST commit</name>
  <files>Controllers/TrainingAdminController.cs</files>

  <read_first>
    1. Controllers/TrainingAdminController.cs L555-605 (DeleteTraining — baca state saat ini)
    2. .planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-CONTEXT.md (D-02, D-03 DeleteTraining block, D-04, D-05, D-06, D-07)
    3. .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md §4.1 (root cause + remediation)
  </read_first>

  <action>
**Refactor `DeleteTraining` (L559-605) — per D-02 + D-03 (DeleteTraining block) + D-04 + D-05 + D-06 + D-07**

Ganti seluruh body method DeleteTraining dari L561 (sesudah signature) sampai L604 (sebelum closing brace method) dengan struktur berikut. Reuse semua kode existing verbatim — hanya reorder + tambah tx wrap + capture path + inner try/catch File.Delete.

```csharp
public async Task<IActionResult> DeleteTraining(int id)
{
    var record = await _context.TrainingRecords.FindAsync(id);
    if (record == null) return NotFound();

    // Phase 331 D-03: Capture file path SEBELUM Remove (record akan detached post-Remove).
    string? sertifikatPath = null;
    if (!string.IsNullOrEmpty(record.SertifikatUrl))
    {
        sertifikatPath = Path.Combine(_env.WebRootPath, record.SertifikatUrl.TrimStart('/'));
    }

    try
    {
        // Phase 325 P05 D-04/D-11: pre-check referencing rows SEBELUM hapus (UX friendly).
        // Phase 331 D-04: pre-check TETAP di POSISI semula, OUTSIDE tx scope (read-only count, no tx needed).
        var referencingTr = await _context.TrainingRecords
            .CountAsync(t => t.RenewsTrainingId == record.Id);
        var referencingAs = await _context.AssessmentSessions
            .CountAsync(a => a.RenewsTrainingId == record.Id);

        if (referencingTr + referencingAs > 0)
        {
            var total = referencingTr + referencingAs;
            TempData["Error"] = $"Tidak bisa hapus: {total} sertifikat lain "
                              + "menggunakan record ini sebagai sumber renewal. "
                              + "Hapus atau update sertifikat pemakai terlebih dulu.";
            return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
        }

        // Phase 331 D-02: tx wrap Remove + SaveChanges + AuditLog. Disposal auto-rollback jika exception escape sebelum CommitAsync.
        using var tx = await _context.Database.BeginTransactionAsync();

        var actor = await _userManager.GetUserAsync(User);
        _context.TrainingRecords.Remove(record);
        await _context.SaveChangesAsync();

        if (actor != null)
            await _auditLog.LogAsync(actor.Id, actor.FullName, "Delete",
                $"Training record dihapus: {record.Judul}", record.Id, "TrainingRecord");

        await tx.CommitAsync();

        // Phase 331 D-03 + D-05 + D-06: File.Delete POST commit dengan inner try/catch warn-only.
        // DB sudah committed — kalau File.Delete fail, log warning + biarkan orphan di disk (acceptable, manual cleanup later).
        if (sertifikatPath != null && System.IO.File.Exists(sertifikatPath))
        {
            try { System.IO.File.Delete(sertifikatPath); }
            catch (Exception ex) { _logger.LogWarning(ex, "File.Delete post-commit failed: {Path}", sertifikatPath); }
        }

        TempData["Success"] = "Training record berhasil dihapus.";
    }
    catch (DbUpdateException ex)
    {
        // Phase 325 P05 D-05: catch specific DbUpdateException (TOCTOU safety net jika race antara pre-check dan delete).
        // Phase 331 D-07: preserve verbatim. using var tx disposal auto-rollback — no explicit RollbackAsync needed.
        _logger.LogWarning(ex, "Delete failed for TrainingRecord {Id}", record.Id);
        TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";
    }
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
```

**Catatan presisi:**
- Pre-check renewal (CountAsync TR+AS) HARUS tetap INSIDE `try` block tapi OUTSIDE tx scope (sebelum `using var tx = ...`). Early return RedirectToAction = exit BEFORE tx creation = tidak ada tx leak.
- `using var tx` HARUS deklarasi SETELAH pre-check check `referencingTr + referencingAs > 0`. Kalau pre-check fail, langsung return tanpa creates tx.
- `tx.CommitAsync()` HARUS dipanggil SETELAH audit log, SEBELUM `File.Delete`.
- `File.Delete` HARUS dibungkus inner `try { } catch (Exception ex) { _logger.LogWarning(...); }` — JANGAN throw, JANGAN rollback (tx sudah commit).
- `TempData["Success"]` HARUS di-set SETELAH `File.Delete` (sukses-atau-warn) tapi masih INSIDE outer try block.
- Catch `DbUpdateException` di L598 verbatim preserved: `_logger.LogWarning(ex, "Delete failed for TrainingRecord {Id}", record.Id);` + `TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";`
- TIDAK pakai explicit `tx.RollbackAsync()` di catch — `using var tx` disposal otomatis rollback kalau catch hit sebelum CommitAsync.
- TIDAK ada perubahan ke L555-558 (attribute `[HttpPost]` + `[ValidateAntiForgeryToken]` + `[Authorize(Roles = "Admin, HC")]` + signature).
  </action>

  <verify>
    <automated>grep -n "Phase 331 D-02: tx wrap" Controllers/TrainingAdminController.cs && grep -n "File.Delete post-commit failed" Controllers/TrainingAdminController.cs && grep -n "Training record dihapus" Controllers/TrainingAdminController.cs</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "string? sertifikatPath = null;" Controllers/TrainingAdminController.cs → 1 match (capture path baru di DeleteTraining)
    - grep -n "using var tx = await _context.Database.BeginTransactionAsync();" Controllers/TrainingAdminController.cs → ≥ 1 match (DeleteTraining; akan jadi 2 setelah Task 2)
    - grep -n "await tx.CommitAsync();" Controllers/TrainingAdminController.cs → ≥ 1 match (DeleteTraining; akan jadi 2 setelah Task 2)
    - grep -n "File.Delete post-commit failed" Controllers/TrainingAdminController.cs → 1 match
    - grep -n "referencingTr \+ referencingAs > 0" Controllers/TrainingAdminController.cs → masih 2 match (pre-check L573 + L807 dipertahankan)
    - grep -n "Delete failed for TrainingRecord" Controllers/TrainingAdminController.cs → 1 match (catch L598 preserved)
    - grep -n "Gagal hapus: ada constraint database" Controllers/TrainingAdminController.cs → masih 2 match (catch L602 + L831 dipertahankan)
    - Catch DbUpdateException untuk DeleteTraining masih ada (Phase 325 P05 preserved verbatim)
    - Pre-check renewal CountAsync TR + AS masih di posisi semula (sebelum `using var tx = ...`)
  </acceptance_criteria>

  <done>DeleteTraining L559-605 di TrainingAdminController.cs: file path captured SEBELUM Remove, Remove+SaveChanges+AuditLog ter-wrap dalam `using var tx = await _context.Database.BeginTransactionAsync();`, `File.Delete` dieksekusi POST `CommitAsync` dengan inner try/catch warn-only. Pre-check renewal Phase 325 P05 + catch DbUpdateException Phase 325 P05 preserved verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor DeleteManualAssessment — capture url, tx wrap, reorder FileUploadHelper.DeleteFile POST commit</name>
  <files>Controllers/TrainingAdminController.cs</files>

  <read_first>
    1. Controllers/TrainingAdminController.cs L789-834 (DeleteManualAssessment — baca state saat ini)
    2. .planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-CONTEXT.md (D-02, D-03 DeleteManualAssessment block, D-04, D-05, D-06, D-07)
    3. .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md §4.2 (root cause + remediation)
    4. Helpers/FileUploadHelper.cs (konfirmasi signature `DeleteFile(string webRootPath, string? relativeUrl)`)
  </read_first>

  <action>
**Refactor `DeleteManualAssessment` (L793-834) — per D-02 + D-03 (DeleteManualAssessment block) + D-04 + D-05 + D-06 + D-07**

Ganti seluruh body method DeleteManualAssessment dari L795 (sesudah signature) sampai L833 (sebelum closing brace method) dengan struktur berikut. Reuse semua kode existing verbatim — hanya reorder + tambah tx wrap + capture URL + inner try/catch FileUploadHelper.DeleteFile.

```csharp
public async Task<IActionResult> DeleteManualAssessment(int id)
{
    var session = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == id && s.IsManualEntry);
    if (session == null) return NotFound();

    // Phase 331 D-03: Capture file URL SEBELUM Remove (session akan detached post-Remove).
    string? manualSertifikatUrl = session.ManualSertifikatUrl;

    try
    {
        // Phase 325 P05 D-04/D-11: pre-check referencing rows SEBELUM hapus.
        // AssessmentSession punya 2 jenis renewal child: TR + AS. FK column = RenewsSessionId (BUKAN RenewsTrainingId — pitfall RESEARCH §Pitfall 2).
        // Phase 331 D-04: pre-check TETAP di POSISI semula, OUTSIDE tx scope.
        var referencingTr = await _context.TrainingRecords
            .CountAsync(t => t.RenewsSessionId == session.Id);
        var referencingAs = await _context.AssessmentSessions
            .CountAsync(a => a.RenewsSessionId == session.Id);

        if (referencingTr + referencingAs > 0)
        {
            var total = referencingTr + referencingAs;
            TempData["Error"] = $"Tidak bisa hapus: {total} sertifikat lain "
                              + "menggunakan record ini sebagai sumber renewal. "
                              + "Hapus atau update sertifikat pemakai terlebih dulu.";
            return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
        }

        // Phase 331 D-02: tx wrap Remove + SaveChanges + AuditLog.
        using var tx = await _context.Database.BeginTransactionAsync();

        var actor = await _userManager.GetUserAsync(User);
        _context.AssessmentSessions.Remove(session);
        await _context.SaveChangesAsync();

        if (actor != null)
            await _auditLog.LogAsync(actor.Id, actor.FullName, "Delete",
                $"Assessment manual dihapus: {session.Title}", session.Id, "AssessmentSession");

        await tx.CommitAsync();

        // Phase 331 D-03 + D-05 + D-06: FileUploadHelper.DeleteFile POST commit dengan inner try/catch warn-only.
        if (!string.IsNullOrEmpty(manualSertifikatUrl))
        {
            try { FileUploadHelper.DeleteFile(_env.WebRootPath, manualSertifikatUrl); }
            catch (Exception ex) { _logger.LogWarning(ex, "FileUploadHelper.DeleteFile post-commit failed: {Url}", manualSertifikatUrl); }
        }

        TempData["Success"] = "Assessment manual berhasil dihapus.";
    }
    catch (DbUpdateException ex)
    {
        // Phase 325 P05 D-05: catch specific DbUpdateException preserved verbatim.
        // Phase 331 D-07: using var tx disposal auto-rollback — no explicit RollbackAsync needed.
        _logger.LogWarning(ex, "Delete failed for AssessmentSession (manual) {Id}", session.Id);
        TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";
    }
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
```

**Catatan presisi:**
- Pre-check renewal HARUS tetap INSIDE `try` block tapi OUTSIDE tx scope (sebelum `using var tx = ...`).
- `using var tx` HARUS deklarasi SETELAH pre-check `referencingTr + referencingAs > 0` check.
- `tx.CommitAsync()` HARUS dipanggil SETELAH audit log, SEBELUM `FileUploadHelper.DeleteFile`.
- `FileUploadHelper.DeleteFile` HARUS dibungkus inner `try { } catch (Exception ex) { _logger.LogWarning(...); }` — JANGAN throw, JANGAN rollback.
- Guard `if (!string.IsNullOrEmpty(manualSertifikatUrl))` redundan dengan idempotency FileUploadHelper.DeleteFile, TAPI tetap dipakai untuk avoid unnecessary helper call + log noise.
- `TempData["Success"]` di-set SETELAH `FileUploadHelper.DeleteFile` (sukses-atau-warn) masih INSIDE outer try block.
- Catch `DbUpdateException` di L828 verbatim preserved: `_logger.LogWarning(ex, "Delete failed for AssessmentSession (manual) {Id}", session.Id);` + `TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar.";`
- TIDAK pakai explicit `tx.RollbackAsync()` di catch.
- TIDAK ada perubahan ke L789-793 (attribute trio + signature).
- TIDAK ada konflik file dengan Task 1 (range L795-833 berbeda dari L561-604) — Task 1 + Task 2 bisa dieksekusi sequential dalam Wave 1 tanpa merge conflict di file yang sama.
  </action>

  <verify>
    <automated>grep -n "Phase 331 D-02: tx wrap" Controllers/TrainingAdminController.cs && grep -n "FileUploadHelper.DeleteFile post-commit failed" Controllers/TrainingAdminController.cs && grep -n "Assessment manual dihapus" Controllers/TrainingAdminController.cs</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "string? manualSertifikatUrl = session.ManualSertifikatUrl;" Controllers/TrainingAdminController.cs → 1 match (capture URL baru di DeleteManualAssessment)
    - grep -c "using var tx = await _context.Database.BeginTransactionAsync();" Controllers/TrainingAdminController.cs → 2 match (DeleteTraining Task 1 + DeleteManualAssessment Task 2)
    - grep -c "await tx.CommitAsync();" Controllers/TrainingAdminController.cs → 2 match
    - grep -n "FileUploadHelper.DeleteFile post-commit failed" Controllers/TrainingAdminController.cs → 1 match
    - grep -n "Delete failed for AssessmentSession (manual)" Controllers/TrainingAdminController.cs → 1 match (catch L828 preserved)
    - grep -c "Phase 331 D-02: tx wrap" Controllers/TrainingAdminController.cs → 2 match (annotation di kedua endpoint)
    - Pre-check renewal `RenewsSessionId` masih di posisi semula (sebelum `using var tx = ...`)
    - Catch DbUpdateException untuk DeleteManualAssessment masih ada (Phase 325 P05 preserved verbatim)
  </acceptance_criteria>

  <done>DeleteManualAssessment L793-834 di TrainingAdminController.cs: file URL captured SEBELUM Remove, Remove+SaveChanges+AuditLog ter-wrap dalam `using var tx = await _context.Database.BeginTransactionAsync();`, `FileUploadHelper.DeleteFile` dieksekusi POST `CommitAsync` dengan inner try/catch warn-only. Pre-check renewal Phase 325 P05 + catch DbUpdateException Phase 325 P05 preserved verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify build + test + grep AC + manual smoke + IT_NOTIFY append + commit + SUMMARY</name>
  <files>docs/IT_NOTIFY.md, .planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-01-SUMMARY.md</files>

  <read_first>
    1. docs/IT_NOTIFY.md (baca entry terakhir — Phase 330 — untuk format append Phase 331)
    2. .planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-CONTEXT.md (D-08, D-09 acceptance criteria lengkap)
    3. C:/Users/Administrator/.claude/get-shit-done/templates/summary.md (template SUMMARY.md)
  </read_first>

  <action>
**[A] Build verification**

Jalankan:
```bash
dotnet build
```
Expected: 0 error. Jika ada compile error → kembali ke Task 1 atau Task 2 sesuai file/baris yang error sebelum lanjut.

**[B] Test suite (no regression Phase 330 baseline)**

Jalankan:
```bash
dotnet test --no-build
```
Expected: 18/18 pass (baseline dari Phase 330). Tidak boleh ada regresi.

**[C] Grep-based AC verification (D-09 items 1-4)**

Jalankan grep pattern berikut sebagai verifikasi mekanis tanpa spin server:
```bash
# AC-1 + AC-2: tx wrap di kedua endpoint
grep -c "BeginTransactionAsync" Controllers/TrainingAdminController.cs   # expected ≥ 2
grep -c "await tx.CommitAsync();" Controllers/TrainingAdminController.cs  # expected 2

# AC-1: DeleteTraining File.Delete POST commit dengan inner catch
grep -n "File.Delete post-commit failed" Controllers/TrainingAdminController.cs   # expected 1 match

# AC-2: DeleteManualAssessment FileUploadHelper.DeleteFile POST commit dengan inner catch
grep -n "FileUploadHelper.DeleteFile post-commit failed" Controllers/TrainingAdminController.cs   # expected 1 match

# AC-3: Pre-check renewal preserved di posisi semula
grep -c "referencingTr \+ referencingAs > 0" Controllers/TrainingAdminController.cs   # expected 2

# AC-4: Catch DbUpdateException Phase 325 P05 preserved verbatim
grep -c "catch (DbUpdateException ex)" Controllers/TrainingAdminController.cs   # expected ≥ 2 (1 DeleteTraining + 1 DeleteManualAssessment)
grep -c "Gagal hapus: ada constraint database" Controllers/TrainingAdminController.cs   # expected 2
```

Semua harus menghasilkan match sesuai expected. Kalau ada mismatch → revisit Task 1 atau Task 2.

**[D] Manual smoke (D-09 item 7) — 3 skenario per endpoint via lokal `dotnet run`**

Spin server lokal (`dotnet run`, port 5277), login admin@pertamina.com (lihat MEMORY reference_dev_credentials.md), navigate ke `/Admin/ManageAssessment?tab=training`:

**DeleteTraining (3 skenario):**
1. **Sukses:** Pilih TrainingRecord yang tidak punya renewal child + ada SertifikatUrl di disk. Klik Delete → konfirmasi. Expected: `TempData["Success"] = "Training record berhasil dihapus."` + row hilang dari tabel + file fisik hilang dari `wwwroot/{SertifikatUrl}`.
2. **Pre-check fail:** Pilih TrainingRecord yang dipakai sebagai sumber renewal (`RenewsTrainingId` exists di TR atau AS lain). Klik Delete. Expected: `TempData["Error"] = "Tidak bisa hapus: {N} sertifikat lain ..."` + row tetap + file tetap di disk.
3. **DB FK violation simulasi:** Pilih TrainingRecord (boleh kosong renewal), lalu DI DB lokal langsung buat FK constraint baru ke TR.Id (atau temporarily drop tx-isolation guard via SQL `INSERT` ke tabel anak yang punya FK ke TR). Klik Delete. Expected: `catch (DbUpdateException)` hit → `TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar."` + row tetap + **file tetap di disk** (membuktikan tx rollback + reorder file delete POST commit).

**DeleteManualAssessment (3 skenario serupa):**
1. **Sukses:** Pilih AssessmentSession `IsManualEntry=true` tanpa renewal child + `ManualSertifikatUrl` ada di disk. Klik Delete. Expected: success message + row hilang + file fisik hilang.
2. **Pre-check fail:** Pilih session yang dipakai sebagai sumber renewal (`RenewsSessionId` exists). Expected: error message + row + file tetap.
3. **DB FK violation simulasi:** Sama pola Task 1 skenario 3 untuk AS. Expected: catch hit → error message + row tetap + file tetap di disk.

Catat hasil 6 skenario ke SUMMARY.md di langkah [F].

**[E] Append docs/IT_NOTIFY.md — per D-08 (bundle v19.0)**

Baca docs/IT_NOTIFY.md, append section berikut di bawah entry Phase 330:

```markdown
## Phase 331 — Fix Cascade DeleteTraining + DeleteManualAssessment Atomicity (file-DB tx wrap)

**Commit:** [commit hash setelah git commit di langkah F]
**Date shipped local:** 2026-05-28
**Flag migration:** NO — zero schema change, zero migration, zero service injection

### Files Modified
- `Controllers/TrainingAdminController.cs` — DeleteTraining (L559-605) + DeleteManualAssessment (L793-834): capture file path/url SEBELUM Remove, `using var tx = await _context.Database.BeginTransactionAsync();` wrap Remove+SaveChanges+AuditLog, `File.Delete` / `FileUploadHelper.DeleteFile` dipindah POST `CommitAsync` dengan inner try/catch warn-only. Pre-check renewal Phase 325 P05 + catch DbUpdateException Phase 325 P05 preserved verbatim.

### Closes Phase 328 Audit Findings
- §4.1 DeleteTraining HIGH (D2 file-DB atomicity + D7 no-tx) → CLOSED
- §4.2 DeleteManualAssessment HIGH (D2 file-DB atomicity + D7 no-tx) → CLOSED
- §9 proposal #1 (`fix-cascade-deletetraining-deletemanualassessment-atomicity`) → CLOSED

### Action Required by IT
Bundle v19.0 batch push (Phase 325+326+327+329+330+331) — tunggu user release push lock per Phase 327 option-b. Tidak ada migration DB. Tidak ada perubahan config. Tidak ada seed data.
```

**[F] Git commit**

```bash
git add Controllers/TrainingAdminController.cs docs/IT_NOTIFY.md
git commit -m "feat(331): cascade atomicity DeleteTraining + DeleteManualAssessment (tx wrap + File.Delete post-commit)"
```

Setelah commit, catat commit hash → update entry IT_NOTIFY.md Phase 331 (ganti `[commit hash setelah git commit di langkah F]` dengan hash aktual, lalu `git add docs/IT_NOTIFY.md && git commit --amend --no-edit` atau commit terpisah `docs(331): record commit hash in IT_NOTIFY`).

**[G] Generate SUMMARY.md**

Buat file `.planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-01-SUMMARY.md` mengikuti template `C:/Users/Administrator/.claude/get-shit-done/templates/summary.md`. Isi minimal:
- Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-08)
- Files modified table (Controllers/TrainingAdminController.cs ~80 LoC delta + docs/IT_NOTIFY.md)
- D-09 AC table 8/8 checklist (build, test, grep counters, smoke 6/6, commit, SUMMARY)
- Manual smoke 6/6 outcome (DeleteTraining 3 skenario + DeleteManualAssessment 3 skenario, file-on-disk state setelah catch hit)
- Grep marker counts (BeginTransactionAsync 2, CommitAsync 2, "File.Delete post-commit failed" 1, "FileUploadHelper.DeleteFile post-commit failed" 1, "referencingTr + referencingAs > 0" 2, "catch (DbUpdateException ex)" ≥ 2)
- Threat model disposition table 4/4 (T-331-01..04)
- Closes Phase 328 §4.1 + §4.2 + §9 proposal #1
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331 ≈ 65 commit) menunggu user release push lock (Phase 327 option-b)
  </action>

  <verify>
    <automated>dotnet build 2>&1 | tail -5 && dotnet test --no-build 2>&1 | grep -E "passed|failed|Total"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build: 0 error (Build succeeded)
    - dotnet test --no-build: 18 passed, 0 failed (atau lebih jika ada test baru dari phase lain — tidak boleh ada regresi)
    - grep -c "BeginTransactionAsync" Controllers/TrainingAdminController.cs → ≥ 2
    - grep -c "await tx.CommitAsync();" Controllers/TrainingAdminController.cs → 2
    - grep -n "File.Delete post-commit failed" Controllers/TrainingAdminController.cs → 1 match
    - grep -n "FileUploadHelper.DeleteFile post-commit failed" Controllers/TrainingAdminController.cs → 1 match
    - grep -c "referencingTr \+ referencingAs > 0" Controllers/TrainingAdminController.cs → 2 (pre-check L573 + L807 dipertahankan)
    - grep -c "catch (DbUpdateException ex)" Controllers/TrainingAdminController.cs → ≥ 2 (1 DeleteTraining + 1 DeleteManualAssessment)
    - grep -c "Gagal hapus: ada constraint database" Controllers/TrainingAdminController.cs → 2
    - Manual smoke 6/6 dilaksanakan + outcome dicatat di SUMMARY.md (3 skenario × 2 endpoint)
    - git log --oneline -1 → commit message mengandung "feat(331): cascade atomicity DeleteTraining + DeleteManualAssessment"
    - docs/IT_NOTIFY.md mengandung section "Phase 331" dengan commit hash terisi
    - 331-01-SUMMARY.md file ada di direktori phase dengan D-09 8/8 AC checklist + smoke 6/6
  </acceptance_criteria>

  <done>Build bersih, 18/18 test pass, semua 8 AC dari D-09 terverifikasi (grep mekanis + manual smoke 6 skenario), IT_NOTIFY.md diupdate Phase 331 entry + commit hash terisi, commit dibuat dengan message per D-09 item 8, SUMMARY.md digenerate dengan status SHIPPED LOCAL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller | POST request dengan anti-forgery token; `[ValidateAntiForgeryToken]` + `[Authorize(Roles = "Admin, HC")]` aktif di kedua endpoint |
| Controller → Database | EF Core DbUpdateException boundary — Phase 325 P05 catch sudah di-place, Phase 331 tambah tx wrap untuk atomicity Remove+Save+AuditLog |
| Controller → Filesystem | `_env.WebRootPath` + `record.SertifikatUrl` / `session.ManualSertifikatUrl` — sekarang dieksekusi POST commit dengan inner try/catch warn-only |
| Controller → AuditLog | `_auditLog.LogAsync` call yang sekarang INSIDE tx scope — kalau fail, tx auto-rollback (DB consistent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-331-01 | D (Denial of Service) | DeleteTraining/DeleteManualAssessment file deletion BEFORE SaveChanges | mitigate | Reorder file delete POST `CommitAsync` — kalau SaveChanges fail, file masih ada di disk (no orphan-delete). Kalau File.Delete fail post-commit, inner try/catch log warning + lanjut (DB sudah consistent) |
| T-331-02 | (Atomicity violation, mapped to T) | DeleteTraining/DeleteManualAssessment partial state (file gone but row alive) | mitigate | `using var tx = await _context.Database.BeginTransactionAsync();` wrap Remove+SaveChanges+AuditLog. Disposal auto-rollback DB jika exception escape sebelum CommitAsync. File.Delete POST commit = file delete hanya kalau DB sukses commit |
| T-331-03 | R (Repudiation) | Audit log non-atomic dengan Remove (jika audit fail tapi Remove sukses) | mitigate | Audit log `_auditLog.LogAsync` dipindah INSIDE tx scope SEBELUM `CommitAsync`. Kalau audit fail → exception escape → tx rollback (Remove ikut batal). Tidak ada window record-gone-but-audit-missing |
| T-331-04 | I (Information Disclosure) | DbUpdateException raw to client (mis. nama tabel/constraint) | mitigate | Catch DbUpdateException Phase 325 P05 (L598 + L828) PRESERVED verbatim — `_logger.LogWarning` server-side + `TempData["Error"] = "Gagal hapus: ada constraint database yang dilanggar."` generic friendly. ex.Message tidak dikirim ke client |
</threat_model>

<verification>
Verifikasi akhir keseluruhan Phase 331:

1. **Build clean:** `dotnet build` → 0 error
2. **Test suite:** `dotnet test --no-build` → 18/18 pass (baseline Phase 330, no regression)
3. **Grep AC mekanis:**
   - `grep -c "BeginTransactionAsync" Controllers/TrainingAdminController.cs` → ≥ 2
   - `grep -c "await tx.CommitAsync();" Controllers/TrainingAdminController.cs` → 2
   - `grep -n "File.Delete post-commit failed" Controllers/TrainingAdminController.cs` → 1 match
   - `grep -n "FileUploadHelper.DeleteFile post-commit failed" Controllers/TrainingAdminController.cs` → 1 match
   - `grep -c "referencingTr \+ referencingAs > 0" Controllers/TrainingAdminController.cs` → 2 (pre-check preserved)
   - `grep -c "catch (DbUpdateException ex)" Controllers/TrainingAdminController.cs` → ≥ 2
   - `grep -c "Gagal hapus: ada constraint database" Controllers/TrainingAdminController.cs` → 2 (Phase 325 P05 preserved)
4. **Manual smoke 6/6:**
   - DeleteTraining: sukses (file gone) + pre-check fail (file stays) + DB FK violation (file stays, tx rollback)
   - DeleteManualAssessment: sukses (file gone) + pre-check fail (file stays) + DB FK violation (file stays, tx rollback)
5. **IT_NOTIFY:** `grep "Phase 331" docs/IT_NOTIFY.md` → 1 section dengan commit hash terisi
6. **Commit:** `git log --oneline -1` → mengandung "feat(331): cascade atomicity DeleteTraining + DeleteManualAssessment"
7. **SUMMARY:** `ls .planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-01-SUMMARY.md` → file ada
</verification>

<success_criteria>
Phase 331 selesai ketika:
- [ ] DeleteTraining L559-605 di-refactor sesuai D-02 + D-03 (tx wrap + File.Delete POST commit)
- [ ] DeleteManualAssessment L793-834 di-refactor sesuai D-02 + D-03 (tx wrap + FileUploadHelper.DeleteFile POST commit)
- [ ] Pre-check renewal L568-580 + L802-805 preserved di POSISI semula (OUTSIDE tx)
- [ ] Catch DbUpdateException L598 + L828 preserved verbatim (warn + TempData["Error"])
- [ ] `dotnet build` clean (0 error)
- [ ] `dotnet test --no-build` 18/18 pass (no regression Phase 330 baseline)
- [ ] Manual smoke 6 skenario PASS (3 per endpoint: sukses + pre-check fail + DB FK violation)
- [ ] Grep AC mekanis 7 counter terpenuhi
- [ ] docs/IT_NOTIFY.md diupdate dengan Phase 331 entry + commit hash terisi
- [ ] 1 commit dengan message `feat(331): cascade atomicity DeleteTraining + DeleteManualAssessment (tx wrap + File.Delete post-commit)`
- [ ] 331-01-SUMMARY.md digenerate
- [ ] Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-08)
</success_criteria>

<output>
Setelah Task 3 selesai, buat `.planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-01-SUMMARY.md` dengan isi:
- Status: SHIPPED LOCAL
- Files modified (Controllers/TrainingAdminController.cs ~80 LoC delta + docs/IT_NOTIFY.md)
- AC D-09 table 8/8 checklist
- Grep marker count verification (7 counter)
- Manual smoke 6/6 outcome table
- Threat model disposition 4/4 (T-331-01..04)
- Closes Phase 328 §4.1 + §4.2 + §9 proposal #1
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331 ≈ 65 commit) menunggu user release push lock (Phase 327 option-b)
</output>
