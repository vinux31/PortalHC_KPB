---
phase: 332-fix-cascade-deletebagian-file-atomicity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/DocumentAdminController.cs
  - docs/IT_NOTIFY.md
autonomous: true
requirements:
  - D-01-DeleteBagian-tx-wrap
  - D-02-DeleteBagian-file-post-commit
  - D-03-DeleteBagian-catch-DbUpdateException
must_haves:
  truths:
    - "DeleteBagian: RemoveRange(KKJ) + RemoveRange(CPDP) + Remove(Bagian) + SaveChangesAsync + AuditLog ter-wrap dalam BeginTransactionAsync tx, CommitAsync mendahului File.Delete loops"
    - "DeleteBagian: System.IO.File.Delete (KKJ + CPDP loops) dieksekusi POST CommitAsync dengan inner try/catch warn-only per file (log + lanjut iterate, tidak break + tidak rollback)"
    - "Pre-check active files L289-302 (BLOCK Json) + confirm dialog L308-317 (needsConfirm Json) preserved verbatim di POSISI semula, OUTSIDE tx scope"
    - "Audit log L353-364 (inner try/catch wrap _auditLog.LogAsync) preserved verbatim INSIDE tx scope, SEBELUM CommitAsync"
    - "Catch DbUpdateException baru → Json success=false friendly: 'Gagal hapus bagian: ada constraint database yang dilanggar.'"
    - "Tidak ada record DB (OrganizationUnit + KkjFiles + CpdpFiles archived) yang ter-Remove kalau Save fail (atomicity dijamin via tx auto-rollback by using disposal)"
    - "Tidak ada file di disk yang ter-Delete kalau Save fail (reorder mencegah orphan-delete)"
    - "AJAX-only Json response pattern preserved (5 existing branches: notfound + blocked + needsConfirm + success + new DbUpdateException catch)"
    - "dotnet build clean, dotnet test 18/18 pass — tidak ada regresi dari Phase 331 baseline"
  artifacts:
    - path: "Controllers/DocumentAdminController.cs"
      provides: "DeleteBagian refactored: tx wrap RemoveRange+Remove+Save+Audit + File.Delete (KKJ+CPDP) POST commit + catch DbUpdateException baru"
      contains: "BeginTransactionAsync"
    - path: "docs/IT_NOTIFY.md"
      provides: "Phase 332 IT notification entry (bundle v19.0 append)"
      contains: "Phase 332"
  key_links:
    - from: "Controllers/DocumentAdminController.cs"
      to: "DeleteBagian L283-367"
      via: "using var tx = await _context.Database.BeginTransactionAsync(); ... await tx.CommitAsync(); ... System.IO.File.Delete loops"
      pattern: "File\\.Delete post-commit failed \\((KKJ|CPDP)\\)"
    - from: "Controllers/DocumentAdminController.cs"
      to: "Pre-check active files L289-302 + confirm dialog L308-317"
      via: "CountAsync !IsArchived + IsArchived OUTSIDE tx, early return Json blocked / needsConfirm"
      pattern: "totalActive > 0|totalArchived > 0 && !confirmed"
    - from: "Controllers/DocumentAdminController.cs"
      to: "Catch DbUpdateException baru"
      via: "catch (DbUpdateException ex) → Json success=false friendly"
      pattern: "Gagal hapus bagian: ada constraint database"
---

<objective>
Tutup 1 HIGH finding Phase 328 Cascade Audit Sweep (§4.7 + §9 proposal #3): file-DB atomicity di `DeleteBagian` via `BeginTransactionAsync` wrap + reorder `System.IO.File.Delete` (2 collection KKJ + CPDP archived) POST `CommitAsync` + tambah catch DbUpdateException. Perubahan ini bersifat mechanical controller-only — tidak ada migration, tidak ada schema change, tidak ada service injection, tidak ada view change.

Purpose: Menghilangkan window orphan-delete (file hilang dari disk tapi row DB masih ada saat Save fail) dan menjamin atomicity RemoveRange(KKJ)+RemoveRange(CPDP)+Remove(Bagian)+SaveChanges+AuditLog dalam satu tx commit. Pre-check active files (L289-302 BLOCK) + confirm dialog (L308-317 needsConfirm) + audit log inner try/catch (L353-364) tetap dipertahankan verbatim. Catch DbUpdateException BARU ditambahkan (sebelumnya tidak ada — raw 500 risk).

Output: 1 file controller dimodifikasi (~50 LoC delta, 1 endpoint), `docs/IT_NOTIFY.md` diupdate dengan Phase 332 entry, 1 commit bertag pesan standar D-10 item 8, SUMMARY.md digenerate.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-CONTEXT.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
@.planning/phases/331-fix-cascade-deletetraining-deletemanualassessment-atomicity/331-01-PLAN.md

<interfaces>
<!-- Kontrak yang digunakan executor — tidak perlu eksplorasi codebase lebih jauh. -->

Dari Controllers/AdminBaseController.cs (base class DocumentAdminController):
```csharp
protected readonly ApplicationDbContext _context;
protected readonly UserManager<ApplicationUser> _userManager;
protected readonly AuditLogService _auditLog;
protected readonly IWebHostEnvironment _env;
protected readonly ILogger _logger;   // tersedia (Phase 325 P05 pattern)
```

Dari Microsoft.EntityFrameworkCore (sudah imported di DocumentAdminController.cs):
```csharp
DbContext.Database.BeginTransactionAsync();   // returns IDbContextTransaction
IDbContextTransaction.CommitAsync();
// using var tx = ...; → disposal otomatis rollback jika exception escape sebelum CommitAsync
```

AuditLog signature yang sudah dipakai (verbatim L359-362):
```csharp
await _auditLog.LogAsync(
    string userId, string actorName, string action,
    string detail, int entityId, string entityType);
// action: "DeleteBagian", entityType: "OrganizationUnit"
```

State kode aktual sebelum fix (verbatim dari Controllers/DocumentAdminController.cs L283-367):

**DeleteBagian L283-367** (target Task 1):
- L283: signature `public async Task<IActionResult> DeleteBagian(int id, bool confirmed = false)`
- L285-287: `var bagianEntity = await _context.OrganizationUnits.FindAsync(id);` + null check → Json notfound
- L289-291: CountAsync activeKkjCount + activeCpdpCount + totalActive
- L293-302: `if (totalActive > 0)` → Json blocked (preserve verbatim)
- L304-306: CountAsync archivedKkjCount + archivedCpdpCount + totalArchived
- L308-317: `if (totalArchived > 0 && !confirmed)` → Json needsConfirm (preserve verbatim)
- L319-321: `archivedKkjFiles = ToListAsync()`
- L322-330: foreach archivedKkjFiles → File.Delete ← SEBELUM Save (D2 broken)
- L331-332: `if (archivedKkjFiles.Any()) RemoveRange(archivedKkjFiles)`
- L334-336: `archivedCpdpFiles = ToListAsync()`
- L337-345: foreach archivedCpdpFiles → File.Delete ← SEBELUM Save (D2 broken)
- L346-347: `if (archivedCpdpFiles.Any()) RemoveRange(archivedCpdpFiles)`
- L349: `_context.OrganizationUnits.Remove(bagianEntity);`
- L350: `await _context.SaveChangesAsync();`
- L352-364: audit log dengan inner try/catch (preserve verbatim — Phase 332 D-06)
- L366: `return Json(new { success = true, message = $"Bagian '{bagianEntity.Name}' berhasil dihapus." });`
- **NO catch DbUpdateException existing** — Phase 332 TAMBAH (D-03)
- **NO BeginTransactionAsync existing** — Phase 332 TAMBAH (D-02)

AJAX response patterns existing (preserve all 5 branches + add 6th):
1. notfound: `Json(new { success = false, message = "Bagian tidak ditemukan." })` (L287)
2. blocked: `Json(new { success = false, blocked = true, message = ... })` (L295-301)
3. needsConfirm: `Json(new { success = false, needsConfirm = true, archivedCount = ..., message = ... })` (L310-316)
4. success: `Json(new { success = true, message = ... })` (L366)
5. **NEW** DbUpdateException catch: `Json(new { success = false, message = "Gagal hapus bagian: ada constraint database yang dilanggar." })`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor DeleteBagian — extract path lists, tx wrap RemoveRange+Remove+Save+Audit, reorder File.Delete loops POST commit, add catch DbUpdateException</name>
  <files>Controllers/DocumentAdminController.cs</files>

  <read_first>
    1. Controllers/DocumentAdminController.cs L280-367 (DeleteBagian — baca state saat ini lengkap)
    2. .planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-CONTEXT.md (D-02 tx scope, D-03 path collection, D-04 reorder + catch, D-05 pre-check preserve, D-06 audit preserve, D-07 file warn-only)
    3. .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md §4.7 (DeleteBagian root cause + remediation D2+D6+D7)
  </read_first>

  <action>
**Refactor `DeleteBagian` (L283-367) — per D-02 + D-03 + D-04 + D-05 + D-06 + D-07**

Ganti seluruh body method DeleteBagian dari L285 (sesudah signature `{`) sampai L366 (sebelum closing brace method) dengan struktur berikut. Reuse semua kode existing verbatim — hanya extract path string lists baru + reorder File.Delete loops + tambah tx wrap + tambah catch DbUpdateException.

```csharp
public async Task<IActionResult> DeleteBagian(int id, bool confirmed = false)
{
    var bagianEntity = await _context.OrganizationUnits.FindAsync(id);
    if (bagianEntity == null)
        return Json(new { success = false, message = "Bagian tidak ditemukan." });

    var activeKkjCount = await _context.KkjFiles.CountAsync(f => f.OrganizationUnitId == id && !f.IsArchived);
    var activeCpdpCount = await _context.CpdpFiles.CountAsync(f => f.OrganizationUnitId == id && !f.IsArchived);
    var totalActive = activeKkjCount + activeCpdpCount;

    if (totalActive > 0)
    {
        return Json(new
        {
            success = false,
            blocked = true,
            message = $"Bagian ini memiliki {totalActive} file aktif (KKJ: {activeKkjCount}, CPDP: {activeCpdpCount}). " +
                      $"Arsipkan atau hapus semua file aktif terlebih dahulu sebelum menghapus bagian ini."
        });
    }

    var archivedKkjCount = await _context.KkjFiles.CountAsync(f => f.OrganizationUnitId == id && f.IsArchived);
    var archivedCpdpCount = await _context.CpdpFiles.CountAsync(f => f.OrganizationUnitId == id && f.IsArchived);
    var totalArchived = archivedKkjCount + archivedCpdpCount;

    if (totalArchived > 0 && !confirmed)
    {
        return Json(new
        {
            success = false,
            needsConfirm = true,
            archivedCount = totalArchived,
            message = $"Bagian '{bagianEntity.Name}' memiliki {totalArchived} file arsip yang akan ikut terhapus permanen (KKJ: {archivedKkjCount}, CPDP: {archivedCpdpCount}). Tindakan ini tidak dapat dibatalkan."
        });
    }

    // Phase 332 D-03: collect archived KKJ + CPDP entities + file paths SEBELUM tx — string list value-typed, safe post-commit
    var archivedKkjFiles = await _context.KkjFiles
        .Where(f => f.OrganizationUnitId == id && f.IsArchived)
        .ToListAsync();
    var kkjPaths = archivedKkjFiles
        .Where(f => !string.IsNullOrEmpty(f.FilePath))
        .Select(f => Path.Combine(_env.WebRootPath, f.FilePath.TrimStart('/')))
        .ToList();

    var archivedCpdpFiles = await _context.CpdpFiles
        .Where(f => f.OrganizationUnitId == id && f.IsArchived)
        .ToListAsync();
    var cpdpPaths = archivedCpdpFiles
        .Where(f => !string.IsNullOrEmpty(f.FilePath))
        .Select(f => Path.Combine(_env.WebRootPath, f.FilePath.TrimStart('/')))
        .ToList();

    // Phase 332 D-02: tx wrap RemoveRange + Remove + SaveChanges + AuditLog. Disposal auto-rollback jika exception escape sebelum CommitAsync.
    using var tx = await _context.Database.BeginTransactionAsync();
    try
    {
        if (archivedKkjFiles.Any()) _context.KkjFiles.RemoveRange(archivedKkjFiles);
        if (archivedCpdpFiles.Any()) _context.CpdpFiles.RemoveRange(archivedCpdpFiles);
        _context.OrganizationUnits.Remove(bagianEntity);
        await _context.SaveChangesAsync();

        // Phase 332 D-06: Audit log INSIDE tx — preserve existing pattern L353-364 verbatim (inner try/catch warn-only)
        var currentUser = await _userManager.GetUserAsync(User);
        try
        {
            var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP)
                ? (currentUser?.FullName ?? "Unknown")
                : $"{currentUser.NIP} - {currentUser.FullName}";
            await _auditLog.LogAsync(
                currentUser?.Id ?? "", actorName, "DeleteBagian",
                $"Deleted bagian '{bagianEntity.Name}' (ID {id}). Cascaded {totalArchived} archived file(s) (KKJ: {archivedKkjCount}, CPDP: {archivedCpdpCount}).",
                id, "OrganizationUnit");
        }
        catch (Exception ex) { _logger.LogWarning(ex, "Audit log failed for DeleteBagian (bagianId={Id})", id); }

        await tx.CommitAsync();
    }
    catch (DbUpdateException ex)
    {
        // Phase 332 D-03 + D-04: catch specific DbUpdateException — tx auto-rollback via using disposal
        _logger.LogWarning(ex, "Delete failed for Bagian (bagianId={Id})", id);
        return Json(new { success = false, message = "Gagal hapus bagian: ada constraint database yang dilanggar." });
    }

    // Phase 332 D-04 + D-07: File.Delete loops POST CommitAsync dengan inner try/catch warn-only per file
    foreach (var path in kkjPaths)
    {
        try { if (System.IO.File.Exists(path)) System.IO.File.Delete(path); }
        catch (Exception ex) { _logger.LogWarning(ex, "File.Delete post-commit failed (KKJ): {Path}", path); }
    }
    foreach (var path in cpdpPaths)
    {
        try { if (System.IO.File.Exists(path)) System.IO.File.Delete(path); }
        catch (Exception ex) { _logger.LogWarning(ex, "File.Delete post-commit failed (CPDP): {Path}", path); }
    }

    return Json(new { success = true, message = $"Bagian '{bagianEntity.Name}' berhasil dihapus." });
}
```

**Catatan presisi:**
- **Pre-check active files L289-302** (BLOCK Json) TIDAK disentuh — preserve verbatim di POSISI semula OUTSIDE tx scope. Early return Json sebelum file collection + tx creation = tidak ada tx leak.
- **Confirm dialog L308-317** (needsConfirm Json) TIDAK disentuh — preserve verbatim di POSISI semula OUTSIDE tx scope.
- **archivedKkjFiles ToListAsync + archivedCpdpFiles ToListAsync** sudah ada existing L319-321 + L334-336 — PERTAHANKAN, hanya extract path string ke `kkjPaths` + `cpdpPaths` lists baru sebelum tx (Path.Combine + TrimStart pattern verbatim dari L326 + L342).
- **Existing foreach File.Delete L322-330 + L337-345** DIHAPUS dari posisi sekarang — akan jadi loop baru POST commit (di luar tx, setelah `tx.CommitAsync()`).
- **RemoveRange L332 + L347** → pindah masuk tx scope (di dalam `try { ... }` setelah `using var tx = ...`).
- **Remove(bagianEntity) L349 + SaveChanges L350** → pindah masuk tx scope.
- **Audit log block L353-364** (variable `currentUser` + inner try/catch wrap `_auditLog.LogAsync`) → pindah masuk tx scope SEBELUM `await tx.CommitAsync();`. Preserved verbatim — body block tidak diubah, hanya dipindah posisi.
- **`using var tx = await _context.Database.BeginTransactionAsync();`** deklarasi SETELAH path collection (kkjPaths + cpdpPaths), SEBELUM `try {`.
- **`await tx.CommitAsync();`** dipanggil SETELAH audit log, di akhir `try` block.
- **`catch (DbUpdateException ex)`** outer → return Json success=false friendly. tx auto-rollback via `using` disposal — TIDAK pakai explicit `tx.RollbackAsync()`.
- **File.Delete loops** dibungkus inner `try { if (File.Exists) File.Delete } catch (Exception ex) { _logger.LogWarning(...) }` per file — JANGAN throw, JANGAN break loop, JANGAN rollback (tx sudah commit).
- **Log message distinct** untuk KKJ vs CPDP: `"File.Delete post-commit failed (KKJ): {Path}"` vs `"File.Delete post-commit failed (CPDP): {Path}"` — agar grep verifikasi bisa bedakan 2 loops.
- **Final `return Json(new { success = true, ... });`** tetap di akhir method, SETELAH 2 File.Delete loops.
- **TIDAK ada perubahan ke L280-282** (attribute `[HttpPost]` + `[Authorize(Roles = "Admin, HC")]` + `[ValidateAntiForgeryToken]` + signature).
- **TIDAK ada import baru** — `Microsoft.EntityFrameworkCore` (DbUpdateException + BeginTransactionAsync), `System.IO.File`, `Path` semua sudah tersedia di file.
  </action>

  <verify>
    <automated>grep -n "Phase 332 D-02: tx wrap" Controllers/DocumentAdminController.cs && grep -n "File.Delete post-commit failed (KKJ)" Controllers/DocumentAdminController.cs && grep -n "File.Delete post-commit failed (CPDP)" Controllers/DocumentAdminController.cs && grep -n "Gagal hapus bagian: ada constraint database" Controllers/DocumentAdminController.cs</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "BeginTransactionAsync" Controllers/DocumentAdminController.cs → 1 match (new DeleteBagian tx)
    - grep -c "tx.CommitAsync" Controllers/DocumentAdminController.cs → 1 match
    - grep -n "kkjPaths" Controllers/DocumentAdminController.cs → ≥ 2 match (declaration + foreach loop)
    - grep -n "cpdpPaths" Controllers/DocumentAdminController.cs → ≥ 2 match (declaration + foreach loop)
    - grep -n "File.Delete post-commit failed (KKJ)" Controllers/DocumentAdminController.cs → 1 match
    - grep -n "File.Delete post-commit failed (CPDP)" Controllers/DocumentAdminController.cs → 1 match
    - grep -c "catch (DbUpdateException" Controllers/DocumentAdminController.cs → 1 match (new catch)
    - grep -n "Gagal hapus bagian: ada constraint database" Controllers/DocumentAdminController.cs → 1 match
    - grep -c "totalActive > 0" Controllers/DocumentAdminController.cs → 1 match (pre-check L293 preserved)
    - grep -c "needsConfirm = true" Controllers/DocumentAdminController.cs → 1 match (confirm dialog L312 preserved)
    - grep -c "Audit log failed for DeleteBagian" Controllers/DocumentAdminController.cs → 1 match (inner audit catch preserved verbatim)
    - grep -c "berhasil dihapus" Controllers/DocumentAdminController.cs → 1 match (success Json preserved)
    - File DeleteBagian masih punya 4 cabang Json existing (notfound + blocked + needsConfirm + success) PLUS 1 cabang baru (DbUpdateException catch) — total 5 Json returns
  </acceptance_criteria>

  <done>DeleteBagian L283-367 di DocumentAdminController.cs: kkjPaths + cpdpPaths string lists captured SEBELUM tx (di luar tx scope), RemoveRange(KKJ)+RemoveRange(CPDP)+Remove(Bagian)+SaveChanges+AuditLog ter-wrap dalam `using var tx = await _context.Database.BeginTransactionAsync();` + `try { ... await tx.CommitAsync(); }`, catch DbUpdateException baru return Json success=false friendly, 2 File.Delete loops (KKJ + CPDP) dieksekusi POST CommitAsync dengan inner try/catch warn-only per file (log distinct KKJ vs CPDP), final success Json di akhir method. Pre-check active files L289-302 + confirm dialog L308-317 + audit log L353-364 preserved verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify build + test + grep AC + IT_NOTIFY append + commit + SUMMARY</name>
  <files>docs/IT_NOTIFY.md, .planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-01-SUMMARY.md</files>

  <read_first>
    1. docs/IT_NOTIFY.md (baca entry terakhir — Phase 331 — untuk format append Phase 332)
    2. .planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-CONTEXT.md (D-09 IT_NOTIFY strategy, D-10 acceptance criteria lengkap)
    3. C:/Users/Administrator/.claude/get-shit-done/templates/summary.md (template SUMMARY.md)
  </read_first>

  <action>
**[A] Build verification (D-10 item 5)**

Jalankan:
```bash
dotnet build
```
Expected: 0 error CS*. Jika ada compile error → kembali ke Task 1 sesuai baris yang error sebelum lanjut.

**[B] Test suite (D-10 item 6, no regression Phase 331 baseline)**

Jalankan:
```bash
dotnet test --no-build
```
Expected: 18/18 pass (baseline dari Phase 331). Tidak boleh ada regresi.

**[C] Grep-based AC verification (D-10 items 1-4)**

Jalankan grep pattern berikut sebagai verifikasi mekanis tanpa spin server:
```bash
# AC-1: tx wrap baru di DeleteBagian
grep -c "BeginTransactionAsync" Controllers/DocumentAdminController.cs   # expected 1
grep -c "tx.CommitAsync" Controllers/DocumentAdminController.cs           # expected 1

# AC-1: path collection variables baru
grep -n "kkjPaths" Controllers/DocumentAdminController.cs                 # expected ≥ 2 match
grep -n "cpdpPaths" Controllers/DocumentAdminController.cs                # expected ≥ 2 match

# AC-1: 2 File.Delete loops POST commit dengan inner catch (distinct log KKJ vs CPDP)
grep -n "File.Delete post-commit failed (KKJ)" Controllers/DocumentAdminController.cs   # expected 1 match
grep -n "File.Delete post-commit failed (CPDP)" Controllers/DocumentAdminController.cs  # expected 1 match

# AC-2: pre-check active + confirm dialog preserved
grep -c "totalActive > 0" Controllers/DocumentAdminController.cs          # expected 1 (preserved L293)
grep -c "needsConfirm = true" Controllers/DocumentAdminController.cs      # expected 1 (preserved L312)

# AC-3: audit log inner try/catch preserved verbatim
grep -c "Audit log failed for DeleteBagian" Controllers/DocumentAdminController.cs   # expected 1

# AC-4: catch DbUpdateException baru + friendly message
grep -c "catch (DbUpdateException" Controllers/DocumentAdminController.cs              # expected 1 (new)
grep -n "Gagal hapus bagian: ada constraint database" Controllers/DocumentAdminController.cs   # expected 1 match

# AC bonus: success Json final preserved
grep -c "berhasil dihapus" Controllers/DocumentAdminController.cs         # expected 1
```

Semua harus menghasilkan match sesuai expected. Kalau ada mismatch → revisit Task 1.

**[D] Manual smoke (D-10 item 7) — DEFERRED ke Dev promo**

Per D-10 item 7: Manual smoke (delete Bagian zero archived + delete Bagian dengan 2 KKJ + 1 CPDP archived + simulasi DB FK violation) deferred ke Dev promo by IT. Tidak dilaksanakan di lokal Phase 332 — sufficient via grep AC mekanis + build + test pass. Catat di SUMMARY.md sebagai "Manual smoke: DEFERRED to Dev promo (D-10 item 7 explicit)".

**[E] Append docs/IT_NOTIFY.md — per D-09 (bundle v19.0)**

Baca docs/IT_NOTIFY.md, append section berikut di bawah entry Phase 331:

```markdown
## Phase 332 — Fix Cascade DeleteBagian File Atomicity (file-DB tx wrap KKJ+CPDP)

**Commit:** [commit hash setelah git commit di langkah F]
**Date shipped local:** 2026-05-28
**Flag migration:** NO — zero schema change, zero migration, zero service injection

### Files Modified
- `Controllers/DocumentAdminController.cs` — DeleteBagian (L283-367): extract `kkjPaths` + `cpdpPaths` string lists SEBELUM tx, `using var tx = await _context.Database.BeginTransactionAsync();` wrap RemoveRange(KKJ) + RemoveRange(CPDP) + Remove(Bagian) + SaveChanges + AuditLog, catch DbUpdateException baru → Json success=false friendly, 2 File.Delete loops (KKJ + CPDP) dipindah POST `CommitAsync` dengan inner try/catch warn-only per file (log distinct KKJ vs CPDP). Pre-check active files (L289-302) + confirm dialog (L308-317) + audit log inner try/catch (L353-364) preserved verbatim.

### Closes Phase 328 Audit Findings
- §4.7 DeleteBagian HIGH (D2 file-DB atomicity + D6 no-catch-DbUpdateException + D7 no-tx) → CLOSED
- §9 proposal #3 (`fix-cascade-deletebagian-file-atomicity`) → CLOSED

### Action Required by IT
Bundle v19.0 batch push (Phase 325+326+327+329+330+331+332) — tunggu user release push lock per Phase 327 option-b. Tidak ada migration DB. Tidak ada perubahan config. Tidak ada seed data. Manual smoke deferred ke Dev promo (D-10 item 7): test 3 skenario di Dev (zero archived sukses + 2 KKJ + 1 CPDP cascade sukses + DB FK violation simulasi catch hit).
```

**[F] Git commit (D-10 item 8)**

```bash
git add Controllers/DocumentAdminController.cs docs/IT_NOTIFY.md
git commit -m "feat(332): cascade atomicity DeleteBagian (tx wrap + File.Delete post-commit KKJ+CPDP)"
```

Setelah commit, catat commit hash → update entry IT_NOTIFY.md Phase 332 (ganti `[commit hash setelah git commit di langkah F]` dengan hash aktual, lalu `git add docs/IT_NOTIFY.md && git commit --amend --no-edit` atau commit terpisah `docs(332): record commit hash in IT_NOTIFY`).

**[G] Generate SUMMARY.md (D-10 item 9)**

Buat file `.planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-01-SUMMARY.md` mengikuti template `C:/Users/Administrator/.claude/get-shit-done/templates/summary.md`. Isi minimal:
- Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-09)
- Files modified table (Controllers/DocumentAdminController.cs ~50 LoC delta + docs/IT_NOTIFY.md)
- D-10 AC table 9/9 checklist (build, test, grep counters, manual smoke deferred, IT_NOTIFY, commit, SUMMARY)
- Grep marker counts table:
  - BeginTransactionAsync: 1
  - tx.CommitAsync: 1
  - kkjPaths: ≥ 2
  - cpdpPaths: ≥ 2
  - "File.Delete post-commit failed (KKJ)": 1
  - "File.Delete post-commit failed (CPDP)": 1
  - "catch (DbUpdateException": 1
  - "Gagal hapus bagian: ada constraint database": 1
  - "totalActive > 0": 1
  - "needsConfirm = true": 1
  - "Audit log failed for DeleteBagian": 1
  - "berhasil dihapus": 1
- Threat model disposition table 4/4 (T-332-01..04)
- Closes Phase 328 §4.7 + §9 proposal #3
- Manual smoke status: DEFERRED to Dev promo (per D-10 item 7)
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331+332 ≈ 67 commit) menunggu user release push lock (Phase 327 option-b)
  </action>

  <verify>
    <automated>dotnet build 2>&1 | tail -5 && dotnet test --no-build 2>&1 | grep -E "passed|failed|Total"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build: 0 error (Build succeeded)
    - dotnet test --no-build: 18 passed, 0 failed (atau lebih jika ada test baru dari phase lain — tidak boleh ada regresi)
    - grep -c "BeginTransactionAsync" Controllers/DocumentAdminController.cs → 1
    - grep -c "tx.CommitAsync" Controllers/DocumentAdminController.cs → 1
    - grep -n "kkjPaths" Controllers/DocumentAdminController.cs → ≥ 2 match
    - grep -n "cpdpPaths" Controllers/DocumentAdminController.cs → ≥ 2 match
    - grep -n "File.Delete post-commit failed (KKJ)" Controllers/DocumentAdminController.cs → 1 match
    - grep -n "File.Delete post-commit failed (CPDP)" Controllers/DocumentAdminController.cs → 1 match
    - grep -c "catch (DbUpdateException" Controllers/DocumentAdminController.cs → 1
    - grep -n "Gagal hapus bagian: ada constraint database" Controllers/DocumentAdminController.cs → 1 match
    - grep -c "totalActive > 0" Controllers/DocumentAdminController.cs → 1 (pre-check preserved)
    - grep -c "needsConfirm = true" Controllers/DocumentAdminController.cs → 1 (confirm dialog preserved)
    - grep -c "Audit log failed for DeleteBagian" Controllers/DocumentAdminController.cs → 1 (audit inner catch preserved verbatim)
    - Manual smoke status DEFERRED dicatat di SUMMARY.md (per D-10 item 7)
    - git log --oneline -1 → commit message mengandung "feat(332): cascade atomicity DeleteBagian"
    - docs/IT_NOTIFY.md mengandung section "Phase 332" dengan commit hash terisi
    - 332-01-SUMMARY.md file ada di direktori phase dengan D-10 9/9 AC checklist
  </acceptance_criteria>

  <done>Build bersih, 18/18 test pass, semua 9 AC dari D-10 terverifikasi (grep mekanis 12 counter + manual smoke deferred per explicit D-10 item 7), IT_NOTIFY.md diupdate Phase 332 entry + commit hash terisi, commit dibuat dengan message per D-10 item 8, SUMMARY.md digenerate dengan status SHIPPED LOCAL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller | POST AJAX request dengan anti-forgery token; `[ValidateAntiForgeryToken]` + `[Authorize(Roles = "Admin, HC")]` aktif di DeleteBagian endpoint |
| Controller → Database | EF Core DbUpdateException boundary — Phase 332 TAMBAH catch DbUpdateException baru (sebelumnya NO catch — raw 500 risk) + BeginTransactionAsync wrap untuk atomicity RemoveRange(KKJ) + RemoveRange(CPDP) + Remove(Bagian) + Save + AuditLog |
| Controller → Filesystem (2 collection: KKJ + CPDP) | `_env.WebRootPath` + `f.FilePath` — sekarang dieksekusi POST commit via 2 separate foreach loops (kkjPaths + cpdpPaths) dengan inner try/catch warn-only per file (log distinct KKJ vs CPDP) |
| Controller → AuditLog | `_auditLog.LogAsync` call yang sekarang INSIDE tx scope SEBELUM CommitAsync — inner try/catch warn-only preserved verbatim (kalau audit fail, log warning + continue commit — tidak rollback DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-332-01 | D (Denial of Service) | DeleteBagian file deletion (2 collection KKJ + CPDP) BEFORE SaveChanges → orphan-delete bila Save fail | mitigate | Reorder 2 File.Delete loops (KKJ + CPDP) POST `CommitAsync` — kalau SaveChanges fail, semua archived files masih ada di disk (no orphan-delete). Kalau File.Delete fail post-commit, inner try/catch log warning + iterate file berikutnya (DB sudah consistent, orphan-on-disk acceptable manual cleanup) |
| T-332-02 | T (Atomicity violation, mapped to T - Tampering) | DeleteBagian multi-entity partial state (KKJ rows gone but Bagian alive, or Bagian gone but CPDP rows alive) | mitigate | `using var tx = await _context.Database.BeginTransactionAsync();` wrap RemoveRange(KKJ) + RemoveRange(CPDP) + Remove(Bagian) + SaveChanges + AuditLog. Disposal auto-rollback DB jika exception escape sebelum CommitAsync. Tidak ada window partial-state across 3 entity types. File.Delete POST commit = file delete hanya kalau DB sukses commit |
| T-332-03 | R (Repudiation) | Audit log non-atomic dengan RemoveRange + Remove (jika audit fail tapi Remove sukses → record gone tapi log missing) | mitigate | Audit log `_auditLog.LogAsync` (dengan inner try/catch warn-only) dipindah INSIDE tx scope SEBELUM `CommitAsync`. Inner catch preserved verbatim (Phase 332 D-06): kalau audit fail → log warning + continue commit (acceptable per established pattern). Inner catch JANGAN throw → tidak escape ke outer catch → tx tetap commit (audit best-effort, tidak boleh block DB delete) |
| T-332-04 | I (Information Disclosure) | DbUpdateException raw 500 to client (mis. nama tabel/constraint terlihat di error response — sebelum Phase 332 NO catch existed!) | mitigate | Catch DbUpdateException BARU ditambahkan Phase 332 D-03 — `_logger.LogWarning` server-side + `Json(new { success = false, message = "Gagal hapus bagian: ada constraint database yang dilanggar." })` generic friendly. ex.Message + ex.StackTrace tidak dikirim ke client. Sebelumnya endpoint NO catch → raw 500 dengan EF detail leak ke browser |
</threat_model>

<verification>
Verifikasi akhir keseluruhan Phase 332:

1. **Build clean:** `dotnet build` → 0 error
2. **Test suite:** `dotnet test --no-build` → 18/18 pass (baseline Phase 331, no regression)
3. **Grep AC mekanis (12 counter):**
   - `grep -c "BeginTransactionAsync" Controllers/DocumentAdminController.cs` → 1
   - `grep -c "tx.CommitAsync" Controllers/DocumentAdminController.cs` → 1
   - `grep -n "kkjPaths" Controllers/DocumentAdminController.cs` → ≥ 2 match
   - `grep -n "cpdpPaths" Controllers/DocumentAdminController.cs` → ≥ 2 match
   - `grep -n "File.Delete post-commit failed (KKJ)" Controllers/DocumentAdminController.cs` → 1 match
   - `grep -n "File.Delete post-commit failed (CPDP)" Controllers/DocumentAdminController.cs` → 1 match
   - `grep -c "catch (DbUpdateException" Controllers/DocumentAdminController.cs` → 1 (new)
   - `grep -n "Gagal hapus bagian: ada constraint database" Controllers/DocumentAdminController.cs` → 1 match
   - `grep -c "totalActive > 0" Controllers/DocumentAdminController.cs` → 1 (pre-check L293 preserved)
   - `grep -c "needsConfirm = true" Controllers/DocumentAdminController.cs` → 1 (confirm dialog L312 preserved)
   - `grep -c "Audit log failed for DeleteBagian" Controllers/DocumentAdminController.cs` → 1 (audit inner catch preserved)
   - `grep -c "berhasil dihapus" Controllers/DocumentAdminController.cs` → 1 (success Json preserved)
4. **Manual smoke:** DEFERRED to Dev promo (per D-10 item 7 explicit) — tidak dilaksanakan di lokal Phase 332
5. **IT_NOTIFY:** `grep "Phase 332" docs/IT_NOTIFY.md` → 1 section dengan commit hash terisi
6. **Commit:** `git log --oneline -1` → mengandung "feat(332): cascade atomicity DeleteBagian"
7. **SUMMARY:** `ls .planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-01-SUMMARY.md` → file ada
</verification>

<success_criteria>
Phase 332 selesai ketika:
- [ ] DeleteBagian L283-367 di-refactor sesuai D-02 + D-03 + D-04 (tx wrap RemoveRange+Remove+Save+Audit + 2 File.Delete loops POST commit + catch DbUpdateException baru)
- [ ] Pre-check active files L289-302 + confirm dialog L308-317 preserved verbatim di POSISI semula (OUTSIDE tx)
- [ ] Audit log block L353-364 (inner try/catch wrap) preserved verbatim INSIDE tx scope (sebelum CommitAsync)
- [ ] Catch DbUpdateException BARU return Json success=false friendly (sebelumnya NO catch existed)
- [ ] `dotnet build` clean (0 error CS*)
- [ ] `dotnet test --no-build` 18/18 pass (no regression Phase 331 baseline)
- [ ] Manual smoke: DEFERRED to Dev promo (per D-10 item 7 — tidak blocking ship lokal)
- [ ] Grep AC mekanis 12 counter terpenuhi
- [ ] docs/IT_NOTIFY.md diupdate dengan Phase 332 entry + commit hash terisi
- [ ] 1 commit dengan message `feat(332): cascade atomicity DeleteBagian (tx wrap + File.Delete post-commit KKJ+CPDP)`
- [ ] 332-01-SUMMARY.md digenerate
- [ ] Status: SHIPPED LOCAL (NOT PUSHED — bundle v19.0 per D-09)
</success_criteria>

<output>
Setelah Task 2 selesai, buat `.planning/phases/332-fix-cascade-deletebagian-file-atomicity/332-01-SUMMARY.md` dengan isi:
- Status: SHIPPED LOCAL
- Files modified (Controllers/DocumentAdminController.cs ~50 LoC delta + docs/IT_NOTIFY.md)
- AC D-10 table 9/9 checklist
- Grep marker count verification (12 counter — BeginTransactionAsync, tx.CommitAsync, kkjPaths, cpdpPaths, 2 distinct File.Delete post-commit logs, catch DbUpdateException, Gagal hapus bagian, totalActive, needsConfirm, Audit log failed, berhasil dihapus)
- Manual smoke status: DEFERRED to Dev promo (D-10 item 7 explicit)
- Threat model disposition 4/4 (T-332-01..04)
- Closes Phase 328 §4.7 + §9 proposal #3
- Next steps: bundle push v19.0 (Phase 325+326+327+329+330+331+332 ≈ 67 commit) menunggu user release push lock (Phase 327 option-b)
</output>
