---
phase: 335-fix-cascade-deleteworker-renewal-files-tx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/WorkerController.cs
  - docs/IT_NOTIFY.md
autonomous: true
requirements:
  - D-01-DeleteWorker-d5-precheck
  - D-02-DeleteWorker-d2-file-collection
  - D-03-DeleteWorker-d7-tx-wrap
  - D-04-DeleteWorker-catch-friendly
must_haves:
  truths:
    - "DeleteWorker: D5 cross-user renewal pre-check INSERTED setelah user lookup L506 SEBELUM userName capture L508 — 3 query (userTrainingIds + crossUserTrReferences CountAsync + crossUserAsReferences CountAsync filter UserId != id), block via TempData friendly + early return ManageWorkers kalau totalCrossRefs > 0, TANPA tx creation"
    - "DeleteWorker: D2 file path collection re-query userTrainingRecords + userAssessmentSessions (full objects) INSIDE tx SEBELUM 9-step RemoveRange, allFilePaths List<string> populated dari TrainingRecord.SertifikatUrl + AssessmentSession.ManualSertifikatUrl + ProtonDeliverableProgress.EvidencePath + JSON parse EvidencePathHistory inner try/catch warn-only — TrainingRecord TIDAK PUNYA ManualSertifikatUrl (verified Models/TrainingRecord.cs L40 hanya SertifikatUrl)"
    - "DeleteWorker: D7 tx wrap using var tx = await _context.Database.BeginTransactionAsync() membungkus existing 9-step RemoveRange (PackageUserResponses + UserPackageAssignments + ProtonDeliverableProgresses + ProtonFinalAssessments + ProtonTrackAssignments + ProtonNotifications + CoachCoacheeMappings + CoachingSessions + CoachingLogs) + SaveChangesAsync + UserManager.DeleteAsync + audit log + tx.CommitAsync — single atomic scope"
    - "DeleteWorker: UserManager.DeleteAsync INSIDE tx scope per D-11 assumption (Program.cs L47 AddEntityFrameworkStores<ApplicationDbContext>() confirmed → Identity store pakai DbContext yang sama via DI scoped lifetime → join active transaction)"
    - "DeleteWorker: Identity error path (result.Succeeded == false) early return INSIDE try block via TempData friendly + return RedirectToAction — tx tidak commit → using var disposal auto-rollback (NO eksplisit RollbackAsync, NO throw)"
    - "DeleteWorker: D4 catch refactor — catch (DbUpdateException dbEx) FIRST + catch (Exception ex) SECOND, masing-masing LogWarning/LogError ke server + TempData friendly Json-free message TANPA + ex.Message (no info leak), NO eksplisit RollbackAsync (using disposal handles), NO throw"
    - "DeleteWorker: File.Delete loop POST tx.CommitAsync SEBELUM TempData[Success] dengan inner try/catch warn-only per file (log distinct 'File.Delete post-commit failed (Worker file): {Path}'), cross-platform path normalize Replace('/', Path.DirectorySeparatorChar)"
    - "DeleteWorker: Self-deletion guard L491-499 + [Authorize(Roles=\"Admin, HC\")] L485 + [ValidateAntiForgeryToken] L486 + null id BadRequest L489 + user lookup null-check L501-506 preserved verbatim OUTSIDE tx"
    - "DeleteWorker: audit log block (try/catch warn-only L591-602) preserved verbatim INSIDE tx setelah UserManager success — TIDAK lagi nested di if (result.Succeeded) branch lama, dipindah ke after Identity early-return check"
    - "DeleteWorker: 9-step RemoveRange order preserved verbatim INSIDE tx (PackageUserResponses + UserPackageAssignments conditional dalam userAssessmentIds.Any() → ProtonDeliverableProgresses → ProtonFinalAssessments → ProtonTrackAssignments → ProtonNotifications → CoachCoacheeMappings → CoachingSessions → CoachingLogs → SaveChangesAsync → UserManager.DeleteAsync)"
    - "dotnet build clean, dotnet test --no-build 18/18 pass — tidak ada regresi dari Phase 334 baseline"
  artifacts:
    - path: "Controllers/WorkerController.cs"
      provides: "DeleteWorker L487-612 refactored: D5 pre-check cross-user renewal (3 query block sebelum tx) + D2 file path collection re-query TR + AS + collect dari ProtonProgress + EvidencePath + JSON history (INSIDE tx sebelum RemoveRange) + D7 tx wrap (BeginTransactionAsync membungkus 9-step RemoveRange + SaveChanges + UserManager.DeleteAsync + audit + CommitAsync) + D4 catch refactor (DbUpdateException + Exception fallback friendly NO ex.Message NO RollbackAsync NO throw) + File.Delete POST CommitAsync warn-only per file"
      contains: "BeginTransactionAsync"
    - path: "docs/IT_NOTIFY.md"
      provides: "Phase 335 IT notification entry (bundle v19.0 final phase append) + smoke scenario #13 multi-skenario (cross-user renewal pre-check block + file orphan cleanup + tx atomicity + Identity error rollback + D6 info leak verification)"
      contains: "Phase 335"
  key_links:
    - from: "Controllers/WorkerController.cs"
      to: "DeleteWorker L487 D5 pre-check block"
      via: "userTrainingIds query (UserId == id) + CountAsync TR (UserId != id AND RenewsXxxId IN user ids) + CountAsync AS (UserId != id AND RenewsXxxId IN user ids) → TempData Error friendly + return RedirectToAction kalau totalCrossRefs > 0, SEBELUM `using var tx`"
      pattern: "Tidak bisa hapus pekerja"
    - from: "Controllers/WorkerController.cs"
      to: "DeleteWorker D7 tx wrap + UserManager INSIDE"
      via: "using var tx = await _context.Database.BeginTransactionAsync(); try { ... 9-step RemoveRange + SaveChangesAsync + UserManager.DeleteAsync (Identity early-return INSIDE try) + audit log + tx.CommitAsync(); } catch (DbUpdateException dbEx) { ... } catch (Exception ex) { ... }"
      pattern: "BeginTransactionAsync"
    - from: "Controllers/WorkerController.cs"
      to: "DeleteWorker D2 file collection + File.Delete POST commit"
      via: "List<string> allFilePaths populate INSIDE tx (TR.SertifikatUrl + AS.ManualSertifikatUrl + ProtonProgress.EvidencePath + JsonSerializer.Deserialize EvidencePathHistory inner try/catch) → tx.CommitAsync() → foreach allFilePaths File.Delete inner try/catch warn-only → TempData Success"
      pattern: "File\\.Delete post-commit failed \\(Worker file\\)"
    - from: "Controllers/WorkerController.cs"
      to: "DeleteWorker D4 catch refactor friendly NO info leak"
      via: "catch DbUpdateException → LogWarning + TempData generic constraint; catch Exception → LogError + TempData generic internal. NO + ex.Message di TempData. NO eksplisit tx.RollbackAsync. NO throw."
      pattern: "Gagal hapus pekerja:"
    - from: "Program.cs L47"
      to: "DeleteWorker D-11 UserManager tx assumption"
      via: ".AddEntityFrameworkStores<ApplicationDbContext>() → Identity UserStore pakai ApplicationDbContext yang sama via DI scoped → UserManager.DeleteAsync INSIDE tx join active transaction"
      pattern: "AddEntityFrameworkStores<ApplicationDbContext>"
---

<objective>
Tutup 1 HIGH triple-dim finding Phase 328 Cascade Audit Sweep §4.3 + §9 proposal #6: `DeleteWorker` triple-fix D2 (orphan files SertifikatUrl + ManualSertifikatUrl + EvidencePath + EvidencePathHistory) + D5 (cross-user renewal cascade silent break = FK NoAction violation 500 dari Worker B side) + D7 (no tx atomicity untuk 9-step RemoveRange + UserManager.DeleteAsync). Strategi mengikuti pattern progression v19.0 milestone: Phase 325 P05 (renewal pre-check same-user) EXTENDED ke cross-user `UserId != id` + Phase 331/332/333/334 (file collection + File.Delete POST commit + tx wrap + catch friendly) PLUS new pattern UserManager.DeleteAsync INSIDE tx scope (per D-11 verified via Program.cs L47 AddEntityFrameworkStores config).

**KEY DIFFERENCES dari Phase 334:**
1. **Triple-fix tunggal phase** — D2 (file orphan) + D5 (cross-user renewal) + D7 (no tx) digabung 1 plan 3 task karena 1 endpoint, sama-sama mechanical refactor.
2. **D5 cross-user renewal pre-check BARU** — extension Phase 325 P05 dengan filter `UserId != id` (Worker B's TR/AS reference Worker A's TR/AS as renewal source). Block strategy (UX clarity + audit trail per D-02), TIDAK null-clear. Position SEBELUM tx creation (early return TempData friendly).
3. **D7 tx wrap dengan UserManager.DeleteAsync INSIDE tx** — Phase 334 tidak ada UserManager interaction. Phase 335 assumption D-11: Identity store via `AddEntityFrameworkStores<ApplicationDbContext>()` → same `_context` per DI scoped → join active transaction. **VERIFIED**: `grep AddEntityFrameworkStores Program.cs` → L47 match `.AddEntityFrameworkStores<ApplicationDbContext>()`. Safe to proceed.
4. **Identity error path BUKAN exception** — `result.Succeeded == false` (IdentityResult) → early return INSIDE try block dengan TempData friendly + return RedirectToAction. Tx tidak commit → using var disposal auto-rollback. NO throw, NO eksplisit RollbackAsync. Beda dari Phase 334 yang catch-only pattern.
5. **File path collection 3-source (multi-source)** — TR.SertifikatUrl (DIVERIFIED Models/TrainingRecord.cs L40 hanya SertifikatUrl, **TIDAK PUNYA ManualSertifikatUrl**) + AS.ManualSertifikatUrl (Models/AssessmentSession.cs L136) + ProtonProgress.EvidencePath + JSON history (Models/ProtonModels.cs L101 + L142). Eksklusi: TR.ManualSertifikatUrl TIDAK EXIST → skip collection untuk source ini.
6. **D6 info leak: current Identity error UX preserved** — D-06 explicit `[auto]`: Identity result.Errors.Description display di TempData ACCEPTABLE (Identity errors usually user-actionable "User has logins" — bukan DB constraint leak). Hanya catch DbUpdateException + Exception fallback yang generic friendly NO `+ ex.Message`. Berbeda dengan Phase 334 yang HAPUS `+ ex.Message` total — Phase 335 fokus pada DbUpdateException/Exception catch saja, Identity error path tetap tampilkan errors.Description.
7. **9-step cascade preserve verbatim INSIDE tx** — bukan reorder, hanya WRAP. Same RemoveRange order L513-582 dipindah INSIDE tx body.
8. **TempData+Redirect response** (BUKAN Json) — endpoint MVC standard ManageWorkers page, bukan AJAX. Mirror Phase 331/332 pattern.

Purpose: Menutup final HIGH triple-dim finding v19.0 — orphan disk files (sertifikat + evidence + history JSON gone dari DB cascade tapi tetap di disk wwwroot) + cascade fail 500 cross-user renewal silent (Worker B kehilangan sertifikat sumber, tidak ada explicit user feedback) + partial-state risk Identity fail mid-cascade (TR/AS terhapus tapi User row gagal delete → orphan IdentityUser context). v19.0 = 11/11 phase 100% post Phase 335 ship.

Output: 1 file controller dimodifikasi (~110 LoC delta total: ~30 pre-check + ~80 tx wrap + file collection + catch refactor), `docs/IT_NOTIFY.md` diupdate dengan Phase 335 entry final v19.0 + scenario #13 multi-skenario, 1 commit bertag pesan standar D-09 item 11, SUMMARY.md digenerate dengan catatan v19.0 milestone close.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-CONTEXT.md
@.planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md
@.planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-PLAN.md
@.planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-SUMMARY.md
@.planning/phases/331-fix-cascade-deletetraining-tx-files/331-01-PLAN.md

<interfaces>
<!-- Kontrak yang digunakan executor — tidak perlu eksplorasi codebase lebih jauh. -->

Dari Controllers/WorkerController.cs (base ApplicationDbContext + DI yang sudah tersedia):
```csharp
private readonly ApplicationDbContext _context;
private readonly UserManager<ApplicationUser> _userManager;
private readonly AuditLogService _auditLog;
private readonly IWebHostEnvironment _env;
private readonly ILogger<WorkerController> _logger;
```

Dari Microsoft.EntityFrameworkCore (sudah imported di WorkerController.cs):
```csharp
DbContext.Database.BeginTransactionAsync();   // returns IDbContextTransaction
IDbContextTransaction.CommitAsync();
// using var tx = ...; → synchronous disposal otomatis rollback jika exception escape sebelum CommitAsync (NO eksplisit RollbackAsync needed)
DbUpdateException;   // catch FIRST untuk constraint violation friendly
```

Dari Microsoft.AspNetCore.Identity (sudah imported via UserManager DI):
```csharp
UserManager<ApplicationUser>.DeleteAsync(ApplicationUser user) → Task<IdentityResult>
IdentityResult.Succeeded (bool)
IdentityResult.Errors → IEnumerable<IdentityError> dengan Description (preserve display per D-06)
```

Verified via Program.cs L47:
```csharp
.AddEntityFrameworkStores<ApplicationDbContext>()
```
→ Identity UserStore pakai ApplicationDbContext yang sama via DI scoped lifetime → UserManager.DeleteAsync INSIDE tx scope join active transaction (D-11 assumption confirmed).

AuditLog signature verbatim L593-600 (PRESERVE INSIDE tx setelah UserManager success):
```csharp
var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP) ? (currentUser?.FullName ?? "Unknown") : $"{currentUser.NIP} - {currentUser.FullName}";
await _auditLog.LogAsync(
    currentUser.Id,
    actorName,
    "DeleteWorker",
    $"Deleted user '{userName}' ({userEmail})",
    null,
    "ApplicationUser");
```

Entity properties yang dipakai (VERIFIED via grep Models/):
```csharp
TrainingRecord.UserId (string, L10)
TrainingRecord.RenewsTrainingId (int?, L62)
TrainingRecord.RenewsSessionId (int?, L68)
TrainingRecord.SertifikatUrl (string?, L40)
// ⚠️ TrainingRecord.ManualSertifikatUrl TIDAK EXIST — skip collection (only AssessmentSession has ManualSertifikatUrl)

AssessmentSession.UserId (string, L10)
AssessmentSession.RenewsSessionId (int?, L116)
AssessmentSession.RenewsTrainingId (int?, L123)
AssessmentSession.ManualSertifikatUrl (string?, L136)

ProtonDeliverableProgress.CoacheeId (string, L93)
ProtonDeliverableProgress.EvidencePath (string?, L101)
ProtonDeliverableProgress.EvidencePathHistory (string? JSON List<string>, L142)
```

State kode aktual sebelum fix (verbatim dari Controllers/WorkerController.cs L483-612):

**DeleteWorker L487-612** (target Task 1+2):
- L483: `// POST /Admin/DeleteWorker` komentar — preserve.
- L484-486: `[HttpPost]` + `[Authorize(Roles = "Admin, HC")]` + `[ValidateAntiForgeryToken]` — preserve verbatim.
- L487: signature `public async Task<IActionResult> DeleteWorker(string id)` — preserve.
- L489: `if (string.IsNullOrEmpty(id)) return BadRequest();` — preserve verbatim OUTSIDE tx.
- L491-492: `var currentUser = await _userManager.GetUserAsync(User);` + `if (currentUser == null) return Challenge();` — preserve verbatim OUTSIDE tx.
- L494-499: self-deletion guard `if (currentUser.Id == id) { TempData["Error"] = ...; return RedirectToAction("ManageWorkers"); }` — preserve verbatim OUTSIDE tx.
- L501-506: user lookup `var user = await _userManager.FindByIdAsync(id);` + null check `if (user == null) { TempData["Error"] = "User tidak ditemukan."; return RedirectToAction("ManageWorkers"); }` — preserve verbatim OUTSIDE tx.
- **TASK 1 INSERT POSITION**: SETELAH L506 user null-check, SEBELUM L508 userName capture. INSERT D5 cross-user renewal pre-check block (3 query + totalCrossRefs block + TempData + return RedirectToAction).
- L508-509: `var userName = user.FullName;` + `var userEmail = user.Email;` — preserve verbatim.
- **TASK 2 BEGIN POSITION**: SETELAH L509 userEmail capture. INSERT file path collection (re-query TR + AS) + List<string> allFilePaths populate (TR.SertifikatUrl + AS.ManualSertifikatUrl, SKIP TR.ManualSertifikatUrl karena tidak exist) + `using var tx = await _context.Database.BeginTransactionAsync();` + `try {` block.
- L513-516: `var userAssessmentIds = await _context.AssessmentSessions.Where(a => a.UserId == id).Select(a => a.Id).ToListAsync();` — preserve verbatim INSIDE tx body (re-declare di dalam tx scope karena pre-check butuh `userAssessmentIdsForPrecheck` terpisah OUTSIDE tx).
- L518-531: PackageUserResponses + UserPackageAssignments conditional RemoveRange — preserve verbatim INSIDE tx.
- L533: komentar `// UserCompetencyLevels removed (Phase 227 CLEN-03 — orphan table dropped)` — preserve.
- L535-540: ProtonDeliverableProgresses load + RemoveRange — **REFACTOR**: INSERT EvidencePath + EvidencePathHistory collection ANTARA `.ToListAsync()` L538 dan `RemoveRange(protonProgress)` L540. allFilePaths.Add per progress.
- L542-547: ProtonFinalAssessments RemoveRange — preserve verbatim INSIDE tx.
- L549-554: ProtonTrackAssignments RemoveRange — preserve verbatim INSIDE tx.
- L556-561: ProtonNotifications RemoveRange — preserve verbatim INSIDE tx.
- L563-568: CoachCoacheeMappings RemoveRange — preserve verbatim INSIDE tx.
- L570-575: CoachingSessions RemoveRange — preserve verbatim INSIDE tx.
- L577-582: CoachingLogs RemoveRange — preserve verbatim INSIDE tx.
- L584: `await _context.SaveChangesAsync();` — preserve verbatim INSIDE tx.
- L586 komentar `// Now delete the user (cascade will handle TrainingRecords, AssessmentSessions, IdpItems)` — preserve.
- L587: `var result = await _userManager.DeleteAsync(user);` — preserve INSIDE tx (D-11 assumption confirmed via Program.cs L47).
- **REFACTOR L588-609**: existing `if (result.Succeeded) { audit log ... TempData Success } else { TempData Error errors.Description }` → split:
  - INSIDE try block: `if (!result.Succeeded) { TempData["Error"] = $"Gagal menghapus user: {string.Join(...)}"; return RedirectToAction("ManageWorkers"); }` (early return, tx disposal auto-rollback — D-06 preserve Identity error UX).
  - Audit log block (try/catch warn-only) preserve verbatim INSIDE tx setelah Identity success check.
  - `await tx.CommitAsync();` — INSERT setelah audit log.
- **CATCH BLOCKS BARU SETELAH try body**:
  - `catch (DbUpdateException dbEx) { _logger.LogWarning(dbEx, "DbUpdate failed for DeleteWorker {Id}", id); TempData["Error"] = "Gagal hapus pekerja: ada constraint database yang dilanggar. Pastikan tidak ada data dependen yang belum dibersihkan."; return RedirectToAction("ManageWorkers"); }`
  - `catch (Exception ex) { _logger.LogError(ex, "Failed to delete worker {Id}", id); TempData["Error"] = "Gagal hapus pekerja: terjadi kesalahan internal. Hubungi admin."; return RedirectToAction("ManageWorkers"); }`
  - NO eksplisit `tx.RollbackAsync()` (using disposal). NO throw.
- **POST tx scope (setelah catch blocks)**:
  - INSERT File.Delete loop: `foreach (var relUrl in allFilePaths) { try { var physical = Path.Combine(_env.WebRootPath, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar)); if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical); } catch (Exception fex) { _logger.LogWarning(fex, "File.Delete post-commit failed (Worker file): {Path}", relUrl); } }`
  - `TempData["Success"] = $"User '{userName}' berhasil dihapus dari sistem.";` — preserve verbatim.
  - `return RedirectToAction("ManageWorkers");` — preserve verbatim.

Pattern yang sudah benar (preserve verbatim):
- Self-deletion guard OUTSIDE tx + Authorization attribute + ValidateAntiForgeryToken + BadRequest null id + currentUser Challenge + user null-check TempData friendly
- 9-step RemoveRange order FK-safe (Restrict-on-AS handled via userAssessmentIds + ProtonProgresses CoacheeId + ProtonFinalAssessments + ProtonTrackAssignments + ProtonNotifications + CoachCoacheeMappings + CoachingSessions + CoachingLogs)
- Audit log nested try/catch warn-only

Method response patterns:
- Existing returns: BadRequest (null id) + Challenge (no currentUser) + TempData Error+Redirect (self-deletion) + TempData Error+Redirect (user not found) + TempData Success+Redirect (sukses)
- **NEW** D5 pre-check block: `TempData["Error"] = $"Tidak bisa hapus pekerja '{user.FullName}': {totalCrossRefs} sertifikat milik pekerja lain..."; return RedirectToAction("ManageWorkers");`
- **PRESERVE** Identity error: `TempData["Error"] = $"Gagal menghapus user: {string.Join(", ", result.Errors.Select(e => e.Description))}";` (D-06 explicit acceptable — Identity errors user-actionable bukan DB constraint leak)
- **NEW** DbUpdateException catch: `TempData["Error"] = "Gagal hapus pekerja: ada constraint database yang dilanggar. Pastikan tidak ada data dependen yang belum dibersihkan."; return RedirectToAction("ManageWorkers");`
- **NEW** Exception fallback catch: `TempData["Error"] = "Gagal hapus pekerja: terjadi kesalahan internal. Hubungi admin."; return RedirectToAction("ManageWorkers");`
- **NO** `+ ex.Message` di DbUpdateException/Exception catch (D6 info leak avoidance — meskipun current DeleteWorker tidak punya pattern ini, future-proof).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: INSERT D5 cross-user renewal pre-check block (3 query + totalCrossRefs block + TempData friendly + early return) SEBELUM userName capture L508</name>
  <files>Controllers/WorkerController.cs</files>

  <read_first>
    1. Controllers/WorkerController.cs L483-612 (DeleteWorker — baca state saat ini lengkap dari attribute sampai closing brace)
    2. .planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-CONTEXT.md (D-02 cross-user pre-check spec)
    3. .planning/phases/328-cascade-audit-sweep-delete-endpoints/328-RESEARCH.md §4.3 (DeleteWorker HIGH D5 cross-user renewal)
    4. Models/TrainingRecord.cs (verify UserId L10 + RenewsTrainingId L62 + RenewsSessionId L68 fields)
    5. Models/AssessmentSession.cs (verify UserId L10 + RenewsSessionId L116 + RenewsTrainingId L123 fields)
  </read_first>

  <action>
**INSERT D5 cross-user renewal pre-check (per D-02)**

Lokasi: SETELAH L506 (user null-check `if (user == null) { ... }` closing brace), SEBELUM L508 (`var userName = user.FullName;`).

Tambahkan block kode berikut PERSIS di posisi tersebut:

```csharp
            // Phase 335 D-02: pre-check cross-user renewal BEFORE tx scope (early return TempData friendly)
            var userTrainingIds = await _context.TrainingRecords
                .Where(t => t.UserId == id)
                .Select(t => t.Id)
                .ToListAsync();
            var userAssessmentIdsForPrecheck = await _context.AssessmentSessions
                .Where(a => a.UserId == id)
                .Select(a => a.Id)
                .ToListAsync();

            var crossUserTrReferences = 0;
            var crossUserAsReferences = 0;
            if (userTrainingIds.Any() || userAssessmentIdsForPrecheck.Any())
            {
                crossUserTrReferences = await _context.TrainingRecords
                    .CountAsync(t => t.UserId != id && (
                        (t.RenewsTrainingId.HasValue && userTrainingIds.Contains(t.RenewsTrainingId.Value)) ||
                        (t.RenewsSessionId.HasValue && userAssessmentIdsForPrecheck.Contains(t.RenewsSessionId.Value))
                    ));
                crossUserAsReferences = await _context.AssessmentSessions
                    .CountAsync(a => a.UserId != id && (
                        (a.RenewsTrainingId.HasValue && userTrainingIds.Contains(a.RenewsTrainingId.Value)) ||
                        (a.RenewsSessionId.HasValue && userAssessmentIdsForPrecheck.Contains(a.RenewsSessionId.Value))
                    ));
            }

            var totalCrossRefs = crossUserTrReferences + crossUserAsReferences;
            if (totalCrossRefs > 0)
            {
                TempData["Error"] = $"Tidak bisa hapus pekerja '{user.FullName}': {totalCrossRefs} sertifikat milik pekerja lain menggunakan sertifikat pekerja ini sebagai sumber renewal. Hapus atau update sertifikat pemakai terlebih dulu.";
                return RedirectToAction("ManageWorkers");
            }

```

**Catatan presisi:**
- Position: tepat SEBELUM `var userName = user.FullName;` (existing L508 dalam baseline pre-fix; post-fix akan geser jadi L508+~30).
- Indentation: 12 spaces (3 level dalam class → method body), match existing L508-509 style.
- `user.FullName` di TempData message: gunakan property `FullName` yang sudah diverifikasi exist via existing L508 capture (`var userName = user.FullName;`).
- NO tx scope — pre-check happens OUTSIDE tx, early return friendly tanpa tx creation overhead.
- 3 query total: (1) userTrainingIds Select(Id) ToList; (2) userAssessmentIdsForPrecheck Select(Id) ToList; (3+4) 2x CountAsync combined OR conditions (TR cross + AS cross). Variable name `userAssessmentIdsForPrecheck` DELIBERATELY distinct dari existing `userAssessmentIds` L513 (yang ada di tx body) untuk avoid scoping conflict — pre-check OUTSIDE tx, body INSIDE tx, both queries justified karena lifetime scope berbeda.
- `userTrainingIds`: same name dipakai di Task 2 (file collection block), bisa di-reuse (declared OUTSIDE tx, accessible INSIDE tx via closure). VERIFY tidak ada re-declare di Task 2 — Task 2 hanya re-query full TrainingRecord objects (`userTrainingRecords`) untuk akses SertifikatUrl, BUKAN re-query IDs.
- Konditional `if (userTrainingIds.Any() || userAssessmentIdsForPrecheck.Any())` mencegah query unnecessary kalau user tidak punya TR/AS sama sekali (CountAsync skip → 0 default).
- Combined OR conditions (RenewsTrainingId || RenewsSessionId) dalam single CountAsync — optimization untuk avoid N+1, 1 query per cross-reference type.
- NO perubahan ke L487-506 (signature + BadRequest + currentUser + Challenge + self-deletion guard + user lookup + user null-check). NO perubahan ke L508+ (userName/userEmail capture + 9-step RemoveRange + UserManager.DeleteAsync + audit + return) DI TASK INI — Task 1 hanya INSERT pre-check block.
- TIDAK ada import baru — `Microsoft.EntityFrameworkCore` (CountAsync) + `System.Linq` (Where/Select/Any/Contains) sudah tersedia (existing code pakai pattern ini di L513-516).
- TIDAK ada perubahan ke method lain dalam WorkerController.cs.
  </action>

  <verify>
    <automated>grep -c "Phase 335 D-02: pre-check cross-user renewal" Controllers/WorkerController.cs && grep -c "crossUserTrReferences" Controllers/WorkerController.cs && grep -c "crossUserAsReferences" Controllers/WorkerController.cs && grep -c "userAssessmentIdsForPrecheck" Controllers/WorkerController.cs && grep -c "Tidak bisa hapus pekerja" Controllers/WorkerController.cs</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "Phase 335 D-02: pre-check cross-user renewal" Controllers/WorkerController.cs -> 1 match
    - grep -c "crossUserTrReferences" Controllers/WorkerController.cs -> >=2 match
    - grep -c "crossUserAsReferences" Controllers/WorkerController.cs -> >=2 match
    - grep -c "userAssessmentIdsForPrecheck" Controllers/WorkerController.cs -> >=3 match
    - grep -c "Tidak bisa hapus pekerja" Controllers/WorkerController.cs -> 1 match
    - grep -c "totalCrossRefs" Controllers/WorkerController.cs -> 2 match
    - File DeleteWorker masih compile-ready (Task 1 standalone block insertion)
  </acceptance_criteria>

  <done>D5 cross-user renewal pre-check block INSERTED di DeleteWorker tepat setelah user null-check L506 + sebelum userName capture L508. 3 query (userTrainingIds + userAssessmentIdsForPrecheck + 2x CountAsync gabung OR conditions) dengan filter UserId != id (cross-user) + Contains IDs source. Block strategy: kalau totalCrossRefs > 0 -> TempData Error friendly Bahasa Indonesia explicit count + return RedirectToAction ManageWorkers tanpa tx creation. Existing L487-506 + L508-612 tetap verbatim - Task 2 akan handle file collection + tx wrap + catch refactor.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REFACTOR DeleteWorker L508-611 - D2 file collection + D7 tx wrap + UserManager INSIDE tx + Identity early-return INSIDE try + catch DbUpdateException/Exception friendly + File.Delete loop POST CommitAsync</name>
  <files>Controllers/WorkerController.cs</files>

  <read_first>
    1. Controllers/WorkerController.cs L483-612 (DeleteWorker state POST Task 1 - pre-check block sudah ada)
    2. .planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-CONTEXT.md (D-03 file collection, D-04 tx wrap + UserManager Identity, D-05 File.Delete POST commit, D-06 catch refactor + Identity error preserve)
    3. .planning/phases/331-fix-cascade-deletetraining-tx-files/331-01-PLAN.md (pattern tx wrap + File.Delete POST commit + catch friendly)
    4. .planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-PLAN.md (pattern multi-source file collection + JSON parse)
    5. Models/ProtonModels.cs L101 + L142 (EvidencePath + EvidencePathHistory schema)
    6. Models/TrainingRecord.cs L40 (SertifikatUrl - verify NO ManualSertifikatUrl exists)
    7. Models/AssessmentSession.cs L136 (ManualSertifikatUrl)
  </read_first>

  <action>
**REFACTOR DeleteWorker body dari L508 (userName capture) sampai L611 (return RedirectToAction)** - per D-03 + D-04 + D-05 + D-06.

Struktur target lengkap (replace existing L508-611, preserve L483-506 + pre-check block Task 1):

```csharp
            var userName = user.FullName;
            var userEmail = user.Email;

            // Phase 335 D-03: re-query TR + AS full objects for file path collection (cascade-delete akan detach)
            var userTrainingRecords = await _context.TrainingRecords
                .Where(t => t.UserId == id)
                .ToListAsync();
            var userAssessmentSessions = await _context.AssessmentSessions
                .Where(a => a.UserId == id)
                .ToListAsync();

            // Phase 335 D-03: build allFilePaths SEBELUM tx - value-typed safe post-commit
            // NOTE: TrainingRecord hanya punya SertifikatUrl (TIDAK punya ManualSertifikatUrl per Models/TrainingRecord.cs L40)
            //       AssessmentSession punya ManualSertifikatUrl (Models/AssessmentSession.cs L136)
            var allFilePaths = new List<string>();
            foreach (var tr in userTrainingRecords)
            {
                if (!string.IsNullOrEmpty(tr.SertifikatUrl)) allFilePaths.Add(tr.SertifikatUrl);
            }
            foreach (var asSession in userAssessmentSessions)
            {
                if (!string.IsNullOrEmpty(asSession.ManualSertifikatUrl)) allFilePaths.Add(asSession.ManualSertifikatUrl);
            }

            using var tx = await _context.Database.BeginTransactionAsync();
            try
            {
                var userAssessmentIds = await _context.AssessmentSessions
                    .Where(a => a.UserId == id)
                    .Select(a => a.Id)
                    .ToListAsync();

                if (userAssessmentIds.Any())
                {
                    var packageUserResponses = await _context.PackageUserResponses
                        .Where(r => userAssessmentIds.Contains(r.AssessmentSessionId))
                        .ToListAsync();
                    if (packageUserResponses.Any())
                        _context.PackageUserResponses.RemoveRange(packageUserResponses);

                    var packageAssignments = await _context.UserPackageAssignments
                        .Where(a => userAssessmentIds.Contains(a.AssessmentSessionId))
                        .ToListAsync();
                    if (packageAssignments.Any())
                        _context.UserPackageAssignments.RemoveRange(packageAssignments);
                }

                // Phase 335 D-03: collect EvidencePath + EvidencePathHistory SEBELUM RemoveRange
                var protonProgress = await _context.ProtonDeliverableProgresses
                    .Where(p => p.CoacheeId == id)
                    .ToListAsync();
                foreach (var p in protonProgress)
                {
                    if (!string.IsNullOrEmpty(p.EvidencePath)) allFilePaths.Add(p.EvidencePath);
                    if (!string.IsNullOrEmpty(p.EvidencePathHistory))
                    {
                        try
                        {
                            var history = System.Text.Json.JsonSerializer
                                .Deserialize<List<string>>(p.EvidencePathHistory) ?? new List<string>();
                            allFilePaths.AddRange(history);
                        }
                        catch (Exception jex)
                        {
                            _logger.LogWarning(jex, "Failed to parse EvidencePathHistory for progress {Pid} (DeleteWorker)", p.Id);
                        }
                    }
                }
                if (protonProgress.Any())
                    _context.ProtonDeliverableProgresses.RemoveRange(protonProgress);

                // ProtonFinalAssessments + ProtonTrackAssignments + ProtonNotifications + CoachCoacheeMappings + CoachingSessions + CoachingLogs (preserve verbatim L542-582 RemoveRange order INSIDE tx)
                // ... [preserve 6 RemoveRange blocks verbatim L542-582]

                await _context.SaveChangesAsync();

                // Phase 335 D-04: UserManager.DeleteAsync INSIDE tx - same ApplicationDbContext via AddEntityFrameworkStores (Program.cs L47)
                var result = await _userManager.DeleteAsync(user);
                if (!result.Succeeded)
                {
                    // Phase 335 D-06: Identity error - early return INSIDE try, tx disposal auto-rollback
                    TempData["Error"] = $"Gagal menghapus user: {string.Join(", ", result.Errors.Select(e => e.Description))}";
                    return RedirectToAction("ManageWorkers");
                }

                // Audit log INSIDE tx (preserve verbatim L591-602)
                try
                {
                    var actorName = string.IsNullOrWhiteSpace(currentUser?.NIP) ? (currentUser?.FullName ?? "Unknown") : $"{currentUser.NIP} - {currentUser.FullName}";
                    await _auditLog.LogAsync(currentUser.Id, actorName, "DeleteWorker",
                        $"Deleted user '{userName}' ({userEmail})", null, "ApplicationUser");
                }
                catch (Exception ex) { _logger.LogWarning(ex, "Audit log failed for DeleteWorker (userId={Id})", id); }

                await tx.CommitAsync();
            }
            catch (DbUpdateException dbEx)
            {
                _logger.LogWarning(dbEx, "DbUpdate failed for DeleteWorker {Id}", id);
                TempData["Error"] = "Gagal hapus pekerja: ada constraint database yang dilanggar. Pastikan tidak ada data dependen yang belum dibersihkan.";
                return RedirectToAction("ManageWorkers");
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "Failed to delete worker {Id}", id);
                TempData["Error"] = "Gagal hapus pekerja: terjadi kesalahan internal. Hubungi admin.";
                return RedirectToAction("ManageWorkers");
            }

            // Phase 335 D-05: File.Delete loop POST CommitAsync - inner try/catch warn-only per file
            foreach (var relUrl in allFilePaths)
            {
                try
                {
                    var physical = Path.Combine(_env.WebRootPath, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
                    if (System.IO.File.Exists(physical)) System.IO.File.Delete(physical);
                }
                catch (Exception fex)
                {
                    _logger.LogWarning(fex, "File.Delete post-commit failed (Worker file): {Path}", relUrl);
                }
            }

            TempData["Success"] = $"User '{userName}' berhasil dihapus dari sistem.";
            return RedirectToAction("ManageWorkers");
        }
```

**Catatan presisi (per CONTEXT D-03..D-06):**
- L508-509 userName/userEmail capture - preserve verbatim, posisi tetap.
- **TAMBAH** SETELAH userEmail: file path collection block (re-query TR + re-query AS + foreach populate). allFilePaths declared OUTSIDE tx (value-typed safe post-commit).
- **CRITICAL SCHEMA NOTE**: TrainingRecord HANYA punya SertifikatUrl (Models/TrainingRecord.cs L40) - TIDAK PUNYA ManualSertifikatUrl. JANGAN tambah baris collection untuk tr.ManualSertifikatUrl - akan CS0117 error.
- **TAMBAH** `using var tx = await _context.Database.BeginTransactionAsync();` + `try {` SEBELUM existing L513 userAssessmentIds.
- L513-516 userAssessmentIds collection - preserve verbatim INSIDE tx (re-query, beda scope dari Task 1 userAssessmentIdsForPrecheck).
- L518-531 PackageUserResponses + UserPackageAssignments - preserve verbatim INSIDE tx.
- L533 komentar UserCompetencyLevels - preserve.
- L535-540 ProtonDeliverableProgresses load + RemoveRange - **REFACTOR**: INSERT EvidencePath + EvidencePathHistory collection ANTARA `.ToListAsync()` dan `RemoveRange(protonProgress)`. JSON parse inner try/catch warn-only.
- L542-582 6 RemoveRange (ProtonFinalAssessments + ProtonTrackAssignments + ProtonNotifications + CoachCoacheeMappings + CoachingSessions + CoachingLogs) - preserve verbatim INSIDE tx.
- L584 SaveChangesAsync - preserve verbatim INSIDE tx.
- L586 komentar - preserve.
- L587 UserManager.DeleteAsync - preserve verbatim INSIDE tx (D-04 + D-11 Program.cs L47 confirmed).
- **REFACTOR L588-609** split: `if (!result.Succeeded) { ... return; }` early return INSIDE try (Identity error UX preserve D-06 explicit acceptable - bukan info leak); audit log block (try/catch warn-only) preserve verbatim INSIDE tx setelah Identity success check (flatten - BUKAN dalam `if (result.Succeeded)` branch lama).
- **TAMBAH** `await tx.CommitAsync();` setelah audit log INSIDE try.
- **TAMBAH** catch blocks: `catch (DbUpdateException dbEx)` FIRST (LogWarning + TempData generic + return); `catch (Exception ex)` SECOND (LogError + TempData generic + return). NO `+ ex.Message`. NO eksplisit `tx.RollbackAsync()`. NO `throw;`.
- **TAMBAH** POST tx scope: File.Delete loop foreach allFilePaths warn-only per file (log distinct "Worker file"). Cross-platform `Replace('/', Path.DirectorySeparatorChar)`.
- **POSISI** `TempData["Success"]` + `return RedirectToAction` POST File.Delete loop di OUTER scope.
- L612 closing brace - preserve verbatim (akan di-shift ~80 baris).
- **TIDAK ada import baru** - EF Core + System.IO.File + Path + System.Text.Json sudah tersedia.
- **TIDAK ada perubahan ke Task 1 pre-check block**.
  </action>

  <verify>
    <automated>grep -c "Phase 335 D-03: re-query TR + AS full objects" Controllers/WorkerController.cs && grep -c "allFilePaths" Controllers/WorkerController.cs && grep -c "using var tx = await _context.Database.BeginTransactionAsync" Controllers/WorkerController.cs && grep -c "tx.CommitAsync" Controllers/WorkerController.cs && grep -c "catch .DbUpdateException dbEx." Controllers/WorkerController.cs && grep -c "File.Delete post-commit failed .Worker file." Controllers/WorkerController.cs</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "Phase 335 D-03: re-query TR + AS full objects" Controllers/WorkerController.cs -> 1
    - grep -c "allFilePaths" Controllers/WorkerController.cs -> >=4 (decl + foreach TR/AS + ProtonProgress + AddRange + File.Delete loop)
    - grep -c "userTrainingRecords" Controllers/WorkerController.cs -> 2
    - grep -c "userAssessmentSessions" Controllers/WorkerController.cs -> 2
    - grep -c "using var tx = await _context.Database.BeginTransactionAsync" Controllers/WorkerController.cs -> 1
    - grep -c "tx.CommitAsync" Controllers/WorkerController.cs -> 1
    - grep -c "tx.RollbackAsync" Controllers/WorkerController.cs -> 0 (using disposal)
    - grep -c "catch (DbUpdateException dbEx)" Controllers/WorkerController.cs -> 1
    - grep -c "DbUpdate failed for DeleteWorker" Controllers/WorkerController.cs -> 1
    - grep -c "Gagal hapus pekerja:" Controllers/WorkerController.cs -> 2 (DbUpdate + Exception)
    - grep -c "Failed to delete worker" Controllers/WorkerController.cs -> 1
    - grep -c "File.Delete post-commit failed (Worker file)" Controllers/WorkerController.cs -> 1
    - grep -c "Failed to parse EvidencePathHistory for progress.*DeleteWorker" Controllers/WorkerController.cs -> 1
    - grep -c "Gagal menghapus user:" Controllers/WorkerController.cs -> 1 (Identity preserve D-06)
    - grep -c "berhasil dihapus dari sistem" Controllers/WorkerController.cs -> 1 (Success preserved)
    - sed -n DeleteWorker range | grep -c "throw;" -> 0
    - sed -n DeleteWorker range | grep -c "+ ex.Message" -> 0
    - dotnet build -> 0 error CS*
  </acceptance_criteria>

  <done>DeleteWorker L508-611 di-refactor sesuai D-03 + D-04 + D-05 + D-06: file collection re-query TR + AS OUTSIDE tx (TR.SertifikatUrl + AS.ManualSertifikatUrl - TR tidak punya ManualSertifikatUrl per Models/TrainingRecord.cs L40), using var tx = BeginTransactionAsync wrap 9-step RemoveRange + SaveChanges + UserManager.DeleteAsync + audit log + CommitAsync INSIDE try, ProtonProgress EvidencePath + JSON history INSIDE tx ANTARA ToListAsync dan RemoveRange, Identity error early return INSIDE try (preserve Description display D-06), catch DbUpdateException + Exception fallback friendly NO + ex.Message NO throw; NO tx.RollbackAsync(), File.Delete loop POST CommitAsync warn-only per file ("Worker file" distinct), TempData Success + Redirect OUTER scope POST file loop. Pre-check Task 1 + Authorization + ValidateAntiForgeryToken + null id + Challenge + self-deletion guard + user null-check preserve verbatim OUTSIDE tx.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify build + test + grep AC + IT_NOTIFY append Phase 335 final v19.0 + commit + SUMMARY</name>
  <files>docs/IT_NOTIFY.md, .planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-01-SUMMARY.md</files>

  <read_first>
    1. docs/IT_NOTIFY.md (baca entry terakhir Phase 334 - format append Phase 335)
    2. .planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-CONTEXT.md (D-08 IT_NOTIFY bundle v19.0 final, D-09 AC 12 item)
    3. C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
    4. .planning/phases/334-fix-cascade-deletekompetensi-orphan-evidence-files/334-01-SUMMARY.md (referensi format)
  </read_first>

  <action>
**[A] Build verification (D-09 item 8)**

```bash
dotnet build
```
Expected 0 error CS*. Common errors:
- CS0117 TrainingRecord does not contain ManualSertifikatUrl -> REMOVE baris (per CRITICAL SCHEMA NOTE Task 2).
- CS0103 userTrainingIds undefined di tx body -> check Task 1 OUTSIDE tx accessible via closure.
- CS0136 conflict userAssessmentIdsForPrecheck vs userAssessmentIds (intentional separate scope).

**[B] Test suite (D-09 item 9)**

```bash
dotnet test --no-build
```
Expected 18/18 pass (baseline Phase 334).

**[C] Grep AC verification (D-09 items 1-7) - 20+ counter** (detail di acceptance_criteria). Range-scoped check `sed -n '/public async Task<IActionResult> DeleteWorker/,/^        }$/p'` untuk throw; dan + ex.Message.

**[D] Manual smoke (D-09 item 10) - DEFERRED**. Catat di SUMMARY: "Manual smoke DEFERRED to Dev promo - IT_NOTIFY scenario #13".

**[E] Append docs/IT_NOTIFY.md - per D-08 (bundle v19.0 FINAL phase)**

Append section `## Phase 335 - Fix Cascade DeleteWorker Triple-Fix D2+D5+D7 (FINAL v19.0 HIGH phase)`:
- Commit hash placeholder
- Date shipped local: 2026-05-28
- Flag migration: NO
- Files Modified: Controllers/WorkerController.cs L487-612 ~110 LoC delta (D5 pre-check + D2 file collection + D7 tx wrap + Identity early-return + catch friendly + File.Delete post-commit) + docs/IT_NOTIFY.md
- Closes Phase 328 §4.3 (D2+D5+D7 triple-dim) + §9 proposal #6
- **v19.0 Milestone Close note**: 11/11 phase 100% shipped lokal post Phase 335, ~64 commit batch
- Action Required by IT: Bundle batch push menunggu Phase 327 option-b release
- **Scenario #13** 7 sub-skenario A-G:
  - A: Delete sukses cascade + multi-source file cleanup
  - B: D5 cross-user renewal BLOCK CRITICAL (TR cross TR) - verify TempData explicit count, NO tx, NO file deleted
  - C: D5 cross-user renewal AS branch - 4 variant TR/AS cross TR/AS
  - D: D7 tx atomicity Identity error rollback CRITICAL - inject IdentityError + verify DB rollback + NO file deleted
  - E: D7 tx atomicity DbUpdateException catch CRITICAL - trigger constraint + verify TempData NO ex.Message leak (D6) + DB rollback + NO file deleted
  - F: File.Delete fail post-commit (orphan disk acceptable) - cascade DB sukses, file warn-only, iterate next
  - G: Self-deletion guard - Admin tidak bisa hapus akun sendiri

**[F] Git commit (D-09 item 11)**

```bash
git add Controllers/WorkerController.cs docs/IT_NOTIFY.md
git commit -m "feat(335): cascade triple-fix DeleteWorker (cross-user renewal pre-check + file post-commit + tx wrap)"
```
Setelah commit, catat hash -> update IT_NOTIFY.md commit hash placeholder -> commit terpisah `docs(335): record commit hash in IT_NOTIFY`.

**[G] Generate SUMMARY.md (D-09 item 12)**

Buat `.planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-01-SUMMARY.md`:
- Status: SHIPPED LOCAL (NOT PUSHED bundle v19.0 FINAL phase per D-08)
- Files modified table
- AC D-09 12/12 checklist
- Grep marker count 20+ counter table
- Threat model T-335-01..04 disposition (all mitigate)
- Closes Phase 328 §4.3 + §9 #6
- Manual smoke DEFERRED + scenario #13 reference
- Key difference: triple-fix tunggal phase (D2+D5+D7), D5 cross-user pre-check BARU pattern (ext Phase 325 P05), UserManager INSIDE tx D-11 confirmed Program.cs L47, Identity error preserve D-06, TR no ManualSertifikatUrl schema fact, TempData+Redirect MVC
- **v19.0 milestone close: 11/11 phase 100% shipped lokal**, ~64 commit batch pending push bundle
- Next steps: bundle push v19.0 menunggu release -> IT promosi Dev -> Prod
  </action>

  <verify>
    <automated>dotnet build 2>&1 | tail -5 && dotnet test --no-build 2>&1 | grep -E "passed|failed|Total"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build: 0 error
    - dotnet test --no-build: 18 passed, 0 failed (no regression Phase 334)
    - **Pre-check D5 6 counter**: Phase 335 D-02 comment=1, crossUserTrReferences>=2, crossUserAsReferences>=2, userAssessmentIdsForPrecheck>=3, Tidak bisa hapus pekerja=1, totalCrossRefs=2
    - **File collection D2 4 counter**: Phase 335 D-03 comment=1, allFilePaths>=4, userTrainingRecords=2, userAssessmentSessions=2
    - **Tx wrap D7 2 counter**: using var tx BeginTransactionAsync=1, tx.CommitAsync=1
    - **Identity D-06 preserve 1 counter**: Gagal menghapus user:=1
    - **File.Delete post-commit 2 counter**: Phase 335 D-05 comment=1, File.Delete post-commit failed (Worker file)=1
    - **Catch refactor 4 counter**: catch (DbUpdateException dbEx)=1, DbUpdate failed for DeleteWorker=1, Gagal hapus pekerja:=2, Failed to delete worker=1
    - **NO rollback/throw/ex.Message 4 counter**: tx.RollbackAsync=0, range-scoped throw;=0, range-scoped DbUpdateException +ex.Message=0, range-scoped Exception +ex.Message=0
    - **Preserved verbatim 4 counter**: Anda tidak dapat menghapus akun=1, User tidak ditemukan=1, berhasil dihapus dari sistem=1, Failed to parse EvidencePathHistory.*DeleteWorker=1
    - Manual smoke DEFERRED dicatat di SUMMARY + scenario #13 7 sub-skenario di IT_NOTIFY
    - git log --oneline -1 -> "feat(335): cascade triple-fix DeleteWorker"
    - docs/IT_NOTIFY.md mengandung "Phase 335" section + commit hash + scenario #13 + v19.0 milestone close note
    - 335-01-SUMMARY.md file ada dengan D-09 12/12 AC + v19.0 milestone close (11/11 phase 100%)
  </acceptance_criteria>

  <done>Build bersih, 18/18 test pass, 20+ grep AC counter terverifikasi, manual smoke deferred per D-09 item 10, IT_NOTIFY.md diupdate Phase 335 entry final v19.0 + scenario #13 7 sub-skenario A-G + commit hash + v19.0 milestone close note, commit per D-09 item 11, SUMMARY.md digenerate dengan SHIPPED LOCAL + key difference + v19.0 milestone close (11/11 phase 100%, ~64 commit batch pending push bundle).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Controller | POST MVC dengan [HttpPost] + [Authorize(Roles="Admin, HC")] + [ValidateAntiForgeryToken] + null id BadRequest + currentUser Challenge + self-deletion guard + user null-check TempData friendly. |
| Controller -> Database (pre-check) | 3 query OUTSIDE tx: userTrainingIds + userAssessmentIdsForPrecheck Select(Id) + 2x CountAsync gabung OR filter UserId != id. Read-only. |
| Controller -> Database (tx body) | EF Core DbUpdateException boundary - catch DbUpdateException spesifik + using var tx = BeginTransactionAsync atomicity 9-step RemoveRange + SaveChanges + UserManager.DeleteAsync + audit + CommitAsync. Identity error early return INSIDE try -> tx disposal auto-rollback. |
| Controller -> ASP.NET Identity Store | UserManager.DeleteAsync INSIDE tx via D-11 (Program.cs L47 AddEntityFrameworkStores<ApplicationDbContext> confirmed) -> same ApplicationDbContext via DI scoped -> join active transaction. Fallback jika assumption salah: _context.Users.Remove(user) + SaveChangesAsync() (skip Identity hooks but stays in tx). |
| Controller -> Filesystem | _env.WebRootPath + relUrl (TR.SertifikatUrl + AS.ManualSertifikatUrl + ProtonProgress.EvidencePath + EvidencePathHistory JSON) - POST CommitAsync inner try/catch warn-only per file (log distinct "Worker file"), cross-platform path normalize. |
| Controller -> AuditLog | _auditLog.LogAsync INSIDE tx setelah UserManager success - tx-coupled. Audit fail INSIDE inner try/catch warn-only -> tx tetap commit. |
| Controller -> Browser (Response) | TempData + RedirectToAction MVC - catch DbUpdateException + Exception fallback generic friendly Bahasa Indonesia, NO + ex.Message. Identity error path PRESERVE Description display per D-06 (user-actionable bukan DB constraint leak). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-335-01 | D (DoS / file orphan) | DeleteWorker UserManager.DeleteAsync cascade-delete User -> TR + AS + IdpItems (FK Cascade); SertifikatUrl (TR) + ManualSertifikatUrl (AS) + EvidencePath + EvidencePathHistory (ProtonProgress) physical files TIDAK di-cleanup -> orphan disk wwwroot | mitigate | Declare allFilePaths = new List<string>() OUTSIDE tx. Populate 3 source: foreach userTrainingRecords TR.SertifikatUrl (skip ManualSertifikatUrl - TR tidak punya), foreach userAssessmentSessions AS.ManualSertifikatUrl, INSIDE tx body antara ProtonDeliverableProgresses ToListAsync dan RemoveRange populate EvidencePath + JSON parse EvidencePathHistory inner try/catch warn-only. File.Delete loop POST tx.CommitAsync inner try/catch warn-only per file (log distinct "File.Delete post-commit failed (Worker file): {Path}" + iterate next, tidak break). Orphan disk acceptable (DB consistent) - cron janitor Phase 999.x backlog. |
| T-335-02 | T (Atomicity violation) | DeleteWorker partial state - TR + AS + 9-step RemoveRange + UserManager.DeleteAsync NOT atomic. Risk: UserManager fail mid-cascade, TR + AS sudah RemoveRange tapi User row tidak terhapus -> orphan IdentityUser. ATAU file evidence sudah dihapus tetapi DB rollback fails -> DB inconsistent. | mitigate | Wrap 9-step RemoveRange + SaveChanges + UserManager.DeleteAsync + audit + CommitAsync dalam using var tx = await _context.Database.BeginTransactionAsync(). UserManager INSIDE tx per D-11 (Program.cs L47 confirmed). Identity error early return INSIDE try -> tx tidak commit -> using disposal auto-rollback DB. File.Delete loop DEFERRED sampai POST tx.CommitAsync - kalau tx fail (DbUpdateException atau Identity early return), loop tidak dieksekusi -> file PRESERVED. Tidak ada window partial-state DB; orphan disk hanya kalau DB commit sukses tapi filesystem fail (acceptable per T-335-01). |
| T-335-03 | R (Repudiation / silent failure) | Current DeleteWorker (pre-fix) tidak pre-check cross-user renewal -> cascade-delete User A TR + AS akan trigger FK NoAction violation 500 dari Worker B side. Silent failure: 500 generic error, no explicit user feedback bahwa Worker A blocked karena cross-user renewal. | mitigate | D5 pre-check block INSERTED setelah user null-check L506 SEBELUM tx creation. 3 query: userTrainingIds + userAssessmentIdsForPrecheck + 2x CountAsync filter UserId != id. Kalau totalCrossRefs > 0 -> TempData explicit Bahasa Indonesia "Tidak bisa hapus pekerja {FullName}: {N} sertifikat milik pekerja lain..." + return RedirectToAction. NO tx creation. Admin/HC dapat explicit feedback + actionable instruction. Block strategy (BUKAN null-clear) per D-02 - preserve audit trail clarity. |
| T-335-04 | I (Information Disclosure) | Future-proof catch refactor - Phase 335 catch DbUpdateException + Exception fallback BARU mungkin disertai + ex.Message jika tidak hati-hati. Risk: DB constraint detail (FK_*, table names), exception type, EF Core internal error BOCOR ke browser via TempData display. | mitigate | catch (DbUpdateException dbEx) -> TempData generic "Gagal hapus pekerja: ada constraint database yang dilanggar..." (NO constraint/table leak). catch (Exception ex) fallback -> TempData generic "Gagal hapus pekerja: terjadi kesalahan internal..." (NO exception type/message leak). Exception detail hanya di server log via _logger.LogWarning + _logger.LogError, TIDAK pernah dikirim ke client. Identity error path PRESERVE Description display per D-06 explicit acceptable (user-actionable bukan DB internal leak). Verification: range-scoped grep + ex.Message di catch DbUpdateException + catch Exception block -> 0 (mandatory). |
</threat_model>

<verification>
Verifikasi akhir Phase 335:

1. **Build clean**: dotnet build -> 0 error (CS0117 TR.ManualSertifikatUrl dihindari per CRITICAL SCHEMA NOTE Task 2)
2. **Test suite**: dotnet test --no-build -> 18/18 pass (baseline Phase 334, no regression)
3. **Grep AC mekanis 20+ counter** (detail di Task 3 acceptance_criteria): Pre-check D5 6 + File collection D2 4 + Tx wrap D7 2 + Identity D-06 preserve 1 + File.Delete POST commit 2 + Catch refactor 4 + NO rollback/throw/ex.Message 4 + Preserved verbatim 4
4. **Manual smoke**: DEFERRED to Dev promo (per D-09 item 10, scenario #13 7 sub-skenario A-G di IT_NOTIFY)
5. **IT_NOTIFY**: grep "Phase 335" docs/IT_NOTIFY.md -> 1 section dengan commit hash + scenario #13 + v19.0 milestone close note
6. **Commit**: git log --oneline -1 -> "feat(335): cascade triple-fix DeleteWorker"
7. **SUMMARY**: ls .planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-01-SUMMARY.md -> file ada dengan v19.0 milestone close 11/11 phase 100%
</verification>

<success_criteria>
Phase 335 selesai ketika:
- [ ] DeleteWorker L487-612 di-refactor sesuai D-02..D-06 (Task 1 pre-check D5 + Task 2 file collection D2 + tx wrap D7 + UserManager INSIDE + Identity early-return + catch friendly + File.Delete POST commit)
- [ ] D5 cross-user renewal pre-check block INSERTED OUTSIDE tx per D-02
- [ ] D2 file path collection allFilePaths populate dari TR.SertifikatUrl + AS.ManualSertifikatUrl OUTSIDE tx + ProtonProgress.EvidencePath + JSON history INSIDE tx per D-03 (TR TIDAK PUNYA ManualSertifikatUrl verified)
- [ ] D7 tx wrap using var tx = BeginTransactionAsync membungkus 9-step RemoveRange + SaveChanges + UserManager.DeleteAsync + audit + CommitAsync per D-04
- [ ] UserManager.DeleteAsync INSIDE tx per D-11 confirmed Program.cs L47 AddEntityFrameworkStores
- [ ] Identity error path early return INSIDE try preserve Description display per D-06 explicit acceptable
- [ ] Catch DbUpdateException + Exception fallback friendly NO + ex.Message NO throw; NO eksplisit tx.RollbackAsync() per D-06
- [ ] File.Delete loop POST tx.CommitAsync() foreach allFilePaths warn-only per file (log distinct "Worker file") per D-05
- [ ] Authorization + ValidateAntiForgeryToken + BadRequest null id + currentUser Challenge + self-deletion guard + user null-check preserve verbatim OUTSIDE tx
- [ ] 9-step RemoveRange order preserve verbatim INSIDE tx
- [ ] dotnet build clean (0 error CS* - guard CS0117 TR.ManualSertifikatUrl)
- [ ] dotnet test --no-build 18/18 pass (no regression Phase 334)
- [ ] Manual smoke: DEFERRED to Dev promo (per D-09 item 10)
- [ ] 20+ grep AC counter mekanis terpenuhi
- [ ] docs/IT_NOTIFY.md diupdate Phase 335 entry FINAL v19.0 + commit hash + scenario #13 7 sub-skenario A-G + v19.0 milestone close note (11/11 phase 100%)
- [ ] 1 commit dengan message "feat(335): cascade triple-fix DeleteWorker (cross-user renewal pre-check + file post-commit + tx wrap)"
- [ ] 335-01-SUMMARY.md digenerate dengan SHIPPED LOCAL + key difference + v19.0 milestone close
- [ ] Status: SHIPPED LOCAL (NOT PUSHED bundle v19.0 FINAL phase per D-08)
- [ ] v19.0 milestone: 11/11 phase 100% post Phase 335 ship (~64 commit batch pending push bundle Phase 327 option-b release)
</success_criteria>

<output>
Setelah Task 3 selesai, buat .planning/phases/335-fix-cascade-deleteworker-renewal-files-tx/335-01-SUMMARY.md dengan isi:
- Status: SHIPPED LOCAL (NOT PUSHED bundle v19.0 FINAL phase)
- Files modified (Controllers/WorkerController.cs ~110 LoC delta + docs/IT_NOTIFY.md)
- AC D-09 table 12/12 checklist
- Grep marker count verification 20+ counter
- Manual smoke status: DEFERRED to Dev promo (D-09 item 10 explicit, scenario #13 di IT_NOTIFY 7 sub-skenario A-G)
- Threat model disposition 4/4 (T-335-01..04 all mitigate)
- Closes Phase 328 §4.3 (D2 + D5 + D7 triple-dim) + §9 proposal #6
- Key difference catatan: **triple-fix tunggal phase** (D2+D5+D7) beda dari Phase 331/332/333/334 single-dim; **D5 cross-user renewal pre-check BARU pattern** (extension Phase 325 P05 same-user -> cross-user filter UserId != id); **UserManager.DeleteAsync INSIDE tx** per D-11 confirmed Program.cs L47 AddEntityFrameworkStores; **Identity error path PRESERVE Description display** D-06 explicit acceptable; **TR TIDAK PUNYA ManualSertifikatUrl** (verified schema fact); **response TempData+Redirect MVC** standard bukan Json AJAX
- **v19.0 milestone close note: 11/11 phase 100% shipped lokal**, ~64 commit batch (Phase 325+326+327+329+330+331+332+333+334+335 - Phase 328 audit-only), pending push bundle (Phase 327 option-b hold)
- Next steps: bundle push v19.0 menunggu user release push lock (Phase 327 option-b) -> IT promosi Dev -> IT promosi Prod
</output>
