---
phase: 336-investigate-pretest-loss-cilacap-restore-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md
  - .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md
  - .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md
autonomous: false
requirements:
  - REST-01-investigate-git-log
  - REST-02-confirm-root-cause
  - REST-03-decide-restore-strategy
tags: [investigation, postmortem, migration, data-loss, naming-convention]

must_haves:
  truths:
    - "REST-01: Git log window 2026-03-30..2026-05-19 untuk Models/AssessmentSession.cs + Migrations/*.cs + Data/SeedData.cs sudah dianalisis; 13 migration candidate ditelaah satu per satu dengan klasifikasi (DROP COLUMN no-preserve | recreate table | schema-preserving | irrelevant)"
    - "REST-02: Root cause diklasifikasi ke salah satu dari 6 kategori decision tree (migration drop / migration recreate / EnsureCreated reset / SeedData reset / schema-preserving / manual cleanup / unable-to-determine) dengan evidence concrete (commit hash + file:line + diff snippet)"
    - "REST-02: OQ-336-2 (EnsureCreated panggilan di Program.cs) resolved YES/NO dengan grep evidence"
    - "REST-02: OQ-336-3 (AuditLog 0 entry confirm bukan silent SQL delete) resolved dengan reasoning + AuditLog model schema check"
    - "REST-03: Strategi A/B/C dipilih berdasarkan decision tree path; OQ-336-1 (.bak Dev DB availability) resolved via user koordinasi"
    - "REST-03: Naming convention spec final dengan format definition + 4+ examples + 3+ edge case (multi-unit, refinery short/long, Pre/Post LinkedGroupId pairing); OQ-336-4 (backward enforce) ditandai DEFER ke Phase 338 W5 dengan rationale"
  artifacts:
    - path: ".planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md"
      provides: "Investigation findings: schema evolution timeline, 13 migration analysis table, culprit commit (kalau ketemu), decision tree path taken, OQ-336-2 + OQ-336-3 answered"
      min_lines: 80
      contains: "Culprit"
    - path: ".planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md"
      provides: "Restore strategy picked (A/B/C) dgn rationale, OQ-336-1 resolved, hand-off spec lengkap ke Phase 338 W4 (REST-04)"
      min_lines: 50
      contains: "Hand-off"
    - path: ".planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md"
      provides: "Naming format spec final + examples + edge cases + hand-off ke Phase 338 W5 (REST-06)"
      min_lines: 60
      contains: "Format"
  key_links:
    - from: ".planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md"
      to: ".planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md"
      via: "Decision tree path → strategy selection"
      pattern: "Strategy: (A|B|C)"
    - from: ".planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md"
      to: ".planning/ROADMAP.md"
      via: "Hand-off REST-04 ke Phase 338 W4"
      pattern: "Phase 338"
    - from: ".planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md"
      to: ".planning/ROADMAP.md"
      via: "Hand-off REST-06 ke Phase 338 W5"
      pattern: "Phase 338"
---

<objective>
Investigasi root cause loss PreTest OJT GAST Cilacap (30 Mar 2026 → hilang dari Dev DB 10.55.3.3) lewat git archeology window 2026-03-30..2026-05-19, putuskan strategi restore A/B/C berdasarkan root cause, dan tulis spec naming convention "{Pre|Post} Test {Track} {Lokasi}".

**Purpose:** Phase 336 = investigation-only deliverable phase v20.0. Output 3 dokumen jadi input gating untuk Phase 338 W4 (restore execute) + W5 (naming enforce + guardrail). Tanpa Phase 336 output, Phase 338 buta — bisa salah pilih strategi atau salah enforce naming.

**Output:** 3 dokumen markdown di `.planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/`:
1. `336-ROOT_CAUSE.md` — investigation findings + culprit identification
2. `336-RESTORE-DECISION.md` — strategi A/B/C terpilih + hand-off Phase 338 W4
3. `336-NAMING-CONVENTION-SPEC.md` — format spec + hand-off Phase 338 W5

**Tidak ada code change. Tidak ada migration. Tidak ada model touch. Tidak ada controller modification. Semua read-only operation (Read tool, Bash grep, git log) + Write 3 dokumen.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-CONTEXT.md
@.planning/notes/2026-05-29-pretest-ojt-gast-cilacap-lost.md
@.planning/todos/pending/002-restore-pretest-ojt-gast-cilacap.md

# Investigation target files (read-only)
@Models/AssessmentSession.cs
@Data/SeedData.cs
@Program.cs

# Migrations directory — 13 file dalam window 2026-03-30..2026-05-19 (ditelaah Task 2)
# - 20260401_AddMaintenanceMode
# - 20260402_FixInterviewResultsJsonColumnType
# - 20260406_AddAssessmentV14Columns       ← suspect HIGH
# - 20260407_AddRubrikEssayScoreMaxCharFields
# - 20260407_RemoveUniqueIndexOnPackageUserResponse
# - 20260407_AddExtraTimeMinutesToAssessmentSession
# - 20260409_AddBudgetItems
# - 20260409_RemoveBudgetItemStatus
# - 20260410_AddCoachWorkloadThreshold
# - 20260413_AddSamePackageToAssessmentSession  ← suspect HIGH
# - 20260414_AddManualEntryToAssessmentSession  ← suspect HIGH
# - 20260414_AddAssessmentExtraFields
# - 20260507_AddManageAssessmentPerfIndexes

<decision_tree_reference>
Per CONTEXT.md D-02 (decision tree restore strategy):

| Root Cause | Forced Strategy | Rationale |
|------------|-----------------|-----------|
| Migration DROP COLUMN no-preserve | B (skip) | Schema info hilang, restore impossible |
| Migration recreate table (drop+create) | A or B | Tergantung .bak Dev availability |
| EnsureCreated() reset Dev DB | A (re-import via AddManualAssessment + AuditLog tag ManualImport-Backfill) | Re-importable |
| SeedData.cs reset/cleanup | A | Same as EnsureCreated |
| Schema-preserving migration | C (tunggu Gap #5 Phase 337 W3 Excel breakdown enabler) | Data theoretical preserved; restore comprehensive butuh Elemen Teknis breakdown |
| Manual cleanup admin UI | B + investigate AuditLog | Bukan migration loss (AuditLog 0 entry suggests NOT this case) |
| Unable to determine | B (default safe) + document | Conservative fallback |
</decision_tree_reference>

<naming_convention_initial_draft>
Per CONTEXT.md D-05 (polish at Phase 336 exec):

**Format wajib:** `{Stage} Test {Track} {Lokasi}`
- Stage: `Pre` | `Post` (capitalize first letter)
- Test: literal word "Test"
- Track: kode track sesuai Master Track table (e.g., `OJT GAST`, `OJT Pekerja GAST`, `CMP`)
- Lokasi: `di Unit {Unit} RU {Refinery}` ATAU short form (e.g., `Cilacap`)

**Mismatch incident source:**
- PreTest user: `OJT GAST - GTO & SRU RU IV` ← TIDAK comply (no "Pre Test", no "Cilacap")
- PostTest counterpart: `Post Test OJT Pekerja GAST di Unit SRU dan GTO RU IV Cilacap` ← partially comply

**Edge case polish:**
- Multi-unit handling (`SRU dan GTO`)
- Refinery short vs long (`RU IV` vs `Refinery Unit IV`)
- Pre/Post LinkedGroupId pairing (auto-detect or admin manual?)
</naming_convention_initial_draft>

<open_questions>
4 OQ resolve during execution:
- **OQ-336-1**: `.bak` SQL Server snapshot Dev DB window Mar 30 – May 19 available? → koordinasi user (Task 5 checkpoint)
- **OQ-336-2**: `EnsureCreated()` panggilan di Program.cs / Startup.cs ada? → grep evidence (Task 3)
- **OQ-336-3**: AuditLog 0 entry confirm bukan silent SQL delete (out-of-band)? → reasoning + schema check (Task 4)
- **OQ-336-4**: Naming convention enforce backward — rename existing atau new-only? → DEFER ke Phase 338 W5 detailed discuss (Task 6 dokumentasi defer + rationale)
</open_questions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git log archeology AssessmentSession schema evolution (2026-03-30..2026-05-19)</name>
  <files>
    .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md
  </files>
  <action>
    READ-ONLY operation. Tulis draft awal `336-ROOT_CAUSE.md` dengan section "Schema Evolution Timeline" berisi:

    1. Run `git log --since="2026-03-30" --until="2026-05-19" --pretty=format:"%h %ad %s" --date=short -- Models/AssessmentSession.cs Data/SeedData.cs Migrations/` (via Bash) → capture full commit list di window.
    2. Run `git log --since="2026-03-30" --until="2026-05-19" -p -- Models/AssessmentSession.cs` (via Bash, head 500 lines) → capture schema diff timeline.
    3. Read current `Models/AssessmentSession.cs` (194 LoC) untuk baseline field list saat ini.
    4. Cross-reference dengan candidate commits dari ROADMAP: `b89b6559` (SamePackage), `a7bb443e` (AddAssessmentV14Columns), `569eb0a8` (7 kolom v14.0), `f82bad2e` (Rubrik/Essay), `0dedd7b7` (AddManualAssessment lifecycle) — verify hash exist + masuk window.

    Output di doc: tabel "Schema Evolution" kolom (Commit Hash | Date | Files Touched | Field Added/Modified/Dropped | Suspect Level Low/Med/High) untuk setiap commit yang touch AssessmentSession dalam window.

    **JANGAN modify Models/AssessmentSession.cs atau file source apapun. Hanya Read + Bash git log + Write ke ROOT_CAUSE.md.**

    Per D-01: scope narrow PreTest OJT GAST Cilacap saja (1 dataset) — JANGAN sweep wider data loss patterns.
  </action>
  <verify>
    <automated>test -f .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md &amp;&amp; grep -q "Schema Evolution" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md &amp;&amp; grep -cE "^\| [0-9a-f]{7,12}" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md | awk '{exit ($1 &gt;= 3) ? 0 : 1}'</automated>
  </verify>
  <done>
    ROOT_CAUSE.md exists dengan section "Schema Evolution Timeline" + tabel minimal 3 row commit (window candidate); zero source code file modified (verify via `git status Models/ Data/ Migrations/ Program.cs` → no changes).
  </done>
</task>

<task type="auto">
  <name>Task 2: 13 migration file content analysis + culprit classification</name>
  <files>
    .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md
  </files>
  <action>
    READ-ONLY operation. Append section "Migration Candidate Analysis (13 files)" ke `336-ROOT_CAUSE.md`.

    Untuk setiap 13 migration candidate per CONTEXT.md daftar:
    1. Read file `Migrations/{YYYYMMDDHHMMSS}_{Name}.cs` (Up + Down method)
    2. Classify schema operation: `DROP_COLUMN_NO_PRESERVE` | `RECREATE_TABLE` | `SCHEMA_PRESERVING_ADD_ONLY` | `INDEX_ONLY` | `IRRELEVANT_TO_AssessmentSession`
    3. Catat di tabel kolom: (Migration Name | Up Operation Summary | Touches AssessmentSession? Y/N | Classification | Forced Strategy per Decision Tree)

    Tabel target minimal 13 row (1 per migration). Migration yang IRRELEVANT_TO_AssessmentSession tetap masuk tabel dgn classification jelas (tidak skip — audit trail).

    Setelah tabel, tulis subsection "Culprit Identification" yang state:
    - Kalau ada migration dgn classification `DROP_COLUMN_NO_PRESERVE` atau `RECREATE_TABLE` yang touch AssessmentSession → **culprit candidate** dgn commit hash + line reference
    - Kalau semua schema-preserving → catat fakta + escalate ke Task 3 (EnsureCreated check) sebagai next suspect

    Per D-04: tooling git log + file read saja, NO DB introspection (sqlcmd skip). NO modify file migration.
  </action>
  <verify>
    <automated>grep -c "Migration Candidate Analysis" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md &amp;&amp; grep -cE "^\| .*_(Add|Remove|Fix).*\|" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md | awk '{exit ($1 &gt;= 10) ? 0 : 1}' &amp;&amp; grep -q "Culprit Identification" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md</automated>
  </verify>
  <done>
    13 migration tertabulasi (minimal 10 row teridentifikasi dgn classification), subsection "Culprit Identification" exists dgn statement explicit (either culprit hash teridentifikasi ATAU "no migration culprit, escalate Task 3"); zero source file modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: EnsureCreated + SeedData reset check (OQ-336-2 resolve)</name>
  <files>
    .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md
  </files>
  <action>
    READ-ONLY operation. Append section "EnsureCreated + SeedData Reset Check" ke `336-ROOT_CAUSE.md`.

    1. Grep `EnsureCreated\|EnsureDeleted\|Database\.Migrate` di Program.cs + Startup.cs (kalau ada) + ApplicationDbContext.cs:
       - Pakai Grep tool: pattern `EnsureCreated|EnsureDeleted|Database\.Migrate`, path scope `Program.cs` + `Startup.cs` (kalau exist) + cari `*DbContext*.cs`
    2. Read `Program.cs` (full) untuk inspect startup flow database init.
    3. Read `Data/SeedData.cs` (106 LoC) + git log -p `Data/SeedData.cs` window 2026-03-30..2026-05-19 → check apakah ada commit yang reset / clear AssessmentSession data.

    Dokumentasikan finding OQ-336-2 (`EnsureCreated()` ever called?):
    - YES + line ref → suspect HIGH untuk Dev DB reset
    - NO → eliminate dari candidate root cause
    - Conditional (e.g., dev env only) → catat condition + impact

    Tulis "Seed Reset Analysis" subsection: history commit SeedData.cs window — adakah pattern Clear/RemoveRange/Truncate AssessmentSession? Catat YES/NO + evidence.

    JANGAN modify Program.cs / Startup.cs / SeedData.cs / ApplicationDbContext.cs.
  </action>
  <verify>
    <automated>grep -q "EnsureCreated.*SeedData Reset Check" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md &amp;&amp; grep -qE "OQ-336-2.*(YES|NO|Conditional)" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md &amp;&amp; grep -q "Seed Reset Analysis" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md</automated>
  </verify>
  <done>
    Section "EnsureCreated + SeedData Reset Check" exists dgn OQ-336-2 resolved (YES/NO/Conditional dengan evidence grep output / file:line ref), subsection "Seed Reset Analysis" exists; zero source code modified.
  </done>
</task>

<task type="auto">
  <name>Task 4: AuditLog silent-delete elimination + Models/AuditLog.cs schema check (OQ-336-3 resolve)</name>
  <files>
    .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md
  </files>
  <action>
    READ-ONLY operation. Append section "AuditLog Silent-Delete Elimination" ke `336-ROOT_CAUSE.md`.

    1. Find `Models/AuditLog.cs` atau equivalent (via Glob `Models/AuditLog*.cs` atau Grep `class AuditLog`).
    2. Read schema fields AuditLog model — confirm logging mechanism (Action + EntityType + EntityId + UserId + Timestamp pattern typical).
    3. Grep `AuditLogService\|RecordAuditLog\|LogAudit\|AuditLog\.Add` di Controllers/ + Services/ → check apakah Delete operation AssessmentSession auto-log via service helper (vs ad-hoc).
    4. Logic reasoning untuk OQ-336-3:
       - Fakta: incident note 2026-05-29 catat AuditLog 0 entry untuk DeleteAssessment/DeleteAssessmentGroup PreTest
       - Hipotesis A: Migration drop schema → row hilang silent (NOT via app endpoint) → bypass AuditLog ✓ konsisten
       - Hipotesis B: EnsureCreated/SeedData reset → row hilang silent (NOT via app endpoint) → bypass AuditLog ✓ konsisten
       - Hipotesis C: Manual SQL DELETE out-of-band (IT direct sqlcmd) → bypass AuditLog ✓ konsisten tapi LOW probability (no IT report)
       - Hipotesis D: Cosmetic delete via admin UI → SHOULD log via AuditLog → 0 entry = NOT this case ✗

    Dokumentasikan: OQ-336-3 = "Silent delete confirmed via path A/B (migration atau reset), bukan manual cleanup admin UI (D eliminated). C low-prob no IT report."

    JANGAN modify AuditLog.cs / controllers / services.
  </action>
  <verify>
    <automated>grep -q "AuditLog Silent-Delete Elimination" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md &amp;&amp; grep -qE "OQ-336-3.*(confirmed|eliminated|resolved)" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md</automated>
  </verify>
  <done>
    Section "AuditLog Silent-Delete Elimination" exists, OQ-336-3 resolved dgn reasoning 4 hipotesis (A/B/C/D) + verdict explicit; zero source code modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: User checkpoint — OQ-336-1 .bak Dev DB availability + ROOT_CAUSE.md decision tree path commit</name>
  <what-built>
    Task 1-4 sudah complete: ROOT_CAUSE.md berisi 4 section investigation:
    1. Schema Evolution Timeline (Task 1)
    2. Migration Candidate Analysis 13 files + Culprit Identification (Task 2)
    3. EnsureCreated + SeedData Reset Check (Task 3) → OQ-336-2 resolved
    4. AuditLog Silent-Delete Elimination (Task 4) → OQ-336-3 resolved

    Sebelum finalize ROOT_CAUSE.md + write RESTORE-DECISION.md (Task 6), butuh:
    - **OQ-336-1 user input**: `.bak` SQL Server snapshot Dev DB 10.55.3.3 untuk window 30 Mar – 19 May 2026 available kah? (Per D-06: butuh IT coordination, defer ke Phase 338 W5 untuk tool selection — tapi cek availability sekarang untuk gating strategy A vs B kalau ketemu "recreate table" classification)
    - **Decision tree path confirmation**: berdasarkan finding Task 1-4, identifikasi path mana dari decision tree D-02 yang terambil (migration drop / migration recreate / EnsureCreated reset / SeedData reset / schema-preserving / unable-to-determine)
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md` full
    2. Verify 4 section investigation lengkap (Schema Evolution + Migration Analysis + EnsureCreated Check + AuditLog Elimination)
    3. Verify OQ-336-2 + OQ-336-3 explicit answered
    4. User menjawab 2 pertanyaan:
       - **OQ-336-1**: Apakah `.bak` SQL Server snapshot Dev DB tersedia untuk window 30 Mar – 19 May 2026? (YES dgn tanggal snapshot / NO / TIDAK TAHU butuh IT check)
       - **Decision tree path**: Berdasarkan ROOT_CAUSE.md finding, path mana yang terambil? Pilih:
         - `migration_drop_no_preserve` → forced B
         - `migration_recreate_table` → A or B depending OQ-336-1
         - `ensure_created_reset` → A
         - `seed_data_reset` → A
         - `schema_preserving` → C (tunggu Gap #5 Phase 337 W3)
         - `manual_cleanup` → B + investigate (low-prob, AuditLog 0 entry)
         - `unable_to_determine` → B default safe
  </how-to-verify>
  <resume-signal>
    Reply dengan format:
    ```
    OQ-336-1: {YES tanggal X | NO | TIDAK TAHU}
    Decision tree path: {pilih satu dari 7 opsi di atas}
    Approved untuk lanjut Task 6 (write RESTORE-DECISION.md + NAMING-CONVENTION-SPEC.md)
    ```
  </resume-signal>
</task>

<task type="auto">
  <name>Task 6: Write RESTORE-DECISION.md + NAMING-CONVENTION-SPEC.md (synthesize findings + hand-off)</name>
  <files>
    .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md
    .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md
    .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md
  </files>
  <action>
    Write 2 deliverable doc baru + final-touch ROOT_CAUSE.md (append "Decision Tree Path Taken" section dgn user input Task 5).

    **A. Append ke `336-ROOT_CAUSE.md`:**
    - Section "Decision Tree Path Taken" — state path terpilih dari Task 5 user input + evidence cross-ref ke 4 section sebelumnya
    - Section "Conclusion" — 1 paragraf summary culprit (kalau ada) + classification final

    **B. Write `336-RESTORE-DECISION.md` (min 50 baris):**
    Struktur:
    1. **Header** — phase ref, date, REQ ref REST-03
    2. **Input dari ROOT_CAUSE.md** — link + decision tree path terambil (1 paragraf)
    3. **OQ-336-1 Resolution** — `.bak` Dev DB availability (dari Task 5 user input) + impact ke strategy choice
    4. **Strategy Picked: A | B | C** — explicit pick dgn rationale 3-5 bullet
    5. **Rationale Decision Tree** — mapping path → forced strategy per tabel D-02
    6. **Hand-off ke Phase 338 W4 (REST-04)** — spec lengkap:
       - Kalau A: endpoint `AddManualAssessment` flow, 13 peserta from Excel `downloads/PreTest-OJT_GAST__GTO__SRU_RU_IV__20260330_Results.xlsx`, AuditLog tag `ManualImport-Backfill`, CompletedAt = 2026-03-30
       - Kalau B: dokumentasi treat Excel sebagai archive, link CSV comparison, NO DB insertion
       - Kalau C: dependency Phase 337 W3 Gap #5 (CIL-05 Excel breakdown Elemen Teknis) shipped dulu, lalu re-evaluate
    7. **Risk + Mitigation** — risks spesifik strategy terpilih + mitigation
    8. **Acceptance Phase 338 W4** — bullet list deliverable yang harus exist saat REST-04 done

    **C. Write `336-NAMING-CONVENTION-SPEC.md` (min 60 baris):**
    Struktur:
    1. **Header** — phase ref, date, REQ ref REST-03 (naming portion) + hand-off REST-06
    2. **Format Definition Final**:
       - Wajib: `{Stage} Test {Track} {Lokasi}`
       - Stage: `Pre` | `Post` (capitalize, single word, no dash)
       - Test: literal "Test" (separator space, NOT "PreTest"/"PostTest" combined)
       - Track: master list (`OJT GAST`, `OJT Pekerja GAST`, `CMP`, `CDP`, dst — list real dari Models/Track.cs kalau exist atau dari incident scope)
       - Lokasi: format primary `di Unit {Unit} RU {Refinery} {Kota}` ATAU fallback short `{Kota}` (e.g., `Cilacap`)
    3. **Examples (minimal 4)**:
       - Good: `Pre Test OJT Pekerja GAST di Unit SRU dan GTO RU IV Cilacap`
       - Good: `Post Test OJT Pekerja GAST di Unit SRU dan GTO RU IV Cilacap`
       - Good (short form): `Pre Test CMP Operator Cilacap`
       - Bad (incident source): `OJT GAST - GTO & SRU RU IV` (no Stage prefix, no Lokasi)
       - Bad: `PreTest OJT GAST` (no space PreTest, no Lokasi)
    4. **Edge Cases (minimal 3)**:
       - Multi-unit: `di Unit SRU dan GTO` vs `di Unit SRU + GTO` — pilih satu standar
       - Refinery: `RU IV` (short) vs `Refinery Unit IV` (long) — short preferred (consistent dgn enterprise convention)
       - Pre/Post LinkedGroupId pairing — auto-detect via Title regex match `^(Pre|Post) Test (.+)$` ATAU admin manual assign (default: admin manual, future enhancement auto-detect di Phase 338 W5)
    5. **Rationale** — kenapa convention ini fix incident: search "Cilacap" hit semua + filter "Pre Test" hit semua + LinkedGroupId pairing aktif ExportGainScoreExcel
    6. **OQ-336-4 Resolution** — backward enforce decision: **DEFER ke Phase 338 W5 detailed discuss** (rationale: butuh user decide rename existing AssessmentSession violate convention vs new-only enforce; impact analysis butuh count row violate via DB introspection yang out-of-scope Phase 336)
    7. **Hand-off ke Phase 338 W5 (REST-06)**:
       - Implement validation di admin create form `AssessmentAdminController` create endpoint (regex check title format)
       - Implement LinkedGroupId auto-pair UI (dropdown "Pair with existing Pre/Post" saat create)
       - Audit existing AssessmentSession Title violate convention (separate task, gating OQ-336-4 user decision)

    Per D-03: NO executable SQL/code di output Phase 336. Spec text only. NO controller/view modification.
    Per D-05: spec only di Phase 336, BUKAN backfill identification (defer Phase 338 W5).
  </action>
  <verify>
    <automated>test -f .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md &amp;&amp; test -f .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md &amp;&amp; grep -qE "Strategy Picked: (A|B|C)" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md &amp;&amp; grep -q "Hand-off ke Phase 338 W4" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md &amp;&amp; grep -q "Hand-off ke Phase 338 W5" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md &amp;&amp; grep -q "Decision Tree Path Taken" .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-ROOT_CAUSE.md &amp;&amp; wc -l .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-RESTORE-DECISION.md | awk '{exit ($1 &gt;= 50) ? 0 : 1}' &amp;&amp; wc -l .planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-NAMING-CONVENTION-SPEC.md | awk '{exit ($1 &gt;= 60) ? 0 : 1}'</automated>
  </verify>
  <done>
    3 deliverable doc exist + lengkap:
    - ROOT_CAUSE.md final dgn "Decision Tree Path Taken" + "Conclusion"
    - RESTORE-DECISION.md min 50 baris dgn strategy A/B/C picked + Hand-off Phase 338 W4
    - NAMING-CONVENTION-SPEC.md min 60 baris dgn format + 4 example + 3 edge case + Hand-off Phase 338 W5
    Zero source code file modified (verify final: `git status Models/ Controllers/ Views/ Services/ Migrations/ Data/ Program.cs` → no changes; hanya `.planning/phases/336-*/` yang berubah).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 336 read-only ↔ source codebase | Investigation tasks read source code + git log; tidak ada write ke source. Risk: accidental modify lewat tool misuse. |
| Phase 336 output ↔ Phase 338 input | RESTORE-DECISION.md + NAMING-CONVENTION-SPEC.md jadi input gating Phase 338 W4 + W5. Risk: ambiguous spec → Phase 338 misinterpret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-336-01 | Tampering | Source code files (Models/AssessmentSession.cs, Migrations/*, Program.cs, Data/SeedData.cs) | mitigate | Per task `done` criteria: explicit `git status` verify zero source file modified. Investigation tools restricted to Read + Bash grep/git log + Write hanya ke `.planning/phases/336-*/`. |
| T-336-02 | Information Disclosure | `.bak` SQL Server snapshot Dev DB path/credentials | accept | OQ-336-1 user koordinasi tanpa expose credentials di doc. RESTORE-DECISION.md catat availability flag (YES/NO) saja, bukan path/credential. |
| T-336-03 | Repudiation | Decision tree path choice (subjective tanpa evidence) | mitigate | Task 5 checkpoint require user explicit pick + Task 6 require cross-ref evidence dari Task 1-4 finding. Decision Tree Path Taken section di ROOT_CAUSE.md = audit trail. |
| T-336-04 | Denial of Service | Phase 338 blocked karena Phase 336 output ambiguous | mitigate | Strict template RESTORE-DECISION.md ("Strategy Picked: A/B/C") + NAMING-CONVENTION-SPEC.md ("Format Definition Final") wajib explicit. Verify command grep enforce keyword present. |
| T-336-05 | Elevation of Privilege | Phase 336 scope creep ke code change (violates D-03) | mitigate | Task action wording explicit "JANGAN modify ..." + Task done criteria verify via `git status`. CONTEXT.md Scope OUT bullet "NO code change" enforced. |
</threat_model>

<verification>
Phase-level verification (run di akhir, sebelum write SUMMARY.md):

1. **Source code untouched** (CRITICAL — investigation-only constraint D-03):
   ```bash
   git status Models/ Controllers/ Views/ Services/ Migrations/ Data/ Program.cs Startup.cs ApplicationDbContext.cs 2>&1 | grep -E "modified|new file" | wc -l
   # Expect: 0
   ```

2. **3 deliverable doc exist + non-trivial size:**
   ```bash
   for f in 336-ROOT_CAUSE.md 336-RESTORE-DECISION.md 336-NAMING-CONVENTION-SPEC.md; do
     test -f .planning/phases/336-*/$f || echo "MISSING: $f"
     wc -l .planning/phases/336-*/$f
   done
   # Expect: 3 file, ROOT_CAUSE >= 80 baris, RESTORE >= 50 baris, NAMING >= 60 baris
   ```

3. **4 OQ resolved:**
   ```bash
   grep -h "OQ-336-" .planning/phases/336-*/336-*.md | sort -u
   # Expect: OQ-336-1 (di RESTORE-DECISION.md), OQ-336-2 + OQ-336-3 (di ROOT_CAUSE.md), OQ-336-4 (di NAMING-CONVENTION-SPEC.md)
   ```

4. **Strategy picked explicit:**
   ```bash
   grep -E "Strategy Picked: [ABC]" .planning/phases/336-*/336-RESTORE-DECISION.md
   # Expect: 1 match
   ```

5. **Hand-off ke Phase 338 lengkap:**
   ```bash
   grep -c "Phase 338" .planning/phases/336-*/336-RESTORE-DECISION.md .planning/phases/336-*/336-NAMING-CONVENTION-SPEC.md
   # Expect: minimal 2 match per file
   ```
</verification>

<success_criteria>
- [ ] REST-01 addressed: git log window 2026-03-30..2026-05-19 ditelaah, 13 migration candidate ditabulasi di ROOT_CAUSE.md dgn classification
- [ ] REST-02 addressed: root cause klasifikasi ke salah satu dari 6 kategori decision tree dengan evidence concrete (commit hash + file:line)
- [ ] REST-03 addressed: strategi A/B/C dipilih dgn rationale di RESTORE-DECISION.md + naming convention spec final di NAMING-CONVENTION-SPEC.md
- [ ] OQ-336-1, OQ-336-2, OQ-336-3 resolved dgn evidence; OQ-336-4 ditandai DEFER ke Phase 338 W5 dgn rationale
- [ ] 3 deliverable doc exist + size threshold met (ROOT_CAUSE >= 80, RESTORE >= 50, NAMING >= 60 baris)
- [ ] Zero source code file modified (git status verify)
- [ ] Hand-off Phase 338 W4 + W5 explicit ditulis di masing-masing doc
- [ ] D-01 (narrow scope) + D-03 (decision doc only) + D-04 (git+files read only) + D-05 (spec only Phase 336) + D-06 (guardrail backup tool defer Phase 338 W5) honored
</success_criteria>

<output>
After completion, create `.planning/phases/336-investigate-pretest-loss-cilacap-restore-strategy/336-01-SUMMARY.md` mengikuti template summary.md standard, dengan extra section:
- "3 Deliverable Doc" — link + 1 paragraf ringkasan masing-masing (ROOT_CAUSE + RESTORE-DECISION + NAMING-CONVENTION-SPEC)
- "OQ Resolution Table" — OQ-336-1..4 dgn answer summary
- "Hand-off to Phase 338" — explicit list REST-04 (W4) + REST-06 (W5) input dari Phase 336 output
- "Investigation-only Compliance" — confirm zero source code modified
</output>
