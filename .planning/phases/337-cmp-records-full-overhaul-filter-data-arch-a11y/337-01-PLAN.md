---
phase: 337-cmp-records-full-overhaul-filter-data-arch-a11y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/WorkerDataService.cs
  - Services/IWorkerDataService.cs
  - Models/AllWorkersHistoryRow.cs
  - Controllers/CMPController.cs
  - Views/CMP/Records.cshtml
  - Views/CMP/RecordsTeam.cshtml
autonomous: false
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05, CMP-06, CMP-07, CMP-08, CMP-09, CMP-10, CMP-11]
must_haves:
  truths:
    - "Status filter ('Sudah'/'Belum') menyaring worker list bahkan tanpa Category (CMP-01)"
    - "Sub Category dropdown menyaring worker list server-side (CMP-02)"
    - "Category filter menarrow worker list (workers di luar Category disembunyikan), bukan hanya mengubah CompletionPercentage (CMP-03)"
    - "Category match memakai equality (case-insensitive) — 'OJT' TIDAK match 'POJT' (CMP-04)"
    - "Export Training endpoint men-drop training di luar Sub Category filter (CMP-05)"
    - "Session Completed dengan IsPassed null tampil 'Completed' BUKAN 'Failed' palsu (CMP-06)"
    - "Training SertifikatUrl tampil sebagai tombol 'Lihat Sertifikat' di kolom Sertifikat My Records (CMP-07)"
    - "Training Status='Permanent' tampil indikator badge 'Permanen' di kolom Valid Until (CMP-08)"
    - "Search My Records dengan karakter '&' match training title yang mengandung '&' (CMP-09)"
    - "Badge counter My Records (total + per-tipe) update saat search/year filter berubah (CMP-10)"
    - "AttemptNumber untuk title-null session dihitung benar (tidak collide dengan title-null lain) (CMP-11)"
  artifacts:
    - path: "Services/WorkerDataService.cs"
      provides: "GetUnifiedRecords + GetWorkersInSection + GetAllWorkersHistory dengan filter fix"
      contains: "subCategory parameter di GetWorkersInSection"
    - path: "Controllers/CMPController.cs"
      provides: "RecordsTeamPartial forward subCategory + Export endpoints honor subCategory"
      contains: "subCategory param di RecordsTeamPartial signature + diteruskan ke GetWorkersInSection"
    - path: "Views/CMP/Records.cshtml"
      provides: "Kolom Sertifikat render SertifikatUrl Training + Permanent badge + per-filter counter + HtmlEncode search fix"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "filterTeamTable() kirim subCategory + updateExportLinks() include subCategory (verify wired correctly)"
  key_links:
    - from: "Views/CMP/RecordsTeam.cshtml"
      to: "Controllers/CMPController.cs RecordsTeamPartial"
      via: "fetch('@Url.Action(\"RecordsTeamPartial\", \"CMP\")?...&subCategory=...')"
      pattern: "params\\.set\\('subCategory'"
    - from: "Controllers/CMPController.cs RecordsTeamPartial"
      to: "Services/WorkerDataService.cs GetWorkersInSection"
      via: "diteruskan dengan parameter subCategory baru"
      pattern: "GetWorkersInSection\\([^)]*subCategory"
    - from: "Services/WorkerDataService.cs GetWorkersInSection statusFilter block"
      to: "filter post-loop"
      via: "Guard '&& !string.IsNullOrEmpty(category)' DIHAPUS — statusFilter apply mandiri"
      pattern: "if \\(!string\\.IsNullOrEmpty\\(statusFilter\\)\\)"
---

<objective>
Wave 1 Phase 337 — Fix 5 CRITICAL filter silent-fail browser-verified + 6 data integrity bug di `/CMP/Records` (My Records + Team View). Tujuan: filter Status/Category/SubCategory secara aktual menarrow worker list, export endpoints honor filter, data render tepat (SertifikatUrl + Permanent + Status derivation), search robust terhadap HtmlEncode, AttemptNumber correct.

Purpose: Hilangkan silent-fail dimana UI tampak respond tapi server abaikan filter — root cause: 12 worker tetap muncul meskipun pilih Status="Sudah" tanpa Category (B-01 audit 2026-05-27). Tanpa fix Wave 1, Wave 2/3 (UX + Arch) dibangun di atas baseline filter yang salah.

Output:
- `Services/WorkerDataService.cs` refactored — GetUnifiedRecords Status derivation + GetWorkersInSection subCategory param + Category equality + statusFilter guard removal + AttemptNumber fix
- `Controllers/CMPController.cs` — RecordsTeamPartial signature + ExportRecordsTeamTraining honor subCategory
- `Views/CMP/Records.cshtml` — kolom Sertifikat render Training SertifikatUrl + Permanent badge + per-filter counter + HtmlEncode search fix
- `Views/CMP/RecordsTeam.cshtml` — verify subCategory wired di filterTeamTable + updateExportLinks (existing code sudah set subCategory di params line 269+277, tapi server-side belum receive — fix server-side)

Setelah Wave 1 selesai: 5 CRITICAL filter Playwright-verified narrow worker list dengan benar. Wave 2 dapat dibangun di atas baseline data correctness ini.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-CONTEXT.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md

# File sumber yang akan dimodifikasi
@Services/WorkerDataService.cs
@Controllers/CMPController.cs
@Views/CMP/Records.cshtml
@Views/CMP/RecordsTeam.cshtml
@Views/CMP/_RecordsTeamBody.cshtml

<interfaces>
<!-- Key signatures executor butuhkan tanpa harus scout codebase -->

From Services/WorkerDataService.cs L175 (current signature):
```csharp
public async Task<List<WorkerTrainingStatus>> GetWorkersInSection(
    string? section,
    string? unitFilter = null,
    string? category = null,
    string? search = null,
    string? statusFilter = null,
    DateTime? dateFrom = null,
    DateTime? dateTo = null)
```

TARGET signature setelah Plan 01 (add subCategory param):
```csharp
public async Task<List<WorkerTrainingStatus>> GetWorkersInSection(
    string? section,
    string? unitFilter = null,
    string? category = null,
    string? search = null,
    string? statusFilter = null,
    DateTime? dateFrom = null,
    DateTime? dateTo = null,
    string? subCategory = null)
```

From Controllers/CMPController.cs L740-758 (current RecordsTeamPartial):
```csharp
public async Task<IActionResult> RecordsTeamPartial(
    string? section, string? unit, string? category, string? subCategory,
    string? statusFilter, string? dateFrom, string? dateTo)
{
    // ...
    var workerList = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, null, statusFilter, from, to);
    // ↑ subCategory di terima param TAPI TIDAK diteruskan ke service — bug B-02
    return PartialView("_RecordsTeamBody", workerList);
}
```

From Models/UnifiedTrainingRecord.cs (verify properties exist):
- `string? SertifikatUrl` — sudah ada (L67 service set dari TrainingRecord.SertifikatUrl)
- `DateTime? ValidUntil`
- `string? Status` — "Passed", "Valid", "Permanent", "Expired", "Pending"

From Models/WorkerTrainingStatus.cs (verify):
- `List<TrainingRecord> TrainingRecords`
- `List<AssessmentSession> AssessmentSessions`
- Bila property `SubKategori` belum di-aggregate untuk filter, gunakan TrainingRecords.Where(t => t.SubKategori == subCategory) di service post-load.

From Services/WorkerDataService.cs L283-291 (Category .Contains BUG):
```csharp
if (!string.IsNullOrEmpty(category))
{
    bool isCompleted = trainingRecords.Any(r =>
        !string.IsNullOrEmpty(r.Kategori) &&
        r.Kategori.Contains(category, StringComparison.OrdinalIgnoreCase) &&  // ← B-04 BUG: harus equality
        (r.Status == "Passed" || r.Status == "Valid" || r.Status == "Permanent")
    );
    worker.CompletionPercentage = isCompleted ? 100 : 0;
}
```

Plus B-03: Category filter HANYA set CompletionPercentage, TIDAK narrow workerList — fix: di post-loop tambah filter `workerList = workerList.Where(w => w.TrainingRecords.Any(t => string.Equals(t.Kategori, category, ...)) || w.AssessmentSessions.Any(a => string.Equals(a.Category, category, ...)))`.

From Services/WorkerDataService.cs L302 (statusFilter guard BUG):
```csharp
if (!string.IsNullOrEmpty(statusFilter) && !string.IsNullOrEmpty(category))
//                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
// ← B-01 BUG: hapus guard ini supaya statusFilter apply mandiri
```

Setelah fix: `if (!string.IsNullOrEmpty(statusFilter) && statusFilter != "ALL")`. Bila Category kosong, gunakan CompletionPercentage berdasarkan total training (existing else-branch L294 hitung dari semua kategori).

From Controllers/CMPController.cs L690 ExportRecordsTeamTraining (current):
```csharp
public async Task<IActionResult> ExportRecordsTeamTraining(
    string? section, string? unit, string? search,
    string? statusFilter, string? category,
    string? dateFrom, string? dateTo)
// ↑ MISSING: string? subCategory parameter
```

TARGET: tambah `string? subCategory` parameter + di filter rows tambah `.Where(r => string.IsNullOrEmpty(subCategory) || ...)` (need: AllWorkersHistoryRow harus expose SubKategori — atau filter by training Kategori path lain. Cek Models/AllWorkersHistoryRow.cs untuk decide approach).

From Views/CMP/Records.cshtml L184-199 (kolom Sertifikat current — B-07/B-08 BUG):
```cshtml
<td class="p-3 text-center">
    @if (item.RecordType == "Training Manual") {
        @if (item.ValidUntil.HasValue) {
            <div>@item.ValidUntil.Value.ToString("dd MMM yyyy", ...)</div>
            @if (item.IsExpired) { <span class="badge bg-danger mt-1">Expired</span> }
        } else { <span class="text-muted">—</span> }
        @* ← MISSING: SertifikatUrl render (B-07) + Permanent badge (B-08) *@
    } else if (item.RecordType == "Assessment Online" && item.GenerateCertificate && ...) {
        @* Assessment cert link OK *@
    }
</td>
```

TARGET: untuk Training Manual, jika `!string.IsNullOrEmpty(item.SertifikatUrl)` → tombol `<a target="_blank">Lihat Sertifikat</a>`. Jika `item.Status == "Permanent"` → badge "Permanen" hijau (selain ValidUntil display).

From Views/CMP/Records.cshtml L157 (HtmlEncode BUG):
```cshtml
data-title="@item.Title.ToLower()"
@* ← Razor encode "&" jadi "&amp;" → JS search "&" fail *@
```

TARGET: `data-title="@Html.Raw(System.Net.WebUtility.HtmlDecode(item.Title.ToLower()))"` ATAU lebih aman — di JS gunakan `decodeHTMLEntities` helper untuk attribute value sebelum compare. Pilih approach yang JS-side untuk hindari XSS risk: di JS `function decodeEntities(s) { var d=document.createElement('div'); d.innerHTML=s; return d.textContent; }` sebelum `title.includes(searchTerm)`.

From Views/CMP/RecordsTeam.cshtml L264-299 doFetch (verify subCategory passed):
```javascript
const subCategory = document.getElementById('subCategoryFilter').value;
// ...
if (subCategory) params.set('subCategory', subCategory);
// ↑ JS sudah kirim — fix di server-side (CMPController + WorkerDataService)
```

JS sudah benar — fix Plan 01 100% server-side untuk B-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix filter silent-fail di WorkerDataService.GetWorkersInSection (CMP-01, CMP-02, CMP-03, CMP-04)</name>
  <files>Services/WorkerDataService.cs</files>
  <behavior>
    Setelah fix:
    - GetWorkersInSection menerima parameter baru `string? subCategory = null` (optional, default null)
    - Bila `subCategory` non-empty, worker list di-narrow ke workers yang punya `TrainingRecords.Any(t => string.Equals(t.SubKategori, subCategory, OrdinalIgnoreCase))`
    - Bila `category` non-empty:
      - Worker list di-narrow ke workers yang punya kategori match (equality, BUKAN .Contains) di Training ATAU Assessment (CMP-03 narrow + CMP-04 equality)
      - CompletionPercentage tetap dihitung benar (existing L283-297 logic preserve, tapi `.Contains` → `string.Equals(..., StringComparison.OrdinalIgnoreCase)`)
    - Bila `statusFilter` non-empty DAN BUKAN "ALL":
      - Apply filter mandiri (TANPA guard `&& !string.IsNullOrEmpty(category)` di L302) — CMP-01
      - "Sudah" → `CompletionPercentage == 100`
      - "Belum" → `CompletionPercentage != 100`
    - Order operasi: section/unit/search → assessment+training load → date filter → per-worker compute → category narrow → subCategory narrow → status filter
  </behavior>
  <action>
    **Pre-step (drift verify ±5 baris):** Sebelum edit, jalankan `grep -n "public.*GetWorkersInSection" Services/WorkerDataService.cs` confirm line actual ≈ L175 (sejak audit 2026-05-27 ada 1 commit `c7adcb73` DateOnly sweep, expected ±5 baris drift). Adjust line reference seluruh action steps kalau drift > ±5.

    1. Buka `Services/WorkerDataService.cs` baca seluruh `GetWorkersInSection` (L175-311).

    2. **Tambah parameter `subCategory`** di signature L175:
    ```csharp
    public async Task<List<WorkerTrainingStatus>> GetWorkersInSection(
        string? section,
        string? unitFilter = null,
        string? category = null,
        string? search = null,
        string? statusFilter = null,
        DateTime? dateFrom = null,
        DateTime? dateTo = null,
        string? subCategory = null)   // ← NEW
    ```

    3. **Fix Category .Contains → equality** (CMP-04) di L287:
    ```csharp
    // SEBELUM:
    r.Kategori.Contains(category, StringComparison.OrdinalIgnoreCase) &&
    // SESUDAH:
    string.Equals(r.Kategori, category, StringComparison.OrdinalIgnoreCase) &&
    ```

    4. **Tambah Category narrow** (CMP-03) — DI POST workerList loop (setelah L300, sebelum statusFilter block):
    ```csharp
    if (!string.IsNullOrEmpty(category))
    {
        workerList = workerList.Where(w =>
            w.TrainingRecords.Any(t => !string.IsNullOrEmpty(t.Kategori) &&
                                       string.Equals(t.Kategori, category, StringComparison.OrdinalIgnoreCase))
            || w.AssessmentSessions.Any(a => !string.IsNullOrEmpty(a.Category) &&
                                              string.Equals(a.Category, category, StringComparison.OrdinalIgnoreCase))
        ).ToList();
    }
    ```

    5. **Tambah SubCategory narrow** (CMP-02) — di bawah Category narrow:
    ```csharp
    if (!string.IsNullOrEmpty(subCategory))
    {
        workerList = workerList.Where(w =>
            w.TrainingRecords.Any(t => !string.IsNullOrEmpty(t.SubKategori) &&
                                       string.Equals(t.SubKategori, subCategory, StringComparison.OrdinalIgnoreCase))
        ).ToList();
    }
    ```

    6. **Fix statusFilter guard removal** (CMP-01) — L302:
    ```csharp
    // SEBELUM:
    if (!string.IsNullOrEmpty(statusFilter) && !string.IsNullOrEmpty(category))
    {
        if (statusFilter == "Sudah") ...
        else if (statusFilter == "Belum") ...
    }
    // SESUDAH:
    if (!string.IsNullOrEmpty(statusFilter) && statusFilter != "ALL")
    {
        if (statusFilter == "Sudah")
            workerList = workerList.Where(w => w.CompletionPercentage == 100).ToList();
        else if (statusFilter == "Belum")
            workerList = workerList.Where(w => w.CompletionPercentage != 100).ToList();
    }
    ```

    7. **Update interface** `IWorkerDataService` (likely di `Services/IWorkerDataService.cs` atau di file yang sama) — tambah parameter `string? subCategory = null` di method signature interface.
    Bila interface tidak ditemukan via grep, cek `using HcPortal.Services` di Program.cs DI registration — possibly menggunakan concrete class. Bila ada interface, MUST update.

    Bash sanity check:
    ```bash
    grep -n "GetWorkersInSection" Services/IWorkerDataService.cs Services/WorkerDataService.cs 2>/dev/null
    ```

    8. **Verify build:**
    ```bash
    dotnet build 2>&1 | tail -20
    ```
    Build harus PASS. Bila ada error "no overload" → cek caller di CMPController + AdminController + sample tests sudah update belum (akan di-fix di Task 2).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error CS" | head -10</automated>
  </verify>
  <done>
    - `GetWorkersInSection` signature memiliki parameter `subCategory` (default null)
    - `.Contains(category, ...)` di L287 diganti dengan `string.Equals(..., OrdinalIgnoreCase)`
    - Block Category narrow workerList ditambahkan setelah loop
    - Block SubCategory narrow workerList ditambahkan setelah Category narrow
    - Statement L302 guard `&& !string.IsNullOrEmpty(category)` dihapus, diganti `&& statusFilter != "ALL"`
    - Interface `IWorkerDataService` updated bila ada
    - `dotnet build` PASS (mungkin caller error di CMPController — di-fix Task 2)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire subCategory di CMPController.RecordsTeamPartial + ExportRecordsTeamTraining + verify Records action (CMP-02, CMP-05)</name>
  <files>Controllers/CMPController.cs</files>
  <behavior>
    Setelah fix:
    - `RecordsTeamPartial` (L740) meneruskan `subCategory` ke `GetWorkersInSection` (sebelumnya pass null) — CMP-02
    - `ExportRecordsTeamTraining` (L690) menerima param `string? subCategory`, meneruskan ke `GetWorkersInSection`, DAN filter `trainingRows` lebih lanjut untuk drop training di luar SubCategory — CMP-05
    - `ExportRecordsTeamAssessment` (L637): subCategory TIDAK applicable (Assessment tidak punya SubKategori field). Tetapi forward ke GetWorkersInSection bila ada param supaya worker list konsisten dengan UI.
    - Records action (L479) tetap memuat workerList awal tanpa filter (filter via AJAX RecordsTeamPartial — tetap)
  </behavior>
  <action>
    1. **Edit RecordsTeamPartial L755-757** — pass subCategory ke service:
    ```csharp
    // SEBELUM:
    var workerList = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, null, statusFilter, from, to);

    // SESUDAH:
    var workerList = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, null, statusFilter, from, to, subCategory);
    ```

    2. **Edit ExportRecordsTeamTraining L690** — tambah param + filter:
    ```csharp
    // SEBELUM signature:
    public async Task<IActionResult> ExportRecordsTeamTraining(
        string? section, string? unit, string? search,
        string? statusFilter, string? category,
        string? dateFrom, string? dateTo)

    // SESUDAH signature (tambah subCategory):
    public async Task<IActionResult> ExportRecordsTeamTraining(
        string? section, string? unit, string? search,
        string? statusFilter, string? category, string? subCategory,
        string? dateFrom, string? dateTo)
    ```

    Di body L710, update call:
    ```csharp
    var filteredWorkers = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, search, statusFilter, from, to, subCategory);
    ```

    Untuk filter `trainingRows` lebih lanjut (drop training row di luar subCategory) — cek Models/AllWorkersHistoryRow.cs apakah ada property SubKategori. Bila TIDAK ADA, dua opsi:
    - **Opsi A (preferred):** tambah property `SubKategori` di AllWorkersHistoryRow + populate di `GetAllWorkersHistory` L157 dari `t.SubKategori`. Kemudian filter:
      ```csharp
      var filtered = trainingRows
          .Where(r => filteredIds.Contains(r.WorkerId))
          .Where(r => from == null || r.Date.Date >= from.Value.Date)
          .Where(r => to == null || r.Date.Date <= to.Value.Date)
          .Where(r => string.IsNullOrEmpty(subCategory) ||
                      string.Equals(r.SubKategori, subCategory, StringComparison.OrdinalIgnoreCase))
          .Where(r => string.IsNullOrEmpty(category) ||
                      string.Equals(r.Kategori, category, StringComparison.OrdinalIgnoreCase))
          .ToList();
      ```
      Note: Property `Kategori` mungkin juga belum ada di AllWorkersHistoryRow — tambah juga supaya filter konsisten.
    - **Opsi B (fallback bila Model gak boleh di-touch):** join in-memory ke `_context.TrainingRecords` lewat WorkerId+Title (kompleks, hindari).

    **Pilih Opsi A.** Edit `Models/AllWorkersHistoryRow.cs` tambah:
    ```csharp
    public string? Kategori { get; set; }
    public string? SubKategori { get; set; }
    ```

    Di `GetAllWorkersHistory` L157-167 populate:
    ```csharp
    var trainingRows = trainings.Select(t => new AllWorkersHistoryRow
    {
        WorkerId      = t.UserId,
        WorkerName    = t.User?.FullName ?? t.UserId,
        WorkerNIP     = t.User?.NIP,
        RecordType    = "Manual",
        Title         = t.Judul ?? "",
        Date          = t.TanggalMulai ?? t.Tanggal,
        Penyelenggara = t.Penyelenggara,
        Kategori      = t.Kategori,         // NEW
        SubKategori   = t.SubKategori       // NEW
    })
    ```

    3. **Edit ExportRecordsTeamAssessment L637** — terima `subCategory` (forward only untuk konsistensi worker list, TIDAK filter assessment rows by subCategory karena AssessmentSession tidak punya SubKategori):
    ```csharp
    public async Task<IActionResult> ExportRecordsTeamAssessment(
        string? section, string? unit, string? search,
        string? statusFilter, string? category, string? subCategory,
        string? dateFrom, string? dateTo)
    ```
    Di body L657 update call dengan subCategory.

    4. **Verify `Records` action L479** — TIDAK ada perubahan (workerList awal di-load tanpa filter, filter via RecordsTeamPartial AJAX). Skip.

    5. **Build sanity:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - `RecordsTeamPartial` pass `subCategory` ke `GetWorkersInSection`
    - `ExportRecordsTeamTraining` signature tambah `string? subCategory`, body forward + filter trainingRows
    - `ExportRecordsTeamAssessment` signature tambah `string? subCategory`, body forward (worker filter only)
    - `Models/AllWorkersHistoryRow.cs` tambah `Kategori` + `SubKategori` properties
    - `GetAllWorkersHistory` populate `Kategori` + `SubKategori` di trainingRows projection
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix data integrity di GetUnifiedRecords + AttemptNumber + Records.cshtml render (CMP-06, CMP-07, CMP-08, CMP-09, CMP-10, CMP-11)</name>
  <files>Services/WorkerDataService.cs, Views/CMP/Records.cshtml</files>
  <behavior>
    Setelah fix:
    - `GetUnifiedRecords` L51: Status untuk session Completed dengan IsPassed null → "Completed" (bukan "Failed"). Three-way switch: true→"Passed", false→"Failed", null→"Completed". (CMP-06)
    - `GetAllWorkersHistory` L129: AttemptNumber tuple key untuk session title-null → gunakan grouping key yang tidak collide. Solusi: tuple `(a.UserId, a.Title)` dimana Title null tetap distinct via nullable string equality. Untuk session title-null, AttemptNumber default 1 (atau hitung archived count berdasarkan UserId saja kalau title null). (CMP-11)
    - `Records.cshtml` kolom Sertifikat L184-199: untuk Training Manual, tampilkan tombol "Lihat Sertifikat" bila `!string.IsNullOrEmpty(item.SertifikatUrl)`. Untuk Status="Permanent", tambah badge "Permanen" hijau. (CMP-07, CMP-08)
    - `Records.cshtml` L157 `data-title`: HtmlDecode supaya "&" search match. Approach: JS-side decode entities sebelum compare. (CMP-09)
    - `Records.cshtml` badge counter L39 `@totalCount`: update dinamis saat filter — JS update span saat filterTable() berjalan. Tambah counter per-tipe (Assessment + Training). (CMP-10)
  </behavior>
  <action>
    1. **Fix GetUnifiedRecords Status derivation** — `Services/WorkerDataService.cs` L51:
    ```csharp
    // SEBELUM:
    Status = a.IsPassed == true ? "Passed" : "Failed",

    // SESUDAH:
    Status = a.IsPassed switch
    {
        true => "Passed",
        false => "Failed",
        null => "Completed"   // Completed tapi belum di-grade (essay pending atau no scoring)
    },
    ```

    2. **Fix AttemptNumber title-null collision** — `Services/WorkerDataService.cs` L96-143. Audit memory citation: tuple key `(a.UserId, a.Title ?? "")` di L129 menyebabkan semua title-null collapse ke "" string → AttemptNumber salah.

    Fix approach: jika `Title` null, set AttemptNumber = 1 (tidak relevan untuk archived count karena title-null session jarang ada — biasanya legacy data). Untuk title non-null, logic existing tetap.

    ```csharp
    // SEBELUM L129:
    var key = (a.UserId, a.Title ?? "");
    int archived = archivedCountLookup.TryGetValue(key, out var c) ? c : 0;
    return new AllWorkersHistoryRow
    {
        ...
        AttemptNumber = archived + 1
    };

    // SESUDAH:
    int attemptNumber;
    if (string.IsNullOrEmpty(a.Title))
    {
        // Title null/empty — tidak ada cara reliable untuk hitung archived attempts
        // Default ke 1; flag di log untuk investigasi data quality
        attemptNumber = 1;
    }
    else
    {
        var key = (a.UserId, a.Title);
        int archived = archivedCountLookup.TryGetValue(key, out var c) ? c : 0;
        attemptNumber = archived + 1;
    }
    return new AllWorkersHistoryRow
    {
        ...
        AttemptNumber = attemptNumber
    };
    ```

    Note: `archivedCountLookup` declared di L96 dengan key `(UserId, Title)`. Verify type compatible — bila `Title` di archived non-null tapi current session null, key mismatch fine (current branch handle separately).

    3. **Fix Records.cshtml kolom Sertifikat** — Edit L184-199 kolom Sertifikat. Replace entire `<td>` block:
    ```cshtml
    <td class="p-3 text-center">
        @if (item.RecordType == "Training Manual") {
            @* CMP-08: Permanent badge *@
            @if (item.Status == "Permanent") {
                <span class="badge bg-success mb-1"><i class="bi bi-infinity me-1"></i>Permanen</span>
                <br />
            } else if (item.ValidUntil.HasValue) {
                <div>@item.ValidUntil.Value.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID"))</div>
                @if (item.IsExpired) {
                    <span class="badge bg-danger mt-1"><i class="bi bi-x-circle me-1"></i>Expired</span>
                }
            }
            @* CMP-07: Training SertifikatUrl link *@
            @if (!string.IsNullOrEmpty(item.SertifikatUrl)) {
                <a href="@item.SertifikatUrl" target="_blank" rel="noopener"
                   class="btn btn-sm btn-outline-primary mt-1">
                    <i class="bi bi-file-earmark-pdf me-1"></i>Lihat
                </a>
            } else if (item.Status != "Permanent" && !item.ValidUntil.HasValue) {
                <span class="text-muted">—</span>
            }
        } else if (item.RecordType == "Assessment Online" && item.GenerateCertificate && item.AssessmentSessionId.HasValue) {
            <a asp-action="Certificate" asp-controller="CMP" asp-route-id="@item.AssessmentSessionId.Value"
               class="btn btn-sm btn-outline-primary" target="_blank">
                <i class="bi bi-award me-1"></i>Lihat
            </a>
        } else {
            <span class="text-muted">—</span>
        }
    </td>
    ```

    4. **Fix HtmlEncode search (CMP-09)** — Edit `Views/CMP/Records.cshtml` L157 `data-title` dan L307-345 `<script>` block.

    Approach: JS-side decode entities. Edit L309-321 `filterTable()`:
    ```javascript
    // Helper to decode HTML entities (handles "&amp;" → "&" etc)
    function decodeEntities(s) {
        if (!s) return '';
        var d = document.createElement('div');
        d.innerHTML = s;
        return d.textContent || '';
    }

    function filterTable() {
        const searchTerm = document.getElementById('searchInput').value.toLowerCase();
        const yearFilter = document.getElementById('yearFilter').value;
        let visibleCount = 0;
        let visibleAssessment = 0;
        let visibleTraining = 0;
        document.querySelectorAll('#recordsTable .training-row').forEach(row => {
            const title = decodeEntities(row.getAttribute('data-title'));
            const year = row.getAttribute('data-year');
            const type = row.getAttribute('data-type');
            const matchSearch = !searchTerm || title.includes(searchTerm);
            const matchYear = !yearFilter || year === yearFilter;
            const show = matchSearch && matchYear;
            row.style.display = show ? '' : 'none';
            if (show) {
                visibleCount++;
                if (type === 'assessment') visibleAssessment++;
                else if (type === 'training') visibleTraining++;
            }
        });

        // CMP-10: per-filter counter update
        var totalBadge = document.getElementById('totalCountBadge');
        if (totalBadge) totalBadge.textContent = visibleCount;
        var aStat = document.getElementById('assessmentStatCount');
        if (aStat) aStat.textContent = visibleAssessment;
        var tStat = document.getElementById('trainingStatCount');
        if (tStat) tStat.textContent = visibleTraining;
        var totalStat = document.getElementById('totalStatCount');
        if (totalStat) totalStat.textContent = visibleCount;

        // ... (existing empty state logic preserved)
    }
    ```

    5. **Wire counter ids** — Edit Records.cshtml:
    - L39 `<span class="badge bg-info ms-1">@totalCount</span>` → `<span id="totalCountBadge" class="badge bg-info ms-1">@totalCount</span>`
    - L96 `<h3 class="fw-bold mb-0">@assessmentCount</h3>` → `<h3 id="assessmentStatCount" class="fw-bold mb-0">@assessmentCount</h3>`
    - L112 `<h3 class="fw-bold mb-0">@trainingCount</h3>` → `<h3 id="trainingStatCount" class="fw-bold mb-0">@trainingCount</h3>`
    - L128 `<h3 class="fw-bold mb-0">@totalCount</h3>` → `<h3 id="totalStatCount" class="fw-bold mb-0">@totalCount</h3>`

    6. **Tambah data-type attribute** untuk per-tipe counter — Edit L156:
    ```cshtml
    <tr class="training-row @(resultsUrl != null ? "table-row-clickable" : "")"
        data-title="@item.Title.ToLower()" data-year="@item.Date.Year"
        data-type="@(item.RecordType == "Assessment Online" ? "assessment" : "training")"
        ...>
    ```

    Note: `data-title` attribute tetap. JS-side decode lewat `decodeEntities()` handle HtmlEncode otomatis.

    7. **Build:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - `GetUnifiedRecords` Status switch handle 3 case (true/false/null)
    - `GetAllWorkersHistory` AttemptNumber title-null fallback ke 1 (no collision)
    - `Records.cshtml` kolom Sertifikat: Training SertifikatUrl link tampil + Permanent badge tampil
    - `Records.cshtml` `<script>` `filterTable()` di-extend dengan `decodeEntities` + per-tipe counter
    - 4 counter IDs ditambahkan + `data-type` attribute di row
    - `dotnet build` PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — Playwright UAT 5 CRITICAL filter fix + 6 data integrity render</name>
  <files>N/A — checkpoint task, verification gating only (no file modification by Claude di task ini)</files>
  <action>Wait for user manual verification per how-to-verify steps. Tidak ada code change di task ini — semua code change selesai di task sebelumnya. Tugas Claude: PAUSE eksekusi sampai user respond resume-signal.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User reply 'approved' di resume-signal — verified semua acceptance criteria per how-to-verify section PASS browser-side.</done>
  <what-built>
    Wave 1 selesai code-side:
    - 5 CRITICAL filter silent-fail fixed (CMP-01..05)
    - 6 data integrity fixed (CMP-06..11)
    - 4 file modified: WorkerDataService.cs, CMPController.cs, Models/AllWorkersHistoryRow.cs, Records.cshtml
    - `dotnet build` PASS (Task 1-3)

    Belum ada human/Playwright verification bahwa filter actually narrow di browser. Wajib UAT sebelum Wave 2.
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Backup DB lokal SEBELUM test (per `docs/SEED_WORKFLOW.md`):
       ```bash
       sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "BACKUP DATABASE [HCPortalDb] TO DISK='C:\Temp\HCPortalDb_pre_337-01_uat.bak'"
       ```
    2. Run app lokal: `dotnet run` → buka `http://localhost:5277`
    3. Login `admin@pertamina.com` / `123456`

    **A. UAT 5 CRITICAL Filter (Team View tab) — gunakan Playwright MCP atau manual browser:**

    Navigate `http://localhost:5277/CMP/Records` → klik tab "Team View".

    **A1. CMP-01 (Status filter without Category):**
    - Set Status = "Sudah" (Category = kosong, SubCategory = kosong)
    - Expected: Worker list TERSARING (hanya worker dengan CompletionPercentage == 100)
    - Pre-fix behavior (bug): 12 worker tetap muncul (statusFilter abaikan tanpa Category)
    - PASS criteria: `#workerCount` badge < total worker DAN row count berkurang

    **A2. CMP-02 (SubCategory filter narrows):**
    - Pilih Category yang punya children, lalu pilih SubCategory non-empty
    - Trigger `?subCategory=...` request via fetch
    - Expected: Worker list tersaring ke yang punya training di SubKategori tersebut
    - Pre-fix behavior (bug): 12 worker tetap muncul (param diterima tapi tidak diteruskan)
    - PASS criteria: row count berkurang relatif ke pilih Category saja
    - Bonus test: Set `subCategory=DOESNOTEXIST` via DevTools URL → expected 0 row

    **A3. CMP-03 (Category narrows worker list):**
    - Pilih Category = "OJT" (atau category yang ada di seed)
    - Expected: Worker yang TIDAK PERNAH ada training/assessment OJT DIHAPUS dari list
    - Pre-fix behavior (bug): Semua worker tetap muncul, hanya CompletionPercentage berubah
    - PASS criteria: row count < total worker

    **A4. CMP-04 (Category equality NOT substring):**
    - Asumsi ada category "OJT" dan "POJT" di seed (atau create temporary test category)
    - Pilih Category = "OJT"
    - Expected: Worker dengan HANYA "POJT" record TIDAK muncul (equality match)
    - Pre-fix behavior (bug): "POJT" worker muncul karena .Contains
    - PASS criteria: count tepat hanya worker dengan literal "OJT"

    **A5. CMP-05 (Export Training drops subCategory):**
    - Set Category + SubCategory non-empty di UI
    - Klik "Export Training" — download .xlsx
    - Open Excel, verify TIDAK ada baris training dengan SubKategori != filter
    - Pre-fix behavior (bug): semua training in Category included, SubKategori filter di-drop
    - PASS criteria: Excel rows hanya berisi SubKategori yang dipilih

    **B. UAT 6 Data Integrity (My Records tab):**

    Navigate ke tab "My Records".

    **B1. CMP-06 (Status Completed null):**
    - Cek apakah ada AssessmentSession dengan `Status=Completed` dan `IsPassed=NULL` (essay pending). Bila tidak ada di seed, SKIP — verify code path via unit test atau create temporary seed.
    - Expected: Kolom Status badge bukan "Failed" merah, harus "Completed" (warna abu/info)

    **B2. CMP-07 (Training SertifikatUrl link):**
    - Cari training row dengan SertifikatUrl populated di DB lokal:
      ```sql
      SELECT TOP 3 Id, Judul, SertifikatUrl FROM TrainingRecords WHERE SertifikatUrl IS NOT NULL AND SertifikatUrl != ''
      ```
    - Login sebagai worker tersebut → My Records → kolom Sertifikat harus tampil tombol "Lihat" yang link ke SertifikatUrl
    - Pre-fix behavior (bug): tampil "—" walau ada SertifikatUrl

    **B3. CMP-08 (Permanent badge):**
    - Cari training row dengan Status="Permanent":
      ```sql
      SELECT TOP 3 Id, Judul, Status, ValidUntil FROM TrainingRecords WHERE Status = 'Permanent'
      ```
    - Login sebagai worker → My Records → row tersebut harus tampil badge hijau "Permanen" (bukan "—")

    **B4. CMP-09 (HtmlEncode search):**
    - Cari training/assessment dengan title mengandung "&" (atau create temporary seed: title "Q&A Session 2026")
    - Di My Records search box ketik "&"
    - Expected: row tersebut MUNCUL di hasil (tidak ter-hide)
    - Pre-fix behavior (bug): row ter-hide karena `data-title="q&amp;a session"` tidak match "&"

    **B5. CMP-10 (Per-filter counter):**
    - Di My Records, ketik search yang menyaring beberapa row
    - Expected: Badge `My Records (N)` di tab DAN 3 stat card (Assessment/Training/Total) update real-time ke jumlah visible
    - Pre-fix behavior (bug): semua counter static = total awal

    **B6. CMP-11 (AttemptNumber title-null):**
    - Verify via SQL bila ada AssessmentSession title-null:
      ```sql
      SELECT COUNT(*) FROM AssessmentSessions WHERE Title IS NULL OR Title = ''
      ```
    - Bila ada multiple session per UserId, expected: AttemptNumber tetap 1 (tidak collide ke nilai gede)
    - Bila tidak ada title-null session di lokal, SKIP (regression check sudah cukup)

    **C. Regression smoke:**
    - Filter Section + Unit masih bekerja (Phase 215 pattern)
    - Date range filter masih bekerja (Phase 239)
    - Reset button clear semua filter
    - L4 user (SectionHead) section lock masih enforce server-side

    **D. Cleanup:**
    - Restore DB bila ada temporary seed:
      ```bash
      sqlcmd -S "(localdb)\MSSQLLocalDB" -E -Q "RESTORE DATABASE [HCPortalDb] FROM DISK='C:\Temp\HCPortalDb_pre_337-01_uat.bak' WITH REPLACE"
      ```
    - Update `docs/SEED_JOURNAL.md` bila ada temporary seed created
  </how-to-verify>
  <resume-signal>
    Ketik "approved" bila ALL 5 CRITICAL + 6 data integrity PASS browser-verified. Bila ada FAIL, sebutkan REQ-ID (e.g., "CMP-04 fail — POJT masih muncul") supaya fix iterasi.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CMPController endpoints | Untrusted query params (section, unit, category, subCategory, statusFilter, dateFrom, dateTo) crossing dari client AJAX |
| CMPController → WorkerDataService | Internal trusted call, tetapi user-supplied filter values di-forward |
| WorkerDataService → ApplicationDbContext | LINQ-to-EF query — parameterized via EF, low SQL injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-337-01-01 | Tampering | `GetWorkersInSection` filter logic regression | mitigate | Playwright UAT Task 4 covers 5 CRITICAL — regression detect bila row count tidak sesuai expected. Bila build error tidak detect logic bug → UAT mandatory. |
| T-337-01-02 | Information Disclosure | L4 user (SectionHead) section lock bypass via `?section=` param injection | mitigate | Existing code L749 `sectionFilter = roleLevel == 4 ? user.Section : section` PRESERVE — Task 2 tidak menyentuh logic ini. Regression check di Task 4 Part C. |
| T-337-01-03 | Information Disclosure | SertifikatUrl link bocor file path internal | accept | SertifikatUrl sudah di-render di admin view existing (TrainingAdmin) — tidak ada new disclosure surface. `rel="noopener"` ditambah untuk hindari window.opener attack. |
| T-337-01-04 | Denial of Service | SubCategory filter trigger full TrainingRecords scan in-memory | accept | Existing `GetWorkersInSection` sudah load TrainingRecords via Include — Plan 01 hanya tambah .Where post-load. Performance regression dibahas di Wave 3 (Plan 03 SQL push-down). Untuk org < 1000 worker acceptable. |
| T-337-01-05 | Elevation of Privilege | RecordsTeamPartial L5/L6 access bypass | mitigate | Existing `if (roleLevel >= 5) return Forbid();` L746 PRESERVE. Task 2 tidak modify auth check. |
| T-337-01-06 | Repudiation | Filter change tidak ter-audit | accept | Read-only filter — tidak modify data, AuditLog tidak applicable per CONCERNS.md convention. |
</threat_model>

<verification>
- `dotnet build` PASS untuk seluruh solution
- Playwright UAT 5 CRITICAL filter PASS (CMP-01..05)
- Playwright UAT 6 data integrity PASS (CMP-06..11)
- Regression smoke PASS (Section/Unit/Date/Reset/L4 lock)
- DB restored bila ada temporary seed
- `docs/SEED_JOURNAL.md` updated bila ada seed yang dibuat
</verification>

<success_criteria>
- [ ] 11 REQ (CMP-01..11) implemented + 4 file modified + 0 build error
- [ ] 5 CRITICAL filter Playwright-verified narrow worker list correctly
- [ ] 6 data integrity Playwright-verified render correctly
- [ ] L4 section lock regression-verified
- [ ] Threat T-337-01-01..05 mitigated, T-337-01-03/04/06 accepted dengan rationale
- [ ] User explicit "approved" sebelum claim Wave 1 complete
</success_criteria>

<output>
Setelah completion, create `.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-01-SUMMARY.md` dengan format `$HOME/.claude/get-shit-done/templates/summary.md`. Wajib catat:
- 11 REQ closed (CMP-01..11)
- Files modified + line ranges
- UAT result per REQ-ID (PASS/FAIL/SKIP dengan reason)
- Threats mitigated/accepted
- Next: Wave 2 Plan 02 (UX + Quality 12 REQ)
</output>
