---
phase: 337-cmp-records-full-overhaul-filter-data-arch-a11y
plan: 02
type: execute
wave: 2
depends_on: [337-01]
files_modified:
  - Models/ViewModels/CMPRecordsViewModel.cs
  - Controllers/CMPController.cs
  - Views/CMP/Records.cshtml
  - Views/CMP/RecordsTeam.cshtml
  - Views/CMP/_RecordsTeamBody.cshtml
autonomous: false
requirements: [CMP-12, CMP-13, CMP-14, CMP-15, CMP-16, CMP-17, CMP-18, CMP-19, CMP-20, CMP-21, CMP-22, CMP-23]
must_haves:
  truths:
    - "AJAX filterTeamTable abort late response — late response TIDAK timpa fresh data (CMP-12)"
    - "Export URL sync sebelum klik — updateExportLinks dipanggil di awal doFetch, bukan di akhir (CMP-13)"
    - "Sub Category dropdown DISABLED secara visual saat Category tidak punya children (CMP-14)"
    - "Date filter UX hint counter turun — pesan 'Beberapa worker hidden karena tidak ada record di rentang' tampil saat counter < total (CMP-15)"
    - "My Records tab punya year quick filter button group '2024 | 2025 | 2026 | Semua Tahun' (CMP-16, OQ-337-4)"
    - "Filter state persist saat switch tab — values disimpan ke sessionStorage, restore on tab return (CMP-17)"
    - "_RecordsTeamBody dead data-* attributes (data-categories/subcategories/completed-*) DIHAPUS, hanya data-section/unit/name/nip yang tersisa (CMP-18)"
    - "Row My Records keyboard-navigable — Enter trigger navigate (replace inline onclick) (CMP-19)"
    - "Tab nav <a> punya role='tab' + aria-controls + aria-selected (CMP-20)"
    - "CMPRecordsViewModel encapsulate (User, RoleLevel, UnifiedRecords, etc) — view TIDAK panggil UserManager.GetUserAsync (CMP-21, OQ-337-1)"
    - "roleLevel computed ONLY di controller, view consume via Model.RoleLevel (CMP-22)"
    - "Year list dihitung sekali di controller, di-passing via ViewModel.YearOptions (CMP-23)"
  artifacts:
    - path: "Models/ViewModels/CMPRecordsViewModel.cs"
      provides: "ViewModel encapsulate User+RoleLevel+UnifiedRecords+YearOptions"
      contains: "public class CMPRecordsViewModel"
    - path: "Controllers/CMPController.cs"
      provides: "Records action populate ViewModel + pass single source roleLevel"
      contains: "new CMPRecordsViewModel"
    - path: "Views/CMP/Records.cshtml"
      provides: "Year quick filter buttons + sessionStorage state persist + ARIA tab roles + keyboard nav + ViewModel consume"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "AbortController + updateExportLinks ordering fix + SubCategory disabled state + Date hint + sessionStorage"
    - path: "Views/CMP/_RecordsTeamBody.cshtml"
      provides: "Dead data-* purge (only data-section/unit/name/nip preserved)"
  key_links:
    - from: "Views/CMP/Records.cshtml"
      to: "Models/ViewModels/CMPRecordsViewModel.cs"
      via: "@model HcPortal.Models.ViewModels.CMPRecordsViewModel"
      pattern: "@model HcPortal\\.Models\\.ViewModels\\.CMPRecordsViewModel"
    - from: "Views/CMP/RecordsTeam.cshtml filterTeamTable JS"
      to: "doFetch dengan AbortController"
      via: "currentAbortController.abort() sebelum new fetch"
      pattern: "AbortController|currentAbortController"
    - from: "Views/CMP/RecordsTeam.cshtml filter inputs"
      to: "sessionStorage 'cmp-records-team-filter'"
      via: "sessionStorage.setItem/getItem on change/load"
      pattern: "sessionStorage\\.(setItem|getItem).*records"
---

<objective>
Wave 2 Phase 337 — Fix 7 UX REQ (race AJAX + export sync + dropdown state + date hint + year parity + state persist + dead code purge) + 5 Quality REQ (keyboard nav + ARIA tab + ViewModel refactor + roleLevel dedup + year memoize). Dibangun di atas Wave 1 baseline filter correctness.

Purpose: Hilangkan race condition (late AJAX timpa fresh), stale export URL, dead code bloat (~10 data-* per row), dan ViewModel/role duplication yang menambah NRE risk. Setup foundation untuk Wave 3 arch (clean view consume ViewModel, bukan UserManager lookup).

Output:
- `Models/ViewModels/CMPRecordsViewModel.cs` NEW — encapsulate User+RoleLevel+UnifiedRecords+YearOptions+TotalCount
- `Controllers/CMPController.cs` Records action — populate ViewModel single-source roleLevel + memoize year list
- `Views/CMP/Records.cshtml` — consume ViewModel + year quick filter button group + ARIA tab + keyboard nav + sessionStorage state persist
- `Views/CMP/RecordsTeam.cshtml` — AbortController + updateExportLinks early + SubCategory disabled state + Date hint + sessionStorage restore + ARIA tab
- `Views/CMP/_RecordsTeamBody.cshtml` — purge dead data-* attributes (data-categories, data-subcategories, data-completed-categories, data-completed-subcategories, data-has-training)

OQ-337-1 RESOLVED: 1 ViewModel (`CMPRecordsViewModel`) untuk My Records, Team View TIDAK butuh ViewModel terpisah (List<WorkerTrainingStatus> sudah cukup, dipassing via ViewData["WorkerList"] existing).

OQ-337-4 RESOLVED: Year quick filter pakai button group `<div class="btn-group">` dengan 3 year terakhir + "Semua Tahun", lebih kontekstual untuk lab worker (mostly 1-2 tahun terakhir).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-CONTEXT.md
@.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-01-PLAN.md
@CLAUDE.md
@docs/DEV_WORKFLOW.md

# File sumber
@Controllers/CMPController.cs
@Views/CMP/Records.cshtml
@Views/CMP/RecordsTeam.cshtml
@Views/CMP/_RecordsTeamBody.cshtml
@Models/AssessmentMonitoringViewModel.cs

<interfaces>
<!-- Existing ViewModel pattern reference -->

From Models/AssessmentMonitoringViewModel.cs (pattern reference):
```csharp
namespace HcPortal.Models
{
    public class MonitoringGroupViewModel
    {
        public int RepresentativeId { get; set; }
        public string Title { get; set; } = "";
        // ... primitive + nested ViewModel
        public List<MonitoringSessionViewModel> Sessions { get; set; } = new();
    }
}
```

Convention catatan: ViewModels di-namespace `HcPortal.Models` (flat) BUKAN `HcPortal.Models.ViewModels` — tetapi memory note user mention "Models/ViewModels/CMPRecordsViewModel.cs". Verify dengan ls:
```bash
ls Models/ViewModels/ 2>/dev/null && ls Models/*.cs | head -5
```
Bila `Models/ViewModels/` tidak ada → create directory + namespace `HcPortal.Models.ViewModels` (better isolation).
Bila convention flat di Models/ → namespace `HcPortal.Models` (consistent dengan AssessmentMonitoringViewModel).

DECISION untuk plan ini: create `Models/ViewModels/CMPRecordsViewModel.cs` dengan namespace `HcPortal.Models.ViewModels` (better organization, low conflict risk, follows memory note path).

From Controllers/CMPController.cs L479-520 (current Records action):
```csharp
public async Task<IActionResult> Records(string? section, ...)
{
    var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
    if (user == null) return RedirectToAction("Login", "Account");
    var unified = await _workerDataService.GetUnifiedRecords(user.Id);

    if (roleLevel <= 4) {
        // Team View setup — ViewData["WorkerList"], ViewBag.SubCategoryMapJson, etc
    }

    return View("Records", unified);   // ← model = List<UnifiedTrainingRecord>
}
```

TARGET: Records action wrap unified ke ViewModel:
```csharp
return View("Records", new CMPRecordsViewModel {
    User = user,
    RoleLevel = roleLevel,
    UnifiedRecords = unified,
    AssessmentCount = unified.Count(r => r.RecordType == "Assessment Online"),
    TrainingCount = unified.Count(r => r.RecordType == "Training Manual"),
    TotalCount = unified.Count,
    YearOptions = unified.Select(r => r.Date.Year).Distinct().OrderByDescending(y => y).ToList()
});
```

From Views/CMP/Records.cshtml L1-18 (current — duplicate UserManager call):
```cshtml
@model List<HcPortal.Models.UnifiedTrainingRecord>
@inject UserManager<ApplicationUser> UserManager

@{
    var currentUser = await UserManager.GetUserAsync(User);
    var userRoles = await UserManager.GetRolesAsync(currentUser);
    var userRole = userRoles.FirstOrDefault();
    var roleLevel = UserRoles.GetRoleLevel(userRole ?? "");
}
```

TARGET:
```cshtml
@model HcPortal.Models.ViewModels.CMPRecordsViewModel

@{
    var roleLevel = Model.RoleLevel;
    var totalCount = Model.TotalCount;
    var assessmentCount = Model.AssessmentCount;
    var trainingCount = Model.TrainingCount;
    var unified = Model.UnifiedRecords;
}
```

Hapus `@inject UserManager` + delete `UserManager.GetUserAsync` block.

From Views/CMP/RecordsTeam.cshtml L1-17 (current — duplicate UserManager):
```cshtml
@model List<HcPortal.Models.WorkerTrainingStatus>
@inject UserManager<ApplicationUser> UserManager

@{
    var currentUser = await UserManager.GetUserAsync(User);
    var userRoles = await UserManager.GetRolesAsync(currentUser);
    var userRole = userRoles.FirstOrDefault();
    var roleLevel = UserRoles.GetRoleLevel(userRole ?? "");
    // ...
    var lockedSection = roleLevel == 4 ? currentUser.Section : null;
}
```

Karena `RecordsTeam.cshtml` dipanggil sebagai PartialAsync dari Records.cshtml L221 dengan model `workerList` (List<WorkerTrainingStatus>) — pertahankan signature partial, TAPI inject `ViewData["RoleLevel"]` dan `ViewData["LockedSection"]` dari controller supaya UserManager call dihapus.

Edit Records.cshtml L221:
```cshtml
@{ ViewData["RoleLevel"] = Model.RoleLevel; ViewData["LockedSection"] = Model.RoleLevel == 4 ? Model.User.Section : null; }
@await Html.PartialAsync("RecordsTeam", workerList)
```

Edit RecordsTeam.cshtml L1-17:
```cshtml
@model List<HcPortal.Models.WorkerTrainingStatus>

@{
    var roleLevel = (int)(ViewData["RoleLevel"] ?? 5);
    var lockedSection = ViewData["LockedSection"] as string;
    var sectionUnitsJson = ViewBag.SectionUnitsJson as string ?? "{}";
    var allSections = (ViewBag.AllSections as List<string>) ?? new List<string>();
}
```

From Views/CMP/Records.cshtml L35-49 (current tab nav — no ARIA):
```cshtml
<ul class="nav nav-tabs mb-3" id="recordsTabs">
    <li class="nav-item">
        <a class="nav-link active" id="tab-myrecords" data-bs-toggle="tab" href="#pane-myrecords">
            <i class="bi bi-person me-1"></i>My Records
            <span class="badge bg-info ms-1">@totalCount</span>
        </a>
    </li>
    @if (roleLevel <= 4) {
        <li class="nav-item">
            <a class="nav-link" id="tab-team" data-bs-toggle="tab" href="#pane-team">
                <i class="bi bi-people me-1"></i>Team View
            </a>
        </li>
    }
</ul>
```

TARGET (ARIA roles per CMP-20):
```cshtml
<ul class="nav nav-tabs mb-3" id="recordsTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <a class="nav-link active" id="tab-myrecords" data-bs-toggle="tab" href="#pane-myrecords"
           role="tab" aria-controls="pane-myrecords" aria-selected="true">
            <i class="bi bi-person me-1" aria-hidden="true"></i>My Records
            <span id="totalCountBadge" class="badge bg-info ms-1">@totalCount</span>
        </a>
    </li>
    @if (roleLevel <= 4) {
        <li class="nav-item" role="presentation">
            <a class="nav-link" id="tab-team" data-bs-toggle="tab" href="#pane-team"
               role="tab" aria-controls="pane-team" aria-selected="false">
                <i class="bi bi-people me-1" aria-hidden="true"></i>Team View
            </a>
        </li>
    }
</ul>
```

Plus tab-pane: `<div class="tab-pane fade show active" id="pane-myrecords" role="tabpanel" aria-labelledby="tab-myrecords">`.

From Views/CMP/Records.cshtml L156-159 (inline onclick BUG — CMP-19):
```cshtml
<tr class="training-row @(resultsUrl != null ? "table-row-clickable" : "")"
    data-title="@item.Title.ToLower()" data-year="@item.Date.Year"
    @(resultsUrl != null ? Html.Raw($"onclick=\"window.location.href='{resultsUrl}'\"") : Html.Raw(""))
    style="@(resultsUrl != null ? "cursor:pointer;" : "")">
```

TARGET (data-href + delegated event + keyboard):
```cshtml
<tr class="training-row @(resultsUrl != null ? "table-row-clickable" : "")"
    data-title="@item.Title.ToLower()" data-year="@item.Date.Year"
    data-type="@(item.RecordType == "Assessment Online" ? "assessment" : "training")"
    @(resultsUrl != null ? Html.Raw($"data-href=\"{resultsUrl}\" tabindex=\"0\" role=\"link\"") : Html.Raw(""))
    style="@(resultsUrl != null ? "cursor:pointer;" : "")">
```

Di `<script>` Records.cshtml tambah:
```javascript
document.querySelectorAll('.training-row[data-href]').forEach(row => {
    row.addEventListener('click', () => window.location.href = row.dataset.href);
    row.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            window.location.href = row.dataset.href;
        }
    });
});
```

From Views/CMP/RecordsTeam.cshtml L258-299 (current doFetch — no AbortController):
```javascript
let debounceTimer;
function filterTeamTable() {
    clearTimeout(debounceTimer);
    debounceTimer = setTimeout(doFetch, 300);
}

async function doFetch() {
    // ... fetch(url, { headers: ... })
    // No abort signal — late response timpa fresh
    // updateExportLinks() called di AKHIR (after fetch await) — stale window
}
```

TARGET (AbortController + early updateExportLinks):
```javascript
let debounceTimer;
let currentAbortController = null;

function filterTeamTable() {
    clearTimeout(debounceTimer);
    debounceTimer = setTimeout(doFetch, 300);
    // Save filter state immediately (sessionStorage)
    saveFilterState();
    // Update export URL immediately (CMP-13)
    updateExportLinks();
}

async function doFetch() {
    // Abort previous request bila masih in-flight (CMP-12)
    if (currentAbortController) {
        currentAbortController.abort();
    }
    currentAbortController = new AbortController();

    // ... build params ...

    try {
        const resp = await fetch(url, {
            headers: { 'X-Requested-With': 'XMLHttpRequest' },
            signal: currentAbortController.signal
        });
        // ... render
    } catch (e) {
        if (e.name === 'AbortError') return;  // Ignore aborted
        // ... existing error handling
    }
}
```

From Views/CMP/RecordsTeam.cshtml L213-230 categoryFilter change (SubCategory state):
```javascript
document.getElementById('categoryFilter').addEventListener('change', function() {
    var cat = this.value;
    var subSelect = document.getElementById('subCategoryFilter');
    subSelect.innerHTML = '<option value="">All Sub Categories</option>';
    subSelect.disabled = !cat;   // ← partial fix: disabled bila Category kosong
    if (cat && subCategoryMap[cat]) { ... }
    filterTeamTable();
});
```

TARGET (CMP-14 — disabled bila Category dipilih tapi tidak punya children):
```javascript
document.getElementById('categoryFilter').addEventListener('change', function() {
    var cat = this.value;
    var subSelect = document.getElementById('subCategoryFilter');
    subSelect.innerHTML = '<option value="">All Sub Categories</option>';
    var hasChildren = cat && subCategoryMap[cat] && subCategoryMap[cat].length > 0;
    subSelect.disabled = !hasChildren;
    if (hasChildren) {
        subCategoryMap[cat].forEach(function(sub) {
            var opt = document.createElement('option');
            opt.value = sub;
            opt.textContent = sub;
            subSelect.appendChild(opt);
        });
    }
    filterTeamTable();
});
```

From Views/CMP/_RecordsTeamBody.cshtml L47-56 (dead code — CMP-18):
```cshtml
<tr class="worker-row"
    data-section="@worker.Section"
    data-unit="@worker.Unit"
    data-categories="@allCatsStr.ToLower()"            @* DEAD — server-side filter sekarang *@
    data-has-training="@hasTraining.ToString().ToLower()"  @* DEAD *@
    data-completed-categories="@completedCatsStr.ToLower()" @* DEAD *@
    data-subcategories="@allSubCatsStr.ToLower()"      @* DEAD *@
    data-completed-subcategories="@completedSubCatsStr.ToLower()" @* DEAD *@
    data-name="@worker.WorkerName.ToLower()"           @* KEEP — bila client-side search ditambah *@
    data-nip="@((worker.NIP ?? "").ToLower())">        @* KEEP *@
```

TARGET (purge dead code + delete preparation L14-45):
```cshtml
<tr class="worker-row"
    data-section="@worker.Section"
    data-unit="@worker.Unit"
    data-name="@worker.WorkerName.ToLower()"
    data-nip="@((worker.NIP ?? "").ToLower())">
```

Plus delete preparation code L17-45 (trainingCats2/assessmentCats2/allCats/completedTrainingCats/passedAssessmentCats/allSubCats/completedSubCats/hasTraining variables — semuanya dead).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CMPRecordsViewModel + refactor Records action populate ViewModel (CMP-21, CMP-22, CMP-23)</name>
  <files>Models/ViewModels/CMPRecordsViewModel.cs, Controllers/CMPController.cs, Views/CMP/Records.cshtml, Views/CMP/RecordsTeam.cshtml</files>
  <behavior>
    Setelah refactor:
    - NEW file `Models/ViewModels/CMPRecordsViewModel.cs` dengan namespace `HcPortal.Models.ViewModels`, kelas `CMPRecordsViewModel` punya properties: `ApplicationUser User`, `int RoleLevel`, `List<UnifiedTrainingRecord> UnifiedRecords`, `int AssessmentCount`, `int TrainingCount`, `int TotalCount`, `List<int> YearOptions`
    - `CMPController.Records` action populate ViewModel + pass via View()
    - `Records.cshtml` `@model` ganti ke `CMPRecordsViewModel`, hapus `@inject UserManager` + 4 baris `UserManager.GetUserAsync` block, consume `Model.RoleLevel/TotalCount/etc`
    - `RecordsTeam.cshtml` hapus `@inject UserManager` + `UserManager.GetUserAsync` block, consume `ViewData["RoleLevel"]` + `ViewData["LockedSection"]` yang di-set dari Records.cshtml partial call
  </behavior>
  <action>
    1. **Cek convention ViewModels directory:**
    ```bash
    ls Models/ViewModels/ 2>/dev/null
    ```
    Bila tidak ada, create directory (mkdir akan dilakukan saat Write file).

    2. **Create `Models/ViewModels/CMPRecordsViewModel.cs`:**
    ```csharp
    using HcPortal.Models;

    namespace HcPortal.Models.ViewModels
    {
        public class CMPRecordsViewModel
        {
            public ApplicationUser User { get; set; } = null!;
            public int RoleLevel { get; set; }
            public List<UnifiedTrainingRecord> UnifiedRecords { get; set; } = new();
            public int AssessmentCount { get; set; }
            public int TrainingCount { get; set; }
            public int TotalCount { get; set; }
            public List<int> YearOptions { get; set; } = new();
        }
    }
    ```

    3. **Edit `Controllers/CMPController.cs` Records action L479-520** — populate ViewModel:
    ```csharp
    public async Task<IActionResult> Records(string? section, string? unit, string? category, string? search, string? statusFilter, string? isFiltered)
    {
        var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
        if (user == null) return RedirectToAction("Login", "Account");

        var unified = await _workerDataService.GetUnifiedRecords(user.Id);

        // CMP-23: memoize year list di controller
        var yearOptions = unified.Select(r => r.Date.Year).Distinct().OrderByDescending(y => y).ToList();

        var vm = new HcPortal.Models.ViewModels.CMPRecordsViewModel
        {
            User = user,
            RoleLevel = roleLevel,
            UnifiedRecords = unified,
            AssessmentCount = unified.Count(r => r.RecordType == "Assessment Online"),
            TrainingCount = unified.Count(r => r.RecordType == "Training Manual"),
            TotalCount = unified.Count,
            YearOptions = yearOptions
        };

        // Phase 104: Get worker list for Team View tab (only for users level 1-4)
        if (roleLevel <= 4)
        {
            // ... existing block PRESERVE (sectionFilter + workerList + ViewBag setup) ...
            // existing code di L487-517 tetap utuh
        }

        return View("Records", vm);
    }
    ```

    Tambah `using HcPortal.Models.ViewModels;` di top jika belum ada (atau pakai FQN).

    4. **Edit `Views/CMP/Records.cshtml` L1-18** — ganti model + hapus duplicate UserManager:
    ```cshtml
    @model HcPortal.Models.ViewModels.CMPRecordsViewModel
    @using System.Globalization
    @using HcPortal.Models

    @{
        ViewData["Title"] = "Training Records";
        var unified = Model.UnifiedRecords;
        var assessmentCount = Model.AssessmentCount;
        var trainingCount = Model.TrainingCount;
        var totalCount = Model.TotalCount;
        var roleLevel = Model.RoleLevel;
        var yearOptions = Model.YearOptions;
    }
    ```

    Hapus baris:
    - `@using Microsoft.AspNetCore.Identity`
    - `@inject UserManager<ApplicationUser> UserManager`
    - 4 baris `var currentUser = await UserManager...` block

    5. **Update reference Model di Records.cshtml** — semua `Model.OrderByDescending` / `Model.Select` / `Model.Count` / `Model.Any` ganti ke `unified.OrderByDescending` / `unified.Select` / dst (variable `unified` sudah di-extract di top).

    Spesifik:
    - L65 `@foreach (var year in Model.Select(r => r.Date.Year).Distinct().OrderByDescending(y => y))` → `@foreach (var year in yearOptions)` (CMP-23 memoize)
    - L151 `@foreach (var item in Model.OrderByDescending(r => r.Date))` → `@foreach (var item in unified.OrderByDescending(r => r.Date))`
    - L203 `@if (!Model.Any())` → `@if (!unified.Any())`
    - L218 `var workerList = ViewData["WorkerList"] as List<HcPortal.Models.WorkerTrainingStatus>;` → tetap (ViewData passing dari controller untuk Team View)

    6. **Edit Records.cshtml L221** — pass RoleLevel + LockedSection ke partial via ViewData:
    ```cshtml
    @{
        ViewData["RoleLevel"] = Model.RoleLevel;
        ViewData["LockedSection"] = Model.RoleLevel == 4 ? Model.User.Section : null;
    }
    @await Html.PartialAsync("RecordsTeam", workerList)
    ```

    7. **Edit `Views/CMP/RecordsTeam.cshtml` L1-17** — hapus duplicate UserManager:
    ```cshtml
    @model List<HcPortal.Models.WorkerTrainingStatus>
    @using HcPortal.Models

    @{
        var roleLevel = (int)(ViewData["RoleLevel"] ?? 5);
        var lockedSection = ViewData["LockedSection"] as string;
        var sectionUnitsJson = ViewBag.SectionUnitsJson as string ?? "{}";
        var allSections = (ViewBag.AllSections as List<string>) ?? new List<string>();
    }
    ```

    Hapus baris `@inject UserManager` + `using Microsoft.AspNetCore.Identity` + 4 baris `UserManager.GetUserAsync`.

    8. **Build verify:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - `Models/ViewModels/CMPRecordsViewModel.cs` exists dengan 7 property
    - `CMPController.Records` populate ViewModel
    - `Records.cshtml` `@model` ganti ke `CMPRecordsViewModel`, tidak ada lagi `UserManager.GetUserAsync`
    - `RecordsTeam.cshtml` consume `ViewData["RoleLevel"]`, tidak ada lagi `UserManager.GetUserAsync`
    - All `Model.OrderByDescending`/`Model.Select` di Records.cshtml diganti ke `unified` local var
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Records.cshtml — ARIA tab + keyboard nav + year quick filter button group + per-tab state persist (CMP-16, CMP-17, CMP-19, CMP-20)</name>
  <files>Views/CMP/Records.cshtml</files>
  <behavior>
    Setelah edit:
    - Tab nav `<ul role="tablist">` + `<a role="tab" aria-controls aria-selected>` + `<div role="tabpanel" aria-labelledby>` (CMP-20)
    - Tab change handler update aria-selected dinamis saat klik tab
    - Row `<tr>` dengan data-href + tabindex="0" + role="link" — keyboard Enter trigger navigate (CMP-19)
    - Inline `onclick="window.location.href=..."` DIHAPUS dari Razor, diganti delegated event di `<script>`
    - Year quick filter button group `<div class="btn-group">` dengan 3 year terakhir dari `Model.YearOptions.Take(3)` + button "Semua Tahun" (CMP-16, OQ-337-4)
    - Filter state (searchInput + yearFilter) persist via sessionStorage key `cmp-records-my-filter`, restore on DOMContentLoaded (CMP-17)
  </behavior>
  <action>
    1. **Edit tab nav L35-49** — tambah ARIA:
    ```cshtml
    <ul class="nav nav-tabs mb-3" id="recordsTabs" role="tablist">
        <li class="nav-item" role="presentation">
            <a class="nav-link active" id="tab-myrecords" data-bs-toggle="tab" href="#pane-myrecords"
               role="tab" aria-controls="pane-myrecords" aria-selected="true">
                <i class="bi bi-person me-1" aria-hidden="true"></i>My Records
                <span id="totalCountBadge" class="badge bg-info ms-1">@totalCount</span>
            </a>
        </li>
        @if (roleLevel <= 4)
        {
            <li class="nav-item" role="presentation">
                <a class="nav-link" id="tab-team" data-bs-toggle="tab" href="#pane-team"
                   role="tab" aria-controls="pane-team" aria-selected="false">
                    <i class="bi bi-people me-1" aria-hidden="true"></i>Team View
                </a>
            </li>
        }
    </ul>
    ```

    2. **Edit tab-pane L54, L216** — tambah role=tabpanel:
    ```cshtml
    <div class="tab-pane fade show active" id="pane-myrecords" role="tabpanel" aria-labelledby="tab-myrecords">
    <div class="tab-pane fade" id="pane-team" role="tabpanel" aria-labelledby="tab-team">
    ```

    3. **Replace year dropdown L62-70 dengan button group (CMP-16):**
    ```cshtml
    <div class="col-12 col-md-6">
        <div class="btn-group w-100" role="group" aria-label="Filter tahun">
            @foreach (var year in yearOptions.Take(3))
            {
                <button type="button" class="btn btn-outline-secondary year-quick-btn" data-year-value="@year">@year</button>
            }
            <button type="button" class="btn btn-outline-secondary year-quick-btn active" data-year-value="">Semua Tahun</button>
        </div>
        <select id="yearFilter" class="d-none">
            <option value="">Semua Tahun</option>
            @foreach (var year in yearOptions)
            {
                <option value="@year">@year</option>
            }
        </select>
    </div>
    ```
    Hidden select tetap ada (existing JS filterTable() baca `getElementById('yearFilter').value` — minim invasive). Button group set value ke hidden select on click.

    Note: kolom width row L58 berubah `col-md-3` → `col-md-6` (button group butuh space). Reset button di L71 → pindah col atau biarkan sebelahnya. Adjust grid: input search col-md-6, year button group col-md-3 (3 button kompak), reset col-md-3. Atau buat 2 row.

    Adjusted layout — Edit L58-83 baris filter:
    ```cshtml
    <div class="row g-3 align-items-center">
        <div class="col-12 col-md-6">
            <input type="text" id="searchInput" class="form-control" placeholder="Cari berdasarkan judul...">
        </div>
        <div class="col-12 col-md-6">
            <div class="btn-group w-100" role="group" aria-label="Filter tahun">
                @foreach (var year in yearOptions.Take(3))
                {
                    <button type="button" class="btn btn-outline-secondary year-quick-btn" data-year-value="@year">@year</button>
                }
                <button type="button" class="btn btn-outline-secondary year-quick-btn active" data-year-value="">Semua</button>
            </div>
            <select id="yearFilter" class="d-none">
                <option value="">Semua Tahun</option>
                @foreach (var year in yearOptions)
                {
                    <option value="@year">@year</option>
                }
            </select>
        </div>
    </div>
    <div class="row g-3 mt-2 align-items-center">
        <div class="col-12 col-md-6">
            <button class="btn btn-outline-secondary w-100" onclick="clearFilters()">
                <i class="bi bi-x-circle me-1" aria-hidden="true"></i> Reset
            </button>
        </div>
        <div class="col-12 col-md-6 d-flex align-items-center">
            <a href="@Url.Action("ExportRecords", "CMP")" class="btn btn-outline-success w-100">
                <i class="bi bi-file-earmark-arrow-down me-2" aria-hidden="true"></i>Export Excel
            </a>
        </div>
    </div>
    ```

    4. **Edit row L156** — replace inline onclick dengan data-href (CMP-19):
    ```cshtml
    <tr class="training-row @(resultsUrl != null ? "table-row-clickable" : "")"
        data-title="@item.Title.ToLower()" data-year="@item.Date.Year"
        data-type="@(item.RecordType == "Assessment Online" ? "assessment" : "training")"
        @(resultsUrl != null ? Html.Raw($"data-href=\"{resultsUrl}\" tabindex=\"0\" role=\"link\"") : Html.Raw(""))
        style="@(resultsUrl != null ? "cursor:pointer;" : "")">
    ```
    Note: `data-type` sudah di-tambah di Plan 01 Task 3 — pastikan tidak duplikat. Bila konflik, edit harus dari Plan 01 baseline.

    5. **Edit `<script>` block L307-345** — tambah keyboard nav + year quick + tab ARIA + sessionStorage:
    ```javascript
    // CMP-09 helper (dari Plan 01 — preserve)
    function decodeEntities(s) {
        if (!s) return '';
        var d = document.createElement('div');
        d.innerHTML = s;
        return d.textContent || '';
    }

    function filterTable() {
        const searchTerm = document.getElementById('searchInput').value.toLowerCase();
        const yearFilter = document.getElementById('yearFilter').value;
        let visibleCount = 0, visibleAssessment = 0, visibleTraining = 0;
        document.querySelectorAll('#recordsTable .training-row').forEach(row => {
            const title = decodeEntities(row.getAttribute('data-title'));
            const year = row.getAttribute('data-year');
            const type = row.getAttribute('data-type');
            const matchSearch = !searchTerm || title.includes(searchTerm);
            const matchYear = !yearFilter || year === yearFilter;
            const show = matchSearch && matchYear;
            row.style.display = show ? '' : 'none';
            if (show) {
                visibleCount++;
                if (type === 'assessment') visibleAssessment++;
                else if (type === 'training') visibleTraining++;
            }
        });

        // CMP-10 counter (Plan 01) — preserve
        var totalBadge = document.getElementById('totalCountBadge');
        if (totalBadge) totalBadge.textContent = visibleCount;
        var aStat = document.getElementById('assessmentStatCount');
        if (aStat) aStat.textContent = visibleAssessment;
        var tStat = document.getElementById('trainingStatCount');
        if (tStat) tStat.textContent = visibleTraining;
        var totalStat = document.getElementById('totalStatCount');
        if (totalStat) totalStat.textContent = visibleCount;

        // CMP-17 persist state
        sessionStorage.setItem('cmp-records-my-filter', JSON.stringify({ search: searchTerm, year: yearFilter }));

        // Empty state existing logic preserve
        var emptyRow = document.getElementById('myRecordsEmptyState');
        if (visibleCount === 0 && document.querySelectorAll('#recordsTable .training-row').length > 0) {
            if (!emptyRow) {
                var tbody = document.querySelector('#recordsTable tbody');
                var tr = document.createElement('tr');
                tr.id = 'myRecordsEmptyState';
                tr.innerHTML = '<td colspan="6" class="text-center p-5 text-muted"><i class="bi bi-inbox fs-1 d-block mb-2"></i>Data belum ada</td>';
                tbody.appendChild(tr);
            } else { emptyRow.style.display = ''; }
        } else if (emptyRow) { emptyRow.style.display = 'none'; }
    }

    document.getElementById('searchInput').addEventListener('input', filterTable);
    document.getElementById('yearFilter').addEventListener('change', filterTable);

    // CMP-16 year quick filter button group
    document.querySelectorAll('.year-quick-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            document.querySelectorAll('.year-quick-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            document.getElementById('yearFilter').value = btn.dataset.yearValue;
            filterTable();
        });
    });

    function clearFilters() {
        document.getElementById('searchInput').value = '';
        document.getElementById('yearFilter').value = '';
        document.querySelectorAll('.year-quick-btn').forEach(b => b.classList.remove('active'));
        var allBtn = document.querySelector('.year-quick-btn[data-year-value=""]');
        if (allBtn) allBtn.classList.add('active');
        sessionStorage.removeItem('cmp-records-my-filter');
        filterTable();
    }

    // CMP-19 keyboard nav row
    document.querySelectorAll('.training-row[data-href]').forEach(row => {
        row.addEventListener('click', () => window.location.href = row.dataset.href);
        row.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' || e.key === ' ') {
                e.preventDefault();
                window.location.href = row.dataset.href;
            }
        });
    });

    // CMP-20 tab ARIA selected toggle on shown.bs.tab
    document.querySelectorAll('#recordsTabs a[data-bs-toggle="tab"]').forEach(tab => {
        tab.addEventListener('shown.bs.tab', (e) => {
            document.querySelectorAll('#recordsTabs a[role="tab"]').forEach(t => t.setAttribute('aria-selected', 'false'));
            e.target.setAttribute('aria-selected', 'true');
        });
    });

    // CMP-17 restore state on load
    document.addEventListener('DOMContentLoaded', () => {
        var stored = sessionStorage.getItem('cmp-records-my-filter');
        if (stored) {
            try {
                var state = JSON.parse(stored);
                if (state.search) document.getElementById('searchInput').value = state.search;
                if (state.year) {
                    document.getElementById('yearFilter').value = state.year;
                    document.querySelectorAll('.year-quick-btn').forEach(b => {
                        b.classList.toggle('active', b.dataset.yearValue === state.year);
                    });
                }
                filterTable();
            } catch (e) { /* ignore parse error */ }
        }
    });
    ```

    6. **Build verify:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - Tab nav punya role=tablist + role=tab + aria-controls + aria-selected
    - Tab-pane punya role=tabpanel + aria-labelledby
    - aria-selected toggle dinamis lewat shown.bs.tab event
    - Row dengan data-href punya tabindex=0 + keyboard Enter/Space handler
    - Inline onclick dihapus dari Razor
    - Year quick filter button group dengan 3 year terakhir + "Semua"
    - sessionStorage 'cmp-records-my-filter' set/restore
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: RecordsTeam.cshtml — AbortController + early updateExportLinks + SubCategory disabled state + Date hint + sessionStorage (CMP-12, CMP-13, CMP-14, CMP-15, CMP-17)</name>
  <files>Views/CMP/RecordsTeam.cshtml</files>
  <behavior>
    Setelah edit:
    - `currentAbortController` global var, abort sebelum new fetch (CMP-12)
    - `updateExportLinks()` dipanggil IMMEDIATELY di `filterTeamTable()` (sebelum debounce setTimeout), BUKAN di akhir `doFetch` (CMP-13)
    - `subCategoryFilter` disabled bila Category tidak punya children di `subCategoryMap` (CMP-14)
    - Date filter trigger hint message "Beberapa worker tidak punya record di rentang tanggal — disembunyikan" saat `workerCount < initialCount` (CMP-15)
    - sessionStorage `cmp-records-team-filter` persist 7 filter values, restore on DOMContentLoaded (CMP-17)
  </behavior>
  <action>
    1. **Edit `<script>` block L258-329 RecordsTeam.cshtml** — restructure dengan AbortController + state persist:

    Replace seluruh block `let debounceTimer;` sampai akhir `</script>` dengan:
    ```javascript
    let debounceTimer;
    let currentAbortController = null;
    let initialWorkerCount = parseInt(document.getElementById('workerCount').textContent) || 0;

    function getFilterState() {
        return {
            section: document.getElementById('sectionFilter').value,
            unit: document.getElementById('unitFilter').value,
            category: document.getElementById('categoryFilter').value,
            subCategory: document.getElementById('subCategoryFilter').value,
            status: document.getElementById('statusFilter').value,
            dateFrom: document.getElementById('dateFrom').value,
            dateTo: document.getElementById('dateTo').value
        };
    }

    function saveFilterState() {
        sessionStorage.setItem('cmp-records-team-filter', JSON.stringify(getFilterState()));
    }

    function restoreFilterState() {
        var stored = sessionStorage.getItem('cmp-records-team-filter');
        if (!stored) return;
        try {
            var state = JSON.parse(stored);
            // Section: skip restore bila locked (L4)
            var sectionFilter = document.getElementById('sectionFilter');
            if (!sectionFilter.disabled && state.section) sectionFilter.value = state.section;
            if (state.section) updateUnitOptions();
            if (state.unit) document.getElementById('unitFilter').value = state.unit;
            if (state.category) {
                document.getElementById('categoryFilter').value = state.category;
                // trigger change to populate subcategory
                document.getElementById('categoryFilter').dispatchEvent(new Event('change'));
                if (state.subCategory) document.getElementById('subCategoryFilter').value = state.subCategory;
            }
            if (state.status) document.getElementById('statusFilter').value = state.status;
            if (state.dateFrom) document.getElementById('dateFrom').value = state.dateFrom;
            if (state.dateTo) document.getElementById('dateTo').value = state.dateTo;
        } catch (e) { /* ignore */ }
    }

    function filterTeamTable() {
        // CMP-13: update export links IMMEDIATELY (sebelum debounce)
        updateExportLinks();
        // CMP-17: persist state immediately
        saveFilterState();

        clearTimeout(debounceTimer);
        debounceTimer = setTimeout(doFetch, 300);
    }

    async function doFetch() {
        // CMP-12: abort previous in-flight request
        if (currentAbortController) {
            currentAbortController.abort();
        }
        currentAbortController = new AbortController();

        const params = new URLSearchParams();
        const s = getFilterState();
        if (s.section) params.set('section', s.section);
        if (s.unit) params.set('unit', s.unit);
        if (s.category) params.set('category', s.category);
        if (s.subCategory) params.set('subCategory', s.subCategory);
        if (s.status && s.status !== 'ALL') params.set('statusFilter', s.status);
        if (s.dateFrom) params.set('dateFrom', s.dateFrom);
        if (s.dateTo) params.set('dateTo', s.dateTo);

        document.getElementById('tableLoadingIndicator').style.display = '';

        try {
            const resp = await fetch('@Url.Action("RecordsTeamPartial", "CMP")?' + params.toString(), {
                headers: { 'X-Requested-With': 'XMLHttpRequest' },
                signal: currentAbortController.signal
            });
            if (!resp.ok) throw new Error('Server error');
            const html = await resp.text();
            document.getElementById('workerTableBody').innerHTML = html;
            const count = document.querySelectorAll('#workerTableBody .worker-row').length;
            document.getElementById('workerCount').textContent = count;

            // CMP-15: Date hint
            updateDateHint(count, s);
        } catch (e) {
            if (e.name === 'AbortError') return;  // CMP-12: ignore aborted
            document.getElementById('workerTableBody').innerHTML =
                '<tr><td colspan="8" class="text-center text-danger p-4">Gagal memuat data. Coba lagi.</td></tr>';
        } finally {
            document.getElementById('tableLoadingIndicator').style.display = 'none';
            // Note: updateExportLinks() sudah dipanggil di filterTeamTable, TIDAK perlu lagi di sini (CMP-13)
        }
    }

    // CMP-15 date hint
    function updateDateHint(currentCount, state) {
        var hintEl = document.getElementById('dateFilterHint');
        if (!hintEl) return;
        var hasDateFilter = state.dateFrom || state.dateTo;
        if (hasDateFilter && currentCount < initialWorkerCount) {
            hintEl.textContent = 'Beberapa worker tidak punya record di rentang tanggal ini — disembunyikan dari list.';
            hintEl.style.display = '';
        } else {
            hintEl.style.display = 'none';
        }
    }

    function updateExportLinks() {
        const s = getFilterState();
        const baseAssessment = '@Url.Action("ExportRecordsTeamAssessment", "CMP")';
        const baseTraining = '@Url.Action("ExportRecordsTeamTraining", "CMP")';
        const params = new URLSearchParams();
        if (s.section) params.set('section', s.section);
        if (s.unit) params.set('unit', s.unit);
        if (s.status && s.status !== 'ALL') params.set('statusFilter', s.status);
        if (s.category) params.set('category', s.category);
        if (s.subCategory) params.set('subCategory', s.subCategory);
        if (s.dateFrom) params.set('dateFrom', s.dateFrom);
        if (s.dateTo) params.set('dateTo', s.dateTo);
        const qs = params.toString();
        document.getElementById('btnExportAssessment').href = baseAssessment + (qs ? '?' + qs : '');
        document.getElementById('btnExportTraining').href = baseTraining + (qs ? '?' + qs : '');
    }

    function resetTeamFilters() {
        var sectionFilter = document.getElementById('sectionFilter');
        if (!sectionFilter.disabled) sectionFilter.value = '';
        document.getElementById('unitFilter').value = '';
        document.getElementById('categoryFilter').value = '';
        document.getElementById('subCategoryFilter').value = '';
        document.getElementById('subCategoryFilter').disabled = true;
        document.getElementById('statusFilter').value = 'ALL';
        document.getElementById('dateFrom').value = '';
        document.getElementById('dateTo').value = '';
        if (sectionFilter.disabled) updateUnitOptions();
        sessionStorage.removeItem('cmp-records-team-filter');
        filterTeamTable();
    }

    // sectionUnits + subCategoryMap + populate code preserve (existing L181-196)
    var sectionUnits = @Html.Raw(sectionUnitsJson);
    var subCategoryMap = @Html.Raw(ViewBag.SubCategoryMapJson ?? "{}");

    (function() {
        var masterCategories = @Html.Raw(ViewBag.MasterCategoriesJson ?? "[]");
        var sel = document.getElementById('categoryFilter');
        masterCategories.forEach(function(cat) {
            var opt = document.createElement('option');
            opt.value = cat;
            opt.textContent = cat;
            sel.appendChild(opt);
        });
    })();

    function updateUnitOptions() {
        var sectionVal = document.getElementById('sectionFilter').value;
        var unitSelect = document.getElementById('unitFilter');
        unitSelect.innerHTML = '<option value="">Semua Unit</option>';
        if (sectionVal && sectionUnits[sectionVal]) {
            sectionUnits[sectionVal].forEach(function(u) {
                var opt = document.createElement('option');
                opt.value = u;
                opt.textContent = u;
                unitSelect.appendChild(opt);
            });
        }
    }

    document.addEventListener('DOMContentLoaded', function() {
        // CMP-14: SubCategory disabled bila no children
        document.getElementById('categoryFilter').addEventListener('change', function() {
            var cat = this.value;
            var subSelect = document.getElementById('subCategoryFilter');
            subSelect.innerHTML = '<option value="">All Sub Categories</option>';
            var hasChildren = cat && subCategoryMap[cat] && subCategoryMap[cat].length > 0;
            subSelect.disabled = !hasChildren;
            if (hasChildren) {
                subCategoryMap[cat].forEach(function(sub) {
                    var opt = document.createElement('option');
                    opt.value = sub;
                    opt.textContent = sub;
                    subSelect.appendChild(opt);
                });
            }
            filterTeamTable();
        });

        // For L4 locked Bagian, auto-populate units
        var sectionFilter = document.getElementById('sectionFilter');
        if (sectionFilter.disabled && sectionFilter.value) updateUnitOptions();

        // CMP-17: restore filter state (jika ada)
        restoreFilterState();

        // Initial export links sync
        updateExportLinks();
    });
    ```

    2. **Tambah Date hint element + adjust workerCount label di template L115-121 RecordsTeam.cshtml:**
    ```cshtml
    <!-- Worker Counter + Date Hint -->
    <div class="mb-3">
        <small class="text-muted">
            Showing <span id="workerCount" class="fw-bold">@Model.Count</span> workers
        </small>
        <div id="dateFilterHint" class="alert alert-info py-2 mt-2 small" style="display:none" role="status" aria-live="polite">
            <!-- Diisi JS oleh updateDateHint -->
        </div>
    </div>
    ```

    3. **Build:**
    ```bash
    dotnet build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <done>
    - `currentAbortController` defined + abort() sebelum new fetch
    - AbortError gracefully ignored di catch block
    - `updateExportLinks()` dipanggil di filterTeamTable() head (sebelum debounce), DIHAPUS dari akhir doFetch
    - `subCategoryFilter.disabled = !hasChildren` logic
    - Date hint element + updateDateHint() function
    - sessionStorage 'cmp-records-team-filter' set/restore
    - `dotnet build` PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: _RecordsTeamBody.cshtml — purge dead data-* attributes + preparation code (CMP-18)</name>
  <files>Views/CMP/_RecordsTeamBody.cshtml</files>
  <behavior>
    Setelah edit:
    - Preparation code L16-45 (trainingCats2/assessmentCats2/allCats/completedTrainingCats/passedAssessmentCats/allSubCats/completedSubCats/hasTraining) DIHAPUS
    - `<tr class="worker-row">` hanya punya `data-section`, `data-unit`, `data-name`, `data-nip` (4 attr) — TIDAK ada lagi 5 dead attr
    - Payload per row berkurang ~50% (estimate <50 bytes per row vs ~200 bytes)
  </behavior>
  <action>
    1. **Edit `Views/CMP/_RecordsTeamBody.cshtml`** — replace seluruh file:
    ```cshtml
    @model List<HcPortal.Models.WorkerTrainingStatus>

    @if (Model == null || Model.Count == 0)
    {
        <tr>
            <td colspan="8" class="text-center p-5 text-muted">
                <i class="bi bi-inbox fs-1 d-block mb-2" aria-hidden="true"></i>
                Data belum ada
            </td>
        </tr>
    }
    else
    {
        @foreach (var worker in Model)
        {
            <tr class="worker-row"
                data-section="@worker.Section"
                data-unit="@worker.Unit"
                data-name="@worker.WorkerName.ToLower()"
                data-nip="@((worker.NIP ?? "").ToLower())">
                <td class="p-3">
                    <span class="fw-semibold">@worker.WorkerName</span>
                </td>
                <td class="p-3">@(worker.NIP ?? "---")</td>
                <td class="p-3">@worker.Position</td>
                <td class="p-3">@(worker.Section ?? "---")</td>
                <td class="p-3">@(worker.Unit ?? "---")</td>
                <td class="p-3 text-center">
                    <span class="badge bg-primary">@worker.CompletedAssessments</span>
                </td>
                <td class="p-3 text-center">
                    <span class="badge bg-success">@worker.CompletedTrainings</span>
                </td>
                <td class="p-3 text-center">
                    <a asp-action="RecordsWorkerDetail" asp-route-workerId="@worker.WorkerId"
                       class="btn btn-sm btn-outline-primary">
                        <i class="bi bi-eye me-1" aria-hidden="true"></i>Detail
                    </a>
                </td>
            </tr>
        }
    }
    ```

    2. **Verify no JS references dead attributes** — grep:
    ```bash
    grep -rn "data-categories\|data-subcategories\|data-completed-\|data-has-training" Views/ wwwroot/ 2>/dev/null | head -10
    ```
    Should return 0 matches (selain di file backup git history). Bila ada match di luar _RecordsTeamBody.cshtml → investigate sebelum delete.

    3. **Build:**
    ```bash
    dotnet build 2>&1 | tail -5
    ```
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5 && grep -c "data-categories\|data-subcategories\|data-completed-" Views/CMP/_RecordsTeamBody.cshtml</automated>
  </verify>
  <done>
    - `_RecordsTeamBody.cshtml` cuma punya 4 data-* attr (section, unit, name, nip)
    - Preparation code di top dihapus
    - Grep dead attr di _RecordsTeamBody.cshtml = 0
    - Grep dead attr di JS code (Views/CMP/*.cshtml, wwwroot/js/) = 0 reference yang masih hidup
    - `dotnet build` PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: CHECKPOINT — Browser smoke test Wave 2 (UX + Quality)</name>
  <files>N/A — checkpoint task, verification gating only (no file modification by Claude di task ini)</files>
  <action>Wait for user manual verification per how-to-verify steps. Tidak ada code change di task ini — semua code change selesai di task sebelumnya. Tugas Claude: PAUSE eksekusi sampai user respond resume-signal.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User reply 'approved' di resume-signal — verified semua acceptance criteria per how-to-verify section PASS browser-side.</done>
  <what-built>
    Wave 2 selesai code-side:
    - 12 REQ (CMP-12..23) implemented
    - 5 file modified/created: CMPRecordsViewModel.cs NEW, CMPController.cs, Records.cshtml, RecordsTeam.cshtml, _RecordsTeamBody.cshtml
    - `dotnet build` PASS (Task 1-4)

    Belum browser-verified bahwa race condition fixed + state persist + a11y kerja real-world.
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Backup DB lokal (per SEED_WORKFLOW)
    2. Restart `dotnet run` (View cached recompile)
    3. Login `admin@pertamina.com` / `123456`
    4. Buka DevTools Network + Console

    **A. AJAX race condition (CMP-12):**
    - Navigate `/CMP/Records` tab Team View
    - DevTools throttling = Slow 3G
    - Cepat ubah Category 3x dalam 1 detik (e.g., A → B → C → D)
    - Expected:
      - Console: lihat aborted requests (atau cek Network tab — earlier requests berstatus "(cancelled)")
      - Final table reflect filter terakhir (D), BUKAN response dari A/B/C yang datang telat
    - Pre-fix behavior: late response timpa fresh — table reflect filter early

    **B. Export URL sync (CMP-13):**
    - DevTools Network tab — throttle Slow 3G
    - Pilih Category = "X" → IMMEDIATELY hover (atau right-click → copy link) "Export Training" button
    - Expected: link href ALREADY contain `?category=X` walaupun fetch belum selesai
    - Pre-fix behavior: link href masih param lama

    **C. SubCategory disabled state (CMP-14):**
    - Pilih Category yang TIDAK punya children di seed (e.g., kalau OJT tidak ada subcategory di AssessmentCategories)
    - Expected: SubCategory dropdown DISABLED visual (greyed out)
    - Pilih Category yang punya children → SubCategory ENABLED + populated

    **D. Date hint (CMP-15):**
    - Set Date From = 1 Jan 2026, Date To = 31 Jan 2026 (range narrow)
    - Expected:
      - `#workerCount` turun
      - `#dateFilterHint` MUNCUL dengan pesan "Beberapa worker tidak punya record..."
    - Clear date range → hint hide

    **E. Year quick filter (CMP-16):**
    - Tab My Records → harus ada button group "2024 | 2025 | 2026 | Semua" (atau 3 tahun terakhir)
    - Klik "2025" → table filter ke year 2025, button "2025" active
    - Klik "Semua" → reset
    - Hidden select element existing tetap sync

    **F. Filter state persist (CMP-17):**
    - Tab Team View → set Category + Status + Date From
    - Switch ke tab My Records → back ke Team View
    - Expected: filter values KEMBALI ke yang di-set, table re-fetch dengan filter restored
    - Verify sessionStorage: DevTools → Application → Session Storage → `cmp-records-team-filter` ada JSON

    - Repeat untuk My Records: set search + year → switch tab → kembali → state restored

    **G. Dead data-* purge (CMP-18):**
    - Tab Team View → inspect 1 row → DevTools Elements
    - Expected: `<tr class="worker-row">` HANYA punya 4 data-* (section/unit/name/nip)
    - Bila masih ada `data-categories`, `data-completed-*`, `data-has-training` → FAIL (rebuild + cache clear)

    **H. Keyboard nav row (CMP-19):**
    - Tab My Records → ada row Assessment dengan link Results
    - Tab key sampai focus pindah ke row (focus ring visible)
    - Press Enter → navigate ke `/CMP/Results/{id}`
    - Press Space (di row lain) → navigate juga

    **I. ARIA tab roles (CMP-20):**
    - DevTools Elements → inspect `<ul id="recordsTabs">` → harus punya `role="tablist"`
    - `<a>` di dalamnya → `role="tab"` + `aria-controls="pane-..."` + `aria-selected="true|false"`
    - Klik tab Team View → aria-selected toggle ke true di Team, false di My Records
    - `<div class="tab-pane">` → `role="tabpanel"` + `aria-labelledby="tab-..."`
    - Bonus: pakai axe DevTools extension untuk a11y scan basic — should have no critical errors (warning OK karena BASIC only per D-03)

    **J. ViewModel refactor (CMP-21, CMP-22):**
    - View source page `/CMP/Records` → tidak ada error
    - Grep code: `Views/CMP/Records.cshtml` + `RecordsTeam.cshtml` TIDAK punya lagi `UserManager.GetUserAsync` atau `@inject UserManager`
    ```bash
    grep -n "UserManager.GetUserAsync\|@inject UserManager" Views/CMP/Records.cshtml Views/CMP/RecordsTeam.cshtml
    ```
    Expected: 0 match

    **K. Year memoize (CMP-23):**
    - Inspect Records.cshtml source render — year options populated dari `yearOptions` local var (passed dari controller `Model.YearOptions`), bukan inline `Model.Select(...).Distinct()`. Verify via grep:
    ```bash
    grep -n "Model.Select(r => r.Date.Year)" Views/CMP/Records.cshtml
    ```
    Expected: 0 match (memoized di controller)

    **L. Regression smoke Wave 1:**
    - 5 CRITICAL filter Wave 1 masih bekerja (Status without Category, SubCategory narrows, Category equality, etc)
    - 6 data integrity masih render correct (SertifikatUrl link, Permanent badge, counter update)

    **M. Cleanup:**
    - Clear sessionStorage manual via DevTools (atau Ctrl+Shift+Del)
    - Restore DB bila ada temp seed
  </how-to-verify>
  <resume-signal>
    Ketik "approved" bila ALL 12 REQ + regression Wave 1 PASS. Bila FAIL, sebutkan REQ-ID + detail.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser sessionStorage → JS read on load | Trusted local storage (per-tab), tetapi user dapat tamper via DevTools. Filter values applied SEBELUM server request, server tetap validate via existing controller scope check. |
| ViewModel User property → Razor view | Trusted internal, ApplicationUser di-pass dari authenticated session (GetCurrentUserRoleLevelAsync). |
| Tab nav role=tab → screen reader | A11y enhancement, no security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-337-02-01 | Tampering | sessionStorage filter tampering bypass scope | mitigate | Server-side scope enforcement L4 section lock di CMPController.RecordsTeamPartial L749 PRESERVE (Plan 01). Client-side restoration HANYA UI convenience, server tetap authoritative. |
| T-337-02-02 | Information Disclosure | CMPRecordsViewModel.User serialized ke view bocor full ApplicationUser ke client | mitigate | Razor view consume Model.User.Section saja (untuk LockedSection ViewData) — tidak pernah serialize ke client JSON. Pastikan View NIP/Email/PasswordHash TIDAK exposed (cek output HTML setelah refactor). |
| T-337-02-03 | Denial of Service | AbortController bug → memory leak fetch handles | accept | Modern browser garbage collect aborted fetch — low risk. Limit concurrent fetch ke 1 via single `currentAbortController` global. |
| T-337-02-04 | Repudiation | Filter state change tidak ter-audit | accept | Read-only filter — no audit required (consistent Plan 01 T-337-01-06). |
| T-337-02-05 | Elevation of Privilege | ViewModel refactor lose tab role check di RecordsTeam.cshtml | mitigate | ViewData["RoleLevel"] di-pass dari controller `Model.RoleLevel`, source single. Server-side `roleLevel >= 5 return Forbid()` di RecordsTeamPartial PRESERVE. View role check fallback `(int)(ViewData["RoleLevel"] ?? 5)` defaults ke 5 (safest = least privilege). |
</threat_model>

<verification>
- `dotnet build` PASS
- 12 REQ browser-verified (Playwright atau manual)
- Wave 1 regression PASS (5 CRITICAL + 6 data integrity tidak rusak)
- 0 grep match `UserManager.GetUserAsync` di Records.cshtml + RecordsTeam.cshtml
- 0 grep match dead data-* di _RecordsTeamBody.cshtml
- sessionStorage entries persist + restore correctly
- a11y basic: no critical axe DevTools errors di tab nav
</verification>

<success_criteria>
- [ ] 12 REQ (CMP-12..23) implemented + 5 file delivered
- [ ] CMPRecordsViewModel encapsulate single-source User/RoleLevel/YearOptions
- [ ] Race condition AbortController verified via DevTools throttle
- [ ] sessionStorage persist verified across tab switch
- [ ] ARIA tab roles + keyboard nav verified
- [ ] Threats T-337-02-01/02/05 mitigated, T-337-02-03/04 accepted
- [ ] User "approved" sebelum claim Wave 2 complete
</success_criteria>

<output>
Create `.planning/phases/337-cmp-records-full-overhaul-filter-data-arch-a11y/337-02-SUMMARY.md`. Catat:
- 12 REQ closed (CMP-12..23) + OQ-337-1 + OQ-337-4 resolved
- Files modified/created (5 file)
- Browser smoke result per REQ-ID
- Threats mitigated/accepted
- Next: Wave 3 Plan 03 (Arch SQL push-down + Pagination 3 REQ)
</output>
