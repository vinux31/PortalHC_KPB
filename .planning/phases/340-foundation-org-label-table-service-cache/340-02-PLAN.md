---
phase: 340-foundation-org-label-table-service-cache
plan: 02
type: execute
wave: 2
depends_on:
  - "340-01"
files_modified:
  - Services/IOrgLabelService.cs
  - Services/OrgLabelService.cs
  - Controllers/OrgLabelController.cs
  - Program.cs
autonomous: true
requirements:
  - ORG-LABEL-02
  - ORG-LABEL-03
  - ORG-LABEL-07
tags:
  - service
  - cache
  - controller
  - di
must_haves:
  truths:
    - "IOrgLabelService interface declared dengan 7 method (GetLabel, GetAll, UpdateAsync, AddAsync, DeleteAsync, GetMaxConfiguredLevel, GetMaxUsedLevelAsync)"
    - "OrgLabelService implementation use IMemoryCache key 'OrgLabels:All' no TTL"
    - "GetLabel(int) return fallback string '\"Level {N}\"' bila level tidak ada di cache"
    - "Mutation methods (UpdateAsync/AddAsync/DeleteAsync) call _cache.Remove + AuditLogService.LogAsync (D-04 field mapping)"
    - "OrgLabelService registered Scoped di Program.cs (D-06, NOT Singleton — captive dependency)"
    - "Endpoint GET /Admin/GetLevelLabels mengembalikan JSON dict authenticated user (no role filter D-03)"
  artifacts:
    - path: "Services/IOrgLabelService.cs"
      provides: "Interface 7 method per D-10"
      contains: "interface IOrgLabelService"
    - path: "Services/OrgLabelService.cs"
      provides: "Impl Scoped, inject IMemoryCache + ApplicationDbContext + AuditLogService"
      contains: "class OrgLabelService : IOrgLabelService"
    - path: "Controllers/OrgLabelController.cs"
      provides: "NEW controller [Authorize] [Route(Admin/[action])] GetLevelLabels endpoint"
      contains: "class OrgLabelController"
    - path: "Program.cs"
      provides: "AddScoped<IOrgLabelService, OrgLabelService>"
      contains: "AddScoped<HcPortal.Services.IOrgLabelService"
  key_links:
    - from: "OrgLabelController.GetLevelLabels"
      to: "IOrgLabelService.GetAll"
      via: "constructor DI"
      pattern: "_orgLabels\\.GetAll"
    - from: "OrgLabelService.GetAll"
      to: "IMemoryCache + ApplicationDbContext.OrganizationLevelLabels"
      via: "_cache.GetOrCreate + DbContext query"
      pattern: "GetOrCreate.*OrgLabels:All"
    - from: "OrgLabelService.UpdateAsync"
      to: "AuditLogService.LogAsync + _cache.Remove"
      via: "post-save audit + invalidate"
      pattern: "_auditLog\\.LogAsync"
---

<objective>
Bangun service layer + endpoint Phase 340: interface `IOrgLabelService` (7 method per D-10), implementation dengan in-memory cache no-TTL + fallback `"Level {N}"` + AuditLogService integration, DI registration Scoped di Program.cs, NEW controller `OrgLabelController` dengan endpoint `GET /Admin/GetLevelLabels`.

Purpose: Service layer dipakai Phase 341 (CRUD page), 342 (tree fixes), 343 (view integration). Phase 340 implement FULL interface (D-10) meskipun mutation method belum dipanggil — boundary phase clean.

Output: Endpoint `GET /Admin/GetLevelLabels` returnable JSON dict 3 entry dari cache, mutation method ready dipakai Phase 341.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v21.0-ROADMAP.md
@.planning/milestones/v21.0-REQUIREMENTS.md
@.planning/phases/340-foundation-org-label-table-service-cache/340-CONTEXT.md
@.planning/phases/340-foundation-org-label-table-service-cache/340-RESEARCH.md
@.planning/phases/340-foundation-org-label-table-service-cache/340-01-PLAN.md
@docs/superpowers/specs/2026-06-02-manageorganization-overhaul-design.md
@CLAUDE.md

# Patterns to replicate (read before implement)
@Services/INotificationService.cs
@Services/AuditLogService.cs
@Program.cs

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

From Services/AuditLogService.cs:21 (reuse, do NOT duplicate insert logic):
```csharp
public async Task LogAsync(
    string actorUserId,
    string actorName,
    string actionType,
    string description,
    int? targetId = null,
    string? targetType = null)
```

From Program.cs (existing registration pattern — replicate L61):
```csharp
builder.Services.AddMemoryCache();                                   // L17 — prerequisite
builder.Services.AddScoped<HcPortal.Services.AuditLogService>();     // L51
builder.Services.AddScoped<HcPortal.Services.INotificationService, HcPortal.Services.NotificationService>();   // L61
builder.Services.AddScoped<HcPortal.Services.IWorkerDataService, HcPortal.Services.WorkerDataService>();        // L62
```

From Plan 01 (Models/OrganizationLevelLabel.cs):
```csharp
public class OrganizationLevelLabel
{
    public int Level { get; set; }
    public string Label { get; set; } = "";
    public DateTime UpdatedAt { get; set; } = DateTime.UtcNow;
    public string UpdatedBy { get; set; } = "";
}
```

ApplicationDbContext has (from Plan 01):
```csharp
public DbSet<OrganizationLevelLabel> OrganizationLevelLabels { get; set; } = null!;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define IOrgLabelService interface (7 methods per D-10)</name>
  <files>Services/IOrgLabelService.cs</files>
  <read_first>
    - Services/INotificationService.cs (XML doc style + interface pattern)
    - .planning/phases/340-foundation-org-label-table-service-cache/340-CONTEXT.md (D-08 + D-10)
    - .planning/phases/340-foundation-org-label-table-service-cache/340-RESEARCH.md (Example 1 + Assumption A6 — sync vs async decision)
  </read_first>
  <action>
Create new file `Services/IOrgLabelService.cs` with EXACT content below. Resolves RESEARCH Open Question #1: `GetMaxUsedLevel` adopts async signature (`GetMaxUsedLevelAsync`) per RESEARCH A6 — idiomatic EF, no sync-over-async deadlock risk. Controller resolves `await` and passes plain `int` to view (Phase 341 consumer). `GetMaxConfiguredLevel` stays sync (data already cached).

```csharp
namespace HcPortal.Services
{
    /// <summary>
    /// Org label service — read + cache label tier organisasi (Bagian/Unit/Sub-unit/...).
    /// Phase 340 — milestone v21.0 ORG-LABEL-02.
    /// </summary>
    public interface IOrgLabelService
    {
        /// <summary>Resolve label untuk level. Fallback "Level {N}" bila tidak ada (D-07).</summary>
        /// <param name="level">Level int (0-indexed root).</param>
        /// <returns>Label string atau fallback "Level {level}".</returns>
        string GetLabel(int level);

        /// <summary>Get all configured labels sebagai immutable dict (cached, no-TTL).</summary>
        /// <returns>Read-only dict { level => label }, sorted by level asc.</returns>
        IReadOnlyDictionary<int, string> GetAll();

        /// <summary>Update label existing level. Invalidate cache + audit log. Throws bila level tidak ada.</summary>
        /// <param name="level">Level int existing.</param>
        /// <param name="label">Label baru.</param>
        /// <param name="userId">ActorUserId (Identity ID).</param>
        /// <param name="actorName">Display name (NIP + FullName), resolved oleh controller.</param>
        Task UpdateAsync(int level, string label, string userId, string actorName);

        /// <summary>Add new level + label. Invalidate cache + audit log. Throws bila level sudah ada.</summary>
        Task AddAsync(int level, string label, string userId, string actorName);

        /// <summary>Delete level (highest only, precondition: tidak dipakai unit). Invalidate cache + audit log.</summary>
        Task DeleteAsync(int level, string userId, string actorName);

        /// <summary>MAX(Level) dari OrganizationLevelLabels cached. Return 0 bila tabel kosong.</summary>
        int GetMaxConfiguredLevel();

        /// <summary>MAX(Level) dari OrganizationUnits live query (no cache, D-08). Return 0 bila tabel kosong.</summary>
        Task<int> GetMaxUsedLevelAsync();
    }
}
```
  </action>
  <verify>
    <automated>grep -c "interface IOrgLabelService" "Services/IOrgLabelService.cs"</automated>
  </verify>
  <acceptance_criteria>
    - File `Services/IOrgLabelService.cs` exists
    - Contains string `namespace HcPortal.Services`
    - Contains string `public interface IOrgLabelService`
    - Contains 7 method signatures (grep each: `string GetLabel`, `IReadOnlyDictionary<int, string> GetAll`, `Task UpdateAsync`, `Task AddAsync`, `Task DeleteAsync`, `int GetMaxConfiguredLevel`, `Task<int> GetMaxUsedLevelAsync`)
    - XML doc `<summary>` on each method
    - `dotnet build` PASS
  </acceptance_criteria>
  <done>
    Interface file committed dengan 7 method per D-10, async/sync decision per RESEARCH A6.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement OrgLabelService + register Scoped + create controller endpoint</name>
  <files>Services/OrgLabelService.cs, Program.cs, Controllers/OrgLabelController.cs</files>
  <read_first>
    - Services/IOrgLabelService.cs (interface from Task 1)
    - Services/AuditLogService.cs (LogAsync signature — D-04 field mapping target)
    - Services/NotificationService.cs (impl style + constructor injection pattern)
    - Program.cs (existing DI registration block L17 + L51 + L61-62 — find exact insertion point)
    - Controllers/OrganizationController.cs (read first ~30 lines — route attribute + Json return pattern)
    - .planning/phases/340-foundation-org-label-table-service-cache/340-RESEARCH.md (Pattern 3 cache, Pattern 4 controller, Pattern 5 DI, Example 2 GetMaxUsedLevel, Pitfall 2 captive dep, Pitfall 7 JSON serialization)
  </read_first>
  <action>
**Step 1 — Create `Services/OrgLabelService.cs`:**

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Caching.Memory;
using HcPortal.Data;
using HcPortal.Models;

namespace HcPortal.Services
{
    /// <summary>
    /// In-memory cached org label service. Phase 340 milestone v21.0.
    /// Cache: no-TTL, manual invalidate on every mutation (D-02).
    /// Audit log: reuse AuditLogService (D-04).
    /// </summary>
    public class OrgLabelService : IOrgLabelService
    {
        private const string LabelsCacheKey = "OrgLabels:All";

        private readonly ApplicationDbContext _context;
        private readonly IMemoryCache _cache;
        private readonly AuditLogService _auditLog;

        public OrgLabelService(
            ApplicationDbContext context,
            IMemoryCache cache,
            AuditLogService auditLog)
        {
            _context = context;
            _cache = cache;
            _auditLog = auditLog;
        }

        public string GetLabel(int level)
        {
            var dict = GetAll();
            return dict.TryGetValue(level, out var label) ? label : $"Level {level}";  // D-07 fallback
        }

        public IReadOnlyDictionary<int, string> GetAll()
        {
            return _cache.GetOrCreate(LabelsCacheKey, entry =>
            {
                // D-02: NO entry.AbsoluteExpirationRelativeToNow — manual invalidate only.
                return (IReadOnlyDictionary<int, string>)_context.OrganizationLevelLabels
                    .AsNoTracking()
                    .OrderBy(l => l.Level)
                    .ToDictionary(l => l.Level, l => l.Label);
            })!;
        }

        public async Task UpdateAsync(int level, string label, string userId, string actorName)
        {
            var row = await _context.OrganizationLevelLabels.FindAsync(level);
            if (row == null)
                throw new InvalidOperationException($"Level {level} not configured");

            var oldLabel = row.Label;
            row.Label = label;
            row.UpdatedAt = DateTime.UtcNow;
            row.UpdatedBy = userId;
            await _context.SaveChangesAsync();

            _cache.Remove(LabelsCacheKey);  // D-02

            await _auditLog.LogAsync(
                actorUserId: userId,
                actorName: actorName,
                actionType: "OrgLabel-Update",                                   // D-04 (15 char, MaxLength 50 safe)
                description: $"Level {level}: '{oldLabel}' → '{label}'",
                targetId: level,
                targetType: "OrganizationLevelLabel"                             // D-04 (24 char, MaxLength 100 safe)
            );
        }

        public async Task AddAsync(int level, string label, string userId, string actorName)
        {
            if (await _context.OrganizationLevelLabels.AnyAsync(l => l.Level == level))
                throw new InvalidOperationException($"Level {level} already exists");

            var row = new OrganizationLevelLabel
            {
                Level = level,
                Label = label,
                UpdatedAt = DateTime.UtcNow,
                UpdatedBy = userId
            };
            _context.OrganizationLevelLabels.Add(row);
            await _context.SaveChangesAsync();

            _cache.Remove(LabelsCacheKey);

            await _auditLog.LogAsync(
                actorUserId: userId,
                actorName: actorName,
                actionType: "OrgLabel-Add",
                description: $"Level {level}: '{label}' created",
                targetId: level,
                targetType: "OrganizationLevelLabel"
            );
        }

        public async Task DeleteAsync(int level, string userId, string actorName)
        {
            var row = await _context.OrganizationLevelLabels.FindAsync(level);
            if (row == null)
                throw new InvalidOperationException($"Level {level} not configured");

            var oldLabel = row.Label;
            _context.OrganizationLevelLabels.Remove(row);
            await _context.SaveChangesAsync();

            _cache.Remove(LabelsCacheKey);

            await _auditLog.LogAsync(
                actorUserId: userId,
                actorName: actorName,
                actionType: "OrgLabel-Delete",
                description: $"Level {level}: '{oldLabel}' deleted",
                targetId: level,
                targetType: "OrganizationLevelLabel"
            );
        }

        public int GetMaxConfiguredLevel()
        {
            var dict = GetAll();
            return dict.Count == 0 ? 0 : dict.Keys.Max();
        }

        public async Task<int> GetMaxUsedLevelAsync()
        {
            // D-08: live query, no cache. Dipanggil jarang dari Phase 341 page render.
            if (!await _context.OrganizationUnits.AnyAsync())
                return 0;
            return await _context.OrganizationUnits.MaxAsync(u => u.Level);
        }
    }
}
```

**Step 2 — Register DI di `Program.cs`:**

Locate line `builder.Services.AddScoped<HcPortal.Services.IWorkerDataService, HcPortal.Services.WorkerDataService>();` (~L62). INSERT setelah baris itu:
```csharp

// Org Label Service — Phase 340 milestone v21.0 (D-06 Scoped, ApplicationDbContext captive prevention)
builder.Services.AddScoped<HcPortal.Services.IOrgLabelService, HcPortal.Services.OrgLabelService>();
```

**CRITICAL:** MUST `AddScoped` (Pitfall 2). `AddSingleton` will fail ASP.NET Core 8 startup with `Cannot consume scoped service 'ApplicationDbContext'`.

**Step 3 — Create `Controllers/OrgLabelController.cs`:**

```csharp
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using HcPortal.Services;

namespace HcPortal.Controllers
{
    /// <summary>
    /// Phase 340 endpoint untuk JS consumer (Phase 342+343 view integration).
    /// Phase 341 nanti tambah page CRUD actions (Index/Add/Update/Delete).
    /// </summary>
    [Authorize]                        // D-03: authenticated user, no role filter — label = public display info
    [Route("Admin/[action]")]
    public class OrgLabelController : Controller
    {
        private readonly IOrgLabelService _orgLabels;

        public OrgLabelController(IOrgLabelService orgLabels)
        {
            _orgLabels = orgLabels;
        }

        // GET /Admin/GetLevelLabels
        // Response example: { "0": "Bagian", "1": "Unit", "2": "Sub-unit" }
        [HttpGet]
        public IActionResult GetLevelLabels()
        {
            var dict = _orgLabels.GetAll();
            // Defensive explicit string-key dict — ASP.NET Core 8 supports int-key Dictionary
            // serialization natively, but explicit conversion deterministic against any global
            // JSON config override (Pitfall 7).
            var jsonDict = dict.ToDictionary(kv => kv.Key.ToString(), kv => kv.Value);
            return Json(jsonDict);
        }
    }
}
```

**Step 4 — Local verify:**
```bash
dotnet build
dotnet run --no-build
# Wait for "Now listening on http://localhost:5277"
# In new terminal:
# Login first (Identity cookie), e.g.:
curl -c cookies.txt -X POST http://localhost:5277/Account/Login -d "Email=admin@pertamina.com&Password=123456"
curl -b cookies.txt http://localhost:5277/Admin/GetLevelLabels
# Expected response body: {"0":"Bagian","1":"Unit","2":"Sub-unit"}
# Ctrl+C dotnet run after verify
```

Alternative manual verify: Login via browser di `http://localhost:5277/Account/Login` (admin@pertamina.com / 123456 per MEMORY reference_dev_credentials), navigate `http://localhost:5277/Admin/GetLevelLabels` — expect JSON dict body.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -c "Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - File `Services/OrgLabelService.cs` exists, contains `class OrgLabelService : IOrgLabelService`
    - Contains string `private const string LabelsCacheKey = "OrgLabels:All"` (Pitfall 3 constant)
    - Contains string `_cache.GetOrCreate(LabelsCacheKey` (cache read)
    - Contains string `_cache.Remove(LabelsCacheKey)` at least 3 times (Update + Add + Delete invalidate)
    - Contains string `_auditLog.LogAsync` at least 3 times (Update + Add + Delete)
    - Contains string `actionType: "OrgLabel-Update"` AND `actionType: "OrgLabel-Add"` AND `actionType: "OrgLabel-Delete"` (D-04 mapping)
    - Contains string `targetType: "OrganizationLevelLabel"` at least 3 times (D-04)
    - Contains string `$"Level {level}"` (D-07 fallback) in GetLabel method
    - Contains string `DateTime.UtcNow` (no `DateTime.Now` — Anti-pattern)
    - Contains string `AsNoTracking()` in GetAll query
    - `Program.cs` contains EXACT string `AddScoped<HcPortal.Services.IOrgLabelService, HcPortal.Services.OrgLabelService>` (NOT AddSingleton — Pitfall 2)
    - `Program.cs` does NOT contain `AddSingleton<HcPortal.Services.IOrgLabelService` (grep zero matches)
    - File `Controllers/OrgLabelController.cs` exists, contains `class OrgLabelController : Controller`
    - Contains EXACT string `[Authorize]` (class-level)
    - Contains EXACT string `[Route("Admin/[action]")]`
    - Contains EXACT string `[HttpGet]` above GetLevelLabels method
    - Contains string `return Json(` (not manual serializer)
    - `dotnet build` PASS zero NEW warnings
    - `dotnet run` startup succeeds without DI scope validation error (Pitfall 2 verify)
    - Manual: `curl -b cookies.txt http://localhost:5277/Admin/GetLevelLabels` returns JSON dict containing `"0":"Bagian"` (or equivalent browser verify)
  </acceptance_criteria>
  <done>
    Service impl + DI registration Scoped + Controller GET endpoint all wired. Endpoint returns JSON dict 3 entry from cache. All audit log mutation paths correctly call `AuditLogService.LogAsync` with D-04 field mapping.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → `/Admin/GetLevelLabels` | Authenticated user (cookie session). No role filter (D-03). |
| Controller → Service | In-process DI, Scoped service. |
| Service → ApplicationDbContext + IMemoryCache + AuditLogService | In-process DI, Scoped service consumes Scoped DbContext (D-06 captive dep prevention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-05 | Spoofing | Anonymous user access `/Admin/GetLevelLabels` | mitigate | Class-level `[Authorize]` attribute (D-03). Pitfall RESEARCH: tanpa attribute, ASP.NET Core 8 default anonymous accessible. |
| T-340-06 | Tampering | Singleton DI registration captures ApplicationDbContext → cross-request data leak / thread bug | mitigate | Task 2 Step 2 mandatory grep `AddSingleton<HcPortal.Services.IOrgLabelService` zero matches. ASP.NET Core 8 EnableValidateOnBuild surfaces this at startup (Pitfall 2). |
| T-340-07 | Repudiation | Mutation tanpa audit trail | mitigate | UpdateAsync/AddAsync/DeleteAsync wajib panggil `_auditLog.LogAsync` setelah SaveChangesAsync (D-04). Acceptance criteria grep `_auditLog.LogAsync` ≥3 matches. |
| T-340-08 | Information Disclosure | Endpoint exposes label dict ke semua authenticated user | accept | D-03 lock decision — label = public display info, semua user perlu untuk render dynamic UI. No PII, no sensitive data. |
| T-340-09 | Denial of Service | Cache key collision dengan key lain di repo causes stale read | mitigate | Constant `LabelsCacheKey = "OrgLabels:All"` referenced via field, bukan literal (Pitfall 3). Existing cache keys di repo: `assessment_categories_distinct` (no collision). |
</threat_model>

<verification>
**Build + DI validation:**
```bash
dotnet build 2>&1 | tail -10
# Expected: "Build succeeded" zero warnings baru
```

**Endpoint live test (after `dotnet run`):**
```bash
# Login + cookie
curl -c cookies.txt -X POST http://localhost:5277/Account/Login \
  -d "Email=admin@pertamina.com&Password=123456&__RequestVerificationToken=..."
# (alternative: browser login at http://localhost:5277/Account/Login then visit endpoint)

curl -b cookies.txt -i http://localhost:5277/Admin/GetLevelLabels
# Expected:
# HTTP/1.1 200 OK
# Content-Type: application/json
# Body: {"0":"Bagian","1":"Unit","2":"Sub-unit"}
```

**DI registration check:**
```bash
grep -c "AddScoped<HcPortal.Services.IOrgLabelService" Program.cs   # expected: 1
grep -c "AddSingleton<HcPortal.Services.IOrgLabelService" Program.cs # expected: 0
```

**Anti-pattern guard (Pitfall 2 + Pitfall 3 + audit fields):**
```bash
grep -c "LabelsCacheKey" Services/OrgLabelService.cs       # >= 4 (1 const + 1 read + 3 invalidate)
grep -c "_cache.Remove(LabelsCacheKey)" Services/OrgLabelService.cs # = 3
grep -c "_auditLog.LogAsync" Services/OrgLabelService.cs   # = 3
grep -c 'targetType: "OrganizationLevelLabel"' Services/OrgLabelService.cs # = 3
```
</verification>

<success_criteria>
- `IOrgLabelService` interface declared dengan 7 method (4 sync + 3 async)
- `OrgLabelService` impl ready dengan cache + fallback `"Level {N}"` + audit hook (D-02/D-04/D-07)
- Mutation methods (Update/Add/Delete) invalidate cache + call `AuditLogService.LogAsync` dengan field mapping D-04
- `Program.cs` register Scoped (Pitfall 2 captive dep avoid)
- `OrgLabelController` class created dengan `[Authorize]` + `[Route("Admin/[action]")]`
- Endpoint `GET /Admin/GetLevelLabels` returns JSON dict 3 entry untuk authenticated user
- `dotnet build` + `dotnet run` PASS, no DI scope validation error
- Manual curl/browser test confirms 200 + JSON dict body
</success_criteria>

<output>
After completion, create `.planning/phases/340-foundation-org-label-table-service-cache/340-02-SUMMARY.md` capturing:
- 4 file paths committed
- Endpoint live test output (curl response body)
- DI registration grep results (Pitfall 2 verification)
- Audit log field mapping verification (3x LogAsync calls grep)
- Commit hashes per task
</output>
