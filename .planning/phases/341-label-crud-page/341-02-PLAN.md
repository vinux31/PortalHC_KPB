---
phase: 341
plan: 02
type: execute
wave: 2
depends_on:
  - 341-01
files_modified:
  - Views/Admin/ManageOrgLevelLabels.cshtml
  - Views/Admin/Index.cshtml
autonomous: false
requirements:
  - ORG-LABEL-04
tags:
  - razor-view
  - bootstrap-modal
  - frontend
  - antiforgery
user_setup: []

must_haves:
  truths:
    - "User Admin/HC dapat akses URL `/Admin/ManageOrgLevelLabels` dan melihat tabel berisi semua level (0..maxConfigured) + 1 buffer row \"(belum diset)\""
    - "User Admin/HC dapat klik Edit button → modal terbuka dengan Level field disabled + Label input pre-filled current value"
    - "User Admin/HC dapat submit Edit modal → fetch POST → toast success/danger render + page reload"
    - "User Admin/HC dapat klik Tambah button di buffer row → modal Tambah terbuka dengan Level disabled = NextAddLevel + Label input kosong"
    - "User Admin/HC dapat klik Delete pada level tertinggi yang tidak dipakai → native confirm() dialog muncul → OK → AJAX → toast + reload"
    - "Delete button hanya muncul untuk row dimana CanDelete=true (highest + !used); muncul disabled dengan tooltip 'Tidak bisa dihapus, masih dipakai unit' kalau highest + used; tidak muncul untuk row bukan highest"
    - "Card baru 'Label Tier Organisasi' tampil di `/Admin/Index` SETELAH card 'Organization Structure' untuk user Admin/HC (role-gated)"
    - "Anti-forgery token render via `@Html.AntiForgeryToken()` di page scope; JS `getAntiForgeryToken()` extract value dari hidden input dan append ke URLSearchParams body"
    - "Toast feedback via `wwwroot/js/shared-toast.js` `showToast(msg, 'success'|'danger')`"
  artifacts:
    - path: "Views/Admin/ManageOrgLevelLabels.cshtml"
      provides: "Server-rendered Razor view + 2 Bootstrap modals + inline JS (~210 LoC)"
      contains: "@model HcPortal.Models.ViewModels.ManageOrgLevelLabelsViewModel"
      min_lines: 180
    - path: "Views/Admin/Index.cshtml"
      provides: "Admin card grid dengan 1 entry baru 'Label Tier Organisasi'"
      contains: "ManageOrgLevelLabels"
  key_links:
    - from: "Views/Admin/Index.cshtml"
      to: "Controllers/OrgLabelController.ManageOrgLevelLabels"
      via: "@Url.Action(\"ManageOrgLevelLabels\", \"OrgLabel\")"
      pattern: "ManageOrgLevelLabels.*OrgLabel"
    - from: "Views/Admin/ManageOrgLevelLabels.cshtml"
      to: "Controllers/OrgLabelController POST actions"
      via: "fetch POST URLSearchParams body + antiforgery token"
      pattern: "fetch\\(.*'/Admin/(Update|Add|Delete)LevelLabel'"
    - from: "Views/Admin/ManageOrgLevelLabels.cshtml"
      to: "wwwroot/js/shared-toast.js"
      via: "@section Scripts script src"
      pattern: "shared-toast.js"
    - from: "Views/Admin/ManageOrgLevelLabels.cshtml"
      to: "Models/ViewModels/ManageOrgLevelLabelsViewModel.cs (Plan 01)"
      via: "@model directive"
      pattern: "@model HcPortal.Models.ViewModels"
---

<objective>
Build Razor view `Views/Admin/ManageOrgLevelLabels.cshtml` (D-10 server-render + 2 modals + inline JS) DAN insert 1 admin card baru di `Views/Admin/Index.cshtml` (D-04 navigation entry). Konsumsi controller actions Plan 01: GET render (model-bound) + 3 POST mutation (fetch + antiforgery). Manual browser smoke verify lokal `http://localhost:5277/Admin/ManageOrgLevelLabels`.

Purpose: Frontend tier — surface CRUD page yang HC/Admin akses via browser. Setelah Plan 02 selesai, full Edit/Add/Delete flow functional di lokal (Plan 03 tinggal write xUnit test + manual UAT Coach 403).

Output: View `Views/Admin/ManageOrgLevelLabels.cshtml` (~200 LoC dengan inline JS) + `Views/Admin/Index.cshtml` 1 card block insert (~14 LoC). Build pass + browser smoke verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/341-label-crud-page/341-CONTEXT.md
@.planning/phases/341-label-crud-page/341-RESEARCH.md
@.planning/phases/341-label-crud-page/341-PATTERNS.md
@.planning/phases/341-label-crud-page/341-VALIDATION.md
@.planning/phases/341-label-crud-page/341-01-SUMMARY.md
@docs/superpowers/specs/2026-06-02-manageorganization-overhaul-design.md
@CLAUDE.md

# Plan 01 artifacts (consume)
@Controllers/OrgLabelController.cs
@Models/ViewModels/ManageOrgLevelLabelsViewModel.cs

# Analog files (read-only — copy patterns)
@Views/Admin/ManageOrganization.cshtml
@Views/Admin/Index.cshtml
@Views/Admin/AssessmentMonitoringDetail.cshtml
@wwwroot/js/shared-toast.js
@wwwroot/js/orgTree.js

<interfaces>
<!-- ViewModel shape dari Plan 01 yang harus di-bind oleh view. -->
<!-- Executor: jangan re-eksplorasi — gunakan langsung struktur ini. -->

@model HcPortal.Models.ViewModels.ManageOrgLevelLabelsViewModel
Properties:
- Model.Rows : List<LabelRowVM>
  - Level (int)
  - Label (string?) — null = "(belum diset)" buffer row
  - IsHighest (bool)
  - IsUsed (bool)
  - CanDelete (bool) — true if Label != null && IsHighest && !IsUsed
- Model.MaxConfigured (int)
- Model.MaxUsed (int)
- Model.NextAddLevel (int) — buffer row level number

POST endpoints to call from fetch:
- POST /Admin/UpdateLevelLabel  body=level&label&__RequestVerificationToken
- POST /Admin/AddLevelLabel     body=level&label&__RequestVerificationToken
- POST /Admin/DeleteLevelLabel  body=level&__RequestVerificationToken

Response shape (all 3): { success: bool, message: string }

Existing shared toast API (wwwroot/js/shared-toast.js):
- showToast(message: string, type: 'success' | 'danger') : void
  (type maps to Bootstrap alert variant — OQ#4 resolved → use 'danger' not 'error')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Views/Admin/ManageOrgLevelLabels.cshtml (server-render table + 2 modals + inline JS)</name>
  <files>Views/Admin/ManageOrgLevelLabels.cshtml</files>
  <read_first>
    - 341-PATTERNS.md §3 "Views/Admin/ManageOrgLevelLabels.cshtml" (full section — modal markup + scripts + row render loop)
    - 341-RESEARCH.md §Code Examples Example 6 (view skeleton starting L648)
    - 341-RESEARCH.md §Common Pitfalls 2 (antiforgery), 5 (distinct modal ids), 6 (reload tradeoff)
    - 341-CONTEXT.md §Implementation Decisions D-01, D-03, D-06, D-07, D-10
    - 341-CONTEXT.md §Specifics (delete button visibility rule + modal exact UX text)
    - Views/Admin/ManageOrganization.cshtml L133-196 (modal markup + scripts section pattern)
    - wwwroot/js/orgTree.js L8-26 (getAntiForgeryToken + ajaxPost inline copy source)
    - wwwroot/js/shared-toast.js full (showToast API)
  </read_first>
  <action>
    Create new file `Views/Admin/ManageOrgLevelLabels.cshtml`. Struktur 4 bagian: (A) Top directive + breadcrumb + heading + info banner + antiforgery token, (B) Tabel server-render via @foreach Model.Rows, (C) 2 Bootstrap modals (Edit + Add), (D) `@section Scripts` dengan inline JS.

    **Bagian A — Top (RESEARCH Example 6 verbatim L651-675):**
    ```cshtml
    @model HcPortal.Models.ViewModels.ManageOrgLevelLabelsViewModel
    @{
        ViewData["Title"] = "Kelola Label Tier Organisasi";
    }

    <div class="container-fluid px-4 py-4">
        <nav aria-label="breadcrumb" class="mb-3">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="@Url.Action("Index", "Admin")">Kelola Data</a></li>
                <li class="breadcrumb-item active" aria-current="page">Label Tier Organisasi</li>
            </ol>
        </nav>

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2 class="fw-bold mb-1"><i class="bi bi-tags text-primary me-2"></i>Kelola Label Tier Organisasi</h2>
                <p class="text-muted mb-0">Ubah nama tampilan tier (Bagian/Unit/Sub-unit/...)</p>
            </div>
        </div>

        <div class="alert alert-info">
            <i class="bi bi-info-circle me-2"></i>
            Label ini hanya nama tampilan tier. Mengubah label TIDAK mengubah struktur data atau tier.
            Cascade backend tetap berlaku berdasarkan level numerik.
        </div>

        @Html.AntiForgeryToken()
    ```

    **Bagian B — Tabel** dengan @foreach Model.Rows. Pakai Bootstrap `.table .table-hover` style. Header 3 kolom: Level (width 120px) / Label saat ini / Aksi (width 200px). Body:
    ```cshtml
    <div class="card border-0 shadow-sm">
        <div class="card-body">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th style="width:120px;">Level</th>
                        <th>Label saat ini</th>
                        <th style="width:200px;">Aksi</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var row in Model.Rows)
                {
                    <tr>
                        <td>@row.Level</td>
                        @if (row.Label == null)
                        {
                            <td><em class="text-muted">(belum diset)</em></td>
                            <td>
                                @if (row.Level == Model.NextAddLevel)
                                {
                                    <button type="button" class="btn btn-sm btn-outline-primary"
                                            onclick="openAddModal(@row.Level)">
                                        <i class="bi bi-plus-circle me-1"></i>Tambah
                                    </button>
                                }
                            </td>
                        }
                        else
                        {
                            <td>@row.Label</td>
                            <td class="d-flex gap-1">
                                <button type="button" class="btn btn-sm btn-outline-secondary"
                                        onclick='openEditModal(@row.Level, @Html.Raw(Json.Serialize(row.Label)))'>
                                    <i class="bi bi-pencil-square me-1"></i>Edit
                                </button>
                                @if (row.CanDelete)
                                {
                                    <button type="button" class="btn btn-sm btn-outline-danger"
                                            onclick='confirmDelete(@row.Level, @Html.Raw(Json.Serialize(row.Label)))'>
                                        <i class="bi bi-trash me-1"></i>Hapus
                                    </button>
                                }
                                else if (row.IsHighest && row.IsUsed)
                                {
                                    <button type="button" class="btn btn-sm btn-outline-danger" disabled
                                            data-bs-toggle="tooltip" title="Tidak bisa dihapus, masih dipakai unit">
                                        <i class="bi bi-trash me-1"></i>Hapus
                                    </button>
                                }
                            </td>
                        }
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>

    </div> <!-- end container-fluid -->
    ```

    **Bagian C — 2 Modals** (Pitfall 5: distinct ids). Replicate `Views/Admin/ManageOrganization.cshtml:134-164` markup, adapt per Patterns §3 C-01:

    Modal 1 `labelEditModal`:
    - Header bg-primary text-white, title id=`labelEditModalLabel` → "Edit Label Tier Level X" (set via JS)
    - Body: hidden input id=`labelEditLevel`; disabled text input id=`labelEditLevelDisplay`; editable text input id=`labelEditValue` (maxlength=50, required, with `<div class="invalid-feedback">Label tidak boleh kosong.</div>`)
    - Footer: Batal button (data-bs-dismiss) + Simpan button (onclick=`submitEdit()`)

    Modal 2 `labelAddModal`:
    - Header bg-primary text-white, title id=`labelAddModalLabel` → "Tambah Level Tier X"
    - Body: hidden input id=`labelAddLevel`; disabled text input id=`labelAddLevelDisplay`; editable text input id=`labelAddValue` (maxlength=50, required)
    - Footer: Batal + Simpan (onclick=`submitAdd()`)

    Tulis kedua modal markup verbatim dari pattern; copy struktur Bootstrap 5 `.modal .modal-dialog .modal-content .modal-header/body/footer` dengan `aria-labelledby` matching `<h5 id>`, `aria-hidden="true"`, `tabindex="-1"`.

    **Bagian D — `@section Scripts`** inline JS (OQ#3 inline, NOT separate file):
    ```cshtml
    @section Scripts {
        <script src="~/js/shared-toast.js"></script>
        <script>
            function getAntiForgeryToken() {
                const input = document.querySelector('input[name="__RequestVerificationToken"]');
                return input ? input.value : '';
            }

            async function ajaxPost(url, data) {
                const params = new URLSearchParams(data);
                params.append('__RequestVerificationToken', getAntiForgeryToken());
                const res = await fetch(url, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/x-www-form-urlencoded',
                        'X-Requested-With': 'XMLHttpRequest'
                    },
                    body: params.toString()
                });
                return res.json();
            }

            function openEditModal(level, currentLabel) {
                document.getElementById('labelEditModalLabel').textContent = `Edit Label Tier Level ${level}`;
                document.getElementById('labelEditLevel').value = level;
                document.getElementById('labelEditLevelDisplay').value = level;
                document.getElementById('labelEditValue').value = currentLabel;
                new bootstrap.Modal(document.getElementById('labelEditModal')).show();
            }

            function openAddModal(level) {
                document.getElementById('labelAddModalLabel').textContent = `Tambah Level Tier ${level}`;
                document.getElementById('labelAddLevel').value = level;
                document.getElementById('labelAddLevelDisplay').value = level;
                document.getElementById('labelAddValue').value = '';
                new bootstrap.Modal(document.getElementById('labelAddModal')).show();
            }

            async function submitEdit() {
                const level = document.getElementById('labelEditLevel').value;
                const label = document.getElementById('labelEditValue').value.trim();
                if (!label) { showToast('Label tidak boleh kosong.', 'danger'); return; }
                if (label.length > 50) { showToast('Label maksimal 50 karakter.', 'danger'); return; }
                const result = await ajaxPost('/Admin/UpdateLevelLabel', { level, label });
                if (result.success) {
                    showToast(result.message, 'success');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    showToast(result.message, 'danger');
                }
            }

            async function submitAdd() {
                const level = document.getElementById('labelAddLevel').value;
                const label = document.getElementById('labelAddValue').value.trim();
                if (!label) { showToast('Label tidak boleh kosong.', 'danger'); return; }
                if (label.length > 50) { showToast('Label maksimal 50 karakter.', 'danger'); return; }
                const result = await ajaxPost('/Admin/AddLevelLabel', { level, label });
                if (result.success) {
                    showToast(result.message, 'success');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    showToast(result.message, 'danger');
                }
            }

            async function confirmDelete(level, label) {
                if (!confirm(`Hapus label Level ${level} "${label}"? Tidak bisa diundo.`)) return;
                const result = await ajaxPost('/Admin/DeleteLevelLabel', { level });
                if (result.success) {
                    showToast(result.message, 'success');
                    setTimeout(() => window.location.reload(), 600);
                } else {
                    showToast(result.message, 'danger');
                }
            }

            // Init Bootstrap tooltips for disabled delete buttons
            document.addEventListener('DOMContentLoaded', function () {
                const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
                tooltipTriggerList.forEach(el => new bootstrap.Tooltip(el));
            });
        </script>
    }
    ```

    **C-03 client-side preview validation** (Claude's Discretion): Submit functions `submitEdit` + `submitAdd` already check empty + length > 50 dengan toast feedback sebelum AJAX — keep as defensive layer (server tetap authoritative).

    **CRITICAL Bahasa Indonesia (CLAUDE.md):** ALL UI copy Indonesian — modal title, button label, breadcrumb, info banner, tooltip, native confirm text, invalid-feedback. Sudah dispesifikasi verbatim di action block atas.

    Pitfall 5 verify: 2 modal id distinct (`labelEditModal` ≠ `labelAddModal`), submit button onclick distinct (`submitEdit()` ≠ `submitAdd()`), hidden input id distinct (`labelEditLevel` ≠ `labelAddLevel`).

    NO HTMX. NO SortableJS. NO orgTree.js include. NO SweetAlert.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj --nologo --verbosity minimal 2&gt;&amp;1 | grep -E "(error|Build succeeded)"</automated>
  </verify>
  <acceptance_criteria>
    - File `Views/Admin/ManageOrgLevelLabels.cshtml` ada
    - `dotnet build` PASS
    - `grep -c "@model HcPortal.Models.ViewModels.ManageOrgLevelLabelsViewModel" Views/Admin/ManageOrgLevelLabels.cshtml` = 1
    - `grep -c "@Html.AntiForgeryToken" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 1
    - `grep -c "labelEditModal" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 3 (id + aria-labelledby + show())
    - `grep -c "labelAddModal" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 3
    - `grep -c "shared-toast.js" Views/Admin/ManageOrgLevelLabels.cshtml` = 1 (D-07)
    - `grep -c "confirm(\`Hapus label Level" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 1 (D-03 native confirm verbatim text)
    - `grep -c "/Admin/UpdateLevelLabel" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 1
    - `grep -c "/Admin/AddLevelLabel" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 1
    - `grep -c "/Admin/DeleteLevelLabel" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 1
    - `grep -c "showToast" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 6 (3 success path + 3 error path minimal)
    - `grep -c "Tidak bisa dihapus, masih dipakai unit" Views/Admin/ManageOrgLevelLabels.cshtml` ≥ 1 (tooltip text)
    - `grep -c "hx-" Views/Admin/ManageOrgLevelLabels.cshtml` = 0 (NO HTMX per D-01)
    - File line count ≥ 180 (`wc -l Views/Admin/ManageOrgLevelLabels.cshtml`)
  </acceptance_criteria>
  <done>View compile-clean (Razor parse pass via dotnet build); siap di-akses browser setelah dotnet run + Task 2 admin card insert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert admin card di Views/Admin/Index.cshtml</name>
  <files>Views/Admin/Index.cshtml</files>
  <read_first>
    - 341-PATTERNS.md §4 "Views/Admin/Index.cshtml" (full section — 1 card insert verbatim adaptation)
    - 341-RESEARCH.md §Existing patterns Pattern 7 "Admin card grid (D-04)" (L266-302)
    - 341-CONTEXT.md §Implementation Decisions D-04
    - Views/Admin/Index.cshtml (current ~85 LoC — read full to identify exact insertion point setelah ManageOrganization card block ends)
  </read_first>
  <action>
    Edit `Views/Admin/Index.cshtml`. Cari card block ManageOrganization (di sekitar L35-49 per Patterns mapping), identifikasi closing `}` brace dari `@if (User.IsInRole("Admin") || User.IsInRole("HC"))` block tersebut.

    Sisipkan block baru SEGERA setelah closing `}` ManageOrganization card (atau di posisi setara dalam grid `.row`):

    ```cshtml
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("ManageOrgLevelLabels", "OrgLabel")" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-tags fs-5 text-primary"></i>
                        <span class="fw-bold">Label Tier Organisasi</span>
                    </div>
                    <small class="text-muted">Kelola nama tier organisasi (Bagian/Unit/Sub-unit) tanpa edit kode</small>
                </div>
            </div>
        </a>
    </div>
    }
    ```

    Adapatasi dari analog ManageOrganization card dengan 4 substitusi:
    1. `Url.Action("ManageOrganization", "Organization")` → `Url.Action("ManageOrgLevelLabels", "OrgLabel")`
    2. Icon `bi-diagram-3` → `bi-tags`
    3. Card title `Organization Structure` → `Label Tier Organisasi`
    4. Subtitle copy → `Kelola nama tier organisasi (Bagian/Unit/Sub-unit) tanpa edit kode`

    JANGAN ubah / hapus card existing lain. JANGAN tambah section header baru ("Pengaturan" tidak dibuat — D-04 lock).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj --nologo --verbosity minimal 2&gt;&amp;1 | grep -E "(error|Build succeeded)"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` PASS
    - `grep -c "ManageOrgLevelLabels" Views/Admin/Index.cshtml` ≥ 1
    - `grep -c "OrgLabel" Views/Admin/Index.cshtml` ≥ 1
    - `grep -c "Label Tier Organisasi" Views/Admin/Index.cshtml` ≥ 1
    - `grep -c "bi-tags" Views/Admin/Index.cshtml` ≥ 1
    - `grep -c "ManageOrganization" Views/Admin/Index.cshtml` ≥ 1 (existing card preserved)
    - `grep -c "bi-diagram-3" Views/Admin/Index.cshtml` ≥ 1 (existing icon preserved)
  </acceptance_criteria>
  <done>Card baru tampil di Admin/Index grid setelah card ManageOrganization, link ke `/Admin/ManageOrgLevelLabels`, role-gated Admin+HC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser smoke verify (manual UAT)</name>
  <files>Views/Admin/ManageOrgLevelLabels.cshtml, Views/Admin/Index.cshtml</files>
  <action>Manual browser verification — see <how-to-verify> below. No code change in this task; this is a checkpoint to confirm Plan 01 + Plan 02 Task 1-2 work end-to-end in the browser before declaring Plan 02 complete.</action>
  <verify>
    <automated>MISSING — manual browser UAT only (no automated test possible; see Plan 03 for xUnit coverage of validation paths)</automated>
  </verify>
  <done>User types "approved" after running through all 10 verification steps in <how-to-verify> without blocking issues.</done>
  <what-built>
    Plan 01 + Plan 02 Task 1-2 selesai: controller actions wired + ViewModel + Razor view + admin card. Page `/Admin/ManageOrgLevelLabels` siap diakses lokal.
  </what-built>
  <how-to-verify>
    1. Jalankan lokal: `dotnet run --project HcPortal.csproj` (atau `dotnet watch run`)
    2. Buka browser: `http://localhost:5277/Admin` — login sebagai Admin (admin@pertamina.com per dev credentials reference)
    3. **Verifikasi card baru:** Card "Label Tier Organisasi" dengan icon tag muncul di grid setelah card "Organization Structure"
    4. **Klik card** → navigasi ke `/Admin/ManageOrgLevelLabels`. Verifikasi:
       - Breadcrumb "Kelola Data > Label Tier Organisasi" tampil
       - Heading "Kelola Label Tier Organisasi" dengan icon tag
       - Info banner alert-info text "Label ini hanya nama tampilan tier..."
       - Tabel 4 row: Level 0 Bagian / Level 1 Unit / Level 2 Sub-unit / Level 3 "(belum diset)" + tombol "+ Tambah"
       - Setiap row terkonfigurasi (0-2) punya tombol Edit
       - Delete button visibility: HANYA muncul di Level 2 IF tidak ada OrganizationUnits.Level=2 (assumes seed DB; kalau tabel kosong Bagian/Unit/Sub-unit semua bisa delete; kalau ada unit semua disabled dengan tooltip)
    5. **Test Edit flow:** Klik Edit di Level 0 → modal "Edit Label Tier Level 0" terbuka:
       - Level field disabled menunjukkan "0"
       - Label input pre-filled "Bagian"
       - Edit jadi "Direktorat" → klik Simpan
       - Toast hijau muncul "Label level 0 berhasil diubah menjadi 'Direktorat'."
       - Page reload, row Level 0 sekarang "Direktorat"
       - **Rollback test data:** Edit Level 0 → ganti balik ke "Bagian" → Simpan
    6. **Test Add flow:** Klik "+ Tambah" di buffer row → modal "Tambah Level Tier 3" terbuka:
       - Level field disabled "3"
       - Label input kosong
       - Isi "Kelompok" → Simpan → toast hijau "Level 3 'Kelompok' berhasil ditambahkan."
       - Page reload, row Level 3 sekarang "Kelompok", row baru Level 4 "(belum diset)" muncul
    7. **Test Delete flow:** Klik Delete di Level 3 "Kelompok" (highest + unused) → native confirm browser muncul "Hapus label Level 3 \"Kelompok\"? Tidak bisa diundo." → OK → toast hijau "Level 3 berhasil dihapus." → reload, Level 3 kembali "(belum diset)"
    8. **Test validation error:** Klik Edit Level 0 → kosongkan input → Simpan → toast merah "Label tidak boleh kosong." (client-side preview catch). Lalu test panjang > 50 karakter (paste 60 'a') → toast merah "Label maksimal 50 karakter."
    9. **Test duplicate:** Klik Edit Level 0 → ganti jadi "Unit" (sudah dipakai Level 1) → Simpan → toast merah "Label 'Unit' sudah dipakai level lain." (server-side catch — sangat penting validate ini)
    10. **Test cancel modal:** Klik Edit → Batal → modal tutup, no change

    **Expected outcome semua step:** tidak ada console error, tidak ada 500/400, semua AJAX response 200 JSON body.

    **Browser DevTools check:** Network tab POST `/Admin/UpdateLevelLabel` body harus mengandung `__RequestVerificationToken=...` (antiforgery wired). Response 200 application/json `{"success":true,"message":"..."}`.
  </how-to-verify>
  <resume-signal>Type "approved" jika semua 10 step PASS, atau describe issues jika ada bug (planner akan revisi Task 1 atau Task 2)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM input → JS submit handler | Untrusted user keystroke + paste data jadi `label` string before POST |
| Browser → Server POST | URLSearchParams body (level, label, antiforgery) crosses ke `/Admin/UpdateLevelLabel|AddLevelLabel|DeleteLevelLabel` |
| Razor template → Browser HTML | Server-rendered Model.Rows data jadi visible markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-341-01 | Tampering | All 3 fetch POST | mitigate | `@Html.AntiForgeryToken()` render hidden input + JS `getAntiForgeryToken()` + `params.append('__RequestVerificationToken', ...)` di `ajaxPost` (Task 1 inline JS); 3-sided contract dengan Plan 01 controller `[ValidateAntiForgeryToken]` |
| T-341-09 | Tampering / XSS | Razor render of `row.Label` string | mitigate | Razor `@row.Label` auto-encodes HTML (default escape). JS interop via `@Html.Raw(Json.Serialize(row.Label))` → JSON string literal safe karena Json.Serialize escapes quotes/backslash properly (Pitfall §RESEARCH not 9 — this is XSS in label, threat T-341-09 dari research) |
| T-341-04 | Tampering | Client-side preview validation bypass via devtools | mitigate (defense-in-depth) | Submit handler `if (!label)` + `length > 50` jaga UX layer; SERVER tetap authoritative (Plan 01 inline checks). Bypass tidak break server. |
| T-341-02 | Elevation of Privilege | Admin card visibility | mitigate | Card wrapped `@if (User.IsInRole("Admin") || User.IsInRole("HC"))` Task 2 (D-04); non-Admin tidak lihat link (defense-in-depth bersama Plan 01 controller `[Authorize(Roles="Admin, HC")]`) |
| T-341-05 | Tampering | Delete button visibility client-side | mitigate (UX layer only) | Client-side `@if (row.CanDelete)` only renders for highest+unused. Server-side guard di Plan 01 DeleteLevelLabel handles bypass. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` PASS (Razor compile clean)
- `dotnet run --project HcPortal.csproj` start tanpa exception
- Browser smoke test Task 3 10 step PASS
- `dotnet test HcPortal.Tests` regression PASS (20/20 existing)
- No console error in browser DevTools saat page load atau saat AJAX submit
</verification>

<success_criteria>
1. **View renders correctly:** Browser at `http://localhost:5277/Admin/ManageOrgLevelLabels` shows 4 rows (3 configured + 1 buffer), breadcrumb, heading, info banner, 2 modals hidden in DOM
2. **Edit flow functional:** Modal opens with pre-filled label, fetch POST → toast success → page reload shows new label
3. **Add flow functional:** Modal opens with NextAddLevel value, fetch POST → toast success → page reload shows new row + new buffer row
4. **Delete flow functional:** Native confirm() → fetch POST → toast success → page reload removes row
5. **Validation error paths:** Empty label / >50 char / duplicate ALL show toast merah dengan message Indonesian sesuai (server-side authoritative)
6. **Admin card visible:** `/Admin/Index` shows new "Label Tier Organisasi" card for Admin+HC role; click navigates to page
7. **Antiforgery wired:** Network tab confirms `__RequestVerificationToken` in POST body; no 400 anti-forgery error
8. **Build + regression safe:** `dotnet build` + `dotnet test` PASS
</success_criteria>

<output>
After completion, create `.planning/phases/341-label-crud-page/341-02-SUMMARY.md` capturing:
- Final commit hash range (Task 1 view + Task 2 card)
- LoC count view file
- Acceptance criteria grep results
- Browser smoke verify outcome (10 step PASS/FAIL log dari Task 3 checkpoint)
- Any deviation dari RESEARCH Example 6 / PATTERNS §3 markup
- Screenshot path optional (Playwright snapshot kalau ada)
- Reference Plan 03 untuk test coverage downstream
</output>
