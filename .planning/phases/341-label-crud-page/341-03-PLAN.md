---
phase: 341
plan: 03
type: execute
wave: 3
depends_on:
  - 341-01
  - 341-02
files_modified:
  - HcPortal.Tests/OrgLabelControllerTests.cs
autonomous: false
requirements:
  - ORG-LABEL-04
  - ORG-LABEL-05
  - ORG-LABEL-06
tags:
  - xunit
  - controller-test
  - validation
  - uat
user_setup: []

must_haves:
  truths:
    - "File `HcPortal.Tests/OrgLabelControllerTests.cs` ada dengan minimal 8 [Fact] test yang exercise inline validation paths Plan 01 OrgLabelController POST actions"
    - "Test factory `MakeControllerWithCtx` instantiate controller dengan ApplicationDbContext InMemory + OrgLabelService + AuditLogService (UserManager substituted, see C-04)"
    - "Validation reject tests (empty / whitespace / >50 char / duplicate / non-next-level Add / non-highest Delete / highest-in-use Delete) PASS"
    - "Happy path test (UpdateLevelLabel valid) PASS dengan DB row mutated"
    - "Audit log entry written verified via Phase 340 existing 11 mutation [Fact] (regression-safe)"
    - "Full suite `dotnet test HcPortal.Tests` PASS — minimal 28 [Fact] (20 existing + 8 new)"
    - "Manual UAT Coach 403 verified: login as Coach role → `/Admin/ManageOrgLevelLabels` → returns 403 / Access Denied redirect (Plan 01 [Authorize(Roles=\"Admin, HC\")] proven)"
    - "Manual UAT audit log verified: edit label in browser → query `SELECT TOP 1 * FROM AuditLogs WHERE ActionType='OrgLabel-Update' ORDER BY Id DESC` returns row dengan ActorName + before/after format"
  artifacts:
    - path: "HcPortal.Tests/OrgLabelControllerTests.cs"
      provides: "xUnit Controller-level test 8 [Fact] covering validation + happy + delete guards"
      contains: "public class OrgLabelControllerTests"
      min_lines: 200
  key_links:
    - from: "HcPortal.Tests/OrgLabelControllerTests.cs"
      to: "Controllers/OrgLabelController (Plan 01)"
      via: "direct instantiation via MakeControllerWithCtx factory"
      pattern: "new OrgLabelController"
    - from: "HcPortal.Tests/OrgLabelControllerTests.cs"
      to: "HcPortal.Tests/OrgLabelServiceTests.cs"
      via: "factory pattern replication (DbContextOptionsBuilder + InMemory + seed 3 row)"
      pattern: "UseInMemoryDatabase"
---

<objective>
Build xUnit Controller-level test `HcPortal.Tests/OrgLabelControllerTests.cs` exercise inline validation paths (D-05 + D-08 + Pitfall 7) Plan 01 OrgLabelController POST actions. Replicate `OrgLabelServiceTests.MakeServiceWithCtx` factory + adapt jadi `MakeControllerWithCtx`. Per Patterns §5 + RESEARCH §Code Examples #7-#8, UserManager substituted via skip-actor-resolution strategy (Option 2 — keep test surface = validation logic, BUKAN actor identity). Plus 2 manual UAT checkpoint: Coach role 403 + audit log row inspection.

Purpose: Quality gate — validate semua validation rule + delete guard work as designed sebelum push commit batch v21.0. Manual UAT verify role-based 403 + audit DB row that automated test tidak cover karena no Identity harness di repo.

Output: 1 file test ~250 LoC dengan 8 [Fact] + manual UAT log di SUMMARY. Full suite PASS ≥28 [Fact].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/341-label-crud-page/341-CONTEXT.md
@.planning/phases/341-label-crud-page/341-RESEARCH.md
@.planning/phases/341-label-crud-page/341-PATTERNS.md
@.planning/phases/341-label-crud-page/341-VALIDATION.md
@.planning/phases/341-label-crud-page/341-01-SUMMARY.md
@.planning/phases/341-label-crud-page/341-02-SUMMARY.md
@docs/superpowers/specs/2026-06-02-manageorganization-overhaul-design.md
@CLAUDE.md

# Plan 01 + 02 artifacts (consume)
@Controllers/OrgLabelController.cs
@Models/ViewModels/ManageOrgLevelLabelsViewModel.cs

# Analog test file (replicate factory pattern)
@HcPortal.Tests/OrgLabelServiceTests.cs
@HcPortal.Tests/HcPortal.Tests.csproj

# Existing test infra references
@Services/OrgLabelService.cs
@Services/AuditLogService.cs
@Data/ApplicationDbContext.cs
@Models/OrganizationLevelLabel.cs

<interfaces>
<!-- Controller surface yang harus di-test, dari Plan 01. -->

OrgLabelController constructor signature (Plan 01 Task 2):
```csharp
public OrgLabelController(
    IOrgLabelService orgLabels,
    ApplicationDbContext context,
    UserManager<ApplicationUser> userManager)
```

Action signatures to test:
- `Task<IActionResult> UpdateLevelLabel(int level, string label)`
- `Task<IActionResult> AddLevelLabel(int level, string label)`
- `Task<IActionResult> DeleteLevelLabel(int level)`

Response shape: `Json(new { success = bool, message = string })` → wrapped in `JsonResult` with anonymous-object `.Value`.

Assertion pattern (reflection for anonymous-object access):
```csharp
private static bool GetSuccess(IActionResult result)
{
    var json = Assert.IsType<JsonResult>(result);
    return (bool)json.Value!.GetType().GetProperty("success")!.GetValue(json.Value)!;
}
private static string GetMessage(IActionResult result)
{
    var json = Assert.IsType<JsonResult>(result);
    return (string)json.Value!.GetType().GetProperty("message")!.GetValue(json.Value)!;
}
```

OrgLabelServiceTests.MakeServiceWithCtx factory (lines 25-45) — base to copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create OrgLabelControllerTests.cs with factory + 8 [Fact] tests</name>
  <files>HcPortal.Tests/OrgLabelControllerTests.cs</files>
  <read_first>
    - 341-PATTERNS.md §5 "HcPortal.Tests/OrgLabelControllerTests.cs" (full section — factory + [Fact] table)
    - 341-RESEARCH.md §Validation Architecture (test structure + assertion pattern)
    - 341-RESEARCH.md §Common Pitfalls 7 (mid-tier delete guard requirement)
    - HcPortal.Tests/OrgLabelServiceTests.cs L1-45 (factory pattern to copy verbatim)
    - HcPortal.Tests/OrgLabelServiceTests.cs full file (existing [Fact] style for reference)
    - HcPortal.Tests/HcPortal.Tests.csproj (verify InMemory provider 8.0.0 sudah installed Phase 340)
    - Controllers/OrgLabelController.cs (Plan 01 final — to confirm action signatures + validation message strings)
  </read_first>
  <action>
    Create file `HcPortal.Tests/OrgLabelControllerTests.cs`. Namespace `HcPortal.Tests`.

    **Imports:**
    ```csharp
    using System.Reflection;
    using Microsoft.AspNetCore.Http;
    using Microsoft.AspNetCore.Mvc;
    using Microsoft.EntityFrameworkCore;
    using Microsoft.Extensions.Caching.Memory;
    using HcPortal.Controllers;
    using HcPortal.Data;
    using HcPortal.Models;
    using HcPortal.Services;
    using Xunit;
    ```

    **Class skeleton:**
    ```csharp
    namespace HcPortal.Tests;

    /// <summary>
    /// Phase 341 Plan 03 — Controller validation paths.
    /// Strategy: skip actor-resolution (UserManager null-substitute) and assert pre-service inline
    /// validation. Validation rejects return before _userManager.GetUserAsync call, so null is safe.
    /// Happy path tests skip actor flow by seeding "system" user not required — service signs audit
    /// log with passed-in actorName which Test1 sets to "system" via direct call.
    /// </summary>
    public class OrgLabelControllerTests
    {
        // ── Factory ──────────────────────────────────────────────────────────────

        private static (OrgLabelController ctrl, ApplicationDbContext ctx) MakeControllerWithCtx(bool seedUnits = false)
        {
            var options = new DbContextOptionsBuilder<ApplicationDbContext>()
                .UseInMemoryDatabase(Guid.NewGuid().ToString())
                .Options;
            var ctx = new ApplicationDbContext(options);

            // Seed 3 default label rows (Level 0/1/2).
            ctx.OrganizationLevelLabels.AddRange(
                new OrganizationLevelLabel { Level = 0, Label = "Bagian",   UpdatedAt = DateTime.UtcNow, UpdatedBy = "system" },
                new OrganizationLevelLabel { Level = 1, Label = "Unit",     UpdatedAt = DateTime.UtcNow, UpdatedBy = "system" },
                new OrganizationLevelLabel { Level = 2, Label = "Sub-unit", UpdatedAt = DateTime.UtcNow, UpdatedBy = "system" }
            );

            // Optionally seed 1 OrganizationUnit at Level 2 to exercise IsUsed delete guard.
            if (seedUnits)
            {
                ctx.OrganizationUnits.Add(new OrganizationUnit
                {
                    Name = "TestUnit-L2",
                    Level = 2,
                    IsActive = true,
                    // ... fill required props per OrganizationUnit schema; check actual props
                });
            }
            ctx.SaveChanges();

            var cache = new MemoryCache(new MemoryCacheOptions());
            var auditLog = new AuditLogService(ctx);
            var svc = new OrgLabelService(ctx, cache, auditLog);

            // UserManager null-substitute: validation-rejection tests don't reach GetUserAsync call.
            // Casting null to UserManager<ApplicationUser> via #pragma warning disable to satisfy compiler.
            #pragma warning disable CS8625 // Cannot convert null literal to non-nullable reference type.
            var ctrl = new OrgLabelController(svc, ctx, null!);
            #pragma warning restore CS8625

            ctrl.ControllerContext = new ControllerContext { HttpContext = new DefaultHttpContext() };
            return (ctrl, ctx);
        }

        // ── Helpers ──────────────────────────────────────────────────────────────

        private static bool GetSuccess(IActionResult result)
        {
            var json = Assert.IsType<JsonResult>(result);
            return (bool)json.Value!.GetType().GetProperty("success")!.GetValue(json.Value)!;
        }

        private static string GetMessage(IActionResult result)
        {
            var json = Assert.IsType<JsonResult>(result);
            return (string)json.Value!.GetType().GetProperty("message")!.GetValue(json.Value)!;
        }

        // ── [Fact] tests ────────────────────────────────────────────────────────
        // ... 8 tests below ...
    }
    ```

    **8 [Fact] tests** (matches RESEARCH §Code Examples test table L575-587):

    1. **`UpdateLevelLabel_EmptyLabel_ReturnsJsonFailure`** — call `await ctrl.UpdateLevelLabel(0, "")` → assert `GetSuccess(result) == false` + `GetMessage(result).Contains("kosong")`

    2. **`UpdateLevelLabel_WhitespaceLabel_ReturnsJsonFailure`** — call `await ctrl.UpdateLevelLabel(0, "   ")` → assert success=false + message contains "kosong"

    3. **`UpdateLevelLabel_TooLong_ReturnsJsonFailure`** — call `await ctrl.UpdateLevelLabel(0, new string('x', 51))` → assert success=false + message contains "50"

    4. **`UpdateLevelLabel_DuplicateAcrossLevels_ReturnsJsonFailure`** — call `await ctrl.UpdateLevelLabel(0, "Unit")` (already at Level 1) → assert success=false + message contains "sudah dipakai"

    5. **`AddLevelLabel_NonNextLevel_ReturnsJsonFailure`** — call `await ctrl.AddLevelLabel(99, "X")` (max configured = 2, expected next = 3) → assert success=false + message contains "Hanya level berikutnya"

    6. **`DeleteLevelLabel_NonHighest_ReturnsJsonFailure`** — call `await ctrl.DeleteLevelLabel(0)` (max = 2) → assert success=false + message contains "tertinggi"

    7. **`DeleteLevelLabel_HighestInUse_ReturnsJsonFailure`** — call `MakeControllerWithCtx(seedUnits: true)` lalu `await ctrl.DeleteLevelLabel(2)` (Level 2 dipakai TestUnit) → assert success=false + message contains "dipakai"

    8. **`UpdateLevelLabel_DuplicateSameLevel_AllowsRewrite`** — sanity check D-05 unique predicate `l.Level != currentLevel`: call `await ctrl.UpdateLevelLabel(0, "Bagian")` (same value as current) → assert success=true (no false-positive duplicate). Verify DB row Level 0 Label still "Bagian".

    **Note untuk Task 7 OrganizationUnit seed:** Read existing `OrgLabelServiceTests.cs` untuk method yang seed OrganizationUnit (kemungkinan ada di test `GetMaxUsedLevelAsync_*`). Re-use pattern; minimal required properties: Name, Level, IsActive, plus FK ParentId nullable. Kalau OrganizationUnit punya required FK lain yang sulit setup di InMemory, alternative: seed langsung dengan `ctx.OrganizationUnits.Add(new OrganizationUnit { ... minimal props ... })` + `ctx.SaveChanges()`.

    Comment di top file:
    ```csharp
    // Phase 341 Plan 03 — OrgLabelController inline validation paths.
    // REQ coverage: ORG-LABEL-04 (validation enforcement), ORG-LABEL-06 (server-side checks).
    // Strategy: skip actor-resolution test (UserManager null-substitute) per RESEARCH §Code Examples #7 Option 2.
    // Happy-path mutation + audit log coverage retained by Phase 340 OrgLabelServiceTests (11 mutation [Fact]).
    ```

    **JANGAN** test happy path AddAsync atau DeleteAsync (memerlukan UserManager.GetUserAsync valid resolution). Test #8 UpdateLevelLabel happy path SAJA jika UserManager call dapat di-bypass (audit log gets `actorUserId=""` and `actorName="Unknown"` because `currentUser` will be null via `await _userManager.GetUserAsync(User)` returning null when UserManager itself is null — this WILL throw NullReferenceException). **Resolution untuk Test 8:** Skip happy UpdateLevelLabel entirely DAN replace dengan validation-side test `UpdateLevelLabel_SameLabelSameLevel_NotFlaggedAsDuplicate` yang exercise duplicate predicate excluding own level WITHOUT triggering actor flow — return before service call by causing benign rejection.

    **Revised Test 8 (no UserManager dep):** `UpdateLevelLabel_DuplicateAcrossLevels_RespectsExcludeOwn` — instead of asserting success, assert that duplicate check correctly excludes own level: call `await ctrl.UpdateLevelLabel(0, "Unit")` returns failure due to Level 1 conflict (proves predicate `l.Level != 0` works) — same outcome as Test 4 but verifies inverse (Update with same value would NOT trigger). Drop happy-path test entirely; document in test comment "Happy-path mutation tested via OrgLabelServiceTests Phase 340 (UpdateAsync_KnownLevel_UpdatesRowAndInvalidatesCacheAndLogsAudit)."

    **Final test count: 7 [Fact]** (drop Test 8 happy path). Add 1 final `UpdateLevelLabel_NoOpSameLabel_SkipsDuplicateCheck` — pass same Label value as current; since `l.Level != currentLevel` filter excludes Level 0 from duplicate match, AND the value WOULD match Level 0's existing "Bagian" but Level 0 itself is excluded → AnyAsync returns false → execution proceeds to service call → NullReferenceException at `await _userManager.GetUserAsync(User)`.

    **Confirm 7 [Fact] count** (Tests 1-7 above). Skip Test 8 (memerlukan UserManager). Manual UAT Task 2 covers happy + audit verification.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --nologo --verbosity minimal --filter "FullyQualifiedName~OrgLabelControllerTests" 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/OrgLabelControllerTests.cs` ada
    - `grep -c "\[Fact\]" HcPortal.Tests/OrgLabelControllerTests.cs` = 7
    - `grep -c "public class OrgLabelControllerTests" HcPortal.Tests/OrgLabelControllerTests.cs` = 1
    - `grep -c "MakeControllerWithCtx" HcPortal.Tests/OrgLabelControllerTests.cs` ≥ 8 (factory definition + 7 test usage)
    - `grep -c "UseInMemoryDatabase" HcPortal.Tests/OrgLabelControllerTests.cs` = 1
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~OrgLabelControllerTests"` → 7/7 PASS
    - `dotnet test HcPortal.Tests` (full suite) → minimum 27/27 PASS (20 existing + 7 new); count grows by 7 from baseline 20
    - Runtime full suite < 5 detik
    - 0 test marked Skipped / Inconclusive
  </acceptance_criteria>
  <done>7 [Fact] PASS via `dotnet test`; validation logic Plan 01 OrgLabelController proven; Phase 340 20/20 still PASS (regression-safe).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — Coach role 403 + audit log row inspection</name>
  <files>Controllers/OrgLabelController.cs, Views/Admin/ManageOrgLevelLabels.cshtml</files>
  <action>Manual UAT — see <how-to-verify> below. No code change in this task; this checkpoint confirms 3 things automated test cannot: (1) role-based 403 via Identity middleware [T-341-02], (2) audit log row content correctness in live DB [ORG-LABEL-05], (3) Phase 340 GET /Admin/GetLevelLabels endpoint regression-safe.</action>
  <verify>
    <automated>MISSING — manual UAT only (Identity harness not present per Phase 340 G7/G8 precedent; live DB row inspection requires SSMS/sqlcmd)</automated>
  </verify>
  <done>User types "approved" after UAT 1, UAT 2, and UAT 3 outcomes documented in SUMMARY; deviations logged if any.</done>
  <what-built>
    Plan 01 + 02 + Plan 03 Task 1 selesai. Code complete + 27/27 xUnit PASS. Last gate: manual UAT untuk 2 hal automated test tidak bisa cover (no Identity harness di repo + audit log inspection memerlukan live DB).
  </what-built>
  <how-to-verify>
    **Pre-req:** `dotnet run` aktif di `http://localhost:5277`. DB lokal HcPortalDB tersedia.

    **UAT 1 — Coach role 403 (T-341-02 mitigation proof):**
    1. Logout dari admin (kalau sedang login). Login as Coach role:
       - Username/email: gunakan akun Coach existing di DB lokal (cek `SELECT TOP 5 Email FROM AspNetUsers u INNER JOIN AspNetUserRoles ur ON u.Id=ur.UserId INNER JOIN AspNetRoles r ON ur.RoleId=r.Id WHERE r.Name='Coach'` untuk find Coach account)
       - Jika tidak ada Coach user di DB lokal, skip UAT 1 dan dokumentasikan deviation
    2. Setelah login Coach, navigate manual ke `http://localhost:5277/Admin/ManageOrgLevelLabels`
    3. **Expected:** Server respond 403 Forbidden ATAU redirect ke `/Account/AccessDenied` ATAU `/Account/Login?ReturnUrl=...`
    4. Verifikasi dari DevTools Network tab: status code 200 redirect chain ending at AccessDenied (ASP.NET Core default) atau 403
    5. **Repeat dengan Admin/HC user:** Login as admin → same URL → status 200 page render
    6. Capture screenshot atau text outcome ke SUMMARY

    **UAT 2 — Audit log row inspection (ORG-LABEL-05 proof):**
    1. Login as Admin
    2. Buka `/Admin/ManageOrgLevelLabels` → Edit Level 0 → ganti dari "Bagian" jadi "Direktorat" → Simpan → toast hijau
    3. Open SSMS / Azure Data Studio / sqlcmd, run:
       ```sql
       SELECT TOP 1 Id, Timestamp, ActorUserId, ActorName, ActionType, Description, TargetId, TargetType
       FROM AuditLogs
       WHERE ActionType = 'OrgLabel-Update'
       ORDER BY Id DESC;
       ```
    4. **Expected row content:**
       - `ActorUserId` = GUID user admin yang login (not empty, not "system")
       - `ActorName` = format `"{NIP} - {FullName}"` atau `"{FullName}"` (e.g., "12345 - Admin User" atau "Admin User")
       - `ActionType` = `"OrgLabel-Update"`
       - `Description` contains `"Level 0"` + `"Bagian"` + `"Direktorat"` (e.g., `"Level 0: 'Bagian' → 'Direktorat'"`)
       - `TargetId` = `0`
       - `TargetType` = `"OrganizationLevelLabel"`
    5. **Test Add audit:** Add Level 3 "Kelompok" → query `WHERE ActionType = 'OrgLabel-Add' ORDER BY Id DESC` → verify Description contains `"Level 3"` + `"Kelompok"` + `"created"`
    6. **Test Delete audit:** Delete Level 3 → query `WHERE ActionType = 'OrgLabel-Delete' ORDER BY Id DESC` → verify Description contains `"Level 3"` + `"Kelompok"` + `"deleted"`
    7. **Rollback test data:** Edit Level 0 → ganti balik ke "Bagian"

    **UAT 3 — Phase 340 endpoint regression smoke:**
    1. Login as any authenticated user (Admin/HC/Coach/Worker — class-level [Authorize] only)
    2. Browser navigate `http://localhost:5277/Admin/GetLevelLabels`
    3. Expected: 200 OK JSON body `{"0":"Bagian","1":"Unit","2":"Sub-unit"}` (verbatim Phase 340 baseline)

    **Outcome:** Document 3 UAT outcomes (PASS / PARTIAL / FAIL with note) di SUMMARY. Sertakan exact SQL row excerpt UAT 2.
  </how-to-verify>
  <resume-signal>Type "approved" jika UAT 1, 2, 3 PASS atau document deviation; describe issues jika ada bug</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| xUnit test → Controller | Direct method invocation via factory-instantiated controller; bypasses HTTP pipeline (no antiforgery/authn middleware) — test ASSERT inline validation logic only |
| Test InMemory DB → assertion | EF InMemory provider — no SQL collation; case-sensitivity diff from prod (Pitfall 9). Tests calibrated dengan literal seed values untuk avoid case discrepancy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-341-04 | Tampering | Test coverage of validation rules | mitigate | 7 [Fact] exercise inline validation (empty/whitespace/long/duplicate/non-next/non-highest/highest-in-use); proves D-05 + D-08 + Pitfall 7 wired correctly |
| T-341-02 | Elevation of Privilege | Coach role 403 | mitigate | Manual UAT Task 2 verifies live; no automated test possible (Phase 340 G7/G8 precedent — no Identity harness) |
| T-341-06 | Information Disclosure | Audit log content correctness | mitigate (manual) | UAT 2 inspects DB row format; ensures Description doesn't leak unintended fields |
| T-341-09 | Tampering | XSS in label render | accept | Razor auto-encode covers (Plan 02 threat); Plan 03 test scope = backend validation only |
</threat_model>

<verification>
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~OrgLabelControllerTests"` → 7/7 PASS
- `dotnet test HcPortal.Tests` (full suite) → ≥27/27 PASS (baseline 20 + new 7)
- Test runtime < 5 detik
- Manual UAT 1 outcome: Coach → 403 / AccessDenied redirect
- Manual UAT 2 outcome: 3 audit rows confirmed in DB (Update + Add + Delete)
- Manual UAT 3 outcome: GET /Admin/GetLevelLabels 200 JSON dict
- 0 regression in Phase 340 13 OrgLabelServiceTests [Fact]
</verification>

<success_criteria>
1. **Test file built:** `HcPortal.Tests/OrgLabelControllerTests.cs` ada, 7 [Fact], factory + assertion helpers complete
2. **All new tests PASS:** `dotnet test --filter OrgLabelControllerTests` → 7/7 PASS
3. **No regression:** Full suite ≥27/27 PASS (20 existing + 7 new); semua Phase 340 OrgLabelServiceTests 13/13 unaffected
4. **Coach 403 verified live:** UAT Task 2 confirms role-based authz working at HTTP layer
5. **Audit log row content correct:** UAT Task 2 confirms Description format matches Phase 340 D-04 spec literally
6. **Phase 340 endpoint regression-safe:** UAT Task 2 confirms GET /Admin/GetLevelLabels still 200 JSON
7. **REQ ORG-LABEL-04, 05, 06 PROVEN:** SC #2 + #3 cover validation logic (06), UAT covers page access + delete rule (04), UAT 2 covers audit log (05)
</success_criteria>

<output>
After completion, create `.planning/phases/341-label-crud-page/341-03-SUMMARY.md` capturing:
- Final commit hash range Task 1 test file
- Test count delta: baseline X → final X+7
- Full suite runtime
- Per-test outcome table (7 [Fact] PASS/FAIL)
- UAT 1 outcome: Coach 403 (with screenshot or text log)
- UAT 2 outcome: 3 audit log SQL row excerpts (Update / Add / Delete)
- UAT 3 outcome: GET /Admin/GetLevelLabels JSON body
- Any deviation dari plan (e.g., UAT 1 skipped karena no Coach user di DB lokal)
- Reference Phase 341 closure: 3 plan SUMMARY linked; ready / pending push origin/main
</output>
