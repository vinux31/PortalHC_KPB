---
phase: 342
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/OrganizationController.cs
autonomous: true
requirements:
  - ORG-TREE-02
  - ORG-TREE-07
tags:
  - controller
  - aspnet-mvc
  - cascade-preview
  - validation
user_setup: []

must_haves:
  truths:
    - "Build solution sukses tanpa error (`dotnet build HcPortal.csproj`) setelah dup-check edit + PreviewEditCascade ditambah"
    - "AddOrganizationUnit dup-check sekarang per-parent: `AnyAsync(u => u.Name == name.Trim() && u.ParentId == parentId)` — 'Operations' boleh ada di 2 Bagian beda (ORG-TREE-02)"
    - "EditOrganizationUnit dup-check sekarang per-parent: `AnyAsync(u => u.Name == name.Trim() && u.ParentId == parentId && u.Id != id)` (ORG-TREE-02)"
    - "Action baru PreviewEditCascade(int id, string name, int? parentId) ada dengan trio attribute [HttpPost] + [Authorize(Roles=\"Admin, HC\")] + [ValidateAntiForgeryToken]"
    - "PreviewEditCascade early-return {nameChanged:false, parentChanged:false} bila tidak ada perubahan nama maupun parent (D-04)"
    - "PreviewEditCascade count predikat MIRROR PERSIS mutasi EditOrganizationUnit: rename Level==0 pakai Section/AssignmentSection/Bagian/Bagian, rename Level>=1 pakai Unit/AssignmentUnit/Unit/Unit, reparent count KEEMPAT field-pair (A1 full-accuracy)"
    - "PreviewEditCascade return Json dengan 6 properti: nameChanged, parentChanged, affectedUsersCount, affectedMappingsCount, affectedKompetensiCount, affectedGuidanceCount"
    - "EditOrganizationUnit + AddOrganizationUnit body cascade existing TIDAK diubah selain 2 baris dup-check (regression-safe)"
    - "PreviewEditCascade tidak memanggil _userManager (test fixture boleh null!)"
  artifacts:
    - path: "Controllers/OrganizationController.cs"
      provides: "Dup-check per-parent (2 edit) + PreviewEditCascade read-only count action"
      contains: "public async Task<IActionResult> PreviewEditCascade"
      min_lines: 320
  key_links:
    - from: "Controllers/OrganizationController.cs PreviewEditCascade"
      to: "Data/ApplicationDbContext (Users/CoachCoacheeMappings/ProtonKompetensiList/CoachingGuidanceFiles)"
      via: "CountAsync per field-pair (mirror EditOrganizationUnit cascade predicate)"
      pattern: "CountAsync\\(u => u.Section == oldName\\)"
    - from: "Controllers/OrganizationController.cs AddOrganizationUnit/EditOrganizationUnit"
      to: "Data/ApplicationDbContext.OrganizationUnits"
      via: "AnyAsync dup-check scoped per ParentId"
      pattern: "u.ParentId == parentId"
---

<objective>
Lapis backend Phase 342: perbaiki validasi duplicate-name dari GLOBAL ke PER-PARENT (ORG-TREE-02) dan tambah action read-only `PreviewEditCascade` (ORG-TREE-07) yang menghitung dampak cascade SEBELUM `EditOrganizationUnit` aktual dijalankan. Hanya `Controllers/OrganizationController.cs` yang disentuh — extend in-place, jangan rewrite.

Purpose: Interface-first. Plan 02 (frontend JS + view) hanya perlu memanggil URL `/Admin/PreviewEditCascade` yang sudah ada dan membaca 6 properti JSON. Plan 03 (xUnit) hanya perlu instantiate controller untuk assert dup-name accept/reject + preview-count==actual. Akurasi count adalah kunci: predikat COUNT WAJIB identik field-by-field dengan loop mutasi `EditOrganizationUnit` agar preview tidak menipu user (Pitfall 1, prioritas tertinggi RESEARCH).

Output: `Controllers/OrganizationController.cs` (extend ~60 LoC: 2 baris dup-check + 1 action baru ~55 LoC). Build pass + cascade existing + AddOrganizationUnit/EditOrganizationUnit body regresi-free.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v21.0-REQUIREMENTS.md
@.planning/phases/342-manageorganization-page-fixes/342-CONTEXT.md
@.planning/phases/342-manageorganization-page-fixes/342-RESEARCH.md
@.planning/phases/342-manageorganization-page-fixes/342-PATTERNS.md
@.planning/phases/342-manageorganization-page-fixes/342-VALIDATION.md
@docs/superpowers/specs/2026-06-02-manageorganization-overhaul-design.md
@CLAUDE.md

# File yang dimodifikasi (extend in-place — JANGAN rewrite)
@Controllers/OrganizationController.cs

<interfaces>
<!-- Predikat mutasi aktual yang HARUS di-mirror oleh PreviewEditCascade. -->
<!-- Diverifikasi langsung dari OrganizationController.cs sesi 2026-06-03 (authoritative). -->
<!-- Executor: pakai field-pair ini PERSIS — JANGAN eksplorasi model lebih dalam. -->

EditOrganizationUnit cascade RENAME Level==0 (L198-213):
  Users.Section == oldName  →  name.Trim()
  CoachCoacheeMappings.AssignmentSection == oldName
  ProtonKompetensiList.Bagian == oldName
  CoachingGuidanceFiles.Bagian == oldName

EditOrganizationUnit cascade RENAME Level>=1 (L214-229):
  Users.Unit == oldName
  CoachCoacheeMappings.AssignmentUnit == oldName
  ProtonKompetensiList.Unit == oldName
  CoachingGuidanceFiles.Unit == oldName

EditOrganizationUnit cascade REPARENT (L233-263, predikat `oldParentId != parentId && unit.Level >= 1`):
  Users.Unit == oldName            (Section di-set ke ancestor baru)
  CoachCoacheeMappings.AssignmentUnit == oldName  (AssignmentSection di-set ke ancestor baru)
  ProtonKompetensiList.Unit == oldName   (Bagian di-set ke ancestor baru)
  CoachingGuidanceFiles.Unit == oldName  (Bagian di-set ke ancestor baru)
  >>> A1 DECISION: count KEEMPAT field-pair untuk reparent (BUKAN affectedUsers saja).
  >>> Spec L433-438 under-reports (hanya users); kita pilih full-accuracy agar preview == actual.

Model field-name (DIVERIFIKASI):
  ApplicationUser.Section / ApplicationUser.Unit
  CoachCoacheeMapping.AssignmentSection / CoachCoacheeMapping.AssignmentUnit
  ProtonKompetensi.Bagian / ProtonKompetensi.Unit  (DbSet: ProtonKompetensiList)
  CoachingGuidanceFile.Bagian / CoachingGuidanceFile.Unit  (DbSet: CoachingGuidanceFiles)

DbSets di ApplicationDbContext: Users, CoachCoacheeMappings, ProtonKompetensiList, CoachingGuidanceFiles, OrganizationUnits.

Dual-response convention (EditOrganizationUnit L271-274): `IsAjaxRequest()` → Json vs RedirectToAction.
PreviewEditCascade = AJAX-only → cukup `Json(...)` (tanpa cabang non-AJAX).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dup-name per-parent di AddOrganizationUnit + EditOrganizationUnit (ORG-TREE-02)</name>
  <files>Controllers/OrganizationController.cs</files>
  <read_first>
    - 342-PATTERNS.md §"Controllers/OrganizationController.cs" A1 (dup-name per-parent — exact before/after)
    - 342-RESEARCH.md §Code Examples #5 (dup-name per-parent verbatim) + Pitfall 5 (collation casing)
    - 342-CONTEXT.md §Spec-locked "Dup-name per-parent"
    - Controllers/OrganizationController.cs L74-122 (AddOrganizationUnit, dup-check L85) + L128-275 (EditOrganizationUnit, dup-check L149)
  </read_first>
  <action>
    Ubah 2 baris dup-check di `Controllers/OrganizationController.cs`. TIDAK ubah baris lain.

    **Edit 1 — AddOrganizationUnit (saat ini L85).** Ganti:
    ```csharp
    bool duplicate = await _context.OrganizationUnits.AnyAsync(u => u.Name == name.Trim());
    ```
    menjadi:
    ```csharp
    bool duplicate = await _context.OrganizationUnits
        .AnyAsync(u => u.Name == name.Trim() && u.ParentId == parentId);
    ```

    **Edit 2 — EditOrganizationUnit (saat ini L149).** Ganti:
    ```csharp
    bool duplicate = await _context.OrganizationUnits.AnyAsync(u => u.Name == name.Trim() && u.Id != id);
    ```
    menjadi:
    ```csharp
    bool duplicate = await _context.OrganizationUnits
        .AnyAsync(u => u.Name == name.Trim() && u.ParentId == parentId && u.Id != id);
    ```

    **Catatan implementasi (per RESEARCH):**
    - Pada Edit, `parentId` adalah nilai BARU dari form (rename+reparent dalam 1 submit aman — dup-check pakai parent tujuan).
    - EF translate `u.ParentId == parentId` dengan `parentId == null` → `IS NULL` (dup-check level root). Tidak perlu handling khusus null.
    - JANGAN ubah pesan error / TempData / branch IsAjaxRequest di sekitar dup-check — hanya predikat `AnyAsync` yang berubah.
    - Pitfall 5: behavior produksi SQL Server case-insensitive (by-design, tidak diubah phase ini). Casing test dikunci di Plan 03.

    Pesan error existing ("Nama unit sudah digunakan. Gunakan nama yang berbeda.") tetap dipertahankan — Bahasa Indonesia sesuai CLAUDE.md.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj --nologo --verbosity minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` PASS (Build succeeded, 0 Error)
    - `grep -c "u.ParentId == parentId" Controllers/OrganizationController.cs` >= 2 (2 dup-check edit)
    - `grep -c "AnyAsync(u => u.Name == name.Trim())" Controllers/OrganizationController.cs` = 0 (predikat global lama hilang dari Add)
    - `grep -c "&& u.ParentId == parentId && u.Id != id" Controllers/OrganizationController.cs` = 1 (Edit per-parent + exclude self)
    - `grep -c "Nama unit sudah digunakan" Controllers/OrganizationController.cs` >= 2 (pesan error existing preserved)
  </acceptance_criteria>
  <done>Dup-check Add + Edit scoped per-parent; build clean; pesan error Bahasa Indonesia preserved; Plan 03 dapat assert accept-di-parent-beda + reject-di-parent-sama.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PreviewEditCascade read-only count action (ORG-TREE-07, A1 full-accuracy)</name>
  <files>Controllers/OrganizationController.cs</files>
  <read_first>
    - 342-RESEARCH.md §Code Examples #2 (PreviewEditCascade endpoint verbatim) + §Common Pitfalls 1 (predicate drift — TERTINGGI) + Pitfall 6 (parentId null parsing)
    - 342-PATTERNS.md §"Controllers/OrganizationController.cs" A2 (auth attribute + count predikat mirror + early-return + JSON shape)
    - 342-CONTEXT.md §D-04 (always-call + early-return) + §Spec-locked "PreviewEditCascade endpoint"
    - Controllers/OrganizationController.cs L191-263 (cascade mutasi aktual — sumber yang di-mirror) + L271-286 (dual-response + IsDescendantAsync placement)
    - <interfaces> block di atas (field-pair predikat — authoritative)
  </read_first>
  <action>
    Tambah action baru `PreviewEditCascade` di `Controllers/OrganizationController.cs`. Letakkan SETELAH `EditOrganizationUnit` (saat ini berakhir L275), SEBELUM `private async Task<bool> IsDescendantAsync` (L277).

    **Attribute trio (copy verbatim dari EditOrganizationUnit L125-127):**
    ```csharp
    // POST /Admin/PreviewEditCascade — read-only dampak cascade sebelum apply Edit (ORG-TREE-07)
    [HttpPost]
    [Authorize(Roles = "Admin, HC")]
    [ValidateAntiForgeryToken]
    public async Task<IActionResult> PreviewEditCascade(int id, string name, int? parentId)
    {
        var unit = await _context.OrganizationUnits.FindAsync(id);
        if (unit == null) return Json(new { error = "Unit tidak ditemukan." });

        var trimmed = (name ?? "").Trim();
        bool nameChanged = !string.IsNullOrEmpty(trimmed) && unit.Name != trimmed;
        bool parentChanged = unit.ParentId != parentId;   // server authoritative (Pitfall 6)

        if (!nameChanged && !parentChanged)
            return Json(new { nameChanged = false, parentChanged = false });   // D-04 early-return

        int affectedUsers = 0, affectedMappings = 0, affectedKompetensi = 0, affectedGuidance = 0;
        string oldName = unit.Name;

        if (nameChanged)
        {
            if (unit.Level == 0)
            {
                affectedUsers      += await _context.Users.CountAsync(u => u.Section == oldName);
                affectedMappings   += await _context.CoachCoacheeMappings.CountAsync(m => m.AssignmentSection == oldName);
                affectedKompetensi += await _context.ProtonKompetensiList.CountAsync(k => k.Bagian == oldName);
                affectedGuidance   += await _context.CoachingGuidanceFiles.CountAsync(g => g.Bagian == oldName);
            }
            else
            {
                affectedUsers      += await _context.Users.CountAsync(u => u.Unit == oldName);
                affectedMappings   += await _context.CoachCoacheeMappings.CountAsync(m => m.AssignmentUnit == oldName);
                affectedKompetensi += await _context.ProtonKompetensiList.CountAsync(k => k.Unit == oldName);
                affectedGuidance   += await _context.CoachingGuidanceFiles.CountAsync(g => g.Unit == oldName);
            }
        }

        // A1 FULL-ACCURACY: reparent meng-update KEEMPAT field-pair di EditOrganizationUnit (L249-261),
        // jadi count KEEMPAT (BUKAN affectedUsers saja seperti spec L433-438) agar preview == actual.
        if (parentChanged && unit.Level >= 1 && !nameChanged)
        {
            affectedUsers      += await _context.Users.CountAsync(u => u.Unit == oldName);
            affectedMappings   += await _context.CoachCoacheeMappings.CountAsync(m => m.AssignmentUnit == oldName);
            affectedKompetensi += await _context.ProtonKompetensiList.CountAsync(k => k.Unit == oldName);
            affectedGuidance   += await _context.CoachingGuidanceFiles.CountAsync(g => g.Unit == oldName);
        }

        return Json(new {
            nameChanged,
            parentChanged,
            affectedUsersCount = affectedUsers,
            affectedMappingsCount = affectedMappings,
            affectedKompetensiCount = affectedKompetensi,
            affectedGuidanceCount = affectedGuidance
        });
    }
    ```

    **CRITICAL — predikat WAJIB identik dengan mutasi aktual (Pitfall 1, prioritas tertinggi).** Sumber yang di-mirror:
    - rename Level==0 → mutasi L198-213 (Section/AssignmentSection/Bagian/Bagian)
    - rename Level>=1 → mutasi L214-229 (Unit/AssignmentUnit/Unit/Unit)
    - reparent (`unit.Level >= 1`, predikat `u.Unit == oldName`) → mutasi L247-261 (KEEMPAT field-pair)
    Jangan menyimpang dari field-pair ini. Test Plan 03 (preview-count == actual) akan gagal kalau drift.

    **Catatan:**
    - Pakai `CountAsync` (single round-trip per field-pair), BUKAN `.Where(...).ToListAsync().Count` (Don't Hand-Roll RESEARCH).
    - `parentChanged` dihitung SERVER (`unit.ParentId != parentId`); JANGAN biarkan client menghitung (Pitfall 6 — `"" vs null`).
    - PreviewEditCascade TIDAK memanggil `_userManager` → fixture test Plan 03 boleh `null!`.
    - AJAX-only: cukup `Json(...)`, tidak ada cabang `IsAjaxRequest()`/RedirectToAction.
    - JANGAN ubah `EditOrganizationUnit` / `AddOrganizationUnit` / `IsDescendantAsync` / `UpdateChildrenLevelsAsync` body — hanya TAMBAH action baru.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj --nologo --verbosity minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` PASS (Build succeeded, 0 Error, 0 Warning baru)
    - `grep -c "public async Task<IActionResult> PreviewEditCascade" Controllers/OrganizationController.cs` = 1
    - `grep -c "\[ValidateAntiForgeryToken\]" Controllers/OrganizationController.cs` >= 4 (Edit + Toggle + Delete existing + PreviewEditCascade baru)
    - `grep -c "nameChanged = false, parentChanged = false" Controllers/OrganizationController.cs` = 1 (D-04 early-return)
    - `grep -c "CountAsync(u => u.Section == oldName)" Controllers/OrganizationController.cs` = 1 (rename Level 0 users)
    - `grep -c "CountAsync(u => u.Unit == oldName)" Controllers/OrganizationController.cs` = 2 (rename Level>=1 + reparent users)
    - `grep -c "affectedMappingsCount = affectedMappings" Controllers/OrganizationController.cs` = 1 (JSON shape)
    - `grep -c "CountAsync(m => m.AssignmentUnit == oldName)" Controllers/OrganizationController.cs` = 2 (rename Level>=1 + reparent mappings — A1 full-accuracy)
    - `grep -c "affectedGuidanceCount = affectedGuidance" Controllers/OrganizationController.cs` = 1
    - `dotnet test HcPortal.Tests --nologo` PASS (existing suite regression-safe; tidak menambah test di plan ini)
  </acceptance_criteria>
  <done>PreviewEditCascade compile-clean, predikat mirror mutasi aktual (A1 full-accuracy 4 field-pair reparent), early-return D-04, JSON 6-properti; cascade EditOrganizationUnit existing regresi-free; Plan 02 dapat `ajaxPost('PreviewEditCascade', {id,name,parentId})`, Plan 03 dapat assert count==actual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller POST | Untrusted form input (`id` int, `name` string, `parentId` int?) crosses ke server action PreviewEditCascade + Add/Edit |
| Controller → ApplicationDbContext | EF CountAsync/AnyAsync server-authoritative untuk count + uniqueness (client tidak boleh otoritatif) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-342-01 | Tampering | POST PreviewEditCascade (CSRF) | mitigate | `[ValidateAntiForgeryToken]` di PreviewEditCascade (Task 2); token validation ASP.NET Core middleware; client kirim via `ajaxPost` `__RequestVerificationToken` (Plan 02) |
| T-342-02 | Elevation of Privilege | POST PreviewEditCascade (role bypass) | mitigate | Method-level `[Authorize(Roles = "Admin, HC")]` (Task 2) — paritas dengan EditOrganizationUnit |
| T-342-03 | Tampering | PreviewEditCascade arbitrary id/parentId (devtools) | mitigate | `FindAsync(id)` null-guard return `{error}`; count read-only (no mutation); circular reparent di-block server-side oleh EditOrganizationUnit `parentId == id` + `IsDescendantAsync` (L160/L168) saat apply aktual |
| T-342-04 | Tampering | Dup-name bypass cross-parent (ORG-TREE-02) | mitigate | Server `AnyAsync(... && u.ParentId == parentId)` authoritative (Task 1); uniqueness tetap di-enforce per-parent server-side |
| T-342-05 | Information Disclosure | Cascade count leak (jumlah user/mapping) | accept | Org structure count bukan PII; hanya role Admin/HC yang bisa akses; nilai count = info display untuk decision support |
| T-342-06 | Tampering | PreviewEditCascade count drift vs actual (Pitfall 1) | mitigate | Predikat COUNT mirror persis mutasi EditOrganizationUnit (Task 2 A1 full-accuracy); dikunci oleh test preview==actual Plan 03 |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` PASS (Build succeeded, 0 Error)
- `dotnet test HcPortal.Tests` PASS (existing suite — regression-safe; Plan 01 tidak menambah test)
- `grep "PreviewEditCascade" Controllers/OrganizationController.cs` returns 1 action signature
- `grep "u.ParentId == parentId" Controllers/OrganizationController.cs` >= 2 (dup-check per-parent Add + Edit)
- EditOrganizationUnit cascade body (L191-263) verbatim tidak berubah (regression check via diff manual jika perlu)
</verification>

<success_criteria>
1. **Build pass:** `dotnet build HcPortal.csproj` exits 0, "Build succeeded", 0 Error baru.
2. **Dup-name per-parent (ORG-TREE-02):** Add + Edit dup-check scoped `&& u.ParentId == parentId`; predikat global lama hilang.
3. **PreviewEditCascade present (ORG-TREE-07):** Action ada dengan trio attribute, signature `(int id, string name, int? parentId)`, return Json 6-properti.
4. **D-04 early-return:** No-change → `{nameChanged:false, parentChanged:false}`.
5. **A1 full-accuracy:** Reparent count KEEMPAT field-pair (mirror mutasi L247-261), bukan users-only.
6. **Regression-safe:** EditOrganizationUnit/AddOrganizationUnit cascade body + dual-response preserved; existing xUnit suite PASS.
7. **Threat T-342-01..04, 06 mitigated:** Attribute + server-authoritative count/dup verifiable via grep.
</success_criteria>

<output>
After completion, create `.planning/phases/342-manageorganization-page-fixes/342-01-SUMMARY.md` capturing:
- Final commit hash range untuk 2 task
- LoC delta `Controllers/OrganizationController.cs` (before ~521 → after ~580)
- A1 decision recorded: full-accuracy reparent count (4 field-pair) — note untuk Plan 03 test calibration
- Acceptance criteria check results (grep counts + build/test pass)
- Confirm EditOrganizationUnit/AddOrganizationUnit cascade body unchanged
- Reference Plan 02 (frontend consume PreviewEditCascade) + Plan 03 (test count==actual)
</output>
