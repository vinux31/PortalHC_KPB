---
phase: 342
plan: 02
type: execute
wave: 2
depends_on:
  - "342-01"
files_modified:
  - wwwroot/js/orgTree.js
  - Views/Admin/ManageOrganization.cshtml
autonomous: false
requirements:
  - ORG-TREE-01
  - ORG-TREE-03
  - ORG-TREE-04
  - ORG-TREE-05
  - ORG-TREE-06
  - ORG-TREE-07
  - ORG-TREE-08
  - ORG-TREE-09
  - ORG-TREE-10
tags:
  - javascript
  - razor-view
  - tree-ui
  - bootstrap-modal
  - css
user_setup: []

must_haves:
  truths:
    - "Dropdown induk modal terurut pre-order DFS (parent → keturunan → sibling) via flattenTreePreOrder, bukan flat per-level (ORG-TREE-01)"
    - "Parent nonaktif TAMPIL di dropdown dengan suffix ' (nonaktif)' + warna grey #999, tidak disembunyikan (ORG-TREE-03)"
    - "openDeleteModal dipanggil via data-id/data-name/data-child-count + event delegation '.js-delete-trigger', bukan inline onclick string interpolation (ORG-TREE-04)"
    - "Icon palette extend level-3/4/5 (CSS) + JS levelClass cap di level 5 (`level <= 5 ? level : 5`), tidak cap di level-2 (ORG-TREE-05)"
    - "Path breadcrumb real-time render saat select#unitModalParent change ('Path: RFCC → LPG Treating → (unit baru di sini)') (ORG-TREE-06)"
    - "submitUnitModal Edit branch ALWAYS POST PreviewEditCascade; bila total affected > 0 tampil Bootstrap cascade-confirm modal 4-line breakdown sebelum EditOrganizationUnit (ORG-TREE-07, D-02/D-04)"
    - "Legend warna↔tier-label di card-header bersumber GetLevelLabels fetch (ORG-TREE-08)"
    - "Modal title dynamic per level induk ('Tambah Bagian'/'Tambah Unit'/...) fallback 'Level N', bersumber labelMap (ORG-TREE-09)"
    - "Tiap tree row punya badge tier label bersumber GetLevelLabels, warna reuse level palette (ORG-TREE-10, D-03)"
    - "labelMap di-fetch SEBELUM renderNode/renderLegend (async init ordering, Pitfall 4) — badge tidak render 'Level N' di first paint"
    - "SortableJS drag-reorder TIDAK rusak: <li data-id> + .drag-handle tidak diubah (Pitfall 2)"
    - "Build view tidak error (`dotnet build`); page /Admin/ManageOrganization render tanpa JS console error (browser smoke)"
  artifacts:
    - path: "wwwroot/js/orgTree.js"
      provides: "flattenTreePreOrder + fetchLabels/getLabelForLevel + renderLegend + escape fix + level cap + badge + path/title + cascade-confirm flow"
      contains: "function flattenTreePreOrder"
      min_lines: 540
    - path: "Views/Admin/ManageOrganization.cshtml"
      provides: "CSS palette level-3/4/5 + tier-badge/legend-swatch + legend block + path div + cascade-confirm modal + async init wrapper"
      contains: "org-node-icon.level-3"
  key_links:
    - from: "wwwroot/js/orgTree.js fetchLabels"
      to: "GET /Admin/GetLevelLabels (Phase 340 endpoint)"
      via: "ajaxGet('GetLevelLabels') 1 fetch on-load cached labelMap"
      pattern: "ajaxGet\\('GetLevelLabels'\\)"
    - from: "wwwroot/js/orgTree.js submitUnitModal"
      to: "POST /Admin/PreviewEditCascade (Plan 01)"
      via: "ajaxPost preview → showCascadeConfirm modal → EditOrganizationUnit"
      pattern: "ajaxPost\\('PreviewEditCascade'"
    - from: "wwwroot/js/orgTree.js renderNode"
      to: "Views/Admin/ManageOrganization.cshtml .org-tier-badge CSS"
      via: "badge span class org-tier-badge level-N"
      pattern: "org-tier-badge"
---

<objective>
Lapis frontend Phase 342: perbaiki 4 bug JS/view (pre-order DFS, parent nonaktif, escape, level cap) + tambah 5 UX (path breadcrumb, cascade-confirm modal, legend, modal title dynamic, tier badge). Sentuh `wwwroot/js/orgTree.js` + `Views/Admin/ManageOrganization.cshtml`. Extend in-place — kembalikan fungsi yang menyimpang ke pola existing, jangan rewrite (RESEARCH Key insight: 90% re-wiring helper yang sudah ada).

Purpose: Page `/Admin/ManageOrganization` jadi hierarki dropdown intuitif (pre-order), parent nonaktif tidak hilang, nama dengan kutip tidak break (data-attribute), warna tier konsisten level 0-5, user lihat path + dampak cascade sebelum apply, label tier eksplisit (legend + badge + title) bersumber endpoint Phase 340.

Output: `wwwroot/js/orgTree.js` (extend ~70 LoC: 4 helper baru + 5 refactor) + `Views/Admin/ManageOrganization.cshtml` (extend ~50 LoC: CSS + legend + path div + cascade modal + async init). Build pass + browser smoke (drag-reorder/toggle/delete tetap jalan). Konsumsi PreviewEditCascade dari Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v21.0-REQUIREMENTS.md
@.planning/phases/342-manageorganization-page-fixes/342-CONTEXT.md
@.planning/phases/342-manageorganization-page-fixes/342-RESEARCH.md
@.planning/phases/342-manageorganization-page-fixes/342-PATTERNS.md
@.planning/phases/342-manageorganization-page-fixes/342-VALIDATION.md
@docs/superpowers/specs/2026-06-02-manageorganization-overhaul-design.md
@CLAUDE.md

# File yang dimodifikasi (extend in-place — JANGAN rewrite)
@wwwroot/js/orgTree.js
@Views/Admin/ManageOrganization.cshtml

# Analog Phase 341 (Bootstrap modal pattern — read-only)
@Views/Admin/ManageOrgLevelLabels.cshtml

<interfaces>
<!-- Helper existing yang DIPAKAI ULANG (jangan rewrite). Line aktual sesi 2026-06-03. -->
<!-- Executor: pakai langsung — JANGAN eksplorasi file lebih dalam. -->

orgTree.js existing helpers:
  ajaxGet(url)                     L28-34  → fetch GET JSON
  ajaxPost(url, data)              L13-26  → fetch POST + __RequestVerificationToken
  escapeHtml(str)                  L38-45  → HTML-escape (pakai untuk data-name value)
  buildTree(flatList)              L47-60  → flat → roots (set .children, me-mutate _flatUnits)
  getDescendantIds(id)             L275-285 → Set descendant ids (anti-circular)
  findUnit(id)                     L271-273 → _flatUnits.find(u => u.id === id)
  populateParentDropdown(excludeId) L287-303 → REFACTOR (pre-order DFS + inactive visible)
  renderNode(node, level)          L118-178 → MODIFY (levelClass L123, escape L150, badge L171-173)
  openAddModal(parentId)           L307-319 → MODIFY (title dynamic)
  openEditModal(id)                L321-333 → MODIFY (title dynamic)
  submitUnitModal()                L335-359 → MODIFY (cascade-confirm Edit branch)
  openDeleteModal(id,name,childCount) L375-386 → signature UNCHANGED (cara panggil berubah)
  initTree()                       L192-231 → fetch GetOrganizationTree → buildTree → renderNode
  DOMContentLoaded listener        L401-468 → container event delegation (tambah .js-delete-trigger di sini)

_flatUnits item shape: { id, name, parentId, level, displayOrder, isActive, children[] }

GET /Admin/GetLevelLabels (Phase 340) → { "0":"Bagian", "1":"Unit", "2":"Sub-unit", ... }

PreviewEditCascade (Plan 01) JSON response:
  { nameChanged, parentChanged, affectedUsersCount, affectedMappingsCount, affectedKompetensiCount, affectedGuidanceCount }
  atau early-return { nameChanged:false, parentChanged:false } bila no-change.

renderNode DOM structure (Pitfall 2 — JANGAN ubah <li>/.drag-handle):
  <li class="tree-node" data-id="${node.id}">     ← data-id WAJIB tetap di <li>
    <div class="tree-row d-flex align-items-center gap-2">
      <i class="bi bi-grip-vertical drag-handle"></i>   ← .drag-handle WAJIB tetap
      ${chevron}
      <div class="org-node-icon ${levelClass}">...</div>
      <span class="tree-label">${escapeHtml(node.name)}</span>   ← sisip badge SETELAH ini
      ${childCount}
      ${badge}    ← badge Aktif/Nonaktif existing
      ${dropdown}

View existing anchors (line aktual):
  CSS palette L37-39 (.org-node-icon.level-0/1/2) — tambah .level-3/4/5 setelah L39
  card-header L114-122 (2 kolom flex) — tambah legend block
  unitModal body L141-155 (select#unitModalParent L150) — tambah path div setelah L153
  deleteModal L166-187 — analog struktur untuk cascade-confirm modal baru
  @section Scripts L189-196 — DOMContentLoaded L194 ganti ke async wrapper
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: orgTree.js — label fetch + pre-order DFS + escape + level cap + badge</name>
  <files>wwwroot/js/orgTree.js</files>
  <read_first>
    - 342-RESEARCH.md §Code Examples #1 (pre-order DFS + inactive), #4 (escape data-attr + delegation), #6 (level palette + badge), #7 (fetchLabels/getLabelForLevel) + Pitfall 2/3/4/7
    - 342-PATTERNS.md §"wwwroot/js/orgTree.js" B1 (pre-order DFS), B2 (escape), B3 (level cap + badge), B4 (label fetch)
    - 342-CONTEXT.md §D-01 (label fetch source), §D-03 (badge palette reuse), §Spec-locked items
    - wwwroot/js/orgTree.js L1-60 (helpers ajaxGet/escapeHtml/buildTree) + L118-178 (renderNode) + L275-303 (getDescendantIds/populateParentDropdown) + L400-468 (DOMContentLoaded delegation)
  </read_first>
  <action>
    Extend `wwwroot/js/orgTree.js` in-place. 4 perubahan.

    **Step 1 — Label fetch map (D-01, Pitfall 4).** Tambah di area module-level (dekat `let _flatUnits` L5):
    ```javascript
    let labelMap = null;
    async function fetchLabels() { if (!labelMap) labelMap = await ajaxGet('GetLevelLabels'); }
    function getLabelForLevel(level) { return (labelMap && labelMap[level]) || `Level ${level}`; }
    ```
    Lalu di AWAL `initTree()` (L205, sebelum `const flat = await ajaxGet('GetOrganizationTree')`): tambah `await fetchLabels();` agar labelMap ready sebelum renderNode. Guard `if (!labelMap)` sudah ada di fetchLabels (cukup fetch sekali; initTree dipanggil ulang post-CRUD).

    **Step 2 — Pre-order DFS dropdown + inactive visible (ORG-TREE-01, 03).** Tambah helper `flattenTreePreOrder` SEBELUM `populateParentDropdown` (L287):
    ```javascript
    function flattenTreePreOrder(roots) {
        const out = [];
        function walk(node, depth) {
            out.push({ ...node, depth });
            (node.children || []).forEach(c => walk(c, depth + 1));
        }
        roots.forEach(r => walk(r, 0));
        return out;
    }
    ```
    REPLACE body `populateParentDropdown` (L293-302, blok `.filter().sort().forEach()`) dengan:
    ```javascript
        const roots = buildTree(_flatUnits);
        flattenTreePreOrder(roots)
            .filter(u => !excludeIds.has(u.id))          // Pitfall 3: anti-circular (self + descendants)
            .forEach(u => {
                const indent = ' '.repeat(u.depth * 4);   // NBSP konsisten existing (A3)
                const suffix = u.isActive ? '' : ' (nonaktif)';  // ORG-TREE-03
                const opt = document.createElement('option');
                opt.value = u.id;
                opt.textContent = indent + u.name + suffix;
                if (!u.isActive) opt.style.color = '#999';
                select.appendChild(opt);
            });
    ```
    Pertahankan baris atas (L288-291: select, innerHTML reset, excludeIds dari getDescendantIds). HAPUS `.filter(u => u.isActive ...)` lama (Bug B + parent nonaktif disembunyikan).

    **Step 3 — Escape fix + level cap + badge (ORG-TREE-04, 05, 10) di renderNode (L118-178).**
    - L123 level cap: ganti `const levelClass = level <= 2 ? \`level-${level}\` : 'level-2';` → `const levelClass = 'level-' + (level <= 5 ? level : 5);`
    - L150 escape: ganti `<li><a class="dropdown-item text-danger" href="#" onclick="event.preventDefault(); openDeleteModal(${node.id}, '${escapeHtml(node.name)}', ${hasChildren ? node.children.length : 0})">...` menjadi:
      ```javascript
      <li><a class="dropdown-item text-danger js-delete-trigger" href="#"
          data-id="${node.id}" data-name="${escapeHtml(node.name)}"
          data-child-count="${hasChildren ? node.children.length : 0}"><i class="bi bi-trash me-2"></i>Hapus</a></li>
      ```
    - Badge tier (ORG-TREE-10): tambah SEBELUM return renderNode:
      ```javascript
      const tierLabel = getLabelForLevel(level);
      const tierBadge = `<span class="badge org-tier-badge level-${level <= 5 ? level : 5}">${escapeHtml(tierLabel)}</span>`;
      ```
      Sisip `${tierBadge}` di template return ANTARA `<span class="tree-label">...</span>` (L171) dan `${childCount}` (L172). JANGAN ubah `<li class="tree-node" data-id>` (L165) atau `.drag-handle` (L168) — Pitfall 2.

    **Step 4 — Delete event delegation listener (ORG-TREE-04).** Di dalam DOMContentLoaded block (dekat L406, tambahan listener pada `container`):
    ```javascript
    container.addEventListener('click', e => {
        const del = e.target.closest('.js-delete-trigger');
        if (!del) return;
        e.preventDefault();
        openDeleteModal(parseInt(del.dataset.id), del.dataset.name, parseInt(del.dataset.childCount));
    });
    ```
    `openDeleteModal(id, name, childCount)` signature L375 TIDAK berubah. Add/Edit/Toggle item L146-148 tetap inline (numeric `${node.id}`, aman).

    ALL UI copy Bahasa Indonesia (CLAUDE.md). JANGAN ubah ajaxGet/ajaxPost/escapeHtml/buildTree/initSortable/renderStats.

    **Step 5 — renderLegend helper (ORG-TREE-08, dapat juga ditaruh di Task 3 — taruh di sini agar 1 file).** Tambah:
    ```javascript
    function renderLegend() {
        const host = document.getElementById('org-legend');
        if (!host || !labelMap) return;
        const levels = Object.keys(labelMap).map(Number).sort((a,b)=>a-b);
        host.innerHTML = levels.map(lv =>
            `<span class="org-legend-item"><span class="org-legend-swatch level-${lv <= 5 ? lv : 5}"></span>${escapeHtml(labelMap[lv])}</span>`
        ).join('');
    }
    ```
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj --nologo --verbosity minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` PASS (view+JS bundled; build clean — JS bukan compile-checked, build gate untuk view ref)
    - `grep -c "function flattenTreePreOrder" wwwroot/js/orgTree.js` = 1
    - `grep -c "async function fetchLabels" wwwroot/js/orgTree.js` = 1
    - `grep -c "ajaxGet('GetLevelLabels')" wwwroot/js/orgTree.js` = 1
    - `grep -c "level <= 5 ? level : 5" wwwroot/js/orgTree.js` >= 2 (levelClass + badge)
    - `grep -c "js-delete-trigger" wwwroot/js/orgTree.js` >= 2 (markup + listener)
    - `grep -c "onclick=\"event.preventDefault(); openDeleteModal" wwwroot/js/orgTree.js` = 0 (inline escape bug hilang)
    - `grep -c "org-tier-badge" wwwroot/js/orgTree.js` = 1 (badge tier)
    - `grep -c "(nonaktif)" wwwroot/js/orgTree.js` = 1 (parent nonaktif suffix)
    - `grep -c "filter(u => u.isActive" wwwroot/js/orgTree.js` = 0 (filter isActive lama hilang dari dropdown)
    - `grep -c "drag-handle" wwwroot/js/orgTree.js` >= 2 (Pitfall 2 — .drag-handle preserved di renderNode + Sortable)
    - `grep -c "function renderLegend" wwwroot/js/orgTree.js` = 1 (legend renderer)
  </acceptance_criteria>
  <done>orgTree.js: pre-order DFS + inactive visible + escape data-attr + level cap 0-5 + tier badge + label fetch ready-before-render + renderLegend; .drag-handle/<li data-id> utuh; siap konsumsi labelMap di legend/title (Task 2/3).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: orgTree.js — modal title dynamic + path breadcrumb + cascade-confirm flow</name>
  <files>wwwroot/js/orgTree.js</files>
  <read_first>
    - 342-RESEARCH.md §Code Examples #3 (cascade-confirm submitUnitModal), #7 (setModalTitleForParent + renderModalPath) + Pitfall 6 (parentId null)
    - 342-PATTERNS.md §"wwwroot/js/orgTree.js" B5 (title + path), B6 (cascade flow) + §Shared Patterns (Bootstrap modal show/hide)
    - 342-CONTEXT.md §D-02 (Bootstrap modal cascade), §D-04 (always-call + modal-if-impact), §Claude's Discretion (path string)
    - Views/Admin/ManageOrgLevelLabels.cshtml (Phase 341 Bootstrap modal show/hide pattern — analog)
    - wwwroot/js/orgTree.js L307-359 (openAddModal/openEditModal/submitUnitModal) + L271 (findUnit)
  </read_first>
  <action>
    Extend `wwwroot/js/orgTree.js` in-place. 3 perubahan (depends Task 1 helper getLabelForLevel + findUnit existing).

    **Step 1 — Modal title dynamic + path breadcrumb helpers (ORG-TREE-09, 06).** Tambah 2 helper (dekat openAddModal L307):
    ```javascript
    function setModalTitleForParent(parentIdVal) {
        const parent = parentIdVal ? findUnit(parseInt(parentIdVal)) : null;
        const childLevel = parent ? parent.level + 1 : 0;
        const verb = document.getElementById('unitModalId').value ? 'Edit' : 'Tambah';
        document.getElementById('unitModalLabel').textContent = `${verb} ${getLabelForLevel(childLevel)}`;
    }
    function renderModalPath(parentIdVal) {
        const el = document.getElementById('unitModalPath');
        if (!parentIdVal) { el.textContent = ''; return; }
        const chain = [];
        let cur = findUnit(parseInt(parentIdVal));
        while (cur) { chain.unshift(cur.name); cur = cur.parentId ? findUnit(cur.parentId) : null; }
        el.textContent = 'Path: ' + chain.join(' → ') + ' → (unit baru di sini)';
    }
    ```
    Di `openAddModal` (L310) ganti hardcoded `'Tambah Unit'` → panggil `setModalTitleForParent(parentId || null)` SETELAH `populateParentDropdown`; lalu `renderModalPath(parentId || null)`. Di `openEditModal` (L326) ganti `'Edit Unit'` → setelah set `unitModalParent.value` (L331) panggil `setModalTitleForParent(unit.parentId || '')` + `renderModalPath(unit.parentId || '')`.

    Wire listener `select#unitModalParent` change SEKALI on-load (di DOMContentLoaded block, container guard sudah lewat):
    ```javascript
    const parentSelect = document.getElementById('unitModalParent');
    if (parentSelect) {
        parentSelect.addEventListener('change', e => {
            setModalTitleForParent(e.target.value);
            renderModalPath(e.target.value);
        });
    }
    ```

    **Step 2 — Cascade-confirm helper (ORG-TREE-07, D-02).** Tambah helper `showCascadeConfirm(pv)` (return Promise<bool>). Isi 4 baris breakdown ke modal markup baru (id `cascadeConfirmModal` dari Task 3 Plan view), show modal, resolve `true` saat [Lanjut Simpan], `false` saat [Batal]/dismiss. Pola replikasi Phase 341 (`new bootstrap.Modal(el).show()` / `getInstance(el).hide()`):
    ```javascript
    function showCascadeConfirm(pv) {
        return new Promise(resolve => {
            document.getElementById('cascadeUsers').textContent = pv.affectedUsersCount || 0;
            document.getElementById('cascadeMappings').textContent = pv.affectedMappingsCount || 0;
            document.getElementById('cascadeKompetensi').textContent = pv.affectedKompetensiCount || 0;
            document.getElementById('cascadeGuidance').textContent = pv.affectedGuidanceCount || 0;
            const modalEl = document.getElementById('cascadeConfirmModal');
            const modal = new bootstrap.Modal(modalEl);
            const btnProceed = document.getElementById('cascadeConfirmProceed');
            let settled = false;
            const onProceed = () => { settled = true; modal.hide(); resolve(true); };
            const onHidden = () => { btnProceed.removeEventListener('click', onProceed); modalEl.removeEventListener('hidden.bs.modal', onHidden); if (!settled) resolve(false); };
            btnProceed.addEventListener('click', onProceed);
            modalEl.addEventListener('hidden.bs.modal', onHidden);
            modal.show();
        });
    }
    ```

    **Step 3 — submitUnitModal Edit branch (D-04 always-call).** MODIFY `submitUnitModal` (L335-359): SETELAH `const isEdit = id !== '';` (L345), SEBELUM bangun url/data, tambah:
    ```javascript
    if (isEdit) {
        const pv = await ajaxPost('PreviewEditCascade', { id, name, parentId });
        const total = (pv.affectedUsersCount || 0) + (pv.affectedMappingsCount || 0)
                    + (pv.affectedKompetensiCount || 0) + (pv.affectedGuidanceCount || 0);
        if (total > 0) {
            const proceed = await showCascadeConfirm(pv);
            if (!proceed) return;   // user Batal — abort sebelum EditOrganizationUnit
        }
    }
    ```
    Sisanya (POST EditOrganizationUnit/AddOrganizationUnit + showToast + initTree, L346-358) TETAP. JANGAN compute parentChanged di client (Pitfall 6 — server authoritative).

    ALL copy Bahasa Indonesia. Path string "Path: ... → (unit baru di sini)" per CONTEXT Claude's discretion.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj --nologo --verbosity minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` PASS
    - `grep -c "function setModalTitleForParent" wwwroot/js/orgTree.js` = 1
    - `grep -c "function renderModalPath" wwwroot/js/orgTree.js` = 1
    - `grep -c "function showCascadeConfirm" wwwroot/js/orgTree.js` = 1
    - `grep -c "ajaxPost('PreviewEditCascade'" wwwroot/js/orgTree.js` = 1
    - `grep -c "unit baru di sini" wwwroot/js/orgTree.js` = 1 (path breadcrumb string)
    - `grep -c "cascadeConfirmModal" wwwroot/js/orgTree.js` = 1
    - `grep -c "'Tambah Unit'" wwwroot/js/orgTree.js` = 0 (hardcoded title hilang)
    - `grep -c "'Edit Unit'" wwwroot/js/orgTree.js` = 0
    - `grep -c "getLabelForLevel(childLevel)" wwwroot/js/orgTree.js` = 1 (title dynamic dari labelMap)
  </acceptance_criteria>
  <done>orgTree.js: modal title dynamic per tier + path breadcrumb real-time + cascade-confirm Edit flow (always-call PreviewEditCascade, modal-if-impact, abort on Batal); konsumsi Plan 01 endpoint; modal markup di-provide Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ManageOrganization.cshtml — CSS palette + legend + path div + cascade modal + async init</name>
  <files>Views/Admin/ManageOrganization.cshtml</files>
  <read_first>
    - 342-RESEARCH.md §Code Examples #6 (CSS level-3/4/5), #8 (renderLegend) + Pitfall 4 (init ordering) + §Open Questions Q2 (legend placement)
    - 342-PATTERNS.md §"Views/Admin/ManageOrganization.cshtml" C1 (CSS), C2 (legend), C3 (path div), C4 (cascade modal)
    - 342-CONTEXT.md §D-03 (palette reuse), §Specifics (legend layout + cascade modal copy), §Claude's Discretion (badge/modal aria)
    - Views/Admin/ManageOrganization.cshtml L37-39 (CSS palette) + L114-122 (card-header) + L141-155 (unitModal body) + L166-187 (deleteModal) + L189-196 (scripts)
    - Views/Admin/ManageOrgLevelLabels.cshtml (Phase 341 modal aria conventions)
  </read_first>
  <action>
    Extend `Views/Admin/ManageOrganization.cshtml` in-place. 5 perubahan. ALL copy Bahasa Indonesia.

    **Step 1 — CSS palette extend + badge/swatch (ORG-TREE-05, 10, 08).** Setelah L39 (`.org-node-icon.level-2`), tambah:
    ```css
    .org-node-icon.level-3 { background: rgba(25,135,84,0.1);  color: #198754; }
    .org-node-icon.level-4 { background: rgba(255,193,7,0.1);  color: #b45309; }
    .org-node-icon.level-5 { background: rgba(220,53,69,0.1);  color: #dc3545; }

    /* Tier badge (D-03 reuse level palette) */
    .org-tier-badge { font-size: 0.68rem; font-weight: 600; }
    .org-tier-badge.level-0 { background: rgba(13,110,253,0.12); color: #0d6efd; }
    .org-tier-badge.level-1 { background: rgba(102,126,234,0.12); color: #667eea; }
    .org-tier-badge.level-2 { background: rgba(13,202,240,0.12); color: #0dcaf0; }
    .org-tier-badge.level-3 { background: rgba(25,135,84,0.12);  color: #198754; }
    .org-tier-badge.level-4 { background: rgba(255,193,7,0.12);  color: #b45309; }
    .org-tier-badge.level-5 { background: rgba(220,53,69,0.12);  color: #dc3545; }

    /* Legend */
    .org-legend { display: flex; flex-wrap: wrap; gap: 1rem; padding: 0.5rem 1rem; border-top: 1px solid #f0f0f0; }
    .org-legend-item { display: inline-flex; align-items: center; gap: 0.35rem; font-size: 0.78rem; color: #495057; }
    .org-legend-swatch { width: 12px; height: 12px; border-radius: 3px; display: inline-block; }
    .org-legend-swatch.level-0 { background: #0d6efd; } .org-legend-swatch.level-1 { background: #667eea; }
    .org-legend-swatch.level-2 { background: #0dcaf0; } .org-legend-swatch.level-3 { background: #198754; }
    .org-legend-swatch.level-4 { background: #b45309; } .org-legend-swatch.level-5 { background: #dc3545; }
    ```

    **Step 2 — Legend block (ORG-TREE-08).** Setelah card-header div tutup (L122 `</div>` penutup card-header), SEBELUM `<div class="card-body">` (L123), tambah baris ke-3:
    ```html
    <div class="org-legend" id="org-legend"><!-- populated by renderLegend() --></div>
    ```

    **Step 3 — Path breadcrumb div (ORG-TREE-06).** Di unitModal body, setelah select `unitModalParent` tutup `</select></div>` (sekitar L153, dalam blok `.mb-3` Induk), tambah:
    ```html
    <div class="text-muted small mt-1" id="unitModalPath"></div>
    ```

    **Step 4 — Cascade-confirm modal markup (ORG-TREE-07, D-02).** Setelah `deleteModal` tutup (L187 `</div>` penutup modal deleteModal), tambah modal baru (analog struktur deleteModal + aria Phase 341):
    ```html
    <!-- Modal Konfirmasi Dampak Cascade -->
    <div class="modal fade" id="cascadeConfirmModal" tabindex="-1" aria-labelledby="cascadeConfirmLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-warning">
                    <h5 class="modal-title" id="cascadeConfirmLabel">Konfirmasi Perubahan</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Tutup"></button>
                </div>
                <div class="modal-body">
                    <p class="mb-2">Perubahan ini akan mempengaruhi:</p>
                    <ul class="mb-2">
                        <li><strong id="cascadeUsers">0</strong> user</li>
                        <li><strong id="cascadeMappings">0</strong> mapping coach-coachee</li>
                        <li><strong id="cascadeKompetensi">0</strong> kompetensi PROTON</li>
                        <li><strong id="cascadeGuidance">0</strong> file panduan</li>
                    </ul>
                    <p class="mb-0">Lanjutkan?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
                    <button type="button" class="btn btn-warning" id="cascadeConfirmProceed">
                        <i class="bi bi-check-lg me-1"></i>Lanjut Simpan
                    </button>
                </div>
            </div>
        </div>
    </div>
    ```

    **Step 5 — Async init wrapper (Pitfall 4, ORG-TREE-08).** Ganti `@section Scripts` callback L193-195:
    ```html
    <script>
        document.addEventListener('DOMContentLoaded', async () => {
            await fetchLabels();
            await initTree();
            renderLegend();
        });
    </script>
    ```
    (`renderLegend` didefinisikan di orgTree.js Task 1 Step 5.) JANGAN ubah SortableJS CDN script (L190), shared-toast (L191), orgTree.js include (L192).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj --nologo --verbosity minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` PASS
    - `grep -c "org-node-icon.level-3" Views/Admin/ManageOrganization.cshtml` = 1
    - `grep -c "org-node-icon.level-5" Views/Admin/ManageOrganization.cshtml` = 1
    - `grep -c "org-tier-badge.level-5" Views/Admin/ManageOrganization.cshtml` = 1
    - `grep -c "id=\"org-legend\"" Views/Admin/ManageOrganization.cshtml` = 1
    - `grep -c "id=\"unitModalPath\"" Views/Admin/ManageOrganization.cshtml` = 1
    - `grep -c "id=\"cascadeConfirmModal\"" Views/Admin/ManageOrganization.cshtml` = 1
    - `grep -c "id=\"cascadeConfirmProceed\"" Views/Admin/ManageOrganization.cshtml` = 1
    - `grep -c "mapping coach-coachee" Views/Admin/ManageOrganization.cshtml` = 1 (cascade copy BI)
    - `grep -c "await fetchLabels()" Views/Admin/ManageOrganization.cshtml` = 1 (init ordering Pitfall 4)
  </acceptance_criteria>
  <done>View: CSS palette 0-5 + tier-badge/legend-swatch + legend block + path div + cascade-confirm modal + async init (fetchLabels→initTree→renderLegend) wired; markup id konsisten dengan handler Task 2; siap browser smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser smoke verify — page render + 9 visual/UX requirement + regression drag/toggle</name>
  <files>(none — verification-only checkpoint, no file change)</files>
  <action>Manual browser smoke — lihat <how-to-verify> di bawah. TIDAK ada perubahan kode di task ini; checkpoint mengkonfirmasi yang tidak bisa di-cover compile gate: render DOM, label fetch ordering (Pitfall 4), interaksi modal/dropdown, dan regression SortableJS drag (Pitfall 2). Jalankan `dotnet build` + `dotnet run` dulu, lalu eksekusi 10 cek di bawah.</action>
  <verify>
    <automated>MANUAL — browser smoke 10-check di http://localhost:5277/Admin/ManageOrganization (lihat how-to-verify). Compile gate `dotnet build HcPortal.csproj` sudah PASS di Task 1-3.</automated>
  </verify>
  <done>User ketik "approved" setelah 10 cek lulus dan didokumentasikan di SUMMARY; deviasi dicatat bila ada.</done>
  <what-built>
    Phase 342 frontend lengkap di `/Admin/ManageOrganization`: pre-order DFS dropdown, parent nonaktif visible, escape data-attribute, level palette 0-5, path breadcrumb, cascade-confirm modal (konsumsi PreviewEditCascade Plan 01), legend, modal title dynamic, tier badge per row. Init ordering label-before-render (Pitfall 4). Drag-reorder/toggle/delete existing dipertahankan (Pitfall 2).
  </what-built>
  <how-to-verify>
    Prasyarat: `dotnet build` PASS + `dotnet run` (login Admin: admin@pertamina.com per dev credentials) → buka `http://localhost:5277/Admin/ManageOrganization`. Buka DevTools Console (cek ZERO error).

    1. **Render + label fetch (ORG-TREE-08/10):** Tree muncul. Legend di card-header tampil swatch warna + label tier (▣ Bagian ▣ Unit ▣ Sub-unit). Tiap row punya badge tier (mis. "Bagian" / "Unit") warna sesuai level. Badge TIDAK "Level 0" di first paint (label ready sebelum render).
    2. **Pre-order DFS dropdown (ORG-TREE-01):** Klik kebab → Tambah Sub-unit (atau Edit). Dropdown Induk urut parent → keturunannya → sibling berikutnya (indent hierarki), BUKAN flat per-level.
    3. **Parent nonaktif (ORG-TREE-03):** Nonaktifkan 1 parent (Toggle), buka modal lagi → parent itu MASIH muncul di dropdown dengan suffix " (nonaktif)" warna grey.
    4. **Modal title dynamic (ORG-TREE-09):** Tambah Sub-unit di bawah Bagian → title "Tambah Unit"; di bawah Unit → "Tambah Sub-unit"; root → "Tambah Bagian". Fallback "Level N" untuk tier tanpa label.
    5. **Path breadcrumb (ORG-TREE-06):** Di modal, pilih induk dari dropdown → muncul "Path: RFCC → LPG Treating → (unit baru di sini)" real-time, update saat ganti pilihan.
    6. **Level palette (ORG-TREE-05):** Bila ada node level 3+ → warna icon berbeda (hijau/kuning/merah), tidak sama dengan level 2.
    7. **Escape fix (ORG-TREE-04):** Buat/rename unit dengan nama mengandung kutip (mis. `O'Brien Unit`) → klik Hapus → modal hapus tampil nama benar tanpa JS error (data-attribute, bukan string break).
    8. **Cascade-confirm (ORG-TREE-07):** Edit unit yang punya >0 user (rename) → klik Simpan → modal "Konfirmasi Perubahan" tampil 4-line count (N user / N mapping / N kompetensi / N file panduan). [Batal] → abort tanpa simpan. [Lanjut Simpan] → tersimpan + toast sukses + tree refresh. Edit unit tanpa perubahan / 0 impact → langsung simpan tanpa modal (early-return D-04).
    9. **Dup-name per-parent (ORG-TREE-02, dari Plan 01):** Tambah unit "Operations" di Bagian A → sukses; tambah "Operations" lagi di Bagian A → ditolak; tambah "Operations" di Bagian B → sukses.
    10. **Regression (Pitfall 2):** Drag-reorder sibling masih jalan (toast "urutan diubah"). Toggle aktif/nonaktif jalan. Hapus unit jalan. Tidak ada console error.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila 10 cek lulus, atau jelaskan isu spesifik (nomor cek + gejala) untuk revisi.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → renderNode | Nama unit (user-controlled) di-render ke HTML — XSS surface via escape |
| Browser → Controller POST | submitUnitModal kirim id/name/parentId ke PreviewEditCascade + EditOrganizationUnit (CSRF surface) |
| GetLevelLabels fetch → JS render | Label dari endpoint Phase 340 di-render ke legend/badge/title |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-342-07 | Tampering | renderNode nama unit dengan kutip/`<script>` (XSS) | mitigate | `escapeHtml(node.name)` di `data-name` value + `tree-label` + badge (Task 1); event delegation hapus inline onclick string-interp (Bug #3 fix ORG-TREE-04) |
| T-342-08 | Tampering | submitUnitModal POST (CSRF) | mitigate | `ajaxPost` append `__RequestVerificationToken` (existing L15) + server `[ValidateAntiForgeryToken]` di PreviewEditCascade/EditOrganizationUnit; view `@Html.AntiForgeryToken()` L124 preserved |
| T-342-09 | Tampering | Client pilih self/descendant sebagai induk (circular) | mitigate | Dropdown exclude `getDescendantIds + self` (Pitfall 3, Task 1); server hard-reject `IsDescendantAsync`/`parentId == id` (Plan 01 EditOrganizationUnit L160/L168) — defense-in-depth |
| T-342-10 | Information Disclosure | Legend/badge expose tier label (org structure) | accept | Label tier = public display info, zero PII; endpoint GetLevelLabels any-auth by-design (Phase 340 D-03) |
| T-342-11 | Tampering | Client bypass cascade-confirm (skip modal) | accept | Cascade-confirm = UX warning; mutasi tetap server-side EditOrganizationUnit (authoritative); bypass modal hanya kehilangan warning, tidak bypass validasi server |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` PASS (Build succeeded, 0 Error)
- grep acceptance criteria Task 1-3 lulus (helper baru hadir, escape bug + filter isActive + hardcoded title hilang)
- Browser smoke Task 4: 10 cek lulus (login Admin lokal, console zero error)
- Regression: drag-reorder/toggle/delete jalan (Pitfall 2 — .drag-handle/<li data-id> utuh)
</verification>

<success_criteria>
1. **Build pass:** `dotnet build HcPortal.csproj` exits 0, "Build succeeded".
2. **9 visual/UX requirement (ORG-TREE-01,03,04,05,06,07-frontend,08,09,10):** Browser smoke Task 4 lulus.
3. **Pre-order DFS + inactive visible (ORG-TREE-01/03):** flattenTreePreOrder + suffix nonaktif; filter isActive lama hilang.
4. **Escape fix (ORG-TREE-04):** data-attribute + event delegation; inline onclick string-interp openDeleteModal hilang.
5. **Level palette 0-5 (ORG-TREE-05):** CSS + JS levelClass cap 5.
6. **Cascade-confirm (ORG-TREE-07):** Always-call PreviewEditCascade, modal-if-impact, abort-on-Batal; konsumsi Plan 01.
7. **Label-sourced UX (ORG-TREE-08/09/10):** Legend + title + badge dari GetLevelLabels; label ready sebelum render (Pitfall 4).
8. **Regression-safe (Pitfall 2):** Drag-reorder/toggle/delete jalan.
9. **Threat T-342-07..09 mitigated:** escape + CSRF + anti-circular verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/342-manageorganization-page-fixes/342-02-SUMMARY.md` capturing:
- Final commit hash range untuk 4 task
- LoC delta orgTree.js (before ~469 → after ~540) + ManageOrganization.cshtml (before ~197 → after ~250)
- Browser smoke result: 10-check pass/fail evidence (screenshot atau deskripsi per cek)
- Konfirmasi regression drag-reorder/toggle/delete jalan (Pitfall 2)
- Any deviation dari Code Examples (path string, badge class, modal aria — Claude's discretion choices recorded)
- Reference Plan 03 (xUnit backend + manual UAT formal)
</output>
