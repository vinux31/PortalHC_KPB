---
phase: 343-integrasi-app-wide
plan: 03
type: execute
wave: 2
depends_on: ["343-01"]
files_modified:
  - Views/ProtonData/Index.cshtml
  - Views/ProtonData/Override.cshtml
  - Views/Admin/CoachCoacheeMapping.cshtml
  - Views/Admin/CreateWorker.cshtml
  - Views/Admin/EditWorker.cshtml
  - Views/Admin/ManageWorkers.cshtml
  - Views/Admin/WorkerDetail.cshtml
  - Views/Admin/RenewalCertificate.cshtml
  - Views/Admin/Shared/_TrainingRecordsTab.cshtml
  - Views/Admin/EditAssessment.cshtml
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/CpdpUpload.cshtml
  - Views/Admin/KkjUpload.cshtml
  - Views/Admin/CpdpFiles.cshtml
  - Views/Admin/KkjMatrix.cshtml
  - Views/Account/Profile.cshtml
  - Views/Account/Settings.cshtml
autonomous: true
requirements: [ORG-INTEG-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Form placeholder + filter + table header + detail field + combined-phrase 'X Penugasan' + selector label tier di ProtonData/* dan Admin/* dan Account/* menampilkan @OrgLabels.GetLabel(N)"
    - "Element id, JS var/func (addBagian/onBagianChange), toast literal, alert literal, property access, nama data, import-doc field-table di file ini TIDAK berubah"
    - "dotnet build hijau (semua @OrgLabels resolve, termasuk partial _TrainingRecordsTab inherit @inject)"
  artifacts:
    - path: "Views/Admin/CoachCoacheeMapping.cshtml"
      provides: "Combined-phrase 'X Penugasan' th + form label dinamis"
      contains: "@OrgLabels.GetLabel(0) Penugasan"
    - path: "Views/Admin/ManageWorkers.cshtml"
      provides: "Filter + table header tier dinamis"
      contains: "@OrgLabels.GetLabel(0)"
    - path: "Views/Account/Profile.cshtml"
      provides: "Profile field label tier dinamis"
      contains: "@OrgLabels.GetLabel(0)"
  key_links:
    - from: "Views/Admin/ManageWorkers.cshtml"
      to: "OrgLabels.GetLabel"
      via: "@inject inherited dari Views/_ViewImports.cshtml (Plan 01)"
      pattern: "@OrgLabels\\.GetLabel\\([012]\\)"
---

<objective>
Terapkan REPLACE swap (hardcoded "Bagian"/"Unit" → `@OrgLabels.GetLabel(N)`) untuk area **ProtonData** + **Admin** + **Account** mengikuti tabel REPLACE otoritatif `343-RESEARCH.md` L158-209 + keputusan AMBIGUOUS (343-AUDIT.md / RESEARCH L213-226). Murni visible-text swap; id/JS/toast/alert/property/data tetap literal.

Purpose: Pemenuhan ORG-INTEG-01 untuk worker form (Admin), coaching mapping, ProtonData form, dan profil. Bersama Plan 02 (CMP/CDP) memenuhi SC2 (rename ≥3 page: Worker form + CMP + CDP) + SC3 (no hardcode tersisa di area target). Catatan: 4 folder Views kosong (Worker/CoachMapping/Renewal/DocumentAdmin) — form-nya ada di Views/Admin/* (sudah dicakup di sini).

Output: 17 file view ProtonData/Admin/Account di-swap; build hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/343-integrasi-app-wide/343-RESEARCH.md
@.planning/phases/343-integrasi-app-wide/343-PATTERNS.md
@.planning/phases/343-integrasi-app-wide/343-VALIDATION.md
@.planning/phases/343-integrasi-app-wide/343-AUDIT.md

<interfaces>
<!-- Kontrak swap — sudah disediakan. -->

@inject SUDAH terpasang global oleh Plan 01 (Views/_ViewImports.cshtml). `@OrgLabels.GetLabel(N)` available di semua view + partial (termasuk Views/Admin/Shared/_TrainingRecordsTab.cshtml — inherit, JANGAN @inject ulang).

Tier mapping: GetLabel(0)=Bagian, (1)=Unit, (2)=Sub-unit. Semua REPLACE row di area ini = tier 0/1.

3 pola swap kanonik (343-PATTERNS.md):
- Pattern A (visible-text-only): `>Bagian<` → `>@OrgLabels.GetLabel(0)<`.
- Pattern B (combined-text): `Semua Bagian` / `-- Pilih Unit --` / `— Pilih Bagian —` / `Bagian Penugasan` / `Lainnya (Tanpa Bagian)` → label dinamis + literal prefix/suffix.
- Pattern C (detail field): hanya sel/elemen LABEL, BUKAN sel nilai data di sebelahnya.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap ProtonData (Index + Override) + CoachCoacheeMapping combined-phrase</name>
  <files>Views/ProtonData/Index.cshtml, Views/ProtonData/Override.cshtml, Views/Admin/CoachCoacheeMapping.cshtml</files>
  <read_first>
    - 3 file yang dimodifikasi
    - 343-RESEARCH.md REPLACE table L158-177 (ProtonData L158-169, CoachCoacheeMapping L170-177)
    - 343-PATTERNS.md §PlanIdp PLACEHOLDER (L150-206) + §CoachCoacheeMapping CANONICAL COMBINED-TEXT (L210-236)
    - 343-AUDIT.md §4 SKIP whitelist (id/JS/alert literal)
  </read_first>
  <action>
    Swap per RESEARCH.md L158-177:

    **ProtonData/Index.cshtml** (L158-165): L79 `<label>Bagian</label>`(0); L81 `-- Pilih Bagian --` → `-- Pilih @OrgLabels.GetLabel(0) --`; L85 `<label>Unit</label>`(1); L87 `-- Pilih Unit --` → `-- Pilih @OrgLabels.GetLabel(1) --`; L230 `<label>Bagian</label>`(0, guidance form); L232 `-- Pilih Bagian --`(0); L236 `<label>Unit</label>`(1, guidance); L238 `-- Pilih Unit --`(1). JANGAN sentuh `id="silabusBagian"` dst. **SKIP ImportSilabus.cshtml:205-206 `<td>Bagian</td>` import-doc** (BUKAN file ini, tapi ingat: ProtonData/ImportSilabus = SKIP, bukan target).

    **ProtonData/Override.cshtml** (L166-169): L29 `<label>Bagian</label>`(0); L31 `-- Pilih Bagian --`(0); L35 `<label>Unit</label>`(1); L37 `-- Pilih Unit --`(1). JANGAN sentuh `id="overrideBagian"` dst.

    **Admin/CoachCoacheeMapping.cshtml** combined-phrase "X Penugasan" (L170-177; verified lines 235/236/435/445/503/513): swap kata tier SAJA, suffix "Penugasan" + `<span class="text-danger">*</span>` TETAP:
    - L235 `<th>Bagian Penugasan</th>` → `<th>@OrgLabels.GetLabel(0) Penugasan</th>`(0)
    - L236 `<th>Unit Penugasan</th>` → `<th>@OrgLabels.GetLabel(1) Penugasan</th>`(1)
    - L435 `<label ...>Bagian Penugasan <span...>*</span></label>` → `@OrgLabels.GetLabel(0) Penugasan <span...>*</span>`(0)
    - L437 `<option>— Pilih Bagian —</option>` → `— Pilih @OrgLabels.GetLabel(0) —`(0, em-dash literal)
    - L445 `<label ...>Unit Penugasan <span...>*</span></label>` → `@OrgLabels.GetLabel(1) Penugasan ...`(1)
    - L503 `<label ...>Bagian Penugasan ...</label>`(0, modal ke-2)
    - L505 `<option>— Pilih Bagian —</option>`(0, modal ke-2)
    - L513 `<label ...>Unit Penugasan ...</label>`(1, modal ke-2)
    **KRITIS SKIP — JS literal (verified L723/724/805/806):** `alert('Pilih Bagian Penugasan.')` + `alert('Pilih Unit Penugasan.')` = JS string → JANGAN ganti. Juga JANGAN sentuh `assignmentSection`/`assignmentUnit` JS var, `name=`/`id=` select.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/CoachCoacheeMapping.cshtml` contains `@OrgLabels.GetLabel(0) Penugasan` AND `@OrgLabels.GetLabel(1) Penugasan`
    - `Views/Admin/CoachCoacheeMapping.cshtml` does NOT contain `<th>Bagian Penugasan</th>` AND does NOT contain `<th>Unit Penugasan</th>`
    - `Views/Admin/CoachCoacheeMapping.cshtml` STILL contains `alert('Pilih Bagian Penugasan.')` (SKIP guard — JS literal untouched)
    - `Views/ProtonData/Index.cshtml` contains `-- Pilih @OrgLabels.GetLabel(0) --` AND does NOT contain `-- Pilih Bagian --` AND does NOT contain `-- Pilih Unit --`
    - `Views/ProtonData/Override.cshtml` contains `@OrgLabels.GetLabel(1)` AND does NOT contain `-- Pilih Bagian --`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>ProtonData form + CoachCoacheeMapping combined-phrase label dinamis; JS alert/var/id literal; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Swap Admin worker views (CreateWorker + EditWorker + ManageWorkers + WorkerDetail + RenewalCertificate + _TrainingRecordsTab partial)</name>
  <files>Views/Admin/CreateWorker.cshtml, Views/Admin/EditWorker.cshtml, Views/Admin/ManageWorkers.cshtml, Views/Admin/WorkerDetail.cshtml, Views/Admin/RenewalCertificate.cshtml, Views/Admin/Shared/_TrainingRecordsTab.cshtml</files>
  <read_first>
    - 6 file yang dimodifikasi
    - 343-RESEARCH.md REPLACE table L178-198 (CreateWorker L178-179, EditWorker L180-181, ManageWorkers L182-187, WorkerDetail L188-189, RenewalCertificate L190-193, _TrainingRecordsTab L194-198)
    - 343-PATTERNS.md §detail/definition-list (L240-262) + §Pattern D partial inheritance (L327-330) + sibling table L280-284
    - 343-AUDIT.md §4 SKIP whitelist
  </read_first>
  <action>
    Swap per RESEARCH.md L178-198:

    **CreateWorker.cshtml**: L116 `-- Pilih Bagian --`(0); L122 `-- Pilih Unit --`(1). NB: CreateWorker punya `@inject ...IConfiguration Config` per-view existing — JANGAN hapus/ubah (itu service lain).
    **EditWorker.cshtml**: L121 `-- Pilih Bagian --`(0); L127 `-- Pilih Unit --`(1).
    **ManageWorkers.cshtml**: L132 `<label>Bagian</label>`(0); L134 `<option>Semua Bagian</option>`(0); L152 `<label>Unit</label>`(1); L154 `<option>Semua Unit</option>`(1); L226 `<th>Bagian</th>`(0); L227 `<th>Unit</th>`(1).
    **WorkerDetail.cshtml** (detail field — Pattern C): L89 `<td class="text-muted">Bagian</td>` → `<td class="text-muted">@OrgLabels.GetLabel(0)</td>`(0); L102 `<td class="text-muted">Unit</td>`(1). **KRITIS: sel NILAI di sebelah kanan (mis. `<td>@w.Section</td>`) = data, JANGAN sentuh.**
    **RenewalCertificate.cshtml**: L55 `<label>Bagian</label>`(0); L57 `<option>Semua Bagian</option>`(0); L65 `<label>Unit</label>`(1); L67 `<option>Semua Unit</option>`(1).
    **_TrainingRecordsTab.cshtml** (partial — inherit @inject, JANGAN re-inject): L37 `<label>Bagian</label>`(0); L45 `<option>Semua Bagian</option>`(0); L87 `<label>Unit</label>`(1); L94 `<option>Semua Unit</option>`(1); L194 `<th>Unit</th>`(1).

    JANGAN sentuh: element id/`for`, JS var, `@w.Section`/`@Model.Unit`/property render (sel nilai), nama data. Line numbers panduan — verifikasi konteks aktual sebelum edit.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/ManageWorkers.cshtml` contains `@OrgLabels.GetLabel(0)` AND `@OrgLabels.GetLabel(1)`
    - `Views/Admin/ManageWorkers.cshtml` does NOT contain `<th>Bagian</th>` AND does NOT contain `<th>Unit</th>` AND does NOT contain `Semua Bagian` AND does NOT contain `Semua Unit`
    - `Views/Admin/WorkerDetail.cshtml` contains `<td class="text-muted">@OrgLabels.GetLabel(0)</td>` AND does NOT contain `<td class="text-muted">Bagian</td>`
    - `Views/Admin/CreateWorker.cshtml` contains `-- Pilih @OrgLabels.GetLabel(0) --` AND does NOT contain `-- Pilih Bagian --`
    - `Views/Admin/CreateWorker.cshtml` STILL contains `@inject` with `IConfiguration` (per-view service untouched)
    - `Views/Admin/Shared/_TrainingRecordsTab.cshtml` contains `@OrgLabels.GetLabel(1)` AND does NOT contain `Semua Unit` AND does NOT contain `<th>Unit</th>`
    - `Views/Admin/Shared/_TrainingRecordsTab.cshtml` does NOT contain a duplicate `@inject ... IOrgLabelService` (inherit from _ViewImports)
    - `Views/Admin/RenewalCertificate.cshtml` does NOT contain `Semua Bagian`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>6 file worker/renewal/training-tab label dinamis; sel nilai data + id + per-view IConfiguration untouched; build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Swap Admin assessment/upload + AMBIGUOUS resolutions + Account views</name>
  <files>Views/Admin/EditAssessment.cshtml, Views/Admin/CreateAssessment.cshtml, Views/Admin/CpdpUpload.cshtml, Views/Admin/KkjUpload.cshtml, Views/Admin/CpdpFiles.cshtml, Views/Admin/KkjMatrix.cshtml, Views/Account/Profile.cshtml, Views/Account/Settings.cshtml</files>
  <read_first>
    - 8 file yang dimodifikasi
    - 343-RESEARCH.md REPLACE table L199-209 + AMBIGUOUS table L213-226 (CpdpFiles/KkjMatrix L217-218, Lainnya-Tanpa-Bagian L224-225)
    - 343-PATTERNS.md §AMBIGUOUS (L364-372) + §detail field Account (L240-262)
    - 343-AUDIT.md (Plan 01) §3 AMBIGUOUS keputusan FINAL + §4 SKIP whitelist
  </read_first>
  <action>
    Swap per RESEARCH.md L199-209 + keputusan AMBIGUOUS (343-AUDIT.md §3):

    **EditAssessment.cshtml**: L522 `<th>Bagian</th>`(0); L598 `<option>Semua Bagian</option>` → `Semua @OrgLabels.GetLabel(0)`(0); L606 `<option>Lainnya (Tanpa Bagian)</option>` → `Lainnya (Tanpa @OrgLabels.GetLabel(0))`(0, **AMBIGUOUS→REPLACE default**).
    **CreateAssessment.cshtml**: L264 `<option>Semua Bagian</option>`(0); L272 `<option>Lainnya (Tanpa Bagian)</option>` → `Lainnya (Tanpa @OrgLabels.GetLabel(0))`(0, **AMBIGUOUS→REPLACE**).
    **CpdpUpload.cshtml**: L39 selector label `Pilih Bagian` → label text jadi `Pilih @OrgLabels.GetLabel(0)`(0); L42 `<option>-- Pilih Bagian --</option>` → `-- Pilih @OrgLabels.GetLabel(0) --`(0).
    **KkjUpload.cshtml** (twin): L39(0); L42(0) — sama CpdpUpload.

    **CpdpFiles.cshtml** (AMBIGUOUS→REPLACE button text, SKIP JS/toast): L64 button `Tambah Bagian` → `Tambah @OrgLabels.GetLabel(0)`(0); L87 button `Hapus Bagian` → `Hapus @OrgLabels.GetLabel(0)`(0). **SKIP: JS func `addBagian()`/`deleteBagian()` + toast `'Bagian berhasil dihapus'` (L172/223/241)** — JANGAN ganti.
    **KkjMatrix.cshtml** (twin): L64 `Tambah Bagian`(0); L87 `Hapus Bagian`(0). SKIP JS func + toast sama.

    **Account/Profile.cshtml** (detail field — Pattern C): L78 `<div class="...">Bagian</div>` → `>@OrgLabels.GetLabel(0)<`(0); L84 `<div ...>Unit</div>` → `>@OrgLabels.GetLabel(1)<`(1). KRITIS: sel NILAI (`@Model.Section`/`@Model.Unit`) JANGAN sentuh.
    **Account/Settings.cshtml**: L96 `<label>Bagian</label>`(0); L114 `<label>Unit</label>`(1).

    JANGAN sentuh: JS func `addBagian/deleteBagian`, toast literal, `@Model.Section`/property render, element id, nama data. **SKIP yang TIDAK ada di sini (jaga jangan tergoda):** Index.cshtml:45/61 subtitle, ManageOrgLevelLabels.cshtml:17 subtitle, ManageOrganization.cshtml (Phase 342), ImportSilabus/ImportWorkers `<td>` import-doc — semua SKIP per AUDIT §4. Line numbers panduan — verifikasi konteks.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/EditAssessment.cshtml` contains `Lainnya (Tanpa @OrgLabels.GetLabel(0))` AND does NOT contain `Lainnya (Tanpa Bagian)` AND does NOT contain `<th>Bagian</th>`
    - `Views/Admin/CreateAssessment.cshtml` contains `Lainnya (Tanpa @OrgLabels.GetLabel(0))` AND does NOT contain `Semua Bagian`
    - `Views/Admin/CpdpUpload.cshtml` contains `-- Pilih @OrgLabels.GetLabel(0) --` AND does NOT contain `-- Pilih Bagian --`
    - `Views/Admin/CpdpFiles.cshtml` contains `Tambah @OrgLabels.GetLabel(0)` (button text swapped)
    - `Views/Admin/CpdpFiles.cshtml` STILL contains `addBagian` (SKIP guard — JS func untouched) AND STILL contains `Bagian berhasil` (toast literal untouched)
    - `Views/Account/Profile.cshtml` contains `@OrgLabels.GetLabel(0)` AND `@OrgLabels.GetLabel(1)` AND does NOT contain `>Bagian</div>` AND does NOT contain `>Unit</div>`
    - `Views/Account/Settings.cshtml` contains `@OrgLabels.GetLabel(0)` AND does NOT contain `<label>Bagian</label>`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Assessment/upload/file-mgmt button/Account label dinamis; AMBIGUOUS resolved (button text REPLACE, JS/toast SKIP); subtitle/import-doc/Phase 342 page untouched; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Display-string swap di view ProtonData/Admin/Account. Tidak ada input user baru, endpoint, auth, atau data path baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-05 | Information Disclosure (XSS) | `@OrgLabels.GetLabel(N)` di view Admin/Account/ProtonData | mitigate | Semua swap pakai plain `@OrgLabels.GetLabel(N)` (Razor auto HTML-encode). ZERO `@Html.Raw` dengan output label. Label value = admin-only CRUD (Phase 341) + seed server-side. |
| T-343-06 | Tampering | JS hook + element id + toast (addBagian, filterBagian, overrideBagian) | mitigate | SKIP guard di setiap task: acceptance criteria asserts JS func `addBagian` + toast literal + id STILL present (over-replace merusak JS cascade/CRUD = Pitfall 1). |
| T-343-07 | Repudiation | (audit log body) | accept | Audit log body literal "Bagian" TIDAK disentuh phase ini (SC4). Tidak ada perubahan logging. |
</threat_model>

<verification>
- `dotnet build` exits 0 setiap task (membuktikan @OrgLabels resolve, termasuk partial _TrainingRecordsTab inherit @inject).
- Grep residual (per VALIDATION.md): di 17 file REPLACE, pola display literal (`>Bagian<`/`>Unit<`/`Semua Bagian`/`-- Pilih Bagian --`/`<th>Bagian</th>`/`Bagian Penugasan`/`Lainnya (Tanpa Bagian)`) = 0. Whitelist (boleh tetap): JS func `addBagian`/`onBagianChange`, toast `'Bagian berhasil...'`, alert `'Pilih Bagian Penugasan.'`, element id, `.Bagian`/`.Section` property, import-doc `<td>Bagian</td>` ImportSilabus/ImportWorkers, subtitle SKIP (Index:61/ManageOrgLevelLabels:17) — cocokkan ke 343-AUDIT.md §4.
- Spot-render manual (Phase 344 formal): Worker form (Views/Admin/CreateWorker atau ManageWorkers filter) tampil label benar; fallback "Level N" TIDAK muncul.
</verification>

<success_criteria>
- 17 file ProtonData/Admin/Account: form placeholder + filter + th + detail field + combined-phrase + selector + button label tier dinamis.
- AMBIGUOUS resolved sesuai 343-AUDIT.md §3 (CpdpFiles/KkjMatrix button text REPLACE + JS/toast SKIP; Lainnya-Tanpa-Bagian REPLACE).
- SKIP guards utuh: JS func/toast/alert/id/property/data/import-doc/subtitle literal.
- Build hijau. ORG-INTEG-01 (ProtonData/Admin/Account portion) terpenuhi.
</success_criteria>

<output>
After completion, create `.planning/phases/343-integrasi-app-wide/343-03-SUMMARY.md`
</output>
