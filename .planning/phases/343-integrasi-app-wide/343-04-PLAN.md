---
phase: 343-integrasi-app-wide
plan: 04
type: execute
wave: 2
depends_on: ["343-01"]
files_modified:
  - Views/Admin/EditAssessment.cshtml
  - Views/Admin/CreateAssessment.cshtml
  - Views/Admin/CpdpUpload.cshtml
  - Views/Admin/KkjUpload.cshtml
  - Views/Admin/CpdpFiles.cshtml
  - Views/Admin/KkjMatrix.cshtml
  - Views/Account/Profile.cshtml
  - Views/Account/Settings.cshtml
autonomous: true
requirements: [ORG-INTEG-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Dropdown + table header + AMBIGUOUS button text + selector label + Account detail field tier di Admin assessment/upload (EditAssessment/CreateAssessment/CpdpUpload/KkjUpload/CpdpFiles/KkjMatrix) dan Account (Profile/Settings) menampilkan @OrgLabels.GetLabel(N)"
    - "JS func (addBagian/deleteBagian), toast literal, property access (@Model.Section), element id, subtitle SKIP, import-doc field-table di file ini TIDAK berubah"
    - "AMBIGUOUS resolved per 343-AUDIT.md §3: CpdpFiles/KkjMatrix button text REPLACE + JS/toast SKIP; Lainnya-(Tanpa-Bagian) REPLACE"
    - "dotnet build hijau (semua @OrgLabels resolve)"
  artifacts:
    - path: "Views/Admin/CpdpFiles.cshtml"
      provides: "AMBIGUOUS button text REPLACE (JS func/toast SKIP)"
      contains: "Tambah @OrgLabels.GetLabel(0)"
    - path: "Views/Admin/EditAssessment.cshtml"
      provides: "AMBIGUOUS Lainnya-(Tanpa-Bagian) REPLACE + th + dropdown tier dinamis"
      contains: "Lainnya (Tanpa @OrgLabels.GetLabel(0))"
    - path: "Views/Account/Profile.cshtml"
      provides: "Profile field label tier dinamis (detail field — value untouched)"
      contains: "@OrgLabels.GetLabel(0)"
  key_links:
    - from: "Views/Account/Profile.cshtml"
      to: "OrgLabels.GetLabel"
      via: "@inject inherited dari Views/_ViewImports.cshtml (Plan 01)"
      pattern: "@OrgLabels\\.GetLabel\\([012]\\)"
---

<objective>
Terapkan REPLACE swap (hardcoded "Bagian"/"Unit" → `@OrgLabels.GetLabel(N)`) untuk area **Admin assessment/upload** (EditAssessment, CreateAssessment, CpdpUpload, KkjUpload, CpdpFiles, KkjMatrix) + **Account** (Profile, Settings) mengikuti tabel REPLACE otoritatif `343-RESEARCH.md` L199-209 + keputusan AMBIGUOUS (343-AUDIT.md / RESEARCH L213-226). Murni visible-text swap; JS func/toast/property/data tetap literal.

Purpose: Pemenuhan ORG-INTEG-01 untuk assessment form, KKJ/CPDP upload+file-mgmt, dan profil. Bersama Plan 02 (CMP/CDP) + Plan 03 (ProtonData + worker-domain) memenuhi SC2 (rename ≥3 page) + SC3 (no hardcode tersisa di area target). Plan ini memegang SEMUA resolusi AMBIGUOUS (CpdpFiles/KkjMatrix button + Lainnya-Tanpa-Bagian) per 343-AUDIT.md §3.

Output: 8 file view Admin assessment/upload + Account di-swap; build hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/343-integrasi-app-wide/343-RESEARCH.md
@.planning/phases/343-integrasi-app-wide/343-PATTERNS.md
@.planning/phases/343-integrasi-app-wide/343-VALIDATION.md
@.planning/phases/343-integrasi-app-wide/343-AUDIT.md

<interfaces>
<!-- Kontrak swap — sudah disediakan. -->

@inject SUDAH terpasang global oleh Plan 01 (Views/_ViewImports.cshtml). `@OrgLabels.GetLabel(N)` available di semua view + partial. JANGAN @inject ulang.

Tier mapping: GetLabel(0)=Bagian, (1)=Unit, (2)=Sub-unit. Semua REPLACE row di area ini = tier 0/1.

3 pola swap kanonik (343-PATTERNS.md):
- Pattern A (visible-text-only): `>Bagian<` → `>@OrgLabels.GetLabel(0)<`.
- Pattern B (combined-text): `Semua Bagian` / `-- Pilih Bagian --` / `Lainnya (Tanpa Bagian)` / button `Tambah Bagian` → label dinamis + literal prefix/suffix.
- Pattern C (detail field): hanya sel/elemen LABEL, BUKAN sel nilai data di sebelahnya.

AMBIGUOUS resolutions (343-AUDIT.md §3, otoritatif untuk plan ini):
- CpdpFiles/KkjMatrix button `Tambah/Hapus Bagian` → REPLACE button visible text; SKIP JS func `addBagian()`/`deleteBagian()` + toast `'Bagian berhasil dihapus'`.
- EditAssessment:606 + CreateAssessment:272 `Lainnya (Tanpa Bagian)` → REPLACE (default konsistensi).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Admin assessment + upload (EditAssessment + CreateAssessment + CpdpUpload + KkjUpload)</name>
  <files>Views/Admin/EditAssessment.cshtml, Views/Admin/CreateAssessment.cshtml, Views/Admin/CpdpUpload.cshtml, Views/Admin/KkjUpload.cshtml</files>
  <read_first>
    - 4 file yang dimodifikasi
    - 343-RESEARCH.md REPLACE table L199-205 (EditAssessment L199-200, CreateAssessment L201, CpdpUpload L202-203, KkjUpload L204-205) + AMBIGUOUS table L224-225 (Lainnya-Tanpa-Bagian)
    - 343-PATTERNS.md §AMBIGUOUS (L364-372)
    - 343-AUDIT.md §3 AMBIGUOUS keputusan FINAL + §4 SKIP whitelist
  </read_first>
  <action>
    Swap per RESEARCH.md L199-205 + keputusan AMBIGUOUS (343-AUDIT.md §3):

    **EditAssessment.cshtml**: L522 `<th>Bagian</th>`(0); L598 `<option>Semua Bagian</option>` → `Semua @OrgLabels.GetLabel(0)`(0); L606 `<option>Lainnya (Tanpa Bagian)</option>` → `Lainnya (Tanpa @OrgLabels.GetLabel(0))`(0, **AMBIGUOUS→REPLACE default**).
    **CreateAssessment.cshtml**: L264 `<option>Semua Bagian</option>`(0); L272 `<option>Lainnya (Tanpa Bagian)</option>` → `Lainnya (Tanpa @OrgLabels.GetLabel(0))`(0, **AMBIGUOUS→REPLACE**).
    **CpdpUpload.cshtml**: L39 selector label `Pilih Bagian` → label text jadi `Pilih @OrgLabels.GetLabel(0)`(0); L42 `<option>-- Pilih Bagian --</option>` → `-- Pilih @OrgLabels.GetLabel(0) --`(0).
    **KkjUpload.cshtml** (twin): L39(0); L42(0) — sama CpdpUpload.

    JANGAN sentuh: `@Model.Section`/property render, element id, nama data. **SKIP yang TIDAK ada di sini (jaga jangan tergoda):** Index.cshtml:45/61 subtitle, ManageOrgLevelLabels.cshtml:17 subtitle, ManageOrganization.cshtml (Phase 342), ImportSilabus/ImportWorkers `<td>` import-doc — semua SKIP per AUDIT §4. Line numbers panduan — verifikasi konteks.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/EditAssessment.cshtml` contains `Lainnya (Tanpa @OrgLabels.GetLabel(0))` AND does NOT contain `Lainnya (Tanpa Bagian)` AND does NOT contain `<th>Bagian</th>`
    - `Views/Admin/CreateAssessment.cshtml` contains `Lainnya (Tanpa @OrgLabels.GetLabel(0))` AND does NOT contain `Semua Bagian`
    - `Views/Admin/CpdpUpload.cshtml` contains `-- Pilih @OrgLabels.GetLabel(0) --` AND does NOT contain `-- Pilih Bagian --`
    - `Views/Admin/KkjUpload.cshtml` contains `-- Pilih @OrgLabels.GetLabel(0) --` AND does NOT contain `-- Pilih Bagian --`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Assessment dropdown/th + upload selector label dinamis; AMBIGUOUS Lainnya-Tanpa-Bagian resolved REPLACE; property/data/subtitle untouched; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Swap Admin file-mgmt AMBIGUOUS (CpdpFiles + KkjMatrix button text) + Account views (Profile + Settings)</name>
  <files>Views/Admin/CpdpFiles.cshtml, Views/Admin/KkjMatrix.cshtml, Views/Account/Profile.cshtml, Views/Account/Settings.cshtml</files>
  <read_first>
    - 4 file yang dimodifikasi
    - 343-RESEARCH.md REPLACE table L206-209 (Profile L206-207, Settings L208-209) + AMBIGUOUS table L217-218 (CpdpFiles/KkjMatrix button)
    - 343-PATTERNS.md §AMBIGUOUS (L364-372) + §detail field Account (L240-262)
    - 343-AUDIT.md §3 AMBIGUOUS keputusan FINAL + §4 SKIP whitelist
  </read_first>
  <action>
    Swap per RESEARCH.md L206-209 + keputusan AMBIGUOUS (343-AUDIT.md §3):

    **CpdpFiles.cshtml** (AMBIGUOUS→REPLACE button text, SKIP JS/toast): L64 button `Tambah Bagian` → `Tambah @OrgLabels.GetLabel(0)`(0); L87 button `Hapus Bagian` → `Hapus @OrgLabels.GetLabel(0)`(0). **SKIP: JS func `addBagian()`/`deleteBagian()` + toast `'Bagian berhasil dihapus'` (L172/223/241)** — JANGAN ganti.
    **KkjMatrix.cshtml** (twin): L64 `Tambah Bagian`(0); L87 `Hapus Bagian`(0). SKIP JS func + toast sama.

    **Account/Profile.cshtml** (detail field — Pattern C): L78 `<div class="...">Bagian</div>` → `>@OrgLabels.GetLabel(0)<`(0); L84 `<div ...>Unit</div>` → `>@OrgLabels.GetLabel(1)<`(1). KRITIS: sel NILAI (`@Model.Section`/`@Model.Unit`) JANGAN sentuh.
    **Account/Settings.cshtml**: L96 `<label>Bagian</label>`(0); L114 `<label>Unit</label>`(1).

    JANGAN sentuh: JS func `addBagian/deleteBagian`, toast literal, `@Model.Section`/property render, element id, nama data. **SKIP yang TIDAK ada di sini (jaga jangan tergoda):** Index.cshtml:45/61 subtitle, ManageOrgLevelLabels.cshtml:17 subtitle, ManageOrganization.cshtml (Phase 342), ImportSilabus/ImportWorkers `<td>` import-doc — semua SKIP per AUDIT §4. Line numbers panduan — verifikasi konteks.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/CpdpFiles.cshtml` contains `Tambah @OrgLabels.GetLabel(0)` (button text swapped)
    - `Views/Admin/CpdpFiles.cshtml` STILL contains `addBagian` (SKIP guard — JS func untouched) AND STILL contains `Bagian berhasil` (toast literal untouched)
    - `Views/Admin/KkjMatrix.cshtml` contains `Tambah @OrgLabels.GetLabel(0)` AND STILL contains `addBagian` (SKIP guard)
    - `Views/Account/Profile.cshtml` contains `@OrgLabels.GetLabel(0)` AND `@OrgLabels.GetLabel(1)` AND does NOT contain `>Bagian</div>` AND does NOT contain `>Unit</div>`
    - `Views/Account/Settings.cshtml` contains `@OrgLabels.GetLabel(0)` AND does NOT contain `<label>Bagian</label>`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>File-mgmt button text dinamis (JS/toast SKIP) + Account label dinamis (value untouched); AMBIGUOUS resolved; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Display-string swap di view Admin assessment/upload + Account. Tidak ada input user baru, endpoint, auth, atau data path baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-08 | Information Disclosure (XSS) | `@OrgLabels.GetLabel(N)` di view Admin/Account | mitigate | Semua swap pakai plain `@OrgLabels.GetLabel(N)` (Razor auto HTML-encode). ZERO `@Html.Raw` dengan output label. Label value = admin-only CRUD (Phase 341) + seed server-side. |
| T-343-09 | Tampering | JS func + toast + element id (addBagian, deleteBagian, toast 'Bagian berhasil dihapus') | mitigate | SKIP guard di setiap task: acceptance criteria asserts JS func `addBagian` + toast literal STILL present (over-replace merusak file-mgmt CRUD cascade = Pitfall 1). |
| T-343-10 | Repudiation | (audit log body) | accept | Audit log body literal "Bagian" TIDAK disentuh phase ini (SC4). Tidak ada perubahan logging. |
</threat_model>

<verification>
- `dotnet build` exits 0 setiap task (membuktikan @OrgLabels resolve).
- Grep residual (per VALIDATION.md): di 8 file REPLACE area ini, pola display literal (`Semua Bagian`/`-- Pilih Bagian --`/`<th>Bagian</th>`/`Lainnya (Tanpa Bagian)`/`>Bagian</div>`/button `Tambah Bagian`/`Hapus Bagian`) = 0. Whitelist (boleh tetap): JS func `addBagian`/`deleteBagian`, toast `'Bagian berhasil...'`, element id, `.Bagian`/`.Section` property, import-doc `<td>Bagian</td>`, subtitle SKIP (Index:61/ManageOrgLevelLabels:17) — cocokkan ke 343-AUDIT.md §4.
- Spot-render manual (Phase 344 formal): Account/Profile field label + assessment dropdown tampil label benar; fallback "Level N" TIDAK muncul.
</verification>

<success_criteria>
- 8 file Admin assessment/upload + Account: dropdown + th + button text + selector + detail field label tier dinamis.
- AMBIGUOUS resolved sesuai 343-AUDIT.md §3 (CpdpFiles/KkjMatrix button text REPLACE + JS/toast SKIP; Lainnya-Tanpa-Bagian REPLACE).
- SKIP guards utuh: JS func/toast/id/property/data/import-doc/subtitle literal.
- Build hijau. ORG-INTEG-01 (assessment/upload/Account portion) terpenuhi.
</success_criteria>

<output>
After completion, create `.planning/phases/343-integrasi-app-wide/343-04-SUMMARY.md`
</output>
