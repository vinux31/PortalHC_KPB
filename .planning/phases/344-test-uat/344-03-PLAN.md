---
phase: 344-test-uat
plan: 03
type: execute
wave: 3
depends_on: [344-01, 344-02]
files_modified:
  - tests/e2e/manage-org-label.spec.ts
  - .planning/phases/344-test-uat/344-HUMAN-UAT.md
autonomous: false
requirements: [TEST-06, TEST-02, ORG-INTEG-03]
must_haves:
  truths:
    - "A new Playwright spec manage-org-label.spec.ts passes its E2E scenarios at http://localhost:5277 (tree+legend, dropdown pre-order+deterministic-inactive, cascade modal, label rename→badge old-gone+new-present, label in CMP+Worker form)"
    - "A non-Admin/non-HC user (coach) is confirmed authenticated as coach, then denied OrgLabel CRUD at the live pipeline on BOTH the GET page AND a mutating POST (TEST-02 EoP), landing on the real Authorize denial path"
    - "Label rename done by Playwright is reverted MANDATORILY by an in-spec afterAll (rename back to Bagian), with global.teardown.ts BACKUP/RESTORE as belt-and-suspenders"
    - "A thin 344-HUMAN-UAT.md captures only the non-automated items: cascade-count visual accuracy + 4 regression smoke (ORG-INTEG-03)"
  artifacts:
    - path: "tests/e2e/manage-org-label.spec.ts"
      provides: "TEST-06 scenarios + TEST-02c live GET-403 + POST-403 + mandatory afterAll rename-back"
      contains: "test.describe"
    - path: ".planning/phases/344-test-uat/344-HUMAN-UAT.md"
      provides: "ORG-INTEG-03 thin manual UAT checklist (cascade count visual + 4 regression smoke)"
      contains: "SMOKE"
  key_links:
    - from: "tests/e2e/manage-org-label.spec.ts"
      to: "tests/helpers/accounts.ts"
      via: "import accounts + inline loginAny"
      pattern: "from '../helpers/accounts'"
    - from: "tests/e2e/manage-org-label.spec.ts"
      to: "/CMP/Records + /Admin/CreateWorker"
      via: "page.goto integration-page label assertion (admin login)"
      pattern: "/CMP/Records|/Admin/CreateWorker"
---

<objective>
Add the Playwright E2E spec covering the TEST-06 scenarios + the live 403 permission checks (TEST-02c, GET page AND mutating POST), and produce a thin manual UAT doc for the items deliberately left non-automated (D-04).

Purpose: TEST-06 requires end-to-end scenarios on the running app. TEST-02's live denial (the pipeline-enforced counterpart to Plan 01's reflection attribute test) belongs here too — and the real privilege-escalation surface is the mutating POSTs, not just the GET page (H3). Per D-04, 4 of the spec §7 manual UAT scenarios are automated in Playwright; only the cascade-count visual-accuracy judgment + 4 regression smoke flows stay manual (ORG-INTEG-03).

**Wave + run-prerequisite (C3 + H1):** This plan is Wave 3 — it depends on Plans 01 and 02 (Wave 2 unit/integration tests done first) because (a) the Plan 02 integration test's `MigrateAsync` and this plan's BACKUP/RESTORE both hit the SAME `localhost\SQLEXPRESS` instance and must NOT overlap, and (b) the app must be running on :5277. **Critically (C3): the Playwright run is matrix-coupled.** The chromium project has `dependencies: ['setup']` (playwright.config.ts:30); `setup` = `tests/e2e/global.setup.ts`, which runs the FULL Phase-315 matrix pipeline BEFORE any spec: BACKUP `HcPortalDB_Dev` → seed 18 `AssessmentSessions` (IDs 9001-9018) + Layer-1 validation, with a pre-check that THROWS if IDs 9001-9018 already exist (global.setup.ts:58-66). Running `manage-org-label.spec.ts` triggers this whole pipeline first; if it aborts, the org spec never runs. The org-label spec inherits the matrix BACKUP (good for revert) BUT the dev DB MUST be free of AssessmentSession IDs 9001-9018 before the run (matrix seed clean). Because globalTeardown RESTORE does not fire on a killed run, the rename revert ALSO has its own in-spec afterAll (H6) — it does NOT rely on RESTORE alone.

Output: 1 new e2e spec + 1 thin human-UAT doc. Plan is non-autonomous because Task 3 is a human-verify checkpoint (the manual UAT must be executed by the user at localhost:5277).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/344-test-uat/344-CONTEXT.md
@.planning/phases/344-test-uat/344-RESEARCH.md
@.planning/phases/344-test-uat/344-PATTERNS.md

<interfaces>
<!-- Login + accounts (TEST-06). manage-assessment-filter.spec.ts:25-41 + helpers/accounts.ts -->
import { accounts, AccountKey } from '../helpers/accounts';
// keys: admin (admin@pertamina.com), hc, coach — all pwd 123456. baseURL http://localhost:5277.
async function loginAny(page, key) { goto('/Account/Login'); fill email/password; click submit + waitForURL(!/Account/Login). }

<!-- MATRIX SETUP COUPLING (C3). Verified 2026-06-04: -->
playwright.config.ts:30  chromium project has `dependencies: ['setup']`
playwright.config.ts:5   globalTeardown = e2e/global.teardown.ts (RESTORE HcPortalDB_Dev — does NOT fire if run is killed)
global.setup.ts:32-130   runs FULL matrix pipeline: BACKUP → seed 18 AssessmentSessions (IDs 9001-9018) → Layer-1 validation
global.setup.ts:58-66    PRE-CHECK: expect(COUNT(*) WHERE Id BETWEEN 9001 AND 9018).toBe(0) — ABORTS the whole run if collision
=> RUN PREREQUISITE: dev DB must have NO AssessmentSession IDs 9001-9018 before the Playwright run, else setup aborts and the org spec never executes.

<!-- Scenario → route map (verified routes 2026-06-04): -->
1 tree+legend     → GET /Admin/ManageOrganization     assert #org-legend items + rows have .org-tier-badge
2 dropdown        → /Admin/ManageOrganization open Add/Edit modal; #unitModalParent <option> order = pre-order;
                    inactive option has " (nonaktif)" suffix + grey #999 (orgTree.js:330,334) — DETERMINISTIC seed required (H5)
3 cascade modal   → /Admin/ManageOrganization edit a referenced unit; #cascadeConfirmModal visible + counts present/non-zero
                    (exact-number accuracy = MANUAL per D-04; modal-appears = automatable)
4 rename→badge    → /Admin/ManageOrgLevelLabels rename Bagian→Direktorat; /Admin/ManageOrganization .org-tier-badge == "Direktorat"
                    AND no badge still reads "Bagian" (old-gone, H4)
5 integration     → /CMP/Records (RecordsTeam.cshtml:20 @OrgLabels.GetLabel(0)) + /Admin/CreateWorker (CreateWorker.cshtml:116) — login ADMIN
TEST-02c GET 403  → loginAny(page,'coach'); CONFIRM authenticated as coach (reach a coach-allowed page); GET /Admin/ManageOrgLevelLabels → land on the real Authorize denial path (e.g. /Account/AccessDenied), NOT just any non-200/login bounce (H2)
TEST-02c POST 403 → coach issues a POST to /Admin/UpdateLevelLabel (with antiforgery token) → denied (403 / AccessDenied redirect) (H3 — the real EoP surface)

<!-- Pitfall 5: CMP Team-View labels render only for roleLevel <= 4 → use 'admin' (NOT coach) for scenario 5. -->
<!-- Pitfall 4: global.teardown.ts BACKUP/RESTOREs HcPortalDB_Dev — but does NOT fire on a killed run; rename revert ALSO has a mandatory in-spec afterAll (H6). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manage-org-label.spec.ts — TEST-06 scenarios + TEST-02c GET-403 + POST-403 + mandatory rename-back</name>
  <read_first>
    - tests/e2e/manage-assessment-filter.spec.ts (analog: header comment, import accounts, inline loginAny, test.describe, relative goto, DOM assertions)
    - tests/helpers/accounts.ts (source-of-truth: admin/hc/coach keys, pwd 123456)
    - tests/playwright.config.ts (source-of-truth: chromium dependencies:['setup'] + globalTeardown — the matrix coupling, C3)
    - tests/e2e/global.setup.ts (source-of-truth: matrix BACKUP + seed 9001-9018 + the THROW-on-collision pre-check — C3; do NOT modify, just be aware the run triggers it)
    - tests/e2e/global.teardown.ts (source-of-truth: existing BACKUP/RESTORE of HcPortalDB_Dev — belt-and-suspenders; does NOT fire on kill, so add an in-spec afterAll rename-back — H6)
    - wwwroot/js/orgTree.js lines 319-337 (populateParentDropdown — inactive suffix " (nonaktif)" + grey #999, for scenario 2 assertion)
    - 344-PATTERNS.md "tests/e2e/manage-org-label.spec.ts" section (scenario→route map + login block to copy verbatim)
  </read_first>
  <action>
    Create NEW file `tests/e2e/manage-org-label.spec.ts`. Copy the header-comment convention + import + inline `loginAny` verbatim from `manage-assessment-filter.spec.ts:25-41`. Use relative `page.goto` paths (baseURL :5277 from playwright.config.ts). Wrap scenarios in a `test.describe('Phase 344 — ManageOrgLevelLabels + integrasi', ...)`.

    **Run prerequisite block (C3) — document at the top of the spec as a comment:** this spec inherits the Phase-315 matrix setup (`chromium` `dependencies: ['setup']`). The dev DB MUST be free of `AssessmentSessions` IDs 9001-9018 before the run, or `global.setup.ts` aborts (its pre-check throws) and this spec never executes. The matrix BACKUP is good for revert, but it is NOT a neutral org-only BACKUP — it also seeds 18 matrix sessions. Do NOT assume setup just does BACKUP/RESTORE.

    **Mandatory rename revert (H6) — add an `afterAll` (or `test.afterAll`) that renames Level 0 back to "Bagian" regardless of test outcome (try/finally inside, or unconditional afterAll). This MUST NOT rely solely on globalTeardown RESTORE (which does not fire if Playwright is killed mid-run → "Direktorat" would persist into manual UAT, Pitfall 4). RESTORE stays as belt-and-suspenders.**

    Implement the scenarios (TEST-06) + TEST-02c GET-403 + TEST-02c POST-403:

    1. **tree load + legend visible** — `loginAny(page,'admin')`; `goto('/Admin/ManageOrganization')`; assert `#org-legend` has visible items AND at least one tree row has a `.org-tier-badge`.
    2. **dropdown pre-order + DETERMINISTIC inactive parent shown (H5)** — BEFORE this scenario, MANDATORILY ensure an inactive unit exists: seed/toggle one unit to inactive (via the existing toggle action or a seeded fixture) — do NOT leave it to "discretion" (a false-green on an all-active dev DB). Open the Add/Edit Unit modal (the control that triggers `populateParentDropdown`); read `#unitModalParent option` textContents; assert the order matches pre-order (parent then its indented descendants before the next root) AND at least one option text ENDS WITH ` (nonaktif)` and has grey style `#999`. After the assertion, REVERT the toggle (set the unit back to active) so the dev DB returns to baseline.
    3. **cascade warning modal (counts present)** — edit a unit known to have references; assert `#cascadeConfirmModal` becomes visible and shows non-zero count text. Per D-04 do NOT assert the EXACT number (reserved manual visual-accuracy item) — assert structure/presence/non-zero only.
    4. **label rename → tree badge updated, OLD label GONE (H4)** — `goto('/Admin/ManageOrgLevelLabels')`; rename Level 0 "Bagian" → "Direktorat" via the edit modal/submit; `goto('/Admin/ManageOrganization')`; assert a `.org-tier-badge` now reads "Direktorat" AND assert NO `.org-tier-badge` still reads "Bagian" (prove propagation/replacement, not stale-coexistence). (The mandatory afterAll renames back to "Bagian"; RESTORE is authoritative belt-and-suspenders — Pitfall 4 / H6.)
    5. **label in 2+ integration pages** — depends on scenario 4's rename being applied (sequence within the spec, or re-apply at start of this test); `loginAny(page,'admin')` (Pitfall 5 — CMP Team View only renders for roleLevel <= 4); `goto('/CMP/Records')` assert "Direktorat" visible; `goto('/Admin/CreateWorker')` assert "Direktorat" visible (CreateWorker.cshtml:116 "-- Pilih @OrgLabels.GetLabel(0) --").
    6. **TEST-02c GET — coach denied on the page, real Authorize path (H2)** — `loginAny(page,'coach')`; FIRST confirm the login SUCCEEDED and the user is authenticated as coach (e.g. reach a coach-allowed page like the coach landing/dashboard and assert it loads — proves we are NOT bounced unauthenticated). THEN `goto('/Admin/ManageOrgLevelLabels')` and assert the denial lands on the app's SPECIFIC Authorize denial destination — verify the configured denied path (likely `/Account/AccessDenied`; confirm against the app's auth config) and assert that EXACT path / a 403 status. Do NOT pass on "any non-200" or a bounce to `/Account/Login` (that would mean unauthenticated, not role-denied).
    7. **TEST-02c POST — coach denied on a mutating POST (H3, the real EoP surface)** — as the authenticated coach, issue a POST to `/Admin/UpdateLevelLabel` WITH a valid antiforgery token (obtain the `__RequestVerificationToken` the way the existing specs/helpers do, or via an authenticated form/page request) and assert the response is denied: HTTP 403 OR a redirect to the AccessDenied path — NOT 200, NOT a successful mutation. This is the concrete T-344-01 EoP mitigation check (a reflection attr test + GET-page 403 alone leave the direct-POST untested).

    Cleanup: the MANDATORY `afterAll` rename-back (H6) handles the rename revert even on a killed-mid run; the existing `tests/e2e/global.teardown.ts` BACKUP/RESTORE is the belt-and-suspenders second layer (Pitfall 4). Write NO new globalTeardown. If a seed is needed (e.g. the inactive unit for scenario 2), reuse `tests/helpers/dbSnapshot.ts` (Claude's discretion) — but prefer driving state through the UI and reverting it in-spec.

    Run prerequisite: the app must be running on :5277 (`dotnet run`, DEV_WORKFLOW step 3) before executing Playwright; dev DB free of matrix IDs 9001-9018 (C3).
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/manage-org-label.spec.ts --reporter=line passes (app running on :5277, dev DB free of IDs 9001-9018)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/manage-org-label.spec.ts` exists.
    - `grep -c "from '../helpers/accounts'" tests/e2e/manage-org-label.spec.ts` returns 1 (reuses existing accounts, no new auth flow).
    - `grep -c "/CMP/Records" tests/e2e/manage-org-label.spec.ts` returns >= 1 AND `grep -c "/Admin/CreateWorker" tests/e2e/manage-org-label.spec.ts` returns >= 1 (scenario 5 integration pages).
    - `grep -c "coach" tests/e2e/manage-org-label.spec.ts` returns >= 1 (TEST-02c uses coach for both GET and POST denial).
    - **H3 POST-denial:** `grep -c "UpdateLevelLabel" tests/e2e/manage-org-label.spec.ts` returns >= 1 AND the spec issues a coach POST to it asserting denial (403/AccessDenied), with an antiforgery token referenced (`grep -ci "RequestVerificationToken\|antiforgery\|verification" tests/e2e/manage-org-label.spec.ts` returns >= 1).
    - **H2 strong GET-denial:** the spec asserts the coach login succeeded (reaches a coach-allowed page) AND asserts the specific AccessDenied/403 destination (`grep -ci "AccessDenied\|403" tests/e2e/manage-org-label.spec.ts` returns >= 1); it does NOT treat a `/Account/Login` bounce as a pass.
    - **H4 old-label-gone:** scenario 4 asserts BOTH "Direktorat" present AND no badge reads "Bagian" (`grep -c "Direktorat" tests/e2e/manage-org-label.spec.ts` >= 1 AND an assertion that "Bagian" is absent from tier badges).
    - **H5 deterministic inactive:** the spec seeds/toggles an inactive unit before scenario 2 then reverts it (`grep -ci "nonaktif" tests/e2e/manage-org-label.spec.ts` returns >= 1, and a toggle/seed step precedes the suffix assertion).
    - **H6 mandatory revert:** `grep -ci "afterAll" tests/e2e/manage-org-label.spec.ts` returns >= 1 AND it renames Level 0 back to "Bagian" (a mandatory revert, not optional).
    - `grep -c "globalTeardown\|global.teardown" tests/e2e/manage-org-label.spec.ts` returns 0 (no new global teardown added — relies on existing global teardown + the in-spec afterAll).
    - With the app on :5277 and dev DB free of IDs 9001-9018 (C3): `cd tests && npx playwright test e2e/manage-org-label.spec.ts --reporter=line` exits 0 with all scenarios passing.
    - After the Playwright run (afterAll + global teardown), `/Admin/ManageOrganization` badge shows "Bagian" again (label restored).
  </acceptance_criteria>
  <done>The new spec passes all scenarios on :5277 (TEST-06 + TEST-02c GET-403 with confirmed-coach + specific AccessDenied path + TEST-02c POST-403 on UpdateLevelLabel); scenario 4 proves old "Bagian" is gone and "Direktorat" present; scenario 2 deterministically seeds+reverts an inactive unit; a mandatory afterAll renames Level 0 back to "Bagian"; it reuses accounts/loginAny, asserts integration labels in CMP + Worker form via admin, and adds no new global teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Write thin 344-HUMAN-UAT.md (cascade count visual + 4 regression smoke)</name>
  <read_first>
    - .planning/phases/343-integrasi-app-wide/343-HUMAN-UAT.md (analog: YAML front-matter status/phase/source/started/updated + ## Tests numbered with expected:/result: + ## Summary + ## Gaps)
    - 344-RESEARCH.md §"344-HUMAN-UAT.md content" + 344-VALIDATION.md "Manual-Only Verifications" (the exact checklist items)
    - docs/superpowers/specs/2026-06-02-manageorganization-overhaul-design.md §7 Manual UAT scenario 5 (cascade count)
  </read_first>
  <action>
    Create NEW file `.planning/phases/344-test-uat/344-HUMAN-UAT.md` mirroring the `343-HUMAN-UAT.md` shape (YAML front-matter: `status: pending`, `phase: 344-test-uat`, `source: [344-RESEARCH.md, spec §7]`, `started`/`updated` = today; `## Tests` numbered with `expected:`/`result:`; `## Summary` total/passed/issues/pending; `## Gaps`). Keep it THIN — ONLY the non-automated items per D-04 (the other 4 spec §7 scenarios are automated in Task 1). All instructions in Bahasa Indonesia (user-facing per CLAUDE.md), executed at `http://localhost:5277` with admin@pertamina.com / 123456.

    Seed exactly these 5 checklist items (1 cascade-count + 4 regression smoke — reconciled to 4 per M2; expected: filled, result: blank/pending):
    - **UAT-5** Edit a Bagian with many users → cascade warning modal appears AND the displayed count numbers (affected Users/Mappings/Kompetensi/Guidance) MATCH the actual affected counts (visual accuracy judgment — the number-correctness item reserved from Playwright per D-04).
    - **SMOKE-1** Tree drag-reorder a unit → order persists after reload (ReorderBatch).
    - **SMOKE-2** Toggle a unit Active/Nonaktif → badge + dropdown suffix update correctly.
    - **SMOKE-3** Delete a leaf unit → removed from tree, no orphan.
    - **SMOKE-4** Add a unit under an existing parent → appears in correct pre-order position with dynamic modal title.

    Add a top note: after manual UAT, confirm DB labels are back to baseline (or restore the DB snapshot) so no renamed label leaks into subsequent work (DB lokal `HcPortalDB_Dev`).
  </action>
  <verify>
    <automated>test -f .planning/phases/344-test-uat/344-HUMAN-UAT.md && grep -c "SMOKE-" .planning/phases/344-test-uat/344-HUMAN-UAT.md returns 4</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/344-test-uat/344-HUMAN-UAT.md` exists with YAML front-matter (`status:`, `phase: 344-test-uat`).
    - `grep -c "UAT-5" .planning/phases/344-test-uat/344-HUMAN-UAT.md` returns >= 1 (cascade count visual item).
    - `grep -c "SMOKE-" .planning/phases/344-test-uat/344-HUMAN-UAT.md` returns 4 (4 regression smoke items — reconciled per M2).
    - The doc is THIN: it contains ONLY the 1 cascade-count + 4 smoke items (the 4 automated spec §7 scenarios are NOT duplicated here).
    - Instructions reference `http://localhost:5277` and admin@pertamina.com.
  </acceptance_criteria>
  <done>344-HUMAN-UAT.md exists with exactly the 5 non-automated items (1 cascade-count visual + 4 regression smoke), in Bahasa Indonesia, executable at localhost:5277.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — cascade count visual accuracy + 4 regression smoke (ORG-INTEG-03)</name>
  <action>Pause for the user to execute the 5 non-automated items in 344-HUMAN-UAT.md at http://localhost:5277 and record PASS/issue per item. These cannot be automated: UAT-5 is a visual count-accuracy judgment reserved by D-04, and the 4 smoke flows are interactive UI checks for pre-v21.0 parity (ORG-INTEG-03). See how-to-verify below.</action>
  <what-built>The Playwright spec is green on :5277 (TEST-06 scenarios + TEST-02c GET-403 + POST-403), and 344-HUMAN-UAT.md lists the 5 non-automated items (cascade-count visual accuracy + 4 regression smoke). These last 5 require a human at the running app because they are visual-accuracy judgments and interactive UI flows (ORG-INTEG-03 / D-04).</what-built>
  <how-to-verify>
    1. Ensure the app is running: `dotnet run` → open `http://localhost:5277`, log in as admin@pertamina.com / 123456.
    2. Open `.planning/phases/344-test-uat/344-HUMAN-UAT.md` and execute each checklist item in order:
       - UAT-5: Edit a Bagian that has many users → confirm the cascade warning modal count numbers match reality.
       - SMOKE-1..4: drag-reorder a unit (order persists on reload), toggle a unit active/nonaktif (badge + dropdown suffix update), delete a leaf unit (gone, no orphan), add a unit under an existing parent (correct pre-order position + dynamic title).
    3. Record `result:` for each item in 344-HUMAN-UAT.md (PASS or describe issue), then update `## Summary` and set `status` to `passed` (or `issues`).
    4. After testing, restore DB labels to baseline (or restore the snapshot) so no renamed label leaks.
  </how-to-verify>
  <verify>Manual — user records PASS/issue for all 5 items in 344-HUMAN-UAT.md and sets status.</verify>
  <done>All 5 manual items (UAT-5 + SMOKE-1..4) recorded PASS in 344-HUMAN-UAT.md with Summary updated and status set; DB labels restored to baseline.</done>
  <resume-signal>Type "approved" once all 5 manual items are PASS and 344-HUMAN-UAT.md is filled in, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (coach role) → /Admin/* OrgLabel CRUD (GET page + mutating POST) | Non-privileged user attempting privileged label page AND mutation |
| Playwright run → HcPortalDB_Dev | Test mutations (rename, tree CRUD) must not persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-01 | Elevation of Privilege | /Admin/ManageOrgLevelLabels GET + UpdateLevelLabel POST | mitigate | TEST-02c live denial (this plan) is the runtime regression guard for the access-control contract (ASVS V4): confirmed-coach → GET /Admin/ManageOrgLevelLabels → specific AccessDenied/403 (H2), AND coach → POST /Admin/UpdateLevelLabel (with antiforgery token) → denied (H3 — the real mutating EoP surface). Complements Plan 01 reflection attribute test. |
| T-344-03 | Tampering | HcPortalDB_Dev during Playwright | mitigate | TWO-layer revert: (1) mandatory in-spec afterAll renames Level 0 back to "Bagian" even on a killed run (H6); (2) existing global.setup matrix BACKUP → global.teardown RESTORE reverts ALL test mutations as belt-and-suspenders (note: RESTORE does NOT fire on a killed run, hence layer 1). The matrix setup is acknowledged as a run precondition (C3): dev DB must be free of AssessmentSession IDs 9001-9018 or setup aborts. |
| T-344-INFO | (test-only note) | new e2e spec + UAT doc | accept | Test-only phase, no new attack surface. Uses existing dev login (local-only creds). No new application code or endpoint. No new threats. |
</threat_model>

<verification>
- App on :5277 + dev DB free of matrix IDs 9001-9018 (C3), then `cd tests && npx playwright test e2e/manage-org-label.spec.ts --reporter=line` exits 0.
- Post-run (mandatory afterAll + global teardown): `/Admin/ManageOrganization` badge back to "Bagian" (revert confirmed via layer 1 even if a kill skips RESTORE).
- 344-HUMAN-UAT.md exists and is filled in by the user at the checkpoint (5 items PASS).
</verification>

<success_criteria>
- TEST-06: E2E scenarios green on :5277 (tree+legend, deterministic dropdown inactive, cascade modal, rename→badge old-gone+new-present, integration labels).
- TEST-02c: live denial for coach verified on BOTH the GET page (specific AccessDenied path, confirmed-coach) AND a mutating POST to UpdateLevelLabel (H2 + H3).
- ORG-INTEG-03: 4 regression smoke + cascade-count visual signed off by user in 344-HUMAN-UAT.md.
- No label-rename leak: mandatory in-spec afterAll rename-back (H6) + global RESTORE + manual restore note.
</success_criteria>

<output>
After completion, create `.planning/phases/344-test-uat/344-03-SUMMARY.md`
</output>
</content>
</invoke>
