---
phase: 344-test-uat
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/manage-org-label.spec.ts
  - .planning/phases/344-test-uat/344-HUMAN-UAT.md
autonomous: false
requirements: [TEST-06, TEST-02, ORG-INTEG-03]
must_haves:
  truths:
    - "A new Playwright spec manage-org-label.spec.ts passes 5 E2E scenarios at http://localhost:5277 (tree+legend, dropdown pre-order+inactive, cascade modal, label rename→badge, label in CMP+Worker form)"
    - "A non-Admin/non-HC user (coach) is denied OrgLabel CRUD at the live pipeline (TEST-02 403/redirect)"
    - "Label rename done by Playwright is auto-reverted by the existing global.teardown.ts BACKUP/RESTORE of HcPortalDB_Dev (no new teardown written)"
    - "A thin 344-HUMAN-UAT.md captures only the non-automated items: cascade-count visual accuracy + 4 regression smoke (ORG-INTEG-03)"
  artifacts:
    - path: "tests/e2e/manage-org-label.spec.ts"
      provides: "TEST-06 5 scenarios + TEST-02c live 403"
      contains: "test.describe"
    - path: ".planning/phases/344-test-uat/344-HUMAN-UAT.md"
      provides: "ORG-INTEG-03 thin manual UAT checklist (cascade count visual + regression smoke)"
      contains: "SMOKE"
  key_links:
    - from: "tests/e2e/manage-org-label.spec.ts"
      to: "tests/helpers/accounts.ts"
      via: "import accounts + inline loginAny"
      pattern: "from '../helpers/accounts'"
    - from: "tests/e2e/manage-org-label.spec.ts"
      to: "/CMP/Records + /Admin/CreateWorker"
      via: "page.goto integration-page label assertion (admin login)"
      pattern: "/CMP/Records|/Admin/CreateWorker"
---

<objective>
Add the Playwright E2E spec covering the 5 TEST-06 scenarios + the live 403 permission check (TEST-02c), and produce a thin manual UAT doc for the items deliberately left non-automated (D-04).

Purpose: TEST-06 requires 5 end-to-end scenarios on the running app. TEST-02's live 403 (the pipeline-enforced counterpart to Plan 01's reflection attribute test) belongs here too. Per D-04, 4 of the spec §7 manual UAT scenarios are automated in Playwright; only the cascade-count visual-accuracy judgment + 4 regression smoke flows stay manual (ORG-INTEG-03). The existing `global.teardown.ts` already BACKUP/RESTOREs `HcPortalDB_Dev`, so the scenario-4 label rename auto-reverts — no new teardown code.

Output: 1 new e2e spec + 1 thin human-UAT doc. Plan is non-autonomous because Task 3 is a human-verify checkpoint (the manual UAT must be executed by the user at localhost:5277).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/344-test-uat/344-CONTEXT.md
@.planning/phases/344-test-uat/344-RESEARCH.md
@.planning/phases/344-test-uat/344-PATTERNS.md

<interfaces>
<!-- Login + accounts (TEST-06). manage-assessment-filter.spec.ts:25-41 + helpers/accounts.ts -->
import { accounts, AccountKey } from '../helpers/accounts';
// keys: admin (admin@pertamina.com), hc, coach — all pwd 123456. baseURL http://localhost:5277.
async function loginAny(page, key) { goto('/Account/Login'); fill email/password; click submit + waitForURL(!/Account/Login). }

<!-- Scenario → route map (verified routes 2026-06-04): -->
1 tree+legend     → GET /Admin/ManageOrganization     assert #org-legend items + rows have .org-tier-badge
2 dropdown        → /Admin/ManageOrganization open Add/Edit modal; #unitModalParent <option> order = pre-order;
                    inactive option has " (nonaktif)" suffix + grey (orgTree.js:330,334)
3 cascade modal   → /Admin/ManageOrganization edit a referenced unit; #cascadeConfirmModal visible + counts present/non-zero
                    (exact-number accuracy = MANUAL per D-04; modal-appears = automatable)
4 rename→badge    → /Admin/ManageOrgLevelLabels rename Bagian→Direktorat; /Admin/ManageOrganization .org-tier-badge == new label
5 integration     → /CMP/Records (RecordsTeam.cshtml:20 @OrgLabels.GetLabel(0)) + /Admin/CreateWorker (CreateWorker.cshtml:116) — login ADMIN
TEST-02c 403      → loginAny(page,'coach') → GET /Admin/ManageOrgLevelLabels → expect redirect away OR 403

<!-- Pitfall 5: CMP Team-View labels render only for roleLevel <= 4 → use 'admin' (NOT coach) for scenario 5. -->
<!-- Pitfall 4: NO new teardown — global.teardown.ts already BACKUP/RESTOREs HcPortalDB_Dev (reverts the rename). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manage-org-label.spec.ts — 5 TEST-06 scenarios + TEST-02c live 403</name>
  <read_first>
    - tests/e2e/manage-assessment-filter.spec.ts (analog: header comment, import accounts, inline loginAny, test.describe, relative goto, DOM assertions)
    - tests/helpers/accounts.ts (source-of-truth: admin/hc/coach keys, pwd 123456)
    - tests/e2e/global.teardown.ts (source-of-truth: existing BACKUP/RESTORE of HcPortalDB_Dev auto-reverts the rename — do NOT add a new teardown)
    - wwwroot/js/orgTree.js lines 319-337 (populateParentDropdown — inactive suffix " (nonaktif)" + grey #999, for scenario 2 assertion)
    - 344-PATTERNS.md "tests/e2e/manage-org-label.spec.ts" section (scenario→route map + login block to copy verbatim)
  </read_first>
  <action>
    Create NEW file `tests/e2e/manage-org-label.spec.ts`. Copy the header-comment convention + import + inline `loginAny` verbatim from `manage-assessment-filter.spec.ts:25-41`. Use relative `page.goto` paths (baseURL :5277 from playwright.config.ts). Wrap scenarios in a `test.describe('Phase 344 — ManageOrgLevelLabels + integrasi', ...)`.

    Implement 6 tests (5 TEST-06 scenarios + 1 TEST-02c 403):

    1. **tree load + legend visible** — `loginAny(page,'admin')`; `goto('/Admin/ManageOrganization')`; assert `#org-legend` has visible items AND at least one tree row has a `.org-tier-badge`.
    2. **dropdown pre-order + inactive parent shown** — open the Add/Edit Unit modal (the control that triggers `populateParentDropdown`); read `#unitModalParent option` textContents; assert the order matches pre-order (parent then its indented descendants before the next root) AND at least one option text ends with ` (nonaktif)` and has grey style `#999`. (If the dev DB has no inactive unit, the assertion may toggle one's active state first via the existing toggle action, then revert — Claude's discretion; global RESTORE cleans up regardless.)
    3. **cascade warning modal (counts present)** — edit a unit known to have references; assert `#cascadeConfirmModal` becomes visible and shows non-zero count text. Per D-04 do NOT assert the EXACT number (that is the reserved manual visual-accuracy item) — assert structure/presence/non-zero only.
    4. **label rename → tree badge updated** — `goto('/Admin/ManageOrgLevelLabels')`; rename Level 0 "Bagian" → "Direktorat" via the edit modal/submit; `goto('/Admin/ManageOrganization')`; assert a `.org-tier-badge` now reads "Direktorat". (Belt-and-suspenders optional: rename back to "Bagian" at test end; the global RESTORE is authoritative — Pitfall 4.)
    5. **label in 2+ integration pages** — depends on scenario 4's rename being applied (sequence within the spec, or re-apply at start of this test); `loginAny(page,'admin')` (Pitfall 5 — CMP Team View only renders for roleLevel <= 4); `goto('/CMP/Records')` assert "Direktorat" visible; `goto('/Admin/CreateWorker')` assert "Direktorat" visible (CreateWorker.cshtml:116 "-- Pilih @OrgLabels.GetLabel(0) --").
    6. **TEST-02c — 403/redirect for non-Admin/non-HC** — `loginAny(page,'coach')`; `goto('/Admin/ManageOrgLevelLabels')`; assert the user is redirected away (URL not on /Admin/ManageOrgLevelLabels — e.g. /Account/AccessDenied or login) OR receives a 403 response. This is the live counterpart to Plan 01's reflection attribute test.

    Cleanup: write NO new teardown. The existing `tests/e2e/global.teardown.ts` BACKUP/RESTOREs `HcPortalDB_Dev` once after all specs, reverting the rename (Pitfall 4). If a seed is needed, reuse `tests/helpers/dbSnapshot.ts` (Claude's discretion) — but prefer driving state through the UI.

    Run prerequisite: the app must be running on :5277 (`dotnet run`, DEV_WORKFLOW step 3) before executing Playwright.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/manage-org-label.spec.ts --reporter=line passes 6 (app running on :5277)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/manage-org-label.spec.ts` exists.
    - `grep -c "from '../helpers/accounts'" tests/e2e/manage-org-label.spec.ts` returns 1 (reuses existing accounts, no new auth flow).
    - `grep -c "/CMP/Records" tests/e2e/manage-org-label.spec.ts` returns >= 1 AND `grep -c "/Admin/CreateWorker" tests/e2e/manage-org-label.spec.ts` returns >= 1 (scenario 5 integration pages).
    - `grep -c "loginAny(page, 'coach')\|loginAny(page,'coach')" tests/e2e/manage-org-label.spec.ts` returns >= 1 (TEST-02c uses coach).
    - `grep -c "globalTeardown\|global.teardown" tests/e2e/manage-org-label.spec.ts` returns 0 (no new teardown added — relies on existing global teardown).
    - With the app running on :5277: `cd tests && npx playwright test e2e/manage-org-label.spec.ts --reporter=line` exits 0 with 6 tests passing.
    - After the Playwright run + global teardown, `/Admin/ManageOrganization` badge shows "Bagian" again (label restored by RESTORE).
  </acceptance_criteria>
  <done>The new spec passes all 6 tests on :5277 (5 TEST-06 + 1 TEST-02c 403); it reuses accounts/loginAny, asserts integration labels in CMP + Worker form via admin, and adds no new teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Write thin 344-HUMAN-UAT.md (cascade count visual + regression smoke)</name>
  <read_first>
    - .planning/phases/343-integrasi-app-wide/343-HUMAN-UAT.md (analog: YAML front-matter status/phase/source/started/updated + ## Tests numbered with expected:/result: + ## Summary + ## Gaps)
    - 344-RESEARCH.md §"344-HUMAN-UAT.md content" + 344-VALIDATION.md "Manual-Only Verifications" (the exact checklist items)
    - docs/superpowers/specs/2026-06-02-manageorganization-overhaul-design.md §7 Manual UAT scenario 5 (cascade count)
  </read_first>
  <action>
    Create NEW file `.planning/phases/344-test-uat/344-HUMAN-UAT.md` mirroring the `343-HUMAN-UAT.md` shape (YAML front-matter: `status: pending`, `phase: 344-test-uat`, `source: [344-RESEARCH.md, spec §7]`, `started`/`updated` = today; `## Tests` numbered with `expected:`/`result:`; `## Summary` total/passed/issues/pending; `## Gaps`). Keep it THIN — ONLY the non-automated items per D-04 (the other 4 spec §7 scenarios are automated in Task 1). All instructions in Bahasa Indonesia (user-facing per CLAUDE.md), executed at `http://localhost:5277` with admin@pertamina.com / 123456.

    Seed exactly these checklist items (expected: filled, result: blank/pending):
    - **UAT-5** Edit a Bagian with many users → cascade warning modal appears AND the displayed count numbers (affected Users/Mappings/Kompetensi/Guidance) MATCH the actual affected counts (visual accuracy judgment — the number-correctness item reserved from Playwright per D-04).
    - **SMOKE-1** Tree drag-reorder a unit → order persists after reload (ReorderBatch).
    - **SMOKE-2** Toggle a unit Active/Nonaktif → badge + dropdown suffix update correctly.
    - **SMOKE-3** Delete a leaf unit → removed from tree, no orphan.
    - **SMOKE-4** Add a unit under an existing parent → appears in correct pre-order position with dynamic modal title.

    Add a top note: after manual UAT, confirm DB labels are back to baseline (or restore the DB snapshot) so no renamed label leaks into subsequent work (DB lokal `HcPortalDB_Dev`).
  </action>
  <verify>
    <automated>test -f .planning/phases/344-test-uat/344-HUMAN-UAT.md && grep -c "SMOKE" .planning/phases/344-test-uat/344-HUMAN-UAT.md returns >= 4</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/344-test-uat/344-HUMAN-UAT.md` exists with YAML front-matter (`status:`, `phase: 344-test-uat`).
    - `grep -c "UAT-5" .planning/phases/344-test-uat/344-HUMAN-UAT.md` returns >= 1 (cascade count visual item).
    - `grep -c "SMOKE-" .planning/phases/344-test-uat/344-HUMAN-UAT.md` returns 4 (4 regression smoke items).
    - The doc is THIN: it contains ONLY the 1 cascade-count + 4 smoke items (the 4 automated spec §7 scenarios are NOT duplicated here).
    - Instructions reference `http://localhost:5277` and admin@pertamina.com.
  </acceptance_criteria>
  <done>344-HUMAN-UAT.md exists with exactly the 5 non-automated items (1 cascade-count visual + 4 regression smoke), in Bahasa Indonesia, executable at localhost:5277.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — cascade count visual accuracy + 4 regression smoke (ORG-INTEG-03)</name>
  <action>Pause for the user to execute the 5 non-automated items in 344-HUMAN-UAT.md at http://localhost:5277 and record PASS/issue per item. These cannot be automated: UAT-5 is a visual count-accuracy judgment reserved by D-04, and the 4 smoke flows are interactive UI checks for pre-v21.0 parity (ORG-INTEG-03). See how-to-verify below.</action>
  <what-built>The Playwright spec (6 automated tests) is green on :5277, and 344-HUMAN-UAT.md lists the 5 non-automated items (cascade-count visual accuracy + 4 regression smoke). These last 5 require a human at the running app because they are visual-accuracy judgments and interactive UI flows (ORG-INTEG-03 / D-04).</what-built>
  <how-to-verify>
    1. Ensure the app is running: `dotnet run` → open `http://localhost:5277`, log in as admin@pertamina.com / 123456.
    2. Open `.planning/phases/344-test-uat/344-HUMAN-UAT.md` and execute each checklist item in order:
       - UAT-5: Edit a Bagian that has many users → confirm the cascade warning modal count numbers match reality.
       - SMOKE-1..4: drag-reorder a unit (order persists on reload), toggle a unit active/nonaktif (badge + dropdown suffix update), delete a leaf unit (gone, no orphan), add a unit under an existing parent (correct pre-order position + dynamic title).
    3. Record `result:` for each item in 344-HUMAN-UAT.md (PASS or describe issue), then update `## Summary` and set `status` to `passed` (or `issues`).
    4. After testing, restore DB labels to baseline (or restore the snapshot) so no renamed label leaks.
  </how-to-verify>
  <verify>Manual — user records PASS/issue for all 5 items in 344-HUMAN-UAT.md and sets status.</verify>
  <done>All 5 manual items (UAT-5 + SMOKE-1..4) recorded PASS in 344-HUMAN-UAT.md with Summary updated and status set; DB labels restored to baseline.</done>
  <resume-signal>Type "approved" once all 5 manual items are PASS and 344-HUMAN-UAT.md is filled in, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (coach role) → /Admin/* OrgLabel CRUD | Non-privileged user attempting privileged label page |
| Playwright run → HcPortalDB_Dev | Test mutations (rename, tree CRUD) must not persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-01 | Elevation of Privilege | /Admin/ManageOrgLevelLabels | mitigate | TEST-02c live 403 scenario (this plan) is the runtime regression guard for the access-control contract (ASVS V4): coach → /Admin/ManageOrgLevelLabels → redirect/403. Complements Plan 01 reflection attribute test. |
| T-344-03 | Tampering | HcPortalDB_Dev during Playwright | mitigate | Existing global.setup BACKUP → global.teardown RESTORE reverts ALL test mutations (label rename, tree CRUD). No new teardown; rely on the proven pipeline (Pitfall 4). |
| T-344-INFO | (test-only note) | new e2e spec + UAT doc | accept | Test-only phase, no new attack surface. Uses existing dev login (local-only creds). No new application code or endpoint. No new threats. |
</threat_model>

<verification>
- App on :5277, then `cd tests && npx playwright test e2e/manage-org-label.spec.ts --reporter=line` exits 0 (6 tests).
- Post-run + global teardown: `/Admin/ManageOrganization` badge back to "Bagian" (RESTORE confirmed).
- 344-HUMAN-UAT.md exists and is filled in by the user at the checkpoint (5 items PASS).
</verification>

<success_criteria>
- TEST-06: 5 E2E scenarios green on :5277.
- TEST-02c: live 403/redirect for coach verified.
- ORG-INTEG-03: 4 regression smoke + cascade-count visual signed off by user in 344-HUMAN-UAT.md.
- No label-rename leak (global RESTORE + manual restore note).
</success_criteria>

<output>
After completion, create `.planning/phases/344-test-uat/344-03-SUMMARY.md`
</output>
