---
phase: 345-assessment-pending-grade-display-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/CDPDashboardViewModel.cs
  - Models/ReportsDashboardViewModel.cs
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/UserAssessmentHistory.cshtml
autonomous: true
requirements: [CMP06R-02]
tags: [admin, assessment, history, stats, viewmodel, nullable, display-correctness]
# WAVE NOTE (intra-file overlap): shares Controllers/AssessmentAdminController.cs with 345-03.
# Disjoint regions - this plan = UserAssessmentHistory action L4712-4764 + ComputeHistoryStats helper;
# 345-03 = GeneratePerPesertaPdf L4610-4622. execute-phase will auto-serialize this pair (run 345-02
# BEFORE 345-03) when wave-1 is parallel. Establishes ComputeHistoryStats consumed by 345-04.

must_haves:
  truths:
    - "Sesi Completed+IsPassed-null di /Admin/UserAssessmentHistory tampil badge amber 'Menunggu Penilaian' (bukan 'Fail' merah)"
    - "passRate denominator = graded-only (IsPassed != null); sesi pending tidak menurunkan passRate"
    - "All-pending edge (gradedCount==0) tampil 'Belum ada penilaian' (bukan '0.0%' menyesatkan)"
    - "Indikator 'Menunggu Penilaian: {N}' tampil di area kartu stat saat ada sesi pending"
    - "averageScore exclude pending (D-07 - skor pending parsial/null tidak menyeret rata-rata)"
    - "Group PassedCount (ManageAssessment) tetap exclude pending - sudah benar by-construction (C-3, 0 code change)"
  artifacts:
    - path: "Models/CDPDashboardViewModel.cs"
      provides: "AssessmentReportItem.IsPassed bool -> bool? (C-1: file ini, BUKAN ReportsDashboardViewModel.cs)"
      contains: "public bool? IsPassed"
    - path: "Models/ReportsDashboardViewModel.cs"
      provides: "UserAssessmentHistoryViewModel + GradedCount/PendingCount props"
      contains: "PendingCount"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ComputeHistoryStats static helper + drop ?? false di projection L4737"
      contains: "ComputeHistoryStats"
    - path: "Views/Admin/UserAssessmentHistory.cshtml"
      provides: "3-way badge L172 + passRate guard L68/L101 + indikator pending"
      contains: "bg-warning text-dark"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (UserAssessmentHistory action)"
      to: "Models/ReportsDashboardViewModel.cs (UserAssessmentHistoryViewModel.GradedCount/PendingCount)"
      via: "ComputeHistoryStats result -> ViewModel props -> view guard"
      pattern: "GradedCount|PendingCount"
    - from: "Views/Admin/UserAssessmentHistory.cshtml (L68, L101)"
      to: "Model.GradedCount guard"
      via: "conditional 'Belum ada penilaian' saat gradedCount==0"
      pattern: "Belum ada penilaian"
---

<objective>
Tutup CMP06R-02: surface /Admin/UserAssessmentHistory harus menangani sesi Completed+IsPassed-null dengan benar di 4 lapis - (1) VM nullable ripple (AssessmentReportItem.IsPassed bool->bool?), (2) controller drop ?? false di projection, (3) view 3-way badge amber, (4) stats exclude-pending (passRate denominator graded-only + all-pending guard + indikator "Menunggu Penilaian: N" + averageScore exclude pending). Plus verifikasi C-3 (group PassedCount sudah benar, 0 code change).

Purpose: Saat ini sesi pending di-collapse IsPassed ?? false (L4737) -> dirender "Fail" merah (L172), dan passRate dihitung dengan denominator total (L4745) -> all-pending tampil "0%" menyesatkan. HC tidak sadar ada sesi yang belum dinilai.

Output: 2 model + 1 controller + 1 view ter-edit. dotnet build 0 error (nullable ripple). ComputeHistoryStats static helper untuk testability (dikonsumsi 345-04). Plan ini PALING dalam dari ke-3 wave-1 plan (nullable ripple lintas ctrl+VM+view+stats).

Honor Critical Corrections (RESEARCH):
- C-1: AssessmentReportItem class + IsPassed field = Models/CDPDashboardViewModel.cs:111, BUKAN ReportsDashboardViewModel.cs (REQUIREMENTS/CONTEXT awal salah file). ReportsDashboardViewModel.cs HANYA punya UserAssessmentHistoryViewModel (itu yang ditambah GradedCount/PendingCount).
- C-3: Group PassedCount (AssessmentAdminController.cs:2759/2775/2789/2821) SUDAH exclude pending - projection induk L2712 collapse IsPassed ?? false jadi bool. Ini VERIFY/TEST ONLY, 0 code change. JANGAN edit baris 2759-2821. JANGAN tambah MenungguPenilaianCount ke PrePost sub-rows (defer Phase 348).

Planner decisions (locked):
- D-07: averageScore EXCLUDE pending. Alasan: projection L4735 Score = a.Score ?? 0 -> sesi pending menyumbang 0 ke Average -> menyeret turun palsu. RESEARCH Q4/Pitfall 4 merekomendasikan exclude. averageScore = rata-rata sesi graded (IsPassed != null).
- D-06 placement: indikator "Menunggu Penilaian: {N}" sebagai sub-line kecil di kartu Pass Rate (reuse area styling L102 "Model.PassedCount passed"), bukan kartu besar baru.
- Test strategy: ekstrak math ke ComputeHistoryStats public static (Opsi A). Alasan: AssessmentAdminController ctor punya 10 dependency -> instantiate untuk test action (Opsi B) rapuh. Helper pure-math testable tanpa DbContext/HttpContext.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/345-assessment-pending-grade-display-fix/345-CONTEXT.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-RESEARCH.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-UI-SPEC.md

<interfaces>
Konstanta label (D-02, WAJIB): @AssessmentConstants.AssessmentStatus.PendingGrading (Razor) / AssessmentConstants.AssessmentStatus.PendingGrading (C#). Views/_ViewImports.cshtml:2 sudah @using HcPortal.Models.

AssessmentReportItem (Models/CDPDashboardViewModel.cs:100-113) - C-1 target file:
  class AssessmentReportItem { int Id; string Title; string Category; string UserId; string UserName;
    string? UserNIP; string? UserSection; int Score; int PassPercentage;
    bool IsPassed;        // L111 -> ubah jadi bool?
    DateTime? CompletedAt; }
CATATAN: AssessmentReportItem dipakai BERSAMA oleh CMP Reports + CDP (komentar L98). Nullable ripple harus aman di SEMUA konsumen -> dotnet build 0 error membuktikan.

UserAssessmentHistoryViewModel (Models/ReportsDashboardViewModel.cs:3-15):
  class UserAssessmentHistoryViewModel { string UserId; string UserFullName; string? UserNIP; string? UserSection; string? UserPosition;
    int TotalAssessments; int PassedCount; double PassRate; double AverageScore; List<AssessmentReportItem> Assessments;
    // TAMBAH: int GradedCount; int PendingCount; }

UserAssessmentHistory action (AssessmentAdminController.cs:4712-4764) - tidak panggil _userManager (cuma _context). Projection L4726-4739, stats inline L4743-4746, VM build L4749-4761.
Group projection (AssessmentAdminController.cs:2697-2716) - L2712 IsPassed = a.IsPassed ?? false (bool). PassedCount L2821 g.Count(a => a.IsPassed) sudah exclude pending. C-3 = VERIFY ONLY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VM nullable ripple + props - AssessmentReportItem bool? + UserAssessmentHistoryViewModel GradedCount/PendingCount (C-1)</name>
  <files>Models/CDPDashboardViewModel.cs, Models/ReportsDashboardViewModel.cs</files>
  <behavior>
    - AssessmentReportItem dengan IsPassed=null tetap null (BUKAN collapse ke false) setelah tipe jadi bool?.
    - UserAssessmentHistoryViewModel punya prop GradedCount (int) + PendingCount (int) baru, default 0.
  </behavior>
  <read_first>
    - Models/CDPDashboardViewModel.cs:98-113 (class AssessmentReportItem - field IsPassed di L111; komentar L98 "Shared: used by CMP Reports and CDP")
    - Models/ReportsDashboardViewModel.cs:1-15 (UserAssessmentHistoryViewModel - full file 15 baris)
    - RESEARCH C-1 + Pitfall 1 (file yang benar)
  </read_first>
  <action>
1a. Models/CDPDashboardViewModel.cs:111 (C-1) - ubah field IsPassed dari bool ke bool?. Saat ini: public bool IsPassed { get; set; } -> Menjadi: public bool? IsPassed { get; set; }
Hanya satu karakter (?). Ini menyebabkan ripple compile di semua konsumen AssessmentReportItem.IsPassed (controller projection + view) - sengaja, Task 2 + Task 3 menangani ripple. JANGAN ubah field lain.

1b. Models/ReportsDashboardViewModel.cs - tambah 2 prop di UserAssessmentHistoryViewModel (setelah AverageScore L13, sebelum Assessments L14):
    public int GradedCount { get; set; }
    public int PendingCount { get; set; }
GradedCount = jumlah sesi IsPassed != null; PendingCount = jumlah sesi IsPassed == null. Dipakai view untuk guard D-05 (all-pending -> "Belum ada penilaian") + indikator D-06 ("Menunggu Penilaian: {N}").
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; Select-String -Path Models/CDPDashboardViewModel.cs -Pattern 'public bool\? IsPassed'; Select-String -Path Models/ReportsDashboardViewModel.cs -Pattern 'PendingCount|GradedCount'</automated>
  </verify>
  <acceptance_criteria>
    - Models/CDPDashboardViewModel.cs contains: public bool? IsPassed
    - Models/CDPDashboardViewModel.cs NO longer contains: public bool IsPassed (without ?)
    - Models/ReportsDashboardViewModel.cs contains both: public int GradedCount AND public int PendingCount
    - (build deferred to Task 2 - ripple intentionally breaks projection until ?? false dropped)
  </acceptance_criteria>
  <done>AssessmentReportItem.IsPassed = bool?; ViewModel punya GradedCount + PendingCount props.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Controller - drop ?? false + ekstrak ComputeHistoryStats static helper + wire VM (CMP06R-02 stats, D-04/05/06/07)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <behavior>
    - ComputeHistoryStats(List of AssessmentReportItem) returns (total, graded, pending, passed, passRate, avgScore):
      - mixed [pass, fail, null] -> total=3, graded=2, pending=1, passed=1, passRate=50.0, avgScore = rata-rata 2 graded
      - all-pending [null, null] -> total=2, graded=0, pending=2, passed=0, passRate=0, avgScore=0
      - all-pass [pass, pass] -> passRate=100.0
      - empty [] -> total=0, graded=0, pending=0, passed=0, passRate=0, avgScore=0
    - UserAssessmentHistory action mengisi ViewModel.GradedCount/PendingCount/PassRate/AverageScore dari helper.
  </behavior>
  <read_first>
    - Controllers/AssessmentAdminController.cs:4712-4764 (action UserAssessmentHistory - projection L4726-4739, stats L4743-4746, VM build L4749-4761)
    - Controllers/AssessmentAdminController.cs:2697-2716, 2759-2825 (group projection - C-3 VERIFY ONLY, JANGAN edit)
    - RESEARCH Q4 (formula lengkap) + Q6 Opsi A (ekstrak helper) + Code Examples #2/#3
  </read_first>
  <action>
2a. Drop ?? false di projection (L4737). Saat ini: IsPassed = a.IsPassed ?? false, -> Menjadi: IsPassed = a.IsPassed, (valid karena AssessmentReportItem.IsPassed = bool? dari Task 1).

2b. Ekstrak math stats ke method static testable. Tambah method baru di AssessmentAdminController (mis. setelah action UserAssessmentHistory, dalam class yang sama):

    // Phase 345 CMP06R-02: stats exclude-pending (D-04 passRate graded denom, D-07 avgScore exclude pending).
    // Static + public untuk unit test (HcPortal.Tests) tanpa instantiate controller (10-dep ctor).
    public static (int total, int graded, int pending, int passed, double passRate, double averageScore)
        ComputeHistoryStats(List<AssessmentReportItem> items)
    {
        var total   = items.Count;
        var graded  = items.Count(a => a.IsPassed != null);
        var pending = items.Count(a => a.IsPassed == null);
        var passed  = items.Count(a => a.IsPassed == true);
        var passRate = graded > 0 ? passed * 100.0 / graded : 0;
        var gradedItems = items.Where(a => a.IsPassed != null).ToList();
        var averageScore = gradedItems.Count > 0 ? gradedItems.Average(a => (double)a.Score) : 0;
        return (total, graded, pending, passed, passRate, averageScore);
    }

2c. Ganti stats inline (L4743-4746) supaya panggil helper, lalu wire ke VM. Saat ini 4 baris (totalAssessments/passedCount/passRate/averageScore) -> ganti jadi:

    var (totalAssessments, gradedCount, pendingCount, passedCount, passRate, averageScore) = ComputeHistoryStats(assessments);

Lalu di VM build (L4749-4761), tambah dua assignment: GradedCount = gradedCount, dan PendingCount = pendingCount, (sisipkan di antara field VM yang sudah ada).

CATATAN: assessments.Count(a => a.IsPassed) lama (L4744) TIDAK lagi compile setelah IsPassed jadi bool? (Count predicate butuh bool). Helper menggantinya dengan a.IsPassed == true. Pastikan tidak ada sisa a.IsPassed dipakai sebagai bool langsung di action ini.

C-3 GUARD: JANGAN sentuh group projection L2697-2825 (PassedCount sudah exclude pending via L2712 collapse). JANGAN tambah MenungguPenilaianCount ke PrePost. Defer Phase 348.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; Select-String -Path Controllers/AssessmentAdminController.cs -Pattern 'public static .*ComputeHistoryStats'; dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AssessmentAdminController.cs contains: ComputeHistoryStats static method (returns tuple total/graded/pending/passed/passRate/averageScore)
    - Controllers/AssessmentAdminController.cs contains: IsPassed = a.IsPassed, (projection L4737, no ?? false)
    - Controllers/AssessmentAdminController.cs NO longer contains: IsPassed = a.IsPassed ?? false inside UserAssessmentHistory action (L4737)
    - Controllers/AssessmentAdminController.cs STILL contains: IsPassed = a.IsPassed ?? false at group projection L2712 (C-3 unchanged)
    - Controllers/AssessmentAdminController.cs STILL contains: PassedCount = g.Count(a => a.IsPassed) (group, unchanged)
    - dotnet build HcPortal.csproj exits 0
  </acceptance_criteria>
  <done>Action pakai ComputeHistoryStats; passRate denom=graded; averageScore exclude pending; VM punya GradedCount/PendingCount; group projection untouched; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: View 3-way badge + passRate guard + indikator pending (CMP06R-02 view, D-05/D-06)</name>
  <files>Views/Admin/UserAssessmentHistory.cshtml</files>
  <read_first>
    - Views/Admin/UserAssessmentHistory.cshtml:65-105 (mini-stat Pass Rate L68 + kartu Pass Rate L101 + L102 "Model.PassedCount passed")
    - Views/Admin/UserAssessmentHistory.cshtml:160-180 (badge status L172 @if(item.IsPassed) binary - akan CS0266 setelah bool?)
    - RESEARCH Pitfall 2 (if(bool?) tidak compile) + Pitfall 3 (passRate 0% menyesatkan) + Code Examples #2/#3
    - UI-SPEC Copywriting Contract (string "Belum ada penilaian" + "Menunggu Penilaian: {N}")
  </read_first>
  <action>
3a. L172 badge 3-way (Pitfall 2 - WAJIB, kalau tidak build gagal CS0266). Saat ini @if (item.IsPassed) { badge bg-success Pass } else { badge bg-danger Fail }. Menjadi:

    @if (item.IsPassed == true)
    {
        <span class="badge bg-success">Pass</span>
    }
    else if (item.IsPassed == false)
    {
        <span class="badge bg-danger">Fail</span>
    }
    else
    {
        <span class="badge bg-warning text-dark">@AssessmentConstants.AssessmentStatus.PendingGrading</span>
    }

JANGAN rename "Pass"/"Fail" jadi Indonesia (POL-01/347 scope, BUKAN 345).

3b. passRate guard D-05 di DUA tempat (mini-stat L68 + kartu L101). Saat ini keduanya: @Model.PassRate.ToString("F1")%. Ganti DUA-DUANYA jadi conditional:

    @if (Model.GradedCount > 0) { @Model.PassRate.ToString("F1")<text>%</text> } else { <text>Belum ada penilaian</text> }

Untuk mini-stat L68 yang ringkas, varian strip "-" boleh di else bila ruang sempit; kartu besar L101 pakai "Belum ada penilaian" (konsisten UI-SPEC).

3c. Indikator pending D-06 - tambah sub-line di kartu Pass Rate (setelah L102 "Model.PassedCount passed"):

    @if (Model.PendingCount > 0)
    {
        <div><small class="badge bg-warning text-dark mt-1">@AssessmentConstants.AssessmentStatus.PendingGrading: @Model.PendingCount</small></div>
    }

Reuse styling badge existing (bukan kartu besar baru). Hanya tampil saat ada pending.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; Select-String -Path Views/Admin/UserAssessmentHistory.cshtml -Pattern 'item.IsPassed == false|Belum ada penilaian|Model.PendingCount'; dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - Views/Admin/UserAssessmentHistory.cshtml contains: item.IsPassed == false (3-way branch)
    - Views/Admin/UserAssessmentHistory.cshtml contains: bg-warning text-dark with @AssessmentConstants.AssessmentStatus.PendingGrading (badge L172)
    - Views/Admin/UserAssessmentHistory.cshtml contains: Belum ada penilaian (passRate guard)
    - Views/Admin/UserAssessmentHistory.cshtml contains: Model.PendingCount (indikator D-06)
    - Views/Admin/UserAssessmentHistory.cshtml NO longer contains bare: @if (item.IsPassed) without == comparison
    - dotnet build HcPortal.csproj exits 0 (Razor compile clean - Pitfall 2 resolved)
  </acceptance_criteria>
  <done>View render 3-way badge amber untuk pending; passRate "Belum ada penilaian" saat all-pending; indikator "Menunggu Penilaian: N" saat ada pending; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Display-correctness murni di surface /Admin/UserAssessmentHistory yang SUDAH ber-[Authorize(Roles="Admin, HC")]. Tidak ada endpoint baru, input user baru, perubahan authz, atau migration. Data assessment sudah ada di DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-345-02-01 | Information Disclosure | Stats passRate/pendingCount di view Admin | accept | Surface [Authorize Admin/HC] existing; passRate/pendingCount adalah agregat dari data yang sudah lolos authz. Mengubah denominator (total->graded) + menambah indikator pendingCount tidak mengekspos sesi/field baru, hanya menghitung ulang yang sudah tampil. Zero new exposure. |
| T-345-02-02 | Tampering | ComputeHistoryStats math | accept | Method pure-function deterministik atas List in-memory (sudah ter-query dari DB dengan filter UserId+Status existing). Tidak ada input runtime yang bisa di-tamper; tidak ada side-effect (tidak tulis DB). |
| T-345-02-03 | (S/R/D/E) | - | N/A | Tidak ada autentikasi/non-repudiation/DoS/elevation surface yang disentuh. Helper static tidak menambah query (dipanggil atas list yang sudah ada). |
</threat_model>

<verification>
- dotnet build HcPortal.csproj exits 0 (nullable ripple AssessmentReportItem bool? aman di SEMUA konsumen CMP Reports + CDP + UserAssessmentHistory).
- grep konfirmasi: bool? IsPassed (VM), ComputeHistoryStats (ctrl), IsPassed = a.IsPassed tanpa ?? false (action), badge bg-warning text-dark + "Belum ada penilaian" + Model.PendingCount (view).
- grep konfirmasi C-3 unchanged: group projection L2712 IsPassed = a.IsPassed ?? false + PassedCount L2821 g.Count(a => a.IsPassed) TETAP ada (0 code change).
- Math correctness (passRate exclude-pending + all-pending guard + averageScore exclude) di-test via xUnit ComputeHistoryStats di Plan 345-04.
- Visual UAT badge amber + indikator di /Admin/UserAssessmentHistory via Playwright/MCP di Plan 345-04.
</verification>

<success_criteria>
- ROADMAP SC1 (sebagian): /Admin/UserAssessmentHistory sesi Completed+IsPassed-null tampil "Menunggu Penilaian" (bukan Fail).
- ROADMAP SC3: passRate exclude pending dari denominator (graded-only).
- ROADMAP SC4 (sebagian): dotnet build 0 error (VM bool->bool? ripple).
- D-05: all-pending -> "Belum ada penilaian" (bukan 0%).
- D-06: indikator "Menunggu Penilaian: {N}" tampil saat ada pending.
- D-07: averageScore exclude pending.
- D-10/C-3: group PassedCount tetap exclude pending (verifikasi - 0 code change).
- D-02: konstanta PendingGrading dipakai (zero literal baru di view/ctrl).
</success_criteria>

<output>
After completion, create `.planning/phases/345-assessment-pending-grade-display-fix/345-02-SUMMARY.md`
</output>
