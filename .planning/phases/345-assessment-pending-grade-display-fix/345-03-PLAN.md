---
phase: 345-assessment-pending-grade-display-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [CMP06R-03]
tags: [admin, assessment, pdf, questpdf, export, display-correctness]
# WAVE NOTE (intra-file overlap): shares Controllers/AssessmentAdminController.cs with 345-02.
# Disjoint regions - 345-02 = UserAssessmentHistory action L4712-4764 + ComputeHistoryStats helper;
# 345-03 (this) = GeneratePerPesertaPdf L4610-4622. ROADMAP locks both in wave 1 (independent-region
# intent), but they touch ONE file. execute-phase intra-wave overlap check WILL auto-serialize this
# pair (run 345-02 BEFORE 345-03). In --interactive mode (this project's default) plans run
# sequentially inline regardless, so the overlap is moot. Recommended serialized order: 345-02 -> 345-03.

must_haves:
  truths:
    - "PDF BulkExportPdf per-peserta untuk sesi Completed+IsPassed-null menampilkan Status 'Menunggu Penilaian' (bukan 'Tidak Lulus')"
    - "Warna teks Status + Skor untuk sesi pending = Orange.Darken2 (amber netral, bukan Red.Darken2 merah)"
    - "Sesi graded (IsPassed true/false) di PDF tetap Lulus hijau / Tidak Lulus merah (no regression)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "GeneratePerPesertaPdf 3-way statusText + statusColor (null -> Menunggu Penilaian + Orange.Darken2)"
      contains: "Colors.Orange.Darken2"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (GeneratePerPesertaPdf:4620-4621)"
      to: "QuestPDF.Helpers.Colors.Orange.Darken2"
      via: "statusColor ternary 3-way untuk session.IsPassed == null"
      pattern: "Colors\\.Orange\\.Darken2"
---

<objective>
Tutup CMP06R-03: PDF per-peserta yang di-generate oleh GeneratePerPesertaPdf (dipanggil BulkExportPdf, route GET /Admin/BulkExportPdf) saat ini render sesi Completed+IsPassed-null sebagai "Tidak Lulus" merah. Ubah jadi 3-way: null -> "Menunggu Penilaian" dengan warna Orange.Darken2 (amber netral).

Purpose: Eligibility BulkExportPdf (L4507 s.Status != "Cancelled" && (s.CompletedAt != null || s.Score != null)) memasukkan sesi Completed+IsPassed-null (CompletedAt != null) -> kena render binary L4620-4621 -> "Tidak Lulus" merah palsu. Bug nyata & reachable. No migration.

Output: 1 file ter-edit (2 baris PDF span di GeneratePerPesertaPdf), dotnet build 0 error, PDF status pending = amber "Menunggu Penilaian".

VERIFIED (RESEARCH Q3): QuestPDF 2026.2.2 -> QuestPDF.Helpers.Colors.Orange.Darken2 VALID (palette Material sama dengan Green.Darken2/Red.Darken2 yang sudah dipakai di file ini L4604/L4620-4621).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/345-assessment-pending-grade-display-fix/345-CONTEXT.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-RESEARCH.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-UI-SPEC.md

<interfaces>
Konstanta label (D-02, WAJIB): AssessmentConstants.AssessmentStatus.PendingGrading (= "Menunggu Penilaian"). AssessmentAdminController.cs sudah using HcPortal.Models (VERIFIED - constructor/projection sudah pakai AssessmentConstants di file ini).

QuestPDF Colors (QuestPDF.Helpers.Colors, dipakai fully-qualified di file ini):
- QuestPDF.Helpers.Colors.Green.Darken2  (existing - IsPassed true)
- QuestPDF.Helpers.Colors.Red.Darken2    (existing - IsPassed false/null saat ini, SALAH untuk null)
- QuestPDF.Helpers.Colors.Orange.Darken2 (BARU - IsPassed null, RESEARCH Q3 confirmed valid)

GeneratePerPesertaPdf(AssessmentSession session, ...) - session.IsPassed = bool?, session.Score = int?.
Baris target (L4620-4621): Skor span + Status span, keduanya pakai binary ternary IsPassed == true ? Green : Red.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDF 3-way - GeneratePerPesertaPdf statusText + statusColor + skor color (CMP06R-03)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:4610-4622 (method GeneratePerPesertaPdf - baris Skor L4620 + Status L4621, binary ternary Green/Red)
    - Controllers/AssessmentAdminController.cs:4497-4516 (BulkExportPdf eligibility L4507 - konfirmasi sesi pending masuk export)
    - RESEARCH Q3 (QuestPDF Colors.Orange.Darken2 valid + pola 3-way ternary lengkap) + Code Examples #4
    - UI-SPEC Color section PDF table (true=Green.Darken2, false=Red.Darken2, null=Orange.Darken2)
  </read_first>
  <action>
Di GeneratePerPesertaPdf, ubah dua baris span (Skor L4620 + Status L4621) dari binary jadi 3-way via dua local var. Saat ini (L4620-4621):

    c.Item().Text(t => { t.Span("Skor: ").Bold(); t.Span(session.Score?.ToString() ?? "—").Bold().FontColor(session.IsPassed == true ? QuestPDF.Helpers.Colors.Green.Darken2 : QuestPDF.Helpers.Colors.Red.Darken2); });
    c.Item().Text(t => { t.Span("Status: ").Bold(); t.Span(session.IsPassed == true ? "Lulus" : "Tidak Lulus").Bold().FontColor(session.IsPassed == true ? QuestPDF.Helpers.Colors.Green.Darken2 : QuestPDF.Helpers.Colors.Red.Darken2); });

Hitung statusText + statusColor SEKALI di atas kedua span (hindari triple-ternary berulang), lalu pakai di kedua baris:

    var statusText  = session.IsPassed == true ? "Lulus"
                    : session.IsPassed == false ? "Tidak Lulus"
                    : AssessmentConstants.AssessmentStatus.PendingGrading;
    var statusColor = session.IsPassed == true ? QuestPDF.Helpers.Colors.Green.Darken2
                    : session.IsPassed == false ? QuestPDF.Helpers.Colors.Red.Darken2
                    : QuestPDF.Helpers.Colors.Orange.Darken2;
    c.Item().Text(t => { t.Span("Skor: ").Bold(); t.Span(session.Score?.ToString() ?? "—").Bold().FontColor(statusColor); });
    c.Item().Text(t => { t.Span("Status: ").Bold(); t.Span(statusText).Bold().FontColor(statusColor); });

Letakkan kedua var di scope yang sama dengan kedua c.Item() (di dalam Column lambda c, sebelum baris Skor). Skor color untuk null sekarang netral (Orange, bukan Red) - sesuai UI-SPEC (skor pending parsial/null tidak ditandai merah "gagal"). JANGAN ubah teks "Lulus"/"Tidak Lulus" untuk true/false (existing). JANGAN ubah eligibility L4507 atau bagian PDF lain (spider chart, peserta info card).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; Select-String -Path Controllers/AssessmentAdminController.cs -Pattern 'Colors.Orange.Darken2'; dotnet build HcPortal.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - Controllers/AssessmentAdminController.cs contains: QuestPDF.Helpers.Colors.Orange.Darken2
    - Controllers/AssessmentAdminController.cs contains: statusText AND statusColor local vars in GeneratePerPesertaPdf
    - Controllers/AssessmentAdminController.cs GeneratePerPesertaPdf statusText null-branch contains: AssessmentConstants.AssessmentStatus.PendingGrading
    - Controllers/AssessmentAdminController.cs STILL contains: "Lulus" AND "Tidak Lulus" (true/false text unchanged) AND Colors.Green.Darken2 AND Colors.Red.Darken2
    - dotnet build HcPortal.csproj exits 0
  </acceptance_criteria>
  <done>PDF per-peserta render Status "Menunggu Penilaian" + Orange.Darken2 untuk sesi pending; graded sessions Lulus/Tidak Lulus unchanged; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | PDF generation server-side (QuestPDF) di endpoint BulkExportPdf yang SUDAH ber-[Authorize(Roles="Admin, HC")] + [HttpGet] + DoS guard max 50 peserta (L4515). Tidak ada endpoint baru, input baru, authz baru, atau migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-345-03-01 | Information Disclosure | Status label di PDF per-peserta | accept | PDF hanya mengganti teks status ("Tidak Lulus"->"Menunggu Penilaian") + warna untuk data peserta yang SUDAH masuk eligible export (lolos authz Admin/HC + filter title/category/schedule existing). Tidak menambah peserta/field ke export, tidak menurunkan eligibility L4507. Zero new exposure. |
| T-345-03-02 | Denial of Service | BulkExportPdf batch | accept | DoS guard existing (max 50 peserta L4515) TIDAK diubah. Perubahan hanya 2 span per PDF (string + color) - tidak menambah cost generation. |
| T-345-03-03 | (S/T/R/E) | - | N/A | Tidak ada autentikasi/tampering/non-repudiation/elevation surface yang disentuh. statusColor dari const Colors.* compile-time, bukan input. |
</threat_model>

<verification>
- dotnet build HcPortal.csproj exits 0 (QuestPDF Colors.Orange.Darken2 resolve - VERIFIED valid 2026.2.2).
- grep konfirmasi: Colors.Orange.Darken2 + statusText/statusColor + PendingGrading null-branch ada; "Lulus"/"Tidak Lulus" + Green/Red.Darken2 tetap ada (no regression).
- Visual UAT: PDF dari BulkExportPdf dibuka -> sesi pending tampil "Menunggu Penilaian" amber (bukan "Tidak Lulus" merah) di Plan 345-04 (download otomatis via Playwright; label-in-PDF via human/MCP verify per RESEARCH A3).
</verification>

<success_criteria>
- ROADMAP SC1 (sebagian): PDF BulkExportPdf sesi Completed+IsPassed-null tampil "Menunggu Penilaian" (bukan Tidak Lulus).
- ROADMAP SC4 (sebagian): dotnet build 0 error.
- D-01: warna amber PDF = Colors.Orange.Darken2 (tier match Green/Red existing).
- D-02: konstanta PendingGrading dipakai (zero literal "Menunggu Penilaian" di PDF code).
</success_criteria>

<output>
After completion, create `.planning/phases/345-assessment-pending-grade-display-fix/345-03-SUMMARY.md`
</output>
