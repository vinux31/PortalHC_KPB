---
phase: 345-assessment-pending-grade-display-fix
plan: 04
type: execute
wave: 2
depends_on: ["345-01", "345-02", "345-03"]
files_modified:
  - HcPortal.Tests/AssessmentHistoryStatsTests.cs
  - tests/e2e/assessment-pending-grade.spec.ts
  - tests/sql/pending345-seed.sql
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [CMP06R-05]
tags: [test, xunit, playwright, seed-workflow, regression, assessment]

must_haves:
  truths:
    - "xUnit ComputeHistoryStats membuktikan passRate denominator = graded-only (mixed pass/fail/null -> 50%) + all-pending guard (gradedCount==0 -> passRate 0) + averageScore exclude pending"
    - "xUnit membuktikan AssessmentReportItem.IsPassed=null tetap null (BUKAN collapse ke false) setelah bool? ripple"
    - "xUnit regression-guard membuktikan group PassedCount tidak naik karena pending (C-3 verifikasi)"
    - "Playwright UAT membuktikan badge amber 'Menunggu Penilaian' tampil di RecordsWorkerDetail + UserAssessmentHistory (DOM assert)"
    - "Playwright membuktikan BulkExportPdf download .zip sukses (size > 0; label-in-PDF via human/MCP)"
    - "SEED sesi Completed+IsPassed-null di-snapshot sebelum + restore sesudah (sukses ATAU gagal) + journal cleaned"
  artifacts:
    - path: "HcPortal.Tests/AssessmentHistoryStatsTests.cs"
      provides: "xUnit math + VM nullable mapping tests untuk ComputeHistoryStats"
      contains: "ComputeHistoryStats"
    - path: "tests/e2e/assessment-pending-grade.spec.ts"
      provides: "Playwright UAT 3 surface + SEED snapshot/restore"
      contains: "Menunggu Penilaian"
    - path: "tests/sql/pending345-seed.sql"
      provides: "INSERT sesi Completed+IsPassed-null prefix [PENDING345]"
      contains: "[PENDING345]"
    - path: "docs/SEED_JOURNAL.md"
      provides: "audit trail entry Phase 345 temporary+local-only cleaned"
      contains: "345"
  key_links:
    - from: "HcPortal.Tests/AssessmentHistoryStatsTests.cs"
      to: "Controllers/AssessmentAdminController.ComputeHistoryStats (dari 345-02)"
      via: "static method call dengan List<AssessmentReportItem> rakitan"
      pattern: "ComputeHistoryStats"
    - from: "tests/e2e/assessment-pending-grade.spec.ts"
      to: "tests/sql/pending345-seed.sql + tests/helpers/dbSnapshot.ts"
      via: "backup -> execScript seed -> UAT -> restore (finally) -> journal"
      pattern: "dbSnapshot|backup|restore"
---

<objective>
Tutup CMP06R-05: regression test untuk Phase 345. Dua lapis: (1) xUnit di HcPortal.Tests - math ComputeHistoryStats (passRate exclude-pending, all-pending guard, averageScore exclude) + VM nullable mapping (IsPassed=null tetap null) + regression-guard group PassedCount (C-3). (2) Playwright UAT 3 surface (RecordsWorkerDetail + UserAssessmentHistory badge amber DOM assert + BulkExportPdf .zip download), dengan SEED_WORKFLOW (snapshot DB -> seed sesi Completed+IsPassed-null -> UAT -> restore finally -> journal cleaned).

Purpose: Mengunci perilaku 345-01/02/03 supaya tidak regresi (terutama nullable ripple + passRate math yang halus). xUnit cepat (<5s) jadi feedback gate; Playwright membuktikan badge visible end-to-end di surface nyata.

Output: 1 xUnit test class + 1 Playwright spec + 1 seed SQL + 1 journal entry. dotnet test HcPortal.Tests hijau. Playwright 3-surface PASS (HC login broken di infra -> pakai admin, dokumentasikan skip).

Depends on 345-01 (service/view label) + 345-02 (ComputeHistoryStats helper + VM bool?) + 345-03 (PDF). Wave 2.

Honor:
- RESEARCH Q6 Opsi A: test math via ComputeHistoryStats static helper (sudah diekstrak di 345-02) - TIDAK instantiate controller (10-dep ctor).
- PATTERNS: BulkExportPdf -> .zip (regex _Bundle\.zip, BUKAN _Summary.xlsx); HC login broken di test infra -> pakai admin (Roles="Admin, HC") + dokumentasikan skip alasan (pola Phase 320).
- SEED_WORKFLOW (CLAUDE.md + docs/SEED_WORKFLOW.md): snapshot SEBELUM seed, restore SESUDAH (finally - sukses/gagal), klasifikasi temporary+local-only, prefix [PENDING345], journal active->cleaned. JANGAN ke Data/SeedData.cs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/345-assessment-pending-grade-display-fix/345-RESEARCH.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-PATTERNS.md
@.planning/phases/345-assessment-pending-grade-display-fix/345-VALIDATION.md
@docs/SEED_WORKFLOW.md

<interfaces>
ComputeHistoryStats (dari 345-02, AssessmentAdminController public static):
  (int total, int graded, int pending, int passed, double passRate, double averageScore)
    ComputeHistoryStats(List<AssessmentReportItem> items)
  - mixed [pass,fail,null] -> (3,2,1,1,50.0, avg-2-graded)
  - all-pending [null,null] -> (2,0,2,0,0,0)
  - empty [] -> (0,0,0,0,0,0)

AssessmentReportItem (Models/CDPDashboardViewModel.cs, namespace HcPortal.Models) setelah 345-02:
  - bool? IsPassed (sudah nullable); int Score; ... (rakit minimal IsPassed+Score untuk test)

xUnit pattern (analog CertificateStatusTests.cs - pure static, Theory/Fact/null-edge):
  using System; using HcPortal.Models; using HcPortal.Controllers; using Xunit; namespace HcPortal.Tests;
  helper: private static AssessmentReportItem Item(bool? p, int s) => new() { IsPassed = p, Score = s };
  ComputeHistoryStats di AssessmentAdminController -> using HcPortal.Controllers.
  csproj HcPortal.Tests.csproj: sudah xunit 2.9.3 + EFCore.InMemory 8.0.0 + ProjectReference ..\HcPortal.csproj (file *.cs auto-discovered, TIDAK perlu ubah csproj).

Playwright (analog export-per-peserta.spec.ts):
  import { test, expect, Page } from '@playwright/test'; import { accounts, AccountKey } from '../helpers/accounts';
  accounts: admin = admin@pertamina.com/123456 (akses UserAssessmentHistory + BulkExportPdf via Roles Admin,HC), coachee = rino.prasetyo@pertamina.com/123456 (RecordsWorkerDetail worker view).
  loginAny(page, key): goto /Account/Login -> fill input[name=email]+input[name=password] -> Promise.all([waitForURL(!includes /Account/Login), click button[type=submit]]).
  triggerDownload(page, url): waitForEvent('download') + page.goto(url).catch(()=>{}).
  BulkExportPdf route: GET /Admin/BulkExportPdf?title=...&category=...&scheduleDate=yyyy-MM-dd -> filename _Bundle.zip.

dbSnapshot helper (tests/helpers/dbSnapshot.ts - semua sudah ada, JANGAN hand-roll):
  db.queryString(sql) / db.backup(path) / db.execScript(sqlPath) / db.restore(path) / db.queryScalar(sql).
  Target DB localhost\SQLEXPRESS / HcPortalDB_Dev (guard reject non-localhost).
  Resolve backup dir: SELECT CAST(SERVERPROPERTY('InstanceDefaultBackupPath') AS NVARCHAR(260)) (C:\Temp blocked).

SEED_JOURNAL.md format (tabel): | Tanggal | Phase | Klasifikasi | Tujuan | Dampak (entitas tersentuh) | Snapshot file | Status |
AssessmentSession seed shape (Models/AssessmentSession.cs): UserId, Title, Category, Schedule, DurationMinutes, Status='Completed', Progress, BannerColor, PassPercentage, AllowAnswerReview, GenerateCertificate, IsPassed=NULL, CompletedAt=GETDATE(), Score=NULL, IsTokenRequired, AccessToken.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: xUnit - ComputeHistoryStats math + VM nullable mapping + group PassedCount regression-guard (D-11a/b, C-3)</name>
  <files>HcPortal.Tests/AssessmentHistoryStatsTests.cs</files>
  <read_first>
    - HcPortal.Tests/CertificateStatusTests.cs (analog FULL - pure static, [Theory]+[InlineData] boundary + [Fact] null-edge; helper private static)
    - PATTERNS Analog A (pure-math helper mapping ke ComputeHistoryStats)
    - 345-02-PLAN.md (signature + behavior ComputeHistoryStats - source of truth untuk expected values)
    - HcPortal.Tests/HcPortal.Tests.csproj (konfirmasi xunit + ProjectReference; TIDAK perlu diubah)
  </read_first>
  <action>
Buat HcPortal.Tests/AssessmentHistoryStatsTests.cs. Test ComputeHistoryStats (static, dari 345-02) tanpa DbContext (pola CertificateStatusTests). Coverage WAJIB:

(a) Math passRate exclude-pending (D-11b):
  - mixed [pass, fail, null] -> total=3, graded=2, pending=1, passed=1, passRate=50.0
  - all-pass [pass, pass] -> passRate=100.0, graded=2
  - all-fail [fail, fail] -> passRate=0, graded=2 (BEDA dari all-pending: graded>0)
  - all-pending [null, null] -> total=2, graded=0, pending=2, passed=0, passRate=0 (D-05 guard: graded==0 tidak div-by-zero)
  - empty [] -> semua 0

(b) averageScore exclude pending (D-07):
  - [Item(true, 80), Item(false, 60), Item(null, 0)] -> averageScore = (80+60)/2 = 70.0 (BUKAN (80+60+0)/3 = 46.67). Buktikan pending TIDAK menyeret rata-rata.

(c) VM nullable mapping (D-11a):
  - [Fact] Item(null, 0).IsPassed == null (Assert.Null) - buktikan AssessmentReportItem.IsPassed=null tetap null setelah bool? ripple (BUKAN collapse false). Ini mengunci 345-02 Task 1.

(d) Group PassedCount regression-guard (C-3, D-10):
  - Karena group PassedCount = Count(a => a.IsPassed) atas bool-projected (sudah collapse null->false di L2712), test ini membuktikan SEMANTIK: list dengan 1 pass + 1 pending, kalau di-Count predicate (a.IsPassed == true) -> passed=1 (pending TIDAK terhitung). Reuse ComputeHistoryStats: [Item(true,80), Item(null,0)] -> passed=1, pending=1. Komentar: ini regression-guard untuk D-10 (group projection tidak menghitung pending sebagai passed - C-3 sudah benar by-construction).

Struktur (pola CertificateStatusTests):

    using System;
    using System.Collections.Generic;
    using HcPortal.Models;
    using HcPortal.Controllers;
    using Xunit;

    namespace HcPortal.Tests;

    public class AssessmentHistoryStatsTests
    {
        private static AssessmentReportItem Item(bool? isPassed, int score) =>
            new() { IsPassed = isPassed, Score = score };

        [Fact]
        public void ComputeHistoryStats_Mixed_PassRateExcludesPending()
        {
            var items = new List<AssessmentReportItem> { Item(true, 80), Item(false, 60), Item(null, 0) };
            var (total, graded, pending, passed, passRate, avg) = AssessmentAdminController.ComputeHistoryStats(items);
            Assert.Equal(3, total);
            Assert.Equal(2, graded);
            Assert.Equal(1, pending);
            Assert.Equal(1, passed);
            Assert.Equal(50.0, passRate);
            Assert.Equal(70.0, avg);   // D-07: (80+60)/2, pending excluded
        }

        [Fact]
        public void ComputeHistoryStats_AllPending_NoDivByZero_PassRateZero() { ... graded=0, passRate=0 ... }

        [Fact]
        public void ComputeHistoryStats_Empty_AllZero() { ... }

        [Fact]
        public void AssessmentReportItem_NullIsPassed_StaysNull()
        {
            var item = Item(null, 0);
            Assert.Null(item.IsPassed);   // D-11a: bool? ripple, tidak collapse false
        }

        [Fact]
        public void ComputeHistoryStats_PassPlusPending_PassedCountExcludesPending() { ... passed=1, pending=1 (C-3 guard) ... }
    }

Isi penuh semua [Fact] dengan Assert konkret (jangan tinggalkan ...). Tambahkan all-pass + all-fail case (boleh [Theory]+[InlineData] dengan tuple atau [Fact] terpisah). JANGAN literal "Menunggu Penilaian" - test math pakai angka. Run: dotnet test HcPortal.Tests.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; dotnet test HcPortal.Tests --filter "FullyQualifiedName~AssessmentHistoryStatsTests"</automated>
  </verify>
  <acceptance_criteria>
    - HcPortal.Tests/AssessmentHistoryStatsTests.cs exists with class AssessmentHistoryStatsTests
    - File contains [Fact] for: mixed passRate 50.0, all-pending passRate 0 (no div-by-zero), averageScore 70.0 (exclude pending), Assert.Null IsPassed (nullable mapping), pass+pending passed=1 (C-3 guard)
    - File calls: AssessmentAdminController.ComputeHistoryStats (static, from 345-02)
    - dotnet test HcPortal.Tests --filter AssessmentHistoryStatsTests exits 0 (all green)
    - dotnet test HcPortal.Tests (full suite) exits 0 (no regression to existing 52 tests)
  </acceptance_criteria>
  <done>xUnit class membuktikan passRate exclude-pending + all-pending guard + averageScore exclude + nullable mapping + C-3 guard; full suite hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright UAT 3 surface + SEED_WORKFLOW snapshot/restore (D-11, CMP06R-05 e2e)</name>
  <files>tests/e2e/assessment-pending-grade.spec.ts, tests/sql/pending345-seed.sql, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/export-per-peserta.spec.ts (analog FULL - loginAny inline + triggerDownload + HC skip pattern + coachee negative)
    - tests/e2e/global.setup.ts + tests/e2e/global.teardown.ts (pola backup->seed->journal active + restore->verify->journal cleaned)
    - tests/helpers/dbSnapshot.ts (signatures backup/restore/execScript/queryScalar/queryString)
    - tests/helpers/accounts.ts (admin/coachee credentials)
    - tests/sql/assessment-matrix-seed.sql (pola seed SQL existing - INSERT + cleanup prefix)
    - PATTERNS SEED_WORKFLOW integration + Playwright section (zip regex, HC broken)
    - docs/SEED_WORKFLOW.md + docs/SEED_JOURNAL.md (format tabel journal + klasifikasi)
  </read_first>
  <action>
2a. tests/sql/pending345-seed.sql - INSERT sesi Completed+IsPassed-null untuk user test, prefix Title '[PENDING345]'. Pakai user existing (resolve UserId via subquery, mis. admin atau coachee yang punya akses surface). Contoh:

    -- Seed sesi Completed + IsPassed=NULL (essay belum dinilai). Klasifikasi: temporary + local-only.
    -- Prefix '[PENDING345]' untuk Layer 1 seeded-check + Layer 4 cleanup verify. JANGAN ke Data/SeedData.cs.
    DECLARE @uid NVARCHAR(450) = (SELECT TOP 1 Id FROM AspNetUsers WHERE Email = 'rino.prasetyo@pertamina.com');
    INSERT INTO AssessmentSessions (UserId, Title, Category, Schedule, DurationMinutes, Status,
        Progress, BannerColor, PassPercentage, AllowAnswerReview, GenerateCertificate,
        IsPassed, CompletedAt, Score, IsTokenRequired, AccessToken)
    VALUES (@uid, '[PENDING345] Essay Pending', 'OJT', GETDATE(), 60, 'Completed',
        100, 'bg-primary', 70, 1, 0, NULL, GETDATE(), NULL, 0, '');

(Verifikasi kolom non-null actual di Models/AssessmentSession.cs saat eksekusi - sesuaikan bila ada kolom required lain. Pakai user yang surface-nya akan dibuka: coachee untuk RecordsWorkerDetail; untuk UserAssessmentHistory admin buka via userId coachee.)

2b. tests/e2e/assessment-pending-grade.spec.ts - spec 3 surface. Pakai test.beforeAll (backup + seed) + test.afterAll (restore finally + journal) - pola sederhana cukup untuk 1 spec (PATTERNS opsi b). Struktur:

    import { test, expect, Page } from '@playwright/test';
    import { accounts, AccountKey } from '../helpers/accounts';
    import * as db from '../helpers/dbSnapshot';
    import * as path from 'node:path';

    let snapshotPath: string;
    async function loginAny(page: Page, key: AccountKey) { /* inline - copy dari export-per-peserta.spec.ts */ }
    async function triggerDownload(page: Page, url: string) { /* copy dari export-per-peserta.spec.ts */ }

    test.beforeAll(async () => {
      const dir = (await db.queryString("SELECT CAST(SERVERPROPERTY('InstanceDefaultBackupPath') AS NVARCHAR(260))")).replace(/\\+$/,'').replace(/\\/g,'/');
      const ts = new Date().toISOString().replace(/[:.]/g,'-');
      snapshotPath = `${dir}/HcPortalDB_Dev-pre345-${ts}.bak`;
      await db.backup(snapshotPath);
      await db.execScript(path.resolve(__dirname, '../sql/pending345-seed.sql'));
      const n = await db.queryScalar("SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[[]PENDING345]%'");
      expect(n, 'Layer 1: pending session seeded').toBeGreaterThan(0);
    });

    test.afterAll(async () => {
      try { await db.restore(snapshotPath); }
      finally {
        const remaining = await db.queryScalar("SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[[]PENDING345]%'");
        // journal flip active->cleaned (manual edit di Task 2c; assert cleanup berhasil)
        expect(remaining, 'Layer 4: cleanup after restore').toBe(0);
      }
    });

    test('Worker Detail: badge amber Menunggu Penilaian visible', async ({ page }) => {
      await loginAny(page, 'coachee');
      await page.goto('/CMP/RecordsWorkerDetail');   // sesuaikan route/param actual saat eksekusi
      await expect(page.locator('.badge.bg-warning', { hasText: 'Menunggu Penilaian' }).first()).toBeVisible();
    });

    test('UserAssessmentHistory: badge amber + indikator pending visible', async ({ page }) => {
      await loginAny(page, 'admin');
      const uid = await db.queryString("SELECT TOP 1 Id FROM AspNetUsers WHERE Email='rino.prasetyo@pertamina.com'");
      await page.goto(`/Admin/UserAssessmentHistory?userId=${uid.trim()}`);
      await expect(page.locator('.badge.bg-warning', { hasText: 'Menunggu Penilaian' }).first()).toBeVisible();
    });

    test('Admin: BulkExportPdf -> .zip download sukses', async ({ page }) => {
      await loginAny(page, 'admin');
      const url = '/Admin/BulkExportPdf?title=' + encodeURIComponent('[PENDING345] Essay Pending') + '&category=OJT&scheduleDate=' + new Date().toISOString().slice(0,10);
      const download = await triggerDownload(page, url);
      const p = await download.path();
      expect(p).toBeTruthy();
      expect(download.suggestedFilename()).toMatch(/_Bundle\.zip$/);
      const fs = await import('node:fs');
      expect(fs.statSync(p!).size).toBeGreaterThan(512);
      // Label "Menunggu Penilaian" di dalam PDF-dalam-zip: verifikasi human/MCP (RESEARCH A3) - download-success = automated gate.
    });

HC login broken di test infra (PATTERNS) -> pakai admin untuk surface Admin-only (Roles="Admin, HC" -> admin cukup). Bila perlu test HC, test.skip + komentar alasan (pola Phase 320). Sesuaikan route/param actual (RecordsWorkerDetail mungkin butuh workerId; BulkExportPdf butuh title/category/scheduleDate yang match seed) saat eksekusi - dotnet run localhost:5277 untuk cek route.

2c. docs/SEED_JOURNAL.md - tambah SATU baris di tabel (jangan hapus baris lama - audit trail). Status 'active' saat seed jalan, ubah ke 'cleaned' setelah restore terverifikasi. Format:

    | 2026-06-04 | 345 | temporary + local-only | UAT CMP06R-05 badge "Menunggu Penilaian" 3 surface (RecordsWorkerDetail + UserAssessmentHistory + BulkExportPdf) - sesi Completed+IsPassed-null | AssessmentSessions(1+) prefix [PENDING345] Title='[PENDING345] Essay Pending' Status=Completed IsPassed=NULL | {InstanceDefaultBackupPath}/HcPortalDB_Dev-pre345-{ts}.bak | cleaned |

(Set 'cleaned' setelah test.afterAll restore sukses + Layer 4 remaining==0. Bila run gagal di tengah, journal tetap ditandai cleaned HANYA setelah restore benar-benar jalan - restore di finally menjamin ini.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests"; npx playwright test assessment-pending-grade.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - tests/sql/pending345-seed.sql exists, contains [PENDING345] AND IsPassed (NULL) AND Status (Completed)
    - tests/e2e/assessment-pending-grade.spec.ts exists, contains: dbSnapshot import, test.beforeAll backup+execScript, test.afterAll restore (finally), _Bundle\.zip regex, 'Menunggu Penilaian' DOM assert (>=2 surface)
    - docs/SEED_JOURNAL.md contains new row: phase 345, temporary + local-only, [PENDING345], status cleaned
    - npx playwright test assessment-pending-grade.spec.ts: 3 surface tests PASS (or HC-related skip documented); SEED restore verified (Layer 4 remaining==0)
    - Post-run: SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[[]PENDING345]%' returns 0 (DB lokal bersih)
  </acceptance_criteria>
  <done>Playwright UAT badge amber visible di RecordsWorkerDetail + UserAssessmentHistory; BulkExportPdf .zip download sukses; SEED snapshot/restore clean; journal cleaned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test seed -> DB lokal | Task 2 INSERT baris AssessmentSessions ([PENDING345] Completed+IsPassed-null) ke HcPortalDB_Dev lokal untuk UAT. Boundary: seed temporary tidak boleh bocor ke Dev/Prod atau nempel di DB lokal lewat sesi kerja. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-345-04-01 | Tampering (data pollution) | Seed [PENDING345] di DB lokal | mitigate | SEED_WORKFLOW WAJIB: db.backup(snapshotPath) di test.beforeAll SEBELUM seed; db.restore(snapshotPath) di test.afterAll finally (sukses ATAU gagal -> restore tetap jalan); Layer 4 assert remaining==0 setelah restore. Prefix '[PENDING345]' untuk identifikasi + cleanup verify. Klasifikasi temporary+local-only - JANGAN ke Data/SeedData.cs. Journal active->cleaned audit trail. dbSnapshot.ts guard reject non-localhost (CLAUDE.md baked-in) -> seed TIDAK bisa kena Dev/Prod. |
| T-345-04-02 | Information Disclosure | Seed user real (coachee email) | accept | Seed pakai user existing (rino.prasetyo) bukan PII baru; data sesi dummy ([PENDING345] Essay Pending) tanpa konten sensitif; restore menghapus seluruh seed. Lokal-only, tidak ter-commit ke DB. |
| T-345-04-03 | Denial of Service | BulkExportPdf di UAT | accept | UAT trigger 1 export atas seed kecil (<50 peserta DoS guard existing tidak terlampaui). Tidak mengubah endpoint. |
| T-345-04-04 | (S/R/E) | - | N/A | Test-only; tidak ada autentikasi/non-repudiation/elevation surface baru. Login pakai akun test existing (accounts.ts). |
</threat_model>

<verification>
- dotnet test HcPortal.Tests exits 0 (AssessmentHistoryStatsTests + 52 existing, no regression).
- npx playwright test assessment-pending-grade.spec.ts: badge amber 'Menunggu Penilaian' visible (DOM) di RecordsWorkerDetail + UserAssessmentHistory; BulkExportPdf _Bundle.zip download size>0.
- SEED_WORKFLOW: snapshot dibuat -> seed [PENDING345] inserted (Layer 1 count>0) -> restore di finally -> Layer 4 remaining==0; journal entry status=cleaned.
- Label "Menunggu Penilaian" di dalam PDF (di dalam .zip) di-verify human/MCP (RESEARCH A3) - tidak diblok untuk automated gate.
- Bila HC login broken (PATTERNS) -> admin dipakai untuk surface Admin; skip HC didokumentasikan (bukan bug Phase 345).
</verification>

<success_criteria>
- ROADMAP SC4 (sebagian): dotnet test hijau + test baru passRate (exclude-pending math).
- ROADMAP SC5: Playwright UAT 3 surface PASS (SEED_WORKFLOW snapshot/restore).
- D-11a: VM nullable mapping test (IsPassed=null tetap null).
- D-11b: passRate exclude-pending math test (termasuk all-pending guard).
- D-07: averageScore exclude-pending test.
- C-3/D-10: group PassedCount regression-guard test (pending tidak dihitung passed).
- SEED_WORKFLOW: journal entry temporary+local-only cleaned; DB lokal bersih post-run.
</success_criteria>

<output>
After completion, create `.planning/phases/345-assessment-pending-grade-display-fix/345-04-SUMMARY.md`
</output>
