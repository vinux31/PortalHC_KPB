---
phase: 346-cmp-records-detail-search-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/Records.cshtml
autonomous: true
requirements: [REC-01, REC-02]
must_haves:
  truths:
    - "Tabel My Records punya kolom header 'Aksi' (kolom ke-7)"
    - "Row Assessment Online menampilkan tombol 'Lihat Hasil' yang menuju /CMP/Results"
    - "Row Training menampilkan tombol 'Detail' yang membuka modal detail training"
    - "Modal training menampilkan Penyelenggara, Kota, Tanggal, Nomor Sertifikat, Kategori, SubKategori, Status, ValidUntil/CertificateType, dan tombol PDF bila SertifikatUrl ada"
    - "Empty-state 'Data belum ada' tetap rapi (colspan benar) saat tabel kosong"
    - "Row assessment tetap clickable (data-href existing tidak rusak)"
  artifacts:
    - path: "Views/CMP/Records.cshtml"
      provides: "Kolom Aksi + tombol Lihat Hasil/Detail + trainingDetailModal + JS handler"
      contains: "trainingDetailModal"
  key_links:
    - from: "Views/CMP/Records.cshtml tombol Lihat Hasil"
      to: "/CMP/Results"
      via: "Url.Action('Results','CMP', new { id = item.AssessmentSessionId.Value })"
      pattern: "Url.Action\\(\"Results\""
    - from: "Views/CMP/Records.cshtml tombol Detail"
      to: "#trainingDetailModal"
      via: "data-bs-toggle modal + show.bs.modal handler membaca dataset"
      pattern: "data-bs-target=\"#trainingDetailModal\""
---

<objective>
Menambah kolom "Aksi" pada tabel My Records (`/CMP/Records`) dengan tombol eksplisit per baris: Assessment Online → `Lihat Hasil` (menuju halaman Results), Training Manual → `Detail` (membuka modal detail). Modal di-port dari `RecordsWorkerDetail.cshtml` dan diperkaya dengan field Kategori/SubKategori/Status/ValidUntil/CertificateType + tombol PDF. Row tetap clickable (D-03). Tanpa perubahan controller/service — semua data sudah ada di `UnifiedTrainingRecord`.

Purpose: Discoverability — saat ini pekerja tidak punya affordance eksplisit untuk membuka hasil assessment atau detail training dari My Records (REC-01/REC-02, finding FUG-001/FEAT-1A, severity MED).
Output: `Views/CMP/Records.cshtml` ter-update dengan kolom Aksi, modal, dan JS handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/346-cmp-records-detail-search-logic/346-CONTEXT.md
@.planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md

<interfaces>
<!-- Field UnifiedTrainingRecord yang dipakai modal (Models/UnifiedTrainingRecord.cs) — semua sudah dalam scope Records.cshtml model (CMPRecordsViewModel.UnifiedRecords). NO controller change. -->
Penyelenggara (string?), CertificateType (string?), ValidUntil (DateOnly?), Status (string?),
SertifikatUrl (string?), Kategori (string?), SubKategori (string?), Kota (string?),
NomorSertifikat (string?), TanggalMulai (DateTime?), TanggalSelesai (DateTime?),
Title (string), RecordType (string), AssessmentSessionId (int?)

<!-- Pola Results URL SUDAH ADA di Records.cshtml L162-164 (variabel resultsUrl per-row, reuse). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah kolom "Aksi" + tombol Lihat Hasil/Detail + fix colspan</name>
  <read_first>
    - Views/CMP/Records.cshtml — BACA UTUH dulu (state current pasca-Phase-345). Fokus: thead L150-157 (6 kolom), assessment-row resultsUrl L162-169, training SertifikatUrl button L208-210, empty-state L227, JS empty-state L381.
    - Views/CMP/RecordsWorkerDetail.cshtml L250-279 — analog kolom Aksi (tombol pattern yang di-port).
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-01" (excerpt thead + tombol + colspan pitfall).
  </read_first>
  <action>
    **RE-GREP dulu** `colspan="6"` di Records.cshtml untuk konfirmasi 2 lokasi (PATTERNS verified L227 + L381, tapi wajib re-grep sebelum edit per STALE-LINE WARNING).

    1. **thead (L150-157)** — tambah header kolom ke-7 setelah `<th class="p-3 text-center">Sertifikat</th>`:
    ```html
    <th class="p-3 text-center">Aksi</th>
    ```

    2. **Per-row sel Aksi** — tambah `<td>` baru di akhir setiap `<tr>` baris data (setelah sel Sertifikat existing). Logika per RecordType:
    ```html
    <td class="p-3 text-center">
        @if (item.RecordType == "Assessment Online" && item.AssessmentSessionId.HasValue)
        {
            <a href="@Url.Action("Results", "CMP", new { id = item.AssessmentSessionId.Value })"
               class="btn btn-sm btn-outline-primary">
                <i class="bi bi-bar-chart-line me-1"></i>Lihat Hasil
            </a>
        }
        else
        {
            <button type="button" class="btn btn-sm btn-outline-info"
                    data-bs-toggle="modal" data-bs-target="#trainingDetailModal"
                    data-title="@item.Title"
                    data-penyelenggara="@(item.Penyelenggara ?? "—")"
                    data-kota="@(item.Kota ?? "—")"
                    data-tanggal-mulai="@(item.TanggalMulai.HasValue ? item.TanggalMulai.Value.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID")) : "—")"
                    data-tanggal-selesai="@(item.TanggalSelesai.HasValue ? item.TanggalSelesai.Value.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID")) : "—")"
                    data-nomor-sertifikat="@(item.NomorSertifikat ?? "—")"
                    data-kategori="@(item.Kategori ?? "—")"
                    data-subcategory="@(item.SubKategori ?? "—")"
                    data-status="@(item.Status ?? "—")"
                    data-validuntil="@(item.ValidUntil.HasValue ? item.ValidUntil.Value.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID")) : "—")"
                    data-certtype="@(item.CertificateType ?? "—")"
                    data-pdf="@(item.SertifikatUrl ?? "")">
                <i class="bi bi-info-circle me-1"></i>Detail
            </button>
        }
    </td>
    ```
    Catatan: jika `CultureInfo` belum di-`@using`, pakai `System.Globalization.CultureInfo.GetCultureInfo("id-ID")` (cek apakah `@using System.Globalization` sudah ada di atas file; RecordsWorkerDetail.cshtml memakainya — ikuti pola file yang sama).

    3. **COLSPAN PITFALL (wajib, 2 lokasi)** — ubah `colspan="6"` → `colspan="7"`:
       - Razor empty-state (~L227): `<tr><td colspan="6" ...>Data belum ada</td></tr>` → `colspan="7"`.
       - JS-injected empty-state (~L381): `tr.innerHTML = '<td colspan="6" ...>Data belum ada</td>';` → `colspan="7"`.

    4. **Row clickable existing** — JANGAN sentuh `data-href` / `.training-row` keyboard-nav handler (L414-422). Tombol Aksi adalah affordance tambahan, bukan pengganti. Pastikan tombol punya `onclick` stopPropagation TIDAK perlu (link/modal-button native; tapi jika row click handler menelan klik tombol, tambahkan `event.stopPropagation()` — verifikasi saat UAT, default biarkan native).

    Kolom "Sertifikat" existing dipertahankan apa adanya.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` Records.cshtml: thead berisi `<th class="p-3 text-center">Aksi</th>` (kolom ke-7).
    - `grep` Records.cshtml: ada `Url.Action("Results", "CMP"` di sel Aksi assessment.
    - `grep` Records.cshtml: ada `data-bs-target="#trainingDetailModal"` pada tombol Detail training.
    - `grep` Records.cshtml: TIDAK ADA lagi `colspan="6"` (kedua jadi `colspan="7"`); `grep` `colspan="7"` mengembalikan ≥2 match.
    - `dotnet build` exit 0 (Build succeeded, 0 error).
  </acceptance_criteria>
  <done>Kolom Aksi tampil dengan tombol Lihat Hasil (assessment) / Detail (training); empty-state colspan benar 7; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Port trainingDetailModal + perkaya field + JS handler</name>
  <read_first>
    - Views/CMP/Records.cshtml — state setelah Task 1 (sudah ada tombol Detail dengan data-* attrs).
    - Views/CMP/RecordsWorkerDetail.cshtml L289-309 (modal HTML), L440-449 (JS show.bs.modal handler) — analog yang di-port.
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-02" (excerpt modal + handler + PDF button).
  </read_first>
  <action>
    **RE-GREP** lokasi modal & handler di RecordsWorkerDetail.cshtml (PATTERNS L289-309/L440-449, geser ±1-2 pasca-345). Port ke Records.cshtml.

    1. **Modal HTML** — tambah di akhir body Records.cshtml (sebelum `@section Scripts` atau setelah tabel; ikuti penempatan modal di RecordsWorkerDetail). Sertakan 5 row baru (Kategori/SubKategori/Status/Valid Until/Certificate Type) + blok PDF:
    ```html
    <!-- Training Detail Modal (REC-02) -->
    <div class="modal fade" id="trainingDetailModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modalTrainingTitle">Detail Training</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-5">Nama Kegiatan</dt><dd class="col-sm-7" id="mdTitle">—</dd>
                        <dt class="col-sm-5">Penyelenggara</dt><dd class="col-sm-7" id="mdPenyelenggara">—</dd>
                        <dt class="col-sm-5">Kota</dt><dd class="col-sm-7" id="mdKota">—</dd>
                        <dt class="col-sm-5">Tanggal Mulai</dt><dd class="col-sm-7" id="mdTanggalMulai">—</dd>
                        <dt class="col-sm-5">Tanggal Selesai</dt><dd class="col-sm-7" id="mdTanggalSelesai">—</dd>
                        <dt class="col-sm-5">Nomor Sertifikat</dt><dd class="col-sm-7" id="mdNomorSertifikat">—</dd>
                        <dt class="col-sm-5">Kategori</dt><dd class="col-sm-7" id="mdKategori">—</dd>
                        <dt class="col-sm-5">Sub Kategori</dt><dd class="col-sm-7" id="mdSubKategori">—</dd>
                        <dt class="col-sm-5">Status</dt><dd class="col-sm-7" id="mdStatus">—</dd>
                        <dt class="col-sm-5">Valid Until</dt><dd class="col-sm-7" id="mdValidUntil">—</dd>
                        <dt class="col-sm-5">Certificate Type</dt><dd class="col-sm-7" id="mdCertType">—</dd>
                    </dl>
                    <div class="mt-3" id="mdPdfWrap" style="display:none;">
                        <a id="mdPdfLink" href="#" target="_blank" rel="noopener" class="btn btn-sm btn-outline-primary">
                            <i class="bi bi-file-earmark-pdf me-1"></i>Lihat Sertifikat
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    ```

    2. **JS handler** — tambah di `@section Scripts` (atau blok script existing). Baca `event.relatedTarget.dataset.*` (camelCase dari attribute kebab-case):
    ```javascript
    var trainingModal = document.getElementById('trainingDetailModal');
    if (trainingModal) {
        trainingModal.addEventListener('show.bs.modal', function (event) {
            var btn = event.relatedTarget;
            var d = btn.dataset;
            document.getElementById('modalTrainingTitle').textContent = d.title || 'Detail Training';
            document.getElementById('mdTitle').textContent = d.title || '—';
            document.getElementById('mdPenyelenggara').textContent = d.penyelenggara || '—';
            document.getElementById('mdKota').textContent = d.kota || '—';
            document.getElementById('mdTanggalMulai').textContent = d.tanggalMulai || '—';
            document.getElementById('mdTanggalSelesai').textContent = d.tanggalSelesai || '—';
            document.getElementById('mdNomorSertifikat').textContent = d.nomorSertifikat || '—';
            document.getElementById('mdKategori').textContent = d.kategori || '—';
            document.getElementById('mdSubKategori').textContent = d.subcategory || '—';
            document.getElementById('mdStatus').textContent = d.status || '—';
            document.getElementById('mdValidUntil').textContent = d.validuntil || '—';
            document.getElementById('mdCertType').textContent = d.certtype || '—';
            var pdfWrap = document.getElementById('mdPdfWrap');
            var pdfLink = document.getElementById('mdPdfLink');
            if (d.pdf && d.pdf.length > 0) {
                pdfLink.href = d.pdf;
                pdfWrap.style.display = '';
            } else {
                pdfWrap.style.display = 'none';
            }
        });
    }
    ```
    Catatan dataset: attribute `data-subcategory` → `dataset.subcategory`; `data-validuntil` → `dataset.validuntil`; `data-certtype` → `dataset.certtype`; `data-tanggal-mulai` → `dataset.tanggalMulai` (kebab ke camel). Pakai persis key di atas.

    D-02 (CONTEXT): tampilkan **BOTH** ValidUntil AND CertificateType bila tersedia (bukan either/or) — kedua row selalu ada, isi "—" jika kosong.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` Records.cshtml: ada `id="trainingDetailModal"` + 11 `id="md*"` elemen (`mdTitle`, `mdPenyelenggara`, `mdKota`, `mdTanggalMulai`, `mdTanggalSelesai`, `mdNomorSertifikat`, `mdKategori`, `mdSubKategori`, `mdStatus`, `mdValidUntil`, `mdCertType`).
    - `grep` Records.cshtml: ada `id="mdPdfWrap"` + `id="mdPdfLink"` dengan `rel="noopener"`.
    - `grep` Records.cshtml: ada `addEventListener('show.bs.modal'` yang membaca `event.relatedTarget`.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>Modal training lengkap (11 field + PDF toggle) ter-port ke Records.cshtml; handler JS membaca data-* attrs; build hijau. (UAT browser di Plan 05.)</done>
</task>

</tasks>

<verification>
- `dotnet build` 0 error.
- `grep` konfirmasi kolom Aksi, modal, colspan=7, tombol Lihat Hasil + Detail.
- Verifikasi visual (klik tombol → modal/Results) ditunda ke Plan 05 (Playwright UAT).
</verification>

<success_criteria>
My Records menampilkan kolom "Aksi" dengan tombol `Lihat Hasil` (assessment → Results) dan `Detail` (training → modal lengkap 11 field + PDF); empty-state colspan benar; row tetap clickable; build hijau. (SC #1 ROADMAP §Phase 346.)
</success_criteria>

<output>
After completion, create `.planning/phases/346-cmp-records-detail-search-logic/346-01-SUMMARY.md`
</output>
