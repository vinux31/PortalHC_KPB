---
phase: 346-cmp-records-detail-search-logic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/RecordsWorkerDetail.cshtml
autonomous: true
requirements: [REC-03, REC-05]
must_haves:
  truths:
    - "Setiap baris Assessment Online di Worker Detail punya tombol 'Lihat Hasil' menuju /CMP/Results"
    - "Tombol Lihat Hasil muncul untuk SEMUA assessment yang submitted (tidak digate GenerateCertificate)"
    - "Tombol Sertifikat existing tetap muncul bila GenerateCertificate true (tidak rusak)"
    - "Modal training Worker Detail menampilkan row Kategori dan SubKategori"
  artifacts:
    - path: "Views/CMP/RecordsWorkerDetail.cshtml"
      provides: "Tombol Lihat Hasil di action column + Kategori/SubKategori di trainingDetailModal"
      contains: "Lihat Hasil"
  key_links:
    - from: "Views/CMP/RecordsWorkerDetail.cshtml tombol Lihat Hasil"
      to: "/CMP/Results"
      via: "asp-action Results asp-route-id AssessmentSessionId"
      pattern: "asp-action=\"Results\""
    - from: "Worker Detail modal data-kategori/data-subcategory"
      to: "#trainingDetailModal mdKategori/mdSubKategori"
      via: "JS show.bs.modal handler"
      pattern: "data-kategori"
---

<objective>
Pada halaman Worker Detail (`/CMP/RecordsWorkerDetail`): (REC-03) menambah tombol `Lihat Hasil` → `/CMP/Results` di setiap baris Assessment Online (di samping tombol Sertifikat existing), dan (REC-05) memperkaya modal training existing dengan row Kategori + SubKategori. Keduanya view-only, tanpa perubahan controller/service — data sudah ada di baris tabel.

Purpose: Atasan/HC yang men-drill-down ke Worker Detail butuh akses langsung ke halaman hasil assessment anggota (REC-03, finding FUG-002/FEAT-1B, MED) + kelengkapan info training di modal (REC-05, LOW). Tombol `Lihat Hasil` baru akan benar-benar ter-authorize untuk L3/L4 setelah REC-04 (Plan 03) — sinergi AUTHZ-01 side-fix.
Output: `Views/CMP/RecordsWorkerDetail.cshtml` ter-update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/346-cmp-records-detail-search-logic/346-CONTEXT.md
@.planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah tombol "Lihat Hasil" di action column Assessment</name>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml — BACA UTUH (state pasca-345). Fokus: action column L250-279 (saat ini assessment hanya render tombol Sertifikat L271-275, digate `item.GenerateCertificate`), Training branch wrapper `<div class="d-flex gap-1 justify-content-center">` (L252).
    - Views/CMP/Records.cshtml L162-169 — analog Results URL (untuk konsistensi route param).
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-03" (excerpt + GATING DIFFERENCE note).
  </read_first>
  <action>
    **RE-GREP** action column RecordsWorkerDetail.cshtml (PATTERNS L250-279, geser +2 pasca-345). Identifikasi cabang `else if (item.GenerateCertificate && item.AssessmentSessionId.HasValue)` (~L271).

    **GATING DIFFERENCE (kritis, PATTERNS note):** tombol Sertifikat existing digate `item.GenerateCertificate`. Tombol `Lihat Hasil` BARU TIDAK BOLEH digate `GenerateCertificate` — setiap assessment submitted punya halaman Results (termasuk yang non-sertifikat). Gate HANYA pada `item.RecordType == "Assessment Online" && item.AssessmentSessionId.HasValue`.

    Restrukturisasi: bungkus tombol assessment dalam `<div class="d-flex gap-1 justify-content-center">` (seperti Training branch L252). Tombol `Lihat Hasil` selalu muncul untuk assessment; tombol `Sertifikat` tetap kondisional `GenerateCertificate`:
    ```csharp
    @if (item.RecordType == "Assessment Online" && item.AssessmentSessionId.HasValue)
    {
        <div class="d-flex gap-1 justify-content-center">
            <a asp-action="Results" asp-controller="CMP" asp-route-id="@item.AssessmentSessionId.Value"
               class="btn btn-sm btn-outline-primary">
                <i class="bi bi-bar-chart-line me-1"></i>Lihat Hasil
            </a>
            @if (item.GenerateCertificate)
            {
                <a asp-action="Certificate" asp-controller="CMP" asp-route-id="@item.AssessmentSessionId.Value"
                   class="btn btn-sm btn-outline-primary" target="_blank">
                    <i class="bi bi-award me-1"></i>Sertifikat
                </a>
            }
        </div>
    }
    ```
    Pertahankan cabang Training existing apa adanya. Pertahankan fallback `<span class="text-muted">—</span>` untuk row yang bukan assessment & bukan training-detail.

    **returnUrl (REC-03 spec L65):** action `Results` saat ini TIDAK menerima param returnUrl. Keputusan: ANDALKAN browser back-button (breadcrumb back-link existing L40-50 sudah round-trip filterState). JANGAN tambah param returnUrl ke action `Results` di plan ini (akan ubah signature + semua caller; out of scope, dan browser-back cukup per D-03). Dokumentasikan di SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` RecordsWorkerDetail.cshtml: ada `asp-action="Results"` dengan `bi-bar-chart-line` + teks `Lihat Hasil`.
    - `grep` RecordsWorkerDetail.cshtml: tombol Lihat Hasil TIDAK berada di dalam blok `if (item.GenerateCertificate)` (gate hanya RecordType+AssessmentSessionId) — verifikasi via read konteks.
    - Tombol Sertifikat existing (`asp-action="Certificate"` + `bi-award`) MASIH ADA dan tetap digate `GenerateCertificate`.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>Setiap baris assessment punya tombol Lihat Hasil (tidak digate sertifikat); tombol Sertifikat tetap utuh; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Tambah Kategori + SubKategori ke modal training</name>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml — state setelah Task 1. Fokus: modal `<dl>` L298-305 (6 row existing), Detail-button data-* attrs L253-264, JS modal handler L440-449, kolom tabel `@item.Kategori` L223 + `@item.SubKategori` L224.
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-05" (excerpt).
  </read_first>
  <action>
    **RE-GREP** modal `<dl>`, Detail-button, dan JS handler (PATTERNS L298-305/L253-264/L440-449, geser +2 pasca-345). Tiga edit terkoordinasi (mekanisme sama persis dgn 6 field existing):

    1. **`<dl>` modal** — tambah 2 row setelah row Nomor Sertifikat:
    ```html
    <dt class="col-sm-5">Kategori</dt><dd class="col-sm-7" id="mdKategori">—</dd>
    <dt class="col-sm-5">Sub Kategori</dt><dd class="col-sm-7" id="mdSubKategori">—</dd>
    ```

    2. **Detail button** (L253-264) — tambah 2 data-* attr:
    ```html
    data-kategori="@(item.Kategori ?? "—")"
    data-subcategory="@(item.SubKategori ?? "—")"
    ```

    3. **JS handler** (L440-449) — tambah 2 baris assignment:
    ```javascript
    document.getElementById('mdKategori').textContent = btn.dataset.kategori || '—';
    document.getElementById('mdSubKategori').textContent = btn.dataset.subcategory || '—';
    ```
    (gunakan id elemen yang sama dengan yang sudah dipakai handler existing; sesuaikan jika handler memakai variabel `event.relatedTarget` langsung).

    NO controller/service change — `item.Kategori`/`item.SubKategori` sudah ada di baris (L223-224).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` RecordsWorkerDetail.cshtml: ada `id="mdKategori"` + `id="mdSubKategori"` di modal `<dl>`.
    - `grep` RecordsWorkerDetail.cshtml: tombol Detail punya `data-kategori` + `data-subcategory`.
    - `grep` RecordsWorkerDetail.cshtml: JS handler set `mdKategori` + `mdSubKategori` dari `dataset`.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>Modal training Worker Detail menampilkan Kategori + SubKategori; build hijau. (UAT browser di Plan 05.)</done>
</task>

</tasks>

<verification>
- `dotnet build` 0 error.
- `grep` konfirmasi tombol Lihat Hasil (un-gated dari sertifikat) + 2 row modal baru.
- Verifikasi authorize L3/L4 (tombol benar-benar buka Results) + UAT visual ditunda ke Plan 05 (depends REC-04).
</verification>

<success_criteria>
Worker Detail: baris Assessment punya tombol `Lihat Hasil` → `/CMP/Results`; modal training tambah Kategori/SubKategori; build hijau. (SC #2 ROADMAP §Phase 346.)
</success_criteria>

<output>
After completion, create `.planning/phases/346-cmp-records-detail-search-logic/346-02-SUMMARY.md`
</output>
