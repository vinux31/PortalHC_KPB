---
phase: 346-cmp-records-detail-search-logic
plan: 04
type: execute
wave: 2
depends_on: [346-03]
files_modified:
  - Services/IWorkerDataService.cs
  - Services/WorkerDataService.cs
  - Controllers/CMPController.cs
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements: [REC-06]
must_haves:
  truths:
    - "Team View punya input pencarian + selektor scope (Nama/Training/Keduanya, default Keduanya)"
    - "Scope Nama menyaring worker berdasarkan FullName/NIP (SQL)"
    - "Scope Training menyaring worker yang punya TrainingRecords.Judul cocok (in-memory)"
    - "Scope Keduanya menyaring worker yang cocok Nama/NIP ATAU punya training cocok (union)"
    - "Search jalan lintas pagination (server-side via RecordsTeamPartial)"
    - "Export Assessment & Training ikut ter-filter oleh search + searchScope"
    - "Caller lain GetWorkersInSection tanpa searchScope tetap jalan (backward-compat)"
  artifacts:
    - path: "Services/WorkerDataService.cs"
      provides: "GetWorkersInSection + param searchScope + cabang Nama/Training/Keduanya"
      contains: "searchScope"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "input teamSearch + select searchScope + wiring 5 fungsi JS"
      contains: "searchScope"
  key_links:
    - from: "RecordsTeam.cshtml doFetch/updateExportLinks"
      to: "RecordsTeamPartial + Export endpoints"
      via: "URLSearchParams set search + searchScope"
      pattern: "searchScope"
    - from: "CMPController RecordsTeamPartial/Export"
      to: "WorkerDataService.GetWorkersInSection"
      via: "forward search + searchScope arg"
      pattern: "GetWorkersInSection\\("
---

<objective>
Menambah pencarian adaptif di Team View (`/CMP/RecordsTeam`): 1 input + selektor scope (Nama / Training / Keduanya, default Keduanya), server-side, jalan lintas pagination, dan export (Assessment + Training) ikut ter-filter (REC-06, D-05/D-07). Rantai penuh: UI/JS (`RecordsTeam.cshtml`) → controller (`RecordsTeamPartial` + 2 Export) → service (`GetWorkersInSection` + interface).

Purpose: Saat ini Team View tidak punya pencarian — atasan dengan banyak anggota sulit menemukan worker/training spesifik (REC-06, finding FUG-003/FILTER-001/FILTER-002/FEAT-2, MED). Strategi query Training = POST-LOAD filter (D-07 locked) mengikuti pola category-narrow existing.
Output: 4 file ter-update (interface + service + controller + view).

**Sequencing:** Wave 2, depends_on 346-03 — karena menyentuh `CMPController.cs` (region L652/704/753) yang juga disentuh Plan 03 (region authz L1815-2184). Region berbeda tapi file sama → serialize. Test REC-06 (searchScope matrix) ada di Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/346-cmp-records-detail-search-logic/346-CONTEXT.md
@.planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md

<interfaces>
<!-- Signature service current (WorkerDataService.cs L242 / IWorkerDataService.cs L14) -->
Task<List<WorkerTrainingStatus>> GetWorkersInSection(
    string? section, string? unitFilter = null, string? category = null,
    string? search = null, string? statusFilter = null,
    DateTime? dateFrom = null, DateTime? dateTo = null, string? subCategory = null);
// REC-06: tambah trailing param `, string? searchScope = null` (backward-compat).

<!-- WorkerTrainingStatus.TrainingRecords (per-worker, dibangun L346 dari trainingsByUser L277-279).
     Tiap TrainingRecord punya .Judul (string) untuk Training-scope filter. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service — GetWorkersInSection + searchScope (Nama/Training/Keduanya)</name>
  <read_first>
    - Services/WorkerDataService.cs — BACA UTUH method GetWorkersInSection (L242-~400). Fokus: signature L242, name-filter SQL L255-262, trainingsByUser L277-279, build workerList L314-346, category POST-LOAD narrow L370-388 (model D-07).
    - Services/IWorkerDataService.cs L14 — signature interface (mirror).
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-06 / 6d" (D-07 IMPLEMENTATION GUIDANCE locked, 4 bullet).
  </read_first>
  <behavior>
    - searchScope="Nama", search="budi": hanya worker dengan FullName/NIP mengandung "budi" muncul (SQL pre-narrow).
    - searchScope="Training", search="k3": hanya worker yang punya TrainingRecords.Any(Judul contains "k3") muncul; worker yang namanya "k3" tapi tanpa training cocok TIDAK muncul.
    - searchScope="Keduanya", search="k3": worker dengan Nama/NIP "k3" ATAU punya training Judul "k3" muncul (union).
    - searchScope=null (caller lama, tanpa search): perilaku identik dengan sebelum REC-06 (tidak ada filter search). [PATTERNS: blok SQL name-filter L255-262 hanya jalan saat scope=="Nama".]
    - search kosong/null (scope apa pun): tidak menyaring.
    - Case-insensitive: "K3" dan "k3" memberi hasil sama (gunakan .ToLower()/OrdinalIgnoreCase konsisten).
  </behavior>
  <action>
    **RE-GREP** signature L242 + name-filter L255-262 + category-narrow L370-388.

    1. **Signature** — tambah trailing param di IMPL (`WorkerDataService.cs:242`) DAN interface (`IWorkerDataService.cs:14`):
    ```csharp
    ..., string? subCategory = null, string? searchScope = null)
    ```

    2. **Guard blok SQL name-filter existing (L255-262)** — bungkus agar HANYA pre-narrow untuk scope "Nama" (jika tidak, "Keduanya"/"Training" akan keliru buang training-only match). Ubah kondisi:
    ```csharp
    // REC-06 D-07: SQL name pre-narrow HANYA untuk scope "Nama"
    if (searchScope == "Nama" && !string.IsNullOrEmpty(search))
    {
        var searchLower = search.ToLower();
        usersQuery = usersQuery.Where(u =>
            u.FullName.ToLower().Contains(searchLower) ||
            (u.NIP != null && u.NIP.Contains(searchLower))
        );
    }
    ```
    (Pertahankan variabel/logika di dalam apa adanya; hanya tambah kondisi `searchScope == "Nama" &&` ke guard luar.)

    3. **Blok POST-LOAD baru** — tambah SETELAH workerList dibangun & category-narrow (setelah L388), MIRROR bentuk category-narrow. Tangani Training + Keduanya:
    ```csharp
    // REC-06 D-07: Training/Keduanya search = post-load in-memory filter
    if (!string.IsNullOrEmpty(search) && (searchScope == "Training" || searchScope == "Keduanya"))
    {
        var searchLower = search.ToLower();
        workerList = workerList.Where(w =>
        {
            bool trainingMatch = w.TrainingRecords != null &&
                w.TrainingRecords.Any(t => !string.IsNullOrEmpty(t.Judul) &&
                                           t.Judul.ToLower().Contains(searchLower));
            if (searchScope == "Training") return trainingMatch;
            // Keduanya: union Nama/NIP OR Training
            bool nameMatch =
                (!string.IsNullOrEmpty(w.WorkerName) && w.WorkerName.ToLower().Contains(searchLower)) ||
                (!string.IsNullOrEmpty(w.NIP) && w.NIP.ToLower().Contains(searchLower));
            return nameMatch || trainingMatch;
        }).ToList();
    }
    ```
    **Verifikasi nama property** WorkerTrainingStatus saat read_first: gunakan nama field yang BENAR untuk nama worker (`WorkerName` per PATTERNS; bila berbeda di file, pakai yang aktual — mis. `FullName`) + NIP. Jangan asumsikan buta — cek `Models/WorkerTrainingStatus.cs`.

    **Catatan "Keduanya" + SQL:** karena scope "Keduanya" TIDAK masuk guard SQL (step 2 hanya "Nama"), full section ter-load dulu lalu union in-memory — sesuai D-07 (SQL name pre-narrow akan buang training-only match).

    Semantik (spec L97): search menyaring WORKER MANA yang muncul; badge count per-worker (`CompletedAssessments`/`CompletedTrainings`) tetap whole-record.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` IWorkerDataService.cs: signature GetWorkersInSection diakhiri `string? searchScope = null`.
    - `grep` WorkerDataService.cs: signature impl diakhiri `string? searchScope = null`.
    - `grep` WorkerDataService.cs: blok SQL name-filter dibungkus `searchScope == "Nama"`.
    - `grep` WorkerDataService.cs: ada blok post-load `searchScope == "Training" || searchScope == "Keduanya"` yang membaca `t.Judul`.
    - `dotnet build` exit 0. (Matrix scope test di Plan 06.)
  </acceptance_criteria>
  <done>GetWorkersInSection menerima searchScope dengan 3 cabang (Nama SQL / Training post-load / Keduanya union); backward-compat null; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Controller — wire searchScope ke RecordsTeamPartial + 2 Export</name>
  <read_first>
    - Controllers/CMPController.cs — Fokus: RecordsTeamPartial L753-770 (TIDAK punya param `search`, passes `null` ke service L770), ExportRecordsTeamAssessment L652 (panggil svc L670), ExportRecordsTeamTraining L704 (panggil svc L722). Kedua Export SUDAH punya `search`.
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-06 / 6c" (3 correction: RecordsTeamPartial tambah BOTH search+searchScope; Export tambah searchScope only).
  </read_first>
  <action>
    **RE-GREP** signature 3 action + 3 pemanggilan GetWorkersInSection (L670/L722/L770).

    1. **RecordsTeamPartial (L753-756)** — saat ini TIDAK punya `search`. Tambah BOTH `search` dan `searchScope` di signature:
    ```csharp
    public async Task<IActionResult> RecordsTeamPartial(
        string? section, string? unit, string? category, string? subCategory,
        string? statusFilter, string? dateFrom, string? dateTo,
        string? search = null, string? searchScope = null,
        int page = 1, int pageSize = 20)
    ```
    Lalu di pemanggilan service (L769-770) ganti argumen `null` (posisi search) menjadi `search` + tambah `searchScope`:
    ```csharp
    var workerList = await _workerDataService.GetWorkersInSection(
        sectionFilter, unit, category, search, statusFilter, from, to, subCategory, searchScope);
    ```
    (sebelumnya posisi ke-4 = `null`.)

    2. **ExportRecordsTeamAssessment (L652)** — sudah punya `search`. Tambah `searchScope` di signature (sebelum atau setelah param existing, default null):
    ```csharp
    public async Task<IActionResult> ExportRecordsTeamAssessment(string? section, string? unit, string? search, string? statusFilter, string? category, string? subCategory, string? dateFrom, string? dateTo, string? searchScope = null)
    ```
    Pemanggilan service L670: append `, searchScope`:
    ```csharp
    var filteredWorkers = await _workerDataService.GetWorkersInSection(sectionFilter, unit, category, search, statusFilter, from, to, subCategory, searchScope);
    ```

    3. **ExportRecordsTeamTraining (L704)** — identik perlakuan: tambah `searchScope = null` di signature; pemanggilan L722 append `, searchScope`.

    Jangan ubah logika section-lock / pagination existing di RecordsTeamPartial (L758-773).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` CMPController.cs: RecordsTeamPartial signature berisi `string? search = null, string? searchScope = null`.
    - `grep` CMPController.cs: pemanggilan service di RecordsTeamPartial TIDAK lagi passes `null` di posisi search — passes `search` + diakhiri `searchScope`.
    - `grep` CMPController.cs: kedua Export (`ExportRecordsTeamAssessment`/`ExportRecordsTeamTraining`) signature berisi `string? searchScope`.
    - `grep` CMPController.cs: ketiga pemanggilan `GetWorkersInSection(` di 3 action ini diakhiri argumen `searchScope)`.
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>RecordsTeamPartial + 2 Export meneruskan search + searchScope ke service; build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: UI/JS — input + selektor scope + wire 5 fungsi JS</name>
  <read_first>
    - Views/CMP/RecordsTeam.cshtml — BACA UTUH. Fokus: filter-control rows L17-91, getFilterState L292-302, saveFilterState/restoreFilterState L304-328, updateExportLinks L330-345, doFetch L371-423 (param block L378-389), resetTeamFilters L425-443, debounce wire existing.
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-06 / 6a + 6b" (excerpt input + 5 fungsi JS).
  </read_first>
  <action>
    **RE-GREP** kelima fungsi JS + filter card.

    1. **UI** — tambah ke filter card (region L17-91, ikut layout `row g-3`/`col-*` existing) sebuah input + select. Default option `Keduanya` selected. Wire ke handler debounce existing (mis. `oninput`/`onchange` yang panggil `filterTeamTable()`/`doFetch()` — ikuti pola control lain di file):
    ```html
    <div class="col-12 col-md-6">
        <input type="text" id="teamSearch" class="form-control" placeholder="Cari nama/NIP atau judul training..." />
    </div>
    <div class="col-12 col-md-3">
        <select id="searchScope" class="form-select">
            <option value="Nama">Nama</option>
            <option value="Training">Training</option>
            <option value="Keduanya" selected>Keduanya</option>
        </select>
    </div>
    ```
    Tambahkan hint kecil (semantik REC-06): teks muted "Pencarian menyaring worker yang muncul; jumlah badge per worker tetap utuh." (opsional, non-blocking).

    2. **getFilterState() (L292-302)** — tambah 2 key:
    ```javascript
    search: document.getElementById('teamSearch').value,
    searchScope: document.getElementById('searchScope').value
    ```

    3. **doFetch() (L378-389)** — tambah 2 param ke URLSearchParams (sebelum `params.set('page', ...)`):
    ```javascript
    if (s.search) params.set('search', s.search);
    if (s.searchScope) params.set('searchScope', s.searchScope);
    ```

    4. **updateExportLinks() (L330-345)** — tambah 2 param yang sama agar export ikut filter:
    ```javascript
    if (s.search) params.set('search', s.search);
    if (s.searchScope) params.set('searchScope', s.searchScope);
    ```

    5. **saveFilterState / restoreFilterState (L304-328)** — persist + restore 2 field (mirror pola per-field existing):
    ```javascript
    // save: state.search = ...; state.searchScope = ...;
    // restore: if (state.search) document.getElementById('teamSearch').value = state.search;
    //          if (state.searchScope) document.getElementById('searchScope').value = state.searchScope;
    ```

    6. **resetTeamFilters (L425-443)** — clear: `teamSearch=''` dan `searchScope='Keduanya'` (default).

    Pastikan input `teamSearch` ikut debounce yang sama dengan control lain (cek apakah file pakai 1 listener delegasi atau per-elemen `oninput`; ikut pola dominan).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` RecordsTeam.cshtml: ada `id="teamSearch"` (input) + `id="searchScope"` (select dengan 3 option + `Keduanya` selected).
    - `grep` RecordsTeam.cshtml: getFilterState mengembalikan key `search` + `searchScope`.
    - `grep` RecordsTeam.cshtml: doFetch DAN updateExportLinks keduanya `params.set('searchScope'`.
    - `grep` RecordsTeam.cshtml: resetTeamFilters set `searchScope` ke `'Keduanya'`.
    - `dotnet build` exit 0. (Verifikasi fungsional search+export di Plan 06 UAT.)
  </acceptance_criteria>
  <done>Team View punya input+selektor scope; 5 fungsi JS wired (state/fetch/export/persist/reset); build hijau.</done>
</task>

</tasks>

<verification>
- `dotnet build` 0 error.
- `grep` rantai penuh: interface→service (searchScope param + 3 cabang) → controller (3 action forward) → view (input+select+5 JS fungsi).
- Verifikasi fungsional (search Nama/Training/Keduanya hasilkan worker-set benar + export ikut filter + matrix scope) di Plan 06.
</verification>

<success_criteria>
Team View search box + selektor scope (Nama/Training/Keduanya, server-side, lintas pagination); Training-search via TrainingRecords.Judul; Keduanya = union; export ikut ke-filter; backward-compat caller lain. (SC #4 ROADMAP §Phase 346.)
</success_criteria>

<output>
After completion, create `.planning/phases/346-cmp-records-detail-search-logic/346-04-SUMMARY.md`
</output>
