---
phase: 346-cmp-records-detail-search-logic
plan: 05
type: execute
wave: 3
depends_on: [346-04]
files_modified:
  - Services/WorkerDataService.cs
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements: [REC-07, REC-08, REC-09]
must_haves:
  truths:
    - "Sesi assessment Status=PendingGrading muncul di My Records dengan label 'Menunggu Penilaian'"
    - "Sesi PendingGrading muncul di export team (GetAllWorkersHistory) tanpa salah label Fail"
    - "Date range dengan dateFrom > dateTo menampilkan warning 'Tanggal Awal lebih besar dari Tanggal Akhir — perbaiki rentang.'"
    - "Header kolom Team View 'Assessment' menjadi 'Assessment Lulus'"
    - "Field CompletedAssessments TIDAK di-rename (hanya label header view)"
  artifacts:
    - path: "Services/WorkerDataService.cs"
      provides: "WHERE include PendingGrading via konstanta di GetUnifiedRecords + GetAllWorkersHistory"
      contains: "AssessmentConstants.AssessmentStatus.PendingGrading"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "Warning inverted date-range (updateDateHint) + header relabel"
      contains: "Assessment Lulus"
  key_links:
    - from: "GetUnifiedRecords / GetAllWorkersHistory WHERE"
      to: "AssessmentSessions PendingGrading rows"
      via: "OR Status == AssessmentConstants.AssessmentStatus.PendingGrading"
      pattern: "AssessmentStatus\\.PendingGrading"
    - from: "RecordsTeam.cshtml updateDateHint"
      to: "dateFilterHint banner"
      via: "inverted-range branch dateFrom > dateTo"
      pattern: "Tanggal Awal lebih besar"
---

<objective>
Tiga perbaikan logic/label kecil: (REC-07) sertakan sesi `PendingGrading` di My Records + export team — WHERE memakai konstanta `AssessmentConstants.AssessmentStatus.PendingGrading`; (REC-08) validasi date range Team View — warning saat `dateFrom > dateTo`; (REC-09) perjelas header "Assessment" → "Assessment Lulus" (view-only, JANGAN rename field).

Purpose: REC-07 (CMP-LOGIC-02/CMP-FILTER-02, MED) — saat ini sesi essay yang menunggu penilaian HILANG dari My Records + export (WHERE hanya `"Completed"`), padahal Phase 345 sudah menyiapkan label "Menunggu Penilaian". REC-08 (FILTER-006, LOW) cegah hasil kosong membingungkan. REC-09 (CMP-FILTER-01, LOW) perjelas makna badge.
Output: `WorkerDataService.cs` (2 WHERE) + `RecordsTeam.cshtml` (1 fungsi JS + 1 header).

**Sequencing:** Wave 3, depends_on 346-04 — `WorkerDataService.cs` & `RecordsTeam.cshtml` keduanya disentuh Plan 04 (REC-06). Region berbeda tapi file sama → serialize setelah 04. **Depends Phase 345** (label "Menunggu Penilaian" sudah ada di switch GetUnifiedRecords L52-57 — JANGAN sentuh switch label).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/346-cmp-records-detail-search-logic/346-CONTEXT.md
@.planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md

<interfaces>
<!-- Konstanta (Models/AssessmentConstants.cs:18) — WAJIB pakai ini, BUKAN literal "PendingGrading" -->
public const string PendingGrading = "Menunggu Penilaian"; // Phase 309 D-04

<!-- Label switch SUDAH BENAR (Phase 345, GetUnifiedRecords L52-57): IsPassed null => PendingGrading.
     PendingGrading session punya IsPassed==null → label otomatis benar. JANGAN sentuh switch. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REC-07 — include PendingGrading di 2 WHERE (konstanta)</name>
  <read_first>
    - Services/WorkerDataService.cs — Fokus: GetUnifiedRecords WHERE L33 (`a.Status == "Completed"`), label switch L52-57 (JANGAN sentuh), GetAllWorkersHistory currentQuery WHERE L136 (`a.Status == "Completed"`). Cek `@using`/namespace AssessmentConstants di atas file.
    - Models/AssessmentConstants.cs — konfirmasi konstanta L18.
    - .planning/phases/345-assessment-pending-grade-display-fix/345-CONTEXT.md — konteks label "Menunggu Penilaian" (predecessor).
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-07" (excerpt + PITFALL konstanta bukan literal).
  </read_first>
  <behavior>
    - GetUnifiedRecords(userId) yang punya 1 sesi Completed(Pass) + 1 sesi PendingGrading → mengembalikan 2 record; sesi pending ber-Status "Menunggu Penilaian" (via switch existing, IsPassed null).
    - GetAllWorkersHistory yang punya sesi PendingGrading → sesi tsb ikut ter-include (sebelumnya hilang).
    - Sesi graded (Pass/Fail) tetap muncul normal (no regression).
    - Sesi Status lain (mis. "InProgress"/"Abandoned") TIDAK ikut (WHERE hanya Completed OR PendingGrading).
  </behavior>
  <action>
    **RE-GREP** kedua WHERE `a.Status == "Completed"` (verified L33 GetUnifiedRecords, L136 GetAllWorkersHistory).

    1. **GetUnifiedRecords (L31-34)** — perluas WHERE:
    ```csharp
    .Where(a => a.UserId == userId && (a.Status == "Completed" || a.Status == AssessmentConstants.AssessmentStatus.PendingGrading))
    ```

    2. **GetAllWorkersHistory (L134-136)** — perluas WHERE currentQuery:
    ```csharp
    .Where(a => a.Status == "Completed" || a.Status == AssessmentConstants.AssessmentStatus.PendingGrading);
    ```

    **PITFALL (wajib):** pakai `AssessmentConstants.AssessmentStatus.PendingGrading` (nilai = "Menunggu Penilaian"), BUKAN literal `"PendingGrading"` (match 0 row). Pastikan `using HcPortal.Models;` (atau namespace yang tepat) sudah ada — jika belum, tambahkan.

    **JANGAN** sentuh switch label L52-57 (sudah benar dari Phase 345). **JANGAN** ubah Excel export map di `ExportRecordsTeamAssessment` L694 (sudah tolerate null IsPassed → render konstanta). Personal Excel `ExportRecords` (CMPController L603) konsumsi GetUnifiedRecords → otomatis ikut.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` WorkerDataService.cs: `AssessmentConstants.AssessmentStatus.PendingGrading` muncul TEPAT 2× (GetUnifiedRecords + GetAllWorkersHistory WHERE).
    - `grep` WorkerDataService.cs: TIDAK ADA literal `"PendingGrading"` (string mentah) di kedua WHERE.
    - `grep` WorkerDataService.cs: switch label (`null => ...PendingGrading`) di GetUnifiedRecords TIDAK berubah (verify-only, masih ada).
    - `dotnet build` exit 0. (Test include-pending di Plan 06.)
  </acceptance_criteria>
  <done>Kedua WHERE include PendingGrading via konstanta; switch label utuh; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: REC-08 + REC-09 — warning date-range + relabel header</name>
  <read_first>
    - Views/CMP/RecordsTeam.cshtml — Fokus: updateDateHint L347-357 (fungsi yang di-extend), dipanggil dari doFetch L409/L413; header "Assessment" L137; container hint `#dateFilterHint` L114 (`alert alert-info`).
    - .planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md §"REC-08" + §"REC-09" (excerpt + D-08 JANGAN rename field).
  </read_first>
  <action>
    **RE-GREP** `updateDateHint` (verified L347-357) + header `>Assessment<` (verified L137).

    1. **REC-08 — inverted-range warning** di `updateDateHint`. Tambah cabang inverted-range PALING ATAS (prioritas di atas hint count existing). Perbandingan string ISO `YYYY-MM-DD` valid (leksikografis == kronologis untuk `<input type="date">`):
    ```javascript
    function updateDateHint(currentCount, state) {
        var hintEl = document.getElementById('dateFilterHint');
        if (!hintEl) return;
        // REC-08: inverted range warning (prioritas)
        if (state.dateFrom && state.dateTo && state.dateFrom > state.dateTo) {
            hintEl.textContent = 'Tanggal Awal lebih besar dari Tanggal Akhir — perbaiki rentang.';
            hintEl.style.display = '';
            return;
        }
        var hasDateFilter = state.dateFrom || state.dateTo;
        if (hasDateFilter && currentCount < initialWorkerCount) {
            hintEl.textContent = 'Beberapa worker tidak punya record di rentang tanggal ini — disembunyikan dari list.';
            hintEl.style.display = '';
        } else {
            hintEl.style.display = 'none';
        }
    }
    ```
    Pertahankan pemanggilan existing di doFetch (L409/L413). Warning inverted-range muncul terlepas dari count (early-return). (Tidak perlu short-circuit doFetch — server akan kembalikan 0/whatever; warning tetap surfaced.)

    2. **REC-09 — relabel header** (L137):
    ```html
    <th class="p-3 text-center">Assessment Lulus</th>
    ```
    **D-08 (wajib):** view-only string. JANGAN rename field `CompletedAssessments` (`Models/WorkerTrainingStatus.cs`, nyebar 3 file). JANGAN sentuh `_RecordsTeamBody.cshtml:29` (`@worker.CompletedAssessments`). Field sudah = `IsPassed==true count` (benar). Tooltip opsional di-drop per D-08.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` RecordsTeam.cshtml: updateDateHint berisi cabang `state.dateFrom > state.dateTo` dengan teks `Tanggal Awal lebih besar dari Tanggal Akhir — perbaiki rentang.`.
    - `grep` RecordsTeam.cshtml: header berisi `>Assessment Lulus<` (relabel L137).
    - `grep` RecordsTeam.cshtml: TIDAK ada perubahan ke `CompletedAssessments` (field tidak di-rename — verify file `_RecordsTeamBody.cshtml` masih `@worker.CompletedAssessments`).
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>Warning inverted date-range aktif; header "Assessment Lulus"; field tidak di-rename; build hijau.</done>
</task>

</tasks>

<verification>
- `dotnet build` 0 error.
- `grep` konfirmasi: konstanta PendingGrading 2× (bukan literal), inverted-range warning, header relabel, field tidak diubah.
- Verifikasi tampil "Menunggu Penilaian" di My Records + warning interaktif di Plan 06 (Playwright UAT).
</verification>

<success_criteria>
Sesi PendingGrading tampil di My Records + export team berlabel "Menunggu Penilaian" (WHERE konstanta); date range invalid → warning; header "Assessment Lulus" (field tak di-rename); build hijau. (SC #5 + #6 ROADMAP §Phase 346.)
</success_criteria>

<output>
After completion, create `.planning/phases/346-cmp-records-detail-search-logic/346-05-SUMMARY.md`
</output>
