---
phase: 346-cmp-records-detail-search-logic
plan: 06
type: execute
wave: 4
depends_on: [346-01, 346-02, 346-03, 346-04, 346-05]
files_modified:
  - HcPortal.Tests/ResultsAuthorizationTests.cs
  - HcPortal.Tests/WorkerDataServiceSearchTests.cs
  - tests/e2e/cmp-records-346.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [REC-04, REC-06, REC-07]
must_haves:
  truths:
    - "xUnit authz matrix 8 kasus × konsep 3 action (via IsResultsAuthorized) PASS"
    - "xUnit GetWorkersInSection searchScope Nama/Training/Keduanya menghasilkan worker-set benar"
    - "xUnit GetUnifiedRecords + GetAllWorkersHistory include sesi PendingGrading"
    - "dotnet test HcPortal.Tests hijau (semua test, termasuk existing)"
    - "Playwright UAT lulus: My Records Aksi/modal, Worker Detail Lihat Hasil (L3 + L4-section + L4-other Forbid), Team search 3 scope + export filter, date-range warning, Tab3 History PendingGrading"
  artifacts:
    - path: "HcPortal.Tests/ResultsAuthorizationTests.cs"
      provides: "Matrix authz pure-static IsResultsAuthorized (8+ kasus)"
      contains: "IsResultsAuthorized"
    - path: "HcPortal.Tests/WorkerDataServiceSearchTests.cs"
      provides: "InMemory tests searchScope + include-pending"
      contains: "GetWorkersInSection"
    - path: "tests/e2e/cmp-records-346.spec.ts"
      provides: "Playwright UAT semua surface Phase 346"
      contains: "Lihat Hasil"
  key_links:
    - from: "ResultsAuthorizationTests"
      to: "CMPController.IsResultsAuthorized"
      via: "pure static [Theory]/[Fact]"
      pattern: "IsResultsAuthorized\\("
    - from: "WorkerDataServiceSearchTests"
      to: "WorkerDataService.GetWorkersInSection / GetUnifiedRecords"
      via: "InMemory DbContext + null-substitute deps"
      pattern: "UseInMemoryDatabase"
---

<objective>
Regression-proof Phase 346 + verifikasi end-to-end: (1) xUnit matrix authz REC-04 (8 kasus via static helper `IsResultsAuthorized`), (2) xUnit REC-06 searchScope (Nama/Training/Keduanya) via InMemory, (3) xUnit REC-07 include-PendingGrading via InMemory, (4) Playwright UAT semua surface termasuk Tab3 History (MAP-20 dedup). SEED_WORKFLOW dipatuhi bila UAT butuh sesi PendingGrading.

Purpose: REC-04 security-sensitive (D-10 "regression-proof penuh") + REC-06/07 logic harus terverifikasi sebelum commit (SC #7 ROADMAP). Plan final, depends semua plan sebelumnya.
Output: 2 file xUnit baru + 1 spec Playwright baru + entri SEED_JOURNAL.

**Harness decision (locked per PATTERNS reco):** REC-04 = Strategy (A) extract pure-static `IsResultsAuthorized` (sudah dibuat Plan 03 Task 1) → test pure `[Theory]` tanpa DB (hindari masalah InMemory tak back Identity GetRolesAsync). REC-06/07 = InMemory DbContext (mirror OrganizationControllerTests) — query path GetWorkersInSection/GetUnifiedRecords hanya pakai `_context`; `_userManager`/`_notificationService`/`_logger` null-substitute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/346-cmp-records-detail-search-logic/346-CONTEXT.md
@.planning/phases/346-cmp-records-detail-search-logic/346-PATTERNS.md

<interfaces>
<!-- Helper di-test (CMPController.cs, dibuat Plan 03) -->
public static bool IsResultsAuthorized(string? ownerUserId, string currentUserId, int roleLevel, string? currentUserSection, string? ownerSection);

<!-- Service ctor (WorkerDataService.cs L15-25) — 4 deps; query path hanya _context -->
WorkerDataService(ApplicationDbContext context, UserManager<ApplicationUser> userManager,
                  INotificationService notificationService, ILogger<WorkerDataService> logger)
// InMemory: context real, 3 lainnya null-substitute (#pragma warning disable CS8625).

<!-- Konstanta -->
AssessmentConstants.AssessmentStatus.PendingGrading == "Menunggu Penilaian"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: xUnit — authz matrix (ResultsAuthorizationTests.cs) + searchScope/pending (WorkerDataServiceSearchTests.cs)</name>
  <read_first>
    - HcPortal.Tests/AssessmentHistoryStatsTests.cs — pola pure-static [Fact] (analog untuk authz matrix).
    - HcPortal.Tests/OrganizationControllerTests.cs L17-49 — pola InMemory MakeController + null-substitute + reflection assert (analog untuk service tests).
    - HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs — referensi seeding Users/roles bila perlu (kita TIDAK pakai real-SQL untuk authz; helper pure-static cukup).
    - Controllers/CMPController.cs — konfirmasi signature IsResultsAuthorized (Plan 03).
    - Services/WorkerDataService.cs — konfirmasi field WorkerTrainingStatus (WorkerName/FullName + NIP + TrainingRecords.Judul) untuk seed yang benar.
  </read_first>
  <action>
    **File 1: `HcPortal.Tests/ResultsAuthorizationTests.cs`** — pure-static matrix (mirror AssessmentHistoryStatsTests). Pakai `[Theory]`+`[InlineData]` untuk 8 kasus inti (konsep "× 3 action" = parameter roleLevel/section sama untuk ketiga; helper single-source, jadi 1 matrix mewakili Results+Certificate+CertificatePdf):
    ```csharp
    using HcPortal.Controllers;
    using Xunit;
    namespace HcPortal.Tests;
    public class ResultsAuthorizationTests
    {
        [Theory]
        // ownerUserId, currentUserId, roleLevel, currentSection, ownerSection, expected
        [InlineData("u1", "u1", 6, "A", "A", true)]   // owner (walau L6)
        [InlineData("u1", "x",  1, null,"A", true)]   // Admin
        [InlineData("u1", "x",  2, null,"A", true)]   // HC
        [InlineData("u1", "x",  3, "X", "A", true)]   // L3 full
        [InlineData("u1", "x",  4, "A", "A", true)]   // L4 same section
        [InlineData("u1", "x",  4, "B", "A", false)]  // L4 other section
        [InlineData("u1", "x",  4, null,null,false)]  // L4 Section null guard
        [InlineData("u1", "x",  4, "",  "",  false)]  // L4 Section empty guard
        [InlineData("u1", "x",  5, "A", "A", false)]  // L5 non-owner
        [InlineData("u1", "x",  6, "A", "A", false)]  // L6 non-owner
        [InlineData("u1", "x",  0, "A", "A", false)]  // roleLevel 0 (no-role) tidak lolos
        public void IsResultsAuthorized_Matrix(string owner, string cur, int lvl, string? curSec, string? ownSec, bool expected)
            => Assert.Equal(expected, CMPController.IsResultsAuthorized(owner, cur, lvl, curSec, ownSec));
    }
    ```

    **File 2: `HcPortal.Tests/WorkerDataServiceSearchTests.cs`** — InMemory (mirror OrganizationControllerTests). Helper `MakeService()` build `WorkerDataService` dengan InMemory ctx + 3 dep null. Seed minimal Users + AssessmentSessions + TrainingRecords. Test:

    REC-06 searchScope (panggil `GetWorkersInSection(section, searchScope: ...)`):
    - `Scope_Nama_FiltersByName`: 2 user (FullName "Budi"/"Andi"), search="budi", scope="Nama" → hanya Budi.
    - `Scope_Training_FiltersByJudul`: user A punya TrainingRecord Judul "K3 Safety", user B tanpa training cocok, search="k3", scope="Training" → hanya user A.
    - `Scope_Keduanya_Union`: user "K3man" (nama match) + user lain dengan training "K3 Safety" + user noise → search="k3", scope="Keduanya" → kedua match muncul, noise tidak.
    - `Scope_Null_NoFilter` (backward-compat): tanpa searchScope/search → semua user section muncul.

    REC-07 include-pending (panggil `GetUnifiedRecords(userId)` + `GetAllWorkersHistory`):
    - `GetUnifiedRecords_IncludesPendingGrading`: seed user 1 sesi Status="Completed" IsPassed=true + 1 sesi Status="Menunggu Penilaian" IsPassed=null → result count==2; sesi pending ber-Status "Menunggu Penilaian".
    - `GetUnifiedRecords_ExcludesOtherStatus`: seed sesi Status="InProgress" → TIDAK muncul.

    **PITFALL InMemory (OrganizationControllerTests L4):** InMemory case-SENSITIVE; filter pakai `.ToLower()`/OrdinalIgnoreCase. Seed + assert dengan casing yang konsisten dengan implementasi (mis. search lowercase, FullName apa adanya — implementasi `.ToLower()` di kedua sisi). Pakai `Status="Menunggu Penilaian"` (nilai konstanta), BUKAN "PendingGrading".

    Gunakan `using HcPortal.Services; using HcPortal.Data; using Microsoft.EntityFrameworkCore;` + `#pragma warning disable CS8625` untuk null-substitute UserManager/NotificationService/Logger (verifikasi GetWorkersInSection/GetUnifiedRecords tidak deref dep ini di query path — jika ternyata deref, fallback ke real UserManager pola OrgLabelMigrationFixture). Re-read method bodies untuk konfirmasi sebelum null-substitute.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests 2>&1 | Select-String -Pattern "Passed!|Failed!|error|Passed:|Failed:"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/ResultsAuthorizationTests.cs` ada; `[Theory]` memanggil `CMPController.IsResultsAuthorized` dengan ≥8 InlineData (termasuk L4-null-guard, L4-other, L5, L6, roleLevel-0).
    - File `HcPortal.Tests/WorkerDataServiceSearchTests.cs` ada; ada test untuk scope Nama/Training/Keduanya/Null + include-PendingGrading.
    - `dotnet test HcPortal.Tests` → semua PASS (existing + baru); 0 failed.
    - Tidak ada literal `"PendingGrading"` di test seed (pakai konstanta atau string "Menunggu Penilaian").
  </acceptance_criteria>
  <done>Matrix authz + searchScope + include-pending hijau via dotnet test; harness sesuai konvensi repo.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright UAT — semua surface Phase 346 (+ SEED_WORKFLOW bila perlu)</name>
  <read_first>
    - tests/e2e/assessment.spec.ts — pola spec existing (describe serial, login helper).
    - tests/helpers/auth.ts + tests/helpers/accounts.ts — login(page, role); akun admin/hc/coach/sectionHead/srSupervisor/manager pwd 123456.
    - docs/SEED_WORKFLOW.md — SOP snapshot/restore DB lokal (bila seed sesi PendingGrading).
    - .planning/phases/346-cmp-records-detail-search-logic/346-CONTEXT.md §specifics (SEED + Dev creds + DB HcPortalDB_Dev).
  </read_first>
  <action>
    Buat `tests/e2e/cmp-records-346.spec.ts` (`test.describe.configure({ mode: 'serial' })`). Login pakai helper existing. Coverage (per Testing Strategy spec L189-193 + sequencing-note Tab3 History):

    1. **My Records (REC-01/02):** login coachee → `/CMP/Records` → assert kolom header "Aksi" ada; assert tombol "Lihat Hasil" (assessment) → klik → URL `/CMP/Results`; back → klik "Detail" (training) → assert `#trainingDetailModal` visible + field (mdPenyelenggara/mdKategori/mdSubKategori/mdStatus) terisi; bila ada SertifikatUrl → `#mdPdfWrap` visible.
    2. **Worker Detail (REC-03/05 + REC-04 authz):**
       - login sebagai L3 (manager) → buka Worker Detail anggota → assert tombol "Lihat Hasil" → klik → `/CMP/Results` 200 (TIDAK Forbid). Assert modal training tampil Kategori/SubKategori.
       - login sebagai L4 section (sectionHead) → Worker Detail anggota SE-SECTION → "Lihat Hasil" → Results 200.
       - login sebagai L4 → akses `/CMP/Results?id=<sesi milik user BEDA section>` langsung → assert Forbid/403 (atau redirect, sesuai ForbidResult). [Butuh tahu 1 sessionId beda-section; ambil via DB query lokal atau seed.]
    3. **Team View (REC-06/08):** login L3/HC → `/CMP/RecordsTeam` → isi `#teamSearch`, pilih `#searchScope`=Nama → assert worker-list menyaring; ulang scope=Training, scope=Keduanya. Assert link Export (Assessment/Training) mengandung param `search`+`searchScope` (cek href setelah `updateExportLinks`). Isi dateFrom > dateTo → assert `#dateFilterHint` menampilkan "Tanggal Awal lebih besar dari Tanggal Akhir".
    4. **REC-07 My Records pending:** assessment esai pending tampil "Menunggu Penilaian" di `/CMP/Records` (butuh 1 sesi PendingGrading — SEED bila tak ada).
    5. **Tab3 History (MAP-20 dedup, depends REC-07):** buka halaman yang merender Tab3 History (ManageAssessment Tab3 / `_HistoryTab`) → assert sel sesi pending menampilkan "Menunggu Penilaian" (bukan Pass/Fail). [Catatan: ini verifikasi dampak REC-07 ke surface History; bila tab butuh role admin, login admin.]

    **SEED_WORKFLOW (bila butuh sesi PendingGrading / sesi beda-section):**
    - Snapshot DB lokal dulu: `sqlcmd ... BACKUP DATABASE HcPortalDB_Dev ...` (atau DB lokal `dotnet run` — konfirmasi nama DB lokal vs Dev; CONTEXT sebut DB Dev untuk UAT).
    - Catat di `docs/SEED_JOURNAL.md`: tujuan (UAT REC-07 PendingGrading + REC-04 cross-section), klasifikasi `temporary + local-only`, entitas tersentuh (AssessmentSessions Status="Menunggu Penilaian" IsPassed=null; user beda Section).
    - Restore DB setelah UAT (sukses ATAU gagal); tandai journal `cleaned`.
    - Bila data Dev SUDAH punya sesi pending + user multi-section, SKIP seed (cek dulu via query) — dokumentasikan keputusan di journal.

    **GATE (CLAUDE.md DEV_WORKFLOW):** jalankan `dotnet build` (0 error) + `dotnet test` (hijau) + `dotnet run` (localhost:5277) SEBELUM Playwright. Ini checkpoint sebelum commit.

    Output spec + ringkasan hasil UAT (PASS/FAIL per surface) ke SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test cmp-records-346 2>&1 | Select-String -Pattern "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/cmp-records-346.spec.ts` ada dengan test untuk: My Records Aksi/modal, Worker Detail Lihat Hasil (L3+L4-section PASS, L4-other Forbid), Team search 3 scope + export param + date-warning, REC-07 pending label, Tab3 History pending.
    - `npx playwright test cmp-records-346` → semua test passed (atau dokumentasi eksplisit per surface bila environment-blocked, dengan alasan).
    - Bila seed dipakai: `docs/SEED_JOURNAL.md` punya entri baru dengan status akhir `cleaned`; DB ter-restore.
    - `dotnet build` 0 error + `dotnet test` hijau (gate pre-commit).
  </acceptance_criteria>
  <done>UAT semua surface Phase 346 PASS via Playwright; SEED (bila ada) ter-restore + journal cleaned; gate build+test hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — visual + RBAC nyata semua surface</name>
  <what-built>
    Phase 346 lengkap: My Records kolom Aksi + modal training; Worker Detail tombol Lihat Hasil + modal Kategori/SubKategori; authz REC-04 dilonggarkan (L≤3 full, L4 section-scoped, guard null); Team View search adaptif 3 scope + export filter + date-range warning; PendingGrading muncul di My Records/export; header "Assessment Lulus". xUnit (authz matrix + searchScope + pending) + Playwright UAT sudah otomatis PASS.
  </what-built>
  <how-to-verify>
    Verifikasi manual di browser lokal (`http://localhost:5277`, `dotnet run`) — fokus pada hal yang sulit di-assert otomatis (visual + RBAC nyata):
    1. **My Records** (`/CMP/Records`, login coachee): kolom "Aksi" muncul; tombol "Lihat Hasil" buka halaman Results yang benar; tombol "Detail" buka modal — cek SEMUA field terisi wajar (Kategori/SubKategori/Status/ValidUntil/CertType) + tombol PDF muncul bila ada sertifikat; row tetap bisa diklik.
    2. **Worker Detail authz** (`/CMP/RecordsWorkerDetail`): login manager (L3) → buka anggota → "Lihat Hasil" tampil Results (tidak Forbid); login sectionHead (L4) → anggota SE-SECTION bisa, anggota BEDA section → akses Results di-Forbid. Konfirmasi tombol Sertifikat L3/L4 sekarang fungsional (AUTHZ-01).
    3. **Team View** (`/CMP/RecordsTeam`): search "Nama" vs "Training" vs "Keduanya" memberi hasil worker yang sesuai; klik Export Excel → file ter-download HANYA berisi worker ter-filter; set Tanggal Awal > Tanggal Akhir → muncul warning merah/biru "Tanggal Awal lebih besar...".
    4. **PendingGrading**: assessment esai yang belum dinilai tampil "Menunggu Penilaian" (amber) di My Records + di Tab3 History (bukan Pass/Fail/Completed).
    5. Pastikan TIDAK ADA regresi: sesi graded tetap Pass/Fail normal; kolom Sertifikat existing utuh.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila semua surface benar, atau jelaskan isu (mis. modal field kosong, L4 cross-section bocor, export tak ikut filter) untuk perbaikan.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` 0 error + `dotnet test HcPortal.Tests` hijau (authz matrix 8+ kasus + searchScope 4 + include-pending 2 + existing).
- `npx playwright test cmp-records-346` PASS semua surface.
- Human checkpoint: RBAC nyata (L3/L4-section/L4-other) + visual modal + export-filter + date-warning + pending label terverifikasi.
- SEED (bila ada) ter-restore, journal `cleaned`.
</verification>

<success_criteria>
`dotnet build` 0 error + `dotnet test` hijau (authz matrix + search scope + PendingGrading) + Playwright UAT PASS + human-verify approved. (SC #7 ROADMAP §Phase 346.)
</success_criteria>

<output>
After completion, create `.planning/phases/346-cmp-records-detail-search-logic/346-06-SUMMARY.md`
</output>
