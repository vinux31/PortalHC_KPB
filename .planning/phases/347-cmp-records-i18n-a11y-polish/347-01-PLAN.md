---
phase: 347-cmp-records-i18n-a11y-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CMP/Records.cshtml
  - Views/CMP/RecordsWorkerDetail.cshtml
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements: [POL-01, POL-02, POL-03, POL-04, POL-05, POL-10]
user_setup: []

must_haves:
  truths:
    - "Badge assessment menampilkan 'Lulus' (IsPassed true) dan 'Tidak Lulus' (IsPassed false) di My Records + Worker Detail"
    - "Case IsPassed null tetap menampilkan 'Menunggu Penilaian' amber (tidak berubah dari Phase 345)"
    - "Header kolom skor = 'Nilai'; label info worker = 'Jabatan' + label organisasi via @OrgLabels.GetLabel(0)"
    - "Opsi filter default berbahasa Indonesia: 'Semua Kategori' / 'Semua Sub Kategori' / 'Semua Tipe'"
    - "Subtitle Worker Detail berbahasa Indonesia"
  artifacts:
    - path: "Views/CMP/Records.cshtml"
      provides: "Badge Lulus/Tidak Lulus + header Nilai (null case intact)"
      contains: ">Lulus<"
    - path: "Views/CMP/RecordsWorkerDetail.cshtml"
      provides: "Badge Lulus/Tidak Lulus + Jabatan/Section label + Semua Kategori/Sub/Tipe + subtitle ID"
      contains: ">Jabatan<"
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "Header Jabatan + Semua Kategori/Sub Kategori"
      contains: ">Jabatan<"
  key_links:
    - from: "Records.cshtml badge switch"
      to: "case IsPassed null"
      via: "Status switch dengan AssessmentConstants.AssessmentStatus.PendingGrading"
      pattern: "Menunggu Penilaian|PendingGrading"
---

<objective>
Swap teks Inggris → Bahasa Indonesia pada 3 view CMP/Records (badge, header, label info worker, opsi filter, subtitle, label tombol). Pure i18n text — TIDAK mengubah perilaku, query, logic, atau class CSS badge.

Purpose: Konsistensi Bahasa Indonesia (POL-01..05 + POL-10 label) sesuai audit 15 finding LOW. Phase 346 sudah memverifikasi REC-01..09 bekerja di browser — plan ini HANYA menukar teks pada baris yang berdekatan, JANGAN meregresi.
Output: 3 view ter-update dengan teks Indonesia; case `IsPassed == null` tetap "Menunggu Penilaian" amber (Phase 345 verbatim).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md
@docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md

<interfaces>
<!-- Verbatim verified anchors pasca-Phase 345+346 (line numbers ASLI, sudah di-grep 2026-06-04). -->
<!-- TETAP re-grep teks target sebelum edit — angka baris bisa bergeser bila plan lain jalan dulu. -->

@OrgLabels global-injected di Views/_ViewImports.cshtml:6 (Phase 343):
```cshtml
@inject HcPortal.Services.IOrgLabelService OrgLabels
```
-> @OrgLabels.GetLabel(0) (Bagian/level-0), GetLabel(1) (Unit/level-1) tersedia di SEMUA view termasuk partial. Tidak perlu @inject lokal.

POL-01 — Records.cshtml badge switch 3-way (L183-192), JANGAN ubah class bg-success/bg-danger:
```cshtml
@if (item.IsPassed == true) {
    <span class="badge bg-success">Passed</span>       <!-- POL-01: Passed -> Lulus -->
} else if (item.IsPassed == false) {
    <span class="badge bg-danger">Failed</span>         <!-- POL-01: Failed -> Tidak Lulus -->
} else if (!string.IsNullOrEmpty(item.Status)) {
    var sc = item.Status switch { ... AssessmentConstants.AssessmentStatus.PendingGrading => "bg-warning text-dark", ... };
    <span class="badge @sc">@item.Status</span>        <!-- null case = "Menunggu Penilaian" amber — JANGAN SENTUH -->
}
```

POL-01 — RecordsWorkerDetail.cshtml badge (L228/230/232):
```cshtml
<span class="badge bg-success">Passed</span>   <!-- L228 -> Lulus -->
<span class="badge bg-danger">Failed</span>     <!-- L230 -> Tidak Lulus -->
<span class="badge bg-warning text-dark">@AssessmentConstants.AssessmentStatus.PendingGrading</span>  <!-- L232 — JANGAN SENTUH -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POL-01/02 Records.cshtml — badge Lulus/Tidak Lulus + header Nilai</name>
  <read_first>
    - Views/CMP/Records.cshtml (baca L150-195 untuk anchor header + badge switch 3-way)
    - docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md (§"Phase 347" baris 145-146: POL-01 + POL-02 teks exact)
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md (POL-01 COORDINATION + D-04)
    - .planning/phases/345-assessment-pending-grade-display-fix/345-CONTEXT.md (null = "Menunggu Penilaian" amber — koordinasi)
  </read_first>
  <action>
    Di Views/CMP/Records.cshtml:

    1. POL-02 — re-grep `>Score<` (perkiraan L154). Ganti `<th class="p-3 text-center">Score</th>` → `<th class="p-3 text-center">Nilai</th>`.

    2. POL-01 — re-grep `>Passed<` dan `>Failed<` (perkiraan L185/187, di dalam blok `@if (item.IsPassed == true)`). Ganti HANYA dua baris ini:
       - `<span class="badge bg-success">Passed</span>` → `<span class="badge bg-success">Lulus</span>`
       - `<span class="badge bg-danger">Failed</span>` → `<span class="badge bg-danger">Tidak Lulus</span>`

    CRITICAL — JANGAN sentuh:
    - Class CSS `bg-success` / `bg-danger` (tetap verbatim).
    - Blok `else if (!string.IsNullOrEmpty(item.Status))` (perkiraan L188-190) — ini cabang `null` yang menampilkan `@item.Status` = "Menunggu Penilaian" amber dari Phase 345. Status switch L189 punya literal string "Passed"/"Failed" sebagai KEY mapping ke warna (`"Passed" => "bg-success"`, `"Failed" => "bg-danger"`) — itu KEY data status, BUKAN teks tampil. JANGAN ubah key switch tersebut.
    - Teks tampil "Menunggu Penilaian" datang dari `@item.Status` (di-set di service Phase 345), bukan literal di view ini.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; (Select-String -Path Views/CMP/Records.cshtml -Pattern '>Lulus<','>Tidak Lulus<','>Nilai<' -SimpleMatch | Measure-Object).Count -eq 3 -and (Select-String -Path Views/CMP/Records.cshtml -Pattern '>Passed</span>','>Failed</span>' -SimpleMatch | Measure-Object).Count -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - grep Records.cshtml: `>Lulus<` ADA, `>Tidak Lulus<` ADA, `>Nilai<` ADA.
    - grep Records.cshtml: `>Passed</span>` TIDAK ADA, `>Failed</span>` TIDAK ADA (sebagai teks tampil badge).
    - grep Records.cshtml: `"Passed" => "bg-success"` MASIH ADA (key switch status tidak diubah).
    - grep Records.cshtml: `bg-success`/`bg-danger` masih ada di kedua badge.
    - Cabang null tidak diubah (Status switch + `@item.Status` utuh).
  </acceptance_criteria>
  <done>Badge true/false berbahasa Indonesia, header skor = "Nilai", null case "Menunggu Penilaian" tidak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 2: POL-01/03/05 RecordsWorkerDetail.cshtml — badge + Jabatan/Section + subtitle ID</name>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml (baca L36-72 subtitle+info worker; L226-233 badge)
    - docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md (§"Phase 347" baris 145,147,149: POL-01/03/05)
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md (D-04 + POL-03 OrgLabels global)
  </read_first>
  <action>
    Di Views/CMP/RecordsWorkerDetail.cshtml:

    1. POL-05 — re-grep subtitle Inggris (perkiraan L38): `<p class="text-muted mb-0">View detailed assessment and training records for team member.</p>`. Ganti isi `<p>` → `Lihat detail rekam jejak penilaian dan pelatihan anggota tim.`

    2. POL-03 — re-grep `>Position<` (perkiraan L66) dan `>Section<` (perkiraan L70), di dalam Worker Info Card `<small class="text-muted d-block">...`:
       - `<small class="text-muted d-block">Position</small>` → `<small class="text-muted d-block">Jabatan</small>`
       - `<small class="text-muted d-block">Section</small>` → `<small class="text-muted d-block">@OrgLabels.GetLabel(0)</small>`

    3. POL-01 — re-grep `>Passed<` (perkiraan L228) dan `>Failed<` (perkiraan L230):
       - `<span class="badge bg-success">Passed</span>` → `<span class="badge bg-success">Lulus</span>`
       - `<span class="badge bg-danger">Failed</span>` → `<span class="badge bg-danger">Tidak Lulus</span>`

    CRITICAL — JANGAN sentuh baris `<span class="badge bg-warning text-dark">@AssessmentConstants.AssessmentStatus.PendingGrading</span>` (perkiraan L232 — null case) dan JANGAN ubah class bg-success/bg-danger.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; (Select-String -Path Views/CMP/RecordsWorkerDetail.cshtml -Pattern '>Lulus<','>Tidak Lulus<','>Jabatan<','rekam jejak penilaian dan pelatihan anggota tim' -SimpleMatch | Measure-Object).Count -eq 4 -and (Select-String -Path Views/CMP/RecordsWorkerDetail.cshtml -Pattern '>Passed</span>','>Failed</span>','>Position<','>Section<','for team member' -SimpleMatch | Measure-Object).Count -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - grep RecordsWorkerDetail.cshtml: `>Lulus<`, `>Tidak Lulus<`, `>Jabatan<`, "rekam jejak penilaian dan pelatihan anggota tim" SEMUA ADA.
    - grep RecordsWorkerDetail.cshtml: `>Section<` (sebagai teks label `<small>`) TIDAK ADA → diganti `@OrgLabels.GetLabel(0)`.
    - grep RecordsWorkerDetail.cshtml: `>Passed</span>`, `>Failed</span>`, `>Position<`, "for team member" SEMUA TIDAK ADA.
    - Baris `@AssessmentConstants.AssessmentStatus.PendingGrading` masih ada (null case intact).
  </acceptance_criteria>
  <done>Subtitle ID, label Jabatan + OrgLabel, badge Lulus/Tidak Lulus, null case tidak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 3: POL-03/04 + POL-10-label RecordsWorkerDetail + RecordsTeam — opsi filter "Semua ..." + header Jabatan + konsistensi label tombol</name>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml (baca L135-167 filter selects; L420-430 JS subSelect rebuild; L250-285 tombol aksi)
    - Views/CMP/RecordsTeam.cshtml (baca L55-65 Category/SubCategory selects; L150-154 header thead; L480-490 JS subSelect rebuild)
    - docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md (§"Phase 347" baris 147-148,154: POL-03/04/10)
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md (D-04 verbatim + POL-04 nuance)
  </read_first>
  <action>
    A. Di Views/CMP/RecordsWorkerDetail.cshtml (POL-04):
    Re-grep ketiga opsi default + JS rebuild. Ganti teks opsi (JANGAN ubah `value=""`):
    1. `<option value="">All Categories</option>` (perkiraan L137) → `<option value="">Semua Kategori</option>`
    2. `<option value="">All Sub Categories</option>` (perkiraan L150, opsi default select) → `<option value="">Semua Sub Kategori</option>`
    3. `<option value="">All Types</option>` (perkiraan L164) → `<option value="">Semua Tipe</option>`
    4. JS string rebuild (perkiraan L426): `subSelect.innerHTML = '<option value="">All Sub Categories</option>';` → `subSelect.innerHTML = '<option value="">Semua Sub Kategori</option>';`
    (Opsi type label "Assessment" L165 / "Training" L166 sudah Indonesia-friendly — biarkan; itu I18N-008 konsisten.)

    B. Di Views/CMP/RecordsTeam.cshtml (POL-03 + POL-04):
    1. POL-03 — re-grep `>Position<` (perkiraan L152, header `<th class="p-3">Position</th>`) → `<th class="p-3">Jabatan</th>`.
    2. POL-04 — `<option value="">All Categories</option>` (perkiraan L58) → `<option value="">Semua Kategori</option>`.
    3. POL-04 — `<option value="">All Sub Categories</option>` (perkiraan L64, opsi default select) → `<option value="">Semua Sub Kategori</option>`.
    4. POL-04 — JS string rebuild (perkiraan L484): `subSelect.innerHTML = '<option value="">All Sub Categories</option>';` → `subSelect.innerHTML = '<option value="">Semua Sub Kategori</option>';`
    (RecordsTeam Bagian/Unit options sudah pakai `Semua @OrgLabels.GetLabel(N)` — biarkan.)

    C. POL-10 (konsistensi label tombol Lihat/Sertifikat) — VERIFIKASI saja, kemungkinan no-op:
    Re-grep tombol aksi RecordsWorkerDetail (perkiraan L268-282) — pastikan label konsisten: tombol assessment = "Lihat Hasil", tombol sertifikat = "Sertifikat". Saat ini sudah konsisten ("Lihat Hasil" L277, "Sertifikat" L269/282). JANGAN ubah teks tombol "Detail" (valid Indonesia per spec I18N-007 rejected). Bila sudah konsisten, tidak ada edit di sub-task ini — cukup catat di SUMMARY bahwa label tombol sudah konsisten (POL-10 I18N-009 terpenuhi tanpa perubahan).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; (Select-String -Path Views/CMP/RecordsWorkerDetail.cshtml,Views/CMP/RecordsTeam.cshtml -Pattern 'All Categories','All Sub Categories','All Types' -SimpleMatch | Measure-Object).Count -eq 0 -and (Select-String -Path Views/CMP/RecordsTeam.cshtml -Pattern '>Jabatan<' -SimpleMatch | Measure-Object).Count -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - grep RecordsWorkerDetail.cshtml + RecordsTeam.cshtml: `All Categories`, `All Sub Categories`, `All Types` SEMUA TIDAK ADA (Razor markup + JS string).
    - grep RecordsWorkerDetail.cshtml: `Semua Kategori`, `Semua Sub Kategori`, `Semua Tipe` ADA.
    - grep RecordsTeam.cshtml: `>Jabatan<` ADA (header thead), `>Position<` TIDAK ADA.
    - grep RecordsTeam.cshtml: `Semua Kategori`, `Semua Sub Kategori` ADA.
    - `value=""` pada opsi-opsi tersebut tidak berubah (hanya teks tampil).
  </acceptance_criteria>
  <done>Opsi filter default berbahasa Indonesia di kedua view (markup + JS), header Team = "Jabatan", label tombol terverifikasi konsisten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Tidak ada trust boundary baru. Plan ini hanya menukar literal teks tampilan (badge/header/option/subtitle) pada view Razor yang sudah ada. Tidak ada input handling baru, tidak ada query, tidak ada authz, tidak ada perubahan data-binding.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-347-01 | Tampering | Teks badge/option Razor | accept | Hanya literal statis; tidak ada user input ter-render baru; nilai data (`@item.Status`, `@OrgLabels.GetLabel(0)`) sudah ter-encode Razor by default. Tidak ada surface baru. |

Block-worthy threats: NONE.
</threat_model>

<verification>
- `dotnet build` 0 error (dijalankan di plan verifikasi 347-04; plan ini cukup grep + build cek lokal opsional).
- grep konfirmasi semua teks Indonesia ADA + teks Inggris lama TIDAK ADA (per acceptance_criteria tiap task).
- Cabang `IsPassed == null` di Records + RecordsWorkerDetail tidak tersentuh (Phase 345 + REC-07 Phase 346 tidak regresi).
</verification>

<success_criteria>
- POL-01: Badge true→"Lulus", false→"Tidak Lulus" di 2 view; null tetap "Menunggu Penilaian".
- POL-02: Header "Score"→"Nilai" di Records.
- POL-03: "Position"→"Jabatan" (WorkerDetail + RecordsTeam header); "Section"→@OrgLabels.GetLabel(0) (WorkerDetail).
- POL-04: "Semua Kategori"/"Semua Sub Kategori"/"Semua Tipe" di WorkerDetail + RecordsTeam (markup + JS).
- POL-05: Subtitle WorkerDetail → Indonesia.
- POL-10 (sebagian — label): label tombol Lihat/Sertifikat terverifikasi konsisten (sisanya type=button + aria-current di plan 347-02).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/347-cmp-records-i18n-a11y-polish/347-01-SUMMARY.md`.
</output>
