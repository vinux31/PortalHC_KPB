---
phase: 347-cmp-records-i18n-a11y-polish
plan: 02
type: execute
wave: 2
depends_on: [347-01]
files_modified:
  - Views/CMP/Records.cshtml
  - Views/CMP/RecordsWorkerDetail.cshtml
  - Views/CMP/RecordsTeam.cshtml
autonomous: true
requirements: [POL-06, POL-07, POL-09, POL-10]
user_setup: []

must_haves:
  truths:
    - "Modal training (Records + Worker Detail) punya role='dialog' + aria-labelledby='modalTrainingTitle'; btn-close punya aria-label='Tutup'"
    - "Semua kontrol filter di 3 view punya label dengan asosiasi for=/id (screen-reader-friendly)"
    - "My Records search punya label terlihat (bukan hanya aria-label)"
    - "Grid filter Worker Detail responsif (col-12 col-sm-6 col-md-...) — tidak menumpuk 6 baris di mobile"
    - "Tombol reset di 3 view punya type='button'; pagination Team menandai halaman aktif dengan aria-current='page'"
  artifacts:
    - path: "Views/CMP/Records.cshtml"
      provides: "Modal a11y + search visible label + reset type=button"
      contains: "role=\"dialog\""
    - path: "Views/CMP/RecordsWorkerDetail.cshtml"
      provides: "Modal a11y + filter labels for= + grid responsif + reset type=button"
      contains: "role=\"dialog\""
    - path: "Views/CMP/RecordsTeam.cshtml"
      provides: "Filter labels for= + pagination aria-current + reset type=button"
      contains: "aria-current"
  key_links:
    - from: "modal .modal-header h5 id=modalTrainingTitle"
      to: ".modal aria-labelledby"
      via: "aria-labelledby=\"modalTrainingTitle\""
      pattern: "aria-labelledby=\"modalTrainingTitle\""
---

<objective>
Tambahkan atribut aksesibilitas (a11y) pada 3 view CMP/Records: ARIA modal, asosiasi label `for=` semua filter, search label terlihat, grid filter responsif, `type="button"` reset, dan `aria-current="page"` pagination. Pure a11y/markup — TIDAK mengubah perilaku, query, atau logic.

Purpose: Aksesibilitas screen-reader + keyboard + mobile (POL-06/07/09/10) sesuai audit 15 finding LOW. Phase 346 sudah verifikasi REC-01..09 di browser — JANGAN regresi struktur fungsional (id, data-bs-target, onclick handler, JS selector).
Output: 3 view dengan atribut a11y; semua id/handler/data-* yang dipakai JS tetap utuh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md
@docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md

<interfaces>
<!-- Verified anchors pasca-Phase 345+346 (line numbers ASLI, sudah di-grep 2026-06-04). RE-GREP sebelum edit. -->
<!-- CATATAN: plan 347-01 (wave 1) menukar teks di file yang SAMA — angka baris bisa bergeser. Selalu re-grep target. -->

POL-06 — Modal training ADA di 2 tempat dengan struktur IDENTIK:
Records.cshtml (modal REC-02 Phase 346, perkiraan L288-294):
```cshtml
<div class="modal fade" id="trainingDetailModal" tabindex="-1">          <!-- tambah aria-labelledby + role -->
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalTrainingTitle">Detail Training</h5>   <!-- id sudah ada -->
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>  <!-- tambah aria-label -->
```
RecordsWorkerDetail.cshtml (perkiraan L300-305): struktur SAMA persis, id="modalTrainingTitle" sudah ada.

POL-10 — Reset button TANPA type (default submit-ish, perlu type="button"):
RecordsWorkerDetail.cshtml (perkiraan L170): `<button class="btn btn-outline-secondary w-100" onclick="clearFilters()">`
Records.cshtml My Records (perkiraan L81): `<button class="btn btn-outline-secondary w-100" onclick="clearFilters()">`
(RecordsTeam reset L87 `onclick="resetTeamFilters()"` — cek apakah sudah ada type="button"; tambah bila belum.)

POL-10 — Pagination Team di-generate JS (perkiraan L282-290), tambah aria-current pada item aktif:
```js
'<li class="page-item ' + active + '"><a class="page-link page-nav" data-page="' + i + '" href="#">' + i + '</a></li>'
```
-> bila `active === 'active'` tambahkan `aria-current="page"` pada <a> (atau <li>).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POL-06 modal a11y (Records + RecordsWorkerDetail) + POL-07 My Records search visible label</name>
  <read_first>
    - Views/CMP/Records.cshtml (baca L52-90 My Records search; L288-296 modal)
    - Views/CMP/RecordsWorkerDetail.cshtml (baca L299-307 modal)
    - docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md (§"Phase 347" baris 150-151: POL-06 + POL-07)
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md (POL-06 Integration Points: modal di 2 tempat)
  </read_first>
  <action>
    A. POL-06 — di KEDUA file (Views/CMP/Records.cshtml + Views/CMP/RecordsWorkerDetail.cshtml), re-grep `<div class="modal fade" id="trainingDetailModal" tabindex="-1">`:
    1. Tambah atribut ARIA pada elemen `.modal`:
       `<div class="modal fade" id="trainingDetailModal" tabindex="-1" role="dialog" aria-labelledby="modalTrainingTitle" aria-hidden="true">`
    2. Re-grep btn-close di dalam modal-header (perkiraan Records:293 / WorkerDetail:305): `<button type="button" class="btn-close" data-bs-dismiss="modal"></button>` → tambah `aria-label="Tutup"`:
       `<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Tutup"></button>`
    JANGAN ubah `id="modalTrainingTitle"` pada `<h5>` (referensi aria-labelledby + dibaca JS). JANGAN ubah `data-bs-dismiss`/`data-bs-target`.

    B. POL-07 (My Records search visible label) — di Views/CMP/Records.cshtml, re-grep input search My Records (perkiraan L58): `<input type="text" id="searchInput" class="form-control" placeholder="Cari berdasarkan judul..." aria-label="Cari judul training">`.
    Bungkus dengan label terlihat: tambahkan SEBELUM `<input>` (di dalam `<div class="col-12 col-md-6">` yang sama):
       `<label for="searchInput" class="form-label small text-muted mb-1">Cari</label>`
    Pertahankan `id="searchInput"` (dibaca JS filterTable). Boleh biarkan `aria-label` ada (redundan tapi tidak salah) atau hapus — pilih biarkan untuk minim risiko.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; (Select-String -Path Views/CMP/Records.cshtml,Views/CMP/RecordsWorkerDetail.cshtml -Pattern 'aria-labelledby="modalTrainingTitle"' -SimpleMatch | Measure-Object).Count -eq 2 -and (Select-String -Path Views/CMP/Records.cshtml,Views/CMP/RecordsWorkerDetail.cshtml -Pattern 'aria-label="Tutup"' -SimpleMatch | Measure-Object).Count -eq 2 -and (Select-String -Path Views/CMP/Records.cshtml -Pattern 'for="searchInput"' -SimpleMatch | Measure-Object).Count -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - grep Records.cshtml + RecordsWorkerDetail.cshtml: `aria-labelledby="modalTrainingTitle"` muncul TEPAT 2x (1 per file).
    - grep keduanya: `role="dialog"` muncul (minimal 1 per file pada modal training).
    - grep keduanya: `aria-label="Tutup"` muncul TEPAT 2x.
    - grep Records.cshtml: `for="searchInput"` ADA (label terlihat).
    - `id="modalTrainingTitle"` masih ada di kedua file; `id="searchInput"` masih ada.
  </acceptance_criteria>
  <done>Modal training di 2 view aksesibel (role+labelledby+btn-close Tutup); My Records search punya visible label.</done>
</task>

<task type="auto">
  <name>Task 2: POL-07 label for= filter (RecordsWorkerDetail + RecordsTeam) + POL-09 grid responsif Worker Detail</name>
  <read_first>
    - Views/CMP/RecordsWorkerDetail.cshtml (baca L128-176 Search & Filter Bar lengkap)
    - Views/CMP/RecordsTeam.cshtml (baca L14-110 Filter Controls — beberapa label sudah ada from 346: L20/50 Bagian/Unit, L95/99 Cari/Lingkup; yang belum: Category L56, Sub Category L62, Status L71, Tanggal L79/83)
    - docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md (§"Phase 347" baris 151,153: POL-07 + POL-09)
    - .planning/phases/347-cmp-records-i18n-a11y-polish/347-CONTEXT.md (POL-07 + POL-09)
  </read_first>
  <action>
    A. POL-07 + POL-09 — di Views/CMP/RecordsWorkerDetail.cshtml, blok "Search & Filter Bar" (perkiraan L128-176, 6 kolom `col-12 col-md-2`):
    Untuk SETIAP kontrol filter (searchInput, categoryFilter, subCategoryFilter, yearFilter, typeFilter), tambahkan `<label for="{id}">` terlihat di atas kontrolnya. Saat ini kontrol-kontrol ini TIDAK punya label sama sekali. Tambahkan pola konsisten dengan RecordsTeam:
    - searchInput: `<label for="searchInput" class="form-label small text-muted mb-1">Cari</label>` lalu input (input search sudah ada placeholder; tambah label).
    - categoryFilter: `<label for="categoryFilter" class="form-label small text-muted mb-1">Kategori</label>`
    - subCategoryFilter: `<label for="subCategoryFilter" class="form-label small text-muted mb-1">Sub Kategori</label>`
    - yearFilter: `<label for="yearFilter" class="form-label small text-muted mb-1">Tahun</label>`
    - typeFilter: `<label for="typeFilter" class="form-label small text-muted mb-1">Tipe</label>`
    (Tombol Reset di kolom ke-6 tidak butuh label.)

    POL-09 — pada SETIAP `<div class="col-12 col-md-2">` di blok filter ini (6 buah), ubah kelas grid → `<div class="col-12 col-sm-6 col-md-2">` agar di mobile (xs) 1 kolom penuh, di sm 2 kolom, di md+ 6 kolom sejajar. Terapkan ke keenam kolom (5 filter + 1 reset) untuk konsistensi.
    JANGAN ubah `id=` kontrol mana pun (dibaca JS filterTable/categoryFilter handler). JANGAN ubah `onclick`/`placeholder` value.

    B. POL-07 — di Views/CMP/RecordsTeam.cshtml, tambah `for=` pada kontrol yang labelnya BELUM ter-asosiasi. Re-grep tiap `<label class="form-label small text-muted mb-1">{teks}</label>` yang TIDAK punya atribut `for=`:
    - Category (perkiraan L56) → `<label class="form-label small text-muted mb-1" for="categoryFilter">Category</label>`
    - Sub Category (perkiraan L62) → `<label ... for="subCategoryFilter">Sub Category</label>`
    - Status (perkiraan L71) → `<label ... for="statusFilter">Status</label>`
    - Tanggal Awal (perkiraan L79) → `<label ... for="dateFrom">Tanggal Awal</label>`
    - Tanggal Akhir (perkiraan L83) → `<label ... for="dateTo">Tanggal Akhir</label>`
    - Bagian/Unit (perkiraan L20/L50) → tambah `for="sectionFilter"` / `for="unitFilter"` bila belum ada.
    (Cari L95 + Lingkup L99 SUDAH punya for="teamSearch"/for="searchScope" dari Phase 346 — biarkan.)
    Verifikasi setiap `for=` cocok dengan `id=` kontrol di bawahnya.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; (Select-String -Path Views/CMP/RecordsWorkerDetail.cshtml -Pattern 'for="categoryFilter"','for="subCategoryFilter"','for="yearFilter"','for="typeFilter"' -SimpleMatch | Measure-Object).Count -eq 4 -and (Select-String -Path Views/CMP/RecordsWorkerDetail.cshtml -Pattern 'col-12 col-sm-6 col-md-2' -SimpleMatch | Measure-Object).Count -ge 5 -and (Select-String -Path Views/CMP/RecordsTeam.cshtml -Pattern 'for="categoryFilter"','for="statusFilter"','for="dateFrom"','for="dateTo"' -SimpleMatch | Measure-Object).Count -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - grep RecordsWorkerDetail.cshtml: `for="categoryFilter"`, `for="subCategoryFilter"`, `for="yearFilter"`, `for="typeFilter"`, `for="searchInput"` SEMUA ADA (5 label).
    - grep RecordsWorkerDetail.cshtml: `col-12 col-sm-6 col-md-2` muncul ≥5x (grid responsif keenam kolom filter).
    - grep RecordsWorkerDetail.cshtml: `id="searchInput"`, `id="categoryFilter"`, `id="subCategoryFilter"`, `id="yearFilter"`, `id="typeFilter"` masih ADA (tidak terubah).
    - grep RecordsTeam.cshtml: `for="categoryFilter"`, `for="subCategoryFilter"`, `for="statusFilter"`, `for="dateFrom"`, `for="dateTo"` SEMUA ADA.
    - grep RecordsTeam.cshtml: `for="teamSearch"`, `for="searchScope"` masih ADA (Phase 346 tidak regresi).
  </acceptance_criteria>
  <done>Semua filter Worker Detail + Team punya label for=; grid Worker Detail responsif mobile-friendly.</done>
</task>

<task type="auto">
  <name>Task 3: POL-10 type="button" reset (3 view) + aria-current="page" pagination Team</name>
  <read_first>
    - Views/CMP/Records.cshtml (baca L79-84 reset My Records)
    - Views/CMP/RecordsWorkerDetail.cshtml (baca L169-173 reset)
    - Views/CMP/RecordsTeam.cshtml (baca L86-90 reset; L280-296 JS pagination template)
    - docs/superpowers/specs/2026-06-04-cmp-records-enhancement-design.md (§"Phase 347" baris 154: POL-10)
  </read_first>
  <action>
    A. POL-10 type="button" — di KETIGA view, re-grep tombol reset:
    - Records.cshtml My Records (perkiraan L81): `<button class="btn btn-outline-secondary w-100" onclick="clearFilters()">` → tambah `type="button"`: `<button type="button" class="btn btn-outline-secondary w-100" onclick="clearFilters()">`
    - RecordsWorkerDetail.cshtml (perkiraan L170): `<button class="btn btn-outline-secondary w-100" onclick="clearFilters()">` → tambah `type="button"`.
    - RecordsTeam.cshtml (perkiraan L87): `<button class="btn btn-outline-secondary w-100" onclick="resetTeamFilters()">` → cek; bila belum ada `type="button"`, tambahkan.
    JANGAN ubah `onclick` handler.

    B. POL-10 aria-current — di Views/CMP/RecordsTeam.cshtml, re-grep template pagination JS (perkiraan L290) yang membuat item halaman bernomor:
    `'<li class="page-item ' + active + '"><a class="page-link page-nav" data-page="' + i + '" href="#">' + i + '</a></li>'`
    Modifikasi agar item AKTIF menandai `aria-current="page"`. Karena `active` bernilai `'active'` (string) atau `''`, gunakan ekspresi turunan, mis. tambahkan variabel `var ariaCur = (i === paging.CurrentPage) ? ' aria-current="page"' : '';` (atau berdasarkan kondisi `active === 'active'`) lalu sisipkan ke `<a ...` :
    `'<li class="page-item ' + active + '"><a class="page-link page-nav"' + ariaCur + ' data-page="' + i + '" href="#">' + i + '</a></li>'`
    Pastikan logika penentuan halaman aktif identik dengan logika `active` yang ada (jangan tebak — baca baris penetapan `active` di sekitarnya, perkiraan L286-289, dan gunakan kondisi yang sama). JANGAN ubah `data-page`, `page-link page-nav` (dibaca handler klik), atau struktur prev/next.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB"; (Select-String -Path Views/CMP/RecordsTeam.cshtml -Pattern 'aria-current="page"' -SimpleMatch | Measure-Object).Count -ge 1 -and (Select-String -Path Views/CMP/Records.cshtml,Views/CMP/RecordsWorkerDetail.cshtml,Views/CMP/RecordsTeam.cshtml -Pattern 'type="button" class="btn btn-outline-secondary w-100"' -SimpleMatch | Measure-Object).Count -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - grep ketiga view: tombol reset memiliki `type="button"` (Records My Records, WorkerDetail, RecordsTeam).
    - grep RecordsTeam.cshtml: `aria-current="page"` ADA di template pagination JS.
    - grep RecordsTeam.cshtml: `page-link page-nav` + `data-page=` masih utuh (handler klik pagination tidak regresi).
    - `onclick="clearFilters()"` / `onclick="resetTeamFilters()"` tidak berubah.
  </acceptance_criteria>
  <done>Reset buttons type=button di 3 view; pagination Team menandai halaman aktif dengan aria-current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Tidak ada trust boundary baru. Plan ini hanya menambah atribut ARIA/HTML statis (role, aria-labelledby, aria-label, for, type, aria-current) dan kelas grid Bootstrap. Tidak ada input handling baru, query, authz, atau perubahan data-flow. Modifikasi JS pagination hanya menambah atribut literal pada string template (bukan data dinamis dari user).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-347-02 | Tampering | Atribut ARIA/for statis + grid class | accept | Semua nilai literal statis hardcoded di markup/JS template; `data-page`/`i` adalah indeks numerik internal (bukan user input), sudah ada sejak Phase 337. Tidak ada surface baru. |

Block-worthy threats: NONE.
</threat_model>

<verification>
- `dotnet build` 0 error (dijalankan di plan 347-04).
- grep konfirmasi atribut a11y ADA + id/handler/data-* fungsional tidak berubah (per acceptance_criteria).
- Tidak meregresi REC-01..09 Phase 346: modal data-bs-target, JS filter id, pagination data-page, export links tetap utuh.
</verification>

<success_criteria>
- POL-06: Modal training (2 view) role=dialog + aria-labelledby + btn-close aria-label="Tutup".
- POL-07: Semua filter (3 view) punya label for=; My Records search visible label.
- POL-09: Grid filter Worker Detail responsif col-12 col-sm-6 col-md-2.
- POL-10 (sebagian): reset type=button (3 view) + pagination Team aria-current=page (label tombol Lihat/Sertifikat dicover di 347-01).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/347-cmp-records-i18n-a11y-polish/347-02-SUMMARY.md`.
</output>
