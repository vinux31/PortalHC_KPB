---
phase: 348-manageassessment-monitoring-med-fix
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Controllers/CMPController.cs
  - Views/Admin/AssessmentMonitoringDetail.cshtml
autonomous: true
requirements: [MAM-04, MAM-05]
must_haves:
  truths:
    - "Peserta yang submit assessment ber-essay (belum dinilai HC) tampil 'Menunggu Penilaian' di Monitoring Detail per-peserta, BUKAN 'Completed' hijau"
    - "CompletedCount di Monitoring Detail tidak menghitung essay yang masih pending (passRate akurat)"
    - "Live SignalR push saat worker submit essay menampilkan status 'Menunggu Penilaian' + result '—', bukan Pass/Fail prematur"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Status derivation Detail cek PendingGrading SEBELUM CompletedAt (MAM-04)"
      contains: "AssessmentConstants.AssessmentStatus.PendingGrading"
    - path: "Controllers/CMPController.cs"
      provides: "SignalR workerSubmitted reload status + branch PendingGrading (MAM-05)"
      contains: "PendingGrading"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Handler workerSubmitted baca data.status, render badge pending (MAM-05)"
      contains: "Menunggu Penilaian"
  key_links:
    - from: "CMPController.SubmitExam (package path)"
      to: "SignalR workerSubmitted payload status"
      via: "reload AssessmentSessions.Status setelah GradeAndCompleteAsync"
      pattern: "freshStatus|FirstAsync"
    - from: "AssessmentMonitoringDetail.cshtml workerSubmitted handler"
      to: "badge status cell"
      via: "data.status === 'Menunggu Penilaian'"
      pattern: "data.status"
---

<objective>
Tutup 2 bug MED Tema B (essay PendingGrading status di Monitoring surface). Akar masalah: GradingService menandai session ber-essay `Status="Menunggu Penilaian"` + `CompletedAt` terisi + `IsPassed=null` BERSAMAAN, tapi (a) status derivation Detail cek `CompletedAt != null` duluan → salah-map "Completed"; (b) SignalR push hardcode `status="Completed"` + Pass/Fail dari skor MC/MA saja → monitor melihat verdict prematur yang bisa flip setelah HC nilai essay.

ISOLASI: plan ini menyentuh shared grading flow + SignalR (risiko tertinggi phase) — dipisah dari plan lain. Konstanta `AssessmentConstants.AssessmentStatus.PendingGrading` WAJIB, BUKAN literal string (D-C).

Purpose: Monitor HC melihat status essay-pending yang jujur (bukan "Completed" hijau / Pass-Fail prematur), CompletedCount/passRate akurat.
Output: status derivation Detail (controller) + SignalR push (controller) + handler view ter-fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-CONTEXT.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md
@docs/superpowers/specs/2026-06-04-manageassessment-monitoring-audit-design.md

Project rules (./CLAUDE.md): respond Bahasa Indonesia; verifikasi lokal `dotnet build` + `dotnet run` sebelum commit. No migration (D-A).

<interfaces>
<!-- Diekstrak dari codebase 2026-06-04. CRITICAL pitfall di bawah — baca sebelum implement. -->

Konstanta (Models/AssessmentConstants.cs:18):
  public const string PendingGrading = "Menunggu Penilaian"; // namespace HcPortal.Models, AssessmentConstants.AssessmentStatus.PendingGrading
  public static bool IsAssessmentSubmitted(string? status) => status == Completed || status == PendingGrading; (L78)

GradingService.GradeAndCompleteAsync (Services/GradingService.cs:189-219) — CRITICAL:
  Essay flow (hasEssay==true) pakai ExecuteUpdateAsync (bulk SQL, BYPASS change-tracker):
    .SetProperty(Status, PendingGrading).SetProperty(IsPassed, (bool?)null).SetProperty(CompletedAt, DateTime.UtcNow)...
  return true; untuk graded DAN pending — caller TIDAK bisa bedakan via bool return.
  Entity `assessment` di CMPController TIDAK ter-update (ExecuteUpdateAsync tak refresh tracked entity).
  → assessment.Status di caller tetap nilai LAMA. HARUS reload dari DB untuk tahu pending.

CMPController.SubmitExam SignalR push (Controllers/CMPController.cs:1756-1772):
  bool graded = await _gradingService.GradeAndCompleteAsync(assessment); (L1756)
  Push (L1766-1771): result = finalPercentage >= PassPercentage ? "Pass":"Fail"; status="Completed" (hardcode)
  Akses tersedia di scope: _context (DbContext), id (sessionId int), _hubContext, user.FullName, finalPercentage, shuffledIds.
  Pola reload precedent: CMPController.cs:1842 cek `assessment.Status == AssessmentConstants.AssessmentStatus.PendingGrading` SETELAH operasi.

AssessmentMonitoringDetail status derivation (Controllers/AssessmentAdminController.cs:3227-3239):
  Saat ini cek `if (a.CompletedAt != null) userStatus = "Completed";` DULU (L3230) — essay-pending punya CompletedAt terisi → salah.
  CompletedCount (L3273) = sessionViewModels.Count(s => s.UserStatus == "Completed") — inflated bila essay-pending salah-map.

AssessmentMonitoringDetail.cshtml workerSubmitted handler (Views/Admin/AssessmentMonitoringDetail.cshtml:1336-1374):
  Hardcode session.status='Completed' (L1341); tds[2].innerHTML = '<span class="badge bg-success">Completed</span>' (L1355).
  result ternary L1359-1361 sudah handle muted untuk non-Pass/non-Fail (fallback text-muted untuk '—').
  Analog badge pending (AssessmentMonitoring.cshtml:278): <span class="badge bg-warning text-dark">Menunggu Penilaian</span>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MAM-04 status derivation Detail cek PendingGrading SEBELUM CompletedAt</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:3227-3262 (status derivation `sessionViewModels` L3229-3239 + CompletedCount L3273 + PassedCount/PendingCount/InProgressCount L3274-3280) — EDIT TARGET
    - Services/WorkerDataService.cs:52-57 (analog switch derivation null/pending-first) — ANALOG ORDERING
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-04" (excerpt BUG + analog + catatan: CompletedCount auto-exclude setelah fix)
    - 348-CONTEXT.md §decisions MAM-04 (cek `if (a.Status == PendingGrading)` SEBELUM CompletedAt; konstanta BUKAN literal)
  </read_first>
  <behavior>
    - Test 1: session essay-pending (Status="Menunggu Penilaian", CompletedAt=now, IsPassed=null) → derivation menghasilkan userStatus "Menunggu Penilaian", BUKAN "Completed".
    - Test 2: session completed normal (Status="Completed", CompletedAt=now, IsPassed=true) → userStatus "Completed" (tak berubah).
    - Test 3: session Cancelled → "Dibatalkan"; session InProgress (StartedAt!=null, CompletedAt=null) → "InProgress"; session belum mulai → "Not started" (regresi guard).
  </behavior>
  <action>
    Sisipkan cabang PERTAMA di status derivation `sessionViewModels.Select(a => {...})` (AssessmentAdminController.cs:3229), SEBELUM cek `a.CompletedAt != null`. Saat ini (BUG L3229-3239):
    ```csharp
    string userStatus;
    if (a.CompletedAt != null)
        userStatus = "Completed";
    else if (a.Status == "Cancelled")
        userStatus = "Dibatalkan";
    ...
    ```
    Ubah jadi (tambah cabang pertama, konstanta WAJIB):
    ```csharp
    string userStatus;
    if (a.Status == AssessmentConstants.AssessmentStatus.PendingGrading)
        userStatus = "Menunggu Penilaian";
    else if (a.CompletedAt != null)
        userStatus = "Completed";
    else if (a.Status == "Cancelled")
        userStatus = "Dibatalkan";
    else if (a.Status == "Abandoned")
        userStatus = "Abandoned";
    else if (a.StartedAt != null)
        userStatus = "InProgress";
    else
        userStatus = "Not started";
    ```
    EFEK OTOMATIS: `CompletedCount` (L3273 `Count(s => s.UserStatus == "Completed")`) auto-exclude ungraded essay (karena userStatus jadi "Menunggu Penilaian" ≠ "Completed"). VERIFIKASI `PassedCount` (L3274 `Count(s => s.IsPassed == true)`), `PendingCount` (L3278 `Count(s => s.UserStatus == "Not started")`), `InProgressCount` (L3280) tetap konsisten — TIDAK perlu ubah (essay-pending punya IsPassed=null → tak ikut PassedCount; userStatus="Menunggu Penilaian" → tak ikut Not started/InProgress).

    **TDD:** Ekstrak logic derivation ke static helper testable HANYA bila trivial dan tak meripple konsumen lain. Karena ini inline LINQ Select dengan banyak field MonitoringSessionViewModel, ekstraksi penuh berisiko — REKOMENDASI: buat static helper `DeriveUserStatus(string? status, DateTime? completedAt, DateTime? startedAt)` di AssessmentAdminController (atau AssessmentConstants bila lebih cocok) yang berisi 6-cabang derivation, panggil dari Select. Tulis xUnit di HcPortal.Tests (pola CertificateStatusTests Theory/Fact) mengetes 6 cabang termasuk Test 1 essay-pending. Bila ekstraksi memunculkan kompleksitas tak terduga, fallback inline + andalkan UAT Plan 05 (dokumentasikan di SUMMARY).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 10; dotnet test HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "Passed!|Failed!|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - grep `Controllers/AssessmentAdminController.cs` menemukan `if (a.Status == AssessmentConstants.AssessmentStatus.PendingGrading)` (atau via helper `DeriveUserStatus`) SEBELUM `a.CompletedAt != null` di derivation Detail.
    - TIDAK ada literal string `"Menunggu Penilaian"` di kondisi cek (HARUS konstanta) — literal hanya boleh di nilai assignment `userStatus = "Menunggu Penilaian"`.
    - Bila helper diekstrak: xUnit baru di HcPortal.Tests mengetes essay-pending → "Menunggu Penilaian"; test PASS.
    - `dotnet build` 0 error; `dotnet test` → "Passed!" (tidak ada Failed).
  </acceptance_criteria>
  <done>Status derivation Detail cek PendingGrading (konstanta) sebagai cabang pertama; CompletedCount auto-exclude essay-pending; build + test PASS.</done>
</task>

<task type="auto">
  <name>Task 2: MAM-05 SignalR workerSubmitted — reload status + branch PendingGrading (controller)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs:1748-1785 (SubmitExam package path: GradeAndCompleteAsync L1756 + SignalR push L1765-1772) — EDIT TARGET
    - Controllers/CMPController.cs:1842 (precedent reload/cek assessment.Status == PendingGrading SETELAH operasi) — POLA RELOAD
    - Services/GradingService.cs:189-219 (CRITICAL: essay ExecuteUpdateAsync bypass change-tracker — assessment.Status tak ter-update) — PITFALL
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-05" (PITFALL KRITIS verbatim + solusi reload minimal-risk = default D-05)
    - 348-CONTEXT.md §decisions MAM-05 + §"Claude's Discretion" (reuse workerSubmitted dgn status override = default minimal-risk)
  </read_first>
  <action>
    PITFALL (verified GradingService.cs:189-204): essay flow set `Status=PendingGrading` via `ExecuteUpdateAsync` (bulk SQL bypass change-tracker). Entity `assessment` di CMPController TIDAK ter-update. `GradeAndCompleteAsync` return `true` untuk pending DAN completed. Maka caller HARUS reload status dari DB.

    Di SubmitExam package path, SETELAH `bool graded = await _gradingService.GradeAndCompleteAsync(assessment);` (L1756) dan guard `if (!graded)` (L1758-1763), SEBELUM blok SignalR push (L1765), tambah reload status (pola minimal-risk, no signature change ke GradingService):
    ```csharp
    // MAM-05: GradingService set essay Status=PendingGrading via ExecuteUpdateAsync (bypass change-tracker).
    // Entity `assessment` tidak ter-update — reload status dari DB untuk tahu apakah essay-pending.
    var freshStatus = await _context.AssessmentSessions
        .Where(s => s.Id == id)
        .Select(s => s.Status)
        .FirstAsync();
    bool isPendingGrading = freshStatus == AssessmentConstants.AssessmentStatus.PendingGrading;
    ```
    Lalu ubah blok SignalR push (L1766-1771) agar status & result mengikuti pending state:
    ```csharp
    {
        var submitBatchKey = $"{assessment.Title}|{assessment.Category}|{assessment.Schedule.Date:yyyy-MM-dd}";
        var result = isPendingGrading
            ? "—"
            : (finalPercentage >= assessment.PassPercentage ? "Pass" : "Fail");
        var pushStatus = isPendingGrading
            ? AssessmentConstants.AssessmentStatus.PendingGrading
            : "Completed";
        int totalQuestionsSubmit = shuffledIds.Count;
        await _hubContext.Clients.Group($"monitor-{submitBatchKey}").SendAsync("workerSubmitted",
            new { sessionId = id, workerName = user.FullName, score = finalPercentage, result, status = pushStatus, totalQuestions = totalQuestionsSubmit });
    }
    ```
    CATATAN: `result` untuk pending = em-dash "—" (`—`), konsisten dengan UI muted. `score` tetap dikirim (interim percentage MC/MA) — handler view (Task 3) akan tampilkan apa adanya, tapi status pending memberi konteks bahwa skor belum final. Konstanta WAJIB. JANGAN ubah signature `GradeAndCompleteAsync` (hindari ripple ke caller lain). JANGAN sentuh path legacy L1779-1784 (sudah error-out, tak ada essay).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - grep `Controllers/CMPController.cs` menemukan reload pattern: `.Where(s => s.Id == id)` + `.Select(s => s.Status)` + `FirstAsync()` di SubmitExam setelah GradeAndCompleteAsync.
    - grep menemukan `isPendingGrading` dipakai untuk menentukan `result` DAN `status` payload SignalR.
    - grep menemukan `AssessmentConstants.AssessmentStatus.PendingGrading` di blok push (konstanta, bukan literal).
    - TIDAK ada lagi hardcode `status = "Completed"` tanpa kondisi di payload workerSubmitted (harus `pushStatus` ternary).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>SubmitExam reload Status dari DB setelah grading; SignalR push status="Menunggu Penilaian"+result="—" untuk essay-pending, "Completed"+Pass/Fail untuk non-essay; konstanta dipakai; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: MAM-05 view handler workerSubmitted baca data.status + render badge pending</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml:1335-1374 (handler workerSubmitted — hardcode status='Completed' L1341 + badge bg-success L1355 + result ternary L1359-1361 + updateSummaryFromDOM L1374) — EDIT TARGET
    - Views/Admin/AssessmentMonitoring.cshtml:278 (analog badge pending: <span class="badge bg-warning text-dark">Menunggu Penilaian</span>) — ANALOG
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-05" catatan adaptasi (handler baca data.status; bg-warning text-dark untuk pending; result ternary sudah handle muted)
  </read_first>
  <action>
    Handler `workerSubmitted` (AssessmentMonitoringDetail.cshtml:1336) harus baca `data.status` dari payload (Task 2 sudah kirim status="Menunggu Penilaian" untuk essay-pending), bukan hardcode 'Completed'.

    Ubah baris status cell (L1354-1355). Saat ini (BUG):
    ```javascript
    // Status (col 2)
    if (tds[2]) tds[2].innerHTML = '<span class="badge bg-success">Completed</span>';
    ```
    Jadi (render berdasarkan data.status):
    ```javascript
    // MAM-05: render status dari payload (essay-pending → badge pending, bukan hijau Completed)
    if (tds[2]) {
        if (data.status === 'Menunggu Penilaian') {
            tds[2].innerHTML = '<span class="badge bg-warning text-dark">Menunggu Penilaian</span>';
        } else {
            tds[2].innerHTML = '<span class="badge bg-success">Completed</span>';
        }
    }
    ```
    Result cell (L1359-1361) TIDAK perlu diubah — ternary existing sudah menampilkan `data.result || '—'` dan className `text-muted` untuk non-Pass/non-Fail (em-dash dari Task 2 → muted, benar). Score cell (L1357) tetap (menampilkan interim score apa adanya). Sinkron `session.status` object (L1341) opsional — tapi karena handler re-implement minimal row update (tak panggil updateRow), cukup ubah status cell saja. JANGAN over-reach ke summary card (`updateSummaryFromDOM` L1374) — koord MAP-10/11/349, di luar scope 348.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB" && dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - grep `Views/Admin/AssessmentMonitoringDetail.cshtml` menemukan `data.status === 'Menunggu Penilaian'` di handler workerSubmitted.
    - grep menemukan `badge bg-warning text-dark">Menunggu Penilaian` di status cell render.
    - TIDAK ada lagi `tds[2].innerHTML = '<span class="badge bg-success">Completed</span>'` tanpa kondisi (harus dibungkus if/else data.status).
    - `dotnet build` 0 error (Razor compile).
  </acceptance_criteria>
  <done>Handler workerSubmitted render badge "Menunggu Penilaian" (bg-warning) bila data.status pending, "Completed" (bg-success) bila tidak; result cell muted untuk em-dash; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker browser → CMPController.SubmitExam | Input ter-authenticate (exam session); submit memicu SignalR push ke grup monitor HC |
| SignalR hub → HC monitor browser | Server-push payload (sessionId, status, score, result) ke grup `monitor-{batchKey}` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-05 | I (Info Disclosure) | SignalR workerSubmitted payload status="Menunggu Penilaian"+score interim | accept | Payload sudah dikirim ke grup `monitor-{batchKey}` yang hanya berisi HC/Admin ter-authorize (mekanisme join grup existing, tak diubah). Perubahan ini MENGURANGI info-leak (sebelumnya push verdict Pass/Fail prematur yang bisa salah; sekarang "—" untuk pending). Tidak ada PII baru. score interim sudah dikirim sebelumnya — tak berubah. |
| T-348-06 | T (Tampering) | freshStatus reload dari DB | accept | Reload = read-only query EF parameterized (`Where Id == id`) pada session milik worker yang baru submit (id dari session ter-authenticate). Tidak ada input user untuk di-tamper; id berasal dari exam flow server-side. |
| T-348-07 | S (Spoofing) | data.status di JS handler view | accept | data.status berasal dari server SignalR push (bukan user input). Client-side handler hanya render badge — tidak ada keputusan authz/state berdasarkan data.status. Worst case spoofed status = label salah di layar HC (visual only, server state of record = DB). |

Block-worthy: NONE. Perubahan ini correctness-fix yang MENINGKATKAN akurasi (mengurangi verdict prematur). SignalR grup authz + EF parameterization existing tak diubah. Tidak ada endpoint/input/authz baru.
</threat_model>

<verification>
- `dotnet build` 0 error + `dotnet test` PASS (termasuk xUnit MAM-04 bila helper diekstrak).
- Manual UAT (Plan 05): buat assessment ber-essay, worker submit → cek Monitoring Detail per-peserta menampilkan "Menunggu Penilaian" (bukan Completed hijau); CompletedCount tidak menghitung essay-pending.
- Manual UAT live SignalR: buka Monitoring Detail di tab HC, worker submit essay di tab lain → row update real-time ke badge "Menunggu Penilaian" + result "—", bukan Pass/Fail.
- Regresi: worker submit assessment non-essay (MC/MA only) → tetap "Completed" + Pass/Fail benar.
</verification>

<success_criteria>
- MAM-04: status derivation Detail cek PendingGrading sebelum CompletedAt; essay-pending → "Menunggu Penilaian"; CompletedCount/passRate akurat.
- MAM-05: SignalR push & handler view render pending state jujur (tidak "Completed"+Pass/Fail prematur untuk essay).
- Konstanta PendingGrading dipakai konsisten (no literal di kondisi).
- Build + test PASS; regresi non-essay tetap benar.
</success_criteria>

<output>
After completion, create `.planning/phases/348-manageassessment-monitoring-med-fix/348-02-SUMMARY.md`
</output>
