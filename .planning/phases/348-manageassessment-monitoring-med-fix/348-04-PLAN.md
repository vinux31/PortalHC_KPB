---
phase: 348-manageassessment-monitoring-med-fix
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
  - Views/Admin/AssessmentMonitoring.cshtml
  - Views/Admin/AssessmentMonitoringDetail.cshtml
autonomous: true
requirements: [MAM-10, MAM-11, MAM-12, MAM-13]
must_haves:
  truths:
    - "Badge status baris Tab1 menampilkan GroupStatus (Open/Upcoming/Closed) yang konsisten dengan filter/stats, bukan rep Status single-session"
    - "Dropdown Kategori di Assessment Monitoring list ter-populate dari AssessmentCategories aktif (data-driven), tanpa 'Proton' phantom"
    - "Tooltip badge Closed tidak menjanjikan search 'lokasi' (jujur: search judul saja)"
    - "Tombol Reshuffle worker menampilkan spinner hanya pada tombol, tidak mengganti seluruh baris"
  artifacts:
    - path: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml"
      provides: "Badge bind GroupStatus + case Closed (MAM-10) + tooltip jujur (MAM-12 kembar)"
      contains: "group.GroupStatus"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ViewBag MonitoringCategories data-driven (MAM-11)"
      contains: "MonitoringCategories"
    - path: "Views/Admin/AssessmentMonitoring.cshtml"
      provides: "Dropdown kategori dari ViewBag (MAM-11) + tooltip jujur (MAM-12)"
      contains: "MonitoringCategories"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "reshuffleWorker selector scoped ke button (MAM-13)"
      contains: "reshuffleWorker(this"
  key_links:
    - from: "_AssessmentGroupsTab badge"
      to: "GroupStatus value (Open/Upcoming/Closed)"
      via: "switch bind group.GroupStatus + render @group.GroupStatus"
      pattern: "group.GroupStatus"
    - from: "AssessmentMonitoring dropdown Kategori"
      to: "ViewBag.MonitoringCategories"
      via: "controller query AssessmentCategories IsActive OrderBy SortOrder"
      pattern: "MonitoringCategories"
    - from: "reshuffleWorker(btn)"
      to: "button element (bukan tr)"
      via: "onclick reshuffleWorker(this) + btn.dataset.sessionId"
      pattern: "reshuffleWorker(this"
---

<objective>
Tutup 4 bug MED Tema D/E/F: badge status Tab1 divergen dari GroupStatus (MAM-10), dropdown Kategori Monitoring hardcoded + "Proton" phantom (MAM-11), tooltip Closed janji search "lokasi" yang tak ada (MAM-12), selector Reshuffle bentrok `<tr>` → spinner ganti seluruh baris (MAM-13).

Purpose: Konsistensi display (badge match filter), filter Monitoring data-driven & jujur, UX reshuffle benar.
Output: `AssessmentAdminController.cs` (ViewBag categories) + `_AssessmentGroupsTab.cshtml` (badge+tooltip) + `AssessmentMonitoring.cshtml` (dropdown+tooltip) + `AssessmentMonitoringDetail.cshtml` (reshuffle selector).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-CONTEXT.md
@.planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md
@docs/superpowers/specs/2026-06-04-manageassessment-monitoring-audit-design.md

Project rules (./CLAUDE.md): Bahasa Indonesia; verifikasi lokal dotnet build+run. No migration (D-A).

<interfaces>
<!-- Diekstrak dari codebase 2026-06-04. -->

Tab1 badge status (_AssessmentGroupsTab.cshtml:195-221):
  L195-203: var statusBadge = (string)group.Status switch { "Open"=>"bg-success", "Completed"=>"bg-secondary", "Upcoming"=>"bg-info", "InProgress"=>"bg-warning text-dark", "Abandoned"=>"bg-dark", _=>"bg-secondary" };
  L221: <td><span class="badge @statusBadge">@group.Status</span></td>
  BUG: bind rep single-session `Status`, bukan derived `GroupStatus`. Dynamic shape PUNYA GroupStatus (string Open/Upcoming/Closed — verified controller L167/188).
  Analog badge GroupStatus BENAR (AssessmentMonitoring.cshtml:220-226): group.GroupStatus switch { "Open"=>"bg-success", "Upcoming"=>"bg-info text-dark", "Closed"=>"bg-secondary", _=>"bg-secondary" }

Tab1 tooltip kembar Closed (_AssessmentGroupsTab.cshtml:121): title="Closed assessment (... search judul/lokasi spesifik)"

AssessmentMonitoring action (Controllers/AssessmentAdminController.cs:2668-2674): AssessmentMonitoring(string? search, string? status, string? category) — perlu tambah ViewBag.MonitoringCategories
Analog categories query (AssessmentAdminController.cs:312-313): _context.AssessmentCategories.Include(...).OrderBy(SortOrder). Untuk Monitoring (flat cukup): _context.AssessmentCategories.Where(c => c.IsActive).OrderBy(c => c.SortOrder)

AssessmentMonitoring dropdown Kategori (AssessmentMonitoring.cshtml:122-150):
  L124: <select name="category" class="form-select">
  L126-136: hardcoded array categories termasuk ("Proton","Proton") L134 phantom + ("Assessment Proton","Assessment Proton")
  L137-147: foreach (val,label) render option, selected bila selCategory==val
  selCategory = variabel existing (cek deklarasi atas view, ViewBag.SelectedCategory atau param)

AssessmentMonitoring tooltip Closed (AssessmentMonitoring.cshtml:169): title="Closed group assessment (... search judul/lokasi spesifik)"
  Controller search (AssessmentAdminController.cs:2685-2688): query.Where(a => a.Title.ToLower().Contains(lower)) — Title only, Kota tak di-search.

reshuffleWorker (AssessmentMonitoringDetail.cshtml:739-743): function reshuffleWorker(sessionId) { var btn = document.querySelector('[data-session-id="' + sessionId + '"]'); ... }
  BUG: <tr data-session-id="@session.Id"> (L268) match duluan (DOM-first) > <button data-session-id> (L353).
  Call-site server-render (L353-354): data-session-id="@session.Id" onclick="reshuffleWorker(@session.Id)"
  Call-site JS-render (L867-868): data-session-id="' + session.sessionId + '" onclick="reshuffleWorker(' + session.sessionId + ')"
  Server reshuffle tetap jalan (sessionId dari arg) — ini glitch visual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MAM-10 badge Tab1 bind GroupStatus + case Closed + MAM-12 tooltip jujur (view)</name>
  <files>Views/Admin/Shared/_AssessmentGroupsTab.cshtml</files>
  <read_first>
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml:195-203 (switch statusBadge) + L221 (render @group.Status) — EDIT TARGET MAM-10
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml:121 (tooltip kembar Closed "judul/lokasi") — EDIT TARGET MAM-12 kembar
    - Views/Admin/AssessmentMonitoring.cshtml:220-226 (analog badge GroupStatus BENAR) — ANALOG
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-10" (drop arm non-GroupStatus, tambah Closed) + §"MAM-12" (buang "lokasi"; tooltip kembar planner putuskan ikut)
    - 348-CONTEXT.md §decisions MAM-10 (GroupStatus values Open/Upcoming/Closed, tambah case Closed) + MAM-12
  </read_first>
  <action>
    **MAM-10 — badge bind GroupStatus (_AssessmentGroupsTab.cshtml:195-203 + L221):**
    Ganti switch (L195-203) agar bind `group.GroupStatus` + drop arm yang bukan nilai GroupStatus + tambah case "Closed":
    ```csharp
    var statusBadge = (string)group.GroupStatus switch
    {
        "Open"     => "bg-success",
        "Upcoming" => "bg-info",
        "Closed"   => "bg-secondary",
        _          => "bg-secondary"
    };
    ```
    (Drop "Completed", "InProgress", "Abandoned" — BUKAN nilai GroupStatus; GroupStatus hanya Open/Upcoming/Closed per derivasi controller L156-159/177-180.)
    Ubah render L221 dari `@group.Status` jadi `@group.GroupStatus`:
    ```html
    <td><span class="badge @statusBadge">@group.GroupStatus</span></td>
    ```
    Setelah fix, baris yang di-filter "Open" tak akan lagi berlabel "Completed/Abandoned" (badge match filter/stats yang sudah pakai GroupStatus).

    **MAM-12 — tooltip kembar jujur (_AssessmentGroupsTab.cshtml:121):**
    Buang kata "lokasi" dari tooltip (controller search hanya Title.Contains). Ubah L121:
    ```
    title="Closed assessment (hidden by default — pilih filter 'Closed' atau 'Semua Status' untuk lihat, atau search judul/lokasi spesifik)"
    ```
    jadi:
    ```
    title="Closed assessment (hidden by default — pilih filter 'Closed' atau 'Semua Status' untuk lihat, atau search judul spesifik)"
    ```
    (Konsisten dengan fix MAM-12 di AssessmentMonitoring.cshtml Task 3. Tooltip kembar ikut diperbaiki untuk konsistensi per PATTERNS.)
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Views/Admin/Shared/_AssessmentGroupsTab.cshtml` menemukan `(string)group.GroupStatus switch` (bind GroupStatus, bukan Status).
    - grep menemukan `"Closed"   => "bg-secondary"` (case Closed ditambah) dalam switch.
    - grep menemukan `<span class="badge @statusBadge">@group.GroupStatus</span>` (render GroupStatus).
    - grep menemukan switch TIDAK lagi punya arm `"Abandoned"` / `"InProgress"` / `"Completed"` (drop arm non-GroupStatus).
    - grep tooltip L121 TIDAK lagi mengandung "judul/lokasi spesifik" — menjadi "judul spesifik".
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Badge Tab1 bind GroupStatus + case Closed + drop arm non-GroupStatus; tooltip kembar buang "lokasi"; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: MAM-11 dropdown Kategori Monitoring data-driven (controller)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:2668-2674 (AssessmentMonitoring action signature + awal body) — EDIT TARGET
    - Controllers/AssessmentAdminController.cs:312-313 (analog query AssessmentCategories Include/OrderBy SortOrder) — ANALOG
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-11" (query IsActive OrderBy SortOrder via ViewBag, flat list cukup) — ANALOG
    - 348-CONTEXT.md §decisions MAM-11 (data-driven _context.AssessmentCategories.Where(IsActive).OrderBy(SortOrder) via ViewBag; buang Proton phantom)
  </read_first>
  <action>
    Di action `AssessmentMonitoring` (AssessmentAdminController.cs:2670), populate ViewBag dengan kategori aktif data-driven. Tambah SEBELUM `return View(...)` (atau di awal body setelah validasi), query flat (Monitoring tak perlu hierarki):
    ```csharp
    // MAM-11: dropdown Kategori data-driven dari AssessmentCategories aktif (buang hardcode "Proton" phantom).
    ViewBag.MonitoringCategories = await _context.AssessmentCategories
        .Where(c => c.IsActive)
        .OrderBy(c => c.SortOrder)
        .ThenBy(c => c.Name)
        .Select(c => c.Name)
        .ToListAsync();
    ```
    CATATAN: verifikasi nama property entity `AssessmentCategory` saat execute — `IsActive` (bool), `SortOrder` (int), `Name` (string) sudah terbukti dipakai di SetCategoriesViewBag L312-313. Bila view butuh value+label berbeda, sesuaikan Select; untuk Monitoring filter, value=label=Name cukup. JANGAN ubah signature action atau logic query sessions existing.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Controllers/AssessmentAdminController.cs` menemukan `ViewBag.MonitoringCategories` di action AssessmentMonitoring.
    - grep menemukan `.Where(c => c.IsActive)` + `.OrderBy(c => c.SortOrder)` untuk AssessmentCategories di scope MonitoringCategories.
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Action AssessmentMonitoring set ViewBag.MonitoringCategories dari AssessmentCategories IsActive OrderBy SortOrder; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: MAM-11 dropdown view dari ViewBag + MAM-12 tooltip Monitoring jujur (view)</name>
  <files>Views/Admin/AssessmentMonitoring.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoring.cshtml:122-150 (dropdown Kategori hardcoded array L126-136 + foreach render L137-147) — EDIT TARGET MAM-11
    - Views/Admin/AssessmentMonitoring.cshtml:169 (tooltip Closed "judul/lokasi") — EDIT TARGET MAM-12
    - Views/Admin/AssessmentMonitoring.cshtml (cari deklarasi `selCategory` di atas — variabel existing untuk selected option)
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-11" (view loop ViewBag.MonitoringCategories) + §"MAM-12"
    - 348-CONTEXT.md §decisions MAM-11 + MAM-12
  </read_first>
  <action>
    **MAM-11 — dropdown dari ViewBag (AssessmentMonitoring.cshtml:124-149):**
    Ganti array hardcoded (L126-136) + foreach (L137-147) dengan render dari `ViewBag.MonitoringCategories` (Task 2). Pertahankan opsi "Semua Kategori" + selected-state via `selCategory`:
    ```csharp
    <select name="category" class="form-select">
        @{
            var monitoringCategories = ViewBag.MonitoringCategories as List<string> ?? new List<string>();
        }
        @if (string.IsNullOrEmpty(selCategory))
        {
            <option value="" selected>Semua Kategori</option>
        }
        else
        {
            <option value="">Semua Kategori</option>
        }
        @foreach (var cat in monitoringCategories)
        {
            if (selCategory == cat)
            {
                <option value="@cat" selected>@cat</option>
            }
            else
            {
                <option value="@cat">@cat</option>
            }
        }
    </select>
    ```
    "Proton" phantom otomatis hilang (data-driven = hanya kategori real di DB). CATATAN: verifikasi nama variabel `selCategory` saat execute (cek deklarasi di atas view — bila beda nama, sesuaikan). JANGAN hapus elemen `<select>` wrapper/label di sekitarnya.

    **MAM-12 — tooltip jujur (AssessmentMonitoring.cshtml:169):**
    Buang "lokasi" dari tooltip Closed:
    ```
    title="Closed group assessment (hidden default — pilih filter 'Closed' atau 'Semua Status' untuk lihat, atau search judul/lokasi spesifik)"
    ```
    jadi:
    ```
    title="Closed group assessment (hidden default — pilih filter 'Closed' atau 'Semua Status' untuk lihat, atau search judul spesifik)"
    ```
    (Extend search ke Kota = out-of-scope MED → MAP-23/349.)
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Views/Admin/AssessmentMonitoring.cshtml` menemukan `ViewBag.MonitoringCategories as List<string>` (render data-driven).
    - grep menemukan `@foreach (var cat in monitoringCategories)` (loop ViewBag).
    - grep TIDAK lagi menemukan hardcode `("Proton", "Proton")` di dropdown Kategori.
    - grep tooltip L169 TIDAK lagi mengandung "judul/lokasi spesifik" — menjadi "judul spesifik".
    - `dotnet build` 0 error (Razor compile).
  </acceptance_criteria>
  <done>Dropdown Kategori render dari ViewBag.MonitoringCategories (Proton phantom hilang); tooltip buang "lokasi"; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 4: MAM-13 reshuffleWorker selector scoped ke button (view)</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml:739-743 (function reshuffleWorker — querySelector ambigu) — EDIT TARGET
    - Views/Admin/AssessmentMonitoringDetail.cshtml:353-354 (call-site server-render onclick) — EDIT TARGET
    - Views/Admin/AssessmentMonitoringDetail.cshtml:867-868 (call-site JS-render onclick string) — EDIT TARGET
    - Views/Admin/AssessmentMonitoringDetail.cshtml:268 (<tr data-session-id> yang bentrok) — KONTEKS
    - .planning/phases/348-manageassessment-monitoring-med-fix/348-PATTERNS.md §"MAM-13" (ubah signature reshuffleWorker(this) atau attribute distinct; update 2 call-site)
    - 348-CONTEXT.md §decisions MAM-13 (scope selector ke tombol; reshuffleWorker(this) hindari querySelector ambigu sepenuhnya)
  </read_first>
  <action>
    Hindari `document.querySelector('[data-session-id=...]')` ambigu (match `<tr>` duluan). Ubah signature `reshuffleWorker` menerima elemen button yang dilempar (`this`), ambil sessionId dari `dataset`.

    **Function (AssessmentMonitoringDetail.cshtml:739-743):**
    ```javascript
    function reshuffleWorker(sessionId) {
        var btn = document.querySelector('[data-session-id="' + sessionId + '"]');
        var originalHtml = btn.innerHTML;
        ...
    ```
    jadi:
    ```javascript
    function reshuffleWorker(btn) {
        var sessionId = btn.dataset.sessionId;
        var originalHtml = btn.innerHTML;
        ...
    ```
    (Sisanya — spinner set ke `btn.innerHTML`, `btn.disabled`, $.ajax dengan `data.sessionId = sessionId`, restore `btn.innerHTML = originalHtml` — TETAP, hanya sumber `btn` dan `sessionId` berubah. Pastikan seluruh referensi `btn`/`sessionId` di body fungsi konsisten dengan signature baru.)

    **Call-site server-render (L354):** `onclick="reshuffleWorker(@session.Id)"` → `onclick="reshuffleWorker(this)"`. (Button sudah punya `data-session-id="@session.Id"` L353 — dataset terbaca.)

    **Call-site JS-render (L868):** `onclick="reshuffleWorker(' + session.sessionId + ')"` → `onclick="reshuffleWorker(this)"`. (Button JS-render sudah punya `data-session-id="' + session.sessionId + '"` L867.)

    Setelah fix, `btn` SELALU elemen button (dari `this`), spinner hanya mengganti isi tombol, restore benar. Server reshuffle tetap jalan (sessionId dari dataset). Tidak ada lagi querySelector ambigu.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB"; dotnet build HcPortal.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Views/Admin/AssessmentMonitoringDetail.cshtml` menemukan `function reshuffleWorker(btn)` + `var sessionId = btn.dataset.sessionId;`.
    - grep menemukan `onclick="reshuffleWorker(this)"` (server-render) DAN `onclick="reshuffleWorker(this)"` dalam string JS-render (`reshuffleWorker(this)`).
    - grep TIDAK lagi menemukan `var btn = document.querySelector('[data-session-id="' + sessionId` (querySelector ambigu dihapus).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>reshuffleWorker menerima button (this), sessionId dari dataset; 2 call-site update ke this; querySelector ambigu dihapus; spinner hanya pada tombol; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → AssessmentMonitoring / AssessmentMonitoringDetail | Input ter-authorize `[Authorize(Roles="Admin, HC")]`; dropdown kategori dari DB; reshuffle POST ber-antiforgery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-11 | I (Info Disclosure) | ViewBag.MonitoringCategories (nama kategori dari DB) | accept | Nama kategori assessment bukan data sensitif/PII; sudah tampil di seluruh UI admin. Query `IsActive` membatasi ke kategori aktif. Action sudah `[Authorize(Roles="Admin, HC")]`. Tidak ada perubahan surface authz. |
| T-348-12 | T (Tampering) | reshuffleWorker sessionId dari dataset (client) | accept | sessionId dikirim ke ReshufflePackage POST yang sudah `[ValidateAntiForgeryToken]` + `[Authorize]` (existing, tak diubah). Bila attacker ubah dataset, server tetap validasi authz + session ownership di ReshufflePackage. Perubahan ini murni sumber sessionId di client (dataset vs arg) — tidak melemahkan validasi server. |
| T-348-13 | T (XSS) | dropdown render @cat dari DB | accept | Razor `@cat` auto HTML-encode (default). Nama kategori dari AssessmentCategories (di-input via admin form yang sudah ter-validasi). Tidak ada raw/Html.Raw. |

Block-worthy: NONE. Semua perubahan = display/UX correctness pada surface admin existing. Authz (role) + antiforgery (reshuffle) + Razor encoding tak diubah. Tidak ada endpoint/input/query baru yang memperluas attack surface.
</threat_model>

<verification>
- `dotnet build` 0 error.
- Manual UAT (Plan 05): Tab1 — baris yang di-filter "Open" berlabel "Open" (bukan "Completed/Abandoned"); grup Closed (bila di-filter "Semua Status") berlabel "Closed".
- Manual UAT: Monitoring list dropdown Kategori berisi kategori real dari DB, TANPA "Proton" phantom; pilih kategori baru (yang admin buat) → filter berfungsi.
- Manual UAT: hover badge Closed → tooltip tak menyebut "lokasi".
- Manual UAT: klik Reshuffle pada worker (Not started/Abandoned) → spinner muncul HANYA di tombol, baris utuh; reshuffle sukses.
</verification>

<success_criteria>
- MAM-10: badge Tab1 = GroupStatus (Open/Upcoming/Closed) konsisten dengan filter/stats; case Closed ada; arm non-GroupStatus di-drop.
- MAM-11: dropdown Kategori Monitoring data-driven dari AssessmentCategories aktif; "Proton" phantom hilang.
- MAM-12: tooltip Closed (kedua surface) tak menjanjikan search "lokasi".
- MAM-13: reshuffle selector scoped ke button; spinner tak ganti seluruh baris.
- Build 0 error.
</success_criteria>

<output>
After completion, create `.planning/phases/348-manageassessment-monitoring-med-fix/348-04-SUMMARY.md`
</output>
