---
phase: 349-manageassessment-monitoring-low-polish
plan: 05
type: execute
wave: 5
depends_on: [349-01, 349-02, 349-03, 349-04]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ManageAssessment.cshtml
  - HcPortal.Tests/ManageAssessmentLowPolishTests.cs
autonomous: false
requirements: [MAP-22, MAP-13, MAP-23]
must_haves:
  truths:
    - "ManageAssessmentTab_History signature tidak lagi punya param mati page/pageSize/statusFilter (kecuali yang dipakai log)"
    - "Wiring urlHistory di ManageAssessment.cshtml tidak kirim param mati; urlTraining tetap kirim page (fungsional MAM-07)"
    - "Test file baru ManageAssessmentLowPolishTests.cs hijau: MAP-13 (TotalCount exclude Cancelled) + MAP-23 (search Category predicate)"
    - "Full suite dotnet test HcPortal.Tests hijau (≥98 dari Phase 348 + test baru)"
    - "Playwright UAT 5 Success Criteria phase 349 PASS + browser-verify MAP-10 card-sum invariant + MAP-13 progress-100%"
  artifacts:
    - path: "HcPortal.Tests/ManageAssessmentLowPolishTests.cs"
      provides: "unit test MAP-13 exclude Cancelled + MAP-23 search Category predicate"
      contains: "ManageAssessmentLowPolishTests"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "MAP-22 drop param mati ManageAssessmentTab_History"
      contains: "ManageAssessmentTab_History"
    - path: ".planning/phases/349-manageassessment-monitoring-low-polish/349-UAT.md"
      provides: "checklist UAT 5 SC Bahasa Indonesia"
      contains: "MAP-10"
  key_links:
    - from: "ManageAssessment.cshtml urlHistory wiring"
      to: "ManageAssessmentTab_History signature"
      via: "query param match"
      pattern: "urlHistory"
---

<objective>
Tutup item code-hygiene terakhir (MAP-22 drop param mati History + cleanup wiring), tulis test file logic-bearing baru untuk Nyquist (MAP-13 exclude Cancelled + MAP-23 search Category), lalu jalankan PHASE GATE penuh: full xUnit suite + Playwright UAT 5 Success Criteria + browser-verify invariant kritis (MAP-10 card-sum + MAP-13 progress-100%). Ini plan penutup v22.0.

Purpose: MAP-22 = controller signature cleanup (Wave 5 — controller di-edit terakhir setelah Plan 02/03/04 selesai, serialize sempurna pada AssessmentAdminController.cs). Plus Nyquist gate: 17/23 MAP = visual (Playwright), tapi MAP-13/23 logic-bearing WAJIB punya automated test (RESEARCH §Validation Architecture). Checkpoint human-verify menutup phase + milestone v22.0.
Output: param mati dibuang; test baru hijau; full suite hijau; UAT 5 SC PASS; phase + milestone siap close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/349-manageassessment-monitoring-low-polish/349-CONTEXT.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-RESEARCH.md
@.planning/phases/349-manageassessment-monitoring-low-polish/349-UI-SPEC.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- MAP-22: History action param mati. Pitfall 8 — Training param SEKARANG DIPAKAI (MAM-07), JANGAN drop. -->
Dari Controllers/AssessmentAdminController.cs:
```csharp
// L301 — page/pageSize/statusFilter TAK DIPAKAI (hanya page di-log L319):
public async Task<IActionResult> ManageAssessmentTab_History(string? search, int page = 1, int pageSize = 20, string? statusFilter = null)
// L245 — page/pageSize DIPAKAI (MAM-07) → JANGAN drop:
public async Task<IActionResult> ManageAssessmentTab_Training(string? search, int page = 1, int pageSize = 20, ...)
```
<!-- Pola test Phase 348 (xUnit static helper + PaginationHelper). -->
Dari HcPortal.Tests/ManageAssessmentMedFixTests.cs:
```csharp
using HcPortal.Controllers; using HcPortal.Helpers; using HcPortal.Models; using Xunit;
namespace HcPortal.Tests;
public class ManageAssessmentMedFixTests { [Fact] public void Pagination_... { var r = PaginationHelper.Calculate(...); Assert.Equal(...); } }
// AssessmentAdminController.DeriveUserStatus(...) dipanggil sebagai static — pola test logic-bearing.
```
<!-- MAP-13 helper-extract: jika TotalCount exclude-Cancelled di-extract jadi static helper (mis. CountExcludingCancelled), testable seperti DeriveUserStatus. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MAP-22 drop param mati History + cleanup wiring urlHistory</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (ManageAssessmentTab_History L301-322 + ManageAssessmentTab_Training L245)
    - Views/Admin/ManageAssessment.cshtml (urlHistory + urlTraining wiring L17,19)
    - 349-RESEARCH.md Pitfall 8 (Training param TIDAK lagi dead) + §Open Questions #2 (cleanup wiring) + §phase_requirements MAP-22
    - 349-UI-SPEC.md §Display Binding Contract (MAP-22)
  </read_first>
  <action>
    Re-Grep kedua action + wiring dulu (Pitfall 1, 8).

    **MAP-22 drop param mati** di `ManageAssessmentTab_History` (~L301-302):
    - `page`/`pageSize`/`statusFilter` TAK DIPAKAI (hanya `page` di-log L319). Drop ketiga param dari signature → `ManageAssessmentTab_History(string? search)`.
    - Hapus referensi `page` di log L317-319 (ganti log agar tak butuh `page`, atau drop param `page` dari format string). History tidak paginate server-side (client-filter), jadi `page` tak bermakna.
    - JANGAN ubah `ManageAssessmentTab_Training` (L245) — `page`/`pageSize` SEKARANG DIPAKAI (MAM-07 Phase 348). Pitfall 8: drop = break pagination Tab2.

    **Cleanup wiring** di `ManageAssessment.cshtml` (~L17,19):
    - `urlHistory` string interpolation: hapus `&page=...`/`&pageSize=...`/`&statusFilter=...` yang no-op (drop param mati dari URL History).
    - `urlTraining`: BIARKAN `page` (fungsional MAM-07).
    - HTMX aman: param ekstra di URL di-ignore action tanpa param, tapi cleanup = jujur + hapus dead wiring (D pilih drop, bukan dokumentasi-intent).

    JANGAN ubah signature service (`GetWorkersInSection`/`GetAllWorkersHistory`) — out-of-scope (Anti-Pattern RESEARCH). MAP-22 hanya drop param di ACTION + wiring.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj -c Debug` exits 0 (build-gate membuktikan signature compile — Nyquist MAP-22)
    - `ManageAssessmentTab_History` signature TIDAK lagi punya `pageSize` maupun `statusFilter`; `page` dibuang dari signature & log
    - `ManageAssessmentTab_Training` signature TETAP punya `page`/`pageSize` (tidak disentuh — Pitfall 8)
    - `ManageAssessment.cshtml` `urlHistory` TIDAK lagi kirim `pageSize`/`statusFilter`; `urlTraining` TETAP kirim `page`
  </acceptance_criteria>
  <done>History action bebas param mati; Training param utuh (fungsional); urlHistory wiring bersih; build 0 error.</done>
</task>

<task type="tdd">
  <name>Task 2: Nyquist test file baru — MAP-13 (exclude Cancelled) + MAP-23 (search Category)</name>
  <read_first>
    - HcPortal.Tests/ManageAssessmentMedFixTests.cs (pola xUnit static helper + PaginationHelper Phase 348)
    - HcPortal.Tests/WorkerDataServiceSearchTests.cs (pola test search predicate)
    - Controllers/AssessmentAdminController.cs (MAP-13 TotalCount exclude Cancelled dari Plan 03 + MAP-23 search Category dari Plan 03)
    - 349-RESEARCH.md §Validation Architecture (Test Map + Wave 0 Gaps) + §phase_requirements MAP-13/23
  </read_first>
  <behavior>
    - MAP-13: count session dengan campuran status → exclude Cancelled = total - jumlah Cancelled. Progress 100% tercapai saat semua non-Cancelled completed.
      - Test 1: 5 session (3 Completed, 1 InProgress, 1 Cancelled) → TotalCount(exclude) = 4.
      - Test 2: semua Cancelled → TotalCount(exclude) = 0 (tak crash; progress N/A).
      - Test 3: 0 Cancelled → TotalCount(exclude) = total (parity dengan g.Count() lama).
    - MAP-23: search predicate match Title ATAU Category (case-insensitive).
      - Test 4: search "kepemimpinan" match assessment dengan Category="Kepemimpinan" meski Title beda.
      - Test 5: search match Title (regression — perilaku lama tetap).
      - Test 6: search yang tak match Title maupun Category → 0 hasil.
  </behavior>
  <action>
    Buat `HcPortal.Tests/ManageAssessmentLowPolishTests.cs` (xUnit, pola `ManageAssessmentMedFixTests.cs`). Header komentar Bahasa Indonesia: jelaskan cakupan (MAP-13 exclude Cancelled + MAP-23 search Category; sisanya visual Playwright).

    **MAP-13:** Bila Plan 03 meng-extract logika exclude-Cancelled jadi static helper (mis. `CountExcludingCancelled(IEnumerable<...>, ...)`), test helper itu langsung (pola `DeriveUserStatus`). Bila TIDAK di-extract (inline `g.Count(a => a.Status != Cancelled)`), tulis test atas LINQ predicate setara menggunakan in-memory list session stub (object anonim/record dengan `Status`), assert `list.Count(a => a.Status != AssessmentConstants.AssessmentStatus.Cancelled)` sesuai Test 1-3. Pakai konstanta `AssessmentConstants.AssessmentStatus.Cancelled`.

    **MAP-23:** Test predicate search setara: in-memory list assessment stub (Title + Category), assert `list.Where(a => a.Title.ToLower().Contains(lower) || a.Category.ToLower().Contains(lower))` sesuai Test 4-6. (Pola in-memory predicate mirip `WorkerDataServiceSearchTests.cs`; tak perlu DbContext bila predicate murni testable atas list — bila Plan 03 extract helper search, test helper.)

    Pastikan test FAIL bila predicate salah (mis. exclude Cancelled tertukar, atau search hanya Title). Semua test pakai konstanta, BUKAN literal status.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter FullyQualifiedName~ManageAssessmentLowPolish</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test HcPortal.Tests --filter FullyQualifiedName~ManageAssessmentLowPolish` exits 0
    - File `HcPortal.Tests/ManageAssessmentLowPolishTests.cs` ada, namespace `HcPortal.Tests`, ≥6 [Fact]/[Theory] (3 MAP-13 + 3 MAP-23)
    - Test memakai `AssessmentConstants.AssessmentStatus.Cancelled` (bukan literal)
    - MAP-13 Test 1: 5 session (3 Completed/1 InProgress/1 Cancelled) → exclude-Cancelled = 4 PASS
    - MAP-23 Test 4: search "kepemimpinan" match Category-only PASS
  </acceptance_criteria>
  <done>Test file baru hijau; MAP-13 exclude-Cancelled + MAP-23 search-Category ter-otomasi (Nyquist gate untuk item logic-bearing).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PHASE GATE — full suite + Playwright UAT 5 SC + browser-verify invariant</name>
  <read_first>
    - .planning/ROADMAP.md §Phase 349 (5 Success Criteria)
    - 349-RESEARCH.md §Validation Architecture → Sampling Rate (Phase gate) + §Environment Availability (app run ASPNETCORE_ENVIRONMENT=Development http://localhost:5277)
    - reference_dev_credentials (admin@pertamina.com lokal untuk /Admin/* — dari MEMORY)
    - CLAUDE.md Develop Workflow (verifikasi lokal sebelum commit) + Seed Workflow (bila UAT butuh data → snapshot + journal + restore)
  </read_first>
  <what-built>
    Seluruh 23 MAP Phase 349 (Plan 01-05): i18n Monitoring Detail/NIP, a11y chevron+aria, empty-state/feedback Tab1/Tab3, 7 kartu summary Detail (MAP-10), TotalCount exclude Cancelled (MAP-13), Pre-Post Regenerate Token (MAP-17), search Category (MAP-23), drop dead var/param (MAP-11/22). Test baru MAP-13/23 hijau.
  </what-built>
  <action>
    Sebelum checkpoint, OTOMASI dulu (jangan minta user kerjakan yang bisa Claude):
    1. `dotnet build HcPortal.csproj -c Debug` → 0 error.
    2. `dotnet test HcPortal.Tests` → full suite hijau (≥98 dari Phase 348 + test baru ManageAssessmentLowPolish). Bila ada gagal → fix sebelum checkpoint.
    3. Jalankan app lokal: `ASPNETCORE_ENVIRONMENT=Development` + `http://localhost:5277` (jangan Dev/Prod — CLAUDE.md). Login admin lokal.
    4. Playwright MCP UAT — verifikasi 5 Success Criteria ROADMAP §Phase 349:
       - SC1 (i18n): Monitoring Detail Indonesia penuh (header "Nama/Progres/Nilai/Hasil/Selesai Pada/Aksi", "Kembali ke Monitoring", "Ekspor Hasil", "Belum ada sesi"); History Training "NIP".
       - SC2 (a11y): chevron Tab1/Tab2 rotate saat expand; aria-label toggle hadir; Tab3 drill-down tanpa ARIA nested (hanya tombol Lihat).
       - SC3 (empty-state/feedback): Tab1 filter-aware ("Tidak ada assessment untuk filter ini" + Reset Semua Filter saat kategori aktif); Tab3 0-match "Tidak ada hasil untuk filter ini." + counter "Menampilkan X dari Y"; skeleton match kolom.
       - SC4 (display nits): kartu Abandoned ada + invariant Total = jumlah 6 kartu lain (BROWSER-VERIFY MAP-10 card-sum — hitung manual di layar); progress bar Monitoring list bisa 100% (BROWSER-VERIFY MAP-13 — grup tanpa Cancelled aktif progress 100%); subtitle list tanpa "real-time"; kategori tidak dobel.
       - SC5 (code-hygiene): `dotnet build` 0 error; no visual regression spot-check (Tab1/Tab2/Tab3 + Monitoring list + Detail render normal).
    5. Bila UAT butuh data (mis. grup dengan essay-pending untuk card "Menunggu Penilaian", atau session Cancelled untuk MAP-13): klasifikasi `temporary+local-only`, snapshot DB lokal, catat `docs/SEED_JOURNAL.md`, restore setelah selesai (CLAUDE.md Seed Workflow).
    6. Catat hasil di `.planning/phases/349-manageassessment-monitoring-low-polish/349-UAT.md` (Bahasa Indonesia, per-SC PASS/FAIL + screenshot).
  </action>
  <how-to-verify>
    Setelah Claude otomasi di atas selesai dan melaporkan hasil:
    1. Buka `http://localhost:5277/Admin/ManageAssessment` (login admin lokal) — cek Tab1/Tab2/Tab3 (chevron rotate, empty-state, NIP, counter Tab3).
    2. Buka `http://localhost:5277/Admin/AssessmentMonitoring` — cek subtitle tanpa "real-time", search "kategori" muncul hasil, Pre-Post punya dropdown Regenerate Token, progress bar grup bisa 100%.
    3. Buka satu Monitoring Detail — cek header Indonesia + 7 kartu summary, dan HITUNG: apakah angka Total = jumlah 6 kartu lain (Selesai+Sedang Mengerjakan+Belum Mulai+Dibatalkan+Abandoned+Menunggu Penilaian)?
    4. Konfirmasi tidak ada regresi visual mencolok.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila 5 SC + invariant card-sum + progress-100% PASS, atau jelaskan isu yang ditemukan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server (History tab fetch) | Param mati dibuang; action terima hanya `search` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-10 | Tampering | ManageAssessmentTab_History param drop | accept | Drop param mati = mengurangi attack surface (tak ada param tak-tervalidasi diterima); search tetap di-handle service existing |
| T-349-11 | Info | test file (in-memory stub) | accept | Test data sintetik in-memory; tak ada kredensial/PII; tak menyentuh DB Dev/Prod |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj -c Debug` exits 0
- `dotnet test HcPortal.Tests --filter FullyQualifiedName~ManageAssessmentLowPolish` exits 0 (test baru)
- `dotnet test HcPortal.Tests` full suite hijau (≥98 + test baru)
- Playwright UAT 5 SC PASS + browser-verify MAP-10 card-sum invariant + MAP-13 progress-100%
- 349-UAT.md dibuat (Bahasa Indonesia, per-SC)
- Human-verify checkpoint approved
</verification>

<success_criteria>
- MAP-22 param mati History dibuang; Training param utuh; wiring urlHistory bersih
- Test file baru hijau (MAP-13 exclude Cancelled + MAP-23 search Category)
- Full xUnit suite hijau (≥98 + test baru)
- Playwright UAT 5 Success Criteria PASS
- Browser-verify: MAP-10 card-sum invariant (Total = jumlah 6 kartu) + MAP-13 progress-100%
- `dotnet build` 0 error; no visual regression
- Phase 349 + milestone v22.0 siap close
</success_criteria>

<output>
After completion, create `.planning/phases/349-manageassessment-monitoring-low-polish/349-05-SUMMARY.md` + `.planning/phases/349-manageassessment-monitoring-low-polish/349-UAT.md`
</output>
