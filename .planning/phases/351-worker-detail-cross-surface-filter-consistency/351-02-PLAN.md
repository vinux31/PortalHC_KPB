---
phase: 351-worker-detail-cross-surface-filter-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - HcPortal.Tests/BuildActualCategoriesTests.cs
autonomous: true
requirements: [SF-04, SF-05]
must_haves:
  truths:
    - "CMPController punya helper public static BuildActualCategories(IEnumerable<UnifiedTrainingRecord>) yang distinct non-empty case-insensitive ordered"
    - "RecordsWorkerDetail action menge-set ViewBag.ActualCategoriesJson dari unifiedRecords actual (bukan master) tanpa mengubah authz :543-556"
    - "Records (My Records) action menge-set ViewBag.ActualCategoriesJson dari unified untuk SEMUA role level"
    - "xUnit [Fact] membuktikan BuildActualCategories: OJT/ojt/Legacy/null/'' → [Legacy, OJT]"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "BuildActualCategories helper + 2 ViewBag.ActualCategoriesJson"
      contains: "BuildActualCategories"
    - path: "HcPortal.Tests/BuildActualCategoriesTests.cs"
      provides: "Unit test untuk distinct-actual option builder"
      contains: "BuildActualCategories"
  key_links:
    - from: "Controllers/CMPController.cs RecordsWorkerDetail"
      to: "ViewBag.ActualCategoriesJson"
      via: "BuildActualCategories(unifiedRecords)"
      pattern: "ActualCategoriesJson"
    - from: "Controllers/CMPController.cs Records"
      to: "ViewBag.ActualCategoriesJson"
      via: "BuildActualCategories(unified) di luar block roleLevel<=4"
      pattern: "ActualCategoriesJson"
    - from: "HcPortal.Tests/BuildActualCategoriesTests.cs"
      to: "CMPController.BuildActualCategories"
      via: "direct static call"
      pattern: "BuildActualCategories"
---

<objective>
SF-04 + SF-05 backend: ganti sumber opsi Kategori dari master `AssessmentCategories` ke distinct-actual `unifiedRecords.Kategori`. Tambah helper `public static BuildActualCategories` (DRY untuk kedua surface), set `ViewBag.ActualCategoriesJson` di action `RecordsWorkerDetail` (SF-04) DAN `Records` (SF-05), plus xUnit `[Fact]` membuktikan logika.

Purpose: opsi dari record aktual membuat kategori free-text/legacy bisa difilter + menghapus opsi mati (master tanpa record). Sumber sama (opsi + data-category) menjaga compare exact-equals tetap aman.
Output: `CMPController.cs` (helper + 2 ViewBag) + `HcPortal.Tests/BuildActualCategoriesTests.cs`.

CRITICAL (locked): TIDAK mengubah `GetUnifiedRecords`/`WorkerDataService`. TIDAK mengubah authz `RecordsWorkerDetail:543-556` (byte-identical). SubKategori cascade tetap master-based (di luar scope). `ViewBag.MasterCategoriesJson` & `allCats` TIDAK dihapus (masih dipakai SubCategoryMapJson + Team View).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-RESEARCH.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Models/UnifiedTrainingRecord.cs — Kategori sumber data: -->
public class UnifiedTrainingRecord { ...; public string? Kategori { get; set; }; ... }
// GetUnifiedRecords mengisi Kategori = a.Category (assessment, free-text) ATAU t.Kategori (training).

<!-- CMPController.cs RecordsWorkerDetail (VERIFIED line refs) — current state: -->
// :538  public async Task<IActionResult> RecordsWorkerDetail(string workerId, ...)
// :543-556  AUTHZ BLOCK — PRESERVE VERBATIM:
//   if (workerId != user.Id) { if (roleLevel >= 5) return Forbid();
//     if (roleLevel == 4) { var targetUser = await _context.Users.FindAsync(workerId);
//       if (targetUser == null || targetUser.Section != user.Section) return Forbid(); } }
// :564  var unifiedRecords = await _workerDataService.GetUnifiedRecords(workerId);
// :566-575 allCats query (KEEP — SubCategoryMapJson butuh ini)
// :576  ViewBag.SubCategoryMapJson = ... (KEEP)
// :577-579 ViewBag.MasterCategoriesJson = JsonSerializer.Serialize(allCats.Select(c=>c.Name).OrderBy(n=>n).ToList())  ← REPLACE source utk Worker Detail

<!-- CMPController.cs Records / My Records (VERIFIED line refs): -->
// :479  public async Task<IActionResult> Records(string? section, ...)
// :484  var unified = await _workerDataService.GetUnifiedRecords(user.Id);
// :490-499 var vm = new CMPRecordsViewModel { ... UnifiedRecords = unified ... }
// :502  if (roleLevel <= 4) { ... :527 ViewBag.MasterCategoriesJson ... }  ← block Team-View-only
// :534  return View("Records", vm);

<!-- Helper analog (in-file private static convention) CMPController.cs:3933 BuildSertifikatGroups, :3962 MapKategori -->

<!-- xUnit analog HcPortal.Tests/WorkerDataServiceSearchTests.cs — namespace HcPortal.Tests; [Fact]; Assert.Equal -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper BuildActualCategories + xUnit [Fact] (RED→GREEN)</name>
  <files>Controllers/CMPController.cs, HcPortal.Tests/BuildActualCategoriesTests.cs</files>
  <behavior>
    BuildActualCategories(IEnumerable<UnifiedTrainingRecord>) → List<string>:
    - Test 1: records Kategori = "OJT","ojt","Legacy Free Text",null,"" → hasil ["Legacy Free Text","OJT"] (drop null+empty, case-insensitive dedupe, OrderBy ascending).
    - Test 2: list kosong → hasil [] (kosong, bukan null).
    - Test 3: semua null/empty → hasil [].
    - Test 4: dedupe ordinal-ignore-case mempertahankan kemunculan pertama nilai tapi urutan output alfabetis.
  </behavior>
  <read_first>
    - HcPortal.Tests/WorkerDataServiceSearchTests.cs (pola [Fact] + Assert.Equal + namespace HcPortal.Tests)
    - 351-PATTERNS.md "Recommended new helper (D-02 / RESEARCH Pattern 2)" (signature + LINQ persis)
    - Models/UnifiedTrainingRecord.cs (Kategori field)
    - Controllers/CMPController.cs sekitar :3933 (konvensi private static helper) — helper baru pakai `public static` (reachable dari test tanpa InternalsVisibleTo; project belum punya attribute itu)
  </read_first>
  <action>
1. Tambah helper di `CMPController.cs` (letakkan dekat helper privat lain ~:3933, sebelum penutup class). Gunakan `public static` (BUKAN private — test harus reach tanpa menambah InternalsVisibleTo):
   ```csharp
   public static List<string> BuildActualCategories(IEnumerable<HcPortal.Models.UnifiedTrainingRecord> records) =>
       records.Where(r => !string.IsNullOrEmpty(r.Kategori))
              .Select(r => r.Kategori!)
              .Distinct(StringComparer.OrdinalIgnoreCase)
              .OrderBy(n => n)
              .ToList();
   ```
   Pastikan `using System.Linq;` tersedia (CMPController sudah pakai LINQ — confirm tidak perlu tambah).
2. Buat `HcPortal.Tests/BuildActualCategoriesTests.cs`, namespace `HcPortal.Tests`, mirror gaya WorkerDataServiceSearchTests:
   ```csharp
   using System.Collections.Generic;
   using HcPortal.Controllers;
   using HcPortal.Models;
   using Xunit;

   namespace HcPortal.Tests;

   public class BuildActualCategoriesTests
   {
       [Fact]
       public void DistinctNonEmpty_CaseInsensitive_Ordered()
       {
           var records = new List<UnifiedTrainingRecord>
           {
               new() { Kategori = "OJT" },
               new() { Kategori = "ojt" },
               new() { Kategori = "Legacy Free Text" },
               new() { Kategori = null },
               new() { Kategori = "" },
           };
           var result = CMPController.BuildActualCategories(records);
           Assert.Equal(new[] { "Legacy Free Text", "OJT" }, result);
       }

       [Fact]
       public void EmptyInput_ReturnsEmptyList()
       {
           Assert.Empty(CMPController.BuildActualCategories(new List<UnifiedTrainingRecord>()));
       }

       [Fact]
       public void AllNullOrEmpty_ReturnsEmptyList()
       {
           var records = new List<UnifiedTrainingRecord> { new() { Kategori = null }, new() { Kategori = "" } };
           Assert.Empty(CMPController.BuildActualCategories(records));
       }
   }
   ```
   Sesuaikan `using HcPortal.Controllers;` dengan namespace CMPController aktual (verify lewat deklarasi class di file).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter BuildActualCategories</automated>
  </verify>
  <acceptance_criteria>
    - `CMPController.cs` memuat `public static List<string> BuildActualCategories(`.
    - `HcPortal.Tests/BuildActualCategoriesTests.cs` ada, memuat 3 [Fact].
    - `dotnet test --filter BuildActualCategories` → semua pass (≥3).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Helper public static ada, 3 [Fact] hijau (distinct/case-insensitive/ordered + 2 edge), build bersih.</done>
</task>

<task type="auto">
  <name>Task 2: ViewBag.ActualCategoriesJson di RecordsWorkerDetail (SF-04) + Records (SF-05) — authz preserve</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs :538-598 (RecordsWorkerDetail — authz :543-556 PRESERVE, ViewBag :576-579) dan :479-535 (Records — unified :484, vm :490-499, block roleLevel<=4 :502-532)
    - 351-PATTERNS.md "SF-04 — RecordsWorkerDetail action" + "SF-05 — Records action (My Records)" (placement: ActualCategoriesJson DI LUAR block roleLevel<=4)
    - 351-RESEARCH.md Pitfall 6 (authz verbatim) + Security Domain V4
  </read_first>
  <action>
**SF-04 — `RecordsWorkerDetail` action:**
Ganti baris `ViewBag.MasterCategoriesJson = ...` (`:577-579`, sumber master) menjadi `ViewBag.ActualCategoriesJson` dari `unifiedRecords`:
```csharp
ViewBag.ActualCategoriesJson = System.Text.Json.JsonSerializer.Serialize(
    BuildActualCategories(unifiedRecords));
```
- PERTAHANKAN `allCats` query (`:566-575`) dan `ViewBag.SubCategoryMapJson` (`:576`) APA ADANYA — SubKategori cascade tetap master-based.
- JANGAN sentuh authz block `:543-556` (byte-identical: `if (roleLevel >= 5) return Forbid();` + L4 section-lock). Ini hanya swap satu baris ViewBag di bawah guard.
- Boleh menghapus baris `ViewBag.MasterCategoriesJson` lama untuk Worker Detail KARENA view-nya (Plan 03) akan baca `ActualCategoriesJson`. Jika ragu kompatibilitas, boleh set KEDUANYA — tapi minimal `ActualCategoriesJson` WAJIB ada.

**SF-05 — `Records` action:**
Tambah `ViewBag.ActualCategoriesJson` dari `unified`, DITEMPATKAN DI LUAR block `if (roleLevel <= 4)` (My Records Kategori filter harus tersedia untuk SEMUA role). Letakkan tepat setelah konstruksi `vm` (~:499), sebelum `if (roleLevel <= 4)`:
```csharp
ViewBag.ActualCategoriesJson = System.Text.Json.JsonSerializer.Serialize(
    BuildActualCategories(unified));
```
- JANGAN ubah block `roleLevel <= 4` (Team View MasterCategoriesJson :527 tetap untuk Team View).

`dotnet build` harus 0 error. JANGAN ubah signature action manapun (D-04 fallback tidak dipicu).
  </action>
  <verify>
    <automated>dotnet build; powershell -Command "$wd = Select-String -Path 'Controllers/CMPController.cs' -Pattern 'BuildActualCategories\(unifiedRecords\)'; $rec = Select-String -Path 'Controllers/CMPController.cs' -Pattern 'BuildActualCategories\(unified\)'; $authz = Select-String -Path 'Controllers/CMPController.cs' -Pattern 'if \(roleLevel >= 5\) return Forbid\(\);'; if ($wd -and $rec -and $authz) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `CMPController.cs` memuat `ViewBag.ActualCategoriesJson = ...BuildActualCategories(unifiedRecords)` (SF-04, RecordsWorkerDetail).
    - `CMPController.cs` memuat `ViewBag.ActualCategoriesJson = ...BuildActualCategories(unified)` (SF-05, Records).
    - `CMPController.cs` MASIH memuat `if (roleLevel >= 5) return Forbid();` (authz preserve, byte-identical).
    - `CMPController.cs` MASIH memuat `ViewBag.SubCategoryMapJson` (SubKategori master tetap).
    - ActualCategoriesJson di `Records` berada DI LUAR block `if (roleLevel <= 4)` (grep konteks: muncul sebelum atau setelah block, bukan di dalamnya).
    - `dotnet build` 0 error; `dotnet test HcPortal.Tests` tetap hijau (no regression).
  </acceptance_criteria>
  <done>Kedua action set ViewBag.ActualCategoriesJson dari record aktual; authz + SubCategoryMap utuh; My Records ViewBag tersedia untuk semua role; build+test hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → CMPController.RecordsWorkerDetail | workerId dari query-string; authz guard menentukan boleh/tidak lihat record orang lain |
| DB AssessmentSession.Category/TrainingRecord.Kategori → ViewBag JSON → view | nilai kategori (sebagian free-text) di-serialize ke JSON untuk render option |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-AC | Elevation of Privilege | RecordsWorkerDetail authz :543-556 | mitigate | Swap ViewBag HANYA di bawah guard; authz block byte-identical (acceptance criteria grep `if (roleLevel >= 5) return Forbid();`). Opsi Kategori berasal dari `unifiedRecords` yang SUDAH terotorisasi untuk workerId itu → tidak melebarkan akses (no IDOR widening) |
| T-351-XSS | Tampering | ViewBag.ActualCategoriesJson serialize nilai kategori free-text | mitigate | Pakai `System.Text.Json.JsonSerializer` (server-side, meng-encode). View deserialize server-side lalu render via Razor `@cat` (auto HTML-encode). JANGAN inject raw string ke inline JS. Severity low |
| T-351-INFO | Information Disclosure | opsi Kategori membocorkan kategori record orang lain | accept | Opsi diturunkan dari record yang sama (`unifiedRecords`) yang sudah ditampilkan di tabel untuk workerId terotorisasi — tidak ada data baru terekspos |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` → semua hijau (109+ existing + 3 baru BuildActualCategories).
- Grep `CMPController.cs`: `BuildActualCategories(unifiedRecords)`, `BuildActualCategories(unified)`, `if (roleLevel >= 5) return Forbid();`, `ViewBag.SubCategoryMapJson` semua present.
</verification>

<success_criteria>
- `BuildActualCategories` helper public static + 3 [Fact] hijau.
- ViewBag.ActualCategoriesJson di RecordsWorkerDetail (SF-04, dari unifiedRecords) + Records (SF-05, dari unified, di luar block roleLevel<=4).
- Authz :543-556 dan SubCategoryMapJson byte-identical (tidak tersentuh).
- build + full unit test hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-02-SUMMARY.md`
</output>
