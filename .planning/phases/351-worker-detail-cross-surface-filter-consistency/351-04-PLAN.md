---
phase: 351-worker-detail-cross-surface-filter-consistency
plan: 04
type: execute
wave: 2
depends_on: [351-02]
files_modified:
  - Views/CMP/Records.cshtml
autonomous: true
requirements: [SF-05, SF-07]
must_haves:
  truths:
    - "My Records punya filter Kategori (opsi actual-distinct) + Tipe (Assessment/Training) sebanding Worker Detail"
    - "Baris My Records membawa data-category sehingga filter Kategori bisa membandingkan client-side"
    - "Opsi Tipe My Records bernilai 'assessment'/'training' (cocok data-type baris), BUKAN 'Assessment Online'"
    - "Kembali via 'Back to Team View' (#team) mengaktifkan tab Team View tanpa klik manual; sessionStorage memulihkan 9 filter"
  artifacts:
    - path: "Views/CMP/Records.cshtml"
      provides: "Kategori+Tipe filter parity + data-category + hash→tab activator"
      contains: "typeFilter"
  key_links:
    - from: "Records.cshtml filter bar"
      to: "ViewBag.ActualCategoriesJson + data-category rows"
      via: "categoryFilter/typeFilter select + filterTable compare"
      pattern: "categoryFilter"
    - from: "Records.cshtml DOMContentLoaded"
      to: "#pane-team tab"
      via: "bootstrap.Tab.getOrCreateInstance(tab-team).show() saat location.hash === '#team'"
      pattern: "getOrCreateInstance"
---

<objective>
SF-05 (parity) + SF-07 (back-nav tab activation) pada `Views/CMP/Records.cshtml` (My Records):
- SF-05: tambah filter **Kategori** (opsi `ViewBag.ActualCategoriesJson` dari Plan 02) + **Tipe** (Assessment/Training); tambah `data-category` ke baris; extend `filterTable()` dengan compare category+type; wire select baru ke filter + sessionStorage + clearFilters.
- SF-07: tambah hash→tab activator di `DOMContentLoaded` — saat URL `#team`, aktifkan tab Team View (Records.cshtml tidak punya handler ini → user mendarat di My Records). sessionStorage `cmp-records-team-filter` SUDAH memulihkan 9 filter (RecordsTeam, tidak disentuh).

Purpose: paritas alat filter self-records vs viewing-others; "Back to Team View" benar-benar membuka Team View dengan filter dipulihkan.
Output: `Records.cshtml` (2 select + data-category + filterTable extend + listeners/clearFilters wire + hash→tab activator).

CRITICAL (locked / 3 traps): (T2) Tipe `<option value>` WAJIB `assessment`/`training` (cocok `data-type` baris My Records :169), BUKAN "Assessment Online". (T3) WAJIB tambah `data-category="@(item.Kategori?.ToLower() ?? "")"` ke `<tr>` (tanpa itu filter Kategori sembunyikan semua). SubKategori TIDAK ditambah (deferred). Year quick-button group (`:62-77`) + myRecordsEmptyState block (`:366-379`) + counter logic (`:355-363`) TIDAK direstruktur — hanya ADD compare. RecordsTeam.cshtml TIDAK disentuh (D-04 fallback tidak dipicu). Tidak ada CSS baru. `<label for>` pada setiap select baru (a11y).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-UI-SPEC.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-PATTERNS.md
@.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- My Records filter bar SAAT INI (Records.cshtml:54-93): Search (col-md-6) + year quick-btn group (col-md-6) + Reset/Export row. NO Kategori, NO Tipe. -->
<!-- Worker Detail cell template untuk REUSE (RecordsWorkerDetail.cshtml:136-149 Kategori, :166-173 Tipe): -->
<div class="col-12 col-sm-6 col-md-2">
  <label for="categoryFilter" class="form-label small text-muted mb-1">Kategori</label>
  <select id="categoryFilter" class="form-select"> <option value="">Semua Kategori</option> ... </select>
</div>

<!-- My Records <tr> SAAT INI (Records.cshtml:167-171) — punya data-title/year/type, TIDAK punya data-category: -->
<tr class="training-row @(resultsUrl != null ? "table-row-clickable" : "")"
    data-title="@item.Title.ToLower()" data-year="@item.Date.Year"
    data-type="@(item.RecordType == "Assessment Online" ? "assessment" : "training")"  // T2: data-type SHORT
    ...>

<!-- My Records filterTable() SAAT INI (Records.cshtml:334-380): baca search+year; sudah baca data-type (:343) utk counter tapi TIDAK filter type. Punya counter (:355-363) + myRecordsEmptyState block (:366-379). -->

<!-- sessionStorage My Records (Records.cshtml:424-448): saveMyFilterState {search,year} + restore on DOMContentLoaded. -->
<!-- Tab: #tab-team ada hanya saat roleLevel <= 4 (Records.cshtml:42); #pane-team target. Bootstrap 5.3 Tab API ter-load. -->
<!-- Back-nav source: RecordsWorkerDetail.cshtml asp-fragment="team" → URL #team. Records.cshtml TIDAK punya hash→tab handler (gap SF-07). -->
<!-- ViewBag.ActualCategoriesJson disediakan oleh Plan 02 (Records action, di luar block roleLevel<=4 → tersedia semua role). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SF-05 — tambah Kategori+Tipe select, data-category, extend filterTable, wire listeners/clearFilters</name>
  <files>Views/CMP/Records.cshtml</files>
  <read_first>
    - Views/CMP/Records.cshtml :54-93 (filter bar) + :161-180 (rows) + :334-402 (filterTable+clearFilters) + :424-448 (saveMyFilterState+restore)
    - Views/CMP/RecordsWorkerDetail.cshtml :136-173 (cell template Kategori+Tipe), :216 (data-category pattern), :350-354 (compare pattern)
    - 351-PATTERNS.md "SF-05" 3 sub-bagian + T2/T3 flag
    - 351-RESEARCH.md Pitfall 2 (T2 value-map) + Pitfall 3 (T3 data-category) + Pitfall 5 (jangan restructure counter/empty-state)
    - 351-UI-SPEC.md C3 (layout contract + value-mapping trap + label for a11y)
  </read_first>
  <action>
**1. Filter bar — tambah 2 cell** (`Records.cshtml:54-93`). Tambahkan ke baris filter (re-flow grid agar muat; goal = paritas SET filter, bukan pixel-identik). Reuse cell template Worker Detail. Pertahankan year quick-button group (`:62-77`) APA ADANYA:
```html
<div class="col-12 col-sm-6 col-md-3">
    <label for="categoryFilter" class="form-label small text-muted mb-1">Kategori</label>
    <select id="categoryFilter" class="form-select">
        <option value="">Semua Kategori</option>
        @{
            var actualCategories = System.Text.Json.JsonSerializer.Deserialize<List<string>>(
                (string)(ViewBag.ActualCategoriesJson ?? "[]"));
        }
        @foreach (var cat in actualCategories) { <option value="@cat">@cat</option> }
    </select>
</div>
<div class="col-12 col-sm-6 col-md-3">
    <label for="typeFilter" class="form-label small text-muted mb-1">Tipe</label>
    <select id="typeFilter" class="form-select">
        <option value="">Semua Tipe</option>
        <option value="assessment">Assessment</option>   @* T2: SHORT, cocok data-type baris *@
        <option value="training">Training</option>        @* T2: SHORT *@
    </select>
</div>
```
- `<label for="...">` WAJIB (a11y, NON-NEGOTIABLE). T2: value `assessment`/`training` (BUKAN "Assessment Online").
- JANGAN tambah SubKategori (deferred). JANGAN buat card baru — semua di filter card existing.

**2. Baris `<tr>` — tambah data-category** (T3) (`Records.cshtml:167-171`):
Tambah atribut `data-category="@(item.Kategori?.ToLower() ?? "")"` ke `<tr class="training-row ...">` (mirror Worker Detail `:216`). JANGAN ubah data-type existing.

**3. Extend `filterTable()`** (`:334-380`) — ADD compare category+type (JANGAN restructure counter/empty-state existing):
- Tambah di awal: `const categoryFilter = document.getElementById('categoryFilter').value.toLowerCase();` dan `const typeFilter = document.getElementById('typeFilter').value.toLowerCase();`.
- Di dalam forEach: tambah `const category = row.getAttribute('data-category');`. `type` sudah dibaca (`:343`).
- Tambah `const matchCategory = !categoryFilter || category === categoryFilter;` dan `const matchType = !typeFilter || type === typeFilter;`.
- Ubah `const show = matchSearch && matchYear;` (`:346`) → `const show = matchSearch && matchYear && matchCategory && matchType;`.
- BIARKAN counter logic (`:355-363`) + myRecordsEmptyState block (`:366-379`) APA ADANYA.

**4. Wire select baru** ke filter + persistence + reset:
- Tambah listener: `document.getElementById('categoryFilter').addEventListener('change', filterTable);` dan `('typeFilter').addEventListener('change', filterTable);` (dekat `:381-382`).
- Extend `saveMyFilterState()` (`:424-430`) untuk simpan `category` + `type`; tambah listener change → saveMyFilterState (dekat `:431-432`).
- Extend restore on DOMContentLoaded (`:434-448`): set `categoryFilter`/`typeFilter` value dari state bila ada.
- Extend `clearFilters()` (`:394-402`): reset `categoryFilter.value=''` + `typeFilter.value=''` sebelum `filterTable()`.
  </action>
  <verify>
    <automated>dotnet build; powershell -Command "$cat = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern 'id=\"categoryFilter\"'; $type = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern '<option value=\"assessment\">'; $dc = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern 'data-category='; $mc = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern 'matchCategory'; $bad = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern '<option value=\"Assessment Online\">'; if ($cat -and $type -and $dc -and $mc -and -not $bad) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Records.cshtml` memuat `id="categoryFilter"` + `id="typeFilter"` (kedua select dengan `<label for=...>`).
    - `Records.cshtml` memuat `<option value="assessment">` dan `<option value="training">` (T2 SHORT).
    - `Records.cshtml` TIDAK memuat `<option value="Assessment Online">` di filter Tipe (T2 guard — anti-pattern).
    - `Records.cshtml` memuat `data-category=` pada baris training-row (T3).
    - `Records.cshtml` filterTable memuat `matchCategory` dan `matchType` di kondisi `show`.
    - `Records.cshtml` MASIH memuat `myRecordsEmptyState` + year quick-btn group (`year-quick-btn`) — tidak diregres.
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Kategori+Tipe filter ada (value SHORT), data-category di baris, filterTable filter category+type, listeners+persistence+reset ter-wire; year-btn + empty-state existing utuh; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: SF-07 — hash→tab activator (#team aktifkan Team View tab)</name>
  <files>Views/CMP/Records.cshtml</files>
  <read_first>
    - Views/CMP/Records.cshtml :30-48 (tab nav, #tab-team hanya roleLevel<=4) + :415-421 (tab aria handler existing) + :434 (DOMContentLoaded existing)
    - Views/CMP/RecordsWorkerDetail.cshtml :27, :42 (back-nav asp-fragment="team")
    - 351-PATTERNS.md "SF-07 — hash→tab activator" + Pitfall 4 (assert active tab, bukan klik manual)
    - 351-RESEARCH.md Pattern 4 (sessionStorage precedence — verified favorable, RecordsTeam tidak disentuh)
  </read_first>
  <action>
Tambah hash→tab activator di `Records.cshtml`. Letakkan di dalam blok `DOMContentLoaded` baru ATAU sisipkan ke logika DOMContentLoaded existing (`:434`). Gunakan Bootstrap Tab API (sudah ter-load):
```javascript
document.addEventListener('DOMContentLoaded', function () {
    if (window.location.hash === '#team' || window.location.hash === '#pane-team') {
        var teamTab = document.getElementById('tab-team');   // ada hanya saat roleLevel <= 4
        if (teamTab) bootstrap.Tab.getOrCreateInstance(teamTab).show();
    }
});
```
- JANGAN sentuh `Views/CMP/RecordsTeam.cshtml` — `restoreFilterState()` + `doFetch` di sana SUDAH memulihkan 9 filter (D-04 sessionStorage-primary). Aktivasi tab memicu render Team View → DOMContentLoaded-nya menjalankan restore.
- JANGAN ubah signature controller / tambah FilterState route (D-04 fallback tidak dipicu).
- Guard `if (teamTab)` WAJIB (untuk roleLevel 5/6 tab tidak ada — jangan error).
  </action>
  <verify>
    <automated>dotnet build; powershell -Command "$h = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern \"location.hash === '#team'\"; $tab = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern 'getOrCreateInstance'; $guard = Select-String -Path 'Views/CMP/Records.cshtml' -Pattern \"getElementById\('tab-team'\)\"; if ($h -and $tab -and $guard) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Records.cshtml` memuat `location.hash === '#team'`.
    - `Records.cshtml` memuat `bootstrap.Tab.getOrCreateInstance` dipanggil pada `getElementById('tab-team')`.
    - `Records.cshtml` memuat guard `if (teamTab)`.
    - `Views/CMP/RecordsTeam.cshtml` TIDAK dimodifikasi (file di luar files_modified plan ini).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>hash→tab activator ada dengan guard; RecordsTeam tidak disentuh; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewBag.ActualCategoriesJson → My Records option render | nilai kategori free-text di-render sebagai `<option>` |
| sessionStorage cmp-records-team-filter | state filter client-side; bukan security session |
| client JS filterTable + hash→tab | murni DOM client-side; tidak ada data baru ke server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-XSS | Tampering/XSS | render `<option value="@cat">@cat</option>` My Records | mitigate | Razor `@cat` auto-HTML-encode; sumber `JsonSerializer.Deserialize` server-side; JANGAN inject raw ke inline JS. Severity low |
| T-351-SS | Spoofing/Tampering | sessionStorage filter state dipulihkan | accept | sessionStorage adalah UX state client (per-origin), bukan session keamanan; tidak ada keputusan authz bergantung padanya; server tetap enforce scope (Plan 02 authz tidak disentuh) |
| T-351-TAB | — | hash→tab activator | accept | Hanya mengaktifkan tab UI; #tab-team hanya ada untuk roleLevel<=4 (server-rendered gate), guard `if (teamTab)` mencegah error utk role lain — tidak melebarkan akses |
</threat_model>

<verification>
- `dotnet build` 0 error.
- Grep Records.cshtml: `id="categoryFilter"`, `<option value="assessment">`, `data-category=`, `matchCategory`, `matchType`, `location.hash === '#team'`, `getOrCreateInstance`, `if (teamTab)`; preserve `year-quick-btn`, `myRecordsEmptyState`. NEGATIVE: tidak ada `<option value="Assessment Online">` di filter Tipe.
- `RecordsTeam.cshtml` unchanged (git status).
- Wave gate (setelah Plan 03+04 + run :5277): `npx playwright test e2e/cmp-records-351.spec.ts` full hijau (parity + back-nav) + regression `e2e/cmp-records-346.spec.ts e2e/cmp-records-350.spec.ts` hijau.
</verification>

<success_criteria>
- My Records punya Kategori (actual-distinct) + Tipe (value SHORT cocok data-type) + data-category; filterTable filter keduanya; counter/empty-state/year-btn existing utuh.
- Back-nav #team mengaktifkan Team View tab (Pitfall 4: tanpa klik manual); sessionStorage memulihkan 9 filter; RecordsTeam tidak disentuh.
- build hijau; SF-05 parity + SF-07 back-nav Playwright group hijau; Phase 346/350 regression hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/351-worker-detail-cross-surface-filter-consistency/351-04-SUMMARY.md`
</output>
