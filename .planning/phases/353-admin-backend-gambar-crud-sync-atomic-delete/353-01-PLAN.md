---
phase: 353-admin-backend-gambar-crud-sync-atomic-delete
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/PackageImageSyncTests.cs
  - HcPortal.Tests/PackageImageDeleteTests.cs
autonomous: true
requirements: [SYN-01]
must_haves:
  truths:
    - "SyncPackagesToPost menyalin ImagePath+ImageAlt soal & opsi Pre→Post (shared-file string copy, tanpa file op)"
    - "xUnit test sync (SYN-01) hijau"
    - "Test scaffold ref-count (D-10) + DeletePackage (D-11) ada dan kompilasi (RED diizinkan sampai Plan 02/03 implement)"
  artifacts:
    - path: "HcPortal.Tests/PackageImageSyncTests.cs"
      provides: "Integration test SYN-01 sync copy ImagePath/ImageAlt"
      contains: "SyncPackagesToPost"
    - path: "HcPortal.Tests/PackageImageDeleteTests.cs"
      provides: "Test ref-count D-10 + DeletePackage D-11 (scaffold + sync-able helper test)"
      contains: "RefCount"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (SyncPackagesToPost)"
      to: "PackageQuestion.ImagePath / PackageOption.ImagePath di paket Post"
      via: "string copy di deep-clone"
      pattern: "ImagePath = q\\.ImagePath"
---

<objective>
Wave 0: bangun test scaffold Nyquist untuk SYN-01 (sync copy) + D-10/D-11 (ref-count/DeletePackage), DAN selesaikan satu-satunya perubahan controller yang aman & terisolasi: 2-baris copy ImagePath+ImageAlt di `SyncPackagesToPost` (SYN-01). Ini membuat test sync langsung GREEN dan menyediakan harness DbContext InMemory + temp-dir yang dipakai test ref-count Plan 02/03.

Purpose: Menyediakan feedback loop otomatis SEBELUM logika berisiko (OQ1 option-preserve, D-10 ref-count) dieksekusi. SyncPackagesToPost adalah edit paling kecil & paling aman (2 baris × 2 lokasi, pure-DB) sehingga cocok dikerjakan bersama scaffold tanpa menyentuh CRUD/delete yang jadi milik plan berikutnya.

Output: 2 file test baru (sync GREEN, ref-count/delpkg scaffold) + edit SyncPackagesToPost.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-CONTEXT.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md
@.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-VALIDATION.md

<interfaces>
<!-- Entity props (Models/AssessmentPackage.cs) — executor pakai langsung, jangan eksplorasi -->
PackageQuestion: int Id, string QuestionText, int Order, int ScoreValue, string? QuestionType,
                 string? ElemenTeknis, string? Rubrik, int MaxCharacters,
                 string? ImagePath (nvarchar max null), string? ImageAlt ([MaxLength(255)] null),
                 ICollection<PackageOption> Options
PackageOption:   int Id, string OptionText, bool IsCorrect,
                 string? ImagePath (nvarchar max null), string? ImageAlt ([MaxLength(255)] null)

<!-- SyncPackagesToPost SEKARANG (AssessmentAdminController.cs L5337, deep-clone L5348+) -->
private async Task SyncPackagesToPost(int preSessionId, int postSessionId)
// Untuk tiap paket Pre → RemoveRange paket Post lama (L5348 RemoveRange(q.Options)) →
// re-clone newQ = new PackageQuestion { QuestionText, Order, ScoreValue, QuestionType,
//   ElemenTeknis, Rubrik, MaxCharacters, Options = q.Options.Select(o => new PackageOption {
//   OptionText, IsCorrect }).ToList() }

<!-- Test helper existing (HcPortal.Tests/FileUploadHelperTests.cs, PRIVATE — duplikasi ke file baru) -->
private static IFormFile MakeFile(string fileName, byte[] content)   // L19
private static string MakeTempDir()                                  // L41
private sealed class TestLogger : ILogger                            // L30
// Baseline suite saat ini 120 test hijau.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SyncPackagesToPost copy ImagePath+ImageAlt (SYN-01)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (baca method SyncPackagesToPost L5337-5391 LENGKAP — pahami struktur deep-clone newQ + Options.Select)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md (bagian "SyncPackagesToPost (service, transform DB-only)")
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md (Pattern 3)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Di method `SyncPackagesToPost` (cari simbol `private async Task SyncPackagesToPost`, ~L5337), pada blok deep-clone `var newQ = new PackageQuestion { ... }`, tambahkan 2 properti di objek soal dan 2 properti di objek opsi. STRING COPY SAJA — TIDAK ADA file op (SYN-01 / C-03 shared-file). Hasil akhir:

```csharp
var newQ = new PackageQuestion
{
    QuestionText = q.QuestionText,
    Order = q.Order,
    ScoreValue = q.ScoreValue,
    QuestionType = q.QuestionType,
    ElemenTeknis = q.ElemenTeknis,
    Rubrik = q.Rubrik,
    MaxCharacters = q.MaxCharacters,
    ImagePath = q.ImagePath,   // ← TAMBAH (SYN-01, shared-file)
    ImageAlt  = q.ImageAlt,    // ← TAMBAH
    Options = q.Options.Select(o => new PackageOption
    {
        OptionText = o.OptionText,
        IsCorrect = o.IsCorrect,
        ImagePath = o.ImagePath, // ← TAMBAH
        ImageAlt  = o.ImageAlt    // ← TAMBAH
    }).ToList()
};
```

PENTING: pertahankan SEMUA properti existing di blok ini verbatim (jangan hapus QuestionText/Order/dst). Hanya SISIPKAN 4 baris. `RemoveRange(q.Options)` / `RemoveRange` paket Post lama di method ini (~L5348) TIDAK boleh diubah dan TIDAK boleh ditambah File.Delete — Post berbagi path dengan Pre; ref-count di plan lain melindunginya. Method ini tetap murni-DB.
  </action>
  <verify>
    <automated>dotnet build HcPortal.Tests/HcPortal.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentAdminController.cs method SyncPackagesToPost memuat `ImagePath = q.ImagePath` DAN `ImageAlt = q.ImageAlt` (soal) DAN `ImagePath = o.ImagePath` DAN `ImageAlt = o.ImageAlt` (opsi).
    - Tidak ada `File.Delete` baru ditambahkan di SyncPackagesToPost (grep `File.Delete` di method ini = 0 match).
    - `dotnet build` exit 0.
  </acceptance_criteria>
  <done>SyncPackagesToPost menyalin ImagePath+ImageAlt untuk soal & opsi; build hijau; tidak ada file op baru.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test SYN-01 — PackageImageSyncTests.cs (GREEN)</name>
  <read_first>
    - HcPortal.Tests/FileUploadHelperTests.cs (baca L1-50 untuk pola MakeFile/MakeTempDir/TestLogger + using statements + namespace)
    - HcPortal.Tests/HcPortal.Tests.csproj (konfirmasi paket EF InMemory tersedia; bila TIDAK ada `Microsoft.EntityFrameworkCore.InMemory`, gunakan strategi unit murni di action)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
  </read_first>
  <behavior>
    - Test 1 (SYN-01 soal): diberikan PackageQuestion Pre dengan ImagePath="/uploads/questions/5/a.jpg" + ImageAlt="diagram pompa", setelah operasi sync/clone, soal Post hasil clone punya ImagePath & ImageAlt yang SAMA PERSIS (string equal).
    - Test 2 (SYN-01 opsi): opsi Pre dengan ImagePath/ImageAlt → opsi Post clone punya nilai sama persis.
    - Test 3 (shared-file invariant): path Post == path Pre (string identity), MEMASTIKAN tidak ada penggandaan/penamaan-ulang file.
    - Test 4 (null aman): soal Pre dengan ImagePath=null → Post ImagePath=null (tidak crash).
  </behavior>
  <action>
Buat file BARU `HcPortal.Tests/PackageImageSyncTests.cs`.

Strategi isolasi (PILIH satu, dokumentasikan di komentar header file):
- **Preferred jika EF InMemory tersedia:** seed AppDbContext InMemory dengan AssessmentSession Pre+Post linked + PackagePair, panggil logika sync via controller (bila bisa di-instantiate dengan dependensi minimal) ATAU replikasi blok deep-clone sebagai assertion langsung terhadap entity yang sudah disalin. Karena `SyncPackagesToPost` private, opsi paling robust: uji INVARIAN clone — bangun PackageQuestion Pre, jalankan ekspresi clone yang IDENTIK dengan controller (`new PackageQuestion { ..., ImagePath = q.ImagePath, ImageAlt = q.ImageAlt, Options = ... }`), assert hasil. Beri komentar: "mirror of SyncPackagesToPost deep-clone — keep in sync".
- **Fallback jika tidak ada EF InMemory:** uji murni in-memory object (tanpa DbContext) — bangun PackageQuestion + Options, terapkan ekspresi clone, assert. Ini cukup untuk SYN-01 (logikanya pure string copy).

Reuse pola test project existing (xUnit `[Fact]`, namespace & using sama dengan FileUploadHelperTests). Duplikasi MakeFile/MakeTempDir HANYA bila dibutuhkan (sync test TIDAK butuh filesystem). Beri nama method jelas mengandung "Sync" agar `--filter ~SyncImage` atau `~Sync` cocok: mis. `SyncCopiesQuestionImagePath`, `SyncCopiesOptionImageAlt`, `SyncSharesSamePath_NoFileDuplication`, `SyncHandlesNullImagePath`.

Tambahkan komentar header: tanggal, SYN-01, "mirror of AssessmentAdminController.SyncPackagesToPost deep-clone".
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~Sync"</automated>
  </verify>
  <acceptance_criteria>
    - File HcPortal.Tests/PackageImageSyncTests.cs ada, ≥4 `[Fact]`.
    - `dotnet test --filter "FullyQualifiedName~Sync"` exit 0 (semua hijau).
    - File memuat assertion `ImagePath` equality + `ImageAlt` equality.
  </acceptance_criteria>
  <done>4 test SYN-01 hijau; membuktikan copy ImagePath+ImageAlt shared-file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold test ref-count D-10 + DeletePackage D-11 — PackageImageDeleteTests.cs</name>
  <read_first>
    - HcPortal.Tests/FileUploadHelperTests.cs (L19-46 MakeFile/MakeTempDir, L180-250 pola temp-dir try/finally cleanup + File.Exists assertion)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-RESEARCH.md (Pattern 2 ref-count + §Validation Architecture Wave 0)
    - .planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-PATTERNS.md (SHARED-2 + test files section — catatan COMPOSE EF InMemory)
  </read_first>
  <behavior>
    - Test ref-count POSITIF (D-10): bila satu PackageQuestion DAN/ATAU PackageOption lain masih memuat ImagePath == path → predikat "shared" = true → File.Delete HARUS di-SKIP (file tetap ada di temp dir).
    - Test ref-count NOL (D-10): bila TIDAK ada baris lain memuat path → predikat = false → File.Delete dijalankan (file hilang dari temp dir).
    - Test DeletePackage path-collect (D-11): diberikan paket dengan soal+opsi bergambar, daftar path yang dikumpulkan = union semua ImagePath non-null soal+opsi.
  </behavior>
  <action>
Buat file BARU `HcPortal.Tests/PackageImageDeleteTests.cs` sebagai SCAFFOLD. Plan 02/03 akan mengimplementasi logika controller; scaffold ini menetapkan kontrak test SEKARANG.

Karena ref-count + File.Delete hidup di dalam method controller (butuh _context/_env), strategi (PILIH, dokumentasikan):
- **Preferred:** uji LOGIKA ref-count secara terisolasi. Jika EF InMemory tersedia: seed DbContext dengan 2 PackageQuestion berbagi path → assert `AnyAsync(q=>q.ImagePath==path) || AnyAsync(o=>o.ImagePath==path)` == true; hapus salah satu → assert == false. Untuk File.Delete: gunakan MakeTempDir + tulis file dummy + jalankan loop delete bersyarat (replika SHARED-1+SHARED-2) + assert `File.Exists`.
- **Jika kontrak controller belum bisa dipanggil:** tulis test sebagai unit terhadap helper kecil yang AKAN dibuat Plan 03 (mis. signature yang disepakati). Bila helper belum ada → tandai test `[Fact(Skip="impl Plan 03 — ref-count helper")]` SEMENTARA agar build hijau, ATAU tulis test inline-logic yang mereplikasi predikat (lebih disukai: inline-logic agar langsung GREEN tanpa skip).

PENTING (Nyquist): JANGAN biarkan banyak test Skip permanen. Minimal Test ref-count POSITIF & NOL harus GREEN sekarang via inline-logic (predikat + temp-dir delete loop direplika persis dari RESEARCH Pattern 2 + SHARED-1). Test DeletePackage path-collect boleh inline-logic union juga.

Nama method mengandung "RefCount" dan "DeletePackageImage": `RefCount_SkipsDelete_WhenPathSharedByOtherRow`, `RefCount_Deletes_WhenNoOtherRowSharesPath`, `DeletePackageImage_CollectsAllNonNullPaths`. Reuse MakeTempDir/MakeFile (duplikasi private ke file ini ATAU extract ke shared helper — planner discretion: duplikasi lebih simpel, no cross-file coupling).

Header komentar: tanggal, D-10/D-11, "ref-count predikat & atomic delete loop = mirror of SHARED-1/SHARED-2 (RESEARCH Pattern 2)".
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~RefCount|FullyQualifiedName~DeletePackageImage"</automated>
  </verify>
  <acceptance_criteria>
    - File HcPortal.Tests/PackageImageDeleteTests.cs ada.
    - Test `RefCount_SkipsDelete_WhenPathSharedByOtherRow` dan `RefCount_Deletes_WhenNoOtherRowSharesPath` GREEN (tidak Skip).
    - `dotnet test --filter "FullyQualifiedName~RefCount"` exit 0.
    - Temp-dir dibersihkan via try/finally (tidak ada folder temp menumpuk).
  </acceptance_criteria>
  <done>Scaffold delete-test ada; ref-count predikat (skip vs delete) GREEN; path-collect test ada; temp-dir bersih.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Plan 01 = pure-DB sync edit + test files; tidak menerima input user/HTTP, tidak menulis file produksi. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-353-01-01 | Tampering | SyncPackagesToPost string copy | accept | Hanya menyalin ImagePath string antar baris DB dalam tx existing; tidak ada file op, tidak ada input eksternal. Tidak menambah surface. |
| T-353-01-02 | Information Disclosure | Test temp dir | mitigate | Test pakai MakeTempDir (path acak) + try/finally cleanup; tidak menyentuh wwwroot produksi. |
</threat_model>

<verification>
- `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` exit 0.
- `dotnet test --filter "FullyQualifiedName~Sync"` hijau (SYN-01).
- `dotnet test --filter "FullyQualifiedName~RefCount"` hijau (D-10 predikat).
- Full suite tidak regresi: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` ≥124 hijau (120 baseline + ≥4 sync; delete-test menambah lagi).
</verification>

<success_criteria>
- SyncPackagesToPost menyalin ImagePath+ImageAlt (soal+opsi) tanpa file op (SC #4 sebagian — sisi DB).
- 2 file test baru ada; sync GREEN; ref-count predikat GREEN.
- Tidak ada Skip permanen pada test ref-count POSITIF/NOL.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/353-admin-backend-gambar-crud-sync-atomic-delete/353-01-SUMMARY.md`.
</output>
