---
phase: 354-render-gambar-di-6-layar
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - Controllers/CMPController.cs
autonomous: true
requirements: [RND-01, RND-02, RND-03]
must_haves:
  truths:
    - "StartExam populate mengisi ImagePath/ImageAlt soal (ExamQuestionItem) + opsi (ExamOptionItem) dari entity q/o"
    - "Results populate mengisi ImagePath/ImageAlt soal (QuestionReviewItem) + opsi (OptionReviewItem)"
    - "ExamSummary populate (3 cabang) mengisi ImagePath/ImageAlt soal + OptionImages dari q.Options"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Populate gambar di 3 loop (StartExam, Results, ExamSummary)"
      contains: "ImagePath = q.ImagePath"
  key_links:
    - from: "CMPController populate loop"
      to: "ExamQuestionItem/ExamOptionItem/QuestionReviewItem/OptionReviewItem/ExamSummaryItem"
      via: "assignment ImagePath = q.ImagePath / o.ImagePath"
      pattern: "ImagePath = (q|o|question|option)\\.ImagePath"
---

<objective>
Isi data gambar (RND-01/02/03) di 3 populate loop `CMPController.cs`: StartExam (~L1048-1070), Results (~L2293-2310), ExamSummary (~L1512-1571). Map `entity.ImagePath/ImageAlt` → field VM yang dibuat Plan 02. Setelah ini, view peserta (Plan 05) punya data untuk di-render.

Purpose: ViewModel field (Plan 02) kosong tanpa populate. Ini wiring DB→VM untuk 3 layar peserta.
Output: CMPController.cs (1 file) — 3 loop ditambahi assignment gambar. Tidak menyentuh AssessmentAdminController (Plan 04) → parallel-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/354-render-gambar-di-6-layar/354-PATTERNS.md

<interfaces>
<!-- StartExam L1048-1070 (PATTERNS.md): scope punya entity q (PackageQuestion) + o (PackageOption).
     new ExamOptionItem { OptionId = o.Id, OptionText = o.OptionText }
     new ExamQuestionItem { QuestionId = q.Id, QuestionText = q.QuestionText, ... } -->

<!-- Results L2293-2310: scope punya question (entity) + o (PackageOption).
     new QuestionReviewItem { ..., Options = question.Options.Select(o => new OptionReviewItem {...}) } -->

<!-- ExamSummary L1512-1571: 3 cabang (Essay/MA/MC), q (entity) di scope tiap cabang, q.Options tersedia.
     .Include(q => q.Options) sudah ada (~L1505). new ExamSummaryItem {...} per cabang. -->

<!-- Field VM baru (Plan 02): ImagePath, ImageAlt di ExamQuestionItem/ExamOptionItem/QuestionReviewItem/
     OptionReviewItem/ExamSummaryItem; ExamSummaryItem.OptionImages = List<ExamSummaryOptionItem>{OptionId,OptionText,ImagePath,ImageAlt}. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Populate gambar di StartExam (~L1048-1070)</name>
  <read_first>
    - Controllers/CMPController.cs L1040-1075 (StartExam populate loop — konfirmasi nama variabel q/o aktual + apakah Include(Options) ada)
    - Models/PackageExamViewModel.cs (field ImagePath/ImageAlt baru Plan 02)
  </read_first>
  <action>
Di `CMPController.cs` StartExam populate (~L1048-1070), tambahkan assignment gambar di kedua tempat:

1. Pada `new ExamOptionItem { OptionId = o.Id, OptionText = o.OptionText }` → tambah:
```csharp
ImagePath = o.ImagePath,
ImageAlt = o.ImageAlt,
```

2. Pada `new ExamQuestionItem { QuestionId = q.Id, QuestionText = q.QuestionText, ... }` → tambah:
```csharp
ImagePath = q.ImagePath,
ImageAlt = q.ImageAlt,
```

Verifikasi `q.Options` di-load (Include) — kolom ImagePath/ImageAlt adalah scalar non-projected pada entity yang sudah di-query, otomatis ter-load tanpa ubah query (PATTERNS.md "kolom non-projected auto-load"). Bila StartExam memakai projection `.Select` eksplisit yang membatasi kolom, tambahkan ImagePath/ImageAlt ke projection. Periksa saat read_first.
  </action>
  <verify>
    <automated>Select-String -Path "Controllers\CMPController.cs" -Pattern "ImagePath = o\.ImagePath","ImagePath = q\.ImagePath" -Context 0,0</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/CMPController.cs` contains `ImagePath = o.ImagePath` dan `ImageAlt = o.ImageAlt` (ExamOptionItem)
    - file contains `ImagePath = q.ImagePath` dan `ImageAlt = q.ImageAlt` (ExamQuestionItem)
  </acceptance_criteria>
  <done>StartExam ExamQuestionItem + ExamOptionItem terisi ImagePath/ImageAlt dari entity.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate gambar di Results (~L2293-2310)</name>
  <read_first>
    - Controllers/CMPController.cs L2285-2315 (Results populate — konfirmasi nama variabel question/o)
    - Models/AssessmentResultsViewModel.cs (field baru Plan 02)
  </read_first>
  <action>
Di `CMPController.cs` Results populate (~L2293-2310):

1. Pada `new QuestionReviewItem { QuestionNumber = ..., QuestionText = question.QuestionText, ... }` → tambah:
```csharp
ImagePath = question.ImagePath,
ImageAlt = question.ImageAlt,
```

2. Pada `new OptionReviewItem { OptionText = o.OptionText, IsCorrect = o.IsCorrect, IsSelected = ... }` → tambah:
```csharp
ImagePath = o.ImagePath,
ImageAlt = o.ImageAlt,
```

Verifikasi `question.Options` di-load (PATTERNS.md: scalar auto-load; bila projection eksplisit batasi kolom, tambahkan ke projection).
  </action>
  <verify>
    <automated>Select-String -Path "Controllers\CMPController.cs" -Pattern "ImagePath = question\.ImagePath","ImageAlt = question\.ImageAlt"</automated>
  </verify>
  <acceptance_criteria>
    - file contains `ImagePath = question.ImagePath` dan `ImageAlt = question.ImageAlt` (QuestionReviewItem)
    - file contains `ImagePath = o.ImagePath` dan `ImageAlt = o.ImageAlt` (OptionReviewItem — minimal total 2x `ImagePath = o.ImagePath` di file dengan Task 1)
  </acceptance_criteria>
  <done>Results QuestionReviewItem + OptionReviewItem terisi gambar dari entity.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Populate gambar di ExamSummary (3 cabang, soal + OptionImages) + build</name>
  <read_first>
    - Controllers/CMPController.cs L1500-1575 (ExamSummary populate — 3 cabang Essay/MA/MC, konfirmasi Include(Options) ~L1505 + nama variabel q)
    - Models/PackageExamViewModel.cs (ExamSummaryItem.ImagePath/ImageAlt + OptionImages List<ExamSummaryOptionItem>)
    - CLAUDE.md (dotnet build wajib)
  </read_first>
  <action>
Di `CMPController.cs` ExamSummary populate (~L1512-1571). Ada 3 cabang membuat `new ExamSummaryItem` (Essay / MultipleAnswer / MultipleChoice). `q` (entity PackageQuestion) di scope tiap cabang; `q.Options` tersedia (Include ~L1505 — verifikasi).

1. Di KETIGA cabang `new ExamSummaryItem { ... }`, tambah gambar soal:
```csharp
ImagePath = q.ImagePath,
ImageAlt = q.ImageAlt,
```

2. Untuk gambar OPSI (RND-02 block-bawah) — isi `OptionImages` dari `q.Options` di cabang MA + MC (cabang Essay tidak punya opsi, SKIP OptionImages-nya). Tambahkan di cabang MA dan MC:
```csharp
OptionImages = q.Options.OrderBy(o => o.Id).Select(o => new ExamSummaryOptionItem
{
    OptionId = o.Id,
    OptionText = o.OptionText,
    ImagePath = o.ImagePath,
    ImageAlt = o.ImageAlt
}).ToList(),
```
(Urutan OrderBy(o.Id) konsisten dengan pola loop existing; shuffle aman object-level L-03 — gambar nempel objek.)

3. Jalankan `dotnet build` — 0 error. Bila Include(q.Options) belum ada di query ExamSummary, tambahkan `.Include(q => q.Options)` agar q.Options ter-load (verifikasi saat read_first; PATTERNS.md catat L1505 confirm Include).
  </action>
  <verify>
    <automated>Select-String -Path "Controllers\CMPController.cs" -Pattern "OptionImages = q\.Options","new ExamSummaryOptionItem"; dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - file contains `ImagePath = q.ImagePath` muncul di konteks ExamSummary cabang (total `ImagePath = q.ImagePath` di file >= 2 termasuk StartExam Task 1)
    - file contains `OptionImages = q.Options` (MA + MC cabang)
    - file contains `new ExamSummaryOptionItem`
    - `dotnet build` output contains "Build succeeded", 0 baris ": error"
  </acceptance_criteria>
  <done>ExamSummary 3 cabang terisi gambar soal; MA+MC cabang isi OptionImages; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB entity → ViewModel | Path gambar admin-input dari DB dipetakan ke VM (akan di-render Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-04 | XSS (deferred to render) | populate ImagePath ke VM | mitigate | Populate hanya copy string DB→VM. Encoding terjadi di render (Plan 01 partial `src="@..."` Razor-encoded, no Html.Raw). Tidak ada surface XSS baru di populate. |
| T-354-05 | Info disclosure | ExamSummary OptionImages | accept | OptionImages hanya path yang sama dengan StartExam (soal yang sama peserta lihat). Tidak ekspos data tambahan. |
</threat_model>

<verification>
- `dotnet build` succeeded 0 error.
- 3 loop CMPController terisi gambar (StartExam soal+opsi, Results soal+opsi, ExamSummary soal + OptionImages MA/MC).
- Render verifikasi visual di Plan 05.
</verification>

<success_criteria>
- RND-01/02/03 data-side: peserta StartExam/ExamSummary/Results menerima ImagePath/ImageAlt soal + opsi via VM.
- Build lokal hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/354-render-gambar-di-6-layar/354-03-SUMMARY.md`
</output>
