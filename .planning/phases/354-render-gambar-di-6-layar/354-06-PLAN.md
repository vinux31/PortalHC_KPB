---
phase: 354-render-gambar-di-6-layar
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 04]
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - Views/Admin/EditPesertaAnswers.cshtml
  - Views/Admin/_PreviewQuestion.cshtml
  - Views/Admin/ManagePackageQuestions.cshtml
autonomous: false
requirements: [RND-05, RND-06, RND-07]
must_haves:
  truths:
    - "Admin melihat gambar soal di halaman nilai essay (AssessmentMonitoringDetail) — soal saja"
    - "Admin melihat gambar soal + opsi block-bawah saat edit jawaban peserta (EditPesertaAnswers)"
    - "_PreviewQuestion di-retrofit ke partial bersama (konsistensi D-04); lightbox berfungsi di host ManagePackageQuestions"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Render gambar soal essay + lightbox (RND-05)"
      contains: "_QuestionImage"
    - path: "Views/Admin/EditPesertaAnswers.cshtml"
      provides: "Render gambar soal+opsi block-bawah + lightbox (RND-06)"
      contains: "_QuestionImage"
    - path: "Views/Admin/_PreviewQuestion.cshtml"
      provides: "Retrofit ke partial _QuestionImage (konsistensi D-04, RND-04)"
      contains: "_QuestionImage"
    - path: "Views/Admin/ManagePackageQuestions.cshtml"
      provides: "Host lightbox modal untuk preview AJAX-injected (_PreviewQuestion)"
      contains: "_ImageLightboxModal"
  key_links:
    - from: "view admin"
      to: "_QuestionImage.cshtml + _ImageLightboxModal.cshtml"
      via: "@await Html.PartialAsync"
      pattern: "PartialAsync\\(\"_QuestionImage\""
---

<objective>
Render gambar (RND-05/06/07) di 3 view admin: AssessmentMonitoringDetail (essay nilai — gambar soal saja), EditPesertaAnswers (soal + opsi block-bawah), dan retrofit _PreviewQuestion ke partial bersama (konsistensi D-04, RND-04 sudah ship fungsional). Karena _PreviewQuestion di-inject via AJAX ke `#previewModalBody` di ManagePackageQuestions, lightbox modal + JS harus hadir di HOST page ManagePackageQuestions (PATTERNS.md catatan host RND-04).

Purpose: Menutup 3 layar admin + harmonisasi visual via partial tunggal. RND-05 soal saja (essay tak punya opsi); RND-06 soal+opsi.
Output: 4 view admin. File berbeda dari Plan 05 → parallel-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/354-render-gambar-di-6-layar/354-UI-SPEC.md
@.planning/phases/354-render-gambar-di-6-layar/354-PATTERNS.md
@Views/Shared/_QuestionImage.cshtml
@Views/Shared/_ImageLightboxModal.cshtml
@Views/Admin/_PreviewQuestion.cshtml

<interfaces>
<!-- AssessmentMonitoringDetail essay loop L405-419: @foreach (var essayItem in essayItems), soal di L410
     (<h6 class="fw-semibold">Soal @essayItem.DisplayNumber: @essayItem.QuestionText</h6>). EssayGradingItemViewModel.ImagePath/ImageAlt (Plan 02/04). Gambar SOAL SAJA. -->
<!-- EditPesertaAnswers loop: soal L53 (<div class="mb-2">@q.QuestionText</div>); opsi MC L64-77 + MA L83-97
     (<label class="form-check-label" for=...>@opt.OptionText</label> dalam <div class="form-check">). EditQuestionRow/EditOptionRow.ImagePath/ImageAlt (Plan 02/04). -->
<!-- _PreviewQuestion.cshtml: @model PackageQuestion. Soal img L20-24 (cap240), opsi img L69-73 (cap120 inline-flex saat ini).
     Retrofit: ganti kedua blok <img> jadi @await Html.PartialAsync("_QuestionImage", ...). Opsi jadi block-bawah (§3). -->
<!-- ManagePackageQuestions.cshtml: #previewModal L263-283 (host AJAX inject _PreviewQuestion ke #previewModalBody L272).
     @section Scripts L326. Lightbox modal harus hadir DI SINI (host), bukan di _PreviewQuestion (yang di-inject). -->

<!-- Pemanggilan partial: soal Cap=240, opsi Cap=120 (block-bawah otomatis). Lightbox sekali per halaman. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Render gambar soal essay di AssessmentMonitoringDetail.cshtml (RND-05 soal saja) + lightbox</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml L400-430 (essay loop L405-419) + cari @section Scripts / akhir body
    - Views/Shared/_QuestionImage.cshtml + _ImageLightboxModal.cshtml
    - .planning/phases/354-render-gambar-di-6-layar/354-CONTEXT.md L-01 (essay = SOAL SAJA)
  </read_first>
  <action>
Di `Views/Admin/AssessmentMonitoringDetail.cshtml`:

1. Gambar SOAL — setelah `<h6 class="fw-semibold">Soal @essayItem.DisplayNumber: @essayItem.QuestionText</h6>` (L410) atau setelah header div (sebelum blok jawaban pekerja L416), sisipkan:
```cshtml
@await Html.PartialAsync("_QuestionImage", new { ImagePath = essayItem.ImagePath, ImageAlt = essayItem.ImageAlt, Cap = 240 })
```
JANGAN render gambar opsi (essay tak punya opsi, RND-05).

2. Lightbox — sisipkan sekali di akhir konten view (sebelum `@section Scripts` bila ada, atau akhir body):
```cshtml
@await Html.PartialAsync("_ImageLightboxModal")
```
Pertahankan struktur card essay-grading existing (badge status, jawaban pekerja, rubrik collapse).
  </action>
  <verify>
    <automated>Select-String -Path "Views\Admin\AssessmentMonitoringDetail.cshtml" -Pattern "_QuestionImage","_ImageLightboxModal","Cap = 240"</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentMonitoringDetail.cshtml contains `_QuestionImage` dengan `Cap = 240` (soal)
    - contains `_ImageLightboxModal` (1x)
    - TIDAK ada `Cap = 120` (essay tanpa opsi, soal saja)
  </acceptance_criteria>
  <done>AssessmentMonitoringDetail render gambar soal essay 240 + lightbox; tidak ada gambar opsi.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render gambar soal + opsi block-bawah di EditPesertaAnswers.cshtml (RND-06) + lightbox</name>
  <read_first>
    - Views/Admin/EditPesertaAnswers.cshtml L45-110 (soal L53 + opsi MC L64-77 + MA L83-97) + cari @section Scripts / akhir body
    - Views/Shared/_QuestionImage.cshtml + _ImageLightboxModal.cshtml
    - .planning/phases/354-render-gambar-di-6-layar/354-UI-SPEC.md §3 (opsi block-bawah)
  </read_first>
  <action>
Di `Views/Admin/EditPesertaAnswers.cshtml`:

1. Gambar SOAL — setelah `<div class="mb-2">@q.QuestionText</div>` (L53), sisipkan:
```cshtml
@await Html.PartialAsync("_QuestionImage", new { ImagePath = q.ImagePath, ImageAlt = q.ImageAlt, Cap = 240 })
```

2. Gambar OPSI (MC L64-77 + MA L83-97) — opsi pakai `<div class="form-check">` dengan `<label class="form-check-label">`. Sisipkan partial opsi block-bawah SETELAH `</label>` di dalam `<div class="form-check">` (block-flow normal → gambar otomatis turun ke baris bawah; partial cap<=120 sudah `d-block w-100 mt-2`):
```cshtml
<div class="form-check">
  <input class="form-check-input answer-input" type="radio" name="Answers[@q.PackageQuestionId]" value="@opt.Id" id="opt-@q.PackageQuestionId-@opt.Id" @(q.SelectedOptionIds.Contains(opt.Id) ? "checked" : "") />
  <label class="form-check-label" for="opt-@q.PackageQuestionId-@opt.Id">
    @opt.OptionText
    @if (opt.IsCorrect) { <span class="badge bg-success ms-2">Kunci</span> }
  </label>
  @await Html.PartialAsync("_QuestionImage", new { ImagePath = opt.ImagePath, ImageAlt = opt.ImageAlt, Cap = 120, AriaContext = "opsi" })
</div>
```
Terapkan di KEDUA cabang (MC radio L64-77 + MA checkbox L83-97). WAJIB pertahankan input `class="answer-input"` + name `Answers[...]` + id `opt-...` (JS edit-jawaban bergantung selectors ini).

3. Lightbox — sisipkan sekali di akhir konten (sebelum `@section Scripts` bila ada, atau akhir body):
```cshtml
@await Html.PartialAsync("_ImageLightboxModal")
```
  </action>
  <verify>
    <automated>Select-String -Path "Views\Admin\EditPesertaAnswers.cshtml" -Pattern "_QuestionImage","_ImageLightboxModal","Cap = 240","Cap = 120","answer-input"</automated>
  </verify>
  <acceptance_criteria>
    - EditPesertaAnswers.cshtml contains `_QuestionImage` minimal 3x (soal + MC opsi + MA opsi)
    - contains `Cap = 240` dan `Cap = 120`
    - contains `_ImageLightboxModal` (1x)
    - input opsi masih punya class `answer-input` (JS selectors utuh)
  </acceptance_criteria>
  <done>EditPesertaAnswers render gambar soal 240 + opsi 120 block-bawah + lightbox; JS edit selectors utuh.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Retrofit _PreviewQuestion.cshtml ke partial + host lightbox di ManagePackageQuestions.cshtml (RND-04 konsistensi) + build</name>
  <read_first>
    - Views/Admin/_PreviewQuestion.cshtml (penuh — soal img L20-24, opsi img L69-73 inline-flex, label opsi L61-74)
    - Views/Admin/ManagePackageQuestions.cshtml L260-290 (#previewModal host) + L326-430 (@section Scripts AJAX inject ke #previewModalBody)
    - Views/Shared/_QuestionImage.cshtml + _ImageLightboxModal.cshtml
    - .planning/phases/354-render-gambar-di-6-layar/354-UI-SPEC.md §3 (catatan konsistensi _PreviewQuestion: opsi ikut block-bawah, BUKAN scope baru)
    - CLAUDE.md (dotnet build wajib)
  </read_first>
  <action>
**A. Retrofit `Views/Admin/_PreviewQuestion.cshtml` ke partial bersama (D-04 konsistensi):**

1. Ganti blok gambar SOAL L20-24:
```cshtml
@if (!string.IsNullOrWhiteSpace(Model.ImagePath))
{
    <img src="@Model.ImagePath" alt="@Model.ImageAlt" class="img-fluid rounded border mb-3" style="max-height:240px" loading="lazy" />
}
```
menjadi:
```cshtml
@await Html.PartialAsync("_QuestionImage", new { ImagePath = Model.ImagePath, ImageAlt = Model.ImageAlt, Cap = 240 })
```

2. Ganti blok gambar OPSI L69-73 (inline-flex saat ini) + restruktur `<label>` opsi (L61-74) ke block-bawah (mekanisme A): ubah `<label class="list-group-item ... d-flex align-items-center gap-3">` jadi `d-flex flex-column`, bungkus baris `[input][huruf][teks]` dalam `<div class="d-flex align-items-center gap-3">`, lalu panggil partial opsi setelahnya:
```cshtml
<label class="list-group-item list-group-item-action d-flex flex-column">
    <div class="d-flex align-items-center gap-3">
        <input class="form-check-input mt-0" type="@(qtype == "MultipleAnswer" ? "checkbox" : "radio")" disabled style="transform:scale(1.2)" />
        <span class="fw-medium">@letter.</span>
        <span>@opt.OptionText</span>
    </div>
    @await Html.PartialAsync("_QuestionImage", new { ImagePath = opt.ImagePath, ImageAlt = opt.ImageAlt, Cap = 120, AriaContext = "opsi " + letter })
</label>
```
Ini harmonisasi visual (opsi inline → block-bawah) — DITERIMA & diinginkan per UI-SPEC §3, BUKAN scope baru (RND-04 sudah ship fungsional). Pertahankan badge tipe, textarea essay, rubrik, footer nilai/elemen-teknis existing.

**B. Host lightbox di `Views/Admin/ManagePackageQuestions.cshtml`:**
Karena `_PreviewQuestion` di-inject via AJAX ke `#previewModalBody` (L272), trigger lightbox di dalamnya butuh modal `#imageLightboxModal` hadir di HOST page (bukan di partial yang di-inject). Tambahkan di ManagePackageQuestions.cshtml (setelah `#previewModal` block ~L283, sebelum `@section Scripts`):
```cshtml
@await Html.PartialAsync("_ImageLightboxModal")
```
CATATAN nested-modal: `#imageLightboxModal` adalah modal terpisah dari `#previewModal`. Saat user klik gambar di preview, lightbox buka di atas preview. Bootstrap mendukung stacked modal; bila ada isu z-index/backdrop, lightbox tetap fungsional (Esc menutup lightbox dulu). JS lightbox sudah self-contained di partial (Plan 01) + guard bound — aman walau host juga punya JS modal sendiri.

**C.** Jalankan `dotnet build` — 0 error.
  </action>
  <verify>
    <automated>Select-String -Path "Views\Admin\_PreviewQuestion.cshtml" -Pattern "_QuestionImage","d-flex flex-column"; Select-String -Path "Views\Admin\ManagePackageQuestions.cshtml" -Pattern "_ImageLightboxModal"; dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - _PreviewQuestion.cshtml contains `_QuestionImage` minimal 2x (soal + opsi)
    - _PreviewQuestion.cshtml contains `d-flex flex-column` (opsi block-bawah retrofit)
    - _PreviewQuestion.cshtml TIDAK lagi contains `<img src="@Model.ImagePath"` mentah (sudah via partial)
    - ManagePackageQuestions.cshtml contains `_ImageLightboxModal` (host)
    - `dotnet build` output contains "Build succeeded", 0 baris ": error"
  </acceptance_criteria>
  <done>_PreviewQuestion retrofit ke partial (soal+opsi block-bawah); lightbox host di ManagePackageQuestions; build hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verifikasi visual Playwright 3 layar admin (SC#5)</name>
  <action>
Checkpoint human-verify — verifikasi visual end-to-end via browser/Playwright sesuai langkah di <how-to-verify>. Bukan tugas implementasi kode; executor menyiapkan dotnet run + data uji lalu pause untuk konfirmasi user.
  </action>
  <what-built>
    Gambar soal di AssessmentMonitoringDetail (essay, soal saja) + EditPesertaAnswers (soal+opsi block-bawah) + _PreviewQuestion retrofit (soal+opsi block-bawah via partial) + lightbox semua layar.
  </what-built>
  <how-to-verify>
    1. `dotnet run` → http://localhost:5277. Login admin (admin@pertamina.com, lihat reference_dev_credentials).
    2. **_PreviewQuestion / RND-04 (ManagePackageQuestions):** buka Manage Package Questions, klik preview soal bergambar. Verifikasi gambar soal 240px + opsi 120px DI BAWAH teks (bukan samping — retrofit) + klik gambar → lightbox buka di atas preview, Esc menutup lightbox.
    3. **AssessmentMonitoringDetail / RND-05:** buka detail monitoring assessment dengan soal essay bergambar → halaman nilai essay. Verifikasi gambar SOAL tampil (≤240px) + lightbox; tidak ada gambar opsi (essay).
    4. **EditPesertaAnswers / RND-06:** buka edit jawaban peserta (assessment dengan soal+opsi bergambar). Verifikasi gambar soal 240px + opsi 120px block-bawah + lightbox; radio/checkbox jawaban masih bisa diubah & disimpan (JS edit utuh).
    5. **Responsif (RND-07):** kecilkan viewport — gambar scale, opsi tetap block-bawah.
    6. Jalankan via Playwright MCP bila tersedia (screenshot tiap layar).
  </how-to-verify>
  <verify>Manual/Playwright human-verify per <how-to-verify> — tidak ada automated gate (visual).</verify>
  <done>3 layar admin render gambar (essay soal-saja, EditPeserta soal+opsi, preview retrofit) + lightbox + edit-jawaban berfungsi; user approve.</done>
  <resume-signal>Ketik "approved" bila 3 layar admin render gambar + lightbox benar + edit-jawaban masih berfungsi, atau jelaskan masalah.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VM/entity ImagePath → HTML admin | Path/alt di-render ke `<img>` di surface admin (authenticated) |
| _PreviewQuestion AJAX-injected → host DOM | Partial di-inject ke #previewModalBody; trigger lightbox refer ke modal host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-09 | Tampering/XSS | render `<img src>` via partial di 3 view admin + retrofit | mitigate | Semua `<img>` via partial _QuestionImage (Razor-encode, no Html.Raw, render-if-not-null L-02). _PreviewQuestion tidak lagi tulis `<img>` mentah. No XSS surface baru. |
| T-354-10 | Stacked modal / DOM | lightbox host di ManagePackageQuestions (atas #previewModal) | accept | Modal terpisah, JS lightbox self-contained + guard bound (Plan 01). Surface admin-only authenticated. Isu z-index = kosmetik non-security; checkpoint Task 4 memverifikasi fungsional. |
</threat_model>

<verification>
- `dotnet build` succeeded 0 error.
- 3 view admin + host memanggil partial sesuai acceptance criteria.
- Checkpoint human-verify Playwright SC#5 approved (gambar tampil + essay soal-saja + EditPeserta soal+opsi + retrofit preview + lightbox).
</verification>

<success_criteria>
- RND-05/06 terpenuhi: admin lihat gambar di essay grading (soal saja) + edit jawaban (soal+opsi).
- RND-04 retrofit ke partial (konsistensi block-bawah) + lightbox di host preview.
- RND-07 responsif via partial; build hijau; edit-jawaban tetap berfungsi.
</success_criteria>

<output>
After completion, create `.planning/phases/354-render-gambar-di-6-layar/354-06-SUMMARY.md`
</output>
