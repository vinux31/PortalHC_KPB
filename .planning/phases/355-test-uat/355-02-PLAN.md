---
phase: 355-test-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/q-image.jpg
  - tests/fixtures/opt-image.png
  - tests/e2e/helpers/wizardSelectors.ts
  - tests/e2e/helpers/examTypes.ts
  - tests/e2e/image-in-assessment.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [TST-02]
tags: [testing, image-upload, playwright, e2e, uat, seed-workflow]

must_haves:
  truths:
    - "Spec committed tests/e2e/image-in-assessment.spec.ts mengupload gambar soal+opsi via form admin NYATA (setInputFiles) lalu memverifikasi render <img> di StartExam + Results"
    - "Spec punya guardrail Seed Workflow: beforeAll BACKUP DB (resolve InstanceDefaultBackupPath) + afterAll RESTORE + fs.rmSync wwwroot/uploads/questions/{pkgId} + entry SEED_JOURNAL cleaned"
    - "Spec assert klik gambar opsi di StartExam → lightbox open DAN radio TIDAK ke-toggle (guard bug 926a57e1)"
    - "Spec assert soal TANPA gambar → tidak render img.question-image-zoom (null-branch guard D-06)"
    - "2 fixture (q-image.jpg + opt-image.png) berupa magic-byte VALID JPG/PNG nyata (bukan rename .txt)"
  artifacts:
    - path: "tests/fixtures/q-image.jpg"
      provides: "fixture upload gambar soal (magic-byte JPG valid FF D8 FF)"
    - path: "tests/fixtures/opt-image.png"
      provides: "fixture upload gambar opsi (magic-byte PNG valid 89 50 4E 47)"
    - path: "tests/e2e/helpers/wizardSelectors.ts"
      provides: "selector field gambar (questionImgField/optAImgField../alt) ditambah ke questionFormSelectors"
      contains: "questionImgField"
    - path: "tests/e2e/helpers/examTypes.ts"
      provides: "addQuestionViaForm extended dengan param images? → setInputFiles"
      contains: "setInputFiles"
    - path: "tests/e2e/image-in-assessment.spec.ts"
      provides: "spec UAT TST-02 + D-06 + guard toggle, dengan snapshot/restore + cleanup file"
      contains: "image-in-assessment"
    - path: "docs/SEED_JOURNAL.md"
      provides: "entry Phase 355 (D-05 Seed Workflow)"
      contains: "355"
  key_links:
    - from: "image-in-assessment.spec.ts beforeAll"
      to: "InstanceDefaultBackupPath + db.backup"
      via: "SERVERPROPERTY query (C:\\Temp diblokir)"
      pattern: "InstanceDefaultBackupPath"
    - from: "image-in-assessment.spec.ts afterAll"
      to: "wwwroot/uploads/questions/{createdPackageId}"
      via: "fs.rmSync recursive (file fisik TIDAK ter-cover DB RESTORE)"
      pattern: "rmSync"
    - from: "addQuestionViaForm(images)"
      to: "#questionImgField / #optAImgField"
      via: "page.setInputFiles pada input hidden"
      pattern: "setInputFiles"
    - from: "spec assert StartExam/Results"
      to: "img.question-image-zoom (_QuestionImage partial)"
      via: "locator visible + img-fluid + loading=lazy + src /uploads/questions/"
      pattern: "question-image-zoom"

user_setup: []
---

<objective>
Tulis satu spec Playwright committed `tests/e2e/image-in-assessment.spec.ts` (TST-02) yang membuktikan fitur gambar v24.0 bekerja end-to-end lintas-stack: admin upload gambar soal + tiap opsi via FORM NYATA (`setInputFiles`) → peserta `StartExam` melihat gambar soal+opsi (responsif) → peserta `Results` (pembahasan) melihat gambar soal+opsi. Plus guard null-branch (soal tanpa gambar) + guard toggle (klik gambar opsi → lightbox tanpa toggle radio). Dengan guardrail Seed Workflow WAJIB (snapshot/restore DB + cleanup file upload + entry SEED_JOURNAL).

Purpose: SC#2 Phase 355 + lesson Phase 354 ("Razor dynamic `_QuestionImage` reflection-based — bug runtime LOLOS build+grep, hanya ketahuan runtime"). UAT WAJIB menjalankan render NYATA di browser.
Output: spec committed + 2 fixture magic-byte valid + helper/selector extended additive + journal entry.

Constraint kritis (test-only phase): plan ini HANYA menyentuh `tests/fixtures/*`, `tests/e2e/helpers/wizardSelectors.ts`, `tests/e2e/helpers/examTypes.ts`, `tests/e2e/image-in-assessment.spec.ts`, `docs/SEED_JOURNAL.md`. ZERO production code/view/controller/migration. JANGAN edit `global.setup.ts`/`global.teardown.ts` (hardcoded matrix Phase 315). Helper extend = ADDITIVE (jangan refactor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/355-test-uat/355-CONTEXT.md
@.planning/phases/355-test-uat/355-RESEARCH.md
@.planning/phases/355-test-uat/355-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Helper existing yang DIPAKAI/EXTEND. Sumber file:line VERIFIED di RESEARCH/PATTERNS. Pakai langsung. -->

From tests/helpers/auth.ts:
```typescript
export async function login(page: Page, account: AccountKey): Promise<void>;
// fill input[name="email"]/[name="password"], click button[type="submit"], wait **/Home/**
```
Account keys (tests/helpers/accounts.ts): admin = admin@pertamina.com / 123456; coachee = rino.prasetyo@pertamina.com / 123456. Semua pwd 123456 (dev lokal — JANGAN staging/prod).

From tests/helpers/dbSnapshot.ts:
```typescript
export function backup(path: string): Promise<void>;   // BACKUP DATABASE WITH INIT,FORMAT
export function restore(path: string): Promise<void>;   // SINGLE_USER → RESTORE WITH REPLACE → MULTI_USER
export function queryString(sql: string): Promise<string>;
export function queryScalar(sql: string): Promise<number>;
// runSqlcmd REJECT non-localhost -S (guard built-in) — jangan bypass
```

From tests/e2e/helpers/examTypes.ts:
```typescript
export type QuestionInput =
  | { type:'MultipleChoice'; text:string; options:[string,string,string,string]; correctIndex:0|1|2|3; score:number }
  | { type:'MultipleAnswer'; ...; correctIndices:(0|1|2|3)[]; score:number }
  | { type:'Essay'; text:string; rubrik:string; maxCharacters?:number; score:number };
export interface CreateAssessmentOpts { title; category; scheduleDate; scheduleTime?; durationMinutes; passPercentage; allowAnswerReview; generateCertificate?; participantEmails:string[]; ewcdDate?; ewcdTime?; }
export function createAssessmentViaWizard(page: Page, opts: CreateAssessmentOpts): Promise<void>;
export function createDefaultPackage(page: Page, packageName?: string): Promise<number>;  // return packageId (regex a[href*=ManagePackageQuestions])
export function addQuestionViaForm(page: Page, packageId: number, q: QuestionInput): Promise<void>;  // EXTEND: terima images?
export function submitExamTwoStep(page: Page): Promise<void>;  // StartExam → ExamSummary → Results, dialog-arm benar
```

From tests/e2e/helpers/wizardSelectors.ts (questionFormSelectors — EXTEND additive):
```typescript
export const questionFormSelectors = {
  formCard:'#questionFormCard', questionType:'#QuestionType', questionText:'#questionText',
  optionsSection:'#optionsSection', maLabel:'#maLabel', rubrikSection:'#rubrikSection', rubrik:'#rubrik',
  maxCharacters:'#maxCharacters', scoreValue:'#scoreValue', submitBtn:'#submitBtn',
  optionA:'#option_A', optionB:'#option_B', optionC:'#option_C', optionD:'#option_D',
  correctA:'#correct_A', correctB:'#correct_B', correctC:'#correct_C', correctD:'#correct_D',
} as const;
```

Selector gambar VERIFIED dari Views/Admin/ManagePackageQuestions.cshtml (L155,165,201,198 — file input HIDDEN, setInputFiles tetap bekerja):
- soal: `#questionImgField` (name questionImage, accept image/png,image/jpeg) + `#questionImageAlt`
- opsi: `#optAImgField`..`#optDImgField` (name optionAImage..) + `#optAImageAlt`..`#optDImageAlt`

Render target VERIFIED Views/Shared/_QuestionImage.cshtml (render HANYA jika ImagePath non-null = guard D-06):
- `img.question-image-zoom` + class `img-fluid` + `loading="lazy"` + `src` match `/uploads/questions/` + `data-bs-target="#imageLightboxModal"` + `data-img-src`.
- Lightbox modal id `#imageLightboxModal`, di-host StartExam:401 + Results:419.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat 2 fixture gambar valid + extend selector & helper (additive)</name>
  <files>tests/fixtures/q-image.jpg, tests/fixtures/opt-image.png, tests/e2e/helpers/wizardSelectors.ts, tests/e2e/helpers/examTypes.ts</files>
  <read_first>
    - tests/e2e/helpers/wizardSelectors.ts (const questionFormSelectors L103-121 — extend additive, ikuti konvensi `as const` + komentar Source)
    - tests/e2e/helpers/examTypes.ts (addQuestionViaForm L154-212 — extend ADDITIVE param images?, sisip setInputFiles SETELAH fill teks opsi SEBELUM scoreValue+submit; JANGAN refactor pitfall-2 applyQTypeSwitch)
    - .planning/phases/355-test-uat/355-RESEARCH.md (Pattern 2 + Concrete Selectors A + Open Question 3 — syarat fixture magic-byte valid)
    - .planning/phases/355-test-uat/355-PATTERNS.md (sec wizardSelectors.ts + sec examTypes.ts + No Analog Found fixtures)
    - Views/Admin/ManagePackageQuestions.cshtml (L145-211 — sumber selector gambar, konfirmasi id/name/accept)
  </read_first>
  <action>
    A. Buat direktori `tests/fixtures/` (belum ada) + 2 fixture gambar VALID (magic-byte benar, kecil ~1-10KB):
       - `tests/fixtures/q-image.jpg` — header bytes WAJIB `FF D8 FF` (JPEG valid). Konten visual tak relevan (partial hanya render src) TAPI file harus parseable browser sebagai gambar agar `<img>` render + lolos `ValidateImageFile`.
       - `tests/fixtures/opt-image.png` — header bytes WAJIB `89 50 4E 47` (PNG valid).
       - JANGAN rename .txt -> .jpg (itu kasus INVALID yang justru diuji ditolak di xUnit). Generate file gambar nyata (mis. via tooling minimal valid 1x1 JPEG/PNG, atau salin gambar kecil existing lalu rename — pastikan magic-byte benar).
       - Verifikasi byte awal: q-image.jpg first 3 bytes == FF D8 FF; opt-image.png first 4 bytes == 89 50 4E 47.

    B. Extend `questionFormSelectors` di `wizardSelectors.ts` (ADDITIVE — tambah di akhir object, JANGAN ubah yang ada):
       ```typescript
       // Phase 355 — image upload fields (ManagePackageQuestions.cshtml:145-211)
       questionImgField: '#questionImgField',
       questionImageAlt: '#questionImageAlt',
       removeQuestionImage: '#removeQuestionImage',
       optAImgField: '#optAImgField', optBImgField: '#optBImgField', optCImgField: '#optCImgField', optDImgField: '#optDImgField',
       optAImageAlt: '#optAImageAlt', optBImageAlt: '#optBImageAlt', optCImageAlt: '#optCImageAlt', optDImageAlt: '#optDImageAlt',
       ```

    C. Extend `addQuestionViaForm` di `examTypes.ts` (ADDITIVE — tambah param opsional `images?`):
       - Tambah parameter keempat opsional: `images?: { question?: string; questionAlt?: string; optionA?: string; optionB?: string; optionC?: string; optionD?: string; optionAAlt?: string; optionBAlt?: string; optionCAlt?: string; optionDAlt?: string }`.
       - Di dalam fungsi, SETELAH loop fill teks opsi & correct, SEBELUM `page.fill(scoreValue)` + submit, sisipkan upload bersyarat (hanya bila images disuplai):
         ```typescript
         if (images?.question)    { await page.setInputFiles(questionFormSelectors.questionImgField, images.question); }
         if (images?.questionAlt) { await page.fill(questionFormSelectors.questionImageAlt, images.questionAlt); }
         if (images?.optionA)     { await page.setInputFiles(questionFormSelectors.optAImgField, images.optionA); }
         if (images?.optionAAlt)  { await page.fill(questionFormSelectors.optAImageAlt, images.optionAAlt); }
         // ...B/C/D analog (optBImgField/optCImgField/optDImgField + alt)...
         ```
       - JANGAN ubah route `/Admin/ManagePackageQuestions`, pitfall-2 wait applyQTypeSwitch, atau alert `.first()` (sudah dimitigasi).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const j=fs.readFileSync('tests/fixtures/q-image.jpg');const p=fs.readFileSync('tests/fixtures/opt-image.png');if(!(j[0]===255&&j[1]===216&&j[2]===255))throw new Error('q-image.jpg bukan JPEG');if(!(p[0]===137&&p[1]===80&&p[2]===78&&p[3]===71))throw new Error('opt-image.png bukan PNG');console.log('fixtures magic-byte OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/q-image.jpg first bytes == FF D8 FF (JPEG magic)
    - tests/fixtures/opt-image.png first bytes == 89 50 4E 47 (PNG magic)
    - tests/e2e/helpers/wizardSelectors.ts contains `questionImgField` AND `optAImgField` AND `questionImageAlt`
    - tests/e2e/helpers/examTypes.ts contains `setInputFiles` AND `images?`
    - `cd tests; npx tsc --noEmit -p tsconfig.json` (bila ada) ATAU `npx playwright test image-in-assessment.spec.ts --list` (Task 2) tidak error karena helper extend invalid TypeScript
  </acceptance_criteria>
  <done>2 fixture magic-byte valid ada; selector gambar + helper images? di-extend additive; tetap valid TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Tulis spec image-in-assessment.spec.ts (snapshot/restore + admin upload + StartExam/Results assert + guard toggle + guard null)</name>
  <files>tests/e2e/image-in-assessment.spec.ts</files>
  <read_first>
    - tests/e2e/cmp-records-351.spec.ts (L1-75 — TEMPLATE: imports + describe.configure serial + beforeAll snapshot resolve InstanceDefaultBackupPath + afterAll restore + Layer-4 + ordering try/catch->throw di akhir)
    - tests/e2e/exam-taking.spec.ts (L116-159 — coachee start-exam flow: login coachee → /CMP/Assessment → .assessment-card hasText title → .btn-start-standard → dialog accept → waitForURL **/CMP/StartExam/**)
    - tests/e2e/helpers/examTypes.ts (createAssessmentViaWizard opts allowAnswerReview:true WAJIB; createDefaultPackage return packageId; addQuestionViaForm extended Task 1; submitExamTwoStep)
    - .planning/phases/355-test-uat/355-RESEARCH.md (Architecture Patterns diagram + Concrete Selectors B/C/D/E + Common Pitfalls 1-6)
    - .planning/phases/355-test-uat/355-PATTERNS.md (sec image-in-assessment.spec.ts semua blok kode + Shared Patterns)
    - Views/Shared/_QuestionImage.cshtml (markup img.question-image-zoom) + Views/CMP/StartExam.cshtml (L105,148,181,401) + Views/CMP/Results.cshtml (L330,388,419)
  </read_first>
  <action>
    Buat `tests/e2e/image-in-assessment.spec.ts` (mode serial). Komposisi 2 analog (cmp-records-351 snapshot + exam-taking flow).

    IMPORTS + module state:
    ```typescript
    import { test, expect, Page } from '@playwright/test';
    import * as db from '../helpers/dbSnapshot';
    import { login } from '../helpers/auth';
    import { createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm, submitExamTwoStep } from './helpers/examTypes';
    import * as path from 'node:path';
    let snapshotPath: string;
    let createdPackageId: number | null = null;
    let assessmentTitle: string;
    test.describe.configure({ mode: 'serial' });
    ```

    beforeAll (snapshot — resolve dir via SERVERPROPERTY karena C:\Temp diblokir; TIDAK execScript seed karena D-03 admin-UI-driven):
    ```typescript
    const dir = (await db.queryString("SELECT CAST(SERVERPROPERTY('InstanceDefaultBackupPath') AS NVARCHAR(260))")).replace(/[\\/]+$/, '').replace(/\\/g, '/');
    const ts = new Date().toISOString().replace(/[:.]/g, '-');
    snapshotPath = `${dir}/HcPortalDB_Dev-pre355-${ts}.bak`;
    await db.backup(snapshotPath);
    ```

    afterAll (restore + cleanup file upload + ordering: try/catch restore -> cleanup -> throw restoreError di akhir per cmp-records-351:62-69):
    ```typescript
    if (!snapshotPath) return;
    let restoreError: unknown = null;
    try { await db.restore(snapshotPath); const fs = await import('node:fs'); try { fs.unlinkSync(snapshotPath); } catch {} }
    catch (e) { restoreError = e; }
    if (createdPackageId != null) {
      const fs = await import('node:fs'); const p = await import('node:path');
      const dir = p.resolve(__dirname, '../../wwwroot/uploads/questions', String(createdPackageId));
      try { fs.rmSync(dir, { recursive: true, force: true }); } catch {}
    }
    if (restoreError) throw restoreError;
    ```

    TEST 1 (admin upload via form NYATA): login('admin') -> createAssessmentViaWizard({ allowAnswerReview:true, participantEmails:['rino.prasetyo@pertamina.com'], category valid (mis. 'OJT'), scheduleDate today, durationMinutes, passPercentage, title=assessmentTitle unik }) -> createDefaultPackage(page) (simpan return ke createdPackageId) -> addQuestionViaForm(page, createdPackageId, { type:'MultipleChoice', text, options, correctIndex:0, score }, { question:path.resolve(__dirname,'../fixtures/q-image.jpg'), questionAlt:'diagram pompa', optionA:path.resolve(__dirname,'../fixtures/opt-image.png'), optionAAlt:'opsi impeller' }). TAMBAH 1 soal KEDUA TANPA images (untuk guard D-06 di Test 4). Assert `.alert-success` setiap submit (helper sudah assert).

    TEST 2 (peserta StartExam render + guard toggle): login('coachee') -> /CMP/Assessment -> card hasText assessmentTitle -> .btn-start-standard -> page.once('dialog', d=>d.accept()) -> click -> waitForURL **/CMP/StartExam/** -> assert #examHeader/#examTimer visible.
      - Assert gambar (JANGAN .nth() positional — opsi di-SHUFFLE, Pitfall 4):
        ```typescript
        const img = page.locator('img.question-image-zoom').first();
        await expect(img).toBeVisible();
        expect(await img.getAttribute('class')).toContain('img-fluid');
        expect(await img.getAttribute('loading')).toBe('lazy');
        expect(await img.getAttribute('src')).toMatch(/\/uploads\/questions\//);
        ```
      - Guard toggle (bug 926a57e1): gambar OPSI berada DI DALAM <label> (opsi punya class tambahan `d-block w-100`). Klik gambar opsi -> assert `#imageLightboxModal` visible DAN radio/checkbox opsi yang sama TIDAK `checked`. Tutup modal sebelum lanjut. Identifikasi gambar opsi via `data-img-alt="opsi impeller"` atau scope dalam label (BUKAN posisi).

    TEST 3 (Results render — perlu submit): masih di StartExam, jawab minimal 1 radio valid (Pitfall 5: ExamSummary blok submit bila unanswered>0) -> submitExamTwoStep(page) -> waitForURL **/CMP/Results/** -> assert section "Tinjauan Jawaban" render (Results butuh allowAnswerReview:true) -> assert `img.question-image-zoom` visible (soal + opsi) di Results.
      (Bila lebih stabil, gabung StartExam-assert + submit + Results-assert dalam SATU test berurutan dengan satu `page` — describe serial. Pilih bentuk paling stabil; yang penting keempat assert area tercakup.)

    TEST 4 (guard null D-06): scope ke qcard soal KEDUA (tanpa gambar) di StartExam ATAU Results -> `await expect(noImgCard.locator('img.question-image-zoom')).toHaveCount(0)`. Identifikasi qcard via teks soal kedua (bukan posisi).

    Header komentar spec: PRECONDITION app running localhost:5277 + SEED_WORKFLOW (temporary+local-only, snapshot/restore + cleanup file). Selector exam flow PERSIS dari exam-taking.spec.ts:116-134. JANGAN tiru blok create-soal exam-taking (route lama /Admin/ManageQuestions).
  </action>
  <verify>
    <automated>cd tests; npx playwright test image-in-assessment.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/image-in-assessment.spec.ts exists
    - file contains `InstanceDefaultBackupPath` (snapshot dir resolve)
    - file contains `db.backup` AND `db.restore`
    - file contains `rmSync` AND `uploads/questions` (cleanup file upload)
    - file passes image fixtures via addQuestionViaForm (upload via form NYATA)
    - file contains `question-image-zoom` (assert render) AND `img-fluid` AND `loading`
    - file contains `#imageLightboxModal` (guard toggle lightbox)
    - file contains `toHaveCount(0)` (guard null-branch D-06)
    - `cd tests; npx playwright test image-in-assessment.spec.ts --list` exits 0 (spec ter-parse + test ter-discover). RUN penuh end-to-end (app live localhost:5277) dilakukan di Plan 03 gate.
  </acceptance_criteria>
  <done>Spec committed lengkap: snapshot/restore + cleanup file, admin upload via form nyata, assert StartExam+Results render responsif, guard toggle, guard null; ter-parse Playwright (--list hijau).</done>
</task>

<task type="auto">
  <name>Task 3: Append entry SEED_JOURNAL.md Phase 355 (D-05 Seed Workflow)</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_JOURNAL.md (header format L7 + contoh Phase 354 L9 & Phase 351 L10 — kolom: Tanggal | Phase | Klasifikasi | Tujuan | Dampak | Snapshot file | Status)
    - .planning/phases/355-test-uat/355-RESEARCH.md (template baris persis Phase 355 sec Code Examples SEED_JOURNAL + Runtime State Inventory)
    - .planning/phases/355-test-uat/355-PATTERNS.md (sec SEED_JOURNAL entry)
    - CLAUDE.md (sec Seed Data Workflow — temporary+local-only -> snapshot -> journal -> restore -> cleaned)
  </read_first>
  <action>
    Append TEPAT SATU baris ke tabel di `docs/SEED_JOURNAL.md` (jangan hapus entry lama — audit trail). Pakai template:
    ```markdown
    | 2026-06-09 | 355 (test-uat gambar) | temporary + local-only | UAT TST-02 image-in-assessment.spec — admin upload gambar soal+opsi (setInputFiles fixture) -> peserta StartExam+Results render <img> + guard toggle. Cleanup file wwwroot/uploads/questions/{pkgId}. | AssessmentSessions(+1), AssessmentPackages(+1), PackageQuestions(+2), PackageOptions(+~8), file wwwroot/uploads/questions/{pkgId}/*.jpg|png (semua revert via RESTORE + rmSync) | {InstanceDefaultBackupPath}/HcPortalDB_Dev-pre355-{ts}.bak (runtime-resolved per BACKUP) | cleaned |
    ```
    Status `cleaned` karena lifecycle otomatis via spec beforeAll/afterAll (BACKUP->RESTORE+rmSync) — pola sama Phase 354 L9 / Phase 345 L11. Sesuaikan angka entitas bila Task 2 membuat lebih/kurang soal.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/SEED_JOURNAL.md','utf8');if(!/2026-06-09 \| 355/.test(s))throw new Error('entry 355 hilang');if(!/uploads\/questions/.test(s))throw new Error('cleanup file tidak dicatat');console.log('journal 355 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/SEED_JOURNAL.md contains baris dengan `2026-06-09 | 355` (entry baru)
    - baris memuat `temporary + local-only` (klasifikasi)
    - baris memuat `uploads/questions` (cleanup file non-DB dicatat)
    - baris status `cleaned`
    - Entry lama tidak terhapus (jumlah baris tabel bertambah 1)
  </acceptance_criteria>
  <done>Entry Phase 355 ter-append di SEED_JOURNAL.md (temporary+local-only, snapshot, cleanup file, cleaned) tanpa menghapus audit trail lama.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (peserta) -> server (upload + render) | Yang DIUJI, bukan diubah: admin upload file via multipart -> FileUploadHelper validasi magic-byte/ext/size + SaveFileAsync strip path-traversal -> _QuestionImage render `src` ber-encode Razor. Spec ini memverifikasi mitigasi existing bekerja end-to-end. |
| Test infra -> DB lokal + filesystem | Spec memutasi DB lokal + tulis file ke wwwroot/uploads/questions. Boundary diamankan oleh Seed Workflow (snapshot/restore + rmSync + journal) + dbSnapshot localhost-guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-01 | Tampering | Upload file-type (magic-byte image-only) — fitur sudah ada (FileUploadHelper.ValidateImageFile) | mitigate (already-mitigated-by-feature) | Plan 02 = EVIDENCE end-to-end: fixture VALID JPG/PNG lolos -> tersimpan -> render. (Kasus invalid/exe-renamed dicover xUnit FileUploadHelperTests — bukan di-upload di spec ini.) Spec tidak menambah surface. |
| T-355-03 | Information Disclosure | Encoded `src` (no raw HTML XSS) — render via atribut `src` ber-encode Razor (spec §8 / D-decision v24.0) | accept / mitigate (already-mitigated-by-feature) | Render gambar via `src` encoded tidak menambah surface XSS (keputusan v24.0). Spec memverifikasi `<img>` render dengan `src` ke /uploads/questions, bukan markup mentah. Tidak ada NEW high-severity threat. |
| T-355-04 | Tampering | DB snapshot target salah server (non-localhost) | mitigate | `dbSnapshot.runSqlcmd` REJECT non-localhost `-S` (guard built-in, jangan bypass). Backup dir resolve via InstanceDefaultBackupPath (C:\Temp diblokir service account). |

Catatan (test-only phase): Plan 02 = kode tes + fixture + helper + 1 baris journal. Tidak ada production code/endpoint/migration baru. Tidak ada NEW high-severity threat yang diperkenalkan; threat di atas semuanya already-mitigated-by-feature dengan spec sebagai evidence. Test creds dev lokal `123456` JANGAN dipakai staging/prod.
</threat_model>

<verification>
- `cd tests; npx playwright test image-in-assessment.spec.ts --list` hijau (spec ter-parse + test ter-discover).
- 2 fixture magic-byte valid (FF D8 FF / 89 50 4E 47).
- Helper/selector extend valid TypeScript (no type error saat --list).
- Entry SEED_JOURNAL Phase 355 ter-append (cleaned, cleanup file dicatat).
- git diff hanya menyentuh 6 file di files_modified (zero production code/view/controller/migration; global.setup/teardown tidak diubah).
- RUN penuh end-to-end (exits 0) + cleanup verified (folder uploads bersih + DB Layer-4) dijalankan di Plan 03 gate dengan app live localhost:5277.
</verification>

<success_criteria>
- Spec committed `image-in-assessment.spec.ts` ada: admin upload via form NYATA (setInputFiles) -> StartExam render gambar soal+opsi (img-fluid+lazy+src /uploads/questions) -> guard toggle (lightbox open, radio tidak ke-toggle) -> Results render -> guard null (soal tanpa gambar tak render img).
- Guardrail Seed Workflow lengkap: beforeAll BACKUP (InstanceDefaultBackupPath) + afterAll RESTORE + rmSync uploads/questions/{pkgId} + entry SEED_JOURNAL cleaned.
- 2 fixture magic-byte valid; helper+selector extended additive; zero production code/migration.
</success_criteria>

<output>
After completion, create `.planning/phases/355-test-uat/355-02-SUMMARY.md`
</output>
