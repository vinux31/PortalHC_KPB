---
phase: 356-audit-fix-assign-coach-coachee
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/CoacheeEligibilityCalculator.cs
  - HcPortal.Tests/CoacheeEligibilityCalculatorTests.cs
  - Controllers/CoachMappingController.cs
autonomous: true
requirements: [AF-1]
tags: [coaching, eligibility, assessment, audit-fix]

must_haves:
  truths:
    - "Coachee di track multi-unit yang 100% deliverable unit-nya Approved muncul di GetEligibleCoachees (track id=4 Alkylation 3/3)"
    - "Coachee yang deliverable unit-nya belum semua Approved TIDAK muncul"
    - "Track Tahun 3 tanpa deliverable: semua assigned-coachee tetap eligible (D-02 dipertahankan verbatim)"
    - "Logic eligibility per-unit dapat di-test tanpa DbContext (helper static pure)"
  artifacts:
    - path: "Helpers/CoacheeEligibilityCalculator.cs"
      provides: "Static pure helper IsEligiblePerUnit(statuses, expectedCount)"
      contains: "public static bool IsEligiblePerUnit"
    - path: "HcPortal.Tests/CoacheeEligibilityCalculatorTests.cs"
      provides: "4 [Fact] minimal AF-1 (full-approved/zero/partial/expectedCount-zero)"
      contains: "IsEligiblePerUnit"
    - path: "Controllers/CoachMappingController.cs"
      provides: "GetEligibleCoachees pakai expectedCount per-unit coachee (mirror AutoCreateProgressForAssignment .Trim() filter)"
      contains: "CoacheeEligibilityCalculator.IsEligiblePerUnit"
  key_links:
    - from: "GetEligibleCoachees (CoachMappingController.cs)"
      to: "CoacheeEligibilityCalculator.IsEligiblePerUnit"
      via: "panggil helper per coachee dengan statuses + expectedCount per-unit"
      pattern: "CoacheeEligibilityCalculator\\.IsEligiblePerUnit"
    - from: "GetEligibleCoachees expectedCount per-unit"
      to: "AutoCreateProgressForAssignment unit-resolution"
      via: "AssignmentUnit aktif -> fallback User.Unit; filter ProtonKompetensi.Unit.Trim() == resolvedUnit.Trim()"
      pattern: "\\.Trim\\(\\) == resolvedUnit\\.Trim\\(\\)"
---

<objective>
AF-1 (HIGH, headline): Perbaiki `GetEligibleCoachees` agar menghitung **expected deliverable per-unit coachee**, bukan total deliverable semua-unit track. Saat ini coachee di track multi-unit (terbukti track id=4: Alkylation 065 = 3 deliverable, RFCC NHT 053 = 1 deliverable) **tak pernah eligible** untuk Assessment Proton karena `mine.Count` (progress unit coachee) dibandingkan dengan `trackDeliverableIds.Count` (total semua unit) yang selalu lebih besar.

Purpose: Coachee track multi-unit yang sudah menyelesaikan seluruh deliverable unit-nya bisa muncul di dropdown CreateAssessment kategori Assessment Proton. Ini adalah bug HIGH yang memblokir alur assessment akhir.

Output: Helper static pure `CoacheeEligibilityCalculator` + 4 xUnit [Fact] + refactor call-site `GetEligibleCoachees`. Migration = false (tidak ada perubahan skema).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-CONTEXT.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-RESEARCH.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-PATTERNS.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-VALIDATION.md

<interfaces>
<!-- Kontrak existing yang dikonsumsi — executor TIDAK perlu eksplorasi codebase. -->

Analog static helper — Helpers/CertNumberHelper.cs L1-44 (class shell):
```csharp
using Microsoft.EntityFrameworkCore;
using HcPortal.Data;
namespace HcPortal.Helpers
{
    public static class CertNumberHelper
    {
        public static bool IsDuplicateKeyException(DbUpdateException ex) { ... }
    }
}
```

SUMBER KEBENARAN unit-resolution — Controllers/CoachMappingController.cs AutoCreateProgressForAssignment L1342-1367 (MIRROR PERSIS, JANGAN ubah file ini di sini, hanya baca):
```csharp
// Resolve unit: AssignmentUnit from active mapping, fallback to User.Unit
var assignmentUnit = await _context.CoachCoacheeMappings
    .Where(m => m.CoacheeId == coacheeId && m.IsActive)
    .Select(m => m.AssignmentUnit)
    .FirstOrDefaultAsync();
var resolvedUnit = assignmentUnit;
if (string.IsNullOrWhiteSpace(resolvedUnit))
    resolvedUnit = await _context.Users.Where(u => u.Id == coacheeId).Select(u => u.Unit).FirstOrDefaultAsync();

var deliverableIds = await _context.ProtonDeliverableList
    .Where(d => d.ProtonSubKompetensi!.ProtonKompetensi!.ProtonTrackId == protonTrackId
             && d.ProtonSubKompetensi!.ProtonKompetensi!.Unit!.Trim() == resolvedUnit.Trim())
    .Select(d => d.Id)
    .ToListAsync();
```

Analog test static — HcPortal.Tests/BuildActualCategoriesTests.cs (panggil method static langsung, no DB):
```csharp
using HcPortal.Helpers; // (di analog asli: using HcPortal.Controllers;)
using Xunit;
namespace HcPortal.Tests;
public class BuildActualCategoriesTests
{
    [Fact]
    public void DistinctNonEmpty_CaseInsensitive_Ordered() { ... Assert.Equal(...); }
}
```

Status literal: `"Approved"` (case-sensitive) — IDENTIK dengan GetEligibleCoachees L1321 dan progress awal `"Pending"`. JANGAN ubah casing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat helper static pure CoacheeEligibilityCalculator + 4 xUnit [Fact] (Wave 0 gap)</name>
  <read_first>
    - Helpers/CertNumberHelper.cs (class-shell analog: `public static class` di `namespace HcPortal.Helpers`)
    - HcPortal.Tests/BuildActualCategoriesTests.cs (analog test static — panggil method langsung tanpa DB)
    - .planning/phases/356-.../356-PATTERNS.md §"Helpers/CoacheeEligibilityCalculator.cs (CREATE)" + §"CoacheeEligibilityCalculatorTests.cs (CREATE)"
  </read_first>
  <behavior>
    - Test 1 FullApproved_Eligible: IsEligiblePerUnit(["Approved","Approved","Approved"], 3) == true
    - Test 2 ZeroProgress_NotEligible: IsEligiblePerUnit([], 1) == false
    - Test 3 PartialApproved_NotEligible: IsEligiblePerUnit(["Approved","Approved","Pending"], 3) == false
    - Test 4 ExpectedCountZero_NotEligible: IsEligiblePerUnit([], 0) == false (Tahun 3 ditangani di call-site, BUKAN helper — D-02)
  </behavior>
  <action>
Buat file BARU `Helpers/CoacheeEligibilityCalculator.cs` dengan isi PERSIS (signature = Claude's discretion D-13, ini default yang dikunci PATTERNS.md):

```csharp
using System.Collections.Generic;
using System.Linq;

namespace HcPortal.Helpers
{
    /// <summary>
    /// AF-1 (Phase 356): coachee eligible bila punya TEPAT expectedCount progress untuk unit-nya
    /// dan SEMUANYA "Approved". expectedCount &lt;= 0 → tidak eligible (track Tahun 3 tanpa
    /// deliverable ditangani TERPISAH di call-site GetEligibleCoachees per D-02, bukan di helper ini).
    /// Ekstrak dari GetEligibleCoachees agar logic eligibility per-unit dapat di-test tanpa DbContext.
    /// </summary>
    public static class CoacheeEligibilityCalculator
    {
        public static bool IsEligiblePerUnit(IReadOnlyList<string> myProgressStatuses, int expectedCount)
        {
            if (expectedCount <= 0) return false;
            if (myProgressStatuses.Count != expectedCount) return false;
            return myProgressStatuses.All(s => s == "Approved");
        }
    }
}
```

Lalu buat file BARU `HcPortal.Tests/CoacheeEligibilityCalculatorTests.cs` dengan isi PERSIS:

```csharp
using HcPortal.Helpers;
using Xunit;

namespace HcPortal.Tests;

public class CoacheeEligibilityCalculatorTests
{
    [Fact] // unit A: 3/3 Approved (expectedCount=3) → eligible
    public void FullApproved_Eligible() =>
        Assert.True(CoacheeEligibilityCalculator.IsEligiblePerUnit(
            new[] { "Approved", "Approved", "Approved" }, 3));

    [Fact] // unit B: 0 progress (expectedCount=1) → NOT eligible
    public void ZeroProgress_NotEligible() =>
        Assert.False(CoacheeEligibilityCalculator.IsEligiblePerUnit(new string[0], 1));

    [Fact] // sebagian (2/3 Approved, 1 Pending) → NOT eligible
    public void PartialApproved_NotEligible() =>
        Assert.False(CoacheeEligibilityCalculator.IsEligiblePerUnit(
            new[] { "Approved", "Approved", "Pending" }, 3));

    [Fact] // expectedCount==0 → NOT eligible (Tahun 3 ditangani di call-site, bukan helper)
    public void ExpectedCountZero_NotEligible() =>
        Assert.False(CoacheeEligibilityCalculator.IsEligiblePerUnit(new string[0], 0));
}
```

KRITIS: literal `"Approved"` case-sensitive — IDENTIK dengan GetEligibleCoachees L1321 `p.Status == "Approved"`. JANGAN ubah casing (InMemory & string compare case-sensitive, RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~IsEligiblePerUnit" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/CoacheeEligibilityCalculator.cs` ada dan berisi `public static bool IsEligiblePerUnit`
    - `HcPortal.Tests/CoacheeEligibilityCalculatorTests.cs` ada dan berisi 4 `[Fact]` memanggil `CoacheeEligibilityCalculator.IsEligiblePerUnit`
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~IsEligiblePerUnit"` exit 0 (4 passed)
  </acceptance_criteria>
  <done>Helper static + 4 [Fact] hijau. Logic eligibility per-unit dapat di-test tanpa DbContext.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor GetEligibleCoachees pakai expectedCount per-unit (mirror AutoCreateProgressForAssignment)</name>
  <read_first>
    - Controllers/CoachMappingController.cs L1277-1334 (GetEligibleCoachees — call-site yang diubah)
    - Controllers/CoachMappingController.cs L1342-1367 (AutoCreateProgressForAssignment — SUMBER KEBENARAN unit-resolution + `.Trim()` filter, MIRROR PERSIS)
    - Helpers/CoacheeEligibilityCalculator.cs (helper dari Task 1)
    - .planning/phases/356-.../356-PATTERNS.md §"GetEligibleCoachees L1277-1334" + RESEARCH Pitfall 1 (§364 .Trim() wajib dua sisi)
  </read_first>
  <action>
Di `Controllers/CoachMappingController.cs`, ganti blok eligibility GLOBAL L1316-1323 (yang membandingkan `mine.Count == trackDeliverableIds.Count`) dengan perhitungan **expectedCount PER-UNIT coachee**.

PERTAHANKAN VERBATIM (JANGAN ubah):
- L1279 guard `if (protonTrackId <= 0)`
- L1282-1288 query `assignedCoacheeIds`
- L1290-1307 cabang **Tahun 3** (`if (!trackDeliverableIds.Any())` → semua assigned eligible) — ini D-02, helper TIDAK menangani Tahun 3, call-site yang menangani.
- L1327-1333 proyeksi `users` + `OrderBy(FullName)` + return Json.

GANTI bagian eligibility (setelah cabang Tahun 3) menjadi: untuk SETIAP `coacheeId` di `assignedCoacheeIds`, resolve unit + hitung expectedCount per-unit dengan filter PERSIS sama seperti `AutoCreateProgressForAssignment` L1342-1367 (`AssignmentUnit` mapping aktif → fallback `User.Unit`; `ProtonDeliverableList` WHERE `ProtonSubKompetensi!.ProtonKompetensi!.ProtonTrackId == protonTrackId && ProtonSubKompetensi!.ProtonKompetensi!.Unit!.Trim() == resolvedUnit.Trim()`), lalu panggil helper:

```csharp
var eligibleCoacheeIds = new List<string>();
foreach (var coacheeId in assignedCoacheeIds)
{
    // Resolve unit per-coachee — MIRROR PERSIS AutoCreateProgressForAssignment L1342-1355
    var assignmentUnit = await _context.CoachCoacheeMappings
        .Where(m => m.CoacheeId == coacheeId && m.IsActive)
        .Select(m => m.AssignmentUnit)
        .FirstOrDefaultAsync();
    var resolvedUnit = assignmentUnit;
    if (string.IsNullOrWhiteSpace(resolvedUnit))
        resolvedUnit = await _context.Users
            .Where(u => u.Id == coacheeId)
            .Select(u => u.Unit)
            .FirstOrDefaultAsync();
    if (string.IsNullOrWhiteSpace(resolvedUnit)) continue; // unit tak terselesaikan → tidak eligible

    // expectedCount PER UNIT — filter PERSIS dengan .Trim() di DUA SISI (L1363-1365)
    var expectedCount = await _context.ProtonDeliverableList
        .CountAsync(d => d.ProtonSubKompetensi!.ProtonKompetensi!.ProtonTrackId == protonTrackId
                      && d.ProtonSubKompetensi!.ProtonKompetensi!.Unit!.Trim() == resolvedUnit.Trim());

    // status progress coachee untuk track ini (reuse progressRecords yang sudah di-batch-load)
    var myStatuses = progressRecords
        .Where(p => p.CoacheeId == coacheeId)
        .Select(p => p.Status)
        .ToList();

    if (CoacheeEligibilityCalculator.IsEligiblePerUnit(myStatuses, expectedCount))
        eligibleCoacheeIds.Add(coacheeId);
}
```

Tambah `using HcPortal.Helpers;` di bagian using controller (cek apakah sudah ada; CertNumberHelper sudah memakai namespace ini). PERTAHANKAN batch-load `progressRecords` L1310-1314 (sudah ada — myStatuses memfilter dari situ; tidak perlu query progress baru per coachee). PERTAHANKAN guard `if (!eligibleCoacheeIds.Any()) return Json(new List<object>());`.

KRITIS Pitfall 1: `.Trim()` WAJIB di DUA sisi (`Unit!.Trim() == resolvedUnit.Trim()`). Lupa `.Trim()` atau pakai `User.Unit` saja → count meleset → bug bergeser bukan hilang. Warning sign: track id=4 Alkylation 3/3 tetap NOT eligible setelah fix.

JANGAN longgarkan `[Authorize]` attribute existing pada action. JANGAN ubah `AutoCreateProgressForAssignment`.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0, 0 error
    - `GetEligibleCoachees` body berisi `CoacheeEligibilityCalculator.IsEligiblePerUnit`
    - `GetEligibleCoachees` body berisi `.Trim() == resolvedUnit.Trim()` (filter per-unit)
    - cabang `if (!trackDeliverableIds.Any())` (Tahun 3, D-02) MASIH ada verbatim
    - `trackDeliverableIds.Count` TIDAK lagi dipakai sebagai pembanding eligibility global
  </acceptance_criteria>
  <done>GetEligibleCoachees menghitung eligibility per-unit via helper; Tahun 3 by-design dipertahankan; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → GetEligibleCoachees | Input `protonTrackId` (int) dari form CreateAssessment; action `[Authorize(Roles="Admin, HC")]` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-01 | Elevation/Tampering | GetEligibleCoachees over-eligibility | mitigate | expectedCount per-unit + `mine.All("Approved")` + `expectedCount > 0` mencegah coachee under-qualified lolos; helper unit-test mengunci batas (partial/zero → not eligible) |
| T-356-02 | Tampering | `protonTrackId` parameter | accept | `if (protonTrackId <= 0) return empty` existing dipertahankan; EF LINQ parameterized (no raw SQL); int param tidak injectable |
| T-356-03 | Information Disclosure | proyeksi user (Id/FullName/Email/NIP/Section) | accept | proyeksi existing tidak diubah; data internal admin-tool, role-gated `Admin, HC` |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~IsEligiblePerUnit"` 4/4 hijau
- Per-unit filter mirror persis `AutoCreateProgressForAssignment` (`.Trim()` dua sisi)
- e2e track id=4 (Alkylation 3/3 → eligible) diverifikasi di Plan 05 (Playwright UAT, butuh seed)
</verification>

<success_criteria>
1. Helper static `CoacheeEligibilityCalculator.IsEligiblePerUnit` ada + 4 [Fact] hijau.
2. `GetEligibleCoachees` memakai expectedCount per-unit coachee (mirror filter `.Trim()` AutoCreateProgressForAssignment).
3. Cabang Tahun 3 (D-02) dipertahankan verbatim.
4. `dotnet build` 0 error.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-01-SUMMARY.md`
</output>
