---
phase: 356-audit-fix-assign-coach-coachee
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/CoachCoacheeMapping.cshtml
autonomous: true
requirements: [AF-2, AF-3]
tags: [coaching, ui-guard, vanilla-js, audit-fix]

must_haves:
  truths:
    - "Saat coachee unit X dicentang pertama, checkbox coachee unit lain ter-disable + label text-muted; hint constraint muncul"
    - "Saat semua centang dilepas, semua checkbox re-enabled + hint disembunyikan"
    - "Item ter-filter (display:none oleh filterCoacheesBySection) tetap dihormati — guard hanya toggle disabled + text-muted, BUKAN display"
    - "Backstop submitAssign: tolak submit bila >1 unit terpilih (sabuk pengaman race DOM)"
    - "Badge Graduated tetap tampil untuk mapping IsCompleted meski IsActive=false (D-06, setelah AF-3 set IsActive=false)"
  artifacts:
    - path: "Views/Admin/CoachCoacheeMapping.cshtml"
      provides: "AF-2 UI guard 1-unit/batch di updateAssignmentDefaults() + hint + backstop submitAssign; D-06 badge Graduated"
      contains: "coacheeUnitConstraintHint"
  key_links:
    - from: "updateAssignmentDefaults()"
      to: ".coachee-checkbox unit-berbeda"
      via: "set disabled=true + toggle class text-muted; reset saat checked.length===0"
      pattern: "\\.disabled = "
    - from: "submitAssign()"
      to: "guard 1-unit/batch"
      via: "cek units.size === 1 sebelum fetch (backstop)"
      pattern: "units"
---

<objective>
AF-2 (MED): UI guard "1 unit per batch" di modal assign. Saat ini batch-assign memaksa satu `AssignmentUnit` tunggal ke semua coachee terpilih → bila coachee beda unit dicampur, `AutoCreateProgressForAssignment` membuat progress unit salah. Fix via UI guard (Opsi A, D-07): saat coachee pertama dicentang, kunci unit-nya dan disable checkbox coachee unit lain. Backend `AutoCreateProgressForAssignment` TIDAK diubah (Opsi B ditolak — bikin makna AssignmentUnit batch kabur).

D-06 (AF-3 dependency, view): Setelah AF-3 (Plan 02) set `IsActive=false` saat graduate, mapping graduated jatuh ke cabang `else` (L329-338: tombol Aktifkan/Hapus) — badge "Graduated" tidak lagi tampil. Sesuaikan view agar badge "Graduated" tetap muncul untuk mapping `IsCompleted` meski `IsActive=false`, dan mapping graduated tidak menawarkan "Aktifkan" yang menyesatkan.

Purpose: Cegah batch cross-unit di sumber (UI) + jaga UX badge graduated konsisten dengan semantik AF-3.

Output: Modifikasi `Views/Admin/CoachCoacheeMapping.cshtml` (JS hooks + markup + badge). Tidak ada CSS baru (reuse Bootstrap 5 + utility class existing). Semua string Bahasa Indonesia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-CONTEXT.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-UI-SPEC.md
@.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-PATTERNS.md

<interfaces>
<!-- Aset existing yang dipakai (REUSE — jangan invent). Executor TIDAK perlu eksplorasi. -->

Markup baris coachee existing (L412-414) — `data-unit` SUDAH ada (REUSE):
```html
<div class="form-check coachee-item" data-section="@coachee.Section" data-unit="@coachee.Unit">
    <input class="form-check-input coachee-checkbox" type="checkbox"
           id="chk-@coachee.Id" value="@coachee.Id" onchange="updateAssignmentDefaults()" />
```
Empty-state existing (L428-431) — JANGAN redesign:
```html
@if (!eligibleCoachees.Any()) { <p class="text-muted small mb-0">Tidak ada coachee yang tersedia (semua sudah memiliki coach aktif).</p> }
```

filterCoacheesBySection() existing (L670-680) — memiliki `style.display` (JANGAN diganggu guard):
```javascript
function filterCoacheesBySection() {
    var selectedSection = document.getElementById('assignSectionFilter').value;
    var items = document.querySelectorAll('#coacheeChecklist .coachee-item');
    items.forEach(function (item) {
        if (!selectedSection || item.getAttribute('data-section') === selectedSection) item.style.display = '';
        else item.style.display = 'none';
    });
}
```

updateAssignmentDefaults() existing (L682-711) — `units` Set sudah dibangun; early-return L684 diganti jadi RESET:
```javascript
function updateAssignmentDefaults() {
    var checked = document.querySelectorAll('.coachee-checkbox:checked');
    if (checked.length === 0) return;   // ← AF-2: ganti jadi RESET
    var sections = new Set(); var units = new Set();
    checked.forEach(function(cb){ var item=cb.closest('.coachee-item'); var sec=item.getAttribute('data-section')||''; var unit=item.getAttribute('data-unit')||''; if(sec)sections.add(sec); if(unit)units.add(unit); });
    if (units.size === 1) { /* auto-fill AssignmentSection/AssignmentUnit L697-708 */ }
    // Multiple different units or no match — don't auto-fill (L710)
}
```

submitAssign() existing (L713-742): membaca `coacheeIds` (L715) + validasi alert + build payload + `fetch(appUrl('/Admin/CoachCoacheeMappingAssign'), {...})` (L735).

Badge region existing (L307-339): `@if (coachee.IsActive) { ... @if(!coachee.IsCompleted){tombol Graduated} else {badge Graduated} } else { tombol Aktifkan + Hapus }`. coachee.IsCompleted tersedia di proyeksi (Controller L114).

Hint markup BARU (DIKUNCI UI-SPEC §88-93), gaya `form-text`:
```html
<div id="coacheeUnitConstraintHint" class="form-text text-muted" style="display:none;">
    Satu batch assign hanya untuk satu unit. Coachee dari unit lain dinonaktifkan — kosongkan pilihan untuk berganti unit.
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AF-2 UI guard 1-unit/batch — extend updateAssignmentDefaults() + hint markup + backstop submitAssign()</name>
  <read_first>
    - Views/Admin/CoachCoacheeMapping.cshtml L407-433 (markup #coacheeChecklist + empty-state)
    - Views/Admin/CoachCoacheeMapping.cshtml L670-680 (filterCoacheesBySection — JANGAN diganggu display)
    - Views/Admin/CoachCoacheeMapping.cshtml L682-711 (updateAssignmentDefaults — extend)
    - Views/Admin/CoachCoacheeMapping.cshtml L713-742 (submitAssign — backstop)
    - .planning/phases/356-.../356-UI-SPEC.md §"AF-2" (states + disabled appearance + hint copy DIKUNCI) + 356-PATTERNS.md §"AF-2"
  </read_first>
  <action>
**(a) Hint markup** — Di bawah `#coacheeChecklist` (~L432, setelah `</div>` penutup checklist, sebelum `</div>` mb-3), tambah PERSIS (Bahasa Indonesia DIKUNCI UI-SPEC §91):
```html
<div id="coacheeUnitConstraintHint" class="form-text text-muted" style="display:none;">
    Satu batch assign hanya untuk satu unit. Coachee dari unit lain dinonaktifkan — kosongkan pilihan untuk berganti unit.
</div>
```

**(b) Extend updateAssignmentDefaults()** (L682-711) — ganti early-return L684 jadi RESET, tambah guard disable, PERTAHANKAN auto-fill existing:
```javascript
function updateAssignmentDefaults() {
    var checked = document.querySelectorAll('.coachee-checkbox:checked');
    var hint = document.getElementById('coacheeUnitConstraintHint');

    if (checked.length === 0) {
        // RESET: re-enable semua, hilangkan text-muted, sembunyikan hint
        document.querySelectorAll('#coacheeChecklist .coachee-item').forEach(function (item) {
            item.querySelector('.coachee-checkbox').disabled = false;
            item.classList.remove('text-muted');
        });
        if (hint) hint.style.display = 'none';
        return;
    }

    // AF-2 guard: kunci unit dari centang pertama, disable checkbox unit berbeda
    var lockedUnit = checked[0].closest('.coachee-item').getAttribute('data-unit') || '';
    document.querySelectorAll('#coacheeChecklist .coachee-item').forEach(function (item) {
        var cb = item.querySelector('.coachee-checkbox');
        var sameUnit = (item.getAttribute('data-unit') || '') === lockedUnit;
        cb.disabled = !sameUnit && !cb.checked;            // JANGAN disable yang sudah dicentang
        item.classList.toggle('text-muted', !sameUnit && !cb.checked);
    });
    if (hint) hint.style.display = '';

    // === auto-fill existing L686-709 TETAP jalan verbatim ===
    var sections = new Set();
    var units = new Set();
    checked.forEach(function(cb) {
        var item = cb.closest('.coachee-item');
        var sec = item.getAttribute('data-section') || '';
        var unit = item.getAttribute('data-unit') || '';
        if (sec) sections.add(sec);
        if (unit) units.add(unit);
    });
    if (units.size === 1) {
        var theUnit = Array.from(units)[0];
        for (var bagian in sectionUnitsMap) {
            if (sectionUnitsMap[bagian].indexOf(theUnit) >= 0) {
                var secSelect = document.getElementById('assignAssignmentSection');
                secSelect.value = bagian;
                filterAssignmentUnits('assign');
                document.getElementById('assignAssignmentUnit').value = theUnit;
                return;
            }
        }
    }
    // Multiple different units or no match — don't auto-fill
}
```

KRITIS (UI-SPEC §78, PATTERNS §368): guard HANYA toggle `disabled` + class `text-muted`. JANGAN sentuh `style.display` (milik filterCoacheesBySection). Disabled appearance = native Bootstrap 5 `.form-check-input:disabled` (NO CSS baru). Item ter-filter sembunyi tetap dihormati.

**(c) Backstop submitAssign()** (L713) — TAMBAH cek 1-unit SEBELUM `fetch` (L735), sebagai sabuk pengaman terhadap race manipulasi DOM (UI-checker note). Setelah membaca `coacheeIds` (L715), hitung unit unik dari checkbox tercentang:
```javascript
var selectedUnits = new Set(
    Array.from(document.querySelectorAll('.coachee-checkbox:checked'))
         .map(cb => cb.closest('.coachee-item').getAttribute('data-unit') || ''));
if (selectedUnits.size > 1) {
    alert('Satu batch assign hanya untuk satu unit. Kosongkan pilihan dan pilih ulang dari satu unit saja.');
    return;
}
```
Letakkan setelah validasi `coacheeIds.length === 0` (L722) agar pesan "pilih minimal satu coachee" tetap diprioritaskan. PERTAHANKAN validasi alert existing + build payload + fetch.

Empty-state (L428-431) JANGAN diubah (guard N/A tak ada checkbox).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0 (Razor compile OK)
    - `CoachCoacheeMapping.cshtml` berisi id `coacheeUnitConstraintHint` + teks `Satu batch assign hanya untuk satu unit`
    - `updateAssignmentDefaults()` berisi `.disabled = ` dan `classList.toggle('text-muted'`
    - `updateAssignmentDefaults()` reset block (checked.length === 0 → re-enable) ada
    - guard TIDAK menulis `style.display` di dalam updateAssignmentDefaults (display tetap milik filterCoacheesBySection)
    - `submitAssign()` berisi backstop `selectedUnits.size > 1` sebelum fetch
  </acceptance_criteria>
  <done>Cross-unit checkbox ter-disable + hint muncul saat 1 unit terpilih; reset saat clear; backstop submit menolak >1 unit; tanpa CSS baru.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 — badge Graduated tetap tampil untuk mapping IsCompleted (IsActive=false setelah AF-3)</name>
  <read_first>
    - Views/Admin/CoachCoacheeMapping.cshtml L307-339 (region tombol per-coachee: IsActive branch + else branch)
    - .planning/phases/356-.../356-CONTEXT.md D-06 (graduated muncul saat showAll dengan badge "Graduated"; default list IsActive-only)
    - .planning/phases/356-.../356-PATTERNS.md §"AF-3 D-06 badge"
  </read_first>
  <action>
PROBLEM: Setelah AF-3 (Plan 02) set `IsActive=false` saat graduate, mapping graduated `IsCompleted=true` jatuh ke cabang `else` existing (L329-338) yang menampilkan tombol "Aktifkan" + "Hapus" — badge "Graduated" (L324-327, hanya di dalam cabang `IsActive`) TIDAK tampil. List default (`!showAll`) memang sudah menyembunyikan graduated (filter IsActive di Controller L52-55, JANGAN diubah), tapi saat `showAll=true` graduated harus tampil dengan badge "Graduated", BUKAN tombol "Aktifkan" yang menyesatkan.

Sesuaikan region L307-339: prioritaskan cek `IsCompleted` SEBELUM `IsActive`, sehingga graduated selalu menampilkan badge dan tidak menawarkan Aktifkan. Struktur target:

```razor
@if (coachee.IsCompleted)
{
    <span class="badge bg-info"><i class="bi bi-award me-1"></i>Graduated</span>
}
else if (coachee.IsActive)
{
    <button class="btn btn-sm btn-outline-danger"
            onclick="confirmDeactivate(@coachee.Id, '@coachee.CoacheeName')">
        <i class="bi bi-x-circle"></i> Nonaktifkan
    </button>
    <form method="post" asp-action="MarkMappingCompleted" asp-controller="CoachMapping" class="d-inline">
        @Html.AntiForgeryToken()
        <input type="hidden" name="mappingId" value="@coachee.Id" />
        <button type="submit" class="btn btn-sm btn-outline-info"
                onclick="return confirm('Tandai coachee ini sebagai graduated?')">
            <i class="bi bi-award"></i> Graduated
        </button>
    </form>
}
else
{
    <button class="btn btn-sm btn-outline-success"
            onclick="reactivateMapping(@coachee.Id)">
        <i class="bi bi-arrow-repeat"></i> Aktifkan
    </button>
    <button class="btn btn-sm btn-outline-danger"
            onclick="confirmDelete(@coachee.Id, '@coachee.CoacheeName')">
        <i class="bi bi-trash me-1"></i> Hapus
    </button>
}
```

PERTAHANKAN tombol Edit existing (L304-306, di luar/atas blok ini) tanpa perubahan. PERTAHANKAN handler JS (`confirmDeactivate`/`reactivateMapping`/`confirmDelete`) — hanya struktur kondisi yang diubah. Badge "Graduated" memakai kelas existing `badge bg-info` + ikon `bi bi-award` (tidak ada warna/ikon baru). Default list (`!showAll`) tetap menyembunyikan graduated via filter Controller — view ini hanya memastikan SAAT graduated muncul (showAll), tampil sebagai badge.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exit 0 (Razor compile OK)
    - region tombol per-coachee menempatkan cek `@if (coachee.IsCompleted)` SEBELUM `coachee.IsActive`
    - mapping `IsCompleted=true` menampilkan `badge bg-info` "Graduated" (tidak menawarkan "Aktifkan")
    - tombol "Aktifkan"/"Hapus" tetap ada untuk mapping `!IsActive && !IsCompleted`
    - tombol "Graduated"/"Nonaktifkan" tetap ada untuk mapping `IsActive && !IsCompleted`
  </acceptance_criteria>
  <done>Badge Graduated tampil untuk mapping IsCompleted meski IsActive=false; tidak ada tombol Aktifkan menyesatkan untuk graduated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser DOM → submitAssign() | guard client-side; backend AutoCreateProgressForAssignment = sumber kebenaran unit (tidak diubah) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-11 | Tampering | UI guard di-bypass via DevTools (re-enable checkbox / manipulasi DOM) | mitigate (defense-in-depth) | backstop `submitAssign()` cek `units.size > 1` sebelum fetch; semantik `AssignmentUnit` eksplisit di backend tetap utuh (D-07); guard UI = UX bukan kontrol keamanan utama |
| T-356-12 | Information Disclosure | hint/badge text | accept | tidak ada data sensitif; reuse Bootstrap utility, no new surface |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error (Razor compile)
- AF-2 interaksi (disable/enable cross-unit + hint + reset) diverifikasi Plan 05 (Playwright UAT localhost:5277)
- D-06 badge Graduated saat showAll: diverifikasi Plan 05 (UAT, butuh mapping graduated dari AF-3)
</verification>

<success_criteria>
1. Centang coachee unit X → checkbox unit lain disabled + text-muted + hint muncul; clear → reset.
2. Backstop submitAssign menolak >1 unit.
3. Guard tidak mengganggu `display` (filterCoacheesBySection utuh); empty-state utuh; no CSS baru.
4. Badge Graduated tampil untuk mapping IsCompleted meski IsActive=false (D-06).
5. `dotnet build` 0 error.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/356-audit-fix-assign-coach-coachee-pastikan-fungsi-assign-benar-/356-04-SUMMARY.md`
</output>
