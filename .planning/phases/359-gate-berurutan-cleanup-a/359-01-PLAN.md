---
phase: 359-gate-berurutan-cleanup-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/ProtonCompletionService.cs
  - HcPortal.Tests/ProtonYearGateTests.cs
autonomous: true
requirements: [PCOMP-07]
must_haves:
  truths:
    - "Ada predikat cross-year murni yang bisa dites tanpa DbContext: Tahun 1 selalu allowed; Tahun N allowed hanya jika TahunKe N-1 ada di daftar passedYears."
    - "Daftar tahun lulus diambil dari penanda (ProtonFinalAssessment) via GetPassedYearsAsync, bukan dari status deliverable Approved."
  artifacts:
    - path: "Services/ProtonCompletionService.cs"
      provides: "Static predicate ProtonYearGate.IsAllowed + (opsional) instance helper IsPrevYearPassedAsync"
      contains: "IsAllowed"
    - path: "HcPortal.Tests/ProtonYearGateTests.cs"
      provides: "Pure [Fact] tests untuk predikat cross-year"
      contains: "ProtonYearGate"
  key_links:
    - from: "Services/ProtonCompletionService.cs (ProtonYearGate.IsAllowed)"
      to: "GetPassedYearsAsync hasil"
      via: "passedYears.Contains(prevTahunKe)"
      pattern: "passedYears.*Contains"
---

<objective>
Sediakan basis gate antar-tahun (cross-year) yang murni dan terukur, dikonsumsi oleh Plan 02 (CreateAssessment) dan Plan 03 (CoachMapping). Ini Wave 0/interface-first: kontrak predikat dibuat lebih dulu agar executor 02/03 tidak perlu menebak bentuk gate.

Purpose: Definisi tunggal "Tahun N-1 lulus" = ada penanda `ProtonFinalAssessment` (bukan sekadar deliverable Approved), konsisten lintas CreateAssessment + assign CoachMapping (D-03/D-04). Reuse `GetPassedYearsAsync` (dari Phase 358, no-gate query) sebagai sumber data.

Output: Static predicate `ProtonYearGate.IsAllowed(prevTahunKe, passedYears)` + (opsional) instance helper di `ProtonCompletionService` + test pure `[Fact]`. TANPA migration, TANPA perubahan DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-CONTEXT.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md

<interfaces>
<!-- Kontrak existing yang dipakai — JANGAN eksplorasi codebase, pakai langsung. -->

Dari Services/ProtonCompletionService.cs (Phase 358, sudah ada):
```csharp
// Query murni: daftar TahunKe (mis. "Tahun 1","Tahun 2") yang sudah ber-penanda untuk (coacheeId, trackType).
// TANPA gate. Basis cross-year gate.
public async Task<List<string>> GetPassedYearsAsync(string coacheeId, string trackType);
```

Dari Models/ProtonModels.cs:
```csharp
// ProtonTrack: punya TrackType (string), TahunKe (string, mis "Tahun 1"), Urutan (int, 1/2/3).
```

Constructor (sudah DI-registered di Program.cs:57):
```csharp
public ProtonCompletionService(ApplicationDbContext context, ILogger<ProtonCompletionService> logger)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Predikat pure ProtonYearGate.IsAllowed + test [Fact]</name>
  <files>Services/ProtonCompletionService.cs, HcPortal.Tests/ProtonYearGateTests.cs</files>
  <read_first>
    - Services/ProtonCompletionService.cs (seluruh file — lihat GetPassedYearsAsync:92-101, XML doc convention :88-91, namespace HcPortal.Services)
    - HcPortal.Tests/CoacheeEligibilityCalculatorTests.cs (shape test pure [Fact], using/namespace, assert pola)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (bagian "Tests — Analog A" + "ProtonCompletionService — cross-year gate basis")
  </read_first>
  <behavior>
    - Test: Tahun 1 (prevTahunKe == null) → IsAllowed == true (tanpa prasyarat, D-03).
    - Test: prevTahunKe = "Tahun 1", passedYears = ["Tahun 1"] → true.
    - Test: prevTahunKe = "Tahun 2", passedYears = ["Tahun 1"] → false (Tahun 2 belum lulus, blok Tahun 3).
    - Test: prevTahunKe = "Tahun 1", passedYears = [] → false.
    - Test: prevTahunKe = "Tahun 1", passedYears = null → false (null-safe, treat as kosong).
    - Test: whitespace/trim — prevTahunKe = "Tahun 1", passedYears = [" Tahun 1 "] → true (kedua sisi di-Trim).
  </behavior>
  <action>
Di `Services/ProtonCompletionService.cs`, dalam namespace `HcPortal.Services`, tambahkan static class baru SETELAH class `ProtonCompletionService` (file ini boleh punya >1 type dalam namespace yang sama):

```csharp
/// <summary>
/// Phase 359 (PCOMP-07) — predikat MURNI gate antar-tahun Proton. Tanpa DbContext/IO.
/// "Tahun N-1 lulus" = TahunKe N-1 ada di daftar passedYears (penanda ProtonFinalAssessment),
/// BUKAN sekadar deliverable Approved (D-03). Tahun 1 (prevTahunKe == null) selalu diizinkan.
/// </summary>
public static class ProtonYearGate
{
    /// <param name="prevTahunKe">TahunKe tahun sebelumnya (mis. "Tahun 1"); null = tidak ada prasyarat (Tahun 1).</param>
    /// <param name="passedYears">Daftar TahunKe yang sudah ber-penanda (hasil GetPassedYearsAsync).</param>
    public static bool IsAllowed(string? prevTahunKe, IEnumerable<string>? passedYears)
    {
        if (string.IsNullOrWhiteSpace(prevTahunKe)) return true; // Tahun 1 — no prereq (D-03)
        if (passedYears == null) return false;
        var needle = prevTahunKe.Trim();
        return passedYears.Any(y => !string.IsNullOrWhiteSpace(y) && y.Trim() == needle);
    }
}
```

Pastikan `using System.Collections.Generic;` dan `using System.Linq;` tersedia (tambah di atas file bila belum — file saat ini sudah pakai EF LINQ jadi `System.Linq` mungkin sudah implisit via global usings; tambahkan eksplisit bila build error).

Buat `HcPortal.Tests/ProtonYearGateTests.cs` meniru shape `CoacheeEligibilityCalculatorTests.cs` (plain `[Fact]`, `using HcPortal.Services; using Xunit; using System.Collections.Generic;`). Implementasikan 6 test sesuai blok `<behavior>` di atas, satu `[Fact]` per kasus, nama deskriptif (mis. `Year1_NullPrev_Allowed`, `Year2_PrevYear1Passed_Allowed`, `Year3_PrevYear2NotPassed_Blocked`, `EmptyPassed_Blocked`, `NullPassed_Blocked`, `Whitespace_Trimmed_Allowed`).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~ProtonYearGate"</automated>
  </verify>
  <acceptance_criteria>
    - grep `class ProtonYearGate` di Services/ProtonCompletionService.cs → 1 match (static class).
    - grep `IsAllowed` di Services/ProtonCompletionService.cs → minimal 1 match (signature `string?`, `IEnumerable<string>?`).
    - File HcPortal.Tests/ProtonYearGateTests.cs ada, berisi minimal 6 `[Fact]`.
    - `dotnet build` 0 error.
    - `dotnet test --filter "FullyQualifiedName~ProtonYearGate"` → semua hijau (6 pass).
  </acceptance_criteria>
  <done>Predikat ProtonYearGate.IsAllowed ada + 6 [Fact] hijau + build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Instance helper IsPrevYearPassedAsync (jembatan ke DB) + integration [Fact]</name>
  <files>Services/ProtonCompletionService.cs, HcPortal.Tests/ProtonYearGateTests.cs</files>
  <read_first>
    - Services/ProtonCompletionService.cs (GetPassedYearsAsync:92-101 — reuse, JANGAN duplikat query)
    - HcPortal.Tests/ProtonCompletionServiceTests.cs:25-71 (ProtonCompletionFixture : IAsyncLifetime — real-SQL disposable HcPortalDB_Test_<guid>, [Trait("Category","Integration")], IClassFixture)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (Tests — Analog B)
  </read_first>
  <action>
Tambahkan instance method di class `ProtonCompletionService` (BUKAN di static `ProtonYearGate`) yang menjembatani DB → predikat pure. Reuse `GetPassedYearsAsync` (JANGAN tulis query baru):

```csharp
/// <summary>
/// Phase 359 (PCOMP-07/D-03) — true bila prevTahunKe (mis. "Tahun 1") sudah lulus untuk coachee+trackType.
/// prevTahunKe == null (Tahun 1) → true. Reuse GetPassedYearsAsync (no-gate) lalu delegasi ke ProtonYearGate.IsAllowed.
/// </summary>
public async Task<bool> IsPrevYearPassedAsync(string coacheeId, string trackType, string? prevTahunKe)
{
    if (string.IsNullOrWhiteSpace(prevTahunKe)) return true;
    var passed = await GetPassedYearsAsync(coacheeId, trackType);
    return ProtonYearGate.IsAllowed(prevTahunKe, passed);
}
```

Tambahkan 1 test integration di `HcPortal.Tests/ProtonYearGateTests.cs` (atau file integration terpisah bila lebih bersih) memakai `ProtonCompletionFixture` pola dari `ProtonCompletionServiceTests.cs`: seed 1 ProtonTrack Tahun 1 + 1 ProtonTrackAssignment + 1 ProtonFinalAssessment, lalu assert `IsPrevYearPassedAsync(coacheeId, trackType, "Tahun 1") == true` dan untuk tahun yang tak ber-penanda == false. Tandai `[Trait("Category","Integration")]` + `: IClassFixture<ProtonCompletionFixture>`. Test ini pakai HcPortalDB_Test_<guid> (BUKAN HcPortalDB_Dev) → tidak perlu SEED_WORKFLOW snapshot.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~ProtonYearGate"</automated>
  </verify>
  <acceptance_criteria>
    - grep `IsPrevYearPassedAsync` di Services/ProtonCompletionService.cs → 1 match (signature `(string coacheeId, string trackType, string? prevTahunKe)` return `Task<bool>`).
    - grep `GetPassedYearsAsync` dipanggil di dalam IsPrevYearPassedAsync (reuse, bukan query duplikat) — verifikasi via baca file.
    - Test integration baru ada `[Trait("Category","Integration")]`.
    - `dotnet build` 0 error; `dotnet test --filter "FullyQualifiedName~ProtonYearGate"` hijau.
  </acceptance_criteria>
  <done>IsPrevYearPassedAsync mendelegasi ke GetPassedYearsAsync + ProtonYearGate.IsAllowed; integration test hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Plan 01 pure helper/service — tidak ada entry HTTP, tidak ada input user langsung. Gate enforcement (boundary) ada di Plan 02/03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-01 | Tampering | ProtonYearGate.IsAllowed | accept | Predikat pure, deterministik, tanpa state; di-cover 6 [Fact]. Salah-logika tertangkap test, bukan vektor serangan runtime. |
| T-359-02 | Information Disclosure | IsPrevYearPassedAsync | accept | Hanya membaca penanda untuk coachee+track tertentu; tidak mengembalikan PII; dipakai internal oleh gate controller (auth di Plan 02/03). |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~ProtonYearGate"` semua hijau.
- grep `ProtonYearGate` + `IsPrevYearPassedAsync` ada di Services/ProtonCompletionService.cs.
- No migration: tidak ada perubahan di Models/ atau Migrations/.
</verification>

<success_criteria>
- Predikat cross-year murni + jembatan DB tersedia dan tertest, siap dikonsumsi Plan 02 & 03.
- Definisi "Tahun N-1 lulus" = penanda-based (GetPassedYearsAsync), bukan deliverable-Approved.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/359-gate-berurutan-cleanup-a/359-01-SUMMARY.md`.
</output>
