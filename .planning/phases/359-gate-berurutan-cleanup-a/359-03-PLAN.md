---
phase: 359-gate-berurutan-cleanup-a
plan: 03
type: execute
wave: 2
depends_on: ["359-01"]
files_modified:
  - Controllers/CoachMappingController.cs
autonomous: true
requirements: [PCOMP-07, PCOMP-09]
must_haves:
  truths:
    - "Assign Tahun N (TrackType sama) di-HARD-BLOCK kalau Tahun N-1 belum lulus (penanda), tanpa opsi warning-override."
    - "'Tahun N-1 lulus' dinilai dari penanda ProtonFinalAssessment (GetPassedYearsAsync), bukan sekadar deliverable Approved — konsisten dengan gate CreateAssessment."
    - "Assignment hasil bypass (D-06) dan request renewal (D-07) di-exempt dari hard-block cross-year (titik exempt disiapkan logis untuk Phase 360)."
    - "Tombol 'Mark graduated' (MarkMappingCompleted) tetap diblok kalau Tahun 3 belum lulus, dengan pesan S2."
  artifacts:
    - path: "Controllers/CoachMappingController.cs"
      provides: "Cross-year hard-block di assign + graduation gate (verify/align) + DI ProtonCompletionService"
      contains: "ProtonCompletionService"
  key_links:
    - from: "CoachMappingController.CoachCoacheeMappingAssign (:496-564)"
      to: "ProtonCompletionService.GetPassedYearsAsync / ProtonYearGate"
      via: "penanda-based cross-year hard-block (drop ConfirmProgressionWarning escape)"
      pattern: "GetPassedYearsAsync|ProtonYearGate"
    - from: "CoachMappingController.MarkMappingCompleted (:1130-1135)"
      to: "IsYearCompletedAsync (Tahun 3)"
      via: "graduation gate (verify/align message)"
      pattern: "IsYearCompletedAsync"
---

<objective>
Ubah blok progression-warning yang SOFT (warning-override) menjadi HARD-BLOCK cross-year (D-05) di assign CoachMapping, pakai definisi penanda-based (D-03) konsisten dengan gate CreateAssessment (Plan 02). Siapkan titik exempt logis untuk bypass (D-06) + renewal (D-07). Verifikasi/align graduation gate (D-10) yang sudah ada di MarkMappingCompleted.

Purpose: Tutup pintu kedua (assign) selain CreateAssessment (A-5.1, spec §4.3). Tahun N tidak bisa di-assign jika Tahun N-1 belum lulus — tanpa escape "Tetap lanjutkan?".

Output: Hard-block di `:556-561`, DI `ProtonCompletionService` di constructor, graduation message S2. TANPA migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-CONTEXT.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md
@.planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang dipakai — pakai langsung. -->

Dari Plan 359-01 (Services/ProtonCompletionService.cs):
```csharp
public async Task<bool> IsPrevYearPassedAsync(string coacheeId, string trackType, string? prevTahunKe);
public async Task<List<string>> GetPassedYearsAsync(string coacheeId, string trackType);
public static class ProtonYearGate { public static bool IsAllowed(string? prevTahunKe, IEnumerable<string>? passedYears); }
```

Existing di CoachMappingController.cs (POINT HOOK — sudah diverifikasi planner):
- Assign POST `CoachCoacheeMappingAssign` :457; progression block :496-564.
  - prevTrack di-resolve :503-506 (TrackType sama + Urutan == requestedTrack.Urutan-1).
  - incompleteCoachees dihitung :537-553 (saat ini berbasis progress Approved — HARUS dialihkan ke penanda).
  - SOFT block saat ini :556-561 (return Json warning=true + ConfirmProgressionWarning escape).
- `requestedTrack` punya TrackType, TahunKe, Urutan (ProtonModels.cs).
- `MarkMappingCompleted` :1112; graduation gate :1130-1135 (IsYearCompletedAsync(tahun3Assignment.Id), TempData["Error"], RedirectToAction).
- `IsYearCompletedAsync` :1096-1106 = allApproved && hasFinalAssessment (penanda IS final assessment → sudah benar).
- Constructor :20-31 TIDAK inject ProtonCompletionService. `using HcPortal.Services;` SUDAH ada (:7). Service sudah AddScoped di Program.cs:57.
- JSON error convention: `return Json(new { success = false, message = "..." });` (:460/489/651).
- CoachAssignRequest punya: CoacheeIds, CoachId, ProtonTrackId, ConfirmProgressionWarning, AssignmentUnit/Section, StartDate. (RenewsSessionId TIDAK ada di CoachAssignRequest — cek; assign normal bukan renewal, lihat catatan D-07 di action.)
</interfaces>

<analog>
<!-- Hard-block target (drop escape) — SOFT saat ini di :556-561: -->
```csharp
// CURRENT (soft, REPLACE):
if (incompleteCoachees.Any() && !req.ConfirmProgressionWarning)
    return Json(new { success = false, warning = true,
        message = $"{incompleteCoachees.Count} coachee belum menyelesaikan {prevTrack.DisplayName}. Tetap lanjutkan?",
        incompleteCount = incompleteCoachees.Count });
```
<!-- S2 copy (verbatim 359-UI-SPEC): "Tidak bisa assign Tahun {N}: Tahun {N-1} ({TrackType}) belum lulus untuk coachee ini." -->
<!-- Graduation S2 copy: "Tidak bisa menandai lulus (graduated): Tahun 3 belum lulus untuk pekerja ini." -->
</analog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject ProtonCompletionService + cross-year hard-block penanda-based (drop warning escape)</name>
  <files>Controllers/CoachMappingController.cs</files>
  <read_first>
    - Controllers/CoachMappingController.cs (constructor :20-31; assign :457-565 SELURUHNYA; cek apakah CoachAssignRequest punya field bypass/renewal)
    - Services/ProtonCompletionService.cs (GetPassedYearsAsync + IsPrevYearPassedAsync + ProtonYearGate dari Plan 01)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (CoachMappingController section + DI note + JSON error convention)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md (S2 cross-year assign copy)
  </read_first>
  <action>
1) DI: tambah `ProtonCompletionService` ke constructor :20-31 (field `private readonly ProtonCompletionService _protonCompletionService;` + parameter + assignment). `using HcPortal.Services;` sudah ada (:7). Service sudah AddScoped (Program.cs:57) — tidak perlu ubah Program.cs.

2) Ubah definisi "incomplete" dari progress-Approved (:537-553) ke PENANDA-based (D-03), konsisten dengan Plan 02. Pertahankan cabang-1 (sudah punya assignment untuk requestedTrack → skip) yang ada di :513-517/:540. Ganti penilaian cabang-2/3 dengan cek penanda prevTrack:

```csharp
// Phase 359 (PCOMP-07/D-03) — "Tahun N-1 lulus" = ada penanda (GetPassedYearsAsync), bukan progress Approved.
var incompleteCoachees = new List<string>();
foreach (var coacheeId in coacheeIdsForWarning)
{
    if (hasForRequestedTrack.Contains(coacheeId)) continue; // cabang 1: reactivated → skip (UNCHANGED)
    // D-06/D-07 exempt point (disiapkan logis; full bypass = Phase 360):
    //   bila request ini bypass-origin ATAU renewal → JANGAN blok cross-year.
    //   (CoachAssignRequest assign normal: tidak ada flag bypass/renewal di Phase 359 → exempt = false.
    //    Sediakan kondisi eksplisit `bool isExemptFromCrossYear = false;` + komentar agar Phase 360 tinggal isi.)
    bool isExemptFromCrossYear = false; // Phase 360 isi: req bypass-origin || renewal
    if (isExemptFromCrossYear) continue;

    bool prevPassed = await _protonCompletionService
        .IsPrevYearPassedAsync(coacheeId, requestedTrack.TrackType, prevTrack.TahunKe);
    if (!prevPassed) incompleteCoachees.Add(coacheeId);
}
```
> Catatan: `prevTrack.TahunKe` adalah TahunKe tahun sebelumnya (mis. "Tahun 1"); IsPrevYearPassedAsync cek apakah itu ada di penanda untuk (coacheeId, TrackType). Tahun 1 tidak masuk blok ini karena `prevTrack` null (`:507 if (prevTrack != null)`).

3) HARD-BLOCK: ganti blok SOFT :556-561 dengan hard-block (drop `!req.ConfirmProgressionWarning`, drop `warning=true`). Pakai S2 copy. Karena assign bisa banyak coachee, sebut jumlah/scope:

```csharp
if (incompleteCoachees.Any())
{
    // Resolve N (tahun diminta) dari requestedTrack.TahunKe; N-1 dari prevTrack.TahunKe.
    return Json(new { success = false,
        message = $"Tidak bisa assign {requestedTrack.TahunKe}: {prevTrack.TahunKe} ({requestedTrack.TrackType}) belum lulus untuk {incompleteCoachees.Count} coachee." });
}
```
(Sesuaikan agar tetap match S2 intent — sebutkan tahun N, tahun N-1, TrackType, scope. Jika lebih natural per-coachee tunggal, gunakan persis string S2 verbatim.)

4) Hapus referensi `ConfirmProgressionWarning` di field `CoachAssignRequest` HANYA jika tidak dipakai lagi di tempat lain (grep dulu; jika dipakai frontend, biarkan field-nya ada tapi tidak lagi memberi escape — aman dibiarkan). JANGAN sentuh transaction/duplicate-race convention :578/:641-651.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `_protonCompletionService` di Controllers/CoachMappingController.cs → ada (field + constructor assign + dipakai di assign).
    - grep `IsPrevYearPassedAsync` di CoachMappingController.cs → minimal 1 match.
    - grep `warning = true` di blok assign → 0 match (escape dihapus).
    - grep `ConfirmProgressionWarning` tidak lagi dipakai sebagai escape di hard-block (verifikasi via baca file).
    - String hard-block memuat "Tidak bisa assign" + tahun + TrackType (S2).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>ProtonCompletionService diinject; cross-year hard-block penanda-based menggantikan soft warning-override; titik exempt D-06/D-07 disiapkan; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Verify/align graduation gate (D-10) + message S2</name>
  <files>Controllers/CoachMappingController.cs</files>
  <read_first>
    - Controllers/CoachMappingController.cs (MarkMappingCompleted :1112-1135 + IsYearCompletedAsync :1096-1106)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-PATTERNS.md (graduation gate ALREADY PRESENT, verify/extend)
    - .planning/phases/359-gate-berurutan-cleanup-a/359-UI-SPEC.md (S2 graduation copy)
  </read_first>
  <action>
Graduation gate SUDAH ADA dan benar: `IsYearCompletedAsync(tahun3Assignment.Id)` (:1130) = `allApproved && hasFinalAssessment` (:1102-1105). Penanda IS final assessment → gate sudah mencerminkan "Tahun 3 lulus". TUGAS: verify-only + align copy.

1) Konfirmasi tidak ada pintu lain yang men-set `IsCompleted=true` tanpa lewat gate ini (grep `IsCompleted = true` di file → harus hanya di MarkMappingCompleted setelah gate). Bila ditemukan pintu lain, tambahkan gate yang sama.

2) Align pesan blok (:1133) ke S2 copy VERBATIM dari 359-UI-SPEC:
   `TempData["Error"] = "Tidak bisa menandai lulus (graduated): Tahun 3 belum lulus untuk pekerja ini.";`
   (Pertahankan `RedirectToAction("CoachCoacheeMapping")`.) Pesan :1127 (belum punya assignment Tahun 3) boleh dibiarkan atau diselaraskan nada (formal-operasional).

3) JANGAN ubah logika IsYearCompletedAsync, transaction, atau cascade deactivate (:1136+).
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - grep `Tidak bisa menandai lulus (graduated)` di CoachMappingController.cs → 1 match (S2 graduation).
    - grep `IsCompleted = true` → hanya muncul di MarkMappingCompleted setelah gate `IsYearCompletedAsync` (verifikasi via baca file; bila ada pintu lain, gate ditambahkan).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Graduation gate dikonfirmasi memblok jika Tahun 3 belum lulus; pesan diselaraskan ke S2; tidak ada pintu set IsCompleted tanpa gate; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /Admin/CoachCoacheeMappingAssign | Admin/HC kirim CoacheeIds + ProtonTrackId. JSON endpoint; `ConfirmProgressionWarning` sebelumnya jadi escape klien-controlled. |
| Browser → POST MarkMappingCompleted | Admin/HC menandai graduated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-07 | Elevation of Privilege / Broken Access Control | CoachCoacheeMappingAssign cross-year | mitigate | Hard-block server-side berbasis penanda; drop `ConfirmProgressionWarning` escape (klien tak bisa override gate dengan flag). |
| T-359-08 | Broken Access Control | MarkMappingCompleted | mitigate | Gate `IsYearCompletedAsync(Tahun 3)` dipastikan satu-satunya jalur set IsCompleted; tidak ada pintu samping. |
| T-359-09 | Information Disclosure | catch DbUpdateException :641-651 | accept | Convention existing (Phase 334 D6) sudah generik message; plan tidak menambah catch baru yang membocorkan. |
| T-359-10 | Tampering (exempt side-door) | isExemptFromCrossYear flag | mitigate | Di Phase 359 exempt = false (hardcoded; bypass belum ada). Phase 360 yang mengisi kondisi exempt harus tetap menjaga gate eligibility 100% (didokumentasikan di komentar). |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests` hijau (tidak ada regresi).
- UAT lokal (CLAUDE.md, AD lokal `Authentication__UseActiveDirectory=false dotnet run`, localhost:5277):
  - Assign Tahun 2 untuk coachee yang Tahun 1 belum lulus → JSON success=false, pesan hard-block, TIDAK ada tombol "Tetap lanjutkan".
  - Assign Tahun 1 (tanpa prasyarat) → sukses.
  - Mark graduated untuk worker Tahun 3 belum lulus → banner Error S2; IsCompleted tidak di-set.
  - Mark graduated untuk worker Tahun 3 lulus (penanda ada) → sukses.
- Snapshot/restore DB lokal per SEED_WORKFLOW bila menyuntik skenario.
</verification>

<success_criteria>
- Assign Tahun N hard-blocked kalau Tahun N-1 belum lulus (penanda), tanpa escape.
- Graduation gate terbukti memblok Tahun 3 belum lulus dengan pesan S2.
- Titik exempt bypass/renewal disiapkan logis untuk Phase 360 tanpa membuka side-door di Phase 359.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/359-gate-berurutan-cleanup-a/359-03-SUMMARY.md`.
</output>
