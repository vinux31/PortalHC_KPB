---
phase: 360-bypass-backend-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/ProtonModels.cs
  - Data/ApplicationDbContext.cs
  - Services/NotificationService.cs
  - Migrations/*_AddPendingProtonBypassAndAssignmentOrigin.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [PBYP-01, PBYP-03]
tags: [proton, bypass, migration, schema, database]
user_setup: []

must_haves:
  truths:
    - "Tabel PendingProtonBypasses ada di DB lokal dengan 12 kolom per spec §6 (TANPA kolom Mode)"
    - "Kolom Origin (nullable nvarchar(20)) ada di ProtonTrackAssignments; baris lama = null"
    - "Notif template PROTON_BYPASS_READY terdaftar di NotificationService._templates"
    - "dotnet build 0 error + dotnet test hijau setelah migration apply"
  artifacts:
    - path: "Models/ProtonModels.cs"
      provides: "class PendingProtonBypass + ProtonTrackAssignment.Origin"
      contains: "public class PendingProtonBypass"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet<PendingProtonBypass> + index config"
      contains: "DbSet<PendingProtonBypass> PendingProtonBypasses"
    - path: "Services/NotificationService.cs"
      provides: "template PROTON_BYPASS_READY"
      contains: "PROTON_BYPASS_READY"
    - path: "Migrations/ApplicationDbContextModelSnapshot.cs"
      provides: "snapshot updated dengan PendingProtonBypass + Origin"
      contains: "PendingProtonBypass"
  key_links:
    - from: "Models/ProtonModels.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "DbSet registration"
      pattern: "DbSet<PendingProtonBypass>"
    - from: "Migration Up()"
      to: "DB lokal HcPortalDB_Dev"
      via: "dotnet ef database update"
      pattern: "CreateTable.*PendingProtonBypasses"
---

<objective>
Fondasi skema Phase 360 Bypass Backend: tabel baru `PendingProtonBypass` (migration#2, spec §6 — 12 kolom verbatim), kolom `Origin` di `ProtonTrackAssignment` (D-04, exempt stempel), registrasi DbSet + index, dan template notif `PROTON_BYPASS_READY`. Migration #2 melebar 2 tabel dalam 1 file (D-04).

Purpose: Semua plan berikutnya (service, hook, endpoint) butuh entity + DbSet + kolom Origin + template ini sebagai kontrak. Tanpa migration ter-apply ke DB lokal, build/test pass tapi runtime gagal (false-positive) — maka apply migration WAJIB di plan ini.
Output: Model + DbSet + index + notif template + file migration ter-commit + DB lokal ter-apply + snapshot DB dicatat di SEED_JOURNAL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@.planning/phases/360-bypass-backend-b/360-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md
@docs/SEED_WORKFLOW.md
@docs/DEV_WORKFLOW.md

<interfaces>
<!-- Kontrak yang dibuat plan ini, dikonsumsi plan 02-08. Executor pakai langsung. -->

PendingProtonBypass (spec §6, 12 kolom — VERBATIM, TANPA kolom Mode) — taruh di Models/ProtonModels.cs setelah ProtonFinalAssessment (:229):
```csharp
public class PendingProtonBypass
{
    public int Id { get; set; }
    public string CoacheeId { get; set; } = "";            // worker, no FK constraint
    public int SourceProtonTrackId { get; set; }           // track asal (ditutup)
    public int TargetProtonTrackId { get; set; }           // track tujuan
    public string TargetUnit { get; set; } = "";           // unit tujuan dari form (E7)
    public string? TargetCoachId { get; set; }             // coach tujuan; null = pertahankan (M-5/D-16)
    public string Reason { get; set; } = "";               // alasan wajib
    public int LinkedAssessmentSessionId { get; set; }     // exam source-year yang dipantau
    public string Status { get; set; } = "Menunggu";       // Menunggu | Siap | Selesai | Dibatalkan
    public string InitiatedById { get; set; } = "";        // HC/admin inisiator (tujuan notif)
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime? ResolvedAt { get; set; }              // saat Selesai/Dibatalkan
}
```
> **W-05 (review): kolom `Mode` DI-DROP.** Spec §6 hanya 12 kolom (Id..ResolvedAt). Pending HANYA pernah dibuat oleh jalur CL-B(b) (plan 04 dispatch) — CL-A/B(a)/C eksekusi instan dan tak pernah masuk tabel. Maka Mode akan selalu "CL-B(b)" → redundan/dead-generality. Ikuti spec verbatim 12 kolom. (Jalur dispatch tetap pakai `BypassRequest.Mode` di memori; itu TIDAK dipersistensikan ke tabel.)

ProtonTrackAssignment.Origin — existing class Models/ProtonModels.cs:71, tambah setelah DeactivatedAt (:83):
```csharp
[MaxLength(20)]
public string? Origin { get; set; }   // null = Normal (tidak exempt); "Bypass" = stempel permanen (D-04)
```

Template notif — Services/NotificationService.cs, deep-link ke Tab2 (Phase 361):
ActionUrlTemplate = "/ProtonData/Override?tab=bypass&pending={PendingId}"
</interfaces>

<!-- CATATAN SCHEMA DRIFT (B-05, untuk IT-notify SUMMARY) -->
<!-- AssessmentSessions.AssessmentType: DB kolom is_nullable=0 (NOT NULL, verified sqlcmd HcPortalDB_Dev), -->
<!-- TAPI CLR model Models/AssessmentSession.cs:155 `public string? AssessmentType` + EF snapshot tanpa IsRequired. -->
<!-- Drift ini SUDAH ada (bukan diciptakan Phase 360); plan 04 bare-session WAJIB set AssessmentType="Standard" eksplisit -->
<!-- karena EF tidak pakai DB default. Catat di SUMMARY plan 01 sebagai temuan IT-notify (bukan diperbaiki di phase ini). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model PendingProtonBypass + ProtonTrackAssignment.Origin + DbSet + index + notif template</name>
  <files>Models/ProtonModels.cs, Data/ApplicationDbContext.cs, Services/NotificationService.cs</files>
  <read_first>
    - Models/ProtonModels.cs (lihat ProtonTrackAssignment :71-84 dan ProtonFinalAssessment :207-229 sebagai analog konvensi: `string ... = ""`, `DateTime ... = DateTime.UtcNow`, FK string tanpa constraint, `[MaxLength(20)]` pada Origin)
    - Data/ApplicationDbContext.cs (DbSet block :30-43 untuk gaya registrasi; cari method OnModelCreating + region index/filtered-unique CoachCoacheeMapping ~:322-330 sebagai analog HasIndex)
    - Services/NotificationService.cs (_templates dict :34-93, lihat ASMT_RESULTS_READY :43-48 sebagai pola NotificationTemplate)
  </read_first>
  <action>
    1. Di `Models/ProtonModels.cs`, dalam class `ProtonTrackAssignment` (mulai :71), tambah SETELAH `DeactivatedAt` (:83):
    ```csharp
    /// <summary>Phase 360 (D-04) — penanda exempt cross-year: null = Normal (tidak exempt), "Bypass" = stempel permanen. Nilai hanya {null, "Bypass"}; baris lama = null, TANPA backfill.</summary>
    [MaxLength(20)]
    public string? Origin { get; set; }
    ```
    2. Di `Models/ProtonModels.cs`, tambah class baru `PendingProtonBypass` SETELAH `ProtonFinalAssessment` (:229) — salin verbatim dari blok `<interfaces>` di atas (12 kolom spec §6, TANPA kolom Mode). Ikuti konvensi file: string FK no-constraint, `DateTime CreatedAt = DateTime.UtcNow`, `ResolvedAt` nullable. Tambahkan XML doc `/// <summary>Phase 360 (PBYP-01) — rencana bypass tahun tertunda. Lifecycle Status: Menunggu→Siap→Selesai|Dibatalkan. 12 kolom spec §6 (tanpa Mode — pending hanya untuk CL-B(b)).</summary>` di atas class.
    3. Di `Data/ApplicationDbContext.cs`, tambah DbSet setelah baris `ProtonFinalAssessments` (:43):
    ```csharp
    // Proton Bypass Tahun (Phase 360 — PBYP-01)
    public DbSet<PendingProtonBypass> PendingProtonBypasses { get; set; }
    ```
    4. Di `Data/ApplicationDbContext.cs` di dalam `OnModelCreating`, tambah index NON-UNIQUE untuk query D-10 (blok dobel pending) — JANGAN filtered-unique (blok dobel pending = app-level, bukan DB constraint):
    ```csharp
    builder.Entity<PendingProtonBypass>(entity =>
    {
        entity.HasIndex(p => new { p.CoacheeId, p.Status })
            .HasDatabaseName("IX_PendingProtonBypasses_CoacheeId_Status");
    });
    ```
    5. Di `Services/NotificationService.cs`, tambah entry ke `_templates` dict (setelah ASMT_RESULTS_READY :48):
    ```csharp
    ["PROTON_BYPASS_READY"] = new NotificationTemplate
    {
        Title = "Bypass Siap Diselesaikan",
        MessageTemplate = "Exam bypass {WorkerName} lulus — siap dikonfirmasi pindah tahun.",
        ActionUrlTemplate = "/ProtonData/Override?tab=bypass&pending={PendingId}"
    },
    ```
    JANGAN ubah template lain. JANGAN buat migration di task ini (task 2). JANGAN tambah kolom `Mode` ke model (W-05 — di-drop).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public class PendingProtonBypass" Models/ProtonModels.cs` → 1 match
    - `grep -c "SourceProtonTrackId\|TargetProtonTrackId\|LinkedAssessmentSessionId\|InitiatedById\|TargetUnit\|TargetCoachId\|ResolvedAt" Models/ProtonModels.cs` → ≥7 (kolom spec §6 ada)
    - `grep -cF "public string Mode" Models/ProtonModels.cs` → 0 (W-05: kolom Mode TIDAK ada di model)
    - `grep -n "public string? Origin" Models/ProtonModels.cs` → match di dalam ProtonTrackAssignment (DUA Origin sekarang: ProtonFinalAssessment :226 dan ProtonTrackAssignment baru)
    - `grep -n "DbSet<PendingProtonBypass> PendingProtonBypasses" Data/ApplicationDbContext.cs` → 1 match
    - `grep -n "IX_PendingProtonBypasses_CoacheeId_Status" Data/ApplicationDbContext.cs` → 1 match (index non-unique, TANPA .IsUnique())
    - `grep -n "PROTON_BYPASS_READY" Services/NotificationService.cs` → 1 match dengan ActionUrlTemplate berisi `tab=bypass&pending={PendingId}`
    - `dotnet build` → "Build succeeded", 0 error
  </acceptance_criteria>
  <done>Model PendingProtonBypass (12 kolom, tanpa Mode) + ProtonTrackAssignment.Origin + DbSet + index + notif template ada; build 0 error. Migration belum dibuat (task 2).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING MIGRATION] Snapshot DB + generate migration#2 + apply + verify snapshot</name>
  <files>Migrations/*_AddPendingProtonBypassAndAssignmentOrigin.cs, Migrations/ApplicationDbContextModelSnapshot.cs, docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_WORKFLOW.md (SOP snapshot/restore: `sqlcmd ... BACKUP DATABASE`, format journal)
    - docs/DEV_WORKFLOW.md (SOP migration: file WAJIB commit, notify IT flag migration)
    - Migrations/20260610014907_AddOriginToProtonFinalAssessment.cs (analog migration#1 — pola AddColumn nvarchar(20) nullable; CATATAN: migration#1 PUNYA Sql("UPDATE ... SET Origin='Interview'") seed — Phase 360 TIDAK boleh ada seed UPDATE, D-04 biarkan null)
  </read_first>
  <action>
    Urutan WAJIB (SEED_WORKFLOW + DEV_WORKFLOW):
    1. SNAPSHOT DB lokal DULU (sebelum apply migration):
    ```powershell
    sqlcmd -S "localhost\SQLEXPRESS" -E -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev_pre360migration_20260610.bak' WITH INIT, COMPRESSION"
    ```
    2. Catat di `docs/SEED_JOURNAL.md` entry baru: tujuan="snapshot pre migration#2 Phase 360 (PendingProtonBypass + ProtonTrackAssignment.Origin)", klasifikasi="schema migration (bukan seed data)", file backup di atas, status="active".
    3. Generate migration:
    ```powershell
    dotnet ef migrations add AddPendingProtonBypassAndAssignmentOrigin --context ApplicationDbContext
    ```
    4. BUKA file migration yang dihasilkan (`Migrations/*_AddPendingProtonBypassAndAssignmentOrigin.cs`). VERIFIKASI manual `Up()` berisi: (a) `AddColumn<string>(name: "Origin", table: "ProtonTrackAssignments", type: "nvarchar(20)", maxLength: 20, nullable: true)` DAN (b) `CreateTable(name: "PendingProtonBypasses", ...)` dengan 12 kolom (TANPA kolom Mode — W-05). PASTIKAN TIDAK ADA `migrationBuilder.Sql("UPDATE ...")` (D-04 — baris lama Origin biar null, BEDA dari migration#1). Kalau ada baris Sql UPDATE, hapus.
    5. Apply ke DB lokal:
    ```powershell
    dotnet ef database update --context ApplicationDbContext
    ```
    6. Verifikasi `Migrations/ApplicationDbContextModelSnapshot.cs` ter-update (cegah drift, Pitfall 6) — harus mengandung `PendingProtonBypass` dan `Origin` di ProtonTrackAssignment.
    7. Verifikasi DB nyata:
    ```powershell
    sqlcmd -S "localhost\SQLEXPRESS" -E -d HcPortalDB_Dev -Q "SELECT COUNT(*) AS Cols FROM sys.columns WHERE object_id=OBJECT_ID('PendingProtonBypasses'); SELECT name FROM sys.columns WHERE object_id=OBJECT_ID('ProtonTrackAssignments') AND name='Origin';"
    ```
    File migration + snapshot WAJIB ter-commit (DEV_WORKFLOW). Notify IT (di SUMMARY): "migration#2 = tabel PendingProtonBypass (12 kolom) + kolom ProtonTrackAssignment.Origin; pure schema (Origin nullable tanpa backfill)".
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - File `Migrations/*_AddPendingProtonBypassAndAssignmentOrigin.cs` ada (Glob match)
    - `grep -n "CreateTable" Migrations/*_AddPendingProtonBypassAndAssignmentOrigin.cs` mengandung table "PendingProtonBypasses"
    - `grep -n "AddColumn.*Origin.*ProtonTrackAssignments" Migrations/*_AddPendingProtonBypassAndAssignmentOrigin.cs` → 1 match
    - `grep -cF "migrationBuilder.Sql" Migrations/*_AddPendingProtonBypassAndAssignmentOrigin.cs` → 0 (TIDAK ada seed UPDATE, D-04)
    - `grep -n "PendingProtonBypass" Migrations/ApplicationDbContextModelSnapshot.cs` → ≥1 (snapshot updated, no drift)
    - sqlcmd query mengembalikan Cols == 12 untuk PendingProtonBypasses (spec §6, tanpa Mode) DAN ada kolom Origin di ProtonTrackAssignments
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau (existing test tak regresi)
    - Entry SEED_JOURNAL.md status="active" dengan path backup
  </acceptance_criteria>
  <done>Migration#2 ter-generate (2 tabel, 12 kolom pending tanpa Mode, tanpa seed UPDATE) + ter-apply ke DB lokal + snapshot ModelSnapshot updated + DB nyata punya tabel + kolom. Snapshot DB dicatat. Build + unit test hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF migration → DB lokal | Operasi schema langsung ke HcPortalDB_Dev; salah migration = data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-01 | Tampering | Migration Up() seed | mitigate | Verifikasi manual: TIDAK ada `Sql("UPDATE ...")` (D-04). Origin nullable tanpa backfill — pure additive schema |
| T-360-02 | Denial of Service | DB lokal corrupt saat apply | mitigate | Snapshot `BACKUP DATABASE` SEBELUM `database update` (SEED_WORKFLOW); restorable bila gagal |
| T-360-03 | Repudiation | Schema drift ModelSnapshot | mitigate | Verifikasi `ApplicationDbContextModelSnapshot.cs` ter-update; cegah migration berikutnya diff tak terduga |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet ef migrations list` menampilkan `AddPendingProtonBypassAndAssignmentOrigin` sebagai Applied.
- DB lokal: tabel `PendingProtonBypasses` (12 kolom, tanpa Mode) + kolom `ProtonTrackAssignments.Origin` ada.
- `dotnet test --filter "Category!=Integration"` hijau (no regresi).
- Snapshot DB tercatat SEED_JOURNAL.md.
</verification>

<success_criteria>
- Entity `PendingProtonBypass` (12 kolom) + `ProtonTrackAssignment.Origin` + DbSet + index + template `PROTON_BYPASS_READY` ada di kode.
- Migration#2 (2 tabel, tanpa seed UPDATE) ter-apply ke DB lokal; ModelSnapshot updated.
- Build + unit test hijau; snapshot DB dicatat.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-01-SUMMARY.md` (pakai template). WAJIB sertakan flag IT notify:
1. "migration#2 = PendingProtonBypass (12 kolom, tanpa Mode) + ProtonTrackAssignment.Origin (pure schema, nullable tanpa backfill)".
2. **SCHEMA DRIFT (B-05):** `AssessmentSessions.AssessmentType` DB NOT NULL (is_nullable=0) tapi CLR model `string? AssessmentType` (Models/AssessmentSession.cs:155) + EF snapshot tanpa IsRequired. Drift PRE-EXISTING (bukan dibuat Phase 360). Plan 04 bare-session set `AssessmentType="Standard"` eksplisit sebagai mitigasi. IT perlu tahu: insert AssessmentSession TANPA set AssessmentType eksplisit akan throw SqlException 515 di DB nyata walau EF model membolehkan null.
</output>
</content>
</invoke>
