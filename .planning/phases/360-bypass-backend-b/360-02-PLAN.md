---
phase: 360-bypass-backend-b
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Helpers/ProtonDeliverableBootstrap.cs
  - Controllers/CoachMappingController.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PBYP-02, PBYP-05]
tags: [proton, bypass, gate, bootstrap, deliverable]
user_setup: []

must_haves:
  truths:
    - "Bootstrap deliverable bisa pakai unit eksplisit dari form (bukan dari active mapping)"
    - "Assignment dengan Origin=='Bypass' lewat gate cross-year di CreateAssessment"
    - "Assignment dengan Origin=='Bypass' lewat exempt cross-year di CoachMapping re-assign"
    - "Gate deliverable 100% target-year TETAP berlaku (tidak ikut di-exempt — D-05)"
  artifacts:
    - path: "Helpers/ProtonDeliverableBootstrap.cs"
      provides: "static CreateProgressAsync(ctx, assignmentId, trackId, coacheeId, resolvedUnit)"
      contains: "resolvedUnit"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "exempt isBypassAssignment di gate :1368"
      contains: "isBypassAssignment"
    - path: "Controllers/CoachMappingController.cs"
      provides: "isExemptFromCrossYear terisi (bukan hardcode false)"
      contains: "Origin == \"Bypass\""
  key_links:
    - from: "CoachMappingController.AutoCreateProgressForAssignment"
      to: "ProtonDeliverableBootstrap.CreateProgressAsync"
      via: "delegasi (no duplicate filter unit)"
      pattern: "ProtonDeliverableBootstrap.CreateProgressAsync"
    - from: "AssessmentAdminController gate :1368"
      to: "ProtonTrackAssignments.Origin"
      via: "AnyAsync Origin=='Bypass'"
      pattern: "Origin == \"Bypass\""
---

<objective>
Siapkan dua dependency pondasi yang dipakai service bypass (plan 03+): (1) ekstrak logic bootstrap deliverable ke helper static yang **terima unit eksplisit** (Pitfall 3 — `AutoCreateProgressForAssignment` saat ini resolve unit dari active mapping, langgar PBYP-05/D-05 yang minta unit dari FORM), dan (2) isi 2 titik exempt gate cross-year (D-06) supaya assignment ber-`Origin="Bypass"` lewat prereq Tahun N-1 — tapi gate deliverable 100% target-year TETAP (D-05).

Purpose: Service bypass (plan 03) memanggil helper bootstrap dengan unit-from-form; tanpa parametrisasi, worker bypass ganti unit dapat deliverable unit LAMA → gate eligibility tak pernah 100% (Pitfall 3). Exempt = mekanisme yang bikin assignment bypass tak keblok gate berurutan Phase 359.
Output: helper baru + refactor CoachMappingController (delegasi, no duplicate filter) + 2 titik exempt terisi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@.planning/phases/360-bypass-backend-b/360-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md

<interfaces>
<!-- Kontrak helper yang dibuat plan ini, dikonsumsi plan 03 (service) + CoachMappingController -->

Helpers/ProtonDeliverableBootstrap.cs (BARU):
```csharp
public static class ProtonDeliverableBootstrap
{
    /// Bootstrap ProtonDeliverableProgress (Status="Pending") + initial DeliverableStatusHistory
    /// untuk assignment, FILTER unit-deliverable pakai resolvedUnit EKSPLISIT (.Trim() 2-sisi).
    /// EXCLUDE deliverableIds yang SUDAH punya progress untuk coachee (B-06 guard anti-dobel).
    /// Return list warning (unit kosong / track 0 deliverable). Caller WAJIB di dalam transaksi.
    public static async Task<List<string>> CreateProgressAsync(
        ApplicationDbContext context, int assignmentId, int protonTrackId,
        string coacheeId, string resolvedUnit);
}
```

Predikat exempt (di-inline di 2 controller, identik):
```csharp
bool isBypassAssignment = await _context.ProtonTrackAssignments
    .AnyAsync(a => a.CoacheeId == uid && a.ProtonTrackId == protonTrackId
                && a.IsActive && a.Origin == "Bypass");
```
</interfaces>

<!-- Analog yang diekstrak: CoachMappingController.AutoCreateProgressForAssignment :1424-1493 -->
<!-- Gate existing: AssessmentAdminController :1336-1392; exempt placeholder: CoachMappingController :530-541 -->
<!-- W-04/W-08/W-10 (review): unit-source mismatch D-16 × ganti-unit DITUTUP di plan 03 (D-16b: update mapping.AssignmentUnit=TargetUnit saat keep-coach+ganti-unit). Threat T-360-05 di bawah dikoreksi agar tak overstate. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ekstrak ProtonDeliverableBootstrap.CreateProgressAsync (unit eksplisit, anti-dobel B-06) + refactor CoachMappingController delegasi</name>
  <files>Helpers/ProtonDeliverableBootstrap.cs, Controllers/CoachMappingController.cs</files>
  <read_first>
    - Controllers/CoachMappingController.cs (method `AutoCreateProgressForAssignment` :1424-1493 — SUMBER ekstraksi: resolve unit :1428-1441, filter deliverable .Trim() 2-sisi :1449-1453, insert ProtonDeliverableProgress Status="Pending" :1465-1474, initial DeliverableStatusHistory ActorId="system" :1477-1490)
    - Controllers/AssessmentAdminController.cs (gate 100% :1380-1383 — filter unit `.Trim()` 2-sisi yang HARUS konsisten dengan helper, supaya gate eligibility & bootstrap pakai filter sama; gate count :1386-1390 menghitung progress per CoacheeId TANPA scope assignment — INI alasan B-06 guard wajib)
    - Helpers/CoacheeEligibilityCalculator.cs (:15-19 `if (myProgressStatuses.Count != expectedCount) return false` — count mismatch = permanen tak-eligible; B-06 guard mencegah ini)
    - Models/ProtonModels.cs (ProtonDeliverableProgress fields + DeliverableStatusHistory fields)
  </read_first>
  <action>
    1. Buat `Helpers/ProtonDeliverableBootstrap.cs` (namespace `HcPortal.Helpers`, `using HcPortal.Data; using HcPortal.Models; using Microsoft.EntityFrameworkCore;`). Implementasi `CreateProgressAsync(ApplicationDbContext context, int assignmentId, int protonTrackId, string coacheeId, string resolvedUnit)` — salin LOGIC dari `AutoCreateProgressForAssignment` :1449-1492 TAPI:
       - TERIMA `resolvedUnit` sebagai parameter (BUKAN resolve dari active mapping). Kalau `string.IsNullOrWhiteSpace(resolvedUnit)` → return `new List<string>{ $"resolvedUnit kosong untuk coachee {coacheeId} — progress tidak dibuat." }`.
       - Filter deliverable PERSIS sama (`.Trim()` 2-sisi):
       ```csharp
       var deliverableIds = await context.ProtonDeliverableList
           .Where(d => d.ProtonSubKompetensi!.ProtonKompetensi!.ProtonTrackId == protonTrackId
                    && d.ProtonSubKompetensi!.ProtonKompetensi!.Unit!.Trim() == resolvedUnit.Trim())
           .Select(d => d.Id).ToListAsync();
       ```
       - **B-06 GUARD ANTI-DOBEL (WAJIB):** SEBELUM insert, EXCLUDE deliverableIds yang SUDAH punya `ProtonDeliverableProgress` untuk coachee ini (track yang pernah dijalani, mis. CL-C turun ke unit yang sama). Tanpa guard ini, count progress per CoacheeId jadi 2N → `CoacheeEligibilityCalculator.IsEligiblePerUnit` (count != expectedCount) return false SELAMANYA → worker permanen tak-eligible (recovery cuma SQL manual). D-E tetap dihormati: assignment baru tetap fresh, hanya progress duplikat yang di-skip.
       ```csharp
       var existingDeliverableIds = await context.ProtonDeliverableProgresses
           .Where(p => p.CoacheeId == coacheeId)
           .Select(p => p.ProtonDeliverableId).ToListAsync();
       var toCreate = deliverableIds.Where(dId => !existingDeliverableIds.Contains(dId)).ToList();
       ```
       (Verifikasi nama DbSet `ProtonDeliverableProgresses` + property `ProtonDeliverableId` + `CoacheeId` saat read_first; sesuaikan bila beda.)
       - Insert HANYA `toCreate`: `ProtonDeliverableProgress { CoacheeId, ProtonDeliverableId=dId, ProtonTrackAssignmentId=assignmentId, Status="Pending", CreatedAt=DateTime.UtcNow }` + `SaveChangesAsync()` untuk dapat Id.
       - Insert initial `DeliverableStatusHistory { ProtonDeliverableProgressId=p.Id, StatusType="Pending", ActorId="system", ActorName="System", ActorRole="System", Timestamp=DateTime.UtcNow }` + `SaveChangesAsync()`.
       - Return list warning (track 0 deliverable / unit tak ada deliverable / SEMUA deliverable sudah ada progress → tambah warning "semua deliverable unit X sudah ter-bootstrap untuk coachee — skip duplikat") — pertahankan pesan analog :1461.
    2. Refactor `CoachMappingController.AutoCreateProgressForAssignment` (:1424-1493): PERTAHANKAN resolve unit existing (active mapping → fallback User.Unit :1428-1441), lalu DELEGASI ke helper:
       ```csharp
       // resolve unit (existing logic :1428-1441) → resolvedUnit
       return await ProtonDeliverableBootstrap.CreateProgressAsync(_context, assignmentId, protonTrackId, coacheeId, resolvedUnit ?? "");
       ```
       Hapus duplikasi filter + insert di controller (sekarang di helper). JANGAN ubah signature method controller (caller existing tak berubah).
    3. Pastikan tidak ada filter unit yang ter-duplikat antara controller dan helper (single source — Pitfall 3).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - File `Helpers/ProtonDeliverableBootstrap.cs` ada; `grep -n "public static async Task<List<string>> CreateProgressAsync" Helpers/ProtonDeliverableBootstrap.cs` → 1 match dengan parameter `string resolvedUnit`
    - `grep -n "Unit!.Trim() == resolvedUnit.Trim()" Helpers/ProtonDeliverableBootstrap.cs` → 1 match (filter unit .Trim() 2-sisi)
    - B-06 guard: `grep -n "existingDeliverableIds\|existing.*Progress" Helpers/ProtonDeliverableBootstrap.cs` → ≥1 (exclude deliverable yang sudah punya progress untuk coachee)
    - `grep -n "StatusType = \"Pending\"" Helpers/ProtonDeliverableBootstrap.cs` + `ActorId = "system"` → ada (initial StatusHistory dipertahankan)
    - `grep -n "ProtonDeliverableBootstrap.CreateProgressAsync" Controllers/CoachMappingController.cs` → 1 match (delegasi)
    - Tidak ada duplikasi filter di method controller (manual review: blok `AutoCreateProgressForAssignment` :1424-1493 sekarang hanya resolve-unit + delegasi, TANPA query `ProtonDeliverableList` — catatan: `ProtonDeliverableList` masih dipakai di method LAIN spt GetEligibleCoachees :1395, jadi grep file-level TIDAK valid; verifikasi scope method manual)
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau (assign coach existing tak regresi)
  </acceptance_criteria>
  <done>Helper bootstrap parametrik unit + guard anti-dobel (B-06) ada; CoachMappingController delegasi ke helper (no duplicate filter); build + unit test hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Isi 2 titik exempt gate cross-year (D-06) — gate 100% TETAP (D-05)</name>
  <files>Controllers/AssessmentAdminController.cs, Controllers/CoachMappingController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (gate :1336-1392 — titik (a) cross-year :1368, gate 100% :1373-1389 JANGAN diubah; variabel `protonTrackId` :1342, `uid` loop :1365, `isRenewal` :1362)
    - Controllers/CoachMappingController.cs (exempt placeholder :530-541 — `isExemptFromCrossYear` :533 hardcode false, di dalam loop `foreach coacheeId`; CABANG 1 :516-528 `hasForRequestedTrack` (assignment APAPUN, TANPA filter IsActive) + `continue` :528; variabel `requestedTrack` untuk TrackId, `prevTrack` untuk TahunKe)
    - .planning/phases/360-bypass-backend-b/360-RESEARCH.md (Code Examples section — predikat isBypassAssignment verbatim)
  </read_first>
  <action>
    Titik (a) — `AssessmentAdminController.cs` gate :1368. SEBELUM baris `if (!isRenewal && !await _protonCompletionService.IsPrevYearPassedAsync(...))`, tambah resolve isBypassAssignment lalu sisipkan ke kondisi:
    ```csharp
    // Phase 360 (D-06a/A-M4): assignment ber-Origin="Bypass" exempt cek cross-year prereq.
    bool isBypassAssignment = await _context.ProtonTrackAssignments
        .AnyAsync(a => a.CoacheeId == uid && a.ProtonTrackId == protonTrackId
                    && a.IsActive && a.Origin == "Bypass");
    if (!isRenewal && !isBypassAssignment
        && !await _protonCompletionService.IsPrevYearPassedAsync(uid, trackType, prevTahunKe))
    {
        gateSkippedPrevYear++; continue;
    }
    ```
    Gate deliverable 100% (:1373-1389) JANGAN disentuh sama sekali (D-05 — worker bypass tetap wajib selesaikan deliverable tahun TUJUAN).

    Titik (b) — `CoachMappingController.cs` :533 (D-06b, keputusan review #6).
    **KONTEKS PENTING (W-07/I-08 review):** CABANG 1 loop (:516-528) membangun `hasForRequestedTrack` dari assignment APAPUN untuk track yang diminta TANPA filter `IsActive`, lalu `if (hasForRequestedTrack.Contains(coacheeId)) continue;` (:528). Worker hasil bypass PASTI sudah punya assignment (Origin=Bypass) untuk track itu → SUDAH ter-skip di cabang 1 SEBELUM mencapai :533. Artinya intent D-06b ("re-assign normal pasca-bypass tidak keblok cross-year") secara de-facto SUDAH dipenuhi cabang 1.
    Tetap ganti placeholder `bool isExemptFromCrossYear = false;` menjadi predikat MEANINGFUL sesuai semantik spec (defense-in-depth, BUKAN dead-code yang dihapus) — exempt by `Origin=="Bypass"` pada assignment AKTIF coachee untuk track yang diminta:
    ```csharp
    // Phase 360 (D-06b): assignment AKTIF ber-Origin="Bypass" untuk track yang diminta → exempt cross-year.
    // CATATAN (W-07/I-08): cabang 1 (:516-528, hasForRequestedTrack TANPA filter IsActive) sudah men-skip
    // coachee bypass lebih dulu, jadi predikat ini bersifat DEFENSE-IN-DEPTH (kalau cabang 1 di-refactor /
    // suatu saat memfilter IsActive, exempt ini tetap menjaga semantik D-06b). Cabang 1 SENGAJA dipertahankan
    // apa adanya (tanpa filter IsActive) — JANGAN ubah cabang 1.
    bool isExemptFromCrossYear = await _context.ProtonTrackAssignments
        .AnyAsync(a => a.CoacheeId == coacheeId && a.ProtonTrackId == requestedTrack.Id
                    && a.IsActive && a.Origin == "Bypass");
    if (isExemptFromCrossYear) continue;
    ```
    (Verifikasi nama variabel track yang benar di scope :530-541 saat read_first — gunakan property `.Id` track yang diminta. Renewal exempt tetap session-based TERPISAH, jangan campur — D-07. Dokumentasikan di SUMMARY plan 02 bahwa predikat = defense-in-depth karena cabang 1 menutupi semua kasus reachable.)
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isBypassAssignment" Controllers/AssessmentAdminController.cs` → ≥2 match (deklarasi + dipakai di kondisi `!isBypassAssignment`)
    - `grep -n "a.Origin == \"Bypass\"" Controllers/AssessmentAdminController.cs` → 1 match
    - `grep -cF "isExemptFromCrossYear = false" Controllers/CoachMappingController.cs` → 0 (placeholder hardcode dihapus)
    - `grep -n "a.Origin == \"Bypass\"" Controllers/CoachMappingController.cs` → 1 match
    - Gate 100% utuh: `grep -n "CoacheeEligibilityCalculator.IsEligiblePerUnit" Controllers/AssessmentAdminController.cs` → masih ada (tidak terhapus, D-05)
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>2 titik exempt terisi cek Origin=="Bypass"; gate 100% utuh; predikat (b) didokumentasikan defense-in-depth (W-07); build + unit test hijau. Verifikasi runtime exempt = integration test Plan 08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate eligibility server-side | Exempt yang salah = worker tak-eligible lolos gate berurutan (privilege gate bypass) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-04 | Elevation of Privilege | gate exempt cross-year | mitigate | Exempt HANYA cek `Origin=="Bypass"` pada assignment AKTIF milik worker+track yang sama; bukan flag dari form/request. Gate 100% deliverable TETAP (D-05) — bukan blanket exempt |
| T-360-05 | Tampering | filter unit bootstrap vs gate (SUMBER unit) | mitigate | Filter `.Trim()` 2-sisi helper IDENTIK dengan gate 100% (`AssessmentAdminController:1382`) → konsistensi FILTER. **SUMBER unit** (gate resolve dari active mapping AssignmentUnit, bootstrap dari FORM) di-rekonsiliasi di plan 03 (D-16b): saat keep-coach (TargetCoachId null) + TargetUnit ≠ mapping.AssignmentUnit → UPDATE mapping.AssignmentUnit=TargetUnit dalam transaksi, sehingga gate & bootstrap resolve unit SAMA. Integration test gate keep-coach+ganti-unit (plan 03) mengunci kombinasi ini |
| T-360-06 | Tampering | regresi gate Phase 359 | accept | Gate cross-year + 100% existing tidak dihapus, hanya tambah kondisi `||`; integration test Plan 08 verifikasi eligibility 100% tetap jalan |
</threat_model>

<verification>
- `dotnet build` 0 error; `dotnet test --filter "Category!=Integration"` hijau (assign coach existing tak regresi).
- Filter unit helper = filter gate 100% (`.Trim()` 2-sisi) — konsisten.
- Gate 100% deliverable + cross-year existing utuh (cuma tambah `|| isBypassAssignment` / exempt-check).
- Guard anti-dobel bootstrap (B-06) mencegah count mismatch eligibility.
</verification>

<success_criteria>
- `ProtonDeliverableBootstrap.CreateProgressAsync` terima unit eksplisit + guard anti-dobel (B-06); CoachMappingController delegasi (no duplicate filter).
- 2 titik exempt cek `Origin=="Bypass"`; gate deliverable 100% TETAP berlaku.
- Build + unit test hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-02-SUMMARY.md`. Catat: (1) keputusan ekstraksi (helper static `Helpers/ProtonDeliverableBootstrap.cs`) + guard anti-dobel B-06 untuk konsumsi service plan 03; (2) predikat exempt (b) di CoachMapping = defense-in-depth karena cabang 1 (:516-528) sudah menutup kasus reachable (W-07/I-08).
</output>
</content>
