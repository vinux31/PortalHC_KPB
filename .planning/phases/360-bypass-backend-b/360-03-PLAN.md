---
phase: 360-bypass-backend-b
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - Services/ProtonBypassService.cs
  - Program.cs
  - HcPortal.Tests/ProtonBypassValidationTests.cs
  - HcPortal.Tests/ProtonBypassServiceTests.cs
autonomous: true
requirements: [PBYP-02, PBYP-04, PBYP-05]
tags: [proton, bypass, service, validation, closure-mode]
user_setup: []

must_haves:
  truths:
    - "Validasi bypass tolak: alasan kosong, T==track aktif S, |Δtahun|>1, ≠1 assignment aktif, CL-A tanpa S komplit (allApproved+final), CL-B saat final S sudah ada"
    - "CL-A/B(a)/C eksekusi instan 1 transaksi: tutup tahun asal → deactivate source → activate target → bootstrap unit-form → coach → audit"
    - "Penanda source terbit SEBELUM source assignment di-deactivate (Pitfall 1)"
    - "Coach: TargetCoachId diisi → deactivate mapping aktif lama DULU → create baru (E15); null → pertahankan (D-16); null+ganti-unit → update mapping.AssignmentUnit (D-16b)"
  artifacts:
    - path: "Services/ProtonBypassService.cs"
      provides: "skeleton + ValidateBypass + ExecuteInstantBypassAsync"
      min_lines: 120
      contains: "ExecuteInstantBypassAsync"
    - path: "Program.cs"
      provides: "AddScoped<ProtonBypassService>"
      contains: "AddScoped<HcPortal.Services.ProtonBypassService>"
    - path: "HcPortal.Tests/ProtonBypassValidationTests.cs"
      provides: "unit tests validasi pure"
      contains: "[Fact]"
  key_links:
    - from: "ProtonBypassService.ExecuteInstantBypassAsync"
      to: "ProtonCompletionService.EnsureAsync"
      via: "penanda Origin=Bypass SEBELUM deactivate"
      pattern: "EnsureAsync.*Bypass"
    - from: "ProtonBypassService.ExecuteInstantBypassAsync"
      to: "ProtonDeliverableBootstrap.CreateProgressAsync"
      via: "bootstrap unit dari form"
      pattern: "ProtonDeliverableBootstrap.CreateProgressAsync"
---

<objective>
Bangun inti `ProtonBypassService` (D-08): skeleton + DI + validasi pure (§5 VALIDASI) + eksekusi instan §5.1 untuk 3 mode INSTAN (CL-A Lulus, CL-B(a) Input manual, CL-C Tinggalkan). Jalur CL-B(b) pending + confirm + cancel = plan 04/05.

Purpose: §5.1 adalah operasi pindah-instan all-or-nothing yang mengorkestrasi service existing dalam URUTAN benar (Pitfall 1: penanda SEBELUM deactivate; Pitfall 3: bootstrap unit-from-form; Pitfall 5: coach deactivate-dulu). Semua step menyalin pola existing — bahaya = urutan, bukan logic baru.
Output: service + DI + validasi ber-test + §5.1 ber-integration-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@.planning/phases/360-bypass-backend-b/360-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md
@Services/ProtonCompletionService.cs
@Helpers/ProtonDeliverableBootstrap.cs

<interfaces>
<!-- Service yang dibangun plan ini; method pending/confirm/cancel ditambah plan 04/05 -->

Services/ProtonBypassService.cs (BARU):
```csharp
public record BypassRequest(
    string CoacheeId, int SourceProtonTrackId, int TargetProtonTrackId,
    string TargetUnit, string? TargetCoachId, string Reason, string Mode,  // "CL-A"|"CL-B(a)"|"CL-B(b)"|"CL-C"
    int? DurationMinutes, string InitiatedById);

public record BypassResult(bool Success, string Message, int? PendingId = null, bool ShowAttachPackageReminder = false);

public class ProtonBypassService
{
    // ctor inject: ApplicationDbContext, ProtonCompletionService, INotificationService, AuditLogService, ILogger<ProtonBypassService>
    // JANGAN inject GradingService (circular — Open Q3)

    // Plan 03:
    public Task<BypassResult> ExecuteInstantBypassAsync(BypassRequest req);   // §5.1 CL-A/B(a)/C
    // (validasi: method static/instance ValidateBypass dipanggil di awal)

    // Plan 04 (placeholder, jangan implement di sini):
    // public Task<BypassResult> BypassSaveAsync(BypassRequest req);
    // public Task<bool> MarkPendingReadyIfAnyAsync(int sessionId);
    // public Task RevertPendingToMenungguAsync(int sessionId);
    // Plan 05:
    // public Task<BypassResult> ConfirmBypassAsync(int pendingId, string hcId, string hcName);
    // public Task<BypassResult> CancelPendingAsync(int pendingId, string hcId, string hcName);
}
```

Reuse (dari 358/Plan 02):
- ProtonCompletionService.EnsureAsync(coacheeId, protonTrackId, createdById, origin, notes) → bool. ⚠️ resolve assignment WHERE IsActive → panggil SEBELUM deactivate source (Pitfall 1).
- ProtonDeliverableBootstrap.CreateProgressAsync(ctx, assignmentId, protonTrackId, coacheeId, resolvedUnit) → List<string> warnings (sudah ber-guard anti-dobel B-06 dari plan 02).
- AuditLogService.LogAsync(actorUserId, actorName, actionType, description, targetId?, targetType?).
</interfaces>

<!-- Analog transaksi: CoachMappingController.cs:565-646 (BeginTransactionAsync + catch DbUpdateException 2601/2627 + generic catch tanpa ex.Message) -->
<!-- Analog filtered-unique E15 + catch: ApplicationDbContext.cs:322-330 (index IX_CoachCoacheeMappings_CoacheeId_ActiveUnique :328); CoachMappingController.cs:628-639 (catch 2601/2627). -->
<!-- I-05 KOREKSI ANCHOR (review): TIDAK ada path existing "deactivate old active coach mapping then create new" di :573-619 — itu logic ProtonTrackAssignment (bukan CoachCoacheeMapping). -->
<!--   CoachCoacheeMapping creation analog = :555-568 (AddRange, set StartDate+AssignmentSection+AssignmentUnit :559-562); dup-coach-aktif DITOLAK pre-tx :480-493 (BUKAN deactivate-then-create). -->
<!--   Maka "coach deactivate-lama→create-baru" di service ini = KODE BARU (E15 swap), pakai field-set analog :555-568 + catch analog :628-639. -->
<!-- Analog skeleton service: Services/ProtonCompletionService.cs (full) -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validasi pure ValidateBypass + unit tests (RED→GREEN)</name>
  <files>Services/ProtonBypassService.cs, HcPortal.Tests/ProtonBypassValidationTests.cs</files>
  <read_first>
    - docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md (§5 VALIDASI: alasan wajib, T≠track aktif S (E14), |Δtahun|≤1 (D-B), tepat 1 assignment aktif (E8), CL-B → final S BELUM ada (D-D), CL-A → S komplit allApproved+final)
    - HcPortal.Tests/ProtonCompletionServiceTests.cs (pola [Fact]/[Theory] + struktur test file; lihat juga ProtonYearGateTests untuk pola static-predicate pure-unit no-DB)
  </read_first>
  <behavior>
    - Test: alasan whitespace → invalid "Alasan wajib diisi"
    - Test: targetTrackId == active source trackId → invalid "Target sama dengan track aktif" (E14)
    - Test: |tahunTarget - tahunSource| == 2 → invalid "Lompat tahun maksimal 1 langkah" (D-B); == 1 → valid; == 0 dengan track beda type → valid (turun/koreksi)
    - Test: mode bukan salah satu {CL-A, CL-B(a), CL-B(b), CL-C} → invalid
    - Test (B-03): mode CL-A + SourceComplete=false → invalid "Tahun asal belum komplit, gunakan CL-B"
    - Test (B-03 BARU): mode CL-A + SourceComplete=true + SourceHasFinal=false → invalid "Penanda Lulus tahun asal belum terbit, gunakan CL-B(a)" (CL-A WAJIB allApproved DAN final ada — spec §4/§5)
    - Test (B-03): mode CL-A + SourceComplete=true + SourceHasFinal=true → valid
    - Test: mode CL-B(a)/CL-B(b) + SourceHasFinal=true → invalid "Final tahun asal sudah ada" (D-D)
    - Test: input valid lengkap → valid
  </behavior>
  <action>
    Buat predikat pure yang TIDAK sentuh DB (input = nilai sudah di-resolve caller). Taruh sebagai static method/class `BypassValidator` di `Services/ProtonBypassService.cs` (atau file partial), signature mis.:
    ```csharp
    public record BypassValidationInput(string Reason, int ActiveSourceTrackId, int TargetTrackId,
        int SourceTahun, int TargetTahun, string Mode, bool SourceComplete, bool SourceHasFinal);
    public static (bool Valid, string Message) Validate(BypassValidationInput v);
    ```
    Aturan persis §5 (lihat behavior). |Δtahun| = `Math.Abs(SourceTahun - TargetTahun) <= 1`. Mode valid = `new[]{"CL-A","CL-B(a)","CL-B(b)","CL-C"}.Contains(Mode)`.
    **B-03 (review — CL-A WAJIB allApproved DAN final ada):** spec §5 "mode CL-A → S komplit (allApproved+final), else tolak" + §4 tabel CL-A "allApproved + final ada". Maka:
      - `CL-A → SourceComplete == true` (allApproved) DAN `SourceHasFinal == true` (penanda Lulus terbit). Jika `SourceComplete && !SourceHasFinal` → tolak + arahkan CL-B(a) (worker deliverable 100% tapi penanda belum terbit = kasus yang spec arahkan ke CL-B(a), supaya penanda terbit Origin="Bypass"). Tanpa aturan ini, CL-A lolos untuk worker tanpa penanda → step §5.1 CL-A "[tutup tahun asal] no-op" jadi salah premis → tahun asal selamanya tak tercatat "Lulus" → merusak histori + graduation chain.
    `CL-B(a)/CL-B(b) → SourceHasFinal wajib false` (D-D). Cek "tepat 1 assignment aktif" (E8) di-handle di method async ExecuteInstant DAN BypassSaveAsync (butuh DB), BUKAN di predikat pure — dokumentasikan di komentar (lihat B-04 di task 2).
    Buat `HcPortal.Tests/ProtonBypassValidationTests.cs` dengan semua [Fact]/[Theory] dari behavior. Tulis test DULU (RED), lalu implement predikat sampai GREEN.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ProtonBypassValidation" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "[Fact]" HcPortal.Tests/ProtonBypassValidationTests.cs` + `grep -cF "[Theory]" HcPortal.Tests/ProtonBypassValidationTests.cs` → total ≥8 (W-14: fixed-string grep)
    - `grep -n "public static (bool Valid, string Message) Validate" Services/ProtonBypassService.cs` → 1 match
    - B-03: ada [Fact] yang assert `CL-A + SourceHasFinal=false → invalid` (cek nama test mengandung "HasFinal" / "CLA": `dotnet test --filter "FullyQualifiedName~ProtonBypassValidation"` count meningkat dari baseline 7 → ≥8)
    - `dotnet test --filter "FullyQualifiedName~ProtonBypassValidation"` → semua Passed (≥8 test)
    - Predikat TIDAK referensikan `_context`/DbContext (pure) — manual review: blok/class `BypassValidator` TIDAK punya parameter atau field DbContext, semua input via `BypassValidationInput` record (grep file-level `_context` TIDAK valid karena ExecuteInstant pakai `_context`; verifikasi scope class BypassValidator manual)
  </acceptance_criteria>
  <done>Validasi pure §5 ber-test hijau (RED→GREEN), termasuk B-03 (CL-A wajib allApproved+final). Cek 1-assignment-aktif (DB) diserahkan ke ExecuteInstant + BypassSave (task 2 / plan 04).</done>
</task>

<task type="auto">
  <name>Task 2: Service skeleton + DI + ExecuteInstantBypassAsync (§5.1 CL-A/B(a)/C) + E8 di resolve</name>
  <files>Services/ProtonBypassService.cs, Program.cs</files>
  <read_first>
    - Services/ProtonCompletionService.cs (skeleton class scoped + ctor inject + EnsureAsync :36 + RemoveExamOriginAsync :70 — pola)
    - Controllers/CoachMappingController.cs (:565-646 transaksi BeginTransactionAsync + catch DbUpdateException 2601/2627 + generic catch tanpa ex.Message; :555-568 create CoachCoacheeMapping set StartDate+AssignmentSection+AssignmentUnit; :480-493 dup-coach-aktif DITOLAK pre-tx — lihat I-05 koreksi anchor di context)
    - Data/ApplicationDbContext.cs (:322-330 filtered-unique CoachCoacheeMapping CoacheeId WHERE IsActive=1)
    - Controllers/ProtonDataController.cs (:1443-1465 OverrideSave force-approve deliverable: progress.Status="Approved", ApprovedAt, ApprovedById, HCApprovalStatus pola) untuk CL-B(a) force-approve
    - Models/CoachCoacheeMapping.cs (fields: CoacheeId, CoachId, AssignmentUnit, AssignmentSection :39, StartDate :29 non-nullable, IsActive) + ProtonModels.cs ProtonTrackAssignment (Origin, AssignedById, IsActive, DeactivatedAt)
    - Models/AssessmentSession.cs (:20 Status hanya {"Open","Upcoming","Completed"} — dasar keputusan D-14 set "Dibatalkan")
    - Program.cs (:56-57 AddScoped ProtonCompletionService — pola registrasi)
  </read_first>
  <action>
    1. `Program.cs`: tambah `builder.Services.AddScoped<HcPortal.Services.ProtonBypassService>();` sebaris setelah ProtonCompletionService (:57).
    2. `Services/ProtonBypassService.cs`: class scoped, ctor inject `ApplicationDbContext context, ProtonCompletionService protonCompletionService, INotificationService notificationService, AuditLogService auditLog, ILogger<ProtonBypassService> logger`. JANGAN inject GradingService (circular, Open Q3). Tambah record `BypassRequest` + `BypassResult` (lihat interfaces).
    3. Implement `ExecuteInstantBypassAsync(BypassRequest req)` — §5.1, 1 transaksi (D-09), URUTAN WAJIB:
       ```
       await using var tx = await _context.Database.BeginTransactionAsync();
       try {
         // (0a) [E8 — B-04] Resolve SEMUA assignment AKTIF worker; hitung count. count != 1 → rollback + BypassResult(false, "Worker punya {count} assignment aktif (harus tepat 1).").
         //      Resolve assignment aktif source (yang ProtonTrackId==SourceProtonTrackId) + track tahun untuk validasi.
         // (0b) Panggil BypassValidator.Validate(...) dengan flag resolved (SourceComplete=allApproved, SourceHasFinal=penanda ada);
         //      gagal → rollback + BypassResult(false, msg).
         // (1) [tutup tahun asal] — SEBELUM deactivate (Pitfall 1):
         //     CL-A    : nilai S sudah ada → no-op (penanda sudah; B-03 menjamin SourceHasFinal==true untuk CL-A).
         //     CL-B(a) : force-approve SEMUA ProtonDeliverableProgress source-assignment (Status="Approved",
         //               ApprovedAt=now, ApprovedById=hc, HCApprovalStatus="Reviewed") + tulis DeliverableStatusHistory
         //               (StatusType="Bypassed-AutoApprove", ActorId/Name/Role=HC, Timestamp=now) PER progress (D-13);
         //               LALU await _protonCompletionService.EnsureAsync(req.CoacheeId, req.SourceProtonTrackId, req.InitiatedById, "Bypass", req.Reason).
         //     CL-C    : tanpa nilai → no-op.
         // (2) [cancel exam aktif S] (E5/D-14): set Status="Dibatalkan" untuk AssessmentSession
         //     WHERE Category=="Assessment Proton" && ProtonTrackId==SourceProtonTrackId && UserId==CoacheeId && Status!="Completed".
         //     (Instan tak punya bare session → tak ada exclusion di sini.) Lihat KEPUTUSAN D-14 di bawah.
         // (3) [deactivate assignment S]: source.IsActive=false, source.DeactivatedAt=now.
         // (4) [aktifkan target T]: create ProtonTrackAssignment { CoacheeId, ProtonTrackId=Target, IsActive=true,
         //     AssignedById=InitiatedById, AssignedAt=now, Origin="Bypass" } + SaveChanges (dapat assignmentId).
         // (5) [bootstrap deliverable T]: await ProtonDeliverableBootstrap.CreateProgressAsync(_context, newAssignmentId,
         //     req.TargetProtonTrackId, req.CoacheeId, req.TargetUnit)  // unit dari FORM (PBYP-05/Pitfall 3).
         // (6) [coach D-16 + D-16b]: resolve mapping AKTIF lama worker (untuk baca AssignmentUnit/AssignmentSection).
         //     if (!string.IsNullOrWhiteSpace(req.TargetCoachId)) {
         //        // GANTI COACH (E15): deactivate CoachCoacheeMapping aktif lama (IsActive=false) → SaveChanges → create baru
         //        // { CoacheeId, CoachId=TargetCoachId, AssignmentUnit=TargetUnit, IsActive=true,
         //        //   StartDate=DateTime.UtcNow, AssignmentSection=<warisi dari mapping lama / resolve dari TargetUnit> }  (W-13/I-04)
         //     } else if (mappingLama != null && mappingLama.AssignmentUnit.Trim() != req.TargetUnit.Trim()) {
         //        // D-16b (review #2) — KEEP COACH + GANTI UNIT: JANGAN deactivate/recreate (pertahankan coach, semangat D-16, E15 tak tersentuh).
         //        // UPDATE field unit mapping existing di dalam transaksi supaya gate 100% (resolve unit dari active mapping
         //        // AssignmentUnit) konsisten dengan bootstrap unit-FORM. Tanpa ini: gate hitung deliverable unit LAMA vs
         //        // progress unit FORM → worker tak pernah eligible 100% (W-04/W-08/W-10).
         //        mappingLama.AssignmentUnit = req.TargetUnit;
         //     } else { /* keep coach, unit sama → JANGAN sentuh mapping (D-16 murni) */ }
         // (7) [audit]: _auditLog.LogAsync(hc, hcName, "ProtonBypass", $"Bypass {Mode} {CoacheeId}: track {Source}→{Target}, unit {TargetUnit}. Alasan: {Reason}", targetType:"PendingProtonBypass").
         await _context.SaveChangesAsync();
         await tx.CommitAsync();
         return new BypassResult(true, "Bypass berhasil.");
       }
       catch (DbUpdateException dbEx) when (dbEx.InnerException?.Message.Contains("IX_CoachCoacheeMappings_CoacheeId_ActiveUnique")==true || dbEx.InnerException?.Message.Contains("2601")==true || dbEx.InnerException?.Message.Contains("2627")==true) {
         _logger.LogWarning(dbEx, "Bypass coach unique-index violation");
         await tx.RollbackAsync();
         return new BypassResult(false, "Coachee sudah punya coach aktif. Nonaktifkan mapping lama dulu.");
       }
       catch (Exception ex) {
         _logger.LogError(ex, "ExecuteInstantBypassAsync gagal Coachee={CoacheeId}", req.CoacheeId);
         await tx.RollbackAsync();
         return new BypassResult(false, "Gagal eksekusi bypass. Operasi dibatalkan.");  // JANGAN expose ex.Message (D6)
       }
       ```
    KEPUTUSAN D-14 (cancel exam aktif S) — di-baking planner: "cancel" = set `AssessmentSession.Status="Dibatalkan"` (nilai non-completable; tak ada penghapusan data → reversible). AssessmentSession.Status existing hanya {"Open","Upcoming","Completed","Menunggu Penilaian"} (lihat Models/AssessmentSession.cs:20) → "Dibatalkan" tak bentrok + exam-entry filter (Open/Upcoming) otomatis menolak worker masuk. JANGAN hapus session/UserPackageAssignment.
    KEPUTUSAN D-16b (review #2) — keep-coach + ganti-unit: UPDATE `mapping.AssignmentUnit = req.TargetUnit` di dalam transaksi (tanpa deactivate/recreate). Berlaku di sini DAN di jalur Confirm (plan 05). Coach dipertahankan (semangat D-16), E15 filtered-unique tak tersentuh (tidak ada insert mapping baru). Dokumentasikan di SUMMARY.
    KEPUTUSAN W-13/I-04 — create mapping baru (jalur ganti-coach): WAJIB set `StartDate=DateTime.UtcNow` (non-nullable; default(DateTime)=0001-01-01 merusak view coaching) + `AssignmentSection` (warisi dari mapping lama yang di-deactivate, atau resolve dari TargetUnit). JANGAN biarkan keduanya default.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AddScoped<HcPortal.Services.ProtonBypassService>" Program.cs` → 1 match
    - `grep -n "public async Task<BypassResult> ExecuteInstantBypassAsync" Services/ProtonBypassService.cs` → 1 match
    - `grep -n "BeginTransactionAsync" Services/ProtonBypassService.cs` → ≥1 (transaksi all-or-nothing)
    - B-04 (E8): di ExecuteInstantBypassAsync ada cek count assignment aktif == 1 sebelum eksekusi — `grep -n "assignment aktif\|count == 1\|Count(" Services/ProtonBypassService.cs` → ≥1 (manual review urutan: cek E8 di langkah (0a) sebelum dispatch step pindah)
    - Pitfall 1 ordering: di ExecuteInstantBypassAsync, baris `EnsureAsync(...,"Bypass",...)` muncul SEBELUM baris yang set `source.IsActive = false` (verifikasi urutan saat review)
    - `grep -n "ProtonDeliverableBootstrap.CreateProgressAsync" Services/ProtonBypassService.cs` mengandung argumen `req.TargetUnit` (unit dari form, bukan mapping)
    - D-16b (W-04/W-08/W-10): `grep -n "AssignmentUnit = req.TargetUnit\|mappingLama.AssignmentUnit" Services/ProtonBypassService.cs` → ≥1 (update mapping unit saat keep-coach+ganti-unit)
    - W-13/I-04: `grep -n "StartDate = DateTime\|AssignmentSection =" Services/ProtonBypassService.cs` → ≥1 (create mapping baru set StartDate+AssignmentSection)
    - `grep -n "\"Dibatalkan\"" Services/ProtonBypassService.cs` → ≥1 (cancel exam D-14)
    - No info-leak (D6): manual review — di blok `catch` yang me-return BypassResult, pesan TIDAK mengandung `ex.Message` (grep `ex.Message` hanya boleh muncul di `_logger.Log...`)
    - JANGAN ada `GradingService` di ctor: `grep -cF "GradingService" Services/ProtonBypassService.cs` → 0
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>Service + DI + §5.1 ExecuteInstantBypassAsync (3 mode instan) build hijau; E8 dicek di resolve (B-04); urutan penanda-sebelum-deactivate dijaga; D-16b update mapping unit; create mapping set StartDate+Section; cancel exam = "Dibatalkan"; no info-leak.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests §5.1 (reuse ProtonCompletionFixture)</name>
  <files>HcPortal.Tests/ProtonBypassServiceTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonCompletionServiceTests.cs (:25-90 fixture ProtonCompletionFixture disposable real-SQL `[Trait("Category","Integration")]` + MigrateAsync; pola seed + assert)
    - Services/ProtonBypassService.cs (method ExecuteInstantBypassAsync yang baru dibuat task 2)
    - Services/ProtonCompletionService.cs (EnsureAsync resolve assignment WHERE IsActive — untuk assert penanda terbit)
    - Helpers/CoacheeEligibilityCalculator.cs (:15-19 count check — untuk assert CL-C turun tetap eligible, B-06)
  </read_first>
  <action>
    Buat `HcPortal.Tests/ProtonBypassServiceTests.cs` — `[Trait("Category","Integration")]`, `IClassFixture<ProtonCompletionFixture>` (REUSE fixture apa adanya — disposable `HcPortalDB_Test_<guid>`, MigrateAsync membuktikan migration#2 apply). Instansiasi ProtonBypassService dengan dependency nyata (atau stub ILogger/NotificationService). Test:
    1. `CL_BSatuA_TerbitPenandaSource_SebelumDeactivate` (Pitfall 1): seed worker + assignment source aktif + deliverable progress; jalankan ExecuteInstantBypassAsync mode "CL-B(a)"; assert ProtonFinalAssessment Origin="Bypass" ADA untuk source assignment + source assignment IsActive==false + target assignment IsActive==true Origin="Bypass".
    2. `CL_A_PindahInstan`: seed source komplit (allApproved + penanda final SUDAH ada → SourceHasFinal=true, B-03) → mode "CL-A" → assert pindah (source inactive, target active).
    3. `CL_C_Tinggalkan`: mode "CL-C" → source deactivate, target active, TANPA penanda baru source.
    4. `Bootstrap_PakaiUnitForm_BukanMapping` (PBYP-05): seed deliverable untuk unit "Unit-FORM" + mapping unit "Unit-LAMA"; bypass TargetUnit="Unit-FORM" → assert ProtonDeliverableProgress dibuat untuk deliverable Unit-FORM (bukan Unit-LAMA).
    5. `Coach_DeactivateLamaCreateBaru_E15` (PBYP-04): TargetCoachId diisi → assert mapping lama IsActive==false + mapping baru aktif CoachId=target + mapping baru StartDate != default(DateTime) + AssignmentSection terisi (W-13); TargetCoachId=null + unit sama → mapping lama tetap aktif & tak berubah.
    6. `KeepCoach_GantiUnit_UpdateMappingUnit_GateKonsisten` (D-16b, W-04/W-08/W-10): seed worker + mapping aktif AssignmentUnit="Unit-LAMA" + coach; bypass TargetCoachId=null TAPI TargetUnit="Unit-FORM" (beda) → assert mapping aktif TIDAK di-deactivate (coach sama, masih 1 mapping aktif) + mapping.AssignmentUnit SEKARANG == "Unit-FORM" + (jika bisa) eligibility/gate resolve unit "Unit-FORM" konsisten dengan progress yang di-bootstrap.
    7. `CL_C_TurunKeTrackPernahDijalani_CountProgressTetapN` (B-06): seed worker punya assignment Tahun-2 aktif + assignment Tahun-1 LAMA (inactive) dengan N ProtonDeliverableProgress (Approved) untuk unit X; bypass CL-C turun Tahun2→Tahun1 unit X → assert jumlah ProtonDeliverableProgress per (coachee, deliverable unit X) TETAP N (bukan 2N) → `CoacheeEligibilityCalculator.IsEligiblePerUnit` BISA true (tidak permanen tak-eligible).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ProtonBypassServiceTests" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "[Fact]" HcPortal.Tests/ProtonBypassServiceTests.cs` → ≥7 (W-14: fixed-string grep)
    - `grep -n "Trait(\"Category\", \"Integration\")" HcPortal.Tests/ProtonBypassServiceTests.cs` → 1 match
    - `grep -n "IClassFixture<ProtonCompletionFixture>" HcPortal.Tests/ProtonBypassServiceTests.cs` → 1 match
    - B-06 test ada: `grep -n "TurunKeTrackPernahDijalani\|CountProgressTetap" HcPortal.Tests/ProtonBypassServiceTests.cs` → ≥1
    - D-16b test ada: `grep -n "KeepCoach_GantiUnit\|UpdateMappingUnit" HcPortal.Tests/ProtonBypassServiceTests.cs` → ≥1
    - `dotnet test --filter "FullyQualifiedName~ProtonBypassServiceTests"` → semua Passed (≥7 test) — membuktikan §5.1 + Pitfall 1 + PBYP-04/05 + B-06 + D-16b
  </acceptance_criteria>
  <done>≥7 integration test §5.1 hijau (real-SQL): Pitfall 1 ordering, CL-A/C, bootstrap unit-form (PBYP-05), coach E15 + StartDate/Section (PBYP-04/W-13), keep-coach+ganti-unit gate konsisten (D-16b), CL-C turun count tetap N (B-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProtonBypassService (internal) | Dipanggil controller (plan 07) + grading hook (plan 06); operasi hak tinggi (pindah tahun + force-approve) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-07 | Tampering | partial state saat error | mitigate | Seluruh §5.1 di-wrap `BeginTransactionAsync`; SaveChanges internal (EnsureAsync/bootstrap/audit) jadi bagian tx → rollback bersih (Pitfall 4) |
| T-360-08 | Information Disclosure | catch generic | mitigate | `catch(Exception)` return pesan ramah generik; `ex` ke `_logger` saja — TIDAK ke return message (D6 Phase 334) |
| T-360-09 | Tampering | penanda tak terbit (urutan salah) | mitigate | EnsureAsync (resolve WHERE IsActive) dipanggil SEBELUM deactivate source (Pitfall 1); integration test assert penanda ADA |
| T-360-10 | Elevation of Privilege | validasi di-skip | mitigate | ValidateBypass dipanggil di awal ExecuteInstant (server-side); §5 rule (Δtahun, 1-aktif E8, mode, CL-A allApproved+final B-03) enforce sebelum eksekusi |
| T-360-31 | Tampering | count progress dobel → worker permanen tak-eligible | mitigate | Helper bootstrap guard anti-dobel (B-06, plan 02); integration test CL-C turun ke track pernah dijalani assert count tetap N |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~ProtonBypassValidation"` hijau (≥8 unit, termasuk B-03 CL-A+final).
- `dotnet test --filter "FullyQualifiedName~ProtonBypassServiceTests"` hijau (≥7 integration, termasuk B-06 + D-16b).
- `dotnet build` 0 error; full suite `dotnet test` hijau.
- Urutan penanda-sebelum-deactivate dijaga (Pitfall 1); E8 dicek di resolve (B-04).
</verification>

<success_criteria>
- Validasi §5 pure ber-test (B-03 CL-A allApproved+final); ExecuteInstantBypassAsync (CL-A/B(a)/C) all-or-nothing dengan E8 (B-04).
- Penanda source terbit sebelum deactivate; bootstrap pakai unit form; coach E15 + D-16b update unit + StartDate/Section.
- DI terdaftar; build + test hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-03-SUMMARY.md`. Catat: signature method service, keputusan D-14 (cancel="Dibatalkan"), keputusan D-16b (keep-coach+ganti-unit → update mapping.AssignmentUnit), penempatan E8 (B-04) di resolve — untuk konsumsi plan 04/05/07.
</output>
</content>
