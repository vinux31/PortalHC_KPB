---
phase: 360-bypass-backend-b
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - Services/ProtonBypassService.cs
  - HcPortal.Tests/ProtonBypassServiceTests.cs
autonomous: true
requirements: [PBYP-02, PBYP-03, PBYP-06]
tags: [proton, bypass, pending, notification, grading-hook]
user_setup: []

must_haves:
  truths:
    - "BypassSaveAsync validasi lalu dispatch: instan (CL-A/B(a)/C) vs pending (CL-B(b))"
    - "Worker sudah punya pending Menunggu/Siap → BypassSave tolak apapun modenya (D-10)"
    - "CL-B(b): force-approve source + bikin AssessmentSession bare (tanpa paket) + insert pending Menunggu + reminder lampirkan paket"
    - "Exam linked lulus → MarkPendingReadyIfAnyAsync flip Menunggu→Siap + notif PROTON_BYPASS_READY ke InitiatedById (tanpa transaksi)"
    - "Re-grade Pass→Fail exam Siap → pending balik Menunggu (D-15)"
  artifacts:
    - path: "Services/ProtonBypassService.cs"
      provides: "BypassSaveAsync + ExecutePendingBypassAsync + MarkPendingReadyIfAnyAsync + RevertPendingToMenungguAsync"
      contains: "MarkPendingReadyIfAnyAsync"
  key_links:
    - from: "MarkPendingReadyIfAnyAsync"
      to: "INotificationService.SendByTemplateAsync"
      via: "PROTON_BYPASS_READY ke InitiatedById"
      pattern: "PROTON_BYPASS_READY"
    - from: "BypassSaveAsync"
      to: "ExecuteInstantBypassAsync / ExecutePendingBypassAsync"
      via: "dispatch per mode"
      pattern: "ExecutePendingBypassAsync"
---

<objective>
Lengkapi jalur CL-B(b) (buat-tunggu, §5.2) dan lifecycle Menunggu→Siap: entry `BypassSaveAsync` (validasi + dispatch instan/pending + blok dobel-pending D-10), `ExecutePendingBypassAsync` (§5.2), serta method hook ringan `MarkPendingReadyIfAnyAsync` (flip + notif, dipanggil GradingService plan 06) dan `RevertPendingToMenungguAsync` (D-15).

Purpose: CL-B(b) = "butuh record exam + sertifikat" → buat sesi exam source-year BARE (tanpa paket, D-01), tunggu worker lulus, lalu HC konfirmasi (plan 05). Hook method TIDAK buka transaksi sendiri (jalan di hot-path grading — anti-pattern RESEARCH).
Output: BypassSaveAsync + §5.2 + 2 hook method + integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@.planning/phases/360-bypass-backend-b/360-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md
@Services/ProtonBypassService.cs

<interfaces>
<!-- Method ditambah plan ini ke ProtonBypassService.cs (dari plan 03) -->
```csharp
public Task<BypassResult> BypassSaveAsync(BypassRequest req);          // validate → dispatch instan/pending + D-10
public Task<BypassResult> ExecutePendingBypassAsync(BypassRequest req); // §5.2 CL-B(b)
public Task<bool> MarkPendingReadyIfAnyAsync(int sessionId);           // hook grading: Menunggu→Siap + notif (NO tx)
public Task RevertPendingToMenungguAsync(int sessionId);              // hook re-grade fail: Siap→Menunggu (NO tx, D-15)
```

Bare AssessmentSession CL-B(b) defaults (Claude's Discretion, baked):
- Category="Assessment Proton", ProtonTrackId=SourceProtonTrackId, TahunKe=source year string ("Tahun {S}")
- Title=$"Bypass Exam Tahun {S} — {workerName}", Schedule=DateTime.Now, DurationMinutes=req.DurationMinutes ?? 60
- PassPercentage=70 (default AssessmentSession.cs:30), Status="Upcoming", GenerateCertificate=true (butuh sertifikat), CreatedBy=InitiatedById, IsTokenRequired=false
- TANPA paket soal (D-01); TANPA UserPackageAssignment (HC pasang paket via Kelola Assessment step 2)

Notif (Pattern 5): SendByTemplateAsync(pending.InitiatedById, "PROTON_BYPASS_READY", { ["PendingId"]=pending.Id, ["WorkerName"]=namaWorker })
</interfaces>

<!-- Analog atomic flip: GradingService.cs:234-251 (ExecuteUpdateAsync + WHERE + rowsAffected) -->
<!-- Notif: NotificationService.cs:231-265 SendByTemplateAsync; template PROTON_BYPASS_READY (plan 01) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: BypassSaveAsync dispatch + D-10 dobel-pending + ExecutePendingBypassAsync (§5.2)</name>
  <files>Services/ProtonBypassService.cs</files>
  <read_first>
    - Services/ProtonBypassService.cs (ExecuteInstantBypassAsync + BypassValidator dari plan 03 — dispatch ke method ini untuk mode instan)
    - Models/AssessmentSession.cs (fields lengkap untuk create bare session: Category, ProtonTrackId, TahunKe, Title, Schedule, DurationMinutes, PassPercentage, Status, GenerateCertificate, CreatedBy, IsTokenRequired)
    - Controllers/ProtonDataController.cs (:1443-1465 pola force-approve progress: Status="Approved", ApprovedAt, ApprovedById, HCApprovalStatus) untuk force-approve source CL-B(b)
    - .planning/phases/360-bypass-backend-b/360-RESEARCH.md ("Blok dobel pending (D-10)" code example)
  </read_first>
  <action>
    1. `BypassSaveAsync(BypassRequest req)`:
       - Resolve flag (assignment aktif source, tahun source/target, SourceComplete, SourceHasFinal) lalu `BypassValidator.Validate(...)`; gagal → return BypassResult(false, msg).
       - D-10 BLOK DOBEL PENDING (semua mode, SEBELUM eksekusi):
       ```csharp
       bool hasActivePending = await _context.PendingProtonBypasses
           .AnyAsync(p => p.CoacheeId == req.CoacheeId && (p.Status == "Menunggu" || p.Status == "Siap"));
       if (hasActivePending) return new BypassResult(false, "Worker sudah punya rencana bypass aktif. Selesaikan/batalkan dulu.");
       ```
       - Dispatch: `req.Mode == "CL-B(b)"` → `return await ExecutePendingBypassAsync(req);` else → `return await ExecuteInstantBypassAsync(req);`
    2. `ExecutePendingBypassAsync(BypassRequest req)` — §5.2, 1 transaksi (pola sama Plan 03 catch):
       ```
       await using tx = BeginTransactionAsync;
       try {
         // [force-approve deliverable source] semua ProtonDeliverableProgress source-assignment → Status="Approved",
         //   ApprovedAt=now, ApprovedById=hc, HCApprovalStatus="Reviewed" + DeliverableStatusHistory
         //   (StatusType="Bypassed-AutoApprove", ActorId/Name/Role=HC, Timestamp=now) per progress (D-13).
         //   (CATATAN: TIDAK EnsureProtonFinalAssessment di sini — penanda terbit nanti oleh GradingService saat exam lulus, Origin="Exam".)
         // [buat AssessmentSession bare] pakai defaults di interfaces (TanpaPaket, D-01) + SaveChanges → bareSessionId.
         // [insert PendingProtonBypass] { CoacheeId, SourceProtonTrackId, TargetProtonTrackId, TargetUnit, TargetCoachId,
         //   Reason, LinkedAssessmentSessionId=bareSessionId, Status="Menunggu", InitiatedById, Mode="CL-B(b)", CreatedAt=now }.
         // [audit] LogAsync(...).
         SaveChanges + Commit;
         return new BypassResult(true, "Sesi exam dibuat — lampirkan paket soal di Kelola Assessment sebelum worker bisa ujian.",
                                 PendingId: pending.Id, ShowAttachPackageReminder: true);  // D-02 reminder
       } catch (...) { rollback + pesan ramah tanpa ex.Message; }
       ```
       D-02: `ShowAttachPackageReminder=true` + message reminder → controller (plan 07) surface ke TempData.
       D-03: source CL-B(b) selalu exam online (naik 1→2 / 2→3) → tipe ikut tahun = exam, hook GradingService §7 pasti kena.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public async Task<BypassResult> BypassSaveAsync" Services/ProtonBypassService.cs` → 1 match
    - `grep -n "hasActivePending" Services/ProtonBypassService.cs` → ≥1 (D-10 blok dobel pending, cek Status Menunggu/Siap)
    - `grep -n "public async Task<BypassResult> ExecutePendingBypassAsync" Services/ProtonBypassService.cs` → 1 match
    - `grep -n "Status = \"Menunggu\"" Services/ProtonBypassService.cs` → ≥1 (insert pending)
    - `grep -n "ShowAttachPackageReminder" Services/ProtonBypassService.cs` → ≥1 (D-02 reminder)
    - `grep -n "Bypassed-AutoApprove" Services/ProtonBypassService.cs` → ≥1 (D-13 force-approve history)
    - Bare session TANPA UserPackageAssignment: `grep -c "UserPackageAssignment" Services/ProtonBypassService.cs` → 0 (D-01)
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>BypassSaveAsync dispatch + D-10 + §5.2 (bare session + pending Menunggu + reminder D-02) build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: MarkPendingReadyIfAnyAsync (flip + notif, NO tx) + RevertPendingToMenungguAsync (D-15)</name>
  <files>Services/ProtonBypassService.cs</files>
  <read_first>
    - Services/GradingService.cs (:234-251 ExecuteUpdateAsync + WHERE + rowsAffected pattern — flip atomik tanpa transaksi)
    - Services/NotificationService.cs (:231-265 SendByTemplateAsync signature + placeholder substitution)
    - Services/ProtonBypassService.cs (ctor _notificationService INotificationService dari plan 03)
    - docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md (§7 pemicu notif + re-grade Pass→Fail revert)
  </read_first>
  <action>
    1. `MarkPendingReadyIfAnyAsync(int sessionId)` — TANPA `BeginTransactionAsync` (jalan di hot-path grading):
       ```csharp
       var pending = await _context.PendingProtonBypasses
           .FirstOrDefaultAsync(p => p.LinkedAssessmentSessionId == sessionId && p.Status == "Menunggu");
       if (pending == null) return false;
       var rows = await _context.PendingProtonBypasses
           .Where(p => p.Id == pending.Id && p.Status == "Menunggu")
           .ExecuteUpdateAsync(p => p.SetProperty(x => x.Status, "Siap"));  // atomik flip
       if (rows == 0) return false;  // sudah diproses
       var workerName = await _context.Users.Where(u => u.Id == pending.CoacheeId).Select(u => u.FullName).FirstOrDefaultAsync() ?? pending.CoacheeId;
       await _notificationService.SendByTemplateAsync(pending.InitiatedById, "PROTON_BYPASS_READY",
           new Dictionary<string, object> { ["PendingId"] = pending.Id, ["WorkerName"] = workerName });  // ke HC inisiator, BUKAN worker
       return true;
       ```
    2. `RevertPendingToMenungguAsync(int sessionId)` (D-15) — TANPA transaksi:
       ```csharp
       await _context.PendingProtonBypasses
           .Where(p => p.LinkedAssessmentSessionId == sessionId && p.Status == "Siap")
           .ExecuteUpdateAsync(p => p.SetProperty(x => x.Status, "Menunggu"));
       // worker belum pindah (Opsi B) → aman, tak ada rollback assignment.
       ```
    Kedua method idempotent + aman dipanggil berkali-kali (guard rowsAffected / WHERE Status). JANGAN buka transaksi (Pitfall 4 — anti-pattern hook).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public async Task<bool> MarkPendingReadyIfAnyAsync" Services/ProtonBypassService.cs` → 1 match
    - `grep -n "public async Task RevertPendingToMenungguAsync" Services/ProtonBypassService.cs` → 1 match
    - Kedua hook method TIDAK buka transaksi: di body MarkPendingReadyIfAnyAsync + RevertPendingToMenungguAsync TIDAK ada `BeginTransactionAsync` (verifikasi saat review; `grep` BeginTransactionAsync hanya muncul di ExecuteInstant/ExecutePending, bukan hook)
    - `grep -n "ExecuteUpdateAsync" Services/ProtonBypassService.cs` → ≥2 (flip Siap + revert Menunggu, atomik)
    - `grep -n "SendByTemplateAsync.*PROTON_BYPASS_READY\|\"PROTON_BYPASS_READY\"" Services/ProtonBypassService.cs` → ≥1 (notif ke InitiatedById)
    - `grep -n "pending.InitiatedById" Services/ProtonBypassService.cs` → ≥1 (notif ke HC, bukan worker)
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>MarkPendingReadyIfAnyAsync (flip Siap + notif HC, no tx) + RevertPendingToMenungguAsync (D-15) build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests CL-B(b) pending + flip + revert + D-10</name>
  <files>HcPortal.Tests/ProtonBypassServiceTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonBypassServiceTests.cs (file dari plan 03 — extend; fixture ProtonCompletionFixture)
    - Services/ProtonBypassService.cs (method baru BypassSaveAsync/ExecutePendingBypassAsync/MarkPendingReadyIfAnyAsync/RevertPendingToMenungguAsync)
  </read_first>
  <action>
    Extend `HcPortal.Tests/ProtonBypassServiceTests.cs` dengan [Fact] (Integration):
    1. `CL_BSatuB_BuatPending_BareSession_NoPackage`: BypassSaveAsync mode "CL-B(b)" → assert PendingProtonBypass Status="Menunggu" + LinkedAssessmentSessionId menunjuk AssessmentSession (Category="Assessment Proton", Status="Upcoming") + TIDAK ada UserPackageAssignment untuk sesi itu + result.ShowAttachPackageReminder==true + deliverable source Approved + DeliverableStatusHistory "Bypassed-AutoApprove".
    2. `MarkPendingReady_FlipSiap_KirimNotif`: setelah pending Menunggu, panggil MarkPendingReadyIfAnyAsync(bareSessionId) → assert Status="Siap" + ada UserNotification untuk InitiatedById tipe PROTON_BYPASS_READY (worker TIDAK dapat notif ini).
    3. `Revert_PassFail_PendingBalikMenunggu` (D-15): pending Siap → RevertPendingToMenungguAsync(bareSessionId) → assert Status="Menunggu" + assignment worker tak berubah (belum pindah).
    4. `D10_DobelPending_Tolak`: worker punya pending Menunggu → BypassSaveAsync lagi (mode apapun) → assert result.Success==false + message mengandung "rencana bypass aktif".
    5. `MarkPendingReady_NoTransaction_SafeRepeat`: panggil MarkPendingReadyIfAnyAsync 2x → kedua aman, Status tetap "Siap", notif tidak dobel-flip (rows==0 di panggilan ke-2).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ProtonBypassServiceTests" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "[Fact]" HcPortal.Tests/ProtonBypassServiceTests.cs` → ≥10 (5 dari plan 03 + ≥5 baru)
    - Test mencakup nama: `CL_BSatuB_BuatPending`, `MarkPendingReady_FlipSiap`, `Revert_PassFail`, `D10_DobelPending` (grep masing-masing → match)
    - `dotnet test --filter "FullyQualifiedName~ProtonBypassServiceTests"` → semua Passed
  </acceptance_criteria>
  <done>≥5 integration test baru hijau: CL-B(b) pending (PBYP-02), flip+notif (PBYP-03), revert (PBYP-03/D-15), D-10 (PBYP-06 guard).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GradingService hot-path → hook | MarkPendingReady dipanggil saat grading; tak boleh buka tx / eksekusi pindah |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-11 | Tampering | nested transaksi hook | mitigate | MarkPendingReady/Revert TANPA BeginTransactionAsync — flip flag + notif saja (spec §7); eksekusi pindah tetap di HC (plan 05) |
| T-360-12 | Tampering | dobel pending tabrakan | mitigate | D-10 blok dobel pending (Status Menunggu/Siap) di BypassSaveAsync semua mode |
| T-360-13 | Information Disclosure | notif salah tujuan | mitigate | Notif PROTON_BYPASS_READY ke `InitiatedById` (HC), bukan worker; worker tetap dapat ASMT_RESULTS_READY normal |
| T-360-14 | Tampering | partial state §5.2 | mitigate | ExecutePendingBypassAsync wrap BeginTransactionAsync; catch generic tanpa ex.Message (D6) |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~ProtonBypassServiceTests"` hijau (≥10 total).
- Hook method tanpa transaksi (review + grep).
- `dotnet build` 0 error; full suite hijau.
</verification>

<success_criteria>
- BypassSaveAsync (validate + dispatch + D-10); ExecutePendingBypassAsync §5.2 (bare session + pending + reminder D-02).
- MarkPendingReadyIfAnyAsync (flip Siap + notif HC, no tx); RevertPendingToMenungguAsync (D-15).
- Build + test hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-04-SUMMARY.md`. Catat signature 4 method untuk konsumsi GradingService hook (plan 06) + endpoint (plan 07).
</output>
