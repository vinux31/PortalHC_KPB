---
phase: 360-bypass-backend-b
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - Services/ProtonBypassService.cs
  - HcPortal.Tests/ProtonBypassServiceTests.cs
autonomous: true
requirements: [PBYP-02, PBYP-06]
tags: [proton, bypass, confirm, cancel, atomic-guard]
user_setup: []

must_haves:
  truths:
    - "ConfirmBypass cek-ulang stale (D-11): assignment asal masih sama + exam lulus + penanda Origin=Exam ada + pending Siap"
    - "Transisi Siap→Selesai atomik (D-12): klik dobel / 2 HC barengan → pindah jalan SEKALI"
    - "Konfirmasi eksekusi pindah §5.1 TANPA re-force-approve & TANPA re-create final (penanda Exam sudah ada)"
    - "Batal pending (§8.1): belum-kerjakan exam→cancel/hapus, sudah-lulus→pertahankan hasil; force-approve source tetap"
  artifacts:
    - path: "Services/ProtonBypassService.cs"
      provides: "ConfirmBypassAsync + CancelPendingAsync"
      contains: "ConfirmBypassAsync"
  key_links:
    - from: "ConfirmBypassAsync"
      to: "PendingProtonBypasses (ExecuteUpdate Siap→Selesai)"
      via: "atomic rowsAffected guard (D-12)"
      pattern: "ExecuteUpdateAsync"
    - from: "CancelPendingAsync"
      to: "LinkedAssessmentSession"
      via: "auto-cancel per status worker (§8.1)"
      pattern: "LinkedAssessmentSessionId"
---

<objective>
Lengkapi resolusi pending: `ConfirmBypassAsync` (§5.3 — HC konfirmasi pindah setelah exam lulus) dan `CancelPendingAsync` (§8.1 — HC batal pending sebelum pindah). Konfirmasi = cek-ulang stale (D-11) + transisi atomik Siap→Selesai (D-12) + eksekusi pindah (reuse logic §5.1 tanpa re-terbitkan penanda). Batal = tutup rencana + auto-cancel exam terkait.

Purpose: Konfirmasi adalah titik race paling rawan (2 HC / klik dobel) dan paling rawan stale (worker dipindah manual / exam di-regrade fail) — D-11 + D-12 menutup keduanya. §8.1 membebaskan HC dari harus ke Manage Assessment manual.
Output: ConfirmBypassAsync + CancelPendingAsync + integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@.planning/phases/360-bypass-backend-b/360-PATTERNS.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md
@Services/ProtonBypassService.cs

<interfaces>
```csharp
public Task<BypassResult> ConfirmBypassAsync(int pendingId, string hcId, string hcName);  // §5.3
public Task<BypassResult> CancelPendingAsync(int pendingId, string hcId, string hcName);  // §8.1
```

§5.3 KONFIRMASI: eksekusi PINDAH-INSTAN (§5.1) TANPA re-force-approve & TANPA re-create final
(penanda sudah terbit Origin="Exam" oleh GradingService) → set pending Status="Selesai".
Re-use step §5.1 (deactivate source + activate target + bootstrap + coach + audit) dari ExecuteInstantBypassAsync;
build BypassRequest dari field pending (CoacheeId, Source/Target track, TargetUnit, TargetCoachId, Reason, Mode, InitiatedById).

§8.1 BATAL: Status="Dibatalkan", ResolvedAt=now. LinkedAssessmentSession:
- worker BELUM kerjakan exam (UserPackageAssignment.IsCompleted==false / belum ada) → cancel/hapus session.
- worker SUDAH lulus (pending Status=="Siap") → PERTAHANKAN hasil exam (penanda Origin="Exam" sah); hanya batalkan rencana.
Force-approve deliverable source TETAP (jejak history). Audit log.
</interfaces>

<!-- Analog atomic guard: GradingService.cs:234-251 (ExecuteUpdateAsync WHERE Status + rowsAffected==0) -->
<!-- Cancel exam D-14 keputusan: set AssessmentSession.Status="Dibatalkan" (plan 03) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfirmBypassAsync (§5.3) — re-check D-11 + atomic Siap→Selesai D-12 + pindah</name>
  <files>Services/ProtonBypassService.cs</files>
  <read_first>
    - Services/ProtonBypassService.cs (ExecuteInstantBypassAsync §5.1 — reuse step deactivate/activate/bootstrap/coach; perhatikan TANPA re-force-approve & TANPA EnsureAsync di confirm)
    - Services/GradingService.cs (:234-251 ExecuteUpdateAsync + WHERE + rowsAffected==0 guard — pola atomic D-12)
    - Services/ProtonCompletionService.cs (penanda Origin="Exam" sudah terbit GradingService — cek keberadaan, JANGAN buat ulang)
    - Models/UserPackageAssignment.cs (IsCompleted) + AssessmentSession (IsPassed) untuk cek "exam beneran lulus" D-11
  </read_first>
  <action>
    `ConfirmBypassAsync(int pendingId, string hcId, string hcName)` — 1 transaksi (pola catch sama plan 03):
    ```
    await using tx = BeginTransactionAsync;
    try {
      var pending = await _context.PendingProtonBypasses.FindAsync(pendingId);
      if (pending == null || pending.Status != "Siap")
          { rollback; return BypassResult(false, "Pending tidak ditemukan atau belum siap."); }

      // D-11 cek-ulang stale (SEBELUM pindah):
      // (a) assignment asal worker MASIH = SourceProtonTrackId aktif:
      bool sourceStillActive = await _context.ProtonTrackAssignments
          .AnyAsync(a => a.CoacheeId==pending.CoacheeId && a.ProtonTrackId==pending.SourceProtonTrackId && a.IsActive);
      // (b) exam beneran lulus + penanda Origin="Exam" untuk source ADA:
      var linked = await _context.AssessmentSessions.FindAsync(pending.LinkedAssessmentSessionId);
      bool examPassed = linked?.IsPassed == true;
      bool penandaExamAda = await (penanda Origin="Exam" untuk source assignment worker) ... AnyAsync;
      if (!sourceStillActive || !examPassed || !penandaExamAda)
          { rollback; return BypassResult(false, "Kondisi rencana sudah berubah (assignment/exam). Konfirmasi dibatalkan."); }

      // D-12 transisi atomik Siap→Selesai (kunci anti klik-dobel / 2 HC):
      var rows = await _context.PendingProtonBypasses
          .Where(p => p.Id==pendingId && p.Status=="Siap")
          .ExecuteUpdateAsync(p => p.SetProperty(x=>x.Status,"Selesai").SetProperty(x=>x.ResolvedAt, DateTime.UtcNow));
      if (rows == 0) { rollback; return BypassResult(false, "Pending sudah diproses (klik ganda)."); }

      // [pindah] eksekusi step §5.1 TANPA re-force-approve & TANPA EnsureAsync (penanda Exam sudah ada):
      //   cancel exam aktif S (kecuali LinkedAssessmentSessionId — itu record lulus, JANGAN dibatalkan),
      //   deactivate source assignment, create target Origin="Bypass", bootstrap TargetUnit, coach D-16, audit.
      SaveChanges + Commit; return BypassResult(true, "Pindah tahun berhasil.");
    } catch (DbUpdateException 2601/2627 → coach pesan) / catch(Exception → ramah tanpa ex.Message);
    ```
    PENTING: di langkah cancel-exam pada confirm, EXCLUDE `LinkedAssessmentSessionId` (sesi exam yang BARU lulus — itu bukti kelulusan, jangan di-"Dibatalkan"). Refactor agar step §5.1 bisa dipanggil dengan param "skipCloseTahunAsal=true" + "excludeSessionId=LinkedAssessmentSessionId" — atau ekstrak helper privat `MoveAssignmentAsync(...)` yang dipakai ExecuteInstant (close-tahun terpisah) dan Confirm. Pilih refactor yang menghindari duplikasi step pindah.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public async Task<BypassResult> ConfirmBypassAsync" Services/ProtonBypassService.cs` → 1 match
    - D-11 re-check: `grep -n "sourceStillActive\|examPassed\|IsPassed == true" Services/ProtonBypassService.cs` → ≥2 match
    - D-12 atomic: `grep -n "Where(p => p.Id == pendingId && p.Status == \"Siap\")" Services/ProtonBypassService.cs` + `rows == 0` → ada (rowsAffected guard)
    - Confirm TIDAK re-create penanda: di body ConfirmBypassAsync TIDAK ada `EnsureAsync(...,"Bypass"...)` baru (penanda Exam sudah ada) — verifikasi review
    - LinkedAssessmentSession di-exclude dari cancel-exam saat confirm (grep `excludeSessionId` / komentar exclude)
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>ConfirmBypassAsync: D-11 stale-check + D-12 atomic Siap→Selesai + pindah tanpa re-penanda; build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: CancelPendingAsync (§8.1) — auto-cancel exam per status worker</name>
  <files>Services/ProtonBypassService.cs</files>
  <read_first>
    - Services/ProtonBypassService.cs (ExecutePendingBypassAsync — untuk paham LinkedAssessmentSessionId + bare session)
    - Models/UserPackageAssignment.cs (IsCompleted — cek worker belum/sudah kerjakan)
    - docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md (§8.1 batal pending)
  </read_first>
  <action>
    `CancelPendingAsync(int pendingId, string hcId, string hcName)` — 1 transaksi:
    ```
    var pending = FindAsync(pendingId);
    if (pending==null || (pending.Status!="Menunggu" && pending.Status!="Siap"))
        return BypassResult(false, "Pending tidak bisa dibatalkan (status tidak valid).");
    await using tx = BeginTransactionAsync;
    try {
      // Simpan status SEBELUM update:
      bool workerLulus = pending.Status == "Siap";  // status SEBELUM update = Siap → worker sudah lulus
      var linkedId = pending.LinkedAssessmentSessionId;

      // Atomik set Dibatalkan (anti klik-dobel):
      var rows = await _context.PendingProtonBypasses
          .Where(p => p.Id==pendingId && (p.Status=="Menunggu" || p.Status=="Siap"))
          .ExecuteUpdateAsync(p => p.SetProperty(x=>x.Status,"Dibatalkan").SetProperty(x=>x.ResolvedAt, DateTime.UtcNow));
      if (rows==0) { rollback; return BypassResult(false,"Pending sudah diproses."); }

      // Auto-cancel LinkedAssessmentSession:
      if (workerLulus) {
         // PERTAHANKAN hasil exam (penanda Origin="Exam" sah) — JANGAN sentuh session. Hanya batalkan rencana.
      } else {
         // worker BELUM kerjakan → cancel sesi bare: set Status="Dibatalkan" (konsisten D-14, reversible, no data loss)
         await _context.AssessmentSessions.Where(s=>s.Id==linkedId).ExecuteUpdateAsync(s=>s.SetProperty(x=>x.Status,"Dibatalkan"));
      }
      // Force-approve deliverable source TETAP (jejak history) — JANGAN revert.
      // audit log.
      SaveChanges + Commit; return BypassResult(true,"Rencana bypass dibatalkan.");
    } catch (...) rollback + ramah tanpa ex.Message;
    ```
    CATATAN: simpan `pending.Status` ke `workerLulus` SEBELUM ExecuteUpdate (update meng-overwrite). Force-approve deliverable source TIDAK di-revert (spec §8.1 — HC rapikan via Tab1 bila perlu).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public async Task<BypassResult> CancelPendingAsync" Services/ProtonBypassService.cs` → 1 match
    - `grep -n "workerLulus\|pending.Status == \"Siap\"" Services/ProtonBypassService.cs` → ≥1 (branching belum/sudah lulus)
    - Cancel exam belum-kerjakan: ExecuteUpdateAsync AssessmentSessions Status="Dibatalkan" muncul di CancelPending — review/grep
    - Branch sudah-lulus TIDAK sentuh session (komentar "pertahankan hasil") — review
    - `dotnet build` 0 error + `dotnet test --filter "Category!=Integration"` hijau
  </acceptance_criteria>
  <done>CancelPendingAsync: Dibatalkan atomik + auto-cancel exam (belum→Dibatalkan, lulus→pertahankan); build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests confirm + cancel</name>
  <files>HcPortal.Tests/ProtonBypassServiceTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonBypassServiceTests.cs (extend; method ConfirmBypassAsync/CancelPendingAsync baru)
    - Services/ProtonBypassService.cs
  </read_first>
  <action>
    Extend `HcPortal.Tests/ProtonBypassServiceTests.cs` [Fact] (Integration):
    1. `Confirm_HappyPath_Pindah_Selesai`: pending Siap + source aktif + linked IsPassed + penanda Origin="Exam" → ConfirmBypassAsync → assert source inactive, target active Origin="Bypass", pending Status="Selesai", linked session TIDAK "Dibatalkan" (record lulus dipertahankan).
    2. `Confirm_Stale_AssignmentBerubah_Tolak` (D-11): source sudah inactive (worker dipindah manual) → ConfirmBypassAsync → assert Success==false, pending tetap "Siap" (tak pindah).
    3. `Confirm_DobelKlik_Atomik_PindahSekali` (D-12): panggil ConfirmBypassAsync 2x berurutan untuk pendingId sama → assert hanya 1 yang Success==true; assignment target cuma 1 (tak dobel).
    4. `Cancel_BelumKerjakan_HapusCancelExam` (PBYP-06): pending Menunggu (worker belum kerjakan) → CancelPendingAsync → assert pending "Dibatalkan" + linked AssessmentSession Status="Dibatalkan".
    5. `Cancel_SudahLulus_PertahankanHasil` (PBYP-06): pending Siap (worker lulus, penanda Exam ada) → CancelPendingAsync → assert pending "Dibatalkan" + linked session TIDAK "Dibatalkan" + penanda Origin="Exam" masih ada.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ProtonBypassServiceTests" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "[Fact]" HcPortal.Tests/ProtonBypassServiceTests.cs` → ≥15 (≥5 baru)
    - Test mencakup: `Confirm_HappyPath`, `Confirm_Stale`, `Confirm_DobelKlik`, `Cancel_BelumKerjakan`, `Cancel_SudahLulus` (grep match)
    - `dotnet test --filter "FullyQualifiedName~ProtonBypassServiceTests"` → semua Passed
  </acceptance_criteria>
  <done>≥5 integration test confirm/cancel hijau: D-11 stale, D-12 atomic, §8.1 dua branch (PBYP-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC konfirmasi → eksekusi pindah | Aksi hak tinggi; race + stale = pindah ganda/salah |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-15 | Tampering | double-confirm race | mitigate | ExecuteUpdateAsync WHERE Status="Siap" + rowsAffected==0 guard (D-12) → pindah sekali |
| T-360-16 | Tampering | stale confirm | mitigate | D-11 re-check (assignment asal aktif + exam IsPassed + penanda Origin="Exam") sebelum pindah; tolak ramah bila basi |
| T-360-17 | Tampering | hapus record lulus | mitigate | Saat confirm/cancel-lulus, EXCLUDE LinkedAssessmentSession dari cancel-exam → bukti kelulusan dipertahankan |
| T-360-18 | Information Disclosure | catch generic | mitigate | Pesan ramah; ex ke logger (D6) |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~ProtonBypassServiceTests"` hijau (≥15 total).
- Confirm tidak re-terbitkan penanda; double-confirm jalan sekali; cancel dua branch benar.
- `dotnet build` 0 error; full suite hijau.
</verification>

<success_criteria>
- ConfirmBypassAsync (D-11 + D-12 + pindah tanpa re-penanda); CancelPendingAsync (§8.1 dua branch).
- Build + test hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-05-SUMMARY.md`. Catat signature ConfirmBypassAsync/CancelPendingAsync untuk konsumsi endpoint (plan 07).
</output>
