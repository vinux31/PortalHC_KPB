---
phase: 360-bypass-backend-b
plan: 08
type: execute
wave: 8
depends_on: [07]
files_modified:
  - HcPortal.Tests/ProtonYearGateIntegrationTests.cs
  - .planning/phases/360-bypass-backend-b/360-UAT.md
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [PBYP-02, PBYP-07]
tags: [proton, bypass, integration-test, uat, gate-exempt]
user_setup: []

must_haves:
  truths:
    - "Assignment Origin='Bypass' lolos gate cross-year, TAPI gate deliverable 100% target-year tetap berlaku (D-05)"
    - "UAT lokal:5277 verifikasi 4 closure mode + pending konfirmasi + batal + re-grade fail"
    - "dotnet build 0 error + full suite hijau"
  artifacts:
    - path: "HcPortal.Tests/ProtonYearGateIntegrationTests.cs"
      provides: "test exempt Origin=Bypass"
      contains: "Bypass"
    - path: ".planning/phases/360-bypass-backend-b/360-UAT.md"
      provides: "checklist UAT 4 closure + pending + batal + re-grade"
      contains: "CL-B(b)"
  key_links:
    - from: "ProtonYearGateIntegrationTests"
      to: "AssessmentAdminController gate exempt"
      via: "Origin=Bypass lolos cross-year, gate 100% tetap"
      pattern: "Origin == \"Bypass\""
---

<objective>
Verifikasi end-to-end fase 360: (1) extend integration test gate untuk kasus exempt `Origin="Bypass"` (cross-year di-skip TAPI gate 100% target-year TETAP — D-05), dan (2) UAT lokal:5277 manusia untuk 4 closure mode + pending konfirmasi + batal + re-grade fail (backend bypass, render UI minimal — UI penuh Phase 361).

Purpose: Exempt adalah titik paling rawan regresi gate berurutan Phase 359 — integration test mengunci "bypass lolos cross-year tapi tetap wajib deliverable tahun tujuan". UAT membuktikan state machine pending bekerja di server nyata (migration#2 ter-apply, hook GradingService, notif).
Output: integration test exempt + 360-UAT.md + verifikasi build/test + restore DB seed note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/360-bypass-backend-b/360-CONTEXT.md
@.planning/phases/360-bypass-backend-b/360-RESEARCH.md
@docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md
@docs/SEED_WORKFLOW.md
@CLAUDE.md

<!-- Test analog: HcPortal.Tests/ProtonYearGateIntegrationTests.cs (existing dari 359) -->
<!-- Gate exempt: AssessmentAdminController:1368 (plan 02); UAT: localhost:5277 AD off -->
<!-- CATATAN VERIFIER (I-03/I-09): SC3/PBYP-06 berbunyi literal "belum-dikerjakan→hapus", tapi implementasi = -->
<!--   soft-cancel Status="Dibatalkan" (keputusan D-14, reversible). Verifier/UAT JANGAN tandai gap — 'hapus' dipenuhi sebagai 'Dibatalkan'. -->
<!-- Plan 06 (hook GradingService) tercakup transitif via depends_on chain 08→07→[05,06] (I-01) — UAT U3 menguji hook. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ProtonYearGateIntegrationTests — exempt Origin="Bypass"</name>
  <files>HcPortal.Tests/ProtonYearGateIntegrationTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonYearGateIntegrationTests.cs (struktur fixture + test existing dari Phase 359 — pola seed assignment + gate cek)
    - Controllers/AssessmentAdminController.cs (gate :1336-1392 — exempt isBypassAssignment :1368 dari plan 02; gate 100% :1373-1389)
  </read_first>
  <action>
    Tambah [Fact] (Integration) ke `ProtonYearGateIntegrationTests.cs`:
    1. `Exempt_BypassOrigin_LolosCrossYear`: seed worker dengan ProtonTrackAssignment Tahun 2 `Origin="Bypass"` aktif, TANPA penanda Tahun 1 (normalnya keblok cross-year). Jalankan path gate CreateAssessment (atau panggil predikat resolver yang dipakai gate). Assert worker TIDAK di-skip gateSkippedPrevYear (lolos cross-year karena Origin=Bypass).
    2. `Exempt_BypassOrigin_GateSeratusPersenTetap` (D-05): worker Origin="Bypass" Tahun 2 TAPI deliverable Tahun 2 belum 100% Approved → assert worker TETAP di-skip gateSkippedNotHundred (gate deliverable 100% target-year masih berlaku — exempt cuma cross-year, BUKAN 100%).
    3. `NoBypass_NormalAssignment_KeblokCrossYear` (regresi): worker Origin=null Tahun 2 tanpa penanda Tahun 1 → tetap di-skip (gate Phase 359 utuh).
    Kalau gate logic tak mudah dipanggil unit-wise, test boleh tingkat repository/predikat: seed lalu evaluasi `_context.ProtonTrackAssignments.AnyAsync(... Origin=="Bypass")` + `IsPrevYearPassedAsync` untuk membuktikan kombinasi exempt benar.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ProtonYearGate" 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Exempt_BypassOrigin\|Origin = \"Bypass\"" HcPortal.Tests/ProtonYearGateIntegrationTests.cs` → ≥2 match
    - Ada test yang assert gate 100% TETAP berlaku untuk Origin="Bypass" (D-05)
    - Ada test regresi normal assignment tetap keblok cross-year
    - `dotnet test --filter "FullyQualifiedName~ProtonYearGate"` → semua Passed
  </acceptance_criteria>
  <done>Integration test exempt: Bypass lolos cross-year + gate 100% tetap + regresi normal; hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Tulis 360-UAT.md + full suite gate</name>
  <files>.planning/phases/360-bypass-backend-b/360-UAT.md, docs/SEED_JOURNAL.md</files>
  <read_first>
    - CLAUDE.md (Develop Workflow: verifikasi lokal `dotnet build` + `dotnet run` :5277 + AD off `Authentication__UseActiveDirectory=false dotnet run`)
    - docs/SEED_WORKFLOW.md (snapshot/restore — kalau UAT butuh seed skenario)
    - .planning/phases/360-bypass-backend-b/360-RESEARCH.md (Sampling Rate — phase gate: full suite + UAT 4 closure mode)
  </read_first>
  <action>
    1. Jalankan full suite + build:
       ```powershell
       dotnet build HcPortal.csproj
       dotnet test HcPortal.Tests/HcPortal.Tests.csproj
       ```
       Pastikan 0 error + semua test (unit + integration real-SQL) hijau. Catat hitungan test di SUMMARY.
    2. Tulis `.planning/phases/360-bypass-backend-b/360-UAT.md` (Bahasa Indonesia) — checklist 6 skenario @ localhost:5277 (AD off), tiap langkah konkret (endpoint/SQL cross-check karena UI penuh = Phase 361):
       - U1 CL-A: worker Tahun 1 komplit (allApproved + penanda final ADA) → POST BypassSave mode CL-A ke Tahun 2 → cek source inactive + target active Origin="Bypass" (SQL) + gate CreateAssessment Tahun 2 lolos. (Catatan B-03: kalau penanda final BELUM ada walau 100%, BypassDetail tidak mengizinkan CL-A — arahkan CL-B(a).)
       - U2 CL-B(a): worker belum final → BypassSave CL-B(a) → cek penanda Origin="Bypass" source + deliverable source Approved + DeliverableStatusHistory "Bypassed-AutoApprove" + pindah.
       - U3 CL-B(b): BypassSave CL-B(b) → cek PendingProtonBypass "Menunggu" + AssessmentSession bare (tanpa paket, **UserId=worker + AssessmentType="Standard"** B-05) + **reminder: cek `TempData["Warning"]` ter-set (W-12 plan 07) DAN field `showAttachPackageReminder=true` di response Json**. HC pasang paket → worker ujian lulus → cek pending "Siap" + notif HC PROTON_BYPASS_READY. HC BypassConfirm → cek pindah + target punya ProtonDeliverableProgress (bootstrap W-02) + pending "Selesai".
       - U4 CL-C: BypassSave CL-C (turun/koreksi) → cek deactivate source + target active tanpa penanda. (Bila turun ke track yang PERNAH dijalani: cek count ProtonDeliverableProgress per unit TETAP N, bukan 2N — B-06; worker tetap bisa eligible.)
       - U5 Batal pending: pending Menunggu (worker belum kerjakan, session belum Completed) → BypassCancelPending → cek "Dibatalkan" + sesi bare Status="Dibatalkan". **Catatan verifier (I-03): SC3/PBYP-06 "hapus" dipenuhi sebagai soft-cancel Status="Dibatalkan" (keputusan D-14) — BUKAN row session terhapus.** Pending Siap → batal → cek hasil exam dipertahankan. (W-03: bila worker kerjakan-GAGAL session Completed → cek session TETAP "Completed", tidak di-overwrite.)
       - U6 Re-grade fail: exam CL-B(b) "Siap" di-regrade Pass→Fail → cek pending balik "Menunggu" + penanda Origin="Exam" dihapus.
    3. Kalau UAT butuh seed skenario di DB lokal: snapshot dulu (SEED_WORKFLOW) + journal entry; restore setelah UAT (sukses/gagal) + tandai journal "cleaned".
    Catatan: task 3 (checkpoint) yang menjalankan UAT manusia; task ini menyiapkan checklist + memastikan suite hijau.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String "Build succeeded|error"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String "Passed!|Failed!"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/360-bypass-backend-b/360-UAT.md` ada dengan 6 skenario U1-U6 (grep "CL-B(b)" + "Batal pending" + "Re-grade")
    - U3 mencakup cek TempData + showAttachPackageReminder (W-12): `grep -n "TempData\|showAttachPackageReminder" .planning/phases/360-bypass-backend-b/360-UAT.md` → ≥1
    - U5 menyatakan 'hapus'='Dibatalkan' D-14 (I-03): `grep -n "Dibatalkan\|D-14" .planning/phases/360-bypass-backend-b/360-UAT.md` → ≥1
    - `dotnet build` "Build succeeded" 0 error
    - `dotnet test` (full suite, termasuk Integration) → "Passed!" 0 Failed
    - Bila ada seed UAT: entry SEED_JOURNAL.md ada dengan rencana restore
  </acceptance_criteria>
  <done>360-UAT.md siap (6 skenario, U3 cek TempData+flag W-12, U5 nyatakan hapus=Dibatalkan I-03); full suite + build hijau (phase gate).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT manusia @ localhost:5277 — 4 closure mode + pending + batal + re-grade</name>
  <files>.planning/phases/360-bypass-backend-b/360-UAT.md, docs/SEED_JOURNAL.md</files>
  <read_first>
    - .planning/phases/360-bypass-backend-b/360-UAT.md
    - docs/SEED_WORKFLOW.md
    - reference_dev_credentials (admin@pertamina.com login lokal untuk UAT /Admin /ProtonData)
  </read_first>
  <what-built>
    Backend Bypass Tahun lengkap: migration#2 (PendingProtonBypass 12 kolom + Origin), ProtonBypassService (4 closure mode + confirm + cancel + hook), 6 endpoint, gate exempt, notif PROTON_BYPASS_READY. UI penuh = Phase 361 (UAT ini verifikasi backend via endpoint/SQL).
  </what-built>
  <action>
    Checkpoint manusia — jalankan UAT 6 skenario sesuai `.planning/phases/360-bypass-backend-b/360-UAT.md`. Claude memandu via Playwright MCP @ localhost:5277 (AD off) + SQL cross-check; user mengonfirmasi tiap skenario PASS atau melaporkan issue. Snapshot DB sebelum seed + restore setelah (SEED_WORKFLOW). TIDAK ada eksekusi kode otomatis di task ini — verifikasi + konfirmasi manusia.
  </action>
  <how-to-verify>
    1. Jalankan AD off: `Authentication__UseActiveDirectory=false dotnet run` → buka `http://localhost:5277`, login admin/HC (admin@pertamina.com).
    2. Ikuti `360-UAT.md` skenario U1-U6 (CL-A/B(a)/B(b)/C + batal pending + re-grade fail). Untuk POST endpoint tanpa UI penuh: gunakan request manual (devtools/Playwright MCP) dengan AntiForgery token + cross-check DB (sqlcmd) per langkah checklist.
    3. Verifikasi kunci: (a) CL-B(b) bikin pending "Menunggu" + sesi bare tanpa paket (UserId+AssessmentType ter-set) + reminder TempData + flag Json; (b) exam lulus → pending "Siap" + notif HC; (c) konfirmasi → pindah + target ber-deliverable progress (bootstrap) + "Selesai"; (d) batal pending → "Dibatalkan" + exam dibatalkan/dipertahankan sesuai status (session Completed-gagal TIDAK ter-overwrite); (e) re-grade fail → pending balik "Menunggu".
    4. SNAPSHOT DB sebelum seed UAT + RESTORE setelah (SEED_WORKFLOW) — jangan tinggalkan seed temporary.
  </how-to-verify>
  <verify>Manual: 6 skenario U1-U6 dikonfirmasi PASS oleh user (atau issue dilaporkan). Tidak ada automated command — verifikasi manusia di browser + SQL.</verify>
  <acceptance_criteria>
    - 6 skenario U1-U6 dijalankan; tiap langkah PASS atau finding tercatat
    - DB lokal di-restore bersih setelah UAT (SEED_JOURNAL "cleaned")
    - Resume signal diterima dari user
  </acceptance_criteria>
  <done>6 skenario UAT U1-U6 dikonfirmasi PASS oleh user; DB lokal di-restore bersih (SEED_JOURNAL "cleaned").</done>
  <resume-signal>Ketik "approved" bila 6 skenario PASS, atau jelaskan issue yang ditemukan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT seed → DB lokal | Seed temporary tak boleh menetap; snapshot/restore wajib |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-28 | Tampering | gate exempt regresi | mitigate | Integration test mengunci: Bypass lolos cross-year + gate 100% TETAP + normal tetap keblok (D-05); cegah celah eligibility |
| T-360-29 | Denial of Service | seed UAT kotori DB | mitigate | Snapshot BACKUP sebelum seed + RESTORE setelah (SEED_WORKFLOW); journal "cleaned" |
| T-360-30 | Elevation of Privilege | UAT bypass via endpoint | accept | UAT pakai akun admin/HC sah (reference_dev_credentials); endpoint auth sudah diverifikasi plan 07 |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~ProtonYearGate"` hijau (exempt + gate 100% + regresi).
- `dotnet build` 0 error + full suite hijau.
- 360-UAT.md 6 skenario PASS (human checkpoint); U3 cek TempData+flag (W-12); U5 nyatakan hapus=Dibatalkan (I-03).
- DB lokal restore bersih.
</verification>

<success_criteria>
- Integration test exempt Origin="Bypass" (cross-year skip, gate 100% tetap) hijau.
- Full suite + build hijau; 360-UAT.md siap + UAT 6 skenario PASS.
- SC1-SC4 phase 360 terverifikasi (instan, pending+notif+konfirmasi, batal, exempt+coach). SC3 'hapus'='Dibatalkan' (D-14) dinyatakan eksplisit (I-03).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/360-bypass-backend-b/360-08-SUMMARY.md`. Sertakan: hitungan test final, hasil UAT 6 skenario, catatan SC3 'hapus'='Dibatalkan' (I-03), dan IT notify reminder (migration#2 = PendingProtonBypass 12 kolom + ProtonTrackAssignment.Origin + schema drift AssessmentType B-05).
</output>
</content>
