# Phase 360 Plan Review — Merged Findings (checker + 3 adversarial lens)

Sumber: gsd-plan-checker, spec-fidelity, codebase-reality, exec-safety (4/4 selesai).

## BLOCKER (6)

### B-01 [gsd-plan-checker] Plan 07 hilang dependency pada Plan 06 (dependency_correctness)
- **Plan:** 07 (dan 08 transitif)
- **Detail:** Plan 07 (Wave 6) mendeklarasikan depends_on: [05], tapi tidak mencantumkan Plan 06. Plan 05 dan Plan 06 sama-sama Wave 5 (paralel, keduanya depend on [04]). Plan 06 memasang 4 hook penting di GradingService + FinalizeEssayGrading (Pitfall 2 — essay CL-B(b)). Jika Plan 07 dieksekusi setelah Plan 05 selesai tapi Plan 06 belum, maka: (a) endpoint BypassSave dll. sudah ada, (b) hook GradingService belum terpasang. Plan 08 UAT U3 (CL-B(b): worker ujian lulus → pending 'Siap' + notif HC) menguji jalur GradingService hook; tanpa hook Plan 06, skenario ini AKAN gagal — tapi Plan 08 hanya depends_on: [07], sehingga Plan 06 bisa belum selesai saat UAT berjalan.
- **Evidence:** Plan 07 frontmatter: `depends_on: [05]` (bukan [05, 06]). Plan 06 frontmatter: `depends_on: [04]`, wave: 5 — sama dengan Plan 05. Plan 08 frontmatter: `depends_on: [07]`. RESEARCH.md Pitfall 2: 'Hook notif harus di 3 titik (essay early-return)' → harus ada di FinalizeEssayGrading (:3754) yang ditangani Plan 06 Task 2.
- **Suggested fix:** Ubah Plan 07 frontmatter: `depends_on: [05, 06]`. Ini memaksa Plan 07 menunggu Plan 06 selesai sebelum dikerjakan, sehingga semua 4 hook GradingService terpasang sebelum endpoint diekspos ke UAT. Plan 08 tetap `depends_on: [07]` (06 sudah tercakup transitif).

### B-02 [gsd-plan-checker] RESEARCH.md Open Questions tidak ditandai RESOLVED (research_resolution)
- **Plan:** 360-RESEARCH.md (phase level)
- **Detail:** RESEARCH.md memiliki section `## Open Questions` tanpa suffix `(RESOLVED)`. Per Dimension 11, ini adalah blocking issue. Ketiga pertanyaan secara substansi sudah diselesaikan di level planning (Q1 di Plan 03 Task 2 action 'KEPUTUSAN D-14 di-baking planner: cancel = set Status=Dibatalkan'; Q2 di Plan 04 interfaces 'defaults baked'; Q3 di Plan 06 'GradingService → ProtonBypassService satu arah'), tapi section RESEARCH.md belum diberi suffix (RESOLVED) sehingga checker berikutnya dan executor tidak tahu status resolusi.
- **Evidence:** 360-RESEARCH.md baris 372-387: `## Open Questions` (tanpa suffix RESOLVED). Tiga pertanyaan terdaftar: (1) Definisi konkret 'cancel exam aktif S', (2) Default jadwal/durasi/KKM, (3) GradingService circular DI. Tidak ada marker RESOLVED inline per pertanyaan.
- **Suggested fix:** Update 360-RESEARCH.md section header ke `## Open Questions (RESOLVED)` dan tambah marker RESOLVED per pertanyaan: (1) RESOLVED: set Status='Dibatalkan' (Plan 03 Task 2 D-14); (2) RESOLVED: DurationMinutes=req.DurationMinutes??60, PassPercentage=70, Schedule=now (Plan 04); (3) RESOLVED: satu arah GradingService→ProtonBypassService, build memverifikasi (Plan 06).

### B-03 [spec-fidelity] Validasi CL-A dilemahkan: syarat 'final ada' hilang — spec minta allApproved+final, plan cuma cek allApproved
- **Plan:** 360-03-PLAN.md (Task 1) + 360-07-PLAN.md (Task 1)
- **Detail:** Spec §5 VALIDASI: 'mode CL-A → S komplit (allApproved+final), else tolak'; §4 baris CL-A Syarat: 'allApproved + final ada'. Plan 03 Task 1 hanya menguji 'mode CL-A + flag SComplete=false → invalid' dan menulis aturan 'CL-A → SourceComplete wajib true' — SourceHasFinal tidak pernah disyaratkan true untuk CL-A (hanya dipakai untuk MENOLAK CL-B via D-D). Plan 07 Task 1 lalu mendefinisikan eksplisit 'SourceComplete (allApproved)' di BypassDetail dan 'eligibleModes: CL-A jika komplit'. Konsekuensi kalau executor ikut definisi ini: CL-A lolos validasi untuk worker yang deliverable 100% tapi penanda ProtonFinalAssessment belum ada → step §5.1 CL-A '[tutup tahun asal] = no-op (nilai S sudah ada)' jadi salah premis → worker pindah tahun TANPA penanda Lulus tahun asal terbit → tahun asal selamanya tidak tercatat 'Lulus' (definisi spec §2: allDeliverableApproved && ProtonFinalAssessment ada), merusak histori + graduation chain. Padahal kasus persis ini yang spec arahkan ke CL-B(a).
- **Evidence:** Spec docs/superpowers/specs/2026-06-09-proton-bypass-tahun-design.md §5 ('mode CL-A → S komplit (allApproved+final), else tolak') + §4 tabel CL-A. Plan 360-03-PLAN.md Task 1 <behavior>: 'Test: mode CL-A + flag SComplete=false → invalid' dan <action>: 'CL-A → SourceComplete wajib true. CL-B(a)/CL-B(b) → SourceHasFinal wajib false' (tanpa aturan CL-A→SourceHasFinal==true). Plan 360-07-PLAN.md Task 1 butir 5: 'SourceComplete (allApproved) + SourceHasFinal → { ..., eligibleModes[] } ... (CL-A jika komplit; ...)'.
- **Suggested fix:** Di BypassValidator (plan 03): tambah aturan 'mode CL-A → SourceComplete && SourceHasFinal, else tolak (arahkan CL-B(a))' + 1 [Fact] baru 'CL-A + SourceHasFinal=false → invalid'. Di plan 07 BypassDetail: eligibleModes CL-A = sourceComplete && sourceHasFinal. Selaras spec §4/§5 tanpa perubahan struktur lain.

### B-04 [spec-fidelity] Validasi E8 'tepat 1 assignment aktif' tidak dieksekusi di jalur CL-B(b) — hanya di ExecuteInstant
- **Plan:** 360-04-PLAN.md (Task 1), akar di 360-03-PLAN.md (Task 1)
- **Detail:** Spec §5 VALIDASI berlaku untuk SEMUA mode di entry BYPASS(): 'worker punya tepat 1 assignment aktif (E8)'. PBYP-02 juga eksplisit: 'validasi |Δtahun|≤1 + 1-assignment-aktif'. Plan 03 Task 1 secara eksplisit memindahkan cek E8 keluar dari predikat pure: 'Cek "tepat 1 assignment aktif" (E8) di-handle di method async ExecuteInstant (butuh DB), BUKAN di predikat pure'. Tapi plan 04 BypassSaveAsync men-dispatch mode CL-B(b) ke ExecutePendingBypassAsync — yang TIDAK pernah diinstruksikan menjalankan cek E8 (langkah resolve di BypassSaveAsync hanya 'Resolve flag (assignment aktif source, tahun source/target, SourceComplete, SourceHasFinal)' — tidak ada cek count==1). Hasil: worker dengan dobel assignment aktif bisa bikin pending CL-B(b); saat ConfirmBypass, D-11 hanya cek source masih aktif (AnyAsync, bukan exactly-1) → pindah dieksekusi, source dideactivate, target dibuat, assignment liar kedua tetap aktif → worker berakhir dengan 2 assignment aktif, mematahkan jaminan E8 spec §11 ('bypass selalu deactivate asal → dijamin 1 aktif'). Validasi spec ter-drop untuk 1 dari 4 mode.
- **Evidence:** Spec §5 VALIDASI butir 'worker punya tepat 1 assignment aktif (E8)' + §11 E8. REQUIREMENTS.md PBYP-02. Plan 360-03-PLAN.md Task 1 <action>: 'Cek "tepat 1 assignment aktif" (E8) di-handle di method async ExecuteInstant (butuh DB)'. Plan 360-04-PLAN.md Task 1 <action> butir 1 (resolve tanpa E8) + butir 2 ExecutePendingBypassAsync (tidak ada langkah E8). Plan 360-05-PLAN.md Task 1 D-11(a) hanya AnyAsync sourceStillActive.
- **Suggested fix:** Pindahkan cek E8 ke langkah resolve BypassSaveAsync (sebelum dispatch, berlaku semua mode): hitung assignment aktif worker; ≠1 → tolak 'Worker punya X assignment aktif'. Tambah integration test 'E8_DobelAssignment_Tolak' (mode CL-B(b)) di plan 04 Task 3. ExecuteInstant boleh tetap re-cek (defense-in-depth).

### B-05 [exec-safety] Bare AssessmentSession CL-B(b) defaults omit UserId dan AssessmentType — insert GAGAL di DB nyata (NOT NULL) tapi integration test FALSE-GREEN karena schema drift
- **Plan:** 360-04 (Task 1) + 360-08 (U3)
- **Detail:** Plan 04 <interfaces> 'Bare AssessmentSession CL-B(b) defaults (baked)' hanya menyebut Category/ProtonTrackId/TahunKe/Title/Schedule/DurationMinutes/PassPercentage/Status/GenerateCertificate/CreatedBy/IsTokenRequired. TIDAK ada `UserId = req.CoacheeId` dan TIDAK ada `AssessmentType`. Verifikasi DB lokal (sqlcmd read-only): AssessmentSessions.AssessmentType is_nullable=0 (NOT NULL), UserId is_nullable=0. CLR model: `public string? AssessmentType { get; set; }` (Models/AssessmentSession.cs:155) dan EF snapshot TANPA IsRequired (ApplicationDbContextModelSnapshot.cs:383-384). Konsekuensi: (a) di HcPortalDB_Dev nyata, insert bare session → SqlException 515 NULL AssessmentType → ExecutePendingBypassAsync SELALU throw → CL-B(b) mati total saat UAT; (b) integration test plan 04 pakai disposable DB hasil MigrateAsync (kolom nullable per EF model) → insert NULL sukses → test HIJAU PALSU; (c) kalaupun AssessmentType lolos, UserId='' (CLR default) → worker tidak pernah melihat exam (CMPController:208 `Where(a => a.UserId == userId)`), penanda GradingService EnsureAsync(session.UserId='') gagal diam-diam → pending nyangkut 'Menunggu' selamanya. Pola existing CreateAssessment eksplisit set keduanya dengan komentar ISS-04 'DB column NOT NULL, must set explicit value (EF tidak memakai DB default)' (AssessmentAdminController.cs:1434-1436).
- **Evidence:** 360-04-PLAN.md <interfaces> 'Bare AssessmentSession CL-B(b) defaults' (line 68-73, tidak ada UserId/AssessmentType); AssessmentAdminController.cs:1434 `UserId = userId` + :1435 `AssessmentType = "Standard", // ISS-04 fix: DB column NOT NULL`; sqlcmd HcPortalDB_Dev: UserId/AccessToken/AssessmentType semua is_nullable=0; Migrations/ApplicationDbContextModelSnapshot.cs:383-384 AssessmentType tanpa IsRequired; Models/AssessmentSession.cs:155 `string? AssessmentType`.
- **Suggested fix:** Tambah ke defaults plan 04: `UserId = req.CoacheeId` dan `AssessmentType = "Standard"` (ikuti analog AssessmentAdminController.cs:1417-1438; AccessToken aman karena CLR default ""). Tambah assert integration test: bare session `UserId == CoacheeId` dan `AssessmentType != null`. Catat drift schema AssessmentType (DB NOT NULL vs EF nullable) sebagai temuan terpisah untuk IT.

### B-06 [exec-safety] D-E create-baru + bootstrap tanpa guard duplikasi → ProtonDeliverableProgress dobel untuk track yang pernah dijalani (CL-C turun = kasus utama U4) → worker PERMANEN tidak eligible
- **Plan:** 360-03 (Task 2 step 4-5), 360-05 (Task 1), 360-02 (Task 1)
- **Detail:** Plan 03 step (4) selalu `create ProtonTrackAssignment` baru (sesuai spec D-E), lalu step (5) bootstrap SEMUA deliverable unit target via helper plan 02. Helper (salinan verbatim :1449-1492) insert progress untuk SEMUA deliverableIds tanpa cek existing. Skenario mainline CL-C turun (Tahun 2 → Tahun 1, unit sama): worker PASTI punya assignment Tahun-1 lama (inactive) + N progress rows lama (Approved). Bootstrap menambah N rows baru (Pending) → coachee punya 2N progress untuk unitDeliverableIds yang sama. Gate 100% (AssessmentAdminController.cs:1386-1390) dan GetEligibleCoachees (CoachMappingController.cs:1403-1407) menghitung progress per CoacheeId TANPA filter assignment → `myStatuses.Count = 2N != N` → CoacheeEligibilityCalculator.IsEligiblePerUnit return false SELAMANYA (Helpers/CoacheeEligibilityCalculator.cs:17 `if (myProgressStatuses.Count != expectedCount) return false`). Force-approve via Tab1 pun tidak menolong (count tetap 2N). Recovery hanya via SQL manual. Kode existing justru sudah punya guard ini — FIX-02 di CoachMappingController.cs:584-605 me-reuse assignment inactive 'instead of creating a duplicate' dan SKIP bootstrap ('existing ProtonDeliverableProgress rows are already linked'). Spec :179 mengklaim D-E 'aman' tapi hanya soal unique constraint (AssignmentId, DeliverableId), BUKAN soal counting gate. Tidak ada integration test maupun langkah UAT (U4 hanya cek 'deactivate source + target active') yang menyentuh re-entry ke track yang pernah dijalani → lolos semua gate verifikasi phase.
- **Evidence:** 360-03-PLAN.md Task 2 step (4)-(5); spec 2026-06-09-proton-bypass-tahun-design.md:79 '[aktifkan target T] create ProtonTrackAssignment baru (D-E)' + :179 'ProtonDeliverableProgress unique (AssignmentId, DeliverableId) → aman krn assignment baru'; CoachMappingController.cs:584-605 (FIX-02 reuse-inactive, anti-duplikat); AssessmentAdminController.cs:1386-1390 (count per CoacheeId tanpa scoping assignment); Helpers/CoacheeEligibilityCalculator.cs:15-19; 360-08-PLAN.md U4 (tidak assert eligibility pasca CL-C).
- **Suggested fix:** Minimal di plan 02/03: helper bootstrap exclude deliverableIds yang SUDAH punya ProtonDeliverableProgress untuk coachee tsb (`!context.ProtonDeliverableProgresses.Any(p => p.CoacheeId == coacheeId && p.ProtonDeliverableId == dId)`) — D-E tetap dihormati (assignment baru fresh) tapi counting gate konsisten. Alternatif: reuse-inactive ala FIX-02 + set Origin="Bypass" pada row reactivated (perlu klarifikasi user karena menyimpang D-E). Tambah integration test plan 03: CL-C turun ke track yang pernah dijalani → assert count progress per (coachee, unit-deliverables) tetap N dan IsEligiblePerUnit tetap bisa true.

## WARNING (14)

### W-01 [gsd-plan-checker] VALIDATION.md tidak diisi (nyquist_compliance — Wave 0 incomplete)
- **Plan:** 360-VALIDATION.md (phase level)
- **Detail:** 360-VALIDATION.md ada di direktori tapi berisi template kosong: `nyquist_compliant: false`, `wave_0_complete: false`, Per-Task Verification Map diisi placeholder '(diisi planner dari...)', Wave 0 Requirements hanya '(diisi planner)'. Setiap plan task memang memiliki `<automated>` verify, tapi VALIDATION.md tidak secara eksplisit mendokumentasikan sampling continuity, wave 0 gaps, dan sign-off. RESEARCH.md Validation Architecture mendefinisikan test requirements lengkap (PBYP-01..07 mapping, wave 0 gaps ProtonBypassServiceTests.cs) tapi tidak ditransfer ke VALIDATION.md.
- **Evidence:** 360-VALIDATION.md: `nyquist_compliant: false`, `wave_0_complete: false`, tabel task map berisi satu baris placeholder. Plans 03/04 Task 3 memang membuat ProtonBypassServiceTests.cs (bukan Wave 0 terpisah), tapi Wave 0 di VALIDATION.md tidak didokumentasikan. Sampling sign-off checklist semua belum dicentang.
- **Suggested fix:** Isi VALIDATION.md Per-Task Verification Map dari RESEARCH.md §Validation Architecture (copy data baris PBYP-01..07). Catat bahwa Wave 0 test creation digabung ke Plan 03 Task 3 dan Plan 04 Task 3 (bukan plan terpisah). Set `wave_0_complete: true` (test creation ada di plan tasks). Centang Sampling continuity checklist. Ubah `nyquist_compliant: true` setelah verifikasi: tidak ada 3 task berturutan tanpa automated verify (konfirmasi sesuai urutan task).

### W-02 [gsd-plan-checker] ConfirmBypassAsync tidak punya acceptance criteria untuk verifikasi bootstrap target (task_completeness)
- **Plan:** 05, task 1
- **Detail:** Plan 05 Task 1 (ConfirmBypassAsync §5.3) menyebutkan di action text 'Re-use step §5.1 (deactivate source + activate target + bootstrap + coach + audit)' — bootstrap deliverable target disertakan. Namun acceptance criteria tidak memuat grep check yang memverifikasi `ProtonDeliverableBootstrap.CreateProgressAsync` dipanggil di body `ConfirmBypassAsync`. Jika executor melewatkan bootstrap saat implement confirm (hanya deactivate+activate+coach+audit), worker bypass CL-B(b) yang dikonfirmasi tidak akan punya deliverable progress untuk tahun tujuan — gate 100% tidak akan pernah tercapai.
- **Evidence:** Plan 05 Task 1 acceptance_criteria: `grep D-11 sourceStillActive/examPassed`, `D-12 ExecuteUpdateAsync`, `EnsureAsync tidak ada`, `excludeSessionId`. TIDAK ADA: `grep 'ProtonDeliverableBootstrap.CreateProgressAsync' Services/ProtonBypassService.cs` di body ConfirmBypassAsync. Tapi Plan 03 Task 2 memiliki acceptance_criteria yang cek ini untuk ExecuteInstantBypassAsync.
- **Suggested fix:** Tambah acceptance criterion di Plan 05 Task 1: `grep -n 'ProtonDeliverableBootstrap.CreateProgressAsync' Services/ProtonBypassService.cs` → ≥2 match (1 di ExecuteInstantBypassAsync dari plan 03, 1 di ConfirmBypassAsync / helper privat MoveAssignmentAsync). Ini memastikan bootstrap target juga dijalankan saat HC konfirmasi CL-B(b).

### W-03 [spec-fidelity] Cancel pending: branch 'belum kerjakan' ditentukan dari pending.Status saja — exam yang SUDAH dikerjakan-tapi-gagal ikut di-overwrite 'Dibatalkan'
- **Plan:** 360-05-PLAN.md (Task 2)
- **Detail:** Spec §8.1 branch pertama: 'worker BELUM kerjakan exam → cancel/hapus session'. Plan 05 <interfaces> sendiri menulis kriteria benar ('UserPackageAssignment.IsCompleted==false / belum ada'), tapi Task 2 implementasinya menjatuhkan kriteria itu menjadi `workerLulus = pending.Status == "Siap"` lalu else-branch men-set AssessmentSession.Status="Dibatalkan" tanpa guard. Kasus MISS-2 (worker sudah mengerjakan dan GAGAL → session Status="Completed", IsPassed=false, pending tetap "Menunggu"): HC klik Batal → session yang sudah Completed di-overwrite jadi "Dibatalkan", menghapus jejak bahwa worker pernah mengerjakan + gagal. Itu bukan 'belum kerjakan' menurut spec. Inkonsistensi internal plan (interface vs task) = ambiguitas yang hampir pasti diselesaikan executor dengan versi Task 2 yang lebih konkret.
- **Evidence:** Spec §8.1 ('worker belum kerjakan exam → cancel/hapus session') + §11 MISS-2. Plan 360-05-PLAN.md <interfaces> ('worker BELUM kerjakan exam (UserPackageAssignment.IsCompleted==false / belum ada)') vs Task 2 <action> (`bool workerLulus = pending.Status == "Siap"` + else-branch ExecuteUpdate Status="Dibatalkan" tanpa cek Completed).
- **Suggested fix:** Di else-branch CancelPendingAsync, tambahkan guard: hanya set "Dibatalkan" bila session belum dikerjakan/selesai (mis. `.Where(s => s.Id==linkedId && s.Status != "Completed")` atau cek UserPackageAssignment.IsCompleted sesuai interface). Session Completed-gagal dibiarkan apa adanya (hanya rencana pindah yang batal). Tambah 1 test 'Cancel_SudahKerjakanGagal_SessionTetapCompleted'.

### W-04 [spec-fidelity] D-16 null-coach + ganti unit: mapping AssignmentUnit lama bikin gate 100% target-year resolve unit BEDA dari bootstrap — klaim mitigasi T-360-05 overstated
- **Plan:** 360-02-PLAN.md / 360-03-PLAN.md (lintas-plan, interaksi D-16 × D-05)
- **Detail:** Plan 03 step (6) implement D-16 setia ke CONTEXT: TargetCoachId null → 'JANGAN sentuh mapping'. Tapi gate deliverable 100% di CreateAssessment (yang per D-05 TETAP berlaku) me-resolve unit dari CoachCoacheeMapping aktif (AssessmentAdminController.cs:1375-1378), BUKAN dari assignment. Skenario: bypass pindah unit (TargetUnit baru dari form) sambil mempertahankan coach (TargetCoachId=null) → bootstrap bikin progress untuk unit BARU (PBYP-05 benar), tapi mapping aktif masih menyimpan AssignmentUnit LAMA → gate 100% menghitung deliverable unit lama vs progress unit baru → worker tak akan pernah eligible 100% di tahun tujuan — persis bencana Pitfall 3 lewat pintu lain. Plan 02 threat T-360-05 mengklaim mismatch termitigasi ('filter .Trim() identik → tak ada mismatch') — itu hanya benar untuk konsistensi filter, bukan untuk SUMBER unit. Tidak ada task/test yang menutup kombinasi ini; UAT U1-U6 juga tidak menguji ganti-unit + pertahankan-coach. Ini gap interaksi spec (E7+D-16+D-05) yang executor tidak bisa selesaikan sendiri dari plan.
- **Evidence:** Plan 360-03-PLAN.md Task 2 step (6) ('else { JANGAN sentuh mapping }'); CONTEXT D-16; AssessmentAdminController.cs:1375-1378 (resolvedUnit dari CoachCoacheeMappings aktif, fallback User.Unit); Plan 360-02-PLAN.md threat T-360-05; Plan 360-08-PLAN.md U1-U6 (tidak ada skenario ganti-unit+null-coach).
- **Suggested fix:** Eskalasi keputusan kecil ke user ATAU baking eksplisit: saat TargetCoachId==null TAPI TargetUnit ≠ AssignmentUnit mapping aktif, update field AssignmentUnit mapping existing (tanpa deactivate/recreate — tidak melanggar E15 maupun semangat D-16 'pertahankan coach'). Minimal: tambah test/assert + catatan di plan 03 supaya verifier sadar kombinasi ini.

### W-05 [spec-fidelity] Skema PendingProtonBypass melebar dari spec §6: kolom ke-13 'Mode' tidak ada di spec + inkonsisten dengan klaim '12 kolom' di plan sendiri
- **Plan:** 360-01-PLAN.md (Task 1)
- **Detail:** Spec §6 mendefinisikan tabel persis 12 kolom (Id..ResolvedAt) — tidak ada 'Mode'. Plan 01 menambah `public string Mode` ('CL-A|CL-B(a)|CL-B(b)|CL-C') sehingga model 13 kolom, sementara must_haves truth plan yang sama berbunyi 'Tabel PendingProtonBypasses ada di DB lokal dengan 12 kolom per spec §6' dan Task 2 menyebut 'CreateTable ... dengan 12 kolom'. Selain deviasi skema diam-diam (D-04 sudah memperingatkan migration#2 melebar dan harus dilaporkan presisi ke IT — kolom ekstra tak terdokumentasi memperburuk), kolom ini dead-generality: hanya jalur CL-B(b) yang insert pending (plan 04 dispatch), jadi Mode selalu 'CL-B(b)'. Acceptance sqlcmd 'Cols ≥ 12' menutupi drift ini.
- **Evidence:** Spec §6 tabel 12 kolom (tanpa Mode). Plan 360-01-PLAN.md <interfaces> field `public string Mode { get; set; }` vs must_haves truths 'dengan 12 kolom per spec §6' vs Task 2 action butir 4 'dengan 12 kolom'; Plan 360-04-PLAN.md Task 1 (hanya ExecutePendingBypassAsync yang insert pending, Mode="CL-B(b)").
- **Suggested fix:** Pilih satu: (a) drop kolom Mode (ikuti spec §6 verbatim; BypassPendingList tak butuh — spec §9 panel hanya nama/S→T/hasil exam), atau (b) pertahankan tapi perbaiki semua klaim '12 kolom' jadi 13, dokumentasikan sebagai deviasi sadar di SUMMARY + IT-notify migration#2.

### W-06 [codebase-reality] Bare AssessmentSession 'baked defaults' omit UserId — CL-B(b) session would be orphaned if executor follows the list literally
- **Plan:** 360-04-PLAN.md
- **Detail:** Plan 04 <interfaces> enumerates the bare-session defaults as baked Claude's-Discretion: Category, ProtonTrackId, TahunKe, Title, Schedule, DurationMinutes, PassPercentage, Status, GenerateCertificate, CreatedBy, IsTokenRequired — but NOT UserId. AssessmentSession.UserId (Models/AssessmentSession.cs:10, non-null default "") is the per-worker key: worker exam visibility filters on a.UserId (Controllers/CMPController.cs:213, WorkerController.cs:737/757), and the grading hook passes session.UserId into EnsureAsync (Services/GradingService.cs:307). With UserId="" the worker can never see/take the bypass exam, the Origin="Exam" penanda is never issued for the coachee, and ConfirmBypassAsync's D-11 re-check (examPassed + penandaExamAda) permanently fails — the whole CL-B(b) lifecycle dead-ends. Plan 04's own integration test (CL_BSatuB_BuatPending_BareSession_NoPackage) only asserts Category/Status/no-package, so it would pass; the bug surfaces only at UAT U3. Not a BLOCKER under the rubric (no nonexistent member is referenced — it is an omission), but it is the highest-impact gap found.
- **Evidence:** Models/AssessmentSession.cs:10 `public string UserId { get; set; } = "";`; Services/GradingService.cs:304-309 hook uses session.UserId; Controllers/CMPController.cs:213 worker query `a.Status == "Open" || a.Status == "Upcoming" ...` scoped per user; plan 04 lines 68-72 (interfaces defaults list) and Task 1 action step '[buat AssessmentSession bare] pakai defaults di interfaces'.
- **Suggested fix:** Add `UserId = req.CoacheeId` to the bare-session defaults list in plan 04 <interfaces> and to Task 1 action step 2; extend test 1 to assert `session.UserId == coacheeId`.

### W-07 [codebase-reality] Titik (b) exempt predicate at CoachMappingController :533 is unreachable dead code — cabang 1 already skips every coachee it could match
- **Plan:** 360-02-PLAN.md
- **Detail:** Plan 02 Task 2(b) replaces `bool isExemptFromCrossYear = false;` (CoachMappingController.cs:533 — anchor verified exact) with an AnyAsync on ProtonTrackAssignments where `a.ProtonTrackId == requestedTrack.Id && a.IsActive && a.Origin == "Bypass"`. But the loop's cabang 1 (lines 517-520 builds hasForRequestedTrack with NO IsActive filter; line 528 `if (hasForRequestedTrack.Contains(coacheeId)) continue;`) already skips any coachee who has ANY assignment — active or inactive — for the requested track. A bypass-origin active assignment for requestedTrack is strictly a subset of that set, so the new predicate can never evaluate on a coachee that reaches line 533. The D-06b intent (re-assign normal pasca-bypass not blocked) is in fact already satisfied by cabang 1, behavior is not broken, and all grep-based acceptance criteria will pass — but the plan ships unreachable code and its rationale ('exempt terisi') is illusory.
- **Evidence:** Controllers/CoachMappingController.cs:517-520 `hasForRequestedTrack` = assignments where `a.ProtonTrackId == req.ProtonTrackId.Value` (no IsActive filter); :528 `continue` cabang 1; :533 `bool isExemptFromCrossYear = false;` placeholder; plan 02 Task 2 action Titik (b).
- **Suggested fix:** Either document that cabang 1 already provides the D-06b exempt and keep the placeholder removal as a comment-only change, or make the predicate meaningful (e.g., exempt by Origin="Bypass" on the coachee's ACTIVE assignment regardless of track if that is the intended spec semantics) — decide against spec §D-06b before executing.

### W-08 [codebase-reality] Gate-100% resolves unit from active mapping (AssessmentAdminController:1375-1378), not from bypass form — D-16 'pertahankan coach' + unit change re-creates the exact mismatch T-360-05 claims to mitigate
- **Plan:** 360-02-PLAN.md + 360-03-PLAN.md
- **Detail:** Verified gate reality: the 100% deliverable gate resolves resolvedUnit from active CoachCoacheeMappings.AssignmentUnit with fallback User.Unit (AssessmentAdminController.cs:1375-1378), then filters deliverables by that unit (:1380-1383). Plan 03 step (6) per D-16: when TargetCoachId is null the old mapping (with OLD AssignmentUnit) is left untouched, while step (5) bootstraps ProtonDeliverableProgress for the FORM TargetUnit. If TargetUnit differs from the old mapping's unit, the worker's progress rows exist for unit-FORM but the gate computes eligibility against unit-LAMA → eligibility can never reach 100% (Pitfall 3 reborn at gate-time). Plan 02's threat T-360-05 asserts 'tak ada mismatch yang bikin gate gak pernah 100%' — that holds only because the .Trim() filter is identical, not because the unit source is reconciled. No §5 validation rule forces TargetCoachId (new mapping carrying AssignmentUnit=TargetUnit) when unit changes.
- **Evidence:** Controllers/AssessmentAdminController.cs:1375-1378 (unit from active mapping, fallback User.Unit) and :1380-1383 (unit filter); plan 03 Task 2 step (6) `else { JANGAN sentuh mapping }`; plan 02 threat T-360-05.
- **Suggested fix:** Add a validation rule in plan 03 BypassValidator/ExecuteInstant: if TargetUnit != current active mapping AssignmentUnit (trimmed) then require TargetCoachId non-null (or update the retained mapping's AssignmentUnit to TargetUnit inside the transaction).

### W-09 [codebase-reality] Hook points 2 and 3 in RegradeAfterEditAsync are brace-less single-statement ifs — naive 'insert after EnsureAsync/RemoveExamOriginAsync' drops the Proton guard
- **Plan:** 360-06-PLAN.md
- **Detail:** Plan 06 instructs inserting the hook 'di dalam if (...), SETELAH EnsureAsync/RemoveExamOriginAsync'. Hook point 1 (GradingService.cs:304-309) is a braced block — safe. But point 3 (Pass→Fail) is `if (session.Category == "Assessment Proton" && session.ProtonTrackId.HasValue)\n    await _protonCompletionService.RemoveExamOriginAsync(...);` at :485-486 (brace-less), and point 2 (Fail→Pass) is the brace-less if spanning :531-534. An executor pasting the new await after the existing statement without adding braces would compile fine but run the hook unconditionally inside the flip branch (for non-Proton sessions too — a benign no-op because the LinkedAssessmentSessionId lookup won't match, but it diverges from the plan's stated guard and from acceptance criterion 'di dalam if'). Line anchors themselves are exact.
- **Evidence:** Services/GradingService.cs:485-486 and :531-534 — both brace-less single-statement ifs; :304-309 braced block; plan 06 Task 1 steps 3-4.
- **Suggested fix:** Plan 06 Task 1 should explicitly say: convert the brace-less ifs at :485-486 and :531-534 to braced blocks before appending the hook call.

### W-10 [exec-safety] D-16 pertahankan mapping + TargetUnit beda dari AssignmentUnit lama → gate 100% & eligibility resolve unit dari MAPPING → mismatch unit, worker tidak pernah eligible
- **Plan:** 360-03 (Task 2 step 6) + 360-02
- **Detail:** Bootstrap pakai unit dari FORM (PBYP-05), tapi gate 100% (AssessmentAdminController.cs:1376-1380) dan GetEligibleCoachees (CoachMappingController.cs:1377-1383) resolve unit dari `CoachCoacheeMappings.Where(IsActive).Select(AssignmentUnit)` DULU. Jika HC bypass ganti unit dengan `TargetCoachId = null` (D-16: jangan sentuh mapping), mapping aktif tetap ber-AssignmentUnit LAMA → gate menghitung deliverable unit LAMA sedangkan progress dibuat untuk unit FORM → myStatuses kosong/mismatch → tidak pernah 100%. T-360-05 plan 02 hanya menjamin filter .Trim() identik — tidak menutup mismatch SUMBER unit. Tidak ada validasi `TargetUnit == mapping.AssignmentUnit` saat TargetCoachId null di plan manapun, dan tidak ada test kombinasi ini (test plan 03 #4 seeding-nya justru sengaja beda unit tapi hanya assert bootstrap, bukan gate).
- **Evidence:** 360-03-PLAN.md step (6) 'else { JANGAN sentuh mapping }'; AssessmentAdminController.cs:1376-1380 resolvedUnit dari active mapping; CoachMappingController.cs:1377-1383 sama; 360-02-PLAN.md T-360-05.
- **Suggested fix:** Di ExecuteInstant/Confirm: bila TargetCoachId null DAN TargetUnit != AssignmentUnit mapping aktif → update `mapping.AssignmentUnit = TargetUnit` (atau tolak di ValidateBypass dengan pesan minta pilih coach). Tambah integration test gate untuk kombinasi keep-coach + ganti-unit.

### W-11 [exec-safety] Wave 5 paralel: files_modified disjoint tapi build/test artifacts & working tree shared — verifikasi plan 06 bisa flaky karena state in-flight plan 05
- **Plan:** 360-05 + 360-06 (wave 5 paralel)
- **Detail:** Plan 05 (ProtonBypassService.cs — termasuk REFACTOR ExecuteInstantBypassAsync jadi MoveAssignmentAsync) dan plan 06 (GradingService.cs + AssessmentAdminController.cs) berjalan paralel di working tree yang sama. Setiap task kedua plan menjalankan `dotnet build` + `dotnet test` penuh: (a) MSBuild konkuren pada csproj sama di Windows rawan file-lock obj/bin; (b) gate `dotnet test Category!=Integration` plan 06 ikut meng-compile kode TDD plan 05 yang sedang RED/in-flight (plan 05 task 3 menulis test sebelum semua hijau) → false-fail; (c) commit interleaving dua agent di branch sama. Tidak ada konflik kompilasi semantik (plan 06 hanya butuh method plan 04), tapi gate verifikasi per-task tidak terisolasi.
- **Evidence:** 360-05-PLAN.md frontmatter wave:5 files_modified [Services/ProtonBypassService.cs, HcPortal.Tests/ProtonBypassServiceTests.cs]; 360-06-PLAN.md wave:5 files_modified [Services/GradingService.cs, Controllers/AssessmentAdminController.cs]; kedua plan <verify> menjalankan dotnet build+test penuh per task.
- **Suggested fix:** Serialkan 05 → 06 (geser 06 ke wave 6, 07 ke wave 7, 08 ke wave 8) — murah karena 06 cuma 2 task kecil; atau jalankan wave 5 dengan executor tunggal sekuensial.

### W-12 [exec-safety] D-02 'WAJIB pengingat TempData' tidak diimplementasi di 360 — plan 07 hanya meneruskan flag Json; UAT U3 mengecek 'reminder TempData' → false-fail di checkpoint
- **Plan:** 360-07 (Task 2) vs 360-08 (U3) vs D-02
- **Detail:** CONTEXT D-02 (locked): 'WAJIB pengingat TempData setelah BypassSave CL-B(b)'. Plan 04 menitipkan ke 'controller (plan 07) surface ke TempData', tapi plan 07 Task 2 hanya return `Json(new { ..., showAttachPackageReminder })` — tidak ada satupun task yang menulis TempData. Plan 08 UAT U3 berbunyi 'cek ... reminder TempData "lampirkan paket"' → langkah UAT tidak bisa PASS sebagaimana tertulis (tidak ada TempData yang di-set), padahal fitur backend benar. Risiko: checkpoint human-verify menghasilkan finding palsu, atau D-02 lolos tanpa tertagih sampai Phase 361.
- **Evidence:** 360-CONTEXT.md D-02; 360-04-PLAN.md Task 1 'D-02 ... controller (plan 07) surface ke TempData'; 360-07-PLAN.md Task 2 return Json saja (tidak ada TempData); 360-08-PLAN.md Task 2 U3 'reminder TempData "lampirkan paket"'.
- **Suggested fix:** Pilih satu: (a) plan 07 BypassSave set `TempData["Warning"]` saat result.ShowAttachPackageReminder (selain Json), atau (b) ubah wording U3 jadi 'cek field showAttachPackageReminder=true di response Json' + catat eksplisit D-02 TempData/toast dipenuhi Phase 361 (PBYP-08).

### W-13 [exec-safety] Create CoachCoacheeMapping baru tanpa AssignmentSection dan StartDate → StartDate=0001-01-01 + Section null merusak data/filtering
- **Plan:** 360-03 (Task 2 step 6)
- **Detail:** Plan 03 step (6) create mapping baru hanya `{ CoacheeId, CoachId=TargetCoachId, AssignmentUnit=TargetUnit, IsActive=true }`. Model CoachCoacheeMapping punya `DateTime StartDate` non-nullable (default(DateTime) = 0001-01-01 → data aneh di view coaching) dan `string? AssignmentSection` (null → filter/pengelompokan per-section di assign page & laporan bolong). Alur assign existing selalu set keduanya (CoachMappingController.cs:559-562 `StartDate = startDate, AssignmentSection = req.AssignmentSection!.Trim(), AssignmentUnit = ...`). Insert tetap sukses (build & test hijau) → data-quality bug senyap.
- **Evidence:** 360-03-PLAN.md Task 2 step (6); Models/CoachCoacheeMapping.cs:29 `public DateTime StartDate` non-nullable, :39 AssignmentSection; CoachMappingController.cs:559-562 analog set lengkap.
- **Suggested fix:** Tambahkan `StartDate = DateTime.UtcNow` + `AssignmentSection` (warisi dari mapping lama yang di-deactivate, atau resolve section dari TargetUnit via org helper) ke spesifikasi create mapping di plan 03 dan jalur confirm plan 05.

### W-14 [exec-safety] Sejumlah acceptance criteria tidak verifiable mekanis sebagaimana tertulis (grep char-class & block-scoped)
- **Plan:** 360-03/04/05/07 (acceptance_criteria)
- **Detail:** (1) `grep -c "[Fact]\|[Theory]"` / `grep -c "[ValidateAntiForgeryToken]"` — dalam regex, `[Fact]` adalah character class (cocok setiap baris yang mengandung huruf F/a/c/t) → hitungan selalu jauh melebihi threshold → kriteria SELALU lulus tanpa memverifikasi apa-apa (perlu `grep -cF '[Fact]'` atau escape). (2) Kriteria ber-scope blok seperti 'grep -c "context\|_context" ... di blok BypassValidator = 0' (plan 03) dan 'grep -c ProtonDeliverableList ... di method AutoCreateProgressForAssignment → 0' (plan 02) tidak bisa dieksekusi grep (grep tidak kenal scope method) — file CoachMappingController masih berisi ProtonDeliverableList di method lain (GetEligibleCoachees :1395) sehingga grep file-level pasti >0. Eksekutor harus interpretasi manual; gate otomatis melemah.
- **Evidence:** 360-03-PLAN.md AC Task 1 ('grep -c "[Fact]\|[Theory]" → ≥7', 'di blok BypassValidator = 0'); 360-04-PLAN.md AC Task 3 ('grep -c "[Fact]" → ≥10'); 360-07-PLAN.md AC Task 2 ('grep -c "[ValidateAntiForgeryToken]" → bertambah 3'); 360-02-PLAN.md AC Task 1; CoachMappingController.cs:1395 ProtonDeliverableList di GetEligibleCoachees.
- **Suggested fix:** Ganti ke fixed-string grep (`grep -cF '[Fact]'`) dan untuk kriteria ber-scope blok ganti dengan perintah verifiable (mis. `dotnet test --filter` count, atau grep pattern unik seperti nama method) / tandai eksplisit 'manual review'.

## INFO (9)

### I-01 [gsd-plan-checker] Plan 06 tidak di-cover pada fase UAT Plan 08 secara eksplisit (verification_derivation)
- **Plan:** 08
- **Detail:** Plan 08 UAT U3 mensyaratkan 'worker ujian lulus → cek pending Siap + notif HC PROTON_BYPASS_READY', yang bergantung pada hook GradingService (Plan 06). Namun Plan 08 hanya depends_on: [07]. Setelah fix dependency Plan 07 menjadi depends_on: [05,06], Plan 08 akan inherits dependency transitif ke Plan 06 melalui Plan 07 — sehingga UAT tidak perlu diubah. Ini informasi untuk memastikan fix dependency diprioritaskan.
- **Evidence:** Plan 08 Task 2 UAT checklist: 'U3 CL-B(b): ... HC pasang paket → worker ujian lulus → cek pending Siap + notif HC PROTON_BYPASS_READY. HC BypassConfirm → cek pindah + pending Selesai.' — membutuhkan GradingService hook (Plan 06). Plan 08 frontmatter `depends_on: [07]`.
- **Suggested fix:** Setelah Plan 07 diupdate menjadi depends_on: [05, 06], dependency transitif Plan 08 sudah mencakup Plan 06. Tidak ada perubahan pada Plan 08 yang diperlukan.

### I-02 [spec-fidelity] D-02 'WAJIB pengingat TempData' direalisasikan sebagai flag Json (showAttachPackageReminder), bukan TempData
- **Plan:** 360-07-PLAN.md (Task 2)
- **Detail:** CONTEXT D-02 locked: 'WAJIB pengingat TempData setelah BypassSave CL-B(b)'. Plan 04 mengembalikan ShowAttachPackageReminder + message reminder di BypassResult; plan 07 meneruskannya via Json response ('D-02 reminder diteruskan ke UI Phase 361'), bukan TempData. Untuk endpoint POST JSON/AJAX, TempData memang tidak akan pernah ter-render — adaptasi ini masuk akal dan intent D-02 (reminder pasti muncul) terjaga lewat message + flag, tapi secara harfiah menyimpang dari mekanisme yang dikunci. Harus dipastikan Phase 361 benar-benar men-surface flag ini, kalau tidak D-02 jadi no-op.
- **Evidence:** CONTEXT 360-CONTEXT.md D-02; Plan 360-04-PLAN.md Task 1 ('controller (plan 07) surface ke TempData' — janji yang lalu tidak ditepati); Plan 360-07-PLAN.md Task 2 butir 1 ('Return Json(... showAttachPackageReminder ...)') dan must_haves 'via TempData/Json'.
- **Suggested fix:** Catat deviasi sadar di 360-07-SUMMARY + tambahkan requirement note untuk Phase 361 (PBYP-08): UI wizard WAJIB menampilkan reminder saat showAttachPackageReminder=true.

### I-03 [spec-fidelity] 'Cancel/hapus' exam diimplementasikan sebagai soft-cancel Status="Dibatalkan" — sah per spec, tapi ROADMAP SC3/PBYP-06 berbunyi 'hapus'
- **Plan:** 360-03-PLAN.md / 360-05-PLAN.md
- **Detail:** Spec §8.1 menulis 'cancel/hapus session' (dua opsi) dan Research Open Q1 mendelegasikan definisi 'cancel' ke planner; plan 03 mem-baking D-14 = set Status="Dibatalkan" (reversible, no data loss) dan plan 05 memakainya konsisten. Ini keputusan dalam wewenang planner dan defensible. Namun teks ROADMAP SC3 dan REQUIREMENTS PBYP-06 berbunyi literal 'belum-dikerjakan→hapus' — verifier/UAT U5 yang membaca harfiah akan mengharapkan row session hilang, bukan berstatus 'Dibatalkan'. Juga 'Dibatalkan' adalah nilai Status baru di luar set existing {Open, Upcoming, Completed} — plan sudah memverifikasi tak bentrok, tapi listing/filter existing yang switch atas Status perlu spot-check saat eksekusi.
- **Evidence:** Spec §8.1 'cancel/hapus session'; ROADMAP.md Phase 360 SC3 '(belum-kerjakan→hapus, ...)'; REQUIREMENTS.md PBYP-06; Plan 360-03-PLAN.md 'KEPUTUSAN D-14 ... JANGAN hapus session/UserPackageAssignment'; Models/AssessmentSession.cs:20 (Status existing).
- **Suggested fix:** Di 360-08 UAT U5 + SUMMARY, nyatakan eksplisit bahwa 'hapus' dipenuhi sebagai soft-cancel Status="Dibatalkan" (keputusan D-14) supaya verifikasi SC3/PBYP-06 tidak false-fail.

### I-04 [codebase-reality] New CoachCoacheeMapping in step (6) omits StartDate (non-nullable) and AssignmentSection
- **Plan:** 360-03-PLAN.md
- **Detail:** Plan 03 step (6) creates `{ CoacheeId, CoachId=TargetCoachId, AssignmentUnit=TargetUnit, IsActive=true }`. CoachCoacheeMapping.StartDate is a non-nullable DateTime (Models/CoachCoacheeMapping.cs:29) → defaults to 0001-01-01, unlike the existing Assign flow which sets StartDate = req.StartDate ?? DateTime.Today and AssignmentSection (CoachMappingController.cs:553-563). Could break coaching-duration displays/sorting. Compiles and saves fine, so INFO.
- **Evidence:** Models/CoachCoacheeMapping.cs:29 `public DateTime StartDate { get; set; }` (no default); Controllers/CoachMappingController.cs:553-563 analog sets StartDate + AssignmentSection.
- **Suggested fix:** Add `StartDate = DateTime.Today` and carry AssignmentSection (copy from deactivated mapping or resolve from TargetUnit's section) in plan 03 step (6).

### I-05 [codebase-reality] Anchor description drift: 'coach deactivate→create' analog cited at CoachMappingController.cs:573-619 is actually ProtonTrackAssignment deactivate/reuse logic
- **Plan:** 360-03-PLAN.md
- **Detail:** Plan 03's context comment cites ':573-619 deactivate→create coach mapping E15'. In reality :573-581 deactivates ProtonTrackAssignments for a different track and :583-619 reuses/creates ProtonTrackAssignments; CoachCoacheeMapping creation is at :555-568 and active-coach duplication is REJECTED pre-transaction (:480-493), not deactivated-then-created. No existing code path performs 'deactivate old active coach mapping then create new' at the cited anchor. The E15 filtered-unique index (ApplicationDbContext.cs:322-330, name IX_CoachCoacheeMappings_CoacheeId_ActiveUnique :328, snapshot :703) and the DbUpdateException 2601/2627 catch analog (:628-639) ARE exact, so the intent is fully recoverable — the executor just won't find a verbatim coach-mapping swap to copy.
- **Evidence:** Controllers/CoachMappingController.cs:555-568 (mapping AddRange), :573-619 (assignment logic), :628-639 (catch analog); Data/ApplicationDbContext.cs:322-330.
- **Suggested fix:** Correct the analog note to 'pattern analog: assignment deactivate→create :573-619 + unique-index catch :628-639; coach-mapping swap is NEW code'.

### I-06 [codebase-reality] Plan 04 grep gate 'UserPackageAssignment count = 0 in ProtonBypassService.cs' can conflict with plan 05 interface text
- **Plan:** 360-04-PLAN.md + 360-05-PLAN.md
- **Detail:** Plan 04 acceptance: `grep -c "UserPackageAssignment" Services/ProtonBypassService.cs → 0` (D-01). Plan 05's <interfaces> §8.1 describes the cancel branch as 'UserPackageAssignment.IsCompleted==false / belum ada', and its read_first points to Models/UserPackageAssignment.cs. Plan 05 Task 2's actual implementation avoids the query (uses `workerLulus = pending.Status == "Siap"`), so there is no real conflict if followed precisely — but an executor implementing the interface prose literally would add a UserPackageAssignments query and silently invalidate plan 04's gate on any re-verification pass. Field IsCompleted exists (Models/UserPackageAssignment.cs:43).
- **Evidence:** Plan 04 Task 1 acceptance criteria; plan 05 interfaces §8.1 + Task 2 action (uses pending.Status, not UserPackageAssignment); Models/UserPackageAssignment.cs:43.
- **Suggested fix:** In plan 05, drop the UserPackageAssignment mention from §8.1 interface text (the status-based branch is the locked implementation) or scope plan 04's grep gate to ExecutePendingBypassAsync only.

### I-07 [codebase-reality] Codebase-reality sweep: every other file:line anchor, signature, column, and pattern verified EXACT against the repo
- **Plan:** all (01-08)
- **Detail:** Adversarial verification found the anchor set to be unusually precise. Verified exact: (1) ProtonCompletionService.EnsureAsync(string coacheeId, int protonTrackId, string createdById, string origin, string? notes)→Task<bool> at :36 with IsActive-filtered assignment resolve at :38-39 (Pitfall 1 premise true); RemoveExamOriginAsync :70; IsPrevYearPassedAsync :107. (2) GradingService ctor :23-33 (ProtonCompletionService param :27), essay early-return :229, ExecuteUpdateAsync+rowsAffected analog :234-251, hook 1 :304-309, RegradeAfterEditAsync :431. (3) CoachMappingController :530-541 exempt placeholder with hardcoded false at :533, requestedTrack/prevTrack in scope (:502/:506), tx+catch analog :565-646. (4) AutoCreateProgressForAssignment(int,int,string) :1424-1493 — sub-anchors :1428-1441 resolve-unit, :1449-1453 Trim filter, :1461 warning, :1465-1474 progress insert, :1477-1490 history — extraction as described is feasible; DbSet name ProtonDeliverableList correct. (5) NotificationService._templates :34-93, ASMT_RESULTS_READY :43-48, SendByTemplateAsync(string,string,Dictionary<string,object>?) :231-265, declared on INotificationService :60. (6) AssessmentSession bare-session fields all exist; Status comment :20 lists Open/Upcoming/Completed, PassPercentage=70 :30; "Dibatalkan" non-colliding and entry filters (CMPController :213) auto-exclude it; existing CreateAssessment seeds "Upcoming" (:1892/:1911) so plan defaults match convention. (7) ProtonTrackAssignment has NO Origin column today (Models/ProtonModels.cs:71-84; only ProtonFinalAssessment.Origin :226; sole Origin migration = 20260610014907 with the Sql UPDATE 'Interview' seed plan 01 warns about) — plan 01 correctly ADDS it. (8) AuditLogService.LogAsync(actorUserId, actorName, actionType, description, int? targetId=null, string? targetType=null) :21-27 — matches all plan call shapes. (9) E15 filtered-unique index ApplicationDbContext :322-330 / snapshot :703, name matches plan 03's catch filter string. (10) ProtonCompletionFixture :25-61 public IAsyncLifetime with public Options + MigrateAsync — reusable via IClassFixture exactly as plans 03/04/05/08 assume; ProtonYearGateIntegrationTests already reuses it. Also exact: AssessmentAdminController gate :1336-1392 (:1342/:1362/:1365/:1368/:1373-1389/:1382/:1387, gateSkippedPrevYear/NotHundred :1339), FinalizeEssayGrading hook block :3754-3759, ProtonDataController :70-77/:79/:88-100/:102-150/:152/:1397-1474/:1443-1465/:1469-1471, Override GET :210 (deep-link target exists), Program.cs AddScoped ProtonCompletionService :57, ApplicationUser.FullName/Unit, _context.Users available. PBYP-01..07 fully covered by plan frontmatter union; SubmitInterviewResults EnsureAsync (:3865) correctly excluded from hooks per D-03 (bypass source is always Tahun 1/2 online exam).
- **Evidence:** See per-item file:line citations in detail; all opened and read in this review.
- **Suggested fix:** -

### I-08 [exec-safety] Exempt D-06b di CoachMappingController adalah dead code — cabang 1 hasForRequestedTrack (assignment APAPUN, aktif/non-aktif) selalu men-skip worker bypass lebih dulu
- **Plan:** 360-02 (Task 2 titik b)
- **Detail:** Loop warning cross-year di CoachMappingController:528 men-skip coachee yang sudah punya assignment APAPUN (tanpa filter IsActive) untuk track yang diminta (`hasForRequestedTrack`). Worker hasil bypass PASTI punya assignment (Origin=Bypass) untuk track itu → sudah lolos di cabang 1, tidak pernah mencapai cek `isExemptFromCrossYear` baru. Predikat plan 02 (`CoacheeId==coacheeId && ProtonTrackId==requestedTrack.Id && IsActive && Origin=="Bypass"`) by construction false untuk semua coachee yang sampai ke baris itu. Tidak berbahaya (outcome 're-assign pasca-bypass tidak keblok' sudah tercapai via cabang 1), tapi D-06b tidak memberikan fungsi nyata; test/UAT yang mencoba membuktikan exempt-(b) secara behavioral tidak akan bisa membedakan.
- **Evidence:** Controllers/CoachMappingController.cs:516-520 (hasForRequestedTrack tanpa filter IsActive) + :528 'cabang 1 ... continue' + :533 placeholder; 360-02-PLAN.md Task 2 titik (b).
- **Suggested fix:** Terima sebagai defensive no-op (catat di SUMMARY plan 02), atau konfirmasi ke user apakah maksud D-06b adalah skenario lain (mis. cabang yang memfilter IsActive) sebelum eksekusi.

### I-09 [exec-safety] Dua catatan desain minor: D-10 check-then-act tanpa DB constraint (race dobel-pending 2 HC simultan), dan SC3/PBYP-06 'belum-dikerjakan→hapus' diimplementasi sebagai Status=Dibatalkan
- **Plan:** 360-04 (D-10) & 360-05 (PBYP-06 wording)
- **Detail:** (1) D-10 blok dobel pending = `AnyAsync` lalu insert, index sengaja non-unique (plan 01 task 4) → dua BypassSave bersamaan untuk worker sama bisa sama-sama lolos cek dan membuat 2 pending. D-12 hanya melindungi confirm, bukan save. Konsisten dengan keputusan app-level yang di-baking planner, tapi perlu disadari reviewer. (2) REQUIREMENTS PBYP-06 dan ROADMAP SC3 menulis 'belum-dikerjakan→hapus' exam; plan 05 CancelPendingAsync mengimplementasi set `Status="Dibatalkan"` (konsisten D-14, reversible) — deviasi sadar dari kata 'hapus'; verifier phase harus memetakan SC3 ke status-cancel agar tidak dianggap gap. Juga D-14 scope `Status != "Completed"` ikut membatalkan sesi 'Menunggu Penilaian' (essay pending grading) — dapat menghapus state penilaian yang sedang berjalan; sesuai semangat E5 tapi layak dikonfirmasi.
- **Evidence:** 360-04-PLAN.md Task 1 (AnyAsync hasActivePending); 360-01-PLAN.md Task 1 step 4 (index non-unique by design); .planning/REQUIREMENTS.md:31 PBYP-06 'hapus'; .planning/ROADMAP.md:107 SC3; 360-05-PLAN.md Task 2 (ExecuteUpdate Status=Dibatalkan); 360-03-PLAN.md step (2) D-14 `Status!="Completed"`; GradingService.cs:193-230 status PendingGrading.
- **Suggested fix:** Tidak wajib diubah. Opsional: catat eksplisit di 360-08 verifikasi bahwa 'hapus' = 'Dibatalkan' (D-14) supaya verifier/auditor milestone tidak menandai gap.

