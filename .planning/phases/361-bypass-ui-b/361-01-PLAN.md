---
phase: 361-bypass-ui-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/ProtonDataController.cs
autonomous: true
requirements: [PBYP-08, PBYP-09]
must_haves:
  truths:
    - "Override() GET menyediakan daftar coach aktif ke view (ViewBag.AllCoaches)"
    - "BypassPendingList mengembalikan skor exam, tanggal selesai exam, alasan, dan nama coach target per pending"
    - "Kontrak field BypassPendingList existing (id, coacheeId, nama, sourceTrack, targetTrack, targetUnit, status, hasilExam, createdAt) tetap utuh"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "ViewBag.AllCoaches di Override() + extended BypassPendingList select (D-18)"
      contains: "ViewBag.AllCoaches"
  key_links:
    - from: "Override() GET"
      to: "_userManager.GetUsersInRoleAsync(UserRoles.Coach)"
      via: "ViewBag.AllCoaches"
      pattern: "GetUsersInRoleAsync\\(UserRoles\\.Coach\\)"
    - from: "BypassPendingList"
      to: "AssessmentSession.Score / CompletedAt + PendingProtonBypass.Reason / TargetCoachId"
      via: "extended anonymous projection"
      pattern: "skorExam|tanggalExam|targetCoachNama"
---

<objective>
Siapkan dua dukungan backend minor (tanpa migration) yang dibutuhkan UI Tab2 Bypass Tahun:
1. **Coach dropdown source (D-12, Pitfall 2 Opsi A):** `Override()` GET set `ViewBag.AllCoaches` = daftar coach aktif. Wizard step 3 butuh `<select>` coach; saat ini `Override()` hanya set `AllTracks` + `SectionUnitsJson`.
2. **Extend `BypassPendingList` SELECT (D-18, Pitfall 4):** tambah skor exam, tanggal selesai exam, `Reason`, `TargetCoachId`, dan nama coach target (via LEFT JOIN Users) supaya modal "Lihat & Konfirmasi" bisa menampilkan detail lengkap.

Purpose: UI Phase 361 = lapisan tipis di atas backend 360 final. Dua sentuhan backend ini adalah SATU-SATUNYA perubahan controller yang disahkan (CONTEXT D-09 risiko coach + D-18). Query-select only — TIDAK ada migration, TIDAK ada perubahan kontrak field existing.
Output: `Controllers/ProtonDataController.cs` ter-extend di 2 method, build hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/361-bypass-ui-b/361-CONTEXT.md
@.planning/phases/361-bypass-ui-b/361-RESEARCH.md
@.planning/phases/361-bypass-ui-b/361-PATTERNS.md

<interfaces>
<!-- Kontrak yang dikonsumsi/diproduksi. Diekstrak dari codebase — executor tidak perlu eksplorasi ulang. -->

Kolom REAL terverifikasi (Pitfall 4 — JANGAN tulis nama lain):
- Models/AssessmentSession.cs:26  → public int? Score
- Models/AssessmentSession.cs:38  → public bool? IsPassed
- Models/AssessmentSession.cs:39  → public DateTime? CompletedAt
- Models/ProtonModels.cs:242      → public string? TargetCoachId   (PendingProtonBypass)
- Models/ProtonModels.cs:243      → public string Reason            (PendingProtonBypass)
- Models/ProtonModels.cs:244      → public int LinkedAssessmentSessionId (NON-nullable; join pakai DefaultIfEmpty() → s bisa null, guard s != null sudah ada)

Konstanta role coach (terverifikasi):
- Models/UserRoles.cs:24 → public const string Coach = "Coach";

Pola coach-list terbukti (verbatim idiom):
- Controllers/CoachMappingController.cs:146-149:
    var coachRoleUsers = await _userManager.GetUsersInRoleAsync(UserRoles.Coach);
    ViewBag.EligibleCoaches = coachRoleUsers.Where(u => u.IsActive).OrderBy(u => u.FullName).ToList();

ApplicationUser fields dipakai: Id (string), FullName (string?), UserName (string?), IsActive (bool).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah ViewBag.AllCoaches di Override() GET</name>
  <read_first>
    - Controllers/ProtonDataController.cs:230-243 (Override() GET — state existing, sebelum diubah)
    - Controllers/CoachMappingController.cs:146-149 (pola coach-list verbatim untuk ditiru)
    - Models/UserRoles.cs:24 (konstanta UserRoles.Coach)
    - .planning/phases/361-bypass-ui-b/361-RESEARCH.md (Pitfall 2 — pilihan Opsi A vs B, alasan A dipilih)
  </read_first>
  <action>
Di `Controllers/ProtonDataController.cs`, method `Override()` GET (`:231-243`), TEPAT SEBELUM baris `return View();` (`:242`), tambah blok:

```csharp
// D-12 (Pitfall 2 Opsi A): daftar coach untuk dropdown wizard step 3.
// Pola persis CoachMappingController.cs:146-149. Coach opsional, default "Pertahankan coach saat ini".
var coachRoleUsers = await _userManager.GetUsersInRoleAsync(UserRoles.Coach);
ViewBag.AllCoaches = coachRoleUsers
    .Where(u => u.IsActive)
    .OrderBy(u => u.FullName)
    .ToList();
```

JANGAN ubah baris existing (`AllTracks`, `SectionUnitsJson`). JANGAN hardcode daftar coach. `_userManager` sudah tersedia sebagai field controller (dipakai di `:233`). `UserRoles` sudah dalam scope namespace `HcPortal.Models` (cek `using` di kepala file; jika belum ada, `UserRoles` ada di `Models/UserRoles.cs` namespace `HcPortal.Models`).
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ViewBag.AllCoaches" Controllers/ProtonDataController.cs` menemukan tepat 1 hit di dalam method Override()
    - `grep -n "GetUsersInRoleAsync(UserRoles.Coach)" Controllers/ProtonDataController.cs` menemukan minimal 1 hit
    - `grep -n ".Where(u => u.IsActive)" Controllers/ProtonDataController.cs` menemukan hit terkait blok coach baru
    - Baris existing `ViewBag.AllTracks = tracks;` dan `ViewBag.SectionUnitsJson` masih ada tak berubah
    - `dotnet build` output mengandung "Build succeeded" dan TIDAK mengandung "error"
  </acceptance_criteria>
  <done>Override() set ViewBag.AllCoaches dengan coach aktif terurut FullName; build hijau; ViewBag existing utuh.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BypassPendingList SELECT (D-18) — skor, tanggal, reason, coach name</name>
  <read_first>
    - Controllers/ProtonDataController.cs:1532-1559 (BypassPendingList existing — query + projection, sebelum diubah)
    - .planning/phases/361-bypass-ui-b/361-PATTERNS.md (bagian "BypassPendingList SELECT extend pattern (D-18)" — projection lengkap target)
    - .planning/phases/361-bypass-ui-b/361-RESEARCH.md (Pitfall 4 — nama kolom REAL: Score/IsPassed/CompletedAt/Reason/TargetCoachId)
    - Models/AssessmentSession.cs:26,38,39 (Score, IsPassed, CompletedAt)
    - Models/ProtonModels.cs:242-244 (TargetCoachId, Reason, LinkedAssessmentSessionId)
  </read_first>
  <action>
Di `Controllers/ProtonDataController.cs`, method `BypassPendingList()` (`:1534-1559`), modifikasi query `from p in _context.PendingProtonBypasses`:

1. Tambah LEFT JOIN ke Users untuk nama coach target (TargetCoachId nullable → pakai `DefaultIfEmpty()`). Letakkan setelah join `s` (AssessmentSessions), sebelum `orderby`:
```csharp
join c in _context.Users on p.TargetCoachId equals c.Id into cj
from c in cj.DefaultIfEmpty()
```

2. Tambah 5 field BARU ke anonymous projection `select new { ... }`, SETELAH field existing `createdAt = p.CreatedAt` (existing field WAJIB utuh byte-for-byte — jangan ubah/hapus id, coacheeId, nama, sourceTrack, targetTrack, targetUnit, status, hasilExam, createdAt):
```csharp
// ===== NEW (D-18) — nama kolom VERIFIED real =====
skorExam = s != null ? s.Score : (int?)null,        // AssessmentSession.Score (int?)
tanggalExam = s != null ? s.CompletedAt : null,     // AssessmentSession.CompletedAt (DateTime?)
reason = p.Reason,                                  // PendingProtonBypass.Reason
targetCoachId = p.TargetCoachId,                    // string?
targetCoachNama = c != null ? (c.FullName ?? c.UserName) : null
```

CATATAN KETAT:
- JANGAN tulis `s.Skor` (salah — `s.Score`) atau `s.Completed` (salah — `s.CompletedAt`).
- Existing join `s` sudah pakai `DefaultIfEmpty()` (`:1542-1543`) → guard `s != null` sudah benar.
- `where p.Status == "Menunggu" || p.Status == "Siap"` JANGAN diubah (D-17 nilai DB tetap).
- TIDAK ada migration, TIDAK ada perubahan model.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "skorExam = s != null ? s.Score" Controllers/ProtonDataController.cs` menemukan 1 hit
    - `grep -n "tanggalExam = s != null ? s.CompletedAt" Controllers/ProtonDataController.cs` menemukan 1 hit
    - `grep -n "reason = p.Reason" Controllers/ProtonDataController.cs` menemukan hit di BypassPendingList
    - `grep -n "targetCoachNama = c != null" Controllers/ProtonDataController.cs` menemukan 1 hit
    - `grep -n "join c in _context.Users on p.TargetCoachId equals c.Id" Controllers/ProtonDataController.cs` menemukan 1 hit
    - `grep -c "s.Skor\|s.Completed[^A]" Controllers/ProtonDataController.cs` mengembalikan 0 (nama kolom salah TIDAK ada)
    - Field existing `hasilExam = s != null ? s.IsPassed : null` dan `createdAt = p.CreatedAt` masih ada utuh
    - `dotnet build` mengandung "Build succeeded" dan TIDAK mengandung "error"
  </acceptance_criteria>
  <done>BypassPendingList projection berisi skorExam/tanggalExam/reason/targetCoachId/targetCoachNama + LEFT JOIN coach; kontrak field existing utuh; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → ProtonDataController (server) | Request GET Override / GET BypassPendingList dari user authenticated [Authorize(Roles="Admin,HC")] |
| ProtonDataController → DB (EF Core) | Query parametrik EF — tidak ada SQL string concat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-01 | Information disclosure | BypassPendingList extended select (skor exam, alasan, coach name) | accept | Endpoint sudah `[Authorize(Roles="Admin,HC")]` class-level (`ProtonDataController.cs:97`); data hanya untuk HC/Admin yang memang berwenang melihat detail bypass — bukan data sensitif lintas-tenant |
| T-361-02 | Information disclosure | ViewBag.AllCoaches (daftar nama coach) | accept | Daftar coach bukan rahasia; halaman Override sudah authz Admin/HC; pola identik CoachMappingController yang sudah live |
| T-361-03 | Injection (SQLi) | extended LINQ projection + LEFT JOIN | mitigate | EF Core LINQ parametrik — tidak ada string interpolation ke SQL. Tetap pakai LINQ query syntax, bukan raw SQL |
| T-361-04 | Tampering | nilai null coach name saat TargetCoachId tidak match User | mitigate | LEFT JOIN via `DefaultIfEmpty()` + guard `c != null ?` → null-safe, tidak throw |
</threat_model>

<verification>
- `dotnet build` 0 error (gate compile setelah kedua perubahan).
- Manual smoke (opsional, di Plan 04 e2e): GET `/ProtonData/Override` render tanpa 500; GET `/ProtonData/BypassPendingList` return JSON array dengan field baru.
- TIDAK ada file migration baru (`grep -L` Migrations dir tak bertambah).
</verification>

<success_criteria>
- ViewBag.AllCoaches tersedia di Override() (coach aktif terurut FullName).
- BypassPendingList JSON berisi skorExam, tanggalExam, reason, targetCoachId, targetCoachNama selain field existing.
- Kontrak field existing BypassPendingList TIDAK berubah.
- Build hijau, tanpa migration.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/361-bypass-ui-b/361-01-SUMMARY.md`.
</output>
