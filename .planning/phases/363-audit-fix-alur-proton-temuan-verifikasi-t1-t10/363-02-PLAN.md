---
phase: 363-audit-fix-alur-proton-temuan-verifikasi-t1-t10
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/CoachingProton.cshtml
  - HcPortal.Tests/ProtonApproveRejectParityTests.cs
autonomous: true
requirements: [T1, T2, T7]
must_haves:
  truths:
    - "Approving the LAST deliverable via the CoachingProton modal (ApproveFromProgress) fires COACH_ALL_COMPLETE to HC — parity with ApproveDeliverable (T1)"
    - "Rejecting via the CoachingProton modal (RejectFromProgress) resets the full chain incl HCApprovalStatus → Pending (T2)"
    - "Concurrent approvers via the modal are blocked by the race-guard now inside the core (T7)"
    - "Both resubmit paths (UploadEvidence wasRejected + SubmitEvidenceWithCoaching isResubmit) also reset HCApprovalStatus (D-03 belt-and-braces)"
    - "RejectFromProgress JSON returns overall Status='Rejected' and the modal badge shows 'Ditolak' (not 'Pending')"
  artifacts:
    - path: "Controllers/CDPController.cs"
      provides: "ApproveFromProgress/RejectFromProgress routed through cores + DispatchApproveNotificationsAsync; resubmit HC reset"
      contains: "ApproveDeliverableCoreAsync"
    - path: "HcPortal.Tests/ProtonApproveRejectParityTests.cs"
      provides: "Parity facts covering the FromProgress code path"
      contains: "FromProgress"
  key_links:
    - from: "Controllers/CDPController.cs ApproveFromProgress (:1939)"
      to: "ApproveDeliverableCoreAsync + DispatchApproveNotificationsAsync"
      via: "direct call replacing inline parsial copy"
      pattern: "ApproveDeliverableCoreAsync\\("
    - from: "Controllers/CDPController.cs RejectFromProgress (:2018)"
      to: "RejectDeliverableCoreAsync"
      via: "direct call + overall Status in JSON"
      pattern: "RejectDeliverableCoreAsync\\("
---

<objective>
Complete D-01: route the modal endpoints `ApproveFromProgress` and `RejectFromProgress` through the SAME cores created in Plan 01, killing drift T1/T2/T7 permanently. After this plan there is exactly one approve path and one reject path in code, so the bugs cannot recur.

Fixes:
- T1 — ApproveFromProgress now fires `COACH_ALL_COMPLETE` to HC when the last deliverable is approved (via shared dispatch).
- T2 — RejectFromProgress now performs the full chain reset incl. `HCApprovalStatus`; plus belt-and-braces HC reset on both resubmit paths (D-03).
- T7 — race-guard now applies to the modal path (it lives in the core).

Purpose: Final, structural drift-kill on the most-used approval surface (CoachingProton modal).
Output: Modal endpoints rewired + resubmit HC-reset + corrected JSON response shaping + parity tests extended to the FromProgress path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-FINDINGS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-CONTEXT.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-RESEARCH.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-PATTERNS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-01-SUMMARY.md

<interfaces>
<!-- Created in Plan 01 (CDPController.cs): -->
internal static Task<(bool ok, string? error, bool allApproved)> ApproveDeliverableCoreAsync(ApplicationDbContext, int progressId, string actorId, string actorName, string actorRole, bool isSrSpv, bool isSH);
internal static Task<(bool ok, string? error)> RejectDeliverableCoreAsync(ApplicationDbContext, int progressId, string actorId, string actorName, string actorRole, string rejectionReason);
private Task DispatchApproveNotificationsAsync(ProtonDeliverableProgress progress, bool allApproved);

Endpoint-specific concerns that STAY in each endpoint (do NOT move to core):
- _userManager.GetUserAsync(User) + role resolve
- authz HasSectionAccess + section-check (DIVERGENT — ApproveDeliverable has Admin cross-section exempt :858-862; ApproveFromProgress does not :1973-1974 — KEEP divergence, Pitfall 1)
- response shaping: Json(...) for FromProgress vs RedirectToAction/TempData for Deliverable page
- rejectionReason empty-check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire ApproveFromProgress through the core + shared notif dispatch (T1, T7)</name>
  <files>Controllers/CDPController.cs</files>
  <read_first>
    - Controllers/CDPController.cs:1939-2013 (ApproveFromProgress — current parsial copy: role-set :1977-1988, no race-guard, no allApproved/notif)
    - Controllers/CDPController.cs:818-973 (ApproveDeliverable — reference for the dispatch + response semantics now shared)
    - 363-RESEARCH.md "T1/T2/T7 Wave 1 — helper" deep notes ("ApproveFromProgress juga tidak kirim notif coach/coachee — D-02 paritas penuh")
    - 363-PATTERNS.md "WAJIB perhatikan saat extract"
  </read_first>
  <action>
In `ApproveFromProgress` (:1939): keep the existing binding, `_userManager.GetUserAsync`, role resolve, authz `HasSectionAccess`, and the FromProgress section-check (:1973-1974, NO Admin exempt — keep as-is, Pitfall 1). Replace the inline role-set/save block with:

```csharp
var (ok, error, allApproved) = await CDPController.ApproveDeliverableCoreAsync(
    _context, progressId, user.Id, user.FullName, userRole, isSrSpv, isSH);
if (!ok) return Json(new { success = false, message = error });
var progress = await _context.ProtonDeliverableProgresses.FirstOrDefaultAsync(p => p.Id == progressId);
if (progress != null) await DispatchApproveNotificationsAsync(progress, allApproved);
return Json(new { success = true, allApproved, status = "Approved" });
```

Match the existing JSON field names the modal JS already consumes (read the current return at the end of ApproveFromProgress and keep field names compatible — add `allApproved` if the modal needs it). This adds, for the first time on the modal path: race-guard (T7), allApproved check + COACH_ALL_COMPLETE HC notif (T1), and coach/coachee COACH_EVIDENCE_APPROVED notif (D-02 full parity). Preserve A-2 (no co-sign forced).
  </action>
  <acceptance_criteria>
    - `grep -n "ApproveDeliverableCoreAsync" Controllers/CDPController.cs` shows a call inside ApproveFromProgress (2 call-sites total with ApproveDeliverable).
    - `grep -n "DispatchApproveNotificationsAsync" Controllers/CDPController.cs` shows it called from BOTH ApproveDeliverable and ApproveFromProgress.
    - ApproveFromProgress no longer contains its own inline `SrSpvApprovalStatus = "Approved"` block (state-set now in core): `grep -n "SrSpvApprovalStatus = \"Approved\"" Controllers/CDPController.cs` appears only inside the core.
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>ApproveFromProgress routes through the core and shared dispatch; HC + coach/coachee notifs fire on the modal path; race-guard active.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire RejectFromProgress through reject-core + fix JSON response shaping + check consumer JS (T2, Open Q2)</name>
  <files>Controllers/CDPController.cs, Views/CDP/CoachingProton.cshtml</files>
  <read_first>
    - Controllers/CDPController.cs:2018-2091 (RejectFromProgress — current: only sets reject role :2055-2069, anomaly sets SrSpvApprovedById/At :2057-2059, returns per-role newStatus)
    - Controllers/CDPController.cs:978-1080 (RejectDeliverable — gold standard full reset)
    - Views/CDP/CoachingProton.cshtml — find the JS that consumes the RejectFromProgress JSON response (search for the reject AJAX handler / how it reads `newStatus`/`status` to update the modal badge)
    - 363-RESEARCH.md Pitfall 2 ("Response newStatus berubah makna setelah full-chain-reset") + Open Question 2
  </read_first>
  <action>
In `RejectFromProgress` (:2018): keep binding, user/role resolve, authz, section-check, `rejectionReason` empty-check. Replace the inline set block (:2055-2069, including the anomalous SrSpvApprovedById/At assignment) with:

```csharp
var (ok, error) = await CDPController.RejectDeliverableCoreAsync(
    _context, progressId, user.Id, user.FullName, userRole, rejectionReason);
if (!ok) return Json(new { success = false, message = error });
return Json(new { success = true, status = "Rejected" });
```

CRITICAL response shaping (Pitfall 2): after the full reset, per-role status is "Pending", so do NOT return a per-role `newStatus`. Return the OVERALL status "Rejected". Read `Views/CDP/CoachingProton.cshtml` reject handler: if the JS reads `newStatus` to render the badge, either (a) keep a `newStatus = "Rejected"` field for compat, or (b) update the JS to read `status`. Make the modal badge show "Ditolak". Document which field the JS uses in the SUMMARY.

The anomaly (setting SrSpvApprovedById/At on reject) MUST be gone — verify reject-core does not set any approver-id.
  </action>
  <acceptance_criteria>
    - `grep -n "RejectDeliverableCoreAsync" Controllers/CDPController.cs` shows a call inside RejectFromProgress (2 call-sites total).
    - RejectFromProgress no longer sets approver-id on reject: `grep -n "SrSpvApprovedById = user.Id" Controllers/CDPController.cs` does NOT appear inside RejectFromProgress (anomaly removed).
    - The reject JSON returns an overall "Rejected" indicator (grep the return in RejectFromProgress shows `"Rejected"`).
    - CoachingProton.cshtml reject handler reads a field that resolves to "Ditolak"/"Rejected" (note exact field in SUMMARY).
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>RejectFromProgress performs full chain reset via core, anomaly removed, JSON returns overall Rejected, modal badge correct.</done>
</task>

<task type="auto">
  <name>Task 3: Belt-and-braces HC reset on both resubmit paths + extend parity tests (T2/D-03)</name>
  <files>Controllers/CDPController.cs, HcPortal.Tests/ProtonApproveRejectParityTests.cs</files>
  <read_first>
    - Controllers/CDPController.cs:1280-1308 (UploadEvidence wasRejected reset block — currently resets SrSpv+SH only)
    - Controllers/CDPController.cs:2270-2279 (SubmitEvidenceWithCoaching isResubmit reset block — currently SrSpv+SH only)
    - 363-CONTEXT.md D-03 (reset HCApprovalStatus in reject AND both resubmit paths)
    - 363-RESEARCH.md "Runtime State Inventory" (no migration data cleanup; defensive-on-write)
  </read_first>
  <action>
1. In `UploadEvidence` resubmit/wasRejected block (~:1297-1308): add `progress.HCApprovalStatus = "Pending"; progress.HCReviewedById = null; progress.HCReviewedAt = null;` alongside the existing SrSpv/SH reset.
2. In `SubmitEvidenceWithCoaching` isResubmit block (~:2270-2279): add the same three HC-reset lines alongside the existing SrSpv/SH reset.
   (NOTE: SubmitEvidenceWithCoaching is also edited by Plan 06 Task for T8 evidence-history append — different block; this plan owns the resubmit-reset block only.)
3. Extend `HcPortal.Tests/ProtonApproveRejectParityTests.cs` with a fact `Reject_ThenResubmit_HCStatusBackToPending`: seed a progress, reject via RejectDeliverableCoreAsync, set HCApprovalStatus="Reviewed" again to simulate stale, then assert that a resubmit path sets it back to Pending (replicate the resubmit reset block predicate or assert state after applying it). Plus a fact confirming reject-core leaves HCApprovalStatus=="Pending" already covered in Plan 01 — add a FromProgress-flavored parity assertion if feasible (state-equality between calling the core for the "deliverable" vs "modal" scenarios with identical input).
  </action>
  <acceptance_criteria>
    - `grep -n "HCApprovalStatus = \"Pending\"" Controllers/CDPController.cs` appears in reject-core, UploadEvidence resubmit block, AND SubmitEvidenceWithCoaching resubmit block (3+ locations).
    - `grep -n "HCReviewedById = null" Controllers/CDPController.cs` appears in all three locations.
    - `grep -n "Reject_ThenResubmit_HCStatusBackToPending" HcPortal.Tests/ProtonApproveRejectParityTests.cs` returns a match.
    - `dotnet test --filter "FullyQualifiedName~ProtonApproveRejectParity"` → green.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonApproveRejectParity"</automated>
  </verify>
  <done>HC review status cannot survive a rejection on any path; parity tests cover the resubmit reset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach/L4 reviewer → CoachingProton modal (AJAX) → CDPController | Most-used approval surface; modal previously skipped notif/race-guard/full-reset |
| coachee (resubmit) → UploadEvidence/SubmitEvidenceWithCoaching | Re-uploading evidence after rejection must clear stale approvals incl HC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-363-01 | (Completeness/control gap) | ApproveFromProgress missing HC notif | mitigate | allApproved + COACH_ALL_COMPLETE now fired via shared dispatch; HC-only broadcast (CreateHCNotificationAsync iterates GetUsersInRoleAsync(HC)) — no info leak to non-HC |
| T-363-02 | Tampering (state integrity) | RejectFromProgress + resubmit paths | mitigate | Full chain reset incl HCApprovalStatus in reject-core; belt-and-braces HC reset on both resubmit paths (D-03) — stale "Reviewed" cannot survive rejection/resubmit |
| T-363-07 | Tampering (lost update) | concurrent modal approve | mitigate | Race-guard reload-fresh inherited from core (D-04) now protects the modal path |
| T-363-A2 | Elevation of Privilege | approval semantics | accept (preserved) | A-2 unchanged: 1 L4 approver suffices, HC not an approver; section-check divergence intentionally preserved per-endpoint (Pitfall 1) |
</threat_model>

<verification>
- `dotnet build` 0 errors.
- `dotnet test --filter "FullyQualifiedName~ProtonApproveRejectParity"` green.
- `dotnet test` full suite green (run at wave merge).
- Manual/UAT (Plan 07): approve last deliverable via CoachingProton modal → HC bell shows COACH_ALL_COMPLETE; reject via modal → badge shows Ditolak + chain reset.
</verification>

<success_criteria>
- Exactly one approve path and one reject path in code (both endpoints call the cores).
- T1 SC: ApproveFromProgress fires HC notif at parity with ApproveDeliverable.
- T2 SC: RejectFromProgress + both resubmit paths reset HCApprovalStatus.
- T7: race-guard active on modal path.
</success_criteria>

<output>
After completion, create `.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-02-SUMMARY.md`
</output>
