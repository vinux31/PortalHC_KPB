---
phase: 363-audit-fix-alur-proton-temuan-verifikasi-t1-t10
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/ProtonCompletionService.cs
  - HcPortal.Tests/ProtonCompletionServiceTests.cs
  - HcPortal.Tests/ProtonYearGateIntegrationTests.cs
  - HcPortal.Tests/ProtonBypassServiceTests.cs
  - HcPortal.Tests/ProtonCompletionMissTests.cs
autonomous: true
requirements: [T4]
must_haves:
  truths:
    - "Passing an exam while no active assignment exists no longer silently drops the penanda — it surfaces an AuditLog entry AND an HC bell notification"
    - "EnsureAsync stays strict on IsActive (D-07) — assignment deactivation is a deliberate admin decision, no auto-penanda"
    - "The idempotent already-exists path does NOT surface (no false alarm)"
    - "The HC notification is broadcast ONLY to HC users (info-disclosure scope)"
  artifacts:
    - path: "Services/ProtonCompletionService.cs"
      provides: "ctor injects INotificationService + AuditLogService; no-active-assignment branch surfaces audit + HC notif"
      contains: "PROTON_PENANDA_MISS"
    - path: "HcPortal.Tests/ProtonCompletionMissTests.cs"
      provides: "Surface-on-miss + idempotent-no-surface integration tests"
      contains: "ProtonCompletionMissTests"
  key_links:
    - from: "Services/ProtonCompletionService.cs EnsureAsync no-assignment branch (:40-43)"
      to: "_auditLog.LogAsync + _notificationService.SendAsync"
      via: "surface inside the no-assignment branch only"
      pattern: "PROTON_PENANDA_MISS"
---

<objective>
Fix T4 (MED): when a worker passes an exam but has no ACTIVE ProtonTrackAssignment (e.g., admin deactivated the mapping mid-exam), the penanda is silently not created and the year-gate later wrongly blocks them. Per D-07/D-08: keep EnsureAsync strict on IsActive (deactivation is deliberate), but SURFACE the miss — write an AuditLog entry and broadcast an HC bell notification pointing admins to BackfillProtonPenanda.

This is surfaced INSIDE `ProtonCompletionService.EnsureAsync` (Open Q3 resolution: uniform across all 4 call-sites) which requires injecting `INotificationService` + `AuditLogService` into the service ctor and updating the 3 existing test constructors.

Purpose: No more silent penanda loss; admins are alerted with an actionable next step.
Output: ctor change + surface logic + 3 test-ctor updates + new ProtonCompletionMissTests.cs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-FINDINGS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-CONTEXT.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-RESEARCH.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-PATTERNS.md

<interfaces>
Current ctor (Services/ProtonCompletionService.cs:23-29):
```csharp
public ProtonCompletionService(ApplicationDbContext context, ILogger<ProtonCompletionService> logger)
```
Target ctor (add 2 deps — both registered scoped in DI, no Program.cs change needed):
```csharp
public ProtonCompletionService(ApplicationDbContext context, ILogger<ProtonCompletionService> logger,
    INotificationService notificationService, AuditLogService auditLog)
```

Dep signatures (reuse — Don't Hand-Roll):
- AuditLogService.LogAsync(string actorUserId, string actorName, string actionType, string description, int? targetId, string targetType)  [Services/AuditLogService.cs:21-42]
- INotificationService.SendAsync(string userId, string type, string title, string message, string? actionUrl = null) : Task<bool>  [Services/NotificationService.cs:108]
- HC users without UserManager: _context.Users.Where(u => u.RoleLevel == 2 && u.IsActive)  [RESEARCH Contoh 6; caveat A2 — RoleLevel denormalized]

The 3 test constructors that MUST be updated (NewSvc helpers):
- HcPortal.Tests/ProtonCompletionServiceTests.cs:109-110
- HcPortal.Tests/ProtonYearGateIntegrationTests.cs:26-27
- HcPortal.Tests/ProtonBypassServiceTests.cs:44

Surface point — EnsureAsync no-assignment branch (Services/ProtonCompletionService.cs:40-44):
```csharp
if (assignment == null)
{
    _logger.LogWarning("ProtonCompletion.EnsureAsync: tidak ada assignment aktif ...", ...);
    return false;   // ← add audit + HC notif HERE (D-08). NOT in the :48 already-exists branch (Pitfall 3).
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ctor + surface miss (audit + HC notif) in the no-assignment branch (T4/D-08)</name>
  <files>Services/ProtonCompletionService.cs</files>
  <read_first>
    - Services/ProtonCompletionService.cs:23-64 (ctor + EnsureAsync full body — confirm the no-assignment branch :40-43 vs already-exists branch :48)
    - Services/AuditLogService.cs:21-42 (LogAsync signature + SaveChanges internal)
    - Services/NotificationService.cs:108-134 (SendAsync — string-type allowed without _templates registration, like COACH_ALL_COMPLETE)
    - Controllers/CDPController.cs:1111-1140 (CreateHCNotificationAsync — dedup-by-exact-message pattern to mirror)
    - 363-PATTERNS.md "ProtonCompletionService — T4 Surface Miss" (audit pattern + notif broadcast + ctor change)
    - 363-CONTEXT.md D-07, D-08
  </read_first>
  <action>
1. Add fields `_notificationService` (INotificationService) + `_auditLog` (AuditLogService); extend the ctor to inject them (signature in &lt;interfaces&gt;).
2. In the `if (assignment == null)` branch (:40-43), AFTER the existing `_logger.LogWarning(...)` (keep it) and BEFORE `return false`, add surface logic:
   a. AuditLog: `await _auditLog.LogAsync("system", "system/grading", "PROTON_PENANDA_MISS", $"Lulus exam tapi tidak ada assignment aktif — Coachee={coacheeId}, Track={protonTrackId}, Origin={origin}. Gunakan BackfillProtonPenanda untuk menambal.", protonTrackId, "ProtonTrackAssignment");`
   b. Resolve HC user ids: `var hcIds = await _context.Users.Where(u => u.RoleLevel == 2 && u.IsActive).Select(u => u.Id).ToListAsync();`
   c. Broadcast HC bell, dedup by exact message (mirror CreateHCNotificationAsync): build `expectedMessage` mentioning the coachee + track + (session if available in scope), then for each hcId, skip if a UserNotification with same UserId+Type=="PROTON_PENANDA_MISS"+Message==expectedMessage already exists, else `await _notificationService.SendAsync(hcId, "PROTON_PENANDA_MISS", "Penanda Proton terlewat", expectedMessage, "/Admin/ManageAssessment");`
3. Leave the `:48 if (exists) return false;` (already-exists/idempotent) branch UNTOUCHED — do NOT surface there (Pitfall 3 false-alarm).
4. Do NOT change the strict `IsActive` filter (D-07). Do NOT touch the Phase-360 hooks (MarkPendingReadyIfAnyAsync etc.).

The notif type "PROTON_PENANDA_MISS" is sent via SendAsync directly (no _templates registration required — same as COACH_ALL_COMPLETE).
  </action>
  <acceptance_criteria>
    - `grep -n "INotificationService\|AuditLogService" Services/ProtonCompletionService.cs` shows both injected as ctor params + fields.
    - `grep -n "PROTON_PENANDA_MISS" Services/ProtonCompletionService.cs` shows audit actionType AND notif type.
    - `grep -n "RoleLevel == 2" Services/ProtonCompletionService.cs` shows HC resolution.
    - The already-exists branch is untouched: surface logic appears only within the `assignment == null` block (verify by line proximity in review).
    - `grep -n "a.IsActive" Services/ProtonCompletionService.cs` — strict IsActive filter still present (D-07).
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>No-assignment miss now writes audit + HC notif; idempotent path silent; IsActive strictness preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update 3 existing test constructors + new ProtonCompletionMissTests.cs (T4)</name>
  <files>HcPortal.Tests/ProtonCompletionServiceTests.cs, HcPortal.Tests/ProtonYearGateIntegrationTests.cs, HcPortal.Tests/ProtonBypassServiceTests.cs, HcPortal.Tests/ProtonCompletionMissTests.cs</files>
  <read_first>
    - HcPortal.Tests/ProtonCompletionServiceTests.cs:109-110 (NewSvc)
    - HcPortal.Tests/ProtonYearGateIntegrationTests.cs:26-27 (NewSvc)
    - HcPortal.Tests/ProtonBypassServiceTests.cs:44 (ProtonCompletionService construction inside NewBypassSvc)
    - HcPortal.Tests/ProtonBypassServiceTests.cs:24-47 (FakeNotificationService pattern — reuse to assert Sent notifs)
    - HcPortal.Tests/ProtonCompletionServiceTests.cs:25-115 (fixture + seeding helpers to reuse)
  </read_first>
  <action>
1. Update the 3 `NewSvc`/construction sites to pass the 2 new ctor args. For the existing files use no-op doubles so behavior is unchanged: a `FakeNotificationService` (reuse/lift the one from ProtonBypassServiceTests :24-47) and an `AuditLogService(ctx)` (real, over the fixture context — it just inserts audit rows; harmless). Where a NullLogger is needed for AuditLogService, supply it per its ctor.
2. Create `HcPortal.Tests/ProtonCompletionMissTests.cs` as `[Trait("Category","Integration")]` using ProtonCompletionFixture:
   - `EnsureAsync_NoActiveAssignment_SurfacesAuditAndHCNotif`: seed an HC user (RoleLevel==2, IsActive) + a passing-exam scenario with NO active assignment for the track; construct service with a FakeNotificationService that records Sent; call EnsureAsync; assert (a) AuditLogs contains a "PROTON_PENANDA_MISS" entry, (b) FakeNotificationService recorded a send to the HC user with type "PROTON_PENANDA_MISS".
   - `EnsureAsync_PenandaAlreadyExists_DoesNotSurface`: seed an existing ProtonFinalAssessment so the already-exists branch returns false; assert NO new "PROTON_PENANDA_MISS" audit/notif (idempotent guard, Pitfall 3).
   Unique coacheeId per fact (`$"miss-{Guid.NewGuid():N}"`).
  </action>
  <acceptance_criteria>
    - `grep -rn "new ProtonCompletionService(" HcPortal.Tests/` — all 3 sites pass 4 args (ctx, logger, notif, audit).
    - File `HcPortal.Tests/ProtonCompletionMissTests.cs` exists with both `[Fact]` named above.
    - `dotnet test --filter "FullyQualifiedName~ProtonCompletionMiss"` → green.
    - `dotnet test --filter "Category=Integration&FullyQualifiedName~Proton"` → green (existing Proton integration tests still pass with new ctor).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProtonCompletionMiss"</automated>
  </verify>
  <done>All ctor call-sites compile; surface-on-miss and idempotent-no-surface proven; existing Proton tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grading subsystem (system actor) → ProtonCompletionService | Auto penanda creation; a miss must be auditable + alertable, not silent |
| notification fan-out → HC users | Miss alert must reach ONLY HC (info-disclosure scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-363-04i | Information Disclosure | PROTON_PENANDA_MISS notif fan-out | mitigate | Broadcast restricted to RoleLevel==2 (HC) users only; never to coach/coachee/atasan |
| T-363-04r | Repudiation | silent penanda miss | mitigate | AuditLogService.LogAsync writes a "PROTON_PENANDA_MISS" record with system actor + coachee/track/session context |
| T-363-04f | (False alarm) | already-exists branch | mitigate | Surface confined to the no-assignment branch; idempotent already-exists path stays silent (Pitfall 3) |
| T-363-04v | V7 Logging | error text | accept | Notif message is operational (coachee/track), no secrets; mirrors existing COACH_ALL_COMPLETE content scope |
</threat_model>

<verification>
- `dotnet build` 0 errors.
- `dotnet test --filter "FullyQualifiedName~ProtonCompletionMiss"` + existing Proton integration tests green.
- Full `dotnet test` at wave merge.
</verification>

<success_criteria>
- T4 SC: exam pass with no active assignment → AuditLog + HC notif (not silent), pointing to BackfillProtonPenanda.
- EnsureAsync remains strict on IsActive (D-07); idempotent path does not false-alarm.
</success_criteria>

<output>
After completion, create `.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-03-SUMMARY.md`
</output>
