---
phase: 363-audit-fix-alur-proton-temuan-verifikasi-t1-t10
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/GradingService.cs
autonomous: true
requirements: [T6]
must_haves:
  truths:
    - "Regrade Fail→Pass no longer hardcodes ValidUntil = today + 3 years; ValidUntil follows the HC session setup, at parity with the normal pass path (GradeAndCompleteAsync)"
    - "Pass→Fail revoke (sets NomorSertifikat + ValidUntil null) is unchanged"
    - "Phase 360 bypass hooks in RegradeAfterEditAsync are untouched"
  artifacts:
    - path: "Services/GradingService.cs"
      provides: "RegradeAfterEditAsync Fail→Pass block sets NomorSertifikat only (no ValidUntil hardcode)"
      contains: "SetProperty(r => r.NomorSertifikat, nomor)"
  key_links:
    - from: "Services/GradingService.cs RegradeAfterEditAsync Fail→Pass (:516-521)"
      to: "AssessmentSessions ExecuteUpdateAsync"
      via: "NomorSertifikat-only SetProperty (ValidUntil removed)"
      pattern: "SetProperty\\(r => r.NomorSertifikat, nomor\\)"
---

<objective>
Fix T6 (MED): asymmetric certificate validity. The normal pass path (`GradeAndCompleteAsync` :286) sets only `NomorSertifikat` (ValidUntil comes from the HC's session setup), but the regrade Fail→Pass path (`RegradeAfterEditAsync` :516-521) ALSO hardcodes `ValidUntil = today + 3 years`. Two certificates from the same session could then have different validity depending on which path issued them.

Per D-10 (LOCKED): drop the hardcoded +3 years from the regrade path so it reaches parity with the normal path. Surgical deletion only — no other behavior changes.

Purpose: One consistent certificate-validity rule regardless of issuance path.
Output: RegradeAfterEditAsync Fail→Pass block issues NomorSertifikat only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-FINDINGS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-CONTEXT.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-RESEARCH.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-PATTERNS.md

<interfaces>
Parity target (normal pass) — Services/GradingService.cs:284-286:
```csharp
.Where(s => s.Id == session.Id && s.NomorSertifikat == null)
.SetProperty(r => r.NomorSertifikat, CertNumberHelper.Build(nextSeq, certNow))   // NO ValidUntil
```

To change (regrade Fail→Pass) — Services/GradingService.cs:516-521:
```csharp
var validUntil = DateOnly.FromDateTime(certNow).AddYears(3);   // :516 ← DELETE
...
.Where(s => s.Id == session.Id && s.NomorSertifikat == null)
.SetProperty(r => r.NomorSertifikat, nomor)
.SetProperty(r => r.ValidUntil, validUntil));                  // :521 ← DELETE this SetProperty
```

MUST NOT TOUCH:
- Pass→Fail revoke block :482-483 (sets NomorSertifikat + ValidUntil null — correct).
- Phase 360 bypass hooks (:494/:496/:549 area — RevertPendingToMenungguAsync / regrade pass hooks).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop hardcoded ValidUntil from RegradeAfterEditAsync Fail→Pass (T6/D-10)</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs:437-555 (RegradeAfterEditAsync full body — confirm Fail→Pass block :499-538, revoke block :479-486, bypass hooks)
    - Services/GradingService.cs:267-301 (GradeAndCompleteAsync normal pass — parity target, NomorSertifikat only)
    - 363-PATTERNS.md "GradingService — T6 Drop ValidUntil Hardcode"
    - 363-RESEARCH.md Pitfall 6 (Pass→Fail→Pass edge: ValidUntil may stay null — accepted per D-10, verify via UAT)
  </read_first>
  <action>
1. Delete line :516 `var validUntil = DateOnly.FromDateTime(certNow).AddYears(3);` (and its comment).
2. In the ExecuteUpdateAsync at :518-521, remove the `.SetProperty(r => r.ValidUntil, validUntil)` line; keep `.SetProperty(r => r.NomorSertifikat, nomor)`. The block now mirrors the normal pass path (NomorSertifikat only).
3. Do NOT touch the Pass→Fail revoke block (:482-483) nor any Phase 360 bypass hook. No other logic changes.
4. Add a one-line comment at the regrade cert block: `// T6/D-10: ValidUntil mengikuti setup sesi HC (paritas GradeAndCompleteAsync) — TIDAK di-hardcode di sini.`
  </action>
  <acceptance_criteria>
    - `grep -n "AddYears(3)" Services/GradingService.cs` → NO match (hardcode removed).
    - `grep -n "SetProperty(r => r.ValidUntil, validUntil)" Services/GradingService.cs` → NO match.
    - `grep -n "SetProperty(r => r.NomorSertifikat, nomor)" Services/GradingService.cs` → still present (regrade still issues cert number).
    - `grep -n "SetProperty(r => r.ValidUntil, (DateOnly?)null)" Services/GradingService.cs` → still present (Pass→Fail revoke untouched).
    - `grep -n "T6/D-10" Services/GradingService.cs` → comment present.
    - `dotnet build` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug; dotnet test --filter "Category!=Integration"</automated>
  </verify>
  <done>Regrade Fail→Pass no longer hardcodes ValidUntil; revoke + bypass hooks intact; build + unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC re-grade action → GradingService.RegradeAfterEditAsync | Re-grading flips Pass/Fail and (re)issues certificate fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-363-06 | Tampering (data integrity) | certificate validity asymmetry | mitigate | Remove hardcoded +3yr so all issuance paths derive ValidUntil from the single HC-controlled session setup — no path-dependent validity divergence |
| T-363-06e | (Edge case) | Pass→Fail→Pass leaves ValidUntil null | accept | Per D-10 (locked): ValidUntil follows session setup which may already be null after revoke; verified via UAT regrade double-flip (Plan 07). Escalate as separate phase only if it becomes a real issue |
</threat_model>

<verification>
- `dotnet build` 0 errors + `dotnet test` (no regression).
- UAT (Plan 07): regrade Fail→Pass at :5277 → certificate ValidUntil follows session setup (or null if session has none), not a hardcoded +3 years.
- NOTE (test-strategy discretion): a bespoke regrade-flow integration test would require constructing the full GradingService graph (incl. ProtonCompletionService whose ctor changes in Plan 03), adding cross-plan coupling for a surgical 2-line deletion. Verification here is grep + no-regression + behavioral UAT; this is a deliberate discretionary choice (CONTEXT: test strategy = planner discretion), not a scope reduction — the D-10 fix itself is fully implemented.
</verification>

<success_criteria>
- T6 SC (part of SC5 triage): regrade Fail→Pass issues NomorSertifikat only, parity with normal pass; revoke + bypass hooks unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-04-SUMMARY.md`
</output>
