---
phase: 363-audit-fix-alur-proton-temuan-verifikasi-t1-t10
plan: 07
type: execute
wave: 4
depends_on: [02, 04, 05, 06]
files_modified:
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [T1, T2, T3, T5, T6]
must_haves:
  truths:
    - "Full dotnet test suite is green (existing 211+ regression + all new 363 integration tests)"
    - "Approve last deliverable via CoachingProton modal → HC bell notification appears (T1)"
    - "Reject via CoachingProton modal → chain reset, modal badge shows 'Ditolak' not 'Pending' (T2)"
    - "Assigning a reactivation of a cross-year inactive assignment without prev-year passed is blocked (T3)"
    - "HistoriProton shows a 'Belum Mulai' coachee and the filter works (T5)"
    - "Regrade Fail→Pass does not stamp a hardcoded +3yr ValidUntil (T6)"
  artifacts:
    - path: "docs/SEED_JOURNAL.md"
      provides: "Seed journal entry for the UAT seed state (snapshot + cleaned)"
      contains: "363"
  key_links:
    - from: "local server @5277 (AD=false)"
      to: "PROTON approve/reject/assign/histori flows"
      via: "browser UAT (Playwright MCP or manual)"
      pattern: "localhost:5277"
---

<objective>
Phase gate: prove the 10-finding audit-fix works end-to-end before `/gsd-verify-work`. Run the full test suite (regression + new integration tests) and perform the live UAT @localhost:5277 (CLAUDE.md Develop Workflow, AD=false) for the behaviorally-observable fixes (T1/T2/T3/T5/T6). T4/T8 are verified by integration tests in Plans 03/06; T9/T10 are code-review/by-design (no behavior).

Per SEED_WORKFLOW: snapshot the local DB, seed the multi-state worker fixtures needed (approve/reject chain, cross-year inactive assignment, active-mapping-without-assignment), journal it, and RESTORE after UAT (success or failure).

Purpose: Single consolidated verification gate isolating the human checkpoint from autonomous implementation.
Output: Green full suite + signed-off UAT + clean seed journal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-FINDINGS.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-CONTEXT.md
@.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-VALIDATION.md
@docs/SEED_WORKFLOW.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite green + seed UAT state (SEED_WORKFLOW)</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_WORKFLOW.md (classification + SQL Server BACKUP/RESTORE commands + journal format)
    - .planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-VALIDATION.md (Sampling Rate → phase gate)
    - Reference dev admin login for UAT (memory: admin@pertamina.com local)
  </read_first>
  <action>
1. Run the FULL test suite: `dotnet build` (0 errors) then `dotnet test` (all green, incl. Integration on localhost\SQLEXPRESS — ProtonApproveRejectParity, ProtonCompletionMiss, ProtonYearGateIntegration reactivation facts, ProtonHistoriAndEvidence). If any red, STOP and report (do not proceed to UAT).
2. Snapshot the local DB (HcPortalDB_Dev) per SEED_WORKFLOW (`BACKUP DATABASE ... TO DISK` to C:\Temp\...363_preUAT_{date}.bak). Record path.
3. Seed temporary `local-only` UAT state and add a `docs/SEED_JOURNAL.md` entry (purpose, classification=temporary+local-only, entities touched):
   - one coachee with a deliverable chain where all-but-one deliverable is Approved (for T1 "approve last → HC bell" + T2 "reject → reset"),
   - one coachee with an INACTIVE cross-year (Tahun 2) ProtonTrackAssignment, Origin=null, no Tahun-1 penanda (for T3 reactivation block),
   - one coachee with an active CoachCoacheeMapping but NO ProtonTrackAssignment (for T5 "Belum Mulai"),
   - one Completed-but-regradable session (for T6 regrade flip). Reuse existing seed/admin tooling where possible.
4. Mark the journal entry status `seeded` (to be flipped to `cleaned` in Task 3).
  </action>
  <acceptance_criteria>
    - `dotnet test` exits 0 (full suite green). Capture pass count (≥211 + new facts).
    - DB backup file exists at the recorded path.
    - `grep -n "363" docs/SEED_JOURNAL.md` shows the new entry with classification + snapshot path.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test</automated>
  </verify>
  <done>Full suite green; UAT state seeded; snapshot taken; journal entry recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT @5277 — T1/T2/T3/T5/T6 (human-verify)</name>
  <files>(no file changes — verification only)</files>
  <what-built>All 10 audit findings (T1-T10) fixed/triaged: shared approve/reject cores kill drift (T1/T2/T7), reactivation year-gate (T3), penanda miss surface (T4), Belum-Mulai (T5), ValidUntil parity (T6), evidence history append (T8), defensive logs (T9), by-design doc (T10). Local server is running the fixes.</what-built>
  <action>
Claude has already (Task 1) run the full test suite green and seeded the UAT state. This task is the human checkpoint: a person drives the browser at localhost:5277 and confirms the 5 observable behaviors below. If Playwright MCP is available, Claude may pre-drive the flows and present screenshots, but the human sign-off is still required. Claude does NOT proceed past this gate until "approved".
  </action>
  <how-to-verify>
Start the app local AD-off: `Authentication__UseActiveDirectory=false dotnet run` → open `http://localhost:5277` (login admin@pertamina.com). Verify:

1. (T1) As an L4 reviewer, open CoachingProton for a coachee whose deliverables are all approved except the last. Approve the LAST deliverable via the modal. Expected: the HC user's bell shows a "COACH_ALL_COMPLETE" notification (the coachee is complete). Compare: this previously fired only from the Deliverable page, not the modal.
2. (T2) Reject a submitted deliverable via the CoachingProton modal with a reason. Expected: modal badge shows "Ditolak" (NOT "Pending"); reloading shows SrSpv/SH/HC approval all reset (no stale "Reviewed").
3. (T3) Go to Assign Coach×Coachee. Try to assign/reactivate a coachee who has an INACTIVE Tahun 2 assignment (Origin=null) without Tahun 1 passed. Expected: blocked with "Tidak bisa assign Tahun 2: Tahun 1 ... belum lulus ...". Then confirm a coachee with an inactive Origin="Bypass" assignment is NOT blocked.
4. (T5) Open HistoriProton. Expected: the coachee with an active mapping but no assignment appears with badge "Belum Mulai"; the "Belum Mulai" filter shows it; Export Excel includes the row.
5. (T6) Re-grade a Fail→Pass session. Expected: the issued certificate's ValidUntil follows the session setup (or is null if the session has none) — NOT a hardcoded today+3-years.

Report any deviation (badge text, missing notif, gate not blocking, missing row).
  </how-to-verify>
  <verify>
    <automated>MANUAL — human-verify checkpoint; the gating signal is the resume-signal below. Task 1 already ran `dotnet test` green as the automated backstop.</automated>
  </verify>
  <resume-signal>Type "approved" if all 5 pass, or describe the failing item(s).</resume-signal>
  <done>Human confirms T1/T2/T3/T5/T6 behave as expected in a live browser @5277.</done>
</task>

<task type="auto">
  <name>Task 3: Restore DB + mark journal cleaned</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_WORKFLOW.md (RESTORE command + journal cleaned convention)
  </read_first>
  <action>
Regardless of UAT outcome (pass or fail), RESTORE the local DB from the Task-1 snapshot per SEED_WORKFLOW (`RESTORE DATABASE HcPortalDB_Dev FROM DISK ...` — single-user mode as documented). Then update the `docs/SEED_JOURNAL.md` entry status to `cleaned`. Confirm no temporary seed rows remain (spot-check the seeded coachees are gone / DB matches pre-UAT state).
  </action>
  <acceptance_criteria>
    - `grep -n "cleaned" docs/SEED_JOURNAL.md` shows the 363 entry marked cleaned.
    - DB restored: the temporary UAT coachees are absent (spot-check query returns 0).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>Local DB restored to pre-UAT state; seed journal marked cleaned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → local DB (HcPortalDB_Dev) | Temporary seed must be snapshotted + restored (no temporary seed left attached, per CLAUDE.md Seed Workflow) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-363-uat | Tampering (test data leak into dev DB) | UAT seed state | mitigate | Snapshot before seed + mandatory restore after (Task 3, runs on pass or fail); journal tracked |
| T-363-uat-env | (Config) | AD bypass during UAT | accept | `Authentication__UseActiveDirectory=false` is the documented local-only UAT mode; no Dev/Prod touched (CLAUDE.md) |
</threat_model>

<verification>
- `dotnet test` full suite green (regression + new integration tests).
- Human UAT sign-off on T1/T2/T3/T5/T6 @5277.
- Local DB restored; seed journal cleaned.
</verification>

<success_criteria>
- SC6: build 0 errors + full test green + UAT @5277 passed.
- All behaviorally-observable findings confirmed fixed in a live browser; no Dev/Prod edits.
</success_criteria>

<output>
After completion, create `.planning/phases/363-audit-fix-alur-proton-temuan-verifikasi-t1-t10/363-07-SUMMARY.md`
</output>
