---
phase: 364-restore-baseline-regresi-e2e-exam
plan: 02
type: execute
wave: 2
depends_on: [364-01]
files_modified:
  - tests/e2e/exam-taking.spec.ts
  - tests/e2e/exam-types.spec.ts
autonomous: true
requirements: []   # test-only — success = ROADMAP Phase 364 SC#1 (validator comply) + SC#3 (LinkedGroupId isolation)
user_setup: []

must_haves:
  truths:
    - "All 10 standard-create titles in exam-taking.spec.ts carry the 'Pre Test ' prefix (validator REST-06 comply)"
    - "All 11 standard-create titles in exam-types.spec.ts carry the 'Pre Test ' prefix"
    - "FLOW P (:860) and FLOW T (:1457) titles are UNCHANGED (exempt / different controller)"
    - "tests/helpers/utils.ts is NOT modified (uniqueTitle helper untouched)"
    - "exam-types FLOW K asserts LinkedGroupId IS NULL for the created assessment (SC#3 / D-11)"
    - "Both specs progress PAST the create step when re-run @5277 (no validator reject)"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "10 title args prefixed with 'Pre Test ' at the documented call sites"
      contains: "Pre Test Legacy Exam"
    - path: "tests/e2e/exam-types.spec.ts"
      provides: "11 title args prefixed + 1 LinkedGroupId IS NULL DB assertion in FLOW K"
      contains: "LinkedGroupId IS NULL"
  key_links:
    - from: "uniqueTitle('Pre Test ...')"
      to: "AssessmentAdminController REST-06 regex ^(Pre|Post)\\s*Test\\s+.+$"
      via: "POST /Admin/CreateAssessment title validation"
      pattern: "uniqueTitle\\('Pre Test "
    - from: "exam-types FLOW K db.queryScalar"
      to: "AssessmentSessions.LinkedGroupId"
      via: "SELECT COUNT(*) ... LinkedGroupId IS NULL"
      pattern: "LinkedGroupId IS NULL"
---

<objective>
Edit the title arguments of `uniqueTitle()` at the 21 standard-create call sites across the two target specs so every standard (non-PrePostTest) assessment created carries the uniform `Pre Test ` prefix (D-01, old marker kept as remainder per D-02/D-04) and passes the case-sensitive REST-06 validator regex `^(Pre|Post)\s*Test\s+.+$` (SC#1). Also add ONE DB assertion in exam-types FLOW K proving the auto-pair (Phase 338) does NOT mis-set `LinkedGroupId` (SC#3 / D-11). FLOW P (PrePostTest, exempt) and FLOW T (Manual, different controller) are explicitly NOT touched. The shared helper `tests/helpers/utils.ts` is NOT touched (D-03) — only per-call prefix args change.

Purpose: Removes the single root cause of the breakage (validator reject at create) so flows can progress to their real assertions. After this plan, any remaining failure is selector/feature drift (handled in Plan 03), not a title problem. Also locks SC#3 (LinkedGroupId isolation) with a concrete DB assertion.
Output: 2 edited spec files (21 prefixed titles + 1 DB assertion), incrementally validated by re-running each spec @5277 and confirming the create step is no longer the blocker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-CONTEXT.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-RESEARCH.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-PATTERNS.md
@.planning/phases/364-restore-baseline-regresi-e2e-exam/364-01-SUMMARY.md

<interfaces>
<!-- Read-only contracts the executor must respect. DO NOT modify these files. -->

REST-06 validator — Controllers/AssessmentAdminController.cs:873-881 [VERIFIED read-only]:
```csharp
// Phase 339 REST-06: Validate Title pattern for standard Pre/Post tests
if (AssessmentTypeInput != "PrePostTest"
    && !string.IsNullOrEmpty(model.Title)
    && !System.Text.RegularExpressions.Regex.IsMatch(model.Title, @"^(Pre|Post)\s*Test\s+.+$"))
{ ModelState.AddModelError("Title", "Title harus pola '{Stage} Test {Track} {Lokasi}' ..."); }
```
- Regex is CASE-SENSITIVE (no RegexOptions). "Pre Test" (capital P + capital T) REQUIRED. "pre test"/"PRE TEST" REJECTED.
- Only active when AssessmentTypeInput != "PrePostTest" (standard mode). PrePostTest is EXEMPT -> FLOW P not edited.

uniqueTitle helper — tests/helpers/utils.ts:23 [DO NOT MODIFY — D-03]:
```typescript
export function uniqueTitle(prefix = 'E2E Test'): string { return `${prefix} ${Date.now()}`; }
```
- Only change the per-call ARGUMENT. The `${Date.now()}` timestamp is the SC#3 isolation guard.

DB assertion helper — tests/helpers/dbSnapshot.ts (already imported in exam-types as `import * as db`):
```typescript
queryScalar(sql: string): Promise<number>   // returns first integer line
```
- localhost-only guarded. exam-types FLOW K already extracts `assessmentId` at K1 (line 206) and already uses `db.queryScalar` at K5 (lines 287-295).
</interfaces>

<edit_table_exam_taking>
<!-- Source: 364-PATTERNS.md + grep verified 2026-06-11. Edit ONLY the string argument. Surrounding code unchanged. -->
tests/e2e/exam-taking.spec.ts — 10 edits (ALL standard-create, ALL hit validator):
| Line | BEFORE | AFTER |
|------|--------|-------|
| :35   | uniqueTitle('Legacy Exam')         | uniqueTitle('Pre Test Legacy Exam') |
| :316  | uniqueTitle('Token Exam')          | uniqueTitle('Pre Test Token Exam') |
| :402  | uniqueTitle('ForceClose Exam')     | uniqueTitle('Pre Test ForceClose Exam') |
| :548  | uniqueTitle('Package Exam')        | uniqueTitle('Pre Test Package Exam') |
| :702  | uniqueTitle('Proton T3 Interview') | uniqueTitle('Pre Test Proton T3 Interview') |
| :833  | uniqueTitle('Multi Worker')        | uniqueTitle('Pre Test Multi Worker') |
| :963  | uniqueTitle('Timer Expired')       | uniqueTitle('Pre Test Timer Expired') |
| :1057 | uniqueTitle('RealTime Mon')        | uniqueTitle('Pre Test RealTime Mon') |
| :1291 | uniqueTitle('EditTest')            | uniqueTitle('Pre Test EditTest') |
| :1404 | uniqueTitle('Abandon Test')        | uniqueTitle('Pre Test Abandon Test') |
</edit_table_exam_taking>

<edit_table_exam_types>
tests/e2e/exam-types.spec.ts — 11 edits + 2 DO-NOT-TOUCH:
| Line | BEFORE | AFTER | Note |
|------|--------|-------|------|
| :37   | uniqueTitle('[317-SMOKE-W0] Order Verify') | uniqueTitle('Pre Test [317-SMOKE-W0] Order Verify') | standard |
| :191  | uniqueTitle('[317-K] MA Exam')             | uniqueTitle('Pre Test [317-K] MA Exam') | standard + D-11 assertion goes in this FLOW |
| :310  | uniqueTitle('[317-L] Essay Exam')          | uniqueTitle('Pre Test [317-L] Essay Exam') | standard |
| :435  | uniqueTitle('[317-M] Mixed Exam')          | uniqueTitle('Pre Test [317-M] Mixed Exam') | standard |
| :588  | uniqueTitle('[317-N] NoReview Exam')       | uniqueTitle('Pre Test [317-N] NoReview Exam') | standard |
| :690  | uniqueTitle('[317-O] ExtraTime Exam')      | uniqueTitle('Pre Test [317-O] ExtraTime Exam') | standard |
| :860  | uniqueTitle('[318-P] PrePost Exam')        | UNCHANGED                              | EXEMPT (PrePostTest) — Pitfall 2, do NOT add prefix |
| :1065 | uniqueTitle('[318-Q] EWCD Past Exam')      | uniqueTitle('Pre Test [318-Q] EWCD Past Exam') | standard |
| :1151 | uniqueTitle('[318-R] Cert Exam')           | uniqueTitle('Pre Test [318-R] Cert Exam') | standard |
| :1278 | uniqueTitle('[318-S-TRUE] Review Exam')    | uniqueTitle('Pre Test [318-S-TRUE] Review Exam') | standard (titleTrue) |
| :1364 | uniqueTitle('[318-S-FALSE] NoReview Exam') | uniqueTitle('Pre Test [318-S-FALSE] NoReview Exam') | standard (titleFalse) |
| :1457 | uniqueTitle('[319-T] Manual CRUD')         | UNCHANGED                              | AddManualAssessment controller, no REST-06 — Pitfall 2, do NOT change |

> NOTE: line numbers may shift by ±1-2 as edits accumulate within a file. Match on the literal `uniqueTitle('<old marker>')` string, NOT solely on line number. The old marker text (e.g. `[317-K] MA Exam`, `Legacy Exam`) is unique per call site, so target by content.
</edit_table_exam_types>

<db_assertion_d11>
<!-- Source: 364-PATTERNS.md §DB Assertion pattern + 364-RESEARCH.md §Auto-Pair Mechanics. assessmentId is an integer extracted at exam-types K1 line 206. -->
Add this assertion inside exam-types FLOW K, in test K1 (right after `assessmentId` is parsed at line ~206). Insert AFTER the `assessmentId = parseInt(...)` line:

```typescript
// SC#3 / D-11 — auto-pair Phase 338 (TryAutoDetectCounterpartGroup) must NOT mis-set LinkedGroupId.
// uniqueTitle timestamp makes the "Post Test <rest>" counterpart impossible -> LinkedGroupId stays NULL.
const linkedNull = await db.queryScalar(
  `SELECT COUNT(*) FROM AssessmentSessions WHERE Id = ${assessmentId} AND LinkedGroupId IS NULL`
);
expect(linkedNull).toBe(1);
```

SECURITY (research §Security): `assessmentId` is an integer literal interpolated into SQL (NOT a user string) — injection-safe. DO NOT add any string-interpolated SQL. Reuse the existing `db` import already at the top of exam-types.spec.ts; do NOT add a new import.
</db_assertion_d11>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prefix the 10 standard-create titles in exam-taking.spec.ts with 'Pre Test '</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts (read current state — confirm the 10 uniqueTitle('<marker>') call sites exist with the BEFORE strings)
    - tests/helpers/utils.ts (confirm uniqueTitle signature — DO NOT edit this file)
    - 364-PATTERNS.md §exam-taking.spec.ts edit table (the canonical per-call edit list)
  </read_first>
  <action>
    Apply the 10 edits from the `<edit_table_exam_taking>` block above — change ONLY the string argument of each `uniqueTitle(...)` call, prepending `Pre Test ` (capital P, capital T, single spaces) to the existing marker. Concrete edits (target by the literal old string, line numbers approximate):

    - uniqueTitle('Legacy Exam')         -> uniqueTitle('Pre Test Legacy Exam')
    - uniqueTitle('Token Exam')          -> uniqueTitle('Pre Test Token Exam')
    - uniqueTitle('ForceClose Exam')     -> uniqueTitle('Pre Test ForceClose Exam')
    - uniqueTitle('Package Exam')        -> uniqueTitle('Pre Test Package Exam')
    - uniqueTitle('Proton T3 Interview') -> uniqueTitle('Pre Test Proton T3 Interview')
    - uniqueTitle('Multi Worker')        -> uniqueTitle('Pre Test Multi Worker')
    - uniqueTitle('Timer Expired')       -> uniqueTitle('Pre Test Timer Expired')
    - uniqueTitle('RealTime Mon')        -> uniqueTitle('Pre Test RealTime Mon')
    - uniqueTitle('EditTest')            -> uniqueTitle('Pre Test EditTest')
    - uniqueTitle('Abandon Test')        -> uniqueTitle('Pre Test Abandon Test')

    Do NOT change anything else: no struct/selector changes, no helper edits, no import changes in this task. The old marker text is preserved as the remainder so flows stay grep-able (D-02/D-04). Do NOT touch tests/helpers/utils.ts.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('tests/e2e/exam-taking.spec.ts','utf8');const m=(s.match(/uniqueTitle\('Pre Test /g)||[]).length;if(m!==10)throw new Error('expected 10 Pre Test prefixes, found '+m);if(/uniqueTitle\('Legacy Exam'\)/.test(s))throw new Error('Legacy Exam not prefixed');console.log('OK 10 prefixes')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "uniqueTitle('Pre Test " tests/e2e/exam-taking.spec.ts returns exactly 10.
    - None of the OLD unprefixed forms remain: grep for `uniqueTitle('Legacy Exam')`, `uniqueTitle('Token Exam')`, `uniqueTitle('ForceClose Exam')`, `uniqueTitle('Package Exam')`, `uniqueTitle('Proton T3 Interview')`, `uniqueTitle('Multi Worker')`, `uniqueTitle('Timer Expired')`, `uniqueTitle('RealTime Mon')`, `uniqueTitle('EditTest')`, `uniqueTitle('Abandon Test')` returns nothing.
    - Every prefix uses exact capitalization `Pre Test ` (capital P, capital T) — grep for lowercase `uniqueTitle('pre test ` or `uniqueTitle('PRE TEST ` returns nothing.
    - git diff tests/helpers/utils.ts is EMPTY (helper untouched).
    - TypeScript still parses: `cd tests && npx tsc --noEmit -p .` shows no new errors attributable to exam-taking.spec.ts.
  </acceptance_criteria>
  <done>All 10 exam-taking standard-create titles carry the `Pre Test ` prefix; old forms gone; utils.ts unchanged; file still type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Prefix the 11 standard-create titles in exam-types.spec.ts + add the D-11 LinkedGroupId assertion in FLOW K</name>
  <files>tests/e2e/exam-types.spec.ts</files>
  <read_first>
    - tests/e2e/exam-types.spec.ts (read current state — confirm the 11 standard call sites + FLOW P :860 + FLOW T :1457 + FLOW K K1 assessmentId extraction at ~line 206 + existing `import * as db` at top + existing db.queryScalar use in K5 ~287-295)
    - 364-PATTERNS.md §exam-types.spec.ts edit table + §DB Assertion pattern
    - 364-RESEARCH.md §Auto-Pair Mechanics (why LinkedGroupId stays NULL — SC#3 rationale)
  </read_first>
  <action>
    Two changes in exam-types.spec.ts:

    (A) Apply the 11 title-prefix edits from `<edit_table_exam_types>` — change ONLY the string argument, prepending `Pre Test `:
      - [317-SMOKE-W0] Order Verify, [317-K] MA Exam, [317-L] Essay Exam, [317-M] Mixed Exam,
        [317-N] NoReview Exam, [317-O] ExtraTime Exam, [318-Q] EWCD Past Exam, [318-R] Cert Exam,
        [318-S-TRUE] Review Exam, [318-S-FALSE] NoReview Exam -> each gets `Pre Test ` prepended.
      - DO NOT TOUCH uniqueTitle('[318-P] PrePost Exam') at :860 (PrePostTest EXEMPT — adding a prefix would break the P1 AssessmentType/LinkedSessionId assertions; Pitfall 2).
      - DO NOT TOUCH uniqueTitle('[319-T] Manual CRUD') at :1457 (AddManualAssessment controller, no REST-06; Pitfall 2).

    (B) Add the SC#3 / D-11 DB assertion inside FLOW K test K1, immediately AFTER the line:
        assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
      Insert exactly:
        // SC#3 / D-11 — auto-pair Phase 338 (TryAutoDetectCounterpartGroup) must NOT mis-set LinkedGroupId.
        // uniqueTitle timestamp makes the "Post Test <rest>" counterpart impossible -> LinkedGroupId stays NULL.
        const linkedNull = await db.queryScalar(
          `SELECT COUNT(*) FROM AssessmentSessions WHERE Id = ${assessmentId} AND LinkedGroupId IS NULL`
        );
        expect(linkedNull).toBe(1);
      Use the EXISTING `db` import (already `import * as db from '../helpers/dbSnapshot'` at the top) — do NOT add a new import. `assessmentId` is an integer (injection-safe). Do NOT add any other DB assertion or string-interpolated SQL.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('tests/e2e/exam-types.spec.ts','utf8');const m=(s.match(/uniqueTitle\('Pre Test /g)||[]).length;if(m!==11)throw new Error('expected 11 Pre Test prefixes, found '+m);if(!/uniqueTitle\('\[318-P\] PrePost Exam'\)/.test(s))throw new Error('FLOW P was wrongly changed');if(!/uniqueTitle\('\[319-T\] Manual CRUD'\)/.test(s))throw new Error('FLOW T was wrongly changed');if(!/LinkedGroupId IS NULL/.test(s))throw new Error('D-11 assertion missing');console.log('OK 11 prefixes + P/T intact + D-11 present')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "uniqueTitle('Pre Test " tests/e2e/exam-types.spec.ts returns exactly 11.
    - FLOW P unchanged: grep finds `uniqueTitle('[318-P] PrePost Exam')` still present (NOT prefixed).
    - FLOW T unchanged: grep finds `uniqueTitle('[319-T] Manual CRUD')` still present (NOT prefixed).
    - The D-11 assertion is present: grep finds `LinkedGroupId IS NULL` AND `expect(linkedNull).toBe(1)`.
    - No new import added: grep count of `from '../helpers/dbSnapshot'` is exactly 1 (the pre-existing import).
    - git diff tests/helpers/utils.ts is EMPTY.
    - TypeScript still parses: `cd tests && npx tsc --noEmit -p .` shows no new errors attributable to exam-types.spec.ts.
  </acceptance_criteria>
  <done>All 11 exam-types standard titles prefixed; FLOW P and FLOW T untouched; D-11 LinkedGroupId IS NULL assertion added to FLOW K reusing the existing db import; file type-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run both specs @5277 and confirm the create step is no longer the blocker</name>
  <files>tests/e2e/exam-taking.spec.ts, tests/e2e/exam-types.spec.ts</files>
  <read_first>
    - 364-01-SUMMARY.md (the baseline classification — compare: every TITLE failure must now disappear at the create step)
    - 364-RESEARCH.md §Playwright Run Config & Lifecycle (run mechanics, AD off, automatic BACKUP/RESTORE)
    - tests/e2e/exam-taking.spec.ts + tests/e2e/exam-types.spec.ts (post-edit state from Tasks 1-2)
  </read_first>
  <action>
    Start the app with AD off (`Authentication__UseActiveDirectory=false dotnet run`, env override — do NOT edit appsettings.json; PowerShell: `$env:Authentication__UseActiveDirectory="false"; dotnet run`). Wait for /Account/Login 200 @localhost:5277.

    From `tests/`, re-run both specs (lifecycle auto-BACKUP/RESTORE wraps each run):
      cd tests
      npx playwright test exam-taking.spec.ts --reporter=list
      npx playwright test exam-types.spec.ts --reporter=list

    Compare against the Task-01 baseline. The ONLY required outcome of THIS plan is: every flow that the baseline classified as TITLE-failure must now PASS its create step (no `expect(success||alert)` false, no `#successModal.show` 15s timeout). Flows may STILL fail at later steps due to selector/feature drift — that is expected and is Plan 03's job. Do NOT fix drift here.

    Record in the plan SUMMARY: (a) which create steps now pass that previously failed on title, (b) which flows still fail and at what step (these are the drift list handed to Plan 03), (c) the FLOW K D-11 assertion result (LinkedGroupId IS NULL = 1). Confirm DB restored clean via SEED_JOURNAL tail (`cleaned`).
  </action>
  <verify>
    <automated>cd tests; npx playwright test exam-taking.spec.ts exam-types.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - Both specs re-ran @5277 with AD off after the edits; raw list output captured.
    - For every flow the baseline tagged TITLE, the create step now passes (no validator-reject signature in the new run) — documented per-flow in the SUMMARY.
    - FLOW K D-11 assertion executed and `linkedNull === 1` (LinkedGroupId IS NULL confirmed) — recorded.
    - Remaining failures (if any) are all POST-create (drift), enumerated as the hand-off list for Plan 03.
    - SEED_JOURNAL tail returns to `cleaned`; no residual `Pre Test%` rows left in DB after teardown.
    - git status shows ONLY tests/e2e/exam-taking.spec.ts + tests/e2e/exam-types.spec.ts modified (no production, no helpers, no utils.ts).
  </acceptance_criteria>
  <done>Re-run confirms zero remaining title-failures at create; D-11 assertion green; drift-only failures (if any) enumerated for Plan 03; DB clean; only the 2 spec files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test spec → REST-06 validator (POST /Admin/CreateAssessment) | Test sends title strings; validator is read-only production code (not modified). |
| exam-types FLOW K db.queryScalar → local SQL Server | Direct sqlcmd query with an INTEGER assessmentId; localhost-guarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-364-04 | Tampering | SQL injection via D-11 assertion (`Id = ${assessmentId}`) | mitigate | `assessmentId` is parsed via `parseInt(...)` to a JS number (integer) before interpolation — not a user-controlled string. No string interpolation into SQL is added. Pattern matches the existing K5 `Id = ${sessionId}` integer query. |
| T-364-05 | Tampering | DB query against non-localhost server | mitigate | `dbSnapshot.runSqlcmd` rejects non-localhost `-S` (guard T-315-01); connection hardcoded to `localhost\SQLEXPRESS`. Reused, not extended. |
| T-364-06 | Spoofing | Test credentials (local accounts, password 123456) | accept | Local-only dev accounts against local DB with AD off; no production secret introduced. Unchanged by this plan. |
</threat_model>

<verification>
- exam-taking.spec.ts: exactly 10 `uniqueTitle('Pre Test ...')` prefixes; no old unprefixed forms; utils.ts unchanged.
- exam-types.spec.ts: exactly 11 `Pre Test ` prefixes; FLOW P (:860) and FLOW T (:1457) unchanged; D-11 `LinkedGroupId IS NULL` assertion present.
- Re-run @5277: every baseline TITLE-failure now passes the create step; D-11 assertion green (linkedNull===1).
- Only the 2 spec files modified; DB restored clean.
</verification>

<success_criteria>
- SC#1 satisfied: all 21 standard titles comply with REST-06 (validator no longer rejects at create).
- SC#3 satisfied: FLOW K asserts `LinkedGroupId IS NULL` (auto-pair isolation proven via timestamp uniqueness).
- D-03 honored: per-call edits only; `tests/helpers/utils.ts` untouched.
- D-12 / Pitfall 2 honored: FLOW P and FLOW T left exactly as-is.
- Drift-only failures (if any) cleanly enumerated for Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/364-restore-baseline-regresi-e2e-exam/364-02-SUMMARY.md` recording: the 21 edits applied, the D-11 assertion result, the re-run create-step results (TITLE failures resolved), and the enumerated drift list handed to Plan 03.
</output>
