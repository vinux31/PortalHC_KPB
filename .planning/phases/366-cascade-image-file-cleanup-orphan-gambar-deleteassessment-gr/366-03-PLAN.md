---
phase: 366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - HcPortal.Tests/ImageCleanupIntegrationTests.cs
  - HcPortal.Tests/PackageImageDeleteTests.cs
autonomous: false
requirements: [SC4-test-uat, SC3-shared-survive]
must_haves:
  truths:
    - "Integration test real-SQL membuktikan file fisik terhapus saat cascade delete session bergambar (SC#2)"
    - "Integration test real-SQL membuktikan shared-path Pre/Post SELAMAT saat 1 sisi dihapus (SC#3 — regresi SYN-01 termahal)"
    - "Tidak ada 2 implementasi ref-count yang divergen (mirror palsu PackageImageDeleteTests direkonsiliasi ke helper produksi)"
    - "UAT @5277: hapus assessment bergambar → file fisik bersih; hapus 1 sisi Pre/Post share gambar → file selamat"
  artifacts:
    - path: "HcPortal.Tests/ImageCleanupIntegrationTests.cs"
      provides: "Integration test real-SQL disposable (Phase 344 TEST-05) untuk helper produksi"
      contains: "ImageFileCleanup.DeleteUnreferencedAsync"
      min_lines: 60
    - path: "HcPortal.Tests/PackageImageDeleteTests.cs"
      provides: "Mirror divergen direkonsiliasi (D-04)"
  key_links:
    - from: "ImageCleanupIntegrationTests"
      to: "Helpers/ImageFileCleanup.DeleteUnreferencedAsync (produksi)"
      via: "panggil helper produksi nyata atas real-SQL DbContext"
      pattern: "ImageFileCleanup\\.DeleteUnreferencedAsync"
---

<objective>
Buktikan helper + 3 install (Plan 01/02) bekerja end-to-end: integration test real-SQL (D-03, pola Phase 344 TEST-05 disposable) yang assert (a) file fisik terhapus saat cascade delete session bergambar, (b) **shared-path Pre/Post SELAMAT** saat salah satu sisi dihapus (SC#3 — regresi paling mahal SYN-01). Plus rekonsiliasi `PackageImageDeleteTests.cs` agar tak ada 2 logika ref-count divergen (D-04). Plus UAT manual @5277.

Purpose: tutup SC#4 (test + UAT) + cegah drift logika ganda.
Output: `HcPortal.Tests/ImageCleanupIntegrationTests.cs` (baru) + `PackageImageDeleteTests.cs` direkonsiliasi + UAT live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-CONTEXT.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-01-SUMMARY.md
@.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-02-SUMMARY.md

<interfaces>
<!-- Kontrak helper produksi yg di-exercise test ini. -->

Helper produksi (Plan 01):
```csharp
public static async Task ImageFileCleanup.DeleteUnreferencedAsync(
    ApplicationDbContext ctx, string webRootPath, ILogger logger,
    IEnumerable<string> paths, string source = "")
```

Fixture disposable real-SQL (analog OrgLabelMigrationIntegrationTests.cs:24-66):
```csharp
// IAsyncLifetime: HcPortalDB_Test_<guid> di localhost\SQLEXPRESS;
// InitializeAsync -> MigrateAsync (full chain); DisposeAsync -> EnsureDeletedAsync;
// catch mid-setup -> EnsureDeletedAsync + XunitException. NEVER sentuh HcPortalDB_Dev.
// connstr: Server=localhost\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30
```

Model untuk seed (verifikasi nama prop saat baca file):
```csharp
// PackageQuestion { ImagePath, Options : List<PackageOption>, AssessmentPackageId / PackageId }
// PackageOption   { ImagePath, IsCorrect, OptionText }
// AssessmentPackage { AssessmentSessionId, Questions }
// AssessmentSession { Title, AssessmentType, ... }
```

Test temp-dir + write bytes (analog PackageImageDeleteTests.cs:20-25,48):
```csharp
var dir = Path.Combine(Path.GetTempPath(), "hcportal-test-" + Guid.NewGuid().ToString("N").Substring(0,8));
Directory.CreateDirectory(dir);
File.WriteAllBytes(path, new byte[] { 1, 2, 3 });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat ImageCleanupIntegrationTests.cs (real-SQL disposable) — 2 kasus wajib</name>
  <files>HcPortal.Tests/ImageCleanupIntegrationTests.cs</files>
  <read_first>
    - .planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md (bagian "NEW integration test" — excerpt (c) fixture harness + class wiring + 2 [Fact] seed-act-assert + catatan webRootPath temp-dir)
    - HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs:24-66 (fixture IAsyncLifetime disposable — connstr, MigrateAsync, EnsureDeletedAsync, M1 catch)
    - HcPortal.Tests/ProtonYearGateIntegrationTests.cs (pola class-level wiring IClassFixture + per-test seed-act-assert atas real-SQL)
    - HcPortal.Tests/PackageImageDeleteTests.cs:20-25,30-39,41-60 (MakeTempDir + File.WriteAllBytes + kontrak ref-count yg sudah ada)
    - Helpers/ImageFileCleanup.cs (helper produksi yg di-test)
    - Controllers/AssessmentAdminController.cs:2314-2334 (urutan collect/RemoveRange/commit yg test tiru)
  </read_first>
  <action>
Buat file BARU `HcPortal.Tests/ImageCleanupIntegrationTests.cs`. Pakai fixture disposable real-SQL — SALIN pola `OrgLabelMigrationFixture` (OrgLabelMigrationIntegrationTests.cs:24-66) verbatim, ganti nama kelas jadi `ImageCleanupFixture` (DbName `HcPortalDB_Test_<guid>`, connstr SQLEXPRESS, `MigrateAsync` di InitializeAsync, `EnsureDeletedAsync` di DisposeAsync + catch M1). Hilangkan pemanggilan `SeedData.SeedOrganizationLevelLabelsAsync` (tak relevan — phase ini tak butuh seed org-label). Kalau `MigrateAsync` sukses tanpa seed tambahan, biarkan tanpa seed.

Tag kelas test: `[Trait("Category", "Integration")]` + `: IClassFixture<ImageCleanupFixture>`.

**Helper test-local untuk temp webroot** (tiru PackageImageDeleteTests `MakeTempDir`):
```csharp
private static string MakeTempWebRoot()
{
    var dir = Path.Combine(Path.GetTempPath(), "hcportal-imgtest-" + Guid.NewGuid().ToString("N").Substring(0, 8));
    Directory.CreateDirectory(dir);
    return dir;
}
private static string WriteFakeImage(string webRoot, string relUrl)
{
    var physical = Path.Combine(webRoot, relUrl.TrimStart('/').Replace('/', Path.DirectorySeparatorChar));
    Directory.CreateDirectory(Path.GetDirectoryName(physical)!);
    File.WriteAllBytes(physical, new byte[] { 1, 2, 3 });
    return physical;
}
private static ILogger SilentLogger() => Microsoft.Extensions.Logging.Abstractions.NullLogger.Instance;
```

**[Fact] 1 — OrphanPath_Deleted_WhenFullCascade (SC#2):**
1. seed: 1 AssessmentSession + 1 AssessmentPackage + 1 PackageQuestion dgn `ImagePath = "/uploads/questions/test/orphan.jpg"`; tulis file fisik via `WriteFakeImage(webRoot, relUrl)`. SaveChanges.
2. act (tiru cascade DeleteAssessment): `RemoveRange` Options-Questions-Packages-Session yg merefer path, `SaveChangesAsync` (commit) lalu panggil `await ImageFileCleanup.DeleteUnreferencedAsync(ctx, webRoot, SilentLogger(), new[]{ relUrl }, "test")`.
3. assert: `Assert.False(File.Exists(physical))` — orphan terhapus.

**[Fact] 2 — SharedPrePostPath_Survives_WhenOneSideDeleted (SC#3, regresi termahal):**
1. seed: 1 Pre session+package+question + 1 Post session+package+question yang `ImagePath`-nya SAMA `"/uploads/questions/test/shared.jpg"` (string identik = shared-file v24.0). Tulis 1 file fisik. SaveChanges.
2. act: hapus HANYA baris Pre (RemoveRange Pre question/package/session), `SaveChangesAsync` (Post MASIH merefer path) lalu panggil helper dgn `new[]{ relUrl }`.
3. assert: `Assert.True(File.Exists(physical))` — shared selamat karena `AnyAsync` masih true (Post pakai).

CATATAN:
- Gunakan `await using var ctx = new ApplicationDbContext(_fixture.Options);` per test (tiap [Fact] DbContext baru, pola ProtonYearGateIntegrationTests).
- `webRootPath` = temp-dir (`MakeTempWebRoot()`), BUKAN `_env.WebRootPath` — test tulis byte nyata di temp.
- Verifikasi nama prop FK saat seed (mis. `PackageQuestion.AssessmentPackageId`/`PackageId`, `AssessmentPackage.AssessmentSessionId`) dgn baca model — kalau salah, build error CS akan nangkap.
- Cleanup temp-dir di akhir tiap [Fact] (`try/finally Directory.Delete(dir, true)`) — best-effort, jangan gagalkan assert.
- Test ini NEVER sentuh `HcPortalDB_Dev` (DB disposable per-guid, drop di DisposeAsync) jadi TIDAK perlu SEED_WORKFLOW snapshot.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ImageCleanupIntegrationTests" 2>&1 | Select-String -Pattern "Passed!|Failed!|error|Passed:|Failed:" | Select-Object -First 8</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/ImageCleanupIntegrationTests.cs` ada; `Select-String "ImageFileCleanup.DeleteUnreferencedAsync" HcPortal.Tests/ImageCleanupIntegrationTests.cs` cocok minimal 2x.
    - `OrphanPath_Deleted_WhenFullCascade` + `SharedPrePostPath_Survives_WhenOneSideDeleted` keduanya ada di file.
    - `Select-String "Trait\(.Category., .Integration.\)" HcPortal.Tests/ImageCleanupIntegrationTests.cs` cocok 1x (skippable di CI SQL-less).
    - `dotnet build` exit 0.
    - `dotnet test --filter "FullyQualifiedName~ImageCleanupIntegrationTests"` exit 0 (2 [Fact] Passed).
  </acceptance_criteria>
  <done>2 [Fact] integration real-SQL hijau: orphan terhapus (SC#2) + shared Pre/Post selamat (SC#3).</done>
</task>

<task type="auto">
  <name>Task 2: Rekonsiliasi PackageImageDeleteTests.cs (D-04 — hindari mirror divergen) + full suite</name>
  <files>HcPortal.Tests/PackageImageDeleteTests.cs</files>
  <read_first>
    - .planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-PATTERNS.md (bagian "REFACTOR PackageImageDeleteTests.cs (D-04)" — nuansa async/DbContext + opsi (a)/(b))
    - HcPortal.Tests/PackageImageDeleteTests.cs (full — mirror DeleteIfUnreferenced :34-39, PathStillReferenced :30-31, ApplyIntent/PathStillReferenced :132-151 yg DI LUAR scope)
    - HcPortal.Tests/ImageCleanupIntegrationTests.cs (test produksi dari Task 1)
  </read_first>
  <action>
Tujuan D-04: jangan tinggalkan DUA implementasi ref-count yang bisa drift. Helper produksi `async`+butuh `ApplicationDbContext`; `[Fact]`-`[Fact]` di `PackageImageDeleteTests.cs` murni in-memory (tanpa DbContext). Pilih **opsi (b)** dari PATTERNS.md (nilai tinggi, risiko rendah):

1. Tambahkan komentar header XML di mirror `DeleteIfUnreferenced` (:34-39) yang menandai bahwa **sumber kebenaran produksi sekarang = `Helpers.ImageFileCleanup.DeleteUnreferencedAsync`** dan integration test (`ImageCleanupIntegrationTests`) meng-exercise helper produksi end-to-end atas real-SQL. Mirror in-memory ini DIPERTAHANKAN HANYA sebagai fast logic-contract test (tak butuh SQL), BUKAN implementasi paralel.
   - Komentar wajib menyebut persis: "Production source of truth: Helpers/ImageFileCleanup.cs. Integration coverage: ImageCleanupIntegrationTests.cs."
2. JANGAN sentuh `ApplyIntent` / `PathStillReferenced` (:30-31,132-151) yang menguji EditQuestion replace-intent — itu di luar scope Phase 366. Biarkan utuh.
3. (Opsional jika praktis tanpa effort berat) Tambah 1 `[Fact]` referensi-silang yang men-assert kontrak yang sama dgn integration test secara in-memory cepat (shared-path skip), supaya kontrak terdokumentasi dua tingkat (cepat in-memory + lambat real-SQL). Jika menambah fixture/DbContext bikin test ini berat, SKIP — cukup langkah 1+2.

Catatan: tujuan akhir = pembaca tahu mana sumber kebenaran. Jangan menulis ulang logika ref-count ketiga.

Lalu jalankan FULL suite untuk pastikan zero regresi lintas test (termasuk PackageImageSyncTests, AssessmentWindowRemovalTests Phase 370, dll). Integration test boleh di-skip di run SQL-less, tapi di lokal (SQLEXPRESS ada) WAJIB ikut hijau.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | Select-String -Pattern "Build succeeded|error" | Select-Object -First 3; dotnet test 2>&1 | Select-String -Pattern "Passed!|Failed!|Passed:|Failed:|Total tests" | Select-Object -First 8</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String "Helpers/ImageFileCleanup.cs|ImageCleanupIntegrationTests" HcPortal.Tests/PackageImageDeleteTests.cs` cocok (komentar sumber-kebenaran ada).
    - `ApplyIntent`/`PathStillReferenced` masih ada di PackageImageDeleteTests.cs (tak terhapus — out of scope).
    - `dotnet build` exit 0.
    - `dotnet test` full suite exit 0 (0 Failed) — termasuk integration test bila SQLEXPRESS tersedia lokal.
  </acceptance_criteria>
  <done>Mirror divergen direkonsiliasi (sumber kebenaran ditandai, D-04); full suite hijau tanpa regresi.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT manual @localhost:5277 (SC#4) — file fisik bersih + shared selamat</name>
  <what-built>
    Helper image-cleanup terpasang di 3 cascade-delete (`DeleteAssessment`/`DeleteAssessmentGroup`/`DeletePrePostGroup`): hapus assessment bergambar kini ikut membersihkan file fisik orphan di `wwwroot/uploads/questions/`, dengan ref-count menjaga shared-path Pre/Post tetap selamat saat hanya 1 sisi dihapus.
  </what-built>
  <how-to-verify>
    PRA: AD off lokal — jalankan `Authentication__UseActiveDirectory=false dotnet run` lalu buka `http://localhost:5277`.
    (Catatan SEED: kalau perlu seed temporary untuk skenario uji, ikuti CLAUDE.md Seed Data Workflow — snapshot DB lokal + catat `docs/SEED_JOURNAL.md` + restore setelah selesai.)

    SKENARIO A — orphan dibersihkan (SC#2):
    1. Login admin (`admin@pertamina.com`), buat/identifikasi 1 assessment NON Pre-Post (individual) dgn paket bergambar (soal/opsi punya gambar). Catat folder/file di `wwwroot/uploads/questions/{packageId}/`.
    2. Hapus assessment itu via tombol Hapus di ManageAssessment.
    3. Cek filesystem: file gambar paket itu HILANG dari `wwwroot/uploads/questions/{packageId}/`. (Folder kosong boleh tetap ada — di luar scope phase ini.)
    Harapan: file fisik orphan terhapus; tidak ada error UI; assessment hilang dari list.

    SKENARIO B — shared Pre/Post selamat saat 1 sisi dihapus (SC#3, paling kritis):
    1. Buat Pre-Post group dgn SamePackage (Pre & Post share gambar — sync v24.0).
    2. Hapus HANYA salah satu sisi via jalur yang TIDAK menghapus group sekaligus (mis. DeletePackage/DeleteQuestion pada satu paket, atau hapus 1 session non-group bila skenario memungkinkan).
    3. Cek filesystem: file gambar yang masih dipakai sisi lain (Post) HARUS MASIH ADA.
    Harapan: file shared SELAMAT.

    SKENARIO C — hapus group (Pre+Post) (SC#2 untuk DeletePrePostGroup):
    1. Hapus seluruh Pre-Post group via "Hapus Grup".
    2. Cek: file gambar group itu terhapus (tak ada sisi lain yang pakai → boleh dihapus).
    Harapan: file terhapus bersih.
  </how-to-verify>
  <action>
Checkpoint manual — JALANKAN setelah Task 1+2 hijau. Eksekutor: start app AD-off (`Authentication__UseActiveDirectory=false dotnet run`), buka `http://localhost:5277`, lakukan 3 skenario di `<how-to-verify>` (A orphan bersih / B shared selamat / C group bersih), cek filesystem `wwwroot/uploads/questions/` di tiap langkah. Bila perlu seed temporary ikuti CLAUDE.md Seed Data Workflow (snapshot + journal + restore). Lalu minta human konfirmasi.
  </action>
  <verify>
    <automated>MISSING — UAT manual via browser @5277 (visual + filesystem check); tak ada automated command. SC#2+SC#3 sudah ter-cover otomatis oleh ImageCleanupIntegrationTests (Task 1) — checkpoint ini konfirmasi end-to-end real app.</automated>
  </verify>
  <done>Human mengetik "approved" — ketiga skenario UAT (orphan bersih / shared selamat / group bersih) sesuai harapan @5277.</done>
  <resume-signal>Ketik "approved" jika ketiga skenario sesuai harapan, atau deskripsikan masalah (mis. file tak terhapus / file shared malah hilang).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> HcPortalDB_Test_<guid> (real SQLEXPRESS) | DB disposable per-guid; localhost + Integrated Security; tak pernah sentuh HcPortalDB_Dev |
| test -> temp webroot filesystem | tulis byte di `Path.GetTempPath()` temp-dir, di-cleanup best-effort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-366-07 | Tampering | test seed path traversal | accept | Test pakai relUrl literal `/uploads/questions/test/*.jpg` (controlled by test), bukan input eksternal; `Path.Combine` confined di temp-dir. Low risk (test-only). |
| T-366-08 | Denial of Service | DB test guid bocor (tak ke-drop) | mitigate | Fixture `DisposeAsync` `EnsureDeletedAsync` + catch M1 di `InitializeAsync` drop DB pada kegagalan mid-setup (pola Phase 344 TEST-05). |
| T-366-09 | Info Disclosure | UAT pakai DB Dev lokal | mitigate | UAT @localhost:5277 atas DB lokal; bila butuh seed temporary ikuti SEED_WORKFLOW (snapshot + journal + restore). Tak push, tak edit Dev/Prod (CLAUDE.md). |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~ImageCleanupIntegrationTests"` hijau (2 [Fact]: SC#2 + SC#3).
- `dotnet test` full suite hijau (0 Failed) — zero regresi lintas test existing.
- `PackageImageDeleteTests.cs` ditandai sumber-kebenaran produksi (D-04); `ApplyIntent`/`PathStillReferenced` utuh.
- UAT @5277 3 skenario (A orphan bersih / B shared selamat / C group bersih) di-approve human.
</verification>

<success_criteria>
- SC#4 terpenuhi: integration test real-SQL hijau (file fisik terhapus + shared selamat) + UAT manual approved.
- SC#3 (shared Pre/Post selamat) terbukti otomatis (test) DAN manual (UAT).
- Satu sumber kebenaran ref-count (helper produksi) — tak ada mirror divergen.
</success_criteria>

<notes>
**Migration=false. NOT pushed.** Verifikasi lokal saja. Notifikasi IT TIDAK perlu flag migration (tak ada schema change).
**SEED_WORKFLOW:** integration test pakai DB disposable (`HcPortalDB_Test_<guid>`) yang TIDAK menyentuh `HcPortalDB_Dev` → tak perlu snapshot. UAT manual: kalau menyeed data temporary di DB lokal, WAJIB ikuti CLAUDE.md Seed Data Workflow (snapshot + journal + restore).
**File-overlap Phase 367:** test ini mengunci kontrak helper image — kalau 367 mengubah cascade, test ini mendeteksi regresi cleanup gambar. Berguna sebagai guardrail.
</notes>

<output>
After completion, create `.planning/phases/366-cascade-image-file-cleanup-orphan-gambar-deleteassessment-gr/366-03-SUMMARY.md`
</output>
