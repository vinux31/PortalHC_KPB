---
phase: 367-delete-records-cascade-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/RecordCascadeDeleteService.cs
  - Program.cs
  - HcPortal.Tests/RecordCascadeServiceTests.cs
  - HcPortal.Tests/MirrorHeuristicTests.cs
autonomous: true
requirements: ["#15", "spec-3.1-traversal", "spec-3.1-preview"]

must_haves:
  truths:
    - "BuildPreviewAsync mengembalikan pohon korban cascade (root + turunan renewal lintas tabel) TANPA mutasi DB"
    - "Traversal BFS guard cycle (HashSet visited) — chain A->B->A lintas tabel tidak loop tak terhingga"
    - "Preview set == execute traversal set (invariant: keduanya pakai metode traversal yang sama)"
    - "Kandidat mirror legacy (judul match + tanggal ±1 hari) muncul di preview sebagai IsMirrorCandidate=true (read-only, tidak auto-hapus)"
  artifacts:
    - path: "Services/RecordCascadeDeleteService.cs"
      provides: "Cascade engine: record CascadeNode + BuildPreviewAsync (BFS read-only) + CollectCascadeIds (shared traversal) + mirror heuristik"
      min_lines: 90
    - path: "HcPortal.Tests/RecordCascadeServiceTests.cs"
      provides: "Unit traversal multi-level + cycle guard + preview-set determinism"
    - path: "HcPortal.Tests/MirrorHeuristicTests.cs"
      provides: "Unit mirror ±1 hari match/non-match"
  key_links:
    - from: "Program.cs"
      to: "RecordCascadeDeleteService"
      via: "builder.Services.AddScoped"
      pattern: "AddScoped<HcPortal.Services.RecordCascadeDeleteService>"
    - from: "RecordCascadeDeleteService.BuildPreviewAsync"
      to: "RecordCascadeDeleteService.CollectCascadeIds"
      via: "shared BFS traversal (preview==execute)"
      pattern: "CollectCascadeIds"
---

<objective>
Bangun fondasi `RecordCascadeDeleteService` — bagian READ-ONLY: kontrak `CascadeNode`, traversal BFS bersama (`CollectCascadeIds`) yang dipakai SAMA oleh preview dan (nanti) execute, metode `BuildPreviewAsync` yang mengembalikan pohon korban tanpa mutasi, dan heuristik kandidat mirror legacy (#15). Daftarkan service di DI. Plan 367-02 (Wave 2) menambah `ExecuteAsync` (mutasi) di file service yang sama.

Purpose: Cascade engine = ORKESTRASI, bukan primitif baru. Traversal renewal lintas `TrainingRecords`↔`AssessmentSessions` (kolom `RenewsTrainingId`/`RenewsSessionId`) adalah inti yang memperbaiki kasus Rino (#3): worker masih lihat sesi pasca hapus karena turunan renewal tak ikut terhapus. Invariant kritis: **preview set == execute set** (admin lihat X, hapus X persis).
Output: `Services/RecordCascadeDeleteService.cs` (preview + traversal + mirror), DI di `Program.cs`, 2 file unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-PATTERNS.md

<interfaces>
<!-- Kontrak yang HARUS dipakai executor — TIDAK perlu eksplorasi codebase. Sumber: RESEARCH §"Cascade Engine Architecture" + Models (verified 2026-06-12). -->

Relasi FK renewal (ApplicationDbContext.cs, OnDelete NoAction — app-level traversal):
- TrainingRecord.RenewsTrainingId (int?, :62), TrainingRecord.RenewsSessionId (int?, :68) — mutual-exclusive
- AssessmentSession.RenewsSessionId (int?, :116), AssessmentSession.RenewsTrainingId (int?, :123) — mutual-exclusive

Arah anak->induk: kolom `RenewsXId` di ANAK menunjuk Id INDUK. Untuk cari ANAK dari node induk `id`:
- Jika induk = AssessmentSession (id): anak TR = TrainingRecords.Where(t => t.RenewsSessionId==id); anak AS = AssessmentSessions.Where(a => a.RenewsSessionId==id)
- Jika induk = TrainingRecord (id): anak TR = TrainingRecords.Where(t => t.RenewsTrainingId==id); anak AS = AssessmentSessions.Where(a => a.RenewsTrainingId==id)
GOTCHA (Pitfall 2, komentar existing TrainingAdminController :987): node session -> anak via RenewsSessionId; node training -> anak via RenewsTrainingId. JANGAN tertukar.

Field model yang dipakai (verified):
- AssessmentSession: Id, UserId (string), Title (string), Schedule (DateTime), ProtonTrackId (int?)
- TrainingRecord: Id, UserId (string), Judul (string, :14), Tanggal (DateTime, :17)
- ApplicationUser: Id, FullName (untuk OwnerName)

Signature target service (RESEARCH §"Signature yang direkomendasikan"):
```csharp
public record CascadeNode(string Type, int Id, string Title, DateTime Date, string OwnerName, bool IsRoot, bool IsMirrorCandidate);
// Type ∈ {"training","session"}
public async Task<List<CascadeNode>> BuildPreviewAsync(string rootType, int rootId);
```

Heuristik mirror (#15, RESEARCH §"Mirror Legacy", BEDA dari guard duplikat #12 yang EXACT):
```csharp
// Untuk SETIAP session node terhapus, cari TrainingRecord mirror milik user yang sama:
var mirrorCandidates = await _context.TrainingRecords
    .Where(t => t.UserId == session.UserId
        && (t.Judul == session.Title || t.Judul == "Assessment: " + session.Title)
        && t.Tanggal >= session.Schedule.AddDays(-1)
        && t.Tanggal <= session.Schedule.AddDays(1))   // ±1 hari HANYA mirror preview
    .ToListAsync();
```

Pola DI existing (Program.cs:51-68, AddScoped service kelas konkret):
```csharp
builder.Services.AddScoped<HcPortal.Services.ProtonCompletionService>();
```
</interfaces>

<preview-analog>
<!-- Konvensi GET-preview-partial read-only. Sumber: CoachMappingController.cs:1196-1227 (verified). -->
[HttpGet] [Authorize(Roles="Admin, HC")] — entity-exists guard + read-only count queries + NO mutation.
Untuk 367 metode BuildPreviewAsync sendiri TIDAK ber-attribute (itu service), tapi WAJIB read-only (zero RemoveRange/Add/SaveChanges).
</preview-analog>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kontrak CascadeNode + traversal BFS bersama (CollectCascadeIds) + cycle guard</name>
  <read_first>
    - Services/ProtonCompletionService.cs (lihat pola kelas service: ctor inject ApplicationDbContext + ILogger, namespace HcPortal.Services)
    - Controllers/AssessmentAdminController.cs:2185-2383 (gold-standard DeleteAssessment — pahami artefak per node yang nanti diorkestrasi 02; di plan ini HANYA traversal)
    - Models/AssessmentSession.cs (konfirmasi field Id/UserId/Title/Schedule/ProtonTrackId + kolom RenewsSessionId/RenewsTrainingId)
    - Models/TrainingRecord.cs (konfirmasi Judul/Tanggal/UserId + RenewsTrainingId/RenewsSessionId)
    - .planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md §"Cascade Engine Architecture" (traversal blueprint + signature)
  </read_first>
  <behavior>
    - Test: chain induk session(1) -> anak TR(via RenewsSessionId=1) -> cucu session(via RenewsTrainingId=anakId) → CollectCascadeIds mengembalikan 3 node (1 session root + 1 training + 1 session), semua ter-visit sekali.
    - Test: cycle buatan A(session) <-> B(training) saling RenewsXId → traversal berhenti (HashSet visited), tidak infinite loop, tiap node muncul tepat sekali.
    - Test: node tanpa anak → hasil hanya root (1 node).
    - Test: node tipe "training" sebagai root → cari anak via RenewsTrainingId (bukan RenewsSessionId).
  </behavior>
  <action>
Buat `Services/RecordCascadeDeleteService.cs`, namespace `HcPortal.Services`. Ctor inject (minimal untuk plan ini): `ApplicationDbContext _context`, `ILogger<RecordCascadeDeleteService> _logger`. (Plan 02 menambah `ProtonCompletionService`, `AuditLogService`, `IWebHostEnvironment` — boleh sudah di-inject sekarang sebagai placeholder field jika tidak memicu error DI.)

Definisikan record publik:
```csharp
public record CascadeNode(string Type, int Id, string Title, DateTime Date, string OwnerName, bool IsRoot, bool IsMirrorCandidate);
```

Implementasikan traversal BFS BERSAMA (dipakai preview DAN execute — invariant preview==execute):
```csharp
// Mengembalikan daftar (Type, Id) terurut BFS, root dulu, dengan cycle guard.
// Type ∈ {"session","training"}.
internal async Task<List<(string Type, int Id)>> CollectCascadeIds(string rootType, int rootId)
{
    var visited = new HashSet<(string, int)>();
    var ordered = new List<(string, int)>();
    var queue = new Queue<(string Type, int Id)>();
    queue.Enqueue((rootType, rootId));
    visited.Add((rootType, rootId));
    while (queue.Count > 0)
    {
        var node = queue.Dequeue();
        ordered.Add(node);
        // cari anak — kolom renewal sesuai TIPE INDUK (Pitfall 2)
        List<int> childTrainingIds; List<int> childSessionIds;
        if (node.Type == "session")
        {
            childTrainingIds = await _context.TrainingRecords
                .Where(t => t.RenewsSessionId == node.Id).Select(t => t.Id).ToListAsync();
            childSessionIds = await _context.AssessmentSessions
                .Where(a => a.RenewsSessionId == node.Id).Select(a => a.Id).ToListAsync();
        }
        else // "training"
        {
            childTrainingIds = await _context.TrainingRecords
                .Where(t => t.RenewsTrainingId == node.Id).Select(t => t.Id).ToListAsync();
            childSessionIds = await _context.AssessmentSessions
                .Where(a => a.RenewsTrainingId == node.Id).Select(a => a.Id).ToListAsync();
        }
        foreach (var tid in childTrainingIds)
            if (visited.Add(("training", tid))) queue.Enqueue(("training", tid));
        foreach (var sid in childSessionIds)
            if (visited.Add(("session", sid))) queue.Enqueue(("session", sid));
    }
    return ordered;
}
```
Whitelist `rootType` HANYA "session"/"training" (V5 input validation) — lempar `ArgumentException` jika lain.

Buat `HcPortal.Tests/RecordCascadeServiceTests.cs` (unit, InMemory atau real-SQL kecil — pakai pola fixture jika butuh FK; traversal tidak butuh FK jadi InMemory cukup). Tulis 4 [Fact] sesuai <behavior>. Quick run: `dotnet test --filter "FullyQualifiedName~RecordCascadeServiceTests"`.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~RecordCascadeServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `Services/RecordCascadeDeleteService.cs` ada + `grep "public record CascadeNode"` match
    - `grep "HashSet<(string, int)> visited"` (atau ekuivalen tuple visited set) match di service
    - `grep "RenewsSessionId == node.Id"` DAN `grep "RenewsTrainingId == node.Id"` keduanya match (kedua arah anak)
    - `RecordCascadeServiceTests.cs` punya minimal 4 [Fact]; `dotnet test --filter "FullyQualifiedName~RecordCascadeServiceTests"` exit 0
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>Traversal BFS lintas tabel dengan cycle guard terimplementasi + teruji 4 kasus; service ter-compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BuildPreviewAsync (read-only tree) + heuristik mirror legacy (#15) + DI</name>
  <read_first>
    - Services/RecordCascadeDeleteService.cs (state aktual pasca Task 1 — CollectCascadeIds sudah ada)
    - Controllers/CoachMappingController.cs:1196-1227 (analog CoachCoacheeMappingDeletePreview — read-only preview convention)
    - Services/GradingService.cs:262-265 (konteks mirror auto-create REMOVED Phase 324 — kenapa #15 legacy residu)
    - Program.cs:51-68 (pola AddScoped service kelas konkret — lokasi sisip DI)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Mirror Legacy" (heuristik ±1 hari + JANGAN auto-hapus)
  </read_first>
  <behavior>
    - Test: TrainingRecord milik user X, Judul == "Assessment: " + session.Title, Tanggal = session.Schedule (selisih 0 hari) → terdeteksi mirror candidate.
    - Test: Tanggal = session.Schedule + 1 hari → terdeteksi (batas ±1 hari inklusif).
    - Test: Tanggal = session.Schedule + 2 hari → TIDAK terdeteksi.
    - Test: Judul beda total → TIDAK terdeteksi.
    - Test: user berbeda → TIDAK terdeteksi.
    - Test: BuildPreviewAsync tidak memutasi DB (count semua tabel before==after).
  </behavior>
  <action>
Tambah ke `RecordCascadeDeleteService.cs` metode publik:
```csharp
public async Task<List<CascadeNode>> BuildPreviewAsync(string rootType, int rootId)
```
Implementasi:
1. Panggil `CollectCascadeIds(rootType, rootId)` → daftar (Type, Id) BFS.
2. Untuk tiap (Type, Id): muat entitas + nama pemilik (`ApplicationUser.FullName` via UserId). Bangun `CascadeNode(Type, Id, Title, Date, OwnerName, IsRoot: (idx==0), IsMirrorCandidate: false)`.
   - session: Title=session.Title, Date=session.Schedule, UserId=session.UserId
   - training: Title=record.Judul, Date=record.Tanggal, UserId=record.UserId
3. Heuristik mirror (#15): untuk SETIAP node session dalam hasil, query (read-only) `TrainingRecords` mirror milik user yang sama dengan predikat ±1 hari (lihat <interfaces> mirror snippet). Untuk tiap mirror yang DITEMUKAN dan BELUM ada di daftar cascade (bukan turunan renewal yang sudah ter-traverse), tambah `CascadeNode(..., IsRoot:false, IsMirrorCandidate:true)`.
4. Return gabungan. WAJIB read-only: ZERO RemoveRange/Add/Update/SaveChanges di metode ini (V-preview).

Helper internal `FindMirrorCandidates(AssessmentSession session)` mengembalikan `List<TrainingRecord>` pakai predikat ±1 hari. Reuse di Task ini DAN nanti di execute (02) untuk validasi mirror-ID milik user yang sama (server-side, jangan percaya checkbox client — V5/IDOR).

Daftarkan DI di `Program.cs` (sisip dekat pola AddScoped existing, ~:57):
```csharp
builder.Services.AddScoped<HcPortal.Services.RecordCascadeDeleteService>();
```

Buat `HcPortal.Tests/MirrorHeuristicTests.cs` — 6 [Fact] sesuai <behavior> (5 mirror + 1 no-mutation preview). Pakai fixture real-SQL bila perlu FK user, atau InMemory bila cukup (mirror query tidak butuh FK enforcement). `[Trait("Category","Integration")]` HANYA jika pakai real-SQL.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~MirrorHeuristicTests"; dotnet test HcPortal.Tests --filter "FullyQualifiedName~RecordCascadeServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public async Task<List<CascadeNode>> BuildPreviewAsync"` match di service
    - `grep "AddDays(-1)"` DAN `grep "AddDays(1)"` keduanya match (toleransi ±1 hari mirror)
    - `grep "\"Assessment: \" + session.Title"` (atau ekuivalen) match — pola judul mirror
    - BuildPreviewAsync TIDAK mengandung `SaveChangesAsync`/`RemoveRange`/`.Add(` — verifikasi via baca metode (read-only invariant)
    - `Program.cs` `grep "AddScoped<HcPortal.Services.RecordCascadeDeleteService>"` match
    - `MirrorHeuristicTests.cs` ≥6 [Fact]; filter test exit 0; full quick suite `dotnet test --filter "Category!=Integration"` exit 0 (baseline ≥229 + tambahan)
  </acceptance_criteria>
  <done>BuildPreviewAsync mengembalikan pohon read-only + kandidat mirror ±1 hari ber-checkbox; DI terdaftar; mirror teruji match/non-match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → BuildPreviewAsync (via controller GET nanti) | Admin terotentikasi memicu preview; id arbitrer bisa dikirim |
| Service → DB (read-only) | Preview hanya membaca; tidak ada mutasi di plan ini |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-01 | Tampering | CollectCascadeIds rootType arbitrer | mitigate | Whitelist `rootType` ∈ {"session","training"}; ArgumentException jika lain (V5) |
| T-367-02 | DoS | Traversal cycle infinite loop | mitigate | HashSet `visited` cegah re-enqueue node yang sama (cycle guard) |
| T-367-03 | Info disclosure | Preview bocorkan data milik user lain | accept | Preview admin-only (controller GET di plan 04/05 ber-`[Authorize(Roles="Admin, HC")]`); admin sah lihat semua record. Mirror-ID divalidasi milik-user saat EXECUTE (plan 02) sebelum hapus |
| T-367-04 | Tampering | Mutasi tak sengaja di preview | mitigate | BuildPreviewAsync read-only invariant — acceptance criteria grep no SaveChanges/RemoveRange |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau (baseline 229 + test baru).
- Service `RecordCascadeDeleteService` ter-resolve via DI (build success konfirmasi registrasi).
- Invariant: BuildPreviewAsync dan execute (02) berbagi `CollectCascadeIds` yang SAMA → preview set == execute set.
</verification>

<success_criteria>
- CascadeNode + traversal BFS lintas tabel + cycle guard + BuildPreviewAsync read-only + mirror heuristik ±1 hari terimplementasi & ter-DI.
- 10 [Fact] (4 traversal + 6 mirror/preview) hijau.
- Migration = FALSE (zero schema change).
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-01-SUMMARY.md`
</output>
