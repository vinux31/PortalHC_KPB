---
phase: 367-delete-records-cascade-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["367-01"]
files_modified:
  - Services/RecordCascadeDeleteService.cs
  - HcPortal.Tests/RecordCascadeIntegrationTests.cs
  - HcPortal.Tests/RecordCascadeFileTests.cs
autonomous: true
requirements: ["#5", "#6", "#8", "#9", "#10", "#11", "#19", "L-04", "L-08", "spec-3.1-execute"]

must_haves:
  truths:
    - "ExecuteAsync menghapus SEMUA artefak per node session (EditLogs, PackageUserResponses, AttemptHistory, UPA, Packages+Q+O) parity gold-standard DeleteAssessment, dalam 1 transaction"
    - "PendingProtonBypass ber-LinkedAssessmentSessionId == node jadi Status='Dibatalkan' + ResolvedAt (soft-cancel L-04) — BUKAN hilang dari DB"
    - "LinkedSessionId pasangan di-null-clear SEBELUM Remove node (gain-score CMP tidak putus, #8)"
    - "Penanda Proton Origin='Exam' tercabut via RemoveExamOriginAsync untuk node Proton (#9); Interview/Bypass kebal"
    - "UserNotifications ActionUrl eksak-match (/CMP/StartExam/{id}) terhapus (#6); TrainingRecord node = 0 notif cleanup"
    - "File sertifikat (SertifikatUrl/ManualSertifikatUrl) dihapus POST-commit warn-only (#19, L-08); transaction rollback utuh saat exception"
    - "AuditLog 1 entri/operasi berisi aktor + node akar + jumlah & daftar Id turunan (L-08)"
  artifacts:
    - path: "Services/RecordCascadeDeleteService.cs"
      provides: "ExecuteAsync (1-tx cascade) + CascadeResult + per-node artefak + soft-cancel + null-clear + RemoveExamOrigin + notif + file post-commit + audit"
      contains: "public async Task<CascadeResult> ExecuteAsync"
    - path: "HcPortal.Tests/RecordCascadeIntegrationTests.cs"
      provides: "Real-SQL assert per-tabel (#5/#6/#8/#9/#10/#11/L-04/L-08) + rollback-on-exception + preview-set==execute-set"
    - path: "HcPortal.Tests/RecordCascadeFileTests.cs"
      provides: "[Fact] file sertifikat manual terhapus post-commit (#19)"
  key_links:
    - from: "RecordCascadeDeleteService.ExecuteAsync"
      to: "RecordCascadeDeleteService.CollectCascadeIds"
      via: "traversal yang SAMA dengan BuildPreviewAsync (preview==execute invariant)"
      pattern: "CollectCascadeIds"
    - from: "RecordCascadeDeleteService"
      to: "ProtonCompletionService.RemoveExamOriginAsync"
      via: "cabut penanda Origin='Exam' node Proton"
      pattern: "RemoveExamOriginAsync"
    - from: "RecordCascadeDeleteService"
      to: "PendingProtonBypasses"
      via: "soft-cancel Status='Dibatalkan'"
      pattern: "Status = \"Dibatalkan\""
---

<objective>
Tambah `ExecuteAsync` (mutasi, 1-transaction) ke `RecordCascadeDeleteService` — hapus semua node hasil traversal `CollectCascadeIds` (yang SAMA dengan preview) + seluruh artefak per node. Per node AssessmentSession = parity verbatim gold-standard `DeleteAssessment` (urutan RemoveRange Restrict-FK teruji 363) + 4 delta 367: LinkedSessionId null-clear (#8), PendingProtonBypass soft-cancel (L-04), RemoveExamOriginAsync (#9), UserNotifications eksak-match (#6). Kumpul file sertifikat (#19) SEBELUM Remove → File.Delete POST-commit warn-only (L-08). AuditLog 1 entri/operasi. Validasi via integration test real-SQL per-tabel + [Fact] file.

Purpose: Ini jantung fix kasus Rino (#3) — turunan renewal + artefak benar-benar terhapus, DB 100% bersih, transaction atomic. Soft-cancel bypass (L-04) menjaga jejak audit; null-clear pasangan (#8) menjaga gain-score CMP. **Invariant preview==execute** dijamin karena keduanya pakai `CollectCascadeIds` yang sama.
Output: `ExecuteAsync` + `CascadeResult` di service; 2 file test (integration real-SQL + file post-commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-PATTERNS.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-01-SUMMARY.md

<gold-standard>
<!-- Parity per node AssessmentSession. Sumber: AssessmentAdminController.cs:2271-2342 (VERIFIED 2026-06-12). Urutan RemoveRange Restrict-FK WAJIB dipertahankan. -->
```csharp
// 1. AssessmentEditLogs (Restrict FK — before session) — by AssessmentSessionId
var editLogs = await _context.AssessmentEditLogs.Where(e => e.AssessmentSessionId == id).ToListAsync();
if (editLogs.Any()) _context.AssessmentEditLogs.RemoveRange(editLogs);
// 2. PackageUserResponses (Restrict FK — before session) — by AssessmentSessionId
var pkgResponses = await _context.PackageUserResponses.Where(r => r.AssessmentSessionId == id).ToListAsync();
if (pkgResponses.Any()) _context.PackageUserResponses.RemoveRange(pkgResponses);
// 3. AssessmentAttemptHistory (no FK, orphan if not removed) — by SessionId (#5)
var attemptHistory = await _context.AssessmentAttemptHistory.Where(h => h.SessionId == id).ToListAsync();
if (attemptHistory.Any()) _context.AssessmentAttemptHistory.RemoveRange(attemptHistory);
// 4. UserPackageAssignments (Restrict FK ke Package — before packages) — by AssessmentSessionId
var pkgAssignments = await _context.UserPackageAssignments.Where(a => a.AssessmentSessionId == id).ToListAsync();
if (pkgAssignments.Any()) _context.UserPackageAssignments.RemoveRange(pkgAssignments);
// 5. AssessmentPackages + Questions + Options — Include + RemoveRange Options->Questions->Packages
var packages = await _context.AssessmentPackages.Include(p => p.Questions).ThenInclude(q => q.Options)
    .Where(p => p.AssessmentSessionId == id).ToListAsync();
// foreach pkg: RemoveRange(q.Options) ; RemoveRange(pkg.Questions) ; then RemoveRange(packages)
// 6. _context.AssessmentSessions.Remove(assessment);
// (luar loop) SaveChangesAsync ; tx.CommitAsync
```
CATATAN: collect ImagePath gambar SOAL = ranah Phase 366, DIKELOLA DI ENDPOINT tab 1 (plan 05), BUKAN di engine ini (Opsi B separasi). Engine ini HANYA collect file SERTIFIKAT (ManualSertifikatUrl/SertifikatUrl).
</gold-standard>

<delta-367>
<!-- 4 tambahan per node session yang TIDAK ada di gold-standard. Sumber: RESEARCH §"Execute per-node" + Code Examples. -->

#8 LinkedSessionId null-clear pasangan SEBELUM Remove node:
```csharp
// Pasangan Pre/Post mungkin BUKAN turunan renewal -> null-clear lebih aman daripada andalkan cascade.
var partners = await _context.AssessmentSessions.Where(a => a.LinkedSessionId == id).ToListAsync();
foreach (var p in partners) p.LinkedSessionId = null;
```

L-04 PendingProtonBypass soft-cancel (BUKAN Remove — jejak audit):
```csharp
var pendingToCancel = await _context.PendingProtonBypasses
    .Where(p => p.LinkedAssessmentSessionId == id && p.Status != "Dibatalkan").ToListAsync();
foreach (var p in pendingToCancel) { p.Status = "Dibatalkan"; p.ResolvedAt = DateTime.UtcNow; }
```

#9 Cabut penanda Proton (RemoveExamOriginAsync, signature verified ProtonCompletionService.cs:113-129):
```csharp
if (session.ProtonTrackId.HasValue)  // node Proton
    await _protonCompletion.RemoveExamOriginAsync(session.UserId, session.ProtonTrackId.Value);
// CATATAN A4: helper ini SaveChanges internal :127 — flush partial DALAM tx caller (belum commit -> rollback tetap utuh).
```

#6 UserNotifications eksak-match (L-05 inventory, HANYA pola terbukti aktif):
```csharp
var notifUrls = new[] {
    $"/CMP/StartExam/{id}",
    $"/CMP/AssessmentResults/{id}",   // template dormant — aman jika ada, no-op jika tidak
    $"/CMP/AssessmentDetails/{id}"
};
var orphanNotifs = await _context.UserNotifications
    .Where(n => n.ActionUrl != null && notifUrls.Contains(n.ActionUrl)).ToListAsync();
if (orphanNotifs.Any()) _context.UserNotifications.RemoveRange(orphanNotifs);
// TrainingRecord node: 0 notif cleanup (no record-bound ActionUrl pattern) — catat eksplisit di komentar.
```

#19 collect file sertifikat SEBELUM Remove:
- session node: kumpulkan `session.ManualSertifikatUrl` (jika field ada; verifikasi nama di Models/AssessmentSession.cs — bisa SertifikatUrl)
- training node: kumpulkan `record.SertifikatUrl`
File.Delete POST-commit, inner try/catch warn-only per file, confined webroot (V12):
```csharp
var path = Path.Combine(_env.WebRootPath, url.TrimStart('/'));
if (System.IO.File.Exists(path)) { try { System.IO.File.Delete(path); } catch (Exception ex) { _logger.LogWarning(ex, "..."); } }
```

L-08 AuditLog 1 entri/operasi (pola gold-standard :2349-2364, POST-commit, try/catch warn-only):
```csharp
await _auditLog.LogAsync(actor.Id, actorName, "CascadeDelete",
    $"Cascade delete root [{rootType}:{rootId} '{rootTitle}'] — {childCount} turunan: [{string.Join(",", childIds)}]",
    rootId, rootType == "session" ? "AssessmentSession" : "TrainingRecord");
```
</delta-367>

<interfaces>
Service field tambahan (inject di ctor, plan 01 mungkin sudah placeholder):
- `ProtonCompletionService _protonCompletion`
- `AuditLogService _auditLog`
- `IWebHostEnvironment _env`
- (sudah ada) `ApplicationDbContext _context`, `ILogger<RecordCascadeDeleteService> _logger`

Signature execute (RESEARCH §"Signature"):
```csharp
public record CascadeResult(bool Success, int DeletedCount, List<int> DeletedSessionIds, List<int> DeletedTrainingIds, string? ErrorMessage);
public async Task<CascadeResult> ExecuteAsync(string rootType, int rootId, IEnumerable<int> mirrorTrainingIdsToInclude);
```
GOTCHA logger: di service ini pakai field `_logger` (kelas-level, normal). GOTCHA logger LOKAL hanya berlaku di 3 endpoint AssessmentAdminController (plan 05), bukan di service.

mirror-ID validasi (V5/IDOR): SEBELUM hapus mirror training yang dikirim caller, VALIDASI tiap id ada di hasil `FindMirrorCandidates` untuk session yang relevan (milik user yang sama) — JANGAN percaya checkbox client.
</interfaces>

<test-fixture>
<!-- Pola disposable real-SQL. Sumber: ImageCleanupIntegrationTests.cs:31-95 (VERIFIED). -->
- Fixture `IAsyncLifetime`: DbName=$"HcPortalDB_Test_{Guid.NewGuid():N}" @localhost\SQLEXPRESS; InitializeAsync -> Database.MigrateAsync(); DisposeAsync -> EnsureDeletedAsync().
- `[Trait("Category","Integration")]` (skip via --filter "Category!=Integration").
- Seed urutan (FK): ApplicationUser minimal DULU (lesson 366), lalu session induk -> TrainingRecord anak (RenewsSessionId=induk.Id) -> AssessmentSession cucu (RenewsTrainingId=anak.Id) + artefak per-tabel (EditLog/Response/AttemptHistory/UPA/Package+Q+O/PendingBypass(Status!="Dibatalkan")/UserNotification(ActionUrl="/CMP/StartExam/{id}")/ProtonFinalAssessment Origin="Exam").
- Helper file temp webroot: MakeTempWebRoot + WriteFakeImage (ImageCleanupIntegrationTests:83-95).
</test-fixture>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExecuteAsync — cascade per-node parity gold-standard + 4 delta 367 + file + audit (1-tx)</name>
  <read_first>
    - Services/RecordCascadeDeleteService.cs (state pasca 01 — CollectCascadeIds + BuildPreviewAsync + FindMirrorCandidates sudah ada)
    - Controllers/AssessmentAdminController.cs:2271-2364 (gold-standard DeleteAssessment artefak + tx + audit — parity verbatim urutan)
    - Services/ProtonCompletionService.cs:113-129 (RemoveExamOriginAsync signature + SaveChanges internal A4)
    - Models/AssessmentSession.cs (verifikasi nama field sertifikat manual: ManualSertifikatUrl vs SertifikatUrl; LinkedSessionId; ProtonTrackId)
    - Models/TrainingRecord.cs (SertifikatUrl field)
    - Models/ProtonModels.cs:235-249 (PendingProtonBypass: LinkedAssessmentSessionId, Status, ResolvedAt)
    - Services/AuditLogService.cs (signature LogAsync — argumen urutan)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Execute per-node" + §"Common Pitfalls" 1-5
  </read_first>
  <behavior>
    - (diuji di Task 2 integration) Execute chain 3-level menghapus root+anak+cucu + semua artefak; PendingBypass jadi "Dibatalkan"; LinkedSessionId pasangan NULL; penanda Origin="Exam" tercabut; notif StartExam terhapus; AuditLog 1 entri berisi daftar Id turunan.
  </behavior>
  <action>
Tambah ke `RecordCascadeDeleteService.cs`:

1. `public record CascadeResult(bool Success, int DeletedCount, List<int> DeletedSessionIds, List<int> DeletedTrainingIds, string? ErrorMessage);`

2. `public async Task<CascadeResult> ExecuteAsync(string rootType, int rootId, IEnumerable<int> mirrorTrainingIdsToInclude)`:
   a. `var nodes = await CollectCascadeIds(rootType, rootId);` (traversal SAMA dengan preview — invariant).
   b. Resolve metadata root (title) untuk audit + ambil actor info dari caller? Service tidak punya User — terima actor via param ATAU caller (controller) lakukan audit. **Keputusan:** AuditLog di engine, actor diteruskan controller. Ubah signature jika perlu: tambah param `string actorId, string actorName` → `ExecuteAsync(string rootType, int rootId, IEnumerable<int> mirrorTrainingIds, string actorId, string actorName)`. Dokumentasikan di SUMMARY.
   c. **Validasi mirror-IDs (V5/IDOR):** untuk tiap id di `mirrorTrainingIdsToInclude`, pastikan ada di FindMirrorCandidates salah satu session node (milik user sama). Buang yang tidak valid (jangan hapus sembarang TrainingRecord).
   d. `using var tx = await _context.Database.BeginTransactionAsync();`
   e. Kumpulkan `var certPaths = new List<string>();`
   f. Untuk tiap node session (urutan apa pun — FK lintas node aman karena semua node ikut terhapus): jalankan artefak gold-standard (EditLogs->Responses->AttemptHistory->UPA->Packages+Q+O) + 4 delta (null-clear LinkedSessionId pasangan, soft-cancel PendingBypass, RemoveExamOriginAsync jika ProtonTrackId, notif eksak-match), collect cert path, lalu `_context.AssessmentSessions.Remove(session)`.
   g. Untuk tiap node training: collect SertifikatUrl, notif=0 (komentar eksplisit), `_context.TrainingRecords.Remove(record)`.
   h. Untuk tiap mirror training valid: collect SertifikatUrl, `_context.TrainingRecords.Remove(mirror)`.
   i. `await _context.SaveChangesAsync(); await tx.CommitAsync();`
   j. POST-commit: File.Delete tiap certPath (confined webroot, inner try/catch warn-only). **CATATAN:** gambar SOAL TIDAK di sini (Opsi B — ranah endpoint 366/plan 05).
   k. POST-commit: AuditLog 1 entri (pola gold-standard, try/catch warn-only) berisi root + childCount + daftar Id.
   l. return `CascadeResult(true, total, sessionIds, trainingIds, null)`.
   - Catch `DbUpdateException`/`Exception`: `using var tx` auto-rollback; log detail; return `CascadeResult(false, 0, ..., "pesan generik")` (V7 — JANGAN ex.Message ke caller).

Urutan RemoveRange WAJIB parity gold-standard (EditLogs/Responses/AttemptHistory/UPA SEBELUM Packages, Packages SEBELUM session) — lihat <gold-standard>.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~RecordCascadeServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public async Task<CascadeResult> ExecuteAsync"` match
    - `grep "BeginTransactionAsync"` DAN `grep "tx.CommitAsync"` match (1-tx)
    - `grep "Status = \"Dibatalkan\""` DAN `grep "ResolvedAt"` match (soft-cancel L-04, BUKAN RemoveRange PendingProtonBypasses)
    - `grep "RemoveExamOriginAsync"` match; `grep "ProtonTrackId.HasValue"` match (#9 selektif)
    - `grep "LinkedSessionId = null"` match (#8 null-clear)
    - `grep "/CMP/StartExam/"` match (notif eksak-match #6)
    - `grep "CollectCascadeIds"` match di ExecuteAsync (preview==execute invariant)
    - File.Delete dipanggil POST `CommitAsync` (baca urutan); ImagePath gambar SOAL TIDAK di-collect di engine (separasi Opsi B)
    - `dotnet build` 0 error; service unit test tetap hijau
  </acceptance_criteria>
  <done>ExecuteAsync menghapus semua node + artefak parity gold-standard + 4 delta + file cert post-commit + audit, dalam 1 transaction, tanpa info leak.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test real-SQL per-tabel + file post-commit + rollback + preview==execute</name>
  <read_first>
    - HcPortal.Tests/ImageCleanupIntegrationTests.cs:31-95 (fixture disposable + MakeTempWebRoot/WriteFakeImage pattern)
    - HcPortal.Tests/ProtonCompletionFixture.cs (jika ada — pola seed Proton)
    - Services/RecordCascadeDeleteService.cs (ExecuteAsync final pasca Task 1)
    - Models/ProtonModels.cs (ProtonFinalAssessment Origin field + PendingProtonBypass)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Cara seed renewal-chain" + §"Validation Architecture"
  </read_first>
  <behavior>
    - Test: seed chain 3-level + artefak → ExecuteAsync → assert per tabel: AssessmentSessions/TrainingRecords node terkait CountAsync==0; EditLogs/Responses/AttemptHistory/UPA/Packages+Q+O==0 (#5/#11).
    - Test (#10/L-04): PendingProtonBypass TIDAK hilang (CountAsync>0) TAPI Status=="Dibatalkan" + ResolvedAt!=null.
    - Test (#8): session pasangan (LinkedSessionId menunjuk node terhapus) → LinkedSessionId==null pasca execute.
    - Test (#9): ProtonFinalAssessment Origin="Exam" untuk coachee+track → hilang; Origin="Interview"/"Bypass" → BERTAHAN (kebal).
    - Test (#6): UserNotification ActionUrl="/CMP/StartExam/{id}" → hilang; ActionUrl non-record-bound (mis "/CDP/ProtonProgress") → BERTAHAN.
    - Test (L-08): AuditLog 1 entri baru, Description contains daftar Id turunan.
    - Test (#19): file sertifikat fisik (WriteFakeImage di temp webroot) → File.Exists==false pasca execute (post-commit).
    - Test (rollback): inject exception (mis. seed FK invalid yang memicu DbUpdateException) → assert TIDAK ada perubahan (semua row utuh, transaction rollback).
    - Test (preview==execute): BuildPreviewAsync(root) node-set (Type,Id) == ExecuteAsync DeletedSessionIds∪DeletedTrainingIds (kecuali mirror opt-in) → set identik.
  </behavior>
  <action>
Buat `HcPortal.Tests/RecordCascadeIntegrationTests.cs` + `HcPortal.Tests/RecordCascadeFileTests.cs`, pakai fixture disposable real-SQL (pola ImageCleanupIntegrationTests). `[Trait("Category","Integration")]`.

Seed helper `SeedRenewalChainAsync(ctx, ...)`: ApplicationUser minimal DULU (FK FK_AssessmentSessions_Users_UserId), lalu session induk -> TR anak (RenewsSessionId=induk.Id) -> AS cucu (RenewsTrainingId=anak.Id) + artefak per-tabel sesuai <behavior>.

Instansiasi service dengan dependency nyata (DbContext fixture + real ProtonCompletionService + AuditLogService + IWebHostEnvironment fake pointing ke temp webroot). Tulis [Fact] sesuai 9 <behavior> kasus. File test (#19) di `RecordCascadeFileTests.cs` pakai WriteFakeImage + assert File.Exists==false.

Quick run (skip integration) tetap hijau; full run jalankan real-SQL.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~RecordCascadeIntegrationTests|FullyQualifiedName~RecordCascadeFileTests"</automated>
  </verify>
  <acceptance_criteria>
    - `RecordCascadeIntegrationTests.cs` ada + `[Trait("Category", "Integration")]` match
    - Test assert `Status == "Dibatalkan"` (soft-cancel, BUKAN Count==0 untuk PendingBypass)
    - Test assert `LinkedSessionId` == null untuk pasangan
    - Test assert Origin="Interview"/"Bypass" BERTAHAN (kebal #9)
    - Test rollback-on-exception ada (assert row utuh)
    - Test preview-set == execute-set ada
    - `RecordCascadeFileTests.cs` assert `File.Exists(certPath) == false` post-commit
    - `dotnet test --filter "FullyQualifiedName~RecordCascadeIntegrationTests|FullyQualifiedName~RecordCascadeFileTests"` exit 0 (real-SQL @localhost\SQLEXPRESS)
    - `dotnet test` full suite exit 0
  </acceptance_criteria>
  <done>Engine ter-validasi per-tabel real-SQL: hapus tuntas, soft-cancel/null-clear/kebal benar, file terhapus post-commit, rollback utuh, preview==execute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller (admin/HC) → ExecuteAsync | actor + rootId + mirror-IDs diteruskan; mirror-IDs bisa di-tamper client |
| ExecuteAsync → DB (mutasi, 1-tx) | Penghapusan rekursif high-impact; atomicity wajib |
| ExecuteAsync → filesystem (post-commit) | File.Delete sertifikat — path traversal risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-05 | Elevation (IDOR) | mirror-IDs ter-inject di param | mitigate | Validasi tiap mirror-ID ada di FindMirrorCandidates (UserId == session.UserId) server-side sebelum hapus; buang yang tidak valid |
| T-367-06 | Tampering/DoS | Over-deletion (cascade nyasar) | mitigate | Traversal = CollectCascadeIds yang SAMA dengan preview (preview==execute); test invariant set identik; 1-tx rollback utuh |
| T-367-07 | Info disclosure | ex.Message bocor ke caller | mitigate | catch → pesan generik di CascadeResult.ErrorMessage; detail ke _logger (V7, pola Phase 334 D6) |
| T-367-08 | Tampering | Path traversal File.Delete | mitigate | `Path.Combine(_env.WebRootPath, url.TrimStart('/'))` confined webroot (V12) |
| T-367-09 | Repudiation | Hapus tanpa jejak | mitigate | AuditLog 1 entri/operasi berisi aktor + daftar Id turunan (L-08); soft-cancel PendingBypass pertahankan jejak |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` full suite hijau (unit + integration real-SQL).
- Per-tabel assert membuktikan #5/#6/#8/#9/#10/#11/#19/L-04/L-08.
- Rollback-on-exception + preview==execute invariant teruji.
</verification>

<success_criteria>
- ExecuteAsync: cascade 1-tx, semua artefak terhapus, soft-cancel + null-clear + cabut Proton + notif eksak benar, file cert post-commit, audit lengkap, no info leak.
- 9+ [Fact] integration/file hijau.
- Migration = FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-02-SUMMARY.md`
</output>
