---
phase: 367-delete-records-cascade-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/WorkerDataService.cs
  - Models/WorkerTrainingStatus.cs
  - HcPortal.Tests/BadgeRecomputeTests.cs
autonomous: true
requirements: ["#16", "#17", "D-01"]

must_haves:
  truths:
    - "Badge count per jenis = jumlah baris yang BENAR-BENAR tampil per jenis di tab Input Records (online + manual + training)"
    - "Angka badge dan isi list tidak saling kontradiksi menurut label yang ditampilkan (acceptance §3.3)"
    - "Pasca cascade delete + re-fetch, badge recompute mencerminkan list yang menyusut"
  artifacts:
    - path: "Services/WorkerDataService.cs"
      provides: "Formula badge recompute = baris tampil per jenis (bukan count ALL IsPassed termasuk yang tak tampil)"
    - path: "HcPortal.Tests/BadgeRecomputeTests.cs"
      provides: "Unit: badge formula == jumlah baris tampil per jenis (online/manual/training)"
  key_links:
    - from: "WorkerDataService.completedAssessments formula"
      to: "baris assessment yang tampil di _TrainingRecordsTab"
      via: "recompute = baris tampil (D-01)"
      pattern: "WorkerTrainingStatus"
---

<objective>
Perbaiki kontradiksi badge↔list (#16/#17) di tab Input Records: count per jenis di baris ringkasan worker (`CompletionDisplayText`, badge `bg-info text-dark`) HARUS sama dengan jumlah baris yang benar-benar tampil per jenis di panel expand. D-01: RECOMPUTE = baris tampil (bukan relabel).

Purpose: Saat ini badge "X assessments" menghitung SEMUA `IsPassed` (termasuk online yang sebelum 371 tak tampil), dan "Y trainings" hanya Passed/Valid/Permanent — sehingga angka badge tak cocok dengan baris yang admin lihat → bikin bingung, dan setelah cascade delete badge bisa stale. Fix ini = data/formula di `WorkerDataService`, BUKAN view (IC-4).
Output: Formula badge ter-recompute di `WorkerDataService.cs` (+ field display di `WorkerTrainingStatus.cs` bila perlu) + unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-UI-SPEC.md

<interfaces>
<!-- Formula existing. Sumber: WorkerDataService.cs (VERIFIED 2026-06-12). -->

#16 — completedAssessments (saat ini count ALL IsPassed, termasuk online):
```csharp
// WorkerDataService.cs:303-313 (passedAssessmentLookup)
passedAssessmentLookup = await _context.AssessmentSessions.AsNoTracking()
    .Where(a => userIds.Contains(a.UserId) && a.IsPassed == true)
    .GroupBy(a => a.UserId).Select(g => new { UserId = g.Key, Count = g.Count() })
    .ToDictionaryAsync(x => x.UserId, x => x.Count);
// :329  int completedAssessments = passedAssessmentLookup.TryGetValue(user.Id, out var aCount) ? aCount : 0;
```

#17 — completedTrainings (saat ini hanya Passed/Valid/Permanent):
```csharp
// WorkerDataService.cs:332-334
var completedTrainings = trainingRecords.Count(tr =>
    tr.Status == "Passed" || tr.Status == "Valid" || tr.Status == "Permanent");
```

Baris yang TAMPIL di partial (seam 371, _TrainingRecordsTab.cshtml:296-302):
- trainingRows = SEMUA training records worker (Type="Training")
- assessmentRows = AssessmentSessions.Where(a => a.IsManualEntry) (Type="Assessment")
- onlineRows = AssessmentSessions.Where(a => !a.IsManualEntry) (Type="AssessmentOnline")
→ semua baris tampil per jenis = total per jenis (bukan hanya yang Passed/Valid).

CompletionDisplayText: Models/WorkerTrainingStatus.cs:55-57 (string formula badge ringkasan).
</interfaces>

<acceptance-neutral>
<!-- Spec §3.3 acceptance OPSI-NETRAL: angka badge dan isi list TIDAK boleh saling kontradiksi MENURUT LABEL yang ditampilkan. D-01 memilih RECOMPUTE (bukan relabel). -->
Pilihan implementasi (D-01 RECOMPUTE): badge per jenis = COUNT baris yang ditampilkan per jenis di partial.
- Jika label badge = "X assessments" → X = jumlah baris assessment (manual + online) yang tampil. ATAU
- Jika ada sub-badge per jenis → masing-masing = jumlah baris jenis itu.
Putuskan saat baca CompletionDisplayText: pertahankan label existing, samakan ANGKA dengan baris tampil. Dokumentasikan formula final di SUMMARY.
</acceptance-neutral>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recompute formula badge = baris tampil per jenis (#16/#17, D-01)</name>
  <read_first>
    - Services/WorkerDataService.cs:290-360 (state aktual: sessionsByUser, passedAssessmentLookup, completedTrainings, konstruksi WorkerTrainingStatus)
    - Models/WorkerTrainingStatus.cs (field count + CompletionDisplayText :55-57 — label badge ringkasan)
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml:246,296-356 (CompletionDisplayText badge + proyeksi baris tampil per jenis — sumber kebenaran "baris tampil")
    - .planning/phases/367-...-cascad/367-RESEARCH.md tabel #16/#17 + §"badge formula"
    - .planning/phases/367-...-cascad/367-UI-SPEC.md IC-4 (badge↔list consistency, fix data/formula bukan view)
  </read_first>
  <behavior>
    - Test: worker dengan 3 assessment tampil (2 manual + 1 online) + 2 training tampil → badge assessment-count == 3, training-count == 2 (= baris tampil, BUKAN hanya yang IsPassed/Valid).
    - Test: worker dengan 1 online IsPassed=false (tampil sebagai baris "Tidak Lulus") → tetap dihitung di badge assessment-count (karena barisnya TAMPIL). [konfirmasi semantik final saat baca CompletionDisplayText; jika label = "completed/passed", samakan label↔angka konsisten]
    - Test: worker tanpa record → badge 0/0, tidak kontradiksi.
    - Test: setelah subset assessment dihapus (simulasi list lebih pendek) → recompute badge turun sesuai.
  </behavior>
  <action>
Baca `CompletionDisplayText` (`WorkerTrainingStatus.cs:55-57`) untuk tahu label persis yang dirender badge. Lalu pilih implementasi RECOMPUTE (D-01) yang membuat ANGKA badge == jumlah baris tampil per jenis:

1. Hitung jumlah baris tampil per jenis dari data yang sudah dimuat per worker:
   - `assessmentRowsCount` = jumlah `assessmentSessions` worker (manual + online) yang akan tampil di partial (= total AssessmentSession worker dalam scope filter, sesuai proyeksi 371 yang menampilkan manual + online).
   - `trainingRowsCount` = `trainingRecords.Count` (semua training tampil).
2. Set field di `WorkerTrainingStatus` yang menjadi sumber `CompletionDisplayText` agar mencerminkan baris-tampil. Jika `CompletionDisplayText` saat ini pakai `completedAssessments` (count IsPassed) → ganti sumbernya ke count baris tampil ATAU pertahankan label "X assessments" dengan X = baris tampil. JANGAN ubah style badge (tetap `bg-info text-dark`).
3. Jika label existing menyiratkan "completed/passed" sementara baris menampilkan SEMUA, samakan: pilih label yang konsisten dengan angka (acceptance: tidak kontradiksi). Dokumentasikan keputusan label↔angka di SUMMARY.
4. JANGAN ubah `_TrainingRecordsTab.cshtml` di plan ini (itu data fix, bukan view — view dipoles di plan 06). Hanya ubah `WorkerDataService.cs` + (bila perlu) `WorkerTrainingStatus.cs`.

Buat `HcPortal.Tests/BadgeRecomputeTests.cs` — 4 [Fact] sesuai <behavior> (pakai unit: konstruksi WorkerTrainingStatus / panggil WorkerDataService dengan data terkontrol; InMemory atau real-SQL kecil sesuai kebutuhan). Quick run: `dotnet test --filter "FullyQualifiedName~BadgeRecomputeTests"`.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~BadgeRecomputeTests"</automated>
  </verify>
  <acceptance_criteria>
    - `WorkerDataService.cs` formula badge sekarang berbasis jumlah baris tampil (grep konteks: count assessmentSessions worker + trainingRecords.Count digunakan untuk badge, bukan HANYA `IsPassed`/`Passed||Valid||Permanent` untuk angka badge)
    - `BadgeRecomputeTests.cs` punya ≥4 [Fact]; filter test exit 0
    - SUMMARY mendokumentasikan keputusan label↔angka (recompute, konsisten, tidak kontradiksi)
    - `_TrainingRecordsTab.cshtml` TIDAK diubah di plan ini (git diff view kosong)
    - `dotnet build` 0 error; full quick suite hijau
  </acceptance_criteria>
  <done>Badge per jenis = baris tampil; angka↔list konsisten; teruji 4 kasus; view tak disentuh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkerDataService (read-only formula) → Razor badge | Tidak ada input untrusted baru; hanya perubahan formula tampilan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-10 | Info disclosure | Badge bocorkan count milik worker lain | accept | Tab Input Records admin/HC-only (existing authz); badge per-worker dalam scope filter admin yang sah |
| T-367-11 | Tampering | Formula salah → count menyesatkan | mitigate | Unit test BadgeRecomputeTests memverifikasi angka == baris tampil; no schema change (read-only formula) |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "FullyQualifiedName~BadgeRecomputeTests"` hijau; full suite hijau.
- Manual (di plan 06 UAT): expand worker → angka badge cocok jumlah baris per jenis.
</verification>

<success_criteria>
- Badge↔list konsisten (D-01 recompute); 4 [Fact] hijau; view tak disentuh; Migration = FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-03-SUMMARY.md`
</output>
