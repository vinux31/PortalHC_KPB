---
phase: 367-delete-records-cascade-overhaul
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/SiblingFilterTests.cs
  - HcPortal.Tests/ResetGuardTests.cs
autonomous: true
requirements: ["#18", "#20"]

must_haves:
  truths:
    - "DeleteAssessmentGroup sibling query TIDAK menyapu Pre/Post group, assessment manual, atau session di luar scope grup tab 1 (#18)"
    - "ResetAssessment menolak session IsManualEntry dengan pesan (#20) — manual tak boleh di-reset"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Sibling filter LinkedGroupId==null && bukan PreTest/PostTest && !IsManualEntry (#18) + guard IsManualEntry di ResetAssessment (#20)"
    - path: "HcPortal.Tests/SiblingFilterTests.cs"
      provides: "Unit: sibling filter no over-match (manual/Pre-Post/linked excluded)"
    - path: "HcPortal.Tests/ResetGuardTests.cs"
      provides: "Unit: ResetAssessment tolak IsManualEntry"
  key_links:
    - from: "DeleteAssessmentGroup siblings query"
      to: "scope baris grup tab 1 (mgStandardSessions LinkedGroupId==null)"
      via: "filter samakan scope tampilan"
      pattern: "LinkedGroupId == null"
---

<objective>
Dua fix standalone di `AssessmentAdminController.cs` yang TIDAK menyentuh cascade engine maupun image-cleanup 366:
- #18: `DeleteAssessmentGroup` over-match — sibling query saat ini `Title==rep.Title && Category==rep.Category && Schedule.Date==scheduleDate` TANPA filter, sehingga bisa menyapu Pre/Post group, assessment manual, atau session di luar scope grup yang ditampilkan tab 1. Tambah filter `LinkedGroupId == null && AssessmentType bukan PreTest/PostTest && !IsManualEntry` (samakan dengan scope `mgStandardSessions` :155).
- #20: `ResetAssessment` — tambah guard tolak session `IsManualEntry` (record manual tak punya jawaban untuk di-reset; reset hanya untuk sesi online).

Purpose: Mencegah penghapusan/reset data di luar maksud admin (over-deletion = ancaman HIGH §security). Keduanya guard kecil dan independen (Wave 1, depends_on kosong). File `AssessmentAdminController.cs` overlap dengan plan 05 (file-cert tab 1) → plan 05 (Wave 3) depends pada plan 04 ini agar tidak konflik file.
Output: 2 guard di controller + 2 file unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-PATTERNS.md

<interfaces>
<!-- Line aktual VERIFIED 2026-06-12 KECUALI ResetAssessment (#20) yang ESTIMATED — lihat catatan di bawah. -->

#18 sibling query existing (AssessmentAdminController.cs:2408-2414, VERIFIED, TANPA filter = bug over-match):
```csharp
var siblings = await _context.AssessmentSessions
    .Where(a =>
        a.Title == rep.Title &&
        a.Category == rep.Category &&
        a.Schedule.Date == scheduleDate)
    .ToListAsync();
```
Scope baris grup yang DITAMPILKAN tab 1 = `mgStandardSessions` (:155): `allSessions.Where(a => a.LinkedGroupId == null)`. Pre/Post group ditangani terpisah (`mgPrePostSessions` LinkedGroupId != null :154). Manual entry tidak masuk grup standard online ini.
→ Filter target #18: `a.LinkedGroupId == null && a.AssessmentType != "PreTest" && a.AssessmentType != "PostTest" && !a.IsManualEntry`.

#20 ResetAssessment (AssessmentAdminController.cs:~4051-4056, ESTIMATED — verifikasi via read_first sebelum edit):
> ⚠️ RESEARCH A2: line ResetAssessment (#20) BELUM diverifikasi langsung (file 7137 baris; method namanya stabil tapi line bisa drift karena fase paralel menyentuh controller). WAJIB `grep`/baca `ResetAssessment` saat eksekusi untuk konfirmasi line + struktur null-check/D-17 SEBELUM menyisipkan guard. Snippet di bawah = ESTIMASI struktur, bukan line terverifikasi.
```csharp
[HttpPost] [Authorize(Roles = "Admin, HC")] [ValidateAntiForgeryToken]
public async Task<IActionResult> ResetAssessment(int id)
{
    var assessment = await _context.AssessmentSessions.FirstOrDefaultAsync(a => a.Id == id);
    if (assessment == null) return NotFound();
    // <-- SISIP guard IsManualEntry DI SINI (setelah null-check, sebelum logic D-17 PreTest)
    ...
```
Pola error existing (ResetAssessment, sekitar :4078): `TempData["Error"] = "..."; return RedirectToAction("AssessmentMonitoringDetail", new { title, category, scheduleDate });` — atau RedirectToAction("ManageAssessment") bila konteks tab. Pakai pesan ramah, JANGAN leak detail. (Line ~:4078 juga ESTIMATED — konfirmasi pola redirect aktual saat baca method.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeleteAssessmentGroup sibling filter no over-match (#18)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:2389-2440 (DeleteAssessmentGroup: rep load, sibling query :2408-2414, pre-check renewal, siblingIds)
    - Controllers/AssessmentAdminController.cs:140-180 (tab 1 list query: mgStandardSessions LinkedGroupId==null :155, mgPrePostSessions :154 — sumber kebenaran "scope grup tampil")
    - .planning/phases/367-...-cascad/367-RESEARCH.md #18 + Open Question 3 (definisi exact filter)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs, HcPortal.Tests/SiblingFilterTests.cs</files>
  <behavior>
    - Test: 3 session sama Title+Category+Schedule.Date — 2 online standard (LinkedGroupId==null, !IsManualEntry, AssessmentType bukan Pre/Post) + 1 manual (IsManualEntry=true) → sibling set HANYA 2 online (manual EXCLUDED).
    - Test: ada session Pre/Post (AssessmentType="PreTest", LinkedGroupId!=null) judul sama → EXCLUDED dari sibling standard.
    - Test: hanya online standard match → semua ikut (no false-negative).
  </behavior>
  <action>
Di `DeleteAssessmentGroup`, ubah sibling query :2408-2414 menambah filter scope (samakan dengan `mgStandardSessions`):
```csharp
var siblings = await _context.AssessmentSessions
    .Where(a =>
        a.Title == rep.Title &&
        a.Category == rep.Category &&
        a.Schedule.Date == scheduleDate &&
        a.LinkedGroupId == null &&            // #18: bukan Pre/Post group
        a.AssessmentType != "PreTest" &&      // #18: bukan PreTest
        a.AssessmentType != "PostTest" &&     // #18: bukan PostTest
        !a.IsManualEntry)                     // #18: bukan assessment manual
    .ToListAsync();
```
Preserve pre-check renewal existing (:2420+) DAN image-cleanup 366 (`:2513-2520` collect + `:2542` helper, logger LOKAL) — JANGAN sentuh image-cleanup di plan ini (itu dipreserve, ranah plan 05). Plan ini hanya ubah predikat sibling query.

Buat `HcPortal.Tests/SiblingFilterTests.cs` — 3 [Fact] sesuai <behavior>. Karena ini predikat LINQ, test bisa: (a) ekstrak predikat ke metode/Expression yang diuji unit, ATAU (b) integration real-SQL kecil yang seed 3 session + panggil DeleteAssessmentGroup + assert hanya 2 terhapus. Pilih (a) bila feasible (predikat pure) untuk test cepat; jika butuh controller HTTP, pakai (b) `[Trait("Category","Integration")]`.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~SiblingFilterTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "a.LinkedGroupId == null"` di DeleteAssessmentGroup sibling query match
    - `grep "!a.IsManualEntry"` (atau `a.IsManualEntry == false`) di sibling query match
    - `grep "a.AssessmentType != \"PreTest\""` DAN `"PostTest"` match
    - Image-cleanup 366 di DeleteAssessmentGroup TETAP ada (`grep "DeleteAssessmentGroup image"` masih match — tak terhapus)
    - `SiblingFilterTests.cs` ≥3 [Fact]; filter test exit 0
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>Sibling query hanya menyapu session online standard dalam scope grup; manual/Pre-Post/linked aman; image 366 preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResetAssessment guard tolak IsManualEntry (#20)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs: cari `ResetAssessment` (grep, line ESTIMATED ~:4051 — file 7137 baris, WAJIB verifikasi line aktual sebelum edit per RESEARCH A2). Baca method penuh: null-check, logic D-17 PreTest, status guard, archive, dan pola TempData Error + RedirectToAction
    - .planning/phases/367-...-cascad/367-RESEARCH.md #20 + A2 (line ESTIMATED, verifikasi saat plan/eksekusi)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs, HcPortal.Tests/ResetGuardTests.cs</files>
  <behavior>
    - Test: ResetAssessment(id) dengan session.IsManualEntry==true → tidak mereset, set TempData Error pesan, redirect (tidak lanjut ke archive/reset logic).
    - Test: session.IsManualEntry==false (online) status valid → lolos guard (lanjut reset seperti existing).
  </behavior>
  <action>
Pertama: `grep`/baca `ResetAssessment` untuk konfirmasi line aktual + struktur (null-check + logic D-17 PreTest) — line ~:4051 ESTIMATED, JANGAN andalkan angka di plan (RESEARCH A2). Lalu di `ResetAssessment`, SISIP guard SETELAH null-check dan SEBELUM logic D-17 PreTest:
```csharp
if (assessment == null) return NotFound();

// #20 Phase 367: record manual tidak punya jawaban untuk di-reset; reset hanya untuk sesi online.
if (assessment.IsManualEntry)
{
    TempData["Error"] = "Assessment manual tidak dapat di-reset. Gunakan Edit untuk mengubah, atau Hapus untuk menghapus record.";
    return RedirectToAction("AssessmentMonitoringDetail", new {
        title = assessment.Title,
        category = assessment.Category,
        scheduleDate = assessment.Schedule.Date.ToString("yyyy-MM-dd")
    });
}
```
Sesuaikan target RedirectToAction dengan pola aktual yang dibaca (jika method redirect ke action lain, ikuti pola existing). Pesan ramah (V7 — no leak). Preserve seluruh logic existing setelahnya verbatim.

Buat `HcPortal.Tests/ResetGuardTests.cs` — 2 [Fact] sesuai <behavior>. Predikat sederhana → unit (mock/in-memory session) atau integration kecil. Pilih pendekatan tercepat yang mengikat perilaku guard.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~ResetGuardTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "if (assessment.IsManualEntry)"` di ResetAssessment match
    - Guard berada SEBELUM logic D-17 PreTest (baca urutan: null-check → guard IsManualEntry → D-17)
    - Pesan TempData ramah, TIDAK mengandung ex.Message/detail internal
    - `ResetGuardTests.cs` ≥2 [Fact]; filter test exit 0
    - `dotnet build` 0 error; full suite hijau
  </acceptance_criteria>
  <done>ResetAssessment menolak IsManualEntry dengan pesan ramah; sesi online tetap bisa di-reset; teruji 2 kasus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → DeleteAssessmentGroup / ResetAssessment | POST terotentikasi; id arbitrer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-12 | Tampering (over-deletion) | DeleteAssessmentGroup sibling over-match | mitigate | Filter LinkedGroupId==null && bukan Pre/Post && !IsManualEntry samakan scope tampilan; test no over-match |
| T-367-13 | Tampering | ResetAssessment reset record manual (data salah) | mitigate | Guard IsManualEntry tolak; test reject |
| T-367-14 | Info disclosure | Pesan error bocorkan detail | mitigate | Pesan ramah (V7), no ex.Message |
| T-367-15 | Spoofing/CSRF | POST tanpa token | mitigate | `[ValidateAntiForgeryToken]` + `[Authorize(Roles="Admin, HC")]` existing — PRESERVE (jangan dilonggarkan) |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "FullyQualifiedName~SiblingFilterTests|FullyQualifiedName~ResetGuardTests"` hijau; full suite hijau.
- Image-cleanup 366 di DeleteAssessmentGroup tetap utuh (regresi 366 test hijau).
</verification>

<success_criteria>
- Sibling no over-match (#18) + reset guard manual (#20) terimplementasi & teruji; authz/antiforgery preserved; image 366 utuh; Migration = FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-04-SUMMARY.md`
</output>
