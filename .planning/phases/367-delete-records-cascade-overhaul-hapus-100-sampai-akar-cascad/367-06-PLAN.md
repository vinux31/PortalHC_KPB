---
phase: 367-delete-records-cascade-overhaul
plan: 06
type: execute
wave: 3
depends_on: ["367-02"]
files_modified:
  - Controllers/TrainingAdminController.cs
  - Views/Admin/Shared/_CascadePreviewModal.cshtml
  - HcPortal.Tests/RecordCascadeUiTests.cs
autonomous: true
requirements: ["#1", "#2", "#3", "#4", "L-03", "L-06", "L-07", "L-08"]

must_haves:
  truths:
    - "DeleteTabResult dibedakan: sukses → HX-Trigger recordDeleted; gagal → HX-Trigger recordDeleteFailed + payload pesan + HTTP status gagal (L-06, #1)"
    - "DeleteManualAssessment jadi endpoint per-session generik: gate IsManualEntry dihapus (1 endpoint layani manual + online), guard Admin,HC + antiforgery preserved (L-07, #3)"
    - "DeleteTraining + DeleteManualAssessment pre-check renewal BLOKIR dibalik jadi cascade engine (L-03, #2/#7)"
    - "GET DeletePreview(type, id) mengembalikan partial modal daftar korban cascade (+ kandidat mirror checkbox) tanpa mutasi"
    - "Online >7 hari bisa dihapus via endpoint generik (#4) — tidak ada batasan umur di POST"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "DeleteTabResult split L-06, DeleteManualAssessment generik+cascade, DeleteTraining cascade, GET DeletePreview"
    - path: "Views/Admin/Shared/_CascadePreviewModal.cshtml"
      provides: "Partial isi modal preview cascade (tree korban + mirror checkbox + Hapus Semua)"
    - path: "HcPortal.Tests/RecordCascadeUiTests.cs"
      provides: "Unit: DeleteTabResult sukses vs gagal HX-Trigger berbeda (#1/L-06) + integration HTTP render DeletePreview partial (Pitfall 6)"
  key_links:
    - from: "DeleteManualAssessment / DeleteTraining"
      to: "RecordCascadeDeleteService.ExecuteAsync"
      via: "cascade no-blocker (L-03)"
      pattern: "ExecuteAsync"
    - from: "GET DeletePreview"
      to: "RecordCascadeDeleteService.BuildPreviewAsync"
      via: "render partial modal"
      pattern: "BuildPreviewAsync"
    - from: "DeleteTabResult (gagal)"
      to: "HTMX partial flash merah"
      via: "HX-Trigger recordDeleteFailed"
      pattern: "recordDeleteFailed"
---

<objective>
Rewire alur hapus tab Input Records (tab 2) di `TrainingAdminController.cs` agar JUJUR, no-blocker, dan melayani online:
- **L-06 / #1 (honesty):** `DeleteTabResult()` saat ini SELALU `recordDeleted` (200) walau gagal → akar "sukses palsu". Pisah: sukses → `recordDeleted`; gagal → `recordDeleteFailed` + payload pesan + HTTP status gagal. Semua catch lewat jalur gagal (Pitfall 5).
- **L-07 / #3 (generik):** `DeleteManualAssessment` hapus gate `&& s.IsManualEntry` → 1 endpoint layani manual + online (cascade sama), preserve `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`. Online >7 hari tetap bisa dihapus (#4 — tak ada batas umur di POST).
- **L-03 / #2/#7 (no-blocker):** pre-check renewal BLOKIR di `DeleteTraining` (:595-602) + `DeleteManualAssessment` (:994-1001) DIBALIK → panggil `RecordCascadeDeleteService.ExecuteAsync` (turunan renewal IKUT terhapus).
- **GET DeletePreview(type, id):** endpoint baru mengembalikan partial modal daftar korban cascade (pohon + kandidat mirror) via `BuildPreviewAsync` — read-only.

Purpose: Menutup akar kasus Rino (#3): hapus "berhasil" tapi worker masih lihat sesi online → karena (a) online tak terjangkau hapus (gate) dan (b) UI sukses-palsu menyembunyikan blokir. Plan ini memperbaiki controller-side; partial view tombol/flash di plan 07.
Output: controller ter-rewire + partial preview modal + unit test honesty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-RESEARCH.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-PATTERNS.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-UI-SPEC.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-02-SUMMARY.md

<interfaces>
<!-- Line aktual VERIFIED 2026-06-12. -->

DeleteTabResult existing (TrainingAdminController.cs:561-569 — SELALU recordDeleted = #1):
```csharp
private IActionResult DeleteTabResult()
{
    if (IsHtmxRequest()) { Response.Headers["HX-Trigger"] = "recordDeleted"; return new EmptyResult(); }
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
```
Honest split (RESEARCH §"Honest HTMX trigger", L-06):
```csharp
// Sukses:
private IActionResult DeleteTabSuccess() {
    if (IsHtmxRequest()) { Response.Headers["HX-Trigger"] = "recordDeleted"; return new EmptyResult(); }
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
// Gagal:
private IActionResult DeleteTabFailure(string pesan) {
    if (IsHtmxRequest()) {
        Response.StatusCode = StatusCodes.Status400BadRequest;
        Response.Headers["HX-Trigger"] = System.Text.Json.JsonSerializer.Serialize(
            new { recordDeleteFailed = new { pesan } });
        return new EmptyResult();
    }
    TempData["Error"] = pesan;
    return RedirectToAction("ManageAssessment", "AssessmentAdmin", new { tab = "training" });
}
```
Pesan generik (V7) — JANGAN ex.Message.

DeleteManualAssessment gate (TrainingAdminController.cs:978):
```csharp
// SEBELUM: var session = ...FirstOrDefaultAsync(s => s.Id == id && s.IsManualEntry);
// SESUDAH (L-07): var session = ...FirstOrDefaultAsync(s => s.Id == id);
```
Pre-check renewal BLOKIR: DeleteManualAssessment :994-1001, DeleteTraining :595-602 (`referencingTr + referencingAs > 0` → TempData + DeleteTabResult). HAPUS blok, ganti cascade.

Atribut preserve (TrainingAdminController.cs:973-975): `[HttpPost] [ValidateAntiForgeryToken] [Authorize(Roles="Admin, HC")]`.

ExecuteAsync (plan 02): `Task<CascadeResult> ExecuteAsync(string rootType, int rootId, IEnumerable<int> mirrorTrainingIds, string actorId, string actorName)`. CascadeResult: Success, DeletedCount, ErrorMessage.

BuildPreviewAsync (plan 01): `Task<List<CascadeNode>> BuildPreviewAsync(string rootType, int rootId)`. CascadeNode: Type, Id, Title, Date, OwnerName, IsRoot, IsMirrorCandidate.

Preview analog (CoachMappingController.cs:1196-1227): GET + [Authorize(Roles="Admin, HC")] + entity-exists guard + return PartialView.
</interfaces>

<ui-spec-refs>
<!-- Copy + struktur modal. Sumber: 367-UI-SPEC.md S1 + Component Inventory. -->
- Modal shell analog: _AssessmentGroupsTab.cshtml:462-518 (deleteAssessmentModal). Partial _CascadePreviewModal hanya ISI body (tree + mirror + warning), shell modal di partial tab (plan 07).
- Copy (Bahasa Indonesia): lead "Data berikut akan dihapus permanen — termasuk seluruh turunan pembaruan (renewal):"; row "{judul} · {tanggal dd MMM yyyy} · {jenis} · {pemilik}"; badge "Induk"/"Turunan renewal"; mirror heading "Kemungkinan duplikat lama (mirror) — periksa sebelum hapus:"; checkbox "Ikut hapus: {judul} · {tanggal}" default CHECKED; warning alert-danger "Tindakan ini tidak dapat dibatalkan."; empty "Hanya record ini yang akan terhapus (tidak ada turunan renewal)."; tombol "Hapus Semua" (btn btn-danger bi bi-trash) + "Batal" (btn btn-secondary).
- Mirror checkbox = real <input type="checkbox"> name="mirrorTrainingIds" value="{id}" checked (IDOR: server validasi milik-user di ExecuteAsync plan 02).
- Bootstrap utility only, ZERO CSS baru.
</ui-spec-refs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeleteTabResult honesty split (L-06, #1)</name>
  <read_first>
    - Controllers/TrainingAdminController.cs:556-635 (DeleteTabResult :561-569, DeleteTraining catch :625-634)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Honest HTMX trigger" + Pitfall 5
    - .planning/phases/367-...-cascad/367-UI-SPEC.md IC-2 (success/failure path)
  </read_first>
  <behavior>
    - Test: DeleteTabSuccess (HTMX) → HX-Trigger header == "recordDeleted", status 200.
    - Test: DeleteTabFailure(pesan) (HTMX) → HX-Trigger header berisi "recordDeleteFailed" + pesan; status 400.
    - Test: pesan gagal TIDAK mengandung detail internal (generik).
  </behavior>
  <action>
Ganti `DeleteTabResult()` menjadi DUA helper: `DeleteTabSuccess()` (recordDeleted) + `DeleteTabFailure(string pesan)` (recordDeleteFailed + status 400 + payload JSON) — lihat <interfaces>. Update SEMUA call-site existing: jalur sukses → `DeleteTabSuccess()`; jalur gagal/catch → `DeleteTabFailure("pesan generik")`. Call-site existing: DeleteTraining :601 (akan jadi cascade Task 3), :634 (catch); DeleteManualAssessment :1000 (cascade Task 3), :1031. Pastikan SEMUA catch (DbUpdateException/Exception) lewat `DeleteTabFailure` dengan pesan generik (V7, no ex.Message).

Buat `HcPortal.Tests/RecordCascadeUiTests.cs` — 3 [Fact] sesuai <behavior> (uji helper via controller test / inspeksi Response.Headers). Mock HttpContext untuk simulasi HTMX header.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~RecordCascadeUiTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "recordDeleteFailed"` di TrainingAdminController.cs match
    - `grep "StatusCodes.Status400BadRequest"` (atau status gagal) di jalur gagal match
    - DeleteTabResult lama tidak lagi dipakai di jalur gagal (semua catch → DeleteTabFailure)
    - `RecordCascadeUiTests.cs` ≥3 [Fact]; filter test exit 0
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>Sukses vs gagal HX-Trigger berbeda; gagal status 400 + pesan generik; teruji.</done>
</task>

<task type="auto">
  <name>Task 2: GET DeletePreview(type, id) + partial _CascadePreviewModal (L-03 preview)</name>
  <read_first>
    - Controllers/CoachMappingController.cs:1196-1227 (analog GET preview read-only)
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml:462-518 (deleteAssessmentModal shell — referensi struktur isi)
    - Views/Admin/Shared/ManageOrganization.cshtml:202-227 (cascade list + footer batal/hapus analog)
    - Services/RecordCascadeDeleteService.cs (BuildPreviewAsync return CascadeNode list)
    - HcPortal.Tests/RecordCascadeUiTests.cs (state pasca Task 1 — tambahkan integration test partial di sini)
    - HcPortal.Tests/ (cek apakah ada WebApplicationFactory/TestServer/ViewRenderService untuk HTTP/partial render test)
    - .planning/phases/367-...-cascad/367-UI-SPEC.md S1 + Copywriting Contract (semua copy Bahasa Indonesia)
  </read_first>
  <behavior>
    - Test (integration HTTP, Pitfall 6): seed 1 session + 1 turunan renewal + 1 mirror → GET DeletePreview(type:"session", id) → render partial TANPA exception (HTTP 200 / PartialViewResult ter-render) DAN body HTML berisi "Hapus Semua" + "Tindakan ini tidak dapat dibatalkan".
    - Test: type tak valid ("foo") → BadRequest (V5 whitelist) — tidak crash.
  </behavior>
  <action>
Tambah endpoint di `TrainingAdminController.cs`:
```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> DeletePreview(string type, int id)
{
    if (type != "training" && type != "session") return BadRequest();  // V5 whitelist
    var nodes = await _cascadeService.BuildPreviewAsync(type, id);     // read-only
    if (nodes == null || nodes.Count == 0) return NotFound();
    return PartialView("Shared/_CascadePreviewModal", nodes);
}
```
Inject `RecordCascadeDeleteService _cascadeService` via ctor (pola DI existing) atau RequestServices.

Buat `Views/Admin/Shared/_CascadePreviewModal.cshtml` (model `List<CascadeNode>`): render isi body modal sesuai UI-SPEC S1 (lead + tree rows {judul · tanggal · jenis · pemilik} dengan badge Induk/Turunan renewal; section mirror dengan checkbox `name="mirrorTrainingIds" value="{id}" checked` untuk IsMirrorCandidate; warning alert-danger; empty-state; tombol "Hapus Semua" btn-danger + "Batal"). Bootstrap utility only, ZERO CSS baru. Semua copy Bahasa Indonesia (UI-SPEC Copywriting Contract). Form "Hapus Semua" POST ke endpoint hapus (DeleteTraining untuk type=training, DeleteManualAssessment generik untuk type=session) + antiforgery token + mirror checkbox values.

**WAJIB — guard runtime partial anon-shape (Pitfall 6, lesson Phase 354/371):** Razor partial yang me-render `List<CascadeNode>` bisa LOLOS `dotnet build` tapi error runtime (`RuntimeBinderException`/null-ref) saat rendering — `dotnet build` SAJA TIDAK memadai. Tambah minimal 1 integration HTTP test ke `HcPortal.Tests/RecordCascadeUiTests.cs` (`[Trait("Category","Integration")]`, pakai `WebApplicationFactory`/`TestServer` bila tersedia, atau render partial via mekanisme test partial yang ada di proyek): seed 1 session + 1 turunan + 1 mirror → panggil `DeletePreview(type:"session", id)` → assert HTTP 200 / `PartialViewResult` ter-render TANPA exception DAN HTML body berisi "Hapus Semua" + "Tindakan ini tidak dapat dibatalkan". Jika infrastruktur HTTP/partial-render test belum ada di proyek (konfirmasi via `read_first` saat eksekusi) DAN render modal di-cover Playwright Plan 08 Task 2 (browser), dokumentasikan fallback itu EKSPLISIT di SUMMARY + tetap jalankan `dotnet test HcPortal.Tests` (full suite regresi) sebagai automated minimum. Render modal di browser (Playwright Plan 08 Task 2) tetap verifikasi runtime definitif yang TIDAK boleh di-skip.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~RecordCascadeUiTests"; dotnet test HcPortal.Tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public async Task<IActionResult> DeletePreview"` match; `[Authorize(Roles = "Admin, HC")]` di atasnya
    - `grep "BuildPreviewAsync"` di DeletePreview match (read-only preview)
    - `grep "type != \"training\" && type != \"session\""` (whitelist V5) match
    - `_CascadePreviewModal.cshtml` ada + `grep "Hapus Semua"` + `grep "mirrorTrainingIds"` + `grep "Tindakan ini tidak dapat dibatalkan"` match
    - `_CascadePreviewModal.cshtml` ZERO `<style>` baru / inline margin arbitrer (Bootstrap utility only)
    - Integration HTTP test DeletePreview di `RecordCascadeUiTests.cs` me-render partial TANPA exception + assert body berisi "Hapus Semua" (ATAU terdokumentasi di SUMMARY bahwa render runtime di-cover Playwright Plan 08 Task 2, dengan `dotnet test HcPortal.Tests` full suite tetap dijalankan)
    - `dotnet test HcPortal.Tests` (full suite regresi) exit 0
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>GET DeletePreview read-only mengembalikan partial modal cascade (tree + mirror checkbox + Hapus Semua); render partial ter-verifikasi runtime (integration HTTP test ATAU Playwright Plan 08 terdokumentasi) bukan hanya build; copy Bahasa Indonesia; no CSS baru.</done>
</task>

<task type="auto">
  <name>Task 3: DeleteManualAssessment generik (L-07) + DeleteTraining cascade (L-03) via engine</name>
  <read_first>
    - Controllers/TrainingAdminController.cs:572-635 (DeleteTraining: pre-check :595-602, tx, cert :580-584/619-623)
    - Controllers/TrainingAdminController.cs:972-1032 (DeleteManualAssessment: gate :978, pre-check :994-1001, cert :982/1017-1021)
    - Services/RecordCascadeDeleteService.cs (ExecuteAsync signature final)
    - .planning/phases/367-...-cascad/367-02-SUMMARY.md (ExecuteAsync actor param + apakah engine handle cert/notif)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"State of the Art" + Pitfall 2 (RenewsSessionId)
  </read_first>
  <action>
**DeleteManualAssessment (L-07 generik + L-03 cascade):**
1. Hapus gate `&& s.IsManualEntry` di :978 → `FirstOrDefaultAsync(s => s.Id == id)` (layani manual + online, #3/#4).
2. HAPUS pre-check renewal BLOKIR :994-1001 (TempData + return).
3. Ganti body delete: panggil `RecordCascadeDeleteService.ExecuteAsync("session", id, mirrorTrainingIds, actor.Id, actor.FullName)`. Ambil `mirrorTrainingIds` dari form (`Request.Form["mirrorTrainingIds"]` → List<int>). Engine handle artefak + cascade + cert(jika engine collect ManualSertifikatUrl session) + soft-cancel + audit.
   - Jika engine TIDAK collect cert (cek 02-SUMMARY): endpoint kumpulkan `manualSertifikatUrl` SEBELUM panggil engine + File.Delete post sukses (pola existing :1017-1021). Hindari dobel.
4. Sukses (CascadeResult.Success) → `DeleteTabSuccess()`. Gagal → `DeleteTabFailure(result.ErrorMessage ?? "Gagal menghapus record.")`. Preserve `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`.

**DeleteTraining (L-03 cascade):**
1. HAPUS pre-check renewal BLOKIR :595-602.
2. Ganti body: `ExecuteAsync("training", id, mirrorTrainingIds, actor.Id, actor.FullName)`. (TrainingRecord node = 0 notif, cert via SertifikatUrl — engine handle; jika tidak, endpoint File.Delete pola existing :619-623.)
3. Sukses → `DeleteTabSuccess()`; gagal → `DeleteTabFailure(...)`.

GOTCHA (Pitfall 2): node session → anak via RenewsSessionId; node training → anak via RenewsTrainingId — sudah ditangani engine CollectCascadeIds. Jangan re-implement traversal di controller.

Extend `RecordCascadeUiTests.cs` atau buat integration kecil: DeleteManualAssessment dengan session online (IsManualEntry=false) → tidak NotFound (gate hilang); dengan turunan renewal → turunan ikut terhapus (tidak blokir).
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "FullyQualifiedName~RecordCascadeUiTests"; dotnet test HcPortal.Tests</automated>
  </verify>
  <acceptance_criteria>
    - DeleteManualAssessment :978 TIDAK lagi `&& s.IsManualEntry` (gate hilang, L-07) — `grep "s.Id == id && s.IsManualEntry"` TIDAK match di DeleteManualAssessment
    - `grep "menggunakan record ini sebagai sumber renewal"` di TrainingAdminController TIDAK match (pre-check blokir hilang di kedua method)
    - `grep "ExecuteAsync"` muncul di DeleteTraining DAN DeleteManualAssessment
    - Jalur sukses → DeleteTabSuccess, gagal → DeleteTabFailure (no recordDeleted di jalur gagal)
    - `[ValidateAntiForgeryToken]` + `[Authorize(Roles = "Admin, HC")]` tetap di kedua endpoint
    - `dotnet test` full suite hijau
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>DeleteManualAssessment generik (manual+online, no gate, no blokir) + DeleteTraining cascade; keduanya jujur sukses/gagal; authz preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → DeletePreview (GET) / DeleteTraining+DeleteManualAssessment (POST) | id + type + mirrorTrainingIds dari client; POST hapus high-impact |
| Controller → RecordCascadeDeleteService | actor + ids diteruskan ke engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-21 | Spoofing/CSRF | POST hapus tanpa token (endpoint generik L-07) | mitigate | `[ValidateAntiForgeryToken]` + `[Authorize(Roles="Admin, HC")]` preserved saat hapus gate IsManualEntry — JANGAN dilonggarkan |
| T-367-22 | Tampering | type param arbitrer di DeletePreview | mitigate | Whitelist type ∈ {"training","session"} → BadRequest (V5) |
| T-367-23 | Elevation (IDOR) | mirrorTrainingIds ter-inject di POST | mitigate | Engine ExecuteAsync validasi mirror-ID milik-user (plan 02 T-367-05); controller hanya teruskan |
| T-367-24 | Info disclosure | ex.Message bocor di flash gagal | mitigate | DeleteTabFailure pesan generik (V7); detail ke _logger |
| T-367-25 | Tampering (false-success) | gagal tampil sukses (#1) | mitigate | Honesty split: catch → DeleteTabFailure + status 400; test #1 |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` full suite hijau.
- Honesty split teruji; endpoint generik melayani online; cascade no-blocker; preview read-only + render partial ter-verifikasi runtime (Pitfall 6); authz/antiforgery preserved.
</verification>

<success_criteria>
- L-06 honesty + L-07 generik + L-03 no-blocker + GET DeletePreview terimplementasi & teruji; #1/#2/#3/#4 ditutup; Migration = FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-06-SUMMARY.md`
</output>
