---
phase: 367-delete-records-cascade-overhaul
plan: 08
type: execute
wave: 5
depends_on: ["367-03", "367-06"]
files_modified:
  - Views/Admin/Shared/_TrainingRecordsTab.cshtml
  - tests/e2e/delete-records-cascade.spec.ts
autonomous: false
requirements: ["#3", "L-06", "L-03", "SC1", "SC2", "SC3", "SC4"]

must_haves:
  truths:
    - "Baris online di tab Input Records punya tombol hapus (btn-outline-danger bi bi-trash) — online jadi deletable (#3, IC-3)"
    - "3 tombol hapus (online+training+manual) memicu GET DeletePreview → modal, BUKAN hx-post langsung; hx-confirm native dihapus (D-03, IC-1)"
    - "Flash sukses/gagal dirender DI DALAM partial: sukses recordDeleted → re-fetch + alert-success; gagal recordDeleteFailed → alert-danger merah (L-06, S3)"
    - "UAT dual-path @5277: seed renewal-chain → preview tampil anak+mirror → hapus → DB bersih + worker view bersih (sukses); skenario gagal → flash merah; online >7hari (kasus Rino) tampil + terhapus"
  artifacts:
    - path: "Views/Admin/Shared/_TrainingRecordsTab.cshtml"
      provides: "Tombol hapus online + modal shell cascade preview + flash block S3 + rewire 3 tombol ke DeletePreview + listener recordDeleteFailed"
    - path: "tests/e2e/delete-records-cascade.spec.ts"
      provides: "Playwright dual-path (sukses + gagal) + online >7hari + preview tree"
  key_links:
    - from: "_TrainingRecordsTab tombol hapus (3 jenis)"
      to: "GET DeletePreview → modal"
      via: "hx-get DeletePreview inject ke modal body"
      pattern: "DeletePreview"
    - from: "_TrainingRecordsTab listener"
      to: "flash merah S3"
      via: "hx-trigger recordDeleteFailed"
      pattern: "recordDeleteFailed"
---

<objective>
Lengkapi UI tab Input Records di atas seam 371 + validasi end-to-end:
- **IC-3 (#3):** tambah tombol hapus `btn-sm btn-outline-danger` + `bi bi-trash` ke baris online (`@if row.IsOnline` :368-379, saat ini hanya "Lihat hasil"). Online tetap NO Edit (View + Delete).
- **IC-1 (D-03):** rewire 3 tombol hapus (online baru + training :387-393 + manual :402-408) → BUKAN `hx-post` langsung. Trigger `GET DeletePreview(type, id)` → inject ke modal body → show. HAPUS `hx-confirm` native (modal pengganti).
- **S3 / L-06:** tambah blok flash DI DALAM partial (top): sukses → alert-success (di-trigger setelah re-fetch recordDeleted), gagal → alert-danger merah (listener `recordDeleteFailed` render `{pesan}`). JANGAN tampil sukses saat gagal (#1).
- **S1:** modal shell cascade preview (reuse pola `deleteAssessmentModal`) yang memuat partial `_CascadePreviewModal` (dari plan 06).
- **UAT:** Playwright dual-path @5277 + Seed Workflow.

Purpose: Ini lapisan yang admin LIHAT — menutup kasus Rino end-to-end (online terlihat + bisa dihapus + UI jujur). Anon-shape 10-property HARUS dijaga (Pitfall 6 — RuntimeBinderException runtime).
Output: partial ter-rewire + spec e2e + UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-CONTEXT.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-UI-SPEC.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-PATTERNS.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-06-SUMMARY.md
@.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-03-SUMMARY.md

<seam-371>
<!-- _TrainingRecordsTab.cshtml VERIFIED 2026-06-12. -->
- Proyeksi anon 10-property (:296-303): { Type, Date, Title, Detail, Status, StatusClass, ValidUntil, Id, IsOnline, CanViewResult } — JAGA shape identik (Pitfall 6).
- Kolom Aksi (:366-411): @if(row.IsOnline) :368-379 HANYA "Lihat hasil" (komentar :371 "placeholder Phase 367 — extension point"); else if Training :380-394 (Edit + hx-post DeleteTraining); else manual :395-409 (Edit + hx-post DeleteManualAssessment).
- Tombol hapus existing (:387-393 training, :402-408 manual): hx-post + hx-vals(id+token) + hx-confirm + hx-swap="none". `antiToken` var tersedia.
- Re-fetch listener (:178-183): hidden div hx-get ManageAssessmentTab_Training, hx-trigger="recordDeleted from:body", hx-include="#filterFormTraining", target closest .htmx-tab-wrapper. PRESERVE filter. Flash S3 di ATAS struktur ini.
- Modal shell analog: _AssessmentGroupsTab.cshtml:462-518 (deleteAssessmentModal: modal-header bg-danger text-white, modal-dialog-scrollable, aria-live spinner).
</seam-371>

<ui-spec-copy>
<!-- Sumber: 367-UI-SPEC.md. Semua Bahasa Indonesia. -->
- Tombol hapus title: online="Hapus sesi online ini (beserta turunannya)", training="Hapus training record ini", manual="Hapus assessment record ini". Icon bi bi-trash. aria-label = title.
- Modal title "Konfirmasi Hapus — Cascade" (bi bi-trash3). Loading "Memuat daftar data yang akan terhapus…" (spinner aria-live).
- Flash sukses: alert-success alert-dismissible fade show "Record beserta {n} turunannya berhasil dihapus." (bi bi-check-circle-fill); n=0 "Record berhasil dihapus."
- Flash gagal: alert-danger alert-dismissible fade show "Gagal menghapus: {pesan}." (bi bi-exclamation-triangle-fill). NEVER success styling saat gagal.
- Color: tombol btn-sm btn-outline-danger; modal header bg-danger text-white; confirm btn-danger. ZERO CSS baru, Bootstrap utility only.
- Badge status palette read-only LOCKED 371 — jangan ubah.
</ui-spec-copy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tombol hapus online + rewire 3 tombol ke DeletePreview modal + flash S3 + modal shell</name>
  <read_first>
    - Views/Admin/Shared/_TrainingRecordsTab.cshtml (PENUH — proyeksi :296-303, listener :178-183, kolom Aksi :366-411)
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml:462-518 (deleteAssessmentModal shell — reuse pola)
    - Views/Admin/Shared/_CascadePreviewModal.cshtml (partial isi dari plan 06)
    - .planning/phases/367-...-cascad/367-UI-SPEC.md S1/S2/S3 + IC-1/IC-2/IC-3/IC-5
    - .planning/phases/367-...-cascad/367-06-SUMMARY.md (route DeletePreview + endpoint hapus)
  </read_first>
  <files>Views/Admin/Shared/_TrainingRecordsTab.cshtml</files>
  <action>
1. **IC-3 tombol hapus online:** di cabang `@if (row.IsOnline)` (:368-379), TAMBAH tombol hapus `btn-sm btn-outline-danger` + `bi bi-trash` (title "Hapus sesi online ini (beserta turunannya)", aria-label sama) dalam `d-flex gap-1` yang sama, SETELAH tombol "Lihat hasil". Online tetap NO Edit.
2. **IC-1 rewire 3 tombol:** ubah ketiga tombol hapus (online baru, training :387-393, manual :402-408) supaya TIDAK `hx-post` langsung. Ganti jadi pemicu modal:
   - `hx-get="@Url.Action("DeletePreview","TrainingAdmin")"` dengan `hx-vals` berisi `type` (training→"training", manual+online→"session") + `id`.
   - `hx-target="#cascade-preview-body"` + `hx-indicator` spinner; setelah swap, show modal (vanilla `bootstrap.Modal(...).show()` via hx-on::after-request, atau pola orchestration deleteAssessmentModal).
   - HAPUS `hx-confirm` native (D-03).
3. **S1 modal shell:** tambah modal shell (reuse `deleteAssessmentModal` pola: `modal-header bg-danger text-white`, title "Konfirmasi Hapus — Cascade" bi bi-trash3, `modal-dialog-scrollable`, body `#cascade-preview-body` dengan loading aria-live "Memuat daftar data yang akan terhapus…"). Body diisi partial `_CascadePreviewModal` via DeletePreview.
4. **S3 flash block:** di TOP partial (di atas listener :178-183), tambah blok flash kosong + listener `hx-trigger="recordDeleteFailed from:body"` yang render `alert alert-danger` merah dengan `{pesan}` dari payload event (vanilla JS handler baca `event.detail.pesan`). Sukses (recordDeleted) → re-fetch existing (preserve) + flash sukses alert-success. JANGAN sukses-styling saat gagal.
5. **Pitfall 6:** JAGA proyeksi anon 10-property identik — jangan tambah/ubah property shape. Jika butuh data ekstra untuk tombol (mis. type), turunkan dari `row.Type`/`row.IsOnline` existing, JANGAN ubah shape.
6. ZERO CSS baru (kecuali chevron-rotate existing). Bootstrap utility + bi icons only.

PRESERVE: re-fetch listener filter (hx-include #filterFormTraining), badge style bg-info text-dark (recompute dari plan 03), status palette 371.
  </action>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - Cabang online (`@if (row.IsOnline)`) sekarang punya tombol `btn-outline-danger` + `bi bi-trash` (grep tombol hapus dalam blok IsOnline)
    - 3 tombol hapus pakai `hx-get` DeletePreview (grep "DeletePreview") — `hx-confirm` native DIHAPUS dari ketiga (grep "hx-confirm" di kolom Aksi = 0)
    - Modal shell cascade ada: `grep "cascade-preview-body"` + `grep "Konfirmasi Hapus"` match
    - Flash listener gagal: `grep "recordDeleteFailed"` di partial match + render alert-danger
    - Proyeksi anon tetap 10-property (Type,Date,Title,Detail,Status,StatusClass,ValidUntil,Id,IsOnline,CanViewResult tidak berubah)
    - ZERO `<style>` baru selain chevron-rotate existing
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>Online deletable; 3 tombol → modal preview; flash jujur S3; modal shell reuse; shape 10-prop terjaga; no CSS baru.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT Playwright dual-path @5277 + Seed Workflow (SC1-SC4)</name>
  <read_first>
    - tests/e2e/ (pola spec existing — proton-bypass.spec.ts / image-in-assessment.spec.ts untuk struktur + login helper)
    - docs/SEED_WORKFLOW.md (snapshot → seed → restore + journal)
    - .planning/phases/367-...-cascad/367-RESEARCH.md §"Validation Architecture" → "Cara seed renewal-chain" + Manual-Only
    - CLAUDE.md (AD lokal: Authentication__UseActiveDirectory=false dotnet run)
  </read_first>
  <files>tests/e2e/delete-records-cascade.spec.ts</files>
  <what-built>
Cascade engine + endpoint generik + UI jujur + preview modal + tombol hapus online. Otomasi yang sudah dijalankan: full `dotnet test` (unit + integration real-SQL) hijau, build 0 error.
  </what-built>
  <action>
Tulis + commit spec e2e `tests/e2e/delete-records-cascade.spec.ts` (dual-path sukses+gagal + online >7hari + preview tree), lalu jalankan UAT manual @5277 mengikuti langkah `how-to-verify` di bawah. Seed Workflow (snapshot → seed renewal-chain → restore + journal `cleaned`) WAJIB. Setelah verifikasi browser, tunggu approval user sebelum lanjut.
  </action>
  <how-to-verify>
**Persiapan (Seed Workflow WAJIB):**
1. Snapshot DB lokal: `sqlcmd ... BACKUP DATABASE HcPortalDB_Dev` (SEED_WORKFLOW). Catat di docs/SEED_JOURNAL.md (tujuan: UAT 367 cascade, klasifikasi temporary+local-only).
2. Seed renewal-chain (SQL atau via UI): worker punya session induk → TrainingRecord anak (RenewsSessionId) → AssessmentSession cucu (RenewsTrainingId) + minimal 1 kandidat mirror legacy (TrainingRecord judul "Assessment: {title}", tanggal ±1 hari). Plus 1 sesi online >7 hari (reproduksi kasus Rino).
3. Jalankan app: `Authentication__UseActiveDirectory=false dotnet run` → http://localhost:5277. Login admin (admin@pertamina.com, lihat reference_dev_credentials).

**Verifikasi (login Admin/HC, buka Manage Assessment → tab Input Records, expand worker):**
- SC4: sesi online (termasuk >7 hari) TAMPIL dengan badge "Assessment Online" + tombol hapus (bi-trash). Klik hapus → modal "Konfirmasi Hapus — Cascade" muncul.
- SC2: modal preview menampilkan daftar PERSIS korban cascade (induk + turunan renewal, badge Induk/Turunan renewal) + section mirror dengan checkbox default tercentang. Tidak ada jalur blokir.
- SC1 (sukses): klik "Hapus Semua" → flash HIJAU "Record beserta {n} turunannya berhasil dihapus." + list re-fetch (worker view bersih, sesi+turunan hilang). Verifikasi DB: `SELECT` per tabel (AssessmentSessions/TrainingRecords/EditLogs/Responses/AttemptHistory/UPA/Packages/PendingProtonBypasses(Status='Dibatalkan')/UserNotifications) bersih sesuai cascade.
- SC3 (gagal): trigger skenario gagal (mis. constraint sengaja) → flash MERAH "Gagal menghapus: {pesan}." (BUKAN hijau). List tidak berubah jadi seolah sukses.
- SC5 (guard sampingan): coba AddManualAssessment dup exact → ditolak; expand worker → angka badge cocok jumlah baris per jenis (D-01).

**Setelah UAT (sukses ATAU gagal):**
4. Restore DB: `sqlcmd ... RESTORE DATABASE` (SEED_WORKFLOW). Tandai docs/SEED_JOURNAL.md `cleaned`.

Jalankan `npx playwright test delete-records-cascade --workers=1` (DB isolation, lesson local-e2e).
  </how-to-verify>
  <verify>
    Manual-Only: Playwright dual-path @5277 + DB cross-check per tabel + Seed Workflow journal `cleaned`. `npx playwright test delete-records-cascade --workers=1` hijau (atau failure tersisa terdokumentasi non-blocking).
  </verify>
  <acceptance_criteria>
    - SC1 PASS: hapus sukses → DB 100% bersih per tabel (cross-check SQL) + flash hijau + worker view bersih
    - SC2 PASS: preview menampilkan korban persis + mirror checkbox; no blokir
    - SC3 PASS: skenario gagal → flash merah (bukan hijau)
    - SC4 PASS: online >7 hari (kasus Rino) tampil + terhapus tuntas
    - SC5 PASS: guard dup ditolak + badge↔list konsisten
    - `tests/e2e/delete-records-cascade.spec.ts` committed; `npx playwright test delete-records-cascade --workers=1` hijau (atau failure tersisa terdokumentasi non-blocking)
    - SEED_JOURNAL.md entry `cleaned` (DB di-restore)
  </acceptance_criteria>
  <done>SC1-SC5 PASS di browser + DB restored + spec e2e committed.</done>
  <resume-signal>Ketik "approved" bila SC1-SC5 PASS di browser + DB restored, atau jelaskan isu yang ditemukan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC browser → tombol hapus → GET DeletePreview / POST hapus | UI memicu cascade rekursif high-impact; CSRF risk pada POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-367-29 | Spoofing/CSRF | Tombol hapus online baru POST tanpa token | mitigate | Form "Hapus Semua" di modal sertakan `__RequestVerificationToken` (antiToken existing); endpoint POST ber-[ValidateAntiForgeryToken] (plan 06) |
| T-367-30 | Tampering (false-success) | Flash hijau saat gagal (#1) | mitigate | Listener recordDeleteFailed render alert-danger; UAT SC3 verifikasi merah; honesty split plan 06 |
| T-367-31 | DoS/UX | RuntimeBinderException shape mismatch (Pitfall 6) | mitigate | Jaga anon 10-property; verifikasi Playwright runtime (bukan cuma build) — UAT expand worker tanpa 500 |
| T-367-32 | Repudiation | Hapus dari UI tanpa konfirmasi sadar | accept | Preview eksplisit (D-03 tombol Hapus Semua) + audit log (engine) + snapshot DB UAT — mitigasi risiko spec §4 |
</threat_model>

<verification>
- `dotnet build` 0 error.
- Playwright dual-path @5277 hijau (sukses + gagal + online >7hari + preview).
- DB cross-check per tabel bersih pasca hapus (SC1).
- Seed Workflow snapshot→restore→journal cleaned.
</verification>

<success_criteria>
- SC1-SC4 (+ SC5 guard/badge) PASS via UAT; online deletable; UI jujur; modal preview; no CSS baru; shape terjaga; Migration = FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/367-delete-records-cascade-overhaul-hapus-100-sampai-akar-cascad/367-08-SUMMARY.md`
</output>
