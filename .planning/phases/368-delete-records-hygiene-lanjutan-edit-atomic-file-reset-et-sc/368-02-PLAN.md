---
phase: 368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Controllers/TrainingAdminController.cs
  - Views/Admin/CleanupAttemptHistory.cshtml
  - Views/Admin/BulkBackfill.cshtml
  - Views/Admin/Index.cshtml
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
  - HcPortal.Tests/ImportTrainingAuditTests.cs
  - HcPortal.Tests/OrphanCleanupTests.cs
autonomous: true
requirements: ["#24", "#27", "#23"]
must_haves:
  truths:
    - "ImportTraining (branch assessment): tiap sesi AssessmentType=Manual (konstanta) + GenerateCertificate=isPassed (bukan unconditional true) + 1 audit log ringkasan (X sukses/Y skip/Z error) di akhir import"
    - "BulkBackfillAssessment: AssessmentType=AssessmentConstants.AssessmentType.Manual (bukan \"Standard\"); label UI di 3 view = persis \"Bulk Import Nilai (Excel)\""
    - "CleanupAttemptHistory: GET tampilkan preview-count orphan (AttemptHistory ber-SessionId dangling) SEBELUM eksekusi; POST [Authorize(Admin)]+[ValidateAntiForgeryToken] hapus orphan + audit log; re-run kedua = 0 (idempotent)"
    - "Zero schema change (migration=false; #23 = endpoint runtime, BUKAN EF data migration/SQL serah-IT)"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "ImportTraining audit+konstanta (#24), BulkBackfill konstanta (#27), CleanupAttemptHistory GET+POST (#23)"
      contains: "CleanupAttemptHistory"
    - path: "Views/Admin/CleanupAttemptHistory.cshtml"
      provides: "preview-count + confirm Hapus form (#23)"
      contains: "AntiForgeryToken"
    - path: "HcPortal.Tests/OrphanCleanupTests.cs"
      provides: "integration real-SQL idempotent orphan cleanup (#23)"
      contains: "RecordCascadeFixture"
    - path: "HcPortal.Tests/ImportTrainingAuditTests.cs"
      provides: "assert AuditLog + field assignment (#24)"
      contains: "ImportTraining"
  key_links:
    - from: "GET CleanupAttemptHistory"
      to: "_context.AssessmentAttemptHistory orphan count"
      via: "Where(h => !AssessmentSessions.Any(s => s.Id == h.SessionId)).CountAsync()"
      pattern: "!_context.AssessmentSessions.Any\\(s => s.Id == h.SessionId\\)"
    - from: "POST CleanupAttemptHistory"
      to: "_auditLog.LogAsync"
      via: "audit setelah RemoveRange+SaveChanges"
      pattern: "_auditLog.LogAsync"
---

<objective>
Tiga fix di `TrainingAdminController.cs` (+ views): **#24** `ImportTraining` audit log + konstanta `AssessmentType` + `GenerateCertificate=isPassed`; **#27** `BulkBackfill` konstanta + rename label UI; **#23** endpoint admin idempotent untuk cleanup orphan `AttemptHistory` legacy (preview→execute+audit).

Purpose: (#24) Konsistensi field + jejak audit import. (#27) Konstanta tipe + label jujur ("Bulk Backfill (Restore Lost Data)" menyesatkan → "Bulk Import Nilai (Excel)"). (#23) Bersihkan orphan AttemptHistory legacy (pra-cascade-engine 367) via UI per-environment, tanpa edit DB langsung (Dev Workflow).
Output: 3 fix di TrainingAdminController + 4 view + 2 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-CONTEXT.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-RESEARCH.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md

<interfaces>
<!-- Kontrak diekstrak dari source. JANGAN eksplorasi codebase. -->

_auditLog.LogAsync [VERIFIED: Services/AuditLogService.cs:21-27] (SaveChanges internal):
```csharp
Task LogAsync(string actorUserId, string actorName, string actionType, string description, int? targetId = null, string? targetType = null);
```

AssessmentConstants.AssessmentType [VERIFIED: Models/AssessmentConstants.cs]:
```csharp
public const string Manual = "Manual"; // gunakan AssessmentConstants.AssessmentType.Manual — JANGAN literal
```

ImportTraining assessment-branch [VERIFIED: TrainingAdminController.cs]:
- L1263 `bool isPassed = lulusStr.Equals("Ya", ...)` (sudah ada)
- L1297 `GenerateCertificate = true,` (→ ubah jadi isPassed)
- L1307 `AssessmentType = ""` (→ ubah jadi konstanta Manual)
- L1415 `return View(results);` (akhir method — tambah audit ringkasan SEBELUM ini)
- `results` = List<ImportTrainingResult> dengan `.Status` ∈ {"Success","Skip","Error"}

BulkBackfillAssessment [VERIFIED: TrainingAdminController.cs:884]: `AssessmentType = "Standard",` (→ Manual)
BulkBackfill GET [VERIFIED: TrainingAdminController.cs:760-765]: `[HttpGet][Authorize(Roles="Admin")] public IActionResult BulkBackfill() => View();`
BulkBackfillAssessment POST attrs [VERIFIED: :768-771]: `[HttpPost][ValidateAntiForgeryToken][Authorize(Roles="Admin")]`

AssessmentAttemptHistory [VERIFIED: Models/AssessmentAttemptHistory.cs]: `int SessionId` (plain, NO FK ke session), `string UserId` + `ApplicationUser? User` (FK ke User).
DbSet [VERIFIED: Data/ApplicationDbContext.cs:65]: `_context.AssessmentAttemptHistory`
Logger field [VERIFIED: TrainingAdminController.cs:17]: `_logger`
_userManager.GetUserAsync(User) → ApplicationUser (FullName/UserName/Id) untuk aktor audit.

View label #27 callsites [VERIFIED]:
- Views/Admin/BulkBackfill.cshtml: L2 ViewData["Title"], L10 breadcrumb, L16 <h2>, L17 subtitle
- Views/Admin/Index.cshtml:298 `<span class="fw-bold">Bulk Backfill (Restore Lost Data)</span>`
- Views/Admin/Shared/_AssessmentGroupsTab.cshtml:320 `...Bulk Backfill (Restore Lost Data)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImportTraining #24 + BulkBackfill #27 (konstanta + audit + label)</name>
  <files>Controllers/TrainingAdminController.cs, Views/Admin/BulkBackfill.cshtml, Views/Admin/Index.cshtml, Views/Admin/Shared/_AssessmentGroupsTab.cshtml</files>
  <read_first>
    - Controllers/TrainingAdminController.cs:1276-1320 (ImportTraining assessment-branch — field assignment L1297/L1307)
    - Controllers/TrainingAdminController.cs:1410-1416 (akhir method, return View(results))
    - Controllers/TrainingAdminController.cs:884 (BulkBackfillAssessment AssessmentType="Standard")
    - Controllers/TrainingAdminController.cs:894-907 (analog audit BulkBackfillAssessment per-row)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (sections "ImportTraining #24", "BulkBackfillAssessment #27", "#27 label swap")
  </read_first>
  <action>
    **A. #24 ImportTraining field assignment** (assessment-branch, L1297 + L1307):
    - L1297 `GenerateCertificate = true,` → `GenerateCertificate = isPassed,` (hanya lulus dapat sertifikat; `isPassed` sudah ada L1263).
    - L1307 `AssessmentType = ""` → `AssessmentType = AssessmentConstants.AssessmentType.Manual,` (perhatikan koma — ini field terakhir; tambah koma jika perlu agar valid).

    **B. #24 ImportTraining audit ringkasan** — sisipkan SEBELUM L1415 `return View(results);` (1 entri ringkasan, BUKAN per-row, Open Question 3 RESEARCH):
    ```csharp
    var importActor = await _userManager.GetUserAsync(User);
    if (importActor != null)
    {
        int ok   = results.Count(r => r.Status == "Success");
        int skip = results.Count(r => r.Status == "Skip");
        int err  = results.Count(r => r.Status == "Error");
        await _auditLog.LogAsync(importActor.Id, importActor.FullName ?? importActor.UserName ?? importActor.Id,
            "ImportTraining", $"Import: {ok} sukses, {skip} skip, {err} error.", null, "AssessmentSession");
    }
    ```

    **C. #27 BulkBackfill konstanta** (L884): `AssessmentType = "Standard",` → `AssessmentType = AssessmentConstants.AssessmentType.Manual,`.

    **D. #27 label UI** → persis **"Bulk Import Nilai (Excel)"** di 3 view:
    - `Views/Admin/BulkBackfill.cshtml`: L2 `ViewData["Title"] = "Bulk Import Nilai (Excel)";`, L10 breadcrumb active text, L16 `<h2>` text (pertahankan ikon `<i class="bi bi-cloud-upload"></i>`), dan L17 subtitle — ganti judul/breadcrumb/h2 jadi "Bulk Import Nilai (Excel)" (subtitle deskripsi boleh diperjelas "upload Excel berisi NIP + Score peserta" tanpa kata "Restore"/"Backfill").
    - `Views/Admin/Index.cshtml:298`: `Bulk Backfill (Restore Lost Data)` → `Bulk Import Nilai (Excel)`.
    - `Views/Admin/Shared/_AssessmentGroupsTab.cshtml:320`: `Bulk Backfill (Restore Lost Data)` → `Bulk Import Nilai (Excel)` (pertahankan ikon `<i class="bi bi-arrow-counterclockwise me-2"></i>`).

    Residu #27 (identitas sesi backfill = Id baru) ACCEPTED by design — JANGAN ubah identitas. Migration=false.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GenerateCertificate = isPassed" Controllers/TrainingAdminController.cs` ≥1.
    - `grep -c "AssessmentType = AssessmentConstants.AssessmentType.Manual" Controllers/TrainingAdminController.cs` ≥3 (ImportTraining L731-area existing + L1307 baru + BulkBackfill L884 baru — minimal naik dari sebelumnya).
    - `grep "AssessmentType = \"\"" Controllers/TrainingAdminController.cs` = 0 match; `grep "AssessmentType = \"Standard\"" ...` = 0 match.
    - `grep "\"ImportTraining\"" Controllers/TrainingAdminController.cs` ≥1 (audit actionType).
    - `grep -rc "Bulk Import Nilai (Excel)" Views/Admin/BulkBackfill.cshtml Views/Admin/Index.cshtml Views/Admin/Shared/_AssessmentGroupsTab.cshtml` ≥3 total; `grep -rn "Bulk Backfill (Restore Lost Data)" Views/Admin/` = 0 match.
    - `dotnet build` 0 error; quick suite no regression.
  </acceptance_criteria>
  <done>ImportTraining: Manual konstanta + isPassed + audit ringkasan; BulkBackfill: Manual konstanta + label "Bulk Import Nilai (Excel)" di 3 view; no regression.</done>
</task>

<task type="auto">
  <name>Task 2: CleanupAttemptHistory endpoint (#23 GET preview + POST execute+audit) + view</name>
  <files>Controllers/TrainingAdminController.cs, Views/Admin/CleanupAttemptHistory.cshtml</files>
  <read_first>
    - Controllers/TrainingAdminController.cs:760-771 (BulkBackfill GET + POST attrs analog — auth/CSRF/role)
    - Controllers/TrainingAdminController.cs:894-907 (audit pattern)
    - Models/AssessmentAttemptHistory.cs (SessionId plain int, no FK session; UserId FK)
    - Views/Admin/BulkBackfill.cshtml (analog view — breadcrumb, card, TempData alert, AntiForgeryToken form)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (sections "CleanupAttemptHistory #23", "CleanupAttemptHistory.cshtml #23")
  </read_first>
  <action>
    **A. GET CleanupAttemptHistory** (read-only preview, di `TrainingAdminController.cs` — punya `_context`, `_auditLog`, `_userManager`):
    ```csharp
    // #23 D-01/D-02: preview orphan AttemptHistory (SessionId dangling — tak ada AssessmentSession induk)
    [HttpGet]
    [Authorize(Roles = "Admin")]
    public async Task<IActionResult> CleanupAttemptHistory()
    {
        int orphanCount = await _context.AssessmentAttemptHistory
            .Where(h => !_context.AssessmentSessions.Any(s => s.Id == h.SessionId))
            .CountAsync();
        ViewData["OrphanCount"] = orphanCount;
        return View();
    }
    ```

    **B. POST CleanupAttemptHistory** (execute + audit, destructive → CSRF + role):
    ```csharp
    [HttpPost]
    [ValidateAntiForgeryToken]
    [Authorize(Roles = "Admin")]
    public async Task<IActionResult> CleanupAttemptHistoryExecute()
    {
        var orphanQuery = _context.AssessmentAttemptHistory
            .Where(h => !_context.AssessmentSessions.Any(s => s.Id == h.SessionId));
        var rows = await orphanQuery.ToListAsync();
        int deleted = rows.Count;
        if (deleted > 0)
        {
            _context.AssessmentAttemptHistory.RemoveRange(rows);
            await _context.SaveChangesAsync();
            var actor = await _userManager.GetUserAsync(User);
            if (actor != null)
                await _auditLog.LogAsync(actor.Id, actor.FullName ?? actor.UserName ?? actor.Id,
                    "CleanupAttemptHistory", $"Hapus {deleted} orphan AttemptHistory (SessionId dangling).", null, "AssessmentAttemptHistory");
        }
        TempData["Success"] = $"Selesai: {deleted} orphan AttemptHistory dihapus.";
        return RedirectToAction(nameof(CleanupAttemptHistory));
    }
    ```
    Idempotent: re-run → query orphan otomatis kosong → deleted=0 (D-02). Definisi orphan SEMPIT (SessionId dangling saja — JANGAN perluas).

    **C. View `Views/Admin/CleanupAttemptHistory.cshtml`** (analog BulkBackfill.cshtml):
    - Breadcrumb ke Admin/Index; `ViewData["Title"] = "Cleanup AttemptHistory Orphan";`.
    - Blok alert `TempData["Success"]` / `TempData["Error"]` (salin pola BulkBackfill).
    - Tampilkan **preview-count**: `var orphanCount = (int)(ViewData["OrphanCount"] ?? 0);` → kartu menampilkan "Ditemukan @orphanCount baris AttemptHistory orphan (sesi induk sudah terhapus)."
    - Form POST ke `CleanupAttemptHistoryExecute` dengan `@Html.AntiForgeryToken()` + tombol "Hapus Orphan" — DISABLE/sembunyikan tombol bila `orphanCount == 0` (tampilkan "Tidak ada orphan — DB bersih."). Tambah konfirmasi JS `onsubmit="return confirm('Hapus @orphanCount orphan AttemptHistory? Tidak bisa di-undo.')"`.
    - Bahasa Indonesia (CLAUDE.md).

    Migration=false — JANGAN buat migration/SQL script. JANGAN sentuh skema.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public async Task<IActionResult> CleanupAttemptHistory(" Controllers/TrainingAdminController.cs` ≥1 (GET) + `grep "CleanupAttemptHistoryExecute" Controllers/TrainingAdminController.cs` ≥1 (POST).
    - `grep "!_context.AssessmentSessions.Any(s => s.Id == h.SessionId)" Controllers/TrainingAdminController.cs` ≥2 (GET preview + POST execute — definisi orphan single-source).
    - POST method punya `[ValidateAntiForgeryToken]` + `[Authorize(Roles = "Admin")]` (grep 3 baris sebelum signature execute).
    - `grep "_auditLog.LogAsync" Controllers/TrainingAdminController.cs` mengandung `"CleanupAttemptHistory"` actionType.
    - File `Views/Admin/CleanupAttemptHistory.cshtml` ada; `grep "AntiForgeryToken" Views/Admin/CleanupAttemptHistory.cshtml` ≥1; `grep "OrphanCount" Views/Admin/CleanupAttemptHistory.cshtml` ≥1.
    - `dotnet build` 0 error; quick suite no regression. Tidak ada `Migrations/*` baru.
  </acceptance_criteria>
  <done>GET preview-count orphan tampil; POST admin-gated+CSRF hapus orphan+audit; re-run idempotent (query auto-empty); view Bahasa Indonesia dengan konfirmasi.</done>
</task>

<task type="auto">
  <name>Task 3: [TEST] OrphanCleanupTests.cs (#23) + ImportTrainingAuditTests.cs (#24)</name>
  <files>HcPortal.Tests/OrphanCleanupTests.cs, HcPortal.Tests/ImportTrainingAuditTests.cs</files>
  <read_first>
    - HcPortal.Tests/RecordCascadeIntegrationTests.cs:21-100 (RecordCascadeFixture + SeedUserAsync pola — REUSE)
    - Controllers/TrainingAdminController.cs CleanupAttemptHistory* (hasil Task 2)
    - Models/AssessmentAttemptHistory.cs (insert orphan: SessionId=dangling, UserId=valid seeded user)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (section "#23 orphan insert in test")
  </read_first>
  <action>
    **A. `HcPortal.Tests/OrphanCleanupTests.cs`** — `[Trait("Category","Integration")]`, `IClassFixture<RecordCascadeFixture>` (reuse). Test logika cleanup orphan langsung pada DbContext (bukan invoke controller penuh — hindari mock UserManager/HttpContext berat):
    1. Seed valid User (FK User). Insert 2 orphan `AssessmentAttemptHistory { SessionId = 999990/999991, UserId = seededUserId, Title="X", Category="Y", AttemptNumber=1 }` + 1 non-orphan (seed AssessmentSession nyata, lalu AttemptHistory dengan SessionId = session.Id valid).
    2. **Preview count**: jalankan query orphan `Where(h => !ctx.AssessmentSessions.Any(s => s.Id == h.SessionId)).CountAsync()` → `Assert.Equal(2, count)` (hanya 2 dangling, non-orphan tak terhitung).
    3. **Execute**: `RemoveRange(orphanRows)` + SaveChanges → assert 2 orphan hilang, non-orphan masih ada.
    4. **Idempotent re-run**: query orphan count lagi → `Assert.Equal(0, ...)`.
    Nama [Fact] mengandung "OrphanCleanup".

    **B. `HcPortal.Tests/ImportTrainingAuditTests.cs`** — assert field + audit. Karena ImportTraining = controller dengan Excel parse (ClosedXML) + UserManager, full invoke berat → uji **kontrak field assignment** via [Fact] data-level: bikin AssessmentSession dengan `AssessmentType = AssessmentConstants.AssessmentType.Manual` + `GenerateCertificate = isPassed` dan assert nilai (membuktikan konstanta + logika isPassed benar untuk Ya/Tidak). Tambah [Fact] audit-shape: panggil `_auditLog.LogAsync(...,"ImportTraining",...)` pada real-SQL ctx + assert 1 row AuditLog dengan ActionType="ImportTraining" tercipta (membuktikan audit ter-persist). Reuse `RecordCascadeFixture`. Nama [Fact] mengandung "ImportTrainingAudit". Bila invoke controller penuh tak praktis → catat di summary (pola A4 fallback) + andalkan acceptance grep Task 1.

    Commit terpisah `git add HcPortal.Tests/OrphanCleanupTests.cs HcPortal.Tests/ImportTrainingAuditTests.cs` eksplisit (untracked-file lesson).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "OrphanCleanup|ImportTrainingAudit"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/OrphanCleanupTests.cs` + `HcPortal.Tests/ImportTrainingAuditTests.cs` ada; keduanya `grep "RecordCascadeFixture"` ≥1.
    - OrphanCleanupTests: `grep "Assert.Equal(2"` ≥1 (preview) + `grep "Assert.Equal(0"` ≥1 (idempotent re-run).
    - ImportTrainingAuditTests: `grep "AssessmentConstants.AssessmentType.Manual"` ≥1 + `grep "ImportTraining"` ≥1.
    - `dotnet test HcPortal.Tests --filter "OrphanCleanup|ImportTrainingAudit"` semua passed (real-SQL, localhost\SQLEXPRESS aktif).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Test real-SQL: orphan preview=2, execute hapus, re-run idempotent=0; #24 field konstanta+isPassed + audit ImportTraining ter-persist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CleanupAttemptHistory POST | Admin trigger destructive bulk-delete (untrusted request) |
| Browser → ImportTraining/BulkBackfill POST | Admin upload Excel (untrusted file) |
| Controller → DB (RemoveRange) | Mass delete orphan rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-368-05 | Tampering (CSRF) | #23 CleanupAttemptHistoryExecute POST (destructive) | mitigate | `[ValidateAntiForgeryToken]` + `[Authorize(Roles="Admin")]` + view `@Html.AntiForgeryToken()` + confirm dialog. |
| T-368-06 | Elevation of Privilege | #23 endpoint diakses non-admin | mitigate | `[Authorize(Roles="Admin")]` di GET + POST (admin-gated). |
| T-368-07 | Denial of Service (mass over-delete) | #23 query orphan terlalu luas → hapus data valid | mitigate | Definisi orphan SEMPIT (D-02: hanya `SessionId` dangling) + preview-count tampil SEBELUM eksekusi + audit log + idempotent. Non-orphan (SessionId valid) tidak pernah masuk query. |
| T-368-08 | Repudiation | #23/#24 delete/import tanpa jejak | mitigate | `_auditLog.LogAsync` (actionType "CleanupAttemptHistory"/"ImportTraining", deskripsi jumlah). |
| T-368-09 | Info Disclosure | preview-count bocorkan jumlah data internal | accept | Preview-count hanya untuk Admin (gated); informasi maintenance non-sensitif, kebutuhan eksplisit kontrak D-01. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "OrphanCleanup|ImportTrainingAudit"` passed (real-SQL).
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` no regression.
- grep acceptance per task; label 3 view = "Bulk Import Nilai (Excel)", 0 "Bulk Backfill (Restore Lost Data)".
- Migration=false: tidak ada `Migrations/*` baru.
- (Manual UAT difinalkan Plan 04 — preview→execute→idempotent di browser dengan Seed Workflow.)
</verification>

<success_criteria>
- #24: ImportTraining Manual konstanta + GenerateCertificate=isPassed + audit ringkasan.
- #27: BulkBackfill Manual konstanta + label "Bulk Import Nilai (Excel)" di 3 view.
- #23: GET preview-count + POST admin-gated+CSRF hapus orphan+audit + idempotent.
- 2 test file baru hijau; no regression; migration=false.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-02-SUMMARY.md`.
</output>
