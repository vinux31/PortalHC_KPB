---
phase: 368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/ResetEtScoreTests.cs
autonomous: true
requirements: ["#22"]
must_haves:
  truths:
    - "ResetAssessment menghapus SessionElemenTeknisScores milik sesi (AssessmentSessionId == id) di dalam batch cleanup existing, SEBELUM SaveChangesAsync L3974"
    - "Retake setelah reset → GradingService meng-insert ET scores BARU tanpa unique-index violation (tidak kena swallow DbUpdateException → analitik tidak stale)"
    - "ResetAssessment TIDAK menambah BeginTransactionAsync baru (tetap pakai SaveChanges + ExecuteUpdateAsync existing); tidak menyentuh cleanup lain selain ET"
    - "Zero schema change (migration=false)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ResetAssessment RemoveRange SessionElemenTeknisScores (#22)"
      contains: "SessionElemenTeknisScores"
    - path: "HcPortal.Tests/ResetEtScoreTests.cs"
      provides: "integration real-SQL retake-after-reset → ET fresh (#22)"
      contains: "RecordCascadeFixture"
  key_links:
    - from: "ResetAssessment cleanup"
      to: "SessionElemenTeknisScores RemoveRange"
      via: "Where(e => e.AssessmentSessionId == id) sebelum L3974 SaveChangesAsync"
      pattern: "SessionElemenTeknisScores\\s*\\.Where\\(e => e.AssessmentSessionId == id\\)"
---

<objective>
Tambahkan `RemoveRange SessionElemenTeknisScores` ke cleanup `ResetAssessment` (#22) supaya retake setelah reset menghasilkan ET scores BARU (bukan stale).

Purpose: Unique index `IX_SessionElemenTeknisScores_AssessmentSessionId_ElemenTeknis` membuat `GradingService` (L174) gagal `Add` ET baru saat retake (AssessmentSessionId sama) → `catch(DbUpdateException)` menelan exception + `ChangeTracker.Clear()` → ET tetap stale, analitik salah. DELETE session sudah bersihkan ET (cascade 367), tapi RESET tidak (session tidak dihapus) — itulah gap #22.
Output: 1 sisipan RemoveRange di ResetAssessment + 1 test file integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-CONTEXT.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-RESEARCH.md
@.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md

<interfaces>
<!-- Kontrak diekstrak dari source. JANGAN eksplorasi codebase. -->

ResetAssessment [VERIFIED: AssessmentAdminController.cs:3889] `public async Task<IActionResult> ResetAssessment(int id)`:
- NO explicit transaction (tidak ada BeginTransactionAsync).
- L3958-3963 RemoveRange PackageUserResponses (Where AssessmentSessionId==id)
- L3965-3970 Remove UserPackageAssignment
- L3974 `await _context.SaveChangesAsync(); // flush archive + delete operations first`  ← SISIP #22 SEBELUM baris ini
- L3976-3988 ExecuteUpdateAsync set status Open + reset fields

SessionElemenTeknisScore [VERIFIED: Models/SessionElemenTeknisScore.cs]: `int Id`, `int AssessmentSessionId` (FK), `string ElemenTeknis`, `int CorrectCount`, `int QuestionCount`.
DbSet [VERIFIED: Data/ApplicationDbContext.cs:81]: `_context.SessionElemenTeknisScores`
Unique index [VERIFIED: ApplicationDbContext.cs:629-631]: `IX_SessionElemenTeknisScores_AssessmentSessionId_ElemenTeknis`.
GradingService insert ET [VERIFIED: Services/GradingService.cs:174] `_context.SessionElemenTeknisScores.Add(new SessionElemenTeknisScore {...})` + catch(DbUpdateException) swallow + ChangeTracker.Clear (L189-193).
FK = OnDelete.Cascade pada AssessmentSessionId.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [TEST-FIRST] ResetEtScoreTests.cs — retake setelah reset → ET fresh (#22)</name>
  <files>HcPortal.Tests/ResetEtScoreTests.cs</files>
  <read_first>
    - HcPortal.Tests/RecordCascadeIntegrationTests.cs:21-100 (RecordCascadeFixture + SeedUserAsync + NewSession — REUSE pola disposable real-SQL)
    - Models/SessionElemenTeknisScore.cs (shape: AssessmentSessionId, ElemenTeknis, CorrectCount, QuestionCount)
    - Data/ApplicationDbContext.cs:629-631 (unique index AssessmentSessionId+ElemenTeknis)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-RESEARCH.md (Pattern 4 + Pitfall 1)
  </read_first>
  <action>
    Buat `HcPortal.Tests/ResetEtScoreTests.cs`. `[Trait("Category","Integration")]`, `IClassFixture<RecordCascadeFixture>` (reuse fixture 367 public). Buat helper lokal `NewCtx()` + `SeedUserAsync(ctx)` (pola 367).

    Uji **logika cleanup ET + regen** langsung pada DbContext (hindari invoke controller penuh — ResetAssessment butuh HubContext/UserManager/SignalR). 2-3 [Fact] (nama mengandung "ResetEtCleanup"):
    1. **ResetEtCleanup_RemovesEtScores**: seed User + AssessmentSession + 2 `SessionElemenTeknisScore {AssessmentSessionId=sessionId, ElemenTeknis="E1"/"E2", CorrectCount=1, QuestionCount=2}`. Jalankan logika cleanup #22: `var et = await ctx.SessionElemenTeknisScores.Where(e => e.AssessmentSessionId == sessionId).ToListAsync(); ctx.SessionElemenTeknisScores.RemoveRange(et); await ctx.SaveChangesAsync();` → assert `ctx.SessionElemenTeknisScores.Where(...==sessionId).Count() == 0`.
    2. **ResetEtCleanup_RetakeReinsertNoUniqueViolation**: setelah cleanup (#1), simulasikan retake → `ctx.SessionElemenTeknisScores.Add(new SessionElemenTeknisScore{AssessmentSessionId=sessionId, ElemenTeknis="E1", CorrectCount=2, QuestionCount=2}); await ctx.SaveChangesAsync();` → TIDAK throw (slot kosong) → assert ET baru `CorrectCount==2` (fresh, bukan stale 1).
    3. (opsional) **ResetEtCleanup_WithoutCleanup_DuplicateThrows**: TANPA cleanup, Add ET ElemenTeknis="E1" kedua → assert `await Assert.ThrowsAnyAsync<DbUpdateException>(...)` (membuktikan unique index = akar #22; bila throw tak konsisten antar provider, boleh skip [Fact] ini + catat).

    Commit terpisah `git add HcPortal.Tests/ResetEtScoreTests.cs` eksplisit (untracked-file lesson).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "ResetEtCleanup"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/ResetEtScoreTests.cs` ada; `grep "RecordCascadeFixture"` ≥1; `grep "[Trait(\"Category\", \"Integration\")]"` ≥1.
    - ≥2 [Fact] mengandung "ResetEtCleanup"; `grep "SessionElemenTeknisScores"` ≥2.
    - `dotnet test HcPortal.Tests --filter "ResetEtCleanup"` passed (real-SQL, localhost\SQLEXPRESS aktif).
    - `dotnet build` 0 error.
  </acceptance_criteria>
  <done>Test real-SQL membuktikan: cleanup hapus ET → retake re-insert ElemenTeknis sama TANPA unique-violation → ET fresh.</done>
</task>

<task type="auto">
  <name>Task 2: ResetAssessment RemoveRange SessionElemenTeknisScores (#22)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:3958-3988 (cleanup block existing — RemoveRange PackageUserResponses L3958, Remove UPA L3965, SaveChangesAsync L3974, ExecuteUpdateAsync L3976)
    - .planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-PATTERNS.md (section "ResetAssessment #22" — target insert sebelum L3974)
    - HcPortal.Tests/ResetEtScoreTests.cs (kontrak dari Task 1)
  </read_first>
  <action>
    Sisipkan RemoveRange ET BERSAMA RemoveRange existing, **SEBELUM L3974 `await _context.SaveChangesAsync();`** (agar ter-flush dalam batch yang sama). Letakkan tepat setelah Remove UserPackageAssignment (L3970), sebelum komentar "3. Reset session state" / SaveChanges:
    ```csharp
    // #22 D-07: bersihkan ET scores stale agar retake regenerasi ET BARU (cegah unique-index violation di GradingService)
    var etScores = await _context.SessionElemenTeknisScores
        .Where(e => e.AssessmentSessionId == id)
        .ToListAsync();
    if (etScores.Any())
        _context.SessionElemenTeknisScores.RemoveRange(etScores);
    ```
    LARANGAN KERAS (anti-scope-creep, D-07):
    - JANGAN tambah `BeginTransactionAsync`/transaction baru — method ini sengaja pakai SaveChanges + ExecuteUpdateAsync (mengubah ini = scope creep).
    - JANGAN RemoveRange tabel analytics lain (hanya `SessionElemenTeknisScores`).
    - JANGAN ubah cleanup existing (PackageUserResponses/UserPackageAssignment/archive AttemptHistory) atau ExecuteUpdateAsync.
    - JANGAN sentuh ResetGuard/IsResettable dari 367 (preserve file-overlap).
    Migration=false.
  </action>
  <verify>
    <automated>dotnet build; dotnet test HcPortal.Tests --filter "ResetEtCleanup"; dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SessionElemenTeknisScores" Controllers/AssessmentAdminController.cs` muncul di ResetAssessment dengan `.Where(e => e.AssessmentSessionId == id)` + `RemoveRange`.
    - Sisipan berada SEBELUM `await _context.SaveChangesAsync();` di L3974-area (grep line: RemoveRange ET line number < SaveChangesAsync L3974 line number di ResetAssessment).
    - `grep "BeginTransactionAsync" Controllers/AssessmentAdminController.cs` di ResetAssessment = 0 match (tidak ditambahkan).
    - `dotnet build` 0 error; `--filter "ResetEtCleanup"` passed; quick suite no regression.
    - Tidak ada `Migrations/*` baru.
  </acceptance_criteria>
  <done>ResetAssessment hapus ET scores dalam batch cleanup; retake regenerasi ET fresh; no new transaction; no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → ResetAssessment POST | Admin reset sesi (existing auth) |
| Controller → DB (RemoveRange ET) | Hapus ET scores milik sesi yang direset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-368-10 | Tampering | #22 RemoveRange ET hapus data sesi lain | mitigate | Filter ketat `AssessmentSessionId == id` (id = sesi yang sedang direset, dari route existing) — tidak ada cross-session delete. |
| T-368-11 | — (no new surface) | #22 menambah cleanup pada alur ResetAssessment yang sudah ter-authorize | accept | Tidak ada permukaan serangan baru: endpoint + authorize existing tak berubah; hanya menambah RemoveRange ke batch cleanup yang sudah ada. ET = data turunan sesi yang sama. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "ResetEtCleanup"` passed (real-SQL).
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` no regression.
- grep: RemoveRange ET sebelum SaveChangesAsync; no BeginTransactionAsync baru.
- Migration=false.
</verification>

<success_criteria>
- #22: ResetAssessment hapus SessionElemenTeknisScores dalam batch cleanup (sebelum SaveChanges); retake → ET fresh tanpa unique violation; no new transaction; tidak menyentuh cleanup lain.
- 1 test file integration hijau; no regression; migration=false.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/368-delete-records-hygiene-lanjutan-edit-atomic-file-reset-et-sc/368-03-SUMMARY.md`.
</output>
