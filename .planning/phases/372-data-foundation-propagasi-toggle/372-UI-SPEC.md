---
phase: 372
slug: data-foundation-propagasi-toggle
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-13
---

# Phase 372 — UI Design Contract

> Visual and interaction contract for the Shuffle Toggle frontend additions to the Create Assessment wizard. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope guard:** This phase adds ONLY (1) dua toggle `form-switch` "Acak Soal" + "Acak Pilihan Jawaban" di Langkah 3 Grup B "Pengaturan Ujian", (2) help-text edukatif Bahasa Indonesia di bawah tiap toggle, (3) baris ringkasan read-only ON/OFF di Langkah 4 Konfirmasi. Toggle di ManagePackages, lock UI, warning, reminder Pre/Post = **Phase 374 — JANGAN dispec di sini**. No new component library, no restyle. Reuse pola existing verbatim.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable (ASP.NET MVC Razor + Bootstrap, bukan React/Next/Vite) |
| Component library | Bootstrap 5.3 (admin theme existing) |
| Icon library | Bootstrap Icons (`bi-*`) |
| Font | Theme default (inherited dari layout existing — tidak diubah) |

**shadcn gate:** N/A. Stack adalah ASP.NET MVC server-rendered Razor + Bootstrap 5.3, bukan React/Next.js/Vite. Tidak ada `components.json`, `tailwind.config.*`, atau `package.json` frontend framework. Tidak ada registry pihak ketiga → registry safety gate tidak berlaku.

**Reuse target verbatim:** Toggle baru WAJIB meniru pola `IsTokenRequired` di `Views/Admin/CreateAssessment.cshtml:505-508` (`div.form-check.form-switch.mb-2` > `input.form-check-input[type=checkbox]` + `label.form-check-label`) plus `div.form-text.text-muted` untuk help-text (pola `PassPercentage` L497, `GenerateCertificate` L537). Summary Langkah 4 meniru baris `dt.col-sm-4`/`dd.col-sm-8` di `dl.row` (`summary-standard-settings` L653-661 dan `summary-ppt-settings` L691-697).

---

## Spacing Scale

Declared values (Bootstrap utility classes, multiples of 4 — diturunkan dari `$spacer` Bootstrap = 16px):

| Token | Value | Bootstrap util | Usage di phase ini |
|-------|-------|----------------|--------------------|
| xs | 4px | `.mb-1` / `.me-1` | Gap ikon–teks dalam label, jarak help-text mikro |
| sm | 8px | `.mb-2` | Jarak bawah tiap `form-switch` (sama persis `IsTokenRequired` L505 `mb-2`) |
| md | 16px | `.g-3` (row gutter) / `.mb-3` | Gutter antar kolom Grup B, jarak antar card summary |
| lg | 24px | `.mb-4` | Jarak bawah card Grup B (sama Grup A/B/C/D existing) |

Exceptions: none. Tidak ada custom CSS. Semua spacing pakai utility class Bootstrap yang sudah dipakai card Grup B existing — JANGAN perkenalkan nilai px baru.

**Placement contract:**
- Kedua toggle ditempatkan di Grup B "Pengaturan Ujian" (`card` L473-522), di dalam `div.row.g-3` (L478), sebagai kolom baru `div.col-md-6` SETELAH kolom Token (L519). TANPA card/grup baru (D-01).
- Dua toggle boleh berbagi satu `col-md-6` (di-stack vertikal, masing-masing `form-check form-switch mb-2` + help-text) ATAU dua `col-md-6` terpisah. Rekomendasi: **satu `col-md-6` berisi dua toggle ber-stack** dengan sub-heading `label.form-label.fw-bold` "Pengacakan Soal & Jawaban" + ikon `bi-shuffle`, mengikuti pola visual blok "Security Token" (L502-503) yang membungkus satu switch + detail. Ini menjaga keduanya tetap berdekatan secara visual karena saling terkait, dan seimbang dengan kolom Token di sebelahnya.

---

## Typography

Tidak ada ukuran/berat font baru. Semua mengikuti kelas Bootstrap yang sudah dipakai di Grup B existing.

| Role | Class | Weight | Usage |
|------|-------|--------|-------|
| Group sub-heading / field label | `.form-label.fw-bold` | 700 (bold) | Label "Pengacakan Soal & Jawaban" (heading kolom) — sama pola L502, L493, L481 |
| Toggle label | `.form-check-label` | 400 (normal) | Teks "Acak Soal" / "Acak Pilihan Jawaban" di samping switch — sama pola L507 |
| Help-text | `.form-text.text-muted` | 400 (normal) | Penjelasan edukatif ON vs OFF — sama pola L497, L537 |
| Summary label (dt) | `dt.col-sm-4` (default) | 400 (normal) | Label baris "Acak Soal" di Langkah 4 — sama pola L656/L659 |
| Summary value (dd) | `dd.col-sm-8` (default) | 400 (normal) | Nilai "Aktif (ON)" / "Nonaktif (OFF)" |

Line height: default Bootstrap body (`1.5`). Tidak diubah.

Exceptions: none. Dilarang menambah `style=` inline ukuran font atau kelas heading baru.

---

## Color

Project memakai palet Bootstrap default admin theme. Phase ini **tidak memperkenalkan warna baru** dan **tidak butuh accent** — toggle adalah kontrol netral, bukan aksi destruktif atau primary CTA.

| Role | Value | Usage di phase ini |
|------|-------|--------------------|
| Dominant (60%) | Bootstrap `--bs-body-bg` (putih) + `bg-light` card-header | Surface card Grup B, body form |
| Secondary (30%) | Bootstrap `--bs-secondary` / `text-muted` (#6c757d) | Help-text `text-muted`, label sekunder |
| Accent (10%) | Bootstrap `text-primary` (#0d6efd) | HANYA ikon `bi-shuffle` di sub-heading kolom (meniru ikon `bi-shield-lock text-primary` Token L503). Switch "checked" state pakai warna primary bawaan Bootstrap `form-switch` — itu default komponen, bukan accent baru. |
| Destructive | not used | Tidak ada aksi destruktif di phase ini |

Accent reserved for: ikon `bi-shuffle.text-primary` pada sub-heading kolom toggle. Tidak ada elemen lain yang boleh memakai accent di tambahan ini.

**Status ON/OFF di Langkah 4 (summary):** gunakan **teks polos** "Aktif (ON)" / "Nonaktif (OFF)" mengikuti pola baris Status/Token existing (teks polos di `dd`, bukan badge berwarna — lihat L656/L658). Boleh opsional pakai `badge` netral tapi DEFAULT = teks polos agar konsisten dengan baris summary lain di blok yang sama. JANGAN pakai badge hijau/merah (akan menyiratkan benar/salah; ON vs OFF dua-duanya valid).

---

## Copywriting Contract

> Ini adalah deliverable PALING PENTING phase ini (D-02 menunda copy final ke contract ini). Copy WAJIB detail, edukatif, dan bebas jargon untuk HC non-teknis. Semua copy Bahasa Indonesia (mandat CLAUDE.md). Copy di bawah ADALAH copy final yang harus diimplementasi executor verbatim.

### Sub-heading kolom (label.form-label.fw-bold)

```
Pengacakan Soal & Jawaban
```
(dengan ikon `<i class="bi bi-shuffle text-primary me-1"></i>` di depan, meniru pola "Security Token" L502-503)

### Toggle 1 — Acak Soal

| Elemen | Copy final |
|--------|-----------|
| Label switch (`form-check-label`) | `Acak Soal` |
| Help-text (`form-text text-muted`) | `Saat aktif, urutan dan pemilihan soal diacak berbeda untuk setiap peserta — sehingga peserta yang duduk berdekatan kecil kemungkinan mendapat soal yang sama urutannya. Saat nonaktif, semua peserta mendapat soal yang sama dengan urutan yang sama persis (urutan asli sesuai paket soal).` |

### Toggle 2 — Acak Pilihan Jawaban

| Elemen | Copy final |
|--------|-----------|
| Label switch (`form-check-label`) | `Acak Pilihan Jawaban` |
| Help-text (`form-text text-muted`) | `Saat aktif, urutan pilihan jawaban (A, B, C, D) diacak berbeda di setiap soal untuk tiap peserta — jawaban benar tetap dinilai dengan benar, hanya posisinya yang berpindah. Saat nonaktif, urutan pilihan jawaban tampil sama untuk semua peserta sesuai urutan yang tersimpan di sistem.` |

**Catatan copy (untuk executor — JANGAN dipangkas):**
- Frasa "jawaban benar tetap dinilai dengan benar" pada Toggle 2 WAJIB ada — menjawab kekhawatiran umum HC bahwa mengacak pilihan akan merusak penilaian (didukung spec §13 / D-11: grading pakai `PackageOption.Id`, bukan posisi huruf).
- Kedua toggle independen (D-10) — copy tidak boleh menyiratkan satu mematikan/menyalakan yang lain.
- Hindari istilah teknis: "Fisher-Yates", "round-robin", "shuffle", "session", "sibling", "paket utuh per worker". HC tidak perlu tahu mekanisme internal.

### Default state

Kedua toggle **checked (ON)** secara default di wizard (D-03). Implementasi: properti model `ShuffleQuestions` / `ShuffleOptions` default `true` (spec §4) → `asp-for` merender `checked`. JANGAN andalkan default browser; nilai harus datang dari model agar konsisten dengan EF bool-trap fix (D-08).

### Langkah 4 — Konfirmasi (summary, read-only) (D-05)

Tambah dua baris `dt`/`dd` di KEDUA blok summary (standard `summary-standard-settings` L653-661 DAN Pre-Post `summary-ppt-settings` L691-697), karena satu pasang toggle berlaku untuk Pre dan Post (D-04). Tempatkan setelah baris "Pass Percentage" (sebelum "Valid Until") agar berkelompok dengan pengaturan ujian.

| Baris (dt) | Nilai ON (dd) | Nilai OFF (dd) |
|------------|---------------|----------------|
| `Acak Soal` | `Aktif (ON)` | `Nonaktif (OFF)` |
| `Acak Pilihan Jawaban` | `Aktif (ON)` | `Nonaktif (OFF)` |

JS `populateSummary()` (`CreateAssessment.cshtml:1053`) mengisi span baru berdasarkan `.checked` dari input toggle — meniru pola token L1160-1163 (`tokenEl.checked ? ... : ...`). ID span yang disarankan: `summary-shuffle-questions`, `summary-shuffle-options` (standard) dan `summary-ppt-shuffle-questions`, `summary-ppt-shuffle-options` (Pre-Post). Penamaan ID exact = diskresi executor (D-05 "format visual summary = diskresi Claude").

### Tabel ringkas elemen wajib

| Element | Copy |
|---------|------|
| Primary CTA | not applicable (CTA "Buat Assessment" existing L712 tidak diubah) |
| Empty state heading | not applicable (tidak ada empty state di tambahan ini) |
| Empty state body | not applicable |
| Error state | not applicable (toggle bool, tidak ada validasi gagal; tidak ada pesan error baru) |
| Destructive confirmation | not applicable (tidak ada aksi destruktif) |

---

## Interaction Contract

| Aspek | Kontrak |
|-------|---------|
| Default | Kedua switch ON (checked) saat wizard dibuka untuk assessment baru (D-03). |
| Independensi | Toggle 1 dan Toggle 2 sepenuhnya independen — mengubah satu tidak mengubah/menonaktifkan yang lain (D-10). Tidak ada conditional show/hide antar keduanya (beda dari Token yang menampilkan `tokenSection` saat dicentang — toggle shuffle TIDAK punya field tambahan saat ON/OFF). |
| Pre/Post | Satu pasang toggle di Langkah 3 berlaku untuk Pre DAN Post saat membuat Pre-Post Test (D-04). TIDAK ada toggle terpisah Pre/Post di wizard. |
| Live summary | Saat user maju ke Langkah 4, `populateSummary()` membaca state checkbox dan menampilkan "Aktif (ON)"/"Nonaktif (OFF)". Saat user "Edit" balik ke Langkah 3 dan mengubah, lalu kembali, summary harus mencerminkan nilai terbaru (pola existing — `populateSummary` dipanggil ulang setiap masuk Langkah 4, L862). |
| Keyboard / a11y | `input.form-check-input[type=checkbox]` + `label[for]` (pola L506-507) sudah memberi label terasosiasi & focusable native. Tiap toggle WAJIB punya `id` unik + `label[for]` cocok (a11y baseline existing). Tidak perlu ARIA tambahan. |
| Disabled/lock | TIDAK ada lock di wizard create (lock hanya muncul di ManagePackages = Phase 374). Di wizard, kedua toggle selalu editable. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none (Bootstrap 5.3 vendored) | tidak ada registry pihak ketiga | not applicable |

shadcn tidak diinisialisasi (stack bukan React/Next/Vite). Tidak ada blok registry pihak ketiga. Registry vetting gate tidak berlaku.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
