---
phase: 375-test-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/ShuffleModeMatrixTests.cs
  - Controllers/CMPController.cs
autonomous: true
requirements: [SHUF-16, SHUF-15]
must_haves:
  truths:
    - "Full xUnit suite hijau termasuk consolidation sweep baru (baseline 347 → 348+)"
    - "Mode-matrix sweep deterministik seed-stable di semua mode (ON/OFF × 1/≥2 paket × opsi ON/OFF)"
    - "Guard paket kosong tidak melempar DivideByZero"
    - "CMPController.cs bebas komentar stale shuffle (SHUF-15 closed)"
  artifacts:
    - path: "HcPortal.Tests/ShuffleModeMatrixTests.cs"
      provides: "Consolidation [Theory] sweep — single-source-of-truth per-mode invariant"
      contains: "[Theory]"
      min_lines: 40
  key_links:
    - from: "HcPortal.Tests/ShuffleModeMatrixTests.cs"
      to: "Helpers/ShuffleEngine.cs"
      via: "ShuffleEngine.BuildQuestionAssignment + BuildOptionShuffle (new Random(42))"
      pattern: "ShuffleEngine\\.(BuildQuestionAssignment|BuildOptionShuffle)"
---

<objective>
Tambah SATU consolidation `[Theory]/[InlineData]` mode-matrix sweep di ATAS 27 test shuffle existing (D-01) sebagai single-source-of-truth eksplisit per-mode, lalu jalankan full suite konfirmasi hijau (SC#1). Sekaligus fold SHUF-15: verify `CMPController.cs` bebas komentar stale shuffle + tutup box (D-07).

Purpose: SC#1 phase 375 = suite xUnit hijau termasuk sweep. Sweep memberi titik kebenaran high-level per-mode yang gampang dibaca, tanpa menduplikasi detail assertion `ShuffleEngineTests` (12 test). SHUF-15 = pekerjaan cleanup terakhir engine v27.0.
Output: `HcPortal.Tests/ShuffleModeMatrixTests.cs` (baru) + konfirmasi `CMPController.cs` clean + suite 348+ hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/375-test-uat/375-CONTEXT.md
@.planning/phases/375-test-uat/375-PATTERNS.md

<interfaces>
<!-- Engine under test. Signatures dari Helpers/ShuffleEngine.cs — executor pakai langsung, no eksplorasi. -->

ShuffleEngine API (Helpers/ShuffleEngine.cs):
```csharp
public static List<int> BuildQuestionAssignment(
    List<AssessmentPackage> packages, bool shuffleQuestions, int workerIndex, Random rng);
//   ON  → BuildCrossPackageAssignment (1pkg=acak; ≥2pkg=sampling K-min ET-balanced)
//   OFF 1pkg → questions.OrderBy(q.Order).Select(q.Id)              [SHUF-05: urut asli, no shuffle]
//   OFF ≥2pkg → packagesWithQuestions[workerIndex % count] paket UTUH urut Order   [SHUF-06]
//              (filter Questions.Count>0 SEBELUM modulo → guard DivideByZero)

public static Dictionary<int, List<int>> BuildOptionShuffle(
    IEnumerable<PackageQuestion> questions, bool shuffleOptions, Random rng);
//   ON  → dict[questionId] = Fisher-Yates option Ids ;  OFF → empty dict (caller serializes "{}")

public static void Shuffle<T>(List<T> list, Random rng);   // Fisher-Yates in-place
```

In-memory `Pkg` builder pattern (reuse dari ShuffleEngineTests.cs lines 18-30 — tiap question dapat 2 option ids `id*10`, `id*10+1`):
```csharp
private static AssessmentPackage Pkg(int packageNumber, params (int id, int order, string? et)[] qs)
{
    var p = new AssessmentPackage { PackageNumber = packageNumber, Id = packageNumber };
    foreach (var (id, order, et) in qs)
        p.Questions.Add(new PackageQuestion {
            Id = id, Order = order, ElemenTeknis = et,
            Options = { new PackageOption { Id = id * 10 }, new PackageOption { Id = id * 10 + 1 } }
        });
    return p;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tulis consolidation mode-matrix sweep ShuffleModeMatrixTests.cs</name>
  <read_first>
    - HcPortal.Tests/ShuffleEngineTests.cs (analog: 12 test, `Pkg` helper lines 18-30, seed-stability pola lines 115-126, option ON/OFF invariant lines 144-161, guard all-empty lines 87-94 — TIRU pola, JANGAN duplikasi detail)
    - HcPortal.Tests/ShuffleToggleRulesTests.cs (analog struktur `[Theory]/[InlineData]`)
    - Helpers/ShuffleEngine.cs (engine under test — verifikasi signature 3 method)
    - .planning/phases/375-test-uat/375-PATTERNS.md (§ "consolidation sweep [Theory]" — shape sweep + InlineData matrix)
  </read_first>
  <action>
    Buat file BARU `HcPortal.Tests/ShuffleModeMatrixTests.cs` (BUKAN edit ShuffleEngineTests.cs — file terpisah untuk single-source-of-truth yang jelas). Namespace + usings ikut ShuffleEngineTests.cs.

    Sertakan private static `Pkg(...)` helper (copy verbatim dari interfaces block di context — tiap question 2 option `id*10`/`id*10+1`) + private static `BuildPackages(int count)` yang bangun `count` paket masing-masing ≥2 question (mis. count=1 → 1 paket 4 soal id 10..13; count=2 → 2 paket masing-masing 2 soal, id berbeda, supaya round-robin per-worker bisa diuji).

    Tulis SATU `[Theory]` `ModeMatrix_Invariant(bool shuffleQuestions, bool shuffleOptions, int packageCount)` dengan 4 `[InlineData]` (D-01a high-level per-mode invariant, JANGAN dobel detail ShuffleEngineTests):
    - `[InlineData(true,  true,  1)]`  — ON 1pkg: hasil shuffled & seed-stable; opt dict non-empty
    - `[InlineData(false, false, 1)]` — OFF 1pkg: urut asli `q.Order` (assert `qIds` == ids terurut Order); opt dict empty
    - `[InlineData(false, true,  2)]` — OFF ≥2pkg: 1 paket UTUH round-robin index-stabil (worker 0 → semua id dari satu paket, urut Order); opt dict non-empty
    - `[InlineData(true,  false, 2)]` — ON ≥2pkg: sampling K-min (hasil non-empty); opt dict empty

    Body `ModeMatrix_Invariant`:
    1. `var packages = BuildPackages(packageCount);`
    2. Determinisme seed (SEMUA mode): panggil `ShuffleEngine.BuildQuestionAssignment(packages, shuffleQuestions, workerIndex: 0, rng: new Random(42))` DUA KALI → `Assert.Equal(qIds, qIds2)`.
    3. Per-mode high-level invariant via switch atas `(shuffleQuestions, packageCount)`:
       - `(false, 1)` → `Assert.Equal(packages[0].Questions.OrderBy(q => q.Order).Select(q => q.Id).ToList(), qIds)` (urut asli)
       - `(false, 2)` → assert semua id di `qIds` berasal dari SATU paket yang sama (worker 0 = `packagesWithQuestions[0 % count]`) DAN urut Order (paket utuh)
       - `(true, _)`  → `Assert.NotEmpty(qIds)` (shuffled assignment ada; tak assert urutan spesifik — sudah di ShuffleEngineTests)
    4. Option dict: `var opt = ShuffleEngine.BuildOptionShuffle(packages.SelectMany(p => p.Questions), shuffleOptions, new Random(42));` → bila `shuffleOptions` true `Assert.NotEmpty(opt)` else `Assert.Empty(opt)`.

    Plus SATU `[Fact] AllPackagesEmpty_NoDivideByZero()` (pola ShuffleEngineTests lines 87-94): `Record.Exception(() => ShuffleEngine.BuildQuestionAssignment(new List<AssessmentPackage> { Pkg(1), Pkg(2) }, false, 0, new Random()))` → `Assert.Null(ex)` (paket tanpa question → filter-then-modulo, tak boleh DivideByZero).

    JANGAN: tambah test detail yang sudah ada (sampling K-min ET-balance exact, reshuffle, propagate, migration default — itu sudah covered 27 test existing). Sweep ini HANYA invariant per-mode + guard.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ShuffleModeMatrix"</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/ShuffleModeMatrixTests.cs` ada dan berisi `[Theory]` + minimal 4 `[InlineData]` + 1 `[Fact]` guard
    - `ShuffleModeMatrixTests.cs` memanggil `ShuffleEngine.BuildQuestionAssignment` DAN `ShuffleEngine.BuildOptionShuffle` dengan `new Random(42)` (grep `new Random(42)` match)
    - `dotnet test --filter "FullyQualifiedName~ShuffleModeMatrix"` exits 0 dengan ≥5 test passed (4 InlineData + 1 Fact)
  </acceptance_criteria>
  <done>Sweep file ada, semua mode-matrix test + guard hijau, no duplikasi detail existing.</done>
</task>

<task type="auto">
  <name>Task 2: SHUF-15 verify CMPController clean + full suite hijau (SC#1)</name>
  <read_first>
    - Controllers/CMPController.cs (target SHUF-15 — re-grep komentar stale; PATTERNS konfirmasi line 989/1060 sudah benar, no stale)
    - .planning/phases/375-test-uat/375-CONTEXT.md (D-07 — SHUF-15 = verify clean + close box, edit HANYA bila ada stale)
  </read_first>
  <action>
    Langkah SHUF-15 (D-07):
    1. Re-grep `Controllers/CMPController.cs` untuk komentar stale: `option shuffle removed` dan `shuffle removed` (case-insensitive). Pre-verifikasi 2026-06-13 → ZERO match (line 989 = `// Option shuffle gated on ShuffleOptions ...` benar, line 1060 = `// ViewBag.OptionShuffle when ShuffleOptions=ON ...` benar).
    2. JIKA grep menemukan komentar stale baru → bersihkan (ganti dengan komentar akurat "gated on ShuffleOptions" / hapus). JIKA ZERO match (expected) → TIDAK ada edit; SHUF-15 = verify-only clean.
    3. Tandai SHUF-15 done (dicatat di SUMMARY plan ini — `CMPController.cs` confirmed clean, no edit needed).

    Lalu SC#1 (suite hijau termasuk sweep):
    4. Jalankan shuffle subset: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~Shuffle"` — harus hijau (incl ShuffleModeMatrix baru).
    5. Jalankan FULL suite: `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` — harus hijau, total naik dari baseline 347 ke 348+ (jumlah sesuai banyak test sweep yang ditambah Task 1).

    CATATAN env (CLAUDE.md): full suite real-SQL fixture (`IClassFixture<ProtonCompletionFixture>`) butuh SQL Server lokal (`HcPortalDB_Dev`) hidup. Bila login/fixture error 53 → start SQLBrowser (memory `reference_local_e2e_sql_env_fix`). JANGAN sentuh DB Dev/Prod.
  </action>
  <verify>
    <automated>cd "C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff"; dotnet test HcPortal.Tests/HcPortal.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "option shuffle removed\|shuffle removed" Controllers/CMPController.cs` → ZERO match (clean) ATAU komentar stale yang ditemukan sudah dibersihkan
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exits 0, total test ≥348 (baseline 347 + sweep), 0 failed
    - SUMMARY plan mencatat SHUF-15 status = done (CMPController clean, no/which edit)
  </acceptance_criteria>
  <done>CMPController bebas komentar stale (SHUF-15 closed); full suite hijau ≥348 test termasuk sweep (SC#1 evidence).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only phase — tidak ada production attack surface baru. SHUF-15 = verify-only/komentar; sweep = in-memory unit test tanpa I/O eksternal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-375-01 | I (Info Disclosure) | xUnit fixtures (real-SQL) | accept | No new attack surface — test-only phase. Fixture `ProtonCompletionFixture` disposable DB per-test (pola existing); tak menyentuh prod/Dev DB (CLAUDE.md). No credential/PII baru ditulis. |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~ShuffleModeMatrix"` → ≥5 passed
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` → ≥348 passed, 0 failed (SC#1)
- `grep -i "option shuffle removed|shuffle removed" Controllers/CMPController.cs` → 0 match
</verification>

<success_criteria>
SC#1 phase 375 terpenuhi: suite xUnit hijau termasuk consolidation sweep semua mode + determinisme round-robin (≥348 test). SHUF-15 closed (CMPController clean).
</success_criteria>

<output>
After completion, create `.planning/phases/375-test-uat/375-01-SUMMARY.md`
</output>
