---
phase: 376-fix-essay-only-score-aggregation
plan: 02
type: execute
wave: 2
depends_on: [376-01]
files_modified:
  - Helpers/AssessmentScoreAggregator.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [GRADE-01, GRADE-02]
must_haves:
  truths:
    - "Helper murni AssessmentScoreAggregator menghitung agregasi skor (D-02/D-04 single source of truth)"
    - "FinalizeEssayGrading memakai helper + derivasi question-set robust (D-06 fallback)"
    - "Essay-only finalize → AssessmentSessions.Score = agregasi (bukan 0) (SC2)"
    - "Mixed (MC+MA+essay) Score tetap benar — no regression (SC3)"
    - "Invariant Phase 310 idempotency / 324 no-TR / 358 Proton hook PRESERVED verbatim"
  artifacts:
    - path: "Helpers/AssessmentScoreAggregator.cs"
      provides: "Helper static murni: Compute(questions, responses, passPercentage) → ScoreAggregateResult"
      contains: "public static class AssessmentScoreAggregator"
      min_lines: 35
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "FinalizeEssayGrading: derivasi robust + panggil helper menggantikan math inline L3535-3565"
      contains: "AssessmentScoreAggregator.Compute"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs FinalizeEssayGrading"
      to: "HcPortal.Helpers.AssessmentScoreAggregator"
      via: "static call Compute(allQuestions, allResponses, session.PassPercentage)"
      pattern: "AssessmentScoreAggregator\\.Compute"
    - from: "FinalizeEssayGrading fallback"
      to: "PackageUserResponses session"
      via: "derive question-set saat shuffledIds kosong + log warning"
      pattern: "LogWarning.*shuffledIds|LogWarning.*maxScore"
---

<objective>
Ekstrak math agregasi inline (`FinalizeEssayGrading` L3535-3565) ke helper murni `Helpers/AssessmentScoreAggregator.cs` (pola ShuffleEngine.cs, kill-drift D-02) — single source of truth dipakai forward path DAN recompute (Plan 03). Pasang di `FinalizeEssayGrading` + derivasi question-set ROBUST (D-06 fallback: shuffledIds kosong → derive dari PackageUserResponses session) + log warning (D-05/D-06). Formula D-04 di-port VERBATIM.

Ini membuat unit test Plan 01 GREEN + memenuhi SC2 (essay-only Score≠0) + SC3 (mixed no-regression).

Purpose: Fix forward path benar + kill-drift agregasi (reuse di recompute Plan 03).
Output: `AssessmentScoreAggregator.cs` (baru), `FinalizeEssayGrading` patched (HANYA blok math + derivasi; invariant lain UTUH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-CONTEXT.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-RESEARCH.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md
@.planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md
@CLAUDE.md

<interfaces>
<!-- Helper signature FINAL (test Plan 01 mengunci behavior). -->
Helpers/AssessmentScoreAggregator.cs (DIBUAT plan ini):
```csharp
namespace HcPortal.Helpers
{
    public readonly record struct ScoreAggregateResult(int TotalScore, int MaxScore, int Percentage, bool IsPassed);
    public static class AssessmentScoreAggregator
    {
        public static ScoreAggregateResult Compute(
            IEnumerable<PackageQuestion> questions,
            IEnumerable<PackageUserResponse> responses,
            int passPercentage);
    }
}
```

<!-- Math inline SUMBER EKSTRAKSI — PORT VERBATIM (Controllers/AssessmentAdminController.cs L3535-3565, [VERIFIED]): -->
```csharp
int totalScore = 0; int maxScore = 0;
foreach (var q in allQuestions)
{
    maxScore += q.ScoreValue;
    switch (q.QuestionType ?? "MultipleChoice")
    {
        case "MultipleChoice":
            var mcResp = allResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue);
            if (mcResp != null) { var opt = q.Options.FirstOrDefault(o => o.Id == mcResp.PackageOptionId!.Value);
                if (opt != null && opt.IsCorrect) totalScore += q.ScoreValue; }
            break;
        case "MultipleAnswer":
            var maSelected = allResponses.Where(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue)
                .Select(r => r.PackageOptionId!.Value).ToHashSet();
            var maCorrect = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
            if (maSelected.SetEquals(maCorrect)) totalScore += q.ScoreValue;
            break;
        case "Essay":
            var essayResp = allResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id);
            if (essayResp?.EssayScore.HasValue == true) totalScore += essayResp.EssayScore.Value;
            break;
    }
}
int finalPercentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;   // D-04 LOCKED
bool isPassed = finalPercentage >= session.PassPercentage;
```

<!-- Logger field tersedia [VERIFIED L25]: -->
private readonly ILogger<AssessmentAdminController> _logger;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat Helpers/AssessmentScoreAggregator.cs (pure, kill-drift D-02/D-04)</name>
  <files>Helpers/AssessmentScoreAggregator.cs</files>
  <read_first>
    - Helpers/ShuffleEngine.cs (L1-22 — pola `public static class` + XML doc "Pure by design")
    - Controllers/AssessmentAdminController.cs (L3535-3565 — math inline sumber ekstraksi VERBATIM)
    - HcPortal.Tests/AssessmentScoreAggregatorTests.cs (test Plan 01 — kontrak yang harus dipenuhi)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md (§AssessmentScoreAggregator.cs — namespace/imports/return shape)
    - Models/AssessmentPackage.cs (PackageQuestion: QuestionType string?, ScoreValue int, Options), Models/PackageUserResponse.cs (PackageQuestionId, PackageOptionId int?, EssayScore int?)
  </read_first>
  <behavior>
    Helper memenuhi SEMUA case AssessmentScoreAggregatorTests (Plan 01):
    - essay-only ScoreValue=100/EssayScore=80, pass=70 → (Total=80, Max=100, Percentage=80, IsPassed=true).
    - maxScore=0 → Percentage=0, no throw (D-05).
    - mixed MC50+MA50+Essay100(80) → Percentage=90 (no-drift vs L3564).
    - empty questions → Percentage=0, no throw.
  </behavior>
  <action>
    BUAT `Helpers/AssessmentScoreAggregator.cs`:
    - `using System; using System.Collections.Generic; using System.Linq; using HcPortal.Models;`
    - `namespace HcPortal.Helpers`
    - XML doc menyebut: "Phase 376 GRADE-01/02 — Pure by design (only System/Linq/HcPortal.Models) → unit-testable without a database. Single source of truth agregasi skor (forward finalize + recompute), kill-drift pola Phase 363/365."
    - `public readonly record struct ScoreAggregateResult(int TotalScore, int MaxScore, int Percentage, bool IsPassed);`
    - `public static class AssessmentScoreAggregator` dengan:
      ```csharp
      public static ScoreAggregateResult Compute(
          IEnumerable<PackageQuestion> questions,
          IEnumerable<PackageUserResponse> responses,
          int passPercentage)
      {
          var respList = responses as IList<PackageUserResponse> ?? responses.ToList();
          int totalScore = 0, maxScore = 0;
          foreach (var q in questions)
          {
              maxScore += q.ScoreValue;
              switch (q.QuestionType ?? "MultipleChoice")
              {
                  case "MultipleChoice":
                      var mcResp = respList.FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue);
                      if (mcResp != null) { var opt = q.Options.FirstOrDefault(o => o.Id == mcResp.PackageOptionId!.Value);
                          if (opt != null && opt.IsCorrect) totalScore += q.ScoreValue; }
                      break;
                  case "MultipleAnswer":
                      var maSelected = respList.Where(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue)
                          .Select(r => r.PackageOptionId!.Value).ToHashSet();
                      var maCorrect = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
                      if (maSelected.SetEquals(maCorrect)) totalScore += q.ScoreValue;
                      break;
                  case "Essay":
                      var essayResp = respList.FirstOrDefault(r => r.PackageQuestionId == q.Id);
                      if (essayResp?.EssayScore.HasValue == true) totalScore += essayResp.EssayScore.Value;
                      break;
              }
          }
          int percentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;   // D-04 LOCKED
          return new ScoreAggregateResult(totalScore, maxScore, percentage, percentage >= passPercentage);
      }
      ```
    - PORT VERBATIM — JANGAN ubah formula. Helper TIDAK throw, TIDAK log (caller yang log warning D-05). TIDAK inject `_context`, TIDAK async.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~AssessmentScoreAggregatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/AssessmentScoreAggregator.cs` ada + berisi `public static class AssessmentScoreAggregator` + `public readonly record struct ScoreAggregateResult`.
    - `grep -c "_context\|async\|await\|ILogger" Helpers/AssessmentScoreAggregator.cs` = 0 (helper murni, no EF/no async/no logger).
    - `dotnet build` exit 0.
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~AssessmentScoreAggregatorTests"` SEMUA GREEN (test Plan 01 sekarang lulus).
  </acceptance_criteria>
  <done>Helper murni ada + unit test Plan 01 GREEN. Formula D-04 single source of truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pasang helper + derivasi robust di FinalizeEssayGrading (SC2/SC3, preserve invariant)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (FinalizeEssayGrading L3469-3669 PENUH — wajib paham apa yang PRESERVE)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-RESEARCH.md (§Robust question-set derivation + §Common Pitfalls 2/3/4/5/6)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-PATTERNS.md (§FinalizeEssayGrading PRESERVE VERBATIM list)
    - .planning/phases/376-fix-essay-only-score-aggregation/376-DIAGNOSE.md (root cause + pilihan derivasi fallback dari Plan 01)
    - Models/UserPackageAssignment.cs (GetShuffledQuestionIds L60-71)
  </read_first>
  <behavior>
    - Essay-only finalize → AssessmentSessions.Score = agregasi (mis. 80, bukan 0). e2e FLOW L6 (Plan 01 un-fixme) GREEN.
    - Mixed (MC+MA+essay) finalize → Score tetap benar (no regression).
    - shuffledIds kosong → fallback derive question-set dari PackageUserResponses session + log warning; agregasi tetap benar.
    - maxScore=0 → Score=0 (fallback D-05) + log warning, finalize TIDAK di-block.
    - Klik finalize 2x → idempotent (Phase 310 replay-guard UTUH).
  </behavior>
  <action>
    Patch `FinalizeEssayGrading` — SENTUH HANYA: (a) derivasi `allQuestions` (L3527-3530) + (b) blok math (L3535-3565). PRESERVE VERBATIM sisanya.

    1. **Derivasi question-set ROBUST (D-06)** — ganti blok L3527-3530 `allQuestions` (gunakan pilihan derivasi dari 376-DIAGNOSE.md; default = PackageUserResponses session untuk essay-only, RESEARCH open-Q2):
       ```csharp
       List<PackageQuestion> allQuestions;
       if (shuffledIds.Count > 0)
       {
           allQuestions = await _context.PackageQuestions.Include(q => q.Options)
               .Where(q => shuffledIds.Contains(q.Id)).ToListAsync();
       }
       else
       {
           // D-06 fallback: shuffledIds kosong (root-cause H1) → derive dari response session.
           _logger.LogWarning("FinalizeEssayGrading: shuffledIds kosong session {SessionId} — fallback derive question-set dari PackageUserResponses.", sessionId);
           var respQIds = await _context.PackageUserResponses
               .Where(r => r.AssessmentSessionId == sessionId)
               .Select(r => r.PackageQuestionId).Distinct().ToListAsync();
           allQuestions = await _context.PackageQuestions.Include(q => q.Options)
               .Where(q => respQIds.Contains(q.Id)).ToListAsync();
       }
       ```
       (CATATAN: bila 376-DIAGNOSE.md menemukan root cause BUKAN H1 (mis. H2 casing), tetap pasang helper + guard; sesuaikan derivasi per temuan. Tetap pasang fallback defensif tipis + log per D-06.)

    2. **Ganti blok math L3535-3565** dengan panggilan helper (allResponses L3531-3533 TETAP):
       ```csharp
       var agg = AssessmentScoreAggregator.Compute(allQuestions, allResponses, session.PassPercentage);
       if (agg.MaxScore == 0)
           _logger.LogWarning("FinalizeEssayGrading: maxScore=0 session {SessionId} — anomali data, Score fallback 0.", sessionId);   // D-05
       int finalPercentage = agg.Percentage;
       bool isPassed = agg.IsPassed;
       ```
       Tambah `using HcPortal.Helpers;` di top file bila belum ada.

    3. **PRESERVE VERBATIM (Pitfall 2/3/4/5/6 — JANGAN sentuh):**
       - Attributes L3466-3468 `[HttpPost][Authorize(Roles="Admin, HC")][ValidateAntiForgeryToken]`.
       - Early-return Completed `alreadyFinalized` L3476-3491.
       - Status switch non-PendingGrading L3494-3504.
       - Guard "semua essay dinilai" L3523-3524.
       - `shuffledIds` line L3512 + `essayQuestions`/`essayResponses` L3514-3521.
       - `ExecuteUpdateAsync` WHERE-guard `Status==PendingGrading` + `rowsAffected` capture L3569-3575.
       - Race branch `rowsAffected==0` L3577-3600.
       - No-TrainingRecord comment L3602-3604 (Phase 324).
       - Cert generation L3606-3620, audit log L3622-3641, Proton hook L3643-3655, NotifyIfGroupCompleted L3657-3660 — forward path D-03 UTUH.

    `git diff` HARUS terbatas: top `using` + L3527-3530 (derivasi) + L3535-3565 (math→helper). TIDAK menyentuh L3466-3526 atau L3566-3669 selain pemanggilan helper.
  </action>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AssessmentScoreAggregator.Compute" Controllers/AssessmentAdminController.cs` ≥ 1.
    - `grep -c "GetShuffledQuestionIds" Controllers/AssessmentAdminController.cs` tetap ≥ 1 (L3512 PRESERVED — tidak dihapus).
    - `grep "Status == AssessmentConstants.AssessmentStatus.PendingGrading" Controllers/AssessmentAdminController.cs` masih ada (WHERE-guard L3570 PRESERVED).
    - `grep -c "_context.TrainingRecords.Add" Controllers/AssessmentAdminController.cs` = 0 (Pitfall 4 — tak ada auto-TR baru).
    - `grep "_protonCompletionService.EnsureAsync\|NotifyIfGroupCompleted" Controllers/AssessmentAdminController.cs` masih ada di forward path (Proton hook + notif PRESERVED).
    - `dotnet build` exit 0. `dotnet test HcPortal.Tests --filter "Category!=Integration"` GREEN.
    - e2e: `npx playwright test exam-types --grep "L6" --workers=1` GREEN (Score=80) — verifikasi di phase gate/wave merge (perlu `--workers=1` + SQLBrowser per MEMORY).
  </acceptance_criteria>
  <done>SC2 + SC3 terpenuhi: essay-only Score≠0, mixed tetap benar, invariant 310/324/358 UTUH, e2e L6 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin → FinalizeEssayGrading (existing) | Endpoint sudah ada (Plan ini tak ubah surface auth). Input `sessionId` int dari form admin ter-autentikasi. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-376-03 | T (Tampering) | Helper salah hitung → Score massal salah saat forward finalize | mitigate | Formula D-04 di-port VERBATIM + unit test no-drift (Plan 01) membuktikan helper == inline L3564; integration real-SQL Plan 03. |
| T-376-04 | E (Elevation) | Auth gate FinalizeEssayGrading | accept | Attributes `[Authorize(Roles="Admin, HC")][ValidateAntiForgeryToken]` PRESERVED verbatim — tak diubah. |
| T-376-05 | R (Repudiation) | Audit log finalize | accept | Audit log existing L3627-3641 PRESERVED (tak disentuh). |
| T-376-06 | D (DoS) / data anomaly | shuffledIds kosong / maxScore=0 senyap → Score=0 tanpa sinyal | mitigate | Log warning di titik fallback + saat maxScore=0 (D-05/D-06) — anomali tervisibilitas, tak block finalize. |
| T-376-07 | T (Tampering) | Helper flip isPassed → Proton hook (L3647) salah trigger | mitigate | isPassed via formula sama (D-04); hook forward path UNCHANGED; unit no-drift mengunci output identik. |
</threat_model>

<verification>
- `dotnet build` exit 0.
- `dotnet test HcPortal.Tests` (unit + integration bila ada) GREEN.
- `npx playwright test exam-types --grep "L6" --workers=1` GREEN (phase gate).
- `git diff Controllers/AssessmentAdminController.cs` terbatas pada using + L3527-3530 + L3535-3565 (verifikasi tak menyentuh invariant).
- Pitfall checks (grep) di acceptance_criteria semua lolos.
</verification>

<success_criteria>
- SC2: HC nilai essay-only + finalize → AssessmentSessions.Score = agregasi (bukan 0).
- SC3: Jalur mixed (MC+MA+essay) Score tetap benar — no regression.
- Helper kill-drift D-02 siap di-reuse Plan 03 (recompute).
- Invariant Phase 310/324/345/358 PRESERVED.
</success_criteria>

<output>
After completion, create `.planning/phases/376-fix-essay-only-score-aggregation/376-02-SUMMARY.md`. Sertakan: diff scope FinalizeEssayGrading, hasil e2e L6, konfirmasi invariant preserved (grep evidence).
</output>
