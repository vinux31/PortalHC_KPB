---
phase: 377-impersonation-identity-across-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/ImpersonationService.cs
  - Middleware/ImpersonationMiddleware.cs
autonomous: true
requirements: [IMP-01, IMP-02]
tags: [impersonation, identity, authorization, middleware, shared-core]

must_haves:
  truths:
    - "ImpersonationService punya effective-user resolver (single source, D-05) yang return X.Id saat mode=user, null saat mode=role (D-03), dan short-circuit ke real user saat non-impersonate (SC4)"
    - "Saat impersonate user X tapi X terhapus/null, middleware Stop() + redirect /Admin/Index + pesan (D-04 fail-closed, JANGAN fallback admin)"
    - "Pure-logic ResolveEffectiveUserDecision test (Plan 01) berubah GREEN"
  artifacts:
    - path: "Services/ImpersonationService.cs"
      provides: "GetEffectiveTargetUserId() + GetEffectiveUserAsync(UserManager) + static ResolveEffectiveUserDecision"
      contains: "GetEffectiveTargetUserId"
    - path: "Middleware/ImpersonationMiddleware.cs"
      provides: "D-04 fallback: target null → Stop()+redirect"
      contains: "User yang di-impersonate tidak ditemukan"
  key_links:
    - from: "ImpersonationService.GetEffectiveTargetUserId"
      to: "ImpersonationService.ResolveEffectiveUserDecision (pure)"
      via: "delegasi keputusan ke fungsi murni yang diuji"
      pattern: "ResolveEffectiveUserDecision"
    - from: "ImpersonationMiddleware.SetContextItems"
      to: "ImpersonationService.Stop"
      via: "targetUser==null → Stop()+redirect (pola auto-expire L56-66)"
      pattern: "service\\.Stop\\(\\)"
---

<objective>
Bangun fondasi single-source (D-05, pola shared-core 363/365/366): tambah effective-user resolver di `ImpersonationService` + fail-closed fallback D-04 di middleware. Setelah plan ini, controller (Wave 2) tinggal memanggil resolver di hulu — semua call-site self-read terfix otomatis tanpa duplikasi logika.

Purpose: 80% mekanisme sudah ada (role-level effective + middleware target-resolve + ownership helper). Plan ini menyambung GAP effective-USER dan mengunci fail-closed (mode-role kosong, target-null redirect) — inti keamanan IMP-01.
Output: `ImpersonationService` resolver (+ pure decision static yang membuat test Plan 01 GREEN) + middleware D-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-CONTEXT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md

# Test kontrak yang harus di-GREEN-kan plan ini:
@HcPortal.Tests/ImpersonationIdentityTests.cs

<interfaces>
<!-- API yang DIBUAT plan ini. Wave 2 (CMP/CDP/Home) konsumsi method ini. -->
<!-- ImpersonationService HANYA inject IHttpContextAccessor (TIDAK punya UserManager) — -->
<!-- maka GetEffectiveUserAsync menerima UserManager sebagai PARAMETER. -->

Building block existing (PAKAI ULANG, jangan duplikasi):
```csharp
public bool IsImpersonating();          // L30
public bool IsExpired();                // L62
public string? GetMode();               // L82  → "role" | "user" | null
public string? GetTargetUserId();       // L92  → X.Id saat mode=user
public int? GetEffectiveRoleLevel();    // L100 (sudah ada — pola yang di-extend)
```

API BARU di ImpersonationService.cs:
```csharp
public enum EffectiveUserDecision { UseRealUser, RoleModeEmpty, TargetUser }

// Pure (diuji Plan 01) — keputusan tanpa I/O.
public static EffectiveUserDecision ResolveEffectiveUserDecision(
    bool isImpersonating, bool isExpired, string? mode, string? targetUserId);

// Wrapper instance: null saat (UseRealUser | RoleModeEmpty), targetUserId saat TargetUser.
public string? GetEffectiveTargetUserId();

// Resolve ApplicationUser efektif. UserManager sebagai PARAMETER (service tak inject UM).
// Return: caller bedakan via tuple decision (lihat di bawah) untuk handle D-03 vs SC4 vs D-04.
public async Task<(ApplicationUser? User, EffectiveUserDecision Decision)> GetEffectiveUserAsync(
    UserManager<ApplicationUser> userManager);
```

Konsumen (Wave 2) pakai `GetEffectiveUserAsync` lalu branch by `Decision`:
- `UseRealUser` → resolve `userManager.GetUserAsync(principal)` di controller (SC4 identik hari ini)
- `RoleModeEmpty` → user=null → surface kosong + hint (D-03)
- `TargetUser` → user=X (sudah di-resolve), pakai X.Id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Effective-user resolver di ImpersonationService (D-05 single-source)</name>
  <files>Services/ImpersonationService.cs</files>
  <read_first>
    - Services/ImpersonationService.cs (FULL — extend paralel dengan GetEffectiveRoleLevel L100-117; building block IsImpersonating/IsExpired/GetMode/GetTargetUserId)
    - HcPortal.Tests/ImpersonationIdentityTests.cs (kontrak ResolveEffectiveUserDecision yang harus GREEN)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§ImpersonationService — guard-clause pattern verbatim)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (Pattern 1 + Pitfall 5 SC4 short-circuit)
  </read_first>
  <behavior>
    - ResolveEffectiveUserDecision(false,_,_,_) → UseRealUser; (true,true,_,_) → UseRealUser (expired)
    - ResolveEffectiveUserDecision(true,false,"role",_) → RoleModeEmpty
    - ResolveEffectiveUserDecision(true,false,"user",null|"") → RoleModeEmpty (D-04 trigger, fail-closed)
    - ResolveEffectiveUserDecision(true,false,"user","X") → TargetUser
    - GetEffectiveTargetUserId() → null untuk UseRealUser & RoleModeEmpty; targetUserId untuk TargetUser
    - GetEffectiveUserAsync: UseRealUser→(null,UseRealUser) [caller resolve real]; RoleModeEmpty→(null,RoleModeEmpty); TargetUser→(FindByIdAsync(X), TargetUser)
  </behavior>
  <action>
    Edit `Services/ImpersonationService.cs`. Tambah `using Microsoft.AspNetCore.Identity;` + `using HcPortal.Models;` (untuk `UserManager<ApplicationUser>` + `ApplicationUser`) bila belum ada (file saat ini namespace `HcPortal.Services`; cek `ApplicationUser` referensi via fully-qualified `HcPortal.Models.ApplicationUser` bila perlu hindari konflik using).

    1. Tambah `public enum EffectiveUserDecision { UseRealUser, RoleModeEmpty, TargetUser }` (di namespace, level sama dengan class atau nested public — pilih top-level public enum di namespace HcPortal.Services agar test `HcPortal.Services.EffectiveUserDecision` resolve).

    2. Tambah pure static (INI yang diuji Plan 01 — keputusan tanpa I/O):
    ```csharp
    public static EffectiveUserDecision ResolveEffectiveUserDecision(
        bool isImpersonating, bool isExpired, string? mode, string? targetUserId)
    {
        if (!isImpersonating || isExpired) return EffectiveUserDecision.UseRealUser; // SC4 + V3 expiry
        if (mode == "role") return EffectiveUserDecision.RoleModeEmpty;              // D-03
        if (mode == "user")
            return string.IsNullOrEmpty(targetUserId)
                ? EffectiveUserDecision.RoleModeEmpty                                 // D-04 trigger, fail-closed
                : EffectiveUserDecision.TargetUser;                                  // SC2
        return EffectiveUserDecision.UseRealUser;                                     // mode tak dikenal → aman
    }
    ```

    3. Tambah instance wrapper (komposisi building block existing — JANGAN re-derive session manual):
    ```csharp
    public string? GetEffectiveTargetUserId()
    {
        var decision = ResolveEffectiveUserDecision(IsImpersonating(), IsExpired(), GetMode(), GetTargetUserId());
        return decision == EffectiveUserDecision.TargetUser ? GetTargetUserId() : null;
    }
    ```

    4. Tambah resolver async (UserManager = PARAMETER; service tak inject UM):
    ```csharp
    public async Task<(HcPortal.Models.ApplicationUser? User, EffectiveUserDecision Decision)> GetEffectiveUserAsync(
        UserManager<HcPortal.Models.ApplicationUser> userManager)
    {
        var decision = ResolveEffectiveUserDecision(IsImpersonating(), IsExpired(), GetMode(), GetTargetUserId());
        if (decision != EffectiveUserDecision.TargetUser) return (null, decision);
        var target = await userManager.FindByIdAsync(GetTargetUserId()!);
        // target==null → D-04; middleware sudah Stop+redirect sebelum sampai sini. Fail-closed: jangan admin.
        return (target, EffectiveUserDecision.TargetUser);
    }
    ```

    Tambah XML doc comment ringkas tiap method (pola file existing). JANGAN ubah method existing (IsImpersonating/IsExpired/GetMode/GetTargetUserId/GetEffectiveRoleLevel) — hanya TAMBAH.

    Verifikasi `ApplicationUser` namespace: bila `ApplicationUser` di `HcPortal.Models`, pakai FQN seperti di atas. Build harus 0 error.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ImpersonationIdentity" --nologo 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0.
    - `Services/ImpersonationService.cs` memuat `enum EffectiveUserDecision`, `ResolveEffectiveUserDecision`, `GetEffectiveTargetUserId`, `GetEffectiveUserAsync`.
    - `dotnet test --filter "FullyQualifiedName~ImpersonationIdentity"` exits 0 (Plan 01 test GREEN — semua InlineData pass).
    - Method existing TIDAK berubah (git diff Services/ImpersonationService.cs hanya ADD, tak hapus method lama).
  </acceptance_criteria>
  <done>Resolver effective-user ada (pure + instance + async), Plan 01 test GREEN, build 0 error, method lama utuh.</done>
</task>

<task type="auto">
  <name>Task 2: Fallback D-04 target null/terhapus di middleware (fail-closed)</name>
  <files>Middleware/ImpersonationMiddleware.cs</files>
  <read_first>
    - Middleware/ImpersonationMiddleware.cs (FULL — InvokeAsync L25-109, SetContextItems L111-146, pola auto-expire L56-66)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§ImpersonationMiddleware — pola Stop()+redirect verbatim L56-66 + titik sisip L128-145)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (Pattern 2 + Assumption A2 + catatan SetContextItems static→bool)
  </read_first>
  <action>
    Edit `Middleware/ImpersonationMiddleware.cs`. Implement D-04: saat `mode=="user"` tapi `FindByIdAsync(targetUserId)` mengembalikan null (user terhapus) → `Stop()` + redirect `/Admin/Index` + pesan, SEBELUM request sampai controller. Fail-closed (JANGAN biarkan request lanjut dengan identitas admin).

    Strategi (paling DRY per A2 — `SetContextItems` sudah `FindByIdAsync` di L135): ubah `SetContextItems` agar bisa signal "sudah redirect, short-circuit".

    1. Ubah signature `private static async Task SetContextItems(...)` → `private static async Task<bool> SetContextItems(HttpContext context, ImpersonationService service)` (return `true` = lanjut normal; `false` = sudah redirect, caller harus `return`).

    2. Di branch `mode == "user"` (L128-145), pada `targetUserId != null`: setelah `var targetUser = await userManager.FindByIdAsync(targetUserId);`, bila `targetUser == null` → eksekusi pola fallback (copy struktur auto-expire L56-66):
    ```csharp
    if (targetUser == null)
    {
        service.Stop();
        var tempDataFactory = context.RequestServices.GetRequiredService<ITempDataDictionaryFactory>();
        var tempData = tempDataFactory.GetTempData(context);
        tempData["ErrorMessage"] = "User yang di-impersonate tidak ditemukan.";
        context.Response.Redirect("/Admin/Index");
        return false;
    }
    ```
    Bila `targetUser != null` → set HttpContext.Items seperti sekarang (tak berubah), lalu di akhir method `return true;`. Untuk branch `mode == "role"` dan path lain juga `return true;`.

    3. Update 2 call-site `SetContextItems` di `InvokeAsync`:
       - L74 (GET/HEAD): `if (!await SetContextItems(context, impersonationService)) return; await _next(context); return;`
       - L107 (whitelisted write): `if (!await SetContextItems(context, impersonationService)) return; await _next(context); return;` (atau bentuk setara yang short-circuit `_next` saat false).

    JANGAN ubah: whitelist, read-only block (L84-104), auto-expire (L56-66), bypass static/unauth.

    Catatan: ini juga memitigasi V3 — target hilang = sesi tak valid = Stop, konsisten auto-expire.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo 2>&1 | powershell -Command "$input | Select-String -Pattern 'Passed!|Failed!|error'"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0.
    - `Middleware/ImpersonationMiddleware.cs` memuat string `User yang di-impersonate tidak ditemukan` dan `service.Stop()` di dalam branch `mode == "user"` `targetUser == null`.
    - `SetContextItems` return `Task<bool>`; kedua call-site di InvokeAsync short-circuit (`return`) saat false.
    - Full xUnit suite hijau (`dotnet test` exits 0) — no regression.
    - git diff TIDAK menyentuh whitelist/read-only-block/auto-expire selain perubahan return-type chaining.
  </acceptance_criteria>
  <done>D-04 fail-closed terpasang di middleware (target null → Stop+redirect+pesan), short-circuit benar, suite hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session impersonation state → effective identity | Resolver menerjemahkan state session jadi identitas efektif untuk query. Salah resolve = data-leak. |
| middleware → controller | Middleware gate satu-satunya tempat fail-closed sebelum controller; bila lolos, controller akan pakai identitas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-377-04 | Information Disclosure | Resolver fallback ke admin saat mode-role / target-null → akar bug terulang | mitigate | `ResolveEffectiveUserDecision` fail-closed: RoleModeEmpty/target-null → user=null, BUKAN admin (Task 1, diuji Plan 01). |
| T-377-05 | Elevation of Privilege / V3 | Sesi impersonasi expired tapi resolver tetap pakai identitas X | mitigate | Guard `isExpired → UseRealUser` (short-circuit), konsisten `IsExpired()` 30-min existing. |
| T-377-06 | Information Disclosure / DoS-soft | Target user terhapus → request lanjut dengan identitas admin (akses tak terdefinisi) | mitigate (block: high) | D-04 middleware Stop()+redirect `/Admin/Index` SEBELUM controller (Task 2, fail-closed). |
| T-377-07 | Regression (SC4) | Rewrite resolver mengubah perilaku non-impersonate | mitigate | Branch pertama `!isImpersonating → UseRealUser`; full xUnit suite hijau acceptance. |
</threat_model>

<verification>
- Build 0 error; full xUnit suite hijau (no regression SC4).
- `dotnet test --filter "FullyQualifiedName~ImpersonationIdentity"` GREEN (Plan 01 RED → GREEN).
- Middleware memuat fallback D-04 string + Stop() di branch user/null.
</verification>

<success_criteria>
- Effective-user resolver single-source ada (D-05), fail-closed (D-03/D-04), short-circuit non-impersonate (SC4).
- Middleware D-04 redirect saat target null.
- Pondasi siap dikonsumsi Wave 2 (CMP/CDP/Home).
</success_criteria>

<output>
After completion, create `.planning/phases/377-impersonation-identity-across-surfaces/377-02-SUMMARY.md`.
Catat: signature final method resolver (untuk konsumen Wave 2), keputusan DRY D-04 (middleware vs helper), hasil full suite.
</output>
