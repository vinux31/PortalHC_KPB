---
phase: 377-impersonation-identity-across-surfaces
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/Records.cshtml
autonomous: true
requirements: [IMP-01, IMP-02]
tags: [impersonation, identity, authorization, cmp, write-on-get]

must_haves:
  truths:
    - "Impersonate user X → /CMP/Records tampil unified records X (assessment+training), bukan admin (SC2 — akar bug live)"
    - "Surface bypass (Assessment:203, ExportRecords:611, StartExam:867) resolve effective user X saat impersonasi"
    - "Mode role (tanpa target user) → /CMP/Records tampil KOSONG + hint, BUKAN redirect Login dan BUKAN data admin, dan TIDAK menampilkan nama/identitas admin di header (D-03)"
    - "StartExam saat impersonasi TIDAK menulis DB (write-on-GET auto-transition di-guard) — read-only (Pitfall 3)"
    - "Mode normal (non-impersonate) tak berubah (SC4)"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Resolver impersonation-aware + 3 bypass surface routed + StartExam guard + D-03 null-handling (Option A locked)"
      contains: "_impersonationService.GetEffectiveUserAsync"
    - path: "Views/CMP/Records.cshtml"
      provides: "Hint mode-role saat UnifiedRecords kosong karena impersonate-role (D-03) + null-guard Model.User"
      contains: "Pilih user spesifik"
  key_links:
    - from: "CMPController.GetCurrentUserRoleLevelAsync"
      to: "ImpersonationService.GetEffectiveUserAsync"
      via: "resolver hulu single-source (D-05)"
      pattern: "GetEffectiveUserAsync"
    - from: "CMPController.StartExam"
      to: "SaveChangesAsync (auto-transition)"
      via: "guard if(!_impersonationService.IsImpersonating()) sebelum Save (Pitfall 3)"
      pattern: "IsImpersonating\\(\\)"
---

<objective>
Rewrite `CMPController.GetCurrentUserRoleLevelAsync` jadi impersonation-aware (konsumsi resolver Plan 02) sehingga ~8 caller self-read terfix otomatis di hulu (Records/RecordsWorkerDetail/Certificate/CertificatePdf/Results/BuildSertifikatRowsAsync). Route 3 surface bypass (Assessment/ExportRecords/StartExam) ke resolver. Tangani D-03 (mode-role → kosong+hint, bukan redirect, bukan tampilkan identitas admin) di Records. Guard write-on-GET StartExam (Pitfall 3, read-only invariant).

Purpose: Ini surface akar bug LIVE (999.6: impersonate Iwan → /CMP/Records tampil 2 assessment admin). SC2 = bukti utama fix bekerja. StartExam guard = keamanan (cegah tampering DB via GET saat read-only).
Output: CMPController impersonation-aware + Records.cshtml hint (D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-CONTEXT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-AUDIT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-02-SUMMARY.md

<interfaces>
<!-- API resolver dari Plan 02 (Services/ImpersonationService.cs). _impersonationService SUDAH ter-inject di CMP (L40/56/71). -->
public enum EffectiveUserDecision { UseRealUser, RoleModeEmpty, TargetUser }
public string? GetEffectiveTargetUserId();
public async Task<(ApplicationUser? User, EffectiveUserDecision Decision)> GetEffectiveUserAsync(UserManager<ApplicationUser> userManager);
public bool IsImpersonating();         // untuk guard StartExam write-on-GET
public int? GetEffectiveRoleLevel();   // existing (L100) — dipakai untuk role-level efektif

<!-- Resolver CMP SEKARANG (L2388-2395, yang DI-rewrite): -->
private async Task<(ApplicationUser? User, int RoleLevel)> GetCurrentUserRoleLevelAsync() {
    var user = await _userManager.GetUserAsync(User);
    if (user == null) return (null, 0);
    var userRoles = await _userManager.GetRolesAsync(user);
    var roleLevel = UserRoles.GetRoleLevel(userRoles.FirstOrDefault() ?? "");
    return (user, roleLevel);
}

<!-- Ownership authz pure static (L2399) — full-fidelity D-01 = kirim currentUserId=X.Id; JANGAN ubah: -->
public static bool IsResultsAuthorized(string? ownerUserId, string currentUserId, int roleLevel, string? currentUserSection, string? ownerSection);

<!-- Caller resolver yang OTOMATIS terfix saat hulu di-rewrite (TIDAK perlu disentuh per-baris): -->
<!-- L481 Records, L545 RecordsWorkerDetail, L660 TeamAssessment, L721 TeamTraining, L774 RecordsTeamPartial, -->
<!-- L1733 Certificate, L1839 CertificatePdf, L2080 Results, L3870 BuildSertifikatRowsAsync -->

<!-- Surface BYPASS (call GetUserAsync(User) langsung — route eksplisit): -->
<!-- L203 Assessment, L611 ExportRecords(personal), L867 StartExam (write-on-GET L872-878) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite resolver impersonation-aware + D-03 null-handling di Records (Option A locked)</name>
  <files>Controllers/CMPController.cs, Views/CMP/Records.cshtml</files>
  <read_first>
    - Controllers/CMPController.cs (L2385-2408 resolver+IsResultsAuthorized; L477-560 Records; L40/56/71 DI _impersonationService; L86-101 contoh GetEffectiveRoleLevel usage)
    - Services/ImpersonationService.cs (API baru dari Plan 02)
    - Views/CMP/Records.cshtml (cari blok render UnifiedRecords + empty-state + tempat `Model.User` di-dereference di header untuk null-guard)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§CMPController — resolver SEKARANG verbatim + Pitfall 1 caller null-handling)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (Pattern 1 code example resolver + Pitfall 1)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-CONTEXT.md (D-03 LOCKED — mode-role: user=null, surface kosong + hint, TIDAK menampilkan data/identitas admin)
  </read_first>
  <action>
    Edit `Controllers/CMPController.cs`.

    A. REWRITE `GetCurrentUserRoleLevelAsync()` (L2388-2395) jadi impersonation-aware. Pertahankan signature `Task<(ApplicationUser? User, int RoleLevel)>` (sudah nullable). Logika:
    ```csharp
    private async Task<(ApplicationUser? User, int RoleLevel)> GetCurrentUserRoleLevelAsync()
    {
        var (effUser, decision) = await _impersonationService.GetEffectiveUserAsync(_userManager);

        // SC4: non-impersonate → identik hari ini
        if (decision == EffectiveUserDecision.UseRealUser)
        {
            var real = await _userManager.GetUserAsync(User);
            if (real == null) return (null, 0);
            var realRoles = await _userManager.GetRolesAsync(real);
            return (real, UserRoles.GetRoleLevel(realRoles.FirstOrDefault() ?? ""));
        }

        // D-03: mode role → user null (caller render kosong+hint); role-level efektif tetap dipakai untuk gating UI
        if (decision == EffectiveUserDecision.RoleModeEmpty)
            return (null, _impersonationService.GetEffectiveRoleLevel() ?? 0);

        // mode user (TargetUser): effUser = X (atau null bila D-04 lolos — middleware sudah redirect; defensif)
        if (effUser == null) return (null, 0);
        var effLevel = _impersonationService.GetEffectiveRoleLevel()
                       ?? UserRoles.GetRoleLevel((await _userManager.GetRolesAsync(effUser)).FirstOrDefault() ?? "");
        return (effUser, effLevel);
    }
    ```

    B. FIX caller `Records:481` D-03 null-handling — **Option A LOCKED (per D-03 CONTEXT.md)**. Pitfall 1: `if (user == null) return RedirectToAction("Login")` SALAH untuk mode-role.

    Aturan TUNGGAL (BUKAN pilihan executor): saat `user == null && _impersonationService.IsImpersonating() && _impersonationService.GetMode() == "role"` → render view Records dengan:
      - `CurrentUser = null` (JANGAN isi nama/identitas admin — D-03 "tidak menampilkan data admin"; menampilkan nama admin = leak identitas, DILARANG).
      - `UnifiedRecords` KOSONG (empty list, tipe sesuai `GetUnifiedRecords` return).
      - Count = 0 semua; `ViewBag.ImpersonateRoleHint = "Pilih user spesifik untuk melihat data worker."`.
    Selain itu (genuinely null, tak login) → `RedirectToAction("Login", "Account")` seperti sekarang.

    Contoh:
    ```csharp
    var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
    if (user == null)
    {
        if (_impersonationService.IsImpersonating() && _impersonationService.GetMode() == "role")
        {
            ViewBag.ImpersonateRoleHint = "Pilih user spesifik untuk melihat data worker.";
            var emptyVm = new HcPortal.Models.ViewModels.CMPRecordsViewModel {
                User = null, RoleLevel = roleLevel,
                UnifiedRecords = new List<...>(), // tipe sesuai GetUnifiedRecords return
                AssessmentCount = 0, TrainingCount = 0, TotalCount = 0,
                YearOptions = new List<int>()
            };
            ViewBag.ActualCategoriesJson = "[]";
            return View(emptyVm);
        }
        return RedirectToAction("Login", "Account");
    }
    ```
    Cek tipe `UnifiedRecords` dari `_workerDataService.GetUnifiedRecords` return type (baca signature) untuk inisialisasi list kosong yang benar.

    Jika `CMPRecordsViewModel.User` non-nullable (tak bisa diisi null tanpa ubah kontrak), pendekatan yang DIWAJIBKAN: render view dengan `Model.User = null` (jadikan property nullable bila perlu — perubahan minimal pada kontrak ViewModel, bukan layout) DAN tangani null-safe di view (lihat C). JANGAN isi dummy/admin user untuk header.

    C. NULL-GUARD Records.cshtml header — `Views/CMP/Records.cshtml`. Karena `Model.User` kini bisa null saat mode-role, setiap dereference `Model.User.<...>` di header/view WAJIB null-safe. Bila view saat ini menampilkan nama via `Model.User.FullName` (atau sejenis) tanpa guard, ubah jadi:
    ```html
    @(Model.User?.FullName ?? "—")
    ```
    Terapkan pola `Model.User?.<prop> ?? "—"` (atau default aman) ke SEMUA dereference `Model.User` di view. Tambah hint mode-role di area empty-state / atas tabel:
    ```html
    @if (ViewBag.ImpersonateRoleHint != null)
    {
        <div class="alert alert-info" role="status">@ViewBag.ImpersonateRoleHint</div>
    }
    ```
    JANGAN ubah layout dashboard lain; hanya null-guard + hint.

    D. Tambah `using` bila perlu untuk `EffectiveUserDecision` (`HcPortal.Services`).

    JANGAN ubah `IsResultsAuthorized` (L2399). JANGAN ubah caller L545/660/721/774/1733/1839/2080/3870 (otomatis terfix via resolver). JANGAN sentuh `[Authorize]` attribute (D-02).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ResultsAuthorization|FullyQualifiedName~Impersonation" --nologo 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0.
    - `GetCurrentUserRoleLevelAsync` memuat `GetEffectiveUserAsync` dan branch `UseRealUser`/`RoleModeEmpty`/`TargetUser`.
    - Records action memuat string `Pilih user spesifik` (hint D-03) DAN tetap punya path `RedirectToAction("Login"` untuk genuinely-null.
    - Records action mode-role mengeset `User = null` (TIDAK mengisi identitas/nama admin) — grep konfirmasi tak ada fallback `GetUserAsync(User)` untuk header di branch mode-role.
    - `Views/CMP/Records.cshtml` memuat `ViewBag.ImpersonateRoleHint` DAN null-guard `Model.User?.` pada dereference header.
    - git diff: `IsResultsAuthorized` TIDAK berubah; caller L545/1733/1839/2080/3870 TIDAK diubah manual.
    - `dotnet test --filter "FullyQualifiedName~ResultsAuthorization|FullyQualifiedName~Impersonation"` exits 0.
  </acceptance_criteria>
  <done>Resolver CMP impersonation-aware; Records D-03 Option A (kosong+hint, User=null, TIDAK tampilkan identitas admin); view null-safe; ownership helper utuh; test hijau.</done>
</task>

<task type="auto">
  <name>Task 2a: Route Assessment + ExportRecords bypass ke resolver</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs (L198-213 Assessment; L605-620 ExportRecords personal; resolver hasil Task 1)
    - Services/ImpersonationService.cs (API baru Plan 02)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§CMPController — Assessment:203 verbatim)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (Pattern 1 — bypass surface route ke resolver)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-AUDIT.md (Tabel 1 — Assessment:203 + ExportRecords:611 BYPASS)
  </read_first>
  <action>
    Edit `Controllers/CMPController.cs` — 2 bypass surface yang call `GetUserAsync(User)` langsung; route ke resolver effective-user (pola resolver-routing identik dengan Task 1, BUKAN auto via hulu).

    A. `Assessment:203` — ganti `var user = await _userManager.GetUserAsync(User);` jadi resolve effective user:
    ```csharp
    var (user, _) = await GetCurrentUserRoleLevelAsync();
    var userId = user?.Id ?? "";
    ```
    Bila mode-role (user null saat impersonate role) → `userId=""` → query `AssessmentSessions.UserId==""` → 0 record (D-03 konsisten, kosong bukan admin). Bila genuinely null (tak login) — action ini sudah toleran (`userId=""`), tak perlu redirect. Verifikasi tak ada NRE downstream (`user?.` di tempat lain dalam action).

    B. `ExportRecords` (personal, L611) — ganti `GetUserAsync(User)` → `(user, _) = await GetCurrentUserRoleLevelAsync()`; bila `user == null` (mode-role/null) → return file kosong / redirect dengan pesan ramah (export personal saat mode-role = data kosong, konsisten D-03). Pakai `user?.Id ?? ""` untuk `GetUnifiedRecords`.

    JANGAN sentuh team-view export (L660/721/774) — otomatis benar via resolver (level/section efektif X), AUDIT Tabel 2. JANGAN ubah `[Authorize]` (D-02). JANGAN ubah StartExam (Task 2b).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo 2>&1 | powershell -Command "$input | Select-String -Pattern 'Passed!|Failed!|error'"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0.
    - `CMPController.cs` Assessment action TIDAK lagi memanggil `_userManager.GetUserAsync(User)` langsung di L~203 (diganti `GetCurrentUserRoleLevelAsync()`); grep konfirmasi.
    - `CMPController.cs` ExportRecords (personal) memakai `GetCurrentUserRoleLevelAsync()` + `user?.Id ?? ""` (bukan `GetUserAsync(User)` langsung); grep konfirmasi.
    - Full xUnit suite hijau (`dotnet test` exits 0) — SC4 no regression.
  </acceptance_criteria>
  <done>Assessment + ExportRecords routed ke effective user (mode-role → kosong, bukan admin); suite hijau.</done>
</task>

<task type="auto">
  <name>Task 2b: StartExam write-on-GET guard (Pitfall 3) + Records.cshtml hint render</name>
  <files>Controllers/CMPController.cs, Views/CMP/Records.cshtml</files>
  <read_first>
    - Controllers/CMPController.cs (L860-886 StartExam — authz + write-on-GET auto-transition L872-878)
    - Views/CMP/Records.cshtml (empty-state / tabel UnifiedRecords untuk konfirmasi hint UI dari Task 1C terpasang)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§CMPController — StartExam:867 verbatim + Pitfall 3)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (Pitfall 3 write-on-GET; resolved Q2 — guard SaveChangesAsync)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-AUDIT.md (Tabel 1 — StartExam:867 BYPASS + write-on-GET)
  </read_first>
  <action>
    Edit `Controllers/CMPController.cs` (StartExam) + verifikasi `Views/CMP/Records.cshtml` hint.

    A. `StartExam:867` — DUA perubahan:
       1. Route authz ke effective user: ganti `var user = await _userManager.GetUserAsync(User);` → `var (user, _) = await GetCurrentUserRoleLevelAsync();` (pertahankan `if (user == null) return Challenge();` — untuk mode-role/genuinely-null, Challenge aman; assessment X bukan milik role-preview).
       2. GUARD write-on-GET (Pitfall 3 / resolved Q2 / threat T-377-09): bungkus auto-transition Save (L872-878) agar TIDAK menulis saat impersonasi:
       ```csharp
       if (assessment.Status == "Upcoming" && assessment.Schedule <= DateTime.UtcNow.AddHours(7))
       {
           if (!_impersonationService.IsImpersonating())   // read-only invariant (Pitfall 3 / resolved Q2)
           {
               assessment.Status = "Open";
               assessment.UpdatedAt = DateTime.UtcNow;
               await _context.SaveChangesAsync();
           }
       }
       ```
       Render tetap berjalan (read-only preview), hanya Save di-skip saat impersonasi. JANGAN ubah authz check `assessment.UserId != user.Id && !User.IsInRole("Admin") && !User.IsInRole("HC")` (catatan: `User.IsInRole` = principal admin asli — pre-existing, OUT D-02; effective user.Id memengaruhi cabang owner saja).

    B. `Views/CMP/Records.cshtml` — konfirmasi/lengkapi render hint mode-role (di-set Task 1). Pastikan area empty-state / atas tabel UnifiedRecords memuat:
    ```html
    @if (ViewBag.ImpersonateRoleHint != null)
    {
        <div class="alert alert-info" role="status">@ViewBag.ImpersonateRoleHint</div>
    }
    ```
    (Bila Task 1C sudah menambahkannya, cukup verifikasi ada + grep-able. Jangan duplikasi blok.)
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -clp:ErrorsOnly 2>&1; dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo 2>&1 | powershell -Command "$input | Select-String -Pattern 'Passed!|Failed!|error'"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` exits 0.
    - `CMPController.cs` StartExam memuat `if (!_impersonationService.IsImpersonating())` MEMBUNGKUS `SaveChangesAsync()` auto-transition (write-on-GET guarded); grep konfirmasi.
    - `CMPController.cs` StartExam authz pakai `GetCurrentUserRoleLevelAsync()` (bukan `GetUserAsync(User)` langsung); grep konfirmasi.
    - `Views/CMP/Records.cshtml` memuat `ViewBag.ImpersonateRoleHint`.
    - Full xUnit suite hijau (`dotnet test` exits 0) — SC4 no regression.
  </acceptance_criteria>
  <done>StartExam read-only saat impersonasi (Save di-skip) + authz routed; Records hint tampil; suite hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET request (impersonate) → CMP read surfaces | Surface ini membaca worker-data; harus pakai effective X, bukan admin (IDOR bila salah). |
| GET StartExam → DB write (auto-transition) | Write tersembunyi di GET; saat read-only impersonation = tampering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-377-08 | Information Disclosure / IDOR | Records/Assessment/Results pakai admin.Id → admin lihat datanya sendiri saat "impersonate X" (akar bug 999.6) | mitigate (block: high) | Resolver hulu → user.Id=X.Id; ownership via `IsResultsAuthorized(currentUserId=X.Id)`. SC2 e2e (Plan 06) membuktikan. |
| T-377-09 | Tampering | StartExam auto-transition `SaveChangesAsync` menulis DB X saat impersonasi (read-only) | mitigate (block: high) | Guard `if(!IsImpersonating())` sebelum Save (Task 2b, Pitfall 3/Q2). |
| T-377-10 | Information Disclosure | Mode-role di-redirect ke Login atau fallback admin (data/nama) di Records | mitigate | D-03 Option A: user-null+mode-role → render kosong + hint, User=null (Task 1B); bukan redirect, bukan admin (data maupun identitas header). |
| T-377-11 | Privilege confusion (V4 full-fidelity) | Impersonate Coachee X lihat sertifikat worker lain (Y) | mitigate | `IsResultsAuthorized` pakai roleLevel+section efektif X → owner-only behavior X (D-01). Test matrix di Plan 06. |
| T-377-12 | Regression (SC4) | Caller non-impersonate berubah perilaku | mitigate | Resolver branch `UseRealUser` identik hari ini; full suite hijau acceptance. |
</threat_model>

<verification>
- Build 0 error; full xUnit suite hijau (SC4).
- Records: string `Pilih user spesifik` + path redirect Login terpisah (D-03 vs genuinely-null) + `Model.User?.` null-guard di view.
- StartExam: `if (!_impersonationService.IsImpersonating())` membungkus `SaveChangesAsync` (Pitfall 3).
- Assessment + ExportRecords + StartExam tak lagi `GetUserAsync(User)` langsung.
</verification>

<success_criteria>
- SC2 path siap (impersonate X → /CMP/Records data X) — divalidasi e2e Plan 06.
- 3 bypass surface routed; StartExam read-only; D-03 hint + identitas admin tidak bocor.
- SC4 no regression (suite hijau).
</success_criteria>

<output>
After completion, create `.planning/phases/377-impersonation-identity-across-surfaces/377-03-SUMMARY.md`.
Catat: perilaku ExportRecords saat mode-role, keputusan view null-guard Records (Option A), hasil suite, catatan StartExam guard untuk UAT.
</output>
</output>
