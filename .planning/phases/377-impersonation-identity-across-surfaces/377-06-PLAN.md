---
phase: 377-impersonation-identity-across-surfaces
plan: 06
type: execute
wave: 3
depends_on: [03, 04, 05]
files_modified:
  - tests/e2e/impersonation.spec.ts
  - HcPortal.Tests/ResultsAuthorizationTests.cs
  - .planning/seeds/377-impersonation-fixtures.sql
autonomous: false
requirements: [IMP-01, IMP-02]
tags: [impersonation, identity, e2e, integration, verification, fidelity]

must_haves:
  truths:
    - "e2e: impersonate user X → /CMP/Records tampil data X (assessment+training), bukan admin (SC2 live proof)"
    - "e2e: impersonate user X → /Home + /CMP/Assessment resolve X (SC3)"
    - "ResultsAuthorizationTests punya kasus impersonate-X fidelity (D-01): X lihat data-X OK, X lihat data-Y (non-owner, level X kurang) Forbid"
    - "Full xUnit suite + e2e impersonation.spec hijau (--workers=1) sebelum phase gate (SC4)"
  artifacts:
    - path: "tests/e2e/impersonation.spec.ts"
      provides: "SC2/SC3 assertion: impersonate X → Records/Home/Assessment = data X"
      contains: "/CMP/Records"
    - path: "HcPortal.Tests/ResultsAuthorizationTests.cs"
      provides: "Impersonate fidelity matrix (D-01 ownership saat currentUserId=X)"
      contains: "u1"
    - path: ".planning/seeds/377-impersonation-fixtures.sql"
      provides: "Seed deterministik user X + ≥1 assessment + ≥1 training (temporary local-only)"
      contains: "AssessmentSessions"
  key_links:
    - from: "tests/e2e/impersonation.spec.ts"
      to: "/CMP/Records (impersonate X)"
      via: "page.goto setelah impersonate → assert tabel berisi record X"
      pattern: "page\\.goto\\('/CMP/Records'\\)"
---

<objective>
Integration & verification wave (gate SC2/SC3/SC4). Finalize e2e SC2/SC3 (extend IMP-02 flow existing → impersonate X lalu assert `/CMP/Records`, `/Home`, `/CMP/Assessment` tampil data X). Tambah ResultsAuthorization impersonate-fidelity matrix (D-01). Buat seed deterministik (temporary local-only, SEED_WORKFLOW). Jalankan full regression gate (xUnit + e2e `--workers=1`) lalu checkpoint UAT browser.

Purpose: SC2 (akar bug 999.6) hanya terbukti via e2e end-to-end dengan data nyata. SC4 no-regression dibuktikan suite penuh. Checkpoint human verifikasi banner jujur lintas surface (visual).
Output: e2e SC2/SC3 GREEN, fidelity matrix, seed, suite penuh hijau, UAT approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-CONTEXT.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-VALIDATION.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-03-SUMMARY.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-04-SUMMARY.md
@.planning/phases/377-impersonation-identity-across-surfaces/377-05-SUMMARY.md

# Analog e2e (self) + seed workflow:
@tests/e2e/impersonation.spec.ts
@HcPortal.Tests/ResultsAuthorizationTests.cs
# docs/SEED_WORKFLOW.md (klasifikasi temporary+local-only, snapshot/restore, journal)

<interfaces>
<!-- e2e IMP-02 flow EXISTING (impersonation.spec.ts:240-281) yang DI-EXTEND: -->
<!-- login admin → GET /Admin/SearchUsersApi?q=<name> → autocomplete #impersonate-search → -->
<!-- click #impersonate-results [data-userid] → waitForURL Home → banner #impersonation-banner contains fullName. -->
<!-- EXTEND: setelah banner verified, page.goto('/CMP/Records') + assert data X. -->

<!-- Gotcha e2e (CLAUDE.md/STATE.md — WAJIB): -->
<!-- test.describe.configure({ mode: 'serial' }) sudah di L4; run combined WAJIB --workers=1; -->
<!-- start SQLBrowser + lpc: shared-memory conn override; AD lokal: Authentication__UseActiveDirectory=false dotnet run. -->

<!-- IsResultsAuthorized signature (pure static, untuk fidelity matrix): -->
public static bool IsResultsAuthorized(string? ownerUserId, string currentUserId, int roleLevel, string? currentUserSection, string? ownerSection);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed deterministik + ResultsAuthorization impersonate-fidelity matrix</name>
  <files>.planning/seeds/377-impersonation-fixtures.sql, HcPortal.Tests/ResultsAuthorizationTests.cs, docs/SEED_JOURNAL.md</files>
  <read_first>
    - HcPortal.Tests/ResultsAuthorizationTests.cs (matrix existing — extend, JANGAN hapus baris lama)
    - docs/SEED_WORKFLOW.md (klasifikasi temporary+local-only, snapshot BACKUP/RESTORE, journal format)
    - docs/SEED_JOURNAL.md (format entri)
    - Data/SeedData.cs (cari user existing yang bisa jadi target X, mis. 'rino'/Iwan; atau model AssessmentSessions/TrainingRecords untuk seed)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (§Validation Test Seam #3 + #4; Pitfall 4 fidelity)
  </read_first>
  <action>
    A. Buat `.planning/seeds/377-impersonation-fixtures.sql` — seed temporary local-only (SEED_WORKFLOW) untuk e2e: user X deterministik (reuse user existing bila ada, mis. yang muncul di `SearchUsersApi?q=rino`/Iwan — cek Data/SeedData.cs) dengan ≥1 `AssessmentSessions` (UserId=X, Status="Completed" agar muncul di Records sebagai "Assessment Online") + ≥1 `TrainingRecords` (UserId=X) yang DISTINCT dari data admin. Sertakan header komentar: tujuan (e2e SC2 impersonate X→Records=X), klasifikasi `temporary + local-only`, daftar tabel tersentuh, + cleanup/DELETE chain FK-respecting di bagian bawah (atau instruksikan snapshot/restore). Catat entri di `docs/SEED_JOURNAL.md` (tujuan, klasifikasi, entitas, status `pending cleanup`).

    CATATAN: Bila user X existing SUDAH punya assessment+training nyata di DB lokal (Iwan punya 3 TrainingRecords per ROADMAP 999.6), seed bisa minimal/optional — verifikasi dulu via query. Jangan duplikasi data yang sudah ada. Prefer reuse > insert.

    B. Extend `HcPortal.Tests/ResultsAuthorizationTests.cs` dengan kasus impersonate-fidelity (D-01) — TAMBAH `[InlineData]` ke matrix existing (atau method `[Theory]` baru `IsResultsAuthorized_ImpersonateFidelity`):
       - Impersonate X (L6 Coachee) lihat data-X (owner): `("X","X",6,"A","A", true)` — owner self OK.
       - Impersonate X (L6) lihat data-Y (non-owner): `("Y","X",6,"A","A", false)` — Coachee X tak boleh lihat orang lain (D-01 fidelity).
       - Impersonate X (L4 SectionHead) lihat data-Y same section: `("Y","X",4,"A","A", true)`.
       - Impersonate X (L4) lihat data-Y beda section: `("Y","X",4,"A","B", false)`.
       Komentar tiap baris: "impersonate X = act as X for read (D-01); ownership pakai currentUserId=X.Id, roleLevel=X.level". JANGAN hapus/ubah InlineData lama (T-346-*).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ResultsAuthorization" --nologo 2>&1; powershell -Command "Test-Path '.planning/seeds/377-impersonation-fixtures.sql'"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/seeds/377-impersonation-fixtures.sql` ada; memuat `AssessmentSessions` + `TrainingRecords` + header klasifikasi `temporary` + cleanup chain.
    - `docs/SEED_JOURNAL.md` punya entri Phase 377 (status pending cleanup).
    - `ResultsAuthorizationTests.cs` memuat ≥4 InlineData fidelity baru (owner-OK, non-owner-Forbid, L4-same-section, L4-diff-section) + InlineData lama T-346-* UTUH.
    - `dotnet test --filter "FullyQualifiedName~ResultsAuthorization"` exits 0 (semua pass).
  </acceptance_criteria>
  <done>Seed deterministik (atau reuse terverifikasi) + journal; fidelity matrix D-01 hijau; matrix lama utuh.</done>
</task>

<task type="auto">
  <name>Task 2: Extend e2e impersonation.spec SC2/SC3 + full regression gate</name>
  <files>tests/e2e/impersonation.spec.ts</files>
  <read_first>
    - tests/e2e/impersonation.spec.ts (FULL — IMP-02 flow L240-281; pola login/SearchUsersApi/autocomplete/banner; serial config L4)
    - tests/helpers/auth.ts (helper login)
    - .planning/seeds/377-impersonation-fixtures.sql (data X dari Task 1)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-RESEARCH.md (§Environment Availability + §Validation seam #4 e2e; gotcha SQLBrowser/lpc:/workers=1/AD=false)
    - .planning/phases/377-impersonation-identity-across-surfaces/377-PATTERNS.md (§impersonation.spec.ts — pola re-use + EXTEND SC2/SC3)
  </read_first>
  <action>
    A. Edit `tests/e2e/impersonation.spec.ts` — tambah test baru (dalam `test.describe('Impersonation — Admin Features')`, serial), nama eksplisit untuk filter:
       - `'IMP-377-SC2: impersonate user shows X data in /CMP/Records (not admin)'`:
         1. login admin; resolve target X via `GET /Admin/SearchUsersApi?q=<X-name>` (pakai nama user X dari seed/Task1).
         2. impersonate X (pola L254-271: autocomplete → click [data-userid] → waitForURL Home → banner contains X.fullName).
         3. `await page.goto('/CMP/Records');`
         4. Assert tabel Records berisi record X: minimal `await expect(page.locator('table')).toContainText(<judul assessment / kategori X yang diketahui dari seed>)` ATAU assert count badge Training > 0 (X punya training, admin tidak) — pilih assertion deterministik dari data seed. Assert TIDAK memuat marker data admin (mis. judul assessment admin Id 157/66 dari 999.6) bila diketahui.
         5. cleanup: stop impersonation (banner submit, waitForURL Admin).
       - `'IMP-377-SC3: impersonate user resolves X in /Home and /CMP/Assessment'`:
         impersonate X → `page.goto('/Home/Index')` assert progress/greeting reflect X (mis. nama X di dashboard atau progress non-admin) → `page.goto('/CMP/Assessment')` assert daftar assessment milik X (atau kosong-konsisten bila X tak punya open assessment). Assertion deterministik berdasar seed.
       - (opsional, bila waktu) `'IMP-377-D03: impersonate ROLE shows empty + hint in /CMP/Records'`: impersonate role HC (pola IMP-01) → `page.goto('/CMP/Records')` → assert text `Pilih user spesifik` (hint D-03) DAN tabel kosong DAN bukan redirect ke Login.

       Pertahankan `test.describe.configure({ mode: 'serial' })`. Tambah cleanup stop-impersonation tiap test (hindari kontaminasi sesi).

    B. Jalankan FULL regression gate (PHASE GATE):
       1. `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` — full xUnit hijau (SC4).
       2. Setup e2e lokal: start SQLBrowser, jalankan app `Authentication__UseActiveDirectory=false dotnet run` (login lokal non-AD), gunakan `lpc:` shared-memory conn override bila perlu. Apply seed Task 1 (snapshot DB dulu).
       3. `npx playwright test tests/e2e/impersonation.spec.ts --workers=1` — hijau (SC2/SC3).
       4. Restore DB dari snapshot (cleanup seed) + tandai SEED_JOURNAL `cleaned`.

    Bila e2e tak bisa dijalankan otomatis di env ini (SQLBrowser/AD), dokumentasikan langkah manual lengkap di SUMMARY + tandai e2e sebagai checkpoint UAT (Task 3) — JANGAN klaim hijau tanpa run.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --nologo 2>&1 | powershell -Command "$input | Select-String -Pattern 'Passed!|Failed!'"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/impersonation.spec.ts` memuat test `IMP-377-SC2` dengan `page.goto('/CMP/Records')` + assertion data X.
    - Memuat test `IMP-377-SC3` dengan `/Home` + `/CMP/Assessment` navigation.
    - Full xUnit suite hijau (`dotnet test` exits 0) — SC4 no regression (semua test existing + ImpersonationIdentity + ResultsAuthorization fidelity).
    - e2e `npx playwright test tests/e2e/impersonation.spec.ts --workers=1` hijau ATAU (bila env block) langkah manual terdokumentasi + di-route ke checkpoint Task 3 (tidak diklaim hijau palsu).
    - SEED_JOURNAL ditandai `cleaned` setelah run (DB restored).
  </acceptance_criteria>
  <done>e2e SC2/SC3 ada + hijau (atau terdokumentasi untuk UAT); full xUnit hijau; DB lokal restored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT browser — banner jujur lintas surface (IMP-01)</name>
  <files>(none — verifikasi manual browser; tidak ada file diubah)</files>
  <action>Checkpoint human-verify (tidak ada otomasi tambahan — semua kode sudah selesai Plan 03-05 + Task 1-2). Lihat what-built / how-to-verify / resume-signal di bawah. JANGAN tulis kode di task ini.</action>
  <what-built>
    Resolver effective-user single-source (Plan 02) + CMP/CDP/Home impersonation-aware (Plan 03-05) + e2e SC2/SC3 (Task 2). Saat impersonate user X, surface worker-data resolve identitas X (data, ownership, scope ikut X — D-01 full-fidelity), banner "Anda melihat sebagai X" jadi jujur. Mode-role → kosong+hint (D-03). Target hilang → Stop+redirect (D-04). StartExam read-only (Pitfall 3).
  </what-built>
  <how-to-verify>
    Lokal (CLAUDE.md: verifikasi lokal wajib sebelum commit). Start app: `Authentication__UseActiveDirectory=false dotnet run` → http://localhost:5277. Login admin@pertamina.com.
    1. Impersonate user X spesifik (dropdown navbar → autocomplete cari user dengan assessment+training, mis. Iwan/rino). Banner merah "Anda melihat sebagai X" muncul.
    2. Buka /CMP/Records → KONFIRMASI tabel berisi assessment + training MILIK X (bukan admin). Badge Training count > 0 bila X punya training. (Akar bug 999.6: dulu tampil 2 assessment admin Id 157/66 + Training 0 — sekarang harus data X.)
    3. Buka /Home → progress + upcoming events = data X (bukan admin).
    4. Buka /CMP/Assessment → daftar assessment X (atau kosong konsisten bila X tak ada open).
    5. (D-03) Stop, lalu impersonate ROLE HC (bukan user). Buka /CMP/Records → tampil KOSONG + hint "Pilih user spesifik untuk melihat data worker" (BUKAN redirect Login, BUKAN data admin).
    6. (Pitfall 3) Impersonate X, buka StartExam untuk assessment X berstatus Upcoming yang waktunya sudah tiba → halaman render TAPI status di DB TIDAK berubah (cek DB lokal `SELECT Status FROM AssessmentSessions WHERE Id=<id>` tetap Upcoming). Saat login normal (tanpa impersonate), auto-transition Upcoming→Open tetap jalan.
    7. (SC4) Stop impersonasi, login normal sebagai user biasa → /CMP/Records + /Home tampil data sendiri (tak ada regresi).
  </how-to-verify>
  <verify>Manual browser UAT (lihat how-to-verify). Tidak ada automated command — checkpoint blocking.</verify>
  <done>User mengetik "approved" setelah semua 7 langkah verifikasi lulus (banner jujur, D-03, StartExam read-only, SC4 normal).</done>
  <resume-signal>Ketik "approved" bila banner jujur di semua surface + D-03 kosong+hint + StartExam read-only + SC4 normal OK; atau jelaskan surface yang masih bocor/regresi.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e seed (temporary) → DB lokal | Seed harus restored (jangan nempel — CLAUDE.md SEED_WORKFLOW). |
| UAT manual → integrity check | Verifikasi visual = lapisan terakhir (banner jujur, data X, read-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-377-22 | Information Disclosure (verification) | Fix diklaim selesai tapi surface masih bocor data admin (uji lemah) | mitigate (block: high) | e2e SC2/SC3 assert data X + UAT manual lintas surface (Task 2/3). |
| T-377-23 | Privilege confusion (V4) | Fidelity D-01 salah (X lihat data Y) tak teruji | mitigate | ResultsAuthorization fidelity matrix (Task 1B) + UAT step 5. |
| T-377-24 | Tampering (StartExam) | Write-on-GET masih menulis saat impersonasi | mitigate | UAT step 6 cek Status DB tetap Upcoming saat impersonate. |
| T-377-25 | Data hygiene | Seed temporary nempel di DB lokal | accept→mitigate | Snapshot+restore + SEED_JOURNAL `cleaned` (Task 2B). |
</threat_model>

<verification>
- e2e SC2/SC3 ada + hijau (`--workers=1`) atau terdokumentasi untuk UAT.
- Full xUnit suite hijau (SC4) — termasuk ImpersonationIdentity + ResultsAuthorization fidelity.
- Seed restored + journal cleaned.
- UAT approved (banner jujur, D-03, StartExam read-only, SC4 normal).
</verification>

<success_criteria>
- SC2 terbukti live (impersonate X → /CMP/Records = data X).
- SC3 terbukti (Home + Assessment resolve X).
- SC4 terbukti (suite penuh + UAT normal tak regresi).
- D-01 fidelity + D-03 + Pitfall 3 ter-verify.
</success_criteria>

<output>
After completion, create `.planning/phases/377-impersonation-identity-across-surfaces/377-06-SUMMARY.md`.
Catat: hasil e2e (atau langkah manual UAT), hasil full suite, status seed cleanup, hasil UAT checkpoint, sisa borderline (DokumenKkj/CertificationManagement) bila ada flag follow-up.
</output>
