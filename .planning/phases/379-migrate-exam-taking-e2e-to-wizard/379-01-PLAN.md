---
phase: 379-migrate-exam-taking-e2e-to-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/helpers/examTypes.ts
  - tests/e2e/helpers/wizardSelectors.ts
  - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [E2E-01]
tags: [e2e, playwright, test-infra, wizard, helpers]

must_haves:
  truths:
    - "Helper extension token/proton tersedia (additive) tanpa mengubah signature existing"
    - "Helper paste-import importQuestionsViaPaste tersedia bila route valid"
    - "3 Open Question Wave-0 (ProtonTrack T3, paste route, examExpiredModal) ter-verifikasi & terdokumentasi"
    - "ProtonTrack Tahun 3 tersedia di DB lokal (existing atau seed temporary local-only)"
    - "Smoke 1 flow (createAssessmentViaWizard + createDefaultPackage + addQuestionViaForm) hijau membuktikan tooling+env"
  artifacts:
    - path: "tests/e2e/helpers/examTypes.ts"
      provides: "CreateAssessmentOpts +isTokenRequired/+accessToken/+protonTrackId/+protonTrackTahun; blok kondisional token (STEP 3) + proton (STEP 1); importQuestionsViaPaste"
      contains: "isTokenRequired"
    - path: "tests/e2e/helpers/wizardSelectors.ts"
      provides: "selector additive tokenSection/protonFieldsSection/protonTrackSelect"
      contains: "tokenSection"
    - path: ".planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md"
      provides: "hasil verifikasi 3 Open Q + keputusan fallback per flow"
      min_lines: 20
  key_links:
    - from: "examTypes.ts createAssessmentViaWizard"
      to: "wizardSelectors.isTokenRequired / #tokenSection"
      via: "blok kondisional STEP 3 opts.isTokenRequired"
      pattern: "opts\\.isTokenRequired"
    - from: "examTypes.ts createAssessmentViaWizard"
      to: "#protonFieldsSection / #protonTrackSelect"
      via: "blok kondisional STEP 1 opts.protonTrackTahun"
      pattern: "opts\\.protonTrackTahun"
---

<objective>
Wave-0 prep: extend helper `examTypes.ts` + `wizardSelectors.ts` secara ADDITIVE (D-04 — JANGAN refactor signature existing) untuk gap token (Flow B), proton (Flow E), paste-import (Flow D3); verifikasi 3 Open Question (ProtonTrack Tahun 3, paste route, `#examExpiredModal`); pastikan ProtonTrack Tahun 3 ada di DB lokal (seed temporary bila perlu, SEED_WORKFLOW); jalankan 1 smoke flow untuk membuktikan tooling + env hijau SEBELUM migrasi massal.

Purpose: Fase 379 = migrasi TEST-INFRA murni. Plan ini menyiapkan SEMUA fondasi helper + memvalidasi asumsi RESEARCH (Open Q1/Q2/Q3) supaya plan-plan migrasi flow berikutnya (02-05) tinggal panggil helper tanpa scavenger-hunt. Gate ini WAJIB selesai dulu karena seluruh flow migration bergantung pada helper extension + ketersediaan data Proton T3.
Output: helper ter-extend + `379-W0-VERIFY.md` (hasil 3 verifikasi + keputusan fallback) + (opsional) journal seed + bukti smoke hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-CONTEXT.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md

<interfaces>
<!-- Kontrak helper existing — executor pakai langsung, JANGAN ubah signature (D-04). -->

From tests/e2e/helpers/examTypes.ts (existing, verbatim):
```typescript
export interface CreateAssessmentOpts {
  title: string;
  category: string;            // value option #Category, e.g. 'OJT','IHT','Assessment Proton'
  scheduleDate: string;        // YYYY-MM-DD
  scheduleTime?: string;       // HH:mm, default '00:01'
  durationMinutes: number;
  passPercentage: number;
  allowAnswerReview: boolean;
  generateCertificate?: boolean;
  participantEmails: string[];
  ewcdDate?: string;
  ewcdTime?: string;
}
export async function createAssessmentViaWizard(page: Page, opts: CreateAssessmentOpts): Promise<void>;
export async function createDefaultPackage(page: Page, packageName?: string): Promise<number>; // returns packageId
export async function addQuestionViaForm(page: Page, packageId: number, q: QuestionInput, images?): Promise<void>;
```

From tests/e2e/helpers/wizardSelectors.ts (existing, L67-68 — SUDAH ADA, pakai langsung):
```typescript
  isTokenRequired: '#IsTokenRequired',
  accessToken: '#AccessToken',
  validUntil: '#ValidUntil',
```

From tests/helpers/dbSnapshot.ts (existing):
```typescript
export function queryScalar(sql: string): Promise<number>;   // localhost-only guard
export function queryString(sql: string): Promise<string>;
```

createAssessmentViaWizard internal flow (verified examTypes.ts:51-118):
- STEP 1 (L54-58): goto /Admin/CreateAssessment → selectOption(category) → fill(title) → btnNext1.click()  ← titik sisip PROTON
- STEP 2 (L60-72): pilih participant
- STEP 3 (L84-96): schedule/duration/passPercentage fill  ← titik sisip TOKEN (setelah passPercentage)
- STEP 4: review + btnSubmit + successModal (data-bs-backdrop=static, helper handle dismiss)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verifikasi 3 Open Question (W0) + materialize 379-W0-VERIFY.md</name>
  <files>.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md</files>
  <read_first>
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md §Open Questions + §Assumptions Log (A1/A3) + §Wave 0 Gaps
    - tests/helpers/dbSnapshot.ts (queryScalar signature + localhost guard)
    - Views/Admin/CreateAssessment.cshtml (token markup ~500-518; proton ~210-235) — konfirmasi `#tokenSection` + `#protonTrackSelect`
    - Views/CMP/StartExam.cshtml — cari `#examExpiredModal` (Open Q3)
    - tests/e2e/exam-taking.spec.ts:599-621 (Flow D3 paste-import — `textarea[name="pasteText"]`)
  </read_first>
  <action>
    Prasyarat env (CLAUDE.md / D-03): pastikan app lokal running `Authentication__UseActiveDirectory=false dotnet run` (@localhost:5277), SQLBrowser running, SQL `localhost\SQLEXPRESS` + `HcPortalDB_Dev` up. Bila login 500 error 53 → conn `lpc:` shared-memory override (lihat memory reference_local_e2e_sql_env_fix).

    Lakukan 3 verifikasi dan tulis hasilnya ke `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md`:

    (W0-1 ProtonTrack Tahun 3 — Open Q1) Jalankan via sqlcmd/dbSnapshot:
      `SELECT COUNT(*) FROM ProtonTracks WHERE TahunKe='Tahun 3'`
    Catat angka. Jika >=1 → Flow E pakai track itu (pilih by `data-tahun='Tahun 3'`), TIDAK perlu seed. Jika 0 → tandai "seed required" (ditangani Task 2).

    (W0-2 paste-import route — Open Q2) Buka manual via browser/curl ATAU baca markup: konfirmasi dari ManagePackages ada link `a:has-text("Import Questions")` → halaman dengan `textarea[name="pasteText"]` + tombol `button:has-text("Import")` masih ada. Catat: VALID (pakai helper paste) atau DRIFT (fallback addQuestionViaForm×3 untuk Flow D3).

    (W0-3 `#examExpiredModal` — Open Q3/A3) Grep `Views/CMP/StartExam.cshtml` untuk `examExpiredModal` dan untuk variabel JS `timerStartRemaining`. Catat: ADA (Flow G pakai waitForFunction modal/remaining) atau ABSEN (Flow G fallback DB-state assert Status='Completed'/'Abandoned' atau URL→Results).

    Format `379-W0-VERIFY.md`: tabel `| W0 item | Query/Check | Hasil | Keputusan fallback |` + 1 paragraf ringkas keputusan per flow (B token selector, E proton seed-or-existing, D3 paste-or-form, G expiry mechanism).

    JANGAN ubah kode produksi. Bila ada anomali (mis. route 500), catat sebagai temuan backlog di file (jangan fix inline — scope test-infra only, CONTEXT D).
  </action>
  <acceptance_criteria>
    - File `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md` ada, berisi tabel 3 W0 item terisi (hasil + keputusan), min 20 baris.
    - W0-1: angka `COUNT(*) ProtonTracks WHERE TahunKe='Tahun 3'` tercatat eksplisit.
    - W0-2: status paste-import route tercatat (VALID/DRIFT + selector aktual).
    - W0-3: status `#examExpiredModal` tercatat (ADA/ABSEN + mekanisme Flow G yang dipilih).
    - 0 perubahan file di Controllers/ atau Views/ (`git status --porcelain Controllers/ Views/` kosong).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md';const s=fs.readFileSync(p,'utf8');if(!/Tahun 3/.test(s)||!/pasteText|Import/i.test(s)||!/examExpiredModal/i.test(s)){console.error('W0-VERIFY incomplete');process.exit(1)}console.log('W0-VERIFY OK')"</automated>
  </verify>
  <done>379-W0-VERIFY.md menjawab 3 Open Question dengan hasil konkret + keputusan fallback; tidak ada perubahan kode produksi.</done>
</task>

<task type="auto">
  <name>Task 2: Pastikan ProtonTrack Tahun 3 di DB lokal (seed temporary bila 0, SEED_WORKFLOW)</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md (hasil W0-1 dari Task 1)
    - CLAUDE.md §Seed Data Workflow + docs/SEED_WORKFLOW.md (klasifikasi, snapshot, journal, restore)
    - docs/SEED_JOURNAL.md (format entry existing)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md §Open Questions Q1 (rekomendasi seed minimal / SeedProtonTracksAsync idempotent)
  </read_first>
  <action>
    CONDITIONAL: hanya jalankan bila W0-1 (Task 1) = 0. Jika sudah >=1, SKIP seed; cukup catat di journal "ProtonTrack Tahun 3 sudah ada, tidak seed" dan tandai task done.

    Bila 0: seed minimal ProtonTrack Tahun 3 sebagai **temporary + local-only** (klasifikasi SEED_WORKFLOW — ini untuk reproduce/test, BUKAN prod-required → jangan masuk Data/SeedData.cs):
    1. Snapshot DB lokal dulu: `sqlcmd -S localhost\SQLEXPRESS -d HcPortalDB_Dev -E -I -Q "BACKUP DATABASE HcPortalDB_Dev TO DISK='C:\Temp\HcPortalDB_Dev_pre379seed.bak' WITH INIT"` (path & syntax sesuai SEED_WORKFLOW; flag `-I` wajib untuk quoted identifier).
    2. INSERT 1 baris ProtonTrack dengan `TahunKe='Tahun 3'` aktif (kolom minimal sesuai schema ProtonTracks; gunakan pola SeedProtonTracksAsync idempotent bila ada — lihat memory project_protondata_planidp_dev_orgdata_bug, SeedProtonTracksAsync idempotent). Verifikasi: `SELECT COUNT(*) FROM ProtonTracks WHERE TahunKe='Tahun 3'` >= 1.
    3. Catat entry di `docs/SEED_JOURNAL.md`: tujuan ("Flow E e2e Proton T3 interview"), klasifikasi (temporary+local-only), entitas tersentuh (ProtonTracks), status `active`, snapshot path.

    CATATAN: global.teardown `db.restore()` me-restore DB penuh pasca-suite (safety net). Tapi restore manual journal tetap WAJIB ditandai setelah Plan 06 selesai (cleanup lifecycle). Karena seed ini diperlukan SAAT run flow E (Plan 03), JANGAN restore di plan ini — biarkan active sampai Plan 06 final gate, lalu restore + tandai journal `cleaned` di Plan 06.

    JANGAN sentuh DB Dev/Prod (CLAUDE.md) — localhost saja.
  </action>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM ProtonTracks WHERE TahunKe='Tahun 3'` di DB lokal >= 1 (verifiable via sqlcmd/queryScalar).
    - `docs/SEED_JOURNAL.md` punya entry baru untuk seed ini (atau catatan "sudah ada, tidak seed") dengan klasifikasi + status.
    - Bila seed dilakukan: snapshot .bak tercatat di journal.
    - 0 perubahan Data/SeedData.cs (seed temporary TIDAK dipromosikan).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/SEED_JOURNAL.md','utf8');if(!/Tahun 3|379|ProtonTrack/i.test(s)){console.error('SEED_JOURNAL missing 379 ProtonTrack entry');process.exit(1)}console.log('SEED_JOURNAL OK')"</automated>
  </verify>
  <done>DB lokal punya ProtonTrack Tahun 3 (existing atau seeded), journal tercatat, Data/SeedData.cs tak tersentuh, DB Dev/Prod tak disentuh.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend helper examTypes.ts + wizardSelectors.ts (additive) + smoke 1 flow hijau</name>
  <files>tests/e2e/helpers/examTypes.ts, tests/e2e/helpers/wizardSelectors.ts</files>
  <read_first>
    - tests/e2e/helpers/examTypes.ts:1-118 (header citation style + CreateAssessmentOpts:23-35 + createAssessmentViaWizard:51-118 titik sisip STEP1/STEP3)
    - tests/e2e/helpers/examTypes.ts:511-613 (Phase 318 PrePost block) + :632 (verifyCertificatePdfDownload) — POLA extend additive di file yang sama
    - tests/e2e/helpers/wizardSelectors.ts:33 (header "extend additive — JANGAN refactor existing") + :67-69 (isTokenRequired/accessToken existing)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"examTypes.ts — extend additive" + §"wizardSelectors.ts — extend additive" + Shared Patterns (Token/Proton Extension)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md (keputusan W0-2 paste route: helper paste vs fallback; W0-1 proton track tahun)
    - Views/Admin/CreateAssessment.cshtml (token toggle JS ~1322-1385; proton fields ~210-235, JS show ~1322-1356)
  </read_first>
  <action>
    Semua perubahan ADDITIVE — JANGAN ubah param order/signature fungsi existing (D-04 preserve blame). Tambah header komentar `// Phase 379 — ...` dengan source citation seperti blok Phase 318/319 existing.

    (3a) `tests/e2e/helpers/wizardSelectors.ts` — tambah ke object `wizardSelectors` (as const) selector additive yang BELUM ada (jangan ubah key existing):
    ```typescript
    tokenSection: '#tokenSection',              // markup current (gantikan #tokenInputContainer drift)
    protonFieldsSection: '#protonFieldsSection',
    protonTrackSelect: '#protonTrackSelect',     // name=ProtonTrackId; opsi punya data-tahun
    ```

    (3b) `tests/e2e/helpers/examTypes.ts` — extend `CreateAssessmentOpts` (TAMBAH field optional, jangan ubah yang ada):
    ```typescript
    isTokenRequired?: boolean;   // default false
    accessToken?: string;        // 6-char alfanumerik; kosong+isTokenRequired → klik Generate
    protonTrackId?: number;      // value option #protonTrackSelect
    protonTrackTahun?: 'Tahun 1' | 'Tahun 2' | 'Tahun 3';  // pilih by data-tahun (lebih robust)
    ```

    (3c) Di `createAssessmentViaWizard` STEP 1 (setelah `selectOption(category)` ~L56, SEBELUM btnNext1) — blok kondisional Proton:
    ```typescript
    if (opts.protonTrackTahun) {
      await page.locator(wizardSelectors.protonFieldsSection).waitFor({ state: 'visible' });
      const opt = page.locator(`${wizardSelectors.protonTrackSelect} option[data-tahun="${opts.protonTrackTahun}"]`).first();
      const val = await opt.getAttribute('value');
      await page.selectOption(wizardSelectors.protonTrackSelect, val!);
    } else if (opts.protonTrackId) {
      await page.selectOption(wizardSelectors.protonTrackSelect, String(opts.protonTrackId));
    }
    ```
    (Catatan: Category 'Assessment Proton' di-pass via opts.category seperti biasa; section show oleh JS saat selectOption category.)

    (3d) Di `createAssessmentViaWizard` STEP 3 (setelah fill passPercentage ~L96) — blok kondisional Token:
    ```typescript
    if (opts.isTokenRequired) {
      await page.locator(wizardSelectors.isTokenRequired).check();
      await page.locator(wizardSelectors.tokenSection).waitFor({ state: 'visible' });
      if (opts.accessToken) await page.fill(wizardSelectors.accessToken, opts.accessToken);
      else await page.locator('button:has-text("Generate"), button[onclick*="generateToken"]').click();
    }
    ```

    (3e) Helper paste-import BARU (hanya bila W0-2 = VALID; jika DRIFT, SKIP helper ini & catat di SUMMARY bahwa Flow D3 pakai fallback addQuestionViaForm×3):
    ```typescript
    /** Phase 379 — import questions ke package via paste (TSV 6-kolom). Source: exam-taking.spec.ts:599-621. */
    export async function importQuestionsViaPaste(page: Page, packageId: number, tsvRows: string): Promise<void> {
      await page.goto(`/Admin/ManagePackageQuestions?packageId=${packageId}`);
      await page.locator('a:has-text("Import Questions")').first().click();
      await page.waitForLoadState('networkidle');
      await page.fill('textarea[name="pasteText"]', tsvRows);
      await page.click('button:has-text("Import")');
      await page.waitForLoadState('networkidle');
      await expect(page.locator('body')).toContainText(/soal|success|berhasil/i);
    }
    ```
    (Verifikasi route Import sebenarnya saat tulis — pakai keputusan W0-2; selector final boleh disesuaikan ke hasil verifikasi.)

    (3f) SMOKE: jalankan 1 flow sederhana end-to-end untuk membuktikan tooling+env hijau SEBELUM migrasi massal. Pilih Flow K skeleton minimal ATAU smoke ad-hoc: buat file sementara ATAU jalankan `npx playwright test tests/e2e/exam-types.spec.ts --workers=1 -g "FLOW L"` (FLOW L sudah un-fixme oleh 376, pakai helper sama) sebagai bukti env+helper hijau. Lampirkan output "passed" di SUMMARY. (Smoke ini bukan bagian deliverable file — hanya gate env.)

    Pastikan TypeScript compile bersih: `npx tsc --noEmit -p tests/tsconfig.json` (atau tsc project root yang relevan) — tidak ada error pada examTypes.ts/wizardSelectors.ts.
  </action>
  <acceptance_criteria>
    - `grep -c "isTokenRequired" tests/e2e/helpers/examTypes.ts` >= 1 (field + blok kondisional).
    - `grep -c "protonTrackTahun" tests/e2e/helpers/examTypes.ts` >= 1.
    - `grep -c "tokenSection" tests/e2e/helpers/wizardSelectors.ts` >= 1.
    - `grep -c "protonTrackSelect" tests/e2e/helpers/wizardSelectors.ts` >= 1.
    - Jika W0-2 VALID: `grep -c "importQuestionsViaPaste" tests/e2e/helpers/examTypes.ts` >= 1.
    - TypeScript compile bersih (`npx tsc --noEmit` pada konfig tests) — 0 error di file helper.
    - Signature `createAssessmentViaWizard`/`createDefaultPackage`/`addQuestionViaForm` TIDAK berubah (param order identik) — `git diff` hanya tunjukkan penambahan, bukan modifikasi param existing.
    - Smoke `npx playwright test tests/e2e/exam-types.spec.ts --workers=1 -g "FLOW L"` → "passed" (bukti env+helper hijau, output di SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-types.spec.ts --workers=1 -g "FLOW L"</automated>
  </verify>
  <done>Helper ter-extend additive (token/proton/paste), selector additive ada, tsc bersih, signature existing utuh, smoke FLOW L hijau membuktikan tooling+env siap untuk migrasi flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → SQL localhost | sqlcmd/dbSnapshot menulis (seed) + restore ke DB lokal — TIDAK boleh non-localhost |
| seed temporary → DB lokal | data ProtonTrack T3 disisipkan untuk test; harus di-restore pasca-suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-379-01 | Tampering | seed ProtonTrack T3 nempel di DB lokal pasca-run | mitigate | SEED_WORKFLOW: snapshot pre-seed + journal + restore di Plan 06; global.teardown db.restore() safety-net |
| T-379-02 | Tampering | test menyentuh DB non-localhost (Dev/Prod) | mitigate | dbSnapshot.runSqlcmd REJECT non-localhost ("Refusing to target non-localhost SQL Server"); CLAUDE.md compliance |
| T-379-03 | Scope/Tampering | migrasi diam-diam ubah kode produksi (controller/view) | accept | scope test-infra only; acceptance criteria cek `git status Controllers/ Views/` kosong; bug prod → backlog (CONTEXT D) |

**Honest note:** Plan ini hanya menambah helper test + verifikasi + seed lokal. NOL perubahan runtime attack surface produksi.
</threat_model>

<verification>
- 379-W0-VERIFY.md menjawab 3 Open Question dengan hasil konkret.
- ProtonTrack Tahun 3 tersedia di DB lokal.
- Helper extension additive (tsc bersih, signature existing utuh).
- Smoke FLOW L hijau (bukti env+tooling).
</verification>

<success_criteria>
- Fondasi helper siap untuk Plan 02-05 (token/proton/paste).
- 3 Open Question terjawab → keputusan fallback per flow terdokumentasi.
- Data Proton T3 siap untuk Flow E (Plan 03).
- Bukti env+helper hijau (smoke) sebelum migrasi massal.
</success_criteria>

<output>
After completion, create `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-01-SUMMARY.md`
</output>
