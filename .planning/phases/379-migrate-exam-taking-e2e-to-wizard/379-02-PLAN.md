---
phase: 379-migrate-exam-taking-e2e-to-wizard
plan: 02
type: execute
wave: 2
depends_on: [379-01]
files_modified:
  - tests/e2e/exam-taking.spec.ts
autonomous: true
requirements: [E2E-01]
tags: [e2e, playwright, test-infra, wizard, migration]

must_haves:
  truths:
    - "Flow A (legacy full lifecycle) berjalan via wizard + package — hijau, fixme dihapus"
    - "Flow B (token) berjalan via wizard + token extension — hijau, fixme dihapus"
    - "Flow C (force close & close early) berjalan via wizard + package (2 worker) — hijau, fixme dihapus"
    - "Import block menambah createAssessmentViaWizard/createDefaultPackage/addQuestionViaForm dari examTypes"
  artifacts:
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "import helper kanonik + Flow A/B/C create+question step pakai wizard; 3 fixme dihapus"
      contains: "createAssessmentViaWizard"
  key_links:
    - from: "exam-taking.spec.ts Flow A1"
      to: "createAssessmentViaWizard + createDefaultPackage"
      via: "helper call ganti flat-form /Admin/CreateAssessment"
      pattern: "createAssessmentViaWizard"
    - from: "exam-taking.spec.ts Flow B1"
      to: "createAssessmentViaWizard({isTokenRequired:true})"
      via: "token extension"
      pattern: "isTokenRequired"
---

<objective>
Migrasi Flow A (legacy full lifecycle), B (token-protected), C (force close & close early) di `tests/e2e/exam-taking.spec.ts` dari flat-form `/Admin/CreateAssessment` + `/Admin/ManageQuestions?id=` usang ke wizard + package via helper kanonik. Tambah import block helper. Hapus `test.fixme` ketiga flow. Flow worker/monitoring/cleanup step SURVIVE (tidak diubah).

Purpose: Batch migrasi pertama (3 flow termudah-menengah). A = baseline pola create+QADD; B = uji token extension (Plan 01); C = uji multi-worker (2 peserta) + force-close. Single-file constraint: plan ini SATU-SATUNYA yang edit exam-taking.spec.ts di wave 2 (tidak ada plan paralel pada file yang sama).
Output: 3 flow hijau (A/B/C) `--workers=1`, 3 fixme dihapus, import block ditambahkan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-CONTEXT.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md

<interfaces>
<!-- Helper kanonik (ter-extend Plan 01) — pakai langsung. -->
From tests/e2e/helpers/examTypes.ts:
```typescript
createAssessmentViaWizard(page, { title, category, scheduleDate, scheduleTime?, durationMinutes,
  passPercentage, allowAnswerReview, generateCertificate?, participantEmails,
  isTokenRequired?, accessToken? }): Promise<void>
createDefaultPackage(page, packageName?='Paket A'): Promise<number>  // returns packageId
addQuestionViaForm(page, packageId, QuestionInput): Promise<void>
// QuestionInput: {type:'MultipleChoice', text, options:[4], correctIndex:0..3, score}
```

Extract assessmentId pasca-create (verbatim dari FLOW L):
```typescript
const href = await page.locator('#modal-manage-btn').getAttribute('href');
assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
```

Import block target (tambahkan ke exam-taking.spec.ts:1-6; PRESERVE import exam313 L4 + Page type L1 + serial-mode L6):
```typescript
import { uniqueTitle, today, autoConfirm } from '../helpers/utils';  // (existing — pertahankan)
import {
  createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm,
  submitExamTwoStep, checkMAOptionsForQuestion, fillEssayAnswer,
  gradeSingleEssaySession, type QuestionInput,
} from './helpers/examTypes';
import * as db from '../helpers/dbSnapshot';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah import block + migrasi Flow A (create+QADD), hapus fixme A</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:1-116 (import header + Flow A full: A1 create flat-form :39-59, A2/A3 questions :62-116, A4-A15 worker/review/cert/monitoring/cleanup — SURVIVE)
    - tests/e2e/exam-types.spec.ts:314-355 (FLOW L1/L2/L3 — pola create-wizard + extract assessmentId + createDefaultPackage + addQuestionViaForm) — ANALOG VERBATIM
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"Flow A-J CREATE step" + §"Flow A-J QADD step"
  </read_first>
  <action>
    (1a) Tambah import block (lihat <interfaces>) ke exam-taking.spec.ts. PRESERVE existing: `import {test,expect,Page}` (L1), `import {login}` (L2), `import {...} from './helpers/exam313'` (L4), `test.describe.configure({mode:'serial'})` (L6). Tambah `db` import + helper examTypes import. Catatan: `autoConfirm` & `uniqueTitle`/`today` sudah di-import L3 — jangan duplikasi.

    (1b) Hapus baris `test.fixme(true, ...)` di Flow A (`:32`).

    (1c) Migrasi A1 (`:36-59`): ganti seluruh blok flat-form (`page.goto('/Admin/CreateAssessment')` + isi `#Title/#Category/#ScheduleDate/#ScheduleTime/#DurationMinutes/#PassPercentage/#AllowAnswerReview/#GenerateCertificate` + `#submitBtn` + waitForTimeout) dengan:
    ```typescript
    title = uniqueTitle('Pre Test Legacy Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 30, passPercentage: 60,
      allowAnswerReview: true,        // A9 answer-review butuh true
      generateCertificate: true,      // A10 cert butuh true
      participantEmails: ['rino.prasetyo@pertamina.com'],
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    ```

    (1d) Migrasi A2/A3 (`:62-116`): ganti `/Admin/ManageQuestions?id=${assessmentId}` flat-form (textarea[name="question_text"] + input[name="options"] + correct_option_index + "Tambah Soal") dengan:
    ```typescript
    await page.goto(`/Admin/ManagePackages?assessmentId=${assessmentId}`);
    await page.waitForLoadState('networkidle');
    const packageId = await createDefaultPackage(page);
    await addQuestionViaForm(page, packageId, { type: 'MultipleChoice', text: 'Apa kepanjangan OJT?',
      options: ['On the Job Training','Online Job Test','Operation Job Task','Operational Job Training'],
      correctIndex: 0, score: 100 });
    // Q2/Q3 jika flow A asli punya 3 soal: ulang addQuestionViaForm dgn correctIndex sesuai
    ```
    Pertahankan jumlah & isi soal sesuai Flow A asli (cek :62-116 berapa soal). Deklarasikan `let packageId` di scope describe bila A4+ butuh.

    (1e) A4-A15 (worker start/answer/results/answer-review/cert/monitoring/reset/cleanup) SURVIVE — JANGAN ubah kecuali selektor jawaban positional fragile (A6) bikin flaky; bila flaky, ganti ke option-text match (PATTERNS Pattern 3). Minimal-change (D-03): biarkan jika hijau.

    (1f) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow A"` → semua sub-test passed.
  </action>
  <acceptance_criteria>
    - `grep -c "createAssessmentViaWizard" tests/e2e/exam-taking.spec.ts` >= 1.
    - Flow A describe TIDAK lagi punya `test.fixme` (grep `test.fixme` di range Flow A = 0).
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow A"` → "passed" (semua A1-A15).
    - Tidak ada residu `'/Admin/CreateAssessment'` flat-form atau `'/Admin/ManageQuestions?id='` di blok Flow A.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow A"</automated>
  </verify>
  <done>Flow A pakai wizard+package, fixme dihapus, import block ada, flow hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Migrasi Flow B (token, pakai extension Plan 01), hapus fixme B</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:314-400 (Flow B full: B1 create+token flat-form+DRIFT :333-346, B2 badge/.btn-start-token, B3 token modal, B4 monitoring token, B5 cleanup — B2-B5 SURVIVE)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §"Flow B (token) CREATE + token" + Shared Patterns "Token Extension"
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md (W0-2 token selector keputusan)
    - tests/e2e/helpers/examTypes.ts (blok token STEP 3 yang ditambah Plan 01) + wizardSelectors.ts (tokenSection additive)
  </read_first>
  <action>
    (2a) Hapus `test.fixme` Flow B (`:316`).

    (2b) Migrasi B1: ganti flat-form create + blok token DRIFT (`#tokenInputContainer` + `:has-text("Generate")` + baca `#AccessToken` :333-346) dengan helper:
    ```typescript
    title = uniqueTitle('Pre Test Token Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 30, passPercentage: 60, allowAnswerReview: false,
      participantEmails: ['rino.prasetyo@pertamina.com'],
      isTokenRequired: true,            // extension Plan 01
      // accessToken kosong → helper klik Generate (token 6-char auto)
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    ```
    Bila B asli butuh nilai token spesifik untuk worker-side (B3 modal), capture token: pass `accessToken: 'ABC123'` eksplisit (6-char) supaya deterministik dan reuse di B3. Pilih pendekatan deterministik (eksplisit token) bila B3 perlu cocokkan token.

    (2c) Flow B NO question (tidak QADD) — sesuai map RESEARCH. JANGAN tambah createDefaultPackage di B kecuali B asli memang punya soal (cek :314-400). Bila B asli punya soal, tambah createDefaultPackage+addQuestionViaForm.

    (2d) B2 (badge "Token Required" + `.btn-start-token`), B3 (token modal input), B4 (monitoring token), B5 (cleanup) = SURVIVE. Bila B3 baca token dari assessment, sesuaikan ke token deterministik (2b) atau ambil dari DB via `db.queryString` bila perlu.

    (2e) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow B"` → passed.

    Bila migrasi mengungkap bug produksi token (mis. Generate tidak set token), SURFACE ke backlog (catat di SUMMARY), JANGAN fix inline (scope test-infra, CONTEXT D).
  </action>
  <acceptance_criteria>
    - `grep -c "isTokenRequired" tests/e2e/exam-taking.spec.ts` >= 1 (dipakai Flow B).
    - Flow B describe TIDAK lagi `test.fixme` (grep di range = 0).
    - Tidak ada residu `#tokenInputContainer` di blok Flow B (DRIFT selector lama dihapus).
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow B"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow B"</automated>
  </verify>
  <done>Flow B pakai wizard + token extension, drift selector lama dihapus, fixme dihapus, hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Migrasi Flow C (force close & close early, 2 worker), hapus fixme C</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts:401-548 (Flow C full: C1 create 2-worker, C2 QADD 2 MC, C3 worker start, C4 ForceClose, C5 CloseEarly modal, C6 ForceCloseAll, C7 cleanup — C3-C7 SURVIVE)
    - tests/e2e/exam-types.spec.ts:314-355 (analog create+QADD)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-PATTERNS.md §Shared Patterns "Auth" (coachee2=iwan3)
  </read_first>
  <action>
    (3a) Hapus `test.fixme` Flow C (`:403`).

    (3b) Migrasi C1: ganti flat-form create dengan helper, 2 peserta:
    ```typescript
    title = uniqueTitle('Pre Test ForceClose Exam');
    await login(page, 'hc');
    await createAssessmentViaWizard(page, {
      title, category: 'OJT', scheduleDate: today(), scheduleTime: '00:01',
      durationMinutes: 30, passPercentage: 60, allowAnswerReview: false,
      participantEmails: ['rino.prasetyo@pertamina.com', 'iwan3@pertamina.com'],
    });
    const href = await page.locator('#modal-manage-btn').getAttribute('href');
    assessmentId = parseInt(href!.match(/(?:\/|assessmentId=)(\d+)/)![1], 10);
    ```

    (3c) Migrasi C2: ganti `/Admin/ManageQuestions?id=` flat dengan createDefaultPackage + addQuestionViaForm ×2 (2 MC, sesuai Flow C asli). Pertahankan jumlah soal + correctIndex.

    (3d) C3 (worker start, login coachee), C4 ForceClose, C5 CloseEarly modal, C6 ForceCloseAll, C7 cleanup = SURVIVE. C3/C4 untuk worker kedua pakai `login(page, 'coachee2')`.

    (3e) Jalankan `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow C"` → passed.
  </action>
  <acceptance_criteria>
    - Flow C describe TIDAK lagi `test.fixme` (grep di range = 0).
    - Blok Flow C pakai `participantEmails: [...,'iwan3@pertamina.com']` (2 worker via createAssessmentViaWizard).
    - Tidak ada residu flat-form `'/Admin/CreateAssessment'` atau `'/Admin/ManageQuestions?id='` di blok Flow C.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow C"` → "passed".
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1 -g "Flow C"</automated>
  </verify>
  <done>Flow C pakai wizard+package (2 worker), fixme dihapus, force-close lifecycle hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app localhost | wizard create + worker exam via UI; no prod code change |
| test → SQL localhost | (Flow B optional) baca token via db.queryString — localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-379-04 | Tampering | test data assessment A/B/C nempel pasca-run | mitigate | uniqueTitle() isolasi + per-flow cleanup (C7/B5/A15 SURVIVE) + global.teardown db.restore() |
| T-379-05 | Scope | migrasi diam-diam ubah kode produksi | accept | scope test-infra only; bug prod (mis. token Generate) → backlog, bukan fix inline |
| T-379-06 | Info Disclosure | token/credential test di spec | accept | pola existing (akun test rino/iwan/hc); bukan attack surface baru, test-only |

**Honest note:** NOL perubahan kode produksi. Hanya rewrite create/question step ke helper.
</threat_model>

<verification>
- 3 flow A/B/C hijau `--workers=1` (run terpisah per flow).
- 3 fixme dihapus.
- Import block helper ada.
- Tidak ada residu flat-form selector di blok A/B/C.
</verification>

<success_criteria>
- Flow A, B, C migrasi ke wizard+package, hijau, fixme dihapus (SC1 sebagian).
- Token extension (Plan 01) terbukti dipakai Flow B.
- Multi-worker terbukti dipakai Flow C.
</success_criteria>

<output>
After completion, create `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-02-SUMMARY.md`
</output>
