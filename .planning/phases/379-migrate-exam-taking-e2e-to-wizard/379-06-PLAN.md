---
phase: 379-migrate-exam-taking-e2e-to-wizard
plan: 06
type: execute
wave: 6
depends_on: [379-05]
files_modified:
  - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RUN-EVIDENCE.md
  - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-VALIDATION.md
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [E2E-01]
tags: [e2e, playwright, test-infra, gate, dod, full-green-run, seed-cleanup]

must_haves:
  truths:
    - "Full suite exam-taking.spec.ts --workers=1 dijalankan HIJAU end-to-end (semua A-J + K)"
    - "Bukti run (output 'N passed') dilampirkan di 379-RUN-EVIDENCE.md (D-03)"
    - "ZERO test.fixme + ZERO test.skip Proton tersisa di exam-taking.spec.ts"
    - "Seed temporary ProtonTrack T3 di-restore (SEED_WORKFLOW) + journal ditandai cleaned"
    - "Helper extension tetap additive (no signature refactor existing examTypes.ts/wizardSelectors.ts)"
  artifacts:
    - path: ".planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RUN-EVIDENCE.md"
      provides: "output full green run + ringkasan per-flow + env yang dipakai"
      min_lines: 20
    - path: ".planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-VALIDATION.md"
      provides: "Validation Sign-Off terisi + nyquist_compliant: true + Per-Task Verification Map terisi"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "379-RUN-EVIDENCE.md"
      to: "npx playwright test exam-taking.spec.ts --workers=1"
      via: "full green run output"
      pattern: "passed"
---

<objective>
Gate fase 379 (D-03 DoD): jalankan FULL suite `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1` end-to-end HIJAU (semua A-J + K), lampirkan bukti run di `379-RUN-EVIDENCE.md`. Konfirmasi struktural: ZERO `test.fixme` + ZERO `test.skip` Proton. Restore seed temporary ProtonTrack T3 (SEED_WORKFLOW, journal cleaned). Isi `379-VALIDATION.md` Sign-Off + `nyquist_compliant: true`.

Purpose: D-03 mandat "full green run lokal WAJIB, bukti dilampirkan". Plan 02-05 menjalankan flow per-grup; plan ini = gate integrasi penuh (semua flow dalam satu run serial, bukti DoD) + cleanup lifecycle seed (SEED_WORKFLOW). Single-file constraint: tidak mengedit exam-taking.spec.ts (read-only gate), hanya jalankan + dokumentasi + restore DB.
Output: bukti full green run + seed restored + VALIDATION sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-CONTEXT.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-VALIDATION.md
@.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-W0-VERIFY.md
@docs/SEED_JOURNAL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full green run + bukti (379-RUN-EVIDENCE.md) + verifikasi struktural ZERO fixme/skip</name>
  <files>.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RUN-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-CONTEXT.md D-03 (full green run wajib + bukti) + D-04 (additive)
    - tests/playwright.config.ts (globalTeardown, baseURL, --workers=1, retries:0)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RESEARCH.md §Validation Architecture (full suite command + env prasyarat)
    - memory reference_local_e2e_sql_env_fix (SQLBrowser + lpc: shared-mem + --workers=1)
  </read_first>
  <action>
    (1a) Prasyarat env (CLAUDE.md / D-03): app lokal running `Authentication__UseActiveDirectory=false dotnet run` (@localhost:5277), SQLBrowser running, SQL `localhost\SQLEXPRESS` + `HcPortalDB_Dev` up, ProtonTrack T3 masih ada (Plan 01 seed belum di-restore — restore di Task 2 SETELAH run). Bila login 500 error 53 → `lpc:` shared-memory conn override.

    (1b) Verifikasi struktural SEBELUM run:
    - `grep -c "test.fixme" tests/e2e/exam-taking.spec.ts` HARUS 0 (semua 10 A-J dihapus oleh Plan 02-05).
    - Tidak ada `test.skip(true, 'No Tahun 3 ProtonTrack` di file (Flow E, D-02).
    - `grep -c "createAssessmentViaWizard" tests/e2e/exam-taking.spec.ts` >= 10 (semua flow create migrasi).
    - `grep -c "/Admin/CreateAssessment'" tests/e2e/exam-taking.spec.ts` — pastikan tidak ada residu flat-form create call (selain di dalam helper, yang berada di file lain).
    - Helper additive utuh: `git diff` pada `examTypes.ts`/`wizardSelectors.ts` hanya penambahan (signature `createAssessmentViaWizard`/`createDefaultPackage`/`addQuestionViaForm` param order tak berubah).

    (1c) Jalankan FULL suite end-to-end:
    `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1`
    Tunggu sampai selesai (serial, beberapa menit; G deterministik). Tangkap output ringkasan "N passed" + daftar flow.

    (1d) Bila ada flaky/red: investigasi root cause. Bila test-side (selector/timing) → perbaiki di flow terkait (catat plan mana yang re-touch; ini gate boleh patch test minor untuk hijau, TAPI bila perubahan substansial pada satu flow, kembalikan ke plan flow yang relevan). Bila bug produksi → SURFACE backlog (STATE + SUMMARY), JANGAN fix inline (CONTEXT D). Re-run sampai HIJAU.

    (1e) Materialize `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RUN-EVIDENCE.md`:
    - Header: tanggal, env (AD=false, SQLBrowser, lpc bila dipakai, --workers=1).
    - Output run penuh (atau ringkasan "N passed, 0 failed" + per-describe pass).
    - Tabel per-flow: `| Flow | Status | Catatan |` (A-J + K semua passed).
    - Catatan temuan backlog (bila ada drift prod yang di-surface).
  </action>
  <acceptance_criteria>
    - `grep -c "test.fixme" tests/e2e/exam-taking.spec.ts` = 0.
    - Tidak ada `test.skip(true, 'No Tahun 3 ProtonTrack` di file.
    - `npx playwright test tests/e2e/exam-taking.spec.ts --workers=1` → "N passed, 0 failed" (semua A-J + K hijau).
    - `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RUN-EVIDENCE.md` ada, berisi output run + tabel per-flow, min 20 baris.
    - `git diff` examTypes.ts/wizardSelectors.ts = additive only (no signature change existing functions).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/exam-taking.spec.ts --workers=1</automated>
  </verify>
  <done>Full suite hijau end-to-end, bukti run dilampirkan (D-03), ZERO fixme/skip, helper additive utuh.</done>
</task>

<task type="auto">
  <name>Task 2: Restore seed temporary ProtonTrack T3 (SEED_WORKFLOW) + isi VALIDATION sign-off</name>
  <files>.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-VALIDATION.md, docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_JOURNAL.md (entry seed 379 dari Plan 01 Task 2 — status active bila seed dilakukan)
    - CLAUDE.md §Seed Data Workflow + docs/SEED_WORKFLOW.md (RESTORE command + tandai journal cleaned)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-VALIDATION.md (skeleton — isi Per-Task Verification Map + Validation Sign-Off + frontmatter nyquist_compliant)
    - .planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-RUN-EVIDENCE.md (Task 1 output)
  </read_first>
  <action>
    (2a) CONDITIONAL restore seed: bila Plan 01 Task 2 MELAKUKAN seed (journal status active, snapshot .bak ada):
    - RESTORE DB lokal dari snapshot pre-seed: `sqlcmd -S localhost\SQLEXPRESS -d master -E -I -Q "RESTORE DATABASE HcPortalDB_Dev FROM DISK='C:\Temp\HcPortalDB_Dev_pre379seed.bak' WITH REPLACE"` (sesuai SEED_WORKFLOW; pastikan tidak ada koneksi aktif). ATAU andalkan global.teardown `db.restore()` yang sudah jalan pasca full-suite Task 1 (verifikasi DB sudah baseline: `SELECT COUNT(*) FROM ProtonTracks WHERE TahunKe='Tahun 3'` kembali ke nilai pre-seed).
    - Tandai entry journal `docs/SEED_JOURNAL.md` jadi `cleaned` dengan tanggal.
    Bila Plan 01 TIDAK seed (track sudah ada): catat journal "tidak seed, tidak perlu restore" → tandai resolved.

    (2b) JANGAN sentuh DB Dev/Prod (CLAUDE.md) — localhost saja.

    (2c) Isi `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-VALIDATION.md`:
    - Per-Task Verification Map: isi baris tiap flow (A-J + K) dengan Plan/Wave/Requirement E2E-01/automated command `-g "Flow X"`/status ✅ green.
    - Validation Sign-Off: centang semua (A-J migrasi, Flow K hijau + Score assert, full suite hijau bukti dilampirkan, helper additive, seed restored).
    - Frontmatter: set `nyquist_compliant: true` + `wave_0_complete: true`.

    (2d) Update STATE.md "Next" / Deferred bila ada temuan backlog dari migrasi (drift prod yang di-surface) — append, jangan overwrite konteks lain.
  </action>
  <acceptance_criteria>
    - Seed lifecycle resolved: `docs/SEED_JOURNAL.md` entry 379 ditandai `cleaned`/resolved (atau "tidak seed" bila track sudah ada).
    - `SELECT COUNT(*) FROM ProtonTracks WHERE TahunKe='Tahun 3'` di DB lokal = nilai pre-seed (restore terbukti) ATAU tetap (bila tidak seed).
    - `379-VALIDATION.md` frontmatter `nyquist_compliant: true` + `wave_0_complete: true`.
    - `379-VALIDATION.md` Per-Task Verification Map terisi (semua flow A-J + K, status green).
    - 0 perubahan Data/SeedData.cs.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const v=fs.readFileSync('.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-VALIDATION.md','utf8');const j=fs.readFileSync('docs/SEED_JOURNAL.md','utf8');if(!/nyquist_compliant:\s*true/.test(v)){console.error('VALIDATION not nyquist_compliant');process.exit(1)}if(!/cleaned|resolved|tidak seed/i.test(j)||!/379|Tahun 3/i.test(j)){console.error('SEED_JOURNAL not resolved');process.exit(1)}console.log('GATE OK')"</automated>
  </verify>
  <done>Seed temporary di-restore (atau N/A), journal cleaned, VALIDATION sign-off lengkap + nyquist_compliant true, Data/SeedData.cs tak tersentuh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → SQL localhost | full-suite run + restore via sqlcmd/dbSnapshot — localhost only |
| seed temporary → DB lokal | ProtonTrack T3 di-restore ke baseline pasca-gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-379-16 | Tampering | seed ProtonTrack T3 residu pasca-fase | mitigate | RESTORE snapshot + journal cleaned (Task 2); global.teardown db.restore() safety-net |
| T-379-17 | Tampering | restore menyentuh DB non-localhost | mitigate | sqlcmd target localhost\SQLEXPRESS only; CLAUDE.md compliance |
| T-379-18 | Scope | gate run ungkap bug prod → tergoda fix inline | accept | bug prod → backlog/STATE; gate hanya patch test-side minor untuk hijau |

**Honest note:** Gate murni run + dokumentasi + restore DB lokal. NOL perubahan kode produksi.
</threat_model>

<verification>
- Full suite `exam-taking.spec.ts --workers=1` hijau end-to-end (bukti dilampirkan, D-03).
- ZERO test.fixme + ZERO test.skip Proton.
- Seed temporary restored, journal cleaned.
- VALIDATION sign-off + nyquist_compliant true.
- Helper additive utuh.
</verification>

<success_criteria>
- SC2 (suite hijau --workers=1) terbukti dengan bukti run (D-03 DoD).
- SC1 + SC3 ter-verifikasi struktural (10 fixme dihapus, Flow K Score assert).
- Seed lifecycle bersih (SEED_WORKFLOW).
- Fase 379 siap untuk /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/379-migrate-exam-taking-e2e-to-wizard/379-06-SUMMARY.md`
</output>
