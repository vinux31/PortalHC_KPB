---
phase: 382-grading-lifecycle-cert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentConstants.cs
  - Services/GradingService.cs
  - HcPortal.Tests/GradingDedupeTests.cs
  - HcPortal.Tests/SubmitResurrectionTests.cs
autonomous: true
requirements: [WSE-06, WSE-07]
tags: [grading, lifecycle, dedupe, anti-resurrection, race-safety]

must_haves:
  truths:
    - "GradingService menilai single-answer (MC) dari jawaban FINAL per soal (SubmittedAt terbaru) — baris duplikat/basi tidak memengaruhi Score"
    - "Branch MultipleAnswer (MA) TIDAK ikut dedupe (multi-row by design tetap dibaca penuh)"
    - "GradeAndCompleteAsync non-essay menolak commit Completed-lulus saat sesi sudah Completed/Abandoned/Cancelled/PendingGrading (rowsAffected==0 → return false), bukan hanya Completed"
    - "Branch essay GradeAndCompleteAsync menolak commit PendingGrading saat sesi sudah Abandoned/Cancelled (selain Completed/PendingGrading yang sudah dijaga)"
    - "AssessmentConstants.AssessmentStatus punya const Abandoned (single-source label, v22.0 discipline)"
    - "Tidak ada migration / perubahan model — dotnet ef migrations list tidak bertambah"
  artifacts:
    - path: "Models/AssessmentConstants.cs"
      provides: "const string Abandoned untuk guard STAT-01/02"
      contains: "public const string Abandoned = \"Abandoned\";"
    - path: "Services/GradingService.cs"
      provides: "in-memory dedupe-read finalByQuestion + STAT-01 guard expand kedua branch"
      contains: "OrderByDescending(r => r.SubmittedAt)"
    - path: "HcPortal.Tests/GradingDedupeTests.cs"
      provides: "unit: dedupe pilih SubmittedAt terbaru (MC), MA tidak ter-dedupe"
      contains: "GradingDedupe"
    - path: "HcPortal.Tests/SubmitResurrectionTests.cs"
      provides: "guard non-resurrection: commit pada sesi Abandoned/Cancelled/PendingGrading → ditolak"
      contains: "SubmitResurrection"
  key_links:
    - from: "Services/GradingService.cs MC scoring loop"
      to: "finalByQuestion lookup (dedupe-read)"
      via: "replace allResponses.FirstOrDefault L97/L152 dengan lookup dari finalByQuestion"
      pattern: "finalByQuestion"
    - from: "Services/GradingService.cs non-essay ExecuteUpdate L239"
      to: "WHERE NOT IN (Completed,Abandoned,Cancelled,PendingGrading)"
      via: "ExecuteUpdateAsync rowsAffected guard"
      pattern: "s.Status != S.Abandoned"
---

<objective>
Fix WSE-06 (SAVE-01, sisi GradingService) + WSE-07 (STAT-01, sisi GradingService) — dua REQ yang sama-sama memutasi `GradeAndCompleteAsync`. Karena densitas same-method tinggi, keduanya digarap dalam SATU plan koheren single-stream (audit: NO intra-phase parallel sub-agent pada GradingService). Plan ini juga menambah const `Abandoned` (dipakai lintas plan) dan membuat Wave-0 test untuk SAVE-01 read-final & STAT-01 anti-resurrection.

Purpose:
- SAVE-01: tanpa fix, `FirstOrDefault` tanpa ORDER BY (L97/L152) memilih baris arbitrer → Score bisa dihitung dari opsi BASI saat ada baris duplikat (race multi-tab). Dedupe-read in-memory (last-write-wins by `SubmittedAt`) menjamin Score = opsi FINAL.
- STAT-01: non-essay guard L239 hanya `!= "Completed"` → sesi Abandoned/Cancelled bisa di-resurrect jadi Completed-lulus + cert. Perluas guard ke terminal/non-resurrectable set.

Output:
- `Models/AssessmentConstants.cs` — tambah const `Abandoned` (A2/OQ-1).
- `Services/GradingService.cs` — `finalByQuestion` dedupe-read (D-01) + guard expand kedua branch (D-02).
- 2 file test xUnit baru (Wave 0): `GradingDedupeTests`, `SubmitResurrectionTests`.

Migration=false (perubahan logika murni, bukan model).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/382-grading-lifecycle-cert/382-CONTEXT.md
@.planning/phases/382-grading-lifecycle-cert/382-RESEARCH.md
@.planning/phases/382-grading-lifecycle-cert/382-PATTERNS.md
@.planning/phases/382-grading-lifecycle-cert/382-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Anchors VERIFIED dari source code session ini (zero drift kecuali dicatat). Executor bekerja dari teks ini. -->

AssessmentConstants (Models/AssessmentConstants.cs, VERIFIED L15-20):
  public static class AssessmentStatus {
    public const string Open = "Open";              // :15
    public const string Upcoming = "Upcoming";       // :16
    public const string Completed = "Completed";     // :17
    public const string PendingGrading = "Menunggu Penilaian"; // :18
    public const string InProgress = "InProgress";   // :19
    public const string Cancelled = "Cancelled";     // :20
    // NO `Abandoned` const → tambah (literal "Abandoned" dipakai di CMPController AbandonExam :1264)
  }

GradingService.GradeAndCompleteAsync (Services/GradingService.cs, VERIFIED current lines):
  :60   var existing session lookup (FirstOrDefaultAsync)
  :75   var questions = ... .ToListAsync();
  :80   var allResponses = ... .ToListAsync();   // ◄ semua response sesi sudah materialized
  :97   var mcResponse = allResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue);  // ◄ MC scoring (non-essay) — REPLACE
  :140  var etGroups = ... .GroupBy(q => ... ElemenTeknis ...);   // ◄ essay/ET branch grouping
  :152  var etMcResponse = ... .FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue);  // ◄ ET MC scoring — REPLACE
  :203  essay branch ExecuteUpdate WHERE Status != Completed && != PendingGrading  // ◄ ADD Abandoned/Cancelled
  :239  non-essay ExecuteUpdate WHERE s.Status != "Completed"  // ◄ EXPAND ke terminal set + pakai konstanta
  :248  rowsAffected==0 branch (return false) — SUDAH ADA, reuse verbatim

NB MA branches: MC = single-answer (1 baris per soal, dedupe). MultipleAnswer membaca SEMUA baris per soal (multi-row by design) — JANGAN dedupe MA. SAVE-01 = MC only.

ProtonCompletionFixture pola real-SQL (HcPortal.Tests/ProtonCompletionServiceTests.cs:25-61) — referensi fixture untuk plan lain; plan INI pakai InMemory (dedupe/guard logic, bukan race fisik).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Add Abandoned const + GradingDedupeTests + SubmitResurrectionTests (RED)</name>
  <files>Models/AssessmentConstants.cs, HcPortal.Tests/GradingDedupeTests.cs, HcPortal.Tests/SubmitResurrectionTests.cs</files>
  <read_first>
    - Models/AssessmentConstants.cs (block AssessmentStatus L13-21 — sisip const Abandoned)
    - HcPortal.Tests/ProtonCompletionServiceTests.cs L25-61 (pola fixture/DbContextOptions InMemory vs SqlServer — plan ini InMemory)
    - HcPortal.Tests/CertificateStatusTests.cs (pola unit Fact sederhana untuk gaya assertion)
    - 382-PATTERNS.md §GradingService + §AssessmentConstants
    - 382-RESEARCH.md Pattern 1 (dedupe) + Pattern 3 (guard) + §Validation (test map WSE-06/07)
  </read_first>
  <behavior>
    - Const: `AssessmentConstants.AssessmentStatus.Abandoned == "Abandoned"`.
    - GradingDedupe Test A (Dedupe_PicksLatestSubmittedAt): untuk satu PackageQuestionId dengan 2 PackageUserResponse (opsi berbeda, SubmittedAt berbeda), `finalByQuestion`/scoring memilih baris ber-SubmittedAt TERBARU. Score dihitung dari opsi FINAL (benar), bukan opsi pertama. RED sebelum Task 2.
    - GradingDedupe Test B (Dedupe_MultipleAnswer_NotDeduped): soal MultipleAnswer dengan ≥2 baris response sah → semua baris tetap terbaca (MA tidak ter-dedupe). Memastikan fix MC tidak merusak MA. (Boleh assert via GradeAndCompleteAsync hasil MA benar, atau via helper jika ada.)
    - SubmitResurrection Test C (Grade_OnAbandonedSession_Rejected): sesi Status=Abandoned → GradeAndCompleteAsync TIDAK mengubah ke Completed (Status tetap Abandoned, Score tidak ter-set lulus, return false). RED sebelum Task 3.
    - SubmitResurrection Test D (Grade_OnCancelledSession_Rejected): sesi Status=Cancelled → idem (tetap Cancelled). RED.
  </behavior>
  <action>
**Part A — const Abandoned.** Di `Models/AssessmentConstants.cs`, dalam block `AssessmentStatus` (setelah L20 `Cancelled`), tambah:

```csharp
public const string Abandoned = "Abandoned";  // Phase 382 STAT-01/02 — sebelumnya literal di AbandonExam :1264
```

**Part B — GradingDedupeTests.cs (baru, InMemory).** Buat `HcPortal.Tests/GradingDedupeTests.cs`. Pakai InMemory DbContext (cukup untuk logika dedupe/guard — bukan race fisik, race fisik di Plan 02 real-SQL). Seed: 1 AssessmentSession single-answer Normal/PostTest, 1 PackageQuestion MC (QuestionType single-answer) dengan 2 opsi (A benar, B salah), dan DUA `PackageUserResponse` untuk soal itu: baris-1 pilih opsi B (`SubmittedAt = t0`), baris-2 pilih opsi A (`SubmittedAt = t0.AddSeconds(10)`). Jalankan `GradeAndCompleteAsync` → assert Score mencerminkan opsi A (FINAL/benar), BUKAN B. Test B: soal MultipleAnswer dengan 2 baris response benar → assert MA tetap dinilai penuh (tidak terpotong jadi 1 baris).

Catatan: gunakan helper seed minimal; reuse pola model dari test existing bila ada (`GradingServiceTests` jika ada — grep dulu). Jika konstruksi GradingService butuh dependency (logger/context), mirror cara test existing meng-instantiate-nya.

**Part C — SubmitResurrectionTests.cs (baru, InMemory).** Buat `HcPortal.Tests/SubmitResurrectionTests.cs`. Seed sesi single-answer dengan Status=Abandoned (Test C) dan Status=Cancelled (Test D). Jalankan `GradeAndCompleteAsync` → assert sesi TIDAK berubah jadi Completed (Status tetap Abandoned/Cancelled) dan method return false (rowsAffected==0 guard). RED sekarang karena guard L239 hanya `!= "Completed"`.

Semua test pakai `using S = HcPortal.Models.AssessmentConstants.AssessmentStatus;` untuk status. Jalankan filter → Test A/C/D MUST FAIL (RED) sebelum Task 2/3; Test B (MA) harus hijau (MA belum disentuh).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~GradingDedupe|FullyQualifiedName~SubmitResurrection" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `Models/AssessmentConstants.cs` contains `public const string Abandoned = "Abandoned";`
    - `HcPortal.Tests/GradingDedupeTests.cs` exists and contains test method names containing `Dedupe_PicksLatestSubmittedAt` and `Dedupe_MultipleAnswer_NotDeduped`
    - `HcPortal.Tests/SubmitResurrectionTests.cs` exists and contains `Grade_OnAbandonedSession_Rejected` and `Grade_OnCancelledSession_Rejected`
    - Before Task 2/3: `dotnet test --filter "GradingDedupe|SubmitResurrection"` shows `Dedupe_PicksLatestSubmittedAt`, `Grade_OnAbandonedSession_Rejected`, `Grade_OnCancelledSession_Rejected` FAILING (RED — proves bugs)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>Const Abandoned added; 4 Wave-0 facts compile; SAVE-01 + STAT-01 RED facts fail (bugs confirmed), MA fact green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SAVE-01 dedupe-read FINAL answer in GradingService MC scoring (GREEN)</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs L56-160 (full GradeAndCompleteAsync MC + ET region; allResponses ToListAsync L80; FirstOrDefault L97 + L152)
    - 382-PATTERNS.md §GradingService (dedupe block exact code)
    - 382-RESEARCH.md Pattern 1 + Pitfall 1 (Score divergen SubmitExam vs GradingService)
  </read_first>
  <action>
Di `Services/GradingService.cs`, SEBELUM loop scoring MC (sebelum L97), bangun lookup final-per-soal SEKALI dari `allResponses` yang sudah `.ToListAsync()` (L80). EF Core 8 TIDAK menerjemahkan `GroupBy(g => g.First())` entity ke SQL — tapi ini berjalan in-memory pada list yang sudah materialized (aman, A1):

```csharp
// SAVE-01 (D-01): final answer per soal (last-write-wins by SubmittedAt) — in-memory pada list yang sudah ToListAsync.
// MC/single-answer only — MultipleAnswer dibaca penuh (multi-row), JANGAN masuk dedupe ini.
var finalByQuestion = allResponses
    .Where(r => r.PackageOptionId.HasValue)
    .GroupBy(r => r.PackageQuestionId)
    .ToDictionary(g => g.Key, g => g.OrderByDescending(r => r.SubmittedAt).First());
```

Lalu GANTI:
- L97 `var mcResponse = allResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue);`
  → `var mcResponse = finalByQuestion.TryGetValue(q.Id, out var fr) ? fr : null;`
- L152 (ET MC scoring) `var etMcResponse = ...FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue);`
  → `var etMcResponse = finalByQuestion.TryGetValue(q.Id, out var efr) ? efr : null;`

JANGAN ubah branch MultipleAnswer (yang membaca semua baris per soal — multi-row by design). JANGAN ubah signature method. Setelah fix, GradingDedupe Test A pass; Test B (MA) tetap pass.

Verifikasi tidak ada model change: `git diff --name-only` tidak menampilkan `Migrations/` atau `*ModelSnapshot.cs`.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~GradingDedupe" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `Services/GradingService.cs` contains `OrderByDescending(r => r.SubmittedAt)` and `finalByQuestion`
    - `Services/GradingService.cs` no longer calls `allResponses.FirstOrDefault(r => r.PackageQuestionId == q.Id && r.PackageOptionId.HasValue)` at the MC scoring sites (replaced by `finalByQuestion.TryGetValue`)
    - `dotnet test --filter GradingDedupe` exits 0 (Test A + Test B green)
    - `dotnet build` exits 0
    - `git diff --name-only` shows no `Migrations/` file and no `*ModelSnapshot.cs`
  </acceptance_criteria>
  <done>MC scoring reads FINAL per soal (SubmittedAt terbaru); MA unchanged; dedupe facts green; no migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: STAT-01 guard expand — GradingService kedua branch exclude Abandoned/Cancelled (GREEN)</name>
  <files>Services/GradingService.cs</files>
  <read_first>
    - Services/GradingService.cs L195-260 (essay branch ExecuteUpdate L203 + non-essay L239 + rowsAffected branch L248)
    - 382-PATTERNS.md §GradingService (guard expand exact diff)
    - 382-RESEARCH.md Pattern 3
  </read_first>
  <action>
Tambah `using S = AssessmentConstants.AssessmentStatus;` di file (atau alias lokal) jika belum ada.

**Non-essay (L239):** GANTI
```csharp
.Where(s => s.Id == session.Id && s.Status != "Completed")
```
menjadi:
```csharp
.Where(s => s.Id == session.Id
    && s.Status != S.Completed && s.Status != S.Abandoned
    && s.Status != S.Cancelled && s.Status != S.PendingGrading)
```
Branch `if (rowsAffected == 0) return false;` di L248 SUDAH ADA — biarkan apa adanya (kini juga menangkap resurrection blocked).

**Essay (L203):** guard saat ini `s.Status != Completed && s.Status != PendingGrading`. TAMBAH Abandoned/Cancelled:
```csharp
.Where(s => s.Id == session.Id
    && s.Status != S.Completed && s.Status != S.PendingGrading
    && s.Status != S.Abandoned && s.Status != S.Cancelled)
```
(PendingGrading tetap di essay branch karena itu state yang DI-SET branch ini; tapi sesi yang sudah Abandoned/Cancelled tak boleh di-set PendingGrading.)

Pakai konstanta `S.*` (carry-forward v22.0), bukan literal. JANGAN ubah SetProperty payload. Setelah fix, SubmitResurrection Test C/D pass.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~SubmitResurrection|FullyQualifiedName~GradingDedupe" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `Services/GradingService.cs` non-essay WHERE contains `s.Status != S.Abandoned` and `s.Status != S.Cancelled` and `s.Status != S.PendingGrading`
    - `Services/GradingService.cs` essay branch WHERE contains `s.Status != S.Abandoned` and `s.Status != S.Cancelled`
    - No literal `s.Status != "Completed"` remains in GradeAndCompleteAsync (replaced by `S.Completed` constant)
    - `dotnet test --filter "SubmitResurrection|GradingDedupe"` exits 0 (all green)
    - `dotnet build` exits 0; `git diff --name-only` shows no `Migrations/`
  </acceptance_criteria>
  <done>Both GradingService branches reject commit on Abandoned/Cancelled/PendingGrading (rowsAffected==0 → false); resurrection facts green; constants used; no migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker browser → GradingService (via SubmitExam) | Worker-submitted answers scored; stored verdict + cert derive from this |
| DB rows (PackageUserResponse) → GradingService scoring | Duplicate/stale rows (from prior race) feed scoring; must read FINAL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-382-01 | Tampering / Integrity (stale-answer score) | `GradingService` MC scoring reads arbitrary row (no ORDER BY) → Score from stale option when duplicate rows exist | mitigate | D-01: in-memory dedupe-read `finalByQuestion` (last-write-wins by SubmittedAt); unit asserts Score = final option |
| T-382-02 | Tampering / Elevation (resurrection) | `GradeAndCompleteAsync` non-essay guard only `!= Completed` → Abandoned/Cancelled session can be committed Completed-lulus + cert | mitigate | D-02: ExecuteUpdate WHERE NOT IN (Completed,Abandoned,Cancelled,PendingGrading); rowsAffected==0 → false; both branches; SubmitResurrection facts |
| T-382-03 | Business Logic (V11) | Essay branch could set PendingGrading on a terminal session | mitigate | D-02 essay branch guard expand (add Abandoned/Cancelled) |

No HIGH-severity unmitigated threats remain in this plan. CMPController-side guards (SubmitExam early guard, TOK-02, TMR) modeled in Plan 02; CERT-01 in Plan 03.
</threat_model>

<verification>
- `dotnet build` exits 0.
- `dotnet test HcPortal.Tests --filter "GradingDedupe|SubmitResurrection"` exits 0 (4 facts green: dedupe-latest, MA-not-deduped, abandoned-rejected, cancelled-rejected).
- `Models/AssessmentConstants.cs` has `Abandoned` const.
- NO migration: `git diff --name-only` shows no `Migrations/*` and no `*ModelSnapshot.cs`.
</verification>

<success_criteria>
- WSE-06 (GradingService half): Score = opsi FINAL via dedupe-read; MA untouched.
- WSE-07 (GradingService half): commit Completed-lulus ditolak pada sesi Abandoned/Cancelled/PendingGrading (kedua branch).
- `Abandoned` const tersedia untuk Plan 02/03.
- migration=false dikonfirmasi (no model/snapshot diff).
</success_criteria>

<output>
After completion, create `.planning/phases/382-grading-lifecycle-cert/382-01-SUMMARY.md`.
</output>
