---
phase: 382-grading-lifecycle-cert
plan: 03
type: execute
wave: 3
depends_on: [382-02]
files_modified:
  - Models/CertificationManagementViewModel.cs
  - HcPortal.Tests/CertificateStatusTests.cs
  - HcPortal.Tests/CertAlertConsistencyTests.cs
  - tests/e2e/exam-taking.spec.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [WSE-11]
tags: [certificate, visibility, single-source, e2e-acceptance, migration-guard]

must_haves:
  truths:
    - "DeriveCertificateStatus(null ValidUntil, null/non-Permanent) → Aktif (BUKAN Expired) — cert lulus tanpa kedaluwarsa = aktif-permanen"
    - "DeriveCertificateStatus(null, 'Permanent') tetap Permanent (tidak berubah)"
    - "Single-source: AdminBase renewal post-filter + Renewal tally + CDP tally OTOMATIS mengeluarkan cert null (konsumsi Status enum) tanpa edit logika consumer"
    - "HomeController badge/notif (ValidUntil.HasValue filter) sudah mengecualikan null — diverifikasi tidak drift (tidak diubah)"
    - "Test lama _NullValidUntil_NonPermanent_ReturnsExpired di-REWRITE ke null→Aktif; _Permanent_ReturnsPermanent tetap"
    - "E2E #8-12 (anti-resurrection, abandon-vs-graded, concurrent save, timer Standard, cert visibility) ditambahkan ke exam-taking.spec.ts"
    - "Migration=false: dotnet ef migrations list tidak bertambah setelah seluruh phase 382"
    - "ROADMAP Phase 382 Migration field di-update ke false; klaim '1 migration @ 382' + 'notify IT' dihapus dari summary v29.0; STATE dicatat 0-migration"
  artifacts:
    - path: "Models/CertificationManagementViewModel.cs"
      provides: "DeriveCertificateStatus null→Aktif (single-source CERT-01)"
      contains: "return CertificateStatus.Aktif"
    - path: "HcPortal.Tests/CertificateStatusTests.cs"
      provides: "rewritten null→Aktif fact"
      contains: "_NullValidUntil_NonPermanent_ReturnsAktif"
    - path: "HcPortal.Tests/CertAlertConsistencyTests.cs"
      provides: "badge/notif/renewal tally konsisten untuk cert null (tidak undercount/kontradiktif)"
      contains: "CertAlertConsistency"
    - path: "tests/e2e/exam-taking.spec.ts"
      provides: "E2E #8-12 acceptance scenarios"
      contains: "anti-resurrection"
  key_links:
    - from: "Models/CertificationManagementViewModel.cs DeriveCertificateStatus L58-59"
      to: "CertificateStatus.Aktif (was Expired) untuk null non-Permanent"
      via: "single-source helper"
      pattern: "validUntil == null"
    - from: "AdminBaseController/RenewalController/CDPController tally"
      to: "Status enum derive (auto-drop null dari renewal worklist)"
      via: "konsumsi return DeriveCertificateStatus — TIDAK diedit"
      pattern: "Status == CertificateStatus.Expired"
---

<objective>
Fix WSE-11 (CERT-01) sebagai single-source: `DeriveCertificateStatus(null, null/non-Permanent)` return `Aktif` (bukan `Expired`). Karena AdminBase renewal post-filter, Renewal tally, CDP tally semua mengonsumsi `Status` enum yang sama, perbaikan satu helper otomatis mengeluarkan cert null dari worklist renewal + tally — tanpa mengedit consumer (keuntungan single-source, Pattern 7). Plan ini juga menutup phase: rewrite test yang break, tambah CertAlertConsistency, extend e2e #8-12 (acceptance lintas semua REQ phase), tegakkan guard Migration=false, dan update ROADMAP/STATE (flip Migration 382 → false, hapus klaim notify-IT-migration).

Purpose:
- CERT-01: helper L58-59 return `Expired` untuk cert lulus ValidUntil=null → muncul "Expired" di dashboard + masuk worklist renewal + undercount/kontradiksi badge. Flip ke `Aktif`.
- Verifikasi drift: HomeController L116/124/206/215 sudah filter `ValidUntil.HasValue` (null sudah dikecualikan) — JANGAN ubah, verifikasi konsisten.
- E2E #8-12 = acceptance spec untuk seluruh phase (STAT-01/02/SAVE-01/TMR-01/CERT-01).
- Migration=false guard + dokumentasi (D-01-IMPACT milestone-level).

Output: helper fix + test rewrite + CertAlertConsistencyTests + e2e extend + ROADMAP/STATE update. autonomous=false (checkpoint human-verify dashboard visual — manual-only per VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/382-grading-lifecycle-cert/382-CONTEXT.md
@.planning/phases/382-grading-lifecycle-cert/382-RESEARCH.md
@.planning/phases/382-grading-lifecycle-cert/382-PATTERNS.md
@.planning/phases/382-grading-lifecycle-cert/382-VALIDATION.md
@.planning/phases/382-grading-lifecycle-cert/382-01-SUMMARY.md
@.planning/phases/382-grading-lifecycle-cert/382-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Anchors VERIFIED dari source code session ini. -->

enum CertificateStatus (Models/CertificationManagementViewModel.cs:13-18): { Aktif, AkanExpired, Expired, Permanent }

DeriveCertificateStatus (Models/CertificationManagementViewModel.cs:53-65, VERIFIED):
  :53  public static CertificateStatus DeriveCertificateStatus(DateOnly? validUntil, string? certificateType)
  :56-57 if (certificateType == "Permanent") return CertificateStatus.Permanent;   // KEEP
  :58-59 if (validUntil == null) return CertificateStatus.Expired;                  // ◄ FLIP → Aktif
  :60-64 days arithmetic (KEEP)

CERT-01 consumers (read-path — KONSUMSI Status enum, auto-koheren, JANGAN edit logika; verifikasi tak drift):
  Controllers/AdminBaseController.cs:187 DeriveCertificateStatus(a.ValidUntil, null)  // AssessmentSession cert → certificateType null
  Controllers/AdminBaseController.cs:200 .Where(... Status==Expired || Status==AkanExpired)   // worklist renewal — null auto-drop setelah fix
  Controllers/RenewalController.cs:149 DeriveCertificateStatus(a.ValidUntil, null); tally :217/277/300/351; order :262/288/338
  Controllers/CDPController.cs:4069 DeriveCertificateStatus(a.ValidUntil, null); tally :3734-3735/3793-3794
  Controllers/HomeController.cs:116/124 notif (raw ValidUntil.HasValue && < today) — null SUDAH excluded; :206/215 badge (ValidUntil.HasValue) — null SUDAH excluded. JANGAN ubah; verifikasi konsisten.

CertificateStatusTests.cs (VERIFIED L31-36 rewrite target; signature DeriveCertificateStatus(DateOnly?, string?)):
  :32-36 _NullValidUntil_NonPermanent_ReturnsExpired → Assert.Equal(Expired) ◄ REWRITE ke Aktif
  :38-43 _NullValidUntil_Permanent_ReturnsPermanent → KEEP

e2e (tests/e2e/exam-taking.spec.ts) — helpers examTypes.ts (createDefaultPackage, addQuestionViaForm, submitExamTwoStep) + tests/helpers/dbSnapshot.ts. WAJIB `--workers=1`; AD lokal off (`Authentication__UseActiveDirectory=false dotnet run`); SQLBrowser + `lpc:` conn (MEMORY reference_local_e2e_sql_env_fix).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CERT-01 helper flip null→Aktif + REWRITE CertificateStatusTests + CertAlertConsistencyTests</name>
  <files>Models/CertificationManagementViewModel.cs, HcPortal.Tests/CertificateStatusTests.cs, HcPortal.Tests/CertAlertConsistencyTests.cs</files>
  <read_first>
    - Models/CertificationManagementViewModel.cs L48-65 (helper + xmldoc — update doc juga)
    - HcPortal.Tests/CertificateStatusTests.cs L31-43 (rewrite L32-36, keep L38-43)
    - Controllers/AdminBaseController.cs L187/L200 + RenewalController.cs L149/L217 + CDPController.cs L4069/L3734 (verifikasi consumer konsumsi Status enum)
    - 382-PATTERNS.md §DeriveCertificateStatus + Pitfall 4 + Pattern 7
    - 382-RESEARCH.md Pattern 7 (cross-surface map) + §Validation WSE-11
  </read_first>
  <behavior>
    - Helper: `DeriveCertificateStatus(null, null)` → `CertificateStatus.Aktif`; `DeriveCertificateStatus(null, "Permanent")` → `Permanent`; date-arithmetic cases (Aktif/AkanExpired/Expired by days) UNCHANGED.
    - CertAlertConsistency: untuk satu set cert termasuk satu AssessmentSession cert lulus ValidUntil=null, status derive = Aktif → tidak masuk hitungan ExpiredCount/AkanExpiredCount (renewal/CDP tally) dan tidak masuk worklist renewal (AdminBase post-filter). Assert tally tidak meng-count cert null sebagai Expired (anti-undercount/kontradiksi). (Uji terhadap helper + filter-predicate yang sama dipakai consumer; reuse pola predicate-mirror dari test existing bila ada — grep `CertificateStatus.Expired` di HcPortal.Tests.)
  </behavior>
  <action>
**Part A — helper flip.** Di `Models/CertificationManagementViewModel.cs` L58-59, GANTI:
```csharp
if (validUntil == null)
    return CertificateStatus.Expired; // non-Permanent with no expiry → treat as expired (needs renewal)
```
menjadi:
```csharp
if (validUntil == null)
    return CertificateStatus.Aktif; // CERT-01 (D-08): cert lulus tanpa kedaluwarsa = Aktif/Permanen (BUKAN Expired) — single-source, semua consumer ikut via Status enum
```
Update xmldoc L50 ("ValidUntil==null yields Permanent") agar akurat: null non-Permanent → Aktif. JANGAN ubah Permanent-branch L56-57 atau date arithmetic L60-64.

**Part B — rewrite test.** Di `HcPortal.Tests/CertificateStatusTests.cs`, ganti method L31-36:
```csharp
[Fact]
public void DeriveCertificateStatus_NullValidUntil_NonPermanent_ReturnsAktif()
{
    var result = SertifikatRow.DeriveCertificateStatus(null, null);
    Assert.Equal(CertificateStatus.Aktif, result);
}
```
Biarkan `_NullValidUntil_Permanent_ReturnsPermanent` (L38-43) dan Theory (L17-29) apa adanya.

**Part C — CertAlertConsistencyTests.cs (baru).** Buat `HcPortal.Tests/CertAlertConsistencyTests.cs`. Uji bahwa cert lulus ValidUntil=null (certificateType null, seperti AssessmentSession cert) menghasilkan Status=Aktif sehingga predikat tally consumer `Status==Expired || Status==AkanExpired` mengecualikannya. Bentuk minimal: bangun beberapa SertifikatRow (atau panggil DeriveCertificateStatus) termasuk satu null-cert; assert `rows.Count(r => r.Status==CertificateStatus.Expired || r.Status==CertificateStatus.AkanExpired)` TIDAK menghitung cert null. Ini mengunci single-source coherence (Pattern 7) tanpa mengedit controller.

Setelah fix, semua CertificateStatus + CertAlertConsistency facts hijau.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~CertificateStatus|FullyQualifiedName~CertAlertConsistency" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `Models/CertificationManagementViewModel.cs` contains `return CertificateStatus.Aktif;` in the `validUntil == null` branch (no longer `Expired`)
    - `Models/CertificationManagementViewModel.cs` Permanent branch unchanged (`certificateType == "Permanent"` → `Permanent`)
    - `CertificateStatusTests.cs` contains `_NullValidUntil_NonPermanent_ReturnsAktif` asserting `CertificateStatus.Aktif`; `_Permanent_ReturnsPermanent` retained
    - `CertAlertConsistencyTests.cs` exists and asserts cert null is NOT counted as Expired/AkanExpired
    - `dotnet test --filter "CertificateStatus|CertAlertConsistency"` exits 0
    - `dotnet build` exits 0; no `Migrations/` diff
  </acceptance_criteria>
  <done>Helper returns Aktif for null non-Permanent (single-source); test rewritten; consistency test locks consumer coherence; green.</done>
</task>

<task type="auto">
  <name>Task 2: E2E #8-12 acceptance scenarios in exam-taking.spec.ts</name>
  <files>tests/e2e/exam-taking.spec.ts</files>
  <read_first>
    - tests/e2e/exam-taking.spec.ts (existing Flow structure + how it logs in worker/HC, starts/submits exam)
    - tests/e2e/helpers/examTypes.ts (createDefaultPackage, addQuestionViaForm, submitExamTwoStep)
    - tests/helpers/dbSnapshot.ts (DB-assert helper)
    - 382-VALIDATION.md §E2E Acceptance (#8-12 assertions) + §Manual-Only
    - 382-PATTERNS.md §exam-taking.spec.ts + 382-CONTEXT.md §specifics (test #8-12)
  </read_first>
  <action>
Extend `tests/e2e/exam-taking.spec.ts` dengan 5 scenario (mirror Flow existing + reuse helpers). WAJIB `--workers=1`; AD off; snapshot+restore DB per CLAUDE.md Seed Workflow jika seed data. Beri judul yang selectable via `-g`:

- **#8 anti-resurrection (STAT-01):** worker mulai ujian single-answer → AbandonExam (atau seed Status=Abandoned) → POST SubmitExam → assert ditolak (tidak jadi Completed-lulus, tidak ada sertifikat). Ulang untuk Cancelled (HC cancel). Title contains "anti-resurrection".
- **#9 abandon-vs-graded (STAT-02):** worker selesai+graded (Status Completed) → panggil AbandonExam → assert rowsAffected==0 (Status tetap Completed, verdict+cert tetap tampil di Results/Records). Title contains "abandon".
- **#10 concurrent save (SAVE-01):** (integration preferred — boleh delegasikan ke `SaveAnswerConcurrent` real-SQL bila e2e tak praktis; jika e2e: dua SaveAnswer beda opsi untuk (sesi,soal) sama → submit → assert Score dari opsi FINAL). Title contains "concurrent".
- **#11 timer Standard (TMR-01):** seed/atur StartedAt mundur untuk ujian Standard → submit manual jauh-telat → assert ditolak + audit SubmitExamBlocked; submit on-time → assert diterima. Title contains "timer".
- **#12 cert visibility (CERT-01):** worker LULUS normal/PostTest single-answer, generateCertificate=true, ValidUntil dibiarkan kosong → assert Results LULUS + NomorSertifikat + PDF; dashboard CMP/CDP/Renewal menampilkan "Aktif/Permanen" (BUKAN Expired); badge Home konsisten. Title contains "cert visibility". (Bagian visual dashboard lintas-surface = checkpoint manual Task 4.)

Gunakan dbSnapshot untuk DB-assert (Score/Status/ValidUntil) di mana memungkinkan. Catat di SUMMARY scenario mana yang didelegasikan ke xUnit integration (mis. #10).
  </action>
  <verify>
    <automated>cd tests && npx playwright test exam-taking --workers=1 -g "anti-resurrection|abandon|timer|cert visibility"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/exam-taking.spec.ts` contains scenarios with titles matching `anti-resurrection`, `abandon`, `timer`, `cert visibility` (and `concurrent` or a documented delegation to xUnit `SaveAnswerConcurrent`)
    - Playwright run (`--workers=1`, `-g`) for #8/#9/#11/#12 passes locally (with AD off + `lpc:` conn per env notes)
    - DB-assert present for at least: #9 Status==Completed preserved, #12 ValidUntil==null + IsPassed==true
  </acceptance_criteria>
  <done>E2E #8-12 added and green (#10 e2e or delegated to integration); acceptance covers STAT-01/02/SAVE-01/TMR-01/CERT-01.</done>
</task>

<task type="auto">
  <name>Task 3: Migration=false guard + full suite gate + ROADMAP/STATE update (D-01-IMPACT)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 382 (Migration field L1317 + Coverage Validation L1351 "1 migration" + Progress Table L1333 "(MIGRATION)")
    - .planning/STATE.md (v28.0 carry L35 "v29.0 akan +1 migration", push-IT L93 migration notes, v29.0 scope L31 "(+1 migration)")
    - 382-CONTEXT.md D-01-IMPACT + 382-RESEARCH.md Pitfall 5 (migration scaffold guard)
  </read_first>
  <action>
**Part A — Migration=false guard (BLOCKING gate).** Pastikan seluruh phase 382 TIDAK menghasilkan migration baru:
1. Catat baseline jumlah migration: `dotnet ef migrations list` (atau hitung file di `Migrations/`).
2. Setelah semua kode 382 selesai (Plan 01+02+03), jalankan `dotnet ef migrations add _verify_382 --no-build` (atau cek model snapshot). Jika ini MENGHASILKAN file migration dengan operasi non-kosong (selain boilerplate empty Up/Down) → BERHENTI: ada perubahan model yang salah; perbaiki agar perubahan murni logika. HAPUS migration verifikasi (`dotnet ef migrations remove`) setelah konfirmasi.
3. Assert akhir: `git diff --name-only` TIDAK menampilkan file `Migrations/*` atau `*ModelSnapshot.cs`. Ini gate BLOKING — phase tak boleh ditutup bila ada migration ter-scaffold.

**Part B — full suite gate.** `dotnet test --nologo` (full xUnit, semua plan) exits 0, lalu `npx playwright test --workers=1` (e2e relevan) hijau.

**Part C — ROADMAP update.** Di `.planning/ROADMAP.md` §Phase 382:
- Field `**Migration**` (L1317): ganti `TRUE — ...` menjadi `false — SAVE-01 = dedupe last-write-wins in-memory (PackageUserResponse TAK punya diskriminator QuestionType → filtered index tak feasible). Read-final di GradingService + SubmitExam. TIDAK ada migration; TIDAK perlu notify IT migration untuk phase ini. (D-01-IMPACT)`.
- Hapus dari field `Files` L1316 referensi `Data/ApplicationDbContext.cs + new migration (filtered-unique-index)` (tidak dipakai).
- Coverage Validation footer (L1351): ganti `1 migration (Phase 382)` menjadi `0 migration (Phase 382 = dedupe, D-01-IMPACT)`.
- Progress Table (L1333): ubah label `382. Grading / Lifecycle / Cert (MIGRATION)` → `382. Grading / Lifecycle / Cert` (hapus "(MIGRATION)").
- Success Criteria #1 (L1319): biarkan substansinya, tapi pastikan tidak mengklaim "filtered unique index" sebagai mekanisme (jika ada frasa demikian, sesuaikan ke "dedupe read-final"). (Hanya jika frasa itu ada; jangan over-edit.)
- Tambah baris changelog di footer ROADMAP: `*Prev: 2026-06-14 (Phase 382 Migration flipped TRUE→false per D-01-IMPACT — SAVE-01 dedupe last-write-wins, v29.0 total = 0 migration baru, tidak perlu notify IT migration untuk milestone ini.)*`

**Part D — STATE update.** Di `.planning/STATE.md`:
- Baris scope v29.0 (L31): hapus/ubah `382(C) grading/lifecycle/cert (+1 migration)` → `382(C) grading/lifecycle/cert (0 migration — D-01 dedupe)`.
- v28.0 carry (L35): hapus klaim `v29.0 akan +1 migration (filtered-index PackageUserResponse/382)`.
- Tambah catatan singkat di Accumulated Context / Decisions: `[v29.0 / 382 / D-01-IMPACT]: SAVE-01 = dedupe last-write-wins in-memory (NO migration). v29.0 = 0 migration baru. Tidak perlu notify IT migration untuk milestone ini.`
JANGAN advance frontmatter `milestone` (tetap v22.0 pin paralel-safe) kecuali orchestrator instruksikan; ini edit konten body saja.

Catatan: STATE/ROADMAP adalah planning docs — edit konten, bukan kode; tidak memicu migration.
  </action>
  <verify>
    <automated>dotnet test --nologo; git diff --name-only</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` (full xUnit) exits 0
    - `git diff --name-only` shows NO `Migrations/*` and NO `*ModelSnapshot.cs` (Migration=false guard holds)
    - `.planning/ROADMAP.md` Phase 382 `**Migration**` field reads `false` and no longer claims "notify IT" migration; Progress Table no longer says "(MIGRATION)"; Coverage footer says 0 migration
    - `.planning/STATE.md` v29.0 scope/carry no longer claims "+1 migration"; D-01-IMPACT decision recorded
  </acceptance_criteria>
  <done>No migration scaffolded; full suite green; ROADMAP+STATE reflect Migration=false (0 migration v29.0, no IT migration notify).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — cert null "Aktif/Permanen" konsisten lintas dashboard (manual-only)</name>
  <files>(none — visual verification only; no file modified)</files>
  <action>
Checkpoint manual-only (per VALIDATION.md §Manual-Only — verifikasi visual lintas 3 dashboard yang tidak bisa di-assert otomatis penuh). Tidak ada kode/file diubah di task ini; ini verifikasi human atas hasil Task 1 (CERT-01 single-source flip). Executor PAUSE dan tunggu sinyal resume.

Yang dibangun (oleh Task 1): worker LULUS single-answer dengan generateCertificate=true + ValidUntil kosong kini menghasilkan Status=Aktif. Dashboard CMP/CDP/Renewal + badge Home seharusnya konsisten ("Aktif/Permanen", BUKAN "Expired"), dan cert null tidak masuk worklist renewal.

Langkah verifikasi human:
1. Jalankan lokal: `Authentication__UseActiveDirectory=false dotnet run` (lihat http://localhost:5277). (SQLBrowser aktif + `lpc:` conn bila login 500.)
2. Pastikan ada worker LULUS single-answer (Normal/PostTest) dengan sertifikat ValidUntil=null (gunakan e2e #12 / seed; snapshot+restore DB per Seed Workflow).
3. Buka dashboard cert CMP → assert baris worker tsb tampil "Aktif/Permanen", BUKAN "Expired".
4. Buka CDP cert tally + Renewal worklist → assert cert null TIDAK muncul sebagai item renewal / tidak menambah ExpiredCount/AkanExpiredCount.
5. Buka Home (login worker tsb) → assert badge cert & notifikasi tidak undercount/kontradiktif (tidak menandai cert null sebagai expired).
6. Buka Results worker → assert LULUS + NomorSertifikat + PDF dapat diunduh.

Resume signal: ketik "approved" bila semua surface konsisten, atau jelaskan surface mana yang masih menampilkan "Expired"/undercount (gap → buat plan gap-closure).
  </action>
  <verify>
    Manual human verification — tidak ada automated command. Lihat <action> langkah 1-6. Resume: ketik "approved" atau jelaskan surface yang masih "Expired"/undercount.
  </verify>
  <done>Human mengonfirmasi cert null tampil "Aktif/Permanen" konsisten di CMP/CDP/Renewal + badge/notif Home; Results LULUS+PDF; atau gap dicatat untuk gap-closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cert read-path (dashboards/badge/notif) → DeriveCertificateStatus | Single helper derives credential status consumed by 5+ surfaces |
| AssessmentSession cert row (ValidUntil=null) → renewal worklist/tally | Misclassification routes a permanent cert into renewal (false work) or undercounts badges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-382-11 | Information / Integrity (misleading credential state) | `DeriveCertificateStatus` returns Expired for valid no-expiry cert → worker sees "Expired", admin worklist polluted, badge undercount | mitigate | CERT-01 single-source flip null→Aktif; consumers auto-derive via Status enum (Pattern 7); CertAlertConsistency test |
| T-382-12 | Integrity (cross-surface drift) | helper fixed but a consumer independently re-derives status from raw ValidUntil → silent drift | accept | Verified: AdminBase/Renewal/CDP consume Status enum (auto-coherent); HomeController already filters ValidUntil.HasValue (no re-derive). Drift-check task verifies no independent raw re-derivation. No HIGH severity. |

No HIGH-severity unmitigated threats. Grading/lifecycle threats modeled in Plans 01/02.
</threat_model>

<verification>
- `dotnet build` exits 0; `dotnet test --nologo` (full xUnit) exits 0.
- `DeriveCertificateStatus(null, null)` → Aktif; `(null,"Permanent")` → Permanent (tests green).
- e2e #8-12 green (`--workers=1`) or #10 delegated to integration (documented).
- Migration=false guard: `git diff --name-only` shows no `Migrations/*` and no `*ModelSnapshot.cs`.
- ROADMAP Phase 382 Migration=false; STATE v29.0 = 0 migration; no IT migration notify.
- Human checkpoint: dashboard/badge/notif cert-null konsisten "Aktif/Permanen".
</verification>

<success_criteria>
- WSE-11: cert lulus ValidUntil=null tampil konsisten "Aktif/Permanen" semua surface; tidak masuk worklist renewal; badge/notif tidak undercount.
- Single-source: hanya helper diedit; consumer ikut otomatis (verified no drift).
- E2E #8-12 acceptance (lintas REQ phase) green.
- Migration=false dikonfirmasi + ROADMAP/STATE diselaraskan (0 migration v29.0).
</success_criteria>

<output>
After completion, create `.planning/phases/382-grading-lifecycle-cert/382-03-SUMMARY.md`.
</output>
