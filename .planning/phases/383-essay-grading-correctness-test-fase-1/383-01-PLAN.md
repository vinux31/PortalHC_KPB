---
phase: 383-essay-grading-correctness-test-fase-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/AssessmentScoreAggregator.cs
  - HcPortal.Tests/IsQuestionCorrectTests.cs
autonomous: true
requirements: [ECG-01]
must_haves:
  truths:
    - "Helper IsQuestionCorrect(q, responsesForQ) returns true/false/null per soal (MC/MA/Essay)"
    - "Essay benar = EssayScore > 0; EssayScore == 0 = Salah; EssayScore null = null (pending)"
    - "MA non-empty guard active (closes GRD-02): 0 selected → false, never SetEquals-true on empty"
    - "MC/MA branches replicate CMPController DISPLAY-path inline logic byte-for-byte"
  artifacts:
    - path: "Helpers/AssessmentScoreAggregator.cs"
      provides: "public static bool? IsQuestionCorrect(PackageQuestion, IEnumerable<PackageUserResponse>)"
      contains: "public static bool? IsQuestionCorrect"
    - path: "HcPortal.Tests/IsQuestionCorrectTests.cs"
      provides: "Pure unit tests: MC/MA/Essay matrix + reproduction sum"
      contains: "IsQuestionCorrect"
  key_links:
    - from: "HcPortal.Tests/IsQuestionCorrectTests.cs"
      to: "AssessmentScoreAggregator.IsQuestionCorrect"
      via: "direct static call"
      pattern: "AssessmentScoreAggregator\\.IsQuestionCorrect"
---

<objective>
Tambah satu helper murni terpusat `AssessmentScoreAggregator.IsQuestionCorrect(q, responsesForQ) -> bool?` yang menjadi single source of truth correctness per-soal untuk SELURUH display surface (count, Elemen Teknis, Tinjauan, PDF) — kill-drift bug "Nilai Anda 100% tapi 4/6 benar". Plus unit test pure (no DB) yang membuktikan matrix MC/MA/Essay.

Purpose: Menutup ECG-01 (helper) + backlog GRD-02 (MA non-empty guard). Foundation phase — belum ada consumer, tapi semua plan berikut (02/03/04) memanggil helper ini. MC/MA WAJIB replikasi DISPLAY-path inline (`CMPController.cs`) byte-for-byte; hanya cabang Essay yang behavior baru.

Output: 1 method baru di `Helpers/AssessmentScoreAggregator.cs` + 1 file test baru `HcPortal.Tests/IsQuestionCorrectTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-CONTEXT.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-RESEARCH.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md

# CLAUDE.md dev workflow: dotnet build + dotnet test wajib sebelum commit. Branch = ITHandoff (bukan main).

<interfaces>
<!-- Kontrak yang executor butuhkan — sudah diekstrak dari codebase. JANGAN eksplorasi ulang. -->

Existing sibling (Helpers/AssessmentScoreAggregator.cs:24-61) — pola yang ditiru (scoring, points):
```csharp
public static class AssessmentScoreAggregator
{
    public static ScoreAggregateResult Compute(
        IEnumerable<PackageQuestion> questions,
        IEnumerable<PackageUserResponse> responses,
        int passPercentage)
    {
        var respList = responses as IList<PackageUserResponse> ?? responses.ToList();
        // switch (q.QuestionType ?? "MultipleChoice") { MultipleChoice / MultipleAnswer / Essay }
        // NB: Compute MA (L50) = maSelected.SetEquals(maCorrect) — TANPA non-empty guard (scoring).
    }
}
```

Model fields yang dipakai (verified):
- `PackageQuestion`: `Id` (int), `QuestionType` (string?, nullable → default "MultipleChoice"), `Options` (List<PackageOption>), `ScoreValue` (int)
- `PackageOption`: `Id` (int), `IsCorrect` (bool)
- `PackageUserResponse`: `PackageQuestionId` (int), `PackageOptionId` (int?), `EssayScore` (int?), `TextAnswer` (string?)

File `Helpers/AssessmentScoreAggregator.cs` SUDAH punya: `using System.Collections.Generic; using System.Linq; using HcPortal.Models;` (L1-3). Tidak perlu tambah using.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tulis unit test IsQuestionCorrect (RED) — pure, no DB</name>
  <read_first>
    - HcPortal.Tests/AssessmentScoreAggregatorTests.cs (analog EXACT — builder Q/Resp di L20-30, struktur [Fact] L33-45)
    - .planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md (section "IsQuestionCorrectTests" — test matrix + builder verbatim)
  </read_first>
  <behavior>
    Test matrix `IsQuestionCorrect` (ECG-01):
    - MC correct (1 opsi benar terpilih) → true
    - MC incorrect (1 opsi salah terpilih) → false
    - MC unanswered (0 opsi terpilih) → false
    - MA exact (semua opsi benar persis terpilih) → true
    - MA partial-subset (sebagian opsi benar terpilih) → false
    - MA superset (semua benar + 1 salah terpilih) → false
    - MA empty/unanswered (0 terpilih) → false (non-empty guard GRD-02)
    - Essay graded >0 (EssayScore=80) → true
    - Essay graded =0 (EssayScore=0) → false
    - Essay ungraded (EssayScore=null) → null (pending)
    - Reproduksi ECG-02: 4 MC (semua benar) + 2 Essay (EssayScore>0) → loop semua soal, sum (IsQuestionCorrect==true) == 6
  </behavior>
  <files>HcPortal.Tests/IsQuestionCorrectTests.cs</files>
  <action>
    Buat file BARU `HcPortal.Tests/IsQuestionCorrectTests.cs`. COPY builder helper `Q(...)` + `Resp(...)` VERBATIM dari `AssessmentScoreAggregatorTests.cs:20-30`:
    ```csharp
    private static PackageQuestion Q(int id, string type, int scoreValue, params (int optId, bool correct)[] opts) =>
        new PackageQuestion { Id = id, QuestionType = type, ScoreValue = scoreValue,
            Options = opts.Select(o => new PackageOption { Id = o.optId, IsCorrect = o.correct }).ToList() };
    private static PackageUserResponse Resp(int qId, int? optId = null, int? essay = null) =>
        new PackageUserResponse { PackageQuestionId = qId, PackageOptionId = optId, EssayScore = essay };
    ```
    Header file (copy gaya AssessmentScoreAggregatorTests.cs:1-7):
    ```csharp
    using System.Collections.Generic;
    using System.Linq;
    using HcPortal.Helpers;
    using HcPortal.Models;
    using Xunit;
    namespace HcPortal.Tests;
    ```
    Tulis 1 `[Fact]` per baris di <behavior>. Tiap test: Arrange (bangun 1 PackageQuestion via Q + response(s) via Resp ke list), Act (`AssessmentScoreAggregator.IsQuestionCorrect(q, responses)`), Assert (`Assert.True/False/Null` pada `bool?` — gunakan `Assert.Null(result)` untuk pending, `Assert.True(result == true)` / `Assert.False(result == false)` untuk benar/salah).

    Contoh konkret MA superset (penting — bukti guard tidak terlalu permisif):
    ```csharp
    [Fact]
    public void MultipleAnswer_Superset_ReturnsFalse()
    {
        var q = Q(1, "MultipleAnswer", 100, (10, true), (11, true), (12, false));
        var responses = new[] { Resp(1, optId: 10), Resp(1, optId: 11), Resp(1, optId: 12) }; // 2 benar + 1 salah
        var result = AssessmentScoreAggregator.IsQuestionCorrect(q, responses);
        Assert.False(result == true);
    }
    ```
    Contoh konkret Essay pending:
    ```csharp
    [Fact]
    public void Essay_Ungraded_ReturnsNull()
    {
        var q = Q(1, "Essay", 100);
        var responses = new[] { Resp(1, essay: null) }; // EssayScore == null
        Assert.Null(AssessmentScoreAggregator.IsQuestionCorrect(q, responses));
    }
    ```
    Contoh reproduksi ECG-02 (6/6):
    ```csharp
    [Fact]
    public void Repro_4Mc_2GradedEssay_SumsTo6()
    {
        var questions = new[] {
            Q(1,"MultipleChoice",10,(10,true),(11,false)),
            Q(2,"MultipleChoice",10,(20,true),(21,false)),
            Q(3,"MultipleChoice",10,(30,true),(31,false)),
            Q(4,"MultipleChoice",10,(40,true),(41,false)),
            Q(5,"Essay",100), Q(6,"Essay",100)
        };
        var allResponses = new[] {
            Resp(1,optId:10), Resp(2,optId:20), Resp(3,optId:30), Resp(4,optId:40),
            Resp(5,essay:80), Resp(6,essay:50)
        };
        int correct = questions.Count(q =>
            AssessmentScoreAggregator.IsQuestionCorrect(q, allResponses.Where(r => r.PackageQuestionId == q.Id)) == true);
        Assert.Equal(6, correct);
    }
    ```
    JANGAN implement helper di task ini — hanya test (akan RED / compile-fail karena method belum ada). Itu yang diinginkan untuk gate RED.
  </action>
  <verify>
    <automated>dotnet build HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "IsQuestionCorrect" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/IsQuestionCorrectTests.cs` ada (grep `public void Essay_Ungraded_ReturnsNull` match).
    - File berisi ≥11 `[Fact]` (grep `\[Fact\]` count ≥ 11) menutupi MC{3} + MA{4} + Essay{3} + reproduksi{1}.
    - File memanggil `AssessmentScoreAggregator.IsQuestionCorrect` (grep match).
    - Build GAGAL pada titik ini karena `IsQuestionCorrect` belum ada (expected RED) — error compiler menyebut `IsQuestionCorrect` does not exist / no definition. (Bukti test ditulis sebelum implementasi.)
  </acceptance_criteria>
  <done>Test file ada dengan matrix lengkap + reproduksi sum; memanggil helper yang belum ada (RED gate confirmed via compile error menyebut IsQuestionCorrect).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement IsQuestionCorrect helper (GREEN) — MC/MA byte-for-byte, Essay >0</name>
  <read_first>
    - Helpers/AssessmentScoreAggregator.cs (file yang dimodifikasi — Compute L24-61 sebagai pola, ditambah method baru)
    - Controllers/CMPController.cs:2258-2271 (MC/MA DISPLAY-path inline — SUMBER byte-for-byte untuk MC/MA)
    - Controllers/CMPController.cs:2304-2327 (review-off — sumber `if (sel.Count==0) return false` MC)
    - .planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md (target shape verified §4.1, baris 67-94)
  </read_first>
  <action>
    Tambahkan method `IsQuestionCorrect` ke `Helpers/AssessmentScoreAggregator.cs` di dalam class `AssessmentScoreAggregator` (setelah `Compute`, sebelum penutup class brace). Copy SHAPE verbatim dari PATTERNS.md §4.1 / RESEARCH design §4.1:
    ```csharp
    /// <summary>
    /// Phase 383 ECG-01 — Per-question correctness untuk DISPLAY (count "(X/Y benar)", Elemen Teknis,
    /// Tinjauan badge, PDF export). bool?: true=Benar, false=Salah, null=essay belum dinilai (pending).
    /// Single source of truth (kill-drift Phase 363/365/376) — JANGAN recompute correctness inline lagi.
    /// MC/MA me-mirror DISPLAY-path inline CMPController.Results (L2259-2324) byte-for-byte:
    ///   - MultipleChoice: benar iff satu-satunya opsi terpilih ber-IsCorrect; 0 terpilih → false.
    ///   - MultipleAnswer: selected.Count > 0 && selected.SetEquals(correct) — non-empty guard (GRD-02).
    ///     (SetEquals simetris: ≡ correct.SetEquals(selected) di inline L2263.)
    ///   - Essay (D-02): EssayScore.HasValue ? EssayScore.Value > 0 : null.
    /// Pure: hanya System.Linq + HcPortal.Models, sinkron, EF-free, unit-testable. Tidak menyentuh Compute (D-04 formula LOCKED).
    /// </summary>
    public static bool? IsQuestionCorrect(PackageQuestion q, IEnumerable<PackageUserResponse> responsesForQ)
    {
        var list = responsesForQ as IList<PackageUserResponse> ?? responsesForQ.ToList();
        switch (q.QuestionType ?? "MultipleChoice")
        {
            case "MultipleAnswer":
            {
                var selected = list.Where(r => r.PackageOptionId.HasValue).Select(r => r.PackageOptionId!.Value).ToHashSet();
                var correct  = q.Options.Where(o => o.IsCorrect).Select(o => o.Id).ToHashSet();
                return selected.Count > 0 && selected.SetEquals(correct);   // GRD-02 non-empty guard
            }
            case "Essay":
            {
                var essay = list.FirstOrDefault(r => r.PackageQuestionId == q.Id);
                if (essay?.EssayScore.HasValue != true) return null;        // pending
                return essay.EssayScore.Value > 0;                          // D-02
            }
            default: // MultipleChoice
            {
                var sel = list.Where(r => r.PackageOptionId.HasValue).Select(r => r.PackageOptionId!.Value).ToHashSet();
                if (sel.Count == 0) return false;
                var opt = q.Options.FirstOrDefault(o => sel.Contains(o.Id));
                return opt != null && opt.IsCorrect;
            }
        }
    }
    ```
    JANGAN ubah `Compute` (D-04 LOCKED). JANGAN tambah using (sudah lengkap L1-3). MC/MA HARUS sama dengan inline display CMPController (RESEARCH Pitfall 5: helper meniru DISPLAY-path, bukan scoring-path Compute yang tak punya MA guard).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~IsQuestionCorrect" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 error.
    - `dotnet test --filter "FullyQualifiedName~IsQuestionCorrect"` → SEMUA test GREEN (≥11 passed, 0 failed).
    - grep `public static bool\? IsQuestionCorrect` di `Helpers/AssessmentScoreAggregator.cs` match.
    - grep `selected.Count > 0 && selected.SetEquals` match (non-empty guard GRD-02 ada).
    - `Compute` tidak berubah: grep `percentage >= passPercentage` masih ada; tidak ada perubahan pada switch case di dalam `Compute`.
  </acceptance_criteria>
  <done>Helper IsQuestionCorrect ada di AssessmentScoreAggregator.cs; semua unit test hijau; Compute tak berubah; MA non-empty guard verified via test MA empty→false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Helper murni: tidak menerima input user langsung, tidak akses DB, tidak ubah authz. Dipanggil oleh controller yang sudah punya authz (REC-04 / [Authorize]). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-383-01 | Information Disclosure | `IsQuestionCorrect` (pure helper) | accept | Helper tidak query DB / tidak baca user identity — zero attack surface baru. Authz tetap di pemanggil (Plan 02/03), tak disentuh di sini. |
| T-383-02 | Tampering | MA non-empty guard | mitigate | `selected.Count > 0 &&` mencegah empty-set false-positive (GRD-02). Diuji via test MA empty→false. |
</threat_model>

<verification>
- `dotnet build` 0 error (CLAUDE.md dev workflow).
- `dotnet test --filter "FullyQualifiedName~IsQuestionCorrect"` semua hijau.
- Helper EF-free (tidak ada `_context` / `await` / `DbContext` di method baru — grep negatif).
- `Compute` formula D-04 utuh (tidak diedit).
</verification>

<success_criteria>
- `AssessmentScoreAggregator.IsQuestionCorrect` ada, pure, return bool? sesuai D-01a + D-02.
- MA non-empty guard (closes GRD-02) terbukti via unit test.
- MC/MA replikasi DISPLAY-path inline CMPController byte-for-byte (acuan L2259-2324).
- Essay: >0 Benar, =0 Salah, null pending.
- Semua unit test ECG-01 hijau; Compute tak berubah.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/383-essay-grading-correctness-test-fase-1/383-01-SUMMARY.md` (ringkas: method signature final, hasil test, konfirmasi Compute utuh).
</output>
