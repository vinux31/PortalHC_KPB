---
phase: 383-essay-grading-correctness-test-fase-1
plan: 04
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - HcPortal.Tests/EssayFinalizeRecomputeTests.cs
autonomous: true
requirements: [ECG-06]
must_haves:
  truths:
    - "Regression mengunci SubmitEssayScore: persist EssayScore + range guard (score 0..ScoreValue) tanpa ubah kode produksi"
    - "Regression mengunci FinalizeEssayGrading: recompute score termasuk essay (essay-aware) + idempotent (no-op saat Completed)"
    - "Authz [Authorize(Roles=Admin, HC)] di kedua action terkunci (reflection-assert) ATAU didokumentasikan sebagai known gap"
    - "NO production code change — Plan 04 hanya menambah test"
  artifacts:
    - path: "HcPortal.Tests/EssayFinalizeRecomputeTests.cs"
      provides: "Test ECG-06: Submit persist+range + Finalize recompute+idempotent + authz lock"
      contains: "SubmitEssayScore"
  key_links:
    - from: "HcPortal.Tests/EssayFinalizeRecomputeTests.cs"
      to: "AssessmentScoreAggregator.Compute (essay-aware recompute)"
      via: "mirror-data-level (precedent ForwardAggregateAsync)"
      pattern: "AssessmentScoreAggregator\\.Compute"
---

<objective>
Tulis regression test yang MENGUNCI logika Simpan Skor + Selesaikan Penilaian (poin 2 — sudah benar, D-05) tanpa mengubah kode produksi. Extend `HcPortal.Tests/EssayFinalizeRecomputeTests.cs` (real-SQL disposable fixture — WAJIB karena `FinalizeEssayGrading` pakai `ExecuteUpdateAsync` yang EF8 InMemory tak dukung, pelajaran Phase 382). Mirror behavior data-level (precedent file ini — hindari ctor 12-dependency controller).

Purpose: Menutup ECG-06. Mengunci: (1) `SubmitEssayScore` persist EssayScore + range guard 0..ScoreValue; (2) `FinalizeEssayGrading` recompute essay-aware via Compute + idempotent saat Completed; (3) authz attribute via reflection-assert (atau dokumentasikan limitasi mirror).

Output: test baru di `HcPortal.Tests/EssayFinalizeRecomputeTests.cs` ([Trait Category=Integration]). TIDAK ada perubahan kode produksi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-CONTEXT.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-RESEARCH.md
@.planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md

# Reference (local e2e SQL env fix memory): integration fixture pakai Server=localhost\SQLEXPRESS (BUKAN lpc/SQLBrowser override yang dipakai Playwright). Bila SQLEXPRESS absen lokal → skip via --filter "Category!=Integration" (degradasi tercatat, unit Plan 01/02 tetap jalan).
# CLAUDE.md: dotnet test. Branch ITHandoff. JANGAN ubah kode produksi di Plan ini (D-05).

<interfaces>
<!-- Pola yang di-reuse — diekstrak dari file yang sama (EssayFinalizeRecomputeTests.cs). -->

Fixture (EssayFinalizeRecomputeTests.cs:19-52) — REUSE apa adanya (jangan duplikat):
```csharp
public class EssayFinalizeRecomputeFixture : IAsyncLifetime {
    public DbContextOptions<ApplicationDbContext> Options => _options;
    // Server=localhost\SQLEXPRESS;Database=HcPortalDB_Test_{guid}; MigrateAsync; EnsureDeletedAsync on dispose
}
```

Test class existing (L54-60) — TAMBAH method di class ini:
```csharp
[Trait("Category", "Integration")]
public class EssayFinalizeRecomputeTests : IClassFixture<EssayFinalizeRecomputeFixture> {
    private ApplicationDbContext NewCtx() => new ApplicationDbContext(_fixture.Options);
}
```

Seed helper existing (REUSE):
- `SeedUserAsync(ctx)` (L62-68) → returns userId.
- `SeedEssayOnlyAsync(ctx, userId, int? score, int essayScore, string status, int passPercentage=70, int scoreValue=100)` (L71-99) → seed session+pkg+question(Essay)+response(EssayScore)+assignment; returns sessionId.
- `ForwardAggregateAsync(ctx, sessionId)` (L101-114) → mirror FinalizeEssayGrading recompute via AssessmentScoreAggregator.Compute → ScoreAggregateResult.

Production methods yang DIKUNCI (verified, AssessmentAdminController.cs — JANGAN diubah):
- `SubmitEssayScore` (L3458-3487): range guard `if (score < 0 || score > question.ScoreValue)` L3472; persist `response.EssayScore = score; SaveChangesAsync()` L3476-3477; pendingCount via Join Essay + CountAsync(EssayScore==null) L3480-3484. Attribute: [Authorize(Roles="Admin, HC")] L3456, [ValidateAntiForgeryToken] L3457.
- `FinalizeEssayGrading` (L3499+): D-03 no-op saat Status==Completed (L3506); recompute via Compute; ExecuteUpdateAsync L3593. Attribute: [Authorize(Roles="Admin, HC")] L3497, [ValidateAntiForgeryToken] L3498.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock SubmitEssayScore — persist + range guard (mirror data-level)</name>
  <read_first>
    - HcPortal.Tests/EssayFinalizeRecomputeTests.cs (FULL — fixture L19-52, seed L62-99, mirror pattern L101-149, test style L151-214)
    - Controllers/AssessmentAdminController.cs:3455-3487 (SubmitEssayScore — kode yang dikunci, JANGAN diubah)
    - .planning/phases/383-essay-grading-correctness-test-fase-1/383-PATTERNS.md (section EssayFinalizeRecomputeTests EXTEND, baris 254-296)
  </read_first>
  <action>
    Tambah method test ke class `EssayFinalizeRecomputeTests` (EssayFinalizeRecomputeTests.cs). Reuse fixture + `NewCtx()` + `SeedUserAsync` + `SeedEssayOnlyAsync`. Mirror logika `SubmitEssayScore` data-level (precedent file ini hindari ctor 12-dep — RESEARCH Open Question #2). Tambah komentar yang menautkan ke `AssessmentAdminController.SubmitEssayScore` (L3458-3487) supaya drift kode ketahuan.

    Tambah helper mirror (di class, samping ForwardAggregateAsync):
    ```csharp
    // Mirror SubmitEssayScore core (AssessmentAdminController.cs:3460-3477) — persist + range guard.
    // Returns (success, message). Mirror data-level (precedent: hindari 12-dep ctor controller).
    private static async Task<(bool success, string? message)> MirrorSubmitEssayScoreAsync(
        ApplicationDbContext ctx, int sessionId, int questionId, int score)
    {
        var response = await ctx.PackageUserResponses
            .FirstOrDefaultAsync(r => r.AssessmentSessionId == sessionId && r.PackageQuestionId == questionId);
        if (response == null) return (false, "Jawaban tidak ditemukan");
        var question = await ctx.PackageQuestions.FindAsync(questionId);
        if (question == null) return (false, "Soal tidak ditemukan");
        if (score < 0 || score > question.ScoreValue) return (false, $"Skor harus antara 0 dan {question.ScoreValue}"); // range guard L3472
        response.EssayScore = score;
        await ctx.SaveChangesAsync();
        return (true, null);
    }
    ```
    Tulis test:
    ```csharp
    [Fact]
    public async Task SubmitEssayScore_Persists_WhenInRange()  // ECG-06 persist
    {
        await using var ctx = NewCtx();
        var userId = await SeedUserAsync(ctx);
        var sessionId = await SeedEssayOnlyAsync(ctx, userId, score: 0, essayScore: 0,
            status: AssessmentConstants.AssessmentStatus.PendingGrading, scoreValue: 100);
        var q = await ctx.PackageQuestions.FirstAsync();
        var (ok, _) = await MirrorSubmitEssayScoreAsync(ctx, sessionId, q.Id, 80);
        Assert.True(ok);
        await using var verify = NewCtx();
        var resp = await verify.PackageUserResponses.FirstAsync(r => r.AssessmentSessionId == sessionId && r.PackageQuestionId == q.Id);
        Assert.Equal(80, resp.EssayScore);  // persisted
    }

    [Fact]
    public async Task SubmitEssayScore_Rejects_WhenOutOfRange()  // ECG-06 range guard (V5 ASVS)
    {
        await using var ctx = NewCtx();
        var userId = await SeedUserAsync(ctx);
        var sessionId = await SeedEssayOnlyAsync(ctx, userId, score: 0, essayScore: 0,
            status: AssessmentConstants.AssessmentStatus.PendingGrading, scoreValue: 100);
        var q = await ctx.PackageQuestions.FirstAsync();
        var (okHigh, _) = await MirrorSubmitEssayScoreAsync(ctx, sessionId, q.Id, 150); // > ScoreValue
        var (okNeg, _)  = await MirrorSubmitEssayScoreAsync(ctx, sessionId, q.Id, -5);  // < 0
        Assert.False(okHigh);
        Assert.False(okNeg);
    }
    ```
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~SubmitEssayScore" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 error.
    - grep `SubmitEssayScore_Persists_WhenInRange` + `SubmitEssayScore_Rejects_WhenOutOfRange` di EssayFinalizeRecomputeTests.cs match.
    - grep `if (score < 0 || score > question.ScoreValue)` di test (mirror range guard) match.
    - `dotnet test --filter "FullyQualifiedName~SubmitEssayScore"` hijau (bila SQLEXPRESS tersedia). Bila SQLEXPRESS absen lokal → catat di SUMMARY sebagai integration-skip (degradasi tercatat), build tetap 0 error.
    - grep NEGATIF: `Controllers/AssessmentAdminController.cs` SubmitEssayScore tak berubah (`response.EssayScore = score;` masih ada).
  </acceptance_criteria>
  <done>Test SubmitEssayScore persist + range guard ada & hijau (atau integration-skip tercatat bila no SQLEXPRESS); kode produksi tak diubah.</done>
</task>

<task type="auto">
  <name>Task 2: Lock FinalizeEssayGrading recompute-incl-essay + idempotent + authz attribute</name>
  <read_first>
    - HcPortal.Tests/EssayFinalizeRecomputeTests.cs (ForwardAggregateAsync L101-114, test idempotent L167-194, no-side-effects L197-214 — pola)
    - Controllers/AssessmentAdminController.cs:3496-3600 (FinalizeEssayGrading — D-03 no-op L3506, recompute, ExecuteUpdateAsync L3593; attribute L3497-3498) — JANGAN diubah
    - .planning/phases/383-essay-grading-correctness-test-fase-1/383-RESEARCH.md (Open Question #2/#3 — mirror vs invoke; authz reflection)
  </read_first>
  <action>
    Tambah test ke class `EssayFinalizeRecomputeTests`.

    **A — Finalize recompute incl essay (essay-aware):** reuse `ForwardAggregateAsync` (sudah mirror Compute essay-aware). Tambah test campuran MC + essay bila perlu, ATAU minimal kunci bahwa recompute mengikutkan EssayScore:
    ```csharp
    [Fact]
    public async Task Finalize_Recompute_IncludesEssayScore()  // ECG-06 recompute incl essay
    {
        await using var ctx = NewCtx();
        var userId = await SeedUserAsync(ctx);
        // essay graded 80 → recompute harus 80% (essay-aware), bukan 0
        var sessionId = await SeedEssayOnlyAsync(ctx, userId, score: 0, essayScore: 80,
            status: AssessmentConstants.AssessmentStatus.PendingGrading);
        var agg = await ForwardAggregateAsync(ctx, sessionId);
        Assert.Equal(80, agg.Percentage);  // essay ikut dihitung
        Assert.True(agg.IsPassed);
    }
    ```

    **B — Finalize idempotent (no-op saat Completed):** mirror guard D-03 (`Status == Completed` → no-op). Tambah helper mirror jika belum ada, ATAU kunci via re-run aggregate konsisten:
    ```csharp
    [Fact]
    public async Task Finalize_NoOp_WhenAlreadyCompleted()  // ECG-06 idempotent
    {
        await using var ctx = NewCtx();
        var userId = await SeedUserAsync(ctx);
        var sessionId = await SeedEssayOnlyAsync(ctx, userId, score: 80, essayScore: 80,
            status: AssessmentConstants.AssessmentStatus.Completed);
        // mirror D-03: status Completed → finalize tidak mengubah Score (no-op). Re-aggregate konsisten.
        var before = (await ctx.AssessmentSessions.FindAsync(sessionId))!.Score;
        var agg = await ForwardAggregateAsync(ctx, sessionId);
        var after = (await ctx.AssessmentSessions.FindAsync(sessionId))!.Score;
        Assert.Equal(before, after);            // tidak berubah oleh re-aggregate (no write)
        Assert.Equal(80, agg.Percentage);       // re-finalize akan hasilkan nilai sama (idempotent)
    }
    ```

    **C — Authz lock (reflection-assert, RESEARCH OQ#3):** karena mirror data-level TIDAK eksekusi attribute, kunci attribute via reflection pada method controller. Tambah test pure (boleh di class lain non-Integration bila lebih bersih, tapi boleh juga di sini):
    ```csharp
    [Fact]
    public void SubmitAndFinalize_RequireAdminHcAuthorize()  // ECG-06 authz (V4 ASVS)
    {
        var t = typeof(HcPortal.Controllers.AssessmentAdminController);
        foreach (var name in new[] { "SubmitEssayScore", "FinalizeEssayGrading" })
        {
            var m = t.GetMethod(name);
            Assert.NotNull(m);
            var authz = m!.GetCustomAttributes(typeof(Microsoft.AspNetCore.Authorization.AuthorizeAttribute), true)
                .Cast<Microsoft.AspNetCore.Authorization.AuthorizeAttribute>().FirstOrDefault();
            Assert.NotNull(authz);
            Assert.Contains("Admin", authz!.Roles ?? "");
            Assert.Contains("HC", authz!.Roles ?? "");
        }
    }
    ```
    (Bila `GetMethod` ambiguitas overload → pakai `GetMethods().First(m => m.Name == name)` atau spesifikasikan parameter types. Tambah `using System.Linq;` sudah ada di file.)
    Bila reflection-assert authz infeasible (mis. method tidak public / ambiguous tak terselesaikan), DOKUMENTASIKAN di SUMMARY sebagai known gap (mirror tidak menangkap regresi authz) — sesuai RESEARCH OQ#3.

    JANGAN ubah kode produksi (D-05).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~Finalize_Recompute|FullyQualifiedName~Finalize_NoOp|FullyQualifiedName~RequireAdminHc" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` 0 error.
    - grep `Finalize_Recompute_IncludesEssayScore` + `Finalize_NoOp_WhenAlreadyCompleted` + `SubmitAndFinalize_RequireAdminHcAuthorize` match di EssayFinalizeRecomputeTests.cs.
    - Authz test memeriksa `Roles` mengandung "Admin" + "HC" (grep `Assert.Contains("Admin"` + `Assert.Contains("HC"`).
    - `dotnet test` untuk test ini hijau (bila SQLEXPRESS tersedia untuk integration; authz test pure jalan tanpa DB). Authz reflection test HARUS hijau (tidak butuh SQL).
    - grep NEGATIF: `Controllers/AssessmentAdminController.cs` FinalizeEssayGrading tak berubah (`ExecuteUpdateAsync` masih ada, D-03 no-op `session.Status == AssessmentConstants.AssessmentStatus.Completed` masih ada).
  </acceptance_criteria>
  <done>Test Finalize recompute-incl-essay + idempotent + authz reflection-assert ada & hijau (atau authz gap didokumentasikan); kode produksi tak diubah.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC/Admin → SubmitEssayScore / FinalizeEssayGrading (POST) | Aksi menilai/finalize essay. [Authorize(Roles="Admin, HC")] + [ValidateAntiForgeryToken] (existing, tidak diubah). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-383-07 | Elevation of Privilege (non-HC menilai essay) | SubmitEssayScore/FinalizeEssayGrading authz | mitigate (lock, no change) | Reflection-assert mengunci [Authorize(Roles="Admin, HC")] di kedua method (Task 2C). Bila infeasible → known gap terdokumentasi (RESEARCH OQ#3). |
| T-383-08 | Tampering (skor essay di luar range) | SubmitEssayScore range guard | mitigate (lock, no change) | Test mengunci guard `score < 0 \|\| score > ScoreValue` (Task 1). |
| T-383-09 | Tampering (double-finalize korup state) | FinalizeEssayGrading idempotency | mitigate (lock, no change) | Test mengunci no-op saat Status==Completed (Task 2B). |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` (full, incl Integration) hijau bila SQLEXPRESS tersedia; authz reflection test hijau tanpa DB.
- NO production code change — grep konfirmasi AssessmentAdminController SubmitEssayScore/FinalizeEssayGrading utuh.
- Bila SQLEXPRESS absen lokal: integration-skip via `--filter "Category!=Integration"` tercatat di SUMMARY (unit Plan 01/02 + authz test tetap jalan).
</verification>

<success_criteria>
- ECG-06: SubmitEssayScore persist + range guard terkunci; FinalizeEssayGrading recompute-incl-essay + idempotent terkunci; authz attribute terkunci (atau gap terdokumentasi).
- Kode produksi (D-05) TIDAK diubah.
- Test pakai real-SQL disposable fixture (Pitfall 2: ExecuteUpdateAsync tak jalan di InMemory).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/383-essay-grading-correctness-test-fase-1/383-04-SUMMARY.md` (ringkas: test yang ditambah, hasil run, status SQLEXPRESS [tersedia/skip], status authz lock [reflection/gap]).
</output>
