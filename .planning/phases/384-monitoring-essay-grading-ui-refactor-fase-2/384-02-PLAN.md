---
phase: 384-monitoring-essay-grading-ui-refactor-fase-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentMonitoringViewModel.cs
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/EssayGrading.cshtml
  - wwwroot/js/essay-grading.js
autonomous: true
requirements: [UIG-02, UIG-03]
must_haves:
  truths:
    - "GET /Admin/EssayGrading?sessionId=... me-load 1 session + essay items dan render page penilaian per-worker"
    - "Page per-worker me-reuse POST SubmitEssayScore + FinalizeEssayGrading (backend tak diubah) via essay-grading.js"
    - "Selesaikan Penilaian (D-09): on-success update IN-PLACE ke state Selesai TANPA location.reload(), URL tetap /EssayGrading"
    - "Session finalized (Status==Completed && NomorSertifikat!=null) → page read-only: input disabled, tombol Simpan/Selesaikan disabled (D-10)"
    - "GET action ber-authz [Authorize(Roles = \"Admin, HC\")] (IDOR mitigation)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "GET EssayGrading action (clone EssayGradingMap builder single session)"
      contains: "public async Task<IActionResult> EssayGrading(int sessionId"
    - path: "Models/AssessmentMonitoringViewModel.cs"
      provides: "EssayGradingPageViewModel wrapper"
      contains: "class EssayGradingPageViewModel"
    - path: "Views/Admin/EssayGrading.cshtml"
      provides: "Page per-worker (clone kartu essay + header + back-link + antiforgery)"
      contains: "essay-grading-card"
    - path: "wwwroot/js/essay-grading.js"
      provides: "Handler AJAX extract (Simpan Skor + Selesaikan D-09 in-place)"
      contains: ".btn-save-essay-score"
  key_links:
    - from: "Views/Admin/EssayGrading.cshtml"
      to: "wwwroot/js/essay-grading.js"
      via: "@section Scripts <script src>"
      pattern: "js/essay-grading\\.js"
    - from: "wwwroot/js/essay-grading.js"
      to: "/Admin/SubmitEssayScore"
      via: "fetch appUrl"
      pattern: "appUrl\\('/Admin/SubmitEssayScore'\\)"
    - from: "Controllers/AssessmentAdminController.cs EssayGrading"
      to: "EssayGradingPageViewModel"
      via: "return View(model)"
      pattern: "new EssayGradingPageViewModel"
---

<objective>
Bangun page penilaian essay per-worker (UIG-02/UIG-03): (1) ViewModel pembungkus `EssayGradingPageViewModel`, (2) GET action baru `EssayGrading` yang clone single-session essay loader + authz Admin/HC, (3) view baru `Views/Admin/EssayGrading.cshtml` yang CLONE kartu essay existing byte-for-byte, (4) extract handler AJAX ke `wwwroot/js/essay-grading.js` dengan perubahan D-09 (finalize → in-place "Selesai", BUKAN reload) + D-10 read-only.

Purpose: HC menilai essay 1 worker = 1 page (alur rapi), reuse endpoint POST existing tanpa ubah backend.
Output: 1 ViewModel + 1 GET action + 1 view + 1 js file.

⚠ FILE BERSAMA: `Controllers/AssessmentAdminController.cs` juga diedit Phase 383 (sesi paralel). WAJIB baca file live + cari analog by METHOD NAME (`AssessmentMonitoringDetail`, `SubmitEssayScore`, `FinalizeEssayGrading`), BUKAN line number. GET action baru = method baru (append-style sebelah `AssessmentMonitoringDetail`) → risiko konflik rendah.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-RESEARCH.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-PATTERNS.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-UI-SPEC.md
@.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Existing markup (CLONE byte-for-byte), handler (EXTRACT), builder (CLONE single session) -->

EssayGradingItemViewModel (Models/AssessmentMonitoringViewModel.cs):
  int QuestionId; int DisplayNumber; string QuestionText; string? Rubrik;
  string? TextAnswer; int? EssayScore; int ScoreValue; string? ImagePath; string? ImageAlt;

MonitoringSessionViewModel: int Id; string UserFullName; string UserNIP; string Status;
  string? NomorSertifikat; int EssayPendingCount; bool HasManualGrading; DateTime? CompletedAt;

Endpoint kontrak (BACKEND TAK DIUBAH):
  POST /Admin/SubmitEssayScore (sessionId, questionId, score)
    -> { success:true, pendingCount:int, allGraded:bool } | { success:false, message }
  POST /Admin/FinalizeEssayGrading (sessionId)
    -> first: { success:true }
       already Completed: { success:true, alreadyFinalized:true, message, score, isPassed, nomorSertifikat }
       non-PendingGrading: { success:false, message }

Builder yang di-CLONE (AssessmentAdminController, blok "Essay grading items per session"):
  UserPackageAssignments.FirstOrDefault(AssessmentSessionId==sess.Id)
   -> assignment.GetShuffledQuestionIds()
   -> PackageQuestions Where(shuffled.Contains(Id) && QuestionType=="Essay")
   -> PackageUserResponses map by PackageQuestionId
   -> List<EssayGradingItemViewModel>

Finalize gate (REUSE Phase 310 D-02 verbatim):
  isFinalized = session.Status == AssessmentConstants.AssessmentStatus.Completed
                && !string.IsNullOrEmpty(session.NomorSertifikat)

appUrl global helper: _Layout.cshtml:55  ->  fetch(appUrl('/Admin/...'))
antiforgery: <form id="antiforgeryForm" style="display:none">@Html.AntiForgeryToken()</form>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EssayGradingPageViewModel wrapper</name>
  <read_first>
    - Models/AssessmentMonitoringViewModel.cs (full — convention: PascalCase, default-init `= ""` / `= new()`, XML doc; tambah class baru di file ini sebelah MonitoringSessionViewModel/EssayGradingItemViewModel)
  </read_first>
  <action>
    Tambahkan class `EssayGradingPageViewModel` di `Models/AssessmentMonitoringViewModel.cs` (namespace `HcPortal.Models`), mengikuti convention file (PascalCase props, default-init string `= ""`, `List<> = new()`, XML doc `<summary>`). Field:

    ```csharp
    /// <summary>
    /// ViewModel page penilaian essay per-worker (Phase 384 UIG-02/03). Membungkus identitas single session +
    /// essay items + flag finalized (D-10) + 4 nav param untuk back-link ke AssessmentMonitoringDetail (Pitfall 3).
    /// </summary>
    public class EssayGradingPageViewModel
    {
        public int SessionId { get; set; }
        public string UserFullName { get; set; } = "";
        public string UserNIP { get; set; } = "";
        public int EssayPendingCount { get; set; }
        public bool IsFinalized { get; set; }
        public DateTime? CompletedAt { get; set; }
        public List<EssayGradingItemViewModel> EssayItems { get; set; } = new();
        // 4 nav param back-link (tz-safe: ScheduleDate string yyyy-MM-dd)
        public string Title { get; set; } = "";
        public string Category { get; set; } = "";
        public string ScheduleDate { get; set; } = "";
        public string? AssessmentType { get; set; }
    }
    ```

    JANGAN buat ViewModel baru untuk tabel worker-list (Plan 03 reuse `MonitoringSessionViewModel`).
  </action>
  <verify>
    <automated>dotnet build --nologo 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - `Models/AssessmentMonitoringViewModel.cs` berisi `class EssayGradingPageViewModel`
    - Berisi properti `SessionId`, `UserFullName`, `UserNIP`, `EssayPendingCount`, `IsFinalized`, `EssayItems`, `Title`, `Category`, `ScheduleDate`, `AssessmentType`
    - `EssayItems` bertipe `List<EssayGradingItemViewModel>`
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>ViewModel pembungkus type-safe untuk page per-worker tersedia, build hijau.</done>
</task>

<task type="auto">
  <name>Task 2: GET action EssayGrading (clone single-session loader)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs — BACA LIVE FILE (diedit Phase 383 paralel). Cari by METHOD NAME:
        `AssessmentMonitoringDetail` (signature + attribute [HttpGet][Authorize(Roles="Admin, HC")] + blok "Essay grading items per session" builder + ViewBag.AssessmentType + ViewBag.BackUrl)
        `SubmitEssayScore` / `FinalizeEssayGrading` (authz attribute verbatim — JANGAN ubah)
    - Controllers/AdminBaseController.cs (class [Authorize] + [Route("Admin/[action]")] routing — confirm action otomatis route /Admin/EssayGrading)
    - Models/AssessmentMonitoringViewModel.cs (EssayGradingPageViewModel dari Task 1)
    - Constants/AssessmentConstants.cs (AssessmentStatus.Completed literal — untuk gate isFinalized)
  </read_first>
  <action>
    Tambah GET action BARU `EssayGrading` di `AssessmentAdminController` (letakkan sebelah `AssessmentMonitoringDetail`, sebagai method append baru — hindari menyentuh body method existing agar tak konflik dengan edit Phase 383). Signature + authz VERBATIM:

    ```csharp
    [HttpGet]
    [Authorize(Roles = "Admin, HC")]
    public async Task<IActionResult> EssayGrading(
        int sessionId, string title, string category, DateTime scheduleDate, string? assessmentType = null)
    {
        var session = await _context.AssessmentSessions
            .Include(a => a.User)
            .FirstOrDefaultAsync(a => a.Id == sessionId);
        if (session == null || !session.HasManualGrading)
        {
            TempData["Error"] = "Sesi penilaian essay tidak ditemukan.";
            return RedirectToAction("AssessmentMonitoringDetail",
                new { title, category, scheduleDate, assessmentType });
        }

        // CLONE single-session essay loader (dari builder "Essay grading items per session" di AssessmentMonitoringDetail)
        var assignment = await _context.UserPackageAssignments
            .FirstOrDefaultAsync(a => a.AssessmentSessionId == session.Id);
        var items = new List<EssayGradingItemViewModel>();
        if (assignment != null)
        {
            var shuffled = assignment.GetShuffledQuestionIds();
            var essayQs = await _context.PackageQuestions
                .Where(q => shuffled.Contains(q.Id) && q.QuestionType == "Essay")
                .ToListAsync();
            var essayRespMap = await _context.PackageUserResponses
                .Where(r => r.AssessmentSessionId == session.Id &&
                       essayQs.Select(q => q.Id).Contains(r.PackageQuestionId))
                .ToDictionaryAsync(r => r.PackageQuestionId);
            items = essayQs.Select((q, idx) => new EssayGradingItemViewModel
            {
                QuestionId    = q.Id,
                DisplayNumber = idx + 1,
                QuestionText  = q.QuestionText ?? "",
                Rubrik        = q.Rubrik,
                TextAnswer    = essayRespMap.TryGetValue(q.Id, out var resp) ? resp.TextAnswer : null,
                EssayScore    = essayRespMap.TryGetValue(q.Id, out var resp2) ? resp2.EssayScore : null,
                ScoreValue    = q.ScoreValue,
                ImagePath     = q.ImagePath,
                ImageAlt      = q.ImageAlt
            }).ToList();
        }

        var essayPendingCount = items.Count(i => i.EssayScore == null);
        var isFinalized = session.Status == AssessmentConstants.AssessmentStatus.Completed
                          && !string.IsNullOrEmpty(session.NomorSertifikat);

        var model = new EssayGradingPageViewModel
        {
            SessionId         = session.Id,
            UserFullName      = session.User?.FullName ?? "(Nama tidak tersedia)",
            UserNIP           = session.User?.NIP ?? "",
            EssayPendingCount = essayPendingCount,
            IsFinalized       = isFinalized,
            CompletedAt       = session.CompletedAt,
            EssayItems        = items,
            Title             = title,
            Category          = category,
            ScheduleDate      = scheduleDate.ToString("yyyy-MM-dd"),
            AssessmentType    = assessmentType
        };
        return View(model);
    }
    ```

    Catatan: confirm nama properti `User.FullName` / `User.NIP` dari read_first (samakan dengan yang dipakai builder existing / MonitoringSessionViewModel mapping). Confirm `GetShuffledQuestionIds()` exists di UserPackageAssignment (builder existing pakai). JANGAN tulis query ad-hoc baru — clone builder agar shuffle-aware + response-join benar.
  </action>
  <verify>
    <automated>dotnet build --nologo 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` berisi `public async Task<IActionResult> EssayGrading(int sessionId`
    - Action ber-attribute `[Authorize(Roles = "Admin, HC")]` dan `[HttpGet]`
    - Berisi guard `session == null || !session.HasManualGrading` → RedirectToAction `AssessmentMonitoringDetail`
    - Berisi `GetShuffledQuestionIds()` + `QuestionType == "Essay"` (clone builder, BUKAN query ad-hoc)
    - Berisi `isFinalized` gate `Status == AssessmentConstants.AssessmentStatus.Completed && !string.IsNullOrEmpty(session.NomorSertifikat)`
    - Berisi `return View(model)` dengan `new EssayGradingPageViewModel`
    - Method existing `AssessmentMonitoringDetail` / `SubmitEssayScore` / `FinalizeEssayGrading` body TIDAK diubah
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>GET action EssayGrading ada, authz Admin/HC, clone builder single session, build hijau, endpoint existing tak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 3: View EssayGrading.cshtml (clone kartu essay byte-for-byte) + JS extract essay-grading.js</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml baris 381-489 (kartu essay + finalizeSection + tooltip wrapper read-only + antiforgeryForm — CLONE byte-for-byte struktur selector)
    - Views/Admin/AssessmentMonitoringDetail.cshtml baris 53-68 (breadcrumb + back-link pola)
    - Views/Admin/AssessmentMonitoringDetail.cshtml baris 1450-1558 (showAlert helper + handler .btn-save-essay-score + .btn-finalize-grading — EXTRACT, D-09 ubah location.reload)
    - Views/Admin/AssessmentMonitoringDetail.cshtml baris 1225-1240 (pola @section Scripts + _ImageLightboxModal partial include)
    - wwwroot/js/edit-peserta-answers.js (pola file extract: DOMContentLoaded guard + early-return)
    - Views/Shared/_QuestionImage.cshtml (signature partial: ImagePath/ImageAlt/Cap)
    - Views/Shared/_Layout.cshtml baris 54-56 (appUrl helper)
    - Models/AssessmentMonitoringViewModel.cs (EssayGradingPageViewModel — model view ini)
  </read_first>
  <action>
    **Bagian A — `Views/Admin/EssayGrading.cshtml` (NEW):**
    `@model HcPortal.Models.EssayGradingPageViewModel`. Layout default (sama dengan view Admin lain). Struktur (urutan):

    1. Header `<div class="container-fluid px-4 py-4">` (pola `:36`). TempData Success/Error alert (clone `:38-51`).
    2. Breadcrumb (pola `:55-61`): Kelola Data → Assessment Monitoring → `@Model.UserFullName`.
    3. Back-link (pola `:64-67`) — carry 4 nav param (Pitfall 3):
       ```cshtml
       <div class="mb-4">
         <a href="@Url.Action("AssessmentMonitoringDetail", new { title = Model.Title, category = Model.Category, scheduleDate = Model.ScheduleDate, assessmentType = Model.AssessmentType })"
            class="btn btn-outline-secondary btn-sm">
           <i class="bi bi-arrow-left me-1"></i>Kembali ke Monitoring
         </a>
       </div>
       ```
    4. Identitas worker:
       ```cshtml
       <h2 class="fw-bold mb-1">@Model.UserFullName</h2>
       <p class="text-muted mb-4"><i class="bi bi-person-badge me-1"></i>NIP: @Model.UserNIP</p>
       ```
    5. `@foreach (var essayItem in Model.EssayItems)` → CLONE kartu essay `:407-446` MEMPERTAHANKAN selector PERSIS (Pitfall 1 — kritis). Ganti referensi `session.Id` → `Model.SessionId`, `session.X` → `Model.X`. Tambah `@(Model.IsFinalized ? "disabled" : "")` pada `<input class="essay-score-input">` (D-10) dan bungkus tombol "Simpan Skor" dengan `@if (!Model.IsFinalized) { ... }` (D-10 hide). MANDATORY selectors yang HARUS persis:
       - `class="card shadow-sm mb-3 essay-grading-card" id="essay_@essayItem.QuestionId"`
       - badge `id="badge_@(Model.SessionId)_@essayItem.QuestionId"` class `badge @(EssayScore.HasValue ? "bg-success" : "bg-secondary") essay-status-badge` text "Sudah Dinilai"/"Belum Dinilai"
       - `@await Html.PartialAsync("_QuestionImage", new { ImagePath = essayItem.ImagePath, ImageAlt = essayItem.ImageAlt, Cap = 240 })`
       - panel jawaban `bg-light` + "Jawaban Pekerja:" + `@(essayItem.TextAnswer ?? "(tidak ada jawaban)")` (Razor auto-encode — JANGAN @Html.Raw)
       - rubrik collapse (clone `:421-430`)
       - `<input type="number" class="form-control essay-score-input" min="0" max="@essayItem.ScoreValue" style="max-width:80px" value="@essayItem.EssayScore" data-question-id="@essayItem.QuestionId" data-session-id="@Model.SessionId" @(Model.IsFinalized ? "disabled" : "") />`
       - tombol `class="btn btn-primary btn-sm btn-save-essay-score" data-question-id data-session-id` text "Simpan Skor" — di dalam `@if (!Model.IsFinalized)`
    6. Section finalize — CLONE `:448-476` + read-only gate D-10 (tooltip wrapper `:460-468`):
       - `<div id="finalizeSection_@Model.SessionId" style="display: @(Model.EssayPendingCount == 0 ? "block" : "none")">`
       - tooltipText (clone `:454-456` pakai `Model.CompletedAt`)
       - `@if (Model.IsFinalized)` → tombol `.btn-finalize-grading` `disabled` + wrap `<span data-bs-toggle="tooltip" title="@tooltipText" style="display:inline-block;">` `style="pointer-events:none;"`; `else` → tombol `.btn-finalize-grading` aktif. Text "Selesaikan Penilaian" + `bi-check-circle`.
    7. Antiforgery form (REUSE `:484` verbatim): `<form id="antiforgeryForm" style="display:none">@Html.AntiForgeryToken()</form>`
    8. `@await Html.PartialAsync("_ImageLightboxModal")` (untuk lightbox _QuestionImage, pola `:1230`).
    9. `@section Scripts { <script src="~/js/essay-grading.js" asp-append-version="true"></script> }` (pola include js + tooltip init bila perlu di js).

    **Bagian B — `wwwroot/js/essay-grading.js` (NEW):**
    Extract handler dari `:1450-1558`. Struktur file pola `edit-peserta-answers.js`:
    ```javascript
    document.addEventListener("DOMContentLoaded", () => {
      if (!document.querySelector('.essay-grading-card')) return;  // guard: hanya page essay grading
      // ... handlers + showAlert
    });
    ```
    - Bawa `showAlert(type, icon, message)` helper (clone `:1452-1468`).
    - Handler `.btn-save-essay-score` — REUSE `:1472-1517` apa adanya (fetch `appUrl('/Admin/SubmitEssayScore')`, token dari `input[name="__RequestVerificationToken"]`, on success update `#badge_{sid}_{qid}` → `bg-success` "Sudah Dinilai", bila `allGraded` show `finalizeSection_{sid}`).
    - Handler `.btn-finalize-grading` — clone `:1520-1558` TAPI **D-09 PERUBAHAN**: ganti `location.reload()` (`:1547`) jadi update IN-PLACE (TANPA redirect):
      - cabang `alreadyFinalized` (`:1537-1544`) DIPERTAHANKAN (showAlert info no-op).
      - cabang `data.success` (finalize pertama) GANTI `location.reload()` dengan:
        - tombol finalize ini → `disabled = true`, wrap state hijau "Selesai" (boleh ubah textContent jadi state selesai atau tambah class — minimal: tetap disabled),
        - semua `document.querySelectorAll('.essay-score-input').forEach(i => i.disabled = true)`,
        - semua `document.querySelectorAll('.btn-save-essay-score').forEach(b => { b.disabled = true; })` (hide/disable),
        - `showAlert('success', 'bi-check-circle-fill', '<strong>Berhasil:</strong> Penilaian telah diselesaikan.')`,
        - TIDAK ada `location.reload()` / redirect.
      - cabang error (`:1548-1551`) DIPERTAHANKAN (showAlert danger).
    - URL WAJIB via `appUrl('/Admin/...')` (sub-path `/KPB-PortalHC`).
    - (Opsional) inisialisasi Bootstrap tooltip untuk wrapper read-only: `document.querySelectorAll('[data-bs-toggle="tooltip"]').forEach(el => new bootstrap.Tooltip(el))` di dalam guard.

    Setelah ini, `essay-grading.js` HANYA di-reference oleh `EssayGrading.cshtml` (Plan 03 menghapus handler inline dari monitoring).
  </action>
  <verify>
    <automated>dotnet build --nologo 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - `Views/Admin/EssayGrading.cshtml` ada, `@model HcPortal.Models.EssayGradingPageViewModel`
    - Berisi `class="...essay-grading-card"`, `class="...essay-score-input"`, `id="badge_@(Model.SessionId)_@essayItem.QuestionId"`, `id="finalizeSection_@Model.SessionId"`, `btn-save-essay-score`, `btn-finalize-grading`
    - Berisi `@Url.Action("AssessmentMonitoringDetail"` dengan 4 param + text "Kembali ke Monitoring"
    - Berisi `<h2 class="fw-bold mb-1">@Model.UserFullName</h2>` + `NIP: @Model.UserNIP`
    - Berisi `<form id="antiforgeryForm"` + `@Html.AntiForgeryToken()`
    - Input skor berisi `@(Model.IsFinalized ? "disabled" : "")`; tombol Simpan Skor dibungkus `@if (!Model.IsFinalized)` (D-10)
    - TIDAK ada `@Html.Raw` pada TextAnswer/QuestionText/Rubrik
    - `wwwroot/js/essay-grading.js` ada, berisi `.btn-save-essay-score`, `.btn-finalize-grading`, `appUrl('/Admin/SubmitEssayScore')`, `appUrl('/Admin/FinalizeEssayGrading')`
    - `essay-grading.js` TIDAK berisi `location.reload()` (D-09 — diganti in-place); berisi `essay-score-input` disable + `showAlert`
    - `@section Scripts` di view berisi `js/essay-grading.js`
    - `dotnet build` exit 0
  </acceptance_criteria>
  <done>Page per-worker (view) + handler js terpisah siap; selector clone byte-for-byte; D-09 in-place + D-10 read-only ter-encode; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /Admin/EssayGrading | sessionId dari query string (untrusted) mengakses grading essay 1 worker |
| browser → POST SubmitEssayScore/FinalizeEssayGrading | AJAX state-changing (existing, antiforgery wajib) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-384-IDOR | Information Disclosure / Elevation | GET EssayGrading(sessionId) expose jawaban + grading worker via ?sessionId= | mitigate | `[Authorize(Roles = "Admin, HC")]` VERBATIM (samakan SubmitEssayScore/FinalizeEssayGrading/AssessmentMonitoringDetail). Admin/HC = full-access tier (HC berwenang nilai semua worker — no per-user ownership check). GET read-only, 0 DB write baru. Acceptance: non-Admin/HC → 403/redirect login. |
| T-384-CSRF | Tampering | POST SubmitEssayScore/FinalizeEssayGrading dari page baru | mitigate | Page render `<form id="antiforgeryForm">@Html.AntiForgeryToken()</form>`; js kirim `__RequestVerificationToken`. Endpoint existing `[ValidateAntiForgeryToken]` TAK diubah. Acceptance: POST tanpa token ditolak (perilaku existing dipertahankan). |
| T-384-RO | Tampering | submit/finalize ke sesi Completed (re-grade) | mitigate (defense-in-depth) | D-10 render input disabled + tombol disabled client-side; backend FinalizeEssayGrading no-op friendly saat Completed (existing). |
| T-384-XSS | Tampering/Info | TextAnswer/QuestionText/Rubrik di kartu clone | mitigate | Razor auto-encode (`@essayItem.TextAnswer`), JANGAN `@Html.Raw`. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test` (non-Integration) tetap hijau (baseline ≥314) — backend endpoint tak diubah, hanya method GET baru + ViewModel.
- grep selector preserved: `essay-grading-card`, `essay-score-input`, `badge_`, `finalizeSection_`, `btn-save-essay-score`, `btn-finalize-grading` ada di `EssayGrading.cshtml`.
- grep `essay-grading.js` TIDAK mengandung `location.reload` (D-09).
- grep authz `[Authorize(Roles = "Admin, HC")]` ada di method `EssayGrading`.
- Runtime/e2e penuh ditunda ke Plan 04 (Razor dynamic → Playwright wajib, Phase 354). Plan ini: build + grep + unit suite.
</verification>

<success_criteria>
- GET `/Admin/EssayGrading?sessionId=...` load + render page per-worker (UIG-02).
- Page reuse `SubmitEssayScore` + `FinalizeEssayGrading` + `EssayGradingItemViewModel`, tombol "Selesaikan Penilaian" ada (UIG-03).
- D-09 in-place "Selesai" (no reload) + D-10 read-only finalized ter-encode.
- Authz Admin/HC + antiforgery terpasang (IDOR + CSRF mitigated).
- Backend endpoint existing TIDAK diubah; build + unit suite hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/384-monitoring-essay-grading-ui-refactor-fase-2/384-02-SUMMARY.md`
</output>
