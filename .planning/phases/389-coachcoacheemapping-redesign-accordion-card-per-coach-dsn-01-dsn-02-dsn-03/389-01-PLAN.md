---
phase: 389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/coachcoacheemapping-389.spec.ts
autonomous: true
requirements: [DSN-01, DSN-02, DSN-03]
tags: [ui, razor, playwright, accordion, parity]

must_haves:
  truths:
    - "Spec file coachcoacheemapping-389.spec.ts ada dgn 14 test (V-01..V-14) ber-label grep yang cocok dgn VALIDATION.md"
    - "Spec login admin via loginAny + navigasi /Admin/CoachCoacheeMapping (template sibling 388)"
    - "Spec compile/parse tanpa error (npx playwright test --list mendaftarkan 14 test)"
  artifacts:
    - path: "tests/e2e/coachcoacheemapping-389.spec.ts"
      provides: "Playwright parity spec V-01..V-14 (smoke + structural, Nyquist safeguard)"
      min_lines: 120
      contains: "Phase 389 — CoachCoacheeMapping accordion parity"
  key_links:
    - from: "tests/e2e/coachcoacheemapping-389.spec.ts"
      to: "tests/helpers/accounts.ts"
      via: "import { accounts, AccountKey }"
      pattern: "from '\\.\\./helpers/accounts'"
    - from: "tests/e2e/coachcoacheemapping-389.spec.ts"
      to: "/Admin/CoachCoacheeMapping"
      via: "page.goto in beforeEach"
      pattern: "/Admin/CoachCoacheeMapping"
---

<objective>
Buat spec Playwright parity `tests/e2e/coachcoacheemapping-389.spec.ts` berisi 14 test (V-01..V-14) yang
memetakan DSN-01/02/03 + smoke parity DSN-06, SEBELUM markup di-rewrite (Plan 02). Ini deliverable Wave 0
(Nyquist safeguard) — pelajaran Phase 354: a11y/Razor dinamis WAJIB di-assert runtime, grep+build TAK cukup.

Pada plan ini spec ditulis penuh terhadap markup TARGET (accordion card). Karena markup lama masih tabel,
sebagian besar test akan RED saat dijalankan sekarang — itu BENAR (test-first). Spec menjadi hijau setelah
Plan 02 me-rewrite markup. Tugas plan ini HANYA menulis spec yang valid & parse-able; jangan menjalankan
full-green di sini (markup belum ada).

Purpose: Kontrak verifikasi runtime tersedia lebih dulu sehingga task markup Plan 02 punya `<automated>` nyata.
Output: 1 file spec baru, parse OK, 14 test terdaftar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-VALIDATION.md
@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-RESEARCH.md
@.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-PATTERNS.md

<interfaces>
<!-- Kontrak yang dipakai spec — extracted dari repo. Executor pakai langsung, tak perlu eksplorasi. -->

From tests/helpers/accounts.ts:
```typescript
export const accounts = {
  admin: { email: 'admin@pertamina.com', password: '123456', role: 'Admin' },
  // ... lainnya
} as const;
export type AccountKey = keyof typeof accounts;
```

From tests/e2e/coachworkload-388.spec.ts (TEMPLATE — salin scaffold login + describe + data-skip):
```typescript
import { test, expect, Page } from '@playwright/test';
import { accounts, AccountKey } from '../helpers/accounts';
async function loginAny(page: Page, accountKey: AccountKey) {
  const { email, password } = accounts[accountKey];
  await page.goto('/Account/Login');
  await page.fill('input[name="email"]', email);
  await page.fill('input[name="password"]', password);
  await Promise.all([
    page.waitForURL(url => !url.toString().includes('/Account/Login'), { timeout: 15_000 }),
    page.click('button[type="submit"]'),
  ]);
}
// data-skip idiom (388 L70-73):
//   const n = await items.count();
//   test.skip(n === 0, 'no data — parity dikunci Phase 390');
// filter submit idiom (388 L103-122): Promise.all([page.waitForURL(/section=/), button.click()])
```

Target page markup contract (yg akan dirender Plan 02 — assert TERHADAP ini):
- H2 page: text "Coach-Coachee Mapping" (existing L43-45).
- Per coach: `<div class="card border-0 shadow-sm mb-3">`; header `.card-header` clickable
  `role="button"` ATAU `<button>`, `data-bs-toggle="collapse"`, `data-bs-target="#collapse-N"`,
  `aria-controls="collapse-N"`, `aria-expanded="false"` default; berisi `.avatar-initial` + nama coach +
  `.badge` (bg-info/bg-warning/bg-danger ikut threshold <5/>=5/>=8).
- Body collapse: `<div class="collapse" id="collapse-N">` (TANPA `show` default closed, TANPA data-bs-parent).
- Mini-table: `thead th` = 9 (Nama, NIP, label0, label1, Jabatan, Proton Track, Status, Mulai, Aksi) — TANPA
  "Coachee Aktif". Baris `<tr data-mapping-id="N">`.
- Toolbar: "Tambah Mapping" = `.btn-primary` solo, TANPA atribut `onclick`; 3 tombol Excel dalam `.btn-group`,
  semua `.btn-sm`; klik "Tambah Mapping" → `#assignModal` visible; klik Edit → `#editModal` visible.
- Modals existing (parity): `#assignModal`, `#editModal`, `#deleteModal`, `#editCoacheeName`.
- AJAX endpoint intercept: `**/Admin/CoachCoacheeMappingDeletePreview*` (confirmDelete via appUrl).
- Filter: dropdown `select[name="section"]` + submit → URL `section=`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold spec + tulis V-01..V-09 (struktur card/collapse/toolbar — DSN-01/02/03)</name>
  <read_first>
    - tests/e2e/coachworkload-388.spec.ts (template scaffold login + describe + data-skip + filter idiom — L1-122)
    - tests/helpers/accounts.ts (accounts.admin = admin@pertamina.com / 123456)
    - tests/playwright.config.ts (baseURL localhost:5277, fullyParallel:false, project chromium)
    - .planning/phases/389-.../389-VALIDATION.md (§Per-Task Verification Map V-01..V-09 + §Concrete Playwright Parity Test Plan 1-8,11,12)
    - .planning/phases/389-.../389-PATTERNS.md (Excerpt H scaffold, Excerpt I data-skip, Excerpt J filter)
  </read_first>
  <files>tests/e2e/coachcoacheemapping-389.spec.ts</files>
  <action>
    Buat file baru `tests/e2e/coachcoacheemapping-389.spec.ts`. Mulai dgn komentar header CARA RUN (salin gaya
    388 L8-15): app TIDAK auto-start → `dotnet run` dgn `Authentication__UseActiveDirectory=false` dulu, lalu
    `cd tests; npx playwright test coachcoacheemapping-389 --workers=1` (`--workers=1` WAJIB — NTLM/shared-mem SQL).
    Tambah catatan: "Sebagian test RED hingga Plan 02 me-rewrite markup (test-first). Mutasi data PENUH = Phase 390."

    Import `{ test, expect, Page }` + `{ accounts, AccountKey }`. Salin `loginAny` verbatim dari 388.
    `test.describe('Phase 389 — CoachCoacheeMapping accordion parity (DSN-01/02/03)', () => { ... })` dgn
    `beforeEach`: `loginAny(page,'admin')` → `page.goto('/Admin/CoachCoacheeMapping')` → assert
    `page.locator('h2', { hasText: 'Coach-Coachee Mapping' })` visible.

    Tulis 9 test dgn NAMA mengandung grep-label PERSIS dari VALIDATION.md (case-sensitive substring):
    - V-01 `'card per coach + header'`: `const cards = page.locator('.card.shadow-sm')`; `expect(await cards.count()).toBeGreaterThan(0)`;
      kartu pertama punya `.avatar-initial` visible + nama coach (text non-kosong). Skip bila count==0 (`test.skip(cardCount===0,'no coach group data')`).
    - V-02 `'badge threshold'`: tiap card-header punya `.badge`; ambil text angka + className via `evaluate`, assert:
      n<5 → mengandung `bg-info`; 5<=n<8 → `bg-warning`; n>=8 → `bg-danger`. (Korelasi angka↔kelas; bila tak ada card → skip.)
    - V-03 `'default closed'`: `expect(await page.locator('.collapse.show').count()).toBe(0)`; tiap card-header
      `getAttribute('aria-expanded')` == `'false'`.
    - V-04 `'collapse buka tutup'`: klik card-header pertama → `#collapse-0` `toBeVisible()` + header `aria-expanded`=='true';
      klik lagi → `#collapse-0` `not.toBeVisible()` (tunggu transisi) + `aria-expanded`=='false'.
    - V-05 `'independent multi-open'`: `test.skip(cardCount<2,'need >=2 coach groups')`; buka header0 lalu header1 →
      KEDUA `#collapse-0` & `#collapse-1` visible bersamaan (bukti no data-bs-parent).
    - V-06 `'9 kolom'`: buka card pertama → `thead th` count == 9; assert tak ada `th` ber-text 'Coachee Aktif'.
    - V-07 `'a11y header toggle'`: card-header pertama tagName=='BUTTON' ATAU `getAttribute('role')`=='button';
      `getAttribute('aria-controls')` == id body collapse; `focus()` → `keyboard.press('Enter')` buka →
      `keyboard.press('Space')` toggle (assert aria-expanded berubah). (Phase 354 lesson.)
    - V-08 `'toolbar seragam'`: `page.locator('button:has-text("Tambah Mapping")')` punya kelas `btn-primary`;
      `page.locator('.btn-group')` berisi 3 tombol Excel; semua tombol toolbar Excel punya kelas `btn-sm`.
    - V-09 `'tambah mapping buka modal'`: tombol "Tambah Mapping" `getAttribute('onclick')` == null atau ''
      (dead onclick hilang); klik → `#assignModal` `toBeVisible()`.

    Gunakan idiom `test.skip` (388 L70-73) untuk SETIAP test yang butuh data coach (card count 0 → skip, jangan fail).
    JANGAN hardcode path AJAX; JANGAN ubah baseURL. Bahasa pesan skip = Bahasa Indonesia ringkas.
  </action>
  <verify>
    <automated>cd tests && npx playwright test coachcoacheemapping-389 --list</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/coachcoacheemapping-389.spec.ts` ada.
    - `cd tests && npx playwright test coachcoacheemapping-389 --list` exit 0 dan mendaftarkan minimal 9 test (parse OK).
    - `grep -c "test('" tests/e2e/coachcoacheemapping-389.spec.ts` >= 9 (atau `test(` ber-arrow).
    - Spec mengandung substring grep-label: `card per coach + header`, `badge threshold`, `default closed`,
      `collapse buka tutup`, `independent multi-open`, `9 kolom`, `a11y header toggle`, `toolbar seragam`,
      `tambah mapping buka modal` (verify: `grep -o "<label>" file` mengembalikan tiap label sekali).
    - `grep "from '../helpers/accounts'" tests/e2e/coachcoacheemapping-389.spec.ts` ketemu.
    - `grep "/Admin/CoachCoacheeMapping" tests/e2e/coachcoacheemapping-389.spec.ts` ketemu.
    - `grep "workers=1" tests/e2e/coachcoacheemapping-389.spec.ts` ketemu (run note di header komentar).
  </acceptance_criteria>
  <done>Spec scaffold + V-01..V-09 tertulis, parse OK, 9 test terdaftar, grep-label cocok VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Tulis V-10..V-14 (smoke parity DSN-06 — edit modal / delete row / aksi branch / ajax appUrl / filter)</name>
  <read_first>
    - tests/e2e/coachcoacheemapping-389.spec.ts (file dari Task 1 — tambahkan test di dalam describe yang sama)
    - tests/e2e/coachworkload-388.spec.ts (idiom filter submit L103-122 + data-skip L70-73)
    - .planning/phases/389-.../389-VALIDATION.md (V-10..V-14 + §Concrete Test Plan 9,10,13,14)
    - .planning/phases/389-.../389-RESEARCH.md (§Parity Contract Inventory H-1 delete row.remove L973-974; §D AJAX endpoints — DeletePreview GET, Edit modal openEditModal)
  </read_first>
  <files>tests/e2e/coachcoacheemapping-389.spec.ts</files>
  <action>
    Tambahkan 5 test smoke-parity (DSN-06*) ke dalam describe yang sama. Grep-label PERSIS:
    - V-10 `'edit modal'`: buka card pertama → klik tombol Edit baris pertama (`button:has-text("Edit")` dalam
      `#collapse-0`) → `#editModal` `toBeVisible()`; `#editCoacheeName` value ter-set non-kosong (bukti
      `openEditModal` 7-arg jalan). `test.skip` bila tak ada coachee row.
    - V-11 `'delete hapus row'`: buka card → bila ada baris non-aktif (tombol "Hapus" ada): klik Hapus →
      `#deleteModal` `toBeVisible()` + tombol submit "Hapus" ada. Mutasi penuh row-removal `tr[data-mapping-id]`
      = Phase 390 → `test.skip(noDeletable,'mutasi penuh = Phase 390')` bila tak ada data disposable. Minimal
      assert `#deleteModal` buka.
    - V-12 `'aksi branch'`: dalam card terbuka — bila ada baris `IsCompleted` (badge "Graduated" hadir) maka
      baris itu TIDAK menampilkan tombol "Aktifkan" (cek IsCompleted DULU, Phase 356 D-06). Bila tak ada data
      graduated → `test.skip`. Assert struktural: setiap baris punya tombol "Edit"; tombol state-spesifik
      (Nonaktifkan/Graduated/Aktifkan/Hapus) mutually-exclusive per branch.
    - V-13 `'ajax appUrl subpath'`: `let hit=false; await page.route('**/Admin/CoachCoacheeMappingDeletePreview*',
      route => { hit=true; route.continue(); });` lalu trigger `confirmDelete` (klik Hapus pada baris non-aktif).
      Assert request path mengandung `/Admin/CoachCoacheeMappingDeletePreview` (bukti appUrl, tak 404). `test.skip`
      bila tak ada tombol Hapus.
    - V-14 `'filter pagination'`: idiom 388 L103-122 — `select[name="section"]` ambil optionValues non-kosong;
      `test.skip(optionValues.length===0)`; `selectOption` → `Promise.all([page.waitForURL(/section=/),
      button Filter/Cari.click()])`; assert `page.url()` mengandung `section=`. Cek nama tombol submit aktual
      di halaman (Cari/Filter) — sesuaikan.

    SEMUA test pakai `test.skip` data-guard (jangan fail saat DB lokal kurang data). Tambah komentar tiap test
    `// DSN-06* smoke — full parity Phase 390`. JANGAN ubah scaffold/login dari Task 1.
  </action>
  <verify>
    <automated>cd tests && npx playwright test coachcoacheemapping-389 --list</automated>
  </verify>
  <acceptance_criteria>
    - `cd tests && npx playwright test coachcoacheemapping-389 --list` exit 0 dan mendaftarkan total 14 test.
    - Spec mengandung 5 grep-label baru: `edit modal`, `delete hapus row`, `aksi branch`, `ajax appUrl subpath`,
      `filter pagination` (tiap label muncul sebagai nama test).
    - `grep "CoachCoacheeMappingDeletePreview" tests/e2e/coachcoacheemapping-389.spec.ts` ketemu (V-13 route intercept).
    - `grep "page.route" tests/e2e/coachcoacheemapping-389.spec.ts` ketemu.
    - `grep -c "test.skip" tests/e2e/coachcoacheemapping-389.spec.ts` >= 5 (data-guard di test parity).
    - `grep "section=" tests/e2e/coachcoacheemapping-389.spec.ts` ketemu (V-14 filter assert).
    - Tak ada flag watch (`--ui`/`--watch`) di file atau komentar run.
  </acceptance_criteria>
  <done>14 test total (V-01..V-14) terdaftar; smoke-parity DSN-06 ber-data-guard; spec parse OK & siap di-run Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app (localhost:5277) | Test menjalankan login admin lokal; hanya membaca/klik UI. Tak menulis kode produksi. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-389-01-01 | I (Info disclosure) | Kredensial admin di spec | accept | Kredensial test lokal (`admin@pertamina.com/123456`) sudah ada di `tests/helpers/accounts.ts` (di-import, tak di-hardcode baru). Lingkungan lokal/Dev saja, bukan prod. Pola identik 28 spec existing. |
| T-389-01-02 | T (Tampering) | Seed data test | accept | Plan ini TIDAK membuat seed. Pakai data existing + `test.skip` bila kurang (CLAUDE.md Seed Workflow). Tak ada write DB. |

**Residual-risk verdict:** LOW. Plan ini hanya menambah 1 file test e2e (tak ada kode produksi, tak ada
endpoint baru, tak ada XSS sink — tak menyentuh `.cshtml`/`@Html.Raw`). Tak ada threat high. Block-on: none.
</threat_model>

<verification>
- `cd tests && npx playwright test coachcoacheemapping-389 --list` → 14 test terdaftar, parse 0 error.
- File mengandung 14 grep-label V-01..V-14 sesuai VALIDATION.md.
- TIDAK menjalankan full-run hijau di plan ini (markup target belum ada — Plan 02). Test RED saat ini = expected.
- 0 file produksi tersentuh (hanya `tests/e2e/coachcoacheemapping-389.spec.ts`).
</verification>

<success_criteria>
- 1 spec baru `tests/e2e/coachcoacheemapping-389.spec.ts` ada, parse-able, 14 test (V-01..V-14).
- Login admin via `loginAny` + nav `/Admin/CoachCoacheeMapping` (template sibling 388).
- Tiap test punya grep-label cocok VALIDATION.md; test parity ber-`test.skip` data-guard.
- Tak ada konflik file dgn Plan 02 (Plan 02 menulis `.cshtml`, plan ini menulis `.spec.ts`).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/389-coachcoacheemapping-redesign-accordion-card-per-coach-dsn-01-dsn-02-dsn-03/389-01-SUMMARY.md`
</output>
