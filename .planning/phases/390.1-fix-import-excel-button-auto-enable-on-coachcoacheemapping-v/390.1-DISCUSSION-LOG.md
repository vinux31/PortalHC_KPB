# Phase 390.1: Fix Import Excel button auto-enable - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-17
**Phase:** 390.1-fix-import-excel-button-auto-enable-on-coachcoacheemapping-v
**Areas discussed:** Teknik Fix, Penempatan Test

---

## Teknik Fix

| Option | Description | Selected |
|--------|-------------|----------|
| Bungkus DOMContentLoaded | Pindah registrasi listener ke dalam DOMContentLoaded. Minimal & lokal; input statis dijamin ada saat DOMContentLoaded fire. Diff terkecil, dekat intent asli. | ✓ |
| Event delegation di document | Satu listener di document, cek `e.target.id`. Lebih bulletproof terhadap timing/re-render tapi ubah pola lebih banyak. | |

**User's choice:** "check ulang lagi, sesuai reko" → minta verifikasi reko dulu, lalu pakai rekomendasi (DOMContentLoaded).
**Notes:** Reko diverifikasi terhadap kode aktual: inline `<script>` (line 625) eksekusi saat parse, modal+input ditaruh setelah `</script>` (input line 1059) → elemen belum ada saat listener register line 1038 → `?.` no-op. DOMContentLoaded fire setelah parse penuh → elemen ada → benar. Input statis (bukan di-inject) → delegation overkill. Reko di-lock.

---

## Penempatan Test

| Option | Description | Selected |
|--------|-------------|----------|
| Extend spec 389 existing | Tambah test case ke `coachcoacheemapping-389.spec.ts`. Satu suite parity, run --workers=1. | ✓ |
| Spec baru terpisah | File spec baru khusus DSN-07. Isolasi tapi tambah file. | |

**User's choice:** Extend spec 389 existing.
**Notes:** ROADMAP minta spec 389 existing tetap green. Konfirmasi file ada: `tests/e2e/coachcoacheemapping-389.spec.ts`; fixture ada: `tests/fixtures/import-mapping-390.xlsx`.

## Claude's Discretion

- Penempatan persis blok DOMContentLoaded di dalam @section Scripts.
- Detail langkah Playwright (buka modal, set file input) sesuai pola spec 389.

## Deferred Ideas

- Todo "One-time cleanup data test/audit lokal setelah Phase 367 ship" — match false positive, tidak di-fold.
