---
phase: 391-penambahan-peserta-fleksibel-saat-ujian-berjalan
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - HcPortal.Tests/FlexibleParticipantAddTests.cs
autonomous: true
requirements: [PART-04]
user_setup: []

must_haves:
  truths:
    - "Automated regression test mengunci: penambahan saat ada InProgress berhasil membuat sesi baru"
    - "Test mengunci: sesi peserta baru ber-status siap-mulai (Open/Upcoming) BUKAN InProgress — bukan warisi induk"
    - "Test mengunci: sesi InProgress existing Status/Schedule/DurationMinutes UNCHANGED setelah edit+tambah"
    - "Test mengunci: penambahan TIDAK terblokir saat sebagian sesi Completed selama window terbuka"
  artifacts:
    - path: "HcPortal.Tests/FlexibleParticipantAddTests.cs"
      provides: "Fixture disposable real-SQL + ≥4 facts (a/b/c/d) PART-04"
      contains: "FlexibleParticipantAddTests"
      min_lines: 120
  key_links:
    - from: "FlexibleParticipantAddFixture"
      to: "HcPortalDB_Test_{guid} @ localhost\\SQLEXPRESS"
      via: "IAsyncLifetime MigrateAsync → EnsureDeletedAsync (disposable; HcPortalDB_Dev TAK disentuh)"
      pattern: "HcPortalDB_Test_"
    - from: "FlexibleParticipantAddTests"
      to: "Category=Integration filter"
      via: "[Trait(\"Category\", \"Integration\")]"
      pattern: "Trait\\(\"Category\", \"Integration\"\\)"
    - from: "test DeriveReadyStatus replica"
      to: "controller DeriveReadyStatus (Plan 01)"
      via: "mirror byte-identik WIB DateTime.UtcNow.AddHours(7)"
      pattern: "DateTime.UtcNow.AddHours\\(7\\)"
---

<objective>
Buat `HcPortal.Tests/FlexibleParticipantAddTests.cs` — regression test xUnit integration real-SQL yang mengunci 4 perilaku penambahan-peserta-saat-berjalan (D-06, PART-04), agar perubahan controller Plan 01 tidak regresi di masa depan.

Purpose: Tanpa test, perilaku fleksibel (D-01..D-03) bisa diam-diam rusak oleh edit berikutnya pada `EditAssessment`. Test mengunci kontrak data-level: (a) add-saat-InProgress berhasil, (b) sesi baru siap-mulai bukan InProgress, (c) sesi InProgress existing tidak ter-overwrite, (d) add tak terblokir saat sebagian Completed + window terbuka.

Output: 1 file test baru (fixture + ≥4 facts), pola disposable `HcPortalDB_Test_{guid}` copy dari `PostLisensorPolishTests.cs`. Replikasi keputusan controller PERSIS di level data (helper controller berat di-instantiate; pola project = replikasi byte-identik, BUKAN WebApplicationFactory). Migration = FALSE.

Catatan: Plan ini Wave 2 — depends_on Plan 01 karena test mereplikasi `DeriveReadyStatus` + filter sesi-berjalan + cek-window byte-identik dengan keputusan controller yang sudah di-implementasi Plan 01. Baca `391-01-SUMMARY.md` lebih dulu untuk memastikan replica match implementasi nyata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-CONTEXT.md
@.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-RESEARCH.md
@.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-PATTERNS.md
@.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-01-SUMMARY.md

<interfaces>
<!-- Kontrak harness + model. Diekstrak langsung dari kode terbaca. -->

Harness disposable real-SQL — Source: HcPortal.Tests/PostLisensorPolishTests.cs L25-67 (copy pola):
```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using HcPortal.Data;
using HcPortal.Models;
using Microsoft.EntityFrameworkCore;
using Xunit;
using S = HcPortal.Models.AssessmentConstants.AssessmentStatus;

namespace HcPortal.Tests;

public class FlexibleParticipantAddFixture : IAsyncLifetime
{
    public string DbName { get; } = $"HcPortalDB_Test_{Guid.NewGuid():N}";
    private readonly string _cs;
    private DbContextOptions<ApplicationDbContext> _options = null!;
    public DbContextOptions<ApplicationDbContext> Options => _options;

    public FlexibleParticipantAddFixture()
    {
        _cs = $"Server=localhost\\SQLEXPRESS;Database={DbName};Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Connect Timeout=30";
    }

    public async Task InitializeAsync()
    {
        _options = new DbContextOptionsBuilder<ApplicationDbContext>().UseSqlServer(_cs).Options;
        try
        {
            await using var ctx = new ApplicationDbContext(_options);
            await ctx.Database.MigrateAsync();
        }
        catch (Exception ex)
        {
            try { await using var cleanup = new ApplicationDbContext(_options); await cleanup.Database.EnsureDeletedAsync(); } catch { }
            throw new Xunit.Sdk.XunitException(
                $"Phase 391 FlexibleParticipantAdd setup failed during MigrateAsync of {DbName}. Indikasi MIGRATION-CHAIN break, BUKAN bug fix. Inner: {ex}");
        }
    }

    public async Task DisposeAsync()
    {
        await using var ctx = new ApplicationDbContext(_options);
        await ctx.Database.EnsureDeletedAsync();
    }
}
```

Model AssessmentSession (field untuk seed) — Source: Models/AssessmentSession.cs:
```csharp
UserId(string) Title(string) Category(string) Schedule(DateTime WIB-naive)
DurationMinutes(int) Status(string) Progress(int=0) PassPercentage(int=70)
StartedAt(DateTime?) CompletedAt(DateTime?) ExamWindowCloseDate(DateTime?) AccessToken(string="")
```

Konstanta status — alias `using S = HcPortal.Models.AssessmentConstants.AssessmentStatus;`:
`S.Open` "Open" · `S.Upcoming` "Upcoming" · `S.InProgress` "InProgress" · `S.Completed` "Completed"

ApplicationUser seed minimal — Source: PostLisensorPolishTests.cs L68-74:
```csharp
new ApplicationUser { UserName = "part-" + Guid.NewGuid().ToString("N")[..8], Email = "part@test.local", FullName = "Flex Test" }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture + seed helpers + replica keputusan controller (D-06 setup)</name>
  <read_first>
    - HcPortal.Tests/PostLisensorPolishTests.cs L1-167 (pola fixture + seed + replication-at-data-level) — analog persis
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-01-SUMMARY.md (implementasi DeriveReadyStatus + filter + fallback window dari Plan 01 — replica harus match)
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-PATTERNS.md "FlexibleParticipantAddTests" block + Shared Patterns
    - Models/AssessmentSession.cs (field seed)
  </read_first>
  <behavior>
    - Fixture `FlexibleParticipantAddFixture` membuat disposable `HcPortalDB_Test_{guid}` @ localhost\SQLEXPRESS, MigrateAsync di Initialize, EnsureDeletedAsync di Dispose (HcPortalDB_Dev TIDAK tersentuh)
    - `SeedUserAsync(ctx)` → buat ApplicationUser, return Id
    - `SeedSiblingSessionAsync(ctx, userId, title, category, schedule, status, startedAt?, completedAt?)` → INSERT 1 sesi sibling, return session.Id
    - Replica helper `DeriveReadyStatus(schedule, window)` byte-identik dgn controller Plan 01 (WIB UTC+7, schedule<=nowWib → Open else Upcoming)
    - Replica filter sesi-berjalan: `StartedAt != null && CompletedAt == null` → skip
    - Replica cek-window/guard sesuai keputusan Plan 01 (fallback ExamWindowCloseDate==null per 391-01-SUMMARY)
  </behavior>
  <files>HcPortal.Tests/FlexibleParticipantAddTests.cs</files>
  <action>
    Buat file baru `HcPortal.Tests/FlexibleParticipantAddTests.cs`. Mulai dengan header komentar (analog PostLisensorPolishTests L1-12) menjelaskan: D-06 PART-04, replikasi keputusan EditAssessment di-level data (controller berat di-instantiate), DB disposable HcPortalDB_Test_{guid}, [Trait Integration].

    Salin fixture `FlexibleParticipantAddFixture` PERSIS dari `<interfaces>` di atas (imports + IAsyncLifetime + connection string + MigrateAsync/EnsureDeletedAsync + error wrap).

    Buka kelas test:
    ```csharp
    [Trait("Category", "Integration")]
    public class FlexibleParticipantAddTests : IClassFixture<FlexibleParticipantAddFixture>
    {
        private readonly FlexibleParticipantAddFixture _fixture;
        public FlexibleParticipantAddTests(FlexibleParticipantAddFixture fixture) => _fixture = fixture;
        private ApplicationDbContext NewCtx() => new ApplicationDbContext(_fixture.Options);
    ```

    Seed helpers (adaptasi PostLisensorPolish L68-94):
    ```csharp
    private static async Task<string> SeedUserAsync(ApplicationDbContext ctx)
    {
        var u = new ApplicationUser { UserName = "part-" + Guid.NewGuid().ToString("N")[..8], Email = "part@test.local", FullName = "Flex Test" };
        ctx.Users.Add(u);
        await ctx.SaveChangesAsync();
        return u.Id;
    }

    private static async Task<int> SeedSiblingSessionAsync(ApplicationDbContext ctx, string userId,
        string title, string category, DateTime schedule, string status,
        DateTime? startedAt = null, DateTime? completedAt = null, DateTime? examWindowCloseDate = null)
    {
        var session = new AssessmentSession
        {
            UserId = userId, Title = title, Category = category, Status = status, AccessToken = "",
            Schedule = schedule, DurationMinutes = 60, PassPercentage = 70,
            StartedAt = startedAt, CompletedAt = completedAt, ExamWindowCloseDate = examWindowCloseDate, Progress = 0
        };
        ctx.AssessmentSessions.Add(session);
        await ctx.SaveChangesAsync();
        return session.Id;
    }
    ```

    Replica keputusan controller — WAJIB byte-identik dgn Plan 01 (baca 391-01-SUMMARY untuk konfirmasi):
    ```csharp
    // Mirror AssessmentAdminController.DeriveReadyStatus (Plan 01 D-01) — WIB = UTC+7.
    private static string DeriveReadyStatus(DateTime schedule, DateTime? examWindowCloseDate)
    {
        var nowWib = DateTime.UtcNow.AddHours(7);
        return schedule <= nowWib ? S.Open : S.Upcoming;
    }

    // Mirror D-03 filter: sesi berjalan = jangan update field bersama.
    private static bool IsRunning(AssessmentSession s) => s.StartedAt != null && s.CompletedAt == null;
    ```
    Sediakan juga method `ApplySharedFieldUpdate(siblings, newTitle, newCategory, newSchedule, newDuration, newStatus)` yang mereplikasi edit-loop Plan 01 (skip `IsRunning`, lainnya update Status/Schedule/DurationMinutes/Title/Category) — agar fact (c) bisa lock UNCHANGED. Dan method `WindowAllowsAddition(group)` yang mereplikasi keputusan guard D-02 sesuai fallback yang dipilih Plan 01 (per 391-01-SUMMARY: null=boleh-tambah).

    Belum ada [Fact] di task ini (facts di Task 2) — tapi pastikan file compile (kelas + helper + fixture lengkap). Jika xUnit menolak kelas tanpa Fact, tambahkan 1 fact placeholder trivial yang kemudian diganti di Task 2, ATAU langsung lanjut Task 2 dalam sesi sama (preferensi: lanjut Task 2, jangan placeholder).
  </action>
  <acceptance_criteria>
    - File `HcPortal.Tests/FlexibleParticipantAddTests.cs` ada
    - `grep -n "public class FlexibleParticipantAddFixture : IAsyncLifetime" HcPortal.Tests/FlexibleParticipantAddTests.cs` → 1 match
    - `grep -n "HcPortalDB_Test_" HcPortal.Tests/FlexibleParticipantAddTests.cs` → 1 match
    - `grep -n "\[Trait(\"Category\", \"Integration\")\]" HcPortal.Tests/FlexibleParticipantAddTests.cs` → 1 match
    - `grep -n "DateTime.UtcNow.AddHours(7)" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 1 match (replica WIB byte-identik, BUKAN DateTime.Now)
    - `grep -n "EnsureDeletedAsync" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 1 match (disposable cleanup)
    - `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → 0 error
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.Tests/HcPortal.Tests.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Fixture disposable + seed helpers + replica keputusan controller (DeriveReadyStatus/IsRunning/ApplySharedFieldUpdate/window) compile; HcPortalDB_Dev tak tersentuh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 facts (a/b/c/d) mengunci PART-04 + full suite hijau</name>
  <read_first>
    - HcPortal.Tests/PostLisensorPolishTests.cs L169-229 (pola [Fact] + seed + verify-separate-context) — analog assertion
    - HcPortal.Tests/FlexibleParticipantAddTests.cs (fixture + helper dari Task 1) — file yang diperluas
    - .planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-RESEARCH.md "Fact + assertion pattern" + Open Question A1 (fallback window)
  </read_first>
  <behavior>
    - (a) AddParticipant_WithInProgressSibling_CreatesNewSession: grup punya 1 sesi InProgress; tambah peserta baru (insert sesi mirror BULK ASSIGN dgn Status=DeriveReadyStatus) → re-query DB: jumlah sibling bertambah, sesi baru ada untuk user baru
    - (b) AddParticipant_NewSession_HasReadyStatus_NotInProgress: dua kasus — schedule masa lalu → sesi baru Status==Open; schedule masa depan → Status==Upcoming; KEDUANYA != InProgress (bukan warisi induk InProgress/Completed)
    - (c) AddParticipant_InProgressSibling_StatusScheduleDurationUnchanged: seed sesi InProgress (StartedAt set, CompletedAt null) dgn Status/Schedule/Duration awal; jalankan ApplySharedFieldUpdate dgn nilai baru berbeda → re-query: sesi InProgress Status & Schedule & DurationMinutes UNCHANGED (filter D-03 skip)
    - (d) AddParticipant_SomeCompleted_NotBlocked_WhileWindowOpen: grup punya sebagian sesi Completed + window terbuka (ExamWindowCloseDate null atau masa depan); WindowAllowsAddition==true → penambahan lolos, sesi baru tercipta
  </behavior>
  <files>HcPortal.Tests/FlexibleParticipantAddTests.cs</files>
  <action>
    Tambahkan 4 `[Fact]` ke kelas `FlexibleParticipantAddTests`. Setiap fact: seed dgn `await using var ctx = NewCtx();`, lalu assert dgn `await using var verify = NewCtx();` TERPISAH (re-query dari DB, bukan in-memory tracking — pola PostLisensorPolish L183-186/L225-228). Pakai group-key konstan (title/category/schedule.Date sama) untuk semua sibling dalam satu grup.

    (a) `AddParticipant_WithInProgressSibling_CreatesNewSession`:
    - Seed user1, schedule masa lalu (mis. `DateTime.UtcNow.AddHours(7).AddHours(-1)`); seed sesi InProgress (StartedAt=now, CompletedAt=null, Status=S.InProgress) untuk user1 di grup G.
    - Seed user2 (peserta baru). Hitung sibling existing untuk filter idempotency (user2 belum ada). Insert sesi baru untuk user2 dgn `Status = DeriveReadyStatus(schedule, null)`, group-key sama.
    - Assert (verify ctx): `count sibling grup G == 2`; ada sesi untuk user2.

    (b) `AddParticipant_NewSession_HasReadyStatus_NotInProgress`:
    - Kasus past: schedule = `DateTime.UtcNow.AddHours(7).AddMinutes(-30)` → insert sesi baru `Status = DeriveReadyStatus(...)`. Assert `== S.Open` dan `!= S.InProgress`.
    - Kasus future: schedule = `DateTime.UtcNow.AddHours(7).AddDays(1)` → Assert `== S.Upcoming` dan `!= S.InProgress`.
    - (boleh 2 fact terpisah atau 1 fact 2 skenario; pilih yang jelas.)

    (c) `AddParticipant_InProgressSibling_StatusScheduleDurationUnchanged`:
    - Seed sesi InProgress untuk user1: Status=S.InProgress, Schedule=schedOrig, DurationMinutes=60, StartedAt=now, CompletedAt=null.
    - Fetch siblings grup → jalankan `ApplySharedFieldUpdate` dgn newStatus=S.Open, newSchedule=schedOrig.AddDays(3), newDuration=120 → SaveChanges.
    - Assert (verify ctx): sesi user1 (yg InProgress) tetap `Status==S.InProgress`, `Schedule==schedOrig`, `DurationMinutes==60` (UNCHANGED). Tambahan: seed 1 sesi belum-mulai (StartedAt=null) di grup yang sama → assert sesi itu JUSTRU berubah (Duration==120) untuk membuktikan filter selektif, bukan no-op total.

    (d) `AddParticipant_SomeCompleted_NotBlocked_WhileWindowOpen`:
    - Seed grup dgn 1 sesi Completed (CompletedAt set, Status=S.Completed) + window terbuka (ExamWindowCloseDate=null ATAU `DateTime.UtcNow.AddHours(7).AddDays(1)`).
    - Assert `WindowAllowsAddition(...) == true`; lalu insert sesi baru untuk user baru → assert (verify) sesi baru tercipta (count bertambah), membuktikan penambahan tidak terblokir saat sebagian Completed.

    Setelah semua fact: jalankan full suite (termasuk Integration). Pastikan SQLEXPRESS up.
  </action>
  <acceptance_criteria>
    - `grep -c "\[Fact\]" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 4
    - `grep -n "CreatesNewSession" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 1 (fact a)
    - `grep -n "HasReadyStatus_NotInProgress\|NewSession.*NotInProgress" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 1 (fact b)
    - `grep -n "Unchanged\|UNCHANGED\|StatusScheduleDuration" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 1 (fact c)
    - `grep -n "NotBlocked\|SomeCompleted" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 1 (fact d)
    - `grep -c "await using var verify = NewCtx()" HcPortal.Tests/FlexibleParticipantAddTests.cs` → ≥ 4 (re-query DB terpisah per fact)
    - `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → 0 error
    - `dotnet test --filter "FullyQualifiedName~FlexibleParticipantAdd"` → semua hijau (≥4 pass, 0 fail)
    - `dotnet test` (full suite incl. Integration) → hijau (no regression; SQLEXPRESS up)
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~FlexibleParticipantAdd"</automated>
  </verify>
  <done>4 facts (a/b/c/d) hijau mengunci PART-04 (add-saat-InProgress berhasil, sesi baru siap-mulai bukan InProgress, sesi InProgress UNCHANGED, add tak terblokir saat Completed+window terbuka); full suite hijau; HcPortalDB_Dev tak tersentuh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → SQL Server (localhost\SQLEXPRESS) | Test membuat & menghapus DB disposable `HcPortalDB_Test_{guid}`. Tidak menyentuh `HcPortalDB_Dev`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-391-T1 | Tampering | Test menulis ke DB Dev produksi-lokal | mitigate | Connection string hard-coded ke `HcPortalDB_Test_{Guid.NewGuid()}` disposable; `DisposeAsync` EnsureDeletedAsync. Pola identik PostLisensorPolishTests (terbukti tak menyentuh Dev). `[Trait("Category","Integration")]` mengecualikan dari fast suite default. |
| T-391-T2 | Repudiation | Test hijau palsu (replica drift dari controller) | mitigate | Replica `DeriveReadyStatus`/filter di-verifikasi byte-identik via baca `391-01-SUMMARY` + grep WIB `DateTime.UtcNow.AddHours(7)`. Fact (c) menambah sesi belum-mulai sebagai kontrol-positif agar filter terbukti selektif (bukan no-op). |
| T-391-T3 | Denial of Service | MigrateAsync gagal → suite merah misleading | accept | Fixture membungkus MigrateAsync dgn pesan eksplisit "MIGRATION-CHAIN break, BUKAN bug fix" + cleanup best-effort (pola PostLisensorPolish). Tanpa SQLEXPRESS, Integration di-skip; fast suite tetap jalan. |
</threat_model>

<verification>
- `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → 0 error.
- `dotnet test --filter "FullyQualifiedName~FlexibleParticipantAdd"` → ≥4 facts hijau.
- `dotnet test` full suite (incl. Integration) → hijau, no regression.
- Grep acceptance semua task PASS.
- Migration = FALSE — test tidak menambah migration; hanya 1 file test baru. `HcPortalDB_Dev` tidak tersentuh (disposable DB).
</verification>

<success_criteria>
- File `FlexibleParticipantAddTests.cs` ada dengan fixture disposable + ≥4 facts.
- 4 perilaku PART-04 terkunci hijau: (a) add berhasil, (b) sesi baru siap-mulai bukan InProgress, (c) sesi InProgress UNCHANGED, (d) add tak terblokir saat Completed+window terbuka.
- Replica keputusan controller byte-identik (WIB UTC+7, filter sesi-berjalan) — match Plan 01.
- Full suite hijau; 0 migration; `HcPortalDB_Dev` tak tersentuh.
</success_criteria>

<output>
After completion, create `.planning/phases/391-penambahan-peserta-fleksibel-saat-ujian-berjalan/391-02-SUMMARY.md`.
Catat: 4 facts + hasil (pass count), konfirmasi replica match Plan 01 (DeriveReadyStatus/filter), keputusan fallback window yang di-lock di fact (d), konfirmasi disposable DB + 0 migration. Catat juga: notify IT bahwa v32.0 phase 391 = migration=FALSE (untuk handoff).
</output>
