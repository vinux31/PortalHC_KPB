---
phase: 393-backend-core-inject
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Services/InjectAssessmentService.cs
autonomous: true
requirements: [INJ-01, INJ-02]
tags: [assessment, grading, inject, backend, service, transaction, certificate, essay, audit]

must_haves:
  truths:
    - "Service melakukan pre-flight validasi seluruh batch (NIP exists, opsi valid, EssayScore 0..ScoreValue, essay-wajib-skor, tanggal ≤ today, cert manual unik) → ada invalid → reject-all 0 tulisan + PerRowErrors (D-03)"
    - "Service skip+lapor pekerja duplikat (UserId+normalized Title+Category+Schedule.Date, cert-aware) tanpa gagalkan batch (D-01/D-02)"
    - "Service bungkus tulisan dalam satu transaction; per worker insert session→package(sentinel 1×)→assignment→responses→GradeAndCompleteAsync→(essay)finalize-block→backdate re-apply→audit; error → rollback-all (D-04)"
    - "Skor/IsPassed/ET/cert DIHITUNG via GradeAndCompleteAsync + AssessmentScoreAggregator (nol duplikasi); essay → Status=Completed setelah finalize-block (D-05)"
    - "Cert auto pakai tanggal backdate untuk ROMAN/year/seq (D-12); cert suppress bila !IsPassed (D-08); cert manual ter-set sebelum grade (D-09); backdate CompletedAt/StartedAt/Schedule ter-preserve pasca-grade"
    - "Audit in-tx via _context.AuditLogs.Add: ManualInject per sesi sukses; ManualInjectSkipped untuk duplikat; ManualInjectRejected untuk pre-flight tolak (D-11) — actor dari parameter"
  artifacts:
    - path: "Services/InjectAssessmentService.cs"
      provides: "Implementasi penuh InjectBatchAsync (pre-flight + dedup + tx batch + grade reuse + essay finalize replication + backdate + audit)"
      contains: "BeginTransactionAsync"
      min_lines: 200
  key_links:
    - from: "Services/InjectAssessmentService.cs"
      to: "Services/GradingService.GradeAndCompleteAsync"
      via: "delegasi mesin skor (JANGAN duplikasi)"
      pattern: "_gradingService\\.GradeAndCompleteAsync"
    - from: "Services/InjectAssessmentService.cs"
      to: "Helpers/AssessmentScoreAggregator.Compute"
      via: "finalize-block essay recompute"
      pattern: "AssessmentScoreAggregator\\.Compute"
    - from: "Services/InjectAssessmentService.cs"
      to: "Helpers/CertNumberHelper"
      via: "cert auto backdate + cert manual collision detect"
      pattern: "CertNumberHelper\\.(Build|GetNextSeqAsync|IsDuplicateKeyException)"
    - from: "Services/InjectAssessmentService.cs"
      to: "AuditLogs (AuditLog entity)"
      via: "_context.AuditLogs.Add in-tx (BUKAN LogAsync)"
      pattern: "_context\\.AuditLogs\\.Add"
---

<objective>
Isi body `InjectBatchAsync` di `Services/InjectAssessmentService.cs` (skeleton dari Plan 01) dengan orkestrasi penuh: pre-flight validasi reject-all (D-03), dedup skip+lapor (D-01/D-02), transaction atomic (D-04), per-worker insert (sentinel anchor) → delegasi grading (D-08, nol duplikasi) → essay finalize-block data-level (D-05) → backdate re-apply (D-06) → cert backdate (D-12)/suppress (D-08)/manual (D-09) → audit in-tx 3 ActionType (D-11). Implementasi = orkestrasi TIPIS yang mendelegasikan perhitungan ke mesin existing — nol duplikasi logic skor/lulus/cert.

Purpose: Inti milestone v32.2 — hasil inject byte-identik jalur online karena memakai pipeline grading yang sama (INJ-01). Atomic per-batch + audit penuh (INJ-02).
Output: `Services/InjectAssessmentService.cs` lengkap (≥200 baris). `dotnet build` 0 error. Fast suite tetap 347/347 (test Integration baru = Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/393-backend-core-inject/393-CONTEXT.md
@.planning/phases/393-backend-core-inject/393-RESEARCH.md
@.planning/phases/393-backend-core-inject/393-PATTERNS.md

<interfaces>
<!-- Kontrak yang dibuat Plan 01 + diverifikasi dari codebase. Pakai langsung. -->

Dari Models/InjectAssessmentDtos.cs (Plan 01): InjectRequest{Title,Category,AssessmentType,CompletedAt,StartedAt?,Schedule?,DurationMinutes,PassPercentage,AllowAnswerReview,CertMode(None/Auto/Manual),LinkedGroupId?,LinkedSessionId?,Questions[InjectQuestionSpec],Workers[InjectWorkerSpec]}; InjectQuestionSpec{QuestionText,QuestionType,ScoreValue,Order,ElemenTeknis?,Rubrik?,TempId,Options[InjectOptionSpec]}; InjectOptionSpec{OptionText,IsCorrect,TempId}; InjectWorkerSpec{Nip,Answers[InjectAnswerSpec],ManualCertNumber?,CertValidUntil?}; InjectAnswerSpec{QuestionTempId,SelectedOptionTempIds[],TextAnswer?,EssayScore?}; InjectResult{Success,Rejected,SuccessSessionIds[],SkippedNips[],PerRowErrors[InjectRowError{Nip,Message}],Message?}.

Signature (Plan 01 skeleton — isi body):
```csharp
public async Task<InjectResult> InjectBatchAsync(InjectRequest req, string actorUserId, string actorName)
```
Field service: `_context` (ApplicationDbContext), `_gradingService` (GradingService), `_logger` (ILogger<InjectAssessmentService>).

GradingService.GradeAndCompleteAsync(session) — Services/GradingService.cs:57: baca DB by session.Id (assignment+responses), NON-tx, set Status/Score/IsPassed/ET, cert auto gate isPassed (DateTime.Now @ :289 — lihat D-12 deviasi). Essay → set Status=PendingGrading + early-return TANPA cert (GradingService.cs:207-248). Non-essay → Completed + cert (gate isPassed @ :287). CompletedAt di-overwrite UtcNow (:224 essay, :263 non-essay → Pitfall 1 backdate).

AssessmentScoreAggregator.Compute(questions, responses, passPct) → ScoreAggregateResult(TotalScore,MaxScore,Percentage,IsPassed). Essay +EssayScore.Value.

CertNumberHelper.Build(seq,date)="KPB/{seq:D3}/{ROMAN}/{year}"; GetNextSeqAsync(ctx,year); IsDuplicateKeyException(ex).

AdminBaseController.NormalizeTitleForDup(string?) static → Regex \s+→" " + ToLowerInvariant. C#-only (tarik kandidat dari DB lalu banding in-memory).

Entity fields: lihat block [interfaces] Plan 01 (sama). UNIQUE filtered index `IX_AssessmentSessions_NomorSertifikat_Unique` (ApplicationDbContext.cs:226).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight validasi reject-all (D-03/D-06/D-07/D-09) + dedup skip (D-01/D-02)</name>
  <files>Services/InjectAssessmentService.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [B] pre-flight NIP up-front + dedup HashSet + cert-aware key + cert manual collision; Shared Pattern NormalizeTitleForDup)
    - .planning/phases/393-backend-core-inject/393-RESEARCH.md (Pitfall 4 cert manual collision pre-flight; D-03/D-06/D-07/D-09 di System Architecture diagram step [1]/[2])
    - Controllers/TrainingAdminController.cs:888-929 (analog pre-validate NIP up-front + dedup pre-load + intra-batch HashSet skip)
    - Controllers/AdminBaseController.cs:271-293 (NormalizeTitleForDup + FindTitleDuplicatesAsync GroupBy in-memory)
    - Data/ApplicationDbContext.cs:225-229 (UNIQUE index NomorSertifikat — untuk cek collision)
    - Services/InjectAssessmentService.cs (skeleton Plan 01 — isi method privat di sini)
  </read_first>
  <action>
Tambah method privat `PreflightValidateAsync(InjectRequest req, out Dictionary<string, ApplicationUser> usersByNip)` (atau bentuk setara yang kembalikan errors + map user) ke `InjectAssessmentService`. SEMUA validasi di bawah berjalan SEBELUM transaction; bila ADA error → kumpulkan `List<InjectRowError>` dan TIDAK tulis apa pun (D-03 reject-all). Pesan error Bahasa Indonesia.

Validasi (kumpulkan per-baris; jangan early-return pada error pertama agar HC lihat semua):
1. **NIP exists** (PATTERNS [B], TrainingAdminController.cs:888-899): `nips = req.Workers.Select(w => w.Nip).Distinct()`; `usersByNip = await _context.Users.Where(u => u.NIP != null && nips.Contains(u.NIP)).ToDictionaryAsync(u => u.NIP!)`. Tiap NIP tak ada → `PerRowErrors.Add(new InjectRowError{Nip=nip, Message=$"NIP {nip} tidak ditemukan di sistem."})`.
2. **Opsi valid** (D-03): untuk tiap worker, tiap `InjectAnswerSpec`: `QuestionTempId` harus cocok dgn salah satu `req.Questions[].TempId`; tiap `SelectedOptionTempIds` harus cocok dgn `InjectOptionSpec.TempId` milik soal itu. Tak cocok → error "Opsi/soal tidak valid untuk NIP {nip}.".
   - MC: tepat 1 opsi dipilih (jika >1 atau 0 dan bukan essay → error). MA: ≥1. Essay: 0 opsi.
3. **EssayScore range** (D-07): untuk tiap jawaban soal Essay, `EssayScore` WAJIB ada (essay tanpa skor = invalid, D-05) dan `0 <= EssayScore <= question.ScoreValue` (BUKAN 0..100). Di luar → error "Skor essay NIP {nip} di luar rentang 0..{ScoreValue}.".
4. **Tanggal ≤ today** (D-06): `req.CompletedAt.Date > DateTime.Today` (juga StartedAt/Schedule bila disuplai) → error "Tanggal ujian NIP-batch tidak boleh di masa depan.". Tahun absurd (mis. < 2000) → error.
5. **Cert manual unik** (D-09, Pitfall 4): bila `req.CertMode == InjectCertMode.Manual`:
   - Kumpulkan `manualNumbers = req.Workers.Where(w => !string.IsNullOrWhiteSpace(w.ManualCertNumber)).Select(w => w.ManualCertNumber!)`.
   - **Intra-batch dup**: dua worker nomor sama → error "Nomor sertifikat manual {n} duplikat dalam batch.".
   - **DB collision**: `var existing = await _context.AssessmentSessions.Where(s => s.NomorSertifikat != null && manualNumbers.Contains(s.NomorSertifikat)).Select(s => s.NomorSertifikat).ToListAsync()`; tiap match → error "Nomor sertifikat {n} sudah dipakai.".
   - Mode Manual: tiap worker WAJIB punya ManualCertNumber non-kosong → error bila kosong.

Tambah method privat `FindDuplicateNipsAsync(InjectRequest req, Dictionary<string,ApplicationUser> usersByNip)` → kembalikan `HashSet<string>` UserId yang duplikat (D-01/D-02, dijalankan SETELAH pre-flight lolos, SEBELUM tx):
- Normalisasi judul: `var norm = AdminBaseController.NormalizeTitleForDup(req.Title)`.
- Tarik kandidat dari DB (normalizer C#-only → banding in-memory, PATTERNS [B] "Change"): `var dateOnly = req.CompletedAt.Date; var relevantUserIds = usersByNip.Values.Select(u => u.Id).ToList(); var candidates = await _context.AssessmentSessions.Where(s => s.IsManualEntry && relevantUserIds.Contains(s.UserId)).Select(s => new { s.UserId, s.Title, s.Category, s.CompletedAt, s.NomorSertifikat }).ToListAsync();`
- Match in-memory: `s.UserId` cocok DAN `NormalizeTitleForDup(s.Title) == norm` DAN `s.Category == req.Category` DAN `s.CompletedAt?.Date == dateOnly` → duplikat.
- **Cert-aware** (D-02): bila `req.CertMode != None` ATAU sesi existing punya NomorSertifikat dgn judul+tanggal sama → juga dianggap dup (cegah double-cert).
- Plus intra-batch dup (dua worker NIP sama dalam req → kedua-setelah-pertama skip): pakai `HashSet<string> seen`.
- Kembalikan set UserId yang harus di-skip; caller (Task 2) `SkippedNips.Add` + audit ManualInjectSkipped (Task 3).

> CRITICAL deviasi BulkBackfill: dedup key BUKAN `s.Title ==` mentah — WAJIB `NormalizeTitleForDup` in-memory. Cert-aware adalah tambahan inject (D-02), tidak ada di BulkBackfill.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "NormalizeTitleForDup"` cocok ≥1× (dedup pakai normalizer, bukan Title mentah).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "tidak ditemukan di sistem"` cocok (NIP missing error Bahasa Indonesia).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "ScoreValue"` cocok (EssayScore range 0..ScoreValue, D-07).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "DateTime.Today|DateTime.Now.Date"` cocok (D-06 tanggal ≤ today).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "ManualCertNumber"` cocok (D-09 cert manual collision pre-flight).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "0\.\.100"` cocok 0× (EssayScore range = 0..ScoreValue, BUKAN 0..100).
    - `dotnet build HcPortal.csproj` → 0 error.
  </acceptance_criteria>
  <done>Pre-flight validasi reject-all (NIP/opsi/EssayScore-range/essay-wajib/tanggal/cert-manual-unik) + dedup cert-aware in-memory ter-compile; SC2-preflight & SC5-validasi siap dikunci Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Transaction batch + per-worker insert (sentinel) + delegasi grade + essay finalize replication + backdate re-apply</name>
  <files>Services/InjectAssessmentService.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [C] tx wrapper, [D] sentinel + field values AssessmentSession, [E] reuse grading, [F] essay finalize data-level replication + D-12 cert backdate, [G] backdate re-apply)
    - .planning/phases/393-backend-core-inject/393-RESEARCH.md (Pitfall 1 CompletedAt overwrite, Pitfall 2 cert date, Pitfall 3 in-tx cert visibility, Pitfall 5 essay PendingGrading, Pitfall 6 insert order)
    - Controllers/TrainingAdminController.cs:905-984 (atomic batch BeginTransactionAsync→try→SaveChanges→Commit/catch→Rollback)
    - Controllers/CMPController.cs:1034-1101 (sentinel UserPackageAssignment + ShuffledQuestionIds)
    - Services/GradingService.cs:200-319 (essay branch PendingGrading early-return + non-essay Completed + cert block DateTime.Now — D-12 referensi)
    - Controllers/AssessmentAdminController.cs:3728-3804 (CORE finalize: status-transition + recompute + cert block — REPLIKASI data-level)
    - HcPortal.Tests/EssayFinalizeRecomputeTests.cs (MirrorFinalizeWriteAsync precedent replikasi)
  </read_first>
  <action>
Tulis inti `InjectBatchAsync`: panggil PreflightValidate (Task 1) → bila ada error: kembalikan `new InjectResult{Rejected=true, Success=false, PerRowErrors=..., Message="Batch ditolak: ada baris tidak valid. Tidak ada data ditulis."}` (audit reject = Task 3). Lalu FindDuplicateNips (Task 1).

Buka transaction (PATTERNS [C], TrainingAdminController.cs:905-984):
```csharp
using var transaction = await _context.Database.BeginTransactionAsync();
try { /* per-worker loop + audit + SaveChanges */ await transaction.CommitAsync(); }
catch (Exception ex) { await transaction.RollbackAsync(); _logger.LogError(ex, "InjectBatch gagal untuk title={Title}", req.Title); return new InjectResult{Success=false, Rejected=false, Message="Terjadi kesalahan saat menyimpan; seluruh batch dibatalkan (rollback)."}; }
```

**Sentinel package 1× per batch** (sebelum loop worker, PATTERNS [D], CMPController sentinel): buat `AssessmentSession` representatif untuk anchor (atau anchor ke session pertama yang dibuat — pilih konsisten), lalu `AssessmentPackage{AssessmentSessionId=anchorId, PackageName="Paket A", PackageNumber=1}` → SaveChanges → insert `PackageQuestion` per `req.Questions` (map `TempId → real Id`) → SaveChanges → insert `PackageOption` per question (map `TempId → real Id`) → SaveChanges. Simpan dictionary `tempQId→realQId` dan `tempOptId→realOptId` untuk memetakan jawaban worker.

**Per worker valid & non-dup** (loop sekuensial — Pitfall 3 cert visibility butuh sequential; urutan insert Pitfall 6):
1. **Session** (PATTERNS [D] field values — TIRU PERSIS, ⚠ deviasi BulkBackfill):
```csharp
var startedAt = req.StartedAt ?? req.CompletedAt;
var schedule  = req.Schedule  ?? req.CompletedAt;
var session = new AssessmentSession {
    UserId = user.Id, Title = req.Title, Category = req.Category,
    IsManualEntry = true,                    // INJ-02
    AccessToken = "INJECT",                   // ⚠ BUKAN "BACKFILL"
    IsTokenRequired = false,
    AssessmentType = req.AssessmentType,      // ⚠ "Standard"/"PreTest"/"PostTest" — BUKAN "Manual"
    AllowAnswerReview = req.AllowAnswerReview,
    GenerateCertificate = req.CertMode != InjectCertMode.None,
    ValidUntil = workerSpec.CertValidUntil,   // null = permanent (D-10)
    NomorSertifikat = req.CertMode == InjectCertMode.Manual ? workerSpec.ManualCertNumber : null,  // D-09
    PassPercentage = req.PassPercentage,
    DurationMinutes = req.DurationMinutes,
    LinkedGroupId = req.LinkedGroupId, LinkedSessionId = req.LinkedSessionId,   // passthrough (RESEARCH OpenQ#3)
    Schedule = schedule, StartedAt = startedAt, CompletedAt = req.CompletedAt,  // backdate (D-06); akan dioverwrite → re-apply
    Status = "Open", Progress = 0, CreatedAt = DateTime.UtcNow, CreatedBy = actorUserId
};
_context.AssessmentSessions.Add(session); await _context.SaveChangesAsync();   // dapat session.Id
```
> ⚠ JANGAN `AssessmentType = AssessmentConstants.AssessmentType.Manual`. `"Manual"` punya branch skip (ShouldEnforceSubmitTimer + sibling Pre/Post exclude). Pakai `req.AssessmentType` ("Standard" default).

2. **Assignment sentinel** (PATTERNS [D], CMPController.cs:1068-1080): `ShuffledQuestionIds = JsonSerializer.Serialize(realQuestionIdsInOrder)` (urutan ID soal apa adanya — TAK shuffle), `ShuffledOptionIdsPerQuestion = "{}"`, `IsCompleted = true`, `SavedQuestionCount = count`. SaveChanges.

3. **Responses** per jawaban worker (map via dict TempId→realId): MC/MA → `PackageUserResponse{AssessmentSessionId, PackageQuestionId=realQId, PackageOptionId=realOptId, SubmittedAt=req.CompletedAt}` (MA = banyak baris); Essay → `{PackageQuestionId=realQId, TextAnswer=ans.TextAnswer, EssayScore=ans.EssayScore, SubmittedAt=req.CompletedAt}`. SaveChanges (Pitfall 6: responses DULU sebelum grade).

4. **Delegasi grade** (PATTERNS [E]): `await _gradingService.GradeAndCompleteAsync(session);` → non-essay: Completed+Score+IsPassed+ET+cert(gate isPassed, D-08). Essay: PendingGrading early-return (tanpa cert).

5. **Essay finalize-block** (PATTERNS [F], REPLIKASI data-level — HANYA bila `hasEssay`): deteksi `hasEssay = req.Questions.Any(q => q.QuestionType == "Essay")`. Jalankan:
```csharp
// load questions+responses by session (sama seperti GradingService baca DB)
var qIds = realQuestionIdsForThisRoom;
var allQuestions = await _context.PackageQuestions.Include(q => q.Options).Where(q => qIds.Contains(q.Id)).ToListAsync();
var allResponses = await _context.PackageUserResponses.Where(r => r.AssessmentSessionId == session.Id).ToListAsync();
var agg = AssessmentScoreAggregator.Compute(allQuestions, allResponses, session.PassPercentage);
int finalPercentage = agg.Percentage; bool isPassed = agg.IsPassed;
await _context.AssessmentSessions
    .Where(s => s.Id == session.Id && s.Status == AssessmentConstants.AssessmentStatus.PendingGrading)
    .ExecuteUpdateAsync(s => s
        .SetProperty(r => r.Score, finalPercentage)
        .SetProperty(r => r.Status, AssessmentConstants.AssessmentStatus.Completed)
        .SetProperty(r => r.IsPassed, isPassed)
        .SetProperty(r => r.CompletedAt, DateTime.UtcNow));   // di-backdate re-apply langkah 7
// cert block essay (D-12 backdate): certNow = req.CompletedAt
if (session.GenerateCertificate && isPassed && req.CertMode == InjectCertMode.Auto) {
    var certNow = req.CompletedAt; int certYear = certNow.Year;   // ⚠ D-12: tahun ujian, BUKAN DateTime.Now
    int attempts = 0; bool saved = false;
    while (!saved && attempts < 3) { attempts++; try {
        var nextSeq = await CertNumberHelper.GetNextSeqAsync(_context, certYear);
        await _context.AssessmentSessions.Where(s => s.Id == session.Id && s.NomorSertifikat == null)
            .ExecuteUpdateAsync(s => s.SetProperty(r => r.NomorSertifikat, CertNumberHelper.Build(nextSeq, certNow)));
        saved = true;
    } catch (DbUpdateException ex) when (attempts < 3 && CertNumberHelper.IsDuplicateKeyException(ex)) { } }
}
```
Hilangkan SEMUA HTTP-coupling (tanpa _userManager/_hubContext/return Json). Pre-check "semua essay ter-skor" boleh di-skip (D-03 sudah jamin).

6. **D-12 cert auto NON-essay**: `GradeAndCompleteAsync` hard-code `DateTime.Now` untuk cert (GradingService.cs:289). Untuk non-essay auto-cert, ROMAN/year harus pakai tanggal ujian. Mekanisme (PATTERNS [F] "CATATAN executor" — pilih satu, dokumentasikan di SUMMARY):
   - **Pilihan default (direkomendasikan):** untuk `req.CertMode == Auto`, JANGAN biarkan GradeAndCompleteAsync generate (ia tetap akan, lalu kita OVERWRITE): setelah grade, bila `session.IsPassed == true` (reload) DAN cert auto, jalankan ulang blok cert dengan `certNow = req.CompletedAt` — set ulang `NomorSertifikat` (release nomor lama bila perlu, atau lebih bersih: reload session, bila `NomorSertifikat != null` dan tahun/bulannya = bulan inject-dijalankan padahal harusnya bulan ujian → ExecuteUpdate set ke `CertNumberHelper.Build(GetNextSeqAsync(req.CompletedAt.Year), req.CompletedAt)`).
   - **Pilihan lebih bersih:** sebelum grade, TIDAK ubah; setelah grade, untuk auto-cert: reload `isPassed`; bila perlu backdate ROMAN, hapus nomor auto yang baru dibuat (ExecuteUpdate set NomorSertifikat=null WHERE id) lalu generate ulang dengan certNow=req.CompletedAt (retry 3×). Karena dalam tx + sequential, GetNextSeqAsync melihat read-your-own-writes (Pitfall 3) → seq benar.
   Implementasikan satu mekanisme yang menjamin: cert auto final ber-ROMAN/tahun = bulan/tahun `req.CompletedAt` (D-12). Untuk cert Manual (D-09) nomor dari HC tidak disentuh. Untuk !isPassed → tidak ada cert (D-08, gratis dari gate).

7. **Backdate re-apply** (PATTERNS [G], Pitfall 1 — WAJIB, setelah grade+finalize):
```csharp
await _context.AssessmentSessions.Where(s => s.Id == session.Id)
    .ExecuteUpdateAsync(s => s
        .SetProperty(r => r.CompletedAt, req.CompletedAt)
        .SetProperty(r => r.StartedAt, startedAt)
        .SetProperty(r => r.Schedule, schedule));
```

Kumpulkan `session.Id` ke list sukses (untuk InjectResult.SuccessSessionIds + audit Task 3). Setelah loop: audit (Task 3) → SaveChanges → Commit. Return `InjectResult{Success=true, SuccessSessionIds=..., SkippedNips=...}`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "BeginTransactionAsync"` cocok 1× (D-04 atomic).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "_gradingService.GradeAndCompleteAsync"` cocok ≥1× (delegasi mesin, nol duplikasi).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "AssessmentScoreAggregator.Compute"` cocok ≥1× (finalize-block essay D-05).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern 'AccessToken = "INJECT"'` cocok 1× (deviasi: BUKAN BACKFILL).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern 'AssessmentType.*Manual'` cocok 0× (deviasi: BUKAN "Manual").
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "IsManualEntry = true"` cocok 1× (INJ-02).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "CertNumberHelper.Build"` cocok ≥1× DAN nearby `req.CompletedAt` (D-12 backdate cert).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "SetProperty\(r => r.CompletedAt, req.CompletedAt\)"` cocok ≥1× (backdate re-apply D-06, Pitfall 1).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "RollbackAsync"` cocok 1× (D-04).
    - `dotnet build HcPortal.csproj` → 0 error. Fast suite `dotnet test --filter "Category!=Integration"` → 347/347 (no regression).
  </acceptance_criteria>
  <done>Transaction batch + sentinel insert + delegasi grade + essay finalize replication + cert backdate (D-12)/suppress (D-08)/manual (D-09) + backdate re-apply ter-compile; SC1/SC2/SC3/SC5 siap dikunci Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Audit in-tx 3 ActionType (D-11) + finalisasi InjectResult</name>
  <files>Services/InjectAssessmentService.cs</files>
  <read_first>
    - .planning/phases/393-backend-core-inject/393-PATTERNS.md (block [H] audit in-tx 3 ActionType; anti-pattern AuditLogService.LogAsync di tengah tx)
    - .planning/phases/393-backend-core-inject/393-RESEARCH.md (D-11 cakupan sukses+skip+reject; anti-pattern LogAsync SaveChanges sendiri)
    - Controllers/TrainingAdminController.cs:958-970 (analog _context.AuditLogs.Add in-tx loop)
    - Models/AuditLog.cs (field ActorUserId/ActorName/ActionType[MaxLength 50]/Description/TargetId/TargetType/CreatedAt)
    - Services/AuditLogService.cs:40-41 (LogAsync SaveChanges sendiri — JANGAN dipakai in-tx)
  </read_first>
  <action>
Tambah audit (PATTERNS [H], TrainingAdminController.cs:958-970) — pakai `_context.AuditLogs.Add` LANGSUNG (BUKAN `AuditLogService.LogAsync` yang SaveChanges sendiri → commit parsial). 3 ActionType TERPISAH (D-11 — agar count "ManualInject" bersih = jumlah sesi sukses, dikunci SC4):

1. **Sukses (in-tx, dalam loop/setelah loop sebelum Commit)** — 1 entri per sesi sukses:
```csharp
foreach (var (session, nip) in successSessions)
    _context.AuditLogs.Add(new AuditLog {
        ActorUserId = actorUserId,
        ActorName = string.IsNullOrWhiteSpace(actorName) ? actorUserId : actorName,
        ActionType = "ManualInject",   // ⚠ TEPAT string ini — count = jumlah sesi (SC4)
        Description = $"Inject hasil assessment manual: NIP={nip}, SessionId={session.Id}, Skor={session.Score}, Tanggal={session.CompletedAt:yyyy-MM-dd}.",
        TargetId = session.Id, TargetType = "AssessmentSession", CreatedAt = DateTime.UtcNow });
await _context.SaveChangesAsync();   // dalam tx, sebelum Commit
```
   Catatan: ambil `session.Score` setelah reload (post-grade+backdate) agar skor benar di deskripsi.

2. **Skip duplikat (in-tx)** — 1 entri per NIP di-skip (D-11b):
```csharp
foreach (var nip in skippedNips)
    _context.AuditLogs.Add(new AuditLog { ActorUserId=actorUserId, ActorName=..., ActionType="ManualInjectSkipped",
        Description=$"Inject dilewati (duplikat): NIP={nip}, Judul={req.Title}, Tanggal={req.CompletedAt:yyyy-MM-dd}.",
        TargetType="AssessmentSession", CreatedAt=DateTime.UtcNow });
```
   ⚠ ActionType "ManualInjectSkipped" (BUKAN "ManualInject") → TIDAK menggembungkan count SC4.

3. **Reject pre-flight (DI LUAR tx batch — reject = SEBELUM tx, no tulisan sesi)** — D-11c: karena reject terjadi sebelum tx dibuka, audit reject pakai 1 commit terpisah. Pilihan: panggil `AuditLogService.LogAsync` (boleh di sini karena bukan di tengah tx batch) ATAU `_context.AuditLogs.Add` + `SaveChangesAsync` langsung (tanpa tx batch). Implementasikan di awal `InjectBatchAsync` setelah pre-flight gagal, sebelum return Rejected:
```csharp
_context.AuditLogs.Add(new AuditLog { ActorUserId=actorUserId, ActorName=..., ActionType="ManualInjectRejected",
    Description=$"Inject ditolak (pre-flight): {jumlahError} baris invalid. Judul={req.Title}.",
    TargetType="AssessmentSession", CreatedAt=DateTime.UtcNow });
await _context.SaveChangesAsync();
```
   ⚠ ActionType "ManualInjectRejected" terpisah; jangan campur ke tx batch sukses.

Finalisasi `InjectResult`: sukses → `{Success=true, SuccessSessionIds=[...], SkippedNips=[...], Message=$"Inject selesai: {n} sesi dibuat, {m} dilewati."}`; reject → sudah dikembalikan Task 2; rollback → `{Success=false, Message="...rollback..."}`.

ActorName guard: bila kosong → fallback ke actorUserId (AuditLog.ActorName Required). ActionType ≤ 50 char (semua 3 string < 50, aman).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern "_context.AuditLogs.Add"` cocok ≥3× (3 ActionType in-context, BUKAN LogAsync untuk sukses/skip).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern 'ActionType = "ManualInject"'` cocok 1× (sukses; string TEPAT untuk SC4 count).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern '"ManualInjectSkipped"'` cocok 1× (D-11b, terpisah).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern '"ManualInjectRejected"'` cocok 1× (D-11c, terpisah).
    - `Select-String -Path Services/InjectAssessmentService.cs -Pattern 'TargetType = "AssessmentSession"'` cocok ≥1× (audit sukses TargetId+TargetType).
    - Audit sukses (ManualInject) tidak dipanggil via `LogAsync` (grep `_auditLog.LogAsync` di blok sukses = 0× untuk ManualInject).
    - `dotnet build HcPortal.csproj` → 0 error. Fast suite 347/347.
  </acceptance_criteria>
  <done>Audit 3 ActionType (ManualInject sukses count-bersih / ManualInjectSkipped / ManualInjectRejected) via _context.AuditLogs.Add in-tx + InjectResult final; SC4 siap dikunci Plan 03; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Phase 394) → InjectBatchAsync | actor identity = parameter; service tidak resolve User. RBAC di Phase 394. |
| InjectRequest DTO → DB write | Semua nilai (score/cert/date/opsi) divalidasi pre-flight SEBELUM write (D-03). Tidak ada write bila ada invalid. |
| GradeAndCompleteAsync → AssessmentSessions | Mesin existing menghitung Score/IsPassed/cert (gate isPassed) — service tidak menulis nilai dengan tangan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-393-05 | Tampering | EssayScore/score di luar range | mitigate | Pre-flight D-07 (0..ScoreValue) reject-all; skor final DIHITUNG AssessmentScoreAggregator/GradeAndCompleteAsync (bukan terima persen final dari input) |
| T-393-06 | Tampering | double-cert / nomor tabrakan | mitigate | UNIQUE index `IX_AssessmentSessions_NomorSertifikat_Unique` + pre-flight D-09 (intra-batch + DB) + retry 3× IsDuplicateKeyException |
| T-393-07 | Tampering | cert untuk yang tidak lulus | mitigate | GradeAndCompleteAsync gate cert pada isPassed (D-08); finalize-block cert juga `if (isPassed)` — mirror online otomatis |
| T-393-08 | (Integrity) | partial write saat error mid-batch | mitigate | Satu transaction (BeginTransactionAsync); catch → RollbackAsync → 0 sesi/assignment/response/audit committed (D-04) |
| T-393-09 | Repudiation | audit hilang saat rollback | mitigate | Audit sukses+skip via `_context.AuditLogs.Add` IN-TX (ikut commit/rollback) — BUKAN LogAsync (SaveChanges sendiri = commit parsial) |
| T-393-10 | Tampering | inject data palsu untuk pekerja | mitigate | IsManualEntry=true + AuditLog ManualInject (actor+NIP+skor+sessionId) — transparansi compliance INJ-02 |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test --filter "Category!=Integration"` → 347/347 (no regression; Integration baru = Plan 03).
- Service mendelegasikan skor/lulus/cert ke mesin existing (grep konfirmasi GradeAndCompleteAsync + AssessmentScoreAggregator.Compute dipakai; tidak ada perhitungan persen manual).
- Deviasi BulkBackfill ditegakkan: AccessToken="INJECT", AssessmentType ≠ "Manual", backdate re-apply pasca-grade.
</verification>

<success_criteria>
- `InjectBatchAsync` lengkap: pre-flight reject-all (D-03/D-06/D-07/D-09) → dedup skip cert-aware (D-01/D-02) → transaction atomic (D-04) → per-worker insert sentinel → GradeAndCompleteAsync (D-08) → essay finalize-block → Completed (D-05) → cert backdate (D-12) → backdate re-apply (Pitfall 1) → audit 3 ActionType (D-11).
- Nol duplikasi logic skor/lulus/cert (delegasi ke mesin).
- `dotnet build` 0 error; fast suite 347/347; 0 migration (no schema change).
</success_criteria>

<output>
After completion, create `.planning/phases/393-backend-core-inject/393-02-SUMMARY.md`
</output>
