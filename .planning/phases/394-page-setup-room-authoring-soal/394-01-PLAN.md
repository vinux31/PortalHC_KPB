---
phase: 394-page-setup-room-authoring-soal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/inject-assessment-394.spec.ts
  - HcPortal.Tests/InjectViewModelMapTests.cs
  - ViewModels/InjectAssessmentViewModel.cs
  - Controllers/InjectAssessmentController.cs
  - Views/Admin/InjectAssessment.cshtml
  - Views/Admin/Index.cshtml
autonomous: true
requirements: [INJ-03]
must_haves:
  truths:
    - "Admin dan HC melihat kartu 'Inject Assessment Manual' di Kelola Data Section C dan dapat membukanya"
    - "Role di luar Admin/HC yang mengakses /Admin/InjectAssessment langsung mendapat redirect/403 (AccessDenied)"
    - "GET /Admin/InjectAssessment mengembalikan 200 dan merender wizard 6-pill (Setup/Pekerja/Soal/Sertifikat/Jawaban/Konfirmasi)"
    - "Navigasi wizard berfungsi runtime: pill aktif/completed/pending, tombol Selanjutnya/Sebelumnya pindah panel"
  artifacts:
    - path: "Controllers/InjectAssessmentController.cs"
      provides: "GET /Admin/InjectAssessment + ViewBag feed + RBAC Admin,HC + View()-folder override"
      contains: "[Authorize(Roles = \"Admin, HC\")]"
    - path: "Views/Admin/InjectAssessment.cshtml"
      provides: "Wizard scaffold: 6 pills + 6 step-panel + single form + WizardController IIFE (goToStep/updatePills/validateStep/populateSummary, loop 1..6)"
      contains: "wizardStepNav"
    - path: "ViewModels/InjectAssessmentViewModel.cs"
      provides: "POST shape mapping to InjectRequest (scalars + UserIds + Questions + cert fields)"
      contains: "class InjectAssessmentViewModel"
    - path: "Views/Admin/Index.cshtml"
      provides: "Section-C card linking to /Admin/InjectAssessment, RBAC-gated @if Admin||HC"
      contains: "InjectAssessment"
    - path: "tests/e2e/inject-assessment-394.spec.ts"
      provides: "Playwright spec scaffold (RBAC + wizard nav describes), login fixture AD-off"
      contains: "inject-assessment"
    - path: "HcPortal.Tests/InjectViewModelMapTests.cs"
      provides: "xUnit scaffold for ViewModel→InjectRequest mapping (Category!=Integration)"
      contains: "InjectViewModelMap"
  key_links:
    - from: "Views/Admin/Index.cshtml"
      to: "/Admin/InjectAssessment"
      via: "@Url.Action(\"InjectAssessment\", \"InjectAssessment\")"
      pattern: "InjectAssessment.*InjectAssessment"
    - from: "Controllers/InjectAssessmentController.cs"
      to: "Views/Admin/InjectAssessment.cshtml"
      via: "View()-folder override resolving ~/Views/Admin/{action}.cshtml"
      pattern: "~/Views/Admin/"
---

<objective>
Bangun fondasi page inject: controller baru `InjectAssessmentController` (route `/Admin/InjectAssessment`, RBAC `Admin,HC`), `InjectAssessmentViewModel`, kartu Section-C di Index.cshtml, dan kerangka wizard 6-langkah (6 pill + 6 step-panel + satu `<form>` + WizardController IIFE) yang di-port dari `CreateAssessment.cshtml` dan diperluas 4→6 langkah. Plan ini juga membuat Wave 0 test scaffold (Playwright spec + xUnit mapping test) yang dipakai semua plan 394 berikutnya.

Purpose: Menutup INJ-03 (page + RBAC + entry card) dan menyediakan scaffold wizard + kontrak ViewModel yang di-extend Plan 02/03/04 (file-overlap → sequential). Step-panel diisi field oleh plan berikut; di sini panel cukup placeholder dengan heading + footer nav.
Output: 1 controller, 1 ViewModel, 1 view (scaffold), 1 kartu Index, 2 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/394-page-setup-room-authoring-soal/394-CONTEXT.md
@.planning/phases/394-page-setup-room-authoring-soal/394-RESEARCH.md
@.planning/phases/394-page-setup-room-authoring-soal/394-PATTERNS.md
@.planning/phases/394-page-setup-room-authoring-soal/394-UI-SPEC.md

<interfaces>
<!-- Commit contract target — VERIFIED Models/InjectAssessmentDtos.cs (do NOT modify this file) -->
InjectRequest { Title, Category, AssessmentType("Standard"/"PreTest"/"PostTest", NEVER "Manual"),
  CompletedAt(DateTime backdate≤today), StartedAt?, Schedule?, DurationMinutes(=60), PassPercentage(=70),
  AllowAnswerReview(=true), CertMode(InjectCertMode: None=0/Auto=1/Manual=2), LinkedGroupId?, LinkedSessionId?,
  List<InjectQuestionSpec> Questions, List<InjectWorkerSpec> Workers }
InjectQuestionSpec { QuestionText, QuestionType, ScoreValue(=10), Order, ElemenTeknis?, Rubrik?, TempId, List<InjectOptionSpec> Options }
InjectOptionSpec { OptionText, IsCorrect, TempId }
InjectWorkerSpec { Nip, List<InjectAnswerSpec> Answers, ManualCertNumber?, CertValidUntil?(DateOnly null=permanent) }
InjectAssessmentService.InjectBatchAsync(...) — registered Program.cs:57 (DI scoped). NOT called in 394.

<!-- Controller scaffold pattern — VERIFIED AssessmentAdminController.cs -->
:19-20  [Route("Admin/[action]")] public class X : AdminBaseController
:58-62  protected new ViewResult View() => base.View("~/Views/Admin/" + ControllerContext.ActionDescriptor.ActionName + ".cshtml");
        protected new ViewResult View(object? model) => base.View("~/Views/Admin/" + ... + ".cshtml", model);
:649-651 [HttpGet] [Authorize(Roles = "Admin, HC")]  // NOTE the SPACE after comma
:653-668 GET feed: ViewBag.Users (Id/FullName/Email/Section, IsActive), ViewBag.Sections=GetAllSectionsAsync(),
         ViewBag.Categories=AssessmentCategories.Where(IsActive).OrderBy(SortOrder).ThenBy(Name)

<!-- Wizard JS to port — VERIFIED CreateAssessment.cshtml -->
:877-937 (function WizardController(){ currentStep=1; visitedSteps=new Set([1]); returnToConfirm=false;
   goToStep(n){hide all .step-panel; show #step-n; updatePills(); if(n===4)populateSummary(); scrollTo top}
   updatePills(){ for(i=1;i<=4;i++){ active:bg-primary text-white active+bi-circle-fill;
     completed(visited&&i<current):bg-success text-white+bi-check-circle-fill; visited:text-primary border border-primary;
     pending:text-muted border disabled } }
   validateStep(n){ per-step is-invalid + step-error alert; return bool }
   populateSummary(){ fill confirm spans via .textContent } })
NOTE: in 394 these become loop 1..6, goToStep populateSummary at n===6.

<!-- Section-C card insert point — VERIFIED Index.cshtml -->
:179-264 <div class="row g-3 mb-2"> ... 5 existing cards (last = Certificate Renewal :244-263) ... </div>
Insert new card just BEFORE the closing </div> at :264 (after Certificate Renewal card).
Do NOT touch the Admin-only "Bulk Import Nilai (Excel)" card Section-D :290-303 (retired Phase 396).

<!-- Login fixture — VERIFIED tests/helpers/accounts.ts + assessment-title-flexible.spec.ts -->
accounts.admin = admin@pertamina.com/123456 (Admin); accounts.hc = meylisa.tjiang@pertamina.com/123456 (HC);
accounts.coachee = rino.prasetyo@pertamina.com/123456 (Coachee — non Admin/HC, for RBAC-deny test).
login: page.goto('/Account/Login'); fill input[name="email"]/input[name="password"]; click button[type=submit].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — test scaffolds (Playwright spec + xUnit mapping test)</name>
  <read_first>
    - tests/e2e/assessment-title-flexible.spec.ts (login fixture pattern, AD-off, loginAny helper)
    - tests/helpers/accounts.ts (admin/hc/coachee fixtures)
    - .planning/phases/394-page-setup-room-authoring-soal/394-RESEARCH.md (Validation Architecture → Test Map; Wave 0 Gaps)
    - HcPortal.Tests/ (pick any existing *Tests.cs to mirror xUnit project conventions, e.g. PostLisensorPolishTests.cs header)
  </read_first>
  <files>tests/e2e/inject-assessment-394.spec.ts, HcPortal.Tests/InjectViewModelMapTests.cs</files>
  <action>
    Create `tests/e2e/inject-assessment-394.spec.ts`: copy the `loginAny(page, accountKey)` helper verbatim from assessment-title-flexible.spec.ts (goto /Account/Login, fill input[name="email"]/input[name="password"] from `accounts[key]`, click button[type=submit], waitForURL away from login). Import `{ accounts, AccountKey }` from '../helpers/accounts'. Add a header comment block (pola login + pre-req: server localhost:5277 AD-off `Authentication__UseActiveDirectory=false`, run `cd tests && npx playwright test e2e/inject-assessment-394.spec.ts --workers=1`). Scaffold these `test.describe` blocks with grep-able names, each containing AT LEAST a `test('RBAC ...')` shell that this plan fully implements and `test.skip(...)` placeholders for behaviors landed by later plans:
      - describe 'INJ-03 RBAC + page' → test 'RBAC Admin reaches page' (IMPLEMENT NOW), test 'RBAC HC reaches page' (IMPLEMENT NOW), test 'RBAC Coachee denied' (IMPLEMENT NOW), test 'wizard nav 6 pills' (IMPLEMENT NOW).
      - describe 'INJ-04 setup + cek judul' → test.skip 'cek judul', test.skip 'backdate guard'.
      - describe 'INJ-06 worker picker' → test.skip 'picker search/select'.
      - describe 'INJ-05 authoring' → test.skip 'authoring type toggle + add soal'.
      - describe 'INJ-07 cert radio' → test.skip 'cert radio 3-mode toggle'.
      - describe 'D-07 step5 placeholder + no DB write' → test.skip 'step5 placeholder navigable'.
    Implement the 4 RBAC/nav tests now (see Task 4 acceptance for selectors).
    Create `HcPortal.Tests/InjectViewModelMapTests.cs`: xUnit test class `InjectViewModelMapTests` with `[Trait("Category","Unit")]` (so it runs under `--filter Category!=Integration`). Add one passing scaffold fact `[Fact] public void Scaffold_MapsTitleAndType()` that constructs an `InjectAssessmentViewModel` (from this plan's Task 2) with Title/Category/AssessmentType set and asserts those scalars survive a mapping helper — keep it minimal here; Plan 04 fills full Questions/Workers/CertMode mapping assertions. If the controller mapping method does not exist yet, assert directly on the VM properties so the build stays green.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/inject-assessment-394.spec.ts --list 2>&1 | findstr /C:"RBAC" /C:"wizard nav"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/inject-assessment-394.spec.ts` exists; `grep -c "test\\.describe" inject-assessment-394.spec.ts` >= 6; contains strings "RBAC Admin", "RBAC Coachee", "wizard nav 6 pills".
    - File `HcPortal.Tests/InjectViewModelMapTests.cs` exists; contains `class InjectViewModelMapTests` and `[Trait("Category"`.
    - `dotnet test --filter Category!=Integration` builds and the scaffold fact passes (no regression to existing 347 tests).
    - `npx playwright test e2e/inject-assessment-394.spec.ts --list` lists the RBAC + wizard-nav tests (skipped ones allowed).
  </acceptance_criteria>
  <done>Both test scaffold files exist; xUnit scaffold fact green; Playwright spec lists named tests; existing suite unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: ViewModel + Controller (GET feed, RBAC, View()-folder override)</name>
  <read_first>
    - Models/InjectAssessmentDtos.cs (target contract — DO NOT modify; the VM mirrors this surface)
    - Controllers/AssessmentAdminController.cs:19-20, 58-62, 649-668, 842-855 (class header, View override, GET feed, CheckTitleAvailability)
    - Controllers/AdminBaseController.cs:271-293 (NormalizeTitleForDup/FindTitleDuplicatesAsync — inherited helpers)
    - Program.cs:51-57 (InjectAssessmentService DI registration — VERIFIED scoped)
  </read_first>
  <files>ViewModels/InjectAssessmentViewModel.cs, Controllers/InjectAssessmentController.cs</files>
  <action>
    Create `ViewModels/InjectAssessmentViewModel.cs` in namespace `HcPortal.ViewModels` (or the project's existing ViewModel namespace — match a sibling file). Properties (lean to the DTO surface, NO image/maxCharacters/Proton fields):
      - Scalars: `string Title`, `string Category`, `string AssessmentType = "Standard"`, `DateTime CompletedAt`, `int DurationMinutes = 60`, `int PassPercentage = 70`, `bool AllowAnswerReview = true`.
      - Cert: `InjectCertMode CertMode = InjectCertMode.None`, `string? ManualCertNumber`, `DateTime? CertValidUntil`, `bool CertPermanent`.
      - Audience: `List<string> UserIds = new()`.
      - Questions: nested `class InjectQuestionVM { string QuestionText; string QuestionType = "MultipleChoice"; int ScoreValue = 10; int Order; string? ElemenTeknis; string? Rubrik; int TempId; List<InjectOptionVM> Options = new(); }` and `class InjectOptionVM { string OptionText = ""; bool IsCorrect; int TempId; }`; property `List<InjectQuestionVM> Questions = new()`. (Plan 03 populates Questions binding; declaring the shape now keeps the contract stable.)
      - Optionally `string? QuestionsJson` for hidden-JSON capture (Plan 03 decides bound-list vs JSON; declare both, leave Plan 03 to wire one).
    Create `Controllers/InjectAssessmentController.cs`:
      - `[Route("Admin/[action]")]` + class `InjectAssessmentController : AdminBaseController`.
      - Copy the View()-folder override verbatim from AssessmentAdminController.cs:58-62 (resolve `~/Views/Admin/{action}.cshtml`) so the action `InjectAssessment` renders `Views/Admin/InjectAssessment.cshtml`.
      - Constructor: pass base ctor args matching AssessmentAdminController's base call, plus DI-inject `HcPortal.Services.InjectAssessmentService injectService` and store in a field `_injectService` (used by Plan 04 POST; unused-but-stored is fine here).
      - `[HttpGet] [Authorize(Roles = "Admin, HC")] public async Task<IActionResult> InjectAssessment()`: populate ViewBag exactly like AssessmentAdminController.cs:653-668 BUT drop Proton/ParentCategories/renewal branches — only `ViewBag.Users` (Id/FullName/Email/Section, `.Where(u=>u.IsActive).OrderBy(FullName)`), `ViewBag.SelectedUserIds = new List<string>()`, `ViewBag.Sections = await _context.GetAllSectionsAsync()`, `ViewBag.Categories = AssessmentCategories.Where(IsActive).OrderBy(SortOrder).ThenBy(Name)`. Return `View(new InjectAssessmentViewModel())`.
      - Scaffold `[HttpPost] [Authorize(Roles = "Admin, HC")] [ValidateAntiForgeryToken] public async Task<IActionResult> InjectAssessment(InjectAssessmentViewModel vm)` as a STUB that re-renders the view with a notice "Lengkapi jawaban dulu (tahap berikutnya)" — DO NOT call `_injectService.InjectBatchAsync` (that wires in Phase 395 per D-07). Re-populate ViewBag feed before returning. Keep `[ValidateAntiForgeryToken]`.
    Do NOT re-declare CheckTitleAvailability here — Plan 02's view points at the existing AssessmentAdmin endpoint.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | findstr /C:"Build succeeded" /C:"error"</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/InjectAssessmentController.cs` exists; grep finds `[Route("Admin/[action]")]`, `[Authorize(Roles = "Admin, HC")]` (with space, appears >=2 times for GET+POST), `~/Views/Admin/`, and `_injectService` field assignment.
    - `ViewModels/InjectAssessmentViewModel.cs` exists; grep finds `class InjectAssessmentViewModel`, `CertMode`, `List<string> UserIds`, `class InjectQuestionVM`, `class InjectOptionVM`.
    - Grep confirms controller does NOT contain `InjectBatchAsync` (commit deferred to 395).
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
  </acceptance_criteria>
  <done>Controller compiles with RBAC on GET+POST, View-folder override, GET feed sans Proton/renewal, POST stub without service call; ViewModel surface declared matching InjectRequest.</done>
</task>

<task type="auto">
  <name>Task 3: Section-C card in Index.cshtml</name>
  <read_first>
    - Views/Admin/Index.cshtml:179-264 (Section-C row + sibling card pattern; Certificate Renewal card :244-263)
    - .planning/phases/394-page-setup-room-authoring-soal/394-UI-SPEC.md:103-105 (card copy + icon)
  </read_first>
  <files>Views/Admin/Index.cshtml</files>
  <action>
    Insert ONE new `col-md-4` card block inside the Section-C `<div class="row g-3 mb-2">`, immediately BEFORE the closing `</div>` at line 264 (i.e. after the "Certificate Renewal" card that ends ~:263). Copy the sibling card pattern verbatim, RBAC-gated `@if (User.IsInRole("Admin") || User.IsInRole("HC"))`. Link: `@Url.Action("InjectAssessment", "InjectAssessment")`. Icon `bi bi-clipboard-plus fs-5 text-primary`. Title `Inject Assessment Manual`. Desc (UI-SPEC line 104): `Catat hasil assessment manual (ujian luring/kertas, data migrasi) seakan online — muncul di riwayat, rincian jawaban, dan sertifikat opsional.`
    Do NOT modify the Section-D "Bulk Import Nilai (Excel)" card (:290-303) — retired in Phase 396, out of scope here.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | findstr /C:"Build succeeded" /C:"error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "InjectAssessment" Views/Admin/Index.cshtml` >= 1; the new block contains `bi bi-clipboard-plus`, `Inject Assessment Manual`, and `@if (User.IsInRole("Admin") || User.IsInRole("HC"))`.
    - `grep -c "Bulk Import Nilai" Views/Admin/Index.cshtml` == 1 (Section-D card untouched).
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
  </acceptance_criteria>
  <done>New Inject card renders in Section C for Admin/HC, links to /Admin/InjectAssessment; Section-D BulkBackfill card unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wizard scaffold view (6 pills + 6 panels + WizardController IIFE) + verify RBAC/nav with Playwright</name>
  <read_first>
    - Views/Admin/CreateAssessment.cshtml:76-103 (nav-pills markup + form + AntiForgeryToken), :877-937 (WizardController goToStep/updatePills/validateStep/populateSummary), :23-57 (TempData/ModelState alerts)
    - .planning/phases/394-page-setup-room-authoring-soal/394-UI-SPEC.md:106-123, 135-136 (mode banner copy, step labels/icons, nav button copy, alerts)
    - tests/e2e/inject-assessment-394.spec.ts (RBAC/nav tests scaffolded in Task 1)
  </read_first>
  <behavior>
    - Test 'RBAC Admin reaches page': login admin → goto /Admin/InjectAssessment → #wizardStepNav visible, URL stays on InjectAssessment.
    - Test 'RBAC HC reaches page': login hc → same.
    - Test 'RBAC Coachee denied': login coachee → goto /Admin/InjectAssessment → URL redirects to AccessDenied/Login OR #wizardStepNav NOT visible.
    - Test 'wizard nav 6 pills': 6 pills present (#pill-1..#pill-6); step-1 panel visible initially; click btnNext1 → step-2 panel visible + #pill-1 gets bg-success; click a btn-prev → returns to step-1.
  </behavior>
  <files>Views/Admin/InjectAssessment.cshtml</files>
  <action>
    Create `Views/Admin/InjectAssessment.cshtml` (model `@model HcPortal.ViewModels.InjectAssessmentViewModel`). Build the SCAFFOLD only (fields filled by Plan 02/03/04):
      - Page header `<h2 class="fw-bold mb-1">Inject Assessment Manual</h2>` + subtitle (UI-SPEC line 102) + mode banner `alert alert-info` + `bi bi-info-circle` (UI-SPEC line 106).
      - TempData/ModelState alert summary block mirrored from CreateAssessment.cshtml:23-57 (success/error/warning + ModelState invalid `alert-warning` + `<ul>`).
      - `<nav class="mb-4"><ul class="nav nav-pills nav-fill gap-2" id="wizardStepNav">` with 6 `<li><button type="button" class="nav-link..." id="pill-N">`: pill-1 active (`bg-primary text-white active` + `bi bi-circle-fill me-1`), pill-2..6 `text-muted border` + `disabled` + `bi bi-circle me-1`. Labels + icons per UI-SPEC line 107-108: 1 Setup Room (bi-gear) · 2 Pilih Pekerja (bi-people) · 3 Authoring Soal (bi-card-list) · 4 Sertifikat (bi-award) · 5 Jawaban (bi-pencil-square) · 6 Konfirmasi (bi-check2-circle).
      - One `<form asp-action="InjectAssessment" asp-controller="InjectAssessment" method="post" id="injectAssessmentForm" novalidate>` with `@Html.AntiForgeryToken()` inside, wrapping 6 `<div class="step-panel" id="step-N">` (step-1 visible, step-2..6 `d-none`). Each panel = a `card` with `<h5 class="fw-bold mb-4">Langkah N: ...</h5>` placeholder body (a `text-muted` "diisi tahap berikut" note is fine for steps 2-6 in THIS plan) + a footer `<div class="d-flex justify-content-between mt-4">` with left `btn-prev` (outline-secondary "Sebelumnya" + bi-arrow-left, omit on step-1) and right `btn-next` primary "Selanjutnya" + bi-arrow-right (id `btnNext1`..`btnNext5`); step-6 has a `btn-success btn-lg w-100` "Inject Assessment" + bi-cloud-arrow-up-fill (present but commit wired in 395) instead of btn-next.
      - `@section Scripts { ... }` with the WizardController IIFE ported from CreateAssessment.cshtml:877-937, REWIRED: `for (var i=1;i<=6;i++)` in updatePills; `goToStep` calls `populateSummary()` at `n===6`; `validateStep(n)` returns true for now (steps fill validation in later plans), step-5 returns true (placeholder). Wire `btnNext1..5` → `if(validateStep(currentStep)) goToStep(currentStep+1)`, `.btn-prev` → `goToStep(currentStep-1)`, pill click → `if(visitedSteps.has(n)) goToStep(n)`. Include `populateSummary(){}` empty stub (Plan 04 fills). Active/completed/visited/pending pill classes exactly as the analog.
      - Ensure `_ValidationScriptsPartial` is NOT required yet (no client jQuery validation in scaffold); rely on vanilla JS.
    After writing, BUILD then run the app from the MAIN tree and exercise the 4 RBAC/nav Playwright tests (Pitfall 4: app must run from main tree, AD-off).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | findstr /C:"Build succeeded" & cd tests && npx playwright test e2e/inject-assessment-394.spec.ts -g "RBAC|wizard nav" --workers=1 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"pill-" Views/Admin/InjectAssessment.cshtml` == 6; `grep -c "class=\"step-panel\"" ...` == 6; contains `wizardStepNav`, `@Html.AntiForgeryToken()`, `function updatePills`, `for (var i = 1; i <= 6`.
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error.
    - Playwright (AD-off, --workers=1, app from MAIN tree): 'RBAC Admin reaches page', 'RBAC HC reaches page', 'RBAC Coachee denied', 'wizard nav 6 pills' all PASS.
  </acceptance_criteria>
  <done>Wizard scaffold renders 6 pills + 6 panels; Admin/HC reach page, Coachee denied (server-side); forward/back nav + pill state work at runtime (Playwright green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Controller (GET /Admin/InjectAssessment) | Untrusted user role crosses here; must be gated server-side |
| Browser → Controller (POST stub) | Form post crosses here; CSRF + RBAC apply even to the stub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-394-RBAC | Elevation of Privilege | `GET/POST InjectAssessment` | mitigate | `[Authorize(Roles = "Admin, HC")]` on BOTH GET and POST actions (server-authoritative). Index.cshtml `@if` card-hide is cosmetic only — verified by Playwright Coachee-denied test. |
| T-394-CSRF | Tampering | `POST InjectAssessment` (stub) | mitigate | `[ValidateAntiForgeryToken]` on POST + `@Html.AntiForgeryToken()` inside the single `<form>` (mirror CreateAssessment:103). |
| T-394-XSS | Tampering (Injection) | wizard summary / re-rendered ModelState | mitigate | Razor auto-encodes; no `@Html.Raw` on user input. `populateSummary` (Plan 04) uses `.textContent`. ModelState alert uses Razor-encoded `<ul>` items. |
| T-394-IDOR | Information Disclosure | ViewBag.Users feed | accept | Feed lists only existing active AspNetUsers (no arbitrary-id surface); picker selection validated NIP-side by service preflight (393). No new IDOR surface. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test --filter Category!=Integration` → no regression (baseline 347/347) + new xUnit scaffold fact green.
- Playwright `e2e/inject-assessment-394.spec.ts -g "RBAC|wizard nav" --workers=1` (AD-off, MAIN tree) → 4 tests green.
- Grep: controller has RBAC on GET+POST, NO `InjectBatchAsync`; Index has Inject card; view has 6 pills/panels.
</verification>

<success_criteria>
INJ-03 satisfied: Admin/HC see Section-C card and open /Admin/InjectAssessment; Coachee denied (server-side); GET returns wizard with 6 pills; forward/back nav works at runtime. Controller/ViewModel/scaffold + test files in place for Plan 02/03/04 to extend. 0 DB write, 0 migration.
</success_criteria>

<output>
After completion, create `.planning/phases/394-page-setup-room-authoring-soal/394-01-SUMMARY.md`
</output>
