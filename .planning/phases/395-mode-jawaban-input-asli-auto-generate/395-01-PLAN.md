---
phase: 395-mode-jawaban-input-asli-auto-generate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/InjectAssessmentService.cs
  - HcPortal.Tests/BuildAutoGenAnswersTests.cs
autonomous: true
requirements: [INJ-08, INJ-09]
must_haves:
  truths:
    - "Auto-gen menghasilkan pola jawaban MC/MA yang, setelah di-grade pipeline, mencapai skor terkecil yang >= target"
    - "Bila target melebihi ceiling MC/MA (essay berat), TargetReachable=false dilaporkan (tidak di-cap diam-diam)"
    - "Pola auto-gen reproducible: input sama → pola identik; room berbeda → pola berbeda"
    - "Essay mode input-asli yang berisi skor tetapi teks kosong ditolak (D-04), tetapi essay auto-gen yang di-omit tidak terblokir"
  artifacts:
    - path: "Services/InjectAssessmentService.cs"
      provides: "BuildAutoGenAnswers + ComputeAutoGenSeed static-pure + rule TextAnswer-wajib di PreflightValidateAsync"
      contains: "BuildAutoGenAnswers"
    - path: "HcPortal.Tests/BuildAutoGenAnswersTests.cs"
      provides: "Unit pure: hit-target, boundary, ceiling-essay, seed reproducible, degenerate"
      contains: "BuildAutoGenAnswersTests"
  key_links:
    - from: "BuildAutoGenAnswers"
      to: "AssessmentScoreAggregator.Compute formula"
      via: "re-cek floor((int)((double)total/max*100)) setelah seleksi subset"
      pattern: "\\(int\\)\\(\\(double\\)"
    - from: "ComputeAutoGenSeed"
      to: "System.Security.Cryptography.SHA256"
      via: "ComputeHash(UTF8(canonical)) → BitConverter.ToInt32 & 0x7FFFFFFF"
      pattern: "SHA256"
---

<objective>
Bangun fondasi server-side auto-generate untuk Phase 395: helper murni `BuildAutoGenAnswers` (translasi skor target → pola jawaban MC/MA benar/salah eksplisit), `ComputeAutoGenSeed` (seed deterministik lintas-proses SHA-256), dan rule validasi BARU "teks essay wajib bila skor diisi" (D-04, mode-guarded) di `PreflightValidateAsync`. Semua dikunci unit test murni (tanpa DB).

Purpose: Lapisan translasi ini adalah satu-satunya algoritma genuine-baru di phase 395. Diletakkan server-side (per CONTEXT.md discretion) agar Phase 396 (Import Excel) bisa reuse — cegah duplikasi/drift. Controller (Plan 02) dan View (Plan 03) mengonsumsi helper ini; jadi ia adalah KONTRAK yang harus ada lebih dulu (interface-first).

Output: `BuildAutoGenAnswers` + `ComputeAutoGenSeed` + record `AutoGenResult` di `InjectAssessmentService.cs`; rule TextAnswer-wajib disisipkan di branch essay `PreflightValidateAsync`; `BuildAutoGenAnswersTests.cs` (unit pure, hijau).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-CONTEXT.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md

<interfaces>
<!-- Formula grading yang HARUS direplika BuildAutoGenAnswers (sumber kebenaran).
     Source: Helpers/AssessmentScoreAggregator.cs:26-60 — EF-free, truncation int, lulus '>='. -->
```csharp
// AssessmentScoreAggregator.Compute (Helpers/AssessmentScoreAggregator.cs:26-60)
public static ScoreAggregateResult Compute(IEnumerable<PackageQuestion> questions,
    IEnumerable<PackageUserResponse> responses, int passPercentage);
// per soal: maxScore += q.ScoreValue  (DENOMINATOR SELALU termasuk essay, :35)
//   MC: 1 opsi IsCorrect → +ScoreValue   MA: SetEquals(correct) → +ScoreValue   Essay: +EssayScore
// int percentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;   // FLOOR/truncation (:58)
// IsPassed = percentage >= passPercentage   // operator '>=' (:59)
public readonly record struct ScoreAggregateResult(int TotalScore, int MaxScore, int Percentage, bool IsPassed);
```

<!-- DTO yang dipakai (Models/InjectAssessmentDtos.cs) — JANGAN ubah; default ScoreValue=10 (:19). -->
```csharp
public class InjectQuestionSpec {
    public string QuestionText; public string QuestionType = "MultipleChoice"; // MultipleChoice/MultipleAnswer/Essay
    public int ScoreValue = 10; public int Order; public string? ElemenTeknis; public string? Rubrik;
    public int TempId; public List<InjectOptionSpec> Options = new();
}
public class InjectOptionSpec { public string OptionText; public bool IsCorrect; public int TempId; }
public class InjectAnswerSpec {
    public int QuestionTempId; public List<int> SelectedOptionTempIds = new();
    public string? TextAnswer; public int? EssayScore;
}
public class InjectRowError { public string Nip = ""; public string Message = ""; }
```

<!-- Branch essay PreflightValidateAsync existing (Services/InjectAssessmentService.cs:382-391) — SISIP rule di sini. -->
```csharp
if (qType == "Essay") {
    if (ans.SelectedOptionTempIds.Count > 0) errors.Add(new InjectRowError { Nip = w.Nip, Message = $"Soal essay tidak boleh punya opsi untuk NIP {w.Nip}." });
    if (!ans.EssayScore.HasValue) errors.Add(new InjectRowError { Nip = w.Nip, Message = $"Skor essay NIP {w.Nip} wajib diisi." });
    else if (ans.EssayScore.Value < 0 || ans.EssayScore.Value > qSpec.ScoreValue) errors.Add(new InjectRowError { Nip = w.Nip, Message = $"Skor essay NIP {w.Nip} di luar rentang 0..{qSpec.ScoreValue}." });
    // ← SISIP rule TextAnswer-wajib DI SINI
}
```

<!-- Pola test unit murni — analog HcPortal.Tests/AssessmentScoreAggregatorTests.cs (in-memory builder, no fixture, no [Trait Integration]). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tulis unit test murni BuildAutoGenAnswersTests (Wave 0 — RED)</name>
  <files>HcPortal.Tests/BuildAutoGenAnswersTests.cs</files>
  <read_first>
    - HcPortal.Tests/AssessmentScoreAggregatorTests.cs (pola builder in-memory, [Fact]/[Theory], TANPA fixture/DB — TIRU)
    - Models/InjectAssessmentDtos.cs (InjectQuestionSpec/InjectOptionSpec/InjectAnswerSpec yang akan dibangun di test)
    - Helpers/AssessmentScoreAggregator.cs:26-60 (formula floor + lulus '>=' yang menjadi sumber kebenaran target)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md (§Validation Architecture → Phase Requirements → Test Map; §Pattern 3/7 algoritma)
  </read_first>
  <behavior>
    - Test hit-target equal-weight: N=10 soal MC ScoreValue=10 (no essay), target=80 → BuildAutoGenAnswers menghasilkan tepat k=8 soal "benar"; map pola ke responses + Compute(passPercentage=70) → Percentage == 80 (smallest-such >= target).
    - Test hit-target smallest-such: target=75 (N=10 equal) → k=8 (floor(7*10/100*100)=70 < 75; floor(8*10/100*100)=80 >= 75) → Percentage == 80, BUKAN 90.
    - Test boundary off-by-one mixed-weight: campur ScoreValue berbeda (mis. 10,10,10,20,20,30 soal MC) target=60 → verifikasi Percentage hasil >= 60 (re-cek floor setelah seleksi, bukan asumsi k).
    - Test ceiling-essay: 2 soal MC (SV=10 masing-masing) + 1 essay (SV=80); target=50 → CeilingPercent = floor(20/100*100)=20; TargetReachable == false (target 50 > ceiling 20).
    - Test essay tidak disentuh: hasil Answers TIDAK pernah memuat InjectAnswerSpec ber-QuestionTempId milik soal Essay (auto-gen MC/MA only, D-08).
    - Test seed reproducible: ComputeAutoGenSeed("123","Judul","Kat",date,80) dipanggil 2× → int identik; ganti title/category/CompletedAt/nip/target → int berbeda (NIP-saja ditolak).
    - Test seed → pola: dua worker (NIP beda) di room sama → BuildAutoGenAnswers dengan seed masing-masing menghasilkan himpunan soal-benar yang BERBEDA (variasi pola D-07) tetapi keduanya tetap Percentage >= target.
    - Test degenerate all-correct MC: soal MC yang SEMUA opsi IsCorrect=true → tidak bisa dibuat salah → forced-correct (selalu menyumbang poin); BuildAutoGenAnswers tetap memenuhi target atau melaporkan unreachable bila forced-correct sudah melampaui/menghalangi target.
    - Test degenerate 1-opsi: soal MC dengan 1 opsi saja → forced-correct.
    - Test MA salah: soal MA (>=2 opsi, ada opsi salah) yang dipilih "salah" → SelectedOptionTempIds menghasilkan SetEquals(correct)=false saat di-grade (Percentage tidak menyumbang poin soal itu).
  </behavior>
  <action>
Buat file `HcPortal.Tests/BuildAutoGenAnswersTests.cs`, `namespace HcPortal.Tests;`, kelas `public class BuildAutoGenAnswersTests` (TANPA `[Trait("Category","Integration")]` — unit murni, masuk fast suite). TIRU pola builder in-memory dari `AssessmentScoreAggregatorTests.cs`: buat helper privat statis untuk membangun `InjectQuestionSpec`/`InjectOptionSpec` (mis. `Q(int tempId, string type, int sv, params (int optTemp, bool correct)[] opts)`).

Test memanggil API yang BELUM ada (RED): `InjectAssessmentService.BuildAutoGenAnswers(IReadOnlyList<InjectQuestionSpec> questions, int targetPercent, int seed)` → `AutoGenResult` dan `InjectAssessmentService.ComputeAutoGenSeed(string nip, string title, string category, DateTime completedAt, int targetPercent)` → `int`.

Untuk membuktikan "skor >= target", petakan `AutoGenResult.Answers` + soal ke in-memory `PackageQuestion`/`PackageOption`/`PackageUserResponse` (TempId sebagai Id sintetis) lalu panggil `AssessmentScoreAggregator.Compute(qInMem, respInMem, passPercentage)` — TIRU §Code Examples RESEARCH.md (map pola usulan → in-memory). Assert `agg.Percentage >= target` dan untuk equal-weight assert nilai eksak smallest-such.

Tulis SEMUA `[Fact]`/`[Theory]` sesuai daftar di `<behavior>`. Jalankan `dotnet test --filter "FullyQualifiedName~BuildAutoGenAnswers&Category!=Integration"` — HARUS gagal compile/red (API belum ada). Itu state RED yang diharapkan; Task 2 membuatnya GREEN.

JANGAN gunakan `string.GetHashCode()` di test assertion apa pun. JANGAN gunakan DB/fixture.
  </action>
  <verify>
    <automated>dotnet build HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | Select-String -Pattern "BuildAutoGenAnswers|ComputeAutoGenSeed|error" | Select-Object -First 5</automated>
  </verify>
  <done>File `BuildAutoGenAnswersTests.cs` ada; berisi kelas `BuildAutoGenAnswersTests` dengan [Fact]/[Theory] untuk hit-target, smallest-such, boundary mixed-weight, ceiling-essay, essay-untouched, seed-reproducible, seed→pola-beda, degenerate all-correct, degenerate 1-opsi, MA-salah. Build gagal KARENA API belum ada (RED) — bukan karena syntax error test. TANPA [Trait Integration].</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BuildAutoGenAnswers + ComputeAutoGenSeed + rule TextAnswer-wajib (GREEN)</name>
  <files>Services/InjectAssessmentService.cs</files>
  <read_first>
    - Services/InjectAssessmentService.cs:1-75 (header, ctor DI, InjectBatchAsync entry — tahu konteks kelas)
    - Services/InjectAssessmentService.cs:336-404 (PreflightValidateAsync — branch essay :382-391 tempat sisip rule D-04)
    - Helpers/AssessmentScoreAggregator.cs:26-60 (formula floor (int)((double)total/max*100) + lulus '>=' — WAJIB direplika)
    - Helpers/CertNumberHelper.cs (pola static-pure helper convention)
    - Controllers/AssessmentAdminController.cs:2770-2784 (pola crypto-disposable `using` — closest analog; SHA256 sendiri belum ada di repo)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md (§Pattern 3 algoritma subset-sum, §Pattern 4 seed, §Pattern 7 MA/MC salah + degenerate)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md (§1, §2 KOREKSI SHA256, §6 rule TextAnswer)
  </read_first>
  <action>
Tambahkan ke kelas `InjectAssessmentService` (di `Services/InjectAssessmentService.cs`):

**(A) Record hasil** — `public sealed record AutoGenResult(List<InjectAnswerSpec> Answers, int CeilingPercent, int MaxScoreIncludingEssay, bool TargetReachable);`

**(B) `public static int ComputeAutoGenSeed(string nip, string title, string category, DateTime completedAt, int targetPercent)`** — seed deterministik lintas-proses. KOREKSI LOAD-BEARING (PATTERNS §2): SHA256 BELUM PERNAH dipakai di repo (`AssessmentAdminController.cs:2774` adalah `RandomNumberGenerator`, BUKAN SHA256) — tulis dari nol pakai BCL. String kanonik = `string.Join('', (nip ?? "").Trim(), (title ?? "").Trim(), (category ?? "").Trim(), completedAt.ToString("yyyy-MM-dd"), targetPercent.ToString())` (pemisah `` = unit separator, cegah tabrakan concat; `CompletedAt` HANYA tanggal `yyyy-MM-dd` agar tahan beda jam preview vs commit). Lalu:
```csharp
using var sha = System.Security.Cryptography.SHA256.Create();
var hash = sha.ComputeHash(System.Text.Encoding.UTF8.GetBytes(canonical));
return BitConverter.ToInt32(hash, 0) & 0x7FFFFFFF;   // non-negatif untuk new Random(seed)
```
JANGAN pakai `string.GetHashCode()` (randomized per-proses → preview≠commit).

**(C) `public static AutoGenResult BuildAutoGenAnswers(IReadOnlyList<InjectQuestionSpec> questions, int targetPercent, int seed)`** — algoritma deterministik (RESEARCH §Pattern 3):
1. `maxScore = Σ ScoreValue SEMUA soal` (termasuk essay — denominator selalu termasuk essay, samakan dengan Aggregator :35).
2. Ambil soal MC/MA saja (`QuestionType` != "Essay"), urutkan STABIL: `OrderBy(q => q.Order).ThenBy(q => q.TempId)` (samakan urutan persist `InjectAssessmentService.cs:146`).
3. `mcMaPoints = Σ ScoreValue soal MC/MA`. `CeilingPercent = maxScore > 0 ? (int)((double)mcMaPoints / maxScore * 100) : 0` (FORMULA SAMA Aggregator :58).
4. Bila `targetPercent > CeilingPercent` → `TargetReachable = false`; kembalikan best-effort = SEMUA MC/MA benar (caller tampilkan ceiling, controller emit BLOCKING D-08.3 dan TIDAK commit worker itu). JANGAN cap diam-diam.
5. **Pre-scan degenerate (PATTERNS §Pattern 7):** per soal hitung `correctCount`/`optionCount`. Soal "forced-correct" = MC dengan SEMUA opsi IsCorrect, ATAU soal 1-opsi → SELALU benar (tak bisa dibuat salah). Tetapkan forced-correct dulu (selalu menyumbang poin).
6. **Pemilihan WHICH soal benar (seeded, D-07):** dari soal MC/MA fleksibel (non-forced), acak urutan kandidat dengan `new Random(seed)` lalu pilih subset agar `floor((Σpoin_benar)/maxScore×100) >= targetPercent` dengan jumlah poin terkecil yang memenuhi (smallest-such). Equal-weight: closed-form `k = (int)Math.Ceiling(targetPercent * Nflex / 100.0)` lalu naikkan k bila floor masih < target. Mixed-weight: greedy `OrderByDescending(ScoreValue)` (acak dalam grup ScoreValue-sama via seed) akumulasi sampai floor >= target, lalu coba kurangi soal terkecil bila tetap >= target.
7. **WAJIB re-cek** `(int)((double)(Σpoin_benar)/maxScore*100) >= targetPercent` SETELAH seleksi final (boundary off-by-one); bila < target tambah 1 soal benar dan ulang.
8. **Konstruksi jawaban per soal MC/MA** (PATTERNS §Pattern 7):
   - benar: `SelectedOptionTempIds` = opsi `IsCorrect` (MC: 1; MA: SEMUA yang IsCorrect).
   - salah MC (≥2 opsi, ada opsi salah): pilih 1 opsi `!IsCorrect` deterministik `OrderBy(TempId).First(o => !o.IsCorrect)`.
   - salah MA (ada opsi salah): pilih {1 opsi salah} `OrderBy(TempId).First(!IsCorrect)`; bila semua opsi benar tapi >1 opsi → proper-subset (buang 1, mis. `OrderBy(TempId).Skip(1)`) agar `SetEquals` gagal.
   - forced-correct (semua benar / 1-opsi): selalu pilih opsi benar.
   Emit `InjectAnswerSpec{ QuestionTempId, SelectedOptionTempIds }` untuk SETIAP soal MC/MA (benar atau salah eksplisit). Essay TIDAK di-emit (D-08 — HC isi manual, caller gabung).
9. Kembalikan `new AutoGenResult(answers, CeilingPercent, maxScore, targetReachable)`.

**(D) Rule TextAnswer-wajib (D-04, mode-guarded)** — sisip di branch essay `PreflightValidateAsync` SETELAH cek range EssayScore (sekitar :390), TEPAT pola berikut:
```csharp
if (ans.EssayScore.HasValue && string.IsNullOrWhiteSpace(ans.TextAnswer))
    errors.Add(new InjectRowError { Nip = w.Nip, Message = $"Teks jawaban essay NIP {w.Nip} wajib diisi (mode input asli)." });
```
KRITIS: guard `ans.EssayScore.HasValue` (= essay "engaged"). Essay di-skip (D-05) = OMIT spec → tak masuk loop → tak terblokir. JANGAN buat validasi global — auto-gen essay yang sah teks-kosong tidak boleh terblokir (D-04). Pakai pola `errors.Add(new InjectRowError{...})` Bahasa Indonesia, kumpul semua (reject-all D-03), TIDAK menulis DB.

Setelah implement, jalankan `dotnet test --filter "FullyQualifiedName~BuildAutoGenAnswers&Category!=Integration"` — HARUS hijau (GREEN). Lalu `dotnet build HcPortal.csproj` 0 error.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~BuildAutoGenAnswers&Category!=Integration" 2>&1 | Select-String -Pattern "Passed!|Failed!|error" | Select-Object -First 5</automated>
  </verify>
  <done>`BuildAutoGenAnswers` + `ComputeAutoGenSeed` + record `AutoGenResult` ada di `InjectAssessmentService.cs`; rule TextAnswer-wajib ber-guard `EssayScore.HasValue` tersisip di branch essay `PreflightValidateAsync`. `ComputeAutoGenSeed` memakai `System.Security.Cryptography.SHA256` (BUKAN GetHashCode). `BuildAutoGenAnswers` re-cek `floor()` formula `(int)((double).../max*100)` setelah seleksi. Unit test BuildAutoGenAnswers GREEN; `dotnet build` 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: Gate regression + 0-migration</name>
  <files>Services/InjectAssessmentService.cs</files>
  <read_first>
    - Services/InjectAssessmentService.cs (konfirmasi hanya penambahan helper + 1 rule, tak ubah happy-path InjectBatchAsync)
  </read_first>
  <action>
Jalankan fast suite penuh untuk memastikan penambahan helper + rule TextAnswer tidak meregresi test existing (393 InjectAssessmentServiceTests + 394). Jalankan `dotnet test --filter "Category!=Integration"`. Lalu konfirmasi 0 migration: `dotnet ef migrations add _verify395p1 --project HcPortal.csproj` lalu cek file migration yang dihasilkan KOSONG (hanya `Up()`/`Down()` tanpa operasi), lalu `dotnet ef migrations remove --project HcPortal.csproj` untuk membatalkan. Bila ada model diff → ada bug (phase ini 0 migration). Tidak ada DTO/schema baru — `AutoGenResult` adalah record in-memory (bukan entity), tak masuk DbContext.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration" 2>&1 | Select-String -Pattern "Passed!|Failed!" | Select-Object -First 2</automated>
  </verify>
  <done>Fast suite hijau (tak ada regresi vs baseline 393/394). `dotnet ef migrations add _verify395p1` menghasilkan migration KOSONG (0 model diff) → di-remove. 0 migration dikonfirmasi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→service | `BuildAutoGenAnswers` menerima pola/target dari controller; output dipakai untuk commit + preview |
| pre-commit validation | `PreflightValidateAsync` adalah server-authoritative gate sebelum write DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-395-01 | Tampering | `BuildAutoGenAnswers` ceiling | mitigate | `TargetReachable=false` dikembalikan saat target>ceiling; helper TIDAK cap diam-diam — controller (Plan 02) WAJIB BLOCKING, integritas sertifikasi (D-08.3) |
| T-395-02 | Repudiation | seed non-deterministik | mitigate | `ComputeAutoGenSeed` SHA-256 deterministik lintas-proses (BUKAN `GetHashCode`) → preview==commit dapat dibuktikan; pola reproducible (dikunci unit test seed) |
| T-395-03 | Tampering | rule TextAnswer-wajib | mitigate | Validasi server-authoritative di `PreflightValidateAsync` ber-guard `EssayScore.HasValue`; reject-all batch (D-03), tak menulis DB |
| T-395-04 | Information Disclosure | SHA-256 seed | accept | SHA-256 dipakai HANYA untuk determinisme, BUKAN kontrol keamanan — non-secret; tidak ada klaim kriptografis (ASVS V6 catatan RESEARCH) |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~BuildAutoGenAnswers&Category!=Integration"` hijau (unit pure subset-sum/seed/ceiling/degenerate).
- `dotnet test --filter "Category!=Integration"` hijau (no regression).
- `dotnet build HcPortal.csproj` 0 error.
- `dotnet ef migrations add _verify395p1` → migration kosong → remove. 0 migration.
- Grep `SHA256` ada di `InjectAssessmentService.cs`; grep `GetHashCode` TIDAK ada.
</verification>

<success_criteria>
- `BuildAutoGenAnswers` menghasilkan pola yang, di-grade pipeline, mencapai skor terkecil >= target (equal-weight closed-form + mixed-weight greedy+verify, re-cek floor). *(INJ-09 SC2)*
- `CeilingPercent`/`TargetReachable` dilaporkan benar untuk room ber-essay (target>ceiling → false, tak di-cap). *(INJ-09 SC3 fondasi)*
- `ComputeAutoGenSeed` reproducible & berbeda antar room (NIP+identitas room+target). *(INJ-09 D-07)*
- Rule TextAnswer-wajib (D-04) menolak essay input-asli skor-terisi-teks-kosong, TIDAK menolak essay auto-gen di-omit. *(INJ-08 D-04)*
- 0 migration dikonfirmasi.
</success_criteria>

<output>
After completion, create `.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-01-SUMMARY.md`
</output>
