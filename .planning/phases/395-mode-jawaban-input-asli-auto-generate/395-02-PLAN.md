---
phase: 395-mode-jawaban-input-asli-auto-generate
plan: 02
type: execute
wave: 2
depends_on: ["395-01"]
files_modified:
  - Controllers/InjectAssessmentController.cs
  - ViewModels/InjectAssessmentViewModel.cs
  - Models/InjectAssessmentDtos.cs
  - HcPortal.Tests/InjectPreviewEqualsCommitTests.cs
autonomous: true
requirements: [INJ-08, INJ-09]
must_haves:
  truths:
    - "POST /Admin/InjectAssessment me-commit aktual via InjectBatchAsync (commit pertama milestone — 394 tak commit)"
    - "Endpoint POST /Admin/PreviewInjectScore mengembalikan skor final + Lulus/Tidak + ceiling/overshoot/blocking TANPA nomor sertifikat dan TANPA write DB"
    - "Skor preview == skor commit (engine AssessmentScoreAggregator.Compute identik di kedua jalur)"
    - "Worker auto-gen di-resolve server-authoritative: BuildAutoGenAnswers dipanggil di controller, mode/target tak pernah masuk DTO/service"
  artifacts:
    - path: "Controllers/InjectAssessmentController.cs"
      provides: "PreviewInjectScore action + ParseAnswerVms + MapToRequest isi Answers + wire commit InjectBatchAsync"
      contains: "PreviewInjectScore"
    - path: "ViewModels/InjectAssessmentViewModel.cs"
      provides: "AnswersJson + InjectAnswerVM + InjectWorkerAnswersVM"
      contains: "AnswersJson"
    - path: "Models/InjectAssessmentDtos.cs"
      provides: "InjectPreviewRequest + InjectPreviewResult DTO"
      contains: "InjectPreviewResult"
    - path: "HcPortal.Tests/InjectPreviewEqualsCommitTests.cs"
      provides: "Integration real-SQL: preview==commit, skip=omit grade 0, TextAnswer-wajib reject"
      contains: "PreviewEqualsCommit"
  key_links:
    - from: "Controllers/InjectAssessmentController.cs MapToRequest:116"
      to: "ParseAnswerVms → InjectWorkerSpec.Answers"
      via: "match per-worker by NIP/UserId; auto worker → BuildAutoGenAnswers(seed)"
      pattern: "ParseAnswerVms"
    - from: "PreviewInjectScore"
      to: "AssessmentScoreAggregator.Compute"
      via: "map pola usulan → in-memory PackageQuestion/Response (TempId=Id sintetis)"
      pattern: "AssessmentScoreAggregator\\.Compute"
    - from: "POST InjectAssessment"
      to: "_injectService.InjectBatchAsync"
      via: "ganti blok no-commit 394 dengan commit aktual + actor identity"
      pattern: "InjectBatchAsync"
---

<objective>
Wire jalur server-side phase 395: endpoint dry-run `PreviewInjectScore` (skor final pra-commit via `AssessmentScoreAggregator.Compute`, NO cert#, NO write — D-09); `ParseAnswerVms` (deserialize `#AnswersJson` per-worker, paralel `ParseQuestionVms`); isi `MapToRequest:116` `Answers` (kini hardcoded `new()`) dari jawaban per-worker — input-asli langsung, auto-gen via `BuildAutoGenAnswers(seed)` server-authoritative; dan **wire `#btnInject` POST → `InjectBatchAsync`** (commit aktual PERTAMA di milestone — 394 berhenti sebelum commit). Dikunci integration test real-SQL preview==commit + skip=omit + TextAnswer-wajib.

Purpose: Ini lapisan controller yang menjembatani helper Plan 01 ke commit + preview. Server adalah otoritas pola (RESEARCH A1): client kirim {mode,target,essayScores,answers}; server hitung pola+skor; commit memakai pola identik → preview==commit terjamin via seed deterministik. View (Plan 03) mengonsumsi endpoint preview + form ini.

Output: `PreviewInjectScore` + `ParseAnswerVms` + `MapToRequest` terisi + commit wired di `InjectAssessmentController.cs`; `AnswersJson`/`InjectAnswerVM`/`InjectWorkerAnswersVM` di VM; `InjectPreviewRequest`/`InjectPreviewResult` di DTO; `InjectPreviewEqualsCommitTests.cs` (integration, hijau).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-CONTEXT.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-01-SUMMARY.md

<interfaces>
<!-- Helper Plan 01 yang dikonsumsi (Services/InjectAssessmentService.cs). -->
```csharp
public sealed record AutoGenResult(List<InjectAnswerSpec> Answers, int CeilingPercent, int MaxScoreIncludingEssay, bool TargetReachable);
public static AutoGenResult BuildAutoGenAnswers(IReadOnlyList<InjectQuestionSpec> questions, int targetPercent, int seed);
public static int ComputeAutoGenSeed(string nip, string title, string category, DateTime completedAt, int targetPercent);
public async Task<InjectResult> InjectBatchAsync(InjectRequest req, string actorUserId, string actorName);  // existing 393
```

<!-- MapToRequest existing (Controllers/InjectAssessmentController.cs:71-123) — Answers=new() di :116 yang harus diisi.
     ParseQuestionVms (:126-139) = analog ParseAnswerVms (try/catch JsonException → fallback). -->
```csharp
public static InjectRequest MapToRequest(InjectAssessmentViewModel vm, IReadOnlyDictionary<string,string> userIdToNip);
// loop UserId→NIP (:109-120): req.Workers.Add(new InjectWorkerSpec { Nip = nip, Answers = new(), ... });  // ← isi Answers
private static List<InjectAssessmentViewModel.InjectQuestionVM> ParseQuestionVms(InjectAssessmentViewModel vm);  // :126-139
// POST action (:52-66): saat ini TempData["Info"] no-commit (D-07 394). userIdToNip dict dibangun :56-58.
```

<!-- POST action existing (:49-66) — atribut RBAC + antiforgery yang harus diulang di PreviewInjectScore. -->
```csharp
[HttpPost] [Authorize(Roles = "Admin, HC")] [ValidateAntiForgeryToken]
public async Task<IActionResult> InjectAssessment(InjectAssessmentViewModel vm)
```

<!-- Engine preview (Helpers/AssessmentScoreAggregator.cs:26-60) — formula identik commit (InjectAssessmentService.cs:236). -->
```csharp
public static ScoreAggregateResult Compute(IEnumerable<PackageQuestion> questions, IEnumerable<PackageUserResponse> responses, int passPercentage);
// percentage = (int)((double)totalScore / maxScore * 100);  IsPassed = percentage >= passPercentage;
```

<!-- VM existing (ViewModels/InjectAssessmentViewModel.cs:33,36-54) — QuestionsJson + nested VM pola. -->
```csharp
public string? QuestionsJson { get; set; }   // :33 — tambah paralel AnswersJson
public class InjectQuestionVM { /* QuestionText, QuestionType, ScoreValue, Order, ElemenTeknis, Rubrik, TempId, Options */ }
public class InjectOptionVM { public string OptionText; public bool IsCorrect; public int TempId; }
```

<!-- DTO existing (Models/InjectAssessmentDtos.cs) — InjectAnswerSpec shape yang VM-nya di-mirror. -->
```csharp
public class InjectAnswerSpec { public int QuestionTempId; public List<int> SelectedOptionTempIds = new(); public string? TextAnswer; public int? EssayScore; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTO preview + VM AnswersJson/InjectAnswerVM</name>
  <files>Models/InjectAssessmentDtos.cs, ViewModels/InjectAssessmentViewModel.cs</files>
  <read_first>
    - Models/InjectAssessmentDtos.cs (InjectAnswerSpec :27-34, InjectRowError :64-69, InjectResult :71-80 — pola POCO/record yang ditiru)
    - ViewModels/InjectAssessmentViewModel.cs (QuestionsJson :33, InjectQuestionVM/InjectOptionVM :36-54 — pola nested VM)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md (§7 VM, §3 record AutoGenResult, §13 DTO preview)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md (§Pattern 5 endpoint shape request/response)
  </read_first>
  <action>
**(A) DTO preview** — di `Models/InjectAssessmentDtos.cs` tambah:
```csharp
/// <summary>Request dry-run preview skor 1 worker (D-09 — pra-persist, NO cert#).</summary>
public class InjectPreviewRequest
{
    public int PassPercentage { get; set; } = 70;
    public string Title { get; set; } = "";
    public string Category { get; set; } = "";
    public DateTime CompletedAt { get; set; }
    public string Nip { get; set; } = "";
    public string Mode { get; set; } = "manual";   // "manual" | "auto"
    public int TargetScore { get; set; }            // auto-gen only
    public List<InjectQuestionSpec> Questions { get; set; } = new();
    public List<InjectAnswerSpec> Answers { get; set; } = new();   // manual/essay manual; auto MC/MA dihitung server
}

/// <summary>Hasil preview skor final (D-09) — TANPA nomor sertifikat.</summary>
public class InjectPreviewResult
{
    public int Percentage { get; set; }
    public bool IsPassed { get; set; }
    public int TotalScore { get; set; }
    public int MaxScore { get; set; }
    public int CeilingPercent { get; set; }       // auto-gen ceiling MC/MA-only (D-08.3)
    public bool TargetReachable { get; set; } = true;
    public int Overshoot { get; set; }            // actual - target (auto-gen; >=0 karena bias jamin-lulus)
    public bool Blocked { get; set; }             // true bila target>ceiling (BLOCKING, D-08.3)
    public string? BlockingMessage { get; set; }  // Bahasa Indonesia
}
```

**(B) VM** — di `ViewModels/InjectAssessmentViewModel.cs`:
- Tambah `public string? AnswersJson { get; set; }` (paralel `QuestionsJson` :33).
- Tambah nested `public class InjectAnswerVM { public int QuestionTempId { get; set; } public List<int> SelectedOptionTempIds { get; set; } = new(); public string? TextAnswer { get; set; } public int? EssayScore { get; set; } }` (mirror `InjectAnswerSpec`).
- Tambah nested wrapper `public class InjectWorkerAnswersVM { public string UserId { get; set; } = ""; public string Mode { get; set; } = "manual"; public int TargetScore { get; set; } public List<InjectAnswerVM> Answers { get; set; } = new(); }` — per-worker payload (key = checkbox value = user.Id, sama dengan `UserIds`).

JANGAN ubah `InjectRequest`/`InjectAnswerSpec`/`InjectWorkerSpec` existing (DTO 393 stabil; mode/target = lapisan VM/controller, TIDAK masuk DTO — D-02).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 5</automated>
  </verify>
  <done>`InjectPreviewRequest`/`InjectPreviewResult` ada di `InjectAssessmentDtos.cs`; `AnswersJson` + `InjectAnswerVM` + `InjectWorkerAnswersVM` ada di `InjectAssessmentViewModel.cs`. `InjectRequest`/`InjectAnswerSpec`/`InjectWorkerSpec` TIDAK berubah (no field Mode/Target di DTO). `dotnet build` 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: ParseAnswerVms + MapToRequest isi Answers + PreviewInjectScore + wire commit</name>
  <files>Controllers/InjectAssessmentController.cs</files>
  <read_first>
    - Controllers/InjectAssessmentController.cs (FULL — POST :49-66, MapToRequest :71-123, ParseQuestionVms :126-139, View-override :33-36, ctor _injectService :19/:29)
    - Services/InjectAssessmentService.cs:42-66 (InjectBatchAsync signature + reject-all return shape InjectResult)
    - Helpers/AssessmentScoreAggregator.cs:26-60 (Compute — engine preview)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md (§3 PreviewInjectScore, §4 ParseAnswerVms, §5 MapToRequest, §10 wire commit, Shared RBAC/CSRF/JSON-fallback)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md (§Pattern 5 dry-run, §Code Examples map pola→in-memory, §Open-Q 1 server-otoritas)
  </read_first>
  <action>
**(A) `ParseAnswerVms`** — method privat statis paralel `ParseQuestionVms:126-139` (try/catch JsonException → fallback `new()`):
```csharp
private static List<InjectAssessmentViewModel.InjectWorkerAnswersVM> ParseAnswerVms(InjectAssessmentViewModel vm)
{
    if (!string.IsNullOrWhiteSpace(vm.AnswersJson))
    {
        try {
            var parsed = JsonSerializer.Deserialize<List<InjectAssessmentViewModel.InjectWorkerAnswersVM>>(
                vm.AnswersJson, new JsonSerializerOptions { PropertyNameCaseInsensitive = true });
            if (parsed != null) return parsed;
        } catch (JsonException) { /* malformed → fallback (Security V5) */ }
    }
    return new();
}
```

**(B) Isi `MapToRequest:116` `Answers`** — `MapToRequest` perlu akses jawaban per-worker. Karena `MapToRequest` static & saat ini hanya terima `(vm, userIdToNip)`, tambah parameter `IReadOnlyList<InjectWorkerAnswersVM> workerAnswers` (panggil `ParseAnswerVms(vm)` di POST action dan teruskan). Di loop `foreach userId` (:109-120), ganti `Answers = new()` dengan resolusi per-worker (key = userId):
- Cari `wa = workerAnswers.FirstOrDefault(x => x.UserId == userId)`.
- Bila `wa?.Mode == "auto"`: panggil `InjectAssessmentService.BuildAutoGenAnswers(req.Questions, wa.TargetScore, InjectAssessmentService.ComputeAutoGenSeed(nip, vm.Title ?? "", vm.Category ?? "", vm.CompletedAt, wa.TargetScore))` → ambil `.Answers` (MC/MA), LALU gabung essay manual dari `wa.Answers` (essay = `InjectAnswerSpec` ber-`EssayScore`/`TextAnswer`, soal `QuestionType=="Essay"`). Server-otoritas pola (RESEARCH A1: commit pakai seed deterministik = pola identik preview).
- Bila mode `manual` (atau null): map `wa.Answers` → `List<InjectAnswerSpec>` (skip=omit sudah ditangani client; controller hanya copy yang dikirim).
- Worker tanpa entry `workerAnswers` → `Answers = new()` (akan grade 0 — tapi pre-flight MC kosong tak terkirim, jadi tak reject-all; lihat D-05 skip=omit).
- **BLOCKING auto-gen (D-08.3):** bila `AutoGenResult.TargetReachable == false`, worker itu TIDAK boleh ter-commit. Karena commit batch atomic, pendekatan: di POST action, sebelum `InjectBatchAsync`, hitung ulang per worker auto apakah reachable; bila ada worker auto unreachable → JANGAN commit, kembalikan View dengan `TempData["Error"]` "Ada pekerja auto-generate dengan target melebihi ceiling — beralih ke input asli." (UI Plan 03 sudah mencegah ini di klien; ini guard server-authoritative). Jangan diam-diam cap.

**(C) `PreviewInjectScore` action (NEW)** — endpoint dry-run, NO write, NO cert#:
```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public IActionResult PreviewInjectScore([FromBody] InjectPreviewRequest preq)  { ... }
```
Logika:
1. Tentukan pola jawaban final: bila `preq.Mode == "auto"` → `var ag = InjectAssessmentService.BuildAutoGenAnswers(preq.Questions, preq.TargetScore, InjectAssessmentService.ComputeAutoGenSeed(preq.Nip, preq.Title, preq.Category, preq.CompletedAt, preq.TargetScore));` lalu `answers = ag.Answers` + gabung essay manual dari `preq.Answers` (soal Essay). Bila `manual` → `answers = preq.Answers`.
2. Map `preq.Questions` + `answers` → in-memory `PackageQuestion`/`PackageOption`/`PackageUserResponse` (TempId sebagai Id sintetis) — TIRU §Code Examples RESEARCH (Essay → response ber-EssayScore/TextAnswer; MC/MA → 1 response per SelectedOptionTempId; soal di-omit dari answers → tak ada response → grade 0).
3. `var agg = AssessmentScoreAggregator.Compute(qInMem, respInMem, preq.PassPercentage);` (formula identik commit → preview==commit).
4. Susun `InjectPreviewResult`: `Percentage=agg.Percentage`, `IsPassed=agg.IsPassed`, `TotalScore/MaxScore`. Untuk auto: `CeilingPercent=ag.CeilingPercent`, `TargetReachable=ag.TargetReachable`, `Blocked = !ag.TargetReachable`, `Overshoot = Math.Max(0, agg.Percentage - preq.TargetScore)`, `BlockingMessage = Blocked ? $"Target {preq.TargetScore}% tidak tercapai: bobot essay dikecualikan dari auto-generate, maksimum {ag.CeilingPercent}%. Beralih ke input asli dan naikkan skor essay secara manual." : null`.
5. **JANGAN** panggil `CertNumberHelper` (D-09). **JANGAN** menulis DB (no SaveChanges). Return `Json(result)`.
6. Catatan View-override: action ini return `Json(...)` (bukan View) → tidak terpengaruh override `~/Views/Admin/` (yang hanya untuk `ViewResult`). Aman.

**(D) Wire commit `#btnInject`** — di POST `InjectAssessment` action (:52-66), GANTI blok no-commit `TempData["Info"]` (:62-65) dengan commit aktual:
```csharp
var workerAnswers = ParseAnswerVms(vm);
var req = MapToRequest(vm, userIdToNip, workerAnswers);
// guard BLOCKING auto-gen unreachable (D-08.3) — lihat (B).
var actorUserId = _userManager.GetUserId(User) ?? "";
var actorName = User?.Identity?.Name ?? actorUserId;
var injResult = await _injectService.InjectBatchAsync(req, actorUserId, actorName);
if (injResult.Rejected) { TempData["Error"] = injResult.Message ?? "Inject ditolak."; /* surface PerRowErrors */ }
else if (injResult.Success) { TempData["Success"] = $"Inject berhasil: {injResult.SuccessSessionIds.Count} sesi. Skip: {injResult.SkippedNips.Count}."; }
await PopulateFeedAsync();
return View(vm);
```
Resolusi `actorUserId`/`actorName`: gunakan `_userManager` (sudah di base controller; cek anggota tersedia — `AdminBaseController` punya `_userManager`). InjectBatchAsync sudah di-DI via `_injectService` (:19/:29).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 5</automated>
  </verify>
  <done>`ParseAnswerVms` (try/catch JsonException) ada; `MapToRequest` mengisi `Answers` per-worker (manual = copy; auto = `BuildAutoGenAnswers(seed)` + gabung essay manual); `PreviewInjectScore` action ([Authorize Admin,HC] + [ValidateAntiForgeryToken], return Json, NO CertNumberHelper, NO SaveChanges) memakai `AssessmentScoreAggregator.Compute`; POST `InjectAssessment` memanggil `_injectService.InjectBatchAsync` (commit aktual, actor identity) menggantikan blok no-commit 394; guard BLOCKING auto-gen unreachable. `dotnet build` 0 error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test real-SQL preview==commit + skip=omit + TextAnswer-wajib</name>
  <files>HcPortal.Tests/InjectPreviewEqualsCommitTests.cs</files>
  <read_first>
    - HcPortal.Tests/InjectAssessmentServiceTests.cs:1-100 (fixture disposable HcPortalDB_Test_{guid}, NewGradingService VERBATIM, NewInjectService, SeedUserAsync — TIRU)
    - Helpers/AssessmentScoreAggregator.cs:26-60 (Compute — engine preview yang harus == commit)
    - Services/InjectAssessmentService.cs:42-66,180-243 (InjectBatchAsync finalize/grade — skor commit yang dibandingkan)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md (§13 test integration)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md (§Validation Architecture → Test Map INJ-08/09 integration)
  </read_first>
  <behavior>
    - PreviewEqualsCommit (input-asli): seed worker NIP, paket MC+MA+Essay jawaban campur (sebagian benar). Hitung preview via `AssessmentScoreAggregator.Compute` atas pola yang sama → preview.Percentage. Commit via `InjectBatchAsync` (req identik) → baca `AssessmentSession.Score` dari DB. Assert preview.Percentage == session.Score (preview == commit).
    - PreviewEqualsCommit (auto-gen): bangun req auto via `BuildAutoGenAnswers(questions, target, seed)`, gabung essay manual; preview Compute == commit session.Score; assert session.Score >= target (bias jamin-lulus).
    - SkipOmit grade 0: paket 4 soal MC, worker hanya jawab 2 (2 soal di-omit dari Answers, BUKAN spec kosong) → batch TIDAK reject-all (pre-flight lolos); session.Score == floor(2*10/40*100)=50; soal di-omit grade 0.
    - TextAnswerRequired reject: worker essay `EssayScore=5` tetapi `TextAnswer` kosong/whitespace → InjectBatchAsync `Rejected==true` (pre-flight D-04). Sebaliknya: essay auto-gen di-omit (tak ada spec essay) → TIDAK reject.
  </behavior>
  <action>
Buat `HcPortal.Tests/InjectPreviewEqualsCommitTests.cs`, `namespace HcPortal.Tests;`, `[Trait("Category","Integration")]` (skip via "Category!=Integration"), `IClassFixture<InjectAssessmentFixture>`. SALIN VERBATIM helper `NewGradingService`/`NewInjectService`/`SeedUserAsync`/`NewCtx` dari `InjectAssessmentServiceTests.cs:65-96` (jangan buat fake baru — semua sudah ada).

Untuk preview gunakan helper map pola→in-memory yang SAMA dengan yang dipakai `PreviewInjectScore` controller (TIRU §Code Examples RESEARCH — TempId=Id sintetis). Untuk commit panggil `InjectBatchAsync(req, actorUserId, actorName)` lalu baca `ctx.AssessmentSessions.Where(...).Select(s => s.Score)` dari DB (read-after-commit). Assert per `<behavior>`.

KRITIS — guarantee preview==commit: pola yang di-Compute untuk preview HARUS pola yang sama yang di-commit. Untuk auto-gen, panggil `BuildAutoGenAnswers` SEKALI dengan seed deterministik, pakai hasilnya untuk KEDUA preview & req commit (atau panggil 2× dengan seed identik — `ComputeAutoGenSeed` deterministik menjamin pola sama).

Jalankan `dotnet test --filter "Category=Integration&FullyQualifiedName~PreviewEqualsCommit"` (real-SQL SQLEXPRESS lokal). Bila SQLEXPRESS tak tersedia, fixture melempar XunitException MIGRATION-CHAIN (bukan bug inject) — jalankan dari dev box dengan SQLEXPRESS.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category=Integration&FullyQualifiedName~PreviewEqualsCommit" 2>&1 | Select-String -Pattern "Passed!|Failed!|error" | Select-Object -First 5</automated>
  </verify>
  <done>`InjectPreviewEqualsCommitTests.cs` ada ([Trait Integration], IClassFixture InjectAssessmentFixture); fact preview==commit (input-asli + auto-gen), skip=omit grade 0, TextAnswer-wajib reject — semua hijau di real-SQL. Skor preview Compute == AssessmentSession.Score commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (POST InjectAssessment + PreviewInjectScore) | input tak-tepercaya (AnswersJson, mode, target, essay) menyeberang ke server |
| server→DB (commit) | `InjectBatchAsync` satu-satunya jalur write (atomic per-batch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-395-05 | Elevation | PreviewInjectScore | mitigate | `[Authorize(Roles="Admin, HC")]` (mirror POST :50) — non-Admin/HC ditolak |
| T-395-06 | Tampering | CSRF preview/commit | mitigate | `[ValidateAntiForgeryToken]` di PreviewInjectScore + commit (pola :51); form #injectAssessmentForm sudah render token |
| T-395-07 | Tampering | forge skor via POST | mitigate | Server-authoritative: skor SELALU dihitung `Compute`/service dari pola, BUKAN diterima sebagai persen final dari client; preview read-only (NO SaveChanges) |
| T-395-08 | DoS | AnswersJson malformed | mitigate | `ParseAnswerVms` try/catch JsonException → fallback `new()` (bukan 500); pola ParseQuestionVms :136 |
| T-395-09 | Integrity/Repudiation | auto-gen unreachable di-commit | mitigate | Guard BLOCKING server-side (D-08.3): worker auto TargetReachable=false → JANGAN commit; AuditLog ManualInject lacak (existing :301) |
| T-395-10 | Information Disclosure | preview bocorkan cert# | accept→mitigate | PreviewInjectScore TIDAK panggil CertNumberHelper (D-09) — tak ada nomor cert di response |
| T-395-11 | Information Disclosure | IDOR targetPercent/EssayScore | mitigate | PreflightValidateAsync (existing) validasi EssayScore 0..ScoreValue; target clamp 0..100 di preview map |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- `dotnet test --filter "Category=Integration&FullyQualifiedName~PreviewEqualsCommit"` hijau (real-SQL).
- `dotnet test --filter "Category!=Integration"` hijau (no regression 393/394).
- Grep `PreviewInjectScore` + `[ValidateAntiForgeryToken]` + `[Authorize(Roles = "Admin, HC")]` di controller.
- Grep `InjectBatchAsync` dipanggil di POST `InjectAssessment` (commit aktual); `Answers = new()` di MapToRequest TIDAK ada lagi (diganti resolusi per-worker).
- Grep `CertNumberHelper` TIDAK muncul di body `PreviewInjectScore`.
</verification>

<success_criteria>
- POST commit aktual via `InjectBatchAsync` (commit pertama milestone). *(INJ-08 SC1)*
- `PreviewInjectScore` mengembalikan skor final + Lulus/Tidak + ceiling/overshoot/blocking, NO cert#, NO write. *(INJ-09 SC3, D-09)*
- preview == commit dibuktikan integration real-SQL (input-asli + auto-gen). *(INJ-08/09 SC4)*
- skip=omit grade 0 (bukan reject-all); TextAnswer-wajib reject. *(INJ-08 D-04/D-05)*
- Worker auto-gen unreachable di-BLOCK server-side (tak di-cap). *(INJ-09 D-08.3)*
</success_criteria>

<output>
After completion, create `.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-02-SUMMARY.md`
</output>
