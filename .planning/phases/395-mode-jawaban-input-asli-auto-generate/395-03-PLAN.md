---
phase: 395-mode-jawaban-input-asli-auto-generate
plan: 03
type: execute
wave: 3
depends_on: ["395-02"]
files_modified:
  - Views/Admin/InjectAssessment.cshtml
  - tests/e2e/inject-assessment-395.spec.ts
autonomous: false
requirements: [INJ-08, INJ-09]
user_setup: []
must_haves:
  truths:
    - "HC membuka Langkah 5: navigasi 1-pekerja-per-layar (Prev/Next + roster), per-pekerja pilih mode input-asli/auto-generate"
    - "Input-asli: render soal authored (MC radio, MA checkbox, Essay textarea+skor); skip=omit (tak push answer)"
    - "Auto-generate: input target → tombol Pratinjau → skor final aktual + Lulus/Tidak; overshoot/BLOCKING ditampilkan"
    - "#AnswersJson terisi saat submit di listener yang sama dengan #QuestionsJson → POST commit menghasilkan skor benar (bukan grade 0 silent)"
    - "Label tipe soal = Single Answer/Multiple Answer (LBL-02), bukan Pilihan Ganda/Pilihan Majemuk"
  artifacts:
    - path: "Views/Admin/InjectAssessment.cshtml"
      provides: "Step-5 sub-komponen IIFE + #AnswersJson hidden + preview JS + LBL-02 fix"
      contains: "AnswersJson"
    - path: "tests/e2e/inject-assessment-395.spec.ts"
      provides: "E2e: input-asli + auto-gen + Pratinjau + commit → skor di /CMP/Results; #AnswersJson terisi"
      contains: "inject-assessment-395"
  key_links:
    - from: "submit listener #injectAssessmentForm"
      to: "#AnswersJson value"
      via: "JSON.stringify(buildWorkerAnswersPayload()) di listener yang SAMA dengan #QuestionsJson"
      pattern: "AnswersJson.*JSON\\.stringify|JSON\\.stringify.*[Aa]nswers"
    - from: "tombol Pratinjau Skor"
      to: "POST /Admin/PreviewInjectScore"
      via: "fetch JSON dengan anti-forgery token; render skor + badge Lulus/Tidak"
      pattern: "PreviewInjectScore"
    - from: "render data pekerja/soal"
      to: ".textContent (XSS-safe)"
      via: "tidak innerHTML untuk data user"
      pattern: "textContent"
---

<objective>
Bangun permukaan UI Langkah 5 wizard `/Admin/InjectAssessment` (menggantikan `#step5Placeholder`): sub-komponen 1-pekerja-per-layar (Prev/Next + roster ringkas), toggle mode per-pekerja (input-asli/auto-generate), form input-asli (MC/MA/Essay), form auto-generate (input target + HYBRID essay), permukaan Pratinjau (panggil endpoint Plan 02), state BLOCKING (target>ceiling → switch input-asli), hidden `#AnswersJson` + serialize di submit, dan carry-in LBL-02. Dikunci Playwright e2e runtime + checkpoint human-verify.

Purpose: Ini permukaan yang dilihat HC + jalur serialize jawaban ke commit. Razor+JS runtime tidak ter-cover grep/build (lesson 354/392) → Playwright WAJIB. Pitfall paling berbahaya: lupa serialize `#AnswersJson` → POST answers kosong → semua worker grade 0 (silent) → e2e WAJIB cek skor pasca-commit di `/CMP/Results`, bukan sekadar redirect.

Output: Step-5 sub-komponen IIFE + `#AnswersJson` + preview JS + LBL-02 fix di `InjectAssessment.cshtml`; `inject-assessment-395.spec.ts` (e2e hijau); checkpoint human-verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-CONTEXT.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-UI-SPEC.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md
@.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-02-SUMMARY.md

<interfaces>
<!-- Endpoint preview Plan 02 (POST /Admin/PreviewInjectScore). Body JSON InjectPreviewRequest; response InjectPreviewResult. -->
```
POST /Admin/PreviewInjectScore  [Authorize Admin,HC] [ValidateAntiForgeryToken]
Body: { passPercentage, title, category, completedAt, nip, mode:"manual"|"auto", targetScore,
        questions:[{tempId,questionType,scoreValue,options:[{tempId,isCorrect}]}],
        answers:[{questionTempId, selectedOptionTempIds:[], textAnswer, essayScore}] }
Response: { percentage, isPassed, totalScore, maxScore, ceilingPercent, targetReachable, overshoot, blocked, blockingMessage }
```

<!-- Bentuk #AnswersJson yang controller ParseAnswerVms harap (List<InjectWorkerAnswersVM>). -->
```json
[ { "userId": "<checkbox value=user.Id>", "mode": "manual"|"auto", "targetScore": 80,
    "answers": [ { "questionTempId": 1, "selectedOptionTempIds": [2], "textAnswer": null, "essayScore": null } ] } ]
```

<!-- injQuestions[] client-state shape (InjectAssessment.cshtml:844-855) — sumber render Step-5. -->
```js
{ TempId, QuestionText, QuestionType:"MultipleChoice"|"MultipleAnswer"|"Essay", ScoreValue, Order,
  ElemenTeknis, Rubrik, Options:[{ TempId, OptionText, IsCorrect }] }
```

<!-- Seam + analog yang dipakai (InjectAssessment.cshtml). -->
<!-- :404 #step5Placeholder (ganti); :408/:411 btnPrev5/btnNext5 (JANGAN sentuh); :312 #QuestionsJson;
     :503-519 goToStep (toggle .step-panel .d-none, panggil populateSummary saat step 6);
     :577/:605 query #userCheckboxContainer .user-checkbox:checked; :651-668 injRenderSelectedNames (.textContent XSS-safe);
     :742-783 injRenderQuestionList (tabel .textContent); :735-738 injTypeLabel; :832-833 pesan validasi;
     :868-875 submit listener (#QuestionsJson serialize — TAMBAH #AnswersJson DI SINI). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step-5 sub-komponen IIFE + form mode + hidden #AnswersJson + serialize + LBL-02</name>
  <files>Views/Admin/InjectAssessment.cshtml</files>
  <read_first>
    - Views/Admin/InjectAssessment.cshtml (FULL — seam :399-417, goToStep :503-519, picker :650-719, authoring/injRenderQuestionList :721-790, injTypeLabel :735-738, validasi :832-833, submit :868-875, #QuestionsJson :312)
    - Views/Admin/_InjectQuestionForm.cshtml (pola opsi A/B/C/D + aria-label "Opsi A benar" :35-47)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-UI-SPEC.md (K1-K7 kontrak komponen + Copywriting + warna/spasi/tipografi)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md (§8 IIFE, §9 #AnswersJson, §11 LBL-02, Shared XSS-safe)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md (§Pattern 1/2/6, Pitfall 3 TempId dangling, Pitfall 4 serialize)
  </read_first>
  <action>
**(A) Hidden #AnswersJson** — di samping `#QuestionsJson` (:312) tambah `<input type="hidden" asp-for="AnswersJson" id="AnswersJson" />`.

**(B) Carry-in LBL-02 (K7, ~4 baris)** —
- `injTypeLabel()` (:736-737): ganti `return 'Pilihan Ganda';` → `return 'Single Answer';` dan `return 'Pilihan Majemuk';` → `return 'Multiple Answer';` (biarkan `'Essay'`).
- Pesan validasi (:832-833): `'Pilihan Ganda harus tepat 1 jawaban benar.'` → `'Single Answer harus tepat 1 jawaban benar.'`; `'Pilihan Majemuk butuh minimal 2 jawaban benar.'` → `'Multiple Answer butuh minimal 2 jawaban benar.'`.

**(C) Markup Step-5** — ganti isi `#step5Placeholder` (:404-406) dengan container sub-komponen (JANGAN sentuh btnPrev5/btnNext5 :408/:411 maupun pills). Susun per UI-SPEC K1-K6:
- Toggle default-room (K2): radio inline "Mode default room: Input asli / Auto-generate".
- Roster ringkas (K1): kartu `.card` → `.card-header.bg-light` "Ringkasan Pekerja" → `.card-body.p-0` `<table class="table table-hover table-sm mb-0 align-middle" role="status" aria-live="polite">` kolom #/NIP·Nama/Mode/Skor Pratinjau/Status; `max-height:280px;overflow-y:auto`.
- Indikator pekerja aktif: `<div class="h6 fw-bold">` "Pekerja {n} dari {total} — {NIP} · {Nama}" (`.textContent`).
- Kontrol antar-pekerja: 2 tombol `.btn.btn-outline-secondary.btn-sm` "« Pekerja Sebelumnya" / "Pekerja Berikutnya »" (`.d-flex.justify-content-between.align-items-center.mb-3`), `disabled` di ujung.
- Toggle mode per-pekerja (K2): radio inline "Input jawaban asli" / "Auto-generate dari skor target".
- Body form input-asli (K3): container yang di-render JS (lihat D).
- Body form auto-gen (K4): input `type=number min=0 max=100` target + banner HYBRID essay (`.alert-info`) + (untuk essay) input skor+teks.
- Permukaan Pratinjau (K5): tombol `.btn.btn-outline-primary` "Pratinjau Skor" (`bi-eye`) + blok hasil tersembunyi (`style="display:none"`): spinner "Menghitung skor…"; `.alert-success`/`.alert-warning` skor `fs-4 fw-bold` + badge `bg-success`"Lulus"/`bg-danger`"Tidak Lulus"; catatan overshoot `.form-text.text-muted`.
- State BLOCKING (K6): `.alert-warning` (ikon `bi-exclamation-triangle-fill`) + copy ceiling + tombol `.btn-sm.btn-outline-primary` "Beralih ke input asli".
- Empty state: "Belum ada pekerja dipilih" / "Belum ada soal. Tambahkan soal di Langkah 3 terlebih dahulu."
Gunakan SEMUA copy Bahasa Indonesia VERBATIM dari UI-SPEC Copywriting Contract.

**(D) Sub-komponen IIFE ber-state (K1, §Pattern 1)** — di dalam `WizardController` (atau IIFE bersarang) di `@section Scripts`, tambah controller Step-5 ber-state:
```js
var workerState = {};   // { [userId]: { mode:'manual'|'auto', targetScore:int, answers:{[qTempId]:{selected:[],text,score,skipped}} } }
var currentWorkerIdx = 0;
function step5SelectedWorkers() { return Array.from(document.querySelectorAll('#userCheckboxContainer .user-checkbox:checked')); }
function step5WorkerName(cb) { var l = document.querySelector('label[for="'+cb.id+'"]'); var s = l && l.querySelector('strong'); return s ? s.textContent : (l ? l.textContent.trim() : cb.value); }
```
Render form per worker dari `injQuestions[]` (`.textContent` untuk teks soal/opsi — XSS-safe, pola :763/:665). MC = radio (`aria-label="opsi A"` dst, pola _InjectQuestionForm:46); MA = checkbox; Essay = textarea + input skor `min=0 max={ScoreValue}`. Checkbox "Lewati soal ini (dihitung 0)" → skip=OMIT (saat serialize JANGAN push answer soal itu — Pattern 2; JANGAN kirim spec MC/MA kosong → reject-all).

**Rebuild on enter (Pitfall 3 TempId dangling):** hook ke `goToStep` — saat `n === 5`, panggil `step5Rebuild()` yang: bangun ulang `workerState` dari worker tercentang (prune worker unchecked), prune `answers` ber-`qTempId` yang tak ada lagi di `injQuestions[]`, default mode dari toggle default-room, render worker idx 0, render roster. `goToStep` luar hanya toggle `.d-none` (:508-512) — sub-komponen independen.

**(E) Pratinjau (K5, §Pattern 5)** — tombol "Pratinjau Skor" → `fetch('/Admin/PreviewInjectScore', { method:'POST', headers:{ 'Content-Type':'application/json', 'RequestVerificationToken': <token> }, body: JSON.stringify(buildPreviewRequest(worker)) })`. Ambil anti-forgery token dari `document.querySelector('input[name="__RequestVerificationToken"]').value` (form sudah render via tag-helper). Render `percentage`/`isPassed`/overshoot/blocking ke permukaan hasil + perbarui roster. Disable tombol saat fetch (spinner). Bila `blocked` → tampilkan K6 BLOCKING + tombol "Beralih ke input asli" (set mode worker=input-asli, pre-fill grid dari pola auto-gen terakhir — D-10). Server-otoritas: JANGAN hitung skor di JS.

**(F) Serialize #AnswersJson (Pitfall 4 — KRITIS)** — di submit-listener yang SAMA dengan `#QuestionsJson` (:871-874), TAMBAH (JANGAN buat listener kedua):
```js
document.getElementById('AnswersJson').value = JSON.stringify(buildWorkerAnswersPayload());
```
`buildWorkerAnswersPayload()` = `step5SelectedWorkers().map(cb => ({ userId: cb.value, mode: workerState[cb.value].mode, targetScore: workerState[cb.value].targetScore, answers: <answers non-skipped → {questionTempId, selectedOptionTempIds, textAnswer, essayScore}> }))`. Soal di-skip → OMIT dari answers (Pattern 2). Essay auto-gen → tetap kirim essay manual (HYBRID D-08.1). MC/MA auto-gen → JANGAN kirim (server hitung via BuildAutoGenAnswers); kirim hanya {userId, mode:'auto', targetScore, answers:<essay manual saja>}.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | Select-String -Pattern "error|Build succeeded" | Select-Object -First 5</automated>
  </verify>
  <done>`#AnswersJson` hidden ada (:~312); `injTypeLabel` mengembalikan "Single Answer"/"Multiple Answer"; pesan validasi pakai label baru; `#step5Placeholder` diganti sub-komponen 1-pekerja-per-layar (roster + Prev/Next + toggle mode + form MC/MA/Essay + Pratinjau + BLOCKING); IIFE rebuild on goToStep(5) + prune TempId dangling; serialize `#AnswersJson` di submit-listener yang SAMA dengan `#QuestionsJson`; render data user via `.textContent`; btnPrev5/btnNext5/pills TIDAK disentuh. `dotnet build` 0 error; grep "Pilihan Ganda"/"Pilihan Majemuk" = 0 match di file.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright e2e inject-assessment-395 (anti silent-grade-0)</name>
  <files>tests/e2e/inject-assessment-395.spec.ts</files>
  <read_first>
    - tests/e2e/inject-assessment-394.spec.ts:1-60 (loginAny, authorMcQuestion, fillWizardToConfirm, db helper — TIRU)
    - tests/helpers/accounts.ts + tests/helpers/dbSnapshot.ts (kredensial admin@pertamina.com / 123456; snapshot/restore DB)
    - tests/playwright.config.ts (base URL localhost:5277, --workers=1)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-PATTERNS.md (§14 e2e)
    - .planning/phases/395-mode-jawaban-input-asli-auto-generate/395-RESEARCH.md (§Validation Architecture e2e + Pitfall 6 runtime)
    - CLAUDE.md (Seed Data Workflow — snapshot DB sebelum commit test, restore sesudah)
  </read_first>
  <action>
Buat `tests/e2e/inject-assessment-395.spec.ts` (TIRU pola 394). Pre-req runtime: server localhost:5277 dari MAIN tree, `Authentication__UseActiveDirectory=false` (Razor di-embed saat build — lesson 354/392). Test e2e ini MENULIS DB (commit inject aktual) → ikuti CLAUDE.md Seed Workflow: snapshot DB lokal di `beforeAll`, restore di `afterAll` (atau hapus sesi inject yang dibuat); catat di `docs/SEED_JOURNAL.md`.

Cakupan:
1. **INJ-08 input-asli → skor benar:** login admin → /Admin/InjectAssessment → `fillWizardToConfirm`-style sampai Step-5 → mode input-asli → pilih opsi benar untuk MC → Pratinjau → assert skor tampil > 0. Submit `#btnInject` → assert redirect/flash sukses → buka `/CMP/Records` pekerja → assert baris muncul → `/CMP/Results` assert skor == skor preview (BUKAN 0). **Anti silent-grade-0:** sebelum submit, assert `document.querySelector('#AnswersJson').value` (via `page.evaluate`) NON-empty & ber-isi answers (anti Pitfall 4).
2. **INJ-09 auto-gen → preview → commit:** Step-5 mode auto-generate → set target (mis. 80) → klik "Pratinjau Skor" → assert permukaan hasil menampilkan persen >= 80 + badge Lulus/Tidak (no cert#). Submit → assert skor di `/CMP/Results` == skor preview.
3. **#AnswersJson terisi:** `page.evaluate(() => document.getElementById('AnswersJson').value)` → JSON parse → array non-kosong dengan field userId/mode/answers.
4. (opsional) **LBL-02:** assert badge tipe soal di form jawaban berisi "Single Answer"/"Multiple Answer", bukan "Pilihan Ganda".

Run: `cd tests && npx playwright test e2e/inject-assessment-395.spec.ts --workers=1`. JANGAN watch-mode.
  </action>
  <verify>
    <automated>cd tests; npx playwright test e2e/inject-assessment-395.spec.ts --workers=1 --reporter=line 2>&1 | Select-String -Pattern "passed|failed|error" | Select-Object -First 5</automated>
  </verify>
  <done>`inject-assessment-395.spec.ts` ada; e2e hijau: input-asli → skor benar di /CMP/Results (BUKAN 0), auto-gen → Pratinjau skor >= target → commit → skor match, `#AnswersJson` terisi (anti silent-grade-0). DB di-restore (SEED_JOURNAL CLEANED). Run dari main tree, AD-off, --workers=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi browser Langkah 5 + commit "seakan online"</name>
  <what-built>
    Sub-komponen Langkah 5 (1-pekerja-per-layar): toggle mode input-asli/auto-generate per pekerja, form jawaban MC/MA/Essay, tombol Pratinjau Skor (skor final aktual + Lulus/Tidak, tanpa nomor sertifikat), state BLOCKING target>ceiling, roster ringkas; `#btnInject` commit aktual (pertama di milestone); label LBL-02 "Single Answer"/"Multiple Answer".
  </what-built>
  <how-to-verify>
    1. Jalankan app dari MAIN tree: `dotnet build HcPortal.csproj` lalu jalankan dengan `Authentication__UseActiveDirectory=false` → buka http://localhost:5277, login admin@pertamina.com / 123456.
    2. Buka /Admin/InjectAssessment. Isi Setup (judul, kategori, tanggal backdate ≤ hari ini), pilih ≥2 pekerja, tulis ≥1 soal MC + ≥1 soal MA + ≥1 soal Essay (Langkah 3). Lanjut ke Langkah 5.
    3. **Navigasi pekerja:** konfirmasi "Pekerja 1 dari N — {NIP} · {Nama}", tombol Prev/Next pindah antar pekerja, roster menampilkan mode + status.
    4. **Input-asli:** pilih opsi MC (radio, 1), MA (checkbox, ≥1), isi Essay teks+skor. Coba isi skor essay tetapi kosongkan teks → klik Pratinjau/submit → konfirmasi pesan "Teks jawaban essay wajib diisi karena skornya diisi." Centang "Lewati soal ini" pada 1 soal → konfirmasi diperlakukan 0 (warn, bukan blok).
    5. **Auto-generate:** ganti mode pekerja ke auto, set target (mis. 80%), klik "Pratinjau Skor" → konfirmasi skor final aktual tampil (mis. 80% atau 83% dengan catatan overshoot) + badge Lulus/Tidak, TANPA nomor sertifikat. Buat room dengan essay berbobot besar + target tinggi → konfirmasi BLOCKING "Target X% tidak tercapai… maksimum Y%" + tombol "Beralih ke input asli".
    6. **Commit "seakan online":** klik "Inject Assessment" → konfirmasi flash sukses. Buka /CMP/Records pekerja → baris muncul (label "Assessment Online"). Buka /CMP/Results → rincian jawaban per-soal + skor == skor preview (BUKAN 0). LBL-02: tipe soal "Single Answer"/"Multiple Answer".
    7. (CLAUDE.md Seed Workflow) Bila menulis data uji ke DB lokal, snapshot sebelum + restore sesudah; catat di docs/SEED_JOURNAL.md.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila Langkah 5 + commit "seakan online" berfungsi (skor preview==commit, BLOCKING benar, LBL-02), atau jelaskan masalahnya.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server | render data user (NIP/nama/teks soal/opsi) + serialize answers + fetch preview menyeberang ke/dari server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-395-12 | Tampering | XSS via teks soal/opsi/nama di render Step-5 | mitigate | Render SEMUA data user via `.textContent` (pola :763/:665), BUKAN innerHTML — sub-komponen 395 WAJIB ikut (UI-SPEC K1/K3) |
| T-395-13 | Tampering | CSRF pada fetch Pratinjau | mitigate | Sertakan `RequestVerificationToken` dari `__RequestVerificationToken` form di header fetch (endpoint Plan 02 ber-[ValidateAntiForgeryToken]) |
| T-395-14 | Tampering/DoS | lupa serialize #AnswersJson → silent grade 0 | mitigate | Serialize di submit-listener SAMA dengan #QuestionsJson; e2e WAJIB assert #AnswersJson non-empty + skor di /CMP/Results > 0 (bukan sekadar redirect) |
| T-395-15 | Integrity | commit worker BLOCKING (target>ceiling) | mitigate | UI cegah di klien (K6) + server guard (Plan 02 T-395-09) — defense-in-depth, integritas sertifikasi |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- `cd tests && npx playwright test e2e/inject-assessment-395.spec.ts --workers=1` hijau (dari main tree, AD-off).
- Grep `AnswersJson` + `PreviewInjectScore` + `textContent` di `InjectAssessment.cshtml`.
- Grep "Pilihan Ganda"/"Pilihan Majemuk" = 0 match di `InjectAssessment.cshtml` (LBL-02 selesai).
- Checkpoint human-verify approved (preview==commit live, BLOCKING, "seakan online").
</verification>

<success_criteria>
- Langkah 5 navigasi 1-pekerja-per-layar + roster; per-pekerja pilih mode + isi jawaban (MC/MA/Essay) / set target. *(INJ-08 SC1, INJ-09 SC2)*
- Pratinjau menampilkan skor final aktual + Lulus/Tidak (+overshoot/BLOCKING), tanpa cert#. *(INJ-09 SC3)*
- `#btnInject` commit aktual → skor di /CMP/Results == preview (bukan grade 0). *(INJ-08/09 SC4/SC5)*
- LBL-02 "Single Answer"/"Multiple Answer" konsisten. *(carry-in 394)*
</success_criteria>

<output>
After completion, create `.planning/phases/395-mode-jawaban-input-asli-auto-generate/395-03-SUMMARY.md`
</output>
