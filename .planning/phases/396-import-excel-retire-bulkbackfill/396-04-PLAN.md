---
phase: 396-import-excel-retire-bulkbackfill
plan: 04
type: execute
wave: 4
depends_on: ["396-03"]
files_modified:
  - Views/Admin/InjectAssessment.cshtml
  - Controllers/InjectAssessmentController.cs
  - tests/e2e/inject-excel-396.spec.ts
files_modified_note: "View + controller shared with done-395; this plan adds the Step-5 Excel branch + binds Step5Method and sets EssayTextRequired=false on the Excel commit. Wave-sequential after Plan 03."
autonomous: false
requirements: [INJ-10]
tags: [excel, import, view, ui, toggle, preview, e2e, checkpoint]

must_haves:
  truths:
    - "HC sees a room-level radio toggle in Step-5: 'Isi via Form' (default) / 'Import Excel'; choosing Excel hides ALL the 395 form sub-components and shows the Excel panel (mutually exclusive)"
    - "The Excel panel offers Download Template, a file picker (.xlsx/.xls), and Upload & Pratinjau"
    - "After a successful upload the batch preview table shows NIP+Nama+Skor+Lulus/Tidak+Soal Terjawab (no cert#) and #AnswersJson is filled from the parse so #btnInject commits byte-identical"
    - "An upload with >=1 error shows the FULL error list in a danger panel and the preview table is not shown (atomic; nothing commits)"
    - "On the Excel path the commit sets EssayTextRequired=false so an essay with score-but-no-text is not rejected (D-05)"
  artifacts:
    - path: "Views/Admin/InjectAssessment.cshtml"
      provides: "Step-5 N1 toggle + N2 Excel panel + N3 preview table + N4 error list (Bahasa Indonesia, Bootstrap 5.3)"
      contains: "step5Method"
    - path: "Controllers/InjectAssessmentController.cs"
      provides: "Excel commit sets EssayTextRequired=false when Step5Method=excel"
      contains: "EssayTextRequired"
    - path: "tests/e2e/inject-excel-396.spec.ts"
      provides: "Playwright: toggle + download + upload-success + upload-error + commit + preview==results"
      contains: "Import Excel"
  key_links:
    - from: "Views/Admin/InjectAssessment.cshtml"
      to: "POST UploadInjectExcel"
      via: "fetch with FormData + antiforgery"
      pattern: "UploadInjectExcel"
    - from: "Views/Admin/InjectAssessment.cshtml"
      to: "#AnswersJson"
      via: "submit listener writes Excel-cache when method=excel"
      pattern: "AnswersJson"
---

<objective>
Build the Step-5 Excel branch in `InjectAssessment.cshtml` (UI-SPEC N1-N4): the room-level Form/Excel toggle, the Excel panel (Download Template + file upload + Upload & Pratinjau), the batch preview table, and the full validation-error panel — all Bahasa Indonesia, reusing the inherited 394/395 design system. Wire the client so the Excel path fills `#AnswersJson` from the upload response, and wire the controller commit to set `EssayTextRequired=false` for the Excel method. Verify end-to-end with Playwright (runtime — grep+build insufficient), then a human-verify checkpoint.

Purpose: This is the only UI surface of INJ-10. The toggle is mutually exclusive (D-03), the preview is mandatory before commit (D-08), the error list is complete and atomic (D-09), and a blank cell warns-but-allows (D-06). Commit stays the existing `#btnInject` -> `MapToRequest` -> `InjectBatchAsync` path; the Excel branch only changes what fills `#AnswersJson`.

Output: extended view, one controller commit wiring, an e2e spec, and an approved human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/396-import-excel-retire-bulkbackfill/396-CONTEXT.md
@.planning/phases/396-import-excel-retire-bulkbackfill/396-UI-SPEC.md
@.planning/phases/396-import-excel-retire-bulkbackfill/396-PATTERNS.md
@.planning/phases/396-03-SUMMARY.md

<interfaces>
<!-- View seams to REUSE (verified HEAD 2345083d). Extend in place; DO NOT refactor pills/goToStep. -->
From Views/Admin/InjectAssessment.cshtml:
- Form: `<form ... id="injectAssessmentForm">` (line 114) with `@Html.AntiForgeryToken()` (line 115).
- Hidden fields: `#QuestionsJson` (line 312), `#AnswersJson` (line 314).
- Step-5: `#step5Root` (line 406) > `#step5Body.d-none` (line 418); the "Mode default room" block + roster `.row.g-4` + per-worker form live INSIDE step5Body starting line 421.
- Submit listener (line 986-996): serializes `#QuestionsJson` = JSON(injQuestions) and `#AnswersJson` = JSON(buildWorkerAnswersPayload()) on the SAME listener. The Excel branch must conditionally fill #AnswersJson from the Excel cache instead.
- Commit button: `#btnInject` (`.btn.btn-success`, line 595) — DO NOT change variant/label.
- Client question state: `var injQuestions = [...]` (line 614); `window.injGetQuestions`.

Endpoints from Plan 03 (POST, antiforgery): DownloadInjectTemplate (returns file), UploadInjectExcel (returns InjectExcelUploadResult JSON: { ok, errors[{nip,message}], answersJson, previews[{nip,name,percentage,isPassed,answered,totalQuestions}], skippedBlankCount }).

Toggle radio analog (mirror verbatim): @Views/Admin/InjectAssessment.cshtml (lines 420-434 #step5DefaultMode)
e2e analog (DB-write, snapshot/restore, anti silent-grade-0): @tests/e2e/inject-assessment-395.spec.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step-5 Excel UI (N1 toggle + N2 panel + N3 preview + N4 errors) + client JS + commit wiring</name>
  <files>Views/Admin/InjectAssessment.cshtml, Controllers/InjectAssessmentController.cs</files>
  <read_first>
    - Views/Admin/InjectAssessment.cshtml (lines 404-600 Step-5 body + roster/form; lines 980-1000 submit listener; lines 1096-1130 step5 toggle/visibility helpers — mirror their show/hide d-none pattern)
    - .planning/phases/396-import-excel-retire-bulkbackfill/396-UI-SPEC.md (N1-N4 full markup contract + Copywriting Contract exact Bahasa Indonesia strings + Color/Typography/Spacing tokens + Interaction Contracts 1-8 + Aksesibilitas role="status"/role="alert"/.textContent)
    - .planning/phases/396-import-excel-retire-bulkbackfill/396-PATTERNS.md (§InjectAssessment.cshtml — N1 verbatim radio, serialize-listener branch, file-input markup, UI tokens, runtime Playwright requirement)
    - Controllers/InjectAssessmentController.cs (the InjectAssessment POST :53-98 — where to set EssayTextRequired for the Excel method; MapToRequest builds req)
    - ViewModels/InjectAssessmentViewModel.cs (Step5Method flag from Plan 01)
  </read_first>
  <action>
**View (Views/Admin/InjectAssessment.cshtml):**

1. N1 — toggle: inside `#step5Body` (line 418), ABOVE the "Mode default room" block (line 421), insert a `.mb-3` group mirroring the `#step5DefaultMode` markup verbatim but with `name="step5Method"`, label `.form-label.fw-bold.mb-1` "Metode pengisian jawaban", two inline radios: `id="step5MethodForm" value="form" checked` label "Isi via Form"; `id="step5MethodExcel" value="excel"` label "Import Excel". Hint `.form-text.text-muted`: "Pilih satu metode untuk seluruh room — semua via form atau semua via Excel (tak bisa campur)." Add a hidden bound input `<input type="hidden" asp-for="Step5Method" id="Step5Method" value="form" />` so the chosen method posts to the server.

2. Wrap the EXISTING 395 form sub-components — the "Mode default room" block + the `.row.g-4` roster/form region — in a container `<div id="step5FormPath">` so the toggle can hide them as a unit. (Do not alter their internals.)

3. N2 — Excel panel: after `#step5FormPath`, add `<div id="step5ExcelPanel" class="card d-none">`:
   - `.card-header.bg-light` with `<h6 class="mb-0 fw-bold"><i class="bi bi-file-earmark-excel text-primary me-2"></i>Import Jawaban via Excel</h6>`.
   - `.card-body.p-4`:
     - N2a instruksi `.alert.alert-info.small` (icon `bi-info-circle me-2`): "1. Unduh template. 2. Isi jawaban — huruf opsi (A untuk Single, A,C untuk Multiple) atau skor untuk Essay. 3. Unggah & pratinjau sebelum commit."
     - N2b Download: `<button type="button" id="btnDownloadTemplate" class="btn btn-outline-primary"><i class="bi bi-download me-1"></i>Download Template</button>` + hint "Template berisi 1 baris per pekerja terpilih + kolom per soal."
     - N2c file: `.mb-3` > `<label for="step5ExcelFile" class="form-label fw-bold">File Excel</label>` + `<input type="file" class="form-control" id="step5ExcelFile" accept=".xlsx,.xls">` + hint "Format .xlsx/.xls, maks 10 MB."
     - N2d Upload: `<button type="button" id="btnUploadExcel" class="btn btn-outline-primary" disabled><i class="bi bi-upload me-1"></i>Upload & Pratinjau</button>` (disabled until a file is chosen / during fetch). Include a spinner span for the loading state "Memproses…".
     - N3 preview container `<div id="step5ExcelPreview" style="display:none">` with heading "Pratinjau Hasil (sebelum commit)", a warn placeholder `<div id="step5ExcelBlankWarn" class="alert alert-warning py-2 small d-none"></div>`, and a `<table class="table table-sm table-hover align-middle" role="status" aria-live="polite">` inside `max-height:320px;overflow-y:auto` with thead.table-light columns "#","NIP / Nama","Skor Final","Lulus/Tidak","Soal Terjawab" and an empty `<tbody id="step5ExcelPreviewBody">`.
     - N4 errors `<div id="step5ExcelErrors" class="alert alert-danger d-none" role="alert">` with `<i class="bi bi-exclamation-triangle-fill me-2"></i><strong>Perbaiki kesalahan berikut, lalu unggah ulang:</strong>` + `<ul class="mb-0 mt-2 small" id="step5ExcelErrorList" style="max-height:240px;overflow-y:auto"></ul>`.

4. Client JS (add a new `<script>` block or extend the existing one):
   - `#step5ExcelFile` change → enable/disable `#btnUploadExcel`.
   - N1 toggle change → if value=excel: hide `#step5FormPath` (add `d-none`), show `#step5ExcelPanel` (remove `d-none`), set `#Step5Method` value to "excel"; else reverse, set "form". Do NOT clear form state (HC can switch back). Mirror the existing d-none show/hide helpers (lines ~1096-1130).
   - `#btnDownloadTemplate` click → serialize `#QuestionsJson` (JSON(injQuestions)) so the server has the questions, then submit a programmatic POST to `DownloadInjectTemplate` carrying the form fields (UserIds + QuestionsJson + antiforgery). Simplest: build a temporary hidden form OR reuse the main form's antiforgery token via a `fetch` that triggers a file download (use `fetch` then `blob` + object URL, OR a hidden `<form method=post action=DownloadInjectTemplate>` with the token + serialized inputs and call `.submit()`). Whichever — the download MUST be a real file download (Playwright asserts a download event).
   - `#btnUploadExcel` click → guard a file is selected; set loading state (disable + spinner + "Memproses…"); build `FormData` with the file (`excel`), the selected `UserIds[]`, `QuestionsJson`, `PassPercentage`, and the antiforgery token (`__RequestVerificationToken` from `@Html.AntiForgeryToken()` hidden input); `fetch('/Admin/UploadInjectExcel', { method:'POST', body: formData })`; parse JSON `InjectExcelUploadResult`:
     - if `!ok`: hide preview (`#step5ExcelPreview` display none), show `#step5ExcelErrors` (remove d-none), clear+populate `#step5ExcelErrorList` with one `<li>` per error using `.textContent` (XSS-safe) from `err.message`; clear `#AnswersJson` Excel cache (set to "[]"); do NOT enable commit path.
     - if `ok`: hide `#step5ExcelErrors` (add d-none), populate `#step5ExcelPreviewBody` rows (#, "NIP / Nama" via `.textContent`, "Skor Final" `fw-bold` + "%", badge `bg-success`"Lulus"/`bg-danger`"Tidak Lulus", "{answered} / {totalQuestions}"); show `#step5ExcelPreview`; if `skippedBlankCount>0` show `#step5ExcelBlankWarn` (remove d-none) with text "{n} jawaban kosong di Excel — soal terkait dihitung 0. Periksa pratinjau sebelum commit."; STORE `result.answersJson` in a JS variable `excelAnswersCache`.
     - always restore the button (enable + remove spinner).
     - on fetch/parse exception: show `#step5ExcelErrors` with the single message "Gagal membaca file Excel. Pastikan file .xlsx/.xls valid dan ≤ 10 MB, lalu coba lagi."
   - Submit listener branch (lines 986-996): change the `#AnswersJson` assignment so when `#Step5Method` value === "excel", set `answersHidden.value = excelAnswersCache || "[]"` instead of `JSON.stringify(buildWorkerAnswersPayload())`. `#QuestionsJson` still serializes injQuestions unconditionally.
   - All data render via `.textContent`, never `innerHTML` (carry 395, XSS-safe).

**Controller (InjectAssessment POST, lines 53-98):** after `var req = MapToRequest(vm, userIdToNip, workerAnswers);`, set the D-05 scoping:
```csharp
// Phase 396 D-05: jalur Excel → teks essay OPSIONAL (jangan tolak essay tanpa teks).
if (string.Equals(vm.Step5Method, "excel", StringComparison.OrdinalIgnoreCase))
    req.EssayTextRequired = false;
```
Leave everything else (BLOCKING guard, InjectBatchAsync call, TempData) unchanged. (Form path keeps default true.)
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "step5Method\|Step5Method" Views/Admin/InjectAssessment.cshtml` >= 2 (radio name + hidden bound input)
    - `grep -c "step5ExcelPanel\|step5ExcelPreview\|step5ExcelErrors\|btnDownloadTemplate\|btnUploadExcel\|step5ExcelFile" Views/Admin/InjectAssessment.cshtml` >= 6
    - Exact Bahasa Indonesia copy present: `grep -c "Import Jawaban via Excel\|Download Template\|Upload & Pratinjau\|Perbaiki kesalahan berikut" Views/Admin/InjectAssessment.cshtml` >= 4
    - Excel path hits the endpoints: `grep -c "UploadInjectExcel\|DownloadInjectTemplate" Views/Admin/InjectAssessment.cshtml` >= 2
    - Commit button untouched: `grep -c "id=\"btnInject\"" Views/Admin/InjectAssessment.cshtml` == 1 and `grep -c "btn-success" Views/Admin/InjectAssessment.cshtml` >= 1 (variant preserved)
    - Controller scoping present: `grep -c "EssayTextRequired = false" Controllers/InjectAssessmentController.cs` >= 1
    - `dotnet build HcPortal.csproj` exits 0; `dotnet test --filter "Category!=Integration"` GREEN (no regression)
  </acceptance_criteria>
  <done>Step-5 has the mutually-exclusive Form/Excel toggle, the Excel panel (download/upload/preview), the batch preview table, and the full error panel; #AnswersJson fills from the Excel cache when method=excel; commit sets EssayTextRequired=false for Excel; build + fast suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright e2e inject-excel-396 (toggle + download + upload-success/commit + upload-error + preview==results)</name>
  <files>tests/e2e/inject-excel-396.spec.ts</files>
  <read_first>
    - tests/e2e/inject-assessment-395.spec.ts (full — loginAdmin, authorMcQuestion, fillToStep5, helpers/accounts ber-NIP, helpers/dbSnapshot BACKUP/RESTORE, mode:'serial', anti silent-grade-0 assertion at /CMP/Results)
    - .planning/phases/396-import-excel-retire-bulkbackfill/396-UI-SPEC.md (Interaction Contracts 1-8 — the runtime behaviors to assert)
    - .planning/phases/396-03-SUMMARY.md (endpoint names + JSON shape) and 396-02-SUMMARY.md (template column layout + parser error strings to fill cells / assert)
    - CLAUDE.md (Develop Workflow: run from main tree, AD-off; Seed Workflow: snapshot DB before DB-write e2e, restore after, journal)
  </read_first>
  <action>
Create `tests/e2e/inject-excel-396.spec.ts`. Reuse the helpers from `inject-assessment-395.spec.ts` (`loginAdmin`, `authorMcQuestion`/`authorMaQuestion`/`authorEssayQuestion` if present, `fillToStep5`, `helpers/accounts`, `helpers/dbSnapshot`). `mode: 'serial'`, run with `--workers=1`, from the MAIN working tree (no Razor RuntimeCompilation — Pitfall 4). Pick workers WITH NIP (e.g. `rino.prasetyo@pertamina.com`). `beforeAll` BACKUP DB; `afterAll` RESTORE + mark SEED_JOURNAL cleaned (CLAUDE.md Seed Workflow).

Scenarios (each = a test):
1. `toggle_hides_form_shows_excel`: fill to Step-5 with >=1 worker + >=1 question; assert `#step5FormPath` visible, `#step5ExcelPanel` hidden; click radio "Import Excel"; assert `#step5FormPath` hidden, `#step5ExcelPanel` visible; toggle back → reversed.
2. `download_template_triggers_download`: in Excel mode, click "Download Template"; assert a Playwright `download` event fires and the suggested filename contains "inject_template" / ends `.xlsx`. Save the downloaded file to a temp path for scenario 3.
3. `upload_success_preview_then_commit_grades_correctly` (anti silent-grade-0): take the downloaded template, open it with a Node xlsx lib (the e2e dev dep — same approach RESEARCH suggests; if not present, build the .xlsx in-test via SheetJS/the generated template mutated), fill the worker's row with CORRECT answers (e.g. the correct option letter for the MC). Set the file on `#step5ExcelFile`, click "Upload & Pratinjau"; assert `#step5ExcelPreview` visible with a row showing a non-zero "Skor Final" and "Lulus"; assert `#step5ExcelErrors` hidden. Click `#btnInject`; expect success TempData; navigate to `/CMP/Results` for that worker/session and assert the per-question detail shows the answer as correct and the score is NOT 0 (the score equals the preview percentage).
4. `upload_invalid_shows_full_error_list_no_commit` (atomic D-09): mutate the template to introduce >=2 distinct problems (e.g. an invalid option letter "E" in one cell AND a NIP not in the picker in an extra row, or an essay score above max). Upload; assert `#step5ExcelErrors` visible (`.alert-danger`) listing MULTIPLE `<li>` items (all problems, not just the first), and `#step5ExcelPreview` hidden. Assert no new session was committed for that title (query via dbSnapshot helper or check /CMP/Records absence) — 0 write.
5. (optional, if time) `blank_cell_warn_but_allow` (D-06): leave one answer cell blank → upload succeeds, `#step5ExcelBlankWarn` shows the "dihitung 0" notice, preview shows that question not counted; commit allowed.

Keep all DB mutations inside the snapshot/restore window. If a Node xlsx lib is unavailable in tests/, generate the .xlsx by downloading the app template (scenario 2) and editing cells with whatever the repo already uses for e2e Excel (check existing specs); document the chosen approach in the SUMMARY.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/inject-excel-396.spec.ts --workers=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/inject-excel-396.spec.ts` exists; `grep -c "Import Excel\|Upload & Pratinjau\|UploadInjectExcel\|inject_template" tests/e2e/inject-excel-396.spec.ts` >= 2
    - Uses serial + snapshot/restore: `grep -c "mode: 'serial'\|dbSnapshot\|BACKUP\|RESTORE" tests/e2e/inject-excel-396.spec.ts` >= 2
    - Asserts anti silent-grade-0 (score not 0 at Results) and the multi-error atomic path: `grep -c "CMP/Results\|step5ExcelErrors\|step5ExcelPreview" tests/e2e/inject-excel-396.spec.ts` >= 2
    - `npx playwright test e2e/inject-excel-396.spec.ts --workers=1` GREEN from the MAIN tree (app built+run from main tree, AD-off) — record pass count in SUMMARY
    - SEED_JOURNAL.md updated to "cleaned" (DB restored)
  </acceptance_criteria>
  <done>e2e proves toggle mutual-exclusivity, real template download, upload→preview→commit grades correctly (not 0), and invalid upload shows the full error list with 0 write (atomic). Green from main tree, DB restored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Step-5 Import Excel path end-to-end</name>
  <files>Views/Admin/InjectAssessment.cshtml</files>
  <action>Human performs the runtime verification steps in <how-to-verify> against the live app (localhost:5277, main tree, AD-off) after the executor has built+run it and confirmed e2e green. The executor pauses here and does not advance to Plan 05 until the human types "approved".</action>
  <what-built>
    The Step-5 Import Excel path on `/Admin/InjectAssessment`: room-level Form/Excel toggle (mutually exclusive), Download Template (.xlsx 2-sheet), file upload, mandatory batch preview (skor/lulus/terjawab, no cert#), full atomic error list, and commit via the existing `#btnInject` (byte-identical to the form/online path). The app has been built and is running from the MAIN working tree (AD-off) at http://localhost:5277, and the Playwright e2e (inject-excel-396) is green.
  </what-built>
  <how-to-verify>
    Login as admin@pertamina.com at http://localhost:5277 (AD-off). Open Kelola Data → Inject Assessment Manual (Section C). Snapshot the local DB first (CLAUDE.md Seed Workflow) since this writes sessions/cert/audit.
    1. Fill Setup (Step 1), pick >=1 worker WITH NIP (e.g. rino.prasetyo) in Step 2, author >=2 questions (1 MC + 1 Essay) in Step 3, set cert mode in Step 4, go to Step 5.
    2. In Step 5, switch the method radio to "Import Excel" — confirm the form roster disappears and the Excel panel appears (and back again).
    3. Click "Download Template" — confirm a .xlsx downloads with a "Jawaban" sheet (1 row per selected worker, columns per question) + a "Legenda" sheet.
    4. Fill the worker's row with correct answers (option letter for MC, a score for the Essay, leave Essay text blank to confirm D-05), upload, click "Upload & Pratinjau" — confirm the preview table shows the worker's NIP/Nama, a non-zero Skor Final, Lulus/Tidak, and Soal Terjawab. No certificate number is shown.
    5. Introduce an error (e.g. type "E" as the MC answer, or a NIP not selected), upload again — confirm the red error panel lists ALL problems and the preview is hidden; nothing is committed.
    6. Fix the file, upload, then click "Inject Assessment" — confirm success, then open /CMP/Records (label "Assessment Online") and /CMP/Results for that worker: the per-question detail + score must match the preview (NOT 0).
    7. Confirm the old BulkBackfill is NOT yet removed (that is Plan 05) — ignore for now.
    8. RESTORE the DB snapshot and mark the journal cleaned.
  </how-to-verify>
  <resume-signal>Type "approved" to advance to Plan 05 (retire BulkBackfill), or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (HC) -> POST UploadInjectExcel / DownloadInjectTemplate | File + form fields cross via fetch/form submit; antiforgery token required. |
| Server JSON -> DOM render | Preview rows / error messages (containing user NIP/Name) rendered into the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-396-06 | Tampering | CSRF on the fetch upload / programmatic download POST | mitigate | The client sends `__RequestVerificationToken` from `@Html.AntiForgeryToken()`; both endpoints `[ValidateAntiForgeryToken]` (Plan 03). |
| T-396-09 | XSS / Information Disclosure | Rendering NIP/Name/error text from server JSON into the DOM | mitigate | All dynamic text rendered via `.textContent` (never `innerHTML`) — carry 395; locked by acceptance grep + e2e. |
| T-396-04 | Tampering | Essay text-optional (D-05) only on Excel | mitigate | Controller sets `req.EssayTextRequired = false` only when `vm.Step5Method == "excel"`; form path keeps default true. |
| T-396-07 | Tampering | No cert# leaked in preview | accept(by-design) | Preview table renders Aggregator output only; no cert number field (Plan 03 endpoint never returns one). |
| T-396-10 | Tampering | Excel cache bypassing preview gate | mitigate | The submit listener fills `#AnswersJson` from `excelAnswersCache` which is set ONLY after a successful 0-error upload; an invalid upload sets it to "[]" so commit produces nothing. Preview is the gate (D-08). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error; `dotnet test --filter "Category!=Integration"` GREEN.
- `npx playwright test e2e/inject-excel-396.spec.ts --workers=1` GREEN from the MAIN tree (AD-off) — toggle, download, upload-success+commit (score not 0), upload-error (full list, 0 write).
- Human-verify checkpoint approved.
- 0 migration.
</verification>

<success_criteria>
- Step-5 Form/Excel toggle is mutually exclusive; Excel panel offers download/upload/preview; preview table shows skor/lulus/terjawab without cert#; error panel lists all problems atomically; blank cell warns-but-allows.
- `#AnswersJson` fills from the Excel parse on commit; commit reuses `#btnInject` -> `InjectBatchAsync`; Excel essays text-optional (EssayTextRequired=false).
- e2e green from main tree; human checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/396-import-excel-retire-bulkbackfill/396-04-SUMMARY.md`. Record: the chosen download mechanism (hidden-form POST vs fetch-blob), the e2e .xlsx authoring approach, the Playwright pass count, the checkpoint approval, and 0 migration / build green.
</output>
