---
phase: 396-import-excel-retire-bulkbackfill
plan: 05
type: execute
wave: 5
depends_on: ["396-04"]
files_modified:
  - Controllers/TrainingAdminController.cs
  - Views/Admin/BulkBackfill.cshtml
  - Views/Admin/Index.cshtml
  - Views/Admin/Shared/_AssessmentGroupsTab.cshtml
  - tests/e2e/inject-excel-396.spec.ts
files_modified_note: "File-disjoint from Plans 02-04 (different controller + different views). Sequenced last so the route-404 e2e runs after the Excel path exists (single inject entry-point claim)."
autonomous: true
requirements: [INJ-11]
tags: [retire, hard-remove, bulkbackfill, cleanup, migration-gate]

must_haves:
  truths:
    - "The GET /Admin/BulkBackfill and POST /Admin/BulkBackfillAssessment actions no longer exist; the routes 404"
    - "CleanupAttemptHistory GET/POST still exist and compile (the non-contiguous block between the removed actions is preserved)"
    - "Views/Admin/BulkBackfill.cshtml is deleted"
    - "Both BulkBackfill UI entry-points are gone: the Section D card in Index.cshtml and the dropdown-item + its orphan divider in _AssessmentGroupsTab.cshtml"
    - "using ClosedXML.Excel and AdminBaseController.ManualDuplicatePredicate are KEPT; DuplicateGuardTests still compiles and passes"
    - "dotnet build is 0 error and dotnet ef migrations add _verify produces an empty Up/Down (0 model diff) confirming 0 migration"
  artifacts:
    - path: "Controllers/TrainingAdminController.cs"
      provides: "BulkBackfill GET+POST removed; CleanupAttemptHistory + ClosedXML + training-import paths intact"
      contains: "CleanupAttemptHistory"
    - path: "Views/Admin/Index.cshtml"
      provides: "Section D BulkBackfill card removed"
    - path: "Views/Admin/Shared/_AssessmentGroupsTab.cshtml"
      provides: "BulkBackfill dropdown-item + orphan divider removed"
  key_links:
    - from: "tests/e2e/inject-excel-396.spec.ts"
      to: "/Admin/BulkBackfill"
      via: "navigation expects 404"
      pattern: "BulkBackfill"
---

<objective>
Hard-remove the legacy BulkBackfill tool (INJ-11): delete the GET `BulkBackfill` + POST `BulkBackfillAssessment` actions (two NON-contiguous blocks in `TrainingAdminController.cs`, keeping `CleanupAttemptHistory` between them), delete the `BulkBackfill.cshtml` view, and remove BOTH UI entry-points (the Section D card in `Index.cshtml` and the dropdown-item + its now-orphan divider in `_AssessmentGroupsTab.cshtml`). Keep `using ClosedXML.Excel`, keep `ManualDuplicatePredicate`, keep `CleanupAttemptHistory`. Verify the build is clean, `DuplicateGuardTests` still passes, the routes 404, and `dotnet ef migrations add _verify` yields 0 diff (0 migration). Add the route-404 + cards-gone assertions to the e2e spec.

Purpose: One inject entry-point only — the Excel path (Plans 02-04) plus auto-generate (395) fully covers what BulkBackfill did (D-12), so removal is safe with zero functionality lost. The risk is surgical precision: removing the wrong line range breaks `CleanupAttemptHistory` or the training-import ClosedXML paths, or leaves a dead link/orphan divider.

Output: 4 source files edited/deleted, the e2e spec extended, a clean build, and a 0-diff migration gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/396-import-excel-retire-bulkbackfill/396-CONTEXT.md
@.planning/phases/396-import-excel-retire-bulkbackfill/396-RESEARCH.md
@.planning/phases/396-import-excel-retire-bulkbackfill/396-PATTERNS.md

<interfaces>
<!-- Exact removal ranges (re-verified live at HEAD 2345083d). -->
Controllers/TrainingAdminController.cs:
- KEEP `using ClosedXML.Excel;` (line 8) — still used at 1159/1211/1298 (training template + import).
- REMOVE block 1: comment header + GET BulkBackfill → lines 776-790 (comment 776-782, `[HttpGet] [Authorize(Roles="Admin")] public IActionResult BulkBackfill() { return View(); }` 784-790).
- KEEP lines 792-829: `CleanupAttemptHistory` GET (797-806) + `CleanupAttemptHistoryExecute` POST (808-829). DO NOT touch.
- REMOVE block 2: comment + POST BulkBackfillAssessment → lines 831-985 (comment 831, method 832-985; method ends at the closing brace on line 985; the next action EditManualAssessment starts at 987).
- `nameof(BulkBackfill)` appears only inside the removed POST (849/854/879/885/898/976/983) → disappears with it (no dangling ref).
- KEEP `ManualDuplicatePredicate` (defined AdminBaseController.cs:265; used TrainingAdmin 722/1342).

Views/Admin/BulkBackfill.cshtml — DELETE the whole file (119 lines).
Views/Admin/Index.cshtml lines 306-321 — REMOVE the entire `@if (User.IsInRole("Admin")) { <div class="col-md-4"> ...BulkBackfill card... </div> }` block (the card is the only content of the @if).
Views/Admin/Shared/_AssessmentGroupsTab.cshtml lines 317-322 — REMOVE the orphan `<li><hr class="dropdown-divider"></li>` (317) AND the `<li><a ...BulkBackfill...></a></li>` (318-322). KEEP the Bulk Export PDF item above (312-315).

DuplicateGuardTests (DOES NOT call the action — uses a HashSet replica + ManualDuplicatePredicate at :118-163): @HcPortal.Tests/DuplicateGuardTests.cs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hard-remove BulkBackfill (controller 2 blocks + view file + 2 UI links)</name>
  <files>Controllers/TrainingAdminController.cs, Views/Admin/BulkBackfill.cshtml, Views/Admin/Index.cshtml, Views/Admin/Shared/_AssessmentGroupsTab.cshtml</files>
  <read_first>
    - Controllers/TrainingAdminController.cs (lines 770-1010 — confirm the exact boundaries: GET block 776-790, KEEP CleanupAttemptHistory 792-829, POST block 831-985, next action 987; confirm `using ClosedXML.Excel` at 8 and ManualDuplicatePredicate usages at 722/1342 stay)
    - Views/Admin/BulkBackfill.cshtml (the file to delete)
    - Views/Admin/Index.cshtml (lines 300-322 — the @if Admin BulkBackfill card block 306-321)
    - Views/Admin/Shared/_AssessmentGroupsTab.cshtml (lines 308-330 — divider 317 + dropdown-item 318-322; Bulk Export PDF 312-315 stays)
    - HcPortal.Tests/DuplicateGuardTests.cs (lines 100-164 — confirm it does NOT reference the action, only ManualDuplicatePredicate)
    - .planning/phases/396-import-excel-retire-bulkbackfill/396-PATTERNS.md (§REMOVALS — the exact non-contiguous ranges + KEEP list)
  </read_first>
  <action>
Perform the surgical removals. Re-confirm line numbers before each delete (the file may have shifted slightly if any prior plan touched it — it should NOT have, but verify against the anchors below).

1. `Controllers/TrainingAdminController.cs`:
   - Delete block 1: the comment header (starting `// =========...` "Phase 338 REST-04 ... Bulk backfill assessment dari Excel") through the end of the GET action `public IActionResult BulkBackfill() { return View(); }` (anchors: starts at the comment block above `[HttpGet] [Authorize(Roles="Admin")] public IActionResult BulkBackfill()`, ends at that method's closing `}`). This is lines ~776-790.
   - DO NOT touch the next region: `// #23 D-01/D-02: one-time cleanup orphan AssessmentAttemptHistory...` through `CleanupAttemptHistoryExecute` (lines ~792-829). KEEP intact.
   - Delete block 2: the comment `// POST /Admin/BulkBackfillAssessment — REST-04 execute` through the closing `}` of `public async Task<IActionResult> BulkBackfillAssessment(...)` (anchors: starts at that comment / `[HttpPost] [ValidateAntiForgeryToken] [Authorize(Roles="Admin")] [RequestFormLimits(...)] public async Task<IActionResult> BulkBackfillAssessment`, ends at the method's closing brace immediately before `// GET /Admin/EditManualAssessment/{id}`). This is lines ~831-985.
   - Verify after: `using ClosedXML.Excel;` (line 8) STILL present; `CleanupAttemptHistory`/`CleanupAttemptHistoryExecute` STILL present; `nameof(BulkBackfill)` gone (was only inside block 2); EditManualAssessment intact.
2. Delete the whole file `Views/Admin/BulkBackfill.cshtml`.
3. `Views/Admin/Index.cshtml`: remove the entire `@if (User.IsInRole("Admin")) { <div class="col-md-4"> <a ...BulkBackfill...> ... </a> </div> }` block (lines ~306-321). The card is the only content of that `@if`, so remove the `@if`, its braces, and the inner `<div>` entirely.
4. `Views/Admin/Shared/_AssessmentGroupsTab.cshtml`: remove the orphan divider `<li><hr class="dropdown-divider"></li>` (line ~317) AND the BulkBackfill `<li>...<a ...BulkBackfill...>...Bulk Import Nilai (Excel)</a></li>` (lines ~318-322). KEEP the "Bulk Export PDF (ZIP)" `<li>` above it (lines ~312-315) and everything after (the regenerate-token block).
5. Comment-only refs (`AssessmentAdminController.cs:4105`, `AdminBaseController.cs:263`) — leave as-is (discretion; do NOT delete `ManualDuplicatePredicate`). Optionally light-touch the cosmetic "BulkBackfill" comments in DuplicateGuardTests (non-blocking; skip if risky).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Action route gone: `grep -c "public IActionResult BulkBackfill()\|public async Task<IActionResult> BulkBackfillAssessment" Controllers/TrainingAdminController.cs` == 0
    - CleanupAttemptHistory preserved: `grep -c "CleanupAttemptHistory" Controllers/TrainingAdminController.cs` >= 2
    - ClosedXML kept: `grep -c "using ClosedXML.Excel" Controllers/TrainingAdminController.cs` == 1 and `grep -c "new XLWorkbook" Controllers/TrainingAdminController.cs` >= 3 (training template/import paths intact)
    - ManualDuplicatePredicate kept: `grep -c "ManualDuplicatePredicate" Controllers/AdminBaseController.cs` >= 1
    - View deleted: `test ! -f Views/Admin/BulkBackfill.cshtml` (file does not exist)
    - UI links gone: `grep -rc "BulkBackfill" Views/Admin/Index.cshtml Views/Admin/Shared/_AssessmentGroupsTab.cshtml` totals 0 (no `Url.Action("BulkBackfill"...)` remains)
    - No dangling ref: `grep -c "nameof(BulkBackfill)" Controllers/TrainingAdminController.cs` == 0
    - `dotnet build HcPortal.csproj` exits 0
    - `dotnet test --filter "FullyQualifiedName~DuplicateGuard"` GREEN (still compiles + passes — predicate untouched)
  </acceptance_criteria>
  <done>BulkBackfill GET+POST removed, view deleted, both UI entry-points + orphan divider removed; CleanupAttemptHistory + ClosedXML + ManualDuplicatePredicate intact; build green; DuplicateGuardTests green.</done>
</task>

<task type="auto">
  <name>Task 2: e2e route-404 + cards-gone assertions, then 0-diff migration gate</name>
  <files>tests/e2e/inject-excel-396.spec.ts</files>
  <read_first>
    - tests/e2e/inject-excel-396.spec.ts (the spec from Plan 04 — add the INJ-11 removal assertions here)
    - tests/e2e/inject-assessment-395.spec.ts (loginAdmin helper, navigation patterns)
    - .planning/phases/393-backend-core-inject/393-03-PLAN.md or any prior inject phase plan (the verify-migration `_verify` gate pattern: `dotnet ef migrations add _verify` → assert empty Up/Down → `dotnet ef migrations remove`)
    - CLAUDE.md (run from main tree, AD-off, --workers=1)
  </read_first>
  <action>
**e2e (extend tests/e2e/inject-excel-396.spec.ts):** add a test `bulkbackfill_retired_404_and_cards_gone` (INJ-11, SC4):
   - Login as admin. Navigate to `/Admin/BulkBackfill`; assert the response is 404 (the route no longer maps). Use `page.goto('/Admin/BulkBackfill')` and assert `response.status() === 404` (or assert the default ASP.NET 404 page, not the BulkBackfill form — assert the form's known element is absent).
   - Navigate to the Admin landing/Index page and assert the Section D card text "Bulk Import Nilai (Excel)" is NOT present.
   - Navigate to the assessment groups tab that renders `_AssessmentGroupsTab` and assert the dropdown-item "Bulk Import Nilai (Excel)" is NOT present (and that the menu still renders the Bulk Export PDF item — no orphan/broken menu).
   Run from the MAIN tree (the view embed must reflect the deletions — rebuild first).

**Migration gate (0 migration):** run the verify-migration probe and assert no model diff:
   - `dotnet ef migrations add _verify396 --project HcPortal.csproj` (or the repo's standard ef invocation).
   - Open the generated `_verify396` migration; assert its `Up(...)` and `Down(...)` bodies are EMPTY (no `migrationBuilder.*` calls) — confirming 0 schema change.
   - `dotnet ef migrations remove --project HcPortal.csproj` to discard the probe. If `ef` rewrites cosmetic `ToTable("X")` → `ToTable("X",(string)null)` on the snapshot (known repo quirk, seen in 393/395), restore the snapshot file with `git checkout -- <snapshot>` so the tree is clean.
   - Confirm `git status` shows no leftover migration files and no snapshot diff.
  </action>
  <verify>
    <automated>cd tests && npx playwright test e2e/inject-excel-396.spec.ts --workers=1 2>&1 | tail -16</automated>
  </verify>
  <acceptance_criteria>
    - e2e asserts the removal: `grep -c "BulkBackfill\|Bulk Import Nilai\|404" tests/e2e/inject-excel-396.spec.ts` >= 2
    - `npx playwright test e2e/inject-excel-396.spec.ts --workers=1` GREEN from the main tree (includes route-404 + cards-gone)
    - Migration gate: the `_verify396` probe Up/Down were EMPTY (recorded in SUMMARY) and the probe + any snapshot rewrite were reverted — final `git status` shows NO migration file under `Migrations/` and NO snapshot diff (`git diff --stat Migrations/` empty)
    - `dotnet build HcPortal.csproj` 0 error; full `dotnet test --filter "Category!=Integration"` GREEN
  </acceptance_criteria>
  <done>e2e confirms BulkBackfill route 404 + both UI entry-points gone (no orphan menu); the 0-diff migration gate confirms 0 migration; tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /Admin/BulkBackfill (post-removal) | The removed route is now an unmapped path; ASP.NET returns 404 (no handler reachable). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-396-03 | Elevation / Tampering | Legacy Admin-only commit-direct BulkBackfill kept alive as a second entry-point | mitigate | Hard-remove the actions + view + both UI links so the only inject path is the preview-gated Admin+HC Excel/form flow. Removing the lower-assurance tool reduces attack surface (no commit-without-preview path). |
| T-396-11 | Denial of Service / Availability | Removing the wrong line range breaks CleanupAttemptHistory or training-import (build break) | mitigate | Non-contiguous removal with explicit KEEP of 792-829 + `using ClosedXML` + `ManualDuplicatePredicate`; build + DuplicateGuardTests + full fast suite gate every removal. |
| T-396-12 | Integrity | Dead link / orphan divider left behind | mitigate | Both UI entry-points removed in the same plan; the orphan `<hr>` divider is removed with the dropdown-item; e2e asserts the menu still renders cleanly (Bulk Export PDF item present, BulkBackfill item absent). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error (non-contiguous removal precise; ClosedXML + CleanupAttemptHistory + ManualDuplicatePredicate intact).
- `dotnet test --filter "Category!=Integration"` GREEN incl. `DuplicateGuard` (predicate untouched).
- `npx playwright test e2e/inject-excel-396.spec.ts --workers=1` GREEN from main tree: route 404 + both cards/dropdown-items gone.
- 0-diff `_verify396` migration probe (empty Up/Down), reverted; `git status` clean.
</verification>

<success_criteria>
- BulkBackfill GET+POST actions removed; view deleted; both UI entry-points + orphan divider gone; routes 404.
- CleanupAttemptHistory, `using ClosedXML.Excel`, training-import ClosedXML paths, and `ManualDuplicatePredicate` all preserved; build + DuplicateGuardTests green.
- Single inject entry-point remains (Section C card → wizard); no duplicate tool.
- 0 migration confirmed by the empty `_verify396` gate; tree clean.
</success_criteria>

<output>
After completion, create `.planning/phases/396-import-excel-retire-bulkbackfill/396-05-SUMMARY.md`. Record: the exact line ranges actually removed, confirmation CleanupAttemptHistory + ClosedXML + ManualDuplicatePredicate survived, the DuplicateGuardTests result, the e2e route-404 result, the empty `_verify396` Up/Down (0 migration), and the clean `git status`. Note for IT handoff: migration=FALSE.
</output>
