---
phase: 397-link-pre-post-ke-room-existing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/InjectLinkPrePostTests.cs
  - HcPortal.Tests/InjectAntiDoubleLinkTests.cs
  - HcPortal.Tests/InjectPreviewPairingTests.cs
  - HcPortal.Tests/InjectCrossGroupingTests.cs
  - HcPortal.Tests/UnlinkInjectGroupTests.cs
  - Models/InjectAssessmentDtos.cs
  - ViewModels/InjectAssessmentViewModel.cs
autonomous: true
requirements: [INJ-12]
tags: [auth, assessment, pre-post-link, tdd, brownfield]

must_haves:
  truths:
    - "Failing xUnit suite locks the per-worker bidirectional linking contract (no broadcast)"
    - "Failing xUnit suite locks Kasus A adopt + Kasus B write-to-online + atomic rollback"
    - "Failing xUnit suite locks anti-double-link reject (full list), preview==commit pairing, cross-grouping intact, unlink revert"
    - "InjectRequest/InjectPreviewResult/ViewModel carry the new link/pairing fields downstream waves implement against"
  artifacts:
    - path: "HcPortal.Tests/InjectLinkPrePostTests.cs"
      provides: "RED tests for per-worker linking + Kasus A/B + atomic rollback"
      contains: "InjectAssessmentFixture"
    - path: "HcPortal.Tests/InjectCrossGroupingTests.cs"
      provides: "RED test cross inject↔online grouping intact (spec §13, KRITIS)"
      contains: "LinkedGroupId"
    - path: "Models/InjectAssessmentDtos.cs"
      provides: "InjectRequest link-resolution fields + InjectPreviewResult pairing fields"
      contains: "LinkTargetRepId"
    - path: "ViewModels/InjectAssessmentViewModel.cs"
      provides: "hidden link field LinkedTargetRepId"
      contains: "LinkedTargetRepId"
  key_links:
    - from: "HcPortal.Tests/*.cs"
      to: "HcPortal.Helpers / Services / Models (contracts)"
      via: "compile-time reference to new symbols"
      pattern: "InjectBatchAsync|UnlinkInjectGroupAsync|LinkTargetRepId"
---

<objective>
Wave 0 (TDD lock) for Phase 397 link Pre/Post. Write the FAILING xUnit suite that locks the
INJ-12 contract, and add the DTO/ViewModel fields (interface-first) that Waves 1-3 implement
against. No production logic — tests describe expected behavior, DTO/VM declare the contract shape.

Purpose: prevent "looks done from grep" (lesson 354/392) and prevent broadcast regression
(Pitfall 1) by encoding the per-worker bidirectional invariant + cross-grouping invariant as
executable red tests BEFORE service/controller/view are written.
Output: 5 new integration test files (RED — real-SQL fixture reused from 393) + extended
InjectAssessmentDtos.cs + InjectAssessmentViewModel.cs (build GREEN, fast suite GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/397-link-pre-post-ke-room-existing/397-CONTEXT.md
@.planning/phases/397-link-pre-post-ke-room-existing/397-RESEARCH.md
@.planning/phases/397-link-pre-post-ke-room-existing/397-PATTERNS.md
@.planning/phases/397-link-pre-post-ke-room-existing/397-VALIDATION.md

<scope_lock>
⚠️ SEQUENTIAL AFTER 396: 397 extends the SAME files as 395 (committed) and 396 (in progress —
only Plans 396-01/02/03 committed at HEAD cdcbf0f3; 396-04 view-toggle + 396-05 retire-BulkBackfill
NOT yet committed). Execution of Phase 397 MUST NOT start until all of 396's commits land.
This plan (01) only creates NEW test files + extends DTO/VM (no overlap with 396's pending view
edits), but the RED tests reference service/controller symbols that Waves 1-3 build — so the
ordering still holds at the phase level.

Before touching anything: re-verify the file:line anchors below against current HEAD. RESEARCH.md
and PATTERNS.md cite lines verified at the pre-395/396-commit HEAD; at planning time (HEAD cdcbf0f3,
396-03 committed) they STILL match — but 396-04/05 may shift Views/Admin/InjectAssessment.cshtml
and InjectAssessmentController.cs lines. Re-grep before editing.
</scope_lock>

<interfaces>
<!-- Current DTO/VM shape (extend, do not rewrite). Read the files directly before editing. -->

Models/InjectAssessmentDtos.cs (current — InjectRequest already has link columns at :63-64):
```csharp
public class InjectRequest {
    public string AssessmentType { get; set; } = "Standard";   // :50  ("PreTest"/"PostTest"/"Standard")
    public int? LinkedGroupId { get; set; }                    // :63  (already exists)
    public int? LinkedSessionId { get; set; }                  // :64  (already exists — broadcast value)
    public List<InjectWorkerSpec> Workers { get; set; } = new();
    // ...
}
public class InjectPreviewResult {                             // :109-120 (already exists, score-only)
    public int Percentage { get; set; }
    public bool IsPassed { get; set; }
    // ...
}
public class InjectRowError { public string Nip; public string Message; }   // :70-74 (Bahasa Indonesia)
```

ViewModels/InjectAssessmentViewModel.cs (current — scalar setup fields :13-42):
```csharp
public string AssessmentType { get; set; } = "Standard";   // :16
public string? Step5Method { get; set; }                   // :42 (precedent: VM-only field, not in DTO)
```

Models/AssessmentSession.cs (read-only ref — link columns confirmed):
```csharp
public bool IsManualEntry { get; set; } = false;   // :137
public string? AssessmentType { get; set; }        // :161
public int? LinkedGroupId { get; set; }            // :172
public int? LinkedSessionId { get; set; }          // :178
```

Models/AssessmentConstants.cs:5-11 — AssessmentType.PreTest="PreTest" / PostTest="PostTest" (EXACT — never "Pre"/"Post").
</interfaces>

<fixture_reference>
HcPortal.Tests/InjectAssessmentServiceTests.cs:1-60 — `InjectAssessmentFixture` (disposable
real-SQL `HcPortalDB_Test_{guid}`, MigrateAsync, EnsureDeletedAsync) + `[Trait("Category","Integration")]`.
REUSE this fixture pattern (IClassFixture). DB lokal `HcPortalDB_Dev` MUST NOT be touched.
⚠️ EF Core 8 ExecuteUpdate/real grading needs REAL SQLEXPRESS — NOT InMemory provider.
</fixture_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DTO + ViewModel with link-resolution + pairing contract fields</name>
  <files>Models/InjectAssessmentDtos.cs, ViewModels/InjectAssessmentViewModel.cs</files>
  <read_first>
    - Models/InjectAssessmentDtos.cs (full — extend InjectRequest + InjectPreviewResult; do NOT remove existing fields)
    - ViewModels/InjectAssessmentViewModel.cs (full — add hidden link field next to Step5Method :42)
    - .planning/phases/397-link-pre-post-ke-room-existing/397-RESEARCH.md §"Recommended Project Structure" + §Code Examples (pairing summary fields)
    - .planning/phases/397-link-pre-post-ke-room-existing/397-PATTERNS.md Surface 5 + Surface 7 + Surface 10
  </read_first>
  <action>
Extend the contract (interface-first) WITHOUT touching existing fields. The Waves 1-3 build against these.

(a) Models/InjectAssessmentDtos.cs — add to `InjectRequest` (keep existing `LinkedGroupId :63` / `LinkedSessionId :64`):
```csharp
// 397 link resolution — server re-resolves the actual LinkedGroupId/LinkedSessionId from these
// (Tampering guard: never trust client LinkedGroupId raw — Surface 7). null = no link (standalone, D-04).
public int? LinkTargetRepId { get; set; }   // RepresentativeId of the target room chosen in picker
```
Do NOT add a client `LinkCaseB` flag to the DTO — server re-derives Kasus A/B from the target room's
LinkedGroupId (Tampering guard). (RESEARCH A1/A3: server-authoritative.)

Add a NEW result type for the pairing dry-run (D-07) — separate from score-only InjectPreviewResult
so 395/396 preview==commit stays untouched:
```csharp
/// <summary>397 D-07 — pairing summary dari dry-run preview (NO write). Dilengkapi controller PreviewInjectScore extend.</summary>
public class InjectPairingPreview
{
    public bool HasLink { get; set; }                 // false → tak ada room tertaut (skip ringkasan, D-04)
    public int Paired { get; set; }                   // pekerja punya sibling tipe-lawan di grup target (LinkedSessionId terisi)
    public int Unpaired { get; set; }                 // D-03 sisi tunggal
    public bool WillTouchOnline { get; set; }         // Kasus B (target.LinkedGroupId == null) → banner D-07
    public bool DateWarn { get; set; }                // D-11 Pre.CompletedAt > Post.CompletedAt
    public List<InjectRowError> DoubleLinkErrors { get; set; } = new();   // D-08 daftar LENGKAP (Nip+Message Bahasa Indonesia)
}
```

(b) ViewModels/InjectAssessmentViewModel.cs — add next to `Step5Method :42` (VM-only layer, not in DTO/service — follow Step5Method precedent):
```csharp
/// <summary>Phase 397 — RepresentativeId room target dari chip picker (D-05). null = belum tertaut (skip, D-04).
/// Server re-resolve LinkedGroupId/LinkedSessionId otoritatif (Surface 7) — JANGAN trust nilai mentah.</summary>
public int? LinkedTargetRepId { get; set; }
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` reports "Build succeeded" with 0 errors
    - Grep `LinkTargetRepId` in Models/InjectAssessmentDtos.cs returns ≥1 match
    - Grep `InjectPairingPreview` in Models/InjectAssessmentDtos.cs returns ≥1 match (class + DoubleLinkErrors field)
    - Grep `LinkedTargetRepId` in ViewModels/InjectAssessmentViewModel.cs returns ≥1 match
    - Existing fields `LinkedGroupId`/`LinkedSessionId` still present in InjectRequest (no removal)
    - No `LinkCaseB` field added to InjectRequest (server re-derives — Tampering guard)
  </acceptance_criteria>
  <done>InjectRequest carries LinkTargetRepId; InjectPairingPreview exists with the 6 D-07/D-08/D-11 fields; ViewModel carries LinkedTargetRepId. Build green, fast suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — linking + Kasus A/B + atomic rollback + anti-double + unlink revert tests</name>
  <files>HcPortal.Tests/InjectLinkPrePostTests.cs, HcPortal.Tests/InjectAntiDoubleLinkTests.cs, HcPortal.Tests/UnlinkInjectGroupTests.cs</files>
  <read_first>
    - HcPortal.Tests/InjectAssessmentServiceTests.cs:1-120 (fixture + seed helpers + assertion style to mirror)
    - HcPortal.Tests/InjectPreviewEqualsCommitTests.cs (preview==commit assertion precedent, if integration)
    - Services/InjectAssessmentService.cs:42-130, :280-334, :336-442 (current InjectBatchAsync signature, audit, PreflightValidateAsync — to know what symbols to assert against)
    - .planning/phases/397-link-pre-post-ke-room-existing/397-RESEARCH.md §"Phase Requirements → Test Map" + §"Wave 0 Gaps"
    - .planning/phases/397-link-pre-post-ke-room-existing/397-PATTERNS.md Surface 2, 3, 4, 8, 11
  </read_first>
  <behavior>
    InjectLinkPrePostTests (per-worker bidirectional + Kasus A/B + atomic rollback):
    - Test PerWorker_NoBroadcast: inject 2 workers (Pre) linked to a group that has 2 Post siblings (1 per UserId).
      Each inject session's LinkedSessionId == its OWN UserId's Post sibling.Id (NOT identical across workers — anti-broadcast).
    - Test KasusA_Adopt_OnlineUntouched: target room already grouped (LinkedGroupId != null). Inject ADOPTs target.LinkedGroupId.
      Online sibling Score/IsPassed/Status/responses UNCHANGED before vs after (assert exact equality).
    - Test KasusB_WriteSticker_AllTargetSessions: target standalone (LinkedGroupId == null). After inject:
      resolvedGroupId == target.RepresentativeId; EVERY session of the target room gets that LinkedGroupId (not just paired ones, Pitfall 2);
      online Score/Status UNCHANGED; audit "LinkPrePost" entry exists per mutated online session (D-09).
    - Test AtomicRollback: force an error mid-batch (e.g. invalid NIP among valid ones OR seeded duplicate-key) →
      NO inject session persisted AND NO online LinkedGroupId/LinkedSessionId mutation persisted (read-after-rollback all null/original).

    InjectAntiDoubleLinkTests (D-08, full list):
    - Test SameTypeSibling_RejectFullList: worker X already has a SAME-type (e.g. Pre) session in the target group.
      Inject Pre for X (+ other clean workers) → result.Rejected==true (or X in PerRowErrors) AND PerRowErrors lists
      ALL offending workers (not early-return on first). Message Bahasa Indonesia contains the NIP.

    UnlinkInjectGroupTests (D-12):
    - Test Unlink_RevertBidirectional: after a committed Kasus-A link, unlink the inject group →
      inject sessions LinkedGroupId==null & LinkedSessionId==null; online sibling.LinkedSessionId reverted to null;
      online Score/Status UNCHANGED; audit "LinkPrePostUndo" entry per mutated session.
    - Test Unlink_KasusB_RevertSticker_WhenOneSided: after a Kasus-B link (sticker written to online),
      unlink → if group becomes single-type, online LinkedGroupId reverted to null (sticker removed); Score/Status UNCHANGED.
    - Test Unlink_Atomic: unlink rollback path leaves all link columns at their pre-unlink state.
  </behavior>
  <action>
Write THREE integration test files, all `[Trait("Category","Integration")]`, all reusing the
`InjectAssessmentFixture` pattern (IClassFixture, real SQLEXPRESS disposable DB). These are RED:
they reference the future contract symbols that Waves 1-2 implement.

Key contract symbols these tests pin (Wave 1-2 must provide them):
- `InjectAssessmentService.InjectBatchAsync(req, actorUserId, actorName)` STILL the entry point — but
  it now reads `req.LinkTargetRepId` and resolves per-worker `LinkedSessionId` by-UserId + Kasus A/B.
  (Do NOT change the InjectBatchAsync signature — link resolution is driven by req.LinkTargetRepId.)
- A NEW service method `Task<InjectResult> UnlinkInjectGroupAsync(int injectGroupId, string actorUserId, string actorName)`
  (Wave 1 creates it). Tests call it directly.

Seeding helpers (mirror InjectAssessmentServiceTests seed style):
- Seed an ONLINE Pre/Post group: 2 PreTest + 2 PostTest sessions (Status="Completed", Score set,
  AssessmentType exact "PreTest"/"PostTest", LinkedGroupId = the Pre rep Id, LinkedSessionId cross-linked) — for Kasus A.
- Seed a STANDALONE online room: N PostTest sessions, LinkedGroupId=null, AssessmentType="PostTest" — for Kasus B.
- Seed users with NIP (real AspNetUsers rows — reuse the fixture's user-seed helper).

Assertions:
- Anti-broadcast: `Assert.NotEqual(injectSessions[0].LinkedSessionId, injectSessions[1].LinkedSessionId)` AND
  each == its own UserId's sibling.Id.
- Online untouched: snapshot Score/IsPassed/Status/PackageUserResponses count BEFORE; assert equal AFTER (Kasus A & B).
- Kasus B sticker on ALL: query all target-room sessions; assert every LinkedGroupId == resolvedGroupId.
- Audit: `_context.AuditLogs.Count(a => a.ActionType == "LinkPrePost")` == number of mutated online sessions (Kasus B).
- Rollback: after forced error, assert 0 new inject sessions AND online link columns reverted.
- Anti-double full list: assert PerRowErrors.Count == number of offending workers (≥2 when 2 seeded).
- Unlink: assert revert + audit "LinkPrePostUndo".

RED expectation: assembly compiles only AFTER Wave 1 adds `UnlinkInjectGroupAsync` + link-resolution.
At Wave 0 the test ASSEMBLY will NOT compile (UnlinkInjectGroupAsync missing) — that is the correct RED
(symbol missing, NOT syntax error). Document this in a file-header comment: "RED until Wave 1 (397-02)
adds UnlinkInjectGroupAsync + per-worker link resolution; fast suite restored to green is the Wave 1 gate."
Verify RED is clean: all compile errors are in these 3 test files and reference the missing symbols only
(no syntax errors, no errors outside HcPortal.Tests).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | grep -E "error CS" | grep -iv "UnlinkInjectGroupAsync\|InjectLinkPrePostTests\|InjectAntiDoubleLinkTests\|UnlinkInjectGroupTests" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - The verify grep returns EMPTY (every compile error is inside the 3 new test files and references the missing UnlinkInjectGroupAsync symbol — a CLEAN RED, not stray syntax errors)
    - HcPortal.Tests/InjectLinkPrePostTests.cs contains `[Trait("Category", "Integration")]` and `InjectAssessmentFixture`
    - InjectLinkPrePostTests.cs asserts `NotEqual` on two workers' LinkedSessionId (anti-broadcast)
    - InjectLinkPrePostTests.cs references `"LinkPrePost"` audit ActionType (Kasus B)
    - InjectAntiDoubleLinkTests.cs asserts PerRowErrors lists ALL offending workers (no early-return)
    - UnlinkInjectGroupTests.cs references `UnlinkInjectGroupAsync` and `"LinkPrePostUndo"`
  </acceptance_criteria>
  <done>3 RED integration test files exist, locking per-worker linking / Kasus A/B / atomic rollback / anti-double full-list / unlink revert. Clean RED (missing symbol, not syntax). HcPortalDB_Dev untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — preview==commit pairing + cross-grouping intact (KRITIS spec §13)</name>
  <files>HcPortal.Tests/InjectPreviewPairingTests.cs, HcPortal.Tests/InjectCrossGroupingTests.cs</files>
  <read_first>
    - HcPortal.Tests/InjectPreviewEqualsCommitTests.cs (preview==commit assertion precedent)
    - Controllers/CMPController.cs:3412-3450 (GetGainScoreData — pairs by LinkedGroupId + UserId, Status=="Completed"; the invariant to assert)
    - Controllers/CMPController.cs:250-344 (Results pairing by LinkedGroupId)
    - Controllers/InjectAssessmentController.cs:100-160 (PreviewInjectScore — current dry-run shape to extend)
    - .planning/phases/397-link-pre-post-ke-room-existing/397-RESEARCH.md §Code Examples "Pairing summary di preview" + §"Wave 0 Gaps"
    - .planning/phases/397-link-pre-post-ke-room-existing/397-PATTERNS.md Surface 5 + §"Display pairing invariant"
  </read_first>
  <behavior>
    InjectPreviewPairingTests (D-07, dry-run NO write):
    - Test PairingPreview_MatchesCommit: given a target group with K sibling UserIds and an inject set of M workers
      (subset overlap = P), the preview pairing summary reports Paired==P, Unpaired==M-P, with NO DB write
      (assert no new sessions, no link mutations after preview). After actual commit, the REAL paired count
      (sessions with LinkedSessionId != null) == P (preview == commit outcome).
    - Test PairingPreview_KasusB_WillTouchOnline: target standalone → preview.WillTouchOnline == true; Kasus A → false.
    - Test PairingPreview_DateWarn: Pre.CompletedAt > Post sibling.CompletedAt (both non-null) → DateWarn==true;
      sibling CompletedAt null → DateWarn==false (D-11 / Open Q 2 skip-when-null).
    - Test PairingPreview_DoubleLink: worker with same-type sibling in target → appears in DoubleLinkErrors (full list).

    InjectCrossGroupingTests (spec §13, KRITIS — silang inject↔online):
    - Test CrossLink_GainScore_Intact: seed an ONLINE Post group (standalone or grouped); inject Pre linked to it;
      after commit, CMPController.GetGainScoreData(resolvedGroupId) returns a per-worker gain row for each worker
      who has BOTH inject Pre (Completed, Score set) AND online Post (Completed, Score set) — proving the pair
      surfaces by LinkedGroupId+UserId even though one side is inject and one is online.
    - Test CrossLink_Both_Inject: inject Post group first (committed), then inject Pre linked to it (inject↔inject);
      pairing intact; NO online session touched (assert 0 "LinkPrePost" audit when both sides inject — D-10).
  </behavior>
  <action>
Write TWO integration test files (`[Trait("Category","Integration")]`, fixture-based real-SQL).

InjectPreviewPairingTests pins the controller pairing contract (Wave 2 implements):
- A controller/service entry that produces `InjectPairingPreview` for a given target + worker set,
  dry-run, NO write. The test may call the service-level helper that Wave 1/2 exposes (decide the
  seam: recommended = a service method `Task<InjectPairingPreview> PreviewPairingAsync(int? linkTargetRepId,
  string injectAssessmentType, IReadOnlyList<string> injectUserIds, DateTime injectCompletedAt)` that the
  controller's PreviewInjectScore extension calls). Pin THIS method signature in the RED test.
- Assert Paired/Unpaired/WillTouchOnline/DateWarn/DoubleLinkErrors per the behavior block.
- Assert NO DB mutation after preview (count sessions + AuditLogs before == after).

InjectCrossGroupingTests pins the load-bearing §13 invariant via the REAL display path:
- Call `CMPController.GetGainScoreData` (or query the same way it does: LinkedGroupId+UserId, Status=="Completed",
  Score.HasValue) and assert the cross inject↔online pair appears. This is the KRITIS test — it proves the
  whole feature's value is real, not just columns written.
- For inject↔inject, assert `_context.AuditLogs.Count(a => a.ActionType=="LinkPrePost") == 0` (no online touched, D-10).

RED expectation: these reference `PreviewPairingAsync` (Wave 1) → clean RED (missing symbol). Header comment:
"RED until Wave 1 (397-02) adds PreviewPairingAsync + Wave 2 wires PreviewInjectScore. CrossGrouping is the
KRITIS spec §13 test — green only when LinkedGroupId is written correctly per D-01."
  </action>
  <verify>
    <!-- Two-part RED check. (1) Confirm the EXPECTED missing-symbol errors are actually present (no false-green
         if the build trivially succeeds or fails elsewhere). (2) Confirm NO compile error exists other than the
         genuinely-missing symbols PreviewPairingAsync / UnlinkInjectGroupAsync (a stray syntax error anywhere
         else must surface — exclusion is narrowed to ONLY those two symbols + the two test files this task adds). -->
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && BUILD=$(dotnet build HcPortal.Tests/HcPortal.Tests.csproj 2>&1); echo "$BUILD" | grep -qE "PreviewPairingAsync|UnlinkInjectGroupAsync" && echo "RED-OK: expected missing-symbol errors present" || echo "FAIL: expected RED errors (PreviewPairingAsync/UnlinkInjectGroupAsync) NOT found"; echo "$BUILD" | grep -E "error CS" | grep -v "PreviewPairingAsync" | grep -v "UnlinkInjectGroupAsync" | grep -v "InjectPreviewPairingTests.cs" | grep -v "InjectCrossGroupingTests.cs" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - The first verify line prints `RED-OK: expected missing-symbol errors present` (the expected RED — `PreviewPairingAsync`/`UnlinkInjectGroupAsync` errors ARE present, not a false-green where the build passed or failed elsewhere)
    - The second verify line (narrowed exclusion = ONLY PreviewPairingAsync/UnlinkInjectGroupAsync + InjectPreviewPairingTests.cs/InjectCrossGroupingTests.cs) returns EMPTY — i.e. NO compile error outside the two genuinely-missing symbols and the two test files this task adds; a stray syntax error anywhere else WILL surface here
    - InjectPreviewPairingTests.cs references `PreviewPairingAsync` and `InjectPairingPreview`
    - InjectPreviewPairingTests.cs asserts session+AuditLog count before == after preview (NO write)
    - InjectCrossGroupingTests.cs calls/queries `GetGainScoreData`-equivalent (LinkedGroupId+UserId pairing) and asserts a cross inject↔online pair surfaces
    - InjectCrossGroupingTests.cs asserts `ActionType=="LinkPrePost"` count == 0 for the inject↔inject case (D-10)
  </acceptance_criteria>
  <done>2 RED integration test files exist locking preview==commit pairing (D-07) + cross inject↔online grouping intact (spec §13 KRITIS) + inject↔inject does not touch online (D-10). Clean RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real SQLEXPRESS | Tests write to a disposable DB (HcPortalDB_Test_{guid}); HcPortalDB_Dev MUST NOT be touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-397-01 | Tampering | DTO InjectRequest.LinkTargetRepId | mitigate | Field is a hint only; tests assert server re-resolves LinkedGroupId (no LinkCaseB client field). Locked in Task 1 contract. |
| T-397-02 | Information Disclosure | test DB isolation | mitigate | Reuse InjectAssessmentFixture (disposable {guid} DB + EnsureDeletedAsync); no Dev DB connection string in new tests. |
| T-397-03 | Tampering | online Score/Status during link | mitigate | RED tests assert online Score/IsPassed/Status/responses UNCHANGED (Kasus A & B) — encodes the "never touch online data" invariant before any code is written. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` succeeds (DTO/VM compile).
- `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` fails ONLY with missing-symbol errors inside the 5 new 397 test files (clean RED).
- No new test file connects to HcPortalDB_Dev (grep new files for "HcPortalDB_Dev" → 0).
- All 5 new test files carry `[Trait("Category", "Integration")]`.
</verification>

<success_criteria>
- InjectRequest carries LinkTargetRepId; InjectPairingPreview + 6 D-07/D-08/D-11 fields exist; ViewModel carries LinkedTargetRepId.
- 5 RED integration test files lock: per-worker bidirectional (anti-broadcast), Kasus A adopt + online untouched, Kasus B write-to-all + audit, atomic rollback, anti-double full-list, preview==commit pairing, cross inject↔online grouping intact (KRITIS), inject↔inject no online touch, unlink revert.
- RED is clean (missing-symbol, not syntax); restoring fast suite to green is the Wave 1 gate.
</success_criteria>

<output>
After completion, create `.planning/phases/397-link-pre-post-ke-room-existing/397-01-SUMMARY.md`
documenting: contract fields added, the exact symbols the RED tests pin (UnlinkInjectGroupAsync,
PreviewPairingAsync, per-worker link resolution via req.LinkTargetRepId), and the Wave 1 gate
(fast suite + integration green).
</output>
