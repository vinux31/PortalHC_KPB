---
phase: 397-link-pre-post-ke-room-existing
plan: 04
type: execute
wave: 4
depends_on: ["397-03"]
files_modified:
  - Views/Admin/InjectAssessment.cshtml
  - tests/e2e/inject-assessment-397.spec.ts
autonomous: false
requirements: [INJ-12]
tags: [auth, assessment, pre-post-link, view, ui, e2e]

must_haves:
  truths:
    - "When AssessmentType is Pre/Post, the 'Cari Room Pasangan' button appears and opens the room picker modal"
    - "Modal lists opposite-type rooms (inject + online) with Kasus A/B badges; clicking a row sets a removable chip + hidden field"
    - "Pratinjau shows the pairing summary (paired/unpaired) + Kasus B 'data online akan disentuh' banner + date warn"
    - "Unlink control uses a Bootstrap confirm modal (not native confirm) and calls UnlinkInjectGroup"
    - "Cross inject↔online grouping is intact at runtime (Records/gain-score) — verified by Playwright + DB"
  artifacts:
    - path: "Views/Admin/InjectAssessment.cshtml"
      provides: "Cari Room button + chip + #roomPickerModal + #previewPairingSummary + unlink confirm modal"
      contains: "roomPickerModal"
    - path: "tests/e2e/inject-assessment-397.spec.ts"
      provides: "Playwright runtime verification of modal/picker/chip/preview/unlink + cross-grouping"
      contains: "roomPickerModal"
  key_links:
    - from: "Views/Admin/InjectAssessment.cshtml (#btnCariRoom)"
      to: "/Admin/SearchLinkTargets"
      via: "fetch (debounced) on search input"
      pattern: "SearchLinkTargets"
    - from: "Views/Admin/InjectAssessment.cshtml (chip)"
      to: "#LinkedTargetRepId hidden field → MapToRequest"
      via: "chip JS sets hidden input value"
      pattern: "LinkedTargetRepId"
    - from: "Views/Admin/InjectAssessment.cshtml (Pratinjau)"
      to: "/Admin/PreviewPairing"
      via: "fetch when linked"
      pattern: "PreviewPairing"
---

<objective>
Wave 3 (view + e2e). Build the runtime UI inside `Views/Admin/InjectAssessment.cshtml`: the
"Cari Room Pasangan" trigger + removable chip in Step-1 (replacing the placeholder note at :162),
the Bootstrap room-picker modal (opposite-type list, Kasus A/B badges, debounced search), the
pairing summary in Pratinjau (D-07), the anti-double-link entries in the error list (D-08), and the
unlink confirm modal (D-12). Then write the Playwright e2e that proves all of it renders/behaves at
runtime and that cross inject↔online grouping is intact. Finish with the 0-migration gate and a
human-verify checkpoint.

Purpose: the modal/picker/chip/preview/unlink are Razor + JS — grep+build is insufficient
(lesson 354/392). Playwright from the MAIN tree (AD-off, --workers=1) is mandatory. This is the
plan that makes INJ-12 visible and confirms the §13 cross-grouping risk is closed.
Output: extended InjectAssessment.cshtml; new inject-assessment-397.spec.ts; 0-migration verified;
human-verify approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/397-link-pre-post-ke-room-existing/397-CONTEXT.md
@.planning/phases/397-link-pre-post-ke-room-existing/397-PATTERNS.md
@.planning/phases/397-link-pre-post-ke-room-existing/397-UI-SPEC.md

<scope_lock>
SEQUENTIAL AFTER 396 and AFTER 397-03. This plan owns `Views/Admin/InjectAssessment.cshtml`
exclusively within 397. ⚠️ 396-04 ALSO edits this view (Step-5 Form/Excel toggle) and MUST be
committed first. RE-VERIFY the cited line numbers with grep before editing — 396-04 will shift them.
The link UI (Step-1) is orthogonal to the Step-5 method toggle (396), but they share the file, so
ordering is enforced. Run the app + Playwright from the MAIN working tree (not the sibling
PortalHC_KPB-ITHandoff), AD-off (`Authentication__UseActiveDirectory=false`), `--workers=1`
(view embedded at build — no Razor RuntimeCompilation; lesson 354/392).
</scope_lock>

<interfaces>
<!-- Current InjectAssessment.cshtml anchors (verified HEAD cdcbf0f3). Read the file directly; 396-04 shifts these. -->

- :114  <form ... id="injectAssessmentForm" novalidate>
- :118  <div id="step-1" class="step-panel">
- :143-149  Title input-group + #btnCheckTitle (analog for the "Cari Room" button input-group)
- :154-163  Tipe Assessment block: :157 <select id="assessmentTypeInput"> (Standard/PreTest/PostTest);
            :162 placeholder note "Penautan Pre/Post ke room existing tersedia pada fase berikutnya." → REPLACE
- :718  setText('sum-type', selText('assessmentTypeInput'));  (summary render — step 6)
- :1029-1073  #btnCheckTitle click handler (fetch pattern to mirror for picker fetch)

Modal pattern source (read-only ref) — CreateAssessment.cshtml:757-834 (#successModal):
.modal.fade → .modal-dialog.modal-dialog-centered.modal-lg → .modal-content.border-0.shadow → header → .modal-body.p-4.

Controller endpoints to call (from 397-03):
- GET  /Admin/SearchLinkTargets?term=&injectType=PreTest|PostTest  → JSON rows
       {RepresentativeId, Title, Category, Schedule, CompletedAt, AssessmentType, LinkedGroupId(null=Kasus B), UserCount, IsPrePostGroup, IsManualEntry}
- POST /Admin/PreviewPairing (anti-forgery) body {LinkTargetRepId, AssessmentType, UserIds[], CompletedAt} → InjectPairingPreview
       {HasLink, Paired, Unpaired, WillTouchOnline, DateWarn, DoubleLinkErrors[]}
- POST /Admin/UnlinkInjectGroup (anti-forgery) {injectGroupId} → {ok, message}

ViewModel hidden field (from 397-01): `LinkedTargetRepId` (int?) — chip sets it; MapToRequest reads it.

UI-SPEC surfaces: N1 (button + chip), N2 (modal), N3 (#previewPairingSummary), N4 (anti-double entries),
N5 (unlink confirm modal). Copy = Bahasa Indonesia (see UI-SPEC Copywriting Contract). Render all
user-authored text (room title/category/NIP/Name) via `.textContent` — NEVER innerHTML (XSS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step-1 trigger + chip + room-picker modal (N1/N2) wired to SearchLinkTargets</name>
  <files>Views/Admin/InjectAssessment.cshtml</files>
  <read_first>
    - Views/Admin/InjectAssessment.cshtml:114-180 (Step-1 structure), :1029-1073 (fetch handler pattern), the JS block where DOM listeners are registered
    - Views/Admin/CreateAssessment.cshtml:757-834 (#successModal — modal pattern to copy verbatim)
    - .planning/phases/397-link-pre-post-ke-room-existing/397-UI-SPEC.md N1 + N2 + Copywriting Contract + Aksesibilitas
    - .planning/phases/397-link-pre-post-ke-room-existing/397-PATTERNS.md Surface 9 + section "XSS-safe render"
  </read_first>
  <action>
Replace the placeholder note at :162 with a conditional `#prePostLinkBlock` (UI-SPEC N1) and add the
`#roomPickerModal` (UI-SPEC N2). Inherit the 394/395/396 design system verbatim (Bootstrap 5.3,
2-weight typography, 60/30/10 color). All copy Bahasa Indonesia (UI-SPEC Copywriting Contract).

(a) N1 — in the Tipe Assessment block (replace :162):
- `#btnCariRoom` = `<button type="button" class="btn btn-outline-primary" id="btnCariRoom"><i class="bi bi-search me-1"></i>Cari Room Pasangan</button>`, `d-none` by default.
- Hint `.form-text.text-muted` (`bi-info-circle`): "Opsional — tautkan ke room pasangan (Pre↔Post) agar tampil berpasangan. Bisa di-skip dan ditautkan nanti." + dynamic tipe-lawan line: inject PreTest → "Cari room Post-Test pasangannya." / PostTest → "Cari room Pre-Test pasangannya."
- Chip `#selectedRoomChip` (UI-SPEC N1b), `d-none` initially: `<span class="badge bg-light text-dark border d-inline-flex align-items-center gap-2 p-2">` with `bi-link-45deg text-primary`, room title+date via `.textContent`, type badge (`bg-secondary`), group badge (Kasus A `bg-success` "Sudah ber-grup" / Kasus B `bg-warning text-dark` "Standalone"), and a `btn-close` (`aria-label="Hapus tautan room"`) to clear.
- Hidden field bound to the VM: `<input type="hidden" asp-for="LinkedTargetRepId" id="LinkedTargetRepId" />`.

(b) JS show/hide: on `#assessmentTypeInput` `change`, toggle `#btnCariRoom` (show for PreTest/PostTest,
    `d-none` for Standard) and update the dynamic tipe-lawan hint; when switching to Standard, clear the
    chip + reset `#LinkedTargetRepId` (link only valid for Pre/Post, D-06).

(c) N2 — `#roomPickerModal` (copy CreateAssessment :757-766 structure): `.modal.fade` →
    `.modal-dialog-centered.modal-lg` → `.modal-content.border-0.shadow`; header `bg-light` "Cari Room
    Pasangan (Pre/Post)" + `btn-close`; body `p-4` with: an `.alert.alert-info.small` instruction
    (UI-SPEC N2b copy), a debounced search `#roomSearchInput` (input-group, `bi-search`), and a
    `#roomPickerResults` list-group (`max-height:340px;overflow-y:auto; role="listbox"`).
- Open via `#btnCariRoom` (Bootstrap `data-bs-toggle` or JS); on `shown.bs.modal` focus `#roomSearchInput`.
- Debounced (~300ms) fetch `GET @Url.Action("SearchLinkTargets","InjectAssessment")?term=...&injectType=...`
  with `injectType` = current `#assessmentTypeInput` value. Render rows as `<button class="list-group-item list-group-item-action">`:
  left = title (`fw-bold`, `.textContent`) + meta `.small.text-muted` "{Kategori} · {tanggal} · {n} peserta";
  right = type badge (`bg-secondary`) + group badge (Kasus A `bg-success` / Kasus B `bg-warning text-dark`) + optional `bg-info text-dark` "Inject" when `IsManualEntry`.
- Loading / empty / error states per UI-SPEC N2d (spinner "Mencari…", "Tidak ada room … cocok", "Gagal memuat daftar room. Coba lagi.").
- Click a row → set `#LinkedTargetRepId` = `RepresentativeId`, populate `#selectedRoomChip` (title/date/type/group badges via `.textContent`), show chip, change `#btnCariRoom` label to "Ganti Room", close modal.
- Chip `btn-close` → clear `#LinkedTargetRepId`, hide chip, restore "Cari Room Pasangan" (skip = standalone, D-04).

Render ALL room title/category text via `.textContent` (XSS). Do NOT introduce a new CSS framework.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` reports "Build succeeded"
    - Grep `id="roomPickerModal"` in Views/Admin/InjectAssessment.cshtml returns 1 match
    - Grep `id="btnCariRoom"` returns 1 match
    - Grep `id="selectedRoomChip"` returns 1 match
    - Grep `id="LinkedTargetRepId"` returns 1 match (hidden field, asp-for bound)
    - Grep `SearchLinkTargets` returns ≥1 match (fetch URL)
    - The placeholder note "tersedia pada fase berikutnya" is REMOVED (grep → 0)
    - Grep `.textContent` near the row-render JS (XSS-safe) returns ≥1; grep `innerHTML` for room data → 0
  </acceptance_criteria>
  <done>Step-1 shows "Cari Room Pasangan" for Pre/Post, opens the modal, lists opposite-type rooms (inject + online) with Kasus A/B badges via SearchLinkTargets, sets a removable chip + hidden field; placeholder note removed. Build green. Runtime verified in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Pairing summary in Pratinjau (N3) + anti-double entries (N4) + unlink confirm modal (N5)</name>
  <files>Views/Admin/InjectAssessment.cshtml</files>
  <read_first>
    - Views/Admin/InjectAssessment.cshtml — the Pratinjau / Step-5/6 preview surface (reuse PreviewInjectScore render area), the existing error-list panel (396 N4), the summary render :718
    - .planning/phases/397-link-pre-post-ke-room-existing/397-UI-SPEC.md N3 + N4 + N5 + Copywriting Contract + Aksesibilitas
    - .planning/phases/397-link-pre-post-ke-room-existing/397-PATTERNS.md Surface 5 (preview) + Surface 8 (unlink)
  </read_first>
  <action>
(a) N3 — `#previewPairingSummary` in the Pratinjau surface, `d-none` initially, `role="status" aria-live="polite"`.
    When a room is linked (`#LinkedTargetRepId` set), fetch `POST @Url.Action("PreviewPairing","InjectAssessment")`
    (anti-forgery header/token) with body `{LinkTargetRepId, AssessmentType, UserIds, CompletedAt}` and render:
- heading `.h6.fw-bold` "Ringkasan Penautan Pre/Post";
- `<span class="badge bg-success">{p} ter-pair</span>` + `<span class="badge bg-warning text-dark">{u} tanpa pasangan</span>` + unpaired help text (UI-SPEC N3 copy);
- Kasus B (`WillTouchOnline`) → `.alert.alert-warning` banner "Data online akan disentuh." (UI-SPEC body copy with the count); Kasus A → optional `.alert.alert-success.small` "Room sudah ber-grup — penautan tidak menyentuh data online.";
- `DateWarn` → `.alert.alert-warning.small.py-2` (`bi-calendar-x`) "Tanggal Pre lebih baru dari Post — urutan janggal untuk gain-score. Periksa backdate. (Tetap diizinkan.)";
- counts + room name via `.textContent`.
When NOT linked → `#previewPairingSummary` stays hidden (D-04 skip).

(b) N4 — anti-double-link entries from `DoubleLinkErrors[]` into the SAME error/warn list panel already
    used by 396 (not a new panel). Default BLOK styling (`.alert-danger` item, `bi-x-octagon-fill`),
    full list, each entry: "Pekerja {Nama} (NIP {NIP}) sudah memiliki {Pre/Post}-Test di grup target —
    tidak dapat ditautkan dua kali. Lepaskan tautan lama atau pilih room lain." Render via `.textContent`,
    wrap `max-height:240px;overflow-y:auto` if >8. (The actual reject-all enforcement is server-side in
    397-02; this surfaces it pre-commit so HC fixes it before clicking Inject.)

(c) N5 — unlink confirm modal + trigger. Per UI-SPEC N5 the host location is plan-time; choose the
    pragmatic host: a "Lepaskan Tautan" control on the post-commit success surface of the inject wizard
    (after a linked commit) AND/OR document that the same modal pattern + endpoint can be mounted on the
    Pre/Post group row in monitoring (note it, do not build the monitoring host this phase — keep minimal,
    D-12). Build:
- trigger `<button type="button" class="btn btn-sm btn-outline-danger" id="btnUnlinkRoom"><i class="bi bi-link-45deg"></i><i class="bi bi-x"></i> Lepaskan Tautan</button>` (shown only when the just-committed batch was linked);
- `#unlinkConfirmModal` Bootstrap modal (NOT native `confirm()`): header `bg-warning text-dark` / `bg-danger text-white`, heading "Lepaskan tautan Pre/Post?", body per UI-SPEC N5 copy (incl. the Kasus B kosong-sebelah caveat), buttons "Batal" (`btn-secondary`, initial focus) + "Ya, Lepaskan" (`btn-danger`, `#btnConfirmUnlink`);
- `#btnConfirmUnlink` → `POST @Url.Action("UnlinkInjectGroup","InjectAssessment")` (anti-forgery) with `injectGroupId` (the resolved group id of the just-committed batch — carry it from the commit response/TempData or a hidden field); on `{ok:true}` show "Tautan dilepas." (`alert-success`/toast) + reset link UI; on error show the message.
Modal a11y per UI-SPEC: focus "Batal" first, `aria-describedby` to body, Esc closes.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` reports "Build succeeded"
    - Grep `id="previewPairingSummary"` returns 1 match with `aria-live="polite"`
    - Grep `PreviewPairing` returns ≥1 match (fetch URL in the preview JS)
    - Grep `Data online akan disentuh` returns 1 match (Kasus B banner copy)
    - Grep `sudah memiliki` returns 1 match (anti-double entry copy, N4)
    - Grep `id="unlinkConfirmModal"` returns 1 match AND grep `confirm(` (native) → 0 (Bootstrap modal, not native)
    - Grep `UnlinkInjectGroup` returns ≥1 match (unlink fetch URL)
    - All new user-data render via `.textContent` (grep new blocks for `innerHTML` with room/worker data → 0)
  </acceptance_criteria>
  <done>Pratinjau shows pairing summary + Kasus B banner + date warn (N3); anti-double entries appear in the error list (N4); unlink uses a Bootstrap confirm modal calling UnlinkInjectGroup (N5). Build green. Runtime verified in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright e2e (modal/picker/chip/preview/unlink + cross-grouping) + 0-migration gate</name>
  <files>tests/e2e/inject-assessment-397.spec.ts</files>
  <read_first>
    - tests/e2e/inject-assessment-395.spec.ts:1-55 (login admin, serial, dbSnapshot beforeAll/afterAll, --workers=1 — mirror structure)
    - tests/helpers/accounts.ts + tests/helpers/dbSnapshot.ts (login + snapshot/restore helpers)
    - .planning/phases/397-link-pre-post-ke-room-existing/397-UI-SPEC.md Interaction Contracts 1-10
    - .planning/phases/397-link-pre-post-ke-room-existing/397-RESEARCH.md section "Wave 0 Gaps" (e2e) + Pitfall 5 + Project Constraints (Seed Workflow)
    - CLAUDE.md (Develop Workflow + Seed Workflow: snapshot/restore, SEED_JOURNAL.md)
  </read_first>
  <action>
Create `tests/e2e/inject-assessment-397.spec.ts`, mirroring inject-assessment-395.spec.ts (serial,
loginAdmin via helpers/accounts, `db.snapshot` in beforeAll + `db.restore` in afterAll, record
`docs/SEED_JOURNAL.md`). Run from MAIN tree, AD-off, `--workers=1`.

Cover UI-SPEC Interaction Contracts (runtime — grep+build insufficient, lesson 354):
1. Cari Room conditional on type: set `#assessmentTypeInput` Standard → `#btnCariRoom` hidden; PreTest/PostTest → visible + tipe-lawan hint; placeholder note absent.
2. Open modal → opposite-type filter: click Cari Room → `#roomPickerModal` visible; rows are opposite type; search by title filters (debounced); inject rooms show "Inject" badge.
3. Kasus A/B badge per row: a grouped target shows "Sudah ber-grup" (success); a standalone shows "Standalone" (warning).
4. Pick room → chip + skippable: click a row → modal closes, `#selectedRoomChip` shows title/date/type/group badge, `#LinkedTargetRepId` set; click chip `btn-close` → cleared (skip), form still submittable standalone.
5. Pairing summary in Pratinjau: with a linked room, the preview shows "{p} ter-pair" + "{u} tanpa pasangan"; Kasus B shows "Data online akan disentuh" banner; Kasus A does not.
6. WARN tanggal: when Pre date > Post target date, the date-warn alert appears; commit not blocked.
7. Anti-double entry: a worker with a same-type sibling in the target group surfaces in the error list (full list); commit blocked for that case.
8. Commit cross-grouping intact (KRITIS §13): seed (or author) so one side is online and one inject; commit a linked inject; then verify via /CMP/Records (or gain-score view) that the pair surfaces, AND query the DB to confirm online Score/Status UNCHANGED + audit "LinkPrePost" present (Kasus B). This is the load-bearing assertion.
9. Unlink + confirm modal: trigger Lepaskan Tautan → `#unlinkConfirmModal` opens (NOT native confirm) → Ya, Lepaskan → success notice; DB shows links reverted.
10. Skip penautan: Pre/Post without picking a room → inject runs standalone (LinkedGroupId null), no pairing summary.

Because this writes DB (commit + cross-grouping + unlink), snapshot/restore is MANDATORY (CLAUDE.md
Seed Workflow). Keep tests self-cleaning. Document any seed in SEED_JOURNAL.md and mark CLEANED.

0-migration gate (run after the suite): confirm no schema diff was introduced this phase.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test e2e/inject-assessment-397.spec.ts --workers=1 2>&1 | grep -E "passed|failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/inject-assessment-397.spec.ts` exists and references `roomPickerModal`, `selectedRoomChip`, `previewPairingSummary`, `unlinkConfirmModal`
    - Playwright run reports all 397 spec tests passed (`--workers=1`, MAIN tree, AD-off)
    - The cross-grouping test (Contract 8) asserts BOTH: the pair surfaces in Records/gain-score AND online Score/Status unchanged in DB + audit "LinkPrePost" present
    - The unlink test (Contract 9) asserts a Bootstrap modal (not native confirm) and DB link reverted
    - 0-migration gate: `dotnet ef migrations add _verify397 --no-build` (or scripted check) shows EMPTY Up()/Down() → then REMOVE the probe migration; OR `git diff --name-only` shows no files under Migrations/ or Data/SeedData.cs for the phase. Document which check was used.
    - SEED_JOURNAL.md updated + marked CLEANED (snapshot/restore ran)
  </acceptance_criteria>
  <done>Playwright proves modal/picker/chip/pairing/anti-double/unlink at runtime AND cross inject↔online grouping intact with online data untouched (KRITIS §13). 0-migration confirmed. DB restored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify link Pre/Post end-to-end (modal/chip/pairing/unlink + cross-grouping)</name>
  <what-built>
Phase 397 link Pre/Post: room-picker modal + chip in Step-1, pairing summary + Kasus B banner +
date warn in Pratinjau, anti-double entries in the error list, and an unlink confirm modal — wired
to SearchLinkTargets / PreviewPairing / UnlinkInjectGroup, with per-worker bidirectional linking and
atomic Kasus B write-to-online + audit "LinkPrePost". Cross inject↔online grouping verified by
Playwright + DB.
  </what-built>
  <how-to-verify>
Run the app from the MAIN working tree (not the sibling worktree), AD-off:
1. `dotnet build HcPortal.csproj` then run on http://localhost:5277 with `Authentication__UseActiveDirectory=false`.
2. Log in as admin@pertamina.com / 123456. Open /Admin/InjectAssessment.
3. Step-1: set Tipe = "Pre-Test" → "Cari Room Pasangan" appears with hint "Cari room Post-Test pasangannya."; set "Standard" → it disappears.
4. Click "Cari Room Pasangan" → modal opens; confirm only Post-Test rooms listed; type in search to filter; confirm Kasus A rooms show "Sudah ber-grup" (green) and standalone rooms show "Standalone" (yellow), inject rooms show "Inject".
5. Pick a STANDALONE (Kasus B) Post room → chip appears with the room title + "Standalone" badge; hidden field set.
6. Author a question, pick workers, go to Pratinjau → confirm "{p} ter-pair" / "{u} tanpa pasangan" + the yellow "Data online akan disentuh." banner (Kasus B). If you backdated Pre later than the Post room → confirm the date-warn alert.
7. (Optional) Pick a worker who already has a Post(=same-type) in the target → confirm the anti-double entry appears and blocks.
8. Commit a linked inject; open the worker's /CMP/Records (and the Pre/Post group / gain-score) → confirm the pair shows as one Pre/Post group (silang inject↔online). Confirm in DB that the online room's scores/answers/status are UNCHANGED (only LinkedGroupId/LinkedSessionId added) and an audit "LinkPrePost" row exists.
9. Use "Lepaskan Tautan" → confirm a Bootstrap confirm modal (not a browser confirm()) → "Ya, Lepaskan" → notice "Tautan dilepas."; confirm the link reverted in DB.
Per CLAUDE.md: snapshot the local DB before testing and RESTORE after (record in SEED_JOURNAL.md).
  </how-to-verify>
  <action>PAUSE for human verification. The executor performs no code changes in this task — it presents the   <resume-signal>Type "approved" or describe issues (e.g. "modal lists wrong type", "Kasus B touched online scores", "pair not showing in Records").</resume-signal>lt;how-to-verify  <resume-signal>Type "approved" or describe issues (e.g. "modal lists wrong type", "Kasus B touched online scores", "pair not showing in Records").</resume-signal>gt; steps to the user, waits for the resume-signal, and only proceeds (marking INJ-12 complete) after explicit approval. If the user reports issues, loop back to the relevant Task (1-3) to fix before re-requesting approval.</action>
  <resume-signal>Type "approved" or describe issues (e.g. "modal lists wrong type", "Kasus B touched online scores", "pair not showing in Records").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → controller endpoints | picker fetch (GET), pairing preview (POST), unlink (POST) — user-driven |
| rendered room/worker data → DOM | room titles/categories + worker NIP/Name are user-authored → XSS sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-397-14 | Tampering/XSS | room title/category + NIP/Name in modal/chip/preview/error list | mitigate | Render via `.textContent`, never innerHTML (acceptance grep). Server endpoints already RBAC'd (397-03). |
| T-397-15 | Tampering | CSRF on PreviewPairing / UnlinkInjectGroup fetch | mitigate | Send anti-forgery token in POST fetch (RequestVerificationToken header); controller has [ValidateAntiForgeryToken]. |
| T-397-16 | Elevation | unlink reachable by non-Admin/HC | mitigate | Endpoint RBAC (397-03); UI control only rendered in the Admin/HC page; server is authoritative. |
| T-397-17 | Tampering | online scores changed via Kasus B link (visible regression) | mitigate | e2e Contract 8 asserts online Score/Status unchanged in DB after a Kasus B commit (the load-bearing runtime check). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` succeeds (view embedded).
- `npx playwright test e2e/inject-assessment-397.spec.ts --workers=1` all green (MAIN tree, AD-off).
- Cross-grouping (Contract 8) green: pair surfaces in Records/gain-score AND online data unchanged + "LinkPrePost" audit present.
- `dotnet test` full suite green (no regression to online assessment flows).
- 0 migration confirmed (probe migration empty + removed, or git diff shows no Migrations/Data changes).
- Human-verify approved.
</verification>

<success_criteria>
- Step-1 conditional "Cari Room Pasangan" + room-picker modal (opposite type, inject + online, Kasus A/B badges) + removable chip → hidden field.
- Pratinjau pairing summary (paired/unpaired) + Kasus B "data online akan disentuh" banner + date warn (D-07/D-11); anti-double entries in error list (D-08).
- Unlink confirm modal (Bootstrap, not native) → UnlinkInjectGroup (D-12).
- Cross inject↔online grouping intact at runtime with online data untouched (INJ-12, spec §13, SC3).
- 0 migration; full suite + Playwright green; human-verify approved.
</success_criteria>

<output>
After completion, create `.planning/phases/397-link-pre-post-ke-room-existing/397-04-SUMMARY.md`
documenting: the view IDs added (roomPickerModal/btnCariRoom/selectedRoomChip/LinkedTargetRepId/
previewPairingSummary/unlinkConfirmModal), the e2e contracts covered (esp. Contract 8 cross-grouping
result), the unlink host location chosen (success surface vs monitoring), the 0-migration check used,
and the human-verify outcome. Mark INJ-12 complete and note Phase 397 ready for Phase 398 (Test + UAT).
</output>
