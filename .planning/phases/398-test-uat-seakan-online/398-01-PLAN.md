---
phase: 398-test-uat-seakan-online
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/inject-seakan-online-398.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: true
requirements: [INJ-13]
must_haves:
  truths:
    - "Sesi inject (Form) muncul di /CMP/Records pekerja berlabel 'Assessment Online' (tanpa penanda visual yang membedakan dari online)"
    - "/CMP/Results/{id} sesi inject menampilkan per-soal Benar/Salah (badge .bi-check-circle-fill / .bi-x-circle-fill), BUKAN empty-state 'Tinjauan jawaban tidak tersedia'"
    - "Skenario yang author soal ber-ElemenTeknis menampilkan card 'Analisis Elemen Teknis' di Results"
    - "Sesi essay inject berakhir Status='Completed' (BUKAN 'Menunggu Penilaian') dan per-soal essay tampil tanpa badge .bi-hourglass-split"
    - "Sertifikat PDF sesi inject (cert auto) dapat diunduh dari /CMP/CertificatePdf/{id} (200 + application/pdf + >1024 byte)"
    - "Mode Auto-generate dan Import Excel masing-masing tembus inject→Records→Results sekali"
    - "Side-by-side: 1 sesi inject + 1 sesi online (IsManualEntry=0) untuk pekerja yang sama keduanya berlabel 'Assessment Online' di Records tanpa pembeda inject↔online"
    - "DB di-restore ke baseline pada afterAll (sukses atau gagal); 0 residu 'ZZ %398%'"
  artifacts:
    - path: "tests/e2e/inject-seakan-online-398.spec.ts"
      provides: "Spec konsolidasi downstream parity (Form+essay+ET / Auto-gen / Excel / Pre-Post linked / side-by-side)"
      min_lines: 250
      contains: "inject-seakan-online-398"
    - path: "docs/SEED_JOURNAL.md"
      provides: "Entri audit-trail Phase 398 (temporary + local-only, snapshot→cleaned)"
      contains: "398"
  key_links:
    - from: "tests/e2e/inject-seakan-online-398.spec.ts"
      to: "tests/helpers/dbSnapshot.ts"
      via: "import * as db from '../helpers/dbSnapshot'"
      pattern: "from '\\.\\./helpers/dbSnapshot'"
    - from: "tests/e2e/inject-seakan-online-398.spec.ts"
      to: "tests/helpers/accounts.ts"
      via: "import { accounts } from '../helpers/accounts'"
      pattern: "from '\\.\\./helpers/accounts'"
    - from: "tests/e2e/inject-seakan-online-398.spec.ts"
      to: "/CMP/RecordsWorkerDetail + /CMP/Results/{id} + /CMP/CertificatePdf/{id}"
      via: "page.goto / page.request.get pasca-commit (admin→worker = RecordsWorkerDetail; /CMP/Records hanya tampil records milik akun login)"
      pattern: "/CMP/RecordsWorkerDetail"
---

<objective>
Bangun satu spec Playwright konsolidasi `tests/e2e/inject-seakan-online-398.spec.ts` yang membuktikan hasil inject (Phase 393-397) tampil **identik dengan assessment online** end-to-end di 4 surface downstream pekerja, dan **tak bisa dibedakan** dari sesi online asli (side-by-side parity).

Ini mengisi GAP yang BELUM diuji di spec 395/396/397: mereka berhenti di assertion **commit** (DB Score == preview, anti silent-grade-0). Plan 398-01 melanjutkan SETELAH commit dengan navigasi ke `/CMP/Records`, `/CMP/Results/{id}`, dan `/CMP/CertificatePdf/{id}`.

Purpose: Kunci INJ-13 surface downstream + side-by-side parity sebagai test repeatable (D-01: E2E otomatis SAJA — tidak ada human UAT 398 terpisah; bukti mata-manusia sudah dari per-phase UAT 394-397).
Output: 1 file e2e baru (4 skenario inject + 1 side-by-side, ~4-5 skenario per D-04) + entri SEED_JOURNAL.md (temporary + local-only, snapshot→cleaned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/398-test-uat-seakan-online/398-CONTEXT.md
@.planning/phases/398-test-uat-seakan-online/398-RESEARCH.md
@.planning/phases/398-test-uat-seakan-online/398-PATTERNS.md
@CLAUDE.md

<critical_facts>
DRIFT YANG SUDAH DIVERIFIKASI (honor PERSIS — CONTEXT/RESEARCH sempat drift; PATTERNS.md + grep mengonfirmasi):
- Helper test inject ada di `tests/helpers/` (BUKAN `tests/e2e/helpers/`). Import `from '../helpers/accounts'` + `from '../helpers/dbSnapshot'`. (examTypes/wizardSelectors ADA di `tests/e2e/helpers/` tapi itu untuk spec online; spec inject TIDAK memakainya.)
- Tabel Identity = `Users` (BUKAN `AspNetUsers`) untuk lookup NIP. `SELECT TOP 1 NIP FROM Users WHERE Email='...'`.
- D-02c "Analisis Elemen Teknis" card di Results HANYA render bila `Model.ElemenTeknisScores` non-empty (Views/CMP/Results.cshtml:208). Itu butuh soal di-author DENGAN field `#elemenTeknis` terisi. Helper authoring di 395/396 TIDAK mengisi `#elemenTeknis` → card tak muncul. Skenario Form 398 WAJIB author ≥1 soal dengan `#elemenTeknis` terisi agar D-02c assertable.
- Inject view punya input authoring `#elemenTeknis` + `#scoreValue` (Views/Admin/InjectAssessment.cshtml:1141-1161); soal di-tambah via `#injAddQuestionBtn` (client-state, bukan POST CreateQuestion).
- D-05 (regresi online-path) ADA di Plan 398-02 — JANGAN tulis di sini.
- Route BulkBackfill 404 sudah diuji di inject-excel-396.spec.ts:348-366 — JANGAN duplikasi di 398.
</critical_facts>

<interfaces>
<!-- Kontrak helper yang dipakai — executor pakai langsung, no eksplorasi codebase -->

From tests/helpers/dbSnapshot.ts (VERIFIED — Read langsung):
```typescript
export async function backup(path: string): Promise<void>;      // BACKUP ... WITH INIT, FORMAT
export async function restore(path: string): Promise<void>;     // SINGLE_USER ROLLBACK → RESTORE WITH REPLACE → MULTI_USER
export async function queryScalar(sql: string): Promise<number>; // first int
export async function queryString(sql: string): Promise<string>; // first non-empty line
export async function execScript(sqlPath: string): Promise<void>; // run .sql file
// Built-in non-localhost guard (dbSnapshot.ts:39-44).
```

From tests/helpers/accounts.ts (VERIFIED):
```typescript
export const accounts = {
  admin:    { email: 'admin@pertamina.com',        password: '123456' },
  coachee:  { email: 'rino.prasetyo@pertamina.com', password: '...' },  // RINO (NIP ada)
  coachee2: { email: 'iwan3@pertamina.com',         password: '...' },  // iwan (NIP ada)
};
```

Wizard selectors inject (VERIFIED dari 395/396 spec + Views/Admin/InjectAssessment.cshtml):
```
#Title, #Category (selectOption index:1), #btnNext1..#btnNext5, #step-1..#step-6
#userCheckboxContainer .user-check-item[data-email="EMAIL"] .user-checkbox  (pilih pekerja)
#QuestionType (selectOption 'MultipleChoice'|'MultipleAnswer'|'Essay'), #questionText
#option_A, #option_B, #correct_A (check), #rubrik, #scoreValue, #elemenTeknis  (authoring)
#injAddQuestionBtn  (tambah soal ke client-state)
#assessmentTypeInput (selectOption 'Standard'|'PreTest'|'PostTest'), #btnCariRoom (muncul Pre/Post)
#step5Body, #step5WorkerHeading, #step5ModeManual, #step5AnswerForm input[type="radio"]
#step5PreviewBtn, #step5PreviewResult, #step5NextWorker
#step5MethodExcel (toggle Excel), #step5ExcelPanel, #step5FormPath, #step5ExcelFile, #btnDownloadTemplate
window.injBuildWorkerAnswers()  (payload builder — anti silent-grade-0), #AnswersJson, #btnInject
.alert-success filter hasText /Inject berhasil/i  (flash sukses pasca-commit)
```

Surface anchors downstream (VERIFIED — Read langsung):
```
/CMP/Records          → Views/CMP/Records.cshtml:138 "Assessment Online" label; :203 data-type="assessment"
                        label di-set server-side Services/WorkerDataService.cs:47 RecordType="Assessment Online"
                        (NO IsManualEntry filter; include Completed + PendingGrading — WorkerDataService.cs:33)
/CMP/Results/{id}     → Controllers/CMPController.cs:2184 Results(int id); terima /CMP/Results/{id} & ?id={id}
  per-soal Benar      → Views/CMP/Results.cshtml:342 badge .bi-check-circle-fill "Benar"
  per-soal Salah      → Views/CMP/Results.cshtml:348 badge .bi-x-circle-fill "Salah"
  essay pending       → Views/CMP/Results.cshtml:336 badge .bi-hourglass-split "Menunggu Penilaian" (HARUS 0)
  card Tinjauan       → Views/CMP/Results.cshtml:320 "Tinjauan Jawaban"
  empty-state         → Views/CMP/Results.cshtml:416 "Tinjauan jawaban tidak tersedia" (HARUS count 0)
  Elemen Teknis card  → Views/CMP/Results.cshtml:207-212 "Analisis Elemen Teknis" (render iff ElemenTeknisScores non-empty)
  gating render       → CMPController.cs:2212-2217 (butuh UserPackageAssignment + PackageUserResponses + AllowAnswerReview=true)
/CMP/CertificatePdf/{id} → CMPController.cs:1943 (HttpGet, QuestPDF; endpoint SAMA online)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec scaffold + Form/essay/ElemenTeknis scenario (D-02 full 4 surface)</name>
  <files>tests/e2e/inject-seakan-online-398.spec.ts</files>
  <read_first>
    - tests/e2e/inject-assessment-395.spec.ts (FULL — copy header, loginAdmin 22-31, authorMcQuestion 34-41, authorEssayQuestion 70-75, fillToStep5 48-67, snapshot/restore beforeAll/afterAll 94-115, commit + anti-silent-grade-0 152-189)
    - Views/CMP/Results.cshtml lines 207-419 (anchor Elemen Teknis card + per-soal Benar/Salah + essay-pending badge + empty-state)
    - Views/CMP/Records.cshtml lines 130-210 (anchor "Assessment Online" label + Results link)
    - Views/Admin/InjectAssessment.cshtml lines 1141-1200 (authoring inputs #scoreValue/#elemenTeknis + addQuestion serialize ScoreValue/ElemenTeknis)
    - tests/helpers/dbSnapshot.ts (signature backup/restore/queryScalar/queryString/execScript)
    - tests/helpers/accounts.ts (accounts.admin)
  </read_first>
  <action>
    Buat file BARU `tests/e2e/inject-seakan-online-398.spec.ts`. Mirror scaffold 395 PERSIS:
    1. Header comment (pre-req: server localhost:5277 dari MAIN tree, `Authentication__UseActiveDirectory=false`, login admin@pertamina.com, `--workers=1`; KRITIS menulis DB → snapshot/restore CLAUDE.md Seed Workflow). Catat tujuan: "downstream parity GAP — navigasi pasca-commit ke 4 surface; 395/396/397 berhenti di commit".
    2. Imports: `import { test, expect, type Page } from '@playwright/test';` + `import { accounts } from '../helpers/accounts';` + `import * as db from '../helpers/dbSnapshot';` + (untuk Excel & seed nanti) `import * as path from 'path'; import * as os from 'os'; import * as fs from 'fs'; import ExcelJS from 'exceljs';`.
    3. `test.describe.configure({ mode: 'serial' });` + `const TS = Date.now();` + `let snapshotPath = '';` + `const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'inject-398-'));`.
    4. `const WORKER_EMAILS = ['rino.prasetyo@pertamina.com', 'iwan3@pertamina.com'];` (pekerja ber-NIP; controller skip null-NIP).
    5. Copy verbatim helper `loginAdmin(page)` (395:22-31).
    6. Copy helper `authorMcQuestion(page, text)` (395:34-41) DAN buat varian baru `authorMcQuestionWithElemen(page, text, elemen)` yang setelah `#correct_A` juga isi `await page.fill('#elemenTeknis', elemen);` SEBELUM `#injAddQuestionBtn` (perlu untuk D-02c card "Analisis Elemen Teknis").
    7. Copy helper `authorEssayQuestion(page, text)` (395:70-75).
    8. Copy `test.beforeAll` (395:94-103) — resolve `InstanceDefaultBackupPath`, snapshotPath = `${dir}/HcPortalDB_Dev-pre398-${ts}.bak`, `await db.backup(snapshotPath)`. Copy `test.afterAll` (395:106-115) — `db.restore(snapshotPath)` di try/catch, throw bila gagal.

    Lalu tulis SKENARIO 1 — `test.describe('INJ-13 Form: 4 surface seakan-online', ...)`:
    - login admin → `page.goto('/Admin/InjectAssessment')`.
    - `const title = 'ZZ Inject 398 Form ' + TS;` `const titleSql = title.replace(/'/g, "''");`.
    - Drive wizard (pola fillToStep5 395:48-67 tapi inline): `#Title`=title, `#Category` selectOption index:1, `#btnNext1`; pilih 1 pekerja WORKER_EMAILS[0] via checkbox; `#btnNext2`; author 1 soal via `authorMcQuestionWithElemen(page, 'Soal MC 1 — '+title, 'Elemen A')` + 1 soal `authorEssayQuestion(page, 'Soal Essay 2 — '+title)`; `#btnNext3`; **Sertifikat: set mode auto** — di step-4, pilih radio cert auto (cari selector cert auto di Views/Admin/InjectAssessment.cshtml step-4, mis. `#certMode` radio value auto / `input[name="CertMode"][value="Auto"]`; bila label beda, gunakan radio "Generate otomatis"); `#btnNext4`; expect `#step-5` + `#step5Body` visible.
    - Step-5 input-asli pekerja 1: pilih radio opsi A (benar) untuk soal MC (`#step5AnswerForm input[type="radio"]` nth(0)); untuk essay isi teks + skor penuh (selector essay step-5: textarea jawaban + input skor — lihat Views/Admin/InjectAssessment.cshtml:1530-1540 `Skor Essay (0–N)`). Isi skor = max agar Lulus.
    - Anti silent-grade-0: `const answersRaw = await page.evaluate(() => JSON.stringify((window as any).injBuildWorkerAnswers())); expect(JSON.parse(answersRaw).length).toBe(1);` dan answers[0].answers.length toBe(2).
    - Commit: `#btnNext5` → expect `#step-6` → `#btnInject` → `page.waitForLoadState('load')` → expect `.alert-success` hasText /Inject berhasil/i visible.
    - `const sessionId = await db.queryScalar(\`SELECT TOP 1 Id FROM AssessmentSessions WHERE Title='${titleSql}' ORDER BY Id DESC\`);`.
    - **D-04 essay §13:** `const status = await db.queryString(\`SELECT Status FROM AssessmentSessions WHERE Id=${sessionId}\`); expect(status).toBe('Completed');`.
    - **D-02a Records:** `await page.goto('/CMP/Records');` (login masih admin — Records admin menampilkan semua; bila Records butuh worker context, gunakan `/CMP/RecordsWorkerDetail?workerId=${uid}` dengan uid = `db.queryString("SELECT TOP 1 UserId FROM AssessmentSessions WHERE Id=...")`). Assert: `const row = page.locator('table tr', { hasText: title }); await expect(row.first()).toBeVisible(); await expect(row.first()).toContainText('Assessment Online');`.
    - **D-02b/c Results:** `await page.goto(\`/CMP/Results/${sessionId}\`);` → `await expect(page.locator('text=Tinjauan jawaban tidak tersedia')).toHaveCount(0);` → `await expect(page.locator('.bi-check-circle-fill, .bi-x-circle-fill').first()).toBeVisible();` → `await expect(page.getByText('Analisis Elemen Teknis')).toBeVisible();` (card render karena soal MC ber-ElemenTeknis) → essay pending badge HARUS 0: `await expect(page.locator('.bi-hourglass-split')).toHaveCount(0);`.
    - **D-02d Cert PDF:** `const resp = await page.request.get(\`/CMP/CertificatePdf/${sessionId}\`); expect(resp.status()).toBe(200); expect(resp.headers()['content-type']).toMatch(/^application\/pdf/i); expect((await resp.body()).length).toBeGreaterThan(1024);`.

    Bila ada selector step-4 cert / step-5 essay yang berbeda dari tebakan, baca Views/Admin/InjectAssessment.cshtml untuk selector AKTUAL dan gunakan itu (jangan invent). Jalankan spec setelah menulis untuk konfirmasi skenario 1 hijau sebelum lanjut Task 2.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test e2e/inject-seakan-online-398.spec.ts -g "Form: 4 surface" --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/inject-seakan-online-398.spec.ts` ada; `grep -c "/CMP/RecordsWorkerDetail" tests/e2e/inject-seakan-online-398.spec.ts` ≥ 1 (DEVIASI Rule-2: admin tak punya akses record pekerja via `/CMP/Records` (hanya akun login) → pakai `/CMP/RecordsWorkerDetail?workerId=` yang controller izinkan untuk admin roleLevel≤3)
    - `grep -c "page.goto(\`/CMP/Results" tests/e2e/inject-seakan-online-398.spec.ts` ≥ 1
    - `grep -c "/CMP/CertificatePdf" tests/e2e/inject-seakan-online-398.spec.ts` ≥ 1
    - `grep "from '../helpers/dbSnapshot'" tests/e2e/inject-seakan-online-398.spec.ts` cocok (BUKAN `../e2e/helpers`)
    - `grep "Analisis Elemen Teknis" tests/e2e/inject-seakan-online-398.spec.ts` cocok (D-02c di-assert)
    - `grep "expect(status).toBe('Completed')" tests/e2e/inject-seakan-online-398.spec.ts` cocok (D-04 essay §13)
    - `npx playwright test ... -g "Form: 4 surface" --workers=1` melaporkan 1 passed
  </acceptance_criteria>
  <done>Skenario 1 (Form + essay + ElemenTeknis) tembus inject→commit→Records(label)→Results(per-soal+ET+no-pending)→cert PDF; essay Status=Completed; spec hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-generate + Excel scenarios (D-04 representatif, masing-masing 1x tembus)</name>
  <files>tests/e2e/inject-seakan-online-398.spec.ts</files>
  <read_first>
    - tests/e2e/inject-assessment-395.spec.ts lines 193-280 (auto-generate: #step5ModeAuto / target input + #step5PreviewBtn + #step5PreviewResult skor>=target)
    - tests/e2e/inject-excel-396.spec.ts lines 59-135 (fillToStep5 + switchToExcel + downloadTemplate + workerNip + buildUploadXlsx exceljs fresh) + 209-267 (upload valid → preview → commit → DB score)
    - Views/Admin/InjectAssessment.cshtml lines 1530-1540 (essay skor input step-5) + toggle Excel #step5MethodExcel
  </read_first>
  <action>
    Tambah 2 skenario ke spec yang sama (reuse helper Task 1):

    SKENARIO 2 — `test.describe('INJ-13 Auto-generate: tembus surface', ...)`:
    - login admin → `/Admin/InjectAssessment`; `const title = 'ZZ Inject 398 AutoGen ' + TS;`.
    - Drive wizard pilih 1 pekerja WORKER_EMAILS[0], author 2 soal MC (`authorMcQuestion`), cert mode auto.
    - Step-5: aktifkan mode auto-generate (`#step5ModeAuto` check) → isi skor target (selector target input dari 395, mis. `#step5TargetScore` / input target — gunakan selector AKTUAL dari 395:193-280) = 100.
    - `#step5PreviewBtn` → `#step5PreviewResult` visible → assert berisi skor (mis. contains '100%' atau ≥ target sesuai pola 395) + 'Lulus'; `not.toContainText('KPB/')` (no cert# di preview).
    - Anti silent-grade-0: `injBuildWorkerAnswers()` length=1.
    - Commit (`#btnNext5`→`#step-6`→`#btnInject`→flash). `sessionId` via queryScalar.
    - Navigasi surface: `/CMP/Records` (row contains title + 'Assessment Online') + `/CMP/Results/${sessionId}` (no empty-state + ≥1 badge Benar/Salah). Cert PDF: `page.request.get('/CMP/CertificatePdf/'+sessionId)` 200 + application/pdf + >1024.

    SKENARIO 3 — `test.describe('INJ-13 Excel: tembus surface', ...)`:
    - Tambah helper `switchToExcel(page)` (copy 396:79-83: check `#step5MethodExcel`, expect `#step5ExcelPanel` visible + `#step5FormPath` hidden), `workerNip(email)` (copy 396:96-102: `SELECT TOP 1 NIP FROM Users WHERE Email=...` — Users BUKAN AspNetUsers), `buildUploadXlsx(...)` (copy 396:108-135: exceljs Workbook + worksheet 'Jawaban', row1 header, row2 col1=NIP/col2=Nama/col3+=jawaban).
    - login admin → `/Admin/InjectAssessment`; `const title = 'ZZ Inject 398 Excel ' + TS;`.
    - Drive wizard pilih 1 pekerja WORKER_EMAILS[0]; author 1 soal MC (A benar) + 1 soal Essay (skor 0..10); cert mode auto; ke step-5.
    - `switchToExcel(page)`; `const nip = await workerNip(WORKER_EMAILS[0]);`; `const filled = await buildUploadXlsx('ok-398', nip, { 3: 'A', 4: 10 });` (col3=MC jawaban 'A', col4=essay skor 10); `await page.setInputFiles('#step5ExcelFile', filled);` → klik upload (selector `#btnUploadExcel` dari 396) → expect `#step5ExcelPreview` visible.
    - Commit (`#btnNext5`→`#step-6`→`#btnInject`→flash). `sessionId` via queryScalar. **Essay §13:** `expect(await db.queryString('SELECT Status ...')).toBe('Completed')`.
    - Navigasi surface: `/CMP/Records` (label) + `/CMP/Results/${sessionId}` (no empty-state + badge + no `.bi-hourglass-split`) + cert PDF 200+pdf+>1024.

    JANGAN duplikasi assertion grading-depth (sudah di 395/396) — fokus surface downstream. JANGAN test route BulkBackfill 404 (sudah di 396). Jalankan ketiga skenario serial untuk konfirmasi hijau.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test e2e/inject-seakan-online-398.spec.ts -g "Auto-generate|Excel" --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.describe" tests/e2e/inject-seakan-online-398.spec.ts` ≥ 3 (Form + Auto-gen + Excel)
    - `grep "FROM Users WHERE Email" tests/e2e/inject-seakan-online-398.spec.ts` cocok (Users, bukan AspNetUsers)
    - `grep "new ExcelJS.Workbook\|ExcelJS.Workbook\|addWorksheet('Jawaban')" tests/e2e/inject-seakan-online-398.spec.ts` cocok (Excel fresh build)
    - `grep "#step5MethodExcel" tests/e2e/inject-seakan-online-398.spec.ts` cocok
    - `npx playwright test ... -g "Auto-generate|Excel" --workers=1` melaporkan 2 passed
  </acceptance_criteria>
  <done>Skenario Auto-generate dan Excel masing-masing tembus inject→commit→Records→Results(+essay Completed Excel)→cert PDF; spec hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Pre/Post linked + side-by-side parity (D-03, D-04 Pre/Post) + SEED_JOURNAL</name>
  <files>tests/e2e/inject-seakan-online-398.spec.ts, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/inject-assessment-397.spec.ts lines 27-99 (RINO_ID const, seedOnlinePostRoom execScript IsManualEntry=0 full column list, fillToStep5PreLinked #assessmentTypeInput PreTest + #btnCariRoom, beforeAll seed + snapshot, afterAll restore) + 100-200 (modal picker #btnCariRoom flow + link commit + cross-grouping assert)
    - docs/SEED_JOURNAL.md lines 1-12 (header + 397-04 + 395-03 row format — copy kolom: Tanggal | Phase | Klasifikasi | Tujuan | Dampak | Snapshot file | Status)
    - .planning/phases/398-test-uat-seakan-online/398-PATTERNS.md lines 194-211 (D-03 seed online sibling decision) + 289-293 (Results-level parity nuance)
  </read_first>
  <action>
    Tambah 2 skenario terakhir + entri SEED_JOURNAL.

    SKENARIO 4 — `test.describe('INJ-13 Pre/Post linked silang inject↔online', ...)` (D-04 +1 Pre/Post):
    - Reuse pola 397: di `beforeAll` SUDAH ada snapshot; tambahkan seed online Post room standalone untuk Rino via `seedOnlinePostRoom()` (copy 397:48-65 — execScript INSERT AssessmentSessions IsManualEntry=0, Score=80, Status='Completed', AssessmentType='PostTest', AccessToken='ONLINE', UserId=RINO_ID; gunakan `const RINO_ID = '4a624dbc-3241-4207-92d7-d1d5784c7137';` dari 397:29). PANGGIL seed di dalam `test.beforeAll` SETELAH `db.backup` (sehingga ter-restore di afterAll).
    - Skenario: login admin → `/Admin/InjectAssessment`; title inject PreTest 'ZZ Inject 398 Pre ' + TS; `#assessmentTypeInput` selectOption 'PreTest' → `#btnCariRoom` visible → klik → modal picker → cari online Post room (by title ONLINE_POST_TITLE) → pilih → chip muncul (selector chip dari 397, mis. `#selectedRoomChip` / hidden `#LinkedTargetRepId`). Pilih Rino, author 1 MC, cert mode None/auto, ke step-5, input-asli benar, commit.
    - Assert grouping silang utuh (TANPA duplikasi assertion online-untouched yang sudah di 397; cukup buktikan surface): inject Pre sessionId → `/CMP/Records` (row inject Pre 'Assessment Online') + assert online Post row juga 'Assessment Online' (keduanya tampil untuk Rino). Opsional: assert `LinkedGroupId` inject == online via queryScalar bila ringkas.
    - CATATAN: ini fokus DOWNSTREAM (Records render pasangan), BUKAN re-prove write-to-online (397 sudah). Bila modal picker selector tidak persis, baca inject-assessment-397.spec.ts:100-200 untuk flow AKTUAL.

    SKENARIO 5 — `test.describe('INJ-13 side-by-side parity inject vs online', ...)` (D-03):
    - PUTUSAN STRUKTUR (PATTERNS Open Q1 + Assumption A1): parity di-scope ke **Records-row level** (bukan Results-render online sibling — seed minimal tak punya package → Results online sibling akan empty-state, bukan parity sebenarnya). Buktikan: sesi inject (dari salah satu skenario di atas, mis. Form sessionId) DAN sesi online sibling (seedOnlineSibling untuk Rino, IsManualEntry=0) KEDUANYA tampil di `/CMP/Records` Rino berlabel **"Assessment Online"** TANPA atribut/badge/teks yang membedakan inject dari online.
    - Implementasi: buat helper `seedOnlineSibling()` (varian seedOnlinePostRoom 397:48-65, title 'ZZ Online Sibling 398 ' + TS, AssessmentType='Standard', Status='Completed', IsManualEntry=0, UserId=RINO_ID). Panggil di beforeAll setelah backup.
    - Drive 1 inject Form RINGAN (1 MC) untuk Rino → commit → sessionId inject.
    - `/CMP/RecordsWorkerDetail?workerId=${RINO_ID}` (atau `/CMP/Records` bila login sebagai Rino) → assert BARIS inject (hasText inject title) contains 'Assessment Online' DAN baris online sibling (hasText sibling title) contains 'Assessment Online'. Assert KEDUA baris punya `data-type="assessment"` (Records.cshtml:203) yang sama — tak ada penanda 'Manual'/'Inject' di DOM: `await expect(page.locator('table')).not.toContainText('Manual Entry'); await expect(page.locator('table')).not.toContainText('Inject');` (load-bearing INJ-13 "tak bisa dibedakan").

    SEED_JOURNAL (docs/SEED_JOURNAL.md): tambah 1 baris BARU setelah header (jangan edit baris existing) dengan format kolom PERSIS dari baris 397-04:
    `| 2026-06-18 | 398 (e2e INJ-13 seakan-online downstream parity) | temporary + local-only | Jalankan inject-seakan-online-398.spec hijau: Form+essay+ElemenTeknis / Auto-gen / Excel / Pre-Post linked silang / side-by-side parity Records-row — tiap skenario inject→commit→navigasi /CMP/Records (label "Assessment Online") + /CMP/Results/{id} (per-soal Benar/Salah + Analisis Elemen Teknis + essay Status=Completed no-pending) + /CMP/CertificatePdf/{id} (>1024 byte). | AssessmentSessions(+N prefix 'ZZ … 398') + Packages/Questions/Options/Assignments/Responses/AuditLogs(+N) + NomorSertifikat (cert auto); online sibling/Post D-03/D-04 IsManualEntry=0 UserId=Rino. Semua revert via RESTORE. Data/SeedData.cs tak tersentuh. | {SQL InstanceDefaultBackupPath}/HcPortalDB_Dev-pre398-{ts}.bak | active |`
    (Status 'active' di sini; Plan 398-02 yang flip ke 'cleaned' setelah verifikasi restore COUNT 'ZZ %398%'=0.)

    Jalankan SELURUH spec serial untuk konfirmasi 5 skenario hijau + DB restore di afterAll.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test e2e/inject-seakan-online-398.spec.ts --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.describe" tests/e2e/inject-seakan-online-398.spec.ts` ≥ 5 (Form, Auto-gen, Excel, Pre/Post linked, side-by-side)
    - `grep "IsManualEntry" tests/e2e/inject-seakan-online-398.spec.ts` cocok (seed online sibling/Post, IsManualEntry=0)
    - `grep "not.toContainText('Manual'\|not.toContainText('Inject'\|toContainText('Assessment Online')" tests/e2e/inject-seakan-online-398.spec.ts` cocok (parity load-bearing)
    - `grep "| 398 " docs/SEED_JOURNAL.md` cocok (entri Phase 398 ditambah, klasifikasi temporary + local-only)
    - `npx playwright test e2e/inject-seakan-online-398.spec.ts --workers=1` melaporkan 5 passed (atau jumlah test == jumlah skenario), DB restore OK di afterAll
  </acceptance_criteria>
  <done>Spec 398 lengkap 5 skenario hijau (Form/Auto-gen/Excel/Pre-Post/side-by-side); SEED_JOURNAL entri Phase 398 active; afterAll restore OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → SQLEXPRESS lokal | Spec menulis DB lokal `HcPortalDB_Dev` (commit inject + cert + audit + seed online). Harus disposable via snapshot/restore; server DB (10.55.3.3) JANGAN disentuh. |
| Playwright → endpoint RBAC admin | Spec drive `/Admin/InjectAssessment` (RBAC Admin,HC) + baca `/CMP/Results` (authz CMPController.IsResultsAuthorized) sebagai admin login. |
| test fixture → repo | File .xlsx + .sql sementara ditulis ke `os.tmpdir()` (bukan repo); SEED_JOURNAL dicatat di repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-398-01 | Tampering | DB lokal tak ter-restore → data 'ZZ' nyangkut di Records/audit produksi-lokal | mitigate | `db.backup` di beforeAll + `db.restore` di afterAll (try/catch, throw bila gagal — pola 395:106-115); Plan 398-02 verifikasi COUNT 'ZZ %398%'=0 |
| T-398-02 | Tampering | sqlcmd target non-localhost (server Dev) | mitigate | dbSnapshot.ts:39-44 built-in non-localhost guard (reuse helper, JANGAN sqlcmd inline) |
| T-398-03 | Info Disclosure | NIP/email pekerja nyata di fixture/log | accept | Email pekerja sudah ada di accounts.ts + spec 395/396/397; tak ada secret baru; lokal-only; tak masuk artifact publik |
| T-398-04 | Elevation of Privilege | spec login admin lalu baca Results pekerja lain | accept | by-design: admin BERWENANG lihat Results (parity online); cross-section Forbid sudah diuji di cmp-records-346.spec.ts (bukan scope 398) |
| T-398-05 | Tampering | seed online sibling (IsManualEntry=0) salah tulis ke sesi produksi | mitigate | INSERT via execScript dengan title prefix 'ZZ … 398' unik per-run + UserId=Rino spesifik; ter-restore di afterAll |
</threat_model>

<verification>
- `npx playwright test e2e/inject-seakan-online-398.spec.ts --workers=1` → semua skenario passed (server jalan dari MAIN tree, AD-off).
- Setiap skenario punya minimal: `page.goto('/CMP/Records')` (label) + `page.goto('/CMP/Results/{id}')` (per-soal) — bukan berhenti di `db.queryScalar` (anti Pitfall 1).
- Skenario essay (Form + Excel) assert `Status='Completed'` + `.bi-hourglass-split` count 0 (Pitfall 3 / §13).
- afterAll `db.restore` jalan; DB kembali baseline (Plan 398-02 verifikasi COUNT).
</verification>

<success_criteria>
- Spec `tests/e2e/inject-seakan-online-398.spec.ts` ada dengan 5 skenario (Form+essay+ET / Auto-gen / Excel / Pre-Post linked silang / side-by-side parity).
- 4 surface (Records label, Results per-soal, Elemen Teknis card, cert PDF) di-assert di ≥1 skenario masing-masing (D-02 a/b/c/d).
- Side-by-side parity (D-03) membuktikan inject↔online tak bisa dibedakan di Records (Records-row level, putusan struktur terdokumentasi).
- Matriks representatif (D-04) — Form/Auto-gen/Excel + essay wajib + Pre/Post linked; bukan full cartesian.
- DB snapshot/restore (CLAUDE.md) diterapkan; SEED_JOURNAL entri Phase 398 ditambahkan.
- Import dari `../helpers/` (BUKAN `../e2e/helpers/`); NIP lookup dari `Users`.
</success_criteria>

<output>
After completion, create `.planning/phases/398-test-uat-seakan-online/398-01-SUMMARY.md`
</output>
