---
phase: 398-test-uat-seakan-online
plan: 02
type: execute
wave: 2
depends_on: [398-01]
files_modified:
  - docs/SEED_JOURNAL.md
  - .planning/phases/398-test-uat-seakan-online/398-VALIDATION.md
autonomous: true
requirements: [INJ-13]
must_haves:
  truths:
    - "Full xUnit suite (HcPortal.Tests) hijau — tidak ada regresi dari milestone v32.2 (D-05 i)"
    - "Online-path e2e existing (exam-types FLOW K/L/M/R, exam-taking Flow A) di-RERUN dan hijau — jalur online create→ambil→grade→cert tetap utuh berdampingan dengan inject (D-05 ii)"
    - "0 migration terkonfirmasi: git diff HEAD -- Migrations/ Data/ kosong (gate)"
    - "DB lokal kembali baseline: COUNT 'ZZ %398%' = 0 setelah restore — SEED_JOURNAL entri Phase 398 di-flip 'cleaned'"
    - "398-VALIDATION.md Per-Task Map terisi (INJ-13 surface + regresi + gate), nyquist_compliant: true, wave_0_complete: true"
  artifacts:
    - path: "docs/SEED_JOURNAL.md"
      provides: "Entri Phase 398 di-flip ke 'cleaned' (restore terverifikasi)"
      contains: "cleaned"
    - path: ".planning/phases/398-test-uat-seakan-online/398-VALIDATION.md"
      provides: "Per-Task Map terisi + nyquist_compliant true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "regresi online-path"
      to: "tests/e2e/exam-types.spec.ts + tests/e2e/exam-taking.spec.ts"
      via: "npx playwright test (rerun, tanpa edit)"
      pattern: "exam-types.spec.ts"
    - from: "full suite"
      to: "HcPortal.Tests/HcPortal.Tests.csproj"
      via: "dotnet test"
      pattern: "HcPortal.Tests"
---

<objective>
Buktikan jalur assessment ONLINE existing tidak ter-regresi berdampingan dengan fitur inject (D-05), konfirmasi 0 migration (gate), dan kunci kebersihan DB lokal (restore terverifikasi → SEED_JOURNAL cleaned). Plus finalize kontrak validasi Nyquist (398-VALIDATION.md Per-Task Map + flag compliant).

Purpose: D-05 — regresi = (i) full `dotnet test` hijau + (ii) RERUN online-path e2e existing (JANGAN tulis ulang). Bukti jalur online asli tetap utuh.
Output: SEED_JOURNAL entri Phase 398 → 'cleaned'; 398-VALIDATION.md compliant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/398-test-uat-seakan-online/398-CONTEXT.md
@.planning/phases/398-test-uat-seakan-online/398-RESEARCH.md
@.planning/phases/398-test-uat-seakan-online/398-PATTERNS.md
@.planning/phases/398-test-uat-seakan-online/398-VALIDATION.md
@CLAUDE.md

<critical_facts>
- D-05 ii = RERUN spec online EXISTING (BUKAN tulis baru). Spec online: `tests/e2e/exam-types.spec.ts` (FLOW K=MA, L=Essay+grade, M=Mixed, R=Cert) + `tests/e2e/exam-taking.spec.ts` (Flow A=MC lifecycle+cert). Mereka pakai login helper BERBEDA (`tests/helpers/auth.ts` `login(page, 'hc'|'coachee')` + `tests/e2e/helpers/examTypes`) — JANGAN ubah; cukup jalankan.
- Assumption A3 (RESEARCH): baseline online-path DULU. Bila ada spec online yang sudah flaky SEBELUM perubahan inject, kegagalan BUKAN karena inject. Catat di SUMMARY bila terjadi.
- 0-migration baseline saat ini KOSONG (verified `git diff HEAD -- Migrations/ Data/` empty). Pertahankan.
- Full suite = `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` (~501 Fact/Theory).
- DB write: spec 398 (Plan 01) + online-path rerun KEDUANYA menulis DB lokal → snapshot/restore sudah di dalam masing-masing spec (beforeAll/afterAll) + playwright.config globalTeardown restore. Plan 02 hanya VERIFIKASI kebersihan, tidak menulis fixture baru.
</critical_facts>

<interfaces>
Commands (VERIFIED env):
```bash
# (i) full xUnit suite
dotnet test "HcPortal.Tests/HcPortal.Tests.csproj"
# (ii) rerun online-path e2e (D-05 ii) — server localhost:5277 dari MAIN tree, AD-off, --workers=1
cd tests && npx playwright test e2e/exam-types.spec.ts e2e/exam-taking.spec.ts --workers=1
# 0-migration gate
git diff --stat HEAD -- Migrations/ Data/   # → kosong = 0 migration
# kebersihan DB lokal (setelah semua e2e + restore)
# queryScalar COUNT 'ZZ %398%' via dbSnapshot helper ATAU sqlcmd lokal -S localhost\SQLEXPRESS → 0
```
SEED_JOURNAL row format: tabel `| Tanggal | Phase | Klasifikasi | Tujuan | Dampak | Snapshot file | Status |` (docs/SEED_JOURNAL.md:8). Status 'active' → 'cleaned' setelah restore terverifikasi.
398-VALIDATION.md frontmatter: `nyquist_compliant: false` + `wave_0_complete: false` → set keduanya `true` di akhir.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline + rerun online-path e2e (D-05 ii) + full xUnit suite (D-05 i)</name>
  <files>(no file edit — verification run; hasil dicatat di SUMMARY)</files>
  <read_first>
    - .planning/phases/398-test-uat-seakan-online/398-PATTERNS.md lines 215-234 (tabel FLOW K/L/M/R + Flow A + run command D-05ii)
    - .planning/phases/398-test-uat-seakan-online/398-RESEARCH.md lines 384-391 (Assumption A3 baseline-first) + 404-418 (Environment Availability + catatan SQLBrowser/lpc/--workers=1)
    - CLAUDE.md (Develop Workflow — dotnet build + dotnet run verifikasi lokal)
  </read_first>
  <action>
    Prasyarat runtime (sekali set): server `dotnet run` jalan dari MAIN tree dengan `Authentication__UseActiveDirectory=false` di localhost:5277, SQLEXPRESS reachable. Bila login 500 / SQL conn fail → start service SQLBrowser + gunakan `lpc:` shared-memory override (memory reference_local_e2e_sql_env_fix).

    1. **Build gate:** `dotnet build HcPortal.csproj` → 0 error. (Bila ada error, hentikan — bukan masalah test, masalah kompilasi.)
    2. **(D-05 i) Full xUnit suite:** `dotnet test "HcPortal.Tests/HcPortal.Tests.csproj"` → catat jumlah passed/failed/skipped. WAJIB 0 failed. Bila gagal, catat test mana + apakah terkait inject (393-397) atau pre-existing.
    3. **(D-05 ii) Baseline + rerun online-path e2e:** Pastikan DB di baseline (restore bila perlu via snapshot manual). Jalankan:
       `cd tests && npx playwright test e2e/exam-types.spec.ts e2e/exam-taking.spec.ts --workers=1`
       Catat hasil per-FLOW (K/L/M/R + Flow A). Spec ini self-snapshot/restore + globalTeardown restore. WAJIB hijau. Bila ada FLOW yang gagal, tentukan apakah pre-flaky (Assumption A3) — bila gagal di baseline tanpa kaitan inject, catat sebagai pre-existing (bukan regresi inject), JANGAN blokir milestone atas dasar flake online yang sudah ada; eskalasi ke user bila ambigu.
    4. (Opsional cross-check) `npx playwright test e2e/cmp-records-346.spec.ts --workers=1` untuk Records surface online — hanya bila waktu memungkinkan, bukan gate.

    Catat SEMUA angka (xUnit passed, e2e per-FLOW) di SUMMARY untuk audit milestone (Plan 398-03).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test "HcPortal.Tests/HcPortal.Tests.csproj" --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build HcPortal.csproj` → "0 Error(s)"
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` output mengandung "Failed: 0" (atau "Passed!" summary tanpa failed)
    - `npx playwright test e2e/exam-types.spec.ts e2e/exam-taking.spec.ts --workers=1` melaporkan semua passed (atau pre-existing flake terdokumentasi + dieskalasi, bukan regresi inject)
    - Angka konkret (xUnit N passed; e2e M passed) tercatat di SUMMARY
  </acceptance_criteria>
  <done>Full xUnit suite hijau (D-05 i) + online-path e2e existing di-rerun hijau (D-05 ii); jalur online tidak ter-regresi; angka tercatat.</done>
</task>

<task type="auto">
  <name>Task 2: 0-migration gate + DB cleanliness verify + SEED_JOURNAL cleaned + VALIDATION compliant</name>
  <files>docs/SEED_JOURNAL.md, .planning/phases/398-test-uat-seakan-online/398-VALIDATION.md</files>
  <read_first>
    - docs/SEED_JOURNAL.md (entri Phase 398 yang ditambahkan Plan 398-01 Task 3 — flip Status 'active' → 'cleaned')
    - .planning/phases/398-test-uat-seakan-online/398-VALIDATION.md (skeleton Per-Task Map + Wave 0 + frontmatter flags)
    - .planning/phases/398-test-uat-seakan-online/398-RESEARCH.md lines 308-315 (0-migration gate opsi) + 341-365 (Validation Architecture → Test Map sumber Per-Task Map)
  </read_first>
  <action>
    1. **0-migration gate (D-discretion → opsi A cepat):** `git diff --stat HEAD -- Migrations/ Data/` → WAJIB kosong. Bila tidak kosong, ada perubahan tak diinginkan — investigasi + bersihkan (fase test tidak boleh menambah migration). Catat hasil "0 migration confirmed" di SUMMARY.
    2. **DB cleanliness verify:** Setelah seluruh e2e (398 spec + online rerun) selesai + restore, verifikasi tidak ada residu test:
       `SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE 'ZZ %398%'` → 0 (via dbSnapshot.queryScalar atau sqlcmd lokal). Bila >0, jalankan restore manual dari snapshot terakhir + ulang verify. JANGAN tandai cleaned sebelum COUNT=0.
    3. **SEED_JOURNAL flip:** Di `docs/SEED_JOURNAL.md`, ubah Status entri Phase 398 (ditambahkan Plan 01) dari `active` menjadi:
       `cleaned (2026-06-18, afterAll RESTORE OK + globalTeardown RESTORE; e2e 398 passed; COUNT 'ZZ %398%'=0 verified)`.
       Jangan ubah kolom lain dari entri itu, jangan sentuh entri lain.
    4. **Finalize 398-VALIDATION.md:**
       - Isi tabel "Per-Task Verification Map" dari RESEARCH §Validation Architecture → Test Map (baris: D-02a Records label, D-02b Results per-soal, D-02c Elemen Teknis, D-02d cert PDF, D-03 side-by-side, D-04 essay §13 — semua Test Type e2e, Automated Command `npx playwright test e2e/inject-seakan-online-398.spec.ts --workers=1`, File `tests/e2e/inject-seakan-online-398.spec.ts` ✅; D-05 i `dotnet test` ✅; D-05 ii rerun exam-types/exam-taking ✅; 0-migration gate `git diff` ✅; D-06 audit di Plan 398-03). Set Status tiap baris ✅ green (karena Plan 01+02 sudah hijau).
       - Centang checklist "Wave 0 Requirements" (spec 398 ada; online-path rerun done).
       - Centang "Validation Sign-Off" semua item.
       - Frontmatter: set `nyquist_compliant: true` + `wave_0_complete: true` + `status: complete`. Ubah "Approval: pending" → "Approval: approved (398-02)".
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && git diff --stat HEAD -- Migrations/ Data/ && grep -c "nyquist_compliant: true" .planning/phases/398-test-uat-seakan-online/398-VALIDATION.md && grep -c "| 398 " docs/SEED_JOURNAL.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat HEAD -- Migrations/ Data/` → output kosong (0 migration confirmed)
    - `SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE 'ZZ %398%'` = 0 (DB bersih)
    - `grep "| 398 " docs/SEED_JOURNAL.md` mengandung "cleaned" (status di-flip)
    - `grep -c "nyquist_compliant: true" .planning/phases/398-test-uat-seakan-online/398-VALIDATION.md` = 1
    - `grep -c "wave_0_complete: true" .planning/phases/398-test-uat-seakan-online/398-VALIDATION.md` = 1
    - 398-VALIDATION.md Per-Task Map tak lagi berisi placeholder "_planner isi_"
  </acceptance_criteria>
  <done>0 migration confirmed; DB bersih (COUNT 'ZZ %398%'=0); SEED_JOURNAL Phase 398 → cleaned; 398-VALIDATION.md nyquist_compliant + wave_0_complete true dengan Per-Task Map terisi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → SQLEXPRESS lokal | Rerun online-path e2e + verifikasi cleanliness menyentuh DB lokal; server DB (10.55.3.3) JANGAN disentuh. |
| verification → repo state | 0-migration gate baca `git diff`; SEED_JOURNAL + VALIDATION.md ditulis di repo (dokumentasi, bukan kode produksi). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-398-06 | Tampering | DB lokal tertinggal data 'ZZ' setelah rerun → audit milestone (398-03) baca data kotor | mitigate | Task 2 verifikasi `COUNT 'ZZ %398%'=0` SEBELUM flip SEED_JOURNAL cleaned; restore manual bila >0 (Pitfall 5) |
| T-398-07 | Tampering | Migration tak sengaja masuk di fase test (langgar 0-migration milestone) | mitigate | `git diff --stat HEAD -- Migrations/ Data/` gate; bila non-empty, investigasi+bersihkan sebelum lanjut |
| T-398-08 | Repudiation | Klaim "regresi hijau" tanpa bukti angka | mitigate | Task 1 catat angka konkret (xUnit N passed, e2e per-FLOW) di SUMMARY untuk audit milestone |
| T-398-09 | Tampering | sqlcmd cleanliness-check target non-localhost | mitigate | gunakan dbSnapshot helper (non-localhost guard) atau sqlcmd lokal `-S localhost\SQLEXPRESS` |
</threat_model>

<verification>
- `dotnet build` 0 error + `dotnet test` 0 failed (D-05 i).
- `exam-types.spec.ts` + `exam-taking.spec.ts` di-rerun hijau (D-05 ii) — jalur online tidak ter-regresi.
- `git diff HEAD -- Migrations/ Data/` kosong (0-migration gate).
- COUNT 'ZZ %398%' = 0 (DB bersih) → SEED_JOURNAL cleaned.
- 398-VALIDATION.md compliant (nyquist_compliant + wave_0_complete true, Per-Task Map terisi).
</verification>

<success_criteria>
- Full xUnit suite hijau + online-path e2e existing di-rerun hijau (D-05 lengkap).
- 0 migration terkonfirmasi (gate).
- DB lokal bersih; SEED_JOURNAL entri Phase 398 di-flip 'cleaned'.
- 398-VALIDATION.md nyquist-compliant dengan Per-Task Map terisi.
- Angka regresi tercatat untuk audit milestone (Plan 398-03).
</success_criteria>

<output>
After completion, create `.planning/phases/398-test-uat-seakan-online/398-02-SUMMARY.md`
</output>
