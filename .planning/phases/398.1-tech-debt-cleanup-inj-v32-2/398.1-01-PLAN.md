---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/InjectAssessmentController.cs
  - Helpers/InjectExcelHelper.cs
  - Views/Admin/InjectAssessment.cshtml
  - HcPortal.Tests/InjectExcelImportTests.cs
  - tests/e2e/inject-excel-396.spec.ts
autonomous: true
requirements: ["tech-debt"]
user_setup: []

must_haves:
  truths:
    - "Upload Excel dengan 2 baris ber-NIP duplikat tidak melempar ArgumentException (ditangkap deterministik atau diverifikasi mustahil)"
    - "Kolom Tipe di sheet Legenda template menampilkan label UI (Single Answer/Multiple Answer/Essay), bukan enum mentah (MultipleChoice)"
    - "Keputusan essay-decimal terdokumentasi: DROP-with-evidence ATAU fix yang mirror online persis (tidak ada divergensi baru)"
    - "Tombol Download Template tidak memicu 2 unduhan saat di-klik-ganda cepat (debounce/disable)"
    - "dotnet build 0 error; fast suite (Category!=Integration) GREEN; integration InjectExcelImportTests GREEN bila WR-01 di-fix"
    - "git diff Migrations/ Data/ kosong (0 migration)"
  artifacts:
    - path: "Controllers/InjectAssessmentController.cs"
      provides: "dup-NIP safe-dictionary build (jika WR-01 real) ATAU drop terdokumentasi"
      contains: "GroupBy"
    - path: "Helpers/InjectExcelHelper.cs"
      provides: "Legenda kolom Tipe pakai QuestionTypeLabels.Short(type)"
      contains: "QuestionTypeLabels.Short"
    - path: "Views/Admin/InjectAssessment.cshtml"
      provides: "download debounce/disable guard di click handler btnDownloadTemplate"
      contains: "btnDownloadTemplate"
  key_links:
    - from: "Helpers/InjectExcelHelper.cs GenerateTemplate Legenda"
      to: "Models/QuestionTypeLabels.cs Short()"
      via: "label-map call"
      pattern: "QuestionTypeLabels\\.Short"
---

<objective>
Cluster A — bersihkan 4 temuan code-review Phase 396 di trio file Import-Excel: `Controllers/InjectAssessmentController.cs` + `Helpers/InjectExcelHelper.cs` + `Views/Admin/InjectAssessment.cshtml`.

Empat item: (1) 396 WR-01 dup-NIP `ToDictionary` throw [MED, verify-first], (2) 396 WR-02 essay decimal truncation [LOW, LIKELY DROP per D-03], (3) 396 WR-03 download form race [LOW, fix debounce], (4) cosmetic LBL-02 Legenda kolom Tipe enum->label-UI [1-baris fix].

Purpose: tutup tech-debt v32.2 sebelum complete-milestone, bundle deploy bersih. Setiap item WAJIB diverifikasi real-vs-false-positive di kode aktual DULU (D-01) — yang false-positive di-DROP dengan bukti file:line di SUMMARY, bukan di-fix asal.

Output: file controller/helper/view ter-update untuk item yang terbukti real + SUMMARY mencatat drop terdokumentasi untuk item false-positive. 0 migration. v32.2 tetap 13/13 REQ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-CONTEXT.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang dikonsumsi/disentuh. Diambil dari kode aktual — JANGAN eksplorasi ulang. -->

Models/QuestionTypeLabels.cs:13-19 (label-map single source of truth — sudah di-`using` di InjectExcelHelper.cs:6):
```csharp
public static string Short(string? type) => type switch
{
    "MultipleChoice" => "Single Answer",
    "MultipleAnswer" => "Multiple Answer",
    "Essay"          => "Essay",
    _                => "Single Answer"
};
```

Controllers/InjectAssessmentController.cs:355-357 (dup-NIP risk — picker dari _context.Users):
```csharp
var allowedNips = picker.Select(p => p.Nip).ToHashSet();
var nipToUserId = picker.ToDictionary(p => p.Nip, p => p.Id);   // throws jika 2 picker rows share NIP
var nipToName = picker.ToDictionary(p => p.Nip, p => p.Name);
```

Helpers/InjectExcelHelper.cs:112-117 (Legenda — LBL-02):
```csharp
var type = q.QuestionType ?? "MultipleChoice";
leg.Cell(lrow, 3).Value = type;   // tulis enum mentah, BUKAN label UI
```

Helpers/InjectExcelHelper.cs:198-205 (essay parse — WR-02):
```csharp
bool hasScore = scoreCell.TryGetValue<double>(out var sd);
int score;
if (hasScore) { score = (int)sd; }   // (int) cast truncate
```

Online essay parity (D-03 — int-only end-to-end, TIDAK ADA rounding online):
- Helpers/AssessmentScoreAggregator.cs:52-54 -> `totalScore += essayResp.EssayScore.Value;` (EssayScore int?)
- Models/PackageUserResponse.cs:32 -> `public int? EssayScore`
- AssessmentAdminController.cs:3558 (SubmitEssayScore) -> `int score`, validasi 0..ScoreValue (int)

Views/Admin/InjectAssessment.cshtml:1859-1881 (download race) + analog setLoading() :1884-1900.
NIP: TIDAK ada unique-index di EF model (ApplicationDbContext.cs — tak ada HasIndex untuk NIP) -> dup-NIP secara skema MUNGKIN (bukan dijamin mustahil oleh constraint DB).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 396 WR-01 dup-NIP + LBL-02 Legenda (verify-first)</name>
  <files>Controllers/InjectAssessmentController.cs, Helpers/InjectExcelHelper.cs</files>
  <read_first>
    - Controllers/InjectAssessmentController.cs:340-370 (picker + ToDictionary build, file dimodifikasi)
    - Helpers/InjectExcelHelper.cs:108-120 (Legenda gen, file dimodifikasi)
    - Models/QuestionTypeLabels.cs (analog label-map, sudah di-using helper:6)
    - Controllers/WorkerController.cs:100 + Controllers/AssessmentAdminController.cs:751 (analog GroupBy->ToDictionary safe-build)
    - Data/ApplicationDbContext.cs (cek ada/tidak unique-index NIP — untuk D-01)
  </read_first>
  <action>
    **D-01 VERIFY dulu (WR-01 dup-NIP):** Cek apakah NIP unique-indexed di `ApplicationDbContext.cs` (grep `NIP`/`Nip` + `IsUnique`). Hasil sudah diketahui: TIDAK ada unique-index NIP -> dup-NIP secara data MUNGKIN terjadi (2 akun aktif NIP sama). Karena tidak dijamin mustahil oleh constraint, ini **REAL** -> FIX.

    **FIX WR-01** di `Controllers/InjectAssessmentController.cs:356-357`: ganti `picker.ToDictionary(p => p.Nip, p => p.Id)` dan `.Name` menjadi GroupBy + representative deterministik (mirror analog WorkerController.cs:100 / AssessmentAdminController.cs:751 + shared pattern "deterministic representative"):
    ```csharp
    var nipToUserId = picker.GroupBy(p => p.Nip)
                            .ToDictionary(g => g.Key, g => g.OrderBy(p => p.Id).First().Id);
    var nipToName   = picker.GroupBy(p => p.Nip)
                            .ToDictionary(g => g.Key, g => g.OrderBy(p => p.Id).First().Name);
    ```
    `OrderBy(p => p.Id).First()` -> UserId yang dipilih untuk NIP duplikat STABIL antar-request (anti non-determinism). `allowedNips` (:355) tidak berubah.

    **FIX LBL-02** di `Helpers/InjectExcelHelper.cs:114` (Legenda kolom 3): ganti `leg.Cell(lrow, 3).Value = type;` menjadi:
    ```csharp
    leg.Cell(lrow, 3).Value = QuestionTypeLabels.Short(type);
    ```
    `using HcPortal.Models;` sudah ada di helper:6 (verifikasi; tambahkan bila hilang). 1-baris, label-map single source of truth.

    Jika ternyata SAAT verifikasi WR-01 NIP terbukti unique-indexed (bukan kasus saat ini) -> DROP dengan bukti file:line di SUMMARY, jangan ubah controller.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj</automated>
  </verify>
  <acceptance_criteria>
    - REAL->FIX branch: grep "GroupBy(p => p.Nip)" di Controllers/InjectAssessmentController.cs muncul 2x (UserId + Name); grep "QuestionTypeLabels.Short" di Helpers/InjectExcelHelper.cs muncul >=1x.
    - FALSE-POSITIVE branch (hanya jika NIP terbukti unique-indexed): SUMMARY berisi baris "396 WR-01 DROPPED: NIP unique-indexed di ApplicationDbContext.cs:<line>, dup mustahil" — controller TIDAK diubah.
    - LBL-02 SELALU di-fix (cosmetic confirmed real): grep "leg.Cell(lrow, 3).Value = type;" di Helpers/InjectExcelHelper.cs = 0 hit (raw enum write hilang).
    - dotnet build HcPortal.csproj -> "Build succeeded" / 0 error.
  </acceptance_criteria>
  <done>WR-01 di-fix dengan GroupBy-deterministik (atau di-drop dengan bukti unique-index); LBL-02 Legenda pakai QuestionTypeLabels.Short; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: 396 WR-02 essay decimal (verify->likely DROP per D-03) + download race fix + regression test</name>
  <files>Helpers/InjectExcelHelper.cs, Views/Admin/InjectAssessment.cshtml, HcPortal.Tests/InjectExcelImportTests.cs</files>
  <read_first>
    - Helpers/InjectExcelHelper.cs:195-215 (essay parse, file dimodifikasi)
    - Helpers/AssessmentScoreAggregator.cs:50-56 (online essay sum int — parity reference D-03)
    - Models/PackageUserResponse.cs:30-34 (EssayScore int?)
    - Controllers/AssessmentAdminController.cs:3555-3560 (SubmitEssayScore int score — online no-rounding)
    - Views/Admin/InjectAssessment.cshtml:1855-1905 (download click handler + setLoading analog, file dimodifikasi)
    - wwwroot/js/shared-loading.js:10-21 (initFormLoading disable pattern)
    - HcPortal.Tests/InjectExcelImportTests.cs (file dimodifikasi — tambah regression)
    - tests/e2e/inject-excel-396.spec.ts (analog Playwright untuk download)
  </read_first>
  <action>
    **D-01/D-03 VERIFY (WR-02 essay decimal):** Baca jalur essay online: `AssessmentScoreAggregator.cs:52-54` (sum `int? EssayScore.Value`), `PackageUserResponse.cs:32` (`int? EssayScore`), `SubmitEssayScore:3558` (`int score`, validasi 0..ScoreValue int). Konfirmasi: **seluruh pipeline essay online<->inject adalah integer-only — TIDAK ADA rounding/decimal di mana pun online.** Karena online tak punya jalur decimal untuk di-mirror, `(int)` cast di helper:200 ADALAH perilaku parity-correct. Menambah `Math.Round` justru MENCIPTAKAN divergensi -> VIOLATE D-03.
    **DEFAULT ACTION = DROP** dengan bukti file:line di SUMMARY: "396 WR-02 DROPPED: essay online int-only (AssessmentScoreAggregator.cs:52-54 + PackageUserResponse.cs:32 + AssessmentAdminController.cs:3558); (int) cast = parity-correct; rounding apa pun = divergensi (violate D-03)." JANGAN ubah `Helpers/InjectExcelHelper.cs:200`.
    (Hanya jika verifikasi MENGUNGKAP jalur decimal online nyata — yang tidak ada — barulah mirror persis; default tetap DROP.)

    **FIX WR-03 download race** di `Views/Admin/InjectAssessment.cshtml` click handler `btnDownloadTemplate` (~:1861): di awal handler, guard + disable tombol, lalu re-enable via `setTimeout` (download tak punya response event). Mirror bentuk disable `setLoading(true)` (:1884-1900):
    ```javascript
    downloadBtn.addEventListener('click', function () {
        if (downloadBtn.disabled) return;          // debounce guard
        downloadBtn.disabled = true;
        serializeQuestionsHidden();
        // ... bangun form & submit (kode existing) ...
        tmp.submit();
        document.body.removeChild(tmp);
        setTimeout(function () { downloadBtn.disabled = false; }, 1500);   // re-enable (no response event)
    });
    ```
    JANGAN ubah logika serialize/form-build existing — hanya bungkus dengan guard + disable + timeout re-enable.

    **REGRESSION TEST WR-01** (jika WR-01 di-fix di Task 1): tambah xUnit ke `HcPortal.Tests/InjectExcelImportTests.cs` yang membuktikan upload dengan 2 picker rows ber-NIP sama TIDAK throw + memilih UserId deterministik (oldest Id). Pakai fixture existing di file. Jika WR-01 di-drop di Task 1, skip test ini & catat di SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - WR-02 DROP branch (default): SUMMARY berisi baris "396 WR-02 DROPPED" + bukti 3 file:line (AssessmentScoreAggregator.cs:52-54, PackageUserResponse.cs:32, AssessmentAdminController.cs:3558); grep "Math.Round" di Helpers/InjectExcelHelper.cs = TIDAK ada penambahan baru di jalur essay.
    - WR-03 FIX: grep "if (downloadBtn.disabled) return" di Views/Admin/InjectAssessment.cshtml muncul; grep "downloadBtn.disabled = true" muncul; grep "setTimeout" di sekitar handler download.
    - dotnet test --filter "Category!=Integration" -> "Passed!" / failed: 0 (baseline ~390, tidak regresi).
  </acceptance_criteria>
  <done>WR-02 di-drop dengan bukti parity int-only; WR-03 download di-debounce; regression test WR-01 hijau (atau dicatat skip); fast suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Verifikasi Playwright targeted (download race + Legenda) + 0-migration gate</name>
  <files>tests/e2e/inject-excel-396.spec.ts</files>
  <read_first>
    - tests/e2e/inject-excel-396.spec.ts (analog spec untuk download + template render)
    - Views/Admin/InjectAssessment.cshtml:1855-1905 (handler download yang baru di-fix)
    - reference_ithandoff_test_port_5270 / CLAUDE.md Develop Workflow (port lokal 5277 untuk main working tree)
  </read_first>
  <action>
    **D-05 Playwright targeted HANYA untuk view/JS render (download race + Legenda LBL-02).** Build & run dari MAIN working tree (view embedded saat build — TIDAK ada Razor runtime compilation; lihat catatan env STATE.md). Port lokal `5277` (main tree; AD-off untuk lokal: `Authentication__UseActiveDirectory=false`).

    (1) **Download race:** extend/tambah skenario di `tests/e2e/inject-excel-396.spec.ts` — klik `#btnDownloadTemplate` 2x cepat, assert HANYA 1 `download` event ter-trigger dalam window debounce (atau tombol disabled setelah klik-1). Gunakan pattern download-event Playwright (`page.waitForEvent('download')`).
    (2) **Legenda LBL-02:** download template, buka sheet `Legenda`, assert kolom `Tipe` untuk soal MultipleChoice menampilkan `"Single Answer"` (BUKAN `"MultipleChoice"`). Parse .xlsx via exceljs (devDep, pola existing spec).

    **0-MIGRATION GATE:** jalankan `git diff --stat -- Migrations/ Data/` -> harus KOSONG (tak ada perubahan schema/seed). Catat di SUMMARY. (Cluster A murni controller-logic + helper + view + test -> 0 migration by construction.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && git diff --stat -- Migrations/ Data/</automated>
  </verify>
  <acceptance_criteria>
    - Playwright: npx playwright test inject-excel-396 -> skenario download-debounce PASS + Legenda-label PASS (atau output spec menunjukkan 2 skenario baru hijau). Jalankan dari main tree port 5277.
    - 0-migration: git diff --stat -- Migrations/ Data/ -> output KOSONG (tidak ada baris file). SUMMARY catat "0 migration: git diff Migrations/ Data/ kosong".
    - SUMMARY menyimpulkan status tiap item Cluster A: WR-01 (fix/drop), WR-02 (drop+bukti), WR-03 (fix), LBL-02 (fix).
  </acceptance_criteria>
  <done>Download race & Legenda LBL-02 terverifikasi Playwright; 0-migration gate kosong; SUMMARY mencatat disposisi 4 item Cluster A.</done>
</task>

</tasks>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- `dotnet test --filter "Category!=Integration"` GREEN (baseline ~390, no regresi).
- `dotnet test InjectExcelImportTests` (integration) GREEN bila WR-01 di-fix.
- Playwright `inject-excel-396` download-debounce + Legenda-label PASS.
- `git diff --stat -- Migrations/ Data/` KOSONG.
- Setiap item punya disposisi terdokumentasi di SUMMARY (fix dengan grep-evidence ATAU drop dengan file:line evidence).
</verification>

<success_criteria>
- WR-01 dup-NIP: di-fix GroupBy-deterministik (NIP tak unique-indexed -> real) ATAU drop dengan bukti.
- WR-02 essay decimal: di-DROP dengan bukti parity int-only (default D-03) — tidak ada rounding baru.
- WR-03 download race: di-debounce (guard+disable+timeout) + verified Playwright.
- LBL-02 Legenda: pakai QuestionTypeLabels.Short — verified Excel render.
- 0 migration; jalur grading online & preview==commit tidak tersentuh.
</success_criteria>

<output>
After completion, create `.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-01-SUMMARY.md` mencatat disposisi tiap item (fix/drop + bukti), hasil suite, hasil Playwright, dan 0-migration gate.
</output>
