---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 01
wave: 1
status: complete
requirements-completed: ["tech-debt"]
migration: false
commits:
  - "561ccc78 fix(398.1-01): WR-01 dup-NIP safe GroupBy + LBL-02 Legenda label (Task 1)"
  - "af772f33 fix(398.1-01): WR-03 download debounce + WR-01 regression test; WR-02 DROP (Task 2)"
  - "824a20e0 test(398.1-01): Playwright download-debounce + ClosedXML LBL-02 unit (Task 3)"
---

# Plan 398.1-01 SUMMARY — Cluster A (Phase 396 fixes)

Bersihkan 4 temuan code-review Phase 396 di trio Import-Excel. Verify-first (D-01) tiap item.

## Disposisi item (4)

| Item | Sev | Disposisi | Bukti |
|------|-----|-----------|-------|
| **396 WR-01** dup-NIP `ToDictionary` | MED | **FIX** | NIP TAK unique-indexed repo-wide (grep `HasIndex.*NIP`/`IsUnique` = 0; `ApplicationDbContext.cs` tak ada index NIP) → 2 akun aktif bisa berbagi NIP → `picker.ToDictionary(p=>p.Nip,...)` (Controller:356-357) lempar `ArgumentException`. Diganti `GroupBy(Nip).ToDictionary(g.Key, g.OrderBy(Id).First())` — build aman + representative deterministik. |
| **396 WR-02** essay decimal truncation | LOW | **DROP (verified false-positive)** | Pipeline essay int-only end-to-end: `InjectExcelHelper.cs:204` `(int)sd`, `AssessmentScoreAggregator.cs:54` `totalScore += EssayScore.Value` (int), `PackageUserResponse.cs:32` `int? EssayScore`. TIDAK ADA jalur decimal online untuk di-mirror → `(int)` cast = parity-correct. `Math.Round` justru MENCIPTAKAN divergensi → violate D-03. **Kode tak diubah.** |
| **396 WR-03** download form race | LOW | **FIX** | `btnDownloadTemplate` handler (`InjectAssessment.cshtml:1861`) tak ada debounce → klik-ganda = 2 unduhan. Tambah `if (downloadBtn.disabled) return` + `disabled=true` + `setTimeout(re-enable,1500)` (download tak punya response event). Logika serialize/form-build existing TAK disentuh. |
| **cosmetic LBL-02** Legenda kolom Tipe | cosmetic | **FIX** | `InjectExcelHelper.cs:116` tulis enum mentah `type`. Diganti `QuestionTypeLabels.Short(type)` → label UI (`Single Answer`/`Multiple Answer`/`Essay`). 1-baris, label-map single source of truth. |

## Verifikasi (D-05)

- **dotnet build HcPortal.csproj** → 0 error (24 warning pre-existing).
- **Fast suite** (`Category!=Integration`) → **391/391 GREEN** (baseline 390 + 1 unit LBL-02 baru; no regression).
- **Integration** `DuplicateNip_PickerBuild_DoesNotThrow_AndIsDeterministic` (SQLEXPRESS) → **PASS** (seed 2 user NIP sama; bukti ekspresi LAMA throw `ArgumentException` + ekspresi BARU deterministik).
- **Unit** `GenerateTemplate_LegendaTipe_UsesUiLabel` (ClosedXML, fast suite) → **PASS** (Legenda Tipe = label UI; enum mentah absent).
- **Playwright targeted** `inject-excel-396 --grep debounce` @5277 AD-off → **PASS** (2 klik sinkron → 1 unduhan). DB auto backup→RESTORE OK, SEED_JOURNAL cleaned (Seed Workflow).
- **0-migration gate**: `git diff Migrations/ Data/` (561ccc78~1..HEAD) → **KOSONG**.

## Catatan teknis

- **LBL-02 verifikasi via ClosedXML unit, BUKAN exceljs-Playwright**: template di-generate ClosedXML; `exceljs.readFile` TIDAK kompatibel baca output ClosedXML (didokumentasi di spec baris 14/107; flaky pass/fail). ClosedXML baca output-nya sendiri andal → unit test deterministik. Skenario exceljs-Legenda dihapus dari spec, diganti catatan + unit test. Lesson 354 (Razor render butuh Playwright) TIDAK berlaku untuk generasi Excel server-side (pure C#).
- **Invariant intact**: jalur grading online TAK disentuh; preview==commit utuh (engine `AssessmentScoreAggregator.Compute` tak diubah); `.textContent`/XSS-safe tak relevan (perubahan handler download non-render-user-data).

## Self-Check: PASSED
Semua 4 item ber-disposisi terdokumentasi (3 FIX + 1 DROP-with-evidence). Suite green. 0 migration.
