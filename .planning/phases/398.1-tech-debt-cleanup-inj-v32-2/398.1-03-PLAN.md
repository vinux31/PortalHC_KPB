---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 03
type: execute
wave: 3
depends_on: ["398.1-02"]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/PostLisensorPolishTests.cs
autonomous: true
requirements: ["tech-debt"]
user_setup: []

must_haves:
  truths:
    - "POST EditAssessment menolak edit assessment/soal saat ada sesi (representatif atau sibling/grup) berstatus PendingGrading — hard-block (D-02)"
    - "Penambahan-peserta murni (hasAddition, Phase 391 D-02) TIDAK ter-regresi oleh guard baru"
    - "Guard pakai konstanta AssessmentConstants.AssessmentStatus.PendingGrading (single source of truth), bukan literal string"
    - "dotnet build 0 error; fast suite GREEN; edit-guard test (PendingGrading hard-block + add-only carve-out) hijau"
    - "git diff Migrations/ Data/ kosong (0 migration)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "PendingGrading hard-block guard di POST EditAssessment (single-mode + Pre/Post branch)"
      contains: "AssessmentStatus.PendingGrading"
    - path: "HcPortal.Tests/PostLisensorPolishTests.cs"
      provides: "edit-guard test: PendingGrading->block, add-only->allow"
      contains: "PendingGrading"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs POST EditAssessment"
      to: "Models/AssessmentConstants.cs AssessmentStatus.PendingGrading"
      via: "guard constant"
      pattern: "AssessmentStatus\\.PendingGrading"
---

<objective>
Cluster C — tutup carry tech-debt v32.0: **999.11 PendingGrading edit-guard gap** di `Controllers/AssessmentAdminController.cs` POST `EditAssessment`.

Saat ini POST EditAssessment hanya hard-block status `Completed` (saat tanpa penambahan peserta), TAPI tidak memblokir `PendingGrading` — sehingga Admin bisa mengedit assessment/soal saat penilaian essay sedang in-flight, berpotensi mengorup skor in-flight. D-02: **HARD-BLOCK** edit selama ada sesi `PendingGrading`, mirror guard kanonik di `SubmitEssayScore:3563-3567` memakai `AssessmentConstants.AssessmentStatus.PendingGrading`.

Purpose: konsistensi guard PendingGrading lintas surface; cegah korup skor. Verify-first (D-01) — konfirmasi gap nyata + pastikan penambahan-peserta Phase 391 tak ter-regresi.

Output: guard PendingGrading di kedua cabang POST EditAssessment + test edit-guard. 0 migration. v32.2 tetap 13/13; Phase 391 add-participant tetap jalan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-CONTEXT.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Kontrak aktual. JANGAN eksplorasi ulang. -->

Models/AssessmentConstants.cs:13-19 (single source of truth status):
```csharp
public static class AssessmentStatus {
    public const string Completed      = "Completed";
    public const string PendingGrading = "Menunggu Penilaian"; // <- WAJIB pakai konstanta ini, jangan literal
    public const string InProgress     = "InProgress";
}
```

Analog hard-block kanonik — Controllers/AssessmentAdminController.cs:3563-3567 (SubmitEssayScore):
```csharp
var session = await _context.AssessmentSessions.FindAsync(sessionId);
if (session == null) return Json(new { success = false, message = "Session tidak ditemukan" });
if (session.Status != AssessmentConstants.AssessmentStatus.PendingGrading)
    return Json(new { success = false, message = "Penilaian hanya bisa dilakukan saat status Menunggu Penilaian." });
```

GAP — Controllers/AssessmentAdminController.cs:1995-2002 (POST EditAssessment, single-mode branch):
```csharp
bool hasAddition = NewUserIds != null && NewUserIds.Count > 0;
if (assessment.Status == AssessmentConstants.AssessmentStatus.Completed && !hasAddition)
{
    TempData["Error"] = "Cannot edit completed assessments.";
    return RedirectToAction("ManageAssessment");
}
// ^ memblokir Completed; TIDAK memblokir PendingGrading (gap 999.11)
```

Sibling fetch single-mode — :2055-2060 (key Title+Category+Schedule.Date):
```csharp
var siblings = await _context.AssessmentSessions
    .Where(a => a.Title == origTitle && a.Category == origCategory && a.Schedule.Date == origScheduleDate)
    .ToListAsync();
```

Pre/Post branch — :1806-1826 (group fetch by LinkedGroupId):
```csharp
if ((assessment.AssessmentType == "PreTest" || assessment.AssessmentType == "PostTest") && assessment.LinkedGroupId.HasValue)
{
    var allGroupSessions = await _context.AssessmentSessions
        .Where(a => a.LinkedGroupId == assessment.LinkedGroupId).ToListAsync();
    // shared-field loop :1832 ...
}
```

Test analog — HcPortal.Tests/PostLisensorPolishTests.cs:104-130 (replica-logic status-guard, parametrized Completed vs PendingGrading).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambah PendingGrading hard-block guard di POST EditAssessment (single-mode + Pre/Post)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:1790-1850 (Pre/Post branch awal + group fetch, file dimodifikasi)
    - Controllers/AssessmentAdminController.cs:1993-2060 (single-mode Completed guard + sibling fetch, file dimodifikasi)
    - Controllers/AssessmentAdminController.cs:3563-3567 (analog hard-block kanonik SubmitEssayScore)
    - Models/AssessmentConstants.cs:13-22 (konstanta PendingGrading)
  </read_first>
  <action>
    **D-01 VERIFY:** Konfirmasi POST EditAssessment TIDAK punya cabang yang memblokir `PendingGrading` (hanya `Completed && !hasAddition` di :1998). REAL gap. Juga konfirmasi semantik Phase 391 D-02: penambahan peserta murni (`hasAddition`) BOLEH meski grup tidak idle. D-02 = hard-block EDIT/soal, tapi pertahankan carve-out `!hasAddition` (mirror guard Completed) agar add-participant tak ter-regresi.

    **FIX single-mode branch** — di SEBELAH guard Completed (~:1998), tambah guard PendingGrading hard-block. Karena "ada sesi PendingGrading" bisa di sibling (bukan hanya `assessment` sendiri), gunakan `AnyAsync` atas sibling key (mirror :2055-2060) SEBELUM mutasi shared-field. Bentuk:
    ```csharp
    bool hasAddition = NewUserIds != null && NewUserIds.Count > 0;
    if (assessment.Status == AssessmentConstants.AssessmentStatus.Completed && !hasAddition)
    {
        TempData["Error"] = "Cannot edit completed assessments.";
        return RedirectToAction("ManageAssessment");
    }
    // 999.11 D-02: hard-block edit murni saat ada sesi PendingGrading (penilaian essay in-flight) — cegah korup skor.
    //             Carve-out !hasAddition mirror guard Completed (Phase 391 D-02 penambahan peserta tetap boleh).
    if (!hasAddition)
    {
        bool anyPendingGrading = await _context.AssessmentSessions.AnyAsync(a =>
            a.Title == assessment.Title && a.Category == assessment.Category
            && a.Schedule.Date == assessment.Schedule.Date
            && a.Status == AssessmentConstants.AssessmentStatus.PendingGrading);
        if (anyPendingGrading)
        {
            TempData["Error"] = "Tidak dapat mengedit assessment saat ada sesi menunggu penilaian (essay belum dinilai). Selesaikan penilaian dahulu.";
            return RedirectToAction("ManageAssessment");
        }
    }
    ```
    (Sesuaikan ekspресi key dengan `origTitle/origCategory/origScheduleDate` bila variabel itu sudah terhitung di scope ini; kalau belum, pakai `assessment.Title/Category/Schedule.Date` seperti di atas. Letakkan SEBELUM loop shared-field :2062.)

    **FIX Pre/Post branch** — di awal cabang Pre/Post (~:1810, setelah validasi PostSchedule, SEBELUM shared-field loop :1832), tambah guard analog atas grup `LinkedGroupId`:
    ```csharp
    if (!hasAddition)
    {
        bool anyPendingGradingGroup = await _context.AssessmentSessions.AnyAsync(a =>
            a.LinkedGroupId == assessment.LinkedGroupId
            && a.Status == AssessmentConstants.AssessmentStatus.PendingGrading);
        if (anyPendingGradingGroup)
        {
            TempData["Error"] = "Tidak dapat mengedit grup Pre/Post saat ada sesi menunggu penilaian.";
            return RedirectToAction("ManageAssessment");
        }
    }
    ```
    (Pastikan `hasAddition` ter-deklarasi/terjangkau di cabang Pre/Post; bila variabel didefinisikan setelah cabang ini, deklarasikan `bool hasAddition = NewUserIds != null && NewUserIds.Count > 0;` lebih awal atau evaluasi inline.)

    **WAJIB pakai konstanta** `AssessmentConstants.AssessmentStatus.PendingGrading` — JANGAN literal `"Menunggu Penilaian"`. Pesan TempData Bahasa Indonesia (CLAUDE.md). JANGAN ubah logika add-participant/shared-field existing selain menyisipkan guard di posisi yang benar.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Single-mode guard: grep "anyPendingGrading" + "AssessmentStatus.PendingGrading" di Controllers/AssessmentAdminController.cs muncul dalam POST EditAssessment single-mode (sekitar :1998-2062).
    - Pre/Post guard: grep "anyPendingGradingGroup" + "LinkedGroupId == assessment.LinkedGroupId" muncul di cabang Pre/Post (sekitar :1810-1832).
    - Carve-out: kedua guard dibungkus `if (!hasAddition)` (grep konfirmasi) -> add-participant tidak ter-block.
    - Tidak ada literal "Menunggu Penilaian" baru di guard (grep: guard pakai konstanta, bukan string).
    - dotnet build HcPortal.csproj -> 0 error.
  </acceptance_criteria>
  <done>Guard PendingGrading hard-block ditambahkan di single-mode + Pre/Post branch POST EditAssessment, pakai konstanta, dengan carve-out !hasAddition; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Edit-guard test + regresi fast suite + 0-migration gate</name>
  <files>HcPortal.Tests/PostLisensorPolishTests.cs</files>
  <read_first>
    - HcPortal.Tests/PostLisensorPolishTests.cs:100-135 (analog replica-logic status-guard, file dimodifikasi)
    - HcPortal.Tests/FlexibleParticipantAddTests.cs (Phase 391 add-participant — pastikan tak regresi)
    - Controllers/AssessmentAdminController.cs:1993-2062 (guard baru yang akan di-test)
  </read_first>
  <action>
    **TEST edit-guard** — tambah xUnit ke `HcPortal.Tests/PostLisensorPolishTests.cs` (atau file test edit-assessment yang tepat) yang mengunci perilaku guard 999.11. Mirror pola replica-logic status-guard di file (RunSubmitEssayScoreLogicAsync :123-130 — seed status berbeda lalu assert keputusan). Minimal 3 kasus (before/after, bukan tautologi):
    1. Sesi `PendingGrading` + edit murni (`hasAddition=false`) -> keputusan = BLOCK (TempData Error / RedirectToAction ManageAssessment). Seed status PendingGrading, jalankan replica guard, assert block.
    2. Sesi `PendingGrading` + penambahan peserta (`hasAddition=true`) -> keputusan = ALLOW (carve-out, mirror Phase 391 D-02). Assert tidak block.
    3. Sesi `Open`/`Upcoming` (idle) + edit murni -> keputusan = ALLOW (tidak ada PendingGrading -> lolos). Assert tidak block.
    Pakai `[Trait("Category","Integration")]` + fixture disposable bila menyentuh DbContext (mirror file existing). Jangan sentuh HcPortalDB_Dev.

    **D-05 verifikasi:** Cluster C = controller-logic murni -> xUnit/integration cukup, TIDAK perlu Playwright.
    (1) Fast suite `dotnet test --filter "Category!=Integration"` -> GREEN, no regresi (baseline ~390).
    (2) FlexibleParticipantAddTests (Phase 391) HARUS tetap hijau -> bukti add-participant tak ter-regresi guard baru.
    (3) **0-MIGRATION GATE:** `git diff --stat -- Migrations/ Data/` -> KOSONG. Catat di SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - Edit-guard test: 3 kasus (PendingGrading+edit->block, PendingGrading+add->allow, idle+edit->allow) ada di PostLisensorPolishTests.cs (grep "PendingGrading" muncul di test baru) dan PASS.
    - FlexibleParticipantAddTests -> failed: 0 (add-participant tak regresi).
    - dotnet test --filter "Category!=Integration" -> "Passed!" / failed: 0.
    - git diff --stat -- Migrations/ Data/ -> KOSONG. SUMMARY catat "0 migration".
  </acceptance_criteria>
  <done>Edit-guard 3-kasus test hijau; FlexibleParticipantAddTests tetap hijau; fast suite GREEN; 0-migration gate kosong.</done>
</task>

</tasks>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- `dotnet test --filter "Category!=Integration"` GREEN (baseline ~390).
- Edit-guard test (PendingGrading->block, add-only->allow, idle->allow) hijau.
- FlexibleParticipantAddTests (Phase 391) tetap hijau (no regresi add-participant).
- `git diff --stat -- Migrations/ Data/` KOSONG.
- Disposisi 999.11 terdokumentasi di SUMMARY (gap real -> fixed; carve-out terverifikasi).
</verification>

<success_criteria>
- 999.11 PendingGrading edit-guard: hard-block ditambahkan di single-mode + Pre/Post branch, pakai konstanta single-source-of-truth.
- Carve-out !hasAddition: penambahan peserta Phase 391 D-02 tetap diizinkan.
- 0 migration; tidak ada perubahan jalur grading online (hanya guard di edit-path).
</success_criteria>

<output>
After completion, create `.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-03-SUMMARY.md` mencatat disposisi 999.11 (gap real -> fixed di 2 cabang), hasil edit-guard test + FlexibleParticipantAddTests, dan 0-migration gate.
</output>
