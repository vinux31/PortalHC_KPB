---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 03
wave: 3
status: complete
requirements-completed: ["tech-debt"]
migration: false
commits:
  - "f4fbf5a1 fix(398.1-03): 999.11 PendingGrading edit-guard hard-block (Task 1)"
  - "d1e1d222 test(398.1-03): 999.11 edit-guard 3-case (Task 2)"
---

# Plan 398.1-03 SUMMARY — Cluster C (999.11 PendingGrading edit-guard)

Tutup carry tech-debt v32.0: **999.11** PendingGrading edit-guard gap di `Controllers/AssessmentAdminController.cs` POST `EditAssessment`. Verify-first (D-01).

## Disposisi

| Item | Sev | Disposisi | Bukti |
|------|-----|-----------|-------|
| **999.11** PendingGrading edit-guard gap | MED | **FIX** | Gap REAL — POST EditAssessment hanya hard-block `Completed && !hasAddition` (:1998), TIDAK blok `PendingGrading` → Admin bisa edit assessment/soal saat penilaian essay in-flight → risiko korup skor. Tambah hard-block `PendingGrading` di KEDUA cabang. |

## Implementasi (D-02 hard-block + carve-out)

- **Single-mode branch** (setelah guard Completed :2002): `if (!hasAddition)` → `AnyAsync` atas group key (`Title + Category + Schedule.Date`) untuk status `PendingGrading` → block + `TempData["Error"]` BI + redirect ManageAssessment.
- **Pre/Post branch** (setelah fetch `allGroupSessions` by `LinkedGroupId` :1826, SEBELUM shared-field loop): `hasAdditionPrePost` inline + `allGroupSessions.Any(PendingGrading)` (reuse list ter-fetch, no extra query) → block.
- **Konstanta**: pakai `AssessmentConstants.AssessmentStatus.PendingGrading` (single source of truth) — BUKAN literal `"Menunggu Penilaian"`. Mirror guard kanonik `SubmitEssayScore:3566`.
- **Carve-out `!hasAddition`**: penambahan peserta murni (Phase 391 D-02) tetap diizinkan — tidak ter-regresi.

## Verifikasi (D-05)

- **dotnet build** → 0 error.
- **Edit-guard test** (3 kasus, `PostLisensorPolishTests.cs`, replica-logic byte-identik):
  - PendingGrading + edit-only → **BLOCK** ✓
  - PendingGrading + hasAddition → **ALLOW** (carve-out) ✓
  - idle (Open) + edit-only → **ALLOW** ✓
  - Title unik per-test → isolasi shared fixture DB.
- **FlexibleParticipantAddTests** (Phase 391) → **4/4 GREEN** (add-participant tak ter-regresi guard baru).
- **Fast suite** (`Category!=Integration`) → **391/391 GREEN** (no regression).
- **0-migration gate**: `git diff Migrations/ Data/` (f4fbf5a1~1..HEAD) → **KOSONG**.
- **D-05**: Cluster C = controller-logic murni → xUnit/integration cukup, TIDAK perlu Playwright.

## Invariant intact

Jalur grading online TAK disentuh (guard hanya di edit-path, menolak edit; tak menulis Score/Status) · 0 migration · v32.2 tetap 13/13.

## Self-Check: PASSED
999.11 gap real → FIX 2 cabang dengan konstanta + carve-out terverifikasi (add-participant green). Suite green. 0 migration.
