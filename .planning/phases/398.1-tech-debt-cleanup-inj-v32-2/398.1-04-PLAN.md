---
phase: 398.1-tech-debt-cleanup-inj-v32-2
plan: 04
type: execute
wave: 4
depends_on: ["398.1-03"]
files_modified:
  - HcPortal.Tests/FlexibleParticipantAddTests.cs
  - tests/e2e/helpers/examTypes.ts
autonomous: true
requirements: ["tech-debt"]
user_setup: []

must_haves:
  truths:
    - "Test 391 (a)/(b)/(d) di-de-tautologize: seed status BERBEDA dari nilai pasca-keputusan lalu assert transformasi (before/after), bukan assert == seed"
    - "e2e helper fillEssayAnswer tidak lagi memanggil hub.invoke('SaveTextAnswer') langsung — mengandalkan product flushEssay (Phase 385) saat submit"
    - "Kode produk TIDAK diubah (Views/CMP/StartExam.cshtml utuh; essay-flush-385 tetap 3/3 + xUnit 557/0)"
    - "dotnet build 0 error; fast suite GREEN; FlexibleParticipantAddTests hijau (non-tautologis)"
    - "git diff Migrations/ Data/ kosong (0 migration); git diff hanya menyentuh file test"
  artifacts:
    - path: "HcPortal.Tests/FlexibleParticipantAddTests.cs"
      provides: "tests (a)/(b)/(d) rewrite: before/after transformation assert"
      contains: "DeriveReadyStatus"
    - path: "tests/e2e/helpers/examTypes.ts"
      provides: "fillEssayAnswer tanpa direct hub.invoke (rely product flush)"
      contains: "fillEssayAnswer"
  key_links:
    - from: "tests/e2e/helpers/examTypes.ts fillEssayAnswer"
      to: "Views/CMP/StartExam.cshtml flushEssay() (product path)"
      via: "submit triggers product flush (no re-implemented SignalR)"
      pattern: "dispatchEvent.*input"
---

<objective>
Cluster D (test-only) — bersihkan 2 tech-debt kualitas-test: **999.12** de-tautologize regression test Phase 391, dan **999.13** perbaiki e2e helper essay-submit yang flaky.

Dua item: (1) 999.12 — `FlexibleParticipantAddTests.cs` tests (a)/(b)/(d) saat ini seed-then-assert-seed (tautologi: assert == fungsi yang baru dipakai untuk seed) -> rewrite jadi before/after transformation assert (mirror test (c) yang non-tautologis). (2) 999.13 — `examTypes.ts:fillEssayAnswer` memanggil `hub.invoke('SaveTextAnswer')` LANGSUNG (re-implement product save -> race/flaky) -> rewrite agar mengandalkan product `flushEssay()` (Phase 385) yang dipicu submit.

Purpose: kualitas test (anti-tautologi + anti-flaky) tanpa menyentuh kode produk. D-01: konfirmasi tiap item real DULU. **JANGAN ubah kode produk** (essay-flush-385 sudah 3/3 + xUnit 557/0 hijau — 999.13 = perbaiki HELPER saja).

Output: 2 file test ter-update; kode produk utuh; suite hijau. 0 migration; git diff hanya file test. v32.2 tetap 13/13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-CONTEXT.md
@.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Kontrak aktual. JANGAN eksplorasi ulang. -->

999.12 — Tautological tests, HcPortal.Tests/FlexibleParticipantAddTests.cs:
- (a) :123-144 AddParticipant_WithInProgressSibling_CreatesNewSession — seed dgn DeriveReadyStatus(...) lalu assert sesi EXISTS (assert seed jalan, bukan keputusan controller).
- (b) :147-169 AddParticipant_NewSession_HasReadyStatus_NotInProgress — seed dgn DeriveReadyStatus(past/future) lalu assert status == DeriveReadyStatus output. PURE TAUTOLOGI.
- (d) :206-230 AddParticipant_SomeCompleted_NotBlocked_WhileWindowOpen — seed lalu assert exists; satu-satunya assert real = Assert.True(WindowAllowsAddition(null)) (trivially true).

Gold-standard NON-tautologis di FILE SAMA — test (c) :171-203 AddParticipant_InProgressSibling_StatusScheduleDurationUnchanged:
```csharp
// seed KNOWN-different state (InProgress dur=60, Upcoming) lalu run replica (ApplySharedFieldUpdate) lalu assert OUTPUT beda dari seed:
await SeedSiblingSessionAsync(seed, uRunning, ..., S.InProgress, startedAt: DateTime.UtcNow, completedAt: null);
await SeedSiblingSessionAsync(seed, uNotStarted, ..., S.Upcoming, startedAt: null, completedAt: null);
ApplySharedFieldUpdate(siblings, title, cat, newSchedule, 120, S.Open);   // run replica-of-controller
Assert.Equal(S.InProgress, running.Status); Assert.Equal(60, running.DurationMinutes);   // protected stays
Assert.Equal(120, notStarted.DurationMinutes); Assert.Equal(newSchedule, notStarted.Schedule);   // unprotected changes
```
Helper tersedia: SeedSiblingSessionAsync, DeriveReadyStatus(schedule, completedAt), ApplySharedFieldUpdate, WindowAllowsAddition. Fixture disposable HcPortalDB_Test_{guid} (:20-53) — JANGAN ubah.

999.13 — Flaky helper, tests/e2e/helpers/examTypes.ts:392-434 (fillEssayAnswer):
```typescript
await textarea.fill(answer);
await textarea.evaluate((el) => { el.dispatchEvent(new Event('input', { bubbles: true })); });
// ... ekstrak qId/sessionId ...
await page.evaluate(async ({ sid, qIdNum, text }) => {
    const hub = w.assessmentHub;
    if (!hub || hub.state !== 'Connected') throw new Error(...);
    await hub.invoke('SaveTextAnswer', sid, qIdNum, text);   // <- DIRECT re-implement product save (flaky)
}, { sid: sessionId, qIdNum: qId, text: answer });
```

Product flush path (yang HARUS diandalkan), Views/CMP/StartExam.cshtml:
- flushEssay() :936-952 — clear debounce timers + invoke('SaveTextAnswer') untuk tiap essay, return Promise.all.
- #reviewSubmitBtn listener :1024-1031 — flushEssay() LALU tunggu hasPendingSaves() sebelum examForm.submit(). (Jaminan produk teks essay persist sebelum submit.)

Analog e2e non-flaky, tests/e2e/essay-flush-385.spec.ts:130-167:
```typescript
await page.waitForFunction(() => { const w = window as ...; return !!w.assessmentHub && w.assessmentHub.state === 'Connected'; }, undefined, { timeout: 15_000 });
const textarea = qcard.locator('textarea.exam-essay');
await textarea.evaluate((el, v) => { el.value = v; el.dispatchEvent(new Event('input', { bubbles: true })); }, ESSAY_TEXT);
// submit langsung — biarkan product flushEssay() persist sebelum submit (NO direct hub.invoke)
```
submitExamTwoStep :327+ memanggil fillEssayAnswer lalu klik #reviewSubmitBtn (memicu product flushEssay).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 999.12 de-tautologize FlexibleParticipantAddTests (a)/(b)/(d)</name>
  <files>HcPortal.Tests/FlexibleParticipantAddTests.cs</files>
  <read_first>
    - HcPortal.Tests/FlexibleParticipantAddTests.cs (seluruh file, file dimodifikasi — pahami helper + test (c) gold-standard)
    - .planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-PATTERNS.md section 9 (apply-de-tautologize guidance)
  </read_first>
  <action>
    **D-01 VERIFY:** Konfirmasi (a)/(b)/(d) tautologis — (b) seed `DeriveReadyStatus(past, null)` lalu assert `== S.Open` (yang adalah output DeriveReadyStatus yang sama) = assert fungsi-yang-baru-dipakai-untuk-seed. REAL test-debt. FIX.

    **REWRITE (b)** AddParticipant_NewSession_HasReadyStatus_NotInProgress: seed sesi dengan status "salah" yang DIWARISI induk (mis. `S.InProgress`), LALU panggil replica decision (`DeriveReadyStatus(schedule, null)`), assert hasilnya `S.Open`/`S.Upcoming` (sesuai jadwal) DAN bahwa sesi baru TIDAK mewarisi `InProgress`. Bukti before(InProgress)->after(Open/Upcoming):
    ```csharp
    // seed status "warisan induk" yang SALAH
    await SeedSiblingSessionAsync(seed, uPast, title, cat, past, S.InProgress);   // before = InProgress (wrong)
    // run replica keputusan controller
    var derived = DeriveReadyStatus(past, null);
    Assert.Equal(S.Open, derived);                 // after = Open (jadwal tiba)
    Assert.NotEqual(S.InProgress, derived);        // TIDAK warisi induk
    // (idem untuk future -> Upcoming)
    ```

    **REWRITE (a)** AddParticipant_WithInProgressSibling_CreatesNewSession: jangan sekadar assert sesi exists. Seed sibling `InProgress` (existing) + tambah sesi baru via replica, assert sesi baru dibuat dengan status siap-mulai (Open/Upcoming) SEMENTARA sibling InProgress tetap (before/after dual-assert mirror (c)). Buktikan keputusan, bukan seed.

    **REWRITE (d)** AddParticipant_SomeCompleted_NotBlocked_WhileWindowOpen: seed campuran (Completed + Upcoming), jalankan replica WindowAllowsAddition + penambahan, assert penambahan TIDAK diblok DAN sesi baru muncul siap-mulai SEMENTARA sesi Completed tidak berubah (dual-assert), bukan `Assert.True(WindowAllowsAddition(null))` yang trivially-true.

    Jaga `[Trait("Category","Integration")]` + fixture disposable HcPortalDB_Test_{guid} (:20-53) UTUH. Pakai helper existing (SeedSiblingSessionAsync, DeriveReadyStatus, ApplySharedFieldUpdate, WindowAllowsAddition). JANGAN ubah test (c) (sudah gold-standard). JANGAN sentuh kode produk.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~FlexibleParticipantAdd"</automated>
  </verify>
  <acceptance_criteria>
    - (b) rewrite: grep di FlexibleParticipantAddTests.cs menunjukkan seed `S.InProgress` lalu assert `derived` (DeriveReadyStatus output) == Open/Upcoming + Assert.NotEqual InProgress (before/after, bukan assert==seed).
    - (a) + (d) rewrite: masing-masing punya MINIMAL satu dual-assert (protected stays + unprotected changes) mirror pola (c) — tidak ada lagi test yang hanya assert "sesi exists" atau Assert.True(trivial).
    - test (c) :171-203 TIDAK diubah (grep AddParticipant_InProgressSibling_StatusScheduleDurationUnchanged utuh).
    - dotnet test --filter "FullyQualifiedName~FlexibleParticipantAdd" -> failed: 0 (semua hijau, non-tautologis).
  </acceptance_criteria>
  <done>Tests (a)/(b)/(d) di-rewrite jadi before/after transformation assert (mirror (c)); fixture utuh; suite FlexibleParticipantAdd hijau; kode produk tak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 2: 999.13 fix e2e helper fillEssayAnswer (rely product flush, NOT direct hub.invoke)</name>
  <files>tests/e2e/helpers/examTypes.ts</files>
  <read_first>
    - tests/e2e/helpers/examTypes.ts:380-434 (fillEssayAnswer, file dimodifikasi)
    - tests/e2e/helpers/examTypes.ts:320-360 (submitExamTwoStep — pemanggil fillEssayAnswer, pahami alur submit)
    - tests/e2e/essay-flush-385.spec.ts:124-167 (analog non-flaky: wait Connected -> fill+input -> submit -> product flush persist)
    - Views/CMP/StartExam.cshtml (HANYA BACA — flushEssay :936-952 + #reviewSubmitBtn :1024-1031; JANGAN ubah)
  </read_first>
  <action>
    **D-01 VERIFY:** Konfirmasi `fillEssayAnswer` (:421-433) me-`page.evaluate(... hub.invoke('SaveTextAnswer'))` langsung — re-implement product save, balapan dengan flushEssay produk -> flaky. REAL. FIX HELPER (bukan produk).

    **REWRITE fillEssayAnswer** (`tests/e2e/helpers/examTypes.ts:392-434`) agar:
    1. **Tunggu hub Connected** sebelum apa pun (flushEssay best-effort skip jika belum Connected) — mirror analog :130-134:
    ```typescript
    await page.waitForFunction(() => {
        const w = window as unknown as { assessmentHub?: { state?: string } };
        return !!w.assessmentHub && w.assessmentHub.state === 'Connected';
    }, undefined, { timeout: 15_000 });
    ```
    2. **Isi textarea + dispatch 'input'** (simulasi ketik — biarkan product debounce/blur + flushEssay yang persist). Pertahankan blok fill+input existing (:394-398).
    3. **HAPUS** seluruh blok ekstрак sessionId + `page.evaluate(async ... hub.invoke('SaveTextAnswer', ...))` (:415-433). Tidak ada lagi direct SignalR invoke di helper. Persist dijamin produk `flushEssay()` saat `#reviewSubmitBtn` di-klik di `submitExamTwoStep`.
    4. (Opsional, jika perlu) tambahkan komentar yang menjelaskan kenapa rely product flush (referensi essay-flush-385 + StartExam.cshtml:1024-1031), supaya tidak di-"perbaiki" balik ke direct invoke.

    Pastikan `submitExamTwoStep` (pemanggil) tetap mengklik `#reviewSubmitBtn` (memicu flushEssay+hasPendingSaves sebelum submit). JANGAN ubah signature `fillEssayAnswer(page, qCard, answer)`. **JANGAN ubah `Views/CMP/StartExam.cshtml` atau `AssessmentHub.cs`** (kode produk — essay-flush-385 3/3 + xUnit 557/0 hijau).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && git diff --name-only -- Views/ Services/ Controllers/ Hubs/</automated>
  </verify>
  <acceptance_criteria>
    - HAPUS direct invoke: grep "hub.invoke('SaveTextAnswer'" di tests/e2e/helpers/examTypes.ts (di dalam fillEssayAnswer) = 0 hit.
    - WAIT Connected: grep "assessmentHub.state === 'Connected'" / "waitForFunction" muncul di fillEssayAnswer.
    - fill+input dipertahankan: grep "dispatchEvent(new Event('input'" muncul di fillEssayAnswer.
    - Kode produk UTUH: `git diff --name-only -- Views/ Services/ Controllers/ Hubs/` -> output KOSONG (helper fix tidak menyentuh produk).
    - signature fillEssayAnswer(page, qCard, answer) tidak berubah (grep konfirmasi).
  </acceptance_criteria>
  <done>fillEssayAnswer di-rewrite: wait-Connected + fill+input, direct hub.invoke dihapus, rely product flushEssay; kode produk tak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 3: Verifikasi e2e essay flow + regresi + 0-migration gate</name>
  <files>tests/e2e/helpers/examTypes.ts, tests/e2e/essay-flush-385.spec.ts</files>
  <read_first>
    - tests/e2e/essay-flush-385.spec.ts (baseline essay flush yang harus tetap hijau)
    - .planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-CONTEXT.md (D-05 + catatan 999.13 backlog)
    - CLAUDE.md Develop Workflow (port lokal main tree 5277, AD-off lokal)
  </read_first>
  <action>
    **D-05 verifikasi:**
    (1) **Fast suite** `dotnet test --filter "Category!=Integration"` -> GREEN (baseline ~390); FlexibleParticipantAdd hijau (999.12).
    (2) **e2e essay** — jalankan spec yang memakai helper `fillEssayAnswer`/`submitExamTwoStep` (mis. exam-types Flow L / exam-taking Flow K + essay-flush-385) dari MAIN working tree (build+run port 5277, AD-off `Authentication__UseActiveDirectory=false`). Assert essay-submit TIDAK lagi flaky (teks persist via product flush sebelum submit; ExamSummary unanswered=0). essay-flush-385 HARUS tetap 3/3.
       Jika spec essay-submit downstream tetap flaky karena sebab di luar helper (lingkungan SignalR lokal), catat sisa di SUMMARY + backlog 999.13 (sesuai catatan STATE) — TIDAK ubah produk. Target utama plan ini: helper tidak lagi re-implement product save.
    (3) **0-MIGRATION GATE:** `git diff --stat -- Migrations/ Data/` -> KOSONG.
    (4) **Test-only gate:** `git diff --name-only` untuk Cluster D HANYA menyentuh `HcPortal.Tests/FlexibleParticipantAddTests.cs` + `tests/e2e/helpers/examTypes.ts` (tidak ada file produk). Catat di SUMMARY.
    Konsolidasikan disposisi 2 item Cluster D + status keseluruhan Phase 398.1 (semua item 4 cluster: fixed/dropped + bukti) di SUMMARY untuk re-audit milestone.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "Category!=Integration"</automated>
  </verify>
  <acceptance_criteria>
    - dotnet test --filter "Category!=Integration" -> "Passed!" / failed: 0 (FlexibleParticipantAdd hijau).
    - e2e: essay-flush-385 tetap 3/3 PASS; spec essay-submit yang pakai helper baru berjalan via product flush (atau sisa flaky dicatat backlog 999.13 + bukti non-produk).
    - git diff --stat -- Migrations/ Data/ -> KOSONG.
    - Cluster D test-only: git diff --name-only hanya FlexibleParticipantAddTests.cs + examTypes.ts (tak ada file produk).
    - SUMMARY mencatat disposisi 999.12 + 999.13 dan ringkasan status seluruh Phase 398.1 (4 cluster) untuk re-audit.
  </acceptance_criteria>
  <done>Fast suite + e2e essay hijau (essay-flush-385 3/3); helper tak re-implement save; 0-migration + test-only gate; SUMMARY ringkas status seluruh phase.</done>
</task>

</tasks>

<verification>
- `dotnet test --filter "Category!=Integration"` GREEN (baseline ~390); FlexibleParticipantAdd hijau (non-tautologis).
- e2e essay-flush-385 tetap 3/3; helper fillEssayAnswer rely product flush.
- Kode produk UTUH (`git diff --name-only -- Views/ Services/ Controllers/ Hubs/` kosong).
- `git diff --stat -- Migrations/ Data/` KOSONG.
- Disposisi 999.12 + 999.13 terdokumentasi di SUMMARY + ringkasan status seluruh Phase 398.1.
</verification>

<success_criteria>
- 999.12: tests (a)/(b)/(d) de-tautologized jadi before/after transformation assert (mirror (c)); fixture utuh.
- 999.13: fillEssayAnswer rely product flushEssay (no direct hub.invoke); kode produk tak tersentuh.
- 0 migration; Cluster D test-only (tidak ada perubahan file produk).
</success_criteria>

<output>
After completion, create `.planning/phases/398.1-tech-debt-cleanup-inj-v32-2/398.1-04-SUMMARY.md` mencatat disposisi 999.12 + 999.13, hasil suite + e2e, gate test-only + 0-migration, dan RINGKASAN status seluruh Phase 398.1 (4 cluster, tiap item fixed/dropped + bukti) untuk re-audit milestone v32.2.
</output>
