---
phase: 399-foundation-junction-userunits-primary-mirror-multi-select-ui-display
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Models/UserUnit.cs
  - Data/ApplicationDbContext.cs
  - Migrations/AddUserUnitsTable.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
  - HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs
  - HcPortal.Tests/UserUnitsWriteThroughTests.cs
  - HcPortal.Tests/PrimaryMirrorTests.cs
  - HcPortal.Tests/UserUnitsAuditDiffTests.cs
  - HcPortal.Tests/ImportMultiUnitParseTests.cs
  - HcPortal.Tests/UnitInSectionValidationTests.cs
  - HcPortal.Tests/RemoveUnitGuardTests.cs
autonomous: false
requirements: [MU-05]
must_haves:
  truths:
    - "Tabel UserUnits ada di DB lokal SQLEXPRESS setelah dotnet ef database update"
    - "Setiap pekerja existing dengan Unit non-null punya tepat 1 baris UserUnits IsPrimary=1 pasca-backfill"
    - "Pekerja dengan Unit null punya 0 baris UserUnits"
    - "Backfill idempotent: re-run migration/SQL tidak membuat baris duplikat"
    - "Filtered-unique index IX_UserUnits_UserId_PrimaryUnique ada di DB (cegah 2 primary/user)"
  artifacts:
    - path: "Models/UserUnit.cs"
      provides: "Entity junction UserUnit (Id, UserId FK, Unit name, IsPrimary, IsActive, nav User)"
      contains: "public bool IsPrimary"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet<UserUnit> + filtered-unique config"
      contains: "DbSet<UserUnit> UserUnits"
    - path: "Migrations/AddUserUnitsTable.cs"
      provides: "CreateTable + filtered index + backfill SQL idempotent"
      contains: "IX_UserUnits_UserId_PrimaryUnique"
  key_links:
    - from: "Migrations/AddUserUnitsTable.cs"
      to: "Users table"
      via: "FK_UserUnits_Users_UserId ON DELETE CASCADE + backfill INSERT...SELECT WHERE NOT EXISTS"
      pattern: "FK_UserUnits_Users_UserId"
    - from: "Data/ApplicationDbContext.cs"
      to: "UserUnit entity"
      via: "HasFilter [IsPrimary] = 1"
      pattern: "HasFilter\\(\"\\[IsPrimary\\] = 1\"\\)"
---

<objective>
Bangun FONDASI multi-unit: tabel junction `UserUnits` (model + DbSet + index filtered-unique primary), migration `AddUserUnitsTable` dengan backfill 1 primary-row/pekerja existing, dan SCAFFOLD test Wave 0 (file test kosong/RED untuk seluruh logic MU-01..07 yang akan diisi plan berikutnya). Wave 0 SOLO — seluruh milestone v32.3 (fase 399-404) berdiri di atas junction ini.

Purpose: Junction `UserUnits` + kontrak primary-mirror dipakai SEMUA fase berikutnya. migration=TRUE (satu-satunya migration di milestone). Filtered-unique index meng-enforce invariant #3 (tepat 1 primary/user) di DB-level.
Output: `Models/UserUnit.cs`, DbSet + config di `ApplicationDbContext.cs`, file migration `AddUserUnitsTable` (applied ke DB lokal), snapshot terupdate, dan 7 file test scaffold (Wave 0 gaps dari VALIDATION.md).

**⚠️ BLOCKING + autonomous:false:** `dotnet ef` CLI global = v10.0.3 sementara project = net8.0 / EF Core 8.0.0. Mismatch major dapat menggagalkan `dotnet ef migrations add`. Task 1 punya jalur fallback eksplisit — bila intervensi interaktif (pin tool lokal / hand-author) diperlukan, checkpoint ada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-RESEARCH.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-PATTERNS.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-VALIDATION.md

<interfaces>
<!-- Kontrak yang dibuat plan ini, dikonsumsi plan 02/03/04. Executor pakai langsung — tidak perlu eksplorasi. -->

Entity baru — Models/UserUnit.cs:
```csharp
public class UserUnit {
    public int Id { get; set; }
    public string UserId { get; set; } = "";   // FK → AspNetUsers.Id (Users.Id), ON DELETE CASCADE
    public string Unit { get; set; } = "";       // NAME-string, anak dari Section pekerja; [MaxLength(200)]
    public bool IsPrimary { get; set; } = false;
    public bool IsActive { get; set; } = true;
    public ApplicationUser? User { get; set; }
}
```

DbSet baru — Data/ApplicationDbContext.cs:
```csharp
public DbSet<UserUnit> UserUnits { get; set; }
```

Analog filtered-unique index existing (SALIN VERBATIM — ApplicationDbContext.cs:327-335 CoachCoacheeMapping, :225-229 NomorSertifikat):
```csharp
entity.HasIndex(m => m.CoacheeId).HasFilter("[IsActive] = 1").IsUnique()
      .HasDatabaseName("IX_CoachCoacheeMappings_CoacheeId_ActiveUnique");
```

Primitif validasi/cascade (sudah ada — ApplicationDbContext.cs:106-133, dipakai plan 02/03):
```csharp
Task<List<string>> GetUnitsForSectionAsync(string? section);   // unit anak 1 Bagian
Task<Dictionary<string,List<string>>> GetSectionUnitsDictAsync(); // dict Bagian→unit
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Scaffold migration AddUserUnitsTable (EF CLI dengan fallback versi-mismatch)</name>
  <files>Migrations/AddUserUnitsTable.cs, Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <read_first>
    - .planning/phases/399-.../399-RESEARCH.md (§Pitfall 1 + §Environment Availability — dotnet ef 10.0.3 vs project EF 8.0.0)
    - .planning/phases/399-.../399-PATTERNS.md (§Migrations/<ts>_AddUserUnitsTable.cs — analog AddOrganizationLevelLabel.cs:14-40 + code-to-write)
    - Migrations/20260603012335_AddOrganizationLevelLabel.cs (analog CreateTable + CreateIndex unique)
    - Migrations/20260308065109_AddAssignmentFieldsAndUniqueConstraint.cs (analog migrationBuilder.Sql + filtered CreateIndex)
  </read_first>
  <action>
PRASYARAT: Task 2 (model UserUnit) + Task 3 (DbSet + config) HARUS sudah ditulis sebelum scaffold CLI, karena `dotnet ef migrations add` membaca model. URUTAN EKSEKUSI plan ini: tulis Task 2 + Task 3 dulu (Models/UserUnit.cs + ApplicationDbContext.cs DbSet+config), `dotnet build` 0 error, BARU jalankan Task 1 scaffold. (Task dinomori per dependensi fondasi tapi dieksekusi 2→3→1→4.)

Jalankan migration scaffold dari root project dengan jalur fallback berlapis:

(a) COBA CLI global dulu:
  `dotnet ef migrations add AddUserUnitsTable --project HcPortal.csproj`
  Jika sukses tanpa error/warn versi → lanjut verifikasi snapshot (lihat acceptance).

(b) Jika gagal dengan "The Entity Framework tools version '10.x' is ... than the runtime version '8.0.0'" ATAU snapshot tidak ter-update → PIN TOOL LOKAL v8:
  `dotnet new tool-manifest` (jika belum ada .config/dotnet-tools.json)
  `dotnet tool install dotnet-ef --version 8.0.* --local`
  `dotnet tool run dotnet-ef migrations add AddUserUnitsTable --project HcPortal.csproj`

(c) Jika kedua jalur CLI gagal → HAND-AUTHOR (fallback aman, no CLI):
  - Tulis manual `Migrations/<timestamp>_AddUserUnitsTable.cs` dari §PATTERNS code-to-write (CreateTable UserUnits + 3 index + backfill SQL — lihat Task 4).
  - Tulis `<timestamp>_AddUserUnitsTable.Designer.cs` (salin struktur Designer dari migration existing terbaru).
  - Update `Migrations/ApplicationDbContextModelSnapshot.cs`: tambah entri entity `UserUnit` (salin pola entri `CoachCoacheeMapping` di snapshot + relasi FK + 3 index). Gunakan timestamp format `yyyyMMddHHmmss`.

Setelah file migration ada (jalur mana pun), JANGAN apply dulu — Task 4 mengisi konten Up/Down + backfill SEBELUM `dotnet ef database update`.

CATATAN: ini checkpoint:human-action HANYA bila jalur (a)+(b) gagal dan hand-author butuh konfirmasi. Bila (a) atau (b) sukses otomatis, lewati pause — lapor metode yang berhasil di SUMMARY.
  </action>
  <acceptance_criteria>
    - File `Migrations/*_AddUserUnitsTable.cs` ada (timestamp prefix)
    - File `Migrations/*_AddUserUnitsTable.Designer.cs` ada
    - `Migrations/ApplicationDbContextModelSnapshot.cs` memuat string "UserUnit" (entity ter-registrasi di snapshot)
    - `dotnet build` exits 0
    - SUMMARY mencatat metode yang dipakai: (a) CLI global / (b) pin tool lokal / (c) hand-author
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <done>File migration + Designer + snapshot ter-update memuat entity UserUnit; `dotnet build` 0 error; metode scaffold tercatat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model UserUnit + Task 3 DbContext DbSet & filtered-unique config</name>
  <files>Models/UserUnit.cs, Data/ApplicationDbContext.cs</files>
  <read_first>
    - Models/CoachCoacheeMapping.cs (analog exact — int Id, string FK-id, string Unit-name, bool IsActive)
    - Models/ApplicationUser.cs (nav target; .Unit :33 scalar dipertahankan = mirror)
    - Data/ApplicationDbContext.cs (:32 DbSet area; :327-335 CoachCoacheeMapping config; :225-229 NomorSertifikat filtered-unique; :188-193 cascade FK idiom)
    - .planning/phases/399-.../399-PATTERNS.md (§Models/UserUnit.cs + §Data/ApplicationDbContext.cs code-to-write)
  </read_first>
  <behavior>
    - UserUnit entity meng-expose: Id(int), UserId(string), Unit(string [MaxLength(200)]), IsPrimary(bool default false), IsActive(bool default true), User(ApplicationUser? nav)
    - ApplicationDbContext.UserUnits DbSet ter-registrasi
    - OnModelCreating config: FK cascade + HasIndex(UserId) + filtered-unique (UserId) WHERE [IsPrimary]=1 + unique (UserId,Unit)
  </behavior>
  <action>
Buat `Models/UserUnit.cs` PERSIS dari §PATTERNS code-to-write:
```csharp
using System.ComponentModel.DataAnnotations;
namespace HcPortal.Models
{
    public class UserUnit
    {
        public int Id { get; set; }
        public string UserId { get; set; } = "";   // FK → Users.Id, ON DELETE CASCADE
        [MaxLength(200)]
        public string Unit { get; set; } = "";       // NAME-string, anak Section pekerja
        public bool IsPrimary { get; set; } = false;
        public bool IsActive { get; set; } = true;
        public ApplicationUser? User { get; set; }
    }
}
```
`[MaxLength(200)]` WAJIB pada `Unit` karena index opsional `(UserId, Unit)` dipasang — SQL Server tolak `nvarchar(max)` sebagai index key (Pitfall 2). UserId sudah nvarchar(450) (Identity key).

Di `Data/ApplicationDbContext.cs`: tambah DbSet sejajar `CoachCoacheeMappings` (:32):
```csharp
public DbSet<UserUnit> UserUnits { get; set; }
```
Di `OnModelCreating`, SETELAH block config `CoachCoacheeMapping` (~:335), tambah PERSIS:
```csharp
builder.Entity<UserUnit>(entity =>
{
    entity.HasOne(uu => uu.User).WithMany()
          .HasForeignKey(uu => uu.UserId)
          .OnDelete(DeleteBehavior.Cascade);

    entity.HasIndex(uu => uu.UserId);

    // Tepat 1 primary per user (invariant #3) — idiom identik IX_CoachCoacheeMappings_CoacheeId_ActiveUnique
    entity.HasIndex(uu => uu.UserId)
          .IsUnique()
          .HasFilter("[IsPrimary] = 1")
          .HasDatabaseName("IX_UserUnits_UserId_PrimaryUnique");

    // Cegah duplikat unit/user (rekomendasi Claude's Discretion — low-cost)
    entity.HasIndex(uu => new { uu.UserId, uu.Unit })
          .IsUnique()
          .HasDatabaseName("IX_UserUnits_UserId_Unit_Unique");
});
```
Filter literal pakai bracket+bit SQL Server `"[IsPrimary] = 1"` (mirror `"[IsActive] = 1"` :331). JANGAN tambah nav collection `ICollection<UserUnit>` di ApplicationUser (`.WithMany()` tanpa argumen sengaja — minimal touch).
  </action>
  <acceptance_criteria>
    - `Models/UserUnit.cs` memuat "public bool IsPrimary" DAN "[MaxLength(200)]"
    - `Data/ApplicationDbContext.cs` memuat "DbSet<UserUnit> UserUnits"
    - `Data/ApplicationDbContext.cs` memuat `HasFilter("[IsPrimary] = 1")`
    - `Data/ApplicationDbContext.cs` memuat "IX_UserUnits_UserId_PrimaryUnique" DAN "IX_UserUnits_UserId_Unit_Unique"
    - `Data/ApplicationDbContext.cs` memuat "OnDelete(DeleteBehavior.Cascade)" dalam block UserUnit
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <done>UserUnit entity + DbSet + filtered-unique config + (UserId,Unit) unique ter-registrasi; build 0 error. (Eksekusi SEBELUM Task 1 scaffold CLI.)</done>
</task>

<task type="auto">
  <name>Task 4: Isi migration Up/Down + backfill idempotent + apply ke DB lokal</name>
  <files>Migrations/AddUserUnitsTable.cs</files>
  <read_first>
    - .planning/phases/399-.../399-PATTERNS.md (§Migrations code-to-write — CreateTable + 3 CreateIndex + backfill Sql)
    - Migrations/20260603012335_AddOrganizationLevelLabel.cs (analog CreateTable + CreateIndex + DropTable Down)
    - Migrations/20260308065109_AddAssignmentFieldsAndUniqueConstraint.cs (analog migrationBuilder.Sql + filtered index)
    - CLAUDE.md (Develop Workflow gate: build + run + cek DB lokal sebelum commit; Seed Data Workflow: snapshot DB sebelum apply)
  </read_first>
  <action>
Isi method `Up`/`Down` file migration `AddUserUnitsTable` PERSIS dari §PATTERNS code-to-write:

Up:
1. `CreateTable("UserUnits")` — kolom: Id (int Identity 1,1), UserId (nvarchar(450), not null), Unit (nvarchar(200), maxLength 200, not null), IsPrimary (bit, not null, defaultValue false), IsActive (bit, not null, defaultValue true). PK_UserUnits(Id). FK_UserUnits_Users_UserId → Users(Id) onDelete Cascade.
2. `CreateIndex("IX_UserUnits_UserId", "UserUnits", "UserId")` — non-unique.
3. `CreateIndex("IX_UserUnits_UserId_PrimaryUnique", "UserUnits", "UserId", unique: true, filter: "[IsPrimary] = 1")` — filtered-unique primary.
4. `CreateIndex("IX_UserUnits_UserId_Unit_Unique", "UserUnits", new[]{"UserId","Unit"}, unique: true)`.
5. BACKFILL idempotent — `migrationBuilder.Sql`:
```sql
INSERT INTO UserUnits (UserId, Unit, IsPrimary, IsActive)
SELECT u.Id, u.Unit, 1, 1
FROM Users u
WHERE u.Unit IS NOT NULL AND u.Unit <> ''
  AND NOT EXISTS (SELECT 1 FROM UserUnits uu WHERE uu.UserId = u.Id)
```
`WHERE NOT EXISTS` = idempotent (re-run tidak dobel). Pekerja Unit null/empty → 0 baris.

Down: `DropTable("UserUnits")`.

LALU apply ke DB lokal SQLEXPRESS (CLAUDE.md: snapshot DB dulu via Seed Data Workflow bila perlu):
- `dotnet ef database update` (atau `dotnet tool run dotnet-ef database update` bila pakai tool lokal v8).
- Verifikasi DB lokal: hitung baris UserUnits, pastikan tiap user Unit-non-null punya 1 baris IsPrimary=1, user Unit-null punya 0 baris. Contoh cek SQL:
  `SELECT COUNT(*) FROM UserUnits;`
  `SELECT (SELECT COUNT(*) FROM Users WHERE Unit IS NOT NULL AND Unit<>'') AS expected, (SELECT COUNT(*) FROM UserUnits WHERE IsPrimary=1) AS actual;` → expected == actual.
- Re-run idempotency manual: jalankan ulang statement INSERT backfill via sqlcmd → 0 baris baru.

JANGAN edit DB Dev/Prod (CLAUDE.md). Hanya DB lokal.
  </action>
  <acceptance_criteria>
    - File migration memuat "CreateTable" + "UserUnits" + "FK_UserUnits_Users_UserId" + "ReferentialAction.Cascade"
    - File migration memuat `filter: "[IsPrimary] = 1"` pada CreateIndex
    - File migration memuat backfill SQL dengan "NOT EXISTS" (idempotent)
    - File migration Down memuat `DropTable(name: "UserUnits")`
    - `dotnet ef database update` (atau tool lokal) sukses tanpa error apply
    - DB lokal: COUNT UserUnits IsPrimary=1 == COUNT Users Unit-non-null
    - Re-run backfill SQL → 0 baris baru (idempotent terverifikasi)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <done>Migration applied ke DB lokal; tabel UserUnits + 3 index ada; backfill 1 primary-row/pekerja Unit-non-null terverifikasi DB; idempotency terverifikasi (re-run 0 dobel).</done>
</task>

<task type="auto">
  <name>Task 5: Scaffold 7 file test Wave 0 (RED) — logic + backfill integration</name>
  <files>HcPortal.Tests/UserUnitsWriteThroughTests.cs, HcPortal.Tests/PrimaryMirrorTests.cs, HcPortal.Tests/UserUnitsAuditDiffTests.cs, HcPortal.Tests/ImportMultiUnitParseTests.cs, HcPortal.Tests/UnitInSectionValidationTests.cs, HcPortal.Tests/RemoveUnitGuardTests.cs, HcPortal.Tests/UserUnitsBackfillIntegrationTests.cs</files>
  <read_first>
    - HcPortal.Tests/WorkerDataServiceSearchTests.cs (analog InMemory pattern :21-24 UseInMemoryDatabase(Guid))
    - HcPortal.Tests/OrgLabelMigrationIntegrationTests.cs (analog SQL-riil fixture :24-48 disposable HcPortalDB_Test_<guid> + [Trait("Category","Integration")])
    - .planning/phases/399-.../399-VALIDATION.md (§Wave 0 Requirements + §Phase Requirements → Test Map — daftar 7 file + behavior)
    - .planning/phases/399-.../399-RESEARCH.md (§Validation Architecture §Wave 0 Gaps)
  </read_first>
  <action>
Buat 7 file test SCAFFOLD di `HcPortal.Tests/` (namespace `HcPortal.Tests`). Tiap file punya 1+ `[Fact]` RED yang menamai behavior tepat (test akan diisi/diaktifkan plan 02 saat helper SyncUserUnitsAsync + parse + guard ada). Wave 0 = scaffold compileable yang mendefinisikan KONTRAK test; gunakan `Assert.True(false, "Wave 0 scaffold — diisi plan 02")` atau `[Fact(Skip="Wave 0 — diisi plan 02-...")]` agar suite tetap hijau tapi kontrak tercatat. UTAMAKAN skip-with-reason (suite existing 351 tetap hijau; bukan red gate).

File + behavior (dari VALIDATION.md Test Map):
1. `UserUnitsWriteThroughTests.cs` — MU-01/02: set unit ter-persist, tepat 1 IsPrimary, mirror `ApplicationUser.Unit` == primary. InMemory.
2. `PrimaryMirrorTests.cs` — MU-02: hapus primary → promote unit lain; kosongkan semua → Unit=null + 0 baris IsPrimary. InMemory.
3. `UserUnitsAuditDiffTests.cs` — MU-02: set-diff hasilkan entri "Unit +'X'" / "Unit -'Y'" / "Primary: 'A' → 'B'". InMemory.
4. `ImportMultiUnitParseTests.cs` — MU-04: parse "UnitA|UnitB" split+trim+dedup first=primary; backward-compat "UnitA" (no pipe) → 1 elemen primary. (pure-logic, no DB).
5. `UnitInSectionValidationTests.cs` — MU-05: unit asing (di luar unit-Bagian) ditolak; unit valid diterima. InMemory + GetUnitsForSectionAsync.
6. `RemoveUnitGuardTests.cs` — MU-07: hapus unit dgn ProtonTrackAssignment aktif → BLOCK; dgn CoachCoacheeMapping aktif (no PTA) → confirm→auto-deactivate. InMemory.
7. `UserUnitsBackfillIntegrationTests.cs` — MU-05 backfill idempotent (`[Trait("Category","Integration")]`, pola OrgLabelMigrationFixture disposable HcPortalDB_Test_<guid> + MigrateAsync; run dua kali → no dup). Ini SQL-riil; boleh skip bila SQLEXPRESS tak tersedia di runner via Skip-condition, tapi WAJIB compile.

Tiap file harus build hijau dan ter-discover oleh xUnit. CATAT di SUMMARY: file mana yang Skip vs aktif.
  </action>
  <acceptance_criteria>
    - 7 file ada di `HcPortal.Tests/` dengan namespace "HcPortal.Tests"
    - `UserUnitsBackfillIntegrationTests.cs` memuat `[Trait("Category", "Integration")]`
    - `dotnet build HcPortal.Tests` exits 0 (semua scaffold compile)
    - `dotnet test HcPortal.Tests --filter "Category!=Integration"` exits 0 (suite existing tidak regresi; scaffold skip/hijau)
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "Category!=Integration"</automated>
  </verify>
  <done>7 file test scaffold compile + ter-discover; suite existing tetap hijau; kontrak test Wave 0 tercatat untuk diisi plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration `Up` SQL → DB | Backfill statement literal statik (tanpa input pengguna) menyentuh tabel Users + UserUnits |
| EF model → DB schema | Filtered-unique index meng-enforce invariant integritas (1 primary/user) di DB-level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-399-01-01 | Tampering | Backfill raw SQL (`migrationBuilder.Sql`) | mitigate | Statement literal statik, no string-concat input pengguna; `WHERE NOT EXISTS` idempotent (re-run tak dobel). Verifikasi DB lokal count == expected. |
| T-399-01-02 | Tampering (integrity) | Mirror desync (Unit ≠ IsPrimary) sejak awal | mitigate | Filtered-unique index `[IsPrimary]=1` di DB cegah 2-primary; backfill set IsPrimary=1 tunggal/pekerja. Enforce SQL-riil resmi Phase 404 (InMemory tak enforce filter — Pitfall 3). |
| T-399-01-03 | Denial of Service | Index `(UserId,Unit)` pada kolom nvarchar(max) gagal apply | mitigate | `[MaxLength(200)]` + `nvarchar(200)` (Pitfall 2) — kolom index-able; migration apply terverifikasi DB lokal. |
| T-399-01-04 | Repudiation | — (audit-log = plan 02) | accept | Plan 01 tidak ada mutasi user-driven; audit set-diff di plan 02 (write-through path). |
</threat_model>

<verification>
- `dotnet build` 0 error (HcPortal + HcPortal.Tests).
- `dotnet ef database update` applied DB lokal SQLEXPRESS; tabel UserUnits + 3 index ada.
- Backfill: COUNT(UserUnits IsPrimary=1) == COUNT(Users Unit-non-null); user Unit-null = 0 baris; re-run idempotent.
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau (suite existing tak regresi + scaffold).
- CLAUDE.md gate: migration=TRUE → SUMMARY catat untuk notify IT (commit hash + flag).
</verification>

<success_criteria>
- Tabel junction `UserUnits` ada di DB lokal dengan filtered-unique primary index.
- Setiap pekerja existing Unit-non-null punya tepat 1 baris UserUnits IsPrimary=1 (backfill); Unit-null → 0 baris; idempotent.
- Model + DbSet + config compile; snapshot ter-update; metode scaffold tercatat (CLI/pin/manual).
- 7 file test Wave 0 scaffold compile + ter-discover; suite existing hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-01-SUMMARY.md`. Catat: metode scaffold migration (a/b/c), hasil verifikasi backfill DB lokal (count), flag migration=TRUE untuk notify IT, daftar file test scaffold (skip vs aktif).
</output>
