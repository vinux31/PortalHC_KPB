---
phase: 399-foundation-junction-userunits-primary-mirror-multi-select-ui-display
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - wwwroot/js/shared-cascade.js
  - Views/Admin/CreateWorker.cshtml
  - Views/Admin/EditWorker.cshtml
  - tests/e2e/multiunit-widget-399.spec.ts
autonomous: false
requirements: [MU-01, MU-02]
must_haves:
  truths:
    - "Pilih Bagian → checkbox-list semua unit Bagian itu ter-render (cascade, no AJAX)"
    - "Centang unit → radio 'Primary'-nya enabled; uncheck → radio clear+disable"
    - "≥1 unit dicentang tanpa primary → default primary = unit tercentang pertama (D-02)"
    - "Edit pekerja 2-unit → reopen form: kedua unit ter-centang, primary ter-pilih"
    - "0 unit dicentang = valid (tidak ada primary; submit OK)"
    - "MU-07 coach-mapping aktif → modal konfirmasi 'Ya, Hapus & Nonaktifkan' muncul; PROTON aktif → error merah blok submit"
  artifacts:
    - path: "wwwroot/js/shared-cascade.js"
      provides: "initSectionUnitMultiCascade(opts) — checkbox-list + primary radio state machine"
      contains: "function initSectionUnitMultiCascade"
    - path: "Views/Admin/CreateWorker.cshtml"
      provides: "unitMultiContainer widget + init call"
      contains: "unitMultiContainer"
    - path: "Views/Admin/EditWorker.cshtml"
      provides: "unitMultiContainer widget (pre-checked) + MU-07 confirm modal"
      contains: "initSectionUnitMultiCascade"
  key_links:
    - from: "Views/Admin/CreateWorker.cshtml + EditWorker.cshtml"
      to: "WorkerController POST (model.Units[], model.PrimaryUnit)"
      via: "name=Units checkboxes + name=PrimaryUnit radio (MVC model-binding)"
      pattern: "name=\"Units\""
    - from: "Views/Admin/EditWorker.cshtml"
      to: "WorkerController MU-07 round-trip (ViewBag.NeedConfirm)"
      via: "Bootstrap modal + hidden ConfirmedDeactivate=true"
      pattern: "ConfirmedDeactivate"
---

<objective>
Bangun widget multi-select Unit (D-01/D-03): Bagian tetap single `<select>`, Unit jadi checkbox-list + radio "Primary" per baris di dalam container ber-border, di-render client-side oleh `initSectionUnitMultiCascade(opts)` (extend `shared-cascade.js`, no AJAX). Pasang di CreateWorker + EditWorker. Tambah modal konfirmasi MU-07 (coach-mapping aktif → re-prompt; PROTON aktif → error merah). Verifikasi runtime Playwright (Lesson Phase 354: Razor dinamis grep+build tak cukup).

Purpose: MU-01 (assign >1 unit) + MU-02 (penanda primary eksplisit) lewat UI. Reuse `ViewBag.SectionUnitsJson` (dict sama, client-side). Widget mengikuti UI-SPEC §A state machine PERSIS.
Output: `initSectionUnitMultiCascade` di `shared-cascade.js`; widget + init di CreateWorker/EditWorker; MU-07 modal; spec Playwright.

**autonomous:false** — Task 4 checkpoint:human-verify (render widget + round-trip 2-unit + MU-07 modal) butuh konfirmasi visual browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-PATTERNS.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-UI-SPEC.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-CONTEXT.md

<interfaces>
<!-- Kontrak dari plan 02 (ViewModel) + plan 01 (dict). Konsumsi langsung. -->
ManageUserViewModel (plan 02): { string? Section; List<string> Units; string? PrimaryUnit; bool ConfirmedDeactivate; List<string> ImpactedMappings; ... }
ViewBag.SectionUnitsJson — JSON dict Bagian->List<unit> (WorkerController :203-204 Create / :337-338 Edit / re-render :243-244,:379-380). Sudah ada.
ViewBag.NeedConfirm (plan 02 EditWorker) — true -> tampilkan modal MU-07.
Analog cascade existing — shared-cascade.js:13-49 initSectionUnitCascade (EXTEND, jangan replace; togglePassword :56-66 tetap).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: initSectionUnitMultiCascade(opts) di shared-cascade.js (state machine UI-SPEC §A)</name>
  <files>wwwroot/js/shared-cascade.js</files>
  <read_first>
    - wwwroot/js/shared-cascade.js (:13-49 initSectionUnitCascade — EXTEND di file yang sama; :28-44 updateUnits analog render loop; :56-66 togglePassword TIDAK disentuh)
    - .planning/phases/399-.../399-PATTERNS.md (§shared-cascade.js code-to-write — fungsi lengkap)
    - .planning/phases/399-.../399-UI-SPEC.md (§A State machine tabel — 8 state; §Accessibility — role=group, label-for, disabled native)
  </read_first>
  <action>
Tambah fungsi `initSectionUnitMultiCascade(opts)` di `shared-cascade.js` (JANGAN ganti `initSectionUnitCascade`/`togglePassword`). Implementasi PERSIS dari §PATTERNS code-to-write + WIRE state machine UI-SPEC §A.

Struktur per-baris (render JS):
```javascript
row.innerHTML =
  '<input type="checkbox" name="Units" value="'+unit+'" id="uu-chk-'+i+'" class="form-check-input uu-check mt-0"'+(checked?' checked':'')+'>' +
  '<input type="radio" name="PrimaryUnit" value="'+unit+'" id="uu-prim-'+i+'" class="form-check-input uu-primary mt-0"'+(isPrim?' checked':'')+(checked?'':' disabled')+'>' +
  '<label for="uu-chk-'+i+'" class="form-check-label small flex-grow-1">'+unit+'</label>' +
  '<label for="uu-prim-'+i+'" class="form-check-label small text-success">Utama</label>';
```
WIRE event (UI-SPEC §A state machine — WAJIB semua state):
- checkbox `change` -> checked: enable radio pasangannya; bila belum ada primary tercentang di grup -> set radio ini checked (default primary = first checked, D-02). unchecked: radio pasangan di-clear + `disabled`; bila ia primary -> promote primary ke unit tercentang pertama yang tersisa (atau none).
- Bagian `change` -> reset `selected=[]; primary=''`, re-render unit Bagian baru (Edit: ganti Bagian hapus pilihan lama — units harus anak Bagian baru, invariant #1).
- No Bagian -> container placeholder muted "Pilih Bagian dahulu untuk menampilkan daftar Unit." (no rows).
- Pre-check (Edit): `opts.selectedUnits` tercentang, `opts.primaryUnit` radio ter-pilih + enabled.

A11y: container `role="group"` + `aria-label` (di view Task 2); label-for asosiasi (bukan wrap); radio disabled native attr (announced unavailable); Tab capai tiap kontrol, Space toggle checkbox, arrow gerak dalam radio group PrimaryUnit.
  </action>
  <acceptance_criteria>
    - `wwwroot/js/shared-cascade.js` memuat "function initSectionUnitMultiCascade"
    - `wwwroot/js/shared-cascade.js` masih memuat "function initSectionUnitCascade" DAN "togglePassword" (tidak dihapus)
    - File memuat `name="Units"` DAN `name="PrimaryUnit"` DAN "uu-check" DAN "uu-primary"
    - File memuat logika enable/disable radio pada checkbox change (string "disabled")
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('wwwroot/js/shared-cascade.js','utf8');if(!/function initSectionUnitMultiCascade/.test(s)||!/function initSectionUnitCascade/.test(s)||!/togglePassword/.test(s))process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>initSectionUnitMultiCascade ter-tambah di shared-cascade.js dengan state machine lengkap; fungsi existing utuh.</done>
</task>

<task type="auto">
  <name>Task 2: Widget di CreateWorker + EditWorker (DOM UI-SPEC §A) + MU-07 modal di EditWorker</name>
  <files>Views/Admin/CreateWorker.cshtml, Views/Admin/EditWorker.cshtml</files>
  <read_first>
    - Views/Admin/CreateWorker.cshtml (:119-124 single-select Unit [GANTI]; :196-203 cascade init [SWAP ke varian]; :188 tombol "Simpan Pekerja" [TETAP]; :130 card idiom)
    - Views/Admin/EditWorker.cshtml (:124-129 single-select Unit [GANTI]; init cascade [SWAP]; pre-fill Model.Units/PrimaryUnit)
    - .planning/phases/399-.../399-PATTERNS.md (§Views Multi-select widget code-to-write — DOM + init call)
    - .planning/phases/399-.../399-UI-SPEC.md (§A DOM contract + §Copywriting Contract — label/helper/error Bahasa Indonesia; §D MU-07 modal copy)
  </read_first>
  <action>
**CreateWorker.cshtml + EditWorker.cshtml** — ganti single `<select id="unitSelect">` (Create :119-124 / Edit :124-129) dengan container UI-SPEC §A (Bagian `<select id="sectionSelect">` TETAP):
```html
<div class="col-md-6">
  <label class="form-label fw-semibold">@OrgLabels.GetLabel(1) Penugasan</label>
  <div id="unitMultiContainer" class="border rounded p-2" style="max-height:220px;overflow-y:auto;"
       role="group" aria-label="Daftar @OrgLabels.GetLabel(1) penugasan">
    <span class="text-muted small">Pilih @OrgLabels.GetLabel(0) dahulu untuk menampilkan daftar @OrgLabels.GetLabel(1).</span>
  </div>
  <div class="form-text">Centang setiap @OrgLabels.GetLabel(1) tempat pekerja ini menjadi anggota. Pilih satu sebagai @OrgLabels.GetLabel(1) Utama.</div>
  <span asp-validation-for="Units" class="text-danger small"></span>
  <span asp-validation-for="PrimaryUnit" class="text-danger small"></span>
</div>
```
SWAP init script (Create :196-203 / Edit setara) ke varian:
```html
<script src="~/js/shared-cascade.js"></script>
<script>
    initSectionUnitMultiCascade({
        sectionUnits: @Html.Raw(ViewBag.SectionUnitsJson ?? "{}"),
        sectionId: 'sectionSelect', containerId: 'unitMultiContainer',
        currentSection: "@(Model.Section ?? "")",
        selectedUnits: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.Units ?? new()))),
        primaryUnit: "@(Model.PrimaryUnit ?? "")"
    });
</script>
```
Pertahankan idiom form (`form-label fw-semibold`, card `card border-0 shadow-sm`), tombol "Simpan Pekerja" + `<i class="bi bi-check-lg me-2">` (:188) TIDAK berubah. JANGAN inline `style="font-size:..."` magic (Lesson DSN-05).

**EditWorker MU-07 modal** (UI-SPEC §D — server round-trip, render bila `ViewBag.NeedConfirm == true`):
```html
@if (ViewBag.NeedConfirm == true)
{
  <div class="modal fade show" id="mu07ConfirmModal" tabindex="-1" style="display:block;" aria-modal="true" role="dialog">
    <div class="modal-dialog"><div class="modal-content">
      <div class="modal-header"><h5 class="modal-title">Konfirmasi Penghapusan @OrgLabels.GetLabel(1)</h5></div>
      <div class="modal-body">
        <p>@OrgLabels.GetLabel(1) masih dipakai pada mapping coach aktif berikut:</p>
        <ul>@foreach (var m in Model.ImpactedMappings) { <li>@m</li> }</ul>
        <p>Menghapus @OrgLabels.GetLabel(1) ini akan <strong>menonaktifkan</strong> mapping tersebut. Lanjutkan?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-light" onclick="document.getElementById('mu07ConfirmModal').remove()">Batal</button>
        <button type="submit" form="editWorkerForm" name="ConfirmedDeactivate" value="true" class="btn btn-danger">Ya, Hapus &amp; Nonaktifkan</button>
      </div>
    </div></div>
  </div>
}
```
Tombol confirm submit form Edit dengan `ConfirmedDeactivate=true` (bind ke ViewModel plan 02). Pastikan `<form id="editWorkerForm">` (tambah id bila belum ada). Error MU-07 PROTON hard-block ditampilkan via `asp-validation-summary` / `ModelState[""]` merah di atas form (sudah ada summary; verifikasi).
  </action>
  <acceptance_criteria>
    - `Views/Admin/CreateWorker.cshtml` memuat `id="unitMultiContainer"` DAN "initSectionUnitMultiCascade" DAN `role="group"`
    - `Views/Admin/CreateWorker.cshtml` TIDAK lagi memuat `id="unitSelect"` (single-select diganti)
    - `Views/Admin/EditWorker.cshtml` memuat "initSectionUnitMultiCascade" DAN "selectedUnits"
    - `Views/Admin/EditWorker.cshtml` memuat "ConfirmedDeactivate" DAN "Ya, Hapus" DAN "NeedConfirm"
    - Kedua view memuat "@OrgLabels.GetLabel(1)" untuk label Unit (tidak hardcode "Unit")
    - `dotnet build HcPortal.csproj` exits 0 (Razor compile)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <done>Widget multi-select ter-render di Create/Edit (DOM UI-SPEC §A); MU-07 modal di Edit; label via OrgLabels; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: Spec Playwright multiunit-widget-399 (test-first runtime — Lesson Phase 354)</name>
  <files>tests/e2e/multiunit-widget-399.spec.ts</files>
  <read_first>
    - tests/e2e/coachcoacheemapping-389.spec.ts (analog spec parity 14-test V-01..V-14 — pola test-first proyek)
    - .agents/skills atau reference_local_e2e_sql_env_fix (login lokal admin@pertamina.com; --workers=1; SQLBrowser + lpc)
    - .planning/phases/399-.../399-UI-SPEC.md (§A state machine tabel — turunkan assert tiap state; §Accessibility)
    - .planning/phases/399-.../399-VALIDATION.md (§Manual-Only Verifications — multi-select renders + round-trips)
  </read_first>
  <action>
Buat `tests/e2e/multiunit-widget-399.spec.ts` (Playwright, localhost:5277, login admin lokal — reference_local_e2e_sql_env_fix). Turunkan assert dari UI-SPEC §A state machine:
- W-01: buka CreateWorker, container tampil placeholder "Pilih Bagian dahulu" (no rows) saat Bagian kosong.
- W-02: pilih Bagian → checkbox-list unit Bagian itu ter-render (count == jumlah unit Bagian).
- W-03: centang unit → radio "Primary"-nya enabled (tidak disabled).
- W-04: centang unit pertama → radionya auto-checked (default primary D-02).
- W-05: uncheck unit yang jadi primary → primary promote ke unit tercentang berikutnya (atau none bila habis); radio uncheck di-disable.
- W-06: centang 2 unit, set primary ke unit kedua, submit → reopen EditWorker pekerja itu: kedua checkbox checked, radio unit kedua checked (round-trip).
- W-07: 0 unit dicentang → submit valid (tidak ada error primary).
- W-08 (a11y): container `role="group"`; tiap checkbox punya `<label for>`; radio disabled punya attr `disabled`.
- W-09 (MU-07, bila fixture tersedia): Edit pekerja dgn coach-mapping aktif, hapus unit-nya, submit → modal "Konfirmasi Penghapusan" + tombol "Ya, Hapus & Nonaktifkan" muncul.

Pakai `--workers=1`. Bila butuh seed multi-unit lokal (W-06/W-09), ikuti Seed Data Workflow (snapshot DB → seed temporary → restore; catat docs/SEED_JOURNAL.md). Spec ini DITULIS dulu (test-first); diisi/dijalankan saat run di Task 4.
  </action>
  <acceptance_criteria>
    - File `tests/e2e/multiunit-widget-399.spec.ts` ada
    - Memuat test untuk: placeholder no-Bagian, render checkbox-list, default-primary-first-checked, primary-promote-on-uncheck, round-trip 2-unit, role="group"
    - Spec referensi `initSectionUnitMultiCascade` widget via selector `#unitMultiContainer` / `name=Units` / `name=PrimaryUnit`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('tests/e2e/multiunit-widget-399.spec.ts','utf8');if(!/unitMultiContainer/.test(s)||!/PrimaryUnit/.test(s))process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>Spec Playwright multiunit-widget-399 ditulis (test-first), assert turunan state machine UI-SPEC §A.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Runtime verify widget + round-trip 2-unit + MU-07 modal (browser)</name>
  <files>tests/e2e/multiunit-widget-399.spec.ts (run-only)</files>
  <what-built>
    Widget multi-select Unit (checkbox-list + radio Primary) di CreateWorker/EditWorker, di-render client-side oleh initSectionUnitMultiCascade dari ViewBag.SectionUnitsJson; modal konfirmasi MU-07 coach-mapping; spec Playwright multiunit-widget-399.
  </what-built>
  <how-to-verify>
1. `dotnet run` → buka http://localhost:5277, login admin lokal (admin@pertamina.com), ke Admin → Create Worker.
2. Pilih sebuah Bagian → konfirmasi checkbox-list semua unit Bagian itu muncul (cascade, tanpa reload).
3. Centang 2 unit → konfirmasi radio "Primary" hanya enabled untuk yang tercentang; unit pertama tercentang otomatis jadi primary (D-02).
4. Set primary ke unit kedua, isi field wajib lain, "Simpan Pekerja" → cek DB lokal: 2 baris UserUnits, 1 IsPrimary (unit kedua), Users.Unit == unit kedua.
5. Buka Edit pekerja tsb → konfirmasi kedua unit ter-centang + primary (unit kedua) ter-pilih (round-trip).
6. (Bila ada fixture coach-mapping aktif) Edit pekerja, hapus unit ber-mapping aktif, simpan → konfirmasi MODAL "Konfirmasi Penghapusan" + "Ya, Hapus & Nonaktifkan" muncul; klik → mapping nonaktif + unit terhapus.
7. (Bila ada fixture PROTON aktif) hapus unit ber-PROTON aktif → konfirmasi ERROR MERAH "Tidak bisa menghapus Unit ... PROTON tahun-berjalan aktif" + form tidak tersimpan.
8. Jalankan `npx playwright test tests/e2e/multiunit-widget-399.spec.ts --workers=1` → semua hijau.
9. (Seed Data Workflow) bila pakai seed multi-unit temporary: snapshot DB sebelum, restore setelah, catat docs/SEED_JOURNAL.md.
  </how-to-verify>
  <action>Verifikasi runtime di browser per langkah how-to-verify: render cascade widget, default-primary, round-trip 2-unit, MU-07 modal/block. Jalankan Playwright spec.</action>
  <verify>
    <automated>npx playwright test tests/e2e/multiunit-widget-399.spec.ts --workers=1</automated>
  </verify>
  <done>Operator approve: widget render + round-trip 2-unit + MU-07 (modal coach / block PROTON) benar; Playwright hijau.</done>
  <resume-signal>Ketik "approved" bila widget render + round-trip 2-unit + MU-07 (modal/block) benar; atau jelaskan masalah.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → WorkerController POST | checkbox `Units[]` + radio `PrimaryUnit` + hidden `ConfirmedDeactivate` = input client (server re-validasi, plan 02) |
| client JS state machine | Default-primary + disable-radio = UX convenience; server tetap otoritatif (plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-399-03-01 | Tampering | Client kirim PrimaryUnit ∉ Units / unit asing via dev-tools | mitigate | Server (plan 02 Task 2) re-validasi `u ∈ GetUnitsForSectionAsync` + `PrimaryUnit ∈ Units`. Widget hanya UX; tidak dipercaya. |
| T-399-03-02 | Tampering | Bypass modal MU-07 → set ConfirmedDeactivate=true tanpa lihat dampak | mitigate | Server (plan 02) tetap jalankan guard: PTA aktif = HARD-BLOCK apa pun nilai ConfirmedDeactivate; mapping deactivate atomic + audit. Modal hanya konfirmasi UX. |
| T-399-03-03 | EoP | Akses form Create/Edit oleh non-Admin/HC | accept | `[Authorize(Roles="Admin, HC")]` di controller (plan 02) — view tidak menambah surface baru; gate di endpoint. |
| T-399-03-04 | Information Disclosure | XSS via nama unit di innerHTML render | mitigate | Nama unit bersumber dari OrganizationUnits internal (admin-curated, bukan input publik); render via innerHTML string. Jika nama unit bisa berisi karakter HTML, escape via textContent — verifikasi sumber dict adalah org-tree internal (low risk, accept residual). |
</threat_model>

<verification>
- `dotnet build` 0 error (Razor compile Create/Edit).
- Widget grep: `#unitMultiContainer` + `initSectionUnitMultiCascade` + `name=Units` + `name=PrimaryUnit` ada di view + JS.
- Runtime (Task 4): render cascade + default-primary + round-trip 2-unit + MU-07 modal/block — browser sign-off.
- `npx playwright test tests/e2e/multiunit-widget-399.spec.ts --workers=1` hijau.
- CLAUDE.md gate: build + run + Playwright sebelum commit.
</verification>

<success_criteria>
- Bagian single + Unit checkbox-list + radio Primary per UI-SPEC §A (8 state).
- Default primary = first checked (D-02); 0-unit valid; round-trip 2-unit benar.
- MU-07 modal coach-mapping (confirm) + error merah PROTON (block) tampil benar.
- Spec Playwright hijau; fungsi cascade/togglePassword existing utuh.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-03-SUMMARY.md`. Catat: hasil runtime widget (state mana terverifikasi), round-trip 2-unit, MU-07 modal/block, hasil Playwright, seed temporary (bila ada) + restore.
</output>
