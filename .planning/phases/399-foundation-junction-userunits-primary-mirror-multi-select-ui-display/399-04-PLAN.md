---
phase: 399-foundation-junction-userunits-primary-mirror-multi-select-ui-display
plan: 04
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - Models/ProfileViewModel.cs
  - Models/SettingsViewModel.cs
  - Models/PSignViewModel.cs
  - Controllers/AccountController.cs
  - Views/Account/Profile.cshtml
  - Views/Account/Settings.cshtml
  - Views/Admin/WorkerDetail.cshtml
  - Views/Admin/ManageWorkers.cshtml
  - Views/Shared/_PSign.cshtml
  - Views/Home/Index.cshtml
  - Controllers/HomeController.cs
  - tests/e2e/multiunit-display-399.spec.ts
autonomous: false
requirements: [MU-03]
must_haves:
  truths:
    - "Profil, WorkerDetail, Settings, tabel ManageWorkers, Home dashboard menampilkan SEMUA unit pekerja"
    - "Unit PRIMARY ditandai (badge hijau + bintang + teks 'Utama'), unit lain badge sekunder"
    - "Unit tampil primary-first lalu alfabetis (konsisten lintas surface)"
    - "_PSign (kartu cetak) menampilkan SEMUA unit primary-first comma-join (D-07, BUKAN primary-only)"
    - "Pekerja tanpa unit menampilkan fallback existing ('Belum diisi' / '-' / kosong) per surface (D-09)"
  artifacts:
    - path: "Controllers/AccountController.cs"
      provides: "Populate Units + PrimaryUnit dari UserUnits di Profile/Settings VM + nested PSign"
      contains: "UserUnits"
    - path: "Views/Shared/_PSign.cshtml"
      provides: "All-units primary-first comma-join (print)"
      contains: "OrderByDescending"
    - path: "Views/Admin/WorkerDetail.cshtml"
      provides: "Multi-unit badge display (primary highlighted)"
      contains: "bi-star-fill"
  key_links:
    - from: "Controllers/AccountController.cs (Profile/Settings GET)"
      to: "UserUnits table"
      via: "_context.UserUnits.Where(UserId).ToListAsync() → VM.Units + PrimaryUnit"
      pattern: "_context\\.UserUnits"
    - from: "Views display surfaces"
      to: "Model.Units / Model.PrimaryUnit"
      via: "foreach badge loop primary-first"
      pattern: "Model\\.Units"
---

<objective>
Tampilkan SEMUA unit pekerja (primary ditandai) di 7 surface: Profile, Settings, WorkerDetail, tabel ManageWorkers, Home dashboard (HTML badge), dan `_PSign` (cetak, comma-join) — D-07/D-08/D-09. Populate VM `Units`/`PrimaryUnit` dari `UserUnits` di AccountController (yang BELUM inject `_context` — tambahkan). Verifikasi runtime browser + Playwright.

Purpose: MU-03 — pengguna melihat seluruh keanggotaan unit. `_PSign` sengaja tampil semua unit (D-07 LOCKED — JANGAN revert ke primary-only) meski cert atribusi tetap primary (D1=b). Badge mengikuti UI-SPEC §B PERSIS (hijau primary + bintang + "Utama").
Output: VM diperluas (Profile/Settings/PSign); AccountController populate; 6 view display; spec Playwright multiunit-display-399.

**autonomous:false** — Task 4 checkpoint:human-verify (render 7 surface incl cert/print) butuh konfirmasi visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-PATTERNS.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-UI-SPEC.md
@.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-CONTEXT.md

<interfaces>
<!-- Kontrak dari plan 01/02. Konsumsi langsung. -->
From UserUnit (plan 01): { string UserId; string Unit; bool IsPrimary; bool IsActive; }
AccountController DI saat ini (VERIFIED Controllers/AccountController.cs:14-32): UserManager, SignInManager, IAuthService, IConfiguration, ILogger.
  ⚠️ TIDAK inject ApplicationDbContext — Task 1 WAJIB tambah `_context` ke constructor + DI.
ManageUserViewModel sudah punya Units/PrimaryUnit (plan 02). WorkerDetail/ManageWorkers VM = ManageUserViewModel/worker DTO.
WorkerController override View() ke ~/Views/Admin/ (:34-37) — render _PSign dari konteks WorkerController WAJIB path absolut `~/Views/Shared/_PSign.cshtml` (Pitfall 5).

Display badge analog (UI-SPEC §B + PATTERNS §Display badges):
- primary: `<span class="badge bg-success bg-opacity-10 text-success me-1"><i class="bi bi-star-fill me-1" aria-hidden="true"></i>@u <span class="visually-hidden">(Utama)</span>Utama</span>`
- sekunder: `<span class="badge bg-secondary bg-opacity-25 text-dark me-1">@u</span>`
- ordering: `.OrderByDescending(x => x == Model.PrimaryUnit).ThenBy(x => x)`
- empty: `<span class="text-muted fst-italic">Belum diisi</span>` (Profile/Settings/WorkerDetail) · "-" (ManageWorkers/Home cell) · "" no-row (_PSign) — D-09
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VM (Profile/Settings/PSign) + AccountController populate Units dari UserUnits</name>
  <files>Models/ProfileViewModel.cs, Models/SettingsViewModel.cs, Models/PSignViewModel.cs, Controllers/AccountController.cs</files>
  <read_first>
    - Models/ProfileViewModel.cs (:14-15 Section/Unit scalar), SettingsViewModel.cs (:19-21), PSignViewModel.cs (:10-12)
    - Controllers/AccountController.cs (:14-32 DI — tambah _context; :140-206 Profile/Settings GET populate; nested PSign = new PSignViewModel{...Unit=user.Unit})
    - .planning/phases/399-.../399-PATTERNS.md (§AccountController Profile/Settings GET + §ViewModels code-to-write)
  </read_first>
  <action>
**ViewModels** — tambah ke `ProfileViewModel`, `SettingsViewModel` (Section TETAP scalar):
```csharp
public List<string> Units { get; set; } = new();
public string? PrimaryUnit { get; set; }
```
`PSignViewModel` tambah (pertahankan scalar `Unit` untuk fallback):
```csharp
public List<string>? Units { get; set; }
public string? PrimaryUnit { get; set; }
```

**AccountController** — tambah `ApplicationDbContext _context` ke field + constructor + DI (saat ini TIDAK ada — VERIFIED :14-32):
```csharp
private readonly ApplicationDbContext _context;
// constructor: tambah param ApplicationDbContext context + _context = context;
```
Di Profile GET (:149-167) + Settings GET (:183-203), sebelum return View:
```csharp
var userUnits = await _context.UserUnits.Where(uu => uu.UserId == user.Id).ToListAsync();
var unitNames = userUnits.Select(x => x.Unit).ToList();
var prim = userUnits.FirstOrDefault(x => x.IsPrimary)?.Unit;
// set di VM halaman:
model.Units = unitNames; model.PrimaryUnit = prim;
// set di nested PSign:
PSign = new PSignViewModel { FullName = user.FullName, Position = user.Position,
                             Unit = user.Unit, Units = unitNames, PrimaryUnit = prim }
```
Pertahankan scalar `Unit = user.Unit` (mirror) untuk pembaca belum-migrasi. JANGAN longgarkan `[Authorize]` controller-level.
  </action>
  <acceptance_criteria>
    - `Models/ProfileViewModel.cs` DAN `Models/SettingsViewModel.cs` memuat "public List<string> Units"
    - `Models/PSignViewModel.cs` memuat "public List<string>? Units"
    - `Controllers/AccountController.cs` memuat "ApplicationDbContext _context" DAN "_context.UserUnits"
    - `Controllers/AccountController.cs` constructor memuat parameter ApplicationDbContext
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <done>VM Profile/Settings/PSign diperluas; AccountController inject _context + populate Units/PrimaryUnit (+ nested PSign); build 0 error.</done>
</task>

<task type="auto">
  <name>Task 2: Display badge 5 surface HTML (Profile/Settings/WorkerDetail/ManageWorkers/Home)</name>
  <files>Views/Account/Profile.cshtml, Views/Account/Settings.cshtml, Views/Admin/WorkerDetail.cshtml, Views/Admin/ManageWorkers.cshtml, Views/Home/Index.cshtml, Controllers/HomeController.cs</files>
  <read_first>
    - Views/Account/Profile.cshtml (:80,:86 unit display + fallback "Belum diisi"), Settings.cshtml (:99,:117)
    - Views/Admin/WorkerDetail.cshtml (:91-98 badge Section analog; :101-104 unit row [GANTI]), ManageWorkers.cshtml (:262 cell `<small>@(user.Unit ?? "-")`)
    - Views/Home/Index.cshtml (:39 render user.Unit)
    - .planning/phases/399-.../399-PATTERNS.md (§Display badges code-to-write)
    - .planning/phases/399-.../399-UI-SPEC.md (§B HTML surfaces — badge primary/sekunder, ordering, density tabel)
  </read_first>
  <action>
Ganti display unit tunggal jadi loop badge multi-unit (UI-SPEC §B) di 5 surface. Snippet panel (Profile/Settings/WorkerDetail — fallback italic "Belum diisi"):
```razor
@if (Model.Units != null && Model.Units.Any())
{
    @foreach (var u in Model.Units.OrderByDescending(x => x == Model.PrimaryUnit).ThenBy(x => x))
    {
        if (u == Model.PrimaryUnit)
        {
            <span class="badge bg-success bg-opacity-10 text-success me-1">
                <i class="bi bi-star-fill me-1" aria-hidden="true"></i>@u <span class="visually-hidden">(Utama)</span>Utama
            </span>
        }
        else
        {
            <span class="badge bg-secondary bg-opacity-25 text-dark me-1">@u</span>
        }
    }
}
else
{
    <span class="text-muted fst-italic">Belum diisi</span>
}
```
**WorkerDetail** unit row :101-104 → snippet di atas (akses VM Units/PrimaryUnit).
**ManageWorkers** cell :262 + **Home** :39 (TABEL — density `small`, fallback "-" BUKAN panel italic, D-09):
```razor
<td class="p-3">
@if (user.Units != null && user.Units.Any())
{
    @foreach (var u in user.Units.OrderByDescending(x => x == user.PrimaryUnit).ThenBy(x => x))
    { /* badge primary/sekunder small, me-1; cell boleh wrap 2 baris */ }
}
else { <small class="text-muted">-</small> }
</td>
```
**Sumber data (file-ownership):** ManageWorkers list VM `Units`/`PrimaryUnit` SUDAH di-populate oleh Plan 02 Task 3 (WorkerController dimiliki Plan 02) — di sini view HANYA membaca `user.Units`/`user.PrimaryUnit` (no edit WorkerController). Home dashboard: `Views/Home/Index.cshtml:39` baca `Model.CurrentUser?.Unit` (mirror) — untuk tampil SEMUA unit, edit `HomeController` (dimiliki Plan 04, sudah inject `_context` VERIFIED :18,:24-29): batch-load `_context.UserUnits.Where(uu => uu.UserId == user.Id)` untuk CurrentUser, expose ke HomeViewModel (tambah `List<string>? CurrentUserUnits`/`string? CurrentUserPrimaryUnit`), render badge di view. Pekerja mode-role (CurrentUser null) tetap null-safe (existing).

Accent hijau HANYA untuk primary; jangan warna-saja (selalu pasang bintang + "Utama"). JANGAN inline font-size magic (Lesson DSN-05). Label tier via @OrgLabels bila ada heading.
  </action>
  <acceptance_criteria>
    - `Views/Admin/WorkerDetail.cshtml` memuat "bi-star-fill" DAN "Utama" DAN "OrderByDescending"
    - `Views/Account/Profile.cshtml` DAN `Views/Account/Settings.cshtml` memuat "Model.Units" DAN "Belum diisi"
    - `Views/Admin/ManageWorkers.cshtml` memuat ".Units" (loop) DAN fallback "-"
    - `Views/Home/Index.cshtml` memuat ".Units"
    - Tidak ada inline `style="font-size:` baru di view yang disentuh
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj</automated>
  </verify>
  <done>5 surface HTML menampilkan semua unit (badge primary hijau+bintang+"Utama"/sekunder), primary-first ordering, fallback D-09; build 0 error.</done>
</task>

<task type="auto">
  <name>Task 3: _PSign all-units comma-join (cetak, D-07) + Export sudah di plan 02 + spec Playwright display</name>
  <files>Views/Shared/_PSign.cshtml, tests/e2e/multiunit-display-399.spec.ts</files>
  <read_first>
    - Views/Shared/_PSign.cshtml (:40-43 .psign-label single unit [GANTI ke all-units])
    - .planning/phases/399-.../399-PATTERNS.md (§_PSign code-to-write — comma-join primary-first)
    - .planning/phases/399-.../399-UI-SPEC.md (§B Print/cert card — plain text, no badge, ALL units D-07; §Copywriting empty="" no-row)
    - .planning/phases/399-.../399-CONTEXT.md (D-07 LOCKED — _PSign tampil SEMUA unit, JANGAN revert primary-only)
    - tests/e2e/coachcoacheemapping-389.spec.ts (analog spec; login lokal --workers=1)
  </read_first>
  <action>
**_PSign.cshtml** ganti :40-43 (D-07 — SEMUA unit, primary-first comma, NO badge di cetak):
```razor
@if (Model.Units != null && Model.Units.Any())
{
    <div class="psign-label">@string.Join(", ", Model.Units.OrderByDescending(u => u == Model.PrimaryUnit).ThenBy(u => u))</div>
}
@* tidak ada baris bila kosong — pertahankan perilaku existing *@
```
JANGAN revert ke `Model.Unit` primary-only (D-07 LOCKED). Bila `_PSign` di-render dari konteks WorkerController, pemanggil pakai path absolut `~/Views/Shared/_PSign.cshtml` (Pitfall 5) — verifikasi pemanggil; fallback scalar `Model.Unit` tetap ada untuk pemanggil yang belum mengisi `Units`.

**Spec Playwright** `tests/e2e/multiunit-display-399.spec.ts` (test-first, login lokal, --workers=1):
- D-01: WorkerDetail pekerja 2-unit → 2 badge tampil, primary punya `.bi-star-fill` + teks "Utama".
- D-02: Profile pekerja 2-unit → kedua unit tampil primary-first.
- D-03: ManageWorkers row pekerja 2-unit → kedua unit di cell.
- D-04: pekerja 0-unit → "Belum diisi"/"-" sesuai surface.
- D-05: ordering primary muncul pertama.
(_PSign cetak + Excel diverifikasi manual di Task 4 — render cetak/binary.)

Export Excel multi-unit SUDAH dikerjakan plan 02 Task 3 (kolom 7 primary-first comma) — plan 04 hanya verifikasi visual di Task 4, tidak menulis ulang.
  </action>
  <acceptance_criteria>
    - `Views/Shared/_PSign.cshtml` memuat "Model.Units" DAN "OrderByDescending" DAN "string.Join"
    - `Views/Shared/_PSign.cshtml` TIDAK lagi hanya menampilkan `@Model.Unit` tunggal (all-units menggantikan)
    - `tests/e2e/multiunit-display-399.spec.ts` ada, memuat "bi-star-fill" DAN "Utama"
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('Views/Shared/_PSign.cshtml','utf8');if(!/Model\.Units/.test(s)||!/OrderByDescending/.test(s))process.exit(1);const t=fs.readFileSync('tests/e2e/multiunit-display-399.spec.ts','utf8');if(!/star-fill/.test(t))process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>_PSign tampil semua unit primary-first comma (D-07); spec Playwright display ditulis; build 0 error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Runtime verify display 7 surface incl _PSign cetak + Excel (browser)</name>
  <files>tests/e2e/multiunit-display-399.spec.ts (run-only)</files>
  <what-built>
    Display semua unit (badge primary hijau+bintang+"Utama") di Profile/Settings/WorkerDetail/ManageWorkers/Home; _PSign cetak all-units comma-join (D-07); Excel export primary-first (plan 02); spec Playwright multiunit-display-399.
  </what-built>
  <how-to-verify>
1. `dotnet run` → http://localhost:5277, login admin lokal. Pastikan ada pekerja 2-unit (dari plan 03 round-trip atau seed temporary — Seed Data Workflow snapshot/restore).
2. Profile pekerja 2-unit → kedua unit tampil; primary badge hijau + bintang + "Utama"; sekunder badge abu.
3. Settings → idem.
4. Admin → WorkerDetail pekerja tsb → kedua unit, primary ditandai, primary-first.
5. Admin → ManageWorkers tabel → cell unit pekerja tampil kedua unit (cell boleh wrap 2 baris).
6. Home dashboard → unit pekerja tampil (semua).
7. Pekerja 0-unit → "Belum diisi" (Profile/Settings/WorkerDetail) / "-" (tabel) / kosong (_PSign).
8. Cetak/preview kartu tanda tangan (_PSign) → menampilkan SEMUA unit primary-first comma-join (BUKAN hanya primary — D-07).
9. Export Excel worker → kolom unit = unit primary-first dipisah koma (mis. "UnitB, UnitA").
10. `npx playwright test tests/e2e/multiunit-display-399.spec.ts --workers=1` → hijau.
11. Bila seed temporary dipakai: restore DB + catat docs/SEED_JOURNAL.md cleaned.
  </how-to-verify>
  <action>Verifikasi runtime di browser per langkah how-to-verify: 7 surface tampil semua unit (primary ditandai) incl _PSign cetak + Excel. Jalankan Playwright spec.</action>
  <verify>
    <automated>npx playwright test tests/e2e/multiunit-display-399.spec.ts --workers=1</automated>
  </verify>
  <done>Operator approve: 7 surface tampil semua unit (primary ditandai) + _PSign all-units + Excel primary-first benar; Playwright hijau.</done>
  <resume-signal>Ketik "approved" bila 7 surface tampil semua unit (primary ditandai) + _PSign all-units + Excel benar; atau jelaskan masalah.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccountController → DB (read) | Profile/Settings baca `UserUnits` user yang login (scoped ke user.Id) |
| display surfaces → render | Nama unit (admin-curated org-tree) di-render di HTML/cetak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-399-04-01 | Information Disclosure | Profile/Settings tampilkan unit user lain | mitigate | Query `Where(uu => uu.UserId == user.Id)` di-scope ke user yang login (`_userManager.GetUserAsync`); tidak ada parameter user-controlled. WorkerDetail = Admin/HC only (gate existing). |
| T-399-04-02 | EoP | Non-authenticated akses Profile/Settings | accept | `[Authorize]` controller-level AccountController (existing) — tidak dilonggarkan. |
| T-399-04-03 | Information Disclosure | XSS nama unit di Razor | mitigate | Razor `@u` auto-HTML-encode (server-rendered, bukan innerHTML). Aman by default. |
| T-399-04-04 | Tampering (read-consistency) | Display unit beda dari mirror (stale) | accept | Display baca UserUnits langsung (sumber kebenaran); mirror hanya untuk pembaca lama. Konsisten by-design. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- Grep: `Model.Units` + `bi-star-fill` + `OrderByDescending` di view; `_context.UserUnits` di AccountController.
- Runtime (Task 4): 7 surface tampil semua unit (primary ditandai) incl _PSign cetak + Excel — browser sign-off.
- `npx playwright test tests/e2e/multiunit-display-399.spec.ts --workers=1` hijau.
- CLAUDE.md gate: build + run + Playwright sebelum commit.
</verification>

<success_criteria>
- 5 surface HTML + Home menampilkan semua unit (badge primary hijau+bintang+"Utama"/sekunder), primary-first.
- _PSign cetak all-units comma-join (D-07); Excel primary-first (plan 02) terverifikasi.
- Fallback D-09 tiap surface ("Belum diisi"/"-"/kosong).
- Spec Playwright display hijau.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/399-foundation-junction-userunits-primary-mirror-multi-select-ui-display/399-04-SUMMARY.md`. Catat: surface mana terverifikasi runtime, hasil _PSign cetak (all-units), Excel kolom unit, hasil Playwright, seed temporary + restore (bila ada). Tandai phase 399 siap /gsd-verify-work + flag migration=TRUE notify IT.
</output>
