# Continue Here — Phase 401 (PROTON Unit-Resolution Hardening)

**Paused:** 2026-06-18 (interactive `/gsd-execute-phase 401 --interactive`, user resumes next day)
**HEAD when paused:** see `git log -1` (last commit `feat(401-03): D-01 orphan-unit warning alert`)

## Where we are

Phase 401 = 6 plans. **5/6 COMPLETE + committed.** Plan 03 is mid-execution (code done, blocked on human-verify checkpoint).

| Plan | Status | Notes |
|------|--------|-------|
| 401-01 | ✅ COMPLETE (SUMMARY) | helper `ValidateAssignmentUnitInUserUnits` + 5 RED scaffolds |
| 401-02 | ✅ COMPLETE (SUMMARY) | GetEligibleCoachees gate + AutoCreateProgress read-path, D-03 channels |
| 401-04 | ✅ COMPLETE (SUMMARY) | AssessmentAdmin cert-gate |
| 401-05 | ✅ COMPLETE (SUMMARY) | CDP filter-axis + defensive resolvers |
| 401-06 | ✅ COMPLETE (SUMMARY) | Bypass filter-axis + TargetUnit validation |
| 401-03 | 🟡 IN PROGRESS | Tasks 1-3 DONE+committed (2 commits). **Checkpoint + Task 4 PENDING.** |

## Plan 03 remaining work (resume point)

1. **BLOCKING human-verify checkpoint (next action):** verify the D-01 orphan-unit indicator RENDERS on `/Admin/CoachCoacheeMapping` (Razor — Phase 354 lesson: grep+build NOT enough; `ViewBag.OrphanUnitMappings as int?` cast must be runtime-checked). How-to-verify is in `401-03-PLAN.md` task `Checkpoint: Human-verify`. Options offered: (a) Claude drives via Playwright (snapshot DB → seed 1 orphan mapping → login Admin admin@pertamina.com → assert yellow `alert-warning` appears w/ count + absent when 0 → screenshot → RESTORE DB per Seed Workflow); (b) user verifies manually + says "approved". **User must approve before Task 4.**
2. **Task 4 (after approval):** turn GREEN the write-path tests — un-skip in `CleanupNoClobberTests.cs` (PSU-04 preserve: UnitY ∈ active → true), `ReactivateUnitValidationTests.cs` (PSU-07: UnitDilepas ∉ active → false), `AssignmentUnitInUserUnitsTests.cs` (add `Assign_batch_rejects_when_one_coachee_lacks_unit`). Currently 4 Skip-RED remain (2 → 404, 2 → 401-03). After Task 4: CleanupNoClobber + Reactivate Skips become GREEN; only Phase-404 deep-SQL Skips remain.
3. **Finalize Plan 03:** write `401-03-SUMMARY.md`, `roadmap update-plan-progress 401 401-03 complete`, commit docs.
4. **Phase 401 done → verification:** then the execute-phase flow continues to code_review_gate → regression_gate → `gsd-verifier` (VERIFICATION.md) → secure (`/gsd-secure-phase 401`) → validate. migration=FALSE for all of 401.

## Critical Anti-Patterns

| Anti-pattern | Severity | Why |
|--------------|----------|-----|
| Marking Plan 03 / Phase 401 complete before the human-verify D-01 checkpoint is satisfied | blocking | Checkpoint is `gate="blocking"`. D-01 is Razor — Phase 354 proved grep+build pass while render is broken. Must confirm live render (Claude-Playwright or user) before Task 4 + completion. |
| Running gsd-tools from inside `.planning/` or a stale Bash cwd | blocking | gsd-tools resolves project_root via git-common-dir → lands on SIBLING main worktree (`...\PortalHC_KPB`, v32.2), NOT this ITHandoff worktree (v32.3). ALWAYS `cd` to the ITHandoff toplevel first, else `phase_found:false`. |
| Touching the AF-4 window (`EF.Functions.DateDiffSecond ±5`) in Reactivate | blocking | D-05 mandates it stays untouched. Currently 2 occurrences — must remain 2. |

## Env notes

- gsd-tools MUST run from `C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB-ITHandoff` (the ITHandoff worktree). Sibling `...\PortalHC_KPB` = main branch v32.2 — different `.planning`.
- Local app: `Authentication__UseActiveDirectory=false ASPNETCORE_ENVIRONMENT=Development dotnet run --project HcPortal.csproj --urls http://localhost:5277`. Admin login `admin@pertamina.com` (reference_dev_credentials).
- Test baseline this phase: **388 passed / 0 failed / 4 skipped** (Category!=Integration). 0 migration. 0 regression across all 6 plans.
- DB seeding for the checkpoint: follow CLAUDE.md Seed Data Workflow (snapshot → SEED_JOURNAL → seed → RESTORE).
