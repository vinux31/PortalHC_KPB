---
phase: 404-test-sql-riil-uat-docs-invariants
plan: 04
type: execute
wave: 3
depends_on: ["404-01", "404-02", "404-03"]
files_modified:
  - docs/milestone-v32.3/index.html
  - docs/milestone-v32.3-batasan-d1b.md
  - docs/SEED_JOURNAL.md
autonomous: false
migration: false
requirements: [QA-02]
tags: [uat, docs, handoff, seed, proton, d1b, milestone-close]

must_haves:
  truths:
    - "Local browser UAT of the riskiest flow (PROTON sequential cross-unit T1@X→T2@Y) passes at localhost:5270 with cert histori per-unit intact and coach multi-unit view/export"
    - "IT handoff HTML exists with migration=TRUE Phase 399 notice, commit hash placeholder, phases 399-404 list, and Dev deploy steps"
    - "D1=b limitation (cert/analytics attributed to primary unit) is documented in markdown"
    - "Seed used for UAT is temporary local-only: snapshot→seed→restore with SEED_JOURNAL entry flipped to cleaned"
  artifacts:
    - path: "docs/milestone-v32.3/index.html"
      provides: "IT handoff report (pola v31.0) — migration=TRUE Phase 399"
      contains: "migration"
      min_lines: 80
    - path: "docs/milestone-v32.3-batasan-d1b.md"
      provides: "D1=b limitation note (primary-unit attribution)"
      contains: "primary"
    - path: "docs/SEED_JOURNAL.md"
      provides: "+1 UAT 404 entry, status cleaned after restore"
      contains: "404"
  key_links:
    - from: "docs/milestone-v32.3/index.html"
      to: "Tim IT deploy (10.55.3.3 / Phase 399 migration)"
      via: "deploy steps + migration=TRUE notice + commit hash"
      pattern: "migration|399"
    - from: "UAT @localhost:5270"
      to: "HcPortalDB_Dev (snapshot→seed→restore)"
      via: "SEED_WORKFLOW snapshot/restore + SEED_JOURNAL entry"
      pattern: "RESTORE|cleaned"
---

<objective>
Close the milestone: (1) run the local browser UAT of the single riskiest flow — **PROTON sequential cross-unit T1@X→T2@Y** with cert histori per-unit intact + coach multi-unit view/export — at `localhost:5270` (branch ITHandoff, AD off), using temporary local-only seed (snapshot→seed→restore); (2) produce the **IT handoff HTML** (pola `docs/milestone-v31.0/index.html`, flipped to **migration=TRUE Phase 399**) + **D1=b limitation markdown**; (3) record the seed in `docs/SEED_JOURNAL.md` and flip to `cleaned` after restore.

Purpose: QA-02 — narrow-but-deep human verification of the highest-risk path (invariant DB depth is already covered by Plans 01-03 xUnit; not repeated manually), plus the deploy artifacts IT needs (this milestone carries migration=TRUE). Cross-unit ASSIGN round-trip was already UAT'd in Phase 402 (`7f5b6a17`) — do NOT redo it; focus on the sequential PROTON progression + cert histori + coach view. (QA-02, D-10..D-14)

Output: `docs/milestone-v32.3/index.html`, `docs/milestone-v32.3-batasan-d1b.md`, `docs/SEED_JOURNAL.md` (+1 entry).

**BOUNDARY:** NOL kode produksi. Docs + UAT only. **GATE (CLAUDE.md Develop Workflow):** UAT runs at `localhost:5270` (ITHandoff, NOT 5277); `dotnet build` 0 error + `dotnet run` + DB check. **SEED (CLAUDE.md Seed Workflow):** snapshot DB lokal → insert temporary seed → UAT → RESTORE → journal `cleaned` (success OR failure). DB app lokal is SEPARATE from the xUnit disposable fixture DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-CONTEXT.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-RESEARCH.md
@.planning/phases/404-test-sql-riil-uat-docs-invariants/404-PATTERNS.md
@docs/SEED_WORKFLOW.md
@docs/DEV_WORKFLOW.md
@docs/milestone-v31.0/index.html
@docs/SEED_JOURNAL.md

<interfaces>
<!-- Load-bearing facts the executor needs. -->

Port + env (CLAUDE.md / DEV_WORKFLOW §1): branch ITHandoff runs at `http://localhost:5270` (NOT 5277 — that is the main worktree). AD off: `dotnet run` with `Authentication__UseActiveDirectory=false`.

Cert histori per-unit (research Open Q3): PROTON cert/result histori is per-assignment via `ProtonFinalAssessment` (1:1 per `ProtonTrackAssignment`, unique index :443) — so T1 and T2 co-exist as 2 assignments (one inactive + one active). D1=b: AGGREGATE reporting attributes to primary unit (`ApplicationUser.Unit` mirror), but per-track cert is NOT lost. Phrase D1=b doc as: "cert/laporan agregat atribusi ke unit primary; cert per-track tetap tersimpan utuh sebagai histori (tidak hilang)."

HTML handoff template (docs/milestone-v31.0/index.html): copy the entire `<style>` block + header/verdict/grid/table/note/footer skeleton. CHANGE: title to "Akun Multi-Unit (1 Bagian) — Paket v32.3"; the "Perlu ubah database" meta to "YA — migration Fase 399 (AddUserUnits + backfill)" (v31.0 said "Tidak"); "Langkah berikutnya (oleh Tim IT)" to list phases 399-404, run migration Fase 399, verify PROTON sequential cross-unit; commit-hash note to branch ITHandoff + commit `<hash>` + flag migration=TRUE (Fase 399). Embed or link the D1=b note.

SEED_JOURNAL row format (docs/SEED_JOURNAL.md:7 header, :9 the 403-02 / :12 400-01 cadence to copy): columns Tanggal | Phase | Klasifikasi | Tujuan | Dampak | Snapshot file | Status. For 404: Klasifikasi `temporary + local-only`; Tujuan = UAT PROTON sekuensial cross-unit T1@X to T2@Y @5270 + cert histori + coach multi-unit view; Dampak = additive UserUnits/ProtonTrackAssignment/CoachCoacheeMapping/ProtonFinalAssessment rows for {X,Y}+coach; Status `active` during, `cleaned` after RESTORE WITH REPLACE (record baseline verify like 400-01 "UserUnits=6 baseline verified").

Pre-UAT gate: Plans 01-03 must be green first. Run `dotnet test` (or `--filter "Category=Integration"`) and confirm no regression vs baseline ~547/0/6 before the manual flow.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: IT handoff HTML + D1=b limitation markdown</name>
  <files>docs/milestone-v32.3/index.html, docs/milestone-v32.3-batasan-d1b.md</files>
  <read_first>
    - docs/milestone-v31.0/index.html (full — copy style + header/verdict/grid/table/note/footer; this is the template)
    - .planning/ROADMAP.md (Phase 404 success criteria + the 399-404 phase list for the deploy steps)
    - .planning/phases/404-test-sql-riil-uat-docs-invariants/404-CONTEXT.md (D-13/D-14 content requirements)
    - docs/superpowers/specs/2026-06-18-akun-multi-unit-within-bagian-design.md (D1=b decision rationale — for the markdown note)
    - .planning/phases/404-test-sql-riil-uat-docs-invariants/404-PATTERNS.md (milestone-v32.3/index.html + D1=b note sections — what-to-copy/change)
  </read_first>
  <action>
1. Create `docs/milestone-v32.3/index.html` modeled on `docs/milestone-v31.0/index.html`:
   - Copy the entire `<style>` block and the header/verdict/grid/table/note/status/footer skeleton.
   - Header: tag "PORTAL HC KPB · RINGKASAN UNTUK TIM IT"; h1 "Akun Multi-Unit (1 Bagian) — Paket v32.3"; subtitle "Pekerja >1 Unit dalam 1 Bagian + coaching cross-unit + PROTON sekuensial".
   - Meta row: Tanggal laporan (today); Status "Selesai dan teruji di komputer lokal"; "Perlu ubah database: YA — migration Fase 399 (AddUserUnits + backfill)" (flip from v31.0's "Tidak").
   - A "Yang berubah" section listing the user-facing capabilities (multi-unit akun, coaching cross-unit dalam 1 Bagian, PROTON sekuensial T1@X to T2@Y) in plain Bahasa Indonesia (audience = IT/awam, like v31.0).
   - "Langkah berikutnya (oleh Tim IT)" ordered list: (1) pasang kode terbaru fase 399-404 ke server Development (10.55.3.3 / `/KPB-PortalHC`); (2) jalankan migration database — Fase 399 `AddUserUnits` (tabel junction + backfill 1 primary-row/pekerja); (3) verifikasi alur PROTON sekuensial cross-unit + coach multi-unit.
   - Commit-hash note (muted): "branch `ITHandoff`, commit `<hash>` (isi saat push), flag migration = TRUE (Fase 399)." Leave `<hash>` as a literal placeholder for the push step.
   - Embed a `.note` box summarizing the D1=b limitation (or link to the markdown file).
   - All copy in Bahasa Indonesia (per CLAUDE.md).

2. Create `docs/milestone-v32.3-batasan-d1b.md` (D-14): a short markdown note documenting:
   - Keputusan D1=b: cert/analytics atribusi ke unit primary (`ApplicationUser.Unit` mirror), bukan per-unit penuh — keterbatasan sadar-desain milestone.
   - Penting: cert per-track tetap tersimpan utuh sebagai histori via `ProtonFinalAssessment` (1:1 per assignment) — T1 dan T2 co-exist sebagai 2 assignment; yang atribusi-primary hanya laporan AGREGAT. Cert tidak hilang.
   - Konsekuensi untuk IT/HC: sebuah cert unit-Y bisa muncul di laporan agregat unit-X (unit primary). Bila compliance per-unit dibutuhkan to v2 (kolom unit-at-issue + backfill, deferred di REQUIREMENTS).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && test -f docs/milestone-v32.3/index.html && test -f docs/milestone-v32.3-batasan-d1b.md && grep -q "Fase 399" docs/milestone-v32.3/index.html && grep -qi "primary" docs/milestone-v32.3-batasan-d1b.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/milestone-v32.3/index.html` exists, contains "Fase 399" and a migration=TRUE / "YA" database-change notice (grep both present).
    - HTML contains a `<hash>` placeholder and "Langkah berikutnya" deploy steps listing fase 399-404.
    - `docs/milestone-v32.3-batasan-d1b.md` exists and mentions primary-unit attribution + "tersimpan utuh" / cert not lost.
    - The verify command prints `DOCS_OK`.
  </acceptance_criteria>
  <done>IT handoff HTML (migration=TRUE Phase 399, commit-hash placeholder, fase 399-404, Dev deploy steps) and D1=b markdown exist and are accurate per D-13/D-14.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT browser — PROTON sequential cross-unit T1@X to T2@Y @5270 (snapshot to seed to restore)</name>
  <files>docs/SEED_JOURNAL.md</files>
  <read_first>
    - docs/SEED_WORKFLOW.md (snapshot/restore SOP — sqlcmd BACKUP/RESTORE, classification, journal rules)
    - docs/DEV_WORKFLOW.md (§1 — port 5270 ITHandoff, local verify gate)
    - docs/SEED_JOURNAL.md (rows :9 403-02 / :12 400-01 — copy the cadence + post-restore baseline-verify proof)
    - .planning/phases/404-test-sql-riil-uat-docs-invariants/404-CONTEXT.md (D-10/D-11/D-12 — UAT scope, port, seed rules)
  </read_first>
  <action>
Automate the full pre-checkpoint pipeline, then PAUSE for the human-verify checkpoint (full steps in <what-built>/<how-to-verify>):
1. `dotnet build` → 0 error; `dotnet test` → green (no regression vs baseline ~547/0/6; Plans 01-03 Facts pass + backfill skip count down 3).
2. `sqlcmd ... BACKUP DATABASE` snapshot of `HcPortalDB_Dev` (record the `.bak` path).
3. Insert TEMPORARY local-only seed {X,Y} 1 Bagian + coach cross-unit + PROTON Tahun1@X (reuse the additive 400-01/403-02 recipe — Iwan/GAST + sibling RFCC pattern). Add a SEED_JOURNAL row with status `active`.
4. `dotnet run` AD-off at `http://localhost:5270`, then STOP and present the checkpoint.
After the human responds (success OR failure): `RESTORE DATABASE ... WITH REPLACE`, verify baseline (temp rows gone, UserUnits back to baseline — record the proof), flip the SEED_JOURNAL row to `cleaned`, delete the `.bak`.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -q "404" docs/SEED_JOURNAL.md && echo SEED_ROW_PRESENT</automated>
  </verify>
  <what-built>
Plans 01-03 produced the SQL-real xUnit invariant suite (single-active, AssignmentUnit∈UserUnits, B-06 anti-dobel, 1:1, one-primary, backfill). Plan 04 Task 1 produced the IT handoff HTML + D1=b markdown. This checkpoint is the manual browser sign-off of the single riskiest end-to-end flow that xUnit cannot cover (live Razor/PROTON progression UI).

BEFORE the checkpoint, the executor MUST automate (CLAUDE.md gate + SEED_WORKFLOW):
1. `dotnet build` → 0 error; `dotnet test` → green, no regression vs baseline ~547/0/6 (Plans 01-03 Facts pass, backfill skip count dropped by 3).
2. Snapshot `HcPortalDB_Dev` via `sqlcmd ... BACKUP DATABASE` (record `.bak` path).
3. Insert TEMPORARY local-only seed: pekerja {X,Y} in 1 Bagian + coach cross-unit + PROTON Tahun1@X (so the sequential T1→T2 flow is reproducible). Prefer reusing the proven recipe from SEED_JOURNAL 402-03/404-style (additive rows; e.g. the Iwan/GAST + RFCC pattern used by 400-01/403-02). Add the SEED_JOURNAL row with status `active`.
4. `dotnet run` with AD off at `http://localhost:5270`.
  </what-built>
  <how-to-verify>
At `http://localhost:5270` (admin@pertamina.com — reference_dev_credentials), verify the riskiest flow:
1. **PROTON sequential cross-unit:** coachee {X,Y} runs PROTON Tahun 1 @ Unit X to completion, then is assigned/bypassed to Tahun 2 @ Unit Y. Confirm: exactly 1 active `ProtonTrackAssignment` at any time (sequential, single-active) — DB check `SELECT COUNT(*) FROM ProtonTrackAssignments WHERE CoacheeId=... AND IsActive=1` returns 1.
2. **Cert histori per-unit intact:** the Tahun 1 @ X cert/record is NOT destroyed by the Tahun 2 @ Y progression — both `ProtonFinalAssessment`/assignment rows co-exist (T1 inactive + T2 active). Confirm via the records/PROTON history surface AND a DB check.
3. **Coach multi-unit view/export:** a coach with a multi-unit account sees/exports coachees across both Unit X and Unit Y within the Bagian (CDP self-scope union).
NOTE: do NOT re-run the cross-unit ASSIGN round-trip — already covered by Phase 402 UAT (`7f5b6a17`). DB-level invariants are covered by Plans 01-03 xUnit — do not re-verify them manually here.

AFTER verification (success OR failure), the executor MUST: `RESTORE DATABASE ... WITH REPLACE` from the snapshot, verify baseline (e.g. `UserUnits` count back to baseline, temp rows gone — record the proof like 400-01), and flip the SEED_JOURNAL row to `cleaned` with the verification note + delete the `.bak`.
  </how-to-verify>
  <resume-signal>Type "approved" (UAT pass: PROTON sequential single-active, cert histori intact, coach multi-unit view OK; DB restored + journal cleaned) or describe the issue.</resume-signal>
  <acceptance_criteria>
    - `dotnet build` 0 error + `dotnet test` green (no regression) recorded before UAT.
    - SEED_JOURNAL has a 404 row that ends at status `cleaned` with a post-restore baseline-verify proof (grep `404` and `cleaned` present in the new row).
    - Human types "approved" confirming: sequential single-active PROTON, cert histori co-exist, coach multi-unit view/export.
    - DB app lokal restored to baseline (temp seed gone) — verified and noted in the journal.
  </acceptance_criteria>
  <done>The riskiest flow (PROTON sequential cross-unit + cert histori + coach multi-unit view) is human-verified at 5270; seed was temporary local-only and fully restored; SEED_JOURNAL entry cleaned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 04 adds ZERO production surface — docs (HTML/markdown) + a manual UAT against the EXISTING app. No new endpoints/inputs/controllers/services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-404-04-01 | Tampering (integritas DB lokal) | UAT seed in HcPortalDB_Dev | mitigate | SEED_WORKFLOW snapshot to seed to RESTORE WITH REPLACE + journal `cleaned` (success OR failure) — no residual seed; baseline verified post-restore. |
| T-404-04-02 | Information Disclosure | IT handoff HTML | accept | Static report, no secrets; commit hash is a public ref. Audience = IT. No new disclosure surface. |

**Justification:** No new production attack surface — docs + UAT against existing app only (test/UAT/docs phase). The single risk (UAT seed leaking into the local app DB) is mitigated by the mandatory snapshot/restore + journal lifecycle. Threats N/A otherwise.
</threat_model>

<verification>
- `dotnet build` 0 error + `dotnet test` green (no regression vs baseline ~547/0/6, backfill skip count down 3) BEFORE UAT.
- `docs/milestone-v32.3/index.html` + `docs/milestone-v32.3-batasan-d1b.md` exist with the required content (Task 1 verify prints DOCS_OK).
- SEED_JOURNAL 404 row ends `cleaned` with baseline-verify proof.
- Human "approved" for the PROTON sequential cross-unit flow.
</verification>

<success_criteria>
- QA-02 satisfied: riskiest flow human-verified at 5270; D1=b documented; IT handoff HTML (migration=TRUE Phase 399 + commit-hash placeholder + fase 399-404 + deploy steps) produced.
- Seed temporary local-only, fully restored, journal cleaned (Seed Workflow honored).
- Milestone v32.3 ready for 1 push + notify IT (migration=TRUE Phase 399, commit hash) — D-15 close gate met.
- Zero production code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/404-test-sql-riil-uat-docs-invariants/404-04-SUMMARY.md`
</output>
