---
phase: 405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentAttemptResponseArchive.cs
  - Models/AssessmentSession.cs
  - Data/ApplicationDbContext.cs
  - Migrations/
autonomous: true
requirements: [RTK-01, RTK-02]
tags: [retake, migration, schema, ef-core]

must_haves:
  truths:
    - "3 kolom config (AllowRetake/MaxAttempts/RetakeCooldownHours) ada di tabel AssessmentSessions DB lokal dengan default benar (false/2/24)"
    - "Tabel AssessmentAttemptResponseArchive ada di DB lokal dengan FK ON DELETE CASCADE ke AssessmentAttemptHistory + index AttemptHistoryId"
    - "dotnet build 0 error dan dotnet ef database update sukses (chain utuh, snapshot ProductVersion tetap 8.0.0)"
  artifacts:
    - path: "Models/AssessmentAttemptResponseArchive.cs"
      provides: "Entity snapshot per-soal (RTK-02)"
      contains: "class AssessmentAttemptResponseArchive"
    - path: "Models/AssessmentSession.cs"
      provides: "3 kolom config retake"
      contains: "public bool AllowRetake"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet + entity config FK cascade + index"
      contains: "AssessmentAttemptResponseArchives"
    - path: "Migrations/"
      provides: "Migration AddRetakeColumnsAndArchive (3 AddColumn + 1 CreateTable)"
  key_links:
    - from: "Data/ApplicationDbContext.cs"
      to: "Models/AssessmentAttemptResponseArchive.cs"
      via: "DbSet<AssessmentAttemptResponseArchive> + HasForeignKey(e => e.AttemptHistoryId).OnDelete(Cascade)"
      pattern: "AssessmentAttemptResponseArchive"
    - from: "Migrations/*_AddRetakeColumnsAndArchive.cs"
      to: "AssessmentSessions + AssessmentAttemptResponseArchives (DB lokal)"
      via: "dotnet ef database update"
      pattern: "AddRetakeColumnsAndArchive"
---

<objective>
Bangun fondasi data ujian ulang: 3 kolom config (`AllowRetake`/`MaxAttempts`/`RetakeCooldownHours`) pada `AssessmentSession` + entitas snapshot per-soal `AssessmentAttemptResponseArchive` (FK cascade ke `AssessmentAttemptHistory`) + satu migration gabung `AddRetakeColumnsAndArchive` (D-03). **[BLOCKING]** — semua plan 405 berikutnya bergantung pada tipe entitas + migration yang ada di chain (integration test pakai disposable-DB `MigrateAsync`; bila migration tak ada → chain break, semua integration gagal).

Purpose: Tanpa kolom config + tabel archive, `RetakeRules`/`RetakeArchiveBuilder`/`RetakeService` tak punya tipe untuk dibangun, dan diskriminator D-01 (snapshot-presence) tak punya tabel anak untuk EXISTS-query.
Output: 2 file model (1 baru, 1 modifikasi), 1 modifikasi DbContext, 1 file migration baru, 1 update snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-CONTEXT.md
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-RESEARCH.md
@CLAUDE.md

<project_notes>
- Branch ITHandoff; app port lokal 5270 (tidak perlu dijalankan di plan ini — pure schema/build).
- DB lokal: `HcPortalDB_Dev` @ `localhost\SQLEXPRESS`, Integrated Security. sqlcmd flags: `-C -I`.
- Migration = tanggung jawab IT untuk promosi ke Dev/Prod (CLAUDE.md Develop Workflow). Plan ini HANYA `dotnet ef database update` lokal + commit file migration. Catat flag **migration=TRUE** untuk notify IT.
- Global `dotnet ef` 10.0.3 vs project EF 8.0.0 — pola Phase 399: snapshot `ProductVersion` HARUS tetap `"8.0.0"` (drift ke 10.x = chain break).
</project_notes>

<interfaces>
<!-- Tipe parent FK — diverifikasi dari repo. Executor pakai langsung, no eksplorasi. -->

From Models/AssessmentAttemptHistory.cs (parent FK target, [Key] Id int):
```csharp
public class AssessmentAttemptHistory
{
    [Key] public int Id { get; set; }
    public int SessionId { get; set; }
    public string UserId { get; set; } = "";
    public string Title { get; set; } = "";
    public string Category { get; set; } = "";
    public int? Score { get; set; }
    public bool? IsPassed { get; set; }
    public int AttemptNumber { get; set; }
    public DateTime ArchivedAt { get; set; } = DateTime.UtcNow;
}
```

From Models/AssessmentSession.cs:42 (insertion point — sisip SETELAH baris ini):
```csharp
[Display(Name = "Acak Pilihan Jawaban")]
public bool ShuffleOptions { get; set; } = true;   // <-- line 42; kolom retake disisip persis setelah ini
```

From Data/ApplicationDbContext.cs (DbSet existing + config block AssessmentAttemptHistory ~:571-581 = pola mirror untuk WithMany()):
```csharp
public DbSet<AssessmentAttemptHistory> AssessmentAttemptHistory { get; set; }   // sisip DbSet baru di bawah ini
```

AssessmentSession.Schedule = DateTime (AssessmentSession.cs:18) → tidak relevan di plan ini, tapi dipakai sibling-key plan 405-04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat entitas AssessmentAttemptResponseArchive (RTK-02)</name>
  <read_first>
    - Models/AssessmentAttemptHistory.cs (parent FK target, lihat `<interfaces>`)
    - 405-RESEARCH.md "Implementation Map → RTK-02" (field list verified vs spec §4.2)
  </read_first>
  <files>Models/AssessmentAttemptResponseArchive.cs</files>
  <action>
Buat file `Models/AssessmentAttemptResponseArchive.cs` namespace `HcPortal.Models`. Entity snapshot per-soal dari SATU attempt yang diarsipkan — dibekukan saat retake/reset SEBELUM `PackageUserResponses` dihapus, tahan terhadap edit/hapus soal kemudian (verdict beku, bukan recompute).

Field (verified vs spec §4.2):
- `[Key] public int Id { get; set; }`
- `public int AttemptHistoryId { get; set; }` — FK ke `AssessmentAttemptHistory.Id` (cascade)
- `public AssessmentAttemptHistory? AttemptHistory { get; set; }` — nav nullable
- `public int PackageQuestionId { get; set; }` — plain int, **NO FK** (soal bisa terhapus kemudian)
- `public string QuestionText { get; set; } = "";` — snapshot teks soal beku
- `public string? AnswerText { get; set; }` — jawaban display (essay full-text per Pitfall 2; diisi plan 405-02)
- `public bool? IsCorrect { get; set; }` — verdict beku: true=Benar, false=Salah, null=essay pending
- `public int AwardedScore { get; set; }` — skor didapat soal ini saat archive
- `public DateTime ArchivedAt { get; set; } = DateTime.UtcNow;`

`using System.ComponentModel.DataAnnotations;` untuk `[Key]`. XML-doc ringkas di kelas (sebut v32.4 RTK-02 + "frozen verdict before delete").
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Models/AssessmentAttemptResponseArchive.cs` ada dan berisi `public class AssessmentAttemptResponseArchive`
    - File mengandung properti: `AttemptHistoryId`, `PackageQuestionId`, `QuestionText`, `AnswerText`, `IsCorrect`, `AwardedScore`, `ArchivedAt`, dan nav `AttemptHistory`
    - `dotnet build` → "Build succeeded" 0 error (entity belum di-wire ke DbContext — itu Task 2)
  </acceptance_criteria>
  <done>Entity compile-clean; nama properti persis sesuai daftar (dipakai builder plan 405-02 + service plan 405-03).</done>
</task>

<task type="auto">
  <name>Task 2: 3 kolom config AssessmentSession + DbSet + entity config (RTK-01/02)</name>
  <read_first>
    - Models/AssessmentSession.cs:38-42 (pola kolom shuffle `[Display]` + default — mirror)
    - Data/ApplicationDbContext.cs (cari `DbSet<AssessmentAttemptHistory>` + blok `modelBuilder.Entity<AssessmentAttemptHistory>` ~:571-581 pola `WithMany()`)
    - 405-RESEARCH.md "Implementation Map → RTK-01/RTK-02" + Pitfall 6
  </read_first>
  <files>Models/AssessmentSession.cs, Data/ApplicationDbContext.cs</files>
  <action>
**A. `Models/AssessmentSession.cs`** — sisip PERSIS setelah baris 42 (`public bool ShuffleOptions { get; set; } = true;`):
```csharp
        // ===== v32.4 Retake config (mirror Shuffle pattern) =====
        [Display(Name = "Izinkan Ujian Ulang")]
        public bool AllowRetake { get; set; } = false;

        [Range(1, 5)]
        [Display(Name = "Maksimal Percobaan")]
        public int MaxAttempts { get; set; } = 2;

        [Range(0, 168)] // 0 = tanpa jeda; cap 1 minggu
        [Display(Name = "Jeda Ujian Ulang (jam)")]
        public int RetakeCooldownHours { get; set; } = 24;
```
(`using System.ComponentModel.DataAnnotations;` sudah ada di file — line 1.)

**B. `Data/ApplicationDbContext.cs` — DbSet:** Tepat di bawah deklarasi `public DbSet<AssessmentAttemptHistory> AssessmentAttemptHistory { get; set; }`, tambah:
```csharp
        public DbSet<AssessmentAttemptResponseArchive> AssessmentAttemptResponseArchives { get; set; } = null!;
```

**C. `Data/ApplicationDbContext.cs` — entity config:** Di dalam `OnModelCreating(...)`, dekat blok `modelBuilder.Entity<AssessmentAttemptHistory>(...)` (~:571-581), tambah:
```csharp
            // v32.4 RTK-02: snapshot per-soal per attempt; cascade dari AttemptHistory.
            modelBuilder.Entity<AssessmentAttemptResponseArchive>(entity =>
            {
                entity.HasIndex(e => e.AttemptHistoryId);
                entity.HasOne(e => e.AttemptHistory)
                      .WithMany()
                      .HasForeignKey(e => e.AttemptHistoryId)
                      .OnDelete(DeleteBehavior.Cascade);
            });
```
(Pola identik config `AssessmentAttemptHistory` yang sudah `WithMany()` untuk User nav.) Pastikan `OnModelCreating` pakai parameter `modelBuilder` (cek nama parameter aktual di file — bisa `builder`).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Models/AssessmentSession.cs` mengandung `public bool AllowRetake { get; set; } = false;`, `public int MaxAttempts { get; set; } = 2;` (`[Range(1, 5)]`), `public int RetakeCooldownHours { get; set; } = 24;` (`[Range(0, 168)]`)
    - `Data/ApplicationDbContext.cs` mengandung `DbSet<AssessmentAttemptResponseArchive> AssessmentAttemptResponseArchives` dan blok `modelBuilder.Entity<AssessmentAttemptResponseArchive>` dengan `OnDelete(DeleteBehavior.Cascade)` + `HasIndex(e => e.AttemptHistoryId)`
    - `dotnet build` → "Build succeeded" 0 error
  </acceptance_criteria>
  <done>Model + DbContext wired; siap generate migration.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Generate + apply migration AddRetakeColumnsAndArchive (RTK-01/02)</name>
  <read_first>
    - Migrations/20260613095102_AddShuffleTogglesToAssessmentSession.cs (pola AddColumn shuffle — mirror untuk kolom)
    - Migrations/ApplicationDbContextModelSnapshot.cs:20 (verifikasi `ProductVersion` tetap "8.0.0" pasca-generate)
    - 405-RESEARCH.md Pitfall 6 (snapshot version) + "Migration command (verified working env)"
  </read_first>
  <files>Migrations/</files>
  <action>
**HARUS dijalankan SETELAH Task 1+2 (model berubah) dan SEBELUM plan 405-03 integration test** — integration pakai disposable-DB `MigrateAsync` yang butuh migration ini di chain.

1. Generate migration via global `dotnet ef`:
```bash
dotnet ef migrations add AddRetakeColumnsAndArchive
```

2. Buka file migration baru, konfirmasi `Up()` berisi:
   - 3× `AddColumn` pada `"AssessmentSessions"`: `AllowRetake` (bit, `defaultValue: false`), `MaxAttempts` (int, `defaultValue: 2`), `RetakeCooldownHours` (int, `defaultValue: 24`).
   - 1× `CreateTable` `"AssessmentAttemptResponseArchives"` dengan kolom (Id PK identity, AttemptHistoryId, PackageQuestionId, QuestionText, AnswerText nullable, IsCorrect nullable bit, AwardedScore int, ArchivedAt) + FK ke `AssessmentAttemptHistory` (`onDelete: ReferentialAction.Cascade`) + `CreateIndex` pada `AttemptHistoryId`.

3. **Verifikasi snapshot version (Pitfall 6):** buka `Migrations/ApplicationDbContextModelSnapshot.cs`, pastikan `.HasAnnotation("ProductVersion", "8.0.0")` (atau "8.0.x") — JANGAN biarkan drift ke "10.x". Bila global ef menulis 10.x, edit balik manual ke `"8.0.0"` (pola Phase 399).

4. Apply ke DB lokal (LOKAL saja — Dev/Prod = IT):
```bash
dotnet ef database update
```

5. Verifikasi kolom + tabel via sqlcmd (`-C -I`):
```bash
sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -Q "SELECT name FROM sys.columns WHERE object_id=OBJECT_ID('AssessmentSessions') AND name IN ('AllowRetake','MaxAttempts','RetakeCooldownHours'); SELECT OBJECT_ID('AssessmentAttemptResponseArchives') AS ArchiveTableId;"
```

**Fallback** bila `dotnet ef` tak tersedia: hand-author migration mirror `Migrations/20260613095102_AddShuffleTogglesToAssessmentSession.cs` untuk kolom (`defaultValue: false`/`2`/`24`) + tambah `CreateTable`/`CreateIndex`/FK cascade manual untuk archive. (`dotnet ef` 10.0.3 terkonfirmasi tersedia di env — fallback jarang dipakai.)

**migration=TRUE** — file migration WAJIB di-commit (akan dipakai IT promosi ke Dev/Prod). Catat hash + flag untuk notify IT di SUMMARY.
  </action>
  <verify>
    <automated>dotnet ef migrations list 2>&1 | grep -i "AddRetakeColumnsAndArchive" && dotnet build 2>&1 | grep -E "Build succeeded|error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `Migrations/*_AddRetakeColumnsAndArchive.cs` ada; `Up()` punya 3 `AddColumn` (default false/2/24) + 1 `CreateTable` archive + FK `ReferentialAction.Cascade` + index `AttemptHistoryId`
    - `Migrations/ApplicationDbContextModelSnapshot.cs` `ProductVersion` = "8.0.0" (BUKAN 10.x)
    - `dotnet ef database update` sukses (exit 0); sqlcmd menampilkan 3 kolom + `ArchiveTableId` non-null
    - `dotnet ef migrations list` menampilkan `AddRetakeColumnsAndArchive`; `dotnet build` 0 error
  </acceptance_criteria>
  <done>Migration ada di chain + applied lokal; kolom & tabel terverifikasi via sqlcmd. Chain siap untuk integration test plan 405-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF model → DB schema | Migration apply mengubah schema DB lokal; salah default/FK = data-integrity risk |
| AssessmentAttemptResponseArchive → AssessmentAttemptHistory | FK cascade — hapus parent harus hapus snapshot (D-04 retain-via-cascade, no orphan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-405-01 | Tampering | Snapshot table tanpa cascade → orphan rows saat parent dihapus | mitigate | `OnDelete(DeleteBehavior.Cascade)` di entity config (Task 2C) — verified di migration `ReferentialAction.Cascade` (Task 3) |
| T-405-02 | Information Disclosure | AnswerText menyimpan jawaban worker — bukan kunci jawaban | accept | Archive menyimpan jawaban worker (data milik worker sendiri) + verdict, BUKAN answer-key. Tier gating (showWrongFlagsOnly) di Phase 407 mengontrol display. Di 405 tabel tak ter-expose ke UI. |
| T-405-03 | Tampering | Migration default salah (mis. AllowRetake default true) → retake aktif tanpa keputusan admin | mitigate | `defaultValue: false` untuk AllowRetake (Task 3 verify) — retake OFF sampai admin ON eksplisit |
| T-405-04 | Denial of Service | Snapshot-presence diskriminator D-01 — legacy archive (no snapshot child) tak boleh kunci pekerja | mitigate (downstream) | Tabel archive lahir KOSONG (Task 3); 5 baris legacy AttemptHistory tetap tanpa child → natural-excluded. Enforcement query EXISTS di plan 405-03. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet ef migrations list` → `AddRetakeColumnsAndArchive` muncul (latest, applied lokal).
- `dotnet ef database update` sukses; sqlcmd konfirmasi 3 kolom `AssessmentSessions` + tabel `AssessmentAttemptResponseArchives`.
- `ApplicationDbContextModelSnapshot.cs` ProductVersion = "8.0.0".
- Migration file ter-commit (migration=TRUE → IT handoff).
</verification>

<success_criteria>
1. 3 kolom config + tabel archive ada di DB lokal (sqlcmd-verified) dengan default & FK cascade benar.
2. Migration `AddRetakeColumnsAndArchive` di chain + ter-commit; snapshot version 8.0.0.
3. `dotnet build` 0 error; chain `MigrateAsync`-ready untuk integration test berikutnya.
</success_criteria>

<output>
After completion, create `.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-01-SUMMARY.md`. Catat: commit hash migration, **migration=TRUE flag untuk notify IT**, hasil sqlcmd (kolom + tabel).
</output>
