---
phase: 405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint
plan: 03
type: execute
wave: 3
depends_on: ["405-01", "405-02"]
files_modified:
  - Services/RetakeService.cs
  - Program.cs
  - HcPortal.Tests/RetakeServiceTests.cs
autonomous: true
requirements: [RTK-07, RTK-13]
tags: [retake, service, claim-atomic, integration, sql-real]

must_haves:
  truths:
    - "RetakeService.ExecuteAsync melakukan claim-transisi atomik DULU (ExecuteUpdateAsync WHERE Status NOT IN (Cancelled,Open)) dan abort bila rowsAffected==0 (anti double-archive)"
    - "Snapshot per-soal ter-tulis (count==jumlah soal) SEBELUM PackageUserResponses dihapus, hanya bila sesi sebelumnya Completed"
    - "CanRetakeAsync: legacy AttemptHistory tanpa snapshot child TIDAK menghitung cap (D-01 snapshot-presence); counting pakai (UserId, Title, Category) anti-konflasi Pre/Post"
    - "RetakeService ter-register scoped di Program.cs"
  artifacts:
    - path: "Services/RetakeService.cs"
      provides: "Shared retake engine: ExecuteAsync + CanRetakeAsync (RTK-07/13)"
      contains: "ExecuteUpdateAsync"
    - path: "Program.cs"
      provides: "DI registration RetakeService scoped"
      contains: "AddScoped<HcPortal.Services.RetakeService>"
    - path: "HcPortal.Tests/RetakeServiceTests.cs"
      provides: "Integration test (SQL-real) claim-atomic + D-01 legacy-no-count + counting + snapshot-before-delete"
      contains: "RetakeService"
  key_links:
    - from: "Services/RetakeService.cs"
      to: "Helpers/RetakeArchiveBuilder.cs"
      via: "RetakeArchiveBuilder.Build(attemptHistory.Id, questions, responses) sebelum RemoveRange"
      pattern: "RetakeArchiveBuilder\\.Build"
    - from: "Services/RetakeService.cs"
      to: "Helpers/RetakeRules.cs"
      via: "CanRetakeAsync membungkus RetakeRules.CanRetake dengan counting DB-aware"
      pattern: "RetakeRules\\.CanRetake"
    - from: "Services/RetakeService.cs"
      to: "AssessmentAttemptResponseArchives (DB)"
      via: "EXISTS subquery snapshot-presence untuk era-retake counting (D-01)"
      pattern: "AssessmentAttemptResponseArchives\\.Any"
---

<objective>
Buat `Services/RetakeService.cs` — mesin retake bersama yang memproduktisasi inti `ResetAssessment` HC, dipanggil dua jalur (worker self-service Phase 407 + HC `ResetAssessment` plan 405-04). Urutan WAJIB (3 koreksi vs existing): **claim-atomik DULU** (anti double-archive) → snapshot per-soal (sebelum delete) → archive → delete live → audit → SignalR `reason` parameterized. `CanRetakeAsync` membungkus `RetakeRules.CanRetake` dengan counting DB-aware **snapshot-presence (D-01)** + grouping `(UserId, Title, Category)`. Register DI scoped. Integration test (SQL-real, disposable-DB `MigrateAsync`) membuktikan claim-atomik + D-01 legacy-no-count + counting + snapshot-before-delete.

Purpose: Memindahkan (bukan menulis ulang) logika reset existing ke service yang reusable, sambil menutup 3 lubang (urutan claim, counting Category, essay snapshot full-text via builder plan 405-02).
Output: 1 service baru + DI registration + 1 integration test class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-CONTEXT.md
@.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-RESEARCH.md

<project_notes>
- Integration test BUTUH SQLEXPRESS hidup (disposable-DB `HcPortalDB_Test_<guid>`). `[Trait("Category","Integration")]`. Migration `AddRetakeColumnsAndArchive` HARUS sudah di chain (plan 405-01) — `MigrateAsync` jalankan FULL chain.
- Quick (unit) run: `dotnet test --filter "Category!=Integration"`. Integration: `dotnet test --filter "Category=Integration"`.
- Status constant: pakai magic-string "Completed"/"Open"/"Cancelled" konsisten existing ResetAssessment (existing TIDAK pakai `using S=` di blok ini — tetap string literal supaya minim-diff; opsional refactor ke `AssessmentConstants` boleh tapi tidak wajib di plan ini).
- TempData token clear (must-fix #1) BUKAN di service (HTTP-scoped) — di caller (plan 405-04 untuk HC; Phase 407 untuk worker).
</project_notes>

<interfaces>
<!-- Semua diverifikasi dari repo. Executor pakai langsung. -->

EXISTING ResetAssessment inline block yang DIPORT (AssessmentAdminController.cs:4238-4323) — pola archive/delete/claim/audit/SignalR. KOREKSI vs existing:
1. Existing claim di :4288 (SETELAH archive, WHERE Status != "Cancelled"). RetakeService: claim DULU, WHERE `Status != "Cancelled" && Status != "Open"` (exclude Open = anti re-claim request kedua).
2. Existing counting :4242 = `(UserId, Title)` SAJA. RetakeService: `(UserId, Title, Category)` (must-fix #3).
3. Existing TIDAK archive snapshot per-soal. RetakeService: tambah `RetakeArchiveBuilder.Build` SEBELUM RemoveRange.

Hub: `IHubContext<AssessmentHub>` (HcPortal.Hubs.AssessmentHub — verified AssessmentAdminController.cs:27 + Hubs/AssessmentHub.cs:10). `using HcPortal.Hubs;`.
Audit: `AuditLogService.LogAsync(string actorUserId, string actorName, string actionType, string description, int targetId, string targetType)` (Services/AuditLogService.cs:21).

From Helpers/RetakeArchiveBuilder.cs (plan 405-02):
```csharp
public static List<AssessmentAttemptResponseArchive> Build(int attemptHistoryId, IEnumerable<PackageQuestion> questions, IEnumerable<PackageUserResponse> responses);
```
From Helpers/RetakeRules.cs (plan 405-02):
```csharp
public static bool CanRetake(bool allowRetake, string? assessmentType, bool isManualEntry, string status, bool? isPassed, int attemptsUsed, int maxAttempts, int retakeCooldownHours, DateTime? completedAt, DateTime nowUtc);
```
From Models/UserPackageAssignment.cs:60: `public List<int> GetShuffledQuestionIds();` (deserialize ShuffledQuestionIds JSON).
From Models/AssessmentAttemptHistory.cs: fields SessionId/UserId/Title/Category/Score/IsPassed/StartedAt/CompletedAt/AttemptNumber/ArchivedAt.

EXISTING claim ExecuteUpdateAsync shape (AssessmentAdminController.cs:4288-4300) — null-out fields yang SAMA:
```csharp
.SetProperty(r => r.Status, "Open").SetProperty(r => r.Score, (int?)null).SetProperty(r => r.IsPassed, (bool?)null)
.SetProperty(r => r.Progress, 0).SetProperty(r => r.StartedAt, (DateTime?)null).SetProperty(r => r.CompletedAt, (DateTime?)null)
.SetProperty(r => r.ElapsedSeconds, (int)0).SetProperty(r => r.LastActivePage, (int?)null).SetProperty(r => r.UpdatedAt, DateTime.UtcNow)
```

D-01 counting query (era-retake, snapshot-presence — RESEARCH "D-01 Mechanism Resolution"):
```csharp
int eraRetakeArchives = await _context.AssessmentAttemptHistory
    .Where(h => h.UserId == s.UserId && h.Title == s.Title && h.Category == s.Category
             && _context.AssessmentAttemptResponseArchives.Any(a => a.AttemptHistoryId == h.Id))
    .CountAsync();
// attemptsUsed = eraRetakeArchives + 1
```

Integration fixture pattern (MIRROR): `HcPortal.Tests/MultiUnitSqlFixture.cs` (IAsyncLifetime, disposable `HcPortalDB_Test_<guid>` @ `localhost\SQLEXPRESS;Integrated Security=True;TrustServerCertificate=True`, `MigrateAsync` full chain, EnsureDeleted di Dispose + mid-failure catch). Simpler analog: `HcPortal.Tests/ResetEtScoreTests.cs` (uji logika DbContext real-SQL langsung, hindari mock Hub/UserManager).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RetakeService.ExecuteAsync + CanRetakeAsync + DI (RTK-07/13)</name>
  <files>Services/RetakeService.cs, Program.cs</files>
  <read_first>
    - AssessmentAdminController.cs:4238-4323 (inline block yang DIPORT — verdict: claim:4288, counting:4242, delete:4262-4282, audit:4315, SignalR:4323)
    - Helpers/RetakeArchiveBuilder.cs + Helpers/RetakeRules.cs (plan 405-02 — signature di `<interfaces>`)
    - Services/AuditLogService.cs:21 (LogAsync signature)
    - Models/UserPackageAssignment.cs:60 (GetShuffledQuestionIds)
    - Program.cs:54 (DI insertion point — setelah GradingService/ProtonBypassService AddScoped)
    - 405-RESEARCH.md "Implementation Map → RTK-07" + Pitfall 1 (claim order) + Pitfall 3 (counting) + Pitfall 7 (SignalR reason)
  </read_first>
  <behavior>
    `ExecuteAsync(int sessionId, string actorUserId, string actorName, string actionType, string reason) → RetakeResult(bool Success, string? Error)`:
    1. Load session; null → (false, "Sesi tidak ditemukan.").
    2. `bool wasCompleted = assessment.Status == "Completed";` (sebelum claim).
    3. CLAIM ATOMIK DULU: `ExecuteUpdateAsync WHERE Id==sessionId && Status != "Cancelled" && Status != "Open"` set Status="Open" + null-out fields (lihat `<interfaces>` shape). `rows==0` → (false, "Sesi tidak dapat direset (sudah dibatalkan atau sudah terbuka)."). [anti double-archive: Status != "Open" cegah re-claim]
    4. Jika `wasCompleted`: counting `(UserId,Title,Category)` total existing → buat AssessmentAttemptHistory (AttemptNumber = eraRetakeArchives+1 per A1/D-01; lihat catatan), SaveChanges (assign Id) → load assignment.GetShuffledQuestionIds → load PackageQuestions(.Include Options) where qIds.Contains(Id) + PackageUserResponses where sessionId → `RetakeArchiveBuilder.Build(attemptHistory.Id, questions, responses)` → AddRange snapshot (jika count>0). [SNAPSHOT SEBELUM delete]
    5. Delete live: PackageUserResponses + UserPackageAssignment + SessionElemenTeknisScores (mirror existing :4262-4282). SaveChanges.
    6. Audit `LogAsync(actorUserId, actorName, actionType, "Retake/reset ... (reason={reason})", sessionId, "AssessmentSession")` try/catch warn-only (`_logger.LogWarning`).
    7. SignalR `_hubContext.Clients.User(assessment.UserId).SendAsync("sessionReset", new { reason })` (parameterized — must-fix #6/Pitfall 7).
    8. return (true, null).

    `CanRetakeAsync(int sessionId) → bool`: load session; null→false. eraRetakeArchives via EXISTS snapshot-presence query (D-01, lihat `<interfaces>`). `return RetakeRules.CanRetake(s.AllowRetake, s.AssessmentType, s.IsManualEntry, s.Status, s.IsPassed, attemptsUsed: eraRetakeArchives+1, s.MaxAttempts, s.RetakeCooldownHours, s.CompletedAt, DateTime.UtcNow);`

    Catatan AttemptNumber (A1/RTK-07): pakai `eraRetakeArchives + 1` (konsisten cap; era-retake counting) ATAU total-count+1 — keduanya valid di 405 (cukup monoton-naik, terisi). REKOMENDASI eraRetakeArchives+1. Jangan blok build atas pilihan ini.
  </behavior>
  <action>
Buat `Services/RetakeService.cs` namespace `HcPortal.Services`. Deps DI: `ApplicationDbContext _context`, `AuditLogService _auditLog`, `IHubContext<AssessmentHub> _hubContext`, `ILogger<RetakeService> _logger`. `using` HcPortal.Data/Helpers/Hubs/Models + Microsoft.AspNetCore.SignalR + Microsoft.EntityFrameworkCore. `public readonly record struct RetakeResult(bool Success, string? Error);`.

Implement `ExecuteAsync` + `CanRetakeAsync` persis `<behavior>`. Port logika delete dari existing ResetAssessment :4262-4282 (PackageUserResponses RemoveRange + UserPackageAssignment Remove + SessionElemenTeknisScores RemoveRange). XML-doc kelas sebut v32.4 RTK-07 + "claim-first anti double-archive (Pitfall 1)" + "TempData clear = tanggung jawab caller (HTTP-scoped)".

**DI:** Di `Program.cs` setelah line 54 (blok AddScoped GradingService/ProtonBypassService), tambah:
```csharp
// v32.4 — retake engine (shared HC reset + worker self-service)
builder.Services.AddScoped<HcPortal.Services.RetakeService>();
```

Jika nama tipe Hub berbeda, samakan dengan `IHubContext<` yang sudah di-inject `AssessmentAdminController` (= `AssessmentHub`, terverifikasi). `dotnet build` → 0 error.
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Services/RetakeService.cs` berisi `ExecuteAsync` + `CanRetakeAsync` + `record struct RetakeResult`
    - Claim `ExecuteUpdateAsync` muncul SEBELUM `RetakeArchiveBuilder.Build` di urutan file (claim-first); WHERE mengandung `Status != "Cancelled"` dan `Status != "Open"`
    - Counting query mengandung `h.Title == ... && h.Category == ...` (bukan Title saja)
    - CanRetakeAsync mengandung `AssessmentAttemptResponseArchives.Any(a => a.AttemptHistoryId == h.Id)` (D-01 EXISTS) + memanggil `RetakeRules.CanRetake`
    - SignalR payload `new { reason }` (parameterized, bukan hardcode "hc_reset")
    - `Program.cs` mengandung `AddScoped<HcPortal.Services.RetakeService>()`
    - `dotnet build` → "Build succeeded" 0 error
  </acceptance_criteria>
  <done>Service compile-clean + DI registered; claim-first + counting-Category + snapshot-builder wired.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test RetakeService — claim-atomic + D-01 legacy-no-count + counting + snapshot-before-delete (RTK-07/13)</name>
  <read_first>
    - HcPortal.Tests/MultiUnitSqlFixture.cs (pola IAsyncLifetime disposable-DB MigrateAsync — MIRROR fixture)
    - HcPortal.Tests/ResetEtScoreTests.cs (pola uji logika DbContext real-SQL langsung, seed helper)
    - Services/RetakeService.cs (Task 1)
    - 405-RESEARCH.md "Validation Architecture → Phase Requirements → Test Map" + "D-01 Mechanism Resolution → Test pembukti"
  </read_first>
  <files>HcPortal.Tests/RetakeServiceTests.cs</files>
  <behavior>
    `[Trait("Category","Integration")]` class. Fixture disposable-DB `MigrateAsync` (mirror MultiUnitSqlFixture; atau IClassFixture reuse fixture yang sudah ada bila kompatibel — buat fixture baru `RetakeServiceFixture` bila perlu, sama pola). Instantiate RetakeService dengan deps real-ish: real `ApplicationDbContext`, real `AuditLogService`, dan `IHubContext<AssessmentHub>` + `ILogger` di-stub/no-op (mirror ResetEtScoreTests yang "hindari mock HubContext" — pakai test-double sederhana: NoOp IHubContext via Moq/NSubstitute bila tersedia, atau panggil ExecuteAsync dan toleransi SignalR no-receiver; cek package test yang ada). Bila mock Hub merepotkan, uji `CanRetakeAsync` + counting + claim langsung lewat DbContext + panggil ExecuteAsync dengan Hub-stub minimal.

    Test cases (WAJIB):
    - `Claim_DoubleExecute_SecondAborts`: seed session Completed + assignment + responses → ExecuteAsync ke-1 → success; ExecuteAsync ke-2 (sesi kini Open) → `Success==false` (rows==0, anti double-archive). Assert hanya 1 AssessmentAttemptHistory ter-create untuk (UserId,Title,Category).
    - `Snapshot_WrittenBeforeResponsesDeleted`: seed Completed dgn N soal + N responses → ExecuteAsync success → `AssessmentAttemptResponseArchives.Count(a => a.AttemptHistoryId == newHistoryId) == N` DAN `PackageUserResponses.Count(sessionId) == 0` (responses dihapus, snapshot tetap).
    - `CanRetake_LegacyArchiveWithoutSnapshot_DoesNotConsumeCap` (D-01): seed session failed (Completed, IsPassed=false, AllowRetake=true, MaxAttempts=2, cooldown lewat) + 1 AssessmentAttemptHistory (UserId/Title/Category sama) TANPA child archive → `CanRetakeAsync == true` (eraRetakeArchives==0 → attemptsUsed==1 < 2).
    - `CanRetake_RetakeEraArchiveWithSnapshot_ConsumesCap` (D-01): seed sama + 1 AttemptHistory DENGAN ≥1 child AssessmentAttemptResponseArchive → `CanRetakeAsync == false` (eraRetakeArchives==1 → attemptsUsed==2 == MaxAttempts).
    - `Counting_PrePostSameTitle_NoConflate` (must-fix #3): seed 2 session Title="X" Category="Pre" dan Category="Post" untuk user sama; 1 era-retake archive di Pre → CanRetakeAsync untuk Post TIDAK terpengaruh (counting (UserId,Title,Category) memisahkan). Assert Post eraRetakeArchives==0.
    Seed helpers mirror ResetEtScoreTests (SeedUser/SeedSession) + tambah seed AttemptHistory + archive child + assignment + responses + questions.
  </behavior>
  <action>
Buat `HcPortal.Tests/RetakeServiceTests.cs` `[Trait("Category","Integration")]`. Buat/refer fixture disposable-DB `MigrateAsync` (mirror `MultiUnitSqlFixture` — `HcPortalDB_Test_<guid>` @ localhost\SQLEXPRESS Integrated Security TrustServerCertificate; EnsureDeleted Dispose + mid-failure catch melempar XunitException "MIGRATION-CHAIN break incl AddRetakeColumnsAndArchive" supaya bedakan chain-break dari bug retake).

Untuk Hub dependency: jika project test sudah punya Moq/NSubstitute (cek `HcPortal.Tests.csproj`), buat no-op `IHubContext<AssessmentHub>` mock (SendAsync no-throw). Jika tidak, buat minimal stub class implement IHubContext yang return no-op IClientProxy. Logger pakai `NullLogger<RetakeService>.Instance` (Microsoft.Extensions.Logging.Abstractions). AuditLogService = real instance atas test DbContext.

Implement 5 test cases di `<behavior>`. Run `dotnet test --filter "FullyQualifiedName~RetakeServiceTests"` (butuh SQLEXPRESS) → semua hijau. Commit: `test(405-03): RetakeService integration — claim-atomic + D-01 legacy-no-count + counting + snapshot (RTK-07/13)`.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeServiceTests" 2>&1 | grep -E "Passed!|Failed!|error|No test" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `HcPortal.Tests/RetakeServiceTests.cs` ada, `[Trait("Category","Integration")]`, ≥5 test cases sesuai `<behavior>`
    - Fixture pakai `MigrateAsync` (full chain incl AddRetakeColumnsAndArchive) pada disposable `HcPortalDB_Test_<guid>`
    - Test `CanRetake_LegacyArchiveWithoutSnapshot_DoesNotConsumeCap` membuktikan D-01 (legacy tanpa child → CanRetake true)
    - Test `Claim_DoubleExecute_SecondAborts` membuktikan anti double-archive (hanya 1 AttemptHistory)
    - Test `Snapshot_WrittenBeforeResponsesDeleted` membuktikan snapshot count==N + responses==0
    - `dotnet test --filter "FullyQualifiedName~RetakeServiceTests"` → Passed (0 failed) dengan SQLEXPRESS hidup
  </acceptance_criteria>
  <done>Klaim-atomik, D-01 snapshot-presence, counting Category, dan snapshot-before-delete terbukti di SQL-real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent requests → ExecuteAsync | Double-click "Ujian Ulang" = dua request paralel; tanpa claim atomik → double-archive |
| Caller authz → RetakeService | Service TIDAK authorize (caller wajib: RBAC HC plan 405-04, ownership worker Phase 407). Service operasi by sessionId. |
| Legacy data → cap counting | 5 baris legacy AttemptHistory (no snapshot) tak boleh kunci pekerja (DoS-by-policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-405-08 | Tampering | Double-click double-archive (dua AttemptHistory + dua snapshot set) | mitigate | Claim-atomik DULU `ExecuteUpdateAsync WHERE Status NOT IN (Cancelled,Open)` + `rows==0` abort (Task 1); test `Claim_DoubleExecute_SecondAborts` (Task 2) |
| T-405-09 | Denial of Service | Legacy archive (no snapshot) konsumsi cap → pekerja terkunci saat AllowRetake ON | mitigate | D-01 snapshot-presence EXISTS subquery — legacy natural-excluded (Task 1); test `CanRetake_LegacyArchiveWithoutSnapshot_DoesNotConsumeCap` (Task 2) |
| T-405-10 | Elevation of Privilege | Cap/cooldown bypass via client (worker path) | mitigate (partial) | `CanRetakeAsync` server-authoritative (Task 1); enforcement worker path = Phase 407. Di 405 API tersedia + integration-tested. |
| T-405-11 | Information Disclosure | Counting (UserId,Title) konflasi Pre/Post → attempt Pre kunci Post | mitigate | Counting `(UserId,Title,Category)` (must-fix #3, Task 1); test `Counting_PrePostSameTitle_NoConflate` (Task 2) |
| T-405-12 | Repudiation | Reset tanpa jejak audit | mitigate | `AuditLogService.LogAsync(actionType, reason)` setelah delete (Task 1); actionType bedakan RetakeAssessment/ResetAssessment |
</threat_model>

<verification>
- `dotnet build` 0 error; `Program.cs` register RetakeService scoped.
- `dotnet test --filter "FullyQualifiedName~RetakeServiceTests"` (SQLEXPRESS) → 5/5 hijau.
- Claim-first terverifikasi (ExecuteUpdateAsync sebelum Build); counting Category; SignalR reason parameterized.
- `dotnet test --filter "Category!=Integration"` tetap hijau (no regresi unit).
</verification>

<success_criteria>
1. `RetakeService.ExecuteAsync` claim-first → snapshot → archive → delete → audit → SignalR(reason); `CanRetakeAsync` D-01 snapshot-presence + counting Category. (RTK-07/13)
2. DI scoped registered.
3. Integration test SQL-real membuktikan claim-atomik, D-01 legacy-no-count, counting no-conflate, snapshot-before-delete — 5/5 hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/405-backend-core-data-retakerules-retakeservice-refactor-reset-config-endpoint/405-03-SUMMARY.md`. Catat: hasil integration test (5/5), keputusan AttemptNumber (era-retake vs total), pola Hub-stub yang dipakai, konfirmasi claim-first urutan.
</output>
