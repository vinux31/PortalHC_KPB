---
phase: 406
slug: admin-config-ui-riwayat-hc
type: playwright-uat
status: passed
date: 2026-06-22
runner: milestone-autopilot
base_url: http://localhost:5270
db: HcPortalDB_Dev (SQLEXPRESS)
result: 12/12 passed
---

# Phase 406 — Playwright UAT (live browser @5270)

Live-browser UAT independen (Lesson v32.5: real-browser WAJIB untuk Razor/JS — runtime-smoke/grep+build tak nangkep ReferenceError yang abort handler-attach). Bukan klaim executor — dijalankan ulang live.

## Setup
- App: `ASPNETCORE_ENVIRONMENT=Development Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5270` (branch ITHandoff). HTTP 200.
- DB: SQLEXPRESS `HcPortalDB_Dev` (60 sesi baseline).
- Auth: admin@pertamina.com (dev lokal).
- SEED_WORKFLOW: spec self-manage — `global.setup.ts` BACKUP → seed matrix (sessions=18 packages=10 questions=30 options=80) → SEED_JOURNAL `active` → teardown RESTORE → SEED_JOURNAL `cleaned`. Layer-4 verifikasi 0 matrix rows post-RESTORE (DB lokal bersih).
- Run: `E2E_BASE_URL=http://localhost:5270 npx playwright test e2e/retake-config-406.spec.ts e2e/riwayat-hc-406.spec.ts --workers=1`.

## Hasil — 12 passed (59.9s)

### RTK-05 — Retake Config ManagePackages (6/6)
| # | Skenario | Hasil |
|---|----------|-------|
| 1 | card render + disclosure: card visible, `#retakeFields` d-none saat OFF, reveal saat toggle | ✅ |
| 2 | hide: card "Ujian Ulang" TIDAK dirender untuk Pre-Test (ShouldHideRetakeToggle) | ✅ |
| 3 | save: set maxAttempts+cooldown → Simpan → PRG success + persisted on reload | ✅ |
| 4 | warning: MaxAttempts < terpakai → alert-warning muncul, Save TETAP enabled (non-blocking) | ✅ |
| 5 | binding: CreateAssessment Step 3 — toggle AllowRetake reveal MaxAttempts/RetakeCooldownHours | ✅ |
| 6 | binding: EditAssessment — 3 field retake render + reflect persisted values | ✅ |

### RTK-08 — Riwayat Percobaan HC modal (5/5)
| # | Skenario | Hasil |
|---|----------|-------|
| 7 | open: dropdown Riwayat Percobaan → modal + lazy-load accordion | ✅ |
| 8 | per-soal: tabel per-soal render kolom + teks soal/jawaban/skor seeded | ✅ |
| 9 | current: attempt saat ini di-badge "Percobaan saat ini" + paling atas | ✅ |
| 10 | pending: essay belum dinilai tampil "—"/Menunggu, bukan ikon salah | ✅ |
| 11 | xss: payload `<script>` di jawaban ter-encode inert (tak eksekusi) | ✅ |

(+1 global.setup verifikasi app+seed = 12 total.)

## Verdict
**PASS 12/12.** Card config + binding Create/Edit + modal riwayat HC (accordion, per-soal tri-state, current badge, essay-pending, XSS-safe) terbukti live @5270. DB lokal restored bersih. Gate UAT 406 TUNTAS.
