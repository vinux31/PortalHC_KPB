---
phase: 407-worker-self-service-gating-tier-feedback-riwayat-pekerja
plan: 02
type: execute
wave: 2
depends_on: ["407-01"]
files_modified:
  - Controllers/CMPController.cs
  - HcPortal.Tests/RetakeExamEndpointTests.cs
autonomous: true
requirements: [RTK-09, RTK-10, RTK-12, RTK-13]
must_haves:
  truths:
    - "POST CMP/RetakeExam(id) menolak non-owner dengan Forbid (IDOR guard)"
    - "RetakeExam re-cek CanRetakeAsync server-side; bila tidak eligible → redirect Results + TempData[Error]"
    - "RetakeExam sukses → clear TempData[TokenVerified_{id}] lalu redirect StartExam"
    - "CMPController menginjeksi RetakeService via constructor"
    - "CMPController.Results mengisi RetakeMode/CanRetake/CurrentAttempt/MaxAttempts/CooldownUntilUtc/IsCapReached/RiwayatAttempts ke VM"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "action RetakeExam (POST, antiforgery+ownership+re-check) + DI RetakeService + Results VM populate + riwayat load"
      contains: "public async Task<IActionResult> RetakeExam(int id)"
    - path: "HcPortal.Tests/RetakeExamEndpointTests.cs"
      provides: "test RetakeExam: non-owner→Forbid, not-eligible→redirect, sukses→token-cleared+redirect StartExam"
      contains: "RetakeExam"
  key_links:
    - from: "Controllers/CMPController.cs"
      to: "Services/RetakeService.cs"
      via: "_retakeService.CanRetakeAsync(id) + _retakeService.ExecuteAsync(...)"
      pattern: "_retakeService\\.(CanRetakeAsync|ExecuteAsync)"
    - from: "Controllers/CMPController.cs"
      to: "Helpers/RetakeRules.cs"
      via: "RetakeRules.ResolveReviewMode(...) saat build VM"
      pattern: "RetakeRules\\.ResolveReviewMode"
    - from: "Controllers/CMPController.cs (RetakeExam)"
      to: "CMP/StartExam"
      via: "RedirectToAction StartExam setelah TempData.Remove token"
      pattern: "TempData\\.Remove\\(\\$\"TokenVerified_"
---

<objective>
Bangun jalur tulis sisi-pekerja: action POST `CMP/RetakeExam(id)` (cermin baris-per-baris HC `ResetAssessment`, beda actor + guard ownership + re-cek `CanRetakeAsync` + redirect `StartExam`), injeksi `RetakeService` ke `CMPController`, dan perluas action `Results` untuk MENGHITUNG semua flag retake/tier + memuat riwayat pekerja ke VM (view 407-03 hanya merender).

Purpose: 407 adalah PERTAMA KALI pekerja men-trigger retake sendiri — endpoint WAJIB ownership-guarded + antiforgery + server-authoritative re-check (countdown JS hanya UX). Tier/eligibility dihitung server (leak-safety = keputusan server), di-inject ke VM; view mempercaya VM.

Output: action `RetakeExam` + DI `RetakeService` + Results VM populate (flags + riwayat load) di `Controllers/CMPController.cs`; test endpoint `RetakeExamEndpointTests.cs` (Wave-0 gap RTK-09). **0 migration.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-CONTEXT.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-RESEARCH.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-PATTERNS.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-01-SUMMARY.md

<interfaces>
<!-- Kontrak siap-pakai. JANGAN duplikasi logika — panggil saja. -->

From Services/RetakeService.cs (sudah ter-DI di Program.cs:63 AddScoped — TIDAK perlu registrasi DI baru):
```csharp
public readonly record struct RetakeResult(bool Success, string? Error);
public class RetakeService {
    public Task<RetakeResult> ExecuteAsync(int sessionId, string actorUserId, string actorName, string actionType, string reason);
    public Task<bool> CanRetakeAsync(int sessionId);   // wraps RetakeRules.CanRetake + DB-aware era-retake counting (UserId,Title,Category)
}
```

From Helpers/RetakeRules.cs (dibuat 407-01):
```csharp
public enum RetakeReviewMode { ShowFullReview, ShowWrongFlagsOnly, ShowScoreOnly }
public static RetakeReviewMode ResolveReviewMode(bool allowAnswerReview, bool? isPassed, bool attemptsRemaining);
```

From Helpers (sudah ada — reuse penuh untuk riwayat load):
```csharp
RetakeArchiveBuilder.Build(int attemptHistoryId, List<PackageQuestion> questions, List<PackageUserResponse> responses) → List<AssessmentAttemptResponseArchive>
RiwayatUnifier.Build(AssessmentSession current, IEnumerable<AssessmentAttemptHistory> histories,
    IEnumerable<AssessmentAttemptResponseArchive> archiveRows, IEnumerable<AssessmentAttemptResponseArchive> currentRows) → List<RiwayatAttemptViewModel>
```

From Models/AssessmentSession.cs (field types VERIFIED): AllowAnswerReview bool :33, AllowRetake bool :46, MaxAttempts int :50, RetakeCooldownHours int :54, IsPassed bool? :56, AssessmentType string?, IsManualEntry bool, Status string, CompletedAt DateTime?.

From Controllers/CMPController.cs (existing ctor :43-73 + field triple pattern):
```csharp
// Field cluster :40-41: _gradingService, _impersonationService
// Ctor params :56-57: GradingService gradingService, ImpersonationService impersonationService
// Ctor assigns :71-72
// GetCurrentUserRoleLevelAsync() → (ApplicationUser? user, ...) — effective user (impersonation-aware), idiom :909
// Results action build site ~ :2347 (append fields after IsPendingGrading); AllowAnswerReview branch builds QuestionReviews ~ :2243 (KEEP)
// StartExam reads TempData.Peek($"TokenVerified_{id}") :944 (non-consume) — token harus di-Remove saat retake
// class-level [Authorize] :25 sudah ada
```

From Controllers/AssessmentAdminController.cs (analog mirror — JANGAN edit file ini, hanya cermin pola):
```csharp
// DI _retakeService: field :32, ctor param :47, assign :58
// ResetAssessment :4244-4327: [HttpPost][Authorize][ValidateAntiForgeryToken], ExecuteAsync call, TempData.Remove($"TokenVerified_{id}") :4319
// actorName format :4298: string.IsNullOrWhiteSpace(user.NIP) ? (user.FullName ?? "Unknown") : $"{user.NIP} - {user.FullName}"
// RiwayatPercobaan data-load :3493-3522 (riwayat load pattern, reuse penuh)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject RetakeService ke CMPController + action RetakeExam (POST, ownership+antiforgery+re-check+token-clear)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs ctor :43-73 (field/ctor/assign triple `_gradingService`/`_impersonationService` sebagai pola mirror) + class [Authorize] :25 + GetCurrentUserRoleLevelAsync idiom :909 + StartExam TempData.Peek :944
    - Controllers/AssessmentAdminController.cs :4244-4327 (ResetAssessment — mirror baris-per-baris) + :4298 (actorName format) + DI triple :32/:47/:58 (JANGAN edit file ini)
    - 407-PATTERNS.md §"Controllers/CMPController.cs — RetakeExam action" + §"DI inject RetakeService" (excerpt siap-salin)
    - 407-RESEARCH.md §"Architecture Patterns Pattern 1" (skeleton lengkap) + §"Common Pitfalls" 2/3 (re-check server + clear token)
  </read_first>
  <action>
    **(1) DI inject** di `Controllers/CMPController.cs` (mirror triple `_gradingService`):
    - Tambah field: `private readonly HcPortal.Services.RetakeService _retakeService;` (atau `RetakeService` bila using sudah ada) di cluster field controller.
    - Tambah param ctor: `HcPortal.Services.RetakeService retakeService` (di akhir param list, mirror posisi gradingService/impersonationService).
    - Tambah assignment: `_retakeService = retakeService;`.
    - TIDAK perlu registrasi DI baru (sudah `AddScoped` di Program.cs:63).

    **(2) Action RetakeExam** — tambahkan action POST baru (cermin ResetAssessment :4244-4327; beda: actor worker, guard ownership, redirect StartExam). Tempatkan dekat action exam-taking lain (mis. setelah StartExam / sebelum/sesudah Results — pilih lokasi yang rapi, JANGAN di tengah method lain):
    ```csharp
    [HttpPost]
    [ValidateAntiForgeryToken]                 // RTK-09 (class-level [Authorize] sudah :25)
    public async Task<IActionResult> RetakeExam(int id)
    {
        var assessment = await _context.AssessmentSessions.FirstOrDefaultAsync(a => a.Id == id);
        if (assessment == null) return NotFound();

        var (user, _) = await GetCurrentUserRoleLevelAsync();   // effective user (impersonation-aware)
        if (user == null) return Challenge();
        if (assessment.UserId != user.Id) return Forbid();      // RTK-09 ownership — IDOR guard

        if (!await _retakeService.CanRetakeAsync(id))           // server-authoritative re-check (D-01)
        {
            TempData["Error"] = "Ujian ulang tidak bisa dijalankan saat ini. Coba muat ulang halaman atau hubungi HC.";
            return RedirectToAction("Results", new { id });
        }

        var actorName = string.IsNullOrWhiteSpace(user.NIP) ? (user.FullName ?? "Unknown") : $"{user.NIP} - {user.FullName}";
        var rs = await _retakeService.ExecuteAsync(id, user.Id, actorName, "RetakeAssessment", "worker_retake");
        if (!rs.Success) { TempData["Error"] = rs.Error ?? "Gagal."; return RedirectToAction("Results", new { id }); }

        TempData.Remove($"TokenVerified_{id}");                 // must-fix #1 — re-arm token (StartExam pakai Peek non-consume)
        return RedirectToAction("StartExam", new { id });       // spec re-entry target
    }
    ```
    Sesuaikan nama destruktur `GetCurrentUserRoleLevelAsync()` dengan signature aktual (cek return tuple di file — bila bukan `(user, _)`, sesuaikan; effective user yang dipakai). Pastikan `using Microsoft.EntityFrameworkCore;` ada untuk `FirstOrDefaultAsync` (kemungkinan sudah).
  </action>
  <acceptance_criteria>
    - Grep: `Controllers/CMPController.cs` mengandung `_retakeService` (field), `RetakeService retakeService` (ctor param), `_retakeService = retakeService;` (assign)
    - Grep: `Controllers/CMPController.cs` mengandung `public async Task<IActionResult> RetakeExam(int id)`
    - Grep: `Controllers/CMPController.cs` mengandung `[ValidateAntiForgeryToken]` di blok RetakeExam dan `if (assessment.UserId != user.Id) return Forbid();`
    - Grep: `Controllers/CMPController.cs` mengandung `await _retakeService.CanRetakeAsync(id)` dan `_retakeService.ExecuteAsync(id, user.Id, actorName, "RetakeAssessment", "worker_retake")`
    - Grep: `Controllers/CMPController.cs` mengandung `TempData.Remove($"TokenVerified_{id}")` dan `RedirectToAction("StartExam", new { id })`
    - `dotnet build` 0 error
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>build 0 error; RetakeService ter-inject; action RetakeExam ada dengan antiforgery + ownership Forbid + CanRetakeAsync re-check + ExecuteAsync + TempData.Remove + redirect StartExam.</done>
</task>

<task type="auto">
  <name>Task 2: Perluas CMPController.Results — hitung flag retake/tier + load riwayat pekerja ke VM</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs action Results (~ :2184-2391; VM build site ~ :2347 append setelah IsPendingGrading; AllowAnswerReview branch ~ :2243 yang membangun QuestionReviews — KEEP, tier WrongFlagsOnly memakai QuestionReviews yang sama)
    - Controllers/AssessmentAdminController.cs :3493-3522 (RiwayatPercobaan data-load — cermin verbatim)
    - 407-PATTERNS.md §"Controllers/CMPController.cs — Results action extend" (excerpt VM flags + riwayat load siap-salin)
    - 407-RESEARCH.md §"Code Examples" (Build VM flags + Riwayat load) + §"Pitfall 5" (pakai assessment.IsPassed bool? BUKAN VM.IsPassed bool)
  </read_first>
  <action>
    Di action `Results` `Controllers/CMPController.cs`, SEBELUM `return View(viewModel)` (setelah viewModel terbangun + assessment ter-load), sisipkan komputasi flag + riwayat, lalu set ke VM. JANGAN ubah/hapus branch `AllowAnswerReview` yang membangun `QuestionReviews` (tier ShowWrongFlagsOnly reuse list itu).

    **(a) Flag retake/tier** (mirror counting `CanRetakeAsync` :237-242; tier pakai `assessment.IsPassed` bool? — Pitfall 5):
    ```csharp
    int eraRetakeArchives = await _context.AssessmentAttemptHistory
        .Where(h => h.UserId == assessment.UserId && h.Title == assessment.Title && h.Category == assessment.Category
                 && _context.AssessmentAttemptResponseArchives.Any(a => a.AttemptHistoryId == h.Id))
        .CountAsync();
    int currentAttempt = eraRetakeArchives + 1;
    bool attemptsRemaining = currentAttempt < assessment.MaxAttempts;
    viewModel.CurrentAttempt = currentAttempt;
    viewModel.MaxAttempts = assessment.MaxAttempts;
    viewModel.CanRetake = await _retakeService.CanRetakeAsync(assessment.Id);
    viewModel.RetakeMode = RetakeRules.ResolveReviewMode(assessment.AllowAnswerReview, assessment.IsPassed, attemptsRemaining);
    viewModel.CooldownUntilUtc = (assessment.AllowRetake && assessment.RetakeCooldownHours > 0 && assessment.CompletedAt.HasValue)
        ? assessment.CompletedAt.Value.AddHours(assessment.RetakeCooldownHours) : (DateTime?)null;
    viewModel.IsCapReached = assessment.IsPassed == false && assessment.AllowRetake && currentAttempt >= assessment.MaxAttempts;
    ```

    **(b) Riwayat load** (cermin verbatim `RiwayatPercobaan` :3493-3522):
    ```csharp
    var histories = await _context.AssessmentAttemptHistory
        .Where(h => h.UserId == assessment.UserId && h.Title == assessment.Title && h.Category == assessment.Category)
        .OrderByDescending(h => h.AttemptNumber).ToListAsync();
    var histIds = histories.Select(h => h.Id).ToList();
    var archiveRows = await _context.AssessmentAttemptResponseArchives
        .Where(a => histIds.Contains(a.AttemptHistoryId)).ToListAsync();
    var currentRows = new List<AssessmentAttemptResponseArchive>();
    if (assessment.Status == "Completed") {
        var assign = await _context.UserPackageAssignments.FirstOrDefaultAsync(a => a.AssessmentSessionId == assessment.Id);
        var qids = assign?.GetShuffledQuestionIds() ?? new List<int>();
        if (qids.Count > 0) {
            var qs = await _context.PackageQuestions.Include(q => q.Options).Where(q => qids.Contains(q.Id)).ToListAsync();
            var resp = await _context.PackageUserResponses.Where(r => r.AssessmentSessionId == assessment.Id).ToListAsync();
            if (qs.Count > 0) currentRows = RetakeArchiveBuilder.Build(0, qs, resp);
        }
    }
    viewModel.RiwayatAttempts = RiwayatUnifier.Build(assessment, histories, archiveRows, currentRows);
    ```
    Sesuaikan nama variabel session (`assessment` vs nama aktual di action Results — pakai variabel session yang sudah ter-load di action). Pastikan `using HcPortal.Helpers;` (RetakeRules/RetakeArchiveBuilder/RiwayatUnifier) + `using HcPortal.Models;` ada (kemungkinan sudah). JANGAN re-grade inline — verdict beku dari builder/archive.
  </action>
  <acceptance_criteria>
    - Grep: `Controllers/CMPController.cs` mengandung `RetakeRules.ResolveReviewMode(assessment.AllowAnswerReview` (atau nama session aktual) — tier dihitung server
    - Grep: `Controllers/CMPController.cs` mengandung `RiwayatUnifier.Build(` dan `RetakeArchiveBuilder.Build(0,`
    - Grep: `Controllers/CMPController.cs` mengandung `viewModel.CanRetake =`, `viewModel.CurrentAttempt =`, `viewModel.CooldownUntilUtc =`, `viewModel.IsCapReached =`, `viewModel.RiwayatAttempts =`
    - Grep: action Results MASIH membangun `QuestionReviews` (branch AllowAnswerReview tak terhapus)
    - `dotnet build` 0 error
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>build 0 error; Results mengisi 7 field VM (tier via assessment.IsPassed bool? — Pitfall 5) + RiwayatAttempts via RiwayatUnifier; QuestionReviews tetap dibangun.</done>
</task>

<task type="auto">
  <name>Task 3: Test endpoint RetakeExam — non-owner→Forbid, not-eligible→redirect, sukses→token-cleared+redirect StartExam</name>
  <files>HcPortal.Tests/RetakeExamEndpointTests.cs</files>
  <read_first>
    - HcPortal.Tests/RetakeServiceTests.cs :1-60 (fixture real-SQL `RetakeServiceFixture` MigrateAsync chain @localhost\SQLEXPRESS, NoOpHubContext hand-stub, NullLogger, [Trait("Category","Integration")], drop-on-dispose) — pola dasar
    - Controllers/CMPController.cs RetakeExam (Task 1 — kontrak yang diuji) + GetCurrentUserRoleLevelAsync (cara controller mendapat effective user)
    - 407-PATTERNS.md §"HcPortal.Tests — NEW RetakeExam endpoint test" + §"Hub/logger stub idiom"
    - 407-VALIDATION.md §"Wave 0 Requirements" (3 kasus RTK-09)
  </read_first>
  <action>
    Buat `HcPortal.Tests/RetakeExamEndpointTests.cs` yang menguji 3 kasus RTK-09. Pilih pendekatan yang paling deterministik untuk codebase ini:

    **Opsi A (disarankan bila controller bisa di-instansiasi dengan deps mock):** controller unit test — instansiasi `CMPController` dengan `RetakeService` over test DbContext (real-SQL fixture mirror RetakeServiceTests, atau EF-InMemory bila cukup) + stub user via `ControllerContext`/fake effective-user. Assert:
    - non-owner (assessment.UserId != effective user) → hasil bertipe `ForbidResult`.
    - eligible=false (mis. assessment AllowRetake=false / passed) → `RedirectToActionResult` ke "Results" + `TempData["Error"]` ter-set.
    - sukses (assessment failed+eligible+cooldown lewat) → `TempData["TokenVerified_{id}"]` TERHAPUS + `RedirectToActionResult` ke "StartExam".

    **Opsi B (bila instansiasi controller terlalu coupled):** integration test mirror `RetakeServiceTests` fixture — uji jalur service + assert pre/post DB state untuk eligible/ownership semantics, plus controller-level token-clear via TempData mock. Tandai `[Trait("Category","Integration")]` bila pakai real-SQL.

    Reuse stub: `NoOpHubContext` (SendAsync no-op, hand-stub tanpa Moq) + `NullLogger` + real `AuditLogService` over test DbContext (mirror RetakeServiceTests :15-16). Untuk TempData, gunakan `TempDataDictionary` dengan `NullTempDataProvider`/`ITempDataProvider` stub; seed `TempData["TokenVerified_{id}"]` sebelum sukses-case lalu assert terhapus.

    Pilih opsi yang bikin test deterministik + cepat. Bila SQLEXPRESS tak tersedia di runner, kasus ownership/Forbid harus tetap jalan tanpa DB (Forbid terjadi sebelum CanRetakeAsync).
  </action>
  <acceptance_criteria>
    - File `HcPortal.Tests/RetakeExamEndpointTests.cs` ADA dan mengandung 3 test bernama jelas mencakup: non-owner Forbid, not-eligible redirect Results, sukses token-cleared + redirect StartExam (mis. grep `Forbid`, `StartExam`, `TokenVerified`)
    - Grep: `HcPortal.Tests/RetakeExamEndpointTests.cs` mengandung `RetakeExam`
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeExam"` hijau (≥3 test; bila ada varian Integration, kasus ownership tetap lulus tanpa SQLEXPRESS)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeExam"</automated>
  </verify>
  <done>3 kasus RTK-09 hijau (Forbid non-owner; redirect+Error not-eligible; token-cleared+redirect StartExam sukses).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CMP/RetakeExam (POST) | Worker (untrusted) memicu mutasi destruktif (arsip+reset) atas sebuah sessionId. Input = `id` route int. |
| client countdown JS → server | Tombol disabled/enabled di klien bukan otoritas; server re-cek eligibility saat POST. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-407-idor | Elevation/Tampering | `RetakeExam` ownership | mitigate | `assessment.UserId != effectiveUser.Id` → `Forbid()` SEBELUM mutasi (Task 1); diuji non-owner→Forbid (Task 3). Effective user via GetCurrentUserRoleLevelAsync (impersonation-aware). |
| T-407-csrf | Tampering | `RetakeExam` POST | mitigate | `[ValidateAntiForgeryToken]` di action (Task 1) + `@Html.AntiForgeryToken()` di form modal (407-03). |
| T-407-bypass | Tampering | cooldown/cap bypass via DevTools enable | mitigate | Server re-cek `await _retakeService.CanRetakeAsync(id)` SEBELUM ExecuteAsync (Task 1); countdown JS non-authoritative; diuji not-eligible→redirect (Task 3). |
| T-407-token | Tampering/AuthN bypass | stale token skip lobby pasca-retake | mitigate | `TempData.Remove($"TokenVerified_{id}")` setelah ExecuteAsync sukses, sebelum redirect StartExam (Task 1, must-fix #1); diuji token-cleared (Task 3). StartExam pakai Peek non-consume. |
| T-407-doublearchive | Tampering | double-click double-archive | accept | Sudah di-handle `ExecuteAsync` claim-atomik (status transition Open → no-op) di service 405; endpoint hanya memanggil. |
| T-407-leak (read-path) | Information Disclosure | Results VM populate (tier dihitung di sini) | mitigate | Tier `ResolveReviewMode` dihitung server pakai `assessment.IsPassed` bool? (Pitfall 5) → di-inject ke VM; view (407-03) men-suppress leak-site berdasar `RetakeMode`. Controller tak pernah mengirim kunci jawaban ke VM untuk mode WrongFlagsOnly (kunci hanya di QuestionReviews.Options yang view kontrol render-nya). |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~RetakeExam"` hijau (3 kasus RTK-09).
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau (no regresi unit; tier helper 407-01 + RetakeExam unit-part).
- `dotnet test HcPortal.Tests` (full incl Integration) hijau — pastikan counting/snapshot 405 tak regresi (per-wave-merge sampling, RESEARCH §Sampling Rate).
</verification>

<success_criteria>
- `CMPController` menginjeksi `RetakeService`.
- `RetakeExam` POST: antiforgery + ownership Forbid + CanRetakeAsync re-check + ExecuteAsync + TempData.Remove token + redirect StartExam.
- `Results` mengisi RetakeMode/CanRetake/CurrentAttempt/MaxAttempts/CooldownUntilUtc/IsCapReached/RiwayatAttempts (tier via assessment.IsPassed bool?).
- 3 kasus endpoint RTK-09 hijau.
- 0 migration; QuestionReviews tetap dibangun (tier WrongFlagsOnly reuse).
</success_criteria>

<output>
After completion, create `.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-02-SUMMARY.md`
</output>
