---
phase: 407-worker-self-service-gating-tier-feedback-riwayat-pekerja
plan: 03
type: execute
wave: 3
depends_on: ["407-01", "407-02"]
files_modified:
  - Views/CMP/Results.cshtml
  - Views/CMP/_RiwayatPekerja.cshtml
  - Views/CMP/Records.cshtml
  - tests/e2e/retake-worker-407.spec.ts
autonomous: false
requirements: [RTK-10, RTK-11, RTK-12]
must_haves:
  truths:
    - "Worker eligible melihat tombol 'Ujian Ulang' yang membuka modal konfirmasi sebelum POST"
    - "Saat cooldown belum lewat, tombol disabled dengan countdown HH:MM:SS yang ticking; saat 0 → enable/relabel"
    - "Saat cap habis, tampil alert-warning 'Batas percobaan tercapai, hubungi HC' tanpa tombol"
    - "Mode ShowWrongFlagsOnly TIDAK merender kunci jawaban (list-group-item-success / '(Jawaban Benar)' / CorrectAnswer)"
    - "Worker melihat riwayat percobaan sendiri (accordion per-attempt, tri-state ✓/✗/—, badge 'Percobaan saat ini')"
  artifacts:
    - path: "Views/CMP/Results.cshtml"
      provides: "retake control + confirm modal + switch(RetakeMode) 3-state + riwayat card + countdown JS"
      contains: "switch (Model.RetakeMode)"
    - path: "Views/CMP/_RiwayatPekerja.cshtml"
      provides: "partial riwayat pekerja ter-gate (varian _RiwayatPercobaan, 'Tidak Lulus'/'Jawaban Saya')"
      contains: "Jawaban Saya"
    - path: "Views/CMP/Records.cshtml"
      provides: "(opsional) trigger riwayat per-row ATAU andalkan 'Lihat Hasil'"
      contains: "Lihat Hasil"
    - path: "tests/e2e/retake-worker-407.spec.ts"
      provides: "Playwright smoke leak-safety @5270 (ShowWrongFlagsOnly tanpa kunci) + control render"
      contains: "retake"
  key_links:
    - from: "Views/CMP/Results.cshtml"
      to: "CMP/RetakeExam"
      via: "form modal POST asp-action RetakeExam + @Html.AntiForgeryToken()"
      pattern: "asp-action=\"RetakeExam\""
    - from: "Views/CMP/Results.cshtml"
      to: "Model.RetakeMode"
      via: "switch 3-state menggantikan boolean AllowAnswerReview"
      pattern: "RetakeReviewMode\\.ShowWrongFlagsOnly"
    - from: "Views/CMP/Results.cshtml"
      to: "Model.CooldownUntilUtc"
      via: "data-cooldown-until + countdown JS"
      pattern: "data-cooldown-until"
---

<objective>
Rakit seluruh UI sisi-pekerja di view (view mempercaya VM dari 407-02): kontrol retake (tombol/counter/cooldown countdown/lock), modal konfirmasi destruktif ber-antiforgery, tier feedback 3-state yang LEAK-SAFE (kunci jawaban ditahan saat masih bisa ulang), dan kartu riwayat percobaan pekerja ter-gate. Plus Playwright smoke leak-safety @5270 (lesson 354/413: build hijau ≠ leak-safe).

Purpose: RTK-10/11/12 user-facing. Leak-safety wajib divalidasi di DOM real-browser, bukan grep+build — `ShowWrongFlagsOnly` TIDAK BOLEH merender `list-group-item-success` / "(Jawaban Benar)" / `CorrectAnswer`.

Output: retake control + modal + `switch(RetakeMode)` + riwayat card + countdown JS di `Views/CMP/Results.cshtml`; partial baru `Views/CMP/_RiwayatPekerja.cshtml` (ter-gate); trigger riwayat di `Views/CMP/Records.cshtml`; spec smoke `tests/e2e/retake-worker-407.spec.ts`. **0 migration.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-CONTEXT.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-RESEARCH.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-PATTERNS.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-UI-SPEC.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-01-SUMMARY.md
@.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-02-SUMMARY.md

<interfaces>
<!-- VM kontrak (dari 407-01/407-02) — view hanya merender, TIDAK menghitung eligibility/tier. -->

Model = AssessmentResultsViewModel (field baru 407-01, diisi 407-02):
```csharp
HcPortal.Helpers.RetakeReviewMode RetakeMode;   // ShowFullReview | ShowWrongFlagsOnly | ShowScoreOnly
bool CanRetake; int CurrentAttempt; int MaxAttempts; DateTime? CooldownUntilUtc; bool IsCapReached;
List<RiwayatAttemptViewModel>? RiwayatAttempts;
// existing dipertahankan: bool AllowAnswerReview; bool IsPassed; bool IsPendingGrading;
// List<QuestionReviewItem>? QuestionReviews (per-soal: QuestionNumber, QuestionText, UserAnswer, CorrectAnswer, IsCorrect, IsEssayPending, Options[OptionText/IsCorrect/IsSelected])
```

RiwayatAttemptViewModel: AttemptNumber, ScorePercent (int?), IsPassed (bool?), CompletedAt (DateTime?), IsCurrent (bool), Rows (List<AssessmentAttemptResponseArchive>: QuestionText/AnswerText/IsCorrect bool?/AwardedScore).

EXISTING Results.cshtml anchors (verified):
- Review block boolean :316 `@if (Model.AllowAnswerReview && Model.QuestionReviews != null)` ... :413 `else if (!Model.AllowAnswerReview)` (else notice :415-417). Full-review markup :318-411 (option loop :357-393 dengan leak-site :366/:367/:386-389; essay branch :397-406 dengan CorrectAnswer :403).
- Action area :420-431 (`<div class="d-flex gap-2 mb-4">`; primary CTA idiom :424 `<a ... class="btn btn-primary"><i class="bi bi-award me-1"></i>Lihat Sertifikat</a>`).
- `@await Html.PartialAsync("_ImageLightboxModal")` :434 (tempat menaruh modal baru).
- Status badge "Tidak Lulus" :82; date locale id-ID :97.

EXISTING analog Views/Admin/_RiwayatPercobaan.cshtml (copy struktur, 3 worker-delta): accordion-per-attempt, accordion-button aria, tri-state status cell, empty-states, `@model List<HcPortal.Models.RiwayatAttemptViewModel>`, semua user-content via Razor `@` (auto-encode, ZERO Html.Raw). Header badge "Gagal" :33 (→ worker "Tidak Lulus"); kolom "Jawaban Peserta" :54 (→ worker "Jawaban Saya"); badge "Percobaan saat ini" :40-43 (keep).

EXISTING Views/CMP/StartExam.cshtml countdown idiom :455-490: `setInterval(updateTimer,1000)` + format HH:MM:SS + `clearInterval` saat `<=0`.

EXISTING Views/CMP/Records.cshtml: Aksi cell :260-283, "Lihat Hasil" :262 (`<a href="@resultsUrl" class="btn btn-sm btn-outline-primary"><i class="bi bi-bar-chart-line me-1"></i>Lihat Hasil</a>`), status "Tidak Lulus" :222.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier feedback 3-state + retake control + modal konfirmasi + countdown JS di Results.cshtml</name>
  <files>Views/CMP/Results.cshtml</files>
  <read_first>
    - Views/CMP/Results.cshtml :316-434 (review block boolean → switch; action area; tempat modal dekat :434)
    - Views/CMP/StartExam.cshtml :455-490 (countdown JS idiom — setInterval/clearInterval/format HH:MM:SS)
    - 407-UI-SPEC.md §"Component Contracts" 1 (retake control), 2 (modal), 3 (tier 3-state — markup preskriptif siap-salin) + §"Leak-safe rules (D-03)"
    - 407-PATTERNS.md §"Views/CMP/Results.cshtml (a)(b)(c)" (mirror line + leak-site list :366/:367/:386-389/:403)
    - <critical_constraints> dari planning_context (LEAK SITES + tier truth table + A1 pending)
  </read_first>
  <action>
    Di `Views/CMP/Results.cshtml`:

    **(A) Tier 3-state** — ganti blok boolean :316-418 dengan `@switch (Model.RetakeMode)` (gunakan `HcPortal.Helpers.RetakeReviewMode`; tambah `@using HcPortal.Helpers` di atas view bila belum ada):
    - `case RetakeReviewMode.ShowFullReview:` → PINDAHKAN markup full-review existing :318-411 VERBATIM (jangan hapus/ubah — termasuk option loop + leak-site + essay CorrectAnswer). Ini cabang passed/exhausted/pending.
    - `case RetakeReviewMode.ShowScoreOnly:` → notice existing :415-417 VERBATIM (`alert alert-info` "Tinjauan jawaban tidak tersedia untuk assessment ini.").
    - `case RetakeReviewMode.ShowWrongFlagsOnly:` → BRANCH BARU per 407-UI-SPEC §Component 3: card "Tinjauan Jawaban" + `alert alert-info role="status"` (note kunci disembunyikan, icon `bi-eye-slash`) + loop `Model.QuestionReviews`: render HANYA `Soal N: QuestionText` + badge verdict tri-state (IsEssayPending → "Menunggu Penilaian"; IsCorrect → "Benar"; else → "Salah") + (bila `!string.IsNullOrEmpty(q.UserAnswer)`) `<div class="small"><span class="text-muted">Jawaban Anda:</span> @q.UserAnswer</div>`.
      **LEAK-SAFE WAJIB (security-critical):** di cabang ini JANGAN render: `list-group-item-success` (:366), icon `bi-check-circle-fill text-success` opsi (:367), label "(Jawaban Benar)" (:386-389), dan `@question.CorrectAnswer` essay (:403). TIDAK ada loop Options dengan highlight kunci; hanya teks jawaban worker + verdict per-soal.

    **(B) Retake control** — di area action (sekitar :420-431, di dalam/dekat `<div class="d-flex gap-2 mb-4">`, atau sebagai blok sebelum action row) tambah per 407-UI-SPEC §Component 1:
    - `@if (Model.IsCapReached)` → `alert alert-warning role="alert"` icon `bi-lock-fill` "Batas percobaan tercapai (@Model.MaxAttempts dari @Model.MaxAttempts). Hubungi HC bila perlu kesempatan tambahan." (NO tombol).
    - `else if (Model.CanRetake)` → wrapper `d-flex align-items-center gap-2 mb-3 flex-wrap`:
      - bila `Model.CooldownUntilUtc.HasValue && Model.CooldownUntilUtc > DateTime.UtcNow` → `<button type="button" class="btn btn-primary" id="btnRetake" disabled data-cooldown-until="@Model.CooldownUntilUtc.Value.ToString("o")"><i class="bi bi-hourglass-split me-1"></i>Bisa ulang dalam <span id="retakeCountdown">--:--:--</span></button>`
      - else → `<button type="button" class="btn btn-primary" id="btnRetake" data-bs-toggle="modal" data-bs-target="#retakeConfirmModal"><i class="bi bi-arrow-repeat me-1"></i>Ujian Ulang</button>`
      - `<span class="text-muted small">Percobaan ke-@Model.CurrentAttempt dari @Model.MaxAttempts</span>`

    **(C) Modal konfirmasi** — dekat `@await Html.PartialAsync("_ImageLightboxModal")` :434, tambah modal per 407-UI-SPEC §Component 2: `#retakeConfirmModal` (tabindex=-1, aria-labelledby, modal-dialog-centered) → header "Konfirmasi Ujian Ulang" + `btn-close aria-label="Tutup"` → body teks konfirmasi destruktif → footer "Batal" (`btn btn-secondary data-bs-dismiss`) + `<form method="post" asp-action="RetakeExam" asp-controller="CMP" asp-route-id="@Model.AssessmentId" class="d-inline">@Html.AntiForgeryToken()<button type="submit" class="btn btn-primary"><i class="bi bi-arrow-repeat me-1"></i>Ya, Ujian Ulang</button></form>`. (RTK-09 antiforgery di form.)

    **(D) Countdown JS** — inline `<script>` (mirror StartExam :455-490): cari `#btnRetake[data-cooldown-until]`; bila ada, baca attr (ISO-8601), `setInterval` 1s hitung `new Date(attr) - Date.now()`, tulis `#retakeCountdown` format `HH:MM:SS`; saat `<=0` → `clearInterval`, hapus `disabled`, set `data-bs-toggle="modal"`+`data-bs-target="#retakeConfirmModal"`, relabel innerHTML jadi `<i class="bi bi-arrow-repeat me-1"></i>Ujian Ulang`. Guard `if (!btn) return;` (anti ReferenceError lesson 413). Pakai `.textContent`/`.setAttribute`, no innerHTML dari user-string.
  </action>
  <acceptance_criteria>
    - Grep: `Views/CMP/Results.cshtml` mengandung `switch (Model.RetakeMode)` dan ketiga case `RetakeReviewMode.ShowFullReview`, `RetakeReviewMode.ShowWrongFlagsOnly`, `RetakeReviewMode.ShowScoreOnly`
    - Grep: `Views/CMP/Results.cshtml` mengandung `id="btnRetake"`, `data-cooldown-until`, `id="retakeCountdown"`, `id="retakeConfirmModal"`
    - Grep: `Views/CMP/Results.cshtml` mengandung `asp-action="RetakeExam"` dan `@Html.AntiForgeryToken()` di blok modal
    - Grep: `Views/CMP/Results.cshtml` mengandung `bi-lock-fill` (lock alert) dan `Percobaan ke-@Model.CurrentAttempt dari @Model.MaxAttempts`
    - **LEAK-SAFE manual-check (executor wajib verifikasi di cabang ShowWrongFlagsOnly):** cabang `ShowWrongFlagsOnly` TIDAK mengandung `list-group-item-success`, TIDAK mengandung string `(Jawaban Benar)`, TIDAK mengandung `@question.CorrectAnswer`/`q.CorrectAnswer`. (String tsb boleh ada di cabang ShowFullReview saja.)
    - `dotnet build` 0 error
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>build 0 error; tier 3-state (ShowFullReview verbatim, ShowScoreOnly verbatim, ShowWrongFlagsOnly leak-safe) + retake control (tombol/counter/cooldown/lock) + modal antiforgery + countdown JS guard-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Partial _RiwayatPekerja.cshtml (ter-gate) + render di Results + trigger Records.cshtml</name>
  <files>Views/CMP/_RiwayatPekerja.cshtml, Views/CMP/Results.cshtml, Views/CMP/Records.cshtml</files>
  <read_first>
    - Views/Admin/_RiwayatPercobaan.cshtml (full file — copy struktur accordion + tri-state status cell; 3 worker-delta)
    - Views/CMP/Results.cshtml (tempat render card riwayat — di bawah tier feedback, sebelum action area; @model AssessmentResultsViewModel)
    - Views/CMP/Records.cshtml :260-283 (Aksi cell + "Lihat Hasil" :262 idiom)
    - 407-UI-SPEC.md §"Component 4" (worker riwayat card markup + copy: "Riwayat Percobaan Saya", "Jawaban Saya", "Tidak Lulus", empty-states) + §"Copywriting Contract" worker riwayat
    - 407-PATTERNS.md §"_RiwayatPekerja.cshtml" (3 worker delta + status cell verbatim) + §"Records.cshtml"
  </read_first>
  <action>
    **(A) Partial baru** `Views/CMP/_RiwayatPekerja.cshtml` — copy struktur `Views/Admin/_RiwayatPercobaan.cshtml`, `@model List<HcPortal.Models.RiwayatAttemptViewModel>`, dengan 3 worker-delta:
    1. Header badge attempt: "Gagal" → **"Tidak Lulus"** (badge `text-bg-danger`), keep "Lulus" (`text-bg-success`) + "Menunggu Penilaian" (`bg-warning text-dark` / `text-bg-secondary`, IsPassed==null).
    2. Kolom tabel per-soal: "Jawaban Peserta" → **"Jawaban Saya"**.
    3. Keep badge `bg-info` "Percobaan saat ini" (driven by `attempt.IsCurrent`); keep empty-states; keep tri-state status cell verbatim (IsCorrect true ✓Benar / false ✗Salah / null —Menunggu, dengan `visually-hidden` label).
    Semua user-content via Razor `@` (auto-encode); ZERO `Html.Raw`. Arsip rows struktural verdict-only (tak ada option-list/kunci) — tak perlu suppress kunci di drill-down archived.
    **Gating ShowScoreOnly:** terima flag gating via `ViewData["HideDetail"]` (bool). Bila true (AllowAnswerReview==false → RetakeMode==ShowScoreOnly), partial render HANYA daftar attempt (skor/lulus/tanggal) TANPA tabel per-soal. (Partial @model = `List<RiwayatAttemptViewModel>` tak bawa RetakeMode; flag dilewatkan via ViewData saat memanggil — lihat B.)

    **(B) Render di Results** `Views/CMP/Results.cshtml` — di bawah tier feedback, tambah card per 407-UI-SPEC §Component 4: `<div class="card shadow-sm mb-4">` header `bi-clock-history` "Riwayat Percobaan Saya" → body: bila `Model.RiwayatAttempts == null || !Model.RiwayatAttempts.Any()` → empty-state "Belum ada riwayat percobaan. Riwayat akan muncul setelah kamu mengulang ujian."; else → `@await Html.PartialAsync("_RiwayatPekerja", Model.RiwayatAttempts, new ViewDataDictionary(ViewData) { ["HideDetail"] = (Model.RetakeMode == HcPortal.Helpers.RetakeReviewMode.ShowScoreOnly) })`. (Partial baca `ViewData["HideDetail"]`.)

    **(C) Records.cshtml** — D-04 discretion: DEFAULT andalkan "Lihat Hasil" :262 yang sudah route ke Results tempat riwayat hidup (cukup; TIDAK wajib tambah trigger). Opsional: bila menambah trigger per-row riwayat, tempatkan di Aksi cell :260-283 sebagai `btn btn-sm btn-outline-secondary` dengan `bi bi-clock-history` + `title`/`aria-label` route ke Results. Pilih DEFAULT (andalkan Lihat Hasil) kecuali ada alasan parity HC; bila default, file Records.cshtml TIDAK diubah (catat di SUMMARY bahwa Records.cshtml tak disentuh).
  </action>
  <acceptance_criteria>
    - File `Views/CMP/_RiwayatPekerja.cshtml` ADA, `@model List<HcPortal.Models.RiwayatAttemptViewModel>`, mengandung "Jawaban Saya", "Tidak Lulus", "Percobaan saat ini", dan tri-state status cell (`bi-check-circle-fill`/`bi-x-circle-fill`/`Menunggu`)
    - Grep: `Views/CMP/_RiwayatPekerja.cshtml` TIDAK mengandung `Html.Raw`
    - Grep: `Views/CMP/_RiwayatPekerja.cshtml` mengandung guard ScoreOnly (mis. `ViewData["HideDetail"]` atau `HideDetail`)
    - Grep: `Views/CMP/Results.cshtml` mengandung `Html.PartialAsync("_RiwayatPekerja"` dan header "Riwayat Percobaan Saya"
    - Grep: `Views/CMP/Records.cshtml` mengandung "Lihat Hasil" (rute ke Results tetap ada)
    - `dotnet build` 0 error
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>build 0 error; partial worker ter-gate ("Tidak Lulus"/"Jawaban Saya"/ScoreOnly hide-detail) dirender di Results; rute Records→Results utuh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playwright smoke leak-safety @5270 + UAT runtime (lesson 354/413)</name>
  <files>tests/e2e/retake-worker-407.spec.ts, tests/sql/, docs/SEED_JOURNAL.md</files>
  <action>
    Automasi yang executor WAJIB jalankan SEBELUM memanggil checkpoint (build + e2e + UAT prep, mirror pola 406):
    1. Buat `tests/e2e/retake-worker-407.spec.ts` (mirror pola `riwayat-hc-406.spec.ts` @5270 `--workers=1`). Seed temporary via SEED_WORKFLOW (snapshot DB → insert: sesi failed+eligible+AllowAnswerReview ON, sesi cap-habis, sesi cooldown-aktif, + AssessmentAttemptHistory/Archive → catat docs/SEED_JOURNAL.md, idempotent WIPE-AND-INSERT). Spec assert:
       - **Leak-safety (KRITIS):** sesi failed+attempt-sisa+AllowAnswerReview ON → DOM cabang feedback TIDAK mengandung "(Jawaban Benar)", TIDAK ada `.list-group-item-success`, TIDAK ada teks CorrectAnswer; ADA badge verdict ✓/✗ + "Jawaban Anda".
       - **Control:** tombol "Ujian Ulang" tampil saat eligible; sesi cap-habis → alert "Batas percobaan tercapai" tanpa tombol; sesi cooldown-aktif → tombol disabled + `#retakeCountdown` ticking.
       - **Riwayat:** card "Riwayat Percobaan Saya" tampil; accordion expand; tri-state status; badge "Percobaan saat ini".
       - **No JS error:** `page.on('pageerror')` tak menangkap uncaught error saat load + buka modal (lesson 413).
    2. `dotnet build` 0 error + `dotnet test HcPortal.Tests` (full suite hijau, no regresi 405/406).
    3. RESTORE DB lokal pasca-test; tandai SEED_JOURNAL `cleaned`.
    Commit spec + seed SQL + journal. Lalu pause untuk human UAT (di bawah).
  </action>
  <what-built>
    Worker self-service UI lengkap di Results.cshtml (407-03 Task 1+2): tier 3-state leak-safe, retake control (tombol/counter/cooldown/lock), modal antiforgery, riwayat card ter-gate. Plus spec Playwright `retake-worker-407.spec.ts` smoke leak-safety @5270 dan full xUnit suite hijau.
  </what-built>
  <how-to-verify>
    1. Jalankan app @5270 (branch ITHandoff): `dotnet run --urls http://localhost:5270` (JANGAN commit launchSettings).
    2. Login sebagai pekerja (lihat MEMORY reference_dev_credentials) yang punya sesi assessment GAGAL dengan AllowRetake ON + AllowAnswerReview ON + attempt tersisa.
    3. Buka /CMP/Results/{id}: pastikan (a) tombol "Ujian Ulang" tampil (atau disabled+countdown bila cooldown), (b) feedback menampilkan ✓/✗ per-soal + "Jawaban Anda" TAPI TIDAK menampilkan kunci jawaban / "(Jawaban Benar)" / pembahasan, (c) card "Riwayat Percobaan Saya" tampil.
    4. Klik "Ujian Ulang" → modal konfirmasi muncul → "Ya, Ujian Ulang" → diarahkan ke StartExam (token diminta ulang).
    5. Cek sesi cap-habis → alert "Batas percobaan tercapai, hubungi HC" tanpa tombol.
    6. Konfirmasi `dotnet test HcPortal.Tests` full hijau + spec `retake-worker-407` GREEN @5270.
  </how-to-verify>
  <verify>
    <automated>dotnet build && dotnet test HcPortal.Tests && npx playwright test tests/e2e/retake-worker-407.spec.ts --workers=1</automated>
  </verify>
  <done>spec retake-worker-407 GREEN @5270 (leak-safety DOM + control + riwayat + no-pageerror); full xUnit hijau; DB di-RESTORE (journal cleaned); human approved.</done>
  <resume-signal>Ketik "approved" bila leak-safe + control + riwayat + modal benar di browser, ATAU jelaskan isu (mis. kunci bocor / countdown mati / modal tak submit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (Razor render) → browser (worker) | View merender feedback per-soal; saat masih bisa ulang, kunci jawaban TIDAK BOLEH ikut terkirim ke DOM (leak surface). |
| browser → CMP/RetakeExam (form modal POST) | Form modal memicu mutasi destruktif; harus membawa antiforgery token. |
| client JS (countdown) → button state | Countdown enable/disable tombol = UX; server (407-02) tetap otoritas re-cek. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-407-leak | Information Disclosure | `ShowWrongFlagsOnly` branch di Results.cshtml + `_RiwayatPekerja` drill-down | mitigate | Branch ShowWrongFlagsOnly TIDAK merender `list-group-item-success`/"(Jawaban Benar)"/`CorrectAnswer` (Task 1, grep-verified + DOM Playwright smoke Task 3). Arsip riwayat struktural verdict-only (tak ada kunci). Divalidasi DOM real-browser (lesson 354/413). |
| T-407-csrf | Tampering | form modal POST RetakeExam | mitigate | `@Html.AntiForgeryToken()` di form modal (Task 1); endpoint `[ValidateAntiForgeryToken]` (407-02). |
| T-407-bypass | Tampering | countdown JS enable tombol | mitigate | Countdown JS non-authoritative; tombol enable hanya UX — server re-cek `CanRetakeAsync` di POST (407-02). View tak punya gate-power. |
| T-407-xss | Tampering | QuestionText/UserAnswer/AnswerText/option text | mitigate | Razor `@` auto-encode; ZERO `Html.Raw` untuk user-content (grep-verified partial); countdown JS pakai `.textContent`/`.setAttribute`. |
| T-407-jsabort | Tampering (DoS handler) | countdown/modal JS ReferenceError abort handler-attach | mitigate | Guard `if(!btn) return;` + Playwright `pageerror` assert (Task 3). Lesson 413: runtime-smoke render-check tak cukup → real-browser. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests` full suite hijau (no regresi 405/406 — per-wave-merge sampling).
- Playwright `tests/e2e/retake-worker-407.spec.ts` GREEN @5270 `--workers=1` (leak-safety DOM + control + riwayat + no-pageerror).
- DB lokal di-RESTORE pasca-test; SEED_JOURNAL `cleaned`.
- Human checkpoint approved (leak-safe + control + riwayat + modal di browser).
</verification>

<success_criteria>
- Tier feedback 3-state di Results.cshtml; `ShowWrongFlagsOnly` LEAK-SAFE (DOM-verified tanpa kunci).
- Retake control: tombol "Ujian Ulang" + counter + cooldown countdown disabled→enable + lock alert cap-habis.
- Modal konfirmasi destruktif ber-antiforgery → POST RetakeExam → StartExam.
- Partial `_RiwayatPekerja` ter-gate ("Tidak Lulus"/"Jawaban Saya"/ScoreOnly hide) dirender di Results; Records→Results utuh.
- Playwright smoke leak-safety @5270 GREEN; full suite hijau; 0 migration.
</success_criteria>

<output>
After completion, create `.planning/phases/407-worker-self-service-gating-tier-feedback-riwayat-pekerja/407-03-SUMMARY.md`
</output>
