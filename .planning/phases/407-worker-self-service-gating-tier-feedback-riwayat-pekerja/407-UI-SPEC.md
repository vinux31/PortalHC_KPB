---
phase: 407
slug: worker-self-service-gating-tier-feedback-riwayat-pekerja
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-22
---

# Phase 407 — UI Design Contract — Worker Self-Service + Gating Tier Feedback + Riwayat Pekerja

> Visual and interaction contract for Phase 407 (milestone v32.4 Ujian Ulang / Attempt-Retake).
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Design system state: EXISTING / MATURE — REUSE.** Brownfield ASP.NET Core MVC app on
> Bootstrap 5.3.0 + Bootstrap Icons 1.10.0 + Razor. There is NO design-token system to invent.
> This contract documents the EXISTING conventions and specifies, prescriptively, how the new
> worker-side surfaces (retake CTA + cooldown countdown + lock + tier-gated feedback + worker
> riwayat) conform to them by mirroring already-shipped components — including the **sibling
> Phase 406 contract** (`406-UI-SPEC.md`) whose riwayat accordion and tri-state verdict
> conventions this phase reuses, GATED. Do NOT introduce new colors, fonts, spacing scales, or
> libraries.

**Surfaces in scope (worker-facing only):**
1. **Retake control block** — `Views/CMP/Results.cshtml` (action-buttons area `:420-431` + result summary card `:43-105`): "Ujian Ulang" button, "Percobaan ke-X dari N" counter, live cooldown countdown (disabled button), lock alert ("hubungi HC"). *(RTK-10)*
2. **Confirmation modal** — `Views/CMP/Results.cshtml` (new Bootstrap modal, mirror existing modal idiom): destructive-action confirm before POST `CMP/RetakeExam`. *(RTK-09 / D-02)*
3. **Tier-gated answer feedback** — `Views/CMP/Results.cshtml` (review block `:316` + else `:413`): expand the boolean `AllowAnswerReview` into a 3-state tier (`showFullReview` / `showWrongFlagsOnly` / `showScoreOnly`). *(RTK-11 / D-03)*
4. **Worker riwayat (own attempts)** — `Views/CMP/Results.cshtml` + `Views/CMP/Records.cshtml`: list of own attempts (score / pass-fail / date / current-marker) with per-soal drill-down that OBEYS the same tier gating as §3. *(RTK-12 / D-04)*

Out of scope (Phase 408): full Playwright lifecycle test + security audit (RTK-14). Out of scope generally: schema/migration (0), admin config UI (406), grading-engine changes.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (brownfield, no preset automation) | shadcn gate N/A — not React/Next/Vite; no `components.json`/`tailwind.config.*`/`postcss.config.*`/`package.json` at repo root (verified) |
| Preset | not applicable | — |
| Component framework | **Bootstrap 5.3.0** (CSS + `bootstrap.bundle.min.js`) | `Views/Shared/_Layout.cshtml:38,246` |
| Component library | none (Bootstrap classes only — `card`, `modal`, `accordion`, `alert`, `table`, `badge`, `btn`, `list-group`) | `_Layout.cshtml` |
| Icon library | **Bootstrap Icons 1.10.0** (`bi bi-*` font classes) | `_Layout.cshtml:39` |
| Font | Bootstrap default system stack (no custom `font-family`) — inherit, do not override | `_Layout.cshtml` |
| View engine | Razor (`.cshtml`), `@`-encoded output by default | `Views/CMP/Results.cshtml`, `Views/CMP/Records.cshtml` |
| Project CSS | `wwwroot/css/site.css` + page-specific (`records.css`) — **do not add new CSS for this phase**; Bootstrap utilities cover everything | `wwwroot/css/` |
| Locale | Bahasa Indonesia; dates `dd MMM yyyy HH:mm` `CultureInfo.GetCultureInfo("id-ID")` (mirror `Results.cshtml:97`) | existing views |

**Registry safety:** Not applicable. No shadcn, no third-party UI registry. All markup is hand-written Razor + Bootstrap utility classes. No vetting gate required.

---

## Spacing Scale

Use Bootstrap 5.3 spacing utilities ONLY (already a 4px-based scale: `0=0, 1=4px, 2=8px, 3=16px, 4=24px, 5=48px`). All values are multiples of 4 by construction. Do NOT hand-roll `style="margin:..."`.

| Token (Bootstrap) | Pixels | Usage in this phase |
|-------------------|--------|---------------------|
| `*-1` | 4px | Icon-to-text gap inside buttons/badges (`me-1`), inline `gap-1` |
| `*-2` | 8px | Button-cluster gap (`d-flex gap-2` action row `:421`), badge spacing (`ms-2`), countdown icon gap (`me-2`) |
| `*-3` | 16px | Helper/counter bottom gap (`mb-3`), accordion item inner blocks, alert internal spacing |
| `*-4` | 24px | Card / alert bottom margin (`mb-4`, mirror `Results.cshtml:318` cards and `:415` alerts), action-buttons block (`d-flex gap-2 mb-4`) |
| `py-3` / `py-4` / `p-4` | 16/24px | Card body / score-tile padding (`p-4` mirror `:52`), section vertical padding |

Exceptions: none. Reuse the EXACT spacing utilities the existing Results/Records surfaces and the Phase 406 riwayat accordion already use (`card shadow-sm mb-4`, `card-header bg-light`, `card-body`, `alert ... mb-4`, `d-flex gap-2 mb-4`, `accordion-body p-0`, `table table-sm align-middle mb-0`).

---

## Typography

No custom typography. Reuse Bootstrap defaults + the heading classes already used by mirror components. Two weights only, matching the existing app.

| Role | Class / Element | Size (Bootstrap default) | Weight | Usage |
|------|-----------------|--------------------------|--------|-------|
| Page heading | `<h2>` (`Results.cshtml:18`) | ≈32px | semibold (heading default) | "Hasil Assessment" page title — unchanged |
| Score figure | `<h2 class="mb-1">` inside score tile (`:54`) | ≈32px | semibold | "@Model.Score%" — unchanged |
| Card / section title | `<h5 class="mb-0">` (review card `:320`) / `<h4 class="mb-0">` (summary header `:46`) | h4≈26px / h5≈20px | semibold | "Tinjauan Jawaban", "Riwayat Percobaan Saya" card headers |
| Modal title | `<h5 class="modal-title">` | ≈20px | semibold | "Konfirmasi Ujian Ulang" |
| Question heading | `<h6 class="mb-2">` (review item `:329`) | ≈16px | semibold (`<strong>` for "Soal N:") | per-soal heading in feedback / riwayat drill-down |
| Body / table | default `<td>`, `<p>`, `<span>` | 16px (1rem) | regular (400) | counter text, accordion rows, per-soal cells, option text |
| Counter / status badge text | `badge ... fs-5` (status `:69`) | ≈20px (fs-5) | semibold (badge) | LULUS/TIDAK LULUS status badge — unchanged |
| Helper / muted | `.text-muted`, `.small`, `<small class="text-muted">` (`:55,:97`) | ≈14px (0.875em) | regular (400) | "Percobaan ke-X dari N", cooldown countdown text, timestamps, "(Jawaban Anda)" |

Line height: inherit Bootstrap defaults (body ~1.5, headings ~1.2). Do NOT set custom line-height.

**Weight rule:** only two weights appear — regular (400) for body/helper/counter, semibold for headings, badge text, and `<strong>` emphasis the existing Results surface already uses. Do not introduce a third weight.

---

## Color

No custom palette. Use Bootstrap 5.3 semantic contextual classes EXACTLY as the existing Results/Records surfaces and the Phase 406 riwayat accordion do. The "10% accent" in this brownfield context = Bootstrap `primary` (used sparingly for the primary CTA), and semantic `success`/`danger`/`warning`/`info` reserved for STATE meaning only.

| Role | Bootstrap class | Usage (this phase) |
|------|-----------------|--------------------|
| Dominant surface (60%) | page/card background (`bg-white`, default body) | Results/Records page background, modal background |
| Secondary (30%) | `card shadow-sm`, `card-header bg-light`, `accordion` chrome | result card, feedback card, riwayat card, accordion item headers |
| Accent (10%) — primary | `btn btn-primary` | **Retake CTA** "Ujian Ulang" (`btn btn-primary`), confirm-in-modal CTA "Ya, Ujian Ulang" — and the existing "Lihat Sertifikat" primary CTA (`:424`, unchanged) |
| Success | `bg-success` / `text-bg-success` / `text-success` | Pass status badge "Lulus", per-soal correct ✓ (`text-success`), correct-option highlight (`list-group-item-success`) — feedback shown ONLY when tier permits |
| Danger | `bg-danger` / `text-bg-danger` / `text-danger` | Fail status badge "Tidak Lulus", per-soal wrong ✗ (`text-danger`), wrong-selected-option highlight (`list-group-item-danger`) |
| Warning | `alert alert-warning` | **Lock state** ("Batas percobaan tercapai, hubungi HC") + cooldown-active notice tone (alternatively `alert-secondary` muted — see Components §1) |
| Info | `alert alert-info` / `badge bg-info` | "Percobaan saat ini" current-attempt marker badge (mirror 406), "Tinjauan jawaban tidak tersedia" notice (`:415`, unchanged), pending-grading banner (`:37`, unchanged) |
| Muted | `text-muted` | counter "Percobaan ke-X dari N", cooldown countdown, timestamps, empty-state, pending-essay "Menunggu", concealed-answer-key explanatory note |

**Accent reserved for (explicit list — never "all interactive elements"):**
- `btn btn-primary` — the **single retake CTA** "Ujian Ulang" (in result card / action row) and its in-modal confirm twin "Ya, Ujian Ulang", plus the pre-existing "Lihat Sertifikat" CTA (unchanged).
- All other buttons use `btn-outline-secondary` / `btn-secondary` (modal cancel "Batal", "Kembali" `:428`, accordion triggers) — NOT primary.

**Semantic-color discipline:** `success`/`danger`/`warning`/`info` are reserved for STATE meaning (pass/fail/lock/cooldown/current-attempt), never decoration. Do not use them on neutral chrome. The retake CTA is `primary` (an action), NOT `success` — passing is not what the button means.

**Leak-safe color rule (D-03):** while a retake remains possible (`showWrongFlagsOnly`), the answer-key colorings `list-group-item-success` (correct option) and the "(Jawaban Benar)" label MUST NOT render. Only the worker's own selection + the per-soal ✓/✗ verdict colors appear. Color is never the only signal (always paired with glyph + text — see Accessibility).

---

## Copywriting Contract

All copy in **Bahasa Indonesia**, matching existing app tone (concise, sentence case for body, existing UPPERCASE only where the app already uppercases status badges). XSS rule: user-supplied content (`QuestionText`, worker answer `UserAnswer`/`AnswerText`, option text) is rendered via Razor `@`-default encoding or DOM `.textContent` — **NEVER `Html.Raw`** for user content. (Note: the existing chart `@Html.Raw(Json.Serialize(...))` at `:272-273` serializes app-controlled label/number arrays and is out of scope — do not extend that pattern to user free-text.)

### Retake control block (Results.cshtml — RTK-10)

| Element | Copy |
|---------|------|
| Primary CTA (eligible) | `Ujian Ulang` (icon `bi bi-arrow-repeat me-1`) |
| Attempt counter | `Percobaan ke-@currentAttempt dari @maxAttempts` (e.g. "Percobaan ke-2 dari 3") rendered as `<span class="text-muted small">` near the CTA |
| Cooldown — disabled button label | `Bisa ulang dalam <span id="retakeCountdown">HH:MM:SS</span>` (button `disabled`, icon `bi bi-hourglass-split me-1`); JS ticks the `#retakeCountdown` text down to `00:00:00` |
| Cooldown — supporting note (optional, near button) | `Ujian ulang tersedia setelah masa jeda berakhir.` (`text-muted small`) |
| Cooldown — reached-zero hint | when countdown hits `00:00:00`: enable button + swap label to `Ujian Ulang`; if poll not used, show `text-muted small`: `Masa jeda selesai — muat ulang halaman untuk mengulang.` (planner's choice: auto-enable vs reload prompt — D-01 discretion) |
| Lock (cap reached) | `alert alert-warning` (`role="alert"`): `Batas percobaan tercapai (@maxAttempts dari @maxAttempts). Hubungi HC bila perlu kesempatan tambahan.` (icon `bi bi-lock-fill me-2`) — NO button rendered |
| Not eligible — passed | (no retake control rendered; existing "Selamat! Anda lulus" success alert `:117` stands) |
| Not eligible — pending grading | (no retake control; existing pending banner `:37` stands; retake requires graded `IsPassed==false`) |
| Not eligible — review disabled by config but failed+attempts-left | retake CTA still renders (eligibility is independent of `AllowAnswerReview`); only the per-soal feedback is suppressed (see Tier §below) |

### Confirmation modal (Results.cshtml — D-02)

| Element | Copy |
|---------|------|
| Modal title | `Konfirmasi Ujian Ulang` (icon `bi bi-arrow-repeat me-2`) |
| Modal body | `Percobaan saat ini akan diarsipkan dan kamu mulai ujian dari awal. Skor saat ini tetap tersimpan di riwayat, tetapi sesi baru menggantikan hasil aktif. Lanjutkan?` |
| Confirm CTA | `Ya, Ujian Ulang` (`btn btn-primary`, icon `bi bi-arrow-repeat me-1`) — submits the antiforgery POST form |
| Cancel | `Batal` (`btn btn-secondary` `data-bs-dismiss="modal"`) |

### Tier-gated feedback (Results.cshtml — RTK-11 / D-03)

| Tier | When | Heading / copy |
|------|------|----------------|
| `showFullReview` | `AllowAnswerReview==true` AND (passed OR attempts exhausted) | existing "Tinjauan Jawaban" card (`:316`) UNCHANGED — score + ✓/✗ + own answer + answer key + explanation |
| `showWrongFlagsOnly` | `AllowAnswerReview==true` AND (failed AND attempts remaining) | Card header `Tinjauan Jawaban` (icon `bi bi-list-check me-2`); under it, an explanatory note `alert alert-info` (`role="status"`): `Kunci jawaban disembunyikan selama kamu masih punya kesempatan ujian ulang. Kamu bisa melihat soal mana yang benar atau salah, tetapi jawaban yang benar baru tampil setelah lulus atau percobaan habis.` Per-soal: question text + your-answer + ✓/✗ verdict badge ONLY; NO correct-option highlight, NO "(Jawaban Benar)", NO explanation/`CorrectAnswer` text |
| `showScoreOnly` | `AllowAnswerReview==false` (any retake status) | existing notice (`:415`) UNCHANGED: `alert alert-info`: `Tinjauan jawaban tidak tersedia untuk assessment ini.` — score card only, no per-soal detail at all |

### Worker riwayat (Results.cshtml + Records.cshtml — RTK-12 / D-04)

| Element | Copy |
|---------|------|
| Section/card header | `Riwayat Percobaan Saya` (icon `bi bi-clock-history me-2`) |
| Accordion item header | `Percobaan ke-{N}: {skor}% — {Lulus/Tidak Lulus} — {dd MMM yyyy HH:mm}` (badge `text-bg-success` "Lulus" / `text-bg-danger` "Tidak Lulus" / `bg-warning text-dark` "Menunggu Penilaian" for `IsPassed==null`) |
| Current-attempt marker | badge `Percobaan saat ini` (`badge bg-info ms-2`) — driven by `IsCurrentAttempt` flag (RTK-12) |
| Per-soal table headers | `No` · `Soal` · `Jawaban Saya` · `Status` · `Skor` (note "Jawaban Saya" — worker's own wording; HC partial uses "Jawaban Peserta") |
| Status correct | `<i class="bi bi-check-circle-fill text-success" title="Benar">` + `<span class="visually-hidden">Benar</span>` |
| Status wrong | `<i class="bi bi-x-circle-fill text-danger" title="Salah">` + `<span class="visually-hidden">Salah</span>` |
| Status pending essay (IsCorrect null) | `<span class="text-muted" title="Menunggu penilaian">—</span>` + `<span class="visually-hidden">Menunggu</span>` |
| Answer-key concealment note (per attempt still retake-eligible) | when an attempt's group still permits retake → above/within the drill-down: `text-muted small`: `Kunci jawaban disembunyikan selama masih ada kesempatan ujian ulang.` |
| Records.cshtml row trigger (per Assessment Online row) | dropdown/inline button `Riwayat Percobaan` (icon `bi bi-clock-history me-1`) OR rely on the existing "Lihat Hasil" → Results page where the riwayat section lives (planner's choice — D-04 discretion) |
| Empty (no archived attempts) | `text-muted text-center py-4`: `Belum ada riwayat percobaan. Riwayat akan muncul setelah kamu mengulang ujian.` |
| Empty per-soal (attempt has no archived responses) | `text-muted text-center py-3`: `Tidak ada rincian jawaban tersimpan untuk percobaan ini.` |

**Error state:** Retake POST failures surface via existing PRG `TempData`/redirect convention. On `RetakeExam` server-side rejection (eligibility lost between render and POST — server is authoritative per D-01), redirect back to Results with a flash `alert alert-warning` (`role="alert"`): `Ujian ulang tidak bisa dijalankan saat ini. Coba muat ulang halaman atau hubungi HC.` If worker riwayat is AJAX-loaded (planner's choice), on fetch failure show inline `alert alert-warning`: `Gagal memuat riwayat. Coba lagi.` (mirror 406 convention).

**Destructive actions:** ONE — triggering a retake (`CMP/RetakeExam`) archives the current attempt and resets the live session to a fresh start. Confirmation: the **mandatory modal** above (D-02). The actual write is a server-side antiforgery POST (`[ValidateAntiForgeryToken]` + ownership + `CanRetakeAsync` re-check); the modal is a click-safety gate, the server is the authoritative guard.

---

## Component Contracts

> These are PRESCRIPTIVE markup contracts. Executor implements them against the cited mirror lines. All classes are real and already in the codebase. **Tier and eligibility values come from the ViewModel** (`AssessmentResultsViewModel`) — the view renders; it does NOT compute eligibility (that is `RetakeRules`/`RetakeService` per CONTEXT, surfaced into the VM in `CMPController.Results`). Recommended VM additions (planner finalizes): `RetakeMode` enum (`ShowFullReview` / `ShowWrongFlagsOnly` / `ShowScoreOnly`) replacing the bare `AllowAnswerReview` boolean at the view level; `CanRetake` (bool), `CurrentAttempt` (int), `MaxAttempts` (int), `CooldownUntilUtc` (DateTime?), `IsCapReached` (bool). Do NOT delete `AllowAnswerReview` — it feeds the tier (Established Patterns, CONTEXT).

### 1. Retake control block — `Results.cshtml` action area (`:420-431`)

Place inside / adjacent to the existing `<div class="d-flex gap-2 mb-4">` action row, OR as a dedicated card section directly under the result summary card (`:105`). Mirror the existing button idiom (`btn ... me-1` + `bi` icon).

```
@* Retake control — rendered from VM flags; server is authoritative *@
@if (Model.IsCapReached)
{
    <div class="alert alert-warning mb-4" role="alert">
        <i class="bi bi-lock-fill me-2"></i>
        Batas percobaan tercapai (@Model.MaxAttempts dari @Model.MaxAttempts). Hubungi HC bila perlu kesempatan tambahan.
    </div>
}
else if (Model.CanRetake)
{
    <div class="d-flex align-items-center gap-2 mb-3 flex-wrap">
        @if (Model.CooldownUntilUtc.HasValue && Model.CooldownUntilUtc > DateTime.UtcNow)
        {
            <button type="button" class="btn btn-primary" id="btnRetake" disabled
                    data-cooldown-until="@Model.CooldownUntilUtc.Value.ToString("o")">
                <i class="bi bi-hourglass-split me-1"></i>Bisa ulang dalam <span id="retakeCountdown">--:--:--</span>
            </button>
        }
        else
        {
            <button type="button" class="btn btn-primary" id="btnRetake"
                    data-bs-toggle="modal" data-bs-target="#retakeConfirmModal">
                <i class="bi bi-arrow-repeat me-1"></i>Ujian Ulang
            </button>
        }
        <span class="text-muted small">Percobaan ke-@Model.CurrentAttempt dari @Model.MaxAttempts</span>
    </div>
}
```

**Interaction rules:**
- **Live countdown (D-01):** inline `<script>` reads `data-cooldown-until` (ISO-8601 UTC, round-trip `"o"`), ticks `#retakeCountdown` every 1s as `HH:MM:SS`. On reaching `<= 0`: clear the interval and either auto-enable+relabel the button to "Ujian Ulang" wired to the modal, OR (planner's choice) prompt reload. Mirror existing exam-timer `setInterval` idiom.
- **Server authoritative:** countdown is UX only; `CMP/RetakeExam` re-checks `CanRetakeAsync` on POST and rejects if cooldown/cap not actually satisfied (D-01).
- **Cap reached → `alert-warning` lock, NO button** (RTK-10).
- The CTA is `btn btn-primary` (action color), never `btn-success`.

### 2. Confirmation modal — `Results.cshtml` (new, near `_ImageLightboxModal` partial `:434`)

ONE modal per page. Mirror the standard Bootstrap modal idiom used elsewhere in the app (header / body / footer; `btn-close` with `aria-label="Tutup"`). The confirm button submits the antiforgery POST form to `CMP/RetakeExam`.

```
<div class="modal fade" id="retakeConfirmModal" tabindex="-1" aria-labelledby="retakeConfirmLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="retakeConfirmLabel"><i class="bi bi-arrow-repeat me-2"></i>Konfirmasi Ujian Ulang</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Tutup"></button>
      </div>
      <div class="modal-body">
        Percobaan saat ini akan diarsipkan dan kamu mulai ujian dari awal. Skor saat ini tetap tersimpan di riwayat, tetapi sesi baru menggantikan hasil aktif. Lanjutkan?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
        <form method="post" asp-action="RetakeExam" asp-controller="CMP" asp-route-id="@Model.AssessmentId" class="d-inline">
          @Html.AntiForgeryToken()
          <button type="submit" class="btn btn-primary"><i class="bi bi-arrow-repeat me-1"></i>Ya, Ujian Ulang</button>
        </form>
      </div>
    </div>
  </div>
</div>
```

**Rules:** the POST form carries `@Html.AntiForgeryToken()` (RTK-09). Ownership + `CanRetakeAsync` re-check + `TempData[TokenVerified_{id}]` clear + redirect to `StartExam(id)` happen server-side in the controller (Component contract does not implement controller logic). The modal NEVER submits without explicit "Ya, Ujian Ulang" (anti-accidental-click, D-02).

### 3. Tier-gated feedback — `Results.cshtml` review block (`:316` / `:413`)

Expand the current boolean branch into three. **Do not delete the existing full-review markup** (`:316-411`) — it becomes the `ShowFullReview` branch. Add a `ShowWrongFlagsOnly` branch that renders the SAME question list but suppresses answer-key signals; keep the existing `else` (`:413-418`) as `ShowScoreOnly`.

```
@switch (Model.RetakeMode)   // ShowFullReview | ShowWrongFlagsOnly | ShowScoreOnly
{
    case RetakeReviewMode.ShowFullReview:
        @* EXISTING markup :318-411 verbatim — full review with answer key *@
        break;

    case RetakeReviewMode.ShowWrongFlagsOnly:
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-light">
                <h5 class="mb-0"><i class="bi bi-list-check me-2"></i>Tinjauan Jawaban</h5>
            </div>
            <div class="card-body">
                <div class="alert alert-info" role="status">
                    <i class="bi bi-eye-slash me-2"></i>Kunci jawaban disembunyikan selama kamu masih punya kesempatan ujian ulang. Kamu bisa melihat soal mana yang benar atau salah, tetapi jawaban yang benar baru tampil setelah lulus atau percobaan habis.
                </div>
                @foreach (var q in Model.QuestionReviews)
                {
                    <div class="border rounded p-3 mb-3">
                        <div class="d-flex justify-content-between align-items-start">
                            <h6 class="mb-2"><strong>Soal @q.QuestionNumber:</strong> @q.QuestionText</h6>
                            @if (q.IsEssayPending) { <span class="badge text-bg-secondary"><i class="bi bi-hourglass-split me-1"></i>Menunggu Penilaian</span> }
                            else if (q.IsCorrect) { <span class="badge text-bg-success"><i class="bi bi-check-circle-fill me-1"></i>Benar</span> }
                            else { <span class="badge text-bg-danger"><i class="bi bi-x-circle-fill me-1"></i>Salah</span> }
                        </div>
                        @* OWN answer only — NO correct-option highlight, NO "(Jawaban Benar)", NO CorrectAnswer/explanation *@
                        @if (!string.IsNullOrEmpty(q.UserAnswer))
                        {
                            <div class="small"><span class="text-muted">Jawaban Anda:</span> @q.UserAnswer</div>
                        }
                    </div>
                }
            </div>
        </div>
        break;

    case RetakeReviewMode.ShowScoreOnly:
        <div class="alert alert-info mb-4">
            <i class="bi bi-info-circle me-2"></i>Tinjauan jawaban tidak tersedia untuk assessment ini.
        </div>
        break;
}
```

**Leak-safe rules (D-03 — security-critical):**
- In `ShowWrongFlagsOnly`, the per-option loop (`:357-393`) and its `list-group-item-success` / `icon = "bi-check-circle-fill"` / `"(Jawaban Benar)"` markers MUST NOT render. Show the worker's selected option text + the per-soal ✓/✗ verdict ONLY.
- The essay branch (`:397-406`) MUST NOT render `@question.CorrectAnswer` (the rubric/key) in this tier — only `@question.UserAnswer`.
- Answer key reappears ONLY in `ShowFullReview` (passed OR exhausted). The tier value is computed server-side; the view trusts the VM.

### 4. Worker riwayat — `Results.cshtml` (section under feedback) + `Records.cshtml` (trigger)

Reuse the **Phase 406 accordion shape** (`_RiwayatPercobaan.cshtml`) but render a **gated worker variant** — do NOT include the HC partial verbatim (it is full-leak). Per CONTEXT D-04, the per-soal drill-down obeys the same tier as §3 for each attempt's assessment group.

```
@* Worker riwayat card — newest attempt first, current marked *@
<div class="card shadow-sm mb-4">
  <div class="card-header bg-light">
    <h5 class="mb-0"><i class="bi bi-clock-history me-2"></i>Riwayat Percobaan Saya</h5>
  </div>
  <div class="card-body">
    @if (Model.RiwayatAttempts == null || !Model.RiwayatAttempts.Any())
    {
        <div class="text-muted text-center py-4">Belum ada riwayat percobaan. Riwayat akan muncul setelah kamu mengulang ujian.</div>
    }
    else
    {
        <div class="accordion" id="riwayatPekerjaAccordion">
          @* for each attempt (descending AttemptNumber): mirror 406 accordion-item *@
          @* header: Percobaan ke-{N}: {skor}% — <badge Lulus/Tidak Lulus/Menunggu Penilaian> — {tanggal} [+ badge bg-info "Percobaan saat ini" if IsCurrentAttempt] *@
          @* body: table table-sm table-hover align-middle mb-0 — headers: No / Soal / Jawaban Saya / Status / Skor *@
          @* Status cell: tri-state ✓/✗/— with visually-hidden text (mirror 406) *@
          @* GATING: if this attempt's group still retake-eligible → drill-down shows verdict only (no key); if passed/exhausted → may show full (subject to AllowAnswerReview) *@
        </div>
    }
  </div>
</div>
```

**Rules:**
- **Reuse `RiwayatUnifier.Build(...)` + `RiwayatAttemptViewModel`** (CONTEXT: gating added at view/VM layer, NOT in the pure unifier).
- **Header badges:** `text-bg-success`="Lulus", `text-bg-danger`="Tidak Lulus", `bg-warning text-dark`="Menunggu Penilaian" (mirror 406 partial `:27-38`). Worker copy uses "Tidak Lulus" (consistent with Results status badge `:82` and Records `:222`), not "Gagal".
- **Current attempt:** `badge bg-info ms-2` "Percobaan saat ini" — driven by `IsCurrentAttempt` (RTK-12 adds this flag to `AllWorkersHistoryRow`).
- **Gating (D-04):** the per-soal table inside each attempt obeys the SAME tier logic as §3 for that attempt's assessment group. While retake remains possible → ✓/✗ + own answer, NO key. Passed/exhausted → key may appear (subject to `AllowAnswerReview`).
- **Records.cshtml:** the existing "Lihat Hasil" link (`:262`) already routes to Results where this section lives — sufficient by default. A per-row "Riwayat Percobaan" trigger is OPTIONAL (planner's choice, D-04 discretion); if added, place in the existing Aksi cell (`:260-283`) as `btn btn-sm btn-outline-secondary` with `bi bi-clock-history` + `title`/`aria-label`.
- **Placement vs modal:** inline accordion card on Results is the default; a modal variant is acceptable (CONTEXT discretion) but MUST reuse the same gated markup.

**XSS (D-02 / RTK-11 leak-safety):** `QuestionText`, `UserAnswer`/`AnswerText`, option text rendered with Razor `@` (auto HTML-encode). If any body is built client-side, use `.textContent`/`.setAttribute`, never `innerHTML` with raw user strings. NEVER `Html.Raw` for user content.

---

## Accessibility

| Element | Requirement |
|---------|-------------|
| Retake CTA | `<button type="button">` (not `<div>`); keyboard-activatable; disabled state uses native `disabled` attribute (announced + non-focusable per Bootstrap). Icon paired with text label — never icon-only for the primary action. |
| Cooldown countdown | the disabled button text includes the human countdown ("Bisa ulang dalam HH:MM:SS"); consider `aria-live="off"` (avoid spamming SR every second). When it enables, the label change to "Ujian Ulang" is meaningful and re-focusable. |
| Lock alert | `role="alert"` so it is announced when rendered; conveys cap via text ("Batas percobaan tercapai"), not color alone. |
| Confirmation modal | `tabindex="-1"`, `aria-labelledby="retakeConfirmLabel"`, `aria-hidden="true"`; `btn-close` has `aria-label="Tutup"`; `modal-dialog-centered`. Bootstrap manages focus trap + Esc-to-close + return-focus to trigger. The destructive confirm is a distinct button ("Ya, Ujian Ulang") separated from "Batal". |
| Tier note (wrong-flags-only) | `role="status"` (polite) info alert announces that the answer key is concealed — meaning conveyed by text + `bi-eye-slash` glyph, not color alone. |
| Per-soal verdict (✓/✗/—) | icon-only cells include a text equivalent (`title` + `<span class="visually-hidden">Benar/Salah/Menunggu</span>`) so meaning is not color-only. Distinct glyph shapes reinforce color (mirror 406). |
| Verdict badges (Benar/Salah/Menunggu) | the existing review badges already carry a text label inside the badge (`:342,:348,:336`) — keep the label; do not reduce to color/icon alone. |
| Riwayat accordion | `accordion-button` has `aria-expanded` + `aria-controls`; collapse panel `id` matches `aria-controls`; first item expanded (`show`). Bootstrap handles Enter/Space toggle (mirror 406 `:23-25,:46`). |
| Status / result badges | "Lulus"/"Tidak Lulus"/"Menunggu Penilaian" carry a text label inside the badge, not color alone (mirror existing `:75-83`). |
| Color independence | every state (pass/fail/lock/cooldown/current/correct/wrong/pending) is reinforced by text and/or distinct glyph in addition to Bootstrap semantic color. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none (Bootstrap 5.3 + Razor only) | hand-written markup; no registry blocks | not applicable — 2026-06-22 |

No shadcn, no third-party UI registry, no `npx shadcn view`. Nothing to vet.

---

## Pre-Population Provenance

| Field group | Source |
|-------------|--------|
| D-01 live countdown + disabled button + server-authoritative | 407-CONTEXT.md D-01 |
| D-02 mandatory confirmation modal before destructive POST | 407-CONTEXT.md D-02 |
| D-03 tier feedback (ShowFullReview / ShowWrongFlagsOnly / ShowScoreOnly) keyed on `AllowAnswerReview` × passed × attempts-left; leak-safe key concealment | 407-CONTEXT.md D-03 + REQUIREMENTS RTK-11 + spec §7 |
| D-04 worker riwayat reuses RiwayatUnifier + accordion, drill-down gated (NOT HC full-leak partial) | 407-CONTEXT.md D-04 + REQUIREMENTS RTK-12 |
| Eligibility / counter / lock copy (RTK-10) | REQUIREMENTS RTK-10 + ROADMAP Phase 407 SC#2 + 407-CONTEXT specifics |
| Retake CTA + modal + form markup idiom | mirror `Results.cshtml:420-431` action area + standard app modal idiom + 406 modal `406-UI-SPEC.md §3` |
| Tier review branch (full-review existing markup) | `Views/CMP/Results.cshtml:316-418` (existing `AllowAnswerReview` block + else) |
| Riwayat accordion shape + tri-state verdict + badges + empty states | `Views/Admin/_RiwayatPercobaan.cshtml` (406 partial) + `406-UI-SPEC.md` Components §3 |
| `IsCurrentAttempt` flag on `AllWorkersHistoryRow` | 407-CONTEXT.md domain + REQUIREMENTS RTK-12 + `Models/AllWorkersHistoryRow.cs` |
| Records row trigger placement | `Views/CMP/Records.cshtml:194-285` (Aksi cell + "Lihat Hasil" link) |
| Status/badge color conventions ("Lulus"/"Tidak Lulus") | `Results.cshtml:73-83`, `Records.cshtml:219-228` |
| Bootstrap 5.3.0 + Icons 1.10.0 | `Views/Shared/_Layout.cshtml:38,39,246` |
| Date locale `dd MMM yyyy HH:mm` id-ID | `Results.cshtml:97` |
| Copywriting (Bahasa Indonesia) | gsd-ui-researcher (matched existing app tone) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
