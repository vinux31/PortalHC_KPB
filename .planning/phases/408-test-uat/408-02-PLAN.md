---
phase: 408-test-uat
plan: 02
type: execute
wave: 2
depends_on: [408-01]
files_modified:
  - tests/e2e/retake-lifecycle-408.spec.ts
  - tests/sql/retake-lifecycle-408-seed.sql
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [RTK-14]
tags: [test, e2e, playwright, retake, lifecycle, certificate]

must_haves:
  truths:
    - "Lifecycle penuh terbukti di real-browser @5270: sesi gagal -> halaman Hasil tampil skor + check/cross TANPA kunci (ShowWrongFlagsOnly) -> klik Ujian Ulang -> modal konfirmasi -> POST RetakeExam -> redirect StartExam -> jawab benar -> submit -> lulus -> cert# muncul."
    - "Seed lifecycle ber-cooldown=0 + GenerateCertificate=1 + paket soal dengan jawaban-benar deterministik sehingga retake bisa LULUS dan cert terbit."
    - "Tidak ada uncaught JS error (pageerror) di tiap langkah lifecycle (lesson 413)."
    - "Seluruh suite xUnit (incl Integration GAP-1) + semua spec e2e retake (config/riwayat-HC/worker/lifecycle) hijau tanpa regresi; SEED_WORKFLOW snapshot->seed->restore bersih (journal cleaned)."
  artifacts:
    - path: "tests/e2e/retake-lifecycle-408.spec.ts"
      provides: "Playwright lifecycle GAP-3 (1 happy-path gagal->ulang->lulus->cert)"
      contains: "submitExamTwoStep"
      min_lines: 70
    - path: "tests/sql/retake-lifecycle-408-seed.sql"
      provides: "Seed sesi gagal cooldown=0 + paket soal jawaban-benar deterministik (prefix [RETAKE408])"
      contains: "RETAKE408"
      min_lines: 80
  key_links:
    - from: "tests/e2e/retake-lifecycle-408.spec.ts"
      to: "tests/sql/retake-lifecycle-408-seed.sql"
      via: "db.execScript(SEED_SQL) di beforeAll"
      pattern: "execScript"
    - from: "tests/e2e/retake-lifecycle-408.spec.ts"
      to: "CMP/RetakeExam -> StartExam"
      via: "klik #btnRetake -> modal submit -> waitForURL StartExam"
      pattern: "StartExam"
    - from: "tests/e2e/retake-lifecycle-408.spec.ts"
      to: "tests/e2e/helpers/examTypes.ts submitExamTwoStep"
      via: "import submitExamTwoStep"
      pattern: "submitExamTwoStep"
---

<objective>
GAP-3 (CONTEXT D-01): Bangun SATU Playwright lifecycle spec real-browser `retake-lifecycle-408.spec.ts` + seed `retake-lifecycle-408-seed.sql` yang membuktikan ALUR RETAKE YANG BENAR-BENAR DIEKSEKUSI end-to-end: gagal -> skor + tanda benar/salah tanpa kunci -> Ujian Ulang -> modal -> POST RetakeExam -> StartExam -> jawab benar -> submit -> lulus -> 1 sertifikat.

Ini GENUINE GAP: smoke 406/407 menguji per-surface dan SENGAJA menghindari exam-taking flow. Lifecycle 408 satu-satunya yang menyentuh jalur ExecuteAsync -> StartExam -> grade -> cert di browser nyata (lesson 354/413: Razor/JS/SignalR WAJIB real-browser; bug `monFlashRow` ReferenceError hanya ketangkap Playwright). Cabang lock (cap habis) & cooldown-aktif SUDAH dibuktikan smoke 407 (skenario 5 & 6) -> TIDAK diulang.

Plan ini juga berisi task regresi/UAT penutup: jalankan seluruh suite xUnit (incl GAP-1 @SQLEXPRESS) + semua spec e2e retake @5270 untuk memastikan 0 regresi + tandai SEED_JOURNAL cleaned, lalu checkpoint sign-off manusia.

Purpose: Menutup RTK-14 lapisan e2e lifecycle penuh -- bukti visual akhir bahwa fitur ujian ulang berfungsi dari kegagalan sampai terbitnya sertifikat.

Output: `tests/e2e/retake-lifecycle-408.spec.ts` + `tests/sql/retake-lifecycle-408-seed.sql` + entry `docs/SEED_JOURNAL.md` (0 production code, 0 migration).

CONSTRAINT KRITIS (CAPSTONE -- JANGAN tulis ulang test hijau): JANGAN sentuh/edit `retake-worker-407.spec.ts`, `retake-config-406.spec.ts`, `riwayat-hc-406.spec.ts`, `exam-taking.spec.ts`, helper (`examTypes.ts`/`auth.ts`/`dbSnapshot.ts`), atau seed existing. Spec+seed 408 PURELY ADDITIVE (prefix Title baru `[RETAKE408]`, file baru). REUSE helper exam-taking (`submitExamTwoStep`, label-by-text) + harness 407 (serial + db.backup/restore + login + pageerror).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/408-test-uat/408-CONTEXT.md
@.planning/phases/408-test-uat/408-RESEARCH.md
@.planning/phases/408-test-uat/408-PATTERNS.md

<project_constraints>
- Branch ITHandoff. App WAJIB jalan @5270 sebelum e2e (`E2E_BASE_URL=http://localhost:5270`; config default 5277). Saat `dotnet run` lokal set `Authentication__UseActiveDirectory=false`.
- e2e WAJIB `--workers=1` + `test.describe.configure({ mode: 'serial' })`.
- SEED_WORKFLOW (CLAUDE.md): db.backup snapshot -> db.execScript(seed) -> db.restore sesudah -> tandai journal `cleaned`. Seed = temporary + local-only, prefix `[RETAKE408]`, JANGAN promosikan ke Data/SeedData.cs. JANGAN seed/edit DB Dev/Prod -- DB lokal `HcPortalDB_Dev` saja.
- migration=FALSE. Shuffle opsi default ON (v27.0) -> pilih jawaban by-TEXT, BUKAN positional `.nth()`.
- Bahasa user-facing = Bahasa Indonesia.
</project_constraints>

<interfaces>
<!-- Helper + kontrak yang executor butuhkan -- diekstrak verbatim dari repo (VERIFIED). -->

REUSE helper existing (JANGAN re-implement):
- `tests/helpers/dbSnapshot.ts` -- `backup(path)`, `restore(path)`, `execScript(sqlPath)`, `queryScalar(sql)`, `queryString(sql)`. localhost-only guard built-in.
- `tests/helpers/auth.ts` -- `login(page, 'coachee')` (worker self-service fixture = `rino.prasetyo@pertamina.com`, dev-local; landing /Home/*). `/CMP/Results/{id}` di-gate ownership -> seed session milik fixture = akses OK.
- `tests/e2e/helpers/examTypes.ts:327-337` -- `submitExamTwoStep(page)`: reviewSubmitBtn -> ExamSummary -> "Kumpulkan Ujian" + confirm() accept -> menanti `/CMP/Results`. JANGAN hand-roll submit.

Harness analog `tests/e2e/retake-worker-407.spec.ts` (SALIN pola, ubah prefix `[RETAKE408]`):
- beforeAll (:46-82): resolve InstanceDefaultBackupPath -> `db.backup` -> `db.execScript(SEED_SQL)` -> resolve sid by Title -> `expect(sid).toBeGreaterThan(0)`.
- afterAll (:46-82): `db.restore(snapshotPath)` + unlink best-effort + re-throw restoreError.
- gotoResults pattern (:85-90): arm `page.on('pageerror', e=>errors.push(e.message))` SEBELUM nav -> `login(page,'coachee')` -> goto Results -> waitForLoadState networkidle.
- leak-safe assert (:99-114): card "Tinjauan Jawaban" + `[role="status"]` "Kunci jawaban disembunyikan" + body not.toContain '(Jawaban Benar)' + `.list-group-item-success` count 0.
- modal->StartExam (:141-147): klik `#btnRetake` -> `#retakeConfirmModal` visible -> Promise.all([waitForURL StartExam, modal submit click]).

Answer-correct shuffle-safe (Source exam-taking.spec.ts:122-137): loop `[id^="qcard_"]`, baca question text (`h6, .fw-bold`), pilih `label[id^="lbl_"]` filter `{ hasText: correctText }` (by TEXT, BUKAN nth), assert `#answeredProgress` = `${qCount}/${qCount}`. Tangani `#resumeConfirmModal` opsional (exam-taking.spec.ts:114-119) -- lifecycle masuk FRESH pasca-reset, modal jarang muncul -> defensive dismiss saja.

Cert format target (Helpers/CertNumberHelper.cs:20-21): `KPB/{seq:D3}/{RomanMonth}/{year}` -> regex `/KPB\/\d+\/[IVX]+\/\d{4}/`.

Seed analog `tests/sql/retake-worker-407-seed.sql` (kolom AssessmentSessions VERIFIED :91-101):
column list = `(UserId, Title, Category, Schedule, DurationMinutes, Status, Progress, BannerColor, PassPercentage, AllowAnswerReview, GenerateCertificate, IsPassed, CompletedAt, Score, IsTokenRequired, AccessToken, CreatedAt, ElapsedSeconds, IsManualEntry, HasManualGrading, SamePackage, AssessmentType, ShuffleOptions, ShuffleQuestions, AllowRetake, MaxAttempts, RetakeCooldownHours)`.
PackageQuestions kolom incl `MaxCharacters` NOT NULL (:111). PackageOptions `(PackageQuestionId, OptionText, IsCorrect)`. UserPackageAssignments `(AssessmentSessionId, AssessmentPackageId, UserId, ShuffledQuestionIds, ShuffledOptionIdsPerQuestion, AssignedAt, IsCompleted, SavedQuestionCount)`. Pre-condition THROW (:78-84). Idempotent FK-safe cleanup (:46-76): arsip->history->response->assignment->option->question->package->session.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat seed lifecycle retake-lifecycle-408-seed.sql -- sesi gagal cooldown=0 + paket soal jawaban-benar deterministik</name>

  <read_first>
    - `tests/sql/retake-worker-407-seed.sql` (ANALOG -- EXTEND, jangan duplikasi) -- baca utuh: header/klasifikasi (:1-44), cleanup FK-safe idempotent (:46-76), pre-condition THROW (:78-84), insert sesi 407A (:91-101), paket+soal+opsi (:104-153), assignment (:141-147), responses current (:150-153), arsip (:156-164). 407 seed sesi gagal TAPI tanpa pass-path; 408 butuh cooldown=0 + paket soal yang bisa DIJAWAB BENAR sehingga retake bisa LULUS.
    - `docs/SEED_WORKFLOW.md` -- klasifikasi temporary+local-only, format SQL BACKUP/RESTORE.
    - `docs/SEED_JOURNAL.md` -- format entry (tujuan/klasifikasi/entitas tersentuh).
  </read_first>

  <files>tests/sql/retake-lifecycle-408-seed.sql, docs/SEED_JOURNAL.md</files>

  <action>
    Buat `tests/sql/retake-lifecycle-408-seed.sql` (ADDITIVE -- SALIN struktur 407 seed, swap prefix `[RETAKE407]` -> `[RETAKE408]`, kurangi ke 1 sesi lifecycle). KEY DIFFS vs 407 (untuk jalur LULUS):

    1. Header + klasifikasi (adaptasi 407 :1-44): judul "Phase 408 RTK-14 -- Lifecycle e2e Seed (gagal -> Ujian Ulang -> lulus -> 1 cert)"; klasifikasi temporary+local-only; prefix Title `[RETAKE408]`; cara run via db.execScript; pre-condition user `rino.prasetyo@pertamina.com`. `SET NOCOUNT ON;`

    2. Cleanup idempotent FK-safe (SALIN 407 :46-76, ganti LIKE pattern ke `'[[]RETAKE408%'`; urutan arsip->history->response->assignment->option->question->package->session).

    3. Pre-condition THROW (407 :78-84, ganti error number ke 51408, pesan sebut user fixture).

    4. 1 sesi GAGAL lifecycle (`@titleLife NVARCHAR(200) = '[RETAKE408] Lifecycle Fail-to-Pass'`), kolom = list VERIFIED di interfaces. CRITICAL VALUES:
       - `Status='Completed'`, `IsPassed=0` (mulai gagal), `Score` rendah (mis 40).
       - `GenerateCertificate=1` (407 pakai 0; cert WAJIB enabled -- Pitfall 2).
       - `PassPercentage` rendah (mis 50) -> jawab all-correct -> LULUS.
       - `RetakeCooldownHours=0` (cooldown=0 -> #btnRetake aktif langsung -- Pitfall 5).
       - `AllowRetake=1`, `MaxAttempts=3`, `AllowAnswerReview=1`, `AssessmentType='PostTest'`, `IsManualEntry=0`.
       - `CompletedAt = DATEADD(DAY,-2,GETUTCDATE())` (lampau -> tak ada gate cooldown).
       - `AccessToken=''`, `IsTokenRequired=0` (hindari token gate StartExam; StartExam pakai TempData.Peek non-consume + RetakeExam clear token, jadi re-entry bersih).

    5. Paket + 2-3 soal MC dengan jawaban-benar DETERMINISTIK (adaptasi 407 :104-153). Untuk SETIAP soal:
       - QuestionText berisi MARKER unik yang dipetakan spec ke opsi benar (mis `'[RETAKE408] Soal Q1: katalis?'`, `'... Q2: produk?'`, `'... Q3: ...'`).
       - 1 `PackageOption IsCorrect=1` dengan OptionText UNIK deterministik (mis `'BENAR408_Q1_AsamHF'`, `'BENAR408_Q2_Alkylate'`) -> spec pilih by-text (shuffle-safe).
       - >=1 opsi salah (mis `'SALAH408_Q1_Air'`).
       - Sertakan kolom `MaxCharacters` (NOT NULL) di PackageQuestions insert (lihat 407 :111).
    6. UserPackageAssignment (`ShuffledQuestionIds = '[' + qids + ']'`, `ShuffledOptionIdsPerQuestion='{}'`, `IsCompleted=1`, `SavedQuestionCount=n`) -- RetakeExam->StartExam butuh assignment (catatan: ExecuteAsync akan hapus & worker generate ulang saat StartExam; cukup pastikan kondisi awal Results konsisten + soal current ada).
    7. Responses current attempt (prior failed): >=1 soal pilih opsi SALAH -> Results pra-retake tampilkan cross verdict (ShowWrongFlagsOnly).
    8. Arsip prior (opsional, kontrol counting): untuk lifecycle bersih single-pass, 0 arsip era-retake (currentAttempt=1) + MaxAttempts>=2 = paling sederhana -> eligible retake. (Jika menambah >=1 arsip, jaga currentAttempt < MaxAttempts.)
    9. Output `SELECT @sidLife AS SidFailed;` (spec resolve by Title; output untuk run manual).

    Lalu tambahkan entry di `docs/SEED_JOURNAL.md`: tujuan (lifecycle e2e 408 gagal->ulang->lulus->cert), klasifikasi temporary+local-only, entitas tersentuh (AssessmentSessions/Packages/Questions/Options/UserResponses/Assignments/AttemptHistory/ResponseArchives prefix [RETAKE408]), status `active` (akan ditandai `cleaned` setelah restore di Task 3).
  </action>

  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && grep -c RETAKE408 tests/sql/retake-lifecycle-408-seed.sql</automated>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && grep -E "GenerateCertificate|RetakeCooldownHours|BENAR408|51408" tests/sql/retake-lifecycle-408-seed.sql | head</automated>
  </verify>

  <acceptance_criteria>
    - `tests/sql/retake-lifecycle-408-seed.sql` ada; `grep -c RETAKE408` >= 5 (header + cleanup + sesi + soal).
    - Seed sesi mengandung `GenerateCertificate` di-set 1 DAN `RetakeCooldownHours` di-set 0 DAN `AllowRetake` 1 DAN `AssessmentType='PostTest'` pada blok insert sesi.
    - Tiap soal punya opsi `IsCorrect` 1 dengan OptionText deterministik ber-prefix unik (mis `BENAR408_`) -- grep menemukan >=1 `BENAR408_`.
    - Pre-condition THROW `51408` + pre-condition user `rino.prasetyo@pertamina.com` ada.
    - Cleanup idempotent memakai pattern `'[[]RETAKE408%'` (escape bracket benar) urutan FK-safe.
    - Entry baru di `docs/SEED_JOURNAL.md` menyebut `[RETAKE408]` + klasifikasi temporary+local-only.
  </acceptance_criteria>

  <done>
    Seed `[RETAKE408]` 1 sesi gagal cooldown=0 + GenerateCertificate=1 + paket soal MC jawaban-benar deterministik (prefix BENAR408_) siap; idempotent FK-safe; pre-condition THROW 51408; journal entry tercatat. 0 migration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Buat retake-lifecycle-408.spec.ts -- lifecycle real-browser gagal->Ujian Ulang->lulus->1 cert</name>

  <read_first>
    - `tests/e2e/retake-worker-407.spec.ts` (ANALOG HARNESS) -- baca utuh: imports + serial config (:31-43), beforeAll backup/seed/resolve-sid + afterAll restore (:46-82), gotoResults + pageerror arm (:85-90), leak-safe assert sc-1 (:92-118), modal sc-3 (:135-150).
    - `tests/e2e/exam-taking.spec.ts:103-165` (ANALOG take-and-pass) Flow A -- pola answer-correct by-text (:122-137), `#answeredProgress` assert, penanganan opsional `#resumeConfirmModal` (:114-119).
    - `tests/e2e/helpers/examTypes.ts:327-337` -- `submitExamTwoStep`.
    - `tests/helpers/auth.ts` + `tests/helpers/dbSnapshot.ts` -- login + snapshot helpers.
    - `tests/sql/retake-lifecycle-408-seed.sql` (Task 1) -- marker soal + OptionText benar untuk mapping by-text.
    - `tests/playwright.config.ts` -- baseURL/E2E_BASE_URL.
    - `Views/CMP/Results.cshtml` + StartExam markup -- konfirmasi selector aktual (#btnRetake/#retakeConfirmModal/#answeredProgress/qcard/lbl/teks "LULUS") bila assert butuh penyesuaian.
  </read_first>

  <files>tests/e2e/retake-lifecycle-408.spec.ts</files>

  <action>
    Buat `tests/e2e/retake-lifecycle-408.spec.ts` (ADDITIVE). Struktur (SALIN harness 407, jahit take-and-pass exam-taking):

    1. Imports + serial (407 :31-43 + tambah `submitExamTwoStep`):
       import { test, expect, type Page } from '@playwright/test';
       import { resolve } from 'path';
       import * as db from '../helpers/dbSnapshot';
       import { login } from '../helpers/auth';
       import { submitExamTwoStep } from './helpers/examTypes';
       const SEED_SQL = resolve(__dirname, '..', 'sql', 'retake-lifecycle-408-seed.sql');
       let snapshotPath: string; let sidFailed = 0;
       test.describe.configure({ mode: 'serial' });

    2. beforeAll/afterAll SALIN VERBATIM dari 407 :46-82, hanya ganti:
       - nama snapshot `HcPortalDB_Dev-pre408-${ts}.bak`.
       - resolve sid: `sidFailed = await db.queryScalar("SELECT TOP 1 Id FROM AssessmentSessions WHERE Title = '[RETAKE408] Lifecycle Fail-to-Pass'")` + `expect(sidFailed).toBeGreaterThan(0)`.

    3. Satu test lifecycle (`test.setTimeout(180_000)`), langkah:
       (a) arm `page.on('pageerror', e=>errors.push(e.message))` SEBELUM nav (lesson 413) -> `login(page,'coachee')` -> goto `/CMP/Results/${sidFailed}` -> waitForLoadState networkidle.
       (b) Hasil pra-retake ShowWrongFlagsOnly: assert card "Tinjauan Jawaban" + `[role="status"]` "Kunci jawaban disembunyikan"; `await page.content()` not.toContain '(Jawaban Benar)'; `.list-group-item-success` count 0; `expect(errors).toEqual([])`.
       (c) Retake: klik `#btnRetake` -> `#retakeConfirmModal` visible -> assert `input[name="__RequestVerificationToken"]` count 1 -> `Promise.all([page.waitForURL('**/CMP/StartExam/**',{timeout:20000}), modal.locator('button[type="submit"]').click()])` -> `expect(errors).toEqual([])`.
       (d) Defensive resume-modal dismiss (non-blocking): `page.locator('#resumeConfirmModal').waitFor({state:'visible',timeout:8000}).then(dismiss).catch(noop)`.
       (e) Jawab BENAR shuffle-safe: loop `[id^="qcard_"]`, baca `h6, .fw-bold`, map marker (`Q1/Q2/Q3`) -> `correctText` prefix `BENAR408_Qn_`, klik `label[id^="lbl_"]` filter `{hasText: correctText}`, `waitForTimeout(700)`; assert `#answeredProgress` = `${qCount}/${qCount}`.
       (f) `await submitExamTwoStep(page)` -> waitForLoadState networkidle.
       (g) ASSERT FINAL: `body` toContainText 'LULUS' + `body` toContainText regex `/KPB\/\d+\/[IVX]+\/\d{4}/`; `expect(errors).toEqual([])`.

    Sesuaikan selector dengan markup aktual (baca Results.cshtml/StartExam bila assert gagal). Mapping marker soal Q1/Q2/Q3 -> prefix OptionText benar HARUS cocok seed Task 1.

    Aturan deviasi: Jika selector/markup berbeda dari asumsi, sesuaikan DI SPEC ini (baca markup real). JANGAN edit produksi, helper, atau spec existing. Batas deviasi 3 -> lebih -> STOP + lapor.

    CATATAN run: Executor TIDAK menjalankan live run di sini (butuh app @5270 + SQLEXPRESS live = checkpoint Task 3). Cukup pastikan spec PARSE bersih (`npx playwright test retake-lifecycle-408.spec.ts --list`).
  </action>

  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff/tests" && npx playwright test retake-lifecycle-408.spec.ts --list 2>&1 | tail -15</automated>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && grep -E "submitExamTwoStep|StartExam|pageerror|RETAKE408" tests/e2e/retake-lifecycle-408.spec.ts | head</automated>
  </verify>

  <acceptance_criteria>
    - `tests/e2e/retake-lifecycle-408.spec.ts` ada; `npx playwright test retake-lifecycle-408.spec.ts --list` mendaftarkan 1 test tanpa parse error.
    - `grep -c submitExamTwoStep` >= 1 (reuse helper, bukan hand-roll submit).
    - `grep -c "waitForURL" ...` >= 1 dengan target `**/CMP/StartExam/**` (retake -> StartExam ter-jahit).
    - `grep -c "pageerror" ...` >= 1 (lesson 413 guard hadir).
    - `grep "KPB" ...` menemukan regex cert assert; `grep "LULUS" ...` menemukan assert lulus.
    - `grep "list-group-item-success" ...` menemukan leak-safe assert pra-retake.
    - 0 edit pada spec/helper/seed existing (git diff hanya file baru `retake-lifecycle-408.spec.ts`).
  </acceptance_criteria>

  <done>
    Spec lifecycle 408 parse bersih: jahit harness 407 (serial + snapshot/seed/restore + pageerror) + exam-taking Flow A (answer-by-text + submitExamTwoStep). Assert: pra-retake leak-safe -> modal antiforgery -> StartExam -> jawab benar -> LULUS + cert#. Live run = Task 3. 0 file existing tersentuh. 0 migration.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regresi penuh + UAT live lifecycle @5270 + tandai journal cleaned</name>

  <what-built>
    - GAP-1 xUnit integration `RetakeThenPassCertTests` (plan 408-01) + GAP-3 e2e lifecycle `retake-lifecycle-408.spec.ts` + seed (Task 1/2 plan ini).
    - Capstone test milestone v32.4: retake-then-pass-1-cert (SQL riil) + lifecycle penuh real-browser.
  </what-built>

  <action>
    SEBELUM checkpoint, executor jalankan gate otomatis berikut (SEED_WORKFLOW compliant) -- bukan minta manusia kerjakan yang bisa di-automate:

    1. Pastikan SQLEXPRESS hidup, lalu suite penuh xUnit (incl Integration GAP-1):
       `dotnet test HcPortal.Tests` -- WAJIB hijau (baseline existing 448 unit + 2 skip + Integration retake incl `RetakeThenPassCert` 1 pass; 0 failed; 0 regresi).
       Jika SQLEXPRESS tak tersedia: jalankan `dotnet test HcPortal.Tests --filter "Category!=Integration"` (unit hijau) + catat Integration butuh SQLEXPRESS untuk gate.
    2. Pastikan app jalan @5270 (`Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5270` -- JANGAN commit launchSettings). SEED_WORKFLOW dijalankan otomatis OLEH spec (beforeAll db.backup + execScript; afterAll db.restore). Jalankan seluruh spec e2e retake serial:
       `cd tests && E2E_BASE_URL=http://localhost:5270 npx playwright test retake-lifecycle-408 retake-worker-407 retake-config-406 riwayat-hc-406 --workers=1`
       -- semua hijau (lifecycle baru + 6+6+5 smoke existing tak regresi).
    3. Verifikasi DB lokal kembali baseline pasca-restore (tidak ada leftover `[RETAKE408]`):
       `sqlcmd -S "localhost\SQLEXPRESS" -d HcPortalDB_Dev -E -C -I -Q "SELECT COUNT(*) FROM AssessmentSessions WHERE Title LIKE '[[]RETAKE408%'"` -> 0.
    4. Tandai entry `[RETAKE408]` di `docs/SEED_JOURNAL.md` -> `cleaned`.
  </action>

  <how-to-verify>
    1. Konfirmasi output `dotnet test HcPortal.Tests` -> Passed (0 failed), termasuk `RetakeThenPassCert` 1 pass @SQLEXPRESS.
    2. Konfirmasi output Playwright -> semua spec retake PASS (lifecycle-408 1/1 + worker-407 6/6 + config-406 6/6 + riwayat-hc-406 5/5), 0 pageerror.
    3. Live (opsional saksikan): buka @5270, login coachee, /CMP/Results/{sesi [RETAKE408]} -> lihat skor + tanda benar/salah TANPA kunci -> klik "Ujian Ulang" -> modal -> StartExam -> jawab benar -> Kumpulkan -> halaman Hasil tampil "LULUS" + nomor sertifikat `KPB/...`.
    4. Konfirmasi DB count `[RETAKE408]` = 0 (restore bersih) + journal `cleaned`.
  </how-to-verify>

  <resume-signal>Ketik "approved" bila suite + lifecycle e2e hijau & DB bersih, atau jelaskan kegagalan (test merah / pageerror / cert tak terbit / leftover seed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (konsolidasi milestone v32.4 retake -- 406 admin/HC + 407 worker + invariant cert)

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| browser (worker, untrusted) -> `CMP/RetakeExam` (POST) | Worker memicu mutasi destruktif (arsip + reset sesi) atas `id` route int | `id` (int), antiforgery token |
| view (Razor) <- VM (server) | `Results.cshtml` mempercaya `RetakeMode`/`CanRetake` dari server (`CMPController.Results`); keputusan leak-safety = server | `RetakeReviewMode` enum + eligibility flags |
| browser (Admin/HC) -> `RiwayatPercobaan` GET / `UpdateRetakeSettings` POST | Admin/HC baca riwayat worker (sessionId int) / set config (maxAttempts, cooldown) | jawaban worker; nilai config numerik |
| client countdown JS -> server | Enable/disable tombol = UX saja; server re-cek `CanRetakeAsync` saat POST | tidak ada data melintas |
| grade -> DB (cert) | `GradingService.GradeAndCompleteAsync` menerbitkan `NomorSertifikat` di bawah unique index | sequence cert (bukan kripto) |

## STRIDE Threat Register (konsolidasi 406 [13/13 closed] + 407 [9/9 closed] + invariant cert-uniqueness)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-407-idor | Elevation / Access Control | `CMPController.RetakeExam` ownership | mitigate | `if (assessment.UserId != user.Id) return Forbid();` sebelum CanRetakeAsync+ExecuteAsync. Diuji `RetakeExam_NonOwner_ReturnsForbid`. (closed 407) |
| T-407-csrf | Tampering / CSRF | RetakeExam POST + modal form | mitigate | `[ValidateAntiForgeryToken]` + `@Html.AntiForgeryToken()`; lifecycle 408 assert token count 1 di modal. (closed 407) |
| T-407-bypass | Tampering / Logic Bypass | cooldown/cap bypass via DevTools | mitigate | Server re-cek `CanRetakeAsync` SEBELUM ExecuteAsync; countdown JS non-authoritative. Diuji `RetakeExam_NotEligible_RedirectsToResultsWithError`. (closed 407) |
| T-407-leak | Information Disclosure | `Results.cshtml` ShowWrongFlagsOnly + `_RiwayatPekerja.cshtml` | mitigate | ShowWrongFlagsOnly TIDAK render `list-group-item-success`/`(Jawaban Benar)`/`CorrectAnswer`; arsip verdict-only. lifecycle 408 assert pra-retake not.toContain kunci + `.list-group-item-success` count 0. (closed 407) |
| T-407-token | Tampering / Auth Bypass | stale TempData token pasca-retake | mitigate | `TempData.Remove($"TokenVerified_{id}")` setelah ExecuteAsync; StartExam pakai Peek. Diuji `RetakeExam_Success_ClearsTokenAndRedirectsToStartExam` + lifecycle 408 (StartExam re-entry bersih). (closed 407) |
| T-407-xss | Tampering / XSS | user-content riwayat/review | mitigate | Razor `@` auto-encode; ZERO Html.Raw di partial retake; countdown via `.textContent`. (closed 407) |
| T-407-jsabort | Availability | countdown ReferenceError abort handler (lesson 413) | mitigate | guard `if(!btn)return`/`if(!iso)return`/`isNaN`; lifecycle 408 `page.on('pageerror')` assert empty di TIAP langkah. (closed 407) |
| T-406-01 | Information Disclosure | RiwayatPercobaan GET (riwayat worker mana pun) | mitigate | `[Authorize(Roles="Admin, HC")]` pada action; RBAC re-cek tiap fetch. (closed 406) |
| T-406-02 | Information Disclosure | per-soal cell membocorkan KUNCI | mitigate | Partial render hanya AnswerText + verdict glyph + AwardedScore; tak ada field kunci di arsip. (closed 406) |
| T-406-06 | Tampering / CSRF | UpdateRetakeSettings POST | mitigate | `@Html.AntiForgeryToken()` + `[ValidateAntiForgeryToken]`. (closed 406) |
| T-406-07 | Tampering | bypass min/max -> MaxAttempts/cooldown out-of-range | mitigate | Server `Math.Clamp(maxAttempts,1,5)` + `Math.Clamp(cooldown,0,168)`. (closed 406) |
| T-406-09 | Information Disclosure | card retake muncul di Pre-Test/Manual (invalid) | mitigate | `@if(ViewBag.HideRetakeToggle != true)` (RetakeRules.ShouldHideRetakeToggle). (closed 406) |
| T-408-cert | Tampering / Logic (double-cert) | retake-then-pass menerbitkan sertifikat dobel | mitigate | unique index `IX_AssessmentSessions_NomorSertifikat` + retry 3x `WHERE NomorSertifikat==null` (GradingService.cs:287-312). DIBUKTIKAN GAP-1 (plan 408-01: `Assert.Equal(1, certCount)`) + lifecycle 408 cert# visual. |

## Accepted Risks (dibawa dari 407 -- JANGAN refactor di 408, test-only phase)
| Risk ID | Threat Ref | Rationale |
|---------|------------|-----------|
| AR-407-01 | T-407-doublearchive | double-submit double-archive dikelola `RetakeService.ExecuteAsync` claim-atomik (Open->no-op; ExecuteUpdate WHERE Status NOT IN(Cancelled,Open)->rows==0 rollback). Ter-cover `RetakeServiceTests`. |
| AR-407-02 | T-407-drift | `eraRetakeArchives` counting diduplikasi 3 tempat (Results + CanRetakeAsync + ExecuteAsync); identik + ter-cover test (RetakeServiceTests + RetakeExamEndpointTests + e2e lifecycle). Single-source = backlog IN-03. JANGAN refactor di 408. |

Disposisi 408: Fase 408 tidak menambah surface kode produksi (test murni). Gerbang formal = `gsd-secure-phase 408` (D-03, dijalankan TERPISAH pasca-eksekusi) memverifikasi seluruh threat 406+407 tetap closed/accepted tanpa regresi + invariant T-408-cert kini di-cover GAP-1 + lifecycle 408.
</threat_model>

<verification>
- `dotnet test HcPortal.Tests` -> Passed 0 failed @SQLEXPRESS (incl `RetakeThenPassCert`).
- `npx playwright test retake-lifecycle-408 retake-worker-407 retake-config-406 riwayat-hc-406 --workers=1` @5270 -> semua PASS, 0 pageerror.
- DB lokal: `SELECT COUNT(*) ... Title LIKE '[[]RETAKE408%'` = 0 pasca-restore; SEED_JOURNAL entry `cleaned`.
- `git status`: file baru `tests/e2e/retake-lifecycle-408.spec.ts` + `tests/sql/retake-lifecycle-408-seed.sql` + edit `docs/SEED_JOURNAL.md`; 0 production code; 0 spec/helper/seed existing termodifikasi.
</verification>

<success_criteria>
- GAP-3 ter-tutup: lifecycle penuh gagal->ulang->lulus->cert terbukti real-browser @5270 (RTK-14), leak-safe + 0 pageerror.
- Seluruh suite + e2e retake hijau tanpa regresi (capstone gate).
- SEED_WORKFLOW bersih (snapshot->seed->restore, journal cleaned), DB baseline pulih.
- 0 production code; 0 test existing tersentuh; 0 migration.
- `<threat_model>` konsolidasi tersedia untuk auditor `gsd-secure-phase 408`.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/408-test-uat/408-02-SUMMARY.md` (template summary). Catat: hasil run suite + e2e (jumlah pass per spec), bukti cert# lifecycle, konfirmasi DB bersih + journal cleaned, deviasi selector (jika ada) + alasan, migration=FALSE, 0 file existing tersentuh. Catat juga reminder: jalankan `gsd-secure-phase 408` (D-03 gate formal) sebelum `/gsd-verify-work`.
</output>
