---
phase: 408
slug: test-uat
type: playwright-uat
status: passed
date: 2026-06-22
runner: milestone-autopilot
base_url: http://localhost:5270
db: HcPortalDB_Dev (SQLEXPRESS)
result: xUnit 614/0/2 PASS + e2e 19/19 PASS (lifecycle-408 + regresi 407/406)
---

# Phase 408 — Capstone UAT: PASS (capstone caught a test-fixture defect, product proven correct)

## Ringkasan
Capstone 408 menjalankan fungsinya: lifecycle e2e penuh awalnya RED → menyingkap **bug fixture seed** (bukan defect produksi). Setelah diperbaiki, seluruh kemampuan retake terbukti end-to-end.

### LULUS — semua
- **xUnit penuh 614/0/2** (incl Integration real-SQL @SQLEXPRESS) — termasuk **GAP-1 `RetakeThenPassCertTests`** (retake → `GradeAndCompleteAsync` → tepat 1 `NomorSertifikat`).
- **e2e 19/19** @5270 (`--workers=1`): **lifecycle-408 (1)** + `retake-worker-407` (6) + `retake-config-406` (6) + `riwayat-hc-406` (5) + setup. DB restored bersih (Layer-4 0 rows, journal cleaned).

### Lifecycle 408 (gap utama) — PASS
Alur penuh via UI nyata: sesi gagal (40%) → Hasil leak-safe (✓/✗ tanpa kunci) → "Ujian Ulang" → modal antiforgery → POST RetakeExam → redirect StartExam → **jawab BENAR semua** → submit → **"Nilai Anda 100%" / LULUS + Nomor Sertifikat terbit** (`KPB/…`) + "Lihat Sertifikat". Riwayat: "Percobaan ke-2: 100% Lulus (saat ini)" + "Percobaan ke-1: 40% Tidak Lulus" (arsip). 0 pageerror.

## Root cause temuan capstone (FIXED — bukan defect produksi)
Run awal lifecycle deterministik **0% / TIDAK LULUS** walau jawaban benar & ter-persist. Investigasi:
- **Bug = SEED FIXTURE.** Seed `retake-lifecycle-408-seed.sql` memakai `QuestionType = 'SingleAnswer'` — **nilai tidak valid**. QuestionType kanonik produk = `{ MultipleChoice, MultipleAnswer, Essay }` (validTypes `AssessmentAdminController.cs:6160/6558`); "Single Answer"/"SA" hanya **label UI** (`QuestionTypeLabels`, single-choice disimpan sebagai `MultipleChoice`).
- Grade switch (`GradingService.GradeAndCompleteAsync:103` + `SubmitExam:1704`) tak punya case "SingleAnswer" → soal tak dinilai → `maxScore`/`totalScore` 0 → **0%**. Read-path riwayat (RetakeArchiveBuilder/IsQuestionCorrect) toleran → tampil benar → kontradiksi yang menyingkap akar.
- **Produk BENAR:** data nyata selalu `MultipleChoice`; 408-01 xUnit (pakai `MultipleChoice`) lulus + cert. 406/407 lolos karena display-only (Score pre-seeded, tak grade-on-submit) sehingga type invalid tak berdampak.
- **Fix:** seed `'SingleAnswer'` → `'MultipleChoice'` (3 soal) + assertion LULUS dirobust-kan (`.first()` anti strict-multi-match + guard skor 100%). Lifecycle → LULUS 100% + cert.

## Catatan (Info, non-blocking)
- **IN-408-A (defensive, backlog):** grade switch (`GradingService`/`SubmitExam`) menilai 0 secara diam untuk QuestionType tak dikenal (tanpa default-throw/log). Untuk data valid tak pernah terpicu; pertimbangkan default-branch yang log/throw agar fixture/data korup kelihatan cepat.
- **IN-408-B:** seed 406/407 juga memakai label 'SingleAnswer' (display-only, aman). Bisa diselaraskan ke 'MultipleChoice' saat sentuh berikutnya (tak mendesak).

## Verdict
**PASS.** Lifecycle retake → pass → cert terbukti live @5270. Capstone bekerja (menangkap fixture-bug sebelum ship). Gate UAT 408 TUNTAS.
