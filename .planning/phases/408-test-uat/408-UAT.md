---
phase: 408
slug: test-uat
type: playwright-uat
status: BLOCKED
date: 2026-06-22
runner: milestone-autopilot
base_url: http://localhost:5270
db: HcPortalDB_Dev (SQLEXPRESS)
result: xUnit 614/0/2 PASS + e2e regression 18/18 PASS; lifecycle-408 1 FAIL (deterministic) — PRODUCT DEFECT caught
blocker: retake-then-pass via real UI grades session 0%/TIDAK LULUS despite all answers correct+persisted; no cert issued (RTK-14 SC#5)
---

# Phase 408 — Capstone UAT: BLOCKED by product defect (capstone working as intended)

## Ringkasan
Capstone 408 menjalankan fungsinya: menangkap **defect produksi** yang tak terlihat di test per-surface 405-407.

### LULUS
- **xUnit penuh 614/0/2** (incl Integration real-SQL @SQLEXPRESS) — termasuk **GAP-1 `RetakeThenPassCertTests`** (retake→re-seed responses+assignment→`GradeAndCompleteAsync`→tepat 1 `NomorSertifikat`). Membuktikan **engine grading + cert benar saat state DB di-set langsung**.
- **e2e regresi 18/18**: `retake-config-406` (6), `riwayat-hc-406` (5), `retake-worker-407` (6) + setup. Semua surface admin/HC/worker existing hijau (leak-safe, cooldown countdown, modal, riwayat).

### GAGAL (deterministik, 2× re-run + flush-wait) — `retake-lifecycle-408.spec.ts`
Alur penuh via UI nyata: gagal → Hasil leak-safe → "Ujian Ulang" → modal → StartExam → **jawab BENAR semua** → submit → **harapan LULUS+cert**.
**Hasil aktual:** session **"Nilai Anda 0%" / TIDAK LULUS**, tak ada sertifikat.

## Bukti kunci (kontradiksi → defect, bukan test flaw)
Dari `error-context.md` Results pasca-submit:
- **Tinjauan Jawaban (current):** Soal 1/2/3 SEMUA "Benar", "Jawaban Anda" = `BENAR408_Q1_AsamHF`/`Q2_Alkylate`/`Q3_Isobutana` (opsi IsCorrect=1).
- **Riwayat "Percobaan ke-2" (current attempt) drill-down:** per-soal Benar, **awarded 34/33/33 = 100**.
- **TAPI session header "Nilai Anda 0%"** + riwayat header "Percobaan ke-2: **0%** Tidak Lulus".

→ Jawaban worker BENAR + **ter-persist** (riwayat baca dari DB menampilkannya), tetapi **skor agregat sesi = 0%**.

## Root cause (terlokalisir, perlu konfirmasi runtime)
- `GradingService.GradeAndCompleteAsync` (`Services/GradingService.cs:57+`) menilai HANYA soal di `packageAssignment.GetShuffledQuestionIds()` (baris 60-75); `finalPercentage = maxScore>0 ? total/max*100 : 0` (baris 144).
- `RetakeService.ExecuteAsync` **menghapus** `UserPackageAssignment` saat reset (`Services/RetakeService.cs:181-184`) + reset `StartedAt=null`/`Status=Open` + hapus responses.
- `StartExam` membuat ulang assignment bila null (`Controllers/CMPController.cs:1051-1092`).
- **Saat submit (`SubmitExam`→`GradeAndCompleteAsync`), skor agregat sesi jadi 0% walau response per-soal benar & ter-persist.** Divergensi: read-path riwayat (RetakeArchiveBuilder, request berikutnya) konsisten benar; grade-path submit-time = 0. Kandidat: assignment yang dibaca grade-time `ShuffledQuestionIds` kosong/mismatch, ATAU response belum terbaca grade-time padahal ada di read-time. **Bukan race auto-save** (flush networkidle+1500ms tak menyembuhkan).

## Mengapa lolos sampai 408
405-407 + 408-01 menguji per-surface/state-langsung. INI alur **end-to-end retake-via-UI pertama** (StartExam→submit pasca-reset). Justru tujuan capstone (lesson 354/413: real-browser WAJIB).

## Status & rekomendasi
**408 BLOCKED.** Test lifecycle SENGAJA dibiarkan RED (assert perilaku benar) sampai produk diperbaiki. JANGAN dilemahkan agar hijau.
- Perbaikan butuh **kode produksi** (RetakeService/CMPController/GradingService) → di luar scope test-only 408 (D-01..03) → keputusan user.
- Rekomendasi: `/gsd-debug` dgn inspeksi DB di batas submit (cek `UserPackageAssignment.ShuffledQuestionIds` + `PackageUserResponses` saat `GradeAndCompleteAsync` jalan), lalu fix sebagai **fase desimal 408.1** (atau hotfix retake submit-grade), re-run lifecycle-408 → harus LULUS+cert.

## SEED_WORKFLOW
Semua run e2e self snapshot→seed→restore (Layer-4 0 rows post-restore; journal cleaned). DB lokal bersih.
