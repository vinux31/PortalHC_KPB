---
phase: 409-data-foundation-re-entry-guards-exclude-removed-query
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/CMPController.cs
  - Hubs/AssessmentHub.cs
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/ParticipantRemovalGuardTests.cs
autonomous: true
requirements: [PRMV-03]
user_setup: []

must_haves:
  truths:
    - "Peserta dengan sesi RemovedAt != null tidak dapat memulai ujian (StartExam → redirect 'Anda telah dikeluarkan dari ujian ini.', sesi TIDAK ter-mark InProgress)"
    - "Peserta dengan sesi RemovedAt != null tidak dapat mensubmit (SubmitExam → redirect sebelum grading, Score tak berubah)"
    - "AssessmentHub.JoinBatch menolak (silent-skip) join bila sesi RemovedAt != null"
    - "AssessmentHub.SaveTextAnswer/SaveMultipleAnswer tidak menyimpan jawaban dari sesi RemovedAt != null (A1 defense-in-depth)"
    - "Monitoring (ManageAssessmentTab_Assessment, AssessmentMonitoring, AssessmentMonitoringDetail + semua count) mengecualikan sesi RemovedAt != null"
    - "UserAssessmentHistory per-pekerja TETAP menampilkan sesi removed (boundary, anti over-exclude)"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "Guard RemovedAt di StartExam + SubmitExam"
      contains: "Anda telah dikeluarkan dari ujian ini."
    - path: "Hubs/AssessmentHub.cs"
      provides: "Predikat RemovedAt==null di JoinBatch + SaveTextAnswer + SaveMultipleAnswer"
      contains: "RemovedAt == null"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Exclude RemovedAt==null di 3 query monitoring batch-aktif"
      contains: "a.RemovedAt == null"
    - path: "HcPortal.Tests/ParticipantRemovalGuardTests.cs"
      provides: "Test de-tautologis: guard block 3+ jalur + exclude count/list + boundary non-regression"
      contains: "ParticipantRemovalGuardTests"
  key_links:
    - from: "Controllers/CMPController.cs StartExam"
      to: "assessment.RemovedAt"
      via: "guard sebelum mark-InProgress :1001"
      pattern: "RemovedAt != null"
    - from: "Hubs/AssessmentHub.cs JoinBatch"
      to: "AnyAsync predicate"
      via: "&& s.RemovedAt == null"
      pattern: "RemovedAt == null"
    - from: "Controllers/AssessmentAdminController.cs"
      to: "AssessmentSessions query"
      via: ".Where(a => a.RemovedAt == null)"
      pattern: "RemovedAt == null"
---

<objective>
Pasang invarian soft-remove di read-path: (1) guard re-entry server-authoritative anti-resubmit di `CMPController.StartExam` + `SubmitExam` + `AssessmentHub.JoinBatch` + (A1) `SaveTextAnswer`/`SaveMultipleAnswer`; (2) exclude sesi `RemovedAt != null` dari 3 query monitoring batch-aktif; (3) test de-tautologis yang menjalankan action/query asli (bukan replica predikat — lesson 999.12) termasuk boundary non-regression (riwayat pekerja TETAP tampil removed).

Purpose: PRMV-03 (peserta dihapus tak bisa lanjut/submit/re-join, jawaban tak terhitung) + fondasi PLIV-01 (exclude-from-count aktif). Server-side = mitigasi keamanan utama (client tak bisa bypass).
Output: 6 guard insertion (2 controller + 2 hub join/answer set) + 3 exclude insertion + 1 test file baru.

**Depends on Plan 01:** kolom `RemovedAt` HARUS sudah ter-apply di DB lokal (migration landing) agar integration test tak false-positive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-CONTEXT.md
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-RESEARCH.md
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-PATTERNS.md
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-VALIDATION.md
@.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-01-SUMMARY.md

<interfaces>
<!-- Insertion points code-verified 2026-06-21 (re-verify line numbers jika file di-edit fase lain sebelum execute). -->

CMPController.StartExam: load `:903-904` (assessment loaded), null-check `:906`, owner-check `:911-912`, Upcoming→Open `:915`, mark-InProgress `:999-1004`. Guard masuk SETELAH owner-check `:912`, SEBELUM `:915`.
CMPController.SubmitExam: load `:1581-1582`, null-check `:1584`, owner-check `:1589-1592`, `ShouldGateMissingStart` `:1596`, grading hilir. Guard masuk SETELAH owner-check `:1592`, SEBELUM `:1596`.
AssessmentHub.JoinBatch `:29-31`: `AnyAsync(s => s.UserId == userId && s.Status == "InProgress")` + silent `return`.
AssessmentHub.SaveTextAnswer `:143-144`: `FirstOrDefaultAsync(s => s.Id == sessionId && s.UserId == userId && s.Status == "InProgress")` + silent return-if-null `:145-148`.
AssessmentHub.SaveMultipleAnswer `:209-210`: predikat identik.
AssessmentAdminController exclude sites: `managementQuery :119-121` (`.AsNoTracking().AsQueryable()`), `AssessmentMonitoring.query :2822-2824` (`.AsNoTracking().AsQueryable()`), `AssessmentMonitoringDetail.query :3328-3332` (`.Include(a=>a.User).Where(a => a.Title==title && a.Category==category && a.Schedule.Date==scheduleDate.Date)`).

From HcPortal.Tests/AssessmentWindowRemovalTests.cs (InMemory real-controller factory — REUSE pattern):
```csharp
// MakeController(): UseInMemoryDatabase(Guid) + AuditLogService(ctx) + MemoryCache + null! for unused deps
//   + NullLogger<AssessmentAdminController>.Instance; ControllerContext + pinned ViewData.
// MakeUser(id,name): minimal ApplicationUser (WAJIB seed FK match — EF InMemory drops rows w/ absent nav FK).
// Read results via ctrl.ViewData["ManagementData"] (NOT ViewBag as T → silent null).
// AssessmentAdminController ctor (12 args): ctx, userManager:null!, auditLog, env:null!, cache, logger:NullLogger,
//   notificationService:null!, hubContext:null!, workerDataService:null!, gradingService:null!,
//   protonCompletionService:null!, protonBypassService:null!.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing test scaffold — ParticipantRemovalGuardTests (de-tautologis)</name>
  <read_first>
    - HcPortal.Tests/AssessmentWindowRemovalTests.cs (FULL — `MakeController()` :38-74, `MakeUser()` :77-86, fact pattern reading `ViewData["ManagementData"]` via reflection)
    - HcPortal.Tests/FlexibleParticipantAddTests.cs (SQLEXPRESS disposable fixture :20-53 + de-tautology structure :122-162 — "BEFORE seed wrong-value → real action → AFTER assert")
    - 409-VALIDATION.md §"De-tautology rule" + §"Observable behaviors yang HARUS divalidasi" (5 behaviors)
    - 409-PATTERNS.md §"Test pattern A/B" + §"De-tautology rule (MANDATORY)"
  </read_first>
  <files>HcPortal.Tests/ParticipantRemovalGuardTests.cs</files>
  <action>
    Buat test file BARU `HcPortal.Tests/ParticipantRemovalGuardTests.cs`. Pakai pola InMemory real-controller (`AssessmentWindowRemovalTests.MakeController` — copy factory + `MakeUser` helper). **De-tautologis (999.12)**: seed sesi nyata, panggil action/query ASLI, observasi behavior — JANGAN tulis ulang predikat lalu assert.

    **Tests untuk EXCLUDE (real AssessmentAdminController action via InMemory):**
    1. `ManageAssessmentTab_Excludes_RemovedSession`: seed 2 sesi sama batch (1 aktif `RemovedAt==null`, 1 `RemovedAt=DateTime.UtcNow`) + `MakeUser` untuk tiap UserId → panggil `ctrl.ManageAssessmentTab_Assessment(...)` → assert `ViewData["ManagementData"]` hanya berisi/menghitung sesi aktif (1), sesi removed lenyap.
    2. `MonitoringDetail_Counts_ExcludeRemoved`: seed batch 2 sesi InProgress (1 removed) → panggil `AssessmentMonitoringDetail(title, category, scheduleDate)` → assert `InProgressCount`/`TotalCount` = 1 (bukan 2). (Baca count via ViewModel di ViewData/Model sesuai struktur action.)
    3. **Boundary non-regression** `UserAssessmentHistory_StillShows_RemovedSession`: seed sesi `RemovedAt != null` milik 1 user → panggil `UserAssessmentHistory(userId)` (`:5262`) → assert sesi removed TETAP muncul (anti over-exclude D-01a / Pitfall 3). Test ini WAJIB tetap GREEN setelah Task 3 — bila Task 3 over-exclude, test ini RED.

    **Tests untuk GUARD (real CMPController action via InMemory bila feasible; jika CMPController butuh banyak dep yang dereferenced, gunakan predikat-via-real-query di disposable DB untuk JoinBatch + integration controller untuk Start/Submit — pilih pendekatan yang menjalankan logika ASLI, bukan replika):**
    4. `StartExam_Blocks_RemovedSession`: seed sesi `RemovedAt != null` + `StartedAt == null` + `Status` non-terminal → panggil `StartExam(id)` → assert hasil `RedirectToActionResult` ke "Assessment" AND DB `Status != "InProgress"` & `StartedAt == null` (sesi removed TIDAK PERNAH ter-mark).
    5. `SubmitExam_Blocks_RemovedSession`: seed sesi `RemovedAt != null` + `Score` lama → panggil `SubmitExam(id, answers)` → assert redirect AND `Score`/`Status` DB tak berubah (grading skipped).
    6. `JoinBatch_Predicate_Rejects_RemovedSession`: seed sesi `Status=="InProgress"` + `RemovedAt != null` → assert query nyata `db.AssessmentSessions.AnyAsync(s => s.UserId==uid && s.Status=="InProgress" && s.RemovedAt==null)` = false (jalankan query asli di DB, BUKAN mock Hub).

    **Wave-0 status:** test ini ditulis SEBELUM implementasi guard/exclude (Task 2-3). Tests 1-2 (exclude) + 4-6 (guard) akan RED untuk guard/exclude yang belum ada, GREEN setelah Task 2-3; test 3 (boundary) GREEN sejak awal (tetap harus GREEN di akhir). Pastikan file compile + run (boleh sebagian RED di tahap ini — itu kontrak Wave-0). Beri `[Fact]` per test; reuse `[Trait("Category","...")]` bila perlu SQLEXPRESS (JoinBatch predicate test boleh InMemory atau disposable).

    Catatan: jika `StartExam`/`SubmitExam` controller dependency terlalu berat untuk InMemory (banyak dep ter-dereference: `_userManager`, `_hubContext`, `GradingService`), gunakan `WebApplicationFactory` ATAU seed via disposable SQLEXPRESS + panggil action dengan dep minimal yang ASLI dipanggil. Hindari `null!` pada dep yang DI-dereference di jalur guard (guard ada di awal action, sebelum grading — sebagian dep mungkin tak ter-reach; verifikasi via run).
  </action>
  <verify>
    <automated>dotnet build HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | tail -3 && dotnet test HcPortal.Tests --filter "FullyQualifiedName~ParticipantRemoval" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/ParticipantRemovalGuardTests.cs` exists, class `ParticipantRemovalGuardTests`
    - `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` exits 0 (compiles)
    - Contains ≥6 `[Fact]`: StartExam block, SubmitExam block, JoinBatch reject, ManageAssessmentTab exclude, MonitoringDetail count exclude, UserAssessmentHistory boundary
    - NO test rewrites the exclude/guard predicate then asserts it (de-tautology: grep for `.Where(.*RemovedAt` inside test that is then asserted → must drive REAL action/query instead)
    - Boundary test `UserAssessmentHistory_StillShows_RemovedSession` present
    - `dotnet test --filter ~ParticipantRemoval` runs (guard/exclude tests may be RED pre-impl — Wave 0 contract)
  </acceptance_criteria>
  <done>Test file compiles + runs; 6 de-tautologis facts ada (3 guard + 2 exclude + 1 boundary); guard/exclude tests RED sampai Task 2-3 (Wave-0 contract); boundary test asserts removed-session VISIBLE di riwayat pekerja.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard re-entry — StartExam + SubmitExam + JoinBatch + (A1) Save* hub methods</name>
  <read_first>
    - Controllers/CMPController.cs :901-1004 (StartExam: load/authz/Abandoned-block-template :966-971/mark-InProgress :999-1004) + :1573-1612 (SubmitExam: load/authz :1589-1592/gate :1596)
    - Hubs/AssessmentHub.cs :21-34 (JoinBatch) + :134-150 (SaveTextAnswer predicate :143-144) + :200-215 (SaveMultipleAnswer predicate :209-210)
    - 409-PATTERNS.md §"CMPController.cs (2 guards)" + §"AssessmentHub.cs (JoinBatch predicate)" (excerpt verbatim + A1 flag)
    - 409-CONTEXT.md D-02/D-02a/D-04 (pesan locked + penempatan)
  </read_first>
  <files>Controllers/CMPController.cs, Hubs/AssessmentHub.cs</files>
  <behavior>
    - StartExam dengan sesi RemovedAt != null → RedirectToAction("Assessment") + TempData["Error"]="Anda telah dikeluarkan dari ujian ini.", sesi TIDAK ter-mark InProgress (guard sebelum :999-1004)
    - SubmitExam dengan sesi RemovedAt != null → RedirectToAction("Assessment") + pesan sama, grading di-skip (guard sebelum :1596), Score tak berubah
    - JoinBatch dengan sesi InProgress+RemovedAt != null → silent return (predikat AnyAsync += `&& s.RemovedAt == null`), tidak masuk group
    - SaveTextAnswer/SaveMultipleAnswer dengan sesi RemovedAt != null → silent return (predikat session-load += `&& s.RemovedAt == null`), jawaban tak tersimpan
  </behavior>
  <action>
    **A. CMPController.StartExam** — sisipkan SETELAH owner-check (`if (assessment.UserId != user.Id && !Admin && !HC) return Forbid();` `:911-912`), SEBELUM auto-transition Upcoming→Open (`:915`). Penempatan paling awal agar sesi removed TAK PERNAH ter-mark InProgress (D-04 invariant sisi worker). VERBATIM:
    ```csharp
    // v32.5 Phase 409 (PRMV-03 / D-02): sesi soft-removed tak boleh lanjut ujian. Guard SEBELUM mark-InProgress.
    if (assessment.RemovedAt != null)
    {
        TempData["Error"] = "Anda telah dikeluarkan dari ujian ini.";
        return RedirectToAction("Assessment");
    }
    ```

    **B. CMPController.SubmitExam** — sisipkan SETELAH owner-check (`:1589-1592`), SEBELUM `ShouldGateMissingStart` (`:1596`) / grading. Jawaban di-discard (D-02a). VERBATIM:
    ```csharp
    // v32.5 Phase 409 (PRMV-03 / D-02a): discard submit dari sesi soft-removed SEBELUM grading.
    if (assessment.RemovedAt != null)
    {
        TempData["Error"] = "Anda telah dikeluarkan dari ujian ini.";
        return RedirectToAction("Assessment");
    }
    ```
    Pesan LOCKED verbatim kedua site. `assessment` sudah loaded + null-checked → `RemovedAt` tersedia, no extra query.

    **C. AssessmentHub.JoinBatch** (`:29-31`) — tambah 1 term ke predikat `AnyAsync`, PERTAHANKAN silent-skip (D-04 — soft-remove TIDAK ubah Status, guard WAJIB eksplisit cek RemovedAt):
    ```csharp
    var hasSession = await db.AssessmentSessions
        .AnyAsync(s => s.UserId == userId && s.Status == "InProgress" && s.RemovedAt == null);
    if (!hasSession) return;
    ```

    **D. (A1 IN scope — orchestrator decided) AssessmentHub.SaveTextAnswer** (`:143-144`) + **SaveMultipleAnswer** (`:209-210`) — tambah `&& s.RemovedAt == null` ke predikat session-load `FirstOrDefaultAsync`, PERTAHANKAN silent return-if-null existing (`if (session == null) { log; return; }`). Rasional: PRMV-03 "jawaban setelah penghapusan tidak terhitung" — worker removed mid-exam dengan koneksi SignalR hidup tak boleh tulis jawaban via Hub langsung (JoinBatch guard tak menendang koneksi yang sudah join). VERBATIM (kedua method):
    ```csharp
    var session = await db.AssessmentSessions
        .FirstOrDefaultAsync(s => s.Id == sessionId && s.UserId == userId && s.Status == "InProgress" && s.RemovedAt == null);
    ```
    JANGAN ubah message/log/return existing — hanya tambah term predikat.

    **JANGAN** tambah cabang "removed" ke `DeriveUserStatus` (status-derivation tak berubah di 409). **JANGAN** pakai `Status` untuk deteksi removed (akar D-04).
  </action>
  <verify>
    <automated>grep -c "Anda telah dikeluarkan dari ujian ini." Controllers/CMPController.cs | grep -q 2 && grep -q 's.RemovedAt == null' Hubs/AssessmentHub.cs && [ "$(grep -c 'RemovedAt == null' Hubs/AssessmentHub.cs)" -ge 3 ] && dotnet build HcPortal.csproj 2>&1 | tail -3 && dotnet test HcPortal.Tests --filter "FullyQualifiedName~ParticipantRemoval" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/CMPController.cs` contains `"Anda telah dikeluarkan dari ujian ini."` exactly 2 times (StartExam + SubmitExam)
    - StartExam guard placed before line that sets `assessment.Status = "InProgress"` (grep: RemovedAt guard appears before `Status = "InProgress"` mark)
    - SubmitExam guard placed before `ShouldGateMissingStart`/grading
    - `Hubs/AssessmentHub.cs` contains `&& s.RemovedAt == null` in JoinBatch AnyAsync, SaveTextAnswer FirstOrDefaultAsync, SaveMultipleAnswer FirstOrDefaultAsync (≥3 occurrences of `RemovedAt == null`)
    - JoinBatch keeps `if (!hasSession) return;` silent-skip (no throw added)
    - `dotnet build HcPortal.csproj` exits 0
    - `dotnet test --filter ~ParticipantRemoval` guard tests (StartExam/SubmitExam/JoinBatch) GREEN
  </acceptance_criteria>
  <done>6 guard insertion live (2 CMPController messages + JoinBatch + 2 Save* predicates); semua silent-skip/redirect convention dipertahankan; guard tests GREEN; build hijau.</done>
</task>

<task type="auto">
  <name>Task 3: Exclude-removed — 3 query monitoring batch-aktif (NOT global filter)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs :119-121 (managementQuery) + :2822-2824 (AssessmentMonitoring.query) + :3328-3332 (AssessmentMonitoringDetail.query) + :179 grouping + :3431-3442 counts (inherit, JANGAN edit)
    - Controllers/AssessmentAdminController.cs :5262 (UserAssessmentHistory — BOUNDARY, JANGAN sentuh)
    - 409-PATTERNS.md §"AssessmentAdminController.cs (3 exclude sites)" (excerpt verbatim) + §"Boundary" table
    - 409-RESEARCH.md §"Don't Hand-Roll" (HasQueryFilter FORBIDDEN) + Pitfall 3 (over-exclude)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
    Tambah `.Where(a => a.RemovedAt == null)` ke 3 query monitoring batch-aktif (D-01). **JANGAN** pakai EF `HasQueryFilter` global (FORBIDDEN — akan sembunyikan removed dari riwayat pekerja D-01a + panel 412 yang justru butuh baca removed). Per-surface eksplisit `.Where`.

    **Site 1 — `managementQuery` (`:119-121`)** — tambah `.Where` setelah `.AsNoTracking()`:
    ```csharp
    var managementQuery = _context.AssessmentSessions
        .AsNoTracking()
        .Where(a => a.RemovedAt == null)   // v32.5 Phase 409 PLIV-01 — exclude soft-removed
        .AsQueryable();
    ```
    > Cover grouping `:179` + pre/post grouping `:157` (konsumsi `allSessions` ← `managementQuery`) — inherit otomatis. JANGAN edit blok grouping.

    **Site 2 — `AssessmentMonitoring.query` (`:2822-2824`)** — identik:
    ```csharp
    var query = _context.AssessmentSessions
        .AsNoTracking()
        .Where(a => a.RemovedAt == null)   // v32.5 Phase 409 PLIV-01
        .AsQueryable();
    ```

    **Site 3 — `AssessmentMonitoringDetail.query` (`:3328-3332`)** — tambah `&& a.RemovedAt == null` ke predikat `.Where` existing (menggerakkan SEMUA count `:3431-3442` termasuk `InProgressCount :3439`):
    ```csharp
    var query = _context.AssessmentSessions
        .Include(a => a.User)
        .Where(a => a.Title == title
                 && a.Category == category
                 && a.Schedule.Date == scheduleDate.Date
                 && a.RemovedAt == null);   // v32.5 Phase 409 PLIV-01 — InProgressCount/TotalCount ikut bersih
    ```
    > `sessionViewModels :3395` ← `sessions` ← query → semua count inherit. JANGAN edit blok count.

    **Ketiga independen — WAJIB edit ketiganya** (anti-pattern: lupa salah satu).

    **BOUNDARY — JANGAN sentuh (D-01a / Pitfall 3):** `UserAssessmentHistory :5262` (per-pekerja `a.UserId == userId`, `PassedCount`/`PassRate` konteks pekerja — sertifikat utuh), `WorkerDataService.GetUnifiedRecords`, jalur sertifikat pekerja. **A2 (OUT scope, orchestrator decided):** JANGAN tambah exclude ke `ExportAssessmentResults`/`BulkExportPdf`/`GetDeleteImpact.certCount` — defer ke 412/413 bila perlu (catat Deferred di SUMMARY).
  </action>
  <verify>
    <automated>[ "$(grep -c 'a.RemovedAt == null' Controllers/AssessmentAdminController.cs)" -ge 3 ] && ! grep -q 'HasQueryFilter' Data/ApplicationDbContext.cs && dotnet build HcPortal.csproj 2>&1 | tail -3 && dotnet test HcPortal.Tests --filter "FullyQualifiedName~ParticipantRemoval" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `Controllers/AssessmentAdminController.cs` contains `a.RemovedAt == null` ≥3 times (managementQuery, AssessmentMonitoring.query, AssessmentMonitoringDetail.query)
    - `Data/ApplicationDbContext.cs` does NOT contain `HasQueryFilter` (global filter FORBIDDEN)
    - `UserAssessmentHistory` (:5262) NOT modified (boundary — grep around :5262 shows no `RemovedAt == null` added)
    - `ExportAssessmentResults`/`BulkExportPdf`/`GetDeleteImpact` NOT modified (A2 out of scope)
    - `dotnet build HcPortal.csproj` exits 0
    - `dotnet test --filter ~ParticipantRemoval` ALL green (exclude tests 1-2 + boundary test 3 GREEN)
    - Boundary test `UserAssessmentHistory_StillShows_RemovedSession` GREEN (removed session still visible in worker history)
  </acceptance_criteria>
  <done>3 exclude .Where insertion live (monitoring batch-aktif); NO global HasQueryFilter; UserAssessmentHistory + export/impact UNTOUCHED (boundary); exclude+boundary tests GREEN; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker browser → CMPController | Untrusted: removed worker dapat POST SubmitExam / reload StartExam langsung (bypass UI) |
| worker SignalR connection → AssessmentHub | Untrusted: koneksi hidup pasca-remove dapat invoke JoinBatch/SaveTextAnswer/SaveMultipleAnswer langsung |
| admin browser → AssessmentAdminController | Trusted (Admin/HC) read-path monitoring; removed harus hilang dari count aktif |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-409-01 | Elevation of Privilege | CMPController.StartExam (removed worker resumes via reload) | mitigate | Server guard `if (assessment.RemovedAt != null) redirect` SEBELUM mark-InProgress :999-1004; session never re-marked. Test `StartExam_Blocks_RemovedSession` (HIGH — blocks) |
| T-409-02 | Tampering | CMPController.SubmitExam (removed worker re-submits via direct POST bypass UI) | mitigate | Server guard SEBELUM grading :1596; jawaban discard, Score unchanged. Test `SubmitExam_Blocks_RemovedSession` (HIGH — blocks) |
| T-409-03 | Information disclosure | AssessmentHub.JoinBatch (removed worker re-joins monitor/batch group) | mitigate | Predicate `&& s.RemovedAt == null` (D-04); silent-skip. Test `JoinBatch_Predicate_Rejects_RemovedSession` |
| T-409-08 | Tampering | AssessmentHub.SaveTextAnswer/SaveMultipleAnswer (removed worker writes answers via live Hub, bypass) | mitigate | (A1 IN scope) Predicate `&& s.RemovedAt == null` on session-load; silent return. Defense-in-depth closes PRMV-03 "jawaban tak terhitung" gap (Pitfall 2) |
| T-409-09 | Repudiation / Availability | Over-exclude hides legitimate worker certificates (UserAssessmentHistory/Records) | mitigate | EXPLICIT boundary: exclude ONLY 3 admin batch queries; NO global HasQueryFilter; UserAssessmentHistory untouched. Boundary test `UserAssessmentHistory_StillShows_RemovedSession` (anti over-exclude) |
| T-409-10 | Tampering | RemovalReason free-text XSS at render | accept (out of 409) | RemovalReason penulisan = Phase 411; XSS-at-render = Phase 412 (panel) concern. 409 read-path only, EF parameterized. Flag noted for 412 |

**High-severity threats T-409-01/02 (removed-worker resubmit/resume) are the security-critical mitigations — both server-authoritative (not UI-only), both with blocking tests. Block gate: high threats must have GREEN tests before phase ships.**
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` 0 error.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~ParticipantRemoval"` ALL green (6 facts: 3 guard + 2 exclude + 1 boundary).
- Full suite `dotnet test HcPortal.Tests` hijau tanpa regresi (termasuk guard Phase 391/398.1 existing + `AssessmentWindowRemovalTests`).
- `dotnet run` (ASPNETCORE_ENVIRONMENT=Development) → localhost:5277 boot tanpa error.
- Manual sanity: removed session (set RemovedAt via DB) → StartExam redirect "Anda telah dikeluarkan dari ujian ini." (opsional, e2e penuh = Phase 413).
</verification>

<success_criteria>
- ROADMAP SC2: sesi `RemovedAt != null` tak bisa StartExam/SubmitExam (pesan jelas; jawaban tak terhitung). ✓ (+A1 Save*)
- ROADMAP SC3: JoinBatch menolak join sesi removed (silent-skip). ✓
- ROADMAP SC4: monitoring/grouping/detail + semua count exclude `RemovedAt != null`; UserAssessmentHistory boundary TETAP tampil removed. ✓
- ROADMAP SC5: build 0 error + test hijau + run @5277. ✓
- PRMV-03 fully delivered; PLIV-01 exclude-foundation landing (panel UI = Phase 412).
</success_criteria>

<output>
After completion, create `.planning/phases/409-data-foundation-re-entry-guards-exclude-removed-query/409-02-SUMMARY.md`.
Catat Deferred di SUMMARY: **A2 OUT scope** — `ExportAssessmentResults`/`BulkExportPdf`/`GetDeleteImpact.certCount` TIDAK di-exclude di 409 (revisit Phase 412/413 bila perlu). **T-409-10** RemovalReason XSS-at-render = concern Phase 412.
</output>
