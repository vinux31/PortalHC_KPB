---
phase: 410-add-participant-backend-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PART-06, PART-07]
migration: false
tags: [assessment, participant, backend, ajax, signalr-deferred]
user_setup: []

must_haves:
  truths:
    - "Admin/HC dapat menambah ≥1 peserta baru ke batch (belum-progres maupun ada InProgress) via POST AddParticipantsLive"
    - "Setiap peserta baru mendapat AssessmentSession ber-status siap-mulai (Open/Upcoming, NEVER InProgress; StartedAt/CompletedAt/RemovedAt = null)"
    - "Setiap peserta baru mendapat UserPackageAssignment EAGER (dibuat dalam transaksi add, bukan lazy) bila batch punya AssessmentPackages"
    - "Penambahan ditolak 400 dengan pesan 'Window ujian sudah tutup, tidak bisa tambah peserta.' bila ExamWindowCloseDate sudah lewat (WIB=UTC+7)"
    - "User yang sudah punya sesi APAPUN di batch (aktif maupun removed) di-skip diam, masuk skipped[] (idempoten)"
    - "Batch Pre/Post membuat pasangan Pre+Post (2 sesi, LinkedSessionId cross-set) per peserta baru"
    - "Sesi Proton (Category=='Assessment Proton') ditolak dengan pesan jelas, tanpa write"
    - "Seluruh create bersifat atomic (gagal → rollback, 0 sesi setengah-jadi)"
    - "GetEligibleParticipantsToAdd mengembalikan semua pekerja IsActive minus yang sudah punya sesi APAPUN di batch (tanpa filter unit/section)"
    - "Response JSON: { added:[{id,fullName,nip,status}], skipped:[{fullName,nip}], addedCount, skippedCount }"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "AddParticipantsLive (HttpPost) + GetEligibleParticipantsToAdd (HttpGet) + BuildReadyParticipantSession helper"
      contains: "public async Task<IActionResult> AddParticipantsLive("
  key_links:
    - from: "AddParticipantsLive"
      to: "DeriveReadyStatus"
      via: "status derivation per sesi baru"
      pattern: "DeriveReadyStatus\\(.*Schedule"
    - from: "AddParticipantsLive"
      to: "ShuffleEngine.BuildQuestionAssignment"
      via: "eager UserPackageAssignment create (mirror StartExam :1074)"
      pattern: "ShuffleEngine\\.BuildQuestionAssignment"
    - from: "AddParticipantsLive"
      to: "_auditLog.LogAsync"
      via: "audit AddParticipantLive"
      pattern: "\"AddParticipantLive\""
---

<objective>
Tambah dua endpoint AJAX backend ke `Controllers/AssessmentAdminController.cs` untuk penambahan peserta live (milestone v32.5 Phase 410):

1. **`GetEligibleParticipantsToAdd` (HttpGet)** — picker: semua pekerja `IsActive` minus yang sudah punya sesi APAPUN di batch (D-01/D-02).
2. **`AddParticipantsLive` (HttpPost)** — resolve batch dari sesi representatif → reject Proton → window guard → idempotency skip → buat `AssessmentSession` + `UserPackageAssignment` EAGER ber-status siap-mulai dalam 1 transaksi atomic → cabang Pre/Post pair → notif + audit → return JSON `added[]`/`skipped[]`.
3. **`BuildReadyParticipantSession` (helper privat)** — pabrik sesi siap-mulai (inherit field dari rep, status `DeriveReadyStatus`, kolom removal null) yang dipakai kedua cabang (Standard + Pre/Post).

Purpose: PART-06 (auto Session+UPA ready-status + window-reject + idempotent) + PART-07 (Pre/Post pair). Output dikonsumsi UI Phase 412 (DOM inject) dan endpoint picker.
Output: 2 endpoint + 1 helper di `AssessmentAdminController.cs`. migration=FALSE.

**KEPUTUSAN DESAIN A1 — LOCKED oleh orchestrator (OVERRIDE rekomendasi RESEARCH opsi-A lazy):**
`UserPackageAssignment` dibuat **EAGER** dalam transaksi add (BUKAN lazy via StartExam), per spec §B1.4 + PART-06 literal. **Reuse logika UPA-creation dari `CMPController.StartExam :1064-1102`** (cermin ke `BuildReadyParticipantSession`/jalur add). JANGAN andalkan lazy-creation. Test 410-02 + 413 akan assert UPA ada setelah AddParticipantsLive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/410-add-participant-backend-live/410-CONTEXT.md
@.planning/phases/410-add-participant-backend-live/410-RESEARCH.md
@.planning/phases/410-add-participant-backend-live/410-PATTERNS.md
@docs/superpowers/specs/2026-06-19-flexible-add-remove-participant-design.md

<interfaces>
<!-- Kontrak existing yang dipakai endpoint baru. Diekstrak dari kode (VERIFIED). Executor pakai langsung — TIDAK perlu eksplorasi. -->

Controller constructor (Controllers/AssessmentAdminController.cs:33-45) — field tersedia:
```csharp
// _context (ApplicationDbContext), _userManager (UserManager<ApplicationUser>),
// _auditLog (AuditLogService), _notificationService (INotificationService),
// _logger (ILogger<AssessmentAdminController>), _hubContext (IHubContext<AssessmentHub>) — JANGAN dipakai (D-04)
```

DeriveReadyStatus — PAKAI LANGSUNG (private static, kelas sama, AssessmentAdminController.cs:2276-2283):
```csharp
private static string DeriveReadyStatus(DateTime schedule, DateTime? examWindowCloseDate)
{
    var nowWib = DateTime.UtcNow.AddHours(7);
    if (schedule <= nowWib) return AssessmentConstants.AssessmentStatus.Open;   // "Open"
    return AssessmentConstants.AssessmentStatus.Upcoming;                        // "Upcoming"
}
```

IsPrePostSession — single-source predikat (Controllers/AdminBaseController.cs:248-249, base class):
```csharp
public static bool IsPrePostSession(AssessmentSession session)
    => session.AssessmentType == "PreTest" || session.AssessmentType == "PostTest";
```

SiblingSessionQuery (Helpers/SiblingSessionQuery.cs:14) — untuk hitung siblingSessionIds + workerIndex saat eager-UPA:
```csharp
public static Expression<Func<AssessmentSession, bool>> SiblingPrePostAwarePredicate(
    string title, string category, DateTime scheduleDate, string? assessmentType);
// isPrePost → isolasi by AssessmentType; non-PrePost → grup bersama (Standard/""/null)
```

ShuffleEngine (Helpers/ShuffleEngine.cs) — eager UPA, cermin StartExam :1074-1082:
```csharp
List<int> BuildQuestionAssignment(List<AssessmentPackage> packages, bool shuffleQuestions, int workerIndex, Random rng);
Dictionary<int,List<int>> BuildOptionShuffle(IEnumerable<PackageQuestion> assignedQuestions, bool shuffleOptions, Random rng);
```

UserPackageAssignment model (Models/UserPackageAssignment.cs) — field wajib:
```csharp
int AssessmentSessionId; int AssessmentPackageId; string UserId;
string ShuffledQuestionIds = "[]";          // JsonSerializer.Serialize(List<int>)
string ShuffledOptionIdsPerQuestion = "{}"; // JsonSerializer.Serialize(Dictionary<int,List<int>>)
int? SavedQuestionCount;                     // = shuffledIds.Count
```

AuditLogService.LogAsync (Services/AuditLogService.cs:21):
```csharp
Task LogAsync(string actorUserId, string actorName, string actionType, string description, int? targetId = null, string? targetType = null);
```

INotificationService.SendAsync (Services/INotificationService.cs:20):
```csharp
Task<bool> SendAsync(string userId, string type, string title, string message, string? actionUrl = null);
```

ApplicationUser (Models/ApplicationUser.cs): `string FullName`, `string? NIP`, `bool IsActive`.
AssessmentSession removal cols (Phase 409): `DateTime? RemovedAt`, `string? RemovedBy`, `string? RemovalReason`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GetEligibleParticipantsToAdd + BuildReadyParticipantSession helper</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:2151-2283 (BULK ASSIGN block — idempotency filter :2161-2174, session-init field-inherit :2195-2217, DeriveReadyStatus :2276-2283) — analog utama
    - Controllers/AssessmentAdminController.cs:33-56 (constructor — field DI tersedia)
    - .planning/phases/410-add-participant-backend-live/410-PATTERNS.md §"GetEligibleParticipantsToAdd" + §"BuildReadyParticipantSession" (analog file:line + excerpt)
    - .planning/phases/410-add-participant-backend-live/410-RESEARCH.md §"Eligible query skeleton" + §"Don't Hand-Roll" (key insight UPA EAGER override)
  </read_first>
  <behavior>
    - Test 1 (eligible exclude-by-batch, D-01): batch berisi 1 sesi aktif (user A) + 1 sesi RemovedAt!=null (user B); pekerja IsActive C belum punya sesi → eligible HANYA mengembalikan C; A DAN B excluded (removed TETAP excluded — hanya balik via Restore).
    - Test 2 (eligible no unit/section, D-02): pekerja IsActive dari unit/section manapun yang belum punya sesi di batch → muncul di eligible (tanpa filter unit/section).
    - Test 3 (rep absen): sessionId tak ada → NotFound/404.
    - Test 4 (helper ready-status): BuildReadyParticipantSession(rep, userId, actorId) → AssessmentSession dengan Status ∈ {Open, Upcoming} (DeriveReadyStatus), StartedAt==null, CompletedAt==null, RemovedAt==null, AssessmentType == (rep.AssessmentType ?? "Standard").
  </behavior>
  <action>
**1a. `GetEligibleParticipantsToAdd` (HttpGet)** — tambah method baru di `AssessmentAdminController` (dekat region BULK ASSIGN / setelah DeriveReadyStatus `:2283`). Attribute persis:
```csharp
[HttpGet]
[Authorize(Roles = "Admin, HC")]
public async Task<IActionResult> GetEligibleParticipantsToAdd(int sessionId)
```
Body:
- Resolve rep server-side (anti-tampering — JANGAN terima batchKey mentah): `var rep = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == sessionId);` → `if (rep == null) return NotFound(new { error = "Sesi tidak ditemukan." });`
- Batch-key = `Title + Category + Schedule.Date` (sama persis BULK ASSIGN `:2163-2165`). Query user yang SUDAH punya sesi APAPUN di batch — **TANPA** filter `RemovedAt` (D-01 verbatim):
```csharp
var alreadyInBatch = await _context.AssessmentSessions
    .Where(a => a.Title == rep.Title && a.Category == rep.Category && a.Schedule.Date == rep.Schedule.Date)
    .Select(a => a.UserId).Distinct().ToListAsync();
```
- Eligible = `_context.Users.Where(u => u.IsActive)` minus `alreadyInBatch` (D-02 — **TANPA** filter unit/section):
```csharp
var eligible = await _context.Users
    .Where(u => u.IsActive && !alreadyInBatch.Contains(u.Id))
    .OrderBy(u => u.FullName)
    .Select(u => new { id = u.Id, fullName = u.FullName, nip = u.NIP })
    .ToListAsync();
return Json(eligible);
```
> D-02 OVERRIDE spek §B4: JANGAN batasi ke unit/section assign-awal — komentar inline: `// D-02 (OVERRIDE spec §B4): semua pekerja IsActive, tanpa filter unit/section — admin bebas tambah siapa saja`. Per A2 (Claude discretion): biarkan semua IsActive (admin/HC jarang jadi peserta + idempotency melindungi); JANGAN tambah GetUsersInRoleAsync (pola baru, tak perlu).

**1b. `BuildReadyParticipantSession` (helper privat)** — pabrik sesi siap-mulai, dipakai cabang Standard di Task 2 (Pre/Post inline pair di Task 2). Tambah `private` method:
```csharp
// Phase 410: pabrik AssessmentSession siap-mulai (inherit rep, status ready, kolom removal null).
// Cermin BULK ASSIGN :2195-2217. AssessmentType ?? "Standard" (Pitfall 2 — NOT NULL Dev/Prod).
private AssessmentSession BuildReadyParticipantSession(AssessmentSession rep, string userId, string? actorId)
```
Body — set field inherit (cermin `:2195-2217`):
```csharp
return new AssessmentSession
{
    Title = rep.Title,
    Category = rep.Category,
    AssessmentType = rep.AssessmentType ?? "Standard",
    Schedule = rep.Schedule,
    DurationMinutes = rep.DurationMinutes,
    Status = DeriveReadyStatus(rep.Schedule, rep.ExamWindowCloseDate),
    PassPercentage = rep.PassPercentage,
    AllowAnswerReview = rep.AllowAnswerReview,
    ShuffleQuestions = rep.ShuffleQuestions,
    ShuffleOptions = rep.ShuffleOptions,
    GenerateCertificate = rep.GenerateCertificate,
    ExamWindowCloseDate = rep.ExamWindowCloseDate,
    IsTokenRequired = rep.IsTokenRequired,
    AccessToken = rep.AccessToken ?? "",
    BannerColor = rep.BannerColor,
    Progress = 0,
    UserId = userId,
    CreatedBy = actorId
    // StartedAt/CompletedAt/RemovedAt/RemovedBy/RemovalReason = null (default) — WAJIB null (PART-06)
};
```
> Verifikasi nama field via Read AssessmentSession.cs bila kompilasi gagal (mis. `AccessToken` non-null string). JANGAN set `LinkedGroupId`/`LinkedSessionId` di helper ini — itu khusus Pre/Post (Task 2 inline). JANGAN set Status hardcoded — selalu via DeriveReadyStatus.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` contains `public async Task<IActionResult> GetEligibleParticipantsToAdd(int sessionId)`
    - `AssessmentAdminController.cs` contains `private AssessmentSession BuildReadyParticipantSession(AssessmentSession rep, string userId, string? actorId)`
    - `GetEligibleParticipantsToAdd` query: grep `alreadyInBatch` block present; NO `&& a.RemovedAt == null` inside the alreadyInBatch query (D-01 — exclude sesi APAPUN)
    - `GetEligibleParticipantsToAdd` query: NO `.Section`/`.Unit` filter on Users (D-02 — no unit/section scope)
    - `BuildReadyParticipantSession` contains `AssessmentType = rep.AssessmentType ?? "Standard"` and `Status = DeriveReadyStatus(rep.Schedule, rep.ExamWindowCloseDate)`
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error
  </acceptance_criteria>
  <done>GetEligibleParticipantsToAdd mengembalikan JSON eligible (semua IsActive minus sesi APAPUN di batch, no unit/section filter); BuildReadyParticipantSession menghasilkan sesi ready-status dengan kolom removal null; build 0 error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AddParticipantsLive (HttpPost) — atomic create + eager UPA + Pre/Post pair + Proton/window guard + JSON</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/CMPController.cs:968-969 (window guard mirror — WIB=UTC+7) + :1038-1117 (eager UPA: siblingSessionIds + sortedSiblingIds + workerIndex + BuildQuestionAssignment + BuildOptionShuffle + sentinelPackage + DbUpdateException race-handling)
    - Controllers/AssessmentAdminController.cs:2151-2272 (BULK ASSIGN — transaction + notif post-commit + audit, analog terdekat) + :1942-1998 (Pre/Post pair-create + cross-link — gunakan INI bukan :1926)
    - .planning/phases/410-add-participant-backend-live/410-PATTERNS.md §"AddParticipantsLive" + §"Pre/Post branch" (excerpt verbatim + pesan locked) + §"Cross-cutting Pitfalls"
    - .planning/phases/410-add-participant-backend-live/410-RESEARCH.md §"Architecture Patterns" Pattern 1-7 + §"Common Pitfalls" 1-6
  </read_first>
  <behavior>
    - Test 1 (ready-status write): add 1 user baru ke batch standard → 1 AssessmentSession dibuat, Status ∈ {Open,Upcoming} (NEVER InProgress), StartedAt/CompletedAt/RemovedAt null.
    - Test 2 (eager UPA): batch punya AssessmentPackages → setelah add, UserPackageAssignment tercipta untuk sesi baru (AssessmentSessionId set, ShuffledQuestionIds non-"[]" bila ada soal); batch tanpa packages → tak ada UPA (skip, tak error).
    - Test 3 (idempotent): user yang sudah punya sesi APAPUN (aktif maupun removed) di batch → masuk skipped[], TIDAK dobel-create.
    - Test 4 (window reject): rep.ExamWindowCloseDate < now(WIB) → 400 "Window ujian sudah tutup, tidak bisa tambah peserta." + 0 sesi baru.
    - Test 5 (Proton reject): rep.Category=="Assessment Proton" → 400 pesan jelas + 0 write.
    - Test 6 (Pre/Post pair): rep AssessmentType PreTest/PostTest → 2 sesi (PreTest+PostTest) + LinkedSessionId cross-set, keduanya ready-status.
    - Test 7 (atomic): exception saat create → RollbackAsync, 0 sesi tertinggal.
    - Test 8 (JSON shape): response = { added:[{id,fullName,nip,status}], skipped:[{fullName,nip}], addedCount, skippedCount }.
  </behavior>
  <action>
Tambah method `AddParticipantsLive` di `AssessmentAdminController` (setelah `GetEligibleParticipantsToAdd`). Attribute LOCKED (spec §H):
```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> AddParticipantsLive(int sessionId, List<string> userIds)
```

**Langkah 1 — resolve rep + validasi input** (anti-tampering, server-authoritative):
```csharp
if (userIds == null || userIds.Count == 0)
    return BadRequest(new { error = "Tidak ada peserta yang dipilih." });
if (userIds.Count > 50)   // rate-limit cermin BULK ASSIGN :2032 (anti mass-add DoS)
    return BadRequest(new { error = "Maksimal 50 peserta per permintaan." });
var rep = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == sessionId);
if (rep == null) return NotFound(new { error = "Sesi tidak ditemukan." });
```

**Langkah 2 — Proton reject (guard dini, sebelum write)** — literal konsisten repo (`:945/1191/3456`):
```csharp
if (rep.Category == "Assessment Proton")
    return BadRequest(new { error = "Penambahan peserta tidak didukung untuk Assessment Proton." });
```

**Langkah 3 — window guard (mirror CMPController.StartExam :968-969, WIB=UTC+7)** — pesan LOCKED:
```csharp
if (rep.ExamWindowCloseDate.HasValue && DateTime.UtcNow.AddHours(7) > rep.ExamWindowCloseDate.Value)
    return BadRequest(new { error = "Window ujian sudah tutup, tidak bisa tambah peserta." });
// window null (.HasValue==false) = bebas
```

**Langkah 4 — idempotency by existence (D-01: skip sesi APAPUN, TANPA filter RemovedAt)**:
```csharp
var alreadyInBatch = await _context.AssessmentSessions
    .Where(a => a.Title == rep.Title && a.Category == rep.Category && a.Schedule.Date == rep.Schedule.Date)
    .Select(a => a.UserId).Distinct().ToListAsync();
var toAdd   = userIds.Where(uid => !alreadyInBatch.Contains(uid)).Distinct().ToList();
var skippedIds = userIds.Where(uid => alreadyInBatch.Contains(uid)).Distinct().ToList();
```
> KOMENTAR inline: `// D-01: skip user dengan sesi APAPUN (tanpa pandang RemovedAt) — user removed hanya balik via Restore (411), bukan Add`. JANGAN tambah `&& a.RemovedAt == null`.

**Langkah 5 — validate user IDs exist** (cermin `:2178-2189`):
```csharp
var userDictionary = await _context.Users.Where(u => toAdd.Contains(u.Id)).ToDictionaryAsync(u => u.Id);
var missingUsers = toAdd.Except(userDictionary.Keys).ToList();
if (missingUsers.Any())
    return BadRequest(new { error = $"User tidak valid: {string.Join(", ", missingUsers)}" });
```

**Langkah 6 — resolve actor** (VERIFIED `:5582-5583`):
```csharp
var hcUser = await _userManager.GetUserAsync(User);
var actorId = hcUser?.Id ?? "";
var actorName = string.IsNullOrWhiteSpace(hcUser?.NIP) ? (hcUser?.FullName ?? "Unknown") : $"{hcUser.NIP} - {hcUser.FullName}";
```

**Langkah 7 — atomic create dalam transaksi** (cermin BULK ASSIGN `:2219-2258` + InjectBatchAsync template). Bila `toAdd` kosong → langsung ke Langkah 9 (return JSON, 0 added). Untuk `toAdd` non-kosong:
```csharp
var createdSessions = new List<AssessmentSession>();   // semua sesi yang dibuat (untuk JSON + eager-UPA)
bool isPrePost = IsPrePostSession(rep);
using var transaction = await _context.Database.BeginTransactionAsync();
try
{
    foreach (var uid in toAdd)
    {
        if (isPrePost)
        {
            // === Pre/Post pair (PART-07) — cermin :1942-1998, ganti hardcoded "Upcoming" → DeriveReadyStatus ===
            var linkedGroupId = rep.LinkedGroupId!.Value;     // :1946 — gabung grup existing
            var newPre  = BuildReadyParticipantSession(rep, uid, actorId);
            newPre.AssessmentType = "PreTest";  newPre.LinkedGroupId = linkedGroupId;
            var newPost = BuildReadyParticipantSession(rep, uid, actorId);
            newPost.AssessmentType = "PostTest"; newPost.LinkedGroupId = linkedGroupId;
            _context.AssessmentSessions.AddRange(newPre, newPost);
            await _context.SaveChangesAsync();                 // dapat Id
            newPre.LinkedSessionId  = newPost.Id;              // :1996-1997 cross-link
            newPost.LinkedSessionId = newPre.Id;
            await _context.SaveChangesAsync();
            createdSessions.Add(newPre); createdSessions.Add(newPost);
        }
        else
        {
            var session = BuildReadyParticipantSession(rep, uid, actorId);
            _context.AssessmentSessions.Add(session);
            await _context.SaveChangesAsync();                 // dapat Id
            createdSessions.Add(session);
        }
    }

    // === EAGER UPA (A1 LOCKED) — cermin CMPController.StartExam :1038-1117, per sesi baru ===
    await CreateEagerAssignmentsAsync(createdSessions, rep);

    // audit DALAM tx (sebelum commit)
    await _auditLog.LogAsync(actorId, actorName, "AddParticipantLive",
        $"Added {createdSessions.Count} participant session(s) to '{rep.Title}' ({rep.Category})", rep.Id, "AssessmentSession");

    await transaction.CommitAsync();
}
catch (Exception ex)
{
    await transaction.RollbackAsync();
    _logger.LogError(ex, "AddParticipantsLive failed for session {SessionId}", sessionId);
    return StatusCode(500, new { error = "Gagal menambahkan peserta. Silakan coba lagi." });
}
```

**Langkah 8 — eager-UPA helper** `CreateEagerAssignmentsAsync` — tambah `private` method baru, cermin StartExam `:1038-1117`. Per sesi baru: hitung siblingSessionIds via `SiblingSessionQuery.SiblingPrePostAwarePredicate`, workerIndex = posisi di sorted sibling set, load packages, bila ada → build assignment:
```csharp
// Phase 410 (A1 EAGER): cermin CMPController.StartExam :1038-1117. Buat UPA untuk tiap sesi baru
// yang batch-nya punya AssessmentPackages. Tanpa packages → skip (StartExam guard if(packages.Any())).
private async Task CreateEagerAssignmentsAsync(List<AssessmentSession> newSessions, AssessmentSession rep)
{
    var rng = Random.Shared;
    foreach (var s in newSessions)
    {
        var siblingSessionIds = await _context.AssessmentSessions
            .Where(SiblingSessionQuery.SiblingPrePostAwarePredicate(s.Title, s.Category, s.Schedule.Date, s.AssessmentType))
            .Select(x => x.Id).ToListAsync();
        int workerIndex = siblingSessionIds.OrderBy(x => x).ToList().IndexOf(s.Id);

        var packages = await _context.AssessmentPackages
            .Include(p => p.Questions).ThenInclude(q => q.Options)
            .Where(p => siblingSessionIds.Contains(p.AssessmentSessionId))
            .OrderBy(p => p.PackageNumber).ToListAsync();
        if (!packages.Any()) continue;   // tanpa paket → UPA lazy nanti (mode non-paket), tak error

        var shuffledIds = ShuffleEngine.BuildQuestionAssignment(packages, s.ShuffleQuestions, workerIndex, rng);
        var assignedQuestions = packages.SelectMany(p => p.Questions).Where(q => shuffledIds.Contains(q.Id));
        var optionShuffleDict = ShuffleEngine.BuildOptionShuffle(assignedQuestions, s.ShuffleOptions, rng);
        var sentinelPackage = packages.First();
        _context.UserPackageAssignments.Add(new UserPackageAssignment
        {
            AssessmentSessionId = s.Id,
            AssessmentPackageId = sentinelPackage.Id,
            UserId = s.UserId,
            ShuffledQuestionIds = System.Text.Json.JsonSerializer.Serialize(shuffledIds),
            ShuffledOptionIdsPerQuestion = System.Text.Json.JsonSerializer.Serialize(optionShuffleDict),
            SavedQuestionCount = shuffledIds.Count
        });
    }
    await _context.SaveChangesAsync();
}
```
> Verifikasi nama field/method ShuffleEngine + AssessmentPackage.PackageNumber via Read bila kompilasi gagal (cermin StartExam :1050-1096 verbatim). EAGER UPA dipanggil DALAM transaksi → atomic dengan sesi (gagal UPA → rollback sesi). JANGAN pakai impersonation guard StartExam (`:1100`) — endpoint admin, selalu persist.

**Langkah 9 — notif post-commit (HANYA setelah commit, Pitfall 4) + JSON response (D-03)**:
```csharp
foreach (var ns in createdSessions)
{
    try { await _notificationService.SendAsync(ns.UserId, "ASMT_ASSIGNED", "Assessment Baru",
        $"Anda telah di-assign assessment \"{ns.Title}\"", $"/CMP/StartExam/{ns.Id}"); }
    catch (Exception ex) { _logger.LogWarning(ex, "Notification send failed for session {Id}", ns.Id); }
}
// added[] = sesi baru (untuk Pre/Post, kedua sesi muncul). skipped[] = nama+NIP (D-03).
var addedJson = createdSessions.Select(s => new {
    id = s.Id,
    fullName = userDictionary.TryGetValue(s.UserId, out var u) ? u.FullName : "",
    nip = userDictionary.TryGetValue(s.UserId, out var u2) ? u2.NIP : null,
    status = s.Status
}).ToList();
var skippedUsers = await _context.Users.Where(u => skippedIds.Contains(u.Id))
    .Select(u => new { fullName = u.FullName, nip = u.NIP }).ToListAsync();
return Json(new {
    added = addedJson, skipped = skippedUsers,
    addedCount = createdSessions.Count, skippedCount = skippedUsers.Count
});
```
> ⚠️ **JANGAN** sentuh `_hubContext` untuk `participantAdded` (D-04 — broadcast = Phase 412). Endpoint cukup return JSON.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `AssessmentAdminController.cs` contains `public async Task<IActionResult> AddParticipantsLive(int sessionId, List<string> userIds)`
    - `AddParticipantsLive` has `[HttpPost]`, `[Authorize(Roles = "Admin, HC")]`, `[ValidateAntiForgeryToken]` (all three present immediately above the method)
    - Contains verbatim string `"Window ujian sudah tutup, tidak bisa tambah peserta."`
    - Contains Proton reject: grep `rep.Category == "Assessment Proton"` present
    - Contains eager UPA helper `private async Task CreateEagerAssignmentsAsync(` AND `ShuffleEngine.BuildQuestionAssignment` called inside it
    - Contains `using var transaction = await _context.Database.BeginTransactionAsync()` AND `await transaction.RollbackAsync()`
    - Contains `await _auditLog.LogAsync(` with actionType string `"AddParticipantLive"`
    - Pre/Post branch: grep `newPre.LinkedSessionId` AND `newPost.LinkedSessionId` present (cross-link)
    - JSON response: grep `addedCount` AND `skippedCount` present
    - NO `_hubContext` reference inside AddParticipantsLive (D-04 — SignalR deferred to 412)
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error
  </acceptance_criteria>
  <done>AddParticipantsLive membuat sesi ready-status + eager UPA atomic, reject Proton/window, idempotent skip (sesi APAPUN), Pre/Post pair cross-linked, audit AddParticipantLive, notif post-commit, return JSON added[]/skipped[]; NO SignalR; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Admin/HC) → AddParticipantsLive (POST) | Input tak-tepercaya: sessionId, userIds[]; crossing ke mutasi DB |
| Browser (Admin/HC) → GetEligibleParticipantsToAdd (GET) | Input tak-tepercaya: sessionId; crossing ke read DB |
| Controller → DB (AssessmentSessions/UserPackageAssignments/AuditLogs) | Write boundary dalam transaksi atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-410-01 | Spoofing/Tampering | CSRF pada POST AddParticipantsLive | mitigate | `[ValidateAntiForgeryToken]` pada AddParticipantsLive (LOCKED spec §H) — Task 2 acceptance |
| T-410-02 | Elevation | Tambah peserta tanpa otoritas | mitigate | `[Authorize(Roles = "Admin, HC")]` pada KEDUA endpoint (mutasi + read picker) — Task 1+2 acceptance |
| T-410-03 | Tampering | Mass-add ke batch arbitrer via batchKey/LinkedGroupId palsu | mitigate | Server-authoritative: terima `sessionId` representatif saja, resolve batch-key (`Title+Category+Schedule.Date`) server-side; JANGAN trust batchKey/LinkedGroupId client (prinsip T-397-06) |
| T-410-04 | Tampering | Korup state Proton via add ke sesi Proton | mitigate | Guard dini `rep.Category == "Assessment Proton"` → 400 sebelum write (Task 2 Langkah 2) |
| T-410-05 | Tampering | Bypass window (tambah peserta setelah ujian tutup) | mitigate | Server-authoritative window guard `DateTime.UtcNow.AddHours(7) > ExamWindowCloseDate` → 400 (Task 2 Langkah 3) |
| T-410-06 | Tampering | Mass-assignment param inbound (over-posting field model) | mitigate | Signature endpoint hanya bind primitif `int sessionId` + `List<string> userIds`; JANGAN bind AssessmentSession/ViewModel; field sesi di-set server-side dari `rep` (BuildReadyParticipantSession), bukan dari client |
| T-410-07 | DoS | Mass-add abuse (ribuan userIds sekali POST) | mitigate | Cap `userIds.Count > 50` → 400 (cermin rate-limit BULK ASSIGN `:2032`) — Task 2 Langkah 1 |
| T-410-08 | Information Disclosure | IDOR userIds (assign user arbitrer) | accept | D-02 by-design membolehkan semua pekerja IsActive (admin bebas); tetap validasi `IsActive` + exist (Task 2 Langkah 5). Bukan IDOR by design — keputusan user eksplisit |
| T-410-09 | Tampering | Rep session sudah removed → warisi field stale | accept | Risiko rendah: rep dipakai untuk inherit field config batch; field config tak berubah saat soft-remove. Mitigasi opsional (pilih rep RemovedAt==null) = enhancement, bukan blocker — di-defer |
| T-410-10 | — | XSS pada nama/NIP di JSON response | accept | 410 hanya emit data JSON (tak render DOM); rendering = Phase 412 (gunakan `.textContent`, carry prinsip T-409-10). Bukan threat di 410 |

> RemovalReason TIDAK ada di 410 (itu remove path 411). SignalR broadcast DEFER 412 (D-04) → tak ada threat broadcast di 410.
</threat_model>

<verification>
Setelah kedua task:
1. `dotnet build HcPortal.csproj` → 0 error (gate per-task sudah cek).
2. `dotnet test HcPortal.Tests --filter "Category!=Integration"` → fast suite tetap hijau (tak regresi 569; endpoint baru belum di-test, ditambah Plan 02).
3. `dotnet run` @localhost:5277 → app boot tanpa error (endpoint terdaftar; verifikasi route `/Admin/GetEligibleParticipantsToAdd` + `/Admin/AddParticipantsLive` ada via startup tanpa exception).
4. Cek manual cepat (opsional, executor discretion): POST AddParticipantsLive ke batch lokal → sesi + UPA tercipta di DB lokal (CLAUDE.md Develop Workflow: cek DB lokal). JANGAN sentuh HcPortalDB_Dev untuk test — gunakan batch lokal disposable bila perlu.
</verification>

<success_criteria>
- `GetEligibleParticipantsToAdd` + `AddParticipantsLive` + `BuildReadyParticipantSession` + `CreateEagerAssignmentsAsync` ada di `AssessmentAdminController.cs`, build 0 error.
- AddParticipantsLive: ready-status (NEVER InProgress) + eager UPA + atomic + idempotent (sesi APAPUN) + window-reject + Proton-reject + Pre/Post pair cross-linked.
- Eligible: semua IsActive minus sesi APAPUN di batch, tanpa unit/section filter (D-02), removed TETAP excluded (D-01).
- JSON: added[]/skipped[] dengan nama+NIP + addedCount/skippedCount (D-03).
- NO SignalR (D-04). migration=FALSE (tak ada Migrations/ diff).
- RBAC + antiforgery + window guard + Proton guard + cap-50 semua terpasang (threat model).
</success_criteria>

<output>
After completion, create `.planning/phases/410-add-participant-backend-live/410-01-SUMMARY.md` (gunakan template summary.md). Catat: keputusan A1 EAGER UPA diterapkan; nama field/method ShuffleEngine + AssessmentPackage yang diverifikasi; deviasi (mis. helper signature) bila ada; migration=FALSE; build + fast-suite status.
</output>
