---
phase: 411-remove-restore-backend-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/EditAssessment.cshtml
autonomous: true
requirements: [PRMV-01, PRMV-04, PRMV-05, PLIV-03]
user_setup: []

must_haves:
  truths:
    - "Admin/HC menghapus peserta belum-mulai+tanpa-data → baris + UPA hilang dari DB (hard-delete cascade)"
    - "Admin/HC menghapus peserta sudah-mulai/Completed/bersertifikat → RemovedAt/RemovedBy/RemovalReason di-set; Score/IsPassed/NomorSertifikat/file/response/Status TIDAK berubah (soft-remove, reversibel)"
    - "Menghapus sesi yang sudah RemovedAt!=null → no-op sukses (idempoten)"
    - "Soft-remove tanpa reason → ditolak 400 'Alasan penghapusan wajib diisi.'; hard-delete tanpa reason → sukses"
    - "Menghapus 1 peserta Pre/Post → pasangan (LinkedSessionId) diperlakukan satu unit: salah satu berdata → soft keduanya; keduanya bersih → hard keduanya"
    - "Admin/HC memulihkan sesi soft-removed → RemovedAt/RemovedBy/RemovalReason di-clear (Restore); non-removed → 400"
    - "Sesi Proton (Category=='Assessment Proton') → ditolak 400 di semua endpoint remove/restore/DeletePeserta"
    - "Setiap remove + restore menulis audit row (actorId/actorName/reason) — non-repudiation"
    - "Tombol Hapus Peserta di EditAssessment (form mati) hidup → POST DeleteAssessmentPeserta delegasi ke core bersama (full-page redirect)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "RemoveParticipantCoreAsync (private shared) + SessionHasDataAsync (private) + RemoveParticipantLive (HttpPost JSON) + RestoreParticipantLive (HttpPost JSON) + DeleteAssessmentPeserta (HttpPost redirect)"
      contains: "RemoveParticipantCoreAsync"
    - path: "Views/Admin/EditAssessment.cshtml"
      provides: "form deletePesertaForm tanpa display:none (tombol hapus per-peserta hidup) + wiring sessionId"
      contains: "deletePesertaForm"
  key_links:
    - from: "RemoveParticipantLive"
      to: "RemoveParticipantCoreAsync"
      via: "delegasi wrapper JSON"
      pattern: "RemoveParticipantCoreAsync"
    - from: "DeleteAssessmentPeserta"
      to: "RemoveParticipantCoreAsync"
      via: "delegasi wrapper redirect"
      pattern: "RemoveParticipantCoreAsync"
    - from: "RemoveParticipantCoreAsync (jalur hard)"
      to: "RecordCascadeDeleteService.ExecuteAsync"
      via: "HttpContext.RequestServices.GetRequiredService"
      pattern: "GetRequiredService<RecordCascadeDeleteService>"
    - from: "RemoveParticipantCoreAsync (Pre/Post)"
      to: "AssessmentSession.LinkedSessionId"
      via: "resolve partner per-peserta (BUKAN LinkedGroupId)"
      pattern: "LinkedSessionId"
---

<objective>
Bangun backend penghapusan + pemulihan peserta live (PRMV-01/04/05 + PLIV-03) sebagai orkestrasi murni di `AssessmentAdminController.cs`: satu private core bersama `RemoveParticipantCoreAsync` (hybrid by-state + Pre/Post pair-as-unit + audit), dibungkus tiga endpoint — `RemoveParticipantLive` (JSON, untuk UI 412), `RestoreParticipantLive` (JSON), dan `DeleteAssessmentPeserta` (redirect, menghidupkan tombol mati `EditAssessment.cshtml:666`). Semua RBAC `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`, tolak Proton, idempoten.

Purpose: DELETE 1-peserta belum ada backend (stub mati grep=0); peserta bersertifikat butuh soft-remove agar cert utuh & reversibel via Restore. Inilah jantung milestone v32.5 sisi hapus.
Output: 2 private helper + 3 endpoint baru di `AssessmentAdminController.cs` + 1 form un-hide di `EditAssessment.cshtml`. migration=FALSE. TANPA SignalR (D-03 defer 412).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/411-remove-restore-backend-live/411-CONTEXT.md
@.planning/phases/411-remove-restore-backend-live/411-RESEARCH.md
@.planning/phases/411-remove-restore-backend-live/411-PATTERNS.md
@docs/superpowers/specs/2026-06-19-flexible-add-remove-participant-design.md

<interfaces>
<!-- Kontrak yang dipakai executor — sudah Read-verified terhadap HEAD 46b88d72. JANGAN eksplorasi ulang. -->

Controller ctor (12 args) — Controllers/AssessmentAdminController.cs:33:
  AssessmentAdminController(ApplicationDbContext ctx, UserManager<ApplicationUser> userManager,
    AuditLogService auditLog, IWebHostEnvironment env, IMemoryCache cache, ILogger<...> logger,
    INotificationService notificationService, IHubContext<...> hubContext, WorkerDataService workerDataService,
    GradingService gradingService, ProtonCompletionService protonCompletionService, ProtonBypassService protonBypassService)
  Field tersedia: _context, _userManager, _auditLog, _logger. (NB: _env, _hubContext ADA tapi JANGAN dipakai di 411 — D-03.)

Cascade hard-delete — Services/RecordCascadeDeleteService.cs:175 (resolve via HttpContext.RequestServices, BUKAN ctor):
  Task<CascadeResult> ExecuteAsync(string rootType, int rootId, IEnumerable<int> mirrorTrainingIdsToInclude, string actorId, string actorName)
  CascadeResult(bool Success, int DeletedCount, List<int> DeletedSessionIds, List<int> DeletedTrainingIds, string? ErrorMessage)
  VERIFIED :221-222 sudah RemoveRange UserPackageAssignments milik sesi → D-01 satisfied, JANGAN perluas service.
  Pakai: rootType="session", mirror = Enumerable.Empty<int>(). 1-tx atomik.

Audit — Services/AuditLogService.cs:21 (SaveChanges internal):
  Task LogAsync(string actorUserId, string actorName, string actionType, string description, int? targetId = null, string? targetType = null)

Pre/Post detection — Controllers/AdminBaseController.cs:248 (public static, panggil langsung):
  public static bool IsPrePostSession(AssessmentSession session) => session.AssessmentType=="PreTest" || =="PostTest"

Kolom removal (409) — Models/AssessmentSession.cs:99/101/103:
  DateTime? RemovedAt   // :99  null=aktif; non-null=removed (SUMBER KEBENARAN)
  string?   RemovedBy   // :101 userId Admin/HC
  string?   RemovalReason // :103 max 500 (Fluent), WAJIB pada soft (D-02)
  Juga: LinkedSessionId (:186 int?, cross-link per-peserta), LinkedGroupId (:180 int?, batch lintas-user — JANGAN dipakai pair), Category, StartedAt, Score, IsPassed, NomorSertifikat, ManualSertifikatUrl, Status, Title.

Actor resolution (verbatim 3 baris) — AssessmentAdminController.cs:2395-2397:
  var hcUser = await _userManager.GetUserAsync(User);
  var actorId = hcUser?.Id ?? "";
  var actorName = string.IsNullOrWhiteSpace(hcUser?.NIP) ? (hcUser?.FullName ?? "Unknown") : $"{hcUser.NIP} - {hcUser.FullName}";

Has-data count idiom — PackageUserResponse.AssessmentSessionId (PackageUserResponse.cs:11):
  await _context.PackageUserResponses.AnyAsync(r => r.AssessmentSessionId == s.Id)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RemoveParticipantCoreAsync + SessionHasDataAsync (private shared core)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - 411-PATTERNS.md §"RemoveParticipantCoreAsync (private shared logic) — JANTUNG fase" (excerpt verbatim has-data + partner + reason-gate + soft-write + cascade + audit)
    - 411-RESEARCH.md Pattern 1-6 + Pitfall 1 (LinkedSessionId bukan LinkedGroupId) + Pitfall 2 (jangan sentuh Status) + Pitfall 5 (reason-gate setelah evaluasi jalur)
    - Controllers/AssessmentAdminController.cs:2348-2542 (AddParticipantsLive — analog atrs/actor/Proton + lokasi sisip setelah CreateEagerAssignmentsAsync)
    - Services/RecordCascadeDeleteService.cs:175-260 (ExecuteAsync signature + cascade UPA :221-222)
  </read_first>
  <action>
    Sisipkan DUA private method di `Controllers/AssessmentAdminController.cs` **tepat setelah `CreateEagerAssignmentsAsync` (berakhir ~:2542) dan SEBELUM blok `// --- DELETE ASSESSMENT ---`** — mengelompokkan semua "participant live" berdekatan. JANGAN ubah method existing.

    **(a) Outcome struct (private nested, di atas core):** representasi hasil core.
    ```csharp
    // Hasil RemoveParticipantCoreAsync — dikonsumsi wrapper JSON + redirect.
    private readonly struct RemoveOutcome
    {
        public bool Ok { get; init; }
        public string Mode { get; init; }        // "hard" | "soft" | "noop"
        public string Message { get; init; }     // pesan user-facing (sukses/error)
        public int? PartnerId { get; init; }     // LinkedSessionId pasangan (null jika bukan Pre/Post)
        public static RemoveOutcome Fail(string msg) => new() { Ok = false, Mode = "", Message = msg, PartnerId = null };
        public static RemoveOutcome Success(string mode, string msg, int? partnerId) => new() { Ok = true, Mode = mode, Message = msg, PartnerId = partnerId };
    }
    ```

    **(b) `SessionHasDataAsync` (private)** — has-data detection D-01 (UPA SENGAJA tidak dihitung):
    ```csharp
    private async Task<bool> SessionHasDataAsync(AssessmentSession s)
    {
        if (s.StartedAt != null) return true;                       // sudah mulai = berdata
        return await _context.PackageUserResponses
            .AnyAsync(r => r.AssessmentSessionId == s.Id);          // ada jawaban = berdata
        // D-01: UserPackageAssignment (eager 410) SENGAJA TIDAK dihitung — bukan jejak pengerjaan.
    }
    ```

    **(c) `RemoveParticipantCoreAsync(AssessmentSession session, string? reason, string actorId, string actorName)` → `Task<RemoveOutcome>`** — orkestrasi hybrid. Asumsi caller SUDAH load `session`, SUDAH cek Proton + idempotency (wrapper tanggung jawab). Urutan WAJIB (Pitfall 5):
    1. **Resolve partner Pre/Post via LinkedSessionId** (Pitfall 1 — BUKAN LinkedGroupId, BUKAN DeletePrePostGroup):
       ```csharp
       AssessmentSession? partner = null;
       if (IsPrePostSession(session) && session.LinkedSessionId.HasValue)
           partner = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == session.LinkedSessionId.Value);
       ```
    2. **Evaluasi gabungan has-data:** `bool anyHasData = await SessionHasDataAsync(session) || (partner != null && await SessionHasDataAsync(partner));`
    3. **Reason-gate (D-02) — SETELAH evaluasi, SEBELUM eksekusi:** `if (anyHasData && string.IsNullOrWhiteSpace(reason)) return RemoveOutcome.Fail("Alasan penghapusan wajib diisi.");`
    4. **Jalur SOFT (anyHasData == true):** set 3 kolom untuk session + partner (loop `new[] { session, partner }.Where(x => x != null)`): `RemovedAt = DateTime.UtcNow; RemovedBy = actorId; RemovalReason = reason;`. JANGAN sentuh Score/IsPassed/NomorSertifikat/ManualSertifikatUrl/Status/response (Pitfall 2). `await _context.SaveChangesAsync();`. Audit (lihat 6). Return `RemoveOutcome.Success("soft", "Peserta dikeluarkan (data diarsip, dapat dipulihkan).", partner?.Id)`.
    5. **Jalur HARD (anyHasData == false):** resolve cascade via `var cascade = HttpContext.RequestServices.GetRequiredService<RecordCascadeDeleteService>();` lalu loop session + partner: `var result = await cascade.ExecuteAsync("session", s.Id, Enumerable.Empty<int>(), actorId, actorName); if (!result.Success) return RemoveOutcome.Fail(result.ErrorMessage ?? "Gagal menghapus peserta.");`. Setelah loop sukses, audit (lihat 6). Return `RemoveOutcome.Success("hard", "Peserta dihapus.", partner?.Id)`.
       - Capture `partner?.Id` ke variabel lokal SEBELUM cascade (cascade hapus baris → navigasi partner bisa stale).
    6. **Audit double-log** (cascade tulis "CascadeDelete" internal; tambah konteks endpoint user-facing) — capture title sebelum hard-delete:
       ```csharp
       await _auditLog.LogAsync(actorId, actorName, "RemoveParticipantLive",
           $"Removed participant session [ID={sessionId}] '{title}' mode={mode} reason='{reason}'",
           sessionId, "AssessmentSession");
       ```
    **Keputusan #5:** JANGAN panggil `EnsureCanDeleteAsync` di core (soft-remove justru DIPILIH untuk Completed/cert sehingga HC boleh; jalur hard hanya not-started → guard no-op natural). Mitigasi = soft + audit wajib.
    **D-03:** JANGAN sentuh `_hubContext` di mana pun (broadcast = 412).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private async Task<RemoveOutcome> RemoveParticipantCoreAsync" Controllers/AssessmentAdminController.cs` → 1 match
    - `grep -n "private async Task<bool> SessionHasDataAsync" Controllers/AssessmentAdminController.cs` → 1 match
    - `grep -n "GetRequiredService<RecordCascadeDeleteService>" Controllers/AssessmentAdminController.cs` → ≥1 match (jalur hard pakai RequestServices)
    - `grep -n "session.LinkedSessionId.HasValue" Controllers/AssessmentAdminController.cs` → ≥1 match (partner via LinkedSessionId)
    - `grep -c "LinkedGroupId" ` pada method core = 0 (JANGAN pakai LinkedGroupId untuk pair — Pitfall 1)
    - `grep -n "Alasan penghapusan wajib diisi." Controllers/AssessmentAdminController.cs` → 1 match (reason-gate D-02)
    - `grep -n "_hubContext" ` di method core/SessionHasData = 0 (D-03 no SignalR)
    - `dotnet build HcPortal.csproj` → Build succeeded, 0 error
  </acceptance_criteria>
  <done>RemoveParticipantCoreAsync + SessionHasDataAsync + RemoveOutcome ada; build 0 error; hybrid (hard via cascade RequestServices, soft set-3-kolom), reason-gate setelah evaluasi, partner via LinkedSessionId, audit, no SignalR, no EnsureCanDeleteAsync call.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 3 endpoint wrapper (RemoveParticipantLive JSON + RestoreParticipantLive JSON + DeleteAssessmentPeserta redirect) + un-hide form view</name>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/EditAssessment.cshtml</files>
  <read_first>
    - 411-PATTERNS.md §"RemoveParticipantLive" + §"RestoreParticipantLive" + §"DeleteAssessmentPeserta (redirect variant)" (excerpt verbatim attrs/load/Proton/idempotency/JSON shape)
    - Controllers/AssessmentAdminController.cs:2353-2369 (AddParticipantsLive — attrs + load + Proton-reject verbatim)
    - Views/Admin/EditAssessment.cshtml:665-670 (dead form: sessionId + returnToId, TANPA reason field)
    - Task 1 di plan ini (core + RemoveOutcome contract yang dibungkus wrapper)
  </read_first>
  <action>
    Sisipkan TIGA endpoint di `Controllers/AssessmentAdminController.cs` tepat sebelum `RemoveParticipantCoreAsync` (urutan: Add existing → RemoveParticipantLive → RestoreParticipantLive → DeleteAssessmentPeserta → core privat). SEMUA `[HttpPost][Authorize(Roles = "Admin, HC")][ValidateAntiForgeryToken]` (PLIV-03).

    **(a) `RemoveParticipantLive(int sessionId, string? reason)` → JSON wrapper:**
    1. Load: `var session = await _context.AssessmentSessions.FirstOrDefaultAsync(s => s.Id == sessionId); if (session == null) return NotFound(new { error = "Sesi tidak ditemukan." });`
    2. Proton reject: `if (session.Category == "Assessment Proton") return BadRequest(new { error = "Penghapusan peserta tidak didukung untuk Assessment Proton." });`
    3. Idempotency (paling awal sesudah Proton, Pitfall 3): `if (session.RemovedAt != null) return Json(new { sessionId, mode = "noop" });`
    4. Resolve actor (verbatim 3 baris :2395-2397).
    5. `var outcome = await RemoveParticipantCoreAsync(session, reason, actorId, actorName);`
    6. `if (!outcome.Ok) return BadRequest(new { error = outcome.Message });` (reason-wajib-soft 400 muncul di sini)
    7. `return Json(new { sessionId, mode = outcome.Mode, linkedSessionId = outcome.PartnerId });`
    8. D-03 comment: `// JANGAN broadcast participantRemoved — Phase 412.`

    **(b) `RestoreParticipantLive(int sessionId)` → JSON wrapper** (PRMV-04, simetri Pre/Post A2):
    1. Load + 404 (pesan "Sesi tidak ditemukan.").
    2. Guard: `if (session.RemovedAt == null) return BadRequest(new { error = "Sesi ini tidak dalam keadaan dihapus." });`
    3. Resolve actor (verbatim).
    4. Pre/Post simetri: resolve partner via LinkedSessionId; loop `new[] { session, partner }.Where(x => x != null && x.RemovedAt != null)`: `RemovedAt = null; RemovedBy = null; RemovalReason = null;`. `await _context.SaveChangesAsync();`
    5. Audit: `await _auditLog.LogAsync(actorId, actorName, "RestoreParticipantLive", $"Restored participant session [ID={sessionId}] '{session.Title}'", sessionId, "AssessmentSession");`
    6. `return Json(new { sessionId, restored = true });`

    **(c) `DeleteAssessmentPeserta(int sessionId, int returnToId)` → redirect wrapper** (D-04 fix stub):
    1. Load → null: `TempData["Error"] = "Sesi tidak ditemukan."; return RedirectToAction("EditAssessment", new { id = returnToId });`
    2. Proton → `TempData["Error"] = "Tidak didukung untuk Assessment Proton."; return RedirectToAction("EditAssessment", new { id = returnToId });`
    3. Idempotency: `if (session.RemovedAt != null) { TempData["Success"] = "Peserta sudah dikeluarkan."; return RedirectToAction("EditAssessment", new { id = returnToId }); }`
    4. Resolve actor (verbatim).
    5. `var outcome = await RemoveParticipantCoreAsync(session, reason: null, actorId, actorName);` — Open Q1 resolusi: form lama TANPA field reason → varian ini efektif jalur HARD (not-started, reason opsional). Bila core kembalikan `outcome.Ok == false` (peserta berdata → butuh reason), `TempData["Error"]` arahkan ke Monitoring Detail: pesan core "Alasan penghapusan wajib diisi." → tambahkan suffix " Gunakan kontrol Hapus di Monitoring Detail (butuh alasan)." sebelum di-assign ke TempData.
    6. `TempData[outcome.Ok ? "Success" : "Error"] = ...; return RedirectToAction("EditAssessment", new { id = returnToId });`

    **(d) View un-hide** `Views/Admin/EditAssessment.cshtml:666`: hapus `style="display:none;"` dari `<form id="deletePesertaForm" ...>` (form jadi terdaftar; tombol per-peserta yang men-set `#deletePesertaSessionId` + submit form sudah ada di JS existing — JANGAN tambah handler baru, hanya un-hide). Form `method="post"` + `asp-action="DeleteAssessmentPeserta"` + antiforgery + hidden `sessionId`/`returnToId` sudah benar (Read-verified).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public async Task<IActionResult> RemoveParticipantLive(int sessionId, string? reason)" Controllers/AssessmentAdminController.cs` → 1 match
    - `grep -n "public async Task<IActionResult> RestoreParticipantLive(int sessionId)" Controllers/AssessmentAdminController.cs` → 1 match
    - `grep -n "public async Task<IActionResult> DeleteAssessmentPeserta(int sessionId, int returnToId)" Controllers/AssessmentAdminController.cs` → 1 match
    - `grep -B3 "RemoveParticipantLive(int sessionId" Controllers/AssessmentAdminController.cs` menampilkan `[Authorize(Roles = "Admin, HC")]` + `[ValidateAntiForgeryToken]` (sama untuk Restore + DeletePeserta — 3 endpoint × 3 attribut)
    - `grep -c "ValidateAntiForgeryToken" Controllers/AssessmentAdminController.cs` bertambah 3 vs baseline
    - `grep -n "mode = \"noop\"" Controllers/AssessmentAdminController.cs` → 1 match (idempotency RemoveParticipantLive)
    - `grep -n "Sesi ini tidak dalam keadaan dihapus." Controllers/AssessmentAdminController.cs` → 1 match (restore guard)
    - `grep -n "RestoreParticipantLive" Controllers/AssessmentAdminController.cs` mencakup audit actionType string "RestoreParticipantLive"
    - `grep -c "display:none" Views/Admin/EditAssessment.cshtml` pada baris form deletePesertaForm = 0 (un-hidden)
    - `dotnet build HcPortal.csproj` → Build succeeded, 0 error
  </acceptance_criteria>
  <done>3 endpoint (Remove JSON + Restore JSON + DeletePeserta redirect) ada dengan RBAC+antiforgery; idempotency + Proton-reject + actor + delegasi core; form view un-hidden; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser Admin/HC) → endpoint POST | Input `sessionId`/`reason`/`returnToId` lintas batas; antiforgery + auth wajib |
| controller → RecordCascadeDeleteService | Server-internal; hard-delete atomik 1-tx |
| controller → DB (EF Core) | Mutasi kolom removal / cascade delete; server-authoritative state decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-411-01 | Spoofing/Tampering (CSRF) | RemoveParticipantLive/RestoreParticipantLive/DeleteAssessmentPeserta POST | mitigate | `[ValidateAntiForgeryToken]` ketiga endpoint + form antiforgery token (EditAssessment:667). Verified attr present di acceptance |
| T-411-02 | Elevation (akses non-Admin/HC) | semua 3 endpoint | mitigate | `[Authorize(Roles = "Admin, HC")]` ketiga endpoint (mirror AddParticipantsLive:2354) |
| T-411-03 | Tampering (IDOR — hapus sesi peserta lain via id tebakan) | sessionId param | accept | Admin/HC memang berwenang lintas-peserta (by-design RBAC). sessionId di-load + validasi exist (404). Partner Pre/Post di-resolve SERVER-side via LinkedSessionId (tak terima id partner dari client) |
| T-411-04 | Tampering (korup ProtonTrack) | sesi Proton | mitigate | Reject `Category=="Assessment Proton"` → 400 sebelum write (ketiga endpoint, spec §F) |
| T-411-05 | Tampering (client memaksa hard vs soft) | hybrid decision | mitigate | Keputusan hard/soft 100% server-side dari kolom DB (`StartedAt` + `PackageUserResponse` count); client tak punya input ke jalur |
| T-411-06 | Repudiation (siapa hapus/restore apa) | audit | mitigate | `AuditLogService.LogAsync` setiap Remove ("RemoveParticipantLive" + mode + reason) + Restore ("RestoreParticipantLive") — actorId/actorName/timestamp. PLIV-03 non-repudiation |
| T-411-07 | Tampering (soft-remove bocor mutasi cert/score) | soft-remove write | mitigate | Soft HANYA set 3 kolom removal; Score/IsPassed/NomorSertifikat/ManualSertifikatUrl/Status/response UNCHANGED (Pitfall 2). Cert utuh & reversibel (keputusan #5) |
| T-411-08 | Elevation (HC hapus cert tanpa jejak) | EnsureCanDeleteAsync longgar | mitigate | Keputusan #5: soft-remove (cert utuh) + audit WAJIB `RemovedBy`/`RemovalReason` (D-02 reason-required-on-soft → 400 jika kosong) — mitigasi bukan block |
| T-411-09 | Mass-assignment / over-post | model-bind | mitigate | Endpoint bind hanya scalar primitif (`int sessionId`, `string? reason`, `int returnToId`) — bukan entity bind; tak ada surface over-post |
| T-411-10 | Information Disclosure (error leak) | cascade fail | mitigate | Pesan generik "Gagal menghapus peserta." ke client; detail hanya ke `_logger` (cascade internal RecordCascadeDeleteService:289) |
| T-411-11 | DoS (mass-delete) | endpoint | accept | Single-session per call (sessionId tunggal), bukan batch → risiko rendah; tak perlu cap |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error (kedua task).
- `grep` acceptance criteria semua match (5 signature/attr + view un-hide).
- `git status Migrations/ Data/` kosong → migration=FALSE (tak ada schema baru — set/clear kolom existing 409 + cascade existing).
- `dotnet run --urls http://localhost:5277` boot bersih; 3 route baru terdaftar (HTTP 302 auth-challenge unauthenticated, BUKAN 404). Verifikasi: `curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:5277/Admin/RemoveParticipantLive` → 400/302/415 (BUKAN 404).
- Test write-path lengkap di Plan 02 (411-02) — plan ini = implementasi endpoint, verifikasi via build + route-registration + no-regression fast-suite.
- `dotnet test` fast-suite tetap hijau (tak regresi 409 guard + 410 add) — endpoint baru tak ubah existing.
</verification>

<success_criteria>
1. `RemoveParticipantCoreAsync` + `SessionHasDataAsync` private ada; hybrid hard (cascade)/soft (3 kolom) + Pre/Post pair via LinkedSessionId + reason-gate D-02 + audit; no SignalR; no EnsureCanDeleteAsync call. *(PRMV-01, PRMV-05)*
2. `RemoveParticipantLive` (JSON) + `RestoreParticipantLive` (JSON) + `DeleteAssessmentPeserta` (redirect) ada, ketiga RBAC `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`; Proton-reject + idempotency. *(PRMV-01, PRMV-04, PLIV-03)*
3. Form `deletePesertaForm` un-hidden (tombol hapus per-peserta hidup, delegasi core). *(PRMV-01 / D-04)*
4. `dotnet build` 0 error; 3 route terdaftar (302/400, bukan 404); migration=FALSE; fast-suite no-regression.
</success_criteria>

<output>
After completion, create `.planning/phases/411-remove-restore-backend-live/411-01-SUMMARY.md`
</output>
