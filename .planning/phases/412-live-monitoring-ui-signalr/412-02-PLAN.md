---
phase: 412-live-monitoring-ui-signalr
plan: 02
type: execute
wave: 2
depends_on: [412-01]
files_modified:
  - Views/Admin/AssessmentMonitoringDetail.cshtml
autonomous: true
requirements: [PART-05, PRMV-02, PLIV-01, PLIV-02]
user_setup: []

must_haves:
  truths:
    - "Admin/HC dapat membuka picker 'Tambah Peserta' dari card-header dan memilih ≥1 pekerja eligible → POST AddParticipantsLive"
    - "Baris peserta baru muncul live di tabel via handler SignalR participantAdded (tanpa reload), dedup by sessionId"
    - "Tombol 'Hapus Peserta' per-baris membuka modal KERAS untuk InProgress/Completed-cert dan modal RINGAN untuk lainnya"
    - "Handler participantRemoved memindahkan baris soft ke panel 'Peserta Dikeluarkan' (mode soft) atau menghapus baris (mode hard)"
    - "Panel 'Peserta Dikeluarkan' menampilkan soft-removed (nama/waktu/oleh/alasan) dengan tombol Restore 1-klik → RestoreParticipantLive"
    - "updateSummaryFromDOM mengecualikan baris di #tbodyRemoved (count aktif benar)"
    - "RemovalReason di-render via Html.Encode (Razor) / textContent (JS) — bukan innerHTML"
  artifacts:
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Tombol+modal tambah, modal hapus keras/ringan, panel removed, handler participantAdded/Removed, buildActionsHtml diperluas, updateSummaryFromDOM exclude removed"
      contains: "participantRemoved"
  key_links:
    - from: "#btnKonfirmasiTambah"
      to: "/Admin/AddParticipantsLive"
      via: "fetch POST userIds[] + antiforgery"
      pattern: "AddParticipantsLive"
    - from: "btnHapusHardKonfirmasi/btnHapusLightKonfirmasi"
      to: "/Admin/RemoveParticipantLive"
      via: "fetch POST sessionId+reason+antiforgery"
      pattern: "RemoveParticipantLive"
    - from: ".btn-restore-peserta"
      to: "/Admin/RestoreParticipantLive"
      via: "fetch POST sessionId+antiforgery"
      pattern: "RestoreParticipantLive"
    - from: "assessmentHub.on participantRemoved"
      to: "#tbodyRemoved"
      via: "mode soft → prepend baris, mode hard → tr.remove"
      pattern: "tbodyRemoved"
    - from: "updateSummaryFromDOM"
      to: "active rows only"
      via: "selector exclude #tbodyRemoved"
      pattern: "tbody:not\\(#tbodyRemoved\\)"
---

<objective>
Bangun seluruh kontrol UI live di `AssessmentMonitoringDetail.cshtml`: tombol+modal picker "Tambah Peserta", item "Hapus Peserta" per-baris + modal konfirmasi keras/ringan (D-01/D-03), panel collapsible "Peserta Dikeluarkan" + Restore (D-04), dan handler SignalR `participantAdded`/`participantRemoved` yang sinkron live tanpa reload. Mengonsumsi event + ViewBag.RemovedSessions dari Plan 01.

Purpose: PART-05 (add live tanpa reload), PRMV-02 sisi-UI (modal keras), PLIV-01 sisi-UI (panel + exclude count), PLIV-02 sisi-UI (handler DOM).

Output: `AssessmentMonitoringDetail.cshtml` (markup modal/panel + handler JS + helper diperluas). migration=FALSE. Patuh design contract 412-UI-SPEC.md verbatim (markup + copywriting + warna). Tidak menyentuh controller (Plan 01) atau StartExam (Plan 03).

CATATAN RUNTIME: Razor dinamis + SignalR = grep+build TIDAK cukup (pelajaran Phase 354). Task terakhir = `dotnet run` @5277 + smoke manual minimal (buka modal, render panel). Playwright e2e LENGKAP = Phase 413.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/412-live-monitoring-ui-signalr/412-UI-SPEC.md
@.planning/phases/412-live-monitoring-ui-signalr/412-RESEARCH.md
@.planning/phases/412-live-monitoring-ui-signalr/412-PATTERNS.md
@.planning/phases/412-live-monitoring-ui-signalr/412-CONTEXT.md
@.planning/phases/410-add-participant-backend-live/410-01-SUMMARY.md
@.planning/phases/411-remove-restore-backend-live/411-01-SUMMARY.md

<interfaces>
<!-- Helper + handler + markup existing yang WAJIB di-reuse. VERIFIED file:line. -->

Helper existing di view (reuse verbatim / perluas):
- `getToken()` :811 — baca antiforgery dari `#antiforgeryForm`. Pakai tiap POST AJAX baru.
- `buildActionsHtml(session, isPackageMode)` :832 — render kolom Aksi baris baru. WAJIB DIPERLUAS: tambah item "Hapus Peserta" (dropdown) + data-status/data-has-cert.
- `flashRow(tr, cssClass)` :1249 — animasi flash setelah inject.
- `updateSummaryFromDOM()` :1258 — hitung 7 kartu. Selector existing :1259 = `tbody tr[data-session-id]`. WAJIB DIUBAH → `tbody:not(#tbodyRemoved) tr[data-session-id]` (Pitfall 2).
- `statusBadgeClass(s)` :817 / `statusDisplayLabel(s)` :826 — badge class + label Indonesia.
- `window.showAssessmentToast(msg)` (assessment-hub.js:98) — toast.
- `appUrl(path)` / `basePath` (assessment-hub.js global) — URL sub-path-aware (deploy /KPB-PortalHC). WAJIB untuk semua fetch.
- `window.assessmentBatchKey` :1200/:1242 — batchKey client.

Handler SignalR existing (pola mirror):
- `workerStarted` :1301 / `workerSubmitted` :1318 / `workerAnswerEdited` :1383 — pola cari tr by sessionId, update cell, flashRow, toast, updateSummaryFromDOM.
- Sisipkan handler baru participantAdded/participantRemoved SETELAH workerAnswerEdited (~:1411), dalam @section Scripts.

Markup existing (titik insert):
- card-header section "Status Per-Peserta" :187-216 — `<div class="d-flex gap-2 align-items-center">` (:191) = tempat sisip #btnTambahPeserta.
- baris tabel `<tr data-session-id="@session.Id">` :277; dropdown `<ul class="dropdown-menu dropdown-menu-end">` :322-368 = tempat sisip <li> "Hapus Peserta".
- `#akhiriSemuaModal` (header bg-danger text-white) = pola modal keras.
- `#antiforgeryForm` + `#hTitle`/`#hCategory`/`#hScheduleDate` hidden fields existing.

Endpoint kontrak (dari 410/411 SUMMARY):
- GET `/Admin/GetEligibleParticipantsToAdd?sessionId={representativeId}` → JSON `[{id, fullName, nip}]`.
- POST `/Admin/AddParticipantsLive` (sessionId, userIds[] repeated form-field, antiforgery) → JSON `{added:[{id,fullName,nip,status}], skipped:[...], addedCount, skippedCount}`.
- POST `/Admin/RemoveParticipantLive` (sessionId, reason, antiforgery) → JSON `{sessionId, mode:"hard"|"soft"|"noop", linkedSessionId}`; error 400 `{error}` bila reason wajib & kosong.
- POST `/Admin/RestoreParticipantLive` (sessionId, antiforgery) → JSON `{sessionId, restored:true}`.

SignalR event dari Plan 01 (payload):
- `participantAdded` {sessionId, userId, fullName, nip, status}
- `participantRemoved` {sessionId, mode, fullName, nip, removedAt, removedBy, removalReason}

ViewBag dari Plan 01:
- `ViewBag.RemovedSessions` = List<RemovedParticipantViewModel> {Id, FullName, Nip, RemovedAt, RemovedByName, RemovalReason}.

representativeId untuk picker = `Model.RepresentativeId` (MonitoringGroupViewModel.RepresentativeId).

Status label PITFALL (PATTERNS Pitfall 1): `session.UserStatus` bisa Indonesia. Nilai yg masuk JALUR KERAS hanya `"InProgress"` (English) ATAU (`"Completed"` && has-cert). `"Dibatalkan"`/`"Menunggu Penilaian"`/`"Not started"`/`"Abandoned"`/`"Completed"`-tanpa-cert → JALUR RINGAN. data-status diisi dari `@session.UserStatus`; data-has-cert dari `session.NomorSertifikat` non-kosong.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Markup — tombol+modal Tambah, item Hapus per-baris, 2 modal hapus, panel "Peserta Dikeluarkan"</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - `412-UI-SPEC.md` §1 (tombol Tambah), §2 (modal picker), §3 (item Hapus dropdown), §4 (modal ringan), §5 (modal keras), §6 (panel collapsible + baris removed), §"Copywriting Contract", §"Layout Visual"
    - `Views/Admin/AssessmentMonitoringDetail.cshtml` :186-217 (card-header) + :277-372 (baris+dropdown) + akhir file (lokasi sisip panel sebelum section Essay Grading)
    - `Views/Admin/AssessmentMonitoringDetail.cshtml` cari `#akhiriSemuaModal` (pola modal keras header bg-danger) + token-card/extra-time-card (pola card border-0 shadow-sm)
    - `412-PATTERNS.md` Pitfall 1 (label status), Pitfall 3 (XSS reason), Pitfall 5 (panel selalu render)
  </read_first>
  <action>
    Sisipkan markup statik (server-rendered) per UI-SPEC. Gunakan kelas Bootstrap PERSIS dari UI-SPEC (jangan introduce warna/spacing baru).

    **(a) Tombol "Tambah Peserta"** — sisip di `<div class="d-flex gap-2 align-items-center">` card-header (:191), SEBELUM form Ekspor Hasil. Conditional `@if (Model.Category != "Assessment Proton")`. Markup verbatim UI-SPEC §1 (`btn btn-outline-primary btn-sm`, `id="btnTambahPeserta"`, `data-bs-target="#tambahPesertaModal"`, ikon `bi-person-plus`).

    **(b) Item "Hapus Peserta" per-baris** — sisip `<li>` di `<ul class="dropdown-menu dropdown-menu-end">` (:322-368), setelah item Reset (bawah :343). Conditional `@if (Model.Category != "Assessment Proton")`. Markup verbatim UI-SPEC §3:
    ```html
    <li>
        <button type="button" class="dropdown-item text-danger btn-hapus-peserta"
                data-session-id="@session.Id"
                data-worker-name="@session.UserFullName"
                data-status="@session.UserStatus"
                data-has-cert="@((!string.IsNullOrEmpty(session.NomorSertifikat)).ToString().ToLower())"
                style="min-height:40px;">
            <i class="bi bi-person-x me-1"></i>Hapus Peserta
        </button>
    </li>
    ```

    **(c) Modal picker `#tambahPesertaModal`** (modal-lg, header bg-primary text-white) — markup verbatim UI-SPEC §2 (loading/content/empty/error states + footer + selected count). Sisip di akhir file dekat modal lain.

    **(d) Modal hapus RINGAN `#hapusPesertaLightModal`** — markup verbatim UI-SPEC §4 (header netral, field reason `#hapusLightReason` maxlength=500, btn `#btnHapusLightKonfirmasi`).

    **(e) Modal hapus KERAS `#hapusPesertaHardModal`** — markup verbatim UI-SPEC §5 (`data-bs-backdrop="static" data-bs-keyboard="false"`, header bg-danger, 2 blok warning `#hapusHardWarnInProgress`/`#hapusHardWarnCompleted`, field reason `#hapusHardReason` + `#hapusHardReasonError`, btn `#btnHapusHardKonfirmasi`).

    **(f) Panel "Peserta Dikeluarkan" `#panelPesertaDikeluarkan`** — markup verbatim UI-SPEC §6. **Pitfall 5: SELALU render** untuk batch non-Proton (`@if (Model.Category != "Assessment Proton")`), collapsed default. Sisip di bawah card "Status Per-Peserta", di atas section Essay Grading. Render baris removed server-side dari `ViewBag.RemovedSessions`:
    ```razor
    @{ var removedList = ViewBag.RemovedSessions as List<HcPortal.Models.RemovedParticipantViewModel> ?? new List<HcPortal.Models.RemovedParticipantViewModel>(); }
    ```
    (verifikasi namespace ViewModel saat baca Models/AssessmentMonitoringViewModel.cs — sesuaikan using).
    `#countRemoved` = `removedList.Count`. Tiap baris: `<tr data-session-id="@r.Id" data-removed="true">` dengan kolom Nama+NIP, Waktu (`@r.RemovedAt?.ToString("dd MMM yyyy HH:mm") WIB`), Oleh (`@r.RemovedByName`), **Alasan via `@(string.IsNullOrEmpty(r.RemovalReason) ? "—" : r.RemovalReason)`** (Razor auto-encode = XSS-safe, Pitfall 3 — JANGAN `Html.Raw`), tombol Restore `btn-outline-success btn-sm btn-restore-peserta` `data-session-id="@r.Id"` `min-height:40px`. Panel visible-state: bila `removedList.Count == 0` panel tetap di DOM tapi boleh `style="display:none;"` pada wrapper card (JS akan tampilkan saat removed pertama).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "btnTambahPeserta" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "tambahPesertaModal" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 2
    - `grep -c "btn-hapus-peserta" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "hapusPesertaHardModal" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1 && `grep -c "hapusPesertaLightModal" ...` ≥ 1
    - `grep -c 'data-bs-backdrop="static"' Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1 (modal keras non-dismissable)
    - `grep -c "panelPesertaDikeluarkan" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1 && `grep -c "tbodyRemoved" ...` ≥ 1
    - `grep -c "data-has-cert" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep "RemovalReason" Views/Admin/AssessmentMonitoringDetail.cshtml` TIDAK ber-`Html.Raw` (XSS-safe)
    - `grep -c "Assessment Proton" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 2 (guard Proton tombol+panel)
    - `dotnet build` 0 error (Razor compile OK)
    - Verbatim banner: `grep "Anda telah dikeluarkan"` TIDAK wajib di file ini (itu StartExam/Plan 03); modal warning text sesuai UI-SPEC §5
  </acceptance_criteria>
  <done>Build 0 error; markup tombol+modal+panel ter-render server-side; data-status/data-has-cert pada item hapus; panel selalu render non-Proton; reason via Razor auto-encode.</done>
</task>

<task type="auto">
  <name>Task 2: JS — picker fetch+POST, modal hapus delegated, restore 1-klik, perluas buildActionsHtml + updateSummaryFromDOM</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - `412-UI-SPEC.md` §2 (perilaku JS picker), §"Perilaku JS shared kedua modal hapus" (§4/§5 bawah), §7 (restore), §"Summary Count Kontrak", §"Pola Antiforgery", §"SignalR Contract" (dedup)
    - `Views/Admin/AssessmentMonitoringDetail.cshtml` :811 (getToken), :832-887 (buildActionsHtml — status-gate Cancelled/InProgress/Completed), :1258-1281 (updateSummaryFromDOM), :1418-1452 (addExtraTime AJAX pattern)
    - `412-PATTERNS.md` Pitfall 1 (tingkat modal label), Pitfall 2 (summary selector), Pitfall 4 (dedup)
  </read_first>
  <action>
    Tambah JS di `@section Scripts` (dekat helper existing). Semua fetch pakai `appUrl()` + `getToken()`.

    **(a) Perluas `buildActionsHtml(session, isPackageMode)` (:832)** — tambah item dropdown "Hapus Peserta" (`btn-hapus-peserta` + data-session-id/worker-name/status/has-cert) sehingga baris yang di-inject SignalR juga punya tombol hapus. Gate Proton: hanya tambah bila bukan Proton (cek `window.assessmentBatchKey` mengandung "Assessment Proton" ATAU pass flag dari hidden field `#hCategory`). data-status dari `session.status`/`session.UserStatus` payload, data-has-cert dari `session.nomorSertifikat` non-kosong (untuk baris baru = "false").

    **(b) Ubah `updateSummaryFromDOM()` (:1259)** — ganti selector `tbody tr[data-session-id]` → `tbody:not(#tbodyRemoved) tr[data-session-id]` (Pitfall 2). count-total = peserta aktif saja.

    **(c) Picker modal JS** — pada `show.bs.modal` `#tambahPesertaModal`: fetch GET `appUrl('/Admin/GetEligibleParticipantsToAdd?sessionId=' + REP_ID)` (REP_ID = `@Model.RepresentativeId` di-render ke var JS). Render checklist (UI-SPEC item template, `tambah-peserta-check`). Empty → `#tambahPesertaEmpty`. On checkbox change → update count + toggle disabled `#btnKonfirmasiTambah`. On `#btnKonfirmasiTambah` click → POST `appUrl('/Admin/AddParticipantsLive')` body `sessionId=REP_ID&userIds=a&userIds=b&__RequestVerificationToken=...` (form-urlencoded; userIds repeated). On success: tutup modal + `showAssessmentToast`. **Sumber kebenaran baris = SignalR participantAdded** (JANGAN inject dari response POST); fallback inject dari `added[]` bila echo tak datang 3 detik (dedup by sessionId). On error: tampilkan pesan dalam modal.

    **(d) Modal hapus delegated** — `document.addEventListener('click', ...)` pada `.btn-hapus-peserta`: baca data-session-id/worker-name/status/has-cert; tentukan tingkat (Pitfall 1): `status === "InProgress"` ATAU (`status === "Completed"` && hasCert==="true") → HARD; lainnya → LIGHT. Simpan sessionId di closure var. Populasi nama; bila HARD tampilkan blok warning sesuai (InProgress vs Completed); reset field reason; `new bootstrap.Modal(...).show()`. Handler `#btnHapusHardKonfirmasi`/`#btnHapusLightKonfirmasi`: POST `appUrl('/Admin/RemoveParticipantLive')` body `sessionId=&reason=&__RequestVerificationToken=`; loading state. On 200 → tutup modal (perubahan baris via SignalR participantRemoved, fallback 3s DOM move). On 400 (reason wajib) → tampilkan error dalam modal (jangan tutup) — server penjaga akhir D-03.

    **(e) Restore 1-klik** — delegated `.btn-restore-peserta` (D-04 tanpa konfirmasi): disable+spinner; POST `appUrl('/Admin/RestoreParticipantLive')` body `sessionId=&__RequestVerificationToken=`. On success: baris balik via SignalR participantAdded (fallback 3s pindah dari #tbodyRemoved ke tbody aktif); update `#countRemoved`; `showAssessmentToast(name + ' dipulihkan ke batch aktif')`. On error: re-enable + `alert('Gagal memulihkan peserta. Silakan coba lagi.')`.

    **(f) Chevron rotate panel** — pada `shown.bs.collapse`/`hidden.bs.collapse` `#panelPesertaDikeluarkanBody` → toggle `transform: rotate(180deg)` `#panelRemovedChevron`.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GetEligibleParticipantsToAdd" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "AddParticipantsLive" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "RemoveParticipantLive" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "RestoreParticipantLive" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "tbody:not(#tbodyRemoved)" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1 (Pitfall 2)
    - `grep -c "userIds=" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1 (repeated form-field)
    - `grep -c "appUrl(" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 4 (sub-path semua fetch baru)
    - `grep -E "InProgress|Completed" Views/Admin/AssessmentMonitoringDetail.cshtml` menunjukkan logic tingkat-modal (cek has-cert)
    - `grep -c "Gagal memulihkan peserta" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1 (copy restore error)
    - `dotnet build` 0 error
  </acceptance_criteria>
  <done>Build 0 error; picker fetch+POST jalan; modal hapus tentukan tingkat dari data-status (label-aware); restore 1-klik; updateSummaryFromDOM exclude removed; buildActionsHtml inject tombol hapus.</done>
</task>

<task type="auto">
  <name>Task 3: SignalR handlers participantAdded/participantRemoved (dedup, mode-aware, XSS-safe) + runtime smoke @5277</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <read_first>
    - `412-UI-SPEC.md` §8a (participantAdded) + §8b (participantRemoved) + §"SignalR Contract" (dedup rule)
    - `Views/Admin/AssessmentMonitoringDetail.cshtml` :1301-1380 (workerStarted/workerSubmitted pola) + :1383-1411 (workerAnswerEdited — titik sisip handler baru)
    - `412-PATTERNS.md` handler participantAdded/participantRemoved (kode contoh) + Pitfall 3/4/6
    - `412-RESEARCH.md` §"Handler participantRemoved (mode-aware, XSS-safe)"
  </read_first>
  <action>
    Sisipkan 2 handler SETELAH `workerAnswerEdited` (~:1411), dalam @section Scripts.

    **(a) `window.assessmentHub.on('participantAdded', function(data) {...})`** (mirror workerStarted + dedup Pitfall 4):
    1. Dedup: bila `document.querySelector('tbody:not(#tbodyRemoved) tr[data-session-id="' + data.sessionId + '"]')` ada → return (skip).
    2. Kasus restore: bila `<tr data-removed="true">` ada di `#tbodyRemoved` → pindahkan ke tbody aktif (hapus attr data-removed, rebuild kolom aktif via buildActionsHtml), update `#countRemoved`--.
    3. Else inject baris baru ke tbody aktif: kolom Nama (`textContent` = data.fullName, XSS-safe), Progres "—", Status badge via `statusBadgeClass`/`statusDisplayLabel` (map "Open"/"Upcoming"→"Not started"), Nilai "—", Hasil "—", Selesai "—", Aksi via `buildActionsHtml({Id:data.sessionId, UserFullName:data.fullName, UserStatus:'Not started', NomorSertifikat:null}, isPackageMode)`. data-status="Not started", data-has-cert="false".
    4. `flashRow(tr, 'flash-update')`; 5. `updateSummaryFromDOM()`; 6. `showAssessmentToast(data.fullName + ' ditambahkan ke batch')`; 7. update `#last-updated-time`.

    **(b) `window.assessmentHub.on('participantRemoved', function(data) {...})`** (mode-aware Pitfall 6, XSS-safe Pitfall 3):
    1. `var tr = document.querySelector('tbody:not(#tbodyRemoved) tr[data-session-id="' + data.sessionId + '"]'); if (!tr) return;`
    2. `if (data.mode === 'hard') { tr.remove(); }` (hard-delete TIDAK masuk panel — Pitfall 6).
    3. else (soft): bangun `<tr data-session-id data-removed="true">` 5 kolom; **kolom Alasan + Nama + Oleh via `.textContent` (BUKAN innerHTML, Pitfall 3)**: Nama+NIP, Waktu (format data.removedAt), Oleh (data.removedBy), Alasan (data.removalReason || '—'), tombol Restore (`btn-restore-peserta`, data-session-id). `document.getElementById('tbodyRemoved').prepend(newTr)`; `tr.remove()`; tampilkan panel (`#panelPesertaDikeluarkan` wrapper card → `style.display=''`); `#countRemoved`++.
    4. `updateSummaryFromDOM()`; 5. `showAssessmentToast(data.fullName + ' dikeluarkan dari batch')`; 6. update `#last-updated-time`.

    **(c) Runtime smoke @5277** (WAJIB — Razor+SignalR butuh runtime, lesson Phase 354): `dotnet run --urls http://localhost:5277` (AD-off `Authentication__UseActiveDirectory=false`); buka satu halaman Monitoring Detail batch non-Proton (login admin@pertamina.com per reference_dev_credentials); verifikasi: (i) tombol "Tambah Peserta" tampil + klik buka modal picker (fetch eligible jalan, list/empty muncul); (ii) panel "Peserta Dikeluarkan" ter-render (collapsed); (iii) tidak ada JS console error saat load (handler ter-register). Hentikan app. Bila ada batch dengan soft-removed: verifikasi baris panel + tombol Restore tampil. CATATAN: e2e multi-tab broadcast + force-kick = Phase 413; smoke ini cukup buktikan markup+handler ter-load tanpa error.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "on('participantAdded'" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "on('participantRemoved'" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1
    - `grep -c "data.mode === 'hard'" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 1 (mode-aware)
    - `grep -c "textContent" Views/Admin/AssessmentMonitoringDetail.cshtml` ≥ 2 (XSS-safe reason/nama — Pitfall 3)
    - `grep -E "innerHTML.*removalReason|innerHTML.*reason" Views/Admin/AssessmentMonitoringDetail.cshtml` → NOL match (tak ada innerHTML untuk reason)
    - `grep -c "ditambahkan ke batch" ...` ≥ 1 && `grep -c "dikeluarkan dari batch" ...` ≥ 1 (toast copy)
    - `dotnet build` 0 error
    - Runtime smoke @5277: modal picker buka, panel render, NOL JS console error saat load (dikonfirmasi manual dalam SUMMARY)
  </acceptance_criteria>
  <done>Build 0 error; handler participantAdded (dedup+restore-aware) + participantRemoved (mode-aware, textContent XSS-safe) ter-register; runtime smoke @5277 buktikan modal+panel load tanpa error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SignalR payload (server) → DOM (browser) | fullName/removalReason dari server di-inject ke DOM pemantau |
| admin input (reason field) → POST endpoint | reason bebas-teks lintas ke RemoveParticipantLive |
| client checklist (userIds) → AddParticipantsLive | pilihan user → POST (server re-validasi eligible) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-412-08 | Tampering (Stored XSS) | kolom Alasan/Nama di panel (Razor + JS inject) | mitigate | Razor `@`-encode (Task 1f, JANGAN Html.Raw) + JS `.textContent` (Task 3b, JANGAN innerHTML) — carry T-409-10 |
| T-412-09 | Spoofing (CSRF) | POST add/remove/restore dari AJAX | mitigate | `getToken()` kirim `__RequestVerificationToken`; endpoint `[ValidateAntiForgeryToken]` (server, Plan 01/410/411) |
| T-412-10 | Elevation | non-Admin akses kontrol UI | mitigate | Action `[Authorize(Roles="Admin, HC")]` (server); endpoint ber-RBAC; UI hanya rendered utk role berwenang |
| T-412-11 | Tampering | client memilih userIds di luar eligible | mitigate | Server `AddParticipantsLive` re-resolve eligible + skip idempotent (410, server-authoritative) — UI tak dipercaya |
| T-412-12 | Information Disclosure | reason ditampilkan ke admin lain via broadcast | accept | By-design: panel "Peserta Dikeluarkan" memang untuk Admin/HC (audit transparan); reason di-encode |
| T-412-13 | Tampering | double-inject (echo+optimistic) baris dobel | mitigate | Dedup `querySelector(...:not([data-removed]))` sebelum inject (Pitfall 4, Task 3a); echo = sumber kebenaran |
</threat_model>

<verification>
- `dotnet build` 0 error (Razor compile)
- grep konfirmasi: btnTambahPeserta, tambahPesertaModal, btn-hapus-peserta, hapusPesertaHard/LightModal, panelPesertaDikeluarkan, tbodyRemoved, on('participantAdded'/'participantRemoved')
- updateSummaryFromDOM pakai `tbody:not(#tbodyRemoved)` (Pitfall 2)
- XSS-safe: textContent (JS) + @-encode (Razor); NOL innerHTML untuk reason
- Semua fetch pakai appUrl() (sub-path)
- Runtime smoke @5277: modal picker buka, panel render, NOL console error
- migration=FALSE (view-only)
</verification>

<success_criteria>
- Picker "Tambah Peserta" → AddParticipantsLive → baris muncul live via participantAdded (PART-05)
- Modal hapus keras untuk InProgress/Completed-cert; ringan untuk lainnya (PRMV-02 UI; D-01)
- participantRemoved: soft → panel, hard → remove (PLIV-02 UI; Pitfall 6)
- Panel "Peserta Dikeluarkan" render + Restore 1-klik (PLIV-01 UI; D-04)
- count aktif exclude removed (Pitfall 2); reason XSS-safe (Pitfall 3)
- Build 0 error; runtime smoke @5277 OK; migration=FALSE
</success_criteria>

<output>
After completion, create `.planning/phases/412-live-monitoring-ui-signalr/412-02-SUMMARY.md`
</output>
