---
phase: 413-test-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/flexible-participant-412.spec.ts
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [PART-05, PRMV-02, PRMV-04, PLIV-01, PLIV-02]
migration: false
tags: [assessment, participant, e2e, playwright, signalr, multi-context, seed-workflow]
user_setup: []

must_haves:
  truths:
    - "Spec BARU tests/e2e/flexible-participant-412.spec.ts menjalankan 7 sinyal handoff 412 di real browser multi-context (browser.newContext() ×2-3), tunggu window.assessmentHub.state==='Connected' KEDUA context sebelum aksi"
    - "Sinyal (a) add picker → baris muncul live di tbody:not(#tbodyRemoved) tanpa reload (PART-05)"
    - "Sinyal (b) modal keras #hapusPesertaHardModal muncul untuk peserta data-status=InProgress (PRMV-02 client)"
    - "Sinyal (c) force-kick 2-context: admin hapus InProgress → worker terima examRemoved → #examRemovedModal visible + 'Anda telah dikeluarkan dari ujian ini.' + redirect /CMP/Assessment (PRMV-02)"
    - "Sinyal (d) baris pindah ke #tbodyRemoved live (PLIV-01); (e) Restore 1-klik .btn-restore-peserta → baris balik ke aktif live (PRMV-04/D-04)"
    - "Sinyal (f) updateSummaryFromDOM count aktif turun/exclude #tbodyRemoved (Pitfall 2); (g) multi-observer admin A+B lihat perubahan sama (PLIV-02)"
    - "Seed via SEED_WORKFLOW: beforeAll BACKUP DB + flip sesi (punya paket soal) ke InProgress; afterAll RESTORE + SEED_JOURNAL active→cleaned (sukses ATAU gagal)"
    - "Run --workers=1; NO sleep buta (event-driven waitForSelector/waitForFunction)"
  artifacts:
    - path: "tests/e2e/flexible-participant-412.spec.ts"
      provides: "Playwright e2e multi-context untuk 7 sinyal live add/remove/restore/force-kick/broadcast + seed lifecycle backup/restore"
      contains: "flexible-participant-412"
      min_lines: 200
    - path: "docs/SEED_JOURNAL.md"
      provides: "Entry seed temporary+local-only Phase 413 e2e (active saat seed, cleaned setelah restore)"
      contains: "413"
  key_links:
    - from: "flexible-participant-412.spec.ts"
      to: "AssessmentMonitoringDetail.cshtml (selector kontrak)"
      via: "page.locator(#btnTambahPeserta / .btn-hapus-peserta / #tbodyRemoved / .btn-restore-peserta)"
      pattern: "btnTambahPeserta|btn-hapus-peserta|tbodyRemoved|btn-restore-peserta"
    - from: "flexible-participant-412.spec.ts (worker context)"
      to: "StartExam.cshtml (#examRemovedModal handler)"
      via: "pageWorker.locator('#examRemovedModal').waitFor visible"
      pattern: "examRemovedModal"
    - from: "flexible-participant-412.spec.ts (seed)"
      to: "dbSnapshot.ts (backup/restore/queryScalar/queryString)"
      via: "db.backup/db.restore di beforeAll/afterAll"
      pattern: "db\\.(backup|restore|queryScalar|queryString)"
---

<objective>
Buat spec Playwright e2e BARU `tests/e2e/flexible-participant-412.spec.ts` yang membuktikan **7 sinyal live** add/remove/restore/force-kick/broadcast di **real browser multi-context** — sinyal yang di-defer eksplisit dari `412-VALIDATION.md §Deferred to Phase 413 e2e (Handoff List)`. Lesson Phase 354: build+grep tak cukup untuk Razor/JS/SignalR dinamis → real browser WAJIB (D-01 CONTEXT).

7 sinyal (sumber kebenaran = handoff 412):
- (a) **add live** (PART-05) — admin klik Tambah picker → baris muncul live di tabel aktif tanpa reload.
- (b) **modal keras** (PRMV-02 client) — peserta `data-status="InProgress"` → `#hapusPesertaHardModal` muncul (bukan modal ringan).
- (c) **force-kick worker** (PRMV-02) — 2-context: admin hapus InProgress → worker terima `examRemoved` → `#examRemovedModal` + redirect `/CMP/Assessment`.
- (d) **panel removed** (PLIV-01) — baris soft-removed pindah ke `#tbodyRemoved` live.
- (e) **restore** (PRMV-04/D-04) — `.btn-restore-peserta` 1-klik → baris balik ke aktif live.
- (f) **count exclude** (Pitfall 2) — `updateSummaryFromDOM` count aktif turun/exclude `#tbodyRemoved`.
- (g) **multi-observer** (PLIV-02) — admin A + admin B lihat perubahan sama tanpa reload.

Seed via **SEED_WORKFLOW** (CLAUDE.md + docs/SEED_WORKFLOW.md): `beforeAll` BACKUP DB lokal + flip sesi (yang PUNYA paket soal) ke InProgress; `afterAll` RESTORE + SEED_JOURNAL `active`→`cleaned`. Klasifikasi `temporary + local-only`.

Purpose: tutup 7 sinyal Playwright-only yang membuktikan UI+SignalR live berfungsi runtime (verifikasi end-to-end milestone v32.5).
Output: 1 spec baru + 1 entry SEED_JOURNAL. migration=FALSE. NOL kode produksi baru.

> ⚠️ Plan ini `autonomous: false` — Task terakhir = `checkpoint:human-verify` yang menjalankan spec di real browser (butuh app @5277 AD-off running + SQLEXPRESS) dan mengonfirmasi 7 sinyal PASS + DB ter-restore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/413-test-uat/413-CONTEXT.md
@.planning/phases/413-test-uat/413-RESEARCH.md
@.planning/phases/412-live-monitoring-ui-signalr/412-VALIDATION.md
@CLAUDE.md
@docs/SEED_WORKFLOW.md

<interfaces>
<!-- Selector kontrak + helper VERIFIED dari kode. Executor pakai langsung — JANGAN eksplorasi ulang. -->

Helper REUSE (import):
  tests/helpers/auth.ts:4         → login(page, account)  // tunggu **/Home/**
  tests/helpers/accounts.ts       → accounts: admin=admin@pertamina.com/123456, hc=meylisa, coachee=rino, coachee2=iwan3
  tests/helpers/dbSnapshot.ts     → import * as db; db.backup(path) / db.restore(path) / db.queryScalar(sql)→number / db.queryString(sql)→string / db.execScript(sqlPath)
  tests/helpers/utils.ts          → today()/tomorrow() (fmtLocal — selaras DateTime.Today server, hindari off-by-one UTC)
  tests/e2e/helpers/examTypes.ts:534-580 → addExtraTimeViaModal (REFERENSI KANONIK multi-context SignalR — pola HC-fire→worker-DOM-await)

Selector kontrak DOM — Views/Admin/AssessmentMonitoringDetail.cshtml [VERIFIED]:
  #btnTambahPeserta (:197 buka picker modal)
  .tambah-peserta-check (:1332 checkbox picker)
  #btnKonfirmasiTambah (:836 konfirmasi → POST AddParticipantsLive)
  tr[data-session-id="{id}"] .btn-hapus-peserta (:360 item hapus per-baris; data-status=@UserStatus :363, data-has-cert :364)
  #hapusPesertaHardModal (:880 modal keras — data-bs-backdrop=static) / #hapusPesertaLightModal (:845 ringan)
  #hapusHardReason (:918 input alasan keras) / #btnHapusHardKonfirmasi (:932 konfirmasi keras)
  #tbodyRemoved (:444 tbody panel) / #panelPesertaDikeluarkan (:415) / #panelPesertaDikeluarkanBody (collapse) / #countRemoved (:426 badge)
  .btn-restore-peserta (:456 restore per-baris di panel)
  Active rows: tbody:not(#tbodyRemoved) tr[data-session-id]  // updateSummaryFromDOM count source

Selector kontrak worker — Views/CMP/StartExam.cshtml [VERIFIED]:
  #examRemovedModal (:384) berisi "Anda telah dikeluarkan dari ujian ini." (:396)
  #examRemovedReasonValue / #examRemovedReasonText (alasan via textContent — XSS-safe)
  #btnExamRemovedKembali → redirect '@Url.Action("Assessment","CMP")' == /CMP/Assessment ; countdown 5s auto-redirect /CMP/Assessment
  window.assessmentHub.state === 'Connected' (handler examRemoved :1319; valid juga di monitoring :1742)

Route monitoring — Controllers/AssessmentAdminController.cs:3884 [VERIFIED]:
  AssessmentMonitoringDetail(string title, string category, DateTime scheduleDate, string? assessmentType=null)
  URL: /Admin/AssessmentMonitoringDetail?title={t}&category={c}&scheduleDate={d}[&assessmentType={at}]

Worker resume ujian InProgress:
  /CMP/StartExam/{sessionId}  (Resume bila StartedAt!=null & CompletedAt==null & window belum tutup & punya paket soal)

Seed InProgress reliable (pola 412-03 SUKSES) — SQL [VERIFIED]:
  -- Cari sesi yang PUNYA paket soal (412-03: sesi tanpa paket → "tidak memiliki paket soal"):
  SELECT TOP 1 s.Id FROM AssessmentSessions s
  WHERE EXISTS (SELECT 1 FROM AssessmentPackages p WHERE p.AssessmentSessionId = s.Id)
    AND s.RemovedAt IS NULL;
  -- Flip ke InProgress + window +1d:
  UPDATE AssessmentSessions SET Status='InProgress', StartedAt=GETDATE(), CompletedAt=NULL,
         ExamWindowCloseDate=DATEADD(day,1,GETDATE()) WHERE Id=@sid;
  UPDATE UserPackageAssignments SET IsCompleted=0 WHERE AssessmentSessionId=@sid;
  -- Resolve batch key + UserId untuk worker login: SELECT Title, Category, CONVERT(varchar(10),Schedule,23), UserId FROM AssessmentSessions WHERE Id=@sid

Pola lifecycle BACKUP/RESTORE spec — tests/e2e/assessment-pending-grade.spec.ts:46-85 [VERIFIED]:
  beforeAll: resolve InstanceDefaultBackupPath via db.queryString → snapshotPath = `${dir}/HcPortalDB_Dev-pre413-${ts}.bak` → db.backup(snapshotPath) → seed (sqlcmd) → Layer-1 assert seeded
  afterAll: db.restore(snapshotPath) → unlink .bak best-effort → Layer-4 assert 0 seed rows; surface restoreError asli
  test.describe.configure({ mode: 'serial' });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec scaffold + seed lifecycle (beforeAll BACKUP+flip InProgress, afterAll RESTORE) + SEED_JOURNAL entry</name>
  <files>tests/e2e/flexible-participant-412.spec.ts, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/assessment-pending-grade.spec.ts:40-90 (pola beforeAll BACKUP→seed→Layer1, afterAll RESTORE→Layer4→surface restoreError; describe.configure serial)
    - tests/helpers/dbSnapshot.ts:67-150 (db.backup/restore/queryScalar/queryString/execScript signatures + InstanceDefaultBackupPath resolve)
    - .planning/phases/413-test-uat/413-RESEARCH.md §"Pattern 3: Reliable InProgress Seed" + §"Runtime State Inventory" (SEED_JOURNAL active→cleaned wajib)
    - docs/SEED_JOURNAL.md (format tabel — tambah 1 baris, JANGAN edit baris lama; status active saat seed)
    - tests/helpers/{auth,accounts,utils}.ts (login + accounts + today())
  </read_first>
  <action>
    Buat file BARU `tests/e2e/flexible-participant-412.spec.ts`. Header comment: Phase 413 e2e — 7 sinyal live add/remove/restore/force-kick/broadcast (handoff 412-VALIDATION), multi-context SignalR, seed via SEED_WORKFLOW. `test.describe.configure({ mode: 'serial' })`. Import `* as db`, `login`, `accounts`/`today`.

    **Seed lifecycle (inline beforeAll/afterAll — JANGAN bergantung global.teardown.ts matrix; isolasi penuh per RESEARCH §Wave 0 Gaps catatan kritis):**
    - `beforeAll`:
      1. Resolve backup dir: `db.queryString("SELECT CAST(SERVERPROPERTY('InstanceDefaultBackupPath') AS NVARCHAR(260))")` → strip trailing slash → `snapshotPath = ${dir}/HcPortalDB_Dev-pre413-${ts}.bak`.
      2. `db.backup(snapshotPath)`.
      3. Resolve sesi InProgress: pilih sesi yang PUNYA paket soal (SQL `EXISTS AssessmentPackages` + `RemovedAt IS NULL`) via `db.queryScalar`. Flip ke InProgress (Status/StartedAt=GETDATE/CompletedAt=NULL/ExamWindowCloseDate=+1d) + `UPA.IsCompleted=0` via `db.execScript` (tulis SQL ke file `tests/sql/flexible-participant-413-seed.sql` ATAU inline via queryScalar/queryString multi-statement — diskresi; execScript lebih bersih). Simpan ke module-scope var: `inProgressSessionId`, `batchTitle`, `batchCategory`, `batchScheduleDate` (CONVERT varchar(10),Schedule,23), `workerUserId` (UserId sesi → untuk resolve account login worker), `workerEmail` (JOIN Users.Email).
      4. **(Opsional, jika mudah)** Resolve juga 1 sesi not-started (StartedAt NULL, RemovedAt NULL) di batch yang sama untuk uji modal ringan — diskresi.
      5. Layer-1 assert: `db.queryScalar("SELECT COUNT(*) FROM AssessmentSessions WHERE Id=${inProgressSessionId} AND Status='InProgress'")` == 1.
      6. Build `monitoringUrl = /Admin/AssessmentMonitoringDetail?title=${enc}&category=${enc}&scheduleDate=${batchScheduleDate}`.
      > Worker login: spec butuh login sebagai PEMILIK sesi InProgress agar `/CMP/StartExam/{id}` Resume. Jika `workerEmail` cocok salah satu `accounts` (rino/iwan3) → pakai account key itu. Jika tidak, FLIP sesi InProgress ke UserId milik account test (rino) lebih dulu di seed (UPDATE UserId), agar login coachee bisa Resume. Dokumentasikan pilihan.
    - `afterAll`:
      1. `db.restore(snapshotPath)` di blok try; tangkap `restoreError` independen (pola pending-grade WR-02).
      2. Unlink `.bak` best-effort bila restore sukses.
      3. Layer-4 assert: 0 seed residu (sesi tetap di Id sama tapi Status balik baseline — assert via query baseline, informational).
      4. Bila `restoreError` → throw (surface error asli).
      5. **SEED_JOURNAL `active`→`cleaned`:** update baris Phase 413 status `active`→`cleaned` (regex/manual) setelah RESTORE OK. (Executor tulis manual di file karena spec tak edit .md; ATAU spec append note. Default: Task ini tulis entry `active` di docs/SEED_JOURNAL.md saat scaffold; checkpoint Task 3 tandai `cleaned` setelah run sukses.)

    **SEED_JOURNAL entry (tambah 1 baris di docs/SEED_JOURNAL.md, status `active`):** Tanggal 2026-06-21, Phase `413 (e2e flexible add/remove/restore force-kick multi-context)`, Klasifikasi `temporary + local-only`, Tujuan ringkas (jalankan 7 sinyal handoff 412), Dampak (`UPDATE in-place AssessmentSessions sesi-punya-paket → InProgress + UPA.IsCompleted=0; e2e add/remove/restore mutasi RemovedAt/RemovedBy/RemovalReason + sesi baru AddParticipantsLive + AuditLogs; semua revert via RESTORE; Data/SeedData.cs tak tersentuh`), Snapshot file (`{InstanceDefaultBackupPath}/HcPortalDB_Dev-pre413-{ts}.bak`), Status `active`.

    Belum tulis test body sinyal di Task ini — hanya scaffold + seed lifecycle + describe kosong/placeholder agar `npx playwright test flexible-participant-412 --list` mengenali file. (Test body = Task 2.)
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/tests" && npx playwright test flexible-participant-412 --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/flexible-participant-412.spec.ts` exists
    - `grep -n "describe.configure({ mode: 'serial' })\|mode: 'serial'" tests/e2e/flexible-participant-412.spec.ts` → 1
    - `grep -cE "db\.(backup|restore)" tests/e2e/flexible-participant-412.spec.ts` ≥ 2 (BACKUP beforeAll + RESTORE afterAll)
    - `grep -n "InstanceDefaultBackupPath" tests/e2e/flexible-participant-412.spec.ts` → 1 (resolve backup dir, C:\Temp blocked)
    - `grep -n "AssessmentPackages" tests/e2e/flexible-participant-412.spec.ts` OR seed SQL file → seed pilih sesi PUNYA paket soal (Pitfall 1)
    - `grep -n "InProgress" tests/e2e/flexible-participant-412.spec.ts` → ≥1 (flip InProgress)
    - `grep -n "413" docs/SEED_JOURNAL.md` → ≥1 (entry baru status active)
    - `npx playwright test flexible-participant-412 --list` → file ter-discover tanpa error parse
  </acceptance_criteria>
  <done>Spec scaffold + seed lifecycle (BACKUP→flip InProgress sesi-punya-paket→RESTORE + surface restoreError) ada; SEED_JOURNAL entry 413 status active; `--list` mengenali file tanpa error.</done>
</task>

<task type="auto">
  <name>Task 2: Tulis 7 sinyal e2e multi-context (add live / modal keras / force-kick 2-ctx / panel removed / restore / count exclude / multi-observer)</name>
  <files>tests/e2e/flexible-participant-412.spec.ts</files>
  <read_first>
    - tests/e2e/exam-types.spec.ts:735-785 (Flow O — pola force-kick multi-context: browser.newContext()×2, waitForFunction hub Connected KEDUA context, fire→await DOM mutate, finally close)
    - tests/e2e/helpers/examTypes.ts:534-580 (addExtraTimeViaModal — HC-fire→worker-DOM-await kanonik)
    - tests/e2e/edit-peserta-answers.spec.ts:44-148 (multi-context admin/worker + 2-admin concurrency)
    - .planning/phases/413-test-uat/413-RESEARCH.md §"Pattern 1/2" + §"Code Examples" (multi-observer + db.queryScalar assert) + §"Anti-Patterns" (NO sleep buta, NO single-context force-kick, NO cookie collision)
    - <interfaces> di plan ini (selector kontrak VERIFIED — pakai langsung)
  </read_first>
  <action>
    Tulis test body di `tests/e2e/flexible-participant-412.spec.ts`. Tiap sinyal = 1 `test(...)` dengan nama yang cocok grep handoff. Gunakan `browser` fixture (`async ({ browser }) =>`) untuk multi-context; `try/finally` tutup context. WAJIB `waitForFunction(() => (window as any).assessmentHub?.state === 'Connected')` di KEDUA context sebelum fire aksi (Pitfall 3 + buffer 2s setelah StartExam agar server set InProgress). Run `--workers=1` (config sudah fullyParallel:false; NTLM loopback).

    **t(a) `add live — picker → baris muncul live tanpa reload (PART-05)`:**
    - 1 context admin: `login(page,'admin')` → goto `monitoringUrl` → tunggu hub Connected.
    - klik `#btnTambahPeserta` → tunggu picker modal → check `.tambah-peserta-check` pertama (resolve id eligible) → klik `#btnKonfirmasiTambah`.
    - tunggu baris baru live: `waitForSelector('tbody:not(#tbodyRemoved) tr[data-session-id="{newId}"]', {timeout:8000})` (participantAdded handler) — TANPA reload.
    - assert DB: `db.queryScalar("SELECT COUNT(*) FROM AssessmentSessions WHERE Id={newId} AND RemovedAt IS NULL")` == 1.

    **t(b) `hapus keras — modal keras muncul untuk InProgress (PRMV-02 client)`:**
    - admin goto monitoring → klik `tr[data-session-id="${inProgressSessionId}"] .btn-hapus-peserta` → assert `#hapusPesertaHardModal` visible (BUKAN `#hapusPesertaLightModal`). (tier dipicu data-status=InProgress)
    - (boleh gabung ke t(c) sebagai langkah awal force-kick.)

    **t(c) `force kick worker — admin hapus InProgress → worker examRemoved (PRMV-02)` [2-context, sinyal utama]:**
    - `ctxWorker = browser.newContext()` + `ctxHc = browser.newContext()` (cookie-isolated, Pitfall 4).
    - Worker: `login(pageWorker, 'coachee')` (atau account pemilik sesi) → `pageWorker.goto('/CMP/StartExam/${inProgressSessionId}')` → tunggu hub Connected + buffer.
    - Admin: `login(pageHc, 'admin')` (atau 'hc') → goto monitoring → tunggu hub Connected.
    - Admin fire: klik `tr[data-session-id="${inProgressSessionId}"] .btn-hapus-peserta` → `#hapusPesertaHardModal` visible → fill `#hapusHardReason` 'force-kick e2e test' → klik `#btnHapusHardKonfirmasi`.
    - ASSERT worker context: `pageWorker.locator('#examRemovedModal').waitFor({state:'visible', timeout:10000})` + `toContainText('Anda telah dikeluarkan dari ujian ini.')` + `pageWorker.waitForURL('**/CMP/Assessment**', {timeout:12000})` (countdown redirect).
    - ASSERT admin context: baris pindah `#tbodyRemoved tr[data-session-id="${inProgressSessionId}"]`.
    - ASSERT DB: `db.queryScalar("... Id={inProgressSessionId} AND RemovedAt IS NOT NULL")` == 1 (soft-remove tertulis).
    - `finally`: tutup kedua context.

    **t(d) `panel removed — baris soft-removed pindah ke #tbodyRemoved live (PLIV-01)`:**
    - bisa di-derive dari t(c) admin-side assert (baris di `#tbodyRemoved`), ATAU test terpisah hapus peserta Completed/InProgress lain. Assert `#tbodyRemoved tr[data-session-id][data-removed="true"]` muncul + `#panelPesertaDikeluarkan` visible.

    **t(e) `restore — .btn-restore-peserta 1-klik → baris balik aktif live (PRMV-04/D-04)`:**
    - lanjutan state removed (sesi dari t(c)/t(d)): klik `#tbodyRemoved tr[data-session-id="${sid}"] .btn-restore-peserta`.
    - assert baris balik: `waitForSelector('tbody:not(#tbodyRemoved) tr[data-session-id="${sid}"]')` + `expect(#tbodyRemoved tr[data-session-id="${sid}"]).toHaveCount(0)`.
    - assert DB: `db.queryScalar("... Id={sid} AND RemovedAt IS NULL")` == 1.

    **t(f) `count exclude — updateSummaryFromDOM exclude #tbodyRemoved (Pitfall 2)`:**
    - snapshot count aktif (`#countTotal`/badge atau hitung `tbody:not(#tbodyRemoved) tr[data-session-id]`) SEBELUM hapus → hapus 1 peserta → assert count aktif TURUN 1 (baris removed tak terhitung). Boleh gabung ke t(c)/t(d).

    **t(g) `multi observer — admin A + admin B lihat perubahan live (PLIV-02)` [2-3 context]:**
    - `ctxA = newContext()` admin, `ctxB = newContext()` hc; keduanya goto monitoring + hub Connected.
    - A fire remove (atau add) → B assert baris pindah `#tbodyRemoved tr[data-session-id="${sid}"]` (atau baris baru muncul) TANPA reload `pageB`.
    - `finally` tutup context.

    **WAJIB (anti-pattern):** NO `waitForTimeout` buta untuk SignalR (pakai waitForSelector/waitForFunction event-driven). NO single-context force-kick. NO login 2 role di 1 context. RESTORE di afterAll (Task 1) jalan apa pun hasil.

    > Catatan eligible-id (t(a)): resolve `{newId}` dari respons AddParticipantsLive (intercept response JSON `added[].id`) ATAU dari DOM baris baru (`tbody:not(#tbodyRemoved) tr[data-session-id]` yang belum ada sebelum klik). Pola intercept response = lebih deterministik.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/tests" && npx playwright test flexible-participant-412 --list 2>&1 | grep -cE "add live|force kick|panel removed|restore|multi observer|hapus keras|count exclude"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "browser.newContext\(\)" tests/e2e/flexible-participant-412.spec.ts` ≥ 2 (multi-context untuk force-kick + multi-observer)
    - `grep -n "assessmentHub?.state === 'Connected'\|assessmentHub.*Connected" tests/e2e/flexible-participant-412.spec.ts` → ≥2 (readiness KEDUA context)
    - `grep -n "examRemovedModal" tests/e2e/flexible-participant-412.spec.ts` → ≥1 (force-kick worker assert)
    - `grep -n "Anda telah dikeluarkan dari ujian ini." tests/e2e/flexible-participant-412.spec.ts` → 1 (pesan force-kick verbatim)
    - `grep -n "/CMP/Assessment" tests/e2e/flexible-participant-412.spec.ts` → ≥1 (redirect target)
    - `grep -cE "btnTambahPeserta|btnKonfirmasiTambah|btn-hapus-peserta|btnHapusHardKonfirmasi|tbodyRemoved|btn-restore-peserta" tests/e2e/flexible-participant-412.spec.ts` ≥ 5 (kontrak add/hapus/panel/restore)
    - `grep -cE "waitForTimeout\(" tests/e2e/flexible-participant-412.spec.ts` == 0 untuk menunggu SignalR (NO sleep buta; waitForTimeout HANYA boleh untuk buffer post-StartExam ≤2s bila perlu — bila ada, ≤1 occurrence dengan komentar)
    - `npx playwright test flexible-participant-412 --list` → 7 sinyal test ter-daftar (add live, hapus keras, force kick, panel removed, restore, count exclude, multi observer)
    - Spec compile (TS) tanpa error: `npx tsc --noEmit` di tests ATAU `--list` sukses
  </acceptance_criteria>
  <done>7 sinyal e2e ter-tulis multi-context dengan selector VERIFIED + readiness hub Connected + event-driven assert + DB queryScalar komplemen; `--list` mendaftarkan ketujuh; TS compile bersih. (Run live = checkpoint Task 3.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Spec e2e `tests/e2e/flexible-participant-412.spec.ts` (7 sinyal multi-context) + seed lifecycle BACKUP/RESTORE + SEED_JOURNAL entry 413. Claude telah menulis seluruh spec + seed (Task 1-2) tetapi BELUM menjalankannya di real browser — e2e Playwright multi-context + SignalR WAJIB dijalankan dengan app @5277 (AD-off) + SQLEXPRESS hidup (real-browser non-negotiable, lesson Phase 354 / D-01 CONTEXT).
  </what-built>
  <how-to-verify>
    1. Pastikan SQLEXPRESS hidup: `sqlcmd -S localhost\SQLEXPRESS -E -C -Q "SELECT 1"` → 1.
    2. Jalankan app lokal AD-off di terminal terpisah (port 5277):
       `Authentication__UseActiveDirectory=false dotnet run --project HcPortal.csproj --urls http://localhost:5277`
       (tunggu "Now listening on: http://localhost:5277"). Login uji: admin@pertamina.com / 123456.
    3. (Jika `tests/node_modules` belum ada) `cd tests && npm ci`.
    4. Jalankan e2e 7 sinyal — WAJIB `--workers=1` (NTLM loopback; lihat reference_local_e2e_sql_env_fix):
       `cd tests && npx playwright test flexible-participant-412 --workers=1`
    5. Konfirmasi 7 sinyal PASS:
       (a) add live — baris muncul tanpa reload; (b) modal keras InProgress; (c) force-kick worker (#examRemovedModal + 'Anda telah dikeluarkan' + redirect /CMP/Assessment); (d) baris pindah #tbodyRemoved; (e) Restore baris balik; (f) count aktif turun/exclude; (g) multi-observer admin A+B.
    6. Konfirmasi DB ter-RESTORE: afterAll RESTORE jalan; cek `sqlcmd -S localhost\SQLEXPRESS -d HcPortalDB_Dev -E -C -Q "SELECT Status FROM AssessmentSessions WHERE Id=<inProgressSessionId>"` → kembali ke baseline (bukan InProgress sisa seed). Tidak ada `HcPortalDB_Test_*` tersisa.
    7. Tandai SEED_JOURNAL baris Phase 413 status `active`→`cleaned` (RESTORE OK).
    > Bila ada sinyal gagal karena flake SignalR (Pitfall 3 readiness/race): re-run; bila konsisten gagal, laporkan sinyal + error untuk perbaikan spec (bukan kode produksi). Bila gagal mengungkap BUG PRODUK nyata → catat sebagai gap (mungkin perlu fix di fase terpisah).
  </how-to-verify>
  <resume-signal>Ketik "approved" bila 7 sinyal PASS + DB ter-restore + SEED_JOURNAL cleaned; atau jelaskan sinyal yang gagal + error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright (real browser) → app @5277 (AD-off) | e2e melintasi auth nyata (login admin/worker); membuktikan RBAC + guard runtime |
| Seed sqlcmd → HcPortalDB_Dev (lokal) | DB Dev lokal disentuh sementara — BACKUP sebelum + RESTORE sesudah (SEED_WORKFLOW; localhost-guard dbSnapshot) |
| 2 browser context (admin + worker) | cookie-isolated; force-kick lintas-koneksi SignalR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-413-E1 | Tampering | Seed e2e korup HcPortalDB_Dev permanen | mitigate | `db.backup()` beforeAll + `db.restore()` afterAll (jalan sukses ATAU gagal, restoreError di-surface); `dbSnapshot.ts` localhost-guard (reject non-localhost); klasifikasi temporary+local-only + SEED_JOURNAL active→cleaned |
| T-413-E2 | Elevation/Tampering | Worker re-submit setelah dihapus (resubmit removed) | mitigate | Force-kick e2e (t(c)) buktikan `examRemoved` + redirect; guard server `IsParticipantRemoved` (PRMV-03) sudah di-lock xUnit 409 + lifecycle 413-01 |
| T-413-E3 | Tampering (XSS) | RemovalReason bebas-teks di modal/panel | mitigate | Render via `.textContent` (T-412-14 review-passed); e2e BOLEH inject reason berisi karakter HTML & assert ter-encode (opsional hardening; minimal reason teks biasa) |
| T-413-E4 | Elevation (RBAC bypass) | Worker akses endpoint add/remove | mitigate | Endpoint `[Authorize(Roles="Admin,HC")]`+antiforgery (lock xUnit 411); worker-context e2e tak punya kontrol Tambah/Hapus di monitoring (view RBAC) — observasi by-design |
| T-413-E5 | Info Disclosure | Kredensial DB bocor di spec | accept | `Integrated Security=True` (Windows auth lokal) via dbSnapshot; nol secret literal di spec |

> Plan 02 = test/UAT-only; nol surface produksi baru. Threat dominan = isolasi DB (BACKUP/RESTORE) + membuktikan kontrol keamanan existing runtime.
</threat_model>

<verification>
1. `npx playwright test flexible-participant-412 --list` → 7 sinyal ter-daftar; TS compile bersih.
2. Checkpoint Task 3: `npx playwright test flexible-participant-412 --workers=1` (app @5277 AD-off + SQLEXPRESS) → 7 sinyal PASS.
3. DB ter-RESTORE pasca-run (afterAll); SEED_JOURNAL 413 `active`→`cleaned`; tak ada `HcPortalDB_Test_*`/`.bak` menumpuk.
4. `git status` → hanya `tests/e2e/flexible-participant-412.spec.ts` (+ opsional `tests/sql/flexible-participant-413-seed.sql`) + `docs/SEED_JOURNAL.md`; NOL diff kode produksi / Migrations / Data (migration=FALSE).
</verification>

<success_criteria>
- Spec `flexible-participant-412.spec.ts` ada; 7 sinyal multi-context (add live / modal keras / force-kick 2-ctx / panel removed / restore / count exclude / multi-observer) PASS di real browser @5277 AD-off `--workers=1`.
- Seed via SEED_WORKFLOW (BACKUP→flip InProgress sesi-punya-paket→RESTORE); SEED_JOURNAL `active`→`cleaned`; DB lokal bersih pasca-run.
- NO sleep buta SignalR; readiness hub Connected KEDUA context; selector VERIFIED. migration=FALSE; nol kode produksi.
</success_criteria>

<output>
After completion, create `.planning/phases/413-test-uat/413-02-SUMMARY.md`. Catat: account login worker yang dipakai (apakah flip UserId sesi InProgress ke rino diperlukan); sinyal mana yang digabung vs terpisah; hasil run 7 sinyal (PASS/flake/re-run); konfirmasi DB ter-restore + SEED_JOURNAL cleaned; temuan bug produk (bila ada) → kandidat gap fase terpisah.
</output>
