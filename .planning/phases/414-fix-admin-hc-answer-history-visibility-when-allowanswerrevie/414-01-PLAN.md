---
phase: 414-fix-admin-hc-answer-history-visibility-when-allowanswerrevie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/AssessmentResultsViewModel.cs
  - Controllers/CMPController.cs
  - Views/CMP/Results.cshtml
  - HcPortal.Tests/CanReviewAnswersTests.cs
autonomous: true
requirements: []
tags: [authorization, view-gating, assessment, results]

must_haves:
  truths:
    - "Admin/HC (non-owner) membuka View Results peserta dengan AllowAnswerReview=false melihat section 'Tinjauan Jawaban' per-soal (SC-1)"
    - "Owner (peserta) membuka hasil sendiri dengan AllowAnswerReview=false tetap diblok dengan alert lama (SC-2)"
    - "AllowAnswerReview=true: perilaku admin & owner tidak berubah, nol regresi (SC-3)"
    - "dotnet build 0 error + dotnet test ~CanReviewAnswers hijau (SC-4)"
    - "Saat review tampil hanya karena bypass non-owner (CanReviewAnswers && !AllowAnswerReview), nota admin Bahasa Indonesia tampil"
  artifacts:
    - path: "Models/AssessmentResultsViewModel.cs"
      provides: "Field VM efektif CanReviewAnswers (raw AllowAnswerReview tetap utuh)"
      contains: "public bool CanReviewAnswers"
    - path: "Controllers/CMPController.cs"
      provides: "Pure static helper CanReviewAnswers + wiring di action Results (gate build + VM flag dari satu variabel)"
      contains: "public static bool CanReviewAnswers"
    - path: "Views/CMP/Results.cshtml"
      provides: "Gate per-soal pakai Model.CanReviewAnswers + nota admin saat toggle OFF (XSS-safe Razor encoded)"
      contains: "Model.CanReviewAnswers"
    - path: "HcPortal.Tests/CanReviewAnswersTests.cs"
      provides: "xUnit Theory pure static, no-DB, 4 InlineData matrix owner-vs-non-owner × toggle"
      contains: "CMPController.CanReviewAnswers"
  key_links:
    - from: "Controllers/CMPController.cs Results action gate (L2266)"
      to: "Controllers/CMPController.cs Results action VM flag (L2379)"
      via: "satu variabel lokal canReviewAnswers dipakai DUA kali"
      pattern: "if \\(canReviewAnswers\\)"
    - from: "Views/CMP/Results.cshtml gate L316"
      to: "Model.CanReviewAnswers"
      via: "Razor @if gate pakai flag efektif"
      pattern: "Model\\.CanReviewAnswers && Model\\.QuestionReviews"
    - from: "HcPortal.Tests/CanReviewAnswersTests.cs"
      to: "CMPController.CanReviewAnswers"
      via: "panggilan pure static langsung (no DB / no WebApplicationFactory)"
      pattern: "CMPController\\.CanReviewAnswers"
---

<objective>
Decouple gate tampilan history jawaban per-soal ("Tinjauan Jawaban") di `CMP/Results` dari toggle `AllowAnswerReview` berdasarkan owner-vs-non-owner: Admin/HC/L3/L4-section-scoped (non-owner, sudah lolos `IsResultsAuthorized`) **selalu** melihat review; peserta (owner) **tetap** di-gate toggle. Bugfix off-theme v32.5 — TIDAK menambah REQ ke 11/11.

Purpose: Saat ini `CMPController.Results` membangun `QuestionReviews` hanya `if (assessment.AllowAnswerReview)` (L2266) dan `Views/CMP/Results.cshtml` (L316/L413) gate flag yang sama, tanpa cek role. Karena action di-share worker & admin/HC (auth `IsResultsAuthorized`), admin/HC ikut buta saat toggle OFF — padahal mereka butuh meninjau jawaban peserta dari Monitoring.

Output: Field VM baru `CanReviewAnswers`, pure static helper `CMPController.CanReviewAnswers(bool, bool)`, view gate pakai flag efektif + nota admin, dan unit test xUnit 4-case. migration=FALSE. Branch main, NOT pushed (IT promosi ke Dev/Prod).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Project rules (CLAUDE.md):** Respons/string user-facing dalam Bahasa Indonesia. Develop Workflow: verifikasi lokal WAJIB sebelum commit — `dotnet build` + `dotnet test` + `dotnet run` (cek `http://localhost:5277`). Branch = main. **JANGAN push** (deploy Dev/Prod = tanggung jawab IT; notify IT commit hash + migration=FALSE). JANGAN edit kode/DB di Dev/Prod.

**Hati-hati (CONTEXT/RESEARCH):** Ada worktree stale `.claude/worktrees/pensive-saha-4b1351/` berisi salinan lama CMPController/VM/test — **JANGAN edit di sana**, kerja HANYA di main tree (`C:\Users\Administrator\OneDrive - PT Pertamina (Persero)\Desktop\PortalHC_KPB`).
</execution_context>

<context>
@.planning/phases/414-fix-admin-hc-answer-history-visibility-when-allowanswerrevie/414-CONTEXT.md
@.planning/phases/414-fix-admin-hc-answer-history-visibility-when-allowanswerrevie/414-RESEARCH.md

<interfaces>
<!-- Kontrak yang dibutuhkan executor — diekstrak langsung dari kode working tree. Pakai langsung, no exploration. -->

From Models/AssessmentResultsViewModel.cs (L12 — sisip CanReviewAnswers tepat setelah baris ini):
```csharp
public bool AllowAnswerReview { get; set; }   // L12 — RAW toggle (D-01: TETAP raw, jangan diubah)
public bool GenerateCertificate { get; set; } // L13
```

From Controllers/CMPController.cs — template pure static helper (CETAKAN, jangan diedit):
```csharp
// L2526
public static bool IsResultsAuthorized(string? ownerUserId, string currentUserId, int roleLevel, string? currentUserSection, string? ownerSection) { ... }
// L2540
public static bool IsParticipantRemoved(AssessmentSession session) => session.RemovedAt != null;
```

From Controllers/CMPController.cs — Results action (titik edit terverifikasi):
```csharp
// L2216-2219 (existing — user & owner diketahui setelah baris ini)
var (user, roleLevel) = await GetCurrentUserRoleLevelAsync();
if (user == null) return Challenge();
bool isAuthorized = IsResultsAuthorized(assessment.UserId, user.Id, roleLevel, user.Section, assessment.User?.Section);
if (!isAuthorized) return Forbid();
// ... L2222 status submitted check ...
// L2240: if (packageAssignment != null) {   ← package path
// L2266:     if (assessment.AllowAnswerReview) {  ← GATE BUILD questionReviews
// L2379:     AllowAnswerReview = assessment.AllowAnswerReview,  ← VM (package path)
// L2391: } else {   ← legacy/empty path
// L2403:     AllowAnswerReview = false,  ← VM (legacy path)
```

From Views/CMP/Results.cshtml:
```cshtml
@* L316 *@ @if (Model.AllowAnswerReview && Model.QuestionReviews != null)
@* L413 *@ else if (!Model.AllowAnswerReview)
@* L415-417 alert lama: "Tinjauan jawaban tidak tersedia untuk assessment ini." *@
```

From HcPortal.Tests/ResultsAuthorizationTests.cs (CETAKAN test pure static — cermin persis):
```csharp
using HcPortal.Controllers;
using Xunit;
namespace HcPortal.Tests;
public class ResultsAuthorizationTests
{
    [Theory]
    [InlineData("u1", "u1", 6, "A", "A", true)]
    // ...
    public void IsResultsAuthorized_Matrix(...) => Assert.Equal(expected, CMPController.IsResultsAuthorized(...));
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tambah field VM efektif CanReviewAnswers</name>
  <read_first>Models/AssessmentResultsViewModel.cs</read_first>
  <files>Models/AssessmentResultsViewModel.cs</files>
  <action>
Di `Models/AssessmentResultsViewModel.cs`, sisipkan satu properti baru **tepat setelah** L12 `public bool AllowAnswerReview { get; set; }` (sebelum L13 `public bool GenerateCertificate { get; set; }`):

```csharp
        public bool AllowAnswerReview { get; set; }
        // Phase 414 (D-01): nilai EFEKTIF "boleh lihat tinjauan jawaban" = AllowAnswerReview || non-owner.
        // AllowAnswerReview (di atas) TETAP raw toggle — view butuh membedakan "OFF tapi admin tetap lihat" untuk nota admin.
        public bool CanReviewAnswers { get; set; }
        public bool GenerateCertificate { get; set; }
```

JANGAN ubah/hapus baris `AllowAnswerReview` yang ada (D-01: raw makna asli utuh). Hanya MENAMBAH `CanReviewAnswers`. Tidak ada properti lain disentuh. Field ini in-memory only — bukan kolom DB, migration=FALSE.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `Models/AssessmentResultsViewModel.cs` contains exact string `public bool CanReviewAnswers { get; set; }`
- `Models/AssessmentResultsViewModel.cs` STILL contains `public bool AllowAnswerReview { get; set; }` (raw, tak terhapus)
- `dotnet build HcPortal.csproj` exit 0 (Build succeeded, 0 error)
  </acceptance_criteria>
  <done>Properti `CanReviewAnswers` ada di VM setelah `AllowAnswerReview`; `AllowAnswerReview` raw tetap; build 0 error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pure static helper CanReviewAnswers + wiring action Results</name>
  <read_first>Controllers/CMPController.cs (action Results L2206-2414; helper region L2526-2540)</read_first>
  <files>Controllers/CMPController.cs</files>
  <action>
Tiga edit di `Controllers/CMPController.cs`, semuanya WAJIB pakai SATU variabel `canReviewAnswers` yang sama (Pitfall 1 — gate build & VM flag desync → null-data / hide tak konsisten).

**(2a) Helper pure static** — sisip tepat SETELAH `IsParticipantRemoved` (L2540), sebelum `GenerateSecureToken` (L2542 `/// <summary>`). Pola identik `IsResultsAuthorized`/`IsParticipantRemoved` (static, no-DB, no-HTTP):

```csharp
        // Phase 414 (D-02/D-03): keputusan "boleh lihat tinjauan jawaban per-soal".
        // Non-owner SELALU true (sudah lolos IsResultsAuthorized = Admin/HC/L3/L4-section); owner ikut toggle.
        // Pure static (pola IsResultsAuthorized/IsParticipantRemoved) → unit-testable tanpa DB/WebApplicationFactory.
        public static bool CanReviewAnswers(bool allowAnswerReview, bool isOwner) => allowAnswerReview || !isOwner;
```

**(2b) Hitung SEKALI di action Results** — sisip SETELAH status-submitted check (setelah L2226 `}` penutup blok `if (!AssessmentConstants.IsAssessmentSubmitted(...))`) dan SEBELUM `// Build ViewModel` (L2228) / `if (packageAssignment != null)` (L2240), agar dipakai kedua branch:

```csharp
            // Phase 414 (D-02/D-03): hitung sekali, pakai dua kali (gate build L2266 + VM flag L2379).
            // user EFEKTIF dari GetCurrentUserRoleLevelAsync (impersonation-aware) — JANGAN _userManager.GetUserAsync(User) lagi (Pitfall 4).
            bool isOwner = assessment.UserId == user.Id;
            bool canReviewAnswers = CanReviewAnswers(assessment.AllowAnswerReview, isOwner);
```

**(2c) Ganti gate build (L2266)** — `if (assessment.AllowAnswerReview)` → `if (canReviewAnswers)`. Body blok di dalamnya (build `questionReviews`) TIDAK berubah.

**(2d) Set VM flag (package path, region L2370-2389)** — BIARKAN L2379 `AllowAnswerReview = assessment.AllowAnswerReview,` (raw, D-01). TAMBAH satu baris baru tepat setelahnya:

```csharp
                    AllowAnswerReview = assessment.AllowAnswerReview,
                    CanReviewAnswers = canReviewAnswers,
```

**(2e) Set VM flag (legacy/empty path, region L2391-2413)** — BIARKAN L2403 `AllowAnswerReview = false,` (D-01, no package = no review). TAMBAH satu baris baru tepat setelahnya (OQ-1 rekomendasi RESEARCH: hardcode `false` — tidak ada `QuestionReviews` untuk ditinjau; mencegah nota admin keliru muncul pada sesi kosong):

```csharp
                    AllowAnswerReview = false,
                    CanReviewAnswers = false,
```

JANGAN cek roleLevel lagi di dalam helper (owner-check tunggal cukup — titik pasca-`IsResultsAuthorized` dijamin hanya non-owner berwenang). JANGAN overwrite `Model.AllowAnswerReview` dengan nilai efektif (DITOLAK D-01).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `Controllers/CMPController.cs` contains exact string `public static bool CanReviewAnswers(bool allowAnswerReview, bool isOwner) => allowAnswerReview || !isOwner;`
- `Controllers/CMPController.cs` contains `bool canReviewAnswers = CanReviewAnswers(assessment.AllowAnswerReview, isOwner);`
- `Controllers/CMPController.cs` contains `if (canReviewAnswers)` (gate build, ex-L2266)
- `Controllers/CMPController.cs` contains `CanReviewAnswers = canReviewAnswers,` (package-path VM)
- `Controllers/CMPController.cs` contains `CanReviewAnswers = false,` (legacy-path VM)
- `Controllers/CMPController.cs` STILL contains `if (!isAuthorized) return Forbid();` (gerbang akses utama tak tersentuh — regression-of-privacy guard T-414-01)
- grep `if \(assessment.AllowAnswerReview\)` di CMPController.cs returns 0 (gate lama sudah diganti)
- `dotnet build HcPortal.csproj` exit 0 (Build succeeded, 0 error)
  </acceptance_criteria>
  <done>Helper `CanReviewAnswers` ada; `canReviewAnswers` dihitung sekali pasca-auth, dipakai di gate build + dua VM-flag (package=variabel, legacy=false); `IsResultsAuthorized`/`Forbid()` utuh; build 0 error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: View gate pakai CanReviewAnswers + nota admin (XSS-safe)</name>
  <read_first>Views/CMP/Results.cshtml (gate L316; alert L413-418)</read_first>
  <files>Views/CMP/Results.cshtml</files>
  <action>
Tiga edit di `Views/CMP/Results.cshtml`. Semua teks Bahasa Indonesia. Razor encoded by default (`@`/static markup) — JANGAN pakai `@Html.Raw` untuk data apa pun (T-414-02 XSS-safe).

**(3a) Ganti gate render (L316)** — `@if (Model.AllowAnswerReview && Model.QuestionReviews != null)` → `@if (Model.CanReviewAnswers && Model.QuestionReviews != null)`.

**(3b) Nota admin** — tambahkan blok nota tepat DI DALAM card "Tinjauan Jawaban", setelah `<div class="card-header bg-light">...</div>` (L319-321) dan sebelum `<div class="card-body p-0">` (L322). Nota tampil HANYA saat review tampil karena bypass non-owner (D-04: `CanReviewAnswers && !AllowAnswerReview`). Teks static Razor (encoded), tanpa interpolasi data user:

```cshtml
            </div>
            @if (Model.CanReviewAnswers && !Model.AllowAnswerReview)
            {
                <div class="alert alert-info py-2 px-3 mb-0 small border-0 rounded-0">
                    <i class="bi bi-eye-slash me-1"></i>Peserta tidak dapat melihat tinjauan ini (Tinjauan Jawaban dinonaktifkan). Hanya admin/HC yang melihatnya.
                </div>
            }
            <div class="card-body p-0">
```

(Penempatan/warna/ikon = Claude's Discretion — boleh `alert-info`/`alert-secondary`, header card vs atas list; pertahankan visual konsisten dengan alert existing.)

**(3c) Ganti gate alert lama (L413)** — `else if (!Model.AllowAnswerReview)` → `else if (!Model.CanReviewAnswers)`. Body alert "Tinjauan jawaban tidak tersedia untuk assessment ini." (L415-417) TIDAK berubah — owner+OFF tetap masuk sini.

Pastikan struktur `@if {...} else if {...}` tetap valid (balanced braces) setelah edit.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `Views/CMP/Results.cshtml` contains `@if (Model.CanReviewAnswers && Model.QuestionReviews != null)`
- `Views/CMP/Results.cshtml` contains `else if (!Model.CanReviewAnswers)`
- `Views/CMP/Results.cshtml` contains `@if (Model.CanReviewAnswers && !Model.AllowAnswerReview)` (kondisi nota admin)
- `Views/CMP/Results.cshtml` contains literal Indonesia nota e.g. `Tinjauan Jawaban dinonaktifkan`
- `Views/CMP/Results.cshtml` STILL contains alert lama `Tinjauan jawaban tidak tersedia untuk assessment ini.` (owner+OFF path)
- grep `@Html.Raw` tidak muncul di blok nota baru (XSS-safe T-414-02)
- `dotnet build HcPortal.csproj` exit 0 (Razor compile 0 error)
  </acceptance_criteria>
  <done>Gate render & alert pakai `Model.CanReviewAnswers`; nota admin tampil saat `CanReviewAnswers && !AllowAnswerReview` (static encoded, no Html.Raw); alert lama untuk owner+OFF utuh; build 0 error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unit test xUnit pure static CanReviewAnswers (no-DB)</name>
  <read_first>HcPortal.Tests/ResultsAuthorizationTests.cs (cetakan); Controllers/CMPController.cs (helper CanReviewAnswers dari Task 2)</read_first>
  <files>HcPortal.Tests/CanReviewAnswersTests.cs</files>
  <behavior>
Matrix `CMPController.CanReviewAnswers(allowAnswerReview, isOwner)` (4 kasus, cermin `ResultsAuthorizationTests`):
- (allowAnswerReview=false, isOwner=false) → true   [non-owner + OFF = inti fix, admin/HC lihat review]
- (allowAnswerReview=true,  isOwner=false) → true   [non-owner + ON = tak berubah]
- (allowAnswerReview=false, isOwner=true)  → false  [owner + OFF = tetap diblok, perilaku worker]
- (allowAnswerReview=true,  isOwner=true)  → true   [owner + ON = tak berubah]
  </behavior>
  <action>
Buat file BARU `HcPortal.Tests/CanReviewAnswersTests.cs`, cermin persis pola `ResultsAuthorizationTests.cs` — xUnit `[Theory]`+`[InlineData]`, pure static, **NO DB, NO WebApplicationFactory, NO mocks** (lesson 999.12 — hindari replica tautologis). Panggil helper produksi `CMPController.CanReviewAnswers` LANGSUNG:

```csharp
// Phase 414 (D-03): regression lock untuk CMPController.CanReviewAnswers.
// Decouple gate tinjauan jawaban: non-owner SELALU lihat (sudah lolos IsResultsAuthorized); owner ikut toggle.
// Pure static (pola ResultsAuthorizationTests) — no DB / no WebApplicationFactory.
using HcPortal.Controllers;
using Xunit;

namespace HcPortal.Tests;

public class CanReviewAnswersTests
{
    [Theory]
    // allowAnswerReview, isOwner, expected
    [InlineData(false, false, true)]   // non-owner + OFF -> bypass, lihat review (SC-1, inti fix)
    [InlineData(true,  false, true)]   // non-owner + ON  -> lihat (tak berubah)
    [InlineData(false, true,  false)]  // owner + OFF      -> tetap diblok (SC-2, perilaku worker)
    [InlineData(true,  true,  true)]   // owner + ON       -> lihat (SC-3, tak berubah)
    public void CanReviewAnswers_Matrix(bool allow, bool isOwner, bool expected)
        => Assert.Equal(expected, CMPController.CanReviewAnswers(allow, isOwner));
}
```

JANGAN buat fixture DB, JANGAN seed, JANGAN instansiasi controller — helper murni primitif.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~CanReviewAnswers" 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- File `HcPortal.Tests/CanReviewAnswersTests.cs` exists
- contains `public class CanReviewAnswersTests`
- contains `CMPController.CanReviewAnswers(allow, isOwner)`
- contains all 4 InlineData: `[InlineData(false, false, true)]`, `[InlineData(true,  false, true)]`, `[InlineData(false, true,  false)]`, `[InlineData(true,  true,  true)]`
- does NOT contain `WebApplicationFactory`, `DbContext`, or `new CMPController` (pure static, no-DB — lesson 999.12)
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~CanReviewAnswers"` exit 0 (4/4 Passed)
  </acceptance_criteria>
  <done>`CanReviewAnswersTests.cs` ada, 4 InlineData pure static memanggil `CMPController.CanReviewAnswers` langsung, no-DB; `dotnet test --filter ~CanReviewAnswers` 4/4 Passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Admin/HC/peserta) → `[GET] CMP/Results/{id}` | User-controlled `id` (route) menentukan sesi yang ditampilkan; viewer identity dari auth cookie/impersonation. Gerbang akses = `IsResultsAuthorized` (L2218). |
| controller → Razor view | VM (`AllowAnswerReview`, `CanReviewAnswers`, `QuestionReviews`) menyeberang ke render; nota admin = teks static. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-414-01 | Information Disclosure (Broken Access Control) | `CMPController.Results` gate review decoupling | mitigate | Gerbang akses `IsResultsAuthorized(...)` + `if (!isAuthorized) return Forbid();` (L2218-2219) **TETAP** dijalankan tanpa perubahan SEBELUM helper baru. `CanReviewAnswers` hanya melonggarkan DISPLAY review untuk non-owner yang SUDAH lolos akses (Admin/HC/L3/L4-section). Tidak ada endpoint baru, tidak ada data baru, tidak ada auth bypass. Net access surface tak berubah — non-owner yang lihat review tambahan sudah berwenang buka Results sebelum fix. **Regression-of-privacy guard:** owner+toggle-OFF WAJIB tetap `false` (terbukti unit test Task 4 kasus `(false,true)→false`; acceptance Task 2 cek `Forbid()` utuh). |
| T-414-02 | Tampering / XSS | Nota admin di `Results.cshtml` | mitigate | Nota = teks Bahasa Indonesia STATIC via Razor encoded markup (`@if` + literal), TANPA interpolasi data user dan TANPA `@Html.Raw`. Acceptance Task 3 verifikasi `@Html.Raw` tidak muncul di blok nota. |
| T-414-03 | Spoofing (impersonation salah-owner) | `isOwner` di action Results | accept | `user.Id` = user EFEKTIF dari `GetCurrentUserRoleLevelAsync` (impersonation-aware, L2500), konsisten dengan `IsResultsAuthorized` yang juga pakai `user.Id` efektif. `isOwner` dihitung dari `user` (bukan `User` claims asli). Risk rendah & sudah ter-mitigasi oleh pola existing (Pitfall 4 di action). |

**Severity:** Tidak ada threat ≥ high. Perubahan net-neutral terhadap access surface (T-414-01) — review hanya dilonggarkan SETELAH gerbang akses lolos. ASVS V4 Access Control: gerbang utama tak diubah.
</threat_model>

<verification>
**Per-task (sampling):** `dotnet build HcPortal.csproj` (Task 1-3) + `dotnet test HcPortal.Tests --filter "FullyQualifiedName~CanReviewAnswers"` (Task 4).

**Phase gate (sebelum verify-work):**
1. `dotnet build` → 0 error.
2. `dotnet test HcPortal.Tests` → full unit suite hijau (baseline v32.5 = 605/605; target tetap hijau + 4 test baru = ~609).
3. `dotnet run` → boot `http://localhost:5277` OK (GET / HTTP 200, AD-off).
4. Grep guard: `if \(assessment.AllowAnswerReview\)` = 0 occurrence di CMPController.cs (gate lama hilang); `Model.AllowAnswerReview && Model.QuestionReviews` = 0 di Results.cshtml (gate view lama hilang).
5. migration check: `git status Migrations/ Data/` kosong → migration=FALSE.

**Manual UAT (lesson 354 — Razor runtime, terpisah dari plan; gate autopilot):**
- Login admin@pertamina.com (non-owner) → View Results sesi peserta dgn `AllowAnswerReview=false` → section "Tinjauan Jawaban" per-soal TAMPIL + nota admin tampil (SC-1).
- Login worker pemilik sesi (owner) → hasil sendiri dgn toggle OFF → alert "Tinjauan jawaban tidak tersedia untuk assessment ini." (SC-2).
- Toggle ON → admin & owner lihat review tanpa nota, seperti sebelum fix (SC-3).
- Seed UAT (bila perlu): assessment package `AllowAnswerReview=false` + responses, klasifikasi temporary+local-only, snapshot+restore (CLAUDE.md SEED_WORKFLOW).
</verification>

<success_criteria>
- **SC-1:** Admin/HC (non-owner) buka View Results peserta dgn `AllowAnswerReview=false` → section "Tinjauan Jawaban" per-soal TAMPIL (benar/salah + opsi dipilih + jawaban benar), BUKAN alert "tidak tersedia"; nota admin tampil. *(unit kasus `(false,false)→true` + manual UAT)*
- **SC-2:** Peserta (owner) buka hasil sendiri dgn `AllowAnswerReview=false` → tetap diblok dgn alert lama. *(unit kasus `(false,true)→false` + manual UAT)*
- **SC-3:** `AllowAnswerReview=true` → perilaku worker & admin tidak berubah (regresi nol). *(unit kasus `(true,*)→true` + suite hijau)*
- **SC-4:** `dotnet build` 0 error + `dotnet test` hijau (4 test baru + full suite tak regresi) + `dotnet run` localhost:5277 OK. migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/414-fix-admin-hc-answer-history-visibility-when-allowanswerrevie/414-01-SUMMARY.md` (gunakan template summary). Catat: commit hash, migration=FALSE, hasil `dotnet build`/`dotnet test` (4 baru + total suite), status UAT manual (gate terpisah), dan reminder notify IT (commit hash + migration=FALSE) + NOT pushed.
</output>
