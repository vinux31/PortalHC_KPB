---
phase: 415-section-foundation-import-excel-diperluas
plan: 03
type: execute
wave: 3
depends_on: ["415-01", "415-02"]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ImportPackageQuestions.cshtml
  - HcPortal.Tests/SectionImportTests.cs
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03, SEC-04]
must_haves:
  truths:
    - "Template universal 13-kolom (Opsi A–F + No.Section + Nama Section) bisa di-download"
    - "File template baru (>9 kolom) ter-import: kolom No.Section auto-buat record Section, Opsi A–F tersimpan, jawaban benar A–F diterima"
    - "File template lama 9-kolom (Opsi A–D tanpa Section) tetap bisa di-import (SectionId=null, opsi E/F kosong) — kompatibel-mundur"
    - "Import menolak keras bila jumlah soal per-Section antar-paket-saudara tidak sama, dengan DAFTAR ketidakcocokan LENGKAP (sebut SectionNumber + jumlah diharapkan vs aktual) + 0 write"
    - "Fingerprint anti-duplikat menyertakan Opsi E/F + SectionNumber (soal sama beda section/jumlah opsi tidak ter-dedup keliru)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "DownloadQuestionTemplate 13-kolom + ImportPackageQuestions dual-format parser + MakePackageFingerprint(+E,F,SectionNumber) + per-Section count validation + auto-create Section"
      contains: "isNewFormat"
    - path: "Views/Admin/ImportPackageQuestions.cshtml"
      provides: "Format-reference card (universal diperluas + dual-format note) + D-13 full mismatch error list"
      contains: "template universal"
    - path: "HcPortal.Tests/SectionImportTests.cs"
      provides: "IMP-01 roundtrip + IMP-02 legacy 9-col + IMP-03 fingerprint + per-Section count reject"
      contains: "SectionImport"
  key_links:
    - from: "ImportPackageQuestions parser"
      to: "format detection"
      via: "header column count > 9"
      pattern: "LastCellUsed|colCount|isNewFormat"
    - from: "import No.Section column"
      to: "AssessmentPackageSection auto-create"
      via: "find-or-create section by (packageId, sectionNumber)"
      pattern: "AssessmentPackageSections"
    - from: "per-Section count mismatch"
      to: "ImportPackageQuestions.cshtml error list"
      via: "TempData full mismatch list"
      pattern: "TempData\\[\"SectionMismatch\"\\]|TempData\\[\"ImportErrors\"\\]"
---

<objective>
Perluas import Excel jadi dual-format Section-aware: template universal 13-kolom, parser deteksi-otomatis (≤9 lama / >9 baru), terima + simpan Opsi A–F + jawaban benar A–F, auto-buat record Section dari kolom No.Section, fingerprint dedup +E/F+SectionNumber, dan validasi jumlah soal per-Section antar-paket dengan tolak-keras (daftar ketidakcocokan lengkap, 0 write).

Purpose: IMP-01/02/03 + SEC-04 (titik validasi #1 = saat import). 415 menyiapkan DATA + kolom import Opsi A–F; render/grading huruf A–F = Phase 418.
Output: DownloadQuestionTemplate diperluas + ImportPackageQuestions dual-format + fingerprint diperluas + per-Section count guard + ImportPackageQuestions.cshtml error/format UI + test suite import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/415-section-foundation-import-excel-diperluas/415-CONTEXT.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-RESEARCH.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-PATTERNS.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-UI-SPEC.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-01-SUMMARY.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing import seams the executor extends. All line refs verified ±20 this session; re-grep symbols. -->

Current parser (AssessmentAdminController.cs ImportPackageQuestions POST ~:6690, parse body ~:6757-6772):
```csharp
var ws = workbook.Worksheets.First();
foreach (var row in ws.RowsUsed().Skip(1)) {
    var q = row.Cell(1).GetString().Trim();   // OLD order: Q(1)|A-D(2-5)|Correct(6)|ET(7)|Type(8)|Rubrik(9)
    var a = row.Cell(2)..., b=Cell(3), c=Cell(4), d=Cell(5);
    var cor = row.Cell(6).GetString().Trim().ToUpper();
    var cell7 = row.Cell(7)...;  // ElemenTeknis
    var questionType = NormalizeQuestionType(row.Cell(8)...);
    var rubrik = row.Cell(9)...;
    rows.Add((q,a,b,c,d,cor,subComp,questionType,rubrik));
}
```

Current answer whitelists (hard-coded A-D — widen to A-F at import parse, Pitfall 5/O-1):
- count-validation ~:6851: `new[]{"A","B","C","D"}.Contains(normalizedCor) || rcor.Contains(',')`
- MA parse ~:6897: `.Where(s => new[]{"A","B","C","D"}.Contains(s))`
- MC parse ~:6913: `if (!new[]{"A","B","C","D"}.Contains(normalizedCor))`

Current fingerprint (~:7081, 5-arg) + callers (existing-set ~:6721, new-row ~:6922):
```csharp
private static string MakePackageFingerprint(string q, string a, string b, string c, string d)
    => string.Join("|||", new[]{q,a,b,c,d}.Select(NormalizePackageText));
private static string ExtractPackageCorrectLetter(string raw) { ... "ABCD".Contains(...) ... }  // ~:7060
```

Current cross-package count validation (~:6823-6863) — sibling key `Title+Category+Schedule.Date`, single total-count check vs `referencePackage.Questions.Count`.

Current template (DownloadQuestionTemplate ~:6599) headers: `{"Pertanyaan","Opsi A","Opsi B","Opsi C","Opsi D","Jawaban Benar","Elemen Teknis","QuestionType","Rubrik"}`.

Atomic persist precedent (~:6962-6977): `BeginTransactionAsync` → `SaveChangesAsync` → `CommitAsync` / rollback-on-catch.

Section auto-create target (from Plan 01/02): `_context.AssessmentPackageSections` with `(AssessmentPackageId, SectionNumber)` unique.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Universal 13-col template + extended fingerprint + A–F answer acceptance</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
1. **DownloadQuestionTemplate** (~:6599): expand the `headers` array to the universal 13-column layout (spec §9.1 order): `{ "Pertanyaan", "Opsi A", "Opsi B", "Opsi C", "Opsi D", "Opsi E", "Opsi F", "Jawaban Benar", "No. Section", "Nama Section", "Elemen Teknis", "QuestionType", "Rubrik" }`. Keep the same styling loop (#16A34A header) + example rows + `ExcelExportHelper.ToFileResult`. Add at least one example row demonstrating a Section (e.g. No.Section=1, Nama="Pompa & Kompresor") + one MA example with 5–6 options.

2. **MakePackageFingerprint** (~:7081): change signature to `(string q, string a, string b, string c, string d, string e, string f, int? sectionNumber)` returning `string.Join("|||", new[]{q,a,b,c,d,e,f}.Select(NormalizePackageText).Append(sectionNumber?.ToString() ?? "_NOSEC_"))`. (§15.C — soal sama beda section / beda jumlah opsi dianggap berbeda.)

3. **Both fingerprint callers** must use the new signature so they stay comparable:
   - Existing-fingerprint set (~:6718-6727): pass options 0..5 (`opts.ElementAtOrDefault(4)??""`, `(5)??""`) + the existing question's `SectionNumber` — derive from `q.Section?.SectionNumber` (include `.ThenInclude` or join to get it; the loaded `pkg.Questions` carry `SectionId` — load section number via the Section nav or a lookup).
   - New-row fingerprint (~:6922): pass parsed e, f, and the row's SectionNumber.
   - Legacy rows: `e=""`, `f=""`, `sectionNumber=null` → `_NOSEC_`.

4. **ExtractPackageCorrectLetter** (~:7060): widen the `'ABCD'` literals to `'ABCDEF'` (two occurrences: the `.Contains(raw[0])` and the `"OPTION "` branch). This is the ONLY render/grading-adjacent change in 415 and it is import-path answer ACCEPTANCE only (Pitfall 5 / O-1: store correct E/F option; grading by PackageOption.Id is already safe; letter DISPLAY/grading = Phase 418).

5. **Answer whitelists** in the import path: widen the three hard-coded `{"A","B","C","D"}` arrays (count-validation ~:6851, MA parse ~:6897, MC parse ~:6913) to `{"A","B","C","D","E","F"}` so new-format MA rows like `"A,C,E"` import. (O-1 resolved: ACCEPT + STORE A–F.)

Do NOT change any RENDER code (StartExam.cshtml, Results.cshtml, _PreviewQuestion) — that is Phase 418.
  </action>
  <verify>
  <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "Opsi E\|Opsi F\|No. Section\|Nama Section" Controllers/AssessmentAdminController.cs` shows the new template headers.
- `grep -n "int? sectionNumber" Controllers/AssessmentAdminController.cs` shows the extended MakePackageFingerprint signature.
- `grep -n "_NOSEC_" Controllers/AssessmentAdminController.cs` present (legacy fingerprint sentinel).
- `grep -n "ABCDEF" Controllers/AssessmentAdminController.cs` present (widened ExtractPackageCorrectLetter).
- `grep -c "\"A\", \"B\", \"C\", \"D\", \"E\", \"F\"" Controllers/AssessmentAdminController.cs` ≥ 3 (widened whitelists) OR equivalent A–F arrays.
- `dotnet build HcPortal.csproj` = 0 errors.
  </acceptance_criteria>
  <done>13-col template + 8-arg fingerprint (both callers) + A–F answer acceptance at import parse; no render code touched; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-format parser + Section auto-create + per-Section count validation (D-13 #1)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
In **ImportPackageQuestions** POST (Excel branch ~:6743-6779):

1. **Format auto-detect from HEADER row** (Pitfall 3/4): after `var ws = workbook.Worksheets.First();`, compute `int colCount = ws.Row(1).LastCellUsed()?.Address.ColumnNumber ?? 0; bool isNewFormat = colCount > 9;`. Boundary = `>9` (CONTEXT D-415-03 is authority; ignore spec's stray "<11"). Detect from the HEADER row (always fully populated), NOT data rows (blank trailing cells on Essay rows skew per-row count).

2. **Widen the parsed row tuple** to carry `OptE, OptF, int? SectionNumber, string? SectionName`. Branch cell indices on `isNewFormat`:
   - NEW (>9): `Q(1) | A-F(2-7) | Correct(8) | No.Section(9) | Nama Section(10) | ET(11) | Type(12) | Rubrik(13)`.
   - OLD (≤9): unchanged `Q(1) | A-D(2-5) | Correct(6) | ET(7) | Type(8) | Rubrik(9)`; set `E=""`, `F=""`, `SectionNumber=null`, `SectionName=null`.
   - Parse `No.Section` as nullable int (blank/non-numeric → null = "Lainnya").
   - The `pasteText` branch stays A–D 9-col only (no new-format paste in 415).

3. **Per-Section count validation (D-13 #1, SEC-04)** — replace/extend the single total-count check (~:6823-6863):
   - Reuse the sibling resolution VERBATIM (key `Title+Category+Schedule.Date` — do NOT invent a new key; Phase 397 lesson). Load sibling packages with their questions, including each question's SectionNumber (via Section nav or lookup).
   - Group the INCOMING valid rows by SectionNumber (null → implicit "Lainnya" group, §15.A) and group EACH sibling package's questions by SectionNumber.
   - Compare counts per SectionNumber across packages. Build the COMPLETE mismatch list — NEVER stop-at-first (D-415-04). Each entry uses UI-SPEC copy: `$"Section {sn}: Paket \"{me}\" punya {x} soal, Paket \"{sib}\" punya {y} soal (harus sama)."` (for null SectionNumber, label as "Lainnya").
   - If the mismatch list is non-empty → store the full list in TempData (e.g. `TempData["SectionMismatch"] = mismatchList;` as a serialized `List<string>` or join), redirect to ImportPackageQuestions with ZERO writes (atomic — like Inject D-09).
   - Legacy guard (Pitfall 6): all-null SectionNumber on BOTH sides → behave like the OLD total-count check (pass when counts match). Single package / no sibling → skip the per-Section guard entirely.

4. **Section auto-create on commit (O-2)**: when persisting a new-format row with a non-null SectionNumber, find-or-create the `AssessmentPackageSection` for `(packageId, sectionNumber)`. If a row brings a Nama and the section was just created, set `Name` (first-non-empty Nama wins; if section already exists keep its existing Name). Default toggles `StartNewPage=false, ShuffleEnabled=true` (Excel does not carry toggles — §15.D; toggles set in UI only). Set `newQ.SectionId = section.Id` (or via the Section nav). Legacy rows → `SectionId=null`.

5. Keep the **atomic persist** (single transaction, zero-write-on-error). Section creation + question insert MUST be in the SAME transaction.

XSS / injection: import is server-side; mismatch messages render via Razor `@` encode in the view (Task 3). Section Name stored as-is, rendered encoded.
  </action>
  <verify>
  <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "isNewFormat" Controllers/AssessmentAdminController.cs` present, computed from `ws.Row(1).LastCellUsed()` (header-based).
- `grep -n "colCount > 9\|> 9" Controllers/AssessmentAdminController.cs` shows the `>9` boundary.
- `grep -n "TempData\[\"SectionMismatch\"\]\|mismatch" Controllers/AssessmentAdminController.cs` present (full-list, not stop-at-first).
- `grep -n "harus sama" Controllers/AssessmentAdminController.cs` present (UI-SPEC mismatch copy).
- Section auto-create present: `grep -n "AssessmentPackageSections" Controllers/AssessmentAdminController.cs` shows find-or-create in the import path.
- Atomic persist preserved (Section create + question insert in same tx).
- `dotnet build HcPortal.csproj` = 0 errors.
  </acceptance_criteria>
  <done>Dual-format parser (header-based >9 detect), Section auto-create from No.Section, per-Section count hard-block with full mismatch list (0 write), legacy/single-pkg pass; atomic.</done>
</task>

<task type="auto">
  <name>Task 3: ImportPackageQuestions.cshtml format card + D-13 mismatch error list</name>
  <files>Views/Admin/ImportPackageQuestions.cshtml</files>
  <action>
Edit `Views/Admin/ImportPackageQuestions.cshtml` per UI-SPEC (Bahasa Indonesia, keep form POST — not AJAX, O-3):

1. **Format-reference card** (`text-info`/`border-info`): title `Format kolom (template universal diperluas)`, list the 13-column order, and the dual-format note: `File template lama (9 kolom, tanpa Section, Opsi A–D) tetap bisa di-import otomatis. File dengan kolom No. Section / Nama Section / Opsi E–F terdeteksi sebagai format baru.`

2. **Download button** label `Template Universal` (`btn-outline-primary` + `bi bi-download`) → existing DownloadQuestionTemplate route.

3. **D-13 mismatch error block** (alert-danger): when `TempData["SectionMismatch"]` is present, render an `alert-danger` titled `Struktur Section antar-paket tidak sama. Perbaiki dulu sebelum import:` followed by a FULL `<ul>` — one `<li>` per mismatch entry (render via `@` encode, NEVER `@Html.Raw`). Hide any success/result block when this fires.

4. Preserve the existing generic per-row error list + success/warning result alerts (reuse existing TempData pattern). Success copy `@n soal berhasil di-import.` / with skips `@n soal di-import, @m duplikat dilewati.`

XSS-safe: all dynamic text via Razor `@` (auto-encode).
  </action>
  <verify>
  <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "template universal\|Template Universal" Views/Admin/ImportPackageQuestions.cshtml` present.
- `grep -n "tetap bisa di-import otomatis\|format baru" Views/Admin/ImportPackageQuestions.cshtml` present (dual-format note).
- `grep -n "SectionMismatch\|Struktur Section antar-paket tidak sama" Views/Admin/ImportPackageQuestions.cshtml` present (mismatch block).
- No new `@Html.Raw` in the mismatch/format markup.
- `dotnet build HcPortal.csproj` = 0 errors.
  </acceptance_criteria>
  <done>Format card + dual-format note + D-13 full mismatch error list rendered (XSS-safe, form POST); success/warning alerts preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: SectionImportTests (IMP-01/02/03 + SEC-04 import)</name>
  <files>HcPortal.Tests/SectionImportTests.cs</files>
  <behavior>
- IMP-01: a 13-col new-format workbook parses into questions with the right options A–F, correct letters A–F stored (E/F IsCorrect when answered), SectionNumber captured, Section auto-created.
- IMP-02: a 9-col legacy workbook parses (A–D, SectionId=null, E/F empty) — backward-compat.
- IMP-03 fingerprint: same question text with different SectionNumber → distinct fingerprint (not deduped); same Q+options+section → deduped. E/F included.
- SEC-04 / D-13: per-Section count mismatch across sibling packages produces a FULL mismatch list and ZERO writes; matching counts pass; legacy all-null + single-package pass.
  </behavior>
  <action>
Create `HcPortal.Tests/SectionImportTests.cs`. Most logic (parser, fingerprint, count-compare) is pure → use EF InMemory 8.0.0 (`Category!=Integration`) where no real-SQL FK/index semantics are needed; mirror the `InjectExcelImportTests` pattern (ClosedXML usings, build a workbook in-memory with exceljs-style cell writes, feed it through the real `ImportPackageQuestions` action via an `IFormFile` stub built from a `MemoryStream`). For the per-Section count + Section auto-create assertions that need FK persistence, use the `SectionFixture` (Integration, real SQL).

Build fresh `.xlsx` fixtures in-test with ClosedXML (`XLWorkbook` write → MemoryStream → IFormFile stub) so the parser reads them by position. Cover: new 13-col file, legacy 9-col file, MA row with `"A,C,E"`, two sibling packages with mismatched per-Section counts. Assert DB state (questions added, sections created, zero writes on mismatch) + fingerprint distinctness.

De-tautology: drive the REAL `MakePackageFingerprint` / `ImportPackageQuestions` (no replica of parser/fingerprint logic in the test).
  </action>
  <verify>
  <automated>cd "$(git rev-parse --show-toplevel)" && dotnet test --filter "FullyQualifiedName~SectionImport" 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `HcPortal.Tests/SectionImportTests.cs` exists covering IMP-01, IMP-02, IMP-03, SEC-04.
- Tests build real `.xlsx` via ClosedXML and drive the real `ImportPackageQuestions` action.
- Fingerprint distinctness test present (same Q, different SectionNumber → not deduped).
- Per-Section mismatch test asserts FULL list + 0 writes.
- `dotnet test --filter "FullyQualifiedName~SectionImport"` = all Passed.
- `dotnet test --filter "Category!=Integration"` fast suite green (no regression); `dotnet test --filter "FullyQualifiedName~Shuffle"` stays green (backward-compat keystone).
  </acceptance_criteria>
  <done>Import tests prove dual-format parse + A–F acceptance + Section auto-create + fingerprint +E/F+Section + per-Section count hard-block; all green; legacy suites unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Uploaded .xlsx → parser | Untrusted file content (cells, column count, answer letters) |
| Client form → format detection | Format MUST be decided server-side from worksheet, never a client field |
| Section Name / mismatch messages → rendered HTML | XSS via imported Nama Section or error text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-415-10 | Tampering | Format auto-detect | mitigate | Detect from server-read worksheet HEADER row (`ws.Row(1).LastCellUsed()`); never trust a client `isNewFormat` field. |
| T-415-11 | Spoofing/Elevation | ImportPackageQuestions endpoint | mitigate | Existing `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` preserved; file-type + 5MB size guard already present (reuse). |
| T-415-12 | Tampering / data integrity | Per-Section count mismatch | mitigate | D-13 hard-block at import: reject + full mismatch list + ZERO writes (atomic tx). Server-authoritative (not client). |
| T-415-13 | Cross-Site Scripting | Imported Nama Section + mismatch error list render | mitigate | Razor `@` auto-encode in ImportPackageQuestions.cshtml + grouped list; no `@Html.Raw`. |
| T-415-14 | Denial of Service | Malformed/large Excel | accept | Existing 5MB size guard + try/catch on workbook read (reuse); risk low for admin-only authenticated endpoint. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` = 0 errors.
- `dotnet test --filter "FullyQualifiedName~SectionImport"` all green.
- `dotnet test --filter "Category!=Integration"` + `~Shuffle` green (backward-compat keystone).
- `dotnet run` @5277: download Template Universal → 13-col file; import a new-format file → sections auto-created + A–F stored; import legacy 9-col → still works; import mismatched sibling → full error list + 0 write.
</verification>

<success_criteria>
- Template universal 13-kolom (Opsi A–F + No.Section/Nama) bisa di-download.
- File baru ter-import: Section auto-buat, Opsi A–F + jawaban A–F tersimpan.
- File lama 9-kolom tetap ter-import (SectionId=null, E/F kosong) — kompatibel-mundur.
- Mismatch struktur Section antar-paket → tolak keras, daftar lengkap, 0 write.
- Fingerprint +E/F+SectionNumber (no false dedup).
- migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/415-section-foundation-import-excel-diperluas/415-03-SUMMARY.md`. Record the extended fingerprint signature + Section auto-create behavior (tiebreak rule applied) + the mismatch-list TempData key (for Plan 04's StartExam re-guard parity) + confirm migration=FALSE.
</output>
