---
phase: 415-section-foundation-import-excel-diperluas
plan: 04
type: execute
wave: 4
depends_on: ["415-01", "415-02", "415-03"]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Controllers/CMPController.cs
  - HcPortal.Tests/SectionSyncPrePostTests.cs
  - HcPortal.Tests/SectionMismatchGuardTests.cs
autonomous: true
requirements: [SEC-06, SEC-04]
must_haves:
  truths:
    - "Saat sync Pre→Post (SamePackage), record AssessmentPackageSection + SectionId soal + opsi 5–6 ikut tersalin ke PostTest dengan FK ter-remap ke section paket baru (bukan section Pre)"
    - "Semua pemicu sync (semua pemanggil SyncPackagesToPost) mewarisi clone Section karena perbaikan di body tunggal"
    - "Saat mulai ujian (StartExam), bila struktur Section antar-paket-saudara tidak identik → ujian diblok dengan pesan jelas (re-guard drift, D-13 #2)"
    - "Assessment legacy (semua SectionId=null) atau 1-paket TIDAK terblok oleh re-guard (kompatibel-mundur)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "SyncPackagesToPost clone Section rows + old→new SectionId remap via nav property + opsi 5–6"
      contains: "AssessmentPackageSection"
    - path: "Controllers/CMPController.cs"
      provides: "D-13 re-guard before BuildQuestionAssignment (per-Section count check, legacy/single-pkg pass)"
      contains: "struktur Section antar-paket tidak identik"
    - path: "HcPortal.Tests/SectionSyncPrePostTests.cs"
      provides: "SEC-06 deep-clone remap + opsi 5–6 (Integration)"
      contains: "SectionSyncPrePost"
    - path: "HcPortal.Tests/SectionMismatchGuardTests.cs"
      provides: "SEC-04 StartExam re-guard block + legacy/single-pkg pass"
      contains: "SectionMismatchGuard"
  key_links:
    - from: "SyncPackagesToPost cloned question"
      to: "new package's cloned Section"
      via: "old→new section map + nav property (not naive SectionId copy)"
      pattern: "newQ.Section =|sectionMap"
    - from: "CMPController.StartExam (before BuildQuestionAssignment)"
      to: "D-13 re-guard"
      via: "per-Section count compare across sibling packages"
      pattern: "BuildQuestionAssignment"
---

<objective>
Lengkapi fondasi Section dengan dua seam terakhir: (1) `SyncPackagesToPost` ikut deep-clone record Section + remap SectionId soal ke section paket baru + salin opsi 5–6 (SEC-06), dan (2) re-guard D-13 di `CMPController.StartExam` sebelum `BuildQuestionAssignment` yang memblok mulai ujian bila struktur Section antar-paket-saudara tidak identik (SEC-04 titik #2 — tangkap drift edit manual pasca-import).

Purpose: SEC-06 (Pre→Post struktur identik — fondasi LinkPrePost 397) + SEC-04 (validasi titik #2). Menutup 9/9 REQ Phase 415.
Output: SyncPackagesToPost diperluas + StartExam re-guard + 2 test suite (sync deep-clone, mismatch guard). Legacy/1-paket TIDAK terblok (kompatibel-mundur keystone).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/415-section-foundation-import-excel-diperluas/415-CONTEXT.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-RESEARCH.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-PATTERNS.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-01-SUMMARY.md
@.planning/phases/415-section-foundation-import-excel-diperluas/415-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing seams the executor extends. Line refs verified ±20 this session; re-grep symbols. -->

SyncPackagesToPost deep-clone body (AssessmentAdminController.cs ~:6366-6424) — single point; 6 callers route through it (~:6439, :6480, :6565, :7274, :7537, :7630):
```csharp
private async Task SyncPackagesToPost(int preSessionId, int postSessionId) {
    // ... deletes existing Post packages ...
    var prePkgs = await _context.AssessmentPackages.Include(p => p.Questions).ThenInclude(q => q.Options)
        .Where(p => p.AssessmentSessionId == preSessionId).OrderBy(p => p.PackageNumber).ToListAsync();
    foreach (var prePkg in prePkgs) {
        var newPkg = new AssessmentPackage { AssessmentSessionId = postSessionId, PackageName = ..., PackageNumber = ... };
        foreach (var q in prePkg.Questions) {
            var newQ = new PackageQuestion {
                QuestionText = q.QuestionText, Order = q.Order, ScoreValue = q.ScoreValue,
                QuestionType = q.QuestionType, ElemenTeknis = q.ElemenTeknis, Rubrik = q.Rubrik,
                MaxCharacters = q.MaxCharacters, ImagePath = q.ImagePath, ImageAlt = q.ImageAlt,
                Options = q.Options.Select(o => new PackageOption { OptionText=o.OptionText, IsCorrect=o.IsCorrect, ImagePath=o.ImagePath, ImageAlt=o.ImageAlt }).ToList()
            };
            newPkg.Questions.Add(newQ);
        }
        _context.AssessmentPackages.Add(newPkg);
    }
    await _context.SaveChangesAsync();
}
```
NOTE: `q.Options.Select(...)` already clones ALL options regardless of count → opsi 5–6 carried automatically once import stores them. The MISSING piece is Section row clone + SectionId remap.

CMPController.StartExam (~:1064-1102) — insertion point BEFORE BuildQuestionAssignment (~:1074):
```csharp
if (packages.Any()) {
    // <<< INSERT D-13 re-guard here (drift check) >>>
    var assignment = await _context.UserPackageAssignments.FirstOrDefaultAsync(...);   // ~:1064
    if (assignment == null) {
        var shuffledIds = ShuffleEngine.BuildQuestionAssignment(packages, assessment.ShuffleQuestions, workerIndex, rng);  // ~:1074
        ...
    }
}
```
Sibling key (same as import): `s.Title == target.Title && s.Category == target.Category && s.Schedule.Date == target.Schedule.Date`.
Early-block precedent in StartExam: `IsParticipantRemoved` guard at ~:373 (redirect + TempData).

AssessmentPackageSection (from Plan 01): `_context.AssessmentPackageSections`, `(AssessmentPackageId, SectionNumber)` unique; PackageQuestion.SectionId + Section nav.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SyncPackagesToPost — clone Section rows + remap SectionId + opsi 5–6 (SEC-06)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Extend the `SyncPackagesToPost` body (~:6366-6424) so the deep-clone also clones Section structure (Pitfall 7/8, Assumption A2):

1. Load Pre sections: include `AssessmentPackageSections` for the Pre packages — e.g. `var preSections = await _context.AssessmentPackageSections.Where(s => prePkgIds.Contains(s.AssessmentPackageId)).ToListAsync();` (group by AssessmentPackageId).

2. The existing delete-Post step already removes Post packages; ensure orphaned Post `AssessmentPackageSection` rows are also removed (they cascade with their package via FK Cascade from Plan 01 — verify they go away when the Post packages are removed; if EF doesn't cascade in-memory before SaveChanges, explicitly RemoveRange the Post sections to avoid stale rows).

3. For each `prePkg` → `newPkg`: clone its Section rows into new `AssessmentPackageSection` entities attached to `newPkg` (copy `SectionNumber`, `Name`, `StartNewPage`, `ShuffleEnabled`). Build an old→new section map within the package — keyed by `SectionNumber` (stable within a package) or `Dictionary<int oldSectionId, AssessmentPackageSection newSection>`.

4. For each cloned question: set `newQ.Section = sectionMap[q.SectionId.Value]` via the NAVIGATION property (NOT `newQ.SectionId = q.SectionId` — that would point to the Pre package's section = cross-package leak / FK violation, Pitfall 8). When `q.SectionId == null`, leave `newQ.Section = null`. EF assigns the new FK on save once the new Section entities are tracked.

5. Attach the new sections so EF persists them (add to context or wire via `newPkg`/`newQ.Section` navigation — confirm EF tracks them; if `AssessmentPackageSection` has no inverse collection on `AssessmentPackage`, add the section entities to `_context.AssessmentPackageSections` explicitly before SaveChanges, then set `newQ.Section`).

6. Options 5–6: the existing `q.Options.Select(...)` already clones ALL options — confirm (no change needed) that E/F options carry over. Add a code comment noting this.

7. **Audit all sync triggers (Pitfall 7 — VERIFY, do not assume)**: confirm all 6 callers (~:6439, :6480, :6565, :7274, :7537, :7630) route through this single body so they inherit the Section clone. Note the verified caller list in the SUMMARY (do not add per-caller logic — the single-body fix covers all).

Keep within the existing `SaveChangesAsync` (single transaction shape).
  </action>
  <verify>
  <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "AssessmentPackageSection" Controllers/AssessmentAdminController.cs` shows Section clone in SyncPackagesToPost body.
- `grep -n "sectionMap\|newQ.Section =" Controllers/AssessmentAdminController.cs` present (nav-property remap, NOT naive SectionId copy).
- The clone does NOT contain `newQ.SectionId = q.SectionId` (cross-package leak guard) — grep confirms absence.
- `dotnet build HcPortal.csproj` = 0 errors.
  </acceptance_criteria>
  <done>SyncPackagesToPost clones Section rows + remaps SectionId via nav property to the new package's sections + opsi 5–6 carried; single-body fix covers all 6 callers (verified).</done>
</task>

<task type="auto">
  <name>Task 2: StartExam D-13 re-guard before BuildQuestionAssignment (SEC-04 #2)</name>
  <files>Controllers/CMPController.cs</files>
  <action>
Insert a D-13 re-guard in `CMPController.StartExam`, inside `if (packages.Any())` and BEFORE `ShuffleEngine.BuildQuestionAssignment` (~:1074), only on the first-assignment path (assignment == null is fine — but the guard should run before building any new assignment) (Pitfall 6):

1. Resolve sibling packages for this assessment using the SAME key the import uses (`Title+Category+Schedule.Date`). Load their questions with SectionNumber.

2. **Fire ONLY when**: ≥2 sibling packages exist AND at least one has any question with a non-null SectionId (i.e. sections are in use). Otherwise SKIP (legacy all-null → pass; single package → pass).

3. When firing: group each sibling package's questions by SectionNumber and compare counts per SectionNumber. On ANY mismatch → block the exam start: return a redirect/block consistent with the existing `IsParticipantRemoved` early-block pattern (~:373) — `TempData["Error"] = "Ujian tidak dapat dimulai: struktur Section antar-paket tidak identik. Hubungi HC untuk memperbaiki paket soal."` (UI-SPEC copy) + redirect to the assessment list/landing (mirror the IsParticipantRemoved redirect target). Do NOT build the assignment when blocked.

4. Keep the guard cheap (single query for sibling packages + in-memory grouping). Must not break legacy exams (Pitfall 6 — over-broad guard would block existing assessments).

Server-authoritative (not client). Place the guard so a worker who already has a locked `UserPackageAssignment` is unaffected only if appropriate — but the safest interpretation: the guard checks structural integrity before generating a NEW assignment; existing locked assignments proceed (shuffle lock §6.5). If the assignment already exists, the structural drift cannot retroactively corrupt it, so the guard primarily protects first-load. Implement: run the guard on the `assignment == null` (first build) path.
  </action>
  <verify>
  <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "struktur Section antar-paket tidak identik" Controllers/CMPController.cs` present (UI-SPEC re-guard copy).
- The guard sits before the `ShuffleEngine.BuildQuestionAssignment` call (verify position relative to ~:1074).
- The guard uses the sibling key `Title.*Category.*Schedule.Date` (grep context) — same as import.
- Guard fires only when ≥2 sibling packages AND sections in use (legacy/single-pkg skip) — verify the conditional gate.
- `dotnet build HcPortal.csproj` = 0 errors.
  </acceptance_criteria>
  <done>StartExam re-guard blocks exam start on per-Section count drift across sibling packages with the UI-SPEC message; legacy all-null + single-package pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SectionSyncPrePostTests (SEC-06) + SectionMismatchGuardTests (SEC-04)</name>
  <files>HcPortal.Tests/SectionSyncPrePostTests.cs, HcPortal.Tests/SectionMismatchGuardTests.cs</files>
  <behavior>
- SEC-06 sync: after SyncPackagesToPost (or CopyPackagesFromPre), the Post package has cloned AssessmentPackageSection rows (same SectionNumber/Name/toggles), Post questions' SectionId point to the POST package's sections (NOT Pre's), and options 5–6 are carried.
- SEC-04 guard: StartExam (or the extracted guard logic) BLOCKS when two sibling packages have mismatched per-Section counts; PASSES when counts match; PASSES for legacy all-null; PASSES for single package.
  </behavior>
  <action>
Create `HcPortal.Tests/SectionSyncPrePostTests.cs` (Integration, `SectionFixture` — real SQL needed for FK remap + new IDs): seed a Pre session + package with sections + questions (incl. a question with 5–6 options + a null-section "Lainnya" question), run the real `SyncPackagesToPost` (via the controller's `CopyPackagesFromPre` action or the private method through a public caller), reload Post, assert: Post sections exist with matching SectionNumber/Name/toggles, Post questions' `SectionId` resolve to POST sections (assert the section's `AssessmentPackageId == postPackage.Id`), options count preserved (5–6), and no Pre section id leaks into Post questions.

Create `HcPortal.Tests/SectionMismatchGuardTests.cs`: the per-Section count-compare is pure → InMemory where possible for the compare logic; the StartExam path needs Integration (`SectionFixture`) to drive the real controller. Cover: (a) two sibling packages, Section 1 has 3 vs 2 questions → block; (b) matching counts → pass; (c) all-null SectionId both packages → pass (legacy); (d) single package → pass. Drive the REAL StartExam guard (de-tautology — no replica of the compare logic in the test).

Both suites `[Trait("Category","Integration")]` where they touch real SQL.
  </action>
  <verify>
  <automated>cd "$(git rev-parse --show-toplevel)" && dotnet test --filter "FullyQualifiedName~SectionSyncPrePost|FullyQualifiedName~SectionMismatchGuard" 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- `HcPortal.Tests/SectionSyncPrePostTests.cs` exists; asserts Post SectionId resolves to POST package sections (not Pre) + opsi 5–6.
- `HcPortal.Tests/SectionMismatchGuardTests.cs` exists; covers block + match-pass + legacy-pass + single-pkg-pass.
- Tests drive real SyncPackagesToPost + real StartExam guard (no logic replica).
- `dotnet test --filter "FullyQualifiedName~SectionSyncPrePost|FullyQualifiedName~SectionMismatchGuard"` = all Passed.
- FULL suite `dotnet test` green; `~Shuffle` stays green (backward-compat keystone).
  </acceptance_criteria>
  <done>Sync deep-clone remap (SEC-06) + StartExam re-guard (SEC-04) proven via real code against real SQL; legacy/single-pkg pass; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre→Post clone | SectionId must remap to new package; cross-package FK leak risk |
| Worker → StartExam | Untrusted exam-start; structural drift must not corrupt assignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-415-15 | Tampering / data integrity | SyncPackagesToPost SectionId remap | mitigate | Remap via nav property + old→new section map (never naive copy); cloned Post questions point only to Post sections. Test asserts no Pre-id leak. |
| T-415-16 | Tampering / DoS | StartExam re-guard over-broad | mitigate | Guard fires only when ≥2 sibling packages AND sections in use; legacy all-null + single-pkg skip → existing exams unaffected (Pitfall 6). |
| T-415-17 | Repudiation / integrity | Exam starts on drifted structure | mitigate | Server-authoritative re-guard (D-13 #2) blocks start before BuildQuestionAssignment with clear admin message. |
| T-415-18 | Information Disclosure | Stale Post sections after re-sync | mitigate | Remove orphaned Post AssessmentPackageSection rows on re-sync (FK Cascade + explicit RemoveRange if needed). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` = 0 errors.
- `dotnet test --filter "FullyQualifiedName~SectionSyncPrePost|FullyQualifiedName~SectionMismatchGuard"` all green.
- FULL `dotnet test` green; `~Shuffle` green (backward-compat keystone invariant).
- `dotnet run` @5277: create Pre with sections → CopyPackagesFromPre → Post mirrors sections; create mismatched sibling packages → StartExam blocked with the re-guard message; legacy single-package exam starts normally.
</verification>

<success_criteria>
- Sync Pre→Post menyalin record Section + remap SectionId ke section paket Post + opsi 5–6 (semua pemicu sync mewarisi via body tunggal).
- StartExam memblok mulai ujian saat struktur Section antar-paket tidak identik (D-13 #2), pesan jelas.
- Legacy (semua null) + 1-paket TIDAK terblok (kompatibel-mundur).
- Full suite hijau; ~Shuffle hijau.
- migration=FALSE — 9/9 REQ Phase 415 tertutup.
</success_criteria>

<output>
After completion, create `.planning/phases/415-section-foundation-import-excel-diperluas/415-04-SUMMARY.md`. Record: the verified list of SyncPackagesToPost callers (Pitfall 7), the remap mechanism used, the StartExam guard placement + skip conditions, and confirm migration=FALSE + that all 9 REQ (SEC-01..06, IMP-01..03) are now delivered across Plans 01-04.
</output>
