---
phase: 415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/PostLisensorPolishTests.cs
autonomous: true
requirements: [BUGFIX-415.1]
migration: false

must_haves:
  truths:
    - "HC dapat menyimpan nilai essay untuk peserta yang questionId-nya ADA di assignment-nya, meski paket-soal dimiliki sesi sibling lain (cross-package) → SUCCESS, tidak ditolak 'Soal bukan milik sesi ini.'"
    - "Simpan nilai DITOLAK 'Soal bukan milik sesi ini.' bila assignment peserta ADA tapi questionId BUKAN bagiannya (genuine tamper)"
    - "Assignment null (degenerate/non-paket) → IZINKAN simpan (andalkan guard tipe-Essay + status PendingGrading + range) per D-02"
    - "Guard lama tetap utuh: WR-01 tipe-Essay, status-guard PendingGrading, range 0..ScoreValue, upsert essay-kosong"
    - "RBAC [Authorize(Roles=\"Admin, HC\")] + [ValidateAntiForgeryToken] pada SubmitEssayScore tetap ada"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Predikat WR-02 baru di SubmitEssayScore: cek questionId ∈ UserPackageAssignment.GetShuffledQuestionIds()"
      contains: "GetShuffledQuestionIds().Contains(questionId)"
    - path: "HcPortal.Tests/PostLisensorPolishTests.cs"
      provides: "Replica predikat baru + test cross-package SUCCESS + test null-allow + test genuine-tamper reject"
      contains: "CrossPackageAssignedQuestion_Succeeds"
  key_links:
    - from: "SubmitEssayScore (POST predikat)"
      to: "UserPackageAssignment.GetShuffledQuestionIds()"
      via: "load UPA by sessionId lalu .Contains(questionId) in-memory"
      pattern: "UserPackageAssignments[\\s\\S]*GetShuffledQuestionIds\\(\\)\\.Contains\\(questionId\\)"
    - from: "RunSubmitEssayScoreLogicAsync (test replica)"
      to: "predikat controller baru"
      via: "byte-identik mirror"
      pattern: "GetShuffledQuestionIds\\(\\)\\.Contains\\(questionId\\)"
---

<objective>
Ganti guard ownership WR-02 di `SubmitEssayScore` (`Controllers/AssessmentAdminController.cs:4210-4215`) yang false-positive menolak penilaian essay dengan alert "Soal bukan milik sesi ini." untuk hampir semua peserta. Penyebab: guard memaksa paket-soal dimiliki sesi yang dinilai (`q.AssessmentPackage.AssessmentSessionId == sessionId`), padahal SEMUA jalur build assignment mem-POOL paket lintas sesi-sibling → worker rutin dapat soal dari paket milik sesi induk lain. Predikat baru = cek `questionId ∈ UserPackageAssignment.GetShuffledQuestionIds()` milik `sessionId` (sumber-kebenaran SAMA dengan GET `EssayGrading` loader `:4131-4143`).

Purpose: HC/Admin kembali bisa menilai essay untuk SEMUA peserta (termasuk data lama Dev status "Menunggu Penilaian"), tanpa migration, tanpa membuka celah tamper.
Output: Predikat WR-02 baru (+ helper privat opsional) di controller; replica test diselaraskan; 2 test baru (cross-package SUCCESS + null-allow); existing test cross-session di-update agar tetap reject genuine tamper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-CONTEXT.md
@.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-RESEARCH.md
@.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang executor butuhkan — diekstrak dari source. Pakai langsung, jangan eksplorasi ulang. -->

Dari Models/UserPackageAssignment.cs:
```csharp
public class UserPackageAssignment
{
    public int Id { get; set; }
    public int AssessmentSessionId { get; set; }        // FK — satu UPA per sesi
    public int AssessmentPackageId { get; set; }
    public string UserId { get; set; }
    public string ShuffledQuestionIds { get; set; } = "[]";   // JSON "[42,17,33,...]"
    public bool IsCompleted { get; set; }
    public List<int> GetShuffledQuestionIds();   // SELALU non-null (catch → new List<int>()); deserialisasi in-memory
}
```

Blok WR-02 LAMA yang DIGANTI (Controllers/AssessmentAdminController.cs:4210-4215):
```csharp
// 2c. WR-02 (386-REVIEW) — questionId WAJIB milik sesi ini (cross-session tampering guard). Rantai nav
//     terverifikasi: PackageQuestion.AssessmentPackage.AssessmentSessionId == sessionId.
var ownsQuestion = await _context.PackageQuestions
    .AnyAsync(q => q.Id == questionId && q.AssessmentPackage.AssessmentSessionId == sessionId);
if (!ownsQuestion)
    return Json(new { success = false, message = "Soal bukan milik sesi ini." });
```

Sumber-kebenaran (GET EssayGrading loader, Controllers/AssessmentAdminController.cs:4131-4136 — JANGAN diubah, hanya dicermin):
```csharp
var assignment = await _context.UserPackageAssignments
    .FirstOrDefaultAsync(a => a.AssessmentSessionId == session.Id);
...
var shuffled = assignment.GetShuffledQuestionIds();
```

Guard yang DIPERTAHANKAN di SubmitEssayScore (urutan saat ini):
- :4189-4191 session null → "Session tidak ditemukan"
- :4192-4193 status != PendingGrading → "Penilaian hanya bisa dilakukan saat status Menunggu Penilaian." (KEEP)
- :4196-4198 question null → "Soal tidak ditemukan"
- :4199-4200 range 0..ScoreValue → "Skor harus antara 0 dan {N}" (KEEP)
- :4208-4209 WR-01 tipe-Essay → "Soal ini bukan tipe Essay." (KEEP)
- :4210-4215 WR-02 ownership → GANTI (target plan ini)
- :4217-4237 UPSERT EssayScore → KEEP (essay-kosong 386 D-08 tetap jalan)
</interfaces>

<test_replica>
<!-- HcPortal.Tests/PostLisensorPolishTests.cs — pola logic-replica real-SQL (BUKAN panggil controller). -->
Replica WR-02 LAMA (L143-147) yang DIGANTI:
```csharp
// 2c. WR-02 — questionId WAJIB milik sesi ini (387-01)
var ownsQuestion = await ctx.PackageQuestions
    .AnyAsync(q => q.Id == questionId && q.AssessmentPackage.AssessmentSessionId == sessionId);
if (!ownsQuestion)
    return new(false, "Soal bukan milik sesi ini.");
```

Existing test cross-session (L189-207) `SubmitEssayScore_CrossSessionQuestion_RejectedAndNoScoreWritten` — SAAT INI tak seed UPA apa pun → di predikat baru + D-02 akan SALAH lolos. WAJIB di-update (seed UPA sessionA TANPA questionId).

Helper seed yang tersedia (reuse apa adanya):
- `SeedUserAsync(ctx) → userId`
- `SeedSessionAsync(ctx, userId, status) → (sessionId, packageId)` — status pakai `S.PendingGrading` / `S.Completed`
- `SeedQuestionAsync(ctx, packageId, type, scoreValue, order, text) → questionId`
- UPA di-seed manual: `ctx.UserPackageAssignments.Add(new UserPackageAssignment { AssessmentSessionId=..., AssessmentPackageId=..., UserId=..., ShuffledQuestionIds = "[" + qId + "]" })`
</test_replica>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ganti predikat WR-02 di SubmitEssayScore (assignment-based ownership)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs (BACA method SubmitEssayScore L4180-4251 PENUH + GET EssayGrading loader L4115-4178 sebagai referensi byte-identik sumber-kebenaran)
    - Models/UserPackageAssignment.cs (kontrak GetShuffledQuestionIds — non-null, deserialisasi in-memory)
    - .planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-RESEARCH.md (§1 + §2 predikat baru + PITFALL-2)
  </read_first>
  <behavior>
    - Cross-package: assignment ADA + GetShuffledQuestionIds() MEMUAT questionId → LOLOS guard (lanjut ke upsert), meski paket-soal dimiliki sesi sibling lain.
    - Genuine tamper: assignment ADA tapi GetShuffledQuestionIds() TIDAK memuat questionId → reject "Soal bukan milik sesi ini." (return Json success=false).
    - Null assignment (D-02): tidak ada UPA untuk sessionId → IZINKAN (jangan reject; andalkan guard tipe-Essay + status + range).
    - Guard WR-01 tipe-Essay (L4208), status PendingGrading (L4192), range (L4199), upsert (L4217-4237) TIDAK berubah perilakunya.
  </behavior>
  <action>
Di `Controllers/AssessmentAdminController.cs`, GANTI blok WR-02 lama (L4210-4215, lihat <interfaces>) dengan predikat baru. Sisipkan TEPAT setelah guard WR-01 tipe-Essay (L4208-4209), SEBELUM upsert (L4217). Teks pengganti EXACT (boleh sesuaikan komentar, kode predikat WAJIB identik makna):

```csharp
// 2c. WR-02 (FIXED 415.1) — questionId WAJIB bagian assignment peserta INI (bukan ownership paket).
//     Paket di-pool lintas sesi-sibling (by design: CreateEagerAssignmentsAsync/StartExam/Reshuffle),
//     jadi ownership-by-package (lama) salah → false-positive. Sumber-kebenaran = UserPackageAssignment
//     .GetShuffledQuestionIds() — IDENTIK dengan GET EssayGrading loader (L4131-4143).
//     D-02: assignment null (degenerate/non-paket) → IZINKAN (andalkan guard tipe-Essay + status + range);
//     peserta yang benar mengerjakan PASTI punya UPA. HC adalah role tepercaya (Admin/HC).
var assignment = await _context.UserPackageAssignments
    .FirstOrDefaultAsync(a => a.AssessmentSessionId == sessionId);
if (assignment != null && !assignment.GetShuffledQuestionIds().Contains(questionId))
    return Json(new { success = false, message = "Soal bukan milik sesi ini." });
```

WAJIB: load `assignment` DULU lalu evaluasi `.Contains(questionId)` IN-MEMORY (PITFALL-2: jangan paksa `GetShuffledQuestionIds().Contains()` masuk ekspresi LINQ-to-SQL — akan throw translation exception).

OPSIONAL (Claude's Discretion, hanya bila lebih bersih): ekstrak helper privat
`private async Task<bool> SessionAssignmentContainsQuestion(int sessionId, int questionId)` yang return `true` saat assignment null, lalu panggil `if (!await SessionAssignmentContainsQuestion(sessionId, questionId)) return Json(...)`. Bila diekstrak, helper letakkan dekat SubmitEssayScore.

JANGAN ubah: RBAC attribute `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]`, guard WR-01/status/range, blok upsert, GET loader, jalur build assignment (`CreateEagerAssignmentsAsync`/StartExam/Reshuffle). NO migration, NO schema, NO Views.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GetShuffledQuestionIds().Contains(questionId)" Controllers/AssessmentAdminController.cs` → ≥ 1
    - `grep -c "q.AssessmentPackage.AssessmentSessionId == sessionId" Controllers/AssessmentAdminController.cs` → predikat ownership lama HILANG dari SubmitEssayScore (boleh tetap muncul di tempat lain yang sah; pastikan tidak lagi di blok WR-02 SubmitEssayScore)
    - `grep -c "Soal bukan milik sesi ini." Controllers/AssessmentAdminController.cs` → tetap 1 (dipakai kasus genuine tamper)
    - `grep -c "\[Authorize(Roles = \"Admin, HC\")\]" Controllers/AssessmentAdminController.cs` mencakup SubmitEssayScore (attribute tetap) + `grep "ValidateAntiForgeryToken" Controllers/AssessmentAdminController.cs` masih ada di atas SubmitEssayScore
    - `dotnet build HcPortal.csproj` → "Build succeeded", 0 error
  </acceptance_criteria>
  <done>Predikat WR-02 SubmitEssayScore mengecek questionId ∈ UPA.GetShuffledQuestionIds() (null→allow); build 0 error; guard lain + RBAC + antiforgery utuh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Selaraskan replica + update existing test + tambah 2 test baru</name>
  <read_first>
    - HcPortal.Tests/PostLisensorPolishTests.cs (BACA PENUH — pola fixture real-SQL `HcPortalDB_Test_{guid}` @SQLEXPRESS, helper Seed*, replica RunSubmitEssayScoreLogicAsync L123-167, existing test L189-207)
    - Controllers/AssessmentAdminController.cs (predikat baru Task 1 — replica WAJIB byte-identik)
    - .planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-RESEARCH.md (§4 daftar update + test baru, PITFALL-1)
  </read_first>
  <behavior>
    - Test 2a `SubmitEssayScore_CrossSessionQuestion_RejectedAndNoScoreWritten` (UPDATED): UPA sessionA ADA tapi ShuffledQuestionIds TIDAK memuat essayB → reject "Soal bukan milik sesi ini.", 0 baris EssayScore tertulis.
    - Test 2b `SubmitEssayScore_CrossPackageAssignedQuestion_Succeeds` (BARU, BUKTI FIX): paket P dibuat di sesi induk; sessionB (worker lain, PendingGrading) punya UPA yang ShuffledQuestionIds MEMUAT essayQ milik paket P → SUCCESS + baris EssayScore=5 tertulis.
    - Test 2c `SubmitEssayScore_NullAssignment_Allowed` (BARU, D-02): sessionA PendingGrading + essayQ milik paketnya, TANPA UPA → SUCCESS.
    - Regresi: `NonEssayQuestion`, `CompletedSession`, `PendingGradingValidEssay` tetap hijau.
  </behavior>
  <action>
Di `HcPortal.Tests/PostLisensorPolishTests.cs`:

**(a) Update replica `RunSubmitEssayScoreLogicAsync` (L143-147)** — ganti blok WR-02 lama:
```csharp
// 2c. WR-02 (FIXED 415.1) — questionId WAJIB bagian assignment peserta INI; null→allow (D-02).
var assignment = await ctx.UserPackageAssignments
    .FirstOrDefaultAsync(a => a.AssessmentSessionId == sessionId);
if (assignment != null && !assignment.GetShuffledQuestionIds().Contains(questionId))
    return new(false, "Soal bukan milik sesi ini.");
```
Predikat WAJIB byte-identik dengan controller Task 1 (replica-validity).

**(b) Update existing `SubmitEssayScore_CrossSessionQuestion_RejectedAndNoScoreWritten` (L189-207)** — SAAT INI tak seed UPA → di logika baru akan SALAH lolos (PITFALL-1). Tambahkan seed UPA untuk sessionA yang ShuffledQuestionIds TIDAK memuat essayB (mis. memuat id soal lain / kosong `"[]"`):
```csharp
// 415.1: seed UPA sessionA TANPA essayB → genuine tamper tetap ditolak (bukan jatuh ke cabang null-allow).
ctx.UserPackageAssignments.Add(new UserPackageAssignment
{ AssessmentSessionId = sessionA, AssessmentPackageId = pkgA, UserId = userId, ShuffledQuestionIds = "[]" });
await ctx.SaveChangesAsync();
```
Assertion tetap: `Assert.False(result.Success)` + `Assert.Equal("Soal bukan milik sesi ini.", result.Message)` + verify 0 baris.

**(c) Tambah `SubmitEssayScore_CrossPackageAssignedQuestion_Succeeds`** (bukti fix):
```csharp
[Fact]
public async Task SubmitEssayScore_CrossPackageAssignedQuestion_Succeeds()  // 415.1 BUKTI FIX
{
    await using var ctx = NewCtx();
    var userId = await SeedUserAsync(ctx);
    // Sesi INDUK (S1) memiliki paket P + essayQ.
    var (parentSession, parentPkg) = await SeedSessionAsync(ctx, userId, S.PendingGrading);
    var essayQ = await SeedQuestionAsync(ctx, parentPkg, "Essay", 10, 1, "Essay milik paket induk.");
    // Sesi worker LAIN (S2) — paket P dipool ke S2 (UPA S2.ShuffledQuestionIds MEMUAT essayQ).
    var (workerSession, _) = await SeedSessionAsync(ctx, userId, S.PendingGrading);
    ctx.UserPackageAssignments.Add(new UserPackageAssignment
    { AssessmentSessionId = workerSession, AssessmentPackageId = parentPkg, UserId = userId,
      ShuffledQuestionIds = "[" + essayQ + "]" });
    await ctx.SaveChangesAsync();

    var result = await RunSubmitEssayScoreLogicAsync(ctx, workerSession, essayQ, 5);

    Assert.True(result.Success);  // sebelum fix: reject "Soal bukan milik sesi ini."
    await using var verify = NewCtx();
    var row = await verify.PackageUserResponses
        .FirstAsync(r => r.AssessmentSessionId == workerSession && r.PackageQuestionId == essayQ);
    Assert.Equal(5, row.EssayScore);  // baris EssayScore tertulis untuk soal cross-package
}
```

**(d) Tambah `SubmitEssayScore_NullAssignment_Allowed`** (D-02):
```csharp
[Fact]
public async Task SubmitEssayScore_NullAssignment_Allowed()  // 415.1 D-02
{
    await using var ctx = NewCtx();
    var userId = await SeedUserAsync(ctx);
    var (sessionId, pkgId) = await SeedSessionAsync(ctx, userId, S.PendingGrading);
    var essayQ = await SeedQuestionAsync(ctx, pkgId, "Essay", 10, 1, "Essay tanpa UPA.");
    // TANPA UserPackageAssignment untuk sessionId (kasus degenerate).
    var result = await RunSubmitEssayScoreLogicAsync(ctx, sessionId, essayQ, 7);

    Assert.True(result.Success);  // null assignment → diizinkan (andalkan guard sisa)
    await using var verify = NewCtx();
    var row = await verify.PackageUserResponses
        .FirstAsync(r => r.AssessmentSessionId == sessionId && r.PackageQuestionId == essayQ);
    Assert.Equal(7, row.EssayScore);
}
```

JANGAN ubah test PXF-09 (EssayCell*), PXF-12 (McUpsert*), 999.11 (EditGuard*) — di luar scope.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests --filter "FullyQualifiedName~PostLisensorPolishTests" 2>&1 | grep -E "Passed!|Failed!|Passed:|Failed:|error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GetShuffledQuestionIds().Contains(questionId)" HcPortal.Tests/PostLisensorPolishTests.cs` → ≥ 1 (replica diselaraskan)
    - `grep -c "CrossPackageAssignedQuestion_Succeeds" HcPortal.Tests/PostLisensorPolishTests.cs` → 1
    - `grep -c "NullAssignment_Allowed" HcPortal.Tests/PostLisensorPolishTests.cs` → 1
    - `grep -c "AssessmentSessionId = sessionA" HcPortal.Tests/PostLisensorPolishTests.cs` → ≥ 1 (UPA di-seed di test cross-session yang di-update)
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~PostLisensorPolishTests"` → exit 0, semua Passed (termasuk CrossPackageAssignedQuestion_Succeeds, NullAssignment_Allowed, CrossSessionQuestion reject, NonEssay/Completed/PendingGradingValidEssay regresi)
    - `dotnet test --filter Name~CrossPackageAssignedQuestion_Succeeds` → exit 0
    - `dotnet test --filter Name~NullAssignment_Allowed` → exit 0
  </acceptance_criteria>
  <done>Replica byte-identik predikat baru; existing cross-session test seed UPA→tetap reject; 2 test baru hijau (cross-package SUCCESS + null-allow); seluruh PostLisensorPolishTests Passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Admin/HC) → POST /Admin/SubmitEssayScore | Admin/HC ter-otentikasi (RBAC), tapi sessionId/questionId/score = input HTTP yang dikontrol klien → boleh ditampering. |
| Controller → DB (UserPackageAssignments / PackageQuestions / PackageUserResponses) | Predikat guard menentukan baris EssayScore mana yang boleh ditulis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-415.1-01 | Tampering | SubmitEssayScore — HC mengirim questionId yang TIDAK di-assign ke peserta target | mitigate | Predikat baru: `assignment != null && !assignment.GetShuffledQuestionIds().Contains(questionId)` → reject "Soal bukan milik sesi ini.". Test `CrossSessionQuestion` (UPA tanpa questionId) mengunci. Sumber-kebenaran SAMA dengan GET loader → tak bisa nilai soal yang tak pernah dilihat peserta. |
| T-415.1-02 | Elevation/Integrity | Relaksasi cabang null-assignment (D-02) — soal di-skor walau UPA hilang | accept | Risiko kecil: Admin/HC = role tepercaya (RBAC `[Authorize(Roles="Admin, HC")]`). Guard sisa tetap berlaku: WR-01 tipe-Essay + status PendingGrading + range 0..ScoreValue. Null hanya muncul pada data degenerate/non-paket di mana tak ada essay gradable realistis. Memblokir = dead-end baru bagi HC. Diterima (rationale terdokumentasi CONTEXT D-02). |
| T-415.1-03 | Tampering | Edit skor pasca-finalize (Completed + sertifikat terbit) | mitigate (unchanged) | Status-guard L4192 (`Status != PendingGrading` → reject) TIDAK diubah. Test `CompletedSession_RejectedByStatusGuard` regresi tetap hijau. By design anti-ubah-nilai pasca-cert. |
| T-415.1-04 | Tampering/Integrity | Upsert EssayScore pada soal MC/MA (korupsi agregasi skor) | mitigate (unchanged) | Guard WR-01 tipe-Essay L4208 (`QuestionType != "Essay"` → reject) TIDAK diubah. Test `NonEssayQuestion` regresi tetap hijau. |
| T-415.1-05 | Spoofing/Repudiation/CSRF | Request lintas-situs / non-Admin memanggil endpoint | mitigate (unchanged) | `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` TIDAK diubah; acceptance_criteria Task 1 memverifikasi keduanya tetap ada. |
| T-415.1-06 | DoS | `GetShuffledQuestionIds().Contains()` dipaksa ke LINQ-to-SQL → exception | mitigate | Predikat load UPA dulu lalu evaluasi in-memory (PITFALL-2). Acceptance_criteria + build gate mengunci. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~PostLisensorPolishTests"` → semua Passed (3 baru/update + regresi).
- `grep "GetShuffledQuestionIds().Contains(questionId)"` ditemukan di controller + test replica (byte-identik).
- RBAC + antiforgery pada SubmitEssayScore tidak berubah (grep).
- TIDAK ada file di `Migrations/` atau `Data/` yang berubah (migration=FALSE).
</verification>

<success_criteria>
- Predikat WR-02 SubmitEssayScore mengecek `questionId ∈ UserPackageAssignment.GetShuffledQuestionIds()` (null→allow per D-02).
- Test cross-package multi-worker SUCCESS (bukti fix) + genuine-tamper reject + null-allow + regresi guard lama semua hijau.
- Build + suite PostLisensorPolishTests hijau. migration=FALSE (0 file Migrations/Data berubah).
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-01-SUMMARY.md`.
Catat: commit hash, daftar test (cross-package/null/cross-session), konfirmasi build 0 error + suite hijau, konfirmasi 0 migration. Catat bahwa branch deploy hotfix (D-01) ditangani di Plan 02.
</output>
