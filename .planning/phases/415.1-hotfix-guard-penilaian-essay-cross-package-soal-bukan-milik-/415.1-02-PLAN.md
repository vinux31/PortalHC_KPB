---
phase: 415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik
plan: 02
type: execute
wave: 2
depends_on: ["415.1-01"]
files_modified:
  - tests/e2e/essay-grading-ownership-415.1.spec.ts
  - tests/sql/essay-grading-ownership-415.1-seed.sql
  - docs/SEED_JOURNAL.md
autonomous: false
requirements: [BUGFIX-415.1]
migration: false

must_haves:
  truths:
    - "Playwright real-browser: HC buka /Admin/EssayGrading untuk peserta paket-pinjam (cross-package), isi skor, klik Simpan → badge 'Sudah Dinilai', TIDAK ada dialog/alert 'Soal bukan milik sesi ini.'"
    - "Skenario Pre/Post SamePackage (Post pakai paket clone) → worker Post dinilai → SUCCESS"
    - "Seed temporary multi-worker dibuat via Seed Data Workflow (snapshot→seed→restore→SEED_JOURNAL cleaned)"
    - "Branch hotfix terpisah dibuat dari origin/main (v31.0, 4b4a09b5) berisi HANYA commit kode/test non-.planning — bukan bundle main, migration=FALSE"
  artifacts:
    - path: "tests/e2e/essay-grading-ownership-415.1.spec.ts"
      provides: "e2e bukti fix: cross-package save SUCCESS no-dialog + Pre/Post paritas"
      contains: "page.on('dialog'"
    - path: "tests/sql/essay-grading-ownership-415.1-seed.sql"
      provides: "Seed 2-worker (paket di sesi induk, UPA pooled) + sesi Post clone"
      contains: "UserPackageAssignments"
  key_links:
    - from: "spec essay-grading-ownership-415.1"
      to: "POST /Admin/SubmitEssayScore (controller asli)"
      via: "klik .btn-save-essay-score lalu assert badge + no dialog"
      pattern: "btn-save-essay-score"
    - from: "branch hotfix"
      to: "origin/main (4b4a09b5)"
      via: "cherry-pick commit kode/test (non-.planning)"
      pattern: "origin/main"
---

<objective>
Buktikan fix Plan 01 berfungsi runtime di real-browser (lesson 354: symptom = `alert()` native dari response JSON `success=false`), lalu kemas fix sebagai branch hotfix terpisah dari `origin/main` (v31.0) siap deploy IT.

Plan ini mengeksekusi kedalaman verifikasi LENGKAP (D-03): repro live 2-worker cross-package @5277 + skenario Pre/Post SamePackage + Playwright spec dengan `page.on('dialog')`. Delivery branch (D-01) = tugas manual (`autonomous:false`) — GSD TIDAK push; developer push branch hotfix + notify IT.

Purpose: Kepercayaan controller-level (replica xUnit Plan 01 tak exercise controller asli) + paket siap promosi Dev tanpa membawa v32.x.
Output: spec Playwright baru + SQL seed temporary + entry SEED_JOURNAL (cleaned) + branch hotfix dari origin/main (di-push manual saat checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-CONTEXT.md
@.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-RESEARCH.md
@.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-VALIDATION.md
@CLAUDE.md
@tests/e2e/essay-grading-384.spec.ts

<interfaces>
<!-- Selector + endpoint front-end (terverifikasi wwwroot/js/essay-grading.js). Pakai langsung. -->
Page penilaian essay: GET /Admin/EssayGrading?sessionId={S}&title={..}&category={..}&scheduleDate={yyyy-MM-dd}
Selectors:
- .essay-grading-card        — kartu per soal essay
- .essay-score-input         — input skor
- .btn-save-essay-score      — tombol Simpan (atribut data-session-id, data-question-id)
- #badge_{sessionId}_{questionId} — badge status; sukses then textContent "Sudah Dinilai" + class bg-success
Front-end flow (essay-grading.js): klik then POST /Admin/SubmitEssayScore then `if (data.success) { badge "Sudah Dinilai" } else { alert(data.message) }`.
BUG (pra-fix) = `alert("Soal bukan milik sesi ini.")`. PASCA-fix = badge hijau, NO alert.

Pola e2e (dari essay-grading-384.spec.ts — REUSE apa adanya):
- import * as db from '../helpers/dbSnapshot';   (db.backup/restore/execScript/queryScalar/queryString)
- import { accounts, AccountKey } from '../helpers/accounts';
- loginAny(page, 'admin') — fill input[name=email]/[name=password] + submit, waitForURL keluar /Account/Login
- beforeAll: resolve InstanceDefaultBackupPath then db.backup then db.execScript(seed.sql) then Layer 1 assert seeded then resolve sessionId/questionId/title/category/scheduleDate dari DB
- afterAll: db.restore + unlink .bak + Layer 4 assert COUNT prefix = 0
- WAJIB --workers=1 (NTLM loopback / shared-memory SQL conn)
</interfaces>

<seed_reference>
<!-- Pola SQL seed dari tests/sql/essay-grading-384-seed.sql. Prefix Title unik: '[ESSAYOWN415]'. -->
Struktur minimal repro cross-package (RESEARCH §5):
- 1 grup sibling (Title+Category+Schedule.Date+Type sama), 2 sesi worker: induk S1 + worker S2.
- Paket P (AssessmentPackages.AssessmentSessionId = S1) dengan >=1 PackageQuestion Essay (essayQ).
- UPA untuk S1 DAN S2: keduanya ShuffledQuestionIds memuat essayQ (pooled lintas-sibling).
- S2.Status = PendingGrading + ada PackageUserResponse essay (TextAnswer terisi atau kosong).
Skenario Pre/Post (RESEARCH §6): paket clone milik sesi induk-Post; UPA worker-Post memuat questionId paket clone then predikat baru menangani identik. Cukup 1 skenario UAT konfirmasi (boleh sesi terpisah prefix '[ESSAYOWN415-PP]').
</seed_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tulis SQL seed temporary 2-worker cross-package + Pre/Post + catat SEED_JOURNAL</name>
  <files>tests/sql/essay-grading-ownership-415.1-seed.sql, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/sql/essay-grading-384-seed.sql (pola INSERT chain idempotent WIPE-AND-INSERT + prefix Title + THROW guard)
    - tests/e2e/essay-grading-384.spec.ts (cara execScript + Layer 1 assert dipakai)
    - docs/SEED_WORKFLOW.md (klasifikasi + format journal)
    - docs/SEED_JOURNAL.md (format baris tabel — tambah 1 baris status active lalu cleaned)
    - .planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-RESEARCH.md (§5 + §6)
  </read_first>
  <action>
Buat `tests/sql/essay-grading-ownership-415.1-seed.sql` (idempotent WIPE-AND-INSERT, pola essay-grading-384-seed.sql), prefix Title `[ESSAYOWN415]`:
1. 1 user worker (reuse user existing by NIP rino.prasetyo / iwan3 ATAU buat user seed — ikuti pola 384 seed).
2. Grup sibling 2 sesi: S1 induk + S2 worker (Title='[ESSAYOWN415] Cross Package', Category + Schedule sama, Status=PendingGrading).
3. AssessmentPackage P dengan AssessmentSessionId = S1; >=1 PackageQuestion QuestionType='Essay' (essayQ) + >=1 MC pelengkap.
4. UserPackageAssignment untuk S1 DAN S2 — keduanya ShuffledQuestionIds memuat essayQ (pooled).
5. PackageUserResponse essay untuk S2 (TextAnswer terisi, EssayScore NULL) then muncul sebagai pending di /Admin/EssayGrading.
6. (Pre/Post paritas) sesi Post '[ESSAYOWN415-PP]' dengan paket CLONE (AssessmentSessionId = induk-Post) + UPA worker-Post memuat questionId clone + response essay pending. Cukup untuk 1 skenario konfirmasi.

Sertakan THROW guard (mis. `THROW 51415, '...', 1`) + komentar header tujuan/klasifikasi.

Tambahkan 1 baris ke `docs/SEED_JOURNAL.md` (status `active`): Tanggal 2026-06-23, Phase `415.1 (e2e cross-package essay ownership)`, Klasifikasi `temporary + local-only`, Tujuan (repro 2-worker paket-pinjam + Pre/Post SamePackage), Dampak (AssessmentSessions/Packages/Questions/Options/UserPackageAssignments/PackageUserResponses +N prefix [ESSAYOWN415]; semua revert via RESTORE; Data/SeedData.cs tak tersentuh), Snapshot file (runtime-resolved InstanceDefaultBackupPath), Status `active`.

JANGAN sentuh `Data/SeedData.cs` (seed temporary, BUKAN permanent).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && test -f tests/sql/essay-grading-ownership-415.1-seed.sql && grep -c "ESSAYOWN415" tests/sql/essay-grading-ownership-415.1-seed.sql && grep -c "ESSAYOWN415" docs/SEED_JOURNAL.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/sql/essay-grading-ownership-415.1-seed.sql` → file ada
    - `grep -c "ESSAYOWN415" tests/sql/essay-grading-ownership-415.1-seed.sql` → ≥ 1
    - `grep -c "UserPackageAssignments" tests/sql/essay-grading-ownership-415.1-seed.sql` → ≥ 1 (UPA pooled di-seed untuk S1 DAN S2)
    - `grep -c "ESSAYOWN415-PP" tests/sql/essay-grading-ownership-415.1-seed.sql` → ≥ 1 (skenario Pre/Post)
    - `grep -c "ESSAYOWN415" docs/SEED_JOURNAL.md` → ≥ 1 (entry journal baru)
    - `git diff --name-only` TIDAK memuat `Data/SeedData.cs`
  </acceptance_criteria>
  <done>SQL seed 2-worker cross-package + Pre/Post clone dibuat (idempotent, prefix [ESSAYOWN415]); SEED_JOURNAL ditambah baris status active; Data/SeedData.cs tak tersentuh.</done>
</task>

<task type="auto">
  <name>Task 2: Tulis Playwright spec essay-grading-ownership-415.1 (lifecycle snapshot/restore + dialog guard)</name>
  <files>tests/e2e/essay-grading-ownership-415.1.spec.ts</files>
  <read_first>
    - tests/e2e/essay-grading-384.spec.ts (BACA PENUH — pola loginAny, monitoringUrl/essayGradingUrl, beforeAll backup+seed+resolve, afterAll restore+Layer4, selector SEL)
    - wwwroot/js/essay-grading.js (alur klik then POST then badge/alert — sumber selector + symptom)
    - tests/sql/essay-grading-ownership-415.1-seed.sql (prefix Title untuk resolve sessionId/questionId)
    - .planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-RESEARCH.md (§5 PITFALL-4 dialog handler)
  </read_first>
  <action>
Buat `tests/e2e/essay-grading-ownership-415.1.spec.ts` (analog `essay-grading-384.spec.ts`, mode serial):

- beforeAll: resolve InstanceDefaultBackupPath then `db.backup(snapshotPath)` then `db.execScript(.../essay-grading-ownership-415.1-seed.sql)` then Layer 1 assert COUNT prefix `[ESSAYOWN415]` > 0 then resolve dari DB: `workerSessionId` (sesi S2 worker, BUKAN induk S1), `questionId` (essay milik paket induk S1, tapi ADA di UPA S2), title/category/scheduleDate. Untuk Pre/Post: resolve `postSessionId` + `postQuestionId` prefix `[ESSAYOWN415-PP]`.
- afterAll: `db.restore(snapshotPath)` + unlink .bak + Layer 4 assert COUNT prefix `[ESSAYOWN415]` = 0 (tangkap restoreError independen, pola 384).

Test A (BUKTI FIX — cross-package, controller asli):
```
test('415.1 cross-package essay save → SUCCESS, no alert', async ({ page }) => {
  const dialogs: string[] = [];
  page.on('dialog', d => { dialogs.push(d.message()); d.dismiss(); });  // PITFALL-4: WAJIB, alert native gantung test
  await loginAny(page, 'admin');
  await page.goto(essayGradingUrl(workerSessionId));   // sesi S2 (paket milik induk S1)
  await page.locator('.essay-score-input').first().fill('5');
  await Promise.all([
    page.waitForResponse(r => r.url().includes('/Admin/SubmitEssayScore') && r.status() === 200),
    page.locator('.btn-save-essay-score').first().click(),
  ]);
  await expect(page.locator(`#badge_${workerSessionId}_${questionId}`)).toHaveText(/Sudah Dinilai/);
  await expect(page.locator(`#badge_${workerSessionId}_${questionId}`)).toHaveClass(/bg-success/);
  expect(dialogs.join('|')).not.toContain('Soal bukan milik sesi ini');   // assert NO alert target
});
```

Test B (Pre/Post SamePackage paritas):
```
test('415.1 Pre/Post SamePackage essay save → SUCCESS', async ({ page }) => {
  const dialogs: string[] = [];
  page.on('dialog', d => { dialogs.push(d.message()); d.dismiss(); });
  await loginAny(page, 'admin');
  await page.goto(essayGradingUrl(postSessionId));     // sesi Post (paket clone)
  await page.locator('.essay-score-input').first().fill('5');
  await Promise.all([
    page.waitForResponse(r => r.url().includes('/Admin/SubmitEssayScore') && r.status() === 200),
    page.locator('.btn-save-essay-score').first().click(),
  ]);
  await expect(page.locator(`#badge_${postSessionId}_${postQuestionId}`)).toHaveText(/Sudah Dinilai/);
  expect(dialogs.join('|')).not.toContain('Soal bukan milik sesi ini');
});
```

`essayGradingUrl(sessionId)` = `/Admin/EssayGrading?sessionId=${sessionId}&title=${enc(title)}&category=${enc(category)}&scheduleDate=${scheduleDate}` (pola 384).

CATATAN PRASYARAT RUN (cantumkan di header komentar spec): app harus running @5277 (atau 5270 ITHandoff) dengan `Authentication__UseActiveDirectory=false`; run `cd tests; npx playwright test essay-grading-ownership-415.1 --workers=1`.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && test -f tests/e2e/essay-grading-ownership-415.1.spec.ts && grep -c "page.on('dialog'" tests/e2e/essay-grading-ownership-415.1.spec.ts && grep -c "Soal bukan milik sesi ini" tests/e2e/essay-grading-ownership-415.1.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/e2e/essay-grading-ownership-415.1.spec.ts` → file ada
    - `grep -c "page.on('dialog'" tests/e2e/essay-grading-ownership-415.1.spec.ts` → ≥ 1 (PITFALL-4 dipatuhi)
    - `grep -c "btn-save-essay-score" tests/e2e/essay-grading-ownership-415.1.spec.ts` → ≥ 1
    - `grep -c "Soal bukan milik sesi ini" tests/e2e/essay-grading-ownership-415.1.spec.ts` → ≥ 1 (assert NO alert target)
    - `grep -c "db.restore" tests/e2e/essay-grading-ownership-415.1.spec.ts` → ≥ 1 (afterAll restore)
    - `grep -c "ESSAYOWN415-PP" tests/e2e/essay-grading-ownership-415.1.spec.ts` → ≥ 1 (resolve Pre/Post)
  </acceptance_criteria>
  <done>Spec Playwright cross-package + Pre/Post ada; pasang page.on('dialog'); assert badge "Sudah Dinilai" + NO dialog "Soal bukan milik sesi ini"; lifecycle backup/restore + Layer 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Repro live @5277 + jalankan Playwright (verifikasi lokal D-03)</name>
  <files>tests/e2e/essay-grading-ownership-415.1.spec.ts, docs/SEED_JOURNAL.md</files>
  <action>CHECKPOINT (human-verify) — otomasi-dulu lalu konfirmasi: jalankan `dotnet build` + `dotnet test` (suite penuh) hijau; `dotnet run` app @5277 (AD-off); `cd tests; npx playwright test essay-grading-ownership-415.1 --workers=1` (2/2 pass: badge "Sudah Dinilai" tanpa dialog "Soal bukan milik sesi ini"); konfirmasi DB lokal di-RESTORE (afterAll) + update baris `docs/SEED_JOURNAL.md` [ESSAYOWN415] status active → cleaned. Detail langkah lengkap di <how-to-verify>.</action>
  <what-built>
    Plan 01 mengganti predikat WR-02 SubmitEssayScore (assignment-based). Task 1-2 menyediakan SQL seed 2-worker cross-package + Pre/Post clone + Playwright spec dengan dialog guard. Build + suite xUnit sudah hijau (Plan 01). Seed + spec menjalankan controller ASLI (bukan replica) untuk membuktikan symptom alert hilang.
  </what-built>
  <how-to-verify>
    Jalankan verifikasi lokal sesuai CLAUDE.md Develop Workflow + Seed Data Workflow (otomatis-dulu, checkpoint memverifikasi SETELAH otomasi):

    1. `dotnet build` → 0 error. `dotnet test` (suite penuh) → hijau (regresi).
    2. Jalankan app lokal: `dotnet run` dengan `Authentication__UseActiveDirectory=false` (login admin lokal). Verifikasi `http://localhost:5277` (atau 5270 bila di ITHandoff).
    3. Jalankan Playwright (spec auto snapshot→seed→restore via beforeAll/afterAll):
       `cd tests; npx playwright test essay-grading-ownership-415.1 --workers=1`
       Harapan: Test A (cross-package) + Test B (Pre/Post) PASS — badge "Sudah Dinilai", 0 dialog "Soal bukan milik sesi ini".
    4. (Opsional repro manual visual) Saat app + seed aktif: buka `/Admin/EssayGrading?sessionId={S2}` → isi skor → Simpan → badge hijau, tanpa alert. (Sebelum fix: alert "Soal bukan milik sesi ini.")
    5. Konfirmasi DB lokal di-RESTORE (afterAll) + `docs/SEED_JOURNAL.md` entry [ESSAYOWN415] ditandai `cleaned` (update baris journal: status active → cleaned + ringkasan hasil).

    Laporkan: hasil `dotnet test` (jumlah pass), hasil Playwright (X/2 pass), konfirmasi badge tanpa alert, konfirmasi DB restored + journal cleaned.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila build+suite hijau + Playwright 2/2 pass (badge tanpa alert) + DB restored/journal cleaned; atau jelaskan kegagalan.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Kemas branch hotfix dari origin/main + push + notify IT (delivery manual D-01)</name>
  <files>(git delivery only — tidak ada file repo yang diubah; aksi git/push)</files>
  <action>CHECKPOINT (human-action) — delivery git manual (GSD TIDAK push otomatis): buat branch `hotfix/essay-grading-ownership` dari `origin/main` (v31.0 `4b4a09b5`), cherry-pick HANYA commit kode/test 415.1 (exclude `.planning/`), verifikasi `git log origin/main..HEAD` hanya commit fix + build/test hijau, `git push origin hotfix/essay-grading-ownership` (JANGAN merge/push main), lalu notify IT (commit hash + migration=FALSE). Detail langkah lengkap di <how-to-verify>.</action>
  <what-built>
    Fix kode/test (Plan 01) + e2e/seed (Plan 02) sudah di-commit di branch `main` (commit GSD atomik normal, termasuk artifact .planning/). Deploy Dev membutuhkan branch hotfix TERPISAH berisi HANYA perubahan kode/test (non-.planning) di atas base `origin/main` (v31.0, `4b4a09b5`) — BUKAN bundle main (yang membawa v32.2/v32.5/v32.6 + migration 409/415). Fakta git terverifikasi: `origin/main` HEAD = `4b4a09b5` (v31.0); guard "Soal bukan milik sesi ini" + `GetShuffledQuestionIds` ADA di base tsb → cherry-pick apply bersih.
  </what-built>
  <how-to-verify>
    Langkah delivery (developer jalankan; GSD TIDAK push otomatis per CLAUDE.md — promosi = IT):

    1. Identifikasi commit kode/test 415.1 di `main` (yang menyentuh HANYA `Controllers/AssessmentAdminController.cs`, `HcPortal.Tests/PostLisensorPolishTests.cs`, `tests/e2e/essay-grading-ownership-415.1.spec.ts`, `tests/sql/essay-grading-ownership-415.1-seed.sql`, `docs/SEED_JOURNAL.md` — TANPA file `.planning/`). Catat hash-nya.
    2. Buat branch hotfix dari base v31.0:
       `git fetch origin && git checkout -b hotfix/essay-grading-ownership origin/main`
    3. Cherry-pick HANYA commit kode/test (non-.planning) tersebut ke branch hotfix:
       `git cherry-pick <hash-kode-test>` (bila satu commit mencampur .planning + kode, pisahkan: cherry-pick lalu `git restore --staged .planning && git checkout .planning` atau gunakan `git cherry-pick -n` + buang path .planning sebelum commit ulang).
    4. Verifikasi branch hotfix: `git log --oneline origin/main..HEAD` hanya menampilkan commit fix; `dotnet build` 0 error + `dotnet test --filter PostLisensorPolishTests` hijau di branch hotfix.
    5. Push branch hotfix: `git push origin hotfix/essay-grading-ownership`. JANGAN merge/push ke `main` (membawa v32.x).
    6. Notify IT: commit hash branch hotfix + **migration=FALSE** + catatan "data lama Dev status Menunggu Penilaian langsung bisa dinilai pasca-deploy, tanpa utak-atik DB". IT yang promosi ke Dev.

    Konfirmasi base benar dengan IT: Dev ditarik dari `origin/main` atau `origin/ITHandoff` (keduanya v31.0 `4b4a09b5`/`64456bd5`; fix identik). Bila IT pakai ITHandoff, ulangi langkah 2 dengan `origin/ITHandoff` sebagai base.
  </how-to-verify>
  <resume-signal>Ketik "delivered" bila branch hotfix dibuat dari origin/main (v31.0), build+test hijau di branch tsb, di-push, dan IT diberitahu (commit hash + migration=FALSE); atau jelaskan blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Admin/HC) → POST /Admin/SubmitEssayScore | e2e mengeksekusi controller ASLI lintas boundary HTTP (bukan replica) — membuktikan guard runtime, bukan hanya logika unit. |
| Test harness → DB lokal (HcPortalDB_Dev) | Seed temporary memutasi DB lokal; WAJIB snapshot→restore agar DB bersih pasca-test. |
| Developer → branch hotfix → origin | Delivery jalur terpisah; tidak boleh mencampur v32.x ke base Dev. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-415.1-07 | Tampering/Integrity | Seed temporary tertinggal di DB lokal lintas sesi (mencemari data lokal) | mitigate | Seed Data Workflow: beforeAll snapshot, afterAll restore + Layer 4 assert COUNT prefix [ESSAYOWN415] = 0; SEED_JOURNAL ditandai cleaned. Tangkap restoreError independen (pola 384) agar restore tidak tertutup assertion. |
| T-415.1-08 | Elevation/Deploy | Branch deploy salah membawa v32.x (migration 409/415) ke Dev v31.0 | mitigate | D-01: branch hotfix dari `origin/main` (v31.0 `4b4a09b5`) berisi HANYA commit kode/test (cherry-pick, exclude .planning). `git log origin/main..HEAD` diverifikasi hanya commit fix. JANGAN push/merge main. migration=FALSE dikomunikasikan ke IT. |
| T-415.1-09 | Repudiation/DoS | Playwright `alert()` native menggantung test (false-negative hijau) | mitigate | PITFALL-4: `page.on('dialog')` dipasang SEBELUM klik; assert dialog NOT contains "Soal bukan milik sesi ini" (deteksi regresi eksplisit, bukan timeout diam). |
| T-415.1-10 | (carry dari Plan 01) Tampering — anti-tamper guard | SubmitEssayScore predikat | mitigate (verified runtime) | e2e cross-package membuktikan fix; guard genuine-tamper/null/status/range diuji di Plan 01 xUnit. RBAC + antiforgery tetap (controller asli mensyaratkannya — login admin di e2e). |
</threat_model>

<verification>
- `tests/sql/essay-grading-ownership-415.1-seed.sql` ada (prefix [ESSAYOWN415], UPA pooled S1+S2, sesi Pre/Post clone).
- `tests/e2e/essay-grading-ownership-415.1.spec.ts` ada (page.on('dialog'), assert badge + NO alert, lifecycle backup/restore).
- `docs/SEED_JOURNAL.md` entry baru (active → cleaned pasca-test).
- Checkpoint Task 3: build + suite penuh hijau + Playwright 2/2 pass + DB restored.
- Checkpoint Task 4: branch hotfix dari origin/main (v31.0) berisi HANYA fix, pushed, IT notified (migration=FALSE).
- TIDAK ada `Data/SeedData.cs` atau `Migrations/` berubah (migration=FALSE).
</verification>

<success_criteria>
- Playwright real-browser membuktikan HC bisa simpan nilai essay cross-package + Pre/Post tanpa alert "Soal bukan milik sesi ini." (controller asli).
- DB lokal bersih pasca-test (snapshot→seed→restore→journal cleaned).
- Branch hotfix terpisah dari origin/main (v31.0) berisi HANYA commit fix, di-push, IT diberitahu migration=FALSE.
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/415.1-hotfix-guard-penilaian-essay-cross-package-soal-bukan-milik-/415.1-02-SUMMARY.md`.
Catat: hasil Playwright (X/2), konfirmasi badge tanpa alert (cross-package + Pre/Post), konfirmasi DB restored + SEED_JOURNAL cleaned, hash branch hotfix + konfirmasi push + notify IT (migration=FALSE).
</output>
