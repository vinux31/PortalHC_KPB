---
phase: 416-scoped-shuffle-acak-per-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/ShuffleEngine.cs
  - HcPortal.Tests/SectionScopedShuffleTests.cs
autonomous: true
requirements: [SHF-01, SHF-02, SHF-03, SHF-04]
tags: [shuffle, assessment, section, refactor, pure-engine]

must_haves:
  truths:
    - "Assessment tanpa Section (semua SectionId=null) menghasilkan urutan soal IDENTIK baseline pra-416 (golden-order)"
    - "Soal hanya diacak di dalam Section-nya (tidak melompat antar-Section)"
    - "Urutan global = blok per-Section berurutan SectionNumber, grup 'Lainnya' selalu terakhir (D-15)"
    - "Induk ShuffleQuestions OFF → semua Section terurut (toggle per-Section diabaikan, D-14)"
    - "Induk ShuffleQuestions ON → tiap Section ikut ShuffleEnabled-nya (anak)"
    - "Untuk >1 paket, tiap Section diisi dari gabungan Section padanan lintas-paket lalu di-sampling dalam batas Section dengan cakupan ET per-Section"
    - "workerIndex + seed sama menghasilkan urutan sama (determinisme)"
  artifacts:
    - path: "Helpers/ShuffleEngine.cs"
      provides: "Section-partitioning di dalam BuildQuestionAssignment + sub-fungsi BuildSectionQuestionAssignment (kunci komposit (SectionNumber, ET)) + gate option per-Section"
      contains: "BuildSectionQuestionAssignment"
    - path: "HcPortal.Tests/SectionScopedShuffleTests.cs"
      provides: "Suite unit baru: golden-order, isolasi section, precedence, pooling, determinisme, ET per-section"
      contains: "AllNullSection_ProducesIdenticalOrderToLegacyBaseline"
  key_links:
    - from: "Helpers/ShuffleEngine.cs BuildQuestionAssignment"
      to: "BuildSectionQuestionAssignment (per-section callee)"
      via: "partisi by SectionId scalar → SectionStructureComparer.KeyOf → loop urut + concat"
      pattern: "BuildSectionQuestionAssignment"
    - from: "Helpers/ShuffleEngine.cs partisi"
      to: "SectionStructureComparer.KeyOf / LainnyaKey"
      via: "null-safe key untuk grup Lainnya"
      pattern: "SectionStructureComparer\\.(KeyOf|LainnyaKey)"
---

<objective>
Refactor `Helpers/ShuffleEngine.cs` (fungsi murni, no-DB) menjadi **section-aware** TANPA mengubah signature publik `BuildQuestionAssignment(packages, shuffleQuestions, workerIndex, rng)`. Inti: mempartisi soal lintas-paket berdasarkan `SectionId` (skalar), lalu untuk tiap Section menjalankan algoritma distribusi yang sudah ada (`BuildCrossPackageAssignment` untuk ON / urut-Order untuk OFF) pada slice Section itu, lalu concat urut SectionNumber dengan grup "Lainnya" selalu terakhir (D-15). Option-shuffle di-gate per-Section via `ShuffleEnabled` (D-416-01), precedence induk/anak (D-14).

Invariant keystone: bila SEMUA `SectionId=null` → 1 grup tunggal "Lainnya" → sub-fungsi dipanggil SEKALI atas seluruh kolam = output **byte-identik** baseline pra-416 (golden-order regression, SHF-04 backward-compat).

Plan ini juga membuat suite test baru `SectionScopedShuffleTests.cs` (Wave-0 scaffold + implementasi) yang membuktikan SHF-01/02/03/04 di tingkat engine murni. Suite lama `ShuffleEngineTests.cs` (Phase 373) HARUS tetap hijau — itu kontrak golden-order.

Purpose: SHF-01 (isolasi section), SHF-02 (precedence + per-section toggle), SHF-03 (pooling lintas-paket per-section + ET coverage per-section), SHF-04 backward-compat golden-order + determinisme — semua dapat dibuktikan di engine murni tanpa DB.
Output: `Helpers/ShuffleEngine.cs` ter-refactor + `HcPortal.Tests/SectionScopedShuffleTests.cs` hijau + `ShuffleEngineTests.cs` tetap hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-CONTEXT.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-RESEARCH.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md

<interfaces>
<!-- Kontrak yang dipakai/dipertahankan. Executor pakai langsung — tidak perlu eksplorasi codebase. -->

Helpers/ShuffleEngine.cs (signature publik WAJIB DIPERTAHANKAN):
```csharp
public static void Shuffle<T>(List<T> list, Random rng);
public static List<int> BuildQuestionAssignment(
    List<AssessmentPackage> packages, bool shuffleQuestions, int workerIndex, Random rng); // signature TETAP
public static Dictionary<int, List<int>> BuildOptionShuffle(
    IEnumerable<PackageQuestion> questions, bool shuffleOptions, Random rng);
private static List<int> BuildCrossPackageAssignment(List<AssessmentPackage> packages, Random rng); // ON-path canonical — JANGAN tulis ulang
```

Model in-memory (Models/AssessmentPackage.cs — VERIFIED):
```csharp
class AssessmentPackage { int Id; int PackageNumber; ICollection<PackageQuestion> Questions; }
class AssessmentPackageSection { int Id; int SectionNumber; string? Name; bool StartNewPage=false; bool ShuffleEnabled=true; }
class PackageQuestion { int Id; int Order; string? ElemenTeknis; int? SectionId; AssessmentPackageSection? Section; ICollection<PackageOption> Options; }
class PackageOption { int Id; bool IsCorrect; }
```
Catatan: `PackageQuestion.SectionId` = kolom SKALAR (SELALU ter-load). `q.Section` = navigasi (butuh Include — JANGAN andalkan untuk partisi, Pitfall 3).

Helpers/SectionStructureComparer.cs (REUSE apa adanya — VERIFIED):
```csharp
public const int LainnyaKey = int.MinValue;               // sentinel grup "Lainnya"
public static int KeyOf(int? sectionNumber) => sectionNumber ?? LainnyaKey;
// Lainnya selalu terakhir: .OrderBy(k => k == LainnyaKey).ThenBy(k => k)
public static string SectionLabel(int sectionKey);        // LainnyaKey → "Lainnya"
```

HcPortal.Tests/ShuffleEngineTests.cs (fixture builder yang DI-EXTEND — VERIFIED :18-30):
```csharp
private static AssessmentPackage Pkg(int packageNumber, params (int id, int order, string? et)[] qs);
// tiap soal: 2 PackageOption (id*10, id*10+1). Determinisme via new Random(42).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 scaffold — buat SectionScopedShuffleTests.cs (fixture ber-section + baseline golden-order + stub tests RED)</name>
  <files>HcPortal.Tests/SectionScopedShuffleTests.cs</files>
  <read_first>
    - HcPortal.Tests/ShuffleEngineTests.cs (analog persis: class header, fixture `Pkg(...)` :18-30, fixed-seed `new Random(42)`, pola assert K-min :128-140 + determinisme :120-126)
    - Helpers/ShuffleEngine.cs (signature `BuildQuestionAssignment` + `BuildCrossPackageAssignment` core yang jadi baseline)
    - Helpers/SectionStructureComparer.cs (LainnyaKey + KeyOf untuk assert urutan "Lainnya" terakhir)
    - Models/AssessmentPackage.cs (AssessmentPackageSection.ShuffleEnabled + PackageQuestion.SectionId/.Section)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md (Test method map :146-156 — daftar 10 metode WAJIB; fixture extend :101-116; golden-order pattern :133-143)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
  </read_first>
  <action>
    Buat file BARU `HcPortal.Tests/SectionScopedShuffleTests.cs` (namespace `HcPortal.Tests`, `public class SectionScopedShuffleTests`, no `[Trait("Category","Integration")]` — engine pure, no DB). Pola persis `ShuffleEngineTests.cs`.

    **(A) Fixture builder ber-section** — extend pola `Pkg(...)`. Buat helper:
    - `PkgSec(int packageNumber, params (int id, int order, string? et, int? sectionNumber)[] qs)` → bangun `AssessmentPackage`; untuk tiap soal set `Order`, `ElemenTeknis`, 2 `PackageOption` (id*10, id*10+1), dan `SectionId` = sectionNumber (pakai sectionNumber langsung sebagai SectionId untuk test — konsisten antar-paket saudara), plus set navigasi `q.Section = new AssessmentPackageSection { Id = sectionNumber.Value, SectionNumber = sectionNumber.Value, ShuffleEnabled = true }` bila sectionNumber != null (set objek langsung karena no-DB). soal sectionNumber=null → `SectionId=null`, `Section=null` (grup "Lainnya").
    - Sediakan overload/parameter agar test bisa set `ShuffleEnabled=false` per-Section (mis. `PkgSecToggle(...)` atau argumen tambahan). Karena partisi ENGINE pakai `SectionId` skalar + lookup ShuffleEnabled, test HARUS bisa men-set `ShuffleEnabled` per SectionNumber konsisten antar paket.
    - Helper `AllNullFixture()` → ≥2 paket, semua `SectionId=null`, ET bervariasi (mis. p1: (10,1,"ET-A"),(11,2,"ET-B"),(12,3,"ET-A"); p2: (20,1,"ET-A"),(21,2,"ET-B")).

    **(B) Baseline golden-order** — Karena `BuildCrossPackageAssignment` `private`, bekukan baseline TANPA mengaksesnya: panggil `ShuffleEngine.BuildQuestionAssignment(AllNullFixture(), true, 0, new Random(42))` SEKARANG (sebelum refactor Task 2), jalankan test sekali, salin output list literal ke konstanta `GoldenOrderBaseline` (komentari "captured pre-416 refactor, seed 42"). ATAU buat helper `Clone(packages)` (deep shallow-copy paket+soal+opsi) + assert dua panggilan jalur sama identik. **Strategi WAJIB:** capture list literal hasil run pra-refactor sebagai baseline beku (Wave-0 gap VALIDATION). Catat nilai literal di komentar.

    **(C) Stub tests (RED awal)** — tulis 10 metode test (boleh body `Assert.True(false, "pending impl")` ATAU langsung implementasi asersi yang akan GREEN setelah Task 2; preferensi: implementasi penuh asersi). Daftar WAJIB (nama persis):
    - `AllNullSection_ProducesIdenticalOrderToLegacyBaseline` — `AllNullFixture()` + seed 42 → `Assert.Equal(GoldenOrderBaseline, actual)` (SHF-04 golden-order)
    - `ScopedShuffle_NoCrossSectionLeak` — assessment 2 section, induk ON → tiap ID hasil punya SectionId yang map ke SectionNumber yang sama dengan blok-nya (assert tidak ada interleave: kelompokkan hasil per-section via lookup SectionId → blok-blok kontigu) (SHF-01)
    - `SectionOrder_LainnyaAlwaysLast` — paket dgn section 1,2 + soal null → urutan = blok Sec1, blok Sec2, blok Lainnya (null) terakhir (SHF-01/D-15)
    - `Precedence_ParentOff_AllOrdered` — induk shuffleQuestions=false → tiap section terurut `Question.Order`, ShuffleEnabled per-section diabaikan (SHF-02/D-14)
    - `Precedence_ParentOn_PerSectionToggle` — induk ON, Section A ShuffleEnabled=false → Section A urut Order; Section B ShuffleEnabled=true → Section B teracak (seed-stabil) (SHF-02)
    - `OptionShuffle_GatedPerSection` — `BuildOptionShuffle` dipanggil per-section gate `induk ∧ ShuffleEnabled`: opsi soal di Section OFF tidak teracak (dict tak punya entry / urut DB), Section ON teracak (D-416-01) — gunakan API gate yang ditambah Task 2
    - `MultiPackage_EtCoveragePerSection` — 2 paket, Section 1 punya ET-A/ET-B; Phase 1 jamin ≥1 soal per (Section,ET) sampai K (SHF-03)
    - `EtSpanningSections_CoveredIndependently` — ET-A muncul di Section 1 DAN Section 2 → tiap section jamin cakupan ET-A independen (SHF-03)
    - `Determinism_WorkerIndexStable` — dua panggilan workerIndex sama + seed sama → `Assert.Equal(a, b)` (SHF-03)
    - `Reshuffle_SectionIsolation` — re-roll (panggil lagi seed beda) tetap blok-blok per-section tak bocor (SHF-04 — engine-level proof; wiring di Plan 02)

    Bahasa komentar BI/EN konsisten existing. JANGAN sentuh `ShuffleEngineTests.cs`.
  </action>
  <behavior>
    - `AllNullSection_ProducesIdenticalOrderToLegacyBaseline`: all-null + seed 42 == baseline beku
    - `ScopedShuffle_NoCrossSectionLeak`: setiap ID hasil ∈ section-nya, blok kontigu, no interleave
    - `SectionOrder_LainnyaAlwaysLast`: grup null (Lainnya) selalu di akhir
    - `Precedence_ParentOff_AllOrdered`: induk OFF → semua urut Order
    - `OptionShuffle_GatedPerSection`: opsi di-gate per-Section (D-416-01)
  </behavior>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.Tests/HcPortal.Tests.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/SectionScopedShuffleTests.cs` ada dan ter-compile (`dotnet build HcPortal.Tests/HcPortal.Tests.csproj` exit 0).
    - `grep -c "public void" HcPortal.Tests/SectionScopedShuffleTests.cs` >= 10 (10 metode WAJIB hadir).
    - `grep "AllNullSection_ProducesIdenticalOrderToLegacyBaseline" HcPortal.Tests/SectionScopedShuffleTests.cs` cocok.
    - `grep "new Random(42)" HcPortal.Tests/SectionScopedShuffleTests.cs` cocok (fixed-seed determinism).
    - `grep -L "GoldenOrderBaseline\|baseline" HcPortal.Tests/SectionScopedShuffleTests.cs` KOSONG (baseline golden-order ada).
    - `ShuffleEngineTests.cs` TIDAK dimodifikasi (`git diff --stat HcPortal.Tests/ShuffleEngineTests.cs` kosong).
    - Build hijau (test boleh RED/gagal sebelum Task 2 — itu diharapkan).
  </acceptance_criteria>
  <done>File test baru ter-compile, 10 metode hadir, baseline golden-order ter-capture, ShuffleEngineTests.cs tak tersentuh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor ShuffleEngine — section-partition di BuildQuestionAssignment + sub-fungsi BuildSectionQuestionAssignment + gate option per-Section</name>
  <files>Helpers/ShuffleEngine.cs</files>
  <read_first>
    - Helpers/ShuffleEngine.cs (SELURUH file — `BuildQuestionAssignment` :39-60 cabang ON/OFF, `BuildOptionShuffle` :67-78, `BuildCrossPackageAssignment` :91-232 Phase 1/2/3, `Shuffle<T>` :25-32)
    - Helpers/SectionStructureComparer.cs (LainnyaKey :19, KeyOf :22, urutan Lainnya-terakhir :31-32)
    - Models/AssessmentPackage.cs (SectionId skalar :99, ShuffleEnabled :53, SectionNumber :44)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-RESEARCH.md (Pattern 1 :168-202 partisi+concat, Pattern 2 :204-221 golden-order/RNG drift, Pattern 3 :223-231 precedence, Pitfall 1 :258-262 leak, Pitfall 2 :264-268 RNG drift, Pitfall 3 :270-274 SectionId skalar, Pitfall 4 :276-280 option gate, Open Q :375-383 slicing + defensif K=0)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md (Refactor shape :61-86 + invariant golden-order :76 + precedence :78-83)
    - HcPortal.Tests/SectionScopedShuffleTests.cs (kontrak test yang harus GREEN)
  </read_first>
  <action>
    Refactor `Helpers/ShuffleEngine.cs`. **JANGAN ubah signature publik** `BuildQuestionAssignment(packages, shuffleQuestions, workerIndex, rng)` (3 call-site memanggilnya IDENTIK — Plan 02 wire). Tambah logika partisi DI DALAM.

    **(1) Refactor `BuildQuestionAssignment` jadi partition+concat wrapper:**
    - Guard `packages.Count == 0 → return new List<int>()` (pertahankan).
    - Bangun lookup `SectionId → (SectionNumber, ShuffleEnabled)` dari soal yang ada. KARENA partisi pakai `SectionId` skalar (Pitfall 3), tapi navigasi `q.Section` mungkin null saat dipanggil produksi (Plan 02 menyediakan map). Strategi: partisi pakai **key komposit dari `q.Section?.SectionNumber`** bila navigasi tersedia (test set langsung), ATAU terima parameter map opsional. **Keputusan teknis (Open Q #1, planner pilih):** faktor ulang core agar partisi pakai `SectionStructureComparer.KeyOf(q.Section?.SectionNumber)` dengan fallback: jika `q.Section == null && q.SectionId != null`, engine TIDAK boleh salah-kelompok — Plan 02 WAJIB sediakan `q.Section` ter-Include ATAU map. Untuk plan ini: pakai `q.Section?.SectionNumber` sebagai sumber SectionNumber (test set objek; Plan 02 pastikan Include). Catat asumsi di komentar XML-doc.
    - Partisi distinct section key: `packages.SelectMany(p => p.Questions).Select(q => SectionStructureComparer.KeyOf(q.Section?.SectionNumber)).Distinct().OrderBy(k => k == SectionStructureComparer.LainnyaKey).ThenBy(k => k).ToList()` (Lainnya terakhir, D-15). **Operasi non-RNG deterministik** (Pitfall 2 — JANGAN konsumsi rng di partisi/sort).
    - FOR EACH section key: bangun `sectionPackages` = slice tiap paket berisi HANYA soal section itu (helper privat `SlicePackagesBySection`). Resolve `sectionShuffle = shuffleQuestions && ResolveSectionShuffle(key)` (precedence D-14; "Lainnya" key tak punya row → effective = induk flag, D-15).
    - Panggil `BuildSectionQuestionAssignment(sectionPackages, sectionShuffle, workerIndex, rng)` → `result.AddRange(...)`. CONCAT urut.

    **(2) Sub-fungsi `BuildSectionQuestionAssignment(List<AssessmentPackage> sectionPackages, bool sectionShuffle, int workerIndex, Random rng)` (private static, internal-visible bila test butuh — pakai `internal` + InternalsVisibleTo bila perlu, ATAU panggil via entry saja).** Logika = SAMA PERSIS `BuildQuestionAssignment` lama (cabang ON `BuildCrossPackageAssignment` + cabang OFF 1-paket/≥2-paket urut Order) tapi beroperasi pada slice section:
    - `sectionShuffle == true` → `return BuildCrossPackageAssignment(sectionPackages, rng)` (canonical, VERBATIM — JANGAN tulis ulang Phase 1/2/3).
    - `sectionShuffle == false` → cabang OFF existing (1 paket urut Order; ≥2 paket `packagesWithQuestions[workerIndex % count]` paket UTUH urut Order). Pindahkan logika OFF :44-59 ke sini.
    - Guard defensif: section slice kosong (count==0) → skip (return empty); JANGAN throw (Open Q #2, best-effort D-416-03).

    **(3) `SlicePackagesBySection(packages, sectionKey)` (private helper, pure):** untuk tiap paket, buat AssessmentPackage shallow dgn `Questions` ter-filter `SectionStructureComparer.KeyOf(q.Section?.SectionNumber) == sectionKey`, pertahankan `PackageNumber`/`Id`. JANGAN clone objek EF dalam-dalam (Open Q #1 — shallow + filtered Questions list cukup; opsi ref same PackageQuestion objek).

    **(4) Option-shuffle per-Section (D-416-01, Pitfall 4):** tambah overload/helper agar caller bisa gate option per-section. Pilihan: `BuildOptionShuffle(IEnumerable<PackageQuestion> questions, bool shuffleOptions, Random rng)` existing tetap, TAMBAH metode baru `BuildSectionAwareOptionShuffle(IEnumerable<PackageQuestion> assignedQuestions, bool parentShuffleOptions, Random rng)` yang untuk tiap soal hitung `effective = parentShuffleOptions && (q.Section?.ShuffleEnabled ?? true)` (Lainnya/null section → ikut induk) dan hanya acak opsi soal yang effective==true. Return `Dictionary<int, List<int>>` (sama bentuk). Soal effective==false → TIDAK masuk dict (caller fallback DB-order, sama existing OFF).

    **(5) Invariant golden-order:** untuk all-null → satu key LainnyaKey → `SlicePackagesBySection` return semua soal → `BuildSectionQuestionAssignment` dipanggil SEKALI atas seluruh kolam dgn `sectionShuffle = shuffleQuestions` (Lainnya effective = induk) → identik `BuildQuestionAssignment` lama. Pastikan TIDAK ada `rng.Next` ekstra di partisi (Pitfall 2).

    **(6) Update XML-doc header** `ShuffleEngine.cs` sebut scoping per-Section (kunci komposit `(SectionNumber, ET)`) + kompatibel-mundur all-null.

    Verifikasi semua test `SectionScopedShuffleTests` GREEN + `ShuffleEngineTests` (Phase 373) tetap GREEN (kontrak golden-order — bila merah, ENGINE salah, BUKAN test).
  </action>
  <behavior>
    - all-null → output identik baseline (BuildSectionQuestionAssignment dipanggil sekali, no RNG drift)
    - 2 section induk ON → tiap ID hasil ∈ section-nya, blok kontigu, Lainnya terakhir
    - induk OFF → semua section urut Order; induk ON + section ShuffleEnabled=false → section itu urut Order
    - BuildSectionAwareOptionShuffle: hanya soal di section ON yang opsinya teracak
    - section slice kosong → skip, no throw (defensif)
  </behavior>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ShuffleEngine|FullyQualifiedName~SectionScopedShuffle" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/ShuffleEngine.cs` mengandung `BuildSectionQuestionAssignment` (`grep "BuildSectionQuestionAssignment" Helpers/ShuffleEngine.cs` cocok).
    - `Helpers/ShuffleEngine.cs` mengandung `SectionStructureComparer.KeyOf` (`grep "SectionStructureComparer.KeyOf" Helpers/ShuffleEngine.cs` cocok).
    - `Helpers/ShuffleEngine.cs` mengandung `BuildSectionAwareOptionShuffle` (gate option per-section, D-416-01).
    - Signature publik `BuildQuestionAssignment(List<AssessmentPackage> packages, bool shuffleQuestions, int workerIndex, Random rng)` TIDAK berubah (`grep "public static List<int> BuildQuestionAssignment(List<AssessmentPackage> packages, bool shuffleQuestions, int workerIndex, Random rng)" Helpers/ShuffleEngine.cs` cocok).
    - `BuildCrossPackageAssignment` masih ada (core canonical tidak ditulis ulang; `grep "BuildCrossPackageAssignment" Helpers/ShuffleEngine.cs` cocok).
    - `dotnet test --filter "FullyQualifiedName~ShuffleEngine|FullyQualifiedName~SectionScopedShuffle"` exit 0 (SEMUA hijau — termasuk ShuffleEngineTests Phase 373 golden-order + SectionScopedShuffleTests 10/10).
    - `AllNullSection_ProducesIdenticalOrderToLegacyBaseline` PASS (golden-order).
  </acceptance_criteria>
  <done>Engine section-aware, signature publik utuh, BuildSectionQuestionAssignment + BuildSectionAwareOptionShuffle ada, semua test engine hijau (Phase 373 + suite baru), golden-order terbukti.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Engine adalah fungsi murni server-side internal. Tidak ada input user baru; `ShuffleEnabled` (bool) sudah divalidasi + persist di Phase 415. Tidak ada endpoint baru di plan ini. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-416-01 | Tampering / Information Disclosure | `ShuffleEngine` section-partition (soal bisa "bocor" antar-Section bila partisi setelah sampling) | mitigate | Partisi SEBELUM sampling; tiap `BuildSectionQuestionAssignment` HANYA terima soal section-nya; fallback "ambil sisa" dibatasi slice section (Pitfall 1). Dibuktikan test `ScopedShuffle_NoCrossSectionLeak`. |
| T-416-02 | Tampering | Manipulasi posisi opsi → ubah nilai | accept | Grading by `PackageOption.Id` (bukan posisi huruf) — acak opsi TIDAK pengaruhi skor. By design, sudah aman (PackageOption.cs:118-123). |
| T-416-03 | (Robustness, bukan STRIDE) | Engine crash saat section slice kosong / K=0 / drift lolos guard | mitigate | Engine defensif: count==0 → skip section (return empty), JANGAN throw. Best-effort (D-416-03). |
</threat_model>

<verification>
- `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` → 0 error.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ShuffleEngine|FullyQualifiedName~SectionScopedShuffle"` → semua hijau.
- Suite penuh `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` → tidak ada regresi (ShuffleEngineTests Phase 373 WAJIB hijau = kontrak golden-order).
- `git diff --stat HcPortal.Tests/ShuffleEngineTests.cs` → kosong (test lama tak diubah).
</verification>

<success_criteria>
- `Helpers/ShuffleEngine.cs` section-aware dengan signature publik `BuildQuestionAssignment` DIPERTAHANKAN.
- All-null golden-order regression PASS (byte-identik baseline pra-416).
- Isolasi section terbukti (no cross-section leak), "Lainnya" terakhir (D-15).
- Precedence induk/anak (D-14) terbukti; option-shuffle di-gate per-Section (D-416-01).
- Pooling lintas-paket per-Section + ET coverage per-Section + determinisme workerIndex terbukti.
- ShuffleEngineTests.cs (Phase 373) tetap hijau.
- migration=FALSE dipertahankan (nol perubahan skema/Migrations/Data).
</success_criteria>

<output>
After completion, create `.planning/phases/416-scoped-shuffle-acak-per-section/416-01-SUMMARY.md`
</output>
