---
phase: 416-scoped-shuffle-acak-per-section
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/CMPController.cs
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ManagePackageQuestions.cshtml
autonomous: true
requirements: [SHF-01, SHF-02, SHF-03, SHF-04]
tags: [shuffle, assessment, section, controller, wiring, et-coverage]

must_haves:
  truths:
    - "StartExam memuat data Section sehingga engine mempartisi soal per-Section (bukan jatuh ke 'Lainnya' palsu)"
    - "Reshuffle per-paket dan semua peserta menghormati batas Section (re-roll dalam batas Section)"
    - "Peserta yang ditambah live (AddParticipantsLive) memakai jalur load + engine section-aware yang SAMA dengan StartExam (tak drift)"
    - "Option-shuffle di tiap call-site di-gate per-Section ShuffleEnabled (D-416-01)"
    - "HC melihat peringatan non-blocking saat K (jumlah soal Section) < jumlah distinct Elemen Teknis di Section itu (D-416-03)"
    - "Peringatan ET-coverage TIDAK memblokir mulai/kelola — hanya sinyal"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "StartExam load Section + wire BuildSectionAwareOptionShuffle (gate per-section)"
      contains: "BuildSectionAwareOptionShuffle"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ReshufflePackage + ReshuffleAll + CreateEagerAssignmentsAsync load Section + gate option per-section + ViewBag.SectionEtWarnings"
      contains: "SectionEtWarnings"
    - path: "Views/Admin/ManagePackageQuestions.cshtml"
      provides: "Render alert peringatan ET-coverage per-Section (non-blocking, teks BI)"
      contains: "Elemen Teknis"
  key_links:
    - from: "Controllers/CMPController.cs StartExam load block"
      to: "ShuffleEngine.BuildQuestionAssignment (partisi pakai q.Section)"
      via: ".ThenInclude(q => q.Section) di assignment block"
      pattern: "ThenInclude\\(q => q\\.Section\\)"
    - from: "Controllers/AssessmentAdminController.cs ManagePackageQuestions GET"
      to: "Views/Admin/ManagePackageQuestions.cshtml"
      via: "ViewBag.SectionEtWarnings"
      pattern: "ViewBag\\.SectionEtWarnings"
---

<objective>
Wire engine section-aware (Plan 01) ke **3 call-site yang memanggilnya IDENTIK** + tambah peringatan ET-coverage (D-416-03).

Karena signature publik `BuildQuestionAssignment` DIPERTAHANKAN, satu-satunya perubahan call-site adalah: (1) pastikan data Section ter-load agar engine bisa mempartisi (Pitfall 3 — tanpa ini scoped-shuffle senyap tak aktif, semua soal jatuh ke "Lainnya"), dan (2) ganti `BuildOptionShuffle(..., assessment.ShuffleOptions, rng)` menjadi `BuildSectionAwareOptionShuffle(..., parentShuffleOptions, rng)` agar opsi di-gate per-Section (D-416-01, Pitfall 4). Ketiganya WAJIB seragam (Pitfall 5 — AddParticipantsLive/EagerAssign mudah terlupa karena ada di AssessmentAdminController, bukan CMP) → ini yang memenuhi SHF-04 (reshuffle section-aware) + drift-free AddParticipantsLive secara otomatis.

Tambah komputasi peringatan ET-coverage di `ManagePackageQuestions` GET → `ViewBag.SectionEtWarnings` → render alert non-blocking di view (D-416-03).

Purpose: SHF-01 (scoped-shuffle aktif runtime), SHF-04 (reshuffle + AddParticipantsLive uniform/drift-free), SHF-02/03 (option gate + ET warning).
Output: 3 call-site ter-wire seragam + peringatan ET-coverage tampil.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-RESEARCH.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-01-SUMMARY.md

<interfaces>
<!-- Engine API setelah Plan 01 (executor pakai langsung). -->

Helpers/ShuffleEngine.cs (setelah Plan 01):
```csharp
public static List<int> BuildQuestionAssignment(
    List<AssessmentPackage> packages, bool shuffleQuestions, int workerIndex, Random rng); // partisi per-Section di dalam (pakai q.Section?.SectionNumber)
public static Dictionary<int, List<int>> BuildSectionAwareOptionShuffle(
    IEnumerable<PackageQuestion> assignedQuestions, bool parentShuffleOptions, Random rng); // gate per-soal: parentShuffleOptions && (q.Section?.ShuffleEnabled ?? true)
```
PENTING (Pitfall 3): engine mempartisi pakai `q.Section?.SectionNumber` + `q.Section?.ShuffleEnabled`. Maka call-site WAJIB `.ThenInclude(q => q.Section)` agar navigasi `q.Section` ter-load — kalau tidak, `q.Section==null` → semua soal jatuh ke "Lainnya" → scoped-shuffle senyap mati.

3 call-site (VERIFIED line refs):
- `CMPController.StartExam` :1047-1048 workerIndex; :1050-1052 load (assignment block, BELUM include Section); :1130-1138 wire BuildQuestionAssignment + BuildOptionShuffle.
- `AssessmentAdminController.CreateEagerAssignmentsAsync` :2544 workerIndex; :2546-2548 load; :2554/:2556 wire (AddParticipantsLive eager).
- `AssessmentAdminController.ReshufflePackage` :6022-6024 load; :6042-6044 workerIndex+wire; :6046 option.
- `AssessmentAdminController.ReshuffleAll` :6102-6104 load; :6146-6148 workerIndex; :6158/:6160 wire (loop per-session, sessionPackages slice type-aware).

ManagePackageQuestions GET (VERIFIED :7637-7663): sudah load `pkg.Questions` (+Options) + `sections` (AssessmentPackageSections by SectionNumber) → `ViewBag.Sections`. `q.SectionId` skalar tersedia di pkg.Questions.

View (VERIFIED): `Views/Admin/ManagePackageQuestions.cshtml` — `sections = ViewBag.Sections` (:8); panel Section render tabel per-section :122-147 dengan `var qCount = questions.Count(q => q.SectionId == s.Id)` (:124). Slot peringatan natural di sini.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 3 call-site uniform — load Section (.ThenInclude q.Section) + ganti ke BuildSectionAwareOptionShuffle</name>
  <files>Controllers/CMPController.cs, Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/CMPController.cs :1037-1160 (StartExam: workerIndex :1047-1048, load assignment block :1050-1055, re-guard 415 :1069-1123 yang SUDAH `.ThenInclude(q => q.Section)` :1086, wire :1130-1138)
    - Controllers/AssessmentAdminController.cs :2539-2560 (CreateEagerAssignmentsAsync), :6018-6050 (ReshufflePackage), :6095-6165 (ReshuffleAll)
    - Helpers/ShuffleEngine.cs (API setelah Plan 01: BuildQuestionAssignment partisi pakai q.Section; BuildSectionAwareOptionShuffle)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md (CMPController wiring :199-230, AssessmentAdmin 3 call-site :234-256 + workerIndex parity :251-255)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-RESEARCH.md (Pitfall 3 :270-274 SectionId/Include, Pitfall 4 :276-280 option gate, Pitfall 5 :282-286 AddParticipantsLive drift, Code Examples :298-322)
  </read_first>
  <action>
    Untuk KETIGA call-site (CMPController.StartExam, AssessmentAdminController.CreateEagerAssignmentsAsync, ReshufflePackage, ReshuffleAll — 4 lokasi load), lakukan SERAGAM:

    **(A) Tambah `.ThenInclude(q => q.Section)` ke load block** yang memanggil `BuildQuestionAssignment` (Pitfall 3). Pola existing:
    ```csharp
    var packages = await _context.AssessmentPackages
        .Include(p => p.Questions)
            .ThenInclude(q => q.Options)
        .Include(p => p.Questions)              // tambah chain kedua untuk Section
            .ThenInclude(q => q.Section)        // 416: WAJIB — engine partisi pakai q.Section?.SectionNumber/ShuffleEnabled
        .Where(p => siblingSessionIds.Contains(p.AssessmentSessionId))
        .OrderBy(p => p.PackageNumber)
        .ToListAsync();
    ```
    Lokasi: CMPController.cs ~:1050-1055; AssessmentAdminController.cs ~:2546-2548 (EagerAssign), ~:6022-6024 (ReshufflePackage), ~:6102-6104 (ReshuffleAll). JANGAN ubah `siblingSessionIds`/`OrderBy(PackageNumber)`/`workerIndex` (invariant Phase 373).

    **(B) Ganti `BuildOptionShuffle(assignedQuestions, X.ShuffleOptions, rng)` → `BuildSectionAwareOptionShuffle(assignedQuestions, X.ShuffleOptions, rng)`** di keempat lokasi wire (Pitfall 4, D-416-01). `BuildQuestionAssignment(...)` TIDAK berubah bentuk panggilannya (signature dipertahankan, partisi internal). Lokasi wire:
    - CMPController.cs :1131 (Build...) tetap; :1137-1138 BuildOptionShuffle → BuildSectionAwareOptionShuffle.
    - AssessmentAdminController.cs :2554 (Build...) tetap; :2556 → BuildSectionAwareOptionShuffle.
    - :6044 (Build...) tetap; :6046 → BuildSectionAwareOptionShuffle.
    - :6158 (Build...) tetap; :6160 → BuildSectionAwareOptionShuffle.

    **(C) ReshuffleAll catatan:** ber-loop per-session dengan `sessionPackages` slice type-aware (:6149-6160). `sessionPackages` di-slice dari `packages` (yang sudah include Section via langkah A). Pastikan slice mempertahankan navigasi `q.Section` (slice by reference, bukan re-query tanpa Include).

    JANGAN ubah re-guard struktur Section StartExam (:1069-1123) — sudah ada (415 SEC-04), assignment hanya jalan setelah guard lolos. JANGAN ubah auth `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` (:5990-5992).

    Verifikasi `dotnet build HcPortal.csproj` 0 error + suite penuh hijau (no regresi).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ThenInclude(q => q.Section)" Controllers/CMPController.cs` >= 2 (assignment block + guard block existing).
    - `grep "BuildSectionAwareOptionShuffle" Controllers/CMPController.cs` cocok.
    - `grep -c "BuildSectionAwareOptionShuffle" Controllers/AssessmentAdminController.cs` >= 3 (EagerAssign + ReshufflePackage + ReshuffleAll).
    - `grep -c "ThenInclude(q => q.Section)" Controllers/AssessmentAdminController.cs` bertambah >= 3 di blok 2546/6024/6104 (verifikasi tiap call-site punya akses Section).
    - `dotnet build HcPortal.csproj` exit 0 (0 error).
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit 0 (suite penuh hijau, no regresi).
    - `git diff Controllers/CMPController.cs` TIDAK mengubah `workerIndex`/`sortedSiblingIds`/re-guard 415 (`grep -n "sortedSiblingIds.IndexOf(id)" Controllers/CMPController.cs` masih ada).
  </acceptance_criteria>
  <done>3 call-site (4 lokasi load) seragam memuat Section + memakai BuildSectionAwareOptionShuffle; build 0 error; suite penuh hijau; invariant workerIndex/guard tak berubah.</done>
</task>

<task type="auto">
  <name>Task 2: ET-coverage warning (D-416-03) — komputasi di ManagePackageQuestions GET + render alert non-blocking di view</name>
  <files>Controllers/AssessmentAdminController.cs, Views/Admin/ManagePackageQuestions.cshtml</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs :7635-7663 (ManagePackageQuestions GET — sudah load pkg.Questions + sections + ViewBag.Sections)
    - Views/Admin/ManagePackageQuestions.cshtml :8 (sections = ViewBag.Sections), :100-147 (panel Section: tabel per-section, qCount per section :124)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-RESEARCH.md (ET-coverage warning :331-344, D-416-03 :238)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md (ET-warning computation :264-285)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-CONTEXT.md (D-416-03: best-effort + peringatan, TIDAK blokir; wording HC discretion)
  </read_first>
  <action>
    **(A) Komputasi warning di GET** `ManagePackageQuestions` (AssessmentAdminController.cs ~:7654, setelah `sections` di-load dan SEBELUM `return View()`):
    ```csharp
    // 416 D-416-03: peringatan cakupan ET non-blocking. K = jumlah soal Section; bila distinct ET > K,
    // sebagian ET tak terjamin muncul (Phase 1 jamin 1-per-ET hanya sampai kuota K). SINYAL, bukan error.
    ViewBag.SectionEtWarnings = sections.Select(s => {
        var qs = pkg.Questions.Where(q => q.SectionId == s.Id).ToList();
        int k = qs.Count;
        int distinctEt = qs.Where(q => !string.IsNullOrWhiteSpace(q.ElemenTeknis))
                           .Select(q => q.ElemenTeknis!).Distinct().Count();
        return new { s.SectionNumber, s.Name, K = k, DistinctEt = distinctEt, Warn = distinctEt > k };
    }).Where(x => x.Warn).Cast<object>().ToList();
    ```
    (Sesuaikan tipe ViewBag ke yang bisa dirender view — bisa anonymous list, atau buat record/class kecil `SectionEtWarning` agar view strongly-typed. Pilih yang konsisten konvensi existing; bila pakai anonymous, view akses via `dynamic`.)

    **(B) Render alert di view** `ManagePackageQuestions.cshtml` — di panel Section, SEBELUM atau SETELAH tabel section (mis. setelah `</table>` di :147 / sebelum `}` else block). Iterasi `ViewBag.SectionEtWarnings`; bila ada, render `<div class="alert alert-warning ...">` (non-blocking, gaya konsisten alert existing). Teks BI per-section (CLAUDE.md — respon & UI BI):
    ```
    "Section {SectionNumber} ({Name}): {DistinctEt} Elemen Teknis tapi hanya {K} soal — sebagian Elemen Teknis tak terjamin muncul saat ujian. Tambah soal bila ingin cakupan penuh."
    ```
    Bila list kosong → tidak render apa-apa. JANGAN tambah tombol blokir / disable apa pun (D-416-03 = sinyal, TIDAK memblokir mulai ujian / simpan).

    **(C)** Pastikan tidak mengubah perilaku existing panel Section (CRUD/urut/toggle 415). Hanya menambah blok alert.

    Verifikasi build 0 error. Karena ini render Razor, tambahkan satu unit/integration test ringan bila seam tersedia (mis. test komputasi `distinctEt > k` di helper bila diekstrak); kalau tidak, andalkan UAT Playwright di Plan 03 (manual-assisted) + assert build. Bila tak ada seam test murni, set verify automated ke build + flag UAT-covered di Plan 03.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet build HcPortal.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SectionEtWarnings" Controllers/AssessmentAdminController.cs` cocok (komputasi di GET).
    - `grep "distinctEt\|DistinctEt" Controllers/AssessmentAdminController.cs` cocok (logika distinct ET vs K).
    - `grep "SectionEtWarnings" Views/Admin/ManagePackageQuestions.cshtml` cocok (render).
    - `grep "Elemen Teknis" Views/Admin/ManagePackageQuestions.cshtml` cocok (teks BI peringatan).
    - View TIDAK mengandung `disabled` / blok mulai-ujian baru terkait warning (`grep -i "tidak bisa mulai\|blokir\|disabled.*section-et" Views/Admin/ManagePackageQuestions.cshtml` KOSONG) — D-416-03 non-blocking.
    - `dotnet build HcPortal.csproj` exit 0.
    - `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` exit 0 (no regresi).
  </acceptance_criteria>
  <done>ViewBag.SectionEtWarnings dihitung di GET (distinctEt > K per-section), alert BI non-blocking dirender di panel Section, tidak ada gate blokir baru, build + suite hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Reshuffle/StartExam endpoints | Sudah ada (Phase 373/415). 416 tidak menambah endpoint atau input baru — hanya menambah `.ThenInclude` dan mengganti pemanggilan option-shuffle. |
| HC → ManagePackageQuestions GET | Read-only view; warning ET = output saja, tak menerima input baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-416-04 | Tampering / Info Disclosure | Soal bocor antar-Section karena Section tak ter-load (engine jatuh ke "Lainnya") | mitigate | `.ThenInclude(q => q.Section)` di 4 lokasi load → engine mempartisi benar. Dibuktikan UAT Plan 03 (assessment ber-section → tak lompat). |
| T-416-05 | Elevation of Privilege | Reshuffle oleh non-HC | accept (existing) | Sudah `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` (AssessmentAdminController.cs:5990-5992) — 416 tak mengubah. |
| T-416-06 | Drift / robustness | AddParticipantsLive (EagerAssign) memakai jalur berbeda → distribusi peserta baru beda | mitigate | Ketiga call-site (4 load) di-update SERAGAM (Pitfall 5). Entry signature dipertahankan → konsistensi cross-call otomatis. |
| T-416-07 | DoS (tiny-Section ET-warning) | Komputasi warning di GET per-section | accept | O(soal) sederhana di halaman admin terautentikasi; non-blocking; tidak ada loop tak terbatas. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` → suite penuh hijau (engine tests Plan 01 + tak ada regresi controller).
- Manual grep: ketiga call-site (4 load) punya `.ThenInclude(q => q.Section)` + `BuildSectionAwareOptionShuffle`.
- ViewBag.SectionEtWarnings dihitung; alert BI dirender; tidak ada gate blokir baru.
- Runtime/render proof (assessment ber-section teracak per-section, AddParticipantsLive konsisten, warning tampil) → UAT Playwright Plan 03.
</verification>

<success_criteria>
- 3 call-site (4 lokasi load) seragam: Section ter-load + BuildSectionAwareOptionShuffle (SHF-01 aktif runtime, SHF-04 reshuffle + AddParticipantsLive drift-free).
- Option-shuffle di-gate per-Section (D-416-01).
- Peringatan ET-coverage non-blocking tampil saat K < distinct-ET (D-416-03).
- Build + suite penuh hijau, invariant workerIndex/guard 415 tak berubah, migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/416-scoped-shuffle-acak-per-section/416-02-SUMMARY.md`
</output>
