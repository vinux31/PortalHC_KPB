---
phase: 416-scoped-shuffle-acak-per-section
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - tests/e2e/scoped-shuffle.spec.ts
autonomous: false
requirements: [SHF-01, SHF-02, SHF-03, SHF-04]
tags: [shuffle, assessment, section, e2e, playwright, uat]

must_haves:
  truths:
    - "Saat ujian real-browser, soal assessment ber-Section hanya teracak DI DALAM Section-nya (tidak melompat antar-Section)"
    - "Assessment tanpa Section menghasilkan urutan yang konsisten dengan perilaku lama (backward-compat visual)"
    - "Peserta yang ditambah live (AddParticipantsLive) mendapat distribusi yang pola-konsisten dengan peserta awal (tak drift)"
    - "Peringatan ET-coverage tampil di UI kelola Section saat K < distinct ET (non-blocking)"
  artifacts:
    - path: "tests/e2e/scoped-shuffle.spec.ts"
      provides: "Playwright e2e: acak per-Section real-browser + all-null backward-compat + ET-warning render + DB snapshot/restore lifecycle"
      contains: "scoped"
  key_links:
    - from: "tests/e2e/scoped-shuffle.spec.ts"
      to: "HcPortalDB_Dev (BACKUP/RESTORE)"
      via: "db.backup beforeAll / db.restore afterAll (helpers/dbSnapshot)"
      pattern: "db\\.(backup|restore)"
    - from: "tests/e2e/scoped-shuffle.spec.ts"
      to: "app @ http://localhost:5277 (StartExam render)"
      via: "createAssessmentViaWizard + StartExam navigation"
      pattern: "createAssessmentViaWizard"
---

<objective>
Buktikan scoped-shuffle berfungsi RUNTIME via Playwright real-browser (D-416-05 #4, lesson 354: Razor/JS/SignalR WAJIB UAT browser — unit tak menangkap render). Buat `tests/e2e/scoped-shuffle.spec.ts` (analog `tests/e2e/shuffle.spec.ts`) dengan lifecycle DB snapshot/restore (SEED_WORKFLOW), seed assessment ber-Section, ambil ujian, dan assert soal teracak DALAM Section (tak lompat antar-Section). Plus verifikasi backward-compat all-null + peringatan ET-coverage + (best-effort) konsistensi AddParticipantsLive.

Ditutup dengan checkpoint human-verify: jalankan app @5277 + Playwright + verifikasi DB lokal, lalu user mengonfirmasi (gate ship sebelum `/gsd-verify-work`).

Purpose: SHF-01/02/03/04 terbukti runtime (bukan cuma unit). Backward-compat visual + isolasi section + ET warning + drift-free AddParticipantsLive.
Output: spec e2e hijau + DB lokal restored bersih + konfirmasi UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-CONTEXT.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-01-SUMMARY.md
@.planning/phases/416-scoped-shuffle-acak-per-section/416-02-SUMMARY.md

<interfaces>
<!-- Helper e2e existing (VERIFIED ada). Executor pakai langsung. -->

tests/e2e/shuffle.spec.ts (analog persis — pola DB snapshot + wizard create):
```typescript
import { test, expect, type Page } from '@playwright/test';
import * as db from '../helpers/dbSnapshot';            // backup/restore/queryScalar/queryString
import { login } from '../helpers/auth';                // login(page,'admin') → admin@pertamina.com / 123456
import { createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm } from './helpers/examTypes';

test.describe.configure({ mode: 'serial' });            // WAJIB serial (DB isolation)
// beforeAll: db.backup(snapshotPath) — afterAll: db.restore + unlink (pola :100-111)
async function execSql(sql: string): Promise<number> { return db.queryScalar(`${sql}; SELECT @@ROWCOUNT;`); }
const futureDate = () => { const d=new Date(); d.setDate(d.getDate()+1); return d.toISOString().slice(0,10); };
```

Konvensi WAJIB (CLAUDE.md SEED_WORKFLOW + lesson 364/375):
- `--workers=1` (playwright.config fullyParallel:false — DB shared).
- Seed = temporary + local-only → BACKUP HcPortalDB_Dev beforeAll → RESTORE afterAll → catat docs/SEED_JOURNAL.md → tandai cleaned.
- App harus jalan @ http://localhost:5277 (`Authentication__UseActiveDirectory=false dotnet run`). Browser hilang → `cd tests; npx playwright install chromium`.
- Auth: admin@pertamina.com / 123456 (dev lokal — JANGAN staging/prod).

Section di UI (Phase 415, VERIFIED):
- Kelola Section: `/Admin/ManagePackageQuestions?packageId={id}` — form tambah Section (`name="sectionNumber"`, `name="sectionName"`, checkbox `name="shuffleEnabled"`, `name="startNewPage"`), assign soal via dropdown SectionId.
- ET-warning (Plan 02): alert `.alert-warning` di panel Section, teks "Elemen Teknis".
- Toggle induk: `/Admin/ManagePackages` — `#shuffleQuestions` / `#shuffleOptions`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat tests/e2e/scoped-shuffle.spec.ts — seed ber-Section + assert isolasi section + backward-compat + ET-warning</name>
  <files>tests/e2e/scoped-shuffle.spec.ts, docs/SEED_JOURNAL.md</files>
  <read_first>
    - tests/e2e/shuffle.spec.ts (analog penuh: imports :26-29, serial :41, execSql :45-47, futureDate :33-37, createAssessmentArriveMP :52-71, beforeAll/afterAll DB snapshot :100-111)
    - tests/e2e/helpers/examTypes.ts (createAssessmentViaWizard, createDefaultPackage, addQuestionViaForm — signature)
    - tests/helpers/dbSnapshot.ts (backup/restore/queryScalar/queryString)
    - tests/helpers/auth.ts (login)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-PATTERNS.md (e2e analog :162-196: imports, serial, DB lifecycle, execSql, UAT inti D-416-05)
    - .planning/phases/416-scoped-shuffle-acak-per-section/416-CONTEXT.md (D-416-05: 4 hal yang WAJIB dibuktikan)
    - Views/Admin/ManagePackageQuestions.cshtml (selektor form Section + dropdown assign + alert ET-warning)
  </read_first>
  <action>
    Buat `tests/e2e/scoped-shuffle.spec.ts` (mode serial, `--workers=1`). Reuse helper existing (`createAssessmentViaWizard`, `login`, `db.backup/restore`, `execSql`). Lifecycle: `beforeAll` BACKUP `HcPortalDB_Dev` → `afterAll` RESTORE + unlink (verbatim pola shuffle.spec.ts :100-111).

    **Skenario WAJIB (D-416-05):**

    1. **Isolasi section (SHF-01 inti):** wizard-create 1 assessment OJT (induk ShuffleQuestions=ON) → buka ManagePackages → buat 1 paket → di ManagePackageQuestions: buat 2 Section (No.1 "Pompa", No.2 "Valve", keduanya ShuffleEnabled=ON) → tambah soal (mis. 4 soal Section 1, 4 soal Section 2; beri ElemenTeknis bervariasi; assign SectionId). Bila seed soal/section via form terlalu lambat, boleh seed via `execSql` (INSERT AssessmentPackageSection + UPDATE PackageQuestions.SectionId pada record yang baru dibuat wizard) — dokumentasikan SQL inline (snapshot melindungi DB). Ambil ujian sebagai peserta (atau buka StartExam) → assert: urutan soal yang dirender = **blok Section 1 (4 soal Pompa) lalu blok Section 2 (4 soal Valve)**; soal Section 1 TIDAK muncul di tengah Section 2 (tak ada interleave). Verifikasi via query `ShuffledQuestionIds` di `UserPackageAssignment` (DB) ATAU urutan DOM soal di StartExam → map tiap question id ke SectionId, assert blok-blok kontigu, "Lainnya" (null) terakhir bila ada.

    2. **Backward-compat all-null (SHF-04 visual):** wizard-create assessment kedua TANPA Section (semua soal SectionId=null), induk ON → ambil ujian → assert urutan = perilaku global lama (semua soal teracak sebagai 1 kolam, tak ada pengelompokan section). Cukup assert: semua soal muncul, tidak ada error render, urutan deterministik-per-worker (tidak crash). (Golden-order byte-identik sudah di-prove unit Plan 01; di sini = smoke visual backward-compat.)

    3. **ET-coverage warning (D-416-03):** di ManagePackageQuestions, buat Section dengan distinct ET > jumlah soal (mis. Section "Sempit" 1 soal tapi set ElemenTeknis pada konteks yang menyiratkan ≥2 ET via paket saudara / atau langsung: 1 section, 2 soal beda ET tapi K=1 melalui paket lain) → assert alert `.alert-warning` mengandung teks "Elemen Teknis" muncul; assert TIDAK ada blokir (tombol mulai/simpan tetap aktif). (Bila sulit men-trigger K<distinctET via UI murni, seed kondisi via execSql lalu reload halaman.)

    4. **(Best-effort) AddParticipantsLive konsistensi (Pitfall 5):** bila waktu/seam memungkinkan — tambah peserta live setelah ujian mulai (via Monitoring Detail AddParticipantsLive, fitur v32.5) → bandingkan struktur distribusi section peserta baru vs awal (harus pola-konsisten: soal tetap dalam batas Section). Bila terlalu kompleks untuk e2e stabil, turunkan ke assertion DB (query UserPackageAssignment peserta baru → assert ShuffledQuestionIds tetap blok-per-section). Tandai opsional bila tak feasible — inti tetap skenario 1-3.

    Setiap skenario yang seed state via SQL: dokumentasikan inline + pastikan ter-cover snapshot/restore. JANGAN sentuh `shuffle.spec.ts` / `exam-taking.spec.ts` / `exam-types.spec.ts`.

    Catat di `docs/SEED_JOURNAL.md`: tujuan (UAT scoped-shuffle 416), klasifikasi (temporary + local-only), entitas tersentuh (AssessmentSession/Package/Section/Question), status setelah run = cleaned (restore).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB/tests" && npx playwright test scoped-shuffle.spec.ts --workers=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/scoped-shuffle.spec.ts` ada.
    - `grep "db.backup" tests/e2e/scoped-shuffle.spec.ts` DAN `grep "db.restore" tests/e2e/scoped-shuffle.spec.ts` cocok (DB lifecycle).
    - `grep "mode: 'serial'\|describe.configure" tests/e2e/scoped-shuffle.spec.ts` cocok.
    - `grep "createAssessmentViaWizard" tests/e2e/scoped-shuffle.spec.ts` cocok (reuse helper, tidak flat-form).
    - Spec mengandung minimal 3 `test(` (isolasi section, backward-compat all-null, ET-warning).
    - `npx playwright test scoped-shuffle.spec.ts --workers=1` exit 0 (semua test pass) — PRECONDITION: app jalan @5277.
    - `docs/SEED_JOURNAL.md` punya entry baru untuk UAT 416 ditandai cleaned.
    - `shuffle.spec.ts` / `exam-taking.spec.ts` / `exam-types.spec.ts` tidak dimodifikasi.
  </acceptance_criteria>
  <done>Spec e2e baru hijau (≥3 skenario: isolasi section, backward-compat, ET-warning), DB snapshot/restore utuh, SEED_JOURNAL ter-catat cleaned, spec lain tak tersentuh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint UAT live @5277 — verifikasi scoped-shuffle real-browser + DB lokal + backward-compat</name>
  <files>(verifikasi manual — tidak memodifikasi file)</files>
  <action>
    GATE human-verify. Claude telah mengotomatiskan seluruh pekerjaan (build + suite xUnit hijau + Playwright `scoped-shuffle.spec.ts` hijau dengan DB backup/restore). Task ini MENJEDA eksekusi untuk verifikasi manual oleh user di browser @5277 (lesson 354: Razor/JS render WAJIB dibuktikan mata-manusia). JANGAN lanjut ke `/gsd-verify-work` sebelum user mengetik "approved". Tampilkan blok <what-built> + <how-to-verify> ke user dan tunggu sinyal resume.
  </action>
  <what-built>
    Engine section-aware (Plan 01) + wiring 3 call-site + ET-warning (Plan 02) + spec e2e `scoped-shuffle.spec.ts` (Task 1 plan ini). Claude sudah otomatiskan: `dotnet build` + suite xUnit penuh hijau (termasuk golden-order Phase 373 + SectionScopedShuffleTests) + Playwright `scoped-shuffle.spec.ts` hijau dengan DB backup/restore.
  </what-built>
  <how-to-verify>
    Prasyarat: app jalan lokal — `Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5277` (cek `http://localhost:5277` hidup) + DB lokal `HcPortalDB_Dev`.

    1. Login admin (`admin@pertamina.com` / `123456`) → buat assessment OJT, tambah 1 paket, buka `/Admin/ManagePackageQuestions?packageId={id}`.
    2. Buat 2 Section ("Pompa" No.1 Acak=ON, "Valve" No.2 Acak=ON), assign beberapa soal ke tiap Section (beri Elemen Teknis bervariasi). Pastikan toggle induk `ShuffleQuestions` ON di `/Admin/ManagePackages`.
    3. Ambil ujian sebagai peserta (atau preview StartExam). **Verifikasi:** soal hanya teracak DI DALAM Section-nya — semua soal "Pompa" muncul berurutan sebagai satu blok, lalu semua soal "Valve" sebagai blok berikutnya. TIDAK ada soal Section yang melompat ke Section lain. Refresh/ambil sebagai peserta lain → urutan dalam Section boleh beda (teracak) TAPI tetap tak lintas-Section.
    4. **Backward-compat:** buat assessment lain TANPA Section (semua soal tanpa Section) → ambil ujian → urutan = perilaku lama (teracak global), tidak ada error.
    5. **Precedence (D-14):** matikan toggle induk `ShuffleQuestions` (OFF) → ambil ujian → SEMUA soal terurut (tak teracak), toggle per-Section diabaikan. Nyalakan lagi induk, set 1 Section Acak=OFF → Section itu terurut, Section lain teracak.
    6. **ET-warning (D-416-03):** buat Section dengan jumlah soal < distinct Elemen Teknis → cek alert kuning peringatan muncul di panel Section dengan teks "Elemen Teknis"; pastikan TIDAK memblokir mulai ujian / simpan.
    7. **DB lokal:** cek `UserPackageAssignment.ShuffledQuestionIds` peserta = blok-per-section (urutan tak lintas-Section). Setelah selesai, pastikan DB lokal kembali bersih (e2e sudah restore; bila uji manual, cleanup assessment test + restore bila perlu).
  </how-to-verify>
  <verify>Manual human-verify — user mengonfirmasi di browser. Tidak ada perintah otomatis (gate menunggu sinyal resume).</verify>
  <done>User mengetik "approved" setelah membuktikan isolasi section + backward-compat + precedence + ET-warning di browser @5277.</done>
  <resume-signal>Ketik "approved" bila scoped-shuffle terbukti (isolasi section + backward-compat + precedence + ET-warning) atau jelaskan masalah yang ditemukan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → HcPortalDB_Dev | e2e men-seed + BACKUP/RESTORE DB lokal. localhost-guard di `dbSnapshot` (tidak menyentuh Dev/Prod). Seed temporary + local-only (CLAUDE.md SEED_WORKFLOW). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-416-08 | Tampering (data lokal) | Seed test menempel di DB lokal pasca-run | mitigate | beforeAll BACKUP → afterAll RESTORE + unlink; SEED_JOURNAL ditandai cleaned. Tidak menyentuh Dev/Prod (localhost-guard). |
| T-416-09 | Information Disclosure | UAT membuktikan soal tak bocor antar-Section (integritas konten ujian) | mitigate | Skenario 1 + DB assert `ShuffledQuestionIds` blok-per-section. Ini verifikasi runtime untuk T-416-01/T-416-04. |
</threat_model>

<verification>
- `npx playwright test scoped-shuffle.spec.ts --workers=1` → exit 0 (app @5277 prasyarat).
- Suite xUnit penuh hijau (carry dari Plan 01/02).
- DB lokal `HcPortalDB_Dev` restored bersih (afterAll); SEED_JOURNAL cleaned.
- Checkpoint human-verify: user mengonfirmasi isolasi section + backward-compat + precedence + ET-warning di browser.
</verification>

<success_criteria>
- Spec e2e `scoped-shuffle.spec.ts` hijau (≥3 skenario membuktikan SHF-01/02/03/04 runtime).
- UAT live: soal teracak DALAM Section (tak lompat), all-null = perilaku lama, precedence D-14 benar, ET-warning non-blocking tampil.
- DB lokal bersih pasca-run; spec lain tak tersentuh; migration=FALSE.
- User approve checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/416-scoped-shuffle-acak-per-section/416-03-SUMMARY.md`
</output>
