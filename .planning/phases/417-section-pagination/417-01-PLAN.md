---
phase: 417-section-pagination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/PackageExamViewModel.cs
  - Helpers/SectionPaginator.cs
  - HcPortal.Tests/SectionPaginatorTests.cs
autonomous: true
requirements: [PAG-01, PAG-02, PAG-03]
tags: [pagination, exam-render, section, helper, xunit]
user_setup: []

must_haves:
  truths:
    - "Fungsi murni ComputePages menetapkan PageNumber per-soal: page baru saat Section StartNewPage=true (bukan soal pertama) ATAU halaman penuh (perPage)"
    - "Assessment tanpa Section (semua SectionNumber=null) menghasilkan page-map IDENTIK dengan index/perPage lama (golden-baseline, backward-compat)"
    - "Section panjang >perPage auto-pecah per perPage dengan IsSectionContinuation=true di soal pertama halaman lanjutan"
    - "Section ber-StartNewPage=true memulai halaman baru sebelum soal pertamanya (kecuali soal pertama keseluruhan)"
    - "Grup Lainnya (SectionNumber=null) tak memaksa page-break kecuali halaman penuh"
    - "Mobile perPage=5 mengikuti aturan Section yang sama"
  artifacts:
    - path: "Helpers/SectionPaginator.cs"
      provides: "Static pure fn ComputePages(IList<ExamQuestionItem> ordered, int perPage) — set PageNumber/IsSectionStart/IsSectionContinuation"
      contains: "public static void ComputePages"
    - path: "Models/PackageExamViewModel.cs"
      provides: "Field section-aware di ExamQuestionItem: SectionNumber, SectionName, SectionStartNewPage, PageNumber, IsSectionStart, IsSectionContinuation"
      contains: "public int PageNumber"
    - path: "HcPortal.Tests/SectionPaginatorTests.cs"
      provides: "xUnit pure unit test ComputePages (PAG-01/02/03 + golden no-Section baseline)"
      contains: "NoSection_IdenticalToFlatBaseline"
  key_links:
    - from: "Helpers/SectionPaginator.cs"
      to: "Models/PackageExamViewModel.cs (ExamQuestionItem)"
      via: "set q.PageNumber/q.IsSectionStart/q.IsSectionContinuation atas field baru"
      pattern: "q\\.PageNumber\\s*="
    - from: "HcPortal.Tests/SectionPaginatorTests.cs"
      to: "Helpers/SectionPaginator.cs"
      via: "panggil SectionPaginator.ComputePages atas List<ExamQuestionItem> in-memory"
      pattern: "SectionPaginator\\.ComputePages"
---

<objective>
Buat fungsi murni `SectionPaginator.ComputePages` (section-aware page-number computation) + tambah field section-aware ke `ExamQuestionItem`, lalu kunci perilakunya dengan xUnit pure (Wave 0). Ini adalah fondasi single-source-of-truth pagination yang dikonsumsi controller+view di Plan 02.

Purpose: Memindahkan algoritma §7.2 (hitung PageNumber per-soal) ke satu fungsi deterministik non-RNG yang mudah di-unit-test tanpa DB — menghindari drift Razor↔JS (Pitfall 1) dan menjamin backward-compat no-Section byte-identik (Pitfall 3) lewat golden-baseline test.
Output: `Helpers/SectionPaginator.cs` (pure fn), field baru di `Models/PackageExamViewModel.cs`, `HcPortal.Tests/SectionPaginatorTests.cs` (suite hijau).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/417-section-pagination/417-CONTEXT.md
@.planning/phases/417-section-pagination/417-RESEARCH.md
@.planning/phases/417-section-pagination/417-PATTERNS.md
@.planning/phases/417-section-pagination/417-VALIDATION.md

<interfaces>
<!-- Kontrak yang dikonsumsi/dihasilkan plan ini. Executor pakai langsung — no codebase exploration. -->

ExamQuestionItem EXISTING (Models/PackageExamViewModel.cs:25-45):
```csharp
public class ExamQuestionItem
{
    public int QuestionId { get; set; }
    public string QuestionText { get; set; } = "";
    public int DisplayNumber { get; set; }      // 1-based, GLOBAL, reflects shuffled position
    public List<ExamOptionItem> Options { get; set; } = new();
    public string QuestionType { get; set; } = "MultipleChoice";
    public int MaxCharacters { get; set; } = 2000;
    public string? ImagePath { get; set; }     // RND-01
    public string? ImageAlt { get; set; }       // RND-01
    // ← TAMBAH field section-aware di sini (Plan 01 Task 1)
}
```

Algoritma §7.2 (RESEARCH Pattern 1 — deterministik NON-RNG):
- Iterasi soal SUDAH urut Section 1→2→…→Lainnya (dari GetShuffledQuestionIds Phase 416).
- page baru bila: (sectionChanged && SectionStartNewPage && !firstQuestion) ATAU (countOnPage >= perPage).
- IsSectionStart = sectionChanged (header polos); IsSectionContinuation = !sectionChanged && countOnPage==0 (header "(lanjutan)").

AssessmentPackageSection (Models/AssessmentPackage.cs:34-57, sumber data Section — READ-ONLY di plan ini):
```csharp
public int SectionNumber { get; set; }          // L44
public string? Name { get; set; }                // L47 nullable
public bool StartNewPage { get; set; }           // L50 default false
public bool ShuffleEnabled { get; set; }         // L53 default true
```
</interfaces>

<read_before_planning>
Sudah dibaca saat planning (line numbers terverifikasi):
- Models/PackageExamViewModel.cs:1-55 (ExamQuestionItem field-addition pattern)
- Helpers/ShuffleEngine.cs:1-40 (class static pure + doc-comment + invariant backward-compat pattern — analog gaya)
- Helpers/PaginationHelper.cs:1-15 (clamp page-math pattern)
</read_before_planning>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tambah field section-aware ke ExamQuestionItem</name>
  <read_first>
    - Models/PackageExamViewModel.cs:25-45 (ExamQuestionItem — pola field-addition RND-01 ImagePath/ImageAlt L42-44, QuestionType L32-37, MaxCharacters L39-40; ikuti pola: append field + komentar fase + XML-doc)
    - .planning/phases/417-section-pagination/417-RESEARCH.md (bagian "Tambah field Section + PageNumber ke ExamQuestionItem")
    - .planning/phases/417-section-pagination/417-PATTERNS.md (bagian ExamQuestionItem MODIFY — target fields)
  </read_first>
  <behavior>
    - Field SectionNumber (int?), SectionName (string?), SectionStartNewPage (bool), PageNumber (int), IsSectionStart (bool), IsSectionContinuation (bool) ADA dan default-nya benar (PageNumber=0, bool=false, nullable=null).
    - Field tidak persisted ke DB (ini viewmodel, bukan entity — tak ada migration).
  </behavior>
  <action>
Edit `Models/PackageExamViewModel.cs`. SETELAH baris `public string? ImageAlt { get; set; }` (L44, di dalam class `ExamQuestionItem`), tambahkan PERSIS field berikut (XML-doc Bahasa Indonesia, komentar fase):

```csharp
        // Phase 417 PAG-01/02/03: metadata pagination section-aware.
        // Dihitung saat render (SectionPaginator.ComputePages), TIDAK disimpan per-soal (D-11, migration=FALSE).
        /// <summary>Nomor Section soal ini; null = grup "Lainnya".</summary>
        public int? SectionNumber { get; set; }
        /// <summary>Nama Section (D-417-01, name-only). Null untuk "Lainnya".</summary>
        public string? SectionName { get; set; }
        /// <summary>Salinan AssessmentPackageSection.StartNewPage — apakah Section ini mulai halaman baru.</summary>
        public bool SectionStartNewPage { get; set; }
        /// <summary>Indeks halaman 0-based hasil ComputePages. Default 0.</summary>
        public int PageNumber { get; set; }
        /// <summary>True bila soal ini awal Section (Section berubah) → render header polos.</summary>
        public bool IsSectionStart { get; set; }
        /// <summary>True bila Section SAMA tapi soal pertama di halaman baru (auto-split) → header "(lanjutan)".</summary>
        public bool IsSectionContinuation { get; set; }
```

JANGAN ubah field existing apa pun. JANGAN sentuh `PackageExamViewModel`, `ExamOptionItem`.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public int PageNumber" Models/PackageExamViewModel.cs` → match
    - `grep -n "public bool IsSectionContinuation" Models/PackageExamViewModel.cs` → match
    - `grep -cE "public (int\?|string\?|bool|int) (SectionNumber|SectionName|SectionStartNewPage|PageNumber|IsSectionStart|IsSectionContinuation)" Models/PackageExamViewModel.cs` → 6
    - `dotnet build HcPortal.csproj` → 0 error
    - `git status --porcelain Migrations/ Data/` → kosong (migration=FALSE)
  </acceptance_criteria>
  <done>6 field baru ada di ExamQuestionItem, build 0 error, tak ada diff Migrations/Data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Buat Helpers/SectionPaginator.cs (fungsi murni ComputePages)</name>
  <read_first>
    - Helpers/ShuffleEngine.cs:1-40 (gaya: class static pure di namespace HcPortal.Helpers + doc-comment menyebut fase + invariant backward-compat + "Pure by design (only System/Linq/HcPortal.Models)")
    - Helpers/PaginationHelper.cs:1-15 (pola clamp Math.Max/Math.Min — boleh tambah ClampPage helper opsional)
    - Models/PackageExamViewModel.cs (ExamQuestionItem dgn field baru dari Task 1)
    - .planning/phases/417-section-pagination/417-RESEARCH.md (Pattern 1 — kode pseudo ComputeSectionPages + Backward-compat invariant)
    - .planning/phases/417-section-pagination/417-PATTERNS.md (Signature target Phase 417)
  </read_first>
  <behavior>
    - ComputePages(ordered, perPage) menset PageNumber/IsSectionStart/IsSectionContinuation tiap item, deterministik, NON-RNG, idempotent (panggil 2x → hasil sama).
    - Backward-compat: semua SectionNumber=null → page hanya naik karena pageFull → PageNumber == index/perPage; IsSectionStart hanya true di item[0]; IsSectionContinuation false semua KECUALI soal pertama tiap halaman lanjutan dgn section sama (tetap konsisten dgn no-Section: item[0] start, sisanya tidak).
    - StartNewPage hanya memicu page-break bila Section BERUBAH dan bukan soal pertama keseluruhan.
    - "Lainnya" (SectionNumber=null, SectionStartNewPage=false) tak memaksa break kecuali pageFull.
    - perPage=5 (mobile) bekerja dgn algoritma yang sama.
  </behavior>
  <action>
Buat file BARU `Helpers/SectionPaginator.cs`. Namespace `HcPortal.Helpers`. Class `public static class SectionPaginator`. Doc-comment Bahasa Indonesia bergaya `ShuffleEngine.cs` (sebut Phase 417, algoritma §7.2, invariant backward-compat all-null = identik index/perPage, "Pure by design").

Implementasi PERSIS algoritma deterministik berikut (RESEARCH Pattern 1 — JANGAN modifikasi logika kondisi):

```csharp
using System;
using System.Collections.Generic;
using HcPortal.Models;

namespace HcPortal.Helpers
{
    /// <summary>
    /// Phase 417 (PAG-01/02/03) — Pure section-aware page computation (no EF, no DB, fully synchronous).
    /// Single source of truth untuk PageNumber per-soal yang dikonsumsi CMPController.StartExam + StartExam.cshtml.
    /// Algoritma §7.2: iterasi soal yang SUDAH urut Section 1→2→…→Lainnya (dari GetShuffledQuestionIds, Phase 416),
    /// naikkan halaman bila (a) Section berubah ke Section ber-StartNewPage=true (kecuali soal pertama), ATAU
    /// (b) halaman sudah berisi perPage soal.
    ///
    /// BACKWARD-COMPAT (invariant): bila SEMUA SectionNumber=null → sectionChanged hanya true di soal pertama
    /// (sentinel) → needNewPageForSection selalu false (guard !firstQuestion) → page hanya naik karena pageFull
    /// → PageNumber IDENTIK index/perPage lama (golden-baseline). NON-RNG, deterministik, idempotent.
    ///
    /// Page-number TIDAK disimpan per-soal (D-11) — selalu dihitung saat render. Pure by design
    /// (only System/Collections/HcPortal.Models) → unit-testable tanpa database.
    /// </summary>
    public static class SectionPaginator
    {
        public static void ComputePages(IList<ExamQuestionItem> ordered, int perPage)
        {
            if (ordered == null) throw new ArgumentNullException(nameof(ordered));
            if (perPage < 1) perPage = 1;

            int page = 0;
            int countOnPage = 0;
            int? prevSection = -1;          // sentinel ≠ section nyata / null
            bool firstQuestion = true;

            foreach (var q in ordered)
            {
                bool sectionChanged = !Equals(q.SectionNumber, prevSection);
                bool needNewPageForSection = sectionChanged && q.SectionStartNewPage && !firstQuestion;
                bool pageFull = countOnPage >= perPage;

                if (needNewPageForSection || pageFull)
                {
                    page++;
                    countOnPage = 0;
                }

                q.PageNumber = page;
                q.IsSectionStart = sectionChanged;
                q.IsSectionContinuation = !sectionChanged && countOnPage == 0;

                countOnPage++;
                prevSection = q.SectionNumber;
                firstQuestion = false;
            }
        }

        /// <summary>Clamp page-index resume ke [0, maxPage]; di luar rentang/negatif → 0 (D-417-05).</summary>
        public static int ClampResumePage(int requested, int maxPage)
        {
            if (requested < 0 || requested > maxPage) return 0;
            return requested;
        }
    }
}
```

JANGAN tambah dependency EF/DB. JANGAN sentuh ShuffleEngine.cs.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `Helpers/SectionPaginator.cs` ada
    - `grep -n "public static void ComputePages" Helpers/SectionPaginator.cs` → match
    - `grep -n "public static int ClampResumePage" Helpers/SectionPaginator.cs` → match
    - `grep -c "using Microsoft.EntityFrameworkCore" Helpers/SectionPaginator.cs` → 0 (pure, no EF)
    - `dotnet build HcPortal.csproj` → 0 error
  </acceptance_criteria>
  <done>SectionPaginator.ComputePages + ClampResumePage ada, pure (no EF), build 0 error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 xUnit — SectionPaginatorTests.cs (PAG-01/02/03 + golden baseline)</name>
  <read_first>
    - HcPortal.Tests/SectionScopedShuffleTests.cs:1-144 (pola fixture pure in-memory PkgSec L34-66 + golden-baseline literal L84-90 + test method pattern L104-110 — kelas pure, no [Trait("Category","Integration")])
    - Helpers/SectionPaginator.cs (dari Task 2 — yang sedang dites)
    - Models/PackageExamViewModel.cs (ExamQuestionItem dgn field baru)
    - .planning/phases/417-section-pagination/417-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map: nama method PAG-01/02/03)
    - .planning/phases/417-section-pagination/417-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
  </read_first>
  <behavior>
    - Test PASS hijau setelah ComputePages benar (Task 2). RED-GREEN: tulis test dulu (mereka harus gagal kalau ComputePages keliru), lalu konfirmasi GREEN.
    - Helper builder bangun List<ExamQuestionItem> in-memory dgn SectionNumber/SectionName/SectionStartNewPage + DisplayNumber 1..N urut.
  </behavior>
  <action>
Buat file BARU `HcPortal.Tests/SectionPaginatorTests.cs`. Namespace `HcPortal.Tests`. Kelas pure `public class SectionPaginatorTests` (TANPA `[Trait("Category","Integration")]` — pure, no DB; cermin `SectionScopedShuffleTests`).

Tambahkan helper builder in-memory di kelas:
```csharp
// Bangun List<ExamQuestionItem> urut (DisplayNumber 1..N). qs: (sectionNumber, startNewPage).
// Soal "Lainnya" pakai sectionNumber=null.
private static List<ExamQuestionItem> Build(params (int? sectionNumber, bool startNewPage)[] qs)
{
    var list = new List<ExamQuestionItem>();
    int n = 1;
    foreach (var (sec, snp) in qs)
    {
        list.Add(new ExamQuestionItem
        {
            QuestionId = n,
            DisplayNumber = n,
            SectionNumber = sec,
            SectionName = sec == null ? null : $"Section {sec}",
            SectionStartNewPage = snp,
        });
        n++;
    }
    return list;
}
```

Tulis [Fact] berikut (nama PERSIS sesuai Test Map RESEARCH — jangan rename):

1. **`PageNumber_FlowsTenPerPage`** (PAG-01): 25 soal Section 1 (StartNewPage=false). perPage=10 → PageNumber: 10×page0, 10×page1, 5×page2. Assert `list.Max(q=>q.PageNumber)==2` dan distribusi 10/10/5.

2. **`NoSection_IdenticalToFlatBaseline`** (PAG-01 GOLDEN, backward-compat): 23 soal SEMUA SectionNumber=null. perPage=10. Assert SETIAP `q.PageNumber == (q.DisplayNumber-1)/10` (identik index/perPage lama). Komentar: "Golden backward-compat — kalau merah perbaiki ComputePages, JANGAN ubah assert ini."

3. **`StartNewPage_BreaksBeforeSection`** (PAG-02): Section 1 (3 soal, snp=false) lalu Section 2 (2 soal, snp=true). perPage=10. Assert 3 soal Section1 di page0; soal pertama Section2 di page1 (`PageNumber==1`, `IsSectionStart==true`), meski page0 belum penuh.

4. **`LongSection_AutoSplitsTenPerPage`** (PAG-02 + continuation): Section 1 = 12 soal, snp=false. perPage=10. Assert soal #11 (`DisplayNumber==11`) `PageNumber==1` & `IsSectionContinuation==true` & `IsSectionStart==false`; soal #1 `IsSectionStart==true` & `IsSectionContinuation==false`.

5. **`LainnyaGroup_NoForcedBreak`** (PAG-02): Section 1 (5 soal, snp=false) lalu Lainnya (3 soal null, snp=false). perPage=10. Assert SEMUA 8 soal di page0 (Lainnya tak paksa break, page belum penuh); soal Lainnya pertama `IsSectionStart==true` (section berubah dari 1→null).

6. **`Resume_ClampsToValidRange`** (PAG-03): `SectionPaginator.ClampResumePage(2, 4)==2` dan `ClampResumePage(0, 4)==0`.

7. **`Resume_OutOfRange_FallsBackToZero`** (PAG-03): `ClampResumePage(99, 4)==0` dan `ClampResumePage(-1, 4)==0`.

8. **`MobileFivePerPage_SectionAware`** (mobile): 12 soal Section 1 (snp=false). perPage=5 → page0=5, page1=5, page2=2. Assert `list.Max(q=>q.PageNumber)==2` & soal #6 `PageNumber==1` `IsSectionContinuation==true`.

Gunakan `Assert.Equal`/`Assert.True`/`Assert.All`. Panggil `SectionPaginator.ComputePages(list, perPage)` lalu assert atas list.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~SectionPaginator" --nologo 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File `HcPortal.Tests/SectionPaginatorTests.cs` ada
    - `grep -cE "\[Fact\]" HcPortal.Tests/SectionPaginatorTests.cs` → 8
    - `grep -n "NoSection_IdenticalToFlatBaseline" HcPortal.Tests/SectionPaginatorTests.cs` → match
    - `grep -n "LongSection_AutoSplitsTenPerPage" HcPortal.Tests/SectionPaginatorTests.cs` → match
    - `grep -c "Trait(\"Category\", \"Integration\")" HcPortal.Tests/SectionPaginatorTests.cs` → 0 (pure, no-DB)
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~SectionPaginator"` → 8/8 Passed, 0 Failed
  </acceptance_criteria>
  <done>8 test pure SectionPaginator hijau (PAG-01/02/03 + golden no-Section baseline), no DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 01 = fungsi murni + viewmodel field + unit test. Tak ada input lintas-batas, tak ada endpoint, tak ada DB write, tak ada auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-417-01 | Tampering | SectionPaginator.ComputePages | accept | N/A — fungsi murni deterministik atas data viewmodel in-memory yang sudah dibangun server (server-authoritative urutan dari GetShuffledQuestionIds). Tak menerima input client. Tak ada permukaan tamper. |
| T-417-02 | (Info Disclosure) | ExamQuestionItem field baru | accept | N/A — field render-metadata (PageNumber/SectionName) bukan data sensitif; SectionName HC-controlled, di-encode saat render (ditangani Plan 02). Plan 01 tak me-render apa pun. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~SectionPaginator"` → 8/8 Passed.
- `dotnet test HcPortal.Tests` (full suite) → baseline 665+ hijau, 0 regresi (jalankan saat wave merge).
- `git status --porcelain Migrations/ Data/` → kosong (migration=FALSE).
</verification>

<success_criteria>
- `ExamQuestionItem` punya 6 field section-aware baru, default benar.
- `SectionPaginator.ComputePages` + `ClampResumePage` ada, pure (no EF), deterministik.
- 8 xUnit `SectionPaginatorTests` hijau, termasuk golden no-Section baseline (backward-compat) + auto-split continuation + StartNewPage break + Lainnya no-force + mobile perPage=5 + resume clamp/fallback.
- Build 0 error; full suite tak regresi; migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/417-section-pagination/417-01-SUMMARY.md`
</output>
