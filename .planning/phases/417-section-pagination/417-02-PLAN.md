---
phase: 417-section-pagination
plan: 02
type: execute
wave: 2
depends_on: [417-01]
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/StartExam.cshtml
autonomous: true
requirements: [PAG-01, PAG-02, PAG-03]
tags: [pagination, exam-render, razor, signalr, resume]
user_setup: []

must_haves:
  truths:
    - "Saat ujian ber-Section: header NAMA Section muncul di atas grup soal saat berganti Section (D-417-01); halaman lanjutan auto-split menampilkan header + '(lanjutan)' (D-417-02)"
    - "Section ber-StartNewPage=true benar-benar mulai di exam-page div baru; Section panjang terpecah per questionsPerPage (PAG-02)"
    - "Navigator (#panelNumbers desktop + #drawerNumbers mobile) dikelompokkan per-Section dengan label grup; assessment tanpa Section tetap flat 1..N (D-417-03 backward-compat)"
    - "Indikator halaman menampilkan 'NamaSection — Halaman n/total'; tanpa Section → 'Halaman n/total' (D-417-04)"
    - "Resume mengarah ke RESUME_PAGE terhitung (bukan hardcode 0); LastActivePage di luar rentang → fallback halaman 0; toast 'Lanjut dari soal no. X' saat resume page > 0 (PAG-03, D-417-05/06)"
    - "Assessment tanpa Section (semua SectionId=null) render byte-identik perilaku lama (no header, navigator flat, indikator tanpa label)"
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "StartExam isi field Section ke ExamQuestionItem + panggil SectionPaginator.ComputePages + clamp RESUME_PAGE + mobile UA sebelum compute"
      contains: "SectionPaginator.ComputePages"
    - path: "Views/CMP/StartExam.cshtml"
      provides: "Render grouping by PageNumber + header Section + navigator per-Section + indikator + resume RESUME_PAGE + toast info"
      contains: "GroupBy(q => q.PageNumber)"
  key_links:
    - from: "Controllers/CMPController.cs (StartExam)"
      to: "Helpers/SectionPaginator.cs"
      via: "panggil ComputePages(examQuestions, questionsPerPage) setelah loop build + mobile UA resolved"
      pattern: "SectionPaginator\\.ComputePages\\(examQuestions"
    - from: "Views/CMP/StartExam.cshtml (pagination loop)"
      to: "ExamQuestionItem.PageNumber"
      via: "Model.Questions.GroupBy(q => q.PageNumber) — bukan Skip/Take naif"
      pattern: "GroupBy\\(q => q\\.PageNumber\\)"
    - from: "Views/CMP/StartExam.cshtml (JS pageQuestionIds/allQuestionsData)"
      to: "ExamQuestionItem.PageNumber"
      via: "serialisasi dari q.PageNumber yang sama (anti-drift Pitfall 1)"
      pattern: "pageNumber = q\\.PageNumber"
    - from: "Views/CMP/StartExam.cshtml (resume handler)"
      to: "RESUME_PAGE"
      via: "currentPage = RESUME_PAGE (ganti hardcode 0) + toast"
      pattern: "currentPage = RESUME_PAGE"
---

<objective>
Wire `SectionPaginator.ComputePages` (Plan 01) ke `CMPController.StartExam` dan generalisasi `Views/CMP/StartExam.cshtml` dari pagination flat menjadi section-aware: header Section, page-break StartNewPage, auto-split "(lanjutan)", navigator per-Section, indikator ber-nama-Section, dan resume yang mendarat di halaman terhitung dengan toast informatif — SEMUA dengan invariant backward-compat no-Section byte-identik.

Purpose: Memenuhi PAG-01/02/03 di permukaan render ujian peserta. Satu sumber kebenaran (`q.PageNumber` dari controller) lintas Razor+JS untuk menghindari drift (Pitfall 1).
Output: `CMPController.StartExam` ter-wire, `StartExam.cshtml` section-aware. migration=FALSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/417-section-pagination/417-CONTEXT.md
@.planning/phases/417-section-pagination/417-RESEARCH.md
@.planning/phases/417-section-pagination/417-PATTERNS.md
@.planning/phases/417-section-pagination/417-UI-SPEC.md
@.planning/phases/417-section-pagination/417-01-SUMMARY.md

<interfaces>
<!-- Dari Plan 01 (sudah selesai). Executor pakai langsung. -->

SectionPaginator (Helpers/SectionPaginator.cs):
```csharp
public static void ComputePages(IList<ExamQuestionItem> ordered, int perPage);  // set PageNumber/IsSectionStart/IsSectionContinuation
public static int ClampResumePage(int requested, int maxPage);                  // di luar rentang → 0 (D-417-05)
```

ExamQuestionItem field baru (Models/PackageExamViewModel.cs):
```csharp
public int? SectionNumber { get; set; }          // null = "Lainnya"
public string? SectionName { get; set; }          // nama Section (name-only, D-417-01)
public bool SectionStartNewPage { get; set; }     // dari AssessmentPackageSection.StartNewPage
public int PageNumber { get; set; }               // 0-based hasil ComputePages
public bool IsSectionStart { get; set; }          // header polos
public bool IsSectionContinuation { get; set; }   // header "(lanjutan)"
```

q.Section SUDAH ter-Include di StartExam (CMPController.cs:1053, wiring Phase 416 Plan 02) — Section.SectionNumber/Name/StartNewPage tersedia di loop.
</interfaces>

<existing_anchors>
<!-- Line numbers terverifikasi saat planning (Read langsung). Jadikan jangkar edit in-place. -->

CMPController.cs (StartExam GET, package-path):
- L1208-1235: loop build `examQuestions` (object initializer ExamQuestionItem). `q.Section` ter-Include di L1053.
- L1224-1234: object initializer ExamQuestionItem yang akan ditambah field Section.
- L1265-1269: ViewBag block (`ViewBag.IsResume`, `ViewBag.LastActivePage = assessment.LastActivePage ?? 0`).
- L1329-1334: mobile UA detection (`ViewBag.QuestionsPerPage = 5`). ⚠️ PINDAHKAN ke SEBELUM ComputePages.
- L1336: `return View(vm)`.
- TAK DISENTUH: `.Include(q => q.Section)` (L1053), guard struktur Section (L1071-1127), reset-null (L1195), `UpdateSessionProgress` (L439-482).

StartExam.cshtml:
- L1-8: header `@{ }` — `questionsPerPage`, `totalPages = Ceiling(...)`.
- L84-90: pagination loop `@for` + `Skip(page*perPage).Take(perPage)`.
- L95-188: kartu soal (`qcard_@q.QuestionId`) — DIPERTAHANKAN VERBATIM.
- L190-212: page-nav Previous/Next/Submit — DIPERTAHANKAN VERBATIM.
- L442-444: const JS `TOTAL_QUESTIONS`/`QUESTIONS_PER_PAGE`/`TOTAL_PAGES`.
- L448: `const RESUME_PAGE = @(ViewBag.LastActivePage ?? 0)`.
- L464-470: `pageQuestionIds` (Skip/Take per p — GANTI ke GroupBy PageNumber).
- L472-479: `allQuestionsData` (`pageNumber = index/questionsPerPage` — GANTI ke q.PageNumber + tambah sectionName).
- L802-822: `showResumeFailureToast` (mekanisme toast — REUSE, clone jadi info).
- L1026-1050: `performPageSwitch` (titik update indikator).
- L1096-1138: `updatePanel()` (render badge flat — generalisasi per-Section grouping).
- L1118-1135: mirror `#drawerNumbers` (mobile).
- L1233-1239: handler `resumeConfirmBtn` HARDCODE `currentPage = 0` (Pitfall 5 — GANTI ke RESUME_PAGE).
- L1535-1542: CSS `#panelNumbers` grid 7-kolom (label grup pakai `grid-column: 1/-1`).
- L986-1024: guard `hasPendingSaves()` di changePage (IC-8 — JANGAN regresi).
</existing_anchors>

<read_before_planning>
Sudah dibaca + line numbers terverifikasi saat planning:
- Controllers/CMPController.cs:1205-1340 (StartExam loop + ViewBag + mobile UA)
- Views/CMP/StartExam.cshtml: L1-12, L83-95, L440-480, L800-825, L1026-1060, L1096-1140, L1228-1245, L1528-1545
</read_before_planning>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CMPController.StartExam — isi field Section + ComputePages + clamp resume + mobile UA</name>
  <read_first>
    - Controllers/CMPController.cs:1205-1340 (loop build examQuestions L1224-1234, ViewBag L1265-1269, mobile UA L1329-1334, return L1336)
    - Helpers/SectionPaginator.cs (ComputePages + ClampResumePage — dari Plan 01)
    - .planning/phases/417-section-pagination/417-RESEARCH.md ("Isi field Section saat build examQuestions" + "ViewBag.SectionConfig" + catatan ⚠️ pindahkan mobile UA SEBELUM compute + Pattern 3 clamp)
    - .planning/phases/417-section-pagination/417-PATTERNS.md (CMPController StartExam MODIFY (a)(b)(c)(d))
  </read_first>
  <action>
Edit `Controllers/CMPController.cs` di method `StartExam` (GET, package-path). Tiga edit in-place:

**(a) Isi field Section di object initializer (L1224-1234).** SETELAH `ImageAlt = q.ImageAlt,` tambahkan:
```csharp
                        // Phase 417: metadata section-aware (q.Section sudah ter-Include di :1053)
                        SectionNumber = q.Section?.SectionNumber,
                        SectionName = q.Section?.Name,
                        SectionStartNewPage = q.Section?.StartNewPage ?? false,  // "Lainnya"/null → tak paksa page-break (§15.A)
```

**(b) Pindahkan mobile UA detection SEBELUM ComputePages, lalu panggil ComputePages.** Blok mobile UA saat ini di L1329-1334 (`ViewBag.QuestionsPerPage = 5`) dievaluasi SETELAH vm dibangun, padahal `ComputePages` butuh `perPage`. Solusi:
1. PINDAHKAN blok deteksi mobile UA ke ATAS — tepat SETELAH loop `examQuestions` selesai dibangun (setelah L1235, sebelum `vm = new PackageExamViewModel`). Resolve `int questionsPerPage` lokal:
```csharp
                // Phase 417: tentukan perPage (mobile 5, desktop 10) SEBELUM compute page-map.
                int questionsPerPage = 10;
                var uaForPaging = Request.Headers["User-Agent"].ToString();
                if (uaForPaging.Contains("Mobile") || uaForPaging.Contains("Android") || uaForPaging.Contains("iPhone"))
                {
                    questionsPerPage = 5;
                }
                ViewBag.QuestionsPerPage = questionsPerPage;

                // Phase 417 PAG-01/02: hitung PageNumber section-aware atas urutan section (Section 1→2→…→Lainnya).
                HcPortal.Helpers.SectionPaginator.ComputePages(examQuestions, questionsPerPage);
```
2. HAPUS blok lama L1329-1334 (`ViewBag.QuestionsPerPage = 5`) supaya tak dobel-set. JANGAN hapus baris lain (mis. `ViewBag.AssessmentBatchKey` L1327 TETAP).
3. Verifikasi tak ada side-effect lain antara loop dan return yang bergantung pada urutan blok mobile UA lama (RESEARCH Assumption A2). Jika `vm` butuh `questionsPerPage`, tak perlu — view baca `ViewBag.QuestionsPerPage`.

**(c) Clamp RESUME_PAGE (ganti L1266).** Ganti baris `ViewBag.LastActivePage = assessment.LastActivePage ?? 0;` menjadi:
```csharp
                // Phase 417 PAG-03 / D-417-05: clamp page resume ke rentang valid; null/out-of-range → 0.
                int maxPage417 = examQuestions.Count > 0 ? examQuestions.Max(q => q.PageNumber) : 0;
                ViewBag.LastActivePage = HcPortal.Helpers.SectionPaginator.ClampResumePage(assessment.LastActivePage ?? 0, maxPage417);
```
⚠️ Pastikan blok ini berada SETELAH `ComputePages` dipanggil (PageNumber sudah terisi). Bila urutan kode menempatkan ViewBag block (L1265) sebelum loop selesai, pindahkan clamp ke titik setelah ComputePages.

Tidak menambah endpoint, tidak mengubah grading/skor/auth. migration=FALSE.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SectionPaginator.ComputePages(examQuestions" Controllers/CMPController.cs` → match
    - `grep -n "SectionNumber = q.Section?.SectionNumber" Controllers/CMPController.cs` → match
    - `grep -n "ClampResumePage" Controllers/CMPController.cs` → match
    - `grep -c "ViewBag.QuestionsPerPage = 5" Controllers/CMPController.cs` → 0 (blok lama dihapus; sekarang via questionsPerPage)
    - `dotnet build HcPortal.csproj` → 0 error
    - `git status --porcelain Migrations/ Data/` → kosong
  </acceptance_criteria>
  <done>StartExam isi 3 field Section, panggil ComputePages dgn perPage benar (mobile 5), clamp resume; build 0 error; no migration.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: StartExam.cshtml — render grouping + header Section + JS page-maps single-source (PAG-01/02)</name>
  <read_first>
    - Views/CMP/StartExam.cshtml:1-12 (header @{}), L83-95 (loop+Skip/Take), L95-212 (kartu soal + page-nav VERBATIM), L442-479 (const JS + pageQuestionIds + allQuestionsData)
    - .planning/phases/417-section-pagination/417-UI-SPEC.md (IC-1/IC-2/IC-3 header + warna text-only no-band + typografi fw-semibold text-primary + copywriting "(lanjutan)")
    - .planning/phases/417-section-pagination/417-RESEARCH.md (Pattern 2 render exam-page per computed page + Pitfall 1/2/3/4)
    - .planning/phases/417-section-pagination/417-PATTERNS.md (StartExam.cshtml MODIFY (1)(2)(3)(4))
  </read_first>
  <action>
Edit `Views/CMP/StartExam.cshtml`. JAGA backward-compat: branch `hasSections`.

**(1) Header @{} (L7) — totalPages dari computed page + hasSections.** Ganti baris `int totalPages = (int)Math.Ceiling((double)Model.TotalQuestions / questionsPerPage);` (L7) dengan:
```razor
    var pageGroups = Model.Questions.GroupBy(q => q.PageNumber).OrderBy(g => g.Key).ToList();
    int totalPages = pageGroups.Count > 0 ? pageGroups.Count : 1;   // Pitfall 2: bukan Ceiling
    bool hasSections = Model.Questions.Any(q => q.SectionNumber != null);
```

**(2) Pagination loop (L84-90) — group by PageNumber (Pitfall 1).** Ganti `@for (int page...)` + `Skip/Take` dengan iterasi `pageGroups`:
```razor
                    @* Pages: section-aware grouping by computed PageNumber (Phase 417) *@
                    @foreach (var pg in pageGroups)
                    {
                        var pageQuestions = pg.ToList();
                        <div class="exam-page" id="page_@(pg.Key)"
                             style="display: @(pg.Key == 0 ? "block" : "none")">
                            @foreach (var q in pageQuestions)
                            {
                                @* (3) Header Section disisipkan di sini *@
                                @* ...kartu soal EXISTING (qcard_@q.QuestionId) TAK BERUBAH... *@
                            }
                            @* ...page-nav EXISTING Previous/Next/Submit TAK BERUBAH... *@
                        </div>
                    }
```
Kartu soal (L95-188) dan page-nav (L190-212) DIPERTAHANKAN VERBATIM di dalam loop baru — hanya wadah loop yang berubah.

**(3) Header Section (D-417-01/02, IC-1/IC-2).** Sisipkan SEBELUM kartu soal, di awal `@foreach (var q in pageQuestions)`, HANYA bila `hasSections`:
```razor
                                @if (hasSections && (q.IsSectionStart || q.IsSectionContinuation))
                                {
                                    <div class="text-primary fw-semibold border-bottom pb-1 mb-2">
                                        @q.SectionName
                                        @if (q.IsSectionContinuation)
                                        { <span class="text-muted small fw-normal">(lanjutan)</span> }
                                    </div>
                                }
```
JANGAN band warna full-width (UI-SPEC: text-only). `@q.SectionName` Razor auto-encode (XSS-safe, V14).

**(4) JS page-maps single-source (Pitfall 1).** 
- `TOTAL_PAGES` (L444): biarkan `@totalPages` (sudah pakai totalPages baru).
- `pageQuestionIds` (L464-470): GANTI Skip/Take → GroupBy PageNumber:
```razor
    const pageQuestionIds = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(
        Model.Questions.GroupBy(q => q.PageNumber).OrderBy(g => g.Key)
            .Select(g => g.Select(q => q.QuestionId).ToList()).ToList()
    ));
```
- `allQuestionsData` (L472-479): GANTI `pageNumber = index/questionsPerPage` → `q.PageNumber` + tambah `sectionName`:
```razor
    const allQuestionsData = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(
        Model.Questions.Select(q => new {
            questionId = q.QuestionId,
            pageNumber = q.PageNumber,
            displayNumber = q.DisplayNumber,
            sectionName = q.SectionName
        }).ToList()
    ));
```
- Tambah `pageSectionMap` (page→sectionName, RESEARCH OQ#2, O(1) lookup indikator) tepat setelah `allQuestionsData`:
```razor
    const pageSectionMap = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(
        Model.Questions.GroupBy(q => q.PageNumber).OrderBy(g => g.Key)
            .Select(g => g.First().SectionName).ToList()
    ));
    const HAS_SECTIONS = @Json.Serialize(hasSections);
```

JANGAN sentuh kartu soal, page-nav, timer, autosave. migration=FALSE.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GroupBy(q => q.PageNumber)" Views/CMP/StartExam.cshtml` → ≥3 match (loop/pageGroups, pageQuestionIds, pageSectionMap)
    - `grep -n "bool hasSections = Model.Questions.Any(q => q.SectionNumber != null)" Views/CMP/StartExam.cshtml` → match
    - `grep -n "pageNumber = q.PageNumber" Views/CMP/StartExam.cshtml` → match
    - `grep -n "(lanjutan)" Views/CMP/StartExam.cshtml` → match
    - `grep -c "Skip(page \* questionsPerPage)" Views/CMP/StartExam.cshtml` → 0 (Skip/Take naif dihapus)
    - `grep -n "const pageSectionMap" Views/CMP/StartExam.cshtml` → match
    - `dotnet build HcPortal.csproj` → 0 error
  </acceptance_criteria>
  <done>Loop render by PageNumber, header Section (text-only, name + "(lanjutan)") branch hasSections, JS page-maps dari q.PageNumber (anti-drift); build 0 error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: StartExam.cshtml — navigator per-Section + indikator + resume RESUME_PAGE + toast (PAG-03, D-417-03/04/05/06)</name>
  <read_first>
    - Views/CMP/StartExam.cshtml: L802-822 (showResumeFailureToast — clone jadi info), L1026-1050 (performPageSwitch — titik update indikator), L1096-1138 (updatePanel render badge flat), L1118-1135 (drawerNumbers mirror), L1233-1239 (resumeConfirmBtn hardcode currentPage=0), L1535-1542 (CSS grid #panelNumbers 7-kolom)
    - .planning/phases/417-section-pagination/417-UI-SPEC.md (IC-4 navigator grouping grid-column:1/-1 + IC-5 indikator format + IC-6 resume toast reuse + IC-8 jangan regresi flush + copywriting)
    - .planning/phases/417-section-pagination/417-RESEARCH.md (Pattern 4 navigator + Pattern 3 resume/toast + Pitfall 5 resume + XSS textContent/innerText)
    - .planning/phases/417-section-pagination/417-PATTERNS.md (StartExam.cshtml MODIFY (5)(6)(7) + Shared Patterns XSS/toast/flush)
  </read_first>
  <action>
Edit `Views/CMP/StartExam.cshtml` — JS. JAGA IC-8 (jangan regresi `hasPendingSaves()` guard). XSS-safe: pakai `.textContent`/`.innerText`, JANGAN `innerHTML` untuk nama Section.

**(5) Navigator per-Section (D-417-03, IC-4).** Generalisasi `updatePanel()` (L1096-1138). Saat ini iterasi `allQuestionsData.forEach` render badge flat di `#panelNumbers` + mirror `#drawerNumbers`. Ubah jadi: bila `HAS_SECTIONS`, deteksi pergantian `q.sectionName` → sisipkan baris label full-width SEBELUM grup badge; bila tidak, render flat seperti sekarang. Label pakai elemen full-width:
```javascript
        // Phase 417 D-417-03: label grup Section (full-width, tak konsumsi sel grid badge)
        function appendSectionLabel(container, name) {
            var lbl = document.createElement('div');
            lbl.className = 'small text-muted fw-bold mt-2 mb-1';
            lbl.style.gridColumn = '1 / -1';          // baris penuh di grid 7-kolom
            lbl.textContent = name || 'Lainnya';       // XSS-safe (HC-controlled)
            container.appendChild(lbl);
        }
```
Di loop render badge (desktop `#panelNumbers` DAN mirror `#drawerNumbers`): lacak `lastSection` per-container; bila `HAS_SECTIONS && q.sectionName !== lastSection` → `appendSectionLabel(container, q.sectionName)` lalu set `lastSection = q.sectionName`. Backward-compat: bila `!HAS_SECTIONS`, jangan panggil appendSectionLabel → render flat identik. Badge tetap pakai styling existing (`badge rounded-pill bg-success/bg-secondary`, `jumpToQuestion(q.questionId, q.pageNumber)`).

**(6) Resume → RESUME_PAGE + toast (Pitfall 5, D-417-05/06, IC-6).** 
- Handler `resumeConfirmBtn` (L1233-1239): GANTI `currentPage = 0;` → gunakan RESUME_PAGE (server sudah clamp di Task 1):
```javascript
        document.getElementById('resumeConfirmBtn').addEventListener('click', function() {
            resumeModal.hide();
            currentPage = (typeof RESUME_PAGE === 'number' && RESUME_PAGE >= 0) ? RESUME_PAGE : 0;
            document.getElementById('page_' + currentPage).style.display = 'block';
            updatePanel();
            updateMobileNavButtons();
            window.scrollTo(0, 0);
            if (currentPage > 0) {
                var firstQid = pageQuestionIds[currentPage] && pageQuestionIds[currentPage][0];
                var num = getDisplayNumForQuestion(firstQid);
                showResumeInfoToast('Lanjut dari soal no. ' + num + '.');
            }
        });
```
⚠️ Pastikan elemen `page_0` di-hide dulu bila currentPage>0 (default page0 `display:block` dari Razor). Tambahkan sebelum set display: bila `currentPage !== 0`, `document.getElementById('page_0').style.display = 'none';`.

- Tambah fungsi `showResumeInfoToast` (clone `showResumeFailureToast` L802-822, REUSE container `#resumeToastContainer` + `bootstrap.Toast`, ganti kelas `text-bg-warning` → `text-bg-info`, ikon `bi-info-circle-fill`):
```javascript
        function showResumeInfoToast(message) {
            let toastContainer = document.getElementById('resumeToastContainer');
            if (!toastContainer) {
                toastContainer = document.createElement('div');
                toastContainer.id = 'resumeToastContainer';
                toastContainer.className = 'position-fixed bottom-0 end-0 p-3';
                toastContainer.style.zIndex = '1100';
                document.body.appendChild(toastContainer);
            }
            const toastId = 'resumeInfoToast';
            const toastHtml = '<div id="' + toastId + '" class="toast align-items-center text-bg-info border-0" role="alert" aria-live="polite" aria-atomic="true" data-bs-autohide="true" data-bs-delay="6000">' +
                '<div class="d-flex"><div class="toast-body fw-semibold"><i class="bi bi-info-circle-fill me-2"></i></div>' +
                '<button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button></div></div>';
            toastContainer.innerHTML = toastHtml;
            // XSS-safe: set pesan via textContent, bukan innerHTML
            toastContainer.querySelector('#' + toastId + ' .toast-body').textContent = '';
            var body = toastContainer.querySelector('#' + toastId + ' .toast-body');
            var icon = document.createElement('i'); icon.className = 'bi bi-info-circle-fill me-2'; body.appendChild(icon);
            body.appendChild(document.createTextNode(message));
            const toastEl = document.getElementById(toastId);
            new bootstrap.Toast(toastEl).show();
        }
```
(Pesan `message` berisi nomor soal — aman, tapi konsisten pakai textContent.)

**(7) Indikator halaman ber-Section (D-417-04, IC-5).** Di `performPageSwitch` (L1026-1050) DAN saat init/resume, update elemen indikator. Jika belum ada elemen indikator khusus, tambahkan span dekat page-nav (mis. id `pageSectionIndicator`) di Razor page-nav area, lalu update di JS:
```javascript
        function updatePageIndicator() {
            var el = document.getElementById('pageSectionIndicator');
            if (!el) return;
            var human = currentPage + 1;
            var secName = HAS_SECTIONS ? (pageSectionMap[currentPage] || 'Lainnya') : null;
            // XSS-safe: textContent
            el.textContent = secName
                ? (secName + ' — Halaman ' + human + '/' + TOTAL_PAGES)
                : ('Halaman ' + human + '/' + TOTAL_PAGES);
        }
```
Panggil `updatePageIndicator()` di akhir `performPageSwitch` dan setelah set currentPage di resume/init. Tambah elemen `<span id="pageSectionIndicator" class="text-muted small"></span>` di area page-navigation (dekat tombol Previous/Next di `StartExam.cshtml`, baik desktop maupun mobile footer bila ada — letak per IC-5). Backward-compat: tanpa Section → "Halaman n/total" saja.

**IC-8 (JANGAN regresi):** guard `hasPendingSaves()` di `changePage` (L986-1024) PERTAHANKAN apa adanya. `updateMobileNavButtons()` (L1138-1153) pakai `TOTAL_PAGES` baru (sudah Task 2). JANGAN ubah timer/autosave/flushEssay.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug --nologo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function appendSectionLabel" Views/CMP/StartExam.cshtml` → match
    - `grep -n "gridColumn = '1 / -1'" Views/CMP/StartExam.cshtml` → match
    - `grep -n "currentPage = (typeof RESUME_PAGE" Views/CMP/StartExam.cshtml` → match
    - `grep -n "function showResumeInfoToast" Views/CMP/StartExam.cshtml` → match
    - `grep -n "Lanjut dari soal no." Views/CMP/StartExam.cshtml` → match
    - `grep -n "function updatePageIndicator" Views/CMP/StartExam.cshtml` → match
    - `grep -n "id=\"pageSectionIndicator\"" Views/CMP/StartExam.cshtml` → match
    - `grep -c "currentPage = 0;" Views/CMP/StartExam.cshtml` di handler resume → 0 (hardcode dihapus; pola `(typeof RESUME_PAGE...` menggantikan)
    - `grep -c "\.innerHTML = " Views/CMP/StartExam.cshtml` tidak bertambah untuk nama Section (label/indikator pakai textContent)
    - `grep -n "hasPendingSaves()" Views/CMP/StartExam.cshtml` → masih ada (IC-8 tak regresi)
    - `dotnet build HcPortal.csproj` → 0 error
  </acceptance_criteria>
  <done>Navigator per-Section (label grid-column:1/-1 XSS-safe), resume mendarat di RESUME_PAGE + toast info "Lanjut dari soal no. X", indikator "NamaSection — Halaman n/total", IC-8 flush guard utuh; build 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → CMPController.StartExam (GET) | Worker meminta render ujian-nya sendiri. Authz owner/Admin/HC existing (CMPController.cs:920) + guard removed (Phase 409) + guard struktur Section (L1071-1127) TAK diubah. |
| client → CMPController.UpdateSessionProgress | Autosave kirim `currentPage` (page-index). Existing; tak diubah Plan 417. |
| HC-controlled data (nama Section) → render (Razor + JS) | Nama Section ditulis HC, di-render di header/navigator/indikator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-417-03 | Tampering/Info | XSS via nama Section di header/navigator/indikator | mitigate | Razor `@q.SectionName` auto-encode (header). JS navigator/indikator/toast pakai `.textContent`/`.innerText` (JANGAN `innerHTML`) — pola existing `btn.innerText` (StartExam.cshtml:1106). Diverifikasi via acceptance grep + e2e (Plan 03). |
| T-417-04 | Tampering | Client memalsukan `currentPage`/`LastActivePage` lompat soal | mitigate | Page compute server-authoritative (ComputePages di controller); resume clamp server-side via `ClampResumePage` (Task 1). Page-index BUKAN kontrol akses — semua soal milik worker sudah terkirim by design (existing). Risiko rendah. |
| T-417-05 | Tampering (by-design) | Section config diubah HC pasca-lock → page shift | accept | Aturan bisnis (D-417-05): recompute dari config + identitas soal stabil by question id + fallback page 0. Bukan ancaman eksternal. Safety-net `SavedQuestionCount` existing (CMPController.cs:1179) tak diubah. |
| T-417-06 | EoP / Access Control | Endpoint mutasi baru | accept | N/A — Plan 417 tak menambah endpoint mutasi. Quick-button admin sudah ada+ber-`[Authorize][ValidateAntiForgeryToken]` (Phase 415). Tak ubah grading/skor/sertifikat/RBAC. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` → 0 error.
- `dotnet test HcPortal.Tests` → baseline 665+ hijau, 0 regresi (full suite saat wave merge).
- Runtime @localhost:5277 (lesson 354 Razor/JS WAJIB): boot AD-off `dotnet run`, GET `/` → HTTP 200, render StartExam page → 200 tanpa Razor compile error (smoke; UAT penuh di Plan 03).
- `git status --porcelain Migrations/ Data/` → kosong (migration=FALSE).
</verification>

<success_criteria>
- StartExam ber-Section: header NAMA Section saat ganti Section + "(lanjutan)" auto-split; StartNewPage mulai exam-page baru; Section >perPage pecah per perPage.
- Navigator (#panelNumbers + #drawerNumbers) per-Section label grup (full-width); indikator "NamaSection — Halaman n/total".
- Resume mendarat RESUME_PAGE terhitung (bukan 0) + toast info "Lanjut dari soal no. X"; out-of-range → page 0.
- Assessment tanpa Section render byte-identik (no header, navigator flat, indikator tanpa label) — branch hasSections.
- JS page-maps dari q.PageNumber yang sama (anti-drift). IC-8 flush guard utuh. XSS-safe. Build 0 error; migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/417-section-pagination/417-02-SUMMARY.md`
</output>
