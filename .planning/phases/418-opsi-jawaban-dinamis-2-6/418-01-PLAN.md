---
phase: 418-opsi-jawaban-dinamis-2-6
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/OptionValidationTests.cs
  - HcPortal.Tests/EditShrinkGuardLogicTests.cs
autonomous: true
requirements: [OPT-03]
tags: [validation, options, guard, tdd]

must_haves:
  truths:
    - "Validator menolak soal MC/MA dengan >6 opsi terisi"
    - "Validator menerima soal dengan 5 dan 6 opsi valid"
    - "Validator menolak opsi ber-flag-benar yang teksnya kosong walau total ≤6"
    - "Logika edit-shrink (irisan removedOptionIds ∩ answeredOptionIds) terbukti benar dgn pure unit test"
  artifacts:
    - path: "HcPortal.Tests/OptionValidationTests.cs"
      provides: "Fact max-6 reject + 5/6-opsi accept + correct-tanpa-teks reject (array-6)"
      contains: "MaxSix"
    - path: "HcPortal.Tests/EditShrinkGuardLogicTests.cs"
      provides: "Pure-logic test irisan removedOptionIds ∩ responseOptionIds (D-418-02)"
      contains: "EditShrinkGuard"
  key_links:
    - from: "HcPortal.Tests/OptionValidationTests.cs"
      to: "Helpers/QuestionOptionValidator.cs"
      via: "ValidateQuestionOptions(type, texts[6], corrects[6])"
      pattern: "ValidateQuestionOptions"
---

<objective>
Tulis test Wave-0 (RED) untuk dua aturan murni Fase 418 SEBELUM kode produksi ada: (1) batas **max-6** pada `QuestionOptionValidator` (OPT-03), (2) logika **edit-shrink guard** (irisan `removedOptionIds ∩ answeredOptionIds`, D-418-02) sebagai fungsi murni yang bisa diuji tanpa DB.

Purpose: Per 418-VALIDATION.md, ini adalah Wave 0 — semua REQ acceptance harus punya test otomatis lebih dulu. Validator sudah min-2 + correct-must-have-text (sudah ada `OptionValidationTests`); kita hanya MENAMBAH Fact max-6 + extend ke array-6, dan menambah file test logika guard murni. Ini mengunci kontrak sebelum Plan 02 menulis produksi.

Output: 2 file test (1 extend, 1 baru) yang merah sampai Plan 02 menambahkan `if (filled > 6)` ke validator + helper logika guard. migration=FALSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-CONTEXT.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-RESEARCH.md
@.planning/phases/418-opsi-jawaban-dinamis-2-6/418-VALIDATION.md

<interfaces>
<!-- Kontrak validator yang diuji — sudah ada di Helpers/QuestionOptionValidator.cs -->
```csharp
// Helpers/QuestionOptionValidator.cs:20 (signature TIDAK berubah)
public static (bool ok, string? error) ValidateQuestionOptions(string type, string?[] texts, bool[] corrects);
// type: "MultipleChoice" | "MultipleAnswer" | "Essay"(bypass)
// Saat ini: filled<2 → reject ; correct-tanpa-teks → reject. Plan 02 menambah: filled>6 → reject "Maksimal 6 opsi per soal."
```

Edit-shrink guard adalah logika murni (intersection set) yang Plan 02 akan tempatkan di controller. Plan ini menguji intersection-nya sebagai fungsi murni statis (lihat Task 2 untuk signature yang harus disepakati).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend OptionValidationTests — max-6 reject + 5/6-opsi accept + correct-tanpa-teks (array-6)</name>
  <read_first>
    - HcPortal.Tests/OptionValidationTests.cs (pelajari pola Fact existing min-2 + naming)
    - Helpers/QuestionOptionValidator.cs (signature + pesan error existing)
    - 418-RESEARCH.md §"Validator Max-6 (OPT-03)" (pesan persis: "Maksimal 6 opsi per soal.")
  </read_first>
  <behavior>
    - Test `MaxSix_Rejected`: type="MultipleChoice", texts = 7 opsi terisi (mis. {"A".."G"} — 7 teks), corrects[index0]=true → `ok=false`, error mengandung "Maksimal 6 opsi".
    - Test `FiveOptions_Accepted`: type="MultipleAnswer", 5 opsi terisi, 2 correct ber-teks → `ok=true`, error=null.
    - Test `SixOptions_Accepted`: type="MultipleChoice", 6 opsi terisi, 1 correct ber-teks → `ok=true`, error=null.
    - Test `SixOpt_CorrectWithoutText_Rejected`: type="MultipleAnswer", array panjang 6, opsi index 4 ber-flag benar TAPI teks kosong/null → `ok=false`, error mengandung "berisi teks" (pesan correct-tanpa-teks existing).
    - Regresi: pastikan Fact min-2 + correct-tanpa-teks lama tetap ada & hijau (jangan hapus).
  </behavior>
  <action>
    Tambahkan 4 `[Fact]` baru ke `HcPortal.Tests/OptionValidationTests.cs` mengikuti pola Fact existing (panggil `QuestionOptionValidator.ValidateQuestionOptions(type, texts, corrects)`, assert `result.ok` + `Assert.Contains(...substring..., result.error)`). Gunakan array panjang 6 atau 7 untuk menguji batas (validator sudah variable-length via `texts.Count(...)` + loop `i < texts.Length`). JANGAN edit produksi di plan ini — `MaxSix_Rejected` MUST RED (validator belum punya `filled > 6` check). Commit RED dulu (test gagal), produksi GREEN menyusul di Plan 02.
    Pesan error yang di-assert harus cocok dgn Copywriting Contract UI-SPEC C5: max-6 = "Maksimal 6 opsi per soal." ; correct-tanpa-teks = pesan existing validator (mengandung "berisi teks").
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~OptionValidation" 2>&1 | tail -20</automated>
  </verify>
  <done>4 Fact baru ada di OptionValidationTests.cs; `MaxSix_Rejected` GAGAL (RED) karena produksi belum cek >6; `FiveOptions_Accepted`/`SixOptions_Accepted` mungkin sudah hijau (validator agnostik panjang) — itu OK; Fact min-2 lama tetap hijau. Commit `test(418-01): add max-6 + 5/6-opsi Facts (RED)`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: New EditShrinkGuardLogicTests — pure intersection logic (D-418-02)</name>
  <read_first>
    - 418-RESEARCH.md §"Edit-Shrink Guard (D-418-02)" (pola removedOptionIds + intersection)
    - 418-CONTEXT.md D-418-02 (tolak hapus opsi yang sudah dijawab)
    - HcPortal.Tests/OptionValidationTests.cs (pola pure unit, no DbContext)
    - Helpers/QuestionOptionValidator.cs (contoh helper static pure di Helpers/)
  </read_first>
  <behavior>
    Logika murni: diberi (a) daftar Id opsi existing yang AKAN dihapus (`removedOptionIds`) dan (b) daftar Id opsi yang sudah dijawab peserta (`answeredOptionIds`), guard memutuskan blocked = ada irisan.
    - `Blocked_WhenRemovedOptionWasAnswered`: removed={10,11}, answered={11,99} → blocked=true, daftar blokir mengandung 11.
    - `Allowed_WhenNoRemovedOptionAnswered`: removed={12}, answered={11,99} → blocked=false.
    - `Allowed_WhenNothingRemoved`: removed={}, answered={11} → blocked=false.
    - `Blocked_ListContainsOnlyIntersection`: removed={10,11,12}, answered={11,12,13} → blocked=true, hasil irisan = {11,12} (distinct).
  </behavior>
  <action>
    Buat helper logika murni statis `OptionShrinkGuard` (LOKASI: `Helpers/OptionShrinkGuard.cs` — TAPI Plan ini hanya menulis TEST yang memanggilnya; Plan 02 menciptakan helper produksinya). Sepakati signature yang Plan 02 WAJIB implementasikan, dokumentasikan di test sebagai kontrak:
    ```csharp
    // Helpers/OptionShrinkGuard.cs (DIBUAT Plan 02)
    public static IReadOnlyList<int> FindBlockedOptionIds(IEnumerable<int> removedOptionIds, IEnumerable<int> answeredOptionIds);
    // return = removedOptionIds ∩ answeredOptionIds (distinct). Empty = boleh hapus.
    ```
    Tulis `HcPortal.Tests/EditShrinkGuardLogicTests.cs` dengan 4 Fact di atas memanggil `OptionShrinkGuard.FindBlockedOptionIds(...)`. Karena `Helpers/OptionShrinkGuard.cs` BELUM ADA, test ini TIDAK akan compile sampai Plan 02 membuatnya — itulah RED yang benar (genuinely-missing symbol drives RED). Commit RED. (Catatan executor: bila harness butuh build hijau untuk commit, buat stub `Helpers/OptionShrinkGuard.cs` yang `throw new NotImplementedException()` di Plan ini agar test compile+RED, lalu Plan 02 mengisi implementasi nyata. Pilih salah satu — dokumentasikan di SUMMARY.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~EditShrinkGuard" 2>&1 | tail -20</automated>
  </verify>
  <done>EditShrinkGuardLogicTests.cs ada dgn 4 Fact memanggil `OptionShrinkGuard.FindBlockedOptionIds`; test RED (symbol belum ada / NotImplementedException). Kontrak signature terdokumentasi. Commit `test(418-01): add edit-shrink guard pure-logic tests (RED)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (tidak ada) | Plan test-only. Tidak ada input lintas-boundary di-eksekusi; menguji fungsi murni in-process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-418-01 | Tampering | `QuestionOptionValidator` (server-authoritative input validation) | mitigate | Test mengunci bahwa validator menolak >6 opsi — mencegah bypass batas dari form/import (defense-in-depth server-side, ASVS V5). |
| T-418-02 | Denial of Service / Tampering | edit-shrink intersection logic | mitigate | Test mengunci logika yang mendasari guard FK-Restrict (D-418-02) — mencegah 500 mentah saat hapus opsi terjawab. Kode produksi guard di Plan 02. |
</threat_model>

<verification>
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~OptionValidation"` — Fact max-6 RED (sampai Plan 02).
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~EditShrinkGuard"` — RED (symbol/impl belum ada).
- `dotnet build HcPortal.csproj` 0-error (bila pakai stub helper agar compile).
- Tidak ada perubahan produksi yang mengubah perilaku (kecuali stub helper opsional). Suite existing lain tetap hijau.
</verification>

<success_criteria>
- 4 Fact validator baru (max-6 reject, 5-opsi accept, 6-opsi accept, correct-tanpa-teks-6 reject) ada di OptionValidationTests.cs.
- 4 Fact guard pure-logic ada di EditShrinkGuardLogicTests.cs memanggil kontrak `OptionShrinkGuard.FindBlockedOptionIds`.
- Test merah pada aturan baru (RED state benar — produksi belum ada), Fact lama tetap hijau.
- Kontrak signature helper guard terdokumentasi untuk Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/418-opsi-jawaban-dinamis-2-6/418-01-SUMMARY.md`
</output>
