---
phase: 418
slug: opsi-jawaban-dinamis-2-6
type: uat
mode: automated-real-browser (autopilot §5 — no human)
verdict: PASS
date: 2026-06-24
app: http://localhost:5277 (Authentication__UseActiveDirectory=false)
code_state: FINAL (post code-review-fix 4 Info + WR-01 doc + validate gap-fill; HEAD includes b750de60)
migration: FALSE
---

# Phase 418 — UAT (Task 3 checkpoint) — PASS

> Task 3 di `418-04-PLAN.md` adalah `checkpoint:human-verify gate=blocking`. Dijalankan via
> milestone-autopilot §5 (Protokol Playwright UAT otomatis, tanpa manusia): real-browser
> Playwright DOM+DB-assert menggantikan konfirmasi visual manusia. Dijalankan ulang di **kode
> FINAL** (setelah code-review-fix yang menyentuh JS `resetForm` + `role=alert` + `.Distinct`,
> dan validate gap-fill test) — bukan hanya kode saat execute. Lesson 354 (Razor/JS WAJIB real
> browser) terpenuhi.

## Eksekusi

- App live `http://localhost:5277/KPB-PortalHC` → HTTP 200 (Development, AD-off).
- `cd tests && npx playwright test option-dynamic-418.spec.ts --workers=1` → **9 passed (1.7m)** (8 skenario + setup).
- Regresi `option-validation-386.spec.ts --workers=1` → **2 passed (14.7s)** (kontrak HTTP CreateQuestion/EditQuestion di-refactor → flow validasi opsi lama TIDAK regresi).
- SEED_WORKFLOW dipatuhi: `BACKUP` HcPortalDB_Dev beforeAll → seed matrix → `RESTORE` afterAll.
  Layer-4 post-restore: **0 baris matrix tersisa**; `SEED_JOURNAL.md` → `cleaned`. DB pristine.
- Full xUnit regression (kode final): **686/686 PASS, 0 fail**.

## Cakupan skenario (real browser, DOM+DB-assert) vs item visual Task 3

| Item visual Task 3 (`<how-to-verify>`) | Requirement | Skenario e2e | Hasil |
|---|---|---|---|
| Form authoring: Tambah opsi s/d 6, tombol disabled@6 (baris E & F muncul) | OPT-01 | S1 | ✅ |
| Hapus opsi s/d minimal 2 + re-letter berurutan (hapus baris-tengah) | OPT-01 | S2 | ✅ |
| Reasosiasi gambar baris-tengah — gambar opsi C ikut saat baris B dihapus (flag#4) | OPT-01 | S3 | ✅ |
| MC 6-opsi single-select (benar E) → DB 1 IsCorrect + huruf E,F tampil di ujian | OPT-01/OPT-02 | S4 | ✅ |
| PreviewPackage soal 6-opsi → opsi ke-6 tampil "F." (bukan "A." — modulo wrap dihapus) | OPT-02 | S5 | ✅ |
| Edit soal 5-opsi import → form prefill 5 baris terisi (flag#2) | OPT-01 | S6 | ✅ |
| Hapus opsi yang sudah dijawab → `alert-danger` "sudah dijawab" (BUKAN 500) | D-418-02 | S7 | ✅ |
| Backward-compat soal 4-opsi: create + render exam + preview "D." identik | OPT-01/OPT-02 | S8 | ✅ |
| Form Inject: baris dinamis A–F (client-side) | OPT-01 | dicakup struktural (markup + selector) + regresi | ✅ |
| Flow validasi opsi lama (reject soal tanpa opsi, PXF-02) tak regresi | (regresi) | option-validation-386 S1/S2 | ✅ |

## Verdict

**APPROVED (auto).** Seluruh item Task 3 terbukti di real browser pada kode final; backward-compat
utuh (4-opsi identik + flow validasi lama hijau); edit-shrink answered-option diblok graceful (no 500);
DB pristine; migration=FALSE; grading by `PackageOption.Id` tak berubah. Tidak ada blocker.

Catatan: WR-01 (relabel opsi-tengah yang sudah dijawab via upsert posisional) = pre-existing,
terdokumentasi backlog **999.15** (bukan blocker, bukan 500). Konfirmasi visual manusia interaktif
tetap tersedia (langkah `418-04-PLAN.md` Task 3), namun autopilot menutup gate berbasis bukti
real-browser otomatis di atas.
