---
phase: 419-export-label-section-polish-test-uat-milestone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HcPortal.Tests/ExportSectionLabelTests.cs
  - HcPortal.Tests/SectionEtWarningTests.cs
  # NOTE: LinkPrePostSectionGuardTests.cs DIBATALKAN — D-02/Plan 03 di-drop ke backlog 999.16 (2026-06-24).
  - tests/e2e/section-lifecycle-419.spec.ts
  - tests/e2e/inject-section-419.spec.ts
  - tests/e2e/linkprepost-section-419.spec.ts
  - tests/e2e/addremove-section-419.spec.ts
autonomous: true
requirements: [PAG-04]
must_haves:
  truths:
    - "Suite gagal/RED secara terkendali untuk perilaku export label, guard LinkPrePost, dan ET-warning positif yang belum diimplementasikan (kontrak test = sumber kebenaran Wave 2)"
    - "Test lama (415-418) tetap hijau (kompatibel-mundur — Section kosong tak terdampak)"
    - "4 spec Playwright stub e2e ada sebagai skeleton (test.fixme) untuk 4 skenario D-04, siap diisi di Plan 05"
  artifacts:
    - path: "HcPortal.Tests/ExportSectionLabelTests.cs"
      provides: "Test PAG-04 export Excel band-header + ordering + backward-compat no-Section"
      contains: "ExportSectionLabelTests"
    - path: "HcPortal.Tests/LinkPrePostSectionGuardTests.cs"
      provides: "Test D-02 guard block-on-mismatch + skip-on-all-Lainnya (real-DB SectionFixture)"
      contains: "LinkPrePostSectionGuardTests"
    - path: "HcPortal.Tests/SectionEtWarningTests.cs"
      provides: "Test D-03 positif (predikat ET-warning FIRE lintas-sibling) + IN-01 group-by-SectionNumber"
      contains: "SectionEtWarningTests"
    - path: "tests/e2e/section-lifecycle-419.spec.ts"
      provides: "Stub e2e D-04.1 lifecycle Section inti"
      contains: "section-lifecycle"
  key_links:
    - from: "HcPortal.Tests/*Tests.cs"
      to: "HcPortal.Tests/SectionFixture.cs"
      via: "IClassFixture<SectionFixture> + AddPackageWithSectionsAsync helper reuse"
      pattern: "IClassFixture<SectionFixture>"
---

<objective>
Buat scaffolding test (RED) Wave 0 untuk Phase 419: tiga file xUnit baru yang mengunci kontrak perilaku PAG-04 (export label Section), D-02 (guard LinkPrePost × Section), dan D-03 (ET-warning positif), plus empat skeleton spec Playwright untuk keempat skenario UAT D-04. Test ini RED/pending sekarang — Wave 2 (Plan 02/03/04) membuatnya GREEN, Plan 05 mengisi e2e.

Purpose: Nyquist — semua perilaku produksi 419 punya `<automated>` verify SEBELUM implementasi. Kontrak test = sumber kebenaran; menghindari shallow execution.
Output: 3 file xUnit baru (RED) + 4 skeleton e2e (test.fixme) + full suite lama tetap hijau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-CONTEXT.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md

<interfaces>
<!-- Kontrak yang dipakai test ini. Diekstrak dari codebase — executor pakai langsung, JANGAN eksplorasi ulang. -->

Helper export (PAG-04) — Helpers/ExcelExportHelper.cs:
  public static void AddDetailPerSoalSheet(
      XLWorkbook workbook,
      List<AssessmentSession> sessions,
      List<PackageUserResponse> responses,
      List<PackageQuestion> questions);   // saat ini OrderBy(q.Order) flat — Plan 02 ubah ke (SectionNumber, Order)

Comparator (REUSE D-02) — Helpers/SectionStructureComparer.cs:
  public const int LainnyaKey = int.MinValue;
  public static int KeyOf(int? sectionNumber);                 // null -> LainnyaKey
  public static List<int> MismatchedSections(IDictionary<int,int> left, IDictionary<int,int> right);
  public static string SectionLabel(int sectionKey);           // LainnyaKey -> "Lainnya"

Service link (D-02) — Services/InjectAssessmentService.cs:
  public async Task<InjectResult> InjectBatchAsync(InjectRequest req, string actorUserId, string actorName);
  // InjectResult.Rejected=true + PerRowErrors saat preflight tolak-semua (reject-all path :49-68).
  // InjectRequest.LinkTargetRepId = RepresentativeId room target (server re-resolve, T-397-06).
  // CATATAN: InjectQuestionSpec (Models/InjectAssessmentDtos.cs:15-25) TIDAK punya SectionId ->
  //   inject package SELALU all-Lainnya -> D-02 guard EFEKTIF no-op untuk inject (skip-on-all-Lainnya).

ET-warning record (D-03) — Controllers/AssessmentAdminController.cs:7686:
  public sealed record SectionEtWarning(int SectionNumber, string? Name, int K, int DistinctEt);
  // Plan 04 re-spec: K = min(count soal SectionNumber=N antar paket-saudara);
  //   DistinctEt = distinct ET pool soal SectionNumber=N LINTAS sibling. Fire bila DistinctEt > K.

Test fixture (REUSE) — HcPortal.Tests/SectionFixture.cs + SectionMismatchGuardTests.cs:
  public class SectionFixture : IAsyncLifetime { public DbContextOptions<ApplicationDbContext> Options; }
  // Disposable HcPortalDB_Test_{guid}, MigrateAsync (jalankan 415 AddAssessmentPackageSection), HcPortalDB_Dev untouched.
  private static async Task AddPackageWithSectionsAsync(
      ApplicationDbContext ctx, int sessionId, int pkgNumber, (int? sectionNumber, int count)[] dist);
  // dist null=Lainnya. Buat AssessmentPackageSection row per key non-null + PackageQuestion ber-SectionId.
  // COPY helper ini ke tiap file test baru (atau ekstrak shared). Pola seed di SectionMismatchGuardTests.cs:172-201.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: xUnit RED — ExportSectionLabelTests (PAG-04) + SectionEtWarningTests (D-03 positif)</name>
  <read_first>
    - HcPortal.Tests/SectionMismatchGuardTests.cs (analog penuh: IClassFixture, AddPackageWithSectionsAsync :172-201, NewCtx pattern :33-40)
    - HcPortal.Tests/SectionFixture.cs (Options + MigrateAsync lifecycle)
    - Helpers/ExcelExportHelper.cs:50-112 (AddDetailPerSoalSheet — signature + struktur header row 1 / data row 2)
    - Controllers/AssessmentAdminController.cs:7644-7686 (ManagePackageQuestions GET + SectionEtWarning record)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md section "Phase Requirements -> Test Map"
  </read_first>
  <files>HcPortal.Tests/ExportSectionLabelTests.cs, HcPortal.Tests/SectionEtWarningTests.cs</files>
  <action>
Buat DUA file test xUnit baru. Keduanya `[Trait("Category", "Integration")]` + `IClassFixture<SectionFixture>`. COPY helper `AddPackageWithSectionsAsync(ctx, sessionId, pkgNumber, (int? sectionNumber, int count)[] dist)` dari SectionMismatchGuardTests.cs:172-201 ke masing-masing file (atau ekstrak ke helper shared SectionTestSeed.cs — Claude's Discretion, tapi JANGAN ubah signature helper).

ExportSectionLabelTests.cs (PAG-04 — RED untuk band-header/ordering, GREEN untuk backward-compat):
- [Fact] BandHeader_OrdersBySectionNumberThenOrder: seed 1 session + 1 package dengan soal: Section 2 (2 soal), Section 1 (3 soal), Lainnya/null (1 soal). Load questions dengan .Include(q => q.Options).Include(q => q.Section). Panggil ExcelExportHelper.AddDetailPerSoalSheet(wb, sessions, responses, questions). ASSERT: urutan kolom soal di sheet "Detail Per Soal" = Section 1 dulu (3 soal), lalu Section 2 (2 soal), lalu Lainnya (1 soal) terakhir, mengikuti urutan canonical OrderBy(SectionNumber ?? int.MaxValue).ThenBy(Order).ThenBy(Id). RED sekarang (helper masih OrderBy(q.Order) flat).
- [Fact] BandHeader_RendersSectionLabelRow: seed soal ber-Section. ASSERT: ada baris band merged bertuliskan "Section 1: {Nama}" di ATAS grup kolom Section 1 (cek cell value + IsMerged / MergedRanges). RED (band belum ada).
- [Fact] NoSection_BackwardCompat: seed semua soal null-Section. ASSERT: TIDAK ada band-header (output identik sekarang — data mulai row 2, freeze row 1). GREEN sekarang (backward-compat) — kunci agar Plan 02 tak regresi legacy.

SectionEtWarningTests.cs (D-03 positif — RED untuk re-spec lintas-sibling):
- [Fact] CrossSiblingPool_Fires: seed 1 assessment session dengan DUA paket-saudara (sibling). Pkg1: Section 1 (SectionNumber=1) berisi 2 soal ElemenTeknis {"ET-A","ET-B"}. Pkg2: Section 1 (SectionNumber=1) berisi 2 soal ElemenTeknis {"ET-C","ET-D"}. Pool ET lintas-sibling SectionNumber=1 = {ET-A..ET-D} -> DistinctEt=4; K = min(count per paket)=min(2,2)=2; 4>2 -> warning FIRE. Drive ManagePackageQuestions(pkg1.Id) GET (instansiasi controller dengan _fixture context — pola SectionMismatchGuardTests drive controller nyata) lalu ASSERT ViewBag.SectionEtWarnings (cast List<SectionEtWarning>) berisi >=1 entry SectionNumber=1. RED sekarang (predikat lama dalam 1-paket -> DistinctEt <= K -> tak pernah fire).
- [Fact] GroupBySectionNumber_NotSectionId: seed 2 sibling SectionNumber sama (1) tapi AssessmentPackageSection.Id berbeda. ASSERT: warning mengelompokkan lintas-sibling by SectionNumber (1 entry untuk Section 1, bukan 2 per-Id). RED (group lama by q.SectionId == s.Id per-paket).
- [Fact] FullCoverage_NoWarning_NonBlocking: seed sibling di mana tiap ET muncul >= K kali (DistinctEt <= K) -> TIDAK ada warning. GREEN (kunci non-blocking semantik tetap utuh).

Gunakan _fixture.Options -> new ApplicationDbContext(_fixture.Options). JANGAN tulis logic replica — drive helper/controller NYATA. BI string assertion mengikuti gaya pesan existing.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ExportSectionLabelTests|FullyQualifiedName~SectionEtWarningTests"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "class ExportSectionLabelTests" HcPortal.Tests/ExportSectionLabelTests.cs == 1
    - grep -c "class SectionEtWarningTests" HcPortal.Tests/SectionEtWarningTests.cs == 1
    - Kedua file grep "IClassFixture<SectionFixture>" match
    - `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` exit 0 (kompilasi hijau — test RED, bukan compile error)
    - Test BandHeader_* + CrossSiblingPool_Fires + GroupBySectionNumber_NotSectionId FAIL (RED); NoSection_BackwardCompat + FullCoverage_NoWarning_NonBlocking PASS (GREEN)
  </acceptance_criteria>
  <done>2 file test ada, kompilasi hijau, test band-header/ordering/ET-positif RED, test backward-compat/full-coverage GREEN. Tak ada regresi suite lain.</done>
</task>

<task type="auto">
  <name>Task 2: xUnit RED — LinkPrePostSectionGuardTests (D-02) + 4 skeleton e2e Playwright</name>
  <read_first>
    - HcPortal.Tests/SectionMismatchGuardTests.cs (drive REAL service, AddPackageWithSectionsAsync, no logic replica)
    - Services/InjectAssessmentService.cs:20-40 (ctor deps), :42-68 (InjectBatchAsync reject-all path), :529-557 (preflight link block pattern), :617-652 (ResolveLinkContextAsync — Kasus A/B target room resolution)
    - Models/InjectAssessmentDtos.cs:15-25 (InjectQuestionSpec — TANPA SectionId -> inject all-Lainnya), :86-103 (InjectRowError, InjectResult)
    - Controllers/CMPController.cs:1088-1126 (guardAnySections skip + SectionStructureComparer call verbatim — analog D-02)
    - tests/e2e/scoped-shuffle.spec.ts:32-61,189-200 (mode:serial + db.backup/restore + import paths), tests/e2e/inject-assessment-397.spec.ts, tests/e2e/flexible-participant-412.spec.ts, tests/e2e/export-per-peserta.spec.ts (download-assert)
  </read_first>
  <files>HcPortal.Tests/LinkPrePostSectionGuardTests.cs, tests/e2e/section-lifecycle-419.spec.ts, tests/e2e/inject-section-419.spec.ts, tests/e2e/linkprepost-section-419.spec.ts, tests/e2e/addremove-section-419.spec.ts</files>
  <action>
LinkPrePostSectionGuardTests.cs (D-02 — real-DB, drive InjectAssessmentService NYATA via SectionFixture):
- [Trait("Category","Integration")] + IClassFixture<SectionFixture>. COPY AddPackageWithSectionsAsync helper. Instansiasi InjectAssessmentService nyata dengan new ApplicationDbContext(_fixture.Options) + dependency lain dari ctor (baca InjectAssessmentService.cs:20-40; pakai stub/real seperti SectionMismatchGuardTests bila ada GradingService/dll).
- [Fact] BothSectioned_Mismatch_Rejects: seed room target (online, tipe-lawan) dengan struktur Section nyata {Section1:3, Section2:2}; seed inject batch yang DIPAKSA ber-Section berbeda {Section1:2} via DB langsung (karena InjectQuestionSpec tak punya SectionId — dokumentasikan di komentar). Bangun InjectRequest dengan LinkTargetRepId = rep room target. Panggil InjectBatchAsync. ASSERT result.Rejected == true + PerRowErrors memuat pesan menyebut "Section" + jumlah diharapkan vs aktual. RED sekarang (guard D-02 belum ada).
- [Fact] InjectAllLainnya_SkipsGuard_Passes: seed room target ber-Section + inject batch all-Lainnya (jalur normal InjectQuestionSpec). ASSERT: TIDAK ada PerRowError ber-pesan struktur Section ("struktur harus sama"/"Section ...: room target") — guard di-skip (skip-on-all-Lainnya per D-02 + Pitfall 2). Boleh rejected oleh alasan LAIN; fokus assert HANYA absennya error struktur-Section. Pin perilaku ini (harus tetap lolos setelah Plan 03 dengan skip-gate).
- [Fact] BothSectioned_Match_Passes: seed room target {Section1:3} + inject batch DB-seeded {Section1:3} identik. ASSERT: tak ada PerRowError struktur-Section. RED sampai Plan 03 implement gate+comparer.

4 skeleton e2e (tests/e2e/*-419.spec.ts): tiap file = skeleton minimal yang IMPORT benar + body test.fixme dengan komentar TODO Plan 05. Tujuan: file ada sebagai placeholder kontrak. Struktur mirror analog (CATATAN import paths: dbSnapshot di '../helpers/dbSnapshot', auth di '../helpers/auth', examTypes di './helpers/examTypes'):
- section-lifecycle-419.spec.ts -> import * as db from '../helpers/dbSnapshot'; import { login } from '../helpers/auth'; import { createAssessmentViaWizard, addQuestionViaForm } from './helpers/examTypes'; + test.describe.configure({ mode: 'serial' }) + test.describe('D-04.1 Lifecycle Section inti', () => { test.fixme('create->assign Section->ujian->render A-F->resume->export label', async () => {}); }); + komentar analog scoped-shuffle.spec.ts + export-per-peserta download-assert.
- inject-section-419.spec.ts -> test.fixme('D-04.2 Inject v32.2 x Section + opsi 5-6', ...) + komentar analog inject-assessment-397.spec.ts.
- linkprepost-section-419.spec.ts -> test.fixme('D-04.3 LinkPrePost 397 x Section (sama=sukses, beda=tertolak)', ...) + komentar analog inject-assessment-397.spec.ts.
- addremove-section-419.spec.ts -> test.fixme('D-04.4 Add/Remove v32.5 x Section + pagination', ...) + komentar analog flexible-participant-412.spec.ts.

Tiap skeleton sertakan komentar blok menamai analog spec + langkah yang akan diisi Plan 05.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~LinkPrePostSectionGuardTests"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "class LinkPrePostSectionGuardTests" HcPortal.Tests/LinkPrePostSectionGuardTests.cs == 1
    - `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` exit 0 (kompilasi hijau)
    - `cd tests && npx playwright test e2e/section-lifecycle-419 e2e/inject-section-419 e2e/linkprepost-section-419 e2e/addremove-section-419 --list` exit 0 (4 spec ter-discover, tak ada syntax/import error)
    - Test BothSectioned_Mismatch_Rejects + BothSectioned_Match_Passes FAIL (RED — guard belum ada); InjectAllLainnya_SkipsGuard_Passes PASS (no guard saat ini = otomatis lolos)
    - 4 file e2e ada; grep "test.fixme" pada masing-masing match
  </acceptance_criteria>
  <done>LinkPrePostSectionGuardTests ada (RED untuk both-sectioned, GREEN untuk skip-all-Lainnya). 4 skeleton e2e ter-discover Playwright tanpa error. Kompilasi hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only plan) | Tak ada surface produksi disentuh — hanya file test xUnit + skeleton e2e. Tak ada input user baru. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-419-01-01 | Information Disclosure | SectionFixture DB nyata (SQLEXPRESS) | accept | Fixture pakai DB disposable `HcPortalDB_Test_{guid}` (EnsureDeletedAsync di DisposeAsync); HcPortalDB_Dev TIDAK disentuh. Pola teruji 415-418. |
| T-419-01-02 | Tampering | Test e2e skeleton menulis DB lokal saat diisi nanti | accept | Plan 05 yang menjalankan e2e wajib snapshot/restore (SEED_WORKFLOW); skeleton ini test.fixme (tidak run). |
</threat_model>

<verification>
- `dotnet build HcPortal.Tests/HcPortal.Tests.csproj` exit 0 (kompilasi hijau — RED via assertion, bukan compile error).
- Test baru RED untuk perilaku belum diimplementasikan (band-header, ordering, ET-positif, D-02 both-sectioned); GREEN untuk backward-compat + skip-all-Lainnya.
- Suite lama (415-418) tetap hijau — JALANKAN `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full untuk konfirmasi 0 regresi.
- 4 spec e2e ter-discover Playwright (`--list` exit 0).
</verification>

<success_criteria>
- 3 file xUnit baru ada + kompilasi hijau.
- RED terkendali pada kontrak Wave 2 (Plan 02/03/04).
- Backward-compat test GREEN (kompatibel-mundur terjaga).
- 4 skeleton e2e siap diisi Plan 05.
- 0 regresi suite existing.
</success_criteria>

<output>
After completion, create `.planning/phases/419-export-label-section-polish-test-uat-milestone/419-01-SUMMARY.md` (gunakan @$HOME/.claude/get-shit-done/templates/summary.md). Catat: daftar Fact RED vs GREEN per file, signature helper yang di-COPY, jumlah suite total, kontrak yang HARUS dipenuhi Plan 02/03/04 agar GREEN.
</output>
