---
phase: 419-export-label-section-polish-test-uat-milestone
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Services/InjectAssessmentService.cs
autonomous: true
requirements: [PAG-04]
must_haves:
  truths:
    - "LinkPrePost (inject Phase 397) menolak menaut Pre<->Post bila KEDUA sisi punya Section nyata dan struktur Section (count per SectionNumber) berbeda, dengan pesan jelas (sebut SectionNumber + jumlah diharapkan vs aktual)"
    - "Guard di-SKIP bila salah satu sisi all-Lainnya (tanpa Section) — inject selalu all-Lainnya -> link inject->room ber-Section tetap boleh (D-02 skip-on-all-Lainnya)"
    - "Cek struktur server-authoritative (pakai data server-resolved via ResolveLinkContextAsync, BUKAN client LinkedGroupId)"
    - "Reuse SectionStructureComparer (SEC-04) — tak ada predikat struktur baru"
  artifacts:
    - path: "Services/InjectAssessmentService.cs"
      provides: "Guard D-02 struktur Section di jalur link inject (preflight reject-all)"
      contains: "SectionStructureComparer"
  key_links:
    - from: "Services/InjectAssessmentService.cs (D-02 guard)"
      to: "Helpers/SectionStructureComparer.MismatchedSections"
      via: "reuse comparator SEC-04 (anti predikat-baru)"
      pattern: "SectionStructureComparer\\.MismatchedSections"
    - from: "D-02 guard"
      to: "ResolveLinkContextAsync (target room server-resolved)"
      via: "compare struktur server-resolved, bukan client"
      pattern: "ResolveLinkContextAsync"
---

<objective>
Implementasi D-02 (polish carry-over, bukan REQ formal): guard integritas struktur Section saat LinkPrePost (jalur inject Phase 397). HARD-BLOCK menaut Pre<->Post bila KEDUA sisi punya Section nyata DAN struktur (count per SectionNumber) berbeda, pesan jelas (SectionNumber + jumlah diharapkan vs aktual). SKIP guard bila salah satu sisi all-Lainnya (tanpa Section) — karena inject (InjectQuestionSpec tak punya SectionId) SELALU all-Lainnya, ini menjaga link inject->room ber-Section tetap boleh (D-02 resolusi Open-Q + Pitfall 2). REUSE SectionStructureComparer (SEC-04) + pola guardAnySections + comparer dari CMPController.StartExam:1094-1119 — JANGAN tulis predikat baru. Server-authoritative: bandingkan data server-resolved (ResolveLinkContextAsync), bukan client LinkedGroupId (T-397-06).

Purpose: SEC-06 sync deep-clone hanya jamin struktur identik untuk jalur SamePackage; LinkPrePost menaut room EXISTING yang bisa di-author independen -> struktur bisa divergen tanpa guard. Cegah link Pre<->Post divergen (perilaku BARU).
Output: Guard di preflight InjectAssessmentService (reject-all path). Test LinkPrePostSectionGuardTests (Plan 01) jadi GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-CONTEXT.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md

<interfaces>
Comparator (REUSE — Helpers/SectionStructureComparer.cs):
  public const int LainnyaKey = int.MinValue;
  public static int KeyOf(int? sectionNumber);                 // null -> LainnyaKey
  public static List<int> MismatchedSections(IDictionary<int,int> left, IDictionary<int,int> right);
  public static string SectionLabel(int sectionKey);           // LainnyaKey -> "Lainnya"

Pola guard verbatim (analog) — Controllers/CMPController.cs:1094-1119:
  bool guardAnySections = guardPackages.SelectMany(p => p.Questions).Any(q => q.SectionId != null);
  if (guardPackages.Count >= 2 && guardAnySections) {
      var refCounts = referencePkg.Questions.GroupBy(q => SectionStructureComparer.KeyOf(q.Section?.SectionNumber)).ToDictionary(g => g.Key, g => g.Count());
      // ... bandingkan vs sibling: SectionStructureComparer.MismatchedSections(refCounts, sibCounts).Any()
  }

Service insertion point — Services/InjectAssessmentService.cs:
  // PreflightValidateAsync (:429-560) reject-all path. Blok anti-double-link (:529-557) memakai:
  if (req.LinkTargetRepId.HasValue && (req.AssessmentType == PreTest || PostTest)) {
      var (groupId, _, _, _, rep) = await ResolveLinkContextAsync(req.LinkTargetRepId, req.AssessmentType);
      ...
  }
  // InjectBatchAsync reject-all (:48-68): errors dari PreflightValidateAsync -> result.Rejected=true bila ada error.

ResolveLinkContextAsync (:617-652) -> (int? groupId, string? opposite, bool kasusB, List<AssessmentSession> targetSessions, AssessmentSession? rep):
  // Kasus A: groupId=rep.LinkedGroupId, targetSessions kosong (room target = grup by LinkedGroupId).
  // Kasus B: groupId=rep.Id, targetSessions = SEMUA sesi room target standalone (Title+Category+Schedule.Date, tipe-lawan).

InjectQuestionSpec (Models/InjectAssessmentDtos.cs:15-25): TANPA SectionId -> inject package SELALU all-Lainnya.
InjectRowError (Models/InjectAssessmentDtos.cs:86-91): { string Nip; string Message; } (Bahasa Indonesia, reject-all).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard D-02 struktur Section di preflight LinkPrePost (reject-all, skip-on-all-Lainnya)</name>
  <behavior>
    - Test BothSectioned_Mismatch_Rejects (Plan 01): room target ber-Section vs inject(DB-seeded) ber-Section beda -> result.Rejected, PerRowError menyebut "Section" + jumlah.
    - Test InjectAllLainnya_SkipsGuard_Passes (Plan 01): inject all-Lainnya (jalur normal) + room target ber-Section -> TIDAK ada PerRowError struktur-Section (guard di-skip).
    - Test BothSectioned_Match_Passes (Plan 01): kedua sisi ber-Section identik -> tak ada PerRowError struktur-Section.
  </behavior>
  <read_first>
    - Services/InjectAssessmentService.cs:42-68 (InjectBatchAsync reject-all path), :429-560 (PreflightValidateAsync FULL — terutama blok link :529-557), :617-652 (ResolveLinkContextAsync Kasus A/B)
    - Controllers/CMPController.cs:1088-1126 (guardAnySections gate + SectionStructureComparer call VERBATIM — analog langsung)
    - Helpers/SectionStructureComparer.cs (KeyOf/MismatchedSections/SectionLabel)
    - Models/InjectAssessmentDtos.cs:15-25 (InjectQuestionSpec tanpa SectionId), :45-73 (InjectRequest fields — Questions, LinkTargetRepId, AssessmentType), :86-91 (InjectRowError)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md section "InjectAssessmentService -> D-02" (insertion point + guardAnySections gate + comparer call)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md Pitfall 2 + Open Q-1 (inject all-Lainnya -> skip)
  </read_first>
  <files>Services/InjectAssessmentService.cs</files>
  <action>
Sisipkan guard D-02 di PreflightValidateAsync (reject-all path) SETELAH/dekat blok anti-double-link (:529-557) yang sudah punya pola `if (req.LinkTargetRepId.HasValue && (PreTest||PostTest)) { var (groupId,_,_,_,rep) = await ResolveLinkContextAsync(...); ... }`. Boleh extend blok existing atau tambah blok baru — pakai ResolveLinkContextAsync yang sama (sumber kebenaran tunggal, server-authoritative T-397-06).

Langkah dalam guard (HANYA saat req.LinkTargetRepId.HasValue && tipe Pre/Post && rep != null && groupId.HasValue):

1. Resolve "questions room target" (server-resolved, bukan client):
   - Kumpulkan sesi room target: Kasus A -> sesi tipe-lawan dengan LinkedGroupId == groupId; Kasus B -> ctx.targetSessions (sudah di-resolve). Dari sesi tsb ambil AssessmentPackageId via UserPackageAssignment (atau relasi paket->sesi), lalu LOAD PackageQuestions room target dengan .Include(q => q.Section). (Gunakan _context; mirror cara load guard di StartExam.)
   - "questions inject batch": bangun dari req.Questions (InjectQuestionSpec) — SEMUA SectionNumber = null (InjectQuestionSpec tak punya SectionId) -> all-Lainnya. (Ini fakta kunci Pitfall 2.)

2. SKIP-gate (guardAnySections — D-02 resolusi Open-Q + Pattern 3): 
   bool injectHasSection = injectQuestions.Any(q => /* SectionNumber non-null */);   // SELALU false untuk inject normal
   bool targetHasSection = targetRoomQuestions.Any(q => q.SectionId != null);
   if (!(injectHasSection && targetHasSection)) -> SKIP guard (tidak menambah error). Ini menjaga link inject(all-Lainnya)->room ber-Section TETAP BOLEH.

3. Bila KEDUA sisi ber-Section: hitung count per key via comparator (VERBATIM pola StartExam):
   var injectCounts = injectQuestions.GroupBy(q => SectionStructureComparer.KeyOf(/*sectionNumber*/)).ToDictionary(g => g.Key, g => g.Count());
   var targetCounts = targetRoomQuestions.GroupBy(q => SectionStructureComparer.KeyOf(q.Section?.SectionNumber)).ToDictionary(g => g.Key, g => g.Count());
   var mismatched = SectionStructureComparer.MismatchedSections(injectCounts, targetCounts);
   foreach (var sn in mismatched) {
       int x = injectCounts.GetValueOrDefault(sn);
       int y = targetCounts.GetValueOrDefault(sn);
       errors.Add(new InjectRowError { Nip = "", Message = $"Section {SectionStructureComparer.SectionLabel(sn)}: room target punya {y} soal, batch inject punya {x} soal (struktur Section harus sama untuk ditautkan)." });
   }

4. Reject-all (V5): TAMBAHKAN semua error mismatch ke list `errors` yang sudah ada (no stop-at-first) -> InjectBatchAsync akan set result.Rejected=true (path :48-68). JANGAN early-return; biarkan error lain (anti-double, NIP) ikut terkumpul.

JANGAN tulis predikat GroupBy+count baru di luar SectionStructureComparer (anti-pattern CONTEXT). JANGAN trust req.LinkedGroupId client. Pesan BI. Pastikan dotnet build HcPortal.csproj 0 error.
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~LinkPrePostSectionGuardTests"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SectionStructureComparer.MismatchedSections" Services/InjectAssessmentService.cs >= 1
    - grep "ResolveLinkContextAsync" Services/InjectAssessmentService.cs match (server-authoritative)
    - grep "struktur Section harus sama" Services/InjectAssessmentService.cs match (pesan D-02)
    - grep -E "injectHasSection|targetHasSection|SectionId != null" Services/InjectAssessmentService.cs match (skip-gate)
    - `dotnet build HcPortal.csproj` exit 0
    - Test BothSectioned_Mismatch_Rejects + InjectAllLainnya_SkipsGuard_Passes + BothSectioned_Match_Passes semua PASS
  </acceptance_criteria>
  <done>Guard D-02 di preflight: block both-sectioned mismatch (reject-all, pesan jelas), skip-on-all-Lainnya (inject normal lolos), reuse SectionStructureComparer, server-authoritative. 3 test guard GREEN. Build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC client -> Inject link POST | Client kirim LinkTargetRepId (hint) — server RE-RESOLVE room target, JANGAN trust LinkedGroupId mentah. |
| Inject batch struktur vs room target struktur | Server bandingkan count per SectionNumber (otoritas server). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-419-03-01 | Tampering | Client kirim LinkedGroupId/grup palsu untuk bypass guard | mitigate | Guard pakai data server-resolved via ResolveLinkContextAsync (re-resolve dari LinkTargetRepId, T-397-06). TIDAK pakai req.LinkedGroupId client. |
| T-419-03-02 | Elevation/IDOR | Link ke room milik scope lain | mitigate | ResolveLinkContextAsync validasi rep tipe-lawan + endpoint Inject [Authorize(Roles="Admin, HC")] existing. Guard tambahan tak melonggarkan authz. |
| T-419-03-03 | DoS / Information Disclosure | Guard load room target questions | accept | Load scoped ke sesi room target server-resolved (bukan cross-assessment arbitrer). Read-only, dalam preflight existing. Volume kecil (1 room). |
| T-419-03-04 | Spoofing | Inject all-Lainnya menyamar untuk skip guard berbahaya | accept | Skip-on-all-Lainnya adalah desain D-02 eksplisit (inject memang tak ber-Section). Tak ada eskalasi: inject tak menulis struktur Section ke room target; LinkedGroupId stiker existing tak diubah guard. |
</threat_model>

<verification>
- dotnet build HcPortal.csproj exit 0.
- LinkPrePostSectionGuardTests (Plan 01) semua GREEN (mismatch reject, all-Lainnya skip, match pass).
- Suite inject existing (Phase 393-397) tetap GREEN saat wave merge (guard hanya fire both-sectioned -> jalur inject normal all-Lainnya tak terdampak).
- grep konfirmasi reuse comparator + skip-gate + server-resolved.
</verification>

<success_criteria>
- Guard D-02 block both-sectioned mismatch dengan pesan jelas; skip salah-satu all-Lainnya.
- Server-authoritative (ResolveLinkContextAsync), reuse SectionStructureComparer.
- Reject-all (semua section error terkumpul), tak early-return.
- 0 regresi jalur inject normal (all-Lainnya selalu lolos guard).
</success_criteria>

<output>
After completion, create `.planning/phases/419-export-label-section-polish-test-uat-milestone/419-03-SUMMARY.md` (@$HOME/.claude/get-shit-done/templates/summary.md). Catat: lokasi insertion (extend blok :529 vs blok baru), cara load room target questions, skip-gate condition, pesan mismatch, konfirmasi inject normal tetap lolos.
</output>
