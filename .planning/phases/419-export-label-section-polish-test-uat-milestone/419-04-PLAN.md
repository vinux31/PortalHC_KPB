---
phase: 419-export-label-section-polish-test-uat-milestone
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [PAG-04]
must_haves:
  truths:
    - "Peringatan ET-coverage (ViewBag.SectionEtWarnings) FIRE bermakna saat pool ElemenTeknis lintas paket-saudara untuk satu SectionNumber melebihi K=min(count soal SectionNumber antar sibling)"
    - "Matching Section lintas-sibling pakai SectionNumber (bukan SectionId) — IN-01"
    - "Peringatan tetap NON-BLOCKING (sinyal, bukan error — D-416-03 load-bearing tak berubah)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Predikat ET-warning re-spec lintas-sibling (DistinctEt pool, K=min) group by SectionNumber"
      contains: "SectionEtWarning"
  key_links:
    - from: "ManagePackageQuestions GET"
      to: "paket-saudara (sibling) di assessment yang sama"
      via: "load Where(p.AssessmentSessionId == pkg.AssessmentSessionId) + Include Questions.Section"
      pattern: "AssessmentSessionId == "
---

<objective>
Re-spec predikat peringatan cakupan Elemen Teknis (ET) per-Section (D-03 / DEF-416-01 / IN-01) yang saat ini dead-code. Predikat lama (`DistinctEt > K` dengan K=count soal Section dalam 1 paket, DistinctEt=distinct ET dalam paket itu) TAK PERNAH true (tiap PackageQuestion = 1 ET -> distinct ET <= jumlah soal). Re-spec: `DistinctEt` = distinct ET dari pool soal SectionNumber yang sama LINTAS paket-saudara (sibling) dalam satu assessment; `K` = min(count soal SectionNumber=N antar paket-saudara). Fire bila DistinctEt > K (sebagian ET tak terjamin muncul). Matching Section lintas-sibling pakai SectionNumber (IN-01, bukan SectionId — sibling beda Id sama nomor). Tetap NON-BLOCKING.

Purpose: Tutup DEF-416-01 (carry-over keputusan user 2026-06-23) — peringatan jadi reachable + bermakna, dengan test positif (416 hanya buktikan non-blocking, tak ada test fire).
Output: ManagePackageQuestions GET muat sibling + predikat re-spec. Test SectionEtWarningTests (Plan 01) jadi GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-CONTEXT.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md
@.planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md
@.planning/phases/416-scoped-shuffle-acak-per-section/deferred-items.md

<interfaces>
AssessmentAdminController.cs:7644-7686 ManagePackageQuestions GET (saat ini):
  // :7646-7649 muat HANYA 1 paket:
  var pkg = await _context.AssessmentPackages.Include(p => p.Questions.OrderBy(q => q.Order)).ThenInclude(q => q.Options).FirstOrDefaultAsync(p => p.Id == packageId);
  // :7658-7661 muat sections paket itu.
  // :7673-7680 PREDIKAT DEAD-CODE yang DI-RE-SPEC:
  ViewBag.SectionEtWarnings = sections.Select(s => {
      var qs = pkg.Questions.Where(q => q.SectionId == s.Id).ToList();   // dalam 1 paket only
      int k = qs.Count;
      int distinctEt = qs.Where(q => !string.IsNullOrWhiteSpace(q.ElemenTeknis)).Select(q => q.ElemenTeknis!).Distinct().Count();
      return new SectionEtWarning(s.SectionNumber, s.Name, k, distinctEt);
  }).Where(w => w.DistinctEt > w.K).ToList();
  // :7686 record (PERTAHANKAN shape):
  public sealed record SectionEtWarning(int SectionNumber, string? Name, int K, int DistinctEt);

Sibling-load idiom (mirror CMPController.StartExam:1088-1092):
  var siblingPkgs = await _context.AssessmentPackages
      .Include(p => p.Questions).ThenInclude(q => q.Section)
      .Where(p => p.AssessmentSessionId == pkg.AssessmentSessionId && p.Questions.Any())
      .ToListAsync();
  // Group cross-sibling by SectionNumber (IN-01); pool ET + K=min(count per package).

Model: PackageQuestion { int? SectionId; string? ElemenTeknis; AssessmentPackageSection? Section { int SectionNumber; string? Name; } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-spec predikat ET-warning lintas-sibling (DistinctEt pool, K=min) group by SectionNumber</name>
  <behavior>
    - Test CrossSiblingPool_Fires (Plan 01): 2 sibling, SectionNumber=1, ET berbeda -> DistinctEt(pool)=4 > K(min)=2 -> warning FIRE.
    - Test GroupBySectionNumber_NotSectionId (Plan 01): sibling beda Id sama SectionNumber -> dikelompokkan 1 entry by SectionNumber.
    - Test FullCoverage_NoWarning_NonBlocking (Plan 01): coverage penuh (DistinctEt <= K) -> tak ada warning; form/aksi tetap aktif (non-blocking).
  </behavior>
  <read_first>
    - Controllers/AssessmentAdminController.cs:7644-7686 (ManagePackageQuestions FULL + predikat :7673-7680 + record :7686)
    - Controllers/CMPController.cs:1088-1092 (sibling-load .ThenInclude(q => q.Section) idiom)
    - .planning/phases/416-scoped-shuffle-acak-per-section/deferred-items.md DEF-416-01 (saran re-spec lengkap: pool ET lintas-sibling vs K=min)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-PATTERNS.md section "ET-warning predicate" (sibling-load idiom + constraints non-blocking)
    - .planning/phases/419-export-label-section-polish-test-uat-milestone/419-RESEARCH.md Pitfall 3 (GET muat 1 paket; harus muat sibling)
  </read_first>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Re-spec predikat ViewBag.SectionEtWarnings di ManagePackageQuestions (:7673-7680). PERTAHANKAN record SectionEtWarning shape (:7686). Tetap NON-BLOCKING (hanya ViewBag, tak mengubah return/redirect).

1. Setelah resolve pkg (sudah ada pkg.AssessmentSessionId), MUAT paket-saudara (Pitfall 3 — GET sekarang hanya 1 paket):
   var siblingPkgs = await _context.AssessmentPackages
       .Include(p => p.Questions).ThenInclude(q => q.Section)
       .Where(p => p.AssessmentSessionId == pkg.AssessmentSessionId && p.Questions.Any())
       .ToListAsync();
   (Bila hanya 1 paket di assessment, siblingPkgs.Count==1 -> K=count paket itu; predikat tetap dihitung tapi umumnya tak fire kecuali memang pool>kuota. Tak crash.)

2. Hitung per SectionNumber (IN-01 — group by SectionNumber, BUKAN SectionId; sibling beda Id sama nomor). Untuk tiap SectionNumber N yang muncul di sibling:
   - allQsForN = semua soal (lintas semua siblingPkgs) dengan q.Section?.SectionNumber == N.
   - DistinctEt = allQsForN.Where(q => !string.IsNullOrWhiteSpace(q.ElemenTeknis)).Select(q => q.ElemenTeknis!).Distinct().Count();
   - K = min, lintas siblingPkgs, dari count soal SectionNumber==N per paket. (Hanya hitung paket yang PUNYA Section N; bila satu paket tak punya N sama sekali, itu kasus struktur-mismatch terpisah — gunakan count=0 -> K=0 akan selalu fire; PERTIMBANGKAN: K=min over paket yang punya >=1 soal N, sesuai semantik "kuota yang dipresentasikan". Pilih definisi yang membuat CrossSiblingPool_Fires GREEN dan FullCoverage GREEN — baca test Plan 01 untuk angka pasti, lalu sesuaikan.)
   - Nama Section = ambil dari salah satu sibling yang punya Section N (Name boleh null).
   - Buat SectionEtWarning(N, name, K, DistinctEt).

3. Filter .Where(w => w.DistinctEt > w.K).ToList() -> ViewBag.SectionEtWarnings.

4. Hilangkan/abaikan grouping lama by `q.SectionId == s.Id` dalam 1 paket (sumber dead-code). Variabel `sections` (paket ini) boleh tetap dipakai untuk panel UI lain — JANGAN hapus bila view memakainya; HANYA sumber data warning yang berubah.

Tetap NON-BLOCKING: ViewBag saja, tak ada TempData["Error"]/redirect. Pastikan dotnet build HcPortal.csproj 0 error. Null-safe: q.Section bisa null (soal Lainnya) -> di-skip dari grouping SectionNumber (Lainnya tak relevan untuk ET-coverage per-Section).
  </action>
  <verify>
    <automated>dotnet build HcPortal.csproj && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~SectionEtWarningTests"</automated>
  </verify>
  <acceptance_criteria>
    - grep "AssessmentSessionId == pkg.AssessmentSessionId" Controllers/AssessmentAdminController.cs match (sibling load)
    - grep -E "ThenInclude\(q => q.Section\)" Controllers/AssessmentAdminController.cs match (eager Section di sibling load)
    - grep -E "SectionNumber" Controllers/AssessmentAdminController.cs di sekitar predikat warning (IN-01 group by SectionNumber); grep TIDAK lagi memakai `q.SectionId == s.Id` sebagai sumber warning
    - grep "SectionEtWarning(" Controllers/AssessmentAdminController.cs match (record shape dipertahankan)
    - `dotnet build HcPortal.csproj` exit 0
    - Test CrossSiblingPool_Fires + GroupBySectionNumber_NotSectionId + FullCoverage_NoWarning_NonBlocking semua PASS
  </acceptance_criteria>
  <done>Predikat ET-warning re-spec: pool ET lintas-sibling vs K=min, group by SectionNumber (IN-01), non-blocking. 3 test ET GREEN. Build hijau. DEF-416-01 ditutup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/HC -> ManagePackageQuestions GET | Read-only, [Authorize(Roles="Admin, HC")] existing; output ViewBag non-blocking (sinyal). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-419-04-01 | Information Disclosure | Sibling-load query lintas paket | accept | Query scoped ke pkg.AssessmentSessionId (assessment yang sama saja) — tak ada cross-assessment leak. Read-only GET, ViewBag. |
| T-419-04-02 | DoS | Muat semua paket+soal sibling | accept | Volume terbatas (paket per assessment, tipikal <=4 paket; soal puluhan). Read-only, dalam GET existing yang sudah muat 1 paket. |
| T-419-04-03 | Tampering | Warning dipakai sebagai blocker? | accept | NON-BLOCKING by design (D-416-03 load-bearing) — hanya ViewBag sinyal, tak mengubah alur. Tak ada sink yang bisa di-tamper. |
</threat_model>

<verification>
- dotnet build HcPortal.csproj exit 0.
- SectionEtWarningTests (Plan 01) semua GREEN (fire, group-by-SectionNumber, full-coverage non-blocking).
- ManagePackageQuestions tetap merender (non-blocking) — verifikasi e2e S3 existing (scoped-shuffle) tetap hijau saat wave merge.
- grep konfirmasi sibling-load + group by SectionNumber.
</verification>

<success_criteria>
- Predikat ET-warning reachable + fire bermakna (test positif GREEN).
- Group by SectionNumber lintas-sibling (IN-01).
- NON-BLOCKING dipertahankan.
- DEF-416-01 carry-over ditutup.
</success_criteria>

<output>
After completion, create `.planning/phases/419-export-label-section-polish-test-uat-milestone/419-04-SUMMARY.md` (@$HOME/.claude/get-shit-done/templates/summary.md). Catat: definisi K final (min over paket yang punya N), penanganan null Section (skip), konfirmasi non-blocking, hasil 3 test ET, status DEF-416-01.
</output>
