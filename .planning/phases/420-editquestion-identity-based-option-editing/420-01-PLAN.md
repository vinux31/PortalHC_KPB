---
phase: 420-editquestion-identity-based-option-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Models/OptionInput.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [OPTEDIT-01, OPTEDIT-02, OPTEDIT-03, OPTEDIT-04, OPTEDIT-05]
tags: [authoring, controller, data-integrity, security]

must_haves:
  truths:
    - "HC menghapus opsi tengah pada soal BELUM dijawab → record yang benar terhapus, opsi tersisa tidak ter-relabel (match by Id)"
    - "HC menghapus opsi yang SUDAH dijawab (posisi manapun, termasuk tengah) → diblokir dengan pesan ramah menyebut huruf + cuplikan teks"
    - "HC mengedit teks/kebenaran opsi terjawab → UPDATE record by Id (jawaban peserta tetap merujuk opsi yang sama)"
    - "Konversi MC/MA→Essay + shrink opsi terjawab → diblokir tanpa error 500 (FK-Restrict tak ter-trip)"
    - "OptionId asing/forged dari client → seluruh edit ditolak fail-closed sebelum mutasi apa pun"
    - "CreateQuestion soal baru tetap normal (Id null → ADD by construction)"
  artifacts:
    - path: "Models/OptionInput.cs"
      provides: "Carrier identity int? Id + kontrak validasi-server (revisi T-418-06)"
      contains: "public int? Id"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "EditQuestion GET emit o.Id + EditQuestion POST identity-based upsert + anti-tamper + guard set-difference"
      contains: "existingIds.Except"
  key_links:
    - from: "EditQuestion GET JSON options projection"
      to: "client hidden options[i].Id"
      via: "id = o.Id field"
      pattern: "id = o\\.Id"
    - from: "EditQuestion POST submitted OptionId"
      to: "OptionShrinkGuard.FindBlockedOptionIds"
      via: "removedOptionIds = existingIds.Except(keptIds)"
      pattern: "existingIds\\.Except"
    - from: "EditQuestion POST anti-tamper"
      to: "fail-closed reject"
      via: "submittedIds.Any(id => !existingIds.Contains(id))"
      pattern: "!existingIds\\.Contains"
---

<objective>
Ganti mekanisme upsert opsi di `AssessmentAdminController.EditQuestion` POST dari **posisional** (`existing[i]` OrderBy Id) menjadi **identity-based** (match baris input ke `PackageOption` existing by stable `Id`). Tambah carrier `int? Id` ke `OptionInput`, emit `id` di GET JSON, dan implement algoritma anti-tamper + set-difference-by-Id + upsert identity dari RESEARCH §2d.

Purpose: Menghapus/mengedit opsi (termasuk opsi TENGAH) pada soal yang sudah dijawab peserta tidak lagi me-relabel jawaban peserta secara senyap. Guard answered-option (D-418-02) menyala untuk delete posisi MANAPUN, bukan hanya ekor. Pertahankan regression-lock 999.14 (FK-Restrict 500 tetap tertutup).

Output: `Models/OptionInput.cs` (+`int? Id`, komentar T-418-06 direvisi) dan `Controllers/AssessmentAdminController.cs` (GET JSON +`id`, POST guard+upsert diganti identity-based).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/420-editquestion-identity-based-option-editing/420-CONTEXT.md
@.planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Kontrak yang executor pakai langsung — tak perlu eksplorasi codebase. -->

Models/OptionInput.cs (SAAT INI — properti yang ada, JANGAN ubah selain ADD Id):
```csharp
public class OptionInput
{
    public string? Text { get; set; }
    public bool IsCorrect { get; set; }
    public IFormFile? Image { get; set; }
    public string? ImageAlt { get; set; }
    public bool RemoveImage { get; set; }
}
```

Helpers/OptionShrinkGuard.cs (REUSE AS-IS, signature LOCKED — JANGAN ubah):
```csharp
public static IReadOnlyList<int> FindBlockedOptionIds(
    IEnumerable<int> removedOptionIds,
    IEnumerable<int> answeredOptionIds)
    => removedOptionIds.Intersect(answeredOptionIds).ToList();
```

Helpers/QuestionOptionValidator.cs (REUSE AS-IS — sudah dipanggil sebelum guard di EditQuestion):
```csharp
ValidateQuestionOptions(string questionType, string?[] texts, bool[] isCorrects) → (bool ok, string? err)
```

Signature method yang ada di controller (REUSE AS-IS):
```csharp
// ResolveCorrectness hanya set IsCorrect per submitted-position; TAK pernah sentuh Id — aman.
private void ResolveCorrectness(string questionType, List<OptionInput> options, int? correctIndex)
// Apply niat gambar per-record (UPDATE/ADD), kumpul old path ke imagePathsToDelete:
private async Task ApplyOptionImageIntent(PackageOption slot, IFormFile? image, string? imageAlt, bool removeImage, int packageId, List<string> imagePathsToDelete)
```

FK relevan (Data/ApplicationDbContext.cs:561-564 — JANGAN ubah): `PackageUserResponse → PackageOption` = Restrict. Sumber hazard 999.14: hapus opsi terjawab → DbUpdateException 500. Guard wajib menyala SEBELUM SaveChanges.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tambah carrier int? Id ke OptionInput + revisi komentar T-418-06</name>
  <files>Models/OptionInput.cs</files>
  <read_first>
    - Models/OptionInput.cs (seluruh file — komentar XML T-418-06 di :13-15 yang melarang properti Id)
    - .planning/phases/420-editquestion-identity-based-option-editing/420-CONTEXT.md §decisions D-01
    - .planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md §2a
  </read_first>
  <action>
    Tambah properti `public int? Id { get; set; }` ke class `OptionInput` (letakkan sebagai properti PERTAMA, sebelum `Text`). Ganti blok komentar XML "KEAMANAN (T-418-06...)" yang melarang Id (baris ~13-15) dengan kontrak baru.

    Markup persis properti baru:
    ```csharp
    /// <summary>
    /// Phase 420 (D-01) — carrier identity opsi existing. Di-bind dari hidden input
    /// options[i].Id. Null = baris opsi BARU (ADD). Non-null = match record PackageOption
    /// existing untuk UPDATE/REMOVE by stable Id (BUKAN posisi).
    ///
    /// KEAMANAN (revisi T-418-06): Id KINI disuplai client (wajib untuk identity-matching),
    /// TAPI server WAJIB memvalidasi setiap Id non-null ∈ q.Options soal ini SEBELUM dipakai
    /// (lihat EditQuestion POST anti-tamper). Id asing/forged → tolak seluruh edit (fail-closed).
    /// Ini menetralkan mass-assignment/IDOR via validasi eksplisit, BUKAN via melarang properti.
    /// </summary>
    public int? Id { get; set; }
    ```

    Pada blok komentar XML class-level (yang saat ini berisi "JANGAN tambah properti Id — Id PackageOption ditentukan SERVER (preserve via existing[i]), tidak boleh disuplai client."), GANTI kalimat itu menjadi:
    ```
    /// KEAMANAN (T-418-06 mass-assignment): properti di-whitelist EKSPLISIT.
    /// Phase 420 (D-01) menambah properti Id (carrier identity) — TIDAK melanggar T-418-06:
    /// Id client-supplied DIVALIDASI server-side (Id ∈ q.Options) sebelum dipakai; forged Id ditolak.
    ```

    JANGAN ubah properti Text/IsCorrect/Image/ImageAlt/RemoveImage. JANGAN tambah properti lain.
  </action>
  <acceptance_criteria>
    - `grep -n "public int? Id" Models/OptionInput.cs` returns 1 match
    - `grep -n "Phase 420 (D-01)" Models/OptionInput.cs` returns ≥1 match
    - `grep -n "JANGAN tambah properti Id" Models/OptionInput.cs` returns 0 matches (larangan dicabut)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>OptionInput punya `int? Id` sebagai carrier identity; komentar T-418-06 direvisi (larangan dicabut, diganti kontrak validasi-server); build hijau.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditQuestion GET emit o.Id + POST ganti guard+upsert jadi identity-based (anti-tamper + set-difference + upsert by Id + pesan D-04)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:7876-7921 (EditQuestion GET — options projection :7908-7914)
    - Controllers/AssessmentAdminController.cs:7923-8160 (EditQuestion POST — guard :8022-8075 + upsert :8110-8160)
    - Controllers/AssessmentAdminController.cs:7707-7838 (CreateQuestion — konfirmasi Id diabaikan by construction, regresi OPTEDIT-05)
    - .planning/phases/420-editquestion-identity-based-option-editing/420-RESEARCH.md §2b, §2d, §2e
    - .planning/phases/420-editquestion-identity-based-option-editing/420-CONTEXT.md §decisions D-01..D-04
  </read_first>
  <behavior>
    - GET JSON `options` projection sekarang menyertakan `id = o.Id` per opsi (urut sudah OrderBy o.Id).
    - POST: Id asing (∉ q.Options) → tolak fail-closed "tidak valid", 0 mutasi, redirect.
    - POST: Id duplikat dalam submit → tolak "duplikat", 0 mutasi.
    - POST: hapus opsi tengah terjawab (Id ∈ answered, ∉ submit) → guard menyala → diblokir, pesan menyebut huruf urutan-tersimpan + cuplikan teks.
    - POST: hapus opsi tengah BELUM dijawab → sukses, record by Id terhapus, opsi tersisa tak ter-relabel.
    - POST: edit teks/kebenaran opsi terjawab (Id ∈ keptIds) → UPDATE record itu, sukses.
    - POST: konversi ke Essay pada soal terjawab → removedOptionIds = SEMUA existing → guard menyala → diblokir tanpa 500.
    - POST: CreateQuestion tetap normal (Id null → ADD).
  </behavior>
  <action>
    **PERUBAHAN A — EditQuestion GET JSON (`:7908-7914`).** Pada projection `q.Options.OrderBy(o => o.Id).Select(o => new { ... })`, TAMBAH field `id = o.Id,` sebagai field PERTAMA di anonymous object:
    ```csharp
    options = q.Options.OrderBy(o => o.Id).Select(o => new
    {
        id = o.Id,                 // Phase 420: carrier identity untuk hidden options[i].Id
        optionText = o.OptionText,
        isCorrect = o.IsCorrect,
        imagePath = o.ImagePath,
        imageAlt = o.ImageAlt
    }).ToList()
    ```

    **PERUBAHAN B — EditQuestion POST: GANTI seluruh blok guard edit-shrink (`:8022-8075`, dari komentar "GUARD EDIT-SHRINK (D-418-02..." sampai penutup `}` blok guard) DENGAN blok identity-based berikut.** Letakkan TEPAT setelah validator `QuestionOptionValidator.ValidateQuestionOptions` (`:8012-8020`), SEBELUM `var oldScore = q.ScoreValue;` (`:8078`). Pertahankan urutan: ResolveCorrectness + whitelist type + image-validate + section-validate + score-range + per-type + option-validator SUDAH jalan sebelum blok ini (jangan dipindah).

    ```csharp
    // ============================================================================================
    // Phase 420 (D-01..D-04) — IDENTITY-BASED option editing (ganti upsert POSISIONAL Phase 418).
    // Match baris input ke PackageOption existing by stable Id (BUKAN posisi). Menutup 999.15:
    // hapus opsi TENGAH pada soal terjawab kini terdeteksi (Id hilang dari submit) → guard menyala,
    // bukan me-relabel senyap. removedOptionIds dihitung set-difference-by-Id; guard + upsert pakai
    // himpunan yang SAMA (kill-drift). Regression-lock 999.14: hapus opsi terjawab tetap diblok pre-
    // SaveChanges (no FK-Restrict 500). D-418-02 (guard answered-option) DIPERTAHANKAN, hanya cara
    // hitung removedOptionIds yang berubah (posisi → identity).
    // ============================================================================================
    var existing   = q.Options.OrderBy(o => o.Id).ToList();
    var existingIds = existing.Select(o => o.Id).ToHashSet();

    // (D-01a) ANTI-TAMPER — fail-closed SEBELUM mutasi apa pun. Id non-null harus ∈ opsi soal ini.
    var submittedIds = options.Where(o => o.Id.HasValue).Select(o => o.Id!.Value).ToList();
    if (submittedIds.Any(id => !existingIds.Contains(id)))
    {
        TempData["Error"] = "Opsi yang diubah tidak valid untuk soal ini.";
        return RedirectToAction("ManagePackageQuestions", new { packageId });
    }
    if (submittedIds.Count != submittedIds.Distinct().Count())
    {
        TempData["Error"] = "Opsi duplikat terdeteksi.";
        return RedirectToAction("ManagePackageQuestions", new { packageId });
    }

    // kept = opsi existing yang Id-nya muncul di baris submit DENGAN teks tak-kosong (akan di-UPDATE).
    var keptIds = options
        .Where(o => o.Id.HasValue && !string.IsNullOrWhiteSpace(o.Text))
        .Select(o => o.Id!.Value)
        .ToHashSet();
    // newRows = baris Id-null + teks terisi (akan di-ADD).
    var newRows = options.Where(o => !o.Id.HasValue && !string.IsNullOrWhiteSpace(o.Text)).ToList();

    // (D-01c) removedOptionIds = set-difference by Id (Essay = SEMUA existing). KILL-DRIFT: guard + upsert pakai set ini.
    var removedOptionIds = (questionType == "Essay")
        ? existingIds.ToList()
        : existingIds.Except(keptIds).ToList();

    // GUARD answered-option (D-418-02 dipertahankan; D-03 answered = SEMUA response apa pun status sesi).
    if (removedOptionIds.Count > 0)
    {
        var answered = await _context.PackageUserResponses
            .Where(r => r.PackageOptionId.HasValue && removedOptionIds.Contains(r.PackageOptionId.Value))
            .Select(r => r.PackageOptionId!.Value)
            .Distinct()
            .ToListAsync();
        var blocked = OptionShrinkGuard.FindBlockedOptionIds(removedOptionIds, answered); // signature LOCKED
        if (blocked.Count > 0)
        {
            // (D-04) Huruf dari URUTAN TERSIMPAN (posisi di existing, OrderBy Id) + cuplikan teks
            // (opsi terblok mungkin sudah HILANG dari form karena baru dihapus HC).
            string[] letters = { "A", "B", "C", "D", "E", "F" };
            var parts = blocked
                .Select(id => existing.FindIndex(o => o.Id == id))
                .Where(idx => idx >= 0)
                .OrderBy(idx => idx)
                .Select(idx =>
                {
                    var letter = idx < letters.Length ? letters[idx] : (idx + 1).ToString();
                    var text = existing[idx].OptionText ?? "";
                    var snippet = text.Length > 40 ? text.Substring(0, 40) + "…" : text;
                    return $"\"{letter}\" (\"{snippet}\")";
                });
            TempData["Error"] = $"Opsi {string.Join("; ", parts)} sudah dijawab peserta dan tidak bisa dihapus. Batalkan perubahan atau pertahankan opsi ini.";
            return RedirectToAction("ManagePackageQuestions", new { packageId });
        }
    }
    ```

    **PERUBAHAN C — EditQuestion POST: GANTI seluruh blok upsert opsi (`:8110-8160`, dari komentar "OQ1: UPDATE-IN-PLACE opsi..." termasuk cabang Essay + cabang else loop posisional) DENGAN upsert identity berikut.** Letakkan di posisi yang sama (setelah resolusi gambar SOAL `:8108`, sebelum `await _context.SaveChangesAsync();` `:8162`). `imagePathsToDelete` sudah dideklarasikan di `:8089` — JANGAN deklarasi ulang.

    ```csharp
    // Phase 420: UPSERT IDENTITY. Match by Id (existing dari blok guard di atas, OrderBy o.Id).
    if (questionType == "Essay")
    {
        // Essay tak punya opsi: hapus SEMUA opsi existing + delete-candidate gambarnya.
        // Aman FK-Restrict: sudah lolos guard (removedOptionIds = semua existing → blocked jika terjawab).
        foreach (var oldOpt in q.Options.ToList())
        {
            if (!string.IsNullOrEmpty(oldOpt.ImagePath)) imagePathsToDelete.Add(oldOpt.ImagePath!);
        }
        _context.PackageOptions.RemoveRange(q.Options);
        q.Options.Clear();
    }
    else
    {
        // UPDATE / REMOVE existing by Id.
        foreach (var o in existing)
        {
            if (keptIds.Contains(o.Id))
            {
                var row = options.First(r => r.Id == o.Id);
                o.OptionText = row.Text!.Trim();
                o.IsCorrect  = row.IsCorrect;
                await ApplyOptionImageIntent(o, row.Image, row.ImageAlt, row.RemoveImage, packageId, imagePathsToDelete);
            }
            else
            {
                // Id tak ada di keptIds → REMOVE (sudah lolos guard, aman FK-Restrict).
                if (!string.IsNullOrEmpty(o.ImagePath)) imagePathsToDelete.Add(o.ImagePath!);
                _context.PackageOptions.Remove(o);
                q.Options.Remove(o);
            }
        }
        // ADD baris baru (Id null + teks).
        foreach (var row in newRows)
        {
            var n = new PackageOption { OptionText = row.Text!.Trim(), IsCorrect = row.IsCorrect };
            await ApplyOptionImageIntent(n, row.Image, row.ImageAlt, row.RemoveImage, packageId, imagePathsToDelete);
            q.Options.Add(n);
        }
    }
    ```

    Catatan implementasi:
    - `ResolveCorrectness` (`:7942`) sudah jalan di awal POST dan hanya set `IsCorrect` per submitted-position — TAK sentuh Id. Aman; baris yang benar membawa `IsCorrect=true` ke opsi yang di-match/di-ADD.
    - D-02 (edit teks/kebenaran opsi terjawab DIIZINKAN): TANPA gerbang baru — opsi terjawab yang Id-nya di `keptIds` cukup di-UPDATE. Modal `affectedSessions` (D-09, GET JSON `:7901`) sudah memperingatkan HC.
    - JANGAN ubah FK, validator, `correctIndex`, atau `ResolveCorrectness`. JANGAN sentuh CreateQuestion (Id diabaikan by construction → OPTEDIT-05 aman).
  </action>
  <acceptance_criteria>
    - `grep -n "id = o.Id" Controllers/AssessmentAdminController.cs` returns ≥1 match (GET JSON)
    - `grep -n "existingIds.Except(keptIds)" Controllers/AssessmentAdminController.cs` returns 1 match
    - `grep -n "!existingIds.Contains(id)" Controllers/AssessmentAdminController.cs` returns 1 match (anti-tamper)
    - `grep -n "Opsi duplikat terdeteksi" Controllers/AssessmentAdminController.cs` returns 1 match
    - `grep -n "tidak valid untuk soal ini" Controllers/AssessmentAdminController.cs` returns 1 match
    - `grep -n "keptIds.Contains(o.Id)" Controllers/AssessmentAdminController.cs` returns 1 match (upsert)
    - `grep -c "for (int i = 0; i < bound" Controllers/AssessmentAdminController.cs` returns 0 (loop posisional dihapus)
    - `grep -n "OptionShrinkGuard.FindBlockedOptionIds" Controllers/AssessmentAdminController.cs` returns 1 match (guard direuse)
    - `dotnet build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build HcPortal.csproj -c Debug</automated>
  </verify>
  <done>GET JSON emit `id`; POST upsert posisional + guard posisional diganti identity-based (anti-tamper fail-closed, set-difference removedOptionIds, upsert UPDATE/REMOVE/ADD by Id, pesan D-04 huruf urutan-tersimpan + cuplikan teks); guard `OptionShrinkGuard.FindBlockedOptionIds` direuse apa adanya; CreateQuestion tak tersentuh; build hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → EditQuestion POST | Input HC/Admin (RBAC Admin,HC + antiforgery), kini termasuk client-supplied `options[i].Id` |
| controller → SQL Server (EF) | Mutasi PackageOption; FK `PackageUserResponse→PackageOption` Restrict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-420-01 | Tampering/Elevation (Mass-assignment/IDOR) | EditQuestion POST `options[i].Id` | mitigate | Anti-tamper fail-closed: `submittedIds.Any(id => !existingIds.Contains(id))` → reject "tidak valid" + redirect SEBELUM mutasi apa pun (D-01a). Id asing tak bisa menyentuh opsi soal/paket lain. |
| T-420-02 | Tampering | EditQuestion POST duplikat Id | mitigate | `submittedIds.Count != Distinct().Count()` → reject "duplikat" SEBELUM mutasi (defensif, cegah `First(r=>r.Id==o.Id)` ambigu). |
| T-420-03 | Spoofing/Repudiation | EditQuestion POST endpoint | mitigate | Pertahankan `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` (sudah ada, JANGAN dihapus). |
| T-420-04 | Tampering (silent data corruption) | upsert relabel opsi terjawab | mitigate | Identity match by Id + guard set-difference (`existingIds.Except(keptIds)`) menyala untuk delete posisi MANAPUN (termasuk tengah) → opsi terjawab tak bisa dihapus/di-relabel senyap. |
| T-420-05 | Denial of Service (DbUpdateException 500) | hapus opsi terjawab → FK-Restrict | mitigate | Guard `OptionShrinkGuard.FindBlockedOptionIds` jalan pre-SaveChanges → blokir + redirect ramah (regression-lock 999.14, no 500). |
</threat_model>

<verification>
- `dotnet build` hijau (0 error).
- Algoritma identity diverifikasi end-to-end oleh integration test + Playwright di Plan 03 (file ini hanya backend; test ada di wave berikutnya).
- Sanity grep: loop posisional `for (int i = 0; i < bound` dihapus; `existingIds.Except(keptIds)` ada; `OptionShrinkGuard.FindBlockedOptionIds` masih dipanggil (guard direuse).
</verification>

<success_criteria>
- `Models/OptionInput.cs` punya `int? Id`; larangan T-418-06 dicabut + diganti kontrak validasi-server.
- EditQuestion GET JSON emit `id = o.Id`.
- EditQuestion POST: anti-tamper fail-closed (Id asing + duplikat) + removedOptionIds set-difference-by-Id + guard direuse + upsert UPDATE/REMOVE/ADD by Id + pesan D-04 (huruf urutan-tersimpan + cuplikan teks).
- CreateQuestion tidak tersentuh.
- Build hijau.
</success_criteria>

<output>
After completion, create `.planning/phases/420-editquestion-identity-based-option-editing/420-01-SUMMARY.md`
</output>
