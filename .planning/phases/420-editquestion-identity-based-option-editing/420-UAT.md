---
phase: 420
slug: editquestion-identity-based-option-editing
status: passed
method: Playwright MCP real-browser driver @5277 (+ DB-verified via sqlcmd)
date: 2026-06-25
---

# Phase 420 — UAT (VRF-01, real-browser)

**App:** http://localhost:5277 (ASPNETCORE_ENVIRONMENT=Development, Authentication__UseActiveDirectory=false). Auth: admin@pertamina.com / 123456.
**SEED_WORKFLOW:** snapshot `HcPortalDB_Dev_uat420.bak` (COPY_ONLY) → seed (PKG 9022, 3 MC questions, response→Q1 opt B) → test → **RESTORE** (verified pristine: ZZ-UAT420 sessions=0, UAT420 questions=0) → .bak deleted. SEED_JOURNAL: cleaned.
**Regression artifact:** `tests/e2e/identity-option-edit-420.spec.ts` (runnable; mirrors the MCP-driven flow below for future regression).

## Result: 3/3 PASS

### S1 — Delete middle option ANSWERED → BLOCKED (OPTEDIT-02 / VRF-01) ✅
Q1 (4-opt MC, participant answered option **B**, the MIDDLE). Edit form opened → hidden carrier `option_B_id` populated from GET JSON = **80225** (= seed Q1 option B). **PATCH C verified.** Deleted row B → reletter: remaining rows kept their Ids + reindexed names (`A:id=80224 | B(txt C):id=80226 | C(txt D):id=80227`, B's 80225 absent from submit). **PATCH B verified.** Submit → real-browser alert:
> **Error:** Opsi "B" ("B") sudah dijawab peserta dan tidak bisa dihapus. Batalkan perubahan atau pertahankan opsi ini.
Exact D-04 message (stored-order letter + text snippet). No 500. **DB after:** Q1 still 4 options (80224..80227, B="B" intact), response→80225 still present (count=1). The silent-relabel bug (999.15) is BLOCKED in real browser.

### S2 — Delete middle option UNANSWERED → success, NO relabel (OPTEDIT-01) ✅
Q2 (4-opt MC, unanswered). Edit → delete row B → submit → "Success: Soal berhasil diperbarui." **DB after:** 3 options `80228=A, 80230=C, 80231=D` — survivors keep original Ids+text; **C is still "C" (Id 80230), NOT relabeled to "B"**; B (80229) gone. Identity match by Id, not position.

### S3 — Add option (clone gotcha §2c) → ADD, A not overwritten (OPTEDIT-05) ✅
Q3 (4-opt MC). Edit → "Tambah Opsi" → new row E hidden Id = **empty** (clone-reset cleared `type=hidden`; A's Id 80232 NOT inherited). **PATCH D verified.** (Observed pre-existing form quirk: adding a row drops the MC correct-radio selection — re-checked A; unrelated to the hidden-Id change.) Filled "E" → submit → success. **DB after:** 5 options `80232=A..80236=E`; option A (80232) text still "A" (Id stable, NOT overwritten by the new option); E added (80236); correct=A.

## Verdict
All 3 success-criteria-derived scenarios PASS in a real browser + DB-confirmed. Client-JS carriers proven (PATCH B/C/D — the part controller-integration tests cannot reach, lesson 354). VRF-01 satisfied (controller-integration leg = 8 tests green; real-browser leg = this UAT). DB restored, no residue.
