---
phase: 421-retake-lifecycle-hardening
plan: 03
type: execute
wave: 3
depends_on: ["421-02"]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Helpers/RetakeRules.cs
  - Views/Admin/EditAssessment.cshtml
  - Views/Admin/ManagePackages.cshtml
  - Views/Admin/AssessmentMonitoringDetail.cshtml
  - HcPortal.Tests/ParticipantRemoveGuardTests.cs
  - HcPortal.Tests/RetakeSettingsEndpointTests.cs
  - HcPortal.Tests/RetakeRulesTests.cs
autonomous: false
requirements: [RTH-02, RTH-04, RTH-05]
must_haves:
  truths:
    - "HC menghapus peserta yang punya riwayat ujian (Abandoned / StartedAt / AttemptHistory) → penghapusan dibatalkan + warning, sampai HC submit ulang dengan flag konfirmasi"
    - "Saat penghapusan benar-benar dijalankan, tidak ada AssessmentAttemptResponseArchive yatim (cascade DB menghapus arsip via AttemptHistory tracked-delete)"
    - "Flag konfirmasi tersembunyi HANYA menyembunyikan peringatan — guard riwayat dievaluasi ulang server-side tiap POST (forged flag tetap lewat guard yang sama)"
    - "Menurunkan MaxAttempts di bawah jumlah percobaan terpakai → modal konfirmasi pra-simpan; bila dikonfirmasi, setelan tetap tersimpan (non-blocking, perilaku retroaktif dipertahankan)"
    - "Cooldown yang bisa mendorong eligibility melewati ExamWindowCloseDate → warning non-blocking ke HC; setelan tetap tersimpan"
    - "HC reset sesi lulus/ber-NomorSertifikat menampilkan konfirmasi pencabutan sertifikat sebelum eksekusi; sesi non-lulus tetap memakai konfirmasi reset biasa"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "EditAssessment POST param confirmRemoveWithHistory + guard Abandoned/StartedAt/AttemptHistory + UpdateRetakeSettings D-02 warning (via RetakeRules.CooldownMayExceedWindow) + D-07 used-count komentar (angka dari GET via helper D-05)"
      contains: "confirmRemoveWithHistory"
    - path: "Helpers/RetakeRules.cs"
      provides: "Pure static CooldownMayExceedWindow(nowUtc, examWindowCloseDate, cooldownHours) — predikat D-02 (kill-drift, +7h WIB satu tempat) dipanggil controller DAN test"
      contains: "CooldownMayExceedWindow"
    - path: "Views/Admin/EditAssessment.cshtml"
      provides: "Server round-trip hidden flag + tombol 'Tetap Hapus' (btn-danger) Pola B"
      contains: "confirmRemoveWithHistory"
    - path: "Views/Admin/ManagePackages.cshtml"
      provides: "Modal pra-simpan MaxAttempts<terpakai (Pola C, non-blocking)"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Conditional confirm cabut-cert (Pola C) — copy peringatan pencabutan NomorSertifikat saat session.IsPassed/NomorSertifikat, view-only (VM sudah expose field)"
      contains: "MENCABUT"
    - path: "HcPortal.Tests/ParticipantRemoveGuardTests.cs"
      provides: "Integration: Abandoned/ber-riwayat ditolak tanpa flag; dihapus + 0 orphan archive dengan flag"
    - path: "HcPortal.Tests/RetakeRulesTests.cs"
      provides: "Unit: CooldownMayExceedWindow — window-buka+cooldown-lewat=true, window-tutup/EWCD-null/cooldown-0=false (+7h WIB di satu tempat)"
  key_links:
    - from: "Views/Admin/EditAssessment.cshtml (re-submit form)"
      to: "AssessmentAdminController.EditAssessment POST (confirmRemoveWithHistory)"
      via: "hidden input confirmRemoveWithHistory=true + @Html.AntiForgeryToken()"
      pattern: "confirmRemoveWithHistory"
    - from: "AssessmentAdminController.EditAssessment removedUserIds loop"
      to: "AssessmentAttemptResponseArchive (cascade)"
      via: "RemoveRange(AttemptHistory) tracked → SaveChanges → DB cascade"
      pattern: "RemoveRange\\(attempts\\)"
    - from: "AssessmentAdminController.UpdateRetakeSettings (D-02 warning)"
      to: "Helpers/RetakeRules.CooldownMayExceedWindow (predikat pure)"
      via: "panggil predikat pure (bukan boolean inline) — controller + test sama-sama call"
      pattern: "CooldownMayExceedWindow"
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml (reset form)"
      to: "AssessmentAdminController.ResetAssessment POST"
      via: "onsubmit confirm conditional (session.IsPassed/NomorSertifikat) — copy cabut-cert sebelum POST"
      pattern: "MENCABUT"
---

<objective>
Tutup RTH-04 (D-06 guard hapus peserta ber-riwayat + cleanup arsip, server round-trip), RTH-05 (D-07 konfirmasi pra-simpan MaxAttempts turun, non-blocking), porsi RTH-01/D-02 (peringatan dini HC cooldown bisa lewat window) di `UpdateRetakeSettings`, plus porsi RTH-02/D-04 (konfirmasi pencabutan sertifikat sebelum HC reset sesi lulus) yang BELUM dicakup 421-01 (Results.cshtml UX) maupun 421-02 (warning-count site). Semua di `AssessmentAdminController.cs` + helper `RetakeRules` + tiga view admin existing — server-authoritative, reuse helper `CountEraRetakeArchives` dari 421-02, dan ekstrak predikat D-02 ke `RetakeRules.CooldownMayExceedWindow` (kill-drift).

Purpose:
- RTH-04: Loop hapus peserta `EditAssessment` POST saat ini HANYA menolak InProgress/Completed — sesi Abandoned atau ber-AttemptHistory/StartedAt bisa terhapus diam-diam tanpa peringatan. Perluas deteksi + soft-confirm via server round-trip + hidden flag (BUKAN JS-only) agar HC sadar menghapus riwayat. Cleanup arsip = andalkan cascade DB (delete AttemptHistory tracked → cascade hapus archives) + assert 0 orphan.
- RTH-05: Menurunkan MaxAttempts di bawah pemakaian = perubahan retroaktif yang memengaruhi peserta — beri konfirmasi pra-simpan (modal) tanpa memblokir. Hitung "terpakai" pakai helper snapshot-presence yang sama (kill-drift).
- D-02 (RTH-01): Cooldown panjang bisa membuat peserta gagal tak sempat retake sebelum window tutup — peringatkan HC saat set (non-blocking). Predikat di-ekstrak ke `RetakeRules.CooldownMayExceedWindow` (pure, +7h WIB di satu tempat) supaya controller dan test memanggil kode yang SAMA (kill-drift, cegah drift +7h→+8h diam-diam).
- D-04 (RTH-02): HC bisa mass-reset sesi yang sudah LULUS / ber-NomorSertifikat dan mencabut sertifikat tanpa peringatan apa pun. Tambah konfirmasi conditional di titik aksi reset (`AssessmentMonitoringDetail.cshtml`) — copy peringatan cabut-cert hanya saat sesi ber-cert; sesi non-lulus tetap pakai confirm reset biasa. View-only: VM `MonitoringSessionViewModel` sudah expose `IsPassed`/`NomorSertifikat`, TANPA ubah controller/VM. Pencabutan/nol-kan cert tetap server-side (D-03 di plan lain via service); D-04 = lapis UX di titik aksi.

Output: `EditAssessment` POST ber-param `confirmRemoveWithHistory` + guard riwayat diperluas; view round-trip flag + tombol "Tetap Hapus"; `UpdateRetakeSettings` warning D-02 via `RetakeRules.CooldownMayExceedWindow`; modal pra-simpan D-07 di ManagePackages; confirm conditional cabut-cert D-04 di AssessmentMonitoringDetail; test guard hapus-peserta (real-SQL, 0-orphan) + unit `CooldownMayExceedWindow` + extend warning-count test; UAT live tiga konfirmasi UI + warning D-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/421-retake-lifecycle-hardening/421-CONTEXT.md
@.planning/phases/421-retake-lifecycle-hardening/421-RESEARCH.md
@.planning/phases/421-retake-lifecycle-hardening/421-PATTERNS.md
@.planning/phases/421-retake-lifecycle-hardening/421-UI-SPEC.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diambil dari source aktual (VERIFIED 2026-06-23). -->

Helper dari 421-02 (REUSE — JANGAN redefinisi) — `Helpers/RetakeCountingRules.cs`:
- `CountForUserAsync(...)` → per-user count snapshot-presence (cap).
- `MaxInGroupAsync(...)` → max-per-user-in-group snapshot-presence (warning). RTH-05 "terpakai" pakai BENTUK INI (nilai sama dengan `ViewBag.RetakeMaxAttemptsUsedInGroup` minus +1). Helper TIDAK menyertakan +1 — caller tambah +1.
> Catatan dependensi: 421-02 me-wire situs warning `AssessmentAdminController.cs:5795-5798` (`ManagePackages` GET) ke `MaxInGroupAsync`. Plan ini TIDAK menyentuh situs warning GET itu lagi — hanya MEMBACA angka `ViewBag.RetakeMaxAttemptsUsedInGroup` yang sama di view (modal D-07). `UpdateRetakeSettings` POST TIDAK menghitung ulang used-count (redirect ke GET yang menghitung ulang) — lihat Task 2 step B (no-op berdokumentasi).

Helper PURE existing — `Helpers/RetakeRules.cs` (static, EF-free, `nowUtc` di-inject; pola kill-drift):
- `CanRetake(...)`, `ShouldHideRetakeToggle(...)`, `ResolveReviewMode(...)` (VERIFIED, full file 85 baris).
- ★ TAMBAH method baru `CooldownMayExceedWindow(DateTime nowUtc, DateTime? examWindowCloseDate, int retakeCooldownHours)` untuk predikat D-02 (lihat Task 2 step A). Konvensi +7h WIB HIDUP HANYA di sini (satu tempat) — controller + test memanggil method ini, BUKAN mereplikasi boolean.

EditAssessment POST signature SAAT INI — AssessmentAdminController.cs:1823-1826 (VERIFIED):
```csharp
public async Task<IActionResult> EditAssessment(int id, AssessmentSession model, List<string> NewUserIds,
    DateTime? PreSchedule, int? PreDurationMinutes, DateTime? PreExamWindowCloseDate,
    DateTime? PostSchedule, int? PostDurationMinutes, DateTime? PostExamWindowCloseDate,
    List<string>? UserIds)
// ★ D-06: tambah param terakhir `bool confirmRemoveWithHistory = false` ★
```

Guard hapus peserta SAAT INI (HANYA InProgress/Completed) — AssessmentAdminController.cs:1924-1939 (VERIFIED), di dalam cabang Pre-Post `removedUserIds` loop:
```csharp
var userPreSession = preGroup.FirstOrDefault(s => s.UserId == removedUserId);
var userPostSession = postGroup.FirstOrDefault(s => s.UserId == removedUserId);
if (userPreSession != null && (userPreSession.Status == "InProgress" || userPreSession.Status == "Completed")) {
    TempData["Error"] = $"Tidak dapat menghapus peserta — sesi Pre-Test sudah {userPreSession.Status}.";
    continue;
}
if (userPostSession != null && (userPostSession.Status == "InProgress" || userPostSession.Status == "Completed")) {
    TempData["Error"] = $"Tidak dapat menghapus peserta — sesi Post-Test sudah {userPostSession.Status}.";
    continue;
}
```

Cascade delete idiom YANG SUDAH ADA di loop yang sama — AssessmentAdminController.cs:1953-1956 (VERIFIED):
```csharp
var attempts = await _context.AssessmentAttemptHistory
    .Where(h => sessionIdsToRemove.Contains(h.SessionId))
    .ToListAsync();
if (attempts.Any()) _context.AssessmentAttemptHistory.RemoveRange(attempts);   // tracked → cascade fires on SaveChanges
```
> KRITIS: delete pakai TRACKED `RemoveRange` + `SaveChanges` (BUKAN ExecuteDelete/raw) → cascade FK DB akan fire. Cascade `AssessmentAttemptResponseArchive → AttemptHistory` = `OnDelete(Cascade)` (ApplicationDbContext.cs:588-595 VERIFIED). JANGAN tambah RemoveRange archives manual (double-delete = error). Cukup assert 0 orphan via test.

UpdateRetakeSettings SAAT INI — AssessmentAdminController.cs:5654-5698 (VERIFIED):
```csharp
public async Task<IActionResult> UpdateRetakeSettings(int assessmentId, bool allowRetake, int maxAttempts, int retakeCooldownHours)
{
    var assessment = await _context.AssessmentSessions.FirstOrDefaultAsync(a => a.Id == assessmentId);
    if (assessment == null) return NotFound();
    if (HcPortal.Helpers.RetakeRules.ShouldHideRetakeToggle(assessment.AssessmentType, assessment.IsManualEntry)) {
        TempData["Error"] = "Ujian ulang tidak berlaku untuk Pre-Test atau assessment manual.";
        return RedirectToAction("ManagePackages", new { assessmentId });
    }
    maxAttempts = Math.Clamp(maxAttempts, 1, 5);
    retakeCooldownHours = Math.Clamp(retakeCooldownHours, 0, 168);
    // ... sibling propagation (Title/Category/Schedule.Date) foreach set 3 field + UpdatedAt ...
    await _context.SaveChangesAsync();
    // ... audit try/catch warn-only ...
    TempData["Success"] = "Pengaturan ujian ulang berhasil disimpan.";
    return RedirectToAction("ManagePackages", new { assessmentId });
}
```
> `assessment.ExamWindowCloseDate` (DateTime?, AssessmentSession:77) tersedia di entity yang sudah di-load → dipakai D-02. RBAC `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` di :5652-5653 (VERIFIED) — PRESERVE saat menambah param/logika.

Konvensi +7h WIB (byte-identik StartExam CMPController:956) — dipakai di `CooldownMayExceedWindow`:
```csharp
DateTime.UtcNow.AddHours(7) > assessment.ExamWindowCloseDate.Value   // verbatim; JANGAN +8h/DateTime.Now/TimeZoneInfo
```

Idiom toast (VERIFIED): `TempData["Error"]` / `TempData["Success"]` dipakai di method ini; `TempData["Warning"]` = non-blocking warning (D-02/D-06), konsumsi di layout/view.

View round-trip target — Views/Admin/EditAssessment.cshtml (main edit form `editAssessmentForm` + `@Html.AntiForgeryToken()`). Form `deletePesertaForm` (:708-713) yang menunjuk `DeleteAssessmentPeserta` adalah DEAD (endpoint 0 match di controller — VERIFIED). D-06 round-trip naik di MAIN edit form, BUKAN dead form. JANGAN tarik v32.5 FlexibleParticipantRemove dari branch main.

View warning/modal target — Views/Admin/ManagePackages.cshtml:
- Alert non-blocking existing (:157-163) = `alert alert-warning d-flex align-items-start` + `bi bi-exclamation-triangle` (idiom untuk D-02/D-07 copy).
- Form retake (:143-145) = `<form method="post" asp-action="UpdateRetakeSettings" ...>` + `@Html.AntiForgeryToken()` + `<input type="hidden" name="assessmentId" ...>`.
- Modal idiom referensi: `Views/CMP/Results.cshtml:534-553` (Bootstrap modal).
- `ViewBag.MaxAttempts` + `ViewBag.RetakeMaxAttemptsUsedInGroup` (di-set GET, sudah snapshot-aware via 421-02) tersedia untuk gating modal pra-simpan.

View confirm cabut-cert target (D-04) — Views/Admin/AssessmentMonitoringDetail.cshtml:333-341 (VERIFIED):
- Loop `@foreach (var session in Model.Sessions)` (:241) → variabel iterasi = `session`.
- Form reset existing (:334-341) = `<form asp-action="ResetAssessment" ... onsubmit="return confirm('Reset sesi ini? Semua jawaban akan dihapus dan peserta dapat mengulang ujian.')">` + `@Html.AntiForgeryToken()` + `<input type="hidden" name="id" value="@session.Id" />`.
- VM `MonitoringSessionViewModel` (AssessmentMonitoringViewModel.cs:48-68) SUDAH expose `IsPassed` (bool?, :55) + `NomorSertifikat` (string?, :67). TIDAK perlu ubah VM/controller — D-04 view-only.

Test fixture (VERIFIED RetakeServiceTests.cs) — REUSE:
- `RetakeServiceFixture : IAsyncLifetime` (disposable `HcPortalDB_Test_{guid}` @ `localhost\SQLEXPRESS`, `MigrateAsync` full chain incl AddRetakeColumnsAndArchive, `EnsureDeletedAsync` on dispose).
- `IClassFixture<RetakeServiceFixture>`, `NewCtx()` = `new ApplicationDbContext(_fixture.Options)`, `[Trait("Category","Integration")]`.
- Seed: `SeedUserAsync(ctx)`, `SeedSessionAsync(ctx, userId, title, category, status, isPassed, ...)` (default status="Completed"), `SeedEraRetakeArchiveAsync(ctx, userId, title, category)` (AttemptHistory + 1 child archive), `SeedLegacyArchiveAsync(...)` (AttemptHistory tanpa child). CATATAN: helper seed ini PRIVATE di RetakeServiceTests.cs — file test BARU replikasi seed serupa (pola RetakeSettingsEndpointTests yang punya `SeedUserAsync`/`MakeSibling` sendiri).
- AttemptHistory field: `SessionId`, `UserId`, `Title`, `Category`, `AttemptNumber`. Archive: `AttemptHistoryId`, `PackageQuestionId`, `QuestionText`, `AnswerText`, `IsCorrect`, `AwardedScore`.
- AssessmentSession field relevan: `Status` ("Open"/"InProgress"/"Completed"/"Abandoned"/"Cancelled"), `StartedAt` (DateTime?), `LinkedGroupId` (int?).

Test fixture PURE (VERIFIED RetakeRulesTests.cs) — REUSE untuk `CooldownMayExceedWindow`:
- No DB, no fixture; `private static readonly DateTime Now = new DateTime(2026,06,19,12,00,00, DateTimeKind.Utc)` (clock tetap). `[Fact]`/`[Theory]`+`[InlineData]`. Pola helper `Can(...)` default-eligible + override per cabang.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard hapus peserta ber-riwayat (Abandoned/StartedAt/AttemptHistory) + server round-trip flag + cleanup arsip via cascade (RTH-04, D-06)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:1823-1826 (signature EditAssessment POST) + :1916-1971 (cabang Pre-Post add/remove peserta: guard :1929-1939, sessionsToRemove + cascade delete :1941-1971)
    - Controllers/AssessmentAdminController.cs:2030-2055 (cari titik SaveChangesAsync setelah loop — pastikan flag/guard tidak melewati commit; cabang Pre-Post return ke ManageAssessment setelah save)
    - Data/ApplicationDbContext.cs:588-595 (cascade FK AssessmentAttemptResponseArchive → AttemptHistory, OnDelete Cascade)
    - Views/Admin/EditAssessment.cshtml:697-754 (main form editAssessmentForm + @Html.AntiForgeryToken() + dead deletePesertaForm :708-713 + native confirm :745-754)
    - 421-PATTERNS.md §"EditAssessment POST removedUserIds loop" (Pola B server round-trip) + §"Views/Admin/EditAssessment.cshtml" (hidden flag)
    - 421-UI-SPEC.md §"RTH-04 / D-06" (Pola B) + Copywriting row "hapus peserta ber-riwayat" + "Tetap Hapus" CTA
    - HcPortal.Tests/RetakeServiceTests.cs:103-194 (fixture + seed helpers untuk direplikasi di file test baru)
  </read_first>
  <action>
    A) Signature (server round-trip flag): tambahkan param terakhir `bool confirmRemoveWithHistory = false` ke `EditAssessment` POST (:1823-1826), default `= false` (sesuai pola optional param existing `List<string>? UserIds`). PRESERVE `[HttpPost]`/`[Authorize]`/`[ValidateAntiForgeryToken]` existing pada action ini.

    B) Perluas guard di loop `removedUserIds` (:1929-1939). SETELAH dua guard InProgress/Completed existing (BIARKAN keduanya apa adanya — itu hard-block, tetap pakai `TempData["Error"]`+`continue`), TAMBAH deteksi riwayat untuk Pre DAN Post. Predikat riwayat (eksplisit, bukan "align"):
    ```csharp
    bool preHasHistory = userPreSession != null &&
        (userPreSession.Status == "Abandoned" || userPreSession.StartedAt != null);
    bool postHasHistory = userPostSession != null &&
        (userPostSession.Status == "Abandoned" || userPostSession.StartedAt != null);
    // AttemptHistory by SessionId (riwayat percobaan tercatat walau status kembali Open via reset)
    var sessIdsForHistCheck = new List<int>();
    if (userPreSession != null) sessIdsForHistCheck.Add(userPreSession.Id);
    if (userPostSession != null) sessIdsForHistCheck.Add(userPostSession.Id);
    bool hasAttemptHistory = sessIdsForHistCheck.Count > 0 && await _context.AssessmentAttemptHistory
        .AnyAsync(h => sessIdsForHistCheck.Contains(h.SessionId));
    if ((preHasHistory || postHasHistory || hasAttemptHistory) && !confirmRemoveWithHistory) {
        var nama = userPreSession?.UserId ?? userPostSession?.UserId ?? removedUserId;
        TempData["Warning"] = $"Peserta {nama} memiliki riwayat ujian (sesi sudah dimulai/ditinggalkan/percobaan tercatat). Menghapusnya akan menghapus riwayat percobaan terkait. Klik 'Tetap Hapus' untuk melanjutkan.";
        continue;   // BATALKAN penghapusan peserta INI; edit form lain TIDAK hilang (field metadata sudah di-set di atas loop)
    }
    ```
    Gunakan nama variabel `userPreSession`/`userPostSession`/`removedUserId` PERSIS seperti source (:1926-1927). Untuk teks nama peserta, bila ada cara load FullName dari `_userManager`/Users di scope, boleh dipakai; bila tidak, pakai `removedUserId` (jangan tambah query mahal). `TempData["Warning"]` = non-blocking (UI-SPEC tingkat-pesan: alert-warning).

    C) Cleanup arsip: JANGAN tambah RemoveRange archives manual. Saat `confirmRemoveWithHistory==true` (atau peserta tak ber-riwayat), eksekusi cascade delete existing (:1953-1956) yang me-`RemoveRange(attempts)` tracked → `SaveChanges` (di akhir cabang) → DB cascade menghapus `AssessmentAttemptResponseArchive` by `AttemptHistoryId`. Verifikasi (saat edit) bahwa delete AttemptHistory ada DI DALAM cabang yang dieksekusi setelah guard lolos, dan tidak ada path ExecuteDelete/raw yang melewati cascade. Acceptance criterion mengunci 0-orphan.

    D) View round-trip (EditAssessment.cshtml): saat `TempData["Warning"]` ber-isi (peserta ber-riwayat), render alert-warning + tombol **"Tetap Hapus"** (`btn btn-danger btn-sm`) yang men-submit ULANG main edit form (`editAssessmentForm`) dengan `<input type="hidden" name="confirmRemoveWithHistory" value="true" />`. Form re-submit WAJIB tetap memuat `@Html.AntiForgeryToken()` (ASVS V13) + field peserta (`UserIds`) yang sama agar removedUserIds terhitung ulang. Native `confirm()` existing (:745-754) BOLEH tetap sebagai first-line UX, tetapi keputusan akhir = server (flag). JANGAN buat CSS/komponen baru — reuse Bootstrap alert + btn existing (UI-SPEC Consistency Rule 1). Semua teks Bahasa Indonesia.

    E) Test (file BARU `HcPortal.Tests/ParticipantRemoveGuardTests.cs`): `[Trait("Category","Integration")]`, `IClassFixture<RetakeServiceFixture>`, replikasi body loop hapus-peserta atas real-SQL (pola RetakeSettingsEndpointTests "replicate endpoint body"). Replikasi seed serupa `SeedSessionAsync`/`SeedEraRetakeArchiveAsync`. Tulis test:
    - Test 1 (Abandoned ditolak tanpa flag): seed Pre+Post sesi grup, set salah satu `Status="Abandoned"` → jalankan logika guard dengan `confirmRemoveWithHistory=false` → assert sesi MASIH ADA (tidak terhapus) + kondisi warning ter-trigger.
    - Test 2 (StartedAt ditolak tanpa flag): seed sesi `StartedAt != null`, status Open → guard `false` → sesi tetap ada.
    - Test 3 (AttemptHistory ditolak tanpa flag): seed sesi + `SeedEraRetakeArchiveAsync` (AttemptHistory by SessionId yang benar) → guard `false` → sesi + AttemptHistory + Archive tetap ada.
    - Test 4 (dengan flag → hapus + 0 orphan): kondisi sama Test 3, `confirmRemoveWithHistory=true` → eksekusi delete (RemoveRange AttemptHistory tracked + SaveChanges) → assert sesi terhapus, AttemptHistory terhapus, DAN `await ctx.AssessmentAttemptResponseArchives.AnyAsync()` untuk AttemptHistoryId terkait == false (0 orphan via cascade).
    - Test 5 (backward-compat tanpa riwayat): seed sesi status Open, StartedAt null, tanpa AttemptHistory → delete langsung jalan tanpa flag (perilaku existing tak berubah).
    CATATAN: `SeedEraRetakeArchiveAsync` existing meng-set `SessionId = 0`; di test Test 3/4 set `SessionId` = Id sesi yang di-seed (buat varian seed lokal yang menerima sessionId) agar `hasAttemptHistory` query `Contains(h.SessionId)` cocok.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ParticipantRemoveGuard" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "confirmRemoveWithHistory" Controllers/AssessmentAdminController.cs` >= 2 (signature param + guard cek)
    - `grep "Status == \"Abandoned\"\|StartedAt != null" Controllers/AssessmentAdminController.cs` cocok (deteksi riwayat diperluas)
    - `grep "AssessmentAttemptHistory" Controllers/AssessmentAdminController.cs` cocok di guard (AnyAsync by SessionId) — deteksi AttemptHistory hadir
    - TIDAK ada RemoveRange archives manual baru: `grep "AssessmentAttemptResponseArchive" Controllers/AssessmentAdminController.cs` TIDAK menambah RemoveRange (cleanup via cascade) — bila ada hit, pastikan bukan double-delete
    - `grep "confirmRemoveWithHistory" Views/Admin/EditAssessment.cshtml` cocok (hidden flag re-submit) DAN `grep "Tetap Hapus" Views/Admin/EditAssessment.cshtml` cocok
    - File `HcPortal.Tests/ParticipantRemoveGuardTests.cs` ada dengan >=5 test — `grep -c "Fact\|Theory" HcPortal.Tests/ParticipantRemoveGuardTests.cs` >= 5
    - Test 4 meng-assert 0 orphan: `grep "Archive" HcPortal.Tests/ParticipantRemoveGuardTests.cs` cocok (cek orphan)
    - `dotnet build` exit 0; `dotnet test --filter "FullyQualifiedName~ParticipantRemoveGuard"` exit 0 (butuh SQLEXPRESS)
  </acceptance_criteria>
  <done>EditAssessment POST ber-param confirmRemoveWithHistory; guard menolak hapus peserta Abandoned/StartedAt/ber-AttemptHistory tanpa flag (warning + cancel), menghapus + 0 orphan archive dengan flag (cascade DB); view round-trip + tombol "Tetap Hapus" server-authoritative; 5 test hijau; backward-compat hapus tanpa-riwayat tak berubah.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ekstrak RetakeRules.CooldownMayExceedWindow (D-02 pure) + warning cooldown>window di UpdateRetakeSettings (RTH-01/D-02) + modal pra-simpan MaxAttempts<terpakai (RTH-05/D-07, non-blocking)</name>
  <read_first>
    - Helpers/RetakeRules.cs (full, 85 baris — pola pure static kill-drift; tempat tambah CooldownMayExceedWindow)
    - HcPortal.Tests/RetakeRulesTests.cs (full — pola helper `Can(...)` + fixed clock `Now` + `[Fact]`/`[Theory]`/`[InlineData]`)
    - Controllers/AssessmentAdminController.cs:5648-5698 (UpdateRetakeSettings full — clamp :5667-5668, sibling propagation :5671-5683, SaveChanges :5684, audit :5686-5694, TempData Success :5696)
    - Controllers/AssessmentAdminController.cs:5789-5798 (warning count GET site — SUDAH di-wire ke MaxInGroupAsync oleh 421-02; BACA untuk konteks used-count, JANGAN edit lagi)
    - Controllers/CMPController.cs:955-960 (+7h WIB expression untuk D-02 verbatim — disalin SEKALI ke CooldownMayExceedWindow)
    - Views/Admin/ManagePackages.cshtml:140-176 (card Ujian Ulang: form+antiforgery :143-145, input maxAttempts/cooldown, alert-warning existing :157-163, tombol Simpan :172)
    - Views/CMP/Results.cshtml:534-553 (idiom Bootstrap modal untuk D-07)
    - 421-PATTERNS.md §"UpdateRetakeSettings ~:5654 — D-02 + D-07" + §"Views/Admin/ManagePackages.cshtml"
    - 421-UI-SPEC.md §"RTH-01 / D-02" + §"RTH-05 / D-07" + Copywriting row D-02 & D-07
    - HcPortal.Tests/RetakeSettingsEndpointTests.cs (full — pola replicate endpoint body untuk extend test)
  </read_first>
  <behavior>
    `RetakeRules.CooldownMayExceedWindow(DateTime nowUtc, DateTime? examWindowCloseDate, int retakeCooldownHours)` — pure, EF-free, +7h WIB di satu tempat. Tabel kebenaran (assert di RetakeRulesTests):
    - EWCD null → false (no window, no warning).
    - retakeCooldownHours <= 0 → false (tak ada jeda → tak mungkin lewat window).
    - Window SUDAH tutup (`nowUtc.AddHours(7) > EWCD`) → false (D-02 hanya relevan saat window MASIH buka; sudah-tutup ditangani gate D-01 di plan lain).
    - Window masih buka (`nowUtc.AddHours(7) <= EWCD`) DAN `nowUtc.AddHours(7).AddHours(cooldown) > EWCD` → true (cooldown mendorong eligibility melewati batas).
    - Window masih buka DAN cooldown muat di sisa window (`nowUtc.AddHours(7).AddHours(cooldown) <= EWCD`) → false.
    - Kasus boundary +7h: bila +7h dihapus/diganti +8h, salah satu assert HARUS gagal (kill-drift) — sertakan case yang sensitif terhadap konstanta 7.
  </behavior>
  <action>
    A) Ekstrak predikat D-02 ke `Helpers/RetakeRules.cs` (RED→GREEN): tambah method pure static (mirror gaya `CanRetake`/`ShouldHideRetakeToggle`, `nowUtc` injected, EF-free, XML-doc Bahasa Indonesia singkat):
    ```csharp
    /// <summary>
    /// v32.5 RTH-01/D-02 — peringatan dini HC: apakah masa jeda (cooldown) bisa mendorong
    /// eligibility retake MELEWATI batas tutup ujian (ExamWindowCloseDate)? PURE, +7h WIB di SATU
    /// tempat (byte-identik StartExam:956) supaya controller & test memanggil kode yang sama (kill-drift).
    /// False bila: tak ada window, cooldown 0, atau window SUDAH tutup (kasus itu = gate D-01, bukan D-02).
    /// </summary>
    public static bool CooldownMayExceedWindow(DateTime nowUtc, DateTime? examWindowCloseDate, int retakeCooldownHours)
    {
        if (examWindowCloseDate == null) return false;
        if (retakeCooldownHours <= 0) return false;
        var nowWib = nowUtc.AddHours(7);                                  // +7h WIB verbatim — JANGAN +8h/DateTime.Now/TimeZoneInfo
        if (nowWib > examWindowCloseDate.Value) return false;            // window sudah tutup → urusan D-01
        return nowWib.AddHours(retakeCooldownHours) > examWindowCloseDate.Value;
    }
    ```
    Tambahkan unit test di `HcPortal.Tests/RetakeRulesTests.cs` SESUAI <behavior> (RED dulu: tulis test memanggil `RetakeRules.CooldownMayExceedWindow` sebelum method ada → gagal kompil/assert; GREEN: implement). Pakai fixed clock `Now` (2026-06-19 12:00 UTC). Sertakan minimal: EWCD-null→false, cooldown-0→false, window-tutup→false, window-buka+cooldown-lewat→true, window-buka+cooldown-muat→false, dan 1 case boundary yang sensitif ke konstanta 7 (mis. EWCD = nowUtc+7h+1jam, cooldown=2 → true; bila +7h→+8h maka nowWib jadi > EWCD → false, test gagal).

    B) D-02 warning di `UpdateRetakeSettings` — PANGGIL predikat pure (BUKAN boolean inline): SETELAH `SaveChangesAsync` (:5684), evaluasi via helper baru:
    ```csharp
    // D-02 (RTH-01): peringatan non-blocking — cooldown bisa lewat window? (predikat pure, kill-drift +7h)
    if (HcPortal.Helpers.RetakeRules.CooldownMayExceedWindow(DateTime.UtcNow, assessment.ExamWindowCloseDate, retakeCooldownHours))
    {
        TempData["Warning"] = "Masa jeda ujian ulang yang Anda atur bisa melewati batas tutup ujian. Peserta yang gagal mungkin tidak sempat mengulang sebelum ujian ditutup. Pengaturan tetap bisa disimpan.";
    }
    ```
    NON-BLOCKING: setelan SUDAH tersimpan (jangan ubah urutan save). `TempData["Warning"]` + `TempData["Success"]` boleh ko-eksis. JANGAN return early. JANGAN replikasi ekspresi `AddHours(7)` boolean langsung di controller — selalu lewat `CooldownMayExceedWindow` (test memanggil method yang SAMA → drift +7h→+8h pasti ketahuan). PRESERVE RBAC + antiforgery + clamp existing.

    C) D-07 used-count — DEFINITE NO-OP berdokumentasi (BUKAN perhitungan duplikat): `UpdateRetakeSettings` POST melakukan PRG redirect ke GET `ManagePackages`, yang sudah menghitung ulang `ViewBag.RetakeMaxAttemptsUsedInGroup` via `MaxInGroupAsync` (helper D-05, wired 421-02). Maka POST TIDAK perlu menghitung used-count lagi. Tambahkan SATU komentar eksplisit di `UpdateRetakeSettings` (dekat akhir, sebelum redirect) — verbatim agar bisa di-assert:
    ```csharp
    // D-07: angka "terpakai" untuk modal pra-simpan TIDAK dihitung di sini — disuplai oleh GET ManagePackages
    //       via RetakeCountingRules.MaxInGroupAsync (helper D-05, wired 421-02). POST = PRG redirect; no recompute.
    ```
    JANGAN tambah pemanggilan `MaxInGroupAsync`/`CountEraRetakeArchives` di `UpdateRetakeSettings` (akan duplikat + bikin grep menyesatkan). Acceptance meng-assert komentar verbatim ini, bukan grep helper (grep helper di POST = misleading, sudah dipuaskan edit GET 421-02).

    D) Modal pra-simpan D-07 (Views/Admin/ManagePackages.cshtml): tambah Bootstrap modal (mirror Results.cshtml:534-553) yang muncul saat HC menekan "Simpan Pengaturan" DAN nilai maxAttempts yang dipilih < `ViewBag.RetakeMaxAttemptsUsedInGroup`. Karena maxAttempts dipilih di input sebelum submit, gate modal via JS minimal: intercept submit form retake, baca nilai input maxAttempts, bandingkan dengan angka terpakai (render dari `ViewBag.RetakeMaxAttemptsUsedInGroup` ke atribut data atau variabel JS), bila lebih kecil → tampilkan modal konfirmasi (Body copy UI-SPEC PERSIS: "{X} peserta sudah memakai lebih dari batas percobaan baru. Mereka tidak akan kehilangan percobaan yang sudah berjalan, tetapi tidak bisa menambah percobaan baru. Lanjutkan menyimpan?"), tombol "Ya, Lanjutkan" (`btn-primary`) submit form, "Batal" (`btn-secondary`, `data-bs-dismiss`). NON-BLOCKING: bila dikonfirmasi, form ter-submit apa adanya; server simpan tanpa hard-lock. Alert-warning inline existing (:157-163) DIPERTAHANKAN (info pasca-simpan). Reuse Bootstrap modal/btn existing — JANGAN CSS/komponen baru. Semua teks Bahasa Indonesia.

    E) Test (extend `HcPortal.Tests/RetakeSettingsEndpointTests.cs`): tambah test yang membuktikan D-07 non-blocking save sebagai kontrak DB/logika (HTTP/modal diverifikasi UAT Task 4):
    - Test D-07 (non-blocking save): replikasi body UpdateRetakeSettings — seed grup sibling + seed AttemptHistory ber-snapshot sehingga max-in-group > maxAttempts baru → jalankan propagasi save dengan maxAttempts kecil → assert SEMUA sibling tersimpan dengan maxAttempts baru (save TIDAK diblok meski terpakai > batas). Buktikan non-blocking.
    - Test parity used-count (RTH-05/D-05 reuse): seed era-archive + legacy → `MaxInGroupAsync` (helper 421-02) menghitung hanya snapshot-presence (legacy tak dihitung) — konsisten dengan helper 421-02 (regresi ringan memastikan reuse benar).
    > CATATAN: predikat D-02 di-cover oleh unit test pure di RetakeRulesTests.cs (step A) — JANGAN replikasi boolean D-02 di endpoint test (kill-drift: satu sumber kebenaran `CooldownMayExceedWindow`).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~RetakeRules|FullyQualifiedName~RetakeSettingsEndpoint" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CooldownMayExceedWindow" Helpers/RetakeRules.cs` cocok (method pure baru ada)
    - `grep "AddHours(7)" Helpers/RetakeRules.cs` cocok di CooldownMayExceedWindow (konvensi WIB verbatim, +7h di SATU tempat)
    - `grep "CooldownMayExceedWindow" Controllers/AssessmentAdminController.cs` cocok di UpdateRetakeSettings (D-02 panggil predikat pure, BUKAN boolean inline)
    - TIDAK ada boolean +7h D-02 inline di controller: `grep "AddHours(7)" Controllers/AssessmentAdminController.cs` di UpdateRetakeSettings TIDAK menambah ekspresi boolean window baru (predikat hidup di helper) — bila ada hit lama di method lain, abaikan; fokus UpdateRetakeSettings
    - `grep "TempData\[\"Warning\"\]" Controllers/AssessmentAdminController.cs` cocok (D-02 non-blocking warning)
    - `grep "CooldownMayExceedWindow" HcPortal.Tests/RetakeRulesTests.cs` cocok (test memanggil method yang SAMA dengan controller — kill-drift)
    - Komentar D-07 no-op verbatim ada: `grep "angka \"terpakai\" untuk modal pra-simpan TIDAK dihitung di sini" Controllers/AssessmentAdminController.cs` cocok
    - TIDAK ada recompute used-count di POST: `MaxInGroupAsync`/`CountEraRetakeArchives` TIDAK dipanggil DI DALAM body `UpdateRetakeSettings` (only di GET site dari 421-02) — verifikasi manual saat edit (no new call in POST method)
    - Modal D-07 di view: `grep "modal" Views/Admin/ManagePackages.cshtml` cocok DAN `grep "tidak bisa menambah percobaan baru. Lanjutkan menyimpan" Views/Admin/ManagePackages.cshtml` cocok (copy UI-SPEC)
    - Alert-warning inline existing dipertahankan: `grep "alert alert-warning" Views/Admin/ManagePackages.cshtml` cocok
    - RetakeRulesTests.cs ber-test baru CooldownMayExceedWindow: `grep -c "CooldownMayExceedWindow" HcPortal.Tests/RetakeRulesTests.cs` >= 5 (≥5 case behavior + boundary +7h)
    - RetakeSettingsEndpointTests.cs ber-test baru D-07: `grep -c "Fact\|Theory" HcPortal.Tests/RetakeSettingsEndpointTests.cs` bertambah dari baseline (>=2 baru)
    - `dotnet build` exit 0; `dotnet test --filter "FullyQualifiedName~RetakeRules"` exit 0 (pure, no SQL); `dotnet test --filter "FullyQualifiedName~RetakeSettingsEndpoint"` exit 0 (butuh SQLEXPRESS)
  </acceptance_criteria>
  <done>`RetakeRules.CooldownMayExceedWindow` pure static ada (+7h WIB di satu tempat) dengan unit test ≥5 case (incl boundary +7h kill-drift); UpdateRetakeSettings memunculkan TempData Warning non-blocking via predikat pure (D-02, setelan tetap tersimpan); D-07 used-count = no-op berdokumentasi (komentar verbatim, angka dari GET via MaxInGroupAsync 421-02 — TIDAK recompute di POST); modal pra-simpan non-blocking saat maxAttempts<terpakai (copy UI-SPEC); test save non-blocking + parity used-count hijau; RBAC/antiforgery/clamp dipertahankan.</done>
</task>

<task type="auto">
  <name>Task 3: Konfirmasi pencabutan sertifikat sebelum HC reset sesi LULUS/ber-cert (RTH-02, D-04 — view-only conditional confirm)</name>
  <read_first>
    - Views/Admin/AssessmentMonitoringDetail.cshtml:241 (loop `@foreach (var session in Model.Sessions)`) + :331-343 (form ResetAssessment existing dengan `onsubmit="return confirm(...)"` + @Html.AntiForgeryToken() + hidden id)
    - Models/AssessmentMonitoringViewModel.cs:48-68 (MonitoringSessionViewModel — `IsPassed` bool? :55, `NomorSertifikat` string? :67 SUDAH di-expose; TIDAK perlu ubah VM)
    - 421-CONTEXT.md §RTH-02 D-04 (konfirmasi cabut cert sebelum reset sesi lulus)
    - 421-UI-SPEC.md §"RTH-02 / D-04" (Pola C conditional confirm, ~baris 107-110) + Copywriting row "Destructive confirmation — reset sesi LULUS / ber-cert" (~baris 141)
    - 421-RESEARCH.md "D-04 conditional confirm copy (view-only, VM sudah expose field)" (~baris 309-320, contoh kode targeting :334-341)
    - 421-PATTERNS.md §"Views/Admin/AssessmentMonitoringDetail.cshtml ~:334 — D-04 confirm cabut-cert" (~baris 246-275, idiom + catatan variabel `session`)
  </read_first>
  <action>
    D-04 = conditional confirm view-only di form reset existing `AssessmentMonitoringDetail.cshtml:334-341`. TIDAK ubah controller/VM — `ResetAssessment` sudah server-authoritative menol-kan cert via service (D-03, plan lain); D-04 hanya lapis UX konfirmasi sebelum POST.

    A) Tepat SEBELUM form `<form asp-action="ResetAssessment" ...>` (di dalam `<li>` :333-342, dalam loop `session`), bangun string confirm conditional via blok Razor (idiom RESEARCH ~:312-318):
    ```razor
    @{
        bool hasCert = session.IsPassed == true || session.NomorSertifikat != null;
        string resetConfirm = hasCert
            ? "Reset sesi ini? Sesi ini SUDAH LULUS dan memiliki nomor sertifikat. Mereset akan MENCABUT sertifikat (nomor dihapus) dan semua jawaban dihapus. Peserta dapat mengulang ujian. Lanjutkan?"
            : "Reset sesi ini? Semua jawaban akan dihapus dan peserta dapat mengulang ujian.";
    }
    ```
    Gunakan nama variabel iterasi PERSIS `session` (dari `@foreach (var session in Model.Sessions)` :241). Pastikan blok `@{ }` ditempatkan di dalam scope `<li>` yang sama (per-`session`) agar tiap baris memakai nilai `IsPassed`/`NomorSertifikat` miliknya sendiri — JANGAN deklarasi di luar loop.

    B) Ubah atribut `onsubmit` form reset (:335) agar memakai string conditional (escape aman untuk apostrof/quotes via `Json.Serialize` + `Html.Raw`, idiom RESEARCH :318):
    ```razor
    <form asp-action="ResetAssessment" asp-controller="AssessmentAdmin" method="post" class="m-0"
          onsubmit="return confirm(@Html.Raw(Json.Serialize(resetConfirm)))">
    ```
    PRESERVE `@Html.AntiForgeryToken()` + `<input type="hidden" name="id" value="@session.Id" />` + tombol existing apa adanya. Copy non-cert TIDAK berubah dari existing (backward-compat). Sesi non-lulus / tanpa cert → confirm reset biasa. Semua teks Bahasa Indonesia (CLAUDE.md).

    C) (Opsional, diskresi) Bila ada titik reset MASSAL di view yang sama, UI-SPEC menyarankan modal Bootstrap yang menyebut jumlah sesi ber-cert. Bila titik reset massal TIDAK ada di `AssessmentMonitoringDetail.cshtml` (grep `ResetAll`/bulk-reset form 0 match), LEWATI — jangan buat fitur baru (Consistency Rule 1: tak ada komponen baru di luar yang tercantum). Catat di SUMMARY bila massal absen.

    Tidak ada perubahan kode C#/test untuk task ini (view-only, low-effort) — verifikasi via build (Razor compile) + grep copy + UAT browser (Task 4).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MENCABUT sertifikat" Views/Admin/AssessmentMonitoringDetail.cshtml` cocok (copy cabut-cert D-04 per UI-SPEC)
    - `grep "session.IsPassed == true || session.NomorSertifikat != null" Views/Admin/AssessmentMonitoringDetail.cshtml` cocok (gating conditional view-only)
    - `grep "Json.Serialize(resetConfirm)" Views/Admin/AssessmentMonitoringDetail.cshtml` cocok (onsubmit memakai string conditional, escape aman)
    - `@Html.AntiForgeryToken()` + hidden `id` form reset TETAP ada (PRESERVE) — verifikasi manual saat edit (tidak terhapus)
    - TIDAK ada perubahan controller/VM untuk D-04: `Models/AssessmentMonitoringViewModel.cs` + `ResetAssessment` di controller TIDAK diubah (view-only)
    - `dotnet build` exit 0 (Razor conditional compile)
  </acceptance_criteria>
  <done>Form reset di AssessmentMonitoringDetail menampilkan konfirmasi pencabutan sertifikat (copy "MENCABUT sertifikat...") HANYA saat sesi lulus/ber-NomorSertifikat; sesi non-lulus tetap confirm reset biasa (backward-compat); view-only (VM/controller tak berubah), antiforgery + hidden id dipertahankan; build hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT live — soft-confirm hapus ber-riwayat D-06 + modal pra-simpan D-07 + warning D-02 + konfirmasi cabut-cert D-04 @5270</name>
  <action>
    Checkpoint verifikasi manusia (live browser @5270) — Claude TIDAK menulis kode di task ini. Jalankan app branch ITHandoff, picu empat skenario di `<how-to-verify>`, verifikasi server-authoritative + non-blocking + 0-orphan + conditional cabut-cert, lalu tunggu sinyal "approved". Bila ada temuan, kembali ke Task 1/2/3 untuk perbaikan.
  </action>
  <what-built>
    Server round-trip hapus peserta ber-riwayat (Pola B, hidden flag confirmRemoveWithHistory + tombol "Tetap Hapus", cascade cleanup arsip 0-orphan), modal pra-simpan non-blocking saat MaxAttempts<terpakai (D-07), warning non-blocking cooldown bisa lewat window via predikat pure CooldownMayExceedWindow (D-02), dan konfirmasi conditional pencabutan sertifikat sebelum HC reset sesi lulus (D-04, view-only). Semua otomasi server + test (Task 1 ParticipantRemoveGuard real-SQL + Task 2 RetakeRules pure + RetakeSettingsEndpoint real-SQL) sudah hijau, build (Task 3 Razor) hijau. Checkpoint ini memverifikasi round-trip + modal + toast + confirm LIVE di browser (Razor/JS + TempData consume) yang test integration tidak menjangkau.
  </what-built>
  <how-to-verify>
    Prasyarat: branch ITHandoff, app jalan `dotnet run --urls http://localhost:5270` (JANGAN commit launchSettings). Login Admin/HC (kredensial Dev di MEMORY reference_dev_credentials). Seed grup Pre-Post dgn ≥1 peserta ber-riwayat (sesi Abandoned ATAU StartedAt!=null ATAU AttemptHistory tercatat) DAN ≥1 sesi LULUS ber-NomorSertifikat — gunakan disposable/local seed sesuai Seed Workflow (snapshot DB dulu, restore setelah).

    1) RTH-04 soft-confirm (D-06):
       - Buka `/Admin/EditAssessment/{id}` untuk grup Pre-Post tsb. Hapus peserta ber-riwayat lalu submit.
       - HARAPAN: peserta TIDAK terhapus; muncul alert-warning "Peserta ... memiliki riwayat ujian ..." + tombol "Tetap Hapus" (merah).
       - Klik "Tetap Hapus" (submit ulang dgn hidden flag).
       - HARAPAN: peserta terhapus. Cek DB: sesi Pre+Post peserta itu hilang, AttemptHistory terkait hilang, DAN `SELECT COUNT(*) FROM AssessmentAttemptResponseArchives ar LEFT JOIN AssessmentAttemptHistory h ON ar.AttemptHistoryId=h.Id WHERE h.Id IS NULL` = 0 (tidak ada orphan).
       - Negatif: hapus peserta TANPA riwayat (Open, belum mulai) → langsung terhapus tanpa warning (backward-compat).
    2) RTH-05 modal pra-simpan (D-07):
       - Buka `/Admin/ManagePackages?assessmentId={id}` grup yang punya peserta sudah memakai N percobaan. Set "Maksimal percobaan" < N, klik "Simpan Pengaturan".
       - HARAPAN: modal muncul "{X} peserta sudah memakai lebih dari batas percobaan baru ... Lanjutkan menyimpan?". Klik "Ya, Lanjutkan".
       - HARAPAN: setelan TERSIMPAN (success toast); buka ulang ManagePackages → alert-warning inline pasca-simpan tampil. Non-blocking terbukti (tersimpan meski terpakai>batas).
    3) RTH-01 D-02 warning cooldown>window:
       - Pada assessment dgn ExamWindowCloseDate dekat (masih buka), set "Jeda Ujian Ulang (jam)" besar (mis. melewati sisa window), Simpan.
       - HARAPAN: toast/alert-warning "Masa jeda ujian ulang ... bisa melewati batas tutup ujian ... tetap bisa disimpan." + setelan tetap tersimpan.
       - Negatif: cooldown kecil yang muat di sisa window → TIDAK ada warning (setelan tetap tersimpan).
    4) RTH-02 D-04 konfirmasi cabut-cert:
       - Buka `/Admin/AssessmentMonitoringDetail` (atau halaman monitoring detail grup). Pada sesi yang SUDAH LULUS / ber-NomorSertifikat, buka menu ⋮ → klik "Reset".
       - HARAPAN: dialog confirm muncul dengan teks memuat "SUDAH LULUS ... MENCABUT sertifikat (nomor dihapus) ... Lanjutkan?". Klik OK → reset terjadi (cek DB: NomorSertifikat sesi itu jadi NULL, server-side via D-03).
       - Negatif: pada sesi BELUM lulus / tanpa cert, klik "Reset" → confirm BIASA ("Reset sesi ini? Semua jawaban akan dihapus ...") tanpa peringatan cabut-cert (backward-compat).

    Setelah UAT: restore DB lokal ke snapshot (Seed Workflow), tandai SEED_JOURNAL cleaned.
  </how-to-verify>
  <resume-signal>Ketik "approved" bila keempat skenario sesuai harapan (termasuk 0-orphan, non-blocking, conditional cabut-cert), atau jelaskan temuan untuk diperbaiki.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC (Admin) → server | HC submit EditAssessment POST (hapus peserta) + UpdateRetakeSettings POST + ResetAssessment POST; input untrusted termasuk hidden flag `confirmRemoveWithHistory`; harus server-authoritative |
| Browser JS → server | Modal/confirm/JS adalah lapis UX; gate sebenarnya (guard riwayat, simpan, cascade, nol-kan cert) di server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-421-09 | Tampering | EditAssessment POST — `confirmRemoveWithHistory` di-forge true via crafted POST untuk lewati peringatan | mitigate | Flag HANYA menekan peringatan; guard riwayat (Abandoned/StartedAt/AttemptHistory) tetap dievaluasi server tiap POST. Forged flag → tetap melewati guard hard-block InProgress/Completed existing (yang TIDAK bisa di-bypass flag) + RBAC + antiforgery. Flag bukan otorisasi — hanya gate UX. |
| T-421-10 | Elevation of Privilege (RBAC) | EditAssessment / UpdateRetakeSettings / ResetAssessment POST tanpa Admin,HC | accept (existing PRESERVED) | `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` existing pada ketiga action — PRESERVE saat menambah param/logika. Tidak ada endpoint baru. |
| T-421-11 | Information Disclosure / Data Integrity | Orphan `AssessmentAttemptResponseArchive` setelah hapus peserta (arsip jawaban yatim lintas attempt) | mitigate | Cleanup via cascade DB (`OnDelete(Cascade)` AttemptHistoryId, ApplicationDbContext:588-595): delete AttemptHistory tracked (RemoveRange + SaveChanges) men-trigger cascade. Test Task 1 #4 assert 0 orphan. Tidak pakai ExecuteDelete/raw (yang bisa skip cascade). |
| T-421-12 | Tampering (integritas retroaktif) | UpdateRetakeSettings — turunkan MaxAttempts/ubah cooldown men-corrupt attempt berjalan | accept-by-design | D-07/D-02 NON-BLOCKING by decision (CONTEXT) — perilaku retroaktif existing (`RetakeRules.cs:46`) dipertahankan; attempt berjalan TIDAK dibatalkan. Konfirmasi/warning = transparansi, bukan blok. Clamp server-side (:5667-5668) tetap defense. |
| T-421-13 | Denial of Service (query) | Guard AttemptHistory AnyAsync per-removed-user di loop (N+1 potensi) | accept | Loop hapus peserta dibatasi (peserta grup kecil); `AnyAsync` ter-index pada SessionId. Rate-limit batch existing (:2047 NewUserIds>50 block). Tidak ada amplifikasi user-controlled signifikan. |
| T-421-14 | Tampering (integritas sertifikat) | HC mass-reset sesi LULUS mencabut NomorSertifikat tanpa peringatan (D-04 absen → pencabutan tak sengaja) | mitigate | D-04 conditional confirm di titik aksi reset (`AssessmentMonitoringDetail.cshtml`) — peringatan eksplisit "MENCABUT sertifikat" saat `session.IsPassed`/`NomorSertifikat`. Konfirmasi = lapis UX; pencabutan/nol-kan cert TETAP server-side (D-03 via service). RBAC `[Authorize(Roles="Admin, HC")]` + antiforgery pada ResetAssessment dipertahankan. Bukan gate keamanan (server-authoritative tetap menol-kan), melainkan cegah aksi destruktif tak sengaja. |

**Catatan keamanan:** D-06 hidden flag = server-authoritative (BUKAN JS-only) per CONTEXT/UI-SPEC Pola B. Modal D-07 + confirm/native D-04 = lapis UX murni; server tetap menyimpan/menghapus/menol-kan-cert berdasarkan re-evaluasi guard. D-02 predikat hidup di `RetakeRules.CooldownMayExceedWindow` (pure, +7h WIB satu tempat) — controller & test memanggil kode yang sama (kill-drift). Semua teks Bahasa Indonesia (CLAUDE.md).
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~ParticipantRemoveGuard"` exit 0 (guard riwayat + 0 orphan; butuh SQLEXPRESS)
- `dotnet test --filter "FullyQualifiedName~RetakeRules"` exit 0 (predikat D-02 CooldownMayExceedWindow pure, no SQL; incl boundary +7h kill-drift)
- `dotnet test --filter "FullyQualifiedName~RetakeSettingsEndpoint"` exit 0 (D-07 non-blocking save + parity used-count; butuh SQLEXPRESS)
- `dotnet build` exit 0 (signature param baru + view modal/flag + confirm conditional D-04 kompilasi)
- Grep guard: `confirmRemoveWithHistory` di controller (param+guard) & view (hidden flag+Tetap Hapus); `CooldownMayExceedWindow` di helper + controller + test (predikat pure satu sumber, bukan boolean inline); komentar D-07 no-op verbatim; copy `MENCABUT sertifikat` + gating conditional di AssessmentMonitoringDetail (D-04 view-only).
- Cascade: tidak ada RemoveRange archives manual baru (andalkan cascade DB; test assert 0 orphan).
- D-07 used-count: TIDAK ada recompute di POST `UpdateRetakeSettings` (no-op berdokumentasi; angka dari GET via MaxInGroupAsync 421-02).
- UAT live @5270 (Task 4): round-trip hapus ber-riwayat (0-orphan), modal pra-simpan non-blocking, warning D-02, konfirmasi cabut-cert conditional — semua server-authoritative.
</verification>

<success_criteria>
- RTH-04: hapus peserta Abandoned/ber-riwayat/StartedAt ditolak tanpa flag (warning + cancel, edit lain tak hilang), dihapus + 0 orphan archive dengan flag (cascade); server round-trip + tombol "Tetap Hapus" server-authoritative.
- RTH-05: MaxAttempts<terpakai → modal pra-simpan non-blocking; setelan tetap tersimpan; angka terpakai via helper snapshot-presence 421-02 (kill-drift, no recompute di POST).
- D-02 (porsi RTH-01 di settings): cooldown bisa lewat window → warning non-blocking ke HC; setelan tetap tersimpan; predikat di `RetakeRules.CooldownMayExceedWindow` (+7h verbatim, satu sumber, controller+test panggil sama).
- D-04 (porsi RTH-02 di monitoring): HC reset sesi lulus/ber-cert → konfirmasi pencabutan sertifikat sebelum POST; sesi non-lulus → confirm reset biasa; view-only (VM/controller tak berubah); pencabutan tetap server-side (D-03 via service).
- Backward-compat: hapus peserta tanpa-riwayat & save retake normal & reset sesi non-lulus tak berubah; RBAC + antiforgery + clamp dipertahankan; tidak ada endpoint baru; migration=FALSE.
- Semua test Plan 03 hijau, build 0-err, UAT 4 skenario approved.
</success_criteria>

<output>
After completion, create `.planning/phases/421-retake-lifecycle-hardening/421-03-SUMMARY.md`
</output>
