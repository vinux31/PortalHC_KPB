---
context: phase
phase: 422-samepackage-shuffle-integrity
task: phase-complete-pending
total_tasks: 3
status: gates_complete
last_updated: 2026-06-23T13:01:22.969Z
---

# Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| New DB constraint breaks existing fixture | Phase 422 unique index rejected RetakeThenPassCertTests' duplicate (sessionId, PackageNumber=1) seed — surfaced only by FULL regression suite. FIXED (MAX+1, commit ce3b280a). | advisory | Run FULL `dotnet test` after any migration/constraint change; fix the fixture, not the constraint. |
| sqlcmd named-pipes fails (NTLM loopback) | Plain `sqlcmd -S localhost\SQLEXPRESS` → "error 53". | advisory | Use `sqlcmd -S "lpc:localhost\SQLEXPRESS"`. dotnet build/test/ef use SqlClient shared-memory and work fine. |

<current_state>
Phase 422 (SamePackage & Shuffle Integrity, SHFX-01..07) — code + ALL 5 quality gates COMPLETE & green. Running under milestone-autopilot v32.7. HEAD = `6c21a9de`, branch ITHandoff, NOT pushed. Working tree clean (only pre-existing untracked: akun-doc*.jpeg, docs/akun-multirole*, xlsx).

Gate results:
- Full xUnit suite: **692 passed / 0 failed / 2 skipped**
- gsd-verifier: PASSED 5/5 (422-VERIFICATION.md)
- gsd-code-review: 0 Critical / 2 Warning / 3 Info → WR-01 (tx), WR-02 (centralized UPA cleanup), IN-01 (CreateQuestion CSRF gap), IN-03 (kill-drift predicate) ALL FIXED (422-REVIEW.md + 422-REVIEW-FIX.md)
- gsd-secure-phase: SECURED 13/13, threats_open=0 (422-SECURITY.md)
- gsd-validate-phase: NYQUIST-COMPLIANT 7/7 SHFX, 0 gaps (422-VALIDATION.md)
- Checkpoint UAT 6/6 PASS @5270 (422-UAT.md), DB restored pristine
- migration=TRUE AddPackageNumberUniqueIndex applied locally (commit 31ef3e1d)

ROADMAP plan-progress already shows 422 Complete (3/3 SUMMARY), but `gsd-tools phase complete 422` (formal close: ROADMAP [x] + REQUIREMENTS traceability + STATE advance) has NOT been run yet.
</current_state>

<completed_work>
- 422-01/02/03 all executed + committed. SHFX-01..07 delivered.
- Regression fix (RetakeThenPassCertTests MAX+1) — ce3b280a.
- 5 gates: verify, code-review(+4 fixes), secure, validate all PASS.
</completed_work>

<remaining_work>
1. Run `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" phase complete 422` → commit ROADMAP.md/STATE.md/REQUIREMENTS.md/*-VERIFICATION.md. Remove this .continue-here.md.
2. milestone-autopilot advances: STEP 0 `roadmap analyze` → next phase = **423 Certificate Issuance Consistency** (CERT-01..07, migration=FALSE, NO CONTEXT) → `/gsd-discuss-phase 423` INTERACTIVE (user answers — NOT --auto) → ui-phase(if frontend)→plan→execute→review→secure→validate→UAT→commit.
</remaining_work>

<decisions_made>
- Executors run SEQUENTIAL on main tree (no worktree): single-plan waves, shared controller, OneDrive risk.
- UAT via Playwright MCP + manual lpc: DB snapshot/restore (spec runner's helper uses failing named-pipes sqlcmd).
- Fixed IN-01 (pre-existing CreateQuestion CSRF gap) — genuine hole, trivial+safe, 422 lock guard lives there.
</decisions_made>

<blockers>
- None. Phase 422 is done pending the formal `phase complete` command.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state + exact next_action
2. `.planning/ROADMAP.md` Phase 423 block — next phase goal (CERT-01..07)
3. milestone-autopilot skill — pipeline to continue (discuss inline interactive, no push, stop before complete-milestone)

## Infrastructure State
- App @5270: STOPPED. Restart: `ASPNETCORE_ENVIRONMENT=Development Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5270`.
- DB HcPortalDB_Dev: pristine (UAT restored). Migration AddPackageNumberUniqueIndex APPLIED locally. Backup `HcPortalDB_Dev_422uat.bak` in SQL default dir (deletable).
- SQL: dotnet uses SqlClient shared-memory (work); sqlcmd needs `-S "lpc:localhost\SQLEXPRESS"`.
- Git: branch ITHandoff, HEAD 6c21a9de, NOT pushed (v32.7 deploy bundle: v32.1+v32.3+v32.4+v32.7).

<context>
Phase 422 is the 3rd of 6 in milestone v32.7 (420✅ 421✅ 422✅ | 423 424 425 pending). Everything for 422 is green. The ONLY next step is the formal phase-complete command, then the autopilot loop continues to Phase 423 (Certificate Issuance Consistency) starting with interactive discuss. Do NOT run audit-milestone (milestone not done). Do NOT push.
</context>

<next_action>
Start with: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" phase complete 422`. Parse result (next_phase, warnings). Commit `docs(phase-422): complete phase execution` with ROADMAP.md/STATE.md/REQUIREMENTS.md/422-VERIFICATION.md. Remove `.planning/phases/422-samepackage-shuffle-integrity/.continue-here.md`. THEN continue milestone-autopilot → Phase 423 discuss (interactive).
</next_action>
