---
context: phase
phase: 422-samepackage-shuffle-integrity
task: post-execution-gates
total_tasks: 9
status: in_progress
last_updated: 2026-06-23T12:10:33.796Z
---

# Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| New DB constraint breaks existing test fixture | Phase 422 added unique index `IX_AssessmentPackages_SessionId_PackageNumber_Unique`. `RetakeThenPassCertTests` seeds two packages with duplicate `(sessionId, PackageNumber=1)` → `DbUpdateException` (1 fail/694). Surfaced only by FULL regression suite, not the phase's own tests. | advisory | Always run FULL `dotnet test` (regression gate) after any migration/constraint change; fix the now-invalid fixture, NOT the constraint. |
| Executor connection-closed mid-finalize | Long executor runs (>25 min) twice dropped "API Error: Connection closed" AFTER committing code but BEFORE writing SUMMARY/tracking. | advisory | Orchestrator spot-checks (commits present + tree clean + tests green) then writes SUMMARY + roadmap progress itself. Bound executor scope (e.g. stop at checkpoint). |

<current_state>
Phase 422 (SamePackage & Shuffle Integrity, SHFX-01..07) — ALL code + UAT COMPLETE. Running under **milestone-autopilot v32.7**. Currently in the EXECUTE post-execution gate sequence. HEAD = `34aae680`, branch ITHandoff.

- 3/3 plans done + committed; ROADMAP plan-progress = Complete (3/3 SUMMARY).
- migration=TRUE applied locally: `AddPackageNumberUniqueIndex` (commit `31ef3e1d`).
- Checkpoint UAT 6/6 PASS (Playwright MCP @5270), DB restored pristine, SEED_JOURNAL cleaned (`422-UAT.md`).
- Full regression suite run: **691 passed / 1 FAILED / 2 skipped (694)**. The 1 failure is the ONLY thing blocking gate completion.
</current_state>

<completed_work>
- 422-01 (Wave 1 keystone): `PackageSizeAnalysis`, `SessionEditLockRules`, `ShuffleToggleRules` ON-path warning; migration `AddPackageNumberUniqueIndex` (dedup ROW_NUMBER → unique index, NO filter, applied locally 0-dup); CreatePackage MAX+1 + 5× ThenBy(Id); SHFX-05 + SHFX-07 backend. Commits 0ed5983f, 31ef3e1d, 461e5616, 9d4d9cf5.
- 422-02 (Wave 2 wiring): `SyncToLinkedPostIfSamePackageAsync` 6-jalur incl Import (SHFX-01 HIGH); `IsSessionEditLocked` guard 5 endpoints (SHFX-03); newPost inherit (SHFX-04); sibling key type-aware LOCK-ONLY (SHFX-06). Commits d8b2ca76, c13576c3, 85be14be. 15 integration tests green.
- 422-03 (Wave 3): `ToggleSamePackage` endpoint (ON sync+lock / OFF keep / guard anyStarted / clear stale UPA) + UI render (toggle card, lock banner, shuffle warning D-03/D-04, mismatch single-source D-05 deleting view dup :72-78) + friendly-disable + Playwright spec. Commits 75e87186, f45fa211. Checkpoint UAT 6/6 PASS → 422-UAT.md + 422-03-SUMMARY.md (commit 34aae680).
</completed_work>

<remaining_work>
1. **(IN PROGRESS) Fix regression** — `RetakeThenPassCertTests.RetakeThenPass_IssuesExactlyOneCertificate` fails: new unique index rejects fixture that seeds duplicate `(sessionId, PackageNumber=1)` (helper called twice, lines 142 + 152). FIX = MAX+1 in `SeedPackageWithResponsesAsync` (see next_action). Confirmed SAFE.
2. gsd-verifier (verify_phase_goal) → 422-VERIFICATION.md
3. gsd-code-review (+fix) → 422-REVIEW.md
4. gsd-secure-phase → 422-SECURITY.md (ASVS L1, block high; threat IDs T-422-01..13 in plans)
5. gsd-validate-phase → finalize 422-VALIDATION.md (Nyquist; skeleton already exists, map 7 REQ → green tests)
6. phase complete (`gsd-tools phase complete 422`) → REQUIREMENTS traceability + advance STATE
7. milestone-autopilot advances to **Phase 423** (Certificate Issuance Consistency, CERT-01..07, migration=FALSE, no CONTEXT yet → discuss inline interactive)
</remaining_work>

<decisions_made>
- Executors run SEQUENTIAL on main tree (no worktree): 3 waves × 1 plan, no parallelism, all touch one controller, OneDrive worktree risk.
- Orchestrator finalized SUMMARYs after 2 executor connection-drops (verified green + committed code first).
- UAT via Playwright MCP + manual lpc: DB snapshot/restore (spec runner's dbSnapshot helper uses named-pipes sqlcmd which fails NTLM-loopback here).
- Regression fix = patch test fixture, NOT the unique index (index = correct production behavior).
- Toggle SamePackage D-01: ON overwrite+lock, OFF keep clones (Pitfall 5), guard rejects when any participant started.
</decisions_made>

<blockers>
- 1 regression test fail (RetakeThenPassCertTests) — fix root-caused + proven safe, just needs applying + re-run. Not a production bug.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state (next_action verbatim with the exact code snippet)
2. `HcPortal.Tests/RetakeThenPassCertTests.cs:98-128` — the helper to fix (line 100 `PackageNumber = 1`)
3. `Services/GradingService.cs:57-100` — confirms grading is assigned-package + response-driven (why MAX+1 fix is safe)
4. `.planning/phases/422-samepackage-shuffle-integrity/422-VALIDATION.md` — Nyquist skeleton to finalize

## Critical Anti-Patterns (do NOT repeat these)
- DO NOT "fix" the regression by weakening/removing the unique index — the index is the SHFX-05 deliverable. Fix the test fixture's duplicate seed.
- DO NOT run `dotnet ef database update` again blindly — migration already applied locally.

## Infrastructure State
- App @5270: STOPPED (was killed to free DB for RESTORE). Restart for any further UAT: `ASPNETCORE_ENVIRONMENT=Development Authentication__UseActiveDirectory=false dotnet run --urls http://localhost:5270`.
- DB HcPortalDB_Dev: pristine (UAT restored). Migration AddPackageNumberUniqueIndex APPLIED. Backup at SQL default backup dir `HcPortalDB_Dev_422uat.bak` (can delete).
- SQL access: `dotnet build/test/ef` use SqlClient shared-memory (WORK). `sqlcmd` needs `-S "lpc:localhost\SQLEXPRESS"` (named-pipes fails error 53 NTLM-loopback).
- Git: branch ITHandoff, HEAD 34aae680, NOT pushed. Stray untracked files (akun-doc*.jpeg, docs/akun-multirole*, xlsx) are pre-existing, ignore.

<context>
Phase 422 is functionally DONE (code + live UAT). Remaining work is purely the GSD quality-gate chain + the one trivial test fix. The fix is the single highest-leverage next step — apply it, confirm full suite green, then run the gate agents in order. After phase complete, milestone-autopilot continues to Phase 423 (discuss inline — user answers design questions, NOT --auto).
</context>

<next_action>
Start with: Edit `HcPortal.Tests/RetakeThenPassCertTests.cs` `SeedPackageWithResponsesAsync` (~line 100). Replace `PackageNumber = 1` with a per-session MAX+1:
```csharp
int nextNum = (await ctx.AssessmentPackages
    .Where(p => p.AssessmentSessionId == sessionId)
    .Select(p => (int?)p.PackageNumber).MaxAsync() ?? 0) + 1;
var pkg = new AssessmentPackage { AssessmentSessionId = sessionId, PackageName = "Paket A", PackageNumber = nextNum };
```
Then `dotnet test --filter "FullyQualifiedName~RetakeThenPassCertTests"` (expect PASS) → full `dotnet test` (expect 692/0/2) → commit `test(422): fix RetakeThenPassCertTests fixture to honor PackageNumber unique index (MAX+1)`. THEN continue gates: gsd-verifier → gsd-code-review → gsd-secure-phase → gsd-validate-phase → `gsd-tools phase complete 422` → milestone-autopilot → Phase 423 discuss.
</next_action>
