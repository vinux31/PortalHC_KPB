---
phase: 422-samepackage-shuffle-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/PackageSizeAnalysis.cs
  - Helpers/ShuffleToggleRules.cs
  - Helpers/SessionEditLockRules.cs
  - Data/ApplicationDbContext.cs
  - Migrations/_AddPackageNumberUniqueIndex.cs
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/PackageSizeAnalysisTests.cs
  - HcPortal.Tests/ShuffleToggleRulesTests.cs
  - HcPortal.Tests/SessionEditLockRulesTests.cs
  - HcPortal.Tests/PackageNumberUniqueTests.cs
  - HcPortal.Tests/PackageNumberMigrationTests.cs
autonomous: true
migration: true
requirements: [SHFX-05, SHFX-07]
must_haves:
  truths:
    - "PackageNumber baru selalu MAX(PackageNumber)+1 per session — setelah hapus paket tengah, nomor baru TIDAK bentrok dengan nomor existing"
    - "Unique index (AssessmentSessionId, PackageNumber) menolak baris duplikat di level DB (DbUpdateException)"
    - "Migration me-renumber baris PackageNumber duplikat existing SEBELUM CreateIndex sehingga apply tidak gagal pada data lama"
    - "Peringatan K=min muncul juga saat Acak Soal ON (bukan hanya OFF) bila ukuran paket beda"
    - "hasMismatch/referenceCount dihitung di SATU tempat (PackageSizeAnalysis.Compute) — tidak ada re-derive view vs controller"
    - "IsSessionEditLocked(session) true hanya bila AssessmentType=='PostTest' && SamePackage; false untuk Pre/Standard/Post-non-same"
  artifacts:
    - path: "Helpers/PackageSizeAnalysis.cs"
      provides: "Pure Compute(packages) -> (PackagesWithQuestions, ReferenceCount, HasMismatch) — kill-drift single source"
      contains: "record struct Result"
    - path: "Helpers/ShuffleToggleRules.cs"
      provides: "ShouldShowKMinTruncationWarning ON-path (D-04) di samping ShouldShowSizeMismatchWarning OFF-path"
      contains: "ShouldShowKMinTruncationWarning"
    - path: "Helpers/SessionEditLockRules.cs"
      provides: "IsSessionEditLocked(AssessmentSession) pure predicate — dipakai guard endpoint + view"
      contains: "IsSessionEditLocked"
    - path: "Migrations/_AddPackageNumberUniqueIndex.cs"
      provides: "Dedup ROW_NUMBER renumber SEBELUM CreateIndex unique (AssessmentSessionId, PackageNumber)"
      contains: "ROW_NUMBER() OVER (PARTITION BY AssessmentSessionId"
    - path: "HcPortal.Tests/PackageSizeAnalysisTests.cs"
      provides: "Pure truth-table paritas hasMismatch/refCount/withQ"
    - path: "HcPortal.Tests/PackageNumberMigrationTests.cs"
      provides: "Integration: seed dup -> renumber SQL -> 0 dup gap-free per session"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs CreatePackage:5969-5976"
      to: "MAX(PackageNumber)+1 (ganti existingCount+1)"
      via: "MaxAsync server-computed"
      pattern: "MaxAsync"
    - from: "Data/ApplicationDbContext.cs"
      to: "AssessmentPackage composite unique index"
      via: "HasIndex(p => new { p.AssessmentSessionId, p.PackageNumber }).IsUnique()"
      pattern: "IX_AssessmentPackages_SessionId_PackageNumber_Unique"
---

<objective>
Keystone fase 422: bangun semua pure-helper + extend + migration data-integrity yang dipakai Wave 2 & 3, plus tutup SHFX-05 (PackageNumber unik+deterministik) dan SHFX-07 (peringatan shuffle satu sumber). Ini wave HIGHEST-RISK (migration dedup-then-index) dan signature-establishing — helper `IsSessionEditLocked`, `PackageSizeAnalysis`, dan extend `ShuffleToggleRules` adalah kontrak yang Wave 2/3 panggil.

Purpose: Helper pure (EF-free) memusatkan keputusan/komputasi yang saat ini terduplikasi tersebar (mismatch dihitung 2× di controller+view = drift SHUF-ISS-07; lock inline di ViewBag saja = view-only SHUF-ISS-02 root). Migration menutup SHUF-ISS-08 (PackageNumber bentrok akibat count-based + DeletePackage tak renumber) dengan jaring pengaman DB-level + dedup data lama defensif. MAX+1 + ThenBy(Id) = pencegahan utama di app-level.
Output: 3 file helper (1 NEW PackageSizeAnalysis, 1 NEW SessionEditLockRules, 1 EXTEND ShuffleToggleRules) + fluent index di ApplicationDbContext + 1 migration AddPackageNumberUniqueIndex (dedup→index) + CreatePackage MAX+1 + 5× ThenBy(Id) + 5 file test (3 NEW + 1 NEW + 1 EXTEND).

⚠️ OVERLAP v32.6 (branch main, Scoped Shuffle, fase 415-419): perubahan pada `ShuffleToggleRules.cs` dan query `OrderBy(PackageNumber)` di controller MUNGKIN bentrok saat merge. Catat tiap perubahan shuffle/order untuk rekonsiliasi merge — JANGAN tarik/duplikasi kode Scoped-Shuffle dari main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/422-samepackage-shuffle-integrity/422-CONTEXT.md
@.planning/phases/422-samepackage-shuffle-integrity/422-RESEARCH.md
@.planning/phases/422-samepackage-shuffle-integrity/422-PATTERNS.md
@.planning/phases/422-samepackage-shuffle-integrity/422-UI-SPEC.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diambil dari source aktual (VERIFIED 2026-06-23). -->

EXISTING Helpers/ShuffleToggleRules.cs:8-21 (extend — tambah method ON-path SEJAJAR, JANGAN ubah yang ada):
```csharp
public static class ShuffleToggleRules
{
    public static bool IsShuffleLocked(bool anyStarted, bool anyAssignment) => anyStarted || anyAssignment;
    public static bool ShouldHideShuffleToggle(string? category, string? tahunKe, bool isManualEntry)
        => (category == "Assessment Proton" && tahunKe == "Tahun 3") || isManualEntry;
    // OFF-path warning (EXISTING — jangan diubah):
    public static bool ShouldShowSizeMismatchWarning(int packagesWithQuestions, bool shuffleQuestions, bool hasMismatch)
        => packagesWithQuestions >= 2 && !shuffleQuestions && hasMismatch;
}
```

EXISTING inline lock (AssessmentAdminController.cs:5810-5811) — angkat jadi pure helper:
```csharp
// SamePackage lock: jika Post-Test dengan SamePackage=true, lock editing
ViewBag.IsSamePackageLocked = isPostSession && assessment.SamePackage;
```

EXISTING CreatePackage count-based (AssessmentAdminController.cs:5969-5976) — GANTI ke MAX+1:
```csharp
var existingCount = await _context.AssessmentPackages
    .Where(p => p.AssessmentSessionId == assessmentId).CountAsync();
var pkg = new AssessmentPackage {
    AssessmentSessionId = assessmentId,
    PackageName = packageName.Trim(),
    PackageNumber = existingCount + 1   // BUG: turun setelah delete -> bentrok
};
```

EXISTING duplikasi mismatch yang DIKONSOLIDASI:
- Controller AssessmentAdminController.cs:5844-5856 (compute hasMismatch + ViewBag set)
- View Views/Admin/ManagePackages.cshtml:72-78 (re-derive — DRIFT, dihapus di Wave 3)

PackageNumber = int NON-nullable (Models/AssessmentPackage.cs:19 VERIFIED) -> PLAIN unique index, TANPA filter.

Migration idiom: Migrations/20260618045427_AddUserUnitsTable.cs (migrationBuilder.Sql backfill + CreateIndex).
DbContext fluent idiom: Data/ApplicationDbContext.cs:359-361 (UserUnits composite unique HasDatabaseName).

Fixture integration kanonik: HcPortal.Tests/ShuffleLockGuardTests.cs:22-30 (IClassFixture<ProtonCompletionFixture> + [Trait("Category","Integration")]).
Pure test kanonik: HcPortal.Tests/ShuffleToggleRulesTests.cs:11-46 ([Theory][InlineData] truth-table, no DB).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pure helpers (PackageSizeAnalysis NEW + ShuffleToggleRules extend + SessionEditLockRules NEW) + pure tests</name>
  <read_first>
    - Helpers/ShuffleToggleRules.cs (EXTEND target + struktur kill-drift) — analog exact
    - Helpers/RetakeRules.cs (pola helper pure XML-doc "dipakai DI DUA TEMPAT")
    - Helpers/ShuffleEngine.cs:96-119 (K=min source — READ ONLY, helper hanya BACA fakta, jangan duplikasi algoritma min)
    - HcPortal.Tests/ShuffleToggleRulesTests.cs:11-46 (pure-test [Theory][InlineData] analog)
    - 422-PATTERNS.md §"PackageSizeAnalysis" + §"ShuffleToggleRules EXTEND" + §"IsSessionEditLocked" (excerpt verbatim)
  </read_first>
  <behavior>
    - PackageSizeAnalysis.Compute([]) -> Result(0, null, false)
    - PackageSizeAnalysis.Compute([pkg(3q)]) -> Result(1, 3, false)
    - PackageSizeAnalysis.Compute([pkg(3q), pkg(3q)]) -> Result(2, 3, false)  // sama -> no mismatch
    - PackageSizeAnalysis.Compute([pkg(3q), pkg(5q)]) -> Result(2, 3, true)   // beda -> mismatch
    - PackageSizeAnalysis.Compute([pkg(0q), pkg(3q)]) -> Result(1, 3, false)  // paket kosong diabaikan
    - ShuffleToggleRules.ShouldShowKMinTruncationWarning(2, true, true) -> true   // ON-path mismatch
    - ShouldShowKMinTruncationWarning(2, false, true) -> false  // OFF-path TIDAK trigger ON-warning
    - ShouldShowKMinTruncationWarning(1, true, true) -> false   // <2 paket
    - ShouldShowKMinTruncationWarning(2, true, false) -> false  // no mismatch
    - ShouldShowSizeMismatchWarning (EXISTING OFF-path) tetap lulus regresi: (2,false,true)->true, (2,true,true)->false
    - SessionEditLockRules.IsSessionEditLocked(session{PostTest, SamePackage=true}) -> true
    - IsSessionEditLocked({PostTest, SamePackage=false}) -> false
    - IsSessionEditLocked({PreTest, SamePackage=true}) -> false
    - IsSessionEditLocked({Standard, SamePackage=false}) -> false
  </behavior>
  <action>
    Buat `Helpers/PackageSizeAnalysis.cs` (namespace `HcPortal.Helpers`, pure static, EF-free, `using HcPortal.Models;`):
    ```csharp
    public static class PackageSizeAnalysis {
        public readonly record struct Result(int PackagesWithQuestions, int? ReferenceCount, bool HasMismatch);
        public static Result Compute(IEnumerable<AssessmentPackage> packages) {
            var withQ = packages.Where(p => p.Questions != null && p.Questions.Any()).ToList();
            if (withQ.Count == 0) return new Result(0, null, false);
            int refCount = withQ[0].Questions.Count;
            return new Result(withQ.Count, refCount, withQ.Any(p => p.Questions.Count != refCount));
        }
    }
    ```
    XML-doc: "D-05/SHFX-07 — satu sumber kebenaran mismatch. Ganti duplikasi controller :5844-5856 + view :72-78 (kill-drift)."

    EXTEND `Helpers/ShuffleToggleRules.cs` — tambah method ON-path SEJAJAR (jangan ubah method existing):
    ```csharp
    // D-04/SHFX-07: warning K=min ON-path (mirror OFF-path, hanya beda !shuffleQuestions -> shuffleQuestions).
    public static bool ShouldShowKMinTruncationWarning(int packagesWithQuestions, bool shuffleQuestions, bool hasMismatch)
        => packagesWithQuestions >= 2 && shuffleQuestions && hasMismatch;
    ```

    Buat `Helpers/SessionEditLockRules.cs` (namespace `HcPortal.Helpers`, pure static, `using HcPortal.Models;`):
    ```csharp
    public static class SessionEditLockRules {
        /// D-07/SHFX-03 — server-authoritative lock. Dipakai guard 5 endpoint POST (Wave 2) + view friendly disable (Wave 3).
        public static bool IsSessionEditLocked(AssessmentSession s)
            => s.AssessmentType == "PostTest" && s.SamePackage;
    }
    ```
    (Helper terpisah supaya bisa di-unit-test tanpa instansiasi controller — pola RetakeRules.)

    Buat `HcPortal.Tests/PackageSizeAnalysisTests.cs` (pure, no fixture, no [Trait]) — `[Theory][InlineData]` cover semua case di <behavior> (bangun AssessmentPackage in-memory dengan `Questions = new List<PackageQuestion>{...}`).
    EXTEND `HcPortal.Tests/ShuffleToggleRulesTests.cs` — tambah `[Theory][InlineData]` block untuk `ShouldShowKMinTruncationWarning` (4 case ON-path) + 2 regresi OFF-path existing.
    Buat `HcPortal.Tests/SessionEditLockRulesTests.cs` (pure, no fixture) — `[Theory][InlineData]` truth-table 4 case (variasi AssessmentType x SamePackage).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test --filter "FullyQualifiedName~PackageSizeAnalysisTests|FullyQualifiedName~ShuffleToggleRulesTests|FullyQualifiedName~SessionEditLockRulesTests" 2>&1 | tail -8</automated>
  </verify>
  <done>
    `dotnet build` 0 error; ketiga test suite hijau. `grep "ShouldShowKMinTruncationWarning" Helpers/ShuffleToggleRules.cs` count >= 1; `grep "record struct Result" Helpers/PackageSizeAnalysis.cs` count == 1; `grep "IsSessionEditLocked" Helpers/SessionEditLockRules.cs` count >= 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migration AddPackageNumberUniqueIndex (dedup ROW_NUMBER -> CreateIndex) + fluent index + ef database update + sqlcmd verify</name>
  <read_first>
    - Migrations/20260618045427_AddUserUnitsTable.cs:35-57 (migrationBuilder.Sql backfill + CreateIndex idiom — analog exact)
    - Data/ApplicationDbContext.cs:353-361 (fluent composite unique HasDatabaseName idiom)
    - Models/AssessmentPackage.cs:19 (PackageNumber int NON-nullable — konfirmasi PLAIN index tanpa filter)
    - 422-RESEARCH.md Pitfall 1 (CreateIndex gagal tanpa dedup) + Pitfall 2 (filter mubazir non-nullable) + §"Code Examples D-02 Migration"
    - 422-PATTERNS.md §"Migrations AddPackageNumberUniqueIndex (HIGHEST RISK)"
    - CLAUDE.md Develop Workflow (migration=TRUE -> file migration WAJIB di-commit + notify IT; verifikasi lokal dotnet ef database update + sqlcmd -C -I)
  </read_first>
  <action>
    1. Tambah fluent index di `Data/ApplicationDbContext.cs` (di OnModelCreating, blok `modelBuilder.Entity<AssessmentPackage>`). PLAIN unique (PackageNumber non-nullable -> TANPA `.HasFilter`):
    ```csharp
    modelBuilder.Entity<AssessmentPackage>(entity =>
    {
        entity.HasIndex(p => new { p.AssessmentSessionId, p.PackageNumber })
              .IsUnique()
              .HasDatabaseName("IX_AssessmentPackages_SessionId_PackageNumber_Unique");
    });
    ```
    2. Generate migration:
    ```bash
    dotnet ef migrations add AddPackageNumberUniqueIndex
    ```
    3. EDIT file migration Up() — sisipkan dedup SQL SEBELUM `CreateIndex` (literal statik, NO user input). Hapus `filter:` bila EF auto-generate-kan (PackageNumber non-nullable):
    ```csharp
    protected override void Up(MigrationBuilder migrationBuilder)
    {
        // STEP 1 — DEDUP/RENUMBER duplikat existing DULU (else CreateIndex gagal). Idempotent.
        migrationBuilder.Sql(@"
            WITH Numbered AS (
                SELECT Id, ROW_NUMBER() OVER (PARTITION BY AssessmentSessionId ORDER BY PackageNumber, Id) AS rn
                FROM AssessmentPackages )
            UPDATE p SET p.PackageNumber = n.rn
            FROM AssessmentPackages p INNER JOIN Numbered n ON p.Id = n.Id;");
        // STEP 2 — plain UNIQUE index (NON-nullable -> NO filter).
        migrationBuilder.CreateIndex(
            name: "IX_AssessmentPackages_SessionId_PackageNumber_Unique",
            table: "AssessmentPackages",
            columns: new[] { "AssessmentSessionId", "PackageNumber" },
            unique: true);
    }
    protected override void Down(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.DropIndex(
            name: "IX_AssessmentPackages_SessionId_PackageNumber_Unique",
            table: "AssessmentPackages");
        // Renumber TIDAK di-revert (data-fix permanen, aman).
    }
    ```
    ⚠️ Verifikasi migration TIDAK menghasilkan `filter: "[PackageNumber] IS NOT NULL"` — bila ada, hapus (Pitfall 2).
    4. Apply + verifikasi lokal SQLEXPRESS (branch ITHandoff):
    ```bash
    dotnet build
    dotnet ef database update
    ```
    5. Verifikasi 0 duplikat via sqlcmd (sebelum dianggap selesai):
    ```bash
    sqlcmd -C -I -Q "SELECT AssessmentSessionId, PackageNumber, COUNT(*) c FROM AssessmentPackages GROUP BY AssessmentSessionId, PackageNumber HAVING COUNT(*) > 1"
    ```
    Harus 0 baris.
    6. Commit migration + Designer + `ApplicationDbContextModelSnapshot.cs` BERSAMA (jangan parsial). Flag migration=TRUE untuk notify IT akhir milestone.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && ls Migrations/*AddPackageNumberUniqueIndex.cs && sqlcmd -C -I -Q "SET NOCOUNT ON; SELECT COUNT(*) AS dups FROM (SELECT AssessmentSessionId, PackageNumber FROM AssessmentPackages GROUP BY AssessmentSessionId, PackageNumber HAVING COUNT(*) > 1) x" 2>&1 | tail -5</automated>
  </verify>
  <done>
    Migration file ada (Up dedup-then-CreateIndex, Down DropIndex-only). `dotnet build` 0 error. `dotnet ef database update` sukses (index hadir). sqlcmd dups == 0. Migration TIDAK punya `filter:` clause. Snapshot + Designer ter-commit bersama migration.
  </done>
</task>

<task type="auto">
  <name>Task 3: CreatePackage MAX+1 + 5x .ThenBy(p => p.Id) + integration tests (PackageNumberUnique + PackageNumberMigration)</name>
  <read_first>
    - Controllers/AssessmentAdminController.cs:5958-5994 (CreatePackage — ganti existingCount+1 -> MAX+1)
    - Controllers/AssessmentAdminController.cs:5447, 5527, 5572, 5764, 5895 (5x OrderBy(p => p.PackageNumber) — append ThenBy)
    - 422-PATTERNS.md §"CreatePackage MAX+1 + 5x ThenBy(Id)" (excerpt verbatim + daftar 5 line)
    - HcPortal.Tests/ShuffleLockGuardTests.cs:22-30 + :150-165 (fixture IClassFixture<ProtonCompletionFixture> + seed AssessmentPackage)
    - Migrations/_AddPackageNumberUniqueIndex.cs (dari Task 2 — SQL dedup yang di-test PackageNumberMigrationTests)
  </read_first>
  <action>
    1. `CreatePackage` (`:5969-5976`): GANTI count-based ke MAX+1 server-computed:
    ```csharp
    var maxNumber = await _context.AssessmentPackages
        .Where(p => p.AssessmentSessionId == assessmentId)
        .Select(p => (int?)p.PackageNumber)
        .MaxAsync();                       // null bila belum ada paket
    var pkg = new AssessmentPackage {
        AssessmentSessionId = assessmentId,
        PackageName = packageName.Trim(),
        PackageNumber = (maxNumber ?? 0) + 1
    };
    ```
    2. Tambah `.ThenBy(p => p.Id)` ke SEMUA 5 query `OrderBy(p => p.PackageNumber)` (deterministik round-robin lintas reshuffle) di line `:5447`, `:5527`, `:5572`, `:5764`, `:5895`. Tiap jadi `.OrderBy(p => p.PackageNumber).ThenBy(p => p.Id)`. (Verifikasi via grep semua occurrence sudah ter-append; jangan ada `OrderBy(p => p.PackageNumber)` telanjang tersisa di file.)
    3. Buat `HcPortal.Tests/PackageNumberUniqueTests.cs` (`[Trait("Category","Integration")]`, IClassFixture<ProtonCompletionFixture>):
       - Test A (MAX+1 anti-bentrok): seed session + 3 paket (PackageNumber 1,2,3) -> delete paket #2 -> insert paket baru via CreatePackage path (atau replikasi MAX+1 logic) -> PackageNumber baru == 4 (BUKAN 3 yang bentrok dgn #3).
       - Test B (index reject dup): seed 2 paket dengan (AssessmentSessionId, PackageNumber) IDENTIK -> SaveChangesAsync -> assert `DbUpdateException` thrown.
       Gunakan marker `"TAG-" + Guid.NewGuid().ToString("N").Substring(0,8)` untuk isolasi seed.
    4. Buat `HcPortal.Tests/PackageNumberMigrationTests.cs` (`[Trait("Category","Integration")]`): seed 3 paket DUPLIKAT (mis. (sessionX, 1), (sessionX, 1), (sessionX, 2)) via raw insert / SaveChanges-sebelum-index-jika-perlu -> jalankan SQL dedup ROW_NUMBER (sama dengan migration Up Step 1) -> assert 0 duplikat per session + renumber gap-free (1,2,3) terurut by (PackageNumber, Id).
       (Test menjalankan SQL dedup langsung via `_context.Database.ExecuteSqlRaw(...)` agar memverifikasi statement migration, bukan menunggu migration apply.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test --filter "FullyQualifiedName~PackageNumberUniqueTests|FullyQualifiedName~PackageNumberMigrationTests" 2>&1 | tail -8</automated>
  </verify>
  <done>
    `dotnet build` 0 error; kedua integration test hijau. `grep -c "OrderBy(p => p.PackageNumber).ThenBy(p => p.Id)" Controllers/AssessmentAdminController.cs` >= 5; `grep "OrderBy(p => p.PackageNumber)$\|OrderBy(p => p.PackageNumber)[^.]" Controllers/AssessmentAdminController.cs` -> 0 telanjang tersisa; `grep "MaxAsync" Controllers/AssessmentAdminController.cs` mencakup CreatePackage; `grep "existingCount + 1" Controllers/AssessmentAdminController.cs` -> 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC browser -> AssessmentAdmin POST (CreatePackage) | Input `assessmentId`/`packageName`; PackageNumber server-computed (BUKAN dari client) |
| Migration runtime -> SQL Server | Raw SQL dedup statement dieksekusi saat `database update` (Dev/Prod oleh IT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-422-01 | Tampering | Migration dedup raw SQL | mitigate | `migrationBuilder.Sql` = literal statik, NO user input (idiom AddUserUnitsTable). ROW_NUMBER deterministik. |
| T-422-02 | Tampering | CreatePackage PackageNumber | mitigate | Server-computed via `MaxAsync` — client tak bisa inject nomor; unique index = jaring DB-level. |
| T-422-03 | Denial of Service | CreateIndex pada data duplikat | mitigate | Dedup ROW_NUMBER dijalankan SEBELUM CreateIndex -> migration tak gagal/abort. Down() non-destruktif. |
| T-422-04 | Information Disclosure | Pure helper PackageSizeAnalysis/ShuffleToggleRules | accept | Helper EF-free, hanya hitung count/boolean; tidak ekspos data sensitif. Low-value. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test --filter "Category!=Integration"` hijau (pure helper) + `dotnet test` full (incl integration real-SQLEXPRESS) hijau.
- `dotnet ef database update` lokal sukses; sqlcmd dups == 0 sebelum & sesudah.
- Migration TIDAK punya `filter:` clause (PackageNumber non-nullable).
- Suite existing tak regresi (ShuffleToggleRulesTests OFF-path tetap lulus).
</verification>

<success_criteria>
- SHFX-05: CreatePackage MAX+1, 5× ThenBy(Id), unique index applied + dedup defensif (migration=TRUE).
- SHFX-07 (partial — backend): ShouldShowKMinTruncationWarning ON-path ada; PackageSizeAnalysis.Compute satu sumber. (View render = Wave 3.)
- IsSessionEditLocked helper ada + unit-tested (dipakai Wave 2 guard + Wave 3 view).
- Semua test Wave 0 fase 422 hijau; build 0 error.
</success_criteria>

<output>
After completion, create `.planning/phases/422-samepackage-shuffle-integrity/422-01-SUMMARY.md`.
Catat: (1) migration=TRUE name `AddPackageNumberUniqueIndex` + commit hash (untuk notify IT); (2) tiap perubahan shuffle/order untuk rekonsiliasi merge v32.6.
</output>
