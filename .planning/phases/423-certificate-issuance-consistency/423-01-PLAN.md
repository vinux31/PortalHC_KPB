---
phase: 423-certificate-issuance-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/CertIssuanceRules.cs
  - Helpers/CertNumberHelper.cs
  - HcPortal.Tests/CertIssuanceRulesTests.cs
autonomous: true
migration: false
requirements: [CERT-01, CERT-02, CERT-03, CERT-06]
must_haves:
  truths:
    - "ShouldIssueCertificate(session) menolak AssessmentType=='PreTest' di SEMUA kasus (true hanya bila GenerateCertificate && IsPassed==true && bukan PreTest)"
    - "DeriveValidUntil('Permanent', completedAt) == null; 'Annual' == CompletedAt.AddYears(1); '3-Year' == CompletedAt.AddYears(3); type non-kanonik/null == null (passthrough caller, tidak derive)"
    - "ResemblesAutoCertFormat menerima persis format auto 'KPB/{3-digit}/{ROMAN}/{4-digit}' (regex ^KPB/\\d{3}/[IVX]+/\\d{4}$), tolak free-text manual lain"
    - "PendingAgeBadgeClass: >7 hari -> 'bg-danger'; >3 hari -> 'bg-warning text-dark'; <=3 -> 'bg-secondary'"
    - "CertNumberHelper.TryAssignNextSeqAsync retry-loop diharden — cap dinaikkan ke 8 + jitter (Task.Delay random), filtered WHERE NomorSertifikat==null + IsDuplicateKeyException dipertahankan; return bool (false=gagal -> caller non-destruktif)"
    - "CertIssuanceRules adalah pure static EF-free — bisa di-unit-test tanpa DbContext/controller (analog SessionEditLockRules)"
  artifacts:
    - path: "Helpers/CertIssuanceRules.cs"
      provides: "Pure static gate kelayakan cert + derive ValidUntil + namespace regex + badge umur — single source-of-truth dipakai Wave 2 (4 site) + Wave 3 (2 view)"
      contains: "public static bool ShouldIssueCertificate("
    - path: "Helpers/CertIssuanceRules.cs"
      provides: "DeriveValidUntil dari CompletedAt per CertificateType kanonik (D-04/D-05)"
      contains: "public static DateOnly? DeriveValidUntil("
    - path: "Helpers/CertIssuanceRules.cs"
      provides: "ResemblesAutoCertFormat regex namespace-guard (D-02/CERT-04)"
      contains: "public static bool ResemblesAutoCertFormat("
    - path: "Helpers/CertIssuanceRules.cs"
      provides: "PendingAgeBadgeClass umur->bootstrap class (D-09/CERT-07)"
      contains: "public static string PendingAgeBadgeClass("
    - path: "Helpers/CertNumberHelper.cs"
      provides: "TryAssignNextSeqAsync — loop retry+jitter terpusat (D-03/CERT-03) yang 3 site grading-time panggil"
      contains: "TryAssignNextSeqAsync"
    - path: "HcPortal.Tests/CertIssuanceRulesTests.cs"
      provides: "Pure truth-table CERT-01/02/04/06/07 (analog SessionEditLockRulesTests)"
      contains: "ShouldIssueCertificate_TruthTable"
  key_links:
    - from: "Helpers/CertIssuanceRules.ShouldIssueCertificate"
      to: "AssessmentConstants.AssessmentType.PreTest"
      via: "perbandingan AssessmentType != PreTest"
      pattern: "AssessmentType.PreTest"
    - from: "Helpers/CertIssuanceRules.DeriveValidUntil"
      to: "AssessmentConstants.CertificateType (Permanent/Annual/3-Year)"
      via: "switch expression"
      pattern: "CertificateType.ThreeYear"
    - from: "Helpers/CertNumberHelper.TryAssignNextSeqAsync"
      to: "filtered unique index IX_AssessmentSessions_NomorSertifikat_Unique"
      via: "ExecuteUpdateAsync WHERE NomorSertifikat==null + catch IsDuplicateKeyException + jitter"
      pattern: "Task.Delay"
---

<objective>
Keystone fase 423: bangun helper pure `CertIssuanceRules` (single source-of-truth kelayakan & derivasi cert) + harden loop seq di `CertNumberHelper` jadi fungsi terpusat `TryAssignNextSeqAsync`, plus pure tests truth-table. Ini wave signature-establishing — keempat method `CertIssuanceRules` dan `TryAssignNextSeqAsync` adalah KONTRAK yang Wave 2 (wire 4 cert-issue site) dan Wave 3 (badge 2 view) panggil. Tidak ada wiring/UI di plan ini — murni helper + test.

Purpose: Saat ini aturan kelayakan cert tersebar & divergen di 4 site (sebagian cek PreTest, sebagian tidak; manual hardcode `GenerateCertificate=true`) = root-cause GRD-01/FLD-5.2-10. Loop retry seq MAX+1 race-prone tersalin identik di 3 site (drift) = GRD-08. Plan ini memusatkan keputusan ke kelas `static` EF-free (analog `SessionEditLockRules` dari fase 422) supaya testable tanpa DB + satu titik perubahan, dan mengeraskan loop seq di SATU fungsi (cap dinaikkan + jitter) supaya tahan burst finalize tanpa migration.
Output: 1 file helper NEW (`CertIssuanceRules.cs`, 4 pure method) + EXTEND `CertNumberHelper.cs` (tambah `TryAssignNextSeqAsync` terpusat retry+jitter, tanpa ganti `Build`/`GetNextSeqAsync`/`IsDuplicateKeyException`) + 1 file test NEW (`CertIssuanceRulesTests.cs`, truth-table CERT-01/02/04/06/07).

migration=FALSE — index `IX_AssessmentSessions_NomorSertifikat_Unique` SUDAH ADA (Migrations/20260317143630); plan ini tidak menyentuh schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/423-certificate-issuance-consistency/423-CONTEXT.md
@.planning/phases/423-certificate-issuance-consistency/423-RESEARCH.md
@.planning/phases/423-certificate-issuance-consistency/423-PATTERNS.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diambil dari source aktual (VERIFIED 2026-06-24). JANGAN eksplorasi codebase — pakai ini. -->

ANALOG SHAPE — Helpers/SessionEditLockRules.cs (template kelas pure static, fase 422):
```csharp
using HcPortal.Models;

namespace HcPortal.Helpers
{
    /// <summary>v32.7 ... predicate tunggal supaya bisa di-unit-test tanpa instansiasi controller.</summary>
    public static class SessionEditLockRules
    {
        public static bool IsSessionEditLocked(AssessmentSession s)
            => s.AssessmentType == "PostTest" && s.SamePackage;
    }
}
```

KONSTANTA (VERIFIED Models/AssessmentConstants.cs):
- AssessmentConstants.AssessmentType.PreTest   = "PreTest"  (:9)
- AssessmentConstants.CertificateType.Permanent = "Permanent" (:26)
- AssessmentConstants.CertificateType.Annual    = "Annual"    (:27)
- AssessmentConstants.CertificateType.ThreeYear = "3-Year"    (:28)

FIELD TYPES (VERIFIED Models/AssessmentSession.cs): CompletedAt=DateTime? (:57), ValidUntil=DateOnly? (:84),
GenerateCertificate=bool default false (:36), IsPassed=bool? (:56), RenewsSessionId=int? (:134),
CertificateType=string? (:166), AssessmentType=string? (:175), UpdatedAt=DateTime? (:106).

EXISTING Helpers/CertNumberHelper.cs (KEEP apa adanya, EXTEND saja):
```csharp
public static string Build(int seq, DateTime date)
    => $"KPB/{seq:D3}/{ToRomanMonth(date.Month)}/{date.Year}";

public static async Task<int> GetNextSeqAsync(ApplicationDbContext context, int year) { /* MAX+1 :23-35 */ }

public static bool IsDuplicateKeyException(DbUpdateException ex)   // :37-42 — handle nama index + 2601/2627
{
    return ex.InnerException?.Message.Contains("IX_AssessmentSessions_NomorSertifikat") == true
        || ex.InnerException?.Message.Contains("2601") == true
        || ex.InnerException?.Message.Contains("2627") == true;
}
```

EXISTING loop yang DIKONSOLIDASI ke TryAssignNextSeqAsync (pola lengkap = GradingService.cs:520-552 RegradeAfterEditAsync, sudah punya guard `updated > 0`):
```csharp
// retry-3x + filtered WHERE NomorSertifikat==null + ExecuteUpdateAsync. Delta D-03: cap-naik + jitter.
const int maxCertAttempts = 3;       // <- naikkan ke 8
while (!certSaved && certAttempts < maxCertAttempts) {
    certAttempts++;
    try {
        var nextSeq = await CertNumberHelper.GetNextSeqAsync(_context, certYear);
        var updated = await _context.AssessmentSessions
            .Where(s => s.Id == session.Id && s.NomorSertifikat == null)
            .ExecuteUpdateAsync(s => s.SetProperty(r => r.NomorSertifikat, CertNumberHelper.Build(nextSeq, certNow)));
        if (updated > 0) certSaved = true;
    }
    catch (DbUpdateException ex) when (certAttempts < maxCertAttempts && CertNumberHelper.IsDuplicateKeyException(ex)) { }
}
```

PURE TEST ANALOG — HcPortal.Tests/SessionEditLockRulesTests.cs (VERBATIM template, no DB, no [Trait]):
```csharp
using HcPortal.Helpers;
using HcPortal.Models;
using Xunit;

namespace HcPortal.Tests;

public class SessionEditLockRulesTests
{
    [Theory]
    [InlineData("PostTest", true, true)]
    [InlineData("PreTest", true, false)]
    [InlineData(null, true, false)]
    public void IsSessionEditLocked_TruthTable(string? assessmentType, bool samePackage, bool expected)
    {
        var session = new AssessmentSession { AssessmentType = assessmentType, SamePackage = samePackage };
        Assert.Equal(expected, SessionEditLockRules.IsSessionEditLocked(session));
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEW Helpers/CertIssuanceRules.cs (4 pure method) + pure truth-table tests</name>
  <read_first>
    - Helpers/SessionEditLockRules.cs (template kelas pure static EF-free — analog exact, salin shape header/namespace/XML-doc)
    - Helpers/ShuffleToggleRules.cs (idiom multi-method static + switch/regex pure)
    - Models/AssessmentConstants.cs:9,26-28 (konstanta PreTest/Permanent/Annual/ThreeYear — pakai konstanta, JANGAN literal string)
    - Models/AssessmentSession.cs:36,56,57,84,166,175 (field types: GenerateCertificate bool, IsPassed bool?, CompletedAt DateTime?, ValidUntil DateOnly?, CertificateType/AssessmentType string?)
    - HcPortal.Tests/SessionEditLockRulesTests.cs (pure-test [Theory][InlineData] analog VERBATIM)
    - 423-PATTERNS.md "Helpers/CertIssuanceRules.cs (NEW)" + 423-RESEARCH.md Pattern 1 (:162-203, signature konkret)
  </read_first>
  <behavior>
    - ShouldIssueCertificate({AssessmentType:"PreTest", GenerateCertificate:true, IsPassed:true}) -> false  (PreTest SELALU tolak, CERT-01)
    - ShouldIssueCertificate({"PostTest", true, true}) -> true
    - ShouldIssueCertificate({"PostTest", false, true}) -> false  (GenerateCertificate=false)
    - ShouldIssueCertificate({"PostTest", true, false}) -> false  (IsPassed false)
    - ShouldIssueCertificate({"PostTest", true, null}) -> false  (IsPassed null != true)
    - ShouldIssueCertificate({"Standard", true, true}) -> true
    - ShouldIssueCertificate({"Manual", true, true}) -> true  (Manual bukan PreTest)
    - DeriveValidUntil("Permanent", 2026-06-24 UTC) -> null  (D-04)
    - DeriveValidUntil("Annual", 2026-06-24) -> 2027-06-24
    - DeriveValidUntil("3-Year", 2026-06-24) -> 2029-06-24
    - DeriveValidUntil("Kompetensi", 2026-06-24) -> null  (non-kanonik: helper TIDAK derive — caller pakai input apa adanya, D-10)
    - DeriveValidUntil("Annual", null) -> null  (CompletedAt null)
    - ResemblesAutoCertFormat("KPB/005/VI/2026") -> true
    - ResemblesAutoCertFormat("KPB/12/VI/2026") -> false  (seq bukan 3-digit)
    - ResemblesAutoCertFormat("SERT/MANUAL/001") -> false
    - ResemblesAutoCertFormat(null) / "" / "   " -> false
    - PendingAgeBadgeClass(now-8hari, now) -> "bg-danger"  (>7, D-09)
    - PendingAgeBadgeClass(now-5hari, now) -> "bg-warning text-dark"  (>3)
    - PendingAgeBadgeClass(now-2hari, now) -> "bg-secondary"  (<=3)
    - PendingAgeBadgeClass(null, now) -> "bg-secondary"
  </behavior>
  <action>
    Buat `Helpers/CertIssuanceRules.cs` (namespace `HcPortal.Helpers`, `public static class`, EF-free, `using HcPortal.Models;` + `using System.Text.RegularExpressions;`). XML-doc kelas: "v32.7 Phase 423 (CERT-01..07) — single source-of-truth kelayakan & derivasi sertifikat. Pure EF-free supaya bisa di-unit-test tanpa DbContext/controller. Dipakai DI EMPAT cert-issue site (Wave 2) + DUA view PendingGrading (Wave 3). Analog SessionEditLockRules (fase 422)."

    Implementasi PERSIS (salin nilai konkret):
    ```csharp
    // CERT-01 (D-01) — gate tunggal: tolak PreTest, wajib GenerateCertificate && lulus. Dipakai di 4 site.
    public static bool ShouldIssueCertificate(AssessmentSession s)
        => s.GenerateCertificate
           && s.IsPassed == true
           && s.AssessmentType != AssessmentConstants.AssessmentType.PreTest;

    // CERT-02/06 (D-04/D-05/D-10) — derive ValidUntil dari CompletedAt utk CertificateType KANONIK saja.
    // Permanent->null; Annual->+1y; 3-Year->+3y; non-kanonik/null->null (caller pakai input apa adanya).
    public static DateOnly? DeriveValidUntil(string? certificateType, DateTime? completedAt)
    {
        if (completedAt == null) return null;
        var baseDate = DateOnly.FromDateTime(completedAt.Value);
        return certificateType switch
        {
            AssessmentConstants.CertificateType.Permanent => null,
            AssessmentConstants.CertificateType.Annual    => baseDate.AddYears(1),
            AssessmentConstants.CertificateType.ThreeYear => baseDate.AddYears(3),
            _ => null
        };
    }

    // CERT-04 (D-02) — nomor manual TIDAK boleh menyerupai format auto KPB/{seq:D3}/{ROMAN}/{YEAR}.
    public static bool ResemblesAutoCertFormat(string? nomor)
        => !string.IsNullOrWhiteSpace(nomor)
           && Regex.IsMatch(nomor!, @"^KPB/\d{3}/[IVX]+/\d{4}$");

    // CERT-07 (D-09) — umur PendingGrading (CompletedAt UTC) -> bootstrap badge class. >7=merah, >3=kuning.
    public static string PendingAgeBadgeClass(DateTime? completedAtUtc, DateTime nowUtc)
    {
        if (completedAtUtc == null) return "bg-secondary";
        var days = (nowUtc - completedAtUtc.Value).TotalDays;
        if (days > 7) return "bg-danger";
        if (days > 3) return "bg-warning text-dark";
        return "bg-secondary";
    }
    ```
    Catatan kanonik: `ThreeYear` = "3-Year" (PAKAI konstanta `AssessmentConstants.CertificateType.ThreeYear`, BUKAN literal). ValidUntil = `DateOnly?` (cast eksplisit saat dipakai caller Wave 2 — pola GradingService.cs:500-501). Untuk type non-kanonik (Kompetensi/Profesi/Pelatihan) helper return null = sinyal "JANGAN derive"; caller manual (Wave 2) memakai `model.ValidUntil` apa adanya (D-10/A1).

    Buat `HcPortal.Tests/CertIssuanceRulesTests.cs` (pure, no fixture, no [Trait]) — `[Theory][InlineData]` cover SEMUA case di <behavior>:
    - `ShouldIssueCertificate_TruthTable(string? type, bool generate, bool passed, bool expected)` — bangun `new AssessmentSession { AssessmentType=type, GenerateCertificate=generate, IsPassed=passed }`. Untuk kasus IsPassed=null gunakan 1 `[Fact]` terpisah (`IsPassed = (bool?)null`) agar InlineData tetap sederhana.
    - `DeriveValidUntil_FromCompletedAt` — pakai `new DateTime(2026,6,24,0,0,0,DateTimeKind.Utc)`, assert `null`/`AddYears(1)`/`AddYears(3)`/`null` (Kompetensi) + 1 `[Fact]` `null` (CompletedAt null).
    - `ResemblesAutoCertFormat_Regex` — `[InlineData]` true/false truth-table + `[Fact]` null/empty/whitespace.
    - `PendingAgeBadgeClass_Thresholds` — pakai `var now = DateTime.UtcNow;` + `now.AddDays(-8)`/`-5`/`-2` + null (gunakan `[Fact]` per-threshold atau satu `[Fact]` multi-assert).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && dotnet test HcPortal.Tests --filter "FullyQualifiedName~CertIssuanceRulesTests" 2>&1 | tail -8</automated>
  </verify>
  <done>
    `dotnet build` 0 error; `CertIssuanceRulesTests` semua hijau. `grep "public static bool ShouldIssueCertificate(" Helpers/CertIssuanceRules.cs` count == 1; `grep "public static DateOnly? DeriveValidUntil(" Helpers/CertIssuanceRules.cs` count == 1; `grep "public static bool ResemblesAutoCertFormat(" Helpers/CertIssuanceRules.cs` count == 1; `grep "public static string PendingAgeBadgeClass(" Helpers/CertIssuanceRules.cs` count == 1; `grep "AssessmentConstants.CertificateType.ThreeYear" Helpers/CertIssuanceRules.cs` count >= 1 (pakai konstanta bukan literal).
  </done>
</task>

<task type="auto">
  <name>Task 2: EXTEND Helpers/CertNumberHelper.cs — TryAssignNextSeqAsync (loop retry+jitter terpusat, cap-naik, non-destruktif)</name>
  <read_first>
    - Helpers/CertNumberHelper.cs (full — KEEP Build/GetNextSeqAsync/IsDuplicateKeyException/ToRomanMonth apa adanya; tambah method async baru di kelas yang sama)
    - Services/GradingService.cs:520-552 (RegradeAfterEditAsync — pola loop PALING LENGKAP: `updated > 0` guard; SALIN struktur ini ke TryAssignNextSeqAsync)
    - Services/GradingService.cs:287-319 (SITE 1 loop — pola yang akan dipanggil-ganti di Wave 2)
    - 423-RESEARCH.md Pattern 2 (:205-234, cap 8 + jitter konkret) + Open-Q #2 (:531-533 ekstrak loop ke 1 fungsi) + 423-PATTERNS.md "CertNumberHelper.cs (MODIFY)"
  </read_first>
  <action>
    Tambah method async terpusat di `Helpers/CertNumberHelper.cs` (di dalam kelas yang sama, di bawah `IsDuplicateKeyException`). Ini mengkonsolidasi loop retry yang saat ini identik di 3 site grading-time (GradingService:287, GradingService:520, AssessmentAdminController:3887) — Wave 2 akan ganti loop inline jadi panggilan ke method ini.

    Signature + body PERSIS (cap 8 + jitter Task.Delay, pertahankan filtered WHERE + IsDuplicateKeyException + `updated > 0` guard):
    ```csharp
    /// <summary>
    /// CERT-03 (D-03) — assign NomorSertifikat atomik: retry+jitter di atas filtered unique index
    /// IX_AssessmentSessions_NomorSertifikat_Unique. Race-safe (WHERE NomorSertifikat==null + ExecuteUpdateAsync).
    /// Return true bila cert tersimpan; false bila gagal setelah semua attempt (caller WAJIB non-destruktif:
    /// sesi sudah Completed/IsPassed sebelum cert -> JANGAN rollback, tandai utk HC). Loop dikonsolidasi
    /// dari 3 site grading-time (kill-drift). Tanpa schema baru (migration=FALSE).
    /// </summary>
    public static async Task<bool> TryAssignNextSeqAsync(
        ApplicationDbContext context, int sessionId, DateTime certNow, int maxAttempts = 8)
    {
        int certYear = certNow.Year;
        int attempts = 0;
        while (attempts < maxAttempts)
        {
            attempts++;
            try
            {
                var nextSeq = await GetNextSeqAsync(context, certYear);
                var nomor = Build(nextSeq, certNow);
                var updated = await context.AssessmentSessions
                    .Where(s => s.Id == sessionId && s.NomorSertifikat == null)
                    .ExecuteUpdateAsync(s => s.SetProperty(r => r.NomorSertifikat, nomor));
                if (updated > 0) return true;
                // updated == 0 -> sudah terisi oleh proses lain (idempotent). Anggap sukses.
                return true;
            }
            catch (DbUpdateException ex) when (attempts < maxAttempts && IsDuplicateKeyException(ex))
            {
                // D-03 jitter: kurangi thundering-herd MAX+1 saat finalize burst.
                await Task.Delay(Random.Shared.Next(10, 60));
            }
        }
        return false;   // gagal setelah maxAttempts — caller tandai non-destruktif (Wave 2).
    }
    ```
    Pastikan `using Microsoft.EntityFrameworkCore;` (untuk `ExecuteUpdateAsync`) + akses `ApplicationDbContext`/`DbUpdateException` sudah tersedia di file (file ini sudah memakai EF — GetNextSeqAsync sudah `ToListAsync`). JANGAN ubah `Build`, `GetNextSeqAsync`, `IsDuplicateKeyException`, `ToRomanMonth`.

    Catatan: `Random.Shared` thread-safe (.NET 6+). `maxAttempts` default 8 (naik dari cap 3 existing, D-03 lebih tahan burst). Method ini TIDAK menulis sinyal-gagal HC — itu tanggung jawab caller di Wave 2 (UpdatedAt + audit, agar GradingService tetap punya akses _logger/_context-nya).
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -3 && grep -c "TryAssignNextSeqAsync\|Task.Delay\|Random.Shared" Helpers/CertNumberHelper.cs</automated>
  </verify>
  <done>
    `dotnet build` 0 error. `grep "public static async Task<bool> TryAssignNextSeqAsync(" Helpers/CertNumberHelper.cs` count == 1; `grep "Task.Delay(Random.Shared.Next" Helpers/CertNumberHelper.cs` count >= 1; `grep "maxAttempts = 8" Helpers/CertNumberHelper.cs` count >= 1. `Build`/`GetNextSeqAsync`/`IsDuplicateKeyException`/`ToRomanMonth` TIDAK berubah (signature utuh). Suite existing tak regresi (`dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (service/controller) -> CertIssuanceRules | Helper pure EF-free; semua input dari domain object server-side (bukan client langsung) |
| TryAssignNextSeqAsync -> SQL Server | ExecuteUpdateAsync + filtered unique index; race control di DB-level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-423-01 | Spoofing | ShouldIssueCertificate (gate PreTest) | mitigate | Gate server-authoritative `AssessmentType != PreTest` pakai konstanta; akan dipasang di SEMUA 4 site (Wave 2) — bukan default/JS warning. Pure-tested truth-table. |
| T-423-02 | Tampering | TryAssignNextSeqAsync seq race | mitigate | Filtered `WHERE NomorSertifikat==null` + `ExecuteUpdateAsync` + `catch IsDuplicateKeyException` + jitter di atas filtered unique index existing (race-safe, no rollback). |
| T-423-03 | Information Disclosure | CertIssuanceRules pure helper | accept | EF-free, hanya hitung bool/DateOnly/string class; tidak ekspos data sensitif. Low-value. |
| T-423-04 | Denial of Service | TryAssignNextSeqAsync retry loop | accept | Cap 8 attempts + jitter 10-60ms bounded; non-destruktif fallback (return false) — tidak menggantung/blok worker. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~CertIssuanceRulesTests"` hijau (semua truth-table CERT-01/02/04/06/07).
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau (tak regresi pure suite).
- `CertIssuanceRules` pure (no `using Microsoft.EntityFrameworkCore`, no DbContext param) — verifikasi EF-free.
- `TryAssignNextSeqAsync` ada + cap 8 + jitter; `Build`/`GetNextSeqAsync`/`IsDuplicateKeyException` signature utuh.
</verification>

<success_criteria>
- CERT-01 (helper): `ShouldIssueCertificate` ada + tolak PreTest + unit-tested. (Wiring 4 site = Wave 2.)
- CERT-02/06 (helper): `DeriveValidUntil` Permanent->null/Annual->+1y/3-Year->+3y dari CompletedAt + unit-tested. (Wiring = Wave 2.)
- CERT-03 (helper): `TryAssignNextSeqAsync` retry+jitter cap 8 terpusat ada. (Wiring 3 site + sinyal-gagal = Wave 2.)
- CERT-04 (helper): `ResemblesAutoCertFormat` regex ada + unit-tested. (Wiring manual = Wave 2.)
- Semua test Wave 0 fase 423 hijau; build 0 error; CertIssuanceRules EF-free.
</success_criteria>

<output>
After completion, create `.planning/phases/423-certificate-issuance-consistency/423-01-SUMMARY.md`.
Catat: (1) signature final `CertIssuanceRules` (4 method) + `TryAssignNextSeqAsync` (kontrak Wave 2/3); (2) keputusan D-10 (non-kanonik return null = caller passthrough) untuk konsumen Wave 2; (3) migration=FALSE (no schema).
</output>
