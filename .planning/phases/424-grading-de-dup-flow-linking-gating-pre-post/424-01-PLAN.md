---
phase: 424-grading-de-dup-flow-linking-gating-pre-post
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/PrePostPairing.cs
  - Helpers/ExamTimeRules.cs
  - Helpers/AssessmentScoreAggregator.cs
  - Services/GradingService.cs
  - HcPortal.Tests/GradingDedupeTests.cs
  - HcPortal.Tests/AssessmentScoreAggregatorTests.cs
  - HcPortal.Tests/ExamTimeRulesTests.cs
  - HcPortal.Tests/PrePostPairingTests.cs
autonomous: true
migration: false
requirements: [GRDF-02]

must_haves:
  truths:
    - "Skor numerik sesi Completed lama TIDAK berubah setelah unifikasi scorer (paritas D-07)"
    - "Ketiga jalur scoring (GradeAndComplete / ComputeScoreAndET / Aggregator.Compute) menghasilkan skor IDENTIK untuk input sama, termasuk MC >1-response (last-write-wins)"
    - "MA multi-row TIDAK ter-dedupe — all-or-nothing tetap utuh"
    - "Essay EssayScore>0=benar / null=pending tetap byte-identik"
    - "Helper ExamTimeRules.AllowedExamSeconds + PrePostPairing.FindPairedPreAsync ada & ter-test"
  artifacts:
    - path: "Helpers/PrePostPairing.cs"
      provides: "FindPairedPreAsync terfilter UserId (GRDF-03 helper, dikonsumsi gate GRDF-01 Plan 02)"
      contains: "FindPairedPreAsync"
    - path: "Helpers/ExamTimeRules.cs"
      provides: "AllowedExamSeconds(duration, extra) = (d+e)*60 (GRDF-05 helper, dikonsumsi clamp Plan 02)"
      contains: "AllowedExamSeconds"
    - path: "Helpers/AssessmentScoreAggregator.cs"
      provides: "Single per-soal scorer dengan dedupe last-write-wins injected (GRDF-02)"
      contains: "OrderByDescending"
    - path: "HcPortal.Tests/GradingDedupeTests.cs"
      provides: "Parity 3-path MC>1-response + MA-no-dedupe guard"
  key_links:
    - from: "Helpers/AssessmentScoreAggregator.cs (Compute)"
      to: "dedupe last-write-wins (GradingService.cs:87-90 pattern)"
      via: "finalByQuestion OrderByDescending(SubmittedAt).First()"
      pattern: "OrderByDescending\\(.*SubmittedAt"
    - from: "Services/GradingService.cs (ComputeScoreAndETInternalAsync)"
      to: "deduped MC selection (bukan mcSel.First())"
      via: "finalByQuestion lookup"
      pattern: "finalByQuestion"
---

<objective>
Tutup GRD-09/GRD-02/GRD-03 (GRDF-02): satukan logika scoring per-soal (MC/MA/Essay) + dedupe ke satu sumber, dan buat dua pure-helper baru (`PrePostPairing`, `ExamTimeRules`) yang akan dikonsumsi Plan 02. KEYSTONE — menetapkan parity-lock SEBELUM konvergensi apa pun.

Purpose: Risiko #1 fase ini adalah "menyatukan tanpa mengubah angka" (D-07). Plan ini menulis Wave-0 parity-characterization test yang MENGUNCI perilaku numerik sekarang, lalu mengkonvergensikan dua jalur drift (`Aggregator.Compute` `FirstOrDefault`, `ComputeScoreAndETInternalAsync` `.First()`) ke dedupe last-write-wins yang sudah jadi kanonik di `GradeAndCompleteAsync:87-90`, dengan test sebagai jaring.

Output: 2 helper baru + Aggregator dipromosikan jadi single-scorer + GradingService di-wire + suite parity hijau (sebelum & sesudah refactor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-CONTEXT.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-RESEARCH.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-PATTERNS.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-VALIDATION.md

<interfaces>
<!-- Kontrak existing yang dikonsumsi — pakai langsung, jangan eksplorasi ulang. -->

Helpers/AssessmentScoreAggregator.cs (EXISTING — yang dimodifikasi):
- ScoreAggregateResult(int TotalScore, int MaxScore, int Percentage, bool IsPassed)
- static ScoreAggregateResult Compute(IEnumerable<PackageQuestion> questions, IEnumerable<PackageUserResponse> responses, int passPercentage)
  → MC branch :38-45 pakai `respList.FirstOrDefault(... PackageOptionId.HasValue)` ❌ no dedupe (DRIFT GRD-02)
  → MA branch :46-50 SetEquals all-or-nothing (KEEP — JANGAN dedupe)
  → Essay branch :52-54 EssayScore.HasValue → totalScore (KEEP)
  → pct :58 LOCKED D-04: `maxScore>0 ? (int)((double)totalScore/maxScore*100) : 0` (JANGAN sentuh)
- static bool? IsQuestionCorrect(PackageQuestion q, IEnumerable<PackageUserResponse> responsesForQ) (KEEP — display correctness Phase 383)

Dedupe kanonik (SUDAH ADA — copy verbatim, Services/GradingService.cs:87-90):
```csharp
var finalByQuestion = allResponses
    .Where(r => r.PackageOptionId.HasValue)        // MC/single-answer only — MA dibaca penuh
    .GroupBy(r => r.PackageQuestionId)
    .ToDictionary(g => g.Key, g => g.OrderByDescending(r => r.SubmittedAt).First());  // last-write-wins (D-06)
```

Services/GradingService.cs — 3 scoring path:
- PATH 1 GradeAndCompleteAsync (:57): dedupe :87-90 ✅ kanonik; MC :105-114 pakai finalByQuestion; MA :116-128; pct :144-145. KEEP perilaku, ini referensi paritas.
- PATH 2 ComputeScoreAndETInternalAsync (private, ~:373): MC `mcSel.First()` (~:414) ❌ no order — DRIFT GRD-03. Dipakai RegradeAfterEditAsync (~:479) + PreviewEditScore.
- (PATH 3 = Aggregator.Compute di atas, dipakai FinalizeEssayGrading.)

Pola model untuk PrePostPairing — Helpers/SiblingSessionQuery.cs (static class Helpers, namespace HcPortal.Helpers, doc-comment naming REQ + call-sites).

AssessmentSession (Models) fields relevan: Id, UserId (string), AssessmentType (string "PreTest"/"PostTest"/"Standard"/...), Status (string), LinkedSessionId (int?), LinkedGroupId (int?), DurationMinutes (int), ExtraTimeMinutes (int?).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 parity-lock + pure helpers PrePostPairing & ExamTimeRules</name>
  <files>Helpers/PrePostPairing.cs, Helpers/ExamTimeRules.cs, HcPortal.Tests/ExamTimeRulesTests.cs, HcPortal.Tests/PrePostPairingTests.cs, HcPortal.Tests/GradingDedupeTests.cs</files>
  <read_first>
    - Helpers/SiblingSessionQuery.cs (analog pure-helper — mirror struktur + doc-comment)
    - Helpers/AssessmentScoreAggregator.cs (current Compute MC `FirstOrDefault` :38-45 — yang akan diparitas-kunci di Task 2)
    - Services/GradingService.cs:80-145 (dedupe kanonik :87-90 + MC/MA/Essay switch — sumber kebenaran paritas)
    - HcPortal.Tests/GradingDedupeTests.cs (FIXTURE recipe `GradingDedupeFixture` :29-83 + `Dedupe_PicksLatestSubmittedAt` + `Dedupe_MultipleAnswer_NotDeduped` — extend di sini, JANGAN regress)
    - HcPortal.Tests/SiblingPrePostFilterTests.cs:12-45 (pola pure truth-table `Compile()`-style)
    - 424-RESEARCH.md §"Pattern 1"/"Pattern 4"/Pitfall 3 + 424-PATTERNS.md §1/§3
  </read_first>
  <behavior>
    - ExamTimeRules: AllowedExamSeconds(10, 5) == 900; AllowedExamSeconds(10, null) == 600; AllowedExamSeconds(0, 0) == 0.
    - PrePostPairing (pure-portion test sekarang; real-SQL gate test ada di Plan 02): FindPairedPreAsync untuk post.AssessmentType=="Standard" → null (pass-through, D-02); untuk "PostTest" tanpa LinkedSessionId & tanpa LinkedGroupId → null.
    - PARITY-LOCK (KRITIS D-07 — tulis & JALANKAN HIJAU terhadap kode SEKARANG sebelum Task 2 mengubah scorer): jalankan input IDENTIK lewat ketiga jalur (GradeAndCompleteAsync hasil, AssessmentScoreAggregator.Compute, dan ComputeScoreAndET via RegradeAfterEditAsync) dan assert skor SAMA untuk: (a) MC 1-response (baseline — hasil tak boleh berubah), (b) MC >1-response opsi-beda SubmittedAt-beda → semua ambil FINAL (last-write-wins), (c) MA multi-row → tidak ter-dedupe (all-or-nothing utuh), (d) Essay EssayScore>0=benar / null=pending.
  </behavior>
  <action>
    Buat `Helpers/ExamTimeRules.cs` — `static class ExamTimeRules` di `namespace HcPortal.Helpers`, doc-comment menyebut GRDF-05 + situs yang disatukan (`CMPController.cs:469` bolong vs `:1175/:1548/:1626/:4596` benar):
    ```csharp
    public static int AllowedExamSeconds(int durationMinutes, int? extraTimeMinutes)
        => (durationMinutes + (extraTimeMinutes ?? 0)) * 60;
    ```

    Buat `Helpers/PrePostPairing.cs` — `static class PrePostPairing` di `namespace HcPortal.Helpers`, doc-comment menyebut GRDF-03 + 3 call-site yang disatukan (CMPController.cs:292-297 display NO-UserId ❌ / :3505-3523 GetGainScoreData ✅ / :2404-2413 LinkedSessionId ✅) + GRDF-01 (gate konsumen). Signatur per 424-RESEARCH Pattern 1 (async EF lookup, A2):
    ```csharp
    public static async Task<AssessmentSession?> FindPairedPreAsync(ApplicationDbContext ctx, AssessmentSession post)
    {
        if (post.AssessmentType != "PostTest") return null;                 // Standard/Pre → bukan target gate (D-02 pass-through)
        if (post.LinkedSessionId.HasValue)                                  // (1) kanonik: Post→Pre eksplisit
            return await ctx.AssessmentSessions.FirstOrDefaultAsync(s =>
                s.Id == post.LinkedSessionId.Value && s.UserId == post.UserId && s.AssessmentType == "PreTest");
        if (post.LinkedGroupId.HasValue)                                    // (2) fallback: group + UserId + type
            return await ctx.AssessmentSessions.FirstOrDefaultAsync(s =>
                s.LinkedGroupId == post.LinkedGroupId.Value && s.UserId == post.UserId && s.AssessmentType == "PreTest");
        return null;                                                        // no link → orphan/Standard, pass-through
    }
    ```
    KRITIS: `s.UserId == post.UserId` WAJIB ada di SETIAP cabang (fix FLOW-01 root :292-297). Title-pattern matching DILARANG. JANGAN pakai pola judul.

    Buat `HcPortal.Tests/ExamTimeRulesTests.cs` — pure xUnit (style `SiblingPrePostFilterTests`), `[Fact]` per kasus behavior di atas. Tanpa DB.

    Buat `HcPortal.Tests/PrePostPairingTests.cs` — pure-portion saja (Standard→null, PostTest-tanpa-link→null) sebagai `[Fact]` murni jika dapat diekstrak tanpa DB; jika butuh `ApplicationDbContext`, tandai `[Trait("Category","Integration")]` reuse `GradingDedupeFixture`. (Skenario gate penuh real-SQL ada di Plan 02.)

    Extend `HcPortal.Tests/GradingDedupeTests.cs` — TAMBAH parity-characterization `[Trait("Category","Integration")]` reuse `GradingDedupeFixture` + `NewGradingService` ctor recipe existing. Tambah test untuk kasus (a)/(b)/(c)/(d) di behavior. JANGAN ubah/hapus `Dedupe_PicksLatestSubmittedAt` & `Dedupe_MultipleAnswer_NotDeduped`. Untuk PATH 2/3 paritas pakai `AssessmentScoreAggregator.Compute` + `RegradeAfterEditAsync` dengan response set yang sama. Tujuan: test HIJAU terhadap kode SEKARANG (lock), tetap hijau setelah Task 2.

    Verifikasi parity-lock hijau SEBELUM lanjut ke Task 2.
  </action>
  <acceptance_criteria>
    - `Helpers/ExamTimeRules.cs` ada & berisi `AllowedExamSeconds`; `Helpers/PrePostPairing.cs` ada & berisi `FindPairedPreAsync` dengan `s.UserId == post.UserId` di setiap cabang (grep: tiga kemunculan `UserId == post.UserId` minimal pada dua cabang Linked*).
    - `grep -c "post.AssessmentType != \"PostTest\"" Helpers/PrePostPairing.cs` ≥ 1 (orphan/Standard pass-through).
    - `grep -i "title\|judul\|regex\|pattern" Helpers/PrePostPairing.cs` → 0 (no title-pattern).
    - `dotnet build` exits 0.
    - `dotnet test HcPortal.Tests --filter "Category!=Integration"` exits 0 (ExamTimeRulesTests + pure PrePostPairing).
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~GradingDedupe"` exits 0 (parity-lock + existing dedupe hijau terhadap kode SEKARANG).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~ExamTimeRules|FullyQualifiedName~PrePostPairing|FullyQualifiedName~GradingDedupe"</automated>
  </verify>
  <done>Dua helper baru ada + ter-test pure; parity-lock characterization test ditulis & HIJAU terhadap kode current (sebelum konvergensi); existing dedupe/MA-no-dedupe test tak regress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promosikan AssessmentScoreAggregator ke single-scorer + konvergensikan 3 jalur (paritas terjaga)</name>
  <files>Helpers/AssessmentScoreAggregator.cs, Services/GradingService.cs, HcPortal.Tests/AssessmentScoreAggregatorTests.cs, HcPortal.Tests/GradingDedupeTests.cs</files>
  <read_first>
    - Helpers/AssessmentScoreAggregator.cs (FULL — Compute :26-60 yang dimodifikasi + IsQuestionCorrect :73-98 yang DIKEEP)
    - Services/GradingService.cs:80-145 (dedupe kanonik :87-90 PATH 1 — referensi) + ComputeScoreAndETInternalAsync (~:373-435, `mcSel.First()` ~:414 — DRIFT yang dikonvergensikan) + RegradeAfterEditAsync caller (~:479)
    - HcPortal.Tests/AssessmentScoreAggregatorTests.cs + HcPortal.Tests/IsQuestionCorrectTests.cs (truth-table existing — extend, JANGAN regress)
    - HcPortal.Tests/GradingDedupeTests.cs (parity test dari Task 1 — harus tetap hijau)
    - 424-RESEARCH.md §"Pattern 3"/Pitfall 2/Pitfall 3 + 424-PATTERNS.md §2
  </read_first>
  <behavior>
    - AssessmentScoreAggregator.Compute MC: saat ada >1 response untuk soal MC sama (PackageOptionId beda, SubmittedAt beda) → pilih response dgn SubmittedAt TERBARU (last-write-wins), BUKAN FirstOrDefault arbitrer.
    - Compute MA: TIDAK ter-dedupe — `SetEquals` dibaca dari SEMUA response soal itu (multi-row). all-or-nothing utuh.
    - Compute Essay: tetap `EssayScore.HasValue → totalScore += EssayScore.Value` (null=skip).
    - pct :58 (D-04 LOCKED) TIDAK berubah byte.
    - ComputeScoreAndETInternalAsync MC: ganti `mcSel.First()` → pilih FINAL via dedupe last-write-wins (sama dgn PATH 1).
    - Parity: ketiga path skor IDENTIK untuk semua kasus di Task 1 (test Task 1 tetap hijau).
  </behavior>
  <action>
    `Helpers/AssessmentScoreAggregator.cs` — pada `Compute`, ganti cabang `case "MultipleChoice"` (:38-45) agar pilih response FINAL via dedupe last-write-wins alih-alih `FirstOrDefault`. Pre-compute sekali di awal `Compute` (sebelum loop) dictionary deduped untuk MC/single-answer (PORT VERBATIM pola dari `GradingService.cs:87-90`):
    ```csharp
    var finalByQuestion = respList
        .Where(r => r.PackageOptionId.HasValue)
        .GroupBy(r => r.PackageQuestionId)
        .ToDictionary(g => g.Key, g => g.OrderByDescending(r => r.SubmittedAt).First());
    ```
    Lalu di `case "MultipleChoice"`: `var mcResp = finalByQuestion.TryGetValue(q.Id, out var fr) ? fr : null;` (ganti `FirstOrDefault`). Cabang MA (:46-50) & Essay (:52-54) & pct (:58) TIDAK disentuh (MA multi-row sah; Essay & formula LOCKED). Update doc-comment kelas (:12-23): klaim "single source of truth" jadikan BENAR — sebut GRDF-02 + dedupe last-write-wins (D-06) + 3 path yang sekarang konvergen; catat "MA TIDAK ter-dedupe (Pitfall 3)".

    `Services/GradingService.cs` — `ComputeScoreAndETInternalAsync`: ganti `mcSel.First()` (~:414) agar memakai dedupe last-write-wins yang sama (last-write-wins by SubmittedAt). Jika method sudah punya list response in-memory, bangun `finalByQuestion` lokal (pola identik :87-90) dan baca FINAL; JANGAN ubah cabang MA/Essay/pct (~:419-430). `GradeAndCompleteAsync` (PATH 1) sudah kanonik — JANGAN ubah logikanya (hanya boleh refactor jika benar-benar netral, tapi tidak diwajibkan).

    JANGAN recompute sesi Completed (D-07) — perubahan ini hanya mengubah SELEKSI saat >1 response; untuk 1 response hasilnya identik. Parity test (Task 1) adalah jaring.

    Extend `HcPortal.Tests/AssessmentScoreAggregatorTests.cs` — tambah `[Fact]` MC >1-response (last-write-wins) + MA multi-row no-dedupe + Essay null=pending, assert skor sesuai. Pure (tanpa DB) bila Compute dapat dipanggil langsung dgn list in-memory.

    Pastikan parity test di `GradingDedupeTests.cs` (Task 1) TETAP HIJAU setelah perubahan (ini bukti paritas D-07).
  </action>
  <acceptance_criteria>
    - `grep "OrderByDescending" Helpers/AssessmentScoreAggregator.cs` → ada (dedupe injected).
    - `grep "FirstOrDefault" Helpers/AssessmentScoreAggregator.cs` pada cabang MC `Compute` → TIDAK ada lagi (sudah finalByQuestion). (IsQuestionCorrect boleh tetap pakai pola lamanya.)
    - `grep "mcSel.First()" Services/GradingService.cs` → 0 (konvergen ke last-write-wins).
    - MA branch `Compute` tetap pakai `SetEquals` (grep ada) dan TIDAK dipindah ke finalByQuestion.
    - `dotnet build` exits 0.
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~AssessmentScoreAggregator|FullyQualifiedName~IsQuestionCorrect|FullyQualifiedName~GradingDedupe"` exits 0 (parity tetap hijau = D-07 terbukti).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~AssessmentScoreAggregator|FullyQualifiedName~IsQuestionCorrect|FullyQualifiedName~GradingDedupe"</automated>
  </verify>
  <done>Aggregator.Compute MC + ComputeScoreAndET MC konvergen ke dedupe last-write-wins; MA/Essay/pct byte-identik; parity test (Task 1) tetap hijau membuktikan tak ada perubahan numerik sesi normal (D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker submit → GradingService | response peserta (PackageUserResponse) di-skor server-side; client tak menentukan skor |
| race multi-tab → DB | >1 response per soal bisa muncul; seleksi harus deterministik |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-424-01 | Tampering | Aggregator.Compute / ComputeScoreAndET (MC seleksi) | mitigate | Dedupe last-write-wins deterministik (D-06) menggantikan FirstOrDefault/`.First()` non-deterministik; parity-characterization test mengunci hasil (race multi-tab tak bisa pilih baris basi sengaja) |
| T-424-02 | Tampering | Paritas sesi Completed lama | mitigate | D-07 forward-only: tak me-recompute; parity test cover 1-response baseline (hasil identik) + >1-response (last-write-wins) — kalau drift, test merah |
| T-424-03 | Information Disclosure | (scoring server-authoritative) | accept | Scoring tidak mengembalikan kunci/answer-key ke client di plan ini; helper murni in-memory, tak ada surface baru. Skor server-authoritative (tak percaya client) dipertahankan. |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests --filter "Category!=Integration"` hijau (pure: ExamTimeRules, PrePostPairing pure-portion, Aggregator).
- `dotnet test HcPortal.Tests` (full, SQLEXPRESS) hijau — parity 3-path + dedupe + MA-no-dedupe + regression guard (GradingDedupeTests, AssessmentScoreAggregatorTests, IsQuestionCorrectTests, EssayFinalizeRecomputeTests) tak ada yang merah.
</verification>

<success_criteria>
- GRDF-02: satu fungsi murni per-soal (Aggregator promoted) + dedupe last-write-wins seragam di 3 jalur.
- D-07 paritas terbukti: parity-characterization test hijau sebelum & sesudah konvergensi.
- Dua helper baru (PrePostPairing, ExamTimeRules) siap dikonsumsi Plan 02.
- 0 regresi pada test existing yang disebut regression guard.
</success_criteria>

<output>
After completion, create `.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-01-SUMMARY.md`
</output>
