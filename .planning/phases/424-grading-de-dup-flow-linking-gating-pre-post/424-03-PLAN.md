---
phase: 424-grading-de-dup-flow-linking-gating-pre-post
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/AutoPairGuardTests.cs
autonomous: false
migration: false
requirements: [GRDF-04, GRDF-05]

must_haves:
  truths:
    - "Assessment mode Standard yang dibuat baru TIDAK mendapat LinkedGroupId dari pola judul (forward-only)"
    - "Baris Standard lama ber-link-semu TIDAK disentuh (non-destruktif D-08)"
    - "Export 'Durasi Aktual' melaporkan ElapsedSeconds benar (tak under-report) setelah clamp fix Plan 02"
    - "Gate gating Pre→Post live: peserta dgn Pre belum Completed di-redirect + pesan ramah @5270"
    - "Submit on-time essay kosong ditolak live; timeout tetap finalize @5270"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Auto-pair judul dimatikan untuk Standard (GRDF-04) + export Durasi Aktual dikonfirmasi (GRDF-05)"
      contains: "TryAutoDetectCounterpartGroup"
    - path: "HcPortal.Tests/AutoPairGuardTests.cs"
      provides: "Standard create tak set LinkedGroupId dari pola judul (forward-only guard)"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs CreateAssessment POST :876-888"
      to: "TryAutoDetectCounterpartGroup dimatikan untuk Standard"
      via: "forward-only disable (D-08)"
      pattern: "TryAutoDetectCounterpartGroup"
---

<objective>
Selesaikan dua sisa GRDF di `AssessmentAdminController.cs` (file berbeda dari Plan 02 — aman di wave terpisah): (1) matikan auto-link Pre/Post semu dari pola judul untuk Standard forward-only (GRDF-04/FLOW-03), (2) konfirmasi export "Durasi Aktual" kini benar setelah clamp fix Plan 02 (GRDF-05). Lalu gerbang checkpoint UAT live @5270 untuk gating redirect + essay reject + smoke.

Purpose: Wave penutup — perubahan creation-time guard + verifikasi end-to-end perilaku gating & essay yang hanya tampak di browser (redirect/TempData/UX). Mirror wave penutup 421/422/423 (autonomous=false, checkpoint manual).

Output: auto-pair Standard mati + guard test + export terkonfirmasi + UAT live disetujui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-CONTEXT.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-RESEARCH.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-PATTERNS.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-VALIDATION.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-01-SUMMARY.md
@.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-02-SUMMARY.md

<interfaces>
<!-- AssessmentAdminController sites (VERIFIED sesi ini) -->

Controllers/AssessmentAdminController.cs:
- CreateAssessment POST auto-pair branch :876-888:
  ```csharp
  // Phase 338 REST-06: Auto-pair LinkedGroupId via title pattern. Hanya untuk standard mode (non PrePost).
  if (CreationMode != "PrePostTest" && model.LinkedGroupId == null && !string.IsNullOrEmpty(model.Title))
  {
      var counterpartId = await TryAutoDetectCounterpartGroup(model.Title, model.Category);  // ← GRDF-04: matikan utk Standard
      if (counterpartId.HasValue) { model.LinkedGroupId = counterpartId.Value; TempData["Info"] = $"Auto-paired ..."; }
  }
  ```
  → branch ini AKTIF tepat untuk Standard (CreationMode != "PrePostTest"). GRDF-04 = matikan forward-only.
- helper TryAutoDetectCounterpartGroup(title, category) :7663-7689 (regex judul `^(Pre|Post)\s*Test\s+...$`) — boleh dibiarkan ada (tak dipanggil lagi) ATAU di-comment; JANGAN hapus call-site lain bila ada.
- export "Durasi Aktual" :4929-4931: `ElapsedSeconds/60` — benar setelah clamp :469 fix (Plan 02). Tak butuh perubahan math; konfirmasi saja.

App: branch ITHandoff, run `dotnet run --urls http://localhost:5270` (JANGAN commit launchSettings). Admin login: lihat reference_dev_credentials (admin@pertamina.com untuk /Admin/*). Seed temporary → ikut SEED_WORKFLOW (snapshot→journal→restore).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Matikan auto-link judul untuk Standard forward-only (GRDF-04) + konfirmasi export Durasi Aktual (GRDF-05)</name>
  <files>Controllers/AssessmentAdminController.cs, HcPortal.Tests/AutoPairGuardTests.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:872-889 (auto-pair branch) + :7663-7689 (TryAutoDetectCounterpartGroup) + :4925-4935 (export Durasi Aktual)
    - 424-RESEARCH.md §"Pattern" GRDF-04 + State of the Art (Phase 338 REST-06 dimatikan forward-only) + Pitfall 8 (export benar setelah clamp) + 424-PATTERNS.md §6
    - 424-CONTEXT.md D-08 (forward-only, baris lama TIDAK disentuh)
  </read_first>
  <behavior>
    - CreateAssessment Standard (CreationMode != "PrePostTest"): TIDAK lagi memanggil TryAutoDetectCounterpartGroup; model.LinkedGroupId tetap null jika tak diisi eksplisit (no title-pseudo-link).
    - Mode PrePostTest: tak terpengaruh (generate GroupId sendiri di logic existing).
    - Baris Standard lama ber-LinkedGroupId semu: TIDAK ada cleanup/perubahan (D-08 non-destruktif).
    - Export "Durasi Aktual": ElapsedSeconds/60 tanpa perubahan math (sudah benar setelah clamp :469 fix Plan 02).
  </behavior>
  <action>
    GRDF-04 — di `AssessmentAdminController.cs:876-888`, matikan auto-pair untuk Standard forward-only (D-08). Pilihan paling bersih: HAPUS/disable blok `if (CreationMode != "PrePostTest" && model.LinkedGroupId == null && ...)` sehingga Standard create tak pernah menetapkan `LinkedGroupId` dari pola judul. Ganti dengan komentar yang menyatakan forward-only disable + referensi GRDF-04/D-08 (mis. "GRDF-04 (FLOW-03/D-08): auto-pair judul DINONAKTIFKAN forward-only untuk Standard — link semu tak dibuat lagi. Baris lama tak disentuh."). JANGAN sentuh logika mode PrePostTest yang generate GroupId sendiri. `TryAutoDetectCounterpartGroup` helper boleh tetap ada (dead) atau ditandai obsolete — JANGAN hapus bila ada call-site lain (grep dulu); jika hanya dipakai di sini, boleh tandai `// GRDF-04: no longer called (forward-only disable)`.

    GRDF-05 (konfirmasi saja) — buka `:4929-4931`, pastikan export "Durasi Aktual" = `ElapsedSeconds/60` (atau setara) TANPA mengubah math. Setelah clamp :469 (Plan 02) memperhitungkan ExtraTime, nilai ElapsedSeconds tersimpan sudah benar → export otomatis benar (Pitfall 8). Jika menemukan export math juga membuang ExtraTime secara terpisah, catat di SUMMARY (tidak diharapkan — root cause = clamp). JANGAN tambahkan double-count ExtraTime di export.

    Buat `HcPortal.Tests/AutoPairGuardTests.cs` — guard forward-only. Bila konstruksi controller tak feasible (ctor besar), uji unit pada helper/decision: assert bahwa untuk CreationMode "Standard"/non-PrePost, jalur create TIDAK menetapkan LinkedGroupId dari judul (mis. test bahwa branch dihapus — minimal: integration real-SQL `[Trait("Category","Integration")]` reuse fixture yang membuat dua sesi judul "Pre Test X"/"Post Test X" mode Standard lalu assert LinkedGroupId tetap null). Jika integrasi terlalu mahal, dokumentasikan guard via UAT di Task 2 dan buat minimal smoke test pada `TryAutoDetectCounterpartGroup` tak lagi dipanggil (atau hapus test bila helper di-obsolete). Prioritaskan test yang menjalankan jalur create nyata.
  </action>
  <acceptance_criteria>
    - `grep -n "TryAutoDetectCounterpartGroup(model.Title" Controllers/AssessmentAdminController.cs` → 0 (call-site dimatikan) ATAU dipastikan tak ter-eksekusi untuk Standard.
    - Komentar GRDF-04/D-08 forward-only ada di lokasi bekas blok auto-pair.
    - Mode PrePostTest GroupId logic tak diubah (grep konfirmasi blok PrePost tetap).
    - Export `:4929-4931` math tak diubah (ElapsedSeconds/60 utuh) — tak ada penambahan ExtraTime di export.
    - `dotnet build` exits 0.
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~AutoPairGuard"` exits 0 (atau guard didokumentasikan di UAT bila integrasi tak feasible — jelaskan di SUMMARY).
    - Full suite `dotnet test HcPortal.Tests` hijau (0 regresi).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~AutoPairGuard"; dotnet build</automated>
  </verify>
  <done>Auto-pair judul mati untuk Standard forward-only; baris lama tak disentuh; export Durasi Aktual dikonfirmasi benar tanpa ubah math; guard test/dokumentasi ada; full suite hijau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint UAT live @5270 — gating + essay + export + forward-only + smoke</name>
  <action>
    Verifikasi end-to-end di browser SETELAH Task 1 + Plan 01/02 selesai dan `dotnet build`/full suite hijau. Ini gerbang manusia (blocking) — automation sudah selesai, checkpoint mengonfirmasi perilaku yang hanya tampak di browser (redirect/TempData/UX). Lihat `<how-to-verify>` untuk langkah persis.
  </action>
  <what-built>
    Seluruh perubahan fase 424 terpasang & build hijau: gate gating Pre→Post (GRDF-01), pairing terfilter UserId (GRDF-03), scorer satu sumber + dedupe (GRDF-02), clamp ExtraTime (GRDF-05), auto-pair Standard mati (GRDF-04), essay kosong on-time ditolak server-side (GRDF-07). Plus full xUnit suite (pure + real-SQL SQLEXPRESS) hijau.
  </what-built>
  <how-to-verify>
    Jalankan app branch ITHandoff: `dotnet build` lalu `dotnet run --urls http://localhost:5270` (JANGAN commit launchSettings). Login Admin (admin@pertamina.com) untuk setup. Snapshot DB lokal dulu (SEED_WORKFLOW) bila perlu seed temporary.

    1. **GRDF-01 gating (HIGH/FLOW-04):** Siapkan pasangan Pre-Post (SamePackage/linked) untuk satu peserta dengan Pre BELUM Completed. Sebagai peserta itu, buka Post-Test → StartExam. EXPECT: redirect ke halaman Assessment + pesan "Selesaikan Pre-Test dulu sebelum mulai Post-Test." Lalu selesaikan Pre (Completed) → buka Post lagi → EXPECT: lolos (masuk ujian).
    2. **GRDF-01 orphan pass-through (D-02):** Buka assessment Standard / Post tanpa pasangan Pre → EXPECT: TIDAK terblok (masuk normal).
    3. **GRDF-07 essay on-time:** Ambil assessment ber-essay, isi sebagian (≥1 essay KOSONG), submit SEBELUM waktu habis → EXPECT: ditolak + pesan ramah "isi semua essay", TIDAK ter-submit. Isi semua essay → submit → EXPECT: lolos. (Opsional) Biarkan waktu habis dgn essay kosong → EXPECT: tetap finalize (PendingGrading), tidak dead-end (D-04).
    4. **GRDF-05 export:** Untuk sesi ber-ExtraTime yang ElapsedSeconds-nya melampaui durasi-dasar, export "Durasi Aktual" → EXPECT: durasi tidak ter-cap di durasi-dasar (tak under-report).
    5. **GRDF-04 forward-only:** Buat assessment Standard baru berjudul mirip pola "Pre Test X" / "Post Test X" → EXPECT: TIDAK ter-auto-link (LinkedGroupId tetap kosong, tak ada info "Auto-paired").
    6. (Smoke) Lifecycle Standard normal: ambil → submit → grade/cert tetap jalan (tak ada regresi).

    Restore DB lokal setelah selesai (SEED_WORKFLOW) + tandai journal cleaned.
  </how-to-verify>
  <verify>
    <automated>MISSING — UAT manual @5270 (redirect/TempData/UX hanya tampak di browser); automation di-cover oleh PrePostGatingTests + EnsureCanSubmitStandardTests (Plan 02) + AutoPairGuardTests (Task 1)</automated>
  </verify>
  <done>6 langkah UAT live disetujui (gating block+pass, orphan pass-through, essay on-time reject + timeout finalize, export tak under-report, Standard tak auto-link, smoke lifecycle no-regresi).</done>
  <resume-signal>Ketik "approved" bila semua langkah sesuai, atau deskripsikan temuan (langkah # + perilaku aktual).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC create POST → DB | pembuatan assessment; auto-link tak boleh membuat pasangan semu lintas-judul |
| worker browser → StartExam/SubmitExam | verifikasi live gate + essay (UAT konfirmasi server-authoritative) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-424-09 | Tampering | CreateAssessment auto-pair (GRDF-04) | mitigate | Matikan auto-link judul untuk Standard (forward-only D-08) — link semu tak dibuat; gate GRDF-01 hanya percaya link eksplisit, jadi link semu lama pun tak bisa salah-gate peserta (FindPairedPreAsync hanya untuk PostTest ber-link valid) |
| T-424-10 | Information Disclosure | export Durasi Aktual | accept | Hanya konfirmasi math; tak ada surface/data baru; RBAC export existing (Admin/HC) dipertahankan |
| T-424-11 | Elevation/Tampering | gate + essay (live) | mitigate | UAT live membuktikan server-authoritative gate + essay reject bekerja di browser (lesson Phase 413: client handler bisa mati — server jadi otoritas) |
</threat_model>

<verification>
- `dotnet build` 0 error.
- `dotnet test HcPortal.Tests` (full, SQLEXPRESS) hijau — AutoPairGuard + seluruh suite fase 424 + regression guard (semua test yang disebut di RESEARCH :506) tak ada yang merah.
- UAT live @5270 (checkpoint) disetujui: 6 langkah gating/essay/export/forward-only/smoke.
</verification>

<success_criteria>
- GRDF-04: Standard tak dapat link semu dari pola judul (forward-only); baris lama tak disentuh (D-08).
- GRDF-05: export Durasi Aktual benar (under-report ditutup di root via clamp Plan 02).
- Seluruh fase 424 ter-verifikasi live: gating redirect + essay reject + orphan pass-through + export + forward-only + smoke.
- 0 regresi guard test.
</success_criteria>

<output>
After completion, create `.planning/phases/424-grading-de-dup-flow-linking-gating-pre-post/424-03-SUMMARY.md`
</output>
