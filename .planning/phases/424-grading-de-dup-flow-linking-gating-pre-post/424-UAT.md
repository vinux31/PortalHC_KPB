# Phase 424 — UAT (browser live @5270)

**Date:** 2026-06-24
**Tester:** otomatis (Playwright) via Admin impersonation worker Iwan
**App:** branch ITHandoff @ http://localhost:5270 · DB HcPortalDB_Dev (snapshot→restore, journal cleaned)
**Verdict:** ✅ PASS — 0 temuan/defect. Headline GRDF-01 gating ter-verifikasi penuh live.

## Setup
- Snapshot `C:/temp/HcPortalDB_Dev_424uat.bak` sebelum seed; restore + journal `cleaned` setelahnya (60 sesi pristine, seed [UAT424] hilang).
- Seed temporary (worker Iwan `66227777-...`): **9023** Pre Gate (Open, LinkedGroupId 999424), **9024** Post Gate (Open, link→9023), **9025** Post Orphan (Open, no link). Gate redirect terjadi SEBELUM cek paket → tak perlu paket/soal untuk uji gate.
- Login Admin `admin@pertamina.com`; impersonate Iwan (read-only) untuk akses sebagai worker.

## Hasil per langkah

### ✅ Step 1 — GRDF-01 gating Pre→Post (HIGH/FLOW-04) — VERIFIED LIVE
| Sub | Aksi | Hasil | Verdict |
|-----|------|-------|---------|
| 1a block | StartExam Post 9024 (Pre 9023 belum Completed) | redirect `/CMP/Assessment` + alert **"Error: Selesaikan Pre-Test dulu sebelum mulai Post-Test."** | ✅ |
| 1b pass | set Pre 9023 → Completed, StartExam Post 9024 | pesan beralih ke "tidak memiliki paket soal" (BUKAN pesan Pre-Test) → gate LEWAT, lanjut ke cek paket | ✅ |
| D-01 | Pre 9023 Completed tapi Score NULL / "Belum Dinilai" (tanpa lulus) | gate tetap LEWAT → **Completed-saja, BUKAN IsPassed** | ✅ |
| lobby UX | kartu Post di lobby | sebelum: tombol disabled "Selesaikan Pre-Test terlebih dahulu"; sesudah Pre Completed: tombol aktif "Mulai Post-Test" | ✅ (defense-in-depth: UX + gate server) |

### ✅ Step 2 — Orphan/Standard pass-through (D-02) — VERIFIED LIVE
StartExam Post Orphan 9025 (tanpa link) → BUKAN pesan Pre-Test, melainkan "Sesi ujian ini tidak memiliki paket soal" → gate **pass-through** (FindPairedPreAsync null), lanjut ke cek paket. ✅

### ◑ Step 3 — GRDF-07 essay kosong on-time — NOT-LIVE (test-covered)
Submit ujian = WRITE worker; impersonation **read-only** memblok POST, dan tak ada kredensial worker untuk login penuh di sesi ini. **Tidak dapat di-drive live.** Tercover otomatis: `EnsureCanSubmitStandardTests` 4/4 (empty-essay→block, all-filled→pass, missing-MC, MC-via-DB) + `EssayEmptyPendingParityTests` 4/4 (timeout tetap finalize PendingGrading, D-04 utuh). Logika murni `EvaluateOnTimeCompletion` di-test eksak.

### ◑ Step 4 — GRDF-05 export Durasi Aktual — NOT-LIVE (test-covered)
Clamp ElapsedSeconds terjadi saat ujian berlangsung (progress AJAX = WRITE); read-only impersonation tak bisa generate ElapsedSeconds live. **Tidak di-drive live.** Tercover: `ExamTimeRulesTests` 3/3 (AllowedExamSeconds incl ExtraTime) + export `:4930` = `ElapsedSeconds/60` (read-only, math tak diubah; benar otomatis pasca clamp root-fix).

### ◑ Step 5 — GRDF-04 forward-only auto-pair off — NOT-LIVE-WIZARD (test-covered)
Create wizard 4-langkah (Kategori→Peserta→Settings→Konfirmasi) butuh paket+soal valid — berat untuk nilai marginal. GRDF-04 = **penghapusan call-site murni** (tak mungkin "tetap link" karena kode hilang). Tercover: `AutoPairGuardTests` 10/10 (sentinel pola LooksLikePrePostTitle) + grep `TryAutoDetectCounterpartGroup(model.Title`=0. Wizard create-path live tidak dijalankan.

### ◑ Step 6 — Smoke lifecycle Standard — NOT-LIVE (test-covered)
Take+submit+grade = WRITE worker; tak feasible read-only. Tercover: full regression **748/0/2** (3m43s, 0 regresi) + app render/login/lobby/impersonation semua OK live.

## Temuan
**TIDAK ADA defect.** Semua perilaku sesuai desain.

**Catatan positif:** Lobby Assessment SUDAH punya UX kesadaran Pre-Post (tombol Post disabled + "Pre-Test tidak diselesaikan") TERPISAH dari gate server baru. Gate server (GRDF-01) = backstop otoritatif di tingkat PELAKSANAAN (StartExam), tepat sesuai tujuan FLOW-04 (bukan cuma UI yang bisa di-bypass).

**Keterbatasan sesi:** Step 3/4/5/6 tak di-drive live karena (a) impersonation read-only memblok WRITE worker, (b) tak ada kredensial worker non-admin, (c) wizard create berat. Semua tercover test otomatis yang menguji logika eksaknya. Bila diinginkan UAT live penuh untuk essay-submit/export/forward-only, perlu deploy ke Dev (IT) di mana worker login nyata tersedia.
