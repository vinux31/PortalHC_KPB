---
phase: 425-cosmetic-naming-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/ManualEntryRules.cs
  - HcPortal.Tests/ManualEntryRulesTests.cs
  - Controllers/TrainingAdminController.cs
  - Views/Admin/AddManualAssessment.cshtml
autonomous: true
requirements: [CLN-02]
migration: false

must_haves:
  truths:
    - "Saat IsPassed tidak selaras dengan (Score >= PassPercentage), HC mendapat PERINGATAN server-side tetapi assessment TETAP tersimpan"
    - "Saat selaras (atau Score null), TIDAK ada peringatan dan tidak ada error"
    - "Cross-validation TIDAK auto-override nilai HC dan TIDAK memblokir submit"
    - "Schedule/CompletedAt tetap selaras (Schedule = model.CompletedAt)"
    - "[Authorize(Roles=\"Admin, HC\")] + [ValidateAntiForgeryToken] pada AddManualAssessment POST tetap utuh"
  artifacts:
    - path: "Helpers/ManualEntryRules.cs"
      provides: "Pure rule PassStatusMismatch(int? score, int passPct, bool isPassed) EF-free"
      contains: "PassStatusMismatch"
    - path: "HcPortal.Tests/ManualEntryRulesTests.cs"
      provides: "Test mismatch→true / match→false / null→false"
      contains: "PassStatusMismatch"
    - path: "Controllers/TrainingAdminController.cs"
      provides: "Warning non-blocking via TempData[Warning] setelah dup-guard, sebelum SaveChanges"
      contains: "TempData[\"Warning\"]"
  key_links:
    - from: "Controllers/TrainingAdminController.cs AddManualAssessment POST"
      to: "Helpers/ManualEntryRules.PassStatusMismatch"
      via: "static helper call setelah ModelState gate + dup-guard, NO return"
      pattern: "ManualEntryRules\\.PassStatusMismatch"
    - from: "Controllers/TrainingAdminController.cs TempData[Warning]"
      to: "Views/Admin/AddManualAssessment.cshtml"
      via: "render TempData warning (Razor auto-encode)"
      pattern: "TempData\\[\"Warning\"\\]"
---

<objective>
CLN-02 — di entry manual (`TrainingAdminController.AddManualAssessment` POST + `CreateManualAssessmentViewModel`): tambah validasi-silang `IsPassed` vs (`Score >= PassPercentage`). Bila mismatch → PERINGATAN server-side (`TempData["Warning"]`) yang TETAP MENYIMPAN (tidak auto-override nilai HC, tidak blokir). Schedule/CompletedAt sudah selaras (`Schedule = model.CompletedAt`) — verifikasi tetap demikian.

Purpose: Menutup FLD-5.2-04/FLD-5.2-05 — HC bisa keliru menandai Lulus saat Score < Pass (atau sebaliknya); warning men-surface inkonsistensi tanpa memaksakan (HC boleh override sengaja untuk entri historis, konsisten filosofi v32.x). Logika cross-validate diekstrak ke pure helper agar testable tanpa DB + reusable.
Output: `ManualEntryRules.cs` (pure rule + test), wiring warning non-blocking di controller, render TempData warning di view. Edit manual (UpdateManualAssessment) di luar scope minimal (Open Question #1) — JANGAN dijadikan blocking requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-CONTEXT.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md

<interfaces>
<!-- Lokasi terverifikasi 2026-06-24 (re-grep WAJIB; line bisa drift). -->

Models/CreateManualAssessmentViewModel.cs (existing):
```csharp
:18  public int? Score { get; set; }                 // NULLABLE — guard .HasValue
:22  public int PassPercentage { get; set; } = 70;
:25  public bool IsPassed { get; set; } = true;
:30  public DateTime CompletedAt { get; set; }
// Score per-submission (1 nilai untuk semua WorkerCerts), bukan per-WorkerCert.
```

Controllers/TrainingAdminController.cs AddManualAssessment POST (existing — verifikasi sesi ini):
```csharp
:686  [HttpPost]
:687  [ValidateAntiForgeryToken]               // ← JANGAN HAPUS (CSRF)
:688  [Authorize(Roles = "Admin, HC")]         // ← JANGAN HAPUS (authz)
:689  public async Task<IActionResult> AddManualAssessment(CreateManualAssessmentViewModel model)
:725  if (!ModelState.IsValid) { ... return View(model); }   // gate blocking existing
:734-743  dup-guard loop (return View on dup)
:747  foreach (var wc in model.WorkerCerts!) { ... }   // build sessions
:762  Schedule = model.CompletedAt,                    // sudah selaras CompletedAt
:783  await _context.SaveChangesAsync();
:798  TempData["Success"] = ...   // ← analog non-blocking (set TempData, lanjut, no return-View)
```

Helpers/CertIssuanceRules.cs (analog pure rule — pola yang DISALIN):
```csharp
public static class CertIssuanceRules {
    public static bool ShouldIssueCertificate(AssessmentSession s) => ...;   // static bool, EF-free
}
```

Konvensi TempData repo: TempData["Success"], TempData["Error"]. Tambah TempData["Warning"] (warning kuning).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): NEW Helpers/ManualEntryRules.cs + ManualEntryRulesTests.cs (CLN-02 pure rule)</name>
  <files>Helpers/ManualEntryRules.cs, HcPortal.Tests/ManualEntryRulesTests.cs</files>
  <read_first>
    - Helpers/CertIssuanceRules.cs (analog pola pure static rule + header XML-doc)
    - Helpers/ExamTimeRules.cs (bentuk minimal)
    - Models/CreateManualAssessmentViewModel.cs (tipe Score=int?, PassPercentage=int, IsPassed=bool)
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md §"ManualEntryRules.cs (NEW)"
  </read_first>
  <behavior>
    - PassStatusMismatch(80, 70, true)  == false   (80>=70 dan IsPassed=true → selaras)
    - PassStatusMismatch(60, 70, false) == false   (60<70 dan IsPassed=false → selaras)
    - PassStatusMismatch(60, 70, true)  == true    (60<70 tapi ditandai Lulus → mismatch)
    - PassStatusMismatch(80, 70, false) == true    (80>=70 tapi ditandai Tidak Lulus → mismatch)
    - PassStatusMismatch(null, 70, true)  == false  (Score null → tidak ada basis → skip, no NRE)
    - PassStatusMismatch(null, 70, false) == false  (Score null → skip)
    - PassStatusMismatch(70, 70, true)  == false   (boundary: Score == Pass dianggap LULUS)
  </behavior>
  <action>
    1) Buat Helpers/ManualEntryRules.cs (pure, EF-free, salin pola header dari CertIssuanceRules.cs):
    ```csharp
    namespace HcPortal.Helpers
    {
        /// <summary>
        /// v32.7 Phase 425 (CLN-02 / FLD-5.2-04, FLD-5.2-05) — cross-validation entry manual.
        /// Pure EF-free supaya bisa di-unit-test tanpa DbContext/controller. Analog
        /// <see cref="CertIssuanceRules"/> (fase 423) &amp; <see cref="ExamTimeRules"/> (fase 424).
        /// </summary>
        public static class ManualEntryRules
        {
            // CLN-02 — true bila status Lulus/Tidak-Lulus TIDAK selaras dgn (Score >= PassPercentage).
            // Score nullable: null => tidak ada basis cross-validate => return false (skip warning, no NRE).
            // Boundary: Score == PassPercentage dianggap LULUS (>=).
            public static bool PassStatusMismatch(int? score, int passPercentage, bool isPassed)
                => score.HasValue && (score.Value >= passPercentage) != isPassed;
        }
    }
    ```

    2) Buat HcPortal.Tests/ManualEntryRulesTests.cs (xUnit, pure, salin pola dari ExamTimeRulesTests.cs):
    ```csharp
    using HcPortal.Helpers;
    using Xunit;

    namespace HcPortal.Tests;

    public class ManualEntryRulesTests
    {
        [Theory]
        [InlineData(80, 70, true, false)]    // selaras lulus
        [InlineData(60, 70, false, false)]   // selaras tidak-lulus
        [InlineData(60, 70, true, true)]     // mismatch: ditandai Lulus, Score<Pass
        [InlineData(80, 70, false, true)]    // mismatch: ditandai Tidak Lulus, Score>=Pass
        [InlineData(70, 70, true, false)]    // boundary == Pass => lulus
        public void PassStatusMismatch_ScoreProvided(int score, int pass, bool isPassed, bool expected) =>
            Assert.Equal(expected, ManualEntryRules.PassStatusMismatch(score, pass, isPassed));

        [Theory]
        [InlineData(true)]
        [InlineData(false)]
        public void PassStatusMismatch_ScoreNull_NeverMismatch(bool isPassed) =>
            Assert.False(ManualEntryRules.PassStatusMismatch(null, 70, isPassed));
    }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ManualEntryRules"</automated>
  </verify>
  <acceptance_criteria>
    - File Helpers/ManualEntryRules.cs ada; `grep -c "public static bool PassStatusMismatch" Helpers/ManualEntryRules.cs` == 1.
    - File HcPortal.Tests/ManualEntryRulesTests.cs ada; `grep -c "PassStatusMismatch_ScoreNull_NeverMismatch" HcPortal.Tests/ManualEntryRulesTests.cs` == 1.
    - Helper EF-free: `grep -c "DbContext\|_context\|ModelState" Helpers/ManualEntryRules.cs` == 0.
    - `dotnet test --filter "FullyQualifiedName~ManualEntryRules"` exit 0 (semua hijau).
  </acceptance_criteria>
  <done>ManualEntryRules.PassStatusMismatch pure & EF-free; test mismatch/match/null/boundary hijau.</done>
</task>

<task type="auto">
  <name>Task 2: Wire warning non-blocking + render di view (CLN-02)</name>
  <files>Controllers/TrainingAdminController.cs, Views/Admin/AddManualAssessment.cshtml</files>
  <read_first>
    - Controllers/TrainingAdminController.cs (re-grep "AddManualAssessment" + "if (!ModelState.IsValid)" + dup-guard loop + "TempData[\"Success\"]" untuk lokasi sisip aktual)
    - Views/Admin/AddManualAssessment.cshtml (cek cara render TempData existing — grep TempData["Success"]/["Error"])
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md §"Pattern 2" + §"Pitfall 2/3" + §"Security Domain"
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md §"TrainingAdminController.cs (MODIFY)"
  </read_first>
  <action>
    Pastikan `using HcPortal.Helpers;` ada di TrainingAdminController.cs (kemungkinan sudah — file pakai CertIssuanceRules).

    1) SISIPKAN warning SETELAH dup-guard loop selesai (setelah blok `foreach (var wc ...) { if (dup) return View; }`, sebelum `var currentUserId = ...` / loop build session). Lokasi ini PASTI lolos ModelState gate + dup-guard, sehingga warning hanya muncul pada submit yang akan benar-benar disimpan. Gunakan helper baru, JANGAN return:
    ```csharp
    // CLN-02 (D-05): cross-validate IsPassed vs (Score >= PassPercentage). Non-blocking — TETAP simpan
    // (HC boleh override sengaja untuk entri historis), TIDAK auto-override nilai, TIDAK blokir.
    // Score nullable: null => skip (PassStatusMismatch return false). Analog TempData["Success"] (non-blocking).
    if (ManualEntryRules.PassStatusMismatch(model.Score, model.PassPercentage, model.IsPassed))
    {
        TempData["Warning"] = model.IsPassed
            ? $"Ditandai Lulus walau Score {model.Score} < Pass {model.PassPercentage}%. Tersimpan apa adanya (override HC)."
            : $"Ditandai Tidak Lulus walau Score {model.Score} >= Pass {model.PassPercentage}%. Tersimpan apa adanya.";
        // TIDAK return — lanjut ke build session + SaveChanges.
    }
    ```
    CATATAN: pesan HANYA memakai nilai numerik (`model.Score`, `model.PassPercentage`) + teks statis. JANGAN echo string input mentah. JANGAN pakai `ModelState.AddModelError` (itu blokir — melanggar D-05). JANGAN sentuh atribut `[ValidateAntiForgeryToken]`/`[Authorize]`.

    2) Verifikasi Schedule/CompletedAt tetap selaras: pastikan `Schedule = model.CompletedAt` (sudah ada di ~:762). Tidak perlu ubah; konfirmasi saja.

    3) Render TempData["Warning"] di Views/Admin/AddManualAssessment.cshtml. Cek apakah view sudah punya blok render TempData["Success"]/["Error"]; bila ya, tambahkan blok warning yang konsisten (alert kuning, Razor auto-encode — JANGAN Html.Raw):
    ```cshtml
    @if (TempData["Warning"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            @TempData["Warning"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    ```
    Bila warning di-render di halaman tujuan redirect (RedirectToAction ke ManageAssessment/AssessmentAdmin tab=training) bukan di AddManualAssessment, render blok warning di view halaman tujuan tersebut. Grep view ManageAssessment untuk blok TempData existing → tempatkan warning di tempat yang sama dengan Success agar pasti tampil pasca-redirect. (TempData survive 1 redirect — konfirmasi alur: POST sukses → RedirectToAction → view tujuan; render di view tujuan.)
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && grep -c "ManualEntryRules.PassStatusMismatch" Controllers/TrainingAdminController.cs && grep -c "TempData\[\"Warning\"\]" Controllers/TrainingAdminController.cs && grep -c "ValidateAntiForgeryToken" Controllers/TrainingAdminController.cs && dotnet build HcPortal.csproj -clp:ErrorsOnly && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ManualEntryRules"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ManualEntryRules.PassStatusMismatch" Controllers/TrainingAdminController.cs` cocok, di dalam AddManualAssessment POST, SETELAH dup-guard.
    - `grep 'TempData\["Warning"\]' Controllers/TrainingAdminController.cs` cocok; TIDAK diikuti `return View` di dalam blok if mismatch (warning non-blocking).
    - TIDAK ada `ModelState.AddModelError` baru di dalam blok cross-validate (grep konteks blok mismatch tidak mengandung AddModelError).
    - Atribut keamanan UTUH: `grep -c "ValidateAntiForgeryToken" Controllers/TrainingAdminController.cs` tidak berkurang; `[Authorize(Roles = "Admin, HC")]` tetap di atas AddManualAssessment POST.
    - `grep "Schedule = model.CompletedAt" Controllers/TrainingAdminController.cs` masih cocok (align terjaga).
    - View tujuan me-render `TempData["Warning"]` (grep `TempData["Warning"]` di view yang relevan).
    - `dotnet build` exit 0; `dotnet test --filter ManualEntryRules` hijau.
  </acceptance_criteria>
  <done>Warning non-blocking ter-wire setelah dup-guard (no return); pesan numerik-only (XSS-safe); CSRF/authz utuh; Schedule align; warning ter-render di view tujuan; build + test hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC browser → AddManualAssessment POST | Untrusted form input (Score/IsPassed/PassPercentage) menyeberang ke controller. Sudah dilindungi [Authorize Admin,HC] + [ValidateAntiForgeryToken] existing. |
| controller → TempData → view | Pesan warning di-render di Razor (auto-encode). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-425-07 | Tampering (CSRF) | AddManualAssessment POST | mitigate | `[ValidateAntiForgeryToken]` (:687) WAJIB tetap utuh — acceptance criteria memverifikasi count tak berkurang. CLN-02 hanya menambah blok di body action, tidak menyentuh atribut. |
| T-425-08 | Elevation (authz bypass) | AddManualAssessment POST | mitigate | `[Authorize(Roles="Admin, HC")]` (:688) WAJIB tetap utuh — acceptance criteria menjaga atribut. |
| T-425-09 | Tampering/Info (XSS via TempData) | TempData["Warning"] render | mitigate | Pesan HANYA berisi nilai numerik (Score, PassPercentage) + teks statis Bahasa Indonesia; TIDAK echo string input mentah. Razor auto-encode; JANGAN Html.Raw. |
| T-425-10 | (Integrity) | Cross-validate logic | mitigate | Server-authoritative cross-validation MEMPERKUAT validasi (net-positif). Non-blocking sesuai D-05 — tidak auto-override; HC tetap berwenang. Logika pure-tested (Task 1). |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exit 0, 0 error baru.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full suite hijau (baseline 748/0/2 + ManualEntryRules tests baru, 0 fail).
- `git diff Controllers/TrainingAdminController.cs` — atribut [ValidateAntiForgeryToken]/[Authorize] tidak tersentuh; tidak ada ModelState.AddModelError baru di jalur cross-validate.
- UAT manual @ http://localhost:5270 (branch ITHandoff; seed sesuai SEED_WORKFLOW bila perlu):
  1. Form AddManualAssessment: isi Score=60, PassPercentage=70, centang Lulus → submit → assessment TERSIMPAN + warning kuning "Ditandai Lulus walau Score 60 < Pass 70%...".
  2. Isi Score=80, Lulus, Pass=70 → submit → TERSIMPAN tanpa warning.
  3. Score dikosongkan (null), Lulus → submit → TERSIMPAN tanpa NRE, tanpa warning spurious.
</verification>

<success_criteria>
- Mismatch IsPassed vs (Score>=Pass) → TempData warning + TETAP simpan (non-blocking, tidak auto-override).
- Match atau Score null → no warning, no error/NRE.
- [ValidateAntiForgeryToken] + [Authorize(Admin,HC)] utuh; Schedule=CompletedAt selaras.
- Warning XSS-safe (numerik-only, Razor encode).
- Build hijau; full suite + ManualEntryRules tests hijau (0 regresi).
- migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-03-SUMMARY.md`
</output>
