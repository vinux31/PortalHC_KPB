---
phase: 425-cosmetic-naming-tech-debt-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/ControllerGuards.cs
  - HcPortal.Tests/ControllerGuardsTests.cs
  - Controllers/AssessmentAdminController.cs
autonomous: true
requirements: [CLN-05]
migration: false

must_haves:
  truths:
    - "Guard-helper static minimal menyeragamkan pembuatan respons JSON gagal (success=false, message)"
    - "Output JSON byte-identik dengan pola Json(new { success=false, message=... }) existing (camelCase: success, message)"
    - "Diterapkan ke SUBSET representatif (cluster SubmitEssayScore), BUKAN sweep semua 23+ call-site"
    - "TANPA mengubah signature action; TANPA introduksi [ApiController]/DTO ber-anotasi"
  artifacts:
    - path: "Helpers/ControllerGuards.cs"
      provides: "Extension static JsonFail(this Controller, string) → JsonResult { success=false, message }"
      contains: "JsonFail"
    - path: "HcPortal.Tests/ControllerGuardsTests.cs"
      provides: "Test shape JSON identik (success=false camelCase + message)"
      contains: "JsonFail"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs SubmitEssayScore"
      to: "Helpers/ControllerGuards.JsonFail"
      via: "this.JsonFail(message) menggantikan Json(new { success=false, message })"
      pattern: "this\\.JsonFail\\("
---

<objective>
CLN-05 — rapikan pola validasi ModelState/guard yang berulang (`return Json(new { success = false, message = ... })`, ~23 call-site di AssessmentAdminController) via guard-helper bersama MINIMAL (helper static), TANPA mengubah signature action dan TANPA refactor DTO. Diterapkan SELEKTIF ke satu cluster representatif (SubmitEssayScore, 5 guard berdekatan) sebagai demonstrasi konvensi — bukan sweep semua.

Purpose: Menutup VAL-07 (konvensi ModelState/guard inkonsisten) dengan permukaan minimal (D-04). Helper membungkus pembuatan objek respons gagal sehingga shape JSON `{ success, message }` byte-identik — konsisten + DRY tanpa risiko regresi frontend JS.
Output: `ControllerGuards.cs` (extension static + test shape), penerapan ke cluster SubmitEssayScore. JSON output WAJIB identik byte-per-byte. Sweep penuh + [ApiController] = OUT OF SCOPE (melanggar D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-CONTEXT.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md
@.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md

<interfaces>
<!-- Cluster terverifikasi 2026-06-24 (re-grep WAJIB; line bisa drift). -->

Controllers/AssessmentAdminController.cs — SubmitEssayScore (cluster target, 5 guard berdekatan):
```csharp
:3684  public async Task<IActionResult> SubmitEssayScore(int sessionId, int questionId, int score)
:3690-3691  if (session == null) return Json(new { success = false, message = "Session tidak ditemukan" });
:3692-3693  if (session.Status != PendingGrading) return Json(new { success = false, message = "Penilaian hanya bisa dilakukan saat status Menunggu Penilaian." });
:3697-3698  if (question == null) return Json(new { success = false, message = "Soal tidak ditemukan" });
:3699-3700  if (score < 0 || score > question.ScoreValue) return Json(new { success = false, message = $"Skor harus antara 0 dan {question.ScoreValue}" });
:3708-3709  if (question.QuestionType != "Essay") return Json(new { success = false, message = "Soal ini bukan tipe Essay." });
:3714-3715  if (!ownsQuestion) return Json(new { success = false, message = "Soal bukan milik sesi ini." });
```
Atribut: [HttpPost] [Authorize(Roles="Admin, HC")] [ValidateAntiForgeryToken] (:3681-3683) — JANGAN sentuh.

JSON config: Program.cs TIDAK punya AddJsonOptions kustom → default System.Text.Json (camelCase). `Json()` (ControllerBase) dan `new JsonResult(...)` keduanya lewat pipeline MVC global yang sama → shape identik: {"success":false,"message":"..."}.

Helpers/CertIssuanceRules.cs (analog pola static EF-free di namespace HcPortal.Helpers).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): NEW Helpers/ControllerGuards.cs + ControllerGuardsTests.cs (CLN-05 shape parity)</name>
  <files>Helpers/ControllerGuards.cs, HcPortal.Tests/ControllerGuardsTests.cs</files>
  <read_first>
    - Helpers/CertIssuanceRules.cs (pola namespace/static EF-free)
    - Program.cs (konfirmasi tidak ada AddJsonOptions kustom → default camelCase)
    - Controllers/AssessmentAdminController.cs:3690-3715 (shape persis Json(new { success=false, message }))
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-PATTERNS.md §"CLN-05 guard-helper"
  </read_first>
  <behavior>
    - JsonFail("X") menghasilkan JsonResult yang, saat diserialisasi, == {"success":false,"message":"X"} (camelCase, urutan success lalu message).
    - JsonResult.Value adalah objek anonim dengan properti success=false dan message=string param.
    - Serialisasi via System.Text.Json default (camelCase, sesuai MVC default) menghasilkan string identik dengan Json(new { success = false, message = "X" }).
  </behavior>
  <action>
    1) Buat Helpers/ControllerGuards.cs — extension method static pada Controller (atau ControllerBase) yang MEMBUNGKUS pembuatan respons gagal. Shape WAJIB identik dengan `Json(new { success = false, message })`:
    ```csharp
    using Microsoft.AspNetCore.Mvc;

    namespace HcPortal.Helpers
    {
        /// <summary>
        /// v32.7 Phase 425 (CLN-05 / VAL-07) — guard-helper MINIMAL untuk menyeragamkan respons JSON gagal.
        /// Membungkus `Json(new { success = false, message })` agar konsisten/DRY TANPA mengubah signature
        /// action atau shape output (byte-identik: {"success":false,"message":"..."} camelCase). D-04: minimal,
        /// tanpa [ApiController]/DTO ber-anotasi. Analog pola static EF-free <see cref="CertIssuanceRules"/>.
        /// </summary>
        public static class ControllerGuards
        {
            // Identik dgn ControllerBase.Json(new { success = false, message }) — lewat JsonResult pipeline MVC.
            public static JsonResult JsonFail(this ControllerBase controller, string message)
                => new JsonResult(new { success = false, message });
        }
    }
    ```

    2) Buat HcPortal.Tests/ControllerGuardsTests.cs — buktikan shape JSON identik. Serialisasi JsonResult.Value dengan opsi camelCase (mirror MVC default) lalu assert string:
    ```csharp
    using System.Text.Json;
    using HcPortal.Helpers;
    using Microsoft.AspNetCore.Mvc;
    using Xunit;

    namespace HcPortal.Tests;

    public class ControllerGuardsTests
    {
        private class DummyController : Controller { }

        [Fact]
        public void JsonFail_ShapeIdenticalToInlinePattern()
        {
            var c = new DummyController();
            var opts = new JsonSerializerOptions { PropertyNamingPolicy = JsonNamingPolicy.CamelCase };

            var helperResult = c.JsonFail("Soal tidak ditemukan");
            var inline = new JsonResult(new { success = false, message = "Soal tidak ditemukan" });

            var helperJson = JsonSerializer.Serialize(helperResult.Value, opts);
            var inlineJson = JsonSerializer.Serialize(inline.Value, opts);

            Assert.Equal(inlineJson, helperJson);
            Assert.Equal("{\"success\":false,\"message\":\"Soal tidak ditemukan\"}", helperJson);
        }
    }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ControllerGuards"</automated>
  </verify>
  <acceptance_criteria>
    - File Helpers/ControllerGuards.cs ada; `grep -c "public static JsonResult JsonFail" Helpers/ControllerGuards.cs` == 1.
    - File HcPortal.Tests/ControllerGuardsTests.cs ada; `grep -c "JsonFail_ShapeIdenticalToInlinePattern" HcPortal.Tests/ControllerGuardsTests.cs` == 1.
    - Helper TIDAK introduksi [ApiController]/DTO: `grep -c "ApiController\|class.*Request\|class.*Dto" Helpers/ControllerGuards.cs` == 0.
    - `dotnet test --filter "FullyQualifiedName~ControllerGuards"` exit 0 (shape parity hijau).
  </acceptance_criteria>
  <done>JsonFail extension menghasilkan shape {"success":false,"message":"..."} identik dgn pola inline; test parity hijau; no [ApiController]/DTO.</done>
</task>

<task type="auto">
  <name>Task 2: Terapkan JsonFail ke cluster SubmitEssayScore (CLN-05 subset representatif)</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs (re-grep "SubmitEssayScore" + 5 baris `return Json(new { success = false, message = ...` di dalamnya — line bisa drift dari :3690-3715)
    - Helpers/ControllerGuards.cs (signature JsonFail dari Task 1)
    - .planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-RESEARCH.md §"Pitfall 5" + §"Open Questions #3"
  </read_first>
  <action>
    Pastikan `using HcPortal.Helpers;` ada di AssessmentAdminController.cs (kemungkinan sudah — file pakai CertIssuanceRules).

    Re-grep `SubmitEssayScore` lalu di dalam action itu SAJA, ganti 5 (lima) guard return dari pola inline ke helper. Shape output WAJIB tetap identik (success=false camelCase + message sama):
    ```csharp
    // SEBELUM:  return Json(new { success = false, message = "Session tidak ditemukan" });
    // SESUDAH:  return this.JsonFail("Session tidak ditemukan");
    ```
    Lakukan untuk kelima pesan di SubmitEssayScore:
    - "Session tidak ditemukan"
    - "Penilaian hanya bisa dilakukan saat status Menunggu Penilaian."
    - "Soal tidak ditemukan"
    - $"Skor harus antara 0 dan {question.ScoreValue}"   (interpolasi tetap — pesan dinamis tetap diteruskan ke JsonFail)
    - "Soal ini bukan tipe Essay."
    - "Soal bukan milik sesi ini."

    BATAS (D-04 minimal): HANYA cluster SubmitEssayScore. JANGAN ubah 17+ call-site lain di file ini (itu sweep, di luar "minimal" + risiko regresi). JANGAN ubah signature action, atribut [HttpPost]/[Authorize]/[ValidateAntiForgeryToken], atau pesan teks (byte-identik). JANGAN ganti `Json(new { success = true, ... })` (jalur sukses) — helper hanya untuk jalur gagal.

    Catatan parity: bila ada keraguan shape, pertahankan `Json(new { success = false, message })` apa adanya untuk guard yang sulit (lebih aman daripada mengubah shape). Output JSON yang dibaca frontend JS (`data.success`, `data.message`) WAJIB tidak berubah.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && test "$(grep -c 'this.JsonFail(' Controllers/AssessmentAdminController.cs)" -ge 5 && grep -c "ValidateAntiForgeryToken" Controllers/AssessmentAdminController.cs && dotnet build HcPortal.csproj -clp:ErrorsOnly && dotnet test HcPortal.Tests/HcPortal.Tests.csproj --filter "FullyQualifiedName~ControllerGuards"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "this.JsonFail(" Controllers/AssessmentAdminController.cs` >= 5 (cluster SubmitEssayScore dikonversi).
    - Kelima/keenam pesan SubmitEssayScore memakai JsonFail dengan teks IDENTIK (grep tiap pesan, mis. `grep "JsonFail(\"Session tidak ditemukan\")"`).
    - Call-site di LUAR SubmitEssayScore TIDAK berubah: total `grep -c "Json(new { success = false" Controllers/AssessmentAdminController.cs` berkurang TEPAT sejumlah yang dikonversi (≈17-18 tersisa, tidak 0).
    - Atribut keamanan SubmitEssayScore utuh: `[HttpPost]`/`[Authorize(Roles="Admin, HC")]`/`[ValidateAntiForgeryToken]` tetap di atas action; signature `SubmitEssayScore(int sessionId, int questionId, int score)` tak berubah.
    - Jalur sukses (`success = true`) TIDAK tersentuh.
    - `dotnet build` exit 0; `dotnet test --filter ControllerGuards` hijau.
  </acceptance_criteria>
  <done>5+ guard SubmitEssayScore memakai this.JsonFail dgn pesan identik; call-site lain tak berubah; atribut keamanan & signature utuh; jalur sukses utuh; build + test hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HC browser → SubmitEssayScore POST (AJAX) | Input (sessionId/questionId/score) menyeberang; dilindungi [Authorize Admin,HC] + [ValidateAntiForgeryToken] existing. Respons JSON dibaca frontend JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-425-11 | Tampering (CSRF) | SubmitEssayScore POST | mitigate | `[ValidateAntiForgeryToken]` (:3683) WAJIB tetap utuh — acceptance criteria menjaga count + posisi. CLN-05 hanya mengganti pembentukan respons di body, tidak menyentuh atribut. |
| T-425-12 | Elevation (authz) | SubmitEssayScore POST | mitigate | `[Authorize(Roles="Admin, HC")]` (:3682) WAJIB utuh — acceptance criteria menjaga atribut. |
| T-425-13 | (Integrity) JSON shape regression | JsonFail vs inline pola | mitigate | Task 1 parity test membuktikan shape byte-identik {"success":false,"message":"..."}. Frontend JS (data.success/data.message) tidak melihat perubahan. Subset minimal (1 cluster) membatasi blast radius. |
| T-425-14 | Information Disclosure | pesan error | accept | Pesan teks IDENTIK dengan existing (tidak menambah info baru). Helper tidak membocorkan detail tambahan. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exit 0, 0 error baru.
- `dotnet test HcPortal.Tests/HcPortal.Tests.csproj` full suite hijau (baseline 748/0/2 + ControllerGuards test, 0 fail).
- `git diff Controllers/AssessmentAdminController.cs` — hanya cluster SubmitEssayScore berubah; atribut [HttpPost]/[Authorize]/[ValidateAntiForgeryToken] tak tersentuh; jalur success=true tak tersentuh; pesan teks identik.
- UAT manual @ http://localhost:5270 (branch ITHandoff): buka EssayGrading, picu salah satu guard (mis. submit skor di luar range pada essay) → respons JSON `{success:false, message:"Skor harus antara 0 dan N"}` muncul identik di UI seperti sebelumnya.
</verification>

<success_criteria>
- ControllerGuards.JsonFail menghasilkan shape JSON byte-identik dengan pola inline.
- Cluster SubmitEssayScore (5-6 guard) memakai this.JsonFail; pesan teks identik.
- Call-site lain (17+) TIDAK disentuh (minimal, D-04).
- Signature action + [HttpPost]/[Authorize]/[ValidateAntiForgeryToken] utuh; jalur success utuh.
- Tidak ada [ApiController]/DTO ber-anotasi baru.
- Build hijau; full suite + ControllerGuards test hijau (0 regresi).
- migration=FALSE.
</success_criteria>

<output>
After completion, create `.planning/phases/425-cosmetic-naming-tech-debt-cleanup/425-04-SUMMARY.md`
</output>
