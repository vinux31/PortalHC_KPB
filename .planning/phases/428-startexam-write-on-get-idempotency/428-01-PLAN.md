---
phase: 428-startexam-write-on-get-idempotency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/CMPController.cs
  - HcPortal.Tests/StartExamIdempotencyTests.cs
autonomous: true
requirements: [EXSEC-02]
migration: false

must_haves:
  truths:
    - "GET StartExam tidak lagi mem-persist transisi status Upcoming->Open (tidak ada SaveChangesAsync untuk transisi status itu di jalur GET)."
    - "Sesi Upcoming yang waktunya SUDAH tiba diperlakukan effective-open (lolos time-gate) tanpa menulis DB."
    - "Sesi Upcoming yang BELUM waktunya tetap diblok (redirect Assessment, Status tetap Upcoming)."
    - "GRDF-01 (Post butuh Pre Completed) tetap memblok di GET."
    - "Token-gate EXSEC-01 (427) tetap memblok bila TokenVerifiedAt==null & StartedAt==null & IsTokenRequired."
    - "Worker asli (non-impersonate) yang mulai ujian waktu-tiba -> Status jadi InProgress + StartedAt ter-set + assignment ter-create (exam-taking utuh)."
  artifacts:
    - path: "Controllers/CMPController.cs"
      provides: "StartExam GET effective-status in-memory (blok persist Upcoming->Open dihapus, time-gate diubah Schedule > nowWib)"
      contains: "Schedule > nowWib"
    - path: "HcPortal.Tests/StartExamIdempotencyTests.cs"
      provides: "6 test integrasi real-SQL membuktikan idempotensi GET + regresi gate"
      contains: "[Fact]"
  key_links:
    - from: "Controllers/CMPController.cs StartExam time-gate"
      to: "assessment.Schedule / nowWib"
      via: "in-memory effective-status (NO SaveChangesAsync)"
      pattern: "Status == \"Upcoming\" && assessment.Schedule > nowWib"
    - from: "HcPortal.Tests/StartExamIdempotencyTests.cs impersonation path"
      to: "CMPController.StartExam GET"
      via: "session Impersonate_Mode=user + reload DB Status assert"
      pattern: "ImpersonationKeys.Mode"
---

<objective>
Jadikan GET `CMP/StartExam(id)` idempoten untuk transisi status `Upcoming->Open` (EXSEC-02). Hapus blok persist yang menulis `Status="Open"` + `SaveChangesAsync` saat GET (CMPController.cs:922-932). Ganti dengan **effective-status in-memory by-schedule**: time-gate hanya memblok bila sesi benar-benar belum waktunya (`Status=="Upcoming" && Schedule > nowWib`). Sesi Upcoming yang waktunya sudah tiba diperlakukan openable (lolos gate) TANPA menulis DB. Lalu tulis 6 test integrasi real-SQL yang membuktikan idempotensi + regresi seluruh gate.

Purpose: GET yang melakukan side-effect mutasi status (write-on-GET, FLOW-10 / backlog 999.14) melanggar idempotensi GET dan menimbulkan side-effect tak terduga (mutasi DB pada navigasi/refresh/impersonate). EXSEC-02 menutup tech-debt ini.

Output: `Controllers/CMPController.cs` (StartExam GET surgical edit), `HcPortal.Tests/StartExamIdempotencyTests.cs` (6 [Fact] real-SQL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/428-startexam-write-on-get-idempotency/428-CONTEXT.md
@.planning/phases/428-startexam-write-on-get-idempotency/428-RESEARCH.md
@.planning/phases/427-exam-token-gate-server-authoritative/427-01-SUMMARY.md

# Lokus implementasi
@Controllers/CMPController.cs

# Factory test real-SQL yang DI-REUSE (salin pola FakeUserStore/MakeUserManager/StubSession/MakeCmp/Seed*)
@HcPortal.Tests/TokenVerifiedAtTests.cs

# Helper pairing GRDF-01 (untuk T4)
@Helpers/PrePostPairing.cs

<interfaces>
<!-- Kontrak yang dibutuhkan executor — sudah diekstrak dari codebase. Tidak perlu eksplorasi tambahan. -->

ImpersonationKeys (Services/ImpersonationService.cs):
```csharp
public static class ImpersonationKeys
{
    public const string Mode = "Impersonate_Mode";            // "user" | "role"
    public const string TargetRole = "Impersonate_TargetRole";
    public const string TargetUserId = "Impersonate_TargetUserId";
    public const string TargetUserName = "Impersonate_TargetUserName";
    public const string StartedAt = "Impersonate_StartedAt";  // DateTime.UtcNow.Ticks.ToString()
}
// IsImpersonating() => Session.GetString(ImpersonationKeys.Mode) != null
// IsExpired() true bila StartedAt absen/usang > 30 menit -> WAJIB set StartedAt = now (Ticks) agar effective user resolve.
```

GetCurrentUserRoleLevelAsync (CMPController.cs:2601) — resolusi effective user:
```csharp
// mode "user" (TargetUser): effUser = FindByIdAsync(TargetUserId) lewat FakeUserStore.
//   -> owner-check StartExam (assessment.UserId == user.Id) pakai effUser.
//   -> SYARAT TEST: tambahkan OWNER ke FakeUserStore + set session keys (Mode=user, TargetUserId=owner.Id,
//      TargetUserName, StartedAt=now-Ticks). Tanpa StartedAt valid -> IsExpired() true -> UseRealUser (impersonate gagal).
// non-impersonate (Mode absen): UseRealUser -> GetUserAsync(User) (NameIdentifier claim) -> owner.
```

PrePostPairing.FindPairedPreAsync (Helpers/PrePostPairing.cs:20) — VERIFIED signature untuk T4:
```csharp
public static async Task<AssessmentSession?> FindPairedPreAsync(ApplicationDbContext ctx, AssessmentSession post)
// post.AssessmentType != "PostTest"      -> return null (Standard/Pre = pass-through, gate tak aktif)
// post.LinkedSessionId.HasValue          -> Pre = Id==LinkedSessionId && UserId==post.UserId && AssessmentType=="PreTest"  (kanonik)
// post.LinkedGroupId.HasValue            -> Pre = LinkedGroupId==post.LinkedGroupId && UserId==post.UserId && AssessmentType=="PreTest" (fallback)
// no link                                -> null (orphan, pass-through)
// GRDF-01 di StartExam (:953): pairedPre != null && pairedPre.Status != "Completed" -> blok.
```

StartExam GET gate ORDER (R-1 WAJIB dipertahankan — CMPController.cs):
```
1. assessment load + null-check + owner/role authz (:911-920)
2. [EDIT DI SINI] effective-status time-gate (ganti :922-939)
3. Completed check (:941-945)
4. GRDF-01 Post-butuh-Pre-Completed (:950-959)   <- owner-only
5. token-gate EXSEC-01 (:964-972)                <- IsTokenRequired && UserId==user.Id && StartedAt==null && TokenVerifiedAt==null
6. exam-window close (:975-979)
7. duration > 0 (:982-986)
8. abandoned block (:989-993)
9. justStarted InProgress write (:1021-1026)     <- TETAP (D-01, out of scope), guard !IsImpersonating()
10. assignment-create (:1106-1123)               <- TETAP (D-01, out of scope), guard !IsImpersonating()
```

AssessmentSession field yang relevan (Models): `Status` (string), `Schedule` (DateTime), `StartedAt` (DateTime?),
`TokenVerifiedAt` (DateTime?), `IsTokenRequired` (bool), `AccessToken` (string?), `DurationMinutes` (int),
`AssessmentType` (string? — null=Standard, "PreTest"/"PostTest"), `LinkedSessionId` (int?), `LinkedGroupId` (int?), `UpdatedAt` (DateTime).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor StartExam GET ke effective-status in-memory (hapus persist Upcoming->Open)</name>
  <files>Controllers/CMPController.cs</files>
  <read_first>
    - Controllers/CMPController.cs:908-1135 (method StartExam — lokus edit :922-939; baca KONTEKS gate berurutan agar urutan R-1 terjaga)
    - Controllers/CMPController.cs:245-251 (pola in-memory effective-status lobby Assessment yang DI-MIRROR)
    - .planning/phases/428-startexam-write-on-get-idempotency/428-RESEARCH.md §1 (before/after code persis) + §2 (konsekuensi perilaku) + §5 (regression)
    - .planning/phases/427-exam-token-gate-server-authoritative/427-01-SUMMARY.md (token-gate EXSEC-01 :964-972 WAJIB utuh)
  </read_first>
  <behavior>
    - Sesi Upcoming + Schedule masa-depan (> nowWib) -> time-gate blok (redirect "Assessment", TempData Error "Ujian belum dibuka..."), Status DB TIDAK berubah.
    - Sesi Upcoming + Schedule lampau (<= nowWib) -> lolos time-gate (effective-open), Status DB TIDAK berubah (no SaveChangesAsync pra-start).
    - Sesi Open -> lolos (Schedule tak relevan untuk time-gate karena Status != "Upcoming").
    - Worker asli yang lolos semua gate -> justStarted write tetap men-set Status=InProgress + StartedAt (alur start utuh).
    - Impersonate -> tidak ada write apa pun (persist-Open dihapus; InProgress/assignment write tetap di-guard !IsImpersonating()).
  </behavior>
  <action>
GANTI blok CMPController.cs:922-939 (auto-transition persist + time-gate lama) dengan effective-status in-memory.

HAPUS persis blok ini (saat ini :922-932):
```csharp
            // Auto-transition: Upcoming → Open when scheduled date+time has arrived in WIB (persisted to DB)
            if (assessment.Status == "Upcoming" && assessment.Schedule <= DateTime.UtcNow.AddHours(7))
            {
                // Phase 377 (Pitfall 3 / T-377-09): write-on-GET guard — JANGAN tulis DB saat impersonasi (read-only invariant).
                if (!_impersonationService.IsImpersonating())
                {
                    assessment.Status = "Open";
                    assessment.UpdatedAt = DateTime.UtcNow;
                    await _context.SaveChangesAsync();
                }
            }

            // Time gate: block access if assessment is still Upcoming (scheduled time not yet reached)
            if (assessment.Status == "Upcoming")
            {
                TempData["Error"] = "Ujian belum dibuka. Silakan kembali setelah waktu ujian dimulai.";
                return RedirectToAction("Assessment");
            }
```

GANTI dengan (D-02, INLINE — JANGAN ekstrak helper; R-1 minimal):
```csharp
            // EXSEC-02: effective-status by-schedule — GET idempoten, TIDAK persist transisi Upcoming->Open.
            // Mirror pola lobby Assessment (:245-251, display-only). Transisi status aktual hanya saat
            // worker mulai (justStarted -> InProgress write di bawah).
            var nowWib = DateTime.UtcNow.AddHours(7);

            // Time gate: blok HANYA bila benar-benar belum waktunya (Upcoming & jadwal di masa depan).
            // Upcoming yang waktunya SUDAH tiba -> diperlakukan openable (tak diblok), tanpa menulis DB.
            if (assessment.Status == "Upcoming" && assessment.Schedule > nowWib)
            {
                TempData["Error"] = "Ujian belum dibuka. Silakan kembali setelah waktu ujian dimulai.";
                return RedirectToAction("Assessment");
            }
```

CATATAN PENTING:
- JANGAN ubah blok lain. Urutan gate setelah ini WAJIB tetap: Completed (:941) -> GRDF-01 (:950) -> token-gate EXSEC-01 (:964) -> exam-window (:975) -> duration (:982) -> abandoned (:989) -> justStarted InProgress write (:1021) -> assignment-create (:1106).
- JANGAN sentuh justStarted write (:1021-1026) maupun assignment-create (:1106-1123) — D-01 menahan keduanya di GET (out of scope).
- Variabel `nowWib` baru di scope StartExam. Pastikan tak ada duplikat deklarasi `nowWib` lain di method ini (cek sebelum build). Jika ada konsumen `Status` antara blok-dihapus & justStarted, RESEARCH §1 sudah verifikasi semuanya aman (keys off StartedAt/field lain, bukan assessment.Status).
- `assessment.UpdatedAt` tidak lagi di-set di sini (tak ada write) — itu memang yang diinginkan (idempoten).
  </action>
  <acceptance_criteria>
    - Di dalam method StartExam (sekitar :908-1135) TIDAK ada lagi baris `assessment.Status = "Open"`. (Pemakaian "Open" lain di luar StartExam — mis. lobby :250 / monitoring — boleh tetap ada.)
    - `grep -n 'assessment.Schedule > nowWib' Controllers/CMPController.cs` menghasilkan tepat 1 baris (time-gate baru).
    - StartExam GET tidak lagi memanggil `SaveChangesAsync()` untuk transisi Upcoming->Open: blok yang menulis `Status="Open"; ... SaveChangesAsync();` tidak ada lagi. (SaveChangesAsync di justStarted :1025 & assignment-create :1111 TETAP — itu in-scope-D-01.)
    - Gate ordering utuh: urutan kemunculan dalam StartExam = time-gate(`Schedule > nowWib`) sebelum `Status == "Completed"` sebelum GRDF-01 (`Selesaikan Pre-Test dulu`) sebelum token-gate (`membutuhkan token akses`).
    - `dotnet build` (branch ITHandoff) exit 0, tanpa error/duplikat `nowWib`.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>Blok persist Upcoming->Open (:922-932) terhapus; time-gate diganti `Status=="Upcoming" && Schedule > nowWib` (in-memory, no SaveChangesAsync); urutan gate R-1 terjaga; `dotnet build` exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tulis StartExamIdempotencyTests.cs (6 test integrasi real-SQL)</name>
  <files>HcPortal.Tests/StartExamIdempotencyTests.cs</files>
  <read_first>
    - HcPortal.Tests/TokenVerifiedAtTests.cs (SALIN pola: FakeUserStore, StubSession, NullTempDataProvider, MakeUserManager, MakeCmp, StubUrlHelper, SeedUserAsync, SeedPackageAsync — semuanya reusable)
    - .planning/phases/428-startexam-write-on-get-idempotency/428-RESEARCH.md §3 (strategi impersonation = satu-satunya GET non-starting) + §4 (tabel 6 test + assert)
    - Services/ImpersonationService.cs:6-56 (ImpersonationKeys + StartUser semantics — Mode=user, TargetUserId, StartedAt Ticks)
    - Controllers/CMPController.cs:2601-2623 (GetCurrentUserRoleLevelAsync — effective user resolve via FindByIdAsync(TargetUserId))
    - Helpers/PrePostPairing.cs:20-37 (FindPairedPreAsync — pairing GRDF-01 untuk T4)
  </read_first>
  <behavior>
    Enam [Fact] real-SQL (disposable DB @ localhost\SQLEXPRESS via RetakeServiceFixture, MigrateAsync full chain):
    - T1 (SC#1, SC#2) StartExam_Impersonate_TimeArrivedUpcoming_RendersWithoutPersisting: impersonate owner, sesi Upcoming + Schedule lampau (waktu tiba) + IsTokenRequired=false + Duration>0 + paket+soal ada -> ViewResult (bukan Redirect); reload DB -> Status=="Upcoming" (TIDAK berubah); StartedAt==null.
    - T2 (SC#1 idempoten) StartExam_Impersonate_DoubleGet_StatusStaysUpcoming: panggil GET 2x berturut (impersonate, sama seperti T1) -> kedua ViewResult; reload DB setelah panggilan ke-2 -> Status=="Upcoming".
    - T3 (SC#3 time-gate) StartExam_Upcoming_NotYetTime_BlocksAndNoWrite: owner non-impersonate, Status="Upcoming", Schedule masa-depan -> RedirectToActionResult ActionName="Assessment"; reload DB -> Status=="Upcoming".
    - T4 (SC#3 GRDF-01) StartExam_PostTest_PreNotCompleted_Blocks: owner non-impersonate, sesi PostTest linked ke PreTest status != "Completed", waktu tiba -> Redirect "Assessment" dengan TempData Error "Selesaikan Pre-Test dulu sebelum mulai Post-Test."; Status tetap Upcoming.
    - T5 (SC#4) StartExam_Owner_TimeArrived_StartsInProgress: owner non-impersonate, Upcoming waktu tiba, IsTokenRequired=false, paket+soal ada -> ViewResult; reload DB -> Status=="InProgress" && StartedAt != null && assignment ter-create (UserPackageAssignments ada utk sessionId).
    - T6 (regresi 427) StartExam_TokenRequired_NotVerified_Blocks: owner non-impersonate, IsTokenRequired=true, TokenVerifiedAt=null, StartedAt=null, waktu tiba -> Redirect "Assessment" dengan TempData Error "Ujian ini membutuhkan token akses..."; Status tetap Upcoming.
  </behavior>
  <action>
Buat file `HcPortal.Tests/StartExamIdempotencyTests.cs`, namespace `HcPortal.Tests`, class `StartExamIdempotencyTests : IClassFixture<RetakeServiceFixture>`, atribut `[Trait("Category", "Integration")]`. Salin struktur dari `TokenVerifiedAtTests.cs`.

REUSE (salin private helper dari TokenVerifiedAtTests.cs apa adanya): `FakeUserStore`, `StubSession`, `NullTempDataProvider`, `MakeUserManager`, `MakeCmp`, `StubUrlHelper`, `NewCtx()`, `SeedUserAsync`, `SeedPackageAsync`. Untuk seed sesi pakai versi fleksibel di bawah (perlu kontrol Status/Schedule/AssessmentType/LinkedSessionId/IsTokenRequired).

Tambahkan using: `using HcPortal.Services;` (ImpersonationKeys), `using HcPortal.Models;` (AssessmentSession/UserPackageAssignment), + using yang sudah ada di TokenVerifiedAtTests.

PERLUASAN factory untuk impersonation (T1/T2) — buat overload `MakeCmpImpersonating(ctx, signedInAdmin, ownerToImpersonate)`:
```csharp
private static (CMPController ctrl, ITempDataDictionary tempData) MakeCmpImpersonating(
    ApplicationDbContext ctx, ApplicationUser signedInAdmin, ApplicationUser owner)
{
    var store = new FakeUserStore();
    store.Add(signedInAdmin);
    store.Add(owner);                                   // WAJIB: GetEffectiveUserAsync -> FindByIdAsync(owner.Id)
    var userManager = MakeUserManager(store);

    var httpContext = new DefaultHttpContext();
    var session = new StubSession();
    session.SetString(ImpersonationKeys.Mode, "user");                                  // IsImpersonating()=true
    session.SetString(ImpersonationKeys.TargetUserId, owner.Id);                        // effective user = owner
    session.SetString(ImpersonationKeys.TargetUserName, owner.FullName ?? "owner");
    session.SetString(ImpersonationKeys.StartedAt, DateTime.UtcNow.Ticks.ToString());   // !IsExpired() agar tak fallback UseRealUser
    httpContext.Session = session;
    httpContext.User = new ClaimsPrincipal(new ClaimsIdentity(new[]
    {
        new Claim(ClaimTypes.NameIdentifier, signedInAdmin.Id)
    }, "TestAuth"));

    var impersonation = new ImpersonationService(new HttpContextAccessor { HttpContext = httpContext });
    var retakeService = new RetakeService(ctx, new AuditLogService(ctx), new NoOpHubContext(), NullLogger<RetakeService>.Instance);
    #pragma warning disable CS8625
    var ctrl = new CMPController(
        userManager, null!, null!, ctx, null!, null!, null!,
        NullLogger<CMPController>.Instance, null!, new NoOpHubContext(), null!, null!, null!,
        impersonation, retakeService);
    #pragma warning restore CS8625
    var tempData = new TempDataDictionary(httpContext, new NullTempDataProvider());
    ctrl.ControllerContext = new ControllerContext { HttpContext = httpContext };
    ctrl.TempData = tempData;
    ctrl.Url = new StubUrlHelper(ctrl.ControllerContext);
    return (ctrl, tempData);
}
```
(Pakai `MakeCmp` non-impersonate dari TokenVerifiedAtTests untuk T3/T4/T5/T6 — owner = signed-in user.)

SEED sesi fleksibel (perluasan SeedSessionAsync agar bisa atur Status/Schedule/type/token/link):
```csharp
private static async Task<int> SeedSessionAsync(
    ApplicationDbContext ctx, string userId, string title,
    string status, DateTime schedule,
    bool isTokenRequired = false, DateTime? tokenVerifiedAt = null,
    DateTime? startedAt = null, string? assessmentType = null,
    int? linkedSessionId = null, int? linkedGroupId = null)
{
    var s = new AssessmentSession
    {
        UserId = userId, Title = title, Category = "Test", Status = status,
        Schedule = schedule, DurationMinutes = 60,
        IsTokenRequired = isTokenRequired, AccessToken = isTokenRequired ? "ABC23X" : null,
        TokenVerifiedAt = tokenVerifiedAt, StartedAt = startedAt,
        AssessmentType = assessmentType, LinkedSessionId = linkedSessionId, LinkedGroupId = linkedGroupId
    };
    ctx.AssessmentSessions.Add(s);
    await ctx.SaveChangesAsync();
    return s.Id;
}
```
Schedule "waktu tiba" = `new DateTime(2026, 2, 1)` (lampau). Schedule "belum waktunya" = `DateTime.UtcNow.AddHours(7).AddDays(7)` (masa depan vs nowWib).

KONSTANTA pesan error (untuk assert TempData):
```csharp
private const string TimeGateMsg = "Ujian belum dibuka. Silakan kembali setelah waktu ujian dimulai.";
private const string Grdf01Msg   = "Selesaikan Pre-Test dulu sebelum mulai Post-Test.";
private const string TokenMsg    = "Ujian ini membutuhkan token akses. Silakan masukkan token terlebih dahulu.";
```

ASSERT pola:
- ViewResult: `Assert.IsType<ViewResult>(result);`
- Redirect Assessment: `var r = Assert.IsType<RedirectToActionResult>(result); Assert.Equal("Assessment", r.ActionName);`
- Reload DB (ctx fresh): `await using var verify = NewCtx(); var st = await verify.AssessmentSessions.Where(a => a.Id == id).Select(a => new { a.Status, a.StartedAt }).SingleAsync();`
- Assignment ter-create (T5): `var asg = await verify.UserPackageAssignments.AnyAsync(a => a.AssessmentSessionId == id); Assert.True(asg);`
- Error spesifik (T4/T6): `Assert.Equal(<Msg>, tempData["Error"]);`

DETAIL T4 (GRDF-01) — gunakan cabang KANONIK LinkedSessionId (VERIFIED dari Helpers/PrePostPairing.cs:24, paling sedikit field matching, deterministik):
  1. Seed Pre milik owner: `SeedSessionAsync(ctx, owner.Id, "T4Pre", status:"InProgress", schedule:new DateTime(2026,2,1), assessmentType:"PreTest")` -> dapat `preId` (status != "Completed" -> GRDF-01 blok).
  2. Seed Post milik owner: `SeedSessionAsync(ctx, owner.Id, "T4Post", status:"Upcoming", schedule:new DateTime(2026,2,1), assessmentType:"PostTest", linkedSessionId:preId)`.
  3. MakeCmp(ctx, owner) (non-impersonate). GET Post -> FindPairedPreAsync resolve Pre (InProgress != Completed) -> GRDF-01 blok.
  Assert: Redirect "Assessment" + `tempData["Error"] == Grdf01Msg`; reload Post -> Status tetap "Upcoming".

DETAIL T1/T2 (impersonate): owner = SeedUserAsync; admin (signed-in) = SeedUserAsync kedua. Sesi milik owner: `SeedSessionAsync(ctx, owner.Id, "T1", status:"Upcoming", schedule:new DateTime(2026,2,1))` (waktu tiba, token off). Pakai MakeCmpImpersonating(ctx, admin, owner). Paket+soal via SeedPackageAsync (agar capai View(vm)). Assert ViewResult + reload Status=="Upcoming" + StartedAt==null (impersonate -> justStarted write di-skip). T2: panggil `ctrl.StartExam(id)` 2x, assert ViewResult kedua kali + reload Status=="Upcoming".

DETAIL T3: owner non-impersonate, `SeedSessionAsync(ctx, owner.Id, "T3", status:"Upcoming", schedule:DateTime.UtcNow.AddHours(7).AddDays(7))` (masa depan) -> Redirect "Assessment" + reload Status=="Upcoming".

DETAIL T5 (owner start): owner = SeedUserAsync; MakeCmp(ctx, owner) (non-impersonate). `SeedSessionAsync(ctx, owner.Id, "T5", status:"Upcoming", schedule:new DateTime(2026,2,1))` (token off), paket+soal via SeedPackageAsync. Assert ViewResult + reload Status=="InProgress" + StartedAt!=null + `UserPackageAssignments.AnyAsync(a => a.AssessmentSessionId==id)`==true.

DETAIL T6 (token regresi): owner non-impersonate, `SeedSessionAsync(ctx, owner.Id, "T6", status:"Upcoming", schedule:new DateTime(2026,2,1), isTokenRequired:true, tokenVerifiedAt:null, startedAt:null)`. MakeCmp(ctx, owner). GET -> Redirect "Assessment" + `tempData["Error"] == TokenMsg`; reload Status=="Upcoming".
  </action>
  <acceptance_criteria>
    - File `HcPortal.Tests/StartExamIdempotencyTests.cs` ada.
    - `grep -c '\[Fact\]' HcPortal.Tests/StartExamIdempotencyTests.cs` == 6.
    - File berisi nama test: `StartExam_Impersonate_TimeArrivedUpcoming_RendersWithoutPersisting`, `StartExam_Impersonate_DoubleGet_StatusStaysUpcoming`, `StartExam_Upcoming_NotYetTime_BlocksAndNoWrite`, `StartExam_PostTest_PreNotCompleted_Blocks`, `StartExam_Owner_TimeArrived_StartsInProgress`, `StartExam_TokenRequired_NotVerified_Blocks`.
    - File berisi `[Trait("Category", "Integration")]` dan `IClassFixture<RetakeServiceFixture>` dan `ImpersonationKeys.Mode`.
    - `dotnet test --filter "FullyQualifiedName~StartExamIdempotencyTests"` exit 0 (6/6 pass, real-SQL @ SQLEXPRESS).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/PortalHC_KPB-ITHandoff" && dotnet test --filter "FullyQualifiedName~StartExamIdempotencyTests" 2>&1 | tail -15</automated>
  </verify>
  <done>6 [Fact] real-SQL ada dan PASS; membuktikan SC#1-#4 + regresi token-gate 427; idempotensi GET terbukti (double-GET impersonate Status tetap Upcoming).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (worker/admin) -> CMP/StartExam GET | Input tak-tepercaya: `id` sesi + identitas (auth cookie) + state impersonasi (session). GET tidak boleh mengubah state DB sebagai side-effect navigasi. |
| Impersonation (admin/HC view-as-user) -> StartExam | Admin/HC mengakses StartExam untuk monitoring/debug; harus read-only (tak boleh menulis state worker). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-428-01 | Tampering | GET StartExam side-effect mutasi status (write-on-GET Upcoming->Open) | mitigate | Hapus persist Upcoming->Open (T1); transisi dihitung in-memory. GET menjadi idempoten untuk transisi status — navigasi/refresh/prefetch tak lagi mengubah DB. Inti EXSEC-02. |
| T-428-02 | Elevation of Privilege | Impersonation menulis state worker (StartedAt/Status) | accept (sudah dimitigasi existing) | Persist-Open (satu-satunya write tak-terguard di blok lama) DIHAPUS; InProgress write (:1021) & assignment-create (:1106) tetap di-guard `!IsImpersonating()`. Setelah T1, impersonate StartExam 100% read-only. Tidak ada surface baru. |
| T-428-03 | Tampering | Bypass time-gate (akses sesi sebelum waktunya) | mitigate | Time-gate baru `Status=="Upcoming" && Schedule > nowWib` tetap memblok sesi yang belum waktunya (T3 membuktikan; Status tak berubah). Tak ada pelonggaran gating untuk sesi belum-waktunya. |
| T-428-04 | Tampering | Regresi gate token (EXSEC-01) / GRDF-01 saat refactor | mitigate | Edit terkurung di blok :922-939; urutan gate Completed->GRDF-01->token->window->duration->abandoned dipertahankan (R-1). T4 (GRDF-01) + T6 (token-gate) membuktikan tetap memblok pasca-refactor. |
| T-428-05 | Information Disclosure | Effective-open render exam ke impersonator sebelum start | accept | Render exam-skeleton read-only saat impersonate adalah perilaku monitoring yang diinginkan (Admin/HC "may access StartExam for debugging/monitoring"); tidak ada data jawaban worker bocor (skeleton soal, bukan hasil). Konsisten dengan token-gate bypass admin existing. |

Tidak ada threat High/Critical baru. Tidak ada surface jaringan/route/schema baru (migration=FALSE). Perubahan REDUCING side-effect (menghapus write), bukan menambah. ASVS L1: gating akses (V4) terbukti utuh via T3/T4/T6; idempotensi GET (V13/best-practice) dipulihkan via T1/T2. block_on high: tidak ada threat High -> tidak memblok.
</threat_model>

<verification>
- `dotnet build` (branch ITHandoff) exit 0.
- `dotnet test --filter "FullyQualifiedName~StartExamIdempotencyTests"` exit 0 (6/6).
- Full suite sebelum verify-phase: `dotnet test` — tak ada regresi (khususnya TokenVerifiedAtTests 5/5, RetakeExamEndpointTests, PrePostPairingTests/PrePostGatingTests).
- Static (checker): `grep` StartExam method TIDAK mengandung `assessment.Status = "Open"` + tidak ada `SaveChangesAsync` untuk transisi Upcoming->Open; `grep` `assessment.Schedule > nowWib` tepat 1.
- Urutan gate R-1 utuh (urutan kemunculan: time-gate -> Completed -> GRDF-01 -> token-gate).
- migration=FALSE — `dotnet ef migrations list` tak bertambah; tidak ada perubahan schema/snapshot.
</verification>

<success_criteria>
1. GET StartExam tidak lagi mem-persist transisi Upcoming->Open (tidak ada SaveChangesAsync write-on-GET untuk transisi status itu). [EXSEC-02 / SC#1] — dibuktikan T1/T2 (Status DB tetap Upcoming pasca-GET impersonate) + static grep.
2. Transisi Upcoming->Open via jalur in-memory/guarded (idempotensi GET dipulihkan). [EXSEC-02 / SC#2] — dibuktikan T1 (effective-open lolos gate tanpa write) + T2 (double-GET stabil).
3. GRDF-01 + time-gate tetap berfungsi di GET. [EXSEC-02 / SC#3] — dibuktikan T3 (time-gate blok) + T4 (GRDF-01 blok) + T6 (token-gate regresi blok).
4. Worker yang waktunya tiba tetap dapat mulai (exam-taking utuh); impersonation guard tetap read-only. [EXSEC-02 / SC#4] — dibuktikan T5 (owner -> InProgress + StartedAt + assignment) + T1 (impersonate read-only, no write).
</success_criteria>

<output>
After completion, create `.planning/phases/428-startexam-write-on-get-idempotency/428-01-SUMMARY.md`.
</output>
