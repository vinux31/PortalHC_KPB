---
phase: 59-hapus-page-protoncatalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/ProtonCatalogController.cs
  - Views/ProtonCatalog/Index.cshtml
  - Views/ProtonCatalog/_CatalogTree.cshtml
autonomous: false
requirements:
  - CONS-02
must_haves:
  truths:
    - "Navigating to /ProtonCatalog returns 404 — no controller handles the route"
    - "No active view, controller, or layout references a /ProtonCatalog URL"
    - "Navigating to /Admin/ProtonData still works correctly (Phase 51 output untouched)"
    - "dotnet build succeeds with zero errors after deletion"
  artifacts:
    - path: "Controllers/ProtonCatalogController.cs"
      provides: "DELETED — must NOT exist"
    - path: "Views/ProtonCatalog/"
      provides: "DELETED — directory must NOT exist"
  key_links:
    - from: "Views/Admin/Index.cshtml"
      to: "/Admin/ProtonData"
      via: "Silabus & Coaching Guidance card href"
      pattern: "ProtonData"
---

<objective>
Delete ProtonCatalogController.cs and the entire Views/ProtonCatalog/ directory. Phase 51 already migrated all Proton Catalog functionality to /Admin/ProtonData. The controller now contains only 11 redirect-only stub actions — technical debt with no value. Removing it completes the consolidation.

Purpose: Clean up legacy redirect-only controller and orphaned views that have been superseded by Phase 51's ProtonData page.
Output: ProtonCatalogController.cs and Views/ProtonCatalog/ deleted, build passes, no stale references remain.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep for stale references, then delete controller and views directory</name>
  <files>
    Controllers/ProtonCatalogController.cs
    Views/ProtonCatalog/Index.cshtml
    Views/ProtonCatalog/_CatalogTree.cshtml
  </files>
  <action>
    Step 1 — Pre-deletion reference sweep. Run grep across the entire codebase EXCLUDING the files you are about to delete:

    ```bash
    grep -r "ProtonCatalog" C:/Users/Administrator/Desktop/PortalHC_KPB \
      --include="*.cs" --include="*.cshtml" --include="*.json" \
      --exclude-dir=".git" \
      | grep -v "ProtonCatalogController.cs" \
      | grep -v "Views/ProtonCatalog/"
    ```

    Expected result: zero output (no matches outside the deletion targets). If any matches appear — STOP and fix them first before deleting. Note: a match in Models/ProtonViewModels.cs for the class definition is harmless and can be ignored.

    Step 2 — Delete the files using git rm to keep history clean:

    ```bash
    cd C:/Users/Administrator/Desktop/PortalHC_KPB
    git rm "Controllers/ProtonCatalogController.cs"
    git rm -r "Views/ProtonCatalog/"
    ```

    Step 3 — Verify the files are gone:

    ```bash
    test -f "C:/Users/Administrator/Desktop/PortalHC_KPB/Controllers/ProtonCatalogController.cs" && echo "FAIL: controller still exists" || echo "PASS: controller deleted"
    test -d "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/ProtonCatalog" && echo "FAIL: views dir still exists" || echo "PASS: views dir deleted"
    ```

    Do NOT touch:
    - Controllers/ProtonDataController.cs (Phase 51 output — keep as-is)
    - Views/ProtonData/ (Phase 51 output — keep as-is)
    - Views/Admin/Index.cshtml (Phase 70 already updated this — no changes needed)
    - Models/ProtonViewModels.cs (safe to leave; ProtonCatalogViewModel class is unused but harmless)
  </action>
  <verify>
    ```bash
    test -f "C:/Users/Administrator/Desktop/PortalHC_KPB/Controllers/ProtonCatalogController.cs" && echo "FAIL" || echo "PASS: controller deleted"
    test -d "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/ProtonCatalog" && echo "FAIL" || echo "PASS: views dir deleted"
    ```
  </verify>
  <done>ProtonCatalogController.cs does not exist, Views/ProtonCatalog/ directory does not exist, grep finds zero active references to ProtonCatalog outside of the unused ViewModel class definition.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build passes and Admin/ProtonData card is still intact</name>
  <files></files>
  <action>
    Step 1 — Build the project to confirm no compilation errors from the deletion:

    ```bash
    cd C:/Users/Administrator/Desktop/PortalHC_KPB
    dotnet build --configuration Debug 2>&1 | tail -20
    ```

    Expected: "Build succeeded" with 0 errors. If errors appear, they are likely stale using statements or references — fix them.

    Step 2 — Verify Admin/Index.cshtml still has the correct ProtonData card (Phase 70 left this in place):

    ```bash
    grep -n "ProtonData" C:/Users/Administrator/Desktop/PortalHC_KPB/Views/Admin/Index.cshtml
    ```

    Expected: At least one line with "ProtonData" in href/action context — confirming the "Silabus & Coaching Guidance" card points to /Admin/ProtonData.

    Step 3 — Final stale URL reference check across all file types:

    ```bash
    grep -r "/ProtonCatalog" C:/Users/Administrator/Desktop/PortalHC_KPB \
      --include="*.cs" --include="*.cshtml" --include="*.js" --include="*.json" \
      --exclude-dir=".git" --exclude-dir=".planning"
    ```

    Expected: zero output. Any match here is a stale href or fetch URL that must be updated to point to /Admin/ProtonData or removed.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --configuration Debug 2>&1 | grep -E "Build succeeded|error CS"</automated>
  </verify>
  <done>dotnet build completes with 0 errors. Views/Admin/Index.cshtml contains a ProtonData href. No /ProtonCatalog URL references remain in any active .cs, .cshtml, or .js file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke test — verify 404 and ProtonData still works</name>
  <files></files>
  <action>Human verification only — no code changes.</action>
  <verify>Manual navigation test in running app.</verify>
  <done>User confirms /ProtonCatalog returns 404 and /Admin/ProtonData loads correctly.</done>
  <what-built>Deleted ProtonCatalogController.cs and Views/ProtonCatalog/ directory. Build passes. All references to /ProtonCatalog removed.</what-built>
  <how-to-verify>
    1. Start the app: dotnet run from C:/Users/Administrator/Desktop/PortalHC_KPB
    2. Navigate to http://localhost:5000/ProtonCatalog — confirm it returns 404 (expected behavior)
    3. Navigate to Kelola Data hub (Admin/Index) and click "Silabus &amp; Coaching Guidance" — confirm it loads /Admin/ProtonData correctly
    4. Confirm all existing ProtonData functionality (Silabus tree, Coaching Guidance tabs) still works normally
  </how-to-verify>
  <resume-signal>Type "approved" if both checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Run before marking phase complete:

```bash
# 1. Controller gone
test -f "C:/Users/Administrator/Desktop/PortalHC_KPB/Controllers/ProtonCatalogController.cs" && echo "FAIL: controller exists" || echo "PASS"

# 2. Views directory gone
test -d "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/ProtonCatalog" && echo "FAIL: dir exists" || echo "PASS"

# 3. No stale /ProtonCatalog URL references
grep -r "/ProtonCatalog" C:/Users/Administrator/Desktop/PortalHC_KPB --include="*.cs" --include="*.cshtml" --exclude-dir=".git" --exclude-dir=".planning" && echo "FAIL: stale refs" || echo "PASS"

# 4. Build clean
cd C:/Users/Administrator/Desktop/PortalHC_KPB && dotnet build --configuration Debug 2>&1 | grep "error CS" | wc -l

# 5. Admin/Index still has ProtonData card
grep -q "ProtonData" C:/Users/Administrator/Desktop/PortalHC_KPB/Views/Admin/Index.cshtml && echo "PASS: ProtonData card intact" || echo "FAIL: ProtonData card missing"
```
</verification>

<success_criteria>
- ProtonCatalogController.cs does not exist in Controllers/
- Views/ProtonCatalog/ directory does not exist
- dotnet build completes with 0 errors
- grep for /ProtonCatalog across *.cs and *.cshtml returns zero results
- Views/Admin/Index.cshtml retains the "Silabus & Coaching Guidance" card pointing to /Admin/ProtonData
- Navigating to /ProtonCatalog in the running app returns 404
- Navigating to /Admin/ProtonData in the running app loads correctly
</success_criteria>

<output>
After completion, create `.planning/phases/59-hapus-page-protoncatalog/59-01-SUMMARY.md` following the summary template at @C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</output>
