---
phase: 75-placeholder-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Shared/_Layout.cshtml
  - Controllers/BPController.cs
  - Views/BP/Index.cshtml
  - Controllers/HomeController.cs
  - Views/Home/Privacy.cshtml
autonomous: true
requirements:
  - STUB-01
  - STUB-05

must_haves:
  truths:
    - The BP navbar link is gone from the navigation bar — no user can navigate to /BP/Index via the navbar
    - BPController.cs no longer exists on disk
    - Views/BP/Index.cshtml no longer exists on disk
    - HomeController.Privacy() action no longer exists — /Home/Privacy returns 404
    - Views/Home/Privacy.cshtml no longer exists on disk
    - The application builds with 0 errors after all deletions
  artifacts:
    - Views/Shared/_Layout.cshtml modified (BP nav-item removed)
    - Controllers/BPController.cs absent
    - Views/BP/Index.cshtml absent
    - Controllers/HomeController.cs modified (Privacy action removed)
    - Views/Home/Privacy.cshtml absent
  key_links:
    - _Layout.cshtml no longer contains `asp-controller="BP"` — no navbar link routes to BPController
    - HomeController.cs no longer contains `public IActionResult Privacy()` — route does not exist
    - No other .cshtml file references BP or Privacy (grep confirmed: only _Layout.cshtml had the BP link, and only Views/Home/Privacy.cshtml referenced Privacy content)
---

<objective>
Remove all BP module infrastructure (navbar link, controller, view) and delete the Privacy placeholder page (action + view).

Purpose: BP is explicitly not-in-scope for any current milestone. The controller, view, and navbar link are stub placeholders with no backing functionality. The Privacy page is an unmodified ASP.NET template default with no portal-relevant content. Both are dead UI that users can accidentally navigate to.

Output: BP navbar link removed, BPController.cs and Views/BP/Index.cshtml deleted, HomeController.Privacy() action removed, Views/Home/Privacy.cshtml deleted. Build passes.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- BP navbar link to DELETE from Views/Shared/_Layout.cshtml (lines 63-65): -->
```html
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-controller="BP" asp-action="Index">BP</a>
                        </li>
```
This is the only BP reference in any .cshtml file (confirmed by grep). Delete all three lines (the <li>, <a>, and </li>).

<!-- BPController.cs — entire file to DELETE: -->
`Controllers/BPController.cs` is the entire BP controller. Content:
```csharp
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Authorization;

namespace HcPortal.Controllers
{
    [Authorize]
    public class BPController : Controller
    {
        public IActionResult Index()
        {
            return View();
        }
    }
}
```
Delete this file entirely.

<!-- Views/BP/Index.cshtml — entire file to DELETE: -->
`Views/BP/Index.cshtml` is the only file under Views/BP/. Delete it, then remove the now-empty `Views/BP/` directory.

<!-- HomeController.Privacy action to DELETE (lines 274-277): -->
From `Controllers/HomeController.cs`:
```csharp
    public IActionResult Privacy()
    {
        return View();
    }
```
This block appears at approximately lines 274-277, between the GetUpcomingDeadlines closing brace region and the Error action. Delete these 4 lines (the method and its blank separator line if present).

After deletion, HomeController.cs should still contain: Index(), GetTimeBasedGreeting(), GetMandatoryTrainingStatus(), GetRecentActivities(), GetUpcomingDeadlines(), GetTimeAgo(), and Error(). The Error action and its [ResponseCache] attribute must remain untouched.

<!-- Views/Home/Privacy.cshtml — entire file to DELETE: -->
`Views/Home/Privacy.cshtml` contains only:
```html
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>
<p>Use this page to detail your site's privacy policy.</p>
```
Delete this file entirely. `Views/Home/Index.cshtml` must remain untouched.

<!-- Reference audit (grep confirmed): -->
<!-- No .cshtml file outside of the listed targets references BP controller or Privacy action. -->
<!-- _Layout.cshtml has no footer with Privacy link — confirmed by grep. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove BP navbar link and delete BP infrastructure</name>
  <files>
    Views/Shared/_Layout.cshtml
    Controllers/BPController.cs
    Views/BP/Index.cshtml
  </files>
  <action>
Make three changes to remove all BP infrastructure:

**CHANGE 1 — Remove BP navbar link from Views/Shared/_Layout.cshtml:**

Open `Views/Shared/_Layout.cshtml` and delete the three-line BP nav item block (approximately lines 63-65):
```html
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-controller="BP" asp-action="Index">BP</a>
                        </li>
```
After deletion, the CMP nav item and CDP nav item should be followed directly by the `@if (userRole == "Admin" || userRole == "HC")` block for Kelola Data.

**CHANGE 2 — Delete Controllers/BPController.cs:**
```bash
rm "C:/Users/Administrator/Desktop/PortalHC_KPB/Controllers/BPController.cs"
```

**CHANGE 3 — Delete Views/BP/Index.cshtml and Views/BP/ directory:**
```bash
rm "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/BP/Index.cshtml"
rmdir "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/BP"
```

After all three changes, verify:
- `grep -r "asp-controller=\"BP\"" Views/` → no output
- `ls Controllers/BPController.cs` → "No such file"
- `ls Views/BP/` → "No such file or directory"
  </action>
  <verify>
    <automated>dotnet build "C:/Users/Administrator/Desktop/PortalHC_KPB/HcPortal.csproj" --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep -r "asp-controller=\"BP\"" Views/` returns no output (link removed)
    - `Controllers/BPController.cs` does not exist on disk
    - `Views/BP/Index.cshtml` does not exist on disk
    - `dotnet build` passes with 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove HomeController.Privacy action and delete Privacy view</name>
  <files>
    Controllers/HomeController.cs
    Views/Home/Privacy.cshtml
  </files>
  <action>
Make two changes to eliminate the Privacy placeholder page:

**CHANGE 1 — Delete HomeController.Privacy() from Controllers/HomeController.cs:**

Open `Controllers/HomeController.cs` and locate the Privacy action (approximately lines 274-277):
```csharp
    public IActionResult Privacy()
    {
        return View();
    }
```
Delete these 4 lines and any blank line separator immediately above them that separates Privacy from the preceding private method (GetTimeAgo). The Error action with its `[ResponseCache]` attribute must remain intact.

After deletion, the flow in HomeController.cs should be: ... GetTimeAgo() closes → blank line → `[ResponseCache(Duration = 0, ...)]` → `public IActionResult Error()` → closing brace.

**CHANGE 2 — Delete Views/Home/Privacy.cshtml:**
```bash
rm "C:/Users/Administrator/Desktop/PortalHC_KPB/Views/Home/Privacy.cshtml"
```

After deletion:
- `ls Views/Home/` should show only `Index.cshtml` (and any other non-Privacy files)
- `grep -c "Privacy" Controllers/HomeController.cs` should return 0

Note: There is no footer Privacy link in `_Layout.cshtml` (confirmed by grep) — no other changes needed.
  </action>
  <verify>
    <automated>dotnet build "C:/Users/Administrator/Desktop/PortalHC_KPB/HcPortal.csproj" --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep "public IActionResult Privacy" Controllers/HomeController.cs` → no output (action removed)
    - `Views/Home/Privacy.cshtml` does not exist on disk
    - `dotnet build` passes with 0 errors
    - HomeController.cs still contains Index(), Error(), and all private helper methods (GetTimeBasedGreeting, GetMandatoryTrainingStatus, GetRecentActivities, GetUpcomingDeadlines, GetTimeAgo) — only Privacy() removed
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Build passes: `dotnet build HcPortal.csproj --no-restore` exits with 0 errors
2. `grep -r "asp-controller=\"BP\"" Views/` → no output (navbar link removed)
3. `ls Controllers/BPController.cs` → "No such file or directory"
4. `ls Views/BP/Index.cshtml` → "No such file or directory"
5. `grep "public IActionResult Privacy" Controllers/HomeController.cs` → no output
6. `ls Views/Home/Privacy.cshtml` → "No such file or directory"
7. Remaining navbar items: CMP, CDP, and Kelola Data (conditional) — BP gone
8. HomeController.cs still has Index() and Error() — only Privacy() removed
</verification>

<success_criteria>
- STUB-01 satisfied: BP navbar link removed from _Layout.cshtml, BPController.cs deleted, Views/BP/Index.cshtml deleted — clean slate, no BP infrastructure remains
- STUB-05 satisfied: HomeController.Privacy() action removed, Views/Home/Privacy.cshtml deleted — /Home/Privacy returns 404
- Build passes after all changes
</success_criteria>

<output>
After completion, create `.planning/phases/75-placeholder-cleanup/75-01-SUMMARY.md`
</output>
