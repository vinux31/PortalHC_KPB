---
phase: 75-placeholder-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/Index.cshtml
  - Views/Account/Settings.cshtml
autonomous: true
requirements:
  - STUB-02
  - STUB-03
  - STUB-04

must_haves:
  truths:
    - Admin hub Section B no longer shows a "Coaching Session Override" card — only functional cards visible
    - Admin hub Section C no longer shows a "Final Assessment Manager" card — only functional cards visible
    - Settings page "Pengaturan Lainnya" section is gone — no disabled 2FA, Notifikasi, or Bahasa items appear
    - Settings page retains two functional sections (Edit Profil and Ubah Password) — the page itself remains
    - The application builds with 0 errors after all changes
  artifacts:
    - Views/Admin/Index.cshtml modified (two stub cards removed)
    - Views/Account/Settings.cshtml modified (Pengaturan Lainnya section removed)
  key_links:
    - Admin/Index.cshtml Section B: only "Coach-Coachee Mapping" and "Deliverable Progress Override" remain — "Coaching Session Override" card block deleted
    - Admin/Index.cshtml Section C: only "Manage Assessments" remains — "Final Assessment Manager" card block deleted
    - Settings.cshtml: Section 3 "Pengaturan Lainnya" block (2FA + Notifikasi + Bahasa rows + preceding hr) deleted
---

<objective>
Remove two stub cards from the Admin hub and delete the three disabled placeholder items from the Settings page.

Purpose: The "Coaching Session Override" and "Final Assessment Manager" cards in Admin/Index link to `href="#"` — they are inert. The "Pengaturan Lainnya" section in Settings contains only disabled form controls with "Segera Hadir" badges — no functionality exists. These are dead UI that misleads users about available features.

Output: Views/Admin/Index.cshtml trimmed (2 stub cards removed), Views/Account/Settings.cshtml trimmed (3 disabled rows + section header + hr removed). Build passes.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- Admin/Index.cshtml — "Coaching Session Override" card block to DELETE (Section B, lines ~107-120): -->
From `Views/Admin/Index.cshtml`:
```html
    <div class="col-md-4">
        <a href="#" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0 opacity-75">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-chat-text fs-5 text-primary"></i>
                        <span class="fw-bold">Coaching Session Override</span>
                        <span class="badge bg-secondary ms-auto" style="font-size:0.6rem;">Segera</span>
                    </div>
                    <small class="text-muted">Override atau hapus coaching session</small>
                </div>
            </div>
        </a>
    </div>
```
Delete the entire `<div class="col-md-4">` block (from opening `<div class="col-md-4">` to matching `</div>`). This is the third card in Section B's row. After deletion, Section B's row should contain exactly two cards: "Coach-Coachee Mapping" and "Deliverable Progress Override".

<!-- Admin/Index.cshtml — "Final Assessment Manager" card block to DELETE (Section C, lines ~144-157): -->
From `Views/Admin/Index.cshtml`:
```html
    <div class="col-md-4">
        <a href="#" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0 opacity-75">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-clipboard-check fs-5 text-primary"></i>
                        <span class="fw-bold">Final Assessment Manager</span>
                        <span class="badge bg-secondary ms-auto" style="font-size:0.6rem;">Segera</span>
                    </div>
                    <small class="text-muted">Kelola final assessment Proton</small>
                </div>
            </div>
        </a>
    </div>
```
Delete the entire `<div class="col-md-4">` block. After deletion, Section C's row contains exactly one card: "Manage Assessments".

<!-- Settings.cshtml — "Pengaturan Lainnya" section to DELETE (lines ~182-222): -->
From `Views/Account/Settings.cshtml`:
```html
        <hr class="my-4">

        @* --- SECTION 3: Pengaturan Lainnya --- *@

        <p class="text-uppercase fw-bold small text-muted mb-3">Pengaturan Lainnya</p>

        <div class="row mb-3 py-1">
            ...Two-Factor Authentication row (disabled toggle)...
        </div>

        <div class="row mb-3 py-1">
            ...Notifikasi Email row (disabled toggle)...
        </div>

        <div class="row mb-3 py-1">
            ...Bahasa row (disabled select)...
        </div>
```
Delete from the `<hr class="my-4">` separator (the one preceding Section 3) through the closing `</div>` of the last Bahasa row. The file should end with `    </div>` (closing the outer col-md-8 wrapper) followed by `</div>` (closing the row). The two functional sections (Edit Profil and Ubah Password, each with their own `<hr class="my-4">` between them) must remain untouched.

After cleanup, Settings.cshtml structure:
- Section 1: Edit Profil (form + Simpan Profil button)
- `<hr class="my-4">`
- Section 2: Ubah Password (form + Ubah Password button)
- (file ends — no Section 3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove two stub cards from Admin hub</name>
  <files>Views/Admin/Index.cshtml</files>
  <action>
Open `Views/Admin/Index.cshtml` and make two deletions:

**DELETION 1 — "Coaching Session Override" stub card (Section B, third card):**

Locate and delete the entire col-md-4 block for Coaching Session Override:
```html
    <div class="col-md-4">
        <a href="#" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0 opacity-75">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-chat-text fs-5 text-primary"></i>
                        <span class="fw-bold">Coaching Session Override</span>
                        <span class="badge bg-secondary ms-auto" style="font-size:0.6rem;">Segera</span>
                    </div>
                    <small class="text-muted">Override atau hapus coaching session</small>
                </div>
            </div>
        </a>
    </div>
```
After deletion, Section B's `<div class="row g-3 mb-2">` contains exactly two cards: "Coach-Coachee Mapping" and "Deliverable Progress Override".

**DELETION 2 — "Final Assessment Manager" stub card (Section C, second card):**

Locate and delete the entire col-md-4 block for Final Assessment Manager:
```html
    <div class="col-md-4">
        <a href="#" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0 opacity-75">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-clipboard-check fs-5 text-primary"></i>
                        <span class="fw-bold">Final Assessment Manager</span>
                        <span class="badge bg-secondary ms-auto" style="font-size:0.6rem;">Segera</span>
                    </div>
                    <small class="text-muted">Kelola final assessment Proton</small>
                </div>
            </div>
        </a>
    </div>
```
After deletion, Section C's `<div class="row g-3 mb-2">` contains exactly one card: "Manage Assessments".

Verify with grep that neither "Coaching Session Override" nor "Final Assessment Manager" appears in the file after edits.
  </action>
  <verify>
    <automated>dotnet build "C:/Users/Administrator/Desktop/PortalHC_KPB/HcPortal.csproj" --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep "Coaching Session Override" Views/Admin/Index.cshtml` → no output
    - `grep "Final Assessment Manager" Views/Admin/Index.cshtml` → no output
    - `grep "Segera" Views/Admin/Index.cshtml` → no output (both "Segera" badges removed)
    - Section B still has Coach-Coachee Mapping and Deliverable Progress Override cards
    - Section C still has Manage Assessments card
    - `dotnet build` passes with 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove disabled placeholder items from Settings page</name>
  <files>Views/Account/Settings.cshtml</files>
  <action>
Open `Views/Account/Settings.cshtml` and delete the entire "Pengaturan Lainnya" section (Section 3).

**Block to delete** starts at the `<hr class="my-4">` separator after the Ubah Password form's submit button and ends at the closing `</div>` of the Bahasa row. The section spans from approximately line 180 to line 222:

Delete from this line:
```html
        <hr class="my-4">

        @* --- SECTION 3: Pengaturan Lainnya --- *@

        <p class="text-uppercase fw-bold small text-muted mb-3">Pengaturan Lainnya</p>

        <div class="row mb-3 py-1">
            <div class="col-sm-8">
                <span class="text-muted">Two-Factor Authentication</span>
                <span class="badge bg-secondary ms-2" style="font-size: 0.7rem;">Segera Hadir</span>
            </div>
            <div class="col-sm-4 text-end">
                <div class="form-check form-switch d-inline-block">
                    <input class="form-check-input" type="checkbox" disabled />
                </div>
            </div>
        </div>

        <div class="row mb-3 py-1">
            <div class="col-sm-8">
                <span class="text-muted">Notifikasi Email</span>
                <span class="badge bg-secondary ms-2" style="font-size: 0.7rem;">Segera Hadir</span>
            </div>
            <div class="col-sm-4 text-end">
                <div class="form-check form-switch d-inline-block">
                    <input class="form-check-input" type="checkbox" disabled />
                </div>
            </div>
        </div>

        <div class="row mb-3 py-1">
            <div class="col-sm-8">
                <span class="text-muted">Bahasa</span>
                <span class="badge bg-secondary ms-2" style="font-size: 0.7rem;">Segera Hadir</span>
            </div>
            <div class="col-sm-4 text-end">
                <select class="form-select form-select-sm w-auto" disabled>
                    <option selected>Bahasa Indonesia</option>
                </select>
            </div>
        </div>
```
...through the end of the Bahasa `</div>` (the last `</div>` before the outer column-closing `</div>` and row-closing `</div>`).

After deletion, the file must end with:
```html
        </form>

    </div>
</div>
```
(the form closing tag for ChangePassword form, then closing the col-md-8 wrapper, then closing the outer row div)

The remaining two sections — Edit Profil (with form and `<hr class="my-4">` after it) and Ubah Password (with form) — must remain completely untouched.

Verify that "Two-Factor Authentication", "Notifikasi Email", and "Bahasa" no longer appear in the file.
  </action>
  <verify>
    <automated>dotnet build "C:/Users/Administrator/Desktop/PortalHC_KPB/HcPortal.csproj" --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep "Two-Factor" Views/Account/Settings.cshtml` → no output
    - `grep "Notifikasi" Views/Account/Settings.cshtml` → no output
    - `grep "Bahasa" Views/Account/Settings.cshtml` → no output
    - `grep "Segera Hadir" Views/Account/Settings.cshtml` → no output
    - Settings.cshtml still contains the Edit Profil form (asp-action="EditProfile") and the Ubah Password form (asp-action="ChangePassword") — only Section 3 removed
    - `dotnet build` passes with 0 errors
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Build passes: `dotnet build HcPortal.csproj --no-restore` exits with 0 errors
2. `grep "Coaching Session Override" Views/Admin/Index.cshtml` → no output
3. `grep "Final Assessment Manager" Views/Admin/Index.cshtml` → no output
4. `grep "Segera" Views/Admin/Index.cshtml` → no output (all "Segera" badges removed from hub)
5. `grep "Two-Factor\|Notifikasi\|Bahasa\|Segera Hadir" Views/Account/Settings.cshtml` → no output
6. Admin hub: Section A has 4 cards, Section B has 2 cards, Section C has 1 card — all functional
7. Settings page: two functional sections remain (Edit Profil + Ubah Password), Section 3 gone
</verification>

<success_criteria>
- STUB-02 satisfied: "Coaching Session Override" stub card removed from Admin hub Section B
- STUB-03 satisfied: "Final Assessment Manager" stub card removed from Admin hub Section C
- STUB-04 satisfied: Disabled 2FA, Notifikasi, and Bahasa items removed from Settings page — only functional controls remain
- Build passes after all changes
</success_criteria>

<output>
After completion, create `.planning/phases/75-placeholder-cleanup/75-02-SUMMARY.md`
</output>
