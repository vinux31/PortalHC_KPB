---
phase: 77-training-record-redirect-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 77-01
files_modified:
  - Views/Admin/ManageAssessment.cshtml
  - Views/Admin/AddTraining.cshtml
  - Views/Admin/EditTraining.cshtml
  - Views/CMP/RecordsWorkerList.cshtml
autonomous: true
requirements:
  - REDIR-01
must_haves:
  truths:
    - "Navigating to /Admin/ManageAssessment shows a 3-tab page: Assessment Groups | Training Records | History"
    - "Training Records tab shows worker list with section/unit filter; default state shows empty list with prompt to filter"
    - "Clicking a worker row expands inline to show their unified training records (assessment+manual) with edit/delete buttons on manual records only"
    - "Tambah Training button navigates to /Admin/AddTraining which has a form for creating a training record"
    - "Edit button navigates to /Admin/EditTraining/{id} which has a pre-populated form"
    - "Delete button submits POST to /Admin/DeleteTraining with confirm dialog"
    - "History tab shows Riwayat Assessment and Riwayat Training sub-tabs"
    - "RecordsWorkerList.cshtml is deleted from disk"
    - "Tab state is preserved via URL query param — ?tab=training activates Training Records tab on load"
  artifacts:
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "3-tab unified page"
      contains: "nav-tabs"
    - path: "Views/Admin/AddTraining.cshtml"
      provides: "Create training record form"
      contains: "CreateTrainingRecordViewModel"
    - path: "Views/Admin/EditTraining.cshtml"
      provides: "Edit training record form"
      contains: "EditTrainingRecordViewModel"
  key_links:
    - from: "Views/Admin/ManageAssessment.cshtml"
      to: "Admin/AddTraining"
      via: "Tambah Training button href"
      pattern: "AddTraining"
    - from: "Views/Admin/ManageAssessment.cshtml"
      to: "Admin/DeleteTraining POST"
      via: "form in expanded row"
      pattern: "DeleteTraining"
    - from: "Views/Admin/ManageAssessment.cshtml"
      to: "ViewBag.ActiveTab"
      via: "JavaScript tab activation on load"
      pattern: "ViewBag.ActiveTab"
---

<objective>
Build the view layer for the unified "Manage Assessment & Training" page.

Purpose: ManageAssessment.cshtml becomes a 3-tab page incorporating assessment groups, training worker list, and history. Dedicated add/edit forms replace the old modal approach. RecordsWorkerList.cshtml is deleted.

Output: Views/Admin/ManageAssessment.cshtml (rewritten), Views/Admin/AddTraining.cshtml (new), Views/Admin/EditTraining.cshtml (new), RecordsWorkerList.cshtml (deleted).
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-training-record-redirect-fix/77-CONTEXT.md
@.planning/phases/77-training-record-redirect-fix/77-01-SUMMARY.md

<interfaces>
<!-- Key ViewBag contracts produced by the Plan 01 controller changes -->

AdminController.ManageAssessment ViewBag (always present):
- ViewBag.ManagementData — IEnumerable<dynamic> of assessment groups (same as before)
- ViewBag.CurrentPage — int
- ViewBag.TotalPages — int
- ViewBag.TotalCount — int
- ViewBag.SearchTerm — string?
- ViewBag.ActiveTab — string: "assessment" | "training" | "history"

AdminController.ManageAssessment ViewBag (present when tab=training or tab=history):
- ViewBag.IsInitialState — bool (true = no filter applied, show empty prompt)
- ViewBag.SelectedSection — string?
- ViewBag.SelectedUnit — string?
- ViewBag.SelectedCategory — string?
- ViewBag.SelectedStatus — string?
- ViewBag.Workers — List&lt;WorkerTrainingStatus&gt;
- ViewBag.AssessmentHistory — List&lt;AllWorkersHistoryRow&gt;
- ViewBag.TrainingHistory — List&lt;AllWorkersHistoryRow&gt;
- ViewBag.AssessmentTitles — List&lt;string&gt;

WorkerTrainingStatus fields (for Training Records tab worker rows):
- WorkerId, WorkerName, NIP, Position, Section, Unit
- TotalTrainings, CompletedTrainings, PendingTrainings, ExpiringSoonTrainings
- TrainingRecords — List&lt;TrainingRecord&gt; (for expanded row)
- CompletionDisplayText — computed string

TrainingRecord fields (for expanded row):
- Id, UserId, Judul, Penyelenggara, Kota, Kategori, Tanggal, Status
- NomorSertifikat, ValidUntil, CertificateType, SertifikatUrl

AllWorkersHistoryRow fields (for History tab):
- WorkerName, WorkerNIP, RecordType ("Manual" | "Assessment Online")
- Title, Date, Penyelenggara, Score, IsPassed, AttemptNumber

Tab activation via URL:
- /Admin/ManageAssessment → assessment tab active (default)
- /Admin/ManageAssessment?tab=training → training tab active
- /Admin/ManageAssessment?tab=history → history tab active

Filter form action for Training tab:
- GET /Admin/ManageAssessment with tab=training + section/unit/isFiltered params

AddTraining form posts to: POST /Admin/AddTraining
EditTraining form posts to: POST /Admin/EditTraining
DeleteTraining form posts to: POST /Admin/DeleteTraining (params: id=int)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ManageAssessment.cshtml as 3-tab page</name>
  <files>Views/Admin/ManageAssessment.cshtml</files>
  <action>
Completely rewrite Views/Admin/ManageAssessment.cshtml. The page title becomes "Manage Assessment & Training". The existing assessment table content moves into Tab 1. Two new tabs are added.

**Page structure:**

```razor
@{
    ViewData["Title"] = "Manage Assessment & Training";
    ViewData["ContainerClass"] = "container-fluid";

    var managementData = ViewBag.ManagementData as IEnumerable<dynamic> ?? Enumerable.Empty<dynamic>();
    var currentPage = (int)(ViewBag.CurrentPage ?? 1);
    var totalPages = (int)(ViewBag.TotalPages ?? 1);
    var totalCount = (int)(ViewBag.TotalCount ?? 0);
    var searchTerm = ViewBag.SearchTerm as string;
    var activeTab = ViewBag.ActiveTab as string ?? "assessment";

    var workers = ViewBag.Workers as List<HcPortal.Models.WorkerTrainingStatus> ?? new();
    var assessmentHistory = ViewBag.AssessmentHistory as List<HcPortal.Models.AllWorkersHistoryRow> ?? new();
    var trainingHistory = ViewBag.TrainingHistory as List<HcPortal.Models.AllWorkersHistoryRow> ?? new();
    var assessmentTitles = ViewBag.AssessmentTitles as List<string> ?? new();
    bool isInitialState = (bool)(ViewBag.IsInitialState ?? true);
}
```

**Breadcrumb:**
Admin > Kelola Data > Manage Assessment & Training

**Page header:**
- H2: "Manage Assessment & Training" with bi-sliders icon
- Header buttons (shown only on Assessment tab):
  - "Buat Assessment" (btn-primary, link to Admin/CreateAssessment) — hide on training/history tabs
  - "Audit Log" (btn-outline-secondary, link to Admin/AuditLog)

**TempData flash messages** (same pattern as existing — show success/error alerts):
```razor
@if (TempData["Success"] != null) { ... alert-success ... }
@if (TempData["Error"] != null) { ... alert-danger ... }
```

**Tab navigation (Bootstrap nav-tabs):**
```html
<ul class="nav nav-tabs mb-0" id="manageAssessmentTabs" role="tablist">
  <li class="nav-item">
    <button class="nav-link" id="tab-assessment" data-bs-toggle="tab" data-bs-target="#pane-assessment" type="button">
      <i class="bi bi-sliders me-1"></i> Assessment Groups
    </button>
  </li>
  <li class="nav-item">
    <button class="nav-link" id="tab-training" data-bs-toggle="tab" data-bs-target="#pane-training" type="button">
      <i class="bi bi-people me-1"></i> Training Records
    </button>
  </li>
  <li class="nav-item">
    <button class="nav-link" id="tab-history" data-bs-toggle="tab" data-bs-target="#pane-history" type="button">
      <i class="bi bi-clock-history me-1"></i> History
    </button>
  </li>
</ul>
```

**Tab 1 — Assessment Groups (pane-assessment):**

Move the existing search form, stats badge, empty state, assessment table, pagination, and Regenerate Token JS exactly as they are now. No functional changes. The search form `asp-action` stays "ManageAssessment" with `asp-controller="Admin"`. Add `<input type="hidden" name="tab" value="assessment">` inside the search form so the tab is preserved on search submit.

**Tab 2 — Training Records (pane-training):**

Header row with:
- Left: "Training Records" heading
- Right: `<a href="@Url.Action("AddTraining", "Admin")" class="btn btn-primary"><i class="bi bi-plus-lg me-2"></i>Tambah Training</a>`

Filter section (form GET to ManageAssessment, preserves tab=training):
```html
<form method="get" action="@Url.Action("ManageAssessment", "Admin")" id="filterFormTraining">
    <input type="hidden" name="tab" value="training" />
    <input type="hidden" name="isFiltered" value="true" />
    <!-- Bagian dropdown, Unit dropdown, Status filter dropdown, search input — copy filter pattern from RecordsWorkerList.cshtml -->
</form>
```

Copy the full filter section from `Views/CMP/RecordsWorkerList.cshtml` (section dropdown, unit dropdown, status dropdown, search field, submit/reset buttons). The filter dropdowns match exactly what was in RecordsWorkerList. The filter form action points to `/Admin/ManageAssessment` with `tab=training`.

If `isInitialState == true`:
Show an empty-state card: "Pilih filter untuk menampilkan data pekerja"

If workers.Count == 0 and not isInitialState:
Show "Tidak ada pekerja ditemukan untuk filter ini."

Otherwise: Worker table with columns: No, Nama (avatar initial + name), Nopeg, Jabatan, Unit, Status Training (badge), Aksi (expand button)

For each worker row, implement expand/collapse using Bootstrap collapse (same pattern as the Assessment Groups Peserta collapse). The collapse pane shows the worker's unified training records inline.

**Expanded row content per worker:**

The expanded area shows a table of the worker's TrainingRecords (`worker.TrainingRecords` — already loaded in `WorkerTrainingStatus`). Note: `WorkerTrainingStatus.TrainingRecords` is `List<TrainingRecord>` (Manual records only). The context says to show unified (assessment + training manual), but the Assessment-side records require a separate query from GetUnifiedRecords. For this view, show only the TrainingRecord list (Manual records) in the expanded row — this is what's pre-loaded in TrainingRecords. Add a note comment in the view: "TODO: Assessment records omitted from expanded view — only manual training records shown here."

Columns in expanded table: Tanggal, Tipe (badge: Training Manual=green), Judul, Penyelenggara, Status, Berlaku Sampai, Aksi

For each Training Manual record, show:
- Edit button: link to `/Admin/EditTraining/@record.Id` (btn-sm btn-outline-primary)
- Delete button: POST form to `/Admin/DeleteTraining` with `id=@record.Id`, `onclick="return confirm('Hapus training record ini?')"` (btn-sm btn-outline-danger)

Also show a "Lihat Detail" link to `/Admin/WorkerDetail/@worker.WorkerId` (btn-sm btn-outline-secondary).

**Tab 3 — History (pane-history):**

Copy the History tab structure from `Views/CMP/RecordsWorkerList.cshtml` (Phase 46 history tab). It has two sub-tabs: Riwayat Assessment and Riwayat Training. Use `assessmentHistory` and `trainingHistory` variables (already extracted from ViewBag above). The assessment titles filter dropdown uses `assessmentTitles`.

**JavaScript (in @section Scripts):**

Activate the correct tab on page load based on `activeTab` value:

```javascript
document.addEventListener('DOMContentLoaded', function() {
    var activeTab = '@activeTab';
    var tabId = activeTab === 'training' ? 'tab-training'
              : activeTab === 'history' ? 'tab-history'
              : 'tab-assessment';
    var tabEl = document.getElementById(tabId);
    if (tabEl) {
        new bootstrap.Tab(tabEl).show();
    }
});
```

Also include the existing Regenerate Token JS from the current ManageAssessment.cshtml.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes; ManageAssessment.cshtml has 3 nav-tabs; Assessment Groups tab contains existing table; Training Records tab has filter form + Tambah Training button + worker table; History tab has assessment/training sub-tabs; tab activation script uses ViewBag.ActiveTab</done>
</task>

<task type="auto">
  <name>Task 2: Create AddTraining.cshtml + EditTraining.cshtml + delete RecordsWorkerList.cshtml</name>
  <files>
    Views/Admin/AddTraining.cshtml
    Views/Admin/EditTraining.cshtml
    Views/CMP/RecordsWorkerList.cshtml
  </files>
  <action>
**Part A: Create Views/Admin/AddTraining.cshtml**

Model: `@model HcPortal.Models.CreateTrainingRecordViewModel`

Copy the structure of `Views/CMP/CreateTrainingRecord.cshtml` but with these changes:
- Title: "Tambah Training Record"
- Breadcrumb: Admin > Kelola Data > Manage Assessment & Training > Tambah Training
  - "Manage Assessment & Training" links to `@Url.Action("ManageAssessment", "Admin", new { tab = "training" })`
- Form `asp-action="AddTraining"` `asp-controller="Admin"` `asp-route-id` removed
- Cancel / Kembali button: link to `@Url.Action("ManageAssessment", "Admin", new { tab = "training" })`
- All field inputs/labels are identical to CreateTrainingRecord.cshtml
- Worker dropdown: populated from `ViewBag.Workers` (SelectListItem list) using `<select asp-for="UserId" asp-items="@ViewBag.Workers">` pattern

**Part B: Create Views/Admin/EditTraining.cshtml**

Model: `@model HcPortal.Models.EditTrainingRecordViewModel`

Copy the structure of the edit form fields from `Views/CMP/RecordsWorkerList.cshtml` (the edit modal) but convert to a standalone page:
- Title: "Edit Training Record"
- Breadcrumb: Admin > Kelola Data > Manage Assessment & Training > Edit Training
- Form `asp-action="EditTraining"` `asp-controller="Admin"` method="post" enctype="multipart/form-data"
- Hidden inputs: `<input asp-for="Id" type="hidden" />`, `<input asp-for="WorkerId" type="hidden" />`, `<input asp-for="ExistingSertifikatUrl" type="hidden" />`
- Show worker name as read-only display: "Pekerja: @Model.WorkerName"
- All editable fields: Judul, Penyelenggara, Kota, Kategori, Tanggal, TanggalMulai, TanggalSelesai, Status, NomorSertifikat, ValidUntil, CertificateType, CertificateFile
- If ExistingSertifikatUrl is set, show current certificate download link
- Submit button: "Simpan Perubahan" (btn-primary)
- Cancel button: link to `@Url.Action("ManageAssessment", "Admin", new { tab = "training" })` (btn-outline-secondary)

**Part C: Delete Views/CMP/RecordsWorkerList.cshtml**

Delete the file `Views/CMP/RecordsWorkerList.cshtml` from disk. It is no longer referenced by any controller action (CMPController.Records now always returns View("Records", ...) as simplified in Plan 01 Task 2). The functionality has been merged into ManageAssessment.

Verify deletion: the file must not exist after this step.

Run build after all three parts: `dotnet build` must pass with 0 errors.
  </action>
  <verify>
    <automated>cd /c/Users/Administrator/Desktop/PortalHC_KPB && dotnet build 2>&1 | tail -5 && ls Views/Admin/AddTraining.cshtml Views/Admin/EditTraining.cshtml 2>/dev/null && ([ ! -f Views/CMP/RecordsWorkerList.cshtml ] && echo "RecordsWorkerList deleted OK" || echo "ERROR: RecordsWorkerList still exists")</automated>
  </verify>
  <done>Build passes; AddTraining.cshtml exists in Views/Admin/; EditTraining.cshtml exists in Views/Admin/; RecordsWorkerList.cshtml does not exist in Views/CMP/</done>
</task>

</tasks>

<verification>
1. `dotnet build` passes with 0 errors
2. `/Admin/ManageAssessment` renders (check response is not 500)
3. `/Admin/ManageAssessment?tab=training` renders (training tab active)
4. `/Admin/ManageAssessment?tab=history` renders (history tab active)
5. `/Admin/AddTraining` renders a form (GET)
6. `Views/CMP/RecordsWorkerList.cshtml` does not exist on disk
</verification>

<success_criteria>
- ManageAssessment.cshtml is a 3-tab page with Assessment Groups, Training Records, History
- AddTraining and EditTraining are standalone pages in Views/Admin/
- RecordsWorkerList.cshtml is deleted
- Build passes
- Tab activation works via ?tab= query parameter
</success_criteria>

<output>
After completion, create `.planning/phases/77-training-record-redirect-fix/77-02-SUMMARY.md`
</output>
