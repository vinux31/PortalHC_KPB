---
phase: 80-per-participant-monitoring-detail-hc-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/AssessmentMonitoringDetail.cshtml
autonomous: true
requirements: [MON-03, MON-04]

must_haves:
  truths:
    - "Clicking 'View Detail' on the Assessment Monitoring group list navigates to AssessmentMonitoringDetail, and the page's Back button returns to /Admin/AssessmentMonitoring (not ManageAssessment)"
    - "The breadcrumb on AssessmentMonitoringDetail reads: Kelola Data > Assessment Monitoring > [actual assessment title]"
    - "When a group requires a token (IsTokenRequired == true), a dedicated token card appears near the page header showing the full token value, a copy-to-clipboard button (with brief 'Copied!' feedback), and a Regenerate button — all inline without page reload for regenerate and copy"
    - "When a group does NOT require a token, no token section is visible on the detail page"
    - "All existing per-participant actions (Reset, Force Close, Bulk Close, Close Early, Export, Reshuffle) continue to work correctly — no regressions"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "AssessmentMonitoringDetail action with BackUrl pointing to AssessmentMonitoring, IsTokenRequired and AccessToken populated on model"
      contains: "ViewBag.BackUrl = Url.Action(\"AssessmentMonitoring\", \"Admin\")"
    - path: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      provides: "Updated breadcrumb + token card section with copy and regenerate"
      contains: "Assessment Monitoring"
  key_links:
    - from: "Views/Admin/AssessmentMonitoringDetail.cshtml"
      to: "Controllers/AdminController.cs (RegenerateToken POST)"
      via: "JS fetch to /Admin/RegenerateToken/{id} — same pattern as Phase 79 AssessmentMonitoring.cshtml"
      pattern: "fetch.*RegenerateToken"
    - from: "Controllers/AdminController.cs (AssessmentMonitoringDetail)"
      to: "Models/AssessmentMonitoringViewModel.cs (MonitoringGroupViewModel)"
      via: "IsTokenRequired and AccessToken set from representative session"
      pattern: "IsTokenRequired.*rep|AccessToken.*rep"
---

<objective>
Phase 80 wires the per-participant monitoring detail page (AssessmentMonitoringDetail) into the new Assessment Monitoring navigation flow and adds a Regenerate Token section to the detail view.

Purpose: HC/Admin now have a complete first-class monitoring workspace — the detail page navigates from and back to the dedicated monitoring page (not ManageAssessment), and the token management capability is available inline on the detail page for token-required groups.

Output:
- Controller: BackUrl points to AssessmentMonitoring; model populated with IsTokenRequired + AccessToken from representative session
- View: breadcrumb updated to reflect new nav hierarchy; token card section added near header (copy + regenerate, inline JS)
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-assessment-monitoring-page-group-list/79-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Models/AssessmentMonitoringViewModel.cs:
```csharp
public class MonitoringGroupViewModel
{
    public int RepresentativeId { get; set; }
    public string Title { get; set; } = "";
    public string Category { get; set; } = "";
    public DateTime Schedule { get; set; }
    public string GroupStatus { get; set; } = "";   // "Open", "Upcoming", or "Closed"
    public int TotalCount { get; set; }
    public int CompletedCount { get; set; }
    public int PassedCount { get; set; }
    public bool IsPackageMode { get; set; }
    public int PendingCount { get; set; }
    public bool IsTokenRequired { get; set; }      // Added in Phase 79
    public string AccessToken { get; set; } = "";  // Added in Phase 79
    public List<MonitoringSessionViewModel> Sessions { get; set; } = new();
}
```

From Models/AssessmentSession.cs (entity):
```csharp
public bool IsTokenRequired { get; set; }
public string AccessToken { get; set; } = "";
```

From Controllers/AdminController.cs — AssessmentMonitoringDetail action (lines 1405-1518):
- Currently sets: `ViewBag.BackUrl = Url.Action("ManageAssessment", "Admin");` (line 1500)
- Currently does NOT set IsTokenRequired or AccessToken on the model
- The representative session is accessed via: `sessions.First()` (line 1486)
- `sessions` is the EF list loaded via `.Include(a => a.User)` — IsTokenRequired/AccessToken are on the entity

From Controllers/AdminController.cs — RegenerateToken POST action (line 1199):
```csharp
[HttpPost]
[Authorize(Roles = "Admin, HC")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> RegenerateToken(int id)
// Returns: Json(new { success = true, token = assessment.AccessToken, message = "..." })
// Returns: Json(new { success = false, message = "..." })
```

From Views/Admin/AssessmentMonitoring.cshtml — JS fetch pattern for Regenerate Token (Phase 79):
```javascript
// Reads antiforgery token from: document.querySelector('input[name="__RequestVerificationToken"]').value
// POST to: /Admin/RegenerateToken/{id}
// Response: { success: bool, token: string }
// On success: alert('Token baru: ' + data.token); location.reload();
```

From Views/Admin/AssessmentMonitoringDetail.cshtml — current breadcrumb (lines 60-66):
```html
<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Admin")">Kelola Data</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("ManageAssessment", "Admin")">Manage Assessment &amp; Training</a></li>
        <li class="breadcrumb-item active">Monitoring Detail</li>
    </ol>
</nav>
```

From Views/Admin/AssessmentMonitoringDetail.cshtml — existing antiforgery setup:
```html
<!-- Global antiforgery token for JS-rendered action buttons -->
<form id="antiforgeryForm" style="display:none">@Html.AntiForgeryToken()</form>
```
The token can be read with: `document.querySelector('#antiforgeryForm input[name="__RequestVerificationToken"]').value`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AssessmentMonitoringDetail controller action — BackUrl and model token fields</name>
  <files>Controllers/AdminController.cs</files>
  <action>
In `AdminController.cs`, find the `AssessmentMonitoringDetail` GET action (around line 1500).

**Change 1 — BackUrl:** Replace:
```csharp
ViewBag.BackUrl = Url.Action("ManageAssessment", "Admin");
```
With:
```csharp
ViewBag.BackUrl = Url.Action("AssessmentMonitoring", "Admin");
```

**Change 2 — Populate token fields on model:** In the same action, after the `MonitoringGroupViewModel` object initializer (which ends at the closing `};` for the `model` variable, around line 1498), add the token fields. The representative session is already available as `sessions.First()`. Add:
```csharp
var repSession = sessions.First();
model.IsTokenRequired = repSession.IsTokenRequired;
model.AccessToken = repSession.AccessToken ?? "";
```

NOTE: The Proton Tahun 3 block below already does `var repSession = await _context.AssessmentSessions.FindAsync(model.RepresentativeId);` — this is a different variable scoped inside the `if` block. The addition above is OUTSIDE that `if` block, placed immediately after the model initializer and before the BackUrl line. Name it `repToken` instead of `repSession` to avoid any future confusion (the Proton block uses its own local). Actually, `sessions.First()` is already in memory — just read directly from it rather than a re-fetch:

```csharp
model.IsTokenRequired = sessions.First().IsTokenRequired;
model.AccessToken = sessions.First().AccessToken ?? "";
```

Place these two lines immediately after the `var model = new MonitoringGroupViewModel { ... };` block (after the closing `};`), before the `ViewBag.BackUrl = ...` line.

The final order in this section of the action should be:
```csharp
var model = new MonitoringGroupViewModel
{
    // ... existing properties ...
};

model.IsTokenRequired = sessions.First().IsTokenRequired;
model.AccessToken = sessions.First().AccessToken ?? "";

ViewBag.BackUrl = Url.Action("AssessmentMonitoring", "Admin");
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Build passes with 0 errors
    - `ViewBag.BackUrl` in AssessmentMonitoringDetail action references "AssessmentMonitoring" (grep confirms)
    - `model.IsTokenRequired` and `model.AccessToken` are set from `sessions.First()` before the ViewBag line
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AssessmentMonitoringDetail.cshtml — breadcrumb and token card section</name>
  <files>Views/Admin/AssessmentMonitoringDetail.cshtml</files>
  <action>
**Change 1 — Breadcrumb:** Replace the existing breadcrumb block (lines 59-66):
```html
<!-- Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Admin")">Kelola Data</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("ManageAssessment", "Admin")">Manage Assessment &amp; Training</a></li>
        <li class="breadcrumb-item active">Monitoring Detail</li>
    </ol>
</nav>
```
With:
```html
<!-- Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Admin")">Kelola Data</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("AssessmentMonitoring", "Admin")">Assessment Monitoring</a></li>
        <li class="breadcrumb-item active">@Model.Title</li>
    </ol>
</nav>
```

**Change 2 — Token card section:** Insert a new token card section AFTER the header block (the `<!-- Header -->` div ending with `</div>` around line 85) and BEFORE the `<!-- Summary cards -->` row. Insert at the position between those two sections:

```html
@if (Model.IsTokenRequired)
{
    <!-- Token Card -->
    <div class="card border-0 shadow-sm mb-4" style="border-left: 4px solid #0d6efd !important;">
        <div class="card-body py-3">
            <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
                <div class="d-flex align-items-center gap-2">
                    <i class="bi bi-key-fill text-primary fs-5"></i>
                    <span class="fw-semibold">Access Token:</span>
                    <code id="token-display" class="fs-6 text-primary">@Model.AccessToken</code>
                </div>
                <div class="d-flex gap-2">
                    <button type="button"
                            class="btn btn-outline-secondary btn-sm"
                            id="btn-copy-token"
                            onclick="copyToken()"
                            title="Copy token">
                        <i class="bi bi-clipboard me-1"></i><span id="copy-label">Copy</span>
                    </button>
                    <button type="button"
                            class="btn btn-outline-warning btn-sm"
                            id="btn-regen-token"
                            data-id="@Model.RepresentativeId"
                            onclick="regenToken(this)"
                            title="Regenerate token">
                        <i class="bi bi-arrow-repeat me-1"></i>Regenerate Token
                    </button>
                </div>
            </div>
        </div>
    </div>
}
```

**Change 3 — Token JS:** At the bottom of the file, BEFORE the closing of the file (after the existing `</script>` block for the polling script), add a new script block for the token actions. This should be added inside `@section Scripts { ... }` if one exists; if not, add it directly at the bottom of the file as a standalone `<script>` block:

```html
@if (Model.IsTokenRequired)
{
    <script>
    function copyToken() {
        var tokenEl = document.getElementById('token-display');
        var labelEl = document.getElementById('copy-label');
        if (!tokenEl) return;
        navigator.clipboard.writeText(tokenEl.textContent.trim()).then(function () {
            labelEl.textContent = 'Copied!';
            setTimeout(function () { labelEl.textContent = 'Copy'; }, 2000);
        });
    }

    function regenToken(btn) {
        if (!confirm('Regenerate token untuk grup ini? Token lama tidak akan bisa digunakan.')) return;
        var id = btn.getAttribute('data-id');
        var token = document.querySelector('#antiforgeryForm input[name="__RequestVerificationToken"]').value;
        btn.disabled = true;
        var originalHtml = btn.innerHTML;
        btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span>';

        fetch('/Admin/RegenerateToken/' + id, {
            method: 'POST',
            headers: { 'RequestVerificationToken': token }
        })
        .then(function (r) { return r.json(); })
        .then(function (data) {
            if (data.success) {
                document.getElementById('token-display').textContent = data.token;
                var labelEl = document.getElementById('copy-label');
                if (labelEl) { labelEl.textContent = 'Copy'; }
            } else {
                alert('Error: ' + (data.message || 'Gagal regenerate token.'));
            }
        })
        .catch(function () {
            alert('Terjadi kesalahan saat regenerate token.');
        })
        .finally(function () {
            btn.disabled = false;
            btn.innerHTML = originalHtml;
        });
    }
    </script>
}
```

NOTE on file structure: `AssessmentMonitoringDetail.cshtml` does NOT currently use `@section Scripts { }` — all scripts are inline at the bottom of the file. Add the new token script block at the very end of the file, after the existing polling `</script>` tag. Wrap it in the `@if (Model.IsTokenRequired)` Razor condition to keep it out of non-token pages.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Build passes with 0 errors
    - Breadcrumb second item reads "Assessment Monitoring" linking to AssessmentMonitoring action; third item is `@Model.Title`
    - Token card section renders for token-required groups: key icon, token value in `<code>`, Copy button, Regenerate button
    - Token section is wrapped in `@if (Model.IsTokenRequired)` — absent for non-token groups
    - Copy button shows "Copied!" for 2 seconds then reverts to "Copy"
    - Regenerate calls `/Admin/RegenerateToken/{id}` and updates `#token-display` in-place without reload
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 80 complete: per-participant monitoring detail wired into Assessment Monitoring navigation, with token card section on detail page.
    - Controller: BackUrl = AssessmentMonitoring; model.IsTokenRequired and model.AccessToken populated from sessions.First()
    - View: breadcrumb updated (Kelola Data > Assessment Monitoring > [Title]); token card with copy + regenerate added
  </what-built>
  <how-to-verify>
    Run the app with `dotnet run` and log in as Admin or HC.

    1. Navigate to Kelola Data hub > Assessment Monitoring card (from Phase 79)
    2. Click "View Detail" on any group — verify the detail page loads
    3. Check breadcrumb: should read "Kelola Data > Assessment Monitoring > [assessment title]" — NOT "Manage Assessment & Training"
    4. Click the Back button on the detail page — should return to /Admin/AssessmentMonitoring (not ManageAssessment)
    5. For a TOKEN-REQUIRED group: verify the token card appears near the header showing a key icon, the full token value, a "Copy" button, and a "Regenerate Token" button
    6. Click "Copy" — verify "Copied!" text appears briefly then reverts to "Copy"
    7. Click "Regenerate Token" — confirm dialog appears; click OK — verify the token value in the card updates in-place (no page reload), the old token is replaced
    8. For a NON-TOKEN group: verify no token card appears on the detail page
    9. Verify existing actions still work: Reset, Force Close, Export Results, Close Early (if group is Open)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` passes with 0 errors and 0 warnings related to changed files
- `ViewBag.BackUrl` in `AssessmentMonitoringDetail` action points to `"AssessmentMonitoring"` (grep: `Url.Action("AssessmentMonitoring", "Admin")` in that action)
- `model.IsTokenRequired` and `model.AccessToken` set from `sessions.First()` in the detail action
- Breadcrumb second item links to `AssessmentMonitoring` action
- Token card: `@if (Model.IsTokenRequired)` guard, contains `#token-display`, `#btn-copy-token`, `#btn-regen-token`
- Token JS: `copyToken()` uses `navigator.clipboard`, `regenToken()` POSTs to `/Admin/RegenerateToken/{id}` and updates DOM
</verification>

<success_criteria>
MON-03 satisfied: HC/Admin can navigate from Assessment Monitoring group list to a per-participant detail view that shows real-time progress (all existing functionality from prior phases — polling, countdown, status, score — remains intact).

MON-04 satisfied: All monitoring actions (Reset, Force Close, Force Close All, Close Early, Export Results, Reshuffle, Regenerate Token) are accessible from within the dedicated monitoring page (the detail page). Regenerate Token is now present on the detail page as a dedicated token card, and all POST actions redirect back to AssessmentMonitoringDetail (already correct from prior phases).
</success_criteria>

<output>
After completion, create `.planning/phases/80-per-participant-monitoring-detail-hc-actions/80-01-SUMMARY.md` following the template at `@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md`.
</output>
