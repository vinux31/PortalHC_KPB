---
phase: 81-cleanup-remove-old-entry-points
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Admin/ManageAssessment.cshtml
  - Views/Admin/Index.cshtml
  - Views/Admin/AssessmentMonitoring.cshtml
autonomous: true
requirements:
  - CLN-01
  - CLN-02

must_haves:
  truths:
    - "ManageAssessment Assessment Groups tab dropdown no longer shows a Monitoring item"
    - "Admin/Index.cshtml Section C no longer shows a Training Records card"
    - "AssessmentMonitoring group table fills the viewport vertically"
  artifacts:
    - path: "Views/Admin/ManageAssessment.cshtml"
      provides: "Dropdown without Monitoring item"
      contains: "bi-binoculars me-2 removed from assessment group dropdown"
    - path: "Views/Admin/Index.cshtml"
      provides: "Hub Section C without Training Records card"
    - path: "Views/Admin/AssessmentMonitoring.cshtml"
      provides: "Table container with min-height styling"
  key_links:
    - from: "Views/Admin/ManageAssessment.cshtml dropdown"
      to: "assessment group row actions"
      via: "ul.dropdown-menu"
      pattern: "bi-binoculars.*Monitoring must NOT appear in dropdown"
    - from: "Views/Admin/Index.cshtml Section C"
      to: "hub cards"
      via: "col-md-4 cards"
      pattern: "Training Records card removed; Assessment Monitoring card remains"
---

<objective>
Remove three redundant elements: the Monitoring dropdown item from ManageAssessment, the Training Records hub card from Kelola Data, and add min-height styling to the Assessment Monitoring table.

Purpose: CLN-01 and CLN-02 from v2.7 requirements. Monitoring now lives on the dedicated Assessment Monitoring page (Phases 79/80). Training Records is accessible from ManageAssessment's Training Records tab. Hub cleanup leaves Section C with only Manage Assessment & Training + Assessment Monitoring cards.

Output: Three surgical edits to existing views — no new files, no controller changes.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Monitoring dropdown item from ManageAssessment (CLN-01)</name>
  <files>Views/Admin/ManageAssessment.cshtml</files>
  <action>
    Remove ONLY the "Monitoring" `<li>` block (lines 256-260) from the assessment group dropdown menu in Views/Admin/ManageAssessment.cshtml. The block to remove is:

    ```html
    <li>
        <a class="dropdown-item" href="@Url.Action("AssessmentMonitoringDetail", "Admin", new { title = (string)group.Title, category = (string)group.Category, scheduleDate = ((DateTime)group.Schedule).Date.ToString("yyyy-MM-dd") })">
            <i class="bi bi-binoculars me-2"></i>Monitoring
        </a>
    </li>
    ```

    The remaining dropdown items to KEEP are: Edit, Export Excel, Regenerate Token (conditional on IsTokenRequired), divider, Delete. Do NOT touch any other lines. The dropdown structure, other `<li>` items, and the surrounding `<ul class="dropdown-menu dropdown-menu-end">` remain intact.
  </action>
  <verify>
    <automated>grep -n "bi-binoculars" Views/Admin/ManageAssessment.cshtml && echo "FAIL: binoculars icon still present" || echo "PASS: Monitoring item removed"</automated>
  </verify>
  <done>The ManageAssessment dropdown contains no reference to "Monitoring" or "bi-binoculars" in the per-group dropdown. Edit, Export Excel, Regenerate Token, and Delete remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Training Records hub card from Admin/Index.cshtml (CLN-02) + fix AssessmentMonitoring table height</name>
  <files>Views/Admin/Index.cshtml, Views/Admin/AssessmentMonitoring.cshtml</files>
  <action>
    **Edit 1 — Admin/Index.cshtml:**
    Remove the Training Records card block. The exact block to remove (lines 139-154) is:

    ```html
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("Records", "CMP")" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-journal-check fs-5 text-primary"></i>
                        <span class="fw-bold">Training Records</span>
                    </div>
                    <small class="text-muted">Lihat riwayat training pekerja</small>
                </div>
            </div>
        </a>
    </div>
    }
    ```

    After removal, Section C will have exactly 2 cards: "Manage Assessment & Training" and "Assessment Monitoring". Do NOT remove the Assessment Monitoring card or any other card.

    **Edit 2 — Views/Admin/AssessmentMonitoring.cshtml:**
    The `<div class="table-responsive">` at line 172 currently has no inline style. Add `style="min-height: calc(100vh - 420px); overflow-y: auto;"` to match the ManageAssessment pattern.

    Change from:
    ```html
    <div class="table-responsive">
    ```
    To:
    ```html
    <div class="table-responsive" style="min-height: calc(100vh - 420px); overflow-y: auto;">
    ```
  </action>
  <verify>
    <automated>grep -n "Training Records\|journal-check" Views/Admin/Index.cshtml && echo "FAIL: Training Records card still present" || echo "PASS: Card removed"</automated>
    <automated>grep -n "min-height.*calc.*100vh" Views/Admin/AssessmentMonitoring.cshtml && echo "PASS: min-height applied" || echo "FAIL: min-height missing"</automated>
  </verify>
  <done>Admin/Index.cshtml Section C has 2 cards only (Manage Assessment & Training + Assessment Monitoring). AssessmentMonitoring.cshtml table container has min-height: calc(100vh - 420px); overflow-y: auto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Three surgical view edits: (1) Monitoring item removed from ManageAssessment dropdown, (2) Training Records card removed from Kelola Data hub, (3) AssessmentMonitoring table fills viewport height.</what-built>
  <how-to-verify>
    1. Run `dotnet run` and navigate to `/Admin/Index` (Kelola Data hub)
    2. Section C should show exactly 2 cards: "Manage Assessment &amp; Training" and "Assessment Monitoring" — no "Training Records" card
    3. Navigate to `/Admin/ManageAssessment` (Assessment Groups tab)
    4. Open any group's dropdown (three-dots button) — should show: Edit, Export Excel, Regenerate Token (if token required), Delete. NO "Monitoring" item
    5. Navigate to `/Admin/AssessmentMonitoring`
    6. The group table should fill the screen vertically (scroll appears only when content exceeds viewport)
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 02, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep -n "bi-binoculars" Views/Admin/ManageAssessment.cshtml` returns no results
- `grep -n "Training Records\|journal-check" Views/Admin/Index.cshtml` returns no results
- `grep -n "min-height.*calc.*100vh" Views/Admin/AssessmentMonitoring.cshtml` returns 1 match
- App builds: `dotnet build` exits 0
</verification>

<success_criteria>
- CLN-01 satisfied: ManageAssessment dropdown has no Monitoring item
- CLN-02 satisfied: Kelola Data hub Section C has no Training Records card
- AssessmentMonitoring table is full-height like ManageAssessment table
- No other existing functionality is broken
</success_criteria>

<output>
After completion, create `.planning/phases/81-cleanup-remove-old-entry-points/81-01-SUMMARY.md`
</output>
