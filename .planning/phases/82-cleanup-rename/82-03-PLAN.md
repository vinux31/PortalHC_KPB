---
phase: 82-cleanup-rename
plan: 03
type: execute
wave: 2
depends_on:
  - 82-02
files_modified:
  - Views/Admin/Index.cshtml
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CLN-05
  - CLN-06

must_haves:
  truths:
    - "Admin/HC users see an AuditLog card in the Kelola Data hub"
    - "Worker role does not see the AuditLog card"
    - "Clicking the AuditLog card navigates to /Admin/AuditLog"
    - "The CLN-06 decision (keep Override Silabus & Coaching Guidance tabs) is documented in PROJECT.md"
  artifacts:
    - path: "Views/Admin/Index.cshtml"
      provides: "AuditLog card in Section C, role-gated to Admin/HC"
      contains: "AuditLog"
    - path: ".planning/PROJECT.md"
      provides: "CLN-06 decision documented"
      contains: "Override Silabus"
  key_links:
    - from: "Views/Admin/Index.cshtml AuditLog card"
      to: "AdminController.AuditLog"
      via: "Url.Action('AuditLog', 'Admin')"
---

<objective>
Add an AuditLog card to the Kelola Data hub (Admin/Index.cshtml) in Section C (Assessment & Training), role-gated to Admin and HC only. Then document the CLN-06 decision (Override Silabus & Coaching Guidance tabs retained as-is) in PROJECT.md.

Purpose: Completes the hub navigation and records the deliberate decision to keep the Override tabs.
Output: AuditLog is discoverable from the Kelola Data hub; decision is durably recorded.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/82-cleanup-rename/82-CONTEXT.md

<interfaces>
<!-- Key references extracted from codebase. Use these directly. -->

AdminController.cs — AuditLog action (already exists, no changes needed):
- `public async Task<IActionResult> AuditLog(int page = 1)` at line ~2138
- Route: /Admin/AuditLog
- Authorization: inherits class-level [Authorize]; no per-action role restriction currently
  (card will be role-gated in the view via User.IsInRole)

Views/Admin/Index.cshtml — Section C "Assessment & Training" current end (lines ~130–168):
```html
<hr class="my-4">

@* Section C: Assessment & Training *@
<div class="d-flex align-items-center mb-3">
    <span class="badge bg-success me-2">C</span>
    <h5 class="fw-bold text-success mb-0">Assessment &amp; Training</h5>
</div>
<div class="row g-3 mb-2">
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("ManageAssessment", "Admin")" ...>Manage Assessment & Training</a>
    </div>
    }
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("AssessmentMonitoring", "Admin")" ...>Assessment Monitoring</a>
    </div>
    }
</div>
```
Add the AuditLog card INSIDE the `<div class="row g-3 mb-2">` at the end of Section C (after the AssessmentMonitoring closing `}` block), still inside the row's closing `</div>`.

Existing card pattern to follow exactly (copy structure from AssessmentMonitoring card):
- Outer: `@if (User.IsInRole("Admin") || User.IsInRole("HC"))` wrapper
- `<div class="col-md-4"><a href="..." class="text-decoration-none"><div class="card shadow-sm h-100 border-0"><div class="card-body">...`
- Icon: `bi bi-journal-text` (audit log icon)
- Title: "Audit Log"
- Description: "Lihat riwayat aktivitas pengelolaan assessment oleh Admin dan HC"

Note: Plan 82-02 removes the CPDP Progress Tracking card from this same file. If 82-02 runs first (wave dependency), Admin/Index.cshtml is clean when this plan executes. If running simultaneously, coordinate to avoid conflict — 82-03 depends_on 82-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AuditLog card to Kelola Data hub (Section C)</name>
  <files>Views/Admin/Index.cshtml</files>
  <action>
In Views/Admin/Index.cshtml, add the AuditLog card inside Section C's row div, after the existing AssessmentMonitoring card block.

Find the closing `}` of the AssessmentMonitoring card's `@if` block (after `</div>` that closes the col-md-4 card) and the row's `</div>`, then insert before the row close:

```html
    @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    {
    <div class="col-md-4">
        <a href="@Url.Action("AuditLog", "Admin")" class="text-decoration-none">
            <div class="card shadow-sm h-100 border-0">
                <div class="card-body">
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <i class="bi bi-journal-text fs-5 text-primary"></i>
                        <span class="fw-bold">Audit Log</span>
                    </div>
                    <small class="text-muted">Lihat riwayat aktivitas pengelolaan assessment oleh Admin dan HC</small>
                </div>
            </div>
        </a>
    </div>
    }
```

Place this block immediately before the closing `</div>` of the Section C row (`<div class="row g-3 mb-2">`).
  </action>
  <verify>
    <automated>grep -n "AuditLog\|Audit Log" Views/Admin/Index.cshtml</automated>
  </verify>
  <done>
    - Views/Admin/Index.cshtml contains AuditLog card in Section C
    - Card is wrapped in @if (User.IsInRole("Admin") || User.IsInRole("HC"))
    - href points to Url.Action("AuditLog", "Admin")
  </done>
</task>

<task type="auto">
  <name>Task 2: Document CLN-06 decision in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
In .planning/PROJECT.md, add a new section documenting the CLN-06 architectural decision. Append the following block after the "Current State" section (after the paragraph ending "Admin can manage questions directly from ManageAssessment dropdown. Hub cleaned up — no redundant cards.") and before the "Shipped Milestones" heading:

```markdown
## Architecture Decisions

### CLN-06: Override Silabus & Coaching Guidance Tabs (2026-03-02)

**Decision:** KEEP as-is — no changes.

**Rationale:** The `ProtonData/Index` page with Silabus and Coaching Guidance tabs is fully functional and tested. It serves as the canonical admin interface for managing silabus entries and uploading coaching guidance files. These tabs are used by Plan IDP (Phase 86) and Coaching Proton (Phase 85) as data sources. Removing or restructuring them would break downstream feature flows.

**Alternative considered:** Removing the Override tabs and merging into a simpler flat list. Rejected because the tabbed interface cleanly separates two distinct data types (silabus vs. guidance files) and the current implementation has no known bugs.
```

If an "Architecture Decisions" section already exists in PROJECT.md, append the CLN-06 entry under it instead of creating a new section.
  </action>
  <verify>
    <automated>grep -n "CLN-06\|Override Silabus" .planning/PROJECT.md</automated>
  </verify>
  <done>
    - .planning/PROJECT.md contains CLN-06 decision with rationale
    - Decision clearly states "KEEP as-is" with reasoning documented
  </done>
</task>

</tasks>

<verification>
```bash
# AuditLog card present and role-gated
grep -A5 "AuditLog" Views/Admin/Index.cshtml | head -20

# CLN-06 documented
grep -n "CLN-06" .planning/PROJECT.md

# No Worker access (no unconditional AuditLog link)
# Confirm all AuditLog references are inside @if (User.IsInRole("Admin") || User.IsInRole("HC"))
grep -B3 "AuditLog.*Admin" Views/Admin/Index.cshtml | head -10
```
</verification>

<success_criteria>
1. Admin/Index.cshtml Section C has an "Audit Log" card visible only when User.IsInRole("Admin") || User.IsInRole("HC")
2. Clicking the card navigates to /Admin/AuditLog (existing action)
3. Worker role sees Section C without the AuditLog card
4. PROJECT.md contains CLN-06 entry stating Override Silabus & Coaching Guidance tabs are kept with documented rationale
</success_criteria>

<output>
After completion, create `.planning/phases/82-cleanup-rename/82-03-SUMMARY.md`
</output>
