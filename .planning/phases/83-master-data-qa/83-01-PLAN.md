---
phase: 83-master-data-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/KkjMatrix.cshtml
autonomous: false
requirements: [DATA-01]

must_haves:
  truths:
    - "Admin/HC can add, edit, and delete KKJ Matrix rows via the spreadsheet editor"
    - "Bulk save persists all changed rows atomically and shows a success toast"
    - "Bagian management: add, rename, and delete (with block-delete guard) work correctly"
    - "Changes made in Admin/KkjMatrix appear correctly when filtering the same Bagian in CMP/Kkj"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "KkjMatrix, KkjMatrixSave, KkjMatrixDelete, KkjBagianAdd, KkjBagianDelete, KkjBagianSave actions"
    - path: "Views/Admin/KkjMatrix.cshtml"
      provides: "Spreadsheet editor UI with inline editing and bulk-save flow"
  key_links:
    - from: "Views/Admin/KkjMatrix.cshtml"
      to: "/Admin/KkjMatrixSave"
      via: "fetch POST with JSON body of changed rows"
      pattern: "KkjMatrixSave"
    - from: "Views/Admin/KkjMatrix.cshtml"
      to: "/Admin/KkjBagianAdd"
      via: "fetch POST, no body"
      pattern: "KkjBagianAdd"
    - from: "Admin/KkjMatrix (KkjMatrices table)"
      to: "CMP/Kkj view"
      via: "same KkjMatrices DbSet, same Bagian filter param"
      pattern: "KkjMatrices"
---

<objective>
Review and fix the KKJ Matrix editor (DATA-01) for the Admin/HC role, then present manual verification steps to the user.

Purpose: KKJ Matrix is the foundation for competency tracking. Bugs here corrupt downstream assessments, CMP/Kkj view, and UserCompetencyLevels. A clean QA pass ensures the editor is reliable before coaching and assessment QA phases.

Output: Fixed AdminController.cs (if bugs found) + fixed KkjMatrix.cshtml (if bugs found) + user-verified browser pass
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-master-data-qa/83-CONTEXT.md

<interfaces>
<!-- Key actions in AdminController.cs for KKJ Matrix (lines 48-261) -->

// GET /Admin/KkjMatrix — loads bagians + matrix rows
public async Task<IActionResult> KkjMatrix()
  ViewBag.Bagians = List<KkjBagian>   // ordered by DisplayOrder
  return View(List<KkjMatrixItem>)

// POST /Admin/KkjMatrixSave — bulk upsert
public async Task<IActionResult> KkjMatrixSave([FromBody] List<KkjMatrixItem> rows)
  row.Id == 0 → Add; row.Id > 0 → FindAsync + update fields
  logs AuditLog BulkUpdate
  return Json({ success, message })

// POST /Admin/KkjBagianSave — bulk update bagian labels/names
public async Task<IActionResult> KkjBagianSave([FromBody] List<KkjBagian> bagians)
  return Json({ success, message })

// POST /Admin/KkjBagianAdd — adds new "Bagian Baru" with next DisplayOrder
public async Task<IActionResult> KkjBagianAdd()
  return Json({ success, id, name, displayOrder })

// POST /Admin/KkjBagianDelete — guarded: blocks if KKJ items assigned to bagian
public async Task<IActionResult> KkjBagianDelete(int id)
  checks assignedCount = KkjMatrices.Count(k => k.Bagian == bagian.Name)
  return Json({ success, blocked?, message })

// POST /Admin/KkjMatrixDelete — guarded: blocks if UserCompetencyLevels reference item
public async Task<IActionResult> KkjMatrixDelete(int id)
  checks usageCount = UserCompetencyLevels.Count(u => u.KkjMatrixItemId == id)
  return Json({ success, blocked?, message })

// KkjMatrixItem fields: Id, No, SkillGroup, SubSkillGroup, Indeks, Kompetensi, Bagian,
//   Target_SectionHead, Target_SrSpv_GSH, Target_ShiftSpv_GSH, Target_Panelman_GSH_12_13,
//   Target_Panelman_GSH_14, Target_Operator_GSH_8_11, Target_Operator_GSH_12_13,
//   Target_ShiftSpv_ARU, Target_Panelman_ARU_12_13, Target_Panelman_ARU_14,
//   Target_Operator_ARU_8_11, Target_Operator_ARU_12_13, Target_SrSpv_Facility,
//   Target_JrAnalyst, Target_HSE

// KkjBagian fields: Id, Name, DisplayOrder, Label_SectionHead, Label_SrSpv_GSH,
//   Label_ShiftSpv_GSH, Label_Panelman_GSH_12_13, Label_Panelman_GSH_14,
//   Label_Operator_GSH_8_11, Label_Operator_GSH_12_13, Label_ShiftSpv_ARU,
//   Label_Panelman_ARU_12_13, Label_Panelman_ARU_14, Label_Operator_ARU_8_11,
//   Label_Operator_ARU_12_13, Label_SrSpv_Facility, Label_JrAnalyst, Label_HSE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review and fix KKJ Matrix controller + view</name>
  <files>Controllers/AdminController.cs, Views/Admin/KkjMatrix.cshtml</files>
  <action>
Read AdminController.cs lines 48–261 (KkjMatrix, KkjMatrixSave, KkjBagianSave, KkjBagianAdd, KkjBagianDelete, KkjMatrixDelete) and Views/Admin/KkjMatrix.cshtml in full.

Review for these specific issues:

1. **Null safety on bulk save** — KkjMatrixSave iterates rows without wrapping in a transaction. If an exception occurs mid-loop, partial rows are saved. Fix: wrap the foreach loop in a try/catch that catches per-row errors, accumulates them, and only calls SaveChangesAsync after all rows are validated. Already partially done — confirm the existing catch is at the right level (outer, not per-row).

2. **AuditLog on BulkSave** — verify actor null check is present before calling _auditLog.LogAsync. Already looks correct (if actor != null), but confirm.

3. **KkjBagianDelete block guard** — checks `KkjMatrices.CountAsync(k => k.Bagian == bagian.Name)`. This is a string comparison. Check if bagian.Name could be null and cause a runtime error. If so, add null guard: `if (bagian == null) return Json(...)`.

4. **KkjMatrixDelete audit log** — logged after the item is already removed. Confirm the log captures `item.Kompetensi` before deletion (it does — `var item = await FindAsync(id)` is above). Confirm no bug.

5. **View: CSRF token included in JS fetch calls** — KkjMatrix.cshtml uses fetch to POST to KkjMatrixSave, KkjBagianAdd, KkjBagianDelete, KkjBagianSave. Each must include the antiforgery token header. Check each fetch call for `RequestVerificationToken` header. If any POST fetch is missing it, add: `headers: { 'Content-Type': 'application/json', 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value }`.

6. **View: filter state on CMP/Kkj cross-link** — After saving KKJ Matrix, does the view include a "View in CMP" link that preserves the selected bagian filter? Check if there's a link like `/CMP/Kkj?section=RFCC`. If absent or if the bagian param is not passed, add/fix it so the user can jump to the CMP view with the current bagian pre-filtered.

7. **View: empty state** — If KkjMatrices is empty (no rows), does the view render gracefully or throw a JS error? Verify the JS initialization handles an empty array.

Fix all bugs found (keep changes < 100 lines aggregate). If an issue requires architectural refactoring, flag it in a code comment and document it in the plan summary instead of implementing it.

Run `dotnet build` to verify the code compiles after any changes.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>dotnet build exits 0 with no new errors. All identified bugs are either fixed or documented as architectural flags in the summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code-reviewed and fixed the KKJ Matrix controller actions (KkjMatrixSave, KkjBagianAdd/Delete, KkjMatrixDelete) and the KkjMatrix.cshtml spreadsheet editor. Any bugs found in Task 1 are now fixed.</what-built>
  <how-to-verify>
Log in as Admin or HC user. Navigate to Admin/KkjMatrix (Kelola Data hub > Section A > KKJ Matrix).

Test these flows:
1. **Add a row** — Click "Tambah Baris", fill in SkillGroup and Kompetensi fields, assign to an existing Bagian, click "Simpan Semua". Verify success toast appears and the new row is visible after page reload.
2. **Edit a row** — Click into an existing cell (e.g., Target_SectionHead), change the value, click "Simpan Semua". Verify the value persists after reload.
3. **Delete a row** — Click delete on a row that is NOT used by any worker (or a freshly created row). Verify the row is removed.
4. **Delete blocked row** — Try to delete a row that IS assigned to a worker. Verify a blocked error message appears (not a generic 500).
5. **Add Bagian** — Click "Tambah Bagian", verify "Bagian Baru" appears in the bagian list.
6. **Delete Bagian (blocked)** — Try to delete a Bagian that has assigned rows. Verify block message.
7. **Cross-feature link** — After saving changes to Bagian "RFCC", navigate to CMP/Kkj (or click the link in the view if present) and filter by RFCC. Verify the edited rows appear correctly.

Report: "approved" if all flows work, or describe any issues found (include which test step failed and the error shown).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- dotnet build exits 0 after Task 1
- Manual browser pass confirms all 7 test flows work (Task 2 checkpoint)
- CMP/Kkj view reflects changes made in Admin/KkjMatrix
</verification>

<success_criteria>
DATA-01 is satisfied when: Admin/HC can add rows, edit cell values, bulk-save, delete rows (with block guard), manage bagians (add/rename/delete with guard), and all changes appear correctly in CMP/Kkj view filtered by the same Bagian.
</success_criteria>

<output>
After completion, create `.planning/phases/83-master-data-qa/83-01-SUMMARY.md` with:
- Bugs found and fixed (list each with file + line range)
- Bugs flagged as architectural (not fixed, documented)
- Manual verification result (approved / issues found)
- Any deferred follow-up items
</output>
