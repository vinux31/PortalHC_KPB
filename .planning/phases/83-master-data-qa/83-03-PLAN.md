---
phase: 83-master-data-qa
plan: 03
type: execute
wave: 2
depends_on: [83-01, 83-02]
files_modified:
  - Controllers/ProtonDataController.cs
  - Views/ProtonData/Index.cshtml
autonomous: false
requirements: [DATA-03]

must_haves:
  truths:
    - "Admin/HC can add, edit, and delete Silabus rows (Kompetensi / SubKompetensi / Deliverable) via the ProtonData editor"
    - "Orphan cleanup runs correctly — deleted rows are not left as ghost records in the database"
    - "Silabus data saved in Admin is visible as selectable options in Plan IDP and Coaching Proton pages (cross-feature round-trip)"
  artifacts:
    - path: "Controllers/ProtonDataController.cs"
      provides: "SilabusSave, SilabusDelete actions with nested upsert + orphan cleanup"
    - path: "Views/ProtonData/Index.cshtml"
      provides: "Silabus editor with Bagian/Unit/Track filter and row management UI"
  key_links:
    - from: "Views/ProtonData/Index.cshtml"
      to: "/ProtonData/SilabusSave"
      via: "fetch POST with JSON body of SilabusRowDto rows"
      pattern: "SilabusSave"
    - from: "ProtonKompetensiList / ProtonSubKompetensiList / ProtonDeliverableList"
      to: "CDP/CoachingProton (Coaching Proton page)"
      via: "same ProtonDeliverableList DbSet queried for coachee deliverables"
      pattern: "ProtonDeliverableList"
---

<objective>
Review and fix the Silabus CRUD feature (DATA-03) in ProtonDataController, then verify the cross-feature round-trip: silabus data saved in Admin appears correctly in Plan IDP and Coaching Proton pages.

Purpose: Silabus is the hierarchical training catalog (Kompetensi → SubKompetensi → Deliverable). Bugs in the nested upsert or orphan cleanup cause ghost records that break the Coaching Proton flow and Plan IDP in Phase 86.

Output: Fixed ProtonDataController.cs (if bugs found) + fixed Index.cshtml (if bugs found) + user-verified browser pass including cross-feature link check
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-master-data-qa/83-CONTEXT.md

<interfaces>
<!-- Key actions in ProtonDataController.cs -->

// GET /ProtonData/Index?bagian=&unit=&trackId=
// Loads silabus editor filtered by bagian/unit/trackId
public async Task<IActionResult> Index(string? bagian, string? unit, int? trackId)

// POST /ProtonData/SilabusSave — nested upsert with orphan cleanup
public async Task<IActionResult> SilabusSave([FromBody] List<SilabusRowDto> rows)
// SilabusRowDto fields: KompetensiId, SubKompetensiId, DeliverableId,
//   Kompetensi (string), SubKompetensi (string), Deliverable (string),
//   Bagian, Unit, TrackId
// Pattern:
//   1. Upsert Kompetensi (Id=0 → Add, Id>0 → FindAsync + update)
//   2. Upsert SubKompetensi (same)
//   3. Upsert Deliverable (same)
//   4. Orphan cleanup: delete Deliverables/SubKompetensi/Kompetensi not in saved IDs
//   CRITICAL: calls SaveChangesAsync after each level to get new IDs

// POST /ProtonData/SilabusDelete
public async Task<IActionResult> SilabusDelete([FromBody] SilabusDeleteRequest req)
// SilabusDeleteRequest fields: Level ("Kompetensi"|"SubKompetensi"|"Deliverable"), Id

// Models:
// ProtonKompetensi: Id, Bagian, Unit, ProtonTrackId, NamaKompetensi
// ProtonSubKompetensi: Id, ProtonKompetensiId, NamaSubKompetensi
// ProtonDeliverable: Id, ProtonSubKompetensiId, NamaDeliverable
// ProtonTrack: Id, Bagian, Unit, OperatorTahun (int)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review and fix Silabus nested upsert + orphan cleanup</name>
  <files>Controllers/ProtonDataController.cs, Views/ProtonData/Index.cshtml</files>
  <action>
Read ProtonDataController.cs lines 64–335 (Index, SilabusSave, SilabusDelete) in full. Read Views/ProtonData/Index.cshtml in full.

Review for these specific issues:

1. **Orphan cleanup scope** — SilabusSave saves rows for a specific bagian/unit/trackId combination. The orphan cleanup must only delete orphans within the current filter scope, not globally. Verify the WHERE clause includes the trackId/bagian/unit filter. If it orphans across all tracks, it would corrupt silabus data for other tracks when saving one. Fix if broken.

2. **SaveChangesAsync inside the loop** — SilabusSave calls SaveChangesAsync after each Kompetensi upsert to get the generated Id before creating SubKompetensi. This is correct but prevents a single atomic transaction. Verify: if one row fails midway, are partial Kompetensi written? If so, add a try/catch with early return on first failure (per the user decision: flag but fix if < 100 lines).

3. **SilabusDelete — cascade check** — Deleting a Kompetensi should cascade to SubKompetensi and Deliverable. Check if the EF delete cascade is configured on the models (via OnDelete(DeleteBehavior.Cascade) or via explicit deletion in the action). If the action only deletes the top-level entity without cascade config, child records become orphaned with null FK. Fix: explicitly delete children before deleting parent if cascade is not set in the model.

4. **Empty rows handling** — SilabusSave should reject an empty rows list with a clear message. Check the guard at the top of the action. If absent, add: `if (rows == null || !rows.Any()) return Json(new { success = false, message = "Tidak ada data." });`.

5. **View: CSRF token in fetch calls** — All fetch POSTs in Index.cshtml (SilabusSave, SilabusDelete) must include the antiforgery token header. Verify.

6. **Cross-feature: does CDP/CoachingProton use ProtonDeliverableList?** — Run a grep to confirm which controller/action loads the Coaching Proton page and how it queries silabus data:
   ```
   grep -n "ProtonDeliverableList\|ProtonDeliverable\|ProtonKompetensi" Controllers/CDPController.cs | head -20
   ```
   Also check if Plan IDP (if it exists as a view) or Phase 86 will consume ProtonKompetensiList. Document the query pattern in the summary. No code change needed here — this is research for the checkpoint verification steps.

Fix all bugs found (< 100 lines aggregate). Run `dotnet build` to confirm no compilation errors.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>dotnet build exits 0. Orphan cleanup is scoped to the current trackId/bagian/unit. SilabusDelete handles cascade correctly. Empty rows guard exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code-reviewed and fixed the Silabus nested upsert (SilabusSave), orphan cleanup, and SilabusDelete in ProtonDataController. Verified view's CSRF token usage.</what-built>
  <how-to-verify>
Log in as Admin or HC user. Navigate to ProtonData/Index (Kelola Data hub > Section B > Silabus Proton or similar card).

Test these flows:
1. **Filter** — Select a Bagian (e.g., RFCC), Unit, and Track from the filter dropdowns. Verify the editor loads the silabus rows for that track.
2. **Add a Deliverable row** — Under an existing Kompetensi and SubKompetensi, add a new Deliverable row. Click Save. Verify the new deliverable appears after reload.
3. **Edit a row** — Change the NamaDeliverable of an existing deliverable. Save. Verify the change persists after reload.
4. **Delete a row** — Delete the newly added deliverable. Verify it is gone after reload (no ghost record).
5. **Cross-track safety** — Switch to a DIFFERENT track filter. Verify the rows from the previous track are NOT deleted (orphan cleanup is scoped correctly).
6. **Cross-feature round-trip** — Navigate to the Coaching Proton page (CDP/CoachingProton or CDP dashboard). Confirm the deliverable you added in step 2 is visible as a coaching deliverable for any coachee assigned to that track. (If no coachee is assigned to the track, just confirm the Silabus data loaded without errors.)

Report: "approved" if all flows work, or describe issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- dotnet build exits 0 after Task 1
- Orphan cleanup is scoped to current filter (not global)
- Manual browser pass confirms all 6 test flows (Task 2 checkpoint)
- Cross-feature round-trip verified: Silabus edits visible in Coaching Proton deliverables
</verification>

<success_criteria>
DATA-03 is satisfied when: Admin/HC can add/edit/delete Silabus rows with correct orphan cleanup, and silabus data appears as deliverables in Coaching Proton pages for correctly assigned coachees.
</success_criteria>

<output>
After completion, create `.planning/phases/83-master-data-qa/83-03-SUMMARY.md` with:
- Bugs found and fixed (list each with file + line range)
- Cross-feature query pattern documented (how CDPController queries ProtonDeliverableList)
- Manual verification result (approved / issues found)
- Any deferred follow-up items
</output>
