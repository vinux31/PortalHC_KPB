---
phase: 83-master-data-qa
plan: 05
type: execute
wave: 3
depends_on: [83-03, 83-04]
files_modified:
  - Controllers/AdminController.cs
  - Views/Admin/ManageWorkers.cshtml
  - Views/Admin/CreateWorker.cshtml
  - Views/Admin/EditWorker.cshtml
  - Views/Admin/WorkerDetail.cshtml
  - Views/Admin/ImportWorkers.cshtml
autonomous: false
requirements: [DATA-05, DATA-06, DATA-07]

must_haves:
  truths:
    - "Admin/HC can create a worker with all fields (Name, Email, NIP, Role, Section, Unit, etc.) and the worker appears in ManageWorkers list"
    - "Admin/HC can edit a worker (including email uniqueness validation) and see changes persisted"
    - "Admin/HC can view a worker's detail page with all profile fields and current role"
    - "Admin/HC can delete a worker (with self-delete guard) and all related data is cascade-cleaned"
    - "Admin/HC can download the Excel import template, fill it, upload, and see per-row Success/Error/Skip results"
    - "Admin/HC can export the ManageWorkers list (with filters applied) to Excel and the downloaded file is valid"
  artifacts:
    - path: "Controllers/AdminController.cs"
      provides: "ManageWorkers, CreateWorker, EditWorker, DeleteWorker, WorkerDetail, ImportWorkers, DownloadImportTemplate, ExportWorkers actions"
    - path: "Views/Admin/ManageWorkers.cshtml"
      provides: "Worker list with search/filter, action buttons, export link"
    - path: "Views/Admin/ImportWorkers.cshtml"
      provides: "Import form with template download link and results table"
  key_links:
    - from: "Views/Admin/ManageWorkers.cshtml"
      to: "/Admin/ExportWorkers"
      via: "GET link with search, sectionFilter, roleFilter query params"
      pattern: "ExportWorkers"
    - from: "Views/Admin/ImportWorkers.cshtml"
      to: "/Admin/ImportWorkers POST"
      via: "form enctype=multipart/form-data with excelFile input"
      pattern: "ImportWorkers"
    - from: "Views/Admin/ImportWorkers.cshtml"
      to: "/Admin/DownloadImportTemplate"
      via: "GET anchor link"
      pattern: "DownloadImportTemplate"
---

<objective>
Review and fix all Worker management features (DATA-05, DATA-06, DATA-07): full CRUD (create/edit/detail/delete), Excel import with template + validation, and filtered Excel export. Create a small test Excel file for import testing. Then present manual verification steps to the user.

Purpose: Workers are the foundational user entities. All assessment, coaching, and IDP features depend on correctly stored worker data. Bugs in CRUD or import can corrupt user accounts. The import is the primary bulk-onboarding path.

Output: Fixed AdminController.cs (if bugs found) + fixed views (if bugs found) + test Excel file for import QA + user-verified browser pass
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-master-data-qa/83-CONTEXT.md

<interfaces>
<!-- Key actions in AdminController.cs for Worker Management (lines 2972–3700) -->

// GET /Admin/ManageWorkers?search=&sectionFilter=&roleFilter=
// Lists all users with search/filter; ViewBag.UserRoles (dict), Stats, filter state

// GET+POST /Admin/CreateWorker
// ManageUserViewModel: Id, FullName, Email, NIP, Position, Section, Unit, Directorate, JoinDate, Role, Password, ConfirmPassword
// POST validates: Password required (local auth), Email uniqueness
// On success: creates ApplicationUser, assigns role, redirects to ManageWorkers

// GET+POST /Admin/EditWorker/{id}
// POST validates: Email uniqueness (excluding self), Password optional
// Updates: FullName, NIP, Position, Section, Unit, Directorate, JoinDate, Role
// Role change: removes old role, adds new role, updates RoleLevel + SelectedView

// POST /Admin/DeleteWorker — guarded: blocks self-deletion
// Cascade-cleans: UserResponses, PackageUserResponses, UserPackageAssignments,
//   UserCompetencyLevels, ProtonDeliverableProgresses, ProtonTrackAssignments,
//   ProtonNotifications, CoachCoacheeMappings, CoachingSessions, CoachingLogs
// Then _userManager.DeleteAsync(user) for cascade on TrainingRecords, AssessmentSessions, IdpItems

// GET /Admin/WorkerDetail/{id}
// ViewBag.Role = first role string

// GET /Admin/DownloadImportTemplate
// Headers: Nama, Email, NIP, Jabatan, Bagian, Unit, Directorate, Role, Tgl Bergabung, [Password if local auth]
// Row 2: example row (italic, gray)
// Row 3-4: notes on Bagian and Role valid values

// POST /Admin/ImportWorkers (IFormFile excelFile)
// Per-row validation: required fields (Nama, Email), email format, email uniqueness
// Result: List<ImportWorkerResult> { FullName, Email, Status ("Success"|"Error"|"Skip"), Message }
// On completion: ViewBag.ImportResults = results; return View() (stays on import page, shows results table)

// GET /Admin/ExportWorkers?search=&sectionFilter=&roleFilter=
// Same filters as ManageWorkers; returns Excel file "Worker_Export_*.xlsx"
// Columns: No, Nama, Email, NIP, Jabatan, Bagian, Unit, Role

// UserRoles.AllRoles: list of valid role names (Admin, HC, Coach, SrSpv, SectionHead, Coachee, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code review, fix Worker CRUD + import/export, create test Excel file</name>
  <files>Controllers/AdminController.cs, Views/Admin/ManageWorkers.cshtml, Views/Admin/CreateWorker.cshtml, Views/Admin/EditWorker.cshtml, Views/Admin/WorkerDetail.cshtml, Views/Admin/ImportWorkers.cshtml</files>
  <action>
Read AdminController.cs lines 2972–3700 (ManageWorkers, CreateWorker, EditWorker, DeleteWorker, WorkerDetail, DownloadImportTemplate, ImportWorkers, ExportWorkers) and all 6 view files.

Review for these specific issues:

**Worker CRUD:**

1. **EditWorker email uniqueness** — Already implemented (FindByEmailAsync + check emailUser.Id != user.Id). Verify the check is for `emailUser != null && emailUser.Id != user.Id` (not just `emailUser != null`). If the check is missing the Id exclusion, an edit that keeps the same email would be incorrectly rejected. Confirm it is correct.

2. **DeleteWorker cascade** — The cascade cleanup is manual (lines 3300–3375). Verify all FK references are covered. Check if `TrainingRecords` are cascade-deleted by EF (via ApplicationUser navigation). If `TrainingRecords` use Restrict delete behavior and are NOT in the manual cleanup, add them. Check: `_context.TrainingRecords.Where(t => t.UserId == id).ToListAsync()` and remove before user delete.

3. **CreateWorker password** — `model.Password!` with null-forgiving operator: if ModelState removed the Password validation but the form is submitted without password in local mode, this could throw NullReferenceException. The guard at line 3051 should prevent this. Confirm the guard correctly prevents reaching `CreateAsync(user, password)` when password is null in local mode.

4. **ManageWorkers — export link preserves filters** — The export button/link in ManageWorkers.cshtml should include the current search, sectionFilter, and roleFilter as query params so the export matches what the user sees. Verify the link is: `/Admin/ExportWorkers?search=@ViewBag.Search&sectionFilter=@ViewBag.SectionFilter&roleFilter=@ViewBag.RoleFilter`. If not, fix the anchor href.

5. **ImportWorkers — results table** — After import, ViewBag.ImportResults is set and the view should render a table with rows colored by status (Success=green, Error=red, Skip=yellow). Verify this table exists in ImportWorkers.cshtml. If the view only redirects and loses results, the import feedback is invisible. ImportWorkers POST should return `View()` (not `RedirectToAction`) when results exist. Confirm this is the current behavior.

6. **ImportWorkers — template column order** — The template has columns: Nama(1), Email(2), NIP(3), Jabatan(4), Bagian(5), Unit(6), Directorate(7), Role(8), Tgl Bergabung(9), [Password(10)]. The ImportWorkers POST reads: `Cell(1)=fullName, Cell(2)=email`. Verify the ImportWorkers POST reads all columns in the same order as the template. If there is a mismatch (e.g., NIP and Jabatan swapped), fix the cell index in the POST action.

7. **ExportWorkers — role column** — ExportWorkers must call `_userManager.GetRolesAsync(u)` per user to get the role string. Check if this is done or if the export uses `RoleLevel` (an integer). RoleLevel is not user-friendly; the role name string is expected. If missing, add role lookup (batch it: call GetRolesAsync in a loop after fetching the user list — already done in ManageWorkers GET, follow the same pattern).

Fix all bugs found (< 100 lines aggregate). Run `dotnet build` to confirm no compilation errors.

**Create test Excel file for Worker import:**

Use ClosedXML or manually write bytes to create a test Excel file at:
`C:/Users/Administrator/Desktop/PortalHC_KPB/wwwroot/test-data/worker_import_test.xlsx`

First, create the directory: `mkdir -p "C:/Users/Administrator/Desktop/PortalHC_KPB/wwwroot/test-data"`

Then create a C# script or use the DownloadImportTemplate output as the base. Since we cannot run C# directly here, instead document the test file content in a text file and instruct the user to download the template first, then fill it manually in the checkpoint step.

Create a plain text file at `wwwroot/test-data/worker_import_test_data.txt` with these 7 rows for the user to copy into the template:

```
Row 1 (valid):    Ahmad Test Satu, ahmad.testsatu@test.com, 111001, Operator, RFCC, RFCC LPG Treating Unit (062), CSU Process, Coachee, 2024-01-15, Test123!
Row 2 (valid):    Budi Test Dua, budi.testdua@test.com, 111002, Sr Operator, GAST, GAST Unit, CSU Gas, Coachee, 2024-02-20, Test123!
Row 3 (valid):    Cici Test Tiga, cici.testtiga@test.com, 111003, Foreman, NGP, NGP Unit, CSU NGP, SrSpv, 2024-03-01, Test123!
Row 4 (duplicate email - should be Skip): Ahmad Test Satu, ahmad.testsatu@test.com, 111004, Operator, RFCC, RFCC LPG, CSU, Coachee, 2024-01-15, Test123!
Row 5 (empty email - should be Error):   Empty Email Test, , 111005, Operator, RFCC, RFCC LPG, CSU, Coachee, 2024-01-15, Test123!
Row 6 (invalid email format - should be Error): Bad Format, notanemail, 111006, Operator, RFCC, RFCC LPG, CSU, Coachee, 2024-01-15, Test123!
Row 7 (valid):    Diana Test Tujuh, diana.testtujuh@test.com, 111007, Analyst, DHT, DHT Unit, CSU DHT, Coachee, 2024-04-10, Test123!
```

This gives 3 successful imports, 1 skip (duplicate), 2 errors, and 1 more success for verifying the results table shows all statuses.

Note: If `Authentication:UseActiveDirectory` is true in appsettings.json, the Password column is omitted from the template. Check and document this in the summary.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>dotnet build exits 0. Export link in ManageWorkers preserves filters. ImportWorkers POST returns View() with results. Export includes role name strings. Test data file created at wwwroot/test-data/worker_import_test_data.txt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code-reviewed and fixed Worker CRUD (CreateWorker, EditWorker, DeleteWorker, WorkerDetail), import (ImportWorkers, DownloadImportTemplate), and export (ExportWorkers). Created test data instructions. Fixed filter preservation on export link and import results display if needed.</what-built>
  <how-to-verify>
Log in as Admin or HC user. Navigate to Admin/ManageWorkers (Kelola Data hub > Section A > Manajemen Pekerja).

**DATA-05: Worker CRUD**
1. **Create** — Click "Tambah Pekerja". Fill in all fields: Nama, Email (new unique email), NIP, Jabatan, Bagian, Unit, Role=Coachee, Password (e.g., Test123!). Submit. Verify worker appears in ManageWorkers list.
2. **Edit** — Click edit on the newly created worker. Change their Jabatan/Position. Save. Verify change persists.
3. **Detail** — Click "Detail" on the worker. Verify WorkerDetail page shows all profile fields and the correct role.
4. **Delete** — Delete the newly created test worker. Verify they are removed from the list and a success message appears.
5. **Self-delete guard** — Try clicking "Hapus" on your own account (the logged-in Admin). Verify an error message appears and you are NOT deleted.

**DATA-06: Worker Import**
6. **Download template** — Click "Import Pekerja" then "Download Template". Verify a valid Excel file downloads with correct headers (Nama, Email, NIP, Jabatan, Bagian, Unit, Directorate, Role, Tgl Bergabung, Password).
7. **Fill template** — Open the downloaded template. Fill in the 7 rows from the test data file at `wwwroot/test-data/worker_import_test_data.txt`. Save the file.
8. **Upload** — Upload the filled template. After processing, verify the results table shows:
   - Rows 1, 2, 3, 7: Status "Berhasil" (green)
   - Row 4: Status "Skip" (duplicate email, yellow)
   - Rows 5, 6: Status "Error" (missing/invalid email, red)
9. **Clean up** — Delete the 4 test workers created by the import (Ahmad Test Satu, Budi Test Dua, Cici Test Tiga, Diana Test Tujuh) to keep the database clean.

**DATA-07: Worker Export**
10. **Export all** — In ManageWorkers, without filters, click "Export Excel". Verify a valid Excel file downloads with columns: No, Nama, Email, NIP, Jabatan, Bagian, Unit, Role (role name, not a number).
11. **Export filtered** — Apply a section filter (e.g., RFCC), then export. Verify the downloaded file contains only workers from that section.

Report: "approved" if all flows work, or describe issues found (include step number and error).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- dotnet build exits 0 after Task 1
- Export link preserves current filters from ManageWorkers
- ImportWorkers POST stays on page and renders results table (not redirect)
- ExportWorkers uses role name strings (not RoleLevel integer)
- Manual browser pass confirms all 11 test flows (Task 2 checkpoint)
</verification>

<success_criteria>
DATA-05 satisfied: Admin/HC can create, edit, view detail, and delete workers with correct guards and cascade cleanup.
DATA-06 satisfied: Template download works; import shows per-row Success/Error/Skip results with correct validation (empty email, duplicate, invalid format).
DATA-07 satisfied: Export downloads a valid Excel file with correct role name column; export respects active search/filter state.
</success_criteria>

<output>
After completion, create `.planning/phases/83-master-data-qa/83-05-SUMMARY.md` with:
- Bugs found and fixed (list each with file + line range)
- Import test results (which rows passed/failed as expected)
- Notes on AD mode (UseActiveDirectory) impact on Password column
- Manual verification result (approved / issues found)
- Any deferred follow-up items
</output>
