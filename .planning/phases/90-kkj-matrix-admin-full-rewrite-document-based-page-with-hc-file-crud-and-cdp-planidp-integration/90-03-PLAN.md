---
phase: 90-kkj-matrix-admin-full-rewrite
plan: 03
type: execute
wave: 3
depends_on: [90-02]
files_modified:
  - Views/Admin/KkjMatrix.cshtml
  - Views/Admin/KkjUpload.cshtml
  - Views/Admin/KkjFileHistory.cshtml
  - Views/Admin/Index.cshtml
  - Views/CMP/KkjSectionSelect.cshtml
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "dotnet build succeeds after all view changes"
    - "Admin/KkjMatrix.cshtml shows Bootstrap 5 nav-tabs per bagian, file list table (name, type, size, upload date, uploader, actions), Upload File button per tab, and Tambah Bagian / edit inline controls"
    - "Admin/KkjUpload.cshtml shows upload form with file picker (drag-drop zone), bagian dropdown, keterangan field, validate button enabled only after file selected"
    - "Admin/KkjFileHistory.cshtml shows archived files list with download buttons"
    - "Admin/Index.cshtml KKJ Matrix card description updated to reflect file-based system"
    - "Views/CMP/KkjSectionSelect.cshtml is deleted"
    - "TempData success/error toasts shown on KkjMatrix page after upload/delete"
  artifacts:
    - path: "Views/Admin/KkjMatrix.cshtml"
      provides: "Tab navigation + file list admin UI"
      contains: "nav-tabs"
    - path: "Views/Admin/KkjUpload.cshtml"
      provides: "File upload form"
      contains: "uploadZone"
    - path: "Views/Admin/KkjFileHistory.cshtml"
      provides: "Archived files history list"
      contains: "ArchivedFiles"
  key_links:
    - from: "Views/Admin/KkjMatrix.cshtml"
      to: "AdminController.KkjUpload"
      via: "Url.Action('KkjUpload', 'Admin', new { bagianId = bagian.Id })"
      pattern: "KkjUpload"
    - from: "Views/Admin/KkjMatrix.cshtml"
      to: "AdminController.KkjFileDelete"
      via: "fetch POST to /Admin/KkjFileDelete"
      pattern: "KkjFileDelete"
    - from: "Views/Admin/KkjUpload.cshtml"
      to: "AdminController.KkjUpload POST"
      via: "form asp-action='KkjUpload'"
      pattern: "method=\"post\""
---

<objective>
Rewrite Views/Admin/KkjMatrix.cshtml (tab navigation + file list), create Views/Admin/KkjUpload.cshtml (upload form), create Views/Admin/KkjFileHistory.cshtml (history list), update Admin/Index.cshtml card description, and delete Views/CMP/KkjSectionSelect.cshtml.

Purpose: The Admin KKJ Matrix page is now a document management UI rather than a spreadsheet editor.
Output: Fully functional Admin views that work with the new KkjFile backend from Plan 02.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/90-kkj-matrix-admin-full-rewrite-document-based-page-with-hc-file-crud-and-cdp-planidp-integration/90-CONTEXT.md
@.planning/phases/90-kkj-matrix-admin-full-rewrite-document-based-page-with-hc-file-crud-and-cdp-planidp-integration/90-RESEARCH.md
@.planning/phases/90-kkj-matrix-admin-full-rewrite-document-based-page-with-hc-file-crud-and-cdp-planidp-integration/90-02-SUMMARY.md

<interfaces>
<!-- ViewBag data from AdminController.KkjMatrix(): -->
<!-- ViewBag.Bagians = List<KkjBagian> (ordered by DisplayOrder) -->
<!-- ViewBag.FilesByBagian = Dictionary<int, List<KkjFile>> (bagianId → files) -->
<!-- ViewBag.SelectedBagianId = int (tab to show active) -->

<!-- ViewBag data from AdminController.KkjUpload() GET: -->
<!-- ViewBag.Bagians = List<KkjBagian> -->
<!-- ViewBag.SelectedBagianId = int (pre-selected bagian from URL param) -->

<!-- ViewBag data from AdminController.KkjFileHistory() GET: -->
<!-- ViewBag.Bagian = KkjBagian -->
<!-- ViewBag.ArchivedFiles = List<KkjFile> -->

<!-- KkjFile model fields for display: -->
<!-- FileName (string), FileType (string: "pdf"/"xlsx"/"xls"), FileSizeBytes (long) -->
<!-- UploadedAt (DateTimeOffset), UploaderName (string), Keterangan (string?), IsArchived (bool) -->
<!-- Id (int), BagianId (int) -->

<!-- Bootstrap 5 nav-tabs pattern already used in portal (Phase 89 views) -->
<!-- TempData["Success"] and TempData["Error"] for toast notifications -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Views/Admin/KkjMatrix.cshtml — tab navigation + file list UI</name>
  <files>Views/Admin/KkjMatrix.cshtml</files>
  <action>
Complete rewrite of Views/Admin/KkjMatrix.cshtml. Reference the RESEARCH.md Pattern 2 example as the structural basis.

The page must contain:

**Header area:**
- Breadcrumb: Kelola Data > KKJ Matrix
- Page title: "KKJ Matrix" with an icon
- Description: "Kelola dokumen KKJ Matrix per bagian. Upload PDF atau Excel untuk setiap bagian."

**TempData toast display** (at top, after header):
```html
@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle me-2"></i>@TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle me-2"></i>@TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
```

**Bagian tab navigation (Bootstrap 5 nav-tabs):**
- One tab per bagian, ordered by DisplayOrder
- Active tab = ViewBag.SelectedBagianId
- "Tambah Bagian" button as a non-tab element next to the tab bar (right side)

**Each tab pane contains:**
1. Bagian management bar: bagian name + edit icon (inline rename) + delete button (with confirmation)
2. File list table: columns = Nama File, Tipe, Ukuran, Keterangan, Tanggal Upload, Di-upload Oleh, Aksi
3. If no files: empty state message "Belum ada file. Upload file KKJ Matrix untuk bagian ini."
4. Action buttons per file row:
   - Download: `<a href="@Url.Action("KkjFileDownload", "Admin", new { id = file.Id })" class="btn btn-sm btn-outline-primary">`
   - Delete: button with onclick confirmation calling `deleteFile(@file.Id)` JS function
   - History (only shown if IsArchived files exist for bagian): `<a href="@Url.Action("KkjFileHistory", "Admin", new { bagianId = bagian.Id })" ...>`
5. Upload File button (bottom of file list): navigates to KkjUpload page for this bagian

**JS deleteFile() function:**
```javascript
async function deleteFile(fileId) {
    if (!confirm('Arsipkan file ini? File akan dipindahkan ke riwayat.')) return;
    const token = document.querySelector('input[name="__RequestVerificationToken"]').value;
    const resp = await fetch('/Admin/KkjFileDelete', {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body: `id=${fileId}&__RequestVerificationToken=${encodeURIComponent(token)}`
    });
    const data = await resp.json();
    if (data.success) {
        document.getElementById(`file-row-${fileId}`).remove();
        // Show success toast
        const toast = document.createElement('div');
        toast.className = 'alert alert-success position-fixed top-0 end-0 m-3';
        toast.style.zIndex = '9999';
        toast.textContent = data.message;
        document.body.appendChild(toast);
        setTimeout(() => toast.remove(), 3000);
    } else {
        alert(data.message);
    }
}
```

**Tambah Bagian functionality:**
- "Tambah Bagian" button opens a small inline form or calls a JS function that prompts for bagian name and POSTs to `/Admin/KkjBagianAdd`
- Keep the same pattern as Phase 89 KkjMatrix.cshtml (reuse the bagian add/delete JS from the old view)

**Note:** Do NOT include any competency table, target value cells, column management, or position mapping panels — this is a pure document management page.

Include the antiforgery token in the page:
```html
@Html.AntiForgeryToken()
```

File size display helper (in Razor, not a separate helper class):
```csharp
@{ string FormatSize(long bytes) => bytes < 1024 * 1024 ? $"{bytes / 1024}KB" : $"{bytes / (1024 * 1024.0):F1}MB"; }
```
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build 2>&1 | grep -E "error.*KkjMatrix|Build succeeded|Build FAILED" | head -10</automated>
  </verify>
  <done>dotnet build succeeds. Views/Admin/KkjMatrix.cshtml exists and contains: nav-tabs, tab-content panes, file list table, KkjFileDownload link, KkjFileDelete JS call, KkjUpload upload button link, Tambah Bagian button.</done>
</task>

<task type="auto">
  <name>Task 2: Create Views/Admin/KkjUpload.cshtml + KkjFileHistory.cshtml + update Index.cshtml + delete KkjSectionSelect.cshtml</name>
  <files>Views/Admin/KkjUpload.cshtml, Views/Admin/KkjFileHistory.cshtml, Views/Admin/Index.cshtml, Views/CMP/KkjSectionSelect.cshtml</files>
  <action>
**Views/Admin/KkjUpload.cshtml** — Create new file based on the full RESEARCH.md "File Upload Form" example (lines ~370-518 of RESEARCH.md). Key requirements:
- Breadcrumb: Kelola Data > KKJ Matrix > Upload File
- Upload form: `enctype="multipart/form-data"` POST to AdminController.KkjUpload
- Bagian dropdown pre-selected to `ViewBag.SelectedBagianId`
- File picker drag-drop zone (click or drag) — accepts .pdf, .xlsx, .xls
- Keterangan text field (optional)
- Upload button disabled until file selected (enabled via JS on file input change)
- Format requirements card at bottom: PDF/Excel, max 10MB
- Error display from TempData["Error"]
- Antiforgery token

Copy the full HTML/JS structure from RESEARCH.md Pattern "File Upload Form" section. The pre-selected bagian should be set on the dropdown `<option selected>` for the matching bagianId.

**Views/Admin/KkjFileHistory.cshtml** — Create new file:
```html
@{
    ViewData["Title"] = ViewData["Title"] as string ?? "Riwayat File";
    var bagian = ViewBag.Bagian as HcPortal.Models.KkjBagian;
    var archivedFiles = ViewBag.ArchivedFiles as List<HcPortal.Models.KkjFile> ?? new();
}

<div class="container py-4" style="max-width: 900px;">
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="@Url.Action("Index", "Admin")">Kelola Data</a></li>
            <li class="breadcrumb-item"><a href="@Url.Action("KkjMatrix", "Admin")">KKJ Matrix</a></li>
            <li class="breadcrumb-item active">Riwayat File — @bagian?.Name</li>
        </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold"><i class="bi bi-clock-history me-2"></i>Riwayat File — @bagian?.Name</h2>
        <a href="@Url.Action("KkjMatrix", "Admin", new { bagian = bagian?.Id })" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left me-2"></i>Kembali
        </a>
    </div>

    @if (!archivedFiles.Any())
    {
        <div class="alert alert-info">Tidak ada file yang diarsipkan untuk bagian ini.</div>
    }
    else
    {
        <div class="card border-0 shadow-sm">
            <div class="card-body p-0">
                <table class="table table-sm mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Nama File</th>
                            <th>Tipe</th>
                            <th>Ukuran</th>
                            <th>Keterangan</th>
                            <th>Tanggal Upload</th>
                            <th>Di-upload Oleh</th>
                            <th>Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var file in archivedFiles)
                        {
                            <tr>
                                <td>@file.FileName</td>
                                <td><span class="badge bg-secondary">@file.FileType.ToUpper()</span></td>
                                <td>@(file.FileSizeBytes < 1048576 ? $"{file.FileSizeBytes / 1024}KB" : $"{file.FileSizeBytes / 1048576.0:F1}MB")</td>
                                <td>@(file.Keterangan ?? "-")</td>
                                <td>@file.UploadedAt.ToString("dd MMM yyyy HH:mm")</td>
                                <td>@file.UploaderName</td>
                                <td>
                                    <a href="@Url.Action("KkjFileDownload", "Admin", new { id = file.Id })"
                                       class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-download"></i> Download
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>
```

**Views/Admin/Index.cshtml** — Update the KKJ Matrix card description:
- Find: `<small class="text-muted">Kelola item KKJ Matrix (master kompetensi)</small>`
- Replace with: `<small class="text-muted">Upload dan kelola dokumen KKJ Matrix (PDF/Excel) per bagian</small>`

**Views/CMP/KkjSectionSelect.cshtml** — DELETE this file. It was used for the old section selector flow which is replaced by the new CMP/Kkj bagian dropdown. If needed, confirm the file exists first: `ls Views/CMP/KkjSectionSelect.cshtml`.

After changes, run `dotnet build` to verify all views compile.
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/PortalHC_KPB" && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>dotnet build succeeds. Views/Admin/KkjUpload.cshtml exists. Views/Admin/KkjFileHistory.cshtml exists. Views/CMP/KkjSectionSelect.cshtml does not exist. Views/Admin/Index.cshtml KKJ Matrix description updated to "Upload dan kelola dokumen".</done>
</task>

</tasks>

<verification>
After Plan 03:
1. `dotnet build` succeeds with zero errors
2. `ls Views/Admin/KkjMatrix.cshtml Views/Admin/KkjUpload.cshtml Views/Admin/KkjFileHistory.cshtml` — all three exist
3. `ls Views/CMP/KkjSectionSelect.cshtml` — file NOT found (deleted)
4. `grep "KkjUpload\|KkjFileDelete\|nav-tabs" Views/Admin/KkjMatrix.cshtml` — all three match
5. `grep "Kelola item KKJ Matrix" Views/Admin/Index.cshtml` — zero matches (updated)
</verification>

<success_criteria>
- KkjMatrix.cshtml tab-based bagian UI with file list table and upload button per tab
- KkjUpload.cshtml upload form with drag-drop zone, bagian dropdown, validation info
- KkjFileHistory.cshtml archived files list with download buttons
- Admin/Index.cshtml KKJ Matrix description updated
- KkjSectionSelect.cshtml deleted
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/90-kkj-matrix-admin-full-rewrite-document-based-page-with-hc-file-crud-and-cdp-planidp-integration/90-03-SUMMARY.md`
</output>
