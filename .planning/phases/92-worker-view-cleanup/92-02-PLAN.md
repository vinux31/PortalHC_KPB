---
phase: 93-worker-view-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 93-01
files_modified:
  - Models/KkjModels.cs
  - Data/ApplicationDbContext.cs
  - Data/SeedMasterData.cs
  - Program.cs
  - Views/CMP/MappingSectionSelect.cshtml
  - Controllers/CMPController.cs
autonomous: true
requirements:
  - CPDP-07

must_haves:
  truths:
    - "The CpdpItems table no longer exists in the database after migration"
    - "No reference to CpdpItem, CpdpItems, GapAnalysisItem (if unused), or MappingSectionSelect remains in controllers or views"
    - "The application builds and runs cleanly with zero errors after cleanup"
    - "EF Core migration to drop CpdpItems table applies without error"
  artifacts:
    - path: "Models/KkjModels.cs"
      provides: "CpdpItem and GapAnalysisItem classes removed"
      contains: "KkjBagian, KkjFile, CpdpFile"
    - path: "Data/ApplicationDbContext.cs"
      provides: "DbSet<CpdpItem> and OnModelCreating CpdpItem config removed"
      contains: "CpdpFiles"
    - path: "Data/SeedMasterData.cs"
      provides: "SeedCpdpItemsAsync method body emptied or method removed"
    - path: "Program.cs"
      provides: "SeedCpdpItemsAsync call removed"
    - path: "Migrations/<timestamp>_DropCpdpItems.cs"
      provides: "EF Core migration that drops the CpdpItems table"
      contains: "DropTable"
  key_links:
    - from: "Migrations/<timestamp>_DropCpdpItems.cs"
      to: "ApplicationDbContext"
      via: "dotnet ef database update"
      pattern: "DropTable.*CpdpItems"
    - from: "Controllers/CMPController.cs"
      to: "CpdpItem"
      via: "grep — must return zero results after cleanup"
      pattern: "CpdpItem"
---

<objective>
Remove the legacy CpdpItem infrastructure completely: model class, DbSet, OnModelCreating config, seed data, seed call, MappingSectionSelect view, and the EF Core table itself.

Purpose: Phase 93 requirement CPDP-07 mandates total removal of the CpdpItem spreadsheet system now that the file-based replacement (Phase 92) and worker view (Plan 93-01) are verified working.

Output: Clean codebase with no CpdpItem references; EF migration drops the CpdpItems database table; dotnet build passes.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/93-worker-view-cleanup/93-CONTEXT.md
@.planning/phases/93-worker-view-cleanup/93-01-SUMMARY.md

<interfaces>
<!-- Key context about what needs to be removed. Extracted from codebase. -->

From Models/KkjModels.cs — classes to DELETE:
```csharp
// DELETE: CpdpItem class (lines ~56-68)
public class CpdpItem { ... }

// DELETE: GapAnalysisItem class (lines ~47-53) — verify no references first
public class GapAnalysisItem { ... }
```

From Data/ApplicationDbContext.cs — lines to REMOVE:
```csharp
// REMOVE this DbSet:
public DbSet<CpdpItem> CpdpItems { get; set; }

// REMOVE this OnModelCreating block:
builder.Entity<CpdpItem>(entity =>
{
    entity.ToTable("CpdpItems");
});
```

From Data/SeedMasterData.cs — entire method to REMOVE:
```csharp
public static async Task SeedCpdpItemsAsync(ApplicationDbContext context) { ... }
```

From Program.cs — call to REMOVE:
```csharp
await SeedMasterData.SeedCpdpItemsAsync(context);
```

Views to DELETE:
- Views/CMP/MappingSectionSelect.cshtml (bagian selector page — replaced by tabs in Plan 93-01)

CMPController.cs — by end of Plan 93-01, the Mapping action no longer uses CpdpItems.
After model removal, confirm no remaining references to CpdpItem in CMPController.

Latest migration: 20260303000729_AddCpdpFiles — new migration must come AFTER this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CpdpItem model, DbSet, seed code, and delete MappingSectionSelect view</name>
  <files>
    Models/KkjModels.cs,
    Data/ApplicationDbContext.cs,
    Data/SeedMasterData.cs,
    Program.cs,
    Views/CMP/MappingSectionSelect.cshtml
  </files>
  <action>
Perform the following removals in order:

**Models/KkjModels.cs:**
- Delete the entire `GapAnalysisItem` class block (verify first: grep codebase for GapAnalysisItem — if referenced anywhere outside KkjModels.cs, do NOT delete it; otherwise delete).
- Delete the entire `CpdpItem` class block.
- Keep KkjBagian, KkjFile, and CpdpFile untouched.

**Data/ApplicationDbContext.cs:**
- Remove `public DbSet<CpdpItem> CpdpItems { get; set; }` line.
- Remove the entire `builder.Entity<CpdpItem>(entity => { entity.ToTable("CpdpItems"); });` block from OnModelCreating.
- Keep the `builder.Entity<CpdpFile>` block intact.

**Data/SeedMasterData.cs:**
- Remove the entire `SeedCpdpItemsAsync` static method (the method and all its body including the large CpdpItem list).
- Keep other seed methods (SeedSampleTrainingRecordsAsync etc.) untouched.

**Program.cs:**
- Remove the line `await SeedMasterData.SeedCpdpItemsAsync(context);`
- Keep surrounding seeding calls untouched.

**Views/CMP/MappingSectionSelect.cshtml:**
- Delete this file entirely (it was the old section-picker that redirected to Mapping?section=X — now replaced by the tabbed layout from Plan 93-01).

After edits, run: `dotnet build --no-restore` — must produce 0 errors before proceeding to Task 2.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | grep -E "^.*error" | head -20</automated>
  </verify>
  <done>Build passes with 0 errors; grep for "CpdpItem" across Controllers/ Models/ Data/ Views/ returns zero hits; MappingSectionSelect.cshtml is deleted</done>
</task>

<task type="auto">
  <name>Task 2: Create and apply EF Core migration to drop CpdpItems table</name>
  <files>
    Migrations/&lt;timestamp&gt;_DropCpdpItems.cs,
    Migrations/&lt;timestamp&gt;_DropCpdpItems.Designer.cs,
    Migrations/ApplicationDbContextModelSnapshot.cs
  </files>
  <action>
Create and apply the EF Core migration that drops the CpdpItems table from the database.

Run from the project root:
```bash
dotnet ef migrations add DropCpdpItems
dotnet ef database update
```

After generating the migration, verify the generated migration file contains:
- `migrationBuilder.DropTable(name: "CpdpItems");` in the Up() method
- A corresponding CreateTable for CpdpItems in the Down() method (for rollback safety)

If the migration file does NOT contain DropTable for CpdpItems (e.g., it's empty or minimal), investigate why — the DbSet removal in Task 1 must have been applied correctly for EF to detect the table removal.

After `dotnet ef database update` succeeds, verify by checking the database or running a quick EF check:
```bash
dotnet ef migrations list
```
The new DropCpdpItems migration should show as Applied.
  </action>
  <verify>
    <automated>dotnet ef migrations list 2>&1 | tail -5</automated>
  </verify>
  <done>Migration DropCpdpItems exists and is marked as Applied; migration file contains DropTable("CpdpItems") in Up(); database no longer has CpdpItems table</done>
</task>

</tasks>

<verification>
- `dotnet build --no-restore` passes with 0 errors
- `grep -r "CpdpItem" Controllers/ Models/ Data/ Views/` returns zero results
- `grep -r "MappingSectionSelect" Controllers/ Views/` returns zero results
- `dotnet ef migrations list` shows DropCpdpItems as Applied
- Application starts without errors (dotnet run or check startup logs)
</verification>

<success_criteria>
- CPDP-07: CpdpItem table and all spreadsheet CRUD code permanently removed
- Build is clean, no orphaned references
- EF migration applied successfully
</success_criteria>

<output>
After completion, create `.planning/phases/93-worker-view-cleanup/93-02-SUMMARY.md`
</output>
