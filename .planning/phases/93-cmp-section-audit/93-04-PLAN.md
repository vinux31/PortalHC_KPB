---
wave: 4
depends_on:
  - 93-02
  - 93-03
files_modified:
  - Controllers/CMPController.cs
  - Views/CMP/*.cshtml
autonomous: false
phase: 93
plan: 04
type: manual
requirements:
  - CMP-01
  - CMP-02
  - CMP-03
  - CMP-04
  - CMP-05
  - CMP-06
must_haves:
  truths:
    - 93-02 fixed date localization across all CMP views
    - 93-03 fixed null safety and validation bugs
    - Smoke test only — quick verification, not exhaustive testing
    - Focus on verifying specific bug fixes and obvious broken flows
  artifacts:
    - Browser verification report (pass/fail per test case)
    - Bug report for any remaining issues (if found)
  key_links:
    - /CMP/Index (Assessment hub)
    - /CMP/Assessment (Assessment list)
    - /CMP/Records (Assessment + Training history)
    - /CMP/Kkj (KKJ Matrix view)
    - /CMP/Mapping (CPDP file download)
---

# Plan 93-04: Browser Verification - Test All CMP Flows

**Goal:** Smoke test all CMP pages and flows to verify bugs are fixed and no regressions introduced

**Context:** After fixing localization (93-02) and null safety (93-03), verify all CMP functionality works correctly end-to-end. This is NOT exhaustive testing — focus on verifying specific bug fixes and obvious broken flows.

**Requirements Coverage:**
- CMP-01: Assessment page loads without errors for all roles
- CMP-02: Assessment monitoring detail page shows real-time data correctly
- CMP-03: Records page displays assessment history and training records with correct pagination
- CMP-04: KKJ Matrix page loads correctly with section-based filtering
- CMP-05: All CMP forms handle validation errors gracefully (no raw exceptions)
- CMP-06: CMP navigation flows work correctly (Create Assessment → Edit → Delete → Monitor)

## Tasks

### Task 1: Smoke test CMP hub and navigation

<files>
(N/A - browser verification)
</files>

<action>
Verify CMP Index hub loads and all cards navigate correctly:

1. Login as Worker → Navigate to `/CMP/Index`
2. Verify page loads without errors
3. Click each card:
   - KKJ Matrix → `/CMP/Kkj`
   - CPDP Mapping → `/CMP/Mapping`
   - Assessment → `/CMP/Assessment`
   - Records → `/CMP/Records`
4. Verify each destination page loads

Focus: Page loads, no 404/500 errors, breadcrumbs display correctly.
</action>

<verify>
Browser test checklist:
- [ ] CMP Index hub loads (Worker role)
- [ ] KKJ Matrix card navigates correctly
- [ ] CPDP Mapping card navigates correctly
- [ ] Assessment card navigates correctly
- [ ] Records card navigates correctly
- [ ] No 404 or 500 status codes
- [ ] Breadcrumbs display correctly on all pages
</verify>

<done>
CMP hub navigation verified:
- All cards navigate to correct URLs
- All destination pages load without errors
- No navigation failures
</done>

---

### Task 2: Smoke test Assessment page (Worker role)

<files>
(N/A - browser verification)
</files>

<action>
Verify Assessment list loads and displays correctly:

1. Login as Worker → Navigate to `/CMP/Assessment`
2. Verify Open tab loads (if assessments available)
3. Verify Upcoming tab loads
4. Check date formatting in assessment list
5. Click "Mulai Tes" if Open assessment exists → Verify StartExam page loads

Focus: Tabs render, dates show Indonesian locale, no null exceptions.
</action>

<verify>
Browser test checklist:
- [ ] Open tab loads (Worker role)
- [ ] Upcoming tab loads (Worker role)
- [ ] Dates show in Indonesian format (e.g., "05 Mar 2026")
- [ ] No null reference exceptions visible
- [ ] StartExam link works (if Open assessment exists)
- [ ] No raw exceptions or error messages
</verify>

<done>
Assessment page verified for Worker role:
- Both tabs render without errors
- Dates formatted in Indonesian locale
- No null reference exceptions
- StartExam navigation works
</done>

---

### Task 3: Smoke test Records page (Worker role)

<files>
(N/A - browser verification)
</files>

<action>
Verify Records 2-tab layout and pagination:

1. Login as Worker → Navigate to `/CMP/Records`
2. Verify Assessment Online tab loads
3. Verify Training Manual tab loads
4. Check date formatting in both tabs
5. Test pagination if records exist (click page 2, etc.)
6. Click an assessment row → Verify Results page loads

Focus: Tabs render, pagination works, dates formatted correctly.
</action>

<verify>
Browser test checklist:
- [ ] Assessment Online tab loads
- [ ] Training Manual tab loads
- [ ] Dates formatted in Indonesian (both tabs)
- [ ] Pagination works (if records exist)
- [ ] Row clicks navigate to Results page
- [ ] No JavaScript errors in console
</verify>

<done>
Records page verified:
- Both tabs render without errors
- Pagination functional (if data exists)
- Dates formatted in Indonesian
- Row navigation to Results works
</done>

---

### Task 4: Smoke test KKJ Matrix page (all roles)

<files>
(N/A - browser verification)
</files>

<action>
Verify KKJ Matrix section filtering and file downloads:

1. Login as Worker (L1-L4) → Navigate to `/CMP/Kkj`
2. Verify all section tabs visible (RFCC, GAST, NGP, DHT)
3. Click each tab → Verify files load
4. Click a download link → Verify file downloads
5. Login as SectionHead (L5) → Navigate to `/CMP/Kkj`
6. Verify only own unit tab visible
7. Verify download still works

Focus: Role-based filtering works, file downloads work, no tab switching errors.
</action>

<verify>
Browser test checklist:
- [ ] L1-L4 see all section tabs
- [ ] L5-L6 see only own unit tab
- [ ] Tab switching works without errors
- [ ] File downloads work (all roles)
- [ ] No null exceptions when switching tabs
</verify>

<done>
KKJ Matrix verified for all role levels:
- L1-L4 see all sections
- L5-L6 see only own unit
- No errors when switching tabs
- File downloads functional
</done>

---

### Task 5: Smoke test Mapping page (all roles)

<files>
(N/A - browser verification)
</files>

<action>
Verify CPDP Mapping section filtering and file downloads:

1. Login as Worker (L1-L4) → Navigate to `/CMP/Mapping`
2. Verify all section tabs visible
3. Click each tab → Verify files load
4. Click a download link → Verify file downloads
5. Login as SectionHead (L5) → Verify only own unit tab

Focus: Same role-based filtering as KKJ Matrix, file downloads work.
</action>

<verify>
Browser test checklist:
- [ ] L1-L4 see all section tabs
- [ ] L5-L6 see only own unit tab
- [ ] Tab switching works without errors
- [ ] File downloads work (all roles)
- [ ] Dates formatted correctly (if any shown)
</verify>

<done>
Mapping page verified:
- Role-based filtering works correctly
- File downloads functional
- No tab switching errors
</done>

---

### Task 6: Document any remaining bugs

<files>
(N/A - documentation task)
</files>

<action>
Record any bugs found during browser testing:

Categories:
- **Critical:** Crashes, null exceptions, raw errors shown to user
- **High:** Broken flows, incorrect data, navigation failures
- **Medium:** UX issues (unclear text, missing links)
- **Low:** Cosmetic issues, typos

Format:
```
## Bug: [Brief description]
**Severity:** Critical/High/Medium/Low
**Location:** File:line or URL
**Steps to Reproduce:**
1.
2.
3.
**Expected:** [what should happen]
**Actual:** [what actually happens]
```

Decision:
- If Critical/High → Fix immediately or create gap closure plan
- If Medium/Low → Document for future phase
</action>

<verify>
Documentation checklist:
- [ ] All bugs found are documented with severity
- [ ] Each bug has steps to reproduce
- [ ] Expected vs Actual behavior documented
- [ ] Decision made (fix now vs defer)
- [ ] Gap closure plan created if Critical/High bugs found
</verify>

<done>
Bug report created (if issues found):
- All bugs documented with severity and reproduction steps
- Critical/High bugs addressed immediately or gap plan created
- Medium/Low bugs documented for future phases
</done>

---

## Verification Criteria

After completing this plan:

1. [ ] CMP Index hub loads and all cards navigate correctly
2. [ ] Assessment page (Worker) loads both tabs without errors
3. [ ] Records page 2-tab layout works with pagination
4. [ ] KKJ Matrix role-based filtering works correctly
5. [ ] Mapping page role-based filtering works correctly
6. [ ] All dates display in Indonesian locale
7. [ ] No null reference exceptions encountered
8. [ ] Any remaining bugs documented with severity

## Success Criteria

**Phase 93 is COMPLETE when:**
- All CMP pages load without errors for tested roles (Worker, HC, Admin)
- Date localization fixes confirmed working in browser
- Null safety fixes prevent crashes during normal navigation
- No Critical or High severity bugs remain
- Any Medium/Low bugs documented for future phases

## Commit

**No commit needed** — this plan is browser verification only. Bugs found are either:
1. Fixed immediately (if Critical/High and quick fix)
2. Documented for gap closure plans (if complex)

---

**Planned Duration:** 20-30 minutes (browser testing)
**Requires:** Human browser verification
**Depends on:** 93-02 (localization fixes), 93-03 (null safety fixes)
