# Bug Inventory - PlanIdp Page (94-01)

## Bug #1: Missing Indonesian Culture in Date Formatting

**Category:** Localization
**Severity:** Medium
**File:** `Controllers/CDPController.cs`
**Line:** 167

**Issue:**
```csharp
UploadedAt = f.UploadedAt.ToString("dd MMM yyyy")
```

The date formatting in the coaching guidance JSON does not include the Indonesian culture parameter. This will cause dates to display in the server's default locale (likely English) instead of Indonesian.

**Expected Fix:**
```csharp
using System.Globalization;

UploadedAt = f.UploadedAt.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID"))
```

**Impact:**
- Dates in the Guidance tab will show in English (e.g., "Mar 05, 2026") instead of Indonesian (e.g., "05 Mar 2026")
- Affects user experience for Indonesian users

---

## Bug #2: Null Safety Issue on UploadedAt

**Category:** Null Safety
**Severity:** Low
**File:** `Controllers/CDPController.cs`
**Line:** 167

**Issue:**
```csharp
UploadedAt = f.UploadedAt.ToString("dd MMM yyyy")
```

If `UploadedAt` is null (unlikely due to database constraints, but possible in edge cases), this will throw a NullReferenceException.

**Expected Fix:**
```csharp
UploadedAt = f.UploadedAt.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID"))
```
With proper null check in the query or ensuring the database field is NOT NULL.

**Note:** Check if `CoachingGuidanceFiles.UploadedAt` has a NOT NULL constraint in the database. If yes, this is low severity. If no, add null handling.

---

## Verification: No Other Bugs Found

### Checked Items:

1. **Null checks on user context** ✓
   - Line 54-55: User null check with Challenge() return

2. **IsActive filters on ProtonKompetensiList** ✓
   - Line 71: `k.IsActive` filter present
   - Line 107: `k.IsActive` filter present

3. **Coachee section locking** ✓
   - Lines 60-94: Proper coachee section locking implemented
   - Line 77: Forces bagian to coachee's own section

4. **GuidanceDownload action existence** ✓
   - Lines 194-199: GuidanceDownload action exists and is accessible to all authenticated users

5. **"Lihat Semua" button** ✓
   - No "Lihat Semua" button in PlanIdp page (this is expected - only CMP/Mapping has it)
   - PlanIdp uses filter form with cascading dropdowns instead

6. **Silabus tab hierarchical table** ✓
   - Lines 206-235 in view: Proper rowspan merging logic implemented
   - Kompetensi and SubKompetensi rowspan calculation looks correct

7. **Hardcoded strings vs ViewBag JSON** ✓
   - Lines 182-185: ViewBag JSON serialization used correctly
   - Lines 114-120 in view: Script data islands used for JS data
   - No inline Razor in JavaScript (good pattern)

8. **Role-based authorization** ✓
   - CDPController has class-level [Authorize] attribute
   - Coachee role checked and handled correctly

---

## Summary

**Total Bugs Found:** 2 (1 medium, 1 low)
- 1 localization bug (missing Indonesian culture)
- 1 potential null safety issue (depends on DB constraint)

**Code Quality:** Good overall
- Proper null checks on user context
- IsActive filters correctly applied
- Coachee section locking enforced on server
- No hardcoded strings in JS (using ViewBag JSON pattern)
- Proper hierarchical table rendering logic

**Recommended Fix Order:**
1. Fix Bug #1 (localization) - Medium severity, affects UX
2. Verify Bug #2 (null safety) - Check DB constraint, add null handling if needed
