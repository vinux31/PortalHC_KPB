---
wave: 1
depends_on:
  - 94-00
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/CoachingProton.cshtml
  - Models/ProtonViewModels.cs
  - Models/TrackingModels.cs
autonomous: true
requirements:
  - CDP-02
  - CDP-03
---

# Plan 94-02: Coaching Workflow Code Review and Localization (CoachingProton Page)

**Goal:** Review CoachingProton page code and fix localization/validation bugs

**Success criteria:**
1. CoachingProton page loads without errors for Coach, Spv, SectionHead, HC/Admin roles
2. Deliverable status displays correctly (Pending/Submitted/Approved/Rejected/Completed)
3. All dates display in Indonesian locale
4. Null checks prevent InvalidOperationException on DateTime properties
5. Status checks use correct enum values (no "Active" checks)

---

## Tasks

### Task 94-02-01: Code review - CoachingProton action and view

<files>
- Controllers/CDPController.cs (lines 400-800: CoachingProton action)
- Views/CDP/CoachingProton.cshtml (1539 lines)
</files>

<action>
Review CDPController.CoachingProton action and Views/CDP/CoachingProton.cshtml (1539 lines)

Steps:
1. Read `Controllers/CDPController.cs` CoachingProton action (lines 400-800)
2. Read `Views/CDP/CoachingProton.cshtml` complete file
3. Check for common bug patterns:
   - Missing null checks on DateTime properties
   - Missing Indonesian culture in date formatting
   - Missing IsActive filters on coachee scope queries
   - Missing role-based coachee scope for SrSpv/SH (level 4)
   - Status enum mismatches (checking for "Active" instead of valid statuses)
   - Pagination splitting coachee groups across pages
   - AJAX actions missing CSRF protection
</action>

<verify>
Bug inventory list created with categories and severity

Expected findings verified:
- Coachee scope queries include IsActive filter (Phase 85-02 bug pattern)
- SrSpv/SH (level 4) coachee dropdown exists (Phase 85-03 bug pattern)
- Status checks use correct enum values (Pending/Submitted/Approved/Rejected/Completed)
- Pagination preserves group boundaries (coachee groups not split)
- AJAX actions (ApproveFromProgress, RejectFromProgress, HCReviewFromProgress) have ValidateAntiForgeryToken
</verify>

<done>
Bug inventory list saved to .planning/phases/94-cdp-section-audit/94-02-BUGS.md
</done>

---

### Task 94-02-02: Fix localization and validation bugs in CoachingProton

<files>
- Controllers/CDPController.cs
- Views/CDP/CoachingProton.cshtml
</files>

<action>
Add Indonesian date formatting and fix validation issues

Steps:
1. Add `@using System.Globalization` to `Views/CDP/CoachingProton.cshtml`
2. Find all date formatting calls and add Indonesian culture
3. Add null checks for nullable DateTime properties
4. Fix any status enum mismatches (e.g., checking for "Active" instead of "Pending")
5. Add parameter validation to CoachingProton action:
   - Null user check
   - Invalid progressId validation
   - Role-based access validation

Example fixes:
```csharp
// Date localization fix
@model.SubmittedAt.Value.ToString("dd MMM yyyy HH:mm",
    CultureInfo.GetCultureInfo("id-ID"))

// Null safety
@if (model.SubmittedAt.HasValue)
{
    @model.SubmittedAt.Value.ToString("dd MMM yyyy HH:mm",
        CultureInfo.GetCultureInfo("id-ID"))
}

// Status enum fix (avoid "Active" check)
if (progress.Status != "Pending" && progress.Status != "Submitted")
{
    // Invalid status for this action
}
```
</action>

<verify>
Build passes without errors
All dates include Indonesian culture
No status checks for "Active" (use valid statuses only)
Null checks prevent InvalidOperationException
</verify>

<done>
Commit created:
fix(cdp): add localization and validation to CoachingProton page

- Add Indonesian date formatting (CultureInfo.GetCultureInfo("id-ID"))
- Add null checks for nullable DateTime properties
- Fix status enum mismatches (use Pending/Submitted/Approved/Rejected/Completed)
- Add parameter validation to CoachingProton action

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
</done>

---

## Verification Criteria

**Manual browser verification:**
1. Log in as Coach → verify CoachingProton page loads
2. Log in as Spv → verify CoachingProton page loads
3. Log in as SectionHead → verify CoachingProton page loads
4. Log in as HC/Admin → verify CoachingProton page loads
5. Check all displayed dates are in Indonesian format
6. Verify no raw exceptions are displayed

**Expected result:** CDP-02, CDP-03 requirements PASS for localization and validation — page loads correctly, dates use Indonesian locale, no null reference exceptions

---

## Must-Haves (Goal-Backward Verification)

If any of these are FALSE, the plan failed:

1. [ ] CoachingProton page loads for Coach, Spv, SectionHead, HC/Admin roles
2. [ ] Deliverable status uses correct enum values (no "Active" checks)
3. [ ] All dates display in Indonesian locale
4. [ ] All nullable DateTime properties have null checks
5. [ ] No raw exceptions displayed to user
6. [ ] Build passes without errors

---

*Plan created: 2026-03-05*
*Phase: 94-cdp-section-audit*
*Requirements: CDP-02, CDP-03, CDP-05*
