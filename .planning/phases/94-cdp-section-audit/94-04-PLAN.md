---
wave: 1
depends_on:
  - 94-00
files_modified:
  - Controllers/CDPController.cs
  - Views/CDP/Index.cshtml
autonomous: true
requirements:
  - CDP-01
---

# Plan 94-04: CDP Index Hub Page Audit

**Goal:** Verify CDP Index hub page loads correctly with proper statistics and navigation for all roles

**Success criteria:**
1. CDP Index page loads without errors for all 5 roles (Coachee, Coach, Spv, SectionHead, HC/Admin)
2. Dashboard statistics display correctly (Proton progress counts, approval counts)
3. Navigation cards display and link to correct CDP sections
4. Role-based access control works (Coachee sees only their data, HC sees all)
5. All dates display in Indonesian locale

---

## Tasks

### Task 94-04-01: Code review - CDP Index action and view

<files>
- Controllers/CDPController.cs (Index action)
- Views/CDP/Index.cshtml (101 lines)
</files>

<action>
Review CDPController.Index action and Views/CDP/Index.cshtml

Steps:
1. Read `Controllers/CDPController.cs` Index action
2. Read `Views/CDP/Index.cshtml` complete file (101 lines)
3. Check for common bug patterns:
   - Missing null checks on DateTime properties
   - Missing Indonesian culture in date formatting
   - Missing role-based filtering on dashboard statistics
   - Missing IsActive filters on user queries
   - Incorrect statistics calculations
   - Broken navigation links to CDP sections
</action>

<verify>
Bug inventory list created with categories and severity

Expected findings verified:
- Role-based filtering on dashboard statistics checked
- Date formatting checked for Indonesian culture
- Navigation links verified for correct targets
- Statistics calculations verified for accuracy
</verify>

<done>
Bug inventory list saved to .planning/phases/94-cdp-section-audit/94-04-BUGS.md
</done>

---

### Task 94-04-02: Fix localization and validation bugs in CDP Index page

<files>
- Controllers/CDPController.cs
- Views/CDP/Index.cshtml
</files>

<action>
Add Indonesian date formatting and fix validation issues in CDP Index page

Steps:
1. Add `@using System.Globalization` to `Views/CDP/Index.cshtml`
2. Find all date formatting calls and add Indonesian culture
3. Add null checks for nullable DateTime properties
4. Verify role-based filtering on statistics queries:
   ```csharp
   // Coachee should see only their stats
   if (isCoachee)
   {
       stats = stats.Where(s => s.CoacheeId == currentUserId);
   }

   // Ensure IsActive filter
   stats = stats.Where(s => s.User.IsActive);
   ```
5. Verify navigation cards link to correct actions:
   - Plan IDP → /CDP/PlanIdp
   - Coaching Proton → /CDP/CoachingProton
   - Dashboard → /CDP/Dashboard
   - Progress → /CDP/Progress

Example fix:
```csharp
// Date localization fix
@item.SubmittedAt.Value.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID"))

// Null safety
@if (item.SubmittedAt.HasValue)
{
    @item.SubmittedAt.Value.ToString("dd MMM yyyy", CultureInfo.GetCultureInfo("id-ID"))
}
```
</action>

<verify>
Build passes without errors
All dates include Indonesian culture
All nullable DateTime properties have null checks
Statistics queries include role-based filtering and IsActive filter
Navigation cards link to correct CDP actions
</verify>

<done>
Commit created:
fix(cdp): add localization and validation to CDP Index page

- Add Indonesian date formatting (CultureInfo.GetCultureInfo("id-ID"))
- Add null checks for nullable DateTime properties
- Verify role-based filtering on dashboard statistics
- Ensure IsActive filter on statistics queries
- Verify navigation cards link to correct CDP sections

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
</done>

---

## Verification Criteria

**Manual browser verification:**
1. Log in as Coachee → verify CDP Index loads and shows only their stats
2. Log in as Coach → verify CDP Index loads with coach-level stats
3. Log in as Spv → verify CDP Index loads with Spv-level stats
4. Log in as SectionHead → verify CDP Index loads with section-level stats
5. Log in as HC/Admin → verify CDP Index loads with full stats
6. Check all displayed dates are in Indonesian format
7. Click each navigation card → verify links work correctly
8. Verify statistics counts are accurate (compare with database)

**Expected result:** CDP-01 requirements PASS for CDP Index hub — page loads without errors for all 5 roles, statistics display correctly, navigation works

---

## Must-Haves (Goal-Backward Verification)

If any of these are FALSE, the plan failed:

1. [ ] CDP Index page loads for all 5 roles without server errors
2. [ ] Dashboard statistics display correctly per role (Coachee sees only their data)
3. [ ] All dates display in Indonesian locale
4. [ ] All nullable DateTime properties have null checks
5. [ ] Statistics queries include role-based filtering and IsActive filter
6. [ ] Navigation cards link to correct CDP actions
7. [ ] No raw exceptions displayed to user
8. [ ] Build passes without errors

---

*Plan created: 2026-03-05*
*Phase: 94-cdp-section-audit*
*Requirements: CDP-01*
