---
wave: 2
depends_on:
  - 98-01
files_modified:
  - .planning/phases/98-data-integrity-audit/98-02-CASCADE-VERIFICATION.md
autonomous: true
requirements:
  - DATA-02
---

# Plan 98-02: Soft-Delete Cascade Verification

> Code review audit of soft-delete cascade behavior across all entity relationships

## Goal

Verify that soft-delete operations cascade correctly across entity relationships without creating orphaned records. Analyze Entity Framework relationships (HasForeignKey, OnDelete) and manual cascade logic in controllers to ensure child records are hidden or cleaned up when parent records are soft-deleted.

## Requirements Mapping

- **DATA-02** (Soft-delete operations cascade correctly): Verify no orphaned records exist when parents are soft-deleted

## Success Criteria

1. All soft-delete cascade relationships documented with parent-child entity pairs
2. EF Core cascade behaviors documented (hard delete cascades vs soft-delete manual logic)
3. Manual cascade logic audited in controllers for soft-delete entities
4. Orphan handling strategy documented (auto-hide via IsActive filter vs manual cleanup)
5. Cross-entity consistency checks identified for plan 98-03 fixes

## Tasks

### Task 98-02-01: Document EF Core cascade behaviors (hard delete)

**Action:** Audit ApplicationDbContext.cs for all OnDelete cascade behaviors

**Output file:** `.planning/phases/98-data-integrity-audit/98-02-CASCADE-VERIFICATION.md`

**EF Core cascade audit format:**

```markdown
# Soft-Delete Cascade Verification - Phase 98

## EF Core Cascade Behaviors (Hard Delete)

### ApplicationDbContext Configuration (Data/ApplicationDbContext.cs)

#### TrainingRecord → User Cascade
**Lines:** <line_range>
**Configuration:**
```csharp
builder.Entity<TrainingRecord>(entity =>
{
    entity.HasOne(t => t.User)
        .WithMany(u => u.TrainingRecords)
        .HasForeignKey(t => t.UserId)
        .OnDelete(DeleteBehavior.Cascade);  // Hard delete cascade
});
```
**Behavior:** When User is hard-deleted → all TrainingRecords cascade deleted
**Notes:** TrainingRecord has no IsActive field - uses hard delete only

#### AssessmentSession → User Cascade
**Lines:** <line_range>
**Configuration:**
```csharp
builder.Entity<AssessmentSession>(entity =>
{
    entity.HasOne(a => a.User)
        .WithMany()
        .HasForeignKey(a => a.UserId)
        .OnDelete(DeleteBehavior.Cascade);  // Hard delete cascade
});
```
**Behavior:** When User is hard-deleted → all AssessmentSessions cascade deleted
**Notes:** AssessmentSession has no IsActive field - uses hard delete only

#### <Other EF Core cascades>
**Lines:** <line_range>
**Configuration:** <copy from code>
**Behavior:** <describe cascade>
**Notes:** <any relevant info>

## Hard Delete Cascade Summary

| Parent Entity | Child Entity | Cascade Type | Child Has IsActive? | Notes |
|---------------|--------------|--------------|---------------------|-------|
| ApplicationUser | TrainingRecord | EF Cascade | No | Hard delete only |
| ApplicationUser | AssessmentSession | EF Cascade | No | Hard delete only |
| ... | ... | ... | ... | ... |

**Key insight:** Hard delete cascades are automatic via EF Core. Soft-delete cascades require manual logic.
```

**Verification:**
- All EF Core OnDelete behaviors documented from ApplicationDbContext.cs
- Hard delete cascades summarized in table
- Child entities' IsActive status documented

---

### Task 98-02-02: Document soft-delete entities and their relationships

**Action:** Create relationship map for all entities with IsActive fields

**Relationship map format:**

```markdown
## Soft-Delete Entities and Relationships

### ApplicationUser (Worker)
**IsActive field:** Phase 83
**Child entities (via No-FK string IDs):**
- CoachCoacheeMapping.CoachId (when Coach deleted)
- CoachCoacheeMapping.CoacheeId (when Coachee deleted)
- ProtonTrackAssignment.CoacheeId (when Coachee deleted)
- AssessmentSession.UserId (hard delete cascade - already handled by EF)

**Expected cascade behavior:**
- When Worker.IsActive = false → hide all CoachCoacheeMapping where this user is Coach
- When Worker.IsActive = false → hide all CoachCoacheeMapping where this user is Coachee
- When Worker.IsActive = false → hide all ProtonTrackAssignment for this coachee

### CoachCoacheeMapping
**IsActive field:** Yes (per ProtonModels.cs)
**Parent entities:**
- Coach (ApplicationUser)
- Coachee (ApplicationUser)
**Child entities:** None (leaf node)

**Expected cascade behavior:**
- When Coach.IsActive = false → hide all CoachCoacheeMapping where this user is Coach
- When Coachee.IsActive = false → hide all CoachCoacheeMapping where this user is Coachee
- No child entities to cascade to

### ProtonTrackAssignment
**IsActive field:** Yes
**Parent entities:**
- Coachee (ApplicationUser.CoacheeId)
- ProtonTrack (ProtonTrack.Id)
**Child entities:**
- ProtonDeliverableProgress (via TrackAssignmentId)

**Expected cascade behavior:**
- When Coachee.IsActive = false → hide all ProtonTrackAssignment for this coachee
- When ProtonKompetensi.IsActive = false → hide all ProtonTrackAssignment for this silabus
- When ProtonTrackAssignment.IsActive = false → hide all ProtonDeliverableProgress

### ProtonKompetensi (Silabus)
**IsActive field:** Phase 83
**Child entities:**
- ProtonTrack (via ProtonTrackId FK)
- ProtonTrackAssignment (via ProtonTrackId)
- ProtonDeliverable (via KompetensiId FK)

**Expected cascade behavior:**
- When ProtonKompetensi.IsActive = false → hide all ProtonTrack for this silabus
- When ProtonKompetensi.IsActive = false → hide all ProtonTrackAssignment for this silabus
- When ProtonKompetensi.IsActive = false → hide all ProtonDeliverable for this silabus

## No-FK Relationship Pattern (Portal-Specific)

**Background:** Proton models use string ID properties without EF Core navigation properties or ForeignKey attributes. This avoids cascade delete complications but requires manual join/consistency logic.

**Entities using No-FK pattern:**
- CoachCoacheeMapping (CoachId, CoacheeId - no FK to ApplicationUser)
- ProtonTrackAssignment (CoacheeId - no FK to ApplicationUser)
- ProtonDeliverableProgress (TrackAssignmentId - no FK to ProtonTrackAssignment)
- CoachingLog (CoacheeId, CoachId - no FKs)

**Implication:** EF Core does NOT auto-cascade soft-deletes. Manual query logic required.

## Relationship Summary Table

| Parent Entity | Child Entity | Relationship Type | Cascade Method | Child Has IsActive? |
|---------------|--------------|-------------------|----------------|---------------------|
| ApplicationUser | CoachCoacheeMapping (as Coach) | No-FK string ID | Manual query filter | Yes |
| ApplicationUser | CoachCoacheeMapping (as Coachee) | No-FK string ID | Manual query filter | Yes |
| ApplicationUser | ProtonTrackAssignment | No-FK string ID | Manual query filter | Yes |
| ProtonKompetensi | ProtonTrack | FK with navigation | EF cascade (hard delete) | No |
| ProtonKompetensi | ProtonTrackAssignment | No-FK (via ProtonTrackId) | Manual query filter | Yes |
| ProtonKompetensi | ProtonDeliverable | FK with navigation | EF cascade (hard delete) | No |
| ProtonTrackAssignment | ProtonDeliverableProgress | No-FK string ID | Manual query filter | Yes |
```

**Verification:**
- All 4 soft-delete entities documented with parent-child relationships
- Cascade method identified (EF cascade vs manual query filter)
- No-FK pattern entities listed

---

### Task 98-02-03: Audit manual cascade logic in controllers

**Action:** Code review of all soft-delete actions in controllers

**Grep search:**

```bash
cd /c/Users/Administrator/Desktop/PortalHC_KPB

# Grep all soft-delete actions (IsActive = false assignments)
grep -rn "IsActive = false" Controllers/ --include="*.cs" | tee .planning/phases/98-data-integrity-audit/grep-soft-delete.txt

# Grep all reactivate actions (IsActive = true assignments)
grep -rn "IsActive = true" Controllers/ --include="*.cs" | tee .planning/phases/98-data-integrity-audit/grep-reactivate.txt
```

**Manual cascade logic audit:**

For each soft-delete action found:

1. **DeactivateWorker** (AdminController)
   ```csharp
   // Line: <search>
   // Action: user.IsActive = false;
   // Cascade check: Does it update CoachCoacheeMapping? ProtonTrackAssignment?
   // Expected: Manual cascade or child queries filter by user.IsActive
   ```

2. **DeactivateMapping** (if exists)
   ```csharp
   // Line: <search>
   // Action: mapping.IsActive = false;
   // Cascade check: N/A (leaf node, no children)
   ```

3. **DeactivateSilabus** (ProtonDataController)
   ```csharp
   // Line: <search>
   // Action: silabus.IsActive = false;
   // Cascade check: Does it update ProtonTrackAssignment?
   // Expected: Manual cascade or child queries filter by silabus.IsActive
   ```

**Output:** Add "Manual Cascade Logic Audit" section to 98-02-CASCADE-VERIFICATION.md:

```markdown
## Manual Cascade Logic Audit

### DeactivateWorker Action (AdminController)
**Line:** <actual_line>
**Code:**
```csharp
<copy_deactivate_worker_action>
```
**Cascade logic:**
- ✅ Manual cascade implemented OR
- ❌ No cascade logic (relies on child query filters)
- **Child entities affected:** CoachCoacheeMapping, ProtonTrackAssignment
- **Expected child query behavior:** `.Where(m => m.Coach.IsActive && m.Coachee.IsActive)`

### DeactivateSilabus Action (ProtonDataController)
**Line:** <actual_line>
**Code:**
```csharp
<copy_deactivate_silabus_action>
```
**Cascade logic:**
- ✅ Manual cascade implemented OR
- ❌ No cascade logic (relies on child query filters)
- **Child entities affected:** ProtonTrackAssignment
- **Expected child query behavior:** `.Where(a => a.ProtonKompetensi.IsActive)`

### <Other soft-delete actions>
**Line:** <actual_line>
**Code:**
```csharp
<copy_action_code>
```
**Cascade logic:** <analyze>

## Manual Cascade Summary

| Action | Parent Entity | Cascade Implemented? | Child Entities Affected | Child Query Filter Required? |
|--------|---------------|----------------------|-------------------------|------------------------------|
| DeactivateWorker | ApplicationUser | ✅ Yes / ❌ No | CoachCoacheeMapping, ProtonTrackAssignment | Yes / No |
| DeactivateSilabus | ProtonKompetensi | ✅ Yes / ❌ No | ProtonTrackAssignment | Yes / No |
| ... | ... | ... | ... | ... |
```

**Verification:**
- grep-soft-delete.txt contains 5+ lines (DeactivateWorker, DeactivateSilabus, etc.)
- All soft-delete actions audited for cascade logic
- Child query filter requirements documented

---

### Task 98-02-04: Verify child query filters for orphan prevention

**Action:** Code review of child entity queries to verify orphan prevention

**Child query categories to verify:**

1. **CoachCoacheeMapping queries**
   - Do they filter by `Coach.IsActive && Coachee.IsActive`?
   - Location: CDPController.CoachingProton, AdminController.CoachCoacheeMapping

2. **ProtonTrackAssignment queries**
   - Do they filter by `Coachee.IsActive`?
   - Do they filter by `ProtonKompetensi.IsActive` (via ProtonTrack)?
   - Location: CDPController.PlanIdp

3. **ProtonDeliverableProgress queries**
   - Do they filter by `ProtonTrackAssignment.IsActive`?
   - Location: CDPController.CoachingProton, Deliverable

**Query filter verification:**

```bash
cd /c/Users/Administrator/Desktop/PortalHC_KPB

# Grep CoachCoacheeMapping queries
grep -rn "CoachCoacheeMapping" Controllers/CDPController.cs | grep -A 5 "ToListAsync" | tee .planning/phases/98-data-integrity-audit/grep-mapping-queries.txt

# Grep ProtonTrackAssignment queries
grep -rn "ProtonTrackAssignment" Controllers/CDPController.cs | grep -A 5 "ToListAsync" | tee .planning/phases/98-data-integrity-audit/grep-assignment-queries.txt

# Grep ProtonDeliverableProgress queries
grep -rn "ProtonDeliverableProgress" Controllers/CDPController.cs | grep -A 5 "ToListAsync" | tee .planning/phases/98-data-integrity-audit/grep-deliverable-queries.txt
```

**Output:** Add "Child Query Filter Verification" section to 98-02-CASCADE-VERIFICATION.md:

```markdown
## Child Query Filter Verification

### CoachCoacheeMapping Queries

#### CDPController.CoachingProton (HC/Admin branch)
**Line:** <actual_line>
**Query:**
```csharp
<copy_query_from_code>
```
**Filters:**
- ❌ No IsActive filter OR
- ✅ `.Where(m => m.IsActive)` only OR
- ✅ `.Where(m => m.IsActive && m.Coach.IsActive && m.Coachee.IsActive)` (ideal)
**Orphan risk:** High / Medium / Low
**Gap:** If no parent.IsActive check, orphaned mappings leak to UI

#### CDPController.CoachingProton (Coach branch)
**Line:** <actual_line>
**Query:**
```csharp
<copy_query_from_code>
```
**Filters:**
- <same analysis>
**Orphan risk:** High / Medium / Low

#### AdminController.CoachCoacheeMapping
**Line:** <actual_line>
**Query:**
```csharp
<copy_query_from_code>
```
**Filters:**
- <same analysis>
**Orphan risk:** High / Medium / Low

### ProtonTrackAssignment Queries

#### CDPController.PlanIdp (Coachee assignment lookup)
**Line:** <actual_line>
**Query:**
```csharp
<copy_query_from_code>
```
**Filters:**
- ❌ No IsActive filter OR
- ✅ `.Where(a => a.IsActive)` only OR
- ✅ `.Where(a => a.IsActive && a.Coachee.IsActive)` (ideal)
**Orphan risk:** High / Medium / Low

### ProtonDeliverableProgress Queries

#### CDPController.CoachingProton (coachee dropdown)
**Line:** <actual_line>
**Query:**
```csharp
<copy_query_from_code>
```
**Filters:**
- ❌ No IsActive filter OR
- ✅ `.Where(p => p.ProtonTrackAssignment.IsActive)` (ideal)
**Orphan risk:** High / Medium / Low

## Orphan Prevention Summary

| Child Query | Filters Child.IsActive? | Filters Parent.IsActive? | Orphan Risk | Fix Required? |
|-------------|-------------------------|--------------------------|-------------|---------------|
| CoachingProton (HC/Admin) | Yes / No | Yes / No | High / Medium / Low | Yes / No |
| CoachingProton (Coach) | Yes / No | Yes / No | High / Medium / Low | Yes / No |
| CoachCoacheeMapping | Yes / No | Yes / No | High / Medium / Low | Yes / No |
| PlanIdp (assignment) | Yes / No | Yes / No | High / Medium / Low | Yes / No |
| Deliverable (progress) | Yes / No | Yes / No | High / Medium / Low | Yes / No |

## Cross-Entity Consistency Checks

### Active User → Inactive Coach Mapping
**Scenario:** User A is active, assigned to Coach B, then Coach B is deactivated (IsActive=false)
**Current behavior:** <analyze from code>
**Expected behavior:** Mapping should not appear in Coaching Proton page
**Gap:** <if gap exists, describe>

### Active User → Deleted Silabus Track
**Scenario:** User A has active ProtonTrackAssignment to Silabus X, then Silabus X is deleted (IsActive=false)
**Current behavior:** <analyze from code>
**Expected behavior:** Track assignment should not appear in Plan IDP
**Gap:** <if gap exists, describe>

### <Other cross-entity scenarios>
**Scenario:** <describe>
**Current behavior:** <analyze>
**Expected behavior:** <describe>
**Gap:** <if gap exists, describe>
```

**Verification:**
- All child entity queries audited for IsActive filters
- Parent.IsActive checks documented
- Orphan risk assessed (High/Medium/Low)
- Cross-entity consistency scenarios analyzed

---

### Task 98-02-05: Document orphan handling strategy

**Action:** Summarize portal's orphan record handling strategy

**Output:** Add "Orphan Handling Strategy" section to 98-02-CASCADE-VERIFICATION.md:

```markdown
## Orphan Handling Strategy

### Current Portal Approach

**Primary strategy:** Auto-hide via IsActive query filters
- Child queries filter by parent.IsActive status
- No manual cascade updates required
- Orphaned records remain in database but hidden from UI

**Example:**
```csharp
// Instead of cascading IsActive=false to children:
// mapping.IsActive = false;  // ❌ Manual cascade

// Child query filters by parent status:
var mappings = await _context.CoachCoacheeMappings
    .Where(m => m.IsActive && m.Coach.IsActive && m.Coachee.IsActive)
    .ToListAsync();  // ✅ Auto-hide orphans
```

**Advantages:**
- Simpler code (no cascade logic)
- Reversible (reactivate parent → children reappear)
- Preserves audit trail (orphaned records not deleted)

**Disadvantages:**
- Database accumulates orphaned records over time
- Requires consistent IsActive filtering in all queries
- No automatic cleanup

### Alternative Strategies (Not Currently Used)

1. **Manual cascade updates:**
   - Set child.IsActive = false when parent soft-deleted
   - Requires more code but cleaner database

2. **Scheduled cleanup jobs:**
   - Background job deletes or archives orphaned records
   - Reduces database bloat but adds complexity

3. **Hard delete with audit table:**
   - Physically delete records, log to AuditLog
   - Irreversible but simple

### Recommendations

**Short-term (Phase 98):**
- ✅ Keep current auto-hide strategy (pragmatic)
- ✅ Fix missing IsActive filters in child queries
- ✅ Document orphan handling in PROJECT.md

**Long-term (Future phase):**
- Consider manual cascade for critical entities (CoachCoacheeMapping)
- Consider scheduled cleanup job for orphaned records > 1 year old
- Monitor database size impact of orphan accumulation

## Strategy Summary

| Aspect | Current Approach | Recommended Change |
|--------|-----------------|-------------------|
| Orphan prevention | Auto-hide via query filters | ✅ Keep (add missing filters) |
| Manual cascade | None (except Phase 83 DeactivateWorker) | Consider for critical entities |
| Scheduled cleanup | None | Evaluate in future phase |
| Audit trail | Orphaned records preserved | ✅ Keep (good for investigation) |
```

**Verification:**
- Current orphan handling strategy documented
- Advantages and disadvantages analyzed
- Recommendations documented (short-term and long-term)

---

## Execution Notes

- **Grep patterns:** Use exact patterns from tasks - adjust if grep produces too much noise
- **Line numbers:** Verify from actual code files - research line numbers are approximate
- **Orphan risk levels:**
  - **High:** Orphaned records leak to user-facing UI (mappings, assignments visible when parent inactive)
  - **Medium:** Internal queries without parent.IsActive check (low exposure but inconsistent)
  - **Low:** Admin-only pages with showInactive toggle
- **Cross-entity scenarios:** Focus on high-risk scenarios from context (active user → inactive coach, active user → deleted silabus)
- **No-FK pattern:** Emphasize that EF Core does NOT auto-cascade soft-deletes - manual query logic required

## Completion Criteria

- [ ] EF Core cascade behaviors documented (all OnDelete configurations)
- [ ] Soft-delete entity relationships mapped (parent-child pairs)
- [ ] Manual cascade logic audited (all soft-delete actions reviewed)
- [ ] Child query filters verified (orphan prevention checks)
- [ ] Cross-entity consistency scenarios analyzed
- [ ] Orphan handling strategy documented
- [ ] Fix recommendations documented for plan 98-03
- [ ] 98-02-CASCADE-VERIFICATION.md created with all sections

---

**Plan 98-02 completes soft-delete cascade verification - results feed into plan 98-03 (bug fixes)**
