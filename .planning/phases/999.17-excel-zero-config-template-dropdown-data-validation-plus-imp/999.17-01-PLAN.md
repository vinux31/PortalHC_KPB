---
phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Helpers/QuestionTemplateBuilder.cs
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/QuestionTemplateBuilderTests.cs
autonomous: true
requirements: [DRP-01, DRP-02, SKR-01]
user_setup: []

must_haves:
  truths:
    - "Admin yang unduh template Universal mendapat dropdown 3 nilai (MultipleChoice/MultipleAnswer/Essay) di kolom QuestionType (L) saat mengetik di baris 2-1000"
    - "Admin yang unduh template legacy (MC/MA/Essay) juga mendapat dropdown QuestionType di kolom H (8)"
    - "Kolom Jawaban Benar dan kolom lain TIDAK mendapat dropdown"
    - "Template Universal punya kolom 'Skor' di posisi 14 (N) dengan Data Validation WholeNumber Between 1-100"
    - "Template legacy TIDAK punya kolom Skor (kompatibel-mundur D-07)"
  artifacts:
    - path: "Helpers/QuestionTemplateBuilder.cs"
      provides: "Pure static XLWorkbook builder dengan DataValidation; unit-testable tanpa DB"
      contains: "CreateDataValidation"
      min_lines: 60
    - path: "HcPortal.Tests/QuestionTemplateBuilderTests.cs"
      provides: "Read-back assertions DataValidation per varian template"
      contains: "DataValidations.TryGet"
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "DownloadQuestionTemplate delegasi tipis ke QuestionTemplateBuilder.Build"
      contains: "QuestionTemplateBuilder.Build"
  key_links:
    - from: "Controllers/AssessmentAdminController.cs (DownloadQuestionTemplate)"
      to: "Helpers/QuestionTemplateBuilder.Build"
      via: "delegasi return ExcelExportHelper.ToFileResult(QuestionTemplateBuilder.Build(type), ...)"
      pattern: "QuestionTemplateBuilder\\.Build"
    - from: "QuestionTemplateBuilder.Build (Universal)"
      to: "Excel cell N2:N1000"
      via: "ws.Range(\"N2:N1000\").CreateDataValidation().WholeNumber.Between(1,100)"
      pattern: "N2:N1000"
---

<objective>
Jadikan template Excel soal "zero-config" di sisi DOWNLOAD: (1) dropdown Data Validation untuk kolom `QuestionType` di SEMUA template (D-01/D-02/D-03), (2) kolom `Skor` baru di template Universal (kolom 14 = N) dengan Data Validation numeric 1-100 (D-07/D-10), (3) kolom lain TANPA dropdown (D-04/D-05).

Cara: ekstrak body generator dari `DownloadQuestionTemplate` ke helper pure `QuestionTemplateBuilder.Build(type) → XLWorkbook` (meniru `Helpers/InjectExcelHelper.GenerateTemplate`), pasang DataValidation di helper agar unit-testable TANPA DB/controller (~12 ctor arg). Controller jadi tipis.

Purpose: Anti salah-ketik QuestionType + memungkinkan set bobot per-soal lewat Excel — selaras tema zero-config. DataValidation = greenfield di repo (`git grep DataValidation -- '*.cs'` = 0), API ClosedXML 0.105.0 sudah diverifikasi penuh (RESEARCH §Pattern 2 + §Code Examples).

Output: `Helpers/QuestionTemplateBuilder.cs` (baru), `HcPortal.Tests/QuestionTemplateBuilderTests.cs` (baru), `DownloadQuestionTemplate` ramping di controller.

Migration = FALSE (tidak menyentuh DB/schema). TIDAK menyentuh grading, parser import, atau form authoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-CONTEXT.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-RESEARCH.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-PATTERNS.md

<interfaces>
<!-- Kontrak yang executor butuhkan. Diekstrak dari codebase 2026-06-30. Pakai langsung — no exploration. -->

# Helper analog (POLA RUJUKAN bentuk pure static builder) — Helpers/InjectExcelHelper.cs:
namespace HcPortal.Helpers
{
    public static class InjectExcelHelper
    {
        public static XLWorkbook GenerateTemplate(/* args */) { ... return workbook; }
    }
}

# Stream-out helper (JANGAN ubah) — Helpers/ExcelExportHelper.cs:
#   ExcelExportHelper.ToFileResult(XLWorkbook workbook, string fileName, Controller ctrl)
#   → AdjustToContents() + SaveAs(MemoryStream) + File(..., xlsx MIME). DataValidation persist sempurna.

# Body yang DIPINDAH (verbatim) — Controllers/AssessmentAdminController.cs:7273-7344 (DownloadQuestionTemplate):
#   - whitelist type { "MC","MA","Essay","Universal" } (boleh ikut pindah atau tetap di controller)
#   - headers Universal 13-kolom vs legacy 9-kolom (lihat di bawah)
#   - AddExampleRow (gray italic) + AddInstruction (DarkRed) helper lokal
# Header Universal SAAT INI (13-kolom) — AAC:7281:
#   { "Pertanyaan","Opsi A","Opsi B","Opsi C","Opsi D","Opsi E","Opsi F","Jawaban Benar","No. Section","Nama Section","Elemen Teknis","QuestionType","Rubrik" }
#   Kolom map: Pertanyaan=1 ... Jawaban Benar=8(H) ... ET=11 QuestionType=12(L) Rubrik=13(M)  → [BARU] Skor=14(N)
# Header legacy SAAT INI (9-kolom) — AAC:7282:
#   { "Pertanyaan","Opsi A","Opsi B","Opsi C","Opsi D","Jawaban Benar","Elemen Teknis","QuestionType","Rubrik" }
#   Kolom map: Jawaban Benar=6(F) ... QuestionType=8(H) Rubrik=9(I)  → TANPA Skor (D-07)

# DataValidation API (ClosedXML 0.105.0, VERIFIED DLL reflection) — RESEARCH §Pattern 2:
#   var dv = ws.Range("L2:L1000").CreateDataValidation();
#   dv.List("\"MultipleChoice,MultipleAnswer,Essay\"", true);   // literal double-quotes WAJIB; inCellDropdown=true
#   dv.WholeNumber.Between(1, 100);                              // overload (Int32,Int32)
#   props: AllowedValues(XLAllowedValues{List,WholeNumber,...}), Operator(XLOperator{Between,...}),
#          MinValue/MaxValue/Value (string), InCellDropdown/IgnoreBlanks (bool), ErrorStyle(XLErrorStyle)
# Read-back (test): ws.DataValidations.TryGet(ws.Range("L2:L1000").RangeAddress, out var dv) → bool
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ekstrak QuestionTemplateBuilder (seam pure) + delegasi controller — refactor dengan test struktur</name>
  <files>Helpers/QuestionTemplateBuilder.cs (baru), Controllers/AssessmentAdminController.cs (DownloadQuestionTemplate), HcPortal.Tests/QuestionTemplateBuilderTests.cs (baru)</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs:7264-7348 (DownloadQuestionTemplate — body yang dipindah; perhatikan `using var workbook`, headers, AddExampleRow, AddInstruction, ToFileResult)
    - Helpers/InjectExcelHelper.cs:1-53 (analog bentuk: static class HcPortal.Helpers, return XLWorkbook)
    - Helpers/ExcelExportHelper.cs:28-39 (ToFileResult — JANGAN ubah, dipakai apa-adanya)
    - HcPortal.Tests/InjectExcelHelperTests.cs (analog test pure helper, no DB, no [Trait Integration])
    - 999.17-PATTERNS.md §"QuestionTemplateBuilder.cs" + §"DownloadQuestionTemplate"
  </read_first>
  <action>
    1. Buat `Helpers/QuestionTemplateBuilder.cs`: `namespace HcPortal.Helpers; public static class QuestionTemplateBuilder` dengan method `public static XLWorkbook Build(string type)`.
       - PINDAHKAN VERBATIM body `DownloadQuestionTemplate` baris 7273-7344 (workbook+worksheet "Question Import", `bool universal = type == "Universal"`, headers array, loop header styling, AddExampleRow, AddInstruction, semua example/instruction rows). JANGAN tambah DataValidation di task ini (itu Task 2 — refactor dulu dengan perilaku identik, baru tambah DV test-first).
       - JANGAN pakai `using var workbook` di dalam Build (helper mengembalikan workbook hidup); cukup `var workbook = new XLWorkbook();` ... `return workbook;` (identik `InjectExcelHelper.GenerateTemplate`).
       - Normalisasi whitelist `type` ({ "MC","MA","Essay","Universal" }, default "MC") boleh ikut pindah ke awal Build ATAU tetap di controller. Pilih: pindah ke Build agar helper self-contained.
    2. Ramping `DownloadQuestionTemplate` (AAC:7267) jadi: `using var workbook = QuestionTemplateBuilder.Build(type); var fileName = $"Template_Soal_{type}.xlsx"; return ExcelExportHelper.ToFileResult(workbook, fileName, this);`. Pertahankan atribut `[HttpGet]` + `[Authorize(Roles = "Admin, HC")]`. Pakai `using var` di controller agar workbook ter-dispose setelah ToFileResult menulis ke stream.
    3. Buat `HcPortal.Tests/QuestionTemplateBuilderTests.cs` (pola InjectExcelHelperTests — class biasa, NO `[Trait("Category","Integration")]` → masuk fast suite). Test STRUKTUR (belum DV):
       - `Build("Universal")` → worksheet pertama header row 1 = 13 kolom persis, cell(1,12)=="QuestionType", cell(1,13)=="Rubrik".
       - `Build("MC")` → header 9 kolom, cell(1,8)=="QuestionType".
       - Round-trip via MemoryStream (`wb.SaveAs(ms); ms.Position=0; new XLWorkbook(ms)`) untuk membuktikan serialisasi OK.
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTemplateBuilderTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Helpers/QuestionTemplateBuilder.cs` ada, `grep "public static XLWorkbook Build" Helpers/QuestionTemplateBuilder.cs` → 1 match
    - `grep "QuestionTemplateBuilder.Build" Controllers/AssessmentAdminController.cs` → ≥1 match (controller delegasi)
    - `grep -c "CreateDataValidation" Helpers/QuestionTemplateBuilder.cs` → 0 (DV belum ditambah di task ini — refactor murni)
    - `dotnet build HcPortal.csproj` exits 0
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTemplateBuilderTests"` exits 0 (struktur header hijau)
  </acceptance_criteria>
  <done>Body template ada di helper pure, controller delegasi tipis, perilaku download identik (header 13/9-kolom), test struktur hijau. Belum ada DataValidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tambah DataValidation (dropdown QuestionType + numeric Skor) + kolom Skor — test-first</name>
  <files>Helpers/QuestionTemplateBuilder.cs, HcPortal.Tests/QuestionTemplateBuilderTests.cs</files>
  <read_first>
    - Helpers/QuestionTemplateBuilder.cs (hasil Task 1 — tempat menambah DV + kolom Skor)
    - HcPortal.Tests/QuestionTemplateBuilderTests.cs (hasil Task 1 — extend)
    - 999.17-RESEARCH.md §Pattern 2 (kode DV verified) + §"Read-back DataValidation" (assertion verified) + §Code Examples
    - 999.17-PATTERNS.md §"Core pattern BARU — DataValidation" + §"QuestionTemplateBuilderTests.cs"
    - CONTEXT.md D-01..D-05, D-07, D-10
  </read_first>
  <behavior>
    Tulis assertion RED dulu di QuestionTemplateBuilderTests.cs, lalu implement hingga GREEN:
    - DV-TYPE Universal: `Build("Universal")` → `ws.DataValidations.TryGet(ws.Range("L2:L1000").RangeAddress, out dvType)`==true; `dvType.AllowedValues`==XLAllowedValues.List; `dvType.InCellDropdown`==true; `dvType.Value` mengandung "MultipleChoice" (string == "\"MultipleChoice,MultipleAnswer,Essay\"").
    - DV-TYPE legacy: `Build("MC")` (juga "MA","Essay") → TryGet `H2:H1000` (QuestionType di legacy = kolom 8 = H) ==true, AllowedValues==List, InCellDropdown==true.
    - SKOR-DV Universal: `Build("Universal")` → TryGet `N2:N1000` (Skor=14=N) ==true; AllowedValues==XLAllowedValues.WholeNumber; Operator==XLOperator.Between; MinValue=="1"; MaxValue=="100".
    - SKOR-COL header: `Build("Universal")` header row 1 sekarang 14 kolom; cell(1,14).GetString()=="Skor".
    - DV-SKIP Universal: TryGet `H2:H1000` (Jawaban Benar di Universal = kolom 8 = H) → false (TANPA dropdown, D-04). TryGet kolom Opsi/Section (mis. B2/I2/J2) → false (D-05).
    - LEGACY tak punya Skor: `Build("MC")` header hanya 9 kolom; TryGet `N2:N1000` → false (D-07 — kolom Skor & DV-nya HANYA Universal).
    - Round-trip serialisasi: assert DV bertahan setelah `SaveAs(ms)` → `new XLWorkbook(ms)` (bukan cuma in-memory model).
  </behavior>
  <action>
    Di `QuestionTemplateBuilder.Build`:
    1. Header Universal: append "Skor" sebagai elemen ke-14 array headers Universal (setelah "Rubrik"). JANGAN sentuh array legacy 9-kolom (D-07). Verifikasi marker dual-format tak bergeser (Opsi E@6, Opsi F@7, No.Section@9, Nama Section@10 tetap di posisi sama — append kolom 14 aman, RESEARCH Pitfall 1).
    2. Example row Universal: tambah elemen ke-14 pada 3 string[] example (AAC:7307-7309) — saran isi "10" (atau "" — Claude's Discretion; "10" lebih informatif). Legacy example JANGAN diubah.
    3. DataValidation QuestionType (D-01/02/03) — SEMUA varian:
       - Universal: `var dvType = ws.Range("L2:L1000").CreateDataValidation();` (QuestionType=kolom 12=L)
       - Legacy (MC/MA/Essay): `var dvType = ws.Range("H2:H1000").CreateDataValidation();` (QuestionType=kolom 8=H)
       - `dvType.List("\"MultipleChoice,MultipleAnswer,Essay\"", true);` (literal double-quotes WAJIB — Pitfall 3); `dvType.IgnoreBlanks = true;` `dvType.ErrorStyle = XLErrorStyle.Warning;`
    4. DataValidation Skor (D-10) — HANYA Universal: `var dvScore = ws.Range("N2:N1000").CreateDataValidation(); dvScore.WholeNumber.Between(1, 100); dvScore.IgnoreBlanks = true; dvScore.ErrorTitle = "Skor tidak valid"; dvScore.ErrorMessage = "Skor harus angka bulat 1-100.";`
    5. (Opsional) tambah AddInstruction Universal untuk kolom Skor: "Skor: bobot poin per soal (angka bulat 1-100). Kosongkan = 10 (default)."
    6. JANGAN pasang DV pada kolom Jawaban Benar / Opsi / Section / Rubrik (D-04/D-05).
    Catatan: gunakan satu `Range("X2:X1000")` per kolom (BUKAN loop per-sel — RESEARCH anti-pattern).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTemplateBuilderTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CreateDataValidation" Helpers/QuestionTemplateBuilder.cs` → ≥3 (Universal QuestionType + Skor + legacy QuestionType)
    - `grep "MultipleChoice,MultipleAnswer,Essay" Helpers/QuestionTemplateBuilder.cs` → match (inline list)
    - `grep "WholeNumber.Between(1, 100)" Helpers/QuestionTemplateBuilder.cs` → match
    - `grep "\"Skor\"" Helpers/QuestionTemplateBuilder.cs` → match (kolom 14 Universal)
    - `grep "N2:N1000" HcPortal.Tests/QuestionTemplateBuilderTests.cs` → match (SKOR-DV read-back)
    - QuestionTemplateBuilderTests berisi assertion DV-SKIP (TryGet → Assert.False) untuk kolom Jawaban Benar (H) Universal
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTemplateBuilderTests"` exits 0
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <done>DataValidation dropdown QuestionType hadir di semua template + numeric Skor 1-100 hadir di Universal kolom N; Jawaban Benar & kolom lain TANPA dropdown; legacy TANPA kolom Skor; semua read-back test (incl round-trip serialisasi) hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (file download) | Template .xlsx di-stream ke browser admin/HC; DataValidation = aturan yang dieksekusi Excel di sisi client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99917-01 | Information Disclosure | DownloadQuestionTemplate (GET) | accept | RBAC `[Authorize(Roles="Admin, HC")]` SUDAH ADA (AAC:7266); template statis tanpa data sensitif/PII. Low-value target. |
| T-99917-02 | Tampering | DataValidation client-side | accept | DV = hint anti-typo NON-otoritatif. Admin bisa edit/hapus DV di .xlsx lokal, TAPI server WAJIB re-validate saat import (D-10, ditangani Plan 02 SKR-03). Tidak ada keputusan keamanan bergantung DV client. |
| T-99917-03 | Tampering | Inline-list payload | mitigate | Nilai list hardcoded literal ("\"MultipleChoice,MultipleAnswer,Essay\"", 37 char < limit 255); tidak ada input user yang masuk ke definisi DV → tak ada injection ke aturan. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exits 0
- `dotnet test HcPortal.Tests --filter "FullyQualifiedName~QuestionTemplateBuilderTests"` hijau (struktur + semua read-back DV)
- `git grep "CreateDataValidation" -- '*.cs'` sekarang menemukan kode (sebelumnya 0)
- Tidak ada perubahan pada `ImportPackageQuestions`, `GradingService`, atau form authoring (di luar scope plan ini)
</verification>

<success_criteria>
- DownloadQuestionTemplate delegasi ke QuestionTemplateBuilder.Build (controller tipis)
- Dropdown QuestionType (List, 3 nilai, inCellDropdown) hadir di Universal (L2:L1000) + legacy MC/MA/Essay (H2:H1000), range baris 2-1000
- Kolom Jawaban Benar & kolom lain TANPA DataValidation
- Kolom Skor (header "Skor") ada di Universal posisi 14 (N) dengan WholeNumber Between 1-100; legacy TANPA Skor
- Marker dual-format (6/7/9/10) tak bergeser (regresi parser dicegah)
- Semua unit test QuestionTemplateBuilderTests hijau
</success_criteria>

<output>
After completion, create `.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-01-SUMMARY.md`
</output>
