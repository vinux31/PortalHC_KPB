---
phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp
plan: 01
subsystem: api
tags: [closedxml, excel, datavalidation, dropdown, assessment, template]

# Dependency graph
requires:
  - phase: 415 (v32.6 Section Foundation + Import Excel)
    provides: header Universal 13-kolom + dual-format isNewFormat marker (6/7/9/10) yang plan ini jaga agar tak bergeser
provides:
  - "Helpers/QuestionTemplateBuilder.cs — pure static builder XLWorkbook (seam, unit-testable tanpa DB)"
  - "Dropdown DataValidation QuestionType (List 3-nilai) di SEMUA template Universal(L)/legacy(H)"
  - "Kolom Skor (header 'Skor') di Universal posisi 14(N) + numeric DataValidation WholeNumber 1-100"
  - "DownloadQuestionTemplate ramping (delegasi ke builder)"
affects: [999.17-02 (ImportPackageQuestions parse Skor->ScoreValue), 999.17-03 (UAT roundtrip)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ClosedXML DataValidation greenfield: range.CreateDataValidation().List(\"\\\"a,b,c\\\"\", true) + .WholeNumber.Between(1,100)"
    - "Pure static template-builder seam (analog InjectExcelHelper.GenerateTemplate) — DV unit-testable tanpa controller/DB"

key-files:
  created:
    - Helpers/QuestionTemplateBuilder.cs
    - HcPortal.Tests/QuestionTemplateBuilderTests.cs
  modified:
    - Controllers/AssessmentAdminController.cs

key-decisions:
  - "Whitelist type dinormalisasi di dalam Build (helper self-contained), bukan di controller"
  - "DV-TYPE ditulis explicit per-varian (3 CreateDataValidation) bukan unified range — match plan action + grep-criterion ≥3"
  - "Skor di-append kolom 14 (N) — marker dual-format (6/7/9/10) tak bergeser (RESEARCH Pitfall 1)"
  - "ErrorStyle.Warning (bukan Stop) — DV anti-typo non-otoritatif, izinkan input tetap (A4)"

patterns-established:
  - "Pure static XLWorkbook builder + DataValidation read-back test (TryGet RangeAddress)"
  - "TDD RED->GREEN untuk DataValidation: assert TryGet + AllowedValues/Operator/Min/Max + round-trip serialisasi"

requirements-completed: [DRP-01, DRP-02, SKR-01]

# Metrics
duration: 10min
completed: 2026-06-30
---

# Phase 999.17 Plan 01: Excel Zero-Config Template — Dropdown DataValidation + Kolom Skor Summary

**Template Excel soal kini "zero-config" di sisi download: dropdown DataValidation QuestionType (3 nilai) di semua template + kolom Skor baru (N/14) dengan numeric WholeNumber 1-100 di Universal, lewat seam pure `QuestionTemplateBuilder`.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-06-30T07:21:21Z
- **Completed:** 2026-06-30T07:32:00Z
- **Tasks:** 2 (Task 1 refactor + Task 2 TDD DataValidation)
- **Files modified:** 3 (2 created, 1 modified)

## Accomplishments
- Ekstrak body `DownloadQuestionTemplate` ke `Helpers/QuestionTemplateBuilder.Build(type) -> XLWorkbook` (pure/EF-free, analog `InjectExcelHelper`) — DataValidation jadi unit-testable tanpa membangun controller (~12 ctor arg) atau DB. Controller jadi delegasi 3-baris.
- Dropdown DataValidation `QuestionType` (List `"MultipleChoice,MultipleAnswer,Essay"`, inCellDropdown, baris 2-1000) di SEMUA template: Universal kolom L(12) + legacy MC/MA/Essay kolom H(8) (DRP-01).
- Kolom `Skor` baru di Universal posisi 14(N) — di-APPEND setelah Rubrik agar marker dual-format `isNewFormat` (Opsi E@6, Opsi F@7, No.Section@9, Nama Section@10) TAK bergeser (SKR-01); legacy tetap 9-kolom tanpa Skor (D-07).
- Numeric DataValidation `Skor` WholeNumber Between 1-100 (IgnoreBlanks, baris 2-1000) HANYA di Universal (SKR-01/D-10).
- Kolom Jawaban Benar / Opsi / Section TANPA dropdown (DRP-02/D-04/D-05) — diverifikasi via assert `TryGet -> false`.
- DataValidation greenfield: `git grep CreateDataValidation -- '*.cs'` kini menemukan kode (sebelumnya 0 match).

## Task Commits

1. **Task 1: Ekstrak QuestionTemplateBuilder (seam pure) + delegasi controller** - `54ad5e77` (refactor)
2. **Task 2 RED: assert DataValidation + kolom Skor** - `2cb78216` (test)
3. **Task 2 GREEN: implement DataValidation + kolom Skor** - `ade2425c` (feat)

_TDD gate sequence verified in git log: test(RED) `2cb78216` -> feat(GREEN) `ade2425c`._

## Files Created/Modified
- `Helpers/QuestionTemplateBuilder.cs` (baru) - pure static builder template soal Excel + DataValidation (dropdown QuestionType semua varian, numeric Skor Universal). Berisi `public static XLWorkbook Build(string type)`.
- `HcPortal.Tests/QuestionTemplateBuilderTests.cs` (baru) - 15 unit test (no DB, fast suite): struktur header 13->14 kolom, posisi QuestionType/Rubrik/Skor, DV-TYPE dropdown read-back per varian, SKOR-DV WholeNumber 1-100, DV-SKIP kolom lain, LEGACY-SAFE (legacy tanpa Skor), round-trip serialisasi OOXML.
- `Controllers/AssessmentAdminController.cs` (modified) - `DownloadQuestionTemplate` ramping jadi delegasi tipis `using var workbook = QuestionTemplateBuilder.Build(type)` + `ExcelExportHelper.ToFileResult`. RBAC `[Authorize(Roles="Admin, HC")]` dipertahankan.

## Decisions Made
- **Whitelist type pindah ke Build** (helper self-contained, default "MC") — sesuai opsi plan Task 1.
- **DV-TYPE ditulis eksplisit per-varian** (3 `CreateDataValidation` call: Universal QuestionType L + Skor N + legacy QuestionType H) alih-alih unified range string, agar match action plan + acceptance grep `CreateDataValidation >= 3`. Duplikasi konfigurasi List diminimalkan via `const string typeList`.
- **ErrorStyle.Warning** (bukan Stop) untuk kedua DV — anti-typo non-otoritatif, izinkan input tetap dengan teguran (RESEARCH A4); server tetap otoritatif saat import (Plan 02).
- **Example row Universal kolom Skor diisi "10"** (informatif, bobot default) — Claude's Discretion; kosong tetap valid (server default 10, D-08).

## Deviations from Plan

None - plan executed exactly as written. Implementasi mengikuti RESEARCH §Pattern 2 + §Code Examples (ClosedXML 0.105.0 verified API) dan PATTERNS persis. Tidak ada bug/missing-critical/blocking issue yang ditemukan (Rule 1-3 tak terpicu); tidak ada perubahan arsitektural (Rule 4 tak terpicu).

## Issues Encountered

- **Hook artifact (out-of-scope, non-blocking):** Sebuah git hook meng-auto-commit file untracked pre-existing `tests/e2e/audit-v31-v32.spec.ts` sebagai commit `f3bd653e` di antara Task 1 dan Task 2 RED. File ini TIDAK terkait plan 999.17 (e2e audit spec lama) dan TIDAK menangkap pekerjaan plan ini (commit hanya berisi 197 baris file tsb). TDD gate RED->GREEN tetap utuh. Dibiarkan apa-adanya (scope boundary — tak menyentuh file unrelated).

## Cross-Plan Handoff (BUKAN stub)

Kolom `Skor` kini ADA di template Universal yang diunduh, TAPI `ImportPackageQuestions` masih hardcode `ScoreValue = 10` (`AAC:7832`) — parsing kolom Skor -> `PackageQuestion.ScoreValue` adalah scope **Plan 02 (SKR-02/03/04)**, sengaja DI LUAR plan ini (lihat plan objective: "TIDAK menyentuh parser import"). Ini handoff lintas-plan yang ter-dokumentasi, bukan stub yang menggantung. DataValidation client = hint anti-typo; validasi server otoritatif (1-100, atomic hard-reject) menyusul di Plan 02.

## Verification
- `dotnet build HcPortal.csproj` — 0 errors (24 pre-existing warnings, di luar scope).
- `dotnet test --filter QuestionTemplateBuilderTests` — 15/15 hijau.
- `dotnet test --filter "Category!=Integration"` (fast suite) — 653 passed / 0 failed / 2 skipped (nol regresi dari refactor controller).
- `git grep CreateDataValidation -- '*.cs'` — kini menemukan `Helpers/QuestionTemplateBuilder.cs` (sebelumnya 0).
- Tidak ada perubahan pada `ImportPackageQuestions`, `GradingService`, atau form authoring (sesuai scope).

## Next Phase Readiness
- Template download zero-config (dropdown + numeric DV) SIAP. Plan 02 (SKR-02/03/04, VRF-01/02) dapat lanjut: parse kolom Skor di branch `isNewFormat` (`row.Cell(14)`) -> validasi server 1-100 -> atomic hard-reject (TempData ScoreErrors, pola SectionMismatch) menggantikan hardcode `ScoreValue=10`.
- Tidak ada blocker. migration=FALSE (tidak menyentuh DB/schema).

## Self-Check: PASSED

- FOUND: Helpers/QuestionTemplateBuilder.cs
- FOUND: HcPortal.Tests/QuestionTemplateBuilderTests.cs
- FOUND: .planning/phases/999.17-.../999.17-01-SUMMARY.md
- FOUND commits: 54ad5e77 (refactor), 2cb78216 (test RED), ade2425c (feat GREEN)

---
*Phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp*
*Completed: 2026-06-30*
