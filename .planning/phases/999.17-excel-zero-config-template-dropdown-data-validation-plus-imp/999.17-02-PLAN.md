---
phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Controllers/AssessmentAdminController.cs
  - Views/Admin/ImportPackageQuestions.cshtml
  - HcPortal.Tests/SectionImportTests.cs
  - HcPortal.Tests/WeightedScoreImportTests.cs
autonomous: true
requirements: [SKR-01, SKR-02, SKR-03, SKR-04, VRF-01, VRF-02]
user_setup: []

must_haves:
  truths:
    - "Import Universal dengan kolom Skor=25 → soal tersimpan ScoreValue=25"
    - "Import dengan kolom Skor kosong → soal tersimpan ScoreValue=10 (default D-08)"
    - "Import dengan Skor invalid (0, 101, 5.5, abc) → SELURUH import dibatalkan (0 soal tertulis) + daftar error per-baris muncul via TempData (D-12 atomic)"
    - "File legacy 9-kolom & file Universal 13-kolom lama (tanpa kolom Skor) tetap ter-import benar dengan ScoreValue=10 (kompatibel-mundur D-07/LEGACY-SAFE)"
    - "Grading menormalisasi skor akhir ke 0-100 dengan bobot non-uniform (D-11 GRADE-LOCK — perilaku existing, dikunci test)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "ImportPackageQuestions: parse kolom Skor + validasi server 1-100 + atomic hard-reject + ScoreValue dari Excel"
      contains: "ScoreErrors"
    - path: "Views/Admin/ImportPackageQuestions.cshtml"
      provides: "Render daftar error Skor (alert-danger) dari TempData[ScoreErrors] + dokumentasi kolom Skor"
      contains: "ScoreErrors"
    - path: "HcPortal.Tests/SectionImportTests.cs"
      provides: "Test SKOR-PARSE + SKOR-VAL atomic-reject + LEGACY-SAFE regression"
      contains: "ScoreValue"
    - path: "HcPortal.Tests/WeightedScoreImportTests.cs"
      provides: "GRADE-LOCK: import bobot non-uniform → grading % benar"
      contains: "ScoreValue"
  key_links:
    - from: "ImportPackageQuestions (branch isNewFormat)"
      to: "PackageQuestion.ScoreValue"
      via: "parse row.Cell(14) → int 1-100 → set ScoreValue (default 10)"
      pattern: "ScoreValue = scoreValue|ScoreValue = parsedScore"
    - from: "ImportPackageQuestions (validasi Skor)"
      to: "Views/Admin/ImportPackageQuestions.cshtml"
      via: "TempData[ScoreErrors] JSON List<string> → RedirectToAction (0 write)"
      pattern: "TempData\\[\"ScoreErrors\"\\]"
---

<objective>
Sisi IMPORT dari fitur "skor per-soal": `ImportPackageQuestions` membaca kolom `Skor` (kolom 14 / N) dari file Universal, memvalidasi server-side int 1-100 (D-09), menolak-keras-atomic bila ada baris invalid (D-12), dan menyetel `PackageQuestion.ScoreValue` dari Excel menggantikan hardcode `=10` (D-06/D-08). Plus mengunci normalisasi grading 0-100 (D-11) dan kompatibel-mundur (D-07/LEGACY-SAFE) dengan test.

Purpose: Menutup satu-satunya gap fitur bobot — field `ScoreValue` + weighted grading SUDAH ADA, hanya jalur import yang belum membacanya. Penolakan invalid harus TERSURFACE (bukan silent-skip seperti pola `errors.Add+continue` existing yang tak pernah ditampilkan — RESEARCH Pitfall 2); pakai pola `SectionMismatch` (tolak-keras atomic, view scaffold siap).

Output: perubahan `ImportPackageQuestions` (parse+validasi+atomic-reject), blok render `ScoreErrors` di view, test SKOR-PARSE/SKOR-VAL/LEGACY-SAFE (extend SectionImportTests) + GRADE-LOCK (WeightedScoreImportTests baru).

Migration = FALSE. JANGAN ubah `GradingService` (D-11 grade-lock — hanya verifikasi/kunci). JANGAN sentuh jalur legacy 9-kolom (D-07). Plan ini berjalan di Wave 2 SETELAH Plan 01 (file-overlap `AssessmentAdminController.cs`).

⚠️ PENTING: Plan 01 mengekstrak body `DownloadQuestionTemplate` ke helper → nomor baris `ImportPackageQuestions` BERGESER NAIK ~75 baris dari yang tercatat di RESEARCH/PATTERNS. JANGAN percaya nomor baris absolut; cari ANCHOR via grep (string kode) yang tercantum di bawah.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-CONTEXT.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-RESEARCH.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-PATTERNS.md

<interfaces>
<!-- Kontrak + anchor. Diverifikasi dari codebase 2026-06-30. Cari via grep (line numbers bergeser pasca-Plan 01). -->

# ANCHOR: deteksi dual-format (JANGAN rusak — Pitfall 1). Grep: "bool isNewFormat = colCount > 9"
#   isNewFormat true bila colCount>9 && HeaderAt(6)=="opsi e" && (7)=="opsi f" && (9)=="no. section" && (10)=="nama section"
#   Append Skor kolom 14 TIDAK menggeser marker 6/7/9/10 → tetap utuh.

# ANCHOR: deklarasi tuple rows (perlu diperlebar +RawScore). Grep: "List<(string Question, string OptA"
#   Saat ini 13-field: (Question,OptA,OptB,OptC,OptD,OptE,OptF,Correct,int? SectionNumber,string? SectionName,string? ElemenTeknis,QuestionType,string? Rubrik)
#   Dua initializer `new List<(...)>(...)` (excel branch + paste branch) — keduanya harus ikut diperlebar.

# ANCHOR: branch isNewFormat baca Cell(1..13). Grep: "rubrik = (row.Cell(13).GetString()"
#   Tambah SETELAHnya: var rawScore = (row.Cell(14).GetString() ?? "").Trim();   // kolom N
#   Branch OLD (legacy ≤9 kolom): TIDAK ada kolom Skor → rawScore = null/"".

# ANCHOR: rows.Add(...) di excel branch. Grep: "rows.Add((q, a, b, c, d, e, f, cor, sectionNumber"
#   Tambah rawScore sbg field terakhir.
# ANCHOR: rows.Add(...) di paste branch (pasteText) — paste TANPA Skor → null.

# ANCHOR: pola tolak-keras atomic existing (TIRU PERSIS). Grep: "TempData[\"SectionMismatch\"]"
#   if (mismatchList.Any()) { TempData["SectionMismatch"] = JsonSerializer.Serialize(mismatchList); return RedirectToAction("ImportPackageQuestions", new { packageId }); }
#   → 0 write. Ini berjalan SEBELUM build-loop & persist.

# ANCHOR: destructure tuple di build-loop. Grep: "var (q, a, b, c, d, e, f, cor, sectionNumber, sectionName, rawSubComp, questionType, rubrik) = rows[i];"
# ANCHOR: hardcode yang DIGANTI. Grep: "ScoreValue = 10,"   (di dalam `new PackageQuestion { ... }`)
# ANCHOR: persist transaction (validasi Skor harus SEBELUM ini). Grep: "if (newQuestions.Count > 0)" / "BeginTransactionAsync"

# Parse + validasi (VERIFIED, RESEARCH §Code Examples). `using System.Globalization;` SUDAH di-import (AAC:12):
#   int scoreValue = 10;  // D-08 default
#   if (!string.IsNullOrWhiteSpace(rawScore)) {
#     if (!int.TryParse(rawScore, NumberStyles.None, CultureInfo.InvariantCulture, out scoreValue)
#         || scoreValue < 1 || scoreValue > 100)   // D-09: tolak ≤0/>100/desimal/non-angka
#     { scoreErrors.Add($"Baris {i + 2}: Skor harus angka bulat 1-100. Ditemukan '{rawScore}'."); }
#   }
#   NumberStyles.None menolak desimal "5.5"/whitespace/tanda.

# GradingService (READ-ONLY, D-11) — Services/GradingService.cs:
#   :99  maxScore += q.ScoreValue;
#   :112 / :127 totalScore += q.ScoreValue;  (MC / MA all-or-nothing)
#   :144 int finalPercentage = maxScore > 0 ? (int)((double)totalScore / maxScore * 100) : 0;
#   JANGAN ubah. Test mengunci: import bobot non-uniform → assert finalPercentage benar.

# Test harness existing — HcPortal.Tests/SectionImportTests.cs:
#   :35  class SectionImportTests : IClassFixture<SectionFixture>   ([Trait Integration], SQLEXPRESS)
#   :39  NewCtx()   :83 MakeController(ctx, actor, actionName)   (12-arg ctor lengkap)
#   :157 BuildNewFormatFile(dataRows) → headers 13-kolom → BuildFile(headers, dataRows)  ◀── EXTEND ke 14-kolom (+"Skor")
#   :170 BuildFile(headers, dataRows) — generic, skip sel kosong (row.Length boleh < headers.Length)
#   :194 NewFormat_ImportsOptionsAtoF_... (template sukses-path) ; :347 PerSectionCountMismatch_RejectsImport_FullList_ZeroWrites (template atomic-reject)

# View scaffold existing — Views/Admin/ImportPackageQuestions.cshtml:30-51 (blok SectionMismatch — SALIN untuk ScoreErrors)
#   @{ List<string>? x=null; if (TempData["KEY"] is string j && !string.IsNullOrWhiteSpace(j)) { try{ x=JsonSerializer.Deserialize<List<string>>(j);}catch{x=null;} } }
#   @if (x!=null && x.Count>0) { <div class="alert alert-danger"><strong>...</strong><ul>@foreach(var l in x){<li>@l</li>}</ul></div> }
#   Kartu "Format kolom" :58-59 menyebut header Universal — tambahkan "| Skor".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tulis test merah (RED) — SKOR-PARSE, SKOR-VAL atomic-reject, LEGACY-SAFE, GRADE-LOCK</name>
  <files>HcPortal.Tests/SectionImportTests.cs (extend), HcPortal.Tests/WeightedScoreImportTests.cs (baru)</files>
  <read_first>
    - HcPortal.Tests/SectionImportTests.cs:35-190 (harness: NewCtx, MakeController, BuildNewFormatFile, BuildFile) + :194-243 (sukses-path template) + :347-404 (atomic-reject template PerSectionCountMismatch_RejectsImport_FullList_ZeroWrites)
    - HcPortal.Tests/SectionFixture.cs (SQLEXPRESS fixture)
    - HcPortal.Tests/GradingDedupeTests.cs (pola test grading bila ada — analog GRADE-LOCK)
    - 999.17-PATTERNS.md §"SectionImportTests.cs (EXTEND)" + §"Atomic-reject assertion pattern"
    - 999.17-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map)
  </read_first>
  <action>
    Tulis test yang COMPILE tapi GAGAL (RED) terhadap perilaku saat ini (Skor belum di-parse, hardcode=10):
    1. EXTEND `BuildNewFormatFile` (SectionImportTests.cs:157): tambahkan "Skor" sebagai header elemen ke-14. `BuildFile` sudah skip sel kosong sehingga dataRows boleh `string[13]` (tanpa Skor → kolom 14 kosong) atau `string[14]`. JANGAN ubah `LegacyFormat_*` existing.
    2. Tambah `BuildUniversal13ColFile(dataRows)` helper: headers 13-kolom Universal LAMA (TANPA "Skor") → untuk regression LEGACY-SAFE (file Universal lama tetap NEW + ScoreValue=10).
    3. Test SKOR-PARSE (sukses, mirror :194): import 1 soal MC Universal dengan kolom Skor="25" → `Assert.Equal(25, q.ScoreValue)`. RED sekarang (hardcode=10).
    4. Test SKOR default (D-08): import soal dengan Skor kosong → `Assert.Equal(10, q.ScoreValue)`. (lewat bahkan sebelum impl — guard kompatibilitas.)
    5. Test SKOR-VAL atomic-reject (mirror :347): import 3 soal, baris 2 Skor="0", baris 3 Skor="abc" → `Assert.IsType<RedirectToActionResult>(res)`; `TempData["ScoreErrors"]` non-empty, deserialize List<string> Count==2; 0 write (`PackageQuestions.CountAsync(...)`==0). RED sekarang (tak ada ScoreErrors, semua ke-import dgn ScoreValue=10).
    6. Test SKOR-VAL varian: "101" (>100) dan "5.5" (desimal) → masing-masing ditolak (ScoreErrors non-empty, 0 write).
    7. Test LEGACY-SAFE: (a) `BuildUniversal13ColFile` (Universal lama tanpa Skor) → terdeteksi NEW, soal ter-import, ScoreValue==10. (b) pastikan test `LegacyFormat_*` (9-kolom) existing tetap hijau (jalankan, jangan ubah).
    8. `HcPortal.Tests/WeightedScoreImportTests.cs` (BARU, `[Trait("Category","Integration")]`, IClassFixture<SectionFixture>) — GRADE-LOCK: import 2 soal MC Universal dengan Skor non-uniform (mis. soal-1 Skor=30, soal-2 Skor=10), simulasikan jawaban benar HANYA soal-1, panggil GradingService (atau jalur grading existing yang dipakai test grading lain) → `finalPercentage == 75` (30/40*100). Jika perlu, mirror seed/grading dari test grading existing. RED sekarang (bobot uniform 10/10 → 50%).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~SectionImportTests|FullyQualifiedName~WeightedScoreImportTests" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Skor" HcPortal.Tests/SectionImportTests.cs` → match (header diperluas)
    - `grep "TempData\[\"ScoreErrors\"\]" HcPortal.Tests/SectionImportTests.cs` → match
    - `grep "BuildUniversal13ColFile" HcPortal.Tests/SectionImportTests.cs` → match (regression helper)
    - `HcPortal.Tests/WeightedScoreImportTests.cs` ada; `grep "finalPercentage\|FinalPercentage\|75" HcPortal.Tests/WeightedScoreImportTests.cs` → match
    - `dotnet build HcPortal.Tests` exits 0 (test COMPILE)
    - `dotnet test --filter "FullyQualifiedName~SectionImportTests"` menunjukkan kegagalan pada test SKOR-PARSE & SKOR-VAL baru (RED yang diharapkan) — test SKOR-default & LEGACY-SAFE & existing tetap hijau
  </acceptance_criteria>
  <done>Test SKOR-PARSE, SKOR-VAL atomic-reject, LEGACY-SAFE, GRADE-LOCK ditulis, compile, dan MERAH (kecuali yang memang guard kompatibilitas). Siap implement di Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parse Skor + validasi server + atomic hard-reject + view error-surface (GREEN)</name>
  <files>Controllers/AssessmentAdminController.cs (ImportPackageQuestions), Views/Admin/ImportPackageQuestions.cshtml</files>
  <read_first>
    - Controllers/AssessmentAdminController.cs — cari anchor (line numbers bergeser pasca-Plan 01): "bool isNewFormat = colCount > 9", "List<(string Question, string OptA", "rubrik = (row.Cell(13).GetString()", "rows.Add((q, a, b, c, d, e, f, cor, sectionNumber", "TempData[\"SectionMismatch\"]", "var (q, a, b, c, d, e, f, cor, sectionNumber, sectionName, rawSubComp, questionType, rubrik) = rows[i];", "ScoreValue = 10,", "if (newQuestions.Count > 0)"
    - Views/Admin/ImportPackageQuestions.cshtml:30-85 (blok SectionMismatch untuk disalin + kartu Format kolom)
    - HcPortal.Tests/SectionImportTests.cs (test hasil Task 1 — target GREEN)
    - 999.17-PATTERNS.md §"ImportPackageQuestions POST" (5 sub-pola) + §"View error-surface scaffold"
    - CONTEXT.md D-06, D-08, D-09, D-12
  </read_first>
  <action>
    Controller `ImportPackageQuestions`:
    1. Perlebar tuple `rows` +1 field `string? RawScore` (field terakhir). Update: deklarasi `List<(...)>` (anchor "List<(string Question"), KEDUA initializer `new List<(...)>` (excel branch + paste branch), dan KEDUA `rows.Add(...)`.
       - Excel branch isNewFormat: setelah baca Rubrik (anchor "rubrik = (row.Cell(13)") tambah `var rawScore = (row.Cell(14).GetString() ?? "").Trim();` lalu sertakan di rows.Add.
       - Excel branch legacy (else): `string? rawScore = null;` (tak ada kolom Skor — D-07).
       - Paste branch: `rawScore = null` (paste tak punya Skor) — sertakan di rows.Add agar arity cocok.
    2. Tambah `var scoreErrors = new List<string>();` (dekat `var errors = new List<string>();`).
    3. VALIDASI Skor PASS atomik — SEBELUM build-loop & persist (penempatan: setelah `rows` terisi penuh, sejajar/sebelum guard SectionMismatch; gunakan satu pass mandiri agar 0-write terjamin):
       ```
       for (int i = 0; i < rows.Count; i++) {
           var rawScore = rows[i].RawScore;
           if (string.IsNullOrWhiteSpace(rawScore)) continue;        // D-08 → default 10 nanti
           if (!int.TryParse(rawScore, NumberStyles.None, CultureInfo.InvariantCulture, out var sv)
               || sv < 1 || sv > 100)
               scoreErrors.Add($"Baris {i + 2}: Skor harus angka bulat 1-100. Ditemukan '{rawScore}'.");
       }
       if (scoreErrors.Any()) {
           TempData["ScoreErrors"] = System.Text.Json.JsonSerializer.Serialize(scoreErrors);
           return RedirectToAction("ImportPackageQuestions", new { packageId });   // 0 write (D-12)
       }
       ```
       (Pakai `i + 2` bila ingin nomor baris Excel sebenarnya = index data + header; selaraskan dengan ekspektasi test Task 1 — sesuaikan agar test hijau.)
    4. Build-loop: destructure tuple ikut `RawScore` (anchor "var (q, a, ...) = rows[i];"). Ganti hardcode `ScoreValue = 10,` (anchor) jadi nilai ter-parse:
       ```
       int scoreValue = 10;  // D-08 default
       if (!string.IsNullOrWhiteSpace(rawScore)
           && int.TryParse(rawScore, NumberStyles.None, CultureInfo.InvariantCulture, out var pv)
           && pv >= 1 && pv <= 100)
           scoreValue = pv;
       ...
       new PackageQuestion { ... ScoreValue = scoreValue, ... }
       ```
       (Validasi sudah dijamin di pass #3 → di sini aman; parse-ulang sederhana, default 10 bila kosong.)
    View `Views/Admin/ImportPackageQuestions.cshtml`:
    5. Salin blok render SectionMismatch (:30-51) jadi blok kembar untuk `TempData["ScoreErrors"]` dengan judul "Skor tidak valid. Perbaiki dulu sebelum import:" (alert-danger + <ul> per-baris).
    6. Kartu "Format kolom" (:58-59): tambahkan "| Skor" pada string header Universal + bullet `<li><strong>Skor:</strong> bobot poin per soal (angka 1-100). Kosongkan = 10 (default).</li>`.
    JANGAN ubah `GradingService` (D-11). JANGAN ubah jalur legacy 9-kolom (D-07).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests --filter "FullyQualifiedName~SectionImportTests|FullyQualifiedName~WeightedScoreImportTests" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep "row.Cell(14)" Controllers/AssessmentAdminController.cs` → match (baca kolom Skor)
    - `grep "TempData\[\"ScoreErrors\"\]" Controllers/AssessmentAdminController.cs` → match (atomic-reject surface)
    - `grep "ScoreValue = scoreValue" Controllers/AssessmentAdminController.cs` → match
    - `grep -c "ScoreValue = 10," Controllers/AssessmentAdminController.cs` → 0 (hardcode di branch import diganti; pastikan tak ada hardcode tersisa di jalur import)
    - `grep "ScoreErrors" Views/Admin/ImportPackageQuestions.cshtml` → match (render block)
    - `grep "Skor" Views/Admin/ImportPackageQuestions.cshtml` → match (dokumentasi kartu format)
    - `git diff Services/GradingService.cs` → kosong (D-11 tidak diubah)
    - `dotnet test HcPortal.Tests --filter "FullyQualifiedName~SectionImportTests|FullyQualifiedName~WeightedScoreImportTests"` exits 0 (semua GREEN incl GRADE-LOCK)
    - `dotnet build HcPortal.csproj` exits 0
  </acceptance_criteria>
  <done>ImportPackageQuestions membaca Skor (Universal kolom 14), validasi server 1-100, tolak-keras-atomic (0 write + ScoreErrors tersurface) bila invalid, set ScoreValue dari Excel (default 10); view merender daftar error Skor; legacy & Universal-13-kolom-lama tetap ter-import benar; grading non-uniform terkunci; semua test hijau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (file upload) | Admin/HC mengunggah `.xlsx` ke POST ImportPackageQuestions — input tak-tepercaya menyeberang di sini |
| Excel cell DataValidation → server | Aturan DV di file = hint client; nilai aktual sel BISA apa saja (DV dapat dihapus/diedit di file lokal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99917-04 | Tampering | Kolom Skor (skew grading via negatif/overflow/desimal/non-angka) | mitigate | Server re-validate `int.TryParse(NumberStyles.None, InvariantCulture)` + range 1-100 + tipe `int` (D-09). Tolak-keras-atomic 0-write bila invalid (D-12). TIDAK percaya DV client (D-10). |
| T-99917-05 | Tampering | Trust DV client sebagai otoritatif | mitigate | Validasi server WAJIB (pass #3) berjalan sebelum persist; DV Plan 01 hanya hint anti-typo. |
| T-99917-06 | Denial of Service | Upload `.xlsx` oversized / zip-bomb | mitigate | Guard ukuran ≤5MB + ekstensi `.xlsx/.xls` + try/catch parse SUDAH ADA (anchor: "File terlalu besar"/"Hanya file Excel"). Tidak diubah. |
| T-99917-07 | Spoofing / Elevation of Privilege | Bypass auth/antiforgery pada POST import | mitigate | `[Authorize(Roles="Admin, HC")]` + `[ValidateAntiForgeryToken]` SUDAH ADA pada endpoint. Dipertahankan (tidak disentuh). |
| T-99917-08 | Injection (CSV/formula) | Sel teks soal | accept | Di luar scope (Skor = numeric murni; rendering teks soal sudah ditangani existing). Tidak diperkenalkan oleh perubahan ini. |
</threat_model>

<verification>
- `dotnet build HcPortal.csproj` exits 0
- `dotnet test HcPortal.Tests` FULL (incl Integration/SQLEXPRESS) hijau — khususnya SectionImportTests (incl `LegacyFormat_*` existing tak rusak) + WeightedScoreImportTests
- `git diff Services/GradingService.cs` kosong (D-11 lock)
- Deteksi dual-format `isNewFormat` tak diubah secara semantik (marker 6/7/9/10 tetap) — `LegacyFormat_*` + Universal-13-lama hijau membuktikannya
</verification>

<success_criteria>
- Skor=N (1-100) di Universal → ScoreValue=N; kosong/legacy → ScoreValue=10
- Skor invalid (≤0/>100/desimal/non-angka) → 0 soal tertulis + daftar error per-baris di TempData[ScoreErrors] → view alert-danger
- Hardcode `ScoreValue = 10` di jalur import diganti nilai ter-parse
- View merender error Skor + mendokumentasikan kolom Skor
- GradingService tidak diubah; normalisasi 0-100 dengan bobot non-uniform terkunci test
- File legacy 9-kolom & Universal 13-kolom lama tetap kompatibel-mundur
</success_criteria>

<output>
After completion, create `.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-02-SUMMARY.md`
</output>
