---
phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - tests/e2e/excel-zeroconfig-99917.spec.ts
autonomous: false
requirements: [VRF-03]
user_setup: []

must_haves:
  truths:
    - "Full suite (unit + integration SQLEXPRESS) hijau setelah kedua fitur terpasang"
    - "Roundtrip nyata: admin unduh template Universal → buka di Excel → dropdown QuestionType muncul + sel Skor menolak nilai di luar 1-100"
    - "Roundtrip import: admin unggah file dengan bobot Skor → grading mencerminkan bobot"
  artifacts:
    - path: "tests/e2e/excel-zeroconfig-99917.spec.ts"
      provides: "(Opsional) Playwright e2e roundtrip download→import-bobot→grade"
      contains: "DownloadQuestionTemplate"
  key_links:
    - from: "template .xlsx terunduh"
      to: "Excel client (dropdown + numeric DV)"
      via: "user membuka file & menguji DataValidation"
      pattern: "manual"
---

<objective>
Gerbang akhir Phase 999.17: pastikan kedua fitur (dropdown DataValidation + import skor per-soal) bekerja end-to-end pada produk yang berjalan, lalu verifikasi manusia atas perilaku yang TIDAK bisa diotomasi — render dropdown & penolakan numeric di aplikasi Excel sungguhan (DataValidation render = perilaku Excel client, bukan ClosedXML write; kehadiran rule SUDAH di-assert otomatis di Plan 01, tapi tampilan/penegakan di Excel hanya bisa dilihat manusia).

Purpose: Lesson 354 (CLAUDE.md) — verifikasi real menangkap apa yang unit/integration tak tangkap. Untuk fase server-side ini, unit+integration menutup mayoritas; sisa = (1) gerbang full-suite, (2) opsional e2e roundtrip browser, (3) konfirmasi manusia dropdown/numeric tampak di Excel.

Output: (opsional) spec Playwright e2e + sign-off UAT manual.

Migration = FALSE. Plan ini TIDAK mengubah kode produk (hanya test e2e opsional + verifikasi). Wave 3 — setelah Plan 01 & 02 selesai.

Catatan environment (CLAUDE.md): app lokal di `http://localhost:5277` (branch main) atau `http://localhost:5270` (branch ITHandoff). Cek branch aktif sebelum UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-CONTEXT.md
@.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-VALIDATION.md

<interfaces>
<!-- Titik UI yang relevan untuk UAT. -->
# Tombol unduh template — Views/Admin/ImportPackageQuestions.cshtml:70-83:
#   Url.Action("DownloadQuestionTemplate","AssessmentAdmin", new { type = "MC"|"MA"|"Essay"|"Universal" })
# Endpoint: GET /Admin/DownloadQuestionTemplate?type=Universal   (RBAC Admin/HC)
# Form import: POST /Admin/ImportPackageQuestions (excelFile) — render ScoreErrors bila Skor invalid
# Kolom DV (Universal): QuestionType = kolom L (12); Skor = kolom N (14). Legacy: QuestionType = kolom H (8), tanpa Skor.
# Playwright specs existing: tests/e2e/*.spec.ts (pola harness — reuse login admin + navigasi)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gerbang full-suite + (opsional) Playwright e2e roundtrip</name>
  <files>tests/e2e/excel-zeroconfig-99917.spec.ts (baru, opsional)</files>
  <read_first>
    - 999.17-VALIDATION.md (Sampling Rate + Manual-Only Verifications)
    - tests/e2e/ (satu spec existing untuk pola login admin + navigasi ManagePackages/Import)
    - 999.17-RESEARCH.md §Validation Architecture (e2e opsional baris terakhir tabel test-map)
  </read_first>
  <action>
    1. Jalankan gerbang full-suite lokal (CLAUDE.md: verifikasi lokal wajib sebelum ship):
       - `dotnet build` (0 error)
       - `dotnet test HcPortal.Tests` (FULL, incl Integration/SQLEXPRESS) — semua hijau.
    2. (OPSIONAL, recommended — bukan blocking bila e2e harness mahal untuk fase server-side ini) Buat `tests/e2e/excel-zeroconfig-99917.spec.ts`:
       - Login sebagai admin, buka halaman ImportPackageQuestions sebuah paket.
       - Klik tombol "Template Universal" → tangkap unduhan .xlsx (Playwright `page.waitForEvent('download')`).
       - (Inti roundtrip) Bangun file impor ber-bobot (Skor=30 & Skor=10 untuk 2 soal MC) → unggah via form import → submit.
       - Assert: redirect sukses (bukan ScoreErrors), lalu (bila praktis) buka Results/grading paket → skor mencerminkan bobot non-uniform. Bila langkah grading terlalu berat untuk e2e, batasi pada: import sukses + soal muncul (bobot di-cover oleh WeightedScoreImportTests integrasi Plan 02).
       - Jalankan: `npx playwright test tests/e2e/excel-zeroconfig-99917.spec.ts`.
       Bila e2e tidak dibuat, dokumentasikan keputusan di SUMMARY (mayoritas perilaku sudah di-cover unit+integration; sisa = UAT manual Task 2).
  </action>
  <verify>
    <automated>dotnet test HcPortal.Tests 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0
    - `dotnet test HcPortal.Tests` (full) exits 0 — incl QuestionTemplateBuilderTests + SectionImportTests + WeightedScoreImportTests + `LegacyFormat_*` existing
    - Bila e2e dibuat: `tests/e2e/excel-zeroconfig-99917.spec.ts` ada dan `npx playwright test tests/e2e/excel-zeroconfig-99917.spec.ts` hijau; bila tidak dibuat: SUMMARY mencatat alasan + bukti cakupan unit/integration
  </acceptance_criteria>
  <done>Full suite hijau; roundtrip e2e (atau keputusan defer terdokumentasi) selesai. Siap UAT manual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT manual — dropdown & numeric DataValidation di Excel sungguhan</name>
  <files>(verifikasi manual — tidak mengubah file)</files>
  <action>
    Tugas verifikasi-manusia (checkpoint blocking) — bukan otomasi. Claude menyiapkan app lokal lebih dulu (`dotnet run`), lalu menyerahkan langkah A-D di `<how-to-verify>` kepada user untuk dijalankan di Excel/LibreOffice + UI Import. Setelah user merespon "approved" atau melaporkan masalah, lanjutkan/perbaiki.
  </action>
  <what-built>
    Template Excel zero-config: dropdown DataValidation `QuestionType` (3 nilai) di semua template + kolom `Skor` baru (Universal) dengan validasi numeric 1-100. Import membaca kolom Skor → ScoreValue, menolak-keras-atomic bila invalid.
  </what-built>
  <how-to-verify>
    Jalankan app lokal (`dotnet run`; buka `http://localhost:5277` — atau `http://localhost:5270` bila di branch ITHandoff). Login admin/HC, buka halaman Import Soal sebuah paket.

    A. Unduh "Template Universal" → buka di Excel atau LibreOffice Calc:
       1. Klik sel di kolom **QuestionType** (kolom L) baris kosong (mis. L5) → harus muncul **dropdown 3 nilai**: MultipleChoice, MultipleAnswer, Essay.
       2. Klik sel di kolom **Skor** (kolom N, header "Skor") → ketik `0` lalu `150` lalu `5.5` → masing-masing **ditolak/ditegur** ("Skor harus angka bulat 1-100"); ketik `25` → diterima.
       3. Kolom **Jawaban Benar** (kolom H) → **TIDAK ada dropdown** (boleh ketik bebas A,C dst).
    B. Unduh "Template Single Answer" (legacy MC) → buka:
       4. Kolom **QuestionType** (kolom H) → dropdown 3 nilai muncul.
       5. **TIDAK ada kolom Skor** (header berhenti di Rubrik).
    C. (Import) Isi Template Universal: 1 soal MC dengan Skor=`25`, simpan, unggah via form import → soal masuk dengan bobot 25 (cek di panel Kelola Soal "Nilai Soal").
    D. (Atomic-reject) Isi Template Universal dengan 1 soal Skor=`abc` → unggah → muncul **alert merah "Skor tidak valid"** berisi nomor baris, dan **TIDAK ada soal yang masuk** (0 tertulis).
  </how-to-verify>
  <verify>Manual human-verify (checkpoint blocking) — bukan command otomatis. Kehadiran DataValidation rule di .xlsx sudah di-assert otomatis di Plan 01 (01-T2). Validasi server Skor + atomic-reject di-assert di Plan 02. Task ini mengonfirmasi RENDER & penegakan di Excel/UI nyata.</verify>
  <resume-signal>Ketik "approved" bila A-D sesuai, atau jelaskan ketidaksesuaian (mis. dropdown tak muncul, numeric tak menolak, alert tak tampil, atau ada soal tertulis saat reject).</resume-signal>
  <done>User mengonfirmasi (approved) bahwa dropdown QuestionType + numeric Skor tampak & menegakkan aturan di Excel, import bobot tersimpan, dan atomic-reject menampilkan alert tanpa menulis soal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (tidak ada permukaan baru) | Plan ini = verifikasi/UAT; tidak menambah kode produk. Mewarisi mitigasi Plan 01 (download GET) & Plan 02 (import POST). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99917-09 | (verifikasi) | UAT roundtrip | accept | Tidak ada surface baru. Tugas justru MEMVERIFIKASI mitigasi T-99917-04/05 (server re-validate Skor, atomic reject) bekerja end-to-end. Spec e2e = test-only, tidak ter-deploy. |
</threat_model>

<verification>
- `dotnet test HcPortal.Tests` (full) hijau
- (opsional) Playwright e2e roundtrip hijau
- UAT manual A-D di-approve manusia (dropdown render + numeric reject + import bobot + atomic reject tampak di aplikasi/Excel nyata)
</verification>

<success_criteria>
- Full suite hijau pada produk terintegrasi
- Manusia mengonfirmasi dropdown QuestionType + numeric Skor tampak & menegakkan aturan di Excel
- Import bobot & tolak-keras-atomic terbukti di UI nyata
</success_criteria>

<output>
After completion, create `.planning/phases/999.17-excel-zero-config-template-dropdown-data-validation-plus-imp/999.17-03-SUMMARY.md`
</output>
