---
phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp
plan: 03
subsystem: qa
tags: [verification, full-suite, e2e, uat, excel, datavalidation, import, assessment]

# Dependency graph
requires:
  - phase: 999.17-01 (Excel zero-config DataValidation + kolom Skor)
    provides: dropdown DataValidation QuestionType (semua template) + kolom Skor (Universal N/14) + numeric DV 1-100 yang Task 2 verifikasi render-nya di Excel
  - phase: 999.17-02 (Import Skor per-soal)
    provides: parse Skor->ScoreValue + validasi server 1-100 + tolak-keras-atomic + grade-lock yang Task 1 gate (full-suite) + Task 2 (UAT import) verifikasi end-to-end
provides:
  - "Gerbang full-suite hijau pada produk terintegrasi (build 0-err + dotnet test HcPortal.Tests 1027/0/2 incl Integration/SQLEXPRESS)"
  - "App lokal disiapkan @ http://localhost:5277 untuk UAT manual (automation-first)"
  - "✅ Sign-off UAT manusia (VRF-03 SATISFIED): render dropdown QuestionType + numeric Skor DataValidation TER-VERIFIKASI di Excel desktop nyata (Step A+B PASS); roundtrip import-bobot + atomic-reject (Step C+D) di-cover-otomasi (WeightedScoreImportTests Score==75 + SectionImportTests 0-write)"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Phase-gate server-side: integration test (WeightedScoreImportTests) = roundtrip de-tautologis (real ImportPackageQuestions action -> grade) menggantikan e2e browser bila import flow = plain server-parsed form-POST (no client JS)"

key-files:
  created: []
  modified: []

key-decisions:
  - "e2e Playwright roundtrip DI-DEFER (sanksi plan: 'bukan blocking bila e2e harness mahal') — import ImportPackageQuestions = plain form-POST server-parsed (no client-side preview/cache JS, kontras inject-396); roundtrip download->import-bobot->grade SUDAH di-cover de-tautologis oleh WeightedScoreImportTests (real action @ SQLEXPRESS, assert Score==75 dari bobot 30/10)"
  - "App lokal di-start (dotnet run @ 5277) sebelum checkpoint human-verify (automation-first) agar user langsung buka URL untuk UAT"

requirements-completed: [VRF-03]  # UAT-approved 2026-06-30 (Step A+B human-verified PASS; C+D covered-by-automation)

# Metrics
duration: ~31m (incl human UAT wait; gate run 4m05s)
completed: 2026-06-30T08:28:59Z
tasks: 2
files: 0
---

# Phase 999.17 Plan 03: Gerbang Akhir + UAT Roundtrip Summary

**Gerbang full-suite hijau (build 0-err + 1027/0/2 incl Integration/SQLEXPRESS) pada produk dengan KEDUA fitur terpasang (dropdown DataValidation + import Skor per-soal); app lokal disiapkan @ localhost:5277 untuk UAT manusia atas render dropdown + numeric DataValidation di Excel nyata — Task 2 (checkpoint blocking) MENUNGGU sign-off.**

## Status: ✅ COMPLETE — Task 1 (gate) hijau + Task 2 (UAT human-verify) di-APPROVE 2026-06-30. VRF-03 SATISFIED.

Kedua task selesai. Task 1 (gerbang otomatis) hijau (1027/0/2); Task 2 (verifikasi-manusia di Excel nyata) di-approve user — render dropdown QuestionType + numeric Skor DataValidation TER-KONFIRMASI di Excel desktop (Step A+B), roundtrip import-bobot + atomic-reject (Step C+D) diterima sebagai covered-by-automation. Phase 999.17 siap close (3/3 plan, 9 pseudo-REQ DRP/SKR/VRF tertutup).

## Performance

- **Started:** 2026-06-30T07:57:13Z
- **Completed:** 2026-06-30T08:28:59Z (~31m incl human UAT wait)
- **Task 1 (gate):** SELESAI — build 0-err + full suite 1027/0/2 (4m05s, incl Integration/SQLEXPRESS)
- **Task 2 (UAT):** ✅ APPROVED — checkpoint human-verify di-approve user, VRF-03 SATISFIED

## Accomplishments (Task 1 — gerbang otomatis)

- **`dotnet build HcPortal.csproj`** — 0 errors (24 warning pre-existing, di luar scope).
- **`dotnet test HcPortal.Tests`** (FULL, incl Integration/SQLEXPRESS) — **1027 passed / 0 failed / 2 skipped** (4m05s). Mencakup:
  - `QuestionTemplateBuilderTests` (Plan 01 — 15 test DataValidation read-back: dropdown QuestionType semua varian + numeric Skor 1-100 + DV-SKIP kolom lain + LEGACY-SAFE).
  - `SectionImportTests` (Plan 02 — SKOR-PARSE/default/atomic-reject/varian 101·5.5·-3 + 13-col-old regression).
  - `WeightedScoreImportTests` (Plan 02 GRADE-LOCK — import bobot non-uniform 30/10 via action asli `ImportPackageQuestions` -> `GradeAndCompleteAsync` -> `Score==75`).
  - `LegacyFormat_*` existing (dual-format kompatibel-mundur).
- **App lokal di-start** (`dotnet run @ http://localhost:5277`, Development) — startup bersih, "No migrations were applied. The database is already up to date." (konfirmasi migration=FALSE), seed no-op. Siap untuk UAT.

## Decisions Made

- **e2e Playwright roundtrip DI-DEFER (terdokumentasi, di-sanksi plan).** Plan Task 1 menandai e2e "OPSIONAL, recommended — bukan blocking bila e2e harness mahal untuk fase server-side ini" dan "Bila e2e tidak dibuat, dokumentasikan keputusan di SUMMARY". Rasional defer:
  1. **Roundtrip inti sudah ter-cover de-tautologis** oleh `WeightedScoreImportTests` (integration, SQLEXPRESS): mem-build .xlsx 14-kolom nyata dengan bobot 30/10, memanggil action asli `ImportPackageQuestions`, lalu `GradeAndCompleteAsync` dan meng-assert `Score==75`. Ini ADALAH roundtrip download->import-bobot->grade dikurangi lapisan HTTP/multipart.
  2. **Form import = plain server-parsed form-POST, TANPA client-side JS** preview/parse/cache (kontras `inject-excel-396.spec.ts` yang justified karena menguji JS `injExcelAnswersCache`). Lapisan browser di sini tak menambah cakupan-JS; server mem-parse file langsung saat POST.
  3. **Atomic-reject + render ScoreErrors** = server (`SectionImportTests`) + view Razor murni (no JS) — sudah ter-cover.
  4. **Permukaan yang genuinely belum terotomasi** = render dropdown Excel + penegakan numeric di Excel client = TEPAT lingkup Task 2 (human-verify), tak bisa diotomasi apa pun caranya (DataValidation render = perilaku Excel client, bukan ClosedXML write — kehadiran rule sudah di-assert di 01-T2 via `ws.DataValidations.TryGet`).
  5. RESEARCH §Validation Architecture eksplisit: "unit/integration menutupi sebagian besar; e2e opsional untuk fase kecil ini" (RESEARCH:334) + "fase ini server-side; e2e/unit menutupi mayoritas" (RESEARCH:368).
- **App di-start sebelum checkpoint** (automation-first, checkpoints.md) — user cukup buka URL, tak menjalankan command.

## Deviations from Plan

None pada Task 1. e2e defer = pilihan yang DI-SANKSI plan (bukan deviasi) — Task 1 secara eksplisit menyebut e2e opsional + minta dokumentasikan keputusan defer di SUMMARY (dilakukan di atas).

## UAT Task 2 — Entry Points (untuk user)

- **App live:** http://localhost:5277 (branch `main`, port 5277). Login admin/HC.
- **Unduh template langsung (tanpa paket):** `http://localhost:5277/Admin/DownloadQuestionTemplate?type=Universal` (dan `?type=MC` untuk legacy Single Answer).
- **Halaman Import (untuk uji import C/D):** `http://localhost:5277/Admin/ImportPackageQuestions?packageId=62` (paket "Quiz Random", 0 soal — target disposable). Tombol "Template Universal"/"Template Single Answer" ada di card biru.
- **Kolom kunci (Universal):** QuestionType = kolom **L**(12); Skor = kolom **N**(14); Jawaban Benar = kolom **H**(8) tanpa dropdown. Legacy: QuestionType = kolom **H**(8), TANPA kolom Skor.
- **Catatan numeric DV:** `ErrorStyle = Warning` (anti-typo non-otoritatif, D-10) — Excel menampilkan **dialog peringatan** berisi "Skor harus angka bulat 1-100" saat nilai di luar 1-100, BUKAN hard-block. Itu perilaku yang BENAR. Server tetap menolak-keras saat import (Plan 02).
- **Catatan klien:** **Excel desktop** paling andal untuk DataValidation. LibreOffice Calc merender dropdown List dengan baik; penegakan numeric warning bisa beda perilaku antar-versi.
- **Cleanup (CLAUDE.md Seed Workflow):** import menulis soal ke paket. Untuk uji C/D, gunakan paket disposable (62) lalu hapus soal uji via Kelola Soal, atau snapshot DB dulu. migration=FALSE (tak menyentuh schema).

## Verification (Task 1)

- `dotnet build HcPortal.csproj` — 0 errors.
- `dotnet test HcPortal.Tests` — 1027 passed / 0 failed / 2 skipped (full, incl Integration/SQLEXPRESS).
- App `dotnet run @ 5277` — listening, DB up-to-date (migration=FALSE), seed no-op.

## UAT Sign-Off (Task 2 — checkpoint:human-verify) — ✅ APPROVED 2026-06-30

User menjalankan langkah A-B di Excel desktop nyata dan membalas **"approved"**. Verdict per langkah:

| Langkah | Uji | Hasil |
|---------|-----|-------|
| **A.1** | Template Universal — dropdown QuestionType (kolom L) | ✅ PASS — dropdown 3 nilai (MultipleChoice, MultipleAnswer, Essay) muncul |
| **A.2** | Skor numeric (kolom N) — `0`/`150`/`5.5` ditegur, `25` diterima | ✅ PASS — warning "Skor harus angka bulat 1-100" menyala di luar 1-100; `25` diterima |
| **A.3** | Jawaban Benar (kolom H) — TANPA dropdown | ✅ PASS — tidak ada dropdown (ketik bebas) |
| **B.4** | Template legacy MC — dropdown QuestionType (kolom H) | ✅ PASS — dropdown 3 nilai muncul |
| **B.5** | Template legacy MC — TANPA kolom Skor | ✅ PASS — header berhenti di Rubrik, tidak ada Skor |
| **C** | Import roundtrip bobot (Skor=25 → tersimpan) | ✅ COVERED-BY-AUTOMATION — user skip manual; `WeightedScoreImportTests` (bobot 30/10 → `Score==75`) via action asli `ImportPackageQuestions`→`GradeAndCompleteAsync` @ SQLEXPRESS |
| **D** | Atomic-reject (Skor invalid → alert + 0-write) | ✅ COVERED-BY-AUTOMATION — user skip manual; `SectionImportTests` atomic-reject (≥1 invalid → TempData ScoreErrors + 0-write) |

**VRF-03 SATISFIED.** Permukaan genuinely-manual (render dropdown + penegakan numeric di Excel client) di-verifikasi manusia (A+B PASS). Permukaan server (import bobot + atomic-reject) di-cover de-tautologis oleh integration tests — user secara eksplisit memilih skip langkah manual C+D karena sudah ter-cover otomasi (1027/0/2). Tidak ada ketidaksesuaian dilaporkan.

## must_haves Verification

| Truth (plan must_haves) | Status |
|-------------------------|--------|
| Full suite (unit + integration SQLEXPRESS) hijau setelah kedua fitur terpasang | ✅ 1027/0/2 (Task 1 gate) |
| Roundtrip nyata: unduh template Universal → dropdown QuestionType muncul + sel Skor menolak nilai luar 1-100 | ✅ UAT Step A.1+A.2 human-verified PASS di Excel desktop |
| Roundtrip import: unggah file ber-bobot Skor → grading mencerminkan bobot | ✅ `WeightedScoreImportTests` Score==75 dari bobot 30/10 (integration, covered-by-automation per UAT) |

Artefak opsional `tests/e2e/excel-zeroconfig-99917.spec.ts` = DI-DEFER (sanksi plan; rasional di "Decisions Made"). key_link template→Excel client = manual, di-verifikasi via UAT Step A/B.

## Next Phase Readiness

- Plan 999.17-03 **COMPLETE**. Phase 999.17 siap close: 3/3 plan dieksekusi, 9 pseudo-REQ (DRP-01/02, SKR-01/02/03/04, VRF-01/02/03) tertutup. Tidak ada REQUIREMENTS.md (backlog phase) → tak ada `requirements mark-complete`.
- migration=FALSE di seluruh phase 999.17. Tidak ada blocker.
- Sisa manual (koordinasi, BUKAN dev): saat bundle deploy berikutnya, notify IT migration=FALSE untuk 999.17. v32.9 (fase 420) = milestone berikutnya, di-resume setelah 999.17 close.

---
*Phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp*
*Status: ✅ COMPLETE — UAT-approved (VRF-03 satisfied), 3/3 plan*
