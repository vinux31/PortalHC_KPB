# Phase 999.17: Excel "zero-config" template — dropdown Data Validation + import skor per-soal - Research

**Researched:** 2026-06-30
**Domain:** ASP.NET Core MVC (.NET 8) + ClosedXML Excel generation/parsing — Data Validation greenfield + import-path widening
**Confidence:** HIGH (API ClosedXML diverifikasi langsung dari DLL 0.105.0 terpasang; integrasi parser dibaca apa-adanya dari source)

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions (D-01..D-11 — verbatim)

**Dropdown Data Validation (Fitur 1)**
- **D-01:** Dropdown list HANYA pada kolom **`QuestionType`** (3 nilai tetap: `MultipleChoice`, `MultipleAnswer`, `Essay`). Satu-satunya kolom dengan set nilai tetap & terbatas.
- **D-02:** Dropdown diterapkan ke **semua template** (Universal 13/14-kolom + legacy MC/MA/Essay 9-kolom). DataValidation = validation-only, TAK mengubah struktur kolom → non-breaking untuk dual-format detection (`isNewFormat`).
- **D-03:** Cakupan baris **lebar: baris 2–1000** (range), bukan cuma baris contoh.
- **D-04:** Kolom **`Jawaban Benar` SKIP** (TIDAK dapat dropdown). Universal isinya campur (MC=1 huruf, MA=comma-list `A,C`, Essay=kosong) → list-validation akan korup. (Opsi per-varian DITOLAK user demi kesederhanaan.)
- **D-05:** Kolom lain (Pertanyaan, Opsi A–F, No. Section [angka dinamis], Nama Section, Elemen Teknis, Rubrik) = TANPA dropdown.

**Import Skor per-soal (Fitur 2)**
- **D-06:** "skor per-soal" = **bobot poin per soal** = `PackageQuestion.ScoreValue` (int, default 10). BUKAN skor hasil peserta. Field & grading-weighted SUDAH ADA; gap tunggal = import.
- **D-07:** Tambah kolom **`Skor`** HANYA di template **Universal** (jadi 14-kolom). Legacy 9-kolom tetap apa adanya (kompatibel-mundur). Hindari otak-atik header-marker detection legacy.
- **D-08:** Default saat kolom Skor **kosong/absen = 10**. Tidak wajib diisi.
- **D-09:** Validasi server saat import: **integer positif 1–100**. Tolak ≤0, >100, desimal, non-angka.
- **D-10:** Kolom Skor juga diberi **Data Validation WholeNumber Between 1–100** di sel (range baris 2–1000). Server TETAP validasi ulang (jangan percaya client).
- **D-11:** Auto-normalisasi skor akhir ke **100 SUDAH ADA** (`GradingService.cs:144`). TIDAK perlu kerjaan baru; cuma diverifikasi/dikunci dengan test. (Hasil di-truncate ke int — perilaku lama, di luar scope.)

### Claude's Discretion
- Nama header kolom persis ("Skor") + posisi kolom dalam header Universal (saran: agar dual-format detection tetap utuh).
- Cara wiring DataValidation ClosedXML (`List(...)` untuk QuestionType; `.WholeNumber.Between(1,100)` untuk Skor).
- Apakah baris contoh tetap ditampilkan di range yang sama dengan DataValidation.

### Deferred Ideas (OUT OF SCOPE)
- Dropdown `Jawaban Benar` A–D di template MC saja — DITOLAK user demi kesederhanaan.
- assessment-spec-wizard-debt (panel 7.2–7.4) — backlog test-debt TERPISAH.
- Kolom Skor di template legacy — sengaja TIDAK (D-07).
- Skor HASIL peserta (sudah ada di Inject v32.2); paritas form authoring (SUDAH ADA); tipe soal baru; breakdown skor per-section; dropdown pada kolom teks-bebas/dinamis.
</user_constraints>

<phase_requirements>
## Phase Requirements

Backlog phase — belum ada REQUIREMENTS.md. Scope diturunkan dari D-01..D-11. Tabel ini memetakan tiap keputusan ke titik implementasi + dukungan research, dipakai planner sebagai pseudo-REQ.

| Pseudo-REQ | Sumber | Behavior | Research Support |
|------------|--------|----------|------------------|
| DV-TYPE | D-01/D-02/D-03 | Dropdown List `QuestionType` (3 nilai) di SEMUA template, range baris 2–1000 | `range.CreateDataValidation().List("\"...\"", true)` (verified §Code Examples) pada kolom L (Universal) & H (legacy) |
| DV-SKIP | D-04/D-05 | Kolom selain QuestionType & Skor TANPA dropdown | Cukup tidak memanggil CreateDataValidation untuk kolom tsb — no-op |
| SKOR-COL | D-07 | Kolom `Skor` ditambah HANYA di template Universal → 14-kolom | Append sebagai kolom 14 (N) setelah Rubrik — tidak menggeser marker 6/7/9/10 (verified §Pitfall 1) |
| SKOR-DV | D-10 | Kolom Skor diberi WholeNumber Between 1–100 di sel (baris 2–1000) | `.WholeNumber.Between(1, 100)` overload `(Int32,Int32)` verified pada kolom N |
| SKOR-PARSE | D-06/D-08 | `ImportPackageQuestions` baca kolom Skor → set `ScoreValue` (kosong→10) ganti hardcode `=10` | Titik ubah: tuple baris (`AAC:7426`), branch isNewFormat (`AAC:7479`), build-loop (`AAC:7832`) |
| SKOR-VAL | D-09 | Validasi server int 1–100; tolak ≤0/>100/desimal/non-angka | Tambah ke pola error existing; lihat Open Question Q1 (skip-row vs tolak-keras) |
| GRADE-LOCK | D-11 | Normalisasi % 0–100 SUDAH ADA — kunci dengan test, jangan ubah | `GradingService.cs:99/112/127/144` verified read-only |
| LEGACY-SAFE | D-02/D-07 | Legacy 9-kolom + dual-format detection TIDAK rusak | Regression test `SectionImportTests.LegacyFormat_*` (existing) tetap hijau |
</phase_requirements>

## Summary

Fase ini menambah dua kapabilitas kecil-tapi-presisi pada satu jalur kode yang sudah matang (`AssessmentAdminController.DownloadQuestionTemplate` + `ImportPackageQuestions`). Keduanya **murni di tier API/Backend** — tidak menyentuh DB schema (no migration), tidak menyentuh grading, tidak menyentuh form authoring. Risiko terbesar BUKAN API ClosedXML (sudah diverifikasi penuh) melainkan **regresi senyap pada dual-format detection** (header-marker `isNewFormat` di `AAC:7463`) saat kolom `Skor` ditambahkan.

ClosedXML `DataValidation` adalah **greenfield di repo ini** (`git grep DataValidation` = 0), tapi library 0.105.0 mendukung penuh. API diverifikasi langsung dari `ClosedXML.dll` terpasang via reflection: `IXLRangeBase.CreateDataValidation()` → `IXLDataValidation` dengan `.List(string, bool)` (dropdown inline) dan `.WholeNumber.Between(int, int)`. Read-back untuk test tersedia via `IXLWorksheet.DataValidations` (enumerable `IXLDataValidation`) + `.GetAllInRange(...)`/`.TryGet(...)`.

Gap import = satu hardcode (`ScoreValue = 10` di `AAC:7832`). Kolom `Skor` paling aman **di-append sebagai kolom 14** (setelah `Rubrik`) — tidak menggeser satupun marker deteksi (posisi 6/7/9/10) dan kompatibel-mundur dengan file Universal 13-kolom yang sudah beredar (Cell(14) kosong → default 10). Pola test sudah ada persis (`SectionImportTests.cs`): bangun `.xlsx` nyata via ClosedXML → IFormFile stub → panggil action asli → assert DB.

**Primary recommendation:** Ekstrak generator template ke helper pure static `QuestionTemplateBuilder.Build(string type) → XLWorkbook` (meniru `InjectExcelHelper`), pasang DataValidation di sana → unit-testable tanpa DB; tambah field Skor sebagai kolom 14 Universal + parse di branch isNewFormat; pakai pola "tolak-keras atomic" (seperti SectionMismatch) untuk Skor invalid (lihat Open Question Q1). Migration = FALSE.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Generate template + DataValidation | API/Backend (controller/helper) | — | ClosedXML server-side; file di-stream ke client |
| Dropdown enforcement di Excel | Client (Excel app) | API/Backend (penulis aturan) | Validation dijalankan Excel saat user mengetik; server hanya menulis aturan |
| Parse kolom Skor → ScoreValue | API/Backend (`ImportPackageQuestions`) | — | Parsing + persist EF |
| Validasi Skor 1–100 (otoritatif) | API/Backend | Client (DV hint, non-otoritatif) | D-10: server WAJIB re-validate; client DV cuma anti-typo |
| Weighted grading (read-only) | API/Backend (`GradingService`) | — | Sudah ada; fase ini hanya mengunci dengan test |
| Persist soal+bobot | Database/Storage (EF→SQL Server) | — | `PackageQuestion.ScoreValue` kolom existing, no migration |

## Standard Stack

### Core
| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| ClosedXML | **0.105.0** | Generate `.xlsx` + Data Validation + parse import | Sudah dipakai luas (`XLWorkbook`, `ExcelExportHelper`, `InjectExcelHelper`); DataValidation built-in `[VERIFIED: HcPortal.csproj:14 + DLL reflection]` |
| .NET / ASP.NET Core MVC | net8.0 | Controller, IFormFile, FileContentResult | Platform proyek `[VERIFIED: HcPortal.csproj:4]` |
| EF Core (SqlServer) | 8.0.0 | Persist `PackageQuestion.ScoreValue` | Existing; no schema change `[VERIFIED: csproj]` |
| xUnit | 2.9.3 | Unit + integration test | Suite existing 702/702 `[VERIFIED: HcPortal.Tests.csproj:15]` |
| Playwright | 1.58.2 | e2e UAT browser | Specs existing di `tests/e2e/` `[VERIFIED: npx playwright --version]` |

### Supporting
| Library | Version | Purpose | When to Use |
|---------|---------|---------|-------------|
| EF Core InMemory | 8.0.0 | Unit test ringan tanpa SQL | Test parser yang tak butuh FK/unique-index `[VERIFIED: Tests.csproj:12]` |
| SQLEXPRESS (localhost\SQLEXPRESS) | running | Integration fixture (`SectionFixture`) | Test yang butuh FK Section→Package + unique index `[VERIFIED: service Running]` |

### Alternatives Considered
| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| ClosedXML DataValidation | Tulis OpenXML SDK manual | ClosedXML sudah dipakai & lebih ringkas; tak ada alasan turun ke OpenXML |
| Inline list `.List("\"a,b,c\"", true)` | List dari range sel (`.List(ws.Range(...))`) | Inline lebih sederhana untuk 3 nilai tetap (37 char, jauh di bawah limit 255). Range-list butuh sheet/kolom bantu — overkill |
| Append Skor kolom 14 | Sisip Skor sebelum QuestionType | Sisipan menggeser indeks kolom ET/Type/Rubrik di parser → perubahan lebih luas + risiko marker. Append = minimal |

**Installation:** Tidak ada paket baru. Semua sudah terpasang.

**Version verification:**
```bash
# ClosedXML 0.105.0 dikonfirmasi terpasang:
#   HcPortal.csproj:14  → <PackageReference Include="ClosedXML" Version="0.105.0" />
#   ~/.nuget/packages/closedxml/0.105.0/lib/netstandard2.0/ClosedXML.dll  (reflected)
```
`[VERIFIED: csproj + NuGet cache + DLL reflection 2026-06-30]`

## Architecture Patterns

### System Architecture Diagram

```
DOWNLOAD (zero-config out):
  Admin/HC ──GET /Admin/DownloadQuestionTemplate?type=Universal──▶ AssessmentAdminController
        │                                                                │
        │                                          ┌─────────────────────▼─────────────────────┐
        │                                          │ [REKOMENDASI] QuestionTemplateBuilder.Build │ (pure helper, no DB)
        │                                          │  • headers (9 vs 13/14 kolom)               │
        │                                          │  • example rows + instruction rows          │
        │                                          │  • CreateDataValidation().List(QuestionType)│  ← D-01/02/03
        │                                          │  • CreateDataValidation().WholeNumber(Skor) │  ← D-10
        │                                          └─────────────────────┬─────────────────────┘
        │                                                                │ XLWorkbook
        ◀────────── .xlsx (FileContentResult) ──── ExcelExportHelper.ToFileResult
        │
   [Excel app menegakkan dropdown + numeric saat admin mengetik]

IMPORT (skor in):
  Admin/HC ──POST /Admin/ImportPackageQuestions (excelFile)──▶ AssessmentAdminController.ImportPackageQuestions
                                                                    │
       guards (ext/size/lock) ──▶ XLWorkbook(stream) ──▶ isNewFormat? (header markers 6/7/9/10)
                                                                    │
                              ┌── NEW (≥ kolom): baca Cell(1..13) + [BARU] Cell(14)=Skor ──┐
                              └── OLD (9-kol): tanpa Skor → default 10 ────────────────────┘
                                                                    │ rows[] (+ Score)
                              validasi per-baris (Q/opsi/correct) + [BARU] validasi Skor 1–100 ── D-09
                                                                    │
                              dedup fingerprint + Section count guard (atomic) ── existing
                                                                    │
                              new PackageQuestion { ScoreValue = parsedScore }  ◀── ganti hardcode =10
                                                                    │ tx atomic
                                                              SaveChanges ──▶ SQL Server
                                                                    │
                              SyncToLinkedPostBestEffortAsync (Pre→Post) ── existing
```

### Recommended Project Structure
```
Controllers/
└── AssessmentAdminController.cs   # DownloadQuestionTemplate (delegasi ke helper) + ImportPackageQuestions (parse Skor)
Helpers/
├── ExcelExportHelper.cs           # ToFileResult (existing, dipakai apa-adanya)
├── InjectExcelHelper.cs           # POLA RUJUKAN: static GenerateTemplate→XLWorkbook + ParseMatrix
└── QuestionTemplateBuilder.cs     # [BARU - REKOMENDASI] pure: Build(string type)→XLWorkbook (DataValidation di sini)
HcPortal.Tests/
├── QuestionTemplateBuilderTests.cs # [BARU] unit: assert DataValidation hadir (no DB)
├── SectionImportTests.cs           # EXTEND: harness import → tambah test Skor parse/range/legacy
└── SectionFixture.cs               # reuse (SQLEXPRESS integration)
```

### Pattern 1: Pure static template-builder (ekstraksi seam)
**What:** Pindahkan logika bangun-workbook dari controller ke `QuestionTemplateBuilder.Build(string type) → XLWorkbook`. DataValidation dipasang di helper.
**When to use:** Selalu untuk fase ini — membuat DataValidation (greenfield) unit-testable TANPA membangun controller (yang butuh ~12 ctor arg) dan TANPA DB/fixture.
**Why:** Selaras pola repo (`InjectExcelHelper.GenerateTemplate`, `ShuffleEngine`, `SectionStructureComparer`, `CoerceCoachUnitScope`) — "ekstrak seam pure" yang dipuji di STATE.md. Controller jadi tipis: `return ExcelExportHelper.ToFileResult(QuestionTemplateBuilder.Build(type), fileName, this);`

### Pattern 2: DataValidation pada range kolom (verified)
**What:** Pasang aturan ke seluruh kolom baris 2–1000 sekali, bukan per-sel.
**Example:**
```csharp
// Source: ClosedXML 0.105.0 (verified DLL reflection) + wiki Data-Validation
// QuestionType dropdown (D-01/02/03) — Universal kolom L=12, legacy kolom H=8
var dvType = ws.Range("L2:L1000").CreateDataValidation();
dvType.List("\"MultipleChoice,MultipleAnswer,Essay\"", true); // inCellDropdown=true
dvType.IgnoreBlanks = true;       // kosong → default MultipleChoice (D-12 parser existing)
dvType.ErrorStyle = XLErrorStyle.Warning; // izinkan tetap, tapi tegur (zero-config friendly)

// Skor numeric (D-10) — Universal kolom N=14
var dvScore = ws.Range("N2:N1000").CreateDataValidation();
dvScore.WholeNumber.Between(1, 100); // overload (Int32,Int32) verified
dvScore.IgnoreBlanks = true;          // kosong diizinkan → server default 10 (D-08)
dvScore.ErrorTitle = "Skor tidak valid";
dvScore.ErrorMessage = "Skor harus angka bulat 1–100.";
```

### Pattern 3: Tambah kolom tanpa merusak dual-format detection
**What:** Append `Skor` sebagai kolom 14 (N) — setelah `Rubrik` (13/M). Header Universal jadi 14-kolom.
**Why aman:** `isNewFormat` (`AAC:7463`) hanya menguji `colCount > 9` + `HeaderAt(6)=="opsi e"` + `(7)=="opsi f"` + `(9)=="no. section"` + `(10)=="nama section"`. Append kolom 14 tidak menggeser satupun. File Universal 13-kolom lama tetap terdeteksi NEW; `Cell(14)` kosong → default 10 (kompatibel-mundur).

### Anti-Patterns to Avoid
- **Sisip Skor sebelum QuestionType / sebelum Section:** menggeser indeks parser (ET/Type/Rubrik) DAN berisiko menggeser marker 6/7/9/10 → korup dual-format. JANGAN.
- **Dropdown pada `Jawaban Benar`:** D-04 — campur format MC/MA/Essay → list-validation menolak `A,C` & memblok Essay kosong. JANGAN.
- **Pasang DataValidation per-sel dalam loop 999 baris:** boros + ClosedXML konsolidasi range; pakai satu `Range("X2:X1000")`. 
- **Percaya validasi client (DV) sebagai otoritatif:** D-10 — server WAJIB re-validate; DV hanya hint anti-typo.
- **Ubah `GradingService`:** D-11 — normalisasi sudah ada; cuma kunci dengan test.
- **Hapus/ubah jalur legacy 9-kolom:** D-07 — biarkan apa-adanya.

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Excel dropdown / numeric constraint | Tulis XML `<dataValidation>` manual ke sharedStrings/sheet | `range.CreateDataValidation().List(...)` / `.WholeNumber.Between(...)` | ClosedXML menangani serialisasi OOXML + escaping |
| Baca DataValidation untuk test | Parse XML `.xlsx` mentah | `ws.DataValidations` (enumerable) + `.GetAllInRange()`/`.TryGet()` | API read-back resmi, verified |
| Build .xlsx untuk test fixture | String/CSV palsu | `XLWorkbook → MemoryStream → FormFile` (pola `SectionImportTests.BuildFile`) | Pola test asli sudah ada; de-tautology (file nyata, action nyata) |
| Normalisasi skor ke 100 | Hitung ulang persentase | `GradingService.cs:144` (sudah ada) | D-11 — jangan duplikasi |
| Validasi int positif | Regex sendiri | `int.TryParse` + cek range | Hindari edge-case desimal/whitespace/negatif |

**Key insight:** Hampir seluruh fase ini = "menyalakan" kapabilitas yang sudah ada (ScoreValue field, weighted grading, dual-format parser, test harness). Satu-satunya yang benar-benar baru = pemanggilan API DataValidation. Jangan bangun ulang apapun yang sudah ada.

## Common Pitfalls

### Pitfall 1: Penambahan kolom Skor merusak `isNewFormat` (regresi senyap)
**What goes wrong:** Kolom Skor disisipkan di tengah → marker header (Opsi E@6, Opsi F@7, No. Section@9, Nama Section@10) bergeser → file salah-deteksi → peta kolom geser → korupsi senyap (jawaban/section nyasar).
**Why:** Detektor mengandalkan POSISI kolom 6/7/9/10 (`AAC:7463-7467`), bukan colCount saja (ini sengaja — M2 fix Phase 415).
**How to avoid:** Append `Skor` sebagai kolom **14** (setelah Rubrik). Tidak menggeser marker. Tambahkan regression test yang mengimpor file 13-kolom lama (tanpa Skor) → tetap terdeteksi NEW + ScoreValue=10.
**Warning signs:** Test `SectionImportTests.LegacyFormat_*` atau `NewFormat_*` mulai gagal; section/correct-letter nyasar.

### Pitfall 2: Error Skor invalid TER-TELAN (silent skip) — bukan "Tolak" sesuai D-09
**What goes wrong:** Pola per-baris existing memakai `errors.Add(...) ; continue;` — TAPI list `errors` ini **tidak pernah disurface ke user di success-path** (hanya dipakai di early-return empty-worksheet, `AAC:7449`). Jadi baris ber-Skor invalid akan **drop senyap** (soal hilang sama sekali), bukan default 10, bukan pesan jelas. Bertentangan dengan tema zero-config & D-09 "Tolak".
**Why:** Loop build hanya `continue` pada baris invalid; final return cuma TempData Success/Warning + count. `errors` di-collect tapi tak ditampilkan.
**How to avoid:** Pakai pola **tolak-keras atomic** yang sudah terbukti (`SectionMismatch`, `AAC:7703-7708`): kumpulkan error Skor, jika ada → serialize ke `TempData["...Error"]` + `RedirectToAction` + **0 write**. View sudah punya scaffold render daftar (lihat `ImportPackageQuestions.cshtml:33-46`). Lihat Open Question Q1 untuk keputusan scope.
**Warning signs:** Admin impor 140 soal, satu Skor typo, hasil "139 added" tanpa tahu yang ke-140 hilang.

### Pitfall 3: Inline-list double-quote & limit 255 char
**What goes wrong:** Lupa membungkus nilai dengan literal double-quotes → ClosedXML/Excel menafsirkan string sebagai formula/range, dropdown tak muncul.
**Why:** Excel membedakan literal-list (`"a,b,c"`) vs formula via tanda kutip.
**How to avoid:** String harus `"\"MultipleChoice,MultipleAnswer,Essay\""` (37 char, < limit 255). `[CITED: ClosedXML wiki Data-Validation]`
**Warning signs:** Dropdown kosong/absen di Excel; atau error "list source must be delimited".

### Pitfall 4: `.xls` lama tak terbaca ClosedXML (pre-existing, di luar scope)
**What goes wrong:** Endpoint mengizinkan ekstensi `.xls` (`AAC:7375`) tapi ClosedXML hanya membaca OOXML `.xlsx` → file `.xls` biner asli melempar exception (sudah ditangkap → TempData Error generik).
**Why:** Pre-existing; bukan diintroduksi fase ini.
**How to avoid:** Tidak perlu diubah (di luar scope). Template DOWNLOAD selalu `.xlsx` → DataValidation persist sempurna. Cukup catat agar tak bingung saat test.

### Pitfall 5: Baris contoh vs DataValidation pada range yang sama
**What goes wrong:** Khawatir baris contoh (gray text, rows 2–4) bentrok dengan DV pada L2:L1000 / N2:N1000.
**Why:** Tidak bentrok — nilai contoh QuestionType ("MultipleChoice" dst) valid terhadap list; baris instruksi ada di kolom A saja (bukan L/N). DV hanya menegur saat EDIT, tidak menandai nilai existing.
**How to avoid:** Biarkan contoh tetap (Claude's Discretion). Pastikan nilai contoh QuestionType persis salah satu dari 3 enum (sudah benar di `AAC:7307-7320`).

## Code Examples

### Read-back DataValidation untuk test (assert dropdown + numeric hadir)
```csharp
// Source: ClosedXML 0.105.0 (verified DLL reflection 2026-06-30)
using var wb = new XLWorkbook(new MemoryStream(fileBytes)); // bytes dari FileContentResult
var ws = wb.Worksheets.First();

// Semua aturan validasi di worksheet (IXLDataValidations : IEnumerable<IXLDataValidation>)
var allDv = ws.DataValidations.ToList();

// Aturan yang mengenai sel QuestionType (Universal L2)
Assert.True(ws.DataValidations.TryGet(ws.Range("L2:L1000").RangeAddress, out var dvType));
Assert.Equal(XLAllowedValues.List, dvType.AllowedValues);
Assert.True(dvType.InCellDropdown);
Assert.Contains("MultipleChoice", dvType.Value); // .Value = "\"MultipleChoice,MultipleAnswer,Essay\""

// Aturan numeric Skor (Universal N2)
Assert.True(ws.DataValidations.TryGet(ws.Range("N2:N1000").RangeAddress, out var dvScore));
Assert.Equal(XLAllowedValues.WholeNumber, dvScore.AllowedValues);
Assert.Equal(XLOperator.Between, dvScore.Operator);
Assert.Equal("1", dvScore.MinValue);
Assert.Equal("100", dvScore.MaxValue);
```
Verified members: `IXLWorksheet.DataValidations` (→ `IXLDataValidations`), `.TryGet(IXLRangeAddress, out IXLDataValidation)`, `.GetAllInRange(IXLRangeAddress)`; `IXLDataValidation` props: `AllowedValues` (enum `XLAllowedValues{AnyValue,WholeNumber,Decimal,Date,Time,TextLength,List,Custom}`), `Operator` (enum `XLOperator{...,Between,...}`), `MinValue`/`MaxValue`/`Value` (string), `InCellDropdown`/`IgnoreBlanks` (bool), `Ranges` (IEnumerable<IXLRange>). `[VERIFIED: DLL reflection]`

### Parse kolom Skor di branch isNewFormat (titik ubah)
```csharp
// Di branch isNewFormat (AAC:7479-7498), tambah setelah baca Rubrik:
var rawScore = (row.Cell(14).GetString() ?? "").Trim();   // kolom N (Skor)
// (legacy branch: tidak ada kolom Skor → score = null/default)

// Di build-loop (AAC:7751+), ganti hardcode ScoreValue=10:
int scoreValue = 10;                                       // D-08 default
if (!string.IsNullOrWhiteSpace(rawScore))
{
    if (!int.TryParse(rawScore, NumberStyles.None, CultureInfo.InvariantCulture, out scoreValue)
        || scoreValue < 1 || scoreValue > 100)             // D-09: tolak desimal/non-angka/≤0/>100
    {
        scoreErrors.Add($"Baris {i + 1}: Skor harus angka bulat 1–100. Ditemukan '{rawScore}'.");
        continue; // ATAU akumulasi → tolak-keras atomic (lihat Open Question Q1)
    }
}
var newQ = new PackageQuestion { /* ... */ ScoreValue = scoreValue, /* ... */ };
```
`NumberStyles.None` menolak desimal/whitespace/tanda — selaras D-09. `[VERIFIED: AAC:7479-7519,7824-7836 read]`

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| `cell.DataValidation` property (getter auto-create, ambigu) | `range.CreateDataValidation()` eksplisit (returns IXLDataValidation) | ClosedXML ~0.95+ | Pakai `CreateDataValidation()` — present & disarankan di 0.105.0 (verified). `.DataValidation` getter masih ada (wiki memakainya) tapi `CreateDataValidation()` lebih eksplisit untuk range. |
| Template `AddInstruction` teks merah (admin ketik manual) | Dropdown DataValidation (pilih dari list) | Fase ini (999.17) | Anti salah-ketik; instruksi teks boleh tetap sebagai pelengkap |
| `ScoreValue = 10` hardcode di import | Baca kolom Skor (default 10) | Fase ini | Bobot per-soal bisa di-set via Excel |

**Deprecated/outdated:**
- Tidak ada API ClosedXML yang deprecated dipakai. `.List(string,bool)`, `.WholeNumber.Between(int,int)`, `CreateDataValidation()`, `ws.DataValidations` semua present di 0.105.0.

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | Untuk Skor invalid, **tolak-keras atomic** (0 write + surface) lebih sesuai D-09 "Tolak" + tema zero-config daripada silent-skip | Pitfall 2 / Open Q1 | Jika user sebenarnya mau skip-row-default, planner pakai pola lain. Behavior-level decision — perlu konfirmasi. |
| A2 | Header kolom dinamai persis **"Skor"** dan di-append sebagai kolom 14 | SKOR-COL | Nama/posisi adalah Claude's Discretion; jika user mau posisi lain, sesuaikan (tapi append-14 paling aman) |
| A3 | Baris contoh tetap ditampilkan di range DV (tidak dihapus) | Pitfall 5 | Claude's Discretion; kosmetik, low risk |
| A4 | `ErrorStyle = Warning` (bukan Stop) agar admin tetap bisa lanjut tapi ditegur | Pattern 2 | Pilihan UX; `Stop` akan memblok input non-list total. Low risk, mudah diubah |

**Catatan:** Tidak ada klaim API ClosedXML yang ASSUMED — seluruhnya VERIFIED via reflection DLL 0.105.0.

## Open Questions (RESOLVED)

1. **Scope penolakan Skor invalid: skip-baris vs tolak-keras seluruh import?** — **RESOLVED via D-12 CONTEXT.md: TOLAK-KERAS ATOMIC** (≥1 Skor invalid → 0 write + surface daftar baris via `TempData["ScoreErrors"]`, pola `SectionMismatch`). User mengonfirmasi 2026-06-30.
   - What we know: Pola per-baris existing = `errors.Add + continue` TAPI `errors` tak pernah disurface (silent). Pola "tolak-keras atomic" sudah ada & terbukti = `SectionMismatch` (0 write + daftar lengkap via TempData + view scaffold siap).
   - What's unclear: D-09 "Tolak" tidak eksplisit menyebut tolak-baris vs tolak-import. → DIPUTUSKAN: tolak-import (atomic).
   - Recommendation: **Tolak-keras atomic**. Alasan: silent-skip akan men-drop soal tanpa jejak (lebih buruk dari default), bertentangan zero-config.

2. **DataValidation pada template legacy: pasang juga numeric? (Tidak)** — **RESOLVED via D-07 CONTEXT.md: TIDAK di legacy** — legacy hanya dropdown QuestionType (kolom H=8), TANPA kolom/DV Skor.
   - What we know: D-07 — kolom Skor HANYA Universal. Legacy 9-kolom tanpa Skor.
   - Recommendation: Legacy dapat **dropdown QuestionType saja** (D-02, kolom H=8). TANPA kolom/DV Skor. Dicatat agar planner tak keliru menambah Skor ke legacy.

3. **Range 1000 baris cukup?** — **RESOLVED via D-03 CONTEXT.md: pakai 1000 baris** (2–1000).
   - What we know: D-03 menetapkan baris 2–1000.
   - Recommendation: Pakai 1000 sesuai D-03. Jika import >999 soal sekaligus, DV tak menjangkau baris >1000 (data tetap ter-import, hanya tanpa hint dropdown — server validasi tetap jalan). Acceptable; jangan over-engineer.

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| .NET SDK | build + test + run | ✓ | 8.0.418 | — |
| ClosedXML | template + parse + DV | ✓ | 0.105.0 | — |
| SQLEXPRESS (localhost\SQLEXPRESS) | integration test (`SectionFixture`) | ✓ | Running | — |
| Node.js | Playwright e2e | ✓ | v24.14.0 | — |
| Playwright | e2e UAT browser | ✓ | 1.58.2 | unit/integration menutupi sebagian besar; e2e opsional untuk fase kecil ini |
| Aplikasi lokal | UAT manual (port 5277 / 5270 ITHandoff) | n/a (start saat UAT) | — | — |

**Missing dependencies with no fallback:** Tidak ada.
**Missing dependencies with fallback:** Tidak ada — toolchain lengkap.

## Validation Architecture

Test framework terdeteksi penuh; ada pola test PERSIS untuk import (`SectionImportTests.cs`) dan pola pure-helper (`InjectExcelHelperTests.cs`).

### Test Framework
| Property | Value |
|----------|-------|
| Framework | xUnit 2.9.3 (+ EF InMemory 8.0.0 / SQLEXPRESS fixture) |
| Config file | `HcPortal.Tests/HcPortal.Tests.csproj` (no separate runner config) |
| Quick run command | `dotnet test HcPortal.Tests --filter "Category!=Integration"` (fast, no DB) |
| Full suite command | `dotnet test HcPortal.Tests` (incl. Integration — butuh SQLEXPRESS) |
| e2e | Playwright 1.58.2, specs di `tests/e2e/*.spec.ts` |

### Phase Requirements → Test Map
| Pseudo-REQ | Behavior | Test Type | Automated Command | File Exists? |
|------------|----------|-----------|-------------------|-------------|
| DV-TYPE | Universal & legacy & MC/MA/Essay punya List DV `QuestionType` (3 nilai) di range | unit (pure helper) | `dotnet test --filter "QuestionTemplateBuilderTests"` | ❌ Wave 0 |
| DV-SKIP | Kolom `Jawaban Benar`/Opsi/Section TANPA DV | unit | (idem) assert `DataValidations` tak mengenai kolom tsb | ❌ Wave 0 |
| SKOR-DV | Universal kolom Skor punya WholeNumber Between 1–100 | unit | (idem) `TryGet(N2:N1000)` → AllowedValues=WholeNumber, Min=1 Max=100 | ❌ Wave 0 |
| SKOR-PARSE | Import Skor=25 → ScoreValue=25; kosong → 10 | integration | `dotnet test --filter "SectionImportTests"` (extend) | ⚠️ extend existing |
| SKOR-VAL | Skor 0/101/"abc"/"5.5" → ditolak (0 write atau skip per Q1) | integration | (idem) | ❌ Wave 0 |
| LEGACY-SAFE | File 9-kolom + Universal 13-kolom lama → detect benar + ScoreValue=10 | integration | `dotnet test --filter "SectionImportTests"` (existing `LegacyFormat_*` + tambah 13-col-no-skor) | ⚠️ extend |
| GRADE-LOCK | maxScore=Σ ScoreValue; finalPercentage=round(total/max*100); bobot non-uniform → % benar | unit/integration | new `WeightedScoreImportTests` atau extend grading test | ❌ Wave 0 |
| E2E (opsional) | Download Universal → buka → dropdown+numeric hadir → import dgn bobot → grading reflect | e2e | Playwright spec `excel-zeroconfig-99917.spec.ts` | ❌ opsional |

### Sampling Rate
- **Per task commit:** `dotnet test HcPortal.Tests --filter "QuestionTemplateBuilderTests|SectionImportTests"` (cepat untuk loop merah-hijau)
- **Per wave merge:** `dotnet test HcPortal.Tests` (full suite, incl. Integration — SQLEXPRESS)
- **Phase gate:** Full suite hijau + (jika diadakan) Playwright e2e hijau sebelum `/gsd-verify-work`. UAT manual browser disarankan (lesson 354 — tapi fase ini server-side; e2e/unit menutupi mayoritas).

### Wave 0 Gaps
- [ ] `HcPortal.Tests/QuestionTemplateBuilderTests.cs` — assert DataValidation hadir/absen per kolom & per varian template (DV-TYPE, DV-SKIP, SKOR-DV). Pure, no DB. Pola: `InjectExcelHelperTests`.
- [ ] `HcPortal.Tests/SectionImportTests.cs` — EXTEND: `BuildNewFormatFile` jadi 14-kolom (header + Skor); tambah test SKOR-PARSE (25→25), default (kosong→10), SKOR-VAL (0/101/desimal/non-angka → ditolak), regression file 13-kolom-lama → ScoreValue=10.
- [ ] `HcPortal.Tests/WeightedScoreImportTests.cs` (atau extend grading test) — GRADE-LOCK: import bobot non-uniform → `GradingService` hasilkan % benar; kunci `:99/112/127/144`.
- [ ] (opsional) `tests/e2e/excel-zeroconfig-99917.spec.ts` — roundtrip download→import→grade.
- Framework install: tidak perlu (semua terpasang).

*(Jika QuestionTemplateBuilder TIDAK diekstrak: test DV harus lewat controller harness `SectionImportTests.MakeController` → panggil `DownloadQuestionTemplate` → `FileContentResult.FileContents` → reopen. Lebih berat; ekstraksi helper lebih disarankan.)*

## Security Domain

Endpoint import = attack surface (file upload). Fase ini menambah satu input-validation surface (Skor). Mayoritas kontrol sudah ada.

### Applicable ASVS Categories
| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | yes (existing) | `[Authorize(Roles="Admin, HC")]` pada Download & Import `[VERIFIED: AAC:7266,7367]` |
| V4 Access Control | yes (existing) | RBAC role-gated + `[ValidateAntiForgeryToken]` pada POST import `[VERIFIED: AAC:7368]` |
| V5 Input Validation | **yes (BARU)** | Skor: `int.TryParse` + range 1–100 server-side (D-09). Jangan percaya DV client (D-10) |
| V6 Cryptography | no | Tidak ada kripto baru |
| V12 File Upload | yes (existing) | Guard ekstensi `.xlsx/.xls` + size ≤5MB + try/catch parse `[VERIFIED: AAC:7373-7389]` |

### Known Threat Patterns for ClosedXML import
| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Skor injection (negatif/overflow/desimal) men-skew grading | Tampering | Server re-validate 1–100, `NumberStyles.None`, tipe `int` (D-09) — JANGAN andalkan DV client |
| Oversized/zip-bomb `.xlsx` | DoS | Size guard 5MB existing; ClosedXML stream read |
| CSV/formula injection di sel teks | Injection (downstream) | Out of scope (Skor=numeric; teks soal sudah ditangani existing rendering) |
| Bypass antiforgery / role | Spoofing/Elevation | `[ValidateAntiForgeryToken]` + `[Authorize]` existing — pertahankan |

**Catatan:** Tidak ada penurunan kontrol keamanan. Tambahan satu-satunya = validasi Skor server-side (memperkuat, bukan melemahkan).

## Sources

### Primary (HIGH confidence)
- **ClosedXML 0.105.0 DLL reflection** (`~/.nuget/packages/closedxml/0.105.0/lib/netstandard2.0/ClosedXML.dll`) — `IXLRangeBase.CreateDataValidation()→IXLDataValidation`; `IXLDataValidation` props (AllowedValues/Operator/MinValue/MaxValue/Value/InCellDropdown/IgnoreBlanks/Ranges) + `.List(string,bool)` + `.WholeNumber.Between(Int32,Int32)`; `IXLWorksheet.DataValidations→IXLDataValidations` (IEnumerable + GetAllInRange/TryGet); enum `XLAllowedValues`/`XLOperator`. Verified 2026-06-30.
- **Source repo (read apa-adanya):** `Controllers/AssessmentAdminController.cs:7264-7348` (DownloadQuestionTemplate), `:7366-7968` (ImportPackageQuestions, isNewFormat `:7463`, hardcode ScoreValue `:7832`), `Services/GradingService.cs:90-144` (weighted+normalize), `Models/AssessmentPackage.cs:73` (ScoreValue), `Helpers/InjectExcelHelper.cs` (pola helper), `HcPortal.Tests/SectionImportTests.cs` (pola test import), `HcPortal.Tests/SectionFixture.cs`, `Views/Admin/ImportPackageQuestions.cshtml:17-46,71-114`.
- **CONTEXT.md** D-01..D-11 (authoritative decisions).

### Secondary (MEDIUM confidence)
- ClosedXML Wiki — Data Validation (via Context7 `/closedxml/closedxml` + WebFetch): `.List("\"a,b,c\"", true)` inline dropdown; `range.DataValidation.WholeNumber.EqualTo(...)`; literal double-quotes wajib untuk inline list.
- WebSearch (itenium.be, c-sharpcorner, ClosedXML wiki) — konfirmasi pola inline-list + InCellDropdown + IgnoreBlanks.

### Tertiary (LOW confidence)
- (none — semua klaim API ter-cross-verify dengan reflection DLL)

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — versi diverifikasi dari csproj + NuGet cache.
- ClosedXML DataValidation API: HIGH — reflection langsung DLL 0.105.0 (bukan training data).
- Integrasi parser/import: HIGH — source dibaca apa-adanya; titik ubah eksak.
- Pola test: HIGH — `SectionImportTests` + `InjectExcelHelperTests` ada & relevan.
- Keputusan scope penolakan Skor (skip vs tolak-keras): MEDIUM — desain, perlu konfirmasi (A1/Open Q1).

**Research date:** 2026-06-30
**Valid until:** ~30 hari (stack stabil; ClosedXML 0.105.0 pinned; tidak ada API fast-moving)
