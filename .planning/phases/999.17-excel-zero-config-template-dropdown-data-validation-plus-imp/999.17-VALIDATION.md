---
phase: 999.17
slug: excel-zero-config-template-dropdown-data-validation-plus-imp
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-30
---

# Phase 999.17 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Detail "Validation Architecture" ada di `999.17-RESEARCH.md` §Validation Architecture (line 340+). Planner mengisi Per-Task map di bawah.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit (.NET) + Playwright (e2e) |
| **Config file** | `tests/` (xUnit project) + `tests/e2e/` (Playwright) |
| **Quick run command** | `dotnet test --filter "FullyQualifiedName~Import|FullyQualifiedName~Template"` |
| **Full suite command** | `dotnet test` |
| **Estimated runtime** | ~60–120 detik (unit) |

---

## Sampling Rate

- **After every task commit:** Run quick filtered test (Import/Template).
- **After every plan wave:** Run full `dotnet test`.
- **Before `/gsd-verify-work`:** Full suite green + (jika UI-touching) Playwright import e2e green.
- **Max feedback latency:** ~120 detik.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _planner mengisi_ | | | (D-01..D-12) | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- Reuse pola test existing: `tests/.../SectionImportTests.cs` (import .xlsx nyata → IFormFile → action → assert DB) + `InjectExcelHelperTests.cs` (pure helper). Planner: ekstrak `QuestionTemplateBuilder.Build(type)→XLWorkbook` agar DataValidation unit-testable tanpa DB.
- Tidak perlu install framework baru (xUnit + Playwright sudah ada).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Dropdown tampak & berfungsi di Excel sungguhan | D-01/D-02/D-03 | DataValidation render = perilaku Excel client, bukan ClosedXML write | Unduh template → buka di Excel/LibreOffice → klik sel QuestionType → konfirmasi dropdown 3 nilai; sel Skor → ketik 0/150 → ditolak |

*Otomatis: kehadiran DataValidation rule di .xlsx di-assert via `ws.DataValidations.TryGet` (bukan manual).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
