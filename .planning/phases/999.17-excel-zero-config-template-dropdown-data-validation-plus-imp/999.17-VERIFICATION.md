---
phase: 999.17-excel-zero-config-template-dropdown-data-validation-plus-imp
verified: 2026-06-30T09:00:00Z
status: passed
score: 7/7
overrides_applied: 0
re_verification: false
---

# Phase 999.17: Excel Zero-Config Template Verification Report

**Phase Goal:** Template Excel soal "zero-config": (1) dropdown Data Validation kolom QuestionType (3 nilai tetap) di SEMUA template + numeric DV Skor 1-100 di Universal; (2) kolom "Skor" baru di Universal ke-parse saat ImportPackageQuestions → PackageQuestion.ScoreValue (ganti hardcode 10), validasi server int 1-100 atomic hard-reject. migration=FALSE; GradingService NOT modified.
**Verified:** 2026-06-30T09:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | QuestionTemplateBuilder.cs exists sebagai pure static seam + DownloadQuestionTemplate mendelegasikan ke-nya | VERIFIED | `Helpers/QuestionTemplateBuilder.cs` 137 baris; AAC:7272 `using var workbook = QuestionTemplateBuilder.Build(type);` |
| 2 | Dropdown DataValidation QuestionType (List 3-nilai) di Universal kolom L(12) baris 2-1000 AND legacy kolom H(8) baris 2-1000 | VERIFIED | QTB.cs:112 `ws.Range("L2:L1000").CreateDataValidation()` + `.List(typeList, true)`; QTB.cs:128 `ws.Range("H2:H1000").CreateDataValidation()` + `.List(typeList, true)`; QuestionTemplateBuilderTests.cs:100-104 + 118-121 assert TryGet + XLAllowedValues.List + InCellDropdown + ketiga nilai |
| 3 | Kolom Skor (N/14) append HANYA di Universal; WholeNumber Between 1-100 DV hadir; legacy TANPA kolom/DV Skor | VERIFIED | QTB.cs:38 header Universal 14 elemen terakhir="Skor"; QTB.cs:118-121 `ws.Range("N2:N1000").CreateDataValidation(); dvScore.WholeNumber.Between(1, 100)`; Legacy header baris 39 = 9 elemen tanpa Skor; Tests:135-139 assert WholeNumber/Between/Min=1/Max=100; Tests:153-156 assert TryGet=false pada kolom-skip |
| 4 | ImportPackageQuestions mem-parse kolom Skor (Cell(14)) di branch isNewFormat → sets ScoreValue; hardcode ScoreValue=10 di import path DIHAPUS | VERIFIED | AAC:7431 `rawScore = (row.Cell(14).GetString() ?? "").Trim()` dalam blok isNewFormat; AAC:7797 `ScoreValue = scoreValue`; `git grep "ScoreValue = 10," Controllers/AssessmentAdminController.cs` = 0 match |
| 5 | Validasi server int 1-100 (NumberStyles.None) + atomic hard-reject: ≥1 baris invalid → TempData["ScoreErrors"] + RedirectToAction + 0-write (bukan silent-skip) | VERIFIED | AAC:7518 `int.TryParse(rawScore, NumberStyles.None, CultureInfo.InvariantCulture, out var sv) \|\| sv < 1 \|\| sv > 100`; AAC:7522-7526 `if (scoreErrors.Any()) { TempData["ScoreErrors"] = ...; return RedirectToAction(...); }`; View:54-65 render alert-danger ScoreErrors; SectionImportTests:558-629 atomic-reject 0-write tests |
| 6 | GradingService.cs TIDAK diubah (D-11 grade-lock) | VERIFIED | `git diff cbab55e1 HEAD -- Services/GradingService.cs` = output kosong (0 diff) |
| 7 | Build 0-err + suite 1027/0/2 (incl Integration/SQLEXPRESS); QuestionTemplateBuilderTests 15 kasus; WeightedScoreImportTests Grade-Lock Score==75 | VERIFIED | Diff scope 6 files 865+/80- confirmed; QuestionTemplateBuilderTests: 6 [Fact] + 3 [Theory]×3 [InlineData] = 15 kasus; WeightedScoreImportTests.cs:269 `Assert.Equal(75, graded!.Score)`; Suite 1027/0/2 verified UAT Plan 03 Task 1 |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Helpers/QuestionTemplateBuilder.cs` | Pure static builder template soal Excel + DataValidation | VERIFIED | 137 baris; `public static XLWorkbook Build(string type)`; 3 CreateDataValidation calls (L-Universal, N-Universal, H-legacy) |
| `HcPortal.Tests/QuestionTemplateBuilderTests.cs` | 15 unit test DataValidation read-back tanpa DB | VERIFIED | 9 method (6 Fact + 3 Theory); 3 Theory × 3 InlineData = 15 kasus; TryGet assert + AllowedValues + InCellDropdown + round-trip serialisasi |
| `HcPortal.Tests/WeightedScoreImportTests.cs` | Grade-lock integration test Score==75 | VERIFIED | 272 baris; `WeightedScoreFixture` disposable SQLEXPRESS; `Assert.Equal(75, graded!.Score)` baris 269; real ImportPackageQuestions action + GradeAndCompleteAsync |
| `Controllers/AssessmentAdminController.cs` (modified) | DownloadQuestionTemplate delegasi tipis + ImportPackageQuestions parse Skor | VERIFIED | AAC:7272 delegasi Build; AAC:7358 rawScores list; AAC:7431 Cell(14); AAC:7514-7526 atomic-reject pass; AAC:7797 ScoreValue=scoreValue |
| `Views/Admin/ImportPackageQuestions.cshtml` (modified) | Render ScoreErrors alert-danger + dokumentasi kolom Skor | VERIFIED | View:54-65 blok ScoreErrors; View:81 header Skor; View:89 bullet penjelasan 1-100/default-10/atomic-reject |
| `HcPortal.Tests/SectionImportTests.cs` (extended) | Skor-parse/default/atomic-reject/legacy-safe/varian tests | VERIFIED | 12 test method terkait Skor; SKOR-PARSE, atomic-reject 2 kasus error, Theory varian (101/5.5/-3), 13-col-old regression |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `DownloadQuestionTemplate` | `QuestionTemplateBuilder.Build` | Delegasi langsung AAC:7272 | WIRED | `using var workbook = QuestionTemplateBuilder.Build(type)` + `ExcelExportHelper.ToFileResult` |
| `ImportPackageQuestions` | `rawScores[i]` (list paralel) | `row.Cell(14).GetString()` di branch isNewFormat | WIRED | AAC:7431 reads Cell(14); AAC:7455 adds to rawScores lockstep; AAC:7500 null untuk legacy |
| `rawScores` atomic-reject pass | `TempData["ScoreErrors"]` + 0-write | `if (scoreErrors.Any()) RedirectToAction` AAC:7522-7526 | WIRED | Pass mandiri mendahului semua DB-write; atomic dijamin |
| `scoreValue` di build-loop | `PackageQuestion.ScoreValue` | AAC:7797 `ScoreValue = scoreValue` | WIRED | Hardcode 10 tidak ada; default 10 hanya jika rawScore kosong/null (D-08) |
| `TempData["ScoreErrors"]` | View alert-danger render | `ImportPackageQuestions.cshtml:56-70` deserialize + `<ul>` per-baris | WIRED | Twin pola SectionMismatch; render terkonfirmasi baris 56+65 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `QuestionTemplateBuilder.Build` | `XLWorkbook` dengan DataValidation | ClosedXML in-memory construction | Ya — DV rules ditulis langsung ke workbook, bukan dari DB | FLOWING |
| `ImportPackageQuestions` import-path | `rawScores` list | `row.Cell(14).GetString()` dari file Excel yang diunggah | Ya — reads actual cell value dari file binary | FLOWING |
| `PackageQuestion.ScoreValue` | `scoreValue` variable | Parsed dari `rawScores[i]` dengan `int.TryParse` + range guard | Ya — ScoreValue Excel → DB tersimpan via EF SaveChanges | FLOWING |
| `WeightedScoreImportTests` | `graded.Score` | Real `ImportPackageQuestions` action (SQLEXPRESS) → `GradeAndCompleteAsync` | Ya — 30/10 bobot → Score=75 de-tautologis | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Evidence | Status |
|----------|----------|--------|
| Dropdown QuestionType muncul di kolom L Universal (Excel desktop) | UAT Step A.1 human-approved 2026-06-30: "dropdown 3 nilai muncul" | PASS |
| Numeric DV Skor menegur 0/150/5.5 di kolom N, menerima 25 | UAT Step A.2 human-approved: "warning menyala di luar 1-100; 25 diterima" | PASS |
| Jawaban Benar (H) TANPA dropdown | UAT Step A.3 human-approved: "tidak ada dropdown (ketik bebas)" | PASS |
| Legacy MC — dropdown QuestionType (kolom H) | UAT Step B.4 human-approved: "dropdown 3 nilai muncul" | PASS |
| Legacy MC — TANPA kolom Skor | UAT Step B.5 human-approved: "header berhenti di Rubrik" | PASS |
| Import roundtrip bobot → grading mencerminkan bobot (Score==75) | WeightedScoreImportTests integration test `Assert.Equal(75, graded!.Score)` — covered-by-automation per UAT | PASS |
| Atomic-reject Skor invalid → TempData ScoreErrors + 0-write | SectionImportTests atomic-reject tests (2 kasus error, Theory varian) — covered-by-automation per UAT | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DRP-01 | Plan 01 | Dropdown QuestionType di semua template (Universal L + legacy H), baris 2-1000 | SATISFIED | QTB.cs L2:L1000 + H2:H1000 CreateDataValidation().List; Tests 100-121 assert |
| DRP-02 | Plan 01 | Kolom Jawaban Benar / Opsi / Section TANPA dropdown | SATISFIED | Tests:153-156 TryGet=false pada H/B/I Universal; logic DV hanya di L+N+H |
| SKR-01 | Plan 01/02 | Kolom Skor (N/14) di Universal; WholeNumber DV 1-100; legacy TANPA Skor | SATISFIED | QTB.cs:38 header+118 DV; Tests:135-139; legacy 9-col tetap |
| SKR-02 | Plan 02 | ImportPackageQuestions parse Cell(14) → ScoreValue ganti hardcode 10 | SATISFIED | AAC:7431+7797; 0 grep ScoreValue=10, di import path |
| SKR-03 | Plan 02 | Validasi server int 1-100 NumberStyles.None (tolak desimal/negatif/non-angka) | SATISFIED | AAC:7518 int.TryParse(NumberStyles.None) + sv<1\|\|sv>100 |
| SKR-04 | Plan 02 | Atomic hard-reject: ≥1 invalid → TempData ScoreErrors + 0-write | SATISFIED | AAC:7522-7526 if scoreErrors.Any() → TempData + RedirectToAction sebelum write |
| VRF-01 | Plan 02 | Grade-lock test: import bobot non-uniform → Score==75 | SATISFIED | WeightedScoreImportTests.cs:269 Assert.Equal(75) via real action+GradeAndCompleteAsync |
| VRF-02 | Plan 02 | View render ScoreErrors alert-danger + header + dokumentasi Skor | SATISFIED | View:54-65+81+89 |
| VRF-03 | Plan 03 | UAT roundtrip: dropdown + numeric DV ter-verifikasi di Excel desktop nyata | SATISFIED | UAT human-approved 2026-06-30 (Steps A.1-B.5 PASS); C+D covered-by-automation |

---

### Anti-Patterns Found

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| `Helpers/QuestionTemplateBuilder.cs` | Tidak ada TODO/FIXME/placeholder/return-null | N/A | Bersih — implementasi lengkap |
| `Controllers/AssessmentAdminController.cs` import-path | `int scoreValue = 10;` di build-loop (baris 7782) | Info | BUKAN stub — ini nilai default D-08 (kosong=10), segera di-override dari rawScores jika ada nilai valid; hardcode `ScoreValue = 10,` (tanpa variable) = 0 match |
| `HcPortal.Tests/WeightedScoreImportTests.cs` | Tidak ada placeholder/mock — real SQLEXPRESS fixture | N/A | Integration test de-tautologis, bukan stub |

Tidak ada blocker anti-pattern ditemukan.

---

### Human Verification Required

Tidak ada item yang memerlukan verifikasi manusia lebih lanjut. UAT untuk VRF-03 (render dropdown + numeric DV di Excel desktop nyata) telah dilakukan dan di-approve oleh user pada 2026-06-30 (dokumentasi di `999.17-03-SUMMARY.md` §UAT Sign-Off, Steps A.1–B.5 PASS). Permukaan server (import bobot + atomic-reject) dikover oleh integration tests.

---

### Gaps Summary

Tidak ada gap. Semua 7 must-have VERIFIED dengan bukti langsung di kodebase:

1. Seam `QuestionTemplateBuilder.cs` terbentuk dan terhubung dengan benar ke controller.
2. DataValidation dropdown QuestionType ada di SEMUA template (Universal L12 + legacy H8), baris 2-1000.
3. Kolom Skor (N14) + WholeNumber DV eksklusif di Universal; legacy tetap 9-kolom (D-07).
4. Parsing Skor → ScoreValue terpasang di branch isNewFormat; hardcode 10 dihapus dari jalur import.
5. Atomic hard-reject berjalan: pass validasi mandiri mendahului semua write; TempData ScoreErrors tersurface di view.
6. GradingService tidak disentuh (D-11); normalisasi 0-100 dikunci oleh WeightedScoreImportTests Score==75.
7. Build 0-err; suite 1027/0/2 dikonfirmasi; 15 unit test DV read-back + grade-lock integration test hadir dengan assertion nyata.

migration=FALSE dikonfirmasi (`ScoreValue` kolom existing sejak v32.0; tidak ada schema change).

---

_Verified: 2026-06-30T09:00:00Z_
_Verifier: Claude (gsd-verifier)_
