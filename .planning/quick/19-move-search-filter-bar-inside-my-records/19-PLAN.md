# Quick Task 19: Move Search & Filter Bar inside My Records tab

## Task
Move the Search & Filter Bar from above the tab navigation into the My Records tab pane in `Views/CMP/Records.cshtml`.

## Tasks

### Task 1: Move Search & Filter Bar into My Records tab
- **files:** `Views/CMP/Records.cshtml`
- **action:** Cut the Search & Filter Bar div (lines 34-57) from its current position above the tabs, and paste it as the first child inside `#pane-myrecords` div (before the summary stats cards).
- **verify:** The search/filter bar only appears when My Records tab is active, not on Team View tab.
- **done:** Search & Filter Bar is inside My Records tab pane.
