---
phase: quick
plan: 260409-s71
type: execute
wave: 1
depends_on: []
files_modified: [Views/CMP/Index.cshtml]
autonomous: true
must_haves:
  truths:
    - "Halaman CMP/Index tidak menampilkan breadcrumb"
  artifacts:
    - path: "Views/CMP/Index.cshtml"
      provides: "CMP index tanpa breadcrumb"
  key_links: []
---

<objective>
Hapus elemen breadcrumb dari halaman CMP/Index.cshtml.

Purpose: Breadcrumb tidak diperlukan di halaman index CMP karena sudah jelas posisinya dari navigasi utama.
Output: Views/CMP/Index.cshtml tanpa blok breadcrumb (baris 6-11).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Views/CMP/Index.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hapus blok breadcrumb dari CMP/Index.cshtml</name>
  <files>Views/CMP/Index.cshtml</files>
  <action>
Hapus blok nav breadcrumb (baris 6-11) dari Views/CMP/Index.cshtml:

```html
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb mb-3">
            <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Home</a></li>
            <li class="breadcrumb-item active" aria-current="page">CMP</li>
        </ol>
    </nav>
```

Pastikan tidak ada baris kosong berlebih setelah penghapusan.
  </action>
  <verify>
    <automated>grep -c "breadcrumb" Views/CMP/Index.cshtml | grep "^0$"</automated>
  </verify>
  <done>Tidak ada elemen breadcrumb di CMP/Index.cshtml</done>
</task>

</tasks>

<verification>
- `grep "breadcrumb" Views/CMP/Index.cshtml` mengembalikan 0 hasil
- File tetap valid Razor syntax (tidak ada tag yang rusak)
</verification>

<success_criteria>
Halaman CMP/Index tidak lagi menampilkan breadcrumb navigation.
</success_criteria>

<output>
After completion, create `.planning/quick/260409-s71-hapus-breadcrumb-dari-cmp-index/260409-s71-SUMMARY.md`
</output>
