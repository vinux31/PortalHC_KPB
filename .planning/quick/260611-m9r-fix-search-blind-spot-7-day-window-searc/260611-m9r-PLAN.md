---
phase: quick-260611-m9r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Controllers/AssessmentAdminController.cs
  - HcPortal.Tests/AssessmentSearchWindowTests.cs
autonomous: true
requirements: [QUICK-260611-m9r]
must_haves:
  truths:
    - "Saat search non-empty di tab Assessment, assessment lama (>7 hari, mis. Post Test OJT) muncul di hasil"
    - "Saat search non-empty di AssessmentMonitoring, assessment lama (>7 hari) muncul di hasil"
    - "Saat search kosong, window 7 hari TETAP berlaku di kedua endpoint (default view tetap cepat — perilaku lama tak berubah)"
    - "Perilaku window+search identik di kedua method (satu sumber kebenaran lewat helper bersama)"
  artifacts:
    - path: "Controllers/AssessmentAdminController.cs"
      provides: "Static helper ApplySevenDayWindow + 2 call site (ManageAssessmentTab_Assessment, AssessmentMonitoring) + komentar 90-review diperbarui"
      contains: "ApplySevenDayWindow"
    - path: "HcPortal.Tests/AssessmentSearchWindowTests.cs"
      provides: "3 [Fact] menguji override-window: search kosong→window jalan, search non-empty→window skip"
      contains: "ApplySevenDayWindow"
  key_links:
    - from: "ManageAssessmentTab_Assessment"
      to: "ApplySevenDayWindow"
      via: "panggil helper menggantikan inline .Where window"
      pattern: "ApplySevenDayWindow\\("
    - from: "AssessmentMonitoring"
      to: "ApplySevenDayWindow"
      via: "panggil helper menggantikan inline .Where window"
      pattern: "ApplySevenDayWindow\\("
---

<objective>
Perbaiki blind-spot search 7-hari di dua endpoint admin: saat parameter `search` non-empty, window `(ExamWindowCloseDate ?? Schedule) >= sevenDaysAgo` di-SKIP supaya assessment lama (mis. Post Test OJT >7 hari) bisa ditemukan via search. Saat `search` kosong, window 7-hari tetap berlaku (default view cepat, perilaku lama tak berubah).

Sejalan dengan preseden CIL-02 (Phase 338): pencarian eksplisit dari user mengalahkan penyempitan default. Lihat juga lesson `338-01-SUMMARY.md:107` ("UAT WAJIB seed within window — historical data tidak muncul di view") — fix ini menghapus batasan itu untuk jalur search.

Purpose: Admin/HC bisa mencari assessment historis (>7 hari) lewat kotak search di Tab Assessment dan halaman Monitoring.
Output: Helper bersama `ApplySevenDayWindow` di `AssessmentAdminController`, dipasang di 2 method, komentar 90-review diperbarui, + 3 unit test [Fact].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<constraints>
- Branch: **ITHandoff** (current) — JANGAN buat branch baru.
- Migration: **none**. Zero perubahan schema.
- Bahasa Indonesia untuk komentar kode & commit message (conventional commits).
- JANGAN sentuh method lain yang akan dimodifikasi phase 363/366/367 (delete endpoints, CDPController). Scope KETAT: 2 window guard + komentar + test.
- Verifikasi lokal wajib (CLAUDE.md): `dotnet build` 0 error + full `dotnet test` green SEBELUM commit.
- UAT live-browser @5277 = tanggung jawab ORCHESTRATOR (executor tak punya browser MCP). Executor cukup sediakan UAT handoff note (lihat Task 2).
</constraints>

<interfaces>
<!-- Tipe & call site yang executor butuhkan — diekstrak dari codebase. Tidak perlu eksplorasi tambahan. -->

Model (Models/AssessmentSession.cs):
```csharp
public string Title { get; set; } = "";       // L13
public string Category { get; set; } = "";     // L16
public DateTime Schedule { get; set; }          // L18  (non-nullable)
public DateTime? ExamWindowCloseDate { get; set; } // L59 (nullable)
```

Call site 1 — Controllers/AssessmentAdminController.cs:116-122 (ManageAssessmentTab_Assessment):
```csharp
var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);
var managementQuery = _context.AssessmentSessions
    .AsNoTracking()
    .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)  // <-- window inline, DIGANTI helper
    .AsQueryable();
// ...search filter di L124-135 (Title/Category/User.FullName/NIP)
```

Call site 2 — Controllers/AssessmentAdminController.cs:2849-2856 (AssessmentMonitoring):
```csharp
// 7-day window — same as ManageAssessment
// 90-review: 7-day window is intentional for monitoring view; Abandoned sessions with no ExamWindowCloseDate
// fall back to Schedule for the window check and naturally age out after 7 days (expected behavior).
var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);
var query = _context.AssessmentSessions
    .Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)  // <-- window inline, DIGANTI helper
    .AsQueryable();
// ...search filter di L2859-2864 (Title/Category)
```

Pola test static-helper yang sudah ada (HcPortal.Tests/ManageAssessmentMedFixTests.cs):
```csharp
using HcPortal.Controllers;   // akses static helper controller
// memanggil AssessmentAdminController.DeriveUserStatus(...) / IsTrainingInitialState(...) langsung — TANPA construct controller
```

Pola factory AssessmentSession (HcPortal.Tests/WorkerDataServiceSearchTests.cs:40-45) — properti minimum yang aman:
```csharp
new AssessmentSession { Id = id, UserId = "u1", Status = "Closed", IsPassed = false,
    Title = "...", Schedule = ..., Score = 0, GenerateCertificate = false }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper ApplySevenDayWindow + wire 2 method + test (RED→GREEN)</name>
  <files>Controllers/AssessmentAdminController.cs, HcPortal.Tests/AssessmentSearchWindowTests.cs</files>
  <behavior>
    Helper static `ApplySevenDayWindow(IQueryable<AssessmentSession> query, string? search, DateTime cutoff)`:
    - Test 1 (Window_SearchKosong_SesiLama_Dikecualikan): query berisi 1 sesi Schedule 30 hari lalu, search=null → hasil KOSONG (window jalan, sesi lama tersaring).
    - Test 2 (Window_SearchAda_SesiLama_Disertakan): query berisi 1 sesi Schedule 30 hari lalu, search="OJT" → hasil 1 sesi (window di-SKIP karena search non-empty).
    - Test 3 (Window_SearchKosong_SesiBaru_Disertakan): query berisi 1 sesi Schedule kemarin, search=null → hasil 1 sesi (window jalan, sesi baru lolos).
    Catatan null-coalesce: sesi tanpa ExamWindowCloseDate jatuh ke Schedule (verifikasi via 1 sesi ExamWindowCloseDate=null Schedule 30 hari lalu di Test 1).
  </behavior>
  <action>
RED dulu, lalu GREEN. Implementasi:

1. **Tambah helper static** di `Controllers/AssessmentAdminController.cs` (dekat helper static lain `DeriveUserStatus`/`IsTrainingInitialState`, di dalam class AssessmentAdminController). Helper menerapkan window HANYA saat search kosong — search override → cari semua sesi termasuk >7 hari:
```csharp
// Quick fix (260611-m9r): window 7-hari hanya berlaku saat search KOSONG.
// Search eksplisit dari user mengalahkan penyempitan default (preseden CIL-02 Phase 338),
// supaya assessment lama (>7 hari, mis. Post Test OJT) bisa ditemukan via search.
public static IQueryable<AssessmentSession> ApplySevenDayWindow(
    IQueryable<AssessmentSession> query, string? search, DateTime cutoff)
{
    if (string.IsNullOrEmpty(search))
        return query.Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= cutoff);
    return query;
}
```

2. **Wire call site 1** (ManageAssessmentTab_Assessment, ~L116-122): hapus inline `.Where(a => (a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo)` dari chain, lalu setelah `.AsQueryable()` panggil helper:
```csharp
var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);
var managementQuery = _context.AssessmentSessions
    .AsNoTracking()
    .AsQueryable();
managementQuery = ApplySevenDayWindow(managementQuery, search, sevenDaysAgo);
```
   Biarkan blok search filter di L124-135 apa adanya (jalan SETELAH window guard). CIL-01 counter (L204-207) & CIL-02 hide-Closed (L209-214) JANGAN diubah — perilaku counter dengan search non-empty = mengikuti hasil search (perilaku lama, tetap).

3. **Wire call site 2** (AssessmentMonitoring, ~L2849-2856): hapus inline `.Where(... >= sevenDaysAgo)` dan panggil helper, SERTA **perbarui komentar 90-review** supaya mencerminkan perilaku baru:
```csharp
// 7-day window — same as ManageAssessment.
// Quick fix (260611-m9r): window HANYA saat search kosong. Saat search non-empty,
// window di-skip supaya assessment lama (>7 hari) bisa ditemukan via search.
// Abandoned sessions tanpa ExamWindowCloseDate fallback ke Schedule untuk cek window
// (relevan hanya saat search kosong).
var sevenDaysAgo = DateTime.UtcNow.AddDays(-7);
var query = _context.AssessmentSessions.AsQueryable();
query = ApplySevenDayWindow(query, search, sevenDaysAgo);
```
   Biarkan blok search filter di L2859-2864 apa adanya.

4. **Buat test** `HcPortal.Tests/AssessmentSearchWindowTests.cs`. Pakai pola static-helper (mirip ManageAssessmentMedFixTests) — TANPA construct controller, TANPA DbContext: bangun data via `new[] { ... }.AsQueryable()` (LINQ-to-Objects, null-coalesce didukung penuh). Header file: `using System; using System.Linq; using HcPortal.Controllers; using HcPortal.Models; using Xunit;`. Factory sesi mirror pola WorkerDataServiceSearchTests:40-45 (set Id/UserId/Status/IsPassed/Title/Schedule/Score/GenerateCertificate; set ExamWindowCloseDate=null untuk uji fallback). cutoff = `DateTime.UtcNow.AddDays(-7)`. Implement 3 [Fact] sesuai &lt;behavior&gt;.

5. Jangan ubah method/endpoint lain. Scope KETAT: helper baru + 2 call site + 1 komentar + 1 file test.
  </action>
  <verify>
    <automated>dotnet build; dotnet test --filter "FullyQualifiedName~AssessmentSearchWindowTests"</automated>
  </verify>
  <done>Build 0 error; 3 [Fact] AssessmentSearchWindowTests green; kedua method memanggil ApplySevenDayWindow; tak ada lagi inline `.Where(... >= sevenDaysAgo)` di 2 method tsb; komentar 90-review AssessmentMonitoring diperbarui.</done>
</task>

<task type="auto">
  <name>Task 2: Gate full-suite + UAT handoff note</name>
  <files>Controllers/AssessmentAdminController.cs</files>
  <action>
Final verification gate + siapkan handoff untuk orchestrator (yang akan jalankan UAT live-browser):

1. Jalankan **full suite**: `dotnet build` 0 error + `dotnet test` (SEMUA test, bukan filter) GREEN. Tidak ada regresi (suite eksisting ~Phase 361 harus tetap hijau). Kalau ada test gagal, perbaiki SEBELUM lanjut.
2. Grep sanity: pastikan tak ada sisa inline window guard di dua method (`(a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo` hanya muncul di dalam body helper `ApplySevenDayWindow`, tidak di ManageAssessmentTab_Assessment / AssessmentMonitoring).
3. **UAT handoff note** — tulis ke SUMMARY (section khusus) langkah yang akan diverifikasi orchestrator via Playwright @ http://localhost:5277 (AD lokal: `Authentication__UseActiveDirectory=false dotnet run`):
   - **Skenario A — Tab Assessment:** buka `/Admin/ManageAssessment` → tab "Assessment". Pastikan ada assessment lama (>7 hari, mis. judul "Post Test OJT ..."). Tanpa search, sesi lama TIDAK tampil (window jalan). Ketik judul sesi lama di kotak search → sesi lama MUNCUL.
   - **Skenario B — Monitoring:** buka `/Admin/AssessmentMonitoring?search=<judul-lama>` → grup assessment lama (>7 hari) MUNCUL. Tanpa `search`, grup lama tidak tampil.
   - **Catatan seed:** kalau DB lokal tak punya sesi >7 hari, orchestrator/HUMAN seed sesuai SEED_WORKFLOW (temporary + local-only, snapshot dulu, restore + tandai journal `cleaned` setelah UAT).
4. Migration flag = FALSE (zero schema change) — catat di SUMMARY untuk notifikasi IT.
  </action>
  <verify>
    <automated>dotnet build; dotnet test</automated>
  </verify>
  <done>Full `dotnet test` suite green + build 0 error; UAT handoff note (Skenario A & B + URL + langkah + catatan seed) tertulis di SUMMARY; migration=FALSE dicatat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→admin endpoint | Parameter `search` dari Admin/HC menyeberang ke query EF (kedua endpoint sudah `[Authorize(Roles = "Admin, HC")]`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m9r-01 | Information Disclosure | ManageAssessmentTab_Assessment / AssessmentMonitoring | accept | Skip window saat search TIDAK memperluas otorisasi — Admin/HC sudah berhak melihat SEMUA sesi (window murni perf/UX default, bukan kontrol akses). Tak ada data baru yang ter-ekspos. |
| T-m9r-02 | Denial of Service | search non-empty → full-table scan | accept | Search `Contains` + paging (PaginationHelper) tetap menyempitkan; endpoint admin-only low-volume; query AsNoTracking. Risiko rendah; tak ada perubahan disposisi. |
| T-m9r-03 | Tampering/Injection | parameter `search` ke `.Where(...Contains)` | mitigate | EF Core parameterize otomatis (LINQ → SQL parameter), bukan string concat — sudah aman pada pola eksisting; helper baru tidak menyentuh jalur search itu sendiri. |
</threat_model>

<verification>
- `dotnet build` 0 error (tak ada warning baru yang jadi error).
- `dotnet test` full suite green (3 [Fact] baru + suite eksisting tanpa regresi).
- Grep: `ApplySevenDayWindow(` muncul di kedua method; `(a.ExamWindowCloseDate ?? a.Schedule) >= sevenDaysAgo` hanya di body helper.
- Komentar 90-review di AssessmentMonitoring sudah mencerminkan perilaku baru (window hanya saat search kosong).
</verification>

<success_criteria>
- Search non-empty menemukan assessment lama (>7 hari) di Tab Assessment & Monitoring (terbukti via unit test Test 2; dikonfirmasi orchestrator via UAT Skenario A/B).
- Search kosong mempertahankan window 7-hari di kedua endpoint (Test 1 & 3; default view cepat tak berubah).
- Logika window+search identik di kedua method (satu helper bersama).
- Zero perubahan schema (migration=FALSE). Branch tetap ITHandoff.
- Commit conventional Bahasa Indonesia, mis. `fix(quick): search override window 7 hari di tab Assessment + Monitoring`.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-m9r-fix-search-blind-spot-7-day-window-searc/260611-m9r-SUMMARY.md` (sertakan UAT handoff note Skenario A & B + flag migration=FALSE).
</output>
