---
milestone: v32.3
audited: 2026-06-21
status: gaps_found
scores:
  requirements: 20/24 satisfied (4 partial — QA-01..04 verification-artifact gap; PSU-01/05 INT-01 code gap)
  phases: 5/6 verified (404 missing VERIFICATION.md)
  integration: 4/4 flows verified end-to-end
  flows: 4/4 complete (0 broken)
gaps:
  requirements:
    - id: "PSU-01"
      status: "partial"
      phase: "401 (escaped scope) — surfaced by 404 integration audit"
      verification_status: "passed (401) but residual fallback remains"
      evidence: "ProtonDataController.cs:524 Phase-129 deliverable auto-sync retains `?? userUnits.GetValueOrDefault(a.CoacheeId)` (scalar User.Unit) fallback — the lone remaining `?? User.Unit` resolver. Flagged out-of-scope by 401-06 SUMMARY, not addressed in 404. (INT-01)"
    - id: "PSU-05"
      status: "partial"
      phase: "401 / 404"
      verification_status: "passed but residual"
      evidence: "Same site (ProtonDataController.cs:524) silently resolves to primary instead of skip+audit-warn when AssignmentUnit empty (INT-01). Also INT-02: 2 HTTP gate-path smoke stubs still [Fact(Skip)]."
    - id: "QA-01"
      status: "partial"
      phase: "404"
      verification_status: "missing (no 404-VERIFICATION.md)"
      evidence: "DB-layer SQL-real coverage complete (MultiUnitSqlFixture + UnitMembership/SingleActive tests green); but (a) no 404-VERIFICATION.md artifact, (b) 2 HTTP end-to-end smoke stubs (ProtonUnitResolveTests:70, UnitUnresolvedAuditTests:73) remain [Fact(Skip)] empty (INT-02)."
    - id: "QA-02"
      status: "partial"
      phase: "404"
      verification_status: "missing (no 404-VERIFICATION.md)"
      evidence: "Browser UAT 3/3 PASS @5270 (multi-unit display, coach cross-unit view, PROTON sequential single-active + cert histori; SEED_JOURNAL cleaned) — but no formal 404-VERIFICATION.md."
    - id: "QA-03"
      status: "partial"
      phase: "404"
      verification_status: "missing (no 404-VERIFICATION.md)"
      evidence: "SingleActiveInvariantSqlTests 3 Facts green (mapping DbUpdateException + reactivate + PTA-bypass COUNT) + CrossUnitAssignSqlTests anchor green — but no 404-VERIFICATION.md."
    - id: "QA-04"
      status: "partial"
      phase: "404"
      verification_status: "missing (no 404-VERIFICATION.md)"
      evidence: "UnitMembershipInvariantSqlTests 4 Facts green (AssignmentUnit∈UserUnits + B-06 + 1:1 + one-primary) — but no 404-VERIFICATION.md."
  integration:
    - id: "INT-01"
      severity: medium
      from: "Phase 401 PROTON resolution"
      to: "ProtonDataController Phase-129 auto-sync"
      reqs: ["PSU-01", "PSU-05"]
      issue: "ProtonDataController.cs:524 retains `?? User.Unit` fallback in new-deliverable auto-sync; orphaned-AssignmentUnit coachee → deliverable silently scoped to primary instead of skip+warn."
    - id: "INT-02"
      severity: low
      reqs: ["QA-01", "PSU-05"]
      issue: "2 HTTP gate-path smoke tests still [Fact(Skip)] empty after 404 close (ProtonUnitResolveTests:70, UnitUnresolvedAuditTests:73) — the suite's 2 skips. Gate covered at helper-level + grep + browser UAT, not HTTP-action level."
  artifacts:
    - phase: "404"
      issue: "No 404-VERIFICATION.md (unverified phase per audit gate)."
    - phase: "404"
      issue: "No 404-SECURITY.md (security gate not run)."
    - phase: "404"
      issue: "404-VALIDATION.md nyquist_compliant:false (planning draft, not finalized — though 404 IS the validation phase; tests green)."
  traceability:
    - phase: "402"
      issue: "402-01..04 SUMMARY frontmatter lacks `requirements-completed` (CXU-01..05). Covered by 402-VERIFICATION passed 9/9 + REQUIREMENTS [x] Complete + SUMMARY bodies — frontmatter hygiene only, not a coverage gap."
    - issue: "QA-01..04 checkboxes still `[ ]` in REQUIREMENTS.md (404 just finished) — tick to `[x]` on close."
tech_debt:
  - phase: 403
    items:
      - "999.11 — EditOrganizationUnit cascade (rename/reparent) writes no AuditLog (asymmetry vs Delete). Backlogged."
nyquist:
  compliant_phases: ["399", "400", "401", "402", "403"]
  partial_phases: ["404"]
  missing_phases: []
  overall: "5/6 COMPLIANT; 404 VALIDATION draft (nyquist_compliant:false) — finalize on close."
---

# Milestone v32.3 Audit — Akun Multi-Unit (1 Bagian)

**Status:** gaps_found (closeable — work is done; gaps are close-out artifacts + 1 medium code finding)
**Audited:** 2026-06-21
**Scope:** Phases 399–404 (6 phases, 24 requirements). 999.x = backlog (out of scope).

## Executive Summary

The milestone is **functionally complete and well-tested** — full suite **562 passed / 0 failed / 2 skipped**, browser UAT **3/3 PASS**, integration **4/4 flows verified end-to-end** (0 orphaned exports, 0 broken flows, 0 unprotected routes). The primary-mirror contract and AssignmentUnit flow (402→401→404) are correctly wired across all phases.

Audit returns **gaps_found** for honesty, but every gap is closeable without new feature work:

### 1. Phase 404 close-out artifacts missing (not blockers to functionality)
- No `404-VERIFICATION.md` (execute ran UAT inline; formal verifier artifact never written).
- No `404-SECURITY.md` (user chose audit before secure 404).
- `404-VALIDATION.md` still `nyquist_compliant:false` (planning draft). Note: Phase 404 *is* the validation phase — its SQL-real suite IS the nyquist coverage; the draft just needs finalizing.

### 2. INT-01 — genuine MEDIUM code finding (PSU-01/PSU-05)
`ProtonDataController.cs:524`, inside the Phase-129 "auto-sync new deliverables to matching-unit assignments" block, retains the **lone remaining `?? User.Unit` fallback**:
```csharp
var resolvedUnit = (mappingUnits.FirstOrDefault(m => m.CoacheeId == a.CoacheeId)?.AssignmentUnit
                    ?? userUnits.GetValueOrDefault(a.CoacheeId))?.Trim() ?? "";
```
For a coachee whose active mapping has an empty/orphaned `AssignmentUnit`, a newly-added deliverable would be silently scoped to the primary `User.Unit` instead of being skipped + audit-warned (PSU-05). 401-06 SUMMARY flagged this out-of-scope; it was not folded into any later plan. Narrow trigger (admin adds deliverables to a track AND a coachee on it has empty AssignmentUnit), but a real PSU-01 deviation.

### 3. INT-02 — LOW (QA-01/PSU-05)
Two HTTP gate-path smoke stubs remain `[Fact(Skip)]` empty (`ProtonUnitResolveTests:70`, `UnitUnresolvedAuditTests:73`) — the suite's 2 skips. The gate is covered at helper-level + grep guard + browser UAT; the HTTP-action smoke promised under QA-01 is absent.

### 4. Traceability hygiene
- 402-01..04 SUMMARYs lack `requirements-completed` frontmatter for CXU-01..05 (covered by VERIFICATION 9/9 + REQUIREMENTS `[x]`).
- QA-01..04 checkboxes still `[ ]` in REQUIREMENTS.md.

## Requirements Coverage (24)

| Group | REQs | Status |
|-------|------|--------|
| MU (399/400) | MU-01..07 (7) | ✓ satisfied |
| PSU (401) | PSU-01,02,03,04,05,07 (6) | ◆ PSU-01/05 partial (INT-01); rest satisfied |
| CXU (402) | CXU-01..05 (5) | ✓ satisfied (frontmatter hygiene only) |
| ORG (403) | ORG-01,02 (2) | ✓ satisfied |
| QA (404) | QA-01..04 (4) | ◆ partial (verification artifact + INT-02) |

## Integration: 4/4 flows verified

1. Create multi-unit worker (399) → set-aware listing (400) — COMPLETE
2. Cross-unit coach assign (402) → PROTON sequential T1@X→T2@Y (401) — COMPLETE (INT-01 narrow gap in auto-sync sub-path)
3. Coach multi-unit CDP union/narrow (402-03) — COMPLETE
4. Org rename cascade + reparent hard-block (403) — COMPLETE

## Remediation Path (recommended)

1. **Fix INT-01** (small, real): `ProtonDataController.cs:524` — drop the `?? userUnits...` fallback; skip + LogWarning when AssignmentUnit empty (mirror PSU-05 pattern). Production change → re-trigger review+secure on that file. Best as decimal phase **404.1** or a guarded quick fix.
2. **Close 404 gates:** `/gsd-verify-work 404` (or write 404-VERIFICATION.md), `/gsd-secure-phase 404` (test/docs surface — quick), finalize `404-VALIDATION.md`.
3. **(Optional) INT-02:** implement the 2 HTTP smoke stubs, or accept as documented (helper+grep+UAT cover the behavior).
4. **Traceability:** add `requirements-completed` to 402 SUMMARYs; tick QA-01..04 `[x]` in REQUIREMENTS.md.
5. Re-audit → expect **passed** → `/gsd-complete-milestone v32.3`.

**Alternatively** (if INT-01 accepted as narrow tech-debt): close 404 gates + tick traceability + record INT-01/INT-02 in backlog → complete with documented debt.
