namespace HcPortal.Data
{
    /// <summary>
    /// Class untuk seeding data master.
    /// Note: SeedSampleTrainingRecordsAsync removed — sample training records only needed for dev/QA.
    /// </summary>
    public static class SeedMasterData
    {
        // Removed: SeedSampleTrainingRecordsAsync — was seeding dummy training records for 3 users.
        // In production, training records are entered manually by users via CMP/Admin.
    }
}
